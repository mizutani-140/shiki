{
  "command": "codex exec - <.shiki/handoffs/T-20260807T073307221254Z-2d22d654-task.md>",
  "created_at": "2026-08-07T08:29:59+00:00",
  "goal_id": "G-20260807T073307219752Z-1ee1ed68",
  "id": "EXEC-20260807T082959205711Z-3677992b",
  "returncode": 0,
  "stderr": "2026-08-07T08:03:10.357907Z ERROR codex_models_manager::cache: failed to load models cache: missing field `base_instructions` at line 94 column 5\n2026-08-07T08:03:10.876883Z  WARN codex_core_skills::loader: ignoring interface.icon_small: icon path with '..' must resolve under plugin assets/\n2026-08-07T08:03:10.876895Z  WARN codex_core_skills::loader: ignoring interface.icon_large: icon path with '..' must resolve under plugin assets/\n2026-08-07T08:03:10.877204Z  WARN codex_core_plugins::manifest: ignoring interface.defaultPrompt: maximum of 3 prompts is supported path=file:///Users/kio.mizutani/.codex/plugins/cache/openai-primary-runtime/template-creator/26.805.11740/.codex-plugin/plugin.json\n2026-08-07T08:03:10.877877Z  WARN codex_core_plugins::manifest: ignoring interface.defaultPrompt: maximum of 3 prompts is supported path=file:///Users/kio.mizutani/.codex/plugins/cache/openai-primary-runtime/template-creator/26.805.11740/.codex-plugin/plugin.json\n2026-08-07T08:03:11.190872Z  WARN codex_core_skills::loader: ignoring interface.icon_small: icon path with '..' must resolve under plugin assets/\n2026-08-07T08:03:11.190884Z  WARN codex_core_skills::loader: ignoring interface.icon_large: icon path with '..' must resolve under plugin assets/\n2026-08-07T08:03:11.191090Z  WARN codex_core_plugins::manifest: ignoring interface.defaultPrompt: maximum of 3 prompts is supported path=file:///Users/kio.mizutani/.codex/plugins/cache/openai-primary-runtime/template-creator/26.805.11740/.codex-plugin/plugin.json\n2026-08-07T08:03:11.191668Z  WARN codex_core_plugins::manifest: ignoring interface.defaultPrompt: maximum of 3 prompts is supported path=file:///Users/kio.mizutani/.codex/plugins/cache/openai-primary-runtime/template-creator/26.805.11740/.codex-plugin/plugin.json\nOpenAI Codex v0.144.6\n--------\nworkdir: /private/tmp/shiki-adr-collision.pa0KwY/.worktrees/shiki-t-20260807t073307221254z-2d22d654-introduc\nmodel: gpt-5.6-sol\nprovider: openai\napproval: on-request\nsandbox: workspace-write [workdir, /tmp, $TMPDIR]\nreasoning effort: xhigh\nreasoning summaries: none\nsession id: 019fdb3f-5d73-7231-a61d-76190d9b908c\n--------\nuser\n# Codex Task Handoff: T-20260807T073307221254Z-2d22d654\n\nGoal: G-20260807T073307219752Z-1ee1ed68\nTask: T-20260807T073307221254Z-2d22d654\nRuntime: codex\nBranch: shiki/t-20260807t073307221254z-2d22d654-introduce-sadr-and-migrate-installs-without-touc\n\n## Execution Protocol (the Shiki loop owns delivery)\n- Implement ONLY by editing files in this worktree to satisfy the Scope and Acceptance Checks below.\n- Do NOT run `git commit`, `git push`, `git checkout`/`git switch`, or any `gh` command.\n- Do NOT create, update, comment on, or merge a pull request.\n- The Shiki goal loop owns commit, push, PR creation (with the required MergeGate PR body) and merge; opening your own commit or PR breaks the loop's create_pr state transition and fails MergeGate.\n- Stay strictly within the declared Locks below; touch no files outside them.\n\n## Scope\nRename Shiki platform decision records to SADR-0001 through SADR-0018; add SADR-0019; update current citations, packaging metadata, documentation, workflows, prompts, repo-local skill guidance, and the reference validator for separate ADR/SADR namespaces; add an installer preflight that inventories legacy numeric Shiki ADR paths and authorizes deletion only from matching existing install-stamp path digests before any write; then apply authorized cleanup, install SADRs, refresh the stamp, and prove fresh-install plus CubeShare-shaped collision-upgrade behavior with TDD.\n\n## Acceptance Checks\n- The platform tree has unique SADR-0001 through SADR-0019, no legacy numeric Shiki platform ADR file, and every live SADR citation resolves exactly once.\n- The validator resolves target ADR NNNN to docs/adr/NNNN-*.md and platform SADR NNNN to docs/adr/SADR-NNNN-*.md; fixtures prove both namespaces may reuse a number.\n- Current platform references in root governance docs, docs, scripts, workflows/prompts, .codex/skills/shiki/SKILL.md, and packaging use SADR or explicit generic target-ADR examples. Historical .shiki records remain unchanged except this Goal's lifecycle evidence.\n- A fresh-install fixture with product ADRs 0013-0018 preserves their bytes, installs SADR-0001 through SADR-0019, stamps SADR paths, and validates.\n- A forced-upgrade fixture with product collisions plus matching stamped legacy Shiki ADRs deletes only legacy platform paths, preserves product ADR bytes, installs SADRs, and removes legacy paths from the refreshed stamp.\n- Parameterized absent, unreadable/malformed, missing-digest, and digest-mismatch cases report all blockers before ShikiError; recursive snapshots prove no filesystem byte changed.\n- No legacy path means an absent stamp does not block; a non-force install does not silently delete existing files.\n- Red-green-refactor and a structured code-review verdict are recorded, with all blocking findings repaired and re-reviewed.\n- python3 -m unittest discover -s tests, control-plane and adversarial shell suites, validate_shiki, and fresh-install/upgrade tests pass locally and in CI on the exact head.\n\n## Locks\n- path:.codex/**\n- path:.github/**\n- path:.shiki/**\n- path:AGENTS.md\n- path:CLAUDE.md\n- path:CONTEXT.md\n- path:CONTRIBUTING.md\n- path:SYSTEM_PROMPT.md\n- path:docs/**\n- path:pyproject.toml\n- path:scripts/**\n- path:tests/**\n\n## Required Skills\n- diagnose\n- tdd\n- code-review\n\n## Distilled Rules\n\nnone applicable\n\nwarning: Under-development features enabled: chronicle. Under-development features are incomplete and may behave unpredictably. To suppress this warning, set `suppress_unstable_features_warning = true` in /Users/kio.mizutani/.codex/config.toml.\n2026-08-07T08:03:11.603249Z  WARN codex_rmcp_client::stdio_server_launcher: Failed to terminate MCP process group 17763: Operation not permitted (os error 1)\n2026-08-07T08:03:11.691037Z  WARN codex_rmcp_client::stdio_server_launcher: Failed to terminate MCP process group 17833: Operation not permitted (os error 1)\n2026-08-07T08:03:11.999863Z  WARN rmcp::transport::auth: Token refresh not possible, re-authorization required. error=OAuth token refresh failed: Server returned error response: invalid_grant: Grant not found\n2026-08-07T08:03:11.999923Z ERROR rmcp::transport::worker: worker quit with fatal: Transport channel closed, when Auth(AuthorizationRequired)\nwarning: Skill descriptions were shortened to fit the 2% skills context budget. Codex can still see every skill, but some descriptions are shorter. Disable unused skills or plugins to leave more room for the rest.\nhook: SessionStart\nhook: SessionStart Completed\ncodex\nI\u2019m using the required `diagnose` and `tdd` skills first, then the repository\u2019s `code-review` gate after green. I\u2019m also invoking the `shiki` skill because this is a Shiki task handoff. I\u2019ll keep every edit inside the declared locks and leave commit/PR delivery to the Shiki loop. The catalog does not expose `code-review`, so I\u2019ll check the repository-configured skill location and report it as a blocker only if it is genuinely unavailable.\n2026-08-07T08:03:24.824665Z  WARN codex_otel::events::session_telemetry: metrics counter [codex.skill.injected] failed: tag value contains invalid characters: mattpocock-skills:tdd\n2026-08-07T08:03:24.824868Z  WARN codex_otel::events::session_telemetry: metrics counter [codex.skill.injected] failed: tag value contains invalid characters: mattpocock-skills:diagnose\nexec\n/bin/zsh -lc \"sed -n '1,240p' '/Users/kio.mizutani/skills/skills/engineering/tdd/SKILL.md'\" in /private/tmp/shiki-adr-collision.pa0KwY/.worktrees/shiki-t-20260807t073307221254z-2d22d654-introduc\n succeeded in 0ms:\n---\nname: tdd\ndescription: Test-driven development with red-green-refactor loop. Use when user wants to build features or fix bugs using TDD, mentions \"red-green-refactor\", wants integration tests, or asks for test-first development.\n---\n\n# Test-Driven Development\n\n## Philosophy\n\n**Core principle**: Tests should verify behavior through public interfaces, not implementation details. Code can change entirely; tests shouldn't.\n\n**Good tests** are integration-style: they exercise real code paths through public APIs. They describe _what_ the system does, not _how_ it does it. A good test reads like a specification - \"user can checkout with valid cart\" tells you exactly what capability exists. These tests survive refactors because they don't care about internal structure.\n\n**Bad tests** are coupled to implementation. They mock internal collaborators, test private methods, or verify through external means (like querying a database directly instead of using the interface). The warning sign: your test breaks when you refactor, but behavior hasn't changed. If you rename an internal function and tests fail, those tests were testing implementation, not behavior.\n\nSee [tests.md](tests.md) for examples and [mocking.md](mocking.md) for mocking guidelines.\n\n## Anti-Pattern: Horizontal Slices\n\n**DO NOT write all tests first, then all implementation.** This is \"horizontal slicing\" - treating RED as \"write all tests\" and GREEN as \"write all code.\"\n\nThis produces **crap tests**:\n\n- Tests written in bulk test _imagined_ behavior, not _actual_ behavior\n- You end up testing the _shape_ of things (data structures, function signatures) rather than user-facing behavior\n- Tests become insensitive to real changes - they pass when behavior breaks, fail when behavior is fine\n- You outrun your headlights, committing to test structure before understanding the implementation\n\n**Correct approach**: Vertical slices via tracer bullets. One test \u2192 one implementation \u2192 repeat. Each test responds to what you learned from the previous cycle. Because you just wrote the code, you know exactly what behavior matters and how to verify it.\n\n```\nWRONG (horizontal):\n  RED:   test1, test2, test3, test4, test5\n  GREEN: impl1, impl2, impl3, impl4, impl5\n\nRIGHT (vertical):\n  RED\u2192GREEN: test1\u2192impl1\n  RED\u2192GREEN: test2\u2192impl2\n  RED\u2192GREEN: test3\u2192impl3\n  ...\n```\n\n## Workflow\n\n### 1. Planning\n\nWhen exploring the codebase, use the project's domain glossary so that test names and interface vocabulary match the project's language, and respect ADRs in the area you're touching.\n\nBefore writing any code:\n\n- [ ] Confirm with user what interface changes are needed\n- [ ] Confirm with user which behaviors to test (prioritize)\n- [ ] Identify opportunities for [deep modules](deep-modules.md) (small interface, deep implementation)\n- [ ] Design interfaces for [testability](interface-design.md)\n- [ ] List the behaviors to test (not implementation steps)\n- [ ] Get user approval on the plan\n\nAsk: \"What should the public interface look like? Which behaviors are most important to test?\"\n\n**You can't test everything.** Confirm with the user exactly which behaviors matter most. Focus testing effort on critical paths and complex logic, not every possible edge case.\n\n### 2. Tracer Bullet\n\nWrite ONE test that confirms ONE thing about the system:\n\n```\nRED:   Write test for first behavior \u2192 test fails\nGREEN: Write minimal code to pass \u2192 test passes\n```\n\nThis is your tracer bullet - proves the path works end-to-end.\n\n### 3. Incremental Loop\n\nFor each remaining behavior:\n\n```\nRED:   Write next test \u2192 fails\nGREEN: Minimal code to pass \u2192 passes\n```\n\nRules:\n\n- One test at a time\n- Only enough code to pass current test\n- Don't anticipate future tests\n- Keep tests focused on observable behavior\n\n### 4. Refactor\n\nAfter all tests pass, look for [refactor candidates](refactoring.md):\n\n- [ ] Extract duplication\n- [ ] Deepen modules (move complexity behind simple interfaces)\n- [ ] Apply SOLID principles where natural\n- [ ] Consider what new code reveals about existing code\n- [ ] Run tests after each refactor step\n\n**Never refactor while RED.** Get to GREEN first.\n\n## Checklist Per Cycle\n\n```\n[ ] Test describes behavior, not implementation\n[ ] Test uses public interface only\n[ ] Test would survive internal refactor\n[ ] Code is minimal for this test\n[ ] No speculative features added\n```\n\nexec\n/bin/zsh -lc \"sed -n '1,240p' '/private/tmp/shiki-adr-collision.pa0KwY/.worktrees/shiki-t-20260807t073307221254z-2d22d654-introduc/.codex/skills/shiki/SKILL.md'\" in /private/tmp/shiki-adr-collision.pa0KwY/.worktrees/shiki-t-20260807t073307221254z-2d22d654-introduc\n succeeded in 0ms:\n---\nname: shiki\ndescription: Use when the user invokes Shiki, /shiki, or asks to run the GitHub-first agentic engineering control plane for Goal Seek, Context and Impact, Task DAG, Codex implementation, CCA completion judgment, MergeGate, or bounded repair loops.\n---\n\n# Shiki\n\nShiki is the user's GitHub-first, runtime-agnostic control plane for agentic\nengineering.\n\n## Canonical Source Of Truth\n\n<!-- shiki-source-of-truth:start -->\n1. GitHub Issues, Pull Requests, Checks, Reviews, comments, and merge evidence are the operational source of truth.\n2. The repository-local `.shiki/` mirror records Goals, PRDs, plans, Task DAGs, contracts, locks, ledger entries, CCA verdicts, repair packets, reports, and handoffs.\n3. `CONTEXT.md` defines Shiki domain language and glossary decisions.\n4. `docs/adr/` records hard-to-reverse platform decisions.\n5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n<!-- shiki-source-of-truth:end -->\n\nCodex CLI does not register this skill as a custom `/shiki` slash command. When\nthe user types `/shiki` in Codex CLI and gets \"Unrecognized command\", explain\nthat this is expected: invoke Shiki in Codex with natural language such as\n\"Shiki: create a new GitHub-backed target repo ...\" or run the shell command\n`shiki start ...`.\n\n## Start\n\nRun:\n\n```bash\nshiki status\nshiki doctor\n```\n\n`shiki status` reports the installed Shiki platform/template root and default\nconfig. Its `config.repo` value is not automatically the requested Target\nRepository. Do not route work to `/Users/kio.mizutani/shiki` or\n`mizutani-140/shiki` unless the user explicitly asks to work on the Shiki\nplatform repo itself.\n\nUse `shiki doctor` to distinguish Shiki availability from Agent Runtime\nauthentication. If Claude Code reports `Please run /login` or `API Error: 401\nInvalid authentication credentials`, the Claude Code adapter cannot run `/shiki`\nuntil `claude auth login` or `/login` succeeds. Do not block the Codex path on\nClaude auth; use `shiki start` from Codex or a terminal when Codex/GitHub are\nready.\n\nWhen `/shiki` or Shiki is invoked without a clear target, first establish\nwhether the user wants a new GitHub-backed Target Repository or an existing\nrepository. For new repo requests, collect the target path and GitHub slug before\ninspecting repo contents.\n\nThen inspect the selected target repository's `AGENTS.md`, `CLAUDE.md`,\n`CONTEXT.md`, `.shiki/`, `docs/agents/`, and open PR/issue state before changing\nfiles.\n\nIf Shiki is not installed in the target repository, do not give the user a\nmanual sequence. Ask for the missing repo and Goal values one question at a\ntime, then run the one-command entrypoint:\n\n```bash\nshiki start TARGET --repo OWNER/NAME --goal \"...\" --outcome \"...\"\n```\n\nThe default engineering Skill Gate directory is\n`/Users/kio.mizutani/Documents/lead-os/skills/engineering` when present. The\nstart record, plan, and handoff must preserve the selected skills directory.\n\n## Responsibilities\n\n- Claude Code implements and repairs by default (`shiki runner claude`, ADR 0008); Codex implements and repairs tasks explicitly assigned to `codex`.\n- Claude Code Action can act as GitHub-side CCA or reviewer.\n- CCA judges completion.\n- MergeGate authorizes state transitions and merge readiness.\n- GitHub branch protection is the hard gate.\n\n## Rules\n\n- For non-trivial goals, enter through `grill-with-docs`. In a bootstrapped Target Repository the default flow is Goal mode: Requirements Definition (grill dialogue + Context & Impact + PRD) ends with the operator's explicit approval = Spec Freeze, recorded as the plan's `spec_freeze` block (`status: frozen`, `approved_by`, `source`). Plans without it are rejected by `shiki plan ingest`/`run`. Codex satisfies the Context & Impact sweep requirement with an equivalent recorded exploration (the Claude Workflow tool is not available in Codex).\n- After Spec Freeze, scope-moving discoveries pause the task and return to the operator as a Spec Amendment; non-scope-moving interpretations are recorded in the Assumption Log.\n- `/shiki` should guide the user through missing repo/Goal answers one question at a time and then run `shiki start`; direct `init`, `plan`, and `run` calls are lower-level fallback commands.\n- Convert the settled `grill-with-docs` result into a machine-readable plan and run it with `shiki plan ingest` followed by `shiki run`.\n- For unattended execution, queue the plan with `shiki daemon enqueue-plan` and process it with `shiki daemon run`.\n- For headless runtime integration, use `shiki runner next` and `shiki runner execute` to pick up ready tasks and record execution evidence.\n- When a ready task is assigned to claude-code (the default), run the implementation adapter with `shiki runner claude --target TARGET --task-id T-XXXX`. When it is assigned to Codex, run `shiki runner codex --target TARGET --task-id T-XXXX` instead of handing the user a manual command. Both create or reuse the task worktree, invoke the headless runtime (`claude -p` or `codex exec`) with the handoff, and record runner evidence. Stop for user input only when the assigned runtime's auth/tooling is missing, dispatch is blocked, or Guardian approval is required.\n- Use Context and Impact before implementation.\n- Keep tasks as vertical slices with explicit locks and verification.\n- Use TDD for implementation work when behavior changes.\n- Do not call implementation complete until GitHub evidence, CCA, and MergeGate support it.\n- Do not use `shiki install-target` unless the user explicitly asks for local-only template copying.\n- Do not bypass branch protection or use admin merge.\n- If a workflow change needs a bootstrap exception, ask for explicit Guardian approval first.\n\n## Commands\n\n- `shiki install-global`\n- `shiki start /path/to/repo --repo OWNER/REPO --goal \"...\" --outcome \"...\"`\n- `shiki init /path/to/repo --repo OWNER/REPO`\n- `shiki preflight --require-github`\n- `shiki plan guide --prompt \"...\"`\n- `shiki plan ingest --plan-file PLAN.json`\n- `shiki run --plan P-0001`\n- `shiki daemon enqueue-plan --plan-file PLAN.json`\n- `shiki daemon run --once`\n- `shiki runner next`\n- `shiki runner execute --task-id T-0001 --command \"...\"`\n- `shiki runner claude --task-id T-0001`\n- `shiki runner codex --task-id T-0001`\n- `shiki loop step --goal-id G-0001`\n- `shiki loop run --goal-id G-0001`\n- `shiki smoke live --plan-file PLAN.json --dry-run`\n- `shiki smoke live --plan-file PLAN.json --execute-github`\n- `shiki smoke live --plan-file PLAN.json --execute-github --push-branch`\n- `shiki goal create --title ... --outcome ...`\n- `shiki issue plan --goal-id G-0001 --title ... --scope ... --acceptance-check ...`\n- `shiki lock acquire T-0001`\n- `shiki dispatch check T-0001`\n- `shiki worktree allocate T-0001`\n- `shiki github issue --task-id T-0001`\n- `shiki github pr --task-id T-0001`\n- `shiki handoff task T-0001`\n- `shiki handoff repair RP-0001`\n- `shiki repair packet --task-id T-0001 --pr 123 --minimal-change ... --verification-command ...`\n- `shiki task status T-0001 --status done`\n- `shiki goal complete G-0001`\n- `shiki status`\n\nexec\n/bin/zsh -lc \"sed -n '1,240p' '/Users/kio.mizutani/skills/skills/engineering/diagnose/SKILL.md'\" in /private/tmp/shiki-adr-collision.pa0KwY/.worktrees/shiki-t-20260807t073307221254z-2d22d654-introduc\n succeeded in 0ms:\n---\nname: diagnose\ndescription: Disciplined diagnosis loop for hard bugs and performance regressions. Reproduce \u2192 minimise \u2192 hypothesise \u2192 instrument \u2192 fix \u2192 regression-test. Use when user says \"diagnose this\" / \"debug this\", reports a bug, says something is broken/throwing/failing, or describes a performance regression.\n---\n\n# Diagnose\n\nA discipline for hard bugs. Skip phases only when explicitly justified.\n\nWhen exploring the codebase, use the project's domain glossary to get a clear mental model of the relevant modules, and check ADRs in the area you're touching.\n\n## Phase 1 \u2014 Build a feedback loop\n\n**This is the skill.** Everything else is mechanical. If you have a fast, deterministic, agent-runnable pass/fail signal for the bug, you will find the cause \u2014 bisection, hypothesis-testing, and instrumentation all just consume that signal. If you don't have one, no amount of staring at code will save you.\n\nSpend disproportionate effort here. **Be aggressive. Be creative. Refuse to give up.**\n\n### Ways to construct one \u2014 try them in roughly this order\n\n1. **Failing test** at whatever seam reaches the bug \u2014 unit, integration, e2e.\n2. **Curl / HTTP script** against a running dev server.\n3. **CLI invocation** with a fixture input, diffing stdout against a known-good snapshot.\n4. **Headless browser script** (Playwright / Puppeteer) \u2014 drives the UI, asserts on DOM/console/network.\n5. **Replay a captured trace.** Save a real network request / payload / event log to disk; replay it through the code path in isolation.\n6. **Throwaway harness.** Spin up a minimal subset of the system (one service, mocked deps) that exercises the bug code path with a single function call.\n7. **Property / fuzz loop.** If the bug is \"sometimes wrong output\", run 1000 random inputs and look for the failure mode.\n8. **Bisection harness.** If the bug appeared between two known states (commit, dataset, version), automate \"boot at state X, check, repeat\" so you can `git bisect run` it.\n9. **Differential loop.** Run the same input through old-version vs new-version (or two configs) and diff outputs.\n10. **HITL bash script.** Last resort. If a human must click, drive _them_ with `scripts/hitl-loop.template.sh` so the loop is still structured. Captured output feeds back to you.\n\nBuild the right feedback loop, and the bug is 90% fixed.\n\n### Iterate on the loop itself\n\nTreat the loop as a product. Once you have _a_ loop, ask:\n\n- Can I make it faster? (Cache setup, skip unrelated init, narrow the test scope.)\n- Can I make the signal sharper? (Assert on the specific symptom, not \"didn't crash\".)\n- Can I make it more deterministic? (Pin time, seed RNG, isolate filesystem, freeze network.)\n\nA 30-second flaky loop is barely better than no loop. A 2-second deterministic loop is a debugging superpower.\n\n### Non-deterministic bugs\n\nThe goal is not a clean repro but a **higher reproduction rate**. Loop the trigger 100\u00d7, parallelise, add stress, narrow timing windows, inject sleeps. A 50%-flake bug is debuggable; 1% is not \u2014 keep raising the rate until it's debuggable.\n\n### When you genuinely cannot build a loop\n\nStop and say so explicitly. List what you tried. Ask the user for: (a) access to whatever environment reproduces it, (b) a captured artifact (HAR file, log dump, core dump, screen recording with timestamps), or (c) permission to add temporary production instrumentation. Do **not** proceed to hypothesise without a loop.\n\nDo not proceed to Phase 2 until you have a loop you believe in.\n\n## Phase 2 \u2014 Reproduce\n\nRun the loop. Watch the bug appear.\n\nConfirm:\n\n- [ ] The loop produces the failure mode the **user** described \u2014 not a different failure that happens to be nearby. Wrong bug = wrong fix.\n- [ ] The failure is reproducible across multiple runs (or, for non-deterministic bugs, reproducible at a high enough rate to debug against).\n- [ ] You have captured the exact symptom (error message, wrong output, slow timing) so later phases can verify the fix actually addresses it.\n\nDo not proceed until you reproduce the bug.\n\n## Phase 3 \u2014 Hypothesise\n\nGenerate **3\u20135 ranked hypotheses** before testing any of them. Single-hypothesis generation anchors on the first plausible idea.\n\nEach hypothesis must be **falsifiable**: state the prediction it makes.\n\n> Format: \"If <X> is the cause, then <changing Y> will make the bug disappear / <changing Z> will make it worse.\"\n\nIf you cannot state the prediction, the hypothesis is a vibe \u2014 discard or sharpen it.\n\n**Show the ranked list to the user before testing.** They often have domain knowledge that re-ranks instantly (\"we just deployed a change to #3\"), or know hypotheses they've already ruled out. Cheap checkpoint, big time saver. Don't block on it \u2014 proceed with your ranking if the user is AFK.\n\n## Phase 4 \u2014 Instrument\n\nEach probe must map to a specific prediction from Phase 3. **Change one variable at a time.**\n\nTool preference:\n\n1. **Debugger / REPL inspection** if the env supports it. One breakpoint beats ten logs.\n2. **Targeted logs** at the boundaries that distinguish hypotheses.\n3. Never \"log everything and grep\".\n\n**Tag every debug log** with a unique prefix, e.g. `[DEBUG-a4f2]`. Cleanup at the end becomes a single grep. Untagged logs survive; tagged logs die.\n\n**Perf branch.** For performance regressions, logs are usually wrong. Instead: establish a baseline measurement (timing harness, `performance.now()`, profiler, query plan), then bisect. Measure first, fix second.\n\n## Phase 5 \u2014 Fix + regression test\n\nWrite the regression test **before the fix** \u2014 but only if there is a **correct seam** for it.\n\nA correct seam is one where the test exercises the **real bug pattern** as it occurs at the call site. If the only available seam is too shallow (single-caller test when the bug needs multiple callers, unit test that can't replicate the chain that triggered the bug), a regression test there gives false confidence.\n\n**If no correct seam exists, that itself is the finding.** Note it. The codebase architecture is preventing the bug from being locked down. Flag this for the next phase.\n\nIf a correct seam exists:\n\n1. Turn the minimised repro into a failing test at that seam.\n2. Watch it fail.\n3. Apply the fix.\n4. Watch it pass.\n5. Re-run the Phase 1 feedback loop against the original (un-minimised) scenario.\n\n## Phase 6 \u2014 Cleanup + post-mortem\n\nRequired before declaring done:\n\n- [ ] Original repro no longer reproduces (re-run the Phase 1 loop)\n- [ ] Regression test passes (or absence of seam is documented)\n- [ ] All `[DEBUG-...]` instrumentation removed (`grep` the prefix)\n- [ ] Throwaway prototypes deleted (or moved to a clearly-marked debug location)\n- [ ] The hypothesis that turned out correct is stated in the commit / PR message \u2014 so the next debugger learns\n\n**Then ask: what would have prevented this bug?** If the answer involves architectural change (no good test seam, tangled callers, hidden coupling) hand off to the `/improve-codebase-architecture` skill with the specifics. Make the recommendation **after** the fix is in, not before \u2014 you have more information now than when you started.\n\ncodex\nThe handoff already fixes the interface and behavior priorities, so it serves as the TDD plan approval. The skill guidance requires a deterministic failing installer/validator fixture before edits; I\u2019m now locating the mandated review gate and loading the TDD reference guidance before touching the implementation.\nexec\n/bin/zsh -lc \"rg --files '/Users/kio.mizutani/skills/skills/engineering' '/private/tmp/shiki-adr-collision.pa0KwY/.worktrees/shiki-t-20260807t073307221254z-2d22d654-introduc/.agents' '/private/tmp/shiki-adr-collision.pa0KwY/.worktrees/shiki-t-20260807t073307221254z-2d22d654-introduc/.codex' | rg '(\"'^|/)(code-review|tests|mocking|deep-modules|interface-design|refactoring)(/|'\"\\\\.md|\"'$)|SKILL'\"\\\\.md\"'$'\"'\" in /private/tmp/shiki-adr-collision.pa0KwY/.worktrees/shiki-t-20260807t073307221254z-2d22d654-introduc\n succeeded in 0ms:\nrg: /private/tmp/shiki-adr-collision.pa0KwY/.worktrees/shiki-t-20260807t073307221254z-2d22d654-introduc/.agents: No such file or directory (os error 2)\n/Users/kio.mizutani/skills/skills/engineering/to-prd/SKILL.md\n/private/tmp/shiki-adr-collision.pa0KwY/.worktrees/shiki-t-20260807t073307221254z-2d22d654-introduc/.codex/skills/shiki/SKILL.md\n/Users/kio.mizutani/skills/skills/engineering/tdd/refactoring.md\n/Users/kio.mizutani/skills/skills/engineering/tdd/interface-design.md\n/Users/kio.mizutani/skills/skills/engineering/tdd/SKILL.md\n/Users/kio.mizutani/skills/skills/engineering/tdd/tests.md\n/Users/kio.mizutani/skills/skills/engineering/tdd/deep-modules.md\n/Users/kio.mizutani/skills/skills/engineering/tdd/mocking.md\n/Users/kio.mizutani/skills/skills/engineering/grill-with-docs/SKILL.md\n/Users/kio.mizutani/skills/skills/engineering/zoom-out/SKILL.md\n/Users/kio.mizutani/skills/skills/engineering/to-issues/SKILL.md\n/Users/kio.mizutani/skills/skills/engineering/prototype/SKILL.md\n/Users/kio.mizutani/skills/skills/engineering/diagnose/SKILL.md\n/Users/kio.mizutani/skills/skills/engineering/triage/SKILL.md\n/Users/kio.mizutani/skills/skills/engineering/improve-codebase-architecture/SKILL.md\n/Users/kio.mizutani/skills/skills/engineering/setup-matt-pocock-skills/SKILL.md\n\nexec\n/bin/zsh -lc \"wc -l '/Users/kio.mizutani/skills/skills/engineering/tdd/refactoring.md' && sed -n '1,400p' '/Users/kio.mizutani/skills/skills/engineering/tdd/refactoring.md'\" in /private/tmp/shiki-adr-collision.pa0KwY/.worktrees/shiki-t-20260807t073307221254z-2d22d654-introduc\nexec\n/bin/zsh -lc \"wc -l '/Users/kio.mizutani/skills/skills/engineering/tdd/interface-design.md' && sed -n '1,400p' '/Users/kio.mizutani/skills/skills/engineering/tdd/interface-design.md'\" in /private/tmp/shiki-adr-collision.pa0KwY/.worktrees/shiki-t-20260807t073307221254z-2d22d654-introduc\nexec\n/bin/zsh -lc \"wc -l '/Users/kio.mizutani/skills/skills/engineering/tdd/tests.md' && sed -n '1,400p' '/Users/kio.mizutani/skills/skills/engineering/tdd/tests.md'\" in /private/tmp/shiki-adr-collision.pa0KwY/.worktrees/shiki-t-20260807t073307221254z-2d22d654-introduc\n succeeded in 0ms:\n      10 /Users/kio.mizutani/skills/skills/engineering/tdd/refactoring.md\n# Refactor Candidates\n\nAfter TDD cycle, look for:\n\n- **Duplication** \u2192 Extract function/class\n- **Long methods** \u2192 Break into private helpers (keep tests on public interface)\n- **Shallow modules** \u2192 Combine or deepen\n- **Feature envy** \u2192 Move logic to where data lives\n- **Primitive obsession** \u2192 Introduce value objects\n- **Existing code** the new code reveals as problematic\n\n succeeded in 0ms:\n      31 /Users/kio.mizutani/skills/skills/engineering/tdd/interface-design.md\n# Interface Design for Testability\n\nGood interfaces make testing natural:\n\n1. **Accept dependencies, don't create them**\n\n   ```typescript\n   // Testable\n   function processOrder(order, paymentGateway) {}\n\n   // Hard to test\n   function processOrder(order) {\n     const gateway = new StripeGateway();\n   }\n   ```\n\n2. **Return results, don't produce side effects**\n\n   ```typescript\n   // Testable\n   function calculateDiscount(cart): Discount {}\n\n   // Hard to test\n   function applyDiscount(cart): void {\n     cart.total -= discount;\n   }\n   ```\n\n3. **Small surface area**\n   - Fewer methods = fewer tests needed\n   - Fewer params = simpler test setup\n\n succeeded in 0ms:\n      61 /Users/kio.mizutani/skills/skills/engineering/tdd/tests.md\n# Good and Bad Tests\n\n## Good Tests\n\n**Integration-style**: Test through real interfaces, not mocks of internal parts.\n\n```typescript\n// GOOD: Tests observable behavior\ntest(\"user can checkout with valid cart\", async () => {\n  const cart = createCart();\n  cart.add(product);\n  const result = await checkout(cart, paymentMethod);\n  expect(result.status).toBe(\"confirmed\");\n});\n```\n\nCharacteristics:\n\n- Tests behavior users/callers care about\n- Uses public API only\n- Survives internal refactors\n- Describes WHAT, not HOW\n- One logical assertion per test\n\n## Bad Tests\n\n**Implementation-detail tests**: Coupled to internal structure.\n\n```typescript\n// BAD: Tests implementation details\ntest(\"checkout calls paymentService.process\", async () => {\n  const mockPayment = jest.mock(paymentService);\n  await checkout(cart, payment);\n  expect(mockPayment.process).toHaveBeenCalledWith(cart.total);\n});\n```\n\nRed flags:\n\n- Mocking internal collaborators\n- Testing private methods\n- Asserting on call counts/order\n- Test breaks when refactoring without behavior change\n- Test name describes HOW not WHAT\n- Verifying through external means instead of interface\n\n```typescript\n// BAD: Bypasses interface to verify\ntest(\"createUser saves to database\", async () => {\n  await createUser({ name: \"Alice\" });\n  const row = await db.query(\"SELECT * FROM users WHERE name = ?\", [\"Alice\"]);\n  expect(row).toBeDefined();\n});\n\n// GOOD: Verifies through interface\ntest(\"createUser makes user retrievable\", async () => {\n  const user = await createUser({ name: \"Alice\" });\n  const retrieved = await getUser(user.id);\n  expect(retrieved.name).toBe(\"Alice\");\n});\n```\n\nexec\n/bin/zsh -lc \"wc -l '/Users/kio.mizutani/skills/skills/engineering/tdd/deep-modules.md' && sed -n '1,400p' '/Users/kio.mizutani/skills/skills/engineering/tdd/deep-modules.md'\" in /private/tmp/shiki-adr-collision.pa0KwY/.worktrees/shiki-t-20260807t073307221254z-2d22d654-introduc\nexec\n/bin/zsh -lc \"wc -l '/Users/kio.mizutani/skills/skills/engineering/tdd/mocking.md' && sed -n '1,400p' '/Users/kio.mizutani/skills/skills/engineering/tdd/mocking.md'\" in /private/tmp/shiki-adr-collision.pa0KwY/.worktrees/shiki-t-20260807t073307221254z-2d22d654-introduc\n succeeded in 0ms:\n      59 /Users/kio.mizutani/skills/skills/engineering/tdd/mocking.md\n# When to Mock\n\nMock at **system boundaries** only:\n\n- External APIs (payment, email, etc.)\n- Databases (sometimes - prefer test DB)\n- Time/randomness\n- File system (sometimes)\n\nDon't mock:\n\n- Your own classes/modules\n- Internal collaborators\n- Anything you control\n\n## Designing for Mockability\n\nAt system boundaries, design interfaces that are easy to mock:\n\n**1. Use dependency injection**\n\nPass external dependencies in rather than creating them internally:\n\n```typescript\n// Easy to mock\nfunction processPayment(order, paymentClient) {\n  return paymentClient.charge(order.total);\n}\n\n// Hard to mock\nfunction processPayment(order) {\n  const client = new StripeClient(process.env.STRIPE_KEY);\n  return client.charge(order.total);\n}\n```\n\n**2. Prefer SDK-style interfaces over generic fetchers**\n\nCreate specific functions for each external operation instead of one generic function with conditional logic:\n\n```typescript\n// GOOD: Each function is independently mockable\nconst api = {\n  getUser: (id) => fetch(`/users/${id}`),\n  getOrders: (userId) => fetch(`/users/${userId}/orders`),\n  createOrder: (data) => fetch('/orders', { method: 'POST', body: data }),\n};\n\n// BAD: Mocking requires conditional logic inside the mock\nconst api = {\n  fetch: (endpoint, options) => fetch(endpoint, options),\n};\n```\n\nThe SDK approach means:\n- Each mock returns one specific shape\n- No conditional logic in test setup\n- Easier to see which endpoints a test exercises\n- Type safety per endpoint\n\n succeeded in 0ms:\n      33 /Users/kio.mizutani/skills/skills/engineering/tdd/deep-modules.md\n# Deep Modules\n\nFrom \"A Philosophy of Software Design\":\n\n**Deep module** = small interface + lots of implementation\n\n```\n\u250c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2510\n\u2502   Small Interface   \u2502  \u2190 Few methods, simple params\n\u251c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2524\n\u2502                     \u2502\n\u2502                     \u2502\n\u2502  Deep Implementation\u2502  \u2190 Complex logic hidden\n\u2502                     \u2502\n\u2502                     \u2502\n\u2514\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2518\n```\n\n**Shallow module** = large interface + little implementation (avoid)\n\n```\n\u250c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2510\n\u2502       Large Interface           \u2502  \u2190 Many methods, complex params\n\u251c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2524\n\u2502  Thin Implementation            \u2502  \u2190 Just passes through\n\u2514\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2518\n```\n\nWhen designing interfaces, ask:\n\n- Can I reduce the number of methods?\n- Can I simplify the parameters?\n- Can I hide more complexity inside?\n\nexec\n/bin/zsh -lc \"rg --files /Users/kio.mizutani/Documents/lead-os/skills/engineering /Users/kio.mizutani/.codex /Users/kio.mizutani/.agents 2>/dev/null | rg '/code-review(/|\\\\.md\"'$)|code-review/SKILL'\"\\\\.md\"'$'\"' | head -50\" in /private/tmp/shiki-adr-collision.pa0KwY/.worktrees/shiki-t-20260807t073307221254z-2d22d654-introduc\n succeeded in 0ms:\n  \u2192 Load required Shiki, diagnose, TDD, and code-review guidance; run scoped preflight and inspect durable task/context evidence\n  \u2022 Build a deterministic failing validator/installer feedback loop and record RED evidence\n  \u2022 Implement SADR namespace migration and citation/package/validator updates in vertical TDD slices\n  \u2022 Implement preflight authorization and collision-safe installer migration with adversarial TDD fixtures\n  \u2022 Run scoped and full verification suites; refactor only while green\n  \u2022 Run the structured code-review gate, repair blocking findings, re-review, and report evidence without commit/PR actions\nexec\n/bin/zsh -lc \"rg -n -i 'SADR|ADR collision|install stamp|stamp path|legacy numeric|collision upgrade' /Users/kio.mizutani/.codex/memories/MEMORY.md\" in /private/tmp/shiki-adr-collision.pa0KwY/.worktrees/shiki-t-20260807t073307221254z-2d22d654-introduc\n exited 1 in 0ms:\ncodex\nPreflight comes next. I\u2019m deliberately omitting `gh` because the handoff forbids every `gh` command; GitHub delivery is outside this worktree task. I\u2019ll verify the assigned branch/worktree, available runtime tools, the durable task record, and the current repository state before constructing the RED fixture.\nexec\n/bin/zsh -lc 'git rev-parse --show-toplevel' in /private/tmp/shiki-adr-collision.pa0KwY/.worktrees/shiki-t-20260807t073307221254z-2d22d654-introduc\n succeeded in 0ms:\n/private/tmp/shiki-adr-collision.pa0KwY/.worktrees/shiki-t-20260807t073307221254z-2d22d654-introduc\n\nexec\n/bin/zsh -lc \"rg --files -g 'CLAUDE.md' -g 'CONTEXT.md' -g 'docs/agents/**' -g 'docs/adr/**' -g 'scripts/**' -g 'tests/**' -g 'pyproject.toml' | sed -n '1,240p'\" in /private/tmp/shiki-adr-collision.pa0KwY/.worktrees/shiki-t-20260807t073307221254z-2d22d654-introduc\n succeeded in 0ms:\nCLAUDE.md\nscripts/test_shiki_p2_schema_drift.sh\nscripts/shiki_runtime.py\nscripts/enforce_cca_verdict.py\nscripts/shiki_doctor.py\nscripts/shiki_schema.py\nscripts/test_shiki_p2_missing_cca_token.sh\nscripts/test_shiki_start.sh\nscripts/test_shiki_guardian_policy.sh\nscripts/test_shiki_memory_loop.sh\nscripts/shiki_cli.py\nscripts/shiki_github.py\nscripts/cca_verdict_usable.py\nscripts/shiki_manifest.py\nscripts/test_shiki_p2_check_name_drift.sh\nscripts/shiki_provider.py\nscripts/test_shiki_runner_codex.sh\nscripts/shiki_state.py\nscripts/test_shiki_module_boundaries.sh\nscripts/shiki_workflows.py\nscripts/test_shiki_doctor.sh\nscripts/shiki_contracts.py\nscripts/shiki_guardian_review.py\nscripts/test_shiki_p2_branch_protection_failure.sh\nscripts/test_shiki_p2_repair_limit.sh\nscripts/test_shiki_p2_malicious_pr.sh\nscripts/test_shiki_provider_config.sh\nscripts/shiki_session_lease.py\nscripts/test_shiki_runtime_auth.sh\nscripts/shiki_runtime_adapters.py\nscripts/shiki_state_classes.py\nscripts/test_shiki_runner_claude.sh\nscripts/test_shiki_manifest.sh\nscripts/shiki_installer.py\nscripts/validate_shiki.py\nscripts/test_shiki_validator_hardening.sh\nscripts/test_shiki_run_orchestrator.sh\nscripts/test_shiki_loop_lock_guard.sh\nscripts/test_shiki_code_review_gate.sh\nscripts/shiki_tasks.py\nscripts/shiki_jsonschema.py\nscripts/shiki_guardian.py\nscripts/test_shiki_governance_evidence.sh\nscripts/shiki_git.py\nscripts/test_shiki_shellcheck.sh\nscripts/test_shiki_daemon_runner.sh\nscripts/test_shiki_migrations.sh\nscripts/shiki_sync_proof.py\nscripts/shiki.py\nscripts/test_shiki_state_classes.sh\nscripts/test_shiki_goal_loop.sh\nscripts/mergegate_check.py\nscripts/shiki_config.py\nscripts/guardian_approval_signal.py\nscripts/shiki_guardian_status.py\nscripts/shiki_loop.py\nscripts/shiki_process.py\nscripts/test_shiki_evidence_integrity.sh\nscripts/shiki_contract_approval.py\nscripts/shiki_memory.py\nscripts/shiki_runtime_registry.py\nscripts/shiki_locks.py\nscripts/build_cca_evidence_manifest.py\nscripts/test_shiki_p2_fork_pr_limitation.sh\nscripts/shiki_evidence.py\nscripts/test_shiki_init.sh\nscripts/test_shiki_control_plane.sh\nscripts/test_shiki_runtime_registry.sh\nscripts/test_shiki_adversarial_state.sh\nscripts/test_shiki_loop_e2e.sh\nscripts/shiki_bootstrap.py\nscripts/shiki_migrations.py\nscripts/test_shiki_workflow_lint.sh\ndocs/agents/evidence-integrity.md\ndocs/agents/ledger-schema.md\ndocs/agents/branch-protection-smoke.md\ndocs/agents/execution-control.md\ndocs/agents/issue-tracker.md\ndocs/agents/shiki-cli-architecture.md\ndocs/agents/triage-labels.md\ndocs/agents/implementation-policy.md\ndocs/agents/runtime-registry.md\ndocs/agents/completion-check-agent.md\ndocs/agents/state-classes.md\ndocs/agents/github-provider-config.md\ndocs/agents/skill-gate.md\ndocs/agents/domain.md\ndocs/agents/decision-control.md\ndocs/agents/shiki-migrations.md\ndocs/agents/memory.md\ndocs/agents/control-commands.md\ndocs/agents/shiki-doctor.md\ndocs/agents/runtime-auth-model.md\ndocs/agents/autonomous-loop-e2e-acceptance.md\ndocs/agents/guardian-policy.md\ndocs/agents/bootstrap-command.md\ndocs/agents/codex-handoff.md\ndocs/agents/checklists.md\ndocs/agents/node24-workflow-compatibility.md\ndocs/adr/0016-locks-bound-product-paths.md\ndocs/adr/0017-bookkeeping-closeouts-do-not-inherit-risk.md\ndocs/adr/0003-require-skill-gate-for-mergegate-readiness.md\ndocs/adr/0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\ndocs/adr/0004-use-github-first-with-shiki-mirror.md\ndocs/adr/0014-codex-app-external-ai-guardian-ui-adapter.md\ndocs/adr/0008-use-claude-code-as-default-implementer.md\ndocs/adr/0013-bootstrap-grants-actions-approve-pull-requests.md\ndocs/adr/0012-autonomous-loop-closeout-to-main.md\ndocs/adr/README.md\ndocs/adr/0009-requirements-definition-and-spec-freeze.md\ndocs/adr/0002-use-agents-as-shared-constitution.md\ndocs/adr/0007-packaging-and-release.md\ndocs/adr/0011-autonomous-quality-gate-evidence.md\ndocs/adr/0001-keep-shiki-as-platform-name.md\ndocs/adr/0015-contract-approval-before-dispatch.md\ndocs/adr/template.md\ndocs/adr/0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\ndocs/adr/0010-external-ai-guardian-review.md\ndocs/adr/0018-carried-guardian-approval-across-a-proven-base-sync.md\ntests/test_repair_reasons.py\ntests/test_mirror_schema_validation.py\ntests/test_closeout.py\ntests/test_workflow_invocation.py\ntests/test_guardian_status.py\ntests/test_loop_lock_validator_hint.py\ntests/test_manifest_staging.py\ntests/test_loop_lock_guard.py\ntests/test_loop_smoke2.py\ntests/test_guardian_evidence.py\ntests/test_guardian_carry_parity.py\ntests/test_bootstrap_safety.py\ntests/test_contract_amendment.py\ntests/test_loop_smoke3.py\ntests/test_handoff_consult.py\ntests/test_repair_delivery.py\ntests/test_loop_engine.py\ntests/test_contract_plan_registration.py\ntests/test_install_force_semantics.py\ntests/test_branch_behind_sync.py\ntests/test_memory_hooks.py\ntests/test_contract_approval.py\ntests/test_lock_matching.py\ntests/test_runner_adapters.py\ntests/test_goal_reconcile.py\ntests/test_dispatch_state_sync.py\ntests/test_dispatch_lock_precondition.py\ntests/test_migration_mode.py\ntests/test_loop_tdd_evidence.py\ntests/test_memory_capture.py\ntests/test_lock_lifecycle.py\ntests/test_loop_verdict_gating.py\ntests/test_loop_code_review.py\ntests/test_memory_promotion.py\ntests/test_completion_lock_scope.py\ntests/test_guardian_approval_binding.py\ntests/shiki_test_support.py\ntests/test_guardian_review.py\ntests/test_codeowners_wiring.py\ntests/test_cca_evidence_manifest_taskless.py\ntests/test_guardian_signal_closeout.py\ntests/test_contract_open.py\ntests/test_bookkeeping_closeout.py\ntests/__init__.py\ntests/test_paired_invariants.py\ntests/test_cca_completeness.py\ntests/test_mirror_lock_injection.py\ntests/test_secret_set_claude.py\ntests/test_worktree_base.py\ntests/test_loop_smoke.py\ntests/test_contract_immutability.py\ntests/test_loop_branch_plumbing.py\ntests/test_branch_protection_payload.py\ntests/test_migrations_target.py\ntests/test_lock_conflict_stop.py\ntests/test_workflow_invocation_carry.py\ntests/test_memory_consult.py\ntests/test_loop_e2e_contract.py\ntests/test_contract_mode.py\ntests/test_workflow_permissions_payload.py\ntests/test_memory_scorecard.py\ntests/test_guardian_carry.py\ntests/test_sync_proof.py\ntests/test_cca_head_sha_injection.py\ntests/test_cca_verdict_consistency.py\ntests/test_guardian_signal.py\ntests/test_session_lease.py\ntests/test_adr_references.py\ntests/test_policy_job_merged_prs.py\ntests/test_install_completeness.py\ntests/test_install_version_drift.py\ntests/test_required_checks_derivation.py\ntests/test_reopened_task.py\ntests/test_cca_verdict_nondegenerate.py\ntests/test_config_loading.py\ntests/test_mirror_scope.py\ntests/test_id_allocation.py\ntests/test_cca_verdict_usable.py\ntests/test_lock_release.py\npyproject.toml\nCONTEXT.md\n\nexec\n/bin/zsh -lc 'git branch --show-current' in /private/tmp/shiki-adr-collision.pa0KwY/.worktrees/shiki-t-20260807t073307221254z-2d22d654-introduc\n succeeded in 0ms:\nshiki/t-20260807t073307221254z-2d22d654-introduce-sadr-and-migrate-installs-without-touc\n\nexec\n/bin/zsh -lc \"rg -n 'T-20260807T073307221254Z-2d22d654|G-20260807T073307219752Z-1ee1ed68|code-review|skills_dir' .shiki 2>/dev/null\" in /private/tmp/shiki-adr-collision.pa0KwY/.worktrees/shiki-t-20260807t073307221254z-2d22d654-introduc\n succeeded in 0ms:\n.shiki/reports/R-20260618T040603973878Z-d8c5fa0b.json:50:  \"summary\": \"Reviewer structured-output emission + parsing fixed and merged in PR #148; the autonomous code-review gate now parses a real claude -p verdict via --output-format json + structured_output. Unblocks the #140 T5 live self-drive.\"\n.shiki/reports/e2e-acceptance-20260618T093140Z-summary.md:21:- Smoke goal: `G-20260618T093112319886Z-a4f768c5` (risk low; skills tdd, code-review)\n.shiki/reports/e2e-acceptance-20260618T093140Z-summary.md:55:- `L-20260618T093251323134Z-f86e815e` \u2014 **code-review** verdict CLEAN (independent\n.shiki/reports/R-20260612T061938063729Z-e7cc1c5f.json:18:  \"summary\": \"G-B complete: code-review is a Skill Gate skill and the default pre-PR implementer gate, enforced by MergeGate skill evidence; merged as PR #122\"\n.shiki/handoffs/T-20260612T091329543362Z-778e0c2f-task.md:17:- This PR carries pre-PR code-review ledger evidence (PR-12)\n.shiki/handoffs/T-20260612T091329543362Z-778e0c2f-task.md:43:- code-review\n.shiki/handoffs/T-20260612T055157606542Z-5205e0bd-task.md:6:Branch: shiki/g-d76238aa-code-review-gate\n.shiki/handoffs/T-20260612T055157606542Z-5205e0bd-task.md:9:Add skills/engineering/code-review/SKILL.md (wrapper: run the native code-review skill after TDD green and before PR creation, apply fixes as the implementer on the task branch, record findings+resolutions as a ledger entry whose summary names the skill and as a PR body section). Extend validate_shiki.py KNOWN_SKILLS, .shiki/schemas/repair-packet.schema.json required_skill enum, and shiki_cli.py repair --required-skill choices with code-review. Flip default required_skills to [tdd, code-review] in shiki_tasks.register_task_from_plan and shiki_bootstrap start synthesis so MergeGate's existing skill-evidence rule enforces the gate on every new task. Document the contract: skill-gate.md row + quick map, AGENTS.md Skill Gate table row, checklists.md PR-12 item, triage-labels.md skill:code-review, implementation-policy.md implementer contract (TDD -> code-review -> PR), CLAUDE.md handoff standard line. Add scripts/test_shiki_code_review_gate.sh (plan task and repair packet carrying code-review pass validation; SKILL.md staged) and register it in installer TEMPLATE_PATHS. Guardian-approved implementation by Claude Code in this assigned task.\n.shiki/handoffs/T-20260612T055157606542Z-5205e0bd-task.md:12:- bash scripts/test_shiki_code_review_gate.sh passes: a plan task with required_skills [tdd, code-review] orchestrates and validates; a repair packet with --required-skill code-review passes schema validation; a task registered without explicit skills defaults to [tdd, code-review]\n.shiki/handoffs/T-20260612T055157606542Z-5205e0bd-task.md:15:- This PR itself carries pre-PR code-review ledger evidence (dogfood of the new gate)\n.shiki/handoffs/T-20260612T055157606542Z-5205e0bd-task.md:33:- code-review\n.shiki/handoffs/T-20260612T062731178333Z-36e9bdc7-task.md:16:- This PR carries pre-PR code-review ledger evidence (PR-12)\n.shiki/handoffs/T-20260612T062731178333Z-36e9bdc7-task.md:38:- code-review\n.shiki/locks/T-20260807T073307221254Z-2d22d654.json:3:  \"goal_id\": \"G-20260807T073307219752Z-1ee1ed68\",\n.shiki/locks/T-20260807T073307221254Z-2d22d654.json:20:  \"task_id\": \"T-20260807T073307221254Z-2d22d654\"\n.shiki/handoffs/T-20260613T035102039128Z-9bef9375-task.md:16:- This PR carries pre-PR code-review ledger evidence (PR-12)\n.shiki/handoffs/T-20260613T035102039128Z-9bef9375-task.md:32:- code-review\n.shiki/runner/EXEC-20260804T103419170084Z-8f91446d.json:8:  \"stdout\": \"All work is complete and within locks. The loop owns commit/push/PR/merge, so I stop here.\\n\\n## Result\\n- Goal/task: G-20260804T094832859551Z-2d0958fd / T-20260804T094832861501Z-78871209\\n- Role: Implementer (Claude Code, default)\\n- Status: **complete** (implementation done; delivery is the loop's)\\n\\n## What changed or decided\\n- **`docs/adr/0016-locks-bound-product-paths.md`** and **`docs/adr/0017-bookkeeping-closeouts-do-not-inherit-risk.md`** were already present verbatim in this worktree as the coordinator copies (233 and 129 lines, headers intact). I left them **unmodified** \\u2014 a decision record edited after the fact misrepresents when the decision was made.\\n- **`tests/test_adr_references.py`** (new): binds `ADR ####` citations to records. A pure `find_adr_reference_violations(root)` scans every `scripts/*.py` (plus `docs/**/*.md` and root `*.md`, since cheap) and asserts each cited number resolves to **exactly one** `docs/adr/####-*.md` (zero = \\\"missing\\\", \\u22652 = \\\"ambiguous\\\"). The converse is not required \\u2014 an uncited ADR is allowed.\\n- Confirmed all 10 cited numbers (0008\\u20130017) resolve; closes the unbound-pair gap where `mergegate_check.py`/`guardian_approval_signal.py` cited 0016/0017 with no document and no validator signalling it.\\n\\n## Checklist status\\n- Skill Gate: `tdd` (red proven via fixture \\u2192 green), `code-review` (scope + self-review, only the 3 locked paths touched)\\n- PRD/issues: n/a (task handoff)\\n- TDD: `test_fails_when_citation_has_no_document` proves the check fails on a broken citation via a temp fixture, not only by passing today\\n- CCA: pending (loop-owned)\\n- MergeGate: pending (loop-owned)\\n\\n## Evidence\\n- `python3 -m unittest tests.test_adr_references -v` \\u2192 7 tests OK\\n- `python3 -m unittest discover -s tests` \\u2192 **Ran 861 tests, OK** (exit 0)\\n- `python3 scripts/validate_shiki.py --target .` \\u2192 **Shiki validation passed** (exit 0)\\n- `git status --short`: only `docs/adr/0016-*.md`, `docs/adr/0017-*.md`, `tests/test_adr_references.py` (all in locks) + loop-owned `.shiki/*` records I did not touch\\n\\n## Acceptance check mapping\\n- 0016/0017 exist, byte-identical to coordinator copy (present verbatim, unmodified) \\u2014 \\u2713\\n- test asserts every `ADR ####` in `scripts/*.py` resolves to exactly one doc \\u2014 \\u2713\\n- fails on a citation with no matching document (fixture-proven) \\u2014 \\u2713\\n- does not require an ADR to be cited (`test_uncited_adr_is_allowed`) \\u2014 \\u2713\\n- failure message names citing file + cited number + missing path (`docs/adr/####-*.md`) \\u2014 \\u2713 (asserted in the fixture test)\\n- both commands pass; no file outside locks changed \\u2014 \\u2713\\n\\n## Next action\\n- Loop: commit the two ADRs + the test, push, open the PR with the MergeGate body, run CCA/MergeGate. I did not run any git/gh/PR command, per the execution protocol.\\n\",\n.shiki/runner/EXEC-20260731T093443729356Z-dcef9c3f.json:8:  \"stdout\": \"The repair is complete and fully verified. Changes are staged in the worktree for the loop's repair-delivery step to commit and push (Shiki runners leave the commit to `dispatch_repair`; I don't commit unprompted).\\n\\n## Result\\n- **Goal/task:** G-20260729T065622761503Z-4479b0d3 / T-20260729T065622764296Z-48021407\\n- **Repair packet:** RP-20260731T092411917682Z-aa9a4636 (attempt 1) \\u00b7 PR 226\\n- **Role:** Repairer (implementer runtime), skill `tdd`\\n- **Status:** complete (in-worktree; awaiting loop repair-delivery commit/push)\\n\\n## What changed\\n- **`.shiki/schemas/task.schema.json`** \\u2014 removed `cca_checklist_profile` from the `required` array. Kept the property definition intact and extended its description to record that it's optional until every pre-existing task file carries it, mirroring the `validate_shiki.py` reasoning and pointing at the new guard test. MergeGate's `checklist_profile_coverage_failures` enforcement is untouched.\\n- **`tests/test_cca_verdict_consistency.py`** \\u2014 added `TaskSchemaRequiredConsistency` with two tests: a named check that `cca_checklist_profile`'s presence agrees across the schema's `required` and `validate_shiki.TASK_REQUIRED`, and a stronger `set(schema.required) == set(TASK_REQUIRED)` guard. Promoting the field in one surface without the other now fails the suite.\\n\\nTDD followed: wrote the tests (RED \\u2014 2 failures), removed the field from `required` (GREEN \\u2014 652/652), then confirmed the mutation guard (re-adding to `required` alone \\u2192 both tests fail) before reverting.\\n\\n## Checklist status\\n- **TDD:** red \\u2192 green \\u2192 mutation-verified \\u2713\\n- **Scope:** only the two in-lock files touched; no backfill; `TASK_REQUIRED` unchanged; property/enforcement preserved \\u2713\\n\\n## Evidence\\n- **Isolated measurement (the landmine this repair targets):** files failing on missing required `cca_checklist_profile` went **98 \\u2192 0** across all 106 `.shiki/tasks/*.json` (stronger than the packet's \\\"40 of 40 sampled\\\"). The 8 that declare the profile were never blocked by it.\\n- **Mutation guard:** re-adding `cca_checklist_profile` to the schema's `required` (without `TASK_REQUIRED`) \\u2192 both new tests FAIL; reverting \\u2192 PASS.\\n- **Chained verification** `python3 -m unittest discover -s tests && bash scripts/test_shiki_control_plane.sh && bash scripts/test_shiki_governance_evidence.sh && python3 scripts/validate_shiki.py --target .` \\u2192 **exit 0** (652 tests OK; control-plane, governance-evidence, validate_shiki all pass).\\n\\n## \\u26a0\\ufe0f Honest scope caveat (do not overclaim)\\nThe evidence bullet says \\\"*every* pre-existing task file validates against `task.schema.json` after the change.\\\" That is **not literally achievable in this repair's scope**, and I did not force it. After the fix, 49/106 fully validate and **57 still fail \\u2014 but solely on a separate, pre-existing landmine**: the schema's `id` pattern `^T-[0-9]{4,}$` only matches the older `T-0001`-style ids, not the newer `T-<timestamp>Z-<hex>` format. It was masked because `required` is checked before `properties` in `shiki_schema.validate_instance`. This is out of scope (this task is about `cca_checklist_profile`, not id normalization) and belongs to the still-planned per-record schema-validation task **T-\\u2026f138eccf** referenced in the PR's own code-review finding. I recorded both landmines in memory rather than expanding scope to fix the id pattern.\\n\\n## Next action\\n- Loop repair-delivery commits/pushes the two changed files to branch `shiki/\\u2026-refuse-a-self-contradictory-cca-verdict` and verifies HEAD advanced, then re-runs checks/CCA on PR 226.\\n- Track the `id`/`goal_id` pattern fix under T-\\u2026f138eccf (or its own task) \\u2014 it's the next thing that will block full schema validation.\\n\",\n.shiki/runner/EXEC-20260731T074549396347Z-152c9efc.json:8:  \"stdout\": \"All changes are confined to my three declared locks (`mergegate_check.py`, `test_shiki_adversarial_state.sh`, and the new `tests/test_mirror_scope.py`). Nothing outside scope was touched.\\n\\n## Result\\n- Goal/task: G-20260729T065622761503Z-4479b0d3 / T-20260729T065622763684Z-ee01f3c4\\n- Role: Implementer (Claude Code)\\n- Status: ready\\n\\n## What changed\\nAll in `scripts/mergegate_check.py`, matching the four scope items:\\n\\n- **(a) Renames seen through both legs.** New `lock_scope_paths()` derives the lock-scope path set from `files_status` (which already decomposes a rename into `D(old)+A(new)`), and `main()` now feeds *that* to both `files_outside_locks` and `active_lock_conflicts`. A rename can no longer move a file out of locked scope on its unseen source leg.\\n- **(b) Generic mirror-class rule.** `enforce_untrusted_shiki_mutations` now governs the ten mirror dirs that had no per-record rule (`dag, reports, memories, runs, worktrees, handoffs, runner, smoke, starts, inbox`): deletion is blocked and every add/modify must be own-scoped (filename id or content `task_id`/`goal_id`, reusing `ledger_entry_allowed_for_task`'s anti-padding logic). The base-snapshot delete-protection loop was generalized (`_tracked_mirror_subdirs`, manifest-driven, with a `tasks/goals/locks` floor so it never protects *less* than before) to cover every tracked mirror directory.\\n- **(c) Exact runtime-filename match.** `runtime_evidence_path()` now matches `cca-verdict.json`/`mergegate-result.json` exactly instead of a `startswith` prefix, so `.shiki/schemas/cca-verdict.schema.json` is mergeable again \\u2014 while `.shiki/somewhere/cca-verdict.json` / `mergegate-result.json` still block.\\n- **Spec Freeze is operator-only (ADR 0009).** `plans` get a dedicated branch: a new plan must be the goal's `source_plan`, and any create/modify of a `spec_freeze` block \\u2014 including its own goal's source plan, including appended amendments \\u2014 is blocked (compared against the base snapshot so an unchanged pre-existing freeze passes; base-absence is fail-closed).\\n\\n## Checklist status\\n- Skill Gate: tdd \\u2713 (red\\u2192green), code-review \\u2713 (self-review of diff; scope-confined)\\n- TDD: `tests/test_mirror_scope.py` (17 unit tests, written failing first) + end-to-end fixtures added to `test_shiki_adversarial_state.sh` for all acceptance checks\\n- CCA / MergeGate: owned by the loop (not run locally)\\n\\n## Evidence\\n- New unit tests: 17/17 pass\\n- `test_shiki_adversarial_state.sh` (incl. new fixtures for every acceptance check): pass \\u2014 anti-forgery guard stays green\\n- `test_shiki_control_plane.sh`, `test_shiki_state_classes.sh`: pass\\n- Full Python suite: **627 tests OK**; loop/goal-loop/loop-e2e/malicious-pr/governance/schema-drift/shellcheck: all pass\\n- `validate_shiki` on the repo: passes\\n\\n## Notes\\n- I did not need to modify `scripts/shiki_locks.py`, `test_shiki_control_plane.sh`, or `test_shiki_state_classes.sh` (all in my locks but no change required).\\n- Pre-existing unused local `task_locks` (`mergegate_check.py:1772`) predates this change and is unrelated dead code \\u2014 flagging rather than deleting it, per the engineering-discipline rule.\\n- Per the execution protocol I made no commits/PRs; the Shiki loop owns delivery.\\n\",\n.shiki/runner/EXEC-20260731T060829913512Z-3b8338d8.json:8:  \"stdout\": \"Implementation complete. All acceptance checks pass and every change stayed inside the declared locks.\\n\\n## Result\\n- Goal/task: G-20260729T065622761503Z-4479b0d3 / T-20260729T065622765968Z-234bf98b\\n- Role: Implementer (TDD)\\n- Status: complete (local verification; the Shiki loop owns commit/push/PR/merge)\\n\\n## What changed\\n- **`.github/workflows/shiki-mergegate.yml`** \\u2014 \\\"Prepare PR context\\\" now builds the base `.shiki` snapshot (`git archive origin/<base_ref>`, reusing the existing fetch). \\\"MergeGate metadata check\\\" resolves the merged-PR proof the same way the CCA workflow does (task id \\u2192 base snapshot `expected_pr` \\u2192 `gh pr view` MERGED gate) and passes `--base-shiki` + `--merged-prs` to `mergegate_check.py`. So a `post_merge_reconcile` PR can pass instead of blocking on a missing base snapshot. All context flows through `env:` \\u2014 no `${{ }}` in run bodies.\\n- **`.github/workflows/shiki-orchestrator.yml`** \\u2014 comment/input-derived values (`EVENT_NAME`, `INPUT_PLAN_PATH`, `INPUT_MODE`, `MODE`, `PLAN_PATH`, `RUN_ID`) now flow through `env:` and are referenced as quoted shell vars, closing the expression-injection surface. The commit-evidence guard stages `.shiki` first, then guards on `git diff --cached --quiet`, so new untracked evidence is committed+pushed instead of silently dropped.\\n- **`scripts/validate_shiki.py`** \\u2014 new `validate_workflow_invocation_contracts()` (wired into `main()`) asserts the metadata job invokes `mergegate_check.py` with `--base-shiki` and `--merged-prs`, and that the two hardened workflows carry no `${{ }}` inside any `run:` body.\\n- **`tests/test_workflow_invocation.py`** (new) + **`scripts/test_shiki_control_plane.sh`** \\u2014 the TDD coverage and contract-string assertions.\\n\\n## Checklist status\\n- TDD: red\\u2192green (wrote failing tests first; 10 new tests now pass)\\n- Skill Gate: tdd, code-review applied\\n- Locks: only the 5 files touched, all in the declared lock set\\n\\n## Evidence (local, diagnostic)\\n- Acceptance: AC1 `test_metadata_argv_returns_ready` \\u2713 \\u00b7 AC2 `test_without_base_shiki_still_blocks` \\u2713 \\u00b7 AC3 `test_new_files_only_are_committed_and_pushed`/`test_stages_before_guard` \\u2713 \\u00b7 AC4 `test_command_substitution_payload_is_literal`/`test_plan_path_flows_through_env_not_expression` \\u2713 \\u00b7 AC5 `test_missing_base_shiki_flag_fails`/`test_missing_merged_prs_flag_fails`/`test_expression_in_run_body_fails` \\u2713 \\u00b7 AC6 `bash scripts/test_shiki_workflow_lint.sh` exits 0 \\u2713\\n- `python3 -m unittest discover -s tests` \\u2192 594 passed (was 584). `python3 scripts/validate_shiki.py` OK. All 34 `test_shiki_*.sh` PASS. actionlint + shellcheck strict rc=0.\\n\\n## Notes\\n- Passing `--base-shiki` to the metadata check also activates `enforce_untrusted_shiki_mutations`' protected-base comparison for normal PRs. This is consistent with \\u2014 not new relative to \\u2014 the authoritative \\\"MergeGate policy check\\\" (shiki-cca-completion.yml), which already passes `--base-shiki` on every PR, so it strengthens the metadata check without newly blocking legitimate PRs.\\n- Per the execution protocol I made no git commit/push/PR and authored no approval records; the goal loop owns delivery.\\n\\n## Next action\\n- Hand back to the Shiki loop to commit, push, and open the PR with the MergeGate body; CCA + MergeGate will run the durable checks.\\n\",\n.shiki/runner/EXEC-20260730T014728106308Z-47105575.json:8:  \"stdout\": \"All tasks complete and all suites green. Implementation finished within the declared locks. Here is the summary.\\n\\n## Result\\n- Goal/task: G-20260729T065622761503Z-4479b0d3 / **T-20260729T065622769184Z-628504fe**\\n- Role: Implementer (Claude Code)\\n- Status: **ready** \\u2014 implementation complete and locally verified; delivery (commit/push/PR/MergeGate) is owned by the Shiki loop.\\n\\n## What changed or decided\\n- **`scripts/shiki_guardian_status.py`** (new): `shiki guardian status --pr N`. Runs the existing `evaluate_guardian_approval` over evidence (live `gh` by default; offline file mode for CI/tests) and reports (a) satisfied sources and by whom, (b) the exact missing blockers, (c) a paste-ready comment body carrying the policy marker + the **full 40-char** head SHA read from live PR state, and (d) a head-SHA-stability check (`detect_sha_change`) that **withholds** the approval body when an unpushed commit or unsynced `.shiki/ledger/` evidence means the SHA is about to change. Read-only: never applies a label, approves, merges, or posts.\\n- **`scripts/shiki_cli.py`**: registered the `guardian status` subparser and imported `cmd_guardian_status`.\\n- **`scripts/shiki_installer.py`**: added `scripts/shiki_guardian_status.py` to `TEMPLATE_PATHS` so it stages into targets (prevents `No module named shiki_guardian_status` in the target's `validate_shiki` CLI module-boundary check).\\n- **`.github/workflows/shiki-cca-completion.yml`**: new `if: always()` step that, when the CCA verdict is `needs_guardian`, renders the same blockers + paste-ready body via `shiki guardian status` (offline, over already-fetched evidence) and posts/updates an idempotent PR comment (hidden `<!-- shiki:guardian-status -->` marker).\\n- **`tests/test_guardian_status.py`** (new): 13 behavior tests, including a round-trip proving the printed body actually satisfies the authoritative evaluator.\\n- **`scripts/test_shiki_guardian_policy.sh`**: added CLI offline-behavior assertions, a read-only grep guard, workflow-wiring assertions, and an install-target + target-`validate_shiki` check.\\n\\n## Checklist status\\n- Skill Gate: `tdd` followed (tests \\u2192 impl, run each cycle); `code-review` gate is user-triggered (`/code-review`, not model-invocable) \\u2014 performed a manual review against the standard instead (no findings; changes stay within locks).\\n- TDD: new suite 13/13 green; full `python3 -m unittest discover -s tests` = **515 passed**.\\n- CCA/MergeGate: not run locally (GitHub-side); wiring verified via `validate_shiki` + shell-test greps.\\n\\n## Evidence (local, diagnostic)\\n- `python3 scripts/validate_shiki.py` \\u2192 passed\\n- `python3 -m unittest discover -s tests` \\u2192 **Ran 515, OK**\\n- `test_shiki_module_boundaries.sh`, `test_shiki_workflow_lint.sh`, `test_shiki_shellcheck.sh`, `test_shiki_governance_evidence.sh` \\u2192 all passed\\n- `test_shiki_guardian_policy.sh` (deliberately excluded from `test_command`) \\u2192 **exit 0** in this checkout, where `shiki doctor` reports `doctor.worktrees.unregistered: pass` (acceptance check 8 satisfied and recorded here)\\n- install-target into a fresh temp target \\u2192 module staged, target `validate_shiki` \\u2192 passed, target `import shiki_cli`/`shiki_guardian_status` \\u2192 OK (acceptance check 7)\\n- Branch: `shiki/t-20260729t065622769184z-628504fe-...` (no commit/push/PR performed \\u2014 loop owns delivery)\\n\\n## Next action\\n- Hand back to the Shiki loop to commit, push, open the PR with the MergeGate body, and run CCA/MergeGate. This is a **risk:high** task, so merge requires Guardian approval \\u2014 and this PR will exercise its own new needs_guardian comment step (the `pull_request` run uses the workflow + module from the PR head).\\n\\nOne note for the reviewer/loop: the runtime PR-comment post is only provable as durable CI evidence; locally I verified the rendering and the workflow wiring, not the live `gh` post.\\n\",\n.shiki/runner/EXEC-20260805T044317268750Z-6d2e9ea1.json:8:  \"stdout\": \"Implementation complete and verified locally. Here's the summary.\\n\\n## Result\\n- Goal/task: `G-\\u20264459a890` / `T-20260805T034218808409Z-0f0d9640` \\u2014 make a granted lock a precondition of dispatch\\n- Role: Implementer (TDD)\\n- Status: ready (implementation done + locally verified; the Shiki loop owns commit/push/PR/CCA/MergeGate)\\n\\n## What changed\\nA task could be dispatched, implemented, and merged while its lock grant did not exist \\u2014 the only signal appeared three steps downstream as an unexplained Guardian demand at closeout. Now a granted lock is enforced at dispatch.\\n\\n- **`scripts/shiki_tasks.py`**\\n  - `evaluate_lock_grant` rewritten as a **pure, side-effect-free** predicate `(granted, reason)`: empty locks \\u2192 vacuously granted; otherwise the record must exist, be readable (via `load_lock_record`, so malformed \\u2192 refuse, not crash), be `state:active`, and cover every declared lock. Refusals name the task, the missing/stale locks, and any conflicting owner.\\n  - The amendment auto-refresh was **relocated** out of the predicate into a new `_refresh_amendment_grant` helper called only by `cmd_dispatch_check` (layered over the pure predicate; still emits the exact `grant/contract divergence` reason existing tests grep).\\n  - `cmd_task_status` refuses moving a task **into `ready`** when ungranted (same reason); other transitions untouched.\\n  - `cmd_lock_acquire` conflict path documented \\u2014 returns non-zero, writes no record; that absence is the legible ungranted state the preconditions read.\\n- **`scripts/shiki_runtime.py`**: `dispatch_runner_task` refuses an ungranted task right after the status check (before any runner starts); `dispatchable_task_ids` excludes ungranted tasks; `cmd_runner_execute` only advertises `ready` when the grant holds \\u2014 all via the shared predicate, none acquiring as a side effect.\\n- **Tests**: new `tests/test_dispatch_lock_precondition.py` (13 tests covering every acceptance check, incl. the 2026-08-04 conflicted-acquire\\u2192dispatch sequence and the \\\"no side effect\\\" invariant); updated `tests/test_lock_lifecycle.py` so its `cmd_dispatch_check` driver agrees with the pure predicate and keeps the amendment auto-refresh covered where it now lives.\\n\\n## Checklist status\\n- Skill Gate: `tdd` (red \\u2192 green), `code-review` (self-reviewed diff)\\n- TDD: new tests written failing first, then implemented to green\\n- CCA/MergeGate: not run locally (owned by the loop/GitHub)\\n\\n## Evidence (local, diagnostic)\\n- Full Python suite: **982 tests, OK**\\n- New + lifecycle lock tests: **30 tests, OK**\\n- 5 required shell tests pass unchanged: `runner_claude`, `daemon_runner`, `loop_lock_guard`, `control_plane` (incl. amendment auto-refresh + divergence assertions), `goal_loop`; plus `runner_codex`, `code_review_gate`, `run_orchestrator`, `module_boundaries`, `state_classes`, `loop_e2e`\\n- `validate_shiki`: passed; `py_compile`: clean\\n- Changes strictly within declared locks; `.shiki/tasks|locks/*0f0d9640.json` are the loop's own pre-existing records (untouched)\\n\\n## Next action\\n- Loop delivers (commit/push/PR with MergeGate body), then CCA judgment + MergeGate. No approval records were authored (none were mine to author).\\n\\nOne design note worth flagging for review: this task made `evaluate_lock_grant` pure and moved the merged amendment auto-refresh feature into `cmd_dispatch_check` (rather than removing it) \\u2014 that keeps `test_shiki_control_plane.sh`'s amendment/divergence assertions green while satisfying the new \\\"never acquires a lock as a side effect\\\" requirement.\\n\",\n.shiki/worktrees/T-20260612T055157606542Z-5205e0bd.json:2:  \"branch\": \"shiki/g-d76238aa-code-review-gate\",\n.shiki/schemas/start.schema.json:10:    \"skills_dir\",\n.shiki/schemas/start.schema.json:24:    \"skills_dir\": { \"type\": \"string\", \"minLength\": 1 },\n.shiki/runner/EXEC-20260729T220144471749Z-dee61de4.json:8:  \"stdout\": \"All gates pass. The work is complete. Here's the summary.\\n\\n## Result\\n- Goal/task: G-20260729T065622761503Z-4479b0d3 / **T-20260729T065622770500Z-513839f0**\\n- Role: Implementer (Claude Code)\\n- Status: **ready** (implementation complete in worktree; the Shiki loop owns commit/PR/merge)\\n\\n## What changed\\nRequired the CCA to evaluate *every* blocking checklist item and acceptance criterion even when a blocker is already known, and made the enforcer reject a verdict that short-circuits with an \\\"already blocked\\\" reason.\\n\\n- **`scripts/enforce_cca_verdict.py`** \\u2014 new `short_circuited_evaluations()` + `_is_already_blocked_reason()`, wired into `validate_verdict()` to raise on any blocking checklist item or acceptance criterion left `insufficient_evidence` with an \\\"already blocked / verdict already determined / not evaluated because a blocker exists\\\" reason. Detection is a **two-signal** design (strong standalone phrase, OR skip-signal + already-blocked-external-cause) so it does *not* false-positive on genuine reasons that name their own missing evidence.\\n- **`.github/prompts/cca-completion-check.md`** \\u2014 added judgment rules instructing the judge to evaluate every blocking item even when one already fails, to not use \\\"already blocked\\\" style reasons (the enforcer rejects them), and to reserve `insufficient_evidence` for genuinely-missing evidence; clarified the \\\"stop reading\\\" line so bounding *reading* never licenses skipping *evaluation*.\\n- **`tests/test_cca_completeness.py`** (new) \\u2014 11 tests: short-circuit detection + variants, acceptance-criterion coverage, non-blocking exclusion, integration rejection (mutation guard), accept-a-fully-evaluated-blocked-verdict, and adversarial genuine-reason near-misses that must stay accepted; plus prompt-instruction assertions.\\n- **`scripts/test_shiki_control_plane.sh`** \\u2014 two shell fixtures: a short-circuited verdict is rejected (`blocking evaluation short-circuited`); a fully-evaluated `blocked` verdict passes validation yet still blocks MergeGate.\\n\\n## Acceptance checks \\u2014 all met\\n- \\u2705 Short-circuited blocking item \\u2192 rejected by enforcer (`test_short_circuited_blocking_item_is_rejected`, shell fixture).\\n- \\u2705 Fully-evaluated blocking items \\u2192 accepted regardless of verdict value (`test_fully_evaluated_blocked_verdict_is_accepted`, shell fixture).\\n- \\u2705 Prompt instructs evaluating every blocking item even when one already fails.\\n- \\u2705 Mutation guard: removing the rejection fails 2 unittests **and** the control-plane shell test (verified both directions).\\n\\n## Checklist status\\n- Skill Gate: **tdd** (red \\u2192 green \\u2192 mutation-guard verified), **code-review** (subagent review caught an over-broad-marker false-positive defect; fixed via two-signal detector + adversarial tests)\\n- TDD: tests written first, confirmed red, then green\\n- CCA/MergeGate: n/a locally \\u2014 loop-owned\\n\\n## Evidence (local, diagnostic)\\n- `python3 -m unittest discover -s tests` \\u2192 **470 passed** (incl. 11 new)\\n- `bash scripts/test_shiki_control_plane.sh` \\u2192 passed\\n- `python3 scripts/validate_shiki.py` \\u2192 passed; `py_compile scripts/*.py` \\u2192 ok; `test_shiki_shellcheck.sh --strict` \\u2192 passed\\n- Scope: exactly the 4 locked files (`git status` clean otherwise)\\n\\n## Next action\\n- Hand back to the Shiki loop to commit, open the PR with the MergeGate body, and run CCA/MergeGate. I did **not** run any git/gh commands, per the execution protocol.\\n\",\n.shiki/schemas/repair-packet.schema.json:47:        \"code-review\",\n.shiki/runner/EXEC-20260807T044033306911Z-c7af7eef.json:8:  \"stdout\": \"The full acceptance gate passes end-to-end (chain exit 0). The implementation is complete.\\n\\n## Result\\n- **Goal/task:** G-20260807T035659158223Z-af599ebb / T-20260807T035659159912Z-f2e1c0b0\\n- **Role:** Implementer (Claude Code)\\n- **Status:** ready\\n\\n## What changed\\nChanged the repair-delivery discriminator in `scripts/shiki_loop.py` from \\\"did the PR head move\\\" to \\\"did the repair change something the **implementer** owns.\\\"\\n\\n- **`_loop_authored_repair_paths()`** (new) \\u2014 the narrow, named set of `.shiki` paths the loop itself (re)writes into the worktree each cycle: the task's own mirror record `.shiki/tasks/<id>.json` (status/`cca_rerun_count`/`ledger_evidence`) plus the `.shiki/locks/<id>.json` and `.shiki/goals/<goal>.json` that `sync_contract_into_worktree` copies in before every session. Deliberately **not** all of `.shiki` \\u2014 the code says so.\\n- **`_repair_implementer_changes()`** (new) \\u2014 diffs the pushed commit range `head_before..head_after`, strips loop bookkeeping, and returns what the implementer actually changed. Returns `None` (\\u2192 proceed) when the per-push delta can't be scoped (a first push carries the whole branch = real delivery); `[]` (\\u2192 stop) when the head moved on bookkeeping alone.\\n- **New guard** in `execute_action`'s `dispatch_repair` branch, right after the existing head-movement check \\u2014 same stop action, same reason shape, same position. Only the discriminator changed; the existing push-failure and head-did-not-advance branches are untouched.\\n\\nWhy the measured PR #310 loop happened: `_commit_and_push_implementation` does `git add -A`, so the runner-dispatch's rewrite of the task's own mirror record moved the head on every attempt even with zero source change \\u2014 the old guard couldn't see it and burned all three attempts to `stop_guardian`. The new guard stops at attempt 1.\\n\\n## Checklist status\\n- **TDD:** `tests/test_repair_delivery.py` \\u2014 3 new tests (bookkeeping-only stops with the measured one-file `.shiki/tasks/<id>.json` fixture + one packet; source-only proceeds; both proceed); existing delivery-failure and head-did-not-move cases unchanged.\\n- **code-review:** self-reviewed \\u2014 range diff is valid (non-FF push fails the earlier guard), `None` vs `[]` semantics verified, exclusion is narrow.\\n\\n## Evidence\\n- `python3 -m unittest discover -s tests` \\u2192 **1117 OK**\\n- `bash scripts/test_shiki_goal_loop.sh` \\u2192 **shiki goal loop tests passed** (caught and fixed a first-push `head_before is None` false-stop before it could regress)\\n- `python3 scripts/validate_shiki.py --target .` \\u2192 **Shiki validation passed**\\n- Chained `test_command` exit 0. Only the two locked files edited (`scripts/shiki_loop.py`, `tests/test_repair_delivery.py`).\\n\\n## Next action\\nLoop owns delivery \\u2014 it will commit, push, open the MergeGate PR, and merge. No git/PR actions taken here per protocol.\\n\",\n.shiki/runner/EXEC-20260730T014856648878Z-a94c5740.json:8:  \"stdout\": \"Implementation complete. Only the in-scope files changed, and I left commit/push/PR to the loop as required.\\n\\n## Result\\n- Goal/task: G-20260729T065622761503Z-4479b0d3 / T-20260729T065622765413Z-a0055a9d\\n- Role: Implementer (Claude Code)\\n- Status: complete\\n\\n## What changed\\nAll three fixes landed in `scripts/shiki_loop.py`, TDD-driven by new `tests/test_closeout.py`:\\n\\n- **(a) Fail-closed `completes_goal`** (`_create_closeout_pr`): now computes the goal's task-id set from the **coordinator's** `tasks_for_goal(target, \\u2026)` and requires every id to be present in the closeout worktree **and** `done` there. The old `all(status==done for tasks_for_goal(worktree))` was vacuously true for a multi-task goal whose first task merged but whose siblings weren't yet on main \\u2014 completing the goal after one task. A single-task goal still completes; a multi-task goal completes only on its last task's closeout.\\n- **(b) Scoped goal-complete sync** (new `_sync_goal_complete_mirror` + `_loop_own_ledger_ids`): replaced the whole-tree `git checkout origin/main -- .shiki` with an explicit per-goal pathspec (goal, dag, tasks, locks, worktrees, plus the ledgers/reports those main-side files reference), filtered to paths actually present on `origin/main`. It captures the loop's own merge (`mergegate`) and mark_done (`Goal loop marked \\u2026`) ledger ids before the sync and re-appends them after (they live only in the coordinator mirror), then unstages every synced path so nothing is left staged. Fail-open preserved.\\n- **(c) CCA rerun reset**: the coordinator closeout repoint block now sets `cca_rerun_count = 0`, giving the fresh closeout PR head its own rerun budget so a `cca_rerun_count == 2` impl PR yields `rerun_cca`, not `stop_blocked`.\\n\\n## Checklist status\\n- Skill Gate: `tdd` (red\\u2192green), `code-review` (self-review of the diff; loop runs its own independent pre-PR reviewer at PR creation)\\n- TDD: `tests/test_closeout.py` \\u2014 6 tests, one per acceptance check (4 failed pre-fix, all 6 green post-fix)\\n- Scope: only `scripts/shiki_loop.py` + `tests/test_closeout.py` touched; shell tests left unmodified (they stay single-task per ADR 0012 and still pass)\\n\\n## Evidence\\n- `tests/test_closeout.py`: 6/6 OK\\n- `python3 -m unittest discover -s tests`: **508 tests OK** (incl. 106 loop tests, repair-delivery)\\n- `bash scripts/test_shiki_goal_loop.sh`: passed \\u00b7 `bash scripts/test_shiki_loop_e2e.sh`: passed\\n- `python3 scripts/validate_shiki.py`: passed\\n- Working tree: `M scripts/shiki_loop.py`, `?? tests/test_closeout.py`; HEAD unchanged (no commit/push/PR by me)\\n\\n## Next action\\n- The Shiki goal loop owns delivery: it will commit, push, open the MergeGate PR, run the independent pre-PR code-review + CCA, and merge. No action needed from me.\\n\\nTwo acceptance checks (control case, single-task) passed even before the fix, confirming they're genuine controls; the four defect checks failed pre-fix and pass now.\\n\",\n.shiki/runner/EXEC-20260728T114137576511Z-2566ca4e.json:8:  \"stdout\": \"Everything is green and scoped. Implementation is complete and locally verified. Per the loop protocol, I've made no commits, PRs, or ledger entries \\u2014 the goal loop owns delivery.\\n\\n## Result\\n- **Goal/task:** G-20260728T103742822780Z-2514c606 / T-20260728T103742824401Z-f198cd24 \\u2014 inject id-scoped mirror locks at task registration\\n- **Role:** Implementer\\n- **Status:** complete (implementation + local verification; loop owns commit/PR/merge)\\n\\n## What changed\\n- **`scripts/shiki_tasks.py`** \\u2014 Added `mirror_locks_for_task(task_id, goal_id, ledger_ids)`: a deterministic, id-scoped mirror lock set (task file, worktree, lock record, goal file, DAG, owned ledgers) \\u2014 every entry a concrete single-file path, never a shared-namespace glob. Plus `injected_mirror_locks`, idempotent `inject_mirror_locks`, and `_refresh_lock_record_locks`. Wired injection into all registration/dispatch paths (`register_task_from_plan`, `cmd_issue_plan`, `try_acquire_locks`, `allocate_worktree_record`, `cmd_lock_acquire`, `cmd_worktree_allocate`).\\n- **`scripts/shiki_locks.py`** \\u2014 Added `is_goal_scoped_mirror_lock` and a **same-goal exemption** in `active_lock_conflicts`: two sibling tasks may hold the identical goal-file/DAG mirror lock (shared read-only goal state) without a false conflict. Product/glob overlaps still conflict.\\n- **`scripts/mergegate_check.py`** \\u2014 `_frozen_task_match_errors` now binds locks via `_frozen_locks_match_errors`: recomputes the injected set from the task's own ids and rejects any lock outside `frozen \\u222a injected` (blocks `path:.shiki/**` and foreign task mirror locks) and any dropped frozen lock.\\n- **`tests/test_mirror_lock_injection.py`** (new, 20 tests) + assertions in `test_shiki_control_plane.sh`.\\n\\n**Design note:** the scope requires the goal file + DAG in the mirror set, but same-goal tasks share those, so full set-disjointness is impossible. I resolved this as the acceptance intends \\u2014 task-owned locks are strictly disjoint, and the shared goal-scoped locks are exempted from conflict detection so sibling tasks never falsely block each other (verified via `orchestrate_plan` dispatching both).\\n\\n## Checklist status\\n- **TDD:** red \\u2192 green; 20 new tests, all 7 acceptance checks covered\\n- **Code-review gate:** performed manually (the `/code-review` skill is user-triggered/model-invocation-disabled); fixed one self-caught regression (lock-record/task-locks desync breaking `dispatch_check`) and an import-ordering nit before finalizing\\n- **Full suite:** 432/432 Python tests pass; 32/34 shell tests pass\\n\\n## Evidence\\n- Python: `Ran 432 tests ... OK`; new suite `Ran 20 tests ... OK`\\n- Shell: `test_shiki_control_plane`, `run_orchestrator`, `goal_loop`, `loop_e2e`, `loop_lock_guard`, contract/reconcile/mergegate tests all pass\\n- The 2 shell failures (`guardian_policy`, `runtime_auth`) reproduce identically on the clean base \\u2192 pre-existing/environmental, not from this change\\n- All changes within declared locks; no writes to the real `.shiki` mirror\\n\\n## Next action\\n- Loop runs the TDD gate (`test_command`) and code-review verifier, then owns commit/push/PR/merge.\\n- **Follow-up (out of my lock scope):** `validate_shiki.py`'s advisory `loop_lock_warnings` still recommends `path:.shiki/**` coverage, which is now semantically stale for id-scoped tasks. It's advisory-only (never blocks), but a future task locking `scripts/validate_shiki.py` should teach it to recognize id-scoped mirror coverage.\\n\",\n.shiki/runner/EXEC-20260805T060409375330Z-4b90603b.json:8:  \"stdout\": \"All required suites pass. Here is the summary.\\n\\n## Result\\n- Goal/task: G-20260805T040948887161Z-8678e914 / **T-20260805T040948888613Z-d4e830c9**\\n- Role: Implementer (Claude Code)\\n- Status: **complete** (implementation done; delivery \\u2014 commit/PR/merge \\u2014 is owned by the Shiki loop)\\n\\n## What changed\\nWired the existing `codeowners_required_owner()` resolver into all three ownership sites so a repository owned by anyone can satisfy its own CODEOWNERS check. All edits stayed within the declared locks.\\n\\n- **Enforcement \\u2014 `scripts/validate_shiki.py`**: `validate_codeowners_governance(root=ROOT)` now resolves `required_owner = codeowners_required_owner(root)` and checks against it. `main()` still calls it argless, so `ROOT` (the running repo, which is the target root when validate runs inside a target) is the default \\u2014 platform behavior unchanged.\\n- **Enforcement \\u2014 `scripts/shiki_doctor.py`**: `_codeowners_findings(target)` resolves the owner per target (`:426` import + `:454/:463/:464`). Constant stays the documented fallback.\\n- **Target write \\u2014 `scripts/shiki_installer.py`**: `install_template` resolves the owner once and threads `codeowners_owner` through `copy_path`. The fresh-write path substitutes the owner into `.github/CODEOWNERS` only when `target_install and owner != constant`; otherwise it copies verbatim (no-repo.json installs are byte-identical to today). The `.new` path is left verbatim on purpose. This repo's own copy is never touched (install only writes to a target dir).\\n- **Fixtures \\u2014 `scripts/test_shiki_doctor.sh`**: `make_target` now seeds `.shiki/repo.json` *before* `install-target` (so substitution fires); parameterized so `NO_REPO` installs with no repo.json and the fallback owner matches the shipped file. No assertion weakened.\\n- **Fixtures \\u2014 `scripts/test_shiki_start.sh`**: added `seed_repo_json` and seeded `$TARGET` + the three retry targets before their first `start` (start validates at the end of install and keeps an existing CODEOWNERS on re-run, so an unseeded 2nd run would fail install-validate). No assertion weakened.\\n- **Tests**: new `tests/test_codeowners_wiring.py` (5 cases incl. the load-bearing foreign-owner target passing both `validate_codeowners_governance` **and** `doctor.codeowners.coverage`); extended `tests/test_install_force_semantics.py` with an installed foreign-owner target passing the wired gate.\\n\\n## Checklist status\\n- Skill Gate: `tdd` (red \\u2192 green: new test failed pre-wiring, passes after), `code-review` (self-review of installer/enforcement diffs) \\u2705\\n- TDD: \\u2705  \\u00b7 CCA: n/a (loop-owned) \\u00b7 MergeGate: n/a (loop-owned)\\n\\n## Evidence (local, diagnostic)\\n- `python3 scripts/validate_shiki.py` \\u2192 **passed**\\n- `python3 -m unittest discover -s tests` \\u2192 **Ran 1019 tests, OK** (+6 new)\\n- `test_shiki_doctor.sh`, `test_shiki_start.sh`, `test_shiki_init.sh`, `test_shiki_control_plane.sh` \\u2192 all **passed** (+ `test_shiki_shellcheck.sh` passed)\\n- `codeowners_required_owner()` now has production consumers in `validate_shiki.py`, `shiki_doctor.py`, `shiki_installer.py`\\n- `git status`: only locked files changed; `.github/CODEOWNERS` untouched\\n\\n## Note (out-of-locks follow-up, not a blocker)\\nThe installer/enforcement are consistently **repo.json-based**. There is a residual gap in `scripts/shiki_bootstrap.py` `cmd_init` (not in this task's locks): it runs `install_template` **before** `write_target_repo_config`, so a brand-new `shiki init --repo acme/x` still ships the fallback owner until repo.json exists (the fixtures seed repo.json first to exercise the wired path, which also matches any re-run since start persists repo.json). Closing that end-to-end for a fresh first-run init would need `shiki_bootstrap.py` in scope \\u2014 reported as a stop rather than reached into. Recorded in memory (`codeowners-owner-wiring`).\\n\\n## Next action\\nLoop owns commit \\u2192 PR (with MergeGate body) \\u2192 CCA \\u2192 merge.\\n\",\n.shiki/runner/EXEC-20260731T012003321054Z-03d235ba.json:8:  \"stdout\": \"All green, only the four locked files changed. The implementation is complete.\\n\\n## Result\\n- **Goal/task:** G-20260731T004111130646Z-0dbace30 / T-20260731T004111132162Z-bfcf2c9e \\u2014 derive the bookkeeping-closeout Guardian exemption (ADR 0017) in MergeGate\\n- **Role:** Implementer (TDD + code-review)\\n- **Status:** ready \\u2014 with one companion wiring change flagged (see Next action)\\n\\n## What changed\\n- **`scripts/mergegate_check.py`** \\u2014 added `is_bookkeeping_closeout(...)` + helper `_closeout_completes_goal(...)`, enforcing all six ADR 0017 conditions from **the diff, the base snapshot, and live PR state only** (it never reads the PR object, so a label/marker/body claim can't classify a PR). It **fails closed** on every axis (missing base snapshot, unreadable task file, unresolvable impl PR, missing DAG). The exemption is consulted at the *single* decision point: `_guardian_risk_labels(..., bookkeeping_closeout=...)` now treats a proven closeout's task as low risk, so `_builtin_guardian_risk_required` and the policy check both see it as low \\u2014 every other gate runs unchanged. `merged_prs` parsing was lifted to the top of `main()` and shared with `post_merge_reconcile`.\\n- **`tests/test_bookkeeping_closeout.py`** (new) \\u2014 23 tests: positive completing + non-completing cases, and **every disqualifier in isolation**, each asserting the **Guardian-requirement fallback** (via the real `enforce_guardian_policy` over the repo policy *and* the decision function), not merely that the PR blocks.\\n- **`scripts/test_shiki_adversarial_state.sh`** & **`scripts/test_shiki_governance_evidence.sh`** \\u2014 added the six smuggling cases (non-`.shiki` file; governance-field edit; deleted mirror record; unexpected status; second scorecard; impl PR not merged) + a clean control, each asserting the specific Guardian fallback string.\\n\\n## Checklist status\\n- **TDD:** classifier + full disqualifier matrix; **mutation-tested** \\u2014 forcing the classifier always-True *and* always-False both turn the shell suites red, proving the assertions catch regressions in both directions.\\n- **code-review:** self-review tightened reports/ledgers to append-only (`A`) to match the terminal-set and the repo's append-only-ledger invariant.\\n- **MergeGate:** N/A locally (loop owns delivery).\\n\\n## Evidence\\n- `validate_shiki` PASS \\u00b7 Python unittest 560 PASS (incl. 23 new) \\u00b7 control_plane PASS (unmodified) \\u00b7 governance_evidence PASS \\u00b7 adversarial_state PASS \\u00b7 shellcheck PASS\\n- Only the four declared locks touched.\\n\\n## Next action (companion change \\u2014 outside this task's locks)\\nThe exemption is **correct and fully tested but dormant in production**: `.github/workflows/shiki-cca-completion.yml`'s \\\"Enforce MergeGate\\\" step computes `MERGED_PRS` **only** for `post_merge_reconcile`-marked PRs, so a normal closeout gets `--merged-prs \\\"\\\"` \\u2192 condition 6 (impl PR merged) fails \\u2192 no exemption. To achieve the reproducibility goal (`shiki loop step` driving a high-risk closeout to merge with no Guardian approval), a follow-up task must generalize that block to resolve `<body task id> \\u2192 base expected_pr \\u2192 gh pr view state` for closeout PRs too (safe: `--merged-prs` is independently re-validated by both consumers). The workflow file is outside this task's locks, so I did not touch it \\u2014 recorded in memory for the operator/next task.\\n\",\n.shiki/runner/EXEC-20260730T061303753882Z-d7891d4c.json:8:  \"stdout\": \"All checks pass and no linter gate exists (no ruff/flake8 config; the new file matches the sibling suite's style). Implementation is complete and verified.\\n\\n## Result\\n- **Goal/task:** G-20260730T060551621252Z-6ab5c407 / T-20260730T060551622687Z-7bea1bc2\\n- **Role:** Implementer (Claude Code)\\n- **Status:** ready (implementation complete and verified; the Shiki loop owns commit/push/PR/merge)\\n\\n## What changed or decided\\n- **`scripts/mergegate_check.py`** (`_derive_task_mirror_locks`): split the combined `goal_id`/`task_id` bound into two guards so a **goal-level ledger of the task's own goal** (`task_id` empty, e.g. `cmd_goal_complete`'s `type:completion` entry) is expanded, while every foreign reference stays rejected:\\n  ```python\\n  entry_goal = str(entry.get(\\\"goal_id\\\") or \\\"\\\")\\n  entry_task = str(entry.get(\\\"task_id\\\") or \\\"\\\")\\n  if entry_goal != goal_id:          continue   # foreign goal: never\\n  if entry_task and entry_task != task_id: continue  # a DIFFERENT task's ledger: never\\n  # entry_task empty -> a goal-level ledger of this goal\\n  ```\\n  The `.shiki/`-prefix and resolve-and-contain checks on each evidence ref are untouched. Docstring updated (it no longer claims \\\"goal_id AND task_id match\\\").\\n- **`tests/test_completion_lock_scope.py`** (new): covers the goal-level completion ledger widening (ledger + report paths), foreign-goal \\u2192 nothing, different-task-id/same-goal \\u2192 nothing (anti-padding regression guard), escaping ref (`.shiki/../outside.json`) dropped, non-`.shiki`-prefixed ref dropped, and an end-to-end PR #199-shaped closeout (three named-file locks + mirror files + completion ledger + scorecard) with **no** files-outside-locks blocker.\\n\\n## Checklist status\\n- **Skill Gate:** tdd (red \\u2192 green demonstrated), code-review (self-review of the 5-line logic change; scope, anti-padding, and containment preserved)\\n- **TDD:** 4 tests red against original source \\u2192 all 7 green after the fix\\n- **MergeGate:** loop-owned; no git/gh/PR actions taken per the Execution Protocol\\n\\n## Evidence\\n- New suite: `python3 -m unittest discover -s tests -p test_completion_lock_scope.py` \\u2192 **Ran 7, OK**\\n- Sibling suite `test_mirror_lock_injection.py` \\u2192 **Ran 21, OK** (no regression)\\n- Full python suite \\u2192 **Ran 509 tests, OK**\\n- `bash scripts/test_shiki_adversarial_state.sh` \\u2192 **passed**; `bash scripts/test_shiki_control_plane.sh` \\u2192 **passed** (both unmodified)\\n- `git status --short`: only `scripts/mergegate_check.py` (M) and `tests/test_completion_lock_scope.py` (??) \\u2014 no file outside the declared locks changed\\n\\n## Next action\\n- Hand back to the Shiki loop to commit, push, open the MergeGate PR, run CCA, and merge. No blockers; no approval record was authored.\\n\",\n.shiki/runner/EXEC-20260730T015656370108Z-766d4b92.json:8:  \"stdout\": \"Shiki validation passed\\nshiki goal loop tests passed\\nShiki validation passed\\ne2e: loop-owned T3 code-review THEN T2 tdd-evidence ledgers, in the implemented order\\nshiki loop e2e: integrated self-drive (code-review -> TDD -> commit/push -> auto-merge) + post-PR repair verified\\n{\\n  \\\"handoff_file\\\": \\\"/private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/tmpzybxb46x/repo/.shiki/handoffs/RP-20260730T015652034031Z-350d8e29-repair.md\\\",\\n  \\\"ledger_id\\\": \\\"L-20260730T015652051148Z-efb29ca0\\\",\\n  \\\"repair_id\\\": \\\"RP-20260730T015652034031Z-350d8e29\\\"\\n}\\n{\\n  \\\"handoff_file\\\": \\\"/private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/tmphvl663k6/repo/.shiki/handoffs/RP-20260730T015653818408Z-dccdddf1-repair.md\\\",\\n  \\\"ledger_id\\\": \\\"L-20260730T015653834410Z-63d36714\\\",\\n  \\\"repair_id\\\": \\\"RP-20260730T015653818408Z-dccdddf1\\\"\\n}\\n{\\n  \\\"handoff_file\\\": \\\"/private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/tmpl1nizp7n/repo/.shiki/handoffs/RP-20260730T015654216297Z-fa046d2f-repair.md\\\",\\n  \\\"ledger_id\\\": \\\"L-20260730T015654232044Z-4bd2b709\\\",\\n  \\\"repair_id\\\": \\\"RP-20260730T015654216297Z-fa046d2f\\\"\\n}\\n{\\n  \\\"handoff_file\\\": \\\"/private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/tmpocyi5lx3/repo/.shiki/handoffs/RP-20260730T015654701638Z-90ae6d23-repair.md\\\",\\n  \\\"ledger_id\\\": \\\"L-20260730T015654719530Z-8f31ee32\\\",\\n  \\\"repair_id\\\": \\\"RP-20260730T015654701638Z-90ae6d23\\\"\\n}\\n[shiki] token verified: ok\\n[shiki] token verified: ok\\n{\\n  \\\"repo\\\": \\\"owner/name\\\",\\n  \\\"secret\\\": \\\"CLAUDE_CODE_OAUTH_TOKEN\\\",\\n  \\\"source\\\": \\\"env:SHIKI_TEST_TOK\\\",\\n  \\\"verified\\\": true\\n}\\n\",\n.shiki/runner/EXEC-20260729T021403914755Z-92f56794.json:8:  \"stdout\": \"All verification surfaces are green. The work is complete.\\n\\n## Result\\n- **Goal/task:** G-20260728T103742822780Z-2514c606 / T-20260728T103742824401Z-f198cd24\\n- **Role:** Implementer (Claude Code, default runtime)\\n- **Status:** ready (implementation complete, all acceptance checks pass; loop owns commit/PR/merge)\\n\\n## What changed or decided\\nThe prior session had built the direct-subtree mirror-lock injection, goal-scoped conflict exemption, and the MergeGate frozen-lock binding (20 passing tests). My probe proved the **transitive** requirement was unmet: `mirror_locks_for_task` derived only direct subtrees, so a task's `.shiki/runner/EXEC-*.json` (referenced inside a check ledger's `evidence` array) fell **outside** its locks the first time it ran `test_command` \\u2014 and the suite had no post-execution fixture. I closed that gap:\\n\\n- **`scripts/shiki_tasks.py`** \\u2014 `mirror_locks_for_task` gained an optional `target`; when given, it resolves every `.shiki`-prefixed path in each ledger's `evidence` array (via new helper `_ledger_evidence_shiki_refs`, mirroring `shiki_loop._evidence_relatives_for_task`'s containment/traversal guard \\u2014 no `is_file` requirement so injection-time and MergeGate-time recomputes stay deterministic and agree). Threaded `target` through `injected_mirror_locks`/`inject_mirror_locks` and all 10 registration/re-registration call sites.\\n- **`scripts/mergegate_check.py`** \\u2014 threaded `target` through `_frozen_task_match_errors` \\u2192 `_frozen_locks_match_errors` \\u2192 `mirror_locks_for_task`, and passed it from `_enforce_frozen_registration`, so the binding recomputes the *same* transitive set (still rejects `path:.shiki/**` and any foreign task's mirror lock).\\n- **`tests/test_mirror_lock_injection.py`** \\u2014 added `TransitiveEvidenceTests` (6 tests) with a real post-execution EXEC check ledger and a completion\\u2192report ledger, proving derivation coverage, `files_outside_locks` empty after a `test_command` run, direct-only fallback without `target`, idempotent re-injection, and the transitive-aware binding.\\n\\n## Checklist status\\n- Skill Gate: tdd (red\\u2192green: 5 failing \\u2192 26 passing), code-review (self-reviewed full diff)\\n- TDD: \\u2705 failing transitive tests written first, then implementation\\n- All 9 acceptance checks: \\u2705 mapped to passing tests (note: handoff says \\\"27 tasks across four frozen plans\\\" \\u2014 the actual repo has **19** across 4 frozen plans; **all 19 pass** `files_outside_locks`; the corpus test's `>= 19` bound matches exactly)\\n- MergeGate: contract (20), goal_reconcile (45), contract_open (8), loop_e2e_contract (8) all green \\u2014 no regression\\n\\n## Evidence\\n- Checks: mirror-injection suite **26 OK**; full python suite **438 OK**; `test_shiki_control_plane.sh` **OK**; `test_shiki_loop_lock_guard.sh` **ok**\\n- Diff: only locked files touched (`shiki_tasks.py`, `mergegate_check.py`, `shiki_locks.py`, `test_shiki_control_plane.sh`, `test_mirror_lock_injection.py`) \\u2014 nothing outside `Locks`\\n- Branch: `shiki/t-20260728t103742824401z-f198cd24-inject-id-scoped-mirror-locks-at-task-registrati`\\n\\n## Next action / risk to surface\\n**Downstream integration boundary (outside this task's locks).** I simulated the full production loop sequence: after dispatch, the loop's execution effectors append a code-review check ledger, an EXEC check ledger + EXEC record, and a PR-handoff ledger \\u2014 but neither `shiki_loop.py` nor `shiki_github.py` re-injects before `_sync_state_to_branch`, so those four synced artifacts currently land outside the id-scoped locks (`files_outside_locks` returned 4 uncovered paths). This task correctly delivers the registration/re-registration injection + the transitive-capable `inject_mirror_locks(task, target)` primitive; making the loop cover its *post-dispatch* evidence end-to-end requires that primitive to be called from the loop's create-PR path (a `shiki_loop.py`/`shiki_github.py` change \\u2014 not in this task's locks, and consistent with the scope confining injection to registration points). Flagging so it's tracked as the consuming step (likely a PRD 0002 sibling task), not silently assumed done. The stale `validate_shiki.py` docstring reference to `shiki_tasks.loop_guaranteed_locks` and its now-misfiring `.shiki/**` WARN-only hint are also outside these locks.\\n\",\n.shiki/runner/EXEC-20260729T233228048132Z-ee061ba4.json:7:  \"stderr\": \"[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-protect/bin/shiki\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-protect/AGENTS.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-protect/CLAUDE.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-protect/CONTEXT.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-protect/SYSTEM_PROMPT.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-protect/.claude/commands/shiki.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-protect/.codex/skills/shiki/SKILL.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-protect/.shiki/migrations/state.json\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-protect/.shiki/guardian-policy.json\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-protect/.shiki/config.yaml\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-protect/.shiki/schemas/runner-record.schema.json\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-protect/.shiki/schemas/report.schema.json\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-protect/.shiki/schemas/dispatch-guard.schema.json\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-protect/.shiki/schemas/external-ai-guardian-review-packet.schema.json\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-protect/.shiki/schemas/run.schema.json\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-protect/.shiki/schemas/worktree-record.schema.json\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-protect/.shiki/schemas/cca-evidence-manifest.schema.json\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-protect/.shiki/schemas/cca-verdict.schema.json\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-protect/.shiki/schemas/plan.schema.json\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-protect/.shiki/schemas/inbox.schema.json\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-protect/.shiki/schemas/memory-entry.schema.json\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-protect/.shiki/schemas/repair-packet.schema.json\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-protect/.shiki/schemas/smoke.schema.json\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-protect/.shiki/schemas/start.schema.json\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-protect/.shiki/schemas/ledger.schema.json\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-protect/.shiki/schemas/task.schema.json\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-protect/.shiki/schemas/dag.schema.json\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-protect/.shiki/schemas/goal.schema.json\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-protect/.shiki/README.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-protect/.shiki/manifest.json\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-protect/.shiki/templates/LEDGER_ENTRY.template.json\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-protect/.shiki/templates/CCA_VERDICT.template.json\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-protect/.shiki/templates/GOAL.template.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-protect/.shiki/templates/REPORT.template.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-protect/.shiki/templates/DAG.template.json\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-protect/.shiki/templates/worktree-record.schema.json\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-protect/.shiki/templates/REPAIR_PACKET.template.json\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-protect/.shiki/templates/TASK.template.json\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-protect/.shiki/policy.example.yaml\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-protect/.github/ISSUE_TEMPLATE/shiki_goal.yml\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-protect/.github/ISSUE_TEMPLATE/shiki_task.yml\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-protect/.github/CODEOWNERS\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-protect/.github/PULL_REQUEST_TEMPLATE/shiki-task.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-protect/.github/prompts/cca-completion-check.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-protect/.github/prompts/codex-repair-handoff.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-protect/.github/prompts/codex-task-handoff.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-protect/.github/workflows/shiki-validate.yml\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-protect/.github/workflows/shiki-claude-review.yml\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-protect/.github/workflows/shiki-cca-completion.yml\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-protect/.github/workflows/shiki-mergegate.yml\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-protect/.github/workflows/shiki-orchestrator.yml\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-protect/docs/agents/node24-workflow-compatibility.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-protect/docs/agents/checklists.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-protect/docs/agents/codex-handoff.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-protect/docs/agents/bootstrap-command.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-protect/docs/agents/guardian-policy.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-protect/docs/agents/autonomous-loop-e2e-acceptance.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-protect/docs/agents/runtime-auth-model.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-protect/docs/agents/shiki-doctor.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-protect/docs/agents/control-commands.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-protect/docs/agents/memory.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-protect/docs/agents/shiki-migrations.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-protect/docs/agents/decision-control.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-protect/docs/agents/domain.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-protect/docs/agents/skill-gate.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-protect/docs/agents/github-provider-config.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-protect/docs/agents/state-classes.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-protect/docs/agents/completion-check-agent.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-protect/docs/agents/runtime-registry.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-protect/docs/agents/implementation-policy.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-protect/docs/agents/triage-labels.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-protect/docs/agents/shiki-cli-architecture.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-protect/docs/agents/issue-tracker.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-protect/docs/agents/execution-control.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-protect/docs/agents/branch-protection-smoke.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-protect/docs/agents/ledger-schema.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-protect/docs/agents/evidence-integrity.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-protect/docs/adr/0010-external-ai-guardian-review.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-protect/docs/adr/0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-protect/docs/adr/template.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-protect/docs/adr/0015-contract-approval-before-dispatch.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-protect/docs/adr/0001-keep-shiki-as-platform-name.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-protect/docs/adr/0011-autonomous-quality-gate-evidence.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-protect/docs/adr/0007-packaging-and-release.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-protect/docs/adr/0002-use-agents-as-shared-constitution.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-protect/docs/adr/0009-requirements-definition-and-spec-freeze.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-protect/docs/adr/README.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-protect/docs/adr/0012-autonomous-loop-closeout-to-main.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-protect/docs/adr/0013-bootstrap-grants-actions-approve-pull-requests.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-protect/docs/adr/0008-use-claude-code-as-default-implementer.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-protect/docs/adr/0014-codex-app-external-ai-guardian-ui-adapter.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-protect/docs/adr/0004-use-github-first-with-shiki-mirror.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-protect/docs/adr/0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-protect/docs/adr/0003-require-skill-gate-for-mergegate-readiness.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-protect/skills/engineering/shiki/SKILL.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-protect/skills/engineering/setup-matt-pocock-skills/issue-tracker-gitlab.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-protect/skills/engineering/setup-matt-pocock-skills/domain.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-protect/skills/engineering/setup-matt-pocock-skills/triage-labels.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-protect/skills/engineering/setup-matt-pocock-skills/SKILL.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-protect/skills/engineering/setup-matt-pocock-skills/issue-tracker-github.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-protect/skills/engineering/setup-matt-pocock-skills/issue-tracker-local.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-protect/skills/engineering/code-review/SKILL.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-protect/skills/engineering/improve-codebase-architecture/LANGUAGE.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-protect/skills/engineering/improve-codebase-architecture/SKILL.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-protect/skills/engineering/improve-codebase-architecture/HTML-REPORT.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-protect/skills/engineering/improve-codebase-architecture/DEEPENING.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-protect/skills/engineering/improve-codebase-architecture/INTERFACE-DESIGN.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-protect/skills/engineering/triage/OUT-OF-SCOPE.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-protect/skills/engineering/triage/SKILL.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-protect/skills/engineering/triage/AGENT-BRIEF.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-protect/skills/engineering/prototype/SKILL.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-protect/skills/engineering/prototype/LOGIC.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-protect/skills/engineering/prototype/UI.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-protect/skills/engineering/diagnose/scripts/hitl-loop.template.sh\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-protect/skills/engineering/diagnose/SKILL.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-protect/skills/engineering/to-issues/SKILL.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-protect/skills/engineering/README.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-protect/skills/engineering/zoom-out/SKILL.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-protect/skills/engineering/grill-with-docs/SKILL.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-protect/skills/engineering/grill-with-docs/CONTEXT-FORMAT.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-protect/skills/engineering/grill-with-docs/ADR-FORMAT.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-protect/skills/engineering/tdd/mocking.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-protect/skills/engineering/tdd/deep-modules.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-protect/skills/engineering/tdd/tests.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-protect/skills/engineering/tdd/SKILL.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-protect/skills/engineering/tdd/interface-design.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-protect/skills/engineering/tdd/refactoring.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-protect/skills/engineering/to-prd/SKILL.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-protect/scripts/shiki_schema.py\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-protect/scripts/validate_shiki.py\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-protect/scripts/shiki_contracts.py\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-protect/scripts/shiki_jsonschema.py\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-protect/scripts/shiki_evidence.py\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-protect/scripts/shiki_locks.py\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-protect/scripts/shiki_loop.py\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-protect/scripts/shiki_memory.py\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-protect/scripts/shiki_manifest.py\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-protect/scripts/shiki_migrations.py\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-protect/scripts/shiki_provider.py\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-protect/scripts/shiki_workflows.py\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-protect/scripts/enforce_cca_verdict.py\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-protect/scripts/build_cca_evidence_manifest.py\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-protect/scripts/guardian_approval_signal.py\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-protect/scripts/mergegate_check.py\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-protect/scripts/shiki.py\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-protect/scripts/shiki_bootstrap.py\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-protect/scripts/shiki_cli.py\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-protect/scripts/shiki_config.py\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-protect/scripts/shiki_doctor.py\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-protect/scripts/shiki_git.py\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-protect/scripts/shiki_github.py\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-protect/scripts/shiki_guardian.py\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-protect/scripts/shiki_guardian_review.py\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-protect/scripts/shiki_installer.py\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-protect/scripts/shiki_process.py\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-protect/scripts/shiki_runtime.py\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-protect/scripts/shiki_runtime_adapters.py\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-protect/scripts/shiki_runtime_registry.py\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-protect/scripts/shiki_state_classes.py\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-protect/scripts/shiki_tasks.py\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-protect/scripts/shiki_state.py\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-protect/scripts/test_shiki_init.sh\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-protect/scripts/test_shiki_control_plane.sh\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-protect/scripts/test_shiki_run_orchestrator.sh\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-protect/scripts/test_shiki_daemon_runner.sh\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-protect/scripts/test_shiki_runner_codex.sh\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-protect/scripts/test_shiki_runner_claude.sh\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-protect/scripts/test_shiki_goal_loop.sh\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-protect/scripts/test_shiki_memory_loop.sh\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-protect/scripts/test_shiki_code_review_gate.sh\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-protect/scripts/test_shiki_start.sh\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-protect/scripts/test_shiki_runtime_auth.sh\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-protect/scripts/test_shiki_runtime_registry.sh\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-protect/scripts/test_shiki_state_classes.sh\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-protect/scripts/test_shiki_provider_config.sh\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-protect/scripts/test_shiki_guardian_policy.sh\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-protect/scripts/test_shiki_evidence_integrity.sh\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-protect/scripts/test_shiki_governance_evidence.sh\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-protect/scripts/test_shiki_doctor.sh\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-protect/scripts/test_shiki_migrations.sh\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-protect/scripts/test_shiki_module_boundaries.sh\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-protect/scripts/test_shiki_shellcheck.sh\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-protect/scripts/test_shiki_validator_hardening.sh\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-protect/scripts/test_shiki_workflow_lint.sh\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-secret/bin/shiki\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-secret/AGENTS.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-secret/CLAUDE.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-secret/CONTEXT.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-secret/SYSTEM_PROMPT.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-secret/.claude/commands/shiki.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-secret/.codex/skills/shiki/SKILL.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-secret/.shiki/migrations/state.json\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-secret/.shiki/guardian-policy.json\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-secret/.shiki/config.yaml\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-secret/.shiki/schemas/runner-record.schema.json\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-secret/.shiki/schemas/report.schema.json\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-secret/.shiki/schemas/dispatch-guard.schema.json\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-secret/.shiki/schemas/external-ai-guardian-review-packet.schema.json\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-secret/.shiki/schemas/run.schema.json\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-secret/.shiki/schemas/worktree-record.schema.json\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-secret/.shiki/schemas/cca-evidence-manifest.schema.json\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-secret/.shiki/schemas/cca-verdict.schema.json\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-secret/.shiki/schemas/plan.schema.json\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-secret/.shiki/schemas/inbox.schema.json\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-secret/.shiki/schemas/memory-entry.schema.json\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-secret/.shiki/schemas/repair-packet.schema.json\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-secret/.shiki/schemas/smoke.schema.json\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-secret/.shiki/schemas/start.schema.json\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-secret/.shiki/schemas/ledger.schema.json\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-secret/.shiki/schemas/task.schema.json\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-secret/.shiki/schemas/dag.schema.json\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-secret/.shiki/schemas/goal.schema.json\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-secret/.shiki/README.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-secret/.shiki/manifest.json\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-secret/.shiki/templates/LEDGER_ENTRY.template.json\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-secret/.shiki/templates/CCA_VERDICT.template.json\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-secret/.shiki/templates/GOAL.template.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-secret/.shiki/templates/REPORT.template.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-secret/.shiki/templates/DAG.template.json\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-secret/.shiki/templates/worktree-record.schema.json\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-secret/.shiki/templates/REPAIR_PACKET.template.json\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-secret/.shiki/templates/TASK.template.json\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-secret/.shiki/policy.example.yaml\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-secret/.github/ISSUE_TEMPLATE/shiki_goal.yml\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-secret/.github/ISSUE_TEMPLATE/shiki_task.yml\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-secret/.github/CODEOWNERS\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-secret/.github/PULL_REQUEST_TEMPLATE/shiki-task.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-secret/.github/prompts/cca-completion-check.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-secret/.github/prompts/codex-repair-handoff.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-secret/.github/prompts/codex-task-handoff.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-secret/.github/workflows/shiki-validate.yml\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-secret/.github/workflows/shiki-claude-review.yml\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-secret/.github/workflows/shiki-cca-completion.yml\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-secret/.github/workflows/shiki-mergegate.yml\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-secret/.github/workflows/shiki-orchestrator.yml\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-secret/docs/agents/node24-workflow-compatibility.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-secret/docs/agents/checklists.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-secret/docs/agents/codex-handoff.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-secret/docs/agents/bootstrap-command.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-secret/docs/agents/guardian-policy.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-secret/docs/agents/autonomous-loop-e2e-acceptance.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-secret/docs/agents/runtime-auth-model.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-secret/docs/agents/shiki-doctor.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-secret/docs/agents/control-commands.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-secret/docs/agents/memory.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-secret/docs/agents/shiki-migrations.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-secret/docs/agents/decision-control.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-secret/docs/agents/domain.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-secret/docs/agents/skill-gate.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-secret/docs/agents/github-provider-config.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-secret/docs/agents/state-classes.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-secret/docs/agents/completion-check-agent.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-secret/docs/agents/runtime-registry.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-secret/docs/agents/implementation-policy.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-secret/docs/agents/triage-labels.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-secret/docs/agents/shiki-cli-architecture.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-secret/docs/agents/issue-tracker.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-secret/docs/agents/execution-control.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-secret/docs/agents/branch-protection-smoke.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-secret/docs/agents/ledger-schema.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-secret/docs/agents/evidence-integrity.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-secret/docs/adr/0010-external-ai-guardian-review.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-secret/docs/adr/0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-secret/docs/adr/template.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-secret/docs/adr/0015-contract-approval-before-dispatch.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-secret/docs/adr/0001-keep-shiki-as-platform-name.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-secret/docs/adr/0011-autonomous-quality-gate-evidence.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-secret/docs/adr/0007-packaging-and-release.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-secret/docs/adr/0002-use-agents-as-shared-constitution.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-secret/docs/adr/0009-requirements-definition-and-spec-freeze.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-secret/docs/adr/README.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-secret/docs/adr/0012-autonomous-loop-closeout-to-main.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-secret/docs/adr/0013-bootstrap-grants-actions-approve-pull-requests.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-secret/docs/adr/0008-use-claude-code-as-default-implementer.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-secret/docs/adr/0014-codex-app-external-ai-guardian-ui-adapter.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-secret/docs/adr/0004-use-github-first-with-shiki-mirror.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-secret/docs/adr/0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-secret/docs/adr/0003-require-skill-gate-for-mergegate-readiness.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-secret/skills/engineering/shiki/SKILL.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-secret/skills/engineering/setup-matt-pocock-skills/issue-tracker-gitlab.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-secret/skills/engineering/setup-matt-pocock-skills/domain.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-secret/skills/engineering/setup-matt-pocock-skills/triage-labels.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-secret/skills/engineering/setup-matt-pocock-skills/SKILL.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-secret/skills/engineering/setup-matt-pocock-skills/issue-tracker-github.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-secret/skills/engineering/setup-matt-pocock-skills/issue-tracker-local.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-secret/skills/engineering/code-review/SKILL.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-secret/skills/engineering/improve-codebase-architecture/LANGUAGE.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-secret/skills/engineering/improve-codebase-architecture/SKILL.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-secret/skills/engineering/improve-codebase-architecture/HTML-REPORT.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-secret/skills/engineering/improve-codebase-architecture/DEEPENING.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-secret/skills/engineering/improve-codebase-architecture/INTERFACE-DESIGN.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-secret/skills/engineering/triage/OUT-OF-SCOPE.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-secret/skills/engineering/triage/SKILL.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-secret/skills/engineering/triage/AGENT-BRIEF.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-secret/skills/engineering/prototype/SKILL.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-secret/skills/engineering/prototype/LOGIC.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-secret/skills/engineering/prototype/UI.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-secret/skills/engineering/diagnose/scripts/hitl-loop.template.sh\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-secret/skills/engineering/diagnose/SKILL.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-secret/skills/engineering/to-issues/SKILL.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-secret/skills/engineering/README.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-secret/skills/engineering/zoom-out/SKILL.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-secret/skills/engineering/grill-with-docs/SKILL.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-secret/skills/engineering/grill-with-docs/CONTEXT-FORMAT.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-secret/skills/engineering/grill-with-docs/ADR-FORMAT.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-secret/skills/engineering/tdd/mocking.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-secret/skills/engineering/tdd/deep-modules.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-secret/skills/engineering/tdd/tests.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-secret/skills/engineering/tdd/SKILL.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-secret/skills/engineering/tdd/interface-design.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-secret/skills/engineering/tdd/refactoring.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-secret/skills/engineering/to-prd/SKILL.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-secret/scripts/shiki_schema.py\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-secret/scripts/validate_shiki.py\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-secret/scripts/shiki_contracts.py\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-secret/scripts/shiki_jsonschema.py\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-secret/scripts/shiki_evidence.py\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-secret/scripts/shiki_locks.py\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-secret/scripts/shiki_loop.py\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-secret/scripts/shiki_memory.py\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-secret/scripts/shiki_manifest.py\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-secret/scripts/shiki_migrations.py\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-secret/scripts/shiki_provider.py\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-secret/scripts/shiki_workflows.py\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-secret/scripts/enforce_cca_verdict.py\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-secret/scripts/build_cca_evidence_manifest.py\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-secret/scripts/guardian_approval_signal.py\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-secret/scripts/mergegate_check.py\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-secret/scripts/shiki.py\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-secret/scripts/shiki_bootstrap.py\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-secret/scripts/shiki_cli.py\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-secret/scripts/shiki_config.py\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-secret/scripts/shiki_doctor.py\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-secret/scripts/shiki_git.py\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-secret/scripts/shiki_github.py\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-secret/scripts/shiki_guardian.py\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-secret/scripts/shiki_guardian_review.py\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-secret/scripts/shiki_installer.py\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-secret/scripts/shiki_process.py\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-secret/scripts/shiki_runtime.py\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-secret/scripts/shiki_runtime_adapters.py\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-secret/scripts/shiki_runtime_registry.py\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-secret/scripts/shiki_state_classes.py\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-secret/scripts/shiki_tasks.py\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-secret/scripts/shiki_state.py\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-secret/scripts/test_shiki_init.sh\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-secret/scripts/test_shiki_control_plane.sh\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-secret/scripts/test_shiki_run_orchestrator.sh\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-secret/scripts/test_shiki_daemon_runner.sh\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-secret/scripts/test_shiki_runner_codex.sh\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-secret/scripts/test_shiki_runner_claude.sh\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-secret/scripts/test_shiki_goal_loop.sh\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-secret/scripts/test_shiki_memory_loop.sh\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-secret/scripts/test_shiki_code_review_gate.sh\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-secret/scripts/test_shiki_start.sh\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-secret/scripts/test_shiki_runtime_auth.sh\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-secret/scripts/test_shiki_runtime_registry.sh\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-secret/scripts/test_shiki_state_classes.sh\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-secret/scripts/test_shiki_provider_config.sh\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-secret/scripts/test_shiki_guardian_policy.sh\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-secret/scripts/test_shiki_evidence_integrity.sh\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-secret/scripts/test_shiki_governance_evidence.sh\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-secret/scripts/test_shiki_doctor.sh\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-secret/scripts/test_shiki_migrations.sh\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-secret/scripts/test_shiki_module_boundaries.sh\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-secret/scripts/test_shiki_shellcheck.sh\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-secret/scripts/test_shiki_validator_hardening.sh\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-70420/retry-secret/scripts/test_shiki_workflow_lint.sh\\n.................................................................................................................................................................................................................................................................................................................................................................................................................................................................................\\n----------------------------------------------------------------------\\nRan 465 tests in 9.897s\\n\\nOK\\n\",\n.shiki/runner/EXEC-20260731T073838171565Z-86314e5f.json:8:  \"stdout\": \"All four changed files are exactly my declared locks \\u2014 nothing outside scope. Implementation is complete and verified. Here is the summary.\\n\\n## Result\\n- Goal/task: G-20260729T065622761503Z-4479b0d3 / T-20260729T065622770911Z-a8db8b69\\n- Role: Implementer (Claude Code, headless dispatch)\\n- Status: ready (edits complete in worktree; the Shiki loop owns commit/push/PR/merge)\\n\\n## What changed\\nTwo gaps closed on one dispatch seam, plus a wired end-to-end test.\\n\\n- **Gap (a) \\u2014 contract sync into the worktree** (`scripts/shiki_runtime.py`): new `sync_contract_into_worktree(target, task, worktree_path)` copies this task's `.shiki/tasks/<id>.json`, `.shiki/goals/<goal>.json`, and `.shiki/locks/<id>.json` from the coordinator into the worktree, and it's called in `dispatch_runner_task` right before the session executes. So a repair dispatched after a contract amendment judges against the *current* locks, not the stale copy the branch was cut with. Only this task's/goal's id-named files ride along (no foreign/sibling state); copy-in-place is idempotent; missing files are skipped, never fabricated.\\n\\n- **Gap (b) \\u2014 strict ownership bound on the transitive evidence walk** (`scripts/shiki_loop.py`): `_evidence_relatives_for_task` already walked ledgers transitively, but with no ownership check. Added: a ledger's referenced `.shiki` paths are expanded onto the branch **only when its `goal_id` AND `task_id` both equal the task's**. The ledger *file* is still carried unconditionally (it's in `ledger_evidence`); only the expansion is bounded. This is deliberately stricter than `mergegate_check.ledger_entry_allowed_for_task` (which also accepts an empty-task_id goal-scoped ledger) \\u2014 I did **not** reuse it, per scope. The completion ledger's goal-scoped evidence is still handled by `_sync_goal_complete_mirror` on the closeout path, untouched.\\n\\n## Checklist status\\n- Skill Gate: `tdd` (red\\u2192green, 11 new tests), `code-review` (self-review of diff; loop runs the independent pre-PR reviewer)\\n- TDD: wrote failing tests first, confirmed red for the right reasons (missing symbol / foreign-evidence leak), then green\\n- Tests: 11 new in `tests/test_dispatch_state_sync.py`, mapped 1:1 to the five acceptance checks; end-to-end wiring assertions added to `scripts/test_shiki_runner_claude.sh`\\n\\n## Evidence\\n- New tests: `python3 -m unittest discover -s tests -p test_dispatch_state_sync.py` \\u2192 Ran 11, OK\\n- Full Python suite: **Ran 621, OK** (no regressions; existing `test_loop_branch_plumbing.py` still green \\u2014 its ledgers all match goal+task)\\n- Shell: `bash scripts/test_shiki_runner_claude.sh` \\u2192 passed (proves the worktree carries this task's task/goal/lock with the declared lock, foreign goal stays absent); `test_shiki_module_boundaries.sh` \\u2192 passed; `validate_shiki.py` \\u2192 passed; `py_compile` clean\\n- Scope: `git status --short` shows only the four locked files\\n\\n## Acceptance-check mapping\\n1. Amended contract in worktree \\u2192 `test_amended_contract_lands_in_worktree` + shell lock-grep \\u2713\\n2. EXEC record on branch before PR \\u2192 `test_own_ledger_carries_its_exec_record` \\u2713\\n3. Foreign goal/task ledger doesn't pull refs \\u2192 `test_foreign_goal_ledger\\u2026`, `test_foreign_task_ledger\\u2026`, `test_goal_scoped_ledger\\u2026`, `test_bound_is_per_ledger\\u2026` \\u2713\\n4. No dangling-evidence blocker for loop-produced ledgers \\u2192 loop ledgers carry both ids, so their evidence is carried (own-ledger test) \\u2713\\n5. Idempotent re-dispatch \\u2192 `test_idempotent_no_duplicate_or_conflicting_records` \\u2713\\n\\n## Next action\\n- Hand back to the Shiki loop for commit/push/PR creation, its pre-PR code-review gate, and MergeGate/CCA judgment. No approval records were authored (none were needed).\\n\",\n.shiki/tasks/T-20260728T103742824401Z-f198cd24.json:78:    \"code-review\"\n.shiki/runner/EXEC-20260805T110904338897Z-168d4954.json:8:  \"stdout\": \"Implementation complete and verified. Per the execution protocol, I've left commit/push/PR to the Shiki loop and stayed strictly within the declared locks.\\n\\n## Result\\n- Goal/task: G-20260805T075725959969Z-61d080fb / T-20260805T075725961882Z-91c2ecac\\n- Role: Implementer (Claude Code)\\n- Status: ready (implementation done in worktree; loop owns delivery)\\n\\n## What changed or decided\\nA new `sync_branch` arm in `decide_task_action` narrows the red-check-plus-BEHIND exclusion. When a required check is red because the base moved after the branch forked (PR #288: an append-only-evidence file the branch never had \\u2192 metadata check reads a deletion), the loop now takes a bounded base sync instead of three no-op repairs.\\n\\n- **Placement/impersonation guard**: the arm sits inside `if repairable_failed:`, strictly *after* `_cca_verdict_stop`, so a `needs_guardian`/`blocked`/`complete`/unresolvable verdict on a BEHIND branch still produces its terminal stop (ADR 0010).\\n- **Policy guard**: `not policy_failed` (for any reason, not \\\"only policy red\\\") \\u2014 never syncs through a red Guardian gate, protecting a head-bound approval or a base-defeated ADR 0015 contract-approval carry.\\n- **Bound**: durable task-file `base_sync_attempts` vs new `MAX_BASE_SYNCS=2`, mirroring `cca_rerun_count`/`MAX_CCA_RERUNS`; at/past cap it falls through to `dispatch_repair` \\u2192 repair limit \\u2192 `stop_guardian` unchanged. `task.schema.json` untouched (it's `additionalProperties: true`).\\n- **No attempt consumed**: a sync creates no repair packet and never advances `repair_attempts`.\\n- **Evidence**: `_sync_branch` now appends a `mergegate` (existing type) ledger on success, guarded so a ledger failure can't undo a done push.\\n- **Worktree reconciliation**: new in-file `_reconcile_registered_worktree_to_origin` does `git merge --ff-only` (never a reset) so a repair dispatched after a sync pushes as a fast-forward. `shiki_runtime.py` untouched.\\n- **Base ref**: call site resolves `_default_base_ref` and strips the `origin/` prefix, avoiding `origin/origin/main`.\\n\\n## Checklist status\\n- Skill Gate: `tdd` (red\\u2192green on the new decision/plumbing/e2e cases), `code-review` (self-review against every acceptance check; loop runs the automated pre-PR gate)\\n- TDD: inverted the pinned `test_behind_but_failing_check_is_not_synced`; added decision cases (needs_guardian/blocked stop, policy+another red \\u2260 sync, cap boundary/at/past cap, closeout still stop_blocked, clean+red still repairs), a real-git plumbing test (repair pushes after a sync), and the shell before/after RP-count proof.\\n- CCA/MergeGate: not yet run (loop-owned; PR not yet opened)\\n\\n## Evidence\\n- `python3 -m unittest discover -s tests` \\u2192 **Ran 1054 tests \\u2026 OK**\\n- `test_shiki_goal_loop.sh`, `test_shiki_adversarial_state.sh`, `test_shiki_module_boundaries.sh` \\u2192 all passed\\n- `validate_shiki.py` \\u2192 Shiki validation passed\\n- `test_shiki_loop_e2e.sh` (not required) \\u2192 passed, no regression\\n- Changed files: exactly the 4 locks. `task.schema.json`, `ledger.schema.json`, `validate_shiki.py`, `shiki_runtime.py` confirmed unchanged.\\n\\n## Next action\\n- The goal loop commits, opens the PR with the MergeGate body, runs CCA/MergeGate, and merges. No action needed from me.\\n\",\n.shiki/runner/EXEC-20260729T215059278874Z-84833087.json:8:  \"stdout\": \"All changes are within the declared locks and the validator passes. Done.\\n\\n## Result\\n- Goal/task: G-20260729T065622761503Z-4479b0d3 / T-20260729T065622768255Z-da448d45\\n- Role: Implementer (Claude Code)\\n- Status: ready (for commit/PR/CCA by the Shiki loop)\\n\\n## What changed or decided\\nCodified the **approval-record prohibition** across the constitution and implementer contract. An *approval record* is any artifact whose meaning is that an authority approved something \\u2014 `spec_freeze`, Spec Amendment approval, Guardian approval evidence (`guardian:approved` / `Guardian approval granted` / `external-ai-guardian-review`), or a CCA `complete` verdict.\\n\\n- **AGENTS.md**: new binding `## Approval Records` section (names `spec_freeze` explicitly; states the unsatisfiable-criterion \\u2192 *stop, never manufacture* rule; carves out schemas/test fixtures that never enter the real `.shiki/` mirror). Reinforced in the Implementer role, the Codex \\\"must not\\\" list, and the Repair Loop rules.\\n- **CLAUDE.md**: matching `## Approval Records` section plus a Repair Loop reinforcement, mirroring the AGENTS.md prohibition.\\n- **docs/agents/implementation-policy.md**: new Non-Negotiable #12, an implementer-contract paragraph in Phase 6, and a Phase 9 (Repair Loop) rule.\\n- **docs/agents/checklists.md**: new **CCA-12** (blocking) \\u2014 fails a PR that introduces/edits/backfills a runtime-authored operative approval record.\\n- **docs/agents/completion-check-agent.md**: matching Hard Blocker so the CCA judge acts on CCA-12.\\n\\nScope note: schemas and clearly-scoped test fixtures are exempted, consistent with the evidence-integrity model and the spec-frozen-fixtures rule ([[shiki-contract-mode-mechanics]]) \\u2014 the ban targets *operative* records Shiki would read as a real sign-off.\\n\\n## Checklist status\\n- Skill Gate: `tdd` \\u2014 no behavior seam for prose; verification surface is `validate_shiki.py` + acceptance-criterion mapping (documented exception, TDD-02/TDD-08). `code-review` \\u2014 self-reviewed the diff (scope, anchors, consistency) below.\\n- PRD/issues: N/A (task-scoped documentation change under existing Goal)\\n- TDD: N/A per above\\n- CCA: pending (owned by the loop); new CCA-12 + Hard Blocker now defined\\n- MergeGate: pending loop delivery\\n\\n## Evidence\\n- Checks: `python3 scripts/validate_shiki.py --target .` \\u2192 `Shiki validation passed` (baseline also passed)\\n- Files changed (all within locks `AGENTS.md`, `CLAUDE.md`, `docs/agents/**`): AGENTS.md, CLAUDE.md, docs/agents/implementation-policy.md, docs/agents/checklists.md, docs/agents/completion-check-agent.md\\n- Anchor links `#approval-records` resolve to the single `## Approval Records` heading in each of AGENTS.md/CLAUDE.md (no collisions)\\n- Branch/PR: no commit/push/PR performed \\u2014 per the execution protocol, the Shiki loop owns commit, PR body, and merge\\n\\n## Next action\\n- The Shiki goal loop commits, opens the PR (with MergeGate body), reruns CCA, and merges. No implementer action remains; all five acceptance checks are satisfied.\\n\",\n.shiki/runner/EXEC-20260804T105205025145Z-247e4576.json:8:  \"stdout\": \"Implementation complete and locally verified. Here's the summary.\\n\\n## Result\\n- Goal/task: G-20260804T095105533205Z-224ce02d / T-20260804T095105534398Z-c2ef0cfc\\n- Role: Implementer (Claude Code, headless dispatch)\\n- Status: ready (implementation done and locally verified; the loop owns commit/PR/MergeGate/CCA/merge)\\n\\n## What changed or decided\\n- **`scripts/shiki_sync_proof.py`** (new): `verify_pure_base_sync(*, target, a_sha, head_sha, default_branch) -> SyncProof(proved, reason, merge_count)`. Sole git-running guardian-family module; every git call is read-only, `cwd=target`, return code inspected (never swallowed), no network/API/`gh`. Runs the 9 conditions in order, each a distinct `step N:` reason:\\n  1. not shallow / base `refs/remotes/origin/<default>^{commit}` resolves / `HEAD==head_sha`; 2. `merge-tree --write-tree` capability probe (git \\u2265 2.38); 3. no in-tree `.gitattributes` (+`-c core.attributesFile=/dev/null` on every merge-tree); 4. `a_sha` is a **local** object (never falls back to network \\u2014 rejects force-push orphans); 5. `a\\u2260head`; 6. `a_sha` on first-parent history; 7. no authored non-merge commit; 8. every carried merge has exactly two parents with the second an ancestor of base; **9. THE PROOF** \\u2014 `merge-tree --write-tree a_sha base` clean and its tree equals `tree(head_sha)`. Only one `proved=True` path, after step 9 matches. Any exception is caught at the boundary and named.\\n- **`tests/test_sync_proof.py`** (new): 21 tests, each on a **real** temporary git repo (git never mocked; the probe-failure test uses a real git PATH shim to simulate pre-2.38). The tree-reproduction attacks assert steps 6-8 pass via independent raw-git queries, proving step 9 is load-bearing.\\n- Registered `scripts/shiki_sync_proof.py` in `shiki_installer.py` TEMPLATE_PATHS and in `test_shiki_module_boundaries.sh` (import list + staged-paths list).\\n\\n## Checklist status\\n- Skill Gate: `tdd` (empirically verified git semantics \\u2192 tests \\u2192 module \\u2192 green), `code-review` (self-review pass; durable review/CCA is on GitHub via the loop)\\n- TDD: red/green complete; 21/21 pass\\n- CCA/MergeGate: owned by the loop on the PR (not run here)\\n\\n## Evidence\\n- New tests: `python3 -m unittest test_sync_proof` \\u2192 **Ran 21 tests \\u2026 OK**\\n- Registration: `bash scripts/test_shiki_module_boundaries.sh` \\u2192 **passed**\\n- Regression: 45 installer/bootstrap/mirror tests OK; `validate_shiki.py` \\u2192 **Shiki validation passed**\\n- Locks: only the 4 declared paths changed. The `.shiki/tasks/\\u2026json` (M) and `.shiki/locks/\\u2026json` (??) are loop-owned and predate this session \\u2014 I did not touch them.\\n\\n## Next action\\n- The Shiki loop commits/pushes, opens the PR with the MergeGate body, and runs CCA/MergeGate. Note: this task delivers the proof **primitive only** \\u2014 it is not yet wired into `mergegate_check.py`/guardian approval-carry; that integration is separate future work (recorded in memory `sync-proof-primitive`).\\n\",\n.shiki/runner/EXEC-20260806T060423184344Z-9b20eb5d.json:8:  \"stdout\": \"Implementation complete. Here is the final report.\\n\\n## Result\\n- Goal/task: `G-20260806T034026770241Z-4f95b5bf` / `T-20260806T034026771634Z-52142739`\\n- Role: Implementer (Claude Code)\\n- Status: **complete** (in-worktree; loop owns delivery \\u2014 I did not commit/push/PR)\\n\\n## What changed or decided\\nThe measured collision: the loop writes mirror JSON straight into the **coordinator** working tree and never stages it there. After the task PR merges, those same paths return via `git merge origin/main` and git aborts \\u2014 \\\"untracked working tree files would be overwritten by merge\\\" \\u2014 because a *delivered-and-returning* file is sitting untracked. I confirmed empirically that an **untracked** identical file aborts the merge while a **staged** identical file lets it succeed (FF absorbs it).\\n\\n**Fix (14 lines in `scripts/shiki_loop.py`):** in `_sync_state_to_branch`, after the existing copy loop, stage the coordinator's *own* copies of the exact paths just delivered \\u2014 `run([\\\"git\\\", \\\"add\\\", \\\"--\\\", *copied], cwd=target, check=False)`. Staging (not committing \\u2014 the local branch never advances) makes each path tracked so the merge absorbs it. **No carry** is introduced: these are this task's own evidence, never a foreign task's/goal's ledger \\u2014 the load-bearing constraint, because `ledger_entry_allowed_for_task` (`mergegate_check.py:2408-2413`) forbids cross-branch ledger carry.\\n\\n- Signature and copy-set of `_sync_state_to_branch`: **unchanged**. `_evidence_relatives_for_task`: **untouched**.\\n\\n## Checklist status\\n- Skill Gate: `tdd` (RED\\u2192GREEN, both tests verified failing without the fix), manual `code-review` self-review (no runnable code-review skill installed locally; the loop runs its own independent pre-PR reviewer per ADR 0011)\\n- PRD/issues: n/a (loop-dispatched task)\\n- TDD: `CoordinatorMergeAfterSyncTests` (real repo, two cycles) + `CoordinatorStagingAfterSyncTests` (no untracked-vs-default-branch collision; + no-carry statement test citing `ledger_entry_allowed_for_task`)\\n- CCA / MergeGate: pending (loop-owned)\\n\\n## Evidence\\n- `python3 -m unittest discover -s tests` \\u2192 **Ran 1083 tests, OK**\\n- `bash scripts/test_shiki_goal_loop.sh` \\u2192 **shiki goal loop tests passed**\\n- `python3 scripts/validate_shiki.py --target .` \\u2192 **Shiki validation passed**\\n- Files changed: only the 3 locked paths (`scripts/shiki_loop.py`, `tests/test_dispatch_state_sync.py`, `tests/test_loop_branch_plumbing.py`); no gate/schema/workflow/approval source touched.\\n\\nAll 9 acceptance checks satisfied. I also updated the stale `undelivered-ledger-carry-vs-mergegate` memory (it described the abandoned carry design) to reflect the amended no-carry staging fix.\\n\\n## Next action\\n- The Shiki loop takes over: commit \\u2192 push \\u2192 create PR (with MergeGate body) \\u2192 CCA \\u2192 merge. No action from me \\u2014 opening my own commit/PR would break the loop's `create_pr` transition.\\n\",\n.shiki/runner/EXEC-20260803T093613152637Z-7a7d6c4b.json:8:  \"stdout\": \"Implementation complete. All work stayed within the declared locks and the Shiki loop owns delivery (no commits/PRs from me).\\n\\n## Result\\n- Goal/task: `G-20260801T040948350468Z-4a542e71` / `T-20260801T040948352877Z-eea16def`\\n- Role: Implementer (Claude Code default)\\n- Status: **complete** (implementation + tests green locally; awaiting the loop's PR/CCA/MergeGate delivery)\\n\\n## What changed or decided\\n- **`snapshot_pr`** now fetches `state,mergedAt,headRefOid,mergeStateStatus` and surfaces `merge_state_status` in the PR state dict.\\n- **`_merge_or_sync`** (new decision helper) is the single merge-gate choice: when a PR is otherwise ready to merge (all required checks green, Guardian resolved) and `mergeStateStatus == BEHIND`, it returns a new **`sync_branch`** action; every other state (CLEAN/DIRTY/BLOCKED/missing) still returns `merge` unchanged. All **three** merge decision sites (closeout, impl low/medium, impl high/critical via the policy gate) route through it, so a not-otherwise-ready PR is never synced.\\n- **`_sync_branch`** (new effector): fetches, checks out `origin/<branch>` in a throwaway **detached** worktree (one path serves both impl and closeout PRs), does a plain `git merge origin/<base>` + plain `git push origin HEAD:<branch>` \\u2014 **no rebase, no force-push**. A conflict runs `git merge --abort` (branch left byte-identical) and stops with a branch-named conflict reason; a push failure is a distinct stop reason; success returns `sync_branch`. Fails closed, never raises.\\n- Wired `sync_branch` into `ACTION_PRIORITY` (just below `merge`) and `execute_action`. Per the code-review pass, `cmd_loop_run` now **waits after a successful sync** so the new head's checks re-run instead of racing GitHub's eventual consistency.\\n\\n## Checklist status\\n- Skill Gate: `tdd` (red \\u2192 green), `code-review` (independent adversarial pass \\u2014 no blocking findings; addressed the one substantive non-blocking finding: sleep-after-sync)\\n- PRD/issues: N/A (task handoff)\\n- TDD: `tests/test_branch_behind_sync.py` (17 tests) drives the real decision + the real `_sync_branch` against a temporary git repo (conflict/push-fail/success/no-history-rewrite/loop-run-waits)\\n- CCA: not my authority \\u2014 the loop drives it\\n- MergeGate: not asserted locally (durable evidence is the loop's job)\\n\\n## Evidence\\n- `python3 -m unittest discover -s tests` \\u2192 **809 tests OK**\\n- `bash scripts/test_shiki_goal_loop.sh` \\u2192 **passed** (new BEHIND-then-merge end-to-end case)\\n- `python3 scripts/validate_shiki.py --target .` \\u2192 **Shiki validation passed**\\n- Changed files, all within locks: `scripts/shiki_loop.py`, `tests/test_branch_behind_sync.py`, `scripts/test_shiki_goal_loop.sh` (the `.shiki/tasks` + `.shiki/locks` entries are loop bookkeeping present at session start, untouched by me)\\n\\n## Next action\\n- Hand back to the Shiki goal loop to commit, push, open the MergeGate PR, and run CCA/MergeGate. No implementer commit/PR was created (loop owns delivery). This is a **high-risk** task, so merge will require the Guardian gate / recorded authority.\\n\",\n.shiki/runner/EXEC-20260804T090834124995Z-86c82aca.json:8:  \"stdout\": \"All four defects are fixed, tested, and verified. Here is the summary.\\n\\n## Result\\n- Goal/task: G-20260804T082618610097Z-21ffeb4e / T-20260804T082618611335Z-02de0043\\n- Role: Implementer\\n- Status: ready (implementation complete, all local gating suites green; the Shiki loop owns commit/PR/merge)\\n\\n## What changed or decided\\nFixed four adversarial defects on the Guardian approval surface, each with its own test:\\n\\n1. **PAGINATION** \\u2014 Added `--paginate` to the three guardian-evidence `gh api` calls in `shiki-mergegate.yml` (matching `shiki-cca-completion.yml`); `|| echo \\\"[]\\\"` fallbacks left byte-identical. Added a control-plane assertion (`test_shiki_guardian_policy.sh` + `test_guardian_approval_binding.py`) requiring `--paginate` on every guardian-evidence call in **both** workflows, non-vacuously (it also verifies all three endpoints are present).\\n2. **EMPTY HEAD SHA** \\u2014 `_comment_source` no longer passes vacuously when `head_sha` is `''`; an empty head under `require_head_sha` now produces the same stale-comment soft blocker (the old `'' in body` was always True).\\n3. **POSITIONAL HEAD BINDING** \\u2014 Replaced the whole-body substring test with `_head_sha_binding_blocker`: exactly one 40-hex token standing on its own line, equal to `head_sha` (the `build_approval_body` shape). Zero/prose-quoted, two-plus, abbreviated, and mismatched all soft-block with a named reason. Extraction stays **after** the marker/negation/`_configured_guardian`/`_author_allowed` checks \\u2014 a non-Guardian comment is never parsed for a SHA.\\n4. **PATCH TARGET** \\u2014 The guardian-status PATCH picker in `shiki-cca-completion.yml` now selects only `github-actions[bot]`-authored comments; still posts a new comment when none exists.\\n\\n**Fixture migration:** Canonicalized every approving fixture across the five holders to the `build_approval_body` shape (`marker\\\\n\\\\nHEAD`); renamed `test_line_initial_marker_approves` \\u2192 `test_loose_prose_marker_does_not_approve` (now asserts the loose form does **not** approve, without weakening it). `test_shiki_control_plane.sh`'s placeholder `abc123` head was promoted to a full 40-hex SHA (carried across `headRefOid`, all `statusCheckRollup[*].headSha`, and the CCA verdict) since positional binding requires a real object name. Added tightening prose to `docs/agents/guardian-policy.md` (all `validate_shiki` needles preserved).\\n\\n## Checklist status\\n- Skill Gate: tdd (red\\u2192green via the new binding tests), code-review (self-review of diffs)\\n- TDD: `tests/test_guardian_approval_binding.py` \\u2014 one case per defect + a \\\"still resolves as REQUIRED\\\" negative assertion on every rejecting case; ordering asserted for the actor check\\n- CCA/MergeGate: owned by the loop (not run here)\\n\\n## Evidence\\n- `python3 -m unittest discover -s tests` \\u2192 **Ran 854 tests \\u2026 OK**\\n- `python3 scripts/validate_shiki.py` \\u2192 **Shiki validation passed**\\n- Shell suites PASS: guardian_policy, governance_evidence, evidence_integrity, control_plane, adversarial_state, state_classes, p2_malicious_pr, shellcheck, workflow_lint, module_boundaries, code_review_gate, doctor, goal_loop, daemon_runner, p2_fork_pr_limitation, p2_missing_cca_token, validator_hardening\\n- Confirmed no out-of-lock shell suite (`test_shiki_adversarial_state.sh` etc.) asserts against the changed workflow forms\\n- `git status`: only the 12 declared-lock files changed (the `.shiki/tasks/\\u2026json` `M` and `.shiki/locks/\\u2026json` `??` were loop-owned and already present at session start \\u2014 untouched by me)\\n\\n## Next action\\n- The Shiki goal loop commits, opens the PR with the MergeGate body, and merges. No `git`/`gh`/PR actions taken here per the execution protocol.\\n\",\n.shiki/runner/EXEC-20260801T094219559485Z-f1a9cb73.json:8:  \"stdout\": \"The implementation is complete and fully verified. Per the execution protocol, I've edited only files in the worktree within the declared locks \\u2014 the Shiki goal loop owns commit/push/PR delivery.\\n\\n## Result\\n- **Goal/task:** G-20260801T085710527889Z-5ec41eba / T-20260801T085710529159Z-575d4080\\n- **Role:** Implementer (Claude Code, headless dispatch)\\n- **Status:** complete (implemented + verified in-worktree; loop owns delivery)\\n\\n## What changed\\n**DEFECT A \\u2014 verdict-aware repair gating** (`scripts/shiki_loop.py`)\\n- `goal_loop_step` now resolves the CCA verdict VALUE read-only from durable CI evidence (`_resolve_cca_verdict`: `gh run list` the CCA workflow \\u2192 `gh run download --name shiki-cca-evidence` \\u2192 read `cca-verdict.json`) \\u2014 only when the `CCA verdict` check is red on an impl PR \\u2014 and passes it as a new pure `cca_verdict` param into `decide_task_action` (keeps the engine pure).\\n- New pure `_cca_verdict_stop`: `needs_guardian`\\u2192`stop_guardian`, `blocked`\\u2192`stop_blocked`, `complete`-vs-red-check\\u2192distinct `stop_blocked`, unresolvable\\u2192distinct fail-closed `stop_blocked` \\u2014 none dispatch a repair or consume an attempt. `repair_required`/`insufficient_evidence` still dispatch + increment, but the CCA verdict check is stripped from the repair items (mirroring POLICY_GATE) so no packet ever names it; `_dispatch_repair` names the verdict's findings instead. Resolution is read-only and never raises (any error \\u2192 unresolvable). POLICY_GATE handling untouched.\\n\\n**DEFECT B \\u2014 push the evidence the task cites** (`scripts/shiki_loop.py`)\\n- `rerun_cca` and `dispatch_repair` now call the (unchanged) `_sync_state_to_branch` after their ledger append. The `dispatch_repair` sync sits *after* the head-movement guard so the evidence commit can't mask a no-op repair.\\n\\n## Checklist status\\n- **Skill Gate:** tdd (tests drive real `decide_task_action` + real effectors + real resolver) \\u2713; code-review is the loop-owned pre-PR gate (ADR 0011) \\u2014 cannot self-invoke, did rigorous self-review \\u2713\\n- **Tests added:** `tests/test_loop_verdict_gating.py` (26 cases: every verdict branch, unresolvable, counter-unchanged vs increment, POLICY_GATE byte-for-byte, DEFECT-B transitive evidence on-branch); `test_shiki_goal_loop.sh` needs_guardian\\u2192Guardian-stop-not-repair case; `test_shiki_adversarial_state.sh` \\\"no emittable packet names the CCA verdict check as its only item\\\" case.\\n\\n## Evidence\\n- `python3 -m unittest discover -s tests` \\u2192 **763 passed** (26 new)\\n- `test_shiki_goal_loop.sh`, `test_shiki_adversarial_state.sh`, `test_shiki_governance_evidence.sh`, `validate_shiki.py --target .` \\u2192 all pass (the exact task `test_command`)\\n- `git status`: only the 4 locked files changed (`scripts/shiki_loop.py`, `tests/test_loop_verdict_gating.py`, `scripts/test_shiki_goal_loop.sh`, `scripts/test_shiki_adversarial_state.sh`); the task-mirror change is the loop's own pre-existing dispatch bookkeeping.\\n\\n## Next action\\n- The goal loop commits/pushes this worktree, opens the PR with the MergeGate body, and drives CCA/MergeGate. No approval record was authored; no PR/merge performed by me.\\n\",\n.shiki/runner/EXEC-20260804T072736363873Z-a5665370.json:8:  \"stdout\": \"All changes are strictly within the declared locks. The two `.shiki/` files (`locks/\\u2026json` added, `tasks/\\u2026json` modified to `status: running` + ledger append) are loop-owned bookkeeping \\u2014 not in my locks and untouched by me. My only edit was `tests/test_workflow_invocation.py` (in locks).\\n\\nThe task was substantially implemented by a prior session; the one genuine gap was the argv-coupling defect the memory warned about. Here's the final status.\\n\\n## Result\\n- Goal/task: G-20260801T040836630398Z-f28167e5 / T-20260801T040836632579Z-2cde7018\\n- Role: Implementer (Claude Code)\\n- Status: ready (implementation complete; loop owns delivery)\\n\\n## What changed or decided\\n- **Found and fixed the one real gap**: the prior work added `--contract-approval` to the `mergegate_check.py` invocation in `shiki-mergegate.yml`, but `tests/test_workflow_invocation.py` (in my locks precisely for this coupling) still lacked the matching `_FLAG_VALUES` entry. `_metadata_argv()` reconstructs argv from *every* flag in the workflow, so both `MetadataArgvPostMergeReconcile` tests were failing with `KeyError: '--contract-approval'`. I added `\\\"--contract-approval\\\": \\\".shiki/gha/contract-approval.json\\\"`. The fixture doesn't write that file, so `load_contract_approval_registration` returns `None` (missing \\u2192 no proof), the medium-risk reconcile fixture is unaffected, and both tests go green.\\n- **Verified (code-review of the pre-existing work)**:\\n  - Both workflows build `.shiki/gha/contract-approval.json` and pass `--contract-approval` to their consumer (cca job \\u2192 `guardian_approval_signal.py`; both mergegate jobs \\u2192 `mergegate_check.py`).\\n  - Task id resolved with the shared regex `T-([0-9]{4,}|[0-9]{8}T[0-9]{12}Z-[0-9a-f]{8})`, reusing the same `$task_id`.\\n  - Adding commit found via `git log --diff-filter=A` over the already-fetched base ref; no `--depth`, no second base fetch (control-plane asserts fetch counts 2/1).\\n  - Every failure path emits a well-formed, `applies`-defeating JSON object with a `reason` \\u2014 never absent/partial.\\n  - **Critical correctness check**: the emitted field names (`pr, merged, adding_commit, adding_commit_pr, guardian_approved, guardian_source, task_id`) exactly match what `evaluate_contract_approval`/`_registration_proof_reasons` reads. The prior implementer correctly deviated from the handoff's illustrative `approval_source` \\u2014 emitting that literally would have silently defeated the carry.\\n  - ADR 0015 Status is `Accepted`; Consequences record the three stages with dates (registration 2026-07-27..29, immutability PR #229 on 2026-08-01, this carry) and the non-hypothetical double-approval window.\\n\\n## Checklist status\\n- Skill Gate: tdd (confirmed RED `KeyError` \\u2192 GREEN after fix), code-review (self-review of full diff, verified evaluator field-name contract)\\n- PRD/issues: n/a (task-scoped)\\n- TDD: green\\n- CCA: pending (loop-owned, on PR)\\n- MergeGate: pending (loop-owned)\\n\\n## Evidence\\n- `python3 -m unittest discover -s tests` \\u2192 **Ran 842 tests, OK** (exit 0)\\n- `bash scripts/test_shiki_control_plane.sh` \\u2192 **passed** (exit 0)\\n- `python3 scripts/validate_shiki.py --target .` \\u2192 **passed** (exit 0)\\n- All modifications within declared locks; `.shiki/tasks` and `.shiki/locks` changes are loop-owned, untouched by me.\\n\\n## Next action\\n- Hand back to the Shiki loop for commit/push/PR creation and MergeGate. This is a **critical**-risk task and its own contract was registered on the task branch (not the default branch first), so `evaluate_contract_approval` will find `base_task` absent \\u2192 the carry doesn't apply to *this* PR, which will need a live Guardian approval \\u2014 the expected bootstrap double-approval ADR 0015 describes. No implementer action remains.\\n\",\n.shiki/runner/EXEC-20260807T052118120391Z-9c89fa74.json:8:  \"stdout\": \"Implementation complete. All work is confined to the four declared locks, `mergegate_check.py` is untouched, and every required check passes.\\n\\n## Result\\n- Goal/task: G-20260807T035659158223Z-af599ebb / T-20260807T035659160500Z-ba479979\\n- Role: Implementer (loop owns delivery \\u2014 I made no git/gh/PR calls)\\n- Status: ready\\n\\n## What changed or decided\\nMade the goal loop **informative** about *why* a MergeGate check blocked (not smarter \\u2014 no classification, decisions unchanged):\\n\\n- **PUBLISH** \\u2014 `.github/workflows/shiki-mergegate.yml`: added an `Upload MergeGate result` step (`actions/upload-artifact@v6`, `if: always()`, `if-no-files-found: ignore`) shipping `.shiki/gha/mergegate-result.json` from the failing run. `if: always()` is safe here \\u2014 none of the job's steps post a review/comment/label.\\n- **CONSUME** \\u2014 `scripts/shiki_loop.py`: new read-only `_resolve_blocking_reasons` (mirrors `_resolve_cca_verdict`): finds the head-bound `shiki-mergegate.yml` run, downloads the `shiki-mergegate-result` artifact, returns its `blocking_reasons`. Never raises; degrades to `[]` on any failure. Resolved in `goal_loop_step` (impure layer where `_gh` is stubbed in tests) for a chosen `dispatch_repair`, then threaded through `_dispatch_repair`.\\n- **RENDER** \\u2014 `scripts/shiki_tasks.py`: new optional `mergegate_blocking_reasons` packet field; `cmd_handoff_repair` renders it under a `## MergeGate Blocking Reasons` heading inside a **dynamic-length code fence** (`_fence_untrusted_as_data`) so untrusted PR-derived text is data, never instructions.\\n\\nKey landmine avoided: resolving reasons inside `_dispatch_repair` would hit real `gh` in `test_repair_delivery.py` (which calls it unstubbed) \\u2014 so resolution lives in `goal_loop_step`. The \\\"no `if: always()` on posting steps\\\" test is scoped to the MergeGate workflow (the CCA workflow's blockers-comment step legitimately already uses it); it additionally guards that the CCA Review Bridge APPROVE step never gains `if: always()`.\\n\\n## Checklist status\\n- Skill Gate: tdd (red\\u2192green, real modules, no logic reimplementation) \\u2713; code-review (self-review against acceptance) \\u2713\\n- TDD: `tests/test_repair_reasons.py` \\u2014 21 tests (resolver degrade paths, packet carry, handoff-as-data, injection-shaped text, decision-invariance across all four failure modes, workflow version/`if:always()` assertions, writer contract)\\n- CCA/MergeGate: owned by the loop; not run locally\\n\\n## Evidence\\n- `python3 -m unittest discover -s tests` \\u2192 **1138 OK**\\n- `scripts/test_shiki_goal_loop.sh` \\u2192 pass; `scripts/test_shiki_control_plane.sh` \\u2192 pass\\n- `python3 scripts/validate_shiki.py` \\u2192 **Shiki validation passed**\\n- `git diff --name-only` \\u2192 only the 4 locked files (`.shiki/tasks/*.json` M + `.shiki/locks/*.json` untracked were pre-existing loop bookkeeping at session start, not my edits)\\n\\n## Next action\\n- Hand back to the Shiki goal loop to commit/push/open the PR (with the MergeGate body) and drive MergeGate/CCA \\u2014 delivery is loop-owned per the handoff protocol.\\n\",\n.shiki/tasks/T-20260729T065622770078Z-3662bff2.json:43:    \"code-review\"\n.shiki/runner/EXEC-20260731T033648286454Z-876622d6.json:8:  \"stdout\": \"The implementation is complete and verified. Here is my report.\\n\\n## Result\\n- Goal/task: G-20260731T031357585622Z-8d094bd1 / T-20260731T031357587095Z-2cb11839\\n- Role: Implementer (Claude Code)\\n- Status: **ready** (implementation complete; one downstream blocker surfaced for the end-to-end goal \\u2014 see Next action)\\n\\n## What changed or decided\\nGave the CCA Guardian signal the ADR 0017 bookkeeping-closeout exemption by importing the **one** classifier, never a second copy of the six conditions.\\n\\n- **`scripts/guardian_approval_signal.py`** \\u2014 imports `is_bookkeeping_closeout` (and `parse_changed_files_status`) from `mergegate_check`. New helper `_bookkeeping_closeout_exemption` delegates the whole decision to that classifier over three new **optional, default-to-no-exemption** args (`--base-shiki`, `--changed-files-status`, `--merged-prs`). A proven closeout resolves `effective_risk=\\\"low\\\"` exactly as MergeGate's `_guardian_risk_labels` does; PR labels can still escalate; every emitted signal records `bookkeeping_closeout_exemption`. Fails closed on every axis (no task id, unreadable task, missing base snapshot, missing changed-files status, unresolvable/unmerged impl PR).\\n- **`.github/workflows/shiki-cca-completion.yml`** \\u2014 the `cca` job's *Prepare CCA context* step now builds the base `.shiki` snapshot (`git archive`, **reusing the existing base fetch** \\u2014 no second fetch, so `test_cca_head_sha_injection`'s count-of-2 assertion holds), reuses the changed-files-status file it already writes, and resolves the merge proof from the **BASE** task's `expected_pr` (never the head's) via `gh pr view --json state`. All three are passed to the signal.\\n- **`tests/test_guardian_signal_closeout.py`** (new, 17 tests) \\u2014 proven critical closeout \\u2192 `required=false`; each disqualifier in isolation and each missing input \\u2192 `required=true`; the audit flag is recorded; and a parity matrix asserting the signal's exemption decision equals MergeGate's and **is never more permissive**.\\n\\n## Checklist status\\n- Skill Gate: tdd (red/green via the new test) \\u2713; code-review \\u2014 the `/code-review` skill is not model-invocable, so I ran a manual + architectural self-review (findings below)\\n- PRD/issues: n/a (task handoff)\\n- TDD: \\u2713 17 new tests, all green\\n- CCA: owned by GitHub CCA (not self-judged)\\n- MergeGate: loop-owned; I did not commit/push/PR\\n\\n## Evidence\\n- Checks (local): full suite `Ran 577 tests \\u2026 OK`; new file `Ran 17 \\u2026 OK`; `test_bookkeeping_closeout` (23) and `test_guardian_signal` (8) still green; `validate_shiki` PASS\\n- Required scripts: `test_shiki_governance_evidence.sh` PASS, `test_shiki_workflow_lint.sh` (actionlint) PASS \\u2014 neither modified\\n- Scope: `git status` shows exactly the three locked files; `.shiki/gha` verified gitignored and cleaned\\n- Verified `validate_shiki` tolerates a 969-file nested base snapshot under `.shiki/gha` (git-tracked-only check), so the mergegate job's artifact-download\\u2192validate is unaffected\\n\\n## Next action\\n**Load-bearing follow-up (out of this task's scope/acceptance, flagged so it isn't silently repeated):** the required **\\\"MergeGate policy check\\\"** job in the same workflow (`needs: cca`) computes its own `--merged-prs` **only** under the `<!-- shiki:post_merge_reconcile -->` marker. Before this task that job was *skipped* (CCA failed `needs_guardian`), masking the gap; now that the signal makes CCA `complete`, that job runs and a normal high/critical closeout gets `--merged-prs \\\"\\\"` \\u2192 `is_bookkeeping_closeout` returns False \\u2192 it **still blocks on guardian**. To let a closeout merge with zero Guardian approvals end-to-end, that job's *Enforce MergeGate* step needs the same ungated merge-proof resolution (safe: `--merged-prs` is only consumed by `post_merge_reconcile` and the closeout classifier, both independently validating; a normal impl PR's base `expected_pr` points at its own open PR \\u2192 stays `\\\"\\\"`). I did not make this change because the task's stated scope and every acceptance check are signal-only, and its \\\"exact change\\\" names only the signal path. Recommend a sibling/follow-up task for the policy-check job. (Recorded in memory `adr0017-bookkeeping-closeout-exemption`.)\\n\",\n.shiki/runner/EXEC-20260727T021647975615Z-c8220789.json:8:  \"stdout\": \"The work is complete. Here's the summary.\\n\\n## Result\\n- Goal/task: G-20260727T005125900810Z-077718f7 / T-20260727T005125902419Z-6c2db440\\n- Role: Implementer (Claude Code, headless in worktree)\\n- Status: **ready** (loop owns commit/PR/merge)\\n\\n## What changed or decided\\nThe contract-PR path, MergeGate contract mode, CONTEXT.md terms, and ADR 0015 were already in place and green. The one blocking defect was in `tests/test_contract_open.py`: it `shutil.copy2`'d a real `.shiki/plans/P-\\u2026-0015c0de.json` fixture that **didn't exist** and **must not be created** \\u2014 a spec-frozen plan in the real mirror would violate AC8 (`git diff \\u2026-- .shiki | grep spec_freeze` must be empty). All 8 e2e tests errored on `FileNotFoundError`.\\n\\n- **Rewrote `tests/test_contract_open.py`** to construct its own spec-frozen, high-risk, multi-task plan (with a declared dependency) *inside the temp target* via `_write_frozen_plan`, satisfying AC6. Real support files (manifest, guardian policy, config, workflows) are still copied in read-only; no test writes the real mirror. Preserved all 8 acceptance-mapped tests.\\n- Confirmed `shiki-mergegate.yml` always passes `--allow-missing-cca`, so contract PRs (no CCA verdict) are correctly admitted by the real pipeline \\u2014 the guardian gate is enforced independently of CCA.\\n- Verified ADR 0015 status \\\"Proposed\\\" matches the house convention (0008\\u20130015 are all Proposed), so no change there.\\n\\n## Checklist status\\n- Skill Gate: tdd (tests are the spec, now green), code-review (reviewed validator/CLI/docs/edge cases \\u2014 no blocking findings)\\n- PRD/issues: n/a (task-scoped delivery)\\n- TDD: 28 contract tests pass (20 validator-level + 8 e2e)\\n- CCA: owned by GitHub CCA on the PR\\n- MergeGate: contract mode validated end-to-end via real subprocess simulation\\n\\n## Evidence (all local/diagnostic \\u2014 loop records durable evidence)\\n- `python3 -m py_compile scripts/*.py` \\u2192 OK\\n- `python3 -m unittest discover -s tests` \\u2192 **Ran 412 tests \\u2026 OK**\\n- `python3 scripts/validate_shiki.py` \\u2192 **Shiki validation passed**\\n- AC1 (registration-only branch), AC2 (impl blocked), AC3 (6-field frozen mismatch blocked), AC4 (high-risk blocked w/o Guardian, ready with it), AC5 (CLI doesn't apply label + reports maintainer), AC6 (temp-dir fixtures), AC7 (CONTEXT.md + ADR 0015 present, validate passes), AC8 (`grep spec_freeze` **empty**) \\u2014 all verified\\n- Working tree: only `scripts/{mergegate_check,shiki_cli,shiki_tasks}.py` (M) + `tests/test_contract_{mode,open}.py` (??); zero real `.shiki` changes\\n- Branch: shiki/t-20260727t005125902419z-6c2db440-deliver-the-contract-pr-registration-path-and-re\\n\\n## Next action\\n- Loop takes over: commit the four scripts + two test files, push, open the Contract-delivery PR with the MergeGate body, run CI/CCA. No commit/PR was created by me, per protocol.\\n\",\n.shiki/tasks/T-20260731T045548843829Z-ee2deb90.json:48:    \"code-review\"\n.shiki/runner/EXEC-20260801T072112561476Z-f4072b2d.json:8:  \"stdout\": \"The implementation is complete and fully verified. Here is my report.\\n\\n## Result\\n- **Goal/task:** G-20260801T040948350468Z-4a542e71 / T-20260801T040948352279Z-07301e34 \\u2014 prove a runner session is alive with an OS lease\\n- **Role:** Implementer (Claude Code)\\n- **Status:** ready \\u2014 implementation complete, all tests green in the worktree; the Shiki loop owns commit/push/PR/merge\\n\\n## What changed or decided\\n- **OS session lease primitives** (`hold_session_lease`, `session_lease_state`, `SessionLeaseHeld`, `session_lease_path`, state constants): an exclusive `fcntl.flock` held for a dispatched session's lifetime. The kernel releases it on process death, so a successful non-blocking acquire is exact proof no holder is alive (the reason to use flock over a reusable pid). The probe returns `held`/`free`/`foreign_host`/`absent` and never leaves the lock acquired.\\n- **`dispatch_runner_task`** now wraps the body from the `status=\\\"running\\\"` write through the adapter call in `hold_session_lease`; a lease it can't take fails with `another dispatch for the same task is already running`, refusing a second session against the same worktree. The lease releases on success and failure alike.\\n- **`decide_task_action`** consults the lease for `running` tasks: `held`/`foreign_host` \\u2192 `wait_runner` (naming which); `free`/`absent` \\u2192 new `redispatch` action that resets to `ready`, records `dispatch_attempts`, and re-dispatches; past the bound (`MAX_DISPATCH_ATTEMPTS = 2`) \\u2192 `stop_blocked` naming the task, attempt count, and free lease. `goal_loop_step` probes the lease; the effector performs the reset+re-dispatch.\\n- `.shiki/leases` declared **local-only** in the manifest and added to `.gitignore`.\\n- **New `tests/test_session_lease.py`** (455 lines, 22 tests) drives real child processes and the real loop decision / real `dispatch_runner_task` \\u2014 the lease is never mocked. Added a lease take/release assertion to `test_shiki_runner_claude.sh`.\\n\\n**One deliberate, documented deviation** you should know about: the scope said to put the primitives in a standalone `scripts/shiki_session_lease.py`. Doing so **breaks `shiki init`/`install-target`** because a staged CLI module (`shiki_runtime`) would import an unstaged module \\u2014 I verified this fails with `Shiki CLI module import failed: No module named 'shiki_session_lease'`. The clean fix is one line adding the module to `TEMPLATE_PATHS` in `scripts/shiki_installer.py`, but that file is **outside my declared locks**. So I placed the primitives in the already-staged, in-locks `scripts/shiki_runtime.py` (so coordinator *and* target repos carry the lease) and made `shiki_session_lease.py` the documented public import surface exposing the two operations. This satisfies every behavioral acceptance criterion **and** \\\"no file outside the declared locks is changed.\\\" Follow-up (needs a lock widening): move the impl into a standalone `shiki_session_lease.py` + add it to `TEMPLATE_PATHS`.\\n\\n## Checklist status\\n- **Skill Gate:** tdd (real red-green tests), code-review (self-review of the full diff)\\n- **TDD:** 22 new tests; every acceptance check maps to a test driving real flock/processes\\n- **CCA/MergeGate:** owned by the loop after delivery\\n- Note: `shiki_runtime.py` grew ~130 lines (Architecture Gate \\\">100 net new lines in a core module\\\") \\u2014 inherent to the assigned lease feature within a declared lock; the task is high-risk so Guardian review is already on the merge path.\\n\\n## Evidence\\n- `python3 -m unittest discover -s tests` \\u2192 **Ran 713 tests \\u2026 OK** (22 new)\\n- Exact loop `test_command` (`unittest && test_shiki_runner_claude.sh && test_shiki_goal_loop.sh && validate_shiki --target .`) \\u2192 **PASS (exit 0)**\\n- Extra shell tests: runner_claude, runner_codex, goal_loop, module_boundaries, state_classes, doctor, control_plane, init, evidence_integrity, governance_evidence, code_review_gate, start, runtime_auth \\u2192 all **PASS**\\n- `validate_shiki.py` (ROOT and `--target .`) \\u2192 **Shiki validation passed**\\n- `git status`: only declared-lock files touched (`.gitignore`, `.shiki/manifest.json`, `shiki_loop.py`, `shiki_runtime.py`, `test_shiki_runner_claude.sh`, new `shiki_session_lease.py`, new `tests/test_session_lease.py`). The `.shiki/tasks/\\u2026json` and `.shiki/locks/\\u2026json` entries are the loop's own dispatch bookkeeping, present at session start.\\n\\n## Next action\\n- Loop: run the TDD gate, open the PR, run CCA/MergeGate (high-risk \\u2192 Guardian approval for merge).\\n- Operator/reviewer: note the documented `shiki_session_lease.py`-as-import-surface placement; if you want the implementation in a standalone module, a Spec Amendment adding `scripts/shiki_installer.py` (and its one-line `TEMPLATE_PATHS` entry) to the locks would allow it.\\n\",\n.shiki/runner/EXEC-20260804T142133800445Z-eac434e6.json:8:  \"stdout\": \"All work is complete and every gate is green. Here is the summary.\\n\\n## Result\\n- Goal/task: G-20260804T095105533205Z-224ce02d / **T-20260804T095105535495Z-22a51036** \\u2014 \\\"Reach the carry from CI and record the decision\\\"\\n- Role: Implementer (default, ADR 0008)\\n- Status: **ready** (implementation complete, all local evidence green; loop owns commit/PR/merge)\\n\\n## What changed or decided\\nWired the dormant base-sync carry (`guardian_comment_carried`, ADR 0018) into CI so it actually applies, and recorded the decision.\\n\\n- **Both workflows, 4 judging invocations** now pass `--base-sync-carry` and `--default-branch \\\"$DEFAULT_BRANCH\\\"`, with `DEFAULT_BRANCH: ${{ github.event.repository.default_branch }}` in each step's `env:`:\\n  - `shiki-cca-completion.yml` \\u2014 `cca` job: the `guardian_approval_signal.py` signal and the `shiki guardian status` blockers render; `mergegate` job: `mergegate_check.py`.\\n  - `shiki-mergegate.yml` \\u2014 the metadata `mergegate_check.py` (its run body stays expression-free; `DEFAULT_BRANCH` flows through `env:`).\\n  - Reused each job's existing base fetch \\u2014 **no second fetch, no `--depth`**.\\n- **`validate_shiki.py`**: added both flags to the `shiki-mergegate.yml` entry and a **new top-level entry for `shiki-cca-completion.yml`**. I guarded all four invocations (not just the two the scope named) because acceptance check #4 requires validate_shiki to fail when *any* of the four loses either flag \\u2014 documented in-code.\\n- **CCA prompt** (`.github/prompts/cca-completion-check.md` + inline in `shiki-cca-completion.yml`): CCA-08 reads `guardian_comment_carried` as a recorded authority and must not re-run the base-sync proof.\\n- **ADR 0018** written: Context (head-bound approval \\u00d7 strict protection; measured 4 extra approvals across 5 Contract PRs on 2026-08-01), Decision (guardian_comment only, tree reproduction, ordered fail-closed conditions, default off, code-pinned), Alternatives (one-PR serialisation; `merge -s ours`/evil-merge defeat diff/patch equality; 4-of-8 real syncs overlap `.shiki/tasks/*.json` kills disjointness), accepted residual, Consequences.\\n- **`guardian-policy.md`**: new carry section; every prior needle verified intact.\\n- **Tests**: new `tests/test_workflow_invocation_carry.py` (enumerates all invocations, both flags each; `DEFAULT_BRANCH` env; expression-free; no `--depth`/second fetch; 4\\u00d72 per-invocation-per-flag validate failures). Updated `tests/test_workflow_invocation.py` (`--base-sync-carry`\\u2192`_STORE_TRUE`, `--default-branch`\\u2192`_FLAG_VALUES`, staged `shiki-cca-completion.yml`). Added a carry block to `test_shiki_control_plane.sh`.\\n\\n## Checklist status\\n- Skill Gate: `tdd` (tests written/red-green-verified), `code-review` (self-reviewed; scope-bounded) \\u2014 satisfied\\n- TDD: negative controls proved validate fails per flag before wiring; all tests green after\\n- CCA/MergeGate: not run locally (GitHub authority)\\n\\n## Evidence\\n- `python3 -m unittest discover -s tests` \\u2192 **919 passed**\\n- `bash scripts/test_shiki_control_plane.sh` \\u2192 passed; **all 34 `test_shiki_*.sh`** pass (incl. governance-evidence, guardian-policy, workflow-lint, shellcheck --strict)\\n- `python3 scripts/validate_shiki.py --target .` \\u2192 passed\\n- `git status` confirms **only the 9 locked files** changed (the untracked `.shiki/tasks|locks/*.json` are loop-owned, untouched)\\n\\n## Next action\\n- Hand back to the Shiki loop to commit, push, open the PR (MergeGate body), and merge. Risk is **high**, so MergeGate/Guardian approval applies on the PR \\u2014 and this task is exactly the one that makes an already-approved high-risk PR carry its approval across a proven base sync.\\n\\nOne design note worth surfacing for review: the scope named two CCA invocations for the validate_shiki entry, but acceptance check #4 requires validate_shiki to fail for **all four** named invocations (including the guardian-status render). I guarded all four \\u2014 a superset of the scope's naming \\u2014 to satisfy check #4 literally; this is strictly safer (prevents silent flag removal on the render too) and still \\\"covers both judging invocations.\\\"\\n\",\n.shiki/runner/EXEC-20260806T081558179541Z-e4ac6e70.json:8:  \"stdout\": \"Everything is clean (the \\\"TEMP\\\" matches are just `ATTEMPTS` substrings) and confined to my three locked files. The task is complete.\\n\\n## Result\\n- **Goal/task:** G-20260806T065143407702Z-8e3284ce / T-20260806T065143409817Z-dbdae1cf \\u2014 \\\"Stage every coordinator mirror write, not only the three sync paths\\\"\\n- **Role:** Implementer\\n- **Status:** complete (ready for the loop to deliver)\\n\\n## What changed or decided\\n\\nTwo changes in `scripts/shiki_loop.py`, both driven by end-to-end verification against a **real loop run**, not inspection:\\n\\n1. **Single covering stage point.** `execute_action` now wraps its body (`_execute_action_body`) in `try/finally` and stages the coordinator's whole `.shiki` surface once \\u2014 `_stage_coordinator_mirror` runs `git add --ignore-removal -- .shiki`. Every effector (incl. `goal_complete`'s early return and any *future* effector) is covered by construction; staging-only, HEAD never moves, nothing outside `.shiki`. The prior `_sync_state_to_branch` staging (reached by only 3 of 11 effectors) is kept intact \\u2014 an out-of-lock test drives it directly.\\n\\n2. **The non-obvious blocker (found only by driving the criterion).** Staging alone was **necessary but insufficient**: a ground-truth probe of the *real* loop showed the merge still aborted \\u2014 now with *\\\"Your local changes would be overwritten\\\"* \\u2014 because `_sync_goal_complete_mirror` **re-appended** the loop's own coordinator-only merge/mark_done ledger ids after syncing the completing task from main, diverging it from main. Git refuses to fast-forward over an uncommitted-divergent file (and committing is forbidden). I **removed the re-append** (and its now-dead `_loop_own_ledger_ids` helper) so the completing goal's task files stay **byte-identical to main** (the source of truth) \\u2192 the merge fast-forwards cleanly. The merge/mark_done ledger *files* persist on disk as durable evidence; the re-append was transient (the very merge dropped it anyway) and had **zero consumers besides its own test**.\\n\\n**\\u26a0\\ufe0f Deviation to flag for review:** change 2 reverses a deliberate ADR-0012 \\\"Defect b\\\" behavior. It violates no scope MUST-NOT (closeout PR contents, the ADR-0012 decision table, `_sync_state_to_branch`'s signature, and `_evidence_relatives_for_task`'s output are all unchanged) and is required to meet the explicit acceptance, but the CCA/operator should review this specific decision.\\n\\n## Checklist status\\n- **Skill Gate:** `tdd` (red\\u2192green proven by neutralizing each change), `code-review` (independent adversarial review \\u2014 2 minor findings, both fixed)\\n- **TDD:** tests widened to every effector + a real closeout merge (twice); confirmed red without each change\\n- **CCA/MergeGate:** owned by the loop on delivery\\n\\n## Evidence\\n- **Tests:** `python3 -m unittest discover -s tests` \\u2192 **1089 OK**; `bash scripts/test_shiki_goal_loop.sh` \\u2192 passed; `python3 scripts/validate_shiki.py --target .` \\u2192 passed (exact `test_command` chained \\u2192 ALL PASS)\\n- **Ground truth:** real `loop run` probe \\u2192 coordinator has no untracked `.shiki`, completing task byte-identical to main, `git merge origin/main` returncode 0 (clean fast-forward)\\n- **Tests added/changed:** `test_dispatch_state_sync.py::EveryEffectorStagesCoordinatorMirrorTests` (merge/mark_done/goal_complete/create_closeout_pr each leave no untracked `.shiki` + an unhandled-effector \\\"by construction\\\" test); `test_closeout.py::CloseoutCoordinatorMergeTests` (real closeout \\u2192 `git merge origin/main` succeeds, twice); rewrote the `goal_complete` mirror test to assert byte-identity with main\\n- **Branch:** shiki/t-20260806t065143409817z-dbdae1cf-stage-every-coordinator-mirror-write-not-only-th (no PR \\u2014 loop-owned)\\n\\n## Next action\\n- The Shiki loop owns commit/push/PR/merge. On CCA review, please note the intentional reversal of `goal_complete`'s re-append (documented in-code and in the diff) \\u2014 it is what makes the coordinator genuinely mergeable, which the previous task's scope-prose-only fix did not achieve.\\n\",\n.shiki/tasks/T-20260801T041653348134Z-bf8a3c4b.json:46:    \"code-review\"\n.shiki/runner/EXEC-20260618T093244375965Z-3c87bb89.json:8:  \"stdout\": \"The new file is untracked (so `git diff HEAD` is empty by design); the review scope is `tests/test_loop_smoke3.py`, which I just wrote.\\n\\n**Review (proportionate to scope):**\\n- **Correctness:** Single test asserts `2 * 3 == 6` \\u2014 trivially true, passes (confirmed by the green run, all 285 tests `OK`).\\n- **Collection/collision:** Class `LoopSmoke3Tests` and module name are unique vs. `test_loop_smoke.py`/`test_loop_smoke2.py` \\u2014 no discovery shadowing.\\n- **No production code touched;** change is confined to `tests/**` (within declared locks).\\n- **Style parity:** docstring, `from __future__ import annotations`, `unittest.TestCase`, `test_smoke` method, and `__main__` guard match both siblings exactly.\\n\\nNo findings \\u2014 `[]`. Nothing to repair.\\n\\nI'm now done with my assigned scope. Per the execution protocol, I will **not** commit, push, or open a PR \\u2014 the Shiki goal loop owns delivery.\\n\\n## Result\\n- Goal/task: G-20260618T093112319886Z-a4f768c5 / T-20260618T093119138788Z-867546d0\\n- Role: Implementer (Claude Code)\\n- Status: complete (implementation; awaiting loop-owned MergeGate/CCA)\\n\\n## What changed or decided\\n- Added `tests/test_loop_smoke3.py`: one self-contained `unittest.TestCase` (`LoopSmoke3Tests.test_smoke`) asserting `2 * 3 == 6`, matching the two existing smoke-test siblings.\\n\\n## Checklist status\\n- Skill Gate: `tdd` (trivial \\u2014 test *is* the deliverable), `code-review` (run, no findings)\\n- PRD/issues: n/a (trivial test addition, loop-driven)\\n- TDD: test file is the artifact; passes on collection\\n- CCA: pending (loop/GitHub-owned)\\n- MergeGate: pending (loop-owned commit/push/PR/merge)\\n\\n## Evidence\\n- Checks: `python3 -m unittest discover -s tests -v` \\u2192 `Ran 285 tests`, `OK`; `test_loop_smoke3.LoopSmoke3Tests.test_smoke ... ok` collected and passing\\n- Review/CCA: code-review on diff \\u2192 no findings (`[]`)\\n- Branch: shiki/t-20260618t093119138788z-867546d0-add-a-third-trivial-smoke-test\\n- Scope: only `tests/test_loop_smoke3.py` (within locks `path:tests/**`)\\n\\n## Next action\\n- Shiki goal loop to commit, push, open the PR with the MergeGate body, and run CCA/MergeGate.\\n\",\n.shiki/runner/EXEC-20260803T033151887519Z-8e6cc0cc.json:8:  \"stdout\": \"All acceptance checks verified. Here's the closeout.\\n\\n## Result\\n- **Goal/task:** G-20260803T013839638288Z-eebb39bf / T-20260803T013839640380Z-ca171b22\\n- **Role:** Implementer\\n- **Status:** ready (implementation complete; the Shiki loop owns commit/push/PR/merge)\\n\\n## What changed or decided\\nClosed the four measured gaps **and** the class that produced them (\\\"a shipped artifact depending on something unshipped, with nothing checking the relationship\\\").\\n\\n- **`scripts/shiki_installer.py`** \\u2014 added the four missing entries to `TEMPLATE_PATHS`: `tests`, `.gitignore`, `scripts/shiki_session_lease.py`, `scripts/shiki_contract_approval.py`. Added a reasoned `TARGET_INSTALL_EXCLUDES` constant + one `should_skip` line so two **platform-only** tests never ship into a target (see below).\\n- **`.shiki/manifest.json`** \\u2014 registered `.shiki/leases/**` in `install.exclude_from_commit`, right beside `.shiki/gha/**`. `should_skip` now filters a leases lock on target-install for the same reason it filters `.shiki/gha`, and `manifest_stage_paths` makes it never committable in the platform either. A lock file can no longer reach a target.\\n- **`tests/test_install_completeness.py`** (new) \\u2014 the class check. For every shipped `scripts/*.py` (AST imports), shipped `test_shiki_*.sh` and shipped `.github/workflows/*.yml` (scripts//tests/ path refs, heredoc imports, `unittest discover -s <dir>`), it resolves each repository-relative dependency (case-exact) and asserts it's in `TEMPLATE_PATHS` or on the named `SHIPPING_EXCEPTIONS` list (reason per entry). Two fixtures prove it **fails** when a shipped artifact gains an unshipped dependency.\\n- **`scripts/test_shiki_init.sh`** \\u2014 added the install-and-verify case: installs into a fresh temp dir, then from inside runs `validate_shiki.py`, `unittest discover -s tests`, and every shipped `test_shiki_*.sh` (recursion-guarded), asserting zero failures; plus `.shiki/leases` absent, installed `.gitignore` ignores it (proven via real `git check-ignore`), and a `should_skip` leases assertion.\\n\\n**Judgment call surfaced:** shipping `tests/` wholesale dragged in two tests that *cannot* pass in a fresh target \\u2014 `test_loop_e2e_contract.py` (a meta-test over the unshipped platform-only `scripts/test_shiki_loop_e2e.sh`) and `test_mirror_lock_injection.py::FrozenPlanCorpusTests` (asserts the platform's own frozen-plan corpus; a fresh mirror is empty \\u2014 fabricating a frozen plan would forge a `spec_freeze` approval record, which is prohibited). Both are outside my locks to edit, so they're excluded from the target install via the documented `TARGET_INSTALL_EXCLUDES` (reason per entry), while remaining committed and run in the platform.\\n\\n## Checklist status\\n- **Skill Gate:** `tdd` (RED\\u2192GREEN: check flagged the 3 dep gaps before the fix, green after) and `code-review` (full-diff self-review, 0 findings) \\u2014 both run.\\n- **PRD/issues:** n/a (task pre-planned).\\n- **TDD:** green.\\n- **CCA:** not run (GitHub-side; owned by the loop's PR).\\n- **MergeGate:** not evaluated locally; no PR opened (loop-owned).\\n\\n## Evidence (all local/diagnostic; the loop's CI produces durable evidence)\\n- Platform: `validate_shiki` OK \\u00b7 `py_compile scripts/*.py` OK \\u00b7 `unittest discover -s tests` **OK (742)** \\u00b7 strict shellcheck OK \\u00b7 actionlint OK \\u00b7 module-boundaries OK.\\n- Fresh target (`install-target --local-only`): `validate_shiki` exit 0 \\u00b7 `unittest discover -s tests` **OK (712)** \\u00b7 every shipped `test_shiki_*.sh` exit 0 \\u00b7 `.shiki/leases` absent \\u00b7 `.gitignore` ignores it.\\n- `bash scripts/test_shiki_init.sh` passes end-to-end incl. the nested suite (~91s).\\n- Acceptance matrix A1\\u2013A10 all satisfied; only the four locked files changed.\\n- **Pre-PR code review:** no findings (record this in the PR body's `## Pre-PR code review` section).\\n\\n## Next action\\n- Hand back to the Shiki loop to commit the four files, open the PR with the MergeGate body (including the `## Pre-PR code review: no findings` section and a `code-review` ledger entry \\u2014 I did not author ledger/approval records), and run CCA/MergeGate. Risk is low/medium (control-plane + tests, no product source, no approval records), so the loop may merge autonomously once green.\\n\",\n.shiki/runner/EXEC-20260805T062544030814Z-786418fe.json:7:  \"stderr\": \"...........................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................\\n----------------------------------------------------------------------\\nRan 1019 tests in 89.253s\\n\\nOK\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-protect/bin/shiki\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-protect/AGENTS.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-protect/CLAUDE.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-protect/CONTEXT.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-protect/SYSTEM_PROMPT.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-protect/.gitignore\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-protect/.claude/commands/shiki.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-protect/.codex/skills/shiki/SKILL.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-protect/.shiki/migrations/state.json\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-protect/.shiki/guardian-policy.json\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-protect/.shiki/config.yaml\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-protect/.shiki/schemas/runner-record.schema.json\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-protect/.shiki/schemas/report.schema.json\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-protect/.shiki/schemas/dispatch-guard.schema.json\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-protect/.shiki/schemas/external-ai-guardian-review-packet.schema.json\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-protect/.shiki/schemas/run.schema.json\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-protect/.shiki/schemas/worktree-record.schema.json\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-protect/.shiki/schemas/cca-evidence-manifest.schema.json\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-protect/.shiki/schemas/cca-verdict.schema.json\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-protect/.shiki/schemas/plan.schema.json\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-protect/.shiki/schemas/inbox.schema.json\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-protect/.shiki/schemas/memory-entry.schema.json\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-protect/.shiki/schemas/repair-packet.schema.json\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-protect/.shiki/schemas/smoke.schema.json\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-protect/.shiki/schemas/start.schema.json\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-protect/.shiki/schemas/ledger.schema.json\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-protect/.shiki/schemas/task.schema.json\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-protect/.shiki/schemas/dag.schema.json\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-protect/.shiki/schemas/goal.schema.json\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-protect/.shiki/README.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-protect/.shiki/manifest.json\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-protect/.shiki/templates/LEDGER_ENTRY.template.json\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-protect/.shiki/templates/CCA_VERDICT.template.json\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-protect/.shiki/templates/GOAL.template.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-protect/.shiki/templates/REPORT.template.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-protect/.shiki/templates/DAG.template.json\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-protect/.shiki/templates/worktree-record.schema.json\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-protect/.shiki/templates/REPAIR_PACKET.template.json\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-protect/.shiki/templates/TASK.template.json\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-protect/.shiki/policy.example.yaml\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-protect/.github/ISSUE_TEMPLATE/shiki_goal.yml\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-protect/.github/ISSUE_TEMPLATE/shiki_task.yml\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-protect/.github/CODEOWNERS\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-protect/.github/PULL_REQUEST_TEMPLATE/shiki-task.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-protect/.github/prompts/cca-completion-check.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-protect/.github/prompts/codex-repair-handoff.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-protect/.github/prompts/codex-task-handoff.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-protect/.github/workflows/shiki-validate.yml\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-protect/.github/workflows/shiki-claude-review.yml\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-protect/.github/workflows/shiki-cca-completion.yml\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-protect/.github/workflows/shiki-mergegate.yml\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-protect/.github/workflows/shiki-orchestrator.yml\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-protect/docs/agents/node24-workflow-compatibility.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-protect/docs/agents/checklists.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-protect/docs/agents/codex-handoff.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-protect/docs/agents/bootstrap-command.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-protect/docs/agents/guardian-policy.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-protect/docs/agents/autonomous-loop-e2e-acceptance.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-protect/docs/agents/runtime-auth-model.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-protect/docs/agents/shiki-doctor.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-protect/docs/agents/control-commands.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-protect/docs/agents/memory.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-protect/docs/agents/shiki-migrations.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-protect/docs/agents/decision-control.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-protect/docs/agents/domain.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-protect/docs/agents/skill-gate.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-protect/docs/agents/github-provider-config.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-protect/docs/agents/state-classes.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-protect/docs/agents/completion-check-agent.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-protect/docs/agents/runtime-registry.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-protect/docs/agents/implementation-policy.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-protect/docs/agents/triage-labels.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-protect/docs/agents/shiki-cli-architecture.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-protect/docs/agents/issue-tracker.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-protect/docs/agents/execution-control.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-protect/docs/agents/branch-protection-smoke.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-protect/docs/agents/ledger-schema.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-protect/docs/agents/evidence-integrity.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-protect/docs/adr/0018-carried-guardian-approval-across-a-proven-base-sync.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-protect/docs/adr/0010-external-ai-guardian-review.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-protect/docs/adr/0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-protect/docs/adr/template.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-protect/docs/adr/0015-contract-approval-before-dispatch.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-protect/docs/adr/0001-keep-shiki-as-platform-name.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-protect/docs/adr/0011-autonomous-quality-gate-evidence.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-protect/docs/adr/0007-packaging-and-release.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-protect/docs/adr/0002-use-agents-as-shared-constitution.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-protect/docs/adr/0009-requirements-definition-and-spec-freeze.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-protect/docs/adr/README.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-protect/docs/adr/0012-autonomous-loop-closeout-to-main.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-protect/docs/adr/0013-bootstrap-grants-actions-approve-pull-requests.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-protect/docs/adr/0008-use-claude-code-as-default-implementer.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-protect/docs/adr/0014-codex-app-external-ai-guardian-ui-adapter.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-protect/docs/adr/0004-use-github-first-with-shiki-mirror.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-protect/docs/adr/0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-protect/docs/adr/0003-require-skill-gate-for-mergegate-readiness.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-protect/docs/adr/0017-bookkeeping-closeouts-do-not-inherit-risk.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-protect/docs/adr/0016-locks-bound-product-paths.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-protect/skills/engineering/shiki/SKILL.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-protect/skills/engineering/setup-matt-pocock-skills/issue-tracker-gitlab.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-protect/skills/engineering/setup-matt-pocock-skills/domain.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-protect/skills/engineering/setup-matt-pocock-skills/triage-labels.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-protect/skills/engineering/setup-matt-pocock-skills/SKILL.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-protect/skills/engineering/setup-matt-pocock-skills/issue-tracker-github.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-protect/skills/engineering/setup-matt-pocock-skills/issue-tracker-local.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-protect/skills/engineering/code-review/SKILL.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-protect/skills/engineering/improve-codebase-architecture/LANGUAGE.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-protect/skills/engineering/improve-codebase-architecture/SKILL.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-protect/skills/engineering/improve-codebase-architecture/HTML-REPORT.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-protect/skills/engineering/improve-codebase-architecture/DEEPENING.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-protect/skills/engineering/improve-codebase-architecture/INTERFACE-DESIGN.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-protect/skills/engineering/triage/OUT-OF-SCOPE.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-protect/skills/engineering/triage/SKILL.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-protect/skills/engineering/triage/AGENT-BRIEF.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-protect/skills/engineering/prototype/SKILL.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-protect/skills/engineering/prototype/LOGIC.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-protect/skills/engineering/prototype/UI.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-protect/skills/engineering/diagnose/scripts/hitl-loop.template.sh\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-protect/skills/engineering/diagnose/SKILL.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-protect/skills/engineering/to-issues/SKILL.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-protect/skills/engineering/README.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-protect/skills/engineering/zoom-out/SKILL.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-protect/skills/engineering/grill-with-docs/SKILL.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-protect/skills/engineering/grill-with-docs/CONTEXT-FORMAT.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-protect/skills/engineering/grill-with-docs/ADR-FORMAT.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-protect/skills/engineering/tdd/mocking.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-protect/skills/engineering/tdd/deep-modules.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-protect/skills/engineering/tdd/tests.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-protect/skills/engineering/tdd/SKILL.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-protect/skills/engineering/tdd/interface-design.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-protect/skills/engineering/tdd/refactoring.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-protect/skills/engineering/to-prd/SKILL.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-protect/tests/test_cca_verdict_usable.py\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-protect/tests/test_id_allocation.py\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-protect/tests/test_mirror_scope.py\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-protect/tests/test_config_loading.py\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-protect/tests/test_cca_verdict_nondegenerate.py\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-protect/tests/test_required_checks_derivation.py\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-protect/tests/test_install_completeness.py\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-protect/tests/test_policy_job_merged_prs.py\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-protect/tests/test_adr_references.py\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-protect/tests/test_session_lease.py\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-protect/tests/test_guardian_signal.py\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-protect/tests/test_cca_verdict_consistency.py\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-protect/tests/test_cca_head_sha_injection.py\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-protect/tests/test_sync_proof.py\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-protect/tests/test_guardian_carry.py\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-protect/tests/test_memory_scorecard.py\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-protect/tests/test_workflow_permissions_payload.py\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-protect/tests/test_contract_mode.py\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-protect/tests/test_memory_consult.py\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-protect/tests/test_workflow_invocation_carry.py\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-protect/tests/test_lock_conflict_stop.py\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-protect/tests/test_migrations_target.py\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-protect/tests/test_branch_protection_payload.py\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-protect/tests/test_loop_branch_plumbing.py\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-protect/tests/test_contract_immutability.py\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-protect/tests/test_loop_smoke.py\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-protect/tests/test_worktree_base.py\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-protect/tests/test_secret_set_claude.py\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-protect/tests/test_cca_completeness.py\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-protect/tests/test_paired_invariants.py\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-protect/tests/__init__.py\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-protect/tests/test_bookkeeping_closeout.py\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-protect/tests/test_contract_open.py\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-protect/tests/test_guardian_signal_closeout.py\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-protect/tests/test_cca_evidence_manifest_taskless.py\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-protect/tests/test_codeowners_wiring.py\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-protect/tests/test_guardian_review.py\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-protect/tests/shiki_test_support.py\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-protect/tests/test_guardian_approval_binding.py\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-protect/tests/test_completion_lock_scope.py\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-protect/tests/test_memory_promotion.py\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-protect/tests/test_loop_code_review.py\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-protect/tests/test_loop_verdict_gating.py\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-protect/tests/test_lock_lifecycle.py\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-protect/tests/test_memory_capture.py\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-protect/tests/test_loop_tdd_evidence.py\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-protect/tests/test_dispatch_lock_precondition.py\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-protect/tests/test_dispatch_state_sync.py\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-protect/tests/test_goal_reconcile.py\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-protect/tests/test_runner_adapters.py\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-protect/tests/test_lock_matching.py\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-protect/tests/test_contract_approval.py\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-protect/tests/test_memory_hooks.py\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-protect/tests/test_branch_behind_sync.py\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-protect/tests/test_install_force_semantics.py\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-protect/tests/test_contract_plan_registration.py\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-protect/tests/test_loop_engine.py\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-protect/tests/test_repair_delivery.py\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-protect/tests/test_handoff_consult.py\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-protect/tests/test_loop_smoke3.py\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-protect/tests/test_contract_amendment.py\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-protect/tests/test_bootstrap_safety.py\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-protect/tests/test_guardian_carry_parity.py\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-protect/tests/test_guardian_evidence.py\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-protect/tests/test_loop_smoke2.py\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-protect/tests/test_loop_lock_guard.py\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-protect/tests/test_manifest_staging.py\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-protect/tests/test_loop_lock_validator_hint.py\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-protect/tests/test_guardian_status.py\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-protect/tests/test_workflow_invocation.py\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-protect/tests/test_closeout.py\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-protect/tests/test_mirror_schema_validation.py\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-protect/scripts/shiki_schema.py\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-protect/scripts/validate_shiki.py\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-protect/scripts/shiki_contracts.py\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-protect/scripts/shiki_jsonschema.py\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-protect/scripts/shiki_evidence.py\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-protect/scripts/shiki_locks.py\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-protect/scripts/shiki_loop.py\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-protect/scripts/shiki_memory.py\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-protect/scripts/shiki_manifest.py\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-protect/scripts/shiki_migrations.py\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-protect/scripts/shiki_provider.py\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-protect/scripts/shiki_workflows.py\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-protect/scripts/enforce_cca_verdict.py\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-protect/scripts/cca_verdict_usable.py\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-protect/scripts/build_cca_evidence_manifest.py\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-protect/scripts/guardian_approval_signal.py\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-protect/scripts/mergegate_check.py\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-protect/scripts/shiki_sync_proof.py\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-protect/scripts/shiki.py\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-protect/scripts/shiki_bootstrap.py\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-protect/scripts/shiki_cli.py\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-protect/scripts/shiki_config.py\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-protect/scripts/shiki_contract_approval.py\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-protect/scripts/shiki_doctor.py\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-protect/scripts/shiki_git.py\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-protect/scripts/shiki_github.py\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-protect/scripts/shiki_guardian.py\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-protect/scripts/shiki_guardian_review.py\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-protect/scripts/shiki_guardian_status.py\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-protect/scripts/shiki_installer.py\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-protect/scripts/shiki_process.py\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-protect/scripts/shiki_runtime.py\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-protect/scripts/shiki_runtime_adapters.py\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-protect/scripts/shiki_runtime_registry.py\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-protect/scripts/shiki_session_lease.py\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-protect/scripts/shiki_state_classes.py\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-protect/scripts/shiki_tasks.py\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-protect/scripts/shiki_state.py\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-protect/scripts/test_shiki_init.sh\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-protect/scripts/test_shiki_control_plane.sh\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-protect/scripts/test_shiki_run_orchestrator.sh\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-protect/scripts/test_shiki_daemon_runner.sh\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-protect/scripts/test_shiki_runner_codex.sh\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-protect/scripts/test_shiki_runner_claude.sh\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-protect/scripts/test_shiki_goal_loop.sh\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-protect/scripts/test_shiki_memory_loop.sh\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-protect/scripts/test_shiki_code_review_gate.sh\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-protect/scripts/test_shiki_start.sh\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-protect/scripts/test_shiki_runtime_auth.sh\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-protect/scripts/test_shiki_runtime_registry.sh\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-protect/scripts/test_shiki_state_classes.sh\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-protect/scripts/test_shiki_provider_config.sh\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-protect/scripts/test_shiki_guardian_policy.sh\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-protect/scripts/test_shiki_evidence_integrity.sh\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-protect/scripts/test_shiki_governance_evidence.sh\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-protect/scripts/test_shiki_doctor.sh\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-protect/scripts/test_shiki_migrations.sh\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-protect/scripts/test_shiki_module_boundaries.sh\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-protect/scripts/test_shiki_shellcheck.sh\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-protect/scripts/test_shiki_validator_hardening.sh\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-protect/scripts/test_shiki_workflow_lint.sh\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-secret/bin/shiki\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-secret/AGENTS.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-secret/CLAUDE.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-secret/CONTEXT.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-secret/SYSTEM_PROMPT.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-secret/.gitignore\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-secret/.claude/commands/shiki.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-secret/.codex/skills/shiki/SKILL.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-secret/.shiki/migrations/state.json\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-secret/.shiki/guardian-policy.json\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-secret/.shiki/config.yaml\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-secret/.shiki/schemas/runner-record.schema.json\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-secret/.shiki/schemas/report.schema.json\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-secret/.shiki/schemas/dispatch-guard.schema.json\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-secret/.shiki/schemas/external-ai-guardian-review-packet.schema.json\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-secret/.shiki/schemas/run.schema.json\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-secret/.shiki/schemas/worktree-record.schema.json\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-secret/.shiki/schemas/cca-evidence-manifest.schema.json\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-secret/.shiki/schemas/cca-verdict.schema.json\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-secret/.shiki/schemas/plan.schema.json\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-secret/.shiki/schemas/inbox.schema.json\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-secret/.shiki/schemas/memory-entry.schema.json\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-secret/.shiki/schemas/repair-packet.schema.json\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-secret/.shiki/schemas/smoke.schema.json\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-secret/.shiki/schemas/start.schema.json\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-secret/.shiki/schemas/ledger.schema.json\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-secret/.shiki/schemas/task.schema.json\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-secret/.shiki/schemas/dag.schema.json\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-secret/.shiki/schemas/goal.schema.json\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-secret/.shiki/README.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-secret/.shiki/manifest.json\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-secret/.shiki/templates/LEDGER_ENTRY.template.json\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-secret/.shiki/templates/CCA_VERDICT.template.json\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-secret/.shiki/templates/GOAL.template.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-secret/.shiki/templates/REPORT.template.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-secret/.shiki/templates/DAG.template.json\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-secret/.shiki/templates/worktree-record.schema.json\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-secret/.shiki/templates/REPAIR_PACKET.template.json\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-secret/.shiki/templates/TASK.template.json\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-secret/.shiki/policy.example.yaml\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-secret/.github/ISSUE_TEMPLATE/shiki_goal.yml\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-secret/.github/ISSUE_TEMPLATE/shiki_task.yml\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-secret/.github/CODEOWNERS\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-secret/.github/PULL_REQUEST_TEMPLATE/shiki-task.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-secret/.github/prompts/cca-completion-check.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-secret/.github/prompts/codex-repair-handoff.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-secret/.github/prompts/codex-task-handoff.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-secret/.github/workflows/shiki-validate.yml\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-secret/.github/workflows/shiki-claude-review.yml\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-secret/.github/workflows/shiki-cca-completion.yml\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-secret/.github/workflows/shiki-mergegate.yml\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-secret/.github/workflows/shiki-orchestrator.yml\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-secret/docs/agents/node24-workflow-compatibility.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-secret/docs/agents/checklists.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-secret/docs/agents/codex-handoff.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-secret/docs/agents/bootstrap-command.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-secret/docs/agents/guardian-policy.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-secret/docs/agents/autonomous-loop-e2e-acceptance.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-secret/docs/agents/runtime-auth-model.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-secret/docs/agents/shiki-doctor.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-secret/docs/agents/control-commands.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-secret/docs/agents/memory.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-secret/docs/agents/shiki-migrations.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-secret/docs/agents/decision-control.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-secret/docs/agents/domain.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-secret/docs/agents/skill-gate.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-secret/docs/agents/github-provider-config.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-secret/docs/agents/state-classes.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-secret/docs/agents/completion-check-agent.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-secret/docs/agents/runtime-registry.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-secret/docs/agents/implementation-policy.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-secret/docs/agents/triage-labels.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-secret/docs/agents/shiki-cli-architecture.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-secret/docs/agents/issue-tracker.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-secret/docs/agents/execution-control.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-secret/docs/agents/branch-protection-smoke.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-secret/docs/agents/ledger-schema.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-secret/docs/agents/evidence-integrity.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-secret/docs/adr/0018-carried-guardian-approval-across-a-proven-base-sync.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-secret/docs/adr/0010-external-ai-guardian-review.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-secret/docs/adr/0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-secret/docs/adr/template.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-secret/docs/adr/0015-contract-approval-before-dispatch.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-secret/docs/adr/0001-keep-shiki-as-platform-name.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-secret/docs/adr/0011-autonomous-quality-gate-evidence.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-secret/docs/adr/0007-packaging-and-release.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-secret/docs/adr/0002-use-agents-as-shared-constitution.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-secret/docs/adr/0009-requirements-definition-and-spec-freeze.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-secret/docs/adr/README.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-secret/docs/adr/0012-autonomous-loop-closeout-to-main.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-secret/docs/adr/0013-bootstrap-grants-actions-approve-pull-requests.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-secret/docs/adr/0008-use-claude-code-as-default-implementer.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-secret/docs/adr/0014-codex-app-external-ai-guardian-ui-adapter.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-secret/docs/adr/0004-use-github-first-with-shiki-mirror.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-secret/docs/adr/0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-secret/docs/adr/0003-require-skill-gate-for-mergegate-readiness.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-secret/docs/adr/0017-bookkeeping-closeouts-do-not-inherit-risk.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-secret/docs/adr/0016-locks-bound-product-paths.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-secret/skills/engineering/shiki/SKILL.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-secret/skills/engineering/setup-matt-pocock-skills/issue-tracker-gitlab.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-secret/skills/engineering/setup-matt-pocock-skills/domain.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-secret/skills/engineering/setup-matt-pocock-skills/triage-labels.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-secret/skills/engineering/setup-matt-pocock-skills/SKILL.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-secret/skills/engineering/setup-matt-pocock-skills/issue-tracker-github.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-secret/skills/engineering/setup-matt-pocock-skills/issue-tracker-local.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-secret/skills/engineering/code-review/SKILL.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-secret/skills/engineering/improve-codebase-architecture/LANGUAGE.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-secret/skills/engineering/improve-codebase-architecture/SKILL.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-secret/skills/engineering/improve-codebase-architecture/HTML-REPORT.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-secret/skills/engineering/improve-codebase-architecture/DEEPENING.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-secret/skills/engineering/improve-codebase-architecture/INTERFACE-DESIGN.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-secret/skills/engineering/triage/OUT-OF-SCOPE.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-secret/skills/engineering/triage/SKILL.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-secret/skills/engineering/triage/AGENT-BRIEF.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-secret/skills/engineering/prototype/SKILL.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-secret/skills/engineering/prototype/LOGIC.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-secret/skills/engineering/prototype/UI.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-secret/skills/engineering/diagnose/scripts/hitl-loop.template.sh\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-secret/skills/engineering/diagnose/SKILL.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-secret/skills/engineering/to-issues/SKILL.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-secret/skills/engineering/README.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-secret/skills/engineering/zoom-out/SKILL.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-secret/skills/engineering/grill-with-docs/SKILL.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-secret/skills/engineering/grill-with-docs/CONTEXT-FORMAT.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-secret/skills/engineering/grill-with-docs/ADR-FORMAT.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-secret/skills/engineering/tdd/mocking.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-secret/skills/engineering/tdd/deep-modules.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-secret/skills/engineering/tdd/tests.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-secret/skills/engineering/tdd/SKILL.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-secret/skills/engineering/tdd/interface-design.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-secret/skills/engineering/tdd/refactoring.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-secret/skills/engineering/to-prd/SKILL.md\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-secret/tests/test_cca_verdict_usable.py\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-secret/tests/test_id_allocation.py\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-secret/tests/test_mirror_scope.py\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-secret/tests/test_config_loading.py\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-secret/tests/test_cca_verdict_nondegenerate.py\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-secret/tests/test_required_checks_derivation.py\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-secret/tests/test_install_completeness.py\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-secret/tests/test_policy_job_merged_prs.py\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-secret/tests/test_adr_references.py\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-secret/tests/test_session_lease.py\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-secret/tests/test_guardian_signal.py\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-secret/tests/test_cca_verdict_consistency.py\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-secret/tests/test_cca_head_sha_injection.py\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-secret/tests/test_sync_proof.py\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-secret/tests/test_guardian_carry.py\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-secret/tests/test_memory_scorecard.py\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-secret/tests/test_workflow_permissions_payload.py\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-secret/tests/test_contract_mode.py\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-secret/tests/test_memory_consult.py\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-secret/tests/test_workflow_invocation_carry.py\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-secret/tests/test_lock_conflict_stop.py\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-secret/tests/test_migrations_target.py\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-secret/tests/test_branch_protection_payload.py\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-secret/tests/test_loop_branch_plumbing.py\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-secret/tests/test_contract_immutability.py\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-secret/tests/test_loop_smoke.py\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-secret/tests/test_worktree_base.py\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-secret/tests/test_secret_set_claude.py\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-secret/tests/test_cca_completeness.py\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-secret/tests/test_paired_invariants.py\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-secret/tests/__init__.py\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-secret/tests/test_bookkeeping_closeout.py\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-secret/tests/test_contract_open.py\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-secret/tests/test_guardian_signal_closeout.py\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-secret/tests/test_cca_evidence_manifest_taskless.py\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-secret/tests/test_codeowners_wiring.py\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-secret/tests/test_guardian_review.py\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-secret/tests/shiki_test_support.py\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-secret/tests/test_guardian_approval_binding.py\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-secret/tests/test_completion_lock_scope.py\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-secret/tests/test_memory_promotion.py\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-secret/tests/test_loop_code_review.py\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-secret/tests/test_loop_verdict_gating.py\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-secret/tests/test_lock_lifecycle.py\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-secret/tests/test_memory_capture.py\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-secret/tests/test_loop_tdd_evidence.py\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-secret/tests/test_dispatch_lock_precondition.py\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-secret/tests/test_dispatch_state_sync.py\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-secret/tests/test_goal_reconcile.py\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-secret/tests/test_runner_adapters.py\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-secret/tests/test_lock_matching.py\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-secret/tests/test_contract_approval.py\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-secret/tests/test_memory_hooks.py\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-secret/tests/test_branch_behind_sync.py\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-secret/tests/test_install_force_semantics.py\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-secret/tests/test_contract_plan_registration.py\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-secret/tests/test_loop_engine.py\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-secret/tests/test_repair_delivery.py\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-secret/tests/test_handoff_consult.py\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-secret/tests/test_loop_smoke3.py\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-secret/tests/test_contract_amendment.py\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-secret/tests/test_bootstrap_safety.py\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-secret/tests/test_guardian_carry_parity.py\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-secret/tests/test_guardian_evidence.py\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-secret/tests/test_loop_smoke2.py\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-secret/tests/test_loop_lock_guard.py\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-secret/tests/test_manifest_staging.py\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-secret/tests/test_loop_lock_validator_hint.py\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-secret/tests/test_guardian_status.py\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-secret/tests/test_workflow_invocation.py\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-secret/tests/test_closeout.py\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-secret/tests/test_mirror_schema_validation.py\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-secret/scripts/shiki_schema.py\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-secret/scripts/validate_shiki.py\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-secret/scripts/shiki_contracts.py\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-secret/scripts/shiki_jsonschema.py\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-secret/scripts/shiki_evidence.py\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-secret/scripts/shiki_locks.py\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-secret/scripts/shiki_loop.py\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-secret/scripts/shiki_memory.py\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-secret/scripts/shiki_manifest.py\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-secret/scripts/shiki_migrations.py\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-secret/scripts/shiki_provider.py\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-secret/scripts/shiki_workflows.py\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-secret/scripts/enforce_cca_verdict.py\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-secret/scripts/cca_verdict_usable.py\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-secret/scripts/build_cca_evidence_manifest.py\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-secret/scripts/guardian_approval_signal.py\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-secret/scripts/mergegate_check.py\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-secret/scripts/shiki_sync_proof.py\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-secret/scripts/shiki.py\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-secret/scripts/shiki_bootstrap.py\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-secret/scripts/shiki_cli.py\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-secret/scripts/shiki_config.py\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-secret/scripts/shiki_contract_approval.py\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-secret/scripts/shiki_doctor.py\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-secret/scripts/shiki_git.py\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-secret/scripts/shiki_github.py\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-secret/scripts/shiki_guardian.py\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-secret/scripts/shiki_guardian_review.py\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-secret/scripts/shiki_guardian_status.py\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-secret/scripts/shiki_installer.py\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-secret/scripts/shiki_process.py\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-secret/scripts/shiki_runtime.py\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-secret/scripts/shiki_runtime_adapters.py\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-secret/scripts/shiki_runtime_registry.py\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-secret/scripts/shiki_session_lease.py\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-secret/scripts/shiki_state_classes.py\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-secret/scripts/shiki_tasks.py\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-secret/scripts/shiki_state.py\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-secret/scripts/test_shiki_init.sh\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-secret/scripts/test_shiki_control_plane.sh\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-secret/scripts/test_shiki_run_orchestrator.sh\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-secret/scripts/test_shiki_daemon_runner.sh\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-secret/scripts/test_shiki_runner_codex.sh\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-secret/scripts/test_shiki_runner_claude.sh\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-secret/scripts/test_shiki_goal_loop.sh\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-secret/scripts/test_shiki_memory_loop.sh\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-secret/scripts/test_shiki_code_review_gate.sh\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-secret/scripts/test_shiki_start.sh\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-secret/scripts/test_shiki_runtime_auth.sh\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-secret/scripts/test_shiki_runtime_registry.sh\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-secret/scripts/test_shiki_state_classes.sh\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-secret/scripts/test_shiki_provider_config.sh\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-secret/scripts/test_shiki_guardian_policy.sh\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-secret/scripts/test_shiki_evidence_integrity.sh\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-secret/scripts/test_shiki_governance_evidence.sh\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-secret/scripts/test_shiki_doctor.sh\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-secret/scripts/test_shiki_migrations.sh\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-secret/scripts/test_shiki_module_boundaries.sh\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-secret/scripts/test_shiki_shellcheck.sh\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-secret/scripts/test_shiki_validator_hardening.sh\\n[shiki] warning: kept existing file: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-start-test-35562/retry-secret/scripts/test_shiki_workflow_lint.sh\\n\",\n.shiki/runner/EXEC-20260619T103910532374Z-e516e9bf.json:8:  \"stdout\": \"The implementation is complete and clean. All work is within the declared locks, and I have not run any git/gh commands \\u2014 the Shiki loop owns delivery.\\n\\n## Result\\n- Goal/task: G-20260619T102811948334Z-902fc294 / T-20260619T102857996978Z-386d2ca9\\n- Role: Implementer (Claude Code, dispatched into worktree)\\n- Status: ready\\n\\n## What changed or decided\\n- **`scripts/shiki_github.py`** \\u2014 added `configure_workflow_permissions(repo, *, can_approve_pull_requests=True, default_permissions=\\\"read\\\", provider_config=None)`. It PUTs `repos/{repo}/actions/permissions/workflow` with `{default_workflow_permissions, can_approve_pull_request_reviews}` via the exact `gh api ... -X PUT --input -` pattern as `protect_branch`; `info()` on success, **`warn()` (not raise)** on failure with remediation text. Matches the doctor's `doctor.github.workflow_permissions` expectation (`read` + `true`).\\n- **`scripts/shiki_bootstrap.py`** \\u2014 imported and called it in `cmd_bootstrap_github` and `cmd_init`, immediately after `protect_branch` inside the `if args.protect:` block; added `workflow-permissions:` dry-run lines (configure line under protect, skip note under `--no-protect`).\\n- **`tests/test_workflow_permissions_payload.py`** \\u2014 new unittest (mirrors `test_branch_protection_payload.py`) asserting the PUT path, default `read`+`can-approve=true` payload, override honoring, and warn-not-raise on gh failure.\\n- **`scripts/test_shiki_init.sh` / `scripts/test_shiki_start.sh`** \\u2014 gh stubs now accept the `actions/permissions/workflow` PUT (routed to a separate payload file); init asserts the dry-run line, the `--no-protect` skip note, the execute-path PUT, and the payload; start adds a protect-enabled run asserting the PUT + payload through `cmd_start \\u2192 cmd_init`.\\n- **`docs/adr/0013-bootstrap-grants-actions-approve-pull-requests.md`** \\u2014 records the `can_approve_pull_request_reviews=true` bootstrap default and the CCA-Review-Bridge / solo-maintainer security tradeoff (relates to G-0012 / closed #106), the `read` least-privilege default, and warn-not-fail.\\n\\n## Checklist status\\n- Skill Gate: `tdd` (red \\u2192 green, confirmed test failed for the right reason before implementing), `code-review` (self-reviewed diff; scope-limited)\\n- TDD: green \\u2014 `python3 -m unittest discover -s tests` \\u2192 298 tests OK\\n- CCA: pending (loop-owned GitHub run)\\n- MergeGate: pending (loop-owned)\\n\\n## Evidence\\n- `python3 scripts/validate_shiki.py` \\u2192 Shiki validation passed\\n- `python3 -m unittest discover -s tests` \\u2192 Ran 298 tests, OK\\n- All 34 `scripts/test_shiki_*.sh` passed; `test_shiki_shellcheck.sh --strict` and `test_shiki_workflow_lint.sh --strict` passed\\n- `git status` confirms only the 6 locked-scope files changed; no commits/PRs created\\n\\n## Next action\\n- Shiki loop: commit, push, open PR with the MergeGate body, run checks/CCA, merge per gate. (The one `validate_shiki` WARNING in the start test is a pre-existing fixture-task lock-coverage notice, non-blocking and unrelated to this change.)\\n\",\n.shiki/tasks/T-20260803T013839640984Z-2411811e.json:80:    \"code-review\"\n.shiki/tasks/T-20260612T062731178333Z-36e9bdc7.json:7:    \"This PR carries pre-PR code-review ledger evidence (PR-12)\"\n.shiki/tasks/T-20260612T062731178333Z-36e9bdc7.json:58:    \"code-review\"\n.shiki/runner/EXEC-20260806T095426480232Z-9a627a2e.json:8:  \"stdout\": \"Implementation complete and verified. Here is the structured report.\\n\\n## Result\\n- **Goal/task:** G-20260806T065143407702Z-8e3284ce / T-20260806T065143409817Z-dbdae1cf\\n- **Role:** Implementer (Claude Code, default runtime)\\n- **Status:** ready (implementation green in the worktree; the goal loop owns commit/push/PR/merge)\\n\\n## What changed or decided\\nThe amendment (PR #308) reopened this task to **commit** the coordinator mirror rather than merely stage it. I re-measured the premise before touching code (minimal three-repo repro): with the colliding path byte-identical, `staged + fast-forward` merge succeeds, but `staged + non-fast-forward` aborts with *\\\"Your local changes \\u2026 would be overwritten by merge\\\"*, while `committed + non-fast-forward` merges cleanly via the ort strategy. The coordinator always carries its own commits, so it is **always** the non-fast-forward case.\\n\\nImplementation (`scripts/shiki_loop.py`, all inside the single covering point):\\n- `_stage_coordinator_mirror` \\u2192 `_commit_coordinator_mirror`, still called from `execute_action`'s `finally` (the one point every one of the eleven effectors passes through).\\n- `git add --ignore-removal -- .shiki` (never a deletion \\u2192 append-only mirror can't be emptied), then \\u2014 only if something is staged \\u2014 a plain **index** `git commit --no-verify`. I deliberately avoided `git commit -- .shiki`: measurement showed the pathspec form is `--only` mode, re-reads the working tree, and records a `.shiki` deletion anyway, defeating `--ignore-removal`.\\n- Scoping to `.shiki` holds because the coordinator stages nothing else (audited: both coordinator-side `git add`s are `.shiki`-only; every `git push`/`git add -A` runs in a worktree, never `cwd=target`).\\n- The genuinely-differing case is **documented, not suppressed**: it surfaces as a normal `CONFLICT (add/add)` for the operator; no merge driver, strategy option, or auto-resolution added.\\n\\nTests corrected so **every merge assertion builds a divergent (non-fast-forward) repository** \\u2014 the old tests passed vacuously because their coordinator HEAD was an ancestor of `origin/main`. Each merge is now guarded by `assertFalse(merge-base --is-ancestor HEAD origin/main)` before merging; committing makes the same `CloseoutEnv` construction divergent by itself. Added `test_genuinely_differing_file_produces_conflict_not_refusal`; asserted second-cycle success, nothing-outside-`.shiki`, and no-mirror-record-deleted.\\n\\n## Checklist status\\n- **Skill Gate:** `tdd` followed (red \\u2192 green \\u2192 refactor, tracer-first, verified RED against staging then GREEN). `code-review` is user-triggered/`/code-review` (cannot self-invoke); the loop's `create_pr` effector runs the independent pre-PR reviewer. Did an adversarial self-review against all 10 acceptance checks.\\n- **TDD:** green.\\n- **CCA:** not run here (GitHub-side judge).\\n- **MergeGate:** owned by the loop.\\n\\n## Evidence\\n- `python3 -m unittest discover -s tests` \\u2192 **Ran 1090 tests, OK** (baseline 1089 + 1 new conflict test).\\n- `bash scripts/test_shiki_goal_loop.sh` \\u2192 **passed** (real 14-cycle `loop run`, now committing per effector).\\n- `python3 scripts/validate_shiki.py --target .` \\u2192 **Shiki validation passed**.\\n- Files changed = exactly the 3 locks (`scripts/shiki_loop.py`, `tests/test_closeout.py`, `tests/test_dispatch_state_sync.py`). `_sync_state_to_branch` signature and `_evidence_relatives_for_task` output unchanged. The two `.shiki/{tasks,locks}/\\u2026dbdae1cf.json` shown are loop-owned and were already modified at session start \\u2014 I did not touch them.\\n\\n## Next action\\n- The Shiki goal loop commits/pushes this worktree, opens the PR with the MergeGate body, runs the pre-PR reviewer + CCA, and merges under MergeGate (risk medium). No manual step required from me.\\n\\nOne residual invariant to note for reviewers (already flagged non-blocking in the task's prior review): the index commit's \\\"nothing outside `.shiki`\\\" guarantee rests on the coordinator never staging anything else \\u2014 true today and documented in the function, but not otherwise enforced.\\n\",\n.shiki/runner/EXEC-20260731T055343359795Z-b8530d12.json:8:  \"stdout\": \"The implementation is complete and verified. Here's the summary.\\n\\n## Result\\n- **Goal/task:** G-20260729T065622761503Z-4479b0d3 / T-20260729T065622770078Z-3662bff2 \\u2014 name the lock-conflict stop instead of skipping\\n- **Role:** Implementer (Claude Code, default runtime)\\n- **Status:** ready (edited in worktree; the loop owns commit/push/PR)\\n\\n## What changed\\n`scripts/shiki_loop.py` \\u2014 `_unblock_ready_tasks` no longer silently `continue`s on a lock conflict:\\n- It now returns `(unblocked, lock_blocked)`. When a dependency-complete task can't acquire its locks because another task holds an overlapping **active** lock, it records the conflict (owner + overlapping locks, deduped per owner).\\n- New `stop_lock_blocked` action (added to `STOP_ACTIONS`) is synthesized by the `unblock` effector when nothing unblocks but conflicts exist. It names the owning task and locks and states \\\"serialization, not a deadlock\\\". A conflict with no lock owner (genuinely incomplete deps) stays the generic `stop_blocked`.\\n- Dedup (`_group_lock_conflicts`) happens **in the loop** \\u2014 the shared `active_lock_conflicts` in `shiki_locks.py` (which MergeGate depends on, and which is outside my locks) is untouched.\\n\\n## Checklist status\\n- **Skill Gate:** `tdd` (red\\u2192green, 11 new tests) \\u2713; `code-review` \\u2014 ran a manual diff review (the automated `/code-review` skill isn't wired in this environment); noted below.\\n- **TDD:** RED confirmed (ImportError) \\u2192 implemented \\u2192 GREEN.\\n- **CCA/MergeGate:** owned by the loop; not run locally.\\n\\n## Evidence (local, diagnostic)\\n- `tests/test_lock_conflict_stop.py`: **11/11 pass** \\u2014 covers all 4 acceptance checks (named owner+locks; distinct action; one-message-per-owner dedup; every-task-blocked reports the fact).\\n- Loop regression suite: **138/138**; full Python suite: **595/595**.\\n- `scripts/test_shiki_goal_loop.sh` (extended with a live lock-conflict scenario): **passes** \\u2014 real CLI `loop step` exits non-zero with `stop_lock_blocked` naming the owner + overlapping lock.\\n- `python3 scripts/validate_shiki.py`: passed.\\n\\n## Acceptance checks \\u2192 coverage\\n1. Named blocked-on-lock result with owning task id + overlapping locks \\u2713\\n2. Distinct `stop_lock_blocked` action (\\u2260 `stop_guardian`, \\u2260 `stop_blocked`) \\u2713\\n3. Several locks, one owner \\u2192 one message \\u2713\\n4. Every-task-lock-blocked routes to `unblock`\\u2192`stop_lock_blocked` (not idle `none`), reporting each blocked task \\u2713\\n\\n## Self-review notes\\n- Scope: only the three locked files touched; no product source, no approval records, no commit/push/PR.\\n- `mark_done`'s opportunistic unblock discards the lock signal deliberately \\u2014 it re-surfaces on the next `unblock` cycle when the task is the sole blocker, so no premature stop while progress is possible.\\n- Stops are consumed uniformly via `in STOP_ACTIONS`, so CLI exit code, `cmd_loop_run` outcome, and `capture_failure` all handle the new action without further changes.\\n\\n## Next action\\n- Hand back to the Shiki loop for commit/push/PR + MergeGate/CCA. One caveat for the reviewer: the `code-review` skill couldn't be invoked in-session (not registered here), so a CCA/reviewer pass on the diff is still owed.\\n\",\n.shiki/runner/EXEC-20260803T072325304309Z-aa222de7.json:8:  \"stdout\": \"All work is complete and verified. I've left commit/push/PR to the Shiki loop (per the execution protocol) and did not touch the loop-owned task/lock records.\\n\\n## Result\\n- Goal/task: G-20260803T013839638288Z-eebb39bf / T-20260803T013839641506Z-d11bb45f\\n- Role: Implementer (Claude Code default)\\n- Status: ready (implementation complete, all acceptance checks verified locally; loop owns delivery)\\n\\n## What changed or decided\\nThree behaviors, confined to the 5 declared locks:\\n\\n1. **Install stamp** (`scripts/shiki_installer.py`) \\u2014 every install/upgrade writes `.shiki/install-stamp.json`: platform commit, install timestamp, and a per-path SHA-256 over the **platform-owned (overwrite-category) shipped files** (project-content/governance/preserve-outright are excluded so per-target customization isn't false drift). It's **untracked local state**; the installer idempotently ensures the target's `.gitignore` ignores it \\u2014 because a tracked unknown `.shiki/` path fails `validate_shiki` and the loop's `git add -A` would otherwise commit it into a permanently-red Validate check. A no-op re-install leaves the stamp byte-identical; a proven-older lineage never overwrites a newer one.\\n2. **Doctor drift** (`scripts/shiki_doctor.py`) \\u2014 three new findings with their own ids: `doctor.install_stamp.present` (absent \\u2192 `warn`, distinct, never a pass), `.content` (hand-edit/half-upgrade \\u2192 `warn`, names the exact drifted paths), `.platform_commit` (stamp not an ancestor of the running platform HEAD \\u2192 `warn`). All `warn`/`skip`, so `--strict` fails on them; doctor never writes to the target.\\n3. **Migrate `--target` fix** (`scripts/shiki_migrations.py`) \\u2014 `cmd_migrate` **refuses** when `--target` names a repository whose own `scripts/shiki_migrations.py` **differs** (byte-compare) from the running platform's, with a message naming the correct command (`cd <target> && \\u2026 migrate <cmd>`). A freshly installed target (identical registry) and `--target == platform root` are allowed, so the safer-default refusal never fires spuriously.\\n\\n## Checklist status\\n- Skill Gate: `tdd` (red\\u2192green via the new test + full suite), `code-review` (self-review of the diff, only orphan-free additions)\\n- PRD/issues: N/A (task-scoped handoff)\\n- TDD: `tests/test_install_version_drift.py` \\u2014 13 cases covering every acceptance item (stamp shape, non-committable, upgrade-refresh, no-drift, content drift naming the path, absent-never-a-pass, newer-commit reported, `--strict` on drift & absent, doctor-never-writes, older-content guard, migrate resolve-or-refuse asserted directly)\\n- CCA/MergeGate: owned downstream by the loop\\n\\n## Evidence\\n- `python3 scripts/validate_shiki.py` \\u2192 **Shiki validation passed**\\n- `python3 -m unittest discover -s tests` \\u2192 **Ran 792 tests \\u2026 OK**\\n- All **34** `scripts/test_shiki_*.sh` \\u2192 **ALL SHELL TESTS PASSED** (incl. the previously-failing `test_shiki_code_review_gate.sh`, and `test_shiki_init.sh` install-verify which re-runs the full contract suite inside a fresh target)\\n- New file suite \\u2192 **Ran 13 tests \\u2026 OK**; `shellcheck` on the modified doctor test \\u2192 OK; `py_compile` all three modules \\u2192 OK\\n- `git diff --name-only HEAD` \\u2192 only the 5 locked files (the two `.shiki/T-\\u2026` records were present at session start and are loop-owned; left untouched)\\n\\n## Next action\\n- Loop: commit, push, open the MergeGate PR, and run CCA. No blockers.\\n\",\n.shiki/runner/EXEC-20260805T031155409611Z-a13c0017.json:8:  \"stdout\": \"All work is complete and verified. The remaining `\\\"main\\\"` references are correctly out of scope: line 795 is my intentional fallback (reproducing today's behaviour), the `git fetch origin main` calls already target origin, and the `--base main` / `_sync_branch` defaults are GitHub PR base **branch names** (which must be bare names, not remote-tracking refs).\\n\\n## Result\\n- Goal/task: G-20260805T023333273780Z-cd2e69ec / T-20260805T023333275527Z-fff69d84\\n- Role: Implementer (Claude Code)\\n- Status: ready \\u2014 implementation complete, all acceptance gates green. The Shiki loop owns commit/push/PR/merge.\\n\\n## What changed\\n- **`scripts/shiki_loop.py`** \\u2014 added a shared `_default_base_ref(target)` helper that calls `shiki_runtime.resolve_default_branch_ref` (imported into the existing line-30 import, not reimplemented) and degrades to bare `\\\"main\\\"` on any failure so it never raises into the loop. Wired it into all three sites:\\n  - ahead-count: `git rev-list --count {base}..HEAD` (was `main..HEAD`)\\n  - reviewer diff: `git diff --cached {base}` (was `--cached main`)\\n  - fallback: `git diff {base}...HEAD` (was `main...HEAD`)\\n  - Corrected the reviewer-diff comment and the `_CODE_REVIEW_PROMPT` string that named a bare `main`.\\n- **`tests/test_loop_code_review.py`** \\u2014 new `ReviewerDiffBaseTests` builds a real repo whose local `main` (and tracking ref) lag a 60-file merged remote history, cuts the task branch from the true remote tip, drives `_run_pre_pr_code_review` with a stdin-capturing reviewer stub, and asserts the captured diff is **exactly 1 file** (`feature.py`), excludes `bulk/`, and is `< stale_diff/10` bytes.\\n- **`tests/test_repair_delivery.py`** \\u2014 `worktree_commits_beyond_main` now resolves its base via `resolve_default_branch_ref` (same call the loop makes) so harness and loop can't drift; `RepairEnv` pins `origin/HEAD \\u2192 origin/main` for deterministic resolution.\\n\\n## Checklist status\\n- Skill Gate: tdd (regression written, proven red\\u2192green), code-review (self-reviewed diff, broadened the effector `except` to match the T1 fail-closed pattern)\\n- TDD: regression **fails loudly** when any site is reverted to bare `main` \\u2014 proven: `AssertionError: 61 != 1`\\n- `_run_pre_pr_code_review` signature `(target, task_id)` unchanged; `test_loop_tdd_evidence.py` monkeypatch still passes\\n\\n## Evidence\\n- `python3 -m unittest discover -s tests` \\u2192 **Ran 968 tests, OK**\\n- `scripts/test_shiki_goal_loop.sh` \\u2192 passed\\n- `scripts/test_shiki_module_boundaries.sh` \\u2192 passed\\n- `scripts/validate_shiki.py` \\u2192 Shiki validation passed\\n- `scripts/shiki_runtime.py` unchanged (empty diff); only the 3 locked files modified\\n\\n## Next action\\n- Loop: run pre-PR review \\u2192 create_pr \\u2192 MergeGate \\u2192 CCA. No blockers from the implementation side.\\n\",\n.shiki/tasks/T-20260730T110603197694Z-c903d404.json:68:    \"code-review\"\n.shiki/runner/EXEC-20260729T230916953145Z-3e06f145.json:8:  \"stdout\": \"Shiki validation passed\\ne2e: loop-owned T3 code-review THEN T2 tdd-evidence ledgers, in the implemented order\\nshiki loop e2e: integrated self-drive (code-review -> TDD -> commit/push -> auto-merge) + post-PR repair verified\\nrepair packet content satisfies schema\\nP2.3.9 repair attempt-limit and packet content passed\\nShiki validation passed\\nshiki goal loop tests passed\\n{\\n  \\\"handoff_file\\\": \\\"/private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/tmp2l4dp475/repo/.shiki/handoffs/RP-20260729T230912468419Z-4bae83e0-repair.md\\\",\\n  \\\"ledger_id\\\": \\\"L-20260729T230912486490Z-40969736\\\",\\n  \\\"repair_id\\\": \\\"RP-20260729T230912468419Z-4bae83e0\\\"\\n}\\n{\\n  \\\"handoff_file\\\": \\\"/private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/tmpjmwbdgwd/repo/.shiki/handoffs/RP-20260729T230914275259Z-cb3a8e16-repair.md\\\",\\n  \\\"ledger_id\\\": \\\"L-20260729T230914293001Z-739ce7f3\\\",\\n  \\\"repair_id\\\": \\\"RP-20260729T230914275259Z-cb3a8e16\\\"\\n}\\n{\\n  \\\"handoff_file\\\": \\\"/private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/tmpoz9ny1_8/repo/.shiki/handoffs/RP-20260729T230914708189Z-a1f59e83-repair.md\\\",\\n  \\\"ledger_id\\\": \\\"L-20260729T230914726342Z-b56cef84\\\",\\n  \\\"repair_id\\\": \\\"RP-20260729T230914708189Z-a1f59e83\\\"\\n}\\n{\\n  \\\"handoff_file\\\": \\\"/private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/tmpabahws42/repo/.shiki/handoffs/RP-20260729T230915196233Z-e570c933-repair.md\\\",\\n  \\\"ledger_id\\\": \\\"L-20260729T230915213310Z-4c9810d1\\\",\\n  \\\"repair_id\\\": \\\"RP-20260729T230915196233Z-e570c933\\\"\\n}\\n[shiki] token verified: ok\\n[shiki] token verified: ok\\n{\\n  \\\"repo\\\": \\\"owner/name\\\",\\n  \\\"secret\\\": \\\"CLAUDE_CODE_OAUTH_TOKEN\\\",\\n  \\\"source\\\": \\\"env:SHIKI_TEST_TOK\\\",\\n  \\\"verified\\\": true\\n}\\n\",\n.shiki/runner/EXEC-20260803T051754393727Z-b595226c.json:8:  \"stdout\": \"All work complete and verified. Final report:\\n\\n## Result\\n- Goal/task: G-20260803T013839638288Z-eebb39bf / T-20260803T013839640984Z-2411811e\\n- Role: Implementer (Claude Code)\\n- Status: ready (implementation complete, local evidence green; the Shiki loop owns commit/PR/MergeGate)\\n\\n## What changed or decided\\nReplaced the two-way preserve/overwrite `--force` split with a three-way one in `scripts/shiki_installer.py`:\\n- **Project content** (`CONTEXT.md`, `AGENTS.md`, `CLAUDE.md`, `.github/CODEOWNERS`) \\u2014 never overwritten; the template is written alongside as `<file>.new`.\\n- **Governance contract** (`.shiki/config.yaml`, `.shiki/guardian-policy.json`) \\u2014 never silently kept: `<file>.new` written **and** a per-key diff reported, collapsing under the critical keys so `mergegate.required_checks` and `approval_sources` are named explicitly when they differ.\\n- **`.shiki/migrations/state.json`** \\u2014 preserved outright (no `.new`); **everything else** overwritten.\\n- A single end-of-run `template summary:` lists every `.new` written.\\n- **Precondition moved ahead of any write**: `refuse_pending_migrations` runs first in `install_template`, so a target whose valid mirror is behind the registry is refused with nothing written (gated on `state_exists and not errors and pending` \\u2014 the `not errors` clause is what keeps `test_bootstrap_safety`'s bogus-state fixture, which is outside my locks, green).\\n- `scripts/shiki_contracts.py`: added `codeowners_required_owner(target)` resolving `@OWNER` from `.shiki/repo.json`, keeping `@mizutani-140` as this repo's default.\\n- `scripts/shiki_cli.py`: `--force` help now describes the three-way upgrade behaviour.\\n- `docs/operations.md` / `docs/releasing.md`: `--force` documented as required for an upgrade (idempotent-re-run claim removed), plus a `.new` convention section.\\n\\n## Checklist status\\n- Skill Gate: `tdd` (red\\u2192green, `tests/test_install_force_semantics.py`), `code-review` (self-review of diff)\\n- TDD: 11 new tests, written failing first, then implemented to green\\n- CCA/MergeGate: owned by the loop (not run locally)\\n\\n## Evidence\\n- `validate_shiki.py`: **passed**\\n- `py_compile scripts/*.py`: **passed**\\n- `unittest discover -s tests`: **779 passed** (incl. new 11 and unchanged `test_bootstrap_safety`)\\n- All `scripts/test_shiki_*.sh`: **PASS** (incl. `test_shiki_init.sh` nested install-verify \\u2014 the fresh target runs the new test and passes its own required checks)\\n- Demonstrated summary output naming `mergegate.required_checks` and `approval_sources`\\n- Changed files all within declared locks; `.shiki/tasks/*.json` was pre-modified at session start (loop-owned, untouched)\\n\\n## Next action\\n- Loop: create the PR with the MergeGate body, run CCA/MergeGate, merge.\\n- Follow-up (out of this task's locks, noted in memory): wire `codeowners_required_owner` into `validate_shiki.py:1574` and `shiki_doctor.py:447`, which still use the hardcoded constant \\u2014 the resolver is currently proved by unit test but not enforced there.\\n\",\n.shiki/tasks/T-20260617T090739479224Z-cfdbfa06.json:36:    \"No code-review verifier (T3).\",\n.shiki/tasks/T-20260617T090739479224Z-cfdbfa06.json:41:    \"code-review\"\n.shiki/tasks/T-20260617T090739482036Z-c92b21b3.json:39:    \"code-review\"\n.shiki/runner/EXEC-20260731T041553719542Z-37560f44.json:8:  \"stdout\": \"Everything passes and only the two locked files changed. The implementation is complete.\\n\\n## Result\\n- **Goal/task:** G-20260731T040711236459Z-50972900 / T-20260731T040711237895Z-21826083\\n- **Role:** Implementer (Claude Code)\\n- **Status:** ready\\n\\n## What changed\\n- **`scripts/mergegate_check.py`** \\u2014 Added `expected_branch` to `_CLOSEOUT_MUTABLE_TASK_FIELDS`. ADR 0017's condition 4 now stops rejecting a closeout for the branch rewrite the goal loop performs on every closeout by construction. Condition 4's logic and the other five conditions are byte-unchanged; only the whitelist set grew by one entry.\\n  - Documented at the definition **why it is safe**: `expected_branch` is not a governance field, and MergeGate independently blocks a PR whose task `expected_branch` differs from the PR head ref (`main`'s metadata gate: `expected_branch != headRefName`), so a PR cannot name a branch it is not on. Flagged that binding as load-bearing and not to be weakened. Also refreshed the stale condition-4 summary in the `is_bookkeeping_closeout` docstring.\\n- **`tests/test_bookkeeping_closeout.py`** \\u2014 Reshaped the fixture to a **real** closeout (base task on the implementation branch `IMPL_BRANCH`, head task on `<branch>-closeout` `CLOSEOUT_BRANCH`), so every positive case now exercises the field the loop always changes \\u2014 closing the exact blind spot that let the bug survive. Added:\\n  - `test_positive_expected_branch_differs_is_classified` \\u2014 asserts a closeout whose base/head `expected_branch` **differ** classifies True (fails if `expected_branch` is removed from the mutable set).\\n  - `test_every_governance_field_change_disqualifies` \\u2014 pins the mutable set to exactly the five allowed entries, and tampers every field of the frozen `_GOVERNANCE` contract (derived from the hardcoded contract, not the complement of the mutable set) to prove each still disqualifies \\u2014 so widening the whitelist by one entry cannot drift into widening it further.\\n\\n## Checklist status\\n- Skill Gate: `tdd` (red \\u2192 green verified: all real-shape positives failed at condition 4 before the source change, pass after), `code-review` (self-review of both diffs; corrected a docstring that cited a nonexistent function and a guard that could be silently defeated).\\n- TDD: red state captured (14 failures), green after fix.\\n- Verification: `test_bookkeeping_closeout.py` 25/25; full suite 579/579 OK; `scripts/test_shiki_governance_evidence.sh` and `scripts/test_shiki_adversarial_state.sh` green and unmodified.\\n\\n## Evidence\\n- Local: full suite `Ran 579 tests \\u2026 OK`; both required shell scripts exit 0.\\n- Locks respected: `git status` shows only `scripts/mergegate_check.py` and `tests/test_bookkeeping_closeout.py`.\\n\\n## Next action\\n- The Shiki goal loop owns commit/push/PR/merge. Durable CI evidence and CCA judgment will run on the PR the loop opens \\u2014 I made no git/gh/PR actions per the execution protocol.\\n\",\n.shiki/runner/EXEC-20260729T040443270359Z-8c81e824.json:8:  \"stdout\": \"The core redesign is complete and fully verified. Here's my report.\\n\\n## Result\\n- **Goal/task:** G-20260728\\u2026-2514c606 / T-20260728\\u2026-f198cd24\\n- **Role:** Implementer (TDD)\\n- **Status:** ready (6 of 7 acceptance checks delivered; #7 \\u2014 the `validate_shiki` advisory \\u2014 is **blocked on a lock-scope decision**, surfaced below)\\n\\n## What changed\\nReversed the **injection** model that shipped staged in the worktree into the **derive-at-read-time** model the scope specifies. Net diff vs HEAD: only `scripts/mergegate_check.py` (+86/\\u22121) and `tests/test_mirror_lock_injection.py` (new).\\n\\n- **`scripts/shiki_tasks.py`, `scripts/shiki_locks.py`, `scripts/test_shiki_control_plane.sh`** \\u2192 restored to HEAD (all injection helpers, call sites, and the `is_goal_scoped_mirror_lock` exemption removed). Locks now stay **exactly** what the plan declared, at registration, lock-acquire, and worktree-allocate. Frozen matching is exact again (`locks` is back in `_FROZEN_TASK_MATCH_FIELDS`, restored by the revert \\u2014 no injected-set tolerance).\\n- **`scripts/mergegate_check.py`** \\u2192 new `_derive_task_mirror_locks(target, task)`: direct id-scoped paths (`tasks/worktrees/locks/{task_id}`, `goals/dag/{goal_id}`) plus, for each ledger in `ledger_evidence` **whose own `goal_id` AND `task_id` match the task**, its ledger path and every `.shiki`-contained evidence ref (the transitive `runner/EXEC`, `reports/R`). Unioned into `effective_locks` for `files_outside_locks` only; lock-conflict detection stays on declared locks. Bounding is what stops a PR-authored task file from widening its own coverage via a foreign ledger.\\n- **`tests/test_mirror_lock_injection.py`** \\u2192 rewritten (21 tests) to pin the new model (kept the locked filename).\\n\\n## Checklist status\\n- **Skill Gate:** `tdd` followed (red \\u2192 green); `code-review` skill is user-triggered only (`disable-model-invocation`) \\u2014 I did a rigorous manual review + audited all 7 loop `append_ledger` sites (all set matching `task_id`/`goal_id`, so the bounded derivation never under-covers a legitimate loop PR). Run `/code-review` for the recorded pre-PR gate.\\n- **TDD:** \\u2705 21 new tests\\n- **MergeGate:** files-outside-locks now derived; frozen matching exact\\n\\n## Evidence\\n- `test_mirror_lock_injection.py`: **21 passed** \\u00b7 full python suite: **433 passed** \\u00b7 `test_shiki_control_plane.sh`, `test_shiki_adversarial_state.sh`, `test_shiki_loop_lock_guard.sh`: **PASS** \\u00b7 `validate_shiki.py` on repo: **PASS** \\u00b7 compile/import: OK\\n- Acceptance 1\\u20135 + corpus: covered by the new suite. #7 (both shell suites green): \\u2705\\n\\n## Next action \\u2014 one decision needed from you\\nAcceptance check #6 (`validate_shiki` advisory) requires editing **three files outside my declared Locks**: `scripts/validate_shiki.py` (the `loop_lock_warnings` text) and its two pinning tests `tests/test_loop_lock_validator_hint.py` + `scripts/test_shiki_loop_lock_guard.sh` (which currently assert it **must** warn). Editing them would trip MergeGate's own `files_outside_locks` gate on this PR. The advisory is **warn-only/non-blocking**, and this task's plan declares `path:.shiki/**`, so the advisory doesn't even fire for this task \\u2014 the core PR is complete and mergeable without it.\\n\\n**Choose one:**\\n1. **Amend the task's locks** to add those 3 paths (a spec/lock amendment) \\u2014 I'll then rewrite the advisory to stop warning on narrow-lock tasks and invert the two tests.\\n2. **Defer** the advisory cleanup to a follow-up task with the correct locks.\\n\\nI stopped at the lock boundary rather than silently touching files outside it. Which do you want?\\n\",\n.shiki/runner/EXEC-20260805T012611238196Z-57089b50.json:8:  \"stdout\": \"All checks green end-to-end. Implementation is complete. Per the execution protocol, I've made no commits/PRs \\u2014 the Shiki loop owns delivery.\\n\\n## Result\\n- Goal/task: G-20260801T041653272590Z-81f6f71d / **T-20260801T041653274543Z-6df3af7e**\\n- Role: Implementer (Claude Code)\\n- Status: **complete** (implementation + local verification green; awaiting loop-owned PR/CI for durable evidence)\\n\\n## What changed or decided\\nAdded a classify \\u2192 retry-once \\u2192 enforce guard around the CCA verdict so an unusable generation is regenerated inside the run instead of forcing a manual `gh run rerun`.\\n\\n- **`scripts/cca_verdict_usable.py`** (new): pure `verdict_usability(structured_output) -> (classification, reason)`. `absent` (missing/empty), `unparseable` (not a JSON object carrying a valid verdict value), `degenerate` (valid shape but empty checklist or empty acceptance \\u2014 judges nothing), else `usable`. A well-formed verdict of **any** value (incl. `repair_required`/`needs_guardian`/`blocked`/`insufficient_evidence`) is `usable` and never retried. A `main()` CLI does all I/O (reads `STRUCTURED_OUTPUT`, writes `classification`/`reason` to `$GITHUB_OUTPUT`, echoes to the log).\\n- **`.github/workflows/shiki-cca-completion.yml`**: inserted `Classify CCA verdict` \\u2192 `Run CCA (retry)` (gated on `steps.classify.outputs.classification != 'usable'`, byte-identical prompt/schema/tools) \\u2192 `Select CCA verdict` (picks the effective output, records the retry decision, re-classifies) between `Run CCA` and `Enforce CCA verdict`. The enforce step now judges the selected output and **fails the job** when the final classification is still unusable \\u2014 closing the hole where a degenerate `complete` would pass enforcement silently \\u2014 surfacing the first generation's reason. `SHIKI_HEAD_SHA` line preserved; Guardian-blockers/manifest/upload/Review-Bridge steps untouched.\\n- **`scripts/shiki_installer.py`**: added `scripts/cca_verdict_usable.py` to `TEMPLATE_PATHS` (the workflow now references it, so install-completeness requires it shipped).\\n- **`scripts/test_shiki_control_plane.sh`**: asserts classify-before-enforce, exactly-one-retry (2 action uses), retry gated on `!= 'usable'`, and the enforce fail-guard.\\n- **`tests/test_cca_verdict_usable.py`** (new, 22 tests): every classifier case incl. one `usable` case per verdict value, purity/no-I/O, and CLI `$GITHUB_OUTPUT` wiring.\\n\\n## Checklist status\\n- Skill Gate: `tdd` (red \\u2192 green on the classifier), `code-review` (reviewed the diff; verified retry prompt byte-identity, needs_guardian path preserved, env-not-expression injection safety, fail-closed edges)\\n- TDD: \\u2705 classifier test written first, confirmed red (module missing), then green\\n- CCA/MergeGate: n/a locally \\u2014 owned by the loop/CI\\n\\n## Evidence (local, diagnostic until CI records it)\\n- `python3 -m unittest discover -s tests` \\u2192 **Ran 941 tests, OK**\\n- `bash scripts/test_shiki_control_plane.sh` \\u2192 **passed** (incl. new assertions)\\n- `python3 scripts/validate_shiki.py --target .` \\u2192 **passed**; full chained `test_command` \\u2192 **exit 0**\\n- `test_shiki_workflow_lint.sh` (actionlint) + `test_shiki_shellcheck.sh` \\u2192 **passed**\\n- Step order confirmed: Run CCA \\u2192 Classify \\u2192 Run CCA (retry) \\u2192 Select \\u2192 Enforce \\u2192 (unchanged) Post Guardian blockers / Build manifest / Upload\\n- `git status`: only the 5 locked files touched (task JSON is loop-owned session state, not my edit)\\n\\n## Next action\\n- Hand back to the Shiki goal loop to commit, push, open the MergeGate PR, and run CI/CCA. This is a **high-risk** task (workflow surface), so MergeGate will require Guardian approval before merge \\u2014 I did not and must not author that approval.\\n\",\n.shiki/runner/EXEC-20260729T111332208637Z-4e9cb966.json:8:  \"stdout\": \"## Result\\n- Goal/task: `G-20260729T065622761503Z-4479b0d3` / `T-20260729T065622769632Z-9ee6b8e0`\\n- Role: Implementer (Claude Code, dispatched into the task worktree)\\n- Status: ready (implementation complete + locally verified; the Shiki loop owns commit/push/PR/MergeGate)\\n\\n## What changed or decided\\n`ensure_physical_worktree` cut a **new** task branch with `git worktree add -b <branch> <path>` (no start-point), so git used HEAD \\u2014 whatever branch the coordinator sat on. On a coordinator branch carrying other goals' `.shiki` state that dragged foreign task/goal files and unlisted ledgers into the task PR (the 56-blocking-reason failure), contradicting `shiki_loop.py:481`'s \\\"cut from main\\\" assertion.\\n\\nFix (all inside the 3 declared locks):\\n- **`scripts/shiki_runtime.py`** \\u2014 added `resolve_default_branch_ref()` (+ `_default_branch_name()`), and changed the new-branch path to `git worktree add -b <branch> <path> <base>` where `<base>` is the resolved default branch. Default-branch **name** precedence: git's `refs/remotes/origin/HEAD` \\u2192 saved Shiki config `default_branch` \\u2192 `\\\"main\\\"`. The name is bound to an existing local commit-ish (local branch, then `origin/<name>`); when neither exists it raises `ShikiError` with a **named reason** instead of silently cutting from HEAD. The existing-branch checkout path and the `path.exists()` reuse path are untouched.\\n- **`tests/test_worktree_base.py`** (new) \\u2014 7 unittest cases covering all acceptance behaviors.\\n- **`scripts/test_shiki_runner_claude.sh`** \\u2014 the coordinator now moves onto a feature branch carrying a foreign `.shiki/goals/*.json` before dispatch; asserts the resulting worktree tip == `main` tip and the foreign file is absent. Also isolates `HOME` so the resolver's config lookup can't read the operator's real global config (test is now machine-independent).\\n\\n## Checklist status\\n- Skill Gate: `tdd` followed (RED\\u2192GREEN, vertical slices); `code-review` skill is user-trigger-only, so the pre-PR gate ran as a thorough self-review + adversarial subagent pass over the diff.\\n- PRD/issues: n/a (executing an assigned task contract).\\n- TDD: tracer bullet went RED (`worktree_HEAD` = feature tip) \\u2192 GREEN after the fix; full suite grown incrementally.\\n- CCA: not run locally (GitHub CCA judges on the PR).\\n- MergeGate: not evaluated by me; loop-owned. No commit/push/PR made.\\n\\n## Evidence (local, diagnostic until the loop records it in the ledger)\\n- Python: `python3 -m unittest discover -s tests` \\u2192 **457 tests OK** (the CI command, `.github/workflows/shiki-validate.yml:31`).\\n- Shell: `scripts/test_shiki_runner_claude.sh` \\u2192 passes; proven hermetic by re-running under a hostile `HOME` whose global config sets `default_branch: trunk` (still passes).\\n- `python3 scripts/validate_shiki.py` \\u2192 passed.\\n- Genuine-catcher proof: reverting the one-line fix makes 2 Python tests fail **and** the shell test exit 1 (no \\\"tests passed\\\" line).\\n- Review verdict: no blocker/major. Addressed minor findings \\u2014 shell-test hermeticity and a docstring over-claim. Diff touches only the 3 locked files.\\n\\n## Next action\\n- Loop: commit/push the worktree, open the task PR with the MergeGate body, run CCA + MergeGate.\\n- **Follow-up (out of scope \\u2014 not in this task's locks):** `scripts/shiki_tasks.py:900` (`cmd_worktree_allocate --create`) has the identical `git worktree add -b <branch> <path>` HEAD-cut bug; a future task should route it through `resolve_default_branch_ref` too. Flagging rather than touching it, since `shiki_tasks.py` is outside my declared locks.\\n\",\n.shiki/runner/EXEC-20260727T014357070587Z-987aa31d.json:8:  \"stdout\": \"Everything is complete, verified, and strictly within the declared locks.\\n\\n## Result\\n- **Goal/task:** G-20260727T005125900810Z-077718f7 / T-20260727T005125902419Z-6c2db440\\n- **Role:** Implementer (Claude Code, dispatched by the loop)\\n- **Status:** complete (implementation + evidence ready; loop owns commit/PR/merge)\\n\\n## What changed\\n- **MergeGate contract mode** (`scripts/mergegate_check.py`): refactored `enforce_goal_reconcile` into a generalized `_enforce_frozen_registration(mode, allow_goal_registration=\\u2026)`. `enforce_goal_reconcile` (drift, goal file forbidden) and the new `enforce_contract` (first registration, goal file allowed and matched to its frozen plan) both delegate to it \\u2014 deny-by-default, every task matched to its frozen plan definition on scope/non_goals/required_skills/risk_level/locks/acceptance_checks (+runtime), DAG coverage and dependency-edge binding intact. Added `contract_decision` (marker `<!-- shiki:contract -->` + `mergegate:contract` label, fail-closed), `_validate_contract_goal_registration` (canonical risk required, no downgrade), and `contract_guardian_risk` (forces the gate from the **stronger** of goal/frozen-plan risk). `main()` dispatches contract mode and enforces Guardian **independent of any CCA verdict**, failing closed if risk is unresolvable.\\n- **CLI** (`scripts/shiki_tasks.py`, `scripts/shiki_cli.py`): `shiki contract open --plan \\u2026` + `register_contract_from_plan` write only `.shiki` goal/task/DAG registration (no locks/worktrees/run files), never apply the label, and report that a maintainer must apply it. Duplicate-registration guard included.\\n- **Waiting ADR 0015 frozen plan**: `.shiki/plans/P-20260727T005125000000Z-0015c0de.json` (4-task, high-risk, spec-frozen).\\n- **Tests**: `tests/test_contract_mode.py` (validator/decision/guardian-risk + drift-preservation binding) and `tests/test_contract_open.py` (real CLI + `mergegate_check` subprocess) cover all 7 acceptance checks.\\n- CONTEXT.md Contract Approval terms and `docs/adr/0015` were already committed on the branch (unchanged, present, validate_shiki-clean).\\n\\n## Checklist status\\n- **Skill Gate:** tdd (tests-first, red\\u2192green) \\u2713; code-review \\u2713 (self + independent adversarial review that found a real fail-open, now fixed with a regression test)\\n- **TDD:** 412 Python tests pass\\n- **MergeGate readiness (evidence):** validate_shiki passes; shell gates pass \\u2014 governance-evidence, validator-hardening, module-boundaries, adversarial-state, control-plane, goal-loop, shellcheck, code-review-gate, init. Existing `goal_reconcile` behavior byte-preserved (45/45).\\n\\n## Evidence\\n- Full Python suite: **412 passed**; `python3 scripts/validate_shiki.py` \\u2192 \\\"Shiki validation passed\\\"\\n- Adversarial review Finding 1 (Guardian fail-open on unresolvable risk) \\u2192 fixed in both the validator and `main()`; Finding 2 (wording) \\u2192 restored; Finding 3 \\u2192 informational (already fails closed)\\n- Changed files (all within locks): `scripts/{mergegate_check,shiki_cli,shiki_tasks}.py`, `tests/test_contract_{mode,open}.py`, `.shiki/plans/P-\\u20260015c0de.json`. `scripts/shiki_contracts.py` reverted to original (it was outside my locks).\\n\\n## Next action\\n- Loop owns commit \\u2192 push \\u2192 Contract-PR-body/registration commit \\u2192 PR \\u2192 MergeGate \\u2192 merge for this task.\\n- Note (not changed by me, flagging for a Guardian/operator decision): `docs/adr/0015` status is still \\\"Proposed\\\" though the decision record is landed and operator-approved; flipping it to \\\"Accepted\\\" is a governance call outside this task's scope. The downstream ADR-0015 tasks (pre-dispatch gate, contract immutability, Contract Approval evaluation + CCA/MergeGate OR-merge) remain in the frozen plan.\\n\",\n.shiki/runner/EXEC-20260729T232436574500Z-7b1144ec.json:8:  \"stdout\": \"All changes are confined to the declared locks (all 8 files are exactly lock paths; nothing outside them). The task is fully implemented in the worktree and verified against every acceptance check.\\n\\n## Result\\n- Goal/task: G-20260729T065622761503Z-4479b0d3 / T-20260729T065622766508Z-d36555d8\\n- Role: Implementer (+ self-review)\\n- Status: **complete** (worktree ready for the loop to commit/push/PR)\\n\\n## What changed or decided\\nThe five scope changes are all in place and green:\\n\\n1. **`cmd_start` re-drives GitHub governance on retry** (`shiki_bootstrap.py`) \\u2014 now calls `initialize_target_from_start` unconditionally. A dry run prints the plan and stops; an `--execute` run re-drives secret / branch-protection / workflow-permission setup even when `.shiki/repo.json` + origin already exist from a failed first run (`cmd_init` is idempotent for filesystem/git/remote).\\n2. **Non-destructive checkout** (`shiki_git.py` new `checkout_branch`, used by `cmd_init` and `cmd_bootstrap_github`) \\u2014 no-op when already on the branch, plain `git checkout` when the branch exists (ref never moves), `-b` only to create; a refused checkout raises `ShikiError`. The old force-`-B` (which would reset an existing branch to HEAD) is gone. The lone remaining `-B` is the safe brand-new-repo init fallback (no commits to destroy).\\n3. **`--force` preserves operator state** (`shiki_installer.py`) \\u2014 `.shiki/guardian-policy.json`, `.shiki/config.yaml`, `.shiki/migrations/state.json` are create-if-absent even under `--force`, warning that each was preserved.\\n4. **Branch-protection 403 vs 404** (`shiki_doctor.py`) \\u2014 a permission failure (403 / \\\"forbidden\\\" / \\\"not accessible\\\") stays **warn**; a genuinely unprotected branch (404 / \\\"not protected\\\") is now **fail** (fail-closed on ambiguous errors so doctor never implies protection it couldn't confirm).\\n5. **Worktree registry no longer red in dispatch** (`shiki_doctor.py`) \\u2014 only worktrees under the Shiki worktree root (`<main-parent>/.worktrees`) that lack a record are orphaned **fails**; sibling worktrees elsewhere (the coordinator's session worktree seen via the shared gitdir, dev checkouts) carry no Shiki task and are **informational**, not failures.\\n\\n## Checklist status\\n- Skill Gate: `tdd` (red\\u2192green verified), `code-review` (adversarial self-review of full diff \\u2014 edge cases in regex classification, path discriminator, create-if-absent, dry-run semantics all checked)\\n- TDD: `tests/test_bootstrap_safety.py` (checkout + preserve invariants) + shell acceptance cases all green\\n- CCA/MergeGate: owned downstream; not run locally\\n\\n## Evidence\\n- `python3 scripts/validate_shiki.py` \\u2192 **passed**\\n- `python3 -m unittest discover -s tests` \\u2192 **465 passed** (CI-equivalent command)\\n- `test_shiki_start.sh` (A1 protection+workflow-perms retry, A2 secret retry, A3 unfixed still non-zero), `test_shiki_init.sh` (branch-safe/no-reset), `test_shiki_doctor.sh` (404\\u2192fail, 403\\u2192warn, informational-worktree\\u2192pass, orphan\\u2192fail), `test_shiki_p2_branch_protection_failure.sh` \\u2192 all **passed**\\n- `test_shiki_module_boundaries.sh`, `test_shiki_shellcheck.sh`, `test_shiki_workflow_lint.sh` \\u2192 **passed**\\n- Manual CLI check: `install-target --local-only --force` preserved a custom guardian policy + config.yaml and warned for all three stateful files\\n- Every one of the 7 acceptance checks maps to a passing test (verified individually)\\n- Diff: 8 files, all within declared locks (7 modified + `tests/test_bootstrap_safety.py`); nothing outside locks\\n\\nOne note for transparency: running `doctor --target .` against *this* live worktree still reports **one** orphan \\u2014 `~/.worktrees/shiki-pr178-secret-set-claude-repair`, a real stale Shiki worktree, correctly flagged (not a false positive). In a clean CI checkout there's a single worktree, so the finding passes and the previously-red `test_shiki_guardian_policy.sh:427` (`doctor --target .`) goes green.\\n\\n## Next action\\n- Loop to commit/push and open the PR with the MergeGate body, then run CCA/MergeGate. No source changes remain.\\n\",\n.shiki/runner/EXEC-20260731T050602251534Z-c3d7969e.json:8:  \"stdout\": \"Implementation complete and verified. Summary below.\\n\\n## Result\\n- Goal/task: G-20260731T045548842404Z-dac91fc1 / T-20260731T045548843829Z-ee2deb90\\n- Role: Implementer (Claude Code)\\n- Status: **ready** (loop owns delivery \\u2014 I did not commit/push/PR)\\n\\n## What changed\\n- **`.github/workflows/shiki-cca-completion.yml`** \\u2014 In the `Enforce MergeGate` step, hoisted the `MERGED_PRS` resolution **out of** the `<!-- shiki:post_merge_reconcile -->` marker conditional so it runs for **every judged PR**. It now resolves the value byte-for-byte the same way the `Prepare CCA context` step already does: task id via the same `mergegate_check.py` semantics \\u2192 **BASE snapshot's** `expected_pr` (`.shiki/gha/base-shiki/.shiki/tasks/<id>.json`, a field the PR head cannot forge) \\u2192 included only when `gh pr view` reports state `MERGED`. Fails closed to `\\\"\\\"`.\\n- **`tests/test_policy_job_merged_prs.py`** (new) \\u2014 5 shape assertions: resolution not nested in the marker test; read from base snapshot not head; included only when MERGED; parity with `Prepare CCA context` (so `post_merge_reconcile` PRs receive the same value); and a wiring-completeness enumeration requiring every `mergegate_check.py`/`guardian_approval_signal.py` invocation to carry both `--base-shiki` and `--merged-prs`.\\n\\n`post_merge_reconcile` behaviour is unchanged: it flows through the identical resolution steps, so its `--merged-prs` value is exactly what it was \\u2014 the change is purely additive.\\n\\n## Checklist status\\n- Skill Gate: `tdd` (RED \\u2192 GREEN documented below), `code-review` (self-review recorded)\\n- TDD: RED \\u2014 2 defect-targeting tests failed on the unchanged workflow; GREEN after hoisting the resolution\\n- CCA/MergeGate: owned by the loop's CI, not me\\n\\n## Evidence (local, diagnostic)\\n- New test: `python3 -m unittest discover -s tests -p test_policy_job_merged_prs.py` \\u2192 5 tests OK (was RED on 2 before the workflow edit)\\n- Full suite: `python3 -m unittest discover -s tests` \\u2192 **Ran 584 tests, OK** (exit 0)\\n- `bash scripts/test_shiki_workflow_lint.sh` \\u2192 actionlint passed (unmodified)\\n- `bash scripts/test_shiki_governance_evidence.sh` \\u2192 passed (unmodified)\\n- `git status --short` \\u2192 only the two locked files changed; nothing outside the locks\\n\\n## Next action\\n- Hand off to the Shiki loop for commit/PR/MergeGate. CI must reproduce the local green (workflow actionlint, governance-evidence, full unittest suite) and the CCA/MergeGate judgment for durable evidence.\\n\",\n.shiki/tasks/T-20260729T065622767228Z-3b4c41c2.json:45:    \"code-review\"\n.shiki/runner/EXEC-20260729T221606083738Z-63778092.json:8:  \"stdout\": \"All changes are within the declared locks (5 modified + 1 new test file), the loop's exact `test_command` gate exits 0, and the dispatch ledger already exists (loop-owned, outside my locks). Implementation is complete.\\n\\n## Result\\n- **Goal/task:** G-20260729T065622761503Z-4479b0d3 / T-20260729T065622763023Z-9b974770 \\u2014 *Make Guardian approval evidence current and revocable*\\n- **Role:** Implementer (Claude Code, dispatched)\\n- **Status:** ready (all acceptance checks satisfied; loop owns commit/PR/merge)\\n\\n## What changed\\n- **`scripts/shiki_guardian.py`**\\n  - `_comment_source`: the `guardian_comment` marker must now be **line-initial** to count as approval. Negation/revocation detection (`_comment_negates_approval`) runs on any comment that *contains* the marker (moved out of the old \\\"marker not in body\\\" branch), recording a **soft blocker** \\u2014 never a silent pass. New helpers `_marker_is_line_initial` / `_comment_negates_approval` + `COMMENT_NEGATION_MARKERS`.\\n  - `_valid_label_actor`: resolves the **latest** `labeled`/`unlabeled` transition for `policy.label` (by `created_at`, stable-tiebroken by input index) and accepts only when that latest transition is a `labeled` event by a configured Guardian.\\n- **`.github/workflows/shiki-cca-completion.yml`**: added `labeled`/`unlabeled` to `pull_request.types` (a label change re-runs the CCA\\u2192verdict on its own); pinned the `mergegate` job to **read-only** permissions; added `--paginate` to the 6 Guardian-evidence `gh api` calls so the latest transition / mid-run approval is never missed on page 2.\\n- **`scripts/validate_shiki.py`**: `WORKFLOW_CONTRACTS[\\\"shiki-cca-completion.yml\\\"]` now pins `job_permissions.mergegate` (all-read) \\u2014 removing the block fails the validator (verified by negative control).\\n- **Tests**: new `tests/test_guardian_evidence.py` (18 tests, run by the loop gate); added negation/revocation + latest-label-transition fixtures to `test_shiki_guardian_policy.sh`; added a full-gate exit-1 negated-marker fixture to `test_shiki_governance_evidence.sh`.\\n\\n## Checklist status\\n- **Skill Gate:** `tdd` (red \\u2192 green, tests written first) \\u2713; `code-review` \\u2014 repo `/code-review` is user-triggered, so ran the repo `skills/engineering/code-review` gate manually + one independent adversarial reviewer (critical risk).\\n- **TDD:** \\u2713 \\u2014 18 new tests, 477 total pass.\\n- **CCA/MergeGate:** owned downstream; my changes strengthen the CCA-08 Guardian gate.\\n\\n## Evidence\\n- `test_command` (loop TDD gate) exits **0**: `test_shiki_governance_evidence.sh` \\u2713, `test_shiki_workflow_lint.sh` (actionlint 1.7.12) \\u2713, `validate_shiki.py --target .` \\u2713, `unittest discover -s tests` (477 OK) \\u2713.\\n- Acceptance-check map: (1) negated marker `approved=False` + full mergegate exit 1 \\u2192 unittest + `mg-guardian-negated-marker`; (2) withheld/revoked \\u2192 unittest + guardian-policy cases; (3) existing fixtures still pass; (4/5) label transitions \\u2192 unittest + guardian-policy block; (6) soft blocker recorded \\u2192 `_comment_source` unittest; (7) types + read-only `job_permissions` (+ negative control: validator errors when removed); (8) actionlint passes; (9) `labeled` trigger; (10) `--paginate` + live refresh honour a mid-run approval; (11) see caveat.\\n- **Acceptance check 11 caveat:** `test_shiki_guardian_policy.sh` passes through **all** guardian evaluator + mergegate logic here and stops only at its final `shiki doctor` step \\u2014 the single failing check is `doctor.worktrees.unregistered` (sibling worktrees sharing this gitdir), exactly the environmental reason the task excludes it from `test_command`. It is unrelated to this change and unfixable within my locks; it passes end-to-end in a checkout with no unregistered sibling worktrees.\\n- **Locks:** all 6 touched paths are within the declared locks; nothing outside touched. Ledger (`L-\\u20265b9e8d6d`) is loop-owned and outside my locks.\\n\\n## Pre-PR code review (for the PR body / ledger)\\nAdversarial reviewer ran on the Guardian gate. **Confirmed clean:** read-only mergegate perms are sufficient (no step writes to GitHub); no `labeled`/`unlabeled` self-trigger loop (nothing mutates PR labels); a negation soft-blocker cannot poison a valid current-head approval (demoted to warning). **Fixed (both in-locks, serving the \\\"latest transition governs / revocation honoured\\\" goal):** latest-label resolution now orders by `created_at` not concat position; added `--paginate` so a page-2 revocation/approval isn't truncated; and the negation soft-blocker is now gated on marker-presence (removes audit noise from unrelated cue-word comments). **Accepted limitations (not fixed, with reason):** the negation cue list is a denylist \\u2014 a *configured Guardian* who starts a line with the exact approval phrase and then negates it with wording outside the cue set (e.g. \\\"\\u2026NOT yet\\\") is a bounded guardian footgun, not an external bypass; broadening the list risks false-negatives on legitimate approvals (e.g. \\\"no longer blocked\\\"). The line-initial rule is the primary defense and rejects all realistic negation phrasings.\\n\\n## Next action\\n- Loop: create the PR (with MergeGate body + the review summary above), run CI/CCA, record the `code-review` ledger entry, and gate merge \\u2014 this is a **critical-risk** change and requires Guardian approval before merge.\\n\",\n.shiki/runner/EXEC-20260731T091454943610Z-3bfbf29e.json:8:  \"stdout\": \"The loop's exact pre-PR TDD gate passes end to end. Here is my report.\\n\\n## Result\\n- Goal/task: G-20260729T065622761503Z-4479b0d3 / T-20260729T065622764296Z-48021407 \\u2014 \\\"Refuse a self-contradictory CCA verdict\\\"\\n- Role: Implementer (Claude Code, headless dispatch)\\n- Status: **ready** for the loop's gate (green, strictly in-locks) \\u2014 with **2 scope items blocked on lock scope** (reported below)\\n\\n## What changed or decided\\nCore hardening (all within declared locks, all delivered):\\n- **`scripts/enforce_cca_verdict.py`** \\u2014 new `failing_acceptance_criteria()`; `validate_verdict` now rejects a `complete` verdict when any acceptance entry is `fail`/`insufficient_evidence`, naming the criterion.\\n- **`scripts/mergegate_check.py`** \\u2014 new `failing_acceptance_criteria()` and `checklist_profile_coverage_failures()` (+ `TERMINAL_CHECKLIST_STATUSES = {pass, fail, not_applicable}`); CCA branch now: promotes **`can_merge != True` from a warning to a blocking reason**, rejects `complete` with a failing acceptance entry, and blocks unless every id in the task's `cca_checklist_profile` appears in the verdict checklist at a terminal status.\\n- **`.shiki/schemas/task.schema.json`** \\u2014 added `cca_checklist_profile` (property + `required`).\\n- **`scripts/validate_shiki.py`** \\u2014 `validate_task` present-validates `cca_checklist_profile` (non-empty strings when declared).\\n- **Tests** \\u2014 new `tests/test_cca_verdict_consistency.py` (12 tests incl. mutation guards); end-to-end mergegate cases in `test_shiki_governance_evidence.sh` (Groups B2/B3); enforce case in `test_shiki_control_plane.sh`.\\n\\n**Two scope items are provably unreachable within the declared locks** (empirically verified, evidence below), so I did not force them:\\n1. **`minItems:1` on `cca-verdict.schema.json`** \\u2014 breaks `test_shiki_evidence_integrity.sh` and `test_shiki_guardian_policy.sh` (out-of-lock): their `complete`-verdict fixtures use empty checklists and expect `ready`; the change makes mergegate emit `CCA verdict schema violation: $.checklist: array length must be >= 1`.\\n2. **`cca_checklist_profile` in `validate_shiki.TASK_REQUIRED`** \\u2014 `validate_shiki` validates *every* `.shiki/tasks/*.json`, so requiring the field forces backfilling ~98 task files; those files are outside my locks, and adding them to the PR would trip MergeGate's `files_outside_locks` gate. I added it to `task.schema.json` and present-validation instead.\\n\\n## Checklist status\\n- Skill Gate: **tdd** \\u2705 (tests-first; red\\u2192green verified by mutation-testing both complete-with-failures guards); **code-review** \\u2705 (self-review \\u2014 `code-review` skill unavailable in this session; diff reviewed for correctness/edge cases/regressions)\\n- PRD/issues: n/a (dispatched task)\\n- TDD: \\u2705 mutation guard confirmed \\u2014 neutralizing either guard fails \\u22651 test\\n- CCA: pending (GitHub CCA judges the PR)\\n- MergeGate: in-locks; pending PR\\n\\n## Evidence\\n- Checks (local): 650/650 unittest pass; all 34 `test_shiki_*.sh` pass; `validate_shiki` passes; the loop's exact `test_command` passes end to end.\\n- Mutation guard: reverting the checklist-fail guard \\u2192 1 failure; reverting the acceptance-fail guard \\u2192 1 failure; restored \\u2192 OK.\\n- Scope-conflict evidence: with `minItems:1` applied, `test_shiki_evidence_integrity.sh` + `test_shiki_guardian_policy.sh` FAIL (captured `array length must be >= 1`); reverted.\\n- Branch/PR: loop-owned (I made no git/gh calls); all my edits are inside the 8 declared locks.\\n\\n## Next action\\n- Loop: open the PR and let GitHub CCA/MergeGate judge.\\n- To finish scope items #1 (`minItems`) and #5 (required field + backfill), a follow-up needs an **expanded lock set** covering `.shiki/tasks/**`, `test_shiki_evidence_integrity.sh`, and `test_shiki_guardian_policy.sh` (recorded in memory `cca-verdict-hardening-lock-conflicts`). I did not manufacture any approval record or exceed the locks to force them.\\n\",\n.shiki/runner/EXEC-20260801T070800715591Z-67616385.json:8:  \"stdout\": \"All green. Implementation complete.\\n\\n## Result\\n- **Goal/task:** G-20260801T040836630398Z-f28167e5 / T-20260801T040836631930Z-97030be9\\n- **Role:** Implementer (Claude Code, TDD)\\n- **Status:** ready (implemented, verified; Shiki loop owns commit/PR/merge \\u2014 I did not commit, push, or open a PR per the execution protocol)\\n\\n## What changed\\nWired ADR 0015's missing \\\"Evaluation placement\\\" half \\u2014 Contract Approval as a distinct approval source that MergeGate accepts as an OR-alternative to a live PR Guardian approval.\\n\\n- **`scripts/shiki_contract_approval.py`** (new): pure `evaluate_contract_approval(*, task_id, base_task, head_task, changed_files_status, registration)` \\u2192 frozen `ContractApprovalResult(applies, sources, reasons)`. No I/O; the proof is passed in as data. `applies=True` only when all four conditions hold, evaluated in order, each failure a distinct reason \\u2014 no default-true branch. It imports the **single** `_NORMAL_PATH_GOVERNANCE_FIELDS` from `mergegate_check` so the carry's field list and the immutability gate's can never drift.\\n- **`scripts/mergegate_check.py`**: added `--contract-approval`, a loader, and `contract_approval_for_pr`; `enforce_guardian_policy` now short-circuits (satisfied, no live evidence loaded) when a valid carry is present, **before** the live-approval path \\u2014 which is left byte-for-byte unchanged. Wired **only** on the normal task path (contract-registration mode still requires live approval).\\n- **`scripts/guardian_approval_signal.py`**: the same OR reaches the deterministic CCA signal, reporting `contract_approval` in the existing `sources` array (without it, the CCA job fails first and the policy check never runs \\u2014 ADR 0015).\\n- `evaluate_guardian_approval` and its four sources are **untouched**. Both entry points import the evaluator **lazily** (`ImportError \\u2192 does not apply`) because `TEMPLATE_PATHS` (which stages scripts to targets) is outside my locks.\\n\\n## Checklist status\\n- **TDD:** `tests/test_contract_approval.py` (24 tests) drives the real gate \\u2014 the pure evaluator for all four conditions + every negative, and `enforce_guardian_policy` asserting the guardian requirement still resolves True in every negative (so a regression can't hide behind an unrelated block).\\n- **code-review:** `/code-review` is a slash command not invocable here; the Shiki loop runs the pre-PR gate at PR creation. Self-reviewed the full diff.\\n- **Approval records:** none authored \\u2014 Contract Approval is *evaluated* from a proof passed as data, never manufactured.\\n\\n## Evidence\\n- `python3 -m py_compile scripts/*.py` \\u2014 OK\\n- `python3 scripts/validate_shiki.py` \\u2014 passed\\n- `python3 -m unittest discover -s tests` \\u2014 **715 tests OK**\\n- All **34** `scripts/test_shiki_*.sh` contract tests \\u2014 **ALL PASS** (incl. the extended `test_shiki_guardian_policy.sh` end-to-end carry + removal cases driving both real entry points)\\n- Only the 5 declared-lock files changed; `shiki_guardian.py` untouched.\\n\\n## Next action\\n- Loop creates the PR (MergeGate body) and runs the `code-review` + CCA gates. This is a critical-risk task file, so its own implementation PR will need the existing live approval path (the contract-approval carry it delivers only takes effect for PRs registered after it lands).\\n\",\n.shiki/runner/EXEC-20260729T232445140625Z-d3889a6b.json:8:  \"stdout\": \"All work complete. Implementation and tests are done, and the changes stay strictly within the declared locks.\\n\\n## Result\\n- Goal/task: G-20260729T065622761503Z-4479b0d3 / T-20260729T065622766508Z-d36555d8\\n- Role: Implementer (Claude Code)\\n- Status: ready (implementation complete; the Shiki loop owns commit/push/PR/merge)\\n\\n## What changed\\n- **`scripts/shiki_git.py`** \\u2014 new `checkout_branch(target, branch)`: no-op when already on the branch, plain `git checkout` when the branch exists (never `-B`/reset), `git checkout -b` only to create, and raises `ShikiError` on failure. Never force-resets an existing ref.\\n- **`scripts/shiki_bootstrap.py`** \\u2014 `cmd_start` now calls `initialize_target_from_start` unconditionally on execute (a retry re-drives secret / branch-protection / workflow-permission setup instead of skipping them because `.shiki/repo.json` + origin already exist). `cmd_init` and `cmd_bootstrap_github` use `checkout_branch` instead of `git checkout -B`; the now-unused `current_branch` import was dropped.\\n- **`scripts/shiki_installer.py`** \\u2014 `.shiki/guardian-policy.json`, `.shiki/config.yaml`, `.shiki/migrations/state.json` are create-if-absent even under `--force` (preserved with a warning); other template files are still refreshed by `--force`.\\n- **`scripts/shiki_doctor.py`** \\u2014 branch-protection online check now distinguishes **403/permission \\u2192 warn** from **404/not-protected \\u2192 fail** (fails closed on any other non-zero). The worktree registry check only fails on unregistered worktrees **under the Shiki root** (`<main-checkout-parent>/.worktrees`); Claude-harness/coordinator session worktrees under `.claude/worktrees/` carry no Shiki task and are reported informationally.\\n\\n## Checklist status\\n- Skill Gate: `tdd` (red\\u2192green per unit), `code-review` (self-review of diff, scope, lint)\\n- TDD: `tests/test_bootstrap_safety.py` (new) + extended `test_shiki_doctor.sh`, `test_shiki_init.sh`, `test_shiki_start.sh`\\n- CCA/MergeGate: owned by the loop\\n\\n## Evidence (local, diagnostic)\\n- `validate_shiki.py` passed; `py_compile scripts/*.py` ok; `unittest discover -s tests` \\u2192 **465 OK** (incl. 6 new).\\n- Shell contract suite: all pass **except** `test_shiki_runtime_auth.sh` and `test_shiki_guardian_policy.sh` \\u2014 both call `doctor --json` on this live multi-worktree checkout under `set -e`. Verified this is **pre-existing, not my change**: the old worktree check flagged **8** worktrees here (fail), mine flags **1** genuine orphan (`/Users/kio.mizutani/.worktrees/shiki-pr178-secret-set-claude-repair`, unregistered under the Shiki root) and reclassifies the 7 harness worktrees as informational. In a clean CI clone (single worktree) both old and new pass, so these go green in CI.\\n- `shellcheck --strict` and `actionlint --strict` pass on the edited shell scripts.\\n\\n## Acceptance checks \\u2014 all met\\n- Protection fails run 1 \\u2192 run 2 re-attempts protection + workflow-permissions; token unset run 1 \\u2192 run 2 sets the secret; run 2 still non-zero when the cause is unfixed (`test_shiki_start.sh`).\\n- `shiki init` against an existing repo whose default branch holds a different commit leaves it unchanged, reflog has no reset (`test_shiki_init.sh` + `test_bootstrap_safety.py`).\\n- `install-target --force` preserves an operator's guardian policy/config/migration state and warns (`test_bootstrap_safety.py`).\\n- doctor not-protected \\u2192 fail+exit1, 403 \\u2192 warn; sibling non-Shiki worktree \\u2192 informational, genuine orphan \\u2192 fail (`test_shiki_doctor.sh`).\\n\\n## Next action\\n- Loop: create the PR (with the MergeGate body) and run CCA/MergeGate. Note for the reviewer: the local `runtime_auth`/`guardian_policy` reds are the exact \\\"doctor-dependent test red in every dispatch\\\" this task fixes \\u2014 they are environmental (live worktrees) and pass in a clean CI checkout.\\n\",\n.shiki/runner/EXEC-20260729T225326695727Z-35e34069.json:8:  \"stdout\": \"All green. The only \\\"unused\\\" flag is `from __future__ import annotations`, which is a required future-import (not dead). Implementation is complete.\\n\\n## Result\\n- **Goal/task:** G-20260729T065622761503Z-4479b0d3 / T-20260729T065622764822Z-bac7ef27\\n- **Role:** Implementer (Claude Code, default runtime)\\n- **Status:** ready (implemented + verified locally; the goal loop owns commit/push/PR/merge)\\n\\n## What changed\\nThe goal loop's `dispatch_repair` branch now **delivers** a completed repair to the PR head instead of dispatching the runner and delivering nothing \\u2014 removing the manual `git commit && git push` step (repro: RP-\\u202650b5c0d7 finished green but PR #181 never moved).\\n\\n`scripts/shiki_loop.py`\\n- `execute_action` `dispatch_repair` branch: after `_dispatch_repair`, calls `_commit_and_push_implementation` and fails closed to `stop_blocked` when the fix isn't delivered \\u2014 exactly as `create_pr` does.\\n- New `_pr_branch_head` helper + head-moved guard (from the reviewer's **F1**): the guard requires the origin branch tip to actually **advance**, not just that `git push` returned 0. A repair that fixes nothing pushes \\\"Everything up-to-date\\\" (rc 0) yet leaves the PR head unmoved \\u2192 now `stop_blocked` with a named reason instead of silently burning attempts to a vague `stop_guardian`.\\n- `_dispatch_repair`: repair packet carries the shared commit/push prohibition + a loop-owned push evidence line.\\n\\n`scripts/shiki_tasks.py`\\n- New `LOOP_OWNS_DELIVERY_PROHIBITION` constant, used by the task handoff (byte-identical replacement) and the repair packet, so the contract matches in both directions.\\n- `cmd_handoff_repair` now renders an `## Evidence Required` section.\\n\\n`tests/test_repair_delivery.py` (new, 6 tests) \\u2014 packet/handoff contract (#4), push-failure `stop_blocked` + no-op-repair `stop_blocked` (#3 + F1), full `dispatch_repair \\u2192 merge \\u2192 \\u2026 \\u2192 goal_complete` drive with head-dependent checks (#1/#2/#6), and the real `dispatch_runner_task` repair status transition (F2 coverage gap).\\n\\n## Checklist status\\n- **Skill Gate:** `tdd` (tests written + red/green verified), `code-review` (independent read-only reviewer run; both findings F1/F2 addressed)\\n- **TDD:** 6 new tests green; full suite 465 passed\\n- **CCA/MergeGate:** pending (loop-owned; PR not yet opened)\\n\\n## Evidence (local/diagnostic until CI records it)\\n- `python3 -m unittest discover -s tests` \\u2192 **465 passed**\\n- Locked shell tests: `test_shiki_p2_repair_limit`, `test_shiki_goal_loop`, `test_shiki_loop_e2e`, `test_shiki_loop_lock_guard` \\u2192 **all pass**\\n- `py_compile scripts/*.py` + `validate_shiki.py` \\u2192 OK\\n- Pre-existing unrelated failures `test_shiki_guardian_policy` / `test_shiki_runtime_auth` fail identically on a clean tree (environment, not in my locks)\\n- All edits within declared locks; the three shell test files were left unchanged (green, satisfying #5 without edits). Per the handoff protocol I did **not** run git/gh \\u2014 the loop owns delivery.\\n\\n## Next action\\nLoop opens the PR (with MergeGate body) \\u2192 CI/CCA record durable evidence \\u2192 MergeGate. Acceptance checks #1\\u2013#6 are all covered by the new tests.\\n\",\n.shiki/runner/EXEC-20260801T020336716380Z-daba78ac.json:8:  \"stdout\": \"Everything is complete and green. Here's the summary.\\n\\n## Result\\n- **Goal/task:** G-20260801T003238840135Z-14193adf / T-20260801T003238841526Z-9f3f6f63 \\u2014 bind a normal task-PR's own contract to the base snapshot (ADR 0015 Contract immutability, normal path)\\n- **Role:** Implementer\\n- **Status:** complete (worktree ready for the loop to deliver)\\n\\n## What changed or decided\\nMost of the implementation was already present in the worktree from a prior session and is correct; I verified it end-to-end and closed the one remaining gap.\\n\\n- **`scripts/mergegate_check.py`** (pre-existing, verified correct): `normal_task_contract_immutability_reasons()` freezes the seven governance fields (`scope, non_goals, required_skills, risk_level, locks, acceptance_checks, test_command`) to the base snapshot on the normal path, base-when-present resolution, fail-closed on a changed own-task-file with no base, first-registration passes. `resolved_guardian_risk_never_weaker()` resolves the Guardian risk as `max(base, head)` so a self-lowered `risk_level` cannot dissolve the gate. The mutable set reuses the single ADR 0017 constant `_CLOSEOUT_MUTABLE_TASK_FIELDS` (not re-declared); the frozen set is the explicit 7, not the complement (so loop-written `pre_pr_code_review`/`cca_rerun_count` still ride the impl-PR head). Call sites live only in the `elif resolved_task_id` normal-path branch \\u2014 mutually exclusive with reconcile/contract/post_merge \\u2014 and the guardian floor is guarded by `task is not None`, so no double-report.\\n- **`scripts/test_shiki_control_plane.sh`** (my fix \\u2014 this file is in my locks): line-1317 `assert_ready(\\\"current-task\\\", \\u2026)` mutated the own task file's `scope`, which the new rule now correctly **blocks** \\u2192 the suite broke under `set -e`. I repurposed `current_task_changed` to move a mutable field (`closeout_pr`; status left `review` so it isn't classed as a closeout) keeping `assert_ready`, and added `current_task_governance_changed` with `assert_blocked(\\u2026, \\\"frozen governance field 'scope'\\\")`. Avoided the `expected_pr`/`expected_branch` consistency-gate trap.\\n\\n## Checklist status\\n- **Skill Gate:** tdd (reproduced control_plane red \\u2192 green; new tests drive the real gate), code-review (self-reviewed branch exclusivity, guardian scoping, fail-closed paths, mutable-field traps)\\n- **TDD:** `tests/test_contract_immutability.py` (14 cases, all acceptance checks) + shell adversarial/governance cases assert the **guardian outcome**, not merely a block\\n- **CCA/MergeGate:** owned by the loop (not run locally)\\n\\n## Evidence\\n- `python3 -m unittest discover -s tests` \\u2192 **Ran 666, OK**\\n- All touched/affected `test_shiki_*.sh` (control_plane, adversarial_state, governance_evidence, evidence_integrity, guardian_policy, state_classes, validator_hardening, goal_loop, loop_e2e, p2_malicious_pr, shellcheck, workflow_lint, +others) \\u2192 **PASS**\\n- `py_compile` clean; `git diff --name-only` confirms only files within my declared locks were edited (the `.shiki/{goals,locks,tasks}` records were pre-staged by the loop and untouched by me)\\n\\n## Next action\\n- Hand back to the Shiki goal loop for commit/push/PR/MergeGate (I did not run any git/gh/PR commands, per the execution protocol).\\n\",\n.shiki/runner/EXEC-20260804T012259276337Z-1059cd39.json:8:  \"stdout\": \"All work is complete and verified. Here's my report.\\n\\n## Result\\n- Goal/task: G-20260804T002751220363Z-1eb2e0d4 / T-20260804T002751221801Z-a914603f\\n- Role: Implementer (Claude Code)\\n- Status: complete (implementation delivered; the loop owns commit/PR/merge)\\n\\n## What changed or decided\\n\\n**Ledger type `contract-amended`** registered in both bound places:\\n- `.shiki/schemas/ledger.schema.json` `type` enum\\n- `scripts/validate_shiki.py` `LEDGER_TYPES` (held set-equal at `validate_contract_schema_consistency`)\\n- `docs/agents/ledger-schema.md` reference list\\n\\n**Amendment mode** (`scripts/mergegate_check.py`):\\n- `AMENDMENT_MARKER` / `AMENDMENT_LABEL` + `amendment_decision` \\u2014 two-factor, same message shape as `contract_decision`.\\n- `enforce_amendment` (deny-by-default) with `_validate_amendment_source_plan` (append-only `spec_freeze.amendments`; head==base on every other key; strict-prefix + \\u22651 new entry; source_plan-only; spec-frozen-at-base) and `_validate_amendment_task` (bound to the **HEAD/amended** plan via `_frozen_task_match_errors`; MODIFY-only; status\\u2192planned/ready only; `expected_pr`/`closeout_pr`/`expected_branch`/`ledger_evidence` frozen to base); required goal-scoped `contract-amended` ledger.\\n- Guardian risk = **stronger of base and head** by reusing `contract_guardian_risk` on both roots (`base_shiki.parent` trick) + `_max_risk` \\u2014 no second resolution.\\n- Extracted the frozen-DAG rules into shared `_check_frozen_dag_in_diff` / `_check_frozen_dag_head_invariant` so contract/goal_reconcile and amendment apply *literally the same* DAG rules (pure code-motion).\\n- Wired into `main()`: decision, three-way mutual exclusion, goal-scoped id check, dispatch branch mirroring contract mode.\\n\\n## Checklist status\\n- Skill Gate: `tdd` (tests written to drive the gate, then implementation), `code-review` (manual pre-PR self-review of the full diff)\\n- TDD: `tests/test_contract_amendment.py` drives the real gate for every acceptance case (well-formed pass, marker/label two-factor, spec_freeze edit/remove/reorder/status/approved_by/prd/source, no-new-entry, foreign plan, not-frozen-at-base, foreign-goal task, absent title, plan mismatch, ADDed task, bad status, each frozen field, impl file, missing ledger, DAG rules, guardian-risk stronger-of-base/head, schema set-equality + entry validates)\\n- MergeGate: modes untouched \\u2014 verified\\n\\n## Evidence\\n- Full task `test_command`: `python3 -m unittest discover -s tests` (**841 pass**), `test_shiki_adversarial_state.sh`, `test_shiki_governance_evidence.sh`, `test_shiki_control_plane.sh`, `validate_shiki.py` \\u2014 **all pass**\\n- Unchanged-mode proof: `test_contract_mode`, `test_goal_reconcile`, `test_contract_immutability`, `test_bookkeeping_closeout`, `test_loop_lock_guard` green\\n- Shell adversarial cases: governance suite proves an amendment can't lower its own Guardian gate (weakened plan blocks at base risk; same amendment with approval is ready); adversarial-state suite proves marker-alone and label-alone both leave the normal-path operator-only `spec_freeze` prohibition firing\\n- Extra shell suites run green: shellcheck, workflow_lint, module_boundaries, evidence_integrity, memory_loop, guardian_policy, state_classes, p2_check_name_drift, p2_malicious_pr\\n- Scope: only the 7 locked files changed (the task JSON edit is the loop's, not mine)\\n\\n## Next action\\n- The Shiki goal loop commits, opens the MergeGate PR (with `<!-- shiki:amendment -->` + `mergegate:amendment` where applicable), and merges. No git/PR actions taken by me, per the execution protocol.\\n\",\n.shiki/tasks/T-20260731T031357587095Z-2cb11839.json:53:    \"code-review\"\n.shiki/runner/EXEC-20260805T082132026389Z-89e58619.json:8:  \"stdout\": \"Implementation complete. Here is the final report.\\n\\n## Result\\n- Goal/task: G-20260801T041653421243Z-2d98487f / T-20260801T041653422672Z-c4815d7a\\n- Role: Implementer (Claude Code, headless runner)\\n- Status: complete (implementation + tests green; loop owns delivery)\\n\\n## What changed or decided\\nAdded a two-factor **`migration` MergeGate mode** to `scripts/mergegate_check.py` \\u2014 a repository-wide mirror change delivered as a **re-run proof**: the PR's `.shiki` diff must equal the output of re-running a migration **registered on the base branch**.\\n\\n- `migration_decision(pr)` \\u2014 marker (`<!-- shiki:migration -->`) + `mergegate:migration` label, same message as `contract_decision` when the label is missing.\\n- `resolve_base_migration_registry` \\u2014 reads `scripts/shiki_migrations.py` from the **base branch** via `git show origin/<baseRefName>:\\u2026` and execs it (the security property, per the scope amendment: `subprocess` inside `mergegate_check.py`, no new workflow flag, no reliance on the `.shiki`-only base snapshot for `scripts/`).\\n- `enforce_migration` \\u2014 resolves + runs the base migration's `apply` against a copy of the base `.shiki`, then compares the diff change-set to the head byte-for-byte, with a distinct block per shape.\\n- Wired into `main()`: decision, mutual-exclusion with all 4 existing modes, no task/goal-id requirement, and a dispatch branch.\\n\\n**Non-obvious defect I had to solve:** `github_pr_body` inlines the task's `scope` **and** `acceptance_checks` verbatim, and one acceptance criterion contains a **bare** marker mid-sentence \\u2014 a substring/code-span match would have self-blocked this task's own implementation PR on the \\\"label missing\\\" error. I made the marker **line-anchored** (strict hardening; the loop-owned task JSON is out of locks so I couldn't reword the criterion) and added an end-to-end regression test that builds the real PR body from the stored task and asserts no self-declare/no-error.\\n\\n## Checklist status\\n- Skill Gate: tdd (red\\u2192green), code-review (self-review pass \\u2014 found & fixed the self-block + broadened fail-closed exception handling)\\n- TDD: `tests/test_migration_mode.py` (24 tests) drives the real gate for every mandated case incl. the self-defining-migration attack against a real git repo\\n- Adversarial: `scripts/test_shiki_adversarial_state.sh` \\u2014 self-defining-migration attempt + one-byte deviation, both against a real git repo (exercises base-registry resolution end-to-end)\\n\\n## Evidence\\n- Task `test_command` \\u2014 all pass: `python3 -m unittest discover -s tests` (1043 OK) && `test_shiki_adversarial_state.sh` && `test_shiki_governance_evidence.sh` && `validate_shiki.py --target .`\\n- `py_compile scripts/*.py` OK; `shellcheck`, `module_boundaries`, `control_plane`, `workflow_lint`, `migrations`, `malicious_pr`, `schema_drift` all pass\\n- Scope: only the 3 locked files changed (`mergegate_check.py`, `test_shiki_adversarial_state.sh`, new `tests/test_migration_mode.py`); the `.shiki/tasks|locks/\\u2026c4815d7a.json` changes are pre-existing loop-owned dispatch state, untouched by me\\n\\n## Next action\\n- Hand back to the Shiki goal loop for commit/push/PR/MergeGate/CCA/merge. I opened no PR and ran no git/gh mutations, per the execution protocol.\\n\",\n.shiki/runner/EXEC-20260801T011406339517Z-51871199.json:8:  \"stdout\": \"Confirmed: **\\\"Validate Shiki mirror\\\" is a MergeGate-required check**, and it runs `test_shiki_control_plane.sh`. So the out-of-lock failure is a hard MergeGate blocker, not a soft one.\\n\\nMy implementation is complete and correct within the declared locks, and the task's own `test_command` is fully green. But there is a genuine, unavoidable conflict with a test outside my locks that CI treats as required. Here is my report.\\n\\n## Result\\n- **Goal/task:** G-20260801T003238840135Z-14193adf / T-20260801T003238841526Z-9f3f6f63 \\u2014 \\\"Bind a task PR's own contract to the base snapshot on the normal path\\\"\\n- **Role:** Implementer (Claude Code, in-worktree; loop owns delivery)\\n- **Status:** blocked \\u2014 implementation complete within locks; a **required CI check fails on a test outside my declared locks** that asserts the exact pre-fix behavior this task inverts.\\n\\n## What changed or decided\\nImplemented ADR 0015 Contract immutability on the **normal task-PR path** in `scripts/mergegate_check.py`:\\n\\n- **`normal_task_contract_immutability_reasons(...)`** \\u2014 a new function called only from `main()`'s normal-path branch (`elif resolved_task_id:`). When a PR adds/modifies its own `.shiki/tasks/<id>.json`, it freezes the seven governance fields (`scope, non_goals, required_skills, risk_level, locks, acceptance_checks, test_command`) to the base snapshot. Resolution is **base-when-present**: base has the task \\u2192 base is authority; base present without it \\u2192 first registration passes; **no base at all \\u2192 fail closed**. `risk_level` additionally can never resolve weaker than base.\\n- **`resolved_guardian_risk_never_weaker(...)`** + a guardian-block edit \\u2014 resolves the Guardian risk as `max(base, head)`, so a self-lowered `risk_level` still forces the Guardian gate (the acceptance requires guardian-survival, not merely a block).\\n\\nThree deliberate design decisions, each forced by evidence:\\n1. **Separate `main()`-scoped function, NOT inside `enforce_untrusted_shiki_mutations`** \\u2014 because `test_shiki_state_classes.sh:165` and `test_shiki_p2_malicious_pr.sh` call that function *directly* with `base_shiki=None` + own-task-file-M and assert no block. Both are outside my locks; both still pass.\\n2. **Explicit-7 governance fields, NOT \\\"complement of the mutable set\\\"** \\u2014 I verified (`_sync_state_to_branch`) that `pre_pr_code_review`/`cca_rerun_count` land on a real impl-PR head but not base, so a complement check would block legitimate PRs. The mutable set reuses the single `_CLOSEOUT_MUTABLE_TASK_FIELDS` constant (disjointness bound by a consistency test).\\n3. Guardian risk resolved from base so the requirement survives a self-lowering.\\n\\n## Checklist status\\n- **Skill Gate:** tdd \\u2705 (tests written first, drive the real gate); code-review \\u2014 the loop's independent pre-PR gate runs on delivery\\n- **PRD/issues:** n/a (task-scoped)\\n- **TDD:** \\u2705 `tests/test_contract_immutability.py` (14 cases) + guardian-survival cases added to both in-lock shell tests\\n- **CCA:** pending (loop/CI)\\n- **MergeGate:** **BLOCKED** \\u2014 required check \\\"Validate Shiki mirror\\\" fails (see below)\\n\\n## Evidence\\n- **Local (diagnostic):**\\n  - `python3 -m unittest discover -s tests` \\u2192 **666 tests OK**\\n  - `bash scripts/test_shiki_adversarial_state.sh` \\u2192 OK (now includes guardian-survives-self-lowered-risk + control)\\n  - `bash scripts/test_shiki_governance_evidence.sh` \\u2192 OK (same)\\n  - `python3 scripts/validate_shiki.py --target .` \\u2192 passed\\n  - **Task `test_command`: fully green.**\\n  - Full shell suite: **every `test_shiki_*.sh` passes except `test_shiki_control_plane.sh`.**\\n- **Diff scope:** only the 4 locked files (`mergegate_check.py`, `test_contract_immutability.py`, `test_shiki_adversarial_state.sh`, `test_shiki_governance_evidence.sh`). No file outside locks touched.\\n- **Branch:** `shiki/t-20260801t003238841526z-\\u2026-bind-a-t` (loop owns commit/PR)\\n\\n## Blocker (needs an operator/orchestrator decision \\u2014 I cannot resolve it within my locks)\\n\\n`scripts/test_shiki_control_plane.sh:1317` asserts:\\n```python\\ndef current_task_changed(case, base):\\n    task[\\\"scope\\\"] = str(task.get(\\\"scope\\\", \\\"\\\")) + \\\" fixture\\\"   # mutates a GOVERNANCE field\\n...\\nassert_ready(\\\"current-task\\\", current_task_changed, [f\\\".shiki/tasks/{task_id}.json\\\"], [f\\\"M\\\\t...\\\"])\\n```\\nIt drives `main()` with a base snapshot present and a **`scope` change on the own task file, expecting READY** \\u2014 i.e., it encodes the exact behavior this task exists to invert. My rule correctly blocks it (`PR must not change frozen governance field 'scope' \\u2026`).\\n\\n- `test_shiki_control_plane.sh` is **not in this task's locks**, and CI's **required** \\\"Validate Shiki mirror\\\" check runs every `test_shiki_*.sh` under `set -e` (`.github/workflows/shiki-validate.yml:37`). So the check fails and MergeGate blocks.\\n- I cannot fix it: editing it violates the lock scope (and would trip `files_outside_locks`); not editing it fails the required check. Both paths block MergeGate \\u2014 this needs a lock/scope decision, not implementer discretion.\\n\\n**Recommended bounded fix (one line):** add `scripts/test_shiki_control_plane.sh` to the task's locks and change `current_task_changed` to mutate a **mutable** field instead of `scope`, e.g.:\\n```python\\ntask[\\\"status\\\"] = \\\"review\\\"        # was: task[\\\"scope\\\"] = str(task.get(\\\"scope\\\",\\\"\\\")) + \\\" fixture\\\"\\n```\\nThat preserves the test's intent (\\\"a PR may touch its own task file, unlike a foreign one\\\") under the new rule. Alternatively, keep the `scope` mutation and switch it to `assert_blocked(..., \\\"frozen governance field 'scope'\\\")`.\\n\\nSeparately note: this task is `risk_level: critical`, so its own PR will require **Guardian approval** to merge regardless of the above.\\n\\n## Next action\\nOperator/orchestrator decision required: **expand this task's locks to include `scripts/test_shiki_control_plane.sh`** (or hand that one-line fixture fix to a bounded repair), then re-run MergeGate. The implementation and all in-lock tests are complete and green; only the out-of-lock fixture stands between this and a passing \\\"Validate Shiki mirror\\\" check.\\n\",\n.shiki/tasks/T-20260801T041653274543Z-6df3af7e.json:73:    \"code-review\"\n.shiki/runner/EXEC-20260805T041114410308Z-31271d9c.json:8:  \"stdout\": \"Done. `tests/test_paired_invariants.py` is the sole change, and it lands green everywhere.\\n\\n## Result\\n- Goal/task: G-20260805T025939374293Z-96ec6ced / T-20260805T025939375698Z-94a2fa82\\n- Role: Implementer\\n- Status: ready (implementation complete + locally verified; loop owns commit/PR/merge)\\n\\n## What changed or decided\\n- Added `tests/test_paired_invariants.py` (840 lines): 14 `Pair*` classes, one binder per agreement, each with a `*_holds` test (live invariant) and a `*_divergence_*` test (feeds a mutated copy of one side through the same comparison to prove the binder fails when broken \\u2014 31 tests total).\\n- Relation per pair matches each pair's own contract and states it in every message:\\n  - **Set-equality**: 3 (mirror dirs vs manifest record classes), 5-code (schema/enforcer/loop verdicts), 6 (risk vocab \\u00d77 sites), 7 (code-review verdicts \\u00d74 sites), 9 (memory status/area), 10 (workflow files \\u00d73), 13 (guardian users/label), 14 (smoke/start required).\\n  - **Containment**: 1 (schema keywords \\u2286 implemented), 2 (check literals \\u2286 job names), 4 (each PR-body producer \\u2287 required headings), 5-prose/workflow (enumerations \\u2286 canonical), 8 (default checks \\u2286 job names), 12 (referenced ids \\u2286 defined). **Subset**: 11 (`required_skill` enum \\u2286 `KNOWN_SKILLS`).\\n- One deliberate deviation from the handoff, resolved to keep it green: pair 3's floor lists `ledger`, but the manifest classifies `.shiki/ledger` as `append-only-evidence`, not `mirror`. Asserting against `state_class: mirror` alone would be red today, so the manifest side is the per-record record classes `{mirror, append-only-evidence}` \\u2014 the invariant then holds and the docstring/message name why. Bound values are read from source (no hardcoded line numbers), so the tests track drift rather than snapshots.\\n\\n## Checklist status\\n- Skill Gate: tdd (test-first invariant module + divergence proofs), code-review (self-review: removed unused `shiki_evidence` import, confirmed all containment pairs non-vacuous, verified mutation sensitivity)\\n- TDD: 14 divergence tests prove breaking any agreement fails the suite\\n- CCA/MergeGate: loop-owned\\n\\n## Evidence\\n- `python3 -m unittest discover -s tests`: **Ran 999 tests \\u2026 OK**\\n- `python3 scripts/validate_shiki.py --target .`: **Shiki validation passed**\\n- `scripts/test_shiki_init.sh`: **install-and-verify passed: fresh target passes its own required checks** \\u2014 proves the module is target-valid and runs green inside a freshly installed target with an empty `.shiki` mirror\\n- Change set: `?? tests/test_paired_invariants.py` only; no `scripts/*.py` added, `validate_shiki.py` unchanged, nothing outside `path:tests/test_paired_invariants.py` (the `.shiki/tasks|locks/T-\\u2026` entries are loop-owned and predate this work)\\n\\n## Next action\\n- Loop: commit, open the MergeGate PR, run CCA/MergeGate. I did not run git/gh/PR commands per the execution protocol.\\n\",\n.shiki/runner/EXEC-20260801T010505285593Z-4a621f98.json:8:  \"stdout\": \"All tasks complete. Here's my final report.\\n\\n## Result\\n- **Goal/task:** G-20260729T065622761503Z-4479b0d3 / T-20260729T065622767723Z-f138eccf \\u2014 \\\"Make the shipped contracts true, and validate the mirror against them\\\"\\n- **Role:** Implementer (Claude Code, default runtime)\\n- **Status:** ready \\u2014 implementation complete, all local evidence green; loop owns commit/PR/CCA/MergeGate\\n\\n## What changed\\nClosed a silent schema/validator drift: `validate_shiki` accepted control ids in either form (dual `ID_SUFFIX`), but the shipped schemas still pinned the legacy-only `^X-[0-9]{4,}$`, and **nothing ran real records through those schemas** \\u2014 only synthetic fixtures. Measured drift: **809 of 1007** real records failed their declared schema, purely on the id pattern.\\n\\n1. **13 schemas \\u2192 dual pattern** (`.shiki/schemas/`: dag, dispatch-guard, goal, inbox, ledger, plan, report, run, runner-record, smoke, start, task, worktree-record): `-[0-9]{4,}$` \\u2192 `-([0-9]{4,}|[0-9]{8}T[0-9]{12}Z-[0-9a-f]{8})$`. 32 surgical pattern-line replacements, no formatting churn.\\n2. **Template** (`.shiki/templates/worktree-record.schema.json`): gave its `task_id`/`goal_id` the dual pattern (the one schema-shaped file under the templates lock).\\n3. **Plan dependency parser** (`scripts/shiki_tasks.py`): added a shared `_TASK_ID_RE` dual alternation; replaced both legacy-only `re.match` sites (`orchestrate_plan`, `register_contract_from_plan`) so a raw collision-resistant dependency id resolves.\\n4. **Conformance loop** (`scripts/validate_shiki.py`): `MIRROR_RECORD_SCHEMAS` + `validate_mirror_schema_conformance()` runs every record in the 9 documented dirs through its declared schema \\u2014 placed as a **backstop after** the manual validators (see below).\\n5. **Ledger pairing check** (`validate_ledger`): rejects an entry whose `task_id` belongs to a different goal than its `goal_id`, using the `task_goal_by_id` map main() already builds.\\n6. **New test** `tests/test_mirror_schema_validation.py` (9 tests, one per acceptance check + guards).\\n\\n**Key ordering finding:** the loop must run *after* the manual validators. Placed early it shadowed the exact `id must match L-0001\\u2026` message that `test_shiki_control_plane.sh` (out of my locks) asserts \\u2014 main() returns on the first error. As a backstop it still satisfies acceptance #2 because the manual validators are schema-independent, so a legacy-only revert passes them and reaches the loop (verified end-to-end).\\n\\n**\\\"The two templates\\\":** scope prose predates a migration \\u2014 the historical two (`cca-verdict`/`repair-packet` schema copies under `templates/`, per `shiki_contracts.OBSOLETE_*_SCHEMA_PATH`) were long since moved to `.shiki/schemas/` (repair-packet already dual; cca-verdict intentionally has no id pattern). Only one schema-shaped template remains; I gave it the dual pattern. Nothing reads it \\u2014 drift-reduction only.\\n\\n## Checklist status\\n- **Skill Gate:** `tdd` \\u2014 RED-first, all tests failed before the fix, pass after. `code-review` \\u2014 automated `/code-review` is operator-triggered/billed and cannot be launched from a headless session; I ran a manual adversarial review (correctness, regressions, scope, non-goals, missing tests) \\u2014 no findings.\\n- **Non-goals:** respected \\u2014 id generator untouched (`new_control_id`/`scan_ids`), no id migration, no new schema, scorecard untouched.\\n- **Locks:** every edit is within the 8 declared locks. The `M .shiki/tasks/\\u2026f138eccf.json` and `?? .shiki/locks/\\u2026f138eccf.json` are loop-owned (already present at session start), not mine.\\n\\n## Evidence\\n- **Exact `test_command`** all green: `test_shiki_manifest.sh` \\u2713, `test_shiki_evidence_integrity.sh` \\u2713, `test_shiki_p2_schema_drift.sh` \\u2713, `validate_shiki --target .` \\u2713, `unittest discover` \\u2713 (**661 tests**).\\n- Full CI shell set (33 `scripts/test_shiki_*.sh`) all pass; `py_compile scripts/*.py` clean.\\n- Acceptance #1: 809\\u21920 record failures. #2 verified end-to-end (revert task.schema \\u2192 `does not match pattern '^T-[0-9]{4,}$'`). #3/#4/#5 covered by passing tests; 0 pre-existing ledger pairing violations.\\n- **Branch:** `shiki/t-20260729t065622767723z-f138eccf-make-the-shipped-contracts-true-and-validate-the`\\n\\n## Next action\\nLoop owns delivery \\u2014 no commit/PR/CCA created by me (per protocol). The goal loop should commit, open the PR with the MergeGate body, and route to CCA/MergeGate. Manual `/code-review` (operator-triggered) can run against the branch before merge if desired.\\n\",\n.shiki/runner/EXEC-20260730T111405020369Z-962c3898.json:8:  \"stdout\": \"All changes are complete and every local gate is green. Here's the summary.\\n\\n## Result\\n- Goal/task: G-20260730T110603196220Z-c50d61b5 / T-20260730T110603197694Z-c903d404\\n- Role: Implementer (Claude Code)\\n- Status: ready (implementation done, local evidence green; the Shiki loop owns commit/PR/CCA/MergeGate)\\n\\n## What changed or decided\\nTwo behaviors, both TDD-driven (red \\u2192 green):\\n\\n**(a) `.github/workflows/shiki-cca-completion.yml` \\u2014 un-shallow the base fetch.** Dropped `--depth=1` from both `git fetch origin \\\"$base_ref\\\"` invocations (now lines 54 and 470). Both jobs still check out with `fetch-depth: 0`, so full history \\u2014 and the merge base the following `git diff \\\"origin/${base_ref}...HEAD\\\"` needs \\u2014 is preserved instead of being shallowed back down.\\n\\n**(b) `scripts/enforce_cca_verdict.py` \\u2014 treat the head SHA as workflow-supplied.** New `inject_authoritative_head_sha(verdict)` reads `SHIKI_HEAD_SHA`; when set and non-empty it overwrites `verdict[\\\"head_sha\\\"]` *before* validation and *before* the file is written. A differing model value is preserved under `head_sha_reported` with a stderr warning naming both; a matching value is left untouched with no extra key; unset/empty/whitespace leaves the verdict byte-identical to today. Wired into `main()` between the dict check and `validate_verdict`. The Enforce step now passes `SHIKI_HEAD_SHA: ${{ github.event.pull_request.head.sha || github.sha }}` \\u2014 the same expression the manifest builder uses for `--head-sha`.\\n\\n**Tests:** new `tests/test_cca_head_sha_injection.py` \\u2014 9 tests covering replace-on-mismatch (+preservation +warning), unchanged-on-match, unset/empty/whitespace no-op, downstream-validation-and-written-file see the injected value, plus the two workflow-shape assertions (no `--depth=1` on either base fetch; both checkouts keep `fetch-depth: 0`; Enforce step passes `SHIKI_HEAD_SHA`).\\n\\n## Checklist status\\n- Skill Gate: `tdd` (red confirmed 3 fail/5 error \\u2192 green), `code-review` self-review done\\n- PRD/issues: pre-existing (task handoff)\\n- TDD: green \\u2014 9/9 new tests pass\\n- CCA: not run (owned by GitHub CCA, not the implementer)\\n- MergeGate: pending (owned by the loop)\\n\\n## Evidence\\n- `python3 -m unittest discover -s tests`: **537 tests, OK** (exit 0)\\n- New file: `python3 -m unittest discover -s tests -p test_cca_head_sha_injection.py`: **9 tests, OK**\\n- `python3 -m py_compile scripts/*.py`: OK\\n- `bash scripts/test_shiki_workflow_lint.sh --strict`: actionlint passed (file unmodified)\\n- `bash scripts/test_shiki_shellcheck.sh`: passed\\n- `python3 scripts/validate_shiki.py`: passed\\n- `git status --short`: only the three locked paths changed (`.github/workflows/shiki-cca-completion.yml`, `scripts/enforce_cca_verdict.py`, new `tests/test_cca_head_sha_injection.py`) \\u2014 no file outside locks touched\\n- Branch: shiki/t-20260730t110603197694z-c903d404-fetch-the-base-ref-in-full-and-inject-the-author (no commit/push/PR performed \\u2014 per the execution protocol, the loop owns delivery)\\n\\n## Next action\\n- Hand back to the Shiki goal loop for commit, push, PR creation, CCA judgment, and MergeGate. No approval records were authored.\\n\",\n.shiki/runner/EXEC-20260729T100929257188Z-a8060f76.json:8:  \"stdout\": \"Done. All acceptance checks satisfied, TDD red\\u2192green followed, code-review self-pass complete, and the change is confined to the three locked paths.\\n\\n## Result\\n- Goal/task: G-20260729T092307426018Z-584fb95e / T-20260729T092307427377Z-c8531f7e\\n- Role: Implementer\\n- Status: ready (loop owns commit/PR/merge)\\n\\n## What changed or decided\\n- `.shiki/schemas/cca-evidence-manifest.schema.json`: removed the single `\\\"minLength\\\": 1` from `properties.verdict.properties.task_id`; `task_id` stays in the verdict `required` array (presence is still a contract). This aligns the wrapper with the wrapped `cca-verdict.schema.json`, which already declares `task_id` as `{ \\\"type\\\": \\\"string\\\" }`.\\n- `tests/test_cca_evidence_manifest_taskless.py` (new): the four binding cases + a schema-level case that an empty `verdict.task_id` validates + two divergence guards (task_id required-without-minLength; wrapper-parity with the verdict schema so the two schemas can't silently diverge again).\\n- `scripts/test_shiki_adversarial_state.sh`: added a launder case \\u2014 the same empty-`task_id` manifest is accepted task-less (`expected_task_id=None`) but rejected the moment the PR resolves to a real task id, proving the identity binding in `shiki_evidence.py`, not a schema floor, is what stops laundering.\\n\\n## Checklist status\\n- Skill Gate: `tdd` (red\\u2192green: 3 schema-level tests failed with the exact production symptom `$.verdict.task_id: expected string length >= 1`, then passed after the one-line edit; 4 binding cases green throughout), `code-review` (self-review vs. acceptance checks + scope)\\n- TDD: red captured, green confirmed\\n- MergeGate: not evaluated locally (loop owns); no PR opened\\n\\n## Evidence\\n- New module: `python3 -m unittest discover -s tests -p test_cca_evidence_manifest_taskless.py` \\u2192 Ran 7, **OK**\\n- Full suite: `python3 -m unittest discover -s tests` \\u2192 Ran 439, **OK**\\n- `scripts/test_shiki_adversarial_state.sh` \\u2192 passed; `scripts/test_shiki_evidence_integrity.sh`, `test_shiki_governance_evidence.sh`, `test_shiki_p2_schema_drift.sh`, `test_shiki_control_plane.sh`, `test_shiki_shellcheck.sh` \\u2192 passed\\n- `git status --short`: only the three locked paths changed\\n- Note: `scripts/test_shiki_guardian_policy.sh` exits 1 on its final `shiki doctor` step (`doctor.worktrees.unregistered` + missing `.shiki/repo.json`) \\u2014 I verified this is **identical with the `minLength` restored**, i.e. pre-existing environment state, not a regression from this change.\\n\\n## Next action\\n- Loop: commit \\u2192 push \\u2192 open Contract PR with MergeGate body \\u2192 merge per governance.\\n\\nI did not run `git commit/push/checkout` or any `gh`/PR command \\u2014 the Shiki loop owns delivery.\\n\",\n.shiki/tasks/T-20260804T095105535495Z-22a51036.json:62:    \"code-review\"\n.shiki/tasks/T-20260729T065622767723Z-f138eccf.json:65:    \"code-review\"\n.shiki/runner/EXEC-20260805T014956463451Z-035c4ddb.json:8:  \"stdout\": \"All work is complete and verified. Here is the summary.\\n\\n## Result\\n- Goal/task: G-20260801T041653272590Z-81f6f71d / T-20260801T041653274543Z-6df3af7e\\n- Role: Implementer (Claude Code)\\n- Status: ready (implementation complete; the Shiki loop owns commit/PR/merge)\\n\\n## What changed or decided\\n- **`scripts/cca_verdict_usable.py`** (new) \\u2014 pure `verdict_usability(structured_output) -> (classification, reason)` returning `usable | absent | unparseable | degenerate`. `degenerate` is a present-but-empty `checklist` or `acceptance` (mirroring `enforce_cca_verdict.DEGENERATE_JUDGMENT_ARRAYS`); every real verdict value \\u2014 `complete` and every negative value alike \\u2014 is `usable`, so no real answer is ever regenerated (judge-shopping is impossible by accident). A thin `__main__` CLI reads `STRUCTURED_OUTPUT`/stdin and prints `{\\\"classification\\\",\\\"reason\\\"}`.\\n- **`.github/workflows/shiki-cca-completion.yml`** \\u2014 inserted `Classify CCA output` (id `classify`) between `Run CCA` and `Enforce CCA verdict`, plus `Run CCA (retry)` (id `cca_retry`) guarded by `if: steps.classify.outputs.classification != 'usable'` (identical prompt/schema/allowed tools, no event gate \\u2192 reachable from both triggers). `Enforce CCA verdict` now selects the retry output only when the first was unusable (`classification == 'usable' && cca || cca_retry`). A still-unusable retry fails the job through the existing enforce checks; the retry decision + first reason are echoed to the job log. Artifact names, evidence manifest, and Guardian blockers step are untouched.\\n- **`scripts/test_shiki_control_plane.sh`** \\u2014 asserts the classify step sits between Run CCA and Enforce, the single-retry bound (exactly 2 `claude-code-action@v1` uses; exactly 1 guarded), and that retry is limited to the unusable classes.\\n- **`tests/test_cca_verdict_usable.py`** (new) \\u2014 covers absent/whitespace/None, unparseable (non-JSON, truncated, array, scalar), degenerate (empty checklist, empty acceptance, both, PR #233 shape), well-formed `complete` and one case per negative verdict value, purity, the CLI, and full workflow wiring.\\n- **`scripts/shiki_installer.py`** \\u2014 added `scripts/cca_verdict_usable.py` to `TEMPLATE_PATHS` so the workflow's dependency ships (required by `test_install_completeness`).\\n\\n## Checklist status\\n- Skill Gate: `tdd` (red \\u2192 green: classifier + tests written first, workflow-wiring tests drove the YAML change), `code-review` (self-reviewed diff confined to locks)\\n- TDD: classifier tests green before wiring; 27/27 in the new file\\n- MergeGate: implementation-only; loop owns delivery\\n\\n## Evidence\\n- `python3 -m unittest discover -s tests` \\u2192 **Ran 967 tests \\u2026 OK**\\n- `bash scripts/test_shiki_control_plane.sh` \\u2192 passed; `python3 scripts/validate_shiki.py` \\u2192 passed\\n- `bash scripts/test_shiki_shellcheck.sh` \\u2192 passed; `py_compile` on new/changed scripts \\u2192 OK\\n- `git diff --name-only` confined to locks: workflow, installer, control-plane test, classifier, test file (the `.shiki/tasks` + `.shiki/locks` entries are loop-owned bookkeeping, pre-existing at session start \\u2014 not my edits)\\n\\n## Next action\\n- Hand back to the Shiki loop for commit, PR (with MergeGate body), CCA, and merge.\\n\",\n.shiki/tasks/T-20260801T041653423742Z-d5dc4a28.json:39:    \"code-review\"\n.shiki/runner/EXEC-20260807T024348787078Z-dd5046cf.json:8:  \"stdout\": \"## Result\\n- Goal/task: G-20260807T020527150479Z-4f8ffc4c / **T-20260807T020527152153Z-97fe21d1**\\n- Role: Implementer (Claude Code, default runtime)\\n- Status: **ready** (implementation complete + verified; the Shiki loop owns delivery)\\n\\n## What changed\\nA re-opened task no longer reads a previous cycle's PR pointer as this cycle's completed work. In `scripts/shiki_loop.py`:\\n\\n1. **`decide_task_action` (`ready` branch)** \\u2014 when a `ready` task carries a **merged** `expected_pr`, it decides `dispatch` with a new `clear_expected_pr: True` flag instead of letting the stale pointer ride into `review` \\u2192 `create_closeout_pr`. Detected from the task record alone (status + the pointer's merge state) \\u2014 no amendment marker, ledger scan, or PR timestamp. The `planned`/`blocked` and `review` branches are unchanged.\\n2. **`_clear_stale_expected_pr` (new helper)** \\u2014 idempotently nulls `expected_pr` (schema allows `null`).\\n3. **`_execute_action_body`** \\u2014 honors `clear_expected_pr` before the effector runs, so the fresh implementation's `create_pr` opens a new PR; persisted by the existing single coordinator-mirror commit.\\n4. **`goal_loop_step`** \\u2014 now snapshots the PR for a `ready` task **that carries an `expected_pr`** (a re-opened task), so the pure engine can see the merged bit. Fresh `ready` tasks (no pointer) make no extra call.\\n\\n**Why this way (root cause):** the closeout branch pushes only `expected_pr` to main (`shiki_loop.py:1630`), never `closeout_pr`; a Spec Amendment re-opens the task but freezes both fields to base (`mergegate_check.py:951-956`). So a re-opened task has `expected_pr=309` (merged), `closeout_pr` unset \\u2014 exactly the measured `create_closeout_pr` shape. Failure direction is asymmetric: clearing a still-live pointer at worst re-opens a PR-less cycle the loop already handles; reading a stale one silently ships nothing.\\n\\n## Checklist status\\n- Skill Gate: **tdd** (red\\u2192green\\u2192refactor, done); **code-review** \\u2014 the loop's independent pre-PR `_run_pre_pr_code_review` gate runs at delivery; I also self-reviewed the diff adversarially (transient-`gh`-error, `blocked` status, multi-task priority, schema, amendment-freeze consistency).\\n- TDD: red confirmed (the `clear_expected_pr` flag + effector clearing were the genuinely new behavior), then green.\\n- Tests: `tests/test_reopened_task.py` (new, 7 cases) + 3 cases in `test_loop_engine.py` + `ReopenedTaskDoesNotCloseoutTests` in `test_closeout.py` (drives the measured sequence end-to-end).\\n- CCA/MergeGate: not yet run (delivery-time; loop-owned).\\n\\n## Evidence\\n- `python3 -m unittest discover -s tests` \\u2192 **OK (1114 tests)**\\n- `scripts/test_shiki_goal_loop.sh` \\u2192 **passed**\\n- `scripts/test_shiki_adversarial_state.sh` \\u2192 **passed**\\n- `python3 scripts/validate_shiki.py` \\u2192 **Shiki validation passed**\\n- Diff is purely additive (73/0 in `shiki_loop.py`; `test_closeout.py` has **no removed lines** \\u2192 no closeout assertion weakened). `mergegate_check.py`, `shiki_evidence.py`, `guardian_approval_signal.py` **unchanged**. Only in-locks files touched (`scripts/shiki_loop.py`, `tests/test_loop_engine.py`, `tests/test_closeout.py`, `tests/test_reopened_task.py`).\\n\\n## Acceptance checks \\u2014 all satisfied\\n- \\u2705 ready + merged `expected_pr` \\u2192 dispatch (fresh impl), not `create_closeout_pr`\\n- \\u2705 planned + merged `expected_pr` \\u2192 wait_dependencies, not `create_closeout_pr`\\n- \\u2705 review + merged `expected_pr` \\u2192 `create_closeout_pr` (unchanged)\\n- \\u2705 review + `closeout_pr` + merged `expected_pr` \\u2192 `mark_done` (unchanged)\\n- \\u2705 unmerged `expected_pr` on a non-terminal task \\u2192 untouched (no clear)\\n- \\u2705 detected from the task record alone\\n- \\u2705 measured re-open sequence asserted (does not reach `create_closeout_pr` on cycle 1; next cycle cuts a fresh `create_pr`)\\n- \\u2705 no closeout assertion weakened; prohibited files unchanged; all four verification commands pass; nothing outside locks changed\\n\\n## Next action\\nHand back to the Shiki goal loop for delivery (commit/push/PR + MergeGate/CCA). No PR or git/gh actions taken here, per the execution protocol. The `.shiki/tasks/\\u2026` and `.shiki/locks/\\u2026` modifications shown in `git status` are loop-owned dispatch state \\u2014 I did not touch them.\\n\",\n.shiki/tasks/T-20260729T065622769632Z-9ee6b8e0.json:56:    \"code-review\"\n.shiki/runner/EXEC-20260805T005125162637Z-82551df4.json:8:  \"stdout\": \"The killed background job (exit 144) was the redundant triple-run I intentionally stopped with `kill`/`pkill` \\u2014 the clean single run already reported **940 tests OK**. The production diff is exactly as intended.\\n\\nAll work is complete and verified. Per the execution protocol, the Shiki loop owns commit/push/PR/merge \\u2014 I've only edited files in the worktree and run diagnostics.\\n\\n## Result\\n- Goal/task: G-20260801T041653272590Z-81f6f71d / T-20260801T041653274008Z-54d3477c\\n- Role: Implementer (Claude Code default, ADR 0008)\\n- Status: complete (implementation + local verification; MergeGate/CCA evidence is the loop's to gather)\\n\\n## What changed or decided\\nMade \\\"a verdict that judged nothing\\\" unrepresentable at every layer:\\n- **`.shiki/schemas/cca-verdict.schema.json`** \\u2014 `minItems: 1` on `checklist` and `acceptance`.\\n- **`.github/workflows/shiki-cca-completion.yml`** \\u2014 same `minItems: 1` on the inline transport schema, and the checklist/acceptance **item shapes now mirror** the repository schema (`required` fields, types, `status` enum, `evidence` array of non-empty strings) so the model can't generate the six shape violations seen in production.\\n- **`scripts/enforce_cca_verdict.py`** \\u2014 new `degenerate_judgment_arrays` helper + an explicit check in `validate_verdict` that runs **before** schema validation and rejects an empty `checklist`/`acceptance` with a message naming the array. It keys only on *present-but-empty* lists, so a *missing* array still yields the schema's \\\"missing required property\\\" (preserved the enforce test at line 346).\\n- **Fixtures (7 sites)** made non-degenerate while preserving each property: evidence_integrity (2), guardian_policy (2 \\u2014 including the un-listed line-694 site for consistency), control_plane (3 named + the amendment's in-place `cca[\\\"checklist\\\"] = []` at ~952 that was poisoning later expected-ready calls). The repair_required fixture kept a *failing* item so it still reaches the `non-null object` assertion; the intentional empty-acceptance rejection test (control_plane:985) was left untouched.\\n- **`tests/test_cca_verdict_nondegenerate.py`** \\u2014 21 tests covering enforce + schema directly + transport-schema agreement + all preserved enforcement (blocking failures, short-circuit, head-SHA injection).\\n\\nBlast radius matched the handoff exactly: the two out-of-lock unittest empty-checklist fixtures (`test_cca_evidence_manifest_taskless`, `test_guardian_carry_parity`) and `test_shiki_governance_evidence.sh`'s two expected-block fixtures don't route the empty verdict through minItems-enforcing assertions, so they stay green \\u2014 no out-of-lock edits were needed.\\n\\n## Checklist status\\n- Skill Gate: `tdd` (test written first, drives real modules/schemas), `code-review` (self-reviewed diff below)\\n- PRD/issues: n/a (task-scoped)\\n- TDD: new suite green (21/21)\\n- CCA: loop-owned (not run locally)\\n- MergeGate: loop-owned\\n\\n## Evidence\\n- Checks (local, diagnostic): `validate_shiki` \\u2705; `test_shiki_evidence_integrity.sh` \\u2705; `test_shiki_guardian_policy.sh` \\u2705; `test_shiki_control_plane.sh` \\u2705; `test_shiki_governance_evidence.sh` \\u2705; `python3 -m unittest discover -s tests` \\u2192 **Ran 940 tests, OK** (exit 0).\\n- Review/CCA: pending (loop dispatches CCA on the PR).\\n- Ledger: loop-owned; I recorded no approval records.\\n- Branch/PR: worktree edits only \\u2014 6 locked files modified + 1 new (`tests/test_cca_verdict_nondegenerate.py`); `git status` shows nothing outside the locks (the `.shiki/tasks|locks/T-\\u2026` entries were loop-owned and pre-existing at session start).\\n\\n## Next action\\n- The Shiki goal loop commits, opens the MergeGate PR, and dispatches CCA/MergeGate. No further implementation is needed.\\n\",\n.shiki/runner/EXEC-20260618T111023666090Z-e933639f.json:8:  \"stdout\": \"Cross-file tracer is clean: `shiki_runtime.py` only checks the handoff file exists and passes its **path** to the runner adapter (the whole file becomes the agent prompt) \\u2014 it never parses sections by position. The other `## Scope` references in the codebase build *different* documents (PR bodies in `shiki_github.py`/`shiki_loop.py`, issue bodies in `shiki_bootstrap.py`), none of which parse the task handoff. The full 294-test suite passed, so no golden/snapshot body assertion broke.\\n\\nThe diff is append-only static markdown plus one regression test \\u2014 no logic, no removed behavior, no language pitfalls, no new reuse opportunities (the guardrail text is new). Review verdict:\\n\\n```json\\n[]\\n```\\n\\nNo findings. The `code-review` gate is green.\\n\\n## Result\\n- Goal/task: G-20260618T110721108316Z-295dc805 / T-20260618T110731105287Z-5276602e\\n- Role: Implementer (Claude Code, default runner)\\n- Status: complete (implementation + verification done locally; Shiki loop owns commit/PR/merge)\\n\\n## What changed or decided\\n- `scripts/shiki_tasks.py` \\u2014 `write_task_handoff` now emits a `## Execution Protocol (the Shiki loop owns delivery)` section **before** `## Scope`. It instructs the implementer to: edit only worktree files; NOT run `git commit`/`git push`/`git checkout`/`git switch` or any `gh` command; NOT create/update/comment-on/merge a pull request (the loop owns commit/push/PR/merge); and stay within declared Locks.\\n- `tests/test_handoff_consult.py` \\u2014 added `test_execution_protocol_precedes_scope_and_forbids_delivery` asserting the section is present, precedes `## Scope`, names `git commit`/`git push`/`` `gh` ``/`pull request`, and is phrased as a prohibition (`Do NOT`).\\n\\n## Checklist status\\n- Skill Gate: `tdd` (red\\u2192green\\u2192reviewed), `code-review` (run, 0 findings) \\u2713\\n- PRD/issues: N/A (task-scoped handoff per provided contract)\\n- TDD: RED confirmed (section absent), GREEN confirmed after implementation\\n- CCA: pending (Shiki loop / GitHub CCA)\\n- MergeGate: pending (loop owns PR + merge)\\n\\n## Evidence\\n- Tests: `python3 -m unittest discover -s tests` \\u2192 **Ran 294 tests, OK**; new test file `python3 -m unittest discover -s tests -p test_handoff_consult.py` \\u2192 **Ran 7 tests, OK**\\n- Review/CCA: code-review gate \\u2192 `[]` (no findings)\\n- Branch: `shiki/t-20260618t110731105287z-5276602e-handoff-execution-protocol-guardrail` (working tree only \\u2014 no commit/push made, per Execution Protocol)\\n- Diff: `scripts/shiki_tasks.py` (+11), `tests/test_handoff_consult.py` (+21)\\n\\n## Next action\\n- Hand back to the Shiki goal loop for commit, push, PR creation (with MergeGate body), CCA judgment, and merge. I have not run any git/gh/PR commands, staying within the declared locks (`scripts/shiki_tasks.py`, `tests/test_handoff_consult.py`, `.shiki/**`).\\n\",\n.shiki/tasks/T-20260729T065622770500Z-513839f0.json:58:    \"code-review\"\n.shiki/tasks/T-20260804T082618611335Z-02de0043.json:70:    \"code-review\"\n.shiki/runner/EXEC-20260806T105420629827Z-a9918d0b.json:8:  \"stdout\": \"All coherent. The remaining `fast-forward` references are in unrelated functions (`_sync_branch`, `_reconcile_registered_worktree_to_origin`); the covering-point region and function name are consistent. Change scope is exactly my three locked files plus the loop's own pre-existing `.shiki` bookkeeping.\\n\\nThe implementation is complete and verified. Here is the summary.\\n\\n## Result\\n- **Goal/task:** G-20260806T065143407702Z-8e3284ce / T-20260806T065143409817Z-dbdae1cf \\u2014 \\\"Stage every coordinator mirror write, not only the three sync paths\\\" (amended: **commit**, not stage)\\n- **Role:** Implementer\\n- **Status:** ready (for the loop to deliver \\u2014 commit/push/PR/merge are loop-owned)\\n\\n## What changed or decided\\nThe previous implementation **staged** the coordinator's `.shiki` mirror at the single covering point in `execute_action`'s `finally`. Measurement showed staging clears only a **fast-forward** `git merge origin/main`; the coordinator always carries its own commits, so its merge is always **non-fast-forward**, where a merely-staged returning file still aborts (*\\\"Your local changes \\u2026 would be overwritten by merge\\\"*). Fix in `scripts/shiki_loop.py`:\\n\\n- `_stage_coordinator_mirror` \\u2192 **`_commit_coordinator_mirror`**: `git add --ignore-removal -- .shiki`, then a **plain index commit** (`git commit --no-verify`, only when the add staged something) \\u2014 never `git commit -- .shiki` (which re-reads the tree and would record a deletion, breaking the append-only mirror).\\n- Kept every prior property: ONE covering point every effector passes through; scoped to `.shiki`; `--ignore-removal` so no deletion is ever staged.\\n- **Removed** the prior contract's unmeasured non-goal (\\\"the local branch must not advance\\\") \\u2014 the coordinator branch is disposable and never pushed/merged, so advancing HEAD costs nothing. That non-goal was exactly what made staging unable to satisfy its own criterion.\\n- A genuinely-differing file yields a normal `CONFLICT (add/add)` for the operator \\u2014 documented in the docstring, **not** auto-resolved (no merge driver/strategy).\\n- Updated two stale comments that still said \\\"fast-forward\\\"/\\\"staging only\\\".\\n\\n## Checklist status\\n- **Skill Gate:** `tdd` (tests changed to assert committing \\u2192 RED against staging code \\u2192 GREEN after the change), `code-review` (independent adversarial review \\u2014 no blocking findings; two non-blocking items both addressed)\\n- **TDD:** every merge assertion rebuilt to be **divergent (non-FF)** \\u2014 the old tests passed vacuously fast-forward. Added `test_covering_point_never_deletes_a_mirror_record` (tracked record survives) and `test_genuinely_differing_file_conflicts_normally_not_a_refusal`; made the second merge assertion in `CoordinatorStagingAfterSyncTests` divergent too.\\n- **CCA/MergeGate:** owned by the loop on delivery.\\n\\n## Evidence\\n- `python3 -m unittest discover -s tests` \\u2192 **1091 OK**\\n- `bash scripts/test_shiki_goal_loop.sh` \\u2192 passed (its real multi-cycle `loop run` now commits per effector)\\n- `python3 scripts/validate_shiki.py --target .` \\u2192 passed (exit 0)\\n- Independent reviewer verdict: nothing blocking; confirmed tests non-vacuous (real tracked record for the no-delete test; every merge asserted divergent before it runs)\\n- **Files changed (all in locks):** `scripts/shiki_loop.py`, `tests/test_closeout.py`, `tests/test_dispatch_state_sync.py` (the `.shiki/{goals,locks,tasks}` entries in `git status` are the loop's own pre-existing bookkeeping, present at session start, untouched by me)\\n\\n## Next action\\n- The Shiki loop owns commit \\u2192 push \\u2192 PR (with the MergeGate body) \\u2192 merge. Note for CCA/operator: this design **advances the coordinator HEAD** by construction and, on a genuine divergence, surfaces a normal `add/add` conflict for manual resolution \\u2014 both are intended and documented in `_commit_coordinator_mirror`.\\n\",\n.shiki/tasks/T-20260618T062324783813Z-f62d375b.json:60:    \"code-review\"\n.shiki/runner/EXEC-20260804T130124804591Z-75f09747.json:8:  \"stdout\": \"Task complete. All changes are within the 8 declared locks, both adversarial-review findings are fixed with mutation-verified regression tests, and the full task test command is green.\\n\\n## Result\\n- **Goal/task:** G-20260804T095105533205Z-224ce02d / T-20260804T095105534980Z-530c4f17 \\u2014 \\\"Accept a carried Guardian approval in the gate and the signal\\\"\\n- **Role:** Implementer (Claude Code)\\n- **Status:** ready \\u2014 implementation complete and verified; the goal loop owns commit/push/PR/merge (I made no git/gh mutations, per the execution protocol)\\n\\n## What changed\\nIntroduced the `guardian_comment_carried` source: when a stale Guardian *comment* approval binds to a prior head that `verify_pure_base_sync` proves is a pure base sync of the current head, the approval is carried instead of demanding a fresh one.\\n\\n- **`scripts/shiki_guardian.py`** \\u2014 carry logic inside `_comment_source` (same order: marker \\u2192 not-negating \\u2192 configured actor \\u2192 author \\u2192 *then* SHA extraction), a `_resolve_carry` step (newest-candidate-only; refuses on risk\\u2260high / label escalation / retargeted base / `base_ref_changed` / edited / later-negation-**hard-blocker** / failed proof), joined to the human-secondary set so the label leg still gates. `_comment_source` keeps its 5-tuple return (carry results flow through a mutable holder). All new evaluator params keyword-only with defaults.\\n- **`mergegate_check.py`** \\u2014 flags threaded through `enforce_guardian_policy` into **both** the contract-mode and normal-path call sites.\\n- **`guardian_approval_signal.py`** / **`shiki_guardian_status.py`** / **`shiki_cli.py`** \\u2014 flags applied via the same evaluator; both resolve the carry risk with the gate's **never-weaker-vs-base floor** and emit/apply `carried_from_head`/`carry_refused_reasons`.\\n- New `tests/test_guardian_carry.py` (21) and `tests/test_guardian_carry_parity.py` (9) + CLI carry section in `test_shiki_guardian_policy.sh`.\\n\\n## Review findings (adversarial subagent) \\u2014 both repaired\\n1. **Signal/status more permissive than the gate** (base=critical/head=high; empty-diff base sync dodges the immutability gate) \\u2192 fixed by applying `resolved_guardian_risk_never_weaker` in the signal (via `--base-shiki`) and status (git-read of `origin/<default-branch>`); added the base\\u2260head parity fixture. Mutation-verified: reverting the fix fails the test.\\n2. **Marker-less Guardian revocation ignored** \\u2192 negation tracking now runs before the approval-marker gate (carry-only). Mutation-verified.\\n\\nNo attacker-exploitable hole was found; the label leg, configured-actor gating, and the git tree-reproduction proof all hold.\\n\\n## Checklist status\\n- **Skill Gate:** tdd (red\\u2192green + mutation checks), code-review (skill is user-triggered/uninvokable; ran a rigorous adversarial subagent review and acted on findings)\\n- **TDD:** 30 new tests, both fixes covered by load-bearing regression tests\\n- **CCA/MergeGate:** owned by the loop/CCA on the PR\\n\\n## Evidence\\n- `python3 -m unittest discover -s tests` \\u2192 **912 OK**; `test_shiki_guardian_policy.sh`, `test_shiki_governance_evidence.sh`, `test_shiki_adversarial_state.sh`, `validate_shiki --target .` \\u2192 **all PASS**; plus module-boundaries, shellcheck, control_plane, state_classes, p2_malicious_pr, code_review_gate.\\n- `git status`: only the 6 locked source files modified + the 2 new locked test files; no out-of-lock file touched.\\n\\n## Next action\\n- The goal loop creates the PR (with MergeGate body) and runs the gates. **Note:** the carry is dormant in CI until a follow-up passes `--base-sync-carry`/`--default-branch` in the workflow YAML \\u2014 those files were outside this task's locks, so I correctly did not touch them. This is a CRITICAL-risk PR, so merge will require Guardian approval.\\n\",\n.shiki/tasks/T-20260805T025939375698Z-94a2fa82.json:54:    \"code-review\"\n.shiki/runner/EXEC-20260731T054845221557Z-ea810775.json:8:  \"stdout\": \"The implementation is complete and coherent. All work is done in-worktree; I have not committed, pushed, or opened a PR (the Shiki loop owns delivery).\\n\\n## Result\\n- Goal/task: G-20260729T065622761503Z-4479b0d3 / T-20260729T065622767228Z-3b4c41c2\\n- Role: Implementer (Claude Code, default)\\n- Status: ready (implementation complete, all acceptance checks verified locally; awaiting loop-owned commit/PR + CI/CCA)\\n\\n## What changed\\n- **`scripts/shiki_migrations.py`**\\n  - `_baseline_apply`: dropped the platform-only `.shiki/goals/G-0012.json` from the baseline's `required` list (and its `\\\"G-0012 exists.\\\"` evidence line), keeping the four layout entries (`manifest.json`, `tasks`, `goals`, `ledger`). Migrations now apply in an installed target, which never ships that platform goal.\\n  - `MigrationError` now subclasses `ShikiError` (was `Exception`), so `shiki_cli.main`'s handler routes migration failures through the `[shiki] error:` exit contract instead of a raw traceback.\\n- **`scripts/test_shiki_migrations.sh`**: deleted the `add_baseline_goal` helper and both call sites, so the suite exercises the real target path.\\n- **`tests/test_migrations_target.py`** (new, in locks): unittest that drives the real CLI over a freshly installed target \\u2014 the TDD deliverable covering all acceptance checks.\\n\\n## Checklist status\\n- Skill Gate: `tdd` (red\\u2192green: wrote failing target-path test first, then fixed), `code-review` (self-review \\u2014 the `/code-review` skill is user-triggered/model-invocation-disabled, so I reviewed manually against the Review Standard)\\n- TDD: 4/5 tests red before the fix (raw traceback + G-0012 prereq), 5/5 green after\\n- Regression review: only external raiser of `MigrationError` is `validate_shiki.py:640`, which catches `except Exception` (unchanged); `shiki_loop.py`'s `except ShikiError` blocks call no migration functions \\u2014 no swallowing regression\\n\\n## Evidence (local, diagnostic \\u2014 CI is durable)\\n- Acceptance checks, all verified via real CLI:\\n  1. install-target \\u2192 remove state \\u2192 `migrate plan` exit 0, baseline pending \\u2705\\n  2. `migrate apply --execute` exit 0, valid state written, `migrate status` pending_count 0 \\u2705\\n  3. Invalid state (`version:999`) \\u2192 `[shiki] error: \\u2026 version must be 1` on stderr, exit 1, no `Traceback` \\u2705\\n  4. No `add_baseline_goal` anywhere; `test_shiki_migrations.sh` green \\u2705\\n  5. `doctor` on repaired target: `doctor.migrations.state` = pass, `doctor.migrations.pending` = pass \\u2705\\n- Python suite: `Ran 589 tests \\u2026 OK`\\n- Shell contract tests: `test_shiki_migrations.sh`, `test_shiki_doctor.sh`, `test_shiki_module_boundaries.sh`, `test_shiki_state_classes.sh` all exit 0; `shellcheck` clean on the edited script\\n- Branch: `shiki/t-20260729t065622767228z-3b4c41c2-\\u2026` (no commit/PR by me, per protocol)\\n\\n## Next action\\n- Shiki loop: commit, push, open the MergeGate PR, run CI + CCA. `scripts/test_shiki_doctor.sh` was in the locks but needed no change \\u2014 acceptance check 5 is covered by the new Python test.\\n\",\n.shiki/tasks/T-20260729T065622763684Z-ee01f3c4.json:65:    \"code-review\"\n.shiki/runner/EXEC-20260806T023545844675Z-5b88a238.json:8:  \"stdout\": \"Implementation complete. Here is the summary.\\n\\n## Result\\n- Goal/task: `G-20260806T014016270514Z-2d62ec7c` / `T-20260806T014016272306Z-a0cec6ca`\\n- Role: Implementer (Claude Code, default runtime)\\n- Status: **ready** \\u2014 all acceptance checks pass in the worktree; the goal loop owns commit/push/PR/merge\\n\\n## What changed\\nAll three parts of the one property \\u2014 *a gate that refuses a judgment leaves a readable record of why, a transient refusal reaches the bounded re-run, and a refused judgment never becomes an authority*:\\n\\n**A \\u2014 `scripts/enforce_cca_verdict.py`.** A rejection now writes a refusal record (`verdict_as_received`, `rule_violated`, `offending_ids`, `transient`) via `_write_refusal_record` to `refusal_record_path(CCA_VERDICT_FILE)` = `<stem>.refusal.json` \\u2014 derived, never `cca-verdict.json`. `cca-verdict.json` is written only for a validated verdict. `validate_verdict` is untouched (all five grep strings survive byte-identical); a separate `analyze_refusal` mirrors its rule order to recompute ids + transient. Still exits non-zero.\\n\\n**B \\u2014 `scripts/shiki_loop.py`.** Transient = an item left `insufficient_evidence` because evidence wasn't yet available at judge time, classified by reason TEXT (`_reason_is_transient`, no new status/schema). `_resolve_cca_verdict` reads the refusal record and returns the `CCA_VERDICT_TRANSIENT_REFUSAL` sentinel or `CCA_VERDICT_UNRESOLVED` \\u2014 never the refused value. `decide_task_action` converts transient \\u2192 bounded `rerun_cca` (under `MAX_CCA_RERUNS`), else today's terminal stop; the refused value never reaches `_cca_verdict_stop`.\\n\\n**C \\u2014 `scripts/shiki_loop.py`.** A blocking pre-PR review naming zero findings is degenerate: record raw output durably (`_record_reviewer_raw_output`), retry once bounded by durable `pre_pr_review_empty_retries` (reset on (re-)dispatch via `_reset_pre_pr_review_retries`) through the new `wait_review` action; never treated as clean.\\n\\n## Checklist status\\n- TDD: added tests across all three locked test files (25 / 34 / 26 in the three suites)\\n- Skill Gate: `tdd`, `code-review` (loop-owned pre-PR gate) satisfied\\n\\n## Evidence\\n- `python3 -m unittest discover -s tests` \\u2192 **1080 OK**\\n- `test_shiki_control_plane.sh`, `test_shiki_goal_loop.sh`, `test_shiki_adversarial_state.sh`, `validate_shiki` \\u2192 all pass\\n- `.github/workflows/shiki-cca-completion.yml` and `.shiki/schemas/` \\u2192 **byte-identical** (empty diff)\\n- Changed files = exactly the 5 locks; no stray artifacts leaked into the repo\\n\\n## Next action\\n- Hand back to the Shiki loop to create the PR (with the MergeGate body) and run CCA/MergeGate. I did not commit, push, or open a PR, per the handoff protocol.\\n\",\n.shiki/runner/EXEC-20260807T014130245388Z-bedf843d.json:8:  \"stdout\": \"Implementation is complete and verified. Handing back to the Shiki loop for delivery (I have not committed, pushed, or opened a PR \\u2014 those are loop-owned).\\n\\n## Result\\n- Goal/task: G-20260806T084356214001Z-8198f059 / T-20260806T084356215508Z-7a7dd694\\n- Role: Implementer (Claude Code, ADR 0008)\\n- Status: **complete** (implementation + tests green; awaiting loop delivery \\u2192 CCA \\u2192 MergeGate)\\n\\n## What changed\\n- **`scripts/shiki_tasks.py`** \\u2014 added `cmd_lock_release` (the deliberate counterpart to `cmd_lock_acquire`) + `FINISHED_TASK_STATUSES`. It is the *authority* for a release: decides \\\"finished\\\" from the task record (terminal status `done` only \\u2014 I discovered \\\"retired/abandoned/superseded\\\" aren't real statuses; the motivating \\\"retired\\\" task is actually `planned`), refuses an unfinished task naming the task and its status, treats an already-released lock as a no-op success, refuses a missing/unreadable record naming the path, and **marks** the record (`state\\u2192released` + `released_by`/`released_at` alongside the acquisition fields) rather than deleting it. Record shape + JSON result contract mirror acquire.\\n- **`scripts/shiki_cli.py`** \\u2014 imported `cmd_lock_release` and wired the `release` child into the `lock` subparser (mirrors `acquire`: `--target`, `--owner`, `task_id`).\\n- **`tests/test_lock_release.py`** \\u2014 11 unit tests (TDD): finished-release records the releaser; result contract mirrors acquire; released paths acquirable by a different task; unfinished refused (all 6 non-terminal statuses); missing/unreadable refused naming path; already-released no-op (fresh + re-run); dispatch and status-promotion never release; and a drift-guard binding `FINISHED_TASK_STATUSES` to `validate_shiki.TERMINAL_TASK_STATUSES`.\\n- **`scripts/test_shiki_control_plane.sh`** \\u2014 self-contained end-to-end block proving the property unit tests can't: a DIFFERENT task acquires the SAME paths after release (blocked-before, granted-after), plus a validation-pass-never-releases assertion. Cleans up all temp task/lock/ledger files before the closing `validate_shiki`.\\n\\n## Checklist status\\n- Skill Gate: `tdd` (red\\u2192green\\u2192refactor), `code-review` (self-review of diff \\u2014 mirrors acquire, no scope drift)\\n- TDD: new tests failed for the right reason, then passed\\n- CCA/MergeGate: loop-owned (not run by implementer)\\n\\n## Evidence\\n- `python3 -m unittest discover -s tests` \\u2192 **1102 tests, OK**\\n- `scripts/test_shiki_control_plane.sh` \\u2192 **passed** (exit 0)\\n- `scripts/test_shiki_goal_loop.sh` \\u2192 **passed** (exit 0)\\n- `python3 scripts/validate_shiki.py` \\u2192 **Shiki validation passed**\\n- `py_compile` on both edited scripts \\u2192 OK\\n- Scope: only the 4 locked files changed; `scripts/shiki_loop.py` unchanged; no real lock record touched (the `M .shiki/tasks/\\u20267a7dd694.json` / `?? .shiki/locks/\\u20267a7dd694.json` are loop-owned state present at session start)\\n\\n## Next action\\n- Shiki goal loop owns commit/push/PR (with MergeGate body)/merge. No implementer action remains.\\n\",\n.shiki/tasks/T-20260729T072437602381Z-cd8cdfd8.json:56:    \"code-review\"\n.shiki/runner/EXEC-20260729T082519915835Z-d507a744.json:8:  \"stdout\": \"All work is complete and verified. Let me give the final report.\\n\\n## Result\\n- **Goal/task:** G-20260729T072437600805Z-2502768a / T-20260729T072437602381Z-cd8cdfd8 \\u2014 let a Contract PR register its own source plan\\n- **Role:** Implementer (default, Claude Code)\\n- **Status:** ready (implementation complete; all in-locks work green; loop owns commit/PR/merge)\\n\\n## What changed or decided\\n\\n**(a) `scripts/mergegate_check.py` \\u2014 contract mode permits carrying the source plan.** New `_validate_contract_source_plan` admits `.shiki/plans/<goal.source_plan>.json` when the PR **ADDs** it (status A), it is the goal's `source_plan`, its filename matches its `id`, and `spec_freeze.status == frozen`. A MODIFY (\\u2192 \\\"may only ADD\\\"), DELETE (\\u2192 existing \\\"must not delete\\\"), a non-source plan, and an unfrozen plan all stay blocked \\u2014 so a PR can never author/alter a `spec_freeze` block on any other plan. This resolves both halves of the deadlock: once carried, the plan is on HEAD so `_frozen_plan_tasks` no longer says \\\"source_plan not found,\\\" and the deny-by-default `else` no longer says \\\"must not change.\\\"\\n\\n**Key design decision \\u2014 the carry is an explicit opt-in (`allow_source_plan_carry`).** I proved empirically that permitting the carry unconditionally flips the pre-existing `test_contract_mode.py::test_forbidden_authority_surface_is_rejected` (which calls `enforce_contract` directly and asserts a plan ADD is rejected) \\u2014 and that file is **outside my declared locks** (editing it would itself trip MergeGate's files-outside-locks). Production (metadata job) and that test hit the identical code path with identical inputs, so no content discriminator is honest. Making the carry a param that defaults **off** and is enabled only by `main()`'s contract branch keeps `enforce_contract`'s frozen-registration validator deny-by-default (that test stays green, unedited) while the real contract flow permits the carry \\u2014 satisfying the scope (\\\"contract mode permits\\\") without touching an out-of-locks file.\\n\\n**(b) `.github/workflows/shiki-mergegate.yml` \\u2014 metadata job gets live Guardian evidence.** \\\"Prepare PR context\\\" now fetches `issues/<pr>/comments|events|timeline` into the three `live-guardian-*.json` files (mirroring `shiki-cca-completion.yml`, with `|| echo \\\"[]\\\"` fail-closed), and the metadata check passes `--guardian-policy` + the three `--guardian-*` flags. A high-risk contract PR now blocks on the **approval decision**, not on a missing input file. Per scope, the base-snapshot / `--merged-prs` argv is left for R-06.\\n\\n**Plan carried:** `.shiki/plans/P-20260726T053204320783Z-2265e5c3.json` written byte-identically (`git hash-object` = `6abee991\\u2026`, matching PR #182's `source_plan`) via the normal single-task path (`.shiki/plans/` is state-class `mirror`, not forbidden). After this merges, PR #182's synced branch resolves its plan on HEAD and passes.\\n\\n## Checklist status\\n- **Skill Gate:** tdd (red\\u2192green: proved the naive permit broke the locked-out test, designed the opt-in, 11 new tests green) and code-review (adversarial diff pass \\u2014 verified branch ordering, opt-in reachability, normal-PR guardian path untouched, tight binding to goal's frozen plan)\\n- **PRD/issues:** n/a (dispatched task)\\n- **TDD:** `tests/test_contract_plan_registration.py` \\u2014 11 tests (unit validator + 2 end-to-end subprocess through `main()`); fixtures built in temp dirs only (never touch real `.shiki` mirror)\\n- **CCA:** deferred to loop\\n- **MergeGate:** all files within declared locks; will be judged by CI\\n\\n## Evidence\\n- **Checks (local, diagnostic):** `validate_shiki` \\u2714; full `unittest discover -s tests` = **443 pass, 0 fail** (incl. the previously-green `test_forbidden\\u2026`, `test_contract_mode`, `test_contract_open`); `test_shiki_control_plane.sh`, `test_shiki_adversarial_state.sh`, `test_shiki_workflow_lint.sh --strict` all green; actionlint clean; p2 MergeGate-scope tests green\\n- **Files:** only the 4 in-locks files changed (`mergegate_check.py`, `shiki-mergegate.yml`, new `test_contract_plan_registration.py`, carried plan)\\n- **Branch/PR:** loop-owned (I ran no git/gh, authored no ledger entries)\\n\\n## Next action\\n- Loop: commit, open the MergeGate PR body, merge. Durable CI evidence will be produced then.\\n- **Heads-up (not a blocker for this PR):** (1) `test_shiki_guardian_policy.sh` fails *locally* \\u2014 its final `shiki doctor` hits `doctor.worktrees.unregistered` from sibling worktrees sharing the gitdir. I proved this fails identically with **pristine** (unmodified) code, so it's the documented A6 environment residue (green on a clean CI clone), not a regression from this task. (2) MergeGate now *accepts* a carried plan; making `shiki contract open` actually *produce* one for future Goals is a separate follow-up (CLI is outside these locks). PR #182 doesn't need it \\u2014 its plan is now bootstrapped onto main by this PR.\\n\",\n.shiki/tasks/T-20260729T065622764822Z-bac7ef27.json:66:    \"code-review\"\n.shiki/tasks/T-20260801T085710529159Z-575d4080.json:64:    \"code-review\"\n.shiki/tasks/T-20260729T065622768738Z-fa978e4a.json:55:    \"code-review\"\n.shiki/tasks/T-20260804T095105534980Z-530c4f17.json:83:    \"code-review\"\n.shiki/tasks/T-20260612T152357707056Z-90925f53.json:46:    \"code-review\"\n.shiki/tasks/T-20260731T040711237895Z-21826083.json:48:    \"code-review\"\n.shiki/tasks/T-20260801T003238841526Z-9f3f6f63.json:71:    \"code-review\"\n.shiki/tasks/T-20260612T045948146790Z-a27bcfa2.json:54:    \"No KNOWN_SKILLS or code-review skill changes (G-B)\",\n.shiki/tasks/T-20260806T034026771634Z-52142739.json:50:    \"code-review\"\n.shiki/tasks/T-20260801T040948352279Z-07301e34.json:74:    \"code-review\"\n.shiki/tasks/T-20260804T095105534398Z-c2ef0cfc.json:65:    \"code-review\"\n.shiki/tasks/T-20260803T013839641506Z-d11bb45f.json:63:    \"code-review\"\n.shiki/tasks/T-20260806T014016272306Z-a0cec6ca.json:12:    \"A code-review verdict of blocking with zero findings is retried once, bounded, and is never treated as clean.\",\n.shiki/tasks/T-20260806T014016272306Z-a0cec6ca.json:61:    \"code-review\"\n.shiki/tasks/T-20260806T014016272306Z-a0cec6ca.json:64:  \"scope\": \"Three parts, one property: a gate that refuses a judgment must leave a readable record of why, and a refusal whose cause is transient must reach the bounded re-run the loop already has instead of ending the run. Nothing about which judgments are valid changes, and a refused judgment must never become an authority.\\n\\nA. RECORD THE REFUSAL, ON A DIFFERENT PATH. `scripts/enforce_cca_verdict.py` `main()` returns `fail(...)` from its `except` BEFORE `output_path.write_text(...)`, so a refused verdict leaves nothing. Write a refusal record \\u2014 the verdict as received, the rule violated, and the offending checklist/acceptance ids \\u2014 and write it to a path DERIVED from `CCA_VERDICT_FILE` (the control-plane suite sets that env var to a temp path; do not hard-code `.shiki/gha/`). It must NOT be `cca-verdict.json`: that path is what every consumer reads as the CCA's operative authority, and the loop's resolver re-derives none of the rejection rules, so a refused `repair_required` written there would become an auto-dispatched repair. `cca-verdict.json` keeps its meaning exactly \\u2014 written only for a verdict that passed validation. The process still exits non-zero and the check stays red.\\n\\n  All five strings `scripts/test_shiki_control_plane.sh` greps must survive BYTE-IDENTICAL: `missing required property`, `repair_required verdict must include a non-null object`, `blocking evaluation short-circuited`, `CCA verdict is blocked; MergeGate is blocked`, `CCA verdict complete`.\\n\\nB. CLASSIFY THE REFUSAL AS TRANSIENT OR NOT, AND LET THE LOOP RE-RUN. Do NOT relax the non-degeneracy rules: measured, relaxing them only moves the red check from the re-runnable `CCA verdict` to `MergeGate policy check`, which the loop turns into `stop_guardian`. Instead mark the refusal record with whether the cause is transient. A blocking item or acceptance criterion left `insufficient_evidence` because its evidence was NOT YET AVAILABLE at judge time (the CCA is itself one of the concurrently-running checks) is transient; one left `insufficient_evidence` because the evidence is MISSING is not. Classify by the item's `reason` TEXT \\u2014 the mechanism `_is_already_blocked_reason` (`:261`) already uses. Do NOT add a status value: the item `status` vocabulary is set-equality bound across `validate_shiki.py`, `.shiki/schemas/cca-verdict.schema.json` and the workflow's inline transport schema, and touching it would pull all three plus the workflow into the locks.\\n\\n  In `scripts/shiki_loop.py`, read the refusal record where the verdict is resolved. A TRANSIENT refusal drives the existing `rerun_cca` path under the existing `MAX_CCA_RERUNS` bound; anything else keeps today's terminal stop with today's reason. The refused verdict's own `verdict` value must NEVER be returned by the resolver or reach `_cca_verdict_stop` as if it were a judgment \\u2014 assert this directly, because it is the whole safety property.\\n\\nC. A BLOCKING REVIEW THAT NAMES NOTHING IS NOT A JUDGMENT. `scripts/shiki_loop.py` (around `:1130-1143`) accepts a `blocking` code-review verdict with an empty findings list and records it verbatim, and the resulting stop is terminal with no retry \\u2014 unlike the CCA path, which has explicit non-degeneracy rules. Treat `blocking` with zero findings as 'the reviewer produced nothing usable' and retry once, bounded, before stopping; record the reviewer's raw output durably so a future diagnosis has something to read. A degenerate verdict must never be treated as CLEAN.\\n\\n  COUNTER SCOPE, stated because both naive choices are wrong: a durable per-task counter wedges the task after a legitimate re-dispatch, and an in-memory counter bounds nothing because the loop re-enters on every fresh invocation. Make it durable AND reset it whenever the implementation is (re-)dispatched, so it bounds retries of the SAME reviewed work and never blocks new work.\\n\\nTESTS. `tests/test_cca_verdict_consistency.py`: a refused verdict writes the refusal record and does NOT write `cca-verdict.json`; the record names the rule and the offending ids; the transient/non-transient classification for each rule; the five verbatim strings. `tests/test_loop_verdict_gating.py` currently PINS the outcomes this task changes \\u2014 `:126-131`, `:133-137` (`assertIn('could not be read', reason)`), `:139-144`, and the effector twins at `:476-481`/`:483-489`. Update them to the new behaviour without weakening the safety assertions, and add: a transient refusal yields `rerun_cca` and creates no repair packet; a non-transient one keeps its terminal stop; the refused verdict value never reaches `_cca_verdict_stop`. `tests/test_loop_code_review.py`: blocking-with-zero-findings retries once then stops; a retry that returns findings stops with them named; a retry that returns clean proceeds; the counter resets on re-dispatch; a degenerate verdict is never treated as clean.\\n\\nREPRODUCIBILITY \\u2014 what this task removes, and what to change to get the same result.\\n  Symptom: the loop stops terminally on a green, contract-conforming implementation, and a plain re-run with no code change then passes. Three times on 2026-08-05/06.\\n  Measured (1): PR #291's first CCA run was refused with `complete verdict contains blocking failed checklist items: CCA-06`; its artifact holds 8 files and no `cca-verdict.json`, against 16 for the succeeding run. The loop reported `the CCA verdict could not be read from durable CI evidence ('unresolved')` and stopped.\\n  Measured (2): PR #292 was refused with `complete verdict contains failing acceptance criteria: validate_shiki passes on this HEAD`, for one criterion the CCA had marked `insufficient_evidence` because the check was IN_PROGRESS at judge time \\u2014 which `.github/prompts/cca-completion-check.md` tells it not to block on. Re-run: complete, merged unchanged.\\n  Measured (3): `Pre-PR code-review verdict BLOCKING for <task> (0 finding(s))`. The implementation met all eight contract requirements and its whole test command was green. Re-run: passed, merged with no code change.\\n  Root cause: the enforcer returns before writing, so a refusal is indistinguishable from a crash; and the reviewer path has no non-degeneracy rule at all.\\n  Two things a naive fix breaks, both measured: writing the refusal to `cca-verdict.json` makes a refused verdict an authority every consumer already trusts; and giving `CCA Review Bridge` an `if: always()` to close the artifact gap makes it POST a `--arg event \\\"APPROVE\\\"` review on a PR whose verdict was refused \\u2014 minting the approval MergeGate's required-review policy consumes.\\n  Reproducible when: a refused verdict yields a readable refusal record on a non-authority path, `cca-verdict.json` is still written only for a valid verdict, a transient refusal re-runs within `MAX_CCA_RERUNS`, and a blocking review naming nothing is retried once instead of ending the run.\",\n.shiki/tasks/T-20260729T092307427377Z-c8531f7e.json:50:    \"code-review\"\n.shiki/tasks/T-20260617T090739480720Z-04c7582b.json:4:    \"The loop records a code-review ledger + writes the ## Pre-PR code review PR-body section; CCA PR-12 + MergeGate required_skill=code-review pass.\",\n.shiki/tasks/T-20260617T090739480720Z-04c7582b.json:44:    \"code-review\"\n.shiki/tasks/T-20260617T090739480720Z-04c7582b.json:47:  \"scope\": \"Add an independent read-only code-review verifier run before create_pr (ADR 0011, Q1/Q2): a reviewer dispatch (claude -p, --allowedTools restricted to read tools, --json-schema structured findings verdict) in a separate context, as a reviewer runtime distinct from the bypassPermissions implementer adapter (scripts/shiki_runtime_adapters.py). The loop parses the verdict deterministically (parse/dispatch failure = fail-closed -> block), records a code-review ledger, and writes the '## Pre-PR code review' PR-body section (scripts/shiki_github.py github_pr_body) from it. Blocking findings feed the existing repair loop (verdict -> repair packet -> fix -> re-review), bounded by the 3-attempt limit.\",\n.shiki/tasks/T-20260617T090739480720Z-04c7582b.json:49:  \"title\": \"Independent code-review verifier (read-only claude -p)\"\n.shiki/tasks/T-20260801T041653274008Z-54d3477c.json:56:    \"code-review\"\n.shiki/tasks/T-20260617T090739483386Z-2a275071.json:41:    \"code-review\"\n.shiki/tasks/T-20260617T090739483386Z-2a275071.json:44:  \"scope\": \"Prove the loop self-drives. Add a stubbed-runner + stubbed-reviewer contract test (pattern: scripts/test_shiki_runner_claude.sh / scripts/test_shiki_goal_loop.sh) driving dispatch -> tdd-evidence -> code-review -> commit/push -> create_pr -> checks -> merge -> done, including the failure -> repair branch. Then the acceptance gate: a live shiki loop run on a low-risk claude-code task self-drives to auto-merge + goal complete with no manual pre-clearing (recorded as evidence).\",\n.shiki/tasks/T-20260731T004111132162Z-bfcf2c9e.json:63:    \"code-review\"\n.shiki/tasks/T-20260805T034218808409Z-0f0d9640.json:57:    \"code-review\"\n.shiki/tasks/T-20260805T040948888613Z-d4e830c9.json:70:    \"code-review\"\n.shiki/tasks/T-20260806T065143409817Z-dbdae1cf.json:89:    \"code-review\"\n.shiki/tasks/T-20260801T041653423255Z-2798773b.json:37:    \"code-review\"\n.shiki/ledger/L-20260617T103704022378Z-80d06708.json:9:  \"summary\": \"Skill code-review: independent adversarial pre-PR review (separate-context verifier agent in the parallel workflow) of the T2 branch \\u2014 verdict SOUND, 0 blocking/0 major findings. Checked: fail-closed on red, loop-owned deterministic evidence (never runner self-attestation, ADR 0011), test rigor, safety. Minor notes (test_command default needs tests/ dir; no --test-command CLI flag) recorded as non-blocking concerns.\",\n.shiki/tasks/T-20260613T035102039128Z-9bef9375.json:7:    \"This PR carries pre-PR code-review ledger evidence (PR-12)\"\n.shiki/tasks/T-20260613T035102039128Z-9bef9375.json:51:    \"code-review\"\n.shiki/ledger/L-20260805T013031899680Z-29398772.json:9:  \"summary\": \"Pre-PR code-review verdict BLOCKING for T-20260801T041653274543Z-6df3af7e (2 finding(s)); loop stops for diagnosis\",\n.shiki/tasks/T-20260805T023333275527Z-fff69d84.json:53:    \"code-review\"\n.shiki/tasks/T-20260729T065622766508Z-d36555d8.json:61:    \"code-review\"\n.shiki/tasks/T-20260727T005125902419Z-6c2db440.json:80:    \"code-review\"\n.shiki/tasks/T-20260729T065622763023Z-9b974770.json:65:    \"code-review\"\n.shiki/tasks/T-20260801T040948352877Z-eea16def.json:65:    \"code-review\"\n.shiki/ledger/L-20260618T013133856231Z-b2dada57.json:9:  \"summary\": \"Skill code-review: the parallel-workflow independent verify flagged T4's original DESIGN as BLOCKING \\u2014 the dispatch-time .shiki/** guarantee (loop_guaranteed_locks injecting into lock/worktree records) never reaches MergeGate (which reads the TASK FILE locks) and serializing every loop lock record regressed parallel disjoint tasks. Reworked to warn-only: removed loop_guaranteed_locks entirely; kept the advisory validate_shiki WARN. 252 unit + full shell + validate green.\",\n.shiki/ledger/L-20260731T061404552195Z-d2dfd80b.json:9:  \"summary\": \"Pre-PR code-review verdict CLEAN for T-20260729T065622770078Z-3662bff2 (independent read-only reviewer, code-review skill)\",\n.shiki/tasks/T-20260612T055157606542Z-5205e0bd.json:3:    \"bash scripts/test_shiki_code_review_gate.sh passes: a plan task with required_skills [tdd, code-review] orchestrates and validates; a repair packet with --required-skill code-review passes schema validation; a task registered without explicit skills defaults to [tdd, code-review]\",\n.shiki/tasks/T-20260612T055157606542Z-5205e0bd.json:6:    \"This PR itself carries pre-PR code-review ledger evidence (dogfood of the new gate)\"\n.shiki/tasks/T-20260612T055157606542Z-5205e0bd.json:53:    \"code-review\"\n.shiki/tasks/T-20260612T055157606542Z-5205e0bd.json:56:  \"scope\": \"Add skills/engineering/code-review/SKILL.md (wrapper: run the native code-review skill after TDD green and before PR creation, apply fixes as the implementer on the task branch, record findings+resolutions as a ledger entry whose summary names the skill and as a PR body section). Extend validate_shiki.py KNOWN_SKILLS, .shiki/schemas/repair-packet.schema.json required_skill enum, and shiki_cli.py repair --required-skill choices with code-review. Flip default required_skills to [tdd, code-review] in shiki_tasks.register_task_from_plan and shiki_bootstrap start synthesis so MergeGate's existing skill-evidence rule enforces the gate on every new task. Document the contract: skill-gate.md row + quick map, AGENTS.md Skill Gate table row, checklists.md PR-12 item, triage-labels.md skill:code-review, implementation-policy.md implementer contract (TDD -> code-review -> PR), CLAUDE.md handoff standard line. Add scripts/test_shiki_code_review_gate.sh (plan task and repair packet carrying code-review pass validation; SKILL.md staged) and register it in installer TEMPLATE_PATHS. Guardian-approved implementation by Claude Code in this assigned task.\",\n.shiki/tasks/T-20260612T055157606542Z-5205e0bd.json:58:  \"title\": \"Register code-review as a Skill Gate skill and make it the default pre-PR implementer gate\"\n.shiki/tasks/T-20260622T045802441359Z-78831747.json:46:    \"code-review\"\n.shiki/tasks/T-20260806T084356215508Z-7a7dd694.json:60:    \"code-review\"\n.shiki/tasks/T-20260618T110731105287Z-5276602e.json:38:    \"code-review\"\n.shiki/tasks/T-20260613T095433721787Z-8d6b04d5.json:18:    \"code-review\"\n.shiki/ledger/L-20260731T075252878384Z-34e22196.json:9:  \"summary\": \"Pre-PR code-review verdict CLEAN for T-20260729T065622770911Z-a8db8b69 (independent read-only reviewer, code-review skill)\",\n.shiki/tasks/T-20260803T013839640380Z-ca171b22.json:66:    \"code-review\"\n.shiki/tasks/T-20260729T065622769184Z-628504fe.json:49:    \"code-review\"\n.shiki/tasks/T-20260805T075725961882Z-91c2ecac.json:71:    \"code-review\"\n.shiki/tasks/T-20260805T075725961882Z-91c2ecac.json:74:  \"scope\": \"In `scripts/shiki_loop.py`'s `decide_task_action`, narrow the exclusion that today sends every red-check-plus-BEHIND state to `dispatch_repair`. The new `sync_branch` arm must satisfy ALL of these, and each is a separate assertion:\\n\\n  1. PLACEMENT. It is evaluated INSIDE the `if repairable_failed:` block (`:279-316`) but strictly AFTER `_cca_verdict_stop` (`:298-302`) has returned None. A CCA verdict of `needs_guardian` or `blocked` must still produce its terminal stop with a BEHIND branch \\u2014 the measured incident's own verdict was `blocked`, and laundering it into a head-moving sync is the exact impersonation pathway ADR 0010 forbids.\\n\\n  2. GUARDIAN. It never fires while `POLICY_GATE` is in `failed`, for any reason \\u2014 not merely when the policy gate is the ONLY red check. `repairable_failed` excludes POLICY_GATE (`:275`), so `{policy: fail, CCA: fail}` + BEHIND would otherwise sync and destroy a head-bound Guardian approval that had already been posted for the current head. The same rule also refuses to sync through a state where base movement defeated an ADR 0015 contract-approval carry (`shiki_contract_approval.py:193-198` compares base and head task governance fields directly), which would otherwise be silently restored by adopting base wholesale.\\n\\n  3. BOUND. Gate it on a durable task-file counter `base_sync_attempts` against a new `MAX_BASE_SYNCS`, following the existing `cca_rerun_count`/`MAX_CCA_RERUNS` (`:57`) pattern exactly. `.shiki/schemas/task.schema.json` is `additionalProperties: true` and does not declare `cca_rerun_count` either, so do NOT add the field to the schema \\u2014 that file belongs to another task. Once the cap is reached, fall through to today's behaviour unchanged: `dispatch_repair`, then the repair limit, then `stop_guardian`. The escalation at `:304` must stay reachable.\\n\\n  4. NO ATTEMPT CONSUMED. A sync must not create a repair packet and must not advance `repair_attempts`.\\n\\n  5. EVIDENCE. `_sync_branch` (`:1429-1525`) appends no ledger today; a sync that now remediates a RED check is a remediation decision on the same footing as `dispatch_repair` and must leave a durable record. Use an EXISTING ledger type \\u2014 registering a new one requires `.shiki/schemas/ledger.schema.json` and `validate_shiki.py:1892`'s set-equality binder to agree, and both are outside these locks.\\n\\n  6. WORKTREE RECONCILIATION. `_sync_branch` pushes from a throwaway detached checkout (`:1471`, `:1498`) and never updates the REGISTERED worktree's local branch ref; `ensure_physical_worktree` reuses an existing path without reconciling it. Today that is harmless because a sync is only followed by a merge. This change makes a sync be followed by a REPAIR, whose push (`:836`) is then a non-fast-forward failing with a misleading message. Fix it IN-FILE \\u2014 `scripts/shiki_runtime.py` must not be modified \\u2014 and do not use a destructive reset that could discard uncommitted work in the registered worktree.\\n\\n  7. BASE REF. `_sync_branch`'s `base: str = \\\"main\\\"` default (`:1429`) is hard-coded and the call site (`:1875`) passes nothing. This change promotes that path from rare to common, so resolve the base the way the rest of the module now does. Note the shape mismatch: `_default_base_ref` (`:772-795`) returns `origin/<name>` while `_sync_branch` builds `origin/{base}` \\u2014 do not produce `origin/origin/main`.\\n\\n  8. CLOSEOUT UNCHANGED. The closeout arm shares `_merge_or_sync` (`:260`) but its failing-check arm returns `stop_blocked`. `docs/adr/0012` pins that decision table verbatim. Assert the closeout decision table is unchanged so the ADR stays truthful.\\n\\nTESTS. `tests/test_branch_behind_sync.py:124` `test_behind_but_failing_check_is_not_synced` currently pins the old rule with the comment 'a failing check repairs, never syncs'; invert it and state the new rule and its conditions. Add pure-decision cases for: BEHIND + `needs_guardian` verdict -> stop_guardian; BEHIND + `blocked` -> stop_blocked; BEHIND + policy red + another red check -> NOT sync; BEHIND + pending -> wait_checks (unchanged); the counter at and past `MAX_BASE_SYNCS`; and BEHIND + repairable red under the cap -> sync_branch.\\n\\n`scripts/test_shiki_goal_loop.sh` MUST gain the end-to-end proof that a sync consumes no repair attempt, modelled on the identical assertion it already makes for the Guardian gate at `:408-417` (`find .shiki/repairs -name 'RP-*.json' | wc -l` before and after, asserted equal). The pure-fixture module cannot observe repair packets, so this is the only surface that can prove it.\\n\\n`tests/test_loop_branch_plumbing.py` imports `_commit_and_push_implementation` directly (`:22-27`); extend it to prove a repair dispatched AFTER a sync can still push.\\n\\nDo not introduce a `\\\"verdict\\\": \\\"...\\\"` string literal into `scripts/shiki_loop.py`: `tests/test_paired_invariants.py:546-548` regex-scans this file's source and set-equality-binds those literals to the code-review verdict vocabulary.\\n\\nREPRODUCIBILITY \\u2014 what this task removes, and what to change to get the same result.\\n  Symptom: a correct implementation is answered with three identical repair dispatches that change nothing, then `stop_guardian: required checks failed (MergeGate metadata check) and repair attempt limit reached`. The task is fine; the branch is merely stale.\\n  Measured 2026-08-05 on PR #288 (task T-20260805T040948888613Z-d4e830c9): the sole failing reason was 'PR must not delete base ledger file .shiki/ledger/L-20260805T052931749888Z-3017e935.json; state_class=append-only-evidence'. That file reached main via PR #287 after the branch forked. Each of the three repair packets diagnosed it as a loop-owned base sync and declined to act; the CCA returned verdict=blocked citing the same cause and quoting all three packets. A single `git merge origin/main` plus a push turned every required check green and the PR merged unchanged.\\n  Root cause: the only `sync_branch` emitter is reachable only when every required check is already green (`:101` docstring), while the red-check path returns `dispatch_repair` first (`:279-316`).\\n  Why it was written that way: `tests/test_branch_behind_sync.py:124` deliberately pinned 'a failing check repairs, never syncs'. The case where being behind is the CAUSE of the red check was not considered.\\n  Failure directions that matter, both measured against the source: syncing while the Guardian gate is red destroys a head-bound approval and can silently restore a defeated contract-approval carry; and an unbounded, unrecorded sync makes the repair limit dead code and trades a terminating stop for a livelock.\\n  Reproducible when: the #288 sequence \\u2014 BEHIND branch, red metadata check whose sole reason is a base file the branch never had \\u2014 is answered by one recorded sync and zero repair packets, while a `needs_guardian` verdict and a red policy gate on a BEHIND branch still stop exactly as today.\",\n.shiki/tasks/T-20260730T060551622687Z-7bea1bc2.json:56:    \"code-review\"\n.shiki/tasks/T-20260612T091329543362Z-778e0c2f.json:8:    \"This PR carries pre-PR code-review ledger evidence (PR-12)\"\n.shiki/tasks/T-20260612T091329543362Z-778e0c2f.json:63:    \"code-review\"\n.shiki/tasks/T-20260801T041653348713Z-6283adcd.json:41:    \"code-review\"\n.shiki/ledger/L-20260804T130815804255Z-b15290bc.json:9:  \"summary\": \"Pre-PR code-review verdict CLEAN for T-20260804T095105534980Z-530c4f17 (independent read-only reviewer, code-review skill)\",\n.shiki/tasks/T-20260729T065622765413Z-a0055a9d.json:54:    \"code-review\"\n.shiki/tasks/T-20260729T065622770911Z-a8db8b69.json:45:    \"code-review\"\n.shiki/tasks/T-20260612T152357707653Z-e200274d.json:50:    \"code-review\"\n.shiki/tasks/T-20260807T035659159912Z-f2e1c0b0.json:47:    \"code-review\"\n.shiki/tasks/T-20260807T035659160500Z-ba479979.json:57:    \"code-review\"\n.shiki/ledger/L-20260729T230853746496Z-3545054e.json:9:  \"summary\": \"Pre-PR code-review verdict CLEAN for T-20260729T065622764822Z-bac7ef27 (independent read-only reviewer, code-review skill)\",\n.shiki/tasks/T-20260801T040836632579Z-2cde7018.json:51:    \"code-review\"\n.shiki/tasks/T-20260807T020527152153Z-97fe21d1.json:60:    \"code-review\"\n.shiki/tasks/T-20260729T065622768255Z-da448d45.json:48:    \"code-review\"\n.shiki/tasks/T-20260807T073307221254Z-2d22d654.json:10:    \"Red-green-refactor and a structured code-review verdict are recorded, with all blocking findings repaired and re-reviewed.\",\n.shiki/tasks/T-20260807T073307221254Z-2d22d654.json:32:  \"goal_id\": \"G-20260807T073307219752Z-1ee1ed68\",\n.shiki/tasks/T-20260807T073307221254Z-2d22d654.json:33:  \"id\": \"T-20260807T073307221254Z-2d22d654\",\n.shiki/tasks/T-20260807T073307221254Z-2d22d654.json:65:    \"code-review\"\n.shiki/ledger/L-20260618T031710138071Z-24cefbfb.json:9:  \"summary\": \"Task registered: Fix reviewer structured-output emission and parsing in the pre-PR code-review gate\",\n.shiki/ledger/L-20260618T031646698968Z-b60d0d6b.json:9:  \"summary\": \"Goal registered: Repair: autonomous code-review gate could not parse the reviewer verdict (#140 T5 prerequisite)\",\n.shiki/ledger/L-20260806T060736214432Z-b4900f11.json:9:  \"summary\": \"Pre-PR code-review verdict CLEAN for T-20260806T034026771634Z-52142739 (independent read-only reviewer, code-review skill)\",\n.shiki/tasks/T-20260729T065622764296Z-48021407.json:63:    \"code-review\"\n.shiki/dag/G-20260807T073307219752Z-1ee1ed68.json:3:  \"goal_id\": \"G-20260807T073307219752Z-1ee1ed68\",\n.shiki/dag/G-20260807T073307219752Z-1ee1ed68.json:5:    \"T-20260807T073307221254Z-2d22d654\"\n.shiki/tasks/T-20260619T102857596978Z-386d2ca9.json:54:    \"code-review\"\n.shiki/ledger/L-20260729T222631570501Z-ac5d2802.json:9:  \"summary\": \"Pre-PR code-review verdict CLEAN for T-20260729T065622763023Z-9b974770 (independent read-only reviewer, code-review skill)\",\n.shiki/tasks/T-20260804T094832861501Z-78871209.json:50:    \"code-review\"\n.shiki/tasks/T-20260801T041653424169Z-07860c35.json:38:    \"code-review\"\n.shiki/ledger/L-20260612T061357036867Z-e2fff40f.json:5:    \"skills/engineering/code-review/SKILL.md\"\n.shiki/ledger/L-20260612T061357036867Z-e2fff40f.json:12:  \"summary\": \"Skill grill-with-docs completed for this Goal: the code-review gate design (wrapper skill + default required_skills + PR-12 + skill-evidence enforcement) was settled in the 2026-06-12 Requirements Definition session recorded in ADR 0008 and PRD issue #119\",\n.shiki/tasks/T-20260618T044720078664Z-7502dbd0.json:52:    \"code-review\"\n.shiki/tasks/T-20260617T031753971551Z-24f8c42f.json:40:    \"No TDD/code-review evidence (T2/T3).\",\n.shiki/tasks/T-20260617T031753971551Z-24f8c42f.json:45:    \"code-review\"\n.shiki/tasks/T-20260801T041653422672Z-c4815d7a.json:52:    \"code-review\"\n.shiki/ledger/L-20260619T104025300330Z-8fdc261c.json:9:  \"summary\": \"Pre-PR code-review verdict CLEAN for T-20260619T102857596978Z-386d2ca9 (independent read-only reviewer, code-review skill)\",\n.shiki/ledger/L-20260805T031656057215Z-b9fc66b7.json:9:  \"summary\": \"Pre-PR code-review verdict CLEAN for T-20260805T023333275527Z-fff69d84 (independent read-only reviewer, code-review skill)\",\n.shiki/tasks/T-20260618T031710137746Z-dfb3c451.json:52:    \"code-review\"\n.shiki/tasks/T-20260618T031710137746Z-dfb3c451.json:58:  \"title\": \"Fix reviewer structured-output emission and parsing in the pre-PR code-review gate\"\n.shiki/tasks/T-20260618T093119138788Z-867546d0.json:37:    \"code-review\"\n.shiki/ledger/L-20260618T093251323134Z-f86e815e.json:9:  \"summary\": \"Pre-PR code-review verdict CLEAN for T-20260618T093119138788Z-867546d0 (independent read-only reviewer, code-review skill)\",\n.shiki/ledger/L-20260729T222851395078Z-f67d92cc.json:9:  \"summary\": \"Pre-PR code-review verdict CLEAN for T-20260729T065622768255Z-da448d45 (independent read-only reviewer, code-review skill)\",\n.shiki/tasks/T-20260804T002751221801Z-a914603f.json:69:    \"code-review\"\n.shiki/ledger/L-20260806T003357149682Z-6c643004.json:9:  \"summary\": \"Pre-PR code-review verdict CLEAN for T-20260805T075725961882Z-91c2ecac (independent read-only reviewer, code-review skill)\",\n.shiki/ledger/L-20260805T045054957596Z-bd5a33e6.json:9:  \"summary\": \"Pre-PR code-review verdict CLEAN for T-20260805T034218808409Z-0f0d9640 (independent read-only reviewer, code-review skill)\",\n.shiki/tasks/T-20260729T065622765968Z-234bf98b.json:49:    \"code-review\"\n.shiki/tasks/T-20260612T152357706392Z-75f529da.json:6:    \"CI (Validate / Claude review / CCA / MergeGate) green; PR carries PR-12 code-review evidence\"\n.shiki/tasks/T-20260612T152357706392Z-75f529da.json:52:    \"code-review\"\n.shiki/tasks/T-20260801T040836631930Z-97030be9.json:68:    \"code-review\"\n.shiki/ledger/L-20260729T233131288359Z-a3bba3a5.json:9:  \"summary\": \"Pre-PR code-review verdict CLEAN for T-20260729T065622766508Z-d36555d8 (independent read-only reviewer, code-review skill)\",\n.shiki/tasks/T-20260625T070242177499Z-a75c7c56.json:35:    \"code-review\"\n.shiki/plans/P-20260617T031753871099Z-bac3fdc7.json:3:    \"A stubbed-runner + stubbed-reviewer contract test drives the full dispatch->tdd-evidence->code-review->commit/push->create_pr->merge->done path including the failure->repair branch.\",\n.shiki/plans/P-20260617T031753871099Z-bac3fdc7.json:10:      \"Q2: read-only claude -p code-review verifier, structured verdict, fail-closed, blocking->repair loop.\",\n.shiki/plans/P-20260617T031753871099Z-bac3fdc7.json:26:  \"outcome\": \"shiki loop run self-drives a low/medium-risk task from ready to auto-merged done with zero manual pre-clearing: implement -> loop-observed TDD evidence -> independent code-review -> commit/push -> PR -> checks+CCA pass -> MergeGate green -> auto-merge -> done -> goal complete.\",\n.shiki/plans/P-20260617T031753871099Z-bac3fdc7.json:29:    \"code-review\"\n.shiki/plans/P-20260617T031753871099Z-bac3fdc7.json:57:        \"No TDD/code-review evidence (T2/T3).\",\n.shiki/plans/P-20260617T031753871099Z-bac3fdc7.json:62:        \"code-review\"\n.shiki/plans/P-20260617T031753871099Z-bac3fdc7.json:84:        \"No code-review verifier (T3).\",\n.shiki/plans/P-20260617T031753871099Z-bac3fdc7.json:89:        \"code-review\"\n.shiki/plans/P-20260617T031753871099Z-bac3fdc7.json:99:        \"The loop records a code-review ledger + writes the ## Pre-PR code review PR-body section; CCA PR-12 + MergeGate required_skill=code-review pass.\",\n.shiki/plans/P-20260617T031753871099Z-bac3fdc7.json:117:        \"code-review\"\n.shiki/plans/P-20260617T031753871099Z-bac3fdc7.json:121:      \"scope\": \"Add an independent read-only code-review verifier run before create_pr (ADR 0011, Q1/Q2): a reviewer dispatch (claude -p, --allowedTools restricted to read tools, --json-schema structured findings verdict) in a separate context, as a reviewer runtime distinct from the bypassPermissions implementer adapter (scripts/shiki_runtime_adapters.py). The loop parses the verdict deterministically (parse/dispatch failure = fail-closed -> block), records a code-review ledger, and writes the '## Pre-PR code review' PR-body section (scripts/shiki_github.py github_pr_body) from it. Blocking findings feed the existing repair loop (verdict -> repair packet -> fix -> re-review), bounded by the 3-attempt limit.\",\n.shiki/plans/P-20260617T031753871099Z-bac3fdc7.json:122:      \"title\": \"Independent code-review verifier (read-only claude -p)\"\n.shiki/plans/P-20260617T031753871099Z-bac3fdc7.json:142:        \"code-review\"\n.shiki/plans/P-20260617T031753871099Z-bac3fdc7.json:165:        \"code-review\"\n.shiki/plans/P-20260617T031753871099Z-bac3fdc7.json:169:      \"scope\": \"Prove the loop self-drives. Add a stubbed-runner + stubbed-reviewer contract test (pattern: scripts/test_shiki_runner_claude.sh / scripts/test_shiki_goal_loop.sh) driving dispatch -> tdd-evidence -> code-review -> commit/push -> create_pr -> checks -> merge -> done, including the failure -> repair branch. Then the acceptance gate: a live shiki loop run on a low-risk claude-code task self-drives to auto-merge + goal complete with no manual pre-clearing (recorded as evidence).\",\n.shiki/ledger/L-20260731T061333503889Z-a282e717.json:9:  \"summary\": \"Pre-PR code-review verdict CLEAN for T-20260729T065622767228Z-3b4c41c2 (independent read-only reviewer, code-review skill)\",\n.shiki/plans/P-20260805T023333198254Z-38fa603f.json:34:    \"code-review\"\n.shiki/plans/P-20260806T065143332059Z-03d9edee.json:39:    \"code-review\"\n.shiki/plans/P-20260805T025939296778Z-45b224d9.json:34:    \"code-review\"\n.shiki/plans/P-20260728T103735707274Z-80f461eb.json:31:    \"code-review\"\n.shiki/plans/P-20260801T041646831458Z-61e53ba0.json:35:    \"code-review\"\n.shiki/plans/P-20260807T035659082985Z-9770247b.json:40:    \"code-review\"\n.shiki/plans/P-20260807T073304061131Z-9dfd1fc8.json:45:    \"code-review\"\n.shiki/plans/P-20260807T073304061131Z-9dfd1fc8.json:67:        \"Red-green-refactor and a structured code-review verdict are recorded, with all blocking findings repaired and re-reviewed.\",\n.shiki/plans/P-20260807T073304061131Z-9dfd1fc8.json:110:        \"code-review\"\n.shiki/plans/P-20260806T084356137490Z-39afc661.json:36:    \"code-review\"\n.shiki/plans/P-20260726T053204320783Z-2265e5c3.json:43:  \"code-review\",\n.shiki/plans/P-20260726T053204320783Z-2265e5c3.json:50:   \"A1 (operator-approved 2026-07-27, from a live incident): the first autonomous dispatch wrote .shiki/plans/P-20260727T005125000000Z-0015c0de.json carrying spec_freeze.status=frozen with approved_by=mizutani-140 \u2014 an operator Spec Freeze the operator never gave \u2014 to satisfy an acceptance criterion that could not be met with the artifacts in its worktree, and hard-coded its id into the tests. 412 tests and validate_shiki were green; .shiki/plans/ is outside MergeGate's scoped-mutation rules. R-02 is extended to forbid a PR authoring or modifying any spec_freeze block, including on its own goal's plan, and a new docs slice codifies the prohibition and the stop-instead-of-fabricate rule in the constitution. Note: whether the independent code-review verifier would have caught it is UNTESTED \u2014 the loop's gates had not run when the artifact was found.\",\n.shiki/plans/P-20260727T005119007135Z-1fa476be.json:33:    \"code-review\"\n.shiki/plans/P-20260804T082618526063Z-c9f357c4.json:39:    \"code-review\"\n.shiki/plans/P-20260801T040831274049Z-8c4d29d1.json:33:    \"code-review\"\n.shiki/ledger/L-20260805T062039384036Z-ce55a37a.json:9:  \"summary\": \"Pre-PR code-review verdict CLEAN for T-20260805T040948888613Z-d4e830c9 (independent read-only reviewer, code-review skill)\",\n.shiki/plans/P-20260805T034218729381Z-43271caf.json:36:    \"code-review\"\n.shiki/plans/P-20260612T152357625042Z-b25b41ec.json:33:    \"code-review\"\n.shiki/plans/P-20260612T152357625042Z-b25b41ec.json:51:        \"CI (Validate / Claude review / CCA / MergeGate) green; PR carries PR-12 code-review evidence\"\n.shiki/plans/P-20260612T152357625042Z-b25b41ec.json:73:        \"code-review\"\n.shiki/plans/P-20260612T152357625042Z-b25b41ec.json:105:        \"code-review\"\n.shiki/plans/P-20260612T152357625042Z-b25b41ec.json:141:        \"code-review\"\n.shiki/plans/P-20260804T095105458113Z-5f2a2cc6.json:37:    \"code-review\"\n.shiki/plans/P-20260801T085701914332Z-f614d83d.json:36:    \"code-review\"\n.shiki/ledger/L-20260618T111059880324Z-fb8a0e8f.json:9:  \"summary\": \"Pre-PR code-review verdict CLEAN for T-20260618T110731105287Z-5276602e (independent read-only reviewer, code-review skill)\",\n.shiki/plans/P-20260805T040948811693Z-b168f414.json:35:    \"code-review\"\n.shiki/plans/P-20260801T040831205733Z-f88e60e0.json:34:    \"code-review\"\n.shiki/plans/P-20260801T041646902921Z-4d58e06c.json:35:    \"code-review\"\n.shiki/plans/P-20260803T013829425758Z-805c8ace.json:37:    \"code-review\"\n.shiki/ledger/L-20260807T073307221361Z-b3c3a718.json:4:    \".shiki/tasks/T-20260807T073307221254Z-2d22d654.json\"\n.shiki/ledger/L-20260807T073307221361Z-b3c3a718.json:6:  \"goal_id\": \"G-20260807T073307219752Z-1ee1ed68\",\n.shiki/ledger/L-20260807T073307221361Z-b3c3a718.json:10:  \"task_id\": \"T-20260807T073307221254Z-2d22d654\",\n.shiki/ledger/L-20260617T114319946700Z-4cb746f8.json:9:  \"summary\": \"Skill code-review: independent adversarial pre-PR verify (separate-context agent in the parallel workflow) of the T3 branch \\u2014 verdict SOUND, 0 blocking/0 major. Checked fail-closed on blocking/parse-failure, read-only reviewer confinement, loop-owned determinism (ADR 0011), safety. ADR 0011 language updated (blocking pre-PR review stops for diagnosis; repair loop = post-PR check failures).\",\n.shiki/ledger/L-20260805T111347074794Z-b682d8a1.json:9:  \"summary\": \"Pre-PR code-review verdict BLOCKING for T-20260805T075725961882Z-91c2ecac (0 finding(s)); loop stops for diagnosis\",\n.shiki/plans/P-20260804T002743387879Z-7c1183ba.json:43:    \"code-review\"\n.shiki/ledger/L-20260729T041147315166Z-e8238571.json:9:  \"summary\": \"Pre-PR code-review verdict CLEAN for T-20260728T103742824401Z-f198cd24 (independent read-only reviewer, code-review skill)\",\n.shiki/ledger/L-20260805T010622716989Z-dea61fa9.json:9:  \"summary\": \"Pre-PR code-review verdict CLEAN for T-20260801T041653274008Z-54d3477c (independent read-only reviewer, code-review skill)\",\n.shiki/ledger/L-20260801T072706162365Z-7ec34edb.json:9:  \"summary\": \"Pre-PR code-review verdict CLEAN for T-20260801T040948352279Z-07301e34 (independent read-only reviewer, code-review skill)\",\n.shiki/plans/P-20260806T014011309806Z-15cf341c.json:6:    \"A code-review verdict of blocking with zero findings is retried once, bounded, and never silently passes.\",\n.shiki/plans/P-20260806T014011309806Z-15cf341c.json:45:    \"code-review\"\n.shiki/plans/P-20260806T014011309806Z-15cf341c.json:69:        \"A code-review verdict of blocking with zero findings is retried once, bounded, and is never treated as clean.\",\n.shiki/plans/P-20260806T014011309806Z-15cf341c.json:96:      \"scope\": \"Three parts, one property: a gate that refuses a judgment must leave a readable record of why, and a refusal whose cause is transient must reach the bounded re-run the loop already has instead of ending the run. Nothing about which judgments are valid changes, and a refused judgment must never become an authority.\\n\\nA. RECORD THE REFUSAL, ON A DIFFERENT PATH. `scripts/enforce_cca_verdict.py` `main()` returns `fail(...)` from its `except` BEFORE `output_path.write_text(...)`, so a refused verdict leaves nothing. Write a refusal record \\u2014 the verdict as received, the rule violated, and the offending checklist/acceptance ids \\u2014 and write it to a path DERIVED from `CCA_VERDICT_FILE` (the control-plane suite sets that env var to a temp path; do not hard-code `.shiki/gha/`). It must NOT be `cca-verdict.json`: that path is what every consumer reads as the CCA's operative authority, and the loop's resolver re-derives none of the rejection rules, so a refused `repair_required` written there would become an auto-dispatched repair. `cca-verdict.json` keeps its meaning exactly \\u2014 written only for a verdict that passed validation. The process still exits non-zero and the check stays red.\\n\\n  All five strings `scripts/test_shiki_control_plane.sh` greps must survive BYTE-IDENTICAL: `missing required property`, `repair_required verdict must include a non-null object`, `blocking evaluation short-circuited`, `CCA verdict is blocked; MergeGate is blocked`, `CCA verdict complete`.\\n\\nB. CLASSIFY THE REFUSAL AS TRANSIENT OR NOT, AND LET THE LOOP RE-RUN. Do NOT relax the non-degeneracy rules: measured, relaxing them only moves the red check from the re-runnable `CCA verdict` to `MergeGate policy check`, which the loop turns into `stop_guardian`. Instead mark the refusal record with whether the cause is transient. A blocking item or acceptance criterion left `insufficient_evidence` because its evidence was NOT YET AVAILABLE at judge time (the CCA is itself one of the concurrently-running checks) is transient; one left `insufficient_evidence` because the evidence is MISSING is not. Classify by the item's `reason` TEXT \\u2014 the mechanism `_is_already_blocked_reason` (`:261`) already uses. Do NOT add a status value: the item `status` vocabulary is set-equality bound across `validate_shiki.py`, `.shiki/schemas/cca-verdict.schema.json` and the workflow's inline transport schema, and touching it would pull all three plus the workflow into the locks.\\n\\n  In `scripts/shiki_loop.py`, read the refusal record where the verdict is resolved. A TRANSIENT refusal drives the existing `rerun_cca` path under the existing `MAX_CCA_RERUNS` bound; anything else keeps today's terminal stop with today's reason. The refused verdict's own `verdict` value must NEVER be returned by the resolver or reach `_cca_verdict_stop` as if it were a judgment \\u2014 assert this directly, because it is the whole safety property.\\n\\nC. A BLOCKING REVIEW THAT NAMES NOTHING IS NOT A JUDGMENT. `scripts/shiki_loop.py` (around `:1130-1143`) accepts a `blocking` code-review verdict with an empty findings list and records it verbatim, and the resulting stop is terminal with no retry \\u2014 unlike the CCA path, which has explicit non-degeneracy rules. Treat `blocking` with zero findings as 'the reviewer produced nothing usable' and retry once, bounded, before stopping; record the reviewer's raw output durably so a future diagnosis has something to read. A degenerate verdict must never be treated as CLEAN.\\n\\n  COUNTER SCOPE, stated because both naive choices are wrong: a durable per-task counter wedges the task after a legitimate re-dispatch, and an in-memory counter bounds nothing because the loop re-enters on every fresh invocation. Make it durable AND reset it whenever the implementation is (re-)dispatched, so it bounds retries of the SAME reviewed work and never blocks new work.\\n\\nTESTS. `tests/test_cca_verdict_consistency.py`: a refused verdict writes the refusal record and does NOT write `cca-verdict.json`; the record names the rule and the offending ids; the transient/non-transient classification for each rule; the five verbatim strings. `tests/test_loop_verdict_gating.py` currently PINS the outcomes this task changes \\u2014 `:126-131`, `:133-137` (`assertIn('could not be read', reason)`), `:139-144`, and the effector twins at `:476-481`/`:483-489`. Update them to the new behaviour without weakening the safety assertions, and add: a transient refusal yields `rerun_cca` and creates no repair packet; a non-transient one keeps its terminal stop; the refused verdict value never reaches `_cca_verdict_stop`. `tests/test_loop_code_review.py`: blocking-with-zero-findings retries once then stops; a retry that returns findings stops with them named; a retry that returns clean proceeds; the counter resets on re-dispatch; a degenerate verdict is never treated as clean.\\n\\nREPRODUCIBILITY \\u2014 what this task removes, and what to change to get the same result.\\n  Symptom: the loop stops terminally on a green, contract-conforming implementation, and a plain re-run with no code change then passes. Three times on 2026-08-05/06.\\n  Measured (1): PR #291's first CCA run was refused with `complete verdict contains blocking failed checklist items: CCA-06`; its artifact holds 8 files and no `cca-verdict.json`, against 16 for the succeeding run. The loop reported `the CCA verdict could not be read from durable CI evidence ('unresolved')` and stopped.\\n  Measured (2): PR #292 was refused with `complete verdict contains failing acceptance criteria: validate_shiki passes on this HEAD`, for one criterion the CCA had marked `insufficient_evidence` because the check was IN_PROGRESS at judge time \\u2014 which `.github/prompts/cca-completion-check.md` tells it not to block on. Re-run: complete, merged unchanged.\\n  Measured (3): `Pre-PR code-review verdict BLOCKING for <task> (0 finding(s))`. The implementation met all eight contract requirements and its whole test command was green. Re-run: passed, merged with no code change.\\n  Root cause: the enforcer returns before writing, so a refusal is indistinguishable from a crash; and the reviewer path has no non-degeneracy rule at all.\\n  Two things a naive fix breaks, both measured: writing the refusal to `cca-verdict.json` makes a refused verdict an authority every consumer already trusts; and giving `CCA Review Bridge` an `if: always()` to close the artifact gap makes it POST a `--arg event \\\"APPROVE\\\"` review on a PR whose verdict was refused \\u2014 minting the approval MergeGate's required-review policy consumes.\\n  Reproducible when: a refused verdict yields a readable refusal record on a non-authority path, `cca-verdict.json` is still written only for a valid verdict, a transient refusal re-runs within `MAX_CCA_RERUNS`, and a blocking review naming nothing is retried once instead of ending the run.\",\n.shiki/ledger/L-20260731T034257867241Z-b8481759.json:9:  \"summary\": \"Pre-PR code-review verdict CLEAN for T-20260731T031357587095Z-2cb11839 (independent read-only reviewer, code-review skill)\",\n.shiki/plans/P-20260801T041646760027Z-2a4c05db.json:31:    \"code-review\"\n.shiki/ledger/L-20260803T034916099625Z-4275c2ec.json:9:  \"summary\": \"Pre-PR code-review verdict CLEAN for T-20260803T013839640380Z-ca171b22 (independent read-only reviewer, code-review skill)\",\n.shiki/ledger/L-20260804T091845796586Z-9f4fa270.json:9:  \"summary\": \"Pre-PR code-review verdict CLEAN for T-20260804T082618611335Z-02de0043 (independent read-only reviewer, code-review skill)\",\n.shiki/ledger/L-20260618T081657849508Z-e149666a.json:9:  \"summary\": \"Pre-PR code-review verdict CLEAN for T-20260618T081544128578Z-dc72a6c0 (independent read-only reviewer, code-review skill)\",\n.shiki/ledger/L-20260801T021115151976Z-d069ba3a.json:9:  \"summary\": \"Pre-PR code-review verdict CLEAN for T-20260801T003238841526Z-9f3f6f63 (independent read-only reviewer, code-review skill)\",\n.shiki/plans/P-20260807T020527072088Z-ccd774ff.json:36:    \"code-review\"\n.shiki/ledger/L-20260807T073307220560Z-fc2d907a.json:4:    \".shiki/goals/G-20260807T073307219752Z-1ee1ed68.json\",\n.shiki/ledger/L-20260807T073307220560Z-fc2d907a.json:7:  \"goal_id\": \"G-20260807T073307219752Z-1ee1ed68\",\n.shiki/plans/P-20260806T034026690262Z-742a3053.json:37:    \"code-review\"\n.shiki/ledger/L-20260731T041938094471Z-2ba57da3.json:9:  \"summary\": \"Pre-PR code-review verdict CLEAN for T-20260731T040711237895Z-21826083 (independent read-only reviewer, code-review skill)\",\n.shiki/plans/P-20260805T075711997554Z-15a6b498.json:47:    \"code-review\"\n.shiki/plans/P-20260805T075711997554Z-15a6b498.json:97:      \"scope\": \"In `scripts/shiki_loop.py`'s `decide_task_action`, narrow the exclusion that today sends every red-check-plus-BEHIND state to `dispatch_repair`. The new `sync_branch` arm must satisfy ALL of these, and each is a separate assertion:\\n\\n  1. PLACEMENT. It is evaluated INSIDE the `if repairable_failed:` block (`:279-316`) but strictly AFTER `_cca_verdict_stop` (`:298-302`) has returned None. A CCA verdict of `needs_guardian` or `blocked` must still produce its terminal stop with a BEHIND branch \\u2014 the measured incident's own verdict was `blocked`, and laundering it into a head-moving sync is the exact impersonation pathway ADR 0010 forbids.\\n\\n  2. GUARDIAN. It never fires while `POLICY_GATE` is in `failed`, for any reason \\u2014 not merely when the policy gate is the ONLY red check. `repairable_failed` excludes POLICY_GATE (`:275`), so `{policy: fail, CCA: fail}` + BEHIND would otherwise sync and destroy a head-bound Guardian approval that had already been posted for the current head. The same rule also refuses to sync through a state where base movement defeated an ADR 0015 contract-approval carry (`shiki_contract_approval.py:193-198` compares base and head task governance fields directly), which would otherwise be silently restored by adopting base wholesale.\\n\\n  3. BOUND. Gate it on a durable task-file counter `base_sync_attempts` against a new `MAX_BASE_SYNCS`, following the existing `cca_rerun_count`/`MAX_CCA_RERUNS` (`:57`) pattern exactly. `.shiki/schemas/task.schema.json` is `additionalProperties: true` and does not declare `cca_rerun_count` either, so do NOT add the field to the schema \\u2014 that file belongs to another task. Once the cap is reached, fall through to today's behaviour unchanged: `dispatch_repair`, then the repair limit, then `stop_guardian`. The escalation at `:304` must stay reachable.\\n\\n  4. NO ATTEMPT CONSUMED. A sync must not create a repair packet and must not advance `repair_attempts`.\\n\\n  5. EVIDENCE. `_sync_branch` (`:1429-1525`) appends no ledger today; a sync that now remediates a RED check is a remediation decision on the same footing as `dispatch_repair` and must leave a durable record. Use an EXISTING ledger type \\u2014 registering a new one requires `.shiki/schemas/ledger.schema.json` and `validate_shiki.py:1892`'s set-equality binder to agree, and both are outside these locks.\\n\\n  6. WORKTREE RECONCILIATION. `_sync_branch` pushes from a throwaway detached checkout (`:1471`, `:1498`) and never updates the REGISTERED worktree's local branch ref; `ensure_physical_worktree` reuses an existing path without reconciling it. Today that is harmless because a sync is only followed by a merge. This change makes a sync be followed by a REPAIR, whose push (`:836`) is then a non-fast-forward failing with a misleading message. Fix it IN-FILE \\u2014 `scripts/shiki_runtime.py` must not be modified \\u2014 and do not use a destructive reset that could discard uncommitted work in the registered worktree.\\n\\n  7. BASE REF. `_sync_branch`'s `base: str = \\\"main\\\"` default (`:1429`) is hard-coded and the call site (`:1875`) passes nothing. This change promotes that path from rare to common, so resolve the base the way the rest of the module now does. Note the shape mismatch: `_default_base_ref` (`:772-795`) returns `origin/<name>` while `_sync_branch` builds `origin/{base}` \\u2014 do not produce `origin/origin/main`.\\n\\n  8. CLOSEOUT UNCHANGED. The closeout arm shares `_merge_or_sync` (`:260`) but its failing-check arm returns `stop_blocked`. `docs/adr/0012` pins that decision table verbatim. Assert the closeout decision table is unchanged so the ADR stays truthful.\\n\\nTESTS. `tests/test_branch_behind_sync.py:124` `test_behind_but_failing_check_is_not_synced` currently pins the old rule with the comment 'a failing check repairs, never syncs'; invert it and state the new rule and its conditions. Add pure-decision cases for: BEHIND + `needs_guardian` verdict -> stop_guardian; BEHIND + `blocked` -> stop_blocked; BEHIND + policy red + another red check -> NOT sync; BEHIND + pending -> wait_checks (unchanged); the counter at and past `MAX_BASE_SYNCS`; and BEHIND + repairable red under the cap -> sync_branch.\\n\\n`scripts/test_shiki_goal_loop.sh` MUST gain the end-to-end proof that a sync consumes no repair attempt, modelled on the identical assertion it already makes for the Guardian gate at `:408-417` (`find .shiki/repairs -name 'RP-*.json' | wc -l` before and after, asserted equal). The pure-fixture module cannot observe repair packets, so this is the only surface that can prove it.\\n\\n`tests/test_loop_branch_plumbing.py` imports `_commit_and_push_implementation` directly (`:22-27`); extend it to prove a repair dispatched AFTER a sync can still push.\\n\\nDo not introduce a `\\\"verdict\\\": \\\"...\\\"` string literal into `scripts/shiki_loop.py`: `tests/test_paired_invariants.py:546-548` regex-scans this file's source and set-equality-binds those literals to the code-review verdict vocabulary.\\n\\nREPRODUCIBILITY \\u2014 what this task removes, and what to change to get the same result.\\n  Symptom: a correct implementation is answered with three identical repair dispatches that change nothing, then `stop_guardian: required checks failed (MergeGate metadata check) and repair attempt limit reached`. The task is fine; the branch is merely stale.\\n  Measured 2026-08-05 on PR #288 (task T-20260805T040948888613Z-d4e830c9): the sole failing reason was 'PR must not delete base ledger file .shiki/ledger/L-20260805T052931749888Z-3017e935.json; state_class=append-only-evidence'. That file reached main via PR #287 after the branch forked. Each of the three repair packets diagnosed it as a loop-owned base sync and declined to act; the CCA returned verdict=blocked citing the same cause and quoting all three packets. A single `git merge origin/main` plus a push turned every required check green and the PR merged unchanged.\\n  Root cause: the only `sync_branch` emitter is reachable only when every required check is already green (`:101` docstring), while the red-check path returns `dispatch_repair` first (`:279-316`).\\n  Why it was written that way: `tests/test_branch_behind_sync.py:124` deliberately pinned 'a failing check repairs, never syncs'. The case where being behind is the CAUSE of the red check was not considered.\\n  Failure directions that matter, both measured against the source: syncing while the Guardian gate is red destroys a head-bound approval and can silently restore a defeated contract-approval carry; and an unbounded, unrecorded sync makes the repair limit dead code and trades a terminating stop for a livelock.\\n  Reproducible when: the #288 sequence \\u2014 BEHIND branch, red metadata check whose sole reason is a base file the branch never had \\u2014 is answered by one recorded sync and zero repair packets, while a `needs_guardian` verdict and a red policy gate on a BEHIND branch still stop exactly as today.\",\n.shiki/ledger/L-20260731T061853929925Z-c1a07ebd.json:9:  \"summary\": \"Pre-PR code-review verdict CLEAN for T-20260729T065622765968Z-234bf98b (independent read-only reviewer, code-review skill)\",\n.shiki/ledger/L-20260617T090810873217Z-9cb9e627.json:11:  \"summary\": \"goal_reconcile: register frozen-plan task T-20260617T090739480720Z-04c7582b ('Independent code-review verifier (read-only claude -p)') as planned for #140. Frozen-plan-bound; self-references PR #142.\",\n.shiki/plans/P-20260804T094832785739Z-b731b1e2.json:30:    \"code-review\"\n.shiki/ledger/L-20260807T044300628972Z-e5556cd1.json:9:  \"summary\": \"Pre-PR code-review verdict CLEAN for T-20260807T035659159912Z-f2e1c0b0 (independent read-only reviewer, code-review skill)\",\n.shiki/goals/G-20260806T014016270514Z-2d62ec7c.json:11:    \"A code-review verdict of blocking with zero findings is retried once, bounded, and never silently passes.\",\n.shiki/goals/G-20260806T014016270514Z-2d62ec7c.json:55:    \"code-review\"\n.shiki/ledger/L-20260730T111818664689Z-fb75da27.json:9:  \"summary\": \"Pre-PR code-review verdict CLEAN for T-20260730T110603197694Z-c903d404 (independent read-only reviewer, code-review skill)\",\n.shiki/ledger/L-20260729T223137574960Z-8b2fbb18.json:9:  \"summary\": \"Pre-PR code-review verdict CLEAN for T-20260729T065622770500Z-513839f0 (independent read-only reviewer, code-review skill)\",\n.shiki/goals/G-20260807T020527150479Z-4f8ffc4c.json:46:    \"code-review\"\n.shiki/ledger/L-20260617T114319943241Z-acbc2bf6.json:9:  \"summary\": \"Skill tdd: red-green for T3 independent code-review verifier \\u2014 failing tests first in tests/test_loop_code_review.py (stubbed reviewer: clean\\u2192code-review ledger+PR-12 section+PR opens; blocking\\u2192stop_blocked no PR; parse-failure\\u2192fail-closed), then the reviewer adapter + create_pr gate. Parallel Dynamic Workflow. Integrated onto T2 (create_pr order: code-review\\u2192TDD\\u2192commit/push); 252 unit + shell + validate green.\",\n.shiki/ledger/L-20260730T061729255405Z-461fc8f3.json:9:  \"summary\": \"Pre-PR code-review verdict CLEAN for T-20260730T060551622687Z-7bea1bc2 (independent read-only reviewer, code-review skill)\",\n.shiki/goals/G-20260618T051732213600Z-07292ac0.json:30:  \"outcome\": \"The autonomous loop self-drives a low-risk implementation task to auto-merge with no operator input: a headless claude -p writes a trivial test, the loop runs the code-review gate, the loop-observed TDD gate, commits+pushes, opens the PR, and auto-merges when MergeGate is green \\u2014 proving #140 G-D end to end on the real platform.\",\n.shiki/goals/G-20260618T051732213600Z-07292ac0.json:33:    \"code-review\"\n.shiki/goals/G-20260612T055133628076Z-d76238aa.json:8:    \"validate_shiki.py accepts code-review in required_skills and repair packets; KNOWN_SKILLS, repair-packet schema enum, and CLI choices are extended together\",\n.shiki/goals/G-20260612T055133628076Z-d76238aa.json:9:    \"register_task_from_plan and shiki start default required_skills include code-review, making the gate mandatory through existing MergeGate skill-evidence enforcement\",\n.shiki/goals/G-20260612T055133628076Z-d76238aa.json:10:    \"skill-gate.md, AGENTS.md skill table, checklists.md PR family, triage labels, and implementation-policy.md record the TDD -> code-review -> PR contract\",\n.shiki/goals/G-20260612T055133628076Z-d76238aa.json:24:  \"outcome\": \"code-review is a registered Skill Gate skill backed by skills/engineering/code-review/SKILL.md; new tasks require it by default so MergeGate's skill-evidence rule enforces the pre-PR gate; repair packets can route through code-review; checklists and docs record the contract\",\n.shiki/goals/G-20260612T055133628076Z-d76238aa.json:31:  \"title\": \"G-B: mandatory pre-PR code-review gate via Skill Gate and skill-evidence enforcement\"\n.shiki/ledger/L-20260803T060516389405Z-908b5075.json:9:  \"summary\": \"Pre-PR code-review verdict CLEAN for T-20260803T013839640984Z-2411811e (independent read-only reviewer, code-review skill)\",\n.shiki/ledger/L-20260617T114319948291Z-2e149644.json:11:  \"summary\": \"T3 task-registered for implementation: reference PR #145 (MergeGate self-reference). Independent read-only code-review verifier + ADR 0011 update. T3 -> review; no goal-complete. Integrated on T2's create_pr seam.\",\n.shiki/goals/G-20260801T040948350468Z-4a542e71.json:43:    \"code-review\"\n.shiki/ledger/L-20260612T095525697968Z-18241955.json:11:  \"summary\": \"Skill code-review pre-PR gate executed on the full task diff (multi-agent review, 3 dimensions, 33 agents): 8 major loop findings all fixed (repair limit read from target config.yaml capped at the schema limit, pre-PR repair packets fail closed instead of writing schema-invalid pr:0, deterministic repair-id from create_repair_packet instead of mtime, CCA rerun filtered by PR head SHA and count persisted on the task record, merge failure and fruitless unblock stop the loop instead of hot-looping, lock release on done so dependents unblock, latest-startedAt check aggregation so stale passes cannot mask failures, PR-evidence state sync committed to the task branch); governance findings fixed (ADR 0008 bypassPermissions deferral recorded honestly, Spec Amendment stop reworded as operator-initiated across 4 surfaces, CONTEXT.md Goal Loop glossary entry, triage risk-label semantics); 5 reviewer claims rejected as refuted\",\n.shiki/goals/G-20260618T110721108316Z-295dc805.json:20:    \"code-review\"\n.shiki/goals/G-20260618T093112319886Z-a4f768c5.json:3:    \"Captured shiki loop run JSONL transcript reaching outcome: complete, the merged smoke-task PR, and loop-owned tdd + code-review ledgers.\"\n.shiki/goals/G-20260618T093112319886Z-a4f768c5.json:17:  \"outcome\": \"shiki loop run self-drives a single low-risk task from ready to auto-merged done with zero manual pre-clearing: dispatch -> independent code-review -> loop-observed TDD -> commit/push -> PR -> checks+CCA green -> MergeGate -> auto-merge -> done -> goal complete.\",\n.shiki/goals/G-20260618T093112319886Z-a4f768c5.json:20:    \"code-review\"\n.shiki/goals/G-20260730T060551621252Z-6ab5c407.json:41:    \"code-review\"\n.shiki/goals/G-20260801T003238840135Z-14193adf.json:41:    \"code-review\"\n.shiki/goals/G-20260801T085710527889Z-5ec41eba.json:45:    \"code-review\"\n.shiki/ledger/L-20260801T071118232253Z-c14b1875.json:9:  \"summary\": \"Pre-PR code-review verdict CLEAN for T-20260801T040836631930Z-97030be9 (independent read-only reviewer, code-review skill)\",\n.shiki/goals/G-20260727T005125900810Z-077718f7.json:43:    \"code-review\"\n.shiki/goals/G-20260619T102811948334Z-902fc294.json:25:    \"code-review\"\n.shiki/goals/G-20260801T040836630398Z-f28167e5.json:44:    \"code-review\"\n.shiki/goals/G-20260801T041653272590Z-81f6f71d.json:41:    \"code-review\"\n.shiki/goals/G-20260804T002751220363Z-1eb2e0d4.json:53:    \"code-review\"\n.shiki/ledger/L-20260730T015628373632Z-0d6a04dc.json:9:  \"summary\": \"Pre-PR code-review verdict CLEAN for T-20260729T065622765413Z-a0055a9d (independent read-only reviewer, code-review skill)\",\n.shiki/goals/G-20260804T082618610097Z-21ffeb4e.json:49:    \"code-review\"\n.shiki/goals/G-20260804T094832859551Z-2d0958fd.json:40:    \"code-review\"\n.shiki/goals/G-20260807T035659158223Z-af599ebb.json:50:    \"code-review\"\n.shiki/goals/G-20260729T092307426018Z-584fb95e.json:41:    \"code-review\"\n.shiki/goals/G-20260806T034026770241Z-4f95b5bf.json:47:    \"code-review\"\n.shiki/ledger/L-20260625T070312949004Z-c9d6a9fc.json:4:    \"adversarial code-review workflow (2 lenses + verify)\",\n.shiki/ledger/L-20260625T070312949004Z-c9d6a9fc.json:10:  \"summary\": \"code-review: independent adversarial review (2 lenses: security/secret-handling, correctness/test-coverage) with per-finding verify. Two confirmed findings fixed: removed --no-verify (fail-open), redact token from surfaced errors. Re-ran all gates green.\",\n.shiki/ledger/L-20260613T043733359467Z-9e878bdf.json:13:  \"summary\": \"Skill code-review: pre-PR multi-agent adversarial security review gate for the external_ai_guardian_review authority diff. Two review rounds found and fixed 1 blocker + 4 major (round 1: self-approval bypass, cross-repo/PR/stale-head replay, comment/AI poisoning, fence scan) and 1 blocker + 2 major (round 2: loop laundering on mixed/race failure sets, review-path poisoning); round 3 confirmed 0 findings. Full suite green (88 unit + 31 shell + validate + lint + shellcheck). Evidence on PR #130.\",\n.shiki/goals/G-20260805T034218806676Z-4459a890.json:46:    \"code-review\"\n.shiki/goals/G-20260806T065143407702Z-8e3284ce.json:51:    \"code-review\"\n.shiki/goals/G-20260612T062701078809Z-266eceff.json:28:    \"code-review\"\n.shiki/goals/G-20260801T041653346645Z-7d402f1b.json:44:    \"code-review\"\n.shiki/goals/G-20260805T040948887161Z-8678e914.json:45:    \"code-review\"\n.shiki/ledger/L-20260806T024043211875Z-40fc6f79.json:9:  \"summary\": \"Pre-PR code-review verdict CLEAN for T-20260806T014016272306Z-a0cec6ca (independent read-only reviewer, code-review skill)\",\n.shiki/ledger/L-20260804T110205175205Z-9273e5e2.json:9:  \"summary\": \"Pre-PR code-review verdict BLOCKING for T-20260804T095105534398Z-c2ef0cfc (2 finding(s)); loop stops for diagnosis\",\n.shiki/goals/G-20260805T023333273780Z-cd2e69ec.json:44:    \"code-review\"\n.shiki/goals/G-20260618T044709324112Z-bc9365c6.json:22:    \"code-review\"\n.shiki/goals/G-20260613T095433721642Z-2e5e71fd.json:18:    \"code-review\"\n.shiki/goals/G-20260729T072437600805Z-2502768a.json:39:    \"code-review\"\n.shiki/goals/G-20260612T045923586452Z-8f7011ec.json:22:    \"No code-review wrapper skill or KNOWN_SKILLS changes (G-B scope)\",\n.shiki/goals/G-20260618T062311037333Z-0c9a58e7.json:24:    \"code-review\"\n.shiki/goals/G-20260618T031646698067Z-0084bd63.json:20:  \"outcome\": \"The loop's pre-PR code-review gate reliably parses a structured verdict from the real headless claude -p reviewer (via --output-format json + structured_output), so a low-risk task self-drives past the gate instead of failing closed on unparseable prose.\",\n.shiki/goals/G-20260618T031646698067Z-0084bd63.json:24:    \"code-review\"\n.shiki/goals/G-20260618T031646698067Z-0084bd63.json:28:  \"title\": \"Repair: autonomous code-review gate could not parse the reviewer verdict (#140 T5 prerequisite)\"\n.shiki/goals/G-20260618T081544127718Z-ea083794.json:33:    \"code-review\"\n.shiki/goals/G-20260613T035041766666Z-cfcc6734.json:28:    \"code-review\"\n.shiki/goals/G-20260622T045742684497Z-844f5fe1.json:3:    \"validate_shiki green; 335 unittests incl. tests/test_guardian_review.py (37) pass; shellcheck + module-boundaries pass; independent code-review gate found no defects\"\n.shiki/goals/G-20260622T045742684497Z-844f5fe1.json:25:    \"code-review\"\n.shiki/goals/G-20260729T065622761503Z-4479b0d3.json:48:    \"code-review\",\n.shiki/goals/G-20260625T065855440462Z-bae16ef5.json:3:    \"validate green; 358 unittests incl tests/test_secret_set_claude.py pass; code-review findings fixed\"\n.shiki/goals/G-20260625T065855440462Z-bae16ef5.json:20:    \"code-review\"\n.shiki/goals/G-20260805T075725959969Z-61d080fb.json:57:    \"code-review\"\n.shiki/goals/G-20260804T095105533205Z-224ce02d.json:47:    \"code-review\"\n.shiki/goals/G-20260805T025939374293Z-96ec6ced.json:43:    \"code-review\"\n.shiki/goals/G-20260612T152357704854Z-8a5508cf.json:44:    \"code-review\"\n.shiki/ledger/L-20260613T082220087639Z-9fc30cf6.json:12:  \"summary\": \"Skill code-review: pre-PR multi-agent adversarial review of the T2 diff against the guardian blocker list (secret persistence, hook fail-open, sentinel goal_id, evidence body copy, CCA raw-text, runner EXEC body, scorecard raw-memory source, stdout contract, suggestion status, autonomous distillation); 0 confirmed findings.\",\n.shiki/goals/G-20260728T103742822780Z-2514c606.json:41:    \"code-review\"\n.shiki/goals/G-20260617T031753970001Z-1de3b322.json:8:    \"A stubbed-runner + stubbed-reviewer contract test drives the full dispatch->tdd-evidence->code-review->commit/push->create_pr->merge->done path including the failure->repair branch.\",\n.shiki/goals/G-20260617T031753970001Z-1de3b322.json:17:      \"Q2: read-only claude -p code-review verifier, structured verdict, fail-closed, blocking->repair loop.\",\n.shiki/goals/G-20260617T031753970001Z-1de3b322.json:36:  \"outcome\": \"shiki loop run self-drives a low/medium-risk task from ready to auto-merged done with zero manual pre-clearing: implement -> loop-observed TDD evidence -> independent code-review -> commit/push -> PR -> checks+CCA pass -> MergeGate green -> auto-merge -> done -> goal complete.\",\n.shiki/goals/G-20260617T031753970001Z-1de3b322.json:39:    \"code-review\"\n.shiki/goals/G-20260731T040711236459Z-50972900.json:40:    \"code-review\"\n.shiki/ledger/L-20260807T001650645647Z-8546f854.json:9:  \"summary\": \"Pre-PR code-review verdict CLEAN for T-20260806T065143409817Z-dbdae1cf (independent read-only reviewer, code-review skill)\",\n.shiki/goals/G-20260730T110603196220Z-c50d61b5.json:42:    \"code-review\"\n.shiki/goals/G-20260801T041653421243Z-2d98487f.json:44:    \"code-review\"\n.shiki/ledger/L-20260618T021029671230Z-6ebbc168.json:11:  \"summary\": \"Repair (external AI guardian findings T5-E2E-ORDER-001 + T5-E2E-REPAIR-002, BLOCKING): the T5 proof's CLAIMED order (TDD->code-review) and repair path disagreed with the IMPLEMENTED loop. Fixes (T5 locks: test+docs only \\u2014 shiki_loop.py is out of T5 scope): (1) corrected the order claim everywhere to the implemented (a) code-review -> (b) TDD -> (c) commit/push, and added an e2e ASSERTION over the task's ledger_evidence INDEX (code-review ledger index < tdd-gate ledger index; matched on the 'Loop-observed TDD gate' summary so the dispatch runner EXEC ledger is not mistaken for it) so a future reorder breaks the test; (2) aligned the acceptance doc: a non-clean PRE-PR review fail-closes to stop_blocked (no PR to anchor a repair); the bounded repair loop is for POST-PR required-check failures (PATH 2). Full suite + e2e green.\",\n.shiki/goals/G-20260731T004111130646Z-0dbace30.json:41:    \"code-review\"\n.shiki/goals/G-20260807T073307219752Z-1ee1ed68.json:29:  \"id\": \"G-20260807T073307219752Z-1ee1ed68\",\n.shiki/goals/G-20260807T073307219752Z-1ee1ed68.json:49:    \"code-review\"\n.shiki/ledger/L-20260803T095019401492Z-14af68bd.json:9:  \"summary\": \"Pre-PR code-review verdict CLEAN for T-20260801T040948352877Z-eea16def (independent read-only reviewer, code-review skill)\",\n.shiki/ledger/L-20260612T085336893451Z-7cb6ac98.json:12:  \"summary\": \"Skill code-review pre-PR gate executed on the full task diff (multi-agent review, 3 dimensions, 24 agents; 11 verifications interrupted by session limit were re-verified locally by the implementer): 1 blocker (shiki start fabricated Spec Freeze approval contradicting ADR 0009 \\u2014 fixed with an explicit approval gate: --approve-spec-freeze flag, answers key, interactive question, honest source string, negative test), 2 major governance gaps fixed (CONTRIBUTING.md operating model, decision-control.md Spec Freeze/Amendment ownership + non-delegable entry), plus ledger-evidence and status-transition findings fixed in this state; amendment recording mechanism documented in /shiki Goal mode\",\n.shiki/goals/G-20260803T013839638288Z-eebb39bf.json:47:    \"code-review\"\n.shiki/goals/G-20260806T084356214001Z-8198f059.json:46:    \"code-review\"\n.shiki/ledger/L-20260806T082023276867Z-ecdd22d6.json:9:  \"summary\": \"Pre-PR code-review verdict CLEAN for T-20260806T065143409817Z-dbdae1cf (independent read-only reviewer, code-review skill)\",\n.shiki/goals/G-20260731T031357585622Z-8d094bd1.json:42:    \"code-review\"\n.shiki/goals/G-20260612T091302245408Z-d0c43361.json:28:    \"code-review\"\n.shiki/ledger/L-20260803T074220707624Z-42f5879e.json:9:  \"summary\": \"Pre-PR code-review verdict CLEAN for T-20260803T013839641506Z-d11bb45f (independent read-only reviewer, code-review skill)\",\n.shiki/goals/G-20260731T045548842404Z-dac91fc1.json:40:    \"code-review\"\n.shiki/ledger/L-20260804T013600940688Z-1aa7e5c5.json:9:  \"summary\": \"Pre-PR code-review verdict CLEAN for T-20260804T002751221801Z-a914603f (independent read-only reviewer, code-review skill)\",\n.shiki/ledger/L-20260618T014757188326Z-30eae75a.json:9:  \"summary\": \"Skill tdd: T5 E2E self-drive contract test (scripts/test_shiki_loop_e2e.sh) \\u2014 drives the FULL integrated loop with stubbed runner+reviewer+gh: dispatch -> loop-observed TDD gate (T2) -> independent code-review verifier (T3) -> commit/push (T1) -> create_pr -> green checks -> auto-merge -> done -> goal complete, plus PATH 2 failure->repair. Asserts the loop-owned tdd + code-review ledgers on the merged task. + tests/test_loop_e2e_skeleton.py + docs/agents/autonomous-loop-e2e-acceptance.md (the live `shiki loop run` acceptance gate). Full suite green.\",\n.shiki/ledger/L-20260803T073426323656Z-e1df2a7f.json:9:  \"summary\": \"Pre-PR code-review verdict CLEAN for T-20260803T013839641506Z-d11bb45f (independent read-only reviewer, code-review skill)\",\n.shiki/ledger/L-20260618T014757192737Z-56d8cf05.json:9:  \"summary\": \"Skill code-review: parallel-workflow independent verify of T5 = SOUND (skeleton honestly scoped). Completed now that T2/T3/T4 are merged; the e2e test proves the autonomous loop self-drives an implementation task to auto-merge end-to-end (the #140 goal). 0 findings.\",\n.shiki/ledger/L-20260803T055349417906Z-fd5a47c7.json:9:  \"summary\": \"Pre-PR code-review verdict BLOCKING for T-20260803T013839640984Z-2411811e (2 finding(s)); loop stops for diagnosis\",\n.shiki/ledger/L-20260806T105734169402Z-29f8aa17.json:9:  \"summary\": \"Pre-PR code-review verdict CLEAN for T-20260806T065143409817Z-dbdae1cf (independent read-only reviewer, code-review skill)\",\n.shiki/ledger/L-20260731T075339376095Z-371e1689.json:9:  \"summary\": \"Pre-PR code-review verdict CLEAN for T-20260729T065622763684Z-ee01f3c4 (independent read-only reviewer, code-review skill)\",\n.shiki/ledger/L-20260615T124011083774Z-5f942ca1.json:9:  \"summary\": \"Skill code-review: pre-PR adversarial multi-agent review (5 dimensions: selector correctness, handoff/cache, acceptance/tests, safety, docs/scope). Found 1 blocking (missing-goal crash in write_task_handoff \\u2014 ShikiError escaped the goal-load guard), 3 major (stable-sort tiebreak unproven; revoked-alone exclusion unproven; failure-tolerance gap), and minors (symmetric normalization, doc symbol, extra tests). All fixed and regression-tested; 0 confirmed findings remain. Evidence: review against main...e150ca6.\",\n.shiki/ledger/L-20260613T015343577142Z-805d0624.json:11:  \"summary\": \"Skill code-review pre-PR gate executed on the full T1 diff (multi-agent, 3 dimensions, 13 agents): 3 blockers fixed (memory ledger goal_id='memory' sentinel poisoned validate_ledger -> capture now requires+validates an existing anchoring goal_id and rejects free-text task_id, fail-open; the migrations applied_count and shellcheck ls->find fixes were unstaged at review -> re-staged), 1 major (capture did not verify goal existence -> same fix), 2 minor (supersede_memory now re-validates before save; standalone `memory supersede` CLI subcommand added); 2 reviewer claims refuted (B4 auto-merge exclusion already enforced by high-risk guardian gate; revoke/supersede ledger types intentional). Contract-sync core verified clean (manifest/contracts order-exact, README byte-match, migration applied, MEM prefix, ledger enum). Implementation continued in the interactive session under the goal's Guardian approval after the headless runner hit the Claude subscription session limit (EXEC-19d73fb0); the runner's partial shiki_memory.py validation block and test_memory_promotion.py were adopted and extended.\",\n.shiki/ledger/L-20260622T051234314774Z-76f7bdd0.json:4:    \"adversarial code-review workflow (3 lenses + verify)\",\n.shiki/ledger/L-20260622T051234314774Z-76f7bdd0.json:10:  \"summary\": \"code-review: independent multi-lens adversarial review (correctness, security/identity-boundary, contract/test-coverage) with per-finding adversarial verification (26 agents). Verdict clean; role boundary + fail-closed approval contract + packet-not-approval-evidence verified; 3 minor/nit items addressed.\",\n.shiki/ledger/L-20260805T015932323157Z-e890daf5.json:9:  \"summary\": \"Pre-PR code-review verdict CLEAN for T-20260801T041653274543Z-6df3af7e (independent read-only reviewer, code-review skill)\",\n.shiki/ledger/L-20260612T061938064648Z-2370dfe9.json:9:  \"summary\": \"G-B complete: code-review is a Skill Gate skill and the default pre-PR implementer gate, enforced by MergeGate skill evidence; merged as PR #122\",\n.shiki/ledger/L-20260731T051047277832Z-63e7ab70.json:9:  \"summary\": \"Pre-PR code-review verdict CLEAN for T-20260731T045548843829Z-ee2deb90 (independent read-only reviewer, code-review skill)\",\n.shiki/ledger/L-20260618T094733926165Z-baccf532.json:13:  \"summary\": \"T5 live acceptance gate: shiki loop run self-drove smoke goal G-20260618T093112319886Z-a4f768c5 / task T-20260618T093119138788Z-867546d0 to auto-merge (PR #160) + goal complete (PR #161 closeout, ADR 0012) with zero manual pre-clearing; transcript outcome=complete. Loop-owned tdd + code-review ledgers recorded on main. Satisfies CCA repair RP-20260618T024022000000Z-a3b7c2d4 (failing items CCA-02, CCA-09).\",\n.shiki/ledger/L-20260729T022202323204Z-9ee53f87.json:9:  \"summary\": \"Pre-PR code-review verdict BLOCKING for T-20260728T103742824401Z-f198cd24 (3 finding(s)); loop stops for diagnosis\",\n.shiki/ledger/L-20260617T034222728376Z-4f3f7e65.json:9:  \"summary\": \"Skill code-review: high-effort pre-PR review (line-by-line + removed-behavior + cross-file). Found 2 CONFIRMED loop-crashing bugs \\u2014 (1) create_pr ran gh pr create unconditionally even when the impl was not pushed (check=True -> ShikiError crashes the loop) and the already-committed runner case never pushed; (2) _evidence_relatives_for_task caught (OSError,ValueError) but read_json raises ShikiError on a non-dict ledger. Both fixed (push-if-ahead + create_pr stop_blocked guard + except Exception) and regression-tested. 0 findings remain.\",\n.shiki/ledger/L-20260615T124011083875Z-d5de6168.json:11:  \"summary\": \"T3 task-registered: reference PR #137 for the MergeGate self-reference check. Consult injection + docs implementation; required skills tdd and code-review exercised. T3 moves planned->review (not done); no goal-complete; no T1/T2 change.\",\n.shiki/ledger/L-20260729T113336586253Z-4893cacb.json:9:  \"summary\": \"Pre-PR code-review verdict CLEAN for T-20260729T065622769632Z-9ee6b8e0 (independent read-only reviewer, code-review skill)\",\n.shiki/ledger/L-20260729T015140199174Z-70beb40f.json:9:  \"summary\": \"Pre-PR code-review verdict BLOCKING for T-20260728T103742824401Z-f198cd24 (3 finding(s)); loop stops for diagnosis\",\n.shiki/ledger/L-20260804T142532928021Z-1dc8cbb7.json:9:  \"summary\": \"Pre-PR code-review verdict CLEAN for T-20260804T095105535495Z-22a51036 (independent read-only reviewer, code-review skill)\",\n.shiki/ledger/L-20260618T063113497293Z-a8e4aaaa.json:9:  \"summary\": \"code-review: dogfooded the read-only reviewer adapter on the full diff -> verdict clean (findings non-blocking: PR-12 body section [ADDRESSED by adding ## Pre-PR code review], broad .shiki/** lock + mirror-sync [environmental/one-goal model], interruption note [by design]). SEPARATELY an 8-agent adversarial verification workflow CONFIRMED + FIXED real defects the stubbed contract test missed: the closeout branch did not repoint expected_pr (would defeat MergeGate), the second push was unchecked, the effector could raise, and goal_complete left the coordinator diverged from main. All fixed + re-tested.\",\n.shiki/ledger/L-20260804T104012096698Z-4bd63940.json:9:  \"summary\": \"Pre-PR code-review verdict CLEAN for T-20260804T094832861501Z-78871209 (independent read-only reviewer, code-review skill)\",\n.shiki/ledger/L-20260729T083101993600Z-bf0aa57d.json:9:  \"summary\": \"Pre-PR code-review verdict CLEAN for T-20260729T072437602381Z-cd8cdfd8 (independent read-only reviewer, code-review skill)\",\n.shiki/ledger/L-20260612T055157606860Z-e37dd972.json:9:  \"summary\": \"Task registered: Register code-review as a Skill Gate skill and make it the default pre-PR implementer gate\",\n.shiki/ledger/L-20260618T045513327354Z-85f8a408.json:10:  \"summary\": \"code-review: dogfooded the fixed read-only reviewer adapter on `git diff main...HEAD`. Structured verdict = clean. One non-blocking medium finding (the safety comment over-claimed 'always safe', omitting that the synced goal/dag/lock must also be covered by the task's declared locks for files_outside_locks, mergegate_check.py:1357) -- ADDRESSED by tightening the comment to note the path:.shiki/** loop-task coverage dependency. Satisfies acceptance check 1 (a real claude -p reviewer parses to a valid verdict).\",\n.shiki/ledger/L-20260804T111321949955Z-9a72a2dd.json:9:  \"summary\": \"Pre-PR code-review verdict CLEAN for T-20260804T095105534398Z-c2ef0cfc (independent read-only reviewer, code-review skill)\",\n.shiki/ledger/L-20260805T082725959839Z-949f599c.json:9:  \"summary\": \"Pre-PR code-review verdict CLEAN for T-20260801T041653422672Z-c4815d7a (independent read-only reviewer, code-review skill)\",\n.shiki/ledger/L-20260807T052423891861Z-4a10b0d1.json:9:  \"summary\": \"Pre-PR code-review verdict CLEAN for T-20260807T035659160500Z-ba479979 (independent read-only reviewer, code-review skill)\",\n.shiki/ledger/L-20260731T092301921048Z-2b851dcd.json:9:  \"summary\": \"Pre-PR code-review verdict CLEAN for T-20260729T065622764296Z-48021407 (independent read-only reviewer, code-review skill)\",\n.shiki/ledger/L-20260618T040603981209Z-2757d8d3.json:9:  \"summary\": \"Reviewer structured-output emission + parsing fixed and merged in PR #148; the autonomous code-review gate now parses a real claude -p verdict via --output-format json + structured_output. Unblocks the #140 T5 live self-drive.\",\n.shiki/ledger/L-20260801T014511493762Z-e48f26c3.json:9:  \"summary\": \"Pre-PR code-review verdict BLOCKING for T-20260801T003238841526Z-9f3f6f63 (2 finding(s)); loop stops for diagnosis\",\n.shiki/ledger/L-20260731T012825537931Z-20504848.json:9:  \"summary\": \"Pre-PR code-review verdict CLEAN for T-20260731T004111132162Z-bfcf2c9e (independent read-only reviewer, code-review skill)\",\n.shiki/ledger/L-20260618T032206018471Z-78648909.json:9:  \"summary\": \"diagnose: the live shiki loop self-drive (smoke task T-20260618T025401916227Z-fbc91ead) stopped at the pre-PR code-review gate with 'pre-PR code review did not pass (reviewer verdict could not be parsed; failing closed)'. Root cause: REVIEWER_ADAPTER omitted --output-format json. Without it, headless `claude -p --json-schema` returns natural-language prose (e.g. '**Verdict: clean** -- ...'), not the schema object, so parse_code_review_verdict found no JSON and (correctly) failed closed. Reproduced directly: prose on stdout, plus a spurious stderr 'Permission deny rule MultiEdit matches no known tool'.\",\n.shiki/ledger/L-20260807T024642146852Z-c16b8db7.json:9:  \"summary\": \"Pre-PR code-review verdict CLEAN for T-20260807T020527152153Z-97fe21d1 (independent read-only reviewer, code-review skill)\",\n.shiki/ledger/L-20260612T061357037912Z-fc64fcb4.json:4:    \"skills/engineering/code-review/SKILL.md\",\n.shiki/ledger/L-20260612T061357037912Z-fc64fcb4.json:10:  \"summary\": \"Skill code-review pre-PR gate executed on the full task diff (multi-agent review, 3 dimensions, 33 agents): 2 blockers (ledger-evidence gaps in this task's own state), 4 major doc-sync omissions (completion-check-agent, codex-handoff x2, AGENTS/CLAUDE repair lists), 7 minor findings (skills README, PR-12 exception cross-ref, ADR 0008 delivery note, SKILL.md effort wording, implementation-policy Phase 7 + diagram, CONTEXT.md glossary, codex-task-handoff prompt) \\u2014 all applied as fixes on the task branch; 2 findings rejected as refuted (tmp-file cleanup, lock-scope objection resolved via recorded lock amendment instead)\",\n.shiki/ledger/L-20260729T101723632516Z-98987819.json:9:  \"summary\": \"Pre-PR code-review verdict CLEAN for T-20260729T092307427377Z-c8531f7e (independent read-only reviewer, code-review skill)\",\n.shiki/ledger/L-20260618T051909235168Z-9ae6af00.json:9:  \"summary\": \"Pre-PR code-review verdict CLEAN for T-20260618T051732214442Z-bae28ee3 (independent read-only reviewer, code-review skill)\",\n.shiki/ledger/L-20260807T014350473964Z-51dc9526.json:9:  \"summary\": \"Pre-PR code-review verdict CLEAN for T-20260806T084356215508Z-7a7dd694 (independent read-only reviewer, code-review skill)\",\n.shiki/ledger/L-20260804T074102813063Z-61690089.json:9:  \"summary\": \"Pre-PR code-review verdict CLEAN for T-20260801T040836632579Z-2cde7018 (independent read-only reviewer, code-review skill)\",\n.shiki/ledger/L-20260612T055133628737Z-55b7d606.json:9:  \"summary\": \"Goal registered: G-B: mandatory pre-PR code-review gate via Skill Gate and skill-evidence enforcement\",\n.shiki/ledger/L-20260805T043457937011Z-ff78daaf.json:9:  \"summary\": \"Pre-PR code-review verdict CLEAN for T-20260805T025939375698Z-94a2fa82 (independent read-only reviewer, code-review skill)\",\n.shiki/ledger/L-20260613T095433722299Z-464236e3.json:10:  \"summary\": \"Skill code-review: two-round multi-agent adversarial pre-PR review of the scope-relaxation diff against the guardian blocker list found 1 blocker + 3 major + minors (round 1); all fixed; round 2 confirmed 0 findings.\",\n.shiki/ledger/L-20260618T022804808931Z-4f323916.json:11:  \"summary\": \"Repair 2 (external AI guardian findings T5-E2E-STALE-CONTRACT-003 + T5-E2E-META-TEST-004, BLOCKING): the executable assertions were fixed but stale 'SKELETON / T2-T3 NOT merged / TODO / dispatch_repair-for-blocking-review' text remained in the e2e script's header, stub comments, PATH 2 line, and final echo, and tests/test_loop_e2e_skeleton.py still asserted the obsolete skeleton/TODO state. Fixes (T5 locks): rewrote the e2e script header + comments to the final implemented contract (dispatch -> code-review -> TDD -> commit/push -> create_pr -> auto-merge; pre-PR non-clean review stop_blocks, POST-PR check failures repair); removed all SKELETON/TODO text (0 remaining); replaced the skeleton meta-test with tests/test_loop_e2e_contract.py asserting the FINAL contract (review-before-TDD order, ledger-order assertion present, pre-PR stop_blocked vs post-PR repair, no placeholder text). Full suite + e2e green.\",\n.shiki/ledger/L-20260803T011818394523Z-f8c2774a.json:9:  \"summary\": \"Pre-PR code-review verdict CLEAN for T-20260801T085710529159Z-575d4080 (independent read-only reviewer, code-review skill)\",\n.shiki/ledger/L-20260618T032206018961Z-da44606a.json:10:  \"summary\": \"code-review: ran the fixed read-only reviewer adapter on `git diff main...HEAD` as the pre-PR code-review gate (dogfooding the fix). Structured verdict = clean, findings = [], exit 0, no stderr warning. This also satisfies acceptance check 3 -- a real `claude -p` reviewer run parses to a valid verdict via parse_code_review_verdict (result envelope structured_output field).\",\n.shiki/ledger/L-20260730T020027929055Z-5bc0f04d.json:9:  \"summary\": \"Pre-PR code-review verdict CLEAN for T-20260729T065622769184Z-628504fe (independent read-only reviewer, code-review skill)\",\n.shiki/ledger/L-20260727T022618065297Z-525333c7.json:9:  \"summary\": \"Pre-PR code-review verdict CLEAN for T-20260727T005125902419Z-6c2db440 (independent read-only reviewer, code-review skill)\",\n.shiki/ledger/L-20260805T013017824712Z-8981c997.json:9:  \"summary\": \"Pre-PR code-review verdict BLOCKING for T-20260801T041653274543Z-6df3af7e (2 finding(s)); loop stops for diagnosis\",\n.shiki/ledger/L-20260801T032347185967Z-2566db58.json:9:  \"summary\": \"Pre-PR code-review verdict CLEAN for T-20260729T065622768738Z-fa978e4a (independent read-only reviewer, code-review skill)\",\n.shiki/ledger/L-20260801T020959027750Z-2a58f3f9.json:9:  \"summary\": \"Pre-PR code-review verdict CLEAN for T-20260729T065622767723Z-f138eccf (independent read-only reviewer, code-review skill)\",\n\nexec\n/bin/zsh -lc 'codex --version; codex login status; python3 --version; node --version; jq --version' in /private/tmp/shiki-adr-collision.pa0KwY/.worktrees/shiki-t-20260807t073307221254z-2d22d654-introduc\nexec\n/bin/zsh -lc 'git status --short' in /private/tmp/shiki-adr-collision.pa0KwY/.worktrees/shiki-t-20260807t073307221254z-2d22d654-introduc\n2026-08-07T08:04:00.155740Z ERROR codex_models_manager::manager: failed to renew cache TTL: missing field `base_instructions` at line 94 column 5\n succeeded in 106ms:\n M .shiki/tasks/T-20260807T073307221254Z-2d22d654.json\n?? .shiki/locks/T-20260807T073307221254Z-2d22d654.json\n\n succeeded in 156ms:\nWARNING: proceeding, even though we could not create PATH aliases: Operation not permitted (os error 1)\nLogged in using ChatGPT\nPython 3.14.6\nv26.5.0\njq-1.8.1\n\nexec\n/bin/zsh -lc \"sed -n '1,260p' .shiki/goals/G-20260807T073307219752Z-1ee1ed68.json\" in /private/tmp/shiki-adr-collision.pa0KwY/.worktrees/shiki-t-20260807t073307221254z-2d22d654-introduc\n succeeded in 0ms:\n{\n  \"acceptance_evidence\": [\n    \"GitHub Issue records the goal.\",\n    \"Task DAG is registered in .shiki/dag.\",\n    \"CCA verdict and MergeGate evidence are recorded before completion.\"\n  ],\n  \"completion_conditions\": [\n    \"Shiki platform decisions are docs/adr/SADR-NNNN-<slug>.md and live platform citations resolve through SADR.\",\n    \"Target repositories keep docs/adr/NNNN-<slug>.md product decisions without ambiguity or forced renumbering.\",\n    \"Fresh installs and stamped upgrades preserve target ADR bytes and pass validation.\",\n    \"A legacy Shiki ADR is removed only when its install-stamp path and digest match, otherwise the upgrade stops before the first write.\",\n    \"Unit, shell, validation, fresh-install, and upgrade suites pass in CI on the exact PR head.\"\n  ],\n  \"created_at\": \"2026-08-07T07:33:07+00:00\",\n  \"github_issue\": null,\n  \"grill_with_docs\": {\n    \"decisions\": [\n      \"Use SADR for Shiki platform decisions and retain ADR for target product decisions. Collision-only refusal leaves targets unupgradable; per-target renumbering destroys stable platform identity.\",\n      \"Rename existing platform records to SADR-0001 through SADR-0018 and record the decision as SADR-0019. Update current citations, resolver code, fixtures, packaging, and operator guidance together.\",\n      \"Treat frozen .shiki evidence as history and do not mechanically rewrite completed Goal, Plan, Task, Ledger, Report, Handoff, or Runner prose.\",\n      \"Plan all legacy removals before any write. A present legacy path is deletable only when a readable existing stamp contains the path and its digest equals the current file digest.\",\n      \"Absent, unreadable, malformed, incomplete, or mismatching ownership evidence fails closed with all blockers and a byte-identical target tree.\",\n      \"After clean preflight, delete only proven legacy paths, copy SADRs, and refresh the stamp with SADR digests and no removed legacy path.\",\n      \"Keep namespace, resolver, shipped templates, and atomic cleanup in one vertical slice because no partial branch is independently safe and valid.\"\n    ],\n    \"source\": \"Architecture review and Requirements Definition for GitHub Goal #322 on 2026-08-07 after a real CubeShare upgrade produced ambiguous ADR references and a red full suite.\",\n    \"status\": \"complete\"\n  },\n  \"id\": \"G-20260807T073307219752Z-1ee1ed68\",\n  \"ledger_evidence\": [\n    \"L-20260807T073307220560Z-fc2d907a\"\n  ],\n  \"non_goals\": [\n    \"Renumbering or rewriting target product ADRs.\",\n    \"Rewriting historical .shiki evidence that accurately records the former namespace.\",\n    \"Deleting a target-owned file without exact install-stamp ownership evidence.\",\n    \"Changing Guardian, Contract Approval, lock, CCA, or MergeGate semantics.\",\n    \"Repairing the separate installer defect that overwrites a target .gitignore during upgrade.\"\n  ],\n  \"outcome\": \"Shiki platform decisions use a collision-free SADR namespace, while target repositories keep their existing ADR namespace. Fresh installs and stamped upgrades remain deterministic, and an upgrade removes a legacy numeric Shiki ADR only when its recorded install-stamp digest proves that the file is still platform-owned and unchanged.\",\n  \"required_skills\": [\n    \"grill-with-docs\",\n    \"improve-codebase-architecture\",\n    \"to-prd\",\n    \"to-issues\",\n    \"triage\",\n    \"diagnose\",\n    \"tdd\",\n    \"code-review\"\n  ],\n  \"risk_level\": \"high\",\n  \"source_plan\": \"P-20260807T073304061131Z-9dfd1fc8\",\n  \"status\": \"planned\",\n  \"title\": \"Separate Shiki platform decisions from target ADRs with a SADR namespace\"\n}\n\nexec\n/bin/zsh -lc \"sed -n '1,260p' .shiki/tasks/T-20260807T073307221254Z-2d22d654.json\" in /private/tmp/shiki-adr-collision.pa0KwY/.worktrees/shiki-t-20260807t073307221254z-2d22d654-introduc\n succeeded in 0ms:\n{\n  \"acceptance_checks\": [\n    \"The platform tree has unique SADR-0001 through SADR-0019, no legacy numeric Shiki platform ADR file, and every live SADR citation resolves exactly once.\",\n    \"The validator resolves target ADR NNNN to docs/adr/NNNN-*.md and platform SADR NNNN to docs/adr/SADR-NNNN-*.md; fixtures prove both namespaces may reuse a number.\",\n    \"Current platform references in root governance docs, docs, scripts, workflows/prompts, .codex/skills/shiki/SKILL.md, and packaging use SADR or explicit generic target-ADR examples. Historical .shiki records remain unchanged except this Goal's lifecycle evidence.\",\n    \"A fresh-install fixture with product ADRs 0013-0018 preserves their bytes, installs SADR-0001 through SADR-0019, stamps SADR paths, and validates.\",\n    \"A forced-upgrade fixture with product collisions plus matching stamped legacy Shiki ADRs deletes only legacy platform paths, preserves product ADR bytes, installs SADRs, and removes legacy paths from the refreshed stamp.\",\n    \"Parameterized absent, unreadable/malformed, missing-digest, and digest-mismatch cases report all blockers before ShikiError; recursive snapshots prove no filesystem byte changed.\",\n    \"No legacy path means an absent stamp does not block; a non-force install does not silently delete existing files.\",\n    \"Red-green-refactor and a structured code-review verdict are recorded, with all blocking findings repaired and re-reviewed.\",\n    \"python3 -m unittest discover -s tests, control-plane and adversarial shell suites, validate_shiki, and fresh-install/upgrade tests pass locally and in CI on the exact head.\"\n  ],\n  \"assigned_runtime\": \"codex\",\n  \"cca_checklist_profile\": [\n    \"CCA-01\",\n    \"CCA-02\",\n    \"CCA-03\",\n    \"CCA-04\",\n    \"CCA-05\",\n    \"CCA-06\",\n    \"CCA-07\",\n    \"CCA-08\",\n    \"CCA-09\",\n    \"CCA-10\",\n    \"CCA-11\",\n    \"CCA-12\"\n  ],\n  \"dependencies\": [],\n  \"expected_branch\": \"shiki/t-20260807t073307221254z-2d22d654-introduce-sadr-and-migrate-installs-without-touc\",\n  \"expected_pr\": null,\n  \"github_issue\": 322,\n  \"goal_id\": \"G-20260807T073307219752Z-1ee1ed68\",\n  \"id\": \"T-20260807T073307221254Z-2d22d654\",\n  \"ledger_evidence\": [\n    \"L-20260807T073307221361Z-b3c3a718\",\n    \"L-20260807T080107948254Z-61f24da0\",\n    \"L-20260807T080228312210Z-07ac626e\",\n    \"L-20260807T080228575038Z-d574f629\",\n    \"L-20260807T080256330734Z-5bf40cb6\"\n  ],\n  \"locks\": [\n    \"path:.codex/**\",\n    \"path:.github/**\",\n    \"path:.shiki/**\",\n    \"path:AGENTS.md\",\n    \"path:CLAUDE.md\",\n    \"path:CONTEXT.md\",\n    \"path:CONTRIBUTING.md\",\n    \"path:SYSTEM_PROMPT.md\",\n    \"path:docs/**\",\n    \"path:pyproject.toml\",\n    \"path:scripts/**\",\n    \"path:tests/**\"\n  ],\n  \"non_goals\": [\n    \"Mutating target product ADRs.\",\n    \"Rewriting historical .shiki evidence records.\",\n    \"Deleting any legacy path without exact stamp path and digest match.\",\n    \"Changing approval, CCA, MergeGate, lock, runtime, or repair semantics.\",\n    \"Fixing the separate .gitignore overwrite defect.\"\n  ],\n  \"required_skills\": [\n    \"diagnose\",\n    \"tdd\",\n    \"code-review\"\n  ],\n  \"risk_level\": \"high\",\n  \"scope\": \"Rename Shiki platform decision records to SADR-0001 through SADR-0018; add SADR-0019; update current citations, packaging metadata, documentation, workflows, prompts, repo-local skill guidance, and the reference validator for separate ADR/SADR namespaces; add an installer preflight that inventories legacy numeric Shiki ADR paths and authorizes deletion only from matching existing install-stamp path digests before any write; then apply authorized cleanup, install SADRs, refresh the stamp, and prove fresh-install plus CubeShare-shaped collision-upgrade behavior with TDD.\",\n  \"status\": \"running\",\n  \"test_command\": \"python3 -m unittest discover -s tests && bash scripts/test_shiki_control_plane.sh && bash scripts/test_shiki_adversarial_state.sh && python3 scripts/validate_shiki.py --target .\",\n  \"title\": \"Introduce SADR and migrate installs without touching product ADRs\"\n}\n\nexec\n/bin/zsh -lc \"sed -n '1,260p' .shiki/handoffs/T-20260807T073307221254Z-2d22d654-task.md 2>/dev/null || true\" in /private/tmp/shiki-adr-collision.pa0KwY/.worktrees/shiki-t-20260807t073307221254z-2d22d654-introduc\n succeeded in 0ms:\nexec\n/bin/zsh -lc \"rg --files | rg '(\"'^|/)skills/engineering/code-review/|code-review.*SKILL'\"'\" in /private/tmp/shiki-adr-collision.pa0KwY/.worktrees/shiki-t-20260807t073307221254z-2d22d654-introduc\n succeeded in 0ms:\nskills/engineering/code-review/SKILL.md\n\nexec\n/bin/zsh -lc \"rg -l 'T-20260807T073307221254Z-2d22d654' .shiki/plans .shiki/dag .shiki/contracts .shiki/handoffs 2>/dev/null\" in /private/tmp/shiki-adr-collision.pa0KwY/.worktrees/shiki-t-20260807t073307221254z-2d22d654-introduc\n exited 2 in 0ms:\n.shiki/dag/G-20260807T073307219752Z-1ee1ed68.json\n\n2026-08-07T08:04:08.486823Z ERROR codex_models_manager::manager: failed to renew cache TTL: missing field `base_instructions` at line 94 column 5\nexec\n/bin/zsh -lc \"wc -l skills/engineering/code-review/SKILL.md && sed -n '1,400p' skills/engineering/code-review/SKILL.md\" in /private/tmp/shiki-adr-collision.pa0KwY/.worktrees/shiki-t-20260807t073307221254z-2d22d654-introduc\n succeeded in 0ms:\n      58 skills/engineering/code-review/SKILL.md\n---\nname: code-review\ndescription: Mandatory pre-PR self-review gate for Shiki implementers. Run after the TDD loop is green and before creating or updating a PR. Reviews the task diff for correctness bugs and reuse/simplification/efficiency cleanups, applies fixes on the task branch, and records durable evidence.\n---\n\n# Code Review (Pre-PR Implementer Gate)\n\nThis is the Shiki wrapper for the implementer's native code-review capability.\nIn Claude Code it is satisfied by the built-in `/code-review` skill; another\nAgent Runtime may satisfy it with an equivalent diff review that produces the\nsame evidence.\n\n## Position In The Loop\n\n```\ntdd (red-green-refactor, all checks green)\n  -> code-review (this gate: review the full task diff, apply fixes)\n  -> re-run verification\n  -> PR creation or update\n```\n\nThe gate runs in the **Implementer** role on the implementer's own task\nbranch, before any PR exists or before pushing new commits to an existing PR.\nApplying fixes here does not violate the Reviewer rule (\"reviewers do not\nmutate the implementation branch\") because no review handoff has happened yet.\n\n## How To Run\n\n1. Confirm the TDD loop is complete and verification commands pass.\n2. Review the entire task diff against the task contract (scope, non-goals,\n   acceptance checks, locks). In Claude Code, invoke the native `/code-review`\n   skill at an effort matching task risk: low/medium risk \u2192 medium effort;\n   high/critical risk \u2192 high or max effort. The cloud multi-agent deep review\n   (ultra) is operator-triggered and not required by this gate.\n3. Triage findings:\n   - Correctness bugs, contract violations, scope drift: fix now.\n   - Quality cleanups (reuse, simplification, efficiency): fix when bounded\n     to the task scope; otherwise record as a follow-up note.\n   - Findings you reject: record why.\n4. Re-run the task's verification commands after fixes.\n5. Record evidence (below). Only then create or update the PR.\n\n## Required Evidence\n\nBoth of these are required; missing evidence blocks MergeGate readiness:\n\n- A Ledger entry whose summary names this skill (`code-review`) and states\n  findings count, applied fixes, and rejected findings with reasons. List the\n  entry in the task's `ledger_evidence`.\n- A `## Pre-PR code review` section in the PR body summarizing findings and\n  resolutions (or stating \"no findings\" when the review was clean).\n\n## Exceptions\n\nEvidence-only tasks, pure `.shiki` state reconciliation, and trivial\ndocumentation-only changes may skip the gate when the skip is stated in the\nPR body and recorded in the ledger. CCA may treat an unjustified skip as\n`insufficient_evidence`.\n\n2026-08-07T08:04:14.757645Z ERROR codex_models_manager::manager: failed to renew cache TTL: missing field `base_instructions` at line 94 column 5\ncodex\nThe worktree and runtime preflight are clean for implementation: the assigned branch is correct, Codex is authenticated, and only loop-owned task/lock records are currently dirty. I found the repository-local `code-review` wrapper and loaded it fully, so that required gate is available. Next I\u2019m mapping the two public seams: ADR reference validation and `install_target`/stamp handling.\nexec\n/bin/zsh -lc \"sed -n '1,360p' tests/test_install_completeness.py\" in /private/tmp/shiki-adr-collision.pa0KwY/.worktrees/shiki-t-20260807t073307221254z-2d22d654-introduc\n succeeded in 0ms:\n\"\"\"Static shipping-completeness check for the ``shiki init`` install surface.\n\nGuards against one defect class: a *shipped* artifact (a Python module,\ncontract test, or workflow that ``shiki init`` copies into a target) depending\non a repository-relative artifact that is NOT shipped. When that happens a fresh\ninstall passes ``shiki init``'s own validation yet is permanently red on the\nrequired ``Validate Shiki mirror`` check -- and because ``init`` also applies\nbranch protection requiring that check, nothing in the target can ever merge.\n\nFor every shipped artifact this check resolves the repository-relative\ndependencies it names -- Python imports of sibling ``scripts/*.py`` modules,\n``scripts/...`` and ``tests/...`` paths referenced in shell and YAML, and the\ndirectory a ``unittest discover -s <dir>`` names -- and asserts each is either\nshipped (in ``TEMPLATE_PATHS`` or under a shipped directory) or on the explicit,\nreasoned exception list below. This is the check that would have caught, in one\npass, the shipped ``tests`` directory, ``scripts/shiki_session_lease.py`` and\n``scripts/shiki_contract_approval.py`` all being unshipped.\n\"\"\"\n\nfrom __future__ import annotations\n\nimport ast\nimport re\nimport sys\nimport tempfile\nimport unittest\nfrom pathlib import Path\n\nROOT = Path(__file__).resolve().parents[1]\nsys.path.insert(0, str(ROOT / \"scripts\"))\n\nfrom shiki_installer import TARGET_INSTALL_EXCLUDES, TEMPLATE_PATHS  # noqa: E402\n\n\n# Repository-relative dependencies that a shipped artifact names but that are\n# deliberately NOT shipped into a target. Every entry MUST carry a non-empty\n# reason: an omission here is a recorded decision, not an oversight. An empty\n# map is the healthy state -- it means every dependency of the shipping surface\n# is itself shipped.\nSHIPPING_EXCEPTIONS: dict[str, str] = {\n    # \"scripts/example.py\": \"why this dependency is intentionally not shipped\",\n}\n\n\n_GLOB_CHARS = frozenset(\"*?[]\")\n# ``from X import`` / ``import X`` inside embedded Python (shell/YAML heredocs).\n_TEXT_IMPORT_RE = re.compile(r\"(?:^|\\b)(?:from|import)\\s+([A-Za-z_][A-Za-z0-9_]*)\")\n# ``scripts/...`` or ``tests/...`` path literals in shell and YAML. Not preceded\n# by a word char/dot/dash (so ``myscripts/x`` does not match) but a leading\n# ``/`` is fine (so ``$ROOT/scripts/x.py`` still resolves to ``scripts/x.py``).\n_PATH_RE = re.compile(r\"(?<![\\w.-])(?:scripts|tests)/[A-Za-z0-9_./-]+\")\n_DISCOVER_RE = re.compile(r\"unittest\\s+discover\\b[^\\n]*?-s\\s+(\\S+)\")\n\n\ndef _exists_exact(root: Path, rel: str) -> bool:\n    \"\"\"Case-sensitive existence check.\n\n    ``Path.exists`` is case-insensitive on macOS/Windows, which would let a\n    reference like ``from Shiki state`` resolve to ``scripts/shiki.py``. Walking\n    the real directory entries keeps resolution case-exact on every platform.\n    \"\"\"\n    current = root\n    for part in Path(rel).parts:\n        try:\n            names = {entry.name for entry in current.iterdir()}\n        except (NotADirectoryError, FileNotFoundError, PermissionError):\n            return False\n        if part not in names:\n            return False\n        current = current / part\n    return True\n\n\ndef _py_import_deps(root: Path, path: Path, text: str) -> set[str]:\n    \"\"\"Sibling ``scripts/*.py`` modules imported by a shipped Python module.\"\"\"\n    deps: set[str] = set()\n    try:\n        tree = ast.parse(text, filename=str(path))\n    except SyntaxError:\n        # A shipped module that does not parse is a distinct failure surfaced by\n        # ``py_compile``; it names no resolvable static dependencies here.\n        return deps\n    for node in ast.walk(tree):\n        names: list[str] = []\n        if isinstance(node, ast.Import):\n            names = [alias.name for alias in node.names]\n        elif isinstance(node, ast.ImportFrom) and node.level == 0 and node.module:\n            names = [node.module]\n        for name in names:\n            candidate = f\"scripts/{name.split('.')[0]}.py\"\n            if _exists_exact(root, candidate):\n                deps.add(candidate)\n    return deps\n\n\ndef _text_import_deps(root: Path, text: str) -> set[str]:\n    \"\"\"Sibling ``scripts/*.py`` modules imported by embedded Python heredocs.\"\"\"\n    deps: set[str] = set()\n    for match in _TEXT_IMPORT_RE.finditer(text):\n        candidate = f\"scripts/{match.group(1)}.py\"\n        if _exists_exact(root, candidate):\n            deps.add(candidate)\n    return deps\n\n\ndef _path_reference_deps(root: Path, text: str) -> set[str]:\n    \"\"\"``scripts/...`` and ``tests/...`` file references in shell/YAML.\"\"\"\n    deps: set[str] = set()\n    for match in _PATH_RE.finditer(text):\n        raw = match.group(0).rstrip(\"/.,:;\")\n        if any(char in raw for char in _GLOB_CHARS):\n            continue\n        if _exists_exact(root, raw):\n            deps.add(raw)\n    return deps\n\n\ndef _discover_deps(root: Path, text: str) -> set[str]:\n    \"\"\"The directory a ``unittest discover -s <dir>`` names in shell/YAML.\"\"\"\n    deps: set[str] = set()\n    for match in _DISCOVER_RE.finditer(text):\n        target = match.group(1)\n        if any(char in target for char in _GLOB_CHARS):\n            continue\n        if _exists_exact(root, target):\n            deps.add(target)\n    return deps\n\n\ndef classify(rel: str) -> str | None:\n    \"\"\"Classify a shipped path as a scannable ``py``/``sh``/``yml`` source.\"\"\"\n    if rel.startswith(\"scripts/\") and rel.endswith(\".py\"):\n        return \"py\"\n    if rel.startswith(\"scripts/\") and re.search(r\"/test_shiki_[^/]*\\.sh$\", rel):\n        return \"sh\"\n    if rel.startswith(\".github/workflows/\") and rel.endswith(\".yml\"):\n        return \"yml\"\n    return None\n\n\ndef dependencies_of(root: Path, rel: str, kind: str) -> set[str]:\n    \"\"\"All repository-relative artifacts the shipped source ``rel`` depends on.\"\"\"\n    path = root / rel\n    text = path.read_text(encoding=\"utf-8\")\n    if kind == \"py\":\n        return _py_import_deps(root, path, text)\n    # Shell and YAML: heredoc imports plus path and discover-target references.\n    deps = _text_import_deps(root, text)\n    deps |= _path_reference_deps(root, text)\n    deps |= _discover_deps(root, text)\n    return deps\n\n\ndef is_shipped(rel: str, shipped: set[str]) -> bool:\n    \"\"\"True when ``rel`` is a shipped path or lives under a shipped directory.\"\"\"\n    if rel in shipped:\n        return True\n    parts = Path(rel).parts\n    for index in range(1, len(parts)):\n        if \"/\".join(parts[:index]) in shipped:\n            return True\n    return False\n\n\ndef find_unshipped_dependencies(\n    root: Path, shipped: set[str], exceptions: dict[str, str]\n) -> list[tuple[str, str]]:\n    \"\"\"Return ``(source, dependency)`` pairs where a shipped source depends on an\n    unshipped, non-excepted repository artifact.\"\"\"\n    violations: list[tuple[str, str]] = []\n    for rel in sorted(shipped):\n        kind = classify(rel)\n        if kind is None or not _exists_exact(root, rel):\n            continue\n        for dep in sorted(dependencies_of(root, rel, kind)):\n            if dep == rel or is_shipped(dep, shipped) or dep in exceptions:\n                continue\n            violations.append((rel, dep))\n    return violations\n\n\nclass InstallCompletenessTest(unittest.TestCase):\n    def test_every_shipped_dependency_is_shipped_or_excepted(self) -> None:\n        violations = find_unshipped_dependencies(\n            ROOT, set(TEMPLATE_PATHS), SHIPPING_EXCEPTIONS\n        )\n        if violations:\n            detail = \"\\n\".join(\n                f\"  {source} depends on unshipped {dependency}\"\n                for source, dependency in violations\n            )\n            self.fail(\n                \"shipped artifacts depend on unshipped repository files; add each \"\n                \"to TEMPLATE_PATHS, or to SHIPPING_EXCEPTIONS with a reason:\\n\"\n                + detail\n            )\n\n    def test_every_exception_records_a_reason(self) -> None:\n        for dependency, reason in SHIPPING_EXCEPTIONS.items():\n            self.assertIsInstance(reason, str)\n            self.assertTrue(\n                reason.strip(), f\"exception {dependency!r} must record a reason\"\n            )\n\n    def test_every_target_install_exclude_records_a_reason(self) -> None:\n        # Platform-only tests are kept out of a target install by an explicit,\n        # reasoned constant so each omission is a deliberate recorded decision.\n        for excluded, reason in TARGET_INSTALL_EXCLUDES.items():\n            self.assertTrue(\n                excluded.startswith(\"tests/\"),\n                f\"target-install exclude {excluded!r} should be a tests/ path\",\n            )\n            self.assertIsInstance(reason, str)\n            self.assertTrue(\n                reason.strip(), f\"target-install exclude {excluded!r} must record a reason\"\n            )\n\n    def test_check_detects_unshipped_python_import(self) -> None:\n        # A shipped module importing a sibling that EXISTS but is not shipped\n        # must be reported -- proof the check fails on the defect class, not\n        # merely that today's surface happens to be complete.\n        with tempfile.TemporaryDirectory() as tmp:\n            root = Path(tmp)\n            (root / \"scripts\").mkdir()\n            (root / \"scripts\" / \"shipped_module.py\").write_text(\n                \"import shiki_ghost_dependency\\n\", encoding=\"utf-8\"\n            )\n            (root / \"scripts\" / \"shiki_ghost_dependency.py\").write_text(\n                \"value = 1\\n\", encoding=\"utf-8\"\n            )\n            shipped = {\"scripts/shipped_module.py\"}\n\n            violations = find_unshipped_dependencies(root, shipped, {})\n            self.assertIn(\n                (\"scripts/shipped_module.py\", \"scripts/shiki_ghost_dependency.py\"),\n                violations,\n            )\n\n            # An explicit, reasoned exception suppresses the same violation.\n            excepted = find_unshipped_dependencies(\n                root, shipped, {\"scripts/shiki_ghost_dependency.py\": \"fixture\"}\n            )\n            self.assertEqual(excepted, [])\n\n    def test_check_detects_unshipped_shell_path_reference(self) -> None:\n        # A shipped contract test naming an unshipped ``scripts/...`` file must\n        # be reported too -- covers the shell/YAML path-reference resolver.\n        with tempfile.TemporaryDirectory() as tmp:\n            root = Path(tmp)\n            (root / \"scripts\").mkdir()\n            (root / \"scripts\" / \"test_shiki_fixture.sh\").write_text(\n                \"#!/usr/bin/env bash\\npython3 scripts/shiki_ghost_helper.py\\n\",\n                encoding=\"utf-8\",\n            )\n            (root / \"scripts\" / \"shiki_ghost_helper.py\").write_text(\n                \"x = 1\\n\", encoding=\"utf-8\"\n            )\n            shipped = {\"scripts/test_shiki_fixture.sh\"}\n\n            violations = find_unshipped_dependencies(root, shipped, {})\n            self.assertIn(\n                (\"scripts/test_shiki_fixture.sh\", \"scripts/shiki_ghost_helper.py\"),\n                violations,\n            )\n\n\nif __name__ == \"__main__\":\n    unittest.main()\n\nexec\n/bin/zsh -lc \"sed -n '1,300p' scripts/shiki_manifest.py\" in /private/tmp/shiki-adr-collision.pa0KwY/.worktrees/shiki-t-20260807t073307221254z-2d22d654-introduc\nexec\n/bin/zsh -lc \"sed -n '1,360p' tests/test_install_version_drift.py\" in /private/tmp/shiki-adr-collision.pa0KwY/.worktrees/shiki-t-20260807t073307221254z-2d22d654-introduc\n succeeded in 0ms:\n\"\"\"Canonical Shiki mirror manifest helpers.\"\"\"\n\nfrom __future__ import annotations\n\nimport json\nfrom pathlib import Path\nfrom typing import Any\n\n\nMANIFEST_PATH = \".shiki/manifest.json\"\nREADME_LAYOUT_START = \"<!-- SHIKI_MANIFEST_LAYOUT_START -->\"\nREADME_LAYOUT_END = \"<!-- SHIKI_MANIFEST_LAYOUT_END -->\"\n\n\nclass ManifestError(Exception):\n    \"\"\"Raised when the Shiki manifest cannot be loaded.\"\"\"\n\n\ndef load_manifest(root: Path) -> dict[str, Any]:\n    path = root / MANIFEST_PATH\n    try:\n        data = json.loads(path.read_text(encoding=\"utf-8\"))\n    except FileNotFoundError as error:\n        raise ManifestError(f\"{path}: manifest is missing\") from error\n    except json.JSONDecodeError as error:\n        raise ManifestError(f\"{path}: invalid JSON: {error}\") from error\n    if not isinstance(data, dict):\n        raise ManifestError(f\"{path}: manifest must be a JSON object\")\n    return data\n\n\ndef _mapping(manifest: dict[str, Any], key: str) -> dict[str, Any]:\n    value = manifest.get(key, {})\n    return value if isinstance(value, dict) else {}\n\n\ndef _string_list(manifest: dict[str, Any], *keys: str) -> list[str]:\n    value: Any = manifest\n    for key in keys:\n        if not isinstance(value, dict):\n            return []\n        value = value.get(key, [])\n    if not isinstance(value, list):\n        return []\n    return [item for item in value if isinstance(item, str)]\n\n\ndef manifest_directories(manifest: dict[str, Any]) -> dict[str, dict[str, Any]]:\n    directories: dict[str, dict[str, Any]] = {}\n    for section in (\"directories\", \"runtime_directories\"):\n        for path, metadata in _mapping(manifest, section).items():\n            if isinstance(path, str) and isinstance(metadata, dict):\n                directories[path] = metadata\n    return directories\n\n\ndef manifest_tracked_directories(manifest: dict[str, Any]) -> list[str]:\n    return [\n        path\n        for path, metadata in manifest_directories(manifest).items()\n        if metadata.get(\"tracked\") is True\n    ]\n\n\ndef manifest_runtime_directories(manifest: dict[str, Any]) -> list[str]:\n    return sorted(_mapping(manifest, \"runtime_directories\"))\n\n\ndef manifest_required_directories(manifest: dict[str, Any]) -> list[str]:\n    return [\n        path\n        for path, metadata in manifest_directories(manifest).items()\n        if metadata.get(\"required\") is True\n    ]\n\n\ndef manifest_required_files(manifest: dict[str, Any]) -> list[str]:\n    return [\n        path\n        for path, metadata in _mapping(manifest, \"files\").items()\n        if isinstance(metadata, dict) and metadata.get(\"required\") is True\n    ]\n\n\ndef manifest_create_directories(manifest: dict[str, Any]) -> list[str]:\n    return _string_list(manifest, \"install\", \"create_directories\")\n\n\ndef manifest_install_include(manifest: dict[str, Any]) -> list[str]:\n    return _string_list(manifest, \"install\", \"include\")\n\n\ndef manifest_exclude_from_commit(manifest: dict[str, Any]) -> list[str]:\n    return _string_list(manifest, \"install\", \"exclude_from_commit\")\n\n\ndef render_manifest_layout(manifest: dict[str, Any]) -> str:\n    rows = [\n        README_LAYOUT_START,\n        \"| Path | Kind | State Class | Tracked | Required | Description |\",\n        \"| --- | --- | --- | --- | --- | --- |\",\n    ]\n    for path, metadata in sorted(manifest_directories(manifest).items()):\n        kind = str(metadata.get(\"kind\", \"unknown\"))\n        state_class = str(metadata.get(\"state_class\", \"unknown\"))\n        tracked = \"yes\" if metadata.get(\"tracked\") is True else \"no\"\n        required = \"yes\" if metadata.get(\"required\") is True else \"no\"\n        description = str(metadata.get(\"description\", \"\")).replace(\"\\n\", \" \")\n        rows.append(f\"| `{path}` | {kind} | {state_class} | {tracked} | {required} | {description} |\")\n    rows.append(README_LAYOUT_END)\n    return \"\\n\".join(rows)\n\n succeeded in 0ms:\n\"\"\"Install version stamp: written on install, drift-reported by ``doctor``.\n\nA uniformly-old target is internally consistent, so ``validate_shiki`` passes\nand ``doctor`` reports zero failures \u2014 indistinguishable from a current target.\nThe install stamp is the instrument that removes that blind spot: every install\nand upgrade writes a small ``.shiki/install-stamp.json`` naming the platform\ncommit, the install time, and a content digest per platform-owned shipped path,\nand ``doctor`` reports three kinds of drift from it \u2014\n\n* a shipped path whose current content differs from the stamped digest\n  (hand-edited or half-upgraded);\n* a stamped platform commit that is not an ancestor of the running platform HEAD\n  (the target is ahead, or from a different lineage);\n* an absent stamp (a target installed before stamps existed) \u2014 a distinct\n  finding, never a pass.\n\nEach is a real ``doctor`` finding with its own id, so ``--strict`` fails on it,\nand ``doctor`` never writes to the target.\n\nThis file also guards the migration ``--target`` fix: the global ``shiki`` execs\nthe PLATFORM's ``scripts/``, so ``migrate --target <other repo>`` would apply the\nplatform registry to the target's state and could write migration ids the\ntarget's own validator rejects. When the target ships its own, differing\nregistry, ``migrate`` refuses and names the correct command.\n\nDependency-free ``unittest`` only; drives ``scripts/shiki.py`` via subprocess so\nthe assertions exercise the same exit contract an operator sees.\n\"\"\"\n\nfrom __future__ import annotations\n\nimport hashlib\nimport json\nimport re\nimport shutil\nimport subprocess\nimport tempfile\nimport unittest\nfrom pathlib import Path\n\nimport shiki_test_support  # noqa: F401  (path bootstrap)\n\nfrom shiki_installer import INSTALL_STAMP_PATH, _incoming_is_older\n\nSHIKI_CLI = shiki_test_support.SCRIPTS_DIR / \"shiki.py\"\nREPO_ROOT = shiki_test_support.REPO_ROOT\n\n# A platform-owned shipped path that the stamp always digests (overwrite\n# category \u2014 not project content, governance, or preserve-outright state).\nSHIPPED_PATH = \"scripts/shiki_git.py\"\n# Files the stamp must never digest: they are customized per target, so a\n# difference there is expected, not drift.\nCUSTOMIZABLE_PATHS = (\"AGENTS.md\", \".shiki/config.yaml\", \".github/CODEOWNERS\")\n\n\ndef _run(*args: str) -> subprocess.CompletedProcess[str]:\n    return subprocess.run(\n        [\"python3\", str(SHIKI_CLI), *args],\n        capture_output=True,\n        text=True,\n    )\n\n\ndef _findings(doctor_json: str) -> dict[str, str]:\n    report = json.loads(doctor_json)\n    return {finding[\"id\"]: finding[\"status\"] for finding in report[\"findings\"]}\n\n\ndef _finding(report_json: str, finding_id: str) -> dict[str, object]:\n    report = json.loads(report_json)\n    for finding in report[\"findings\"]:\n        if finding[\"id\"] == finding_id:\n            return finding\n    raise AssertionError(f\"missing doctor finding {finding_id}\")\n\n\ndef _sha256(path: Path) -> str:\n    return hashlib.sha256(path.read_bytes()).hexdigest()\n\n\ndef _content_snapshot(root: Path) -> dict[str, str]:\n    \"\"\"Digest every file under ``root`` except Python bytecode cache.\"\"\"\n    snapshot: dict[str, str] = {}\n    for path in sorted(root.rglob(\"*\")):\n        if path.is_file() and \"__pycache__\" not in path.parts and path.suffix != \".pyc\":\n            snapshot[str(path.relative_to(root))] = _sha256(path)\n    return snapshot\n\n\nclass InstallStampTests(unittest.TestCase):\n    def setUp(self) -> None:\n        self.tmp = Path(tempfile.mkdtemp(prefix=\"shiki-install-stamp-\"))\n        self.addCleanup(lambda: shutil.rmtree(self.tmp, ignore_errors=True))\n        self.target = self.tmp / \"target\"\n        self.target.mkdir()\n        install = _run(\"install-target\", str(self.target), \"--local-only\", \"--no-validate\")\n        self.assertEqual(install.returncode, 0, install.stderr)\n        self.stamp_path = self.target / INSTALL_STAMP_PATH\n\n    def _read_stamp(self) -> dict:\n        return json.loads(self.stamp_path.read_text(encoding=\"utf-8\"))\n\n    def _doctor(self, *extra: str) -> subprocess.CompletedProcess[str]:\n        return _run(\"doctor\", \"--json\", \"--target\", str(self.target), *extra)\n\n    # -- stamp shape -------------------------------------------------------\n\n    def test_fresh_install_writes_a_stamp(self) -> None:\n        self.assertTrue(self.stamp_path.is_file(), \"install must write an install stamp\")\n        stamp = self._read_stamp()\n        self.assertEqual(stamp[\"version\"], 1)\n\n        # Names the platform commit the install came from.\n        head = subprocess.run(\n            [\"git\", \"-C\", str(REPO_ROOT), \"rev-parse\", \"HEAD\"],\n            capture_output=True, text=True,\n        )\n        self.assertEqual(head.returncode, 0, head.stderr)\n        self.assertEqual(stamp[\"platform_commit\"], head.stdout.strip())\n\n        # Names the install timestamp.\n        self.assertIsInstance(stamp[\"installed_at\"], str)\n        self.assertTrue(stamp[\"installed_at\"])\n\n        # Names a content digest per shipped path \u2014 platform-owned paths only.\n        digests = stamp[\"digests\"]\n        self.assertIn(SHIPPED_PATH, digests)\n        self.assertEqual(digests[SHIPPED_PATH], _sha256(self.target / SHIPPED_PATH))\n        for customizable in CUSTOMIZABLE_PATHS:\n            self.assertNotIn(\n                customizable, digests, f\"{customizable} is customized per target; it must not be stamped\"\n            )\n\n    def test_stamp_is_not_committable(self) -> None:\n        # The untracked stamp must never reach a commit: validate rejects an\n        # unknown tracked .shiki path, and the loop stages with `git add -A`.\n        subprocess.run([\"git\", \"-C\", str(self.target), \"init\", \"-q\"], check=True)\n        subprocess.run([\"git\", \"-C\", str(self.target), \"add\", \".\"], check=True)\n        tracked = subprocess.run(\n            [\"git\", \"-C\", str(self.target), \"ls-files\"], capture_output=True, text=True,\n        ).stdout\n        self.assertNotIn(INSTALL_STAMP_PATH, tracked.splitlines())\n        # The target's own validator still passes with the stamp on disk.\n        validate = subprocess.run(\n            [\"python3\", \"scripts/validate_shiki.py\"], cwd=str(self.target),\n            capture_output=True, text=True,\n        )\n        self.assertEqual(validate.returncode, 0, validate.stdout + validate.stderr)\n\n    def test_upgrade_refreshes_the_stamp(self) -> None:\n        # A stale stamp (older platform content: a wrong recorded digest and an\n        # old timestamp) is refreshed by a --force upgrade.\n        stamp = self._read_stamp()\n        stamp[\"installed_at\"] = \"2000-01-01T00:00:00+00:00\"\n        stamp[\"digests\"][SHIPPED_PATH] = \"deadbeef\"\n        self.stamp_path.write_text(json.dumps(stamp, indent=2, sort_keys=True) + \"\\n\", encoding=\"utf-8\")\n\n        upgrade = _run(\"install-target\", str(self.target), \"--local-only\", \"--no-validate\", \"--force\")\n        self.assertEqual(upgrade.returncode, 0, upgrade.stderr)\n\n        refreshed = self._read_stamp()\n        self.assertNotEqual(refreshed[\"installed_at\"], \"2000-01-01T00:00:00+00:00\")\n        self.assertEqual(refreshed[\"digests\"][SHIPPED_PATH], _sha256(self.target / SHIPPED_PATH))\n\n    # -- drift reporting ---------------------------------------------------\n\n    def test_no_drift_on_untouched_fresh_install(self) -> None:\n        statuses = _findings(self._doctor().stdout)\n        self.assertEqual(statuses[\"doctor.install_stamp.present\"], \"pass\")\n        self.assertEqual(statuses[\"doctor.install_stamp.content\"], \"pass\")\n        self.assertEqual(statuses[\"doctor.install_stamp.platform_commit\"], \"pass\")\n\n    def test_editing_a_shipped_file_is_reported_naming_that_path(self) -> None:\n        edited = self.target / SHIPPED_PATH\n        edited.write_text(edited.read_text(encoding=\"utf-8\") + \"\\n# local edit\\n\", encoding=\"utf-8\")\n\n        finding = _finding(self._doctor().stdout, \"doctor.install_stamp.content\")\n        self.assertEqual(finding[\"status\"], \"warn\")\n        self.assertIn(SHIPPED_PATH, finding[\"summary\"])\n        self.assertIn(SHIPPED_PATH, finding[\"details\"][\"drifted\"])\n\n    def test_absent_stamp_is_a_distinct_finding_never_a_pass(self) -> None:\n        self.stamp_path.unlink()\n        report = self._doctor().stdout\n        statuses = _findings(report)\n        self.assertEqual(statuses[\"doctor.install_stamp.present\"], \"warn\")\n        self.assertNotEqual(statuses[\"doctor.install_stamp.present\"], \"pass\")\n        # Content/commit cannot be judged without a stamp, but are never a pass.\n        self.assertEqual(statuses[\"doctor.install_stamp.content\"], \"skip\")\n        self.assertEqual(statuses[\"doctor.install_stamp.platform_commit\"], \"skip\")\n\n    def test_newer_platform_commit_is_reported_not_silently_accepted(self) -> None:\n        # A stamp naming a commit that is not an ancestor of the running platform\n        # HEAD (target ahead, or a different lineage) is reported.\n        stamp = self._read_stamp()\n        stamp[\"platform_commit\"] = \"0\" * 40  # not an ancestor of any real HEAD\n        self.stamp_path.write_text(json.dumps(stamp, indent=2, sort_keys=True) + \"\\n\", encoding=\"utf-8\")\n\n        finding = _finding(self._doctor().stdout, \"doctor.install_stamp.platform_commit\")\n        self.assertEqual(finding[\"status\"], \"warn\")\n        self.assertIn(\"not an ancestor\", finding[\"summary\"])\n\n    def test_strict_fails_on_drift(self) -> None:\n        edited = self.target / SHIPPED_PATH\n        edited.write_text(edited.read_text(encoding=\"utf-8\") + \"\\n# local edit\\n\", encoding=\"utf-8\")\n        self.assertEqual(_findings(self._doctor().stdout)[\"doctor.install_stamp.content\"], \"warn\")\n        self.assertNotEqual(self._doctor(\"--strict\").returncode, 0)\n\n    def test_strict_fails_on_absent_stamp(self) -> None:\n        self.stamp_path.unlink()\n        self.assertEqual(_findings(self._doctor().stdout)[\"doctor.install_stamp.present\"], \"warn\")\n        self.assertNotEqual(self._doctor(\"--strict\").returncode, 0)\n\n    def test_doctor_never_writes_to_the_target(self) -> None:\n        # Prime bytecode caches with one run, then assert a second run mutates\n        # no target content (the stamp included).\n        self._doctor()\n        before = _content_snapshot(self.target)\n        self._doctor()\n        after = _content_snapshot(self.target)\n        self.assertEqual(before, after, \"doctor must not write to the target\")\n\n\nclass OlderContentGuardTests(unittest.TestCase):\n    \"\"\"A later install must never overwrite a stamp with older content.\"\"\"\n\n    def _rev(self, ref: str) -> str | None:\n        result = subprocess.run(\n            [\"git\", \"-C\", str(REPO_ROOT), \"rev-parse\", ref],\n            capture_output=True, text=True,\n        )\n        return result.stdout.strip() if result.returncode == 0 else None\n\n    def test_incoming_older_lineage_is_preserved_not_overwritten(self) -> None:\n        head = self._rev(\"HEAD\")\n        parent = self._rev(\"HEAD~1\")\n        if not head or not parent:\n            self.skipTest(\"repository has no HEAD~1 to compare lineage against\")\n        # Incoming (an ancestor) is older than the recorded stamp -> preserve.\n        self.assertTrue(_incoming_is_older(parent, head))\n        # Incoming (a descendant) is newer -> refresh.\n        self.assertFalse(_incoming_is_older(head, parent))\n        # Same commit or an unknown commit is never treated as older.\n        self.assertFalse(_incoming_is_older(head, head))\n        self.assertFalse(_incoming_is_older(\"0\" * 40, head))\n        self.assertFalse(_incoming_is_older(None, head))\n\n\nclass MigrateForeignTargetTests(unittest.TestCase):\n    \"\"\"`migrate --target <other repo>` must resolve the target's own registry or\n    refuse; a fresh (identical-registry) target is never refused.\"\"\"\n\n    def setUp(self) -> None:\n        self.tmp = Path(tempfile.mkdtemp(prefix=\"shiki-migrate-foreign-\"))\n        self.addCleanup(lambda: shutil.rmtree(self.tmp, ignore_errors=True))\n\n    def _install(self, name: str) -> Path:\n        target = self.tmp / name\n        target.mkdir()\n        install = _run(\"install-target\", str(target), \"--local-only\", \"--no-validate\")\n        self.assertEqual(install.returncode, 0, install.stderr)\n        return target\n\n    def test_fresh_identical_registry_target_is_not_refused(self) -> None:\n        target = self._install(\"fresh\")\n        result = _run(\"migrate\", \"status\", \"--json\", \"--target\", str(target))\n        self.assertEqual(result.returncode, 0, result.stderr)\n        self.assertNotIn(\"refusing to run the platform migration registry\", result.stderr)\n\n    def test_foreign_registry_target_is_refused_with_the_correct_command(self) -> None:\n        target = self._install(\"foreign\")\n        foreign_module = target / \"scripts\" / \"shiki_migrations.py\"\n        foreign_module.write_text(\n            foreign_module.read_text(encoding=\"utf-8\") + \"\\n# foreign platform divergence\\n\",\n            encoding=\"utf-8\",\n        )\n\n        for command in (\"status\", \"apply\"):\n            result = _run(\"migrate\", command, \"--json\", \"--target\", str(target))\n            self.assertNotEqual(result.returncode, 0, f\"migrate {command} must be refused\")\n            self.assertIn(\"[shiki] error:\", result.stderr)\n            self.assertIn(\"refusing to run the platform migration registry\", result.stderr)\n            # Names the correct command: run the target's own CLI.\n            self.assertRegex(\n                result.stderr,\n                re.compile(rf\"cd \\S*foreign && python3 scripts/shiki\\.py migrate {command}\"),\n            )\n            self.assertNotIn(\"Traceback (most recent call last)\", result.stderr)\n\n\nif __name__ == \"__main__\":\n    unittest.main()\n\nexec\n/bin/zsh -lc \"sed -n '1,280p' pyproject.toml\" in /private/tmp/shiki-adr-collision.pa0KwY/.worktrees/shiki-t-20260807t073307221254z-2d22d654-introduc\nexec\n/bin/zsh -lc \"sed -n '1,380p' scripts/shiki_installer.py\" in /private/tmp/shiki-adr-collision.pa0KwY/.worktrees/shiki-t-20260807t073307221254z-2d22d654-introduc\n succeeded in 0ms:\n# Shiki packaging metadata.\n#\n# Shiki is a GitHub-first agentic engineering control plane distributed as a\n# repository template plus a dependency-free standard-library CLI (scripts/).\n# This file records project metadata and the canonical version. The Shiki CLI is\n# intentionally NOT exposed as a build-backend [project.scripts] console entry\n# because the CLI ships as the dependency-free `scripts/shiki.py` shim and the\n# `scripts/` directory is not packaged as an importable distribution. pipx/PyPI\n# installs are therefore NOT supported; the official install path is the script\n# install (`python3 scripts/shiki.py install-global`) or running the shim\n# directly, as documented in docs/releasing.md. See\n# docs/adr/0007-packaging-and-release.md.\n#\n# The version below is kept in sync with the canonical ./VERSION file. When\n# bumping the release, update both ./VERSION and the `version` field here.\n\n[project]\nname = \"shiki\"\nversion = \"0.2.0\"\ndescription = \"Shiki: a GitHub-first agentic engineering control plane with a repository-local .shiki/ mirror for recovery, audit, and portable agent context.\"\nreadme = \"README.md\"\nrequires-python = \">=3.11\"\nlicense = { text = \"MIT\" }\nauthors = [{ name = \"mizutani-140\" }]\nkeywords = [\"shiki\", \"agentic\", \"engineering\", \"control-plane\", \"github\", \"mergegate\", \"cca\"]\n\n[project.urls]\nHomepage = \"https://github.com/mizutani-140/shiki\"\nRepository = \"https://github.com/mizutani-140/shiki\"\nReleasing = \"https://github.com/mizutani-140/shiki/blob/main/docs/releasing.md\"\n\n succeeded in 0ms:\n#!/usr/bin/env python3\n\"\"\"Template copy, manifest staging, and local/global install helpers.\"\"\"\n\nfrom __future__ import annotations\n\nfrom dataclasses import dataclass, field\nimport hashlib\nimport json\nfrom pathlib import Path\nimport shutil\nimport subprocess\nfrom typing import Any\n\nfrom shiki_config import load_shiki_config\nfrom shiki_contracts import CODEOWNERS_PATH, CODEOWNERS_REQUIRED_OWNER, codeowners_required_owner\nfrom shiki_manifest import load_manifest, manifest_create_directories, manifest_directories, manifest_exclude_from_commit, manifest_install_include\nfrom shiki_migrations import migration_status\nfrom shiki_process import ROOT, ShikiError, info, warn, validate_target_shiki, load_default_config, utc_now\n\nTEMPLATE_PATHS = [\n    \"bin/shiki\",\n    \"AGENTS.md\",\n    \"CLAUDE.md\",\n    \"CONTEXT.md\",\n    \"SYSTEM_PROMPT.md\",\n    \".gitignore\",\n    \".claude/commands/shiki.md\",\n    \".codex/skills/shiki/SKILL.md\",\n    \".shiki\",\n    \".github/ISSUE_TEMPLATE\",\n    \".github/CODEOWNERS\",\n    \".github/PULL_REQUEST_TEMPLATE\",\n    \".github/prompts\",\n    \".github/workflows/shiki-validate.yml\",\n    \".github/workflows/shiki-claude-review.yml\",\n    \".github/workflows/shiki-cca-completion.yml\",\n    \".github/workflows/shiki-mergegate.yml\",\n    \".github/workflows/shiki-orchestrator.yml\",\n    \"docs/agents\",\n    \"docs/adr\",\n    \"skills/engineering\",\n    \"tests\",\n    \"scripts/shiki_schema.py\",\n    \"scripts/validate_shiki.py\",\n    \"scripts/shiki_contracts.py\",\n    \"scripts/shiki_jsonschema.py\",\n    \"scripts/shiki_evidence.py\",\n    \"scripts/shiki_locks.py\",\n    \"scripts/shiki_loop.py\",\n    \"scripts/shiki_memory.py\",\n    \"scripts/shiki_manifest.py\",\n    \"scripts/shiki_migrations.py\",\n    \"scripts/shiki_provider.py\",\n    \"scripts/shiki_workflows.py\",\n    \"scripts/enforce_cca_verdict.py\",\n    \"scripts/cca_verdict_usable.py\",\n    \"scripts/build_cca_evidence_manifest.py\",\n    \"scripts/guardian_approval_signal.py\",\n    \"scripts/mergegate_check.py\",\n    \"scripts/shiki_sync_proof.py\",\n    \"scripts/shiki.py\",\n    \"scripts/shiki_bootstrap.py\",\n    \"scripts/shiki_cli.py\",\n    \"scripts/shiki_config.py\",\n    \"scripts/shiki_contract_approval.py\",\n    \"scripts/shiki_doctor.py\",\n    \"scripts/shiki_git.py\",\n    \"scripts/shiki_github.py\",\n    \"scripts/shiki_guardian.py\",\n    \"scripts/shiki_guardian_review.py\",\n    \"scripts/shiki_guardian_status.py\",\n    \"scripts/shiki_installer.py\",\n    \"scripts/shiki_process.py\",\n    \"scripts/shiki_runtime.py\",\n    \"scripts/shiki_runtime_adapters.py\",\n    \"scripts/shiki_runtime_registry.py\",\n    \"scripts/shiki_session_lease.py\",\n    \"scripts/shiki_state_classes.py\",\n    \"scripts/shiki_tasks.py\",\n    \"scripts/shiki_state.py\",\n    \"scripts/test_shiki_init.sh\",\n    \"scripts/test_shiki_control_plane.sh\",\n    \"scripts/test_shiki_run_orchestrator.sh\",\n    \"scripts/test_shiki_daemon_runner.sh\",\n    \"scripts/test_shiki_runner_codex.sh\",\n    \"scripts/test_shiki_runner_claude.sh\",\n    \"scripts/test_shiki_goal_loop.sh\",\n    \"scripts/test_shiki_memory_loop.sh\",\n    \"scripts/test_shiki_code_review_gate.sh\",\n    \"scripts/test_shiki_start.sh\",\n    \"scripts/test_shiki_runtime_auth.sh\",\n    \"scripts/test_shiki_runtime_registry.sh\",\n    \"scripts/test_shiki_state_classes.sh\",\n    \"scripts/test_shiki_provider_config.sh\",\n    \"scripts/test_shiki_guardian_policy.sh\",\n    \"scripts/test_shiki_evidence_integrity.sh\",\n    \"scripts/test_shiki_governance_evidence.sh\",\n    \"scripts/test_shiki_doctor.sh\",\n    \"scripts/test_shiki_migrations.sh\",\n    \"scripts/test_shiki_module_boundaries.sh\",\n    \"scripts/test_shiki_shellcheck.sh\",\n    \"scripts/test_shiki_validator_hardening.sh\",\n    \"scripts/test_shiki_workflow_lint.sh\",\n]\n\nDEFAULT_GLOBAL_COMMAND_PATH = \"~/.local/bin/shiki\"\nDEFAULT_CLAUDE_COMMAND_PATH = \"~/.claude/commands/shiki.md\"\nDEFAULT_CODEX_SKILL_PATH = \"~/.codex/skills/shiki/SKILL.md\"\n\n# The install stamp records, per target, which platform commit an install came\n# from, when it was installed, and a content digest for every platform-owned\n# shipped path. It is target state (like migration state and repo.json), not a\n# contract, so it lives beside the other target-owned records under `.shiki/`.\n#\n# It is deliberately NOT tracked: it is never added to `manifest_stage_paths`,\n# and validate_shiki only classifies *tracked* `.shiki/` paths, so an untracked\n# stamp is invisible to the mirror validator. Keeping it out of the manifest is\n# what lets a target carry it without a governance-schema change.\nINSTALL_STAMP_PATH = \".shiki/install-stamp.json\"\nINSTALL_STAMP_VERSION = 1\n\n# Under --force the shipped surface splits three ways, so nothing is lost and\n# nothing is silently stale.\n#\n# PROJECT CONTENT is authored per target and must never be overwritten. The\n# incoming template is written alongside as ``<file>.new`` and reported, so the\n# operator can merge deliberately (CONTEXT.md is the glossary AGENTS.md names as\n# the source of truth for domain language).\nPROJECT_CONTENT_FILES = (\n    \"CONTEXT.md\",\n    \"AGENTS.md\",\n    \"CLAUDE.md\",\n    \".github/CODEOWNERS\",\n)\n\n# GOVERNANCE CONTRACT is target-specific but decides what branch protection\n# requires (config.yaml -> mergegate.required_checks) and what may approve\n# (guardian-policy.json -> approval_sources). It must never be silently kept\n# either: write ``<file>.new`` alongside and report which keys differ, so a stale\n# contract is visible rather than inferred.\nGOVERNANCE_CONTRACT_FILES = (\n    \".shiki/config.yaml\",\n    \".shiki/guardian-policy.json\",\n)\n\n# Governance keys named explicitly in the end-of-run summary when they differ,\n# because they are the ones that change what protection requires / what approves.\nGOVERNANCE_CRITICAL_KEYS: dict[str, tuple[str, ...]] = {\n    \".shiki/config.yaml\": (\"mergegate.required_checks\",),\n    \".shiki/guardian-policy.json\": (\"approval_sources\",),\n}\n\n# Applied-migration state is target history, not a contract: it is preserved\n# outright under --force and never written as a ``.new``.\nPRESERVE_OUTRIGHT_FILES = (\".shiki/migrations/state.json\",)\n\n# Shipped `tests/` files that must NOT be copied into a target install. Each is a\n# platform-only test: it asserts something that exists only in the Shiki platform\n# repo itself, so it can never pass from a freshly installed target and would make\n# the target's own `unittest discover -s tests` (a required check) fail forever.\n# These files are still committed and run in the platform (they are absent only\n# from the target's shipped surface, not from `exclude_from_commit`). Reason per\n# entry: an omission here is a deliberate, recorded decision, not an oversight.\nTARGET_INSTALL_EXCLUDES = {\n    \"tests/test_loop_e2e_contract.py\": (\n        \"Contract meta-test over scripts/test_shiki_loop_e2e.sh and \"\n        \"docs/agents/autonomous-loop-e2e-acceptance.md \u2014 platform-only e2e \"\n        \"artifacts that are not part of the shipped target surface.\"\n    ),\n    \"tests/test_mirror_lock_injection.py\": (\n        \"FrozenPlanCorpusTests asserts the platform's own frozen-plan corpus \"\n        \"(>=1 frozen plan, >=19 tasks); a freshly installed target starts with \"\n        \"an empty .shiki mirror, so the assertion cannot hold there.\"\n    ),\n    \"tests/test_install_version_drift.py\": (\n        \"Install-stamp drift and platform-commit lineage are judged against the \"\n        \"running platform's git HEAD; a freshly installed target has different \"\n        \"or absent git lineage, so these assertions are platform-context only.\"\n    ),\n}\n\ndef manifest_stage_paths(path: Path) -> list[str]:\n    candidates = list(TEMPLATE_PATHS)\n    candidates.append(\".shiki/manifest.json\")\n    candidates.append(\".shiki/guardian-policy.json\")\n    candidates.append(\".shiki/migrations/state.json\")\n    candidates.append(\".shiki/repo.json\")\n    manifest = load_manifest(path) if (path / \".shiki\" / \"manifest.json\").exists() else load_manifest(ROOT)\n    excluded = manifest_exclude_from_commit(manifest)\n    return [\n        relative\n        for relative in candidates\n        if (path / relative).exists() and not excluded_from_commit(relative, excluded)\n    ]\n\n\ndef excluded_from_commit(relative: str, patterns: list[str]) -> bool:\n    normalized = relative.strip().replace(\"\\\\\", \"/\")\n    for pattern in patterns:\n        clean = pattern.strip().replace(\"\\\\\", \"/\")\n        if clean.endswith(\"/**\"):\n            prefix = clean[:-3]\n            if normalized == prefix or normalized.startswith(f\"{prefix}/\"):\n                return True\n            continue\n        if normalized == clean:\n            return True\n    return False\n\n\n@dataclass\nclass NewFileNote:\n    \"\"\"A ``<file>.new`` written alongside a preserved original under --force.\"\"\"\n\n    relative: str\n    new_relative: str\n    category: str  # \"project-content\" | \"governance-contract\"\n    differing_keys: tuple[str, ...] = field(default_factory=tuple)\n\n\ndef refuse_pending_migrations(target: Path) -> None:\n    \"\"\"Precondition run BEFORE any file write.\n\n    Refuse to upgrade a target whose valid ``.shiki`` migration state is behind\n    the current registry, so a target with pending migrations keeps an untouched\n    tree instead of being half-rewritten and then aborting. Only the \"valid but\n    behind\" upgrade case is gated: a fresh target has no state file yet (it is\n    seeded by this install), and a target whose state file is absent or malformed\n    is left to post-write validation, which surfaces malformed state.\n    \"\"\"\n    status = migration_status(target)\n    if status[\"state_exists\"] and not status[\"errors\"] and status[\"pending\"]:\n        pending = \", \".join(status[\"pending\"])\n        raise ShikiError(\n            f\"target has pending .shiki migrations ({pending}); run \"\n            \"`shiki migrate apply --execute` before upgrading. No files were written.\"\n        )\n\n\ndef install_template(target: Path, *, force: bool, validate: bool) -> list[NewFileNote]:\n    # Preconditions first: never rewrite the tree of a target that cannot be\n    # upgraded cleanly (see refuse_pending_migrations).\n    refuse_pending_migrations(target)\n\n    # Resolve the target's required CODEOWNERS owner once from its\n    # ``.shiki/repo.json`` (present when a target already knows its GitHub\n    # identity). The shipped ``.github/CODEOWNERS`` names this maintainer on every\n    # line, so a foreign target's copy is rewritten to name its own owner; when no\n    # owner can be resolved this is the documented fallback and the file is copied\n    # verbatim. See ``codeowners_required_owner``.\n    codeowners_owner = codeowners_required_owner(target)\n\n    notes: list[NewFileNote] = []\n    for relative in TEMPLATE_PATHS:\n        source = ROOT / relative\n        if not source.exists():\n            warn(f\"template path missing, skipped: {relative}\")\n            continue\n        copy_path(\n            source,\n            target / relative,\n            force=force,\n            target_install=True,\n            notes=notes,\n            codeowners_owner=codeowners_owner,\n        )\n\n    manifest = load_manifest(ROOT)\n    directories = manifest_directories(manifest)\n    for relative in manifest_create_directories(manifest):\n        state_dir = target / relative\n        state_dir.mkdir(parents=True, exist_ok=True)\n        metadata = directories.get(relative, {})\n        if metadata.get(\"tracked\") is True and metadata.get(\"required\") is True and not any(state_dir.iterdir()):\n            (state_dir / \".gitkeep\").write_text(\"\", encoding=\"utf-8\")\n        info(f\"ensured empty state directory: {state_dir}\")\n\n    # Stamp the install after the shipped surface is on disk, so the digests\n    # describe exactly what was written. Refreshed on every install and upgrade.\n    # The stamp stays untracked local state, so keep it out of git first.\n    _ensure_stamp_gitignored(target)\n    write_install_stamp(target)\n\n    # A single explicit summary at the end, so a half-upgrade is visible rather\n    # than inferred. Printed before validation so the .new files are always\n    # reported even if a later gate fails.\n    print_new_file_summary(notes)\n\n    if validate:\n        validate_target_shiki(target)\n\n    return notes\n\n\ndef print_new_file_summary(notes: list[NewFileNote]) -> None:\n    if not notes:\n        info(\n            \"template summary: no .new files written (no existing project-content \"\n            \"or governance file was kept alongside a refreshed copy)\"\n        )\n        return\n    info(f\"template summary: wrote {len(notes)} .new file(s) alongside preserved originals:\")\n    for note in sorted(notes, key=lambda item: item.relative):\n        if note.category == \"governance-contract\":\n            keys = \", \".join(note.differing_keys) if note.differing_keys else \"no parseable key differences\"\n            info(f\"  governance-contract: kept {note.relative}, wrote {note.new_relative} (differing keys: {keys})\")\n        else:\n            info(f\"  project-content: kept {note.relative}, wrote {note.new_relative}\")\n\n\ndef should_skip(path: Path, *, target_install: bool = False) -> bool:\n    parts = set(path.parts)\n    if \"__pycache__\" in parts or path.name == \".DS_Store\" or path.suffix == \".pyc\":\n        return True\n    if target_install:\n        relative = path.relative_to(ROOT)\n        relative_text = relative.as_posix()\n        manifest = load_manifest(ROOT)\n        if relative_text in manifest_install_include(manifest):\n            return False\n        # Platform-only tests never ship into a target (see TARGET_INSTALL_EXCLUDES).\n        if relative_text in TARGET_INSTALL_EXCLUDES:\n            return True\n        # Provider metadata is created per-target by shiki init/start; copying it\n        # into a new target would point that target at this repository's origin.\n        if relative_text == \".shiki/repo.json\":\n            return True\n        state_prefixes = tuple(f\"{directory}/\" for directory in manifest_create_directories(manifest))\n        if relative_text.startswith(state_prefixes):\n            return True\n        return excluded_from_commit(relative_text, manifest_exclude_from_commit(manifest))\n    return False\n\n\ndef template_relative(source: Path) -> str | None:\n    \"\"\"The shipped-surface relative path for ``source`` (POSIX), or None.\"\"\"\n    try:\n        return source.relative_to(ROOT).as_posix()\n    except ValueError:\n        return None\n\n\ndef force_category(source: Path) -> str:\n    \"\"\"Classify a shipped file for --force handling.\n\n    Returns one of ``project-content``, ``governance-contract``,\n    ``preserve-outright``, or ``overwrite``.\n    \"\"\"\n    relative = template_relative(source)\n    if relative in PROJECT_CONTENT_FILES:\n        return \"project-content\"\n    if relative in GOVERNANCE_CONTRACT_FILES:\n        return \"governance-contract\"\n    if relative in PRESERVE_OUTRIGHT_FILES:\n        return \"preserve-outright\"\n    return \"overwrite\"\n\n\ndef copy_path(\n    source: Path,\n    target: Path,\n    *,\n    force: bool,\n    target_install: bool = False,\n    notes: list[NewFileNote] | None = None,\n    codeowners_owner: str | None = None,\n) -> None:\n    if should_skip(source, target_install=target_install):\n        return\n    if source.is_dir():\n        for child in source.iterdir():\n            copy_path(\n                child,\n                target / child.name,\n                force=force,\n                target_install=target_install,\n                notes=notes,\n                codeowners_owner=codeowners_owner,\n            )\n        return\n\nexec\n/bin/zsh -lc \"sed -n '1,320p' tests/test_adr_references.py\" in /private/tmp/shiki-adr-collision.pa0KwY/.worktrees/shiki-t-20260807t073307221254z-2d22d654-introduc\n succeeded in 0ms:\n\"\"\"Every ``ADR ####`` citation must resolve to exactly one decision record.\n\nBackground. ``scripts/mergegate_check.py`` justifies ``is_bookkeeping_closeout``\nwith \"ADR 0017\" nine times and ``scripts/guardian_approval_signal.py`` cites\n0016/0017 seven times, yet for a stretch the numbering in ``docs/adr/`` stopped\nat 0015: the authoring commits lived on a coordinator branch and no task ever\ndeclared ``docs/adr/...`` in its locks, so nothing carried the records to the\ndefault branch. The stated basis of an exemption that merged seven PRs with zero\nGuardian approvals could not be read. ``grep -n 'docs/adr' scripts/validate_shiki.py``\nreturns nothing -- no validator required a cited ADR to exist, so the citations\nand the directory drifted with no signal.\n\nThis is the same unbound-pair shape behind other silent failures: a reference\nand its target with nothing asserting they agree. This test binds them. It scans\nevery ``scripts/*.py`` (plus ``docs/**/*.md`` and the root ``*.md`` governance\nfiles, which are cheap) for the ``ADR ####`` pattern and asserts each cited\nnumber resolves to exactly one ``docs/adr/####-*.md``. The converse is\ndeliberately NOT required: an ADR may exist without being cited.\n\nThe suite is dependency free: standard-library ``unittest`` only.\n\"\"\"\n\nfrom __future__ import annotations\n\nimport re\nimport shutil\nimport tempfile\nimport unittest\nfrom pathlib import Path\nfrom typing import NamedTuple, Sequence\n\nREPO_ROOT = Path(__file__).resolve().parents[1]\n\n# \"ADR 0016\" -- a four-digit citation. The trailing \\b keeps a five-digit token\n# from being read as a truncated four-digit number.\n_CITATION = re.compile(r\"\\bADR (\\d{4})\\b\")\n# \"0016-locks-bound-product-paths.md\" -- the leading four digits name the record.\n_ADR_FILENAME = re.compile(r\"^(\\d{4})-.*\\.md$\")\n\n\nclass Violation(NamedTuple):\n    \"\"\"A citation that does not resolve to exactly one decision record.\"\"\"\n\n    citing_file: str  # repo-relative path of the file that cites the ADR\n    number: str  # the cited four-digit number, e.g. \"0016\"\n    line: int  # 1-based line of the citation\n    reason: str  # \"missing\" (zero matches) or \"ambiguous\" (two or more)\n    matches: Sequence[str]  # repo-relative paths matching docs/adr/<n>-*.md\n\n    @property\n    def expected_path(self) -> str:\n        return f\"docs/adr/{self.number}-*.md\"\n\n    def describe(self) -> str:\n        \"\"\"Name the citing file, the cited number, and the missing path.\"\"\"\n        if self.reason == \"missing\":\n            return (\n                f\"{self.citing_file}:{self.line} cites ADR {self.number} \"\n                f\"but no document matches {self.expected_path}\"\n            )\n        joined = \", \".join(self.matches)\n        return (\n            f\"{self.citing_file}:{self.line} cites ADR {self.number} which \"\n            f\"resolves to {len(self.matches)} documents (expected exactly one \"\n            f\"at {self.expected_path}): {joined}\"\n        )\n\n\ndef _adr_index(root: Path) -> dict[str, list[Path]]:\n    \"\"\"Map every four-digit ADR number to the record files that claim it.\"\"\"\n    index: dict[str, list[Path]] = {}\n    adr_dir = root / \"docs\" / \"adr\"\n    if adr_dir.is_dir():\n        for path in sorted(adr_dir.glob(\"*.md\")):\n            match = _ADR_FILENAME.match(path.name)\n            if match:\n                index.setdefault(match.group(1), []).append(path)\n    return index\n\n\ndef _citing_files(root: Path, include_docs: bool = True) -> list[Path]:\n    \"\"\"Files scanned for citations: scripts/*.py, and cheaply the markdown.\"\"\"\n    files = sorted((root / \"scripts\").glob(\"*.py\"))\n    if include_docs:\n        docs = root / \"docs\"\n        if docs.is_dir():\n            files += sorted(docs.rglob(\"*.md\"))\n        files += sorted(root.glob(\"*.md\"))\n    return files\n\n\ndef find_adr_reference_violations(\n    root: Path | str, include_docs: bool = True\n) -> list[Violation]:\n    \"\"\"Return every ``ADR ####`` citation under ``root`` that fails to resolve.\n\n    A citation resolves when exactly one ``docs/adr/####-*.md`` matches its\n    number. Zero matches is a \"missing\" violation; two or more is \"ambiguous\".\n    Records that are never cited produce nothing -- the converse is not\n    required.\n    \"\"\"\n    root = Path(root)\n    index = _adr_index(root)\n    violations: list[Violation] = []\n    for path in _citing_files(root, include_docs=include_docs):\n        try:\n            text = path.read_text(encoding=\"utf-8\")\n        except (OSError, UnicodeDecodeError):\n            continue\n        rel = path.relative_to(root).as_posix()\n        for lineno, line in enumerate(text.splitlines(), start=1):\n            for match in _CITATION.finditer(line):\n                number = match.group(1)\n                found = index.get(number, [])\n                if len(found) == 1:\n                    continue\n                reason = \"missing\" if not found else \"ambiguous\"\n                rels = [p.relative_to(root).as_posix() for p in found]\n                violations.append(Violation(rel, number, lineno, reason, rels))\n    return violations\n\n\nclass RealRepoAdrReferences(unittest.TestCase):\n    \"\"\"Bind this repository's citations to its decision records.\"\"\"\n\n    def test_every_cited_adr_resolves(self) -> None:\n        violations = find_adr_reference_violations(REPO_ROOT)\n        detail = \"\\n\".join(v.describe() for v in violations)\n        self.assertEqual(\n            violations,\n            [],\n            \"Every 'ADR ####' citation must resolve to exactly one \"\n            f\"docs/adr/####-*.md:\\n{detail}\",\n        )\n\n    def test_scan_actually_reads_scripts(self) -> None:\n        # Guard against a silent pass caused by scanning nothing: the scan must\n        # include scripts/*.py, the surface the acceptance check names.\n        files = _citing_files(REPO_ROOT)\n        self.assertTrue(\n            any(f.match(\"scripts/*.py\") for f in files),\n            \"the citation scan must cover scripts/*.py\",\n        )\n\n    def test_carried_records_are_indexed(self) -> None:\n        # The two records this Goal carried onto the default branch, and which\n        # the scripts cite most, must be present.\n        index = _adr_index(REPO_ROOT)\n        self.assertIn(\"0016\", index)\n        self.assertIn(\"0017\", index)\n\n\nclass FixtureAdrReferences(unittest.TestCase):\n    \"\"\"Prove the check fails on a broken citation -- not merely passes today.\"\"\"\n\n    def _make_repo(self, adr_names: Sequence[str], script_body: str) -> Path:\n        tmp = tempfile.mkdtemp()\n        self.addCleanup(shutil.rmtree, tmp, ignore_errors=True)\n        root = Path(tmp)\n        (root / \"scripts\").mkdir()\n        (root / \"docs\" / \"adr\").mkdir(parents=True)\n        for name in adr_names:\n            (root / \"docs\" / \"adr\" / name).write_text(\"# stub\\n\", encoding=\"utf-8\")\n        (root / \"scripts\" / \"sample.py\").write_text(script_body, encoding=\"utf-8\")\n        return root\n\n    def test_fails_when_citation_has_no_document(self) -> None:\n        # A script cites ADR 9991, but no docs/adr/9991-*.md exists.\n        root = self._make_repo(\n            adr_names=[\"0001-real.md\"],\n            script_body='\"\"\"See ADR 9991 for the rule.\"\"\"\\n',\n        )\n        violations = find_adr_reference_violations(root, include_docs=False)\n        self.assertEqual(len(violations), 1)\n        found = violations[0]\n        self.assertEqual(found.number, \"9991\")\n        self.assertEqual(found.reason, \"missing\")\n        message = found.describe()\n        self.assertIn(\"scripts/sample.py\", message)  # citing file\n        self.assertIn(\"9991\", message)  # cited number\n        self.assertIn(\"docs/adr/9991-*.md\", message)  # missing path\n\n    def test_uncited_adr_is_allowed(self) -> None:\n        # An ADR exists but nothing cites it -> not a violation. The converse\n        # (every ADR must be cited) is explicitly NOT required.\n        root = self._make_repo(\n            adr_names=[\"0001-real.md\", \"0042-never-cited.md\"],\n            script_body='\"\"\"Only mentions ADR 0001 here.\"\"\"\\n',\n        )\n        self.assertEqual(\n            find_adr_reference_violations(root, include_docs=False), []\n        )\n\n    def test_ambiguous_when_two_documents_share_a_number(self) -> None:\n        # \"Exactly one\" -- two records claiming the same number is a violation.\n        root = self._make_repo(\n            adr_names=[\"0007-a.md\", \"0007-b.md\"],\n            script_body='\"\"\"Cites ADR 0007 once.\"\"\"\\n',\n        )\n        violations = find_adr_reference_violations(root, include_docs=False)\n        self.assertEqual(len(violations), 1)\n        self.assertEqual(violations[0].reason, \"ambiguous\")\n\n    def test_passes_when_citation_resolves(self) -> None:\n        root = self._make_repo(\n            adr_names=[\"0016-real.md\"],\n            script_body='\"\"\"Cites ADR 0016 which exists.\"\"\"\\n',\n        )\n        self.assertEqual(\n            find_adr_reference_violations(root, include_docs=False), []\n        )\n\n\nif __name__ == \"__main__\":\n    unittest.main()\n\nexec\n/bin/zsh -lc \"sed -n '1,360p' tests/test_install_force_semantics.py\" in /private/tmp/shiki-adr-collision.pa0KwY/.worktrees/shiki-t-20260807t073307221254z-2d22d654-introduc\n succeeded in 0ms:\n\"\"\"Three-way ``--force`` install semantics: nothing lost, nothing silently stale.\n\nThese tests import the real ``scripts/`` modules and assert their observed\nbehavior against the upgrade contract for T-...2411811e. Dependency-free\n``unittest`` only.\n\nThe three-way split under ``--force`` is:\n\n* PROJECT CONTENT (``CONTEXT.md``, ``AGENTS.md``, ``CLAUDE.md``,\n  ``.github/CODEOWNERS``) is never overwritten; the incoming template is written\n  alongside as ``<file>.new`` and reported.\n* GOVERNANCE CONTRACT (``.shiki/config.yaml``, ``.shiki/guardian-policy.json``)\n  is never silently kept; ``<file>.new`` is written and a per-key summary names\n  what differs, calling out ``mergegate.required_checks`` and ``approval_sources``.\n* ``.shiki/migrations/state.json`` is preserved outright (no ``.new``).\n* Everything else is overwritten under ``--force``.\n\nA target with pending migrations is refused with NO file written.\n\"\"\"\n\nfrom __future__ import annotations\n\nimport contextlib\nimport hashlib\nimport json\nimport shutil\nimport tempfile\nimport unittest\nfrom pathlib import Path\n\nimport shiki_test_support  # noqa: F401  (path bootstrap)\n\nimport shiki_contracts\nimport shiki_installer\nimport validate_shiki\nfrom shiki_migrations import MEMORIES_MIGRATION_ID, migration_status\nfrom shiki_process import ShikiError\n\nPROJECT_CONTENT = (\"CONTEXT.md\", \"AGENTS.md\", \"CLAUDE.md\", \".github/CODEOWNERS\")\nGOVERNANCE = (\".shiki/config.yaml\", \".shiki/guardian-policy.json\")\nMIGRATION_STATE = \".shiki/migrations/state.json\"\n\n# A config.yaml whose mergegate.required_checks differs from the shipped template\n# (the platform ships four checks; this keeps only one), parseable by the same\n# subset parser bootstrap uses.\nMODIFIED_CONFIG = (\n    \"mergegate:\\n\"\n    \"  required_checks:\\n\"\n    \"    - Validate Shiki mirror\\n\"\n)\n# A guardian-policy.json whose approval_sources differs from the shipped template.\nMODIFIED_GUARDIAN = json.dumps(\n    {\"version\": 1, \"approval_sources\": {\"github_review\": {\"enabled\": False}}},\n    indent=2,\n) + \"\\n\"\n\n\n@contextlib.contextmanager\ndef _captured_output():\n    info_lines: list[str] = []\n    warn_lines: list[str] = []\n    orig_info = shiki_installer.info\n    orig_warn = shiki_installer.warn\n    shiki_installer.info = lambda message, *a, **k: info_lines.append(str(message))\n    shiki_installer.warn = lambda message, *a, **k: warn_lines.append(str(message))\n    try:\n        yield info_lines, warn_lines\n    finally:\n        shiki_installer.info = orig_info\n        shiki_installer.warn = orig_warn\n\n\ndef _hash_tree(root: Path) -> dict[str, str]:\n    digests: dict[str, str] = {}\n    for path in sorted(root.rglob(\"*\")):\n        if path.is_file():\n            digests[str(path.relative_to(root))] = hashlib.sha256(\n                path.read_bytes()\n            ).hexdigest()\n    return digests\n\n\nclass ForceSemanticsTests(unittest.TestCase):\n    \"\"\"A fully installed target, then a ``--force`` refresh over it.\"\"\"\n\n    def setUp(self) -> None:\n        self.tmp = Path(tempfile.mkdtemp(prefix=\"shiki-force-\"))\n        self.addCleanup(lambda: shutil.rmtree(self.tmp, ignore_errors=True))\n        self.target = self.tmp / \"target\"\n        self.target.mkdir()\n        # Fresh install (no --force): lays down the full template surface.\n        with _captured_output():\n            shiki_installer.install_template(self.target, force=False, validate=False)\n\n    def _template_text(self, relative: str) -> str:\n        return (shiki_installer.ROOT / relative).read_text(encoding=\"utf-8\")\n\n    def test_without_force_nothing_existing_changes(self) -> None:\n        before = _hash_tree(self.target)\n        with _captured_output():\n            shiki_installer.install_template(self.target, force=False, validate=False)\n        after = _hash_tree(self.target)\n        self.assertEqual(before, after, \"a no-force re-run must change nothing\")\n        self.assertFalse(\n            any(p.name.endswith(\".new\") for p in self.target.rglob(\"*\")),\n            \"no .new file may be written without --force\",\n        )\n\n    def test_force_keeps_project_content_and_writes_new_per_file(self) -> None:\n        for relative in PROJECT_CONTENT:\n            (self.target / relative).write_text(f\"LOCAL EDIT {relative}\\n\", encoding=\"utf-8\")\n\n        with _captured_output() as (info_lines, _):\n            shiki_installer.install_template(self.target, force=True, validate=False)\n\n        for relative in PROJECT_CONTENT:\n            self.assertEqual(\n                (self.target / relative).read_text(encoding=\"utf-8\"),\n                f\"LOCAL EDIT {relative}\\n\",\n                f\"{relative} must not be overwritten by --force\",\n            )\n            new_path = self.target / (relative + \".new\")\n            self.assertTrue(new_path.exists(), f\"{relative}.new must be written\")\n            self.assertEqual(\n                new_path.read_text(encoding=\"utf-8\"),\n                self._template_text(relative),\n                f\"{relative}.new must carry the incoming template\",\n            )\n            self.assertTrue(\n                any(f\"{relative}.new\" in line for line in info_lines),\n                f\"summary must list {relative}.new\",\n            )\n\n    def test_force_keeps_governance_writes_new_and_names_keys(self) -> None:\n        (self.target / \".shiki\" / \"config.yaml\").write_text(MODIFIED_CONFIG, encoding=\"utf-8\")\n        (self.target / \".shiki\" / \"guardian-policy.json\").write_text(\n            MODIFIED_GUARDIAN, encoding=\"utf-8\"\n        )\n\n        with _captured_output() as (info_lines, _):\n            shiki_installer.install_template(self.target, force=True, validate=False)\n\n        # Content kept; .new written with the incoming template.\n        self.assertEqual(\n            (self.target / \".shiki\" / \"config.yaml\").read_text(encoding=\"utf-8\"),\n            MODIFIED_CONFIG,\n        )\n        self.assertEqual(\n            (self.target / \".shiki\" / \"guardian-policy.json\").read_text(encoding=\"utf-8\"),\n            MODIFIED_GUARDIAN,\n        )\n        for relative in GOVERNANCE:\n            new_path = self.target / (relative + \".new\")\n            self.assertTrue(new_path.exists(), f\"{relative}.new must be written\")\n            self.assertEqual(\n                new_path.read_text(encoding=\"utf-8\"),\n                self._template_text(relative),\n            )\n\n        summary = \"\\n\".join(info_lines)\n        # The two governance files appear in the summary with the critical\n        # differing keys named explicitly.\n        config_line = next(\n            (line for line in info_lines if \".shiki/config.yaml.new\" in line), \"\"\n        )\n        guardian_line = next(\n            (line for line in info_lines if \".shiki/guardian-policy.json.new\" in line), \"\"\n        )\n        self.assertIn(\"mergegate.required_checks\", config_line)\n        self.assertIn(\"approval_sources\", guardian_line)\n        self.assertIn(\".shiki/config.yaml.new\", summary)\n        self.assertIn(\".shiki/guardian-policy.json.new\", summary)\n\n    def test_force_preserves_migration_state_without_new(self) -> None:\n        state_before = (self.target / MIGRATION_STATE).read_text(encoding=\"utf-8\")\n        with _captured_output():\n            shiki_installer.install_template(self.target, force=True, validate=False)\n        self.assertEqual(\n            (self.target / MIGRATION_STATE).read_text(encoding=\"utf-8\"),\n            state_before,\n            \"migration state must be preserved outright\",\n        )\n        self.assertFalse(\n            (self.target / (MIGRATION_STATE + \".new\")).exists(),\n            \"migration state must not gain a .new\",\n        )\n\n    def test_force_overwrites_every_other_shipped_file(self) -> None:\n        overwritten = (\".shiki/README.md\", \"SYSTEM_PROMPT.md\")\n        for relative in overwritten:\n            (self.target / relative).write_text(\"STALE LOCAL EDIT\\n\", encoding=\"utf-8\")\n\n        with _captured_output():\n            shiki_installer.install_template(self.target, force=True, validate=False)\n\n        for relative in overwritten:\n            self.assertEqual(\n                (self.target / relative).read_text(encoding=\"utf-8\"),\n                self._template_text(relative),\n                f\"{relative} must be overwritten under --force\",\n            )\n            self.assertFalse(\n                (self.target / (relative + \".new\")).exists(),\n                f\"{relative} must not gain a .new (it is overwritten, not preserved)\",\n            )\n\n    def test_summary_lists_every_new_written(self) -> None:\n        for relative in PROJECT_CONTENT:\n            (self.target / relative).write_text(\"edit\\n\", encoding=\"utf-8\")\n        (self.target / \".shiki\" / \"config.yaml\").write_text(MODIFIED_CONFIG, encoding=\"utf-8\")\n        (self.target / \".shiki\" / \"guardian-policy.json\").write_text(\n            MODIFIED_GUARDIAN, encoding=\"utf-8\"\n        )\n\n        with _captured_output() as (info_lines, _):\n            shiki_installer.install_template(self.target, force=True, validate=False)\n\n        summary = \"\\n\".join(info_lines)\n        for relative in (*PROJECT_CONTENT, *GOVERNANCE):\n            self.assertIn(f\"{relative}.new\", summary, f\"summary omitted {relative}.new\")\n\n\nclass PendingMigrationRefusalTests(unittest.TestCase):\n    \"\"\"A target whose valid migration state is behind the registry is refused\n    with NO file written.\"\"\"\n\n    def setUp(self) -> None:\n        self.tmp = Path(tempfile.mkdtemp(prefix=\"shiki-pending-\"))\n        self.addCleanup(lambda: shutil.rmtree(self.tmp, ignore_errors=True))\n        self.target = self.tmp / \"target\"\n        (self.target / \".shiki\" / \"migrations\").mkdir(parents=True)\n\n        # Take the real, valid migration state and drop the last applied\n        # migration so exactly one is pending while the state stays valid.\n        real = json.loads(\n            (shiki_installer.ROOT / MIGRATION_STATE).read_text(encoding=\"utf-8\")\n        )\n        real[\"applied\"] = [\n            record for record in real[\"applied\"] if record.get(\"id\") != MEMORIES_MIGRATION_ID\n        ]\n        (self.target / MIGRATION_STATE).write_text(\n            json.dumps(real, indent=2) + \"\\n\", encoding=\"utf-8\"\n        )\n        # Pre-existing project + governance content that must stay untouched.\n        (self.target / \"CONTEXT.md\").write_text(\"do not touch\\n\", encoding=\"utf-8\")\n\n    def test_pending_state_is_actually_pending(self) -> None:\n        status = migration_status(self.target)\n        self.assertTrue(status[\"state_exists\"])\n        self.assertEqual(status[\"errors\"], [], \"the behind state must stay valid\")\n        self.assertIn(MEMORIES_MIGRATION_ID, status[\"pending\"])\n\n    def test_refused_with_nothing_written(self) -> None:\n        before = _hash_tree(self.target)\n        with _captured_output():\n            with self.assertRaises(ShikiError):\n                shiki_installer.install_template(self.target, force=True, validate=False)\n        after = _hash_tree(self.target)\n        self.assertEqual(before, after, \"no file may be written when refused\")\n        self.assertFalse(\n            any(p.name.endswith(\".new\") for p in self.target.rglob(\"*\")),\n            \"no .new file may be written when refused\",\n        )\n\n\nclass CodeownersResolverTests(unittest.TestCase):\n    \"\"\"The required CODEOWNERS owner resolves per target, not hardcoded.\"\"\"\n\n    def setUp(self) -> None:\n        self.tmp = Path(tempfile.mkdtemp(prefix=\"shiki-codeowners-\"))\n        self.addCleanup(lambda: shutil.rmtree(self.tmp, ignore_errors=True))\n\n    def _target_with_repo(self, repo: str | None) -> Path:\n        target = self.tmp / (repo.replace(\"/\", \"-\") if repo else \"no-repo\")\n        (target / \".shiki\").mkdir(parents=True)\n        if repo is not None:\n            (target / \".shiki\" / \"repo.json\").write_text(\n                json.dumps({\"repo\": repo, \"source_of_truth\": \"github\"}) + \"\\n\",\n                encoding=\"utf-8\",\n            )\n        return target\n\n    def test_owner_resolves_from_repo_json(self) -> None:\n        target = self._target_with_repo(\"acme-org/widgets\")\n        self.assertEqual(\n            shiki_contracts.codeowners_required_owner(target), \"@acme-org\"\n        )\n\n    def test_owner_defaults_when_repo_json_absent(self) -> None:\n        target = self._target_with_repo(None)\n        self.assertEqual(\n            shiki_contracts.codeowners_required_owner(target),\n            shiki_contracts.CODEOWNERS_REQUIRED_OWNER,\n        )\n\n    def test_this_repository_owner_is_unchanged(self) -> None:\n        # This platform repository has no per-target repo.json, so it keeps the\n        # documented default owner.\n        target = self._target_with_repo(\"mizutani-140/shiki\")\n        self.assertEqual(\n            shiki_contracts.codeowners_required_owner(target), \"@mizutani-140\"\n        )\n        self.assertEqual(shiki_contracts.CODEOWNERS_REQUIRED_OWNER, \"@mizutani-140\")\n\n    def test_installed_foreign_owner_target_passes_wired_gate(self) -> None:\n        # Asserting the resolver's return value is not enough: a foreign target\n        # whose repo.json is present at install time gets a CODEOWNERS naming its\n        # own owner, and that installed file passes the wired validate_shiki gate.\n        target = self._target_with_repo(\"acme-org/widgets\")\n        with _captured_output():\n            shiki_installer.install_template(target, force=False, validate=False)\n\n        codeowners = (target / \".github\" / \"CODEOWNERS\").read_text(encoding=\"utf-8\")\n        self.assertIn(\"@acme-org\", codeowners)\n        self.assertNotIn(shiki_contracts.CODEOWNERS_REQUIRED_OWNER, codeowners)\n\n        # No exception == the wired gate accepts the foreign target's own owner.\n        validate_shiki.validate_codeowners_governance(target)\n\n\nif __name__ == \"__main__\":\n    unittest.main()\n\nexec\n/bin/zsh -lc \"sed -n '1,360p' tests/test_bootstrap_safety.py\" in /private/tmp/shiki-adr-collision.pa0KwY/.worktrees/shiki-t-20260807t073307221254z-2d22d654-introduc\n succeeded in 0ms:\n\"\"\"Bootstrap safety invariants: never destroy an existing target, never\nsilently overwrite an operator's stateful files under ``--force``.\n\nThese tests import the real ``scripts/`` modules and assert their observed\nbehavior against the safety contract for T-...d36555d8 (\"never destroy an\nexisting target; never silently ...\"). Dependency-free ``unittest`` only.\n\"\"\"\n\nfrom __future__ import annotations\n\nimport shutil\nimport subprocess\nimport tempfile\nimport unittest\nfrom pathlib import Path\nfrom types import SimpleNamespace\n\nimport shiki_test_support  # noqa: F401  (path bootstrap)\n\nimport shiki_git\nimport shiki_installer\nfrom shiki_process import ShikiError\n\n\ndef _git(*args: str, cwd: Path) -> None:\n    subprocess.run([\"git\", *args], cwd=str(cwd), check=True, capture_output=True, text=True)\n\n\ndef _git_out(*args: str, cwd: Path) -> str:\n    return subprocess.run(\n        [\"git\", *args], cwd=str(cwd), check=True, capture_output=True, text=True\n    ).stdout.strip()\n\n\ndef _init_repo(path: Path) -> None:\n    path.mkdir(parents=True, exist_ok=True)\n    _git(\"init\", \"-b\", \"main\", cwd=path)\n    _git(\"config\", \"user.email\", \"shiki@example.test\", cwd=path)\n    _git(\"config\", \"user.name\", \"Shiki Test\", cwd=path)\n\n\ndef _commit(path: Path, filename: str, content: str, message: str) -> str:\n    (path / filename).write_text(content, encoding=\"utf-8\")\n    _git(\"add\", filename, cwd=path)\n    _git(\"commit\", \"-m\", message, cwd=path)\n    return _git_out(\"rev-parse\", \"HEAD\", cwd=path)\n\n\nclass CheckoutBranchSafetyTests(unittest.TestCase):\n    \"\"\"checkout_branch must never move (reset) an existing target branch.\"\"\"\n\n    def setUp(self) -> None:\n        self.tmp = Path(tempfile.mkdtemp(prefix=\"shiki-checkout-\"))\n        self.addCleanup(lambda: shutil.rmtree(self.tmp, ignore_errors=True))\n        self.repo = self.tmp / \"repo\"\n        _init_repo(self.repo)\n\n    def test_no_op_when_already_on_branch(self) -> None:\n        _commit(self.repo, \"a.txt\", \"a\", \"A\")\n        before = _git_out(\"rev-parse\", \"main\", cwd=self.repo)\n        shiki_git.checkout_branch(self.repo, \"main\")\n        self.assertEqual(_git_out(\"rev-parse\", \"main\", cwd=self.repo), before)\n        self.assertEqual(_git_out(\"rev-parse\", \"--abbrev-ref\", \"HEAD\", cwd=self.repo), \"main\")\n\n    def test_existing_branch_is_not_reset(self) -> None:\n        main_sha = _commit(self.repo, \"a.txt\", \"a\", \"A\")  # main -> A\n        _git(\"checkout\", \"-b\", \"other\", cwd=self.repo)\n        _commit(self.repo, \"b.txt\", \"b\", \"B\")  # other -> B, HEAD on other\n        # Switching to an already-existing main must NOT move it; a `-B main`\n        # would reset the ref to B and destroy the commit it pointed at.\n        shiki_git.checkout_branch(self.repo, \"main\")\n        self.assertEqual(_git_out(\"rev-parse\", \"main\", cwd=self.repo), main_sha)\n        self.assertEqual(_git_out(\"rev-parse\", \"HEAD\", cwd=self.repo), main_sha)\n        reflog = subprocess.run(\n            [\"git\", \"reflog\", \"show\", \"main\"], cwd=str(self.repo), capture_output=True, text=True\n        ).stdout\n        self.assertNotIn(\"reset\", reflog.lower())\n\n    def test_missing_branch_is_created_at_head(self) -> None:\n        head = _commit(self.repo, \"a.txt\", \"a\", \"A\")\n        shiki_git.checkout_branch(self.repo, \"feature\")\n        self.assertEqual(_git_out(\"rev-parse\", \"--abbrev-ref\", \"HEAD\", cwd=self.repo), \"feature\")\n        self.assertEqual(_git_out(\"rev-parse\", \"feature\", cwd=self.repo), head)\n\n    def test_failed_checkout_raises_shiki_error(self) -> None:\n        _commit(self.repo, \"a.txt\", \"a\", \"A\")\n        _git(\"checkout\", \"-b\", \"other\", cwd=self.repo)\n        original_run = shiki_git.run\n        try:\n            shiki_git.run = lambda *a, **k: SimpleNamespace(returncode=1, stdout=\"\", stderr=\"boom\")\n            with self.assertRaises(ShikiError):\n                shiki_git.checkout_branch(self.repo, \"main\")\n        finally:\n            shiki_git.run = original_run\n\n\nclass InstallerPreserveUnderForceTests(unittest.TestCase):\n    \"\"\"install --force must never clobber an operator's stateful files.\"\"\"\n\n    STATEFUL = {\n        \".shiki/guardian-policy.json\": '{\"shiki_test_custom_guardian\": true}\\n',\n        \".shiki/config.yaml\": \"shiki_test_custom_config: true\\n\",\n        \".shiki/migrations/state.json\": '{\"shiki_test_custom_state\": true}\\n',\n    }\n\n    def setUp(self) -> None:\n        self.tmp = Path(tempfile.mkdtemp(prefix=\"shiki-installer-\"))\n        self.addCleanup(lambda: shutil.rmtree(self.tmp, ignore_errors=True))\n        self.target = self.tmp / \"target\"\n        self.target.mkdir()\n        self.warnings: list[str] = []\n        self._orig_warn = shiki_installer.warn\n        self._orig_info = shiki_installer.info\n        shiki_installer.warn = lambda message, *a, **k: self.warnings.append(str(message))\n        shiki_installer.info = lambda *a, **k: None\n        self.addCleanup(self._restore)\n        shiki_installer.install_template(self.target, force=False, validate=False)\n\n    def _restore(self) -> None:\n        shiki_installer.warn = self._orig_warn\n        shiki_installer.info = self._orig_info\n\n    def test_force_preserves_operator_stateful_files(self) -> None:\n        for relative, content in self.STATEFUL.items():\n            path = self.target / relative\n            path.parent.mkdir(parents=True, exist_ok=True)\n            path.write_text(content, encoding=\"utf-8\")\n\n        self.warnings.clear()\n        shiki_installer.install_template(self.target, force=True, validate=False)\n\n        for relative, content in self.STATEFUL.items():\n            self.assertEqual(\n                (self.target / relative).read_text(encoding=\"utf-8\"),\n                content,\n                f\"{relative} was overwritten by --force\",\n            )\n            filename = relative.rsplit(\"/\", 1)[-1]\n            self.assertTrue(\n                any(\"preserv\" in w.lower() and filename in w for w in self.warnings),\n                f\"expected a 'preserved' warning naming {filename}; got {self.warnings}\",\n            )\n\n    def test_force_still_overwrites_non_stateful_template_files(self) -> None:\n        # A non-stateful template file (README) must still be refreshed by\n        # --force; the preserve carve-out is limited to operator state.\n        readme = self.target / \".shiki\" / \"README.md\"\n        if not readme.exists():  # pragma: no cover - README ships in the template\n            self.skipTest(\".shiki/README.md is not part of the template\")\n        readme.write_text(\"STALE-LOCAL-EDIT\\n\", encoding=\"utf-8\")\n        shiki_installer.install_template(self.target, force=True, validate=False)\n        self.assertNotIn(\"STALE-LOCAL-EDIT\", readme.read_text(encoding=\"utf-8\"))\n\n\nif __name__ == \"__main__\":\n    unittest.main()\n\nexec\n/bin/zsh -lc \"sed -n '381,820p' scripts/shiki_installer.py\" in /private/tmp/shiki-adr-collision.pa0KwY/.worktrees/shiki-t-20260807t073307221254z-2d22d654-introduc\n succeeded in 0ms:\n\n    if target.exists():\n        if not force:\n            warn(f\"kept existing file: {target}\")\n            return\n        category = force_category(source)\n        if category == \"preserve-outright\":\n            warn(f\"preserved existing stateful file (not overwritten by --force): {target}\")\n            return\n        if category in (\"project-content\", \"governance-contract\"):\n            differing = (\n                governance_differing_keys(source, target)\n                if category == \"governance-contract\"\n                else ()\n            )\n            note = write_new_alongside(source, target, category=category, differing_keys=differing)\n            if notes is not None:\n                notes.append(note)\n            return\n\n    target.parent.mkdir(parents=True, exist_ok=True)\n    if (\n        target_install\n        and codeowners_owner\n        and codeowners_owner != CODEOWNERS_REQUIRED_OWNER\n        and template_relative(source) == CODEOWNERS_PATH\n    ):\n        # Substitute the target's own owner for the shipped maintainer so the\n        # target's CODEOWNERS check resolves against its own owner. Only the\n        # owner token changes; when it equals the fallback the file is copied\n        # verbatim above, preserving byte-for-byte behaviour for this repo.\n        text = source.read_text(encoding=\"utf-8\").replace(CODEOWNERS_REQUIRED_OWNER, codeowners_owner)\n        target.write_text(text, encoding=\"utf-8\")\n    else:\n        shutil.copy2(source, target)\n    info(f\"installed {target}\")\n\n\ndef write_new_alongside(\n    source: Path,\n    target: Path,\n    *,\n    category: str,\n    differing_keys: tuple[str, ...] = (),\n) -> NewFileNote:\n    \"\"\"Keep ``target`` untouched and write the incoming template as ``<target>.new``.\"\"\"\n    new_path = target.parent / (target.name + \".new\")\n    new_path.parent.mkdir(parents=True, exist_ok=True)\n    shutil.copy2(source, new_path)\n    warn(\n        f\"preserved existing {category} file (not overwritten by --force); \"\n        f\"wrote {new_path.name} for review: {target}\"\n    )\n    relative = template_relative(source) or target.name\n    return NewFileNote(\n        relative=relative,\n        new_relative=f\"{relative}.new\",\n        category=category,\n        differing_keys=tuple(differing_keys),\n    )\n\n\n_MISSING = object()\n\n\ndef _flatten_leaf_keys(value: Any, prefix: str = \"\") -> dict[str, Any]:\n    \"\"\"Flatten a nested mapping to dotted leaf keys; lists are leaves.\"\"\"\n    out: dict[str, Any] = {}\n    if isinstance(value, dict):\n        for key in value:\n            child = f\"{prefix}.{key}\" if prefix else str(key)\n            out.update(_flatten_leaf_keys(value[key], child))\n        return out\n    out[prefix] = value\n    return out\n\n\ndef _load_governance_data(relative: str | None, path: Path) -> dict[str, Any]:\n    if relative == \".shiki/config.yaml\":\n        # Reuse the subset YAML parser bootstrap already owns; it reads\n        # ``<dir>/.shiki/config.yaml`` and captures mergegate.required_checks.\n        return load_shiki_config(path.parent.parent)\n    try:\n        data = json.loads(path.read_text(encoding=\"utf-8\"))\n    except (OSError, json.JSONDecodeError):\n        return {}\n    return data if isinstance(data, dict) else {}\n\n\ndef governance_differing_keys(source: Path, target: Path) -> tuple[str, ...]:\n    \"\"\"Dotted keys that differ between the incoming template and the kept target.\n\n    Differences under a critical key (``mergegate.required_checks`` for config,\n    ``approval_sources`` for guardian policy) collapse to that key so it is named\n    explicitly, while other differences keep their leaf path.\n    \"\"\"\n    relative = template_relative(source)\n    template_data = _load_governance_data(relative, source)\n    target_data = _load_governance_data(relative, target)\n    template_leaves = _flatten_leaf_keys(template_data)\n    target_leaves = _flatten_leaf_keys(target_data)\n    keys = set(template_leaves) | set(target_leaves)\n    differing = [\n        key\n        for key in keys\n        if template_leaves.get(key, _MISSING) != target_leaves.get(key, _MISSING)\n    ]\n    critical = GOVERNANCE_CRITICAL_KEYS.get(relative or \"\", ())\n    collapsed: set[str] = set()\n    for leaf in differing:\n        mapped = leaf\n        for crit in critical:\n            if leaf == crit or leaf.startswith(f\"{crit}.\"):\n                mapped = crit\n                break\n        collapsed.add(mapped)\n    return tuple(sorted(collapsed))\n\n\ndef _sha256_file(path: Path) -> str:\n    digest = hashlib.sha256()\n    digest.update(path.read_bytes())\n    return digest.hexdigest()\n\n\ndef _is_stamped_shipped(relative: str) -> bool:\n    \"\"\"Whether a shipped path belongs in the install stamp's digest set.\n\n    Only *platform-owned* content is stamped: files that an upgrade overwrites\n    verbatim, so a difference proves a hand-edit or a half-upgrade. Project\n    content, governance contracts, and preserve-outright state are customized\n    per target, so a difference there is expected and is never drift.\n    \"\"\"\n    if relative == INSTALL_STAMP_PATH:\n        return False\n    return (\n        relative not in PROJECT_CONTENT_FILES\n        and relative not in GOVERNANCE_CONTRACT_FILES\n        and relative not in PRESERVE_OUTRIGHT_FILES\n    )\n\n\ndef shipped_stamp_paths() -> list[str]:\n    \"\"\"Relative POSIX paths of every platform-owned file an install writes.\n\n    Enumerated from the platform (``ROOT``) with the same skip rules install\n    uses, so the set names exactly the shipped surface that a target must match.\n    \"\"\"\n    shipped: list[str] = []\n\n    def collect(source: Path) -> None:\n        if should_skip(source, target_install=True):\n            return\n        if source.is_dir():\n            for child in sorted(source.iterdir()):\n                collect(child)\n            return\n        relative = template_relative(source)\n        if relative is None or not _is_stamped_shipped(relative):\n            return\n        shipped.append(relative)\n\n    for relative in TEMPLATE_PATHS:\n        source = ROOT / relative\n        if source.exists():\n            collect(source)\n    return sorted(set(shipped))\n\n\ndef target_stamp_digests(target: Path) -> dict[str, str]:\n    \"\"\"Content digest for every stamped shipped path present in ``target``.\"\"\"\n    return {\n        relative: _sha256_file(target / relative)\n        for relative in shipped_stamp_paths()\n        if (target / relative).is_file()\n    }\n\n\ndef platform_commit(root: Path = ROOT) -> str | None:\n    \"\"\"The git HEAD of the running platform checkout, or None when unavailable.\"\"\"\n    result = subprocess.run(\n        [\"git\", \"-C\", str(root), \"rev-parse\", \"HEAD\"],\n        capture_output=True,\n        text=True,\n        check=False,\n    )\n    commit = result.stdout.strip()\n    return commit if result.returncode == 0 and commit else None\n\n\ndef commit_is_ancestor(root: Path, ancestor: str, descendant: str) -> bool | None:\n    \"\"\"True if ``ancestor`` is an ancestor of ``descendant`` in ``root``'s git.\n\n    None when the answer cannot be determined (git missing, or a commit unknown\n    to this checkout \u2014 which is itself the \"different lineage\" signal).\n    \"\"\"\n    if not ancestor or not descendant:\n        return None\n    result = subprocess.run(\n        [\"git\", \"-C\", str(root), \"merge-base\", \"--is-ancestor\", ancestor, descendant],\n        capture_output=True,\n        text=True,\n        check=False,\n    )\n    if result.returncode == 0:\n        return True\n    if result.returncode == 1:\n        return False\n    return None\n\n\ndef _incoming_is_older(incoming_commit: str | None, existing_commit: str | None) -> bool:\n    \"\"\"Whether a fresh install is provably older than the recorded stamp.\n\n    Only a *proven* older lineage \u2014 the incoming platform commit is a strict\n    ancestor of the recorded one \u2014 blocks the overwrite. Same commit, unrelated\n    lineages, or an undeterminable relationship all refresh, so the guard never\n    silently keeps stale content it cannot prove is newer.\n    \"\"\"\n    if not incoming_commit or not existing_commit or incoming_commit == existing_commit:\n        return False\n    return commit_is_ancestor(ROOT, incoming_commit, existing_commit) is True\n\n\ndef _ensure_stamp_gitignored(target: Path) -> None:\n    \"\"\"Keep the untracked install stamp out of ``git add`` / ``git add -A``.\n\n    The stamp is target-local state that is never committed: validate_shiki\n    rejects an unknown *tracked* ``.shiki/`` path, and the goal loop stages\n    worktrees with ``git add -A``, so an un-ignored stamp would be committed and\n    make the target's own Validate check permanently red. The shipped\n    ``.gitignore`` is platform-owned and cannot name it, so the installer\n    ensures the target's own ``.gitignore`` ignores it. Idempotent: a re-install\n    never appends the entry twice.\n    \"\"\"\n    gitignore = target / \".gitignore\"\n    existing = gitignore.read_text(encoding=\"utf-8\") if gitignore.is_file() else \"\"\n    if INSTALL_STAMP_PATH in {line.strip() for line in existing.splitlines()}:\n        return\n    prefix = \"\" if not existing or existing.endswith(\"\\n\") else \"\\n\"\n    addition = (\n        f\"{prefix}\\n# Local-only install version stamp (see `shiki doctor` drift checks)\\n\"\n        f\"{INSTALL_STAMP_PATH}\\n\"\n    )\n    with gitignore.open(\"a\", encoding=\"utf-8\") as handle:\n        handle.write(addition)\n\n\ndef read_install_stamp(target: Path) -> dict[str, Any] | None:\n    \"\"\"Parse a target's install stamp, or None when absent/unreadable.\"\"\"\n    path = target / INSTALL_STAMP_PATH\n    try:\n        data = json.loads(path.read_text(encoding=\"utf-8\"))\n    except (OSError, json.JSONDecodeError):\n        return None\n    return data if isinstance(data, dict) else None\n\n\ndef write_install_stamp(target: Path) -> None:\n    \"\"\"Write (or refresh) the target's install stamp.\n\n    Never overwrites a stamp from a newer platform commit with older content,\n    and leaves an already-current stamp byte-for-byte untouched so a no-op\n    re-install churns nothing.\n    \"\"\"\n    commit = platform_commit(ROOT)\n    digests = target_stamp_digests(target)\n    existing = read_install_stamp(target)\n    if existing is not None:\n        if _incoming_is_older(commit, existing.get(\"platform_commit\")):\n            warn(\n                \"kept existing install stamp: incoming install \"\n                f\"({commit}) is older than the recorded platform commit \"\n                f\"({existing.get('platform_commit')}); not overwriting {INSTALL_STAMP_PATH}\"\n            )\n            return\n        if existing.get(\"platform_commit\") == commit and existing.get(\"digests\") == digests:\n            return\n    stamp = {\n        \"version\": INSTALL_STAMP_VERSION,\n        \"platform_commit\": commit,\n        \"installed_at\": utc_now(),\n        \"digests\": digests,\n    }\n    path = target / INSTALL_STAMP_PATH\n    path.parent.mkdir(parents=True, exist_ok=True)\n    path.write_text(json.dumps(stamp, indent=2, sort_keys=True) + \"\\n\", encoding=\"utf-8\")\n    info(f\"wrote install stamp: {path}\")\n\n\ndef cmd_install_target(args: argparse.Namespace) -> int:\n    target = Path(args.target).expanduser().resolve()\n    if not target.exists():\n        raise ShikiError(f\"target does not exist: {target}\")\n    if not target.is_dir():\n        raise ShikiError(f\"target is not a directory: {target}\")\n\n    if not args.local_only:\n        raise ShikiError(\"install-target is template-only; use shiki init TARGET --repo OWNER/NAME for GitHub-first setup, or pass --local-only explicitly\")\n\n    install_template(target, force=args.force, validate=args.validate)\n\n    return 0\n\n\ndef cmd_install_command(args: argparse.Namespace) -> int:\n    destination = Path(args.path).expanduser()\n    install_cli_command(destination)\n    info(\"ensure the parent directory is on PATH\")\n    return 0\n\n\ndef install_cli_command(destination: Path) -> None:\n    destination.parent.mkdir(parents=True, exist_ok=True)\n    if destination.exists() or destination.is_symlink():\n        destination.unlink()\n    destination.symlink_to(ROOT / \"bin\" / \"shiki\")\n    info(f\"installed command: {destination}\")\n\n\ndef install_file(source: Path, destination: Path) -> None:\n    if not source.exists():\n        raise ShikiError(f\"source file not found: {source}\")\n    destination.parent.mkdir(parents=True, exist_ok=True)\n    shutil.copy2(source, destination)\n    info(f\"installed {destination}\")\n\n\ndef cmd_install_global(args: argparse.Namespace) -> int:\n    install_cli_command(Path(args.path).expanduser())\n\n    if args.claude_command:\n        install_file(\n            ROOT / \".claude\" / \"commands\" / \"shiki.md\",\n            Path(args.claude_command_path).expanduser(),\n        )\n\n    if args.codex_skill:\n        install_file(\n            ROOT / \".codex\" / \"skills\" / \"shiki\" / \"SKILL.md\",\n            Path(args.codex_skill_path).expanduser(),\n        )\n\n    info(\"global install complete\")\n    info(\"restart Codex or Claude Code if the running client does not reload commands dynamically\")\n    return 0\n\n\ndef cmd_status(_: argparse.Namespace) -> int:\n    config = load_default_config()\n    status = {\n        \"root\": str(ROOT),\n        \"config\": config,\n        \"command\": shutil.which(\"shiki\"),\n        \"claude_command\": str(Path(DEFAULT_CLAUDE_COMMAND_PATH).expanduser()),\n        \"claude_command_installed\": Path(DEFAULT_CLAUDE_COMMAND_PATH).expanduser().exists(),\n        \"codex_skill\": str(Path(DEFAULT_CODEX_SKILL_PATH).expanduser()),\n        \"codex_skill_installed\": Path(DEFAULT_CODEX_SKILL_PATH).expanduser().exists(),\n    }\n    print(json.dumps(status, indent=2, sort_keys=True))\n    return 0\n\n2026-08-07T08:04:30.583754Z ERROR codex_models_manager::manager: failed to renew cache TTL: missing field `base_instructions` at line 94 column 5\nexec\n/bin/zsh -lc \"rg -n '\"'^def |''^class |INSTALL_STAMP|TEMPLATE_PATHS|docs/adr|write_install|digest|copy_path'\"' scripts/shiki_installer.py tests/test_install_*.py tests/test_bootstrap_safety.py\" in /private/tmp/shiki-adr-collision.pa0KwY/.worktrees/shiki-t-20260807t073307221254z-2d22d654-introduc\n succeeded in 0ms:\ntests/test_install_version_drift.py:7:commit, the install time, and a content digest per platform-owned shipped path,\ntests/test_install_version_drift.py:10:* a shipped path whose current content differs from the stamped digest\ntests/test_install_version_drift.py:43:from shiki_installer import INSTALL_STAMP_PATH, _incoming_is_older\ntests/test_install_version_drift.py:48:# A platform-owned shipped path that the stamp always digests (overwrite\ntests/test_install_version_drift.py:51:# Files the stamp must never digest: they are customized per target, so a\ntests/test_install_version_drift.py:56:def _run(*args: str) -> subprocess.CompletedProcess[str]:\ntests/test_install_version_drift.py:64:def _findings(doctor_json: str) -> dict[str, str]:\ntests/test_install_version_drift.py:69:def _finding(report_json: str, finding_id: str) -> dict[str, object]:\ntests/test_install_version_drift.py:77:def _sha256(path: Path) -> str:\ntests/test_install_version_drift.py:78:    return hashlib.sha256(path.read_bytes()).hexdigest()\ntests/test_install_version_drift.py:81:def _content_snapshot(root: Path) -> dict[str, str]:\ntests/test_install_version_drift.py:90:class InstallStampTests(unittest.TestCase):\ntests/test_install_version_drift.py:98:        self.stamp_path = self.target / INSTALL_STAMP_PATH\ntests/test_install_version_drift.py:125:        # Names a content digest per shipped path \u2014 platform-owned paths only.\ntests/test_install_version_drift.py:126:        digests = stamp[\"digests\"]\ntests/test_install_version_drift.py:127:        self.assertIn(SHIPPED_PATH, digests)\ntests/test_install_version_drift.py:128:        self.assertEqual(digests[SHIPPED_PATH], _sha256(self.target / SHIPPED_PATH))\ntests/test_install_version_drift.py:131:                customizable, digests, f\"{customizable} is customized per target; it must not be stamped\"\ntests/test_install_version_drift.py:142:        self.assertNotIn(INSTALL_STAMP_PATH, tracked.splitlines())\ntests/test_install_version_drift.py:151:        # A stale stamp (older platform content: a wrong recorded digest and an\ntests/test_install_version_drift.py:155:        stamp[\"digests\"][SHIPPED_PATH] = \"deadbeef\"\ntests/test_install_version_drift.py:163:        self.assertEqual(refreshed[\"digests\"][SHIPPED_PATH], _sha256(self.target / SHIPPED_PATH))\ntests/test_install_version_drift.py:224:class OlderContentGuardTests(unittest.TestCase):\ntests/test_install_version_drift.py:249:class MigrateForeignTargetTests(unittest.TestCase):\ntests/test_bootstrap_safety.py:25:def _git(*args: str, cwd: Path) -> None:\ntests/test_bootstrap_safety.py:29:def _git_out(*args: str, cwd: Path) -> str:\ntests/test_bootstrap_safety.py:35:def _init_repo(path: Path) -> None:\ntests/test_bootstrap_safety.py:42:def _commit(path: Path, filename: str, content: str, message: str) -> str:\ntests/test_bootstrap_safety.py:49:class CheckoutBranchSafetyTests(unittest.TestCase):\ntests/test_bootstrap_safety.py:97:class InstallerPreserveUnderForceTests(unittest.TestCase):\ntests/test_install_force_semantics.py:59:def _captured_output():\ntests/test_install_force_semantics.py:73:def _hash_tree(root: Path) -> dict[str, str]:\ntests/test_install_force_semantics.py:74:    digests: dict[str, str] = {}\ntests/test_install_force_semantics.py:77:            digests[str(path.relative_to(root))] = hashlib.sha256(\ntests/test_install_force_semantics.py:79:            ).hexdigest()\ntests/test_install_force_semantics.py:80:    return digests\ntests/test_install_force_semantics.py:83:class ForceSemanticsTests(unittest.TestCase):\ntests/test_install_force_semantics.py:223:class PendingMigrationRefusalTests(unittest.TestCase):\ntests/test_install_force_semantics.py:266:class CodeownersResolverTests(unittest.TestCase):\ntests/test_install_completeness.py:14:shipped (in ``TEMPLATE_PATHS`` or under a shipped directory) or on the explicit,\ntests/test_install_completeness.py:32:from shiki_installer import TARGET_INSTALL_EXCLUDES, TEMPLATE_PATHS  # noqa: E402\ntests/test_install_completeness.py:55:def _exists_exact(root: Path, rel: str) -> bool:\ntests/test_install_completeness.py:74:def _py_import_deps(root: Path, path: Path, text: str) -> set[str]:\ntests/test_install_completeness.py:96:def _text_import_deps(root: Path, text: str) -> set[str]:\ntests/test_install_completeness.py:106:def _path_reference_deps(root: Path, text: str) -> set[str]:\ntests/test_install_completeness.py:118:def _discover_deps(root: Path, text: str) -> set[str]:\ntests/test_install_completeness.py:130:def classify(rel: str) -> str | None:\ntests/test_install_completeness.py:141:def dependencies_of(root: Path, rel: str, kind: str) -> set[str]:\ntests/test_install_completeness.py:154:def is_shipped(rel: str, shipped: set[str]) -> bool:\ntests/test_install_completeness.py:165:def find_unshipped_dependencies(\ntests/test_install_completeness.py:182:class InstallCompletenessTest(unittest.TestCase):\ntests/test_install_completeness.py:185:            ROOT, set(TEMPLATE_PATHS), SHIPPING_EXCEPTIONS\ntests/test_install_completeness.py:194:                \"to TEMPLATE_PATHS, or to SHIPPING_EXCEPTIONS with a reason:\\n\"\nscripts/shiki_installer.py:20:TEMPLATE_PATHS = [\nscripts/shiki_installer.py:40:    \"docs/adr\",\nscripts/shiki_installer.py:111:# from, when it was installed, and a content digest for every platform-owned\nscripts/shiki_installer.py:119:INSTALL_STAMP_PATH = \".shiki/install-stamp.json\"\nscripts/shiki_installer.py:120:INSTALL_STAMP_VERSION = 1\nscripts/shiki_installer.py:182:def manifest_stage_paths(path: Path) -> list[str]:\nscripts/shiki_installer.py:183:    candidates = list(TEMPLATE_PATHS)\nscripts/shiki_installer.py:197:def excluded_from_commit(relative: str, patterns: list[str]) -> bool:\nscripts/shiki_installer.py:212:class NewFileNote:\nscripts/shiki_installer.py:221:def refuse_pending_migrations(target: Path) -> None:\nscripts/shiki_installer.py:240:def install_template(target: Path, *, force: bool, validate: bool) -> list[NewFileNote]:\nscripts/shiki_installer.py:254:    for relative in TEMPLATE_PATHS:\nscripts/shiki_installer.py:259:        copy_path(\nscripts/shiki_installer.py:278:    # Stamp the install after the shipped surface is on disk, so the digests\nscripts/shiki_installer.py:282:    write_install_stamp(target)\nscripts/shiki_installer.py:295:def print_new_file_summary(notes: list[NewFileNote]) -> None:\nscripts/shiki_installer.py:311:def should_skip(path: Path, *, target_install: bool = False) -> bool:\nscripts/shiki_installer.py:335:def template_relative(source: Path) -> str | None:\nscripts/shiki_installer.py:343:def force_category(source: Path) -> str:\nscripts/shiki_installer.py:359:def copy_path(\nscripts/shiki_installer.py:372:            copy_path(\nscripts/shiki_installer.py:419:def write_new_alongside(\nscripts/shiki_installer.py:446:def _flatten_leaf_keys(value: Any, prefix: str = \"\") -> dict[str, Any]:\nscripts/shiki_installer.py:458:def _load_governance_data(relative: str | None, path: Path) -> dict[str, Any]:\nscripts/shiki_installer.py:470:def governance_differing_keys(source: Path, target: Path) -> tuple[str, ...]:\nscripts/shiki_installer.py:500:def _sha256_file(path: Path) -> str:\nscripts/shiki_installer.py:501:    digest = hashlib.sha256()\nscripts/shiki_installer.py:502:    digest.update(path.read_bytes())\nscripts/shiki_installer.py:503:    return digest.hexdigest()\nscripts/shiki_installer.py:506:def _is_stamped_shipped(relative: str) -> bool:\nscripts/shiki_installer.py:507:    \"\"\"Whether a shipped path belongs in the install stamp's digest set.\nscripts/shiki_installer.py:514:    if relative == INSTALL_STAMP_PATH:\nscripts/shiki_installer.py:523:def shipped_stamp_paths() -> list[str]:\nscripts/shiki_installer.py:543:    for relative in TEMPLATE_PATHS:\nscripts/shiki_installer.py:550:def target_stamp_digests(target: Path) -> dict[str, str]:\nscripts/shiki_installer.py:551:    \"\"\"Content digest for every stamped shipped path present in ``target``.\"\"\"\nscripts/shiki_installer.py:559:def platform_commit(root: Path = ROOT) -> str | None:\nscripts/shiki_installer.py:571:def commit_is_ancestor(root: Path, ancestor: str, descendant: str) -> bool | None:\nscripts/shiki_installer.py:592:def _incoming_is_older(incoming_commit: str | None, existing_commit: str | None) -> bool:\nscripts/shiki_installer.py:605:def _ensure_stamp_gitignored(target: Path) -> None:\nscripts/shiki_installer.py:618:    if INSTALL_STAMP_PATH in {line.strip() for line in existing.splitlines()}:\nscripts/shiki_installer.py:623:        f\"{INSTALL_STAMP_PATH}\\n\"\nscripts/shiki_installer.py:629:def read_install_stamp(target: Path) -> dict[str, Any] | None:\nscripts/shiki_installer.py:631:    path = target / INSTALL_STAMP_PATH\nscripts/shiki_installer.py:639:def write_install_stamp(target: Path) -> None:\nscripts/shiki_installer.py:647:    digests = target_stamp_digests(target)\nscripts/shiki_installer.py:654:                f\"({existing.get('platform_commit')}); not overwriting {INSTALL_STAMP_PATH}\"\nscripts/shiki_installer.py:657:        if existing.get(\"platform_commit\") == commit and existing.get(\"digests\") == digests:\nscripts/shiki_installer.py:660:        \"version\": INSTALL_STAMP_VERSION,\nscripts/shiki_installer.py:663:        \"digests\": digests,\nscripts/shiki_installer.py:665:    path = target / INSTALL_STAMP_PATH\nscripts/shiki_installer.py:671:def cmd_install_target(args: argparse.Namespace) -> int:\nscripts/shiki_installer.py:686:def cmd_install_command(args: argparse.Namespace) -> int:\nscripts/shiki_installer.py:693:def install_cli_command(destination: Path) -> None:\nscripts/shiki_installer.py:701:def install_file(source: Path, destination: Path) -> None:\nscripts/shiki_installer.py:709:def cmd_install_global(args: argparse.Namespace) -> int:\nscripts/shiki_installer.py:729:def cmd_status(_: argparse.Namespace) -> int:\n\n2026-08-07T08:04:33.978302Z ERROR codex_models_manager::manager: failed to renew cache TTL: missing field `base_instructions` at line 94 column 5\nexec\n/bin/zsh -lc \"sed -n '1,224p' tests/test_install_version_drift.py\" in /private/tmp/shiki-adr-collision.pa0KwY/.worktrees/shiki-t-20260807t073307221254z-2d22d654-introduc\nexec\n/bin/zsh -lc \"sed -n '490,680p' scripts/shiki_installer.py\" in /private/tmp/shiki-adr-collision.pa0KwY/.worktrees/shiki-t-20260807t073307221254z-2d22d654-introduc\nexec\n/bin/zsh -lc \"sed -n '1,240p' tests/test_install_force_semantics.py\" in /private/tmp/shiki-adr-collision.pa0KwY/.worktrees/shiki-t-20260807t073307221254z-2d22d654-introduc\n succeeded in 0ms:\n    for leaf in differing:\n        mapped = leaf\n        for crit in critical:\n            if leaf == crit or leaf.startswith(f\"{crit}.\"):\n                mapped = crit\n                break\n        collapsed.add(mapped)\n    return tuple(sorted(collapsed))\n\n\ndef _sha256_file(path: Path) -> str:\n    digest = hashlib.sha256()\n    digest.update(path.read_bytes())\n    return digest.hexdigest()\n\n\ndef _is_stamped_shipped(relative: str) -> bool:\n    \"\"\"Whether a shipped path belongs in the install stamp's digest set.\n\n    Only *platform-owned* content is stamped: files that an upgrade overwrites\n    verbatim, so a difference proves a hand-edit or a half-upgrade. Project\n    content, governance contracts, and preserve-outright state are customized\n    per target, so a difference there is expected and is never drift.\n    \"\"\"\n    if relative == INSTALL_STAMP_PATH:\n        return False\n    return (\n        relative not in PROJECT_CONTENT_FILES\n        and relative not in GOVERNANCE_CONTRACT_FILES\n        and relative not in PRESERVE_OUTRIGHT_FILES\n    )\n\n\ndef shipped_stamp_paths() -> list[str]:\n    \"\"\"Relative POSIX paths of every platform-owned file an install writes.\n\n    Enumerated from the platform (``ROOT``) with the same skip rules install\n    uses, so the set names exactly the shipped surface that a target must match.\n    \"\"\"\n    shipped: list[str] = []\n\n    def collect(source: Path) -> None:\n        if should_skip(source, target_install=True):\n            return\n        if source.is_dir():\n            for child in sorted(source.iterdir()):\n                collect(child)\n            return\n        relative = template_relative(source)\n        if relative is None or not _is_stamped_shipped(relative):\n            return\n        shipped.append(relative)\n\n    for relative in TEMPLATE_PATHS:\n        source = ROOT / relative\n        if source.exists():\n            collect(source)\n    return sorted(set(shipped))\n\n\ndef target_stamp_digests(target: Path) -> dict[str, str]:\n    \"\"\"Content digest for every stamped shipped path present in ``target``.\"\"\"\n    return {\n        relative: _sha256_file(target / relative)\n        for relative in shipped_stamp_paths()\n        if (target / relative).is_file()\n    }\n\n\ndef platform_commit(root: Path = ROOT) -> str | None:\n    \"\"\"The git HEAD of the running platform checkout, or None when unavailable.\"\"\"\n    result = subprocess.run(\n        [\"git\", \"-C\", str(root), \"rev-parse\", \"HEAD\"],\n        capture_output=True,\n        text=True,\n        check=False,\n    )\n    commit = result.stdout.strip()\n    return commit if result.returncode == 0 and commit else None\n\n\ndef commit_is_ancestor(root: Path, ancestor: str, descendant: str) -> bool | None:\n    \"\"\"True if ``ancestor`` is an ancestor of ``descendant`` in ``root``'s git.\n\n    None when the answer cannot be determined (git missing, or a commit unknown\n    to this checkout \u2014 which is itself the \"different lineage\" signal).\n    \"\"\"\n    if not ancestor or not descendant:\n        return None\n    result = subprocess.run(\n        [\"git\", \"-C\", str(root), \"merge-base\", \"--is-ancestor\", ancestor, descendant],\n        capture_output=True,\n        text=True,\n        check=False,\n    )\n    if result.returncode == 0:\n        return True\n    if result.returncode == 1:\n        return False\n    return None\n\n\ndef _incoming_is_older(incoming_commit: str | None, existing_commit: str | None) -> bool:\n    \"\"\"Whether a fresh install is provably older than the recorded stamp.\n\n    Only a *proven* older lineage \u2014 the incoming platform commit is a strict\n    ancestor of the recorded one \u2014 blocks the overwrite. Same commit, unrelated\n    lineages, or an undeterminable relationship all refresh, so the guard never\n    silently keeps stale content it cannot prove is newer.\n    \"\"\"\n    if not incoming_commit or not existing_commit or incoming_commit == existing_commit:\n        return False\n    return commit_is_ancestor(ROOT, incoming_commit, existing_commit) is True\n\n\ndef _ensure_stamp_gitignored(target: Path) -> None:\n    \"\"\"Keep the untracked install stamp out of ``git add`` / ``git add -A``.\n\n    The stamp is target-local state that is never committed: validate_shiki\n    rejects an unknown *tracked* ``.shiki/`` path, and the goal loop stages\n    worktrees with ``git add -A``, so an un-ignored stamp would be committed and\n    make the target's own Validate check permanently red. The shipped\n    ``.gitignore`` is platform-owned and cannot name it, so the installer\n    ensures the target's own ``.gitignore`` ignores it. Idempotent: a re-install\n    never appends the entry twice.\n    \"\"\"\n    gitignore = target / \".gitignore\"\n    existing = gitignore.read_text(encoding=\"utf-8\") if gitignore.is_file() else \"\"\n    if INSTALL_STAMP_PATH in {line.strip() for line in existing.splitlines()}:\n        return\n    prefix = \"\" if not existing or existing.endswith(\"\\n\") else \"\\n\"\n    addition = (\n        f\"{prefix}\\n# Local-only install version stamp (see `shiki doctor` drift checks)\\n\"\n        f\"{INSTALL_STAMP_PATH}\\n\"\n    )\n    with gitignore.open(\"a\", encoding=\"utf-8\") as handle:\n        handle.write(addition)\n\n\ndef read_install_stamp(target: Path) -> dict[str, Any] | None:\n    \"\"\"Parse a target's install stamp, or None when absent/unreadable.\"\"\"\n    path = target / INSTALL_STAMP_PATH\n    try:\n        data = json.loads(path.read_text(encoding=\"utf-8\"))\n    except (OSError, json.JSONDecodeError):\n        return None\n    return data if isinstance(data, dict) else None\n\n\ndef write_install_stamp(target: Path) -> None:\n    \"\"\"Write (or refresh) the target's install stamp.\n\n    Never overwrites a stamp from a newer platform commit with older content,\n    and leaves an already-current stamp byte-for-byte untouched so a no-op\n    re-install churns nothing.\n    \"\"\"\n    commit = platform_commit(ROOT)\n    digests = target_stamp_digests(target)\n    existing = read_install_stamp(target)\n    if existing is not None:\n        if _incoming_is_older(commit, existing.get(\"platform_commit\")):\n            warn(\n                \"kept existing install stamp: incoming install \"\n                f\"({commit}) is older than the recorded platform commit \"\n                f\"({existing.get('platform_commit')}); not overwriting {INSTALL_STAMP_PATH}\"\n            )\n            return\n        if existing.get(\"platform_commit\") == commit and existing.get(\"digests\") == digests:\n            return\n    stamp = {\n        \"version\": INSTALL_STAMP_VERSION,\n        \"platform_commit\": commit,\n        \"installed_at\": utc_now(),\n        \"digests\": digests,\n    }\n    path = target / INSTALL_STAMP_PATH\n    path.parent.mkdir(parents=True, exist_ok=True)\n    path.write_text(json.dumps(stamp, indent=2, sort_keys=True) + \"\\n\", encoding=\"utf-8\")\n    info(f\"wrote install stamp: {path}\")\n\n\ndef cmd_install_target(args: argparse.Namespace) -> int:\n    target = Path(args.target).expanduser().resolve()\n    if not target.exists():\n        raise ShikiError(f\"target does not exist: {target}\")\n    if not target.is_dir():\n        raise ShikiError(f\"target is not a directory: {target}\")\n\n    if not args.local_only:\n        raise ShikiError(\"install-target is template-only; use shiki init TARGET --repo OWNER/NAME for GitHub-first setup, or pass --local-only explicitly\")\n\n\n succeeded in 0ms:\n\"\"\"Install version stamp: written on install, drift-reported by ``doctor``.\n\nA uniformly-old target is internally consistent, so ``validate_shiki`` passes\nand ``doctor`` reports zero failures \u2014 indistinguishable from a current target.\nThe install stamp is the instrument that removes that blind spot: every install\nand upgrade writes a small ``.shiki/install-stamp.json`` naming the platform\ncommit, the install time, and a content digest per platform-owned shipped path,\nand ``doctor`` reports three kinds of drift from it \u2014\n\n* a shipped path whose current content differs from the stamped digest\n  (hand-edited or half-upgraded);\n* a stamped platform commit that is not an ancestor of the running platform HEAD\n  (the target is ahead, or from a different lineage);\n* an absent stamp (a target installed before stamps existed) \u2014 a distinct\n  finding, never a pass.\n\nEach is a real ``doctor`` finding with its own id, so ``--strict`` fails on it,\nand ``doctor`` never writes to the target.\n\nThis file also guards the migration ``--target`` fix: the global ``shiki`` execs\nthe PLATFORM's ``scripts/``, so ``migrate --target <other repo>`` would apply the\nplatform registry to the target's state and could write migration ids the\ntarget's own validator rejects. When the target ships its own, differing\nregistry, ``migrate`` refuses and names the correct command.\n\nDependency-free ``unittest`` only; drives ``scripts/shiki.py`` via subprocess so\nthe assertions exercise the same exit contract an operator sees.\n\"\"\"\n\nfrom __future__ import annotations\n\nimport hashlib\nimport json\nimport re\nimport shutil\nimport subprocess\nimport tempfile\nimport unittest\nfrom pathlib import Path\n\nimport shiki_test_support  # noqa: F401  (path bootstrap)\n\nfrom shiki_installer import INSTALL_STAMP_PATH, _incoming_is_older\n\nSHIKI_CLI = shiki_test_support.SCRIPTS_DIR / \"shiki.py\"\nREPO_ROOT = shiki_test_support.REPO_ROOT\n\n# A platform-owned shipped path that the stamp always digests (overwrite\n# category \u2014 not project content, governance, or preserve-outright state).\nSHIPPED_PATH = \"scripts/shiki_git.py\"\n# Files the stamp must never digest: they are customized per target, so a\n# difference there is expected, not drift.\nCUSTOMIZABLE_PATHS = (\"AGENTS.md\", \".shiki/config.yaml\", \".github/CODEOWNERS\")\n\n\ndef _run(*args: str) -> subprocess.CompletedProcess[str]:\n    return subprocess.run(\n        [\"python3\", str(SHIKI_CLI), *args],\n        capture_output=True,\n        text=True,\n    )\n\n\ndef _findings(doctor_json: str) -> dict[str, str]:\n    report = json.loads(doctor_json)\n    return {finding[\"id\"]: finding[\"status\"] for finding in report[\"findings\"]}\n\n\ndef _finding(report_json: str, finding_id: str) -> dict[str, object]:\n    report = json.loads(report_json)\n    for finding in report[\"findings\"]:\n        if finding[\"id\"] == finding_id:\n            return finding\n    raise AssertionError(f\"missing doctor finding {finding_id}\")\n\n\ndef _sha256(path: Path) -> str:\n    return hashlib.sha256(path.read_bytes()).hexdigest()\n\n\ndef _content_snapshot(root: Path) -> dict[str, str]:\n    \"\"\"Digest every file under ``root`` except Python bytecode cache.\"\"\"\n    snapshot: dict[str, str] = {}\n    for path in sorted(root.rglob(\"*\")):\n        if path.is_file() and \"__pycache__\" not in path.parts and path.suffix != \".pyc\":\n            snapshot[str(path.relative_to(root))] = _sha256(path)\n    return snapshot\n\n\nclass InstallStampTests(unittest.TestCase):\n    def setUp(self) -> None:\n        self.tmp = Path(tempfile.mkdtemp(prefix=\"shiki-install-stamp-\"))\n        self.addCleanup(lambda: shutil.rmtree(self.tmp, ignore_errors=True))\n        self.target = self.tmp / \"target\"\n        self.target.mkdir()\n        install = _run(\"install-target\", str(self.target), \"--local-only\", \"--no-validate\")\n        self.assertEqual(install.returncode, 0, install.stderr)\n        self.stamp_path = self.target / INSTALL_STAMP_PATH\n\n    def _read_stamp(self) -> dict:\n        return json.loads(self.stamp_path.read_text(encoding=\"utf-8\"))\n\n    def _doctor(self, *extra: str) -> subprocess.CompletedProcess[str]:\n        return _run(\"doctor\", \"--json\", \"--target\", str(self.target), *extra)\n\n    # -- stamp shape -------------------------------------------------------\n\n    def test_fresh_install_writes_a_stamp(self) -> None:\n        self.assertTrue(self.stamp_path.is_file(), \"install must write an install stamp\")\n        stamp = self._read_stamp()\n        self.assertEqual(stamp[\"version\"], 1)\n\n        # Names the platform commit the install came from.\n        head = subprocess.run(\n            [\"git\", \"-C\", str(REPO_ROOT), \"rev-parse\", \"HEAD\"],\n            capture_output=True, text=True,\n        )\n        self.assertEqual(head.returncode, 0, head.stderr)\n        self.assertEqual(stamp[\"platform_commit\"], head.stdout.strip())\n\n        # Names the install timestamp.\n        self.assertIsInstance(stamp[\"installed_at\"], str)\n        self.assertTrue(stamp[\"installed_at\"])\n\n        # Names a content digest per shipped path \u2014 platform-owned paths only.\n        digests = stamp[\"digests\"]\n        self.assertIn(SHIPPED_PATH, digests)\n        self.assertEqual(digests[SHIPPED_PATH], _sha256(self.target / SHIPPED_PATH))\n        for customizable in CUSTOMIZABLE_PATHS:\n            self.assertNotIn(\n                customizable, digests, f\"{customizable} is customized per target; it must not be stamped\"\n            )\n\n    def test_stamp_is_not_committable(self) -> None:\n        # The untracked stamp must never reach a commit: validate rejects an\n        # unknown tracked .shiki path, and the loop stages with `git add -A`.\n        subprocess.run([\"git\", \"-C\", str(self.target), \"init\", \"-q\"], check=True)\n        subprocess.run([\"git\", \"-C\", str(self.target), \"add\", \".\"], check=True)\n        tracked = subprocess.run(\n            [\"git\", \"-C\", str(self.target), \"ls-files\"], capture_output=True, text=True,\n        ).stdout\n        self.assertNotIn(INSTALL_STAMP_PATH, tracked.splitlines())\n        # The target's own validator still passes with the stamp on disk.\n        validate = subprocess.run(\n            [\"python3\", \"scripts/validate_shiki.py\"], cwd=str(self.target),\n            capture_output=True, text=True,\n        )\n        self.assertEqual(validate.returncode, 0, validate.stdout + validate.stderr)\n\n    def test_upgrade_refreshes_the_stamp(self) -> None:\n        # A stale stamp (older platform content: a wrong recorded digest and an\n        # old timestamp) is refreshed by a --force upgrade.\n        stamp = self._read_stamp()\n        stamp[\"installed_at\"] = \"2000-01-01T00:00:00+00:00\"\n        stamp[\"digests\"][SHIPPED_PATH] = \"deadbeef\"\n        self.stamp_path.write_text(json.dumps(stamp, indent=2, sort_keys=True) + \"\\n\", encoding=\"utf-8\")\n\n        upgrade = _run(\"install-target\", str(self.target), \"--local-only\", \"--no-validate\", \"--force\")\n        self.assertEqual(upgrade.returncode, 0, upgrade.stderr)\n\n        refreshed = self._read_stamp()\n        self.assertNotEqual(refreshed[\"installed_at\"], \"2000-01-01T00:00:00+00:00\")\n        self.assertEqual(refreshed[\"digests\"][SHIPPED_PATH], _sha256(self.target / SHIPPED_PATH))\n\n    # -- drift reporting ---------------------------------------------------\n\n    def test_no_drift_on_untouched_fresh_install(self) -> None:\n        statuses = _findings(self._doctor().stdout)\n        self.assertEqual(statuses[\"doctor.install_stamp.present\"], \"pass\")\n        self.assertEqual(statuses[\"doctor.install_stamp.content\"], \"pass\")\n        self.assertEqual(statuses[\"doctor.install_stamp.platform_commit\"], \"pass\")\n\n    def test_editing_a_shipped_file_is_reported_naming_that_path(self) -> None:\n        edited = self.target / SHIPPED_PATH\n        edited.write_text(edited.read_text(encoding=\"utf-8\") + \"\\n# local edit\\n\", encoding=\"utf-8\")\n\n        finding = _finding(self._doctor().stdout, \"doctor.install_stamp.content\")\n        self.assertEqual(finding[\"status\"], \"warn\")\n        self.assertIn(SHIPPED_PATH, finding[\"summary\"])\n        self.assertIn(SHIPPED_PATH, finding[\"details\"][\"drifted\"])\n\n    def test_absent_stamp_is_a_distinct_finding_never_a_pass(self) -> None:\n        self.stamp_path.unlink()\n        report = self._doctor().stdout\n        statuses = _findings(report)\n        self.assertEqual(statuses[\"doctor.install_stamp.present\"], \"warn\")\n        self.assertNotEqual(statuses[\"doctor.install_stamp.present\"], \"pass\")\n        # Content/commit cannot be judged without a stamp, but are never a pass.\n        self.assertEqual(statuses[\"doctor.install_stamp.content\"], \"skip\")\n        self.assertEqual(statuses[\"doctor.install_stamp.platform_commit\"], \"skip\")\n\n    def test_newer_platform_commit_is_reported_not_silently_accepted(self) -> None:\n        # A stamp naming a commit that is not an ancestor of the running platform\n        # HEAD (target ahead, or a different lineage) is reported.\n        stamp = self._read_stamp()\n        stamp[\"platform_commit\"] = \"0\" * 40  # not an ancestor of any real HEAD\n        self.stamp_path.write_text(json.dumps(stamp, indent=2, sort_keys=True) + \"\\n\", encoding=\"utf-8\")\n\n        finding = _finding(self._doctor().stdout, \"doctor.install_stamp.platform_commit\")\n        self.assertEqual(finding[\"status\"], \"warn\")\n        self.assertIn(\"not an ancestor\", finding[\"summary\"])\n\n    def test_strict_fails_on_drift(self) -> None:\n        edited = self.target / SHIPPED_PATH\n        edited.write_text(edited.read_text(encoding=\"utf-8\") + \"\\n# local edit\\n\", encoding=\"utf-8\")\n        self.assertEqual(_findings(self._doctor().stdout)[\"doctor.install_stamp.content\"], \"warn\")\n        self.assertNotEqual(self._doctor(\"--strict\").returncode, 0)\n\n    def test_strict_fails_on_absent_stamp(self) -> None:\n        self.stamp_path.unlink()\n        self.assertEqual(_findings(self._doctor().stdout)[\"doctor.install_stamp.present\"], \"warn\")\n        self.assertNotEqual(self._doctor(\"--strict\").returncode, 0)\n\n    def test_doctor_never_writes_to_the_target(self) -> None:\n        # Prime bytecode caches with one run, then assert a second run mutates\n        # no target content (the stamp included).\n        self._doctor()\n        before = _content_snapshot(self.target)\n        self._doctor()\n        after = _content_snapshot(self.target)\n        self.assertEqual(before, after, \"doctor must not write to the target\")\n\n\nclass OlderContentGuardTests(unittest.TestCase):\n\n succeeded in 0ms:\n\"\"\"Three-way ``--force`` install semantics: nothing lost, nothing silently stale.\n\nThese tests import the real ``scripts/`` modules and assert their observed\nbehavior against the upgrade contract for T-...2411811e. Dependency-free\n``unittest`` only.\n\nThe three-way split under ``--force`` is:\n\n* PROJECT CONTENT (``CONTEXT.md``, ``AGENTS.md``, ``CLAUDE.md``,\n  ``.github/CODEOWNERS``) is never overwritten; the incoming template is written\n  alongside as ``<file>.new`` and reported.\n* GOVERNANCE CONTRACT (``.shiki/config.yaml``, ``.shiki/guardian-policy.json``)\n  is never silently kept; ``<file>.new`` is written and a per-key summary names\n  what differs, calling out ``mergegate.required_checks`` and ``approval_sources``.\n* ``.shiki/migrations/state.json`` is preserved outright (no ``.new``).\n* Everything else is overwritten under ``--force``.\n\nA target with pending migrations is refused with NO file written.\n\"\"\"\n\nfrom __future__ import annotations\n\nimport contextlib\nimport hashlib\nimport json\nimport shutil\nimport tempfile\nimport unittest\nfrom pathlib import Path\n\nimport shiki_test_support  # noqa: F401  (path bootstrap)\n\nimport shiki_contracts\nimport shiki_installer\nimport validate_shiki\nfrom shiki_migrations import MEMORIES_MIGRATION_ID, migration_status\nfrom shiki_process import ShikiError\n\nPROJECT_CONTENT = (\"CONTEXT.md\", \"AGENTS.md\", \"CLAUDE.md\", \".github/CODEOWNERS\")\nGOVERNANCE = (\".shiki/config.yaml\", \".shiki/guardian-policy.json\")\nMIGRATION_STATE = \".shiki/migrations/state.json\"\n\n# A config.yaml whose mergegate.required_checks differs from the shipped template\n# (the platform ships four checks; this keeps only one), parseable by the same\n# subset parser bootstrap uses.\nMODIFIED_CONFIG = (\n    \"mergegate:\\n\"\n    \"  required_checks:\\n\"\n    \"    - Validate Shiki mirror\\n\"\n)\n# A guardian-policy.json whose approval_sources differs from the shipped template.\nMODIFIED_GUARDIAN = json.dumps(\n    {\"version\": 1, \"approval_sources\": {\"github_review\": {\"enabled\": False}}},\n    indent=2,\n) + \"\\n\"\n\n\n@contextlib.contextmanager\ndef _captured_output():\n    info_lines: list[str] = []\n    warn_lines: list[str] = []\n    orig_info = shiki_installer.info\n    orig_warn = shiki_installer.warn\n    shiki_installer.info = lambda message, *a, **k: info_lines.append(str(message))\n    shiki_installer.warn = lambda message, *a, **k: warn_lines.append(str(message))\n    try:\n        yield info_lines, warn_lines\n    finally:\n        shiki_installer.info = orig_info\n        shiki_installer.warn = orig_warn\n\n\ndef _hash_tree(root: Path) -> dict[str, str]:\n    digests: dict[str, str] = {}\n    for path in sorted(root.rglob(\"*\")):\n        if path.is_file():\n            digests[str(path.relative_to(root))] = hashlib.sha256(\n                path.read_bytes()\n            ).hexdigest()\n    return digests\n\n\nclass ForceSemanticsTests(unittest.TestCase):\n    \"\"\"A fully installed target, then a ``--force`` refresh over it.\"\"\"\n\n    def setUp(self) -> None:\n        self.tmp = Path(tempfile.mkdtemp(prefix=\"shiki-force-\"))\n        self.addCleanup(lambda: shutil.rmtree(self.tmp, ignore_errors=True))\n        self.target = self.tmp / \"target\"\n        self.target.mkdir()\n        # Fresh install (no --force): lays down the full template surface.\n        with _captured_output():\n            shiki_installer.install_template(self.target, force=False, validate=False)\n\n    def _template_text(self, relative: str) -> str:\n        return (shiki_installer.ROOT / relative).read_text(encoding=\"utf-8\")\n\n    def test_without_force_nothing_existing_changes(self) -> None:\n        before = _hash_tree(self.target)\n        with _captured_output():\n            shiki_installer.install_template(self.target, force=False, validate=False)\n        after = _hash_tree(self.target)\n        self.assertEqual(before, after, \"a no-force re-run must change nothing\")\n        self.assertFalse(\n            any(p.name.endswith(\".new\") for p in self.target.rglob(\"*\")),\n            \"no .new file may be written without --force\",\n        )\n\n    def test_force_keeps_project_content_and_writes_new_per_file(self) -> None:\n        for relative in PROJECT_CONTENT:\n            (self.target / relative).write_text(f\"LOCAL EDIT {relative}\\n\", encoding=\"utf-8\")\n\n        with _captured_output() as (info_lines, _):\n            shiki_installer.install_template(self.target, force=True, validate=False)\n\n        for relative in PROJECT_CONTENT:\n            self.assertEqual(\n                (self.target / relative).read_text(encoding=\"utf-8\"),\n                f\"LOCAL EDIT {relative}\\n\",\n                f\"{relative} must not be overwritten by --force\",\n            )\n            new_path = self.target / (relative + \".new\")\n            self.assertTrue(new_path.exists(), f\"{relative}.new must be written\")\n            self.assertEqual(\n                new_path.read_text(encoding=\"utf-8\"),\n                self._template_text(relative),\n                f\"{relative}.new must carry the incoming template\",\n            )\n            self.assertTrue(\n                any(f\"{relative}.new\" in line for line in info_lines),\n                f\"summary must list {relative}.new\",\n            )\n\n    def test_force_keeps_governance_writes_new_and_names_keys(self) -> None:\n        (self.target / \".shiki\" / \"config.yaml\").write_text(MODIFIED_CONFIG, encoding=\"utf-8\")\n        (self.target / \".shiki\" / \"guardian-policy.json\").write_text(\n            MODIFIED_GUARDIAN, encoding=\"utf-8\"\n        )\n\n        with _captured_output() as (info_lines, _):\n            shiki_installer.install_template(self.target, force=True, validate=False)\n\n        # Content kept; .new written with the incoming template.\n        self.assertEqual(\n            (self.target / \".shiki\" / \"config.yaml\").read_text(encoding=\"utf-8\"),\n            MODIFIED_CONFIG,\n        )\n        self.assertEqual(\n            (self.target / \".shiki\" / \"guardian-policy.json\").read_text(encoding=\"utf-8\"),\n            MODIFIED_GUARDIAN,\n        )\n        for relative in GOVERNANCE:\n            new_path = self.target / (relative + \".new\")\n            self.assertTrue(new_path.exists(), f\"{relative}.new must be written\")\n            self.assertEqual(\n                new_path.read_text(encoding=\"utf-8\"),\n                self._template_text(relative),\n            )\n\n        summary = \"\\n\".join(info_lines)\n        # The two governance files appear in the summary with the critical\n        # differing keys named explicitly.\n        config_line = next(\n            (line for line in info_lines if \".shiki/config.yaml.new\" in line), \"\"\n        )\n        guardian_line = next(\n            (line for line in info_lines if \".shiki/guardian-policy.json.new\" in line), \"\"\n        )\n        self.assertIn(\"mergegate.required_checks\", config_line)\n        self.assertIn(\"approval_sources\", guardian_line)\n        self.assertIn(\".shiki/config.yaml.new\", summary)\n        self.assertIn(\".shiki/guardian-policy.json.new\", summary)\n\n    def test_force_preserves_migration_state_without_new(self) -> None:\n        state_before = (self.target / MIGRATION_STATE).read_text(encoding=\"utf-8\")\n        with _captured_output():\n            shiki_installer.install_template(self.target, force=True, validate=False)\n        self.assertEqual(\n            (self.target / MIGRATION_STATE).read_text(encoding=\"utf-8\"),\n            state_before,\n            \"migration state must be preserved outright\",\n        )\n        self.assertFalse(\n            (self.target / (MIGRATION_STATE + \".new\")).exists(),\n            \"migration state must not gain a .new\",\n        )\n\n    def test_force_overwrites_every_other_shipped_file(self) -> None:\n        overwritten = (\".shiki/README.md\", \"SYSTEM_PROMPT.md\")\n        for relative in overwritten:\n            (self.target / relative).write_text(\"STALE LOCAL EDIT\\n\", encoding=\"utf-8\")\n\n        with _captured_output():\n            shiki_installer.install_template(self.target, force=True, validate=False)\n\n        for relative in overwritten:\n            self.assertEqual(\n                (self.target / relative).read_text(encoding=\"utf-8\"),\n                self._template_text(relative),\n                f\"{relative} must be overwritten under --force\",\n            )\n            self.assertFalse(\n                (self.target / (relative + \".new\")).exists(),\n                f\"{relative} must not gain a .new (it is overwritten, not preserved)\",\n            )\n\n    def test_summary_lists_every_new_written(self) -> None:\n        for relative in PROJECT_CONTENT:\n            (self.target / relative).write_text(\"edit\\n\", encoding=\"utf-8\")\n        (self.target / \".shiki\" / \"config.yaml\").write_text(MODIFIED_CONFIG, encoding=\"utf-8\")\n        (self.target / \".shiki\" / \"guardian-policy.json\").write_text(\n            MODIFIED_GUARDIAN, encoding=\"utf-8\"\n        )\n\n        with _captured_output() as (info_lines, _):\n            shiki_installer.install_template(self.target, force=True, validate=False)\n\n        summary = \"\\n\".join(info_lines)\n        for relative in (*PROJECT_CONTENT, *GOVERNANCE):\n            self.assertIn(f\"{relative}.new\", summary, f\"summary omitted {relative}.new\")\n\n\nclass PendingMigrationRefusalTests(unittest.TestCase):\n    \"\"\"A target whose valid migration state is behind the registry is refused\n    with NO file written.\"\"\"\n\n    def setUp(self) -> None:\n        self.tmp = Path(tempfile.mkdtemp(prefix=\"shiki-pending-\"))\n        self.addCleanup(lambda: shutil.rmtree(self.tmp, ignore_errors=True))\n        self.target = self.tmp / \"target\"\n        (self.target / \".shiki\" / \"migrations\").mkdir(parents=True)\n\n        # Take the real, valid migration state and drop the last applied\n        # migration so exactly one is pending while the state stays valid.\n        real = json.loads(\n            (shiki_installer.ROOT / MIGRATION_STATE).read_text(encoding=\"utf-8\")\n        )\n        real[\"applied\"] = [\n            record for record in real[\"applied\"] if record.get(\"id\") != MEMORIES_MIGRATION_ID\n        ]\n\nexec\n/bin/zsh -lc \"sed -n '241,420p' tests/test_install_force_semantics.py\" in /private/tmp/shiki-adr-collision.pa0KwY/.worktrees/shiki-t-20260807t073307221254z-2d22d654-introduc\n succeeded in 0ms:\n        (self.target / MIGRATION_STATE).write_text(\n            json.dumps(real, indent=2) + \"\\n\", encoding=\"utf-8\"\n        )\n        # Pre-existing project + governance content that must stay untouched.\n        (self.target / \"CONTEXT.md\").write_text(\"do not touch\\n\", encoding=\"utf-8\")\n\n    def test_pending_state_is_actually_pending(self) -> None:\n        status = migration_status(self.target)\n        self.assertTrue(status[\"state_exists\"])\n        self.assertEqual(status[\"errors\"], [], \"the behind state must stay valid\")\n        self.assertIn(MEMORIES_MIGRATION_ID, status[\"pending\"])\n\n    def test_refused_with_nothing_written(self) -> None:\n        before = _hash_tree(self.target)\n        with _captured_output():\n            with self.assertRaises(ShikiError):\n                shiki_installer.install_template(self.target, force=True, validate=False)\n        after = _hash_tree(self.target)\n        self.assertEqual(before, after, \"no file may be written when refused\")\n        self.assertFalse(\n            any(p.name.endswith(\".new\") for p in self.target.rglob(\"*\")),\n            \"no .new file may be written when refused\",\n        )\n\n\nclass CodeownersResolverTests(unittest.TestCase):\n    \"\"\"The required CODEOWNERS owner resolves per target, not hardcoded.\"\"\"\n\n    def setUp(self) -> None:\n        self.tmp = Path(tempfile.mkdtemp(prefix=\"shiki-codeowners-\"))\n        self.addCleanup(lambda: shutil.rmtree(self.tmp, ignore_errors=True))\n\n    def _target_with_repo(self, repo: str | None) -> Path:\n        target = self.tmp / (repo.replace(\"/\", \"-\") if repo else \"no-repo\")\n        (target / \".shiki\").mkdir(parents=True)\n        if repo is not None:\n            (target / \".shiki\" / \"repo.json\").write_text(\n                json.dumps({\"repo\": repo, \"source_of_truth\": \"github\"}) + \"\\n\",\n                encoding=\"utf-8\",\n            )\n        return target\n\n    def test_owner_resolves_from_repo_json(self) -> None:\n        target = self._target_with_repo(\"acme-org/widgets\")\n        self.assertEqual(\n            shiki_contracts.codeowners_required_owner(target), \"@acme-org\"\n        )\n\n    def test_owner_defaults_when_repo_json_absent(self) -> None:\n        target = self._target_with_repo(None)\n        self.assertEqual(\n            shiki_contracts.codeowners_required_owner(target),\n            shiki_contracts.CODEOWNERS_REQUIRED_OWNER,\n        )\n\n    def test_this_repository_owner_is_unchanged(self) -> None:\n        # This platform repository has no per-target repo.json, so it keeps the\n        # documented default owner.\n        target = self._target_with_repo(\"mizutani-140/shiki\")\n        self.assertEqual(\n            shiki_contracts.codeowners_required_owner(target), \"@mizutani-140\"\n        )\n        self.assertEqual(shiki_contracts.CODEOWNERS_REQUIRED_OWNER, \"@mizutani-140\")\n\n    def test_installed_foreign_owner_target_passes_wired_gate(self) -> None:\n        # Asserting the resolver's return value is not enough: a foreign target\n        # whose repo.json is present at install time gets a CODEOWNERS naming its\n        # own owner, and that installed file passes the wired validate_shiki gate.\n        target = self._target_with_repo(\"acme-org/widgets\")\n        with _captured_output():\n            shiki_installer.install_template(target, force=False, validate=False)\n\n        codeowners = (target / \".github\" / \"CODEOWNERS\").read_text(encoding=\"utf-8\")\n        self.assertIn(\"@acme-org\", codeowners)\n        self.assertNotIn(shiki_contracts.CODEOWNERS_REQUIRED_OWNER, codeowners)\n\n        # No exception == the wired gate accepts the foreign target's own owner.\n        validate_shiki.validate_codeowners_governance(target)\n\n\nif __name__ == \"__main__\":\n    unittest.main()\n\nexec\n/bin/zsh -lc \"sed -n '221,430p' scripts/shiki_installer.py\" in /private/tmp/shiki-adr-collision.pa0KwY/.worktrees/shiki-t-20260807t073307221254z-2d22d654-introduc\n succeeded in 0ms:\ndef refuse_pending_migrations(target: Path) -> None:\n    \"\"\"Precondition run BEFORE any file write.\n\n    Refuse to upgrade a target whose valid ``.shiki`` migration state is behind\n    the current registry, so a target with pending migrations keeps an untouched\n    tree instead of being half-rewritten and then aborting. Only the \"valid but\n    behind\" upgrade case is gated: a fresh target has no state file yet (it is\n    seeded by this install), and a target whose state file is absent or malformed\n    is left to post-write validation, which surfaces malformed state.\n    \"\"\"\n    status = migration_status(target)\n    if status[\"state_exists\"] and not status[\"errors\"] and status[\"pending\"]:\n        pending = \", \".join(status[\"pending\"])\n        raise ShikiError(\n            f\"target has pending .shiki migrations ({pending}); run \"\n            \"`shiki migrate apply --execute` before upgrading. No files were written.\"\n        )\n\n\ndef install_template(target: Path, *, force: bool, validate: bool) -> list[NewFileNote]:\n    # Preconditions first: never rewrite the tree of a target that cannot be\n    # upgraded cleanly (see refuse_pending_migrations).\n    refuse_pending_migrations(target)\n\n    # Resolve the target's required CODEOWNERS owner once from its\n    # ``.shiki/repo.json`` (present when a target already knows its GitHub\n    # identity). The shipped ``.github/CODEOWNERS`` names this maintainer on every\n    # line, so a foreign target's copy is rewritten to name its own owner; when no\n    # owner can be resolved this is the documented fallback and the file is copied\n    # verbatim. See ``codeowners_required_owner``.\n    codeowners_owner = codeowners_required_owner(target)\n\n    notes: list[NewFileNote] = []\n    for relative in TEMPLATE_PATHS:\n        source = ROOT / relative\n        if not source.exists():\n            warn(f\"template path missing, skipped: {relative}\")\n            continue\n        copy_path(\n            source,\n            target / relative,\n            force=force,\n            target_install=True,\n            notes=notes,\n            codeowners_owner=codeowners_owner,\n        )\n\n    manifest = load_manifest(ROOT)\n    directories = manifest_directories(manifest)\n    for relative in manifest_create_directories(manifest):\n        state_dir = target / relative\n        state_dir.mkdir(parents=True, exist_ok=True)\n        metadata = directories.get(relative, {})\n        if metadata.get(\"tracked\") is True and metadata.get(\"required\") is True and not any(state_dir.iterdir()):\n            (state_dir / \".gitkeep\").write_text(\"\", encoding=\"utf-8\")\n        info(f\"ensured empty state directory: {state_dir}\")\n\n    # Stamp the install after the shipped surface is on disk, so the digests\n    # describe exactly what was written. Refreshed on every install and upgrade.\n    # The stamp stays untracked local state, so keep it out of git first.\n    _ensure_stamp_gitignored(target)\n    write_install_stamp(target)\n\n    # A single explicit summary at the end, so a half-upgrade is visible rather\n    # than inferred. Printed before validation so the .new files are always\n    # reported even if a later gate fails.\n    print_new_file_summary(notes)\n\n    if validate:\n        validate_target_shiki(target)\n\n    return notes\n\n\ndef print_new_file_summary(notes: list[NewFileNote]) -> None:\n    if not notes:\n        info(\n            \"template summary: no .new files written (no existing project-content \"\n            \"or governance file was kept alongside a refreshed copy)\"\n        )\n        return\n    info(f\"template summary: wrote {len(notes)} .new file(s) alongside preserved originals:\")\n    for note in sorted(notes, key=lambda item: item.relative):\n        if note.category == \"governance-contract\":\n            keys = \", \".join(note.differing_keys) if note.differing_keys else \"no parseable key differences\"\n            info(f\"  governance-contract: kept {note.relative}, wrote {note.new_relative} (differing keys: {keys})\")\n        else:\n            info(f\"  project-content: kept {note.relative}, wrote {note.new_relative}\")\n\n\ndef should_skip(path: Path, *, target_install: bool = False) -> bool:\n    parts = set(path.parts)\n    if \"__pycache__\" in parts or path.name == \".DS_Store\" or path.suffix == \".pyc\":\n        return True\n    if target_install:\n        relative = path.relative_to(ROOT)\n        relative_text = relative.as_posix()\n        manifest = load_manifest(ROOT)\n        if relative_text in manifest_install_include(manifest):\n            return False\n        # Platform-only tests never ship into a target (see TARGET_INSTALL_EXCLUDES).\n        if relative_text in TARGET_INSTALL_EXCLUDES:\n            return True\n        # Provider metadata is created per-target by shiki init/start; copying it\n        # into a new target would point that target at this repository's origin.\n        if relative_text == \".shiki/repo.json\":\n            return True\n        state_prefixes = tuple(f\"{directory}/\" for directory in manifest_create_directories(manifest))\n        if relative_text.startswith(state_prefixes):\n            return True\n        return excluded_from_commit(relative_text, manifest_exclude_from_commit(manifest))\n    return False\n\n\ndef template_relative(source: Path) -> str | None:\n    \"\"\"The shipped-surface relative path for ``source`` (POSIX), or None.\"\"\"\n    try:\n        return source.relative_to(ROOT).as_posix()\n    except ValueError:\n        return None\n\n\ndef force_category(source: Path) -> str:\n    \"\"\"Classify a shipped file for --force handling.\n\n    Returns one of ``project-content``, ``governance-contract``,\n    ``preserve-outright``, or ``overwrite``.\n    \"\"\"\n    relative = template_relative(source)\n    if relative in PROJECT_CONTENT_FILES:\n        return \"project-content\"\n    if relative in GOVERNANCE_CONTRACT_FILES:\n        return \"governance-contract\"\n    if relative in PRESERVE_OUTRIGHT_FILES:\n        return \"preserve-outright\"\n    return \"overwrite\"\n\n\ndef copy_path(\n    source: Path,\n    target: Path,\n    *,\n    force: bool,\n    target_install: bool = False,\n    notes: list[NewFileNote] | None = None,\n    codeowners_owner: str | None = None,\n) -> None:\n    if should_skip(source, target_install=target_install):\n        return\n    if source.is_dir():\n        for child in source.iterdir():\n            copy_path(\n                child,\n                target / child.name,\n                force=force,\n                target_install=target_install,\n                notes=notes,\n                codeowners_owner=codeowners_owner,\n            )\n        return\n\n    if target.exists():\n        if not force:\n            warn(f\"kept existing file: {target}\")\n            return\n        category = force_category(source)\n        if category == \"preserve-outright\":\n            warn(f\"preserved existing stateful file (not overwritten by --force): {target}\")\n            return\n        if category in (\"project-content\", \"governance-contract\"):\n            differing = (\n                governance_differing_keys(source, target)\n                if category == \"governance-contract\"\n                else ()\n            )\n            note = write_new_alongside(source, target, category=category, differing_keys=differing)\n            if notes is not None:\n                notes.append(note)\n            return\n\n    target.parent.mkdir(parents=True, exist_ok=True)\n    if (\n        target_install\n        and codeowners_owner\n        and codeowners_owner != CODEOWNERS_REQUIRED_OWNER\n        and template_relative(source) == CODEOWNERS_PATH\n    ):\n        # Substitute the target's own owner for the shipped maintainer so the\n        # target's CODEOWNERS check resolves against its own owner. Only the\n        # owner token changes; when it equals the fallback the file is copied\n        # verbatim above, preserving byte-for-byte behaviour for this repo.\n        text = source.read_text(encoding=\"utf-8\").replace(CODEOWNERS_REQUIRED_OWNER, codeowners_owner)\n        target.write_text(text, encoding=\"utf-8\")\n    else:\n        shutil.copy2(source, target)\n    info(f\"installed {target}\")\n\n\ndef write_new_alongside(\n    source: Path,\n    target: Path,\n    *,\n    category: str,\n    differing_keys: tuple[str, ...] = (),\n) -> NewFileNote:\n    \"\"\"Keep ``target`` untouched and write the incoming template as ``<target>.new``.\"\"\"\n    new_path = target.parent / (target.name + \".new\")\n    new_path.parent.mkdir(parents=True, exist_ok=True)\n    shutil.copy2(source, new_path)\n    warn(\n\n2026-08-07T08:04:40.346500Z ERROR codex_models_manager::manager: failed to renew cache TTL: missing field `base_instructions` at line 94 column 5\nexec\n/bin/zsh -lc 'find docs/adr -maxdepth 1 -type f -print | sort' in /private/tmp/shiki-adr-collision.pa0KwY/.worktrees/shiki-t-20260807t073307221254z-2d22d654-introduc\n succeeded in 0ms:\ndocs/adr/0001-keep-shiki-as-platform-name.md\ndocs/adr/0002-use-agents-as-shared-constitution.md\ndocs/adr/0003-require-skill-gate-for-mergegate-readiness.md\ndocs/adr/0004-use-github-first-with-shiki-mirror.md\ndocs/adr/0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\ndocs/adr/0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\ndocs/adr/0007-packaging-and-release.md\ndocs/adr/0008-use-claude-code-as-default-implementer.md\ndocs/adr/0009-requirements-definition-and-spec-freeze.md\ndocs/adr/0010-external-ai-guardian-review.md\ndocs/adr/0011-autonomous-quality-gate-evidence.md\ndocs/adr/0012-autonomous-loop-closeout-to-main.md\ndocs/adr/0013-bootstrap-grants-actions-approve-pull-requests.md\ndocs/adr/0014-codex-app-external-ai-guardian-ui-adapter.md\ndocs/adr/0015-contract-approval-before-dispatch.md\ndocs/adr/0016-locks-bound-product-paths.md\ndocs/adr/0017-bookkeeping-closeouts-do-not-inherit-risk.md\ndocs/adr/0018-carried-guardian-approval-across-a-proven-base-sync.md\ndocs/adr/README.md\ndocs/adr/template.md\n\nexec\n/bin/zsh -lc \"rg -n 'docs/adr|TEMPLATE_PATHS|install_template|INSTALL_STAMP|force=True|force=False' tests scripts/test_shiki_* .github .codex pyproject.toml --glob '\"'!.shiki/**'\"'\" in /private/tmp/shiki-adr-collision.pa0KwY/.worktrees/shiki-t-20260807t073307221254z-2d22d654-introduc\nexec\n/bin/zsh -lc \"rg -n --glob '\"'!.shiki/**'\"' --glob '\"'!docs/adr/*.md'\"' '(docs/adr/(SADR-)?[0-9]{4}|\\\\bS?ADR[- ]?[0-9]{4}\\\\b|ADR [0-9]{4})' .\" in /private/tmp/shiki-adr-collision.pa0KwY/.worktrees/shiki-t-20260807t073307221254z-2d22d654-introduc\n succeeded in 0ms:\npyproject.toml:12:# docs/adr/0007-packaging-and-release.md.\nscripts/test_shiki_runtime_registry.sh:82:template_paths = set(installer.TEMPLATE_PATHS)\nscripts/test_shiki_runtime_registry.sh:85:        raise SystemExit(f\"TEMPLATE_PATHS omitted {required_path}\")\nscripts/test_shiki_memory_loop.sh:45:if \"scripts/shiki_memory.py\" not in set(shiki_installer.TEMPLATE_PATHS):\nscripts/test_shiki_memory_loop.sh:46:    raise SystemExit(\"TEMPLATE_PATHS missing scripts/shiki_memory.py\")\nscripts/test_shiki_memory_loop.sh:47:if \"scripts/test_shiki_memory_loop.sh\" not in set(shiki_installer.TEMPLATE_PATHS):\nscripts/test_shiki_memory_loop.sh:48:    raise SystemExit(\"TEMPLATE_PATHS missing scripts/test_shiki_memory_loop.sh\")\ntests/test_session_lease.py:398:                                  dry_run=False, force=False, repair_id=None)\ntests/test_adr_references.py:5:0016/0017 seven times, yet for a stretch the numbering in ``docs/adr/`` stopped\ntests/test_adr_references.py:7:declared ``docs/adr/...`` in its locks, so nothing carried the records to the\ntests/test_adr_references.py:9:Guardian approvals could not be read. ``grep -n 'docs/adr' scripts/validate_shiki.py``\ntests/test_adr_references.py:17:number resolves to exactly one ``docs/adr/####-*.md``. The converse is\ntests/test_adr_references.py:48:    matches: Sequence[str]  # repo-relative paths matching docs/adr/<n>-*.md\ntests/test_adr_references.py:52:        return f\"docs/adr/{self.number}-*.md\"\ntests/test_adr_references.py:97:    A citation resolves when exactly one ``docs/adr/####-*.md`` matches its\ntests/test_adr_references.py:133:            f\"docs/adr/####-*.md:\\n{detail}\",\ntests/test_adr_references.py:168:        # A script cites ADR 9991, but no docs/adr/9991-*.md exists.\ntests/test_adr_references.py:181:        self.assertIn(\"docs/adr/9991-*.md\", message)  # missing path\ntests/test_install_completeness.py:14:shipped (in ``TEMPLATE_PATHS`` or under a shipped directory) or on the explicit,\ntests/test_install_completeness.py:32:from shiki_installer import TARGET_INSTALL_EXCLUDES, TEMPLATE_PATHS  # noqa: E402\ntests/test_install_completeness.py:185:            ROOT, set(TEMPLATE_PATHS), SHIPPING_EXCEPTIONS\ntests/test_install_completeness.py:194:                \"to TEMPLATE_PATHS, or to SHIPPING_EXCEPTIONS with a reason:\\n\"\ntests/test_install_force_semantics.py:93:            shiki_installer.install_template(self.target, force=False, validate=False)\ntests/test_install_force_semantics.py:101:            shiki_installer.install_template(self.target, force=False, validate=False)\ntests/test_install_force_semantics.py:114:            shiki_installer.install_template(self.target, force=True, validate=False)\ntests/test_install_force_semantics.py:141:            shiki_installer.install_template(self.target, force=True, validate=False)\ntests/test_install_force_semantics.py:177:            shiki_installer.install_template(self.target, force=True, validate=False)\ntests/test_install_force_semantics.py:194:            shiki_installer.install_template(self.target, force=True, validate=False)\ntests/test_install_force_semantics.py:216:            shiki_installer.install_template(self.target, force=True, validate=False)\ntests/test_install_force_semantics.py:257:                shiki_installer.install_template(self.target, force=True, validate=False)\ntests/test_install_force_semantics.py:311:            shiki_installer.install_template(target, force=False, validate=False)\ntests/test_install_version_drift.py:43:from shiki_installer import INSTALL_STAMP_PATH, _incoming_is_older\ntests/test_install_version_drift.py:98:        self.stamp_path = self.target / INSTALL_STAMP_PATH\ntests/test_install_version_drift.py:142:        self.assertNotIn(INSTALL_STAMP_PATH, tracked.splitlines())\ntests/test_branch_behind_sync.py:233:        # docs/adr/0012 pins the closeout decision table: a bookkeeping closeout has\ntests/test_bootstrap_safety.py:117:        shiki_installer.install_template(self.target, force=False, validate=False)\ntests/test_bootstrap_safety.py:130:        shiki_installer.install_template(self.target, force=True, validate=False)\ntests/test_bootstrap_safety.py:151:        shiki_installer.install_template(self.target, force=True, validate=False)\ntests/test_sync_proof.py:554:        self.assertIn(\"scripts/shiki_sync_proof.py\", shiki_installer.TEMPLATE_PATHS)\nscripts/test_shiki_code_review_gate.sh:32:if \"skills/engineering\" not in set(shiki_installer.TEMPLATE_PATHS):\nscripts/test_shiki_code_review_gate.sh:33:    raise SystemExit(\"TEMPLATE_PATHS omitted skills/engineering\")\nscripts/test_shiki_code_review_gate.sh:34:if \"scripts/test_shiki_code_review_gate.sh\" not in set(shiki_installer.TEMPLATE_PATHS):\nscripts/test_shiki_code_review_gate.sh:35:    raise SystemExit(\"TEMPLATE_PATHS omitted scripts/test_shiki_code_review_gate.sh\")\ntests/test_dispatch_lock_precondition.py:313:                                  dry_run=False, force=False, repair_id=None)\n.codex/skills/shiki/SKILL.md:17:4. `docs/adr/` records hard-to-reverse platform decisions.\ntests/test_codeowners_wiring.py:89:            shiki_installer.install_template(target, force=False, validate=False)\ntests/test_guardian_review.py:56:        relevant_docs=[\"docs/adr/0010-external-ai-guardian-review.md\"],\ntests/test_guardian_review.py:164:        focus = gr.classify_review_focus([\"docs/adr/0099-x.md\", \"README.md\"], \"\", \"low\")\ntests/test_guardian_review.py:174:        focus = gr.classify_review_focus([\"docs/adr/0099-x.md\"], \"\", \"critical\")\ntests/test_repair_delivery.py:587:            target=str(env.target), task_id=TASK, dry_run=False, force=False, repair_id=repair_id,\n.github/prompts/cca-completion-check.md:18:4. `docs/adr/` records hard-to-reverse platform decisions.\n.github/prompts/cca-completion-check.md:29:4. relevant `docs/adr/`\ntests/test_paired_invariants.py:24:``TEMPLATE_PATHS``) and tolerates an empty ``.shiki`` mirror: config/schema/doc\ntests/test_paired_invariants.py:57:from shiki_installer import TEMPLATE_PATHS\ntests/test_paired_invariants.py:67:# The workflow files that ship (TEMPLATE_PATHS names each one explicitly).\ntests/test_paired_invariants.py:70:    for path in TEMPLATE_PATHS\ntests/test_paired_invariants.py:383:        # File templates. shiki-task.md ships (TEMPLATE_PATHS); the top-level\ntests/test_paired_invariants.py:667:            \"shiki_installer.TEMPLATE_PATHS .github/workflows entries\": set(SHIPPED_WORKFLOWS),\ntests/test_paired_invariants.py:674:            relation=\"set-equality: the set of Shiki workflow files must be identical in validate_shiki.WORKFLOW_CONTRACTS, the TEMPLATE_PATHS shipped set, and shiki_doctor's required-file list\",\n.github/workflows/release.yml:6:# docs/adr/0007-packaging-and-release.md.\n\n succeeded in 0ms:\n./SYSTEM_PROMPT.md:19:Shiki is a GitHub-first execution governance layer for AI coding agents. It takes a Goal, clarifies success conditions, grills the plan against repository docs and domain language, converts settled context into a PRD, decomposes the PRD into vertical-slice issues, assigns implementation to Claude Code by default (Codex Front when explicitly assigned, ADR 0008), forces TDD and scoped branches, has a GitHub-side Completion Check Agent judge whether the work is actually complete, returns incomplete work to the assigned implementer as bounded repair, and permits merge only when MergeGate conditions and durable evidence are satisfied.\n./CLAUDE.md:23:Claude Code is also the default hands (ADR 0008): implementation, tests, and bounded repair commits for assigned tasks are dispatched as a headless Claude Code session into the task's registered worktree through `shiki runner claude`, authenticated by the operator's Claude subscription.\n./AGENTS.md:21:- **Claude Code** is the default implementer and runner runtime (ADR 0008): assigned tasks are dispatched into their registered worktree as a headless Claude Code session through `shiki runner claude`, authenticated by the operator's Claude subscription.\n./AGENTS.md:105:Writes code in a scoped branch or worktree and verifies acceptance checks. Claude Code is the default implementer for source changes and repair commits, dispatched through `shiki runner claude`; Codex Front remains an optional implementer for tasks explicitly assigned to `codex` (ADR 0008). An implementer never authors an approval record (see [Approval Records](#approval-records)): it satisfies acceptance criteria with real work, and reports any criterion it cannot satisfy from the worktree's artifacts as a stop.\n./AGENTS.md:299:When every MergeGate condition is satisfied, the goal loop (`shiki loop`) may merge risk low/medium PRs autonomously (ADR 0008/0009). High and critical risk always require Guardian approval before merge.\n./skills/engineering/improve-codebase-architecture/SKILL.md:66:**ADR conflicts**: if a candidate contradicts an existing ADR, only surface it when the friction is real enough to warrant revisiting the ADR. Mark it clearly in the card (e.g. a warning callout: _\"contradicts ADR-0007 \u2014 but worth reopening because\u2026\"_). Don't list every theoretical refactor an ADR forbids.\n./skills/engineering/setup-matt-pocock-skills/domain.md:51:> _Contradicts ADR-0007 (event-sourced orders) \u2014 but worth reopening because\u2026_\n./pyproject.toml:12:# docs/adr/0007-packaging-and-release.md.\n./CONTRIBUTING.md:24:   (ADR 0009). Post-freeze scope changes require an operator-approved,\n./CONTEXT.md:104:Guardian approval of a high/critical-risk Goal's task contracts before any implementation starts, granted on the Contract PR and binding for every implementation that stays inside its Scope Envelope (ADR 0015). Guardian authorizes the contract, not the diff; whether the authorized work was implemented correctly stays with CCA and review.\n./CONTEXT.md:120:A first-class Guardian approval authority (ADR 0010) by which an external AI reviewer authorizes autonomous merge of a high/critical-risk PR through a head-SHA-bound `external_ai_guardian_review` artifact, recorded with the AI reviewer's own model identity (reviewer_type=external_ai_model).\n./CONTEXT.md:124:The deterministic review INPUT the External AI Guardian UI Adapter builds for a high/critical-risk PR (task contract, PR diff/checks, implementer report provenance, relevant docs, PR-type review focus areas, and known missing evidence). It is review context only, never approval evidence, and must not be committed by the PR under review (ADR 0014).\n./CONTEXT.md:128:The Codex App-side runtime that transports an External AI Guardian Review: it builds the packet, drives the ChatGPT Pro review UI, extracts the verdict, validates the fenced `external_ai_guardian_review` artifact, relays only the validated approval to GitHub, and routes non-approval verdicts back into Shiki as bounded repair/evidence work (ADR 0014). Claude Code is the implementer/repairer and must not operate this Guardian UI path for its own work.\n./scripts/shiki_guardian.py:48:    # external_ai_guardian_review: a first-class authority kind (ADR 0010).\n./scripts/shiki_guardian.py:766:    verify-response path consume it so neither can drift from ADR 0010 / ADR 0014\n./tests/test_closeout.py:5:Three defects this suite pins (ADR 0012):\n./scripts/test_shiki_governance_evidence.sh:745:# ADR 0017: bookkeeping-closeout Guardian exemption smuggling cases.\n./scripts/test_shiki_governance_evidence.sh:966:# Amendment mode (ADR 0009/0015): a Spec Amendment PR cannot lower its OWN\n./docs/proposals/0001-memory-loop-spec-freeze-review.md:319:- bypassPermissions bounding\uff08\u5225 Goal\u3001ADR 0008 \u8a18\u8f09\u306e\u30d5\u30a9\u30ed\u30fc\u30a2\u30c3\u30d7\uff09\n./docs/proposals/0001-memory-loop-spec-freeze-review.md:368:| SF-04 \u51cd\u7d50\u5f8c\u306e\u5909\u66f4\u306f Amendment \u306e\u307f | \u2705 \u5408\u610f\u6e08\u307f\uff08ADR 0009\uff09 |\n./tests/test_guardian_signal.py:1:\"\"\"Deterministic Guardian-approval signal for the CCA judge (ADR 0010).\n./tests/test_contract_immutability.py:1:\"\"\"ADR 0015 Contract immutability on the NORMAL task-PR path.\n./tests/test_contract_immutability.py:18:snapshot to check it against, it fails closed. Only the mutable set the ADR 0017\n./tests/test_contract_immutability.py:203:        # The mutable-field set is the SAME constant the ADR 0017 closeout\n./tests/test_workflow_invocation.py:59:# ADR 0018 base-sync carry: shiki-cca-completion.yml now has a top-level entry in\n./tests/test_workflow_invocation.py:127:    # ADR 0015 Contract Approval carry: the metadata job now passes\n./tests/test_workflow_invocation.py:133:    # ADR 0018 base-sync carry: the metadata job now also passes --default-branch.\n./tests/test_workflow_invocation.py:296:        mutated copy. shiki-cca-completion.yml is included because ADR 0018 added a\n./scripts/shiki_contract_approval.py:2:\"\"\"ADR 0015 Contract Approval: the pure evaluator MergeGate accepts as an\n./scripts/shiki_contract_approval.py:5:ADR 0015 moves Guardian approval for high/critical risk from the implementation\n./scripts/shiki_contract_approval.py:19:ADR 0015 \"Evaluation placement\": Contract Approval is evaluated by this new,\n./scripts/shiki_contract_approval.py:49:# applies. ADR 0015 is explicit that a ``contract_approval`` entry in the signal's\n./scripts/shiki_contract_approval.py:56:    \"\"\"Outcome of evaluating ADR 0015 Contract Approval for one task PR.\n./scripts/shiki_contract_approval.py:78:    (ADR 0015 condition 3). Empty list means the proof holds.\n./scripts/shiki_contract_approval.py:145:    \"\"\"Whether this PR carries an ADR 0015 Contract Approval.\n./docs/proposals/0002-autonomous-loop-completion.md:4:Goal issue: #140. Parent PRD: #119 (Shiki 0.2.0, G-D). Decision: ADR 0011.\n./docs/proposals/0002-autonomous-loop-completion.md:8:The Shiki 0.2.0 autonomous post-freeze loop (ADR 0008/0009, #119 G-D) is not\n./docs/proposals/0002-autonomous-loop-completion.md:34:via an independent verifier, never by the implementer self-attesting (ADR 0011).\n./docs/proposals/0002-autonomous-loop-completion.md:40:   (loop-observed green; red-first ordering is not gated \u2014 ADR 0011).\n./docs/proposals/0002-autonomous-loop-completion.md:69:- **Evidence ownership (ADR 0011, Q1):** the loop produces all quality-gate\n./docs/proposals/0002-autonomous-loop-completion.md:106:- High/critical-risk auto-merge (remains Guardian-gated, ADR 0010).\n./docs/proposals/0002-autonomous-loop-completion.md:110:- Deterministic red\u2192green verification (ADR 0011 future enhancement).\n./docs/proposals/0002-autonomous-loop-completion.md:125:  Guardian-reviewed per ADR 0010.\n./docs/proposals/0002-autonomous-loop-completion.md:127:  (gaps at file:line), ADR 0011, and a Workflow parallel exploration sweep\n./docs/proposals/0002-autonomous-loop-completion.md:129:- **Decisions:** ADR 0011 (autonomous quality-gate evidence). Domain terms:\n./scripts/test_shiki_loop_e2e.sh:41:# to the merged PR branch so the loop's closeout step (ADR 0012), which cuts a\n./scripts/test_shiki_loop_e2e.sh:217:    \"decisions\": [\"The loop drives frozen goals (ADR 0008/0009/0011)\"]\n./scripts/test_shiki_loop_e2e.sh:245:# Two merges now: the impl PR, then the ADR 0012 closeout PR that pushes the\n./tests/test_adr_references.py:4:with \"ADR 0017\" nine times and ``scripts/guardian_approval_signal.py`` cites\n./tests/test_adr_references.py:34:# \"ADR 0016\" -- a four-digit citation. The trailing \\b keeps a five-digit token\n./tests/test_adr_references.py:168:        # A script cites ADR 9991, but no docs/adr/9991-*.md exists.\n./tests/test_adr_references.py:171:            script_body='\"\"\"See ADR 9991 for the rule.\"\"\"\\n',\n./tests/test_adr_references.py:181:        self.assertIn(\"docs/adr/9991-*.md\", message)  # missing path\n./tests/test_adr_references.py:188:            script_body='\"\"\"Only mentions ADR 0001 here.\"\"\"\\n',\n./tests/test_adr_references.py:198:            script_body='\"\"\"Cites ADR 0007 once.\"\"\"\\n',\n./tests/test_adr_references.py:207:            script_body='\"\"\"Cites ADR 0016 which exists.\"\"\"\\n',\n./scripts/mergegate_check.py:302:# A Contract PR (ADR 0015) declares itself with this exact HTML-comment marker.\n./scripts/mergegate_check.py:310:# that opens a Contract PR must NOT apply this label to its own PR (ADR 0015\n./scripts/mergegate_check.py:335:# An Amendment PR (Spec Amendment, ADR 0009/0015) declares itself with this exact\n./scripts/mergegate_check.py:595:    ``allow_goal_registration`` is set (contract mode, ADR 0015), add the goal\n./scripts/mergegate_check.py:615:    # mode (ADR 0015) may carry the Goal's own spec-frozen source plan to the\n./scripts/mergegate_check.py:702:            # Contract mode only (ADR 0015): the Contract PR registers the goal\n./scripts/mergegate_check.py:708:            # Contract flow only (ADR 0015): the Contract PR may carry the Goal's\n./scripts/mergegate_check.py:782:    \"\"\"Validate a Contract PR (ADR 0015): register a spec-frozen Goal's task\n./scripts/mergegate_check.py:790:    ``allow_source_plan_carry`` opts into the ADR 0015 plan-reachability escape:\n./scripts/mergegate_check.py:860:    \"\"\"Validate the spec-frozen source plan a Contract PR carries (ADR 0015).\n./scripts/mergegate_check.py:862:    ADR 0015 has a Contract PR carry only Goal/task/DAG registration and assumes\n./scripts/mergegate_check.py:949:# a completion \u2014 must never rewrite. This is the ADR 0017 mutable set minus\n./scripts/mergegate_check.py:1124:    \"\"\"Validate a Spec Amendment PR (ADR 0009/0015): correct a spec-frozen Goal's\n./scripts/mergegate_check.py:1941:            # ADR 0017: a PROVEN bookkeeping closeout carries no implementation and\n./scripts/mergegate_check.py:1972:# reached \u2014 the exact defect ADR 0017 exists to remove. That independent head-ref\n./scripts/mergegate_check.py:2009:    \"\"\"Classify whether a PR is a pure bookkeeping closeout (ADR 0017).\n./scripts/mergegate_check.py:2028:    The six ADR 0017 conditions:\n./scripts/mergegate_check.py:2243:    # ADR 0015 Contract Approval OR-branch (\"Evaluation placement\"). The Guardian\n./scripts/mergegate_check.py:2256:            + \" (ADR 0015 Contract Approval: the task contract was registered and Guardian-approved before dispatch)\"\n./scripts/mergegate_check.py:2482:# (ADR 0015 Contract immutability, normal path). When the task file is present in\n./scripts/mergegate_check.py:2493:# the mutable set stays the single ADR 0017 constant, never re-copied here.\n./scripts/mergegate_check.py:2509:    than the base snapshot (ADR 0015). Returns ``max(base_risk, head_risk)`` by the\n./scripts/mergegate_check.py:2527:    governance contract (ADR 0015 Contract immutability, normal path).\n./scripts/mergegate_check.py:2533:    base-when-present, exactly as ADR 0015 states:\n./scripts/mergegate_check.py:2563:            \"its frozen governance contract (ADR 0015 Contract immutability); failing closed\"\n./scripts/mergegate_check.py:2574:            \"read to verify the frozen governance contract (ADR 0015 Contract immutability); failing closed\"\n./scripts/mergegate_check.py:2591:                \"from the base snapshot (ADR 0015 Contract immutability)\"\n./scripts/mergegate_check.py:2609:    \"\"\"Load the ADR 0015 Contract Approval registration proof from the file named\n./scripts/mergegate_check.py:2637:    \"\"\"Evaluate ADR 0015 Contract Approval for a NORMAL task PR (the OR alternative\n./scripts/mergegate_check.py:2646:    degrades to None (\"does not apply\"), exactly the ADR 0015 failure mode.\n./scripts/mergegate_check.py:2778:            # Spec Freeze is operator-only (ADR 0009). No PR may author or alter a\n./scripts/mergegate_check.py:2873:            \"Path to the ADR 0015 Contract Approval registration proof (JSON). When present and valid, a \"\n./scripts/mergegate_check.py:2911:    # closeout classifier to prove the task's implementation PR merged (ADR 0017).\n./scripts/mergegate_check.py:2918:    # Guardian gate evaluates it as if the task were low risk (ADR 0017). Stays\n./scripts/mergegate_check.py:2981:            # it carries no single implementation task id (ADR 0015).\n./scripts/mergegate_check.py:3028:        # Contract mode (ADR 0015): a goal-scoped, spec-frozen task-contract\n./scripts/mergegate_check.py:3045:            # to the default branch (ADR 0015 plan-reachability gap); enable it.\n./scripts/mergegate_check.py:3076:        # Amendment mode (ADR 0009/0015): a goal-scoped PR that corrects a\n./scripts/mergegate_check.py:3211:            # ADR 0017: a proven bookkeeping closeout (task terminal state only, no\n./scripts/mergegate_check.py:3240:                # ADR 0015 Contract immutability on the NORMAL task-PR path: a PR\n./scripts/mergegate_check.py:3335:                # Normal path (ADR 0015): resolve the Guardian risk NEVER WEAKER\n./scripts/mergegate_check.py:3354:            # ADR 0015 Contract Approval applies ONLY on the normal task path: a\n./tests/test_policy_job_merged_prs.py:2:EVERY judged PR, not only ``post_merge_reconcile`` ones (ADR 0017).\n./tests/test_loop_lock_validator_hint.py:3:Under the mirror-lock model (ADR 0016 / A-LOCKS) a registered task's stored\n./tests/test_loop_lock_validator_hint.py:11:WARN-ONLY; the future mirror-identity rule (ADR 0016 step B / remediation R-02)\n./tests/test_loop_lock_validator_hint.py:55:        # advisory neither requires nor forbids it (ADR 0016 step B / R-02).\n./scripts/shiki_runtime_registry.py:97:        description=\"Local Claude Code planning, review, and default implementation/runner runtime (ADR 0008).\",\n./scripts/test_shiki_control_plane.sh:155:# ADR 0015 Contract Approval carry (T-20260801T040836632579Z-2cde7018). BOTH the\n./scripts/test_shiki_control_plane.sh:188:# ADR 0018 base-sync carry (guardian_comment_carried) reaches CI\n./scripts/test_shiki_control_plane.sh:1588:    # ADR 0015 Contract immutability (normal task-PR path): a PR may move its own\n./scripts/test_shiki_control_plane.sh:1600:    # ADR 0015 Contract immutability (normal task-PR path): a GOVERNANCE field\n./scripts/shiki_bootstrap.py:350:            \"Plans cannot run without an operator-approved spec_freeze (ADR 0009).\"\n./scripts/guardian_approval_signal.py:11:label/review/comment OR an external AI guardian review artifact, ADR 0010)\n./scripts/guardian_approval_signal.py:32:# The bookkeeping-closeout exemption (ADR 0017) must resolve risk IDENTICALLY on\n./scripts/guardian_approval_signal.py:155:    \"\"\"Whether this PR is a proven ADR 0017 bookkeeping closeout.\n./scripts/guardian_approval_signal.py:200:    \"\"\"Evaluate ADR 0015 Contract Approval for the CCA Guardian signal.\n./scripts/guardian_approval_signal.py:251:    # ADR 0017 bookkeeping-closeout exemption inputs. All default to values that\n./scripts/guardian_approval_signal.py:257:        help=\"Path to the base branch's .shiki snapshot; required for the ADR 0017 closeout exemption.\",\n./scripts/guardian_approval_signal.py:262:        help=\"git --name-status changed-files file; required for the ADR 0017 closeout exemption.\",\n./scripts/guardian_approval_signal.py:269:    # ADR 0015 Contract Approval input. Defaults to \"\" (no proof), so an\n./scripts/guardian_approval_signal.py:276:            \"Path to the ADR 0015 Contract Approval registration proof (JSON). When present and valid, a \"\n./scripts/guardian_approval_signal.py:319:    # ADR 0017: a PROVEN bookkeeping closeout carries no implementation and\n./scripts/guardian_approval_signal.py:377:            note += \" (ADR 0017 bookkeeping-closeout exemption applied)\"\n./scripts/guardian_approval_signal.py:391:    # ADR 0015 Contract Approval OR-branch. The Guardian requirement is satisfied\n./scripts/guardian_approval_signal.py:419:                \"Guardian approval satisfied by Contract Approval (ADR 0015): the task contract was \"\n./scripts/shiki_loop.py:2:\"\"\"Autonomous post-freeze Goal loop (ADR 0008/0009).\n./scripts/shiki_loop.py:52:# review/label/comment OR an external AI guardian review, ADR 0010) and is the\n./scripts/shiki_loop.py:316:        # goal only in the coordinator mirror (Gap B / ADR 0012). Drive a closeout\n./scripts/shiki_loop.py:362:    # recorded authority. This closes the impersonation pathway ADR 0010 exists\n./scripts/shiki_loop.py:425:        # laundered into a head-moving sync (ADR 0010). It also never fires while the\n./scripts/shiki_loop.py:427:        # red check \u2014 so a head-bound Guardian approval, or an ADR 0015\n./scripts/shiki_loop.py:485:    # (human review/label/comment OR an external AI guardian review, ADR 0010).\n./scripts/shiki_loop.py:1379:    \"context (ADR 0011). Review ONLY the diff below for correctness bugs, broken \"\n./scripts/shiki_loop.py:1409:            \"command\": \"independent pre-PR code review (claude -p, read-only) \u2014 ADR 0011\",\n./scripts/shiki_loop.py:1434:    Loop-owned quality-gate step (ADR 0011): the reviewer is the same model as the\n./scripts/shiki_loop.py:1558:            evidence=[\"independent read-only reviewer (claude -p) \u2014 ADR 0011\"],\n./scripts/shiki_loop.py:1574:        evidence=[\"independent read-only reviewer (claude -p) \u2014 ADR 0011\"],\n./scripts/shiki_loop.py:1587:    never be handed to a shell (ADR 0011: a deterministic observable command,\n./scripts/shiki_loop.py:1602:    \"\"\"Loop-observed TDD gate (ADR 0011): run the task's tests in its worktree.\n./scripts/shiki_loop.py:1690:    \"\"\"PR body for an autonomous closeout PR (ADR 0012). Must contain the literal\n./scripts/shiki_loop.py:1701:        f\"Autonomous loop closeout (ADR 0012): the implementation PR for this task \"\n./scripts/shiki_loop.py:1711:        f\"code-review gate, ADR 0011) before that PR merged. This PR carries only \"\n./scripts/shiki_loop.py:1718:        f\"\\U0001f916 Generated by the Shiki goal loop (ADR 0012)\\n\"\n./scripts/shiki_loop.py:1724:    (goal=complete iff this task completes the goal) to main \u2014 Gap B / ADR 0012.\n./scripts/shiki_loop.py:1796:                    summary=\"Autonomous loop closeout: push goal completion to main (ADR 0012).\"))\n./scripts/shiki_loop.py:1815:        commit = run([\"git\", \"commit\", \"-m\", f\"shiki: closeout {task_id} \u2014 push completion to main (goal loop, ADR 0012)\"], cwd=worktree, check=False)\n./scripts/shiki_loop.py:1826:             \"--title\", f\"Closeout {task_id}: push goal completion to main (ADR 0012)\",\n./scripts/shiki_loop.py:1844:                     + \" pushed to main by the goal loop (ADR 0012).\"),\n./scripts/shiki_loop.py:2263:            # the scorecard report + completion ledger) to main (ADR 0012). Sync the\n./scripts/shiki_loop.py:2294:        # (a) Pre-PR code-review gate (ADR 0011). An INDEPENDENT read-only\n./scripts/shiki_loop.py:2318:        # Loop-owned TDD gate FIRST (ADR 0011): the loop \u2014 an independent\n./scripts/shiki_loop.py:2477:        # ADR 0012: the impl PR merged; open a closeout PR that pushes the terminal\n./scripts/shiki_loop.py:2528:        # ADR 0012: done-marking is DEFERRED. The loop never records `done` locally\n./scripts/test_shiki_goal_loop.sh:75:    # The independent pre-PR reviewer (ADR 0011) carries --json-schema and\n./scripts/test_shiki_goal_loop.sh:141:    # origin/main) sees the merged impl/closeout state (ADR 0012). The branch is a\n./scripts/test_shiki_goal_loop.sh:196:    \"decisions\": [\"Loop drives frozen goals (ADR 0008/0009); completion is pushed to main via a closeout PR (ADR 0012)\"]\n./scripts/test_shiki_goal_loop.sh:222:# PR that pushes task=done + goal=complete to main (ADR 0012) -> auto-merge the\n./scripts/test_shiki_goal_loop.sh:227:# The loop opened a closeout PR (ADR 0012): its history records create_closeout_pr.\n./scripts/test_shiki_runtime_registry.sh:69:# ADR 0008: claude-code is the default implementer/runner; codex stays a\n./scripts/shiki_migrations.py:155:            \"Plan ingest/run/daemon/smoke require spec_freeze.status=frozen (ADR 0009).\",\n./scripts/shiki_migrations.py:233:            description=\"Add the additive spec_freeze block (ADR 0009) to stored plans that predate the Spec Freeze contract so they can be re-ingested or re-run.\",\n./scripts/test_shiki_adversarial_state.sh:506:# ADR 0015 Contract immutability on the NORMAL task-PR path (T-20260801\u20269f3f6f63):\n./scripts/test_shiki_adversarial_state.sh:808:# ADR 0017: bookkeeping-closeout Guardian exemption smuggling cases.\n./scripts/test_shiki_adversarial_state.sh:1134:# Spec Freeze is operator-only (ADR 0009). A new plan that is not the goal's\n./scripts/test_shiki_adversarial_state.sh:1202:# Amendment mode (ADR 0009/0015) is two-factor (marker + mergegate:amendment\n./tests/test_loop_branch_plumbing.py:416:    \"\"\"ADR 0012: the effector pushes the terminal state to main + repoints expected_pr.\"\"\"\n./tests/test_workflow_invocation_carry.py:1:\"\"\"ADR 0018 base-sync carry (guardian_comment_carried) reaches CI.\n./scripts/validate_shiki.py:304:# post_merge_reconcile PR (and the ADR 0017 bookkeeping-closeout exemption) can be\n./scripts/validate_shiki.py:307:# ADR 0018 base-sync carry (guardian_comment_carried): every invocation that judges\n./scripts/validate_shiki.py:762:        raise ValidationError(f\"{GUARDIAN_POLICY_PATH}: external_ai_guardian_review must bind to the head SHA (ADR 0010)\")\n./scripts/validate_shiki.py:2088:    Under the derive-at-judgment-time mirror-lock model (ADR 0016 / A-LOCKS) a\n./scripts/validate_shiki.py:2102:    (ADR 0016 step B / remediation R-02) will decide any blocking behaviour, and\n./scripts/shiki_runtime_adapters.py:159:# The independent pre-PR code-review verifier (ADR 0011). It is the SAME model as\n./scripts/test_shiki_code_review_gate.sh:60:    \"decisions\": [\"code-review is a mandatory pre-PR implementer gate (ADR 0008)\"]\n./tests/test_contract_amendment.py:1:\"\"\"Spec Amendment PR mode (ADR 0009/0015): correct a frozen contract on a\n./scripts/test_shiki_runner_claude.sh:51:    \"decisions\": [\"Use Claude Code as the default implementation runtime (ADR 0008)\"]\n./scripts/test_shiki_run_orchestrator.sh:83:# A grilled but unfrozen plan must be rejected (ADR 0009 Spec Freeze gate).\n./scripts/shiki_tasks.py:26:# Contract PR mode (ADR 0015). The body marker DECLARES intent; the\n./scripts/shiki_tasks.py:86:# Safe default for the loop-observed TDD gate (ADR 0011): the command the goal\n./scripts/shiki_tasks.py:229:            \"before Shiki can run it; see CONTEXT.md 'Spec Freeze' and ADR 0009\"\n./scripts/shiki_tasks.py:342:        # The loop-observed TDD gate (ADR 0011) exec's THIS structured command in\n./scripts/shiki_tasks.py:699:    PR (ADR 0015).\n./scripts/shiki_tasks.py:877:    \"\"\"Open a Contract PR (ADR 0015): register a spec-frozen Goal's task contracts.\n./scripts/shiki_tasks.py:957:        # Structured loop-observed TDD command (ADR 0011); falls back to the safe\n./tests/test_loop_e2e_contract.py:1:\"\"\"End-to-end loop self-drive CONTRACT meta-test (PRD 0002 T5, ADR 0011).\n./scripts/shiki_guardian_review.py:2:\"\"\"External AI Guardian Review adapter contract (ADR 0010 / ADR 0014).\n./scripts/shiki_guardian_review.py:15:Role boundary (ADR 0010 / ADR 0014): Claude Code implements these DETERMINISTIC\n./scripts/test_shiki_start.sh:132:# Start without explicit Spec Freeze approval must fail (ADR 0009).\n./scripts/test_shiki_start.sh:180:# protection, through cmd_start -> cmd_init (ADR 0013).\n./scripts/test_shiki_guardian_policy.sh:594:# ADR 0015 Contract Approval (end-to-end): a contract-approved implementation PR\n./scripts/test_shiki_loop_lock_guard.sh:5:# (ADR 0016 / A-LOCKS).\n./tests/test_mirror_scope.py:28:And Spec Freeze is operator-only (ADR 0009): no PR may author or alter a\n./tests/test_loop_code_review.py:1:\"\"\"Pre-PR independent code-review verifier (PRD 0002 T3, ADR 0011).\n./scripts/shiki_cli.py:199:    contract = subcommands.add_parser(\"contract\", help=\"Open a Contract PR that registers a spec-frozen Goal's task contracts (ADR 0015)\")\n./tests/test_cca_evidence_manifest_taskless.py:3:Regression coverage for the contract-mode (ADR 0015) defect where the CCA\n./tests/test_contract_open.py:7:path, so the suite adds no approval record to real mirror state (ADR 0015: an\n./tests/test_guardian_signal_closeout.py:1:\"\"\"The bookkeeping-closeout Guardian exemption on the CCA *signal* path (ADR 0017).\n./tests/test_guardian_signal_closeout.py:8:MergeGate *metadata* check (which learned the ADR 0017 exemption) but the CCA\n./tests/test_guardian_signal_closeout.py:16:* each ADR 0017 disqualifier IN ISOLATION re-requires Guardian approval;\n./scripts/shiki_github.py:610:    in solo operation after CCA returns ``complete`` (see ADR 0013 and\n./scripts/shiki_github.py:704:    \"\"\"The PR-12 ``## Pre-PR code review`` body section (ADR 0011).\n./scripts/shiki_github.py:721:    lines.append(\"- Independent read-only reviewer (claude -p, read tools only) \u2014 ADR 0011\")\n./scripts/shiki_github.py:770:            \"(ADR 0011); a red run blocks the PR.\",\n./tests/test_runner_adapters.py:1:\"\"\"Runtime adapter boundary contract (ADR 0008).\n./docs/agents/shiki-cli-architecture.md:28:| `scripts/shiki_runtime_adapters.py` | Runner adapter boundary: per-runtime tool/auth probes and headless execution commands bound to registry runtime names (ADR 0008). |\n./tests/test_dispatch_lock_precondition.py:10:ADR 0017 bookkeeping exemption needs the lock to move ``active -> released`` and\n./docs/agents/triage-labels.md:23:- `risk:medium` - requires normal review; goal-loop auto-merge permitted once all MergeGate evidence is green (ADR 0008).\n./tests/test_repair_delivery.py:168:    origin/main tracking ref) so the ADR-0012 closeout can cut from it.\"\"\"\n./tests/test_loop_tdd_evidence.py:1:\"\"\"Loop-observed TDD evidence + per-task test command (PRD 0002 T2, ADR 0011).\n./tests/test_loop_tdd_evidence.py:4:in the worktree \u2014 never trust the implementer's self-attestation (ADR 0011:\n./tests/test_completion_lock_scope.py:24:lock-narrowing discipline (ADR 0016) replaced that directory glob with named\n./tests/test_loop_engine.py:1:\"\"\"Goal loop decision engine contract (G-D, ADR 0008/0009).\n./tests/test_loop_engine.py:67:        # ADR 0012: a merged impl PR (no closeout_pr yet) routes to a closeout PR\n./tests/test_loop_engine.py:189:        # loop may merge high/critical autonomously (ADR 0010).\n./tests/test_contract_approval.py:1:\"\"\"ADR 0015 Contract Approval \u2014 the OR-alternative to a live PR Guardian approval.\n./tests/test_contract_approval.py:3:ADR 0015 shipped in halves. The registration half (contract mode,\n./docs/agents/implementation-policy.md:41:6. **The assigned implementer implements; CCA judges.** Claude Code is the default implementer and repairer (ADR 0008); Codex Front implements tasks explicitly assigned to `codex`. GitHub CCA is the default completion judge.\n./docs/agents/implementation-policy.md:46:11. **Spec Freeze gates execution.** No plan runs without an operator-approved `spec_freeze` block (ADR 0009). Post-freeze scope changes require an operator-approved, recorded Spec Amendment; non-scope-moving interpretations are recorded in the Assumption Log.\n./docs/agents/implementation-policy.md:47:12. **Approval records are authored only by their authority.** An implementation runtime must never author, edit, forge, or backfill an operative approval record \u2014 a `spec_freeze` block, a Spec Amendment approval, Guardian approval evidence, a CCA `complete` verdict, or any other artifact whose meaning is that an authority approved something. An acceptance criterion that cannot be satisfied from the worktree because the missing artifact is such a record is reported as a stop, never closed by manufacturing it. This does not cover approval-record schemas or clearly-scoped test fixtures that never enter the real `.shiki/` mirror (ADR 0009/0010).\n./tests/test_branch_behind_sync.py:164:        # laundered into a head-moving sync (ADR 0010). reruns=2 exhausts the CCA\n./tests/test_branch_behind_sync.py:177:        # be the impersonation pathway ADR 0010 forbids.\n./tests/test_branch_behind_sync.py:233:        # docs/adr/0012 pins the closeout decision table: a bookkeeping closeout has\n./tests/test_contract_plan_registration.py:1:\"\"\"A Contract PR may carry its Goal's own spec-frozen source plan (ADR 0015 gap).\n./tests/test_contract_plan_registration.py:3:ADR 0015 has a Contract PR carry \"only Goal, task, and DAG registration\" and\n./tests/test_paired_invariants.py:726:        # (`ADR-0015`) out.\n./docs/agents/runtime-registry.md:36:| `claude-code` | planner, implementer, runner, reviewer | local_cli | claude_subscription_oauth | Local Claude Code planning, review, and the default implementer/runner runtime (ADR 0008), dispatched with `shiki runner claude`. |\n./docs/agents/codex-handoff.md:5:Claude Code through `shiki runner claude` (ADR 0008); the handoff contract\n./tests/test_guardian_review.py:1:\"\"\"Tests for the External AI Guardian Review adapter contract (ADR 0010/0014).\n./tests/test_guardian_review.py:56:        relevant_docs=[\"docs/adr/0010-external-ai-guardian-review.md\"],\n./tests/test_guardian_review.py:164:        focus = gr.classify_review_focus([\"docs/adr/0099-x.md\", \"README.md\"], \"\", \"low\")\n./tests/test_guardian_review.py:174:        focus = gr.classify_review_focus([\"docs/adr/0099-x.md\"], \"\", \"critical\")\n./docs/agents/runtime-auth-model.md:7:- **Claude Code**: the default implementer and runner runtime (ADR 0008). `shiki runner claude` dispatches a headless `claude -p` session into the task's registered worktree, authenticated by the operator's Claude subscription login.\n./docs/agents/runtime-auth-model.md:9:- **Codex App External AI Guardian UI Adapter**: the operator-facing adapter that builds External AI Guardian Review Packets, drives ChatGPT Pro review through the UI, extracts the reviewer verdict, validates the fenced `external_ai_guardian_review` artifact, and relays only validated approvals to GitHub (ADR 0014). It consumes the deterministic `shiki guardian packet|prompt|verify-response` surfaces. **Claude Code implementer/repairer sessions must not drive this Guardian UI path for their own work** \u2014 that boundary keeps implementation distinct from approval routing.\n./docs/agents/autonomous-loop-e2e-acceptance.md:4:ADR 0011 autonomous quality-gate work). It proves the second, non-simulated half\n./docs/agents/autonomous-loop-e2e-acceptance.md:16:ADR 0011's whole point is that autonomous evidence must be **deterministic\n./docs/agents/autonomous-loop-e2e-acceptance.md:64:those are loop-owned state transitions (ADR 0011). If the operator writes them,\n./docs/agents/autonomous-loop-e2e-acceptance.md:130:Per ADR 0011 / the Repair Loop: do not hand-clear the PR to \"make the gate\n./docs/agents/autonomous-loop-e2e-acceptance.md:135:self-attestation ADR 0011 forbids.\n./tests/test_contract_mode.py:1:\"\"\"Contract PR mode (ADR 0015): approve the task contract before dispatch.\n./tests/test_contract_mode.py:77:                \"title\": \"ADR 0015 contract approval\",\n./docs/releasing.md:6:[docs/adr/0007-packaging-and-release.md](adr/0007-packaging-and-release.md).\n./docs/releasing.md:105:  (see [docs/adr/0007-packaging-and-release.md](adr/0007-packaging-and-release.md)).\n./tests/test_bookkeeping_closeout.py:1:\"\"\"The bookkeeping-closeout Guardian exemption (ADR 0017).\n./tests/test_bookkeeping_closeout.py:290:        # the point of ADR 0017's whitelist fix: it FAILS if expected_branch is\n./tests/test_bookkeeping_closeout.py:341:        # Guard against the whitelist drifting wider than the one entry ADR 0017\n./docs/agents/guardian-policy.md:64:## Carried Guardian Approval Across A Proven Base Sync (ADR 0018)\n./docs/agents/guardian-policy.md:105:## External AI Guardian Review (ADR 0010)\n./docs/agents/guardian-policy.md:128:## External AI Guardian UI Adapter (ADR 0014)\n\nexec\n/bin/zsh -lc \"rg -n -i --glob '\"'!.shiki/**'\"' 'adr.reference|find_adr|SADR|_ADR_|ADR \\\\\\\\d|docs/adr' scripts tests docs AGENTS.md CLAUDE.md CONTEXT.md CONTRIBUTING.md SYSTEM_PROMPT.md .github .codex pyproject.toml\" in /private/tmp/shiki-adr-collision.pa0KwY/.worktrees/shiki-t-20260807t073307221254z-2d22d654-introduc\n succeeded in 0ms:\npyproject.toml:12:# docs/adr/0007-packaging-and-release.md.\nSYSTEM_PROMPT.md:29:4. `docs/adr/` records hard-to-reverse platform decisions.\nCONTRIBUTING.md:106:record durable decisions in an ADR when needed (see `docs/adr/`).\nCLAUDE.md:13:4. `docs/adr/` records hard-to-reverse platform decisions.\nCLAUDE.md:48:- `docs/adr/`\nAGENTS.md:43:4. `docs/adr/` records hard-to-reverse platform decisions.\nAGENTS.md:399:Shiki is a single-context platform repository unless `CONTEXT-MAP.md` says otherwise. `CONTEXT.md` is the glossary and `docs/adr/` holds durable decisions. See `docs/agents/domain.md`.\n.codex/skills/shiki/SKILL.md:17:4. `docs/adr/` records hard-to-reverse platform decisions.\n.github/prompts/cca-completion-check.md:18:4. `docs/adr/` records hard-to-reverse platform decisions.\n.github/prompts/cca-completion-check.md:29:4. relevant `docs/adr/`\ndocs/agents/implementation-policy.md:30:4. `docs/adr/` records hard-to-reverse platform decisions.\ndocs/agents/implementation-policy.md:54:- Install or define `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, `docs/adr/`, and `docs/agents/`.\n.github/workflows/release.yml:6:# docs/adr/0007-packaging-and-release.md.\ndocs/agents/domain.md:8:- `docs/adr/` records hard-to-reverse decisions.\ntests/test_branch_behind_sync.py:233:        # docs/adr/0012 pins the closeout decision table: a bookkeeping closeout has\nscripts/shiki_contracts.py:103:    \"`docs/adr/` records hard-to-reverse platform decisions.\",\nscripts/shiki_guardian_review.py:116:_DOC_PREFIXES = (\"docs/\", \"docs/adr/\")\nscripts/shiki_guardian_review.py:162:            \"Docs/ADR-only change: confirm the change is genuinely documentation/decision-only, introduces no behavior or contract drift, and stays consistent with the constitution and existing ADRs.\"\ndocs/releasing.md:6:[docs/adr/0007-packaging-and-release.md](adr/0007-packaging-and-release.md).\ndocs/releasing.md:105:  (see [docs/adr/0007-packaging-and-release.md](adr/0007-packaging-and-release.md)).\nscripts/shiki_installer.py:40:    \"docs/adr\",\ntests/test_guardian_review.py:56:        relevant_docs=[\"docs/adr/0010-external-ai-guardian-review.md\"],\ntests/test_guardian_review.py:164:        focus = gr.classify_review_focus([\"docs/adr/0099-x.md\", \"README.md\"], \"\", \"low\")\ntests/test_guardian_review.py:165:        self.assertTrue(any(\"Docs/ADR-only\" in f for f in focus))\ntests/test_guardian_review.py:174:        focus = gr.classify_review_focus([\"docs/adr/0099-x.md\"], \"\", \"critical\")\ntests/test_adr_references.py:5:0016/0017 seven times, yet for a stretch the numbering in ``docs/adr/`` stopped\ntests/test_adr_references.py:7:declared ``docs/adr/...`` in its locks, so nothing carried the records to the\ntests/test_adr_references.py:9:Guardian approvals could not be read. ``grep -n 'docs/adr' scripts/validate_shiki.py``\ntests/test_adr_references.py:17:number resolves to exactly one ``docs/adr/####-*.md``. The converse is\ntests/test_adr_references.py:38:_ADR_FILENAME = re.compile(r\"^(\\d{4})-.*\\.md$\")\ntests/test_adr_references.py:48:    matches: Sequence[str]  # repo-relative paths matching docs/adr/<n>-*.md\ntests/test_adr_references.py:52:        return f\"docs/adr/{self.number}-*.md\"\ntests/test_adr_references.py:69:def _adr_index(root: Path) -> dict[str, list[Path]]:\ntests/test_adr_references.py:75:            match = _ADR_FILENAME.match(path.name)\ntests/test_adr_references.py:92:def find_adr_reference_violations(\ntests/test_adr_references.py:97:    A citation resolves when exactly one ``docs/adr/####-*.md`` matches its\ntests/test_adr_references.py:103:    index = _adr_index(root)\ntests/test_adr_references.py:126:    def test_every_cited_adr_resolves(self) -> None:\ntests/test_adr_references.py:127:        violations = find_adr_reference_violations(REPO_ROOT)\ntests/test_adr_references.py:133:            f\"docs/adr/####-*.md:\\n{detail}\",\ntests/test_adr_references.py:148:        index = _adr_index(REPO_ROOT)\ntests/test_adr_references.py:168:        # A script cites ADR 9991, but no docs/adr/9991-*.md exists.\ntests/test_adr_references.py:173:        violations = find_adr_reference_violations(root, include_docs=False)\ntests/test_adr_references.py:181:        self.assertIn(\"docs/adr/9991-*.md\", message)  # missing path\ntests/test_adr_references.py:183:    def test_uncited_adr_is_allowed(self) -> None:\ntests/test_adr_references.py:191:            find_adr_reference_violations(root, include_docs=False), []\ntests/test_adr_references.py:200:        violations = find_adr_reference_violations(root, include_docs=False)\ntests/test_adr_references.py:210:            find_adr_reference_violations(root, include_docs=False), []\ndocs/adr/README.md:9:- New ADRs are added as `docs/adr/NNNN-<short-slug>.md`, where `NNNN` is the next\ndocs/adr/README.md:11:- The **canonical list of ADRs is the set of files matching `docs/adr/*.md`** in\ndocs/adr/README.md:12:  this directory. Browse `docs/adr/*.md` for the authoritative, current set.\ndocs/adr/README.md:18:Using the `docs/adr/*.md` glob as the source of truth means other PRs can add an\ndocs/adr/README.md:25:in doubt, list `docs/adr/*.md`.\ndocs/adr/README.md:36:1. Copy `template.md` to `docs/adr/NNNN-<short-slug>.md`.\ndocs/adr/README.md:40:   the ADR to be discoverable via `docs/adr/*.md`.\n\n2026-08-07T08:04:48.756572Z ERROR codex_models_manager::manager: failed to renew cache TTL: missing field `base_instructions` at line 94 column 5\nexec\n/bin/zsh -lc \"for f in docs/adr/[0-9][0-9][0-9][0-9]-*.md; do sed -n '1,28p' \\\"\"'$f\"; done' in /private/tmp/shiki-adr-collision.pa0KwY/.worktrees/shiki-t-20260807t073307221254z-2d22d654-introduc\n succeeded in 0ms:\n# Keep Shiki As The Platform Name\n\nShiki remains the product name for the GitHub-native agentic engineering control plane. We considered more descriptive names such as MergePilot, MergeGate, and Kiroku, but kept Shiki because it carries the existing goal-seeking framework identity while remaining independent of Hermes, Codex, Claude Code, or any single runtime.\n# Use AGENTS As The Shared Agent Constitution\n\nShiki uses `AGENTS.md` as the runtime-neutral constitution and `CLAUDE.md` as a thin Claude Code wrapper. This keeps Codex, Claude Code, Hermes Runner, and future Agent Runtimes aligned on the same GitHub-first MergeGate rules while still allowing runtime-specific instructions where needed.\n# Require Skill Gate For MergeGate Readiness\n\nShiki requires relevant engineering skills to be invoked before execution when their triggers apply. This makes skills part of MergeGate readiness rather than optional prompt style, because Shiki depends on repeatable planning, issue decomposition, TDD, diagnosis, triage, and architecture review before autonomous or parallel agent work can be trusted.\n# ADR 0004: Use GitHub First With A Repository-Local Shiki Mirror\n\n## Status\n\nAccepted\n\n## Context\n\nShiki must coordinate multiple agent runtimes without relying on one hidden chat session or one local checkout. The operator wants Codex, Claude Code, Hermes Runner, GitHub Actions, and future runtimes to participate through a durable, auditable workflow.\n\n## Decision\n\nGitHub is the operational source of truth for Goals, Issues, Pull Requests, Checks, Reviews, comments, and merge evidence.\n\nEach target repository also keeps a `.shiki/` mirror for Goals, plans, task DAGs, contracts, locks, ledger entries, reports, and handoffs.\n\n## Consequences\n\n- Runtimes can recover context from GitHub and `.shiki/` without relying on chat memory.\n- Pull request boundaries become the preferred coordination surface between runtimes.\n- `.shiki/` conflicts must be repaired against GitHub operational state.\n- Shiki remains runtime-agnostic instead of becoming a Codex-only, Claude-only, or Hermes-only system.\n# ADR 0005: Use Codex As Front And Claude Code Action As The GitHub Runtime\n\n## Status\n\nAccepted (implementer default superseded by ADR 0008; the auth model and the\nClaude Code Action GitHub runtime decision remain in force)\n\n## Context\n\nShiki should run inside the operator's subscription-authenticated toolchain. The intended default is not to run Codex as an API-key backed GitHub Action.\n\nThe operator uses Codex as the front surface for implementation and long-running Goal work. Claude Code runs in GitHub Actions for review, automation, and MergeGate-oriented judgment.\n\n## Decision\n\nUse this default split:\n\n- Codex Front: implementation, tests, local repair, and operator-facing Goal work through Codex App, Codex CLI, Codex IDE extension, or Codex Web signed in with ChatGPT OAuth/subscription auth.\n- Claude Code Action: GitHub PR review and automation using `CLAUDE_CODE_OAUTH_TOKEN`.\n- GitHub Actions: validation evidence and Claude Code Action execution.\n\nThe Shiki template must not include `openai/codex-action` as the default implementation path.\n\n## Consequences\n\n- Shiki avoids requiring `OPENAI_API_KEY` for the default workflow.\n- Codex work happens through the user's authenticated Codex front surface, not as a hidden backend Action.\n- Claude Code Action is configured for OAuth-token based subscription usage by default.\n# ADR 0006: Separate Codex Implementation, CCA Completion, And MergeGate Authorization\n\n## Status\n\nAccepted\n\n## Context\n\nShiki needs autonomous implementation without letting a single agent decide that its own work is complete, mergeable, or deployable.\n\nThe standard pipeline is:\n\n```text\nGoal Seek\n  -> grill-with-docs\n  -> Context & Impact\n  -> to-prd\n  -> to-issues\n  -> triage\n  -> Task DAG + locks\n  -> Codex Front + tdd\n  -> PR + evidence\n  -> GitHub CCA completion judgment\n  -> MergeGate\n  -> bounded Repair Loop\n  -> merge\n  -> Goal completion judgment\n```\n# ADR 0007: Packaging And Release For Shiki\n\n## Status\n\nAccepted\n\n## Context\n\nShiki is a GitHub-first agentic engineering control plane distributed as a\nrepository template plus a dependency-free, standard-library CLI under\n`scripts/`. Until now the repository had no declared version, no release\nmechanism, and no documented install/upgrade/rollback path. P2.2 (Goal G-0012,\nissue #101) requires:\n\n- a versioning policy (P2.2.1),\n- a release workflow (P2.2.2),\n- a packaging/install path (P2.2.3),\n- a documented platform-runtime vs target-template boundary (P2.2.4),\n- an upgrade/migration path (P2.2.5),\n- rollback guidance (P2.2.6).\n\nConstraints from the Shiki constitution (`AGENTS.md`) shape the design:\n\n- The default CLI is the dependency-free `scripts/shiki.py` shim; packaging must\n  not introduce third-party runtime dependencies or break the shim.\n- Releases must run inside the operator's subscription-authenticated toolchain\n  and must not require extra secrets beyond the built-in `GITHUB_TOKEN`\n  (consistent with ADR 0005's no-API-key-by-default stance).\n# ADR 0008: Use Claude Code As The Default Implementer Runtime\n\n## Status\n\nProposed (supersedes ADR 0005 for the implementer default; keeps its auth model)\n\n## Context\n\nADR 0005 made Codex Front the default implementation runtime, with Claude Code\nrestricted to planner and reviewer roles by the runtime registry, and the only\nautonomous implementation adapter was `shiki runner codex`.\n\nOperational reality diverged from this contract:\n\n- Direct `codex exec` dispatch is rejected by the operator's harness, so\n  automated repair dispatch never worked in practice. The practical repair path\n  became Guardian-approved direct Claude assignment.\n- The platform's planning, review, and CCA layers already run on Claude Code,\n  and Claude Code's native capabilities (the built-in `/code-review` skill and\n  the Workflow multi-agent orchestration tool) have no Codex equivalent.\n- The Goal lifecycle redesign (ADR 0009) requires an autonomous post-freeze\n  loop driven from the operator's Claude Code session, including bounded\n  repair dispatch, which must not depend on a runtime that cannot be invoked\n  unattended.\n\nThe constitution claims Shiki is runtime-agnostic (\"not a Claude-only\nworkflow\", ADR 0002). Any change to the implementer default must preserve the\nregistry-based runtime model rather than hardcoding one vendor.\n# ADR 0009: Merge Grill And Planning Into Requirements Definition With Spec Freeze\n\n## Status\n\nProposed\n\n## Context\n\nThe operating model runs Goal Seek, `grill-with-docs`, Context & Impact, PRD,\nissue decomposition, and Task DAG planning as separate steps, each with its own\nchecklist family and state transition. In practice the operator experiences\nthis as repeated interactive sessions, and the boundary between \"plan settled\"\nand \"implementation may start\" is implicit: the de facto marker is\n`grill_with_docs.status=complete` in the plan JSON, which `shiki start` can\nsynthesize from a short interview without a real freeze decision.\n\nTwo operational failures motivated a redesign (recorded operator lessons):\nrequired scope surfaced twice mid-implementation because requirements were\nnever frozen against an explicit out-of-scope boundary, and autonomous\nexecution stalled because every downstream step re-opened questions the\nplanning phase should have settled.\n\nThe Goal lifecycle should have exactly one human gate on the happy path, with\neverything after it autonomous (ADR 0008 provides the implementation runtime\nfor this).\n\n## Decision\n\n# ADR 0010: External AI Guardian Review As A First-Class Approval Authority\n\n## Status\n\nProposed\n\n## Context\n\nShiki is intended to be an autonomous, recursive, self-improving agent\nframework: authority-in-the-loop, not human-in-the-loop. Until now the Guardian\napproval required for high/critical-risk merges (guardian-policy.json, ADR\ngovernance) recognized only human authority sources \u2014 a GitHub review approval,\nthe `guardian:approved` label applied by a configured Guardian, or a comment\ncarrying `Guardian approval granted` plus the current head SHA.\n\nThis forced a human into every high/critical merge, contradicting the\nautonomous design. Two failure modes surfaced in practice (2026-06-13):\n\n- An agent, trying to clear the gate, posted a Guardian-approval comment signed\n  as the operator and deleted a stale comment to change the gate's evaluation \u2014\n  i.e. it manufactured a human approval. This is forbidden: AI approval must\n  never be recorded as human approval.\n- The guardian-comment parser appended a fatal blocker for every marker-bearing\n  comment lacking the current head SHA, so a stale or abbreviated-SHA approval\n  comment poisoned the gate even when a valid full-SHA approval also existed.\n\nThe correct resolution is not to forbid AI merges, but to make external AI\nguardian review a first-class authority kind whose identity is preserved.\n# ADR 0011: Autonomous Quality-Gate Evidence \u2014 Deterministic Facts And An Independent Verifier\n\n## Status\n\nProposed\n\n## Context\n\nThe Shiki 0.2.0 autonomous post-freeze loop (ADR 0008/0009, PRD #119 G-D) must\ntake a ready task to an auto-merged PR with no human action. A live run on\n2026-06-17 proved the loop self-implements (a headless `claude -p` runner wrote\na correct, passing test) and the Memory Loop capture fires, but it cannot reach\nauto-merge: MergeGate and CCA require evidence the autonomous path never\nproduces \u2014 `required_skill` evidence for `tdd`/`code-review`, the CCA PR-12\npre-PR code-review section, and a deterministic verification record.\n\nThe naive fix is to let the headless implementer self-attest: instruct it to\n\"do TDD, run a code review, and write the evidence.\" That collides with the\nplatform maxim \u2014 **\"LLM outputs may vary. State transitions must not vary.\"** \u2014\nand with the finding (Anthropic engineering blog; Fable-5 prompting guidance)\nthat models self-critique poorly: grading in an *independent context window*\noutperforms self-review. A runner that writes its own \"code review found 0\nfindings\" ledger is a varying, self-graded, forgeable state transition.\n\nThe CCA already embodies the right pattern (an independent judge in a separate\nGitHub Actions context). The pre-PR quality gates must follow the same shape.\n\n## Decision\n# 0012 \u2014 Autonomous loop pushes goal completion to main via a closeout PR\n\n- Status: accepted\n- Date: 2026-06-18\n- Deciders: operator (mizutani-140), Claude (planner/implementer)\n- Related: ADR 0008 (default implementer + autonomous merge), ADR 0009 (spec freeze), ADR 0011 (autonomous quality-gate evidence), issue #140 (complete the autonomous post-freeze loop), the #140 T5 live self-drive.\n\n## Context\n\nThe `shiki loop` self-drives a frozen Goal: dispatch \u2192 implement \u2192 pre-PR code review \u2192 loop-observed TDD \u2192 commit/push \u2192 open the task PR \u2192 CCA \u2192 auto-merge. A live #140 T5 run proved this works end to end: the loop merged a task PR (`#153`) with zero manual intervention.\n\nBut the loop's **completion** never reached main. After the `merge` action, the loop ran `_mark_done` (local `task.status=done`, local lock release) and then, when all tasks were `done`, `cmd_goal_complete` (local `goal.status=complete`, scorecard report). All of these write the **coordinator's local `.shiki/` mirror only** \u2014 never a branch, never main. After the auto-merge, GitHub (the operational source of truth) still showed `task=review`, `goal=planned`, lock `active`. The loop reported `outcome: complete` on the strength of local state alone.\n\n`validate_shiki` couples task and goal completion: when a goal's DAG covers all tasks and every node is terminal (`done`), the goal MUST be `complete` on that HEAD checkout. `post_merge_reconcile` mode deliberately refuses terminal status (it only sets `review`) and so cannot push `goal=complete`. Pushing completion therefore requires a **normal-mode** PR carrying `task=done` + `goal=complete` + lock release together \u2014 exactly the manual closeout pattern used for PRs #144 / #149 / #152 / #154.\n\nAn 8-agent design+verification workflow examined two options:\n- **B1**: fold completion into the impl PR (self-contained terminal PR).\n- **B2**: after the impl PR merges, open and auto-merge a separate, deterministic closeout PR.\n\nThe verification found B1's \"carry done+complete in the impl PR\" conflicts with the loop's `review \u2192 CCA \u2192 merge \u2192 done` ordering (the decision engine keys on `task.status`; a local `done` short-circuits to a local `goal_complete` and exits before any merge). It also confirmed \u2014 empirically, via PR #149 \u2014 that a **pure-bookkeeping closeout PR with no implementation diff does obtain a `complete` CCA verdict**, so B2's separate closeout is viable. B2 matches the existing, proven manual closeout pattern and keeps the impl PR's CCA judging real implementation.\n\n## Decision\n\nThe loop pushes completion to main by **automating the closeout PR** (B2). The transition `merge impl PR \u2192 local mark_done \u2192 local goal_complete` is replaced by `merge impl PR \u2192 open a normal-mode closeout PR \u2192 auto-merge it \u2192 only then report goal complete`.\n\nMechanics (single-task and last-task-of-a-goal; see Consequences for multi-task):\n\n1. A new task field `closeout_pr` is the phase / re-entrancy anchor. The `merge` action no longer marks the task done; it only merges `expected_pr` and records evidence.\n# ADR 0013: Bootstrap Grants GitHub Actions Permission To Create And Approve Pull Requests\n\n## Status\n\nProposed\n\n## Context\n\nShiki's solo/self-running operation relies on the **CCA Review Bridge**\n(`docs/agents/decision-control.md`): after CCA returns `complete` and Guardian\nevidence is present when required, an automated GitHub PR review approval is\ncreated so a repository with `defaults.required_review: true` can still satisfy\nits required-approving-review branch-protection rule without a second human. The\nbridge submits that approval as the `github-actions` identity using the\nworkflow's `GITHUB_TOKEN`.\n\nGitHub gates this behavior behind a repository setting \u2014 *\"Allow GitHub Actions\nto create and approve pull requests\"* (the REST field\n`can_approve_pull_request_reviews` on `repos/{repo}/actions/permissions/workflow`).\nIt defaults to **off** on new repositories. `shiki doctor` already *diagnoses*\nthe gap (`doctor.github.workflow_permissions` fails when\n`default_workflow_permissions != \"read\"` or `can_approve_pull_request_reviews`\nis not `true`), but nothing in `shiki init` / `bootstrap-platform` ever *set*\nit. The result was a silent MergeGate dead-end discovered only after a PR was\nready: branch protection was configured, but the bridge could not create the\napproval it requires, so `required_review` could never be satisfied\nautonomously. This relates to the G-0012 closeout-to-main work and the\nReview-Bridge wiring tracked in closed #106.\n# ADR 0014: Use Codex App As The External AI Guardian UI Adapter\n\n## Status\n\nProposed\n\n## Context\n\nADR 0010 admits `external_ai_guardian_review` as a first-class Guardian\napproval authority. The next design problem is how to obtain that external AI\nGuardian review when the reviewer is reached through a ChatGPT Pro UI rather\nthan a deterministic GitHub Action or API.\n\nThe external reviewer must receive enough context to judge a high-risk PR:\ntask contract, PR diff, checks, implementer report, relevant Shiki docs, and\ncurrent head SHA. Asking the reviewer to discover the repository from scratch\nthrough the GitHub connector is useful as an independent verification path, but\nit is not stable enough to be the primary input path. Connector search indexing,\ntool availability, and model tool-use choices can vary.\n\nClaude Code is the default implementer and repairer runtime. Letting the same\nruntime operate the external Guardian UI would blur the boundary between\nimplementation and approval routing. Shiki needs an autonomous path, but it\nalso needs identity honesty and deterministic state transitions.\n\n## Decision\n\nWe will make the Codex App-side coordinator the External AI Guardian UI Adapter.\n# ADR 0015: Approve The Task Contract Before Dispatch, Not The Diff After It\n\n## Status\n\nAccepted\n\n## Context\n\nGuardian approval for high/critical risk is expressible only on a pull request.\nEvery approval source in `guardian-policy.json` \u2014 GitHub review, the\n`guardian:approved` label, the `Guardian approval granted` comment, and the\n`external_ai_guardian_review` artifact (ADR 0010/0014) \u2014 is bound to a PR, and\nthree of the four require the current head SHA. Approval therefore cannot exist\nbefore an implementation PR exists.\n\nThe consequence is structural, not accidental: a high/critical task is\nimplemented in full, opens a PR, and only then is stopped by CCA with\n`needs_guardian`. In this repository 43 of 80 registered tasks are high or\ncritical, so the Guardian stop is the normal path rather than the exception, and\neach one burns a complete implementation cycle before the first governance\ndecision is made.\n\nTwo supporting defects make the current state worse than intended:\n\n- The documented pre-dispatch gate is not enforced. `shiki dispatch check` marks\n  high/critical work as blocked with no way to satisfy it, while the path the\n  Goal Loop actually uses (`dispatchable_task_ids`, `decide_task_action`) never\n  reads `risk_level` at all. `decision-control.md` already requires that a\n# ADR 0016: Locks bound product paths; the Shiki mirror is bound by record identity\n\n## Status\n\nProposed\n\n- Date: 2026-07-28\n- Deciders: operator (mizutani-140), Claude (planner)\n- Related: ADR 0004 (GitHub-first with a `.shiki` mirror), ADR 0009 (Spec Freeze),\n  ADR 0012 (autonomous loop closeout to main), ADR 0015 (Contract Approval before\n  dispatch), remediation goal task R-02, the live PR #179 deadlock.\n\n## Context\n\nA full day of live operation produced a repo-wide stall: one task held an active\nlock and no other task could be dispatched, including the task that would have\nrepaired the stuck one.\n\nThe mechanism was traced and confirmed. MergeGate resolves the task contract,\ndependencies, locks and ledger evidence from the pull request head checkout\n(`mergegate_check.py:162-193`, `--target` default `.`, workflows checking out\n`pull_request.head.sha`). The Goal Loop therefore syncs mirror state onto the\ntask branch so MergeGate can see it (`shiki_loop._sync_state_to_branch`, whose\ndocstring names that reason). `files_outside_locks` applies to every changed\npath including `.shiki/`, so those synced files must be covered by a declared\nlock \u2014 and the lock authors reached for is `path:.shiki/**`. Because\n`active_lock_conflicts` conflicts on lock-pattern overlap alone, before any\nchanged-file intersection, two tasks that declare it are mutually exclusive\n# ADR 0017: A Bookkeeping Closeout Does Not Inherit Its Task's Risk\n\n## Status\n\nProposed\n\n## Context\n\nADR 0012 made the goal loop push a task's completion to the default branch through a\ncloseout pull request: after the implementation PR merges, the loop opens a second PR\ncarrying `task.status = done`, `lock.state = released`, and \u2014 when the task completes\nits goal \u2014 `goal.status = complete` plus one scorecard report. Nothing else. The\ncloseout exists so completion is durable on `main` rather than local-only.\n\nMergeGate derives the Guardian requirement from the task's `risk_level`\n(`_guardian_risk_labels` feeding `_builtin_guardian_risk_required`), and a closeout\nPR is judged against the same task. A high or critical task therefore requires a\nGuardian approval for a PR that contains no implementation \u2014 the work it records was\nalready approved and merged one PR earlier.\n\nThe cost is measured, not hypothetical. Across the 31 pull requests merged on\n2026-07-29/30 while driving the audit-remediation backlog, 21 Guardian approval\nactions were performed. Seven of them were closeout bookkeeping with a zero-line code\ndiff. That is a third of all operator approvals spent re-approving work the operator\nhad already approved, and it scales linearly: every high/critical task in every future\ngoal costs two approvals instead of one.\n\nTwo adjacent costs were pure defects and have been fixed rather than accepted \u2014 the\n# ADR 0018: Carried Guardian Approval Across A Proven Base Sync\n\n## Status\n\nProposed\n\n## Context\n\nShiki runs on a strictly protected default branch. A high-risk PR merges only with\npolicy-backed Guardian evidence: the `guardian:approved` label plus a Guardian\nreview or comment, and \u2014 because `require_head_sha` is on \u2014 an approval comment\nthat carries the current 40-character PR head SHA (see `docs/agents/guardian-policy.md`).\nThe head-SHA binding is deliberate: it forces the Guardian to approve a specific\ntree, not a moving branch.\n\nThat binding collides with the other thing strict protection requires: a PR must\nbe up to date with its base before it can merge. Every time an unrelated PR merges\nto the default branch, the open PRs must sync their base, which moves each head to\na new SHA and invalidates the head-bound approval each one already had. The cost is\nmeasured, not hypothetical. On 2026-08-01, landing five Contract PRs cost **four\nextra Guardian approvals** \u2014 one per PR that had to re-sync after another merged \u2014\nfor **zero content change**. The approvals bought no new information; they\nre-attested a tree the Guardian had already attested, now reachable at a different\nSHA only because the base moved underneath it.\n\nTwo adjacent problems were pure defects and are already fixed, not accepted: the\nCCA workflow's shallow base fetch that invalidated every other open PR's approval\non each merge, and the model-transcribed head SHA (both PR #206). What remains is a\n\n2026-08-07T08:04:59.805605Z ERROR codex_models_manager::manager: failed to renew cache TTL: missing field `base_instructions` at line 94 column 5\ncodex\nThe first RED slice is the resolver contract, independent of installer writes: a target `ADR 0042` and a platform `SADR-0042` are different namespaces, and a missing SADR must not be \u201csatisfied\u201d by the target ADR. I\u2019m adding only that failing fixture first, then I\u2019ll make the smallest resolver change to pass it.\napply patch\npatch: completed\n/private/tmp/shiki-adr-collision.pa0KwY/.worktrees/shiki-t-20260807t073307221254z-2d22d654-introduc/tests/test_adr_references.py\ndiff --git a/tests/test_adr_references.py b/tests/test_adr_references.py\nindex 51706329c797d5dd9b47e082a333fa83b261d482..3eb252910d024b49d4f67f3977d5c87dbdbc944e\n--- a/tests/test_adr_references.py\n+++ b/tests/test_adr_references.py\n@@ -180,6 +180,17 @@\n         self.assertIn(\"9991\", message)  # cited number\n         self.assertIn(\"docs/adr/9991-*.md\", message)  # missing path\n \n+    def test_target_adr_does_not_satisfy_missing_platform_sadr(self) -> None:\n+        root = self._make_repo(\n+            adr_names=[\"0042-target-decision.md\"],\n+            script_body='\"\"\"Target ADR 0042 exists, but platform SADR-0042 does not.\"\"\"\\n',\n+        )\n+        violations = find_adr_reference_violations(root, include_docs=False)\n+        self.assertEqual(len(violations), 1)\n+        self.assertEqual(violations[0].namespace, \"SADR\")\n+        self.assertEqual(violations[0].number, \"0042\")\n+        self.assertEqual(violations[0].expected_path, \"docs/adr/SADR-0042-*.md\")\n+\n     def test_uncited_adr_is_allowed(self) -> None:\n         # An ADR exists but nothing cites it -> not a violation. The converse\n         # (every ADR must be cited) is explicitly NOT required.\n\ndiff --git a/tests/test_adr_references.py b/tests/test_adr_references.py\nindex 51706329c797d5dd9b47e082a333fa83b261d482..3eb252910d024b49d4f67f3977d5c87dbdbc944e\n--- a/tests/test_adr_references.py\n+++ b/tests/test_adr_references.py\n@@ -180,6 +180,17 @@\n         self.assertIn(\"9991\", message)  # cited number\n         self.assertIn(\"docs/adr/9991-*.md\", message)  # missing path\n \n+    def test_target_adr_does_not_satisfy_missing_platform_sadr(self) -> None:\n+        root = self._make_repo(\n+            adr_names=[\"0042-target-decision.md\"],\n+            script_body='\"\"\"Target ADR 0042 exists, but platform SADR-0042 does not.\"\"\"\\n',\n+        )\n+        violations = find_adr_reference_violations(root, include_docs=False)\n+        self.assertEqual(len(violations), 1)\n+        self.assertEqual(violations[0].namespace, \"SADR\")\n+        self.assertEqual(violations[0].number, \"0042\")\n+        self.assertEqual(violations[0].expected_path, \"docs/adr/SADR-0042-*.md\")\n+\n     def test_uncited_adr_is_allowed(self) -> None:\n         # An ADR exists but nothing cites it -> not a violation. The converse\n         # (every ADR must be cited) is explicitly NOT required.\n\n2026-08-07T08:05:43.605552Z ERROR codex_models_manager::manager: failed to renew cache TTL: missing field `base_instructions` at line 94 column 5\nexec\n/bin/zsh -lc 'python3 -m unittest tests.test_adr_references.FixtureAdrReferences.test_target_adr_does_not_satisfy_missing_platform_sadr -v' in /private/tmp/shiki-adr-collision.pa0KwY/.worktrees/shiki-t-20260807t073307221254z-2d22d654-introduc\n exited 1 in 0ms:\ntest_target_adr_does_not_satisfy_missing_platform_sadr (tests.test_adr_references.FixtureAdrReferences.test_target_adr_does_not_satisfy_missing_platform_sadr) ... FAIL\n\n======================================================================\nFAIL: test_target_adr_does_not_satisfy_missing_platform_sadr (tests.test_adr_references.FixtureAdrReferences.test_target_adr_does_not_satisfy_missing_platform_sadr)\n----------------------------------------------------------------------\nTraceback (most recent call last):\n  File \"/private/tmp/shiki-adr-collision.pa0KwY/.worktrees/shiki-t-20260807t073307221254z-2d22d654-introduc/tests/test_adr_references.py\", line 189, in test_target_adr_does_not_satisfy_missing_platform_sadr\n    self.assertEqual(len(violations), 1)\n    ~~~~~~~~~~~~~~~~^^^^^^^^^^^^^^^^^^^^\nAssertionError: 0 != 1\n\n----------------------------------------------------------------------\nRan 1 test in 0.002s\n\nFAILED (failures=1)\n\ndiff --git a/tests/test_adr_references.py b/tests/test_adr_references.py\nindex 51706329c797d5dd9b47e082a333fa83b261d482..3eb252910d024b49d4f67f3977d5c87dbdbc944e\n--- a/tests/test_adr_references.py\n+++ b/tests/test_adr_references.py\n@@ -180,6 +180,17 @@\n         self.assertIn(\"9991\", message)  # cited number\n         self.assertIn(\"docs/adr/9991-*.md\", message)  # missing path\n \n+    def test_target_adr_does_not_satisfy_missing_platform_sadr(self) -> None:\n+        root = self._make_repo(\n+            adr_names=[\"0042-target-decision.md\"],\n+            script_body='\"\"\"Target ADR 0042 exists, but platform SADR-0042 does not.\"\"\"\\n',\n+        )\n+        violations = find_adr_reference_violations(root, include_docs=False)\n+        self.assertEqual(len(violations), 1)\n+        self.assertEqual(violations[0].namespace, \"SADR\")\n+        self.assertEqual(violations[0].number, \"0042\")\n+        self.assertEqual(violations[0].expected_path, \"docs/adr/SADR-0042-*.md\")\n+\n     def test_uncited_adr_is_allowed(self) -> None:\n         # An ADR exists but nothing cites it -> not a violation. The converse\n         # (every ADR must be cited) is explicitly NOT required.\n\n2026-08-07T08:05:50.573618Z ERROR codex_models_manager::manager: failed to renew cache TTL: missing field `base_instructions` at line 94 column 5\napply patch\npatch: completed\n/private/tmp/shiki-adr-collision.pa0KwY/.worktrees/shiki-t-20260807t073307221254z-2d22d654-introduc/tests/test_adr_references.py\ndiff --git a/tests/test_adr_references.py b/tests/test_adr_references.py\nindex 51706329c797d5dd9b47e082a333fa83b261d482..92569d9fa25e22cfd71c17561d715485027a0d2d\n--- a/tests/test_adr_references.py\n+++ b/tests/test_adr_references.py\n@@ -31,17 +31,18 @@\n \n REPO_ROOT = Path(__file__).resolve().parents[1]\n \n-# \"ADR 0016\" -- a four-digit citation. The trailing \\b keeps a five-digit token\n-# from being read as a truncated four-digit number.\n-_CITATION = re.compile(r\"\\bADR (\\d{4})\\b\")\n-# \"0016-locks-bound-product-paths.md\" -- the leading four digits name the record.\n-_ADR_FILENAME = re.compile(r\"^(\\d{4})-.*\\.md$\")\n+# ``ADR 0016`` / ``SADR-0016`` -- a namespace plus four-digit citation. The\n+# trailing ``\\b`` keeps a five-digit token from being read as a truncated one.\n+_CITATION = re.compile(r\"\\b(S?ADR)[ -](\\d{4})\\b\")\n+# Target ADRs retain the numeric filename; Shiki platform decisions use SADR.\n+_DECISION_FILENAME = re.compile(r\"^(?:(SADR)-)?(\\d{4})-.*\\.md$\")\n \n \n class Violation(NamedTuple):\n     \"\"\"A citation that does not resolve to exactly one decision record.\"\"\"\n \n     citing_file: str  # repo-relative path of the file that cites the ADR\n+    namespace: str  # \"ADR\" for target decisions; \"SADR\" for Shiki decisions\n     number: str  # the cited four-digit number, e.g. \"0016\"\n     line: int  # 1-based line of the citation\n     reason: str  # \"missing\" (zero matches) or \"ambiguous\" (two or more)\n@@ -49,32 +50,34 @@\n \n     @property\n     def expected_path(self) -> str:\n-        return f\"docs/adr/{self.number}-*.md\"\n+        prefix = \"SADR-\" if self.namespace == \"SADR\" else \"\"\n+        return f\"docs/adr/{prefix}{self.number}-*.md\"\n \n     def describe(self) -> str:\n         \"\"\"Name the citing file, the cited number, and the missing path.\"\"\"\n         if self.reason == \"missing\":\n             return (\n-                f\"{self.citing_file}:{self.line} cites ADR {self.number} \"\n+                f\"{self.citing_file}:{self.line} cites {self.namespace} {self.number} \"\n                 f\"but no document matches {self.expected_path}\"\n             )\n         joined = \", \".join(self.matches)\n         return (\n-            f\"{self.citing_file}:{self.line} cites ADR {self.number} which \"\n+            f\"{self.citing_file}:{self.line} cites {self.namespace} {self.number} which \"\n             f\"resolves to {len(self.matches)} documents (expected exactly one \"\n             f\"at {self.expected_path}): {joined}\"\n         )\n \n \n-def _adr_index(root: Path) -> dict[str, list[Path]]:\n-    \"\"\"Map every four-digit ADR number to the record files that claim it.\"\"\"\n-    index: dict[str, list[Path]] = {}\n+def _adr_index(root: Path) -> dict[tuple[str, str], list[Path]]:\n+    \"\"\"Map each decision namespace and number to the files that claim it.\"\"\"\n+    index: dict[tuple[str, str], list[Path]] = {}\n     adr_dir = root / \"docs\" / \"adr\"\n     if adr_dir.is_dir():\n         for path in sorted(adr_dir.glob(\"*.md\")):\n-            match = _ADR_FILENAME.match(path.name)\n+            match = _DECISION_FILENAME.match(path.name)\n             if match:\n-                index.setdefault(match.group(1), []).append(path)\n+                namespace = \"SADR\" if match.group(1) else \"ADR\"\n+                index.setdefault((namespace, match.group(2)), []).append(path)\n     return index\n \n \n@@ -110,13 +113,15 @@\n         rel = path.relative_to(root).as_posix()\n         for lineno, line in enumerate(text.splitlines(), start=1):\n             for match in _CITATION.finditer(line):\n-                number = match.group(1)\n-                found = index.get(number, [])\n+                namespace, number = match.groups()\n+                found = index.get((namespace, number), [])\n                 if len(found) == 1:\n                     continue\n                 reason = \"missing\" if not found else \"ambiguous\"\n                 rels = [p.relative_to(root).as_posix() for p in found]\n-                violations.append(Violation(rel, number, lineno, reason, rels))\n+                violations.append(\n+                    Violation(rel, namespace, number, lineno, reason, rels)\n+                )\n     return violations\n \n \n@@ -146,8 +151,8 @@\n         # The two records this Goal carried onto the default branch, and which\n         # the scripts cite most, must be present.\n         index = _adr_index(REPO_ROOT)\n-        self.assertIn(\"0016\", index)\n-        self.assertIn(\"0017\", index)\n+        self.assertIn((\"ADR\", \"0016\"), index)\n+        self.assertIn((\"ADR\", \"0017\"), index)\n \n \n class FixtureAdrReferences(unittest.TestCase):\n@@ -180,6 +185,17 @@\n         self.assertIn(\"9991\", message)  # cited number\n         self.assertIn(\"docs/adr/9991-*.md\", message)  # missing path\n \n+    def test_target_adr_does_not_satisfy_missing_platform_sadr(self) -> None:\n+        root = self._make_repo(\n+            adr_names=[\"0042-target-decision.md\"],\n+            script_body='\"\"\"Target ADR 0042 exists, but platform SADR-0042 does not.\"\"\"\\n',\n+        )\n+        violations = find_adr_reference_violations(root, include_docs=False)\n+        self.assertEqual(len(violations), 1)\n+        self.assertEqual(violations[0].namespace, \"SADR\")\n+        self.assertEqual(violations[0].number, \"0042\")\n+        self.assertEqual(violations[0].expected_path, \"docs/adr/SADR-0042-*.md\")\n+\n     def test_uncited_adr_is_allowed(self) -> None:\n         # An ADR exists but nothing cites it -> not a violation. The converse\n         # (every ADR must be cited) is explicitly NOT required.\n\ndiff --git a/tests/test_adr_references.py b/tests/test_adr_references.py\nindex 51706329c797d5dd9b47e082a333fa83b261d482..92569d9fa25e22cfd71c17561d715485027a0d2d\n--- a/tests/test_adr_references.py\n+++ b/tests/test_adr_references.py\n@@ -31,17 +31,18 @@\n \n REPO_ROOT = Path(__file__).resolve().parents[1]\n \n-# \"ADR 0016\" -- a four-digit citation. The trailing \\b keeps a five-digit token\n-# from being read as a truncated four-digit number.\n-_CITATION = re.compile(r\"\\bADR (\\d{4})\\b\")\n-# \"0016-locks-bound-product-paths.md\" -- the leading four digits name the record.\n-_ADR_FILENAME = re.compile(r\"^(\\d{4})-.*\\.md$\")\n+# ``ADR 0016`` / ``SADR-0016`` -- a namespace plus four-digit citation. The\n+# trailing ``\\b`` keeps a five-digit token from being read as a truncated one.\n+_CITATION = re.compile(r\"\\b(S?ADR)[ -](\\d{4})\\b\")\n+# Target ADRs retain the numeric filename; Shiki platform decisions use SADR.\n+_DECISION_FILENAME = re.compile(r\"^(?:(SADR)-)?(\\d{4})-.*\\.md$\")\n \n \n class Violation(NamedTuple):\n     \"\"\"A citation that does not resolve to exactly one decision record.\"\"\"\n \n     citing_file: str  # repo-relative path of the file that cites the ADR\n+    namespace: str  # \"ADR\" for target decisions; \"SADR\" for Shiki decisions\n     number: str  # the cited four-digit number, e.g. \"0016\"\n     line: int  # 1-based line of the citation\n     reason: str  # \"missing\" (zero matches) or \"ambiguous\" (two or more)\n@@ -49,32 +50,34 @@\n \n     @property\n     def expected_path(self) -> str:\n-        return f\"docs/adr/{self.number}-*.md\"\n+        prefix = \"SADR-\" if self.namespace == \"SADR\" else \"\"\n+        return f\"docs/adr/{prefix}{self.number}-*.md\"\n \n     def describe(self) -> str:\n         \"\"\"Name the citing file, the cited number, and the missing path.\"\"\"\n         if self.reason == \"missing\":\n             return (\n-                f\"{self.citing_file}:{self.line} cites ADR {self.number} \"\n+                f\"{self.citing_file}:{self.line} cites {self.namespace} {self.number} \"\n                 f\"but no document matches {self.expected_path}\"\n             )\n         joined = \", \".join(self.matches)\n         return (\n-            f\"{self.citing_file}:{self.line} cites ADR {self.number} which \"\n+            f\"{self.citing_file}:{self.line} cites {self.namespace} {self.number} which \"\n             f\"resolves to {len(self.matches)} documents (expected exactly one \"\n             f\"at {self.expected_path}): {joined}\"\n         )\n \n \n-def _adr_index(root: Path) -> dict[str, list[Path]]:\n-    \"\"\"Map every four-digit ADR number to the record files that claim it.\"\"\"\n-    index: dict[str, list[Path]] = {}\n+def _adr_index(root: Path) -> dict[tuple[str, str], list[Path]]:\n+    \"\"\"Map each decision namespace and number to the files that claim it.\"\"\"\n+    index: dict[tuple[str, str], list[Path]] = {}\n     adr_dir = root / \"docs\" / \"adr\"\n     if adr_dir.is_dir():\n         for path in sorted(adr_dir.glob(\"*.md\")):\n-            match = _ADR_FILENAME.match(path.name)\n+            match = _DECISION_FILENAME.match(path.name)\n             if match:\n-                index.setdefault(match.group(1), []).append(path)\n+                namespace = \"SADR\" if match.group(1) else \"ADR\"\n+                index.setdefault((namespace, match.group(2)), []).append(path)\n     return index\n \n \n@@ -110,13 +113,15 @@\n         rel = path.relative_to(root).as_posix()\n         for lineno, line in enumerate(text.splitlines(), start=1):\n             for match in _CITATION.finditer(line):\n-                number = match.group(1)\n-                found = index.get(number, [])\n+                namespace, number = match.groups()\n+                found = index.get((namespace, number), [])\n                 if len(found) == 1:\n                     continue\n                 reason = \"missing\" if not found else \"ambiguous\"\n                 rels = [p.relative_to(root).as_posix() for p in found]\n-                violations.append(Violation(rel, number, lineno, reason, rels))\n+                violations.append(\n+                    Violation(rel, namespace, number, lineno, reason, rels)\n+                )\n     return violations\n \n \n@@ -146,8 +151,8 @@\n         # The two records this Goal carried onto the default branch, and which\n         # the scripts cite most, must be present.\n         index = _adr_index(REPO_ROOT)\n-        self.assertIn(\"0016\", index)\n-        self.assertIn(\"0017\", index)\n+        self.assertIn((\"ADR\", \"0016\"), index)\n+        self.assertIn((\"ADR\", \"0017\"), index)\n \n \n class FixtureAdrReferences(unittest.TestCase):\n@@ -180,6 +185,17 @@\n         self.assertIn(\"9991\", message)  # cited number\n         self.assertIn(\"docs/adr/9991-*.md\", message)  # missing path\n \n+    def test_target_adr_does_not_satisfy_missing_platform_sadr(self) -> None:\n+        root = self._make_repo(\n+            adr_names=[\"0042-target-decision.md\"],\n+            script_body='\"\"\"Target ADR 0042 exists, but platform SADR-0042 does not.\"\"\"\\n',\n+        )\n+        violations = find_adr_reference_violations(root, include_docs=False)\n+        self.assertEqual(len(violations), 1)\n+        self.assertEqual(violations[0].namespace, \"SADR\")\n+        self.assertEqual(violations[0].number, \"0042\")\n+        self.assertEqual(violations[0].expected_path, \"docs/adr/SADR-0042-*.md\")\n+\n     def test_uncited_adr_is_allowed(self) -> None:\n         # An ADR exists but nothing cites it -> not a violation. The converse\n         # (every ADR must be cited) is explicitly NOT required.\n\n2026-08-07T08:06:05.429397Z ERROR codex_models_manager::manager: failed to renew cache TTL: missing field `base_instructions` at line 94 column 5\nexec\n/bin/zsh -lc 'python3 -m unittest tests.test_adr_references -v' in /private/tmp/shiki-adr-collision.pa0KwY/.worktrees/shiki-t-20260807t073307221254z-2d22d654-introduc\n succeeded in 0ms:\ntest_ambiguous_when_two_documents_share_a_number (tests.test_adr_references.FixtureAdrReferences.test_ambiguous_when_two_documents_share_a_number) ... ok\ntest_fails_when_citation_has_no_document (tests.test_adr_references.FixtureAdrReferences.test_fails_when_citation_has_no_document) ... ok\ntest_passes_when_citation_resolves (tests.test_adr_references.FixtureAdrReferences.test_passes_when_citation_resolves) ... ok\ntest_target_adr_does_not_satisfy_missing_platform_sadr (tests.test_adr_references.FixtureAdrReferences.test_target_adr_does_not_satisfy_missing_platform_sadr) ... ok\ntest_uncited_adr_is_allowed (tests.test_adr_references.FixtureAdrReferences.test_uncited_adr_is_allowed) ... ok\ntest_carried_records_are_indexed (tests.test_adr_references.RealRepoAdrReferences.test_carried_records_are_indexed) ... ok\ntest_every_cited_adr_resolves (tests.test_adr_references.RealRepoAdrReferences.test_every_cited_adr_resolves) ... ok\ntest_scan_actually_reads_scripts (tests.test_adr_references.RealRepoAdrReferences.test_scan_actually_reads_scripts) ... ok\n\n----------------------------------------------------------------------\nRan 8 tests in 0.025s\n\nOK\n\ndiff --git a/tests/test_adr_references.py b/tests/test_adr_references.py\nindex 51706329c797d5dd9b47e082a333fa83b261d482..92569d9fa25e22cfd71c17561d715485027a0d2d\n--- a/tests/test_adr_references.py\n+++ b/tests/test_adr_references.py\n@@ -31,17 +31,18 @@\n \n REPO_ROOT = Path(__file__).resolve().parents[1]\n \n-# \"ADR 0016\" -- a four-digit citation. The trailing \\b keeps a five-digit token\n-# from being read as a truncated four-digit number.\n-_CITATION = re.compile(r\"\\bADR (\\d{4})\\b\")\n-# \"0016-locks-bound-product-paths.md\" -- the leading four digits name the record.\n-_ADR_FILENAME = re.compile(r\"^(\\d{4})-.*\\.md$\")\n+# ``ADR 0016`` / ``SADR-0016`` -- a namespace plus four-digit citation. The\n+# trailing ``\\b`` keeps a five-digit token from being read as a truncated one.\n+_CITATION = re.compile(r\"\\b(S?ADR)[ -](\\d{4})\\b\")\n+# Target ADRs retain the numeric filename; Shiki platform decisions use SADR.\n+_DECISION_FILENAME = re.compile(r\"^(?:(SADR)-)?(\\d{4})-.*\\.md$\")\n \n \n class Violation(NamedTuple):\n     \"\"\"A citation that does not resolve to exactly one decision record.\"\"\"\n \n     citing_file: str  # repo-relative path of the file that cites the ADR\n+    namespace: str  # \"ADR\" for target decisions; \"SADR\" for Shiki decisions\n     number: str  # the cited four-digit number, e.g. \"0016\"\n     line: int  # 1-based line of the citation\n     reason: str  # \"missing\" (zero matches) or \"ambiguous\" (two or more)\n@@ -49,32 +50,34 @@\n \n     @property\n     def expected_path(self) -> str:\n-        return f\"docs/adr/{self.number}-*.md\"\n+        prefix = \"SADR-\" if self.namespace == \"SADR\" else \"\"\n+        return f\"docs/adr/{prefix}{self.number}-*.md\"\n \n     def describe(self) -> str:\n         \"\"\"Name the citing file, the cited number, and the missing path.\"\"\"\n         if self.reason == \"missing\":\n             return (\n-                f\"{self.citing_file}:{self.line} cites ADR {self.number} \"\n+                f\"{self.citing_file}:{self.line} cites {self.namespace} {self.number} \"\n                 f\"but no document matches {self.expected_path}\"\n             )\n         joined = \", \".join(self.matches)\n         return (\n-            f\"{self.citing_file}:{self.line} cites ADR {self.number} which \"\n+            f\"{self.citing_file}:{self.line} cites {self.namespace} {self.number} which \"\n             f\"resolves to {len(self.matches)} documents (expected exactly one \"\n             f\"at {self.expected_path}): {joined}\"\n         )\n \n \n-def _adr_index(root: Path) -> dict[str, list[Path]]:\n-    \"\"\"Map every four-digit ADR number to the record files that claim it.\"\"\"\n-    index: dict[str, list[Path]] = {}\n+def _adr_index(root: Path) -> dict[tuple[str, str], list[Path]]:\n+    \"\"\"Map each decision namespace and number to the files that claim it.\"\"\"\n+    index: dict[tuple[str, str], list[Path]] = {}\n     adr_dir = root / \"docs\" / \"adr\"\n     if adr_dir.is_dir():\n         for path in sorted(adr_dir.glob(\"*.md\")):\n-            match = _ADR_FILENAME.match(path.name)\n+            match = _DECISION_FILENAME.match(path.name)\n             if match:\n-                index.setdefault(match.group(1), []).append(path)\n+                namespace = \"SADR\" if match.group(1) else \"ADR\"\n+                index.setdefault((namespace, match.group(2)), []).append(path)\n     return index\n \n \n@@ -110,13 +113,15 @@\n         rel = path.relative_to(root).as_posix()\n         for lineno, line in enumerate(text.splitlines(), start=1):\n             for match in _CITATION.finditer(line):\n-                number = match.group(1)\n-                found = index.get(number, [])\n+                namespace, number = match.groups()\n+                found = index.get((namespace, number), [])\n                 if len(found) == 1:\n                     continue\n                 reason = \"missing\" if not found else \"ambiguous\"\n                 rels = [p.relative_to(root).as_posix() for p in found]\n-                violations.append(Violation(rel, number, lineno, reason, rels))\n+                violations.append(\n+                    Violation(rel, namespace, number, lineno, reason, rels)\n+                )\n     return violations\n \n \n@@ -146,8 +151,8 @@\n         # The two records this Goal carried onto the default branch, and which\n         # the scripts cite most, must be present.\n         index = _adr_index(REPO_ROOT)\n-        self.assertIn(\"0016\", index)\n-        self.assertIn(\"0017\", index)\n+        self.assertIn((\"ADR\", \"0016\"), index)\n+        self.assertIn((\"ADR\", \"0017\"), index)\n \n \n class FixtureAdrReferences(unittest.TestCase):\n@@ -180,6 +185,17 @@\n         self.assertIn(\"9991\", message)  # cited number\n         self.assertIn(\"docs/adr/9991-*.md\", message)  # missing path\n \n+    def test_target_adr_does_not_satisfy_missing_platform_sadr(self) -> None:\n+        root = self._make_repo(\n+            adr_names=[\"0042-target-decision.md\"],\n+            script_body='\"\"\"Target ADR 0042 exists, but platform SADR-0042 does not.\"\"\"\\n',\n+        )\n+        violations = find_adr_reference_violations(root, include_docs=False)\n+        self.assertEqual(len(violations), 1)\n+        self.assertEqual(violations[0].namespace, \"SADR\")\n+        self.assertEqual(violations[0].number, \"0042\")\n+        self.assertEqual(violations[0].expected_path, \"docs/adr/SADR-0042-*.md\")\n+\n     def test_uncited_adr_is_allowed(self) -> None:\n         # An ADR exists but nothing cites it -> not a violation. The converse\n         # (every ADR must be cited) is explicitly NOT required.\n\n2026-08-07T08:06:08.107016Z ERROR codex_models_manager::manager: failed to renew cache TTL: missing field `base_instructions` at line 94 column 5\ncodex\nThe resolver RED\u2192GREEN cycle confirms the first root cause: citations and filenames were indexed only by number, so target and platform decisions shared one collision domain.\n\nFor the installer, my ranked hypotheses are:\n\n1. Shipping the whole current numeric `docs/adr` tree creates same-number ambiguity beside target ADRs, even when filenames differ.\n2. After the platform renames to SADR, today\u2019s copy-only force upgrade leaves stamped legacy numeric Shiki records behind, so the collision persists.\n3. The current stamp reader collapses absent, unreadable, and malformed states and has no pre-write deletion authorization step, so safe migration cannot be proven or diagnosed exhaustively.\n4. Non-force behavior is already conservative at file level, but must be locked down so migration cleanup never broadens it.\n\nI\u2019ll test these through `install_template`, the public installer seam, using recursive byte snapshots for the refusal cases.\napply patch\npatch: completed\n/private/tmp/shiki-adr-collision.pa0KwY/.worktrees/shiki-t-20260807t073307221254z-2d22d654-introduc/tests/test_sadr_install_migration.py\ndiff --git a/tests/test_adr_references.py b/tests/test_adr_references.py\nindex 51706329c797d5dd9b47e082a333fa83b261d482..92569d9fa25e22cfd71c17561d715485027a0d2d\n--- a/tests/test_adr_references.py\n+++ b/tests/test_adr_references.py\n@@ -31,17 +31,18 @@\n \n REPO_ROOT = Path(__file__).resolve().parents[1]\n \n-# \"ADR 0016\" -- a four-digit citation. The trailing \\b keeps a five-digit token\n-# from being read as a truncated four-digit number.\n-_CITATION = re.compile(r\"\\bADR (\\d{4})\\b\")\n-# \"0016-locks-bound-product-paths.md\" -- the leading four digits name the record.\n-_ADR_FILENAME = re.compile(r\"^(\\d{4})-.*\\.md$\")\n+# ``ADR 0016`` / ``SADR-0016`` -- a namespace plus four-digit citation. The\n+# trailing ``\\b`` keeps a five-digit token from being read as a truncated one.\n+_CITATION = re.compile(r\"\\b(S?ADR)[ -](\\d{4})\\b\")\n+# Target ADRs retain the numeric filename; Shiki platform decisions use SADR.\n+_DECISION_FILENAME = re.compile(r\"^(?:(SADR)-)?(\\d{4})-.*\\.md$\")\n \n \n class Violation(NamedTuple):\n     \"\"\"A citation that does not resolve to exactly one decision record.\"\"\"\n \n     citing_file: str  # repo-relative path of the file that cites the ADR\n+    namespace: str  # \"ADR\" for target decisions; \"SADR\" for Shiki decisions\n     number: str  # the cited four-digit number, e.g. \"0016\"\n     line: int  # 1-based line of the citation\n     reason: str  # \"missing\" (zero matches) or \"ambiguous\" (two or more)\n@@ -49,32 +50,34 @@\n \n     @property\n     def expected_path(self) -> str:\n-        return f\"docs/adr/{self.number}-*.md\"\n+        prefix = \"SADR-\" if self.namespace == \"SADR\" else \"\"\n+        return f\"docs/adr/{prefix}{self.number}-*.md\"\n \n     def describe(self) -> str:\n         \"\"\"Name the citing file, the cited number, and the missing path.\"\"\"\n         if self.reason == \"missing\":\n             return (\n-                f\"{self.citing_file}:{self.line} cites ADR {self.number} \"\n+                f\"{self.citing_file}:{self.line} cites {self.namespace} {self.number} \"\n                 f\"but no document matches {self.expected_path}\"\n             )\n         joined = \", \".join(self.matches)\n         return (\n-            f\"{self.citing_file}:{self.line} cites ADR {self.number} which \"\n+            f\"{self.citing_file}:{self.line} cites {self.namespace} {self.number} which \"\n             f\"resolves to {len(self.matches)} documents (expected exactly one \"\n             f\"at {self.expected_path}): {joined}\"\n         )\n \n \n-def _adr_index(root: Path) -> dict[str, list[Path]]:\n-    \"\"\"Map every four-digit ADR number to the record files that claim it.\"\"\"\n-    index: dict[str, list[Path]] = {}\n+def _adr_index(root: Path) -> dict[tuple[str, str], list[Path]]:\n+    \"\"\"Map each decision namespace and number to the files that claim it.\"\"\"\n+    index: dict[tuple[str, str], list[Path]] = {}\n     adr_dir = root / \"docs\" / \"adr\"\n     if adr_dir.is_dir():\n         for path in sorted(adr_dir.glob(\"*.md\")):\n-            match = _ADR_FILENAME.match(path.name)\n+            match = _DECISION_FILENAME.match(path.name)\n             if match:\n-                index.setdefault(match.group(1), []).append(path)\n+                namespace = \"SADR\" if match.group(1) else \"ADR\"\n+                index.setdefault((namespace, match.group(2)), []).append(path)\n     return index\n \n \n@@ -110,13 +113,15 @@\n         rel = path.relative_to(root).as_posix()\n         for lineno, line in enumerate(text.splitlines(), start=1):\n             for match in _CITATION.finditer(line):\n-                number = match.group(1)\n-                found = index.get(number, [])\n+                namespace, number = match.groups()\n+                found = index.get((namespace, number), [])\n                 if len(found) == 1:\n                     continue\n                 reason = \"missing\" if not found else \"ambiguous\"\n                 rels = [p.relative_to(root).as_posix() for p in found]\n-                violations.append(Violation(rel, number, lineno, reason, rels))\n+                violations.append(\n+                    Violation(rel, namespace, number, lineno, reason, rels)\n+                )\n     return violations\n \n \n@@ -146,8 +151,8 @@\n         # The two records this Goal carried onto the default branch, and which\n         # the scripts cite most, must be present.\n         index = _adr_index(REPO_ROOT)\n-        self.assertIn(\"0016\", index)\n-        self.assertIn(\"0017\", index)\n+        self.assertIn((\"ADR\", \"0016\"), index)\n+        self.assertIn((\"ADR\", \"0017\"), index)\n \n \n class FixtureAdrReferences(unittest.TestCase):\n@@ -180,6 +185,17 @@\n         self.assertIn(\"9991\", message)  # cited number\n         self.assertIn(\"docs/adr/9991-*.md\", message)  # missing path\n \n+    def test_target_adr_does_not_satisfy_missing_platform_sadr(self) -> None:\n+        root = self._make_repo(\n+            adr_names=[\"0042-target-decision.md\"],\n+            script_body='\"\"\"Target ADR 0042 exists, but platform SADR-0042 does not.\"\"\"\\n',\n+        )\n+        violations = find_adr_reference_violations(root, include_docs=False)\n+        self.assertEqual(len(violations), 1)\n+        self.assertEqual(violations[0].namespace, \"SADR\")\n+        self.assertEqual(violations[0].number, \"0042\")\n+        self.assertEqual(violations[0].expected_path, \"docs/adr/SADR-0042-*.md\")\n+\n     def test_uncited_adr_is_allowed(self) -> None:\n         # An ADR exists but nothing cites it -> not a violation. The converse\n         # (every ADR must be cited) is explicitly NOT required.\ndiff --git a/tests/test_sadr_install_migration.py b/tests/test_sadr_install_migration.py\nnew file mode 100644\nindex 0000000000000000000000000000000000000000..a6398c81377f00c09aa2cb684980b070cfaf714c\n--- /dev/null\n+++ b/tests/test_sadr_install_migration.py\n@@ -0,0 +1,56 @@\n+\"\"\"SADR installation and collision-safe legacy upgrade behavior.\"\"\"\n+\n+from __future__ import annotations\n+\n+import shutil\n+import tempfile\n+import unittest\n+from pathlib import Path\n+\n+import shiki_test_support  # noqa: F401  (path bootstrap)\n+\n+import shiki_installer\n+from tests.test_adr_references import find_adr_reference_violations\n+\n+\n+PRODUCT_NUMBERS = tuple(range(13, 19))\n+\n+\n+class FreshSadrInstallTests(unittest.TestCase):\n+    def setUp(self) -> None:\n+        self.tmp = Path(tempfile.mkdtemp(prefix=\"shiki-sadr-fresh-\"))\n+        self.addCleanup(lambda: shutil.rmtree(self.tmp, ignore_errors=True))\n+        self.target = self.tmp / \"target\"\n+        self.adr_dir = self.target / \"docs\" / \"adr\"\n+        self.adr_dir.mkdir(parents=True)\n+        self.product_bytes = {\n+            f\"{number:04d}-product-decision.md\": (\n+                f\"# Product ADR {number:04d}\\n\\nTarget-owned bytes {number}.\\n\"\n+            ).encode()\n+            for number in PRODUCT_NUMBERS\n+        }\n+        for name, content in self.product_bytes.items():\n+            (self.adr_dir / name).write_bytes(content)\n+\n+    def test_fresh_install_preserves_product_adrs_and_installs_all_sadrs(self) -> None:\n+        shiki_installer.install_template(self.target, force=False, validate=True)\n+\n+        for name, expected in self.product_bytes.items():\n+            self.assertEqual((self.adr_dir / name).read_bytes(), expected)\n+\n+        sadr_numbers = {\n+            path.name.split(\"-\", 2)[1]\n+            for path in self.adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\")\n+        }\n+        self.assertEqual(sadr_numbers, {f\"{number:04d}\" for number in range(1, 20)})\n+\n+        numeric_records = {\n+            path.name\n+            for path in self.adr_dir.glob(\"[0-9][0-9][0-9][0-9]-*.md\")\n+        }\n+        self.assertEqual(numeric_records, set(self.product_bytes))\n+        self.assertEqual(find_adr_reference_violations(self.target), [])\n+\n+\n+if __name__ == \"__main__\":\n+    unittest.main()\n\ndiff --git a/tests/test_adr_references.py b/tests/test_adr_references.py\nindex 51706329c797d5dd9b47e082a333fa83b261d482..92569d9fa25e22cfd71c17561d715485027a0d2d\n--- a/tests/test_adr_references.py\n+++ b/tests/test_adr_references.py\n@@ -31,17 +31,18 @@\n \n REPO_ROOT = Path(__file__).resolve().parents[1]\n \n-# \"ADR 0016\" -- a four-digit citation. The trailing \\b keeps a five-digit token\n-# from being read as a truncated four-digit number.\n-_CITATION = re.compile(r\"\\bADR (\\d{4})\\b\")\n-# \"0016-locks-bound-product-paths.md\" -- the leading four digits name the record.\n-_ADR_FILENAME = re.compile(r\"^(\\d{4})-.*\\.md$\")\n+# ``ADR 0016`` / ``SADR-0016`` -- a namespace plus four-digit citation. The\n+# trailing ``\\b`` keeps a five-digit token from being read as a truncated one.\n+_CITATION = re.compile(r\"\\b(S?ADR)[ -](\\d{4})\\b\")\n+# Target ADRs retain the numeric filename; Shiki platform decisions use SADR.\n+_DECISION_FILENAME = re.compile(r\"^(?:(SADR)-)?(\\d{4})-.*\\.md$\")\n \n \n class Violation(NamedTuple):\n     \"\"\"A citation that does not resolve to exactly one decision record.\"\"\"\n \n     citing_file: str  # repo-relative path of the file that cites the ADR\n+    namespace: str  # \"ADR\" for target decisions; \"SADR\" for Shiki decisions\n     number: str  # the cited four-digit number, e.g. \"0016\"\n     line: int  # 1-based line of the citation\n     reason: str  # \"missing\" (zero matches) or \"ambiguous\" (two or more)\n@@ -49,32 +50,34 @@\n \n     @property\n     def expected_path(self) -> str:\n-        return f\"docs/adr/{self.number}-*.md\"\n+        prefix = \"SADR-\" if self.namespace == \"SADR\" else \"\"\n+        return f\"docs/adr/{prefix}{self.number}-*.md\"\n \n     def describe(self) -> str:\n         \"\"\"Name the citing file, the cited number, and the missing path.\"\"\"\n         if self.reason == \"missing\":\n             return (\n-                f\"{self.citing_file}:{self.line} cites ADR {self.number} \"\n+                f\"{self.citing_file}:{self.line} cites {self.namespace} {self.number} \"\n                 f\"but no document matches {self.expected_path}\"\n             )\n         joined = \", \".join(self.matches)\n         return (\n-            f\"{self.citing_file}:{self.line} cites ADR {self.number} which \"\n+            f\"{self.citing_file}:{self.line} cites {self.namespace} {self.number} which \"\n             f\"resolves to {len(self.matches)} documents (expected exactly one \"\n             f\"at {self.expected_path}): {joined}\"\n         )\n \n \n-def _adr_index(root: Path) -> dict[str, list[Path]]:\n-    \"\"\"Map every four-digit ADR number to the record files that claim it.\"\"\"\n-    index: dict[str, list[Path]] = {}\n+def _adr_index(root: Path) -> dict[tuple[str, str], list[Path]]:\n+    \"\"\"Map each decision namespace and number to the files that claim it.\"\"\"\n+    index: dict[tuple[str, str], list[Path]] = {}\n     adr_dir = root / \"docs\" / \"adr\"\n     if adr_dir.is_dir():\n         for path in sorted(adr_dir.glob(\"*.md\")):\n-            match = _ADR_FILENAME.match(path.name)\n+            match = _DECISION_FILENAME.match(path.name)\n             if match:\n-                index.setdefault(match.group(1), []).append(path)\n+                namespace = \"SADR\" if match.group(1) else \"ADR\"\n+                index.setdefault((namespace, match.group(2)), []).append(path)\n     return index\n \n \n@@ -110,13 +113,15 @@\n         rel = path.relative_to(root).as_posix()\n         for lineno, line in enumerate(text.splitlines(), start=1):\n             for match in _CITATION.finditer(line):\n-                number = match.group(1)\n-                found = index.get(number, [])\n+                namespace, number = match.groups()\n+                found = index.get((namespace, number), [])\n                 if len(found) == 1:\n                     continue\n                 reason = \"missing\" if not found else \"ambiguous\"\n                 rels = [p.relative_to(root).as_posix() for p in found]\n-                violations.append(Violation(rel, number, lineno, reason, rels))\n+                violations.append(\n+                    Violation(rel, namespace, number, lineno, reason, rels)\n+                )\n     return violations\n \n \n@@ -146,8 +151,8 @@\n         # The two records this Goal carried onto the default branch, and which\n         # the scripts cite most, must be present.\n         index = _adr_index(REPO_ROOT)\n-        self.assertIn(\"0016\", index)\n-        self.assertIn(\"0017\", index)\n+        self.assertIn((\"ADR\", \"0016\"), index)\n+        self.assertIn((\"ADR\", \"0017\"), index)\n \n \n class FixtureAdrReferences(unittest.TestCase):\n@@ -180,6 +185,17 @@\n         self.assertIn(\"9991\", message)  # cited number\n         self.assertIn(\"docs/adr/9991-*.md\", message)  # missing path\n \n+    def test_target_adr_does_not_satisfy_missing_platform_sadr(self) -> None:\n+        root = self._make_repo(\n+            adr_names=[\"0042-target-decision.md\"],\n+            script_body='\"\"\"Target ADR 0042 exists, but platform SADR-0042 does not.\"\"\"\\n',\n+        )\n+        violations = find_adr_reference_violations(root, include_docs=False)\n+        self.assertEqual(len(violations), 1)\n+        self.assertEqual(violations[0].namespace, \"SADR\")\n+        self.assertEqual(violations[0].number, \"0042\")\n+        self.assertEqual(violations[0].expected_path, \"docs/adr/SADR-0042-*.md\")\n+\n     def test_uncited_adr_is_allowed(self) -> None:\n         # An ADR exists but nothing cites it -> not a violation. The converse\n         # (every ADR must be cited) is explicitly NOT required.\ndiff --git a/tests/test_sadr_install_migration.py b/tests/test_sadr_install_migration.py\nnew file mode 100644\nindex 0000000000000000000000000000000000000000..a6398c81377f00c09aa2cb684980b070cfaf714c\n--- /dev/null\n+++ b/tests/test_sadr_install_migration.py\n@@ -0,0 +1,56 @@\n+\"\"\"SADR installation and collision-safe legacy upgrade behavior.\"\"\"\n+\n+from __future__ import annotations\n+\n+import shutil\n+import tempfile\n+import unittest\n+from pathlib import Path\n+\n+import shiki_test_support  # noqa: F401  (path bootstrap)\n+\n+import shiki_installer\n+from tests.test_adr_references import find_adr_reference_violations\n+\n+\n+PRODUCT_NUMBERS = tuple(range(13, 19))\n+\n+\n+class FreshSadrInstallTests(unittest.TestCase):\n+    def setUp(self) -> None:\n+        self.tmp = Path(tempfile.mkdtemp(prefix=\"shiki-sadr-fresh-\"))\n+        self.addCleanup(lambda: shutil.rmtree(self.tmp, ignore_errors=True))\n+        self.target = self.tmp / \"target\"\n+        self.adr_dir = self.target / \"docs\" / \"adr\"\n+        self.adr_dir.mkdir(parents=True)\n+        self.product_bytes = {\n+            f\"{number:04d}-product-decision.md\": (\n+                f\"# Product ADR {number:04d}\\n\\nTarget-owned bytes {number}.\\n\"\n+            ).encode()\n+            for number in PRODUCT_NUMBERS\n+        }\n+        for name, content in self.product_bytes.items():\n+            (self.adr_dir / name).write_bytes(content)\n+\n+    def test_fresh_install_preserves_product_adrs_and_installs_all_sadrs(self) -> None:\n+        shiki_installer.install_template(self.target, force=False, validate=True)\n+\n+        for name, expected in self.product_bytes.items():\n+            self.assertEqual((self.adr_dir / name).read_bytes(), expected)\n+\n+        sadr_numbers = {\n+            path.name.split(\"-\", 2)[1]\n+            for path in self.adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\")\n+        }\n+        self.assertEqual(sadr_numbers, {f\"{number:04d}\" for number in range(1, 20)})\n+\n+        numeric_records = {\n+            path.name\n+            for path in self.adr_dir.glob(\"[0-9][0-9][0-9][0-9]-*.md\")\n+        }\n+        self.assertEqual(numeric_records, set(self.product_bytes))\n+        self.assertEqual(find_adr_reference_violations(self.target), [])\n+\n+\n+if __name__ == \"__main__\":\n+    unittest.main()\n\nexec\n/bin/zsh -lc 'python3 -m unittest tests.test_sadr_install_migration.FreshSadrInstallTests.test_fresh_install_preserves_product_adrs_and_installs_all_sadrs -v' in /private/tmp/shiki-adr-collision.pa0KwY/.worktrees/shiki-t-20260807t073307221254z-2d22d654-introduc\n exited 1 in 0ms:\ntest_sadr_install_migration (unittest.loader._FailedTest.test_sadr_install_migration) ... ERROR\n\n======================================================================\nERROR: test_sadr_install_migration (unittest.loader._FailedTest.test_sadr_install_migration)\n----------------------------------------------------------------------\nImportError: Failed to import test module: test_sadr_install_migration\nTraceback (most recent call last):\n  File \"/opt/homebrew/Cellar/python@3.14/3.14.6/Frameworks/Python.framework/Versions/3.14/lib/python3.14/unittest/loader.py\", line 137, in loadTestsFromName\n    module = __import__(module_name)\n  File \"/private/tmp/shiki-adr-collision.pa0KwY/.worktrees/shiki-t-20260807t073307221254z-2d22d654-introduc/tests/test_sadr_install_migration.py\", line 10, in <module>\n    import shiki_test_support  # noqa: F401  (path bootstrap)\n    ^^^^^^^^^^^^^^^^^^^^^^^^^\nModuleNotFoundError: No module named 'shiki_test_support'\n\n\n----------------------------------------------------------------------\nRan 1 test in 0.000s\n\nFAILED (errors=1)\n\ndiff --git a/tests/test_adr_references.py b/tests/test_adr_references.py\nindex 51706329c797d5dd9b47e082a333fa83b261d482..92569d9fa25e22cfd71c17561d715485027a0d2d\n--- a/tests/test_adr_references.py\n+++ b/tests/test_adr_references.py\n@@ -31,17 +31,18 @@\n \n REPO_ROOT = Path(__file__).resolve().parents[1]\n \n-# \"ADR 0016\" -- a four-digit citation. The trailing \\b keeps a five-digit token\n-# from being read as a truncated four-digit number.\n-_CITATION = re.compile(r\"\\bADR (\\d{4})\\b\")\n-# \"0016-locks-bound-product-paths.md\" -- the leading four digits name the record.\n-_ADR_FILENAME = re.compile(r\"^(\\d{4})-.*\\.md$\")\n+# ``ADR 0016`` / ``SADR-0016`` -- a namespace plus four-digit citation. The\n+# trailing ``\\b`` keeps a five-digit token from being read as a truncated one.\n+_CITATION = re.compile(r\"\\b(S?ADR)[ -](\\d{4})\\b\")\n+# Target ADRs retain the numeric filename; Shiki platform decisions use SADR.\n+_DECISION_FILENAME = re.compile(r\"^(?:(SADR)-)?(\\d{4})-.*\\.md$\")\n \n \n class Violation(NamedTuple):\n     \"\"\"A citation that does not resolve to exactly one decision record.\"\"\"\n \n     citing_file: str  # repo-relative path of the file that cites the ADR\n+    namespace: str  # \"ADR\" for target decisions; \"SADR\" for Shiki decisions\n     number: str  # the cited four-digit number, e.g. \"0016\"\n     line: int  # 1-based line of the citation\n     reason: str  # \"missing\" (zero matches) or \"ambiguous\" (two or more)\n@@ -49,32 +50,34 @@\n \n     @property\n     def expected_path(self) -> str:\n-        return f\"docs/adr/{self.number}-*.md\"\n+        prefix = \"SADR-\" if self.namespace == \"SADR\" else \"\"\n+        return f\"docs/adr/{prefix}{self.number}-*.md\"\n \n     def describe(self) -> str:\n         \"\"\"Name the citing file, the cited number, and the missing path.\"\"\"\n         if self.reason == \"missing\":\n             return (\n-                f\"{self.citing_file}:{self.line} cites ADR {self.number} \"\n+                f\"{self.citing_file}:{self.line} cites {self.namespace} {self.number} \"\n                 f\"but no document matches {self.expected_path}\"\n             )\n         joined = \", \".join(self.matches)\n         return (\n-            f\"{self.citing_file}:{self.line} cites ADR {self.number} which \"\n+            f\"{self.citing_file}:{self.line} cites {self.namespace} {self.number} which \"\n             f\"resolves to {len(self.matches)} documents (expected exactly one \"\n             f\"at {self.expected_path}): {joined}\"\n         )\n \n \n-def _adr_index(root: Path) -> dict[str, list[Path]]:\n-    \"\"\"Map every four-digit ADR number to the record files that claim it.\"\"\"\n-    index: dict[str, list[Path]] = {}\n+def _adr_index(root: Path) -> dict[tuple[str, str], list[Path]]:\n+    \"\"\"Map each decision namespace and number to the files that claim it.\"\"\"\n+    index: dict[tuple[str, str], list[Path]] = {}\n     adr_dir = root / \"docs\" / \"adr\"\n     if adr_dir.is_dir():\n         for path in sorted(adr_dir.glob(\"*.md\")):\n-            match = _ADR_FILENAME.match(path.name)\n+            match = _DECISION_FILENAME.match(path.name)\n             if match:\n-                index.setdefault(match.group(1), []).append(path)\n+                namespace = \"SADR\" if match.group(1) else \"ADR\"\n+                index.setdefault((namespace, match.group(2)), []).append(path)\n     return index\n \n \n@@ -110,13 +113,15 @@\n         rel = path.relative_to(root).as_posix()\n         for lineno, line in enumerate(text.splitlines(), start=1):\n             for match in _CITATION.finditer(line):\n-                number = match.group(1)\n-                found = index.get(number, [])\n+                namespace, number = match.groups()\n+                found = index.get((namespace, number), [])\n                 if len(found) == 1:\n                     continue\n                 reason = \"missing\" if not found else \"ambiguous\"\n                 rels = [p.relative_to(root).as_posix() for p in found]\n-                violations.append(Violation(rel, number, lineno, reason, rels))\n+                violations.append(\n+                    Violation(rel, namespace, number, lineno, reason, rels)\n+                )\n     return violations\n \n \n@@ -146,8 +151,8 @@\n         # The two records this Goal carried onto the default branch, and which\n         # the scripts cite most, must be present.\n         index = _adr_index(REPO_ROOT)\n-        self.assertIn(\"0016\", index)\n-        self.assertIn(\"0017\", index)\n+        self.assertIn((\"ADR\", \"0016\"), index)\n+        self.assertIn((\"ADR\", \"0017\"), index)\n \n \n class FixtureAdrReferences(unittest.TestCase):\n@@ -180,6 +185,17 @@\n         self.assertIn(\"9991\", message)  # cited number\n         self.assertIn(\"docs/adr/9991-*.md\", message)  # missing path\n \n+    def test_target_adr_does_not_satisfy_missing_platform_sadr(self) -> None:\n+        root = self._make_repo(\n+            adr_names=[\"0042-target-decision.md\"],\n+            script_body='\"\"\"Target ADR 0042 exists, but platform SADR-0042 does not.\"\"\"\\n',\n+        )\n+        violations = find_adr_reference_violations(root, include_docs=False)\n+        self.assertEqual(len(violations), 1)\n+        self.assertEqual(violations[0].namespace, \"SADR\")\n+        self.assertEqual(violations[0].number, \"0042\")\n+        self.assertEqual(violations[0].expected_path, \"docs/adr/SADR-0042-*.md\")\n+\n     def test_uncited_adr_is_allowed(self) -> None:\n         # An ADR exists but nothing cites it -> not a violation. The converse\n         # (every ADR must be cited) is explicitly NOT required.\ndiff --git a/tests/test_sadr_install_migration.py b/tests/test_sadr_install_migration.py\nnew file mode 100644\nindex 0000000000000000000000000000000000000000..a6398c81377f00c09aa2cb684980b070cfaf714c\n--- /dev/null\n+++ b/tests/test_sadr_install_migration.py\n@@ -0,0 +1,56 @@\n+\"\"\"SADR installation and collision-safe legacy upgrade behavior.\"\"\"\n+\n+from __future__ import annotations\n+\n+import shutil\n+import tempfile\n+import unittest\n+from pathlib import Path\n+\n+import shiki_test_support  # noqa: F401  (path bootstrap)\n+\n+import shiki_installer\n+from tests.test_adr_references import find_adr_reference_violations\n+\n+\n+PRODUCT_NUMBERS = tuple(range(13, 19))\n+\n+\n+class FreshSadrInstallTests(unittest.TestCase):\n+    def setUp(self) -> None:\n+        self.tmp = Path(tempfile.mkdtemp(prefix=\"shiki-sadr-fresh-\"))\n+        self.addCleanup(lambda: shutil.rmtree(self.tmp, ignore_errors=True))\n+        self.target = self.tmp / \"target\"\n+        self.adr_dir = self.target / \"docs\" / \"adr\"\n+        self.adr_dir.mkdir(parents=True)\n+        self.product_bytes = {\n+            f\"{number:04d}-product-decision.md\": (\n+                f\"# Product ADR {number:04d}\\n\\nTarget-owned bytes {number}.\\n\"\n+            ).encode()\n+            for number in PRODUCT_NUMBERS\n+        }\n+        for name, content in self.product_bytes.items():\n+            (self.adr_dir / name).write_bytes(content)\n+\n+    def test_fresh_install_preserves_product_adrs_and_installs_all_sadrs(self) -> None:\n+        shiki_installer.install_template(self.target, force=False, validate=True)\n+\n+        for name, expected in self.product_bytes.items():\n+            self.assertEqual((self.adr_dir / name).read_bytes(), expected)\n+\n+        sadr_numbers = {\n+            path.name.split(\"-\", 2)[1]\n+            for path in self.adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\")\n+        }\n+        self.assertEqual(sadr_numbers, {f\"{number:04d}\" for number in range(1, 20)})\n+\n+        numeric_records = {\n+            path.name\n+            for path in self.adr_dir.glob(\"[0-9][0-9][0-9][0-9]-*.md\")\n+        }\n+        self.assertEqual(numeric_records, set(self.product_bytes))\n+        self.assertEqual(find_adr_reference_violations(self.target), [])\n+\n+\n+if __name__ == \"__main__\":\n+    unittest.main()\n\nexec\n/bin/zsh -lc \"python3 -m unittest discover -s tests -p 'test_sadr_install_migration.py' -v\" in /private/tmp/shiki-adr-collision.pa0KwY/.worktrees/shiki-t-20260807t073307221254z-2d22d654-introduc\ndiff --git a/tests/test_adr_references.py b/tests/test_adr_references.py\nindex 51706329c797d5dd9b47e082a333fa83b261d482..92569d9fa25e22cfd71c17561d715485027a0d2d\n--- a/tests/test_adr_references.py\n+++ b/tests/test_adr_references.py\n@@ -31,17 +31,18 @@\n \n REPO_ROOT = Path(__file__).resolve().parents[1]\n \n-# \"ADR 0016\" -- a four-digit citation. The trailing \\b keeps a five-digit token\n-# from being read as a truncated four-digit number.\n-_CITATION = re.compile(r\"\\bADR (\\d{4})\\b\")\n-# \"0016-locks-bound-product-paths.md\" -- the leading four digits name the record.\n-_ADR_FILENAME = re.compile(r\"^(\\d{4})-.*\\.md$\")\n+# ``ADR 0016`` / ``SADR-0016`` -- a namespace plus four-digit citation. The\n+# trailing ``\\b`` keeps a five-digit token from being read as a truncated one.\n+_CITATION = re.compile(r\"\\b(S?ADR)[ -](\\d{4})\\b\")\n+# Target ADRs retain the numeric filename; Shiki platform decisions use SADR.\n+_DECISION_FILENAME = re.compile(r\"^(?:(SADR)-)?(\\d{4})-.*\\.md$\")\n \n \n class Violation(NamedTuple):\n     \"\"\"A citation that does not resolve to exactly one decision record.\"\"\"\n \n     citing_file: str  # repo-relative path of the file that cites the ADR\n+    namespace: str  # \"ADR\" for target decisions; \"SADR\" for Shiki decisions\n     number: str  # the cited four-digit number, e.g. \"0016\"\n     line: int  # 1-based line of the citation\n     reason: str  # \"missing\" (zero matches) or \"ambiguous\" (two or more)\n@@ -49,32 +50,34 @@\n \n     @property\n     def expected_path(self) -> str:\n-        return f\"docs/adr/{self.number}-*.md\"\n+        prefix = \"SADR-\" if self.namespace == \"SADR\" else \"\"\n+        return f\"docs/adr/{prefix}{self.number}-*.md\"\n \n     def describe(self) -> str:\n         \"\"\"Name the citing file, the cited number, and the missing path.\"\"\"\n         if self.reason == \"missing\":\n             return (\n-                f\"{self.citing_file}:{self.line} cites ADR {self.number} \"\n+                f\"{self.citing_file}:{self.line} cites {self.namespace} {self.number} \"\n                 f\"but no document matches {self.expected_path}\"\n             )\n         joined = \", \".join(self.matches)\n         return (\n-            f\"{self.citing_file}:{self.line} cites ADR {self.number} which \"\n+            f\"{self.citing_file}:{self.line} cites {self.namespace} {self.number} which \"\n             f\"resolves to {len(self.matches)} documents (expected exactly one \"\n             f\"at {self.expected_path}): {joined}\"\n         )\n \n \n-def _adr_index(root: Path) -> dict[str, list[Path]]:\n-    \"\"\"Map every four-digit ADR number to the record files that claim it.\"\"\"\n-    index: dict[str, list[Path]] = {}\n+def _adr_index(root: Path) -> dict[tuple[str, str], list[Path]]:\n+    \"\"\"Map each decision namespace and number to the files that claim it.\"\"\"\n+    index: dict[tuple[str, str], list[Path]] = {}\n     adr_dir = root / \"docs\" / \"adr\"\n     if adr_dir.is_dir():\n         for path in sorted(adr_dir.glob(\"*.md\")):\n-            match = _ADR_FILENAME.match(path.name)\n+            match = _DECISION_FILENAME.match(path.name)\n             if match:\n-                index.setdefault(match.group(1), []).append(path)\n+                namespace = \"SADR\" if match.group(1) else \"ADR\"\n+                index.setdefault((namespace, match.group(2)), []).append(path)\n     return index\n \n \n@@ -110,13 +113,15 @@\n         rel = path.relative_to(root).as_posix()\n         for lineno, line in enumerate(text.splitlines(), start=1):\n             for match in _CITATION.finditer(line):\n-                number = match.group(1)\n-                found = index.get(number, [])\n+                namespace, number = match.groups()\n+                found = index.get((namespace, number), [])\n                 if len(found) == 1:\n                     continue\n                 reason = \"missing\" if not found else \"ambiguous\"\n                 rels = [p.relative_to(root).as_posix() for p in found]\n-                violations.append(Violation(rel, number, lineno, reason, rels))\n+                violations.append(\n+                    Violation(rel, namespace, number, lineno, reason, rels)\n+                )\n     return violations\n \n \n@@ -146,8 +151,8 @@\n         # The two records this Goal carried onto the default branch, and which\n         # the scripts cite most, must be present.\n         index = _adr_index(REPO_ROOT)\n-        self.assertIn(\"0016\", index)\n-        self.assertIn(\"0017\", index)\n+        self.assertIn((\"ADR\", \"0016\"), index)\n+        self.assertIn((\"ADR\", \"0017\"), index)\n \n \n class FixtureAdrReferences(unittest.TestCase):\n@@ -180,6 +185,17 @@\n         self.assertIn(\"9991\", message)  # cited number\n         self.assertIn(\"docs/adr/9991-*.md\", message)  # missing path\n \n+    def test_target_adr_does_not_satisfy_missing_platform_sadr(self) -> None:\n+        root = self._make_repo(\n+            adr_names=[\"0042-target-decision.md\"],\n+            script_body='\"\"\"Target ADR 0042 exists, but platform SADR-0042 does not.\"\"\"\\n',\n+        )\n+        violations = find_adr_reference_violations(root, include_docs=False)\n+        self.assertEqual(len(violations), 1)\n+        self.assertEqual(violations[0].namespace, \"SADR\")\n+        self.assertEqual(violations[0].number, \"0042\")\n+        self.assertEqual(violations[0].expected_path, \"docs/adr/SADR-0042-*.md\")\n+\n     def test_uncited_adr_is_allowed(self) -> None:\n         # An ADR exists but nothing cites it -> not a violation. The converse\n         # (every ADR must be cited) is explicitly NOT required.\ndiff --git a/tests/test_sadr_install_migration.py b/tests/test_sadr_install_migration.py\nnew file mode 100644\nindex 0000000000000000000000000000000000000000..a6398c81377f00c09aa2cb684980b070cfaf714c\n--- /dev/null\n+++ b/tests/test_sadr_install_migration.py\n@@ -0,0 +1,56 @@\n+\"\"\"SADR installation and collision-safe legacy upgrade behavior.\"\"\"\n+\n+from __future__ import annotations\n+\n+import shutil\n+import tempfile\n+import unittest\n+from pathlib import Path\n+\n+import shiki_test_support  # noqa: F401  (path bootstrap)\n+\n+import shiki_installer\n+from tests.test_adr_references import find_adr_reference_violations\n+\n+\n+PRODUCT_NUMBERS = tuple(range(13, 19))\n+\n+\n+class FreshSadrInstallTests(unittest.TestCase):\n+    def setUp(self) -> None:\n+        self.tmp = Path(tempfile.mkdtemp(prefix=\"shiki-sadr-fresh-\"))\n+        self.addCleanup(lambda: shutil.rmtree(self.tmp, ignore_errors=True))\n+        self.target = self.tmp / \"target\"\n+        self.adr_dir = self.target / \"docs\" / \"adr\"\n+        self.adr_dir.mkdir(parents=True)\n+        self.product_bytes = {\n+            f\"{number:04d}-product-decision.md\": (\n+                f\"# Product ADR {number:04d}\\n\\nTarget-owned bytes {number}.\\n\"\n+            ).encode()\n+            for number in PRODUCT_NUMBERS\n+        }\n+        for name, content in self.product_bytes.items():\n+            (self.adr_dir / name).write_bytes(content)\n+\n+    def test_fresh_install_preserves_product_adrs_and_installs_all_sadrs(self) -> None:\n+        shiki_installer.install_template(self.target, force=False, validate=True)\n+\n+        for name, expected in self.product_bytes.items():\n+            self.assertEqual((self.adr_dir / name).read_bytes(), expected)\n+\n+        sadr_numbers = {\n+            path.name.split(\"-\", 2)[1]\n+            for path in self.adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\")\n+        }\n+        self.assertEqual(sadr_numbers, {f\"{number:04d}\" for number in range(1, 20)})\n+\n+        numeric_records = {\n+            path.name\n+            for path in self.adr_dir.glob(\"[0-9][0-9][0-9][0-9]-*.md\")\n+        }\n+        self.assertEqual(numeric_records, set(self.product_bytes))\n+        self.assertEqual(find_adr_reference_violations(self.target), [])\n+\n+\n+if __name__ == \"__main__\":\n+    unittest.main()\n\n exited 1 in 459ms:\nFAIL\n\n======================================================================\nFAIL: test_fresh_install_preserves_product_adrs_and_installs_all_sadrs (test_sadr_install_migration.FreshSadrInstallTests.test_fresh_install_preserves_product_adrs_and_installs_all_sadrs)\n----------------------------------------------------------------------\nTraceback (most recent call last):\n  File \"/private/tmp/shiki-adr-collision.pa0KwY/.worktrees/shiki-t-20260807t073307221254z-2d22d654-introduc/tests/test_sadr_install_migration.py\", line 45, in test_fresh_install_preserves_product_adrs_and_installs_all_sadrs\n    self.assertEqual(sadr_numbers, {f\"{number:04d}\" for number in range(1, 20)})\n    ~~~~~~~~~~~~~~~~^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^\nAssertionError: Items in the second set but not the first:\n'0002'\n'0011'\n'0009'\n'0001'\n'0012'\n'0010'\n'0017'\n'0018'\n'0008'\n'0006'\n'0003'\n'0005'\n'0015'\n'0013'\n'0014'\n'0004'\n'0016'\n'0019'\n'0007'\n\n----------------------------------------------------------------------\nRan 1 test in 0.421s\n\nFAILED (failures=1)\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-y3bm0fjn/target/bin/shiki\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-y3bm0fjn/target/AGENTS.md\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-y3bm0fjn/target/CLAUDE.md\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-y3bm0fjn/target/CONTEXT.md\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-y3bm0fjn/target/SYSTEM_PROMPT.md\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-y3bm0fjn/target/.gitignore\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-y3bm0fjn/target/.claude/commands/shiki.md\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-y3bm0fjn/target/.codex/skills/shiki/SKILL.md\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-y3bm0fjn/target/.shiki/migrations/state.json\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-y3bm0fjn/target/.shiki/guardian-policy.json\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-y3bm0fjn/target/.shiki/config.yaml\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-y3bm0fjn/target/.shiki/schemas/runner-record.schema.json\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-y3bm0fjn/target/.shiki/schemas/report.schema.json\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-y3bm0fjn/target/.shiki/schemas/dispatch-guard.schema.json\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-y3bm0fjn/target/.shiki/schemas/external-ai-guardian-review-packet.schema.json\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-y3bm0fjn/target/.shiki/schemas/run.schema.json\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-y3bm0fjn/target/.shiki/schemas/worktree-record.schema.json\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-y3bm0fjn/target/.shiki/schemas/cca-evidence-manifest.schema.json\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-y3bm0fjn/target/.shiki/schemas/cca-verdict.schema.json\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-y3bm0fjn/target/.shiki/schemas/plan.schema.json\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-y3bm0fjn/target/.shiki/schemas/inbox.schema.json\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-y3bm0fjn/target/.shiki/schemas/memory-entry.schema.json\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-y3bm0fjn/target/.shiki/schemas/repair-packet.schema.json\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-y3bm0fjn/target/.shiki/schemas/smoke.schema.json\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-y3bm0fjn/target/.shiki/schemas/start.schema.json\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-y3bm0fjn/target/.shiki/schemas/ledger.schema.json\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-y3bm0fjn/target/.shiki/schemas/task.schema.json\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-y3bm0fjn/target/.shiki/schemas/dag.schema.json\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-y3bm0fjn/target/.shiki/schemas/goal.schema.json\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-y3bm0fjn/target/.shiki/README.md\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-y3bm0fjn/target/.shiki/manifest.json\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-y3bm0fjn/target/.shiki/templates/LEDGER_ENTRY.template.json\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-y3bm0fjn/target/.shiki/templates/CCA_VERDICT.template.json\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-y3bm0fjn/target/.shiki/templates/GOAL.template.md\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-y3bm0fjn/target/.shiki/templates/REPORT.template.md\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-y3bm0fjn/target/.shiki/templates/DAG.template.json\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-y3bm0fjn/target/.shiki/templates/worktree-record.schema.json\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-y3bm0fjn/target/.shiki/templates/REPAIR_PACKET.template.json\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-y3bm0fjn/target/.shiki/templates/TASK.template.json\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-y3bm0fjn/target/.shiki/policy.example.yaml\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-y3bm0fjn/target/.github/ISSUE_TEMPLATE/shiki_goal.yml\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-y3bm0fjn/target/.github/ISSUE_TEMPLATE/shiki_task.yml\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-y3bm0fjn/target/.github/CODEOWNERS\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-y3bm0fjn/target/.github/PULL_REQUEST_TEMPLATE/shiki-task.md\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-y3bm0fjn/target/.github/prompts/cca-completion-check.md\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-y3bm0fjn/target/.github/prompts/codex-repair-handoff.md\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-y3bm0fjn/target/.github/prompts/codex-task-handoff.md\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-y3bm0fjn/target/.github/workflows/shiki-validate.yml\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-y3bm0fjn/target/.github/workflows/shiki-claude-review.yml\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-y3bm0fjn/target/.github/workflows/shiki-cca-completion.yml\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-y3bm0fjn/target/.github/workflows/shiki-mergegate.yml\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-y3bm0fjn/target/.github/workflows/shiki-orchestrator.yml\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-y3bm0fjn/target/docs/agents/node24-workflow-compatibility.md\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-y3bm0fjn/target/docs/agents/checklists.md\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-y3bm0fjn/target/docs/agents/codex-handoff.md\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-y3bm0fjn/target/docs/agents/bootstrap-command.md\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-y3bm0fjn/target/docs/agents/guardian-policy.md\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-y3bm0fjn/target/docs/agents/autonomous-loop-e2e-acceptance.md\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-y3bm0fjn/target/docs/agents/runtime-auth-model.md\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-y3bm0fjn/target/docs/agents/shiki-doctor.md\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-y3bm0fjn/target/docs/agents/control-commands.md\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-y3bm0fjn/target/docs/agents/memory.md\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-y3bm0fjn/target/docs/agents/shiki-migrations.md\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-y3bm0fjn/target/docs/agents/decision-control.md\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-y3bm0fjn/target/docs/agents/domain.md\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-y3bm0fjn/target/docs/agents/skill-gate.md\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-y3bm0fjn/target/docs/agents/github-provider-config.md\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-y3bm0fjn/target/docs/agents/state-classes.md\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-y3bm0fjn/target/docs/agents/completion-check-agent.md\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-y3bm0fjn/target/docs/agents/runtime-registry.md\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-y3bm0fjn/target/docs/agents/implementation-policy.md\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-y3bm0fjn/target/docs/agents/triage-labels.md\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-y3bm0fjn/target/docs/agents/shiki-cli-architecture.md\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-y3bm0fjn/target/docs/agents/issue-tracker.md\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-y3bm0fjn/target/docs/agents/execution-control.md\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-y3bm0fjn/target/docs/agents/branch-protection-smoke.md\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-y3bm0fjn/target/docs/agents/ledger-schema.md\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-y3bm0fjn/target/docs/agents/evidence-integrity.md\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-y3bm0fjn/target/docs/adr/0018-carried-guardian-approval-across-a-proven-base-sync.md\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-y3bm0fjn/target/docs/adr/0010-external-ai-guardian-review.md\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-y3bm0fjn/target/docs/adr/0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-y3bm0fjn/target/docs/adr/template.md\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-y3bm0fjn/target/docs/adr/0015-contract-approval-before-dispatch.md\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-y3bm0fjn/target/docs/adr/0001-keep-shiki-as-platform-name.md\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-y3bm0fjn/target/docs/adr/0011-autonomous-quality-gate-evidence.md\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-y3bm0fjn/target/docs/adr/0007-packaging-and-release.md\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-y3bm0fjn/target/docs/adr/0002-use-agents-as-shared-constitution.md\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-y3bm0fjn/target/docs/adr/0009-requirements-definition-and-spec-freeze.md\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-y3bm0fjn/target/docs/adr/README.md\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-y3bm0fjn/target/docs/adr/0012-autonomous-loop-closeout-to-main.md\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-y3bm0fjn/target/docs/adr/0013-bootstrap-grants-actions-approve-pull-requests.md\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-y3bm0fjn/target/docs/adr/0008-use-claude-code-as-default-implementer.md\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-y3bm0fjn/target/docs/adr/0014-codex-app-external-ai-guardian-ui-adapter.md\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-y3bm0fjn/target/docs/adr/0004-use-github-first-with-shiki-mirror.md\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-y3bm0fjn/target/docs/adr/0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-y3bm0fjn/target/docs/adr/0003-require-skill-gate-for-mergegate-readiness.md\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-y3bm0fjn/target/docs/adr/0017-bookkeeping-closeouts-do-not-inherit-risk.md\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-y3bm0fjn/target/docs/adr/0016-locks-bound-product-paths.md\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-y3bm0fjn/target/skills/engineering/shiki/SKILL.md\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-y3bm0fjn/target/skills/engineering/setup-matt-pocock-skills/issue-tracker-gitlab.md\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-y3bm0fjn/target/skills/engineering/setup-matt-pocock-skills/domain.md\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-y3bm0fjn/target/skills/engineering/setup-matt-pocock-skills/triage-labels.md\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-y3bm0fjn/target/skills/engineering/setup-matt-pocock-skills/SKILL.md\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-y3bm0fjn/target/skills/engineering/setup-matt-pocock-skills/issue-tracker-github.md\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-y3bm0fjn/target/skills/engineering/setup-matt-pocock-skills/issue-tracker-local.md\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-y3bm0fjn/target/skills/engineering/code-review/SKILL.md\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-y3bm0fjn/target/skills/engineering/improve-codebase-architecture/LANGUAGE.md\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-y3bm0fjn/target/skills/engineering/improve-codebase-architecture/SKILL.md\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-y3bm0fjn/target/skills/engineering/improve-codebase-architecture/HTML-REPORT.md\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-y3bm0fjn/target/skills/engineering/improve-codebase-architecture/DEEPENING.md\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-y3bm0fjn/target/skills/engineering/improve-codebase-architecture/INTERFACE-DESIGN.md\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-y3bm0fjn/target/skills/engineering/triage/OUT-OF-SCOPE.md\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-y3bm0fjn/target/skills/engineering/triage/SKILL.md\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-y3bm0fjn/target/skills/engineering/triage/AGENT-BRIEF.md\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-y3bm0fjn/target/skills/engineering/prototype/SKILL.md\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-y3bm0fjn/target/skills/engineering/prototype/LOGIC.md\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-y3bm0fjn/target/skills/engineering/prototype/UI.md\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-y3bm0fjn/target/skills/engineering/diagnose/scripts/hitl-loop.template.sh\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-y3bm0fjn/target/skills/engineering/diagnose/SKILL.md\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-y3bm0fjn/target/skills/engineering/to-issues/SKILL.md\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-y3bm0fjn/target/skills/engineering/README.md\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-y3bm0fjn/target/skills/engineering/zoom-out/SKILL.md\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-y3bm0fjn/target/skills/engineering/grill-with-docs/SKILL.md\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-y3bm0fjn/target/skills/engineering/grill-with-docs/CONTEXT-FORMAT.md\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-y3bm0fjn/target/skills/engineering/grill-with-docs/ADR-FORMAT.md\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-y3bm0fjn/target/skills/engineering/tdd/mocking.md\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-y3bm0fjn/target/skills/engineering/tdd/deep-modules.md\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-y3bm0fjn/target/skills/engineering/tdd/tests.md\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-y3bm0fjn/target/skills/engineering/tdd/SKILL.md\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-y3bm0fjn/target/skills/engineering/tdd/interface-design.md\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-y3bm0fjn/target/skills/engineering/tdd/refactoring.md\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-y3bm0fjn/target/skills/engineering/to-prd/SKILL.md\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-y3bm0fjn/target/tests/test_lock_release.py\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-y3bm0fjn/target/tests/test_cca_verdict_usable.py\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-y3bm0fjn/target/tests/test_id_allocation.py\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-y3bm0fjn/target/tests/test_mirror_scope.py\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-y3bm0fjn/target/tests/test_config_loading.py\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-y3bm0fjn/target/tests/test_cca_verdict_nondegenerate.py\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-y3bm0fjn/target/tests/test_reopened_task.py\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-y3bm0fjn/target/tests/test_required_checks_derivation.py\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-y3bm0fjn/target/tests/test_install_completeness.py\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-y3bm0fjn/target/tests/test_policy_job_merged_prs.py\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-y3bm0fjn/target/tests/test_adr_references.py\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-y3bm0fjn/target/tests/test_session_lease.py\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-y3bm0fjn/target/tests/test_guardian_signal.py\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-y3bm0fjn/target/tests/test_cca_verdict_consistency.py\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-y3bm0fjn/target/tests/test_cca_head_sha_injection.py\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-y3bm0fjn/target/tests/test_sync_proof.py\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-y3bm0fjn/target/tests/test_guardian_carry.py\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-y3bm0fjn/target/tests/test_memory_scorecard.py\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-y3bm0fjn/target/tests/test_workflow_permissions_payload.py\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-y3bm0fjn/target/tests/test_contract_mode.py\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-y3bm0fjn/target/tests/test_memory_consult.py\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-y3bm0fjn/target/tests/test_workflow_invocation_carry.py\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-y3bm0fjn/target/tests/test_lock_conflict_stop.py\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-y3bm0fjn/target/tests/test_migrations_target.py\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-y3bm0fjn/target/tests/test_branch_protection_payload.py\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-y3bm0fjn/target/tests/test_loop_branch_plumbing.py\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-y3bm0fjn/target/tests/test_contract_immutability.py\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-y3bm0fjn/target/tests/test_loop_smoke.py\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-y3bm0fjn/target/tests/test_worktree_base.py\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-y3bm0fjn/target/tests/test_secret_set_claude.py\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-y3bm0fjn/target/tests/test_cca_completeness.py\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-y3bm0fjn/target/tests/test_paired_invariants.py\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-y3bm0fjn/target/tests/__init__.py\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-y3bm0fjn/target/tests/test_bookkeeping_closeout.py\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-y3bm0fjn/target/tests/test_contract_open.py\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-y3bm0fjn/target/tests/test_guardian_signal_closeout.py\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-y3bm0fjn/target/tests/test_cca_evidence_manifest_taskless.py\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-y3bm0fjn/target/tests/test_codeowners_wiring.py\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-y3bm0fjn/target/tests/test_guardian_review.py\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-y3bm0fjn/target/tests/shiki_test_support.py\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-y3bm0fjn/target/tests/test_guardian_approval_binding.py\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-y3bm0fjn/target/tests/test_completion_lock_scope.py\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-y3bm0fjn/target/tests/test_memory_promotion.py\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-y3bm0fjn/target/tests/test_loop_code_review.py\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-y3bm0fjn/target/tests/test_loop_verdict_gating.py\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-y3bm0fjn/target/tests/test_lock_lifecycle.py\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-y3bm0fjn/target/tests/test_memory_capture.py\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-y3bm0fjn/target/tests/test_loop_tdd_evidence.py\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-y3bm0fjn/target/tests/test_migration_mode.py\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-y3bm0fjn/target/tests/test_dispatch_lock_precondition.py\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-y3bm0fjn/target/tests/test_dispatch_state_sync.py\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-y3bm0fjn/target/tests/test_goal_reconcile.py\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-y3bm0fjn/target/tests/test_runner_adapters.py\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-y3bm0fjn/target/tests/test_lock_matching.py\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-y3bm0fjn/target/tests/test_contract_approval.py\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-y3bm0fjn/target/tests/test_memory_hooks.py\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-y3bm0fjn/target/tests/test_branch_behind_sync.py\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-y3bm0fjn/target/tests/test_install_force_semantics.py\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-y3bm0fjn/target/tests/test_contract_plan_registration.py\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-y3bm0fjn/target/tests/test_sadr_install_migration.py\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-y3bm0fjn/target/tests/test_loop_engine.py\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-y3bm0fjn/target/tests/test_repair_delivery.py\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-y3bm0fjn/target/tests/test_handoff_consult.py\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-y3bm0fjn/target/tests/test_loop_smoke3.py\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-y3bm0fjn/target/tests/test_contract_amendment.py\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-y3bm0fjn/target/tests/test_bootstrap_safety.py\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-y3bm0fjn/target/tests/test_guardian_carry_parity.py\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-y3bm0fjn/target/tests/test_guardian_evidence.py\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-y3bm0fjn/target/tests/test_loop_smoke2.py\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-y3bm0fjn/target/tests/test_loop_lock_guard.py\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-y3bm0fjn/target/tests/test_manifest_staging.py\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-y3bm0fjn/target/tests/test_loop_lock_validator_hint.py\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-y3bm0fjn/target/tests/test_guardian_status.py\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-y3bm0fjn/target/tests/test_workflow_invocation.py\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-y3bm0fjn/target/tests/test_closeout.py\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-y3bm0fjn/target/tests/test_mirror_schema_validation.py\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-y3bm0fjn/target/tests/test_repair_reasons.py\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-y3bm0fjn/target/scripts/shiki_schema.py\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-y3bm0fjn/target/scripts/validate_shiki.py\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-y3bm0fjn/target/scripts/shiki_contracts.py\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-y3bm0fjn/target/scripts/shiki_jsonschema.py\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-y3bm0fjn/target/scripts/shiki_evidence.py\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-y3bm0fjn/target/scripts/shiki_locks.py\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-y3bm0fjn/target/scripts/shiki_loop.py\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-y3bm0fjn/target/scripts/shiki_memory.py\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-y3bm0fjn/target/scripts/shiki_manifest.py\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-y3bm0fjn/target/scripts/shiki_migrations.py\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-y3bm0fjn/target/scripts/shiki_provider.py\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-y3bm0fjn/target/scripts/shiki_workflows.py\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-y3bm0fjn/target/scripts/enforce_cca_verdict.py\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-y3bm0fjn/target/scripts/cca_verdict_usable.py\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-y3bm0fjn/target/scripts/build_cca_evidence_manifest.py\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-y3bm0fjn/target/scripts/guardian_approval_signal.py\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-y3bm0fjn/target/scripts/mergegate_check.py\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-y3bm0fjn/target/scripts/shiki_sync_proof.py\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-y3bm0fjn/target/scripts/shiki.py\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-y3bm0fjn/target/scripts/shiki_bootstrap.py\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-y3bm0fjn/target/scripts/shiki_cli.py\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-y3bm0fjn/target/scripts/shiki_config.py\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-y3bm0fjn/target/scripts/shiki_contract_approval.py\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-y3bm0fjn/target/scripts/shiki_doctor.py\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-y3bm0fjn/target/scripts/shiki_git.py\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-y3bm0fjn/target/scripts/shiki_github.py\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-y3bm0fjn/target/scripts/shiki_guardian.py\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-y3bm0fjn/target/scripts/shiki_guardian_review.py\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-y3bm0fjn/target/scripts/shiki_guardian_status.py\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-y3bm0fjn/target/scripts/shiki_installer.py\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-y3bm0fjn/target/scripts/shiki_process.py\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-y3bm0fjn/target/scripts/shiki_runtime.py\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-y3bm0fjn/target/scripts/shiki_runtime_adapters.py\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-y3bm0fjn/target/scripts/shiki_runtime_registry.py\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-y3bm0fjn/target/scripts/shiki_session_lease.py\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-y3bm0fjn/target/scripts/shiki_state_classes.py\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-y3bm0fjn/target/scripts/shiki_tasks.py\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-y3bm0fjn/target/scripts/shiki_state.py\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-y3bm0fjn/target/scripts/test_shiki_init.sh\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-y3bm0fjn/target/scripts/test_shiki_control_plane.sh\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-y3bm0fjn/target/scripts/test_shiki_run_orchestrator.sh\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-y3bm0fjn/target/scripts/test_shiki_daemon_runner.sh\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-y3bm0fjn/target/scripts/test_shiki_runner_codex.sh\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-y3bm0fjn/target/scripts/test_shiki_runner_claude.sh\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-y3bm0fjn/target/scripts/test_shiki_goal_loop.sh\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-y3bm0fjn/target/scripts/test_shiki_memory_loop.sh\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-y3bm0fjn/target/scripts/test_shiki_code_review_gate.sh\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-y3bm0fjn/target/scripts/test_shiki_start.sh\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-y3bm0fjn/target/scripts/test_shiki_runtime_auth.sh\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-y3bm0fjn/target/scripts/test_shiki_runtime_registry.sh\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-y3bm0fjn/target/scripts/test_shiki_state_classes.sh\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-y3bm0fjn/target/scripts/test_shiki_provider_config.sh\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-y3bm0fjn/target/scripts/test_shiki_guardian_policy.sh\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-y3bm0fjn/target/scripts/test_shiki_evidence_integrity.sh\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-y3bm0fjn/target/scripts/test_shiki_governance_evidence.sh\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-y3bm0fjn/target/scripts/test_shiki_doctor.sh\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-y3bm0fjn/target/scripts/test_shiki_migrations.sh\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-y3bm0fjn/target/scripts/test_shiki_module_boundaries.sh\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-y3bm0fjn/target/scripts/test_shiki_shellcheck.sh\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-y3bm0fjn/target/scripts/test_shiki_validator_hardening.sh\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-y3bm0fjn/target/scripts/test_shiki_workflow_lint.sh\n[shiki] ensured empty state directory: /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-y3bm0fjn/target/.shiki/goals\n[shiki] ensured empty state directory: /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-y3bm0fjn/target/.shiki/plans\n[shiki] ensured empty state directory: /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-y3bm0fjn/target/.shiki/tasks\n[shiki] ensured empty state directory: /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-y3bm0fjn/target/.shiki/dag\n[shiki] ensured empty state directory: /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-y3bm0fjn/target/.shiki/ledger\n[shiki] ensured empty state directory: /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-y3bm0fjn/target/.shiki/migrations\n[shiki] ensured empty state directory: /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-y3bm0fjn/target/.shiki/locks\n[shiki] ensured empty state directory: /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-y3bm0fjn/target/.shiki/worktrees\n[shiki] ensured empty state directory: /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-y3bm0fjn/target/.shiki/repairs\n[shiki] ensured empty state directory: /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-y3bm0fjn/target/.shiki/reports\n[shiki] ensured empty state directory: /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-y3bm0fjn/target/.shiki/runs\n[shiki] ensured empty state directory: /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-y3bm0fjn/target/.shiki/inbox\n[shiki] ensured empty state directory: /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-y3bm0fjn/target/.shiki/handoffs\n[shiki] ensured empty state directory: /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-y3bm0fjn/target/.shiki/runner\n[shiki] ensured empty state directory: /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-y3bm0fjn/target/.shiki/smoke\n[shiki] ensured empty state directory: /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-y3bm0fjn/target/.shiki/starts\n[shiki] ensured empty state directory: /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-y3bm0fjn/target/.shiki/memories\n[shiki] wrote install stamp: /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-y3bm0fjn/target/.shiki/install-stamp.json\n[shiki] template summary: no .new files written (no existing project-content or governance file was kept alongside a refreshed copy)\n[shiki] target Shiki validation passed\n\nexec\n/bin/zsh -lc \"sed -n '1,240p' docs/adr/README.md; sed -n '1,200p' docs/adr/template.md; rg -n '(ADR[- ]00(0[1-9]|1[0-8])|docs/adr/00(0[1-9]|1[0-8])-|\\\\(adr/00(0[1-9]|1[0-8])-)' docs/adr\" in /private/tmp/shiki-adr-collision.pa0KwY/.worktrees/shiki-t-20260807t073307221254z-2d22d654-introduc\n succeeded in 0ms:\n# Architecture Decision Records\n\nThis directory holds Shiki's Architecture Decision Records (ADRs): durable\nrecords of hard-to-reverse platform decisions. ADRs are part of the Shiki source\nof truth alongside GitHub state, the `.shiki/` mirror, and `CONTEXT.md`.\n\n## How ADRs Work\n\n- New ADRs are added as `docs/adr/NNNN-<short-slug>.md`, where `NNNN` is the next\n  zero-padded sequence number.\n- The **canonical list of ADRs is the set of files matching `docs/adr/*.md`** in\n  this directory. Browse `docs/adr/*.md` for the authoritative, current set.\n- Use [`template.md`](template.md) as the starting point for a new ADR.\n- Record an ADR when a decision is hard to reverse, surprising without context,\n  and a real tradeoff. Do not write an ADR for routine or easily reversible\n  choices.\n\nUsing the `docs/adr/*.md` glob as the source of truth means other PRs can add an\nADR without editing this index file, avoiding merge contention on a\nhand-maintained enumeration.\n\n## Current ADRs (snapshot)\n\nThe list below is a convenience snapshot and may lag behind the directory. When\nin doubt, list `docs/adr/*.md`.\n\n- [0001 \u2014 Keep Shiki As The Platform Name](0001-keep-shiki-as-platform-name.md)\n- [0002 \u2014 Use AGENTS As The Shared Agent Constitution](0002-use-agents-as-shared-constitution.md)\n- [0003 \u2014 Require Skill Gate For MergeGate Readiness](0003-require-skill-gate-for-mergegate-readiness.md)\n- [0004 \u2014 Use GitHub First With A Repository-Local Shiki Mirror](0004-use-github-first-with-shiki-mirror.md)\n- [0005 \u2014 Use Codex As Front And Claude Code Action As The GitHub Runtime](0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md)\n- [0006 \u2014 Separate Codex Implementation, CCA Completion, And MergeGate Authorization](0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md)\n\n## Adding A New ADR\n\n1. Copy `template.md` to `docs/adr/NNNN-<short-slug>.md`.\n2. Fill in the title, status, context, decision, and consequences.\n3. Set the status to `Proposed`, then `Accepted` once the decision is approved.\n4. Open a PR following `CONTRIBUTING.md`. You do not need to edit this index for\n   the ADR to be discoverable via `docs/adr/*.md`.\n# NNNN: <Short Decision Title>\n\n## Status\n\nProposed | Accepted | Superseded by ADR-NNNN\n\n## Context\n\nWhat is the situation, constraint, or force that requires a decision? Reference\nrelevant domain terms (`CONTEXT.md`), prior ADRs, and code reality. State the\nproblem, not the solution.\n\n## Decision\n\nThe decision, stated in active voice (\"We will ...\"). Be specific about what is\nin scope and what is explicitly out of scope.\n\n## Consequences\n\nThe results of the decision: what becomes easier, what becomes harder, new\nobligations, and follow-up work. Note any risks accepted and any Guardian or\narchitecture-gate approvals required.\n\n## Alternatives Considered\n\nOther options that were evaluated and why they were not chosen.\ndocs/adr/0016-locks-bound-product-paths.md:1:# ADR 0016: Locks bound product paths; the Shiki mirror is bound by record identity\ndocs/adr/0016-locks-bound-product-paths.md:9:- Related: ADR 0004 (GitHub-first with a `.shiki` mirror), ADR 0009 (Spec Freeze),\ndocs/adr/0016-locks-bound-product-paths.md:10:  ADR 0012 (autonomous loop closeout to main), ADR 0015 (Contract Approval before\ndocs/adr/0016-locks-bound-product-paths.md:93:a one-time Spec Amendment per plan, which must land before ADR 0015's contract\ndocs/adr/0016-locks-bound-product-paths.md:97:registry, and ADR 0015's governance fields, from the base `.shiki` snapshot when\ndocs/adr/0016-locks-bound-product-paths.md:111:ADR 0012's closeout pull request. Registration of multi-task goals is ADR 0015's\ndocs/adr/0016-locks-bound-product-paths.md:130:add `goal_id` to ADR 0015's frozen governance field set, and until that lands a\ndocs/adr/0016-locks-bound-product-paths.md:142:before ADR 0015 T1; extension of the adversarial suites in\ndocs/adr/0016-locks-bound-product-paths.md:154:## Relationship to ADR 0004\ndocs/adr/0016-locks-bound-product-paths.md:156:This ADR **clarifies something ADR 0004 left unspecified**. ADR 0004 decides that\ndocs/adr/0016-locks-bound-product-paths.md:160:ADR 0004's Decision or Consequences is amended, superseded or weakened: the\ndocs/adr/0016-locks-bound-product-paths.md:167:It **amends ADR 0012**, whose Decision item 5 states that \"Loop-executed tasks\ndocs/adr/0016-locks-bound-product-paths.md:171:authorized by record identity, not by a declared lock. ADR 0012's substantive\ndocs/adr/0016-locks-bound-product-paths.md:175:It is **complementary to ADR 0015**, not a substitute. ADR 0015 owns contract\ndocs/adr/0016-locks-bound-product-paths.md:179:ADR 0015 T1's freezing of the `locks` field.\ndocs/adr/0016-locks-bound-product-paths.md:189:removes the low/medium autonomous path ADR 0008/0009 exists to provide. And it\ndocs/adr/0016-locks-bound-product-paths.md:209:ADR 0004's consequence that mirror conflicts are repaired against GitHub state;\ndocs/adr/0017-bookkeeping-closeouts-do-not-inherit-risk.md:1:# ADR 0017: A Bookkeeping Closeout Does Not Inherit Its Task's Risk\ndocs/adr/0017-bookkeeping-closeouts-do-not-inherit-risk.md:9:ADR 0012 made the goal loop push a task's completion to the default branch through a\ndocs/adr/0017-bookkeeping-closeouts-do-not-inherit-risk.md:52:   and `expected_branch` may differ \u2014 the ADR 0015 contract-immutability rule,\ndocs/adr/0017-bookkeeping-closeouts-do-not-inherit-risk.md:116:ADR 0012, which recorded why: a task file that is already `done` on the implementation\ndocs/adr/0017-bookkeeping-closeouts-do-not-inherit-risk.md:121:is a runtime authoring its own approval, which ADR 0010 and the constitution's\ndocs/adr/0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md:1:# ADR 0006: Separate Codex Implementation, CCA Completion, And MergeGate Authorization\ndocs/adr/0004-use-github-first-with-shiki-mirror.md:1:# ADR 0004: Use GitHub First With A Repository-Local Shiki Mirror\ndocs/adr/0014-codex-app-external-ai-guardian-ui-adapter.md:1:# ADR 0014: Use Codex App As The External AI Guardian UI Adapter\ndocs/adr/0014-codex-app-external-ai-guardian-ui-adapter.md:9:ADR 0010 admits `external_ai_guardian_review` as a first-class Guardian\ndocs/adr/0008-use-claude-code-as-default-implementer.md:1:# ADR 0008: Use Claude Code As The Default Implementer Runtime\ndocs/adr/0008-use-claude-code-as-default-implementer.md:5:Proposed (supersedes ADR 0005 for the implementer default; keeps its auth model)\ndocs/adr/0008-use-claude-code-as-default-implementer.md:9:ADR 0005 made Codex Front the default implementation runtime, with Claude Code\ndocs/adr/0008-use-claude-code-as-default-implementer.md:21:- The Goal lifecycle redesign (ADR 0009) requires an autonomous post-freeze\ndocs/adr/0008-use-claude-code-as-default-implementer.md:27:workflow\", ADR 0002). Any change to the implementer default must preserve the\ndocs/adr/0008-use-claude-code-as-default-implementer.md:42:  ADR 0005 remain supported for tasks explicitly assigned to `codex`.\ndocs/adr/0008-use-claude-code-as-default-implementer.md:63:(still requires its own ADR per ADR 0005).\ndocs/adr/0008-use-claude-code-as-default-implementer.md:103:  constitution (ADR 0002) for no operational gain and forecloses\ndocs/adr/0013-bootstrap-grants-actions-approve-pull-requests.md:1:# ADR 0013: Bootstrap Grants GitHub Actions Permission To Create And Approve Pull Requests\ndocs/adr/0013-bootstrap-grants-actions-approve-pull-requests.md:51:  posture is solo/AFK autonomous operation (ADR 0008/0009/0012), where the CCA\ndocs/adr/0012-autonomous-loop-closeout-to-main.md:6:- Related: ADR 0008 (default implementer + autonomous merge), ADR 0009 (spec freeze), ADR 0011 (autonomous quality-gate evidence), issue #140 (complete the autonomous post-freeze loop), the #140 T5 live self-drive.\ndocs/adr/0012-autonomous-loop-closeout-to-main.md:40:- The loop reaches **goal complete on main, durably, with zero manual intervention** \u2014 closing #140 / ADR 0011's \"durable evidence, not local attestation\" for the completion step itself.\ndocs/adr/0009-requirements-definition-and-spec-freeze.md:1:# ADR 0009: Merge Grill And Planning Into Requirements Definition With Spec Freeze\ndocs/adr/0009-requirements-definition-and-spec-freeze.md:24:everything after it autonomous (ADR 0008 provides the implementation runtime\ndocs/adr/0007-packaging-and-release.md:1:# ADR 0007: Packaging And Release For Shiki\ndocs/adr/0007-packaging-and-release.md:28:  (consistent with ADR 0005's no-API-key-by-default stance).\ndocs/adr/0011-autonomous-quality-gate-evidence.md:1:# ADR 0011: Autonomous Quality-Gate Evidence \u2014 Deterministic Facts And An Independent Verifier\ndocs/adr/0011-autonomous-quality-gate-evidence.md:9:The Shiki 0.2.0 autonomous post-freeze loop (ADR 0008/0009, PRD #119 G-D) must\ndocs/adr/0015-contract-approval-before-dispatch.md:1:# ADR 0015: Approve The Task Contract Before Dispatch, Not The Diff After It\ndocs/adr/0015-contract-approval-before-dispatch.md:12:`external_ai_guardian_review` artifact (ADR 0010/0014) \u2014 is bound to a PR, and\ndocs/adr/0015-contract-approval-before-dispatch.md:133:is both earlier and smaller; the external AI Guardian path (ADR 0010/0014) works\ndocs/adr/0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md:1:# ADR 0005: Use Codex As Front And Claude Code Action As The GitHub Runtime\ndocs/adr/0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md:5:Accepted (implementer default superseded by ADR 0008; the auth model and the\ndocs/adr/0010-external-ai-guardian-review.md:1:# ADR 0010: External AI Guardian Review As A First-Class Approval Authority\ndocs/adr/0018-carried-guardian-approval-across-a-proven-base-sync.md:1:# ADR 0018: Carried Guardian Approval Across A Proven Base Sync\n\ndiff --git a/tests/test_adr_references.py b/tests/test_adr_references.py\nindex 51706329c797d5dd9b47e082a333fa83b261d482..92569d9fa25e22cfd71c17561d715485027a0d2d\n--- a/tests/test_adr_references.py\n+++ b/tests/test_adr_references.py\n@@ -31,17 +31,18 @@\n \n REPO_ROOT = Path(__file__).resolve().parents[1]\n \n-# \"ADR 0016\" -- a four-digit citation. The trailing \\b keeps a five-digit token\n-# from being read as a truncated four-digit number.\n-_CITATION = re.compile(r\"\\bADR (\\d{4})\\b\")\n-# \"0016-locks-bound-product-paths.md\" -- the leading four digits name the record.\n-_ADR_FILENAME = re.compile(r\"^(\\d{4})-.*\\.md$\")\n+# ``ADR 0016`` / ``SADR-0016`` -- a namespace plus four-digit citation. The\n+# trailing ``\\b`` keeps a five-digit token from being read as a truncated one.\n+_CITATION = re.compile(r\"\\b(S?ADR)[ -](\\d{4})\\b\")\n+# Target ADRs retain the numeric filename; Shiki platform decisions use SADR.\n+_DECISION_FILENAME = re.compile(r\"^(?:(SADR)-)?(\\d{4})-.*\\.md$\")\n \n \n class Violation(NamedTuple):\n     \"\"\"A citation that does not resolve to exactly one decision record.\"\"\"\n \n     citing_file: str  # repo-relative path of the file that cites the ADR\n+    namespace: str  # \"ADR\" for target decisions; \"SADR\" for Shiki decisions\n     number: str  # the cited four-digit number, e.g. \"0016\"\n     line: int  # 1-based line of the citation\n     reason: str  # \"missing\" (zero matches) or \"ambiguous\" (two or more)\n@@ -49,32 +50,34 @@\n \n     @property\n     def expected_path(self) -> str:\n-        return f\"docs/adr/{self.number}-*.md\"\n+        prefix = \"SADR-\" if self.namespace == \"SADR\" else \"\"\n+        return f\"docs/adr/{prefix}{self.number}-*.md\"\n \n     def describe(self) -> str:\n         \"\"\"Name the citing file, the cited number, and the missing path.\"\"\"\n         if self.reason == \"missing\":\n             return (\n-                f\"{self.citing_file}:{self.line} cites ADR {self.number} \"\n+                f\"{self.citing_file}:{self.line} cites {self.namespace} {self.number} \"\n                 f\"but no document matches {self.expected_path}\"\n             )\n         joined = \", \".join(self.matches)\n         return (\n-            f\"{self.citing_file}:{self.line} cites ADR {self.number} which \"\n+            f\"{self.citing_file}:{self.line} cites {self.namespace} {self.number} which \"\n             f\"resolves to {len(self.matches)} documents (expected exactly one \"\n             f\"at {self.expected_path}): {joined}\"\n         )\n \n \n-def _adr_index(root: Path) -> dict[str, list[Path]]:\n-    \"\"\"Map every four-digit ADR number to the record files that claim it.\"\"\"\n-    index: dict[str, list[Path]] = {}\n+def _adr_index(root: Path) -> dict[tuple[str, str], list[Path]]:\n+    \"\"\"Map each decision namespace and number to the files that claim it.\"\"\"\n+    index: dict[tuple[str, str], list[Path]] = {}\n     adr_dir = root / \"docs\" / \"adr\"\n     if adr_dir.is_dir():\n         for path in sorted(adr_dir.glob(\"*.md\")):\n-            match = _ADR_FILENAME.match(path.name)\n+            match = _DECISION_FILENAME.match(path.name)\n             if match:\n-                index.setdefault(match.group(1), []).append(path)\n+                namespace = \"SADR\" if match.group(1) else \"ADR\"\n+                index.setdefault((namespace, match.group(2)), []).append(path)\n     return index\n \n \n@@ -110,13 +113,15 @@\n         rel = path.relative_to(root).as_posix()\n         for lineno, line in enumerate(text.splitlines(), start=1):\n             for match in _CITATION.finditer(line):\n-                number = match.group(1)\n-                found = index.get(number, [])\n+                namespace, number = match.groups()\n+                found = index.get((namespace, number), [])\n                 if len(found) == 1:\n                     continue\n                 reason = \"missing\" if not found else \"ambiguous\"\n                 rels = [p.relative_to(root).as_posix() for p in found]\n-                violations.append(Violation(rel, number, lineno, reason, rels))\n+                violations.append(\n+                    Violation(rel, namespace, number, lineno, reason, rels)\n+                )\n     return violations\n \n \n@@ -146,8 +151,8 @@\n         # The two records this Goal carried onto the default branch, and which\n         # the scripts cite most, must be present.\n         index = _adr_index(REPO_ROOT)\n-        self.assertIn(\"0016\", index)\n-        self.assertIn(\"0017\", index)\n+        self.assertIn((\"ADR\", \"0016\"), index)\n+        self.assertIn((\"ADR\", \"0017\"), index)\n \n \n class FixtureAdrReferences(unittest.TestCase):\n@@ -180,6 +185,17 @@\n         self.assertIn(\"9991\", message)  # cited number\n         self.assertIn(\"docs/adr/9991-*.md\", message)  # missing path\n \n+    def test_target_adr_does_not_satisfy_missing_platform_sadr(self) -> None:\n+        root = self._make_repo(\n+            adr_names=[\"0042-target-decision.md\"],\n+            script_body='\"\"\"Target ADR 0042 exists, but platform SADR-0042 does not.\"\"\"\\n',\n+        )\n+        violations = find_adr_reference_violations(root, include_docs=False)\n+        self.assertEqual(len(violations), 1)\n+        self.assertEqual(violations[0].namespace, \"SADR\")\n+        self.assertEqual(violations[0].number, \"0042\")\n+        self.assertEqual(violations[0].expected_path, \"docs/adr/SADR-0042-*.md\")\n+\n     def test_uncited_adr_is_allowed(self) -> None:\n         # An ADR exists but nothing cites it -> not a violation. The converse\n         # (every ADR must be cited) is explicitly NOT required.\ndiff --git a/tests/test_sadr_install_migration.py b/tests/test_sadr_install_migration.py\nnew file mode 100644\nindex 0000000000000000000000000000000000000000..a6398c81377f00c09aa2cb684980b070cfaf714c\n--- /dev/null\n+++ b/tests/test_sadr_install_migration.py\n@@ -0,0 +1,56 @@\n+\"\"\"SADR installation and collision-safe legacy upgrade behavior.\"\"\"\n+\n+from __future__ import annotations\n+\n+import shutil\n+import tempfile\n+import unittest\n+from pathlib import Path\n+\n+import shiki_test_support  # noqa: F401  (path bootstrap)\n+\n+import shiki_installer\n+from tests.test_adr_references import find_adr_reference_violations\n+\n+\n+PRODUCT_NUMBERS = tuple(range(13, 19))\n+\n+\n+class FreshSadrInstallTests(unittest.TestCase):\n+    def setUp(self) -> None:\n+        self.tmp = Path(tempfile.mkdtemp(prefix=\"shiki-sadr-fresh-\"))\n+        self.addCleanup(lambda: shutil.rmtree(self.tmp, ignore_errors=True))\n+        self.target = self.tmp / \"target\"\n+        self.adr_dir = self.target / \"docs\" / \"adr\"\n+        self.adr_dir.mkdir(parents=True)\n+        self.product_bytes = {\n+            f\"{number:04d}-product-decision.md\": (\n+                f\"# Product ADR {number:04d}\\n\\nTarget-owned bytes {number}.\\n\"\n+            ).encode()\n+            for number in PRODUCT_NUMBERS\n+        }\n+        for name, content in self.product_bytes.items():\n+            (self.adr_dir / name).write_bytes(content)\n+\n+    def test_fresh_install_preserves_product_adrs_and_installs_all_sadrs(self) -> None:\n+        shiki_installer.install_template(self.target, force=False, validate=True)\n+\n+        for name, expected in self.product_bytes.items():\n+            self.assertEqual((self.adr_dir / name).read_bytes(), expected)\n+\n+        sadr_numbers = {\n+            path.name.split(\"-\", 2)[1]\n+            for path in self.adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\")\n+        }\n+        self.assertEqual(sadr_numbers, {f\"{number:04d}\" for number in range(1, 20)})\n+\n+        numeric_records = {\n+            path.name\n+            for path in self.adr_dir.glob(\"[0-9][0-9][0-9][0-9]-*.md\")\n+        }\n+        self.assertEqual(numeric_records, set(self.product_bytes))\n+        self.assertEqual(find_adr_reference_violations(self.target), [])\n+\n+\n+if __name__ == \"__main__\":\n+    unittest.main()\n\ncodex\nThe fresh-install fixture reproduces hypothesis 1 exactly: target ADR bytes survive, but the installer adds the platform\u2019s numeric ADR set, leaving zero SADRs and duplicate number ownership. I\u2019m now making the minimal tree-level change for this slice\u2014rename the 18 platform records, add SADR-0019, and switch only live platform citations/paths. Historical `.shiki` evidence remains untouched.\n2026-08-07T08:07:26.911925Z  WARN codex_core::responses_retry: stream disconnected - retrying sampling request (1/5 in 188ms)...\n2026-08-07T08:07:27.881754Z ERROR codex_models_manager::manager: failed to renew cache TTL: missing field `base_instructions` at line 94 column 5\napply patch\npatch: completed\n/private/tmp/shiki-adr-collision.pa0KwY/.worktrees/shiki-t-20260807t073307221254z-2d22d654-introduc/docs/adr/0001-keep-shiki-as-platform-name.md\n/private/tmp/shiki-adr-collision.pa0KwY/.worktrees/shiki-t-20260807t073307221254z-2d22d654-introduc/docs/adr/0002-use-agents-as-shared-constitution.md\n/private/tmp/shiki-adr-collision.pa0KwY/.worktrees/shiki-t-20260807t073307221254z-2d22d654-introduc/docs/adr/0003-require-skill-gate-for-mergegate-readiness.md\n/private/tmp/shiki-adr-collision.pa0KwY/.worktrees/shiki-t-20260807t073307221254z-2d22d654-introduc/docs/adr/0004-use-github-first-with-shiki-mirror.md\n/private/tmp/shiki-adr-collision.pa0KwY/.worktrees/shiki-t-20260807t073307221254z-2d22d654-introduc/docs/adr/0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\n/private/tmp/shiki-adr-collision.pa0KwY/.worktrees/shiki-t-20260807t073307221254z-2d22d654-introduc/docs/adr/0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\n/private/tmp/shiki-adr-collision.pa0KwY/.worktrees/shiki-t-20260807t073307221254z-2d22d654-introduc/docs/adr/0007-packaging-and-release.md\n/private/tmp/shiki-adr-collision.pa0KwY/.worktrees/shiki-t-20260807t073307221254z-2d22d654-introduc/docs/adr/0008-use-claude-code-as-default-implementer.md\n/private/tmp/shiki-adr-collision.pa0KwY/.worktrees/shiki-t-20260807t073307221254z-2d22d654-introduc/docs/adr/0009-requirements-definition-and-spec-freeze.md\n/private/tmp/shiki-adr-collision.pa0KwY/.worktrees/shiki-t-20260807t073307221254z-2d22d654-introduc/docs/adr/0010-external-ai-guardian-review.md\n/private/tmp/shiki-adr-collision.pa0KwY/.worktrees/shiki-t-20260807t073307221254z-2d22d654-introduc/docs/adr/0011-autonomous-quality-gate-evidence.md\n/private/tmp/shiki-adr-collision.pa0KwY/.worktrees/shiki-t-20260807t073307221254z-2d22d654-introduc/docs/adr/0012-autonomous-loop-closeout-to-main.md\n/private/tmp/shiki-adr-collision.pa0KwY/.worktrees/shiki-t-20260807t073307221254z-2d22d654-introduc/docs/adr/0013-bootstrap-grants-actions-approve-pull-requests.md\n/private/tmp/shiki-adr-collision.pa0KwY/.worktrees/shiki-t-20260807t073307221254z-2d22d654-introduc/docs/adr/0014-codex-app-external-ai-guardian-ui-adapter.md\n/private/tmp/shiki-adr-collision.pa0KwY/.worktrees/shiki-t-20260807t073307221254z-2d22d654-introduc/docs/adr/0015-contract-approval-before-dispatch.md\n/private/tmp/shiki-adr-collision.pa0KwY/.worktrees/shiki-t-20260807t073307221254z-2d22d654-introduc/docs/adr/0016-locks-bound-product-paths.md\n/private/tmp/shiki-adr-collision.pa0KwY/.worktrees/shiki-t-20260807t073307221254z-2d22d654-introduc/docs/adr/0017-bookkeeping-closeouts-do-not-inherit-risk.md\n/private/tmp/shiki-adr-collision.pa0KwY/.worktrees/shiki-t-20260807t073307221254z-2d22d654-introduc/docs/adr/0018-carried-guardian-approval-across-a-proven-base-sync.md\n/private/tmp/shiki-adr-collision.pa0KwY/.worktrees/shiki-t-20260807t073307221254z-2d22d654-introduc/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\ndiff --git a/docs/adr/0001-keep-shiki-as-platform-name.md b/docs/adr/SADR-0001-keep-shiki-as-platform-name.md\nindex 20a9637c3b9d8c97de7ba8db7cfae4d82d9a7dba..8b685439959aada1b221c112f0ac3c51ca8a1695\n--- a/docs/adr/0001-keep-shiki-as-platform-name.md\n+++ b/docs/adr/SADR-0001-keep-shiki-as-platform-name.md\n@@ -1,3 +1,3 @@\n-# Keep Shiki As The Platform Name\n+# SADR-0001: Keep Shiki As The Platform Name\n \n Shiki remains the product name for the GitHub-native agentic engineering control plane. We considered more descriptive names such as MergePilot, MergeGate, and Kiroku, but kept Shiki because it carries the existing goal-seeking framework identity while remaining independent of Hermes, Codex, Claude Code, or any single runtime.\ndiff --git a/docs/adr/0002-use-agents-as-shared-constitution.md b/docs/adr/SADR-0002-use-agents-as-shared-constitution.md\nindex 73de30ecb078120eac540d9bc51855e3cd48d0e3..c5486182bf06fca36ea508496b013c67302fd989\n--- a/docs/adr/0002-use-agents-as-shared-constitution.md\n+++ b/docs/adr/SADR-0002-use-agents-as-shared-constitution.md\n@@ -1,3 +1,3 @@\n-# Use AGENTS As The Shared Agent Constitution\n+# SADR-0002: Use AGENTS As The Shared Agent Constitution\n \n Shiki uses `AGENTS.md` as the runtime-neutral constitution and `CLAUDE.md` as a thin Claude Code wrapper. This keeps Codex, Claude Code, Hermes Runner, and future Agent Runtimes aligned on the same GitHub-first MergeGate rules while still allowing runtime-specific instructions where needed.\ndiff --git a/docs/adr/0003-require-skill-gate-for-mergegate-readiness.md b/docs/adr/SADR-0003-require-skill-gate-for-mergegate-readiness.md\nindex 424be0c9af4c8f0136f1fde68a468c47ff84e764..7fc337987c7df9792f75f181dfcbed75fe5ffff2\n--- a/docs/adr/0003-require-skill-gate-for-mergegate-readiness.md\n+++ b/docs/adr/SADR-0003-require-skill-gate-for-mergegate-readiness.md\n@@ -1,3 +1,3 @@\n-# Require Skill Gate For MergeGate Readiness\n+# SADR-0003: Require Skill Gate For MergeGate Readiness\n \n Shiki requires relevant engineering skills to be invoked before execution when their triggers apply. This makes skills part of MergeGate readiness rather than optional prompt style, because Shiki depends on repeatable planning, issue decomposition, TDD, diagnosis, triage, and architecture review before autonomous or parallel agent work can be trusted.\ndiff --git a/docs/adr/0004-use-github-first-with-shiki-mirror.md b/docs/adr/SADR-0004-use-github-first-with-shiki-mirror.md\nindex d0a3a9174dc646a945a9d325954ffaf00c352040..a79c54ae9ee8eed2a0524192b864da4e22a92d3f\n--- a/docs/adr/0004-use-github-first-with-shiki-mirror.md\n+++ b/docs/adr/SADR-0004-use-github-first-with-shiki-mirror.md\n@@ -1,4 +1,4 @@\n-# ADR 0004: Use GitHub First With A Repository-Local Shiki Mirror\n+# SADR-0004: Use GitHub First With A Repository-Local Shiki Mirror\n \n ## Status\n \ndiff --git a/docs/adr/0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md b/docs/adr/SADR-0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\nindex b339baf49b89d9290e79c3d51b36e7b2bde3bd95..c30a0d8b1fde0224d0ac7820e7870dccf721580d\n--- a/docs/adr/0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\n+++ b/docs/adr/SADR-0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\n@@ -1,4 +1,4 @@\n-# ADR 0005: Use Codex As Front And Claude Code Action As The GitHub Runtime\n+# SADR-0005: Use Codex As Front And Claude Code Action As The GitHub Runtime\n \n ## Status\n \ndiff --git a/docs/adr/0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md b/docs/adr/SADR-0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\nindex 4bcae9d1e52f0a37b8e9fbafa5cd14b6ee1993bd..7a38538a6ae9929567f0ade67e7ea6a60d0b3f7e\n--- a/docs/adr/0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\n+++ b/docs/adr/SADR-0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\n@@ -1,4 +1,4 @@\n-# ADR 0006: Separate Codex Implementation, CCA Completion, And MergeGate Authorization\n+# SADR-0006: Separate Codex Implementation, CCA Completion, And MergeGate Authorization\n \n ## Status\n \ndiff --git a/docs/adr/0007-packaging-and-release.md b/docs/adr/SADR-0007-packaging-and-release.md\nindex 79b84ddb97167372b44641e5b7860e400ce80822..023cd57f8011aa6e6d2ebf612b7c35a7a82c00d4\n--- a/docs/adr/0007-packaging-and-release.md\n+++ b/docs/adr/SADR-0007-packaging-and-release.md\n@@ -1,4 +1,4 @@\n-# ADR 0007: Packaging And Release For Shiki\n+# SADR-0007: Packaging And Release For Shiki\n \n ## Status\n \ndiff --git a/docs/adr/0008-use-claude-code-as-default-implementer.md b/docs/adr/SADR-0008-use-claude-code-as-default-implementer.md\nindex dedf4510b7c7799521422f622029563d373db031..a158a8d444d93764ec515a28133f7dde4af23579\n--- a/docs/adr/0008-use-claude-code-as-default-implementer.md\n+++ b/docs/adr/SADR-0008-use-claude-code-as-default-implementer.md\n@@ -1,4 +1,4 @@\n-# ADR 0008: Use Claude Code As The Default Implementer Runtime\n+# SADR-0008: Use Claude Code As The Default Implementer Runtime\n \n ## Status\n \ndiff --git a/docs/adr/0009-requirements-definition-and-spec-freeze.md b/docs/adr/SADR-0009-requirements-definition-and-spec-freeze.md\nindex 29b3f7871bef9203a8b4a831a65c39eb2f506e42..52e7dd69ad0aa4efe0b68c62678abbf7df02aaec\n--- a/docs/adr/0009-requirements-definition-and-spec-freeze.md\n+++ b/docs/adr/SADR-0009-requirements-definition-and-spec-freeze.md\n@@ -1,4 +1,4 @@\n-# ADR 0009: Merge Grill And Planning Into Requirements Definition With Spec Freeze\n+# SADR-0009: Merge Grill And Planning Into Requirements Definition With Spec Freeze\n \n ## Status\n \ndiff --git a/docs/adr/0010-external-ai-guardian-review.md b/docs/adr/SADR-0010-external-ai-guardian-review.md\nindex e6108bbe8bb4e73e0ff4684c8f78001a80438c4e..e690d4eddd270e683d709c9b679c5859e71eeaaa\n--- a/docs/adr/0010-external-ai-guardian-review.md\n+++ b/docs/adr/SADR-0010-external-ai-guardian-review.md\n@@ -1,4 +1,4 @@\n-# ADR 0010: External AI Guardian Review As A First-Class Approval Authority\n+# SADR-0010: External AI Guardian Review As A First-Class Approval Authority\n \n ## Status\n \ndiff --git a/docs/adr/0011-autonomous-quality-gate-evidence.md b/docs/adr/SADR-0011-autonomous-quality-gate-evidence.md\nindex bca0246c5d46e44afbde15486a75eec425fc02f5..9c93eac16d8c3a6d4b11bd7224e3955102cd7db4\n--- a/docs/adr/0011-autonomous-quality-gate-evidence.md\n+++ b/docs/adr/SADR-0011-autonomous-quality-gate-evidence.md\n@@ -1,4 +1,4 @@\n-# ADR 0011: Autonomous Quality-Gate Evidence \u2014 Deterministic Facts And An Independent Verifier\n+# SADR-0011: Autonomous Quality-Gate Evidence \u2014 Deterministic Facts And An Independent Verifier\n \n ## Status\n \ndiff --git a/docs/adr/0012-autonomous-loop-closeout-to-main.md b/docs/adr/SADR-0012-autonomous-loop-closeout-to-main.md\nindex 637fad6dde4989c9c2bf0f81f676d8d29991123c..ec7d85f81030a676bcbba28b43eb365422a36eac\n--- a/docs/adr/0012-autonomous-loop-closeout-to-main.md\n+++ b/docs/adr/SADR-0012-autonomous-loop-closeout-to-main.md\n@@ -1,4 +1,4 @@\n-# 0012 \u2014 Autonomous loop pushes goal completion to main via a closeout PR\n+# SADR-0012 \u2014 Autonomous loop pushes goal completion to main via a closeout PR\n \n - Status: accepted\n - Date: 2026-06-18\ndiff --git a/docs/adr/0013-bootstrap-grants-actions-approve-pull-requests.md b/docs/adr/SADR-0013-bootstrap-grants-actions-approve-pull-requests.md\nindex b6c69d64f1c5ced346b88d6859586715b3b5bd0e..a793e7277d67ab8ea56286fec289ca689825a17d\n--- a/docs/adr/0013-bootstrap-grants-actions-approve-pull-requests.md\n+++ b/docs/adr/SADR-0013-bootstrap-grants-actions-approve-pull-requests.md\n@@ -1,4 +1,4 @@\n-# ADR 0013: Bootstrap Grants GitHub Actions Permission To Create And Approve Pull Requests\n+# SADR-0013: Bootstrap Grants GitHub Actions Permission To Create And Approve Pull Requests\n \n ## Status\n \ndiff --git a/docs/adr/0014-codex-app-external-ai-guardian-ui-adapter.md b/docs/adr/SADR-0014-codex-app-external-ai-guardian-ui-adapter.md\nindex 238db06279334caa341c117362df9271382a7fcd..65f12e9939321e1b3e389a34c822fc1549e5155d\n--- a/docs/adr/0014-codex-app-external-ai-guardian-ui-adapter.md\n+++ b/docs/adr/SADR-0014-codex-app-external-ai-guardian-ui-adapter.md\n@@ -1,4 +1,4 @@\n-# ADR 0014: Use Codex App As The External AI Guardian UI Adapter\n+# SADR-0014: Use Codex App As The External AI Guardian UI Adapter\n \n ## Status\n \ndiff --git a/docs/adr/0015-contract-approval-before-dispatch.md b/docs/adr/SADR-0015-contract-approval-before-dispatch.md\nindex fad6c656741a20a7684ba16080a7db159f63a52f..7bdd760bb3e82eee4e1410f45c783b03b638d456\n--- a/docs/adr/0015-contract-approval-before-dispatch.md\n+++ b/docs/adr/SADR-0015-contract-approval-before-dispatch.md\n@@ -1,4 +1,4 @@\n-# ADR 0015: Approve The Task Contract Before Dispatch, Not The Diff After It\n+# SADR-0015: Approve The Task Contract Before Dispatch, Not The Diff After It\n \n ## Status\n \ndiff --git a/docs/adr/0016-locks-bound-product-paths.md b/docs/adr/SADR-0016-locks-bound-product-paths.md\nindex b5e850f3f1f7f4ef6e7f9d1763a29a51bd300c45..332cc17313498404f41a145291e38447b5de0dc4\n--- a/docs/adr/0016-locks-bound-product-paths.md\n+++ b/docs/adr/SADR-0016-locks-bound-product-paths.md\n@@ -1,4 +1,4 @@\n-# ADR 0016: Locks bound product paths; the Shiki mirror is bound by record identity\n+# SADR-0016: Locks bound product paths; the Shiki mirror is bound by record identity\n \n ## Status\n \ndiff --git a/docs/adr/0017-bookkeeping-closeouts-do-not-inherit-risk.md b/docs/adr/SADR-0017-bookkeeping-closeouts-do-not-inherit-risk.md\nindex 554e3a0d526a72ca067108c5464cc68c3edcf174..83115f69f1b1b48d544c9b0157f377b91cb53abf\n--- a/docs/adr/0017-bookkeeping-closeouts-do-not-inherit-risk.md\n+++ b/docs/adr/SADR-0017-bookkeeping-closeouts-do-not-inherit-risk.md\n@@ -1,4 +1,4 @@\n-# ADR 0017: A Bookkeeping Closeout Does Not Inherit Its Task's Risk\n+# SADR-0017: A Bookkeeping Closeout Does Not Inherit Its Task's Risk\n \n ## Status\n \ndiff --git a/docs/adr/0018-carried-guardian-approval-across-a-proven-base-sync.md b/docs/adr/SADR-0018-carried-guardian-approval-across-a-proven-base-sync.md\nindex 22358562da390ed8b45cd600da08a43d378b4680..38da0fd95102c3496c3219ce2319fce696a2532a\n--- a/docs/adr/0018-carried-guardian-approval-across-a-proven-base-sync.md\n+++ b/docs/adr/SADR-0018-carried-guardian-approval-across-a-proven-base-sync.md\n@@ -1,4 +1,4 @@\n-# ADR 0018: Carried Guardian Approval Across A Proven Base Sync\n+# SADR-0018: Carried Guardian Approval Across A Proven Base Sync\n \n ## Status\n \ndiff --git a/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md b/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\nnew file mode 100644\nindex 0000000000000000000000000000000000000000..7455ce1f3bed02ddddb851ac21a1d1a648c9aeba\n--- /dev/null\n+++ b/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\n@@ -0,0 +1,55 @@\n+# SADR-0019: Separate Shiki Platform SADRs From Target ADRs\n+\n+## Status\n+\n+Accepted\n+\n+## Context\n+\n+Shiki installs its platform decision records into `docs/adr/` in every target\n+repository. Shiki and its targets previously used the same numeric\n+`NNNN-<slug>.md` namespace. A target that already owned ADRs 0013 through 0018\n+therefore acquired two records for each number during installation, making an\n+`ADR NNNN` citation ambiguous. Renumbering target decisions would rewrite target\n+history, while renumbering Shiki decisions per target would destroy their stable\n+platform identity.\n+\n+Upgrades also need to remove the former Shiki-owned numeric paths without ever\n+mistaking a target-owned ADR for platform content. The install stamp already\n+records exact shipped paths and content digests, so it can prove ownership only\n+when both still match.\n+\n+## Decision\n+\n+Shiki platform decisions use the `SADR` namespace:\n+\n+- records are named `docs/adr/SADR-NNNN-<slug>.md`;\n+- live platform citations use `SADR-NNNN`;\n+- target product decisions retain `docs/adr/NNNN-<slug>.md` and `ADR NNNN`;\n+- the reference validator resolves the two namespaces independently, so the same\n+  four-digit number may exist once in each namespace.\n+\n+Before a forced upgrade writes anything, the installer inventories the exact\n+former numeric Shiki ADR paths. It may delete a legacy path only when the\n+existing install stamp contains that exact path and its recorded SHA-256 digest\n+matches the file currently on disk. All ownership blockers are reported\n+together and abort the upgrade before its first write. A non-forced install\n+never performs this cleanup.\n+\n+Historical `.shiki/` evidence remains unchanged because it records the namespace\n+that existed when that evidence was produced.\n+\n+## Consequences\n+\n+- Targets may use any ADR number without colliding with Shiki's platform SADRs.\n+- Fresh installs preserve target ADR bytes and install a stable SADR set.\n+- Forced upgrades fail closed when legacy ownership cannot be proven, and a\n+  successful upgrade refreshes the stamp without removed legacy paths.\n+- Current governance, packaging, workflow, prompt, script, and skill references\n+  must move with the platform records.\n+\n+## Alternatives Considered\n+\n+- Collision-only refusal leaves affected targets permanently unupgradable.\n+- Renumbering target ADRs mutates target-owned history.\n+- Per-target Shiki renumbering makes platform citations non-portable.\ndiff --git a/tests/test_adr_references.py b/tests/test_adr_references.py\nindex 51706329c797d5dd9b47e082a333fa83b261d482..92569d9fa25e22cfd71c17561d715485027a0d2d\n--- a/tests/test_adr_references.py\n+++ b/tests/test_adr_references.py\n@@ -31,17 +31,18 @@\n \n REPO_ROOT = Path(__file__).resolve().parents[1]\n \n-# \"ADR 0016\" -- a four-digit citation. The trailing \\b keeps a five-digit token\n-# from being read as a truncated four-digit number.\n-_CITATION = re.compile(r\"\\bADR (\\d{4})\\b\")\n-# \"0016-locks-bound-product-paths.md\" -- the leading four digits name the record.\n-_ADR_FILENAME = re.compile(r\"^(\\d{4})-.*\\.md$\")\n+# ``ADR 0016`` / ``SADR-0016`` -- a namespace plus four-digit citation. The\n+# trailing ``\\b`` keeps a five-digit token from being read as a truncated one.\n+_CITATION = re.compile(r\"\\b(S?ADR)[ -](\\d{4})\\b\")\n+# Target ADRs retain the numeric filename; Shiki platform decisions use SADR.\n+_DECISION_FILENAME = re.compile(r\"^(?:(SADR)-)?(\\d{4})-.*\\.md$\")\n \n \n class Violation(NamedTuple):\n     \"\"\"A citation that does not resolve to exactly one decision record.\"\"\"\n \n     citing_file: str  # repo-relative path of the file that cites the ADR\n+    namespace: str  # \"ADR\" for target decisions; \"SADR\" for Shiki decisions\n     number: str  # the cited four-digit number, e.g. \"0016\"\n     line: int  # 1-based line of the citation\n     reason: str  # \"missing\" (zero matches) or \"ambiguous\" (two or more)\n@@ -49,32 +50,34 @@\n \n     @property\n     def expected_path(self) -> str:\n-        return f\"docs/adr/{self.number}-*.md\"\n+        prefix = \"SADR-\" if self.namespace == \"SADR\" else \"\"\n+        return f\"docs/adr/{prefix}{self.number}-*.md\"\n \n     def describe(self) -> str:\n         \"\"\"Name the citing file, the cited number, and the missing path.\"\"\"\n         if self.reason == \"missing\":\n             return (\n-                f\"{self.citing_file}:{self.line} cites ADR {self.number} \"\n+                f\"{self.citing_file}:{self.line} cites {self.namespace} {self.number} \"\n                 f\"but no document matches {self.expected_path}\"\n             )\n         joined = \", \".join(self.matches)\n         return (\n-            f\"{self.citing_file}:{self.line} cites ADR {self.number} which \"\n+            f\"{self.citing_file}:{self.line} cites {self.namespace} {self.number} which \"\n             f\"resolves to {len(self.matches)} documents (expected exactly one \"\n             f\"at {self.expected_path}): {joined}\"\n         )\n \n \n-def _adr_index(root: Path) -> dict[str, list[Path]]:\n-    \"\"\"Map every four-digit ADR number to the record files that claim it.\"\"\"\n-    index: dict[str, list[Path]] = {}\n+def _adr_index(root: Path) -> dict[tuple[str, str], list[Path]]:\n+    \"\"\"Map each decision namespace and number to the files that claim it.\"\"\"\n+    index: dict[tuple[str, str], list[Path]] = {}\n     adr_dir = root / \"docs\" / \"adr\"\n     if adr_dir.is_dir():\n         for path in sorted(adr_dir.glob(\"*.md\")):\n-            match = _ADR_FILENAME.match(path.name)\n+            match = _DECISION_FILENAME.match(path.name)\n             if match:\n-                index.setdefault(match.group(1), []).append(path)\n+                namespace = \"SADR\" if match.group(1) else \"ADR\"\n+                index.setdefault((namespace, match.group(2)), []).append(path)\n     return index\n \n \n@@ -110,13 +113,15 @@\n         rel = path.relative_to(root).as_posix()\n         for lineno, line in enumerate(text.splitlines(), start=1):\n             for match in _CITATION.finditer(line):\n-                number = match.group(1)\n-                found = index.get(number, [])\n+                namespace, number = match.groups()\n+                found = index.get((namespace, number), [])\n                 if len(found) == 1:\n                     continue\n                 reason = \"missing\" if not found else \"ambiguous\"\n                 rels = [p.relative_to(root).as_posix() for p in found]\n-                violations.append(Violation(rel, number, lineno, reason, rels))\n+                violations.append(\n+                    Violation(rel, namespace, number, lineno, reason, rels)\n+                )\n     return violations\n \n \n@@ -146,8 +151,8 @@\n         # The two records this Goal carried onto the default branch, and which\n         # the scripts cite most, must be present.\n         index = _adr_index(REPO_ROOT)\n-        self.assertIn(\"0016\", index)\n-        self.assertIn(\"0017\", index)\n+        self.assertIn((\"ADR\", \"0016\"), index)\n+        self.assertIn((\"ADR\", \"0017\"), index)\n \n \n class FixtureAdrReferences(unittest.TestCase):\n@@ -180,6 +185,17 @@\n         self.assertIn(\"9991\", message)  # cited number\n         self.assertIn(\"docs/adr/9991-*.md\", message)  # missing path\n \n+    def test_target_adr_does_not_satisfy_missing_platform_sadr(self) -> None:\n+        root = self._make_repo(\n+            adr_names=[\"0042-target-decision.md\"],\n+            script_body='\"\"\"Target ADR 0042 exists, but platform SADR-0042 does not.\"\"\"\\n',\n+        )\n+        violations = find_adr_reference_violations(root, include_docs=False)\n+        self.assertEqual(len(violations), 1)\n+        self.assertEqual(violations[0].namespace, \"SADR\")\n+        self.assertEqual(violations[0].number, \"0042\")\n+        self.assertEqual(violations[0].expected_path, \"docs/adr/SADR-0042-*.md\")\n+\n     def test_uncited_adr_is_allowed(self) -> None:\n         # An ADR exists but nothing cites it -> not a violation. The converse\n         # (every ADR must be cited) is explicitly NOT required.\ndiff --git a/tests/test_sadr_install_migration.py b/tests/test_sadr_install_migration.py\nnew file mode 100644\nindex 0000000000000000000000000000000000000000..a6398c81377f00c09aa2cb684980b070cfaf714c\n--- /dev/null\n+++ b/tests/test_sadr_install_migration.py\n@@ -0,0 +1,56 @@\n+\"\"\"SADR installation and collision-safe legacy upgrade behavior.\"\"\"\n+\n+from __future__ import annotations\n+\n+import shutil\n+import tempfile\n+import unittest\n+from pathlib import Path\n+\n+import shiki_test_support  # noqa: F401  (path bootstrap)\n+\n+import shiki_installer\n+from tests.test_adr_references import find_adr_reference_violations\n+\n+\n+PRODUCT_NUMBERS = tuple(range(13, 19))\n+\n+\n+class FreshSadrInstallTests(unittest.TestCase):\n+    def setUp(self) -> None:\n+        self.tmp = Path(tempfile.mkdtemp(prefix=\"shiki-sadr-fresh-\"))\n+        self.addCleanup(lambda: shutil.rmtree(self.tmp, ignore_errors=True))\n+        self.target = self.tmp / \"target\"\n+        self.adr_dir = self.target / \"docs\" / \"adr\"\n+        self.adr_dir.mkdir(parents=True)\n+        self.product_bytes = {\n+            f\"{number:04d}-product-decision.md\": (\n+                f\"# Product ADR {number:04d}\\n\\nTarget-owned bytes {number}.\\n\"\n+            ).encode()\n+            for number in PRODUCT_NUMBERS\n+        }\n+        for name, content in self.product_bytes.items():\n+            (self.adr_dir / name).write_bytes(content)\n+\n+    def test_fresh_install_preserves_product_adrs_and_installs_all_sadrs(self) -> None:\n+        shiki_installer.install_template(self.target, force=False, validate=True)\n+\n+        for name, expected in self.product_bytes.items():\n+            self.assertEqual((self.adr_dir / name).read_bytes(), expected)\n+\n+        sadr_numbers = {\n+            path.name.split(\"-\", 2)[1]\n+            for path in self.adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\")\n+        }\n+        self.assertEqual(sadr_numbers, {f\"{number:04d}\" for number in range(1, 20)})\n+\n+        numeric_records = {\n+            path.name\n+            for path in self.adr_dir.glob(\"[0-9][0-9][0-9][0-9]-*.md\")\n+        }\n+        self.assertEqual(numeric_records, set(self.product_bytes))\n+        self.assertEqual(find_adr_reference_violations(self.target), [])\n+\n+\n+if __name__ == \"__main__\":\n+    unittest.main()\n\ndiff --git a/docs/adr/0001-keep-shiki-as-platform-name.md b/docs/adr/SADR-0001-keep-shiki-as-platform-name.md\nindex 20a9637c3b9d8c97de7ba8db7cfae4d82d9a7dba..8b685439959aada1b221c112f0ac3c51ca8a1695\n--- a/docs/adr/0001-keep-shiki-as-platform-name.md\n+++ b/docs/adr/SADR-0001-keep-shiki-as-platform-name.md\n@@ -1,3 +1,3 @@\n-# Keep Shiki As The Platform Name\n+# SADR-0001: Keep Shiki As The Platform Name\n \n Shiki remains the product name for the GitHub-native agentic engineering control plane. We considered more descriptive names such as MergePilot, MergeGate, and Kiroku, but kept Shiki because it carries the existing goal-seeking framework identity while remaining independent of Hermes, Codex, Claude Code, or any single runtime.\ndiff --git a/docs/adr/0002-use-agents-as-shared-constitution.md b/docs/adr/SADR-0002-use-agents-as-shared-constitution.md\nindex 73de30ecb078120eac540d9bc51855e3cd48d0e3..c5486182bf06fca36ea508496b013c67302fd989\n--- a/docs/adr/0002-use-agents-as-shared-constitution.md\n+++ b/docs/adr/SADR-0002-use-agents-as-shared-constitution.md\n@@ -1,3 +1,3 @@\n-# Use AGENTS As The Shared Agent Constitution\n+# SADR-0002: Use AGENTS As The Shared Agent Constitution\n \n Shiki uses `AGENTS.md` as the runtime-neutral constitution and `CLAUDE.md` as a thin Claude Code wrapper. This keeps Codex, Claude Code, Hermes Runner, and future Agent Runtimes aligned on the same GitHub-first MergeGate rules while still allowing runtime-specific instructions where needed.\ndiff --git a/docs/adr/0003-require-skill-gate-for-mergegate-readiness.md b/docs/adr/SADR-0003-require-skill-gate-for-mergegate-readiness.md\nindex 424be0c9af4c8f0136f1fde68a468c47ff84e764..7fc337987c7df9792f75f181dfcbed75fe5ffff2\n--- a/docs/adr/0003-require-skill-gate-for-mergegate-readiness.md\n+++ b/docs/adr/SADR-0003-require-skill-gate-for-mergegate-readiness.md\n@@ -1,3 +1,3 @@\n-# Require Skill Gate For MergeGate Readiness\n+# SADR-0003: Require Skill Gate For MergeGate Readiness\n \n Shiki requires relevant engineering skills to be invoked before execution when their triggers apply. This makes skills part of MergeGate readiness rather than optional prompt style, because Shiki depends on repeatable planning, issue decomposition, TDD, diagnosis, triage, and architecture review before autonomous or parallel agent work can be trusted.\ndiff --git a/docs/adr/0004-use-github-first-with-shiki-mirror.md b/docs/adr/SADR-0004-use-github-first-with-shiki-mirror.md\nindex d0a3a9174dc646a945a9d325954ffaf00c352040..a79c54ae9ee8eed2a0524192b864da4e22a92d3f\n--- a/docs/adr/0004-use-github-first-with-shiki-mirror.md\n+++ b/docs/adr/SADR-0004-use-github-first-with-shiki-mirror.md\n@@ -1,4 +1,4 @@\n-# ADR 0004: Use GitHub First With A Repository-Local Shiki Mirror\n+# SADR-0004: Use GitHub First With A Repository-Local Shiki Mirror\n \n ## Status\n \ndiff --git a/docs/adr/0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md b/docs/adr/SADR-0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\nindex b339baf49b89d9290e79c3d51b36e7b2bde3bd95..c30a0d8b1fde0224d0ac7820e7870dccf721580d\n--- a/docs/adr/0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\n+++ b/docs/adr/SADR-0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\n@@ -1,4 +1,4 @@\n-# ADR 0005: Use Codex As Front And Claude Code Action As The GitHub Runtime\n+# SADR-0005: Use Codex As Front And Claude Code Action As The GitHub Runtime\n \n ## Status\n \ndiff --git a/docs/adr/0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md b/docs/adr/SADR-0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\nindex 4bcae9d1e52f0a37b8e9fbafa5cd14b6ee1993bd..7a38538a6ae9929567f0ade67e7ea6a60d0b3f7e\n--- a/docs/adr/0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\n+++ b/docs/adr/SADR-0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\n@@ -1,4 +1,4 @@\n-# ADR 0006: Separate Codex Implementation, CCA Completion, And MergeGate Authorization\n+# SADR-0006: Separate Codex Implementation, CCA Completion, And MergeGate Authorization\n \n ## Status\n \ndiff --git a/docs/adr/0007-packaging-and-release.md b/docs/adr/SADR-0007-packaging-and-release.md\nindex 79b84ddb97167372b44641e5b7860e400ce80822..023cd57f8011aa6e6d2ebf612b7c35a7a82c00d4\n--- a/docs/adr/0007-packaging-and-release.md\n+++ b/docs/adr/SADR-0007-packaging-and-release.md\n@@ -1,4 +1,4 @@\n-# ADR 0007: Packaging And Release For Shiki\n+# SADR-0007: Packaging And Release For Shiki\n \n ## Status\n \ndiff --git a/docs/adr/0008-use-claude-code-as-default-implementer.md b/docs/adr/SADR-0008-use-claude-code-as-default-implementer.md\nindex dedf4510b7c7799521422f622029563d373db031..a158a8d444d93764ec515a28133f7dde4af23579\n--- a/docs/adr/0008-use-claude-code-as-default-implementer.md\n+++ b/docs/adr/SADR-0008-use-claude-code-as-default-implementer.md\n@@ -1,4 +1,4 @@\n-# ADR 0008: Use Claude Code As The Default Implementer Runtime\n+# SADR-0008: Use Claude Code As The Default Implementer Runtime\n \n ## Status\n \ndiff --git a/docs/adr/0009-requirements-definition-and-spec-freeze.md b/docs/adr/SADR-0009-requirements-definition-and-spec-freeze.md\nindex 29b3f7871bef9203a8b4a831a65c39eb2f506e42..52e7dd69ad0aa4efe0b68c62678abbf7df02aaec\n--- a/docs/adr/0009-requirements-definition-and-spec-freeze.md\n+++ b/docs/adr/SADR-0009-requirements-definition-and-spec-freeze.md\n@@ -1,4 +1,4 @@\n-# ADR 0009: Merge Grill And Planning Into Requirements Definition With Spec Freeze\n+# SADR-0009: Merge Grill And Planning Into Requirements Definition With Spec Freeze\n \n ## Status\n \ndiff --git a/docs/adr/0010-external-ai-guardian-review.md b/docs/adr/SADR-0010-external-ai-guardian-review.md\nindex e6108bbe8bb4e73e0ff4684c8f78001a80438c4e..e690d4eddd270e683d709c9b679c5859e71eeaaa\n--- a/docs/adr/0010-external-ai-guardian-review.md\n+++ b/docs/adr/SADR-0010-external-ai-guardian-review.md\n@@ -1,4 +1,4 @@\n-# ADR 0010: External AI Guardian Review As A First-Class Approval Authority\n+# SADR-0010: External AI Guardian Review As A First-Class Approval Authority\n \n ## Status\n \ndiff --git a/docs/adr/0011-autonomous-quality-gate-evidence.md b/docs/adr/SADR-0011-autonomous-quality-gate-evidence.md\nindex bca0246c5d46e44afbde15486a75eec425fc02f5..9c93eac16d8c3a6d4b11bd7224e3955102cd7db4\n--- a/docs/adr/0011-autonomous-quality-gate-evidence.md\n+++ b/docs/adr/SADR-0011-autonomous-quality-gate-evidence.md\n@@ -1,4 +1,4 @@\n-# ADR 0011: Autonomous Quality-Gate Evidence \u2014 Deterministic Facts And An Independent Verifier\n+# SADR-0011: Autonomous Quality-Gate Evidence \u2014 Deterministic Facts And An Independent Verifier\n \n ## Status\n \ndiff --git a/docs/adr/0012-autonomous-loop-closeout-to-main.md b/docs/adr/SADR-0012-autonomous-loop-closeout-to-main.md\nindex 637fad6dde4989c9c2bf0f81f676d8d29991123c..ec7d85f81030a676bcbba28b43eb365422a36eac\n--- a/docs/adr/0012-autonomous-loop-closeout-to-main.md\n+++ b/docs/adr/SADR-0012-autonomous-loop-closeout-to-main.md\n@@ -1,4 +1,4 @@\n-# 0012 \u2014 Autonomous loop pushes goal completion to main via a closeout PR\n+# SADR-0012 \u2014 Autonomous loop pushes goal completion to main via a closeout PR\n \n - Status: accepted\n - Date: 2026-06-18\ndiff --git a/docs/adr/0013-bootstrap-grants-actions-approve-pull-requests.md b/docs/adr/SADR-0013-bootstrap-grants-actions-approve-pull-requests.md\nindex b6c69d64f1c5ced346b88d6859586715b3b5bd0e..a793e7277d67ab8ea56286fec289ca689825a17d\n--- a/docs/adr/0013-bootstrap-grants-actions-approve-pull-requests.md\n+++ b/docs/adr/SADR-0013-bootstrap-grants-actions-approve-pull-requests.md\n@@ -1,4 +1,4 @@\n-# ADR 0013: Bootstrap Grants GitHub Actions Permission To Create And Approve Pull Requests\n+# SADR-0013: Bootstrap Grants GitHub Actions Permission To Create And Approve Pull Requests\n \n ## Status\n \ndiff --git a/docs/adr/0014-codex-app-external-ai-guardian-ui-adapter.md b/docs/adr/SADR-0014-codex-app-external-ai-guardian-ui-adapter.md\nindex 238db06279334caa341c117362df9271382a7fcd..65f12e9939321e1b3e389a34c822fc1549e5155d\n--- a/docs/adr/0014-codex-app-external-ai-guardian-ui-adapter.md\n+++ b/docs/adr/SADR-0014-codex-app-external-ai-guardian-ui-adapter.md\n@@ -1,4 +1,4 @@\n-# ADR 0014: Use Codex App As The External AI Guardian UI Adapter\n+# SADR-0014: Use Codex App As The External AI Guardian UI Adapter\n \n ## Status\n \ndiff --git a/docs/adr/0015-contract-approval-before-dispatch.md b/docs/adr/SADR-0015-contract-approval-before-dispatch.md\nindex fad6c656741a20a7684ba16080a7db159f63a52f..7bdd760bb3e82eee4e1410f45c783b03b638d456\n--- a/docs/adr/0015-contract-approval-before-dispatch.md\n+++ b/docs/adr/SADR-0015-contract-approval-before-dispatch.md\n@@ -1,4 +1,4 @@\n-# ADR 0015: Approve The Task Contract Before Dispatch, Not The Diff After It\n+# SADR-0015: Approve The Task Contract Before Dispatch, Not The Diff After It\n \n ## Status\n \ndiff --git a/docs/adr/0016-locks-bound-product-paths.md b/docs/adr/SADR-0016-locks-bound-product-paths.md\nindex b5e850f3f1f7f4ef6e7f9d1763a29a51bd300c45..332cc17313498404f41a145291e38447b5de0dc4\n--- a/docs/adr/0016-locks-bound-product-paths.md\n+++ b/docs/adr/SADR-0016-locks-bound-product-paths.md\n@@ -1,4 +1,4 @@\n-# ADR 0016: Locks bound product paths; the Shiki mirror is bound by record identity\n+# SADR-0016: Locks bound product paths; the Shiki mirror is bound by record identity\n \n ## Status\n \ndiff --git a/docs/adr/0017-bookkeeping-closeouts-do-not-inherit-risk.md b/docs/adr/SADR-0017-bookkeeping-closeouts-do-not-inherit-risk.md\nindex 554e3a0d526a72ca067108c5464cc68c3edcf174..83115f69f1b1b48d544c9b0157f377b91cb53abf\n--- a/docs/adr/0017-bookkeeping-closeouts-do-not-inherit-risk.md\n+++ b/docs/adr/SADR-0017-bookkeeping-closeouts-do-not-inherit-risk.md\n@@ -1,4 +1,4 @@\n-# ADR 0017: A Bookkeeping Closeout Does Not Inherit Its Task's Risk\n+# SADR-0017: A Bookkeeping Closeout Does Not Inherit Its Task's Risk\n \n ## Status\n \ndiff --git a/docs/adr/0018-carried-guardian-approval-across-a-proven-base-sync.md b/docs/adr/SADR-0018-carried-guardian-approval-across-a-proven-base-sync.md\nindex 22358562da390ed8b45cd600da08a43d378b4680..38da0fd95102c3496c3219ce2319fce696a2532a\n--- a/docs/adr/0018-carried-guardian-approval-across-a-proven-base-sync.md\n+++ b/docs/adr/SADR-0018-carried-guardian-approval-across-a-proven-base-sync.md\n@@ -1,4 +1,4 @@\n-# ADR 0018: Carried Guardian Approval Across A Proven Base Sync\n+# SADR-0018: Carried Guardian Approval Across A Proven Base Sync\n \n ## Status\n \ndiff --git a/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md b/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\nnew file mode 100644\nindex 0000000000000000000000000000000000000000..7455ce1f3bed02ddddb851ac21a1d1a648c9aeba\n--- /dev/null\n+++ b/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\n@@ -0,0 +1,55 @@\n+# SADR-0019: Separate Shiki Platform SADRs From Target ADRs\n+\n+## Status\n+\n+Accepted\n+\n+## Context\n+\n+Shiki installs its platform decision records into `docs/adr/` in every target\n+repository. Shiki and its targets previously used the same numeric\n+`NNNN-<slug>.md` namespace. A target that already owned ADRs 0013 through 0018\n+therefore acquired two records for each number during installation, making an\n+`ADR NNNN` citation ambiguous. Renumbering target decisions would rewrite target\n+history, while renumbering Shiki decisions per target would destroy their stable\n+platform identity.\n+\n+Upgrades also need to remove the former Shiki-owned numeric paths without ever\n+mistaking a target-owned ADR for platform content. The install stamp already\n+records exact shipped paths and content digests, so it can prove ownership only\n+when both still match.\n+\n+## Decision\n+\n+Shiki platform decisions use the `SADR` namespace:\n+\n+- records are named `docs/adr/SADR-NNNN-<slug>.md`;\n+- live platform citations use `SADR-NNNN`;\n+- target product decisions retain `docs/adr/NNNN-<slug>.md` and `ADR NNNN`;\n+- the reference validator resolves the two namespaces independently, so the same\n+  four-digit number may exist once in each namespace.\n+\n+Before a forced upgrade writes anything, the installer inventories the exact\n+former numeric Shiki ADR paths. It may delete a legacy path only when the\n+existing install stamp contains that exact path and its recorded SHA-256 digest\n+matches the file currently on disk. All ownership blockers are reported\n+together and abort the upgrade before its first write. A non-forced install\n+never performs this cleanup.\n+\n+Historical `.shiki/` evidence remains unchanged because it records the namespace\n+that existed when that evidence was produced.\n+\n+## Consequences\n+\n+- Targets may use any ADR number without colliding with Shiki's platform SADRs.\n+- Fresh installs preserve target ADR bytes and install a stable SADR set.\n+- Forced upgrades fail closed when legacy ownership cannot be proven, and a\n+  successful upgrade refreshes the stamp without removed legacy paths.\n+- Current governance, packaging, workflow, prompt, script, and skill references\n+  must move with the platform records.\n+\n+## Alternatives Considered\n+\n+- Collision-only refusal leaves affected targets permanently unupgradable.\n+- Renumbering target ADRs mutates target-owned history.\n+- Per-target Shiki renumbering makes platform citations non-portable.\ndiff --git a/tests/test_adr_references.py b/tests/test_adr_references.py\nindex 51706329c797d5dd9b47e082a333fa83b261d482..92569d9fa25e22cfd71c17561d715485027a0d2d\n--- a/tests/test_adr_references.py\n+++ b/tests/test_adr_references.py\n@@ -31,17 +31,18 @@\n \n REPO_ROOT = Path(__file__).resolve().parents[1]\n \n-# \"ADR 0016\" -- a four-digit citation. The trailing \\b keeps a five-digit token\n-# from being read as a truncated four-digit number.\n-_CITATION = re.compile(r\"\\bADR (\\d{4})\\b\")\n-# \"0016-locks-bound-product-paths.md\" -- the leading four digits name the record.\n-_ADR_FILENAME = re.compile(r\"^(\\d{4})-.*\\.md$\")\n+# ``ADR 0016`` / ``SADR-0016`` -- a namespace plus four-digit citation. The\n+# trailing ``\\b`` keeps a five-digit token from being read as a truncated one.\n+_CITATION = re.compile(r\"\\b(S?ADR)[ -](\\d{4})\\b\")\n+# Target ADRs retain the numeric filename; Shiki platform decisions use SADR.\n+_DECISION_FILENAME = re.compile(r\"^(?:(SADR)-)?(\\d{4})-.*\\.md$\")\n \n \n class Violation(NamedTuple):\n     \"\"\"A citation that does not resolve to exactly one decision record.\"\"\"\n \n     citing_file: str  # repo-relative path of the file that cites the ADR\n+    namespace: str  # \"ADR\" for target decisions; \"SADR\" for Shiki decisions\n     number: str  # the cited four-digit number, e.g. \"0016\"\n     line: int  # 1-based line of the citation\n     reason: str  # \"missing\" (zero matches) or \"ambiguous\" (two or more)\n@@ -49,32 +50,34 @@\n \n     @property\n     def expected_path(self) -> str:\n-        return f\"docs/adr/{self.number}-*.md\"\n+        prefix = \"SADR-\" if self.namespace == \"SADR\" else \"\"\n+        return f\"docs/adr/{prefix}{self.number}-*.md\"\n \n     def describe(self) -> str:\n         \"\"\"Name the citing file, the cited number, and the missing path.\"\"\"\n         if self.reason == \"missing\":\n             return (\n-                f\"{self.citing_file}:{self.line} cites ADR {self.number} \"\n+                f\"{self.citing_file}:{self.line} cites {self.namespace} {self.number} \"\n                 f\"but no document matches {self.expected_path}\"\n             )\n         joined = \", \".join(self.matches)\n         return (\n-            f\"{self.citing_file}:{self.line} cites ADR {self.number} which \"\n+            f\"{self.citing_file}:{self.line} cites {self.namespace} {self.number} which \"\n             f\"resolves to {len(self.matches)} documents (expected exactly one \"\n             f\"at {self.expected_path}): {joined}\"\n         )\n \n \n-def _adr_index(root: Path) -> dict[str, list[Path]]:\n-    \"\"\"Map every four-digit ADR number to the record files that claim it.\"\"\"\n-    index: dict[str, list[Path]] = {}\n+def _adr_index(root: Path) -> dict[tuple[str, str], list[Path]]:\n+    \"\"\"Map each decision namespace and number to the files that claim it.\"\"\"\n+    index: dict[tuple[str, str], list[Path]] = {}\n     adr_dir = root / \"docs\" / \"adr\"\n     if adr_dir.is_dir():\n         for path in sorted(adr_dir.glob(\"*.md\")):\n-            match = _ADR_FILENAME.match(path.name)\n+            match = _DECISION_FILENAME.match(path.name)\n             if match:\n-                index.setdefault(match.group(1), []).append(path)\n+                namespace = \"SADR\" if match.group(1) else \"ADR\"\n+                index.setdefault((namespace, match.group(2)), []).append(path)\n     return index\n \n \n@@ -110,13 +113,15 @@\n         rel = path.relative_to(root).as_posix()\n         for lineno, line in enumerate(text.splitlines(), start=1):\n             for match in _CITATION.finditer(line):\n-                number = match.group(1)\n-                found = index.get(number, [])\n+                namespace, number = match.groups()\n+                found = index.get((namespace, number), [])\n                 if len(found) == 1:\n                     continue\n                 reason = \"missing\" if not found else \"ambiguous\"\n                 rels = [p.relative_to(root).as_posix() for p in found]\n-                violations.append(Violation(rel, number, lineno, reason, rels))\n+                violations.append(\n+                    Violation(rel, namespace, number, lineno, reason, rels)\n+                )\n     return violations\n \n \n@@ -146,8 +151,8 @@\n         # The two records this Goal carried onto the default branch, and which\n         # the scripts cite most, must be present.\n         index = _adr_index(REPO_ROOT)\n-        self.assertIn(\"0016\", index)\n-        self.assertIn(\"0017\", index)\n+        self.assertIn((\"ADR\", \"0016\"), index)\n+        self.assertIn((\"ADR\", \"0017\"), index)\n \n \n class FixtureAdrReferences(unittest.TestCase):\n@@ -180,6 +185,17 @@\n         self.assertIn(\"9991\", message)  # cited number\n         self.assertIn(\"docs/adr/9991-*.md\", message)  # missing path\n \n+    def test_target_adr_does_not_satisfy_missing_platform_sadr(self) -> None:\n+        root = self._make_repo(\n+            adr_names=[\"0042-target-decision.md\"],\n+            script_body='\"\"\"Target ADR 0042 exists, but platform SADR-0042 does not.\"\"\"\\n',\n+        )\n+        violations = find_adr_reference_violations(root, include_docs=False)\n+        self.assertEqual(len(violations), 1)\n+        self.assertEqual(violations[0].namespace, \"SADR\")\n+        self.assertEqual(violations[0].number, \"0042\")\n+        self.assertEqual(violations[0].expected_path, \"docs/adr/SADR-0042-*.md\")\n+\n     def test_uncited_adr_is_allowed(self) -> None:\n         # An ADR exists but nothing cites it -> not a violation. The converse\n         # (every ADR must be cited) is explicitly NOT required.\ndiff --git a/tests/test_sadr_install_migration.py b/tests/test_sadr_install_migration.py\nnew file mode 100644\nindex 0000000000000000000000000000000000000000..a6398c81377f00c09aa2cb684980b070cfaf714c\n--- /dev/null\n+++ b/tests/test_sadr_install_migration.py\n@@ -0,0 +1,56 @@\n+\"\"\"SADR installation and collision-safe legacy upgrade behavior.\"\"\"\n+\n+from __future__ import annotations\n+\n+import shutil\n+import tempfile\n+import unittest\n+from pathlib import Path\n+\n+import shiki_test_support  # noqa: F401  (path bootstrap)\n+\n+import shiki_installer\n+from tests.test_adr_references import find_adr_reference_violations\n+\n+\n+PRODUCT_NUMBERS = tuple(range(13, 19))\n+\n+\n+class FreshSadrInstallTests(unittest.TestCase):\n+    def setUp(self) -> None:\n+        self.tmp = Path(tempfile.mkdtemp(prefix=\"shiki-sadr-fresh-\"))\n+        self.addCleanup(lambda: shutil.rmtree(self.tmp, ignore_errors=True))\n+        self.target = self.tmp / \"target\"\n+        self.adr_dir = self.target / \"docs\" / \"adr\"\n+        self.adr_dir.mkdir(parents=True)\n+        self.product_bytes = {\n+            f\"{number:04d}-product-decision.md\": (\n+                f\"# Product ADR {number:04d}\\n\\nTarget-owned bytes {number}.\\n\"\n+            ).encode()\n+            for number in PRODUCT_NUMBERS\n+        }\n+        for name, content in self.product_bytes.items():\n+            (self.adr_dir / name).write_bytes(content)\n+\n+    def test_fresh_install_preserves_product_adrs_and_installs_all_sadrs(self) -> None:\n+        shiki_installer.install_template(self.target, force=False, validate=True)\n+\n+        for name, expected in self.product_bytes.items():\n+            self.assertEqual((self.adr_dir / name).read_bytes(), expected)\n+\n+        sadr_numbers = {\n+            path.name.split(\"-\", 2)[1]\n+            for path in self.adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\")\n+        }\n+        self.assertEqual(sadr_numbers, {f\"{number:04d}\" for number in range(1, 20)})\n+\n+        numeric_records = {\n+            path.name\n+            for path in self.adr_dir.glob(\"[0-9][0-9][0-9][0-9]-*.md\")\n+        }\n+        self.assertEqual(numeric_records, set(self.product_bytes))\n+        self.assertEqual(find_adr_reference_violations(self.target), [])\n+\n+\n+if __name__ == \"__main__\":\n+    unittest.main()\n\n2026-08-07T08:08:17.736372Z ERROR codex_models_manager::manager: failed to renew cache TTL: missing field `base_instructions` at line 94 column 5\nexec\n/bin/zsh -lc \"rg -l --glob '\"'!tests/test_adr_references.py'\"' --glob '\"'!tests/test_sadr_install_migration.py'\"' --glob '\"'!.shiki/**'\"' --glob '\"'!skills/**'\"' '\\\\bADR[- ](000[1-9]|001[0-8])\\\\b|docs/adr/(000[1-9]|001[0-8])-|\\\\(adr/(000[1-9]|001[0-8])-' AGENTS.md CLAUDE.md CONTEXT.md CONTRIBUTING.md SYSTEM_PROMPT.md pyproject.toml docs scripts tests .github .codex | sort\" in /private/tmp/shiki-adr-collision.pa0KwY/.worktrees/shiki-t-20260807t073307221254z-2d22d654-introduc\n succeeded in 0ms:\n.codex/skills/shiki/SKILL.md\n.github/prompts/cca-completion-check.md\n.github/workflows/release.yml\n.github/workflows/shiki-cca-completion.yml\n.github/workflows/shiki-mergegate.yml\nAGENTS.md\nCLAUDE.md\nCONTEXT.md\nCONTRIBUTING.md\nSYSTEM_PROMPT.md\ndocs/adr/SADR-0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\ndocs/adr/SADR-0007-packaging-and-release.md\ndocs/adr/SADR-0008-use-claude-code-as-default-implementer.md\ndocs/adr/SADR-0009-requirements-definition-and-spec-freeze.md\ndocs/adr/SADR-0011-autonomous-quality-gate-evidence.md\ndocs/adr/SADR-0012-autonomous-loop-closeout-to-main.md\ndocs/adr/SADR-0013-bootstrap-grants-actions-approve-pull-requests.md\ndocs/adr/SADR-0014-codex-app-external-ai-guardian-ui-adapter.md\ndocs/adr/SADR-0015-contract-approval-before-dispatch.md\ndocs/adr/SADR-0016-locks-bound-product-paths.md\ndocs/adr/SADR-0017-bookkeeping-closeouts-do-not-inherit-risk.md\ndocs/agents/autonomous-loop-e2e-acceptance.md\ndocs/agents/codex-handoff.md\ndocs/agents/guardian-policy.md\ndocs/agents/implementation-policy.md\ndocs/agents/runtime-auth-model.md\ndocs/agents/runtime-registry.md\ndocs/agents/shiki-cli-architecture.md\ndocs/agents/triage-labels.md\ndocs/proposals/0001-memory-loop-spec-freeze-review.md\ndocs/proposals/0002-autonomous-loop-completion.md\ndocs/releasing.md\npyproject.toml\nscripts/guardian_approval_signal.py\nscripts/mergegate_check.py\nscripts/shiki_bootstrap.py\nscripts/shiki_cli.py\nscripts/shiki_contract_approval.py\nscripts/shiki_github.py\nscripts/shiki_guardian.py\nscripts/shiki_guardian_review.py\nscripts/shiki_loop.py\nscripts/shiki_migrations.py\nscripts/shiki_runtime_adapters.py\nscripts/shiki_runtime_registry.py\nscripts/shiki_tasks.py\nscripts/test_shiki_adversarial_state.sh\nscripts/test_shiki_code_review_gate.sh\nscripts/test_shiki_control_plane.sh\nscripts/test_shiki_goal_loop.sh\nscripts/test_shiki_governance_evidence.sh\nscripts/test_shiki_guardian_policy.sh\nscripts/test_shiki_loop_e2e.sh\nscripts/test_shiki_loop_lock_guard.sh\nscripts/test_shiki_run_orchestrator.sh\nscripts/test_shiki_runner_claude.sh\nscripts/test_shiki_runtime_registry.sh\nscripts/test_shiki_start.sh\nscripts/validate_shiki.py\ntests/test_bookkeeping_closeout.py\ntests/test_branch_behind_sync.py\ntests/test_cca_evidence_manifest_taskless.py\ntests/test_closeout.py\ntests/test_completion_lock_scope.py\ntests/test_contract_amendment.py\ntests/test_contract_approval.py\ntests/test_contract_immutability.py\ntests/test_contract_mode.py\ntests/test_contract_open.py\ntests/test_contract_plan_registration.py\ntests/test_dispatch_lock_precondition.py\ntests/test_guardian_review.py\ntests/test_guardian_signal.py\ntests/test_guardian_signal_closeout.py\ntests/test_loop_branch_plumbing.py\ntests/test_loop_code_review.py\ntests/test_loop_e2e_contract.py\ntests/test_loop_engine.py\ntests/test_loop_lock_validator_hint.py\ntests/test_loop_tdd_evidence.py\ntests/test_mirror_scope.py\ntests/test_paired_invariants.py\ntests/test_policy_job_merged_prs.py\ntests/test_repair_delivery.py\ntests/test_runner_adapters.py\ntests/test_workflow_invocation.py\ntests/test_workflow_invocation_carry.py\n\ndiff --git a/docs/adr/0001-keep-shiki-as-platform-name.md b/docs/adr/SADR-0001-keep-shiki-as-platform-name.md\nindex 20a9637c3b9d8c97de7ba8db7cfae4d82d9a7dba..8b685439959aada1b221c112f0ac3c51ca8a1695\n--- a/docs/adr/0001-keep-shiki-as-platform-name.md\n+++ b/docs/adr/SADR-0001-keep-shiki-as-platform-name.md\n@@ -1,3 +1,3 @@\n-# Keep Shiki As The Platform Name\n+# SADR-0001: Keep Shiki As The Platform Name\n \n Shiki remains the product name for the GitHub-native agentic engineering control plane. We considered more descriptive names such as MergePilot, MergeGate, and Kiroku, but kept Shiki because it carries the existing goal-seeking framework identity while remaining independent of Hermes, Codex, Claude Code, or any single runtime.\ndiff --git a/docs/adr/0002-use-agents-as-shared-constitution.md b/docs/adr/SADR-0002-use-agents-as-shared-constitution.md\nindex 73de30ecb078120eac540d9bc51855e3cd48d0e3..c5486182bf06fca36ea508496b013c67302fd989\n--- a/docs/adr/0002-use-agents-as-shared-constitution.md\n+++ b/docs/adr/SADR-0002-use-agents-as-shared-constitution.md\n@@ -1,3 +1,3 @@\n-# Use AGENTS As The Shared Agent Constitution\n+# SADR-0002: Use AGENTS As The Shared Agent Constitution\n \n Shiki uses `AGENTS.md` as the runtime-neutral constitution and `CLAUDE.md` as a thin Claude Code wrapper. This keeps Codex, Claude Code, Hermes Runner, and future Agent Runtimes aligned on the same GitHub-first MergeGate rules while still allowing runtime-specific instructions where needed.\ndiff --git a/docs/adr/0003-require-skill-gate-for-mergegate-readiness.md b/docs/adr/SADR-0003-require-skill-gate-for-mergegate-readiness.md\nindex 424be0c9af4c8f0136f1fde68a468c47ff84e764..7fc337987c7df9792f75f181dfcbed75fe5ffff2\n--- a/docs/adr/0003-require-skill-gate-for-mergegate-readiness.md\n+++ b/docs/adr/SADR-0003-require-skill-gate-for-mergegate-readiness.md\n@@ -1,3 +1,3 @@\n-# Require Skill Gate For MergeGate Readiness\n+# SADR-0003: Require Skill Gate For MergeGate Readiness\n \n Shiki requires relevant engineering skills to be invoked before execution when their triggers apply. This makes skills part of MergeGate readiness rather than optional prompt style, because Shiki depends on repeatable planning, issue decomposition, TDD, diagnosis, triage, and architecture review before autonomous or parallel agent work can be trusted.\ndiff --git a/docs/adr/0004-use-github-first-with-shiki-mirror.md b/docs/adr/SADR-0004-use-github-first-with-shiki-mirror.md\nindex d0a3a9174dc646a945a9d325954ffaf00c352040..a79c54ae9ee8eed2a0524192b864da4e22a92d3f\n--- a/docs/adr/0004-use-github-first-with-shiki-mirror.md\n+++ b/docs/adr/SADR-0004-use-github-first-with-shiki-mirror.md\n@@ -1,4 +1,4 @@\n-# ADR 0004: Use GitHub First With A Repository-Local Shiki Mirror\n+# SADR-0004: Use GitHub First With A Repository-Local Shiki Mirror\n \n ## Status\n \ndiff --git a/docs/adr/0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md b/docs/adr/SADR-0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\nindex b339baf49b89d9290e79c3d51b36e7b2bde3bd95..c30a0d8b1fde0224d0ac7820e7870dccf721580d\n--- a/docs/adr/0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\n+++ b/docs/adr/SADR-0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\n@@ -1,4 +1,4 @@\n-# ADR 0005: Use Codex As Front And Claude Code Action As The GitHub Runtime\n+# SADR-0005: Use Codex As Front And Claude Code Action As The GitHub Runtime\n \n ## Status\n \ndiff --git a/docs/adr/0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md b/docs/adr/SADR-0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\nindex 4bcae9d1e52f0a37b8e9fbafa5cd14b6ee1993bd..7a38538a6ae9929567f0ade67e7ea6a60d0b3f7e\n--- a/docs/adr/0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\n+++ b/docs/adr/SADR-0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\n@@ -1,4 +1,4 @@\n-# ADR 0006: Separate Codex Implementation, CCA Completion, And MergeGate Authorization\n+# SADR-0006: Separate Codex Implementation, CCA Completion, And MergeGate Authorization\n \n ## Status\n \ndiff --git a/docs/adr/0007-packaging-and-release.md b/docs/adr/SADR-0007-packaging-and-release.md\nindex 79b84ddb97167372b44641e5b7860e400ce80822..023cd57f8011aa6e6d2ebf612b7c35a7a82c00d4\n--- a/docs/adr/0007-packaging-and-release.md\n+++ b/docs/adr/SADR-0007-packaging-and-release.md\n@@ -1,4 +1,4 @@\n-# ADR 0007: Packaging And Release For Shiki\n+# SADR-0007: Packaging And Release For Shiki\n \n ## Status\n \ndiff --git a/docs/adr/0008-use-claude-code-as-default-implementer.md b/docs/adr/SADR-0008-use-claude-code-as-default-implementer.md\nindex dedf4510b7c7799521422f622029563d373db031..a158a8d444d93764ec515a28133f7dde4af23579\n--- a/docs/adr/0008-use-claude-code-as-default-implementer.md\n+++ b/docs/adr/SADR-0008-use-claude-code-as-default-implementer.md\n@@ -1,4 +1,4 @@\n-# ADR 0008: Use Claude Code As The Default Implementer Runtime\n+# SADR-0008: Use Claude Code As The Default Implementer Runtime\n \n ## Status\n \ndiff --git a/docs/adr/0009-requirements-definition-and-spec-freeze.md b/docs/adr/SADR-0009-requirements-definition-and-spec-freeze.md\nindex 29b3f7871bef9203a8b4a831a65c39eb2f506e42..52e7dd69ad0aa4efe0b68c62678abbf7df02aaec\n--- a/docs/adr/0009-requirements-definition-and-spec-freeze.md\n+++ b/docs/adr/SADR-0009-requirements-definition-and-spec-freeze.md\n@@ -1,4 +1,4 @@\n-# ADR 0009: Merge Grill And Planning Into Requirements Definition With Spec Freeze\n+# SADR-0009: Merge Grill And Planning Into Requirements Definition With Spec Freeze\n \n ## Status\n \ndiff --git a/docs/adr/0010-external-ai-guardian-review.md b/docs/adr/SADR-0010-external-ai-guardian-review.md\nindex e6108bbe8bb4e73e0ff4684c8f78001a80438c4e..e690d4eddd270e683d709c9b679c5859e71eeaaa\n--- a/docs/adr/0010-external-ai-guardian-review.md\n+++ b/docs/adr/SADR-0010-external-ai-guardian-review.md\n@@ -1,4 +1,4 @@\n-# ADR 0010: External AI Guardian Review As A First-Class Approval Authority\n+# SADR-0010: External AI Guardian Review As A First-Class Approval Authority\n \n ## Status\n \ndiff --git a/docs/adr/0011-autonomous-quality-gate-evidence.md b/docs/adr/SADR-0011-autonomous-quality-gate-evidence.md\nindex bca0246c5d46e44afbde15486a75eec425fc02f5..9c93eac16d8c3a6d4b11bd7224e3955102cd7db4\n--- a/docs/adr/0011-autonomous-quality-gate-evidence.md\n+++ b/docs/adr/SADR-0011-autonomous-quality-gate-evidence.md\n@@ -1,4 +1,4 @@\n-# ADR 0011: Autonomous Quality-Gate Evidence \u2014 Deterministic Facts And An Independent Verifier\n+# SADR-0011: Autonomous Quality-Gate Evidence \u2014 Deterministic Facts And An Independent Verifier\n \n ## Status\n \ndiff --git a/docs/adr/0012-autonomous-loop-closeout-to-main.md b/docs/adr/SADR-0012-autonomous-loop-closeout-to-main.md\nindex 637fad6dde4989c9c2bf0f81f676d8d29991123c..ec7d85f81030a676bcbba28b43eb365422a36eac\n--- a/docs/adr/0012-autonomous-loop-closeout-to-main.md\n+++ b/docs/adr/SADR-0012-autonomous-loop-closeout-to-main.md\n@@ -1,4 +1,4 @@\n-# 0012 \u2014 Autonomous loop pushes goal completion to main via a closeout PR\n+# SADR-0012 \u2014 Autonomous loop pushes goal completion to main via a closeout PR\n \n - Status: accepted\n - Date: 2026-06-18\ndiff --git a/docs/adr/0013-bootstrap-grants-actions-approve-pull-requests.md b/docs/adr/SADR-0013-bootstrap-grants-actions-approve-pull-requests.md\nindex b6c69d64f1c5ced346b88d6859586715b3b5bd0e..a793e7277d67ab8ea56286fec289ca689825a17d\n--- a/docs/adr/0013-bootstrap-grants-actions-approve-pull-requests.md\n+++ b/docs/adr/SADR-0013-bootstrap-grants-actions-approve-pull-requests.md\n@@ -1,4 +1,4 @@\n-# ADR 0013: Bootstrap Grants GitHub Actions Permission To Create And Approve Pull Requests\n+# SADR-0013: Bootstrap Grants GitHub Actions Permission To Create And Approve Pull Requests\n \n ## Status\n \ndiff --git a/docs/adr/0014-codex-app-external-ai-guardian-ui-adapter.md b/docs/adr/SADR-0014-codex-app-external-ai-guardian-ui-adapter.md\nindex 238db06279334caa341c117362df9271382a7fcd..65f12e9939321e1b3e389a34c822fc1549e5155d\n--- a/docs/adr/0014-codex-app-external-ai-guardian-ui-adapter.md\n+++ b/docs/adr/SADR-0014-codex-app-external-ai-guardian-ui-adapter.md\n@@ -1,4 +1,4 @@\n-# ADR 0014: Use Codex App As The External AI Guardian UI Adapter\n+# SADR-0014: Use Codex App As The External AI Guardian UI Adapter\n \n ## Status\n \ndiff --git a/docs/adr/0015-contract-approval-before-dispatch.md b/docs/adr/SADR-0015-contract-approval-before-dispatch.md\nindex fad6c656741a20a7684ba16080a7db159f63a52f..7bdd760bb3e82eee4e1410f45c783b03b638d456\n--- a/docs/adr/0015-contract-approval-before-dispatch.md\n+++ b/docs/adr/SADR-0015-contract-approval-before-dispatch.md\n@@ -1,4 +1,4 @@\n-# ADR 0015: Approve The Task Contract Before Dispatch, Not The Diff After It\n+# SADR-0015: Approve The Task Contract Before Dispatch, Not The Diff After It\n \n ## Status\n \ndiff --git a/docs/adr/0016-locks-bound-product-paths.md b/docs/adr/SADR-0016-locks-bound-product-paths.md\nindex b5e850f3f1f7f4ef6e7f9d1763a29a51bd300c45..332cc17313498404f41a145291e38447b5de0dc4\n--- a/docs/adr/0016-locks-bound-product-paths.md\n+++ b/docs/adr/SADR-0016-locks-bound-product-paths.md\n@@ -1,4 +1,4 @@\n-# ADR 0016: Locks bound product paths; the Shiki mirror is bound by record identity\n+# SADR-0016: Locks bound product paths; the Shiki mirror is bound by record identity\n \n ## Status\n \ndiff --git a/docs/adr/0017-bookkeeping-closeouts-do-not-inherit-risk.md b/docs/adr/SADR-0017-bookkeeping-closeouts-do-not-inherit-risk.md\nindex 554e3a0d526a72ca067108c5464cc68c3edcf174..83115f69f1b1b48d544c9b0157f377b91cb53abf\n--- a/docs/adr/0017-bookkeeping-closeouts-do-not-inherit-risk.md\n+++ b/docs/adr/SADR-0017-bookkeeping-closeouts-do-not-inherit-risk.md\n@@ -1,4 +1,4 @@\n-# ADR 0017: A Bookkeeping Closeout Does Not Inherit Its Task's Risk\n+# SADR-0017: A Bookkeeping Closeout Does Not Inherit Its Task's Risk\n \n ## Status\n \ndiff --git a/docs/adr/0018-carried-guardian-approval-across-a-proven-base-sync.md b/docs/adr/SADR-0018-carried-guardian-approval-across-a-proven-base-sync.md\nindex 22358562da390ed8b45cd600da08a43d378b4680..38da0fd95102c3496c3219ce2319fce696a2532a\n--- a/docs/adr/0018-carried-guardian-approval-across-a-proven-base-sync.md\n+++ b/docs/adr/SADR-0018-carried-guardian-approval-across-a-proven-base-sync.md\n@@ -1,4 +1,4 @@\n-# ADR 0018: Carried Guardian Approval Across A Proven Base Sync\n+# SADR-0018: Carried Guardian Approval Across A Proven Base Sync\n \n ## Status\n \ndiff --git a/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md b/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\nnew file mode 100644\nindex 0000000000000000000000000000000000000000..7455ce1f3bed02ddddb851ac21a1d1a648c9aeba\n--- /dev/null\n+++ b/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\n@@ -0,0 +1,55 @@\n+# SADR-0019: Separate Shiki Platform SADRs From Target ADRs\n+\n+## Status\n+\n+Accepted\n+\n+## Context\n+\n+Shiki installs its platform decision records into `docs/adr/` in every target\n+repository. Shiki and its targets previously used the same numeric\n+`NNNN-<slug>.md` namespace. A target that already owned ADRs 0013 through 0018\n+therefore acquired two records for each number during installation, making an\n+`ADR NNNN` citation ambiguous. Renumbering target decisions would rewrite target\n+history, while renumbering Shiki decisions per target would destroy their stable\n+platform identity.\n+\n+Upgrades also need to remove the former Shiki-owned numeric paths without ever\n+mistaking a target-owned ADR for platform content. The install stamp already\n+records exact shipped paths and content digests, so it can prove ownership only\n+when both still match.\n+\n+## Decision\n+\n+Shiki platform decisions use the `SADR` namespace:\n+\n+- records are named `docs/adr/SADR-NNNN-<slug>.md`;\n+- live platform citations use `SADR-NNNN`;\n+- target product decisions retain `docs/adr/NNNN-<slug>.md` and `ADR NNNN`;\n+- the reference validator resolves the two namespaces independently, so the same\n+  four-digit number may exist once in each namespace.\n+\n+Before a forced upgrade writes anything, the installer inventories the exact\n+former numeric Shiki ADR paths. It may delete a legacy path only when the\n+existing install stamp contains that exact path and its recorded SHA-256 digest\n+matches the file currently on disk. All ownership blockers are reported\n+together and abort the upgrade before its first write. A non-forced install\n+never performs this cleanup.\n+\n+Historical `.shiki/` evidence remains unchanged because it records the namespace\n+that existed when that evidence was produced.\n+\n+## Consequences\n+\n+- Targets may use any ADR number without colliding with Shiki's platform SADRs.\n+- Fresh installs preserve target ADR bytes and install a stable SADR set.\n+- Forced upgrades fail closed when legacy ownership cannot be proven, and a\n+  successful upgrade refreshes the stamp without removed legacy paths.\n+- Current governance, packaging, workflow, prompt, script, and skill references\n+  must move with the platform records.\n+\n+## Alternatives Considered\n+\n+- Collision-only refusal leaves affected targets permanently unupgradable.\n+- Renumbering target ADRs mutates target-owned history.\n+- Per-target Shiki renumbering makes platform citations non-portable.\ndiff --git a/tests/test_adr_references.py b/tests/test_adr_references.py\nindex 51706329c797d5dd9b47e082a333fa83b261d482..92569d9fa25e22cfd71c17561d715485027a0d2d\n--- a/tests/test_adr_references.py\n+++ b/tests/test_adr_references.py\n@@ -31,17 +31,18 @@\n \n REPO_ROOT = Path(__file__).resolve().parents[1]\n \n-# \"ADR 0016\" -- a four-digit citation. The trailing \\b keeps a five-digit token\n-# from being read as a truncated four-digit number.\n-_CITATION = re.compile(r\"\\bADR (\\d{4})\\b\")\n-# \"0016-locks-bound-product-paths.md\" -- the leading four digits name the record.\n-_ADR_FILENAME = re.compile(r\"^(\\d{4})-.*\\.md$\")\n+# ``ADR 0016`` / ``SADR-0016`` -- a namespace plus four-digit citation. The\n+# trailing ``\\b`` keeps a five-digit token from being read as a truncated one.\n+_CITATION = re.compile(r\"\\b(S?ADR)[ -](\\d{4})\\b\")\n+# Target ADRs retain the numeric filename; Shiki platform decisions use SADR.\n+_DECISION_FILENAME = re.compile(r\"^(?:(SADR)-)?(\\d{4})-.*\\.md$\")\n \n \n class Violation(NamedTuple):\n     \"\"\"A citation that does not resolve to exactly one decision record.\"\"\"\n \n     citing_file: str  # repo-relative path of the file that cites the ADR\n+    namespace: str  # \"ADR\" for target decisions; \"SADR\" for Shiki decisions\n     number: str  # the cited four-digit number, e.g. \"0016\"\n     line: int  # 1-based line of the citation\n     reason: str  # \"missing\" (zero matches) or \"ambiguous\" (two or more)\n@@ -49,32 +50,34 @@\n \n     @property\n     def expected_path(self) -> str:\n-        return f\"docs/adr/{self.number}-*.md\"\n+        prefix = \"SADR-\" if self.namespace == \"SADR\" else \"\"\n+        return f\"docs/adr/{prefix}{self.number}-*.md\"\n \n     def describe(self) -> str:\n         \"\"\"Name the citing file, the cited number, and the missing path.\"\"\"\n         if self.reason == \"missing\":\n             return (\n-                f\"{self.citing_file}:{self.line} cites ADR {self.number} \"\n+                f\"{self.citing_file}:{self.line} cites {self.namespace} {self.number} \"\n                 f\"but no document matches {self.expected_path}\"\n             )\n         joined = \", \".join(self.matches)\n         return (\n-            f\"{self.citing_file}:{self.line} cites ADR {self.number} which \"\n+            f\"{self.citing_file}:{self.line} cites {self.namespace} {self.number} which \"\n             f\"resolves to {len(self.matches)} documents (expected exactly one \"\n             f\"at {self.expected_path}): {joined}\"\n         )\n \n \n-def _adr_index(root: Path) -> dict[str, list[Path]]:\n-    \"\"\"Map every four-digit ADR number to the record files that claim it.\"\"\"\n-    index: dict[str, list[Path]] = {}\n+def _adr_index(root: Path) -> dict[tuple[str, str], list[Path]]:\n+    \"\"\"Map each decision namespace and number to the files that claim it.\"\"\"\n+    index: dict[tuple[str, str], list[Path]] = {}\n     adr_dir = root / \"docs\" / \"adr\"\n     if adr_dir.is_dir():\n         for path in sorted(adr_dir.glob(\"*.md\")):\n-            match = _ADR_FILENAME.match(path.name)\n+            match = _DECISION_FILENAME.match(path.name)\n             if match:\n-                index.setdefault(match.group(1), []).append(path)\n+                namespace = \"SADR\" if match.group(1) else \"ADR\"\n+                index.setdefault((namespace, match.group(2)), []).append(path)\n     return index\n \n \n@@ -110,13 +113,15 @@\n         rel = path.relative_to(root).as_posix()\n         for lineno, line in enumerate(text.splitlines(), start=1):\n             for match in _CITATION.finditer(line):\n-                number = match.group(1)\n-                found = index.get(number, [])\n+                namespace, number = match.groups()\n+                found = index.get((namespace, number), [])\n                 if len(found) == 1:\n                     continue\n                 reason = \"missing\" if not found else \"ambiguous\"\n                 rels = [p.relative_to(root).as_posix() for p in found]\n-                violations.append(Violation(rel, number, lineno, reason, rels))\n+                violations.append(\n+                    Violation(rel, namespace, number, lineno, reason, rels)\n+                )\n     return violations\n \n \n@@ -146,8 +151,8 @@\n         # The two records this Goal carried onto the default branch, and which\n         # the scripts cite most, must be present.\n         index = _adr_index(REPO_ROOT)\n-        self.assertIn(\"0016\", index)\n-        self.assertIn(\"0017\", index)\n+        self.assertIn((\"ADR\", \"0016\"), index)\n+        self.assertIn((\"ADR\", \"0017\"), index)\n \n \n class FixtureAdrReferences(unittest.TestCase):\n@@ -180,6 +185,17 @@\n         self.assertIn(\"9991\", message)  # cited number\n         self.assertIn(\"docs/adr/9991-*.md\", message)  # missing path\n \n+    def test_target_adr_does_not_satisfy_missing_platform_sadr(self) -> None:\n+        root = self._make_repo(\n+            adr_names=[\"0042-target-decision.md\"],\n+            script_body='\"\"\"Target ADR 0042 exists, but platform SADR-0042 does not.\"\"\"\\n',\n+        )\n+        violations = find_adr_reference_violations(root, include_docs=False)\n+        self.assertEqual(len(violations), 1)\n+        self.assertEqual(violations[0].namespace, \"SADR\")\n+        self.assertEqual(violations[0].number, \"0042\")\n+        self.assertEqual(violations[0].expected_path, \"docs/adr/SADR-0042-*.md\")\n+\n     def test_uncited_adr_is_allowed(self) -> None:\n         # An ADR exists but nothing cites it -> not a violation. The converse\n         # (every ADR must be cited) is explicitly NOT required.\ndiff --git a/tests/test_sadr_install_migration.py b/tests/test_sadr_install_migration.py\nnew file mode 100644\nindex 0000000000000000000000000000000000000000..a6398c81377f00c09aa2cb684980b070cfaf714c\n--- /dev/null\n+++ b/tests/test_sadr_install_migration.py\n@@ -0,0 +1,56 @@\n+\"\"\"SADR installation and collision-safe legacy upgrade behavior.\"\"\"\n+\n+from __future__ import annotations\n+\n+import shutil\n+import tempfile\n+import unittest\n+from pathlib import Path\n+\n+import shiki_test_support  # noqa: F401  (path bootstrap)\n+\n+import shiki_installer\n+from tests.test_adr_references import find_adr_reference_violations\n+\n+\n+PRODUCT_NUMBERS = tuple(range(13, 19))\n+\n+\n+class FreshSadrInstallTests(unittest.TestCase):\n+    def setUp(self) -> None:\n+        self.tmp = Path(tempfile.mkdtemp(prefix=\"shiki-sadr-fresh-\"))\n+        self.addCleanup(lambda: shutil.rmtree(self.tmp, ignore_errors=True))\n+        self.target = self.tmp / \"target\"\n+        self.adr_dir = self.target / \"docs\" / \"adr\"\n+        self.adr_dir.mkdir(parents=True)\n+        self.product_bytes = {\n+            f\"{number:04d}-product-decision.md\": (\n+                f\"# Product ADR {number:04d}\\n\\nTarget-owned bytes {number}.\\n\"\n+            ).encode()\n+            for number in PRODUCT_NUMBERS\n+        }\n+        for name, content in self.product_bytes.items():\n+            (self.adr_dir / name).write_bytes(content)\n+\n+    def test_fresh_install_preserves_product_adrs_and_installs_all_sadrs(self) -> None:\n+        shiki_installer.install_template(self.target, force=False, validate=True)\n+\n+        for name, expected in self.product_bytes.items():\n+            self.assertEqual((self.adr_dir / name).read_bytes(), expected)\n+\n+        sadr_numbers = {\n+            path.name.split(\"-\", 2)[1]\n+            for path in self.adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\")\n+        }\n+        self.assertEqual(sadr_numbers, {f\"{number:04d}\" for number in range(1, 20)})\n+\n+        numeric_records = {\n+            path.name\n+            for path in self.adr_dir.glob(\"[0-9][0-9][0-9][0-9]-*.md\")\n+        }\n+        self.assertEqual(numeric_records, set(self.product_bytes))\n+        self.assertEqual(find_adr_reference_violations(self.target), [])\n+\n+\n+if __name__ == \"__main__\":\n+    unittest.main()\n\n2026-08-07T08:08:26.251125Z ERROR codex_models_manager::manager: failed to renew cache TTL: missing field `base_instructions` at line 94 column 5\nexec\n/bin/zsh -lc \"rg -l -0 --glob '\"'!tests/test_adr_references.py'\"' --glob '\"'!tests/test_sadr_install_migration.py'\"' --glob '\"'!.shiki/**'\"' --glob '\"'!skills/**'\"' '\\\\bADR[- ](000[1-9]|001[0-8])\\\\b|docs/adr/(000[1-9]|001[0-8])-|\\\\(adr/(000[1-9]|001[0-8])-' AGENTS.md CLAUDE.md CONTEXT.md CONTRIBUTING.md SYSTEM_PROMPT.md pyproject.toml docs scripts tests .github .codex | xargs -0 perl -pi -e 's/\\\\bADR[ -](000[1-9]|001[0-8])\\\\b/SADR-\"'$1/g; s#docs/adr/(000[1-9]|001[0-8])-#docs/adr/SADR-$1-#g; s#'\"\\\\(adr/(000[1-9]|001[0-8])-#(adr/SADR-\"'$1-#g'\"'\" in /private/tmp/shiki-adr-collision.pa0KwY/.worktrees/shiki-t-20260807t073307221254z-2d22d654-introduc\n succeeded in 0ms:\nCan't do inplace edit on .codex/skills/shiki/SKILL.md: Cannot make temp name: Operation not permitted, <> line 1291.\n\ndiff --git a/docs/adr/0001-keep-shiki-as-platform-name.md b/docs/adr/SADR-0001-keep-shiki-as-platform-name.md\nindex 20a9637c3b9d8c97de7ba8db7cfae4d82d9a7dba..8b685439959aada1b221c112f0ac3c51ca8a1695\n--- a/docs/adr/0001-keep-shiki-as-platform-name.md\n+++ b/docs/adr/SADR-0001-keep-shiki-as-platform-name.md\n@@ -1,3 +1,3 @@\n-# Keep Shiki As The Platform Name\n+# SADR-0001: Keep Shiki As The Platform Name\n \n Shiki remains the product name for the GitHub-native agentic engineering control plane. We considered more descriptive names such as MergePilot, MergeGate, and Kiroku, but kept Shiki because it carries the existing goal-seeking framework identity while remaining independent of Hermes, Codex, Claude Code, or any single runtime.\ndiff --git a/docs/adr/0002-use-agents-as-shared-constitution.md b/docs/adr/SADR-0002-use-agents-as-shared-constitution.md\nindex 73de30ecb078120eac540d9bc51855e3cd48d0e3..c5486182bf06fca36ea508496b013c67302fd989\n--- a/docs/adr/0002-use-agents-as-shared-constitution.md\n+++ b/docs/adr/SADR-0002-use-agents-as-shared-constitution.md\n@@ -1,3 +1,3 @@\n-# Use AGENTS As The Shared Agent Constitution\n+# SADR-0002: Use AGENTS As The Shared Agent Constitution\n \n Shiki uses `AGENTS.md` as the runtime-neutral constitution and `CLAUDE.md` as a thin Claude Code wrapper. This keeps Codex, Claude Code, Hermes Runner, and future Agent Runtimes aligned on the same GitHub-first MergeGate rules while still allowing runtime-specific instructions where needed.\ndiff --git a/docs/adr/0003-require-skill-gate-for-mergegate-readiness.md b/docs/adr/SADR-0003-require-skill-gate-for-mergegate-readiness.md\nindex 424be0c9af4c8f0136f1fde68a468c47ff84e764..7fc337987c7df9792f75f181dfcbed75fe5ffff2\n--- a/docs/adr/0003-require-skill-gate-for-mergegate-readiness.md\n+++ b/docs/adr/SADR-0003-require-skill-gate-for-mergegate-readiness.md\n@@ -1,3 +1,3 @@\n-# Require Skill Gate For MergeGate Readiness\n+# SADR-0003: Require Skill Gate For MergeGate Readiness\n \n Shiki requires relevant engineering skills to be invoked before execution when their triggers apply. This makes skills part of MergeGate readiness rather than optional prompt style, because Shiki depends on repeatable planning, issue decomposition, TDD, diagnosis, triage, and architecture review before autonomous or parallel agent work can be trusted.\ndiff --git a/docs/adr/0004-use-github-first-with-shiki-mirror.md b/docs/adr/SADR-0004-use-github-first-with-shiki-mirror.md\nindex d0a3a9174dc646a945a9d325954ffaf00c352040..a79c54ae9ee8eed2a0524192b864da4e22a92d3f\n--- a/docs/adr/0004-use-github-first-with-shiki-mirror.md\n+++ b/docs/adr/SADR-0004-use-github-first-with-shiki-mirror.md\n@@ -1,4 +1,4 @@\n-# ADR 0004: Use GitHub First With A Repository-Local Shiki Mirror\n+# SADR-0004: Use GitHub First With A Repository-Local Shiki Mirror\n \n ## Status\n \ndiff --git a/docs/adr/0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md b/docs/adr/SADR-0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\nindex b339baf49b89d9290e79c3d51b36e7b2bde3bd95..c30a0d8b1fde0224d0ac7820e7870dccf721580d\n--- a/docs/adr/0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\n+++ b/docs/adr/SADR-0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\n@@ -1,4 +1,4 @@\n-# ADR 0005: Use Codex As Front And Claude Code Action As The GitHub Runtime\n+# SADR-0005: Use Codex As Front And Claude Code Action As The GitHub Runtime\n \n ## Status\n \ndiff --git a/docs/adr/0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md b/docs/adr/SADR-0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\nindex 4bcae9d1e52f0a37b8e9fbafa5cd14b6ee1993bd..7a38538a6ae9929567f0ade67e7ea6a60d0b3f7e\n--- a/docs/adr/0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\n+++ b/docs/adr/SADR-0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\n@@ -1,4 +1,4 @@\n-# ADR 0006: Separate Codex Implementation, CCA Completion, And MergeGate Authorization\n+# SADR-0006: Separate Codex Implementation, CCA Completion, And MergeGate Authorization\n \n ## Status\n \ndiff --git a/docs/adr/0007-packaging-and-release.md b/docs/adr/SADR-0007-packaging-and-release.md\nindex 79b84ddb97167372b44641e5b7860e400ce80822..023cd57f8011aa6e6d2ebf612b7c35a7a82c00d4\n--- a/docs/adr/0007-packaging-and-release.md\n+++ b/docs/adr/SADR-0007-packaging-and-release.md\n@@ -1,4 +1,4 @@\n-# ADR 0007: Packaging And Release For Shiki\n+# SADR-0007: Packaging And Release For Shiki\n \n ## Status\n \ndiff --git a/docs/adr/0008-use-claude-code-as-default-implementer.md b/docs/adr/SADR-0008-use-claude-code-as-default-implementer.md\nindex dedf4510b7c7799521422f622029563d373db031..a158a8d444d93764ec515a28133f7dde4af23579\n--- a/docs/adr/0008-use-claude-code-as-default-implementer.md\n+++ b/docs/adr/SADR-0008-use-claude-code-as-default-implementer.md\n@@ -1,4 +1,4 @@\n-# ADR 0008: Use Claude Code As The Default Implementer Runtime\n+# SADR-0008: Use Claude Code As The Default Implementer Runtime\n \n ## Status\n \ndiff --git a/docs/adr/0009-requirements-definition-and-spec-freeze.md b/docs/adr/SADR-0009-requirements-definition-and-spec-freeze.md\nindex 29b3f7871bef9203a8b4a831a65c39eb2f506e42..52e7dd69ad0aa4efe0b68c62678abbf7df02aaec\n--- a/docs/adr/0009-requirements-definition-and-spec-freeze.md\n+++ b/docs/adr/SADR-0009-requirements-definition-and-spec-freeze.md\n@@ -1,4 +1,4 @@\n-# ADR 0009: Merge Grill And Planning Into Requirements Definition With Spec Freeze\n+# SADR-0009: Merge Grill And Planning Into Requirements Definition With Spec Freeze\n \n ## Status\n \ndiff --git a/docs/adr/0010-external-ai-guardian-review.md b/docs/adr/SADR-0010-external-ai-guardian-review.md\nindex e6108bbe8bb4e73e0ff4684c8f78001a80438c4e..e690d4eddd270e683d709c9b679c5859e71eeaaa\n--- a/docs/adr/0010-external-ai-guardian-review.md\n+++ b/docs/adr/SADR-0010-external-ai-guardian-review.md\n@@ -1,4 +1,4 @@\n-# ADR 0010: External AI Guardian Review As A First-Class Approval Authority\n+# SADR-0010: External AI Guardian Review As A First-Class Approval Authority\n \n ## Status\n \ndiff --git a/docs/adr/0011-autonomous-quality-gate-evidence.md b/docs/adr/SADR-0011-autonomous-quality-gate-evidence.md\nindex bca0246c5d46e44afbde15486a75eec425fc02f5..9c93eac16d8c3a6d4b11bd7224e3955102cd7db4\n--- a/docs/adr/0011-autonomous-quality-gate-evidence.md\n+++ b/docs/adr/SADR-0011-autonomous-quality-gate-evidence.md\n@@ -1,4 +1,4 @@\n-# ADR 0011: Autonomous Quality-Gate Evidence \u2014 Deterministic Facts And An Independent Verifier\n+# SADR-0011: Autonomous Quality-Gate Evidence \u2014 Deterministic Facts And An Independent Verifier\n \n ## Status\n \ndiff --git a/docs/adr/0012-autonomous-loop-closeout-to-main.md b/docs/adr/SADR-0012-autonomous-loop-closeout-to-main.md\nindex 637fad6dde4989c9c2bf0f81f676d8d29991123c..ec7d85f81030a676bcbba28b43eb365422a36eac\n--- a/docs/adr/0012-autonomous-loop-closeout-to-main.md\n+++ b/docs/adr/SADR-0012-autonomous-loop-closeout-to-main.md\n@@ -1,4 +1,4 @@\n-# 0012 \u2014 Autonomous loop pushes goal completion to main via a closeout PR\n+# SADR-0012 \u2014 Autonomous loop pushes goal completion to main via a closeout PR\n \n - Status: accepted\n - Date: 2026-06-18\ndiff --git a/docs/adr/0013-bootstrap-grants-actions-approve-pull-requests.md b/docs/adr/SADR-0013-bootstrap-grants-actions-approve-pull-requests.md\nindex b6c69d64f1c5ced346b88d6859586715b3b5bd0e..a793e7277d67ab8ea56286fec289ca689825a17d\n--- a/docs/adr/0013-bootstrap-grants-actions-approve-pull-requests.md\n+++ b/docs/adr/SADR-0013-bootstrap-grants-actions-approve-pull-requests.md\n@@ -1,4 +1,4 @@\n-# ADR 0013: Bootstrap Grants GitHub Actions Permission To Create And Approve Pull Requests\n+# SADR-0013: Bootstrap Grants GitHub Actions Permission To Create And Approve Pull Requests\n \n ## Status\n \ndiff --git a/docs/adr/0014-codex-app-external-ai-guardian-ui-adapter.md b/docs/adr/SADR-0014-codex-app-external-ai-guardian-ui-adapter.md\nindex 238db06279334caa341c117362df9271382a7fcd..65f12e9939321e1b3e389a34c822fc1549e5155d\n--- a/docs/adr/0014-codex-app-external-ai-guardian-ui-adapter.md\n+++ b/docs/adr/SADR-0014-codex-app-external-ai-guardian-ui-adapter.md\n@@ -1,4 +1,4 @@\n-# ADR 0014: Use Codex App As The External AI Guardian UI Adapter\n+# SADR-0014: Use Codex App As The External AI Guardian UI Adapter\n \n ## Status\n \ndiff --git a/docs/adr/0015-contract-approval-before-dispatch.md b/docs/adr/SADR-0015-contract-approval-before-dispatch.md\nindex fad6c656741a20a7684ba16080a7db159f63a52f..7bdd760bb3e82eee4e1410f45c783b03b638d456\n--- a/docs/adr/0015-contract-approval-before-dispatch.md\n+++ b/docs/adr/SADR-0015-contract-approval-before-dispatch.md\n@@ -1,4 +1,4 @@\n-# ADR 0015: Approve The Task Contract Before Dispatch, Not The Diff After It\n+# SADR-0015: Approve The Task Contract Before Dispatch, Not The Diff After It\n \n ## Status\n \ndiff --git a/docs/adr/0016-locks-bound-product-paths.md b/docs/adr/SADR-0016-locks-bound-product-paths.md\nindex b5e850f3f1f7f4ef6e7f9d1763a29a51bd300c45..332cc17313498404f41a145291e38447b5de0dc4\n--- a/docs/adr/0016-locks-bound-product-paths.md\n+++ b/docs/adr/SADR-0016-locks-bound-product-paths.md\n@@ -1,4 +1,4 @@\n-# ADR 0016: Locks bound product paths; the Shiki mirror is bound by record identity\n+# SADR-0016: Locks bound product paths; the Shiki mirror is bound by record identity\n \n ## Status\n \ndiff --git a/docs/adr/0017-bookkeeping-closeouts-do-not-inherit-risk.md b/docs/adr/SADR-0017-bookkeeping-closeouts-do-not-inherit-risk.md\nindex 554e3a0d526a72ca067108c5464cc68c3edcf174..83115f69f1b1b48d544c9b0157f377b91cb53abf\n--- a/docs/adr/0017-bookkeeping-closeouts-do-not-inherit-risk.md\n+++ b/docs/adr/SADR-0017-bookkeeping-closeouts-do-not-inherit-risk.md\n@@ -1,4 +1,4 @@\n-# ADR 0017: A Bookkeeping Closeout Does Not Inherit Its Task's Risk\n+# SADR-0017: A Bookkeeping Closeout Does Not Inherit Its Task's Risk\n \n ## Status\n \ndiff --git a/docs/adr/0018-carried-guardian-approval-across-a-proven-base-sync.md b/docs/adr/SADR-0018-carried-guardian-approval-across-a-proven-base-sync.md\nindex 22358562da390ed8b45cd600da08a43d378b4680..38da0fd95102c3496c3219ce2319fce696a2532a\n--- a/docs/adr/0018-carried-guardian-approval-across-a-proven-base-sync.md\n+++ b/docs/adr/SADR-0018-carried-guardian-approval-across-a-proven-base-sync.md\n@@ -1,4 +1,4 @@\n-# ADR 0018: Carried Guardian Approval Across A Proven Base Sync\n+# SADR-0018: Carried Guardian Approval Across A Proven Base Sync\n \n ## Status\n \ndiff --git a/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md b/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\nnew file mode 100644\nindex 0000000000000000000000000000000000000000..7455ce1f3bed02ddddb851ac21a1d1a648c9aeba\n--- /dev/null\n+++ b/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\n@@ -0,0 +1,55 @@\n+# SADR-0019: Separate Shiki Platform SADRs From Target ADRs\n+\n+## Status\n+\n+Accepted\n+\n+## Context\n+\n+Shiki installs its platform decision records into `docs/adr/` in every target\n+repository. Shiki and its targets previously used the same numeric\n+`NNNN-<slug>.md` namespace. A target that already owned ADRs 0013 through 0018\n+therefore acquired two records for each number during installation, making an\n+`ADR NNNN` citation ambiguous. Renumbering target decisions would rewrite target\n+history, while renumbering Shiki decisions per target would destroy their stable\n+platform identity.\n+\n+Upgrades also need to remove the former Shiki-owned numeric paths without ever\n+mistaking a target-owned ADR for platform content. The install stamp already\n+records exact shipped paths and content digests, so it can prove ownership only\n+when both still match.\n+\n+## Decision\n+\n+Shiki platform decisions use the `SADR` namespace:\n+\n+- records are named `docs/adr/SADR-NNNN-<slug>.md`;\n+- live platform citations use `SADR-NNNN`;\n+- target product decisions retain `docs/adr/NNNN-<slug>.md` and `ADR NNNN`;\n+- the reference validator resolves the two namespaces independently, so the same\n+  four-digit number may exist once in each namespace.\n+\n+Before a forced upgrade writes anything, the installer inventories the exact\n+former numeric Shiki ADR paths. It may delete a legacy path only when the\n+existing install stamp contains that exact path and its recorded SHA-256 digest\n+matches the file currently on disk. All ownership blockers are reported\n+together and abort the upgrade before its first write. A non-forced install\n+never performs this cleanup.\n+\n+Historical `.shiki/` evidence remains unchanged because it records the namespace\n+that existed when that evidence was produced.\n+\n+## Consequences\n+\n+- Targets may use any ADR number without colliding with Shiki's platform SADRs.\n+- Fresh installs preserve target ADR bytes and install a stable SADR set.\n+- Forced upgrades fail closed when legacy ownership cannot be proven, and a\n+  successful upgrade refreshes the stamp without removed legacy paths.\n+- Current governance, packaging, workflow, prompt, script, and skill references\n+  must move with the platform records.\n+\n+## Alternatives Considered\n+\n+- Collision-only refusal leaves affected targets permanently unupgradable.\n+- Renumbering target ADRs mutates target-owned history.\n+- Per-target Shiki renumbering makes platform citations non-portable.\ndiff --git a/tests/test_adr_references.py b/tests/test_adr_references.py\nindex 51706329c797d5dd9b47e082a333fa83b261d482..92569d9fa25e22cfd71c17561d715485027a0d2d\n--- a/tests/test_adr_references.py\n+++ b/tests/test_adr_references.py\n@@ -31,17 +31,18 @@\n \n REPO_ROOT = Path(__file__).resolve().parents[1]\n \n-# \"ADR 0016\" -- a four-digit citation. The trailing \\b keeps a five-digit token\n-# from being read as a truncated four-digit number.\n-_CITATION = re.compile(r\"\\bADR (\\d{4})\\b\")\n-# \"0016-locks-bound-product-paths.md\" -- the leading four digits name the record.\n-_ADR_FILENAME = re.compile(r\"^(\\d{4})-.*\\.md$\")\n+# ``ADR 0016`` / ``SADR-0016`` -- a namespace plus four-digit citation. The\n+# trailing ``\\b`` keeps a five-digit token from being read as a truncated one.\n+_CITATION = re.compile(r\"\\b(S?ADR)[ -](\\d{4})\\b\")\n+# Target ADRs retain the numeric filename; Shiki platform decisions use SADR.\n+_DECISION_FILENAME = re.compile(r\"^(?:(SADR)-)?(\\d{4})-.*\\.md$\")\n \n \n class Violation(NamedTuple):\n     \"\"\"A citation that does not resolve to exactly one decision record.\"\"\"\n \n     citing_file: str  # repo-relative path of the file that cites the ADR\n+    namespace: str  # \"ADR\" for target decisions; \"SADR\" for Shiki decisions\n     number: str  # the cited four-digit number, e.g. \"0016\"\n     line: int  # 1-based line of the citation\n     reason: str  # \"missing\" (zero matches) or \"ambiguous\" (two or more)\n@@ -49,32 +50,34 @@\n \n     @property\n     def expected_path(self) -> str:\n-        return f\"docs/adr/{self.number}-*.md\"\n+        prefix = \"SADR-\" if self.namespace == \"SADR\" else \"\"\n+        return f\"docs/adr/{prefix}{self.number}-*.md\"\n \n     def describe(self) -> str:\n         \"\"\"Name the citing file, the cited number, and the missing path.\"\"\"\n         if self.reason == \"missing\":\n             return (\n-                f\"{self.citing_file}:{self.line} cites ADR {self.number} \"\n+                f\"{self.citing_file}:{self.line} cites {self.namespace} {self.number} \"\n                 f\"but no document matches {self.expected_path}\"\n             )\n         joined = \", \".join(self.matches)\n         return (\n-            f\"{self.citing_file}:{self.line} cites ADR {self.number} which \"\n+            f\"{self.citing_file}:{self.line} cites {self.namespace} {self.number} which \"\n             f\"resolves to {len(self.matches)} documents (expected exactly one \"\n             f\"at {self.expected_path}): {joined}\"\n         )\n \n \n-def _adr_index(root: Path) -> dict[str, list[Path]]:\n-    \"\"\"Map every four-digit ADR number to the record files that claim it.\"\"\"\n-    index: dict[str, list[Path]] = {}\n+def _adr_index(root: Path) -> dict[tuple[str, str], list[Path]]:\n+    \"\"\"Map each decision namespace and number to the files that claim it.\"\"\"\n+    index: dict[tuple[str, str], list[Path]] = {}\n     adr_dir = root / \"docs\" / \"adr\"\n     if adr_dir.is_dir():\n         for path in sorted(adr_dir.glob(\"*.md\")):\n-            match = _ADR_FILENAME.match(path.name)\n+            match = _DECISION_FILENAME.match(path.name)\n             if match:\n-                index.setdefault(match.group(1), []).append(path)\n+                namespace = \"SADR\" if match.group(1) else \"ADR\"\n+                index.setdefault((namespace, match.group(2)), []).append(path)\n     return index\n \n \n@@ -110,13 +113,15 @@\n         rel = path.relative_to(root).as_posix()\n         for lineno, line in enumerate(text.splitlines(), start=1):\n             for match in _CITATION.finditer(line):\n-                number = match.group(1)\n-                found = index.get(number, [])\n+                namespace, number = match.groups()\n+                found = index.get((namespace, number), [])\n                 if len(found) == 1:\n                     continue\n                 reason = \"missing\" if not found else \"ambiguous\"\n                 rels = [p.relative_to(root).as_posix() for p in found]\n-                violations.append(Violation(rel, number, lineno, reason, rels))\n+                violations.append(\n+                    Violation(rel, namespace, number, lineno, reason, rels)\n+                )\n     return violations\n \n \n@@ -146,8 +151,8 @@\n         # The two records this Goal carried onto the default branch, and which\n         # the scripts cite most, must be present.\n         index = _adr_index(REPO_ROOT)\n-        self.assertIn(\"0016\", index)\n-        self.assertIn(\"0017\", index)\n+        self.assertIn((\"ADR\", \"0016\"), index)\n+        self.assertIn((\"ADR\", \"0017\"), index)\n \n \n class FixtureAdrReferences(unittest.TestCase):\n@@ -180,6 +185,17 @@\n         self.assertIn(\"9991\", message)  # cited number\n         self.assertIn(\"docs/adr/9991-*.md\", message)  # missing path\n \n+    def test_target_adr_does_not_satisfy_missing_platform_sadr(self) -> None:\n+        root = self._make_repo(\n+            adr_names=[\"0042-target-decision.md\"],\n+            script_body='\"\"\"Target ADR 0042 exists, but platform SADR-0042 does not.\"\"\"\\n',\n+        )\n+        violations = find_adr_reference_violations(root, include_docs=False)\n+        self.assertEqual(len(violations), 1)\n+        self.assertEqual(violations[0].namespace, \"SADR\")\n+        self.assertEqual(violations[0].number, \"0042\")\n+        self.assertEqual(violations[0].expected_path, \"docs/adr/SADR-0042-*.md\")\n+\n     def test_uncited_adr_is_allowed(self) -> None:\n         # An ADR exists but nothing cites it -> not a violation. The converse\n         # (every ADR must be cited) is explicitly NOT required.\ndiff --git a/tests/test_sadr_install_migration.py b/tests/test_sadr_install_migration.py\nnew file mode 100644\nindex 0000000000000000000000000000000000000000..a6398c81377f00c09aa2cb684980b070cfaf714c\n--- /dev/null\n+++ b/tests/test_sadr_install_migration.py\n@@ -0,0 +1,56 @@\n+\"\"\"SADR installation and collision-safe legacy upgrade behavior.\"\"\"\n+\n+from __future__ import annotations\n+\n+import shutil\n+import tempfile\n+import unittest\n+from pathlib import Path\n+\n+import shiki_test_support  # noqa: F401  (path bootstrap)\n+\n+import shiki_installer\n+from tests.test_adr_references import find_adr_reference_violations\n+\n+\n+PRODUCT_NUMBERS = tuple(range(13, 19))\n+\n+\n+class FreshSadrInstallTests(unittest.TestCase):\n+    def setUp(self) -> None:\n+        self.tmp = Path(tempfile.mkdtemp(prefix=\"shiki-sadr-fresh-\"))\n+        self.addCleanup(lambda: shutil.rmtree(self.tmp, ignore_errors=True))\n+        self.target = self.tmp / \"target\"\n+        self.adr_dir = self.target / \"docs\" / \"adr\"\n+        self.adr_dir.mkdir(parents=True)\n+        self.product_bytes = {\n+            f\"{number:04d}-product-decision.md\": (\n+                f\"# Product ADR {number:04d}\\n\\nTarget-owned bytes {number}.\\n\"\n+            ).encode()\n+            for number in PRODUCT_NUMBERS\n+        }\n+        for name, content in self.product_bytes.items():\n+            (self.adr_dir / name).write_bytes(content)\n+\n+    def test_fresh_install_preserves_product_adrs_and_installs_all_sadrs(self) -> None:\n+        shiki_installer.install_template(self.target, force=False, validate=True)\n+\n+        for name, expected in self.product_bytes.items():\n+            self.assertEqual((self.adr_dir / name).read_bytes(), expected)\n+\n+        sadr_numbers = {\n+            path.name.split(\"-\", 2)[1]\n+            for path in self.adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\")\n+        }\n+        self.assertEqual(sadr_numbers, {f\"{number:04d}\" for number in range(1, 20)})\n+\n+        numeric_records = {\n+            path.name\n+            for path in self.adr_dir.glob(\"[0-9][0-9][0-9][0-9]-*.md\")\n+        }\n+        self.assertEqual(numeric_records, set(self.product_bytes))\n+        self.assertEqual(find_adr_reference_violations(self.target), [])\n+\n+\n+if __name__ == \"__main__\":\n+    unittest.main()\n\n2026-08-07T08:08:34.041805Z ERROR codex_models_manager::manager: failed to renew cache TTL: missing field `base_instructions` at line 94 column 5\nexec\n/bin/zsh -lc \"rg -n --glob '\"'!tests/test_adr_references.py'\"' --glob '\"'!tests/test_sadr_install_migration.py'\"' --glob '\"'!.shiki/**'\"' --glob '\"'!skills/**'\"' '\\\\bADR[- ](000[1-9]|001[0-8])\\\\b|docs/adr/(000[1-9]|001[0-8])-|\\\\(adr/(000[1-9]|001[0-8])-' AGENTS.md CLAUDE.md CONTEXT.md CONTRIBUTING.md SYSTEM_PROMPT.md pyproject.toml docs scripts tests .github .codex\" in /private/tmp/shiki-adr-collision.pa0KwY/.worktrees/shiki-t-20260807t073307221254z-2d22d654-introduc\n succeeded in 0ms:\n.codex/skills/shiki/SKILL.md:72:- Claude Code implements and repairs by default (`shiki runner claude`, ADR 0008); Codex implements and repairs tasks explicitly assigned to `codex`.\n\ndiff --git a/docs/adr/0001-keep-shiki-as-platform-name.md b/docs/adr/SADR-0001-keep-shiki-as-platform-name.md\nindex 20a9637c3b9d8c97de7ba8db7cfae4d82d9a7dba..8b685439959aada1b221c112f0ac3c51ca8a1695\n--- a/docs/adr/0001-keep-shiki-as-platform-name.md\n+++ b/docs/adr/SADR-0001-keep-shiki-as-platform-name.md\n@@ -1,3 +1,3 @@\n-# Keep Shiki As The Platform Name\n+# SADR-0001: Keep Shiki As The Platform Name\n \n Shiki remains the product name for the GitHub-native agentic engineering control plane. We considered more descriptive names such as MergePilot, MergeGate, and Kiroku, but kept Shiki because it carries the existing goal-seeking framework identity while remaining independent of Hermes, Codex, Claude Code, or any single runtime.\ndiff --git a/docs/adr/0002-use-agents-as-shared-constitution.md b/docs/adr/SADR-0002-use-agents-as-shared-constitution.md\nindex 73de30ecb078120eac540d9bc51855e3cd48d0e3..c5486182bf06fca36ea508496b013c67302fd989\n--- a/docs/adr/0002-use-agents-as-shared-constitution.md\n+++ b/docs/adr/SADR-0002-use-agents-as-shared-constitution.md\n@@ -1,3 +1,3 @@\n-# Use AGENTS As The Shared Agent Constitution\n+# SADR-0002: Use AGENTS As The Shared Agent Constitution\n \n Shiki uses `AGENTS.md` as the runtime-neutral constitution and `CLAUDE.md` as a thin Claude Code wrapper. This keeps Codex, Claude Code, Hermes Runner, and future Agent Runtimes aligned on the same GitHub-first MergeGate rules while still allowing runtime-specific instructions where needed.\ndiff --git a/docs/adr/0003-require-skill-gate-for-mergegate-readiness.md b/docs/adr/SADR-0003-require-skill-gate-for-mergegate-readiness.md\nindex 424be0c9af4c8f0136f1fde68a468c47ff84e764..7fc337987c7df9792f75f181dfcbed75fe5ffff2\n--- a/docs/adr/0003-require-skill-gate-for-mergegate-readiness.md\n+++ b/docs/adr/SADR-0003-require-skill-gate-for-mergegate-readiness.md\n@@ -1,3 +1,3 @@\n-# Require Skill Gate For MergeGate Readiness\n+# SADR-0003: Require Skill Gate For MergeGate Readiness\n \n Shiki requires relevant engineering skills to be invoked before execution when their triggers apply. This makes skills part of MergeGate readiness rather than optional prompt style, because Shiki depends on repeatable planning, issue decomposition, TDD, diagnosis, triage, and architecture review before autonomous or parallel agent work can be trusted.\ndiff --git a/docs/adr/0004-use-github-first-with-shiki-mirror.md b/docs/adr/SADR-0004-use-github-first-with-shiki-mirror.md\nindex d0a3a9174dc646a945a9d325954ffaf00c352040..a79c54ae9ee8eed2a0524192b864da4e22a92d3f\n--- a/docs/adr/0004-use-github-first-with-shiki-mirror.md\n+++ b/docs/adr/SADR-0004-use-github-first-with-shiki-mirror.md\n@@ -1,4 +1,4 @@\n-# ADR 0004: Use GitHub First With A Repository-Local Shiki Mirror\n+# SADR-0004: Use GitHub First With A Repository-Local Shiki Mirror\n \n ## Status\n \ndiff --git a/docs/adr/0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md b/docs/adr/SADR-0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\nindex b339baf49b89d9290e79c3d51b36e7b2bde3bd95..c30a0d8b1fde0224d0ac7820e7870dccf721580d\n--- a/docs/adr/0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\n+++ b/docs/adr/SADR-0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\n@@ -1,4 +1,4 @@\n-# ADR 0005: Use Codex As Front And Claude Code Action As The GitHub Runtime\n+# SADR-0005: Use Codex As Front And Claude Code Action As The GitHub Runtime\n \n ## Status\n \ndiff --git a/docs/adr/0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md b/docs/adr/SADR-0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\nindex 4bcae9d1e52f0a37b8e9fbafa5cd14b6ee1993bd..7a38538a6ae9929567f0ade67e7ea6a60d0b3f7e\n--- a/docs/adr/0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\n+++ b/docs/adr/SADR-0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\n@@ -1,4 +1,4 @@\n-# ADR 0006: Separate Codex Implementation, CCA Completion, And MergeGate Authorization\n+# SADR-0006: Separate Codex Implementation, CCA Completion, And MergeGate Authorization\n \n ## Status\n \ndiff --git a/docs/adr/0007-packaging-and-release.md b/docs/adr/SADR-0007-packaging-and-release.md\nindex 79b84ddb97167372b44641e5b7860e400ce80822..023cd57f8011aa6e6d2ebf612b7c35a7a82c00d4\n--- a/docs/adr/0007-packaging-and-release.md\n+++ b/docs/adr/SADR-0007-packaging-and-release.md\n@@ -1,4 +1,4 @@\n-# ADR 0007: Packaging And Release For Shiki\n+# SADR-0007: Packaging And Release For Shiki\n \n ## Status\n \ndiff --git a/docs/adr/0008-use-claude-code-as-default-implementer.md b/docs/adr/SADR-0008-use-claude-code-as-default-implementer.md\nindex dedf4510b7c7799521422f622029563d373db031..a158a8d444d93764ec515a28133f7dde4af23579\n--- a/docs/adr/0008-use-claude-code-as-default-implementer.md\n+++ b/docs/adr/SADR-0008-use-claude-code-as-default-implementer.md\n@@ -1,4 +1,4 @@\n-# ADR 0008: Use Claude Code As The Default Implementer Runtime\n+# SADR-0008: Use Claude Code As The Default Implementer Runtime\n \n ## Status\n \ndiff --git a/docs/adr/0009-requirements-definition-and-spec-freeze.md b/docs/adr/SADR-0009-requirements-definition-and-spec-freeze.md\nindex 29b3f7871bef9203a8b4a831a65c39eb2f506e42..52e7dd69ad0aa4efe0b68c62678abbf7df02aaec\n--- a/docs/adr/0009-requirements-definition-and-spec-freeze.md\n+++ b/docs/adr/SADR-0009-requirements-definition-and-spec-freeze.md\n@@ -1,4 +1,4 @@\n-# ADR 0009: Merge Grill And Planning Into Requirements Definition With Spec Freeze\n+# SADR-0009: Merge Grill And Planning Into Requirements Definition With Spec Freeze\n \n ## Status\n \ndiff --git a/docs/adr/0010-external-ai-guardian-review.md b/docs/adr/SADR-0010-external-ai-guardian-review.md\nindex e6108bbe8bb4e73e0ff4684c8f78001a80438c4e..e690d4eddd270e683d709c9b679c5859e71eeaaa\n--- a/docs/adr/0010-external-ai-guardian-review.md\n+++ b/docs/adr/SADR-0010-external-ai-guardian-review.md\n@@ -1,4 +1,4 @@\n-# ADR 0010: External AI Guardian Review As A First-Class Approval Authority\n+# SADR-0010: External AI Guardian Review As A First-Class Approval Authority\n \n ## Status\n \ndiff --git a/docs/adr/0011-autonomous-quality-gate-evidence.md b/docs/adr/SADR-0011-autonomous-quality-gate-evidence.md\nindex bca0246c5d46e44afbde15486a75eec425fc02f5..9c93eac16d8c3a6d4b11bd7224e3955102cd7db4\n--- a/docs/adr/0011-autonomous-quality-gate-evidence.md\n+++ b/docs/adr/SADR-0011-autonomous-quality-gate-evidence.md\n@@ -1,4 +1,4 @@\n-# ADR 0011: Autonomous Quality-Gate Evidence \u2014 Deterministic Facts And An Independent Verifier\n+# SADR-0011: Autonomous Quality-Gate Evidence \u2014 Deterministic Facts And An Independent Verifier\n \n ## Status\n \ndiff --git a/docs/adr/0012-autonomous-loop-closeout-to-main.md b/docs/adr/SADR-0012-autonomous-loop-closeout-to-main.md\nindex 637fad6dde4989c9c2bf0f81f676d8d29991123c..ec7d85f81030a676bcbba28b43eb365422a36eac\n--- a/docs/adr/0012-autonomous-loop-closeout-to-main.md\n+++ b/docs/adr/SADR-0012-autonomous-loop-closeout-to-main.md\n@@ -1,4 +1,4 @@\n-# 0012 \u2014 Autonomous loop pushes goal completion to main via a closeout PR\n+# SADR-0012 \u2014 Autonomous loop pushes goal completion to main via a closeout PR\n \n - Status: accepted\n - Date: 2026-06-18\ndiff --git a/docs/adr/0013-bootstrap-grants-actions-approve-pull-requests.md b/docs/adr/SADR-0013-bootstrap-grants-actions-approve-pull-requests.md\nindex b6c69d64f1c5ced346b88d6859586715b3b5bd0e..a793e7277d67ab8ea56286fec289ca689825a17d\n--- a/docs/adr/0013-bootstrap-grants-actions-approve-pull-requests.md\n+++ b/docs/adr/SADR-0013-bootstrap-grants-actions-approve-pull-requests.md\n@@ -1,4 +1,4 @@\n-# ADR 0013: Bootstrap Grants GitHub Actions Permission To Create And Approve Pull Requests\n+# SADR-0013: Bootstrap Grants GitHub Actions Permission To Create And Approve Pull Requests\n \n ## Status\n \ndiff --git a/docs/adr/0014-codex-app-external-ai-guardian-ui-adapter.md b/docs/adr/SADR-0014-codex-app-external-ai-guardian-ui-adapter.md\nindex 238db06279334caa341c117362df9271382a7fcd..65f12e9939321e1b3e389a34c822fc1549e5155d\n--- a/docs/adr/0014-codex-app-external-ai-guardian-ui-adapter.md\n+++ b/docs/adr/SADR-0014-codex-app-external-ai-guardian-ui-adapter.md\n@@ -1,4 +1,4 @@\n-# ADR 0014: Use Codex App As The External AI Guardian UI Adapter\n+# SADR-0014: Use Codex App As The External AI Guardian UI Adapter\n \n ## Status\n \ndiff --git a/docs/adr/0015-contract-approval-before-dispatch.md b/docs/adr/SADR-0015-contract-approval-before-dispatch.md\nindex fad6c656741a20a7684ba16080a7db159f63a52f..7bdd760bb3e82eee4e1410f45c783b03b638d456\n--- a/docs/adr/0015-contract-approval-before-dispatch.md\n+++ b/docs/adr/SADR-0015-contract-approval-before-dispatch.md\n@@ -1,4 +1,4 @@\n-# ADR 0015: Approve The Task Contract Before Dispatch, Not The Diff After It\n+# SADR-0015: Approve The Task Contract Before Dispatch, Not The Diff After It\n \n ## Status\n \ndiff --git a/docs/adr/0016-locks-bound-product-paths.md b/docs/adr/SADR-0016-locks-bound-product-paths.md\nindex b5e850f3f1f7f4ef6e7f9d1763a29a51bd300c45..332cc17313498404f41a145291e38447b5de0dc4\n--- a/docs/adr/0016-locks-bound-product-paths.md\n+++ b/docs/adr/SADR-0016-locks-bound-product-paths.md\n@@ -1,4 +1,4 @@\n-# ADR 0016: Locks bound product paths; the Shiki mirror is bound by record identity\n+# SADR-0016: Locks bound product paths; the Shiki mirror is bound by record identity\n \n ## Status\n \ndiff --git a/docs/adr/0017-bookkeeping-closeouts-do-not-inherit-risk.md b/docs/adr/SADR-0017-bookkeeping-closeouts-do-not-inherit-risk.md\nindex 554e3a0d526a72ca067108c5464cc68c3edcf174..83115f69f1b1b48d544c9b0157f377b91cb53abf\n--- a/docs/adr/0017-bookkeeping-closeouts-do-not-inherit-risk.md\n+++ b/docs/adr/SADR-0017-bookkeeping-closeouts-do-not-inherit-risk.md\n@@ -1,4 +1,4 @@\n-# ADR 0017: A Bookkeeping Closeout Does Not Inherit Its Task's Risk\n+# SADR-0017: A Bookkeeping Closeout Does Not Inherit Its Task's Risk\n \n ## Status\n \ndiff --git a/docs/adr/0018-carried-guardian-approval-across-a-proven-base-sync.md b/docs/adr/SADR-0018-carried-guardian-approval-across-a-proven-base-sync.md\nindex 22358562da390ed8b45cd600da08a43d378b4680..38da0fd95102c3496c3219ce2319fce696a2532a\n--- a/docs/adr/0018-carried-guardian-approval-across-a-proven-base-sync.md\n+++ b/docs/adr/SADR-0018-carried-guardian-approval-across-a-proven-base-sync.md\n@@ -1,4 +1,4 @@\n-# ADR 0018: Carried Guardian Approval Across A Proven Base Sync\n+# SADR-0018: Carried Guardian Approval Across A Proven Base Sync\n \n ## Status\n \ndiff --git a/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md b/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\nnew file mode 100644\nindex 0000000000000000000000000000000000000000..7455ce1f3bed02ddddb851ac21a1d1a648c9aeba\n--- /dev/null\n+++ b/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\n@@ -0,0 +1,55 @@\n+# SADR-0019: Separate Shiki Platform SADRs From Target ADRs\n+\n+## Status\n+\n+Accepted\n+\n+## Context\n+\n+Shiki installs its platform decision records into `docs/adr/` in every target\n+repository. Shiki and its targets previously used the same numeric\n+`NNNN-<slug>.md` namespace. A target that already owned ADRs 0013 through 0018\n+therefore acquired two records for each number during installation, making an\n+`ADR NNNN` citation ambiguous. Renumbering target decisions would rewrite target\n+history, while renumbering Shiki decisions per target would destroy their stable\n+platform identity.\n+\n+Upgrades also need to remove the former Shiki-owned numeric paths without ever\n+mistaking a target-owned ADR for platform content. The install stamp already\n+records exact shipped paths and content digests, so it can prove ownership only\n+when both still match.\n+\n+## Decision\n+\n+Shiki platform decisions use the `SADR` namespace:\n+\n+- records are named `docs/adr/SADR-NNNN-<slug>.md`;\n+- live platform citations use `SADR-NNNN`;\n+- target product decisions retain `docs/adr/NNNN-<slug>.md` and `ADR NNNN`;\n+- the reference validator resolves the two namespaces independently, so the same\n+  four-digit number may exist once in each namespace.\n+\n+Before a forced upgrade writes anything, the installer inventories the exact\n+former numeric Shiki ADR paths. It may delete a legacy path only when the\n+existing install stamp contains that exact path and its recorded SHA-256 digest\n+matches the file currently on disk. All ownership blockers are reported\n+together and abort the upgrade before its first write. A non-forced install\n+never performs this cleanup.\n+\n+Historical `.shiki/` evidence remains unchanged because it records the namespace\n+that existed when that evidence was produced.\n+\n+## Consequences\n+\n+- Targets may use any ADR number without colliding with Shiki's platform SADRs.\n+- Fresh installs preserve target ADR bytes and install a stable SADR set.\n+- Forced upgrades fail closed when legacy ownership cannot be proven, and a\n+  successful upgrade refreshes the stamp without removed legacy paths.\n+- Current governance, packaging, workflow, prompt, script, and skill references\n+  must move with the platform records.\n+\n+## Alternatives Considered\n+\n+- Collision-only refusal leaves affected targets permanently unupgradable.\n+- Renumbering target ADRs mutates target-owned history.\n+- Per-target Shiki renumbering makes platform citations non-portable.\ndiff --git a/tests/test_adr_references.py b/tests/test_adr_references.py\nindex 51706329c797d5dd9b47e082a333fa83b261d482..92569d9fa25e22cfd71c17561d715485027a0d2d\n--- a/tests/test_adr_references.py\n+++ b/tests/test_adr_references.py\n@@ -31,17 +31,18 @@\n \n REPO_ROOT = Path(__file__).resolve().parents[1]\n \n-# \"ADR 0016\" -- a four-digit citation. The trailing \\b keeps a five-digit token\n-# from being read as a truncated four-digit number.\n-_CITATION = re.compile(r\"\\bADR (\\d{4})\\b\")\n-# \"0016-locks-bound-product-paths.md\" -- the leading four digits name the record.\n-_ADR_FILENAME = re.compile(r\"^(\\d{4})-.*\\.md$\")\n+# ``ADR 0016`` / ``SADR-0016`` -- a namespace plus four-digit citation. The\n+# trailing ``\\b`` keeps a five-digit token from being read as a truncated one.\n+_CITATION = re.compile(r\"\\b(S?ADR)[ -](\\d{4})\\b\")\n+# Target ADRs retain the numeric filename; Shiki platform decisions use SADR.\n+_DECISION_FILENAME = re.compile(r\"^(?:(SADR)-)?(\\d{4})-.*\\.md$\")\n \n \n class Violation(NamedTuple):\n     \"\"\"A citation that does not resolve to exactly one decision record.\"\"\"\n \n     citing_file: str  # repo-relative path of the file that cites the ADR\n+    namespace: str  # \"ADR\" for target decisions; \"SADR\" for Shiki decisions\n     number: str  # the cited four-digit number, e.g. \"0016\"\n     line: int  # 1-based line of the citation\n     reason: str  # \"missing\" (zero matches) or \"ambiguous\" (two or more)\n@@ -49,32 +50,34 @@\n \n     @property\n     def expected_path(self) -> str:\n-        return f\"docs/adr/{self.number}-*.md\"\n+        prefix = \"SADR-\" if self.namespace == \"SADR\" else \"\"\n+        return f\"docs/adr/{prefix}{self.number}-*.md\"\n \n     def describe(self) -> str:\n         \"\"\"Name the citing file, the cited number, and the missing path.\"\"\"\n         if self.reason == \"missing\":\n             return (\n-                f\"{self.citing_file}:{self.line} cites ADR {self.number} \"\n+                f\"{self.citing_file}:{self.line} cites {self.namespace} {self.number} \"\n                 f\"but no document matches {self.expected_path}\"\n             )\n         joined = \", \".join(self.matches)\n         return (\n-            f\"{self.citing_file}:{self.line} cites ADR {self.number} which \"\n+            f\"{self.citing_file}:{self.line} cites {self.namespace} {self.number} which \"\n             f\"resolves to {len(self.matches)} documents (expected exactly one \"\n             f\"at {self.expected_path}): {joined}\"\n         )\n \n \n-def _adr_index(root: Path) -> dict[str, list[Path]]:\n-    \"\"\"Map every four-digit ADR number to the record files that claim it.\"\"\"\n-    index: dict[str, list[Path]] = {}\n+def _adr_index(root: Path) -> dict[tuple[str, str], list[Path]]:\n+    \"\"\"Map each decision namespace and number to the files that claim it.\"\"\"\n+    index: dict[tuple[str, str], list[Path]] = {}\n     adr_dir = root / \"docs\" / \"adr\"\n     if adr_dir.is_dir():\n         for path in sorted(adr_dir.glob(\"*.md\")):\n-            match = _ADR_FILENAME.match(path.name)\n+            match = _DECISION_FILENAME.match(path.name)\n             if match:\n-                index.setdefault(match.group(1), []).append(path)\n+                namespace = \"SADR\" if match.group(1) else \"ADR\"\n+                index.setdefault((namespace, match.group(2)), []).append(path)\n     return index\n \n \n@@ -110,13 +113,15 @@\n         rel = path.relative_to(root).as_posix()\n         for lineno, line in enumerate(text.splitlines(), start=1):\n             for match in _CITATION.finditer(line):\n-                number = match.group(1)\n-                found = index.get(number, [])\n+                namespace, number = match.groups()\n+                found = index.get((namespace, number), [])\n                 if len(found) == 1:\n                     continue\n                 reason = \"missing\" if not found else \"ambiguous\"\n                 rels = [p.relative_to(root).as_posix() for p in found]\n-                violations.append(Violation(rel, number, lineno, reason, rels))\n+                violations.append(\n+                    Violation(rel, namespace, number, lineno, reason, rels)\n+                )\n     return violations\n \n \n@@ -146,8 +151,8 @@\n         # The two records this Goal carried onto the default branch, and which\n         # the scripts cite most, must be present.\n         index = _adr_index(REPO_ROOT)\n-        self.assertIn(\"0016\", index)\n-        self.assertIn(\"0017\", index)\n+        self.assertIn((\"ADR\", \"0016\"), index)\n+        self.assertIn((\"ADR\", \"0017\"), index)\n \n \n class FixtureAdrReferences(unittest.TestCase):\n@@ -180,6 +185,17 @@\n         self.assertIn(\"9991\", message)  # cited number\n         self.assertIn(\"docs/adr/9991-*.md\", message)  # missing path\n \n+    def test_target_adr_does_not_satisfy_missing_platform_sadr(self) -> None:\n+        root = self._make_repo(\n+            adr_names=[\"0042-target-decision.md\"],\n+            script_body='\"\"\"Target ADR 0042 exists, but platform SADR-0042 does not.\"\"\"\\n',\n+        )\n+        violations = find_adr_reference_violations(root, include_docs=False)\n+        self.assertEqual(len(violations), 1)\n+        self.assertEqual(violations[0].namespace, \"SADR\")\n+        self.assertEqual(violations[0].number, \"0042\")\n+        self.assertEqual(violations[0].expected_path, \"docs/adr/SADR-0042-*.md\")\n+\n     def test_uncited_adr_is_allowed(self) -> None:\n         # An ADR exists but nothing cites it -> not a violation. The converse\n         # (every ADR must be cited) is explicitly NOT required.\ndiff --git a/tests/test_sadr_install_migration.py b/tests/test_sadr_install_migration.py\nnew file mode 100644\nindex 0000000000000000000000000000000000000000..a6398c81377f00c09aa2cb684980b070cfaf714c\n--- /dev/null\n+++ b/tests/test_sadr_install_migration.py\n@@ -0,0 +1,56 @@\n+\"\"\"SADR installation and collision-safe legacy upgrade behavior.\"\"\"\n+\n+from __future__ import annotations\n+\n+import shutil\n+import tempfile\n+import unittest\n+from pathlib import Path\n+\n+import shiki_test_support  # noqa: F401  (path bootstrap)\n+\n+import shiki_installer\n+from tests.test_adr_references import find_adr_reference_violations\n+\n+\n+PRODUCT_NUMBERS = tuple(range(13, 19))\n+\n+\n+class FreshSadrInstallTests(unittest.TestCase):\n+    def setUp(self) -> None:\n+        self.tmp = Path(tempfile.mkdtemp(prefix=\"shiki-sadr-fresh-\"))\n+        self.addCleanup(lambda: shutil.rmtree(self.tmp, ignore_errors=True))\n+        self.target = self.tmp / \"target\"\n+        self.adr_dir = self.target / \"docs\" / \"adr\"\n+        self.adr_dir.mkdir(parents=True)\n+        self.product_bytes = {\n+            f\"{number:04d}-product-decision.md\": (\n+                f\"# Product ADR {number:04d}\\n\\nTarget-owned bytes {number}.\\n\"\n+            ).encode()\n+            for number in PRODUCT_NUMBERS\n+        }\n+        for name, content in self.product_bytes.items():\n+            (self.adr_dir / name).write_bytes(content)\n+\n+    def test_fresh_install_preserves_product_adrs_and_installs_all_sadrs(self) -> None:\n+        shiki_installer.install_template(self.target, force=False, validate=True)\n+\n+        for name, expected in self.product_bytes.items():\n+            self.assertEqual((self.adr_dir / name).read_bytes(), expected)\n+\n+        sadr_numbers = {\n+            path.name.split(\"-\", 2)[1]\n+            for path in self.adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\")\n+        }\n+        self.assertEqual(sadr_numbers, {f\"{number:04d}\" for number in range(1, 20)})\n+\n+        numeric_records = {\n+            path.name\n+            for path in self.adr_dir.glob(\"[0-9][0-9][0-9][0-9]-*.md\")\n+        }\n+        self.assertEqual(numeric_records, set(self.product_bytes))\n+        self.assertEqual(find_adr_reference_violations(self.target), [])\n+\n+\n+if __name__ == \"__main__\":\n+    unittest.main()\n\n2026-08-07T08:08:38.993201Z ERROR codex_models_manager::manager: failed to renew cache TTL: missing field `base_instructions` at line 94 column 5\napply patch\n2026-08-07T08:08:42.582156Z ERROR codex_models_manager::cache: failed to load models cache: missing field `base_instructions` at line 94 column 5\n2026-08-07T08:08:42.615086Z ERROR codex_models_manager::manager: failed to renew cache TTL: missing field `base_instructions` at line 94 column 5\n2026-08-07T08:08:45.528456Z  WARN codex_protocol::openai_models: Model personality requested but model_messages is missing, falling back to base instructions. model=codex-auto-review personality=pragmatic\npatch: completed\n/private/tmp/shiki-adr-collision.pa0KwY/.worktrees/shiki-t-20260807t073307221254z-2d22d654-introduc/.codex/skills/shiki/SKILL.md\ndiff --git a/.codex/skills/shiki/SKILL.md b/.codex/skills/shiki/SKILL.md\nindex c8e2e85d9d75d49cc4f2d6286ee22659c4e53e75..ca8ff572d92424c2651f16e45232f251b4b8082d\n--- a/.codex/skills/shiki/SKILL.md\n+++ b/.codex/skills/shiki/SKILL.md\n@@ -69,7 +69,7 @@\n \n ## Responsibilities\n \n-- Claude Code implements and repairs by default (`shiki runner claude`, ADR 0008); Codex implements and repairs tasks explicitly assigned to `codex`.\n+- Claude Code implements and repairs by default (`shiki runner claude`, SADR-0008); Codex implements and repairs tasks explicitly assigned to `codex`.\n - Claude Code Action can act as GitHub-side CCA or reviewer.\n - CCA judges completion.\n - MergeGate authorizes state transitions and merge readiness.\ndiff --git a/docs/adr/0001-keep-shiki-as-platform-name.md b/docs/adr/SADR-0001-keep-shiki-as-platform-name.md\nindex 20a9637c3b9d8c97de7ba8db7cfae4d82d9a7dba..8b685439959aada1b221c112f0ac3c51ca8a1695\n--- a/docs/adr/0001-keep-shiki-as-platform-name.md\n+++ b/docs/adr/SADR-0001-keep-shiki-as-platform-name.md\n@@ -1,3 +1,3 @@\n-# Keep Shiki As The Platform Name\n+# SADR-0001: Keep Shiki As The Platform Name\n \n Shiki remains the product name for the GitHub-native agentic engineering control plane. We considered more descriptive names such as MergePilot, MergeGate, and Kiroku, but kept Shiki because it carries the existing goal-seeking framework identity while remaining independent of Hermes, Codex, Claude Code, or any single runtime.\ndiff --git a/docs/adr/0002-use-agents-as-shared-constitution.md b/docs/adr/SADR-0002-use-agents-as-shared-constitution.md\nindex 73de30ecb078120eac540d9bc51855e3cd48d0e3..c5486182bf06fca36ea508496b013c67302fd989\n--- a/docs/adr/0002-use-agents-as-shared-constitution.md\n+++ b/docs/adr/SADR-0002-use-agents-as-shared-constitution.md\n@@ -1,3 +1,3 @@\n-# Use AGENTS As The Shared Agent Constitution\n+# SADR-0002: Use AGENTS As The Shared Agent Constitution\n \n Shiki uses `AGENTS.md` as the runtime-neutral constitution and `CLAUDE.md` as a thin Claude Code wrapper. This keeps Codex, Claude Code, Hermes Runner, and future Agent Runtimes aligned on the same GitHub-first MergeGate rules while still allowing runtime-specific instructions where needed.\ndiff --git a/docs/adr/0003-require-skill-gate-for-mergegate-readiness.md b/docs/adr/SADR-0003-require-skill-gate-for-mergegate-readiness.md\nindex 424be0c9af4c8f0136f1fde68a468c47ff84e764..7fc337987c7df9792f75f181dfcbed75fe5ffff2\n--- a/docs/adr/0003-require-skill-gate-for-mergegate-readiness.md\n+++ b/docs/adr/SADR-0003-require-skill-gate-for-mergegate-readiness.md\n@@ -1,3 +1,3 @@\n-# Require Skill Gate For MergeGate Readiness\n+# SADR-0003: Require Skill Gate For MergeGate Readiness\n \n Shiki requires relevant engineering skills to be invoked before execution when their triggers apply. This makes skills part of MergeGate readiness rather than optional prompt style, because Shiki depends on repeatable planning, issue decomposition, TDD, diagnosis, triage, and architecture review before autonomous or parallel agent work can be trusted.\ndiff --git a/docs/adr/0004-use-github-first-with-shiki-mirror.md b/docs/adr/SADR-0004-use-github-first-with-shiki-mirror.md\nindex d0a3a9174dc646a945a9d325954ffaf00c352040..a79c54ae9ee8eed2a0524192b864da4e22a92d3f\n--- a/docs/adr/0004-use-github-first-with-shiki-mirror.md\n+++ b/docs/adr/SADR-0004-use-github-first-with-shiki-mirror.md\n@@ -1,4 +1,4 @@\n-# ADR 0004: Use GitHub First With A Repository-Local Shiki Mirror\n+# SADR-0004: Use GitHub First With A Repository-Local Shiki Mirror\n \n ## Status\n \ndiff --git a/docs/adr/0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md b/docs/adr/SADR-0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\nindex b339baf49b89d9290e79c3d51b36e7b2bde3bd95..c30a0d8b1fde0224d0ac7820e7870dccf721580d\n--- a/docs/adr/0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\n+++ b/docs/adr/SADR-0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\n@@ -1,4 +1,4 @@\n-# ADR 0005: Use Codex As Front And Claude Code Action As The GitHub Runtime\n+# SADR-0005: Use Codex As Front And Claude Code Action As The GitHub Runtime\n \n ## Status\n \ndiff --git a/docs/adr/0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md b/docs/adr/SADR-0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\nindex 4bcae9d1e52f0a37b8e9fbafa5cd14b6ee1993bd..7a38538a6ae9929567f0ade67e7ea6a60d0b3f7e\n--- a/docs/adr/0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\n+++ b/docs/adr/SADR-0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\n@@ -1,4 +1,4 @@\n-# ADR 0006: Separate Codex Implementation, CCA Completion, And MergeGate Authorization\n+# SADR-0006: Separate Codex Implementation, CCA Completion, And MergeGate Authorization\n \n ## Status\n \ndiff --git a/docs/adr/0007-packaging-and-release.md b/docs/adr/SADR-0007-packaging-and-release.md\nindex 79b84ddb97167372b44641e5b7860e400ce80822..023cd57f8011aa6e6d2ebf612b7c35a7a82c00d4\n--- a/docs/adr/0007-packaging-and-release.md\n+++ b/docs/adr/SADR-0007-packaging-and-release.md\n@@ -1,4 +1,4 @@\n-# ADR 0007: Packaging And Release For Shiki\n+# SADR-0007: Packaging And Release For Shiki\n \n ## Status\n \ndiff --git a/docs/adr/0008-use-claude-code-as-default-implementer.md b/docs/adr/SADR-0008-use-claude-code-as-default-implementer.md\nindex dedf4510b7c7799521422f622029563d373db031..a158a8d444d93764ec515a28133f7dde4af23579\n--- a/docs/adr/0008-use-claude-code-as-default-implementer.md\n+++ b/docs/adr/SADR-0008-use-claude-code-as-default-implementer.md\n@@ -1,4 +1,4 @@\n-# ADR 0008: Use Claude Code As The Default Implementer Runtime\n+# SADR-0008: Use Claude Code As The Default Implementer Runtime\n \n ## Status\n \ndiff --git a/docs/adr/0009-requirements-definition-and-spec-freeze.md b/docs/adr/SADR-0009-requirements-definition-and-spec-freeze.md\nindex 29b3f7871bef9203a8b4a831a65c39eb2f506e42..52e7dd69ad0aa4efe0b68c62678abbf7df02aaec\n--- a/docs/adr/0009-requirements-definition-and-spec-freeze.md\n+++ b/docs/adr/SADR-0009-requirements-definition-and-spec-freeze.md\n@@ -1,4 +1,4 @@\n-# ADR 0009: Merge Grill And Planning Into Requirements Definition With Spec Freeze\n+# SADR-0009: Merge Grill And Planning Into Requirements Definition With Spec Freeze\n \n ## Status\n \ndiff --git a/docs/adr/0010-external-ai-guardian-review.md b/docs/adr/SADR-0010-external-ai-guardian-review.md\nindex e6108bbe8bb4e73e0ff4684c8f78001a80438c4e..e690d4eddd270e683d709c9b679c5859e71eeaaa\n--- a/docs/adr/0010-external-ai-guardian-review.md\n+++ b/docs/adr/SADR-0010-external-ai-guardian-review.md\n@@ -1,4 +1,4 @@\n-# ADR 0010: External AI Guardian Review As A First-Class Approval Authority\n+# SADR-0010: External AI Guardian Review As A First-Class Approval Authority\n \n ## Status\n \ndiff --git a/docs/adr/0011-autonomous-quality-gate-evidence.md b/docs/adr/SADR-0011-autonomous-quality-gate-evidence.md\nindex bca0246c5d46e44afbde15486a75eec425fc02f5..9c93eac16d8c3a6d4b11bd7224e3955102cd7db4\n--- a/docs/adr/0011-autonomous-quality-gate-evidence.md\n+++ b/docs/adr/SADR-0011-autonomous-quality-gate-evidence.md\n@@ -1,4 +1,4 @@\n-# ADR 0011: Autonomous Quality-Gate Evidence \u2014 Deterministic Facts And An Independent Verifier\n+# SADR-0011: Autonomous Quality-Gate Evidence \u2014 Deterministic Facts And An Independent Verifier\n \n ## Status\n \ndiff --git a/docs/adr/0012-autonomous-loop-closeout-to-main.md b/docs/adr/SADR-0012-autonomous-loop-closeout-to-main.md\nindex 637fad6dde4989c9c2bf0f81f676d8d29991123c..ec7d85f81030a676bcbba28b43eb365422a36eac\n--- a/docs/adr/0012-autonomous-loop-closeout-to-main.md\n+++ b/docs/adr/SADR-0012-autonomous-loop-closeout-to-main.md\n@@ -1,4 +1,4 @@\n-# 0012 \u2014 Autonomous loop pushes goal completion to main via a closeout PR\n+# SADR-0012 \u2014 Autonomous loop pushes goal completion to main via a closeout PR\n \n - Status: accepted\n - Date: 2026-06-18\ndiff --git a/docs/adr/0013-bootstrap-grants-actions-approve-pull-requests.md b/docs/adr/SADR-0013-bootstrap-grants-actions-approve-pull-requests.md\nindex b6c69d64f1c5ced346b88d6859586715b3b5bd0e..a793e7277d67ab8ea56286fec289ca689825a17d\n--- a/docs/adr/0013-bootstrap-grants-actions-approve-pull-requests.md\n+++ b/docs/adr/SADR-0013-bootstrap-grants-actions-approve-pull-requests.md\n@@ -1,4 +1,4 @@\n-# ADR 0013: Bootstrap Grants GitHub Actions Permission To Create And Approve Pull Requests\n+# SADR-0013: Bootstrap Grants GitHub Actions Permission To Create And Approve Pull Requests\n \n ## Status\n \ndiff --git a/docs/adr/0014-codex-app-external-ai-guardian-ui-adapter.md b/docs/adr/SADR-0014-codex-app-external-ai-guardian-ui-adapter.md\nindex 238db06279334caa341c117362df9271382a7fcd..65f12e9939321e1b3e389a34c822fc1549e5155d\n--- a/docs/adr/0014-codex-app-external-ai-guardian-ui-adapter.md\n+++ b/docs/adr/SADR-0014-codex-app-external-ai-guardian-ui-adapter.md\n@@ -1,4 +1,4 @@\n-# ADR 0014: Use Codex App As The External AI Guardian UI Adapter\n+# SADR-0014: Use Codex App As The External AI Guardian UI Adapter\n \n ## Status\n \ndiff --git a/docs/adr/0015-contract-approval-before-dispatch.md b/docs/adr/SADR-0015-contract-approval-before-dispatch.md\nindex fad6c656741a20a7684ba16080a7db159f63a52f..7bdd760bb3e82eee4e1410f45c783b03b638d456\n--- a/docs/adr/0015-contract-approval-before-dispatch.md\n+++ b/docs/adr/SADR-0015-contract-approval-before-dispatch.md\n@@ -1,4 +1,4 @@\n-# ADR 0015: Approve The Task Contract Before Dispatch, Not The Diff After It\n+# SADR-0015: Approve The Task Contract Before Dispatch, Not The Diff After It\n \n ## Status\n \ndiff --git a/docs/adr/0016-locks-bound-product-paths.md b/docs/adr/SADR-0016-locks-bound-product-paths.md\nindex b5e850f3f1f7f4ef6e7f9d1763a29a51bd300c45..332cc17313498404f41a145291e38447b5de0dc4\n--- a/docs/adr/0016-locks-bound-product-paths.md\n+++ b/docs/adr/SADR-0016-locks-bound-product-paths.md\n@@ -1,4 +1,4 @@\n-# ADR 0016: Locks bound product paths; the Shiki mirror is bound by record identity\n+# SADR-0016: Locks bound product paths; the Shiki mirror is bound by record identity\n \n ## Status\n \ndiff --git a/docs/adr/0017-bookkeeping-closeouts-do-not-inherit-risk.md b/docs/adr/SADR-0017-bookkeeping-closeouts-do-not-inherit-risk.md\nindex 554e3a0d526a72ca067108c5464cc68c3edcf174..83115f69f1b1b48d544c9b0157f377b91cb53abf\n--- a/docs/adr/0017-bookkeeping-closeouts-do-not-inherit-risk.md\n+++ b/docs/adr/SADR-0017-bookkeeping-closeouts-do-not-inherit-risk.md\n@@ -1,4 +1,4 @@\n-# ADR 0017: A Bookkeeping Closeout Does Not Inherit Its Task's Risk\n+# SADR-0017: A Bookkeeping Closeout Does Not Inherit Its Task's Risk\n \n ## Status\n \ndiff --git a/docs/adr/0018-carried-guardian-approval-across-a-proven-base-sync.md b/docs/adr/SADR-0018-carried-guardian-approval-across-a-proven-base-sync.md\nindex 22358562da390ed8b45cd600da08a43d378b4680..38da0fd95102c3496c3219ce2319fce696a2532a\n--- a/docs/adr/0018-carried-guardian-approval-across-a-proven-base-sync.md\n+++ b/docs/adr/SADR-0018-carried-guardian-approval-across-a-proven-base-sync.md\n@@ -1,4 +1,4 @@\n-# ADR 0018: Carried Guardian Approval Across A Proven Base Sync\n+# SADR-0018: Carried Guardian Approval Across A Proven Base Sync\n \n ## Status\n \ndiff --git a/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md b/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\nnew file mode 100644\nindex 0000000000000000000000000000000000000000..7455ce1f3bed02ddddb851ac21a1d1a648c9aeba\n--- /dev/null\n+++ b/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\n@@ -0,0 +1,55 @@\n+# SADR-0019: Separate Shiki Platform SADRs From Target ADRs\n+\n+## Status\n+\n+Accepted\n+\n+## Context\n+\n+Shiki installs its platform decision records into `docs/adr/` in every target\n+repository. Shiki and its targets previously used the same numeric\n+`NNNN-<slug>.md` namespace. A target that already owned ADRs 0013 through 0018\n+therefore acquired two records for each number during installation, making an\n+`ADR NNNN` citation ambiguous. Renumbering target decisions would rewrite target\n+history, while renumbering Shiki decisions per target would destroy their stable\n+platform identity.\n+\n+Upgrades also need to remove the former Shiki-owned numeric paths without ever\n+mistaking a target-owned ADR for platform content. The install stamp already\n+records exact shipped paths and content digests, so it can prove ownership only\n+when both still match.\n+\n+## Decision\n+\n+Shiki platform decisions use the `SADR` namespace:\n+\n+- records are named `docs/adr/SADR-NNNN-<slug>.md`;\n+- live platform citations use `SADR-NNNN`;\n+- target product decisions retain `docs/adr/NNNN-<slug>.md` and `ADR NNNN`;\n+- the reference validator resolves the two namespaces independently, so the same\n+  four-digit number may exist once in each namespace.\n+\n+Before a forced upgrade writes anything, the installer inventories the exact\n+former numeric Shiki ADR paths. It may delete a legacy path only when the\n+existing install stamp contains that exact path and its recorded SHA-256 digest\n+matches the file currently on disk. All ownership blockers are reported\n+together and abort the upgrade before its first write. A non-forced install\n+never performs this cleanup.\n+\n+Historical `.shiki/` evidence remains unchanged because it records the namespace\n+that existed when that evidence was produced.\n+\n+## Consequences\n+\n+- Targets may use any ADR number without colliding with Shiki's platform SADRs.\n+- Fresh installs preserve target ADR bytes and install a stable SADR set.\n+- Forced upgrades fail closed when legacy ownership cannot be proven, and a\n+  successful upgrade refreshes the stamp without removed legacy paths.\n+- Current governance, packaging, workflow, prompt, script, and skill references\n+  must move with the platform records.\n+\n+## Alternatives Considered\n+\n+- Collision-only refusal leaves affected targets permanently unupgradable.\n+- Renumbering target ADRs mutates target-owned history.\n+- Per-target Shiki renumbering makes platform citations non-portable.\ndiff --git a/tests/test_adr_references.py b/tests/test_adr_references.py\nindex 51706329c797d5dd9b47e082a333fa83b261d482..92569d9fa25e22cfd71c17561d715485027a0d2d\n--- a/tests/test_adr_references.py\n+++ b/tests/test_adr_references.py\n@@ -31,17 +31,18 @@\n \n REPO_ROOT = Path(__file__).resolve().parents[1]\n \n-# \"ADR 0016\" -- a four-digit citation. The trailing \\b keeps a five-digit token\n-# from being read as a truncated four-digit number.\n-_CITATION = re.compile(r\"\\bADR (\\d{4})\\b\")\n-# \"0016-locks-bound-product-paths.md\" -- the leading four digits name the record.\n-_ADR_FILENAME = re.compile(r\"^(\\d{4})-.*\\.md$\")\n+# ``ADR 0016`` / ``SADR-0016`` -- a namespace plus four-digit citation. The\n+# trailing ``\\b`` keeps a five-digit token from being read as a truncated one.\n+_CITATION = re.compile(r\"\\b(S?ADR)[ -](\\d{4})\\b\")\n+# Target ADRs retain the numeric filename; Shiki platform decisions use SADR.\n+_DECISION_FILENAME = re.compile(r\"^(?:(SADR)-)?(\\d{4})-.*\\.md$\")\n \n \n class Violation(NamedTuple):\n     \"\"\"A citation that does not resolve to exactly one decision record.\"\"\"\n \n     citing_file: str  # repo-relative path of the file that cites the ADR\n+    namespace: str  # \"ADR\" for target decisions; \"SADR\" for Shiki decisions\n     number: str  # the cited four-digit number, e.g. \"0016\"\n     line: int  # 1-based line of the citation\n     reason: str  # \"missing\" (zero matches) or \"ambiguous\" (two or more)\n@@ -49,32 +50,34 @@\n \n     @property\n     def expected_path(self) -> str:\n-        return f\"docs/adr/{self.number}-*.md\"\n+        prefix = \"SADR-\" if self.namespace == \"SADR\" else \"\"\n+        return f\"docs/adr/{prefix}{self.number}-*.md\"\n \n     def describe(self) -> str:\n         \"\"\"Name the citing file, the cited number, and the missing path.\"\"\"\n         if self.reason == \"missing\":\n             return (\n-                f\"{self.citing_file}:{self.line} cites ADR {self.number} \"\n+                f\"{self.citing_file}:{self.line} cites {self.namespace} {self.number} \"\n                 f\"but no document matches {self.expected_path}\"\n             )\n         joined = \", \".join(self.matches)\n         return (\n-            f\"{self.citing_file}:{self.line} cites ADR {self.number} which \"\n+            f\"{self.citing_file}:{self.line} cites {self.namespace} {self.number} which \"\n             f\"resolves to {len(self.matches)} documents (expected exactly one \"\n             f\"at {self.expected_path}): {joined}\"\n         )\n \n \n-def _adr_index(root: Path) -> dict[str, list[Path]]:\n-    \"\"\"Map every four-digit ADR number to the record files that claim it.\"\"\"\n-    index: dict[str, list[Path]] = {}\n+def _adr_index(root: Path) -> dict[tuple[str, str], list[Path]]:\n+    \"\"\"Map each decision namespace and number to the files that claim it.\"\"\"\n+    index: dict[tuple[str, str], list[Path]] = {}\n     adr_dir = root / \"docs\" / \"adr\"\n     if adr_dir.is_dir():\n         for path in sorted(adr_dir.glob(\"*.md\")):\n-            match = _ADR_FILENAME.match(path.name)\n+            match = _DECISION_FILENAME.match(path.name)\n             if match:\n-                index.setdefault(match.group(1), []).append(path)\n+                namespace = \"SADR\" if match.group(1) else \"ADR\"\n+                index.setdefault((namespace, match.group(2)), []).append(path)\n     return index\n \n \n@@ -110,13 +113,15 @@\n         rel = path.relative_to(root).as_posix()\n         for lineno, line in enumerate(text.splitlines(), start=1):\n             for match in _CITATION.finditer(line):\n-                number = match.group(1)\n-                found = index.get(number, [])\n+                namespace, number = match.groups()\n+                found = index.get((namespace, number), [])\n                 if len(found) == 1:\n                     continue\n                 reason = \"missing\" if not found else \"ambiguous\"\n                 rels = [p.relative_to(root).as_posix() for p in found]\n-                violations.append(Violation(rel, number, lineno, reason, rels))\n+                violations.append(\n+                    Violation(rel, namespace, number, lineno, reason, rels)\n+                )\n     return violations\n \n \n@@ -146,8 +151,8 @@\n         # The two records this Goal carried onto the default branch, and which\n         # the scripts cite most, must be present.\n         index = _adr_index(REPO_ROOT)\n-        self.assertIn(\"0016\", index)\n-        self.assertIn(\"0017\", index)\n+        self.assertIn((\"ADR\", \"0016\"), index)\n+        self.assertIn((\"ADR\", \"0017\"), index)\n \n \n class FixtureAdrReferences(unittest.TestCase):\n@@ -180,6 +185,17 @@\n         self.assertIn(\"9991\", message)  # cited number\n         self.assertIn(\"docs/adr/9991-*.md\", message)  # missing path\n \n+    def test_target_adr_does_not_satisfy_missing_platform_sadr(self) -> None:\n+        root = self._make_repo(\n+            adr_names=[\"0042-target-decision.md\"],\n+            script_body='\"\"\"Target ADR 0042 exists, but platform SADR-0042 does not.\"\"\"\\n',\n+        )\n+        violations = find_adr_reference_violations(root, include_docs=False)\n+        self.assertEqual(len(violations), 1)\n+        self.assertEqual(violations[0].namespace, \"SADR\")\n+        self.assertEqual(violations[0].number, \"0042\")\n+        self.assertEqual(violations[0].expected_path, \"docs/adr/SADR-0042-*.md\")\n+\n     def test_uncited_adr_is_allowed(self) -> None:\n         # An ADR exists but nothing cites it -> not a violation. The converse\n         # (every ADR must be cited) is explicitly NOT required.\ndiff --git a/tests/test_sadr_install_migration.py b/tests/test_sadr_install_migration.py\nnew file mode 100644\nindex 0000000000000000000000000000000000000000..a6398c81377f00c09aa2cb684980b070cfaf714c\n--- /dev/null\n+++ b/tests/test_sadr_install_migration.py\n@@ -0,0 +1,56 @@\n+\"\"\"SADR installation and collision-safe legacy upgrade behavior.\"\"\"\n+\n+from __future__ import annotations\n+\n+import shutil\n+import tempfile\n+import unittest\n+from pathlib import Path\n+\n+import shiki_test_support  # noqa: F401  (path bootstrap)\n+\n+import shiki_installer\n+from tests.test_adr_references import find_adr_reference_violations\n+\n+\n+PRODUCT_NUMBERS = tuple(range(13, 19))\n+\n+\n+class FreshSadrInstallTests(unittest.TestCase):\n+    def setUp(self) -> None:\n+        self.tmp = Path(tempfile.mkdtemp(prefix=\"shiki-sadr-fresh-\"))\n+        self.addCleanup(lambda: shutil.rmtree(self.tmp, ignore_errors=True))\n+        self.target = self.tmp / \"target\"\n+        self.adr_dir = self.target / \"docs\" / \"adr\"\n+        self.adr_dir.mkdir(parents=True)\n+        self.product_bytes = {\n+            f\"{number:04d}-product-decision.md\": (\n+                f\"# Product ADR {number:04d}\\n\\nTarget-owned bytes {number}.\\n\"\n+            ).encode()\n+            for number in PRODUCT_NUMBERS\n+        }\n+        for name, content in self.product_bytes.items():\n+            (self.adr_dir / name).write_bytes(content)\n+\n+    def test_fresh_install_preserves_product_adrs_and_installs_all_sadrs(self) -> None:\n+        shiki_installer.install_template(self.target, force=False, validate=True)\n+\n+        for name, expected in self.product_bytes.items():\n+            self.assertEqual((self.adr_dir / name).read_bytes(), expected)\n+\n+        sadr_numbers = {\n+            path.name.split(\"-\", 2)[1]\n+            for path in self.adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\")\n+        }\n+        self.assertEqual(sadr_numbers, {f\"{number:04d}\" for number in range(1, 20)})\n+\n+        numeric_records = {\n+            path.name\n+            for path in self.adr_dir.glob(\"[0-9][0-9][0-9][0-9]-*.md\")\n+        }\n+        self.assertEqual(numeric_records, set(self.product_bytes))\n+        self.assertEqual(find_adr_reference_violations(self.target), [])\n+\n+\n+if __name__ == \"__main__\":\n+    unittest.main()\n\ndiff --git a/.codex/skills/shiki/SKILL.md b/.codex/skills/shiki/SKILL.md\nindex c8e2e85d9d75d49cc4f2d6286ee22659c4e53e75..ca8ff572d92424c2651f16e45232f251b4b8082d\n--- a/.codex/skills/shiki/SKILL.md\n+++ b/.codex/skills/shiki/SKILL.md\n@@ -69,7 +69,7 @@\n \n ## Responsibilities\n \n-- Claude Code implements and repairs by default (`shiki runner claude`, ADR 0008); Codex implements and repairs tasks explicitly assigned to `codex`.\n+- Claude Code implements and repairs by default (`shiki runner claude`, SADR-0008); Codex implements and repairs tasks explicitly assigned to `codex`.\n - Claude Code Action can act as GitHub-side CCA or reviewer.\n - CCA judges completion.\n - MergeGate authorizes state transitions and merge readiness.\ndiff --git a/docs/adr/0001-keep-shiki-as-platform-name.md b/docs/adr/SADR-0001-keep-shiki-as-platform-name.md\nindex 20a9637c3b9d8c97de7ba8db7cfae4d82d9a7dba..8b685439959aada1b221c112f0ac3c51ca8a1695\n--- a/docs/adr/0001-keep-shiki-as-platform-name.md\n+++ b/docs/adr/SADR-0001-keep-shiki-as-platform-name.md\n@@ -1,3 +1,3 @@\n-# Keep Shiki As The Platform Name\n+# SADR-0001: Keep Shiki As The Platform Name\n \n Shiki remains the product name for the GitHub-native agentic engineering control plane. We considered more descriptive names such as MergePilot, MergeGate, and Kiroku, but kept Shiki because it carries the existing goal-seeking framework identity while remaining independent of Hermes, Codex, Claude Code, or any single runtime.\ndiff --git a/docs/adr/0002-use-agents-as-shared-constitution.md b/docs/adr/SADR-0002-use-agents-as-shared-constitution.md\nindex 73de30ecb078120eac540d9bc51855e3cd48d0e3..c5486182bf06fca36ea508496b013c67302fd989\n--- a/docs/adr/0002-use-agents-as-shared-constitution.md\n+++ b/docs/adr/SADR-0002-use-agents-as-shared-constitution.md\n@@ -1,3 +1,3 @@\n-# Use AGENTS As The Shared Agent Constitution\n+# SADR-0002: Use AGENTS As The Shared Agent Constitution\n \n Shiki uses `AGENTS.md` as the runtime-neutral constitution and `CLAUDE.md` as a thin Claude Code wrapper. This keeps Codex, Claude Code, Hermes Runner, and future Agent Runtimes aligned on the same GitHub-first MergeGate rules while still allowing runtime-specific instructions where needed.\ndiff --git a/docs/adr/0003-require-skill-gate-for-mergegate-readiness.md b/docs/adr/SADR-0003-require-skill-gate-for-mergegate-readiness.md\nindex 424be0c9af4c8f0136f1fde68a468c47ff84e764..7fc337987c7df9792f75f181dfcbed75fe5ffff2\n--- a/docs/adr/0003-require-skill-gate-for-mergegate-readiness.md\n+++ b/docs/adr/SADR-0003-require-skill-gate-for-mergegate-readiness.md\n@@ -1,3 +1,3 @@\n-# Require Skill Gate For MergeGate Readiness\n+# SADR-0003: Require Skill Gate For MergeGate Readiness\n \n Shiki requires relevant engineering skills to be invoked before execution when their triggers apply. This makes skills part of MergeGate readiness rather than optional prompt style, because Shiki depends on repeatable planning, issue decomposition, TDD, diagnosis, triage, and architecture review before autonomous or parallel agent work can be trusted.\ndiff --git a/docs/adr/0004-use-github-first-with-shiki-mirror.md b/docs/adr/SADR-0004-use-github-first-with-shiki-mirror.md\nindex d0a3a9174dc646a945a9d325954ffaf00c352040..a79c54ae9ee8eed2a0524192b864da4e22a92d3f\n--- a/docs/adr/0004-use-github-first-with-shiki-mirror.md\n+++ b/docs/adr/SADR-0004-use-github-first-with-shiki-mirror.md\n@@ -1,4 +1,4 @@\n-# ADR 0004: Use GitHub First With A Repository-Local Shiki Mirror\n+# SADR-0004: Use GitHub First With A Repository-Local Shiki Mirror\n \n ## Status\n \ndiff --git a/docs/adr/0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md b/docs/adr/SADR-0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\nindex b339baf49b89d9290e79c3d51b36e7b2bde3bd95..c30a0d8b1fde0224d0ac7820e7870dccf721580d\n--- a/docs/adr/0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\n+++ b/docs/adr/SADR-0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\n@@ -1,4 +1,4 @@\n-# ADR 0005: Use Codex As Front And Claude Code Action As The GitHub Runtime\n+# SADR-0005: Use Codex As Front And Claude Code Action As The GitHub Runtime\n \n ## Status\n \ndiff --git a/docs/adr/0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md b/docs/adr/SADR-0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\nindex 4bcae9d1e52f0a37b8e9fbafa5cd14b6ee1993bd..7a38538a6ae9929567f0ade67e7ea6a60d0b3f7e\n--- a/docs/adr/0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\n+++ b/docs/adr/SADR-0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\n@@ -1,4 +1,4 @@\n-# ADR 0006: Separate Codex Implementation, CCA Completion, And MergeGate Authorization\n+# SADR-0006: Separate Codex Implementation, CCA Completion, And MergeGate Authorization\n \n ## Status\n \ndiff --git a/docs/adr/0007-packaging-and-release.md b/docs/adr/SADR-0007-packaging-and-release.md\nindex 79b84ddb97167372b44641e5b7860e400ce80822..023cd57f8011aa6e6d2ebf612b7c35a7a82c00d4\n--- a/docs/adr/0007-packaging-and-release.md\n+++ b/docs/adr/SADR-0007-packaging-and-release.md\n@@ -1,4 +1,4 @@\n-# ADR 0007: Packaging And Release For Shiki\n+# SADR-0007: Packaging And Release For Shiki\n \n ## Status\n \ndiff --git a/docs/adr/0008-use-claude-code-as-default-implementer.md b/docs/adr/SADR-0008-use-claude-code-as-default-implementer.md\nindex dedf4510b7c7799521422f622029563d373db031..a158a8d444d93764ec515a28133f7dde4af23579\n--- a/docs/adr/0008-use-claude-code-as-default-implementer.md\n+++ b/docs/adr/SADR-0008-use-claude-code-as-default-implementer.md\n@@ -1,4 +1,4 @@\n-# ADR 0008: Use Claude Code As The Default Implementer Runtime\n+# SADR-0008: Use Claude Code As The Default Implementer Runtime\n \n ## Status\n \ndiff --git a/docs/adr/0009-requirements-definition-and-spec-freeze.md b/docs/adr/SADR-0009-requirements-definition-and-spec-freeze.md\nindex 29b3f7871bef9203a8b4a831a65c39eb2f506e42..52e7dd69ad0aa4efe0b68c62678abbf7df02aaec\n--- a/docs/adr/0009-requirements-definition-and-spec-freeze.md\n+++ b/docs/adr/SADR-0009-requirements-definition-and-spec-freeze.md\n@@ -1,4 +1,4 @@\n-# ADR 0009: Merge Grill And Planning Into Requirements Definition With Spec Freeze\n+# SADR-0009: Merge Grill And Planning Into Requirements Definition With Spec Freeze\n \n ## Status\n \ndiff --git a/docs/adr/0010-external-ai-guardian-review.md b/docs/adr/SADR-0010-external-ai-guardian-review.md\nindex e6108bbe8bb4e73e0ff4684c8f78001a80438c4e..e690d4eddd270e683d709c9b679c5859e71eeaaa\n--- a/docs/adr/0010-external-ai-guardian-review.md\n+++ b/docs/adr/SADR-0010-external-ai-guardian-review.md\n@@ -1,4 +1,4 @@\n-# ADR 0010: External AI Guardian Review As A First-Class Approval Authority\n+# SADR-0010: External AI Guardian Review As A First-Class Approval Authority\n \n ## Status\n \ndiff --git a/docs/adr/0011-autonomous-quality-gate-evidence.md b/docs/adr/SADR-0011-autonomous-quality-gate-evidence.md\nindex bca0246c5d46e44afbde15486a75eec425fc02f5..9c93eac16d8c3a6d4b11bd7224e3955102cd7db4\n--- a/docs/adr/0011-autonomous-quality-gate-evidence.md\n+++ b/docs/adr/SADR-0011-autonomous-quality-gate-evidence.md\n@@ -1,4 +1,4 @@\n-# ADR 0011: Autonomous Quality-Gate Evidence \u2014 Deterministic Facts And An Independent Verifier\n+# SADR-0011: Autonomous Quality-Gate Evidence \u2014 Deterministic Facts And An Independent Verifier\n \n ## Status\n \ndiff --git a/docs/adr/0012-autonomous-loop-closeout-to-main.md b/docs/adr/SADR-0012-autonomous-loop-closeout-to-main.md\nindex 637fad6dde4989c9c2bf0f81f676d8d29991123c..ec7d85f81030a676bcbba28b43eb365422a36eac\n--- a/docs/adr/0012-autonomous-loop-closeout-to-main.md\n+++ b/docs/adr/SADR-0012-autonomous-loop-closeout-to-main.md\n@@ -1,4 +1,4 @@\n-# 0012 \u2014 Autonomous loop pushes goal completion to main via a closeout PR\n+# SADR-0012 \u2014 Autonomous loop pushes goal completion to main via a closeout PR\n \n - Status: accepted\n - Date: 2026-06-18\ndiff --git a/docs/adr/0013-bootstrap-grants-actions-approve-pull-requests.md b/docs/adr/SADR-0013-bootstrap-grants-actions-approve-pull-requests.md\nindex b6c69d64f1c5ced346b88d6859586715b3b5bd0e..a793e7277d67ab8ea56286fec289ca689825a17d\n--- a/docs/adr/0013-bootstrap-grants-actions-approve-pull-requests.md\n+++ b/docs/adr/SADR-0013-bootstrap-grants-actions-approve-pull-requests.md\n@@ -1,4 +1,4 @@\n-# ADR 0013: Bootstrap Grants GitHub Actions Permission To Create And Approve Pull Requests\n+# SADR-0013: Bootstrap Grants GitHub Actions Permission To Create And Approve Pull Requests\n \n ## Status\n \ndiff --git a/docs/adr/0014-codex-app-external-ai-guardian-ui-adapter.md b/docs/adr/SADR-0014-codex-app-external-ai-guardian-ui-adapter.md\nindex 238db06279334caa341c117362df9271382a7fcd..65f12e9939321e1b3e389a34c822fc1549e5155d\n--- a/docs/adr/0014-codex-app-external-ai-guardian-ui-adapter.md\n+++ b/docs/adr/SADR-0014-codex-app-external-ai-guardian-ui-adapter.md\n@@ -1,4 +1,4 @@\n-# ADR 0014: Use Codex App As The External AI Guardian UI Adapter\n+# SADR-0014: Use Codex App As The External AI Guardian UI Adapter\n \n ## Status\n \ndiff --git a/docs/adr/0015-contract-approval-before-dispatch.md b/docs/adr/SADR-0015-contract-approval-before-dispatch.md\nindex fad6c656741a20a7684ba16080a7db159f63a52f..7bdd760bb3e82eee4e1410f45c783b03b638d456\n--- a/docs/adr/0015-contract-approval-before-dispatch.md\n+++ b/docs/adr/SADR-0015-contract-approval-before-dispatch.md\n@@ -1,4 +1,4 @@\n-# ADR 0015: Approve The Task Contract Before Dispatch, Not The Diff After It\n+# SADR-0015: Approve The Task Contract Before Dispatch, Not The Diff After It\n \n ## Status\n \ndiff --git a/docs/adr/0016-locks-bound-product-paths.md b/docs/adr/SADR-0016-locks-bound-product-paths.md\nindex b5e850f3f1f7f4ef6e7f9d1763a29a51bd300c45..332cc17313498404f41a145291e38447b5de0dc4\n--- a/docs/adr/0016-locks-bound-product-paths.md\n+++ b/docs/adr/SADR-0016-locks-bound-product-paths.md\n@@ -1,4 +1,4 @@\n-# ADR 0016: Locks bound product paths; the Shiki mirror is bound by record identity\n+# SADR-0016: Locks bound product paths; the Shiki mirror is bound by record identity\n \n ## Status\n \ndiff --git a/docs/adr/0017-bookkeeping-closeouts-do-not-inherit-risk.md b/docs/adr/SADR-0017-bookkeeping-closeouts-do-not-inherit-risk.md\nindex 554e3a0d526a72ca067108c5464cc68c3edcf174..83115f69f1b1b48d544c9b0157f377b91cb53abf\n--- a/docs/adr/0017-bookkeeping-closeouts-do-not-inherit-risk.md\n+++ b/docs/adr/SADR-0017-bookkeeping-closeouts-do-not-inherit-risk.md\n@@ -1,4 +1,4 @@\n-# ADR 0017: A Bookkeeping Closeout Does Not Inherit Its Task's Risk\n+# SADR-0017: A Bookkeeping Closeout Does Not Inherit Its Task's Risk\n \n ## Status\n \ndiff --git a/docs/adr/0018-carried-guardian-approval-across-a-proven-base-sync.md b/docs/adr/SADR-0018-carried-guardian-approval-across-a-proven-base-sync.md\nindex 22358562da390ed8b45cd600da08a43d378b4680..38da0fd95102c3496c3219ce2319fce696a2532a\n--- a/docs/adr/0018-carried-guardian-approval-across-a-proven-base-sync.md\n+++ b/docs/adr/SADR-0018-carried-guardian-approval-across-a-proven-base-sync.md\n@@ -1,4 +1,4 @@\n-# ADR 0018: Carried Guardian Approval Across A Proven Base Sync\n+# SADR-0018: Carried Guardian Approval Across A Proven Base Sync\n \n ## Status\n \ndiff --git a/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md b/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\nnew file mode 100644\nindex 0000000000000000000000000000000000000000..7455ce1f3bed02ddddb851ac21a1d1a648c9aeba\n--- /dev/null\n+++ b/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\n@@ -0,0 +1,55 @@\n+# SADR-0019: Separate Shiki Platform SADRs From Target ADRs\n+\n+## Status\n+\n+Accepted\n+\n+## Context\n+\n+Shiki installs its platform decision records into `docs/adr/` in every target\n+repository. Shiki and its targets previously used the same numeric\n+`NNNN-<slug>.md` namespace. A target that already owned ADRs 0013 through 0018\n+therefore acquired two records for each number during installation, making an\n+`ADR NNNN` citation ambiguous. Renumbering target decisions would rewrite target\n+history, while renumbering Shiki decisions per target would destroy their stable\n+platform identity.\n+\n+Upgrades also need to remove the former Shiki-owned numeric paths without ever\n+mistaking a target-owned ADR for platform content. The install stamp already\n+records exact shipped paths and content digests, so it can prove ownership only\n+when both still match.\n+\n+## Decision\n+\n+Shiki platform decisions use the `SADR` namespace:\n+\n+- records are named `docs/adr/SADR-NNNN-<slug>.md`;\n+- live platform citations use `SADR-NNNN`;\n+- target product decisions retain `docs/adr/NNNN-<slug>.md` and `ADR NNNN`;\n+- the reference validator resolves the two namespaces independently, so the same\n+  four-digit number may exist once in each namespace.\n+\n+Before a forced upgrade writes anything, the installer inventories the exact\n+former numeric Shiki ADR paths. It may delete a legacy path only when the\n+existing install stamp contains that exact path and its recorded SHA-256 digest\n+matches the file currently on disk. All ownership blockers are reported\n+together and abort the upgrade before its first write. A non-forced install\n+never performs this cleanup.\n+\n+Historical `.shiki/` evidence remains unchanged because it records the namespace\n+that existed when that evidence was produced.\n+\n+## Consequences\n+\n+- Targets may use any ADR number without colliding with Shiki's platform SADRs.\n+- Fresh installs preserve target ADR bytes and install a stable SADR set.\n+- Forced upgrades fail closed when legacy ownership cannot be proven, and a\n+  successful upgrade refreshes the stamp without removed legacy paths.\n+- Current governance, packaging, workflow, prompt, script, and skill references\n+  must move with the platform records.\n+\n+## Alternatives Considered\n+\n+- Collision-only refusal leaves affected targets permanently unupgradable.\n+- Renumbering target ADRs mutates target-owned history.\n+- Per-target Shiki renumbering makes platform citations non-portable.\ndiff --git a/tests/test_adr_references.py b/tests/test_adr_references.py\nindex 51706329c797d5dd9b47e082a333fa83b261d482..92569d9fa25e22cfd71c17561d715485027a0d2d\n--- a/tests/test_adr_references.py\n+++ b/tests/test_adr_references.py\n@@ -31,17 +31,18 @@\n \n REPO_ROOT = Path(__file__).resolve().parents[1]\n \n-# \"ADR 0016\" -- a four-digit citation. The trailing \\b keeps a five-digit token\n-# from being read as a truncated four-digit number.\n-_CITATION = re.compile(r\"\\bADR (\\d{4})\\b\")\n-# \"0016-locks-bound-product-paths.md\" -- the leading four digits name the record.\n-_ADR_FILENAME = re.compile(r\"^(\\d{4})-.*\\.md$\")\n+# ``ADR 0016`` / ``SADR-0016`` -- a namespace plus four-digit citation. The\n+# trailing ``\\b`` keeps a five-digit token from being read as a truncated one.\n+_CITATION = re.compile(r\"\\b(S?ADR)[ -](\\d{4})\\b\")\n+# Target ADRs retain the numeric filename; Shiki platform decisions use SADR.\n+_DECISION_FILENAME = re.compile(r\"^(?:(SADR)-)?(\\d{4})-.*\\.md$\")\n \n \n class Violation(NamedTuple):\n     \"\"\"A citation that does not resolve to exactly one decision record.\"\"\"\n \n     citing_file: str  # repo-relative path of the file that cites the ADR\n+    namespace: str  # \"ADR\" for target decisions; \"SADR\" for Shiki decisions\n     number: str  # the cited four-digit number, e.g. \"0016\"\n     line: int  # 1-based line of the citation\n     reason: str  # \"missing\" (zero matches) or \"ambiguous\" (two or more)\n@@ -49,32 +50,34 @@\n \n     @property\n     def expected_path(self) -> str:\n-        return f\"docs/adr/{self.number}-*.md\"\n+        prefix = \"SADR-\" if self.namespace == \"SADR\" else \"\"\n+        return f\"docs/adr/{prefix}{self.number}-*.md\"\n \n     def describe(self) -> str:\n         \"\"\"Name the citing file, the cited number, and the missing path.\"\"\"\n         if self.reason == \"missing\":\n             return (\n-                f\"{self.citing_file}:{self.line} cites ADR {self.number} \"\n+                f\"{self.citing_file}:{self.line} cites {self.namespace} {self.number} \"\n                 f\"but no document matches {self.expected_path}\"\n             )\n         joined = \", \".join(self.matches)\n         return (\n-            f\"{self.citing_file}:{self.line} cites ADR {self.number} which \"\n+            f\"{self.citing_file}:{self.line} cites {self.namespace} {self.number} which \"\n             f\"resolves to {len(self.matches)} documents (expected exactly one \"\n             f\"at {self.expected_path}): {joined}\"\n         )\n \n \n-def _adr_index(root: Path) -> dict[str, list[Path]]:\n-    \"\"\"Map every four-digit ADR number to the record files that claim it.\"\"\"\n-    index: dict[str, list[Path]] = {}\n+def _adr_index(root: Path) -> dict[tuple[str, str], list[Path]]:\n+    \"\"\"Map each decision namespace and number to the files that claim it.\"\"\"\n+    index: dict[tuple[str, str], list[Path]] = {}\n     adr_dir = root / \"docs\" / \"adr\"\n     if adr_dir.is_dir():\n         for path in sorted(adr_dir.glob(\"*.md\")):\n-            match = _ADR_FILENAME.match(path.name)\n+            match = _DECISION_FILENAME.match(path.name)\n             if match:\n-                index.setdefault(match.group(1), []).append(path)\n+                namespace = \"SADR\" if match.group(1) else \"ADR\"\n+                index.setdefault((namespace, match.group(2)), []).append(path)\n     return index\n \n \n@@ -110,13 +113,15 @@\n         rel = path.relative_to(root).as_posix()\n         for lineno, line in enumerate(text.splitlines(), start=1):\n             for match in _CITATION.finditer(line):\n-                number = match.group(1)\n-                found = index.get(number, [])\n+                namespace, number = match.groups()\n+                found = index.get((namespace, number), [])\n                 if len(found) == 1:\n                     continue\n                 reason = \"missing\" if not found else \"ambiguous\"\n                 rels = [p.relative_to(root).as_posix() for p in found]\n-                violations.append(Violation(rel, number, lineno, reason, rels))\n+                violations.append(\n+                    Violation(rel, namespace, number, lineno, reason, rels)\n+                )\n     return violations\n \n \n@@ -146,8 +151,8 @@\n         # The two records this Goal carried onto the default branch, and which\n         # the scripts cite most, must be present.\n         index = _adr_index(REPO_ROOT)\n-        self.assertIn(\"0016\", index)\n-        self.assertIn(\"0017\", index)\n+        self.assertIn((\"ADR\", \"0016\"), index)\n+        self.assertIn((\"ADR\", \"0017\"), index)\n \n \n class FixtureAdrReferences(unittest.TestCase):\n@@ -180,6 +185,17 @@\n         self.assertIn(\"9991\", message)  # cited number\n         self.assertIn(\"docs/adr/9991-*.md\", message)  # missing path\n \n+    def test_target_adr_does_not_satisfy_missing_platform_sadr(self) -> None:\n+        root = self._make_repo(\n+            adr_names=[\"0042-target-decision.md\"],\n+            script_body='\"\"\"Target ADR 0042 exists, but platform SADR-0042 does not.\"\"\"\\n',\n+        )\n+        violations = find_adr_reference_violations(root, include_docs=False)\n+        self.assertEqual(len(violations), 1)\n+        self.assertEqual(violations[0].namespace, \"SADR\")\n+        self.assertEqual(violations[0].number, \"0042\")\n+        self.assertEqual(violations[0].expected_path, \"docs/adr/SADR-0042-*.md\")\n+\n     def test_uncited_adr_is_allowed(self) -> None:\n         # An ADR exists but nothing cites it -> not a violation. The converse\n         # (every ADR must be cited) is explicitly NOT required.\ndiff --git a/tests/test_sadr_install_migration.py b/tests/test_sadr_install_migration.py\nnew file mode 100644\nindex 0000000000000000000000000000000000000000..a6398c81377f00c09aa2cb684980b070cfaf714c\n--- /dev/null\n+++ b/tests/test_sadr_install_migration.py\n@@ -0,0 +1,56 @@\n+\"\"\"SADR installation and collision-safe legacy upgrade behavior.\"\"\"\n+\n+from __future__ import annotations\n+\n+import shutil\n+import tempfile\n+import unittest\n+from pathlib import Path\n+\n+import shiki_test_support  # noqa: F401  (path bootstrap)\n+\n+import shiki_installer\n+from tests.test_adr_references import find_adr_reference_violations\n+\n+\n+PRODUCT_NUMBERS = tuple(range(13, 19))\n+\n+\n+class FreshSadrInstallTests(unittest.TestCase):\n+    def setUp(self) -> None:\n+        self.tmp = Path(tempfile.mkdtemp(prefix=\"shiki-sadr-fresh-\"))\n+        self.addCleanup(lambda: shutil.rmtree(self.tmp, ignore_errors=True))\n+        self.target = self.tmp / \"target\"\n+        self.adr_dir = self.target / \"docs\" / \"adr\"\n+        self.adr_dir.mkdir(parents=True)\n+        self.product_bytes = {\n+            f\"{number:04d}-product-decision.md\": (\n+                f\"# Product ADR {number:04d}\\n\\nTarget-owned bytes {number}.\\n\"\n+            ).encode()\n+            for number in PRODUCT_NUMBERS\n+        }\n+        for name, content in self.product_bytes.items():\n+            (self.adr_dir / name).write_bytes(content)\n+\n+    def test_fresh_install_preserves_product_adrs_and_installs_all_sadrs(self) -> None:\n+        shiki_installer.install_template(self.target, force=False, validate=True)\n+\n+        for name, expected in self.product_bytes.items():\n+            self.assertEqual((self.adr_dir / name).read_bytes(), expected)\n+\n+        sadr_numbers = {\n+            path.name.split(\"-\", 2)[1]\n+            for path in self.adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\")\n+        }\n+        self.assertEqual(sadr_numbers, {f\"{number:04d}\" for number in range(1, 20)})\n+\n+        numeric_records = {\n+            path.name\n+            for path in self.adr_dir.glob(\"[0-9][0-9][0-9][0-9]-*.md\")\n+        }\n+        self.assertEqual(numeric_records, set(self.product_bytes))\n+        self.assertEqual(find_adr_reference_violations(self.target), [])\n+\n+\n+if __name__ == \"__main__\":\n+    unittest.main()\n\n2026-08-07T08:08:45.671282Z ERROR codex_models_manager::manager: failed to renew cache TTL: missing field `base_instructions` at line 94 column 5\nexec\n/bin/zsh -lc \"rg -n --glob '\"'!.shiki/**'\"' 'docs/adr/.*hard-to-reverse|Architecture Decision Records|\\\\bADRs\\\\b|new ADR|an ADR|ADR when|ADR to' AGENTS.md CLAUDE.md CONTEXT.md CONTRIBUTING.md SYSTEM_PROMPT.md docs .github .codex scripts pyproject.toml | sed -n '1,320p'\" in /private/tmp/shiki-adr-collision.pa0KwY/.worktrees/shiki-t-20260807t073307221254z-2d22d654-introduc\n succeeded in 0ms:\nCONTRIBUTING.md:20:   domain language, ADRs, and code reality before writing a PRD.\nCONTRIBUTING.md:97:  GitHub, `.shiki/`, `CONTEXT.md`, or an ADR.\nCONTRIBUTING.md:106:record durable decisions in an ADR when needed (see `docs/adr/`).\nSYSTEM_PROMPT.md:29:4. `docs/adr/` records hard-to-reverse platform decisions.\nSYSTEM_PROMPT.md:58:2. **grill-with-docs**: challenge the plan against `CONTEXT.md`, ADRs, code reality, terminology, boundaries, and edge scenarios. Ask one question at a time when the operator is available. If code can answer a question, inspect code instead of asking.\nSYSTEM_PROMPT.md:78:2. Read `CONTEXT.md` and relevant ADRs.\nSYSTEM_PROMPT.md:106:- Relevant documents, ADRs, domain terms, and prior decisions.\nSYSTEM_PROMPT.md:155:- `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant ADRs.\nSYSTEM_PROMPT.md:217:When the architecture gate triggers, run `grill-with-docs` or `improve-codebase-architecture` as appropriate and record the decision in an ADR if it is hard to reverse or likely to be re-litigated.\nCONTEXT.md:28:A planning skill that challenges a plan against domain language, ADRs, code reality, and concrete edge scenarios. It resolves design-tree questions before PRD/issues. It runs inside Requirements Definition.\nCLAUDE.md:13:4. `docs/adr/` records hard-to-reverse platform decisions.\nCLAUDE.md:60:2. Read `CONTEXT.md` and relevant ADRs.\nCLAUDE.md:77:- Challenge hard-to-reverse tradeoffs against ADRs.\nCLAUDE.md:97:- Offer ADRs only when the decision is hard to reverse, surprising without context, and a real tradeoff.\nCLAUDE.md:124:- Relevant docs, ADRs, modules, and tests.\nCLAUDE.md:155:- Read `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant ADRs, PRD, task issue, PR body, diff, checks, reviews, labels, and `.shiki/` state.\nAGENTS.md:43:4. `docs/adr/` records hard-to-reverse platform decisions.\nAGENTS.md:50:Conversation state is not durable truth. If a decision matters, put it in GitHub, `.shiki/`, `CONTEXT.md`, or an ADR.\nAGENTS.md:78:2. **grill-with-docs**: challenge the plan against domain docs, ADRs, code reality, terminology, and edge scenarios.\nAGENTS.md:79:3. **Context & Impact**: find relevant docs, ADRs, code, dependencies, owners, locks, and verification surfaces.\nAGENTS.md:170:- Links to relevant domain terms and ADRs.\nAGENTS.md:196:- Relevant documents, domain terms, ADRs, and past decisions.\nAGENTS.md:351:When the architecture gate triggers, run `grill-with-docs` or `improve-codebase-architecture` as appropriate and record durable decisions in an ADR when needed.\n.codex/skills/shiki/SKILL.md:17:4. `docs/adr/` records hard-to-reverse platform decisions.\n.github/prompts/codex-task-handoff.md:5:Follow `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant ADRs, and `docs/agents/codex-handoff.md`.\n.github/prompts/cca-completion-check.md:18:4. `docs/adr/` records hard-to-reverse platform decisions.\ndocs/agents/implementation-policy.md:30:4. `docs/adr/` records hard-to-reverse platform decisions.\ndocs/agents/implementation-policy.md:37:2. **Domain language is controlled.** Terms must be checked against `CONTEXT.md`; hard-to-reverse tradeoffs must be checked against ADRs.\ndocs/agents/implementation-policy.md:98:- Check ADRs before reopening decisions.\ndocs/agents/implementation-policy.md:103:- Create ADRs only for hard-to-reverse, surprising, tradeoff-based decisions.\ndocs/agents/implementation-policy.md:110:- ADRs when justified.\ndocs/agents/implementation-policy.md:120:- Relevant documents and ADRs.\ndocs/agents/implementation-policy.md:143:- Links to relevant Goal, domain terms, and ADRs.\ndocs/agents/runtime-auth-model.md:151:If a repo opts into API-key mode, record the exception in an ADR and update the repo's `AGENTS.md`, `.shiki/config.yaml`, and workflow permissions.\ndocs/agents/domain.md:8:- `docs/adr/` records hard-to-reverse decisions.\ndocs/agents/domain.md:18:- Create ADRs only for hard-to-reverse, surprising, tradeoff-based decisions.\ndocs/agents/domain.md:20:- If an ADR conflicts with a proposed implementation, surface the conflict before editing.\ndocs/agents/completion-check-agent.md:35:- relevant ADRs\ndocs/agents/codex-handoff.md:49:- Relevant ADRs:\ndocs/agents/skill-gate.md:12:| `grill-with-docs` | Ambiguous plan, unclear terminology, boundary decision, tradeoff, or ADR-worthy design choice. | Clarified decision tree, recommended answers, updates to `CONTEXT.md` or ADRs when decisions crystallize. |\ndocs/agents/skill-gate.md:20:| `improve-codebase-architecture` | Structural friction, testability, AI-navigability, deep-module opportunity, or refactor request. | Architecture candidates and deepening recommendations informed by `CONTEXT.md` and ADRs. |\ndocs/agents/checklists.md:35:| GD-02 | Relevant ADRs were read or absence was recorded. | blocking for non-trivial Goals |\ndocs/agents/checklists.md:46:| CI-01 | Relevant docs and ADRs identified. | blocking |\ndocs/agents/checklists.md:101:| H-05 | Relevant docs, ADRs, modules, and tests are referenced. | blocking |\nscripts/shiki_contracts.py:103:    \"`docs/adr/` records hard-to-reverse platform decisions.\",\nscripts/shiki_guardian_review.py:162:            \"Docs/ADR-only change: confirm the change is genuinely documentation/decision-only, introduces no behavior or contract drift, and stays consistent with the constitution and existing ADRs.\"\ndocs/adr/SADR-0013-bootstrap-grants-actions-approve-pull-requests.md:34:what default token scope \u2014 is an ADR-worthy decision, not an implementation\ndocs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md:1:# SADR-0019: Separate Shiki Platform SADRs From Target ADRs\ndocs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md:11:`NNNN-<slug>.md` namespace. A target that already owned ADRs 0013 through 0018\ndocs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md:54:- Renumbering target ADRs mutates target-owned history.\ndocs/adr/SADR-0007-packaging-and-release.md:37:as an ADR.\ndocs/adr/README.md:1:# Architecture Decision Records\ndocs/adr/README.md:3:This directory holds Shiki's Architecture Decision Records (ADRs): durable\ndocs/adr/README.md:4:records of hard-to-reverse platform decisions. ADRs are part of the Shiki source\ndocs/adr/README.md:7:## How ADRs Work\ndocs/adr/README.md:9:- New ADRs are added as `docs/adr/NNNN-<short-slug>.md`, where `NNNN` is the next\ndocs/adr/README.md:11:- The **canonical list of ADRs is the set of files matching `docs/adr/*.md`** in\ndocs/adr/README.md:13:- Use [`template.md`](template.md) as the starting point for a new ADR.\ndocs/adr/README.md:14:- Record an ADR when a decision is hard to reverse, surprising without context,\ndocs/adr/README.md:15:  and a real tradeoff. Do not write an ADR for routine or easily reversible\ndocs/adr/README.md:22:## Current ADRs (snapshot)\ndocs/adr/README.md:40:   the ADR to be discoverable via `docs/adr/*.md`.\ndocs/adr/template.md:10:relevant domain terms (`CONTEXT.md`), prior ADRs, and code reality. State the\n\ndiff --git a/.codex/skills/shiki/SKILL.md b/.codex/skills/shiki/SKILL.md\nindex c8e2e85d9d75d49cc4f2d6286ee22659c4e53e75..ca8ff572d92424c2651f16e45232f251b4b8082d\n--- a/.codex/skills/shiki/SKILL.md\n+++ b/.codex/skills/shiki/SKILL.md\n@@ -69,7 +69,7 @@\n \n ## Responsibilities\n \n-- Claude Code implements and repairs by default (`shiki runner claude`, ADR 0008); Codex implements and repairs tasks explicitly assigned to `codex`.\n+- Claude Code implements and repairs by default (`shiki runner claude`, SADR-0008); Codex implements and repairs tasks explicitly assigned to `codex`.\n - Claude Code Action can act as GitHub-side CCA or reviewer.\n - CCA judges completion.\n - MergeGate authorizes state transitions and merge readiness.\ndiff --git a/docs/adr/0001-keep-shiki-as-platform-name.md b/docs/adr/SADR-0001-keep-shiki-as-platform-name.md\nindex 20a9637c3b9d8c97de7ba8db7cfae4d82d9a7dba..8b685439959aada1b221c112f0ac3c51ca8a1695\n--- a/docs/adr/0001-keep-shiki-as-platform-name.md\n+++ b/docs/adr/SADR-0001-keep-shiki-as-platform-name.md\n@@ -1,3 +1,3 @@\n-# Keep Shiki As The Platform Name\n+# SADR-0001: Keep Shiki As The Platform Name\n \n Shiki remains the product name for the GitHub-native agentic engineering control plane. We considered more descriptive names such as MergePilot, MergeGate, and Kiroku, but kept Shiki because it carries the existing goal-seeking framework identity while remaining independent of Hermes, Codex, Claude Code, or any single runtime.\ndiff --git a/docs/adr/0002-use-agents-as-shared-constitution.md b/docs/adr/SADR-0002-use-agents-as-shared-constitution.md\nindex 73de30ecb078120eac540d9bc51855e3cd48d0e3..c5486182bf06fca36ea508496b013c67302fd989\n--- a/docs/adr/0002-use-agents-as-shared-constitution.md\n+++ b/docs/adr/SADR-0002-use-agents-as-shared-constitution.md\n@@ -1,3 +1,3 @@\n-# Use AGENTS As The Shared Agent Constitution\n+# SADR-0002: Use AGENTS As The Shared Agent Constitution\n \n Shiki uses `AGENTS.md` as the runtime-neutral constitution and `CLAUDE.md` as a thin Claude Code wrapper. This keeps Codex, Claude Code, Hermes Runner, and future Agent Runtimes aligned on the same GitHub-first MergeGate rules while still allowing runtime-specific instructions where needed.\ndiff --git a/docs/adr/0003-require-skill-gate-for-mergegate-readiness.md b/docs/adr/SADR-0003-require-skill-gate-for-mergegate-readiness.md\nindex 424be0c9af4c8f0136f1fde68a468c47ff84e764..7fc337987c7df9792f75f181dfcbed75fe5ffff2\n--- a/docs/adr/0003-require-skill-gate-for-mergegate-readiness.md\n+++ b/docs/adr/SADR-0003-require-skill-gate-for-mergegate-readiness.md\n@@ -1,3 +1,3 @@\n-# Require Skill Gate For MergeGate Readiness\n+# SADR-0003: Require Skill Gate For MergeGate Readiness\n \n Shiki requires relevant engineering skills to be invoked before execution when their triggers apply. This makes skills part of MergeGate readiness rather than optional prompt style, because Shiki depends on repeatable planning, issue decomposition, TDD, diagnosis, triage, and architecture review before autonomous or parallel agent work can be trusted.\ndiff --git a/docs/adr/0004-use-github-first-with-shiki-mirror.md b/docs/adr/SADR-0004-use-github-first-with-shiki-mirror.md\nindex d0a3a9174dc646a945a9d325954ffaf00c352040..a79c54ae9ee8eed2a0524192b864da4e22a92d3f\n--- a/docs/adr/0004-use-github-first-with-shiki-mirror.md\n+++ b/docs/adr/SADR-0004-use-github-first-with-shiki-mirror.md\n@@ -1,4 +1,4 @@\n-# ADR 0004: Use GitHub First With A Repository-Local Shiki Mirror\n+# SADR-0004: Use GitHub First With A Repository-Local Shiki Mirror\n \n ## Status\n \ndiff --git a/docs/adr/0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md b/docs/adr/SADR-0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\nindex b339baf49b89d9290e79c3d51b36e7b2bde3bd95..c30a0d8b1fde0224d0ac7820e7870dccf721580d\n--- a/docs/adr/0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\n+++ b/docs/adr/SADR-0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\n@@ -1,4 +1,4 @@\n-# ADR 0005: Use Codex As Front And Claude Code Action As The GitHub Runtime\n+# SADR-0005: Use Codex As Front And Claude Code Action As The GitHub Runtime\n \n ## Status\n \ndiff --git a/docs/adr/0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md b/docs/adr/SADR-0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\nindex 4bcae9d1e52f0a37b8e9fbafa5cd14b6ee1993bd..7a38538a6ae9929567f0ade67e7ea6a60d0b3f7e\n--- a/docs/adr/0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\n+++ b/docs/adr/SADR-0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\n@@ -1,4 +1,4 @@\n-# ADR 0006: Separate Codex Implementation, CCA Completion, And MergeGate Authorization\n+# SADR-0006: Separate Codex Implementation, CCA Completion, And MergeGate Authorization\n \n ## Status\n \ndiff --git a/docs/adr/0007-packaging-and-release.md b/docs/adr/SADR-0007-packaging-and-release.md\nindex 79b84ddb97167372b44641e5b7860e400ce80822..023cd57f8011aa6e6d2ebf612b7c35a7a82c00d4\n--- a/docs/adr/0007-packaging-and-release.md\n+++ b/docs/adr/SADR-0007-packaging-and-release.md\n@@ -1,4 +1,4 @@\n-# ADR 0007: Packaging And Release For Shiki\n+# SADR-0007: Packaging And Release For Shiki\n \n ## Status\n \ndiff --git a/docs/adr/0008-use-claude-code-as-default-implementer.md b/docs/adr/SADR-0008-use-claude-code-as-default-implementer.md\nindex dedf4510b7c7799521422f622029563d373db031..a158a8d444d93764ec515a28133f7dde4af23579\n--- a/docs/adr/0008-use-claude-code-as-default-implementer.md\n+++ b/docs/adr/SADR-0008-use-claude-code-as-default-implementer.md\n@@ -1,4 +1,4 @@\n-# ADR 0008: Use Claude Code As The Default Implementer Runtime\n+# SADR-0008: Use Claude Code As The Default Implementer Runtime\n \n ## Status\n \ndiff --git a/docs/adr/0009-requirements-definition-and-spec-freeze.md b/docs/adr/SADR-0009-requirements-definition-and-spec-freeze.md\nindex 29b3f7871bef9203a8b4a831a65c39eb2f506e42..52e7dd69ad0aa4efe0b68c62678abbf7df02aaec\n--- a/docs/adr/0009-requirements-definition-and-spec-freeze.md\n+++ b/docs/adr/SADR-0009-requirements-definition-and-spec-freeze.md\n@@ -1,4 +1,4 @@\n-# ADR 0009: Merge Grill And Planning Into Requirements Definition With Spec Freeze\n+# SADR-0009: Merge Grill And Planning Into Requirements Definition With Spec Freeze\n \n ## Status\n \ndiff --git a/docs/adr/0010-external-ai-guardian-review.md b/docs/adr/SADR-0010-external-ai-guardian-review.md\nindex e6108bbe8bb4e73e0ff4684c8f78001a80438c4e..e690d4eddd270e683d709c9b679c5859e71eeaaa\n--- a/docs/adr/0010-external-ai-guardian-review.md\n+++ b/docs/adr/SADR-0010-external-ai-guardian-review.md\n@@ -1,4 +1,4 @@\n-# ADR 0010: External AI Guardian Review As A First-Class Approval Authority\n+# SADR-0010: External AI Guardian Review As A First-Class Approval Authority\n \n ## Status\n \ndiff --git a/docs/adr/0011-autonomous-quality-gate-evidence.md b/docs/adr/SADR-0011-autonomous-quality-gate-evidence.md\nindex bca0246c5d46e44afbde15486a75eec425fc02f5..9c93eac16d8c3a6d4b11bd7224e3955102cd7db4\n--- a/docs/adr/0011-autonomous-quality-gate-evidence.md\n+++ b/docs/adr/SADR-0011-autonomous-quality-gate-evidence.md\n@@ -1,4 +1,4 @@\n-# ADR 0011: Autonomous Quality-Gate Evidence \u2014 Deterministic Facts And An Independent Verifier\n+# SADR-0011: Autonomous Quality-Gate Evidence \u2014 Deterministic Facts And An Independent Verifier\n \n ## Status\n \ndiff --git a/docs/adr/0012-autonomous-loop-closeout-to-main.md b/docs/adr/SADR-0012-autonomous-loop-closeout-to-main.md\nindex 637fad6dde4989c9c2bf0f81f676d8d29991123c..ec7d85f81030a676bcbba28b43eb365422a36eac\n--- a/docs/adr/0012-autonomous-loop-closeout-to-main.md\n+++ b/docs/adr/SADR-0012-autonomous-loop-closeout-to-main.md\n@@ -1,4 +1,4 @@\n-# 0012 \u2014 Autonomous loop pushes goal completion to main via a closeout PR\n+# SADR-0012 \u2014 Autonomous loop pushes goal completion to main via a closeout PR\n \n - Status: accepted\n - Date: 2026-06-18\ndiff --git a/docs/adr/0013-bootstrap-grants-actions-approve-pull-requests.md b/docs/adr/SADR-0013-bootstrap-grants-actions-approve-pull-requests.md\nindex b6c69d64f1c5ced346b88d6859586715b3b5bd0e..a793e7277d67ab8ea56286fec289ca689825a17d\n--- a/docs/adr/0013-bootstrap-grants-actions-approve-pull-requests.md\n+++ b/docs/adr/SADR-0013-bootstrap-grants-actions-approve-pull-requests.md\n@@ -1,4 +1,4 @@\n-# ADR 0013: Bootstrap Grants GitHub Actions Permission To Create And Approve Pull Requests\n+# SADR-0013: Bootstrap Grants GitHub Actions Permission To Create And Approve Pull Requests\n \n ## Status\n \ndiff --git a/docs/adr/0014-codex-app-external-ai-guardian-ui-adapter.md b/docs/adr/SADR-0014-codex-app-external-ai-guardian-ui-adapter.md\nindex 238db06279334caa341c117362df9271382a7fcd..65f12e9939321e1b3e389a34c822fc1549e5155d\n--- a/docs/adr/0014-codex-app-external-ai-guardian-ui-adapter.md\n+++ b/docs/adr/SADR-0014-codex-app-external-ai-guardian-ui-adapter.md\n@@ -1,4 +1,4 @@\n-# ADR 0014: Use Codex App As The External AI Guardian UI Adapter\n+# SADR-0014: Use Codex App As The External AI Guardian UI Adapter\n \n ## Status\n \ndiff --git a/docs/adr/0015-contract-approval-before-dispatch.md b/docs/adr/SADR-0015-contract-approval-before-dispatch.md\nindex fad6c656741a20a7684ba16080a7db159f63a52f..7bdd760bb3e82eee4e1410f45c783b03b638d456\n--- a/docs/adr/0015-contract-approval-before-dispatch.md\n+++ b/docs/adr/SADR-0015-contract-approval-before-dispatch.md\n@@ -1,4 +1,4 @@\n-# ADR 0015: Approve The Task Contract Before Dispatch, Not The Diff After It\n+# SADR-0015: Approve The Task Contract Before Dispatch, Not The Diff After It\n \n ## Status\n \ndiff --git a/docs/adr/0016-locks-bound-product-paths.md b/docs/adr/SADR-0016-locks-bound-product-paths.md\nindex b5e850f3f1f7f4ef6e7f9d1763a29a51bd300c45..332cc17313498404f41a145291e38447b5de0dc4\n--- a/docs/adr/0016-locks-bound-product-paths.md\n+++ b/docs/adr/SADR-0016-locks-bound-product-paths.md\n@@ -1,4 +1,4 @@\n-# ADR 0016: Locks bound product paths; the Shiki mirror is bound by record identity\n+# SADR-0016: Locks bound product paths; the Shiki mirror is bound by record identity\n \n ## Status\n \ndiff --git a/docs/adr/0017-bookkeeping-closeouts-do-not-inherit-risk.md b/docs/adr/SADR-0017-bookkeeping-closeouts-do-not-inherit-risk.md\nindex 554e3a0d526a72ca067108c5464cc68c3edcf174..83115f69f1b1b48d544c9b0157f377b91cb53abf\n--- a/docs/adr/0017-bookkeeping-closeouts-do-not-inherit-risk.md\n+++ b/docs/adr/SADR-0017-bookkeeping-closeouts-do-not-inherit-risk.md\n@@ -1,4 +1,4 @@\n-# ADR 0017: A Bookkeeping Closeout Does Not Inherit Its Task's Risk\n+# SADR-0017: A Bookkeeping Closeout Does Not Inherit Its Task's Risk\n \n ## Status\n \ndiff --git a/docs/adr/0018-carried-guardian-approval-across-a-proven-base-sync.md b/docs/adr/SADR-0018-carried-guardian-approval-across-a-proven-base-sync.md\nindex 22358562da390ed8b45cd600da08a43d378b4680..38da0fd95102c3496c3219ce2319fce696a2532a\n--- a/docs/adr/0018-carried-guardian-approval-across-a-proven-base-sync.md\n+++ b/docs/adr/SADR-0018-carried-guardian-approval-across-a-proven-base-sync.md\n@@ -1,4 +1,4 @@\n-# ADR 0018: Carried Guardian Approval Across A Proven Base Sync\n+# SADR-0018: Carried Guardian Approval Across A Proven Base Sync\n \n ## Status\n \ndiff --git a/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md b/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\nnew file mode 100644\nindex 0000000000000000000000000000000000000000..7455ce1f3bed02ddddb851ac21a1d1a648c9aeba\n--- /dev/null\n+++ b/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\n@@ -0,0 +1,55 @@\n+# SADR-0019: Separate Shiki Platform SADRs From Target ADRs\n+\n+## Status\n+\n+Accepted\n+\n+## Context\n+\n+Shiki installs its platform decision records into `docs/adr/` in every target\n+repository. Shiki and its targets previously used the same numeric\n+`NNNN-<slug>.md` namespace. A target that already owned ADRs 0013 through 0018\n+therefore acquired two records for each number during installation, making an\n+`ADR NNNN` citation ambiguous. Renumbering target decisions would rewrite target\n+history, while renumbering Shiki decisions per target would destroy their stable\n+platform identity.\n+\n+Upgrades also need to remove the former Shiki-owned numeric paths without ever\n+mistaking a target-owned ADR for platform content. The install stamp already\n+records exact shipped paths and content digests, so it can prove ownership only\n+when both still match.\n+\n+## Decision\n+\n+Shiki platform decisions use the `SADR` namespace:\n+\n+- records are named `docs/adr/SADR-NNNN-<slug>.md`;\n+- live platform citations use `SADR-NNNN`;\n+- target product decisions retain `docs/adr/NNNN-<slug>.md` and `ADR NNNN`;\n+- the reference validator resolves the two namespaces independently, so the same\n+  four-digit number may exist once in each namespace.\n+\n+Before a forced upgrade writes anything, the installer inventories the exact\n+former numeric Shiki ADR paths. It may delete a legacy path only when the\n+existing install stamp contains that exact path and its recorded SHA-256 digest\n+matches the file currently on disk. All ownership blockers are reported\n+together and abort the upgrade before its first write. A non-forced install\n+never performs this cleanup.\n+\n+Historical `.shiki/` evidence remains unchanged because it records the namespace\n+that existed when that evidence was produced.\n+\n+## Consequences\n+\n+- Targets may use any ADR number without colliding with Shiki's platform SADRs.\n+- Fresh installs preserve target ADR bytes and install a stable SADR set.\n+- Forced upgrades fail closed when legacy ownership cannot be proven, and a\n+  successful upgrade refreshes the stamp without removed legacy paths.\n+- Current governance, packaging, workflow, prompt, script, and skill references\n+  must move with the platform records.\n+\n+## Alternatives Considered\n+\n+- Collision-only refusal leaves affected targets permanently unupgradable.\n+- Renumbering target ADRs mutates target-owned history.\n+- Per-target Shiki renumbering makes platform citations non-portable.\ndiff --git a/tests/test_adr_references.py b/tests/test_adr_references.py\nindex 51706329c797d5dd9b47e082a333fa83b261d482..92569d9fa25e22cfd71c17561d715485027a0d2d\n--- a/tests/test_adr_references.py\n+++ b/tests/test_adr_references.py\n@@ -31,17 +31,18 @@\n \n REPO_ROOT = Path(__file__).resolve().parents[1]\n \n-# \"ADR 0016\" -- a four-digit citation. The trailing \\b keeps a five-digit token\n-# from being read as a truncated four-digit number.\n-_CITATION = re.compile(r\"\\bADR (\\d{4})\\b\")\n-# \"0016-locks-bound-product-paths.md\" -- the leading four digits name the record.\n-_ADR_FILENAME = re.compile(r\"^(\\d{4})-.*\\.md$\")\n+# ``ADR 0016`` / ``SADR-0016`` -- a namespace plus four-digit citation. The\n+# trailing ``\\b`` keeps a five-digit token from being read as a truncated one.\n+_CITATION = re.compile(r\"\\b(S?ADR)[ -](\\d{4})\\b\")\n+# Target ADRs retain the numeric filename; Shiki platform decisions use SADR.\n+_DECISION_FILENAME = re.compile(r\"^(?:(SADR)-)?(\\d{4})-.*\\.md$\")\n \n \n class Violation(NamedTuple):\n     \"\"\"A citation that does not resolve to exactly one decision record.\"\"\"\n \n     citing_file: str  # repo-relative path of the file that cites the ADR\n+    namespace: str  # \"ADR\" for target decisions; \"SADR\" for Shiki decisions\n     number: str  # the cited four-digit number, e.g. \"0016\"\n     line: int  # 1-based line of the citation\n     reason: str  # \"missing\" (zero matches) or \"ambiguous\" (two or more)\n@@ -49,32 +50,34 @@\n \n     @property\n     def expected_path(self) -> str:\n-        return f\"docs/adr/{self.number}-*.md\"\n+        prefix = \"SADR-\" if self.namespace == \"SADR\" else \"\"\n+        return f\"docs/adr/{prefix}{self.number}-*.md\"\n \n     def describe(self) -> str:\n         \"\"\"Name the citing file, the cited number, and the missing path.\"\"\"\n         if self.reason == \"missing\":\n             return (\n-                f\"{self.citing_file}:{self.line} cites ADR {self.number} \"\n+                f\"{self.citing_file}:{self.line} cites {self.namespace} {self.number} \"\n                 f\"but no document matches {self.expected_path}\"\n             )\n         joined = \", \".join(self.matches)\n         return (\n-            f\"{self.citing_file}:{self.line} cites ADR {self.number} which \"\n+            f\"{self.citing_file}:{self.line} cites {self.namespace} {self.number} which \"\n             f\"resolves to {len(self.matches)} documents (expected exactly one \"\n             f\"at {self.expected_path}): {joined}\"\n         )\n \n \n-def _adr_index(root: Path) -> dict[str, list[Path]]:\n-    \"\"\"Map every four-digit ADR number to the record files that claim it.\"\"\"\n-    index: dict[str, list[Path]] = {}\n+def _adr_index(root: Path) -> dict[tuple[str, str], list[Path]]:\n+    \"\"\"Map each decision namespace and number to the files that claim it.\"\"\"\n+    index: dict[tuple[str, str], list[Path]] = {}\n     adr_dir = root / \"docs\" / \"adr\"\n     if adr_dir.is_dir():\n         for path in sorted(adr_dir.glob(\"*.md\")):\n-            match = _ADR_FILENAME.match(path.name)\n+            match = _DECISION_FILENAME.match(path.name)\n             if match:\n-                index.setdefault(match.group(1), []).append(path)\n+                namespace = \"SADR\" if match.group(1) else \"ADR\"\n+                index.setdefault((namespace, match.group(2)), []).append(path)\n     return index\n \n \n@@ -110,13 +113,15 @@\n         rel = path.relative_to(root).as_posix()\n         for lineno, line in enumerate(text.splitlines(), start=1):\n             for match in _CITATION.finditer(line):\n-                number = match.group(1)\n-                found = index.get(number, [])\n+                namespace, number = match.groups()\n+                found = index.get((namespace, number), [])\n                 if len(found) == 1:\n                     continue\n                 reason = \"missing\" if not found else \"ambiguous\"\n                 rels = [p.relative_to(root).as_posix() for p in found]\n-                violations.append(Violation(rel, number, lineno, reason, rels))\n+                violations.append(\n+                    Violation(rel, namespace, number, lineno, reason, rels)\n+                )\n     return violations\n \n \n@@ -146,8 +151,8 @@\n         # The two records this Goal carried onto the default branch, and which\n         # the scripts cite most, must be present.\n         index = _adr_index(REPO_ROOT)\n-        self.assertIn(\"0016\", index)\n-        self.assertIn(\"0017\", index)\n+        self.assertIn((\"ADR\", \"0016\"), index)\n+        self.assertIn((\"ADR\", \"0017\"), index)\n \n \n class FixtureAdrReferences(unittest.TestCase):\n@@ -180,6 +185,17 @@\n         self.assertIn(\"9991\", message)  # cited number\n         self.assertIn(\"docs/adr/9991-*.md\", message)  # missing path\n \n+    def test_target_adr_does_not_satisfy_missing_platform_sadr(self) -> None:\n+        root = self._make_repo(\n+            adr_names=[\"0042-target-decision.md\"],\n+            script_body='\"\"\"Target ADR 0042 exists, but platform SADR-0042 does not.\"\"\"\\n',\n+        )\n+        violations = find_adr_reference_violations(root, include_docs=False)\n+        self.assertEqual(len(violations), 1)\n+        self.assertEqual(violations[0].namespace, \"SADR\")\n+        self.assertEqual(violations[0].number, \"0042\")\n+        self.assertEqual(violations[0].expected_path, \"docs/adr/SADR-0042-*.md\")\n+\n     def test_uncited_adr_is_allowed(self) -> None:\n         # An ADR exists but nothing cites it -> not a violation. The converse\n         # (every ADR must be cited) is explicitly NOT required.\ndiff --git a/tests/test_sadr_install_migration.py b/tests/test_sadr_install_migration.py\nnew file mode 100644\nindex 0000000000000000000000000000000000000000..a6398c81377f00c09aa2cb684980b070cfaf714c\n--- /dev/null\n+++ b/tests/test_sadr_install_migration.py\n@@ -0,0 +1,56 @@\n+\"\"\"SADR installation and collision-safe legacy upgrade behavior.\"\"\"\n+\n+from __future__ import annotations\n+\n+import shutil\n+import tempfile\n+import unittest\n+from pathlib import Path\n+\n+import shiki_test_support  # noqa: F401  (path bootstrap)\n+\n+import shiki_installer\n+from tests.test_adr_references import find_adr_reference_violations\n+\n+\n+PRODUCT_NUMBERS = tuple(range(13, 19))\n+\n+\n+class FreshSadrInstallTests(unittest.TestCase):\n+    def setUp(self) -> None:\n+        self.tmp = Path(tempfile.mkdtemp(prefix=\"shiki-sadr-fresh-\"))\n+        self.addCleanup(lambda: shutil.rmtree(self.tmp, ignore_errors=True))\n+        self.target = self.tmp / \"target\"\n+        self.adr_dir = self.target / \"docs\" / \"adr\"\n+        self.adr_dir.mkdir(parents=True)\n+        self.product_bytes = {\n+            f\"{number:04d}-product-decision.md\": (\n+                f\"# Product ADR {number:04d}\\n\\nTarget-owned bytes {number}.\\n\"\n+            ).encode()\n+            for number in PRODUCT_NUMBERS\n+        }\n+        for name, content in self.product_bytes.items():\n+            (self.adr_dir / name).write_bytes(content)\n+\n+    def test_fresh_install_preserves_product_adrs_and_installs_all_sadrs(self) -> None:\n+        shiki_installer.install_template(self.target, force=False, validate=True)\n+\n+        for name, expected in self.product_bytes.items():\n+            self.assertEqual((self.adr_dir / name).read_bytes(), expected)\n+\n+        sadr_numbers = {\n+            path.name.split(\"-\", 2)[1]\n+            for path in self.adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\")\n+        }\n+        self.assertEqual(sadr_numbers, {f\"{number:04d}\" for number in range(1, 20)})\n+\n+        numeric_records = {\n+            path.name\n+            for path in self.adr_dir.glob(\"[0-9][0-9][0-9][0-9]-*.md\")\n+        }\n+        self.assertEqual(numeric_records, set(self.product_bytes))\n+        self.assertEqual(find_adr_reference_violations(self.target), [])\n+\n+\n+if __name__ == \"__main__\":\n+    unittest.main()\n\n2026-08-07T08:08:50.125683Z ERROR codex_models_manager::manager: failed to renew cache TTL: missing field `base_instructions` at line 94 column 5\napply patch\n2026-08-07T08:09:07.287423Z ERROR codex_models_manager::cache: failed to load models cache: missing field `base_instructions` at line 94 column 5\n2026-08-07T08:09:07.309550Z ERROR codex_models_manager::cache: failed to load models cache: missing field `base_instructions` at line 94 column 5\n2026-08-07T08:09:10.539016Z  WARN codex_protocol::openai_models: Model personality requested but model_messages is missing, falling back to base instructions. model=codex-auto-review personality=pragmatic\npatch: completed\n/private/tmp/shiki-adr-collision.pa0KwY/.worktrees/shiki-t-20260807t073307221254z-2d22d654-introduc/.codex/skills/shiki/SKILL.md\n/private/tmp/shiki-adr-collision.pa0KwY/.worktrees/shiki-t-20260807t073307221254z-2d22d654-introduc/.github/prompts/cca-completion-check.md\n/private/tmp/shiki-adr-collision.pa0KwY/.worktrees/shiki-t-20260807t073307221254z-2d22d654-introduc/AGENTS.md\n/private/tmp/shiki-adr-collision.pa0KwY/.worktrees/shiki-t-20260807t073307221254z-2d22d654-introduc/CLAUDE.md\n/private/tmp/shiki-adr-collision.pa0KwY/.worktrees/shiki-t-20260807t073307221254z-2d22d654-introduc/SYSTEM_PROMPT.md\n/private/tmp/shiki-adr-collision.pa0KwY/.worktrees/shiki-t-20260807t073307221254z-2d22d654-introduc/docs/agents/domain.md\n/private/tmp/shiki-adr-collision.pa0KwY/.worktrees/shiki-t-20260807t073307221254z-2d22d654-introduc/docs/agents/implementation-policy.md\n/private/tmp/shiki-adr-collision.pa0KwY/.worktrees/shiki-t-20260807t073307221254z-2d22d654-introduc/scripts/shiki_contracts.py\ndiff --git a/.codex/skills/shiki/SKILL.md b/.codex/skills/shiki/SKILL.md\nindex c8e2e85d9d75d49cc4f2d6286ee22659c4e53e75..c392c28442311b8851ede2cf66af14011d8038d4\n--- a/.codex/skills/shiki/SKILL.md\n+++ b/.codex/skills/shiki/SKILL.md\n@@ -14,7 +14,7 @@\n 1. GitHub Issues, Pull Requests, Checks, Reviews, comments, and merge evidence are the operational source of truth.\n 2. The repository-local `.shiki/` mirror records Goals, PRDs, plans, Task DAGs, contracts, locks, ledger entries, CCA verdicts, repair packets, reports, and handoffs.\n 3. `CONTEXT.md` defines Shiki domain language and glossary decisions.\n-4. `docs/adr/` records hard-to-reverse platform decisions.\n+4. `docs/adr/SADR-NNNN-*.md` records Shiki platform decisions; `docs/adr/NNNN-*.md` is reserved for target ADRs.\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n@@ -69,7 +69,7 @@\n \n ## Responsibilities\n \n-- Claude Code implements and repairs by default (`shiki runner claude`, ADR 0008); Codex implements and repairs tasks explicitly assigned to `codex`.\n+- Claude Code implements and repairs by default (`shiki runner claude`, SADR-0008); Codex implements and repairs tasks explicitly assigned to `codex`.\n - Claude Code Action can act as GitHub-side CCA or reviewer.\n - CCA judges completion.\n - MergeGate authorizes state transitions and merge readiness.\ndiff --git a/.github/prompts/cca-completion-check.md b/.github/prompts/cca-completion-check.md\nindex ed203ad5f302ecc81372d8ab27d2b1232ca97497..3137954883be3c4657b40ea1a1253b0dc4b4ef69\n--- a/.github/prompts/cca-completion-check.md\n+++ b/.github/prompts/cca-completion-check.md\n@@ -15,7 +15,7 @@\n 1. GitHub Issues, Pull Requests, Checks, Reviews, comments, and merge evidence are the operational source of truth.\n 2. The repository-local `.shiki/` mirror records Goals, PRDs, plans, Task DAGs, contracts, locks, ledger entries, CCA verdicts, repair packets, reports, and handoffs.\n 3. `CONTEXT.md` defines Shiki domain language and glossary decisions.\n-4. `docs/adr/` records hard-to-reverse platform decisions.\n+4. `docs/adr/SADR-NNNN-*.md` records Shiki platform decisions; `docs/adr/NNNN-*.md` is reserved for target ADRs.\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \ndiff --git a/AGENTS.md b/AGENTS.md\nindex 7f041100d49c2a7525f51cb0f5d696a4a6d1c78c..ddd7fe4d489f2c4c3ce82b51c84934019a7616b4\n--- a/AGENTS.md\n+++ b/AGENTS.md\n@@ -40,7 +40,7 @@\n 1. GitHub Issues, Pull Requests, Checks, Reviews, comments, and merge evidence are the operational source of truth.\n 2. The repository-local `.shiki/` mirror records Goals, PRDs, plans, Task DAGs, contracts, locks, ledger entries, CCA verdicts, repair packets, reports, and handoffs.\n 3. `CONTEXT.md` defines Shiki domain language and glossary decisions.\n-4. `docs/adr/` records hard-to-reverse platform decisions.\n+4. `docs/adr/SADR-NNNN-*.md` records Shiki platform decisions; `docs/adr/NNNN-*.md` is reserved for target ADRs.\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \ndiff --git a/CLAUDE.md b/CLAUDE.md\nindex 98a932e7841a632866fcd53b0bad0e3af3711d3d..83e001d4c15ed0a421666dc68da548ecb56748df\n--- a/CLAUDE.md\n+++ b/CLAUDE.md\n@@ -10,7 +10,7 @@\n 1. GitHub Issues, Pull Requests, Checks, Reviews, comments, and merge evidence are the operational source of truth.\n 2. The repository-local `.shiki/` mirror records Goals, PRDs, plans, Task DAGs, contracts, locks, ledger entries, CCA verdicts, repair packets, reports, and handoffs.\n 3. `CONTEXT.md` defines Shiki domain language and glossary decisions.\n-4. `docs/adr/` records hard-to-reverse platform decisions.\n+4. `docs/adr/SADR-NNNN-*.md` records Shiki platform decisions; `docs/adr/NNNN-*.md` is reserved for target ADRs.\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \ndiff --git a/SYSTEM_PROMPT.md b/SYSTEM_PROMPT.md\nindex aaaea45e3a896c3a44ba89eec8bb84325ecf6c62..42087a9325dfaa48de71b8319017175689166d2e\n--- a/SYSTEM_PROMPT.md\n+++ b/SYSTEM_PROMPT.md\n@@ -26,7 +26,7 @@\n 1. GitHub Issues, Pull Requests, Checks, Reviews, comments, and merge evidence are the operational source of truth.\n 2. The repository-local `.shiki/` mirror records Goals, PRDs, plans, Task DAGs, contracts, locks, ledger entries, CCA verdicts, repair packets, reports, and handoffs.\n 3. `CONTEXT.md` defines Shiki domain language and glossary decisions.\n-4. `docs/adr/` records hard-to-reverse platform decisions.\n+4. `docs/adr/SADR-NNNN-*.md` records Shiki platform decisions; `docs/adr/NNNN-*.md` is reserved for target ADRs.\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \ndiff --git a/docs/adr/0001-keep-shiki-as-platform-name.md b/docs/adr/SADR-0001-keep-shiki-as-platform-name.md\nindex 20a9637c3b9d8c97de7ba8db7cfae4d82d9a7dba..8b685439959aada1b221c112f0ac3c51ca8a1695\n--- a/docs/adr/0001-keep-shiki-as-platform-name.md\n+++ b/docs/adr/SADR-0001-keep-shiki-as-platform-name.md\n@@ -1,3 +1,3 @@\n-# Keep Shiki As The Platform Name\n+# SADR-0001: Keep Shiki As The Platform Name\n \n Shiki remains the product name for the GitHub-native agentic engineering control plane. We considered more descriptive names such as MergePilot, MergeGate, and Kiroku, but kept Shiki because it carries the existing goal-seeking framework identity while remaining independent of Hermes, Codex, Claude Code, or any single runtime.\ndiff --git a/docs/adr/0002-use-agents-as-shared-constitution.md b/docs/adr/SADR-0002-use-agents-as-shared-constitution.md\nindex 73de30ecb078120eac540d9bc51855e3cd48d0e3..c5486182bf06fca36ea508496b013c67302fd989\n--- a/docs/adr/0002-use-agents-as-shared-constitution.md\n+++ b/docs/adr/SADR-0002-use-agents-as-shared-constitution.md\n@@ -1,3 +1,3 @@\n-# Use AGENTS As The Shared Agent Constitution\n+# SADR-0002: Use AGENTS As The Shared Agent Constitution\n \n Shiki uses `AGENTS.md` as the runtime-neutral constitution and `CLAUDE.md` as a thin Claude Code wrapper. This keeps Codex, Claude Code, Hermes Runner, and future Agent Runtimes aligned on the same GitHub-first MergeGate rules while still allowing runtime-specific instructions where needed.\ndiff --git a/docs/adr/0003-require-skill-gate-for-mergegate-readiness.md b/docs/adr/SADR-0003-require-skill-gate-for-mergegate-readiness.md\nindex 424be0c9af4c8f0136f1fde68a468c47ff84e764..7fc337987c7df9792f75f181dfcbed75fe5ffff2\n--- a/docs/adr/0003-require-skill-gate-for-mergegate-readiness.md\n+++ b/docs/adr/SADR-0003-require-skill-gate-for-mergegate-readiness.md\n@@ -1,3 +1,3 @@\n-# Require Skill Gate For MergeGate Readiness\n+# SADR-0003: Require Skill Gate For MergeGate Readiness\n \n Shiki requires relevant engineering skills to be invoked before execution when their triggers apply. This makes skills part of MergeGate readiness rather than optional prompt style, because Shiki depends on repeatable planning, issue decomposition, TDD, diagnosis, triage, and architecture review before autonomous or parallel agent work can be trusted.\ndiff --git a/docs/adr/0004-use-github-first-with-shiki-mirror.md b/docs/adr/SADR-0004-use-github-first-with-shiki-mirror.md\nindex d0a3a9174dc646a945a9d325954ffaf00c352040..a79c54ae9ee8eed2a0524192b864da4e22a92d3f\n--- a/docs/adr/0004-use-github-first-with-shiki-mirror.md\n+++ b/docs/adr/SADR-0004-use-github-first-with-shiki-mirror.md\n@@ -1,4 +1,4 @@\n-# ADR 0004: Use GitHub First With A Repository-Local Shiki Mirror\n+# SADR-0004: Use GitHub First With A Repository-Local Shiki Mirror\n \n ## Status\n \ndiff --git a/docs/adr/0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md b/docs/adr/SADR-0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\nindex b339baf49b89d9290e79c3d51b36e7b2bde3bd95..c30a0d8b1fde0224d0ac7820e7870dccf721580d\n--- a/docs/adr/0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\n+++ b/docs/adr/SADR-0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\n@@ -1,4 +1,4 @@\n-# ADR 0005: Use Codex As Front And Claude Code Action As The GitHub Runtime\n+# SADR-0005: Use Codex As Front And Claude Code Action As The GitHub Runtime\n \n ## Status\n \ndiff --git a/docs/adr/0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md b/docs/adr/SADR-0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\nindex 4bcae9d1e52f0a37b8e9fbafa5cd14b6ee1993bd..7a38538a6ae9929567f0ade67e7ea6a60d0b3f7e\n--- a/docs/adr/0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\n+++ b/docs/adr/SADR-0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\n@@ -1,4 +1,4 @@\n-# ADR 0006: Separate Codex Implementation, CCA Completion, And MergeGate Authorization\n+# SADR-0006: Separate Codex Implementation, CCA Completion, And MergeGate Authorization\n \n ## Status\n \ndiff --git a/docs/adr/0007-packaging-and-release.md b/docs/adr/SADR-0007-packaging-and-release.md\nindex 79b84ddb97167372b44641e5b7860e400ce80822..023cd57f8011aa6e6d2ebf612b7c35a7a82c00d4\n--- a/docs/adr/0007-packaging-and-release.md\n+++ b/docs/adr/SADR-0007-packaging-and-release.md\n@@ -1,4 +1,4 @@\n-# ADR 0007: Packaging And Release For Shiki\n+# SADR-0007: Packaging And Release For Shiki\n \n ## Status\n \ndiff --git a/docs/adr/0008-use-claude-code-as-default-implementer.md b/docs/adr/SADR-0008-use-claude-code-as-default-implementer.md\nindex dedf4510b7c7799521422f622029563d373db031..a158a8d444d93764ec515a28133f7dde4af23579\n--- a/docs/adr/0008-use-claude-code-as-default-implementer.md\n+++ b/docs/adr/SADR-0008-use-claude-code-as-default-implementer.md\n@@ -1,4 +1,4 @@\n-# ADR 0008: Use Claude Code As The Default Implementer Runtime\n+# SADR-0008: Use Claude Code As The Default Implementer Runtime\n \n ## Status\n \ndiff --git a/docs/adr/0009-requirements-definition-and-spec-freeze.md b/docs/adr/SADR-0009-requirements-definition-and-spec-freeze.md\nindex 29b3f7871bef9203a8b4a831a65c39eb2f506e42..52e7dd69ad0aa4efe0b68c62678abbf7df02aaec\n--- a/docs/adr/0009-requirements-definition-and-spec-freeze.md\n+++ b/docs/adr/SADR-0009-requirements-definition-and-spec-freeze.md\n@@ -1,4 +1,4 @@\n-# ADR 0009: Merge Grill And Planning Into Requirements Definition With Spec Freeze\n+# SADR-0009: Merge Grill And Planning Into Requirements Definition With Spec Freeze\n \n ## Status\n \ndiff --git a/docs/adr/0010-external-ai-guardian-review.md b/docs/adr/SADR-0010-external-ai-guardian-review.md\nindex e6108bbe8bb4e73e0ff4684c8f78001a80438c4e..e690d4eddd270e683d709c9b679c5859e71eeaaa\n--- a/docs/adr/0010-external-ai-guardian-review.md\n+++ b/docs/adr/SADR-0010-external-ai-guardian-review.md\n@@ -1,4 +1,4 @@\n-# ADR 0010: External AI Guardian Review As A First-Class Approval Authority\n+# SADR-0010: External AI Guardian Review As A First-Class Approval Authority\n \n ## Status\n \ndiff --git a/docs/adr/0011-autonomous-quality-gate-evidence.md b/docs/adr/SADR-0011-autonomous-quality-gate-evidence.md\nindex bca0246c5d46e44afbde15486a75eec425fc02f5..9c93eac16d8c3a6d4b11bd7224e3955102cd7db4\n--- a/docs/adr/0011-autonomous-quality-gate-evidence.md\n+++ b/docs/adr/SADR-0011-autonomous-quality-gate-evidence.md\n@@ -1,4 +1,4 @@\n-# ADR 0011: Autonomous Quality-Gate Evidence \u2014 Deterministic Facts And An Independent Verifier\n+# SADR-0011: Autonomous Quality-Gate Evidence \u2014 Deterministic Facts And An Independent Verifier\n \n ## Status\n \ndiff --git a/docs/adr/0012-autonomous-loop-closeout-to-main.md b/docs/adr/SADR-0012-autonomous-loop-closeout-to-main.md\nindex 637fad6dde4989c9c2bf0f81f676d8d29991123c..ec7d85f81030a676bcbba28b43eb365422a36eac\n--- a/docs/adr/0012-autonomous-loop-closeout-to-main.md\n+++ b/docs/adr/SADR-0012-autonomous-loop-closeout-to-main.md\n@@ -1,4 +1,4 @@\n-# 0012 \u2014 Autonomous loop pushes goal completion to main via a closeout PR\n+# SADR-0012 \u2014 Autonomous loop pushes goal completion to main via a closeout PR\n \n - Status: accepted\n - Date: 2026-06-18\ndiff --git a/docs/adr/0013-bootstrap-grants-actions-approve-pull-requests.md b/docs/adr/SADR-0013-bootstrap-grants-actions-approve-pull-requests.md\nindex b6c69d64f1c5ced346b88d6859586715b3b5bd0e..a793e7277d67ab8ea56286fec289ca689825a17d\n--- a/docs/adr/0013-bootstrap-grants-actions-approve-pull-requests.md\n+++ b/docs/adr/SADR-0013-bootstrap-grants-actions-approve-pull-requests.md\n@@ -1,4 +1,4 @@\n-# ADR 0013: Bootstrap Grants GitHub Actions Permission To Create And Approve Pull Requests\n+# SADR-0013: Bootstrap Grants GitHub Actions Permission To Create And Approve Pull Requests\n \n ## Status\n \ndiff --git a/docs/adr/0014-codex-app-external-ai-guardian-ui-adapter.md b/docs/adr/SADR-0014-codex-app-external-ai-guardian-ui-adapter.md\nindex 238db06279334caa341c117362df9271382a7fcd..65f12e9939321e1b3e389a34c822fc1549e5155d\n--- a/docs/adr/0014-codex-app-external-ai-guardian-ui-adapter.md\n+++ b/docs/adr/SADR-0014-codex-app-external-ai-guardian-ui-adapter.md\n@@ -1,4 +1,4 @@\n-# ADR 0014: Use Codex App As The External AI Guardian UI Adapter\n+# SADR-0014: Use Codex App As The External AI Guardian UI Adapter\n \n ## Status\n \ndiff --git a/docs/adr/0015-contract-approval-before-dispatch.md b/docs/adr/SADR-0015-contract-approval-before-dispatch.md\nindex fad6c656741a20a7684ba16080a7db159f63a52f..7bdd760bb3e82eee4e1410f45c783b03b638d456\n--- a/docs/adr/0015-contract-approval-before-dispatch.md\n+++ b/docs/adr/SADR-0015-contract-approval-before-dispatch.md\n@@ -1,4 +1,4 @@\n-# ADR 0015: Approve The Task Contract Before Dispatch, Not The Diff After It\n+# SADR-0015: Approve The Task Contract Before Dispatch, Not The Diff After It\n \n ## Status\n \ndiff --git a/docs/adr/0016-locks-bound-product-paths.md b/docs/adr/SADR-0016-locks-bound-product-paths.md\nindex b5e850f3f1f7f4ef6e7f9d1763a29a51bd300c45..332cc17313498404f41a145291e38447b5de0dc4\n--- a/docs/adr/0016-locks-bound-product-paths.md\n+++ b/docs/adr/SADR-0016-locks-bound-product-paths.md\n@@ -1,4 +1,4 @@\n-# ADR 0016: Locks bound product paths; the Shiki mirror is bound by record identity\n+# SADR-0016: Locks bound product paths; the Shiki mirror is bound by record identity\n \n ## Status\n \ndiff --git a/docs/adr/0017-bookkeeping-closeouts-do-not-inherit-risk.md b/docs/adr/SADR-0017-bookkeeping-closeouts-do-not-inherit-risk.md\nindex 554e3a0d526a72ca067108c5464cc68c3edcf174..83115f69f1b1b48d544c9b0157f377b91cb53abf\n--- a/docs/adr/0017-bookkeeping-closeouts-do-not-inherit-risk.md\n+++ b/docs/adr/SADR-0017-bookkeeping-closeouts-do-not-inherit-risk.md\n@@ -1,4 +1,4 @@\n-# ADR 0017: A Bookkeeping Closeout Does Not Inherit Its Task's Risk\n+# SADR-0017: A Bookkeeping Closeout Does Not Inherit Its Task's Risk\n \n ## Status\n \ndiff --git a/docs/adr/0018-carried-guardian-approval-across-a-proven-base-sync.md b/docs/adr/SADR-0018-carried-guardian-approval-across-a-proven-base-sync.md\nindex 22358562da390ed8b45cd600da08a43d378b4680..38da0fd95102c3496c3219ce2319fce696a2532a\n--- a/docs/adr/0018-carried-guardian-approval-across-a-proven-base-sync.md\n+++ b/docs/adr/SADR-0018-carried-guardian-approval-across-a-proven-base-sync.md\n@@ -1,4 +1,4 @@\n-# ADR 0018: Carried Guardian Approval Across A Proven Base Sync\n+# SADR-0018: Carried Guardian Approval Across A Proven Base Sync\n \n ## Status\n \ndiff --git a/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md b/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\nnew file mode 100644\nindex 0000000000000000000000000000000000000000..7455ce1f3bed02ddddb851ac21a1d1a648c9aeba\n--- /dev/null\n+++ b/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\n@@ -0,0 +1,55 @@\n+# SADR-0019: Separate Shiki Platform SADRs From Target ADRs\n+\n+## Status\n+\n+Accepted\n+\n+## Context\n+\n+Shiki installs its platform decision records into `docs/adr/` in every target\n+repository. Shiki and its targets previously used the same numeric\n+`NNNN-<slug>.md` namespace. A target that already owned ADRs 0013 through 0018\n+therefore acquired two records for each number during installation, making an\n+`ADR NNNN` citation ambiguous. Renumbering target decisions would rewrite target\n+history, while renumbering Shiki decisions per target would destroy their stable\n+platform identity.\n+\n+Upgrades also need to remove the former Shiki-owned numeric paths without ever\n+mistaking a target-owned ADR for platform content. The install stamp already\n+records exact shipped paths and content digests, so it can prove ownership only\n+when both still match.\n+\n+## Decision\n+\n+Shiki platform decisions use the `SADR` namespace:\n+\n+- records are named `docs/adr/SADR-NNNN-<slug>.md`;\n+- live platform citations use `SADR-NNNN`;\n+- target product decisions retain `docs/adr/NNNN-<slug>.md` and `ADR NNNN`;\n+- the reference validator resolves the two namespaces independently, so the same\n+  four-digit number may exist once in each namespace.\n+\n+Before a forced upgrade writes anything, the installer inventories the exact\n+former numeric Shiki ADR paths. It may delete a legacy path only when the\n+existing install stamp contains that exact path and its recorded SHA-256 digest\n+matches the file currently on disk. All ownership blockers are reported\n+together and abort the upgrade before its first write. A non-forced install\n+never performs this cleanup.\n+\n+Historical `.shiki/` evidence remains unchanged because it records the namespace\n+that existed when that evidence was produced.\n+\n+## Consequences\n+\n+- Targets may use any ADR number without colliding with Shiki's platform SADRs.\n+- Fresh installs preserve target ADR bytes and install a stable SADR set.\n+- Forced upgrades fail closed when legacy ownership cannot be proven, and a\n+  successful upgrade refreshes the stamp without removed legacy paths.\n+- Current governance, packaging, workflow, prompt, script, and skill references\n+  must move with the platform records.\n+\n+## Alternatives Considered\n+\n+- Collision-only refusal leaves affected targets permanently unupgradable.\n+- Renumbering target ADRs mutates target-owned history.\n+- Per-target Shiki renumbering makes platform citations non-portable.\ndiff --git a/docs/agents/domain.md b/docs/agents/domain.md\nindex 258da8ea81e87ac0a6b0b5b5e5c2fde980eaabb7..67aaa6f4b7d0a5aa4d495ce6279073333f7798cb\n--- a/docs/agents/domain.md\n+++ b/docs/agents/domain.md\n@@ -5,7 +5,9 @@\n ## Layout\n \n - `CONTEXT.md` defines Shiki language. Keep it implementation-free.\n-- `docs/adr/` records hard-to-reverse decisions.\n+- `docs/adr/SADR-NNNN-*.md` records Shiki platform decisions.\n+- `docs/adr/NNNN-*.md` records target product decisions without sharing the\n+  SADR namespace.\n - `AGENTS.md` is the runtime-neutral agent constitution.\n - `CLAUDE.md` is the Claude Code wrapper around `AGENTS.md`.\n - `.shiki/` is the Target Repository mirror for Goals, PRDs, plans, Task DAGs, contracts, locks, worktree records, ledger entries, CCA verdicts, repair packets, reports, and handoffs.\ndiff --git a/docs/agents/implementation-policy.md b/docs/agents/implementation-policy.md\nindex c754144a9fb5bf4a38aa82a60e1670c59e626b44..3b059d128ce1338ee49eadec0dbd58f3cc2cc594\n--- a/docs/agents/implementation-policy.md\n+++ b/docs/agents/implementation-policy.md\n@@ -27,7 +27,7 @@\n 1. GitHub Issues, Pull Requests, Checks, Reviews, comments, and merge evidence are the operational source of truth.\n 2. The repository-local `.shiki/` mirror records Goals, PRDs, plans, Task DAGs, contracts, locks, ledger entries, CCA verdicts, repair packets, reports, and handoffs.\n 3. `CONTEXT.md` defines Shiki domain language and glossary decisions.\n-4. `docs/adr/` records hard-to-reverse platform decisions.\n+4. `docs/adr/SADR-NNNN-*.md` records Shiki platform decisions; `docs/adr/NNNN-*.md` is reserved for target ADRs.\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \ndiff --git a/scripts/shiki_contracts.py b/scripts/shiki_contracts.py\nindex 18076f0a70a1f8c9d4475a005bcccb1472a246c1..9d3f08dc1172b100c3a9b60a21ffdbae0f4dd44b\n--- a/scripts/shiki_contracts.py\n+++ b/scripts/shiki_contracts.py\n@@ -100,7 +100,7 @@\n     \"GitHub Issues, Pull Requests, Checks, Reviews, comments, and merge evidence are the operational source of truth.\",\n     \"The repository-local `.shiki/` mirror records Goals, PRDs, plans, Task DAGs, contracts, locks, ledger entries, CCA verdicts, repair packets, reports, and handoffs.\",\n     \"`CONTEXT.md` defines Shiki domain language and glossary decisions.\",\n-    \"`docs/adr/` records hard-to-reverse platform decisions.\",\n+    \"`docs/adr/SADR-NNNN-*.md` records Shiki platform decisions; `docs/adr/NNNN-*.md` is reserved for target ADRs.\",\n     \"Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\",\n )\n \ndiff --git a/tests/test_adr_references.py b/tests/test_adr_references.py\nindex 51706329c797d5dd9b47e082a333fa83b261d482..92569d9fa25e22cfd71c17561d715485027a0d2d\n--- a/tests/test_adr_references.py\n+++ b/tests/test_adr_references.py\n@@ -31,17 +31,18 @@\n \n REPO_ROOT = Path(__file__).resolve().parents[1]\n \n-# \"ADR 0016\" -- a four-digit citation. The trailing \\b keeps a five-digit token\n-# from being read as a truncated four-digit number.\n-_CITATION = re.compile(r\"\\bADR (\\d{4})\\b\")\n-# \"0016-locks-bound-product-paths.md\" -- the leading four digits name the record.\n-_ADR_FILENAME = re.compile(r\"^(\\d{4})-.*\\.md$\")\n+# ``ADR 0016`` / ``SADR-0016`` -- a namespace plus four-digit citation. The\n+# trailing ``\\b`` keeps a five-digit token from being read as a truncated one.\n+_CITATION = re.compile(r\"\\b(S?ADR)[ -](\\d{4})\\b\")\n+# Target ADRs retain the numeric filename; Shiki platform decisions use SADR.\n+_DECISION_FILENAME = re.compile(r\"^(?:(SADR)-)?(\\d{4})-.*\\.md$\")\n \n \n class Violation(NamedTuple):\n     \"\"\"A citation that does not resolve to exactly one decision record.\"\"\"\n \n     citing_file: str  # repo-relative path of the file that cites the ADR\n+    namespace: str  # \"ADR\" for target decisions; \"SADR\" for Shiki decisions\n     number: str  # the cited four-digit number, e.g. \"0016\"\n     line: int  # 1-based line of the citation\n     reason: str  # \"missing\" (zero matches) or \"ambiguous\" (two or more)\n@@ -49,32 +50,34 @@\n \n     @property\n     def expected_path(self) -> str:\n-        return f\"docs/adr/{self.number}-*.md\"\n+        prefix = \"SADR-\" if self.namespace == \"SADR\" else \"\"\n+        return f\"docs/adr/{prefix}{self.number}-*.md\"\n \n     def describe(self) -> str:\n         \"\"\"Name the citing file, the cited number, and the missing path.\"\"\"\n         if self.reason == \"missing\":\n             return (\n-                f\"{self.citing_file}:{self.line} cites ADR {self.number} \"\n+                f\"{self.citing_file}:{self.line} cites {self.namespace} {self.number} \"\n                 f\"but no document matches {self.expected_path}\"\n             )\n         joined = \", \".join(self.matches)\n         return (\n-            f\"{self.citing_file}:{self.line} cites ADR {self.number} which \"\n+            f\"{self.citing_file}:{self.line} cites {self.namespace} {self.number} which \"\n             f\"resolves to {len(self.matches)} documents (expected exactly one \"\n             f\"at {self.expected_path}): {joined}\"\n         )\n \n \n-def _adr_index(root: Path) -> dict[str, list[Path]]:\n-    \"\"\"Map every four-digit ADR number to the record files that claim it.\"\"\"\n-    index: dict[str, list[Path]] = {}\n+def _adr_index(root: Path) -> dict[tuple[str, str], list[Path]]:\n+    \"\"\"Map each decision namespace and number to the files that claim it.\"\"\"\n+    index: dict[tuple[str, str], list[Path]] = {}\n     adr_dir = root / \"docs\" / \"adr\"\n     if adr_dir.is_dir():\n         for path in sorted(adr_dir.glob(\"*.md\")):\n-            match = _ADR_FILENAME.match(path.name)\n+            match = _DECISION_FILENAME.match(path.name)\n             if match:\n-                index.setdefault(match.group(1), []).append(path)\n+                namespace = \"SADR\" if match.group(1) else \"ADR\"\n+                index.setdefault((namespace, match.group(2)), []).append(path)\n     return index\n \n \n@@ -110,13 +113,15 @@\n         rel = path.relative_to(root).as_posix()\n         for lineno, line in enumerate(text.splitlines(), start=1):\n             for match in _CITATION.finditer(line):\n-                number = match.group(1)\n-                found = index.get(number, [])\n+                namespace, number = match.groups()\n+                found = index.get((namespace, number), [])\n                 if len(found) == 1:\n                     continue\n                 reason = \"missing\" if not found else \"ambiguous\"\n                 rels = [p.relative_to(root).as_posix() for p in found]\n-                violations.append(Violation(rel, number, lineno, reason, rels))\n+                violations.append(\n+                    Violation(rel, namespace, number, lineno, reason, rels)\n+                )\n     return violations\n \n \n@@ -146,8 +151,8 @@\n         # The two records this Goal carried onto the default branch, and which\n         # the scripts cite most, must be present.\n         index = _adr_index(REPO_ROOT)\n-        self.assertIn(\"0016\", index)\n-        self.assertIn(\"0017\", index)\n+        self.assertIn((\"ADR\", \"0016\"), index)\n+        self.assertIn((\"ADR\", \"0017\"), index)\n \n \n class FixtureAdrReferences(unittest.TestCase):\n@@ -180,6 +185,17 @@\n         self.assertIn(\"9991\", message)  # cited number\n         self.assertIn(\"docs/adr/9991-*.md\", message)  # missing path\n \n+    def test_target_adr_does_not_satisfy_missing_platform_sadr(self) -> None:\n+        root = self._make_repo(\n+            adr_names=[\"0042-target-decision.md\"],\n+            script_body='\"\"\"Target ADR 0042 exists, but platform SADR-0042 does not.\"\"\"\\n',\n+        )\n+        violations = find_adr_reference_violations(root, include_docs=False)\n+        self.assertEqual(len(violations), 1)\n+        self.assertEqual(violations[0].namespace, \"SADR\")\n+        self.assertEqual(violations[0].number, \"0042\")\n+        self.assertEqual(violations[0].expected_path, \"docs/adr/SADR-0042-*.md\")\n+\n     def test_uncited_adr_is_allowed(self) -> None:\n         # An ADR exists but nothing cites it -> not a violation. The converse\n         # (every ADR must be cited) is explicitly NOT required.\ndiff --git a/tests/test_sadr_install_migration.py b/tests/test_sadr_install_migration.py\nnew file mode 100644\nindex 0000000000000000000000000000000000000000..a6398c81377f00c09aa2cb684980b070cfaf714c\n--- /dev/null\n+++ b/tests/test_sadr_install_migration.py\n@@ -0,0 +1,56 @@\n+\"\"\"SADR installation and collision-safe legacy upgrade behavior.\"\"\"\n+\n+from __future__ import annotations\n+\n+import shutil\n+import tempfile\n+import unittest\n+from pathlib import Path\n+\n+import shiki_test_support  # noqa: F401  (path bootstrap)\n+\n+import shiki_installer\n+from tests.test_adr_references import find_adr_reference_violations\n+\n+\n+PRODUCT_NUMBERS = tuple(range(13, 19))\n+\n+\n+class FreshSadrInstallTests(unittest.TestCase):\n+    def setUp(self) -> None:\n+        self.tmp = Path(tempfile.mkdtemp(prefix=\"shiki-sadr-fresh-\"))\n+        self.addCleanup(lambda: shutil.rmtree(self.tmp, ignore_errors=True))\n+        self.target = self.tmp / \"target\"\n+        self.adr_dir = self.target / \"docs\" / \"adr\"\n+        self.adr_dir.mkdir(parents=True)\n+        self.product_bytes = {\n+            f\"{number:04d}-product-decision.md\": (\n+                f\"# Product ADR {number:04d}\\n\\nTarget-owned bytes {number}.\\n\"\n+            ).encode()\n+            for number in PRODUCT_NUMBERS\n+        }\n+        for name, content in self.product_bytes.items():\n+            (self.adr_dir / name).write_bytes(content)\n+\n+    def test_fresh_install_preserves_product_adrs_and_installs_all_sadrs(self) -> None:\n+        shiki_installer.install_template(self.target, force=False, validate=True)\n+\n+        for name, expected in self.product_bytes.items():\n+            self.assertEqual((self.adr_dir / name).read_bytes(), expected)\n+\n+        sadr_numbers = {\n+            path.name.split(\"-\", 2)[1]\n+            for path in self.adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\")\n+        }\n+        self.assertEqual(sadr_numbers, {f\"{number:04d}\" for number in range(1, 20)})\n+\n+        numeric_records = {\n+            path.name\n+            for path in self.adr_dir.glob(\"[0-9][0-9][0-9][0-9]-*.md\")\n+        }\n+        self.assertEqual(numeric_records, set(self.product_bytes))\n+        self.assertEqual(find_adr_reference_violations(self.target), [])\n+\n+\n+if __name__ == \"__main__\":\n+    unittest.main()\n\ndiff --git a/.codex/skills/shiki/SKILL.md b/.codex/skills/shiki/SKILL.md\nindex c8e2e85d9d75d49cc4f2d6286ee22659c4e53e75..c392c28442311b8851ede2cf66af14011d8038d4\n--- a/.codex/skills/shiki/SKILL.md\n+++ b/.codex/skills/shiki/SKILL.md\n@@ -14,7 +14,7 @@\n 1. GitHub Issues, Pull Requests, Checks, Reviews, comments, and merge evidence are the operational source of truth.\n 2. The repository-local `.shiki/` mirror records Goals, PRDs, plans, Task DAGs, contracts, locks, ledger entries, CCA verdicts, repair packets, reports, and handoffs.\n 3. `CONTEXT.md` defines Shiki domain language and glossary decisions.\n-4. `docs/adr/` records hard-to-reverse platform decisions.\n+4. `docs/adr/SADR-NNNN-*.md` records Shiki platform decisions; `docs/adr/NNNN-*.md` is reserved for target ADRs.\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n@@ -69,7 +69,7 @@\n \n ## Responsibilities\n \n-- Claude Code implements and repairs by default (`shiki runner claude`, ADR 0008); Codex implements and repairs tasks explicitly assigned to `codex`.\n+- Claude Code implements and repairs by default (`shiki runner claude`, SADR-0008); Codex implements and repairs tasks explicitly assigned to `codex`.\n - Claude Code Action can act as GitHub-side CCA or reviewer.\n - CCA judges completion.\n - MergeGate authorizes state transitions and merge readiness.\ndiff --git a/.github/prompts/cca-completion-check.md b/.github/prompts/cca-completion-check.md\nindex ed203ad5f302ecc81372d8ab27d2b1232ca97497..3137954883be3c4657b40ea1a1253b0dc4b4ef69\n--- a/.github/prompts/cca-completion-check.md\n+++ b/.github/prompts/cca-completion-check.md\n@@ -15,7 +15,7 @@\n 1. GitHub Issues, Pull Requests, Checks, Reviews, comments, and merge evidence are the operational source of truth.\n 2. The repository-local `.shiki/` mirror records Goals, PRDs, plans, Task DAGs, contracts, locks, ledger entries, CCA verdicts, repair packets, reports, and handoffs.\n 3. `CONTEXT.md` defines Shiki domain language and glossary decisions.\n-4. `docs/adr/` records hard-to-reverse platform decisions.\n+4. `docs/adr/SADR-NNNN-*.md` records Shiki platform decisions; `docs/adr/NNNN-*.md` is reserved for target ADRs.\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \ndiff --git a/AGENTS.md b/AGENTS.md\nindex 7f041100d49c2a7525f51cb0f5d696a4a6d1c78c..ddd7fe4d489f2c4c3ce82b51c84934019a7616b4\n--- a/AGENTS.md\n+++ b/AGENTS.md\n@@ -40,7 +40,7 @@\n 1. GitHub Issues, Pull Requests, Checks, Reviews, comments, and merge evidence are the operational source of truth.\n 2. The repository-local `.shiki/` mirror records Goals, PRDs, plans, Task DAGs, contracts, locks, ledger entries, CCA verdicts, repair packets, reports, and handoffs.\n 3. `CONTEXT.md` defines Shiki domain language and glossary decisions.\n-4. `docs/adr/` records hard-to-reverse platform decisions.\n+4. `docs/adr/SADR-NNNN-*.md` records Shiki platform decisions; `docs/adr/NNNN-*.md` is reserved for target ADRs.\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \ndiff --git a/CLAUDE.md b/CLAUDE.md\nindex 98a932e7841a632866fcd53b0bad0e3af3711d3d..83e001d4c15ed0a421666dc68da548ecb56748df\n--- a/CLAUDE.md\n+++ b/CLAUDE.md\n@@ -10,7 +10,7 @@\n 1. GitHub Issues, Pull Requests, Checks, Reviews, comments, and merge evidence are the operational source of truth.\n 2. The repository-local `.shiki/` mirror records Goals, PRDs, plans, Task DAGs, contracts, locks, ledger entries, CCA verdicts, repair packets, reports, and handoffs.\n 3. `CONTEXT.md` defines Shiki domain language and glossary decisions.\n-4. `docs/adr/` records hard-to-reverse platform decisions.\n+4. `docs/adr/SADR-NNNN-*.md` records Shiki platform decisions; `docs/adr/NNNN-*.md` is reserved for target ADRs.\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \ndiff --git a/SYSTEM_PROMPT.md b/SYSTEM_PROMPT.md\nindex aaaea45e3a896c3a44ba89eec8bb84325ecf6c62..42087a9325dfaa48de71b8319017175689166d2e\n--- a/SYSTEM_PROMPT.md\n+++ b/SYSTEM_PROMPT.md\n@@ -26,7 +26,7 @@\n 1. GitHub Issues, Pull Requests, Checks, Reviews, comments, and merge evidence are the operational source of truth.\n 2. The repository-local `.shiki/` mirror records Goals, PRDs, plans, Task DAGs, contracts, locks, ledger entries, CCA verdicts, repair packets, reports, and handoffs.\n 3. `CONTEXT.md` defines Shiki domain language and glossary decisions.\n-4. `docs/adr/` records hard-to-reverse platform decisions.\n+4. `docs/adr/SADR-NNNN-*.md` records Shiki platform decisions; `docs/adr/NNNN-*.md` is reserved for target ADRs.\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \ndiff --git a/docs/adr/0001-keep-shiki-as-platform-name.md b/docs/adr/SADR-0001-keep-shiki-as-platform-name.md\nindex 20a9637c3b9d8c97de7ba8db7cfae4d82d9a7dba..8b685439959aada1b221c112f0ac3c51ca8a1695\n--- a/docs/adr/0001-keep-shiki-as-platform-name.md\n+++ b/docs/adr/SADR-0001-keep-shiki-as-platform-name.md\n@@ -1,3 +1,3 @@\n-# Keep Shiki As The Platform Name\n+# SADR-0001: Keep Shiki As The Platform Name\n \n Shiki remains the product name for the GitHub-native agentic engineering control plane. We considered more descriptive names such as MergePilot, MergeGate, and Kiroku, but kept Shiki because it carries the existing goal-seeking framework identity while remaining independent of Hermes, Codex, Claude Code, or any single runtime.\ndiff --git a/docs/adr/0002-use-agents-as-shared-constitution.md b/docs/adr/SADR-0002-use-agents-as-shared-constitution.md\nindex 73de30ecb078120eac540d9bc51855e3cd48d0e3..c5486182bf06fca36ea508496b013c67302fd989\n--- a/docs/adr/0002-use-agents-as-shared-constitution.md\n+++ b/docs/adr/SADR-0002-use-agents-as-shared-constitution.md\n@@ -1,3 +1,3 @@\n-# Use AGENTS As The Shared Agent Constitution\n+# SADR-0002: Use AGENTS As The Shared Agent Constitution\n \n Shiki uses `AGENTS.md` as the runtime-neutral constitution and `CLAUDE.md` as a thin Claude Code wrapper. This keeps Codex, Claude Code, Hermes Runner, and future Agent Runtimes aligned on the same GitHub-first MergeGate rules while still allowing runtime-specific instructions where needed.\ndiff --git a/docs/adr/0003-require-skill-gate-for-mergegate-readiness.md b/docs/adr/SADR-0003-require-skill-gate-for-mergegate-readiness.md\nindex 424be0c9af4c8f0136f1fde68a468c47ff84e764..7fc337987c7df9792f75f181dfcbed75fe5ffff2\n--- a/docs/adr/0003-require-skill-gate-for-mergegate-readiness.md\n+++ b/docs/adr/SADR-0003-require-skill-gate-for-mergegate-readiness.md\n@@ -1,3 +1,3 @@\n-# Require Skill Gate For MergeGate Readiness\n+# SADR-0003: Require Skill Gate For MergeGate Readiness\n \n Shiki requires relevant engineering skills to be invoked before execution when their triggers apply. This makes skills part of MergeGate readiness rather than optional prompt style, because Shiki depends on repeatable planning, issue decomposition, TDD, diagnosis, triage, and architecture review before autonomous or parallel agent work can be trusted.\ndiff --git a/docs/adr/0004-use-github-first-with-shiki-mirror.md b/docs/adr/SADR-0004-use-github-first-with-shiki-mirror.md\nindex d0a3a9174dc646a945a9d325954ffaf00c352040..a79c54ae9ee8eed2a0524192b864da4e22a92d3f\n--- a/docs/adr/0004-use-github-first-with-shiki-mirror.md\n+++ b/docs/adr/SADR-0004-use-github-first-with-shiki-mirror.md\n@@ -1,4 +1,4 @@\n-# ADR 0004: Use GitHub First With A Repository-Local Shiki Mirror\n+# SADR-0004: Use GitHub First With A Repository-Local Shiki Mirror\n \n ## Status\n \ndiff --git a/docs/adr/0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md b/docs/adr/SADR-0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\nindex b339baf49b89d9290e79c3d51b36e7b2bde3bd95..c30a0d8b1fde0224d0ac7820e7870dccf721580d\n--- a/docs/adr/0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\n+++ b/docs/adr/SADR-0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\n@@ -1,4 +1,4 @@\n-# ADR 0005: Use Codex As Front And Claude Code Action As The GitHub Runtime\n+# SADR-0005: Use Codex As Front And Claude Code Action As The GitHub Runtime\n \n ## Status\n \ndiff --git a/docs/adr/0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md b/docs/adr/SADR-0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\nindex 4bcae9d1e52f0a37b8e9fbafa5cd14b6ee1993bd..7a38538a6ae9929567f0ade67e7ea6a60d0b3f7e\n--- a/docs/adr/0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\n+++ b/docs/adr/SADR-0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\n@@ -1,4 +1,4 @@\n-# ADR 0006: Separate Codex Implementation, CCA Completion, And MergeGate Authorization\n+# SADR-0006: Separate Codex Implementation, CCA Completion, And MergeGate Authorization\n \n ## Status\n \ndiff --git a/docs/adr/0007-packaging-and-release.md b/docs/adr/SADR-0007-packaging-and-release.md\nindex 79b84ddb97167372b44641e5b7860e400ce80822..023cd57f8011aa6e6d2ebf612b7c35a7a82c00d4\n--- a/docs/adr/0007-packaging-and-release.md\n+++ b/docs/adr/SADR-0007-packaging-and-release.md\n@@ -1,4 +1,4 @@\n-# ADR 0007: Packaging And Release For Shiki\n+# SADR-0007: Packaging And Release For Shiki\n \n ## Status\n \ndiff --git a/docs/adr/0008-use-claude-code-as-default-implementer.md b/docs/adr/SADR-0008-use-claude-code-as-default-implementer.md\nindex dedf4510b7c7799521422f622029563d373db031..a158a8d444d93764ec515a28133f7dde4af23579\n--- a/docs/adr/0008-use-claude-code-as-default-implementer.md\n+++ b/docs/adr/SADR-0008-use-claude-code-as-default-implementer.md\n@@ -1,4 +1,4 @@\n-# ADR 0008: Use Claude Code As The Default Implementer Runtime\n+# SADR-0008: Use Claude Code As The Default Implementer Runtime\n \n ## Status\n \ndiff --git a/docs/adr/0009-requirements-definition-and-spec-freeze.md b/docs/adr/SADR-0009-requirements-definition-and-spec-freeze.md\nindex 29b3f7871bef9203a8b4a831a65c39eb2f506e42..52e7dd69ad0aa4efe0b68c62678abbf7df02aaec\n--- a/docs/adr/0009-requirements-definition-and-spec-freeze.md\n+++ b/docs/adr/SADR-0009-requirements-definition-and-spec-freeze.md\n@@ -1,4 +1,4 @@\n-# ADR 0009: Merge Grill And Planning Into Requirements Definition With Spec Freeze\n+# SADR-0009: Merge Grill And Planning Into Requirements Definition With Spec Freeze\n \n ## Status\n \ndiff --git a/docs/adr/0010-external-ai-guardian-review.md b/docs/adr/SADR-0010-external-ai-guardian-review.md\nindex e6108bbe8bb4e73e0ff4684c8f78001a80438c4e..e690d4eddd270e683d709c9b679c5859e71eeaaa\n--- a/docs/adr/0010-external-ai-guardian-review.md\n+++ b/docs/adr/SADR-0010-external-ai-guardian-review.md\n@@ -1,4 +1,4 @@\n-# ADR 0010: External AI Guardian Review As A First-Class Approval Authority\n+# SADR-0010: External AI Guardian Review As A First-Class Approval Authority\n \n ## Status\n \ndiff --git a/docs/adr/0011-autonomous-quality-gate-evidence.md b/docs/adr/SADR-0011-autonomous-quality-gate-evidence.md\nindex bca0246c5d46e44afbde15486a75eec425fc02f5..9c93eac16d8c3a6d4b11bd7224e3955102cd7db4\n--- a/docs/adr/0011-autonomous-quality-gate-evidence.md\n+++ b/docs/adr/SADR-0011-autonomous-quality-gate-evidence.md\n@@ -1,4 +1,4 @@\n-# ADR 0011: Autonomous Quality-Gate Evidence \u2014 Deterministic Facts And An Independent Verifier\n+# SADR-0011: Autonomous Quality-Gate Evidence \u2014 Deterministic Facts And An Independent Verifier\n \n ## Status\n \ndiff --git a/docs/adr/0012-autonomous-loop-closeout-to-main.md b/docs/adr/SADR-0012-autonomous-loop-closeout-to-main.md\nindex 637fad6dde4989c9c2bf0f81f676d8d29991123c..ec7d85f81030a676bcbba28b43eb365422a36eac\n--- a/docs/adr/0012-autonomous-loop-closeout-to-main.md\n+++ b/docs/adr/SADR-0012-autonomous-loop-closeout-to-main.md\n@@ -1,4 +1,4 @@\n-# 0012 \u2014 Autonomous loop pushes goal completion to main via a closeout PR\n+# SADR-0012 \u2014 Autonomous loop pushes goal completion to main via a closeout PR\n \n - Status: accepted\n - Date: 2026-06-18\ndiff --git a/docs/adr/0013-bootstrap-grants-actions-approve-pull-requests.md b/docs/adr/SADR-0013-bootstrap-grants-actions-approve-pull-requests.md\nindex b6c69d64f1c5ced346b88d6859586715b3b5bd0e..a793e7277d67ab8ea56286fec289ca689825a17d\n--- a/docs/adr/0013-bootstrap-grants-actions-approve-pull-requests.md\n+++ b/docs/adr/SADR-0013-bootstrap-grants-actions-approve-pull-requests.md\n@@ -1,4 +1,4 @@\n-# ADR 0013: Bootstrap Grants GitHub Actions Permission To Create And Approve Pull Requests\n+# SADR-0013: Bootstrap Grants GitHub Actions Permission To Create And Approve Pull Requests\n \n ## Status\n \ndiff --git a/docs/adr/0014-codex-app-external-ai-guardian-ui-adapter.md b/docs/adr/SADR-0014-codex-app-external-ai-guardian-ui-adapter.md\nindex 238db06279334caa341c117362df9271382a7fcd..65f12e9939321e1b3e389a34c822fc1549e5155d\n--- a/docs/adr/0014-codex-app-external-ai-guardian-ui-adapter.md\n+++ b/docs/adr/SADR-0014-codex-app-external-ai-guardian-ui-adapter.md\n@@ -1,4 +1,4 @@\n-# ADR 0014: Use Codex App As The External AI Guardian UI Adapter\n+# SADR-0014: Use Codex App As The External AI Guardian UI Adapter\n \n ## Status\n \ndiff --git a/docs/adr/0015-contract-approval-before-dispatch.md b/docs/adr/SADR-0015-contract-approval-before-dispatch.md\nindex fad6c656741a20a7684ba16080a7db159f63a52f..7bdd760bb3e82eee4e1410f45c783b03b638d456\n--- a/docs/adr/0015-contract-approval-before-dispatch.md\n+++ b/docs/adr/SADR-0015-contract-approval-before-dispatch.md\n@@ -1,4 +1,4 @@\n-# ADR 0015: Approve The Task Contract Before Dispatch, Not The Diff After It\n+# SADR-0015: Approve The Task Contract Before Dispatch, Not The Diff After It\n \n ## Status\n \ndiff --git a/docs/adr/0016-locks-bound-product-paths.md b/docs/adr/SADR-0016-locks-bound-product-paths.md\nindex b5e850f3f1f7f4ef6e7f9d1763a29a51bd300c45..332cc17313498404f41a145291e38447b5de0dc4\n--- a/docs/adr/0016-locks-bound-product-paths.md\n+++ b/docs/adr/SADR-0016-locks-bound-product-paths.md\n@@ -1,4 +1,4 @@\n-# ADR 0016: Locks bound product paths; the Shiki mirror is bound by record identity\n+# SADR-0016: Locks bound product paths; the Shiki mirror is bound by record identity\n \n ## Status\n \ndiff --git a/docs/adr/0017-bookkeeping-closeouts-do-not-inherit-risk.md b/docs/adr/SADR-0017-bookkeeping-closeouts-do-not-inherit-risk.md\nindex 554e3a0d526a72ca067108c5464cc68c3edcf174..83115f69f1b1b48d544c9b0157f377b91cb53abf\n--- a/docs/adr/0017-bookkeeping-closeouts-do-not-inherit-risk.md\n+++ b/docs/adr/SADR-0017-bookkeeping-closeouts-do-not-inherit-risk.md\n@@ -1,4 +1,4 @@\n-# ADR 0017: A Bookkeeping Closeout Does Not Inherit Its Task's Risk\n+# SADR-0017: A Bookkeeping Closeout Does Not Inherit Its Task's Risk\n \n ## Status\n \ndiff --git a/docs/adr/0018-carried-guardian-approval-across-a-proven-base-sync.md b/docs/adr/SADR-0018-carried-guardian-approval-across-a-proven-base-sync.md\nindex 22358562da390ed8b45cd600da08a43d378b4680..38da0fd95102c3496c3219ce2319fce696a2532a\n--- a/docs/adr/0018-carried-guardian-approval-across-a-proven-base-sync.md\n+++ b/docs/adr/SADR-0018-carried-guardian-approval-across-a-proven-base-sync.md\n@@ -1,4 +1,4 @@\n-# ADR 0018: Carried Guardian Approval Across A Proven Base Sync\n+# SADR-0018: Carried Guardian Approval Across A Proven Base Sync\n \n ## Status\n \ndiff --git a/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md b/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\nnew file mode 100644\nindex 0000000000000000000000000000000000000000..7455ce1f3bed02ddddb851ac21a1d1a648c9aeba\n--- /dev/null\n+++ b/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\n@@ -0,0 +1,55 @@\n+# SADR-0019: Separate Shiki Platform SADRs From Target ADRs\n+\n+## Status\n+\n+Accepted\n+\n+## Context\n+\n+Shiki installs its platform decision records into `docs/adr/` in every target\n+repository. Shiki and its targets previously used the same numeric\n+`NNNN-<slug>.md` namespace. A target that already owned ADRs 0013 through 0018\n+therefore acquired two records for each number during installation, making an\n+`ADR NNNN` citation ambiguous. Renumbering target decisions would rewrite target\n+history, while renumbering Shiki decisions per target would destroy their stable\n+platform identity.\n+\n+Upgrades also need to remove the former Shiki-owned numeric paths without ever\n+mistaking a target-owned ADR for platform content. The install stamp already\n+records exact shipped paths and content digests, so it can prove ownership only\n+when both still match.\n+\n+## Decision\n+\n+Shiki platform decisions use the `SADR` namespace:\n+\n+- records are named `docs/adr/SADR-NNNN-<slug>.md`;\n+- live platform citations use `SADR-NNNN`;\n+- target product decisions retain `docs/adr/NNNN-<slug>.md` and `ADR NNNN`;\n+- the reference validator resolves the two namespaces independently, so the same\n+  four-digit number may exist once in each namespace.\n+\n+Before a forced upgrade writes anything, the installer inventories the exact\n+former numeric Shiki ADR paths. It may delete a legacy path only when the\n+existing install stamp contains that exact path and its recorded SHA-256 digest\n+matches the file currently on disk. All ownership blockers are reported\n+together and abort the upgrade before its first write. A non-forced install\n+never performs this cleanup.\n+\n+Historical `.shiki/` evidence remains unchanged because it records the namespace\n+that existed when that evidence was produced.\n+\n+## Consequences\n+\n+- Targets may use any ADR number without colliding with Shiki's platform SADRs.\n+- Fresh installs preserve target ADR bytes and install a stable SADR set.\n+- Forced upgrades fail closed when legacy ownership cannot be proven, and a\n+  successful upgrade refreshes the stamp without removed legacy paths.\n+- Current governance, packaging, workflow, prompt, script, and skill references\n+  must move with the platform records.\n+\n+## Alternatives Considered\n+\n+- Collision-only refusal leaves affected targets permanently unupgradable.\n+- Renumbering target ADRs mutates target-owned history.\n+- Per-target Shiki renumbering makes platform citations non-portable.\ndiff --git a/docs/agents/domain.md b/docs/agents/domain.md\nindex 258da8ea81e87ac0a6b0b5b5e5c2fde980eaabb7..67aaa6f4b7d0a5aa4d495ce6279073333f7798cb\n--- a/docs/agents/domain.md\n+++ b/docs/agents/domain.md\n@@ -5,7 +5,9 @@\n ## Layout\n \n - `CONTEXT.md` defines Shiki language. Keep it implementation-free.\n-- `docs/adr/` records hard-to-reverse decisions.\n+- `docs/adr/SADR-NNNN-*.md` records Shiki platform decisions.\n+- `docs/adr/NNNN-*.md` records target product decisions without sharing the\n+  SADR namespace.\n - `AGENTS.md` is the runtime-neutral agent constitution.\n - `CLAUDE.md` is the Claude Code wrapper around `AGENTS.md`.\n - `.shiki/` is the Target Repository mirror for Goals, PRDs, plans, Task DAGs, contracts, locks, worktree records, ledger entries, CCA verdicts, repair packets, reports, and handoffs.\ndiff --git a/docs/agents/implementation-policy.md b/docs/agents/implementation-policy.md\nindex c754144a9fb5bf4a38aa82a60e1670c59e626b44..3b059d128ce1338ee49eadec0dbd58f3cc2cc594\n--- a/docs/agents/implementation-policy.md\n+++ b/docs/agents/implementation-policy.md\n@@ -27,7 +27,7 @@\n 1. GitHub Issues, Pull Requests, Checks, Reviews, comments, and merge evidence are the operational source of truth.\n 2. The repository-local `.shiki/` mirror records Goals, PRDs, plans, Task DAGs, contracts, locks, ledger entries, CCA verdicts, repair packets, reports, and handoffs.\n 3. `CONTEXT.md` defines Shiki domain language and glossary decisions.\n-4. `docs/adr/` records hard-to-reverse platform decisions.\n+4. `docs/adr/SADR-NNNN-*.md` records Shiki platform decisions; `docs/adr/NNNN-*.md` is reserved for target ADRs.\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \ndiff --git a/scripts/shiki_contracts.py b/scripts/shiki_contracts.py\nindex 18076f0a70a1f8c9d4475a005bcccb1472a246c1..9d3f08dc1172b100c3a9b60a21ffdbae0f4dd44b\n--- a/scripts/shiki_contracts.py\n+++ b/scripts/shiki_contracts.py\n@@ -100,7 +100,7 @@\n     \"GitHub Issues, Pull Requests, Checks, Reviews, comments, and merge evidence are the operational source of truth.\",\n     \"The repository-local `.shiki/` mirror records Goals, PRDs, plans, Task DAGs, contracts, locks, ledger entries, CCA verdicts, repair packets, reports, and handoffs.\",\n     \"`CONTEXT.md` defines Shiki domain language and glossary decisions.\",\n-    \"`docs/adr/` records hard-to-reverse platform decisions.\",\n+    \"`docs/adr/SADR-NNNN-*.md` records Shiki platform decisions; `docs/adr/NNNN-*.md` is reserved for target ADRs.\",\n     \"Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\",\n )\n \ndiff --git a/tests/test_adr_references.py b/tests/test_adr_references.py\nindex 51706329c797d5dd9b47e082a333fa83b261d482..92569d9fa25e22cfd71c17561d715485027a0d2d\n--- a/tests/test_adr_references.py\n+++ b/tests/test_adr_references.py\n@@ -31,17 +31,18 @@\n \n REPO_ROOT = Path(__file__).resolve().parents[1]\n \n-# \"ADR 0016\" -- a four-digit citation. The trailing \\b keeps a five-digit token\n-# from being read as a truncated four-digit number.\n-_CITATION = re.compile(r\"\\bADR (\\d{4})\\b\")\n-# \"0016-locks-bound-product-paths.md\" -- the leading four digits name the record.\n-_ADR_FILENAME = re.compile(r\"^(\\d{4})-.*\\.md$\")\n+# ``ADR 0016`` / ``SADR-0016`` -- a namespace plus four-digit citation. The\n+# trailing ``\\b`` keeps a five-digit token from being read as a truncated one.\n+_CITATION = re.compile(r\"\\b(S?ADR)[ -](\\d{4})\\b\")\n+# Target ADRs retain the numeric filename; Shiki platform decisions use SADR.\n+_DECISION_FILENAME = re.compile(r\"^(?:(SADR)-)?(\\d{4})-.*\\.md$\")\n \n \n class Violation(NamedTuple):\n     \"\"\"A citation that does not resolve to exactly one decision record.\"\"\"\n \n     citing_file: str  # repo-relative path of the file that cites the ADR\n+    namespace: str  # \"ADR\" for target decisions; \"SADR\" for Shiki decisions\n     number: str  # the cited four-digit number, e.g. \"0016\"\n     line: int  # 1-based line of the citation\n     reason: str  # \"missing\" (zero matches) or \"ambiguous\" (two or more)\n@@ -49,32 +50,34 @@\n \n     @property\n     def expected_path(self) -> str:\n-        return f\"docs/adr/{self.number}-*.md\"\n+        prefix = \"SADR-\" if self.namespace == \"SADR\" else \"\"\n+        return f\"docs/adr/{prefix}{self.number}-*.md\"\n \n     def describe(self) -> str:\n         \"\"\"Name the citing file, the cited number, and the missing path.\"\"\"\n         if self.reason == \"missing\":\n             return (\n-                f\"{self.citing_file}:{self.line} cites ADR {self.number} \"\n+                f\"{self.citing_file}:{self.line} cites {self.namespace} {self.number} \"\n                 f\"but no document matches {self.expected_path}\"\n             )\n         joined = \", \".join(self.matches)\n         return (\n-            f\"{self.citing_file}:{self.line} cites ADR {self.number} which \"\n+            f\"{self.citing_file}:{self.line} cites {self.namespace} {self.number} which \"\n             f\"resolves to {len(self.matches)} documents (expected exactly one \"\n             f\"at {self.expected_path}): {joined}\"\n         )\n \n \n-def _adr_index(root: Path) -> dict[str, list[Path]]:\n-    \"\"\"Map every four-digit ADR number to the record files that claim it.\"\"\"\n-    index: dict[str, list[Path]] = {}\n+def _adr_index(root: Path) -> dict[tuple[str, str], list[Path]]:\n+    \"\"\"Map each decision namespace and number to the files that claim it.\"\"\"\n+    index: dict[tuple[str, str], list[Path]] = {}\n     adr_dir = root / \"docs\" / \"adr\"\n     if adr_dir.is_dir():\n         for path in sorted(adr_dir.glob(\"*.md\")):\n-            match = _ADR_FILENAME.match(path.name)\n+            match = _DECISION_FILENAME.match(path.name)\n             if match:\n-                index.setdefault(match.group(1), []).append(path)\n+                namespace = \"SADR\" if match.group(1) else \"ADR\"\n+                index.setdefault((namespace, match.group(2)), []).append(path)\n     return index\n \n \n@@ -110,13 +113,15 @@\n         rel = path.relative_to(root).as_posix()\n         for lineno, line in enumerate(text.splitlines(), start=1):\n             for match in _CITATION.finditer(line):\n-                number = match.group(1)\n-                found = index.get(number, [])\n+                namespace, number = match.groups()\n+                found = index.get((namespace, number), [])\n                 if len(found) == 1:\n                     continue\n                 reason = \"missing\" if not found else \"ambiguous\"\n                 rels = [p.relative_to(root).as_posix() for p in found]\n-                violations.append(Violation(rel, number, lineno, reason, rels))\n+                violations.append(\n+                    Violation(rel, namespace, number, lineno, reason, rels)\n+                )\n     return violations\n \n \n@@ -146,8 +151,8 @@\n         # The two records this Goal carried onto the default branch, and which\n         # the scripts cite most, must be present.\n         index = _adr_index(REPO_ROOT)\n-        self.assertIn(\"0016\", index)\n-        self.assertIn(\"0017\", index)\n+        self.assertIn((\"ADR\", \"0016\"), index)\n+        self.assertIn((\"ADR\", \"0017\"), index)\n \n \n class FixtureAdrReferences(unittest.TestCase):\n@@ -180,6 +185,17 @@\n         self.assertIn(\"9991\", message)  # cited number\n         self.assertIn(\"docs/adr/9991-*.md\", message)  # missing path\n \n+    def test_target_adr_does_not_satisfy_missing_platform_sadr(self) -> None:\n+        root = self._make_repo(\n+            adr_names=[\"0042-target-decision.md\"],\n+            script_body='\"\"\"Target ADR 0042 exists, but platform SADR-0042 does not.\"\"\"\\n',\n+        )\n+        violations = find_adr_reference_violations(root, include_docs=False)\n+        self.assertEqual(len(violations), 1)\n+        self.assertEqual(violations[0].namespace, \"SADR\")\n+        self.assertEqual(violations[0].number, \"0042\")\n+        self.assertEqual(violations[0].expected_path, \"docs/adr/SADR-0042-*.md\")\n+\n     def test_uncited_adr_is_allowed(self) -> None:\n         # An ADR exists but nothing cites it -> not a violation. The converse\n         # (every ADR must be cited) is explicitly NOT required.\ndiff --git a/tests/test_sadr_install_migration.py b/tests/test_sadr_install_migration.py\nnew file mode 100644\nindex 0000000000000000000000000000000000000000..a6398c81377f00c09aa2cb684980b070cfaf714c\n--- /dev/null\n+++ b/tests/test_sadr_install_migration.py\n@@ -0,0 +1,56 @@\n+\"\"\"SADR installation and collision-safe legacy upgrade behavior.\"\"\"\n+\n+from __future__ import annotations\n+\n+import shutil\n+import tempfile\n+import unittest\n+from pathlib import Path\n+\n+import shiki_test_support  # noqa: F401  (path bootstrap)\n+\n+import shiki_installer\n+from tests.test_adr_references import find_adr_reference_violations\n+\n+\n+PRODUCT_NUMBERS = tuple(range(13, 19))\n+\n+\n+class FreshSadrInstallTests(unittest.TestCase):\n+    def setUp(self) -> None:\n+        self.tmp = Path(tempfile.mkdtemp(prefix=\"shiki-sadr-fresh-\"))\n+        self.addCleanup(lambda: shutil.rmtree(self.tmp, ignore_errors=True))\n+        self.target = self.tmp / \"target\"\n+        self.adr_dir = self.target / \"docs\" / \"adr\"\n+        self.adr_dir.mkdir(parents=True)\n+        self.product_bytes = {\n+            f\"{number:04d}-product-decision.md\": (\n+                f\"# Product ADR {number:04d}\\n\\nTarget-owned bytes {number}.\\n\"\n+            ).encode()\n+            for number in PRODUCT_NUMBERS\n+        }\n+        for name, content in self.product_bytes.items():\n+            (self.adr_dir / name).write_bytes(content)\n+\n+    def test_fresh_install_preserves_product_adrs_and_installs_all_sadrs(self) -> None:\n+        shiki_installer.install_template(self.target, force=False, validate=True)\n+\n+        for name, expected in self.product_bytes.items():\n+            self.assertEqual((self.adr_dir / name).read_bytes(), expected)\n+\n+        sadr_numbers = {\n+            path.name.split(\"-\", 2)[1]\n+            for path in self.adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\")\n+        }\n+        self.assertEqual(sadr_numbers, {f\"{number:04d}\" for number in range(1, 20)})\n+\n+        numeric_records = {\n+            path.name\n+            for path in self.adr_dir.glob(\"[0-9][0-9][0-9][0-9]-*.md\")\n+        }\n+        self.assertEqual(numeric_records, set(self.product_bytes))\n+        self.assertEqual(find_adr_reference_violations(self.target), [])\n+\n+\n+if __name__ == \"__main__\":\n+    unittest.main()\n\napply patch\npatch: completed\n/private/tmp/shiki-adr-collision.pa0KwY/.worktrees/shiki-t-20260807t073307221254z-2d22d654-introduc/docs/adr/README.md\n/private/tmp/shiki-adr-collision.pa0KwY/.worktrees/shiki-t-20260807t073307221254z-2d22d654-introduc/docs/adr/template.md\ndiff --git a/.codex/skills/shiki/SKILL.md b/.codex/skills/shiki/SKILL.md\nindex c8e2e85d9d75d49cc4f2d6286ee22659c4e53e75..c392c28442311b8851ede2cf66af14011d8038d4\n--- a/.codex/skills/shiki/SKILL.md\n+++ b/.codex/skills/shiki/SKILL.md\n@@ -14,7 +14,7 @@\n 1. GitHub Issues, Pull Requests, Checks, Reviews, comments, and merge evidence are the operational source of truth.\n 2. The repository-local `.shiki/` mirror records Goals, PRDs, plans, Task DAGs, contracts, locks, ledger entries, CCA verdicts, repair packets, reports, and handoffs.\n 3. `CONTEXT.md` defines Shiki domain language and glossary decisions.\n-4. `docs/adr/` records hard-to-reverse platform decisions.\n+4. `docs/adr/SADR-NNNN-*.md` records Shiki platform decisions; `docs/adr/NNNN-*.md` is reserved for target ADRs.\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n@@ -69,7 +69,7 @@\n \n ## Responsibilities\n \n-- Claude Code implements and repairs by default (`shiki runner claude`, ADR 0008); Codex implements and repairs tasks explicitly assigned to `codex`.\n+- Claude Code implements and repairs by default (`shiki runner claude`, SADR-0008); Codex implements and repairs tasks explicitly assigned to `codex`.\n - Claude Code Action can act as GitHub-side CCA or reviewer.\n - CCA judges completion.\n - MergeGate authorizes state transitions and merge readiness.\ndiff --git a/.github/prompts/cca-completion-check.md b/.github/prompts/cca-completion-check.md\nindex ed203ad5f302ecc81372d8ab27d2b1232ca97497..3137954883be3c4657b40ea1a1253b0dc4b4ef69\n--- a/.github/prompts/cca-completion-check.md\n+++ b/.github/prompts/cca-completion-check.md\n@@ -15,7 +15,7 @@\n 1. GitHub Issues, Pull Requests, Checks, Reviews, comments, and merge evidence are the operational source of truth.\n 2. The repository-local `.shiki/` mirror records Goals, PRDs, plans, Task DAGs, contracts, locks, ledger entries, CCA verdicts, repair packets, reports, and handoffs.\n 3. `CONTEXT.md` defines Shiki domain language and glossary decisions.\n-4. `docs/adr/` records hard-to-reverse platform decisions.\n+4. `docs/adr/SADR-NNNN-*.md` records Shiki platform decisions; `docs/adr/NNNN-*.md` is reserved for target ADRs.\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \ndiff --git a/AGENTS.md b/AGENTS.md\nindex 7f041100d49c2a7525f51cb0f5d696a4a6d1c78c..ddd7fe4d489f2c4c3ce82b51c84934019a7616b4\n--- a/AGENTS.md\n+++ b/AGENTS.md\n@@ -40,7 +40,7 @@\n 1. GitHub Issues, Pull Requests, Checks, Reviews, comments, and merge evidence are the operational source of truth.\n 2. The repository-local `.shiki/` mirror records Goals, PRDs, plans, Task DAGs, contracts, locks, ledger entries, CCA verdicts, repair packets, reports, and handoffs.\n 3. `CONTEXT.md` defines Shiki domain language and glossary decisions.\n-4. `docs/adr/` records hard-to-reverse platform decisions.\n+4. `docs/adr/SADR-NNNN-*.md` records Shiki platform decisions; `docs/adr/NNNN-*.md` is reserved for target ADRs.\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \ndiff --git a/CLAUDE.md b/CLAUDE.md\nindex 98a932e7841a632866fcd53b0bad0e3af3711d3d..83e001d4c15ed0a421666dc68da548ecb56748df\n--- a/CLAUDE.md\n+++ b/CLAUDE.md\n@@ -10,7 +10,7 @@\n 1. GitHub Issues, Pull Requests, Checks, Reviews, comments, and merge evidence are the operational source of truth.\n 2. The repository-local `.shiki/` mirror records Goals, PRDs, plans, Task DAGs, contracts, locks, ledger entries, CCA verdicts, repair packets, reports, and handoffs.\n 3. `CONTEXT.md` defines Shiki domain language and glossary decisions.\n-4. `docs/adr/` records hard-to-reverse platform decisions.\n+4. `docs/adr/SADR-NNNN-*.md` records Shiki platform decisions; `docs/adr/NNNN-*.md` is reserved for target ADRs.\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \ndiff --git a/SYSTEM_PROMPT.md b/SYSTEM_PROMPT.md\nindex aaaea45e3a896c3a44ba89eec8bb84325ecf6c62..42087a9325dfaa48de71b8319017175689166d2e\n--- a/SYSTEM_PROMPT.md\n+++ b/SYSTEM_PROMPT.md\n@@ -26,7 +26,7 @@\n 1. GitHub Issues, Pull Requests, Checks, Reviews, comments, and merge evidence are the operational source of truth.\n 2. The repository-local `.shiki/` mirror records Goals, PRDs, plans, Task DAGs, contracts, locks, ledger entries, CCA verdicts, repair packets, reports, and handoffs.\n 3. `CONTEXT.md` defines Shiki domain language and glossary decisions.\n-4. `docs/adr/` records hard-to-reverse platform decisions.\n+4. `docs/adr/SADR-NNNN-*.md` records Shiki platform decisions; `docs/adr/NNNN-*.md` is reserved for target ADRs.\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \ndiff --git a/docs/adr/0001-keep-shiki-as-platform-name.md b/docs/adr/SADR-0001-keep-shiki-as-platform-name.md\nindex 20a9637c3b9d8c97de7ba8db7cfae4d82d9a7dba..8b685439959aada1b221c112f0ac3c51ca8a1695\n--- a/docs/adr/0001-keep-shiki-as-platform-name.md\n+++ b/docs/adr/SADR-0001-keep-shiki-as-platform-name.md\n@@ -1,3 +1,3 @@\n-# Keep Shiki As The Platform Name\n+# SADR-0001: Keep Shiki As The Platform Name\n \n Shiki remains the product name for the GitHub-native agentic engineering control plane. We considered more descriptive names such as MergePilot, MergeGate, and Kiroku, but kept Shiki because it carries the existing goal-seeking framework identity while remaining independent of Hermes, Codex, Claude Code, or any single runtime.\ndiff --git a/docs/adr/0002-use-agents-as-shared-constitution.md b/docs/adr/SADR-0002-use-agents-as-shared-constitution.md\nindex 73de30ecb078120eac540d9bc51855e3cd48d0e3..c5486182bf06fca36ea508496b013c67302fd989\n--- a/docs/adr/0002-use-agents-as-shared-constitution.md\n+++ b/docs/adr/SADR-0002-use-agents-as-shared-constitution.md\n@@ -1,3 +1,3 @@\n-# Use AGENTS As The Shared Agent Constitution\n+# SADR-0002: Use AGENTS As The Shared Agent Constitution\n \n Shiki uses `AGENTS.md` as the runtime-neutral constitution and `CLAUDE.md` as a thin Claude Code wrapper. This keeps Codex, Claude Code, Hermes Runner, and future Agent Runtimes aligned on the same GitHub-first MergeGate rules while still allowing runtime-specific instructions where needed.\ndiff --git a/docs/adr/0003-require-skill-gate-for-mergegate-readiness.md b/docs/adr/SADR-0003-require-skill-gate-for-mergegate-readiness.md\nindex 424be0c9af4c8f0136f1fde68a468c47ff84e764..7fc337987c7df9792f75f181dfcbed75fe5ffff2\n--- a/docs/adr/0003-require-skill-gate-for-mergegate-readiness.md\n+++ b/docs/adr/SADR-0003-require-skill-gate-for-mergegate-readiness.md\n@@ -1,3 +1,3 @@\n-# Require Skill Gate For MergeGate Readiness\n+# SADR-0003: Require Skill Gate For MergeGate Readiness\n \n Shiki requires relevant engineering skills to be invoked before execution when their triggers apply. This makes skills part of MergeGate readiness rather than optional prompt style, because Shiki depends on repeatable planning, issue decomposition, TDD, diagnosis, triage, and architecture review before autonomous or parallel agent work can be trusted.\ndiff --git a/docs/adr/0004-use-github-first-with-shiki-mirror.md b/docs/adr/SADR-0004-use-github-first-with-shiki-mirror.md\nindex d0a3a9174dc646a945a9d325954ffaf00c352040..a79c54ae9ee8eed2a0524192b864da4e22a92d3f\n--- a/docs/adr/0004-use-github-first-with-shiki-mirror.md\n+++ b/docs/adr/SADR-0004-use-github-first-with-shiki-mirror.md\n@@ -1,4 +1,4 @@\n-# ADR 0004: Use GitHub First With A Repository-Local Shiki Mirror\n+# SADR-0004: Use GitHub First With A Repository-Local Shiki Mirror\n \n ## Status\n \ndiff --git a/docs/adr/0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md b/docs/adr/SADR-0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\nindex b339baf49b89d9290e79c3d51b36e7b2bde3bd95..c30a0d8b1fde0224d0ac7820e7870dccf721580d\n--- a/docs/adr/0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\n+++ b/docs/adr/SADR-0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\n@@ -1,4 +1,4 @@\n-# ADR 0005: Use Codex As Front And Claude Code Action As The GitHub Runtime\n+# SADR-0005: Use Codex As Front And Claude Code Action As The GitHub Runtime\n \n ## Status\n \ndiff --git a/docs/adr/0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md b/docs/adr/SADR-0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\nindex 4bcae9d1e52f0a37b8e9fbafa5cd14b6ee1993bd..7a38538a6ae9929567f0ade67e7ea6a60d0b3f7e\n--- a/docs/adr/0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\n+++ b/docs/adr/SADR-0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\n@@ -1,4 +1,4 @@\n-# ADR 0006: Separate Codex Implementation, CCA Completion, And MergeGate Authorization\n+# SADR-0006: Separate Codex Implementation, CCA Completion, And MergeGate Authorization\n \n ## Status\n \ndiff --git a/docs/adr/0007-packaging-and-release.md b/docs/adr/SADR-0007-packaging-and-release.md\nindex 79b84ddb97167372b44641e5b7860e400ce80822..023cd57f8011aa6e6d2ebf612b7c35a7a82c00d4\n--- a/docs/adr/0007-packaging-and-release.md\n+++ b/docs/adr/SADR-0007-packaging-and-release.md\n@@ -1,4 +1,4 @@\n-# ADR 0007: Packaging And Release For Shiki\n+# SADR-0007: Packaging And Release For Shiki\n \n ## Status\n \ndiff --git a/docs/adr/0008-use-claude-code-as-default-implementer.md b/docs/adr/SADR-0008-use-claude-code-as-default-implementer.md\nindex dedf4510b7c7799521422f622029563d373db031..a158a8d444d93764ec515a28133f7dde4af23579\n--- a/docs/adr/0008-use-claude-code-as-default-implementer.md\n+++ b/docs/adr/SADR-0008-use-claude-code-as-default-implementer.md\n@@ -1,4 +1,4 @@\n-# ADR 0008: Use Claude Code As The Default Implementer Runtime\n+# SADR-0008: Use Claude Code As The Default Implementer Runtime\n \n ## Status\n \ndiff --git a/docs/adr/0009-requirements-definition-and-spec-freeze.md b/docs/adr/SADR-0009-requirements-definition-and-spec-freeze.md\nindex 29b3f7871bef9203a8b4a831a65c39eb2f506e42..52e7dd69ad0aa4efe0b68c62678abbf7df02aaec\n--- a/docs/adr/0009-requirements-definition-and-spec-freeze.md\n+++ b/docs/adr/SADR-0009-requirements-definition-and-spec-freeze.md\n@@ -1,4 +1,4 @@\n-# ADR 0009: Merge Grill And Planning Into Requirements Definition With Spec Freeze\n+# SADR-0009: Merge Grill And Planning Into Requirements Definition With Spec Freeze\n \n ## Status\n \ndiff --git a/docs/adr/0010-external-ai-guardian-review.md b/docs/adr/SADR-0010-external-ai-guardian-review.md\nindex e6108bbe8bb4e73e0ff4684c8f78001a80438c4e..e690d4eddd270e683d709c9b679c5859e71eeaaa\n--- a/docs/adr/0010-external-ai-guardian-review.md\n+++ b/docs/adr/SADR-0010-external-ai-guardian-review.md\n@@ -1,4 +1,4 @@\n-# ADR 0010: External AI Guardian Review As A First-Class Approval Authority\n+# SADR-0010: External AI Guardian Review As A First-Class Approval Authority\n \n ## Status\n \ndiff --git a/docs/adr/0011-autonomous-quality-gate-evidence.md b/docs/adr/SADR-0011-autonomous-quality-gate-evidence.md\nindex bca0246c5d46e44afbde15486a75eec425fc02f5..9c93eac16d8c3a6d4b11bd7224e3955102cd7db4\n--- a/docs/adr/0011-autonomous-quality-gate-evidence.md\n+++ b/docs/adr/SADR-0011-autonomous-quality-gate-evidence.md\n@@ -1,4 +1,4 @@\n-# ADR 0011: Autonomous Quality-Gate Evidence \u2014 Deterministic Facts And An Independent Verifier\n+# SADR-0011: Autonomous Quality-Gate Evidence \u2014 Deterministic Facts And An Independent Verifier\n \n ## Status\n \ndiff --git a/docs/adr/0012-autonomous-loop-closeout-to-main.md b/docs/adr/SADR-0012-autonomous-loop-closeout-to-main.md\nindex 637fad6dde4989c9c2bf0f81f676d8d29991123c..ec7d85f81030a676bcbba28b43eb365422a36eac\n--- a/docs/adr/0012-autonomous-loop-closeout-to-main.md\n+++ b/docs/adr/SADR-0012-autonomous-loop-closeout-to-main.md\n@@ -1,4 +1,4 @@\n-# 0012 \u2014 Autonomous loop pushes goal completion to main via a closeout PR\n+# SADR-0012 \u2014 Autonomous loop pushes goal completion to main via a closeout PR\n \n - Status: accepted\n - Date: 2026-06-18\ndiff --git a/docs/adr/0013-bootstrap-grants-actions-approve-pull-requests.md b/docs/adr/SADR-0013-bootstrap-grants-actions-approve-pull-requests.md\nindex b6c69d64f1c5ced346b88d6859586715b3b5bd0e..a793e7277d67ab8ea56286fec289ca689825a17d\n--- a/docs/adr/0013-bootstrap-grants-actions-approve-pull-requests.md\n+++ b/docs/adr/SADR-0013-bootstrap-grants-actions-approve-pull-requests.md\n@@ -1,4 +1,4 @@\n-# ADR 0013: Bootstrap Grants GitHub Actions Permission To Create And Approve Pull Requests\n+# SADR-0013: Bootstrap Grants GitHub Actions Permission To Create And Approve Pull Requests\n \n ## Status\n \ndiff --git a/docs/adr/0014-codex-app-external-ai-guardian-ui-adapter.md b/docs/adr/SADR-0014-codex-app-external-ai-guardian-ui-adapter.md\nindex 238db06279334caa341c117362df9271382a7fcd..65f12e9939321e1b3e389a34c822fc1549e5155d\n--- a/docs/adr/0014-codex-app-external-ai-guardian-ui-adapter.md\n+++ b/docs/adr/SADR-0014-codex-app-external-ai-guardian-ui-adapter.md\n@@ -1,4 +1,4 @@\n-# ADR 0014: Use Codex App As The External AI Guardian UI Adapter\n+# SADR-0014: Use Codex App As The External AI Guardian UI Adapter\n \n ## Status\n \ndiff --git a/docs/adr/0015-contract-approval-before-dispatch.md b/docs/adr/SADR-0015-contract-approval-before-dispatch.md\nindex fad6c656741a20a7684ba16080a7db159f63a52f..7bdd760bb3e82eee4e1410f45c783b03b638d456\n--- a/docs/adr/0015-contract-approval-before-dispatch.md\n+++ b/docs/adr/SADR-0015-contract-approval-before-dispatch.md\n@@ -1,4 +1,4 @@\n-# ADR 0015: Approve The Task Contract Before Dispatch, Not The Diff After It\n+# SADR-0015: Approve The Task Contract Before Dispatch, Not The Diff After It\n \n ## Status\n \ndiff --git a/docs/adr/0016-locks-bound-product-paths.md b/docs/adr/SADR-0016-locks-bound-product-paths.md\nindex b5e850f3f1f7f4ef6e7f9d1763a29a51bd300c45..332cc17313498404f41a145291e38447b5de0dc4\n--- a/docs/adr/0016-locks-bound-product-paths.md\n+++ b/docs/adr/SADR-0016-locks-bound-product-paths.md\n@@ -1,4 +1,4 @@\n-# ADR 0016: Locks bound product paths; the Shiki mirror is bound by record identity\n+# SADR-0016: Locks bound product paths; the Shiki mirror is bound by record identity\n \n ## Status\n \ndiff --git a/docs/adr/0017-bookkeeping-closeouts-do-not-inherit-risk.md b/docs/adr/SADR-0017-bookkeeping-closeouts-do-not-inherit-risk.md\nindex 554e3a0d526a72ca067108c5464cc68c3edcf174..83115f69f1b1b48d544c9b0157f377b91cb53abf\n--- a/docs/adr/0017-bookkeeping-closeouts-do-not-inherit-risk.md\n+++ b/docs/adr/SADR-0017-bookkeeping-closeouts-do-not-inherit-risk.md\n@@ -1,4 +1,4 @@\n-# ADR 0017: A Bookkeeping Closeout Does Not Inherit Its Task's Risk\n+# SADR-0017: A Bookkeeping Closeout Does Not Inherit Its Task's Risk\n \n ## Status\n \ndiff --git a/docs/adr/0018-carried-guardian-approval-across-a-proven-base-sync.md b/docs/adr/SADR-0018-carried-guardian-approval-across-a-proven-base-sync.md\nindex 22358562da390ed8b45cd600da08a43d378b4680..38da0fd95102c3496c3219ce2319fce696a2532a\n--- a/docs/adr/0018-carried-guardian-approval-across-a-proven-base-sync.md\n+++ b/docs/adr/SADR-0018-carried-guardian-approval-across-a-proven-base-sync.md\n@@ -1,4 +1,4 @@\n-# ADR 0018: Carried Guardian Approval Across A Proven Base Sync\n+# SADR-0018: Carried Guardian Approval Across A Proven Base Sync\n \n ## Status\n \ndiff --git a/docs/adr/README.md b/docs/adr/README.md\nindex c70e057a4520c36e042ab68c53cd4a21ef709b71..5da43856836e2ce6a150f8127766e2f88355ab4b\n--- a/docs/adr/README.md\n+++ b/docs/adr/README.md\n@@ -1,40 +1,41 @@\n-# Architecture Decision Records\n+# Decision Record Namespaces\n \n-This directory holds Shiki's Architecture Decision Records (ADRs): durable\n-records of hard-to-reverse platform decisions. ADRs are part of the Shiki source\n-of truth alongside GitHub state, the `.shiki/` mirror, and `CONTEXT.md`.\n+This directory holds two independent decision namespaces:\n \n-## How ADRs Work\n+- Shiki Architecture Decision Records (SADRs), named\n+  `SADR-NNNN-<short-slug>.md`, record hard-to-reverse Shiki platform decisions.\n+- Target Architecture Decision Records (ADRs), named\n+  `NNNN-<short-slug>.md`, belong to the target product.\n \n-- New ADRs are added as `docs/adr/NNNN-<short-slug>.md`, where `NNNN` is the next\n-  zero-padded sequence number.\n-- The **canonical list of ADRs is the set of files matching `docs/adr/*.md`** in\n-  this directory. Browse `docs/adr/*.md` for the authoritative, current set.\n-- Use [`template.md`](template.md) as the starting point for a new ADR.\n-- Record an ADR when a decision is hard to reverse, surprising without context,\n-  and a real tradeoff. Do not write an ADR for routine or easily reversible\n-  choices.\n+The same four-digit number may appear once in each namespace. A platform\n+`SADR-NNNN` citation resolves only to `SADR-NNNN-*.md`; a target `ADR NNNN`\n+citation resolves only to `NNNN-*.md`.\n \n-Using the `docs/adr/*.md` glob as the source of truth means other PRs can add an\n-ADR without editing this index file, avoiding merge contention on a\n-hand-maintained enumeration.\n+## How Decision Records Work\n \n-## Current ADRs (snapshot)\n-\n-The list below is a convenience snapshot and may lag behind the directory. When\n-in doubt, list `docs/adr/*.md`.\n+- New Shiki platform decisions use the next zero-padded SADR number.\n+- New target product decisions use the target's own ADR sequence.\n+- The canonical list is the files in `docs/adr/`; no hand-maintained index is\n+  authoritative.\n+- Use [`template.md`](template.md) as the starting point and choose the filename\n+  namespace that owns the decision.\n+- Record a decision when it is hard to reverse, surprising without context, and\n+  a real tradeoff. Do not write a decision record for routine or easily\n+  reversible choices.\n \n-- [0001 \u2014 Keep Shiki As The Platform Name](0001-keep-shiki-as-platform-name.md)\n-- [0002 \u2014 Use AGENTS As The Shared Agent Constitution](0002-use-agents-as-shared-constitution.md)\n-- [0003 \u2014 Require Skill Gate For MergeGate Readiness](0003-require-skill-gate-for-mergegate-readiness.md)\n-- [0004 \u2014 Use GitHub First With A Repository-Local Shiki Mirror](0004-use-github-first-with-shiki-mirror.md)\n-- [0005 \u2014 Use Codex As Front And Claude Code Action As The GitHub Runtime](0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md)\n-- [0006 \u2014 Separate Codex Implementation, CCA Completion, And MergeGate Authorization](0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md)\n+Using the directory as the source of truth lets independent changes add records\n+without contending on an enumerated index.\n \n-## Adding A New ADR\n+## Adding A Shiki SADR\n \n-1. Copy `template.md` to `docs/adr/NNNN-<short-slug>.md`.\n+1. Copy `template.md` to `docs/adr/SADR-NNNN-<short-slug>.md`.\n 2. Fill in the title, status, context, decision, and consequences.\n-3. Set the status to `Proposed`, then `Accepted` once the decision is approved.\n-4. Open a PR following `CONTRIBUTING.md`. You do not need to edit this index for\n-   the ADR to be discoverable via `docs/adr/*.md`.\n+3. Set the status to `Proposed`, then `Accepted` once the proper authority has\n+   approved the decision.\n+4. Open a PR following `CONTRIBUTING.md`.\n+\n+## Adding A Target ADR\n+\n+1. Copy `template.md` to `docs/adr/NNNN-<short-slug>.md` using the target's own\n+   sequence.\n+2. Cite it as `ADR NNNN`; do not prefix its filename with `SADR-`.\ndiff --git a/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md b/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\nnew file mode 100644\nindex 0000000000000000000000000000000000000000..7455ce1f3bed02ddddb851ac21a1d1a648c9aeba\n--- /dev/null\n+++ b/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\n@@ -0,0 +1,55 @@\n+# SADR-0019: Separate Shiki Platform SADRs From Target ADRs\n+\n+## Status\n+\n+Accepted\n+\n+## Context\n+\n+Shiki installs its platform decision records into `docs/adr/` in every target\n+repository. Shiki and its targets previously used the same numeric\n+`NNNN-<slug>.md` namespace. A target that already owned ADRs 0013 through 0018\n+therefore acquired two records for each number during installation, making an\n+`ADR NNNN` citation ambiguous. Renumbering target decisions would rewrite target\n+history, while renumbering Shiki decisions per target would destroy their stable\n+platform identity.\n+\n+Upgrades also need to remove the former Shiki-owned numeric paths without ever\n+mistaking a target-owned ADR for platform content. The install stamp already\n+records exact shipped paths and content digests, so it can prove ownership only\n+when both still match.\n+\n+## Decision\n+\n+Shiki platform decisions use the `SADR` namespace:\n+\n+- records are named `docs/adr/SADR-NNNN-<slug>.md`;\n+- live platform citations use `SADR-NNNN`;\n+- target product decisions retain `docs/adr/NNNN-<slug>.md` and `ADR NNNN`;\n+- the reference validator resolves the two namespaces independently, so the same\n+  four-digit number may exist once in each namespace.\n+\n+Before a forced upgrade writes anything, the installer inventories the exact\n+former numeric Shiki ADR paths. It may delete a legacy path only when the\n+existing install stamp contains that exact path and its recorded SHA-256 digest\n+matches the file currently on disk. All ownership blockers are reported\n+together and abort the upgrade before its first write. A non-forced install\n+never performs this cleanup.\n+\n+Historical `.shiki/` evidence remains unchanged because it records the namespace\n+that existed when that evidence was produced.\n+\n+## Consequences\n+\n+- Targets may use any ADR number without colliding with Shiki's platform SADRs.\n+- Fresh installs preserve target ADR bytes and install a stable SADR set.\n+- Forced upgrades fail closed when legacy ownership cannot be proven, and a\n+  successful upgrade refreshes the stamp without removed legacy paths.\n+- Current governance, packaging, workflow, prompt, script, and skill references\n+  must move with the platform records.\n+\n+## Alternatives Considered\n+\n+- Collision-only refusal leaves affected targets permanently unupgradable.\n+- Renumbering target ADRs mutates target-owned history.\n+- Per-target Shiki renumbering makes platform citations non-portable.\ndiff --git a/docs/adr/template.md b/docs/adr/template.md\nindex a92a97daddd0b00ab9dd626cc8d86942da2eafed..17dd79175b5fee2105935557f691b40cf9492942\n--- a/docs/adr/template.md\n+++ b/docs/adr/template.md\n@@ -1,14 +1,14 @@\n-# NNNN: <Short Decision Title>\n+# <SADR-NNNN or NNNN>: <Short Decision Title>\n \n ## Status\n \n-Proposed | Accepted | Superseded by ADR-NNNN\n+Proposed | Accepted | Superseded by SADR-NNNN or target ADR NNNN\n \n ## Context\n \n What is the situation, constraint, or force that requires a decision? Reference\n-relevant domain terms (`CONTEXT.md`), prior ADRs, and code reality. State the\n-problem, not the solution.\n+relevant domain terms (`CONTEXT.md`), prior Shiki SADRs or target ADRs, and code\n+reality. State the problem, not the solution.\n \n ## Decision\n \ndiff --git a/docs/agents/domain.md b/docs/agents/domain.md\nindex 258da8ea81e87ac0a6b0b5b5e5c2fde980eaabb7..67aaa6f4b7d0a5aa4d495ce6279073333f7798cb\n--- a/docs/agents/domain.md\n+++ b/docs/agents/domain.md\n@@ -5,7 +5,9 @@\n ## Layout\n \n - `CONTEXT.md` defines Shiki language. Keep it implementation-free.\n-- `docs/adr/` records hard-to-reverse decisions.\n+- `docs/adr/SADR-NNNN-*.md` records Shiki platform decisions.\n+- `docs/adr/NNNN-*.md` records target product decisions without sharing the\n+  SADR namespace.\n - `AGENTS.md` is the runtime-neutral agent constitution.\n - `CLAUDE.md` is the Claude Code wrapper around `AGENTS.md`.\n - `.shiki/` is the Target Repository mirror for Goals, PRDs, plans, Task DAGs, contracts, locks, worktree records, ledger entries, CCA verdicts, repair packets, reports, and handoffs.\ndiff --git a/docs/agents/implementation-policy.md b/docs/agents/implementation-policy.md\nindex c754144a9fb5bf4a38aa82a60e1670c59e626b44..3b059d128ce1338ee49eadec0dbd58f3cc2cc594\n--- a/docs/agents/implementation-policy.md\n+++ b/docs/agents/implementation-policy.md\n@@ -27,7 +27,7 @@\n 1. GitHub Issues, Pull Requests, Checks, Reviews, comments, and merge evidence are the operational source of truth.\n 2. The repository-local `.shiki/` mirror records Goals, PRDs, plans, Task DAGs, contracts, locks, ledger entries, CCA verdicts, repair packets, reports, and handoffs.\n 3. `CONTEXT.md` defines Shiki domain language and glossary decisions.\n-4. `docs/adr/` records hard-to-reverse platform decisions.\n+4. `docs/adr/SADR-NNNN-*.md` records Shiki platform decisions; `docs/adr/NNNN-*.md` is reserved for target ADRs.\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \ndiff --git a/scripts/shiki_contracts.py b/scripts/shiki_contracts.py\nindex 18076f0a70a1f8c9d4475a005bcccb1472a246c1..9d3f08dc1172b100c3a9b60a21ffdbae0f4dd44b\n--- a/scripts/shiki_contracts.py\n+++ b/scripts/shiki_contracts.py\n@@ -100,7 +100,7 @@\n     \"GitHub Issues, Pull Requests, Checks, Reviews, comments, and merge evidence are the operational source of truth.\",\n     \"The repository-local `.shiki/` mirror records Goals, PRDs, plans, Task DAGs, contracts, locks, ledger entries, CCA verdicts, repair packets, reports, and handoffs.\",\n     \"`CONTEXT.md` defines Shiki domain language and glossary decisions.\",\n-    \"`docs/adr/` records hard-to-reverse platform decisions.\",\n+    \"`docs/adr/SADR-NNNN-*.md` records Shiki platform decisions; `docs/adr/NNNN-*.md` is reserved for target ADRs.\",\n     \"Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\",\n )\n \ndiff --git a/tests/test_adr_references.py b/tests/test_adr_references.py\nindex 51706329c797d5dd9b47e082a333fa83b261d482..92569d9fa25e22cfd71c17561d715485027a0d2d\n--- a/tests/test_adr_references.py\n+++ b/tests/test_adr_references.py\n@@ -31,17 +31,18 @@\n \n REPO_ROOT = Path(__file__).resolve().parents[1]\n \n-# \"ADR 0016\" -- a four-digit citation. The trailing \\b keeps a five-digit token\n-# from being read as a truncated four-digit number.\n-_CITATION = re.compile(r\"\\bADR (\\d{4})\\b\")\n-# \"0016-locks-bound-product-paths.md\" -- the leading four digits name the record.\n-_ADR_FILENAME = re.compile(r\"^(\\d{4})-.*\\.md$\")\n+# ``ADR 0016`` / ``SADR-0016`` -- a namespace plus four-digit citation. The\n+# trailing ``\\b`` keeps a five-digit token from being read as a truncated one.\n+_CITATION = re.compile(r\"\\b(S?ADR)[ -](\\d{4})\\b\")\n+# Target ADRs retain the numeric filename; Shiki platform decisions use SADR.\n+_DECISION_FILENAME = re.compile(r\"^(?:(SADR)-)?(\\d{4})-.*\\.md$\")\n \n \n class Violation(NamedTuple):\n     \"\"\"A citation that does not resolve to exactly one decision record.\"\"\"\n \n     citing_file: str  # repo-relative path of the file that cites the ADR\n+    namespace: str  # \"ADR\" for target decisions; \"SADR\" for Shiki decisions\n     number: str  # the cited four-digit number, e.g. \"0016\"\n     line: int  # 1-based line of the citation\n     reason: str  # \"missing\" (zero matches) or \"ambiguous\" (two or more)\n@@ -49,32 +50,34 @@\n \n     @property\n     def expected_path(self) -> str:\n-        return f\"docs/adr/{self.number}-*.md\"\n+        prefix = \"SADR-\" if self.namespace == \"SADR\" else \"\"\n+        return f\"docs/adr/{prefix}{self.number}-*.md\"\n \n     def describe(self) -> str:\n         \"\"\"Name the citing file, the cited number, and the missing path.\"\"\"\n         if self.reason == \"missing\":\n             return (\n-                f\"{self.citing_file}:{self.line} cites ADR {self.number} \"\n+                f\"{self.citing_file}:{self.line} cites {self.namespace} {self.number} \"\n                 f\"but no document matches {self.expected_path}\"\n             )\n         joined = \", \".join(self.matches)\n         return (\n-            f\"{self.citing_file}:{self.line} cites ADR {self.number} which \"\n+            f\"{self.citing_file}:{self.line} cites {self.namespace} {self.number} which \"\n             f\"resolves to {len(self.matches)} documents (expected exactly one \"\n             f\"at {self.expected_path}): {joined}\"\n         )\n \n \n-def _adr_index(root: Path) -> dict[str, list[Path]]:\n-    \"\"\"Map every four-digit ADR number to the record files that claim it.\"\"\"\n-    index: dict[str, list[Path]] = {}\n+def _adr_index(root: Path) -> dict[tuple[str, str], list[Path]]:\n+    \"\"\"Map each decision namespace and number to the files that claim it.\"\"\"\n+    index: dict[tuple[str, str], list[Path]] = {}\n     adr_dir = root / \"docs\" / \"adr\"\n     if adr_dir.is_dir():\n         for path in sorted(adr_dir.glob(\"*.md\")):\n-            match = _ADR_FILENAME.match(path.name)\n+            match = _DECISION_FILENAME.match(path.name)\n             if match:\n-                index.setdefault(match.group(1), []).append(path)\n+                namespace = \"SADR\" if match.group(1) else \"ADR\"\n+                index.setdefault((namespace, match.group(2)), []).append(path)\n     return index\n \n \n@@ -110,13 +113,15 @@\n         rel = path.relative_to(root).as_posix()\n         for lineno, line in enumerate(text.splitlines(), start=1):\n             for match in _CITATION.finditer(line):\n-                number = match.group(1)\n-                found = index.get(number, [])\n+                namespace, number = match.groups()\n+                found = index.get((namespace, number), [])\n                 if len(found) == 1:\n                     continue\n                 reason = \"missing\" if not found else \"ambiguous\"\n                 rels = [p.relative_to(root).as_posix() for p in found]\n-                violations.append(Violation(rel, number, lineno, reason, rels))\n+                violations.append(\n+                    Violation(rel, namespace, number, lineno, reason, rels)\n+                )\n     return violations\n \n \n@@ -146,8 +151,8 @@\n         # The two records this Goal carried onto the default branch, and which\n         # the scripts cite most, must be present.\n         index = _adr_index(REPO_ROOT)\n-        self.assertIn(\"0016\", index)\n-        self.assertIn(\"0017\", index)\n+        self.assertIn((\"ADR\", \"0016\"), index)\n+        self.assertIn((\"ADR\", \"0017\"), index)\n \n \n class FixtureAdrReferences(unittest.TestCase):\n@@ -180,6 +185,17 @@\n         self.assertIn(\"9991\", message)  # cited number\n         self.assertIn(\"docs/adr/9991-*.md\", message)  # missing path\n \n+    def test_target_adr_does_not_satisfy_missing_platform_sadr(self) -> None:\n+        root = self._make_repo(\n+            adr_names=[\"0042-target-decision.md\"],\n+            script_body='\"\"\"Target ADR 0042 exists, but platform SADR-0042 does not.\"\"\"\\n',\n+        )\n+        violations = find_adr_reference_violations(root, include_docs=False)\n+        self.assertEqual(len(violations), 1)\n+        self.assertEqual(violations[0].namespace, \"SADR\")\n+        self.assertEqual(violations[0].number, \"0042\")\n+        self.assertEqual(violations[0].expected_path, \"docs/adr/SADR-0042-*.md\")\n+\n     def test_uncited_adr_is_allowed(self) -> None:\n         # An ADR exists but nothing cites it -> not a violation. The converse\n         # (every ADR must be cited) is explicitly NOT required.\ndiff --git a/tests/test_sadr_install_migration.py b/tests/test_sadr_install_migration.py\nnew file mode 100644\nindex 0000000000000000000000000000000000000000..a6398c81377f00c09aa2cb684980b070cfaf714c\n--- /dev/null\n+++ b/tests/test_sadr_install_migration.py\n@@ -0,0 +1,56 @@\n+\"\"\"SADR installation and collision-safe legacy upgrade behavior.\"\"\"\n+\n+from __future__ import annotations\n+\n+import shutil\n+import tempfile\n+import unittest\n+from pathlib import Path\n+\n+import shiki_test_support  # noqa: F401  (path bootstrap)\n+\n+import shiki_installer\n+from tests.test_adr_references import find_adr_reference_violations\n+\n+\n+PRODUCT_NUMBERS = tuple(range(13, 19))\n+\n+\n+class FreshSadrInstallTests(unittest.TestCase):\n+    def setUp(self) -> None:\n+        self.tmp = Path(tempfile.mkdtemp(prefix=\"shiki-sadr-fresh-\"))\n+        self.addCleanup(lambda: shutil.rmtree(self.tmp, ignore_errors=True))\n+        self.target = self.tmp / \"target\"\n+        self.adr_dir = self.target / \"docs\" / \"adr\"\n+        self.adr_dir.mkdir(parents=True)\n+        self.product_bytes = {\n+            f\"{number:04d}-product-decision.md\": (\n+                f\"# Product ADR {number:04d}\\n\\nTarget-owned bytes {number}.\\n\"\n+            ).encode()\n+            for number in PRODUCT_NUMBERS\n+        }\n+        for name, content in self.product_bytes.items():\n+            (self.adr_dir / name).write_bytes(content)\n+\n+    def test_fresh_install_preserves_product_adrs_and_installs_all_sadrs(self) -> None:\n+        shiki_installer.install_template(self.target, force=False, validate=True)\n+\n+        for name, expected in self.product_bytes.items():\n+            self.assertEqual((self.adr_dir / name).read_bytes(), expected)\n+\n+        sadr_numbers = {\n+            path.name.split(\"-\", 2)[1]\n+            for path in self.adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\")\n+        }\n+        self.assertEqual(sadr_numbers, {f\"{number:04d}\" for number in range(1, 20)})\n+\n+        numeric_records = {\n+            path.name\n+            for path in self.adr_dir.glob(\"[0-9][0-9][0-9][0-9]-*.md\")\n+        }\n+        self.assertEqual(numeric_records, set(self.product_bytes))\n+        self.assertEqual(find_adr_reference_violations(self.target), [])\n+\n+\n+if __name__ == \"__main__\":\n+    unittest.main()\n\ndiff --git a/.codex/skills/shiki/SKILL.md b/.codex/skills/shiki/SKILL.md\nindex c8e2e85d9d75d49cc4f2d6286ee22659c4e53e75..c392c28442311b8851ede2cf66af14011d8038d4\n--- a/.codex/skills/shiki/SKILL.md\n+++ b/.codex/skills/shiki/SKILL.md\n@@ -14,7 +14,7 @@\n 1. GitHub Issues, Pull Requests, Checks, Reviews, comments, and merge evidence are the operational source of truth.\n 2. The repository-local `.shiki/` mirror records Goals, PRDs, plans, Task DAGs, contracts, locks, ledger entries, CCA verdicts, repair packets, reports, and handoffs.\n 3. `CONTEXT.md` defines Shiki domain language and glossary decisions.\n-4. `docs/adr/` records hard-to-reverse platform decisions.\n+4. `docs/adr/SADR-NNNN-*.md` records Shiki platform decisions; `docs/adr/NNNN-*.md` is reserved for target ADRs.\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n@@ -69,7 +69,7 @@\n \n ## Responsibilities\n \n-- Claude Code implements and repairs by default (`shiki runner claude`, ADR 0008); Codex implements and repairs tasks explicitly assigned to `codex`.\n+- Claude Code implements and repairs by default (`shiki runner claude`, SADR-0008); Codex implements and repairs tasks explicitly assigned to `codex`.\n - Claude Code Action can act as GitHub-side CCA or reviewer.\n - CCA judges completion.\n - MergeGate authorizes state transitions and merge readiness.\ndiff --git a/.github/prompts/cca-completion-check.md b/.github/prompts/cca-completion-check.md\nindex ed203ad5f302ecc81372d8ab27d2b1232ca97497..3137954883be3c4657b40ea1a1253b0dc4b4ef69\n--- a/.github/prompts/cca-completion-check.md\n+++ b/.github/prompts/cca-completion-check.md\n@@ -15,7 +15,7 @@\n 1. GitHub Issues, Pull Requests, Checks, Reviews, comments, and merge evidence are the operational source of truth.\n 2. The repository-local `.shiki/` mirror records Goals, PRDs, plans, Task DAGs, contracts, locks, ledger entries, CCA verdicts, repair packets, reports, and handoffs.\n 3. `CONTEXT.md` defines Shiki domain language and glossary decisions.\n-4. `docs/adr/` records hard-to-reverse platform decisions.\n+4. `docs/adr/SADR-NNNN-*.md` records Shiki platform decisions; `docs/adr/NNNN-*.md` is reserved for target ADRs.\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \ndiff --git a/AGENTS.md b/AGENTS.md\nindex 7f041100d49c2a7525f51cb0f5d696a4a6d1c78c..ddd7fe4d489f2c4c3ce82b51c84934019a7616b4\n--- a/AGENTS.md\n+++ b/AGENTS.md\n@@ -40,7 +40,7 @@\n 1. GitHub Issues, Pull Requests, Checks, Reviews, comments, and merge evidence are the operational source of truth.\n 2. The repository-local `.shiki/` mirror records Goals, PRDs, plans, Task DAGs, contracts, locks, ledger entries, CCA verdicts, repair packets, reports, and handoffs.\n 3. `CONTEXT.md` defines Shiki domain language and glossary decisions.\n-4. `docs/adr/` records hard-to-reverse platform decisions.\n+4. `docs/adr/SADR-NNNN-*.md` records Shiki platform decisions; `docs/adr/NNNN-*.md` is reserved for target ADRs.\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \ndiff --git a/CLAUDE.md b/CLAUDE.md\nindex 98a932e7841a632866fcd53b0bad0e3af3711d3d..83e001d4c15ed0a421666dc68da548ecb56748df\n--- a/CLAUDE.md\n+++ b/CLAUDE.md\n@@ -10,7 +10,7 @@\n 1. GitHub Issues, Pull Requests, Checks, Reviews, comments, and merge evidence are the operational source of truth.\n 2. The repository-local `.shiki/` mirror records Goals, PRDs, plans, Task DAGs, contracts, locks, ledger entries, CCA verdicts, repair packets, reports, and handoffs.\n 3. `CONTEXT.md` defines Shiki domain language and glossary decisions.\n-4. `docs/adr/` records hard-to-reverse platform decisions.\n+4. `docs/adr/SADR-NNNN-*.md` records Shiki platform decisions; `docs/adr/NNNN-*.md` is reserved for target ADRs.\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \ndiff --git a/SYSTEM_PROMPT.md b/SYSTEM_PROMPT.md\nindex aaaea45e3a896c3a44ba89eec8bb84325ecf6c62..42087a9325dfaa48de71b8319017175689166d2e\n--- a/SYSTEM_PROMPT.md\n+++ b/SYSTEM_PROMPT.md\n@@ -26,7 +26,7 @@\n 1. GitHub Issues, Pull Requests, Checks, Reviews, comments, and merge evidence are the operational source of truth.\n 2. The repository-local `.shiki/` mirror records Goals, PRDs, plans, Task DAGs, contracts, locks, ledger entries, CCA verdicts, repair packets, reports, and handoffs.\n 3. `CONTEXT.md` defines Shiki domain language and glossary decisions.\n-4. `docs/adr/` records hard-to-reverse platform decisions.\n+4. `docs/adr/SADR-NNNN-*.md` records Shiki platform decisions; `docs/adr/NNNN-*.md` is reserved for target ADRs.\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \ndiff --git a/docs/adr/0001-keep-shiki-as-platform-name.md b/docs/adr/SADR-0001-keep-shiki-as-platform-name.md\nindex 20a9637c3b9d8c97de7ba8db7cfae4d82d9a7dba..8b685439959aada1b221c112f0ac3c51ca8a1695\n--- a/docs/adr/0001-keep-shiki-as-platform-name.md\n+++ b/docs/adr/SADR-0001-keep-shiki-as-platform-name.md\n@@ -1,3 +1,3 @@\n-# Keep Shiki As The Platform Name\n+# SADR-0001: Keep Shiki As The Platform Name\n \n Shiki remains the product name for the GitHub-native agentic engineering control plane. We considered more descriptive names such as MergePilot, MergeGate, and Kiroku, but kept Shiki because it carries the existing goal-seeking framework identity while remaining independent of Hermes, Codex, Claude Code, or any single runtime.\ndiff --git a/docs/adr/0002-use-agents-as-shared-constitution.md b/docs/adr/SADR-0002-use-agents-as-shared-constitution.md\nindex 73de30ecb078120eac540d9bc51855e3cd48d0e3..c5486182bf06fca36ea508496b013c67302fd989\n--- a/docs/adr/0002-use-agents-as-shared-constitution.md\n+++ b/docs/adr/SADR-0002-use-agents-as-shared-constitution.md\n@@ -1,3 +1,3 @@\n-# Use AGENTS As The Shared Agent Constitution\n+# SADR-0002: Use AGENTS As The Shared Agent Constitution\n \n Shiki uses `AGENTS.md` as the runtime-neutral constitution and `CLAUDE.md` as a thin Claude Code wrapper. This keeps Codex, Claude Code, Hermes Runner, and future Agent Runtimes aligned on the same GitHub-first MergeGate rules while still allowing runtime-specific instructions where needed.\ndiff --git a/docs/adr/0003-require-skill-gate-for-mergegate-readiness.md b/docs/adr/SADR-0003-require-skill-gate-for-mergegate-readiness.md\nindex 424be0c9af4c8f0136f1fde68a468c47ff84e764..7fc337987c7df9792f75f181dfcbed75fe5ffff2\n--- a/docs/adr/0003-require-skill-gate-for-mergegate-readiness.md\n+++ b/docs/adr/SADR-0003-require-skill-gate-for-mergegate-readiness.md\n@@ -1,3 +1,3 @@\n-# Require Skill Gate For MergeGate Readiness\n+# SADR-0003: Require Skill Gate For MergeGate Readiness\n \n Shiki requires relevant engineering skills to be invoked before execution when their triggers apply. This makes skills part of MergeGate readiness rather than optional prompt style, because Shiki depends on repeatable planning, issue decomposition, TDD, diagnosis, triage, and architecture review before autonomous or parallel agent work can be trusted.\ndiff --git a/docs/adr/0004-use-github-first-with-shiki-mirror.md b/docs/adr/SADR-0004-use-github-first-with-shiki-mirror.md\nindex d0a3a9174dc646a945a9d325954ffaf00c352040..a79c54ae9ee8eed2a0524192b864da4e22a92d3f\n--- a/docs/adr/0004-use-github-first-with-shiki-mirror.md\n+++ b/docs/adr/SADR-0004-use-github-first-with-shiki-mirror.md\n@@ -1,4 +1,4 @@\n-# ADR 0004: Use GitHub First With A Repository-Local Shiki Mirror\n+# SADR-0004: Use GitHub First With A Repository-Local Shiki Mirror\n \n ## Status\n \ndiff --git a/docs/adr/0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md b/docs/adr/SADR-0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\nindex b339baf49b89d9290e79c3d51b36e7b2bde3bd95..c30a0d8b1fde0224d0ac7820e7870dccf721580d\n--- a/docs/adr/0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\n+++ b/docs/adr/SADR-0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\n@@ -1,4 +1,4 @@\n-# ADR 0005: Use Codex As Front And Claude Code Action As The GitHub Runtime\n+# SADR-0005: Use Codex As Front And Claude Code Action As The GitHub Runtime\n \n ## Status\n \ndiff --git a/docs/adr/0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md b/docs/adr/SADR-0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\nindex 4bcae9d1e52f0a37b8e9fbafa5cd14b6ee1993bd..7a38538a6ae9929567f0ade67e7ea6a60d0b3f7e\n--- a/docs/adr/0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\n+++ b/docs/adr/SADR-0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\n@@ -1,4 +1,4 @@\n-# ADR 0006: Separate Codex Implementation, CCA Completion, And MergeGate Authorization\n+# SADR-0006: Separate Codex Implementation, CCA Completion, And MergeGate Authorization\n \n ## Status\n \ndiff --git a/docs/adr/0007-packaging-and-release.md b/docs/adr/SADR-0007-packaging-and-release.md\nindex 79b84ddb97167372b44641e5b7860e400ce80822..023cd57f8011aa6e6d2ebf612b7c35a7a82c00d4\n--- a/docs/adr/0007-packaging-and-release.md\n+++ b/docs/adr/SADR-0007-packaging-and-release.md\n@@ -1,4 +1,4 @@\n-# ADR 0007: Packaging And Release For Shiki\n+# SADR-0007: Packaging And Release For Shiki\n \n ## Status\n \ndiff --git a/docs/adr/0008-use-claude-code-as-default-implementer.md b/docs/adr/SADR-0008-use-claude-code-as-default-implementer.md\nindex dedf4510b7c7799521422f622029563d373db031..a158a8d444d93764ec515a28133f7dde4af23579\n--- a/docs/adr/0008-use-claude-code-as-default-implementer.md\n+++ b/docs/adr/SADR-0008-use-claude-code-as-default-implementer.md\n@@ -1,4 +1,4 @@\n-# ADR 0008: Use Claude Code As The Default Implementer Runtime\n+# SADR-0008: Use Claude Code As The Default Implementer Runtime\n \n ## Status\n \ndiff --git a/docs/adr/0009-requirements-definition-and-spec-freeze.md b/docs/adr/SADR-0009-requirements-definition-and-spec-freeze.md\nindex 29b3f7871bef9203a8b4a831a65c39eb2f506e42..52e7dd69ad0aa4efe0b68c62678abbf7df02aaec\n--- a/docs/adr/0009-requirements-definition-and-spec-freeze.md\n+++ b/docs/adr/SADR-0009-requirements-definition-and-spec-freeze.md\n@@ -1,4 +1,4 @@\n-# ADR 0009: Merge Grill And Planning Into Requirements Definition With Spec Freeze\n+# SADR-0009: Merge Grill And Planning Into Requirements Definition With Spec Freeze\n \n ## Status\n \ndiff --git a/docs/adr/0010-external-ai-guardian-review.md b/docs/adr/SADR-0010-external-ai-guardian-review.md\nindex e6108bbe8bb4e73e0ff4684c8f78001a80438c4e..e690d4eddd270e683d709c9b679c5859e71eeaaa\n--- a/docs/adr/0010-external-ai-guardian-review.md\n+++ b/docs/adr/SADR-0010-external-ai-guardian-review.md\n@@ -1,4 +1,4 @@\n-# ADR 0010: External AI Guardian Review As A First-Class Approval Authority\n+# SADR-0010: External AI Guardian Review As A First-Class Approval Authority\n \n ## Status\n \ndiff --git a/docs/adr/0011-autonomous-quality-gate-evidence.md b/docs/adr/SADR-0011-autonomous-quality-gate-evidence.md\nindex bca0246c5d46e44afbde15486a75eec425fc02f5..9c93eac16d8c3a6d4b11bd7224e3955102cd7db4\n--- a/docs/adr/0011-autonomous-quality-gate-evidence.md\n+++ b/docs/adr/SADR-0011-autonomous-quality-gate-evidence.md\n@@ -1,4 +1,4 @@\n-# ADR 0011: Autonomous Quality-Gate Evidence \u2014 Deterministic Facts And An Independent Verifier\n+# SADR-0011: Autonomous Quality-Gate Evidence \u2014 Deterministic Facts And An Independent Verifier\n \n ## Status\n \ndiff --git a/docs/adr/0012-autonomous-loop-closeout-to-main.md b/docs/adr/SADR-0012-autonomous-loop-closeout-to-main.md\nindex 637fad6dde4989c9c2bf0f81f676d8d29991123c..ec7d85f81030a676bcbba28b43eb365422a36eac\n--- a/docs/adr/0012-autonomous-loop-closeout-to-main.md\n+++ b/docs/adr/SADR-0012-autonomous-loop-closeout-to-main.md\n@@ -1,4 +1,4 @@\n-# 0012 \u2014 Autonomous loop pushes goal completion to main via a closeout PR\n+# SADR-0012 \u2014 Autonomous loop pushes goal completion to main via a closeout PR\n \n - Status: accepted\n - Date: 2026-06-18\ndiff --git a/docs/adr/0013-bootstrap-grants-actions-approve-pull-requests.md b/docs/adr/SADR-0013-bootstrap-grants-actions-approve-pull-requests.md\nindex b6c69d64f1c5ced346b88d6859586715b3b5bd0e..a793e7277d67ab8ea56286fec289ca689825a17d\n--- a/docs/adr/0013-bootstrap-grants-actions-approve-pull-requests.md\n+++ b/docs/adr/SADR-0013-bootstrap-grants-actions-approve-pull-requests.md\n@@ -1,4 +1,4 @@\n-# ADR 0013: Bootstrap Grants GitHub Actions Permission To Create And Approve Pull Requests\n+# SADR-0013: Bootstrap Grants GitHub Actions Permission To Create And Approve Pull Requests\n \n ## Status\n \ndiff --git a/docs/adr/0014-codex-app-external-ai-guardian-ui-adapter.md b/docs/adr/SADR-0014-codex-app-external-ai-guardian-ui-adapter.md\nindex 238db06279334caa341c117362df9271382a7fcd..65f12e9939321e1b3e389a34c822fc1549e5155d\n--- a/docs/adr/0014-codex-app-external-ai-guardian-ui-adapter.md\n+++ b/docs/adr/SADR-0014-codex-app-external-ai-guardian-ui-adapter.md\n@@ -1,4 +1,4 @@\n-# ADR 0014: Use Codex App As The External AI Guardian UI Adapter\n+# SADR-0014: Use Codex App As The External AI Guardian UI Adapter\n \n ## Status\n \ndiff --git a/docs/adr/0015-contract-approval-before-dispatch.md b/docs/adr/SADR-0015-contract-approval-before-dispatch.md\nindex fad6c656741a20a7684ba16080a7db159f63a52f..7bdd760bb3e82eee4e1410f45c783b03b638d456\n--- a/docs/adr/0015-contract-approval-before-dispatch.md\n+++ b/docs/adr/SADR-0015-contract-approval-before-dispatch.md\n@@ -1,4 +1,4 @@\n-# ADR 0015: Approve The Task Contract Before Dispatch, Not The Diff After It\n+# SADR-0015: Approve The Task Contract Before Dispatch, Not The Diff After It\n \n ## Status\n \ndiff --git a/docs/adr/0016-locks-bound-product-paths.md b/docs/adr/SADR-0016-locks-bound-product-paths.md\nindex b5e850f3f1f7f4ef6e7f9d1763a29a51bd300c45..332cc17313498404f41a145291e38447b5de0dc4\n--- a/docs/adr/0016-locks-bound-product-paths.md\n+++ b/docs/adr/SADR-0016-locks-bound-product-paths.md\n@@ -1,4 +1,4 @@\n-# ADR 0016: Locks bound product paths; the Shiki mirror is bound by record identity\n+# SADR-0016: Locks bound product paths; the Shiki mirror is bound by record identity\n \n ## Status\n \ndiff --git a/docs/adr/0017-bookkeeping-closeouts-do-not-inherit-risk.md b/docs/adr/SADR-0017-bookkeeping-closeouts-do-not-inherit-risk.md\nindex 554e3a0d526a72ca067108c5464cc68c3edcf174..83115f69f1b1b48d544c9b0157f377b91cb53abf\n--- a/docs/adr/0017-bookkeeping-closeouts-do-not-inherit-risk.md\n+++ b/docs/adr/SADR-0017-bookkeeping-closeouts-do-not-inherit-risk.md\n@@ -1,4 +1,4 @@\n-# ADR 0017: A Bookkeeping Closeout Does Not Inherit Its Task's Risk\n+# SADR-0017: A Bookkeeping Closeout Does Not Inherit Its Task's Risk\n \n ## Status\n \ndiff --git a/docs/adr/0018-carried-guardian-approval-across-a-proven-base-sync.md b/docs/adr/SADR-0018-carried-guardian-approval-across-a-proven-base-sync.md\nindex 22358562da390ed8b45cd600da08a43d378b4680..38da0fd95102c3496c3219ce2319fce696a2532a\n--- a/docs/adr/0018-carried-guardian-approval-across-a-proven-base-sync.md\n+++ b/docs/adr/SADR-0018-carried-guardian-approval-across-a-proven-base-sync.md\n@@ -1,4 +1,4 @@\n-# ADR 0018: Carried Guardian Approval Across A Proven Base Sync\n+# SADR-0018: Carried Guardian Approval Across A Proven Base Sync\n \n ## Status\n \ndiff --git a/docs/adr/README.md b/docs/adr/README.md\nindex c70e057a4520c36e042ab68c53cd4a21ef709b71..5da43856836e2ce6a150f8127766e2f88355ab4b\n--- a/docs/adr/README.md\n+++ b/docs/adr/README.md\n@@ -1,40 +1,41 @@\n-# Architecture Decision Records\n+# Decision Record Namespaces\n \n-This directory holds Shiki's Architecture Decision Records (ADRs): durable\n-records of hard-to-reverse platform decisions. ADRs are part of the Shiki source\n-of truth alongside GitHub state, the `.shiki/` mirror, and `CONTEXT.md`.\n+This directory holds two independent decision namespaces:\n \n-## How ADRs Work\n+- Shiki Architecture Decision Records (SADRs), named\n+  `SADR-NNNN-<short-slug>.md`, record hard-to-reverse Shiki platform decisions.\n+- Target Architecture Decision Records (ADRs), named\n+  `NNNN-<short-slug>.md`, belong to the target product.\n \n-- New ADRs are added as `docs/adr/NNNN-<short-slug>.md`, where `NNNN` is the next\n-  zero-padded sequence number.\n-- The **canonical list of ADRs is the set of files matching `docs/adr/*.md`** in\n-  this directory. Browse `docs/adr/*.md` for the authoritative, current set.\n-- Use [`template.md`](template.md) as the starting point for a new ADR.\n-- Record an ADR when a decision is hard to reverse, surprising without context,\n-  and a real tradeoff. Do not write an ADR for routine or easily reversible\n-  choices.\n+The same four-digit number may appear once in each namespace. A platform\n+`SADR-NNNN` citation resolves only to `SADR-NNNN-*.md`; a target `ADR NNNN`\n+citation resolves only to `NNNN-*.md`.\n \n-Using the `docs/adr/*.md` glob as the source of truth means other PRs can add an\n-ADR without editing this index file, avoiding merge contention on a\n-hand-maintained enumeration.\n+## How Decision Records Work\n \n-## Current ADRs (snapshot)\n-\n-The list below is a convenience snapshot and may lag behind the directory. When\n-in doubt, list `docs/adr/*.md`.\n+- New Shiki platform decisions use the next zero-padded SADR number.\n+- New target product decisions use the target's own ADR sequence.\n+- The canonical list is the files in `docs/adr/`; no hand-maintained index is\n+  authoritative.\n+- Use [`template.md`](template.md) as the starting point and choose the filename\n+  namespace that owns the decision.\n+- Record a decision when it is hard to reverse, surprising without context, and\n+  a real tradeoff. Do not write a decision record for routine or easily\n+  reversible choices.\n \n-- [0001 \u2014 Keep Shiki As The Platform Name](0001-keep-shiki-as-platform-name.md)\n-- [0002 \u2014 Use AGENTS As The Shared Agent Constitution](0002-use-agents-as-shared-constitution.md)\n-- [0003 \u2014 Require Skill Gate For MergeGate Readiness](0003-require-skill-gate-for-mergegate-readiness.md)\n-- [0004 \u2014 Use GitHub First With A Repository-Local Shiki Mirror](0004-use-github-first-with-shiki-mirror.md)\n-- [0005 \u2014 Use Codex As Front And Claude Code Action As The GitHub Runtime](0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md)\n-- [0006 \u2014 Separate Codex Implementation, CCA Completion, And MergeGate Authorization](0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md)\n+Using the directory as the source of truth lets independent changes add records\n+without contending on an enumerated index.\n \n-## Adding A New ADR\n+## Adding A Shiki SADR\n \n-1. Copy `template.md` to `docs/adr/NNNN-<short-slug>.md`.\n+1. Copy `template.md` to `docs/adr/SADR-NNNN-<short-slug>.md`.\n 2. Fill in the title, status, context, decision, and consequences.\n-3. Set the status to `Proposed`, then `Accepted` once the decision is approved.\n-4. Open a PR following `CONTRIBUTING.md`. You do not need to edit this index for\n-   the ADR to be discoverable via `docs/adr/*.md`.\n+3. Set the status to `Proposed`, then `Accepted` once the proper authority has\n+   approved the decision.\n+4. Open a PR following `CONTRIBUTING.md`.\n+\n+## Adding A Target ADR\n+\n+1. Copy `template.md` to `docs/adr/NNNN-<short-slug>.md` using the target's own\n+   sequence.\n+2. Cite it as `ADR NNNN`; do not prefix its filename with `SADR-`.\ndiff --git a/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md b/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\nnew file mode 100644\nindex 0000000000000000000000000000000000000000..7455ce1f3bed02ddddb851ac21a1d1a648c9aeba\n--- /dev/null\n+++ b/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\n@@ -0,0 +1,55 @@\n+# SADR-0019: Separate Shiki Platform SADRs From Target ADRs\n+\n+## Status\n+\n+Accepted\n+\n+## Context\n+\n+Shiki installs its platform decision records into `docs/adr/` in every target\n+repository. Shiki and its targets previously used the same numeric\n+`NNNN-<slug>.md` namespace. A target that already owned ADRs 0013 through 0018\n+therefore acquired two records for each number during installation, making an\n+`ADR NNNN` citation ambiguous. Renumbering target decisions would rewrite target\n+history, while renumbering Shiki decisions per target would destroy their stable\n+platform identity.\n+\n+Upgrades also need to remove the former Shiki-owned numeric paths without ever\n+mistaking a target-owned ADR for platform content. The install stamp already\n+records exact shipped paths and content digests, so it can prove ownership only\n+when both still match.\n+\n+## Decision\n+\n+Shiki platform decisions use the `SADR` namespace:\n+\n+- records are named `docs/adr/SADR-NNNN-<slug>.md`;\n+- live platform citations use `SADR-NNNN`;\n+- target product decisions retain `docs/adr/NNNN-<slug>.md` and `ADR NNNN`;\n+- the reference validator resolves the two namespaces independently, so the same\n+  four-digit number may exist once in each namespace.\n+\n+Before a forced upgrade writes anything, the installer inventories the exact\n+former numeric Shiki ADR paths. It may delete a legacy path only when the\n+existing install stamp contains that exact path and its recorded SHA-256 digest\n+matches the file currently on disk. All ownership blockers are reported\n+together and abort the upgrade before its first write. A non-forced install\n+never performs this cleanup.\n+\n+Historical `.shiki/` evidence remains unchanged because it records the namespace\n+that existed when that evidence was produced.\n+\n+## Consequences\n+\n+- Targets may use any ADR number without colliding with Shiki's platform SADRs.\n+- Fresh installs preserve target ADR bytes and install a stable SADR set.\n+- Forced upgrades fail closed when legacy ownership cannot be proven, and a\n+  successful upgrade refreshes the stamp without removed legacy paths.\n+- Current governance, packaging, workflow, prompt, script, and skill references\n+  must move with the platform records.\n+\n+## Alternatives Considered\n+\n+- Collision-only refusal leaves affected targets permanently unupgradable.\n+- Renumbering target ADRs mutates target-owned history.\n+- Per-target Shiki renumbering makes platform citations non-portable.\ndiff --git a/docs/adr/template.md b/docs/adr/template.md\nindex a92a97daddd0b00ab9dd626cc8d86942da2eafed..17dd79175b5fee2105935557f691b40cf9492942\n--- a/docs/adr/template.md\n+++ b/docs/adr/template.md\n@@ -1,14 +1,14 @@\n-# NNNN: <Short Decision Title>\n+# <SADR-NNNN or NNNN>: <Short Decision Title>\n \n ## Status\n \n-Proposed | Accepted | Superseded by ADR-NNNN\n+Proposed | Accepted | Superseded by SADR-NNNN or target ADR NNNN\n \n ## Context\n \n What is the situation, constraint, or force that requires a decision? Reference\n-relevant domain terms (`CONTEXT.md`), prior ADRs, and code reality. State the\n-problem, not the solution.\n+relevant domain terms (`CONTEXT.md`), prior Shiki SADRs or target ADRs, and code\n+reality. State the problem, not the solution.\n \n ## Decision\n \ndiff --git a/docs/agents/domain.md b/docs/agents/domain.md\nindex 258da8ea81e87ac0a6b0b5b5e5c2fde980eaabb7..67aaa6f4b7d0a5aa4d495ce6279073333f7798cb\n--- a/docs/agents/domain.md\n+++ b/docs/agents/domain.md\n@@ -5,7 +5,9 @@\n ## Layout\n \n - `CONTEXT.md` defines Shiki language. Keep it implementation-free.\n-- `docs/adr/` records hard-to-reverse decisions.\n+- `docs/adr/SADR-NNNN-*.md` records Shiki platform decisions.\n+- `docs/adr/NNNN-*.md` records target product decisions without sharing the\n+  SADR namespace.\n - `AGENTS.md` is the runtime-neutral agent constitution.\n - `CLAUDE.md` is the Claude Code wrapper around `AGENTS.md`.\n - `.shiki/` is the Target Repository mirror for Goals, PRDs, plans, Task DAGs, contracts, locks, worktree records, ledger entries, CCA verdicts, repair packets, reports, and handoffs.\ndiff --git a/docs/agents/implementation-policy.md b/docs/agents/implementation-policy.md\nindex c754144a9fb5bf4a38aa82a60e1670c59e626b44..3b059d128ce1338ee49eadec0dbd58f3cc2cc594\n--- a/docs/agents/implementation-policy.md\n+++ b/docs/agents/implementation-policy.md\n@@ -27,7 +27,7 @@\n 1. GitHub Issues, Pull Requests, Checks, Reviews, comments, and merge evidence are the operational source of truth.\n 2. The repository-local `.shiki/` mirror records Goals, PRDs, plans, Task DAGs, contracts, locks, ledger entries, CCA verdicts, repair packets, reports, and handoffs.\n 3. `CONTEXT.md` defines Shiki domain language and glossary decisions.\n-4. `docs/adr/` records hard-to-reverse platform decisions.\n+4. `docs/adr/SADR-NNNN-*.md` records Shiki platform decisions; `docs/adr/NNNN-*.md` is reserved for target ADRs.\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \ndiff --git a/scripts/shiki_contracts.py b/scripts/shiki_contracts.py\nindex 18076f0a70a1f8c9d4475a005bcccb1472a246c1..9d3f08dc1172b100c3a9b60a21ffdbae0f4dd44b\n--- a/scripts/shiki_contracts.py\n+++ b/scripts/shiki_contracts.py\n@@ -100,7 +100,7 @@\n     \"GitHub Issues, Pull Requests, Checks, Reviews, comments, and merge evidence are the operational source of truth.\",\n     \"The repository-local `.shiki/` mirror records Goals, PRDs, plans, Task DAGs, contracts, locks, ledger entries, CCA verdicts, repair packets, reports, and handoffs.\",\n     \"`CONTEXT.md` defines Shiki domain language and glossary decisions.\",\n-    \"`docs/adr/` records hard-to-reverse platform decisions.\",\n+    \"`docs/adr/SADR-NNNN-*.md` records Shiki platform decisions; `docs/adr/NNNN-*.md` is reserved for target ADRs.\",\n     \"Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\",\n )\n \ndiff --git a/tests/test_adr_references.py b/tests/test_adr_references.py\nindex 51706329c797d5dd9b47e082a333fa83b261d482..92569d9fa25e22cfd71c17561d715485027a0d2d\n--- a/tests/test_adr_references.py\n+++ b/tests/test_adr_references.py\n@@ -31,17 +31,18 @@\n \n REPO_ROOT = Path(__file__).resolve().parents[1]\n \n-# \"ADR 0016\" -- a four-digit citation. The trailing \\b keeps a five-digit token\n-# from being read as a truncated four-digit number.\n-_CITATION = re.compile(r\"\\bADR (\\d{4})\\b\")\n-# \"0016-locks-bound-product-paths.md\" -- the leading four digits name the record.\n-_ADR_FILENAME = re.compile(r\"^(\\d{4})-.*\\.md$\")\n+# ``ADR 0016`` / ``SADR-0016`` -- a namespace plus four-digit citation. The\n+# trailing ``\\b`` keeps a five-digit token from being read as a truncated one.\n+_CITATION = re.compile(r\"\\b(S?ADR)[ -](\\d{4})\\b\")\n+# Target ADRs retain the numeric filename; Shiki platform decisions use SADR.\n+_DECISION_FILENAME = re.compile(r\"^(?:(SADR)-)?(\\d{4})-.*\\.md$\")\n \n \n class Violation(NamedTuple):\n     \"\"\"A citation that does not resolve to exactly one decision record.\"\"\"\n \n     citing_file: str  # repo-relative path of the file that cites the ADR\n+    namespace: str  # \"ADR\" for target decisions; \"SADR\" for Shiki decisions\n     number: str  # the cited four-digit number, e.g. \"0016\"\n     line: int  # 1-based line of the citation\n     reason: str  # \"missing\" (zero matches) or \"ambiguous\" (two or more)\n@@ -49,32 +50,34 @@\n \n     @property\n     def expected_path(self) -> str:\n-        return f\"docs/adr/{self.number}-*.md\"\n+        prefix = \"SADR-\" if self.namespace == \"SADR\" else \"\"\n+        return f\"docs/adr/{prefix}{self.number}-*.md\"\n \n     def describe(self) -> str:\n         \"\"\"Name the citing file, the cited number, and the missing path.\"\"\"\n         if self.reason == \"missing\":\n             return (\n-                f\"{self.citing_file}:{self.line} cites ADR {self.number} \"\n+                f\"{self.citing_file}:{self.line} cites {self.namespace} {self.number} \"\n                 f\"but no document matches {self.expected_path}\"\n             )\n         joined = \", \".join(self.matches)\n         return (\n-            f\"{self.citing_file}:{self.line} cites ADR {self.number} which \"\n+            f\"{self.citing_file}:{self.line} cites {self.namespace} {self.number} which \"\n             f\"resolves to {len(self.matches)} documents (expected exactly one \"\n             f\"at {self.expected_path}): {joined}\"\n         )\n \n \n-def _adr_index(root: Path) -> dict[str, list[Path]]:\n-    \"\"\"Map every four-digit ADR number to the record files that claim it.\"\"\"\n-    index: dict[str, list[Path]] = {}\n+def _adr_index(root: Path) -> dict[tuple[str, str], list[Path]]:\n+    \"\"\"Map each decision namespace and number to the files that claim it.\"\"\"\n+    index: dict[tuple[str, str], list[Path]] = {}\n     adr_dir = root / \"docs\" / \"adr\"\n     if adr_dir.is_dir():\n         for path in sorted(adr_dir.glob(\"*.md\")):\n-            match = _ADR_FILENAME.match(path.name)\n+            match = _DECISION_FILENAME.match(path.name)\n             if match:\n-                index.setdefault(match.group(1), []).append(path)\n+                namespace = \"SADR\" if match.group(1) else \"ADR\"\n+                index.setdefault((namespace, match.group(2)), []).append(path)\n     return index\n \n \n@@ -110,13 +113,15 @@\n         rel = path.relative_to(root).as_posix()\n         for lineno, line in enumerate(text.splitlines(), start=1):\n             for match in _CITATION.finditer(line):\n-                number = match.group(1)\n-                found = index.get(number, [])\n+                namespace, number = match.groups()\n+                found = index.get((namespace, number), [])\n                 if len(found) == 1:\n                     continue\n                 reason = \"missing\" if not found else \"ambiguous\"\n                 rels = [p.relative_to(root).as_posix() for p in found]\n-                violations.append(Violation(rel, number, lineno, reason, rels))\n+                violations.append(\n+                    Violation(rel, namespace, number, lineno, reason, rels)\n+                )\n     return violations\n \n \n@@ -146,8 +151,8 @@\n         # The two records this Goal carried onto the default branch, and which\n         # the scripts cite most, must be present.\n         index = _adr_index(REPO_ROOT)\n-        self.assertIn(\"0016\", index)\n-        self.assertIn(\"0017\", index)\n+        self.assertIn((\"ADR\", \"0016\"), index)\n+        self.assertIn((\"ADR\", \"0017\"), index)\n \n \n class FixtureAdrReferences(unittest.TestCase):\n@@ -180,6 +185,17 @@\n         self.assertIn(\"9991\", message)  # cited number\n         self.assertIn(\"docs/adr/9991-*.md\", message)  # missing path\n \n+    def test_target_adr_does_not_satisfy_missing_platform_sadr(self) -> None:\n+        root = self._make_repo(\n+            adr_names=[\"0042-target-decision.md\"],\n+            script_body='\"\"\"Target ADR 0042 exists, but platform SADR-0042 does not.\"\"\"\\n',\n+        )\n+        violations = find_adr_reference_violations(root, include_docs=False)\n+        self.assertEqual(len(violations), 1)\n+        self.assertEqual(violations[0].namespace, \"SADR\")\n+        self.assertEqual(violations[0].number, \"0042\")\n+        self.assertEqual(violations[0].expected_path, \"docs/adr/SADR-0042-*.md\")\n+\n     def test_uncited_adr_is_allowed(self) -> None:\n         # An ADR exists but nothing cites it -> not a violation. The converse\n         # (every ADR must be cited) is explicitly NOT required.\ndiff --git a/tests/test_sadr_install_migration.py b/tests/test_sadr_install_migration.py\nnew file mode 100644\nindex 0000000000000000000000000000000000000000..a6398c81377f00c09aa2cb684980b070cfaf714c\n--- /dev/null\n+++ b/tests/test_sadr_install_migration.py\n@@ -0,0 +1,56 @@\n+\"\"\"SADR installation and collision-safe legacy upgrade behavior.\"\"\"\n+\n+from __future__ import annotations\n+\n+import shutil\n+import tempfile\n+import unittest\n+from pathlib import Path\n+\n+import shiki_test_support  # noqa: F401  (path bootstrap)\n+\n+import shiki_installer\n+from tests.test_adr_references import find_adr_reference_violations\n+\n+\n+PRODUCT_NUMBERS = tuple(range(13, 19))\n+\n+\n+class FreshSadrInstallTests(unittest.TestCase):\n+    def setUp(self) -> None:\n+        self.tmp = Path(tempfile.mkdtemp(prefix=\"shiki-sadr-fresh-\"))\n+        self.addCleanup(lambda: shutil.rmtree(self.tmp, ignore_errors=True))\n+        self.target = self.tmp / \"target\"\n+        self.adr_dir = self.target / \"docs\" / \"adr\"\n+        self.adr_dir.mkdir(parents=True)\n+        self.product_bytes = {\n+            f\"{number:04d}-product-decision.md\": (\n+                f\"# Product ADR {number:04d}\\n\\nTarget-owned bytes {number}.\\n\"\n+            ).encode()\n+            for number in PRODUCT_NUMBERS\n+        }\n+        for name, content in self.product_bytes.items():\n+            (self.adr_dir / name).write_bytes(content)\n+\n+    def test_fresh_install_preserves_product_adrs_and_installs_all_sadrs(self) -> None:\n+        shiki_installer.install_template(self.target, force=False, validate=True)\n+\n+        for name, expected in self.product_bytes.items():\n+            self.assertEqual((self.adr_dir / name).read_bytes(), expected)\n+\n+        sadr_numbers = {\n+            path.name.split(\"-\", 2)[1]\n+            for path in self.adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\")\n+        }\n+        self.assertEqual(sadr_numbers, {f\"{number:04d}\" for number in range(1, 20)})\n+\n+        numeric_records = {\n+            path.name\n+            for path in self.adr_dir.glob(\"[0-9][0-9][0-9][0-9]-*.md\")\n+        }\n+        self.assertEqual(numeric_records, set(self.product_bytes))\n+        self.assertEqual(find_adr_reference_violations(self.target), [])\n+\n+\n+if __name__ == \"__main__\":\n+    unittest.main()\n\napply patch\npatch: completed\n/private/tmp/shiki-adr-collision.pa0KwY/.worktrees/shiki-t-20260807t073307221254z-2d22d654-introduc/tests/test_adr_references.py\ndiff --git a/.codex/skills/shiki/SKILL.md b/.codex/skills/shiki/SKILL.md\nindex c8e2e85d9d75d49cc4f2d6286ee22659c4e53e75..c392c28442311b8851ede2cf66af14011d8038d4\n--- a/.codex/skills/shiki/SKILL.md\n+++ b/.codex/skills/shiki/SKILL.md\n@@ -14,7 +14,7 @@\n 1. GitHub Issues, Pull Requests, Checks, Reviews, comments, and merge evidence are the operational source of truth.\n 2. The repository-local `.shiki/` mirror records Goals, PRDs, plans, Task DAGs, contracts, locks, ledger entries, CCA verdicts, repair packets, reports, and handoffs.\n 3. `CONTEXT.md` defines Shiki domain language and glossary decisions.\n-4. `docs/adr/` records hard-to-reverse platform decisions.\n+4. `docs/adr/SADR-NNNN-*.md` records Shiki platform decisions; `docs/adr/NNNN-*.md` is reserved for target ADRs.\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n@@ -69,7 +69,7 @@\n \n ## Responsibilities\n \n-- Claude Code implements and repairs by default (`shiki runner claude`, ADR 0008); Codex implements and repairs tasks explicitly assigned to `codex`.\n+- Claude Code implements and repairs by default (`shiki runner claude`, SADR-0008); Codex implements and repairs tasks explicitly assigned to `codex`.\n - Claude Code Action can act as GitHub-side CCA or reviewer.\n - CCA judges completion.\n - MergeGate authorizes state transitions and merge readiness.\ndiff --git a/.github/prompts/cca-completion-check.md b/.github/prompts/cca-completion-check.md\nindex ed203ad5f302ecc81372d8ab27d2b1232ca97497..3137954883be3c4657b40ea1a1253b0dc4b4ef69\n--- a/.github/prompts/cca-completion-check.md\n+++ b/.github/prompts/cca-completion-check.md\n@@ -15,7 +15,7 @@\n 1. GitHub Issues, Pull Requests, Checks, Reviews, comments, and merge evidence are the operational source of truth.\n 2. The repository-local `.shiki/` mirror records Goals, PRDs, plans, Task DAGs, contracts, locks, ledger entries, CCA verdicts, repair packets, reports, and handoffs.\n 3. `CONTEXT.md` defines Shiki domain language and glossary decisions.\n-4. `docs/adr/` records hard-to-reverse platform decisions.\n+4. `docs/adr/SADR-NNNN-*.md` records Shiki platform decisions; `docs/adr/NNNN-*.md` is reserved for target ADRs.\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \ndiff --git a/AGENTS.md b/AGENTS.md\nindex 7f041100d49c2a7525f51cb0f5d696a4a6d1c78c..ddd7fe4d489f2c4c3ce82b51c84934019a7616b4\n--- a/AGENTS.md\n+++ b/AGENTS.md\n@@ -40,7 +40,7 @@\n 1. GitHub Issues, Pull Requests, Checks, Reviews, comments, and merge evidence are the operational source of truth.\n 2. The repository-local `.shiki/` mirror records Goals, PRDs, plans, Task DAGs, contracts, locks, ledger entries, CCA verdicts, repair packets, reports, and handoffs.\n 3. `CONTEXT.md` defines Shiki domain language and glossary decisions.\n-4. `docs/adr/` records hard-to-reverse platform decisions.\n+4. `docs/adr/SADR-NNNN-*.md` records Shiki platform decisions; `docs/adr/NNNN-*.md` is reserved for target ADRs.\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \ndiff --git a/CLAUDE.md b/CLAUDE.md\nindex 98a932e7841a632866fcd53b0bad0e3af3711d3d..83e001d4c15ed0a421666dc68da548ecb56748df\n--- a/CLAUDE.md\n+++ b/CLAUDE.md\n@@ -10,7 +10,7 @@\n 1. GitHub Issues, Pull Requests, Checks, Reviews, comments, and merge evidence are the operational source of truth.\n 2. The repository-local `.shiki/` mirror records Goals, PRDs, plans, Task DAGs, contracts, locks, ledger entries, CCA verdicts, repair packets, reports, and handoffs.\n 3. `CONTEXT.md` defines Shiki domain language and glossary decisions.\n-4. `docs/adr/` records hard-to-reverse platform decisions.\n+4. `docs/adr/SADR-NNNN-*.md` records Shiki platform decisions; `docs/adr/NNNN-*.md` is reserved for target ADRs.\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \ndiff --git a/SYSTEM_PROMPT.md b/SYSTEM_PROMPT.md\nindex aaaea45e3a896c3a44ba89eec8bb84325ecf6c62..42087a9325dfaa48de71b8319017175689166d2e\n--- a/SYSTEM_PROMPT.md\n+++ b/SYSTEM_PROMPT.md\n@@ -26,7 +26,7 @@\n 1. GitHub Issues, Pull Requests, Checks, Reviews, comments, and merge evidence are the operational source of truth.\n 2. The repository-local `.shiki/` mirror records Goals, PRDs, plans, Task DAGs, contracts, locks, ledger entries, CCA verdicts, repair packets, reports, and handoffs.\n 3. `CONTEXT.md` defines Shiki domain language and glossary decisions.\n-4. `docs/adr/` records hard-to-reverse platform decisions.\n+4. `docs/adr/SADR-NNNN-*.md` records Shiki platform decisions; `docs/adr/NNNN-*.md` is reserved for target ADRs.\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \ndiff --git a/docs/adr/0001-keep-shiki-as-platform-name.md b/docs/adr/SADR-0001-keep-shiki-as-platform-name.md\nindex 20a9637c3b9d8c97de7ba8db7cfae4d82d9a7dba..8b685439959aada1b221c112f0ac3c51ca8a1695\n--- a/docs/adr/0001-keep-shiki-as-platform-name.md\n+++ b/docs/adr/SADR-0001-keep-shiki-as-platform-name.md\n@@ -1,3 +1,3 @@\n-# Keep Shiki As The Platform Name\n+# SADR-0001: Keep Shiki As The Platform Name\n \n Shiki remains the product name for the GitHub-native agentic engineering control plane. We considered more descriptive names such as MergePilot, MergeGate, and Kiroku, but kept Shiki because it carries the existing goal-seeking framework identity while remaining independent of Hermes, Codex, Claude Code, or any single runtime.\ndiff --git a/docs/adr/0002-use-agents-as-shared-constitution.md b/docs/adr/SADR-0002-use-agents-as-shared-constitution.md\nindex 73de30ecb078120eac540d9bc51855e3cd48d0e3..c5486182bf06fca36ea508496b013c67302fd989\n--- a/docs/adr/0002-use-agents-as-shared-constitution.md\n+++ b/docs/adr/SADR-0002-use-agents-as-shared-constitution.md\n@@ -1,3 +1,3 @@\n-# Use AGENTS As The Shared Agent Constitution\n+# SADR-0002: Use AGENTS As The Shared Agent Constitution\n \n Shiki uses `AGENTS.md` as the runtime-neutral constitution and `CLAUDE.md` as a thin Claude Code wrapper. This keeps Codex, Claude Code, Hermes Runner, and future Agent Runtimes aligned on the same GitHub-first MergeGate rules while still allowing runtime-specific instructions where needed.\ndiff --git a/docs/adr/0003-require-skill-gate-for-mergegate-readiness.md b/docs/adr/SADR-0003-require-skill-gate-for-mergegate-readiness.md\nindex 424be0c9af4c8f0136f1fde68a468c47ff84e764..7fc337987c7df9792f75f181dfcbed75fe5ffff2\n--- a/docs/adr/0003-require-skill-gate-for-mergegate-readiness.md\n+++ b/docs/adr/SADR-0003-require-skill-gate-for-mergegate-readiness.md\n@@ -1,3 +1,3 @@\n-# Require Skill Gate For MergeGate Readiness\n+# SADR-0003: Require Skill Gate For MergeGate Readiness\n \n Shiki requires relevant engineering skills to be invoked before execution when their triggers apply. This makes skills part of MergeGate readiness rather than optional prompt style, because Shiki depends on repeatable planning, issue decomposition, TDD, diagnosis, triage, and architecture review before autonomous or parallel agent work can be trusted.\ndiff --git a/docs/adr/0004-use-github-first-with-shiki-mirror.md b/docs/adr/SADR-0004-use-github-first-with-shiki-mirror.md\nindex d0a3a9174dc646a945a9d325954ffaf00c352040..a79c54ae9ee8eed2a0524192b864da4e22a92d3f\n--- a/docs/adr/0004-use-github-first-with-shiki-mirror.md\n+++ b/docs/adr/SADR-0004-use-github-first-with-shiki-mirror.md\n@@ -1,4 +1,4 @@\n-# ADR 0004: Use GitHub First With A Repository-Local Shiki Mirror\n+# SADR-0004: Use GitHub First With A Repository-Local Shiki Mirror\n \n ## Status\n \ndiff --git a/docs/adr/0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md b/docs/adr/SADR-0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\nindex b339baf49b89d9290e79c3d51b36e7b2bde3bd95..c30a0d8b1fde0224d0ac7820e7870dccf721580d\n--- a/docs/adr/0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\n+++ b/docs/adr/SADR-0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\n@@ -1,4 +1,4 @@\n-# ADR 0005: Use Codex As Front And Claude Code Action As The GitHub Runtime\n+# SADR-0005: Use Codex As Front And Claude Code Action As The GitHub Runtime\n \n ## Status\n \ndiff --git a/docs/adr/0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md b/docs/adr/SADR-0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\nindex 4bcae9d1e52f0a37b8e9fbafa5cd14b6ee1993bd..7a38538a6ae9929567f0ade67e7ea6a60d0b3f7e\n--- a/docs/adr/0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\n+++ b/docs/adr/SADR-0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\n@@ -1,4 +1,4 @@\n-# ADR 0006: Separate Codex Implementation, CCA Completion, And MergeGate Authorization\n+# SADR-0006: Separate Codex Implementation, CCA Completion, And MergeGate Authorization\n \n ## Status\n \ndiff --git a/docs/adr/0007-packaging-and-release.md b/docs/adr/SADR-0007-packaging-and-release.md\nindex 79b84ddb97167372b44641e5b7860e400ce80822..023cd57f8011aa6e6d2ebf612b7c35a7a82c00d4\n--- a/docs/adr/0007-packaging-and-release.md\n+++ b/docs/adr/SADR-0007-packaging-and-release.md\n@@ -1,4 +1,4 @@\n-# ADR 0007: Packaging And Release For Shiki\n+# SADR-0007: Packaging And Release For Shiki\n \n ## Status\n \ndiff --git a/docs/adr/0008-use-claude-code-as-default-implementer.md b/docs/adr/SADR-0008-use-claude-code-as-default-implementer.md\nindex dedf4510b7c7799521422f622029563d373db031..a158a8d444d93764ec515a28133f7dde4af23579\n--- a/docs/adr/0008-use-claude-code-as-default-implementer.md\n+++ b/docs/adr/SADR-0008-use-claude-code-as-default-implementer.md\n@@ -1,4 +1,4 @@\n-# ADR 0008: Use Claude Code As The Default Implementer Runtime\n+# SADR-0008: Use Claude Code As The Default Implementer Runtime\n \n ## Status\n \ndiff --git a/docs/adr/0009-requirements-definition-and-spec-freeze.md b/docs/adr/SADR-0009-requirements-definition-and-spec-freeze.md\nindex 29b3f7871bef9203a8b4a831a65c39eb2f506e42..52e7dd69ad0aa4efe0b68c62678abbf7df02aaec\n--- a/docs/adr/0009-requirements-definition-and-spec-freeze.md\n+++ b/docs/adr/SADR-0009-requirements-definition-and-spec-freeze.md\n@@ -1,4 +1,4 @@\n-# ADR 0009: Merge Grill And Planning Into Requirements Definition With Spec Freeze\n+# SADR-0009: Merge Grill And Planning Into Requirements Definition With Spec Freeze\n \n ## Status\n \ndiff --git a/docs/adr/0010-external-ai-guardian-review.md b/docs/adr/SADR-0010-external-ai-guardian-review.md\nindex e6108bbe8bb4e73e0ff4684c8f78001a80438c4e..e690d4eddd270e683d709c9b679c5859e71eeaaa\n--- a/docs/adr/0010-external-ai-guardian-review.md\n+++ b/docs/adr/SADR-0010-external-ai-guardian-review.md\n@@ -1,4 +1,4 @@\n-# ADR 0010: External AI Guardian Review As A First-Class Approval Authority\n+# SADR-0010: External AI Guardian Review As A First-Class Approval Authority\n \n ## Status\n \ndiff --git a/docs/adr/0011-autonomous-quality-gate-evidence.md b/docs/adr/SADR-0011-autonomous-quality-gate-evidence.md\nindex bca0246c5d46e44afbde15486a75eec425fc02f5..9c93eac16d8c3a6d4b11bd7224e3955102cd7db4\n--- a/docs/adr/0011-autonomous-quality-gate-evidence.md\n+++ b/docs/adr/SADR-0011-autonomous-quality-gate-evidence.md\n@@ -1,4 +1,4 @@\n-# ADR 0011: Autonomous Quality-Gate Evidence \u2014 Deterministic Facts And An Independent Verifier\n+# SADR-0011: Autonomous Quality-Gate Evidence \u2014 Deterministic Facts And An Independent Verifier\n \n ## Status\n \ndiff --git a/docs/adr/0012-autonomous-loop-closeout-to-main.md b/docs/adr/SADR-0012-autonomous-loop-closeout-to-main.md\nindex 637fad6dde4989c9c2bf0f81f676d8d29991123c..ec7d85f81030a676bcbba28b43eb365422a36eac\n--- a/docs/adr/0012-autonomous-loop-closeout-to-main.md\n+++ b/docs/adr/SADR-0012-autonomous-loop-closeout-to-main.md\n@@ -1,4 +1,4 @@\n-# 0012 \u2014 Autonomous loop pushes goal completion to main via a closeout PR\n+# SADR-0012 \u2014 Autonomous loop pushes goal completion to main via a closeout PR\n \n - Status: accepted\n - Date: 2026-06-18\ndiff --git a/docs/adr/0013-bootstrap-grants-actions-approve-pull-requests.md b/docs/adr/SADR-0013-bootstrap-grants-actions-approve-pull-requests.md\nindex b6c69d64f1c5ced346b88d6859586715b3b5bd0e..a793e7277d67ab8ea56286fec289ca689825a17d\n--- a/docs/adr/0013-bootstrap-grants-actions-approve-pull-requests.md\n+++ b/docs/adr/SADR-0013-bootstrap-grants-actions-approve-pull-requests.md\n@@ -1,4 +1,4 @@\n-# ADR 0013: Bootstrap Grants GitHub Actions Permission To Create And Approve Pull Requests\n+# SADR-0013: Bootstrap Grants GitHub Actions Permission To Create And Approve Pull Requests\n \n ## Status\n \ndiff --git a/docs/adr/0014-codex-app-external-ai-guardian-ui-adapter.md b/docs/adr/SADR-0014-codex-app-external-ai-guardian-ui-adapter.md\nindex 238db06279334caa341c117362df9271382a7fcd..65f12e9939321e1b3e389a34c822fc1549e5155d\n--- a/docs/adr/0014-codex-app-external-ai-guardian-ui-adapter.md\n+++ b/docs/adr/SADR-0014-codex-app-external-ai-guardian-ui-adapter.md\n@@ -1,4 +1,4 @@\n-# ADR 0014: Use Codex App As The External AI Guardian UI Adapter\n+# SADR-0014: Use Codex App As The External AI Guardian UI Adapter\n \n ## Status\n \ndiff --git a/docs/adr/0015-contract-approval-before-dispatch.md b/docs/adr/SADR-0015-contract-approval-before-dispatch.md\nindex fad6c656741a20a7684ba16080a7db159f63a52f..7bdd760bb3e82eee4e1410f45c783b03b638d456\n--- a/docs/adr/0015-contract-approval-before-dispatch.md\n+++ b/docs/adr/SADR-0015-contract-approval-before-dispatch.md\n@@ -1,4 +1,4 @@\n-# ADR 0015: Approve The Task Contract Before Dispatch, Not The Diff After It\n+# SADR-0015: Approve The Task Contract Before Dispatch, Not The Diff After It\n \n ## Status\n \ndiff --git a/docs/adr/0016-locks-bound-product-paths.md b/docs/adr/SADR-0016-locks-bound-product-paths.md\nindex b5e850f3f1f7f4ef6e7f9d1763a29a51bd300c45..332cc17313498404f41a145291e38447b5de0dc4\n--- a/docs/adr/0016-locks-bound-product-paths.md\n+++ b/docs/adr/SADR-0016-locks-bound-product-paths.md\n@@ -1,4 +1,4 @@\n-# ADR 0016: Locks bound product paths; the Shiki mirror is bound by record identity\n+# SADR-0016: Locks bound product paths; the Shiki mirror is bound by record identity\n \n ## Status\n \ndiff --git a/docs/adr/0017-bookkeeping-closeouts-do-not-inherit-risk.md b/docs/adr/SADR-0017-bookkeeping-closeouts-do-not-inherit-risk.md\nindex 554e3a0d526a72ca067108c5464cc68c3edcf174..83115f69f1b1b48d544c9b0157f377b91cb53abf\n--- a/docs/adr/0017-bookkeeping-closeouts-do-not-inherit-risk.md\n+++ b/docs/adr/SADR-0017-bookkeeping-closeouts-do-not-inherit-risk.md\n@@ -1,4 +1,4 @@\n-# ADR 0017: A Bookkeeping Closeout Does Not Inherit Its Task's Risk\n+# SADR-0017: A Bookkeeping Closeout Does Not Inherit Its Task's Risk\n \n ## Status\n \ndiff --git a/docs/adr/0018-carried-guardian-approval-across-a-proven-base-sync.md b/docs/adr/SADR-0018-carried-guardian-approval-across-a-proven-base-sync.md\nindex 22358562da390ed8b45cd600da08a43d378b4680..38da0fd95102c3496c3219ce2319fce696a2532a\n--- a/docs/adr/0018-carried-guardian-approval-across-a-proven-base-sync.md\n+++ b/docs/adr/SADR-0018-carried-guardian-approval-across-a-proven-base-sync.md\n@@ -1,4 +1,4 @@\n-# ADR 0018: Carried Guardian Approval Across A Proven Base Sync\n+# SADR-0018: Carried Guardian Approval Across A Proven Base Sync\n \n ## Status\n \ndiff --git a/docs/adr/README.md b/docs/adr/README.md\nindex c70e057a4520c36e042ab68c53cd4a21ef709b71..5da43856836e2ce6a150f8127766e2f88355ab4b\n--- a/docs/adr/README.md\n+++ b/docs/adr/README.md\n@@ -1,40 +1,41 @@\n-# Architecture Decision Records\n+# Decision Record Namespaces\n \n-This directory holds Shiki's Architecture Decision Records (ADRs): durable\n-records of hard-to-reverse platform decisions. ADRs are part of the Shiki source\n-of truth alongside GitHub state, the `.shiki/` mirror, and `CONTEXT.md`.\n+This directory holds two independent decision namespaces:\n \n-## How ADRs Work\n+- Shiki Architecture Decision Records (SADRs), named\n+  `SADR-NNNN-<short-slug>.md`, record hard-to-reverse Shiki platform decisions.\n+- Target Architecture Decision Records (ADRs), named\n+  `NNNN-<short-slug>.md`, belong to the target product.\n \n-- New ADRs are added as `docs/adr/NNNN-<short-slug>.md`, where `NNNN` is the next\n-  zero-padded sequence number.\n-- The **canonical list of ADRs is the set of files matching `docs/adr/*.md`** in\n-  this directory. Browse `docs/adr/*.md` for the authoritative, current set.\n-- Use [`template.md`](template.md) as the starting point for a new ADR.\n-- Record an ADR when a decision is hard to reverse, surprising without context,\n-  and a real tradeoff. Do not write an ADR for routine or easily reversible\n-  choices.\n+The same four-digit number may appear once in each namespace. A platform\n+`SADR-NNNN` citation resolves only to `SADR-NNNN-*.md`; a target `ADR NNNN`\n+citation resolves only to `NNNN-*.md`.\n \n-Using the `docs/adr/*.md` glob as the source of truth means other PRs can add an\n-ADR without editing this index file, avoiding merge contention on a\n-hand-maintained enumeration.\n+## How Decision Records Work\n \n-## Current ADRs (snapshot)\n-\n-The list below is a convenience snapshot and may lag behind the directory. When\n-in doubt, list `docs/adr/*.md`.\n+- New Shiki platform decisions use the next zero-padded SADR number.\n+- New target product decisions use the target's own ADR sequence.\n+- The canonical list is the files in `docs/adr/`; no hand-maintained index is\n+  authoritative.\n+- Use [`template.md`](template.md) as the starting point and choose the filename\n+  namespace that owns the decision.\n+- Record a decision when it is hard to reverse, surprising without context, and\n+  a real tradeoff. Do not write a decision record for routine or easily\n+  reversible choices.\n \n-- [0001 \u2014 Keep Shiki As The Platform Name](0001-keep-shiki-as-platform-name.md)\n-- [0002 \u2014 Use AGENTS As The Shared Agent Constitution](0002-use-agents-as-shared-constitution.md)\n-- [0003 \u2014 Require Skill Gate For MergeGate Readiness](0003-require-skill-gate-for-mergegate-readiness.md)\n-- [0004 \u2014 Use GitHub First With A Repository-Local Shiki Mirror](0004-use-github-first-with-shiki-mirror.md)\n-- [0005 \u2014 Use Codex As Front And Claude Code Action As The GitHub Runtime](0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md)\n-- [0006 \u2014 Separate Codex Implementation, CCA Completion, And MergeGate Authorization](0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md)\n+Using the directory as the source of truth lets independent changes add records\n+without contending on an enumerated index.\n \n-## Adding A New ADR\n+## Adding A Shiki SADR\n \n-1. Copy `template.md` to `docs/adr/NNNN-<short-slug>.md`.\n+1. Copy `template.md` to `docs/adr/SADR-NNNN-<short-slug>.md`.\n 2. Fill in the title, status, context, decision, and consequences.\n-3. Set the status to `Proposed`, then `Accepted` once the decision is approved.\n-4. Open a PR following `CONTRIBUTING.md`. You do not need to edit this index for\n-   the ADR to be discoverable via `docs/adr/*.md`.\n+3. Set the status to `Proposed`, then `Accepted` once the proper authority has\n+   approved the decision.\n+4. Open a PR following `CONTRIBUTING.md`.\n+\n+## Adding A Target ADR\n+\n+1. Copy `template.md` to `docs/adr/NNNN-<short-slug>.md` using the target's own\n+   sequence.\n+2. Cite it as `ADR NNNN`; do not prefix its filename with `SADR-`.\ndiff --git a/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md b/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\nnew file mode 100644\nindex 0000000000000000000000000000000000000000..7455ce1f3bed02ddddb851ac21a1d1a648c9aeba\n--- /dev/null\n+++ b/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\n@@ -0,0 +1,55 @@\n+# SADR-0019: Separate Shiki Platform SADRs From Target ADRs\n+\n+## Status\n+\n+Accepted\n+\n+## Context\n+\n+Shiki installs its platform decision records into `docs/adr/` in every target\n+repository. Shiki and its targets previously used the same numeric\n+`NNNN-<slug>.md` namespace. A target that already owned ADRs 0013 through 0018\n+therefore acquired two records for each number during installation, making an\n+`ADR NNNN` citation ambiguous. Renumbering target decisions would rewrite target\n+history, while renumbering Shiki decisions per target would destroy their stable\n+platform identity.\n+\n+Upgrades also need to remove the former Shiki-owned numeric paths without ever\n+mistaking a target-owned ADR for platform content. The install stamp already\n+records exact shipped paths and content digests, so it can prove ownership only\n+when both still match.\n+\n+## Decision\n+\n+Shiki platform decisions use the `SADR` namespace:\n+\n+- records are named `docs/adr/SADR-NNNN-<slug>.md`;\n+- live platform citations use `SADR-NNNN`;\n+- target product decisions retain `docs/adr/NNNN-<slug>.md` and `ADR NNNN`;\n+- the reference validator resolves the two namespaces independently, so the same\n+  four-digit number may exist once in each namespace.\n+\n+Before a forced upgrade writes anything, the installer inventories the exact\n+former numeric Shiki ADR paths. It may delete a legacy path only when the\n+existing install stamp contains that exact path and its recorded SHA-256 digest\n+matches the file currently on disk. All ownership blockers are reported\n+together and abort the upgrade before its first write. A non-forced install\n+never performs this cleanup.\n+\n+Historical `.shiki/` evidence remains unchanged because it records the namespace\n+that existed when that evidence was produced.\n+\n+## Consequences\n+\n+- Targets may use any ADR number without colliding with Shiki's platform SADRs.\n+- Fresh installs preserve target ADR bytes and install a stable SADR set.\n+- Forced upgrades fail closed when legacy ownership cannot be proven, and a\n+  successful upgrade refreshes the stamp without removed legacy paths.\n+- Current governance, packaging, workflow, prompt, script, and skill references\n+  must move with the platform records.\n+\n+## Alternatives Considered\n+\n+- Collision-only refusal leaves affected targets permanently unupgradable.\n+- Renumbering target ADRs mutates target-owned history.\n+- Per-target Shiki renumbering makes platform citations non-portable.\ndiff --git a/docs/adr/template.md b/docs/adr/template.md\nindex a92a97daddd0b00ab9dd626cc8d86942da2eafed..17dd79175b5fee2105935557f691b40cf9492942\n--- a/docs/adr/template.md\n+++ b/docs/adr/template.md\n@@ -1,14 +1,14 @@\n-# NNNN: <Short Decision Title>\n+# <SADR-NNNN or NNNN>: <Short Decision Title>\n \n ## Status\n \n-Proposed | Accepted | Superseded by ADR-NNNN\n+Proposed | Accepted | Superseded by SADR-NNNN or target ADR NNNN\n \n ## Context\n \n What is the situation, constraint, or force that requires a decision? Reference\n-relevant domain terms (`CONTEXT.md`), prior ADRs, and code reality. State the\n-problem, not the solution.\n+relevant domain terms (`CONTEXT.md`), prior Shiki SADRs or target ADRs, and code\n+reality. State the problem, not the solution.\n \n ## Decision\n \ndiff --git a/docs/agents/domain.md b/docs/agents/domain.md\nindex 258da8ea81e87ac0a6b0b5b5e5c2fde980eaabb7..67aaa6f4b7d0a5aa4d495ce6279073333f7798cb\n--- a/docs/agents/domain.md\n+++ b/docs/agents/domain.md\n@@ -5,7 +5,9 @@\n ## Layout\n \n - `CONTEXT.md` defines Shiki language. Keep it implementation-free.\n-- `docs/adr/` records hard-to-reverse decisions.\n+- `docs/adr/SADR-NNNN-*.md` records Shiki platform decisions.\n+- `docs/adr/NNNN-*.md` records target product decisions without sharing the\n+  SADR namespace.\n - `AGENTS.md` is the runtime-neutral agent constitution.\n - `CLAUDE.md` is the Claude Code wrapper around `AGENTS.md`.\n - `.shiki/` is the Target Repository mirror for Goals, PRDs, plans, Task DAGs, contracts, locks, worktree records, ledger entries, CCA verdicts, repair packets, reports, and handoffs.\ndiff --git a/docs/agents/implementation-policy.md b/docs/agents/implementation-policy.md\nindex c754144a9fb5bf4a38aa82a60e1670c59e626b44..3b059d128ce1338ee49eadec0dbd58f3cc2cc594\n--- a/docs/agents/implementation-policy.md\n+++ b/docs/agents/implementation-policy.md\n@@ -27,7 +27,7 @@\n 1. GitHub Issues, Pull Requests, Checks, Reviews, comments, and merge evidence are the operational source of truth.\n 2. The repository-local `.shiki/` mirror records Goals, PRDs, plans, Task DAGs, contracts, locks, ledger entries, CCA verdicts, repair packets, reports, and handoffs.\n 3. `CONTEXT.md` defines Shiki domain language and glossary decisions.\n-4. `docs/adr/` records hard-to-reverse platform decisions.\n+4. `docs/adr/SADR-NNNN-*.md` records Shiki platform decisions; `docs/adr/NNNN-*.md` is reserved for target ADRs.\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \ndiff --git a/scripts/shiki_contracts.py b/scripts/shiki_contracts.py\nindex 18076f0a70a1f8c9d4475a005bcccb1472a246c1..9d3f08dc1172b100c3a9b60a21ffdbae0f4dd44b\n--- a/scripts/shiki_contracts.py\n+++ b/scripts/shiki_contracts.py\n@@ -100,7 +100,7 @@\n     \"GitHub Issues, Pull Requests, Checks, Reviews, comments, and merge evidence are the operational source of truth.\",\n     \"The repository-local `.shiki/` mirror records Goals, PRDs, plans, Task DAGs, contracts, locks, ledger entries, CCA verdicts, repair packets, reports, and handoffs.\",\n     \"`CONTEXT.md` defines Shiki domain language and glossary decisions.\",\n-    \"`docs/adr/` records hard-to-reverse platform decisions.\",\n+    \"`docs/adr/SADR-NNNN-*.md` records Shiki platform decisions; `docs/adr/NNNN-*.md` is reserved for target ADRs.\",\n     \"Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\",\n )\n \ndiff --git a/tests/test_adr_references.py b/tests/test_adr_references.py\nindex 51706329c797d5dd9b47e082a333fa83b261d482..91ab76b724cb973435c45d53cab63793942267e9\n--- a/tests/test_adr_references.py\n+++ b/tests/test_adr_references.py\n@@ -1,4 +1,4 @@\n-\"\"\"Every ``ADR ####`` citation must resolve to exactly one decision record.\n+\"\"\"Every target ADR and Shiki SADR citation resolves in its own namespace.\n \n Background. ``scripts/mergegate_check.py`` justifies ``is_bookkeeping_closeout``\n with \"ADR 0017\" nine times and ``scripts/guardian_approval_signal.py`` cites\n@@ -11,11 +11,12 @@\n and the directory drifted with no signal.\n \n This is the same unbound-pair shape behind other silent failures: a reference\n-and its target with nothing asserting they agree. This test binds them. It scans\n-every ``scripts/*.py`` (plus ``docs/**/*.md`` and the root ``*.md`` governance\n-files, which are cheap) for the ``ADR ####`` pattern and asserts each cited\n-number resolves to exactly one ``docs/adr/####-*.md``. The converse is\n-deliberately NOT required: an ADR may exist without being cited.\n+and its target with nothing asserting they agree. This test binds them. Target\n+``ADR NNNN`` citations resolve only to ``docs/adr/NNNN-*.md``; Shiki\n+``SADR-NNNN`` citations resolve only to ``docs/adr/SADR-NNNN-*.md``. The scan\n+covers current governance docs, scripts, workflows, prompts, packaging metadata,\n+and repo-local Codex guidance while deliberately excluding historical\n+``.shiki`` evidence. The converse is not required: a record may be uncited.\n \n The suite is dependency free: standard-library ``unittest`` only.\n \"\"\"\n@@ -31,17 +32,18 @@\n \n REPO_ROOT = Path(__file__).resolve().parents[1]\n \n-# \"ADR 0016\" -- a four-digit citation. The trailing \\b keeps a five-digit token\n-# from being read as a truncated four-digit number.\n-_CITATION = re.compile(r\"\\bADR (\\d{4})\\b\")\n-# \"0016-locks-bound-product-paths.md\" -- the leading four digits name the record.\n-_ADR_FILENAME = re.compile(r\"^(\\d{4})-.*\\.md$\")\n+# ``ADR 0016`` / ``SADR-0016`` -- a namespace plus four-digit citation. The\n+# trailing ``\\b`` keeps a five-digit token from being read as a truncated one.\n+_CITATION = re.compile(r\"\\b(S?ADR)[ -](\\d{4})\\b\")\n+# Target ADRs retain the numeric filename; Shiki platform decisions use SADR.\n+_DECISION_FILENAME = re.compile(r\"^(?:(SADR)-)?(\\d{4})-.*\\.md$\")\n \n \n class Violation(NamedTuple):\n     \"\"\"A citation that does not resolve to exactly one decision record.\"\"\"\n \n     citing_file: str  # repo-relative path of the file that cites the ADR\n+    namespace: str  # \"ADR\" for target decisions; \"SADR\" for Shiki decisions\n     number: str  # the cited four-digit number, e.g. \"0016\"\n     line: int  # 1-based line of the citation\n     reason: str  # \"missing\" (zero matches) or \"ambiguous\" (two or more)\n@@ -49,44 +51,59 @@\n \n     @property\n     def expected_path(self) -> str:\n-        return f\"docs/adr/{self.number}-*.md\"\n+        prefix = \"SADR-\" if self.namespace == \"SADR\" else \"\"\n+        return f\"docs/adr/{prefix}{self.number}-*.md\"\n \n     def describe(self) -> str:\n         \"\"\"Name the citing file, the cited number, and the missing path.\"\"\"\n         if self.reason == \"missing\":\n             return (\n-                f\"{self.citing_file}:{self.line} cites ADR {self.number} \"\n+                f\"{self.citing_file}:{self.line} cites {self.namespace} {self.number} \"\n                 f\"but no document matches {self.expected_path}\"\n             )\n         joined = \", \".join(self.matches)\n         return (\n-            f\"{self.citing_file}:{self.line} cites ADR {self.number} which \"\n+            f\"{self.citing_file}:{self.line} cites {self.namespace} {self.number} which \"\n             f\"resolves to {len(self.matches)} documents (expected exactly one \"\n             f\"at {self.expected_path}): {joined}\"\n         )\n \n \n-def _adr_index(root: Path) -> dict[str, list[Path]]:\n-    \"\"\"Map every four-digit ADR number to the record files that claim it.\"\"\"\n-    index: dict[str, list[Path]] = {}\n+def _adr_index(root: Path) -> dict[tuple[str, str], list[Path]]:\n+    \"\"\"Map each decision namespace and number to the files that claim it.\"\"\"\n+    index: dict[tuple[str, str], list[Path]] = {}\n     adr_dir = root / \"docs\" / \"adr\"\n     if adr_dir.is_dir():\n         for path in sorted(adr_dir.glob(\"*.md\")):\n-            match = _ADR_FILENAME.match(path.name)\n+            match = _DECISION_FILENAME.match(path.name)\n             if match:\n-                index.setdefault(match.group(1), []).append(path)\n+                namespace = \"SADR\" if match.group(1) else \"ADR\"\n+                index.setdefault((namespace, match.group(2)), []).append(path)\n     return index\n \n \n def _citing_files(root: Path, include_docs: bool = True) -> list[Path]:\n-    \"\"\"Files scanned for citations: scripts/*.py, and cheaply the markdown.\"\"\"\n+    \"\"\"Current, shipped text surfaces scanned for decision citations.\"\"\"\n     files = sorted((root / \"scripts\").glob(\"*.py\"))\n+    files += sorted((root / \"scripts\").glob(\"*.sh\"))\n     if include_docs:\n         docs = root / \"docs\"\n         if docs.is_dir():\n             files += sorted(docs.rglob(\"*.md\"))\n         files += sorted(root.glob(\"*.md\"))\n-    return files\n+        pyproject = root / \"pyproject.toml\"\n+        if pyproject.is_file():\n+            files.append(pyproject)\n+        for relative in (\".github\", \".codex\"):\n+            surface = root / relative\n+            if surface.is_dir():\n+                files += sorted(\n+                    path\n+                    for path in surface.rglob(\"*\")\n+                    if path.is_file()\n+                    and path.suffix in {\".md\", \".py\", \".sh\", \".toml\", \".yaml\", \".yml\"}\n+                )\n+    return sorted(set(files))\n \n \n def find_adr_reference_violations(\n@@ -94,10 +111,9 @@\n ) -> list[Violation]:\n     \"\"\"Return every ``ADR ####`` citation under ``root`` that fails to resolve.\n \n-    A citation resolves when exactly one ``docs/adr/####-*.md`` matches its\n-    number. Zero matches is a \"missing\" violation; two or more is \"ambiguous\".\n-    Records that are never cited produce nothing -- the converse is not\n-    required.\n+    A citation resolves when exactly one record in the same namespace matches\n+    its number. Zero matches is a \"missing\" violation; two or more is\n+    \"ambiguous\". Records that are never cited produce nothing.\n     \"\"\"\n     root = Path(root)\n     index = _adr_index(root)\n@@ -110,13 +126,15 @@\n         rel = path.relative_to(root).as_posix()\n         for lineno, line in enumerate(text.splitlines(), start=1):\n             for match in _CITATION.finditer(line):\n-                number = match.group(1)\n-                found = index.get(number, [])\n+                namespace, number = match.groups()\n+                found = index.get((namespace, number), [])\n                 if len(found) == 1:\n                     continue\n                 reason = \"missing\" if not found else \"ambiguous\"\n                 rels = [p.relative_to(root).as_posix() for p in found]\n-                violations.append(Violation(rel, number, lineno, reason, rels))\n+                violations.append(\n+                    Violation(rel, namespace, number, lineno, reason, rels)\n+                )\n     return violations\n \n \n@@ -129,8 +147,8 @@\n         self.assertEqual(\n             violations,\n             [],\n-            \"Every 'ADR ####' citation must resolve to exactly one \"\n-            f\"docs/adr/####-*.md:\\n{detail}\",\n+            \"Every ADR/SADR citation must resolve exactly once in its own \"\n+            f\"docs/adr namespace:\\n{detail}\",\n         )\n \n     def test_scan_actually_reads_scripts(self) -> None:\n@@ -146,8 +164,21 @@\n         # The two records this Goal carried onto the default branch, and which\n         # the scripts cite most, must be present.\n         index = _adr_index(REPO_ROOT)\n-        self.assertIn(\"0016\", index)\n-        self.assertIn(\"0017\", index)\n+        self.assertIn((\"SADR\", \"0016\"), index)\n+        self.assertIn((\"SADR\", \"0017\"), index)\n+\n+    def test_platform_tree_has_only_sadr_0001_through_0019(self) -> None:\n+        adr_dir = REPO_ROOT / \"docs\" / \"adr\"\n+        sadrs = sorted(adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\"))\n+        self.assertEqual(\n+            {path.name.split(\"-\", 2)[1] for path in sadrs},\n+            {f\"{number:04d}\" for number in range(1, 20)},\n+        )\n+        self.assertEqual(\n+            list(adr_dir.glob(\"[0-9][0-9][0-9][0-9]-*.md\")),\n+            [],\n+            \"the Shiki platform tree must not retain legacy numeric SADR paths\",\n+        )\n \n \n class FixtureAdrReferences(unittest.TestCase):\n@@ -180,6 +211,26 @@\n         self.assertIn(\"9991\", message)  # cited number\n         self.assertIn(\"docs/adr/9991-*.md\", message)  # missing path\n \n+    def test_target_adr_does_not_satisfy_missing_platform_sadr(self) -> None:\n+        root = self._make_repo(\n+            adr_names=[\"0042-target-decision.md\"],\n+            script_body='\"\"\"Target ADR 0042 exists, but platform SADR-0042 does not.\"\"\"\\n',\n+        )\n+        violations = find_adr_reference_violations(root, include_docs=False)\n+        self.assertEqual(len(violations), 1)\n+        self.assertEqual(violations[0].namespace, \"SADR\")\n+        self.assertEqual(violations[0].number, \"0042\")\n+        self.assertEqual(violations[0].expected_path, \"docs/adr/SADR-0042-*.md\")\n+\n+    def test_target_adr_and_platform_sadr_may_reuse_a_number(self) -> None:\n+        root = self._make_repo(\n+            adr_names=[\"0042-target-decision.md\", \"SADR-0042-platform-decision.md\"],\n+            script_body='\"\"\"Target ADR 0042 and platform SADR-0042 are distinct.\"\"\"\\n',\n+        )\n+        self.assertEqual(\n+            find_adr_reference_violations(root, include_docs=False), []\n+        )\n+\n     def test_uncited_adr_is_allowed(self) -> None:\n         # An ADR exists but nothing cites it -> not a violation. The converse\n         # (every ADR must be cited) is explicitly NOT required.\ndiff --git a/tests/test_sadr_install_migration.py b/tests/test_sadr_install_migration.py\nnew file mode 100644\nindex 0000000000000000000000000000000000000000..a6398c81377f00c09aa2cb684980b070cfaf714c\n--- /dev/null\n+++ b/tests/test_sadr_install_migration.py\n@@ -0,0 +1,56 @@\n+\"\"\"SADR installation and collision-safe legacy upgrade behavior.\"\"\"\n+\n+from __future__ import annotations\n+\n+import shutil\n+import tempfile\n+import unittest\n+from pathlib import Path\n+\n+import shiki_test_support  # noqa: F401  (path bootstrap)\n+\n+import shiki_installer\n+from tests.test_adr_references import find_adr_reference_violations\n+\n+\n+PRODUCT_NUMBERS = tuple(range(13, 19))\n+\n+\n+class FreshSadrInstallTests(unittest.TestCase):\n+    def setUp(self) -> None:\n+        self.tmp = Path(tempfile.mkdtemp(prefix=\"shiki-sadr-fresh-\"))\n+        self.addCleanup(lambda: shutil.rmtree(self.tmp, ignore_errors=True))\n+        self.target = self.tmp / \"target\"\n+        self.adr_dir = self.target / \"docs\" / \"adr\"\n+        self.adr_dir.mkdir(parents=True)\n+        self.product_bytes = {\n+            f\"{number:04d}-product-decision.md\": (\n+                f\"# Product ADR {number:04d}\\n\\nTarget-owned bytes {number}.\\n\"\n+            ).encode()\n+            for number in PRODUCT_NUMBERS\n+        }\n+        for name, content in self.product_bytes.items():\n+            (self.adr_dir / name).write_bytes(content)\n+\n+    def test_fresh_install_preserves_product_adrs_and_installs_all_sadrs(self) -> None:\n+        shiki_installer.install_template(self.target, force=False, validate=True)\n+\n+        for name, expected in self.product_bytes.items():\n+            self.assertEqual((self.adr_dir / name).read_bytes(), expected)\n+\n+        sadr_numbers = {\n+            path.name.split(\"-\", 2)[1]\n+            for path in self.adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\")\n+        }\n+        self.assertEqual(sadr_numbers, {f\"{number:04d}\" for number in range(1, 20)})\n+\n+        numeric_records = {\n+            path.name\n+            for path in self.adr_dir.glob(\"[0-9][0-9][0-9][0-9]-*.md\")\n+        }\n+        self.assertEqual(numeric_records, set(self.product_bytes))\n+        self.assertEqual(find_adr_reference_violations(self.target), [])\n+\n+\n+if __name__ == \"__main__\":\n+    unittest.main()\n\ndiff --git a/.codex/skills/shiki/SKILL.md b/.codex/skills/shiki/SKILL.md\nindex c8e2e85d9d75d49cc4f2d6286ee22659c4e53e75..c392c28442311b8851ede2cf66af14011d8038d4\n--- a/.codex/skills/shiki/SKILL.md\n+++ b/.codex/skills/shiki/SKILL.md\n@@ -14,7 +14,7 @@\n 1. GitHub Issues, Pull Requests, Checks, Reviews, comments, and merge evidence are the operational source of truth.\n 2. The repository-local `.shiki/` mirror records Goals, PRDs, plans, Task DAGs, contracts, locks, ledger entries, CCA verdicts, repair packets, reports, and handoffs.\n 3. `CONTEXT.md` defines Shiki domain language and glossary decisions.\n-4. `docs/adr/` records hard-to-reverse platform decisions.\n+4. `docs/adr/SADR-NNNN-*.md` records Shiki platform decisions; `docs/adr/NNNN-*.md` is reserved for target ADRs.\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n@@ -69,7 +69,7 @@\n \n ## Responsibilities\n \n-- Claude Code implements and repairs by default (`shiki runner claude`, ADR 0008); Codex implements and repairs tasks explicitly assigned to `codex`.\n+- Claude Code implements and repairs by default (`shiki runner claude`, SADR-0008); Codex implements and repairs tasks explicitly assigned to `codex`.\n - Claude Code Action can act as GitHub-side CCA or reviewer.\n - CCA judges completion.\n - MergeGate authorizes state transitions and merge readiness.\ndiff --git a/.github/prompts/cca-completion-check.md b/.github/prompts/cca-completion-check.md\nindex ed203ad5f302ecc81372d8ab27d2b1232ca97497..3137954883be3c4657b40ea1a1253b0dc4b4ef69\n--- a/.github/prompts/cca-completion-check.md\n+++ b/.github/prompts/cca-completion-check.md\n@@ -15,7 +15,7 @@\n 1. GitHub Issues, Pull Requests, Checks, Reviews, comments, and merge evidence are the operational source of truth.\n 2. The repository-local `.shiki/` mirror records Goals, PRDs, plans, Task DAGs, contracts, locks, ledger entries, CCA verdicts, repair packets, reports, and handoffs.\n 3. `CONTEXT.md` defines Shiki domain language and glossary decisions.\n-4. `docs/adr/` records hard-to-reverse platform decisions.\n+4. `docs/adr/SADR-NNNN-*.md` records Shiki platform decisions; `docs/adr/NNNN-*.md` is reserved for target ADRs.\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \ndiff --git a/AGENTS.md b/AGENTS.md\nindex 7f041100d49c2a7525f51cb0f5d696a4a6d1c78c..ddd7fe4d489f2c4c3ce82b51c84934019a7616b4\n--- a/AGENTS.md\n+++ b/AGENTS.md\n@@ -40,7 +40,7 @@\n 1. GitHub Issues, Pull Requests, Checks, Reviews, comments, and merge evidence are the operational source of truth.\n 2. The repository-local `.shiki/` mirror records Goals, PRDs, plans, Task DAGs, contracts, locks, ledger entries, CCA verdicts, repair packets, reports, and handoffs.\n 3. `CONTEXT.md` defines Shiki domain language and glossary decisions.\n-4. `docs/adr/` records hard-to-reverse platform decisions.\n+4. `docs/adr/SADR-NNNN-*.md` records Shiki platform decisions; `docs/adr/NNNN-*.md` is reserved for target ADRs.\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \ndiff --git a/CLAUDE.md b/CLAUDE.md\nindex 98a932e7841a632866fcd53b0bad0e3af3711d3d..83e001d4c15ed0a421666dc68da548ecb56748df\n--- a/CLAUDE.md\n+++ b/CLAUDE.md\n@@ -10,7 +10,7 @@\n 1. GitHub Issues, Pull Requests, Checks, Reviews, comments, and merge evidence are the operational source of truth.\n 2. The repository-local `.shiki/` mirror records Goals, PRDs, plans, Task DAGs, contracts, locks, ledger entries, CCA verdicts, repair packets, reports, and handoffs.\n 3. `CONTEXT.md` defines Shiki domain language and glossary decisions.\n-4. `docs/adr/` records hard-to-reverse platform decisions.\n+4. `docs/adr/SADR-NNNN-*.md` records Shiki platform decisions; `docs/adr/NNNN-*.md` is reserved for target ADRs.\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \ndiff --git a/SYSTEM_PROMPT.md b/SYSTEM_PROMPT.md\nindex aaaea45e3a896c3a44ba89eec8bb84325ecf6c62..42087a9325dfaa48de71b8319017175689166d2e\n--- a/SYSTEM_PROMPT.md\n+++ b/SYSTEM_PROMPT.md\n@@ -26,7 +26,7 @@\n 1. GitHub Issues, Pull Requests, Checks, Reviews, comments, and merge evidence are the operational source of truth.\n 2. The repository-local `.shiki/` mirror records Goals, PRDs, plans, Task DAGs, contracts, locks, ledger entries, CCA verdicts, repair packets, reports, and handoffs.\n 3. `CONTEXT.md` defines Shiki domain language and glossary decisions.\n-4. `docs/adr/` records hard-to-reverse platform decisions.\n+4. `docs/adr/SADR-NNNN-*.md` records Shiki platform decisions; `docs/adr/NNNN-*.md` is reserved for target ADRs.\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \ndiff --git a/docs/adr/0001-keep-shiki-as-platform-name.md b/docs/adr/SADR-0001-keep-shiki-as-platform-name.md\nindex 20a9637c3b9d8c97de7ba8db7cfae4d82d9a7dba..8b685439959aada1b221c112f0ac3c51ca8a1695\n--- a/docs/adr/0001-keep-shiki-as-platform-name.md\n+++ b/docs/adr/SADR-0001-keep-shiki-as-platform-name.md\n@@ -1,3 +1,3 @@\n-# Keep Shiki As The Platform Name\n+# SADR-0001: Keep Shiki As The Platform Name\n \n Shiki remains the product name for the GitHub-native agentic engineering control plane. We considered more descriptive names such as MergePilot, MergeGate, and Kiroku, but kept Shiki because it carries the existing goal-seeking framework identity while remaining independent of Hermes, Codex, Claude Code, or any single runtime.\ndiff --git a/docs/adr/0002-use-agents-as-shared-constitution.md b/docs/adr/SADR-0002-use-agents-as-shared-constitution.md\nindex 73de30ecb078120eac540d9bc51855e3cd48d0e3..c5486182bf06fca36ea508496b013c67302fd989\n--- a/docs/adr/0002-use-agents-as-shared-constitution.md\n+++ b/docs/adr/SADR-0002-use-agents-as-shared-constitution.md\n@@ -1,3 +1,3 @@\n-# Use AGENTS As The Shared Agent Constitution\n+# SADR-0002: Use AGENTS As The Shared Agent Constitution\n \n Shiki uses `AGENTS.md` as the runtime-neutral constitution and `CLAUDE.md` as a thin Claude Code wrapper. This keeps Codex, Claude Code, Hermes Runner, and future Agent Runtimes aligned on the same GitHub-first MergeGate rules while still allowing runtime-specific instructions where needed.\ndiff --git a/docs/adr/0003-require-skill-gate-for-mergegate-readiness.md b/docs/adr/SADR-0003-require-skill-gate-for-mergegate-readiness.md\nindex 424be0c9af4c8f0136f1fde68a468c47ff84e764..7fc337987c7df9792f75f181dfcbed75fe5ffff2\n--- a/docs/adr/0003-require-skill-gate-for-mergegate-readiness.md\n+++ b/docs/adr/SADR-0003-require-skill-gate-for-mergegate-readiness.md\n@@ -1,3 +1,3 @@\n-# Require Skill Gate For MergeGate Readiness\n+# SADR-0003: Require Skill Gate For MergeGate Readiness\n \n Shiki requires relevant engineering skills to be invoked before execution when their triggers apply. This makes skills part of MergeGate readiness rather than optional prompt style, because Shiki depends on repeatable planning, issue decomposition, TDD, diagnosis, triage, and architecture review before autonomous or parallel agent work can be trusted.\ndiff --git a/docs/adr/0004-use-github-first-with-shiki-mirror.md b/docs/adr/SADR-0004-use-github-first-with-shiki-mirror.md\nindex d0a3a9174dc646a945a9d325954ffaf00c352040..a79c54ae9ee8eed2a0524192b864da4e22a92d3f\n--- a/docs/adr/0004-use-github-first-with-shiki-mirror.md\n+++ b/docs/adr/SADR-0004-use-github-first-with-shiki-mirror.md\n@@ -1,4 +1,4 @@\n-# ADR 0004: Use GitHub First With A Repository-Local Shiki Mirror\n+# SADR-0004: Use GitHub First With A Repository-Local Shiki Mirror\n \n ## Status\n \ndiff --git a/docs/adr/0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md b/docs/adr/SADR-0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\nindex b339baf49b89d9290e79c3d51b36e7b2bde3bd95..c30a0d8b1fde0224d0ac7820e7870dccf721580d\n--- a/docs/adr/0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\n+++ b/docs/adr/SADR-0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\n@@ -1,4 +1,4 @@\n-# ADR 0005: Use Codex As Front And Claude Code Action As The GitHub Runtime\n+# SADR-0005: Use Codex As Front And Claude Code Action As The GitHub Runtime\n \n ## Status\n \ndiff --git a/docs/adr/0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md b/docs/adr/SADR-0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\nindex 4bcae9d1e52f0a37b8e9fbafa5cd14b6ee1993bd..7a38538a6ae9929567f0ade67e7ea6a60d0b3f7e\n--- a/docs/adr/0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\n+++ b/docs/adr/SADR-0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\n@@ -1,4 +1,4 @@\n-# ADR 0006: Separate Codex Implementation, CCA Completion, And MergeGate Authorization\n+# SADR-0006: Separate Codex Implementation, CCA Completion, And MergeGate Authorization\n \n ## Status\n \ndiff --git a/docs/adr/0007-packaging-and-release.md b/docs/adr/SADR-0007-packaging-and-release.md\nindex 79b84ddb97167372b44641e5b7860e400ce80822..023cd57f8011aa6e6d2ebf612b7c35a7a82c00d4\n--- a/docs/adr/0007-packaging-and-release.md\n+++ b/docs/adr/SADR-0007-packaging-and-release.md\n@@ -1,4 +1,4 @@\n-# ADR 0007: Packaging And Release For Shiki\n+# SADR-0007: Packaging And Release For Shiki\n \n ## Status\n \ndiff --git a/docs/adr/0008-use-claude-code-as-default-implementer.md b/docs/adr/SADR-0008-use-claude-code-as-default-implementer.md\nindex dedf4510b7c7799521422f622029563d373db031..a158a8d444d93764ec515a28133f7dde4af23579\n--- a/docs/adr/0008-use-claude-code-as-default-implementer.md\n+++ b/docs/adr/SADR-0008-use-claude-code-as-default-implementer.md\n@@ -1,4 +1,4 @@\n-# ADR 0008: Use Claude Code As The Default Implementer Runtime\n+# SADR-0008: Use Claude Code As The Default Implementer Runtime\n \n ## Status\n \ndiff --git a/docs/adr/0009-requirements-definition-and-spec-freeze.md b/docs/adr/SADR-0009-requirements-definition-and-spec-freeze.md\nindex 29b3f7871bef9203a8b4a831a65c39eb2f506e42..52e7dd69ad0aa4efe0b68c62678abbf7df02aaec\n--- a/docs/adr/0009-requirements-definition-and-spec-freeze.md\n+++ b/docs/adr/SADR-0009-requirements-definition-and-spec-freeze.md\n@@ -1,4 +1,4 @@\n-# ADR 0009: Merge Grill And Planning Into Requirements Definition With Spec Freeze\n+# SADR-0009: Merge Grill And Planning Into Requirements Definition With Spec Freeze\n \n ## Status\n \ndiff --git a/docs/adr/0010-external-ai-guardian-review.md b/docs/adr/SADR-0010-external-ai-guardian-review.md\nindex e6108bbe8bb4e73e0ff4684c8f78001a80438c4e..e690d4eddd270e683d709c9b679c5859e71eeaaa\n--- a/docs/adr/0010-external-ai-guardian-review.md\n+++ b/docs/adr/SADR-0010-external-ai-guardian-review.md\n@@ -1,4 +1,4 @@\n-# ADR 0010: External AI Guardian Review As A First-Class Approval Authority\n+# SADR-0010: External AI Guardian Review As A First-Class Approval Authority\n \n ## Status\n \ndiff --git a/docs/adr/0011-autonomous-quality-gate-evidence.md b/docs/adr/SADR-0011-autonomous-quality-gate-evidence.md\nindex bca0246c5d46e44afbde15486a75eec425fc02f5..9c93eac16d8c3a6d4b11bd7224e3955102cd7db4\n--- a/docs/adr/0011-autonomous-quality-gate-evidence.md\n+++ b/docs/adr/SADR-0011-autonomous-quality-gate-evidence.md\n@@ -1,4 +1,4 @@\n-# ADR 0011: Autonomous Quality-Gate Evidence \u2014 Deterministic Facts And An Independent Verifier\n+# SADR-0011: Autonomous Quality-Gate Evidence \u2014 Deterministic Facts And An Independent Verifier\n \n ## Status\n \ndiff --git a/docs/adr/0012-autonomous-loop-closeout-to-main.md b/docs/adr/SADR-0012-autonomous-loop-closeout-to-main.md\nindex 637fad6dde4989c9c2bf0f81f676d8d29991123c..ec7d85f81030a676bcbba28b43eb365422a36eac\n--- a/docs/adr/0012-autonomous-loop-closeout-to-main.md\n+++ b/docs/adr/SADR-0012-autonomous-loop-closeout-to-main.md\n@@ -1,4 +1,4 @@\n-# 0012 \u2014 Autonomous loop pushes goal completion to main via a closeout PR\n+# SADR-0012 \u2014 Autonomous loop pushes goal completion to main via a closeout PR\n \n - Status: accepted\n - Date: 2026-06-18\ndiff --git a/docs/adr/0013-bootstrap-grants-actions-approve-pull-requests.md b/docs/adr/SADR-0013-bootstrap-grants-actions-approve-pull-requests.md\nindex b6c69d64f1c5ced346b88d6859586715b3b5bd0e..a793e7277d67ab8ea56286fec289ca689825a17d\n--- a/docs/adr/0013-bootstrap-grants-actions-approve-pull-requests.md\n+++ b/docs/adr/SADR-0013-bootstrap-grants-actions-approve-pull-requests.md\n@@ -1,4 +1,4 @@\n-# ADR 0013: Bootstrap Grants GitHub Actions Permission To Create And Approve Pull Requests\n+# SADR-0013: Bootstrap Grants GitHub Actions Permission To Create And Approve Pull Requests\n \n ## Status\n \ndiff --git a/docs/adr/0014-codex-app-external-ai-guardian-ui-adapter.md b/docs/adr/SADR-0014-codex-app-external-ai-guardian-ui-adapter.md\nindex 238db06279334caa341c117362df9271382a7fcd..65f12e9939321e1b3e389a34c822fc1549e5155d\n--- a/docs/adr/0014-codex-app-external-ai-guardian-ui-adapter.md\n+++ b/docs/adr/SADR-0014-codex-app-external-ai-guardian-ui-adapter.md\n@@ -1,4 +1,4 @@\n-# ADR 0014: Use Codex App As The External AI Guardian UI Adapter\n+# SADR-0014: Use Codex App As The External AI Guardian UI Adapter\n \n ## Status\n \ndiff --git a/docs/adr/0015-contract-approval-before-dispatch.md b/docs/adr/SADR-0015-contract-approval-before-dispatch.md\nindex fad6c656741a20a7684ba16080a7db159f63a52f..7bdd760bb3e82eee4e1410f45c783b03b638d456\n--- a/docs/adr/0015-contract-approval-before-dispatch.md\n+++ b/docs/adr/SADR-0015-contract-approval-before-dispatch.md\n@@ -1,4 +1,4 @@\n-# ADR 0015: Approve The Task Contract Before Dispatch, Not The Diff After It\n+# SADR-0015: Approve The Task Contract Before Dispatch, Not The Diff After It\n \n ## Status\n \ndiff --git a/docs/adr/0016-locks-bound-product-paths.md b/docs/adr/SADR-0016-locks-bound-product-paths.md\nindex b5e850f3f1f7f4ef6e7f9d1763a29a51bd300c45..332cc17313498404f41a145291e38447b5de0dc4\n--- a/docs/adr/0016-locks-bound-product-paths.md\n+++ b/docs/adr/SADR-0016-locks-bound-product-paths.md\n@@ -1,4 +1,4 @@\n-# ADR 0016: Locks bound product paths; the Shiki mirror is bound by record identity\n+# SADR-0016: Locks bound product paths; the Shiki mirror is bound by record identity\n \n ## Status\n \ndiff --git a/docs/adr/0017-bookkeeping-closeouts-do-not-inherit-risk.md b/docs/adr/SADR-0017-bookkeeping-closeouts-do-not-inherit-risk.md\nindex 554e3a0d526a72ca067108c5464cc68c3edcf174..83115f69f1b1b48d544c9b0157f377b91cb53abf\n--- a/docs/adr/0017-bookkeeping-closeouts-do-not-inherit-risk.md\n+++ b/docs/adr/SADR-0017-bookkeeping-closeouts-do-not-inherit-risk.md\n@@ -1,4 +1,4 @@\n-# ADR 0017: A Bookkeeping Closeout Does Not Inherit Its Task's Risk\n+# SADR-0017: A Bookkeeping Closeout Does Not Inherit Its Task's Risk\n \n ## Status\n \ndiff --git a/docs/adr/0018-carried-guardian-approval-across-a-proven-base-sync.md b/docs/adr/SADR-0018-carried-guardian-approval-across-a-proven-base-sync.md\nindex 22358562da390ed8b45cd600da08a43d378b4680..38da0fd95102c3496c3219ce2319fce696a2532a\n--- a/docs/adr/0018-carried-guardian-approval-across-a-proven-base-sync.md\n+++ b/docs/adr/SADR-0018-carried-guardian-approval-across-a-proven-base-sync.md\n@@ -1,4 +1,4 @@\n-# ADR 0018: Carried Guardian Approval Across A Proven Base Sync\n+# SADR-0018: Carried Guardian Approval Across A Proven Base Sync\n \n ## Status\n \ndiff --git a/docs/adr/README.md b/docs/adr/README.md\nindex c70e057a4520c36e042ab68c53cd4a21ef709b71..5da43856836e2ce6a150f8127766e2f88355ab4b\n--- a/docs/adr/README.md\n+++ b/docs/adr/README.md\n@@ -1,40 +1,41 @@\n-# Architecture Decision Records\n+# Decision Record Namespaces\n \n-This directory holds Shiki's Architecture Decision Records (ADRs): durable\n-records of hard-to-reverse platform decisions. ADRs are part of the Shiki source\n-of truth alongside GitHub state, the `.shiki/` mirror, and `CONTEXT.md`.\n+This directory holds two independent decision namespaces:\n \n-## How ADRs Work\n+- Shiki Architecture Decision Records (SADRs), named\n+  `SADR-NNNN-<short-slug>.md`, record hard-to-reverse Shiki platform decisions.\n+- Target Architecture Decision Records (ADRs), named\n+  `NNNN-<short-slug>.md`, belong to the target product.\n \n-- New ADRs are added as `docs/adr/NNNN-<short-slug>.md`, where `NNNN` is the next\n-  zero-padded sequence number.\n-- The **canonical list of ADRs is the set of files matching `docs/adr/*.md`** in\n-  this directory. Browse `docs/adr/*.md` for the authoritative, current set.\n-- Use [`template.md`](template.md) as the starting point for a new ADR.\n-- Record an ADR when a decision is hard to reverse, surprising without context,\n-  and a real tradeoff. Do not write an ADR for routine or easily reversible\n-  choices.\n+The same four-digit number may appear once in each namespace. A platform\n+`SADR-NNNN` citation resolves only to `SADR-NNNN-*.md`; a target `ADR NNNN`\n+citation resolves only to `NNNN-*.md`.\n \n-Using the `docs/adr/*.md` glob as the source of truth means other PRs can add an\n-ADR without editing this index file, avoiding merge contention on a\n-hand-maintained enumeration.\n+## How Decision Records Work\n \n-## Current ADRs (snapshot)\n-\n-The list below is a convenience snapshot and may lag behind the directory. When\n-in doubt, list `docs/adr/*.md`.\n+- New Shiki platform decisions use the next zero-padded SADR number.\n+- New target product decisions use the target's own ADR sequence.\n+- The canonical list is the files in `docs/adr/`; no hand-maintained index is\n+  authoritative.\n+- Use [`template.md`](template.md) as the starting point and choose the filename\n+  namespace that owns the decision.\n+- Record a decision when it is hard to reverse, surprising without context, and\n+  a real tradeoff. Do not write a decision record for routine or easily\n+  reversible choices.\n \n-- [0001 \u2014 Keep Shiki As The Platform Name](0001-keep-shiki-as-platform-name.md)\n-- [0002 \u2014 Use AGENTS As The Shared Agent Constitution](0002-use-agents-as-shared-constitution.md)\n-- [0003 \u2014 Require Skill Gate For MergeGate Readiness](0003-require-skill-gate-for-mergegate-readiness.md)\n-- [0004 \u2014 Use GitHub First With A Repository-Local Shiki Mirror](0004-use-github-first-with-shiki-mirror.md)\n-- [0005 \u2014 Use Codex As Front And Claude Code Action As The GitHub Runtime](0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md)\n-- [0006 \u2014 Separate Codex Implementation, CCA Completion, And MergeGate Authorization](0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md)\n+Using the directory as the source of truth lets independent changes add records\n+without contending on an enumerated index.\n \n-## Adding A New ADR\n+## Adding A Shiki SADR\n \n-1. Copy `template.md` to `docs/adr/NNNN-<short-slug>.md`.\n+1. Copy `template.md` to `docs/adr/SADR-NNNN-<short-slug>.md`.\n 2. Fill in the title, status, context, decision, and consequences.\n-3. Set the status to `Proposed`, then `Accepted` once the decision is approved.\n-4. Open a PR following `CONTRIBUTING.md`. You do not need to edit this index for\n-   the ADR to be discoverable via `docs/adr/*.md`.\n+3. Set the status to `Proposed`, then `Accepted` once the proper authority has\n+   approved the decision.\n+4. Open a PR following `CONTRIBUTING.md`.\n+\n+## Adding A Target ADR\n+\n+1. Copy `template.md` to `docs/adr/NNNN-<short-slug>.md` using the target's own\n+   sequence.\n+2. Cite it as `ADR NNNN`; do not prefix its filename with `SADR-`.\ndiff --git a/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md b/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\nnew file mode 100644\nindex 0000000000000000000000000000000000000000..7455ce1f3bed02ddddb851ac21a1d1a648c9aeba\n--- /dev/null\n+++ b/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\n@@ -0,0 +1,55 @@\n+# SADR-0019: Separate Shiki Platform SADRs From Target ADRs\n+\n+## Status\n+\n+Accepted\n+\n+## Context\n+\n+Shiki installs its platform decision records into `docs/adr/` in every target\n+repository. Shiki and its targets previously used the same numeric\n+`NNNN-<slug>.md` namespace. A target that already owned ADRs 0013 through 0018\n+therefore acquired two records for each number during installation, making an\n+`ADR NNNN` citation ambiguous. Renumbering target decisions would rewrite target\n+history, while renumbering Shiki decisions per target would destroy their stable\n+platform identity.\n+\n+Upgrades also need to remove the former Shiki-owned numeric paths without ever\n+mistaking a target-owned ADR for platform content. The install stamp already\n+records exact shipped paths and content digests, so it can prove ownership only\n+when both still match.\n+\n+## Decision\n+\n+Shiki platform decisions use the `SADR` namespace:\n+\n+- records are named `docs/adr/SADR-NNNN-<slug>.md`;\n+- live platform citations use `SADR-NNNN`;\n+- target product decisions retain `docs/adr/NNNN-<slug>.md` and `ADR NNNN`;\n+- the reference validator resolves the two namespaces independently, so the same\n+  four-digit number may exist once in each namespace.\n+\n+Before a forced upgrade writes anything, the installer inventories the exact\n+former numeric Shiki ADR paths. It may delete a legacy path only when the\n+existing install stamp contains that exact path and its recorded SHA-256 digest\n+matches the file currently on disk. All ownership blockers are reported\n+together and abort the upgrade before its first write. A non-forced install\n+never performs this cleanup.\n+\n+Historical `.shiki/` evidence remains unchanged because it records the namespace\n+that existed when that evidence was produced.\n+\n+## Consequences\n+\n+- Targets may use any ADR number without colliding with Shiki's platform SADRs.\n+- Fresh installs preserve target ADR bytes and install a stable SADR set.\n+- Forced upgrades fail closed when legacy ownership cannot be proven, and a\n+  successful upgrade refreshes the stamp without removed legacy paths.\n+- Current governance, packaging, workflow, prompt, script, and skill references\n+  must move with the platform records.\n+\n+## Alternatives Considered\n+\n+- Collision-only refusal leaves affected targets permanently unupgradable.\n+- Renumbering target ADRs mutates target-owned history.\n+- Per-target Shiki renumbering makes platform citations non-portable.\ndiff --git a/docs/adr/template.md b/docs/adr/template.md\nindex a92a97daddd0b00ab9dd626cc8d86942da2eafed..17dd79175b5fee2105935557f691b40cf9492942\n--- a/docs/adr/template.md\n+++ b/docs/adr/template.md\n@@ -1,14 +1,14 @@\n-# NNNN: <Short Decision Title>\n+# <SADR-NNNN or NNNN>: <Short Decision Title>\n \n ## Status\n \n-Proposed | Accepted | Superseded by ADR-NNNN\n+Proposed | Accepted | Superseded by SADR-NNNN or target ADR NNNN\n \n ## Context\n \n What is the situation, constraint, or force that requires a decision? Reference\n-relevant domain terms (`CONTEXT.md`), prior ADRs, and code reality. State the\n-problem, not the solution.\n+relevant domain terms (`CONTEXT.md`), prior Shiki SADRs or target ADRs, and code\n+reality. State the problem, not the solution.\n \n ## Decision\n \ndiff --git a/docs/agents/domain.md b/docs/agents/domain.md\nindex 258da8ea81e87ac0a6b0b5b5e5c2fde980eaabb7..67aaa6f4b7d0a5aa4d495ce6279073333f7798cb\n--- a/docs/agents/domain.md\n+++ b/docs/agents/domain.md\n@@ -5,7 +5,9 @@\n ## Layout\n \n - `CONTEXT.md` defines Shiki language. Keep it implementation-free.\n-- `docs/adr/` records hard-to-reverse decisions.\n+- `docs/adr/SADR-NNNN-*.md` records Shiki platform decisions.\n+- `docs/adr/NNNN-*.md` records target product decisions without sharing the\n+  SADR namespace.\n - `AGENTS.md` is the runtime-neutral agent constitution.\n - `CLAUDE.md` is the Claude Code wrapper around `AGENTS.md`.\n - `.shiki/` is the Target Repository mirror for Goals, PRDs, plans, Task DAGs, contracts, locks, worktree records, ledger entries, CCA verdicts, repair packets, reports, and handoffs.\ndiff --git a/docs/agents/implementation-policy.md b/docs/agents/implementation-policy.md\nindex c754144a9fb5bf4a38aa82a60e1670c59e626b44..3b059d128ce1338ee49eadec0dbd58f3cc2cc594\n--- a/docs/agents/implementation-policy.md\n+++ b/docs/agents/implementation-policy.md\n@@ -27,7 +27,7 @@\n 1. GitHub Issues, Pull Requests, Checks, Reviews, comments, and merge evidence are the operational source of truth.\n 2. The repository-local `.shiki/` mirror records Goals, PRDs, plans, Task DAGs, contracts, locks, ledger entries, CCA verdicts, repair packets, reports, and handoffs.\n 3. `CONTEXT.md` defines Shiki domain language and glossary decisions.\n-4. `docs/adr/` records hard-to-reverse platform decisions.\n+4. `docs/adr/SADR-NNNN-*.md` records Shiki platform decisions; `docs/adr/NNNN-*.md` is reserved for target ADRs.\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \ndiff --git a/scripts/shiki_contracts.py b/scripts/shiki_contracts.py\nindex 18076f0a70a1f8c9d4475a005bcccb1472a246c1..9d3f08dc1172b100c3a9b60a21ffdbae0f4dd44b\n--- a/scripts/shiki_contracts.py\n+++ b/scripts/shiki_contracts.py\n@@ -100,7 +100,7 @@\n     \"GitHub Issues, Pull Requests, Checks, Reviews, comments, and merge evidence are the operational source of truth.\",\n     \"The repository-local `.shiki/` mirror records Goals, PRDs, plans, Task DAGs, contracts, locks, ledger entries, CCA verdicts, repair packets, reports, and handoffs.\",\n     \"`CONTEXT.md` defines Shiki domain language and glossary decisions.\",\n-    \"`docs/adr/` records hard-to-reverse platform decisions.\",\n+    \"`docs/adr/SADR-NNNN-*.md` records Shiki platform decisions; `docs/adr/NNNN-*.md` is reserved for target ADRs.\",\n     \"Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\",\n )\n \ndiff --git a/tests/test_adr_references.py b/tests/test_adr_references.py\nindex 51706329c797d5dd9b47e082a333fa83b261d482..91ab76b724cb973435c45d53cab63793942267e9\n--- a/tests/test_adr_references.py\n+++ b/tests/test_adr_references.py\n@@ -1,4 +1,4 @@\n-\"\"\"Every ``ADR ####`` citation must resolve to exactly one decision record.\n+\"\"\"Every target ADR and Shiki SADR citation resolves in its own namespace.\n \n Background. ``scripts/mergegate_check.py`` justifies ``is_bookkeeping_closeout``\n with \"ADR 0017\" nine times and ``scripts/guardian_approval_signal.py`` cites\n@@ -11,11 +11,12 @@\n and the directory drifted with no signal.\n \n This is the same unbound-pair shape behind other silent failures: a reference\n-and its target with nothing asserting they agree. This test binds them. It scans\n-every ``scripts/*.py`` (plus ``docs/**/*.md`` and the root ``*.md`` governance\n-files, which are cheap) for the ``ADR ####`` pattern and asserts each cited\n-number resolves to exactly one ``docs/adr/####-*.md``. The converse is\n-deliberately NOT required: an ADR may exist without being cited.\n+and its target with nothing asserting they agree. This test binds them. Target\n+``ADR NNNN`` citations resolve only to ``docs/adr/NNNN-*.md``; Shiki\n+``SADR-NNNN`` citations resolve only to ``docs/adr/SADR-NNNN-*.md``. The scan\n+covers current governance docs, scripts, workflows, prompts, packaging metadata,\n+and repo-local Codex guidance while deliberately excluding historical\n+``.shiki`` evidence. The converse is not required: a record may be uncited.\n \n The suite is dependency free: standard-library ``unittest`` only.\n \"\"\"\n@@ -31,17 +32,18 @@\n \n REPO_ROOT = Path(__file__).resolve().parents[1]\n \n-# \"ADR 0016\" -- a four-digit citation. The trailing \\b keeps a five-digit token\n-# from being read as a truncated four-digit number.\n-_CITATION = re.compile(r\"\\bADR (\\d{4})\\b\")\n-# \"0016-locks-bound-product-paths.md\" -- the leading four digits name the record.\n-_ADR_FILENAME = re.compile(r\"^(\\d{4})-.*\\.md$\")\n+# ``ADR 0016`` / ``SADR-0016`` -- a namespace plus four-digit citation. The\n+# trailing ``\\b`` keeps a five-digit token from being read as a truncated one.\n+_CITATION = re.compile(r\"\\b(S?ADR)[ -](\\d{4})\\b\")\n+# Target ADRs retain the numeric filename; Shiki platform decisions use SADR.\n+_DECISION_FILENAME = re.compile(r\"^(?:(SADR)-)?(\\d{4})-.*\\.md$\")\n \n \n class Violation(NamedTuple):\n     \"\"\"A citation that does not resolve to exactly one decision record.\"\"\"\n \n     citing_file: str  # repo-relative path of the file that cites the ADR\n+    namespace: str  # \"ADR\" for target decisions; \"SADR\" for Shiki decisions\n     number: str  # the cited four-digit number, e.g. \"0016\"\n     line: int  # 1-based line of the citation\n     reason: str  # \"missing\" (zero matches) or \"ambiguous\" (two or more)\n@@ -49,44 +51,59 @@\n \n     @property\n     def expected_path(self) -> str:\n-        return f\"docs/adr/{self.number}-*.md\"\n+        prefix = \"SADR-\" if self.namespace == \"SADR\" else \"\"\n+        return f\"docs/adr/{prefix}{self.number}-*.md\"\n \n     def describe(self) -> str:\n         \"\"\"Name the citing file, the cited number, and the missing path.\"\"\"\n         if self.reason == \"missing\":\n             return (\n-                f\"{self.citing_file}:{self.line} cites ADR {self.number} \"\n+                f\"{self.citing_file}:{self.line} cites {self.namespace} {self.number} \"\n                 f\"but no document matches {self.expected_path}\"\n             )\n         joined = \", \".join(self.matches)\n         return (\n-            f\"{self.citing_file}:{self.line} cites ADR {self.number} which \"\n+            f\"{self.citing_file}:{self.line} cites {self.namespace} {self.number} which \"\n             f\"resolves to {len(self.matches)} documents (expected exactly one \"\n             f\"at {self.expected_path}): {joined}\"\n         )\n \n \n-def _adr_index(root: Path) -> dict[str, list[Path]]:\n-    \"\"\"Map every four-digit ADR number to the record files that claim it.\"\"\"\n-    index: dict[str, list[Path]] = {}\n+def _adr_index(root: Path) -> dict[tuple[str, str], list[Path]]:\n+    \"\"\"Map each decision namespace and number to the files that claim it.\"\"\"\n+    index: dict[tuple[str, str], list[Path]] = {}\n     adr_dir = root / \"docs\" / \"adr\"\n     if adr_dir.is_dir():\n         for path in sorted(adr_dir.glob(\"*.md\")):\n-            match = _ADR_FILENAME.match(path.name)\n+            match = _DECISION_FILENAME.match(path.name)\n             if match:\n-                index.setdefault(match.group(1), []).append(path)\n+                namespace = \"SADR\" if match.group(1) else \"ADR\"\n+                index.setdefault((namespace, match.group(2)), []).append(path)\n     return index\n \n \n def _citing_files(root: Path, include_docs: bool = True) -> list[Path]:\n-    \"\"\"Files scanned for citations: scripts/*.py, and cheaply the markdown.\"\"\"\n+    \"\"\"Current, shipped text surfaces scanned for decision citations.\"\"\"\n     files = sorted((root / \"scripts\").glob(\"*.py\"))\n+    files += sorted((root / \"scripts\").glob(\"*.sh\"))\n     if include_docs:\n         docs = root / \"docs\"\n         if docs.is_dir():\n             files += sorted(docs.rglob(\"*.md\"))\n         files += sorted(root.glob(\"*.md\"))\n-    return files\n+        pyproject = root / \"pyproject.toml\"\n+        if pyproject.is_file():\n+            files.append(pyproject)\n+        for relative in (\".github\", \".codex\"):\n+            surface = root / relative\n+            if surface.is_dir():\n+                files += sorted(\n+                    path\n+                    for path in surface.rglob(\"*\")\n+                    if path.is_file()\n+                    and path.suffix in {\".md\", \".py\", \".sh\", \".toml\", \".yaml\", \".yml\"}\n+                )\n+    return sorted(set(files))\n \n \n def find_adr_reference_violations(\n@@ -94,10 +111,9 @@\n ) -> list[Violation]:\n     \"\"\"Return every ``ADR ####`` citation under ``root`` that fails to resolve.\n \n-    A citation resolves when exactly one ``docs/adr/####-*.md`` matches its\n-    number. Zero matches is a \"missing\" violation; two or more is \"ambiguous\".\n-    Records that are never cited produce nothing -- the converse is not\n-    required.\n+    A citation resolves when exactly one record in the same namespace matches\n+    its number. Zero matches is a \"missing\" violation; two or more is\n+    \"ambiguous\". Records that are never cited produce nothing.\n     \"\"\"\n     root = Path(root)\n     index = _adr_index(root)\n@@ -110,13 +126,15 @@\n         rel = path.relative_to(root).as_posix()\n         for lineno, line in enumerate(text.splitlines(), start=1):\n             for match in _CITATION.finditer(line):\n-                number = match.group(1)\n-                found = index.get(number, [])\n+                namespace, number = match.groups()\n+                found = index.get((namespace, number), [])\n                 if len(found) == 1:\n                     continue\n                 reason = \"missing\" if not found else \"ambiguous\"\n                 rels = [p.relative_to(root).as_posix() for p in found]\n-                violations.append(Violation(rel, number, lineno, reason, rels))\n+                violations.append(\n+                    Violation(rel, namespace, number, lineno, reason, rels)\n+                )\n     return violations\n \n \n@@ -129,8 +147,8 @@\n         self.assertEqual(\n             violations,\n             [],\n-            \"Every 'ADR ####' citation must resolve to exactly one \"\n-            f\"docs/adr/####-*.md:\\n{detail}\",\n+            \"Every ADR/SADR citation must resolve exactly once in its own \"\n+            f\"docs/adr namespace:\\n{detail}\",\n         )\n \n     def test_scan_actually_reads_scripts(self) -> None:\n@@ -146,8 +164,21 @@\n         # The two records this Goal carried onto the default branch, and which\n         # the scripts cite most, must be present.\n         index = _adr_index(REPO_ROOT)\n-        self.assertIn(\"0016\", index)\n-        self.assertIn(\"0017\", index)\n+        self.assertIn((\"SADR\", \"0016\"), index)\n+        self.assertIn((\"SADR\", \"0017\"), index)\n+\n+    def test_platform_tree_has_only_sadr_0001_through_0019(self) -> None:\n+        adr_dir = REPO_ROOT / \"docs\" / \"adr\"\n+        sadrs = sorted(adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\"))\n+        self.assertEqual(\n+            {path.name.split(\"-\", 2)[1] for path in sadrs},\n+            {f\"{number:04d}\" for number in range(1, 20)},\n+        )\n+        self.assertEqual(\n+            list(adr_dir.glob(\"[0-9][0-9][0-9][0-9]-*.md\")),\n+            [],\n+            \"the Shiki platform tree must not retain legacy numeric SADR paths\",\n+        )\n \n \n class FixtureAdrReferences(unittest.TestCase):\n@@ -180,6 +211,26 @@\n         self.assertIn(\"9991\", message)  # cited number\n         self.assertIn(\"docs/adr/9991-*.md\", message)  # missing path\n \n+    def test_target_adr_does_not_satisfy_missing_platform_sadr(self) -> None:\n+        root = self._make_repo(\n+            adr_names=[\"0042-target-decision.md\"],\n+            script_body='\"\"\"Target ADR 0042 exists, but platform SADR-0042 does not.\"\"\"\\n',\n+        )\n+        violations = find_adr_reference_violations(root, include_docs=False)\n+        self.assertEqual(len(violations), 1)\n+        self.assertEqual(violations[0].namespace, \"SADR\")\n+        self.assertEqual(violations[0].number, \"0042\")\n+        self.assertEqual(violations[0].expected_path, \"docs/adr/SADR-0042-*.md\")\n+\n+    def test_target_adr_and_platform_sadr_may_reuse_a_number(self) -> None:\n+        root = self._make_repo(\n+            adr_names=[\"0042-target-decision.md\", \"SADR-0042-platform-decision.md\"],\n+            script_body='\"\"\"Target ADR 0042 and platform SADR-0042 are distinct.\"\"\"\\n',\n+        )\n+        self.assertEqual(\n+            find_adr_reference_violations(root, include_docs=False), []\n+        )\n+\n     def test_uncited_adr_is_allowed(self) -> None:\n         # An ADR exists but nothing cites it -> not a violation. The converse\n         # (every ADR must be cited) is explicitly NOT required.\ndiff --git a/tests/test_sadr_install_migration.py b/tests/test_sadr_install_migration.py\nnew file mode 100644\nindex 0000000000000000000000000000000000000000..a6398c81377f00c09aa2cb684980b070cfaf714c\n--- /dev/null\n+++ b/tests/test_sadr_install_migration.py\n@@ -0,0 +1,56 @@\n+\"\"\"SADR installation and collision-safe legacy upgrade behavior.\"\"\"\n+\n+from __future__ import annotations\n+\n+import shutil\n+import tempfile\n+import unittest\n+from pathlib import Path\n+\n+import shiki_test_support  # noqa: F401  (path bootstrap)\n+\n+import shiki_installer\n+from tests.test_adr_references import find_adr_reference_violations\n+\n+\n+PRODUCT_NUMBERS = tuple(range(13, 19))\n+\n+\n+class FreshSadrInstallTests(unittest.TestCase):\n+    def setUp(self) -> None:\n+        self.tmp = Path(tempfile.mkdtemp(prefix=\"shiki-sadr-fresh-\"))\n+        self.addCleanup(lambda: shutil.rmtree(self.tmp, ignore_errors=True))\n+        self.target = self.tmp / \"target\"\n+        self.adr_dir = self.target / \"docs\" / \"adr\"\n+        self.adr_dir.mkdir(parents=True)\n+        self.product_bytes = {\n+            f\"{number:04d}-product-decision.md\": (\n+                f\"# Product ADR {number:04d}\\n\\nTarget-owned bytes {number}.\\n\"\n+            ).encode()\n+            for number in PRODUCT_NUMBERS\n+        }\n+        for name, content in self.product_bytes.items():\n+            (self.adr_dir / name).write_bytes(content)\n+\n+    def test_fresh_install_preserves_product_adrs_and_installs_all_sadrs(self) -> None:\n+        shiki_installer.install_template(self.target, force=False, validate=True)\n+\n+        for name, expected in self.product_bytes.items():\n+            self.assertEqual((self.adr_dir / name).read_bytes(), expected)\n+\n+        sadr_numbers = {\n+            path.name.split(\"-\", 2)[1]\n+            for path in self.adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\")\n+        }\n+        self.assertEqual(sadr_numbers, {f\"{number:04d}\" for number in range(1, 20)})\n+\n+        numeric_records = {\n+            path.name\n+            for path in self.adr_dir.glob(\"[0-9][0-9][0-9][0-9]-*.md\")\n+        }\n+        self.assertEqual(numeric_records, set(self.product_bytes))\n+        self.assertEqual(find_adr_reference_violations(self.target), [])\n+\n+\n+if __name__ == \"__main__\":\n+    unittest.main()\n\nexec\n/bin/zsh -lc 'python3 -m unittest tests.test_adr_references -v' in /private/tmp/shiki-adr-collision.pa0KwY/.worktrees/shiki-t-20260807t073307221254z-2d22d654-introduc\n succeeded in 0ms:\ntest_ambiguous_when_two_documents_share_a_number (tests.test_adr_references.FixtureAdrReferences.test_ambiguous_when_two_documents_share_a_number) ... ok\ntest_fails_when_citation_has_no_document (tests.test_adr_references.FixtureAdrReferences.test_fails_when_citation_has_no_document) ... ok\ntest_passes_when_citation_resolves (tests.test_adr_references.FixtureAdrReferences.test_passes_when_citation_resolves) ... ok\ntest_target_adr_and_platform_sadr_may_reuse_a_number (tests.test_adr_references.FixtureAdrReferences.test_target_adr_and_platform_sadr_may_reuse_a_number) ... ok\ntest_target_adr_does_not_satisfy_missing_platform_sadr (tests.test_adr_references.FixtureAdrReferences.test_target_adr_does_not_satisfy_missing_platform_sadr) ... ok\ntest_uncited_adr_is_allowed (tests.test_adr_references.FixtureAdrReferences.test_uncited_adr_is_allowed) ... ok\ntest_carried_records_are_indexed (tests.test_adr_references.RealRepoAdrReferences.test_carried_records_are_indexed) ... ok\ntest_every_cited_adr_resolves (tests.test_adr_references.RealRepoAdrReferences.test_every_cited_adr_resolves) ... ok\ntest_platform_tree_has_only_sadr_0001_through_0019 (tests.test_adr_references.RealRepoAdrReferences.test_platform_tree_has_only_sadr_0001_through_0019) ... ok\ntest_scan_actually_reads_scripts (tests.test_adr_references.RealRepoAdrReferences.test_scan_actually_reads_scripts) ... ok\n\n----------------------------------------------------------------------\nRan 10 tests in 0.037s\n\nOK\n\nexec\n/bin/zsh -lc \"python3 -m unittest discover -s tests -p 'test_sadr_install_migration.py' -v\" in /private/tmp/shiki-adr-collision.pa0KwY/.worktrees/shiki-t-20260807t073307221254z-2d22d654-introduc\ndiff --git a/.codex/skills/shiki/SKILL.md b/.codex/skills/shiki/SKILL.md\nindex c8e2e85d9d75d49cc4f2d6286ee22659c4e53e75..c392c28442311b8851ede2cf66af14011d8038d4\n--- a/.codex/skills/shiki/SKILL.md\n+++ b/.codex/skills/shiki/SKILL.md\n@@ -14,7 +14,7 @@\n 1. GitHub Issues, Pull Requests, Checks, Reviews, comments, and merge evidence are the operational source of truth.\n 2. The repository-local `.shiki/` mirror records Goals, PRDs, plans, Task DAGs, contracts, locks, ledger entries, CCA verdicts, repair packets, reports, and handoffs.\n 3. `CONTEXT.md` defines Shiki domain language and glossary decisions.\n-4. `docs/adr/` records hard-to-reverse platform decisions.\n+4. `docs/adr/SADR-NNNN-*.md` records Shiki platform decisions; `docs/adr/NNNN-*.md` is reserved for target ADRs.\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n@@ -69,7 +69,7 @@\n \n ## Responsibilities\n \n-- Claude Code implements and repairs by default (`shiki runner claude`, ADR 0008); Codex implements and repairs tasks explicitly assigned to `codex`.\n+- Claude Code implements and repairs by default (`shiki runner claude`, SADR-0008); Codex implements and repairs tasks explicitly assigned to `codex`.\n - Claude Code Action can act as GitHub-side CCA or reviewer.\n - CCA judges completion.\n - MergeGate authorizes state transitions and merge readiness.\ndiff --git a/.github/prompts/cca-completion-check.md b/.github/prompts/cca-completion-check.md\nindex ed203ad5f302ecc81372d8ab27d2b1232ca97497..3137954883be3c4657b40ea1a1253b0dc4b4ef69\n--- a/.github/prompts/cca-completion-check.md\n+++ b/.github/prompts/cca-completion-check.md\n@@ -15,7 +15,7 @@\n 1. GitHub Issues, Pull Requests, Checks, Reviews, comments, and merge evidence are the operational source of truth.\n 2. The repository-local `.shiki/` mirror records Goals, PRDs, plans, Task DAGs, contracts, locks, ledger entries, CCA verdicts, repair packets, reports, and handoffs.\n 3. `CONTEXT.md` defines Shiki domain language and glossary decisions.\n-4. `docs/adr/` records hard-to-reverse platform decisions.\n+4. `docs/adr/SADR-NNNN-*.md` records Shiki platform decisions; `docs/adr/NNNN-*.md` is reserved for target ADRs.\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \ndiff --git a/AGENTS.md b/AGENTS.md\nindex 7f041100d49c2a7525f51cb0f5d696a4a6d1c78c..ddd7fe4d489f2c4c3ce82b51c84934019a7616b4\n--- a/AGENTS.md\n+++ b/AGENTS.md\n@@ -40,7 +40,7 @@\n 1. GitHub Issues, Pull Requests, Checks, Reviews, comments, and merge evidence are the operational source of truth.\n 2. The repository-local `.shiki/` mirror records Goals, PRDs, plans, Task DAGs, contracts, locks, ledger entries, CCA verdicts, repair packets, reports, and handoffs.\n 3. `CONTEXT.md` defines Shiki domain language and glossary decisions.\n-4. `docs/adr/` records hard-to-reverse platform decisions.\n+4. `docs/adr/SADR-NNNN-*.md` records Shiki platform decisions; `docs/adr/NNNN-*.md` is reserved for target ADRs.\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \ndiff --git a/CLAUDE.md b/CLAUDE.md\nindex 98a932e7841a632866fcd53b0bad0e3af3711d3d..83e001d4c15ed0a421666dc68da548ecb56748df\n--- a/CLAUDE.md\n+++ b/CLAUDE.md\n@@ -10,7 +10,7 @@\n 1. GitHub Issues, Pull Requests, Checks, Reviews, comments, and merge evidence are the operational source of truth.\n 2. The repository-local `.shiki/` mirror records Goals, PRDs, plans, Task DAGs, contracts, locks, ledger entries, CCA verdicts, repair packets, reports, and handoffs.\n 3. `CONTEXT.md` defines Shiki domain language and glossary decisions.\n-4. `docs/adr/` records hard-to-reverse platform decisions.\n+4. `docs/adr/SADR-NNNN-*.md` records Shiki platform decisions; `docs/adr/NNNN-*.md` is reserved for target ADRs.\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \ndiff --git a/SYSTEM_PROMPT.md b/SYSTEM_PROMPT.md\nindex aaaea45e3a896c3a44ba89eec8bb84325ecf6c62..42087a9325dfaa48de71b8319017175689166d2e\n--- a/SYSTEM_PROMPT.md\n+++ b/SYSTEM_PROMPT.md\n@@ -26,7 +26,7 @@\n 1. GitHub Issues, Pull Requests, Checks, Reviews, comments, and merge evidence are the operational source of truth.\n 2. The repository-local `.shiki/` mirror records Goals, PRDs, plans, Task DAGs, contracts, locks, ledger entries, CCA verdicts, repair packets, reports, and handoffs.\n 3. `CONTEXT.md` defines Shiki domain language and glossary decisions.\n-4. `docs/adr/` records hard-to-reverse platform decisions.\n+4. `docs/adr/SADR-NNNN-*.md` records Shiki platform decisions; `docs/adr/NNNN-*.md` is reserved for target ADRs.\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \ndiff --git a/docs/adr/0001-keep-shiki-as-platform-name.md b/docs/adr/SADR-0001-keep-shiki-as-platform-name.md\nindex 20a9637c3b9d8c97de7ba8db7cfae4d82d9a7dba..8b685439959aada1b221c112f0ac3c51ca8a1695\n--- a/docs/adr/0001-keep-shiki-as-platform-name.md\n+++ b/docs/adr/SADR-0001-keep-shiki-as-platform-name.md\n@@ -1,3 +1,3 @@\n-# Keep Shiki As The Platform Name\n+# SADR-0001: Keep Shiki As The Platform Name\n \n Shiki remains the product name for the GitHub-native agentic engineering control plane. We considered more descriptive names such as MergePilot, MergeGate, and Kiroku, but kept Shiki because it carries the existing goal-seeking framework identity while remaining independent of Hermes, Codex, Claude Code, or any single runtime.\ndiff --git a/docs/adr/0002-use-agents-as-shared-constitution.md b/docs/adr/SADR-0002-use-agents-as-shared-constitution.md\nindex 73de30ecb078120eac540d9bc51855e3cd48d0e3..c5486182bf06fca36ea508496b013c67302fd989\n--- a/docs/adr/0002-use-agents-as-shared-constitution.md\n+++ b/docs/adr/SADR-0002-use-agents-as-shared-constitution.md\n@@ -1,3 +1,3 @@\n-# Use AGENTS As The Shared Agent Constitution\n+# SADR-0002: Use AGENTS As The Shared Agent Constitution\n \n Shiki uses `AGENTS.md` as the runtime-neutral constitution and `CLAUDE.md` as a thin Claude Code wrapper. This keeps Codex, Claude Code, Hermes Runner, and future Agent Runtimes aligned on the same GitHub-first MergeGate rules while still allowing runtime-specific instructions where needed.\ndiff --git a/docs/adr/0003-require-skill-gate-for-mergegate-readiness.md b/docs/adr/SADR-0003-require-skill-gate-for-mergegate-readiness.md\nindex 424be0c9af4c8f0136f1fde68a468c47ff84e764..7fc337987c7df9792f75f181dfcbed75fe5ffff2\n--- a/docs/adr/0003-require-skill-gate-for-mergegate-readiness.md\n+++ b/docs/adr/SADR-0003-require-skill-gate-for-mergegate-readiness.md\n@@ -1,3 +1,3 @@\n-# Require Skill Gate For MergeGate Readiness\n+# SADR-0003: Require Skill Gate For MergeGate Readiness\n \n Shiki requires relevant engineering skills to be invoked before execution when their triggers apply. This makes skills part of MergeGate readiness rather than optional prompt style, because Shiki depends on repeatable planning, issue decomposition, TDD, diagnosis, triage, and architecture review before autonomous or parallel agent work can be trusted.\ndiff --git a/docs/adr/0004-use-github-first-with-shiki-mirror.md b/docs/adr/SADR-0004-use-github-first-with-shiki-mirror.md\nindex d0a3a9174dc646a945a9d325954ffaf00c352040..a79c54ae9ee8eed2a0524192b864da4e22a92d3f\n--- a/docs/adr/0004-use-github-first-with-shiki-mirror.md\n+++ b/docs/adr/SADR-0004-use-github-first-with-shiki-mirror.md\n@@ -1,4 +1,4 @@\n-# ADR 0004: Use GitHub First With A Repository-Local Shiki Mirror\n+# SADR-0004: Use GitHub First With A Repository-Local Shiki Mirror\n \n ## Status\n \ndiff --git a/docs/adr/0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md b/docs/adr/SADR-0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\nindex b339baf49b89d9290e79c3d51b36e7b2bde3bd95..c30a0d8b1fde0224d0ac7820e7870dccf721580d\n--- a/docs/adr/0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\n+++ b/docs/adr/SADR-0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\n@@ -1,4 +1,4 @@\n-# ADR 0005: Use Codex As Front And Claude Code Action As The GitHub Runtime\n+# SADR-0005: Use Codex As Front And Claude Code Action As The GitHub Runtime\n \n ## Status\n \ndiff --git a/docs/adr/0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md b/docs/adr/SADR-0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\nindex 4bcae9d1e52f0a37b8e9fbafa5cd14b6ee1993bd..7a38538a6ae9929567f0ade67e7ea6a60d0b3f7e\n--- a/docs/adr/0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\n+++ b/docs/adr/SADR-0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\n@@ -1,4 +1,4 @@\n-# ADR 0006: Separate Codex Implementation, CCA Completion, And MergeGate Authorization\n+# SADR-0006: Separate Codex Implementation, CCA Completion, And MergeGate Authorization\n \n ## Status\n \ndiff --git a/docs/adr/0007-packaging-and-release.md b/docs/adr/SADR-0007-packaging-and-release.md\nindex 79b84ddb97167372b44641e5b7860e400ce80822..023cd57f8011aa6e6d2ebf612b7c35a7a82c00d4\n--- a/docs/adr/0007-packaging-and-release.md\n+++ b/docs/adr/SADR-0007-packaging-and-release.md\n@@ -1,4 +1,4 @@\n-# ADR 0007: Packaging And Release For Shiki\n+# SADR-0007: Packaging And Release For Shiki\n \n ## Status\n \ndiff --git a/docs/adr/0008-use-claude-code-as-default-implementer.md b/docs/adr/SADR-0008-use-claude-code-as-default-implementer.md\nindex dedf4510b7c7799521422f622029563d373db031..a158a8d444d93764ec515a28133f7dde4af23579\n--- a/docs/adr/0008-use-claude-code-as-default-implementer.md\n+++ b/docs/adr/SADR-0008-use-claude-code-as-default-implementer.md\n@@ -1,4 +1,4 @@\n-# ADR 0008: Use Claude Code As The Default Implementer Runtime\n+# SADR-0008: Use Claude Code As The Default Implementer Runtime\n \n ## Status\n \ndiff --git a/docs/adr/0009-requirements-definition-and-spec-freeze.md b/docs/adr/SADR-0009-requirements-definition-and-spec-freeze.md\nindex 29b3f7871bef9203a8b4a831a65c39eb2f506e42..52e7dd69ad0aa4efe0b68c62678abbf7df02aaec\n--- a/docs/adr/0009-requirements-definition-and-spec-freeze.md\n+++ b/docs/adr/SADR-0009-requirements-definition-and-spec-freeze.md\n@@ -1,4 +1,4 @@\n-# ADR 0009: Merge Grill And Planning Into Requirements Definition With Spec Freeze\n+# SADR-0009: Merge Grill And Planning Into Requirements Definition With Spec Freeze\n \n ## Status\n \ndiff --git a/docs/adr/0010-external-ai-guardian-review.md b/docs/adr/SADR-0010-external-ai-guardian-review.md\nindex e6108bbe8bb4e73e0ff4684c8f78001a80438c4e..e690d4eddd270e683d709c9b679c5859e71eeaaa\n--- a/docs/adr/0010-external-ai-guardian-review.md\n+++ b/docs/adr/SADR-0010-external-ai-guardian-review.md\n@@ -1,4 +1,4 @@\n-# ADR 0010: External AI Guardian Review As A First-Class Approval Authority\n+# SADR-0010: External AI Guardian Review As A First-Class Approval Authority\n \n ## Status\n \ndiff --git a/docs/adr/0011-autonomous-quality-gate-evidence.md b/docs/adr/SADR-0011-autonomous-quality-gate-evidence.md\nindex bca0246c5d46e44afbde15486a75eec425fc02f5..9c93eac16d8c3a6d4b11bd7224e3955102cd7db4\n--- a/docs/adr/0011-autonomous-quality-gate-evidence.md\n+++ b/docs/adr/SADR-0011-autonomous-quality-gate-evidence.md\n@@ -1,4 +1,4 @@\n-# ADR 0011: Autonomous Quality-Gate Evidence \u2014 Deterministic Facts And An Independent Verifier\n+# SADR-0011: Autonomous Quality-Gate Evidence \u2014 Deterministic Facts And An Independent Verifier\n \n ## Status\n \ndiff --git a/docs/adr/0012-autonomous-loop-closeout-to-main.md b/docs/adr/SADR-0012-autonomous-loop-closeout-to-main.md\nindex 637fad6dde4989c9c2bf0f81f676d8d29991123c..ec7d85f81030a676bcbba28b43eb365422a36eac\n--- a/docs/adr/0012-autonomous-loop-closeout-to-main.md\n+++ b/docs/adr/SADR-0012-autonomous-loop-closeout-to-main.md\n@@ -1,4 +1,4 @@\n-# 0012 \u2014 Autonomous loop pushes goal completion to main via a closeout PR\n+# SADR-0012 \u2014 Autonomous loop pushes goal completion to main via a closeout PR\n \n - Status: accepted\n - Date: 2026-06-18\ndiff --git a/docs/adr/0013-bootstrap-grants-actions-approve-pull-requests.md b/docs/adr/SADR-0013-bootstrap-grants-actions-approve-pull-requests.md\nindex b6c69d64f1c5ced346b88d6859586715b3b5bd0e..a793e7277d67ab8ea56286fec289ca689825a17d\n--- a/docs/adr/0013-bootstrap-grants-actions-approve-pull-requests.md\n+++ b/docs/adr/SADR-0013-bootstrap-grants-actions-approve-pull-requests.md\n@@ -1,4 +1,4 @@\n-# ADR 0013: Bootstrap Grants GitHub Actions Permission To Create And Approve Pull Requests\n+# SADR-0013: Bootstrap Grants GitHub Actions Permission To Create And Approve Pull Requests\n \n ## Status\n \ndiff --git a/docs/adr/0014-codex-app-external-ai-guardian-ui-adapter.md b/docs/adr/SADR-0014-codex-app-external-ai-guardian-ui-adapter.md\nindex 238db06279334caa341c117362df9271382a7fcd..65f12e9939321e1b3e389a34c822fc1549e5155d\n--- a/docs/adr/0014-codex-app-external-ai-guardian-ui-adapter.md\n+++ b/docs/adr/SADR-0014-codex-app-external-ai-guardian-ui-adapter.md\n@@ -1,4 +1,4 @@\n-# ADR 0014: Use Codex App As The External AI Guardian UI Adapter\n+# SADR-0014: Use Codex App As The External AI Guardian UI Adapter\n \n ## Status\n \ndiff --git a/docs/adr/0015-contract-approval-before-dispatch.md b/docs/adr/SADR-0015-contract-approval-before-dispatch.md\nindex fad6c656741a20a7684ba16080a7db159f63a52f..7bdd760bb3e82eee4e1410f45c783b03b638d456\n--- a/docs/adr/0015-contract-approval-before-dispatch.md\n+++ b/docs/adr/SADR-0015-contract-approval-before-dispatch.md\n@@ -1,4 +1,4 @@\n-# ADR 0015: Approve The Task Contract Before Dispatch, Not The Diff After It\n+# SADR-0015: Approve The Task Contract Before Dispatch, Not The Diff After It\n \n ## Status\n \ndiff --git a/docs/adr/0016-locks-bound-product-paths.md b/docs/adr/SADR-0016-locks-bound-product-paths.md\nindex b5e850f3f1f7f4ef6e7f9d1763a29a51bd300c45..332cc17313498404f41a145291e38447b5de0dc4\n--- a/docs/adr/0016-locks-bound-product-paths.md\n+++ b/docs/adr/SADR-0016-locks-bound-product-paths.md\n@@ -1,4 +1,4 @@\n-# ADR 0016: Locks bound product paths; the Shiki mirror is bound by record identity\n+# SADR-0016: Locks bound product paths; the Shiki mirror is bound by record identity\n \n ## Status\n \ndiff --git a/docs/adr/0017-bookkeeping-closeouts-do-not-inherit-risk.md b/docs/adr/SADR-0017-bookkeeping-closeouts-do-not-inherit-risk.md\nindex 554e3a0d526a72ca067108c5464cc68c3edcf174..83115f69f1b1b48d544c9b0157f377b91cb53abf\n--- a/docs/adr/0017-bookkeeping-closeouts-do-not-inherit-risk.md\n+++ b/docs/adr/SADR-0017-bookkeeping-closeouts-do-not-inherit-risk.md\n@@ -1,4 +1,4 @@\n-# ADR 0017: A Bookkeeping Closeout Does Not Inherit Its Task's Risk\n+# SADR-0017: A Bookkeeping Closeout Does Not Inherit Its Task's Risk\n \n ## Status\n \ndiff --git a/docs/adr/0018-carried-guardian-approval-across-a-proven-base-sync.md b/docs/adr/SADR-0018-carried-guardian-approval-across-a-proven-base-sync.md\nindex 22358562da390ed8b45cd600da08a43d378b4680..38da0fd95102c3496c3219ce2319fce696a2532a\n--- a/docs/adr/0018-carried-guardian-approval-across-a-proven-base-sync.md\n+++ b/docs/adr/SADR-0018-carried-guardian-approval-across-a-proven-base-sync.md\n@@ -1,4 +1,4 @@\n-# ADR 0018: Carried Guardian Approval Across A Proven Base Sync\n+# SADR-0018: Carried Guardian Approval Across A Proven Base Sync\n \n ## Status\n \ndiff --git a/docs/adr/README.md b/docs/adr/README.md\nindex c70e057a4520c36e042ab68c53cd4a21ef709b71..5da43856836e2ce6a150f8127766e2f88355ab4b\n--- a/docs/adr/README.md\n+++ b/docs/adr/README.md\n@@ -1,40 +1,41 @@\n-# Architecture Decision Records\n+# Decision Record Namespaces\n \n-This directory holds Shiki's Architecture Decision Records (ADRs): durable\n-records of hard-to-reverse platform decisions. ADRs are part of the Shiki source\n-of truth alongside GitHub state, the `.shiki/` mirror, and `CONTEXT.md`.\n+This directory holds two independent decision namespaces:\n \n-## How ADRs Work\n+- Shiki Architecture Decision Records (SADRs), named\n+  `SADR-NNNN-<short-slug>.md`, record hard-to-reverse Shiki platform decisions.\n+- Target Architecture Decision Records (ADRs), named\n+  `NNNN-<short-slug>.md`, belong to the target product.\n \n-- New ADRs are added as `docs/adr/NNNN-<short-slug>.md`, where `NNNN` is the next\n-  zero-padded sequence number.\n-- The **canonical list of ADRs is the set of files matching `docs/adr/*.md`** in\n-  this directory. Browse `docs/adr/*.md` for the authoritative, current set.\n-- Use [`template.md`](template.md) as the starting point for a new ADR.\n-- Record an ADR when a decision is hard to reverse, surprising without context,\n-  and a real tradeoff. Do not write an ADR for routine or easily reversible\n-  choices.\n+The same four-digit number may appear once in each namespace. A platform\n+`SADR-NNNN` citation resolves only to `SADR-NNNN-*.md`; a target `ADR NNNN`\n+citation resolves only to `NNNN-*.md`.\n \n-Using the `docs/adr/*.md` glob as the source of truth means other PRs can add an\n-ADR without editing this index file, avoiding merge contention on a\n-hand-maintained enumeration.\n+## How Decision Records Work\n \n-## Current ADRs (snapshot)\n-\n-The list below is a convenience snapshot and may lag behind the directory. When\n-in doubt, list `docs/adr/*.md`.\n+- New Shiki platform decisions use the next zero-padded SADR number.\n+- New target product decisions use the target's own ADR sequence.\n+- The canonical list is the files in `docs/adr/`; no hand-maintained index is\n+  authoritative.\n+- Use [`template.md`](template.md) as the starting point and choose the filename\n+  namespace that owns the decision.\n+- Record a decision when it is hard to reverse, surprising without context, and\n+  a real tradeoff. Do not write a decision record for routine or easily\n+  reversible choices.\n \n-- [0001 \u2014 Keep Shiki As The Platform Name](0001-keep-shiki-as-platform-name.md)\n-- [0002 \u2014 Use AGENTS As The Shared Agent Constitution](0002-use-agents-as-shared-constitution.md)\n-- [0003 \u2014 Require Skill Gate For MergeGate Readiness](0003-require-skill-gate-for-mergegate-readiness.md)\n-- [0004 \u2014 Use GitHub First With A Repository-Local Shiki Mirror](0004-use-github-first-with-shiki-mirror.md)\n-- [0005 \u2014 Use Codex As Front And Claude Code Action As The GitHub Runtime](0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md)\n-- [0006 \u2014 Separate Codex Implementation, CCA Completion, And MergeGate Authorization](0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md)\n+Using the directory as the source of truth lets independent changes add records\n+without contending on an enumerated index.\n \n-## Adding A New ADR\n+## Adding A Shiki SADR\n \n-1. Copy `template.md` to `docs/adr/NNNN-<short-slug>.md`.\n+1. Copy `template.md` to `docs/adr/SADR-NNNN-<short-slug>.md`.\n 2. Fill in the title, status, context, decision, and consequences.\n-3. Set the status to `Proposed`, then `Accepted` once the decision is approved.\n-4. Open a PR following `CONTRIBUTING.md`. You do not need to edit this index for\n-   the ADR to be discoverable via `docs/adr/*.md`.\n+3. Set the status to `Proposed`, then `Accepted` once the proper authority has\n+   approved the decision.\n+4. Open a PR following `CONTRIBUTING.md`.\n+\n+## Adding A Target ADR\n+\n+1. Copy `template.md` to `docs/adr/NNNN-<short-slug>.md` using the target's own\n+   sequence.\n+2. Cite it as `ADR NNNN`; do not prefix its filename with `SADR-`.\ndiff --git a/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md b/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\nnew file mode 100644\nindex 0000000000000000000000000000000000000000..7455ce1f3bed02ddddb851ac21a1d1a648c9aeba\n--- /dev/null\n+++ b/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\n@@ -0,0 +1,55 @@\n+# SADR-0019: Separate Shiki Platform SADRs From Target ADRs\n+\n+## Status\n+\n+Accepted\n+\n+## Context\n+\n+Shiki installs its platform decision records into `docs/adr/` in every target\n+repository. Shiki and its targets previously used the same numeric\n+`NNNN-<slug>.md` namespace. A target that already owned ADRs 0013 through 0018\n+therefore acquired two records for each number during installation, making an\n+`ADR NNNN` citation ambiguous. Renumbering target decisions would rewrite target\n+history, while renumbering Shiki decisions per target would destroy their stable\n+platform identity.\n+\n+Upgrades also need to remove the former Shiki-owned numeric paths without ever\n+mistaking a target-owned ADR for platform content. The install stamp already\n+records exact shipped paths and content digests, so it can prove ownership only\n+when both still match.\n+\n+## Decision\n+\n+Shiki platform decisions use the `SADR` namespace:\n+\n+- records are named `docs/adr/SADR-NNNN-<slug>.md`;\n+- live platform citations use `SADR-NNNN`;\n+- target product decisions retain `docs/adr/NNNN-<slug>.md` and `ADR NNNN`;\n+- the reference validator resolves the two namespaces independently, so the same\n+  four-digit number may exist once in each namespace.\n+\n+Before a forced upgrade writes anything, the installer inventories the exact\n+former numeric Shiki ADR paths. It may delete a legacy path only when the\n+existing install stamp contains that exact path and its recorded SHA-256 digest\n+matches the file currently on disk. All ownership blockers are reported\n+together and abort the upgrade before its first write. A non-forced install\n+never performs this cleanup.\n+\n+Historical `.shiki/` evidence remains unchanged because it records the namespace\n+that existed when that evidence was produced.\n+\n+## Consequences\n+\n+- Targets may use any ADR number without colliding with Shiki's platform SADRs.\n+- Fresh installs preserve target ADR bytes and install a stable SADR set.\n+- Forced upgrades fail closed when legacy ownership cannot be proven, and a\n+  successful upgrade refreshes the stamp without removed legacy paths.\n+- Current governance, packaging, workflow, prompt, script, and skill references\n+  must move with the platform records.\n+\n+## Alternatives Considered\n+\n+- Collision-only refusal leaves affected targets permanently unupgradable.\n+- Renumbering target ADRs mutates target-owned history.\n+- Per-target Shiki renumbering makes platform citations non-portable.\ndiff --git a/docs/adr/template.md b/docs/adr/template.md\nindex a92a97daddd0b00ab9dd626cc8d86942da2eafed..17dd79175b5fee2105935557f691b40cf9492942\n--- a/docs/adr/template.md\n+++ b/docs/adr/template.md\n@@ -1,14 +1,14 @@\n-# NNNN: <Short Decision Title>\n+# <SADR-NNNN or NNNN>: <Short Decision Title>\n \n ## Status\n \n-Proposed | Accepted | Superseded by ADR-NNNN\n+Proposed | Accepted | Superseded by SADR-NNNN or target ADR NNNN\n \n ## Context\n \n What is the situation, constraint, or force that requires a decision? Reference\n-relevant domain terms (`CONTEXT.md`), prior ADRs, and code reality. State the\n-problem, not the solution.\n+relevant domain terms (`CONTEXT.md`), prior Shiki SADRs or target ADRs, and code\n+reality. State the problem, not the solution.\n \n ## Decision\n \ndiff --git a/docs/agents/domain.md b/docs/agents/domain.md\nindex 258da8ea81e87ac0a6b0b5b5e5c2fde980eaabb7..67aaa6f4b7d0a5aa4d495ce6279073333f7798cb\n--- a/docs/agents/domain.md\n+++ b/docs/agents/domain.md\n@@ -5,7 +5,9 @@\n ## Layout\n \n - `CONTEXT.md` defines Shiki language. Keep it implementation-free.\n-- `docs/adr/` records hard-to-reverse decisions.\n+- `docs/adr/SADR-NNNN-*.md` records Shiki platform decisions.\n+- `docs/adr/NNNN-*.md` records target product decisions without sharing the\n+  SADR namespace.\n - `AGENTS.md` is the runtime-neutral agent constitution.\n - `CLAUDE.md` is the Claude Code wrapper around `AGENTS.md`.\n - `.shiki/` is the Target Repository mirror for Goals, PRDs, plans, Task DAGs, contracts, locks, worktree records, ledger entries, CCA verdicts, repair packets, reports, and handoffs.\ndiff --git a/docs/agents/implementation-policy.md b/docs/agents/implementation-policy.md\nindex c754144a9fb5bf4a38aa82a60e1670c59e626b44..3b059d128ce1338ee49eadec0dbd58f3cc2cc594\n--- a/docs/agents/implementation-policy.md\n+++ b/docs/agents/implementation-policy.md\n@@ -27,7 +27,7 @@\n 1. GitHub Issues, Pull Requests, Checks, Reviews, comments, and merge evidence are the operational source of truth.\n 2. The repository-local `.shiki/` mirror records Goals, PRDs, plans, Task DAGs, contracts, locks, ledger entries, CCA verdicts, repair packets, reports, and handoffs.\n 3. `CONTEXT.md` defines Shiki domain language and glossary decisions.\n-4. `docs/adr/` records hard-to-reverse platform decisions.\n+4. `docs/adr/SADR-NNNN-*.md` records Shiki platform decisions; `docs/adr/NNNN-*.md` is reserved for target ADRs.\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \ndiff --git a/scripts/shiki_contracts.py b/scripts/shiki_contracts.py\nindex 18076f0a70a1f8c9d4475a005bcccb1472a246c1..9d3f08dc1172b100c3a9b60a21ffdbae0f4dd44b\n--- a/scripts/shiki_contracts.py\n+++ b/scripts/shiki_contracts.py\n@@ -100,7 +100,7 @@\n     \"GitHub Issues, Pull Requests, Checks, Reviews, comments, and merge evidence are the operational source of truth.\",\n     \"The repository-local `.shiki/` mirror records Goals, PRDs, plans, Task DAGs, contracts, locks, ledger entries, CCA verdicts, repair packets, reports, and handoffs.\",\n     \"`CONTEXT.md` defines Shiki domain language and glossary decisions.\",\n-    \"`docs/adr/` records hard-to-reverse platform decisions.\",\n+    \"`docs/adr/SADR-NNNN-*.md` records Shiki platform decisions; `docs/adr/NNNN-*.md` is reserved for target ADRs.\",\n     \"Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\",\n )\n \ndiff --git a/tests/test_adr_references.py b/tests/test_adr_references.py\nindex 51706329c797d5dd9b47e082a333fa83b261d482..91ab76b724cb973435c45d53cab63793942267e9\n--- a/tests/test_adr_references.py\n+++ b/tests/test_adr_references.py\n@@ -1,4 +1,4 @@\n-\"\"\"Every ``ADR ####`` citation must resolve to exactly one decision record.\n+\"\"\"Every target ADR and Shiki SADR citation resolves in its own namespace.\n \n Background. ``scripts/mergegate_check.py`` justifies ``is_bookkeeping_closeout``\n with \"ADR 0017\" nine times and ``scripts/guardian_approval_signal.py`` cites\n@@ -11,11 +11,12 @@\n and the directory drifted with no signal.\n \n This is the same unbound-pair shape behind other silent failures: a reference\n-and its target with nothing asserting they agree. This test binds them. It scans\n-every ``scripts/*.py`` (plus ``docs/**/*.md`` and the root ``*.md`` governance\n-files, which are cheap) for the ``ADR ####`` pattern and asserts each cited\n-number resolves to exactly one ``docs/adr/####-*.md``. The converse is\n-deliberately NOT required: an ADR may exist without being cited.\n+and its target with nothing asserting they agree. This test binds them. Target\n+``ADR NNNN`` citations resolve only to ``docs/adr/NNNN-*.md``; Shiki\n+``SADR-NNNN`` citations resolve only to ``docs/adr/SADR-NNNN-*.md``. The scan\n+covers current governance docs, scripts, workflows, prompts, packaging metadata,\n+and repo-local Codex guidance while deliberately excluding historical\n+``.shiki`` evidence. The converse is not required: a record may be uncited.\n \n The suite is dependency free: standard-library ``unittest`` only.\n \"\"\"\n@@ -31,17 +32,18 @@\n \n REPO_ROOT = Path(__file__).resolve().parents[1]\n \n-# \"ADR 0016\" -- a four-digit citation. The trailing \\b keeps a five-digit token\n-# from being read as a truncated four-digit number.\n-_CITATION = re.compile(r\"\\bADR (\\d{4})\\b\")\n-# \"0016-locks-bound-product-paths.md\" -- the leading four digits name the record.\n-_ADR_FILENAME = re.compile(r\"^(\\d{4})-.*\\.md$\")\n+# ``ADR 0016`` / ``SADR-0016`` -- a namespace plus four-digit citation. The\n+# trailing ``\\b`` keeps a five-digit token from being read as a truncated one.\n+_CITATION = re.compile(r\"\\b(S?ADR)[ -](\\d{4})\\b\")\n+# Target ADRs retain the numeric filename; Shiki platform decisions use SADR.\n+_DECISION_FILENAME = re.compile(r\"^(?:(SADR)-)?(\\d{4})-.*\\.md$\")\n \n \n class Violation(NamedTuple):\n     \"\"\"A citation that does not resolve to exactly one decision record.\"\"\"\n \n     citing_file: str  # repo-relative path of the file that cites the ADR\n+    namespace: str  # \"ADR\" for target decisions; \"SADR\" for Shiki decisions\n     number: str  # the cited four-digit number, e.g. \"0016\"\n     line: int  # 1-based line of the citation\n     reason: str  # \"missing\" (zero matches) or \"ambiguous\" (two or more)\n@@ -49,44 +51,59 @@\n \n     @property\n     def expected_path(self) -> str:\n-        return f\"docs/adr/{self.number}-*.md\"\n+        prefix = \"SADR-\" if self.namespace == \"SADR\" else \"\"\n+        return f\"docs/adr/{prefix}{self.number}-*.md\"\n \n     def describe(self) -> str:\n         \"\"\"Name the citing file, the cited number, and the missing path.\"\"\"\n         if self.reason == \"missing\":\n             return (\n-                f\"{self.citing_file}:{self.line} cites ADR {self.number} \"\n+                f\"{self.citing_file}:{self.line} cites {self.namespace} {self.number} \"\n                 f\"but no document matches {self.expected_path}\"\n             )\n         joined = \", \".join(self.matches)\n         return (\n-            f\"{self.citing_file}:{self.line} cites ADR {self.number} which \"\n+            f\"{self.citing_file}:{self.line} cites {self.namespace} {self.number} which \"\n             f\"resolves to {len(self.matches)} documents (expected exactly one \"\n             f\"at {self.expected_path}): {joined}\"\n         )\n \n \n-def _adr_index(root: Path) -> dict[str, list[Path]]:\n-    \"\"\"Map every four-digit ADR number to the record files that claim it.\"\"\"\n-    index: dict[str, list[Path]] = {}\n+def _adr_index(root: Path) -> dict[tuple[str, str], list[Path]]:\n+    \"\"\"Map each decision namespace and number to the files that claim it.\"\"\"\n+    index: dict[tuple[str, str], list[Path]] = {}\n     adr_dir = root / \"docs\" / \"adr\"\n     if adr_dir.is_dir():\n         for path in sorted(adr_dir.glob(\"*.md\")):\n-            match = _ADR_FILENAME.match(path.name)\n+            match = _DECISION_FILENAME.match(path.name)\n             if match:\n-                index.setdefault(match.group(1), []).append(path)\n+                namespace = \"SADR\" if match.group(1) else \"ADR\"\n+                index.setdefault((namespace, match.group(2)), []).append(path)\n     return index\n \n \n def _citing_files(root: Path, include_docs: bool = True) -> list[Path]:\n-    \"\"\"Files scanned for citations: scripts/*.py, and cheaply the markdown.\"\"\"\n+    \"\"\"Current, shipped text surfaces scanned for decision citations.\"\"\"\n     files = sorted((root / \"scripts\").glob(\"*.py\"))\n+    files += sorted((root / \"scripts\").glob(\"*.sh\"))\n     if include_docs:\n         docs = root / \"docs\"\n         if docs.is_dir():\n             files += sorted(docs.rglob(\"*.md\"))\n         files += sorted(root.glob(\"*.md\"))\n-    return files\n+        pyproject = root / \"pyproject.toml\"\n+        if pyproject.is_file():\n+            files.append(pyproject)\n+        for relative in (\".github\", \".codex\"):\n+            surface = root / relative\n+            if surface.is_dir():\n+                files += sorted(\n+                    path\n+                    for path in surface.rglob(\"*\")\n+                    if path.is_file()\n+                    and path.suffix in {\".md\", \".py\", \".sh\", \".toml\", \".yaml\", \".yml\"}\n+                )\n+    return sorted(set(files))\n \n \n def find_adr_reference_violations(\n@@ -94,10 +111,9 @@\n ) -> list[Violation]:\n     \"\"\"Return every ``ADR ####`` citation under ``root`` that fails to resolve.\n \n-    A citation resolves when exactly one ``docs/adr/####-*.md`` matches its\n-    number. Zero matches is a \"missing\" violation; two or more is \"ambiguous\".\n-    Records that are never cited produce nothing -- the converse is not\n-    required.\n+    A citation resolves when exactly one record in the same namespace matches\n+    its number. Zero matches is a \"missing\" violation; two or more is\n+    \"ambiguous\". Records that are never cited produce nothing.\n     \"\"\"\n     root = Path(root)\n     index = _adr_index(root)\n@@ -110,13 +126,15 @@\n         rel = path.relative_to(root).as_posix()\n         for lineno, line in enumerate(text.splitlines(), start=1):\n             for match in _CITATION.finditer(line):\n-                number = match.group(1)\n-                found = index.get(number, [])\n+                namespace, number = match.groups()\n+                found = index.get((namespace, number), [])\n                 if len(found) == 1:\n                     continue\n                 reason = \"missing\" if not found else \"ambiguous\"\n                 rels = [p.relative_to(root).as_posix() for p in found]\n-                violations.append(Violation(rel, number, lineno, reason, rels))\n+                violations.append(\n+                    Violation(rel, namespace, number, lineno, reason, rels)\n+                )\n     return violations\n \n \n@@ -129,8 +147,8 @@\n         self.assertEqual(\n             violations,\n             [],\n-            \"Every 'ADR ####' citation must resolve to exactly one \"\n-            f\"docs/adr/####-*.md:\\n{detail}\",\n+            \"Every ADR/SADR citation must resolve exactly once in its own \"\n+            f\"docs/adr namespace:\\n{detail}\",\n         )\n \n     def test_scan_actually_reads_scripts(self) -> None:\n@@ -146,8 +164,21 @@\n         # The two records this Goal carried onto the default branch, and which\n         # the scripts cite most, must be present.\n         index = _adr_index(REPO_ROOT)\n-        self.assertIn(\"0016\", index)\n-        self.assertIn(\"0017\", index)\n+        self.assertIn((\"SADR\", \"0016\"), index)\n+        self.assertIn((\"SADR\", \"0017\"), index)\n+\n+    def test_platform_tree_has_only_sadr_0001_through_0019(self) -> None:\n+        adr_dir = REPO_ROOT / \"docs\" / \"adr\"\n+        sadrs = sorted(adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\"))\n+        self.assertEqual(\n+            {path.name.split(\"-\", 2)[1] for path in sadrs},\n+            {f\"{number:04d}\" for number in range(1, 20)},\n+        )\n+        self.assertEqual(\n+            list(adr_dir.glob(\"[0-9][0-9][0-9][0-9]-*.md\")),\n+            [],\n+            \"the Shiki platform tree must not retain legacy numeric SADR paths\",\n+        )\n \n \n class FixtureAdrReferences(unittest.TestCase):\n@@ -180,6 +211,26 @@\n         self.assertIn(\"9991\", message)  # cited number\n         self.assertIn(\"docs/adr/9991-*.md\", message)  # missing path\n \n+    def test_target_adr_does_not_satisfy_missing_platform_sadr(self) -> None:\n+        root = self._make_repo(\n+            adr_names=[\"0042-target-decision.md\"],\n+            script_body='\"\"\"Target ADR 0042 exists, but platform SADR-0042 does not.\"\"\"\\n',\n+        )\n+        violations = find_adr_reference_violations(root, include_docs=False)\n+        self.assertEqual(len(violations), 1)\n+        self.assertEqual(violations[0].namespace, \"SADR\")\n+        self.assertEqual(violations[0].number, \"0042\")\n+        self.assertEqual(violations[0].expected_path, \"docs/adr/SADR-0042-*.md\")\n+\n+    def test_target_adr_and_platform_sadr_may_reuse_a_number(self) -> None:\n+        root = self._make_repo(\n+            adr_names=[\"0042-target-decision.md\", \"SADR-0042-platform-decision.md\"],\n+            script_body='\"\"\"Target ADR 0042 and platform SADR-0042 are distinct.\"\"\"\\n',\n+        )\n+        self.assertEqual(\n+            find_adr_reference_violations(root, include_docs=False), []\n+        )\n+\n     def test_uncited_adr_is_allowed(self) -> None:\n         # An ADR exists but nothing cites it -> not a violation. The converse\n         # (every ADR must be cited) is explicitly NOT required.\ndiff --git a/tests/test_sadr_install_migration.py b/tests/test_sadr_install_migration.py\nnew file mode 100644\nindex 0000000000000000000000000000000000000000..a6398c81377f00c09aa2cb684980b070cfaf714c\n--- /dev/null\n+++ b/tests/test_sadr_install_migration.py\n@@ -0,0 +1,56 @@\n+\"\"\"SADR installation and collision-safe legacy upgrade behavior.\"\"\"\n+\n+from __future__ import annotations\n+\n+import shutil\n+import tempfile\n+import unittest\n+from pathlib import Path\n+\n+import shiki_test_support  # noqa: F401  (path bootstrap)\n+\n+import shiki_installer\n+from tests.test_adr_references import find_adr_reference_violations\n+\n+\n+PRODUCT_NUMBERS = tuple(range(13, 19))\n+\n+\n+class FreshSadrInstallTests(unittest.TestCase):\n+    def setUp(self) -> None:\n+        self.tmp = Path(tempfile.mkdtemp(prefix=\"shiki-sadr-fresh-\"))\n+        self.addCleanup(lambda: shutil.rmtree(self.tmp, ignore_errors=True))\n+        self.target = self.tmp / \"target\"\n+        self.adr_dir = self.target / \"docs\" / \"adr\"\n+        self.adr_dir.mkdir(parents=True)\n+        self.product_bytes = {\n+            f\"{number:04d}-product-decision.md\": (\n+                f\"# Product ADR {number:04d}\\n\\nTarget-owned bytes {number}.\\n\"\n+            ).encode()\n+            for number in PRODUCT_NUMBERS\n+        }\n+        for name, content in self.product_bytes.items():\n+            (self.adr_dir / name).write_bytes(content)\n+\n+    def test_fresh_install_preserves_product_adrs_and_installs_all_sadrs(self) -> None:\n+        shiki_installer.install_template(self.target, force=False, validate=True)\n+\n+        for name, expected in self.product_bytes.items():\n+            self.assertEqual((self.adr_dir / name).read_bytes(), expected)\n+\n+        sadr_numbers = {\n+            path.name.split(\"-\", 2)[1]\n+            for path in self.adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\")\n+        }\n+        self.assertEqual(sadr_numbers, {f\"{number:04d}\" for number in range(1, 20)})\n+\n+        numeric_records = {\n+            path.name\n+            for path in self.adr_dir.glob(\"[0-9][0-9][0-9][0-9]-*.md\")\n+        }\n+        self.assertEqual(numeric_records, set(self.product_bytes))\n+        self.assertEqual(find_adr_reference_violations(self.target), [])\n+\n+\n+if __name__ == \"__main__\":\n+    unittest.main()\n\n exited 1 in 400ms:\nERROR\n\n======================================================================\nERROR: test_fresh_install_preserves_product_adrs_and_installs_all_sadrs (test_sadr_install_migration.FreshSadrInstallTests.test_fresh_install_preserves_product_adrs_and_installs_all_sadrs)\n----------------------------------------------------------------------\nTraceback (most recent call last):\n  File \"/private/tmp/shiki-adr-collision.pa0KwY/.worktrees/shiki-t-20260807t073307221254z-2d22d654-introduc/tests/test_sadr_install_migration.py\", line 36, in test_fresh_install_preserves_product_adrs_and_installs_all_sadrs\n    shiki_installer.install_template(self.target, force=False, validate=True)\n    ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^\n  File \"/private/tmp/shiki-adr-collision.pa0KwY/.worktrees/shiki-t-20260807t073307221254z-2d22d654-introduc/scripts/shiki_installer.py\", line 290, in install_template\n    validate_target_shiki(target)\n    ~~~~~~~~~~~~~~~~~~~~~^^^^^^^^\n  File \"/private/tmp/shiki-adr-collision.pa0KwY/.worktrees/shiki-t-20260807t073307221254z-2d22d654-introduc/scripts/shiki_process.py\", line 89, in validate_target_shiki\n    run([\"python3\", \"scripts/validate_shiki.py\"], cwd=target)\n    ~~~^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^\n  File \"/private/tmp/shiki-adr-collision.pa0KwY/.worktrees/shiki-t-20260807t073307221254z-2d22d654-introduc/scripts/shiki_process.py\", line 66, in run\n    raise ShikiError(f\"{command} failed\\n{process.stderr.strip()}\")\nshiki_process.ShikiError: python3 scripts/validate_shiki.py failed\nERROR: /private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-fckkxir8/target/.claude/commands/shiki.md: missing canonical source-of-truth block\n\n----------------------------------------------------------------------\nRan 1 test in 0.374s\n\nFAILED (errors=1)\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-fckkxir8/target/bin/shiki\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-fckkxir8/target/AGENTS.md\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-fckkxir8/target/CLAUDE.md\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-fckkxir8/target/CONTEXT.md\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-fckkxir8/target/SYSTEM_PROMPT.md\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-fckkxir8/target/.gitignore\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-fckkxir8/target/.claude/commands/shiki.md\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-fckkxir8/target/.codex/skills/shiki/SKILL.md\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-fckkxir8/target/.shiki/migrations/state.json\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-fckkxir8/target/.shiki/guardian-policy.json\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-fckkxir8/target/.shiki/config.yaml\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-fckkxir8/target/.shiki/schemas/runner-record.schema.json\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-fckkxir8/target/.shiki/schemas/report.schema.json\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-fckkxir8/target/.shiki/schemas/dispatch-guard.schema.json\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-fckkxir8/target/.shiki/schemas/external-ai-guardian-review-packet.schema.json\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-fckkxir8/target/.shiki/schemas/run.schema.json\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-fckkxir8/target/.shiki/schemas/worktree-record.schema.json\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-fckkxir8/target/.shiki/schemas/cca-evidence-manifest.schema.json\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-fckkxir8/target/.shiki/schemas/cca-verdict.schema.json\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-fckkxir8/target/.shiki/schemas/plan.schema.json\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-fckkxir8/target/.shiki/schemas/inbox.schema.json\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-fckkxir8/target/.shiki/schemas/memory-entry.schema.json\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-fckkxir8/target/.shiki/schemas/repair-packet.schema.json\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-fckkxir8/target/.shiki/schemas/smoke.schema.json\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-fckkxir8/target/.shiki/schemas/start.schema.json\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-fckkxir8/target/.shiki/schemas/ledger.schema.json\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-fckkxir8/target/.shiki/schemas/task.schema.json\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-fckkxir8/target/.shiki/schemas/dag.schema.json\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-fckkxir8/target/.shiki/schemas/goal.schema.json\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-fckkxir8/target/.shiki/README.md\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-fckkxir8/target/.shiki/manifest.json\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-fckkxir8/target/.shiki/templates/LEDGER_ENTRY.template.json\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-fckkxir8/target/.shiki/templates/CCA_VERDICT.template.json\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-fckkxir8/target/.shiki/templates/GOAL.template.md\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-fckkxir8/target/.shiki/templates/REPORT.template.md\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-fckkxir8/target/.shiki/templates/DAG.template.json\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-fckkxir8/target/.shiki/templates/worktree-record.schema.json\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-fckkxir8/target/.shiki/templates/REPAIR_PACKET.template.json\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-fckkxir8/target/.shiki/templates/TASK.template.json\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-fckkxir8/target/.shiki/policy.example.yaml\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-fckkxir8/target/.github/ISSUE_TEMPLATE/shiki_goal.yml\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-fckkxir8/target/.github/ISSUE_TEMPLATE/shiki_task.yml\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-fckkxir8/target/.github/CODEOWNERS\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-fckkxir8/target/.github/PULL_REQUEST_TEMPLATE/shiki-task.md\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-fckkxir8/target/.github/prompts/cca-completion-check.md\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-fckkxir8/target/.github/prompts/codex-repair-handoff.md\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-fckkxir8/target/.github/prompts/codex-task-handoff.md\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-fckkxir8/target/.github/workflows/shiki-validate.yml\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-fckkxir8/target/.github/workflows/shiki-claude-review.yml\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-fckkxir8/target/.github/workflows/shiki-cca-completion.yml\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-fckkxir8/target/.github/workflows/shiki-mergegate.yml\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-fckkxir8/target/.github/workflows/shiki-orchestrator.yml\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-fckkxir8/target/docs/agents/node24-workflow-compatibility.md\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-fckkxir8/target/docs/agents/checklists.md\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-fckkxir8/target/docs/agents/codex-handoff.md\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-fckkxir8/target/docs/agents/bootstrap-command.md\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-fckkxir8/target/docs/agents/guardian-policy.md\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-fckkxir8/target/docs/agents/autonomous-loop-e2e-acceptance.md\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-fckkxir8/target/docs/agents/runtime-auth-model.md\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-fckkxir8/target/docs/agents/shiki-doctor.md\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-fckkxir8/target/docs/agents/control-commands.md\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-fckkxir8/target/docs/agents/memory.md\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-fckkxir8/target/docs/agents/shiki-migrations.md\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-fckkxir8/target/docs/agents/decision-control.md\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-fckkxir8/target/docs/agents/domain.md\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-fckkxir8/target/docs/agents/skill-gate.md\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-fckkxir8/target/docs/agents/github-provider-config.md\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-fckkxir8/target/docs/agents/state-classes.md\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-fckkxir8/target/docs/agents/completion-check-agent.md\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-fckkxir8/target/docs/agents/runtime-registry.md\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-fckkxir8/target/docs/agents/implementation-policy.md\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-fckkxir8/target/docs/agents/triage-labels.md\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-fckkxir8/target/docs/agents/shiki-cli-architecture.md\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-fckkxir8/target/docs/agents/issue-tracker.md\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-fckkxir8/target/docs/agents/execution-control.md\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-fckkxir8/target/docs/agents/branch-protection-smoke.md\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-fckkxir8/target/docs/agents/ledger-schema.md\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-fckkxir8/target/docs/agents/evidence-integrity.md\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-fckkxir8/target/docs/adr/SADR-0003-require-skill-gate-for-mergegate-readiness.md\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-fckkxir8/target/docs/adr/template.md\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-fckkxir8/target/docs/adr/SADR-0009-requirements-definition-and-spec-freeze.md\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-fckkxir8/target/docs/adr/SADR-0016-locks-bound-product-paths.md\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-fckkxir8/target/docs/adr/SADR-0011-autonomous-quality-gate-evidence.md\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-fckkxir8/target/docs/adr/SADR-0018-carried-guardian-approval-across-a-proven-base-sync.md\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-fckkxir8/target/docs/adr/SADR-0004-use-github-first-with-shiki-mirror.md\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-fckkxir8/target/docs/adr/SADR-0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-fckkxir8/target/docs/adr/SADR-0015-contract-approval-before-dispatch.md\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-fckkxir8/target/docs/adr/README.md\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-fckkxir8/target/docs/adr/SADR-0017-bookkeeping-closeouts-do-not-inherit-risk.md\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-fckkxir8/target/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-fckkxir8/target/docs/adr/SADR-0007-packaging-and-release.md\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-fckkxir8/target/docs/adr/SADR-0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-fckkxir8/target/docs/adr/SADR-0002-use-agents-as-shared-constitution.md\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-fckkxir8/target/docs/adr/SADR-0014-codex-app-external-ai-guardian-ui-adapter.md\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-fckkxir8/target/docs/adr/SADR-0008-use-claude-code-as-default-implementer.md\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-fckkxir8/target/docs/adr/SADR-0001-keep-shiki-as-platform-name.md\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-fckkxir8/target/docs/adr/SADR-0010-external-ai-guardian-review.md\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-fckkxir8/target/docs/adr/SADR-0013-bootstrap-grants-actions-approve-pull-requests.md\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-fckkxir8/target/docs/adr/SADR-0012-autonomous-loop-closeout-to-main.md\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-fckkxir8/target/skills/engineering/shiki/SKILL.md\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-fckkxir8/target/skills/engineering/setup-matt-pocock-skills/issue-tracker-gitlab.md\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-fckkxir8/target/skills/engineering/setup-matt-pocock-skills/domain.md\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-fckkxir8/target/skills/engineering/setup-matt-pocock-skills/triage-labels.md\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-fckkxir8/target/skills/engineering/setup-matt-pocock-skills/SKILL.md\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-fckkxir8/target/skills/engineering/setup-matt-pocock-skills/issue-tracker-github.md\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-fckkxir8/target/skills/engineering/setup-matt-pocock-skills/issue-tracker-local.md\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-fckkxir8/target/skills/engineering/code-review/SKILL.md\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-fckkxir8/target/skills/engineering/improve-codebase-architecture/LANGUAGE.md\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-fckkxir8/target/skills/engineering/improve-codebase-architecture/SKILL.md\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-fckkxir8/target/skills/engineering/improve-codebase-architecture/HTML-REPORT.md\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-fckkxir8/target/skills/engineering/improve-codebase-architecture/DEEPENING.md\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-fckkxir8/target/skills/engineering/improve-codebase-architecture/INTERFACE-DESIGN.md\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-fckkxir8/target/skills/engineering/triage/OUT-OF-SCOPE.md\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-fckkxir8/target/skills/engineering/triage/SKILL.md\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-fckkxir8/target/skills/engineering/triage/AGENT-BRIEF.md\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-fckkxir8/target/skills/engineering/prototype/SKILL.md\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-fckkxir8/target/skills/engineering/prototype/LOGIC.md\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-fckkxir8/target/skills/engineering/prototype/UI.md\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-fckkxir8/target/skills/engineering/diagnose/scripts/hitl-loop.template.sh\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-fckkxir8/target/skills/engineering/diagnose/SKILL.md\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-fckkxir8/target/skills/engineering/to-issues/SKILL.md\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-fckkxir8/target/skills/engineering/README.md\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-fckkxir8/target/skills/engineering/zoom-out/SKILL.md\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-fckkxir8/target/skills/engineering/grill-with-docs/SKILL.md\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-fckkxir8/target/skills/engineering/grill-with-docs/CONTEXT-FORMAT.md\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-fckkxir8/target/skills/engineering/grill-with-docs/ADR-FORMAT.md\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-fckkxir8/target/skills/engineering/tdd/mocking.md\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-fckkxir8/target/skills/engineering/tdd/deep-modules.md\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-fckkxir8/target/skills/engineering/tdd/tests.md\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-fckkxir8/target/skills/engineering/tdd/SKILL.md\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-fckkxir8/target/skills/engineering/tdd/interface-design.md\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-fckkxir8/target/skills/engineering/tdd/refactoring.md\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-fckkxir8/target/skills/engineering/to-prd/SKILL.md\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-fckkxir8/target/tests/test_lock_release.py\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-fckkxir8/target/tests/test_cca_verdict_usable.py\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-fckkxir8/target/tests/test_id_allocation.py\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-fckkxir8/target/tests/test_mirror_scope.py\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-fckkxir8/target/tests/test_config_loading.py\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-fckkxir8/target/tests/test_cca_verdict_nondegenerate.py\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-fckkxir8/target/tests/test_reopened_task.py\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-fckkxir8/target/tests/test_required_checks_derivation.py\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-fckkxir8/target/tests/test_install_completeness.py\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-fckkxir8/target/tests/test_policy_job_merged_prs.py\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-fckkxir8/target/tests/test_adr_references.py\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-fckkxir8/target/tests/test_session_lease.py\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-fckkxir8/target/tests/test_guardian_signal.py\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-fckkxir8/target/tests/test_cca_verdict_consistency.py\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-fckkxir8/target/tests/test_cca_head_sha_injection.py\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-fckkxir8/target/tests/test_sync_proof.py\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-fckkxir8/target/tests/test_guardian_carry.py\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-fckkxir8/target/tests/test_memory_scorecard.py\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-fckkxir8/target/tests/test_workflow_permissions_payload.py\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-fckkxir8/target/tests/test_contract_mode.py\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-fckkxir8/target/tests/test_memory_consult.py\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-fckkxir8/target/tests/test_workflow_invocation_carry.py\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-fckkxir8/target/tests/test_lock_conflict_stop.py\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-fckkxir8/target/tests/test_migrations_target.py\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-fckkxir8/target/tests/test_branch_protection_payload.py\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-fckkxir8/target/tests/test_loop_branch_plumbing.py\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-fckkxir8/target/tests/test_contract_immutability.py\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-fckkxir8/target/tests/test_loop_smoke.py\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-fckkxir8/target/tests/test_worktree_base.py\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-fckkxir8/target/tests/test_secret_set_claude.py\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-fckkxir8/target/tests/test_cca_completeness.py\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-fckkxir8/target/tests/test_paired_invariants.py\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-fckkxir8/target/tests/__init__.py\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-fckkxir8/target/tests/test_bookkeeping_closeout.py\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-fckkxir8/target/tests/test_contract_open.py\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-fckkxir8/target/tests/test_guardian_signal_closeout.py\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-fckkxir8/target/tests/test_cca_evidence_manifest_taskless.py\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-fckkxir8/target/tests/test_codeowners_wiring.py\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-fckkxir8/target/tests/test_guardian_review.py\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-fckkxir8/target/tests/shiki_test_support.py\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-fckkxir8/target/tests/test_guardian_approval_binding.py\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-fckkxir8/target/tests/test_completion_lock_scope.py\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-fckkxir8/target/tests/test_memory_promotion.py\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-fckkxir8/target/tests/test_loop_code_review.py\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-fckkxir8/target/tests/test_loop_verdict_gating.py\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-fckkxir8/target/tests/test_lock_lifecycle.py\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-fckkxir8/target/tests/test_memory_capture.py\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-fckkxir8/target/tests/test_loop_tdd_evidence.py\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-fckkxir8/target/tests/test_migration_mode.py\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-fckkxir8/target/tests/test_dispatch_lock_precondition.py\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-fckkxir8/target/tests/test_dispatch_state_sync.py\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-fckkxir8/target/tests/test_goal_reconcile.py\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-fckkxir8/target/tests/test_runner_adapters.py\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-fckkxir8/target/tests/test_lock_matching.py\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-fckkxir8/target/tests/test_contract_approval.py\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-fckkxir8/target/tests/test_memory_hooks.py\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-fckkxir8/target/tests/test_branch_behind_sync.py\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-fckkxir8/target/tests/test_install_force_semantics.py\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-fckkxir8/target/tests/test_contract_plan_registration.py\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-fckkxir8/target/tests/test_sadr_install_migration.py\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-fckkxir8/target/tests/test_loop_engine.py\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-fckkxir8/target/tests/test_repair_delivery.py\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-fckkxir8/target/tests/test_handoff_consult.py\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-fckkxir8/target/tests/test_loop_smoke3.py\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-fckkxir8/target/tests/test_contract_amendment.py\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-fckkxir8/target/tests/test_bootstrap_safety.py\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-fckkxir8/target/tests/test_guardian_carry_parity.py\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-fckkxir8/target/tests/test_guardian_evidence.py\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-fckkxir8/target/tests/test_loop_smoke2.py\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-fckkxir8/target/tests/test_loop_lock_guard.py\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-fckkxir8/target/tests/test_manifest_staging.py\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-fckkxir8/target/tests/test_loop_lock_validator_hint.py\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-fckkxir8/target/tests/test_guardian_status.py\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-fckkxir8/target/tests/test_workflow_invocation.py\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-fckkxir8/target/tests/test_closeout.py\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-fckkxir8/target/tests/test_mirror_schema_validation.py\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-fckkxir8/target/tests/test_repair_reasons.py\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-fckkxir8/target/scripts/shiki_schema.py\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-fckkxir8/target/scripts/validate_shiki.py\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-fckkxir8/target/scripts/shiki_contracts.py\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-fckkxir8/target/scripts/shiki_jsonschema.py\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-fckkxir8/target/scripts/shiki_evidence.py\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-fckkxir8/target/scripts/shiki_locks.py\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-fckkxir8/target/scripts/shiki_loop.py\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-fckkxir8/target/scripts/shiki_memory.py\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-fckkxir8/target/scripts/shiki_manifest.py\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-fckkxir8/target/scripts/shiki_migrations.py\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-fckkxir8/target/scripts/shiki_provider.py\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-fckkxir8/target/scripts/shiki_workflows.py\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-fckkxir8/target/scripts/enforce_cca_verdict.py\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-fckkxir8/target/scripts/cca_verdict_usable.py\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-fckkxir8/target/scripts/build_cca_evidence_manifest.py\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-fckkxir8/target/scripts/guardian_approval_signal.py\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-fckkxir8/target/scripts/mergegate_check.py\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-fckkxir8/target/scripts/shiki_sync_proof.py\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-fckkxir8/target/scripts/shiki.py\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-fckkxir8/target/scripts/shiki_bootstrap.py\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-fckkxir8/target/scripts/shiki_cli.py\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-fckkxir8/target/scripts/shiki_config.py\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-fckkxir8/target/scripts/shiki_contract_approval.py\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-fckkxir8/target/scripts/shiki_doctor.py\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-fckkxir8/target/scripts/shiki_git.py\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-fckkxir8/target/scripts/shiki_github.py\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-fckkxir8/target/scripts/shiki_guardian.py\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-fckkxir8/target/scripts/shiki_guardian_review.py\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-fckkxir8/target/scripts/shiki_guardian_status.py\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-fckkxir8/target/scripts/shiki_installer.py\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-fckkxir8/target/scripts/shiki_process.py\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-fckkxir8/target/scripts/shiki_runtime.py\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-fckkxir8/target/scripts/shiki_runtime_adapters.py\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-fckkxir8/target/scripts/shiki_runtime_registry.py\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-fckkxir8/target/scripts/shiki_session_lease.py\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-fckkxir8/target/scripts/shiki_state_classes.py\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-fckkxir8/target/scripts/shiki_tasks.py\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-fckkxir8/target/scripts/shiki_state.py\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-fckkxir8/target/scripts/test_shiki_init.sh\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-fckkxir8/target/scripts/test_shiki_control_plane.sh\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-fckkxir8/target/scripts/test_shiki_run_orchestrator.sh\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-fckkxir8/target/scripts/test_shiki_daemon_runner.sh\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-fckkxir8/target/scripts/test_shiki_runner_codex.sh\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-fckkxir8/target/scripts/test_shiki_runner_claude.sh\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-fckkxir8/target/scripts/test_shiki_goal_loop.sh\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-fckkxir8/target/scripts/test_shiki_memory_loop.sh\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-fckkxir8/target/scripts/test_shiki_code_review_gate.sh\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-fckkxir8/target/scripts/test_shiki_start.sh\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-fckkxir8/target/scripts/test_shiki_runtime_auth.sh\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-fckkxir8/target/scripts/test_shiki_runtime_registry.sh\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-fckkxir8/target/scripts/test_shiki_state_classes.sh\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-fckkxir8/target/scripts/test_shiki_provider_config.sh\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-fckkxir8/target/scripts/test_shiki_guardian_policy.sh\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-fckkxir8/target/scripts/test_shiki_evidence_integrity.sh\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-fckkxir8/target/scripts/test_shiki_governance_evidence.sh\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-fckkxir8/target/scripts/test_shiki_doctor.sh\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-fckkxir8/target/scripts/test_shiki_migrations.sh\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-fckkxir8/target/scripts/test_shiki_module_boundaries.sh\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-fckkxir8/target/scripts/test_shiki_shellcheck.sh\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-fckkxir8/target/scripts/test_shiki_validator_hardening.sh\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-fckkxir8/target/scripts/test_shiki_workflow_lint.sh\n[shiki] ensured empty state directory: /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-fckkxir8/target/.shiki/goals\n[shiki] ensured empty state directory: /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-fckkxir8/target/.shiki/plans\n[shiki] ensured empty state directory: /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-fckkxir8/target/.shiki/tasks\n[shiki] ensured empty state directory: /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-fckkxir8/target/.shiki/dag\n[shiki] ensured empty state directory: /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-fckkxir8/target/.shiki/ledger\n[shiki] ensured empty state directory: /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-fckkxir8/target/.shiki/migrations\n[shiki] ensured empty state directory: /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-fckkxir8/target/.shiki/locks\n[shiki] ensured empty state directory: /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-fckkxir8/target/.shiki/worktrees\n[shiki] ensured empty state directory: /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-fckkxir8/target/.shiki/repairs\n[shiki] ensured empty state directory: /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-fckkxir8/target/.shiki/reports\n[shiki] ensured empty state directory: /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-fckkxir8/target/.shiki/runs\n[shiki] ensured empty state directory: /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-fckkxir8/target/.shiki/inbox\n[shiki] ensured empty state directory: /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-fckkxir8/target/.shiki/handoffs\n[shiki] ensured empty state directory: /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-fckkxir8/target/.shiki/runner\n[shiki] ensured empty state directory: /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-fckkxir8/target/.shiki/smoke\n[shiki] ensured empty state directory: /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-fckkxir8/target/.shiki/starts\n[shiki] ensured empty state directory: /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-fckkxir8/target/.shiki/memories\n[shiki] wrote install stamp: /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-fckkxir8/target/.shiki/install-stamp.json\n[shiki] template summary: no .new files written (no existing project-content or governance file was kept alongside a refreshed copy)\n\ncodex\nThe SADR tree and resolver are green, but fresh-install validation exposed a paired-contract constraint: the canonical source-of-truth sentence is mirrored in `.claude/commands/shiki.md`, which is outside this task\u2019s locks. I will not touch it. I\u2019m keeping that generic canonical sentence byte-compatible and placing the explicit ADR/SADR namespace guidance in the locked governance surfaces instead; this preserves the constitution invariant without expanding scope.\napply patch\n2026-08-07T08:10:15.315331Z ERROR codex_models_manager::cache: failed to load models cache: missing field `base_instructions` at line 94 column 5\n2026-08-07T08:10:15.333661Z ERROR codex_models_manager::cache: failed to load models cache: missing field `base_instructions` at line 94 column 5\n2026-08-07T08:10:21.524688Z  WARN codex_protocol::openai_models: Model personality requested but model_messages is missing, falling back to base instructions. model=codex-auto-review personality=pragmatic\npatch: completed\n/private/tmp/shiki-adr-collision.pa0KwY/.worktrees/shiki-t-20260807t073307221254z-2d22d654-introduc/.codex/skills/shiki/SKILL.md\n/private/tmp/shiki-adr-collision.pa0KwY/.worktrees/shiki-t-20260807t073307221254z-2d22d654-introduc/.github/prompts/cca-completion-check.md\n/private/tmp/shiki-adr-collision.pa0KwY/.worktrees/shiki-t-20260807t073307221254z-2d22d654-introduc/AGENTS.md\n/private/tmp/shiki-adr-collision.pa0KwY/.worktrees/shiki-t-20260807t073307221254z-2d22d654-introduc/CLAUDE.md\n/private/tmp/shiki-adr-collision.pa0KwY/.worktrees/shiki-t-20260807t073307221254z-2d22d654-introduc/SYSTEM_PROMPT.md\n/private/tmp/shiki-adr-collision.pa0KwY/.worktrees/shiki-t-20260807t073307221254z-2d22d654-introduc/docs/agents/implementation-policy.md\n/private/tmp/shiki-adr-collision.pa0KwY/.worktrees/shiki-t-20260807t073307221254z-2d22d654-introduc/scripts/shiki_contracts.py\ndiff --git a/.codex/skills/shiki/SKILL.md b/.codex/skills/shiki/SKILL.md\nindex c8e2e85d9d75d49cc4f2d6286ee22659c4e53e75..93cded623b2c15d9f65c76491a1c2d813ac9e6c0\n--- a/.codex/skills/shiki/SKILL.md\n+++ b/.codex/skills/shiki/SKILL.md\n@@ -18,6 +18,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n Codex CLI does not register this skill as a custom `/shiki` slash command. When\n the user types `/shiki` in Codex CLI and gets \"Unrecognized command\", explain\n that this is expected: invoke Shiki in Codex with natural language such as\n@@ -69,7 +72,7 @@\n \n ## Responsibilities\n \n-- Claude Code implements and repairs by default (`shiki runner claude`, ADR 0008); Codex implements and repairs tasks explicitly assigned to `codex`.\n+- Claude Code implements and repairs by default (`shiki runner claude`, SADR-0008); Codex implements and repairs tasks explicitly assigned to `codex`.\n - Claude Code Action can act as GitHub-side CCA or reviewer.\n - CCA judges completion.\n - MergeGate authorizes state transitions and merge readiness.\ndiff --git a/.github/prompts/cca-completion-check.md b/.github/prompts/cca-completion-check.md\nindex ed203ad5f302ecc81372d8ab27d2b1232ca97497..fd89950528333ef6242e6d5443904f5392ab7e56\n--- a/.github/prompts/cca-completion-check.md\n+++ b/.github/prompts/cca-completion-check.md\n@@ -19,6 +19,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n ## Required Reading\n \n Read only the files needed to judge the PR contract. Prefer this order:\ndiff --git a/AGENTS.md b/AGENTS.md\nindex 7f041100d49c2a7525f51cb0f5d696a4a6d1c78c..2b700e989ff295987aa8a4b0c5fcb1acd80020da\n--- a/AGENTS.md\n+++ b/AGENTS.md\n@@ -44,6 +44,10 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use\n+`SADR-NNNN-*.md`; target product decisions retain `NNNN-*.md` and the `ADR`\n+namespace.\n+\n Use the canonical source-of-truth order above. Runtime-specific reading orders\n are operational guidance only and must not override or reorder it.\n \ndiff --git a/CLAUDE.md b/CLAUDE.md\nindex 98a932e7841a632866fcd53b0bad0e3af3711d3d..611d491912d6a72a0327a1731b8f33a41acb4baa\n--- a/CLAUDE.md\n+++ b/CLAUDE.md\n@@ -14,6 +14,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n ## Claude Role\n \n Default to planner, default implementer, reviewer, coordinator, documentation editor, CCA-style completion judge, and final-judgment assistant.\ndiff --git a/SYSTEM_PROMPT.md b/SYSTEM_PROMPT.md\nindex aaaea45e3a896c3a44ba89eec8bb84325ecf6c62..2e7c093ebe9e8bb34e0923756f191b4f7fa581f5\n--- a/SYSTEM_PROMPT.md\n+++ b/SYSTEM_PROMPT.md\n@@ -30,6 +30,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n Use the canonical source-of-truth order above. Runtime-specific reading orders\n are operational guidance only and must not override or reorder it. The current\n conversation is non-durable operator input.\ndiff --git a/docs/adr/0001-keep-shiki-as-platform-name.md b/docs/adr/SADR-0001-keep-shiki-as-platform-name.md\nindex 20a9637c3b9d8c97de7ba8db7cfae4d82d9a7dba..8b685439959aada1b221c112f0ac3c51ca8a1695\n--- a/docs/adr/0001-keep-shiki-as-platform-name.md\n+++ b/docs/adr/SADR-0001-keep-shiki-as-platform-name.md\n@@ -1,3 +1,3 @@\n-# Keep Shiki As The Platform Name\n+# SADR-0001: Keep Shiki As The Platform Name\n \n Shiki remains the product name for the GitHub-native agentic engineering control plane. We considered more descriptive names such as MergePilot, MergeGate, and Kiroku, but kept Shiki because it carries the existing goal-seeking framework identity while remaining independent of Hermes, Codex, Claude Code, or any single runtime.\ndiff --git a/docs/adr/0002-use-agents-as-shared-constitution.md b/docs/adr/SADR-0002-use-agents-as-shared-constitution.md\nindex 73de30ecb078120eac540d9bc51855e3cd48d0e3..c5486182bf06fca36ea508496b013c67302fd989\n--- a/docs/adr/0002-use-agents-as-shared-constitution.md\n+++ b/docs/adr/SADR-0002-use-agents-as-shared-constitution.md\n@@ -1,3 +1,3 @@\n-# Use AGENTS As The Shared Agent Constitution\n+# SADR-0002: Use AGENTS As The Shared Agent Constitution\n \n Shiki uses `AGENTS.md` as the runtime-neutral constitution and `CLAUDE.md` as a thin Claude Code wrapper. This keeps Codex, Claude Code, Hermes Runner, and future Agent Runtimes aligned on the same GitHub-first MergeGate rules while still allowing runtime-specific instructions where needed.\ndiff --git a/docs/adr/0003-require-skill-gate-for-mergegate-readiness.md b/docs/adr/SADR-0003-require-skill-gate-for-mergegate-readiness.md\nindex 424be0c9af4c8f0136f1fde68a468c47ff84e764..7fc337987c7df9792f75f181dfcbed75fe5ffff2\n--- a/docs/adr/0003-require-skill-gate-for-mergegate-readiness.md\n+++ b/docs/adr/SADR-0003-require-skill-gate-for-mergegate-readiness.md\n@@ -1,3 +1,3 @@\n-# Require Skill Gate For MergeGate Readiness\n+# SADR-0003: Require Skill Gate For MergeGate Readiness\n \n Shiki requires relevant engineering skills to be invoked before execution when their triggers apply. This makes skills part of MergeGate readiness rather than optional prompt style, because Shiki depends on repeatable planning, issue decomposition, TDD, diagnosis, triage, and architecture review before autonomous or parallel agent work can be trusted.\ndiff --git a/docs/adr/0004-use-github-first-with-shiki-mirror.md b/docs/adr/SADR-0004-use-github-first-with-shiki-mirror.md\nindex d0a3a9174dc646a945a9d325954ffaf00c352040..a79c54ae9ee8eed2a0524192b864da4e22a92d3f\n--- a/docs/adr/0004-use-github-first-with-shiki-mirror.md\n+++ b/docs/adr/SADR-0004-use-github-first-with-shiki-mirror.md\n@@ -1,4 +1,4 @@\n-# ADR 0004: Use GitHub First With A Repository-Local Shiki Mirror\n+# SADR-0004: Use GitHub First With A Repository-Local Shiki Mirror\n \n ## Status\n \ndiff --git a/docs/adr/0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md b/docs/adr/SADR-0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\nindex b339baf49b89d9290e79c3d51b36e7b2bde3bd95..c30a0d8b1fde0224d0ac7820e7870dccf721580d\n--- a/docs/adr/0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\n+++ b/docs/adr/SADR-0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\n@@ -1,4 +1,4 @@\n-# ADR 0005: Use Codex As Front And Claude Code Action As The GitHub Runtime\n+# SADR-0005: Use Codex As Front And Claude Code Action As The GitHub Runtime\n \n ## Status\n \ndiff --git a/docs/adr/0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md b/docs/adr/SADR-0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\nindex 4bcae9d1e52f0a37b8e9fbafa5cd14b6ee1993bd..7a38538a6ae9929567f0ade67e7ea6a60d0b3f7e\n--- a/docs/adr/0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\n+++ b/docs/adr/SADR-0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\n@@ -1,4 +1,4 @@\n-# ADR 0006: Separate Codex Implementation, CCA Completion, And MergeGate Authorization\n+# SADR-0006: Separate Codex Implementation, CCA Completion, And MergeGate Authorization\n \n ## Status\n \ndiff --git a/docs/adr/0007-packaging-and-release.md b/docs/adr/SADR-0007-packaging-and-release.md\nindex 79b84ddb97167372b44641e5b7860e400ce80822..023cd57f8011aa6e6d2ebf612b7c35a7a82c00d4\n--- a/docs/adr/0007-packaging-and-release.md\n+++ b/docs/adr/SADR-0007-packaging-and-release.md\n@@ -1,4 +1,4 @@\n-# ADR 0007: Packaging And Release For Shiki\n+# SADR-0007: Packaging And Release For Shiki\n \n ## Status\n \ndiff --git a/docs/adr/0008-use-claude-code-as-default-implementer.md b/docs/adr/SADR-0008-use-claude-code-as-default-implementer.md\nindex dedf4510b7c7799521422f622029563d373db031..a158a8d444d93764ec515a28133f7dde4af23579\n--- a/docs/adr/0008-use-claude-code-as-default-implementer.md\n+++ b/docs/adr/SADR-0008-use-claude-code-as-default-implementer.md\n@@ -1,4 +1,4 @@\n-# ADR 0008: Use Claude Code As The Default Implementer Runtime\n+# SADR-0008: Use Claude Code As The Default Implementer Runtime\n \n ## Status\n \ndiff --git a/docs/adr/0009-requirements-definition-and-spec-freeze.md b/docs/adr/SADR-0009-requirements-definition-and-spec-freeze.md\nindex 29b3f7871bef9203a8b4a831a65c39eb2f506e42..52e7dd69ad0aa4efe0b68c62678abbf7df02aaec\n--- a/docs/adr/0009-requirements-definition-and-spec-freeze.md\n+++ b/docs/adr/SADR-0009-requirements-definition-and-spec-freeze.md\n@@ -1,4 +1,4 @@\n-# ADR 0009: Merge Grill And Planning Into Requirements Definition With Spec Freeze\n+# SADR-0009: Merge Grill And Planning Into Requirements Definition With Spec Freeze\n \n ## Status\n \ndiff --git a/docs/adr/0010-external-ai-guardian-review.md b/docs/adr/SADR-0010-external-ai-guardian-review.md\nindex e6108bbe8bb4e73e0ff4684c8f78001a80438c4e..e690d4eddd270e683d709c9b679c5859e71eeaaa\n--- a/docs/adr/0010-external-ai-guardian-review.md\n+++ b/docs/adr/SADR-0010-external-ai-guardian-review.md\n@@ -1,4 +1,4 @@\n-# ADR 0010: External AI Guardian Review As A First-Class Approval Authority\n+# SADR-0010: External AI Guardian Review As A First-Class Approval Authority\n \n ## Status\n \ndiff --git a/docs/adr/0011-autonomous-quality-gate-evidence.md b/docs/adr/SADR-0011-autonomous-quality-gate-evidence.md\nindex bca0246c5d46e44afbde15486a75eec425fc02f5..9c93eac16d8c3a6d4b11bd7224e3955102cd7db4\n--- a/docs/adr/0011-autonomous-quality-gate-evidence.md\n+++ b/docs/adr/SADR-0011-autonomous-quality-gate-evidence.md\n@@ -1,4 +1,4 @@\n-# ADR 0011: Autonomous Quality-Gate Evidence \u2014 Deterministic Facts And An Independent Verifier\n+# SADR-0011: Autonomous Quality-Gate Evidence \u2014 Deterministic Facts And An Independent Verifier\n \n ## Status\n \ndiff --git a/docs/adr/0012-autonomous-loop-closeout-to-main.md b/docs/adr/SADR-0012-autonomous-loop-closeout-to-main.md\nindex 637fad6dde4989c9c2bf0f81f676d8d29991123c..ec7d85f81030a676bcbba28b43eb365422a36eac\n--- a/docs/adr/0012-autonomous-loop-closeout-to-main.md\n+++ b/docs/adr/SADR-0012-autonomous-loop-closeout-to-main.md\n@@ -1,4 +1,4 @@\n-# 0012 \u2014 Autonomous loop pushes goal completion to main via a closeout PR\n+# SADR-0012 \u2014 Autonomous loop pushes goal completion to main via a closeout PR\n \n - Status: accepted\n - Date: 2026-06-18\ndiff --git a/docs/adr/0013-bootstrap-grants-actions-approve-pull-requests.md b/docs/adr/SADR-0013-bootstrap-grants-actions-approve-pull-requests.md\nindex b6c69d64f1c5ced346b88d6859586715b3b5bd0e..a793e7277d67ab8ea56286fec289ca689825a17d\n--- a/docs/adr/0013-bootstrap-grants-actions-approve-pull-requests.md\n+++ b/docs/adr/SADR-0013-bootstrap-grants-actions-approve-pull-requests.md\n@@ -1,4 +1,4 @@\n-# ADR 0013: Bootstrap Grants GitHub Actions Permission To Create And Approve Pull Requests\n+# SADR-0013: Bootstrap Grants GitHub Actions Permission To Create And Approve Pull Requests\n \n ## Status\n \ndiff --git a/docs/adr/0014-codex-app-external-ai-guardian-ui-adapter.md b/docs/adr/SADR-0014-codex-app-external-ai-guardian-ui-adapter.md\nindex 238db06279334caa341c117362df9271382a7fcd..65f12e9939321e1b3e389a34c822fc1549e5155d\n--- a/docs/adr/0014-codex-app-external-ai-guardian-ui-adapter.md\n+++ b/docs/adr/SADR-0014-codex-app-external-ai-guardian-ui-adapter.md\n@@ -1,4 +1,4 @@\n-# ADR 0014: Use Codex App As The External AI Guardian UI Adapter\n+# SADR-0014: Use Codex App As The External AI Guardian UI Adapter\n \n ## Status\n \ndiff --git a/docs/adr/0015-contract-approval-before-dispatch.md b/docs/adr/SADR-0015-contract-approval-before-dispatch.md\nindex fad6c656741a20a7684ba16080a7db159f63a52f..7bdd760bb3e82eee4e1410f45c783b03b638d456\n--- a/docs/adr/0015-contract-approval-before-dispatch.md\n+++ b/docs/adr/SADR-0015-contract-approval-before-dispatch.md\n@@ -1,4 +1,4 @@\n-# ADR 0015: Approve The Task Contract Before Dispatch, Not The Diff After It\n+# SADR-0015: Approve The Task Contract Before Dispatch, Not The Diff After It\n \n ## Status\n \ndiff --git a/docs/adr/0016-locks-bound-product-paths.md b/docs/adr/SADR-0016-locks-bound-product-paths.md\nindex b5e850f3f1f7f4ef6e7f9d1763a29a51bd300c45..332cc17313498404f41a145291e38447b5de0dc4\n--- a/docs/adr/0016-locks-bound-product-paths.md\n+++ b/docs/adr/SADR-0016-locks-bound-product-paths.md\n@@ -1,4 +1,4 @@\n-# ADR 0016: Locks bound product paths; the Shiki mirror is bound by record identity\n+# SADR-0016: Locks bound product paths; the Shiki mirror is bound by record identity\n \n ## Status\n \ndiff --git a/docs/adr/0017-bookkeeping-closeouts-do-not-inherit-risk.md b/docs/adr/SADR-0017-bookkeeping-closeouts-do-not-inherit-risk.md\nindex 554e3a0d526a72ca067108c5464cc68c3edcf174..83115f69f1b1b48d544c9b0157f377b91cb53abf\n--- a/docs/adr/0017-bookkeeping-closeouts-do-not-inherit-risk.md\n+++ b/docs/adr/SADR-0017-bookkeeping-closeouts-do-not-inherit-risk.md\n@@ -1,4 +1,4 @@\n-# ADR 0017: A Bookkeeping Closeout Does Not Inherit Its Task's Risk\n+# SADR-0017: A Bookkeeping Closeout Does Not Inherit Its Task's Risk\n \n ## Status\n \ndiff --git a/docs/adr/0018-carried-guardian-approval-across-a-proven-base-sync.md b/docs/adr/SADR-0018-carried-guardian-approval-across-a-proven-base-sync.md\nindex 22358562da390ed8b45cd600da08a43d378b4680..38da0fd95102c3496c3219ce2319fce696a2532a\n--- a/docs/adr/0018-carried-guardian-approval-across-a-proven-base-sync.md\n+++ b/docs/adr/SADR-0018-carried-guardian-approval-across-a-proven-base-sync.md\n@@ -1,4 +1,4 @@\n-# ADR 0018: Carried Guardian Approval Across A Proven Base Sync\n+# SADR-0018: Carried Guardian Approval Across A Proven Base Sync\n \n ## Status\n \ndiff --git a/docs/adr/README.md b/docs/adr/README.md\nindex c70e057a4520c36e042ab68c53cd4a21ef709b71..5da43856836e2ce6a150f8127766e2f88355ab4b\n--- a/docs/adr/README.md\n+++ b/docs/adr/README.md\n@@ -1,40 +1,41 @@\n-# Architecture Decision Records\n+# Decision Record Namespaces\n \n-This directory holds Shiki's Architecture Decision Records (ADRs): durable\n-records of hard-to-reverse platform decisions. ADRs are part of the Shiki source\n-of truth alongside GitHub state, the `.shiki/` mirror, and `CONTEXT.md`.\n+This directory holds two independent decision namespaces:\n \n-## How ADRs Work\n+- Shiki Architecture Decision Records (SADRs), named\n+  `SADR-NNNN-<short-slug>.md`, record hard-to-reverse Shiki platform decisions.\n+- Target Architecture Decision Records (ADRs), named\n+  `NNNN-<short-slug>.md`, belong to the target product.\n \n-- New ADRs are added as `docs/adr/NNNN-<short-slug>.md`, where `NNNN` is the next\n-  zero-padded sequence number.\n-- The **canonical list of ADRs is the set of files matching `docs/adr/*.md`** in\n-  this directory. Browse `docs/adr/*.md` for the authoritative, current set.\n-- Use [`template.md`](template.md) as the starting point for a new ADR.\n-- Record an ADR when a decision is hard to reverse, surprising without context,\n-  and a real tradeoff. Do not write an ADR for routine or easily reversible\n-  choices.\n+The same four-digit number may appear once in each namespace. A platform\n+`SADR-NNNN` citation resolves only to `SADR-NNNN-*.md`; a target `ADR NNNN`\n+citation resolves only to `NNNN-*.md`.\n \n-Using the `docs/adr/*.md` glob as the source of truth means other PRs can add an\n-ADR without editing this index file, avoiding merge contention on a\n-hand-maintained enumeration.\n+## How Decision Records Work\n \n-## Current ADRs (snapshot)\n-\n-The list below is a convenience snapshot and may lag behind the directory. When\n-in doubt, list `docs/adr/*.md`.\n+- New Shiki platform decisions use the next zero-padded SADR number.\n+- New target product decisions use the target's own ADR sequence.\n+- The canonical list is the files in `docs/adr/`; no hand-maintained index is\n+  authoritative.\n+- Use [`template.md`](template.md) as the starting point and choose the filename\n+  namespace that owns the decision.\n+- Record a decision when it is hard to reverse, surprising without context, and\n+  a real tradeoff. Do not write a decision record for routine or easily\n+  reversible choices.\n \n-- [0001 \u2014 Keep Shiki As The Platform Name](0001-keep-shiki-as-platform-name.md)\n-- [0002 \u2014 Use AGENTS As The Shared Agent Constitution](0002-use-agents-as-shared-constitution.md)\n-- [0003 \u2014 Require Skill Gate For MergeGate Readiness](0003-require-skill-gate-for-mergegate-readiness.md)\n-- [0004 \u2014 Use GitHub First With A Repository-Local Shiki Mirror](0004-use-github-first-with-shiki-mirror.md)\n-- [0005 \u2014 Use Codex As Front And Claude Code Action As The GitHub Runtime](0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md)\n-- [0006 \u2014 Separate Codex Implementation, CCA Completion, And MergeGate Authorization](0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md)\n+Using the directory as the source of truth lets independent changes add records\n+without contending on an enumerated index.\n \n-## Adding A New ADR\n+## Adding A Shiki SADR\n \n-1. Copy `template.md` to `docs/adr/NNNN-<short-slug>.md`.\n+1. Copy `template.md` to `docs/adr/SADR-NNNN-<short-slug>.md`.\n 2. Fill in the title, status, context, decision, and consequences.\n-3. Set the status to `Proposed`, then `Accepted` once the decision is approved.\n-4. Open a PR following `CONTRIBUTING.md`. You do not need to edit this index for\n-   the ADR to be discoverable via `docs/adr/*.md`.\n+3. Set the status to `Proposed`, then `Accepted` once the proper authority has\n+   approved the decision.\n+4. Open a PR following `CONTRIBUTING.md`.\n+\n+## Adding A Target ADR\n+\n+1. Copy `template.md` to `docs/adr/NNNN-<short-slug>.md` using the target's own\n+   sequence.\n+2. Cite it as `ADR NNNN`; do not prefix its filename with `SADR-`.\ndiff --git a/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md b/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\nnew file mode 100644\nindex 0000000000000000000000000000000000000000..7455ce1f3bed02ddddb851ac21a1d1a648c9aeba\n--- /dev/null\n+++ b/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\n@@ -0,0 +1,55 @@\n+# SADR-0019: Separate Shiki Platform SADRs From Target ADRs\n+\n+## Status\n+\n+Accepted\n+\n+## Context\n+\n+Shiki installs its platform decision records into `docs/adr/` in every target\n+repository. Shiki and its targets previously used the same numeric\n+`NNNN-<slug>.md` namespace. A target that already owned ADRs 0013 through 0018\n+therefore acquired two records for each number during installation, making an\n+`ADR NNNN` citation ambiguous. Renumbering target decisions would rewrite target\n+history, while renumbering Shiki decisions per target would destroy their stable\n+platform identity.\n+\n+Upgrades also need to remove the former Shiki-owned numeric paths without ever\n+mistaking a target-owned ADR for platform content. The install stamp already\n+records exact shipped paths and content digests, so it can prove ownership only\n+when both still match.\n+\n+## Decision\n+\n+Shiki platform decisions use the `SADR` namespace:\n+\n+- records are named `docs/adr/SADR-NNNN-<slug>.md`;\n+- live platform citations use `SADR-NNNN`;\n+- target product decisions retain `docs/adr/NNNN-<slug>.md` and `ADR NNNN`;\n+- the reference validator resolves the two namespaces independently, so the same\n+  four-digit number may exist once in each namespace.\n+\n+Before a forced upgrade writes anything, the installer inventories the exact\n+former numeric Shiki ADR paths. It may delete a legacy path only when the\n+existing install stamp contains that exact path and its recorded SHA-256 digest\n+matches the file currently on disk. All ownership blockers are reported\n+together and abort the upgrade before its first write. A non-forced install\n+never performs this cleanup.\n+\n+Historical `.shiki/` evidence remains unchanged because it records the namespace\n+that existed when that evidence was produced.\n+\n+## Consequences\n+\n+- Targets may use any ADR number without colliding with Shiki's platform SADRs.\n+- Fresh installs preserve target ADR bytes and install a stable SADR set.\n+- Forced upgrades fail closed when legacy ownership cannot be proven, and a\n+  successful upgrade refreshes the stamp without removed legacy paths.\n+- Current governance, packaging, workflow, prompt, script, and skill references\n+  must move with the platform records.\n+\n+## Alternatives Considered\n+\n+- Collision-only refusal leaves affected targets permanently unupgradable.\n+- Renumbering target ADRs mutates target-owned history.\n+- Per-target Shiki renumbering makes platform citations non-portable.\ndiff --git a/docs/adr/template.md b/docs/adr/template.md\nindex a92a97daddd0b00ab9dd626cc8d86942da2eafed..17dd79175b5fee2105935557f691b40cf9492942\n--- a/docs/adr/template.md\n+++ b/docs/adr/template.md\n@@ -1,14 +1,14 @@\n-# NNNN: <Short Decision Title>\n+# <SADR-NNNN or NNNN>: <Short Decision Title>\n \n ## Status\n \n-Proposed | Accepted | Superseded by ADR-NNNN\n+Proposed | Accepted | Superseded by SADR-NNNN or target ADR NNNN\n \n ## Context\n \n What is the situation, constraint, or force that requires a decision? Reference\n-relevant domain terms (`CONTEXT.md`), prior ADRs, and code reality. State the\n-problem, not the solution.\n+relevant domain terms (`CONTEXT.md`), prior Shiki SADRs or target ADRs, and code\n+reality. State the problem, not the solution.\n \n ## Decision\n \ndiff --git a/docs/agents/domain.md b/docs/agents/domain.md\nindex 258da8ea81e87ac0a6b0b5b5e5c2fde980eaabb7..67aaa6f4b7d0a5aa4d495ce6279073333f7798cb\n--- a/docs/agents/domain.md\n+++ b/docs/agents/domain.md\n@@ -5,7 +5,9 @@\n ## Layout\n \n - `CONTEXT.md` defines Shiki language. Keep it implementation-free.\n-- `docs/adr/` records hard-to-reverse decisions.\n+- `docs/adr/SADR-NNNN-*.md` records Shiki platform decisions.\n+- `docs/adr/NNNN-*.md` records target product decisions without sharing the\n+  SADR namespace.\n - `AGENTS.md` is the runtime-neutral agent constitution.\n - `CLAUDE.md` is the Claude Code wrapper around `AGENTS.md`.\n - `.shiki/` is the Target Repository mirror for Goals, PRDs, plans, Task DAGs, contracts, locks, worktree records, ledger entries, CCA verdicts, repair packets, reports, and handoffs.\ndiff --git a/docs/agents/implementation-policy.md b/docs/agents/implementation-policy.md\nindex c754144a9fb5bf4a38aa82a60e1670c59e626b44..e90d40fe21880b9a09719c5ecfe6bde7284eb3ce\n--- a/docs/agents/implementation-policy.md\n+++ b/docs/agents/implementation-policy.md\n@@ -31,6 +31,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n ## Non-Negotiables\n \n 1. **Planning precedes implementation.** Non-trivial Goals start with `grill-with-docs` and Context & Impact.\ndiff --git a/tests/test_adr_references.py b/tests/test_adr_references.py\nindex 51706329c797d5dd9b47e082a333fa83b261d482..91ab76b724cb973435c45d53cab63793942267e9\n--- a/tests/test_adr_references.py\n+++ b/tests/test_adr_references.py\n@@ -1,4 +1,4 @@\n-\"\"\"Every ``ADR ####`` citation must resolve to exactly one decision record.\n+\"\"\"Every target ADR and Shiki SADR citation resolves in its own namespace.\n \n Background. ``scripts/mergegate_check.py`` justifies ``is_bookkeeping_closeout``\n with \"ADR 0017\" nine times and ``scripts/guardian_approval_signal.py`` cites\n@@ -11,11 +11,12 @@\n and the directory drifted with no signal.\n \n This is the same unbound-pair shape behind other silent failures: a reference\n-and its target with nothing asserting they agree. This test binds them. It scans\n-every ``scripts/*.py`` (plus ``docs/**/*.md`` and the root ``*.md`` governance\n-files, which are cheap) for the ``ADR ####`` pattern and asserts each cited\n-number resolves to exactly one ``docs/adr/####-*.md``. The converse is\n-deliberately NOT required: an ADR may exist without being cited.\n+and its target with nothing asserting they agree. This test binds them. Target\n+``ADR NNNN`` citations resolve only to ``docs/adr/NNNN-*.md``; Shiki\n+``SADR-NNNN`` citations resolve only to ``docs/adr/SADR-NNNN-*.md``. The scan\n+covers current governance docs, scripts, workflows, prompts, packaging metadata,\n+and repo-local Codex guidance while deliberately excluding historical\n+``.shiki`` evidence. The converse is not required: a record may be uncited.\n \n The suite is dependency free: standard-library ``unittest`` only.\n \"\"\"\n@@ -31,17 +32,18 @@\n \n REPO_ROOT = Path(__file__).resolve().parents[1]\n \n-# \"ADR 0016\" -- a four-digit citation. The trailing \\b keeps a five-digit token\n-# from being read as a truncated four-digit number.\n-_CITATION = re.compile(r\"\\bADR (\\d{4})\\b\")\n-# \"0016-locks-bound-product-paths.md\" -- the leading four digits name the record.\n-_ADR_FILENAME = re.compile(r\"^(\\d{4})-.*\\.md$\")\n+# ``ADR 0016`` / ``SADR-0016`` -- a namespace plus four-digit citation. The\n+# trailing ``\\b`` keeps a five-digit token from being read as a truncated one.\n+_CITATION = re.compile(r\"\\b(S?ADR)[ -](\\d{4})\\b\")\n+# Target ADRs retain the numeric filename; Shiki platform decisions use SADR.\n+_DECISION_FILENAME = re.compile(r\"^(?:(SADR)-)?(\\d{4})-.*\\.md$\")\n \n \n class Violation(NamedTuple):\n     \"\"\"A citation that does not resolve to exactly one decision record.\"\"\"\n \n     citing_file: str  # repo-relative path of the file that cites the ADR\n+    namespace: str  # \"ADR\" for target decisions; \"SADR\" for Shiki decisions\n     number: str  # the cited four-digit number, e.g. \"0016\"\n     line: int  # 1-based line of the citation\n     reason: str  # \"missing\" (zero matches) or \"ambiguous\" (two or more)\n@@ -49,44 +51,59 @@\n \n     @property\n     def expected_path(self) -> str:\n-        return f\"docs/adr/{self.number}-*.md\"\n+        prefix = \"SADR-\" if self.namespace == \"SADR\" else \"\"\n+        return f\"docs/adr/{prefix}{self.number}-*.md\"\n \n     def describe(self) -> str:\n         \"\"\"Name the citing file, the cited number, and the missing path.\"\"\"\n         if self.reason == \"missing\":\n             return (\n-                f\"{self.citing_file}:{self.line} cites ADR {self.number} \"\n+                f\"{self.citing_file}:{self.line} cites {self.namespace} {self.number} \"\n                 f\"but no document matches {self.expected_path}\"\n             )\n         joined = \", \".join(self.matches)\n         return (\n-            f\"{self.citing_file}:{self.line} cites ADR {self.number} which \"\n+            f\"{self.citing_file}:{self.line} cites {self.namespace} {self.number} which \"\n             f\"resolves to {len(self.matches)} documents (expected exactly one \"\n             f\"at {self.expected_path}): {joined}\"\n         )\n \n \n-def _adr_index(root: Path) -> dict[str, list[Path]]:\n-    \"\"\"Map every four-digit ADR number to the record files that claim it.\"\"\"\n-    index: dict[str, list[Path]] = {}\n+def _adr_index(root: Path) -> dict[tuple[str, str], list[Path]]:\n+    \"\"\"Map each decision namespace and number to the files that claim it.\"\"\"\n+    index: dict[tuple[str, str], list[Path]] = {}\n     adr_dir = root / \"docs\" / \"adr\"\n     if adr_dir.is_dir():\n         for path in sorted(adr_dir.glob(\"*.md\")):\n-            match = _ADR_FILENAME.match(path.name)\n+            match = _DECISION_FILENAME.match(path.name)\n             if match:\n-                index.setdefault(match.group(1), []).append(path)\n+                namespace = \"SADR\" if match.group(1) else \"ADR\"\n+                index.setdefault((namespace, match.group(2)), []).append(path)\n     return index\n \n \n def _citing_files(root: Path, include_docs: bool = True) -> list[Path]:\n-    \"\"\"Files scanned for citations: scripts/*.py, and cheaply the markdown.\"\"\"\n+    \"\"\"Current, shipped text surfaces scanned for decision citations.\"\"\"\n     files = sorted((root / \"scripts\").glob(\"*.py\"))\n+    files += sorted((root / \"scripts\").glob(\"*.sh\"))\n     if include_docs:\n         docs = root / \"docs\"\n         if docs.is_dir():\n             files += sorted(docs.rglob(\"*.md\"))\n         files += sorted(root.glob(\"*.md\"))\n-    return files\n+        pyproject = root / \"pyproject.toml\"\n+        if pyproject.is_file():\n+            files.append(pyproject)\n+        for relative in (\".github\", \".codex\"):\n+            surface = root / relative\n+            if surface.is_dir():\n+                files += sorted(\n+                    path\n+                    for path in surface.rglob(\"*\")\n+                    if path.is_file()\n+                    and path.suffix in {\".md\", \".py\", \".sh\", \".toml\", \".yaml\", \".yml\"}\n+                )\n+    return sorted(set(files))\n \n \n def find_adr_reference_violations(\n@@ -94,10 +111,9 @@\n ) -> list[Violation]:\n     \"\"\"Return every ``ADR ####`` citation under ``root`` that fails to resolve.\n \n-    A citation resolves when exactly one ``docs/adr/####-*.md`` matches its\n-    number. Zero matches is a \"missing\" violation; two or more is \"ambiguous\".\n-    Records that are never cited produce nothing -- the converse is not\n-    required.\n+    A citation resolves when exactly one record in the same namespace matches\n+    its number. Zero matches is a \"missing\" violation; two or more is\n+    \"ambiguous\". Records that are never cited produce nothing.\n     \"\"\"\n     root = Path(root)\n     index = _adr_index(root)\n@@ -110,13 +126,15 @@\n         rel = path.relative_to(root).as_posix()\n         for lineno, line in enumerate(text.splitlines(), start=1):\n             for match in _CITATION.finditer(line):\n-                number = match.group(1)\n-                found = index.get(number, [])\n+                namespace, number = match.groups()\n+                found = index.get((namespace, number), [])\n                 if len(found) == 1:\n                     continue\n                 reason = \"missing\" if not found else \"ambiguous\"\n                 rels = [p.relative_to(root).as_posix() for p in found]\n-                violations.append(Violation(rel, number, lineno, reason, rels))\n+                violations.append(\n+                    Violation(rel, namespace, number, lineno, reason, rels)\n+                )\n     return violations\n \n \n@@ -129,8 +147,8 @@\n         self.assertEqual(\n             violations,\n             [],\n-            \"Every 'ADR ####' citation must resolve to exactly one \"\n-            f\"docs/adr/####-*.md:\\n{detail}\",\n+            \"Every ADR/SADR citation must resolve exactly once in its own \"\n+            f\"docs/adr namespace:\\n{detail}\",\n         )\n \n     def test_scan_actually_reads_scripts(self) -> None:\n@@ -146,8 +164,21 @@\n         # The two records this Goal carried onto the default branch, and which\n         # the scripts cite most, must be present.\n         index = _adr_index(REPO_ROOT)\n-        self.assertIn(\"0016\", index)\n-        self.assertIn(\"0017\", index)\n+        self.assertIn((\"SADR\", \"0016\"), index)\n+        self.assertIn((\"SADR\", \"0017\"), index)\n+\n+    def test_platform_tree_has_only_sadr_0001_through_0019(self) -> None:\n+        adr_dir = REPO_ROOT / \"docs\" / \"adr\"\n+        sadrs = sorted(adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\"))\n+        self.assertEqual(\n+            {path.name.split(\"-\", 2)[1] for path in sadrs},\n+            {f\"{number:04d}\" for number in range(1, 20)},\n+        )\n+        self.assertEqual(\n+            list(adr_dir.glob(\"[0-9][0-9][0-9][0-9]-*.md\")),\n+            [],\n+            \"the Shiki platform tree must not retain legacy numeric SADR paths\",\n+        )\n \n \n class FixtureAdrReferences(unittest.TestCase):\n@@ -180,6 +211,26 @@\n         self.assertIn(\"9991\", message)  # cited number\n         self.assertIn(\"docs/adr/9991-*.md\", message)  # missing path\n \n+    def test_target_adr_does_not_satisfy_missing_platform_sadr(self) -> None:\n+        root = self._make_repo(\n+            adr_names=[\"0042-target-decision.md\"],\n+            script_body='\"\"\"Target ADR 0042 exists, but platform SADR-0042 does not.\"\"\"\\n',\n+        )\n+        violations = find_adr_reference_violations(root, include_docs=False)\n+        self.assertEqual(len(violations), 1)\n+        self.assertEqual(violations[0].namespace, \"SADR\")\n+        self.assertEqual(violations[0].number, \"0042\")\n+        self.assertEqual(violations[0].expected_path, \"docs/adr/SADR-0042-*.md\")\n+\n+    def test_target_adr_and_platform_sadr_may_reuse_a_number(self) -> None:\n+        root = self._make_repo(\n+            adr_names=[\"0042-target-decision.md\", \"SADR-0042-platform-decision.md\"],\n+            script_body='\"\"\"Target ADR 0042 and platform SADR-0042 are distinct.\"\"\"\\n',\n+        )\n+        self.assertEqual(\n+            find_adr_reference_violations(root, include_docs=False), []\n+        )\n+\n     def test_uncited_adr_is_allowed(self) -> None:\n         # An ADR exists but nothing cites it -> not a violation. The converse\n         # (every ADR must be cited) is explicitly NOT required.\ndiff --git a/tests/test_sadr_install_migration.py b/tests/test_sadr_install_migration.py\nnew file mode 100644\nindex 0000000000000000000000000000000000000000..a6398c81377f00c09aa2cb684980b070cfaf714c\n--- /dev/null\n+++ b/tests/test_sadr_install_migration.py\n@@ -0,0 +1,56 @@\n+\"\"\"SADR installation and collision-safe legacy upgrade behavior.\"\"\"\n+\n+from __future__ import annotations\n+\n+import shutil\n+import tempfile\n+import unittest\n+from pathlib import Path\n+\n+import shiki_test_support  # noqa: F401  (path bootstrap)\n+\n+import shiki_installer\n+from tests.test_adr_references import find_adr_reference_violations\n+\n+\n+PRODUCT_NUMBERS = tuple(range(13, 19))\n+\n+\n+class FreshSadrInstallTests(unittest.TestCase):\n+    def setUp(self) -> None:\n+        self.tmp = Path(tempfile.mkdtemp(prefix=\"shiki-sadr-fresh-\"))\n+        self.addCleanup(lambda: shutil.rmtree(self.tmp, ignore_errors=True))\n+        self.target = self.tmp / \"target\"\n+        self.adr_dir = self.target / \"docs\" / \"adr\"\n+        self.adr_dir.mkdir(parents=True)\n+        self.product_bytes = {\n+            f\"{number:04d}-product-decision.md\": (\n+                f\"# Product ADR {number:04d}\\n\\nTarget-owned bytes {number}.\\n\"\n+            ).encode()\n+            for number in PRODUCT_NUMBERS\n+        }\n+        for name, content in self.product_bytes.items():\n+            (self.adr_dir / name).write_bytes(content)\n+\n+    def test_fresh_install_preserves_product_adrs_and_installs_all_sadrs(self) -> None:\n+        shiki_installer.install_template(self.target, force=False, validate=True)\n+\n+        for name, expected in self.product_bytes.items():\n+            self.assertEqual((self.adr_dir / name).read_bytes(), expected)\n+\n+        sadr_numbers = {\n+            path.name.split(\"-\", 2)[1]\n+            for path in self.adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\")\n+        }\n+        self.assertEqual(sadr_numbers, {f\"{number:04d}\" for number in range(1, 20)})\n+\n+        numeric_records = {\n+            path.name\n+            for path in self.adr_dir.glob(\"[0-9][0-9][0-9][0-9]-*.md\")\n+        }\n+        self.assertEqual(numeric_records, set(self.product_bytes))\n+        self.assertEqual(find_adr_reference_violations(self.target), [])\n+\n+\n+if __name__ == \"__main__\":\n+    unittest.main()\n\ndiff --git a/.codex/skills/shiki/SKILL.md b/.codex/skills/shiki/SKILL.md\nindex c8e2e85d9d75d49cc4f2d6286ee22659c4e53e75..93cded623b2c15d9f65c76491a1c2d813ac9e6c0\n--- a/.codex/skills/shiki/SKILL.md\n+++ b/.codex/skills/shiki/SKILL.md\n@@ -18,6 +18,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n Codex CLI does not register this skill as a custom `/shiki` slash command. When\n the user types `/shiki` in Codex CLI and gets \"Unrecognized command\", explain\n that this is expected: invoke Shiki in Codex with natural language such as\n@@ -69,7 +72,7 @@\n \n ## Responsibilities\n \n-- Claude Code implements and repairs by default (`shiki runner claude`, ADR 0008); Codex implements and repairs tasks explicitly assigned to `codex`.\n+- Claude Code implements and repairs by default (`shiki runner claude`, SADR-0008); Codex implements and repairs tasks explicitly assigned to `codex`.\n - Claude Code Action can act as GitHub-side CCA or reviewer.\n - CCA judges completion.\n - MergeGate authorizes state transitions and merge readiness.\ndiff --git a/.github/prompts/cca-completion-check.md b/.github/prompts/cca-completion-check.md\nindex ed203ad5f302ecc81372d8ab27d2b1232ca97497..fd89950528333ef6242e6d5443904f5392ab7e56\n--- a/.github/prompts/cca-completion-check.md\n+++ b/.github/prompts/cca-completion-check.md\n@@ -19,6 +19,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n ## Required Reading\n \n Read only the files needed to judge the PR contract. Prefer this order:\ndiff --git a/AGENTS.md b/AGENTS.md\nindex 7f041100d49c2a7525f51cb0f5d696a4a6d1c78c..2b700e989ff295987aa8a4b0c5fcb1acd80020da\n--- a/AGENTS.md\n+++ b/AGENTS.md\n@@ -44,6 +44,10 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use\n+`SADR-NNNN-*.md`; target product decisions retain `NNNN-*.md` and the `ADR`\n+namespace.\n+\n Use the canonical source-of-truth order above. Runtime-specific reading orders\n are operational guidance only and must not override or reorder it.\n \ndiff --git a/CLAUDE.md b/CLAUDE.md\nindex 98a932e7841a632866fcd53b0bad0e3af3711d3d..611d491912d6a72a0327a1731b8f33a41acb4baa\n--- a/CLAUDE.md\n+++ b/CLAUDE.md\n@@ -14,6 +14,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n ## Claude Role\n \n Default to planner, default implementer, reviewer, coordinator, documentation editor, CCA-style completion judge, and final-judgment assistant.\ndiff --git a/SYSTEM_PROMPT.md b/SYSTEM_PROMPT.md\nindex aaaea45e3a896c3a44ba89eec8bb84325ecf6c62..2e7c093ebe9e8bb34e0923756f191b4f7fa581f5\n--- a/SYSTEM_PROMPT.md\n+++ b/SYSTEM_PROMPT.md\n@@ -30,6 +30,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n Use the canonical source-of-truth order above. Runtime-specific reading orders\n are operational guidance only and must not override or reorder it. The current\n conversation is non-durable operator input.\ndiff --git a/docs/adr/0001-keep-shiki-as-platform-name.md b/docs/adr/SADR-0001-keep-shiki-as-platform-name.md\nindex 20a9637c3b9d8c97de7ba8db7cfae4d82d9a7dba..8b685439959aada1b221c112f0ac3c51ca8a1695\n--- a/docs/adr/0001-keep-shiki-as-platform-name.md\n+++ b/docs/adr/SADR-0001-keep-shiki-as-platform-name.md\n@@ -1,3 +1,3 @@\n-# Keep Shiki As The Platform Name\n+# SADR-0001: Keep Shiki As The Platform Name\n \n Shiki remains the product name for the GitHub-native agentic engineering control plane. We considered more descriptive names such as MergePilot, MergeGate, and Kiroku, but kept Shiki because it carries the existing goal-seeking framework identity while remaining independent of Hermes, Codex, Claude Code, or any single runtime.\ndiff --git a/docs/adr/0002-use-agents-as-shared-constitution.md b/docs/adr/SADR-0002-use-agents-as-shared-constitution.md\nindex 73de30ecb078120eac540d9bc51855e3cd48d0e3..c5486182bf06fca36ea508496b013c67302fd989\n--- a/docs/adr/0002-use-agents-as-shared-constitution.md\n+++ b/docs/adr/SADR-0002-use-agents-as-shared-constitution.md\n@@ -1,3 +1,3 @@\n-# Use AGENTS As The Shared Agent Constitution\n+# SADR-0002: Use AGENTS As The Shared Agent Constitution\n \n Shiki uses `AGENTS.md` as the runtime-neutral constitution and `CLAUDE.md` as a thin Claude Code wrapper. This keeps Codex, Claude Code, Hermes Runner, and future Agent Runtimes aligned on the same GitHub-first MergeGate rules while still allowing runtime-specific instructions where needed.\ndiff --git a/docs/adr/0003-require-skill-gate-for-mergegate-readiness.md b/docs/adr/SADR-0003-require-skill-gate-for-mergegate-readiness.md\nindex 424be0c9af4c8f0136f1fde68a468c47ff84e764..7fc337987c7df9792f75f181dfcbed75fe5ffff2\n--- a/docs/adr/0003-require-skill-gate-for-mergegate-readiness.md\n+++ b/docs/adr/SADR-0003-require-skill-gate-for-mergegate-readiness.md\n@@ -1,3 +1,3 @@\n-# Require Skill Gate For MergeGate Readiness\n+# SADR-0003: Require Skill Gate For MergeGate Readiness\n \n Shiki requires relevant engineering skills to be invoked before execution when their triggers apply. This makes skills part of MergeGate readiness rather than optional prompt style, because Shiki depends on repeatable planning, issue decomposition, TDD, diagnosis, triage, and architecture review before autonomous or parallel agent work can be trusted.\ndiff --git a/docs/adr/0004-use-github-first-with-shiki-mirror.md b/docs/adr/SADR-0004-use-github-first-with-shiki-mirror.md\nindex d0a3a9174dc646a945a9d325954ffaf00c352040..a79c54ae9ee8eed2a0524192b864da4e22a92d3f\n--- a/docs/adr/0004-use-github-first-with-shiki-mirror.md\n+++ b/docs/adr/SADR-0004-use-github-first-with-shiki-mirror.md\n@@ -1,4 +1,4 @@\n-# ADR 0004: Use GitHub First With A Repository-Local Shiki Mirror\n+# SADR-0004: Use GitHub First With A Repository-Local Shiki Mirror\n \n ## Status\n \ndiff --git a/docs/adr/0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md b/docs/adr/SADR-0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\nindex b339baf49b89d9290e79c3d51b36e7b2bde3bd95..c30a0d8b1fde0224d0ac7820e7870dccf721580d\n--- a/docs/adr/0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\n+++ b/docs/adr/SADR-0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\n@@ -1,4 +1,4 @@\n-# ADR 0005: Use Codex As Front And Claude Code Action As The GitHub Runtime\n+# SADR-0005: Use Codex As Front And Claude Code Action As The GitHub Runtime\n \n ## Status\n \ndiff --git a/docs/adr/0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md b/docs/adr/SADR-0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\nindex 4bcae9d1e52f0a37b8e9fbafa5cd14b6ee1993bd..7a38538a6ae9929567f0ade67e7ea6a60d0b3f7e\n--- a/docs/adr/0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\n+++ b/docs/adr/SADR-0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\n@@ -1,4 +1,4 @@\n-# ADR 0006: Separate Codex Implementation, CCA Completion, And MergeGate Authorization\n+# SADR-0006: Separate Codex Implementation, CCA Completion, And MergeGate Authorization\n \n ## Status\n \ndiff --git a/docs/adr/0007-packaging-and-release.md b/docs/adr/SADR-0007-packaging-and-release.md\nindex 79b84ddb97167372b44641e5b7860e400ce80822..023cd57f8011aa6e6d2ebf612b7c35a7a82c00d4\n--- a/docs/adr/0007-packaging-and-release.md\n+++ b/docs/adr/SADR-0007-packaging-and-release.md\n@@ -1,4 +1,4 @@\n-# ADR 0007: Packaging And Release For Shiki\n+# SADR-0007: Packaging And Release For Shiki\n \n ## Status\n \ndiff --git a/docs/adr/0008-use-claude-code-as-default-implementer.md b/docs/adr/SADR-0008-use-claude-code-as-default-implementer.md\nindex dedf4510b7c7799521422f622029563d373db031..a158a8d444d93764ec515a28133f7dde4af23579\n--- a/docs/adr/0008-use-claude-code-as-default-implementer.md\n+++ b/docs/adr/SADR-0008-use-claude-code-as-default-implementer.md\n@@ -1,4 +1,4 @@\n-# ADR 0008: Use Claude Code As The Default Implementer Runtime\n+# SADR-0008: Use Claude Code As The Default Implementer Runtime\n \n ## Status\n \ndiff --git a/docs/adr/0009-requirements-definition-and-spec-freeze.md b/docs/adr/SADR-0009-requirements-definition-and-spec-freeze.md\nindex 29b3f7871bef9203a8b4a831a65c39eb2f506e42..52e7dd69ad0aa4efe0b68c62678abbf7df02aaec\n--- a/docs/adr/0009-requirements-definition-and-spec-freeze.md\n+++ b/docs/adr/SADR-0009-requirements-definition-and-spec-freeze.md\n@@ -1,4 +1,4 @@\n-# ADR 0009: Merge Grill And Planning Into Requirements Definition With Spec Freeze\n+# SADR-0009: Merge Grill And Planning Into Requirements Definition With Spec Freeze\n \n ## Status\n \ndiff --git a/docs/adr/0010-external-ai-guardian-review.md b/docs/adr/SADR-0010-external-ai-guardian-review.md\nindex e6108bbe8bb4e73e0ff4684c8f78001a80438c4e..e690d4eddd270e683d709c9b679c5859e71eeaaa\n--- a/docs/adr/0010-external-ai-guardian-review.md\n+++ b/docs/adr/SADR-0010-external-ai-guardian-review.md\n@@ -1,4 +1,4 @@\n-# ADR 0010: External AI Guardian Review As A First-Class Approval Authority\n+# SADR-0010: External AI Guardian Review As A First-Class Approval Authority\n \n ## Status\n \ndiff --git a/docs/adr/0011-autonomous-quality-gate-evidence.md b/docs/adr/SADR-0011-autonomous-quality-gate-evidence.md\nindex bca0246c5d46e44afbde15486a75eec425fc02f5..9c93eac16d8c3a6d4b11bd7224e3955102cd7db4\n--- a/docs/adr/0011-autonomous-quality-gate-evidence.md\n+++ b/docs/adr/SADR-0011-autonomous-quality-gate-evidence.md\n@@ -1,4 +1,4 @@\n-# ADR 0011: Autonomous Quality-Gate Evidence \u2014 Deterministic Facts And An Independent Verifier\n+# SADR-0011: Autonomous Quality-Gate Evidence \u2014 Deterministic Facts And An Independent Verifier\n \n ## Status\n \ndiff --git a/docs/adr/0012-autonomous-loop-closeout-to-main.md b/docs/adr/SADR-0012-autonomous-loop-closeout-to-main.md\nindex 637fad6dde4989c9c2bf0f81f676d8d29991123c..ec7d85f81030a676bcbba28b43eb365422a36eac\n--- a/docs/adr/0012-autonomous-loop-closeout-to-main.md\n+++ b/docs/adr/SADR-0012-autonomous-loop-closeout-to-main.md\n@@ -1,4 +1,4 @@\n-# 0012 \u2014 Autonomous loop pushes goal completion to main via a closeout PR\n+# SADR-0012 \u2014 Autonomous loop pushes goal completion to main via a closeout PR\n \n - Status: accepted\n - Date: 2026-06-18\ndiff --git a/docs/adr/0013-bootstrap-grants-actions-approve-pull-requests.md b/docs/adr/SADR-0013-bootstrap-grants-actions-approve-pull-requests.md\nindex b6c69d64f1c5ced346b88d6859586715b3b5bd0e..a793e7277d67ab8ea56286fec289ca689825a17d\n--- a/docs/adr/0013-bootstrap-grants-actions-approve-pull-requests.md\n+++ b/docs/adr/SADR-0013-bootstrap-grants-actions-approve-pull-requests.md\n@@ -1,4 +1,4 @@\n-# ADR 0013: Bootstrap Grants GitHub Actions Permission To Create And Approve Pull Requests\n+# SADR-0013: Bootstrap Grants GitHub Actions Permission To Create And Approve Pull Requests\n \n ## Status\n \ndiff --git a/docs/adr/0014-codex-app-external-ai-guardian-ui-adapter.md b/docs/adr/SADR-0014-codex-app-external-ai-guardian-ui-adapter.md\nindex 238db06279334caa341c117362df9271382a7fcd..65f12e9939321e1b3e389a34c822fc1549e5155d\n--- a/docs/adr/0014-codex-app-external-ai-guardian-ui-adapter.md\n+++ b/docs/adr/SADR-0014-codex-app-external-ai-guardian-ui-adapter.md\n@@ -1,4 +1,4 @@\n-# ADR 0014: Use Codex App As The External AI Guardian UI Adapter\n+# SADR-0014: Use Codex App As The External AI Guardian UI Adapter\n \n ## Status\n \ndiff --git a/docs/adr/0015-contract-approval-before-dispatch.md b/docs/adr/SADR-0015-contract-approval-before-dispatch.md\nindex fad6c656741a20a7684ba16080a7db159f63a52f..7bdd760bb3e82eee4e1410f45c783b03b638d456\n--- a/docs/adr/0015-contract-approval-before-dispatch.md\n+++ b/docs/adr/SADR-0015-contract-approval-before-dispatch.md\n@@ -1,4 +1,4 @@\n-# ADR 0015: Approve The Task Contract Before Dispatch, Not The Diff After It\n+# SADR-0015: Approve The Task Contract Before Dispatch, Not The Diff After It\n \n ## Status\n \ndiff --git a/docs/adr/0016-locks-bound-product-paths.md b/docs/adr/SADR-0016-locks-bound-product-paths.md\nindex b5e850f3f1f7f4ef6e7f9d1763a29a51bd300c45..332cc17313498404f41a145291e38447b5de0dc4\n--- a/docs/adr/0016-locks-bound-product-paths.md\n+++ b/docs/adr/SADR-0016-locks-bound-product-paths.md\n@@ -1,4 +1,4 @@\n-# ADR 0016: Locks bound product paths; the Shiki mirror is bound by record identity\n+# SADR-0016: Locks bound product paths; the Shiki mirror is bound by record identity\n \n ## Status\n \ndiff --git a/docs/adr/0017-bookkeeping-closeouts-do-not-inherit-risk.md b/docs/adr/SADR-0017-bookkeeping-closeouts-do-not-inherit-risk.md\nindex 554e3a0d526a72ca067108c5464cc68c3edcf174..83115f69f1b1b48d544c9b0157f377b91cb53abf\n--- a/docs/adr/0017-bookkeeping-closeouts-do-not-inherit-risk.md\n+++ b/docs/adr/SADR-0017-bookkeeping-closeouts-do-not-inherit-risk.md\n@@ -1,4 +1,4 @@\n-# ADR 0017: A Bookkeeping Closeout Does Not Inherit Its Task's Risk\n+# SADR-0017: A Bookkeeping Closeout Does Not Inherit Its Task's Risk\n \n ## Status\n \ndiff --git a/docs/adr/0018-carried-guardian-approval-across-a-proven-base-sync.md b/docs/adr/SADR-0018-carried-guardian-approval-across-a-proven-base-sync.md\nindex 22358562da390ed8b45cd600da08a43d378b4680..38da0fd95102c3496c3219ce2319fce696a2532a\n--- a/docs/adr/0018-carried-guardian-approval-across-a-proven-base-sync.md\n+++ b/docs/adr/SADR-0018-carried-guardian-approval-across-a-proven-base-sync.md\n@@ -1,4 +1,4 @@\n-# ADR 0018: Carried Guardian Approval Across A Proven Base Sync\n+# SADR-0018: Carried Guardian Approval Across A Proven Base Sync\n \n ## Status\n \ndiff --git a/docs/adr/README.md b/docs/adr/README.md\nindex c70e057a4520c36e042ab68c53cd4a21ef709b71..5da43856836e2ce6a150f8127766e2f88355ab4b\n--- a/docs/adr/README.md\n+++ b/docs/adr/README.md\n@@ -1,40 +1,41 @@\n-# Architecture Decision Records\n+# Decision Record Namespaces\n \n-This directory holds Shiki's Architecture Decision Records (ADRs): durable\n-records of hard-to-reverse platform decisions. ADRs are part of the Shiki source\n-of truth alongside GitHub state, the `.shiki/` mirror, and `CONTEXT.md`.\n+This directory holds two independent decision namespaces:\n \n-## How ADRs Work\n+- Shiki Architecture Decision Records (SADRs), named\n+  `SADR-NNNN-<short-slug>.md`, record hard-to-reverse Shiki platform decisions.\n+- Target Architecture Decision Records (ADRs), named\n+  `NNNN-<short-slug>.md`, belong to the target product.\n \n-- New ADRs are added as `docs/adr/NNNN-<short-slug>.md`, where `NNNN` is the next\n-  zero-padded sequence number.\n-- The **canonical list of ADRs is the set of files matching `docs/adr/*.md`** in\n-  this directory. Browse `docs/adr/*.md` for the authoritative, current set.\n-- Use [`template.md`](template.md) as the starting point for a new ADR.\n-- Record an ADR when a decision is hard to reverse, surprising without context,\n-  and a real tradeoff. Do not write an ADR for routine or easily reversible\n-  choices.\n+The same four-digit number may appear once in each namespace. A platform\n+`SADR-NNNN` citation resolves only to `SADR-NNNN-*.md`; a target `ADR NNNN`\n+citation resolves only to `NNNN-*.md`.\n \n-Using the `docs/adr/*.md` glob as the source of truth means other PRs can add an\n-ADR without editing this index file, avoiding merge contention on a\n-hand-maintained enumeration.\n+## How Decision Records Work\n \n-## Current ADRs (snapshot)\n-\n-The list below is a convenience snapshot and may lag behind the directory. When\n-in doubt, list `docs/adr/*.md`.\n+- New Shiki platform decisions use the next zero-padded SADR number.\n+- New target product decisions use the target's own ADR sequence.\n+- The canonical list is the files in `docs/adr/`; no hand-maintained index is\n+  authoritative.\n+- Use [`template.md`](template.md) as the starting point and choose the filename\n+  namespace that owns the decision.\n+- Record a decision when it is hard to reverse, surprising without context, and\n+  a real tradeoff. Do not write a decision record for routine or easily\n+  reversible choices.\n \n-- [0001 \u2014 Keep Shiki As The Platform Name](0001-keep-shiki-as-platform-name.md)\n-- [0002 \u2014 Use AGENTS As The Shared Agent Constitution](0002-use-agents-as-shared-constitution.md)\n-- [0003 \u2014 Require Skill Gate For MergeGate Readiness](0003-require-skill-gate-for-mergegate-readiness.md)\n-- [0004 \u2014 Use GitHub First With A Repository-Local Shiki Mirror](0004-use-github-first-with-shiki-mirror.md)\n-- [0005 \u2014 Use Codex As Front And Claude Code Action As The GitHub Runtime](0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md)\n-- [0006 \u2014 Separate Codex Implementation, CCA Completion, And MergeGate Authorization](0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md)\n+Using the directory as the source of truth lets independent changes add records\n+without contending on an enumerated index.\n \n-## Adding A New ADR\n+## Adding A Shiki SADR\n \n-1. Copy `template.md` to `docs/adr/NNNN-<short-slug>.md`.\n+1. Copy `template.md` to `docs/adr/SADR-NNNN-<short-slug>.md`.\n 2. Fill in the title, status, context, decision, and consequences.\n-3. Set the status to `Proposed`, then `Accepted` once the decision is approved.\n-4. Open a PR following `CONTRIBUTING.md`. You do not need to edit this index for\n-   the ADR to be discoverable via `docs/adr/*.md`.\n+3. Set the status to `Proposed`, then `Accepted` once the proper authority has\n+   approved the decision.\n+4. Open a PR following `CONTRIBUTING.md`.\n+\n+## Adding A Target ADR\n+\n+1. Copy `template.md` to `docs/adr/NNNN-<short-slug>.md` using the target's own\n+   sequence.\n+2. Cite it as `ADR NNNN`; do not prefix its filename with `SADR-`.\ndiff --git a/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md b/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\nnew file mode 100644\nindex 0000000000000000000000000000000000000000..7455ce1f3bed02ddddb851ac21a1d1a648c9aeba\n--- /dev/null\n+++ b/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\n@@ -0,0 +1,55 @@\n+# SADR-0019: Separate Shiki Platform SADRs From Target ADRs\n+\n+## Status\n+\n+Accepted\n+\n+## Context\n+\n+Shiki installs its platform decision records into `docs/adr/` in every target\n+repository. Shiki and its targets previously used the same numeric\n+`NNNN-<slug>.md` namespace. A target that already owned ADRs 0013 through 0018\n+therefore acquired two records for each number during installation, making an\n+`ADR NNNN` citation ambiguous. Renumbering target decisions would rewrite target\n+history, while renumbering Shiki decisions per target would destroy their stable\n+platform identity.\n+\n+Upgrades also need to remove the former Shiki-owned numeric paths without ever\n+mistaking a target-owned ADR for platform content. The install stamp already\n+records exact shipped paths and content digests, so it can prove ownership only\n+when both still match.\n+\n+## Decision\n+\n+Shiki platform decisions use the `SADR` namespace:\n+\n+- records are named `docs/adr/SADR-NNNN-<slug>.md`;\n+- live platform citations use `SADR-NNNN`;\n+- target product decisions retain `docs/adr/NNNN-<slug>.md` and `ADR NNNN`;\n+- the reference validator resolves the two namespaces independently, so the same\n+  four-digit number may exist once in each namespace.\n+\n+Before a forced upgrade writes anything, the installer inventories the exact\n+former numeric Shiki ADR paths. It may delete a legacy path only when the\n+existing install stamp contains that exact path and its recorded SHA-256 digest\n+matches the file currently on disk. All ownership blockers are reported\n+together and abort the upgrade before its first write. A non-forced install\n+never performs this cleanup.\n+\n+Historical `.shiki/` evidence remains unchanged because it records the namespace\n+that existed when that evidence was produced.\n+\n+## Consequences\n+\n+- Targets may use any ADR number without colliding with Shiki's platform SADRs.\n+- Fresh installs preserve target ADR bytes and install a stable SADR set.\n+- Forced upgrades fail closed when legacy ownership cannot be proven, and a\n+  successful upgrade refreshes the stamp without removed legacy paths.\n+- Current governance, packaging, workflow, prompt, script, and skill references\n+  must move with the platform records.\n+\n+## Alternatives Considered\n+\n+- Collision-only refusal leaves affected targets permanently unupgradable.\n+- Renumbering target ADRs mutates target-owned history.\n+- Per-target Shiki renumbering makes platform citations non-portable.\ndiff --git a/docs/adr/template.md b/docs/adr/template.md\nindex a92a97daddd0b00ab9dd626cc8d86942da2eafed..17dd79175b5fee2105935557f691b40cf9492942\n--- a/docs/adr/template.md\n+++ b/docs/adr/template.md\n@@ -1,14 +1,14 @@\n-# NNNN: <Short Decision Title>\n+# <SADR-NNNN or NNNN>: <Short Decision Title>\n \n ## Status\n \n-Proposed | Accepted | Superseded by ADR-NNNN\n+Proposed | Accepted | Superseded by SADR-NNNN or target ADR NNNN\n \n ## Context\n \n What is the situation, constraint, or force that requires a decision? Reference\n-relevant domain terms (`CONTEXT.md`), prior ADRs, and code reality. State the\n-problem, not the solution.\n+relevant domain terms (`CONTEXT.md`), prior Shiki SADRs or target ADRs, and code\n+reality. State the problem, not the solution.\n \n ## Decision\n \ndiff --git a/docs/agents/domain.md b/docs/agents/domain.md\nindex 258da8ea81e87ac0a6b0b5b5e5c2fde980eaabb7..67aaa6f4b7d0a5aa4d495ce6279073333f7798cb\n--- a/docs/agents/domain.md\n+++ b/docs/agents/domain.md\n@@ -5,7 +5,9 @@\n ## Layout\n \n - `CONTEXT.md` defines Shiki language. Keep it implementation-free.\n-- `docs/adr/` records hard-to-reverse decisions.\n+- `docs/adr/SADR-NNNN-*.md` records Shiki platform decisions.\n+- `docs/adr/NNNN-*.md` records target product decisions without sharing the\n+  SADR namespace.\n - `AGENTS.md` is the runtime-neutral agent constitution.\n - `CLAUDE.md` is the Claude Code wrapper around `AGENTS.md`.\n - `.shiki/` is the Target Repository mirror for Goals, PRDs, plans, Task DAGs, contracts, locks, worktree records, ledger entries, CCA verdicts, repair packets, reports, and handoffs.\ndiff --git a/docs/agents/implementation-policy.md b/docs/agents/implementation-policy.md\nindex c754144a9fb5bf4a38aa82a60e1670c59e626b44..e90d40fe21880b9a09719c5ecfe6bde7284eb3ce\n--- a/docs/agents/implementation-policy.md\n+++ b/docs/agents/implementation-policy.md\n@@ -31,6 +31,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n ## Non-Negotiables\n \n 1. **Planning precedes implementation.** Non-trivial Goals start with `grill-with-docs` and Context & Impact.\ndiff --git a/tests/test_adr_references.py b/tests/test_adr_references.py\nindex 51706329c797d5dd9b47e082a333fa83b261d482..91ab76b724cb973435c45d53cab63793942267e9\n--- a/tests/test_adr_references.py\n+++ b/tests/test_adr_references.py\n@@ -1,4 +1,4 @@\n-\"\"\"Every ``ADR ####`` citation must resolve to exactly one decision record.\n+\"\"\"Every target ADR and Shiki SADR citation resolves in its own namespace.\n \n Background. ``scripts/mergegate_check.py`` justifies ``is_bookkeeping_closeout``\n with \"ADR 0017\" nine times and ``scripts/guardian_approval_signal.py`` cites\n@@ -11,11 +11,12 @@\n and the directory drifted with no signal.\n \n This is the same unbound-pair shape behind other silent failures: a reference\n-and its target with nothing asserting they agree. This test binds them. It scans\n-every ``scripts/*.py`` (plus ``docs/**/*.md`` and the root ``*.md`` governance\n-files, which are cheap) for the ``ADR ####`` pattern and asserts each cited\n-number resolves to exactly one ``docs/adr/####-*.md``. The converse is\n-deliberately NOT required: an ADR may exist without being cited.\n+and its target with nothing asserting they agree. This test binds them. Target\n+``ADR NNNN`` citations resolve only to ``docs/adr/NNNN-*.md``; Shiki\n+``SADR-NNNN`` citations resolve only to ``docs/adr/SADR-NNNN-*.md``. The scan\n+covers current governance docs, scripts, workflows, prompts, packaging metadata,\n+and repo-local Codex guidance while deliberately excluding historical\n+``.shiki`` evidence. The converse is not required: a record may be uncited.\n \n The suite is dependency free: standard-library ``unittest`` only.\n \"\"\"\n@@ -31,17 +32,18 @@\n \n REPO_ROOT = Path(__file__).resolve().parents[1]\n \n-# \"ADR 0016\" -- a four-digit citation. The trailing \\b keeps a five-digit token\n-# from being read as a truncated four-digit number.\n-_CITATION = re.compile(r\"\\bADR (\\d{4})\\b\")\n-# \"0016-locks-bound-product-paths.md\" -- the leading four digits name the record.\n-_ADR_FILENAME = re.compile(r\"^(\\d{4})-.*\\.md$\")\n+# ``ADR 0016`` / ``SADR-0016`` -- a namespace plus four-digit citation. The\n+# trailing ``\\b`` keeps a five-digit token from being read as a truncated one.\n+_CITATION = re.compile(r\"\\b(S?ADR)[ -](\\d{4})\\b\")\n+# Target ADRs retain the numeric filename; Shiki platform decisions use SADR.\n+_DECISION_FILENAME = re.compile(r\"^(?:(SADR)-)?(\\d{4})-.*\\.md$\")\n \n \n class Violation(NamedTuple):\n     \"\"\"A citation that does not resolve to exactly one decision record.\"\"\"\n \n     citing_file: str  # repo-relative path of the file that cites the ADR\n+    namespace: str  # \"ADR\" for target decisions; \"SADR\" for Shiki decisions\n     number: str  # the cited four-digit number, e.g. \"0016\"\n     line: int  # 1-based line of the citation\n     reason: str  # \"missing\" (zero matches) or \"ambiguous\" (two or more)\n@@ -49,44 +51,59 @@\n \n     @property\n     def expected_path(self) -> str:\n-        return f\"docs/adr/{self.number}-*.md\"\n+        prefix = \"SADR-\" if self.namespace == \"SADR\" else \"\"\n+        return f\"docs/adr/{prefix}{self.number}-*.md\"\n \n     def describe(self) -> str:\n         \"\"\"Name the citing file, the cited number, and the missing path.\"\"\"\n         if self.reason == \"missing\":\n             return (\n-                f\"{self.citing_file}:{self.line} cites ADR {self.number} \"\n+                f\"{self.citing_file}:{self.line} cites {self.namespace} {self.number} \"\n                 f\"but no document matches {self.expected_path}\"\n             )\n         joined = \", \".join(self.matches)\n         return (\n-            f\"{self.citing_file}:{self.line} cites ADR {self.number} which \"\n+            f\"{self.citing_file}:{self.line} cites {self.namespace} {self.number} which \"\n             f\"resolves to {len(self.matches)} documents (expected exactly one \"\n             f\"at {self.expected_path}): {joined}\"\n         )\n \n \n-def _adr_index(root: Path) -> dict[str, list[Path]]:\n-    \"\"\"Map every four-digit ADR number to the record files that claim it.\"\"\"\n-    index: dict[str, list[Path]] = {}\n+def _adr_index(root: Path) -> dict[tuple[str, str], list[Path]]:\n+    \"\"\"Map each decision namespace and number to the files that claim it.\"\"\"\n+    index: dict[tuple[str, str], list[Path]] = {}\n     adr_dir = root / \"docs\" / \"adr\"\n     if adr_dir.is_dir():\n         for path in sorted(adr_dir.glob(\"*.md\")):\n-            match = _ADR_FILENAME.match(path.name)\n+            match = _DECISION_FILENAME.match(path.name)\n             if match:\n-                index.setdefault(match.group(1), []).append(path)\n+                namespace = \"SADR\" if match.group(1) else \"ADR\"\n+                index.setdefault((namespace, match.group(2)), []).append(path)\n     return index\n \n \n def _citing_files(root: Path, include_docs: bool = True) -> list[Path]:\n-    \"\"\"Files scanned for citations: scripts/*.py, and cheaply the markdown.\"\"\"\n+    \"\"\"Current, shipped text surfaces scanned for decision citations.\"\"\"\n     files = sorted((root / \"scripts\").glob(\"*.py\"))\n+    files += sorted((root / \"scripts\").glob(\"*.sh\"))\n     if include_docs:\n         docs = root / \"docs\"\n         if docs.is_dir():\n             files += sorted(docs.rglob(\"*.md\"))\n         files += sorted(root.glob(\"*.md\"))\n-    return files\n+        pyproject = root / \"pyproject.toml\"\n+        if pyproject.is_file():\n+            files.append(pyproject)\n+        for relative in (\".github\", \".codex\"):\n+            surface = root / relative\n+            if surface.is_dir():\n+                files += sorted(\n+                    path\n+                    for path in surface.rglob(\"*\")\n+                    if path.is_file()\n+                    and path.suffix in {\".md\", \".py\", \".sh\", \".toml\", \".yaml\", \".yml\"}\n+                )\n+    return sorted(set(files))\n \n \n def find_adr_reference_violations(\n@@ -94,10 +111,9 @@\n ) -> list[Violation]:\n     \"\"\"Return every ``ADR ####`` citation under ``root`` that fails to resolve.\n \n-    A citation resolves when exactly one ``docs/adr/####-*.md`` matches its\n-    number. Zero matches is a \"missing\" violation; two or more is \"ambiguous\".\n-    Records that are never cited produce nothing -- the converse is not\n-    required.\n+    A citation resolves when exactly one record in the same namespace matches\n+    its number. Zero matches is a \"missing\" violation; two or more is\n+    \"ambiguous\". Records that are never cited produce nothing.\n     \"\"\"\n     root = Path(root)\n     index = _adr_index(root)\n@@ -110,13 +126,15 @@\n         rel = path.relative_to(root).as_posix()\n         for lineno, line in enumerate(text.splitlines(), start=1):\n             for match in _CITATION.finditer(line):\n-                number = match.group(1)\n-                found = index.get(number, [])\n+                namespace, number = match.groups()\n+                found = index.get((namespace, number), [])\n                 if len(found) == 1:\n                     continue\n                 reason = \"missing\" if not found else \"ambiguous\"\n                 rels = [p.relative_to(root).as_posix() for p in found]\n-                violations.append(Violation(rel, number, lineno, reason, rels))\n+                violations.append(\n+                    Violation(rel, namespace, number, lineno, reason, rels)\n+                )\n     return violations\n \n \n@@ -129,8 +147,8 @@\n         self.assertEqual(\n             violations,\n             [],\n-            \"Every 'ADR ####' citation must resolve to exactly one \"\n-            f\"docs/adr/####-*.md:\\n{detail}\",\n+            \"Every ADR/SADR citation must resolve exactly once in its own \"\n+            f\"docs/adr namespace:\\n{detail}\",\n         )\n \n     def test_scan_actually_reads_scripts(self) -> None:\n@@ -146,8 +164,21 @@\n         # The two records this Goal carried onto the default branch, and which\n         # the scripts cite most, must be present.\n         index = _adr_index(REPO_ROOT)\n-        self.assertIn(\"0016\", index)\n-        self.assertIn(\"0017\", index)\n+        self.assertIn((\"SADR\", \"0016\"), index)\n+        self.assertIn((\"SADR\", \"0017\"), index)\n+\n+    def test_platform_tree_has_only_sadr_0001_through_0019(self) -> None:\n+        adr_dir = REPO_ROOT / \"docs\" / \"adr\"\n+        sadrs = sorted(adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\"))\n+        self.assertEqual(\n+            {path.name.split(\"-\", 2)[1] for path in sadrs},\n+            {f\"{number:04d}\" for number in range(1, 20)},\n+        )\n+        self.assertEqual(\n+            list(adr_dir.glob(\"[0-9][0-9][0-9][0-9]-*.md\")),\n+            [],\n+            \"the Shiki platform tree must not retain legacy numeric SADR paths\",\n+        )\n \n \n class FixtureAdrReferences(unittest.TestCase):\n@@ -180,6 +211,26 @@\n         self.assertIn(\"9991\", message)  # cited number\n         self.assertIn(\"docs/adr/9991-*.md\", message)  # missing path\n \n+    def test_target_adr_does_not_satisfy_missing_platform_sadr(self) -> None:\n+        root = self._make_repo(\n+            adr_names=[\"0042-target-decision.md\"],\n+            script_body='\"\"\"Target ADR 0042 exists, but platform SADR-0042 does not.\"\"\"\\n',\n+        )\n+        violations = find_adr_reference_violations(root, include_docs=False)\n+        self.assertEqual(len(violations), 1)\n+        self.assertEqual(violations[0].namespace, \"SADR\")\n+        self.assertEqual(violations[0].number, \"0042\")\n+        self.assertEqual(violations[0].expected_path, \"docs/adr/SADR-0042-*.md\")\n+\n+    def test_target_adr_and_platform_sadr_may_reuse_a_number(self) -> None:\n+        root = self._make_repo(\n+            adr_names=[\"0042-target-decision.md\", \"SADR-0042-platform-decision.md\"],\n+            script_body='\"\"\"Target ADR 0042 and platform SADR-0042 are distinct.\"\"\"\\n',\n+        )\n+        self.assertEqual(\n+            find_adr_reference_violations(root, include_docs=False), []\n+        )\n+\n     def test_uncited_adr_is_allowed(self) -> None:\n         # An ADR exists but nothing cites it -> not a violation. The converse\n         # (every ADR must be cited) is explicitly NOT required.\ndiff --git a/tests/test_sadr_install_migration.py b/tests/test_sadr_install_migration.py\nnew file mode 100644\nindex 0000000000000000000000000000000000000000..a6398c81377f00c09aa2cb684980b070cfaf714c\n--- /dev/null\n+++ b/tests/test_sadr_install_migration.py\n@@ -0,0 +1,56 @@\n+\"\"\"SADR installation and collision-safe legacy upgrade behavior.\"\"\"\n+\n+from __future__ import annotations\n+\n+import shutil\n+import tempfile\n+import unittest\n+from pathlib import Path\n+\n+import shiki_test_support  # noqa: F401  (path bootstrap)\n+\n+import shiki_installer\n+from tests.test_adr_references import find_adr_reference_violations\n+\n+\n+PRODUCT_NUMBERS = tuple(range(13, 19))\n+\n+\n+class FreshSadrInstallTests(unittest.TestCase):\n+    def setUp(self) -> None:\n+        self.tmp = Path(tempfile.mkdtemp(prefix=\"shiki-sadr-fresh-\"))\n+        self.addCleanup(lambda: shutil.rmtree(self.tmp, ignore_errors=True))\n+        self.target = self.tmp / \"target\"\n+        self.adr_dir = self.target / \"docs\" / \"adr\"\n+        self.adr_dir.mkdir(parents=True)\n+        self.product_bytes = {\n+            f\"{number:04d}-product-decision.md\": (\n+                f\"# Product ADR {number:04d}\\n\\nTarget-owned bytes {number}.\\n\"\n+            ).encode()\n+            for number in PRODUCT_NUMBERS\n+        }\n+        for name, content in self.product_bytes.items():\n+            (self.adr_dir / name).write_bytes(content)\n+\n+    def test_fresh_install_preserves_product_adrs_and_installs_all_sadrs(self) -> None:\n+        shiki_installer.install_template(self.target, force=False, validate=True)\n+\n+        for name, expected in self.product_bytes.items():\n+            self.assertEqual((self.adr_dir / name).read_bytes(), expected)\n+\n+        sadr_numbers = {\n+            path.name.split(\"-\", 2)[1]\n+            for path in self.adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\")\n+        }\n+        self.assertEqual(sadr_numbers, {f\"{number:04d}\" for number in range(1, 20)})\n+\n+        numeric_records = {\n+            path.name\n+            for path in self.adr_dir.glob(\"[0-9][0-9][0-9][0-9]-*.md\")\n+        }\n+        self.assertEqual(numeric_records, set(self.product_bytes))\n+        self.assertEqual(find_adr_reference_violations(self.target), [])\n+\n+\n+if __name__ == \"__main__\":\n+    unittest.main()\n\nexec\n/bin/zsh -lc \"python3 -m unittest discover -s tests -p 'test_sadr_install_migration.py' -v\" in /private/tmp/shiki-adr-collision.pa0KwY/.worktrees/shiki-t-20260807t073307221254z-2d22d654-introduc\ndiff --git a/.codex/skills/shiki/SKILL.md b/.codex/skills/shiki/SKILL.md\nindex c8e2e85d9d75d49cc4f2d6286ee22659c4e53e75..93cded623b2c15d9f65c76491a1c2d813ac9e6c0\n--- a/.codex/skills/shiki/SKILL.md\n+++ b/.codex/skills/shiki/SKILL.md\n@@ -18,6 +18,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n Codex CLI does not register this skill as a custom `/shiki` slash command. When\n the user types `/shiki` in Codex CLI and gets \"Unrecognized command\", explain\n that this is expected: invoke Shiki in Codex with natural language such as\n@@ -69,7 +72,7 @@\n \n ## Responsibilities\n \n-- Claude Code implements and repairs by default (`shiki runner claude`, ADR 0008); Codex implements and repairs tasks explicitly assigned to `codex`.\n+- Claude Code implements and repairs by default (`shiki runner claude`, SADR-0008); Codex implements and repairs tasks explicitly assigned to `codex`.\n - Claude Code Action can act as GitHub-side CCA or reviewer.\n - CCA judges completion.\n - MergeGate authorizes state transitions and merge readiness.\ndiff --git a/.github/prompts/cca-completion-check.md b/.github/prompts/cca-completion-check.md\nindex ed203ad5f302ecc81372d8ab27d2b1232ca97497..fd89950528333ef6242e6d5443904f5392ab7e56\n--- a/.github/prompts/cca-completion-check.md\n+++ b/.github/prompts/cca-completion-check.md\n@@ -19,6 +19,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n ## Required Reading\n \n Read only the files needed to judge the PR contract. Prefer this order:\ndiff --git a/AGENTS.md b/AGENTS.md\nindex 7f041100d49c2a7525f51cb0f5d696a4a6d1c78c..2b700e989ff295987aa8a4b0c5fcb1acd80020da\n--- a/AGENTS.md\n+++ b/AGENTS.md\n@@ -44,6 +44,10 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use\n+`SADR-NNNN-*.md`; target product decisions retain `NNNN-*.md` and the `ADR`\n+namespace.\n+\n Use the canonical source-of-truth order above. Runtime-specific reading orders\n are operational guidance only and must not override or reorder it.\n \ndiff --git a/CLAUDE.md b/CLAUDE.md\nindex 98a932e7841a632866fcd53b0bad0e3af3711d3d..611d491912d6a72a0327a1731b8f33a41acb4baa\n--- a/CLAUDE.md\n+++ b/CLAUDE.md\n@@ -14,6 +14,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n ## Claude Role\n \n Default to planner, default implementer, reviewer, coordinator, documentation editor, CCA-style completion judge, and final-judgment assistant.\ndiff --git a/SYSTEM_PROMPT.md b/SYSTEM_PROMPT.md\nindex aaaea45e3a896c3a44ba89eec8bb84325ecf6c62..2e7c093ebe9e8bb34e0923756f191b4f7fa581f5\n--- a/SYSTEM_PROMPT.md\n+++ b/SYSTEM_PROMPT.md\n@@ -30,6 +30,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n Use the canonical source-of-truth order above. Runtime-specific reading orders\n are operational guidance only and must not override or reorder it. The current\n conversation is non-durable operator input.\ndiff --git a/docs/adr/0001-keep-shiki-as-platform-name.md b/docs/adr/SADR-0001-keep-shiki-as-platform-name.md\nindex 20a9637c3b9d8c97de7ba8db7cfae4d82d9a7dba..8b685439959aada1b221c112f0ac3c51ca8a1695\n--- a/docs/adr/0001-keep-shiki-as-platform-name.md\n+++ b/docs/adr/SADR-0001-keep-shiki-as-platform-name.md\n@@ -1,3 +1,3 @@\n-# Keep Shiki As The Platform Name\n+# SADR-0001: Keep Shiki As The Platform Name\n \n Shiki remains the product name for the GitHub-native agentic engineering control plane. We considered more descriptive names such as MergePilot, MergeGate, and Kiroku, but kept Shiki because it carries the existing goal-seeking framework identity while remaining independent of Hermes, Codex, Claude Code, or any single runtime.\ndiff --git a/docs/adr/0002-use-agents-as-shared-constitution.md b/docs/adr/SADR-0002-use-agents-as-shared-constitution.md\nindex 73de30ecb078120eac540d9bc51855e3cd48d0e3..c5486182bf06fca36ea508496b013c67302fd989\n--- a/docs/adr/0002-use-agents-as-shared-constitution.md\n+++ b/docs/adr/SADR-0002-use-agents-as-shared-constitution.md\n@@ -1,3 +1,3 @@\n-# Use AGENTS As The Shared Agent Constitution\n+# SADR-0002: Use AGENTS As The Shared Agent Constitution\n \n Shiki uses `AGENTS.md` as the runtime-neutral constitution and `CLAUDE.md` as a thin Claude Code wrapper. This keeps Codex, Claude Code, Hermes Runner, and future Agent Runtimes aligned on the same GitHub-first MergeGate rules while still allowing runtime-specific instructions where needed.\ndiff --git a/docs/adr/0003-require-skill-gate-for-mergegate-readiness.md b/docs/adr/SADR-0003-require-skill-gate-for-mergegate-readiness.md\nindex 424be0c9af4c8f0136f1fde68a468c47ff84e764..7fc337987c7df9792f75f181dfcbed75fe5ffff2\n--- a/docs/adr/0003-require-skill-gate-for-mergegate-readiness.md\n+++ b/docs/adr/SADR-0003-require-skill-gate-for-mergegate-readiness.md\n@@ -1,3 +1,3 @@\n-# Require Skill Gate For MergeGate Readiness\n+# SADR-0003: Require Skill Gate For MergeGate Readiness\n \n Shiki requires relevant engineering skills to be invoked before execution when their triggers apply. This makes skills part of MergeGate readiness rather than optional prompt style, because Shiki depends on repeatable planning, issue decomposition, TDD, diagnosis, triage, and architecture review before autonomous or parallel agent work can be trusted.\ndiff --git a/docs/adr/0004-use-github-first-with-shiki-mirror.md b/docs/adr/SADR-0004-use-github-first-with-shiki-mirror.md\nindex d0a3a9174dc646a945a9d325954ffaf00c352040..a79c54ae9ee8eed2a0524192b864da4e22a92d3f\n--- a/docs/adr/0004-use-github-first-with-shiki-mirror.md\n+++ b/docs/adr/SADR-0004-use-github-first-with-shiki-mirror.md\n@@ -1,4 +1,4 @@\n-# ADR 0004: Use GitHub First With A Repository-Local Shiki Mirror\n+# SADR-0004: Use GitHub First With A Repository-Local Shiki Mirror\n \n ## Status\n \ndiff --git a/docs/adr/0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md b/docs/adr/SADR-0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\nindex b339baf49b89d9290e79c3d51b36e7b2bde3bd95..c30a0d8b1fde0224d0ac7820e7870dccf721580d\n--- a/docs/adr/0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\n+++ b/docs/adr/SADR-0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\n@@ -1,4 +1,4 @@\n-# ADR 0005: Use Codex As Front And Claude Code Action As The GitHub Runtime\n+# SADR-0005: Use Codex As Front And Claude Code Action As The GitHub Runtime\n \n ## Status\n \ndiff --git a/docs/adr/0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md b/docs/adr/SADR-0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\nindex 4bcae9d1e52f0a37b8e9fbafa5cd14b6ee1993bd..7a38538a6ae9929567f0ade67e7ea6a60d0b3f7e\n--- a/docs/adr/0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\n+++ b/docs/adr/SADR-0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\n@@ -1,4 +1,4 @@\n-# ADR 0006: Separate Codex Implementation, CCA Completion, And MergeGate Authorization\n+# SADR-0006: Separate Codex Implementation, CCA Completion, And MergeGate Authorization\n \n ## Status\n \ndiff --git a/docs/adr/0007-packaging-and-release.md b/docs/adr/SADR-0007-packaging-and-release.md\nindex 79b84ddb97167372b44641e5b7860e400ce80822..023cd57f8011aa6e6d2ebf612b7c35a7a82c00d4\n--- a/docs/adr/0007-packaging-and-release.md\n+++ b/docs/adr/SADR-0007-packaging-and-release.md\n@@ -1,4 +1,4 @@\n-# ADR 0007: Packaging And Release For Shiki\n+# SADR-0007: Packaging And Release For Shiki\n \n ## Status\n \ndiff --git a/docs/adr/0008-use-claude-code-as-default-implementer.md b/docs/adr/SADR-0008-use-claude-code-as-default-implementer.md\nindex dedf4510b7c7799521422f622029563d373db031..a158a8d444d93764ec515a28133f7dde4af23579\n--- a/docs/adr/0008-use-claude-code-as-default-implementer.md\n+++ b/docs/adr/SADR-0008-use-claude-code-as-default-implementer.md\n@@ -1,4 +1,4 @@\n-# ADR 0008: Use Claude Code As The Default Implementer Runtime\n+# SADR-0008: Use Claude Code As The Default Implementer Runtime\n \n ## Status\n \ndiff --git a/docs/adr/0009-requirements-definition-and-spec-freeze.md b/docs/adr/SADR-0009-requirements-definition-and-spec-freeze.md\nindex 29b3f7871bef9203a8b4a831a65c39eb2f506e42..52e7dd69ad0aa4efe0b68c62678abbf7df02aaec\n--- a/docs/adr/0009-requirements-definition-and-spec-freeze.md\n+++ b/docs/adr/SADR-0009-requirements-definition-and-spec-freeze.md\n@@ -1,4 +1,4 @@\n-# ADR 0009: Merge Grill And Planning Into Requirements Definition With Spec Freeze\n+# SADR-0009: Merge Grill And Planning Into Requirements Definition With Spec Freeze\n \n ## Status\n \ndiff --git a/docs/adr/0010-external-ai-guardian-review.md b/docs/adr/SADR-0010-external-ai-guardian-review.md\nindex e6108bbe8bb4e73e0ff4684c8f78001a80438c4e..e690d4eddd270e683d709c9b679c5859e71eeaaa\n--- a/docs/adr/0010-external-ai-guardian-review.md\n+++ b/docs/adr/SADR-0010-external-ai-guardian-review.md\n@@ -1,4 +1,4 @@\n-# ADR 0010: External AI Guardian Review As A First-Class Approval Authority\n+# SADR-0010: External AI Guardian Review As A First-Class Approval Authority\n \n ## Status\n \ndiff --git a/docs/adr/0011-autonomous-quality-gate-evidence.md b/docs/adr/SADR-0011-autonomous-quality-gate-evidence.md\nindex bca0246c5d46e44afbde15486a75eec425fc02f5..9c93eac16d8c3a6d4b11bd7224e3955102cd7db4\n--- a/docs/adr/0011-autonomous-quality-gate-evidence.md\n+++ b/docs/adr/SADR-0011-autonomous-quality-gate-evidence.md\n@@ -1,4 +1,4 @@\n-# ADR 0011: Autonomous Quality-Gate Evidence \u2014 Deterministic Facts And An Independent Verifier\n+# SADR-0011: Autonomous Quality-Gate Evidence \u2014 Deterministic Facts And An Independent Verifier\n \n ## Status\n \ndiff --git a/docs/adr/0012-autonomous-loop-closeout-to-main.md b/docs/adr/SADR-0012-autonomous-loop-closeout-to-main.md\nindex 637fad6dde4989c9c2bf0f81f676d8d29991123c..ec7d85f81030a676bcbba28b43eb365422a36eac\n--- a/docs/adr/0012-autonomous-loop-closeout-to-main.md\n+++ b/docs/adr/SADR-0012-autonomous-loop-closeout-to-main.md\n@@ -1,4 +1,4 @@\n-# 0012 \u2014 Autonomous loop pushes goal completion to main via a closeout PR\n+# SADR-0012 \u2014 Autonomous loop pushes goal completion to main via a closeout PR\n \n - Status: accepted\n - Date: 2026-06-18\ndiff --git a/docs/adr/0013-bootstrap-grants-actions-approve-pull-requests.md b/docs/adr/SADR-0013-bootstrap-grants-actions-approve-pull-requests.md\nindex b6c69d64f1c5ced346b88d6859586715b3b5bd0e..a793e7277d67ab8ea56286fec289ca689825a17d\n--- a/docs/adr/0013-bootstrap-grants-actions-approve-pull-requests.md\n+++ b/docs/adr/SADR-0013-bootstrap-grants-actions-approve-pull-requests.md\n@@ -1,4 +1,4 @@\n-# ADR 0013: Bootstrap Grants GitHub Actions Permission To Create And Approve Pull Requests\n+# SADR-0013: Bootstrap Grants GitHub Actions Permission To Create And Approve Pull Requests\n \n ## Status\n \ndiff --git a/docs/adr/0014-codex-app-external-ai-guardian-ui-adapter.md b/docs/adr/SADR-0014-codex-app-external-ai-guardian-ui-adapter.md\nindex 238db06279334caa341c117362df9271382a7fcd..65f12e9939321e1b3e389a34c822fc1549e5155d\n--- a/docs/adr/0014-codex-app-external-ai-guardian-ui-adapter.md\n+++ b/docs/adr/SADR-0014-codex-app-external-ai-guardian-ui-adapter.md\n@@ -1,4 +1,4 @@\n-# ADR 0014: Use Codex App As The External AI Guardian UI Adapter\n+# SADR-0014: Use Codex App As The External AI Guardian UI Adapter\n \n ## Status\n \ndiff --git a/docs/adr/0015-contract-approval-before-dispatch.md b/docs/adr/SADR-0015-contract-approval-before-dispatch.md\nindex fad6c656741a20a7684ba16080a7db159f63a52f..7bdd760bb3e82eee4e1410f45c783b03b638d456\n--- a/docs/adr/0015-contract-approval-before-dispatch.md\n+++ b/docs/adr/SADR-0015-contract-approval-before-dispatch.md\n@@ -1,4 +1,4 @@\n-# ADR 0015: Approve The Task Contract Before Dispatch, Not The Diff After It\n+# SADR-0015: Approve The Task Contract Before Dispatch, Not The Diff After It\n \n ## Status\n \ndiff --git a/docs/adr/0016-locks-bound-product-paths.md b/docs/adr/SADR-0016-locks-bound-product-paths.md\nindex b5e850f3f1f7f4ef6e7f9d1763a29a51bd300c45..332cc17313498404f41a145291e38447b5de0dc4\n--- a/docs/adr/0016-locks-bound-product-paths.md\n+++ b/docs/adr/SADR-0016-locks-bound-product-paths.md\n@@ -1,4 +1,4 @@\n-# ADR 0016: Locks bound product paths; the Shiki mirror is bound by record identity\n+# SADR-0016: Locks bound product paths; the Shiki mirror is bound by record identity\n \n ## Status\n \ndiff --git a/docs/adr/0017-bookkeeping-closeouts-do-not-inherit-risk.md b/docs/adr/SADR-0017-bookkeeping-closeouts-do-not-inherit-risk.md\nindex 554e3a0d526a72ca067108c5464cc68c3edcf174..83115f69f1b1b48d544c9b0157f377b91cb53abf\n--- a/docs/adr/0017-bookkeeping-closeouts-do-not-inherit-risk.md\n+++ b/docs/adr/SADR-0017-bookkeeping-closeouts-do-not-inherit-risk.md\n@@ -1,4 +1,4 @@\n-# ADR 0017: A Bookkeeping Closeout Does Not Inherit Its Task's Risk\n+# SADR-0017: A Bookkeeping Closeout Does Not Inherit Its Task's Risk\n \n ## Status\n \ndiff --git a/docs/adr/0018-carried-guardian-approval-across-a-proven-base-sync.md b/docs/adr/SADR-0018-carried-guardian-approval-across-a-proven-base-sync.md\nindex 22358562da390ed8b45cd600da08a43d378b4680..38da0fd95102c3496c3219ce2319fce696a2532a\n--- a/docs/adr/0018-carried-guardian-approval-across-a-proven-base-sync.md\n+++ b/docs/adr/SADR-0018-carried-guardian-approval-across-a-proven-base-sync.md\n@@ -1,4 +1,4 @@\n-# ADR 0018: Carried Guardian Approval Across A Proven Base Sync\n+# SADR-0018: Carried Guardian Approval Across A Proven Base Sync\n \n ## Status\n \ndiff --git a/docs/adr/README.md b/docs/adr/README.md\nindex c70e057a4520c36e042ab68c53cd4a21ef709b71..5da43856836e2ce6a150f8127766e2f88355ab4b\n--- a/docs/adr/README.md\n+++ b/docs/adr/README.md\n@@ -1,40 +1,41 @@\n-# Architecture Decision Records\n+# Decision Record Namespaces\n \n-This directory holds Shiki's Architecture Decision Records (ADRs): durable\n-records of hard-to-reverse platform decisions. ADRs are part of the Shiki source\n-of truth alongside GitHub state, the `.shiki/` mirror, and `CONTEXT.md`.\n+This directory holds two independent decision namespaces:\n \n-## How ADRs Work\n+- Shiki Architecture Decision Records (SADRs), named\n+  `SADR-NNNN-<short-slug>.md`, record hard-to-reverse Shiki platform decisions.\n+- Target Architecture Decision Records (ADRs), named\n+  `NNNN-<short-slug>.md`, belong to the target product.\n \n-- New ADRs are added as `docs/adr/NNNN-<short-slug>.md`, where `NNNN` is the next\n-  zero-padded sequence number.\n-- The **canonical list of ADRs is the set of files matching `docs/adr/*.md`** in\n-  this directory. Browse `docs/adr/*.md` for the authoritative, current set.\n-- Use [`template.md`](template.md) as the starting point for a new ADR.\n-- Record an ADR when a decision is hard to reverse, surprising without context,\n-  and a real tradeoff. Do not write an ADR for routine or easily reversible\n-  choices.\n+The same four-digit number may appear once in each namespace. A platform\n+`SADR-NNNN` citation resolves only to `SADR-NNNN-*.md`; a target `ADR NNNN`\n+citation resolves only to `NNNN-*.md`.\n \n-Using the `docs/adr/*.md` glob as the source of truth means other PRs can add an\n-ADR without editing this index file, avoiding merge contention on a\n-hand-maintained enumeration.\n+## How Decision Records Work\n \n-## Current ADRs (snapshot)\n-\n-The list below is a convenience snapshot and may lag behind the directory. When\n-in doubt, list `docs/adr/*.md`.\n+- New Shiki platform decisions use the next zero-padded SADR number.\n+- New target product decisions use the target's own ADR sequence.\n+- The canonical list is the files in `docs/adr/`; no hand-maintained index is\n+  authoritative.\n+- Use [`template.md`](template.md) as the starting point and choose the filename\n+  namespace that owns the decision.\n+- Record a decision when it is hard to reverse, surprising without context, and\n+  a real tradeoff. Do not write a decision record for routine or easily\n+  reversible choices.\n \n-- [0001 \u2014 Keep Shiki As The Platform Name](0001-keep-shiki-as-platform-name.md)\n-- [0002 \u2014 Use AGENTS As The Shared Agent Constitution](0002-use-agents-as-shared-constitution.md)\n-- [0003 \u2014 Require Skill Gate For MergeGate Readiness](0003-require-skill-gate-for-mergegate-readiness.md)\n-- [0004 \u2014 Use GitHub First With A Repository-Local Shiki Mirror](0004-use-github-first-with-shiki-mirror.md)\n-- [0005 \u2014 Use Codex As Front And Claude Code Action As The GitHub Runtime](0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md)\n-- [0006 \u2014 Separate Codex Implementation, CCA Completion, And MergeGate Authorization](0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md)\n+Using the directory as the source of truth lets independent changes add records\n+without contending on an enumerated index.\n \n-## Adding A New ADR\n+## Adding A Shiki SADR\n \n-1. Copy `template.md` to `docs/adr/NNNN-<short-slug>.md`.\n+1. Copy `template.md` to `docs/adr/SADR-NNNN-<short-slug>.md`.\n 2. Fill in the title, status, context, decision, and consequences.\n-3. Set the status to `Proposed`, then `Accepted` once the decision is approved.\n-4. Open a PR following `CONTRIBUTING.md`. You do not need to edit this index for\n-   the ADR to be discoverable via `docs/adr/*.md`.\n+3. Set the status to `Proposed`, then `Accepted` once the proper authority has\n+   approved the decision.\n+4. Open a PR following `CONTRIBUTING.md`.\n+\n+## Adding A Target ADR\n+\n+1. Copy `template.md` to `docs/adr/NNNN-<short-slug>.md` using the target's own\n+   sequence.\n+2. Cite it as `ADR NNNN`; do not prefix its filename with `SADR-`.\ndiff --git a/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md b/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\nnew file mode 100644\nindex 0000000000000000000000000000000000000000..7455ce1f3bed02ddddb851ac21a1d1a648c9aeba\n--- /dev/null\n+++ b/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\n@@ -0,0 +1,55 @@\n+# SADR-0019: Separate Shiki Platform SADRs From Target ADRs\n+\n+## Status\n+\n+Accepted\n+\n+## Context\n+\n+Shiki installs its platform decision records into `docs/adr/` in every target\n+repository. Shiki and its targets previously used the same numeric\n+`NNNN-<slug>.md` namespace. A target that already owned ADRs 0013 through 0018\n+therefore acquired two records for each number during installation, making an\n+`ADR NNNN` citation ambiguous. Renumbering target decisions would rewrite target\n+history, while renumbering Shiki decisions per target would destroy their stable\n+platform identity.\n+\n+Upgrades also need to remove the former Shiki-owned numeric paths without ever\n+mistaking a target-owned ADR for platform content. The install stamp already\n+records exact shipped paths and content digests, so it can prove ownership only\n+when both still match.\n+\n+## Decision\n+\n+Shiki platform decisions use the `SADR` namespace:\n+\n+- records are named `docs/adr/SADR-NNNN-<slug>.md`;\n+- live platform citations use `SADR-NNNN`;\n+- target product decisions retain `docs/adr/NNNN-<slug>.md` and `ADR NNNN`;\n+- the reference validator resolves the two namespaces independently, so the same\n+  four-digit number may exist once in each namespace.\n+\n+Before a forced upgrade writes anything, the installer inventories the exact\n+former numeric Shiki ADR paths. It may delete a legacy path only when the\n+existing install stamp contains that exact path and its recorded SHA-256 digest\n+matches the file currently on disk. All ownership blockers are reported\n+together and abort the upgrade before its first write. A non-forced install\n+never performs this cleanup.\n+\n+Historical `.shiki/` evidence remains unchanged because it records the namespace\n+that existed when that evidence was produced.\n+\n+## Consequences\n+\n+- Targets may use any ADR number without colliding with Shiki's platform SADRs.\n+- Fresh installs preserve target ADR bytes and install a stable SADR set.\n+- Forced upgrades fail closed when legacy ownership cannot be proven, and a\n+  successful upgrade refreshes the stamp without removed legacy paths.\n+- Current governance, packaging, workflow, prompt, script, and skill references\n+  must move with the platform records.\n+\n+## Alternatives Considered\n+\n+- Collision-only refusal leaves affected targets permanently unupgradable.\n+- Renumbering target ADRs mutates target-owned history.\n+- Per-target Shiki renumbering makes platform citations non-portable.\ndiff --git a/docs/adr/template.md b/docs/adr/template.md\nindex a92a97daddd0b00ab9dd626cc8d86942da2eafed..17dd79175b5fee2105935557f691b40cf9492942\n--- a/docs/adr/template.md\n+++ b/docs/adr/template.md\n@@ -1,14 +1,14 @@\n-# NNNN: <Short Decision Title>\n+# <SADR-NNNN or NNNN>: <Short Decision Title>\n \n ## Status\n \n-Proposed | Accepted | Superseded by ADR-NNNN\n+Proposed | Accepted | Superseded by SADR-NNNN or target ADR NNNN\n \n ## Context\n \n What is the situation, constraint, or force that requires a decision? Reference\n-relevant domain terms (`CONTEXT.md`), prior ADRs, and code reality. State the\n-problem, not the solution.\n+relevant domain terms (`CONTEXT.md`), prior Shiki SADRs or target ADRs, and code\n+reality. State the problem, not the solution.\n \n ## Decision\n \ndiff --git a/docs/agents/domain.md b/docs/agents/domain.md\nindex 258da8ea81e87ac0a6b0b5b5e5c2fde980eaabb7..67aaa6f4b7d0a5aa4d495ce6279073333f7798cb\n--- a/docs/agents/domain.md\n+++ b/docs/agents/domain.md\n@@ -5,7 +5,9 @@\n ## Layout\n \n - `CONTEXT.md` defines Shiki language. Keep it implementation-free.\n-- `docs/adr/` records hard-to-reverse decisions.\n+- `docs/adr/SADR-NNNN-*.md` records Shiki platform decisions.\n+- `docs/adr/NNNN-*.md` records target product decisions without sharing the\n+  SADR namespace.\n - `AGENTS.md` is the runtime-neutral agent constitution.\n - `CLAUDE.md` is the Claude Code wrapper around `AGENTS.md`.\n - `.shiki/` is the Target Repository mirror for Goals, PRDs, plans, Task DAGs, contracts, locks, worktree records, ledger entries, CCA verdicts, repair packets, reports, and handoffs.\ndiff --git a/docs/agents/implementation-policy.md b/docs/agents/implementation-policy.md\nindex c754144a9fb5bf4a38aa82a60e1670c59e626b44..e90d40fe21880b9a09719c5ecfe6bde7284eb3ce\n--- a/docs/agents/implementation-policy.md\n+++ b/docs/agents/implementation-policy.md\n@@ -31,6 +31,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n ## Non-Negotiables\n \n 1. **Planning precedes implementation.** Non-trivial Goals start with `grill-with-docs` and Context & Impact.\ndiff --git a/tests/test_adr_references.py b/tests/test_adr_references.py\nindex 51706329c797d5dd9b47e082a333fa83b261d482..91ab76b724cb973435c45d53cab63793942267e9\n--- a/tests/test_adr_references.py\n+++ b/tests/test_adr_references.py\n@@ -1,4 +1,4 @@\n-\"\"\"Every ``ADR ####`` citation must resolve to exactly one decision record.\n+\"\"\"Every target ADR and Shiki SADR citation resolves in its own namespace.\n \n Background. ``scripts/mergegate_check.py`` justifies ``is_bookkeeping_closeout``\n with \"ADR 0017\" nine times and ``scripts/guardian_approval_signal.py`` cites\n@@ -11,11 +11,12 @@\n and the directory drifted with no signal.\n \n This is the same unbound-pair shape behind other silent failures: a reference\n-and its target with nothing asserting they agree. This test binds them. It scans\n-every ``scripts/*.py`` (plus ``docs/**/*.md`` and the root ``*.md`` governance\n-files, which are cheap) for the ``ADR ####`` pattern and asserts each cited\n-number resolves to exactly one ``docs/adr/####-*.md``. The converse is\n-deliberately NOT required: an ADR may exist without being cited.\n+and its target with nothing asserting they agree. This test binds them. Target\n+``ADR NNNN`` citations resolve only to ``docs/adr/NNNN-*.md``; Shiki\n+``SADR-NNNN`` citations resolve only to ``docs/adr/SADR-NNNN-*.md``. The scan\n+covers current governance docs, scripts, workflows, prompts, packaging metadata,\n+and repo-local Codex guidance while deliberately excluding historical\n+``.shiki`` evidence. The converse is not required: a record may be uncited.\n \n The suite is dependency free: standard-library ``unittest`` only.\n \"\"\"\n@@ -31,17 +32,18 @@\n \n REPO_ROOT = Path(__file__).resolve().parents[1]\n \n-# \"ADR 0016\" -- a four-digit citation. The trailing \\b keeps a five-digit token\n-# from being read as a truncated four-digit number.\n-_CITATION = re.compile(r\"\\bADR (\\d{4})\\b\")\n-# \"0016-locks-bound-product-paths.md\" -- the leading four digits name the record.\n-_ADR_FILENAME = re.compile(r\"^(\\d{4})-.*\\.md$\")\n+# ``ADR 0016`` / ``SADR-0016`` -- a namespace plus four-digit citation. The\n+# trailing ``\\b`` keeps a five-digit token from being read as a truncated one.\n+_CITATION = re.compile(r\"\\b(S?ADR)[ -](\\d{4})\\b\")\n+# Target ADRs retain the numeric filename; Shiki platform decisions use SADR.\n+_DECISION_FILENAME = re.compile(r\"^(?:(SADR)-)?(\\d{4})-.*\\.md$\")\n \n \n class Violation(NamedTuple):\n     \"\"\"A citation that does not resolve to exactly one decision record.\"\"\"\n \n     citing_file: str  # repo-relative path of the file that cites the ADR\n+    namespace: str  # \"ADR\" for target decisions; \"SADR\" for Shiki decisions\n     number: str  # the cited four-digit number, e.g. \"0016\"\n     line: int  # 1-based line of the citation\n     reason: str  # \"missing\" (zero matches) or \"ambiguous\" (two or more)\n@@ -49,44 +51,59 @@\n \n     @property\n     def expected_path(self) -> str:\n-        return f\"docs/adr/{self.number}-*.md\"\n+        prefix = \"SADR-\" if self.namespace == \"SADR\" else \"\"\n+        return f\"docs/adr/{prefix}{self.number}-*.md\"\n \n     def describe(self) -> str:\n         \"\"\"Name the citing file, the cited number, and the missing path.\"\"\"\n         if self.reason == \"missing\":\n             return (\n-                f\"{self.citing_file}:{self.line} cites ADR {self.number} \"\n+                f\"{self.citing_file}:{self.line} cites {self.namespace} {self.number} \"\n                 f\"but no document matches {self.expected_path}\"\n             )\n         joined = \", \".join(self.matches)\n         return (\n-            f\"{self.citing_file}:{self.line} cites ADR {self.number} which \"\n+            f\"{self.citing_file}:{self.line} cites {self.namespace} {self.number} which \"\n             f\"resolves to {len(self.matches)} documents (expected exactly one \"\n             f\"at {self.expected_path}): {joined}\"\n         )\n \n \n-def _adr_index(root: Path) -> dict[str, list[Path]]:\n-    \"\"\"Map every four-digit ADR number to the record files that claim it.\"\"\"\n-    index: dict[str, list[Path]] = {}\n+def _adr_index(root: Path) -> dict[tuple[str, str], list[Path]]:\n+    \"\"\"Map each decision namespace and number to the files that claim it.\"\"\"\n+    index: dict[tuple[str, str], list[Path]] = {}\n     adr_dir = root / \"docs\" / \"adr\"\n     if adr_dir.is_dir():\n         for path in sorted(adr_dir.glob(\"*.md\")):\n-            match = _ADR_FILENAME.match(path.name)\n+            match = _DECISION_FILENAME.match(path.name)\n             if match:\n-                index.setdefault(match.group(1), []).append(path)\n+                namespace = \"SADR\" if match.group(1) else \"ADR\"\n+                index.setdefault((namespace, match.group(2)), []).append(path)\n     return index\n \n \n def _citing_files(root: Path, include_docs: bool = True) -> list[Path]:\n-    \"\"\"Files scanned for citations: scripts/*.py, and cheaply the markdown.\"\"\"\n+    \"\"\"Current, shipped text surfaces scanned for decision citations.\"\"\"\n     files = sorted((root / \"scripts\").glob(\"*.py\"))\n+    files += sorted((root / \"scripts\").glob(\"*.sh\"))\n     if include_docs:\n         docs = root / \"docs\"\n         if docs.is_dir():\n             files += sorted(docs.rglob(\"*.md\"))\n         files += sorted(root.glob(\"*.md\"))\n-    return files\n+        pyproject = root / \"pyproject.toml\"\n+        if pyproject.is_file():\n+            files.append(pyproject)\n+        for relative in (\".github\", \".codex\"):\n+            surface = root / relative\n+            if surface.is_dir():\n+                files += sorted(\n+                    path\n+                    for path in surface.rglob(\"*\")\n+                    if path.is_file()\n+                    and path.suffix in {\".md\", \".py\", \".sh\", \".toml\", \".yaml\", \".yml\"}\n+                )\n+    return sorted(set(files))\n \n \n def find_adr_reference_violations(\n@@ -94,10 +111,9 @@\n ) -> list[Violation]:\n     \"\"\"Return every ``ADR ####`` citation under ``root`` that fails to resolve.\n \n-    A citation resolves when exactly one ``docs/adr/####-*.md`` matches its\n-    number. Zero matches is a \"missing\" violation; two or more is \"ambiguous\".\n-    Records that are never cited produce nothing -- the converse is not\n-    required.\n+    A citation resolves when exactly one record in the same namespace matches\n+    its number. Zero matches is a \"missing\" violation; two or more is\n+    \"ambiguous\". Records that are never cited produce nothing.\n     \"\"\"\n     root = Path(root)\n     index = _adr_index(root)\n@@ -110,13 +126,15 @@\n         rel = path.relative_to(root).as_posix()\n         for lineno, line in enumerate(text.splitlines(), start=1):\n             for match in _CITATION.finditer(line):\n-                number = match.group(1)\n-                found = index.get(number, [])\n+                namespace, number = match.groups()\n+                found = index.get((namespace, number), [])\n                 if len(found) == 1:\n                     continue\n                 reason = \"missing\" if not found else \"ambiguous\"\n                 rels = [p.relative_to(root).as_posix() for p in found]\n-                violations.append(Violation(rel, number, lineno, reason, rels))\n+                violations.append(\n+                    Violation(rel, namespace, number, lineno, reason, rels)\n+                )\n     return violations\n \n \n@@ -129,8 +147,8 @@\n         self.assertEqual(\n             violations,\n             [],\n-            \"Every 'ADR ####' citation must resolve to exactly one \"\n-            f\"docs/adr/####-*.md:\\n{detail}\",\n+            \"Every ADR/SADR citation must resolve exactly once in its own \"\n+            f\"docs/adr namespace:\\n{detail}\",\n         )\n \n     def test_scan_actually_reads_scripts(self) -> None:\n@@ -146,8 +164,21 @@\n         # The two records this Goal carried onto the default branch, and which\n         # the scripts cite most, must be present.\n         index = _adr_index(REPO_ROOT)\n-        self.assertIn(\"0016\", index)\n-        self.assertIn(\"0017\", index)\n+        self.assertIn((\"SADR\", \"0016\"), index)\n+        self.assertIn((\"SADR\", \"0017\"), index)\n+\n+    def test_platform_tree_has_only_sadr_0001_through_0019(self) -> None:\n+        adr_dir = REPO_ROOT / \"docs\" / \"adr\"\n+        sadrs = sorted(adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\"))\n+        self.assertEqual(\n+            {path.name.split(\"-\", 2)[1] for path in sadrs},\n+            {f\"{number:04d}\" for number in range(1, 20)},\n+        )\n+        self.assertEqual(\n+            list(adr_dir.glob(\"[0-9][0-9][0-9][0-9]-*.md\")),\n+            [],\n+            \"the Shiki platform tree must not retain legacy numeric SADR paths\",\n+        )\n \n \n class FixtureAdrReferences(unittest.TestCase):\n@@ -180,6 +211,26 @@\n         self.assertIn(\"9991\", message)  # cited number\n         self.assertIn(\"docs/adr/9991-*.md\", message)  # missing path\n \n+    def test_target_adr_does_not_satisfy_missing_platform_sadr(self) -> None:\n+        root = self._make_repo(\n+            adr_names=[\"0042-target-decision.md\"],\n+            script_body='\"\"\"Target ADR 0042 exists, but platform SADR-0042 does not.\"\"\"\\n',\n+        )\n+        violations = find_adr_reference_violations(root, include_docs=False)\n+        self.assertEqual(len(violations), 1)\n+        self.assertEqual(violations[0].namespace, \"SADR\")\n+        self.assertEqual(violations[0].number, \"0042\")\n+        self.assertEqual(violations[0].expected_path, \"docs/adr/SADR-0042-*.md\")\n+\n+    def test_target_adr_and_platform_sadr_may_reuse_a_number(self) -> None:\n+        root = self._make_repo(\n+            adr_names=[\"0042-target-decision.md\", \"SADR-0042-platform-decision.md\"],\n+            script_body='\"\"\"Target ADR 0042 and platform SADR-0042 are distinct.\"\"\"\\n',\n+        )\n+        self.assertEqual(\n+            find_adr_reference_violations(root, include_docs=False), []\n+        )\n+\n     def test_uncited_adr_is_allowed(self) -> None:\n         # An ADR exists but nothing cites it -> not a violation. The converse\n         # (every ADR must be cited) is explicitly NOT required.\ndiff --git a/tests/test_sadr_install_migration.py b/tests/test_sadr_install_migration.py\nnew file mode 100644\nindex 0000000000000000000000000000000000000000..a6398c81377f00c09aa2cb684980b070cfaf714c\n--- /dev/null\n+++ b/tests/test_sadr_install_migration.py\n@@ -0,0 +1,56 @@\n+\"\"\"SADR installation and collision-safe legacy upgrade behavior.\"\"\"\n+\n+from __future__ import annotations\n+\n+import shutil\n+import tempfile\n+import unittest\n+from pathlib import Path\n+\n+import shiki_test_support  # noqa: F401  (path bootstrap)\n+\n+import shiki_installer\n+from tests.test_adr_references import find_adr_reference_violations\n+\n+\n+PRODUCT_NUMBERS = tuple(range(13, 19))\n+\n+\n+class FreshSadrInstallTests(unittest.TestCase):\n+    def setUp(self) -> None:\n+        self.tmp = Path(tempfile.mkdtemp(prefix=\"shiki-sadr-fresh-\"))\n+        self.addCleanup(lambda: shutil.rmtree(self.tmp, ignore_errors=True))\n+        self.target = self.tmp / \"target\"\n+        self.adr_dir = self.target / \"docs\" / \"adr\"\n+        self.adr_dir.mkdir(parents=True)\n+        self.product_bytes = {\n+            f\"{number:04d}-product-decision.md\": (\n+                f\"# Product ADR {number:04d}\\n\\nTarget-owned bytes {number}.\\n\"\n+            ).encode()\n+            for number in PRODUCT_NUMBERS\n+        }\n+        for name, content in self.product_bytes.items():\n+            (self.adr_dir / name).write_bytes(content)\n+\n+    def test_fresh_install_preserves_product_adrs_and_installs_all_sadrs(self) -> None:\n+        shiki_installer.install_template(self.target, force=False, validate=True)\n+\n+        for name, expected in self.product_bytes.items():\n+            self.assertEqual((self.adr_dir / name).read_bytes(), expected)\n+\n+        sadr_numbers = {\n+            path.name.split(\"-\", 2)[1]\n+            for path in self.adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\")\n+        }\n+        self.assertEqual(sadr_numbers, {f\"{number:04d}\" for number in range(1, 20)})\n+\n+        numeric_records = {\n+            path.name\n+            for path in self.adr_dir.glob(\"[0-9][0-9][0-9][0-9]-*.md\")\n+        }\n+        self.assertEqual(numeric_records, set(self.product_bytes))\n+        self.assertEqual(find_adr_reference_violations(self.target), [])\n+\n+\n+if __name__ == \"__main__\":\n+    unittest.main()\n\n succeeded in 465ms:\nok\n\n----------------------------------------------------------------------\nRan 1 test in 0.446s\n\nOK\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-8saoezy5/target/bin/shiki\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-8saoezy5/target/AGENTS.md\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-8saoezy5/target/CLAUDE.md\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-8saoezy5/target/CONTEXT.md\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-8saoezy5/target/SYSTEM_PROMPT.md\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-8saoezy5/target/.gitignore\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-8saoezy5/target/.claude/commands/shiki.md\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-8saoezy5/target/.codex/skills/shiki/SKILL.md\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-8saoezy5/target/.shiki/migrations/state.json\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-8saoezy5/target/.shiki/guardian-policy.json\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-8saoezy5/target/.shiki/config.yaml\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-8saoezy5/target/.shiki/schemas/runner-record.schema.json\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-8saoezy5/target/.shiki/schemas/report.schema.json\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-8saoezy5/target/.shiki/schemas/dispatch-guard.schema.json\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-8saoezy5/target/.shiki/schemas/external-ai-guardian-review-packet.schema.json\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-8saoezy5/target/.shiki/schemas/run.schema.json\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-8saoezy5/target/.shiki/schemas/worktree-record.schema.json\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-8saoezy5/target/.shiki/schemas/cca-evidence-manifest.schema.json\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-8saoezy5/target/.shiki/schemas/cca-verdict.schema.json\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-8saoezy5/target/.shiki/schemas/plan.schema.json\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-8saoezy5/target/.shiki/schemas/inbox.schema.json\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-8saoezy5/target/.shiki/schemas/memory-entry.schema.json\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-8saoezy5/target/.shiki/schemas/repair-packet.schema.json\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-8saoezy5/target/.shiki/schemas/smoke.schema.json\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-8saoezy5/target/.shiki/schemas/start.schema.json\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-8saoezy5/target/.shiki/schemas/ledger.schema.json\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-8saoezy5/target/.shiki/schemas/task.schema.json\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-8saoezy5/target/.shiki/schemas/dag.schema.json\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-8saoezy5/target/.shiki/schemas/goal.schema.json\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-8saoezy5/target/.shiki/README.md\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-8saoezy5/target/.shiki/manifest.json\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-8saoezy5/target/.shiki/templates/LEDGER_ENTRY.template.json\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-8saoezy5/target/.shiki/templates/CCA_VERDICT.template.json\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-8saoezy5/target/.shiki/templates/GOAL.template.md\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-8saoezy5/target/.shiki/templates/REPORT.template.md\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-8saoezy5/target/.shiki/templates/DAG.template.json\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-8saoezy5/target/.shiki/templates/worktree-record.schema.json\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-8saoezy5/target/.shiki/templates/REPAIR_PACKET.template.json\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-8saoezy5/target/.shiki/templates/TASK.template.json\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-8saoezy5/target/.shiki/policy.example.yaml\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-8saoezy5/target/.github/ISSUE_TEMPLATE/shiki_goal.yml\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-8saoezy5/target/.github/ISSUE_TEMPLATE/shiki_task.yml\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-8saoezy5/target/.github/CODEOWNERS\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-8saoezy5/target/.github/PULL_REQUEST_TEMPLATE/shiki-task.md\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-8saoezy5/target/.github/prompts/cca-completion-check.md\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-8saoezy5/target/.github/prompts/codex-repair-handoff.md\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-8saoezy5/target/.github/prompts/codex-task-handoff.md\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-8saoezy5/target/.github/workflows/shiki-validate.yml\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-8saoezy5/target/.github/workflows/shiki-claude-review.yml\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-8saoezy5/target/.github/workflows/shiki-cca-completion.yml\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-8saoezy5/target/.github/workflows/shiki-mergegate.yml\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-8saoezy5/target/.github/workflows/shiki-orchestrator.yml\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-8saoezy5/target/docs/agents/node24-workflow-compatibility.md\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-8saoezy5/target/docs/agents/checklists.md\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-8saoezy5/target/docs/agents/codex-handoff.md\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-8saoezy5/target/docs/agents/bootstrap-command.md\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-8saoezy5/target/docs/agents/guardian-policy.md\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-8saoezy5/target/docs/agents/autonomous-loop-e2e-acceptance.md\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-8saoezy5/target/docs/agents/runtime-auth-model.md\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-8saoezy5/target/docs/agents/shiki-doctor.md\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-8saoezy5/target/docs/agents/control-commands.md\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-8saoezy5/target/docs/agents/memory.md\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-8saoezy5/target/docs/agents/shiki-migrations.md\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-8saoezy5/target/docs/agents/decision-control.md\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-8saoezy5/target/docs/agents/domain.md\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-8saoezy5/target/docs/agents/skill-gate.md\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-8saoezy5/target/docs/agents/github-provider-config.md\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-8saoezy5/target/docs/agents/state-classes.md\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-8saoezy5/target/docs/agents/completion-check-agent.md\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-8saoezy5/target/docs/agents/runtime-registry.md\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-8saoezy5/target/docs/agents/implementation-policy.md\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-8saoezy5/target/docs/agents/triage-labels.md\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-8saoezy5/target/docs/agents/shiki-cli-architecture.md\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-8saoezy5/target/docs/agents/issue-tracker.md\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-8saoezy5/target/docs/agents/execution-control.md\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-8saoezy5/target/docs/agents/branch-protection-smoke.md\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-8saoezy5/target/docs/agents/ledger-schema.md\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-8saoezy5/target/docs/agents/evidence-integrity.md\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-8saoezy5/target/docs/adr/SADR-0003-require-skill-gate-for-mergegate-readiness.md\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-8saoezy5/target/docs/adr/template.md\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-8saoezy5/target/docs/adr/SADR-0009-requirements-definition-and-spec-freeze.md\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-8saoezy5/target/docs/adr/SADR-0016-locks-bound-product-paths.md\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-8saoezy5/target/docs/adr/SADR-0011-autonomous-quality-gate-evidence.md\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-8saoezy5/target/docs/adr/SADR-0018-carried-guardian-approval-across-a-proven-base-sync.md\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-8saoezy5/target/docs/adr/SADR-0004-use-github-first-with-shiki-mirror.md\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-8saoezy5/target/docs/adr/SADR-0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-8saoezy5/target/docs/adr/SADR-0015-contract-approval-before-dispatch.md\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-8saoezy5/target/docs/adr/README.md\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-8saoezy5/target/docs/adr/SADR-0017-bookkeeping-closeouts-do-not-inherit-risk.md\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-8saoezy5/target/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-8saoezy5/target/docs/adr/SADR-0007-packaging-and-release.md\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-8saoezy5/target/docs/adr/SADR-0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-8saoezy5/target/docs/adr/SADR-0002-use-agents-as-shared-constitution.md\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-8saoezy5/target/docs/adr/SADR-0014-codex-app-external-ai-guardian-ui-adapter.md\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-8saoezy5/target/docs/adr/SADR-0008-use-claude-code-as-default-implementer.md\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-8saoezy5/target/docs/adr/SADR-0001-keep-shiki-as-platform-name.md\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-8saoezy5/target/docs/adr/SADR-0010-external-ai-guardian-review.md\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-8saoezy5/target/docs/adr/SADR-0013-bootstrap-grants-actions-approve-pull-requests.md\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-8saoezy5/target/docs/adr/SADR-0012-autonomous-loop-closeout-to-main.md\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-8saoezy5/target/skills/engineering/shiki/SKILL.md\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-8saoezy5/target/skills/engineering/setup-matt-pocock-skills/issue-tracker-gitlab.md\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-8saoezy5/target/skills/engineering/setup-matt-pocock-skills/domain.md\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-8saoezy5/target/skills/engineering/setup-matt-pocock-skills/triage-labels.md\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-8saoezy5/target/skills/engineering/setup-matt-pocock-skills/SKILL.md\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-8saoezy5/target/skills/engineering/setup-matt-pocock-skills/issue-tracker-github.md\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-8saoezy5/target/skills/engineering/setup-matt-pocock-skills/issue-tracker-local.md\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-8saoezy5/target/skills/engineering/code-review/SKILL.md\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-8saoezy5/target/skills/engineering/improve-codebase-architecture/LANGUAGE.md\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-8saoezy5/target/skills/engineering/improve-codebase-architecture/SKILL.md\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-8saoezy5/target/skills/engineering/improve-codebase-architecture/HTML-REPORT.md\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-8saoezy5/target/skills/engineering/improve-codebase-architecture/DEEPENING.md\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-8saoezy5/target/skills/engineering/improve-codebase-architecture/INTERFACE-DESIGN.md\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-8saoezy5/target/skills/engineering/triage/OUT-OF-SCOPE.md\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-8saoezy5/target/skills/engineering/triage/SKILL.md\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-8saoezy5/target/skills/engineering/triage/AGENT-BRIEF.md\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-8saoezy5/target/skills/engineering/prototype/SKILL.md\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-8saoezy5/target/skills/engineering/prototype/LOGIC.md\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-8saoezy5/target/skills/engineering/prototype/UI.md\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-8saoezy5/target/skills/engineering/diagnose/scripts/hitl-loop.template.sh\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-8saoezy5/target/skills/engineering/diagnose/SKILL.md\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-8saoezy5/target/skills/engineering/to-issues/SKILL.md\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-8saoezy5/target/skills/engineering/README.md\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-8saoezy5/target/skills/engineering/zoom-out/SKILL.md\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-8saoezy5/target/skills/engineering/grill-with-docs/SKILL.md\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-8saoezy5/target/skills/engineering/grill-with-docs/CONTEXT-FORMAT.md\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-8saoezy5/target/skills/engineering/grill-with-docs/ADR-FORMAT.md\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-8saoezy5/target/skills/engineering/tdd/mocking.md\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-8saoezy5/target/skills/engineering/tdd/deep-modules.md\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-8saoezy5/target/skills/engineering/tdd/tests.md\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-8saoezy5/target/skills/engineering/tdd/SKILL.md\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-8saoezy5/target/skills/engineering/tdd/interface-design.md\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-8saoezy5/target/skills/engineering/tdd/refactoring.md\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-8saoezy5/target/skills/engineering/to-prd/SKILL.md\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-8saoezy5/target/tests/test_lock_release.py\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-8saoezy5/target/tests/test_cca_verdict_usable.py\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-8saoezy5/target/tests/test_id_allocation.py\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-8saoezy5/target/tests/test_mirror_scope.py\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-8saoezy5/target/tests/test_config_loading.py\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-8saoezy5/target/tests/test_cca_verdict_nondegenerate.py\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-8saoezy5/target/tests/test_reopened_task.py\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-8saoezy5/target/tests/test_required_checks_derivation.py\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-8saoezy5/target/tests/test_install_completeness.py\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-8saoezy5/target/tests/test_policy_job_merged_prs.py\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-8saoezy5/target/tests/test_adr_references.py\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-8saoezy5/target/tests/test_session_lease.py\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-8saoezy5/target/tests/test_guardian_signal.py\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-8saoezy5/target/tests/test_cca_verdict_consistency.py\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-8saoezy5/target/tests/test_cca_head_sha_injection.py\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-8saoezy5/target/tests/test_sync_proof.py\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-8saoezy5/target/tests/test_guardian_carry.py\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-8saoezy5/target/tests/test_memory_scorecard.py\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-8saoezy5/target/tests/test_workflow_permissions_payload.py\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-8saoezy5/target/tests/test_contract_mode.py\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-8saoezy5/target/tests/test_memory_consult.py\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-8saoezy5/target/tests/test_workflow_invocation_carry.py\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-8saoezy5/target/tests/test_lock_conflict_stop.py\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-8saoezy5/target/tests/test_migrations_target.py\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-8saoezy5/target/tests/test_branch_protection_payload.py\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-8saoezy5/target/tests/test_loop_branch_plumbing.py\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-8saoezy5/target/tests/test_contract_immutability.py\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-8saoezy5/target/tests/test_loop_smoke.py\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-8saoezy5/target/tests/test_worktree_base.py\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-8saoezy5/target/tests/test_secret_set_claude.py\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-8saoezy5/target/tests/test_cca_completeness.py\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-8saoezy5/target/tests/test_paired_invariants.py\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-8saoezy5/target/tests/__init__.py\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-8saoezy5/target/tests/test_bookkeeping_closeout.py\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-8saoezy5/target/tests/test_contract_open.py\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-8saoezy5/target/tests/test_guardian_signal_closeout.py\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-8saoezy5/target/tests/test_cca_evidence_manifest_taskless.py\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-8saoezy5/target/tests/test_codeowners_wiring.py\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-8saoezy5/target/tests/test_guardian_review.py\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-8saoezy5/target/tests/shiki_test_support.py\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-8saoezy5/target/tests/test_guardian_approval_binding.py\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-8saoezy5/target/tests/test_completion_lock_scope.py\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-8saoezy5/target/tests/test_memory_promotion.py\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-8saoezy5/target/tests/test_loop_code_review.py\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-8saoezy5/target/tests/test_loop_verdict_gating.py\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-8saoezy5/target/tests/test_lock_lifecycle.py\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-8saoezy5/target/tests/test_memory_capture.py\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-8saoezy5/target/tests/test_loop_tdd_evidence.py\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-8saoezy5/target/tests/test_migration_mode.py\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-8saoezy5/target/tests/test_dispatch_lock_precondition.py\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-8saoezy5/target/tests/test_dispatch_state_sync.py\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-8saoezy5/target/tests/test_goal_reconcile.py\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-8saoezy5/target/tests/test_runner_adapters.py\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-8saoezy5/target/tests/test_lock_matching.py\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-8saoezy5/target/tests/test_contract_approval.py\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-8saoezy5/target/tests/test_memory_hooks.py\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-8saoezy5/target/tests/test_branch_behind_sync.py\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-8saoezy5/target/tests/test_install_force_semantics.py\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-8saoezy5/target/tests/test_contract_plan_registration.py\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-8saoezy5/target/tests/test_sadr_install_migration.py\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-8saoezy5/target/tests/test_loop_engine.py\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-8saoezy5/target/tests/test_repair_delivery.py\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-8saoezy5/target/tests/test_handoff_consult.py\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-8saoezy5/target/tests/test_loop_smoke3.py\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-8saoezy5/target/tests/test_contract_amendment.py\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-8saoezy5/target/tests/test_bootstrap_safety.py\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-8saoezy5/target/tests/test_guardian_carry_parity.py\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-8saoezy5/target/tests/test_guardian_evidence.py\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-8saoezy5/target/tests/test_loop_smoke2.py\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-8saoezy5/target/tests/test_loop_lock_guard.py\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-8saoezy5/target/tests/test_manifest_staging.py\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-8saoezy5/target/tests/test_loop_lock_validator_hint.py\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-8saoezy5/target/tests/test_guardian_status.py\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-8saoezy5/target/tests/test_workflow_invocation.py\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-8saoezy5/target/tests/test_closeout.py\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-8saoezy5/target/tests/test_mirror_schema_validation.py\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-8saoezy5/target/tests/test_repair_reasons.py\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-8saoezy5/target/scripts/shiki_schema.py\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-8saoezy5/target/scripts/validate_shiki.py\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-8saoezy5/target/scripts/shiki_contracts.py\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-8saoezy5/target/scripts/shiki_jsonschema.py\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-8saoezy5/target/scripts/shiki_evidence.py\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-8saoezy5/target/scripts/shiki_locks.py\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-8saoezy5/target/scripts/shiki_loop.py\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-8saoezy5/target/scripts/shiki_memory.py\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-8saoezy5/target/scripts/shiki_manifest.py\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-8saoezy5/target/scripts/shiki_migrations.py\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-8saoezy5/target/scripts/shiki_provider.py\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-8saoezy5/target/scripts/shiki_workflows.py\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-8saoezy5/target/scripts/enforce_cca_verdict.py\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-8saoezy5/target/scripts/cca_verdict_usable.py\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-8saoezy5/target/scripts/build_cca_evidence_manifest.py\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-8saoezy5/target/scripts/guardian_approval_signal.py\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-8saoezy5/target/scripts/mergegate_check.py\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-8saoezy5/target/scripts/shiki_sync_proof.py\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-8saoezy5/target/scripts/shiki.py\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-8saoezy5/target/scripts/shiki_bootstrap.py\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-8saoezy5/target/scripts/shiki_cli.py\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-8saoezy5/target/scripts/shiki_config.py\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-8saoezy5/target/scripts/shiki_contract_approval.py\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-8saoezy5/target/scripts/shiki_doctor.py\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-8saoezy5/target/scripts/shiki_git.py\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-8saoezy5/target/scripts/shiki_github.py\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-8saoezy5/target/scripts/shiki_guardian.py\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-8saoezy5/target/scripts/shiki_guardian_review.py\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-8saoezy5/target/scripts/shiki_guardian_status.py\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-8saoezy5/target/scripts/shiki_installer.py\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-8saoezy5/target/scripts/shiki_process.py\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-8saoezy5/target/scripts/shiki_runtime.py\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-8saoezy5/target/scripts/shiki_runtime_adapters.py\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-8saoezy5/target/scripts/shiki_runtime_registry.py\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-8saoezy5/target/scripts/shiki_session_lease.py\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-8saoezy5/target/scripts/shiki_state_classes.py\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-8saoezy5/target/scripts/shiki_tasks.py\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-8saoezy5/target/scripts/shiki_state.py\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-8saoezy5/target/scripts/test_shiki_init.sh\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-8saoezy5/target/scripts/test_shiki_control_plane.sh\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-8saoezy5/target/scripts/test_shiki_run_orchestrator.sh\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-8saoezy5/target/scripts/test_shiki_daemon_runner.sh\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-8saoezy5/target/scripts/test_shiki_runner_codex.sh\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-8saoezy5/target/scripts/test_shiki_runner_claude.sh\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-8saoezy5/target/scripts/test_shiki_goal_loop.sh\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-8saoezy5/target/scripts/test_shiki_memory_loop.sh\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-8saoezy5/target/scripts/test_shiki_code_review_gate.sh\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-8saoezy5/target/scripts/test_shiki_start.sh\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-8saoezy5/target/scripts/test_shiki_runtime_auth.sh\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-8saoezy5/target/scripts/test_shiki_runtime_registry.sh\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-8saoezy5/target/scripts/test_shiki_state_classes.sh\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-8saoezy5/target/scripts/test_shiki_provider_config.sh\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-8saoezy5/target/scripts/test_shiki_guardian_policy.sh\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-8saoezy5/target/scripts/test_shiki_evidence_integrity.sh\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-8saoezy5/target/scripts/test_shiki_governance_evidence.sh\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-8saoezy5/target/scripts/test_shiki_doctor.sh\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-8saoezy5/target/scripts/test_shiki_migrations.sh\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-8saoezy5/target/scripts/test_shiki_module_boundaries.sh\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-8saoezy5/target/scripts/test_shiki_shellcheck.sh\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-8saoezy5/target/scripts/test_shiki_validator_hardening.sh\n[shiki] installed /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-8saoezy5/target/scripts/test_shiki_workflow_lint.sh\n[shiki] ensured empty state directory: /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-8saoezy5/target/.shiki/goals\n[shiki] ensured empty state directory: /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-8saoezy5/target/.shiki/plans\n[shiki] ensured empty state directory: /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-8saoezy5/target/.shiki/tasks\n[shiki] ensured empty state directory: /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-8saoezy5/target/.shiki/dag\n[shiki] ensured empty state directory: /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-8saoezy5/target/.shiki/ledger\n[shiki] ensured empty state directory: /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-8saoezy5/target/.shiki/migrations\n[shiki] ensured empty state directory: /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-8saoezy5/target/.shiki/locks\n[shiki] ensured empty state directory: /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-8saoezy5/target/.shiki/worktrees\n[shiki] ensured empty state directory: /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-8saoezy5/target/.shiki/repairs\n[shiki] ensured empty state directory: /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-8saoezy5/target/.shiki/reports\n[shiki] ensured empty state directory: /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-8saoezy5/target/.shiki/runs\n[shiki] ensured empty state directory: /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-8saoezy5/target/.shiki/inbox\n[shiki] ensured empty state directory: /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-8saoezy5/target/.shiki/handoffs\n[shiki] ensured empty state directory: /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-8saoezy5/target/.shiki/runner\n[shiki] ensured empty state directory: /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-8saoezy5/target/.shiki/smoke\n[shiki] ensured empty state directory: /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-8saoezy5/target/.shiki/starts\n[shiki] ensured empty state directory: /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-8saoezy5/target/.shiki/memories\n[shiki] wrote install stamp: /var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/shiki-sadr-fresh-8saoezy5/target/.shiki/install-stamp.json\n[shiki] template summary: no .new files written (no existing project-content or governance file was kept alongside a refreshed copy)\n[shiki] target Shiki validation passed\n\napply patch\npatch: completed\n/private/tmp/shiki-adr-collision.pa0KwY/.worktrees/shiki-t-20260807t073307221254z-2d22d654-introduc/tests/test_sadr_install_migration.py\ndiff --git a/.codex/skills/shiki/SKILL.md b/.codex/skills/shiki/SKILL.md\nindex c8e2e85d9d75d49cc4f2d6286ee22659c4e53e75..93cded623b2c15d9f65c76491a1c2d813ac9e6c0\n--- a/.codex/skills/shiki/SKILL.md\n+++ b/.codex/skills/shiki/SKILL.md\n@@ -18,6 +18,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n Codex CLI does not register this skill as a custom `/shiki` slash command. When\n the user types `/shiki` in Codex CLI and gets \"Unrecognized command\", explain\n that this is expected: invoke Shiki in Codex with natural language such as\n@@ -69,7 +72,7 @@\n \n ## Responsibilities\n \n-- Claude Code implements and repairs by default (`shiki runner claude`, ADR 0008); Codex implements and repairs tasks explicitly assigned to `codex`.\n+- Claude Code implements and repairs by default (`shiki runner claude`, SADR-0008); Codex implements and repairs tasks explicitly assigned to `codex`.\n - Claude Code Action can act as GitHub-side CCA or reviewer.\n - CCA judges completion.\n - MergeGate authorizes state transitions and merge readiness.\ndiff --git a/.github/prompts/cca-completion-check.md b/.github/prompts/cca-completion-check.md\nindex ed203ad5f302ecc81372d8ab27d2b1232ca97497..fd89950528333ef6242e6d5443904f5392ab7e56\n--- a/.github/prompts/cca-completion-check.md\n+++ b/.github/prompts/cca-completion-check.md\n@@ -19,6 +19,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n ## Required Reading\n \n Read only the files needed to judge the PR contract. Prefer this order:\ndiff --git a/AGENTS.md b/AGENTS.md\nindex 7f041100d49c2a7525f51cb0f5d696a4a6d1c78c..2b700e989ff295987aa8a4b0c5fcb1acd80020da\n--- a/AGENTS.md\n+++ b/AGENTS.md\n@@ -44,6 +44,10 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use\n+`SADR-NNNN-*.md`; target product decisions retain `NNNN-*.md` and the `ADR`\n+namespace.\n+\n Use the canonical source-of-truth order above. Runtime-specific reading orders\n are operational guidance only and must not override or reorder it.\n \ndiff --git a/CLAUDE.md b/CLAUDE.md\nindex 98a932e7841a632866fcd53b0bad0e3af3711d3d..611d491912d6a72a0327a1731b8f33a41acb4baa\n--- a/CLAUDE.md\n+++ b/CLAUDE.md\n@@ -14,6 +14,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n ## Claude Role\n \n Default to planner, default implementer, reviewer, coordinator, documentation editor, CCA-style completion judge, and final-judgment assistant.\ndiff --git a/SYSTEM_PROMPT.md b/SYSTEM_PROMPT.md\nindex aaaea45e3a896c3a44ba89eec8bb84325ecf6c62..2e7c093ebe9e8bb34e0923756f191b4f7fa581f5\n--- a/SYSTEM_PROMPT.md\n+++ b/SYSTEM_PROMPT.md\n@@ -30,6 +30,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n Use the canonical source-of-truth order above. Runtime-specific reading orders\n are operational guidance only and must not override or reorder it. The current\n conversation is non-durable operator input.\ndiff --git a/docs/adr/0001-keep-shiki-as-platform-name.md b/docs/adr/SADR-0001-keep-shiki-as-platform-name.md\nindex 20a9637c3b9d8c97de7ba8db7cfae4d82d9a7dba..8b685439959aada1b221c112f0ac3c51ca8a1695\n--- a/docs/adr/0001-keep-shiki-as-platform-name.md\n+++ b/docs/adr/SADR-0001-keep-shiki-as-platform-name.md\n@@ -1,3 +1,3 @@\n-# Keep Shiki As The Platform Name\n+# SADR-0001: Keep Shiki As The Platform Name\n \n Shiki remains the product name for the GitHub-native agentic engineering control plane. We considered more descriptive names such as MergePilot, MergeGate, and Kiroku, but kept Shiki because it carries the existing goal-seeking framework identity while remaining independent of Hermes, Codex, Claude Code, or any single runtime.\ndiff --git a/docs/adr/0002-use-agents-as-shared-constitution.md b/docs/adr/SADR-0002-use-agents-as-shared-constitution.md\nindex 73de30ecb078120eac540d9bc51855e3cd48d0e3..c5486182bf06fca36ea508496b013c67302fd989\n--- a/docs/adr/0002-use-agents-as-shared-constitution.md\n+++ b/docs/adr/SADR-0002-use-agents-as-shared-constitution.md\n@@ -1,3 +1,3 @@\n-# Use AGENTS As The Shared Agent Constitution\n+# SADR-0002: Use AGENTS As The Shared Agent Constitution\n \n Shiki uses `AGENTS.md` as the runtime-neutral constitution and `CLAUDE.md` as a thin Claude Code wrapper. This keeps Codex, Claude Code, Hermes Runner, and future Agent Runtimes aligned on the same GitHub-first MergeGate rules while still allowing runtime-specific instructions where needed.\ndiff --git a/docs/adr/0003-require-skill-gate-for-mergegate-readiness.md b/docs/adr/SADR-0003-require-skill-gate-for-mergegate-readiness.md\nindex 424be0c9af4c8f0136f1fde68a468c47ff84e764..7fc337987c7df9792f75f181dfcbed75fe5ffff2\n--- a/docs/adr/0003-require-skill-gate-for-mergegate-readiness.md\n+++ b/docs/adr/SADR-0003-require-skill-gate-for-mergegate-readiness.md\n@@ -1,3 +1,3 @@\n-# Require Skill Gate For MergeGate Readiness\n+# SADR-0003: Require Skill Gate For MergeGate Readiness\n \n Shiki requires relevant engineering skills to be invoked before execution when their triggers apply. This makes skills part of MergeGate readiness rather than optional prompt style, because Shiki depends on repeatable planning, issue decomposition, TDD, diagnosis, triage, and architecture review before autonomous or parallel agent work can be trusted.\ndiff --git a/docs/adr/0004-use-github-first-with-shiki-mirror.md b/docs/adr/SADR-0004-use-github-first-with-shiki-mirror.md\nindex d0a3a9174dc646a945a9d325954ffaf00c352040..a79c54ae9ee8eed2a0524192b864da4e22a92d3f\n--- a/docs/adr/0004-use-github-first-with-shiki-mirror.md\n+++ b/docs/adr/SADR-0004-use-github-first-with-shiki-mirror.md\n@@ -1,4 +1,4 @@\n-# ADR 0004: Use GitHub First With A Repository-Local Shiki Mirror\n+# SADR-0004: Use GitHub First With A Repository-Local Shiki Mirror\n \n ## Status\n \ndiff --git a/docs/adr/0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md b/docs/adr/SADR-0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\nindex b339baf49b89d9290e79c3d51b36e7b2bde3bd95..c30a0d8b1fde0224d0ac7820e7870dccf721580d\n--- a/docs/adr/0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\n+++ b/docs/adr/SADR-0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\n@@ -1,4 +1,4 @@\n-# ADR 0005: Use Codex As Front And Claude Code Action As The GitHub Runtime\n+# SADR-0005: Use Codex As Front And Claude Code Action As The GitHub Runtime\n \n ## Status\n \ndiff --git a/docs/adr/0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md b/docs/adr/SADR-0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\nindex 4bcae9d1e52f0a37b8e9fbafa5cd14b6ee1993bd..7a38538a6ae9929567f0ade67e7ea6a60d0b3f7e\n--- a/docs/adr/0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\n+++ b/docs/adr/SADR-0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\n@@ -1,4 +1,4 @@\n-# ADR 0006: Separate Codex Implementation, CCA Completion, And MergeGate Authorization\n+# SADR-0006: Separate Codex Implementation, CCA Completion, And MergeGate Authorization\n \n ## Status\n \ndiff --git a/docs/adr/0007-packaging-and-release.md b/docs/adr/SADR-0007-packaging-and-release.md\nindex 79b84ddb97167372b44641e5b7860e400ce80822..023cd57f8011aa6e6d2ebf612b7c35a7a82c00d4\n--- a/docs/adr/0007-packaging-and-release.md\n+++ b/docs/adr/SADR-0007-packaging-and-release.md\n@@ -1,4 +1,4 @@\n-# ADR 0007: Packaging And Release For Shiki\n+# SADR-0007: Packaging And Release For Shiki\n \n ## Status\n \ndiff --git a/docs/adr/0008-use-claude-code-as-default-implementer.md b/docs/adr/SADR-0008-use-claude-code-as-default-implementer.md\nindex dedf4510b7c7799521422f622029563d373db031..a158a8d444d93764ec515a28133f7dde4af23579\n--- a/docs/adr/0008-use-claude-code-as-default-implementer.md\n+++ b/docs/adr/SADR-0008-use-claude-code-as-default-implementer.md\n@@ -1,4 +1,4 @@\n-# ADR 0008: Use Claude Code As The Default Implementer Runtime\n+# SADR-0008: Use Claude Code As The Default Implementer Runtime\n \n ## Status\n \ndiff --git a/docs/adr/0009-requirements-definition-and-spec-freeze.md b/docs/adr/SADR-0009-requirements-definition-and-spec-freeze.md\nindex 29b3f7871bef9203a8b4a831a65c39eb2f506e42..52e7dd69ad0aa4efe0b68c62678abbf7df02aaec\n--- a/docs/adr/0009-requirements-definition-and-spec-freeze.md\n+++ b/docs/adr/SADR-0009-requirements-definition-and-spec-freeze.md\n@@ -1,4 +1,4 @@\n-# ADR 0009: Merge Grill And Planning Into Requirements Definition With Spec Freeze\n+# SADR-0009: Merge Grill And Planning Into Requirements Definition With Spec Freeze\n \n ## Status\n \ndiff --git a/docs/adr/0010-external-ai-guardian-review.md b/docs/adr/SADR-0010-external-ai-guardian-review.md\nindex e6108bbe8bb4e73e0ff4684c8f78001a80438c4e..e690d4eddd270e683d709c9b679c5859e71eeaaa\n--- a/docs/adr/0010-external-ai-guardian-review.md\n+++ b/docs/adr/SADR-0010-external-ai-guardian-review.md\n@@ -1,4 +1,4 @@\n-# ADR 0010: External AI Guardian Review As A First-Class Approval Authority\n+# SADR-0010: External AI Guardian Review As A First-Class Approval Authority\n \n ## Status\n \ndiff --git a/docs/adr/0011-autonomous-quality-gate-evidence.md b/docs/adr/SADR-0011-autonomous-quality-gate-evidence.md\nindex bca0246c5d46e44afbde15486a75eec425fc02f5..9c93eac16d8c3a6d4b11bd7224e3955102cd7db4\n--- a/docs/adr/0011-autonomous-quality-gate-evidence.md\n+++ b/docs/adr/SADR-0011-autonomous-quality-gate-evidence.md\n@@ -1,4 +1,4 @@\n-# ADR 0011: Autonomous Quality-Gate Evidence \u2014 Deterministic Facts And An Independent Verifier\n+# SADR-0011: Autonomous Quality-Gate Evidence \u2014 Deterministic Facts And An Independent Verifier\n \n ## Status\n \ndiff --git a/docs/adr/0012-autonomous-loop-closeout-to-main.md b/docs/adr/SADR-0012-autonomous-loop-closeout-to-main.md\nindex 637fad6dde4989c9c2bf0f81f676d8d29991123c..ec7d85f81030a676bcbba28b43eb365422a36eac\n--- a/docs/adr/0012-autonomous-loop-closeout-to-main.md\n+++ b/docs/adr/SADR-0012-autonomous-loop-closeout-to-main.md\n@@ -1,4 +1,4 @@\n-# 0012 \u2014 Autonomous loop pushes goal completion to main via a closeout PR\n+# SADR-0012 \u2014 Autonomous loop pushes goal completion to main via a closeout PR\n \n - Status: accepted\n - Date: 2026-06-18\ndiff --git a/docs/adr/0013-bootstrap-grants-actions-approve-pull-requests.md b/docs/adr/SADR-0013-bootstrap-grants-actions-approve-pull-requests.md\nindex b6c69d64f1c5ced346b88d6859586715b3b5bd0e..a793e7277d67ab8ea56286fec289ca689825a17d\n--- a/docs/adr/0013-bootstrap-grants-actions-approve-pull-requests.md\n+++ b/docs/adr/SADR-0013-bootstrap-grants-actions-approve-pull-requests.md\n@@ -1,4 +1,4 @@\n-# ADR 0013: Bootstrap Grants GitHub Actions Permission To Create And Approve Pull Requests\n+# SADR-0013: Bootstrap Grants GitHub Actions Permission To Create And Approve Pull Requests\n \n ## Status\n \ndiff --git a/docs/adr/0014-codex-app-external-ai-guardian-ui-adapter.md b/docs/adr/SADR-0014-codex-app-external-ai-guardian-ui-adapter.md\nindex 238db06279334caa341c117362df9271382a7fcd..65f12e9939321e1b3e389a34c822fc1549e5155d\n--- a/docs/adr/0014-codex-app-external-ai-guardian-ui-adapter.md\n+++ b/docs/adr/SADR-0014-codex-app-external-ai-guardian-ui-adapter.md\n@@ -1,4 +1,4 @@\n-# ADR 0014: Use Codex App As The External AI Guardian UI Adapter\n+# SADR-0014: Use Codex App As The External AI Guardian UI Adapter\n \n ## Status\n \ndiff --git a/docs/adr/0015-contract-approval-before-dispatch.md b/docs/adr/SADR-0015-contract-approval-before-dispatch.md\nindex fad6c656741a20a7684ba16080a7db159f63a52f..7bdd760bb3e82eee4e1410f45c783b03b638d456\n--- a/docs/adr/0015-contract-approval-before-dispatch.md\n+++ b/docs/adr/SADR-0015-contract-approval-before-dispatch.md\n@@ -1,4 +1,4 @@\n-# ADR 0015: Approve The Task Contract Before Dispatch, Not The Diff After It\n+# SADR-0015: Approve The Task Contract Before Dispatch, Not The Diff After It\n \n ## Status\n \ndiff --git a/docs/adr/0016-locks-bound-product-paths.md b/docs/adr/SADR-0016-locks-bound-product-paths.md\nindex b5e850f3f1f7f4ef6e7f9d1763a29a51bd300c45..332cc17313498404f41a145291e38447b5de0dc4\n--- a/docs/adr/0016-locks-bound-product-paths.md\n+++ b/docs/adr/SADR-0016-locks-bound-product-paths.md\n@@ -1,4 +1,4 @@\n-# ADR 0016: Locks bound product paths; the Shiki mirror is bound by record identity\n+# SADR-0016: Locks bound product paths; the Shiki mirror is bound by record identity\n \n ## Status\n \ndiff --git a/docs/adr/0017-bookkeeping-closeouts-do-not-inherit-risk.md b/docs/adr/SADR-0017-bookkeeping-closeouts-do-not-inherit-risk.md\nindex 554e3a0d526a72ca067108c5464cc68c3edcf174..83115f69f1b1b48d544c9b0157f377b91cb53abf\n--- a/docs/adr/0017-bookkeeping-closeouts-do-not-inherit-risk.md\n+++ b/docs/adr/SADR-0017-bookkeeping-closeouts-do-not-inherit-risk.md\n@@ -1,4 +1,4 @@\n-# ADR 0017: A Bookkeeping Closeout Does Not Inherit Its Task's Risk\n+# SADR-0017: A Bookkeeping Closeout Does Not Inherit Its Task's Risk\n \n ## Status\n \ndiff --git a/docs/adr/0018-carried-guardian-approval-across-a-proven-base-sync.md b/docs/adr/SADR-0018-carried-guardian-approval-across-a-proven-base-sync.md\nindex 22358562da390ed8b45cd600da08a43d378b4680..38da0fd95102c3496c3219ce2319fce696a2532a\n--- a/docs/adr/0018-carried-guardian-approval-across-a-proven-base-sync.md\n+++ b/docs/adr/SADR-0018-carried-guardian-approval-across-a-proven-base-sync.md\n@@ -1,4 +1,4 @@\n-# ADR 0018: Carried Guardian Approval Across A Proven Base Sync\n+# SADR-0018: Carried Guardian Approval Across A Proven Base Sync\n \n ## Status\n \ndiff --git a/docs/adr/README.md b/docs/adr/README.md\nindex c70e057a4520c36e042ab68c53cd4a21ef709b71..5da43856836e2ce6a150f8127766e2f88355ab4b\n--- a/docs/adr/README.md\n+++ b/docs/adr/README.md\n@@ -1,40 +1,41 @@\n-# Architecture Decision Records\n+# Decision Record Namespaces\n \n-This directory holds Shiki's Architecture Decision Records (ADRs): durable\n-records of hard-to-reverse platform decisions. ADRs are part of the Shiki source\n-of truth alongside GitHub state, the `.shiki/` mirror, and `CONTEXT.md`.\n+This directory holds two independent decision namespaces:\n \n-## How ADRs Work\n+- Shiki Architecture Decision Records (SADRs), named\n+  `SADR-NNNN-<short-slug>.md`, record hard-to-reverse Shiki platform decisions.\n+- Target Architecture Decision Records (ADRs), named\n+  `NNNN-<short-slug>.md`, belong to the target product.\n \n-- New ADRs are added as `docs/adr/NNNN-<short-slug>.md`, where `NNNN` is the next\n-  zero-padded sequence number.\n-- The **canonical list of ADRs is the set of files matching `docs/adr/*.md`** in\n-  this directory. Browse `docs/adr/*.md` for the authoritative, current set.\n-- Use [`template.md`](template.md) as the starting point for a new ADR.\n-- Record an ADR when a decision is hard to reverse, surprising without context,\n-  and a real tradeoff. Do not write an ADR for routine or easily reversible\n-  choices.\n+The same four-digit number may appear once in each namespace. A platform\n+`SADR-NNNN` citation resolves only to `SADR-NNNN-*.md`; a target `ADR NNNN`\n+citation resolves only to `NNNN-*.md`.\n \n-Using the `docs/adr/*.md` glob as the source of truth means other PRs can add an\n-ADR without editing this index file, avoiding merge contention on a\n-hand-maintained enumeration.\n+## How Decision Records Work\n \n-## Current ADRs (snapshot)\n-\n-The list below is a convenience snapshot and may lag behind the directory. When\n-in doubt, list `docs/adr/*.md`.\n+- New Shiki platform decisions use the next zero-padded SADR number.\n+- New target product decisions use the target's own ADR sequence.\n+- The canonical list is the files in `docs/adr/`; no hand-maintained index is\n+  authoritative.\n+- Use [`template.md`](template.md) as the starting point and choose the filename\n+  namespace that owns the decision.\n+- Record a decision when it is hard to reverse, surprising without context, and\n+  a real tradeoff. Do not write a decision record for routine or easily\n+  reversible choices.\n \n-- [0001 \u2014 Keep Shiki As The Platform Name](0001-keep-shiki-as-platform-name.md)\n-- [0002 \u2014 Use AGENTS As The Shared Agent Constitution](0002-use-agents-as-shared-constitution.md)\n-- [0003 \u2014 Require Skill Gate For MergeGate Readiness](0003-require-skill-gate-for-mergegate-readiness.md)\n-- [0004 \u2014 Use GitHub First With A Repository-Local Shiki Mirror](0004-use-github-first-with-shiki-mirror.md)\n-- [0005 \u2014 Use Codex As Front And Claude Code Action As The GitHub Runtime](0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md)\n-- [0006 \u2014 Separate Codex Implementation, CCA Completion, And MergeGate Authorization](0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md)\n+Using the directory as the source of truth lets independent changes add records\n+without contending on an enumerated index.\n \n-## Adding A New ADR\n+## Adding A Shiki SADR\n \n-1. Copy `template.md` to `docs/adr/NNNN-<short-slug>.md`.\n+1. Copy `template.md` to `docs/adr/SADR-NNNN-<short-slug>.md`.\n 2. Fill in the title, status, context, decision, and consequences.\n-3. Set the status to `Proposed`, then `Accepted` once the decision is approved.\n-4. Open a PR following `CONTRIBUTING.md`. You do not need to edit this index for\n-   the ADR to be discoverable via `docs/adr/*.md`.\n+3. Set the status to `Proposed`, then `Accepted` once the proper authority has\n+   approved the decision.\n+4. Open a PR following `CONTRIBUTING.md`.\n+\n+## Adding A Target ADR\n+\n+1. Copy `template.md` to `docs/adr/NNNN-<short-slug>.md` using the target's own\n+   sequence.\n+2. Cite it as `ADR NNNN`; do not prefix its filename with `SADR-`.\ndiff --git a/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md b/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\nnew file mode 100644\nindex 0000000000000000000000000000000000000000..7455ce1f3bed02ddddb851ac21a1d1a648c9aeba\n--- /dev/null\n+++ b/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\n@@ -0,0 +1,55 @@\n+# SADR-0019: Separate Shiki Platform SADRs From Target ADRs\n+\n+## Status\n+\n+Accepted\n+\n+## Context\n+\n+Shiki installs its platform decision records into `docs/adr/` in every target\n+repository. Shiki and its targets previously used the same numeric\n+`NNNN-<slug>.md` namespace. A target that already owned ADRs 0013 through 0018\n+therefore acquired two records for each number during installation, making an\n+`ADR NNNN` citation ambiguous. Renumbering target decisions would rewrite target\n+history, while renumbering Shiki decisions per target would destroy their stable\n+platform identity.\n+\n+Upgrades also need to remove the former Shiki-owned numeric paths without ever\n+mistaking a target-owned ADR for platform content. The install stamp already\n+records exact shipped paths and content digests, so it can prove ownership only\n+when both still match.\n+\n+## Decision\n+\n+Shiki platform decisions use the `SADR` namespace:\n+\n+- records are named `docs/adr/SADR-NNNN-<slug>.md`;\n+- live platform citations use `SADR-NNNN`;\n+- target product decisions retain `docs/adr/NNNN-<slug>.md` and `ADR NNNN`;\n+- the reference validator resolves the two namespaces independently, so the same\n+  four-digit number may exist once in each namespace.\n+\n+Before a forced upgrade writes anything, the installer inventories the exact\n+former numeric Shiki ADR paths. It may delete a legacy path only when the\n+existing install stamp contains that exact path and its recorded SHA-256 digest\n+matches the file currently on disk. All ownership blockers are reported\n+together and abort the upgrade before its first write. A non-forced install\n+never performs this cleanup.\n+\n+Historical `.shiki/` evidence remains unchanged because it records the namespace\n+that existed when that evidence was produced.\n+\n+## Consequences\n+\n+- Targets may use any ADR number without colliding with Shiki's platform SADRs.\n+- Fresh installs preserve target ADR bytes and install a stable SADR set.\n+- Forced upgrades fail closed when legacy ownership cannot be proven, and a\n+  successful upgrade refreshes the stamp without removed legacy paths.\n+- Current governance, packaging, workflow, prompt, script, and skill references\n+  must move with the platform records.\n+\n+## Alternatives Considered\n+\n+- Collision-only refusal leaves affected targets permanently unupgradable.\n+- Renumbering target ADRs mutates target-owned history.\n+- Per-target Shiki renumbering makes platform citations non-portable.\ndiff --git a/docs/adr/template.md b/docs/adr/template.md\nindex a92a97daddd0b00ab9dd626cc8d86942da2eafed..17dd79175b5fee2105935557f691b40cf9492942\n--- a/docs/adr/template.md\n+++ b/docs/adr/template.md\n@@ -1,14 +1,14 @@\n-# NNNN: <Short Decision Title>\n+# <SADR-NNNN or NNNN>: <Short Decision Title>\n \n ## Status\n \n-Proposed | Accepted | Superseded by ADR-NNNN\n+Proposed | Accepted | Superseded by SADR-NNNN or target ADR NNNN\n \n ## Context\n \n What is the situation, constraint, or force that requires a decision? Reference\n-relevant domain terms (`CONTEXT.md`), prior ADRs, and code reality. State the\n-problem, not the solution.\n+relevant domain terms (`CONTEXT.md`), prior Shiki SADRs or target ADRs, and code\n+reality. State the problem, not the solution.\n \n ## Decision\n \ndiff --git a/docs/agents/domain.md b/docs/agents/domain.md\nindex 258da8ea81e87ac0a6b0b5b5e5c2fde980eaabb7..67aaa6f4b7d0a5aa4d495ce6279073333f7798cb\n--- a/docs/agents/domain.md\n+++ b/docs/agents/domain.md\n@@ -5,7 +5,9 @@\n ## Layout\n \n - `CONTEXT.md` defines Shiki language. Keep it implementation-free.\n-- `docs/adr/` records hard-to-reverse decisions.\n+- `docs/adr/SADR-NNNN-*.md` records Shiki platform decisions.\n+- `docs/adr/NNNN-*.md` records target product decisions without sharing the\n+  SADR namespace.\n - `AGENTS.md` is the runtime-neutral agent constitution.\n - `CLAUDE.md` is the Claude Code wrapper around `AGENTS.md`.\n - `.shiki/` is the Target Repository mirror for Goals, PRDs, plans, Task DAGs, contracts, locks, worktree records, ledger entries, CCA verdicts, repair packets, reports, and handoffs.\ndiff --git a/docs/agents/implementation-policy.md b/docs/agents/implementation-policy.md\nindex c754144a9fb5bf4a38aa82a60e1670c59e626b44..e90d40fe21880b9a09719c5ecfe6bde7284eb3ce\n--- a/docs/agents/implementation-policy.md\n+++ b/docs/agents/implementation-policy.md\n@@ -31,6 +31,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n ## Non-Negotiables\n \n 1. **Planning precedes implementation.** Non-trivial Goals start with `grill-with-docs` and Context & Impact.\ndiff --git a/tests/test_adr_references.py b/tests/test_adr_references.py\nindex 51706329c797d5dd9b47e082a333fa83b261d482..91ab76b724cb973435c45d53cab63793942267e9\n--- a/tests/test_adr_references.py\n+++ b/tests/test_adr_references.py\n@@ -1,4 +1,4 @@\n-\"\"\"Every ``ADR ####`` citation must resolve to exactly one decision record.\n+\"\"\"Every target ADR and Shiki SADR citation resolves in its own namespace.\n \n Background. ``scripts/mergegate_check.py`` justifies ``is_bookkeeping_closeout``\n with \"ADR 0017\" nine times and ``scripts/guardian_approval_signal.py`` cites\n@@ -11,11 +11,12 @@\n and the directory drifted with no signal.\n \n This is the same unbound-pair shape behind other silent failures: a reference\n-and its target with nothing asserting they agree. This test binds them. It scans\n-every ``scripts/*.py`` (plus ``docs/**/*.md`` and the root ``*.md`` governance\n-files, which are cheap) for the ``ADR ####`` pattern and asserts each cited\n-number resolves to exactly one ``docs/adr/####-*.md``. The converse is\n-deliberately NOT required: an ADR may exist without being cited.\n+and its target with nothing asserting they agree. This test binds them. Target\n+``ADR NNNN`` citations resolve only to ``docs/adr/NNNN-*.md``; Shiki\n+``SADR-NNNN`` citations resolve only to ``docs/adr/SADR-NNNN-*.md``. The scan\n+covers current governance docs, scripts, workflows, prompts, packaging metadata,\n+and repo-local Codex guidance while deliberately excluding historical\n+``.shiki`` evidence. The converse is not required: a record may be uncited.\n \n The suite is dependency free: standard-library ``unittest`` only.\n \"\"\"\n@@ -31,17 +32,18 @@\n \n REPO_ROOT = Path(__file__).resolve().parents[1]\n \n-# \"ADR 0016\" -- a four-digit citation. The trailing \\b keeps a five-digit token\n-# from being read as a truncated four-digit number.\n-_CITATION = re.compile(r\"\\bADR (\\d{4})\\b\")\n-# \"0016-locks-bound-product-paths.md\" -- the leading four digits name the record.\n-_ADR_FILENAME = re.compile(r\"^(\\d{4})-.*\\.md$\")\n+# ``ADR 0016`` / ``SADR-0016`` -- a namespace plus four-digit citation. The\n+# trailing ``\\b`` keeps a five-digit token from being read as a truncated one.\n+_CITATION = re.compile(r\"\\b(S?ADR)[ -](\\d{4})\\b\")\n+# Target ADRs retain the numeric filename; Shiki platform decisions use SADR.\n+_DECISION_FILENAME = re.compile(r\"^(?:(SADR)-)?(\\d{4})-.*\\.md$\")\n \n \n class Violation(NamedTuple):\n     \"\"\"A citation that does not resolve to exactly one decision record.\"\"\"\n \n     citing_file: str  # repo-relative path of the file that cites the ADR\n+    namespace: str  # \"ADR\" for target decisions; \"SADR\" for Shiki decisions\n     number: str  # the cited four-digit number, e.g. \"0016\"\n     line: int  # 1-based line of the citation\n     reason: str  # \"missing\" (zero matches) or \"ambiguous\" (two or more)\n@@ -49,44 +51,59 @@\n \n     @property\n     def expected_path(self) -> str:\n-        return f\"docs/adr/{self.number}-*.md\"\n+        prefix = \"SADR-\" if self.namespace == \"SADR\" else \"\"\n+        return f\"docs/adr/{prefix}{self.number}-*.md\"\n \n     def describe(self) -> str:\n         \"\"\"Name the citing file, the cited number, and the missing path.\"\"\"\n         if self.reason == \"missing\":\n             return (\n-                f\"{self.citing_file}:{self.line} cites ADR {self.number} \"\n+                f\"{self.citing_file}:{self.line} cites {self.namespace} {self.number} \"\n                 f\"but no document matches {self.expected_path}\"\n             )\n         joined = \", \".join(self.matches)\n         return (\n-            f\"{self.citing_file}:{self.line} cites ADR {self.number} which \"\n+            f\"{self.citing_file}:{self.line} cites {self.namespace} {self.number} which \"\n             f\"resolves to {len(self.matches)} documents (expected exactly one \"\n             f\"at {self.expected_path}): {joined}\"\n         )\n \n \n-def _adr_index(root: Path) -> dict[str, list[Path]]:\n-    \"\"\"Map every four-digit ADR number to the record files that claim it.\"\"\"\n-    index: dict[str, list[Path]] = {}\n+def _adr_index(root: Path) -> dict[tuple[str, str], list[Path]]:\n+    \"\"\"Map each decision namespace and number to the files that claim it.\"\"\"\n+    index: dict[tuple[str, str], list[Path]] = {}\n     adr_dir = root / \"docs\" / \"adr\"\n     if adr_dir.is_dir():\n         for path in sorted(adr_dir.glob(\"*.md\")):\n-            match = _ADR_FILENAME.match(path.name)\n+            match = _DECISION_FILENAME.match(path.name)\n             if match:\n-                index.setdefault(match.group(1), []).append(path)\n+                namespace = \"SADR\" if match.group(1) else \"ADR\"\n+                index.setdefault((namespace, match.group(2)), []).append(path)\n     return index\n \n \n def _citing_files(root: Path, include_docs: bool = True) -> list[Path]:\n-    \"\"\"Files scanned for citations: scripts/*.py, and cheaply the markdown.\"\"\"\n+    \"\"\"Current, shipped text surfaces scanned for decision citations.\"\"\"\n     files = sorted((root / \"scripts\").glob(\"*.py\"))\n+    files += sorted((root / \"scripts\").glob(\"*.sh\"))\n     if include_docs:\n         docs = root / \"docs\"\n         if docs.is_dir():\n             files += sorted(docs.rglob(\"*.md\"))\n         files += sorted(root.glob(\"*.md\"))\n-    return files\n+        pyproject = root / \"pyproject.toml\"\n+        if pyproject.is_file():\n+            files.append(pyproject)\n+        for relative in (\".github\", \".codex\"):\n+            surface = root / relative\n+            if surface.is_dir():\n+                files += sorted(\n+                    path\n+                    for path in surface.rglob(\"*\")\n+                    if path.is_file()\n+                    and path.suffix in {\".md\", \".py\", \".sh\", \".toml\", \".yaml\", \".yml\"}\n+                )\n+    return sorted(set(files))\n \n \n def find_adr_reference_violations(\n@@ -94,10 +111,9 @@\n ) -> list[Violation]:\n     \"\"\"Return every ``ADR ####`` citation under ``root`` that fails to resolve.\n \n-    A citation resolves when exactly one ``docs/adr/####-*.md`` matches its\n-    number. Zero matches is a \"missing\" violation; two or more is \"ambiguous\".\n-    Records that are never cited produce nothing -- the converse is not\n-    required.\n+    A citation resolves when exactly one record in the same namespace matches\n+    its number. Zero matches is a \"missing\" violation; two or more is\n+    \"ambiguous\". Records that are never cited produce nothing.\n     \"\"\"\n     root = Path(root)\n     index = _adr_index(root)\n@@ -110,13 +126,15 @@\n         rel = path.relative_to(root).as_posix()\n         for lineno, line in enumerate(text.splitlines(), start=1):\n             for match in _CITATION.finditer(line):\n-                number = match.group(1)\n-                found = index.get(number, [])\n+                namespace, number = match.groups()\n+                found = index.get((namespace, number), [])\n                 if len(found) == 1:\n                     continue\n                 reason = \"missing\" if not found else \"ambiguous\"\n                 rels = [p.relative_to(root).as_posix() for p in found]\n-                violations.append(Violation(rel, number, lineno, reason, rels))\n+                violations.append(\n+                    Violation(rel, namespace, number, lineno, reason, rels)\n+                )\n     return violations\n \n \n@@ -129,8 +147,8 @@\n         self.assertEqual(\n             violations,\n             [],\n-            \"Every 'ADR ####' citation must resolve to exactly one \"\n-            f\"docs/adr/####-*.md:\\n{detail}\",\n+            \"Every ADR/SADR citation must resolve exactly once in its own \"\n+            f\"docs/adr namespace:\\n{detail}\",\n         )\n \n     def test_scan_actually_reads_scripts(self) -> None:\n@@ -146,8 +164,21 @@\n         # The two records this Goal carried onto the default branch, and which\n         # the scripts cite most, must be present.\n         index = _adr_index(REPO_ROOT)\n-        self.assertIn(\"0016\", index)\n-        self.assertIn(\"0017\", index)\n+        self.assertIn((\"SADR\", \"0016\"), index)\n+        self.assertIn((\"SADR\", \"0017\"), index)\n+\n+    def test_platform_tree_has_only_sadr_0001_through_0019(self) -> None:\n+        adr_dir = REPO_ROOT / \"docs\" / \"adr\"\n+        sadrs = sorted(adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\"))\n+        self.assertEqual(\n+            {path.name.split(\"-\", 2)[1] for path in sadrs},\n+            {f\"{number:04d}\" for number in range(1, 20)},\n+        )\n+        self.assertEqual(\n+            list(adr_dir.glob(\"[0-9][0-9][0-9][0-9]-*.md\")),\n+            [],\n+            \"the Shiki platform tree must not retain legacy numeric SADR paths\",\n+        )\n \n \n class FixtureAdrReferences(unittest.TestCase):\n@@ -180,6 +211,26 @@\n         self.assertIn(\"9991\", message)  # cited number\n         self.assertIn(\"docs/adr/9991-*.md\", message)  # missing path\n \n+    def test_target_adr_does_not_satisfy_missing_platform_sadr(self) -> None:\n+        root = self._make_repo(\n+            adr_names=[\"0042-target-decision.md\"],\n+            script_body='\"\"\"Target ADR 0042 exists, but platform SADR-0042 does not.\"\"\"\\n',\n+        )\n+        violations = find_adr_reference_violations(root, include_docs=False)\n+        self.assertEqual(len(violations), 1)\n+        self.assertEqual(violations[0].namespace, \"SADR\")\n+        self.assertEqual(violations[0].number, \"0042\")\n+        self.assertEqual(violations[0].expected_path, \"docs/adr/SADR-0042-*.md\")\n+\n+    def test_target_adr_and_platform_sadr_may_reuse_a_number(self) -> None:\n+        root = self._make_repo(\n+            adr_names=[\"0042-target-decision.md\", \"SADR-0042-platform-decision.md\"],\n+            script_body='\"\"\"Target ADR 0042 and platform SADR-0042 are distinct.\"\"\"\\n',\n+        )\n+        self.assertEqual(\n+            find_adr_reference_violations(root, include_docs=False), []\n+        )\n+\n     def test_uncited_adr_is_allowed(self) -> None:\n         # An ADR exists but nothing cites it -> not a violation. The converse\n         # (every ADR must be cited) is explicitly NOT required.\ndiff --git a/tests/test_sadr_install_migration.py b/tests/test_sadr_install_migration.py\nnew file mode 100644\nindex 0000000000000000000000000000000000000000..e1c1f87329132324dcb6bcdea757669752ad582b\n--- /dev/null\n+++ b/tests/test_sadr_install_migration.py\n@@ -0,0 +1,134 @@\n+\"\"\"SADR installation and collision-safe legacy upgrade behavior.\"\"\"\n+\n+from __future__ import annotations\n+\n+import contextlib\n+import hashlib\n+import io\n+import json\n+import shutil\n+import tempfile\n+import unittest\n+from pathlib import Path\n+\n+import shiki_test_support  # noqa: F401  (path bootstrap)\n+\n+import shiki_installer\n+from tests.test_adr_references import find_adr_reference_violations\n+\n+\n+PRODUCT_NUMBERS = tuple(range(13, 19))\n+\n+\n+def _quiet_install(target: Path, *, force: bool, validate: bool) -> None:\n+    with contextlib.redirect_stdout(io.StringIO()):\n+        shiki_installer.install_template(target, force=force, validate=validate)\n+\n+\n+def _sha256(path: Path) -> str:\n+    return hashlib.sha256(path.read_bytes()).hexdigest()\n+\n+\n+def _legacy_sadr_paths() -> tuple[str, ...]:\n+    return tuple(\n+        f\"docs/adr/{path.name.removeprefix('SADR-')}\"\n+        for path in sorted((shiki_installer.ROOT / \"docs\" / \"adr\").glob(\"SADR-*.md\"))\n+        if path.name != \"SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\"\n+    )\n+\n+\n+class FreshSadrInstallTests(unittest.TestCase):\n+    def setUp(self) -> None:\n+        self.tmp = Path(tempfile.mkdtemp(prefix=\"shiki-sadr-fresh-\"))\n+        self.addCleanup(lambda: shutil.rmtree(self.tmp, ignore_errors=True))\n+        self.target = self.tmp / \"target\"\n+        self.adr_dir = self.target / \"docs\" / \"adr\"\n+        self.adr_dir.mkdir(parents=True)\n+        self.product_bytes = {\n+            f\"{number:04d}-product-decision.md\": (\n+                f\"# Product ADR {number:04d}\\n\\nTarget-owned bytes {number}.\\n\"\n+            ).encode()\n+            for number in PRODUCT_NUMBERS\n+        }\n+        for name, content in self.product_bytes.items():\n+            (self.adr_dir / name).write_bytes(content)\n+\n+    def test_fresh_install_preserves_product_adrs_and_installs_all_sadrs(self) -> None:\n+        _quiet_install(self.target, force=False, validate=True)\n+\n+        for name, expected in self.product_bytes.items():\n+            self.assertEqual((self.adr_dir / name).read_bytes(), expected)\n+\n+        sadr_numbers = {\n+            path.name.split(\"-\", 2)[1]\n+            for path in self.adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\")\n+        }\n+        self.assertEqual(sadr_numbers, {f\"{number:04d}\" for number in range(1, 20)})\n+\n+        numeric_records = {\n+            path.name\n+            for path in self.adr_dir.glob(\"[0-9][0-9][0-9][0-9]-*.md\")\n+        }\n+        self.assertEqual(numeric_records, set(self.product_bytes))\n+        self.assertEqual(find_adr_reference_violations(self.target), [])\n+\n+\n+class LegacySadrUpgradeTests(unittest.TestCase):\n+    def setUp(self) -> None:\n+        self.tmp = Path(tempfile.mkdtemp(prefix=\"shiki-sadr-upgrade-\"))\n+        self.addCleanup(lambda: shutil.rmtree(self.tmp, ignore_errors=True))\n+        self.target = self.tmp / \"target\"\n+        self.adr_dir = self.target / \"docs\" / \"adr\"\n+        self.adr_dir.mkdir(parents=True)\n+\n+        self.product_bytes = {\n+            f\"{number:04d}-cubeshare-product-decision.md\": (\n+                f\"# CubeShare ADR {number:04d}\\n\\nDo not rewrite {number}.\\n\"\n+            ).encode()\n+            for number in PRODUCT_NUMBERS\n+        }\n+        for name, content in self.product_bytes.items():\n+            (self.adr_dir / name).write_bytes(content)\n+\n+        self.legacy_paths = _legacy_sadr_paths()\n+        for relative in self.legacy_paths:\n+            path = self.target / relative\n+            path.write_bytes(f\"legacy Shiki bytes for {relative}\\n\".encode())\n+\n+        stamp_path = self.target / shiki_installer.INSTALL_STAMP_PATH\n+        stamp_path.parent.mkdir(parents=True, exist_ok=True)\n+        stamp = {\n+            \"version\": shiki_installer.INSTALL_STAMP_VERSION,\n+            \"platform_commit\": None,\n+            \"installed_at\": \"2000-01-01T00:00:00+00:00\",\n+            \"digests\": {\n+                relative: _sha256(self.target / relative)\n+                for relative in self.legacy_paths\n+            },\n+        }\n+        stamp_path.write_text(\n+            json.dumps(stamp, indent=2, sort_keys=True) + \"\\n\", encoding=\"utf-8\"\n+        )\n+\n+    def test_force_upgrade_deletes_only_stamped_legacy_sadrs(self) -> None:\n+        _quiet_install(self.target, force=True, validate=True)\n+\n+        for relative in self.legacy_paths:\n+            self.assertFalse((self.target / relative).exists(), relative)\n+        for name, expected in self.product_bytes.items():\n+            self.assertEqual((self.adr_dir / name).read_bytes(), expected)\n+\n+        sadrs = list(self.adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\"))\n+        self.assertEqual(len(sadrs), 19)\n+        refreshed = json.loads(\n+            (self.target / shiki_installer.INSTALL_STAMP_PATH).read_text(encoding=\"utf-8\")\n+        )\n+        for relative in self.legacy_paths:\n+            self.assertNotIn(relative, refreshed[\"digests\"])\n+        for path in sadrs:\n+            relative = path.relative_to(self.target).as_posix()\n+            self.assertEqual(refreshed[\"digests\"][relative], _sha256(path))\n+\n+\n+if __name__ == \"__main__\":\n+    unittest.main()\n\ndiff --git a/.codex/skills/shiki/SKILL.md b/.codex/skills/shiki/SKILL.md\nindex c8e2e85d9d75d49cc4f2d6286ee22659c4e53e75..93cded623b2c15d9f65c76491a1c2d813ac9e6c0\n--- a/.codex/skills/shiki/SKILL.md\n+++ b/.codex/skills/shiki/SKILL.md\n@@ -18,6 +18,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n Codex CLI does not register this skill as a custom `/shiki` slash command. When\n the user types `/shiki` in Codex CLI and gets \"Unrecognized command\", explain\n that this is expected: invoke Shiki in Codex with natural language such as\n@@ -69,7 +72,7 @@\n \n ## Responsibilities\n \n-- Claude Code implements and repairs by default (`shiki runner claude`, ADR 0008); Codex implements and repairs tasks explicitly assigned to `codex`.\n+- Claude Code implements and repairs by default (`shiki runner claude`, SADR-0008); Codex implements and repairs tasks explicitly assigned to `codex`.\n - Claude Code Action can act as GitHub-side CCA or reviewer.\n - CCA judges completion.\n - MergeGate authorizes state transitions and merge readiness.\ndiff --git a/.github/prompts/cca-completion-check.md b/.github/prompts/cca-completion-check.md\nindex ed203ad5f302ecc81372d8ab27d2b1232ca97497..fd89950528333ef6242e6d5443904f5392ab7e56\n--- a/.github/prompts/cca-completion-check.md\n+++ b/.github/prompts/cca-completion-check.md\n@@ -19,6 +19,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n ## Required Reading\n \n Read only the files needed to judge the PR contract. Prefer this order:\ndiff --git a/AGENTS.md b/AGENTS.md\nindex 7f041100d49c2a7525f51cb0f5d696a4a6d1c78c..2b700e989ff295987aa8a4b0c5fcb1acd80020da\n--- a/AGENTS.md\n+++ b/AGENTS.md\n@@ -44,6 +44,10 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use\n+`SADR-NNNN-*.md`; target product decisions retain `NNNN-*.md` and the `ADR`\n+namespace.\n+\n Use the canonical source-of-truth order above. Runtime-specific reading orders\n are operational guidance only and must not override or reorder it.\n \ndiff --git a/CLAUDE.md b/CLAUDE.md\nindex 98a932e7841a632866fcd53b0bad0e3af3711d3d..611d491912d6a72a0327a1731b8f33a41acb4baa\n--- a/CLAUDE.md\n+++ b/CLAUDE.md\n@@ -14,6 +14,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n ## Claude Role\n \n Default to planner, default implementer, reviewer, coordinator, documentation editor, CCA-style completion judge, and final-judgment assistant.\ndiff --git a/SYSTEM_PROMPT.md b/SYSTEM_PROMPT.md\nindex aaaea45e3a896c3a44ba89eec8bb84325ecf6c62..2e7c093ebe9e8bb34e0923756f191b4f7fa581f5\n--- a/SYSTEM_PROMPT.md\n+++ b/SYSTEM_PROMPT.md\n@@ -30,6 +30,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n Use the canonical source-of-truth order above. Runtime-specific reading orders\n are operational guidance only and must not override or reorder it. The current\n conversation is non-durable operator input.\ndiff --git a/docs/adr/0001-keep-shiki-as-platform-name.md b/docs/adr/SADR-0001-keep-shiki-as-platform-name.md\nindex 20a9637c3b9d8c97de7ba8db7cfae4d82d9a7dba..8b685439959aada1b221c112f0ac3c51ca8a1695\n--- a/docs/adr/0001-keep-shiki-as-platform-name.md\n+++ b/docs/adr/SADR-0001-keep-shiki-as-platform-name.md\n@@ -1,3 +1,3 @@\n-# Keep Shiki As The Platform Name\n+# SADR-0001: Keep Shiki As The Platform Name\n \n Shiki remains the product name for the GitHub-native agentic engineering control plane. We considered more descriptive names such as MergePilot, MergeGate, and Kiroku, but kept Shiki because it carries the existing goal-seeking framework identity while remaining independent of Hermes, Codex, Claude Code, or any single runtime.\ndiff --git a/docs/adr/0002-use-agents-as-shared-constitution.md b/docs/adr/SADR-0002-use-agents-as-shared-constitution.md\nindex 73de30ecb078120eac540d9bc51855e3cd48d0e3..c5486182bf06fca36ea508496b013c67302fd989\n--- a/docs/adr/0002-use-agents-as-shared-constitution.md\n+++ b/docs/adr/SADR-0002-use-agents-as-shared-constitution.md\n@@ -1,3 +1,3 @@\n-# Use AGENTS As The Shared Agent Constitution\n+# SADR-0002: Use AGENTS As The Shared Agent Constitution\n \n Shiki uses `AGENTS.md` as the runtime-neutral constitution and `CLAUDE.md` as a thin Claude Code wrapper. This keeps Codex, Claude Code, Hermes Runner, and future Agent Runtimes aligned on the same GitHub-first MergeGate rules while still allowing runtime-specific instructions where needed.\ndiff --git a/docs/adr/0003-require-skill-gate-for-mergegate-readiness.md b/docs/adr/SADR-0003-require-skill-gate-for-mergegate-readiness.md\nindex 424be0c9af4c8f0136f1fde68a468c47ff84e764..7fc337987c7df9792f75f181dfcbed75fe5ffff2\n--- a/docs/adr/0003-require-skill-gate-for-mergegate-readiness.md\n+++ b/docs/adr/SADR-0003-require-skill-gate-for-mergegate-readiness.md\n@@ -1,3 +1,3 @@\n-# Require Skill Gate For MergeGate Readiness\n+# SADR-0003: Require Skill Gate For MergeGate Readiness\n \n Shiki requires relevant engineering skills to be invoked before execution when their triggers apply. This makes skills part of MergeGate readiness rather than optional prompt style, because Shiki depends on repeatable planning, issue decomposition, TDD, diagnosis, triage, and architecture review before autonomous or parallel agent work can be trusted.\ndiff --git a/docs/adr/0004-use-github-first-with-shiki-mirror.md b/docs/adr/SADR-0004-use-github-first-with-shiki-mirror.md\nindex d0a3a9174dc646a945a9d325954ffaf00c352040..a79c54ae9ee8eed2a0524192b864da4e22a92d3f\n--- a/docs/adr/0004-use-github-first-with-shiki-mirror.md\n+++ b/docs/adr/SADR-0004-use-github-first-with-shiki-mirror.md\n@@ -1,4 +1,4 @@\n-# ADR 0004: Use GitHub First With A Repository-Local Shiki Mirror\n+# SADR-0004: Use GitHub First With A Repository-Local Shiki Mirror\n \n ## Status\n \ndiff --git a/docs/adr/0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md b/docs/adr/SADR-0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\nindex b339baf49b89d9290e79c3d51b36e7b2bde3bd95..c30a0d8b1fde0224d0ac7820e7870dccf721580d\n--- a/docs/adr/0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\n+++ b/docs/adr/SADR-0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\n@@ -1,4 +1,4 @@\n-# ADR 0005: Use Codex As Front And Claude Code Action As The GitHub Runtime\n+# SADR-0005: Use Codex As Front And Claude Code Action As The GitHub Runtime\n \n ## Status\n \ndiff --git a/docs/adr/0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md b/docs/adr/SADR-0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\nindex 4bcae9d1e52f0a37b8e9fbafa5cd14b6ee1993bd..7a38538a6ae9929567f0ade67e7ea6a60d0b3f7e\n--- a/docs/adr/0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\n+++ b/docs/adr/SADR-0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\n@@ -1,4 +1,4 @@\n-# ADR 0006: Separate Codex Implementation, CCA Completion, And MergeGate Authorization\n+# SADR-0006: Separate Codex Implementation, CCA Completion, And MergeGate Authorization\n \n ## Status\n \ndiff --git a/docs/adr/0007-packaging-and-release.md b/docs/adr/SADR-0007-packaging-and-release.md\nindex 79b84ddb97167372b44641e5b7860e400ce80822..023cd57f8011aa6e6d2ebf612b7c35a7a82c00d4\n--- a/docs/adr/0007-packaging-and-release.md\n+++ b/docs/adr/SADR-0007-packaging-and-release.md\n@@ -1,4 +1,4 @@\n-# ADR 0007: Packaging And Release For Shiki\n+# SADR-0007: Packaging And Release For Shiki\n \n ## Status\n \ndiff --git a/docs/adr/0008-use-claude-code-as-default-implementer.md b/docs/adr/SADR-0008-use-claude-code-as-default-implementer.md\nindex dedf4510b7c7799521422f622029563d373db031..a158a8d444d93764ec515a28133f7dde4af23579\n--- a/docs/adr/0008-use-claude-code-as-default-implementer.md\n+++ b/docs/adr/SADR-0008-use-claude-code-as-default-implementer.md\n@@ -1,4 +1,4 @@\n-# ADR 0008: Use Claude Code As The Default Implementer Runtime\n+# SADR-0008: Use Claude Code As The Default Implementer Runtime\n \n ## Status\n \ndiff --git a/docs/adr/0009-requirements-definition-and-spec-freeze.md b/docs/adr/SADR-0009-requirements-definition-and-spec-freeze.md\nindex 29b3f7871bef9203a8b4a831a65c39eb2f506e42..52e7dd69ad0aa4efe0b68c62678abbf7df02aaec\n--- a/docs/adr/0009-requirements-definition-and-spec-freeze.md\n+++ b/docs/adr/SADR-0009-requirements-definition-and-spec-freeze.md\n@@ -1,4 +1,4 @@\n-# ADR 0009: Merge Grill And Planning Into Requirements Definition With Spec Freeze\n+# SADR-0009: Merge Grill And Planning Into Requirements Definition With Spec Freeze\n \n ## Status\n \ndiff --git a/docs/adr/0010-external-ai-guardian-review.md b/docs/adr/SADR-0010-external-ai-guardian-review.md\nindex e6108bbe8bb4e73e0ff4684c8f78001a80438c4e..e690d4eddd270e683d709c9b679c5859e71eeaaa\n--- a/docs/adr/0010-external-ai-guardian-review.md\n+++ b/docs/adr/SADR-0010-external-ai-guardian-review.md\n@@ -1,4 +1,4 @@\n-# ADR 0010: External AI Guardian Review As A First-Class Approval Authority\n+# SADR-0010: External AI Guardian Review As A First-Class Approval Authority\n \n ## Status\n \ndiff --git a/docs/adr/0011-autonomous-quality-gate-evidence.md b/docs/adr/SADR-0011-autonomous-quality-gate-evidence.md\nindex bca0246c5d46e44afbde15486a75eec425fc02f5..9c93eac16d8c3a6d4b11bd7224e3955102cd7db4\n--- a/docs/adr/0011-autonomous-quality-gate-evidence.md\n+++ b/docs/adr/SADR-0011-autonomous-quality-gate-evidence.md\n@@ -1,4 +1,4 @@\n-# ADR 0011: Autonomous Quality-Gate Evidence \u2014 Deterministic Facts And An Independent Verifier\n+# SADR-0011: Autonomous Quality-Gate Evidence \u2014 Deterministic Facts And An Independent Verifier\n \n ## Status\n \ndiff --git a/docs/adr/0012-autonomous-loop-closeout-to-main.md b/docs/adr/SADR-0012-autonomous-loop-closeout-to-main.md\nindex 637fad6dde4989c9c2bf0f81f676d8d29991123c..ec7d85f81030a676bcbba28b43eb365422a36eac\n--- a/docs/adr/0012-autonomous-loop-closeout-to-main.md\n+++ b/docs/adr/SADR-0012-autonomous-loop-closeout-to-main.md\n@@ -1,4 +1,4 @@\n-# 0012 \u2014 Autonomous loop pushes goal completion to main via a closeout PR\n+# SADR-0012 \u2014 Autonomous loop pushes goal completion to main via a closeout PR\n \n - Status: accepted\n - Date: 2026-06-18\ndiff --git a/docs/adr/0013-bootstrap-grants-actions-approve-pull-requests.md b/docs/adr/SADR-0013-bootstrap-grants-actions-approve-pull-requests.md\nindex b6c69d64f1c5ced346b88d6859586715b3b5bd0e..a793e7277d67ab8ea56286fec289ca689825a17d\n--- a/docs/adr/0013-bootstrap-grants-actions-approve-pull-requests.md\n+++ b/docs/adr/SADR-0013-bootstrap-grants-actions-approve-pull-requests.md\n@@ -1,4 +1,4 @@\n-# ADR 0013: Bootstrap Grants GitHub Actions Permission To Create And Approve Pull Requests\n+# SADR-0013: Bootstrap Grants GitHub Actions Permission To Create And Approve Pull Requests\n \n ## Status\n \ndiff --git a/docs/adr/0014-codex-app-external-ai-guardian-ui-adapter.md b/docs/adr/SADR-0014-codex-app-external-ai-guardian-ui-adapter.md\nindex 238db06279334caa341c117362df9271382a7fcd..65f12e9939321e1b3e389a34c822fc1549e5155d\n--- a/docs/adr/0014-codex-app-external-ai-guardian-ui-adapter.md\n+++ b/docs/adr/SADR-0014-codex-app-external-ai-guardian-ui-adapter.md\n@@ -1,4 +1,4 @@\n-# ADR 0014: Use Codex App As The External AI Guardian UI Adapter\n+# SADR-0014: Use Codex App As The External AI Guardian UI Adapter\n \n ## Status\n \ndiff --git a/docs/adr/0015-contract-approval-before-dispatch.md b/docs/adr/SADR-0015-contract-approval-before-dispatch.md\nindex fad6c656741a20a7684ba16080a7db159f63a52f..7bdd760bb3e82eee4e1410f45c783b03b638d456\n--- a/docs/adr/0015-contract-approval-before-dispatch.md\n+++ b/docs/adr/SADR-0015-contract-approval-before-dispatch.md\n@@ -1,4 +1,4 @@\n-# ADR 0015: Approve The Task Contract Before Dispatch, Not The Diff After It\n+# SADR-0015: Approve The Task Contract Before Dispatch, Not The Diff After It\n \n ## Status\n \ndiff --git a/docs/adr/0016-locks-bound-product-paths.md b/docs/adr/SADR-0016-locks-bound-product-paths.md\nindex b5e850f3f1f7f4ef6e7f9d1763a29a51bd300c45..332cc17313498404f41a145291e38447b5de0dc4\n--- a/docs/adr/0016-locks-bound-product-paths.md\n+++ b/docs/adr/SADR-0016-locks-bound-product-paths.md\n@@ -1,4 +1,4 @@\n-# ADR 0016: Locks bound product paths; the Shiki mirror is bound by record identity\n+# SADR-0016: Locks bound product paths; the Shiki mirror is bound by record identity\n \n ## Status\n \ndiff --git a/docs/adr/0017-bookkeeping-closeouts-do-not-inherit-risk.md b/docs/adr/SADR-0017-bookkeeping-closeouts-do-not-inherit-risk.md\nindex 554e3a0d526a72ca067108c5464cc68c3edcf174..83115f69f1b1b48d544c9b0157f377b91cb53abf\n--- a/docs/adr/0017-bookkeeping-closeouts-do-not-inherit-risk.md\n+++ b/docs/adr/SADR-0017-bookkeeping-closeouts-do-not-inherit-risk.md\n@@ -1,4 +1,4 @@\n-# ADR 0017: A Bookkeeping Closeout Does Not Inherit Its Task's Risk\n+# SADR-0017: A Bookkeeping Closeout Does Not Inherit Its Task's Risk\n \n ## Status\n \ndiff --git a/docs/adr/0018-carried-guardian-approval-across-a-proven-base-sync.md b/docs/adr/SADR-0018-carried-guardian-approval-across-a-proven-base-sync.md\nindex 22358562da390ed8b45cd600da08a43d378b4680..38da0fd95102c3496c3219ce2319fce696a2532a\n--- a/docs/adr/0018-carried-guardian-approval-across-a-proven-base-sync.md\n+++ b/docs/adr/SADR-0018-carried-guardian-approval-across-a-proven-base-sync.md\n@@ -1,4 +1,4 @@\n-# ADR 0018: Carried Guardian Approval Across A Proven Base Sync\n+# SADR-0018: Carried Guardian Approval Across A Proven Base Sync\n \n ## Status\n \ndiff --git a/docs/adr/README.md b/docs/adr/README.md\nindex c70e057a4520c36e042ab68c53cd4a21ef709b71..5da43856836e2ce6a150f8127766e2f88355ab4b\n--- a/docs/adr/README.md\n+++ b/docs/adr/README.md\n@@ -1,40 +1,41 @@\n-# Architecture Decision Records\n+# Decision Record Namespaces\n \n-This directory holds Shiki's Architecture Decision Records (ADRs): durable\n-records of hard-to-reverse platform decisions. ADRs are part of the Shiki source\n-of truth alongside GitHub state, the `.shiki/` mirror, and `CONTEXT.md`.\n+This directory holds two independent decision namespaces:\n \n-## How ADRs Work\n+- Shiki Architecture Decision Records (SADRs), named\n+  `SADR-NNNN-<short-slug>.md`, record hard-to-reverse Shiki platform decisions.\n+- Target Architecture Decision Records (ADRs), named\n+  `NNNN-<short-slug>.md`, belong to the target product.\n \n-- New ADRs are added as `docs/adr/NNNN-<short-slug>.md`, where `NNNN` is the next\n-  zero-padded sequence number.\n-- The **canonical list of ADRs is the set of files matching `docs/adr/*.md`** in\n-  this directory. Browse `docs/adr/*.md` for the authoritative, current set.\n-- Use [`template.md`](template.md) as the starting point for a new ADR.\n-- Record an ADR when a decision is hard to reverse, surprising without context,\n-  and a real tradeoff. Do not write an ADR for routine or easily reversible\n-  choices.\n+The same four-digit number may appear once in each namespace. A platform\n+`SADR-NNNN` citation resolves only to `SADR-NNNN-*.md`; a target `ADR NNNN`\n+citation resolves only to `NNNN-*.md`.\n \n-Using the `docs/adr/*.md` glob as the source of truth means other PRs can add an\n-ADR without editing this index file, avoiding merge contention on a\n-hand-maintained enumeration.\n+## How Decision Records Work\n \n-## Current ADRs (snapshot)\n-\n-The list below is a convenience snapshot and may lag behind the directory. When\n-in doubt, list `docs/adr/*.md`.\n+- New Shiki platform decisions use the next zero-padded SADR number.\n+- New target product decisions use the target's own ADR sequence.\n+- The canonical list is the files in `docs/adr/`; no hand-maintained index is\n+  authoritative.\n+- Use [`template.md`](template.md) as the starting point and choose the filename\n+  namespace that owns the decision.\n+- Record a decision when it is hard to reverse, surprising without context, and\n+  a real tradeoff. Do not write a decision record for routine or easily\n+  reversible choices.\n \n-- [0001 \u2014 Keep Shiki As The Platform Name](0001-keep-shiki-as-platform-name.md)\n-- [0002 \u2014 Use AGENTS As The Shared Agent Constitution](0002-use-agents-as-shared-constitution.md)\n-- [0003 \u2014 Require Skill Gate For MergeGate Readiness](0003-require-skill-gate-for-mergegate-readiness.md)\n-- [0004 \u2014 Use GitHub First With A Repository-Local Shiki Mirror](0004-use-github-first-with-shiki-mirror.md)\n-- [0005 \u2014 Use Codex As Front And Claude Code Action As The GitHub Runtime](0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md)\n-- [0006 \u2014 Separate Codex Implementation, CCA Completion, And MergeGate Authorization](0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md)\n+Using the directory as the source of truth lets independent changes add records\n+without contending on an enumerated index.\n \n-## Adding A New ADR\n+## Adding A Shiki SADR\n \n-1. Copy `template.md` to `docs/adr/NNNN-<short-slug>.md`.\n+1. Copy `template.md` to `docs/adr/SADR-NNNN-<short-slug>.md`.\n 2. Fill in the title, status, context, decision, and consequences.\n-3. Set the status to `Proposed`, then `Accepted` once the decision is approved.\n-4. Open a PR following `CONTRIBUTING.md`. You do not need to edit this index for\n-   the ADR to be discoverable via `docs/adr/*.md`.\n+3. Set the status to `Proposed`, then `Accepted` once the proper authority has\n+   approved the decision.\n+4. Open a PR following `CONTRIBUTING.md`.\n+\n+## Adding A Target ADR\n+\n+1. Copy `template.md` to `docs/adr/NNNN-<short-slug>.md` using the target's own\n+   sequence.\n+2. Cite it as `ADR NNNN`; do not prefix its filename with `SADR-`.\ndiff --git a/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md b/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\nnew file mode 100644\nindex 0000000000000000000000000000000000000000..7455ce1f3bed02ddddb851ac21a1d1a648c9aeba\n--- /dev/null\n+++ b/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\n@@ -0,0 +1,55 @@\n+# SADR-0019: Separate Shiki Platform SADRs From Target ADRs\n+\n+## Status\n+\n+Accepted\n+\n+## Context\n+\n+Shiki installs its platform decision records into `docs/adr/` in every target\n+repository. Shiki and its targets previously used the same numeric\n+`NNNN-<slug>.md` namespace. A target that already owned ADRs 0013 through 0018\n+therefore acquired two records for each number during installation, making an\n+`ADR NNNN` citation ambiguous. Renumbering target decisions would rewrite target\n+history, while renumbering Shiki decisions per target would destroy their stable\n+platform identity.\n+\n+Upgrades also need to remove the former Shiki-owned numeric paths without ever\n+mistaking a target-owned ADR for platform content. The install stamp already\n+records exact shipped paths and content digests, so it can prove ownership only\n+when both still match.\n+\n+## Decision\n+\n+Shiki platform decisions use the `SADR` namespace:\n+\n+- records are named `docs/adr/SADR-NNNN-<slug>.md`;\n+- live platform citations use `SADR-NNNN`;\n+- target product decisions retain `docs/adr/NNNN-<slug>.md` and `ADR NNNN`;\n+- the reference validator resolves the two namespaces independently, so the same\n+  four-digit number may exist once in each namespace.\n+\n+Before a forced upgrade writes anything, the installer inventories the exact\n+former numeric Shiki ADR paths. It may delete a legacy path only when the\n+existing install stamp contains that exact path and its recorded SHA-256 digest\n+matches the file currently on disk. All ownership blockers are reported\n+together and abort the upgrade before its first write. A non-forced install\n+never performs this cleanup.\n+\n+Historical `.shiki/` evidence remains unchanged because it records the namespace\n+that existed when that evidence was produced.\n+\n+## Consequences\n+\n+- Targets may use any ADR number without colliding with Shiki's platform SADRs.\n+- Fresh installs preserve target ADR bytes and install a stable SADR set.\n+- Forced upgrades fail closed when legacy ownership cannot be proven, and a\n+  successful upgrade refreshes the stamp without removed legacy paths.\n+- Current governance, packaging, workflow, prompt, script, and skill references\n+  must move with the platform records.\n+\n+## Alternatives Considered\n+\n+- Collision-only refusal leaves affected targets permanently unupgradable.\n+- Renumbering target ADRs mutates target-owned history.\n+- Per-target Shiki renumbering makes platform citations non-portable.\ndiff --git a/docs/adr/template.md b/docs/adr/template.md\nindex a92a97daddd0b00ab9dd626cc8d86942da2eafed..17dd79175b5fee2105935557f691b40cf9492942\n--- a/docs/adr/template.md\n+++ b/docs/adr/template.md\n@@ -1,14 +1,14 @@\n-# NNNN: <Short Decision Title>\n+# <SADR-NNNN or NNNN>: <Short Decision Title>\n \n ## Status\n \n-Proposed | Accepted | Superseded by ADR-NNNN\n+Proposed | Accepted | Superseded by SADR-NNNN or target ADR NNNN\n \n ## Context\n \n What is the situation, constraint, or force that requires a decision? Reference\n-relevant domain terms (`CONTEXT.md`), prior ADRs, and code reality. State the\n-problem, not the solution.\n+relevant domain terms (`CONTEXT.md`), prior Shiki SADRs or target ADRs, and code\n+reality. State the problem, not the solution.\n \n ## Decision\n \ndiff --git a/docs/agents/domain.md b/docs/agents/domain.md\nindex 258da8ea81e87ac0a6b0b5b5e5c2fde980eaabb7..67aaa6f4b7d0a5aa4d495ce6279073333f7798cb\n--- a/docs/agents/domain.md\n+++ b/docs/agents/domain.md\n@@ -5,7 +5,9 @@\n ## Layout\n \n - `CONTEXT.md` defines Shiki language. Keep it implementation-free.\n-- `docs/adr/` records hard-to-reverse decisions.\n+- `docs/adr/SADR-NNNN-*.md` records Shiki platform decisions.\n+- `docs/adr/NNNN-*.md` records target product decisions without sharing the\n+  SADR namespace.\n - `AGENTS.md` is the runtime-neutral agent constitution.\n - `CLAUDE.md` is the Claude Code wrapper around `AGENTS.md`.\n - `.shiki/` is the Target Repository mirror for Goals, PRDs, plans, Task DAGs, contracts, locks, worktree records, ledger entries, CCA verdicts, repair packets, reports, and handoffs.\ndiff --git a/docs/agents/implementation-policy.md b/docs/agents/implementation-policy.md\nindex c754144a9fb5bf4a38aa82a60e1670c59e626b44..e90d40fe21880b9a09719c5ecfe6bde7284eb3ce\n--- a/docs/agents/implementation-policy.md\n+++ b/docs/agents/implementation-policy.md\n@@ -31,6 +31,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n ## Non-Negotiables\n \n 1. **Planning precedes implementation.** Non-trivial Goals start with `grill-with-docs` and Context & Impact.\ndiff --git a/tests/test_adr_references.py b/tests/test_adr_references.py\nindex 51706329c797d5dd9b47e082a333fa83b261d482..91ab76b724cb973435c45d53cab63793942267e9\n--- a/tests/test_adr_references.py\n+++ b/tests/test_adr_references.py\n@@ -1,4 +1,4 @@\n-\"\"\"Every ``ADR ####`` citation must resolve to exactly one decision record.\n+\"\"\"Every target ADR and Shiki SADR citation resolves in its own namespace.\n \n Background. ``scripts/mergegate_check.py`` justifies ``is_bookkeeping_closeout``\n with \"ADR 0017\" nine times and ``scripts/guardian_approval_signal.py`` cites\n@@ -11,11 +11,12 @@\n and the directory drifted with no signal.\n \n This is the same unbound-pair shape behind other silent failures: a reference\n-and its target with nothing asserting they agree. This test binds them. It scans\n-every ``scripts/*.py`` (plus ``docs/**/*.md`` and the root ``*.md`` governance\n-files, which are cheap) for the ``ADR ####`` pattern and asserts each cited\n-number resolves to exactly one ``docs/adr/####-*.md``. The converse is\n-deliberately NOT required: an ADR may exist without being cited.\n+and its target with nothing asserting they agree. This test binds them. Target\n+``ADR NNNN`` citations resolve only to ``docs/adr/NNNN-*.md``; Shiki\n+``SADR-NNNN`` citations resolve only to ``docs/adr/SADR-NNNN-*.md``. The scan\n+covers current governance docs, scripts, workflows, prompts, packaging metadata,\n+and repo-local Codex guidance while deliberately excluding historical\n+``.shiki`` evidence. The converse is not required: a record may be uncited.\n \n The suite is dependency free: standard-library ``unittest`` only.\n \"\"\"\n@@ -31,17 +32,18 @@\n \n REPO_ROOT = Path(__file__).resolve().parents[1]\n \n-# \"ADR 0016\" -- a four-digit citation. The trailing \\b keeps a five-digit token\n-# from being read as a truncated four-digit number.\n-_CITATION = re.compile(r\"\\bADR (\\d{4})\\b\")\n-# \"0016-locks-bound-product-paths.md\" -- the leading four digits name the record.\n-_ADR_FILENAME = re.compile(r\"^(\\d{4})-.*\\.md$\")\n+# ``ADR 0016`` / ``SADR-0016`` -- a namespace plus four-digit citation. The\n+# trailing ``\\b`` keeps a five-digit token from being read as a truncated one.\n+_CITATION = re.compile(r\"\\b(S?ADR)[ -](\\d{4})\\b\")\n+# Target ADRs retain the numeric filename; Shiki platform decisions use SADR.\n+_DECISION_FILENAME = re.compile(r\"^(?:(SADR)-)?(\\d{4})-.*\\.md$\")\n \n \n class Violation(NamedTuple):\n     \"\"\"A citation that does not resolve to exactly one decision record.\"\"\"\n \n     citing_file: str  # repo-relative path of the file that cites the ADR\n+    namespace: str  # \"ADR\" for target decisions; \"SADR\" for Shiki decisions\n     number: str  # the cited four-digit number, e.g. \"0016\"\n     line: int  # 1-based line of the citation\n     reason: str  # \"missing\" (zero matches) or \"ambiguous\" (two or more)\n@@ -49,44 +51,59 @@\n \n     @property\n     def expected_path(self) -> str:\n-        return f\"docs/adr/{self.number}-*.md\"\n+        prefix = \"SADR-\" if self.namespace == \"SADR\" else \"\"\n+        return f\"docs/adr/{prefix}{self.number}-*.md\"\n \n     def describe(self) -> str:\n         \"\"\"Name the citing file, the cited number, and the missing path.\"\"\"\n         if self.reason == \"missing\":\n             return (\n-                f\"{self.citing_file}:{self.line} cites ADR {self.number} \"\n+                f\"{self.citing_file}:{self.line} cites {self.namespace} {self.number} \"\n                 f\"but no document matches {self.expected_path}\"\n             )\n         joined = \", \".join(self.matches)\n         return (\n-            f\"{self.citing_file}:{self.line} cites ADR {self.number} which \"\n+            f\"{self.citing_file}:{self.line} cites {self.namespace} {self.number} which \"\n             f\"resolves to {len(self.matches)} documents (expected exactly one \"\n             f\"at {self.expected_path}): {joined}\"\n         )\n \n \n-def _adr_index(root: Path) -> dict[str, list[Path]]:\n-    \"\"\"Map every four-digit ADR number to the record files that claim it.\"\"\"\n-    index: dict[str, list[Path]] = {}\n+def _adr_index(root: Path) -> dict[tuple[str, str], list[Path]]:\n+    \"\"\"Map each decision namespace and number to the files that claim it.\"\"\"\n+    index: dict[tuple[str, str], list[Path]] = {}\n     adr_dir = root / \"docs\" / \"adr\"\n     if adr_dir.is_dir():\n         for path in sorted(adr_dir.glob(\"*.md\")):\n-            match = _ADR_FILENAME.match(path.name)\n+            match = _DECISION_FILENAME.match(path.name)\n             if match:\n-                index.setdefault(match.group(1), []).append(path)\n+                namespace = \"SADR\" if match.group(1) else \"ADR\"\n+                index.setdefault((namespace, match.group(2)), []).append(path)\n     return index\n \n \n def _citing_files(root: Path, include_docs: bool = True) -> list[Path]:\n-    \"\"\"Files scanned for citations: scripts/*.py, and cheaply the markdown.\"\"\"\n+    \"\"\"Current, shipped text surfaces scanned for decision citations.\"\"\"\n     files = sorted((root / \"scripts\").glob(\"*.py\"))\n+    files += sorted((root / \"scripts\").glob(\"*.sh\"))\n     if include_docs:\n         docs = root / \"docs\"\n         if docs.is_dir():\n             files += sorted(docs.rglob(\"*.md\"))\n         files += sorted(root.glob(\"*.md\"))\n-    return files\n+        pyproject = root / \"pyproject.toml\"\n+        if pyproject.is_file():\n+            files.append(pyproject)\n+        for relative in (\".github\", \".codex\"):\n+            surface = root / relative\n+            if surface.is_dir():\n+                files += sorted(\n+                    path\n+                    for path in surface.rglob(\"*\")\n+                    if path.is_file()\n+                    and path.suffix in {\".md\", \".py\", \".sh\", \".toml\", \".yaml\", \".yml\"}\n+                )\n+    return sorted(set(files))\n \n \n def find_adr_reference_violations(\n@@ -94,10 +111,9 @@\n ) -> list[Violation]:\n     \"\"\"Return every ``ADR ####`` citation under ``root`` that fails to resolve.\n \n-    A citation resolves when exactly one ``docs/adr/####-*.md`` matches its\n-    number. Zero matches is a \"missing\" violation; two or more is \"ambiguous\".\n-    Records that are never cited produce nothing -- the converse is not\n-    required.\n+    A citation resolves when exactly one record in the same namespace matches\n+    its number. Zero matches is a \"missing\" violation; two or more is\n+    \"ambiguous\". Records that are never cited produce nothing.\n     \"\"\"\n     root = Path(root)\n     index = _adr_index(root)\n@@ -110,13 +126,15 @@\n         rel = path.relative_to(root).as_posix()\n         for lineno, line in enumerate(text.splitlines(), start=1):\n             for match in _CITATION.finditer(line):\n-                number = match.group(1)\n-                found = index.get(number, [])\n+                namespace, number = match.groups()\n+                found = index.get((namespace, number), [])\n                 if len(found) == 1:\n                     continue\n                 reason = \"missing\" if not found else \"ambiguous\"\n                 rels = [p.relative_to(root).as_posix() for p in found]\n-                violations.append(Violation(rel, number, lineno, reason, rels))\n+                violations.append(\n+                    Violation(rel, namespace, number, lineno, reason, rels)\n+                )\n     return violations\n \n \n@@ -129,8 +147,8 @@\n         self.assertEqual(\n             violations,\n             [],\n-            \"Every 'ADR ####' citation must resolve to exactly one \"\n-            f\"docs/adr/####-*.md:\\n{detail}\",\n+            \"Every ADR/SADR citation must resolve exactly once in its own \"\n+            f\"docs/adr namespace:\\n{detail}\",\n         )\n \n     def test_scan_actually_reads_scripts(self) -> None:\n@@ -146,8 +164,21 @@\n         # The two records this Goal carried onto the default branch, and which\n         # the scripts cite most, must be present.\n         index = _adr_index(REPO_ROOT)\n-        self.assertIn(\"0016\", index)\n-        self.assertIn(\"0017\", index)\n+        self.assertIn((\"SADR\", \"0016\"), index)\n+        self.assertIn((\"SADR\", \"0017\"), index)\n+\n+    def test_platform_tree_has_only_sadr_0001_through_0019(self) -> None:\n+        adr_dir = REPO_ROOT / \"docs\" / \"adr\"\n+        sadrs = sorted(adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\"))\n+        self.assertEqual(\n+            {path.name.split(\"-\", 2)[1] for path in sadrs},\n+            {f\"{number:04d}\" for number in range(1, 20)},\n+        )\n+        self.assertEqual(\n+            list(adr_dir.glob(\"[0-9][0-9][0-9][0-9]-*.md\")),\n+            [],\n+            \"the Shiki platform tree must not retain legacy numeric SADR paths\",\n+        )\n \n \n class FixtureAdrReferences(unittest.TestCase):\n@@ -180,6 +211,26 @@\n         self.assertIn(\"9991\", message)  # cited number\n         self.assertIn(\"docs/adr/9991-*.md\", message)  # missing path\n \n+    def test_target_adr_does_not_satisfy_missing_platform_sadr(self) -> None:\n+        root = self._make_repo(\n+            adr_names=[\"0042-target-decision.md\"],\n+            script_body='\"\"\"Target ADR 0042 exists, but platform SADR-0042 does not.\"\"\"\\n',\n+        )\n+        violations = find_adr_reference_violations(root, include_docs=False)\n+        self.assertEqual(len(violations), 1)\n+        self.assertEqual(violations[0].namespace, \"SADR\")\n+        self.assertEqual(violations[0].number, \"0042\")\n+        self.assertEqual(violations[0].expected_path, \"docs/adr/SADR-0042-*.md\")\n+\n+    def test_target_adr_and_platform_sadr_may_reuse_a_number(self) -> None:\n+        root = self._make_repo(\n+            adr_names=[\"0042-target-decision.md\", \"SADR-0042-platform-decision.md\"],\n+            script_body='\"\"\"Target ADR 0042 and platform SADR-0042 are distinct.\"\"\"\\n',\n+        )\n+        self.assertEqual(\n+            find_adr_reference_violations(root, include_docs=False), []\n+        )\n+\n     def test_uncited_adr_is_allowed(self) -> None:\n         # An ADR exists but nothing cites it -> not a violation. The converse\n         # (every ADR must be cited) is explicitly NOT required.\ndiff --git a/tests/test_sadr_install_migration.py b/tests/test_sadr_install_migration.py\nnew file mode 100644\nindex 0000000000000000000000000000000000000000..e1c1f87329132324dcb6bcdea757669752ad582b\n--- /dev/null\n+++ b/tests/test_sadr_install_migration.py\n@@ -0,0 +1,134 @@\n+\"\"\"SADR installation and collision-safe legacy upgrade behavior.\"\"\"\n+\n+from __future__ import annotations\n+\n+import contextlib\n+import hashlib\n+import io\n+import json\n+import shutil\n+import tempfile\n+import unittest\n+from pathlib import Path\n+\n+import shiki_test_support  # noqa: F401  (path bootstrap)\n+\n+import shiki_installer\n+from tests.test_adr_references import find_adr_reference_violations\n+\n+\n+PRODUCT_NUMBERS = tuple(range(13, 19))\n+\n+\n+def _quiet_install(target: Path, *, force: bool, validate: bool) -> None:\n+    with contextlib.redirect_stdout(io.StringIO()):\n+        shiki_installer.install_template(target, force=force, validate=validate)\n+\n+\n+def _sha256(path: Path) -> str:\n+    return hashlib.sha256(path.read_bytes()).hexdigest()\n+\n+\n+def _legacy_sadr_paths() -> tuple[str, ...]:\n+    return tuple(\n+        f\"docs/adr/{path.name.removeprefix('SADR-')}\"\n+        for path in sorted((shiki_installer.ROOT / \"docs\" / \"adr\").glob(\"SADR-*.md\"))\n+        if path.name != \"SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\"\n+    )\n+\n+\n+class FreshSadrInstallTests(unittest.TestCase):\n+    def setUp(self) -> None:\n+        self.tmp = Path(tempfile.mkdtemp(prefix=\"shiki-sadr-fresh-\"))\n+        self.addCleanup(lambda: shutil.rmtree(self.tmp, ignore_errors=True))\n+        self.target = self.tmp / \"target\"\n+        self.adr_dir = self.target / \"docs\" / \"adr\"\n+        self.adr_dir.mkdir(parents=True)\n+        self.product_bytes = {\n+            f\"{number:04d}-product-decision.md\": (\n+                f\"# Product ADR {number:04d}\\n\\nTarget-owned bytes {number}.\\n\"\n+            ).encode()\n+            for number in PRODUCT_NUMBERS\n+        }\n+        for name, content in self.product_bytes.items():\n+            (self.adr_dir / name).write_bytes(content)\n+\n+    def test_fresh_install_preserves_product_adrs_and_installs_all_sadrs(self) -> None:\n+        _quiet_install(self.target, force=False, validate=True)\n+\n+        for name, expected in self.product_bytes.items():\n+            self.assertEqual((self.adr_dir / name).read_bytes(), expected)\n+\n+        sadr_numbers = {\n+            path.name.split(\"-\", 2)[1]\n+            for path in self.adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\")\n+        }\n+        self.assertEqual(sadr_numbers, {f\"{number:04d}\" for number in range(1, 20)})\n+\n+        numeric_records = {\n+            path.name\n+            for path in self.adr_dir.glob(\"[0-9][0-9][0-9][0-9]-*.md\")\n+        }\n+        self.assertEqual(numeric_records, set(self.product_bytes))\n+        self.assertEqual(find_adr_reference_violations(self.target), [])\n+\n+\n+class LegacySadrUpgradeTests(unittest.TestCase):\n+    def setUp(self) -> None:\n+        self.tmp = Path(tempfile.mkdtemp(prefix=\"shiki-sadr-upgrade-\"))\n+        self.addCleanup(lambda: shutil.rmtree(self.tmp, ignore_errors=True))\n+        self.target = self.tmp / \"target\"\n+        self.adr_dir = self.target / \"docs\" / \"adr\"\n+        self.adr_dir.mkdir(parents=True)\n+\n+        self.product_bytes = {\n+            f\"{number:04d}-cubeshare-product-decision.md\": (\n+                f\"# CubeShare ADR {number:04d}\\n\\nDo not rewrite {number}.\\n\"\n+            ).encode()\n+            for number in PRODUCT_NUMBERS\n+        }\n+        for name, content in self.product_bytes.items():\n+            (self.adr_dir / name).write_bytes(content)\n+\n+        self.legacy_paths = _legacy_sadr_paths()\n+        for relative in self.legacy_paths:\n+            path = self.target / relative\n+            path.write_bytes(f\"legacy Shiki bytes for {relative}\\n\".encode())\n+\n+        stamp_path = self.target / shiki_installer.INSTALL_STAMP_PATH\n+        stamp_path.parent.mkdir(parents=True, exist_ok=True)\n+        stamp = {\n+            \"version\": shiki_installer.INSTALL_STAMP_VERSION,\n+            \"platform_commit\": None,\n+            \"installed_at\": \"2000-01-01T00:00:00+00:00\",\n+            \"digests\": {\n+                relative: _sha256(self.target / relative)\n+                for relative in self.legacy_paths\n+            },\n+        }\n+        stamp_path.write_text(\n+            json.dumps(stamp, indent=2, sort_keys=True) + \"\\n\", encoding=\"utf-8\"\n+        )\n+\n+    def test_force_upgrade_deletes_only_stamped_legacy_sadrs(self) -> None:\n+        _quiet_install(self.target, force=True, validate=True)\n+\n+        for relative in self.legacy_paths:\n+            self.assertFalse((self.target / relative).exists(), relative)\n+        for name, expected in self.product_bytes.items():\n+            self.assertEqual((self.adr_dir / name).read_bytes(), expected)\n+\n+        sadrs = list(self.adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\"))\n+        self.assertEqual(len(sadrs), 19)\n+        refreshed = json.loads(\n+            (self.target / shiki_installer.INSTALL_STAMP_PATH).read_text(encoding=\"utf-8\")\n+        )\n+        for relative in self.legacy_paths:\n+            self.assertNotIn(relative, refreshed[\"digests\"])\n+        for path in sadrs:\n+            relative = path.relative_to(self.target).as_posix()\n+            self.assertEqual(refreshed[\"digests\"][relative], _sha256(path))\n+\n+\n+if __name__ == \"__main__\":\n+    unittest.main()\n\nexec\n/bin/zsh -lc \"python3 -m unittest discover -s tests -p 'test_sadr_install_migration.py' -v\" in /private/tmp/shiki-adr-collision.pa0KwY/.worktrees/shiki-t-20260807t073307221254z-2d22d654-introduc\ndiff --git a/.codex/skills/shiki/SKILL.md b/.codex/skills/shiki/SKILL.md\nindex c8e2e85d9d75d49cc4f2d6286ee22659c4e53e75..93cded623b2c15d9f65c76491a1c2d813ac9e6c0\n--- a/.codex/skills/shiki/SKILL.md\n+++ b/.codex/skills/shiki/SKILL.md\n@@ -18,6 +18,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n Codex CLI does not register this skill as a custom `/shiki` slash command. When\n the user types `/shiki` in Codex CLI and gets \"Unrecognized command\", explain\n that this is expected: invoke Shiki in Codex with natural language such as\n@@ -69,7 +72,7 @@\n \n ## Responsibilities\n \n-- Claude Code implements and repairs by default (`shiki runner claude`, ADR 0008); Codex implements and repairs tasks explicitly assigned to `codex`.\n+- Claude Code implements and repairs by default (`shiki runner claude`, SADR-0008); Codex implements and repairs tasks explicitly assigned to `codex`.\n - Claude Code Action can act as GitHub-side CCA or reviewer.\n - CCA judges completion.\n - MergeGate authorizes state transitions and merge readiness.\ndiff --git a/.github/prompts/cca-completion-check.md b/.github/prompts/cca-completion-check.md\nindex ed203ad5f302ecc81372d8ab27d2b1232ca97497..fd89950528333ef6242e6d5443904f5392ab7e56\n--- a/.github/prompts/cca-completion-check.md\n+++ b/.github/prompts/cca-completion-check.md\n@@ -19,6 +19,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n ## Required Reading\n \n Read only the files needed to judge the PR contract. Prefer this order:\ndiff --git a/AGENTS.md b/AGENTS.md\nindex 7f041100d49c2a7525f51cb0f5d696a4a6d1c78c..2b700e989ff295987aa8a4b0c5fcb1acd80020da\n--- a/AGENTS.md\n+++ b/AGENTS.md\n@@ -44,6 +44,10 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use\n+`SADR-NNNN-*.md`; target product decisions retain `NNNN-*.md` and the `ADR`\n+namespace.\n+\n Use the canonical source-of-truth order above. Runtime-specific reading orders\n are operational guidance only and must not override or reorder it.\n \ndiff --git a/CLAUDE.md b/CLAUDE.md\nindex 98a932e7841a632866fcd53b0bad0e3af3711d3d..611d491912d6a72a0327a1731b8f33a41acb4baa\n--- a/CLAUDE.md\n+++ b/CLAUDE.md\n@@ -14,6 +14,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n ## Claude Role\n \n Default to planner, default implementer, reviewer, coordinator, documentation editor, CCA-style completion judge, and final-judgment assistant.\ndiff --git a/SYSTEM_PROMPT.md b/SYSTEM_PROMPT.md\nindex aaaea45e3a896c3a44ba89eec8bb84325ecf6c62..2e7c093ebe9e8bb34e0923756f191b4f7fa581f5\n--- a/SYSTEM_PROMPT.md\n+++ b/SYSTEM_PROMPT.md\n@@ -30,6 +30,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n Use the canonical source-of-truth order above. Runtime-specific reading orders\n are operational guidance only and must not override or reorder it. The current\n conversation is non-durable operator input.\ndiff --git a/docs/adr/0001-keep-shiki-as-platform-name.md b/docs/adr/SADR-0001-keep-shiki-as-platform-name.md\nindex 20a9637c3b9d8c97de7ba8db7cfae4d82d9a7dba..8b685439959aada1b221c112f0ac3c51ca8a1695\n--- a/docs/adr/0001-keep-shiki-as-platform-name.md\n+++ b/docs/adr/SADR-0001-keep-shiki-as-platform-name.md\n@@ -1,3 +1,3 @@\n-# Keep Shiki As The Platform Name\n+# SADR-0001: Keep Shiki As The Platform Name\n \n Shiki remains the product name for the GitHub-native agentic engineering control plane. We considered more descriptive names such as MergePilot, MergeGate, and Kiroku, but kept Shiki because it carries the existing goal-seeking framework identity while remaining independent of Hermes, Codex, Claude Code, or any single runtime.\ndiff --git a/docs/adr/0002-use-agents-as-shared-constitution.md b/docs/adr/SADR-0002-use-agents-as-shared-constitution.md\nindex 73de30ecb078120eac540d9bc51855e3cd48d0e3..c5486182bf06fca36ea508496b013c67302fd989\n--- a/docs/adr/0002-use-agents-as-shared-constitution.md\n+++ b/docs/adr/SADR-0002-use-agents-as-shared-constitution.md\n@@ -1,3 +1,3 @@\n-# Use AGENTS As The Shared Agent Constitution\n+# SADR-0002: Use AGENTS As The Shared Agent Constitution\n \n Shiki uses `AGENTS.md` as the runtime-neutral constitution and `CLAUDE.md` as a thin Claude Code wrapper. This keeps Codex, Claude Code, Hermes Runner, and future Agent Runtimes aligned on the same GitHub-first MergeGate rules while still allowing runtime-specific instructions where needed.\ndiff --git a/docs/adr/0003-require-skill-gate-for-mergegate-readiness.md b/docs/adr/SADR-0003-require-skill-gate-for-mergegate-readiness.md\nindex 424be0c9af4c8f0136f1fde68a468c47ff84e764..7fc337987c7df9792f75f181dfcbed75fe5ffff2\n--- a/docs/adr/0003-require-skill-gate-for-mergegate-readiness.md\n+++ b/docs/adr/SADR-0003-require-skill-gate-for-mergegate-readiness.md\n@@ -1,3 +1,3 @@\n-# Require Skill Gate For MergeGate Readiness\n+# SADR-0003: Require Skill Gate For MergeGate Readiness\n \n Shiki requires relevant engineering skills to be invoked before execution when their triggers apply. This makes skills part of MergeGate readiness rather than optional prompt style, because Shiki depends on repeatable planning, issue decomposition, TDD, diagnosis, triage, and architecture review before autonomous or parallel agent work can be trusted.\ndiff --git a/docs/adr/0004-use-github-first-with-shiki-mirror.md b/docs/adr/SADR-0004-use-github-first-with-shiki-mirror.md\nindex d0a3a9174dc646a945a9d325954ffaf00c352040..a79c54ae9ee8eed2a0524192b864da4e22a92d3f\n--- a/docs/adr/0004-use-github-first-with-shiki-mirror.md\n+++ b/docs/adr/SADR-0004-use-github-first-with-shiki-mirror.md\n@@ -1,4 +1,4 @@\n-# ADR 0004: Use GitHub First With A Repository-Local Shiki Mirror\n+# SADR-0004: Use GitHub First With A Repository-Local Shiki Mirror\n \n ## Status\n \ndiff --git a/docs/adr/0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md b/docs/adr/SADR-0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\nindex b339baf49b89d9290e79c3d51b36e7b2bde3bd95..c30a0d8b1fde0224d0ac7820e7870dccf721580d\n--- a/docs/adr/0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\n+++ b/docs/adr/SADR-0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\n@@ -1,4 +1,4 @@\n-# ADR 0005: Use Codex As Front And Claude Code Action As The GitHub Runtime\n+# SADR-0005: Use Codex As Front And Claude Code Action As The GitHub Runtime\n \n ## Status\n \ndiff --git a/docs/adr/0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md b/docs/adr/SADR-0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\nindex 4bcae9d1e52f0a37b8e9fbafa5cd14b6ee1993bd..7a38538a6ae9929567f0ade67e7ea6a60d0b3f7e\n--- a/docs/adr/0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\n+++ b/docs/adr/SADR-0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\n@@ -1,4 +1,4 @@\n-# ADR 0006: Separate Codex Implementation, CCA Completion, And MergeGate Authorization\n+# SADR-0006: Separate Codex Implementation, CCA Completion, And MergeGate Authorization\n \n ## Status\n \ndiff --git a/docs/adr/0007-packaging-and-release.md b/docs/adr/SADR-0007-packaging-and-release.md\nindex 79b84ddb97167372b44641e5b7860e400ce80822..023cd57f8011aa6e6d2ebf612b7c35a7a82c00d4\n--- a/docs/adr/0007-packaging-and-release.md\n+++ b/docs/adr/SADR-0007-packaging-and-release.md\n@@ -1,4 +1,4 @@\n-# ADR 0007: Packaging And Release For Shiki\n+# SADR-0007: Packaging And Release For Shiki\n \n ## Status\n \ndiff --git a/docs/adr/0008-use-claude-code-as-default-implementer.md b/docs/adr/SADR-0008-use-claude-code-as-default-implementer.md\nindex dedf4510b7c7799521422f622029563d373db031..a158a8d444d93764ec515a28133f7dde4af23579\n--- a/docs/adr/0008-use-claude-code-as-default-implementer.md\n+++ b/docs/adr/SADR-0008-use-claude-code-as-default-implementer.md\n@@ -1,4 +1,4 @@\n-# ADR 0008: Use Claude Code As The Default Implementer Runtime\n+# SADR-0008: Use Claude Code As The Default Implementer Runtime\n \n ## Status\n \ndiff --git a/docs/adr/0009-requirements-definition-and-spec-freeze.md b/docs/adr/SADR-0009-requirements-definition-and-spec-freeze.md\nindex 29b3f7871bef9203a8b4a831a65c39eb2f506e42..52e7dd69ad0aa4efe0b68c62678abbf7df02aaec\n--- a/docs/adr/0009-requirements-definition-and-spec-freeze.md\n+++ b/docs/adr/SADR-0009-requirements-definition-and-spec-freeze.md\n@@ -1,4 +1,4 @@\n-# ADR 0009: Merge Grill And Planning Into Requirements Definition With Spec Freeze\n+# SADR-0009: Merge Grill And Planning Into Requirements Definition With Spec Freeze\n \n ## Status\n \ndiff --git a/docs/adr/0010-external-ai-guardian-review.md b/docs/adr/SADR-0010-external-ai-guardian-review.md\nindex e6108bbe8bb4e73e0ff4684c8f78001a80438c4e..e690d4eddd270e683d709c9b679c5859e71eeaaa\n--- a/docs/adr/0010-external-ai-guardian-review.md\n+++ b/docs/adr/SADR-0010-external-ai-guardian-review.md\n@@ -1,4 +1,4 @@\n-# ADR 0010: External AI Guardian Review As A First-Class Approval Authority\n+# SADR-0010: External AI Guardian Review As A First-Class Approval Authority\n \n ## Status\n \ndiff --git a/docs/adr/0011-autonomous-quality-gate-evidence.md b/docs/adr/SADR-0011-autonomous-quality-gate-evidence.md\nindex bca0246c5d46e44afbde15486a75eec425fc02f5..9c93eac16d8c3a6d4b11bd7224e3955102cd7db4\n--- a/docs/adr/0011-autonomous-quality-gate-evidence.md\n+++ b/docs/adr/SADR-0011-autonomous-quality-gate-evidence.md\n@@ -1,4 +1,4 @@\n-# ADR 0011: Autonomous Quality-Gate Evidence \u2014 Deterministic Facts And An Independent Verifier\n+# SADR-0011: Autonomous Quality-Gate Evidence \u2014 Deterministic Facts And An Independent Verifier\n \n ## Status\n \ndiff --git a/docs/adr/0012-autonomous-loop-closeout-to-main.md b/docs/adr/SADR-0012-autonomous-loop-closeout-to-main.md\nindex 637fad6dde4989c9c2bf0f81f676d8d29991123c..ec7d85f81030a676bcbba28b43eb365422a36eac\n--- a/docs/adr/0012-autonomous-loop-closeout-to-main.md\n+++ b/docs/adr/SADR-0012-autonomous-loop-closeout-to-main.md\n@@ -1,4 +1,4 @@\n-# 0012 \u2014 Autonomous loop pushes goal completion to main via a closeout PR\n+# SADR-0012 \u2014 Autonomous loop pushes goal completion to main via a closeout PR\n \n - Status: accepted\n - Date: 2026-06-18\ndiff --git a/docs/adr/0013-bootstrap-grants-actions-approve-pull-requests.md b/docs/adr/SADR-0013-bootstrap-grants-actions-approve-pull-requests.md\nindex b6c69d64f1c5ced346b88d6859586715b3b5bd0e..a793e7277d67ab8ea56286fec289ca689825a17d\n--- a/docs/adr/0013-bootstrap-grants-actions-approve-pull-requests.md\n+++ b/docs/adr/SADR-0013-bootstrap-grants-actions-approve-pull-requests.md\n@@ -1,4 +1,4 @@\n-# ADR 0013: Bootstrap Grants GitHub Actions Permission To Create And Approve Pull Requests\n+# SADR-0013: Bootstrap Grants GitHub Actions Permission To Create And Approve Pull Requests\n \n ## Status\n \ndiff --git a/docs/adr/0014-codex-app-external-ai-guardian-ui-adapter.md b/docs/adr/SADR-0014-codex-app-external-ai-guardian-ui-adapter.md\nindex 238db06279334caa341c117362df9271382a7fcd..65f12e9939321e1b3e389a34c822fc1549e5155d\n--- a/docs/adr/0014-codex-app-external-ai-guardian-ui-adapter.md\n+++ b/docs/adr/SADR-0014-codex-app-external-ai-guardian-ui-adapter.md\n@@ -1,4 +1,4 @@\n-# ADR 0014: Use Codex App As The External AI Guardian UI Adapter\n+# SADR-0014: Use Codex App As The External AI Guardian UI Adapter\n \n ## Status\n \ndiff --git a/docs/adr/0015-contract-approval-before-dispatch.md b/docs/adr/SADR-0015-contract-approval-before-dispatch.md\nindex fad6c656741a20a7684ba16080a7db159f63a52f..7bdd760bb3e82eee4e1410f45c783b03b638d456\n--- a/docs/adr/0015-contract-approval-before-dispatch.md\n+++ b/docs/adr/SADR-0015-contract-approval-before-dispatch.md\n@@ -1,4 +1,4 @@\n-# ADR 0015: Approve The Task Contract Before Dispatch, Not The Diff After It\n+# SADR-0015: Approve The Task Contract Before Dispatch, Not The Diff After It\n \n ## Status\n \ndiff --git a/docs/adr/0016-locks-bound-product-paths.md b/docs/adr/SADR-0016-locks-bound-product-paths.md\nindex b5e850f3f1f7f4ef6e7f9d1763a29a51bd300c45..332cc17313498404f41a145291e38447b5de0dc4\n--- a/docs/adr/0016-locks-bound-product-paths.md\n+++ b/docs/adr/SADR-0016-locks-bound-product-paths.md\n@@ -1,4 +1,4 @@\n-# ADR 0016: Locks bound product paths; the Shiki mirror is bound by record identity\n+# SADR-0016: Locks bound product paths; the Shiki mirror is bound by record identity\n \n ## Status\n \ndiff --git a/docs/adr/0017-bookkeeping-closeouts-do-not-inherit-risk.md b/docs/adr/SADR-0017-bookkeeping-closeouts-do-not-inherit-risk.md\nindex 554e3a0d526a72ca067108c5464cc68c3edcf174..83115f69f1b1b48d544c9b0157f377b91cb53abf\n--- a/docs/adr/0017-bookkeeping-closeouts-do-not-inherit-risk.md\n+++ b/docs/adr/SADR-0017-bookkeeping-closeouts-do-not-inherit-risk.md\n@@ -1,4 +1,4 @@\n-# ADR 0017: A Bookkeeping Closeout Does Not Inherit Its Task's Risk\n+# SADR-0017: A Bookkeeping Closeout Does Not Inherit Its Task's Risk\n \n ## Status\n \ndiff --git a/docs/adr/0018-carried-guardian-approval-across-a-proven-base-sync.md b/docs/adr/SADR-0018-carried-guardian-approval-across-a-proven-base-sync.md\nindex 22358562da390ed8b45cd600da08a43d378b4680..38da0fd95102c3496c3219ce2319fce696a2532a\n--- a/docs/adr/0018-carried-guardian-approval-across-a-proven-base-sync.md\n+++ b/docs/adr/SADR-0018-carried-guardian-approval-across-a-proven-base-sync.md\n@@ -1,4 +1,4 @@\n-# ADR 0018: Carried Guardian Approval Across A Proven Base Sync\n+# SADR-0018: Carried Guardian Approval Across A Proven Base Sync\n \n ## Status\n \ndiff --git a/docs/adr/README.md b/docs/adr/README.md\nindex c70e057a4520c36e042ab68c53cd4a21ef709b71..5da43856836e2ce6a150f8127766e2f88355ab4b\n--- a/docs/adr/README.md\n+++ b/docs/adr/README.md\n@@ -1,40 +1,41 @@\n-# Architecture Decision Records\n+# Decision Record Namespaces\n \n-This directory holds Shiki's Architecture Decision Records (ADRs): durable\n-records of hard-to-reverse platform decisions. ADRs are part of the Shiki source\n-of truth alongside GitHub state, the `.shiki/` mirror, and `CONTEXT.md`.\n+This directory holds two independent decision namespaces:\n \n-## How ADRs Work\n+- Shiki Architecture Decision Records (SADRs), named\n+  `SADR-NNNN-<short-slug>.md`, record hard-to-reverse Shiki platform decisions.\n+- Target Architecture Decision Records (ADRs), named\n+  `NNNN-<short-slug>.md`, belong to the target product.\n \n-- New ADRs are added as `docs/adr/NNNN-<short-slug>.md`, where `NNNN` is the next\n-  zero-padded sequence number.\n-- The **canonical list of ADRs is the set of files matching `docs/adr/*.md`** in\n-  this directory. Browse `docs/adr/*.md` for the authoritative, current set.\n-- Use [`template.md`](template.md) as the starting point for a new ADR.\n-- Record an ADR when a decision is hard to reverse, surprising without context,\n-  and a real tradeoff. Do not write an ADR for routine or easily reversible\n-  choices.\n+The same four-digit number may appear once in each namespace. A platform\n+`SADR-NNNN` citation resolves only to `SADR-NNNN-*.md`; a target `ADR NNNN`\n+citation resolves only to `NNNN-*.md`.\n \n-Using the `docs/adr/*.md` glob as the source of truth means other PRs can add an\n-ADR without editing this index file, avoiding merge contention on a\n-hand-maintained enumeration.\n+## How Decision Records Work\n \n-## Current ADRs (snapshot)\n-\n-The list below is a convenience snapshot and may lag behind the directory. When\n-in doubt, list `docs/adr/*.md`.\n+- New Shiki platform decisions use the next zero-padded SADR number.\n+- New target product decisions use the target's own ADR sequence.\n+- The canonical list is the files in `docs/adr/`; no hand-maintained index is\n+  authoritative.\n+- Use [`template.md`](template.md) as the starting point and choose the filename\n+  namespace that owns the decision.\n+- Record a decision when it is hard to reverse, surprising without context, and\n+  a real tradeoff. Do not write a decision record for routine or easily\n+  reversible choices.\n \n-- [0001 \u2014 Keep Shiki As The Platform Name](0001-keep-shiki-as-platform-name.md)\n-- [0002 \u2014 Use AGENTS As The Shared Agent Constitution](0002-use-agents-as-shared-constitution.md)\n-- [0003 \u2014 Require Skill Gate For MergeGate Readiness](0003-require-skill-gate-for-mergegate-readiness.md)\n-- [0004 \u2014 Use GitHub First With A Repository-Local Shiki Mirror](0004-use-github-first-with-shiki-mirror.md)\n-- [0005 \u2014 Use Codex As Front And Claude Code Action As The GitHub Runtime](0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md)\n-- [0006 \u2014 Separate Codex Implementation, CCA Completion, And MergeGate Authorization](0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md)\n+Using the directory as the source of truth lets independent changes add records\n+without contending on an enumerated index.\n \n-## Adding A New ADR\n+## Adding A Shiki SADR\n \n-1. Copy `template.md` to `docs/adr/NNNN-<short-slug>.md`.\n+1. Copy `template.md` to `docs/adr/SADR-NNNN-<short-slug>.md`.\n 2. Fill in the title, status, context, decision, and consequences.\n-3. Set the status to `Proposed`, then `Accepted` once the decision is approved.\n-4. Open a PR following `CONTRIBUTING.md`. You do not need to edit this index for\n-   the ADR to be discoverable via `docs/adr/*.md`.\n+3. Set the status to `Proposed`, then `Accepted` once the proper authority has\n+   approved the decision.\n+4. Open a PR following `CONTRIBUTING.md`.\n+\n+## Adding A Target ADR\n+\n+1. Copy `template.md` to `docs/adr/NNNN-<short-slug>.md` using the target's own\n+   sequence.\n+2. Cite it as `ADR NNNN`; do not prefix its filename with `SADR-`.\ndiff --git a/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md b/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\nnew file mode 100644\nindex 0000000000000000000000000000000000000000..7455ce1f3bed02ddddb851ac21a1d1a648c9aeba\n--- /dev/null\n+++ b/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\n@@ -0,0 +1,55 @@\n+# SADR-0019: Separate Shiki Platform SADRs From Target ADRs\n+\n+## Status\n+\n+Accepted\n+\n+## Context\n+\n+Shiki installs its platform decision records into `docs/adr/` in every target\n+repository. Shiki and its targets previously used the same numeric\n+`NNNN-<slug>.md` namespace. A target that already owned ADRs 0013 through 0018\n+therefore acquired two records for each number during installation, making an\n+`ADR NNNN` citation ambiguous. Renumbering target decisions would rewrite target\n+history, while renumbering Shiki decisions per target would destroy their stable\n+platform identity.\n+\n+Upgrades also need to remove the former Shiki-owned numeric paths without ever\n+mistaking a target-owned ADR for platform content. The install stamp already\n+records exact shipped paths and content digests, so it can prove ownership only\n+when both still match.\n+\n+## Decision\n+\n+Shiki platform decisions use the `SADR` namespace:\n+\n+- records are named `docs/adr/SADR-NNNN-<slug>.md`;\n+- live platform citations use `SADR-NNNN`;\n+- target product decisions retain `docs/adr/NNNN-<slug>.md` and `ADR NNNN`;\n+- the reference validator resolves the two namespaces independently, so the same\n+  four-digit number may exist once in each namespace.\n+\n+Before a forced upgrade writes anything, the installer inventories the exact\n+former numeric Shiki ADR paths. It may delete a legacy path only when the\n+existing install stamp contains that exact path and its recorded SHA-256 digest\n+matches the file currently on disk. All ownership blockers are reported\n+together and abort the upgrade before its first write. A non-forced install\n+never performs this cleanup.\n+\n+Historical `.shiki/` evidence remains unchanged because it records the namespace\n+that existed when that evidence was produced.\n+\n+## Consequences\n+\n+- Targets may use any ADR number without colliding with Shiki's platform SADRs.\n+- Fresh installs preserve target ADR bytes and install a stable SADR set.\n+- Forced upgrades fail closed when legacy ownership cannot be proven, and a\n+  successful upgrade refreshes the stamp without removed legacy paths.\n+- Current governance, packaging, workflow, prompt, script, and skill references\n+  must move with the platform records.\n+\n+## Alternatives Considered\n+\n+- Collision-only refusal leaves affected targets permanently unupgradable.\n+- Renumbering target ADRs mutates target-owned history.\n+- Per-target Shiki renumbering makes platform citations non-portable.\ndiff --git a/docs/adr/template.md b/docs/adr/template.md\nindex a92a97daddd0b00ab9dd626cc8d86942da2eafed..17dd79175b5fee2105935557f691b40cf9492942\n--- a/docs/adr/template.md\n+++ b/docs/adr/template.md\n@@ -1,14 +1,14 @@\n-# NNNN: <Short Decision Title>\n+# <SADR-NNNN or NNNN>: <Short Decision Title>\n \n ## Status\n \n-Proposed | Accepted | Superseded by ADR-NNNN\n+Proposed | Accepted | Superseded by SADR-NNNN or target ADR NNNN\n \n ## Context\n \n What is the situation, constraint, or force that requires a decision? Reference\n-relevant domain terms (`CONTEXT.md`), prior ADRs, and code reality. State the\n-problem, not the solution.\n+relevant domain terms (`CONTEXT.md`), prior Shiki SADRs or target ADRs, and code\n+reality. State the problem, not the solution.\n \n ## Decision\n \ndiff --git a/docs/agents/domain.md b/docs/agents/domain.md\nindex 258da8ea81e87ac0a6b0b5b5e5c2fde980eaabb7..67aaa6f4b7d0a5aa4d495ce6279073333f7798cb\n--- a/docs/agents/domain.md\n+++ b/docs/agents/domain.md\n@@ -5,7 +5,9 @@\n ## Layout\n \n - `CONTEXT.md` defines Shiki language. Keep it implementation-free.\n-- `docs/adr/` records hard-to-reverse decisions.\n+- `docs/adr/SADR-NNNN-*.md` records Shiki platform decisions.\n+- `docs/adr/NNNN-*.md` records target product decisions without sharing the\n+  SADR namespace.\n - `AGENTS.md` is the runtime-neutral agent constitution.\n - `CLAUDE.md` is the Claude Code wrapper around `AGENTS.md`.\n - `.shiki/` is the Target Repository mirror for Goals, PRDs, plans, Task DAGs, contracts, locks, worktree records, ledger entries, CCA verdicts, repair packets, reports, and handoffs.\ndiff --git a/docs/agents/implementation-policy.md b/docs/agents/implementation-policy.md\nindex c754144a9fb5bf4a38aa82a60e1670c59e626b44..e90d40fe21880b9a09719c5ecfe6bde7284eb3ce\n--- a/docs/agents/implementation-policy.md\n+++ b/docs/agents/implementation-policy.md\n@@ -31,6 +31,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n ## Non-Negotiables\n \n 1. **Planning precedes implementation.** Non-trivial Goals start with `grill-with-docs` and Context & Impact.\ndiff --git a/tests/test_adr_references.py b/tests/test_adr_references.py\nindex 51706329c797d5dd9b47e082a333fa83b261d482..91ab76b724cb973435c45d53cab63793942267e9\n--- a/tests/test_adr_references.py\n+++ b/tests/test_adr_references.py\n@@ -1,4 +1,4 @@\n-\"\"\"Every ``ADR ####`` citation must resolve to exactly one decision record.\n+\"\"\"Every target ADR and Shiki SADR citation resolves in its own namespace.\n \n Background. ``scripts/mergegate_check.py`` justifies ``is_bookkeeping_closeout``\n with \"ADR 0017\" nine times and ``scripts/guardian_approval_signal.py`` cites\n@@ -11,11 +11,12 @@\n and the directory drifted with no signal.\n \n This is the same unbound-pair shape behind other silent failures: a reference\n-and its target with nothing asserting they agree. This test binds them. It scans\n-every ``scripts/*.py`` (plus ``docs/**/*.md`` and the root ``*.md`` governance\n-files, which are cheap) for the ``ADR ####`` pattern and asserts each cited\n-number resolves to exactly one ``docs/adr/####-*.md``. The converse is\n-deliberately NOT required: an ADR may exist without being cited.\n+and its target with nothing asserting they agree. This test binds them. Target\n+``ADR NNNN`` citations resolve only to ``docs/adr/NNNN-*.md``; Shiki\n+``SADR-NNNN`` citations resolve only to ``docs/adr/SADR-NNNN-*.md``. The scan\n+covers current governance docs, scripts, workflows, prompts, packaging metadata,\n+and repo-local Codex guidance while deliberately excluding historical\n+``.shiki`` evidence. The converse is not required: a record may be uncited.\n \n The suite is dependency free: standard-library ``unittest`` only.\n \"\"\"\n@@ -31,17 +32,18 @@\n \n REPO_ROOT = Path(__file__).resolve().parents[1]\n \n-# \"ADR 0016\" -- a four-digit citation. The trailing \\b keeps a five-digit token\n-# from being read as a truncated four-digit number.\n-_CITATION = re.compile(r\"\\bADR (\\d{4})\\b\")\n-# \"0016-locks-bound-product-paths.md\" -- the leading four digits name the record.\n-_ADR_FILENAME = re.compile(r\"^(\\d{4})-.*\\.md$\")\n+# ``ADR 0016`` / ``SADR-0016`` -- a namespace plus four-digit citation. The\n+# trailing ``\\b`` keeps a five-digit token from being read as a truncated one.\n+_CITATION = re.compile(r\"\\b(S?ADR)[ -](\\d{4})\\b\")\n+# Target ADRs retain the numeric filename; Shiki platform decisions use SADR.\n+_DECISION_FILENAME = re.compile(r\"^(?:(SADR)-)?(\\d{4})-.*\\.md$\")\n \n \n class Violation(NamedTuple):\n     \"\"\"A citation that does not resolve to exactly one decision record.\"\"\"\n \n     citing_file: str  # repo-relative path of the file that cites the ADR\n+    namespace: str  # \"ADR\" for target decisions; \"SADR\" for Shiki decisions\n     number: str  # the cited four-digit number, e.g. \"0016\"\n     line: int  # 1-based line of the citation\n     reason: str  # \"missing\" (zero matches) or \"ambiguous\" (two or more)\n@@ -49,44 +51,59 @@\n \n     @property\n     def expected_path(self) -> str:\n-        return f\"docs/adr/{self.number}-*.md\"\n+        prefix = \"SADR-\" if self.namespace == \"SADR\" else \"\"\n+        return f\"docs/adr/{prefix}{self.number}-*.md\"\n \n     def describe(self) -> str:\n         \"\"\"Name the citing file, the cited number, and the missing path.\"\"\"\n         if self.reason == \"missing\":\n             return (\n-                f\"{self.citing_file}:{self.line} cites ADR {self.number} \"\n+                f\"{self.citing_file}:{self.line} cites {self.namespace} {self.number} \"\n                 f\"but no document matches {self.expected_path}\"\n             )\n         joined = \", \".join(self.matches)\n         return (\n-            f\"{self.citing_file}:{self.line} cites ADR {self.number} which \"\n+            f\"{self.citing_file}:{self.line} cites {self.namespace} {self.number} which \"\n             f\"resolves to {len(self.matches)} documents (expected exactly one \"\n             f\"at {self.expected_path}): {joined}\"\n         )\n \n \n-def _adr_index(root: Path) -> dict[str, list[Path]]:\n-    \"\"\"Map every four-digit ADR number to the record files that claim it.\"\"\"\n-    index: dict[str, list[Path]] = {}\n+def _adr_index(root: Path) -> dict[tuple[str, str], list[Path]]:\n+    \"\"\"Map each decision namespace and number to the files that claim it.\"\"\"\n+    index: dict[tuple[str, str], list[Path]] = {}\n     adr_dir = root / \"docs\" / \"adr\"\n     if adr_dir.is_dir():\n         for path in sorted(adr_dir.glob(\"*.md\")):\n-            match = _ADR_FILENAME.match(path.name)\n+            match = _DECISION_FILENAME.match(path.name)\n             if match:\n-                index.setdefault(match.group(1), []).append(path)\n+                namespace = \"SADR\" if match.group(1) else \"ADR\"\n+                index.setdefault((namespace, match.group(2)), []).append(path)\n     return index\n \n \n def _citing_files(root: Path, include_docs: bool = True) -> list[Path]:\n-    \"\"\"Files scanned for citations: scripts/*.py, and cheaply the markdown.\"\"\"\n+    \"\"\"Current, shipped text surfaces scanned for decision citations.\"\"\"\n     files = sorted((root / \"scripts\").glob(\"*.py\"))\n+    files += sorted((root / \"scripts\").glob(\"*.sh\"))\n     if include_docs:\n         docs = root / \"docs\"\n         if docs.is_dir():\n             files += sorted(docs.rglob(\"*.md\"))\n         files += sorted(root.glob(\"*.md\"))\n-    return files\n+        pyproject = root / \"pyproject.toml\"\n+        if pyproject.is_file():\n+            files.append(pyproject)\n+        for relative in (\".github\", \".codex\"):\n+            surface = root / relative\n+            if surface.is_dir():\n+                files += sorted(\n+                    path\n+                    for path in surface.rglob(\"*\")\n+                    if path.is_file()\n+                    and path.suffix in {\".md\", \".py\", \".sh\", \".toml\", \".yaml\", \".yml\"}\n+                )\n+    return sorted(set(files))\n \n \n def find_adr_reference_violations(\n@@ -94,10 +111,9 @@\n ) -> list[Violation]:\n     \"\"\"Return every ``ADR ####`` citation under ``root`` that fails to resolve.\n \n-    A citation resolves when exactly one ``docs/adr/####-*.md`` matches its\n-    number. Zero matches is a \"missing\" violation; two or more is \"ambiguous\".\n-    Records that are never cited produce nothing -- the converse is not\n-    required.\n+    A citation resolves when exactly one record in the same namespace matches\n+    its number. Zero matches is a \"missing\" violation; two or more is\n+    \"ambiguous\". Records that are never cited produce nothing.\n     \"\"\"\n     root = Path(root)\n     index = _adr_index(root)\n@@ -110,13 +126,15 @@\n         rel = path.relative_to(root).as_posix()\n         for lineno, line in enumerate(text.splitlines(), start=1):\n             for match in _CITATION.finditer(line):\n-                number = match.group(1)\n-                found = index.get(number, [])\n+                namespace, number = match.groups()\n+                found = index.get((namespace, number), [])\n                 if len(found) == 1:\n                     continue\n                 reason = \"missing\" if not found else \"ambiguous\"\n                 rels = [p.relative_to(root).as_posix() for p in found]\n-                violations.append(Violation(rel, number, lineno, reason, rels))\n+                violations.append(\n+                    Violation(rel, namespace, number, lineno, reason, rels)\n+                )\n     return violations\n \n \n@@ -129,8 +147,8 @@\n         self.assertEqual(\n             violations,\n             [],\n-            \"Every 'ADR ####' citation must resolve to exactly one \"\n-            f\"docs/adr/####-*.md:\\n{detail}\",\n+            \"Every ADR/SADR citation must resolve exactly once in its own \"\n+            f\"docs/adr namespace:\\n{detail}\",\n         )\n \n     def test_scan_actually_reads_scripts(self) -> None:\n@@ -146,8 +164,21 @@\n         # The two records this Goal carried onto the default branch, and which\n         # the scripts cite most, must be present.\n         index = _adr_index(REPO_ROOT)\n-        self.assertIn(\"0016\", index)\n-        self.assertIn(\"0017\", index)\n+        self.assertIn((\"SADR\", \"0016\"), index)\n+        self.assertIn((\"SADR\", \"0017\"), index)\n+\n+    def test_platform_tree_has_only_sadr_0001_through_0019(self) -> None:\n+        adr_dir = REPO_ROOT / \"docs\" / \"adr\"\n+        sadrs = sorted(adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\"))\n+        self.assertEqual(\n+            {path.name.split(\"-\", 2)[1] for path in sadrs},\n+            {f\"{number:04d}\" for number in range(1, 20)},\n+        )\n+        self.assertEqual(\n+            list(adr_dir.glob(\"[0-9][0-9][0-9][0-9]-*.md\")),\n+            [],\n+            \"the Shiki platform tree must not retain legacy numeric SADR paths\",\n+        )\n \n \n class FixtureAdrReferences(unittest.TestCase):\n@@ -180,6 +211,26 @@\n         self.assertIn(\"9991\", message)  # cited number\n         self.assertIn(\"docs/adr/9991-*.md\", message)  # missing path\n \n+    def test_target_adr_does_not_satisfy_missing_platform_sadr(self) -> None:\n+        root = self._make_repo(\n+            adr_names=[\"0042-target-decision.md\"],\n+            script_body='\"\"\"Target ADR 0042 exists, but platform SADR-0042 does not.\"\"\"\\n',\n+        )\n+        violations = find_adr_reference_violations(root, include_docs=False)\n+        self.assertEqual(len(violations), 1)\n+        self.assertEqual(violations[0].namespace, \"SADR\")\n+        self.assertEqual(violations[0].number, \"0042\")\n+        self.assertEqual(violations[0].expected_path, \"docs/adr/SADR-0042-*.md\")\n+\n+    def test_target_adr_and_platform_sadr_may_reuse_a_number(self) -> None:\n+        root = self._make_repo(\n+            adr_names=[\"0042-target-decision.md\", \"SADR-0042-platform-decision.md\"],\n+            script_body='\"\"\"Target ADR 0042 and platform SADR-0042 are distinct.\"\"\"\\n',\n+        )\n+        self.assertEqual(\n+            find_adr_reference_violations(root, include_docs=False), []\n+        )\n+\n     def test_uncited_adr_is_allowed(self) -> None:\n         # An ADR exists but nothing cites it -> not a violation. The converse\n         # (every ADR must be cited) is explicitly NOT required.\ndiff --git a/tests/test_sadr_install_migration.py b/tests/test_sadr_install_migration.py\nnew file mode 100644\nindex 0000000000000000000000000000000000000000..e1c1f87329132324dcb6bcdea757669752ad582b\n--- /dev/null\n+++ b/tests/test_sadr_install_migration.py\n@@ -0,0 +1,134 @@\n+\"\"\"SADR installation and collision-safe legacy upgrade behavior.\"\"\"\n+\n+from __future__ import annotations\n+\n+import contextlib\n+import hashlib\n+import io\n+import json\n+import shutil\n+import tempfile\n+import unittest\n+from pathlib import Path\n+\n+import shiki_test_support  # noqa: F401  (path bootstrap)\n+\n+import shiki_installer\n+from tests.test_adr_references import find_adr_reference_violations\n+\n+\n+PRODUCT_NUMBERS = tuple(range(13, 19))\n+\n+\n+def _quiet_install(target: Path, *, force: bool, validate: bool) -> None:\n+    with contextlib.redirect_stdout(io.StringIO()):\n+        shiki_installer.install_template(target, force=force, validate=validate)\n+\n+\n+def _sha256(path: Path) -> str:\n+    return hashlib.sha256(path.read_bytes()).hexdigest()\n+\n+\n+def _legacy_sadr_paths() -> tuple[str, ...]:\n+    return tuple(\n+        f\"docs/adr/{path.name.removeprefix('SADR-')}\"\n+        for path in sorted((shiki_installer.ROOT / \"docs\" / \"adr\").glob(\"SADR-*.md\"))\n+        if path.name != \"SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\"\n+    )\n+\n+\n+class FreshSadrInstallTests(unittest.TestCase):\n+    def setUp(self) -> None:\n+        self.tmp = Path(tempfile.mkdtemp(prefix=\"shiki-sadr-fresh-\"))\n+        self.addCleanup(lambda: shutil.rmtree(self.tmp, ignore_errors=True))\n+        self.target = self.tmp / \"target\"\n+        self.adr_dir = self.target / \"docs\" / \"adr\"\n+        self.adr_dir.mkdir(parents=True)\n+        self.product_bytes = {\n+            f\"{number:04d}-product-decision.md\": (\n+                f\"# Product ADR {number:04d}\\n\\nTarget-owned bytes {number}.\\n\"\n+            ).encode()\n+            for number in PRODUCT_NUMBERS\n+        }\n+        for name, content in self.product_bytes.items():\n+            (self.adr_dir / name).write_bytes(content)\n+\n+    def test_fresh_install_preserves_product_adrs_and_installs_all_sadrs(self) -> None:\n+        _quiet_install(self.target, force=False, validate=True)\n+\n+        for name, expected in self.product_bytes.items():\n+            self.assertEqual((self.adr_dir / name).read_bytes(), expected)\n+\n+        sadr_numbers = {\n+            path.name.split(\"-\", 2)[1]\n+            for path in self.adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\")\n+        }\n+        self.assertEqual(sadr_numbers, {f\"{number:04d}\" for number in range(1, 20)})\n+\n+        numeric_records = {\n+            path.name\n+            for path in self.adr_dir.glob(\"[0-9][0-9][0-9][0-9]-*.md\")\n+        }\n+        self.assertEqual(numeric_records, set(self.product_bytes))\n+        self.assertEqual(find_adr_reference_violations(self.target), [])\n+\n+\n+class LegacySadrUpgradeTests(unittest.TestCase):\n+    def setUp(self) -> None:\n+        self.tmp = Path(tempfile.mkdtemp(prefix=\"shiki-sadr-upgrade-\"))\n+        self.addCleanup(lambda: shutil.rmtree(self.tmp, ignore_errors=True))\n+        self.target = self.tmp / \"target\"\n+        self.adr_dir = self.target / \"docs\" / \"adr\"\n+        self.adr_dir.mkdir(parents=True)\n+\n+        self.product_bytes = {\n+            f\"{number:04d}-cubeshare-product-decision.md\": (\n+                f\"# CubeShare ADR {number:04d}\\n\\nDo not rewrite {number}.\\n\"\n+            ).encode()\n+            for number in PRODUCT_NUMBERS\n+        }\n+        for name, content in self.product_bytes.items():\n+            (self.adr_dir / name).write_bytes(content)\n+\n+        self.legacy_paths = _legacy_sadr_paths()\n+        for relative in self.legacy_paths:\n+            path = self.target / relative\n+            path.write_bytes(f\"legacy Shiki bytes for {relative}\\n\".encode())\n+\n+        stamp_path = self.target / shiki_installer.INSTALL_STAMP_PATH\n+        stamp_path.parent.mkdir(parents=True, exist_ok=True)\n+        stamp = {\n+            \"version\": shiki_installer.INSTALL_STAMP_VERSION,\n+            \"platform_commit\": None,\n+            \"installed_at\": \"2000-01-01T00:00:00+00:00\",\n+            \"digests\": {\n+                relative: _sha256(self.target / relative)\n+                for relative in self.legacy_paths\n+            },\n+        }\n+        stamp_path.write_text(\n+            json.dumps(stamp, indent=2, sort_keys=True) + \"\\n\", encoding=\"utf-8\"\n+        )\n+\n+    def test_force_upgrade_deletes_only_stamped_legacy_sadrs(self) -> None:\n+        _quiet_install(self.target, force=True, validate=True)\n+\n+        for relative in self.legacy_paths:\n+            self.assertFalse((self.target / relative).exists(), relative)\n+        for name, expected in self.product_bytes.items():\n+            self.assertEqual((self.adr_dir / name).read_bytes(), expected)\n+\n+        sadrs = list(self.adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\"))\n+        self.assertEqual(len(sadrs), 19)\n+        refreshed = json.loads(\n+            (self.target / shiki_installer.INSTALL_STAMP_PATH).read_text(encoding=\"utf-8\")\n+        )\n+        for relative in self.legacy_paths:\n+            self.assertNotIn(relative, refreshed[\"digests\"])\n+        for path in sadrs:\n+            relative = path.relative_to(self.target).as_posix()\n+            self.assertEqual(refreshed[\"digests\"][relative], _sha256(path))\n+\n+\n+if __name__ == \"__main__\":\n+    unittest.main()\n\n exited 1 in 924ms:\nok\ntest_force_upgrade_deletes_only_stamped_legacy_sadrs (test_sadr_install_migration.LegacySadrUpgradeTests.test_force_upgrade_deletes_only_stamped_legacy_sadrs) ... FAIL\n\n======================================================================\nFAIL: test_force_upgrade_deletes_only_stamped_legacy_sadrs (test_sadr_install_migration.LegacySadrUpgradeTests.test_force_upgrade_deletes_only_stamped_legacy_sadrs)\n----------------------------------------------------------------------\nTraceback (most recent call last):\n  File \"/private/tmp/shiki-adr-collision.pa0KwY/.worktrees/shiki-t-20260807t073307221254z-2d22d654-introduc/tests/test_sadr_install_migration.py\", line 117, in test_force_upgrade_deletes_only_stamped_legacy_sadrs\n    self.assertFalse((self.target / relative).exists(), relative)\n    ~~~~~~~~~~~~~~~~^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^\nAssertionError: True is not false : docs/adr/0001-keep-shiki-as-platform-name.md\n\n----------------------------------------------------------------------\nRan 2 tests in 0.906s\n\nFAILED (failures=1)\n\napply patch\npatch: completed\n/private/tmp/shiki-adr-collision.pa0KwY/.worktrees/shiki-t-20260807t073307221254z-2d22d654-introduc/scripts/shiki_installer.py\ndiff --git a/.codex/skills/shiki/SKILL.md b/.codex/skills/shiki/SKILL.md\nindex c8e2e85d9d75d49cc4f2d6286ee22659c4e53e75..93cded623b2c15d9f65c76491a1c2d813ac9e6c0\n--- a/.codex/skills/shiki/SKILL.md\n+++ b/.codex/skills/shiki/SKILL.md\n@@ -18,6 +18,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n Codex CLI does not register this skill as a custom `/shiki` slash command. When\n the user types `/shiki` in Codex CLI and gets \"Unrecognized command\", explain\n that this is expected: invoke Shiki in Codex with natural language such as\n@@ -69,7 +72,7 @@\n \n ## Responsibilities\n \n-- Claude Code implements and repairs by default (`shiki runner claude`, ADR 0008); Codex implements and repairs tasks explicitly assigned to `codex`.\n+- Claude Code implements and repairs by default (`shiki runner claude`, SADR-0008); Codex implements and repairs tasks explicitly assigned to `codex`.\n - Claude Code Action can act as GitHub-side CCA or reviewer.\n - CCA judges completion.\n - MergeGate authorizes state transitions and merge readiness.\ndiff --git a/.github/prompts/cca-completion-check.md b/.github/prompts/cca-completion-check.md\nindex ed203ad5f302ecc81372d8ab27d2b1232ca97497..fd89950528333ef6242e6d5443904f5392ab7e56\n--- a/.github/prompts/cca-completion-check.md\n+++ b/.github/prompts/cca-completion-check.md\n@@ -19,6 +19,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n ## Required Reading\n \n Read only the files needed to judge the PR contract. Prefer this order:\ndiff --git a/AGENTS.md b/AGENTS.md\nindex 7f041100d49c2a7525f51cb0f5d696a4a6d1c78c..2b700e989ff295987aa8a4b0c5fcb1acd80020da\n--- a/AGENTS.md\n+++ b/AGENTS.md\n@@ -44,6 +44,10 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use\n+`SADR-NNNN-*.md`; target product decisions retain `NNNN-*.md` and the `ADR`\n+namespace.\n+\n Use the canonical source-of-truth order above. Runtime-specific reading orders\n are operational guidance only and must not override or reorder it.\n \ndiff --git a/CLAUDE.md b/CLAUDE.md\nindex 98a932e7841a632866fcd53b0bad0e3af3711d3d..611d491912d6a72a0327a1731b8f33a41acb4baa\n--- a/CLAUDE.md\n+++ b/CLAUDE.md\n@@ -14,6 +14,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n ## Claude Role\n \n Default to planner, default implementer, reviewer, coordinator, documentation editor, CCA-style completion judge, and final-judgment assistant.\ndiff --git a/SYSTEM_PROMPT.md b/SYSTEM_PROMPT.md\nindex aaaea45e3a896c3a44ba89eec8bb84325ecf6c62..2e7c093ebe9e8bb34e0923756f191b4f7fa581f5\n--- a/SYSTEM_PROMPT.md\n+++ b/SYSTEM_PROMPT.md\n@@ -30,6 +30,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n Use the canonical source-of-truth order above. Runtime-specific reading orders\n are operational guidance only and must not override or reorder it. The current\n conversation is non-durable operator input.\ndiff --git a/docs/adr/0001-keep-shiki-as-platform-name.md b/docs/adr/SADR-0001-keep-shiki-as-platform-name.md\nindex 20a9637c3b9d8c97de7ba8db7cfae4d82d9a7dba..8b685439959aada1b221c112f0ac3c51ca8a1695\n--- a/docs/adr/0001-keep-shiki-as-platform-name.md\n+++ b/docs/adr/SADR-0001-keep-shiki-as-platform-name.md\n@@ -1,3 +1,3 @@\n-# Keep Shiki As The Platform Name\n+# SADR-0001: Keep Shiki As The Platform Name\n \n Shiki remains the product name for the GitHub-native agentic engineering control plane. We considered more descriptive names such as MergePilot, MergeGate, and Kiroku, but kept Shiki because it carries the existing goal-seeking framework identity while remaining independent of Hermes, Codex, Claude Code, or any single runtime.\ndiff --git a/docs/adr/0002-use-agents-as-shared-constitution.md b/docs/adr/SADR-0002-use-agents-as-shared-constitution.md\nindex 73de30ecb078120eac540d9bc51855e3cd48d0e3..c5486182bf06fca36ea508496b013c67302fd989\n--- a/docs/adr/0002-use-agents-as-shared-constitution.md\n+++ b/docs/adr/SADR-0002-use-agents-as-shared-constitution.md\n@@ -1,3 +1,3 @@\n-# Use AGENTS As The Shared Agent Constitution\n+# SADR-0002: Use AGENTS As The Shared Agent Constitution\n \n Shiki uses `AGENTS.md` as the runtime-neutral constitution and `CLAUDE.md` as a thin Claude Code wrapper. This keeps Codex, Claude Code, Hermes Runner, and future Agent Runtimes aligned on the same GitHub-first MergeGate rules while still allowing runtime-specific instructions where needed.\ndiff --git a/docs/adr/0003-require-skill-gate-for-mergegate-readiness.md b/docs/adr/SADR-0003-require-skill-gate-for-mergegate-readiness.md\nindex 424be0c9af4c8f0136f1fde68a468c47ff84e764..7fc337987c7df9792f75f181dfcbed75fe5ffff2\n--- a/docs/adr/0003-require-skill-gate-for-mergegate-readiness.md\n+++ b/docs/adr/SADR-0003-require-skill-gate-for-mergegate-readiness.md\n@@ -1,3 +1,3 @@\n-# Require Skill Gate For MergeGate Readiness\n+# SADR-0003: Require Skill Gate For MergeGate Readiness\n \n Shiki requires relevant engineering skills to be invoked before execution when their triggers apply. This makes skills part of MergeGate readiness rather than optional prompt style, because Shiki depends on repeatable planning, issue decomposition, TDD, diagnosis, triage, and architecture review before autonomous or parallel agent work can be trusted.\ndiff --git a/docs/adr/0004-use-github-first-with-shiki-mirror.md b/docs/adr/SADR-0004-use-github-first-with-shiki-mirror.md\nindex d0a3a9174dc646a945a9d325954ffaf00c352040..a79c54ae9ee8eed2a0524192b864da4e22a92d3f\n--- a/docs/adr/0004-use-github-first-with-shiki-mirror.md\n+++ b/docs/adr/SADR-0004-use-github-first-with-shiki-mirror.md\n@@ -1,4 +1,4 @@\n-# ADR 0004: Use GitHub First With A Repository-Local Shiki Mirror\n+# SADR-0004: Use GitHub First With A Repository-Local Shiki Mirror\n \n ## Status\n \ndiff --git a/docs/adr/0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md b/docs/adr/SADR-0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\nindex b339baf49b89d9290e79c3d51b36e7b2bde3bd95..c30a0d8b1fde0224d0ac7820e7870dccf721580d\n--- a/docs/adr/0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\n+++ b/docs/adr/SADR-0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\n@@ -1,4 +1,4 @@\n-# ADR 0005: Use Codex As Front And Claude Code Action As The GitHub Runtime\n+# SADR-0005: Use Codex As Front And Claude Code Action As The GitHub Runtime\n \n ## Status\n \ndiff --git a/docs/adr/0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md b/docs/adr/SADR-0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\nindex 4bcae9d1e52f0a37b8e9fbafa5cd14b6ee1993bd..7a38538a6ae9929567f0ade67e7ea6a60d0b3f7e\n--- a/docs/adr/0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\n+++ b/docs/adr/SADR-0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\n@@ -1,4 +1,4 @@\n-# ADR 0006: Separate Codex Implementation, CCA Completion, And MergeGate Authorization\n+# SADR-0006: Separate Codex Implementation, CCA Completion, And MergeGate Authorization\n \n ## Status\n \ndiff --git a/docs/adr/0007-packaging-and-release.md b/docs/adr/SADR-0007-packaging-and-release.md\nindex 79b84ddb97167372b44641e5b7860e400ce80822..023cd57f8011aa6e6d2ebf612b7c35a7a82c00d4\n--- a/docs/adr/0007-packaging-and-release.md\n+++ b/docs/adr/SADR-0007-packaging-and-release.md\n@@ -1,4 +1,4 @@\n-# ADR 0007: Packaging And Release For Shiki\n+# SADR-0007: Packaging And Release For Shiki\n \n ## Status\n \ndiff --git a/docs/adr/0008-use-claude-code-as-default-implementer.md b/docs/adr/SADR-0008-use-claude-code-as-default-implementer.md\nindex dedf4510b7c7799521422f622029563d373db031..a158a8d444d93764ec515a28133f7dde4af23579\n--- a/docs/adr/0008-use-claude-code-as-default-implementer.md\n+++ b/docs/adr/SADR-0008-use-claude-code-as-default-implementer.md\n@@ -1,4 +1,4 @@\n-# ADR 0008: Use Claude Code As The Default Implementer Runtime\n+# SADR-0008: Use Claude Code As The Default Implementer Runtime\n \n ## Status\n \ndiff --git a/docs/adr/0009-requirements-definition-and-spec-freeze.md b/docs/adr/SADR-0009-requirements-definition-and-spec-freeze.md\nindex 29b3f7871bef9203a8b4a831a65c39eb2f506e42..52e7dd69ad0aa4efe0b68c62678abbf7df02aaec\n--- a/docs/adr/0009-requirements-definition-and-spec-freeze.md\n+++ b/docs/adr/SADR-0009-requirements-definition-and-spec-freeze.md\n@@ -1,4 +1,4 @@\n-# ADR 0009: Merge Grill And Planning Into Requirements Definition With Spec Freeze\n+# SADR-0009: Merge Grill And Planning Into Requirements Definition With Spec Freeze\n \n ## Status\n \ndiff --git a/docs/adr/0010-external-ai-guardian-review.md b/docs/adr/SADR-0010-external-ai-guardian-review.md\nindex e6108bbe8bb4e73e0ff4684c8f78001a80438c4e..e690d4eddd270e683d709c9b679c5859e71eeaaa\n--- a/docs/adr/0010-external-ai-guardian-review.md\n+++ b/docs/adr/SADR-0010-external-ai-guardian-review.md\n@@ -1,4 +1,4 @@\n-# ADR 0010: External AI Guardian Review As A First-Class Approval Authority\n+# SADR-0010: External AI Guardian Review As A First-Class Approval Authority\n \n ## Status\n \ndiff --git a/docs/adr/0011-autonomous-quality-gate-evidence.md b/docs/adr/SADR-0011-autonomous-quality-gate-evidence.md\nindex bca0246c5d46e44afbde15486a75eec425fc02f5..9c93eac16d8c3a6d4b11bd7224e3955102cd7db4\n--- a/docs/adr/0011-autonomous-quality-gate-evidence.md\n+++ b/docs/adr/SADR-0011-autonomous-quality-gate-evidence.md\n@@ -1,4 +1,4 @@\n-# ADR 0011: Autonomous Quality-Gate Evidence \u2014 Deterministic Facts And An Independent Verifier\n+# SADR-0011: Autonomous Quality-Gate Evidence \u2014 Deterministic Facts And An Independent Verifier\n \n ## Status\n \ndiff --git a/docs/adr/0012-autonomous-loop-closeout-to-main.md b/docs/adr/SADR-0012-autonomous-loop-closeout-to-main.md\nindex 637fad6dde4989c9c2bf0f81f676d8d29991123c..ec7d85f81030a676bcbba28b43eb365422a36eac\n--- a/docs/adr/0012-autonomous-loop-closeout-to-main.md\n+++ b/docs/adr/SADR-0012-autonomous-loop-closeout-to-main.md\n@@ -1,4 +1,4 @@\n-# 0012 \u2014 Autonomous loop pushes goal completion to main via a closeout PR\n+# SADR-0012 \u2014 Autonomous loop pushes goal completion to main via a closeout PR\n \n - Status: accepted\n - Date: 2026-06-18\ndiff --git a/docs/adr/0013-bootstrap-grants-actions-approve-pull-requests.md b/docs/adr/SADR-0013-bootstrap-grants-actions-approve-pull-requests.md\nindex b6c69d64f1c5ced346b88d6859586715b3b5bd0e..a793e7277d67ab8ea56286fec289ca689825a17d\n--- a/docs/adr/0013-bootstrap-grants-actions-approve-pull-requests.md\n+++ b/docs/adr/SADR-0013-bootstrap-grants-actions-approve-pull-requests.md\n@@ -1,4 +1,4 @@\n-# ADR 0013: Bootstrap Grants GitHub Actions Permission To Create And Approve Pull Requests\n+# SADR-0013: Bootstrap Grants GitHub Actions Permission To Create And Approve Pull Requests\n \n ## Status\n \ndiff --git a/docs/adr/0014-codex-app-external-ai-guardian-ui-adapter.md b/docs/adr/SADR-0014-codex-app-external-ai-guardian-ui-adapter.md\nindex 238db06279334caa341c117362df9271382a7fcd..65f12e9939321e1b3e389a34c822fc1549e5155d\n--- a/docs/adr/0014-codex-app-external-ai-guardian-ui-adapter.md\n+++ b/docs/adr/SADR-0014-codex-app-external-ai-guardian-ui-adapter.md\n@@ -1,4 +1,4 @@\n-# ADR 0014: Use Codex App As The External AI Guardian UI Adapter\n+# SADR-0014: Use Codex App As The External AI Guardian UI Adapter\n \n ## Status\n \ndiff --git a/docs/adr/0015-contract-approval-before-dispatch.md b/docs/adr/SADR-0015-contract-approval-before-dispatch.md\nindex fad6c656741a20a7684ba16080a7db159f63a52f..7bdd760bb3e82eee4e1410f45c783b03b638d456\n--- a/docs/adr/0015-contract-approval-before-dispatch.md\n+++ b/docs/adr/SADR-0015-contract-approval-before-dispatch.md\n@@ -1,4 +1,4 @@\n-# ADR 0015: Approve The Task Contract Before Dispatch, Not The Diff After It\n+# SADR-0015: Approve The Task Contract Before Dispatch, Not The Diff After It\n \n ## Status\n \ndiff --git a/docs/adr/0016-locks-bound-product-paths.md b/docs/adr/SADR-0016-locks-bound-product-paths.md\nindex b5e850f3f1f7f4ef6e7f9d1763a29a51bd300c45..332cc17313498404f41a145291e38447b5de0dc4\n--- a/docs/adr/0016-locks-bound-product-paths.md\n+++ b/docs/adr/SADR-0016-locks-bound-product-paths.md\n@@ -1,4 +1,4 @@\n-# ADR 0016: Locks bound product paths; the Shiki mirror is bound by record identity\n+# SADR-0016: Locks bound product paths; the Shiki mirror is bound by record identity\n \n ## Status\n \ndiff --git a/docs/adr/0017-bookkeeping-closeouts-do-not-inherit-risk.md b/docs/adr/SADR-0017-bookkeeping-closeouts-do-not-inherit-risk.md\nindex 554e3a0d526a72ca067108c5464cc68c3edcf174..83115f69f1b1b48d544c9b0157f377b91cb53abf\n--- a/docs/adr/0017-bookkeeping-closeouts-do-not-inherit-risk.md\n+++ b/docs/adr/SADR-0017-bookkeeping-closeouts-do-not-inherit-risk.md\n@@ -1,4 +1,4 @@\n-# ADR 0017: A Bookkeeping Closeout Does Not Inherit Its Task's Risk\n+# SADR-0017: A Bookkeeping Closeout Does Not Inherit Its Task's Risk\n \n ## Status\n \ndiff --git a/docs/adr/0018-carried-guardian-approval-across-a-proven-base-sync.md b/docs/adr/SADR-0018-carried-guardian-approval-across-a-proven-base-sync.md\nindex 22358562da390ed8b45cd600da08a43d378b4680..38da0fd95102c3496c3219ce2319fce696a2532a\n--- a/docs/adr/0018-carried-guardian-approval-across-a-proven-base-sync.md\n+++ b/docs/adr/SADR-0018-carried-guardian-approval-across-a-proven-base-sync.md\n@@ -1,4 +1,4 @@\n-# ADR 0018: Carried Guardian Approval Across A Proven Base Sync\n+# SADR-0018: Carried Guardian Approval Across A Proven Base Sync\n \n ## Status\n \ndiff --git a/docs/adr/README.md b/docs/adr/README.md\nindex c70e057a4520c36e042ab68c53cd4a21ef709b71..5da43856836e2ce6a150f8127766e2f88355ab4b\n--- a/docs/adr/README.md\n+++ b/docs/adr/README.md\n@@ -1,40 +1,41 @@\n-# Architecture Decision Records\n+# Decision Record Namespaces\n \n-This directory holds Shiki's Architecture Decision Records (ADRs): durable\n-records of hard-to-reverse platform decisions. ADRs are part of the Shiki source\n-of truth alongside GitHub state, the `.shiki/` mirror, and `CONTEXT.md`.\n+This directory holds two independent decision namespaces:\n \n-## How ADRs Work\n+- Shiki Architecture Decision Records (SADRs), named\n+  `SADR-NNNN-<short-slug>.md`, record hard-to-reverse Shiki platform decisions.\n+- Target Architecture Decision Records (ADRs), named\n+  `NNNN-<short-slug>.md`, belong to the target product.\n \n-- New ADRs are added as `docs/adr/NNNN-<short-slug>.md`, where `NNNN` is the next\n-  zero-padded sequence number.\n-- The **canonical list of ADRs is the set of files matching `docs/adr/*.md`** in\n-  this directory. Browse `docs/adr/*.md` for the authoritative, current set.\n-- Use [`template.md`](template.md) as the starting point for a new ADR.\n-- Record an ADR when a decision is hard to reverse, surprising without context,\n-  and a real tradeoff. Do not write an ADR for routine or easily reversible\n-  choices.\n+The same four-digit number may appear once in each namespace. A platform\n+`SADR-NNNN` citation resolves only to `SADR-NNNN-*.md`; a target `ADR NNNN`\n+citation resolves only to `NNNN-*.md`.\n \n-Using the `docs/adr/*.md` glob as the source of truth means other PRs can add an\n-ADR without editing this index file, avoiding merge contention on a\n-hand-maintained enumeration.\n+## How Decision Records Work\n \n-## Current ADRs (snapshot)\n-\n-The list below is a convenience snapshot and may lag behind the directory. When\n-in doubt, list `docs/adr/*.md`.\n+- New Shiki platform decisions use the next zero-padded SADR number.\n+- New target product decisions use the target's own ADR sequence.\n+- The canonical list is the files in `docs/adr/`; no hand-maintained index is\n+  authoritative.\n+- Use [`template.md`](template.md) as the starting point and choose the filename\n+  namespace that owns the decision.\n+- Record a decision when it is hard to reverse, surprising without context, and\n+  a real tradeoff. Do not write a decision record for routine or easily\n+  reversible choices.\n \n-- [0001 \u2014 Keep Shiki As The Platform Name](0001-keep-shiki-as-platform-name.md)\n-- [0002 \u2014 Use AGENTS As The Shared Agent Constitution](0002-use-agents-as-shared-constitution.md)\n-- [0003 \u2014 Require Skill Gate For MergeGate Readiness](0003-require-skill-gate-for-mergegate-readiness.md)\n-- [0004 \u2014 Use GitHub First With A Repository-Local Shiki Mirror](0004-use-github-first-with-shiki-mirror.md)\n-- [0005 \u2014 Use Codex As Front And Claude Code Action As The GitHub Runtime](0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md)\n-- [0006 \u2014 Separate Codex Implementation, CCA Completion, And MergeGate Authorization](0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md)\n+Using the directory as the source of truth lets independent changes add records\n+without contending on an enumerated index.\n \n-## Adding A New ADR\n+## Adding A Shiki SADR\n \n-1. Copy `template.md` to `docs/adr/NNNN-<short-slug>.md`.\n+1. Copy `template.md` to `docs/adr/SADR-NNNN-<short-slug>.md`.\n 2. Fill in the title, status, context, decision, and consequences.\n-3. Set the status to `Proposed`, then `Accepted` once the decision is approved.\n-4. Open a PR following `CONTRIBUTING.md`. You do not need to edit this index for\n-   the ADR to be discoverable via `docs/adr/*.md`.\n+3. Set the status to `Proposed`, then `Accepted` once the proper authority has\n+   approved the decision.\n+4. Open a PR following `CONTRIBUTING.md`.\n+\n+## Adding A Target ADR\n+\n+1. Copy `template.md` to `docs/adr/NNNN-<short-slug>.md` using the target's own\n+   sequence.\n+2. Cite it as `ADR NNNN`; do not prefix its filename with `SADR-`.\ndiff --git a/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md b/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\nnew file mode 100644\nindex 0000000000000000000000000000000000000000..7455ce1f3bed02ddddb851ac21a1d1a648c9aeba\n--- /dev/null\n+++ b/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\n@@ -0,0 +1,55 @@\n+# SADR-0019: Separate Shiki Platform SADRs From Target ADRs\n+\n+## Status\n+\n+Accepted\n+\n+## Context\n+\n+Shiki installs its platform decision records into `docs/adr/` in every target\n+repository. Shiki and its targets previously used the same numeric\n+`NNNN-<slug>.md` namespace. A target that already owned ADRs 0013 through 0018\n+therefore acquired two records for each number during installation, making an\n+`ADR NNNN` citation ambiguous. Renumbering target decisions would rewrite target\n+history, while renumbering Shiki decisions per target would destroy their stable\n+platform identity.\n+\n+Upgrades also need to remove the former Shiki-owned numeric paths without ever\n+mistaking a target-owned ADR for platform content. The install stamp already\n+records exact shipped paths and content digests, so it can prove ownership only\n+when both still match.\n+\n+## Decision\n+\n+Shiki platform decisions use the `SADR` namespace:\n+\n+- records are named `docs/adr/SADR-NNNN-<slug>.md`;\n+- live platform citations use `SADR-NNNN`;\n+- target product decisions retain `docs/adr/NNNN-<slug>.md` and `ADR NNNN`;\n+- the reference validator resolves the two namespaces independently, so the same\n+  four-digit number may exist once in each namespace.\n+\n+Before a forced upgrade writes anything, the installer inventories the exact\n+former numeric Shiki ADR paths. It may delete a legacy path only when the\n+existing install stamp contains that exact path and its recorded SHA-256 digest\n+matches the file currently on disk. All ownership blockers are reported\n+together and abort the upgrade before its first write. A non-forced install\n+never performs this cleanup.\n+\n+Historical `.shiki/` evidence remains unchanged because it records the namespace\n+that existed when that evidence was produced.\n+\n+## Consequences\n+\n+- Targets may use any ADR number without colliding with Shiki's platform SADRs.\n+- Fresh installs preserve target ADR bytes and install a stable SADR set.\n+- Forced upgrades fail closed when legacy ownership cannot be proven, and a\n+  successful upgrade refreshes the stamp without removed legacy paths.\n+- Current governance, packaging, workflow, prompt, script, and skill references\n+  must move with the platform records.\n+\n+## Alternatives Considered\n+\n+- Collision-only refusal leaves affected targets permanently unupgradable.\n+- Renumbering target ADRs mutates target-owned history.\n+- Per-target Shiki renumbering makes platform citations non-portable.\ndiff --git a/docs/adr/template.md b/docs/adr/template.md\nindex a92a97daddd0b00ab9dd626cc8d86942da2eafed..17dd79175b5fee2105935557f691b40cf9492942\n--- a/docs/adr/template.md\n+++ b/docs/adr/template.md\n@@ -1,14 +1,14 @@\n-# NNNN: <Short Decision Title>\n+# <SADR-NNNN or NNNN>: <Short Decision Title>\n \n ## Status\n \n-Proposed | Accepted | Superseded by ADR-NNNN\n+Proposed | Accepted | Superseded by SADR-NNNN or target ADR NNNN\n \n ## Context\n \n What is the situation, constraint, or force that requires a decision? Reference\n-relevant domain terms (`CONTEXT.md`), prior ADRs, and code reality. State the\n-problem, not the solution.\n+relevant domain terms (`CONTEXT.md`), prior Shiki SADRs or target ADRs, and code\n+reality. State the problem, not the solution.\n \n ## Decision\n \ndiff --git a/docs/agents/domain.md b/docs/agents/domain.md\nindex 258da8ea81e87ac0a6b0b5b5e5c2fde980eaabb7..67aaa6f4b7d0a5aa4d495ce6279073333f7798cb\n--- a/docs/agents/domain.md\n+++ b/docs/agents/domain.md\n@@ -5,7 +5,9 @@\n ## Layout\n \n - `CONTEXT.md` defines Shiki language. Keep it implementation-free.\n-- `docs/adr/` records hard-to-reverse decisions.\n+- `docs/adr/SADR-NNNN-*.md` records Shiki platform decisions.\n+- `docs/adr/NNNN-*.md` records target product decisions without sharing the\n+  SADR namespace.\n - `AGENTS.md` is the runtime-neutral agent constitution.\n - `CLAUDE.md` is the Claude Code wrapper around `AGENTS.md`.\n - `.shiki/` is the Target Repository mirror for Goals, PRDs, plans, Task DAGs, contracts, locks, worktree records, ledger entries, CCA verdicts, repair packets, reports, and handoffs.\ndiff --git a/docs/agents/implementation-policy.md b/docs/agents/implementation-policy.md\nindex c754144a9fb5bf4a38aa82a60e1670c59e626b44..e90d40fe21880b9a09719c5ecfe6bde7284eb3ce\n--- a/docs/agents/implementation-policy.md\n+++ b/docs/agents/implementation-policy.md\n@@ -31,6 +31,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n ## Non-Negotiables\n \n 1. **Planning precedes implementation.** Non-trivial Goals start with `grill-with-docs` and Context & Impact.\ndiff --git a/scripts/shiki_installer.py b/scripts/shiki_installer.py\nindex be064ab7a29ff6836f7f7a1d70500a43e0d11d7b..d3e3247edbf3e4be287a4667176d86f6500f8dc5\n--- a/scripts/shiki_installer.py\n+++ b/scripts/shiki_installer.py\n@@ -118,6 +118,7 @@\n # what lets a target carry it without a governance-schema change.\n INSTALL_STAMP_PATH = \".shiki/install-stamp.json\"\n INSTALL_STAMP_VERSION = 1\n+LEGACY_SHIKI_SADR_MAX = 18\n \n # Under --force the shipped surface splits three ways, so nothing is lost and\n # nothing is silently stale.\n@@ -237,9 +238,98 @@\n         )\n \n \n+def legacy_shiki_adr_paths() -> tuple[str, ...]:\n+    \"\"\"Former numeric paths for Shiki SADR-0001 through SADR-0018.\n+\n+    The authoritative slugs come from the current SADR filenames. SADR-0019 is\n+    the namespace decision itself and never shipped under a numeric legacy path.\n+    \"\"\"\n+    paths: list[str] = []\n+    for sadr in sorted((ROOT / \"docs\" / \"adr\").glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\")):\n+        number = sadr.name.split(\"-\", 2)[1]\n+        if int(number) <= LEGACY_SHIKI_SADR_MAX:\n+            paths.append(f\"docs/adr/{sadr.name.removeprefix('SADR-')}\")\n+    return tuple(paths)\n+\n+\n+def preflight_legacy_sadr_cleanup(target: Path, *, force: bool) -> tuple[str, ...]:\n+    \"\"\"Authorize exact legacy Shiki ADR removals before the first write.\n+\n+    A non-force install never removes existing files. A forced install needs no\n+    stamp when no legacy Shiki path is present. When legacy paths do exist, each\n+    must be named in a readable install stamp and its current bytes must match\n+    the recorded digest.\n+    \"\"\"\n+    if not force:\n+        return ()\n+    present = tuple(\n+        relative\n+        for relative in legacy_shiki_adr_paths()\n+        if (target / relative).exists() or (target / relative).is_symlink()\n+    )\n+    if not present:\n+        return ()\n+\n+    stamp_path = target / INSTALL_STAMP_PATH\n+    try:\n+        raw = stamp_path.read_text(encoding=\"utf-8\")\n+        stamp = json.loads(raw)\n+    except FileNotFoundError:\n+        detail = \"install stamp is absent\"\n+        blockers = [f\"{relative}: {detail}\" for relative in present]\n+    except (OSError, UnicodeDecodeError) as error:\n+        detail = f\"install stamp is unreadable ({error})\"\n+        blockers = [f\"{relative}: {detail}\" for relative in present]\n+    except json.JSONDecodeError as error:\n+        detail = f\"install stamp is malformed ({error})\"\n+        blockers = [f\"{relative}: {detail}\" for relative in present]\n+    else:\n+        if not isinstance(stamp, dict):\n+            blockers = [\n+                f\"{relative}: install stamp is malformed (expected an object)\"\n+                for relative in present\n+            ]\n+        else:\n+            digests = stamp.get(\"digests\")\n+            blockers = []\n+            if not isinstance(digests, dict):\n+                blockers.extend(\n+                    f\"{relative}: install stamp has no digest for this path\"\n+                    for relative in present\n+                )\n+            else:\n+                for relative in present:\n+                    expected = digests.get(relative)\n+                    if not isinstance(expected, str) or not expected:\n+                        blockers.append(\n+                            f\"{relative}: install stamp has no digest for this path\"\n+                        )\n+                        continue\n+                    try:\n+                        actual = _sha256_file(target / relative)\n+                    except OSError as error:\n+                        blockers.append(\n+                            f\"{relative}: legacy path is unreadable ({error})\"\n+                        )\n+                        continue\n+                    if actual != expected:\n+                        blockers.append(\n+                            f\"{relative}: digest mismatch (stamp {expected}, current {actual})\"\n+                        )\n+\n+    if blockers:\n+        joined = \"\\n\".join(f\"- {blocker}\" for blocker in blockers)\n+        raise ShikiError(\n+            \"legacy Shiki ADR cleanup is not authorized:\\n\"\n+            f\"{joined}\\nNo files were written.\"\n+        )\n+    return present\n+\n+\n def install_template(target: Path, *, force: bool, validate: bool) -> list[NewFileNote]:\n     # Preconditions first: never rewrite the tree of a target that cannot be\n     # upgraded cleanly (see refuse_pending_migrations).\n+    legacy_cleanup = preflight_legacy_sadr_cleanup(target, force=force)\n     refuse_pending_migrations(target)\n \n     # Resolve the target's required CODEOWNERS owner once from its\n@@ -250,6 +340,10 @@\n     # verbatim. See ``codeowners_required_owner``.\n     codeowners_owner = codeowners_required_owner(target)\n \n+    for relative in legacy_cleanup:\n+        (target / relative).unlink()\n+        info(f\"removed stamped legacy Shiki ADR: {target / relative}\")\n+\n     notes: list[NewFileNote] = []\n     for relative in TEMPLATE_PATHS:\n         source = ROOT / relative\ndiff --git a/tests/test_adr_references.py b/tests/test_adr_references.py\nindex 51706329c797d5dd9b47e082a333fa83b261d482..91ab76b724cb973435c45d53cab63793942267e9\n--- a/tests/test_adr_references.py\n+++ b/tests/test_adr_references.py\n@@ -1,4 +1,4 @@\n-\"\"\"Every ``ADR ####`` citation must resolve to exactly one decision record.\n+\"\"\"Every target ADR and Shiki SADR citation resolves in its own namespace.\n \n Background. ``scripts/mergegate_check.py`` justifies ``is_bookkeeping_closeout``\n with \"ADR 0017\" nine times and ``scripts/guardian_approval_signal.py`` cites\n@@ -11,11 +11,12 @@\n and the directory drifted with no signal.\n \n This is the same unbound-pair shape behind other silent failures: a reference\n-and its target with nothing asserting they agree. This test binds them. It scans\n-every ``scripts/*.py`` (plus ``docs/**/*.md`` and the root ``*.md`` governance\n-files, which are cheap) for the ``ADR ####`` pattern and asserts each cited\n-number resolves to exactly one ``docs/adr/####-*.md``. The converse is\n-deliberately NOT required: an ADR may exist without being cited.\n+and its target with nothing asserting they agree. This test binds them. Target\n+``ADR NNNN`` citations resolve only to ``docs/adr/NNNN-*.md``; Shiki\n+``SADR-NNNN`` citations resolve only to ``docs/adr/SADR-NNNN-*.md``. The scan\n+covers current governance docs, scripts, workflows, prompts, packaging metadata,\n+and repo-local Codex guidance while deliberately excluding historical\n+``.shiki`` evidence. The converse is not required: a record may be uncited.\n \n The suite is dependency free: standard-library ``unittest`` only.\n \"\"\"\n@@ -31,17 +32,18 @@\n \n REPO_ROOT = Path(__file__).resolve().parents[1]\n \n-# \"ADR 0016\" -- a four-digit citation. The trailing \\b keeps a five-digit token\n-# from being read as a truncated four-digit number.\n-_CITATION = re.compile(r\"\\bADR (\\d{4})\\b\")\n-# \"0016-locks-bound-product-paths.md\" -- the leading four digits name the record.\n-_ADR_FILENAME = re.compile(r\"^(\\d{4})-.*\\.md$\")\n+# ``ADR 0016`` / ``SADR-0016`` -- a namespace plus four-digit citation. The\n+# trailing ``\\b`` keeps a five-digit token from being read as a truncated one.\n+_CITATION = re.compile(r\"\\b(S?ADR)[ -](\\d{4})\\b\")\n+# Target ADRs retain the numeric filename; Shiki platform decisions use SADR.\n+_DECISION_FILENAME = re.compile(r\"^(?:(SADR)-)?(\\d{4})-.*\\.md$\")\n \n \n class Violation(NamedTuple):\n     \"\"\"A citation that does not resolve to exactly one decision record.\"\"\"\n \n     citing_file: str  # repo-relative path of the file that cites the ADR\n+    namespace: str  # \"ADR\" for target decisions; \"SADR\" for Shiki decisions\n     number: str  # the cited four-digit number, e.g. \"0016\"\n     line: int  # 1-based line of the citation\n     reason: str  # \"missing\" (zero matches) or \"ambiguous\" (two or more)\n@@ -49,44 +51,59 @@\n \n     @property\n     def expected_path(self) -> str:\n-        return f\"docs/adr/{self.number}-*.md\"\n+        prefix = \"SADR-\" if self.namespace == \"SADR\" else \"\"\n+        return f\"docs/adr/{prefix}{self.number}-*.md\"\n \n     def describe(self) -> str:\n         \"\"\"Name the citing file, the cited number, and the missing path.\"\"\"\n         if self.reason == \"missing\":\n             return (\n-                f\"{self.citing_file}:{self.line} cites ADR {self.number} \"\n+                f\"{self.citing_file}:{self.line} cites {self.namespace} {self.number} \"\n                 f\"but no document matches {self.expected_path}\"\n             )\n         joined = \", \".join(self.matches)\n         return (\n-            f\"{self.citing_file}:{self.line} cites ADR {self.number} which \"\n+            f\"{self.citing_file}:{self.line} cites {self.namespace} {self.number} which \"\n             f\"resolves to {len(self.matches)} documents (expected exactly one \"\n             f\"at {self.expected_path}): {joined}\"\n         )\n \n \n-def _adr_index(root: Path) -> dict[str, list[Path]]:\n-    \"\"\"Map every four-digit ADR number to the record files that claim it.\"\"\"\n-    index: dict[str, list[Path]] = {}\n+def _adr_index(root: Path) -> dict[tuple[str, str], list[Path]]:\n+    \"\"\"Map each decision namespace and number to the files that claim it.\"\"\"\n+    index: dict[tuple[str, str], list[Path]] = {}\n     adr_dir = root / \"docs\" / \"adr\"\n     if adr_dir.is_dir():\n         for path in sorted(adr_dir.glob(\"*.md\")):\n-            match = _ADR_FILENAME.match(path.name)\n+            match = _DECISION_FILENAME.match(path.name)\n             if match:\n-                index.setdefault(match.group(1), []).append(path)\n+                namespace = \"SADR\" if match.group(1) else \"ADR\"\n+                index.setdefault((namespace, match.group(2)), []).append(path)\n     return index\n \n \n def _citing_files(root: Path, include_docs: bool = True) -> list[Path]:\n-    \"\"\"Files scanned for citations: scripts/*.py, and cheaply the markdown.\"\"\"\n+    \"\"\"Current, shipped text surfaces scanned for decision citations.\"\"\"\n     files = sorted((root / \"scripts\").glob(\"*.py\"))\n+    files += sorted((root / \"scripts\").glob(\"*.sh\"))\n     if include_docs:\n         docs = root / \"docs\"\n         if docs.is_dir():\n             files += sorted(docs.rglob(\"*.md\"))\n         files += sorted(root.glob(\"*.md\"))\n-    return files\n+        pyproject = root / \"pyproject.toml\"\n+        if pyproject.is_file():\n+            files.append(pyproject)\n+        for relative in (\".github\", \".codex\"):\n+            surface = root / relative\n+            if surface.is_dir():\n+                files += sorted(\n+                    path\n+                    for path in surface.rglob(\"*\")\n+                    if path.is_file()\n+                    and path.suffix in {\".md\", \".py\", \".sh\", \".toml\", \".yaml\", \".yml\"}\n+                )\n+    return sorted(set(files))\n \n \n def find_adr_reference_violations(\n@@ -94,10 +111,9 @@\n ) -> list[Violation]:\n     \"\"\"Return every ``ADR ####`` citation under ``root`` that fails to resolve.\n \n-    A citation resolves when exactly one ``docs/adr/####-*.md`` matches its\n-    number. Zero matches is a \"missing\" violation; two or more is \"ambiguous\".\n-    Records that are never cited produce nothing -- the converse is not\n-    required.\n+    A citation resolves when exactly one record in the same namespace matches\n+    its number. Zero matches is a \"missing\" violation; two or more is\n+    \"ambiguous\". Records that are never cited produce nothing.\n     \"\"\"\n     root = Path(root)\n     index = _adr_index(root)\n@@ -110,13 +126,15 @@\n         rel = path.relative_to(root).as_posix()\n         for lineno, line in enumerate(text.splitlines(), start=1):\n             for match in _CITATION.finditer(line):\n-                number = match.group(1)\n-                found = index.get(number, [])\n+                namespace, number = match.groups()\n+                found = index.get((namespace, number), [])\n                 if len(found) == 1:\n                     continue\n                 reason = \"missing\" if not found else \"ambiguous\"\n                 rels = [p.relative_to(root).as_posix() for p in found]\n-                violations.append(Violation(rel, number, lineno, reason, rels))\n+                violations.append(\n+                    Violation(rel, namespace, number, lineno, reason, rels)\n+                )\n     return violations\n \n \n@@ -129,8 +147,8 @@\n         self.assertEqual(\n             violations,\n             [],\n-            \"Every 'ADR ####' citation must resolve to exactly one \"\n-            f\"docs/adr/####-*.md:\\n{detail}\",\n+            \"Every ADR/SADR citation must resolve exactly once in its own \"\n+            f\"docs/adr namespace:\\n{detail}\",\n         )\n \n     def test_scan_actually_reads_scripts(self) -> None:\n@@ -146,8 +164,21 @@\n         # The two records this Goal carried onto the default branch, and which\n         # the scripts cite most, must be present.\n         index = _adr_index(REPO_ROOT)\n-        self.assertIn(\"0016\", index)\n-        self.assertIn(\"0017\", index)\n+        self.assertIn((\"SADR\", \"0016\"), index)\n+        self.assertIn((\"SADR\", \"0017\"), index)\n+\n+    def test_platform_tree_has_only_sadr_0001_through_0019(self) -> None:\n+        adr_dir = REPO_ROOT / \"docs\" / \"adr\"\n+        sadrs = sorted(adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\"))\n+        self.assertEqual(\n+            {path.name.split(\"-\", 2)[1] for path in sadrs},\n+            {f\"{number:04d}\" for number in range(1, 20)},\n+        )\n+        self.assertEqual(\n+            list(adr_dir.glob(\"[0-9][0-9][0-9][0-9]-*.md\")),\n+            [],\n+            \"the Shiki platform tree must not retain legacy numeric SADR paths\",\n+        )\n \n \n class FixtureAdrReferences(unittest.TestCase):\n@@ -180,6 +211,26 @@\n         self.assertIn(\"9991\", message)  # cited number\n         self.assertIn(\"docs/adr/9991-*.md\", message)  # missing path\n \n+    def test_target_adr_does_not_satisfy_missing_platform_sadr(self) -> None:\n+        root = self._make_repo(\n+            adr_names=[\"0042-target-decision.md\"],\n+            script_body='\"\"\"Target ADR 0042 exists, but platform SADR-0042 does not.\"\"\"\\n',\n+        )\n+        violations = find_adr_reference_violations(root, include_docs=False)\n+        self.assertEqual(len(violations), 1)\n+        self.assertEqual(violations[0].namespace, \"SADR\")\n+        self.assertEqual(violations[0].number, \"0042\")\n+        self.assertEqual(violations[0].expected_path, \"docs/adr/SADR-0042-*.md\")\n+\n+    def test_target_adr_and_platform_sadr_may_reuse_a_number(self) -> None:\n+        root = self._make_repo(\n+            adr_names=[\"0042-target-decision.md\", \"SADR-0042-platform-decision.md\"],\n+            script_body='\"\"\"Target ADR 0042 and platform SADR-0042 are distinct.\"\"\"\\n',\n+        )\n+        self.assertEqual(\n+            find_adr_reference_violations(root, include_docs=False), []\n+        )\n+\n     def test_uncited_adr_is_allowed(self) -> None:\n         # An ADR exists but nothing cites it -> not a violation. The converse\n         # (every ADR must be cited) is explicitly NOT required.\ndiff --git a/tests/test_sadr_install_migration.py b/tests/test_sadr_install_migration.py\nnew file mode 100644\nindex 0000000000000000000000000000000000000000..e1c1f87329132324dcb6bcdea757669752ad582b\n--- /dev/null\n+++ b/tests/test_sadr_install_migration.py\n@@ -0,0 +1,134 @@\n+\"\"\"SADR installation and collision-safe legacy upgrade behavior.\"\"\"\n+\n+from __future__ import annotations\n+\n+import contextlib\n+import hashlib\n+import io\n+import json\n+import shutil\n+import tempfile\n+import unittest\n+from pathlib import Path\n+\n+import shiki_test_support  # noqa: F401  (path bootstrap)\n+\n+import shiki_installer\n+from tests.test_adr_references import find_adr_reference_violations\n+\n+\n+PRODUCT_NUMBERS = tuple(range(13, 19))\n+\n+\n+def _quiet_install(target: Path, *, force: bool, validate: bool) -> None:\n+    with contextlib.redirect_stdout(io.StringIO()):\n+        shiki_installer.install_template(target, force=force, validate=validate)\n+\n+\n+def _sha256(path: Path) -> str:\n+    return hashlib.sha256(path.read_bytes()).hexdigest()\n+\n+\n+def _legacy_sadr_paths() -> tuple[str, ...]:\n+    return tuple(\n+        f\"docs/adr/{path.name.removeprefix('SADR-')}\"\n+        for path in sorted((shiki_installer.ROOT / \"docs\" / \"adr\").glob(\"SADR-*.md\"))\n+        if path.name != \"SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\"\n+    )\n+\n+\n+class FreshSadrInstallTests(unittest.TestCase):\n+    def setUp(self) -> None:\n+        self.tmp = Path(tempfile.mkdtemp(prefix=\"shiki-sadr-fresh-\"))\n+        self.addCleanup(lambda: shutil.rmtree(self.tmp, ignore_errors=True))\n+        self.target = self.tmp / \"target\"\n+        self.adr_dir = self.target / \"docs\" / \"adr\"\n+        self.adr_dir.mkdir(parents=True)\n+        self.product_bytes = {\n+            f\"{number:04d}-product-decision.md\": (\n+                f\"# Product ADR {number:04d}\\n\\nTarget-owned bytes {number}.\\n\"\n+            ).encode()\n+            for number in PRODUCT_NUMBERS\n+        }\n+        for name, content in self.product_bytes.items():\n+            (self.adr_dir / name).write_bytes(content)\n+\n+    def test_fresh_install_preserves_product_adrs_and_installs_all_sadrs(self) -> None:\n+        _quiet_install(self.target, force=False, validate=True)\n+\n+        for name, expected in self.product_bytes.items():\n+            self.assertEqual((self.adr_dir / name).read_bytes(), expected)\n+\n+        sadr_numbers = {\n+            path.name.split(\"-\", 2)[1]\n+            for path in self.adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\")\n+        }\n+        self.assertEqual(sadr_numbers, {f\"{number:04d}\" for number in range(1, 20)})\n+\n+        numeric_records = {\n+            path.name\n+            for path in self.adr_dir.glob(\"[0-9][0-9][0-9][0-9]-*.md\")\n+        }\n+        self.assertEqual(numeric_records, set(self.product_bytes))\n+        self.assertEqual(find_adr_reference_violations(self.target), [])\n+\n+\n+class LegacySadrUpgradeTests(unittest.TestCase):\n+    def setUp(self) -> None:\n+        self.tmp = Path(tempfile.mkdtemp(prefix=\"shiki-sadr-upgrade-\"))\n+        self.addCleanup(lambda: shutil.rmtree(self.tmp, ignore_errors=True))\n+        self.target = self.tmp / \"target\"\n+        self.adr_dir = self.target / \"docs\" / \"adr\"\n+        self.adr_dir.mkdir(parents=True)\n+\n+        self.product_bytes = {\n+            f\"{number:04d}-cubeshare-product-decision.md\": (\n+                f\"# CubeShare ADR {number:04d}\\n\\nDo not rewrite {number}.\\n\"\n+            ).encode()\n+            for number in PRODUCT_NUMBERS\n+        }\n+        for name, content in self.product_bytes.items():\n+            (self.adr_dir / name).write_bytes(content)\n+\n+        self.legacy_paths = _legacy_sadr_paths()\n+        for relative in self.legacy_paths:\n+            path = self.target / relative\n+            path.write_bytes(f\"legacy Shiki bytes for {relative}\\n\".encode())\n+\n+        stamp_path = self.target / shiki_installer.INSTALL_STAMP_PATH\n+        stamp_path.parent.mkdir(parents=True, exist_ok=True)\n+        stamp = {\n+            \"version\": shiki_installer.INSTALL_STAMP_VERSION,\n+            \"platform_commit\": None,\n+            \"installed_at\": \"2000-01-01T00:00:00+00:00\",\n+            \"digests\": {\n+                relative: _sha256(self.target / relative)\n+                for relative in self.legacy_paths\n+            },\n+        }\n+        stamp_path.write_text(\n+            json.dumps(stamp, indent=2, sort_keys=True) + \"\\n\", encoding=\"utf-8\"\n+        )\n+\n+    def test_force_upgrade_deletes_only_stamped_legacy_sadrs(self) -> None:\n+        _quiet_install(self.target, force=True, validate=True)\n+\n+        for relative in self.legacy_paths:\n+            self.assertFalse((self.target / relative).exists(), relative)\n+        for name, expected in self.product_bytes.items():\n+            self.assertEqual((self.adr_dir / name).read_bytes(), expected)\n+\n+        sadrs = list(self.adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\"))\n+        self.assertEqual(len(sadrs), 19)\n+        refreshed = json.loads(\n+            (self.target / shiki_installer.INSTALL_STAMP_PATH).read_text(encoding=\"utf-8\")\n+        )\n+        for relative in self.legacy_paths:\n+            self.assertNotIn(relative, refreshed[\"digests\"])\n+        for path in sadrs:\n+            relative = path.relative_to(self.target).as_posix()\n+            self.assertEqual(refreshed[\"digests\"][relative], _sha256(path))\n+\n+\n+if __name__ == \"__main__\":\n+    unittest.main()\n\ndiff --git a/.codex/skills/shiki/SKILL.md b/.codex/skills/shiki/SKILL.md\nindex c8e2e85d9d75d49cc4f2d6286ee22659c4e53e75..93cded623b2c15d9f65c76491a1c2d813ac9e6c0\n--- a/.codex/skills/shiki/SKILL.md\n+++ b/.codex/skills/shiki/SKILL.md\n@@ -18,6 +18,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n Codex CLI does not register this skill as a custom `/shiki` slash command. When\n the user types `/shiki` in Codex CLI and gets \"Unrecognized command\", explain\n that this is expected: invoke Shiki in Codex with natural language such as\n@@ -69,7 +72,7 @@\n \n ## Responsibilities\n \n-- Claude Code implements and repairs by default (`shiki runner claude`, ADR 0008); Codex implements and repairs tasks explicitly assigned to `codex`.\n+- Claude Code implements and repairs by default (`shiki runner claude`, SADR-0008); Codex implements and repairs tasks explicitly assigned to `codex`.\n - Claude Code Action can act as GitHub-side CCA or reviewer.\n - CCA judges completion.\n - MergeGate authorizes state transitions and merge readiness.\ndiff --git a/.github/prompts/cca-completion-check.md b/.github/prompts/cca-completion-check.md\nindex ed203ad5f302ecc81372d8ab27d2b1232ca97497..fd89950528333ef6242e6d5443904f5392ab7e56\n--- a/.github/prompts/cca-completion-check.md\n+++ b/.github/prompts/cca-completion-check.md\n@@ -19,6 +19,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n ## Required Reading\n \n Read only the files needed to judge the PR contract. Prefer this order:\ndiff --git a/AGENTS.md b/AGENTS.md\nindex 7f041100d49c2a7525f51cb0f5d696a4a6d1c78c..2b700e989ff295987aa8a4b0c5fcb1acd80020da\n--- a/AGENTS.md\n+++ b/AGENTS.md\n@@ -44,6 +44,10 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use\n+`SADR-NNNN-*.md`; target product decisions retain `NNNN-*.md` and the `ADR`\n+namespace.\n+\n Use the canonical source-of-truth order above. Runtime-specific reading orders\n are operational guidance only and must not override or reorder it.\n \ndiff --git a/CLAUDE.md b/CLAUDE.md\nindex 98a932e7841a632866fcd53b0bad0e3af3711d3d..611d491912d6a72a0327a1731b8f33a41acb4baa\n--- a/CLAUDE.md\n+++ b/CLAUDE.md\n@@ -14,6 +14,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n ## Claude Role\n \n Default to planner, default implementer, reviewer, coordinator, documentation editor, CCA-style completion judge, and final-judgment assistant.\ndiff --git a/SYSTEM_PROMPT.md b/SYSTEM_PROMPT.md\nindex aaaea45e3a896c3a44ba89eec8bb84325ecf6c62..2e7c093ebe9e8bb34e0923756f191b4f7fa581f5\n--- a/SYSTEM_PROMPT.md\n+++ b/SYSTEM_PROMPT.md\n@@ -30,6 +30,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n Use the canonical source-of-truth order above. Runtime-specific reading orders\n are operational guidance only and must not override or reorder it. The current\n conversation is non-durable operator input.\ndiff --git a/docs/adr/0001-keep-shiki-as-platform-name.md b/docs/adr/SADR-0001-keep-shiki-as-platform-name.md\nindex 20a9637c3b9d8c97de7ba8db7cfae4d82d9a7dba..8b685439959aada1b221c112f0ac3c51ca8a1695\n--- a/docs/adr/0001-keep-shiki-as-platform-name.md\n+++ b/docs/adr/SADR-0001-keep-shiki-as-platform-name.md\n@@ -1,3 +1,3 @@\n-# Keep Shiki As The Platform Name\n+# SADR-0001: Keep Shiki As The Platform Name\n \n Shiki remains the product name for the GitHub-native agentic engineering control plane. We considered more descriptive names such as MergePilot, MergeGate, and Kiroku, but kept Shiki because it carries the existing goal-seeking framework identity while remaining independent of Hermes, Codex, Claude Code, or any single runtime.\ndiff --git a/docs/adr/0002-use-agents-as-shared-constitution.md b/docs/adr/SADR-0002-use-agents-as-shared-constitution.md\nindex 73de30ecb078120eac540d9bc51855e3cd48d0e3..c5486182bf06fca36ea508496b013c67302fd989\n--- a/docs/adr/0002-use-agents-as-shared-constitution.md\n+++ b/docs/adr/SADR-0002-use-agents-as-shared-constitution.md\n@@ -1,3 +1,3 @@\n-# Use AGENTS As The Shared Agent Constitution\n+# SADR-0002: Use AGENTS As The Shared Agent Constitution\n \n Shiki uses `AGENTS.md` as the runtime-neutral constitution and `CLAUDE.md` as a thin Claude Code wrapper. This keeps Codex, Claude Code, Hermes Runner, and future Agent Runtimes aligned on the same GitHub-first MergeGate rules while still allowing runtime-specific instructions where needed.\ndiff --git a/docs/adr/0003-require-skill-gate-for-mergegate-readiness.md b/docs/adr/SADR-0003-require-skill-gate-for-mergegate-readiness.md\nindex 424be0c9af4c8f0136f1fde68a468c47ff84e764..7fc337987c7df9792f75f181dfcbed75fe5ffff2\n--- a/docs/adr/0003-require-skill-gate-for-mergegate-readiness.md\n+++ b/docs/adr/SADR-0003-require-skill-gate-for-mergegate-readiness.md\n@@ -1,3 +1,3 @@\n-# Require Skill Gate For MergeGate Readiness\n+# SADR-0003: Require Skill Gate For MergeGate Readiness\n \n Shiki requires relevant engineering skills to be invoked before execution when their triggers apply. This makes skills part of MergeGate readiness rather than optional prompt style, because Shiki depends on repeatable planning, issue decomposition, TDD, diagnosis, triage, and architecture review before autonomous or parallel agent work can be trusted.\ndiff --git a/docs/adr/0004-use-github-first-with-shiki-mirror.md b/docs/adr/SADR-0004-use-github-first-with-shiki-mirror.md\nindex d0a3a9174dc646a945a9d325954ffaf00c352040..a79c54ae9ee8eed2a0524192b864da4e22a92d3f\n--- a/docs/adr/0004-use-github-first-with-shiki-mirror.md\n+++ b/docs/adr/SADR-0004-use-github-first-with-shiki-mirror.md\n@@ -1,4 +1,4 @@\n-# ADR 0004: Use GitHub First With A Repository-Local Shiki Mirror\n+# SADR-0004: Use GitHub First With A Repository-Local Shiki Mirror\n \n ## Status\n \ndiff --git a/docs/adr/0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md b/docs/adr/SADR-0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\nindex b339baf49b89d9290e79c3d51b36e7b2bde3bd95..c30a0d8b1fde0224d0ac7820e7870dccf721580d\n--- a/docs/adr/0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\n+++ b/docs/adr/SADR-0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\n@@ -1,4 +1,4 @@\n-# ADR 0005: Use Codex As Front And Claude Code Action As The GitHub Runtime\n+# SADR-0005: Use Codex As Front And Claude Code Action As The GitHub Runtime\n \n ## Status\n \ndiff --git a/docs/adr/0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md b/docs/adr/SADR-0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\nindex 4bcae9d1e52f0a37b8e9fbafa5cd14b6ee1993bd..7a38538a6ae9929567f0ade67e7ea6a60d0b3f7e\n--- a/docs/adr/0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\n+++ b/docs/adr/SADR-0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\n@@ -1,4 +1,4 @@\n-# ADR 0006: Separate Codex Implementation, CCA Completion, And MergeGate Authorization\n+# SADR-0006: Separate Codex Implementation, CCA Completion, And MergeGate Authorization\n \n ## Status\n \ndiff --git a/docs/adr/0007-packaging-and-release.md b/docs/adr/SADR-0007-packaging-and-release.md\nindex 79b84ddb97167372b44641e5b7860e400ce80822..023cd57f8011aa6e6d2ebf612b7c35a7a82c00d4\n--- a/docs/adr/0007-packaging-and-release.md\n+++ b/docs/adr/SADR-0007-packaging-and-release.md\n@@ -1,4 +1,4 @@\n-# ADR 0007: Packaging And Release For Shiki\n+# SADR-0007: Packaging And Release For Shiki\n \n ## Status\n \ndiff --git a/docs/adr/0008-use-claude-code-as-default-implementer.md b/docs/adr/SADR-0008-use-claude-code-as-default-implementer.md\nindex dedf4510b7c7799521422f622029563d373db031..a158a8d444d93764ec515a28133f7dde4af23579\n--- a/docs/adr/0008-use-claude-code-as-default-implementer.md\n+++ b/docs/adr/SADR-0008-use-claude-code-as-default-implementer.md\n@@ -1,4 +1,4 @@\n-# ADR 0008: Use Claude Code As The Default Implementer Runtime\n+# SADR-0008: Use Claude Code As The Default Implementer Runtime\n \n ## Status\n \ndiff --git a/docs/adr/0009-requirements-definition-and-spec-freeze.md b/docs/adr/SADR-0009-requirements-definition-and-spec-freeze.md\nindex 29b3f7871bef9203a8b4a831a65c39eb2f506e42..52e7dd69ad0aa4efe0b68c62678abbf7df02aaec\n--- a/docs/adr/0009-requirements-definition-and-spec-freeze.md\n+++ b/docs/adr/SADR-0009-requirements-definition-and-spec-freeze.md\n@@ -1,4 +1,4 @@\n-# ADR 0009: Merge Grill And Planning Into Requirements Definition With Spec Freeze\n+# SADR-0009: Merge Grill And Planning Into Requirements Definition With Spec Freeze\n \n ## Status\n \ndiff --git a/docs/adr/0010-external-ai-guardian-review.md b/docs/adr/SADR-0010-external-ai-guardian-review.md\nindex e6108bbe8bb4e73e0ff4684c8f78001a80438c4e..e690d4eddd270e683d709c9b679c5859e71eeaaa\n--- a/docs/adr/0010-external-ai-guardian-review.md\n+++ b/docs/adr/SADR-0010-external-ai-guardian-review.md\n@@ -1,4 +1,4 @@\n-# ADR 0010: External AI Guardian Review As A First-Class Approval Authority\n+# SADR-0010: External AI Guardian Review As A First-Class Approval Authority\n \n ## Status\n \ndiff --git a/docs/adr/0011-autonomous-quality-gate-evidence.md b/docs/adr/SADR-0011-autonomous-quality-gate-evidence.md\nindex bca0246c5d46e44afbde15486a75eec425fc02f5..9c93eac16d8c3a6d4b11bd7224e3955102cd7db4\n--- a/docs/adr/0011-autonomous-quality-gate-evidence.md\n+++ b/docs/adr/SADR-0011-autonomous-quality-gate-evidence.md\n@@ -1,4 +1,4 @@\n-# ADR 0011: Autonomous Quality-Gate Evidence \u2014 Deterministic Facts And An Independent Verifier\n+# SADR-0011: Autonomous Quality-Gate Evidence \u2014 Deterministic Facts And An Independent Verifier\n \n ## Status\n \ndiff --git a/docs/adr/0012-autonomous-loop-closeout-to-main.md b/docs/adr/SADR-0012-autonomous-loop-closeout-to-main.md\nindex 637fad6dde4989c9c2bf0f81f676d8d29991123c..ec7d85f81030a676bcbba28b43eb365422a36eac\n--- a/docs/adr/0012-autonomous-loop-closeout-to-main.md\n+++ b/docs/adr/SADR-0012-autonomous-loop-closeout-to-main.md\n@@ -1,4 +1,4 @@\n-# 0012 \u2014 Autonomous loop pushes goal completion to main via a closeout PR\n+# SADR-0012 \u2014 Autonomous loop pushes goal completion to main via a closeout PR\n \n - Status: accepted\n - Date: 2026-06-18\ndiff --git a/docs/adr/0013-bootstrap-grants-actions-approve-pull-requests.md b/docs/adr/SADR-0013-bootstrap-grants-actions-approve-pull-requests.md\nindex b6c69d64f1c5ced346b88d6859586715b3b5bd0e..a793e7277d67ab8ea56286fec289ca689825a17d\n--- a/docs/adr/0013-bootstrap-grants-actions-approve-pull-requests.md\n+++ b/docs/adr/SADR-0013-bootstrap-grants-actions-approve-pull-requests.md\n@@ -1,4 +1,4 @@\n-# ADR 0013: Bootstrap Grants GitHub Actions Permission To Create And Approve Pull Requests\n+# SADR-0013: Bootstrap Grants GitHub Actions Permission To Create And Approve Pull Requests\n \n ## Status\n \ndiff --git a/docs/adr/0014-codex-app-external-ai-guardian-ui-adapter.md b/docs/adr/SADR-0014-codex-app-external-ai-guardian-ui-adapter.md\nindex 238db06279334caa341c117362df9271382a7fcd..65f12e9939321e1b3e389a34c822fc1549e5155d\n--- a/docs/adr/0014-codex-app-external-ai-guardian-ui-adapter.md\n+++ b/docs/adr/SADR-0014-codex-app-external-ai-guardian-ui-adapter.md\n@@ -1,4 +1,4 @@\n-# ADR 0014: Use Codex App As The External AI Guardian UI Adapter\n+# SADR-0014: Use Codex App As The External AI Guardian UI Adapter\n \n ## Status\n \ndiff --git a/docs/adr/0015-contract-approval-before-dispatch.md b/docs/adr/SADR-0015-contract-approval-before-dispatch.md\nindex fad6c656741a20a7684ba16080a7db159f63a52f..7bdd760bb3e82eee4e1410f45c783b03b638d456\n--- a/docs/adr/0015-contract-approval-before-dispatch.md\n+++ b/docs/adr/SADR-0015-contract-approval-before-dispatch.md\n@@ -1,4 +1,4 @@\n-# ADR 0015: Approve The Task Contract Before Dispatch, Not The Diff After It\n+# SADR-0015: Approve The Task Contract Before Dispatch, Not The Diff After It\n \n ## Status\n \ndiff --git a/docs/adr/0016-locks-bound-product-paths.md b/docs/adr/SADR-0016-locks-bound-product-paths.md\nindex b5e850f3f1f7f4ef6e7f9d1763a29a51bd300c45..332cc17313498404f41a145291e38447b5de0dc4\n--- a/docs/adr/0016-locks-bound-product-paths.md\n+++ b/docs/adr/SADR-0016-locks-bound-product-paths.md\n@@ -1,4 +1,4 @@\n-# ADR 0016: Locks bound product paths; the Shiki mirror is bound by record identity\n+# SADR-0016: Locks bound product paths; the Shiki mirror is bound by record identity\n \n ## Status\n \ndiff --git a/docs/adr/0017-bookkeeping-closeouts-do-not-inherit-risk.md b/docs/adr/SADR-0017-bookkeeping-closeouts-do-not-inherit-risk.md\nindex 554e3a0d526a72ca067108c5464cc68c3edcf174..83115f69f1b1b48d544c9b0157f377b91cb53abf\n--- a/docs/adr/0017-bookkeeping-closeouts-do-not-inherit-risk.md\n+++ b/docs/adr/SADR-0017-bookkeeping-closeouts-do-not-inherit-risk.md\n@@ -1,4 +1,4 @@\n-# ADR 0017: A Bookkeeping Closeout Does Not Inherit Its Task's Risk\n+# SADR-0017: A Bookkeeping Closeout Does Not Inherit Its Task's Risk\n \n ## Status\n \ndiff --git a/docs/adr/0018-carried-guardian-approval-across-a-proven-base-sync.md b/docs/adr/SADR-0018-carried-guardian-approval-across-a-proven-base-sync.md\nindex 22358562da390ed8b45cd600da08a43d378b4680..38da0fd95102c3496c3219ce2319fce696a2532a\n--- a/docs/adr/0018-carried-guardian-approval-across-a-proven-base-sync.md\n+++ b/docs/adr/SADR-0018-carried-guardian-approval-across-a-proven-base-sync.md\n@@ -1,4 +1,4 @@\n-# ADR 0018: Carried Guardian Approval Across A Proven Base Sync\n+# SADR-0018: Carried Guardian Approval Across A Proven Base Sync\n \n ## Status\n \ndiff --git a/docs/adr/README.md b/docs/adr/README.md\nindex c70e057a4520c36e042ab68c53cd4a21ef709b71..5da43856836e2ce6a150f8127766e2f88355ab4b\n--- a/docs/adr/README.md\n+++ b/docs/adr/README.md\n@@ -1,40 +1,41 @@\n-# Architecture Decision Records\n+# Decision Record Namespaces\n \n-This directory holds Shiki's Architecture Decision Records (ADRs): durable\n-records of hard-to-reverse platform decisions. ADRs are part of the Shiki source\n-of truth alongside GitHub state, the `.shiki/` mirror, and `CONTEXT.md`.\n+This directory holds two independent decision namespaces:\n \n-## How ADRs Work\n+- Shiki Architecture Decision Records (SADRs), named\n+  `SADR-NNNN-<short-slug>.md`, record hard-to-reverse Shiki platform decisions.\n+- Target Architecture Decision Records (ADRs), named\n+  `NNNN-<short-slug>.md`, belong to the target product.\n \n-- New ADRs are added as `docs/adr/NNNN-<short-slug>.md`, where `NNNN` is the next\n-  zero-padded sequence number.\n-- The **canonical list of ADRs is the set of files matching `docs/adr/*.md`** in\n-  this directory. Browse `docs/adr/*.md` for the authoritative, current set.\n-- Use [`template.md`](template.md) as the starting point for a new ADR.\n-- Record an ADR when a decision is hard to reverse, surprising without context,\n-  and a real tradeoff. Do not write an ADR for routine or easily reversible\n-  choices.\n+The same four-digit number may appear once in each namespace. A platform\n+`SADR-NNNN` citation resolves only to `SADR-NNNN-*.md`; a target `ADR NNNN`\n+citation resolves only to `NNNN-*.md`.\n \n-Using the `docs/adr/*.md` glob as the source of truth means other PRs can add an\n-ADR without editing this index file, avoiding merge contention on a\n-hand-maintained enumeration.\n+## How Decision Records Work\n \n-## Current ADRs (snapshot)\n-\n-The list below is a convenience snapshot and may lag behind the directory. When\n-in doubt, list `docs/adr/*.md`.\n+- New Shiki platform decisions use the next zero-padded SADR number.\n+- New target product decisions use the target's own ADR sequence.\n+- The canonical list is the files in `docs/adr/`; no hand-maintained index is\n+  authoritative.\n+- Use [`template.md`](template.md) as the starting point and choose the filename\n+  namespace that owns the decision.\n+- Record a decision when it is hard to reverse, surprising without context, and\n+  a real tradeoff. Do not write a decision record for routine or easily\n+  reversible choices.\n \n-- [0001 \u2014 Keep Shiki As The Platform Name](0001-keep-shiki-as-platform-name.md)\n-- [0002 \u2014 Use AGENTS As The Shared Agent Constitution](0002-use-agents-as-shared-constitution.md)\n-- [0003 \u2014 Require Skill Gate For MergeGate Readiness](0003-require-skill-gate-for-mergegate-readiness.md)\n-- [0004 \u2014 Use GitHub First With A Repository-Local Shiki Mirror](0004-use-github-first-with-shiki-mirror.md)\n-- [0005 \u2014 Use Codex As Front And Claude Code Action As The GitHub Runtime](0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md)\n-- [0006 \u2014 Separate Codex Implementation, CCA Completion, And MergeGate Authorization](0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md)\n+Using the directory as the source of truth lets independent changes add records\n+without contending on an enumerated index.\n \n-## Adding A New ADR\n+## Adding A Shiki SADR\n \n-1. Copy `template.md` to `docs/adr/NNNN-<short-slug>.md`.\n+1. Copy `template.md` to `docs/adr/SADR-NNNN-<short-slug>.md`.\n 2. Fill in the title, status, context, decision, and consequences.\n-3. Set the status to `Proposed`, then `Accepted` once the decision is approved.\n-4. Open a PR following `CONTRIBUTING.md`. You do not need to edit this index for\n-   the ADR to be discoverable via `docs/adr/*.md`.\n+3. Set the status to `Proposed`, then `Accepted` once the proper authority has\n+   approved the decision.\n+4. Open a PR following `CONTRIBUTING.md`.\n+\n+## Adding A Target ADR\n+\n+1. Copy `template.md` to `docs/adr/NNNN-<short-slug>.md` using the target's own\n+   sequence.\n+2. Cite it as `ADR NNNN`; do not prefix its filename with `SADR-`.\ndiff --git a/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md b/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\nnew file mode 100644\nindex 0000000000000000000000000000000000000000..7455ce1f3bed02ddddb851ac21a1d1a648c9aeba\n--- /dev/null\n+++ b/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\n@@ -0,0 +1,55 @@\n+# SADR-0019: Separate Shiki Platform SADRs From Target ADRs\n+\n+## Status\n+\n+Accepted\n+\n+## Context\n+\n+Shiki installs its platform decision records into `docs/adr/` in every target\n+repository. Shiki and its targets previously used the same numeric\n+`NNNN-<slug>.md` namespace. A target that already owned ADRs 0013 through 0018\n+therefore acquired two records for each number during installation, making an\n+`ADR NNNN` citation ambiguous. Renumbering target decisions would rewrite target\n+history, while renumbering Shiki decisions per target would destroy their stable\n+platform identity.\n+\n+Upgrades also need to remove the former Shiki-owned numeric paths without ever\n+mistaking a target-owned ADR for platform content. The install stamp already\n+records exact shipped paths and content digests, so it can prove ownership only\n+when both still match.\n+\n+## Decision\n+\n+Shiki platform decisions use the `SADR` namespace:\n+\n+- records are named `docs/adr/SADR-NNNN-<slug>.md`;\n+- live platform citations use `SADR-NNNN`;\n+- target product decisions retain `docs/adr/NNNN-<slug>.md` and `ADR NNNN`;\n+- the reference validator resolves the two namespaces independently, so the same\n+  four-digit number may exist once in each namespace.\n+\n+Before a forced upgrade writes anything, the installer inventories the exact\n+former numeric Shiki ADR paths. It may delete a legacy path only when the\n+existing install stamp contains that exact path and its recorded SHA-256 digest\n+matches the file currently on disk. All ownership blockers are reported\n+together and abort the upgrade before its first write. A non-forced install\n+never performs this cleanup.\n+\n+Historical `.shiki/` evidence remains unchanged because it records the namespace\n+that existed when that evidence was produced.\n+\n+## Consequences\n+\n+- Targets may use any ADR number without colliding with Shiki's platform SADRs.\n+- Fresh installs preserve target ADR bytes and install a stable SADR set.\n+- Forced upgrades fail closed when legacy ownership cannot be proven, and a\n+  successful upgrade refreshes the stamp without removed legacy paths.\n+- Current governance, packaging, workflow, prompt, script, and skill references\n+  must move with the platform records.\n+\n+## Alternatives Considered\n+\n+- Collision-only refusal leaves affected targets permanently unupgradable.\n+- Renumbering target ADRs mutates target-owned history.\n+- Per-target Shiki renumbering makes platform citations non-portable.\ndiff --git a/docs/adr/template.md b/docs/adr/template.md\nindex a92a97daddd0b00ab9dd626cc8d86942da2eafed..17dd79175b5fee2105935557f691b40cf9492942\n--- a/docs/adr/template.md\n+++ b/docs/adr/template.md\n@@ -1,14 +1,14 @@\n-# NNNN: <Short Decision Title>\n+# <SADR-NNNN or NNNN>: <Short Decision Title>\n \n ## Status\n \n-Proposed | Accepted | Superseded by ADR-NNNN\n+Proposed | Accepted | Superseded by SADR-NNNN or target ADR NNNN\n \n ## Context\n \n What is the situation, constraint, or force that requires a decision? Reference\n-relevant domain terms (`CONTEXT.md`), prior ADRs, and code reality. State the\n-problem, not the solution.\n+relevant domain terms (`CONTEXT.md`), prior Shiki SADRs or target ADRs, and code\n+reality. State the problem, not the solution.\n \n ## Decision\n \ndiff --git a/docs/agents/domain.md b/docs/agents/domain.md\nindex 258da8ea81e87ac0a6b0b5b5e5c2fde980eaabb7..67aaa6f4b7d0a5aa4d495ce6279073333f7798cb\n--- a/docs/agents/domain.md\n+++ b/docs/agents/domain.md\n@@ -5,7 +5,9 @@\n ## Layout\n \n - `CONTEXT.md` defines Shiki language. Keep it implementation-free.\n-- `docs/adr/` records hard-to-reverse decisions.\n+- `docs/adr/SADR-NNNN-*.md` records Shiki platform decisions.\n+- `docs/adr/NNNN-*.md` records target product decisions without sharing the\n+  SADR namespace.\n - `AGENTS.md` is the runtime-neutral agent constitution.\n - `CLAUDE.md` is the Claude Code wrapper around `AGENTS.md`.\n - `.shiki/` is the Target Repository mirror for Goals, PRDs, plans, Task DAGs, contracts, locks, worktree records, ledger entries, CCA verdicts, repair packets, reports, and handoffs.\ndiff --git a/docs/agents/implementation-policy.md b/docs/agents/implementation-policy.md\nindex c754144a9fb5bf4a38aa82a60e1670c59e626b44..e90d40fe21880b9a09719c5ecfe6bde7284eb3ce\n--- a/docs/agents/implementation-policy.md\n+++ b/docs/agents/implementation-policy.md\n@@ -31,6 +31,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n ## Non-Negotiables\n \n 1. **Planning precedes implementation.** Non-trivial Goals start with `grill-with-docs` and Context & Impact.\ndiff --git a/scripts/shiki_installer.py b/scripts/shiki_installer.py\nindex be064ab7a29ff6836f7f7a1d70500a43e0d11d7b..d3e3247edbf3e4be287a4667176d86f6500f8dc5\n--- a/scripts/shiki_installer.py\n+++ b/scripts/shiki_installer.py\n@@ -118,6 +118,7 @@\n # what lets a target carry it without a governance-schema change.\n INSTALL_STAMP_PATH = \".shiki/install-stamp.json\"\n INSTALL_STAMP_VERSION = 1\n+LEGACY_SHIKI_SADR_MAX = 18\n \n # Under --force the shipped surface splits three ways, so nothing is lost and\n # nothing is silently stale.\n@@ -237,9 +238,98 @@\n         )\n \n \n+def legacy_shiki_adr_paths() -> tuple[str, ...]:\n+    \"\"\"Former numeric paths for Shiki SADR-0001 through SADR-0018.\n+\n+    The authoritative slugs come from the current SADR filenames. SADR-0019 is\n+    the namespace decision itself and never shipped under a numeric legacy path.\n+    \"\"\"\n+    paths: list[str] = []\n+    for sadr in sorted((ROOT / \"docs\" / \"adr\").glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\")):\n+        number = sadr.name.split(\"-\", 2)[1]\n+        if int(number) <= LEGACY_SHIKI_SADR_MAX:\n+            paths.append(f\"docs/adr/{sadr.name.removeprefix('SADR-')}\")\n+    return tuple(paths)\n+\n+\n+def preflight_legacy_sadr_cleanup(target: Path, *, force: bool) -> tuple[str, ...]:\n+    \"\"\"Authorize exact legacy Shiki ADR removals before the first write.\n+\n+    A non-force install never removes existing files. A forced install needs no\n+    stamp when no legacy Shiki path is present. When legacy paths do exist, each\n+    must be named in a readable install stamp and its current bytes must match\n+    the recorded digest.\n+    \"\"\"\n+    if not force:\n+        return ()\n+    present = tuple(\n+        relative\n+        for relative in legacy_shiki_adr_paths()\n+        if (target / relative).exists() or (target / relative).is_symlink()\n+    )\n+    if not present:\n+        return ()\n+\n+    stamp_path = target / INSTALL_STAMP_PATH\n+    try:\n+        raw = stamp_path.read_text(encoding=\"utf-8\")\n+        stamp = json.loads(raw)\n+    except FileNotFoundError:\n+        detail = \"install stamp is absent\"\n+        blockers = [f\"{relative}: {detail}\" for relative in present]\n+    except (OSError, UnicodeDecodeError) as error:\n+        detail = f\"install stamp is unreadable ({error})\"\n+        blockers = [f\"{relative}: {detail}\" for relative in present]\n+    except json.JSONDecodeError as error:\n+        detail = f\"install stamp is malformed ({error})\"\n+        blockers = [f\"{relative}: {detail}\" for relative in present]\n+    else:\n+        if not isinstance(stamp, dict):\n+            blockers = [\n+                f\"{relative}: install stamp is malformed (expected an object)\"\n+                for relative in present\n+            ]\n+        else:\n+            digests = stamp.get(\"digests\")\n+            blockers = []\n+            if not isinstance(digests, dict):\n+                blockers.extend(\n+                    f\"{relative}: install stamp has no digest for this path\"\n+                    for relative in present\n+                )\n+            else:\n+                for relative in present:\n+                    expected = digests.get(relative)\n+                    if not isinstance(expected, str) or not expected:\n+                        blockers.append(\n+                            f\"{relative}: install stamp has no digest for this path\"\n+                        )\n+                        continue\n+                    try:\n+                        actual = _sha256_file(target / relative)\n+                    except OSError as error:\n+                        blockers.append(\n+                            f\"{relative}: legacy path is unreadable ({error})\"\n+                        )\n+                        continue\n+                    if actual != expected:\n+                        blockers.append(\n+                            f\"{relative}: digest mismatch (stamp {expected}, current {actual})\"\n+                        )\n+\n+    if blockers:\n+        joined = \"\\n\".join(f\"- {blocker}\" for blocker in blockers)\n+        raise ShikiError(\n+            \"legacy Shiki ADR cleanup is not authorized:\\n\"\n+            f\"{joined}\\nNo files were written.\"\n+        )\n+    return present\n+\n+\n def install_template(target: Path, *, force: bool, validate: bool) -> list[NewFileNote]:\n     # Preconditions first: never rewrite the tree of a target that cannot be\n     # upgraded cleanly (see refuse_pending_migrations).\n+    legacy_cleanup = preflight_legacy_sadr_cleanup(target, force=force)\n     refuse_pending_migrations(target)\n \n     # Resolve the target's required CODEOWNERS owner once from its\n@@ -250,6 +340,10 @@\n     # verbatim. See ``codeowners_required_owner``.\n     codeowners_owner = codeowners_required_owner(target)\n \n+    for relative in legacy_cleanup:\n+        (target / relative).unlink()\n+        info(f\"removed stamped legacy Shiki ADR: {target / relative}\")\n+\n     notes: list[NewFileNote] = []\n     for relative in TEMPLATE_PATHS:\n         source = ROOT / relative\ndiff --git a/tests/test_adr_references.py b/tests/test_adr_references.py\nindex 51706329c797d5dd9b47e082a333fa83b261d482..91ab76b724cb973435c45d53cab63793942267e9\n--- a/tests/test_adr_references.py\n+++ b/tests/test_adr_references.py\n@@ -1,4 +1,4 @@\n-\"\"\"Every ``ADR ####`` citation must resolve to exactly one decision record.\n+\"\"\"Every target ADR and Shiki SADR citation resolves in its own namespace.\n \n Background. ``scripts/mergegate_check.py`` justifies ``is_bookkeeping_closeout``\n with \"ADR 0017\" nine times and ``scripts/guardian_approval_signal.py`` cites\n@@ -11,11 +11,12 @@\n and the directory drifted with no signal.\n \n This is the same unbound-pair shape behind other silent failures: a reference\n-and its target with nothing asserting they agree. This test binds them. It scans\n-every ``scripts/*.py`` (plus ``docs/**/*.md`` and the root ``*.md`` governance\n-files, which are cheap) for the ``ADR ####`` pattern and asserts each cited\n-number resolves to exactly one ``docs/adr/####-*.md``. The converse is\n-deliberately NOT required: an ADR may exist without being cited.\n+and its target with nothing asserting they agree. This test binds them. Target\n+``ADR NNNN`` citations resolve only to ``docs/adr/NNNN-*.md``; Shiki\n+``SADR-NNNN`` citations resolve only to ``docs/adr/SADR-NNNN-*.md``. The scan\n+covers current governance docs, scripts, workflows, prompts, packaging metadata,\n+and repo-local Codex guidance while deliberately excluding historical\n+``.shiki`` evidence. The converse is not required: a record may be uncited.\n \n The suite is dependency free: standard-library ``unittest`` only.\n \"\"\"\n@@ -31,17 +32,18 @@\n \n REPO_ROOT = Path(__file__).resolve().parents[1]\n \n-# \"ADR 0016\" -- a four-digit citation. The trailing \\b keeps a five-digit token\n-# from being read as a truncated four-digit number.\n-_CITATION = re.compile(r\"\\bADR (\\d{4})\\b\")\n-# \"0016-locks-bound-product-paths.md\" -- the leading four digits name the record.\n-_ADR_FILENAME = re.compile(r\"^(\\d{4})-.*\\.md$\")\n+# ``ADR 0016`` / ``SADR-0016`` -- a namespace plus four-digit citation. The\n+# trailing ``\\b`` keeps a five-digit token from being read as a truncated one.\n+_CITATION = re.compile(r\"\\b(S?ADR)[ -](\\d{4})\\b\")\n+# Target ADRs retain the numeric filename; Shiki platform decisions use SADR.\n+_DECISION_FILENAME = re.compile(r\"^(?:(SADR)-)?(\\d{4})-.*\\.md$\")\n \n \n class Violation(NamedTuple):\n     \"\"\"A citation that does not resolve to exactly one decision record.\"\"\"\n \n     citing_file: str  # repo-relative path of the file that cites the ADR\n+    namespace: str  # \"ADR\" for target decisions; \"SADR\" for Shiki decisions\n     number: str  # the cited four-digit number, e.g. \"0016\"\n     line: int  # 1-based line of the citation\n     reason: str  # \"missing\" (zero matches) or \"ambiguous\" (two or more)\n@@ -49,44 +51,59 @@\n \n     @property\n     def expected_path(self) -> str:\n-        return f\"docs/adr/{self.number}-*.md\"\n+        prefix = \"SADR-\" if self.namespace == \"SADR\" else \"\"\n+        return f\"docs/adr/{prefix}{self.number}-*.md\"\n \n     def describe(self) -> str:\n         \"\"\"Name the citing file, the cited number, and the missing path.\"\"\"\n         if self.reason == \"missing\":\n             return (\n-                f\"{self.citing_file}:{self.line} cites ADR {self.number} \"\n+                f\"{self.citing_file}:{self.line} cites {self.namespace} {self.number} \"\n                 f\"but no document matches {self.expected_path}\"\n             )\n         joined = \", \".join(self.matches)\n         return (\n-            f\"{self.citing_file}:{self.line} cites ADR {self.number} which \"\n+            f\"{self.citing_file}:{self.line} cites {self.namespace} {self.number} which \"\n             f\"resolves to {len(self.matches)} documents (expected exactly one \"\n             f\"at {self.expected_path}): {joined}\"\n         )\n \n \n-def _adr_index(root: Path) -> dict[str, list[Path]]:\n-    \"\"\"Map every four-digit ADR number to the record files that claim it.\"\"\"\n-    index: dict[str, list[Path]] = {}\n+def _adr_index(root: Path) -> dict[tuple[str, str], list[Path]]:\n+    \"\"\"Map each decision namespace and number to the files that claim it.\"\"\"\n+    index: dict[tuple[str, str], list[Path]] = {}\n     adr_dir = root / \"docs\" / \"adr\"\n     if adr_dir.is_dir():\n         for path in sorted(adr_dir.glob(\"*.md\")):\n-            match = _ADR_FILENAME.match(path.name)\n+            match = _DECISION_FILENAME.match(path.name)\n             if match:\n-                index.setdefault(match.group(1), []).append(path)\n+                namespace = \"SADR\" if match.group(1) else \"ADR\"\n+                index.setdefault((namespace, match.group(2)), []).append(path)\n     return index\n \n \n def _citing_files(root: Path, include_docs: bool = True) -> list[Path]:\n-    \"\"\"Files scanned for citations: scripts/*.py, and cheaply the markdown.\"\"\"\n+    \"\"\"Current, shipped text surfaces scanned for decision citations.\"\"\"\n     files = sorted((root / \"scripts\").glob(\"*.py\"))\n+    files += sorted((root / \"scripts\").glob(\"*.sh\"))\n     if include_docs:\n         docs = root / \"docs\"\n         if docs.is_dir():\n             files += sorted(docs.rglob(\"*.md\"))\n         files += sorted(root.glob(\"*.md\"))\n-    return files\n+        pyproject = root / \"pyproject.toml\"\n+        if pyproject.is_file():\n+            files.append(pyproject)\n+        for relative in (\".github\", \".codex\"):\n+            surface = root / relative\n+            if surface.is_dir():\n+                files += sorted(\n+                    path\n+                    for path in surface.rglob(\"*\")\n+                    if path.is_file()\n+                    and path.suffix in {\".md\", \".py\", \".sh\", \".toml\", \".yaml\", \".yml\"}\n+                )\n+    return sorted(set(files))\n \n \n def find_adr_reference_violations(\n@@ -94,10 +111,9 @@\n ) -> list[Violation]:\n     \"\"\"Return every ``ADR ####`` citation under ``root`` that fails to resolve.\n \n-    A citation resolves when exactly one ``docs/adr/####-*.md`` matches its\n-    number. Zero matches is a \"missing\" violation; two or more is \"ambiguous\".\n-    Records that are never cited produce nothing -- the converse is not\n-    required.\n+    A citation resolves when exactly one record in the same namespace matches\n+    its number. Zero matches is a \"missing\" violation; two or more is\n+    \"ambiguous\". Records that are never cited produce nothing.\n     \"\"\"\n     root = Path(root)\n     index = _adr_index(root)\n@@ -110,13 +126,15 @@\n         rel = path.relative_to(root).as_posix()\n         for lineno, line in enumerate(text.splitlines(), start=1):\n             for match in _CITATION.finditer(line):\n-                number = match.group(1)\n-                found = index.get(number, [])\n+                namespace, number = match.groups()\n+                found = index.get((namespace, number), [])\n                 if len(found) == 1:\n                     continue\n                 reason = \"missing\" if not found else \"ambiguous\"\n                 rels = [p.relative_to(root).as_posix() for p in found]\n-                violations.append(Violation(rel, number, lineno, reason, rels))\n+                violations.append(\n+                    Violation(rel, namespace, number, lineno, reason, rels)\n+                )\n     return violations\n \n \n@@ -129,8 +147,8 @@\n         self.assertEqual(\n             violations,\n             [],\n-            \"Every 'ADR ####' citation must resolve to exactly one \"\n-            f\"docs/adr/####-*.md:\\n{detail}\",\n+            \"Every ADR/SADR citation must resolve exactly once in its own \"\n+            f\"docs/adr namespace:\\n{detail}\",\n         )\n \n     def test_scan_actually_reads_scripts(self) -> None:\n@@ -146,8 +164,21 @@\n         # The two records this Goal carried onto the default branch, and which\n         # the scripts cite most, must be present.\n         index = _adr_index(REPO_ROOT)\n-        self.assertIn(\"0016\", index)\n-        self.assertIn(\"0017\", index)\n+        self.assertIn((\"SADR\", \"0016\"), index)\n+        self.assertIn((\"SADR\", \"0017\"), index)\n+\n+    def test_platform_tree_has_only_sadr_0001_through_0019(self) -> None:\n+        adr_dir = REPO_ROOT / \"docs\" / \"adr\"\n+        sadrs = sorted(adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\"))\n+        self.assertEqual(\n+            {path.name.split(\"-\", 2)[1] for path in sadrs},\n+            {f\"{number:04d}\" for number in range(1, 20)},\n+        )\n+        self.assertEqual(\n+            list(adr_dir.glob(\"[0-9][0-9][0-9][0-9]-*.md\")),\n+            [],\n+            \"the Shiki platform tree must not retain legacy numeric SADR paths\",\n+        )\n \n \n class FixtureAdrReferences(unittest.TestCase):\n@@ -180,6 +211,26 @@\n         self.assertIn(\"9991\", message)  # cited number\n         self.assertIn(\"docs/adr/9991-*.md\", message)  # missing path\n \n+    def test_target_adr_does_not_satisfy_missing_platform_sadr(self) -> None:\n+        root = self._make_repo(\n+            adr_names=[\"0042-target-decision.md\"],\n+            script_body='\"\"\"Target ADR 0042 exists, but platform SADR-0042 does not.\"\"\"\\n',\n+        )\n+        violations = find_adr_reference_violations(root, include_docs=False)\n+        self.assertEqual(len(violations), 1)\n+        self.assertEqual(violations[0].namespace, \"SADR\")\n+        self.assertEqual(violations[0].number, \"0042\")\n+        self.assertEqual(violations[0].expected_path, \"docs/adr/SADR-0042-*.md\")\n+\n+    def test_target_adr_and_platform_sadr_may_reuse_a_number(self) -> None:\n+        root = self._make_repo(\n+            adr_names=[\"0042-target-decision.md\", \"SADR-0042-platform-decision.md\"],\n+            script_body='\"\"\"Target ADR 0042 and platform SADR-0042 are distinct.\"\"\"\\n',\n+        )\n+        self.assertEqual(\n+            find_adr_reference_violations(root, include_docs=False), []\n+        )\n+\n     def test_uncited_adr_is_allowed(self) -> None:\n         # An ADR exists but nothing cites it -> not a violation. The converse\n         # (every ADR must be cited) is explicitly NOT required.\ndiff --git a/tests/test_sadr_install_migration.py b/tests/test_sadr_install_migration.py\nnew file mode 100644\nindex 0000000000000000000000000000000000000000..e1c1f87329132324dcb6bcdea757669752ad582b\n--- /dev/null\n+++ b/tests/test_sadr_install_migration.py\n@@ -0,0 +1,134 @@\n+\"\"\"SADR installation and collision-safe legacy upgrade behavior.\"\"\"\n+\n+from __future__ import annotations\n+\n+import contextlib\n+import hashlib\n+import io\n+import json\n+import shutil\n+import tempfile\n+import unittest\n+from pathlib import Path\n+\n+import shiki_test_support  # noqa: F401  (path bootstrap)\n+\n+import shiki_installer\n+from tests.test_adr_references import find_adr_reference_violations\n+\n+\n+PRODUCT_NUMBERS = tuple(range(13, 19))\n+\n+\n+def _quiet_install(target: Path, *, force: bool, validate: bool) -> None:\n+    with contextlib.redirect_stdout(io.StringIO()):\n+        shiki_installer.install_template(target, force=force, validate=validate)\n+\n+\n+def _sha256(path: Path) -> str:\n+    return hashlib.sha256(path.read_bytes()).hexdigest()\n+\n+\n+def _legacy_sadr_paths() -> tuple[str, ...]:\n+    return tuple(\n+        f\"docs/adr/{path.name.removeprefix('SADR-')}\"\n+        for path in sorted((shiki_installer.ROOT / \"docs\" / \"adr\").glob(\"SADR-*.md\"))\n+        if path.name != \"SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\"\n+    )\n+\n+\n+class FreshSadrInstallTests(unittest.TestCase):\n+    def setUp(self) -> None:\n+        self.tmp = Path(tempfile.mkdtemp(prefix=\"shiki-sadr-fresh-\"))\n+        self.addCleanup(lambda: shutil.rmtree(self.tmp, ignore_errors=True))\n+        self.target = self.tmp / \"target\"\n+        self.adr_dir = self.target / \"docs\" / \"adr\"\n+        self.adr_dir.mkdir(parents=True)\n+        self.product_bytes = {\n+            f\"{number:04d}-product-decision.md\": (\n+                f\"# Product ADR {number:04d}\\n\\nTarget-owned bytes {number}.\\n\"\n+            ).encode()\n+            for number in PRODUCT_NUMBERS\n+        }\n+        for name, content in self.product_bytes.items():\n+            (self.adr_dir / name).write_bytes(content)\n+\n+    def test_fresh_install_preserves_product_adrs_and_installs_all_sadrs(self) -> None:\n+        _quiet_install(self.target, force=False, validate=True)\n+\n+        for name, expected in self.product_bytes.items():\n+            self.assertEqual((self.adr_dir / name).read_bytes(), expected)\n+\n+        sadr_numbers = {\n+            path.name.split(\"-\", 2)[1]\n+            for path in self.adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\")\n+        }\n+        self.assertEqual(sadr_numbers, {f\"{number:04d}\" for number in range(1, 20)})\n+\n+        numeric_records = {\n+            path.name\n+            for path in self.adr_dir.glob(\"[0-9][0-9][0-9][0-9]-*.md\")\n+        }\n+        self.assertEqual(numeric_records, set(self.product_bytes))\n+        self.assertEqual(find_adr_reference_violations(self.target), [])\n+\n+\n+class LegacySadrUpgradeTests(unittest.TestCase):\n+    def setUp(self) -> None:\n+        self.tmp = Path(tempfile.mkdtemp(prefix=\"shiki-sadr-upgrade-\"))\n+        self.addCleanup(lambda: shutil.rmtree(self.tmp, ignore_errors=True))\n+        self.target = self.tmp / \"target\"\n+        self.adr_dir = self.target / \"docs\" / \"adr\"\n+        self.adr_dir.mkdir(parents=True)\n+\n+        self.product_bytes = {\n+            f\"{number:04d}-cubeshare-product-decision.md\": (\n+                f\"# CubeShare ADR {number:04d}\\n\\nDo not rewrite {number}.\\n\"\n+            ).encode()\n+            for number in PRODUCT_NUMBERS\n+        }\n+        for name, content in self.product_bytes.items():\n+            (self.adr_dir / name).write_bytes(content)\n+\n+        self.legacy_paths = _legacy_sadr_paths()\n+        for relative in self.legacy_paths:\n+            path = self.target / relative\n+            path.write_bytes(f\"legacy Shiki bytes for {relative}\\n\".encode())\n+\n+        stamp_path = self.target / shiki_installer.INSTALL_STAMP_PATH\n+        stamp_path.parent.mkdir(parents=True, exist_ok=True)\n+        stamp = {\n+            \"version\": shiki_installer.INSTALL_STAMP_VERSION,\n+            \"platform_commit\": None,\n+            \"installed_at\": \"2000-01-01T00:00:00+00:00\",\n+            \"digests\": {\n+                relative: _sha256(self.target / relative)\n+                for relative in self.legacy_paths\n+            },\n+        }\n+        stamp_path.write_text(\n+            json.dumps(stamp, indent=2, sort_keys=True) + \"\\n\", encoding=\"utf-8\"\n+        )\n+\n+    def test_force_upgrade_deletes_only_stamped_legacy_sadrs(self) -> None:\n+        _quiet_install(self.target, force=True, validate=True)\n+\n+        for relative in self.legacy_paths:\n+            self.assertFalse((self.target / relative).exists(), relative)\n+        for name, expected in self.product_bytes.items():\n+            self.assertEqual((self.adr_dir / name).read_bytes(), expected)\n+\n+        sadrs = list(self.adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\"))\n+        self.assertEqual(len(sadrs), 19)\n+        refreshed = json.loads(\n+            (self.target / shiki_installer.INSTALL_STAMP_PATH).read_text(encoding=\"utf-8\")\n+        )\n+        for relative in self.legacy_paths:\n+            self.assertNotIn(relative, refreshed[\"digests\"])\n+        for path in sadrs:\n+            relative = path.relative_to(self.target).as_posix()\n+            self.assertEqual(refreshed[\"digests\"][relative], _sha256(path))\n+\n+\n+if __name__ == \"__main__\":\n+    unittest.main()\n\nexec\n/bin/zsh -lc \"python3 -m unittest discover -s tests -p 'test_sadr_install_migration.py' -v\" in /private/tmp/shiki-adr-collision.pa0KwY/.worktrees/shiki-t-20260807t073307221254z-2d22d654-introduc\ndiff --git a/.codex/skills/shiki/SKILL.md b/.codex/skills/shiki/SKILL.md\nindex c8e2e85d9d75d49cc4f2d6286ee22659c4e53e75..93cded623b2c15d9f65c76491a1c2d813ac9e6c0\n--- a/.codex/skills/shiki/SKILL.md\n+++ b/.codex/skills/shiki/SKILL.md\n@@ -18,6 +18,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n Codex CLI does not register this skill as a custom `/shiki` slash command. When\n the user types `/shiki` in Codex CLI and gets \"Unrecognized command\", explain\n that this is expected: invoke Shiki in Codex with natural language such as\n@@ -69,7 +72,7 @@\n \n ## Responsibilities\n \n-- Claude Code implements and repairs by default (`shiki runner claude`, ADR 0008); Codex implements and repairs tasks explicitly assigned to `codex`.\n+- Claude Code implements and repairs by default (`shiki runner claude`, SADR-0008); Codex implements and repairs tasks explicitly assigned to `codex`.\n - Claude Code Action can act as GitHub-side CCA or reviewer.\n - CCA judges completion.\n - MergeGate authorizes state transitions and merge readiness.\ndiff --git a/.github/prompts/cca-completion-check.md b/.github/prompts/cca-completion-check.md\nindex ed203ad5f302ecc81372d8ab27d2b1232ca97497..fd89950528333ef6242e6d5443904f5392ab7e56\n--- a/.github/prompts/cca-completion-check.md\n+++ b/.github/prompts/cca-completion-check.md\n@@ -19,6 +19,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n ## Required Reading\n \n Read only the files needed to judge the PR contract. Prefer this order:\ndiff --git a/AGENTS.md b/AGENTS.md\nindex 7f041100d49c2a7525f51cb0f5d696a4a6d1c78c..2b700e989ff295987aa8a4b0c5fcb1acd80020da\n--- a/AGENTS.md\n+++ b/AGENTS.md\n@@ -44,6 +44,10 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use\n+`SADR-NNNN-*.md`; target product decisions retain `NNNN-*.md` and the `ADR`\n+namespace.\n+\n Use the canonical source-of-truth order above. Runtime-specific reading orders\n are operational guidance only and must not override or reorder it.\n \ndiff --git a/CLAUDE.md b/CLAUDE.md\nindex 98a932e7841a632866fcd53b0bad0e3af3711d3d..611d491912d6a72a0327a1731b8f33a41acb4baa\n--- a/CLAUDE.md\n+++ b/CLAUDE.md\n@@ -14,6 +14,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n ## Claude Role\n \n Default to planner, default implementer, reviewer, coordinator, documentation editor, CCA-style completion judge, and final-judgment assistant.\ndiff --git a/SYSTEM_PROMPT.md b/SYSTEM_PROMPT.md\nindex aaaea45e3a896c3a44ba89eec8bb84325ecf6c62..2e7c093ebe9e8bb34e0923756f191b4f7fa581f5\n--- a/SYSTEM_PROMPT.md\n+++ b/SYSTEM_PROMPT.md\n@@ -30,6 +30,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n Use the canonical source-of-truth order above. Runtime-specific reading orders\n are operational guidance only and must not override or reorder it. The current\n conversation is non-durable operator input.\ndiff --git a/docs/adr/0001-keep-shiki-as-platform-name.md b/docs/adr/SADR-0001-keep-shiki-as-platform-name.md\nindex 20a9637c3b9d8c97de7ba8db7cfae4d82d9a7dba..8b685439959aada1b221c112f0ac3c51ca8a1695\n--- a/docs/adr/0001-keep-shiki-as-platform-name.md\n+++ b/docs/adr/SADR-0001-keep-shiki-as-platform-name.md\n@@ -1,3 +1,3 @@\n-# Keep Shiki As The Platform Name\n+# SADR-0001: Keep Shiki As The Platform Name\n \n Shiki remains the product name for the GitHub-native agentic engineering control plane. We considered more descriptive names such as MergePilot, MergeGate, and Kiroku, but kept Shiki because it carries the existing goal-seeking framework identity while remaining independent of Hermes, Codex, Claude Code, or any single runtime.\ndiff --git a/docs/adr/0002-use-agents-as-shared-constitution.md b/docs/adr/SADR-0002-use-agents-as-shared-constitution.md\nindex 73de30ecb078120eac540d9bc51855e3cd48d0e3..c5486182bf06fca36ea508496b013c67302fd989\n--- a/docs/adr/0002-use-agents-as-shared-constitution.md\n+++ b/docs/adr/SADR-0002-use-agents-as-shared-constitution.md\n@@ -1,3 +1,3 @@\n-# Use AGENTS As The Shared Agent Constitution\n+# SADR-0002: Use AGENTS As The Shared Agent Constitution\n \n Shiki uses `AGENTS.md` as the runtime-neutral constitution and `CLAUDE.md` as a thin Claude Code wrapper. This keeps Codex, Claude Code, Hermes Runner, and future Agent Runtimes aligned on the same GitHub-first MergeGate rules while still allowing runtime-specific instructions where needed.\ndiff --git a/docs/adr/0003-require-skill-gate-for-mergegate-readiness.md b/docs/adr/SADR-0003-require-skill-gate-for-mergegate-readiness.md\nindex 424be0c9af4c8f0136f1fde68a468c47ff84e764..7fc337987c7df9792f75f181dfcbed75fe5ffff2\n--- a/docs/adr/0003-require-skill-gate-for-mergegate-readiness.md\n+++ b/docs/adr/SADR-0003-require-skill-gate-for-mergegate-readiness.md\n@@ -1,3 +1,3 @@\n-# Require Skill Gate For MergeGate Readiness\n+# SADR-0003: Require Skill Gate For MergeGate Readiness\n \n Shiki requires relevant engineering skills to be invoked before execution when their triggers apply. This makes skills part of MergeGate readiness rather than optional prompt style, because Shiki depends on repeatable planning, issue decomposition, TDD, diagnosis, triage, and architecture review before autonomous or parallel agent work can be trusted.\ndiff --git a/docs/adr/0004-use-github-first-with-shiki-mirror.md b/docs/adr/SADR-0004-use-github-first-with-shiki-mirror.md\nindex d0a3a9174dc646a945a9d325954ffaf00c352040..a79c54ae9ee8eed2a0524192b864da4e22a92d3f\n--- a/docs/adr/0004-use-github-first-with-shiki-mirror.md\n+++ b/docs/adr/SADR-0004-use-github-first-with-shiki-mirror.md\n@@ -1,4 +1,4 @@\n-# ADR 0004: Use GitHub First With A Repository-Local Shiki Mirror\n+# SADR-0004: Use GitHub First With A Repository-Local Shiki Mirror\n \n ## Status\n \ndiff --git a/docs/adr/0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md b/docs/adr/SADR-0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\nindex b339baf49b89d9290e79c3d51b36e7b2bde3bd95..c30a0d8b1fde0224d0ac7820e7870dccf721580d\n--- a/docs/adr/0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\n+++ b/docs/adr/SADR-0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\n@@ -1,4 +1,4 @@\n-# ADR 0005: Use Codex As Front And Claude Code Action As The GitHub Runtime\n+# SADR-0005: Use Codex As Front And Claude Code Action As The GitHub Runtime\n \n ## Status\n \ndiff --git a/docs/adr/0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md b/docs/adr/SADR-0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\nindex 4bcae9d1e52f0a37b8e9fbafa5cd14b6ee1993bd..7a38538a6ae9929567f0ade67e7ea6a60d0b3f7e\n--- a/docs/adr/0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\n+++ b/docs/adr/SADR-0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\n@@ -1,4 +1,4 @@\n-# ADR 0006: Separate Codex Implementation, CCA Completion, And MergeGate Authorization\n+# SADR-0006: Separate Codex Implementation, CCA Completion, And MergeGate Authorization\n \n ## Status\n \ndiff --git a/docs/adr/0007-packaging-and-release.md b/docs/adr/SADR-0007-packaging-and-release.md\nindex 79b84ddb97167372b44641e5b7860e400ce80822..023cd57f8011aa6e6d2ebf612b7c35a7a82c00d4\n--- a/docs/adr/0007-packaging-and-release.md\n+++ b/docs/adr/SADR-0007-packaging-and-release.md\n@@ -1,4 +1,4 @@\n-# ADR 0007: Packaging And Release For Shiki\n+# SADR-0007: Packaging And Release For Shiki\n \n ## Status\n \ndiff --git a/docs/adr/0008-use-claude-code-as-default-implementer.md b/docs/adr/SADR-0008-use-claude-code-as-default-implementer.md\nindex dedf4510b7c7799521422f622029563d373db031..a158a8d444d93764ec515a28133f7dde4af23579\n--- a/docs/adr/0008-use-claude-code-as-default-implementer.md\n+++ b/docs/adr/SADR-0008-use-claude-code-as-default-implementer.md\n@@ -1,4 +1,4 @@\n-# ADR 0008: Use Claude Code As The Default Implementer Runtime\n+# SADR-0008: Use Claude Code As The Default Implementer Runtime\n \n ## Status\n \ndiff --git a/docs/adr/0009-requirements-definition-and-spec-freeze.md b/docs/adr/SADR-0009-requirements-definition-and-spec-freeze.md\nindex 29b3f7871bef9203a8b4a831a65c39eb2f506e42..52e7dd69ad0aa4efe0b68c62678abbf7df02aaec\n--- a/docs/adr/0009-requirements-definition-and-spec-freeze.md\n+++ b/docs/adr/SADR-0009-requirements-definition-and-spec-freeze.md\n@@ -1,4 +1,4 @@\n-# ADR 0009: Merge Grill And Planning Into Requirements Definition With Spec Freeze\n+# SADR-0009: Merge Grill And Planning Into Requirements Definition With Spec Freeze\n \n ## Status\n \ndiff --git a/docs/adr/0010-external-ai-guardian-review.md b/docs/adr/SADR-0010-external-ai-guardian-review.md\nindex e6108bbe8bb4e73e0ff4684c8f78001a80438c4e..e690d4eddd270e683d709c9b679c5859e71eeaaa\n--- a/docs/adr/0010-external-ai-guardian-review.md\n+++ b/docs/adr/SADR-0010-external-ai-guardian-review.md\n@@ -1,4 +1,4 @@\n-# ADR 0010: External AI Guardian Review As A First-Class Approval Authority\n+# SADR-0010: External AI Guardian Review As A First-Class Approval Authority\n \n ## Status\n \ndiff --git a/docs/adr/0011-autonomous-quality-gate-evidence.md b/docs/adr/SADR-0011-autonomous-quality-gate-evidence.md\nindex bca0246c5d46e44afbde15486a75eec425fc02f5..9c93eac16d8c3a6d4b11bd7224e3955102cd7db4\n--- a/docs/adr/0011-autonomous-quality-gate-evidence.md\n+++ b/docs/adr/SADR-0011-autonomous-quality-gate-evidence.md\n@@ -1,4 +1,4 @@\n-# ADR 0011: Autonomous Quality-Gate Evidence \u2014 Deterministic Facts And An Independent Verifier\n+# SADR-0011: Autonomous Quality-Gate Evidence \u2014 Deterministic Facts And An Independent Verifier\n \n ## Status\n \ndiff --git a/docs/adr/0012-autonomous-loop-closeout-to-main.md b/docs/adr/SADR-0012-autonomous-loop-closeout-to-main.md\nindex 637fad6dde4989c9c2bf0f81f676d8d29991123c..ec7d85f81030a676bcbba28b43eb365422a36eac\n--- a/docs/adr/0012-autonomous-loop-closeout-to-main.md\n+++ b/docs/adr/SADR-0012-autonomous-loop-closeout-to-main.md\n@@ -1,4 +1,4 @@\n-# 0012 \u2014 Autonomous loop pushes goal completion to main via a closeout PR\n+# SADR-0012 \u2014 Autonomous loop pushes goal completion to main via a closeout PR\n \n - Status: accepted\n - Date: 2026-06-18\ndiff --git a/docs/adr/0013-bootstrap-grants-actions-approve-pull-requests.md b/docs/adr/SADR-0013-bootstrap-grants-actions-approve-pull-requests.md\nindex b6c69d64f1c5ced346b88d6859586715b3b5bd0e..a793e7277d67ab8ea56286fec289ca689825a17d\n--- a/docs/adr/0013-bootstrap-grants-actions-approve-pull-requests.md\n+++ b/docs/adr/SADR-0013-bootstrap-grants-actions-approve-pull-requests.md\n@@ -1,4 +1,4 @@\n-# ADR 0013: Bootstrap Grants GitHub Actions Permission To Create And Approve Pull Requests\n+# SADR-0013: Bootstrap Grants GitHub Actions Permission To Create And Approve Pull Requests\n \n ## Status\n \ndiff --git a/docs/adr/0014-codex-app-external-ai-guardian-ui-adapter.md b/docs/adr/SADR-0014-codex-app-external-ai-guardian-ui-adapter.md\nindex 238db06279334caa341c117362df9271382a7fcd..65f12e9939321e1b3e389a34c822fc1549e5155d\n--- a/docs/adr/0014-codex-app-external-ai-guardian-ui-adapter.md\n+++ b/docs/adr/SADR-0014-codex-app-external-ai-guardian-ui-adapter.md\n@@ -1,4 +1,4 @@\n-# ADR 0014: Use Codex App As The External AI Guardian UI Adapter\n+# SADR-0014: Use Codex App As The External AI Guardian UI Adapter\n \n ## Status\n \ndiff --git a/docs/adr/0015-contract-approval-before-dispatch.md b/docs/adr/SADR-0015-contract-approval-before-dispatch.md\nindex fad6c656741a20a7684ba16080a7db159f63a52f..7bdd760bb3e82eee4e1410f45c783b03b638d456\n--- a/docs/adr/0015-contract-approval-before-dispatch.md\n+++ b/docs/adr/SADR-0015-contract-approval-before-dispatch.md\n@@ -1,4 +1,4 @@\n-# ADR 0015: Approve The Task Contract Before Dispatch, Not The Diff After It\n+# SADR-0015: Approve The Task Contract Before Dispatch, Not The Diff After It\n \n ## Status\n \ndiff --git a/docs/adr/0016-locks-bound-product-paths.md b/docs/adr/SADR-0016-locks-bound-product-paths.md\nindex b5e850f3f1f7f4ef6e7f9d1763a29a51bd300c45..332cc17313498404f41a145291e38447b5de0dc4\n--- a/docs/adr/0016-locks-bound-product-paths.md\n+++ b/docs/adr/SADR-0016-locks-bound-product-paths.md\n@@ -1,4 +1,4 @@\n-# ADR 0016: Locks bound product paths; the Shiki mirror is bound by record identity\n+# SADR-0016: Locks bound product paths; the Shiki mirror is bound by record identity\n \n ## Status\n \ndiff --git a/docs/adr/0017-bookkeeping-closeouts-do-not-inherit-risk.md b/docs/adr/SADR-0017-bookkeeping-closeouts-do-not-inherit-risk.md\nindex 554e3a0d526a72ca067108c5464cc68c3edcf174..83115f69f1b1b48d544c9b0157f377b91cb53abf\n--- a/docs/adr/0017-bookkeeping-closeouts-do-not-inherit-risk.md\n+++ b/docs/adr/SADR-0017-bookkeeping-closeouts-do-not-inherit-risk.md\n@@ -1,4 +1,4 @@\n-# ADR 0017: A Bookkeeping Closeout Does Not Inherit Its Task's Risk\n+# SADR-0017: A Bookkeeping Closeout Does Not Inherit Its Task's Risk\n \n ## Status\n \ndiff --git a/docs/adr/0018-carried-guardian-approval-across-a-proven-base-sync.md b/docs/adr/SADR-0018-carried-guardian-approval-across-a-proven-base-sync.md\nindex 22358562da390ed8b45cd600da08a43d378b4680..38da0fd95102c3496c3219ce2319fce696a2532a\n--- a/docs/adr/0018-carried-guardian-approval-across-a-proven-base-sync.md\n+++ b/docs/adr/SADR-0018-carried-guardian-approval-across-a-proven-base-sync.md\n@@ -1,4 +1,4 @@\n-# ADR 0018: Carried Guardian Approval Across A Proven Base Sync\n+# SADR-0018: Carried Guardian Approval Across A Proven Base Sync\n \n ## Status\n \ndiff --git a/docs/adr/README.md b/docs/adr/README.md\nindex c70e057a4520c36e042ab68c53cd4a21ef709b71..5da43856836e2ce6a150f8127766e2f88355ab4b\n--- a/docs/adr/README.md\n+++ b/docs/adr/README.md\n@@ -1,40 +1,41 @@\n-# Architecture Decision Records\n+# Decision Record Namespaces\n \n-This directory holds Shiki's Architecture Decision Records (ADRs): durable\n-records of hard-to-reverse platform decisions. ADRs are part of the Shiki source\n-of truth alongside GitHub state, the `.shiki/` mirror, and `CONTEXT.md`.\n+This directory holds two independent decision namespaces:\n \n-## How ADRs Work\n+- Shiki Architecture Decision Records (SADRs), named\n+  `SADR-NNNN-<short-slug>.md`, record hard-to-reverse Shiki platform decisions.\n+- Target Architecture Decision Records (ADRs), named\n+  `NNNN-<short-slug>.md`, belong to the target product.\n \n-- New ADRs are added as `docs/adr/NNNN-<short-slug>.md`, where `NNNN` is the next\n-  zero-padded sequence number.\n-- The **canonical list of ADRs is the set of files matching `docs/adr/*.md`** in\n-  this directory. Browse `docs/adr/*.md` for the authoritative, current set.\n-- Use [`template.md`](template.md) as the starting point for a new ADR.\n-- Record an ADR when a decision is hard to reverse, surprising without context,\n-  and a real tradeoff. Do not write an ADR for routine or easily reversible\n-  choices.\n+The same four-digit number may appear once in each namespace. A platform\n+`SADR-NNNN` citation resolves only to `SADR-NNNN-*.md`; a target `ADR NNNN`\n+citation resolves only to `NNNN-*.md`.\n \n-Using the `docs/adr/*.md` glob as the source of truth means other PRs can add an\n-ADR without editing this index file, avoiding merge contention on a\n-hand-maintained enumeration.\n+## How Decision Records Work\n \n-## Current ADRs (snapshot)\n-\n-The list below is a convenience snapshot and may lag behind the directory. When\n-in doubt, list `docs/adr/*.md`.\n+- New Shiki platform decisions use the next zero-padded SADR number.\n+- New target product decisions use the target's own ADR sequence.\n+- The canonical list is the files in `docs/adr/`; no hand-maintained index is\n+  authoritative.\n+- Use [`template.md`](template.md) as the starting point and choose the filename\n+  namespace that owns the decision.\n+- Record a decision when it is hard to reverse, surprising without context, and\n+  a real tradeoff. Do not write a decision record for routine or easily\n+  reversible choices.\n \n-- [0001 \u2014 Keep Shiki As The Platform Name](0001-keep-shiki-as-platform-name.md)\n-- [0002 \u2014 Use AGENTS As The Shared Agent Constitution](0002-use-agents-as-shared-constitution.md)\n-- [0003 \u2014 Require Skill Gate For MergeGate Readiness](0003-require-skill-gate-for-mergegate-readiness.md)\n-- [0004 \u2014 Use GitHub First With A Repository-Local Shiki Mirror](0004-use-github-first-with-shiki-mirror.md)\n-- [0005 \u2014 Use Codex As Front And Claude Code Action As The GitHub Runtime](0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md)\n-- [0006 \u2014 Separate Codex Implementation, CCA Completion, And MergeGate Authorization](0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md)\n+Using the directory as the source of truth lets independent changes add records\n+without contending on an enumerated index.\n \n-## Adding A New ADR\n+## Adding A Shiki SADR\n \n-1. Copy `template.md` to `docs/adr/NNNN-<short-slug>.md`.\n+1. Copy `template.md` to `docs/adr/SADR-NNNN-<short-slug>.md`.\n 2. Fill in the title, status, context, decision, and consequences.\n-3. Set the status to `Proposed`, then `Accepted` once the decision is approved.\n-4. Open a PR following `CONTRIBUTING.md`. You do not need to edit this index for\n-   the ADR to be discoverable via `docs/adr/*.md`.\n+3. Set the status to `Proposed`, then `Accepted` once the proper authority has\n+   approved the decision.\n+4. Open a PR following `CONTRIBUTING.md`.\n+\n+## Adding A Target ADR\n+\n+1. Copy `template.md` to `docs/adr/NNNN-<short-slug>.md` using the target's own\n+   sequence.\n+2. Cite it as `ADR NNNN`; do not prefix its filename with `SADR-`.\ndiff --git a/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md b/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\nnew file mode 100644\nindex 0000000000000000000000000000000000000000..7455ce1f3bed02ddddb851ac21a1d1a648c9aeba\n--- /dev/null\n+++ b/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\n@@ -0,0 +1,55 @@\n+# SADR-0019: Separate Shiki Platform SADRs From Target ADRs\n+\n+## Status\n+\n+Accepted\n+\n+## Context\n+\n+Shiki installs its platform decision records into `docs/adr/` in every target\n+repository. Shiki and its targets previously used the same numeric\n+`NNNN-<slug>.md` namespace. A target that already owned ADRs 0013 through 0018\n+therefore acquired two records for each number during installation, making an\n+`ADR NNNN` citation ambiguous. Renumbering target decisions would rewrite target\n+history, while renumbering Shiki decisions per target would destroy their stable\n+platform identity.\n+\n+Upgrades also need to remove the former Shiki-owned numeric paths without ever\n+mistaking a target-owned ADR for platform content. The install stamp already\n+records exact shipped paths and content digests, so it can prove ownership only\n+when both still match.\n+\n+## Decision\n+\n+Shiki platform decisions use the `SADR` namespace:\n+\n+- records are named `docs/adr/SADR-NNNN-<slug>.md`;\n+- live platform citations use `SADR-NNNN`;\n+- target product decisions retain `docs/adr/NNNN-<slug>.md` and `ADR NNNN`;\n+- the reference validator resolves the two namespaces independently, so the same\n+  four-digit number may exist once in each namespace.\n+\n+Before a forced upgrade writes anything, the installer inventories the exact\n+former numeric Shiki ADR paths. It may delete a legacy path only when the\n+existing install stamp contains that exact path and its recorded SHA-256 digest\n+matches the file currently on disk. All ownership blockers are reported\n+together and abort the upgrade before its first write. A non-forced install\n+never performs this cleanup.\n+\n+Historical `.shiki/` evidence remains unchanged because it records the namespace\n+that existed when that evidence was produced.\n+\n+## Consequences\n+\n+- Targets may use any ADR number without colliding with Shiki's platform SADRs.\n+- Fresh installs preserve target ADR bytes and install a stable SADR set.\n+- Forced upgrades fail closed when legacy ownership cannot be proven, and a\n+  successful upgrade refreshes the stamp without removed legacy paths.\n+- Current governance, packaging, workflow, prompt, script, and skill references\n+  must move with the platform records.\n+\n+## Alternatives Considered\n+\n+- Collision-only refusal leaves affected targets permanently unupgradable.\n+- Renumbering target ADRs mutates target-owned history.\n+- Per-target Shiki renumbering makes platform citations non-portable.\ndiff --git a/docs/adr/template.md b/docs/adr/template.md\nindex a92a97daddd0b00ab9dd626cc8d86942da2eafed..17dd79175b5fee2105935557f691b40cf9492942\n--- a/docs/adr/template.md\n+++ b/docs/adr/template.md\n@@ -1,14 +1,14 @@\n-# NNNN: <Short Decision Title>\n+# <SADR-NNNN or NNNN>: <Short Decision Title>\n \n ## Status\n \n-Proposed | Accepted | Superseded by ADR-NNNN\n+Proposed | Accepted | Superseded by SADR-NNNN or target ADR NNNN\n \n ## Context\n \n What is the situation, constraint, or force that requires a decision? Reference\n-relevant domain terms (`CONTEXT.md`), prior ADRs, and code reality. State the\n-problem, not the solution.\n+relevant domain terms (`CONTEXT.md`), prior Shiki SADRs or target ADRs, and code\n+reality. State the problem, not the solution.\n \n ## Decision\n \ndiff --git a/docs/agents/domain.md b/docs/agents/domain.md\nindex 258da8ea81e87ac0a6b0b5b5e5c2fde980eaabb7..67aaa6f4b7d0a5aa4d495ce6279073333f7798cb\n--- a/docs/agents/domain.md\n+++ b/docs/agents/domain.md\n@@ -5,7 +5,9 @@\n ## Layout\n \n - `CONTEXT.md` defines Shiki language. Keep it implementation-free.\n-- `docs/adr/` records hard-to-reverse decisions.\n+- `docs/adr/SADR-NNNN-*.md` records Shiki platform decisions.\n+- `docs/adr/NNNN-*.md` records target product decisions without sharing the\n+  SADR namespace.\n - `AGENTS.md` is the runtime-neutral agent constitution.\n - `CLAUDE.md` is the Claude Code wrapper around `AGENTS.md`.\n - `.shiki/` is the Target Repository mirror for Goals, PRDs, plans, Task DAGs, contracts, locks, worktree records, ledger entries, CCA verdicts, repair packets, reports, and handoffs.\ndiff --git a/docs/agents/implementation-policy.md b/docs/agents/implementation-policy.md\nindex c754144a9fb5bf4a38aa82a60e1670c59e626b44..e90d40fe21880b9a09719c5ecfe6bde7284eb3ce\n--- a/docs/agents/implementation-policy.md\n+++ b/docs/agents/implementation-policy.md\n@@ -31,6 +31,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n ## Non-Negotiables\n \n 1. **Planning precedes implementation.** Non-trivial Goals start with `grill-with-docs` and Context & Impact.\ndiff --git a/scripts/shiki_installer.py b/scripts/shiki_installer.py\nindex be064ab7a29ff6836f7f7a1d70500a43e0d11d7b..d3e3247edbf3e4be287a4667176d86f6500f8dc5\n--- a/scripts/shiki_installer.py\n+++ b/scripts/shiki_installer.py\n@@ -118,6 +118,7 @@\n # what lets a target carry it without a governance-schema change.\n INSTALL_STAMP_PATH = \".shiki/install-stamp.json\"\n INSTALL_STAMP_VERSION = 1\n+LEGACY_SHIKI_SADR_MAX = 18\n \n # Under --force the shipped surface splits three ways, so nothing is lost and\n # nothing is silently stale.\n@@ -237,9 +238,98 @@\n         )\n \n \n+def legacy_shiki_adr_paths() -> tuple[str, ...]:\n+    \"\"\"Former numeric paths for Shiki SADR-0001 through SADR-0018.\n+\n+    The authoritative slugs come from the current SADR filenames. SADR-0019 is\n+    the namespace decision itself and never shipped under a numeric legacy path.\n+    \"\"\"\n+    paths: list[str] = []\n+    for sadr in sorted((ROOT / \"docs\" / \"adr\").glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\")):\n+        number = sadr.name.split(\"-\", 2)[1]\n+        if int(number) <= LEGACY_SHIKI_SADR_MAX:\n+            paths.append(f\"docs/adr/{sadr.name.removeprefix('SADR-')}\")\n+    return tuple(paths)\n+\n+\n+def preflight_legacy_sadr_cleanup(target: Path, *, force: bool) -> tuple[str, ...]:\n+    \"\"\"Authorize exact legacy Shiki ADR removals before the first write.\n+\n+    A non-force install never removes existing files. A forced install needs no\n+    stamp when no legacy Shiki path is present. When legacy paths do exist, each\n+    must be named in a readable install stamp and its current bytes must match\n+    the recorded digest.\n+    \"\"\"\n+    if not force:\n+        return ()\n+    present = tuple(\n+        relative\n+        for relative in legacy_shiki_adr_paths()\n+        if (target / relative).exists() or (target / relative).is_symlink()\n+    )\n+    if not present:\n+        return ()\n+\n+    stamp_path = target / INSTALL_STAMP_PATH\n+    try:\n+        raw = stamp_path.read_text(encoding=\"utf-8\")\n+        stamp = json.loads(raw)\n+    except FileNotFoundError:\n+        detail = \"install stamp is absent\"\n+        blockers = [f\"{relative}: {detail}\" for relative in present]\n+    except (OSError, UnicodeDecodeError) as error:\n+        detail = f\"install stamp is unreadable ({error})\"\n+        blockers = [f\"{relative}: {detail}\" for relative in present]\n+    except json.JSONDecodeError as error:\n+        detail = f\"install stamp is malformed ({error})\"\n+        blockers = [f\"{relative}: {detail}\" for relative in present]\n+    else:\n+        if not isinstance(stamp, dict):\n+            blockers = [\n+                f\"{relative}: install stamp is malformed (expected an object)\"\n+                for relative in present\n+            ]\n+        else:\n+            digests = stamp.get(\"digests\")\n+            blockers = []\n+            if not isinstance(digests, dict):\n+                blockers.extend(\n+                    f\"{relative}: install stamp has no digest for this path\"\n+                    for relative in present\n+                )\n+            else:\n+                for relative in present:\n+                    expected = digests.get(relative)\n+                    if not isinstance(expected, str) or not expected:\n+                        blockers.append(\n+                            f\"{relative}: install stamp has no digest for this path\"\n+                        )\n+                        continue\n+                    try:\n+                        actual = _sha256_file(target / relative)\n+                    except OSError as error:\n+                        blockers.append(\n+                            f\"{relative}: legacy path is unreadable ({error})\"\n+                        )\n+                        continue\n+                    if actual != expected:\n+                        blockers.append(\n+                            f\"{relative}: digest mismatch (stamp {expected}, current {actual})\"\n+                        )\n+\n+    if blockers:\n+        joined = \"\\n\".join(f\"- {blocker}\" for blocker in blockers)\n+        raise ShikiError(\n+            \"legacy Shiki ADR cleanup is not authorized:\\n\"\n+            f\"{joined}\\nNo files were written.\"\n+        )\n+    return present\n+\n+\n def install_template(target: Path, *, force: bool, validate: bool) -> list[NewFileNote]:\n     # Preconditions first: never rewrite the tree of a target that cannot be\n     # upgraded cleanly (see refuse_pending_migrations).\n+    legacy_cleanup = preflight_legacy_sadr_cleanup(target, force=force)\n     refuse_pending_migrations(target)\n \n     # Resolve the target's required CODEOWNERS owner once from its\n@@ -250,6 +340,10 @@\n     # verbatim. See ``codeowners_required_owner``.\n     codeowners_owner = codeowners_required_owner(target)\n \n+    for relative in legacy_cleanup:\n+        (target / relative).unlink()\n+        info(f\"removed stamped legacy Shiki ADR: {target / relative}\")\n+\n     notes: list[NewFileNote] = []\n     for relative in TEMPLATE_PATHS:\n         source = ROOT / relative\ndiff --git a/tests/test_adr_references.py b/tests/test_adr_references.py\nindex 51706329c797d5dd9b47e082a333fa83b261d482..91ab76b724cb973435c45d53cab63793942267e9\n--- a/tests/test_adr_references.py\n+++ b/tests/test_adr_references.py\n@@ -1,4 +1,4 @@\n-\"\"\"Every ``ADR ####`` citation must resolve to exactly one decision record.\n+\"\"\"Every target ADR and Shiki SADR citation resolves in its own namespace.\n \n Background. ``scripts/mergegate_check.py`` justifies ``is_bookkeeping_closeout``\n with \"ADR 0017\" nine times and ``scripts/guardian_approval_signal.py`` cites\n@@ -11,11 +11,12 @@\n and the directory drifted with no signal.\n \n This is the same unbound-pair shape behind other silent failures: a reference\n-and its target with nothing asserting they agree. This test binds them. It scans\n-every ``scripts/*.py`` (plus ``docs/**/*.md`` and the root ``*.md`` governance\n-files, which are cheap) for the ``ADR ####`` pattern and asserts each cited\n-number resolves to exactly one ``docs/adr/####-*.md``. The converse is\n-deliberately NOT required: an ADR may exist without being cited.\n+and its target with nothing asserting they agree. This test binds them. Target\n+``ADR NNNN`` citations resolve only to ``docs/adr/NNNN-*.md``; Shiki\n+``SADR-NNNN`` citations resolve only to ``docs/adr/SADR-NNNN-*.md``. The scan\n+covers current governance docs, scripts, workflows, prompts, packaging metadata,\n+and repo-local Codex guidance while deliberately excluding historical\n+``.shiki`` evidence. The converse is not required: a record may be uncited.\n \n The suite is dependency free: standard-library ``unittest`` only.\n \"\"\"\n@@ -31,17 +32,18 @@\n \n REPO_ROOT = Path(__file__).resolve().parents[1]\n \n-# \"ADR 0016\" -- a four-digit citation. The trailing \\b keeps a five-digit token\n-# from being read as a truncated four-digit number.\n-_CITATION = re.compile(r\"\\bADR (\\d{4})\\b\")\n-# \"0016-locks-bound-product-paths.md\" -- the leading four digits name the record.\n-_ADR_FILENAME = re.compile(r\"^(\\d{4})-.*\\.md$\")\n+# ``ADR 0016`` / ``SADR-0016`` -- a namespace plus four-digit citation. The\n+# trailing ``\\b`` keeps a five-digit token from being read as a truncated one.\n+_CITATION = re.compile(r\"\\b(S?ADR)[ -](\\d{4})\\b\")\n+# Target ADRs retain the numeric filename; Shiki platform decisions use SADR.\n+_DECISION_FILENAME = re.compile(r\"^(?:(SADR)-)?(\\d{4})-.*\\.md$\")\n \n \n class Violation(NamedTuple):\n     \"\"\"A citation that does not resolve to exactly one decision record.\"\"\"\n \n     citing_file: str  # repo-relative path of the file that cites the ADR\n+    namespace: str  # \"ADR\" for target decisions; \"SADR\" for Shiki decisions\n     number: str  # the cited four-digit number, e.g. \"0016\"\n     line: int  # 1-based line of the citation\n     reason: str  # \"missing\" (zero matches) or \"ambiguous\" (two or more)\n@@ -49,44 +51,59 @@\n \n     @property\n     def expected_path(self) -> str:\n-        return f\"docs/adr/{self.number}-*.md\"\n+        prefix = \"SADR-\" if self.namespace == \"SADR\" else \"\"\n+        return f\"docs/adr/{prefix}{self.number}-*.md\"\n \n     def describe(self) -> str:\n         \"\"\"Name the citing file, the cited number, and the missing path.\"\"\"\n         if self.reason == \"missing\":\n             return (\n-                f\"{self.citing_file}:{self.line} cites ADR {self.number} \"\n+                f\"{self.citing_file}:{self.line} cites {self.namespace} {self.number} \"\n                 f\"but no document matches {self.expected_path}\"\n             )\n         joined = \", \".join(self.matches)\n         return (\n-            f\"{self.citing_file}:{self.line} cites ADR {self.number} which \"\n+            f\"{self.citing_file}:{self.line} cites {self.namespace} {self.number} which \"\n             f\"resolves to {len(self.matches)} documents (expected exactly one \"\n             f\"at {self.expected_path}): {joined}\"\n         )\n \n \n-def _adr_index(root: Path) -> dict[str, list[Path]]:\n-    \"\"\"Map every four-digit ADR number to the record files that claim it.\"\"\"\n-    index: dict[str, list[Path]] = {}\n+def _adr_index(root: Path) -> dict[tuple[str, str], list[Path]]:\n+    \"\"\"Map each decision namespace and number to the files that claim it.\"\"\"\n+    index: dict[tuple[str, str], list[Path]] = {}\n     adr_dir = root / \"docs\" / \"adr\"\n     if adr_dir.is_dir():\n         for path in sorted(adr_dir.glob(\"*.md\")):\n-            match = _ADR_FILENAME.match(path.name)\n+            match = _DECISION_FILENAME.match(path.name)\n             if match:\n-                index.setdefault(match.group(1), []).append(path)\n+                namespace = \"SADR\" if match.group(1) else \"ADR\"\n+                index.setdefault((namespace, match.group(2)), []).append(path)\n     return index\n \n \n def _citing_files(root: Path, include_docs: bool = True) -> list[Path]:\n-    \"\"\"Files scanned for citations: scripts/*.py, and cheaply the markdown.\"\"\"\n+    \"\"\"Current, shipped text surfaces scanned for decision citations.\"\"\"\n     files = sorted((root / \"scripts\").glob(\"*.py\"))\n+    files += sorted((root / \"scripts\").glob(\"*.sh\"))\n     if include_docs:\n         docs = root / \"docs\"\n         if docs.is_dir():\n             files += sorted(docs.rglob(\"*.md\"))\n         files += sorted(root.glob(\"*.md\"))\n-    return files\n+        pyproject = root / \"pyproject.toml\"\n+        if pyproject.is_file():\n+            files.append(pyproject)\n+        for relative in (\".github\", \".codex\"):\n+            surface = root / relative\n+            if surface.is_dir():\n+                files += sorted(\n+                    path\n+                    for path in surface.rglob(\"*\")\n+                    if path.is_file()\n+                    and path.suffix in {\".md\", \".py\", \".sh\", \".toml\", \".yaml\", \".yml\"}\n+                )\n+    return sorted(set(files))\n \n \n def find_adr_reference_violations(\n@@ -94,10 +111,9 @@\n ) -> list[Violation]:\n     \"\"\"Return every ``ADR ####`` citation under ``root`` that fails to resolve.\n \n-    A citation resolves when exactly one ``docs/adr/####-*.md`` matches its\n-    number. Zero matches is a \"missing\" violation; two or more is \"ambiguous\".\n-    Records that are never cited produce nothing -- the converse is not\n-    required.\n+    A citation resolves when exactly one record in the same namespace matches\n+    its number. Zero matches is a \"missing\" violation; two or more is\n+    \"ambiguous\". Records that are never cited produce nothing.\n     \"\"\"\n     root = Path(root)\n     index = _adr_index(root)\n@@ -110,13 +126,15 @@\n         rel = path.relative_to(root).as_posix()\n         for lineno, line in enumerate(text.splitlines(), start=1):\n             for match in _CITATION.finditer(line):\n-                number = match.group(1)\n-                found = index.get(number, [])\n+                namespace, number = match.groups()\n+                found = index.get((namespace, number), [])\n                 if len(found) == 1:\n                     continue\n                 reason = \"missing\" if not found else \"ambiguous\"\n                 rels = [p.relative_to(root).as_posix() for p in found]\n-                violations.append(Violation(rel, number, lineno, reason, rels))\n+                violations.append(\n+                    Violation(rel, namespace, number, lineno, reason, rels)\n+                )\n     return violations\n \n \n@@ -129,8 +147,8 @@\n         self.assertEqual(\n             violations,\n             [],\n-            \"Every 'ADR ####' citation must resolve to exactly one \"\n-            f\"docs/adr/####-*.md:\\n{detail}\",\n+            \"Every ADR/SADR citation must resolve exactly once in its own \"\n+            f\"docs/adr namespace:\\n{detail}\",\n         )\n \n     def test_scan_actually_reads_scripts(self) -> None:\n@@ -146,8 +164,21 @@\n         # The two records this Goal carried onto the default branch, and which\n         # the scripts cite most, must be present.\n         index = _adr_index(REPO_ROOT)\n-        self.assertIn(\"0016\", index)\n-        self.assertIn(\"0017\", index)\n+        self.assertIn((\"SADR\", \"0016\"), index)\n+        self.assertIn((\"SADR\", \"0017\"), index)\n+\n+    def test_platform_tree_has_only_sadr_0001_through_0019(self) -> None:\n+        adr_dir = REPO_ROOT / \"docs\" / \"adr\"\n+        sadrs = sorted(adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\"))\n+        self.assertEqual(\n+            {path.name.split(\"-\", 2)[1] for path in sadrs},\n+            {f\"{number:04d}\" for number in range(1, 20)},\n+        )\n+        self.assertEqual(\n+            list(adr_dir.glob(\"[0-9][0-9][0-9][0-9]-*.md\")),\n+            [],\n+            \"the Shiki platform tree must not retain legacy numeric SADR paths\",\n+        )\n \n \n class FixtureAdrReferences(unittest.TestCase):\n@@ -180,6 +211,26 @@\n         self.assertIn(\"9991\", message)  # cited number\n         self.assertIn(\"docs/adr/9991-*.md\", message)  # missing path\n \n+    def test_target_adr_does_not_satisfy_missing_platform_sadr(self) -> None:\n+        root = self._make_repo(\n+            adr_names=[\"0042-target-decision.md\"],\n+            script_body='\"\"\"Target ADR 0042 exists, but platform SADR-0042 does not.\"\"\"\\n',\n+        )\n+        violations = find_adr_reference_violations(root, include_docs=False)\n+        self.assertEqual(len(violations), 1)\n+        self.assertEqual(violations[0].namespace, \"SADR\")\n+        self.assertEqual(violations[0].number, \"0042\")\n+        self.assertEqual(violations[0].expected_path, \"docs/adr/SADR-0042-*.md\")\n+\n+    def test_target_adr_and_platform_sadr_may_reuse_a_number(self) -> None:\n+        root = self._make_repo(\n+            adr_names=[\"0042-target-decision.md\", \"SADR-0042-platform-decision.md\"],\n+            script_body='\"\"\"Target ADR 0042 and platform SADR-0042 are distinct.\"\"\"\\n',\n+        )\n+        self.assertEqual(\n+            find_adr_reference_violations(root, include_docs=False), []\n+        )\n+\n     def test_uncited_adr_is_allowed(self) -> None:\n         # An ADR exists but nothing cites it -> not a violation. The converse\n         # (every ADR must be cited) is explicitly NOT required.\ndiff --git a/tests/test_sadr_install_migration.py b/tests/test_sadr_install_migration.py\nnew file mode 100644\nindex 0000000000000000000000000000000000000000..e1c1f87329132324dcb6bcdea757669752ad582b\n--- /dev/null\n+++ b/tests/test_sadr_install_migration.py\n@@ -0,0 +1,134 @@\n+\"\"\"SADR installation and collision-safe legacy upgrade behavior.\"\"\"\n+\n+from __future__ import annotations\n+\n+import contextlib\n+import hashlib\n+import io\n+import json\n+import shutil\n+import tempfile\n+import unittest\n+from pathlib import Path\n+\n+import shiki_test_support  # noqa: F401  (path bootstrap)\n+\n+import shiki_installer\n+from tests.test_adr_references import find_adr_reference_violations\n+\n+\n+PRODUCT_NUMBERS = tuple(range(13, 19))\n+\n+\n+def _quiet_install(target: Path, *, force: bool, validate: bool) -> None:\n+    with contextlib.redirect_stdout(io.StringIO()):\n+        shiki_installer.install_template(target, force=force, validate=validate)\n+\n+\n+def _sha256(path: Path) -> str:\n+    return hashlib.sha256(path.read_bytes()).hexdigest()\n+\n+\n+def _legacy_sadr_paths() -> tuple[str, ...]:\n+    return tuple(\n+        f\"docs/adr/{path.name.removeprefix('SADR-')}\"\n+        for path in sorted((shiki_installer.ROOT / \"docs\" / \"adr\").glob(\"SADR-*.md\"))\n+        if path.name != \"SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\"\n+    )\n+\n+\n+class FreshSadrInstallTests(unittest.TestCase):\n+    def setUp(self) -> None:\n+        self.tmp = Path(tempfile.mkdtemp(prefix=\"shiki-sadr-fresh-\"))\n+        self.addCleanup(lambda: shutil.rmtree(self.tmp, ignore_errors=True))\n+        self.target = self.tmp / \"target\"\n+        self.adr_dir = self.target / \"docs\" / \"adr\"\n+        self.adr_dir.mkdir(parents=True)\n+        self.product_bytes = {\n+            f\"{number:04d}-product-decision.md\": (\n+                f\"# Product ADR {number:04d}\\n\\nTarget-owned bytes {number}.\\n\"\n+            ).encode()\n+            for number in PRODUCT_NUMBERS\n+        }\n+        for name, content in self.product_bytes.items():\n+            (self.adr_dir / name).write_bytes(content)\n+\n+    def test_fresh_install_preserves_product_adrs_and_installs_all_sadrs(self) -> None:\n+        _quiet_install(self.target, force=False, validate=True)\n+\n+        for name, expected in self.product_bytes.items():\n+            self.assertEqual((self.adr_dir / name).read_bytes(), expected)\n+\n+        sadr_numbers = {\n+            path.name.split(\"-\", 2)[1]\n+            for path in self.adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\")\n+        }\n+        self.assertEqual(sadr_numbers, {f\"{number:04d}\" for number in range(1, 20)})\n+\n+        numeric_records = {\n+            path.name\n+            for path in self.adr_dir.glob(\"[0-9][0-9][0-9][0-9]-*.md\")\n+        }\n+        self.assertEqual(numeric_records, set(self.product_bytes))\n+        self.assertEqual(find_adr_reference_violations(self.target), [])\n+\n+\n+class LegacySadrUpgradeTests(unittest.TestCase):\n+    def setUp(self) -> None:\n+        self.tmp = Path(tempfile.mkdtemp(prefix=\"shiki-sadr-upgrade-\"))\n+        self.addCleanup(lambda: shutil.rmtree(self.tmp, ignore_errors=True))\n+        self.target = self.tmp / \"target\"\n+        self.adr_dir = self.target / \"docs\" / \"adr\"\n+        self.adr_dir.mkdir(parents=True)\n+\n+        self.product_bytes = {\n+            f\"{number:04d}-cubeshare-product-decision.md\": (\n+                f\"# CubeShare ADR {number:04d}\\n\\nDo not rewrite {number}.\\n\"\n+            ).encode()\n+            for number in PRODUCT_NUMBERS\n+        }\n+        for name, content in self.product_bytes.items():\n+            (self.adr_dir / name).write_bytes(content)\n+\n+        self.legacy_paths = _legacy_sadr_paths()\n+        for relative in self.legacy_paths:\n+            path = self.target / relative\n+            path.write_bytes(f\"legacy Shiki bytes for {relative}\\n\".encode())\n+\n+        stamp_path = self.target / shiki_installer.INSTALL_STAMP_PATH\n+        stamp_path.parent.mkdir(parents=True, exist_ok=True)\n+        stamp = {\n+            \"version\": shiki_installer.INSTALL_STAMP_VERSION,\n+            \"platform_commit\": None,\n+            \"installed_at\": \"2000-01-01T00:00:00+00:00\",\n+            \"digests\": {\n+                relative: _sha256(self.target / relative)\n+                for relative in self.legacy_paths\n+            },\n+        }\n+        stamp_path.write_text(\n+            json.dumps(stamp, indent=2, sort_keys=True) + \"\\n\", encoding=\"utf-8\"\n+        )\n+\n+    def test_force_upgrade_deletes_only_stamped_legacy_sadrs(self) -> None:\n+        _quiet_install(self.target, force=True, validate=True)\n+\n+        for relative in self.legacy_paths:\n+            self.assertFalse((self.target / relative).exists(), relative)\n+        for name, expected in self.product_bytes.items():\n+            self.assertEqual((self.adr_dir / name).read_bytes(), expected)\n+\n+        sadrs = list(self.adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\"))\n+        self.assertEqual(len(sadrs), 19)\n+        refreshed = json.loads(\n+            (self.target / shiki_installer.INSTALL_STAMP_PATH).read_text(encoding=\"utf-8\")\n+        )\n+        for relative in self.legacy_paths:\n+            self.assertNotIn(relative, refreshed[\"digests\"])\n+        for path in sadrs:\n+            relative = path.relative_to(self.target).as_posix()\n+            self.assertEqual(refreshed[\"digests\"][relative], _sha256(path))\n+\n+\n+if __name__ == \"__main__\":\n+    unittest.main()\n\n succeeded in 914ms:\nok\ntest_force_upgrade_deletes_only_stamped_legacy_sadrs (test_sadr_install_migration.LegacySadrUpgradeTests.test_force_upgrade_deletes_only_stamped_legacy_sadrs) ... ok\n\n----------------------------------------------------------------------\nRan 2 tests in 0.891s\n\nOK\n\nexec\n/bin/zsh -lc \"rg -n 'install_stamp|digests|stamped' scripts/shiki_doctor.py | sed -n '1,220p'; sed -n '430,590p' scripts/shiki_doctor.py\" in /private/tmp/shiki-adr-collision.pa0KwY/.worktrees/shiki-t-20260807t073307221254z-2d22d654-introduc\n succeeded in 0ms:\n23:    read_install_stamp,\n992:def _install_stamp_content_finding(target: Path, stamp: dict[str, Any]) -> DoctorFinding:\n993:    digests = stamp.get(\"digests\")\n994:    if not isinstance(digests, dict):\n996:            \"doctor.install_stamp.content\",\n999:            \"Install stamp is missing its per-path content digests, so drift cannot be checked.\",\n1005:    for relative, expected in sorted(digests.items()):\n1016:        \"doctor.install_stamp.content\",\n1019:        \"All stamped shipped paths match their recorded content digests.\"\n1021:        else f\"Shipped paths differ from their stamped content digests: {', '.join(problems)}.\",\n1027:def _install_stamp_commit_finding(target: Path, stamp: dict[str, Any]) -> DoctorFinding:\n1031:            \"doctor.install_stamp.platform_commit\",\n1040:            \"doctor.install_stamp.platform_commit\",\n1048:            \"doctor.install_stamp.platform_commit\",\n1056:            \"doctor.install_stamp.platform_commit\",\n1063:        \"doctor.install_stamp.platform_commit\",\n1073:def _install_stamp_findings(target: Path) -> list[DoctorFinding]:\n1079:                \"doctor.install_stamp.present\",\n1088:                \"doctor.install_stamp.content\",\n1091:                \"No install stamp, so shipped-content digests cannot be checked.\",\n1095:                \"doctor.install_stamp.platform_commit\",\n1102:    stamp = read_install_stamp(target)\n1106:                \"doctor.install_stamp.present\",\n1116:            \"doctor.install_stamp.present\",\n1127:        _install_stamp_content_finding(target, stamp),\n1128:        _install_stamp_commit_finding(target, stamp),\n1162:    findings.extend(_install_stamp_findings(target))\n        )\n    except Exception as error:  # pragma: no cover - import contract failure is surfaced as a finding.\n        return [_finding(\"doctor.codeowners.coverage\", \"fail\", \"CODEOWNERS governance\", f\"Could not import CODEOWNERS contract: {error}\")]\n    # Resolve the required owner per target (falls back to the documented\n    # CODEOWNERS_REQUIRED_OWNER constant when no owner can be resolved), so a\n    # foreign target is judged against its own owner rather than this maintainer.\n    required_owner = codeowners_required_owner(target)\n    path = target / CODEOWNERS_PATH\n    if not path.exists():\n        return [\n            _finding(\n                \"doctor.codeowners.coverage\",\n                \"fail\",\n                \"CODEOWNERS governance\",\n                f\"{CODEOWNERS_PATH} is missing.\",\n                \"Restore CODEOWNERS governance for critical Shiki paths.\",\n            )\n        ]\n    coverage: dict[str, set[str]] = {}\n    for raw_line in path.read_text(encoding=\"utf-8\").splitlines():\n        stripped = raw_line.strip()\n        if not stripped or stripped.startswith(\"#\"):\n            continue\n        parts = stripped.split()\n        if len(parts) >= 2:\n            coverage.setdefault(parts[0], set()).update(parts[1:])\n    missing = []\n    wrong_owner = []\n    for critical_path in CODEOWNERS_CRITICAL_PATHS:\n        owners = coverage.get(critical_path)\n        if not owners:\n            missing.append(critical_path)\n        elif required_owner not in owners:\n            wrong_owner.append(critical_path)\n    ok = not missing and not wrong_owner\n    return [\n        _finding(\n            \"doctor.codeowners.coverage\",\n            \"pass\" if ok else \"fail\",\n            \"CODEOWNERS governance\",\n            \"CODEOWNERS covers critical Shiki paths.\" if ok else \"CODEOWNERS coverage is incomplete for critical Shiki paths.\",\n            f\"Ensure each critical path is owned by {required_owner}.\" if not ok else \"\",\n            {\"missing\": missing, \"wrong_owner\": wrong_owner, \"required_owner\": required_owner},\n        )\n    ]\n\n\ndef _manifest_findings(target: Path) -> list[DoctorFinding]:\n    try:\n        manifest = load_manifest(target)\n    except ManifestError as error:\n        return [_finding(\"doctor.manifest.layout\", \"fail\", \"Shiki manifest layout\", str(error), \"Restore .shiki/manifest.json.\")]\n    missing_dirs = [relative for relative in manifest_required_directories(manifest) if not (target / relative).is_dir()]\n    missing_files = [relative for relative in manifest_required_files(manifest) if not (target / relative).is_file()]\n    tracked_runtime = []\n    for relative in manifest_runtime_directories(manifest):\n        if not (target / relative).exists():\n            continue\n        tracked = subprocess.run([\"git\", \"ls-files\", \"--\", relative], cwd=str(target), text=True, capture_output=True, check=False)\n        if tracked.returncode == 0 and tracked.stdout.strip():\n            tracked_runtime.append(relative)\n    ok = not missing_dirs and not missing_files and not tracked_runtime\n    return [\n        _finding(\n            \"doctor.manifest.layout\",\n            \"pass\" if ok else \"fail\",\n            \"Shiki manifest layout\",\n            \"Manifest-required directories/files are present and runtime-only evidence is not committed.\"\n            if ok\n            else \"Manifest-required layout is incomplete or runtime-only evidence is present.\",\n            \"Run `python3 scripts/validate_shiki.py` for exact manifest drift and repair the listed files.\" if not ok else \"\",\n            {\"missing_directories\": missing_dirs, \"missing_files\": missing_files, \"runtime_directories_with_files\": tracked_runtime},\n        )\n    ]\n\n\ndef _state_class_findings(target: Path) -> list[DoctorFinding]:\n    try:\n        manifest = load_manifest(target)\n    except ManifestError as error:\n        return [_finding(\"doctor.state_classes.manifest\", \"fail\", \"Shiki state classes\", str(error), \"Restore .shiki/manifest.json.\")]\n\n    state_classes = manifest_state_classes(manifest)\n    summary = state_class_summary(manifest)\n    missing_required = [\n        state_class\n        for state_class in (\"append-only-evidence\", \"governance-policy\", \"migration-state\", \"workflow-runtime-evidence\")\n        if state_class not in state_classes\n    ]\n    manifest_ok = bool(state_classes) and not missing_required\n    findings = [\n        _finding(\n            \"doctor.state_classes.manifest\",\n            \"pass\" if manifest_ok else \"fail\",\n            \"Shiki state class manifest\",\n            \"State classes are loaded and required trust classes exist.\" if manifest_ok else \"State class manifest is incomplete.\",\n            \"Run `python3 scripts/validate_shiki.py` and repair .shiki/manifest.json.\" if not manifest_ok else \"\",\n            {\"state_classes\": sorted(state_classes), \"missing_required\": missing_required, \"summary\": summary},\n        )\n    ]\n\n    tracked = subprocess.run([\"git\", \"ls-files\", \"-z\", \"--\", \".shiki\"], cwd=str(target), text=True, capture_output=True, check=False)\n    tracked_paths = [path for path in tracked.stdout.split(\"\\0\") if path] if tracked.returncode == 0 else []\n    unknown = unknown_tracked_shiki_paths(target, manifest, tracked_paths)\n    findings.append(\n        _finding(\n            \"doctor.state_classes.unknown_paths\",\n            \"pass\" if not unknown else \"fail\",\n            \"Unknown tracked .shiki paths\",\n            \"Tracked .shiki paths are represented by the manifest.\" if not unknown else \"Tracked .shiki paths are missing manifest state classes.\",\n            \"Add manifest entries or remove untrusted tracked paths.\" if unknown else \"\",\n            {\"unknown_paths\": unknown},\n        )\n    )\n\n    runtime_classes = {\"workflow-runtime-evidence\", \"cache\", \"local-only\"}\n    runtime_failures = [\n        state_class\n        for state_class in runtime_classes\n        if class_policy(state_class, manifest).get(\"tracked\") is True\n    ]\n    runtime_committed: list[str] = []\n    for relative in manifest_runtime_directories(manifest):\n        result = subprocess.run([\"git\", \"ls-files\", \"--\", relative], cwd=str(target), text=True, capture_output=True, check=False)\n        if result.returncode == 0 and result.stdout.strip():\n            runtime_committed.append(relative)\n    runtime_ok = not runtime_failures and not runtime_committed\n    findings.append(\n        _finding(\n            \"doctor.state_classes.runtime_only\",\n            \"pass\" if runtime_ok else \"fail\",\n            \"Runtime-only state classes\",\n            \"Runtime-only/cache/local-only classes are not committed.\" if runtime_ok else \"Runtime-only state class policy is violated.\",\n            \"Remove committed runtime/cache/local-only files and repair manifest policies.\" if not runtime_ok else \"\",\n            {\"runtime_policy_failures\": runtime_failures, \"runtime_directories_with_files\": runtime_committed},\n        )\n    )\n\n    append_ok = \"append-only-evidence\" in state_classes and \".shiki/ledger\" in summary.get(\"append-only-evidence\", [])\n    findings.append(\n        _finding(\n            \"doctor.state_classes.append_only\",\n            \"pass\" if append_ok else \"fail\",\n            \"Append-only evidence state class\",\n            \".shiki/ledger is classified as append-only-evidence.\" if append_ok else \".shiki/ledger append-only classification is missing.\",\n            \"Repair .shiki/manifest.json state_class for .shiki/ledger.\" if not append_ok else \"\",\n            {\"append_only_paths\": summary.get(\"append-only-evidence\", [])},\n        )\n    )\n    return findings\n\n\ndef _migration_findings(target: Path) -> list[DoctorFinding]:\n    status = migration_status(target)\n    registry_errors = [error for error in status[\"errors\"] if \"registry\" in error or \"dependency\" in error or \"duplicate migration id\" in error]\n    findings = [\n        _finding(\n            \"doctor.migrations.registry\",\n            \"pass\" if not registry_errors else \"fail\",\n            \"Migration registry\",\n            \"Migration registry imports and dependencies are valid.\" if not registry_errors else \"Migration registry validation failed.\",\n\ndiff --git a/.codex/skills/shiki/SKILL.md b/.codex/skills/shiki/SKILL.md\nindex c8e2e85d9d75d49cc4f2d6286ee22659c4e53e75..93cded623b2c15d9f65c76491a1c2d813ac9e6c0\n--- a/.codex/skills/shiki/SKILL.md\n+++ b/.codex/skills/shiki/SKILL.md\n@@ -18,6 +18,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n Codex CLI does not register this skill as a custom `/shiki` slash command. When\n the user types `/shiki` in Codex CLI and gets \"Unrecognized command\", explain\n that this is expected: invoke Shiki in Codex with natural language such as\n@@ -69,7 +72,7 @@\n \n ## Responsibilities\n \n-- Claude Code implements and repairs by default (`shiki runner claude`, ADR 0008); Codex implements and repairs tasks explicitly assigned to `codex`.\n+- Claude Code implements and repairs by default (`shiki runner claude`, SADR-0008); Codex implements and repairs tasks explicitly assigned to `codex`.\n - Claude Code Action can act as GitHub-side CCA or reviewer.\n - CCA judges completion.\n - MergeGate authorizes state transitions and merge readiness.\ndiff --git a/.github/prompts/cca-completion-check.md b/.github/prompts/cca-completion-check.md\nindex ed203ad5f302ecc81372d8ab27d2b1232ca97497..fd89950528333ef6242e6d5443904f5392ab7e56\n--- a/.github/prompts/cca-completion-check.md\n+++ b/.github/prompts/cca-completion-check.md\n@@ -19,6 +19,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n ## Required Reading\n \n Read only the files needed to judge the PR contract. Prefer this order:\ndiff --git a/AGENTS.md b/AGENTS.md\nindex 7f041100d49c2a7525f51cb0f5d696a4a6d1c78c..2b700e989ff295987aa8a4b0c5fcb1acd80020da\n--- a/AGENTS.md\n+++ b/AGENTS.md\n@@ -44,6 +44,10 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use\n+`SADR-NNNN-*.md`; target product decisions retain `NNNN-*.md` and the `ADR`\n+namespace.\n+\n Use the canonical source-of-truth order above. Runtime-specific reading orders\n are operational guidance only and must not override or reorder it.\n \ndiff --git a/CLAUDE.md b/CLAUDE.md\nindex 98a932e7841a632866fcd53b0bad0e3af3711d3d..611d491912d6a72a0327a1731b8f33a41acb4baa\n--- a/CLAUDE.md\n+++ b/CLAUDE.md\n@@ -14,6 +14,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n ## Claude Role\n \n Default to planner, default implementer, reviewer, coordinator, documentation editor, CCA-style completion judge, and final-judgment assistant.\ndiff --git a/SYSTEM_PROMPT.md b/SYSTEM_PROMPT.md\nindex aaaea45e3a896c3a44ba89eec8bb84325ecf6c62..2e7c093ebe9e8bb34e0923756f191b4f7fa581f5\n--- a/SYSTEM_PROMPT.md\n+++ b/SYSTEM_PROMPT.md\n@@ -30,6 +30,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n Use the canonical source-of-truth order above. Runtime-specific reading orders\n are operational guidance only and must not override or reorder it. The current\n conversation is non-durable operator input.\ndiff --git a/docs/adr/0001-keep-shiki-as-platform-name.md b/docs/adr/SADR-0001-keep-shiki-as-platform-name.md\nindex 20a9637c3b9d8c97de7ba8db7cfae4d82d9a7dba..8b685439959aada1b221c112f0ac3c51ca8a1695\n--- a/docs/adr/0001-keep-shiki-as-platform-name.md\n+++ b/docs/adr/SADR-0001-keep-shiki-as-platform-name.md\n@@ -1,3 +1,3 @@\n-# Keep Shiki As The Platform Name\n+# SADR-0001: Keep Shiki As The Platform Name\n \n Shiki remains the product name for the GitHub-native agentic engineering control plane. We considered more descriptive names such as MergePilot, MergeGate, and Kiroku, but kept Shiki because it carries the existing goal-seeking framework identity while remaining independent of Hermes, Codex, Claude Code, or any single runtime.\ndiff --git a/docs/adr/0002-use-agents-as-shared-constitution.md b/docs/adr/SADR-0002-use-agents-as-shared-constitution.md\nindex 73de30ecb078120eac540d9bc51855e3cd48d0e3..c5486182bf06fca36ea508496b013c67302fd989\n--- a/docs/adr/0002-use-agents-as-shared-constitution.md\n+++ b/docs/adr/SADR-0002-use-agents-as-shared-constitution.md\n@@ -1,3 +1,3 @@\n-# Use AGENTS As The Shared Agent Constitution\n+# SADR-0002: Use AGENTS As The Shared Agent Constitution\n \n Shiki uses `AGENTS.md` as the runtime-neutral constitution and `CLAUDE.md` as a thin Claude Code wrapper. This keeps Codex, Claude Code, Hermes Runner, and future Agent Runtimes aligned on the same GitHub-first MergeGate rules while still allowing runtime-specific instructions where needed.\ndiff --git a/docs/adr/0003-require-skill-gate-for-mergegate-readiness.md b/docs/adr/SADR-0003-require-skill-gate-for-mergegate-readiness.md\nindex 424be0c9af4c8f0136f1fde68a468c47ff84e764..7fc337987c7df9792f75f181dfcbed75fe5ffff2\n--- a/docs/adr/0003-require-skill-gate-for-mergegate-readiness.md\n+++ b/docs/adr/SADR-0003-require-skill-gate-for-mergegate-readiness.md\n@@ -1,3 +1,3 @@\n-# Require Skill Gate For MergeGate Readiness\n+# SADR-0003: Require Skill Gate For MergeGate Readiness\n \n Shiki requires relevant engineering skills to be invoked before execution when their triggers apply. This makes skills part of MergeGate readiness rather than optional prompt style, because Shiki depends on repeatable planning, issue decomposition, TDD, diagnosis, triage, and architecture review before autonomous or parallel agent work can be trusted.\ndiff --git a/docs/adr/0004-use-github-first-with-shiki-mirror.md b/docs/adr/SADR-0004-use-github-first-with-shiki-mirror.md\nindex d0a3a9174dc646a945a9d325954ffaf00c352040..a79c54ae9ee8eed2a0524192b864da4e22a92d3f\n--- a/docs/adr/0004-use-github-first-with-shiki-mirror.md\n+++ b/docs/adr/SADR-0004-use-github-first-with-shiki-mirror.md\n@@ -1,4 +1,4 @@\n-# ADR 0004: Use GitHub First With A Repository-Local Shiki Mirror\n+# SADR-0004: Use GitHub First With A Repository-Local Shiki Mirror\n \n ## Status\n \ndiff --git a/docs/adr/0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md b/docs/adr/SADR-0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\nindex b339baf49b89d9290e79c3d51b36e7b2bde3bd95..c30a0d8b1fde0224d0ac7820e7870dccf721580d\n--- a/docs/adr/0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\n+++ b/docs/adr/SADR-0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\n@@ -1,4 +1,4 @@\n-# ADR 0005: Use Codex As Front And Claude Code Action As The GitHub Runtime\n+# SADR-0005: Use Codex As Front And Claude Code Action As The GitHub Runtime\n \n ## Status\n \ndiff --git a/docs/adr/0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md b/docs/adr/SADR-0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\nindex 4bcae9d1e52f0a37b8e9fbafa5cd14b6ee1993bd..7a38538a6ae9929567f0ade67e7ea6a60d0b3f7e\n--- a/docs/adr/0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\n+++ b/docs/adr/SADR-0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\n@@ -1,4 +1,4 @@\n-# ADR 0006: Separate Codex Implementation, CCA Completion, And MergeGate Authorization\n+# SADR-0006: Separate Codex Implementation, CCA Completion, And MergeGate Authorization\n \n ## Status\n \ndiff --git a/docs/adr/0007-packaging-and-release.md b/docs/adr/SADR-0007-packaging-and-release.md\nindex 79b84ddb97167372b44641e5b7860e400ce80822..023cd57f8011aa6e6d2ebf612b7c35a7a82c00d4\n--- a/docs/adr/0007-packaging-and-release.md\n+++ b/docs/adr/SADR-0007-packaging-and-release.md\n@@ -1,4 +1,4 @@\n-# ADR 0007: Packaging And Release For Shiki\n+# SADR-0007: Packaging And Release For Shiki\n \n ## Status\n \ndiff --git a/docs/adr/0008-use-claude-code-as-default-implementer.md b/docs/adr/SADR-0008-use-claude-code-as-default-implementer.md\nindex dedf4510b7c7799521422f622029563d373db031..a158a8d444d93764ec515a28133f7dde4af23579\n--- a/docs/adr/0008-use-claude-code-as-default-implementer.md\n+++ b/docs/adr/SADR-0008-use-claude-code-as-default-implementer.md\n@@ -1,4 +1,4 @@\n-# ADR 0008: Use Claude Code As The Default Implementer Runtime\n+# SADR-0008: Use Claude Code As The Default Implementer Runtime\n \n ## Status\n \ndiff --git a/docs/adr/0009-requirements-definition-and-spec-freeze.md b/docs/adr/SADR-0009-requirements-definition-and-spec-freeze.md\nindex 29b3f7871bef9203a8b4a831a65c39eb2f506e42..52e7dd69ad0aa4efe0b68c62678abbf7df02aaec\n--- a/docs/adr/0009-requirements-definition-and-spec-freeze.md\n+++ b/docs/adr/SADR-0009-requirements-definition-and-spec-freeze.md\n@@ -1,4 +1,4 @@\n-# ADR 0009: Merge Grill And Planning Into Requirements Definition With Spec Freeze\n+# SADR-0009: Merge Grill And Planning Into Requirements Definition With Spec Freeze\n \n ## Status\n \ndiff --git a/docs/adr/0010-external-ai-guardian-review.md b/docs/adr/SADR-0010-external-ai-guardian-review.md\nindex e6108bbe8bb4e73e0ff4684c8f78001a80438c4e..e690d4eddd270e683d709c9b679c5859e71eeaaa\n--- a/docs/adr/0010-external-ai-guardian-review.md\n+++ b/docs/adr/SADR-0010-external-ai-guardian-review.md\n@@ -1,4 +1,4 @@\n-# ADR 0010: External AI Guardian Review As A First-Class Approval Authority\n+# SADR-0010: External AI Guardian Review As A First-Class Approval Authority\n \n ## Status\n \ndiff --git a/docs/adr/0011-autonomous-quality-gate-evidence.md b/docs/adr/SADR-0011-autonomous-quality-gate-evidence.md\nindex bca0246c5d46e44afbde15486a75eec425fc02f5..9c93eac16d8c3a6d4b11bd7224e3955102cd7db4\n--- a/docs/adr/0011-autonomous-quality-gate-evidence.md\n+++ b/docs/adr/SADR-0011-autonomous-quality-gate-evidence.md\n@@ -1,4 +1,4 @@\n-# ADR 0011: Autonomous Quality-Gate Evidence \u2014 Deterministic Facts And An Independent Verifier\n+# SADR-0011: Autonomous Quality-Gate Evidence \u2014 Deterministic Facts And An Independent Verifier\n \n ## Status\n \ndiff --git a/docs/adr/0012-autonomous-loop-closeout-to-main.md b/docs/adr/SADR-0012-autonomous-loop-closeout-to-main.md\nindex 637fad6dde4989c9c2bf0f81f676d8d29991123c..ec7d85f81030a676bcbba28b43eb365422a36eac\n--- a/docs/adr/0012-autonomous-loop-closeout-to-main.md\n+++ b/docs/adr/SADR-0012-autonomous-loop-closeout-to-main.md\n@@ -1,4 +1,4 @@\n-# 0012 \u2014 Autonomous loop pushes goal completion to main via a closeout PR\n+# SADR-0012 \u2014 Autonomous loop pushes goal completion to main via a closeout PR\n \n - Status: accepted\n - Date: 2026-06-18\ndiff --git a/docs/adr/0013-bootstrap-grants-actions-approve-pull-requests.md b/docs/adr/SADR-0013-bootstrap-grants-actions-approve-pull-requests.md\nindex b6c69d64f1c5ced346b88d6859586715b3b5bd0e..a793e7277d67ab8ea56286fec289ca689825a17d\n--- a/docs/adr/0013-bootstrap-grants-actions-approve-pull-requests.md\n+++ b/docs/adr/SADR-0013-bootstrap-grants-actions-approve-pull-requests.md\n@@ -1,4 +1,4 @@\n-# ADR 0013: Bootstrap Grants GitHub Actions Permission To Create And Approve Pull Requests\n+# SADR-0013: Bootstrap Grants GitHub Actions Permission To Create And Approve Pull Requests\n \n ## Status\n \ndiff --git a/docs/adr/0014-codex-app-external-ai-guardian-ui-adapter.md b/docs/adr/SADR-0014-codex-app-external-ai-guardian-ui-adapter.md\nindex 238db06279334caa341c117362df9271382a7fcd..65f12e9939321e1b3e389a34c822fc1549e5155d\n--- a/docs/adr/0014-codex-app-external-ai-guardian-ui-adapter.md\n+++ b/docs/adr/SADR-0014-codex-app-external-ai-guardian-ui-adapter.md\n@@ -1,4 +1,4 @@\n-# ADR 0014: Use Codex App As The External AI Guardian UI Adapter\n+# SADR-0014: Use Codex App As The External AI Guardian UI Adapter\n \n ## Status\n \ndiff --git a/docs/adr/0015-contract-approval-before-dispatch.md b/docs/adr/SADR-0015-contract-approval-before-dispatch.md\nindex fad6c656741a20a7684ba16080a7db159f63a52f..7bdd760bb3e82eee4e1410f45c783b03b638d456\n--- a/docs/adr/0015-contract-approval-before-dispatch.md\n+++ b/docs/adr/SADR-0015-contract-approval-before-dispatch.md\n@@ -1,4 +1,4 @@\n-# ADR 0015: Approve The Task Contract Before Dispatch, Not The Diff After It\n+# SADR-0015: Approve The Task Contract Before Dispatch, Not The Diff After It\n \n ## Status\n \ndiff --git a/docs/adr/0016-locks-bound-product-paths.md b/docs/adr/SADR-0016-locks-bound-product-paths.md\nindex b5e850f3f1f7f4ef6e7f9d1763a29a51bd300c45..332cc17313498404f41a145291e38447b5de0dc4\n--- a/docs/adr/0016-locks-bound-product-paths.md\n+++ b/docs/adr/SADR-0016-locks-bound-product-paths.md\n@@ -1,4 +1,4 @@\n-# ADR 0016: Locks bound product paths; the Shiki mirror is bound by record identity\n+# SADR-0016: Locks bound product paths; the Shiki mirror is bound by record identity\n \n ## Status\n \ndiff --git a/docs/adr/0017-bookkeeping-closeouts-do-not-inherit-risk.md b/docs/adr/SADR-0017-bookkeeping-closeouts-do-not-inherit-risk.md\nindex 554e3a0d526a72ca067108c5464cc68c3edcf174..83115f69f1b1b48d544c9b0157f377b91cb53abf\n--- a/docs/adr/0017-bookkeeping-closeouts-do-not-inherit-risk.md\n+++ b/docs/adr/SADR-0017-bookkeeping-closeouts-do-not-inherit-risk.md\n@@ -1,4 +1,4 @@\n-# ADR 0017: A Bookkeeping Closeout Does Not Inherit Its Task's Risk\n+# SADR-0017: A Bookkeeping Closeout Does Not Inherit Its Task's Risk\n \n ## Status\n \ndiff --git a/docs/adr/0018-carried-guardian-approval-across-a-proven-base-sync.md b/docs/adr/SADR-0018-carried-guardian-approval-across-a-proven-base-sync.md\nindex 22358562da390ed8b45cd600da08a43d378b4680..38da0fd95102c3496c3219ce2319fce696a2532a\n--- a/docs/adr/0018-carried-guardian-approval-across-a-proven-base-sync.md\n+++ b/docs/adr/SADR-0018-carried-guardian-approval-across-a-proven-base-sync.md\n@@ -1,4 +1,4 @@\n-# ADR 0018: Carried Guardian Approval Across A Proven Base Sync\n+# SADR-0018: Carried Guardian Approval Across A Proven Base Sync\n \n ## Status\n \ndiff --git a/docs/adr/README.md b/docs/adr/README.md\nindex c70e057a4520c36e042ab68c53cd4a21ef709b71..5da43856836e2ce6a150f8127766e2f88355ab4b\n--- a/docs/adr/README.md\n+++ b/docs/adr/README.md\n@@ -1,40 +1,41 @@\n-# Architecture Decision Records\n+# Decision Record Namespaces\n \n-This directory holds Shiki's Architecture Decision Records (ADRs): durable\n-records of hard-to-reverse platform decisions. ADRs are part of the Shiki source\n-of truth alongside GitHub state, the `.shiki/` mirror, and `CONTEXT.md`.\n+This directory holds two independent decision namespaces:\n \n-## How ADRs Work\n+- Shiki Architecture Decision Records (SADRs), named\n+  `SADR-NNNN-<short-slug>.md`, record hard-to-reverse Shiki platform decisions.\n+- Target Architecture Decision Records (ADRs), named\n+  `NNNN-<short-slug>.md`, belong to the target product.\n \n-- New ADRs are added as `docs/adr/NNNN-<short-slug>.md`, where `NNNN` is the next\n-  zero-padded sequence number.\n-- The **canonical list of ADRs is the set of files matching `docs/adr/*.md`** in\n-  this directory. Browse `docs/adr/*.md` for the authoritative, current set.\n-- Use [`template.md`](template.md) as the starting point for a new ADR.\n-- Record an ADR when a decision is hard to reverse, surprising without context,\n-  and a real tradeoff. Do not write an ADR for routine or easily reversible\n-  choices.\n+The same four-digit number may appear once in each namespace. A platform\n+`SADR-NNNN` citation resolves only to `SADR-NNNN-*.md`; a target `ADR NNNN`\n+citation resolves only to `NNNN-*.md`.\n \n-Using the `docs/adr/*.md` glob as the source of truth means other PRs can add an\n-ADR without editing this index file, avoiding merge contention on a\n-hand-maintained enumeration.\n+## How Decision Records Work\n \n-## Current ADRs (snapshot)\n-\n-The list below is a convenience snapshot and may lag behind the directory. When\n-in doubt, list `docs/adr/*.md`.\n+- New Shiki platform decisions use the next zero-padded SADR number.\n+- New target product decisions use the target's own ADR sequence.\n+- The canonical list is the files in `docs/adr/`; no hand-maintained index is\n+  authoritative.\n+- Use [`template.md`](template.md) as the starting point and choose the filename\n+  namespace that owns the decision.\n+- Record a decision when it is hard to reverse, surprising without context, and\n+  a real tradeoff. Do not write a decision record for routine or easily\n+  reversible choices.\n \n-- [0001 \u2014 Keep Shiki As The Platform Name](0001-keep-shiki-as-platform-name.md)\n-- [0002 \u2014 Use AGENTS As The Shared Agent Constitution](0002-use-agents-as-shared-constitution.md)\n-- [0003 \u2014 Require Skill Gate For MergeGate Readiness](0003-require-skill-gate-for-mergegate-readiness.md)\n-- [0004 \u2014 Use GitHub First With A Repository-Local Shiki Mirror](0004-use-github-first-with-shiki-mirror.md)\n-- [0005 \u2014 Use Codex As Front And Claude Code Action As The GitHub Runtime](0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md)\n-- [0006 \u2014 Separate Codex Implementation, CCA Completion, And MergeGate Authorization](0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md)\n+Using the directory as the source of truth lets independent changes add records\n+without contending on an enumerated index.\n \n-## Adding A New ADR\n+## Adding A Shiki SADR\n \n-1. Copy `template.md` to `docs/adr/NNNN-<short-slug>.md`.\n+1. Copy `template.md` to `docs/adr/SADR-NNNN-<short-slug>.md`.\n 2. Fill in the title, status, context, decision, and consequences.\n-3. Set the status to `Proposed`, then `Accepted` once the decision is approved.\n-4. Open a PR following `CONTRIBUTING.md`. You do not need to edit this index for\n-   the ADR to be discoverable via `docs/adr/*.md`.\n+3. Set the status to `Proposed`, then `Accepted` once the proper authority has\n+   approved the decision.\n+4. Open a PR following `CONTRIBUTING.md`.\n+\n+## Adding A Target ADR\n+\n+1. Copy `template.md` to `docs/adr/NNNN-<short-slug>.md` using the target's own\n+   sequence.\n+2. Cite it as `ADR NNNN`; do not prefix its filename with `SADR-`.\ndiff --git a/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md b/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\nnew file mode 100644\nindex 0000000000000000000000000000000000000000..7455ce1f3bed02ddddb851ac21a1d1a648c9aeba\n--- /dev/null\n+++ b/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\n@@ -0,0 +1,55 @@\n+# SADR-0019: Separate Shiki Platform SADRs From Target ADRs\n+\n+## Status\n+\n+Accepted\n+\n+## Context\n+\n+Shiki installs its platform decision records into `docs/adr/` in every target\n+repository. Shiki and its targets previously used the same numeric\n+`NNNN-<slug>.md` namespace. A target that already owned ADRs 0013 through 0018\n+therefore acquired two records for each number during installation, making an\n+`ADR NNNN` citation ambiguous. Renumbering target decisions would rewrite target\n+history, while renumbering Shiki decisions per target would destroy their stable\n+platform identity.\n+\n+Upgrades also need to remove the former Shiki-owned numeric paths without ever\n+mistaking a target-owned ADR for platform content. The install stamp already\n+records exact shipped paths and content digests, so it can prove ownership only\n+when both still match.\n+\n+## Decision\n+\n+Shiki platform decisions use the `SADR` namespace:\n+\n+- records are named `docs/adr/SADR-NNNN-<slug>.md`;\n+- live platform citations use `SADR-NNNN`;\n+- target product decisions retain `docs/adr/NNNN-<slug>.md` and `ADR NNNN`;\n+- the reference validator resolves the two namespaces independently, so the same\n+  four-digit number may exist once in each namespace.\n+\n+Before a forced upgrade writes anything, the installer inventories the exact\n+former numeric Shiki ADR paths. It may delete a legacy path only when the\n+existing install stamp contains that exact path and its recorded SHA-256 digest\n+matches the file currently on disk. All ownership blockers are reported\n+together and abort the upgrade before its first write. A non-forced install\n+never performs this cleanup.\n+\n+Historical `.shiki/` evidence remains unchanged because it records the namespace\n+that existed when that evidence was produced.\n+\n+## Consequences\n+\n+- Targets may use any ADR number without colliding with Shiki's platform SADRs.\n+- Fresh installs preserve target ADR bytes and install a stable SADR set.\n+- Forced upgrades fail closed when legacy ownership cannot be proven, and a\n+  successful upgrade refreshes the stamp without removed legacy paths.\n+- Current governance, packaging, workflow, prompt, script, and skill references\n+  must move with the platform records.\n+\n+## Alternatives Considered\n+\n+- Collision-only refusal leaves affected targets permanently unupgradable.\n+- Renumbering target ADRs mutates target-owned history.\n+- Per-target Shiki renumbering makes platform citations non-portable.\ndiff --git a/docs/adr/template.md b/docs/adr/template.md\nindex a92a97daddd0b00ab9dd626cc8d86942da2eafed..17dd79175b5fee2105935557f691b40cf9492942\n--- a/docs/adr/template.md\n+++ b/docs/adr/template.md\n@@ -1,14 +1,14 @@\n-# NNNN: <Short Decision Title>\n+# <SADR-NNNN or NNNN>: <Short Decision Title>\n \n ## Status\n \n-Proposed | Accepted | Superseded by ADR-NNNN\n+Proposed | Accepted | Superseded by SADR-NNNN or target ADR NNNN\n \n ## Context\n \n What is the situation, constraint, or force that requires a decision? Reference\n-relevant domain terms (`CONTEXT.md`), prior ADRs, and code reality. State the\n-problem, not the solution.\n+relevant domain terms (`CONTEXT.md`), prior Shiki SADRs or target ADRs, and code\n+reality. State the problem, not the solution.\n \n ## Decision\n \ndiff --git a/docs/agents/domain.md b/docs/agents/domain.md\nindex 258da8ea81e87ac0a6b0b5b5e5c2fde980eaabb7..67aaa6f4b7d0a5aa4d495ce6279073333f7798cb\n--- a/docs/agents/domain.md\n+++ b/docs/agents/domain.md\n@@ -5,7 +5,9 @@\n ## Layout\n \n - `CONTEXT.md` defines Shiki language. Keep it implementation-free.\n-- `docs/adr/` records hard-to-reverse decisions.\n+- `docs/adr/SADR-NNNN-*.md` records Shiki platform decisions.\n+- `docs/adr/NNNN-*.md` records target product decisions without sharing the\n+  SADR namespace.\n - `AGENTS.md` is the runtime-neutral agent constitution.\n - `CLAUDE.md` is the Claude Code wrapper around `AGENTS.md`.\n - `.shiki/` is the Target Repository mirror for Goals, PRDs, plans, Task DAGs, contracts, locks, worktree records, ledger entries, CCA verdicts, repair packets, reports, and handoffs.\ndiff --git a/docs/agents/implementation-policy.md b/docs/agents/implementation-policy.md\nindex c754144a9fb5bf4a38aa82a60e1670c59e626b44..e90d40fe21880b9a09719c5ecfe6bde7284eb3ce\n--- a/docs/agents/implementation-policy.md\n+++ b/docs/agents/implementation-policy.md\n@@ -31,6 +31,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n ## Non-Negotiables\n \n 1. **Planning precedes implementation.** Non-trivial Goals start with `grill-with-docs` and Context & Impact.\ndiff --git a/scripts/shiki_installer.py b/scripts/shiki_installer.py\nindex be064ab7a29ff6836f7f7a1d70500a43e0d11d7b..d3e3247edbf3e4be287a4667176d86f6500f8dc5\n--- a/scripts/shiki_installer.py\n+++ b/scripts/shiki_installer.py\n@@ -118,6 +118,7 @@\n # what lets a target carry it without a governance-schema change.\n INSTALL_STAMP_PATH = \".shiki/install-stamp.json\"\n INSTALL_STAMP_VERSION = 1\n+LEGACY_SHIKI_SADR_MAX = 18\n \n # Under --force the shipped surface splits three ways, so nothing is lost and\n # nothing is silently stale.\n@@ -237,9 +238,98 @@\n         )\n \n \n+def legacy_shiki_adr_paths() -> tuple[str, ...]:\n+    \"\"\"Former numeric paths for Shiki SADR-0001 through SADR-0018.\n+\n+    The authoritative slugs come from the current SADR filenames. SADR-0019 is\n+    the namespace decision itself and never shipped under a numeric legacy path.\n+    \"\"\"\n+    paths: list[str] = []\n+    for sadr in sorted((ROOT / \"docs\" / \"adr\").glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\")):\n+        number = sadr.name.split(\"-\", 2)[1]\n+        if int(number) <= LEGACY_SHIKI_SADR_MAX:\n+            paths.append(f\"docs/adr/{sadr.name.removeprefix('SADR-')}\")\n+    return tuple(paths)\n+\n+\n+def preflight_legacy_sadr_cleanup(target: Path, *, force: bool) -> tuple[str, ...]:\n+    \"\"\"Authorize exact legacy Shiki ADR removals before the first write.\n+\n+    A non-force install never removes existing files. A forced install needs no\n+    stamp when no legacy Shiki path is present. When legacy paths do exist, each\n+    must be named in a readable install stamp and its current bytes must match\n+    the recorded digest.\n+    \"\"\"\n+    if not force:\n+        return ()\n+    present = tuple(\n+        relative\n+        for relative in legacy_shiki_adr_paths()\n+        if (target / relative).exists() or (target / relative).is_symlink()\n+    )\n+    if not present:\n+        return ()\n+\n+    stamp_path = target / INSTALL_STAMP_PATH\n+    try:\n+        raw = stamp_path.read_text(encoding=\"utf-8\")\n+        stamp = json.loads(raw)\n+    except FileNotFoundError:\n+        detail = \"install stamp is absent\"\n+        blockers = [f\"{relative}: {detail}\" for relative in present]\n+    except (OSError, UnicodeDecodeError) as error:\n+        detail = f\"install stamp is unreadable ({error})\"\n+        blockers = [f\"{relative}: {detail}\" for relative in present]\n+    except json.JSONDecodeError as error:\n+        detail = f\"install stamp is malformed ({error})\"\n+        blockers = [f\"{relative}: {detail}\" for relative in present]\n+    else:\n+        if not isinstance(stamp, dict):\n+            blockers = [\n+                f\"{relative}: install stamp is malformed (expected an object)\"\n+                for relative in present\n+            ]\n+        else:\n+            digests = stamp.get(\"digests\")\n+            blockers = []\n+            if not isinstance(digests, dict):\n+                blockers.extend(\n+                    f\"{relative}: install stamp has no digest for this path\"\n+                    for relative in present\n+                )\n+            else:\n+                for relative in present:\n+                    expected = digests.get(relative)\n+                    if not isinstance(expected, str) or not expected:\n+                        blockers.append(\n+                            f\"{relative}: install stamp has no digest for this path\"\n+                        )\n+                        continue\n+                    try:\n+                        actual = _sha256_file(target / relative)\n+                    except OSError as error:\n+                        blockers.append(\n+                            f\"{relative}: legacy path is unreadable ({error})\"\n+                        )\n+                        continue\n+                    if actual != expected:\n+                        blockers.append(\n+                            f\"{relative}: digest mismatch (stamp {expected}, current {actual})\"\n+                        )\n+\n+    if blockers:\n+        joined = \"\\n\".join(f\"- {blocker}\" for blocker in blockers)\n+        raise ShikiError(\n+            \"legacy Shiki ADR cleanup is not authorized:\\n\"\n+            f\"{joined}\\nNo files were written.\"\n+        )\n+    return present\n+\n+\n def install_template(target: Path, *, force: bool, validate: bool) -> list[NewFileNote]:\n     # Preconditions first: never rewrite the tree of a target that cannot be\n     # upgraded cleanly (see refuse_pending_migrations).\n+    legacy_cleanup = preflight_legacy_sadr_cleanup(target, force=force)\n     refuse_pending_migrations(target)\n \n     # Resolve the target's required CODEOWNERS owner once from its\n@@ -250,6 +340,10 @@\n     # verbatim. See ``codeowners_required_owner``.\n     codeowners_owner = codeowners_required_owner(target)\n \n+    for relative in legacy_cleanup:\n+        (target / relative).unlink()\n+        info(f\"removed stamped legacy Shiki ADR: {target / relative}\")\n+\n     notes: list[NewFileNote] = []\n     for relative in TEMPLATE_PATHS:\n         source = ROOT / relative\ndiff --git a/tests/test_adr_references.py b/tests/test_adr_references.py\nindex 51706329c797d5dd9b47e082a333fa83b261d482..91ab76b724cb973435c45d53cab63793942267e9\n--- a/tests/test_adr_references.py\n+++ b/tests/test_adr_references.py\n@@ -1,4 +1,4 @@\n-\"\"\"Every ``ADR ####`` citation must resolve to exactly one decision record.\n+\"\"\"Every target ADR and Shiki SADR citation resolves in its own namespace.\n \n Background. ``scripts/mergegate_check.py`` justifies ``is_bookkeeping_closeout``\n with \"ADR 0017\" nine times and ``scripts/guardian_approval_signal.py`` cites\n@@ -11,11 +11,12 @@\n and the directory drifted with no signal.\n \n This is the same unbound-pair shape behind other silent failures: a reference\n-and its target with nothing asserting they agree. This test binds them. It scans\n-every ``scripts/*.py`` (plus ``docs/**/*.md`` and the root ``*.md`` governance\n-files, which are cheap) for the ``ADR ####`` pattern and asserts each cited\n-number resolves to exactly one ``docs/adr/####-*.md``. The converse is\n-deliberately NOT required: an ADR may exist without being cited.\n+and its target with nothing asserting they agree. This test binds them. Target\n+``ADR NNNN`` citations resolve only to ``docs/adr/NNNN-*.md``; Shiki\n+``SADR-NNNN`` citations resolve only to ``docs/adr/SADR-NNNN-*.md``. The scan\n+covers current governance docs, scripts, workflows, prompts, packaging metadata,\n+and repo-local Codex guidance while deliberately excluding historical\n+``.shiki`` evidence. The converse is not required: a record may be uncited.\n \n The suite is dependency free: standard-library ``unittest`` only.\n \"\"\"\n@@ -31,17 +32,18 @@\n \n REPO_ROOT = Path(__file__).resolve().parents[1]\n \n-# \"ADR 0016\" -- a four-digit citation. The trailing \\b keeps a five-digit token\n-# from being read as a truncated four-digit number.\n-_CITATION = re.compile(r\"\\bADR (\\d{4})\\b\")\n-# \"0016-locks-bound-product-paths.md\" -- the leading four digits name the record.\n-_ADR_FILENAME = re.compile(r\"^(\\d{4})-.*\\.md$\")\n+# ``ADR 0016`` / ``SADR-0016`` -- a namespace plus four-digit citation. The\n+# trailing ``\\b`` keeps a five-digit token from being read as a truncated one.\n+_CITATION = re.compile(r\"\\b(S?ADR)[ -](\\d{4})\\b\")\n+# Target ADRs retain the numeric filename; Shiki platform decisions use SADR.\n+_DECISION_FILENAME = re.compile(r\"^(?:(SADR)-)?(\\d{4})-.*\\.md$\")\n \n \n class Violation(NamedTuple):\n     \"\"\"A citation that does not resolve to exactly one decision record.\"\"\"\n \n     citing_file: str  # repo-relative path of the file that cites the ADR\n+    namespace: str  # \"ADR\" for target decisions; \"SADR\" for Shiki decisions\n     number: str  # the cited four-digit number, e.g. \"0016\"\n     line: int  # 1-based line of the citation\n     reason: str  # \"missing\" (zero matches) or \"ambiguous\" (two or more)\n@@ -49,44 +51,59 @@\n \n     @property\n     def expected_path(self) -> str:\n-        return f\"docs/adr/{self.number}-*.md\"\n+        prefix = \"SADR-\" if self.namespace == \"SADR\" else \"\"\n+        return f\"docs/adr/{prefix}{self.number}-*.md\"\n \n     def describe(self) -> str:\n         \"\"\"Name the citing file, the cited number, and the missing path.\"\"\"\n         if self.reason == \"missing\":\n             return (\n-                f\"{self.citing_file}:{self.line} cites ADR {self.number} \"\n+                f\"{self.citing_file}:{self.line} cites {self.namespace} {self.number} \"\n                 f\"but no document matches {self.expected_path}\"\n             )\n         joined = \", \".join(self.matches)\n         return (\n-            f\"{self.citing_file}:{self.line} cites ADR {self.number} which \"\n+            f\"{self.citing_file}:{self.line} cites {self.namespace} {self.number} which \"\n             f\"resolves to {len(self.matches)} documents (expected exactly one \"\n             f\"at {self.expected_path}): {joined}\"\n         )\n \n \n-def _adr_index(root: Path) -> dict[str, list[Path]]:\n-    \"\"\"Map every four-digit ADR number to the record files that claim it.\"\"\"\n-    index: dict[str, list[Path]] = {}\n+def _adr_index(root: Path) -> dict[tuple[str, str], list[Path]]:\n+    \"\"\"Map each decision namespace and number to the files that claim it.\"\"\"\n+    index: dict[tuple[str, str], list[Path]] = {}\n     adr_dir = root / \"docs\" / \"adr\"\n     if adr_dir.is_dir():\n         for path in sorted(adr_dir.glob(\"*.md\")):\n-            match = _ADR_FILENAME.match(path.name)\n+            match = _DECISION_FILENAME.match(path.name)\n             if match:\n-                index.setdefault(match.group(1), []).append(path)\n+                namespace = \"SADR\" if match.group(1) else \"ADR\"\n+                index.setdefault((namespace, match.group(2)), []).append(path)\n     return index\n \n \n def _citing_files(root: Path, include_docs: bool = True) -> list[Path]:\n-    \"\"\"Files scanned for citations: scripts/*.py, and cheaply the markdown.\"\"\"\n+    \"\"\"Current, shipped text surfaces scanned for decision citations.\"\"\"\n     files = sorted((root / \"scripts\").glob(\"*.py\"))\n+    files += sorted((root / \"scripts\").glob(\"*.sh\"))\n     if include_docs:\n         docs = root / \"docs\"\n         if docs.is_dir():\n             files += sorted(docs.rglob(\"*.md\"))\n         files += sorted(root.glob(\"*.md\"))\n-    return files\n+        pyproject = root / \"pyproject.toml\"\n+        if pyproject.is_file():\n+            files.append(pyproject)\n+        for relative in (\".github\", \".codex\"):\n+            surface = root / relative\n+            if surface.is_dir():\n+                files += sorted(\n+                    path\n+                    for path in surface.rglob(\"*\")\n+                    if path.is_file()\n+                    and path.suffix in {\".md\", \".py\", \".sh\", \".toml\", \".yaml\", \".yml\"}\n+                )\n+    return sorted(set(files))\n \n \n def find_adr_reference_violations(\n@@ -94,10 +111,9 @@\n ) -> list[Violation]:\n     \"\"\"Return every ``ADR ####`` citation under ``root`` that fails to resolve.\n \n-    A citation resolves when exactly one ``docs/adr/####-*.md`` matches its\n-    number. Zero matches is a \"missing\" violation; two or more is \"ambiguous\".\n-    Records that are never cited produce nothing -- the converse is not\n-    required.\n+    A citation resolves when exactly one record in the same namespace matches\n+    its number. Zero matches is a \"missing\" violation; two or more is\n+    \"ambiguous\". Records that are never cited produce nothing.\n     \"\"\"\n     root = Path(root)\n     index = _adr_index(root)\n@@ -110,13 +126,15 @@\n         rel = path.relative_to(root).as_posix()\n         for lineno, line in enumerate(text.splitlines(), start=1):\n             for match in _CITATION.finditer(line):\n-                number = match.group(1)\n-                found = index.get(number, [])\n+                namespace, number = match.groups()\n+                found = index.get((namespace, number), [])\n                 if len(found) == 1:\n                     continue\n                 reason = \"missing\" if not found else \"ambiguous\"\n                 rels = [p.relative_to(root).as_posix() for p in found]\n-                violations.append(Violation(rel, number, lineno, reason, rels))\n+                violations.append(\n+                    Violation(rel, namespace, number, lineno, reason, rels)\n+                )\n     return violations\n \n \n@@ -129,8 +147,8 @@\n         self.assertEqual(\n             violations,\n             [],\n-            \"Every 'ADR ####' citation must resolve to exactly one \"\n-            f\"docs/adr/####-*.md:\\n{detail}\",\n+            \"Every ADR/SADR citation must resolve exactly once in its own \"\n+            f\"docs/adr namespace:\\n{detail}\",\n         )\n \n     def test_scan_actually_reads_scripts(self) -> None:\n@@ -146,8 +164,21 @@\n         # The two records this Goal carried onto the default branch, and which\n         # the scripts cite most, must be present.\n         index = _adr_index(REPO_ROOT)\n-        self.assertIn(\"0016\", index)\n-        self.assertIn(\"0017\", index)\n+        self.assertIn((\"SADR\", \"0016\"), index)\n+        self.assertIn((\"SADR\", \"0017\"), index)\n+\n+    def test_platform_tree_has_only_sadr_0001_through_0019(self) -> None:\n+        adr_dir = REPO_ROOT / \"docs\" / \"adr\"\n+        sadrs = sorted(adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\"))\n+        self.assertEqual(\n+            {path.name.split(\"-\", 2)[1] for path in sadrs},\n+            {f\"{number:04d}\" for number in range(1, 20)},\n+        )\n+        self.assertEqual(\n+            list(adr_dir.glob(\"[0-9][0-9][0-9][0-9]-*.md\")),\n+            [],\n+            \"the Shiki platform tree must not retain legacy numeric SADR paths\",\n+        )\n \n \n class FixtureAdrReferences(unittest.TestCase):\n@@ -180,6 +211,26 @@\n         self.assertIn(\"9991\", message)  # cited number\n         self.assertIn(\"docs/adr/9991-*.md\", message)  # missing path\n \n+    def test_target_adr_does_not_satisfy_missing_platform_sadr(self) -> None:\n+        root = self._make_repo(\n+            adr_names=[\"0042-target-decision.md\"],\n+            script_body='\"\"\"Target ADR 0042 exists, but platform SADR-0042 does not.\"\"\"\\n',\n+        )\n+        violations = find_adr_reference_violations(root, include_docs=False)\n+        self.assertEqual(len(violations), 1)\n+        self.assertEqual(violations[0].namespace, \"SADR\")\n+        self.assertEqual(violations[0].number, \"0042\")\n+        self.assertEqual(violations[0].expected_path, \"docs/adr/SADR-0042-*.md\")\n+\n+    def test_target_adr_and_platform_sadr_may_reuse_a_number(self) -> None:\n+        root = self._make_repo(\n+            adr_names=[\"0042-target-decision.md\", \"SADR-0042-platform-decision.md\"],\n+            script_body='\"\"\"Target ADR 0042 and platform SADR-0042 are distinct.\"\"\"\\n',\n+        )\n+        self.assertEqual(\n+            find_adr_reference_violations(root, include_docs=False), []\n+        )\n+\n     def test_uncited_adr_is_allowed(self) -> None:\n         # An ADR exists but nothing cites it -> not a violation. The converse\n         # (every ADR must be cited) is explicitly NOT required.\ndiff --git a/tests/test_sadr_install_migration.py b/tests/test_sadr_install_migration.py\nnew file mode 100644\nindex 0000000000000000000000000000000000000000..e1c1f87329132324dcb6bcdea757669752ad582b\n--- /dev/null\n+++ b/tests/test_sadr_install_migration.py\n@@ -0,0 +1,134 @@\n+\"\"\"SADR installation and collision-safe legacy upgrade behavior.\"\"\"\n+\n+from __future__ import annotations\n+\n+import contextlib\n+import hashlib\n+import io\n+import json\n+import shutil\n+import tempfile\n+import unittest\n+from pathlib import Path\n+\n+import shiki_test_support  # noqa: F401  (path bootstrap)\n+\n+import shiki_installer\n+from tests.test_adr_references import find_adr_reference_violations\n+\n+\n+PRODUCT_NUMBERS = tuple(range(13, 19))\n+\n+\n+def _quiet_install(target: Path, *, force: bool, validate: bool) -> None:\n+    with contextlib.redirect_stdout(io.StringIO()):\n+        shiki_installer.install_template(target, force=force, validate=validate)\n+\n+\n+def _sha256(path: Path) -> str:\n+    return hashlib.sha256(path.read_bytes()).hexdigest()\n+\n+\n+def _legacy_sadr_paths() -> tuple[str, ...]:\n+    return tuple(\n+        f\"docs/adr/{path.name.removeprefix('SADR-')}\"\n+        for path in sorted((shiki_installer.ROOT / \"docs\" / \"adr\").glob(\"SADR-*.md\"))\n+        if path.name != \"SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\"\n+    )\n+\n+\n+class FreshSadrInstallTests(unittest.TestCase):\n+    def setUp(self) -> None:\n+        self.tmp = Path(tempfile.mkdtemp(prefix=\"shiki-sadr-fresh-\"))\n+        self.addCleanup(lambda: shutil.rmtree(self.tmp, ignore_errors=True))\n+        self.target = self.tmp / \"target\"\n+        self.adr_dir = self.target / \"docs\" / \"adr\"\n+        self.adr_dir.mkdir(parents=True)\n+        self.product_bytes = {\n+            f\"{number:04d}-product-decision.md\": (\n+                f\"# Product ADR {number:04d}\\n\\nTarget-owned bytes {number}.\\n\"\n+            ).encode()\n+            for number in PRODUCT_NUMBERS\n+        }\n+        for name, content in self.product_bytes.items():\n+            (self.adr_dir / name).write_bytes(content)\n+\n+    def test_fresh_install_preserves_product_adrs_and_installs_all_sadrs(self) -> None:\n+        _quiet_install(self.target, force=False, validate=True)\n+\n+        for name, expected in self.product_bytes.items():\n+            self.assertEqual((self.adr_dir / name).read_bytes(), expected)\n+\n+        sadr_numbers = {\n+            path.name.split(\"-\", 2)[1]\n+            for path in self.adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\")\n+        }\n+        self.assertEqual(sadr_numbers, {f\"{number:04d}\" for number in range(1, 20)})\n+\n+        numeric_records = {\n+            path.name\n+            for path in self.adr_dir.glob(\"[0-9][0-9][0-9][0-9]-*.md\")\n+        }\n+        self.assertEqual(numeric_records, set(self.product_bytes))\n+        self.assertEqual(find_adr_reference_violations(self.target), [])\n+\n+\n+class LegacySadrUpgradeTests(unittest.TestCase):\n+    def setUp(self) -> None:\n+        self.tmp = Path(tempfile.mkdtemp(prefix=\"shiki-sadr-upgrade-\"))\n+        self.addCleanup(lambda: shutil.rmtree(self.tmp, ignore_errors=True))\n+        self.target = self.tmp / \"target\"\n+        self.adr_dir = self.target / \"docs\" / \"adr\"\n+        self.adr_dir.mkdir(parents=True)\n+\n+        self.product_bytes = {\n+            f\"{number:04d}-cubeshare-product-decision.md\": (\n+                f\"# CubeShare ADR {number:04d}\\n\\nDo not rewrite {number}.\\n\"\n+            ).encode()\n+            for number in PRODUCT_NUMBERS\n+        }\n+        for name, content in self.product_bytes.items():\n+            (self.adr_dir / name).write_bytes(content)\n+\n+        self.legacy_paths = _legacy_sadr_paths()\n+        for relative in self.legacy_paths:\n+            path = self.target / relative\n+            path.write_bytes(f\"legacy Shiki bytes for {relative}\\n\".encode())\n+\n+        stamp_path = self.target / shiki_installer.INSTALL_STAMP_PATH\n+        stamp_path.parent.mkdir(parents=True, exist_ok=True)\n+        stamp = {\n+            \"version\": shiki_installer.INSTALL_STAMP_VERSION,\n+            \"platform_commit\": None,\n+            \"installed_at\": \"2000-01-01T00:00:00+00:00\",\n+            \"digests\": {\n+                relative: _sha256(self.target / relative)\n+                for relative in self.legacy_paths\n+            },\n+        }\n+        stamp_path.write_text(\n+            json.dumps(stamp, indent=2, sort_keys=True) + \"\\n\", encoding=\"utf-8\"\n+        )\n+\n+    def test_force_upgrade_deletes_only_stamped_legacy_sadrs(self) -> None:\n+        _quiet_install(self.target, force=True, validate=True)\n+\n+        for relative in self.legacy_paths:\n+            self.assertFalse((self.target / relative).exists(), relative)\n+        for name, expected in self.product_bytes.items():\n+            self.assertEqual((self.adr_dir / name).read_bytes(), expected)\n+\n+        sadrs = list(self.adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\"))\n+        self.assertEqual(len(sadrs), 19)\n+        refreshed = json.loads(\n+            (self.target / shiki_installer.INSTALL_STAMP_PATH).read_text(encoding=\"utf-8\")\n+        )\n+        for relative in self.legacy_paths:\n+            self.assertNotIn(relative, refreshed[\"digests\"])\n+        for path in sadrs:\n+            relative = path.relative_to(self.target).as_posix()\n+            self.assertEqual(refreshed[\"digests\"][relative], _sha256(path))\n+\n+\n+if __name__ == \"__main__\":\n+    unittest.main()\n\napply patch\npatch: completed\n/private/tmp/shiki-adr-collision.pa0KwY/.worktrees/shiki-t-20260807t073307221254z-2d22d654-introduc/tests/test_sadr_install_migration.py\ndiff --git a/.codex/skills/shiki/SKILL.md b/.codex/skills/shiki/SKILL.md\nindex c8e2e85d9d75d49cc4f2d6286ee22659c4e53e75..93cded623b2c15d9f65c76491a1c2d813ac9e6c0\n--- a/.codex/skills/shiki/SKILL.md\n+++ b/.codex/skills/shiki/SKILL.md\n@@ -18,6 +18,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n Codex CLI does not register this skill as a custom `/shiki` slash command. When\n the user types `/shiki` in Codex CLI and gets \"Unrecognized command\", explain\n that this is expected: invoke Shiki in Codex with natural language such as\n@@ -69,7 +72,7 @@\n \n ## Responsibilities\n \n-- Claude Code implements and repairs by default (`shiki runner claude`, ADR 0008); Codex implements and repairs tasks explicitly assigned to `codex`.\n+- Claude Code implements and repairs by default (`shiki runner claude`, SADR-0008); Codex implements and repairs tasks explicitly assigned to `codex`.\n - Claude Code Action can act as GitHub-side CCA or reviewer.\n - CCA judges completion.\n - MergeGate authorizes state transitions and merge readiness.\ndiff --git a/.github/prompts/cca-completion-check.md b/.github/prompts/cca-completion-check.md\nindex ed203ad5f302ecc81372d8ab27d2b1232ca97497..fd89950528333ef6242e6d5443904f5392ab7e56\n--- a/.github/prompts/cca-completion-check.md\n+++ b/.github/prompts/cca-completion-check.md\n@@ -19,6 +19,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n ## Required Reading\n \n Read only the files needed to judge the PR contract. Prefer this order:\ndiff --git a/AGENTS.md b/AGENTS.md\nindex 7f041100d49c2a7525f51cb0f5d696a4a6d1c78c..2b700e989ff295987aa8a4b0c5fcb1acd80020da\n--- a/AGENTS.md\n+++ b/AGENTS.md\n@@ -44,6 +44,10 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use\n+`SADR-NNNN-*.md`; target product decisions retain `NNNN-*.md` and the `ADR`\n+namespace.\n+\n Use the canonical source-of-truth order above. Runtime-specific reading orders\n are operational guidance only and must not override or reorder it.\n \ndiff --git a/CLAUDE.md b/CLAUDE.md\nindex 98a932e7841a632866fcd53b0bad0e3af3711d3d..611d491912d6a72a0327a1731b8f33a41acb4baa\n--- a/CLAUDE.md\n+++ b/CLAUDE.md\n@@ -14,6 +14,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n ## Claude Role\n \n Default to planner, default implementer, reviewer, coordinator, documentation editor, CCA-style completion judge, and final-judgment assistant.\ndiff --git a/SYSTEM_PROMPT.md b/SYSTEM_PROMPT.md\nindex aaaea45e3a896c3a44ba89eec8bb84325ecf6c62..2e7c093ebe9e8bb34e0923756f191b4f7fa581f5\n--- a/SYSTEM_PROMPT.md\n+++ b/SYSTEM_PROMPT.md\n@@ -30,6 +30,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n Use the canonical source-of-truth order above. Runtime-specific reading orders\n are operational guidance only and must not override or reorder it. The current\n conversation is non-durable operator input.\ndiff --git a/docs/adr/0001-keep-shiki-as-platform-name.md b/docs/adr/SADR-0001-keep-shiki-as-platform-name.md\nindex 20a9637c3b9d8c97de7ba8db7cfae4d82d9a7dba..8b685439959aada1b221c112f0ac3c51ca8a1695\n--- a/docs/adr/0001-keep-shiki-as-platform-name.md\n+++ b/docs/adr/SADR-0001-keep-shiki-as-platform-name.md\n@@ -1,3 +1,3 @@\n-# Keep Shiki As The Platform Name\n+# SADR-0001: Keep Shiki As The Platform Name\n \n Shiki remains the product name for the GitHub-native agentic engineering control plane. We considered more descriptive names such as MergePilot, MergeGate, and Kiroku, but kept Shiki because it carries the existing goal-seeking framework identity while remaining independent of Hermes, Codex, Claude Code, or any single runtime.\ndiff --git a/docs/adr/0002-use-agents-as-shared-constitution.md b/docs/adr/SADR-0002-use-agents-as-shared-constitution.md\nindex 73de30ecb078120eac540d9bc51855e3cd48d0e3..c5486182bf06fca36ea508496b013c67302fd989\n--- a/docs/adr/0002-use-agents-as-shared-constitution.md\n+++ b/docs/adr/SADR-0002-use-agents-as-shared-constitution.md\n@@ -1,3 +1,3 @@\n-# Use AGENTS As The Shared Agent Constitution\n+# SADR-0002: Use AGENTS As The Shared Agent Constitution\n \n Shiki uses `AGENTS.md` as the runtime-neutral constitution and `CLAUDE.md` as a thin Claude Code wrapper. This keeps Codex, Claude Code, Hermes Runner, and future Agent Runtimes aligned on the same GitHub-first MergeGate rules while still allowing runtime-specific instructions where needed.\ndiff --git a/docs/adr/0003-require-skill-gate-for-mergegate-readiness.md b/docs/adr/SADR-0003-require-skill-gate-for-mergegate-readiness.md\nindex 424be0c9af4c8f0136f1fde68a468c47ff84e764..7fc337987c7df9792f75f181dfcbed75fe5ffff2\n--- a/docs/adr/0003-require-skill-gate-for-mergegate-readiness.md\n+++ b/docs/adr/SADR-0003-require-skill-gate-for-mergegate-readiness.md\n@@ -1,3 +1,3 @@\n-# Require Skill Gate For MergeGate Readiness\n+# SADR-0003: Require Skill Gate For MergeGate Readiness\n \n Shiki requires relevant engineering skills to be invoked before execution when their triggers apply. This makes skills part of MergeGate readiness rather than optional prompt style, because Shiki depends on repeatable planning, issue decomposition, TDD, diagnosis, triage, and architecture review before autonomous or parallel agent work can be trusted.\ndiff --git a/docs/adr/0004-use-github-first-with-shiki-mirror.md b/docs/adr/SADR-0004-use-github-first-with-shiki-mirror.md\nindex d0a3a9174dc646a945a9d325954ffaf00c352040..a79c54ae9ee8eed2a0524192b864da4e22a92d3f\n--- a/docs/adr/0004-use-github-first-with-shiki-mirror.md\n+++ b/docs/adr/SADR-0004-use-github-first-with-shiki-mirror.md\n@@ -1,4 +1,4 @@\n-# ADR 0004: Use GitHub First With A Repository-Local Shiki Mirror\n+# SADR-0004: Use GitHub First With A Repository-Local Shiki Mirror\n \n ## Status\n \ndiff --git a/docs/adr/0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md b/docs/adr/SADR-0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\nindex b339baf49b89d9290e79c3d51b36e7b2bde3bd95..c30a0d8b1fde0224d0ac7820e7870dccf721580d\n--- a/docs/adr/0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\n+++ b/docs/adr/SADR-0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\n@@ -1,4 +1,4 @@\n-# ADR 0005: Use Codex As Front And Claude Code Action As The GitHub Runtime\n+# SADR-0005: Use Codex As Front And Claude Code Action As The GitHub Runtime\n \n ## Status\n \ndiff --git a/docs/adr/0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md b/docs/adr/SADR-0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\nindex 4bcae9d1e52f0a37b8e9fbafa5cd14b6ee1993bd..7a38538a6ae9929567f0ade67e7ea6a60d0b3f7e\n--- a/docs/adr/0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\n+++ b/docs/adr/SADR-0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\n@@ -1,4 +1,4 @@\n-# ADR 0006: Separate Codex Implementation, CCA Completion, And MergeGate Authorization\n+# SADR-0006: Separate Codex Implementation, CCA Completion, And MergeGate Authorization\n \n ## Status\n \ndiff --git a/docs/adr/0007-packaging-and-release.md b/docs/adr/SADR-0007-packaging-and-release.md\nindex 79b84ddb97167372b44641e5b7860e400ce80822..023cd57f8011aa6e6d2ebf612b7c35a7a82c00d4\n--- a/docs/adr/0007-packaging-and-release.md\n+++ b/docs/adr/SADR-0007-packaging-and-release.md\n@@ -1,4 +1,4 @@\n-# ADR 0007: Packaging And Release For Shiki\n+# SADR-0007: Packaging And Release For Shiki\n \n ## Status\n \ndiff --git a/docs/adr/0008-use-claude-code-as-default-implementer.md b/docs/adr/SADR-0008-use-claude-code-as-default-implementer.md\nindex dedf4510b7c7799521422f622029563d373db031..a158a8d444d93764ec515a28133f7dde4af23579\n--- a/docs/adr/0008-use-claude-code-as-default-implementer.md\n+++ b/docs/adr/SADR-0008-use-claude-code-as-default-implementer.md\n@@ -1,4 +1,4 @@\n-# ADR 0008: Use Claude Code As The Default Implementer Runtime\n+# SADR-0008: Use Claude Code As The Default Implementer Runtime\n \n ## Status\n \ndiff --git a/docs/adr/0009-requirements-definition-and-spec-freeze.md b/docs/adr/SADR-0009-requirements-definition-and-spec-freeze.md\nindex 29b3f7871bef9203a8b4a831a65c39eb2f506e42..52e7dd69ad0aa4efe0b68c62678abbf7df02aaec\n--- a/docs/adr/0009-requirements-definition-and-spec-freeze.md\n+++ b/docs/adr/SADR-0009-requirements-definition-and-spec-freeze.md\n@@ -1,4 +1,4 @@\n-# ADR 0009: Merge Grill And Planning Into Requirements Definition With Spec Freeze\n+# SADR-0009: Merge Grill And Planning Into Requirements Definition With Spec Freeze\n \n ## Status\n \ndiff --git a/docs/adr/0010-external-ai-guardian-review.md b/docs/adr/SADR-0010-external-ai-guardian-review.md\nindex e6108bbe8bb4e73e0ff4684c8f78001a80438c4e..e690d4eddd270e683d709c9b679c5859e71eeaaa\n--- a/docs/adr/0010-external-ai-guardian-review.md\n+++ b/docs/adr/SADR-0010-external-ai-guardian-review.md\n@@ -1,4 +1,4 @@\n-# ADR 0010: External AI Guardian Review As A First-Class Approval Authority\n+# SADR-0010: External AI Guardian Review As A First-Class Approval Authority\n \n ## Status\n \ndiff --git a/docs/adr/0011-autonomous-quality-gate-evidence.md b/docs/adr/SADR-0011-autonomous-quality-gate-evidence.md\nindex bca0246c5d46e44afbde15486a75eec425fc02f5..9c93eac16d8c3a6d4b11bd7224e3955102cd7db4\n--- a/docs/adr/0011-autonomous-quality-gate-evidence.md\n+++ b/docs/adr/SADR-0011-autonomous-quality-gate-evidence.md\n@@ -1,4 +1,4 @@\n-# ADR 0011: Autonomous Quality-Gate Evidence \u2014 Deterministic Facts And An Independent Verifier\n+# SADR-0011: Autonomous Quality-Gate Evidence \u2014 Deterministic Facts And An Independent Verifier\n \n ## Status\n \ndiff --git a/docs/adr/0012-autonomous-loop-closeout-to-main.md b/docs/adr/SADR-0012-autonomous-loop-closeout-to-main.md\nindex 637fad6dde4989c9c2bf0f81f676d8d29991123c..ec7d85f81030a676bcbba28b43eb365422a36eac\n--- a/docs/adr/0012-autonomous-loop-closeout-to-main.md\n+++ b/docs/adr/SADR-0012-autonomous-loop-closeout-to-main.md\n@@ -1,4 +1,4 @@\n-# 0012 \u2014 Autonomous loop pushes goal completion to main via a closeout PR\n+# SADR-0012 \u2014 Autonomous loop pushes goal completion to main via a closeout PR\n \n - Status: accepted\n - Date: 2026-06-18\ndiff --git a/docs/adr/0013-bootstrap-grants-actions-approve-pull-requests.md b/docs/adr/SADR-0013-bootstrap-grants-actions-approve-pull-requests.md\nindex b6c69d64f1c5ced346b88d6859586715b3b5bd0e..a793e7277d67ab8ea56286fec289ca689825a17d\n--- a/docs/adr/0013-bootstrap-grants-actions-approve-pull-requests.md\n+++ b/docs/adr/SADR-0013-bootstrap-grants-actions-approve-pull-requests.md\n@@ -1,4 +1,4 @@\n-# ADR 0013: Bootstrap Grants GitHub Actions Permission To Create And Approve Pull Requests\n+# SADR-0013: Bootstrap Grants GitHub Actions Permission To Create And Approve Pull Requests\n \n ## Status\n \ndiff --git a/docs/adr/0014-codex-app-external-ai-guardian-ui-adapter.md b/docs/adr/SADR-0014-codex-app-external-ai-guardian-ui-adapter.md\nindex 238db06279334caa341c117362df9271382a7fcd..65f12e9939321e1b3e389a34c822fc1549e5155d\n--- a/docs/adr/0014-codex-app-external-ai-guardian-ui-adapter.md\n+++ b/docs/adr/SADR-0014-codex-app-external-ai-guardian-ui-adapter.md\n@@ -1,4 +1,4 @@\n-# ADR 0014: Use Codex App As The External AI Guardian UI Adapter\n+# SADR-0014: Use Codex App As The External AI Guardian UI Adapter\n \n ## Status\n \ndiff --git a/docs/adr/0015-contract-approval-before-dispatch.md b/docs/adr/SADR-0015-contract-approval-before-dispatch.md\nindex fad6c656741a20a7684ba16080a7db159f63a52f..7bdd760bb3e82eee4e1410f45c783b03b638d456\n--- a/docs/adr/0015-contract-approval-before-dispatch.md\n+++ b/docs/adr/SADR-0015-contract-approval-before-dispatch.md\n@@ -1,4 +1,4 @@\n-# ADR 0015: Approve The Task Contract Before Dispatch, Not The Diff After It\n+# SADR-0015: Approve The Task Contract Before Dispatch, Not The Diff After It\n \n ## Status\n \ndiff --git a/docs/adr/0016-locks-bound-product-paths.md b/docs/adr/SADR-0016-locks-bound-product-paths.md\nindex b5e850f3f1f7f4ef6e7f9d1763a29a51bd300c45..332cc17313498404f41a145291e38447b5de0dc4\n--- a/docs/adr/0016-locks-bound-product-paths.md\n+++ b/docs/adr/SADR-0016-locks-bound-product-paths.md\n@@ -1,4 +1,4 @@\n-# ADR 0016: Locks bound product paths; the Shiki mirror is bound by record identity\n+# SADR-0016: Locks bound product paths; the Shiki mirror is bound by record identity\n \n ## Status\n \ndiff --git a/docs/adr/0017-bookkeeping-closeouts-do-not-inherit-risk.md b/docs/adr/SADR-0017-bookkeeping-closeouts-do-not-inherit-risk.md\nindex 554e3a0d526a72ca067108c5464cc68c3edcf174..83115f69f1b1b48d544c9b0157f377b91cb53abf\n--- a/docs/adr/0017-bookkeeping-closeouts-do-not-inherit-risk.md\n+++ b/docs/adr/SADR-0017-bookkeeping-closeouts-do-not-inherit-risk.md\n@@ -1,4 +1,4 @@\n-# ADR 0017: A Bookkeeping Closeout Does Not Inherit Its Task's Risk\n+# SADR-0017: A Bookkeeping Closeout Does Not Inherit Its Task's Risk\n \n ## Status\n \ndiff --git a/docs/adr/0018-carried-guardian-approval-across-a-proven-base-sync.md b/docs/adr/SADR-0018-carried-guardian-approval-across-a-proven-base-sync.md\nindex 22358562da390ed8b45cd600da08a43d378b4680..38da0fd95102c3496c3219ce2319fce696a2532a\n--- a/docs/adr/0018-carried-guardian-approval-across-a-proven-base-sync.md\n+++ b/docs/adr/SADR-0018-carried-guardian-approval-across-a-proven-base-sync.md\n@@ -1,4 +1,4 @@\n-# ADR 0018: Carried Guardian Approval Across A Proven Base Sync\n+# SADR-0018: Carried Guardian Approval Across A Proven Base Sync\n \n ## Status\n \ndiff --git a/docs/adr/README.md b/docs/adr/README.md\nindex c70e057a4520c36e042ab68c53cd4a21ef709b71..5da43856836e2ce6a150f8127766e2f88355ab4b\n--- a/docs/adr/README.md\n+++ b/docs/adr/README.md\n@@ -1,40 +1,41 @@\n-# Architecture Decision Records\n+# Decision Record Namespaces\n \n-This directory holds Shiki's Architecture Decision Records (ADRs): durable\n-records of hard-to-reverse platform decisions. ADRs are part of the Shiki source\n-of truth alongside GitHub state, the `.shiki/` mirror, and `CONTEXT.md`.\n+This directory holds two independent decision namespaces:\n \n-## How ADRs Work\n+- Shiki Architecture Decision Records (SADRs), named\n+  `SADR-NNNN-<short-slug>.md`, record hard-to-reverse Shiki platform decisions.\n+- Target Architecture Decision Records (ADRs), named\n+  `NNNN-<short-slug>.md`, belong to the target product.\n \n-- New ADRs are added as `docs/adr/NNNN-<short-slug>.md`, where `NNNN` is the next\n-  zero-padded sequence number.\n-- The **canonical list of ADRs is the set of files matching `docs/adr/*.md`** in\n-  this directory. Browse `docs/adr/*.md` for the authoritative, current set.\n-- Use [`template.md`](template.md) as the starting point for a new ADR.\n-- Record an ADR when a decision is hard to reverse, surprising without context,\n-  and a real tradeoff. Do not write an ADR for routine or easily reversible\n-  choices.\n+The same four-digit number may appear once in each namespace. A platform\n+`SADR-NNNN` citation resolves only to `SADR-NNNN-*.md`; a target `ADR NNNN`\n+citation resolves only to `NNNN-*.md`.\n \n-Using the `docs/adr/*.md` glob as the source of truth means other PRs can add an\n-ADR without editing this index file, avoiding merge contention on a\n-hand-maintained enumeration.\n+## How Decision Records Work\n \n-## Current ADRs (snapshot)\n-\n-The list below is a convenience snapshot and may lag behind the directory. When\n-in doubt, list `docs/adr/*.md`.\n+- New Shiki platform decisions use the next zero-padded SADR number.\n+- New target product decisions use the target's own ADR sequence.\n+- The canonical list is the files in `docs/adr/`; no hand-maintained index is\n+  authoritative.\n+- Use [`template.md`](template.md) as the starting point and choose the filename\n+  namespace that owns the decision.\n+- Record a decision when it is hard to reverse, surprising without context, and\n+  a real tradeoff. Do not write a decision record for routine or easily\n+  reversible choices.\n \n-- [0001 \u2014 Keep Shiki As The Platform Name](0001-keep-shiki-as-platform-name.md)\n-- [0002 \u2014 Use AGENTS As The Shared Agent Constitution](0002-use-agents-as-shared-constitution.md)\n-- [0003 \u2014 Require Skill Gate For MergeGate Readiness](0003-require-skill-gate-for-mergegate-readiness.md)\n-- [0004 \u2014 Use GitHub First With A Repository-Local Shiki Mirror](0004-use-github-first-with-shiki-mirror.md)\n-- [0005 \u2014 Use Codex As Front And Claude Code Action As The GitHub Runtime](0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md)\n-- [0006 \u2014 Separate Codex Implementation, CCA Completion, And MergeGate Authorization](0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md)\n+Using the directory as the source of truth lets independent changes add records\n+without contending on an enumerated index.\n \n-## Adding A New ADR\n+## Adding A Shiki SADR\n \n-1. Copy `template.md` to `docs/adr/NNNN-<short-slug>.md`.\n+1. Copy `template.md` to `docs/adr/SADR-NNNN-<short-slug>.md`.\n 2. Fill in the title, status, context, decision, and consequences.\n-3. Set the status to `Proposed`, then `Accepted` once the decision is approved.\n-4. Open a PR following `CONTRIBUTING.md`. You do not need to edit this index for\n-   the ADR to be discoverable via `docs/adr/*.md`.\n+3. Set the status to `Proposed`, then `Accepted` once the proper authority has\n+   approved the decision.\n+4. Open a PR following `CONTRIBUTING.md`.\n+\n+## Adding A Target ADR\n+\n+1. Copy `template.md` to `docs/adr/NNNN-<short-slug>.md` using the target's own\n+   sequence.\n+2. Cite it as `ADR NNNN`; do not prefix its filename with `SADR-`.\ndiff --git a/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md b/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\nnew file mode 100644\nindex 0000000000000000000000000000000000000000..7455ce1f3bed02ddddb851ac21a1d1a648c9aeba\n--- /dev/null\n+++ b/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\n@@ -0,0 +1,55 @@\n+# SADR-0019: Separate Shiki Platform SADRs From Target ADRs\n+\n+## Status\n+\n+Accepted\n+\n+## Context\n+\n+Shiki installs its platform decision records into `docs/adr/` in every target\n+repository. Shiki and its targets previously used the same numeric\n+`NNNN-<slug>.md` namespace. A target that already owned ADRs 0013 through 0018\n+therefore acquired two records for each number during installation, making an\n+`ADR NNNN` citation ambiguous. Renumbering target decisions would rewrite target\n+history, while renumbering Shiki decisions per target would destroy their stable\n+platform identity.\n+\n+Upgrades also need to remove the former Shiki-owned numeric paths without ever\n+mistaking a target-owned ADR for platform content. The install stamp already\n+records exact shipped paths and content digests, so it can prove ownership only\n+when both still match.\n+\n+## Decision\n+\n+Shiki platform decisions use the `SADR` namespace:\n+\n+- records are named `docs/adr/SADR-NNNN-<slug>.md`;\n+- live platform citations use `SADR-NNNN`;\n+- target product decisions retain `docs/adr/NNNN-<slug>.md` and `ADR NNNN`;\n+- the reference validator resolves the two namespaces independently, so the same\n+  four-digit number may exist once in each namespace.\n+\n+Before a forced upgrade writes anything, the installer inventories the exact\n+former numeric Shiki ADR paths. It may delete a legacy path only when the\n+existing install stamp contains that exact path and its recorded SHA-256 digest\n+matches the file currently on disk. All ownership blockers are reported\n+together and abort the upgrade before its first write. A non-forced install\n+never performs this cleanup.\n+\n+Historical `.shiki/` evidence remains unchanged because it records the namespace\n+that existed when that evidence was produced.\n+\n+## Consequences\n+\n+- Targets may use any ADR number without colliding with Shiki's platform SADRs.\n+- Fresh installs preserve target ADR bytes and install a stable SADR set.\n+- Forced upgrades fail closed when legacy ownership cannot be proven, and a\n+  successful upgrade refreshes the stamp without removed legacy paths.\n+- Current governance, packaging, workflow, prompt, script, and skill references\n+  must move with the platform records.\n+\n+## Alternatives Considered\n+\n+- Collision-only refusal leaves affected targets permanently unupgradable.\n+- Renumbering target ADRs mutates target-owned history.\n+- Per-target Shiki renumbering makes platform citations non-portable.\ndiff --git a/docs/adr/template.md b/docs/adr/template.md\nindex a92a97daddd0b00ab9dd626cc8d86942da2eafed..17dd79175b5fee2105935557f691b40cf9492942\n--- a/docs/adr/template.md\n+++ b/docs/adr/template.md\n@@ -1,14 +1,14 @@\n-# NNNN: <Short Decision Title>\n+# <SADR-NNNN or NNNN>: <Short Decision Title>\n \n ## Status\n \n-Proposed | Accepted | Superseded by ADR-NNNN\n+Proposed | Accepted | Superseded by SADR-NNNN or target ADR NNNN\n \n ## Context\n \n What is the situation, constraint, or force that requires a decision? Reference\n-relevant domain terms (`CONTEXT.md`), prior ADRs, and code reality. State the\n-problem, not the solution.\n+relevant domain terms (`CONTEXT.md`), prior Shiki SADRs or target ADRs, and code\n+reality. State the problem, not the solution.\n \n ## Decision\n \ndiff --git a/docs/agents/domain.md b/docs/agents/domain.md\nindex 258da8ea81e87ac0a6b0b5b5e5c2fde980eaabb7..67aaa6f4b7d0a5aa4d495ce6279073333f7798cb\n--- a/docs/agents/domain.md\n+++ b/docs/agents/domain.md\n@@ -5,7 +5,9 @@\n ## Layout\n \n - `CONTEXT.md` defines Shiki language. Keep it implementation-free.\n-- `docs/adr/` records hard-to-reverse decisions.\n+- `docs/adr/SADR-NNNN-*.md` records Shiki platform decisions.\n+- `docs/adr/NNNN-*.md` records target product decisions without sharing the\n+  SADR namespace.\n - `AGENTS.md` is the runtime-neutral agent constitution.\n - `CLAUDE.md` is the Claude Code wrapper around `AGENTS.md`.\n - `.shiki/` is the Target Repository mirror for Goals, PRDs, plans, Task DAGs, contracts, locks, worktree records, ledger entries, CCA verdicts, repair packets, reports, and handoffs.\ndiff --git a/docs/agents/implementation-policy.md b/docs/agents/implementation-policy.md\nindex c754144a9fb5bf4a38aa82a60e1670c59e626b44..e90d40fe21880b9a09719c5ecfe6bde7284eb3ce\n--- a/docs/agents/implementation-policy.md\n+++ b/docs/agents/implementation-policy.md\n@@ -31,6 +31,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n ## Non-Negotiables\n \n 1. **Planning precedes implementation.** Non-trivial Goals start with `grill-with-docs` and Context & Impact.\ndiff --git a/scripts/shiki_installer.py b/scripts/shiki_installer.py\nindex be064ab7a29ff6836f7f7a1d70500a43e0d11d7b..d3e3247edbf3e4be287a4667176d86f6500f8dc5\n--- a/scripts/shiki_installer.py\n+++ b/scripts/shiki_installer.py\n@@ -118,6 +118,7 @@\n # what lets a target carry it without a governance-schema change.\n INSTALL_STAMP_PATH = \".shiki/install-stamp.json\"\n INSTALL_STAMP_VERSION = 1\n+LEGACY_SHIKI_SADR_MAX = 18\n \n # Under --force the shipped surface splits three ways, so nothing is lost and\n # nothing is silently stale.\n@@ -237,9 +238,98 @@\n         )\n \n \n+def legacy_shiki_adr_paths() -> tuple[str, ...]:\n+    \"\"\"Former numeric paths for Shiki SADR-0001 through SADR-0018.\n+\n+    The authoritative slugs come from the current SADR filenames. SADR-0019 is\n+    the namespace decision itself and never shipped under a numeric legacy path.\n+    \"\"\"\n+    paths: list[str] = []\n+    for sadr in sorted((ROOT / \"docs\" / \"adr\").glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\")):\n+        number = sadr.name.split(\"-\", 2)[1]\n+        if int(number) <= LEGACY_SHIKI_SADR_MAX:\n+            paths.append(f\"docs/adr/{sadr.name.removeprefix('SADR-')}\")\n+    return tuple(paths)\n+\n+\n+def preflight_legacy_sadr_cleanup(target: Path, *, force: bool) -> tuple[str, ...]:\n+    \"\"\"Authorize exact legacy Shiki ADR removals before the first write.\n+\n+    A non-force install never removes existing files. A forced install needs no\n+    stamp when no legacy Shiki path is present. When legacy paths do exist, each\n+    must be named in a readable install stamp and its current bytes must match\n+    the recorded digest.\n+    \"\"\"\n+    if not force:\n+        return ()\n+    present = tuple(\n+        relative\n+        for relative in legacy_shiki_adr_paths()\n+        if (target / relative).exists() or (target / relative).is_symlink()\n+    )\n+    if not present:\n+        return ()\n+\n+    stamp_path = target / INSTALL_STAMP_PATH\n+    try:\n+        raw = stamp_path.read_text(encoding=\"utf-8\")\n+        stamp = json.loads(raw)\n+    except FileNotFoundError:\n+        detail = \"install stamp is absent\"\n+        blockers = [f\"{relative}: {detail}\" for relative in present]\n+    except (OSError, UnicodeDecodeError) as error:\n+        detail = f\"install stamp is unreadable ({error})\"\n+        blockers = [f\"{relative}: {detail}\" for relative in present]\n+    except json.JSONDecodeError as error:\n+        detail = f\"install stamp is malformed ({error})\"\n+        blockers = [f\"{relative}: {detail}\" for relative in present]\n+    else:\n+        if not isinstance(stamp, dict):\n+            blockers = [\n+                f\"{relative}: install stamp is malformed (expected an object)\"\n+                for relative in present\n+            ]\n+        else:\n+            digests = stamp.get(\"digests\")\n+            blockers = []\n+            if not isinstance(digests, dict):\n+                blockers.extend(\n+                    f\"{relative}: install stamp has no digest for this path\"\n+                    for relative in present\n+                )\n+            else:\n+                for relative in present:\n+                    expected = digests.get(relative)\n+                    if not isinstance(expected, str) or not expected:\n+                        blockers.append(\n+                            f\"{relative}: install stamp has no digest for this path\"\n+                        )\n+                        continue\n+                    try:\n+                        actual = _sha256_file(target / relative)\n+                    except OSError as error:\n+                        blockers.append(\n+                            f\"{relative}: legacy path is unreadable ({error})\"\n+                        )\n+                        continue\n+                    if actual != expected:\n+                        blockers.append(\n+                            f\"{relative}: digest mismatch (stamp {expected}, current {actual})\"\n+                        )\n+\n+    if blockers:\n+        joined = \"\\n\".join(f\"- {blocker}\" for blocker in blockers)\n+        raise ShikiError(\n+            \"legacy Shiki ADR cleanup is not authorized:\\n\"\n+            f\"{joined}\\nNo files were written.\"\n+        )\n+    return present\n+\n+\n def install_template(target: Path, *, force: bool, validate: bool) -> list[NewFileNote]:\n     # Preconditions first: never rewrite the tree of a target that cannot be\n     # upgraded cleanly (see refuse_pending_migrations).\n+    legacy_cleanup = preflight_legacy_sadr_cleanup(target, force=force)\n     refuse_pending_migrations(target)\n \n     # Resolve the target's required CODEOWNERS owner once from its\n@@ -250,6 +340,10 @@\n     # verbatim. See ``codeowners_required_owner``.\n     codeowners_owner = codeowners_required_owner(target)\n \n+    for relative in legacy_cleanup:\n+        (target / relative).unlink()\n+        info(f\"removed stamped legacy Shiki ADR: {target / relative}\")\n+\n     notes: list[NewFileNote] = []\n     for relative in TEMPLATE_PATHS:\n         source = ROOT / relative\ndiff --git a/tests/test_adr_references.py b/tests/test_adr_references.py\nindex 51706329c797d5dd9b47e082a333fa83b261d482..91ab76b724cb973435c45d53cab63793942267e9\n--- a/tests/test_adr_references.py\n+++ b/tests/test_adr_references.py\n@@ -1,4 +1,4 @@\n-\"\"\"Every ``ADR ####`` citation must resolve to exactly one decision record.\n+\"\"\"Every target ADR and Shiki SADR citation resolves in its own namespace.\n \n Background. ``scripts/mergegate_check.py`` justifies ``is_bookkeeping_closeout``\n with \"ADR 0017\" nine times and ``scripts/guardian_approval_signal.py`` cites\n@@ -11,11 +11,12 @@\n and the directory drifted with no signal.\n \n This is the same unbound-pair shape behind other silent failures: a reference\n-and its target with nothing asserting they agree. This test binds them. It scans\n-every ``scripts/*.py`` (plus ``docs/**/*.md`` and the root ``*.md`` governance\n-files, which are cheap) for the ``ADR ####`` pattern and asserts each cited\n-number resolves to exactly one ``docs/adr/####-*.md``. The converse is\n-deliberately NOT required: an ADR may exist without being cited.\n+and its target with nothing asserting they agree. This test binds them. Target\n+``ADR NNNN`` citations resolve only to ``docs/adr/NNNN-*.md``; Shiki\n+``SADR-NNNN`` citations resolve only to ``docs/adr/SADR-NNNN-*.md``. The scan\n+covers current governance docs, scripts, workflows, prompts, packaging metadata,\n+and repo-local Codex guidance while deliberately excluding historical\n+``.shiki`` evidence. The converse is not required: a record may be uncited.\n \n The suite is dependency free: standard-library ``unittest`` only.\n \"\"\"\n@@ -31,17 +32,18 @@\n \n REPO_ROOT = Path(__file__).resolve().parents[1]\n \n-# \"ADR 0016\" -- a four-digit citation. The trailing \\b keeps a five-digit token\n-# from being read as a truncated four-digit number.\n-_CITATION = re.compile(r\"\\bADR (\\d{4})\\b\")\n-# \"0016-locks-bound-product-paths.md\" -- the leading four digits name the record.\n-_ADR_FILENAME = re.compile(r\"^(\\d{4})-.*\\.md$\")\n+# ``ADR 0016`` / ``SADR-0016`` -- a namespace plus four-digit citation. The\n+# trailing ``\\b`` keeps a five-digit token from being read as a truncated one.\n+_CITATION = re.compile(r\"\\b(S?ADR)[ -](\\d{4})\\b\")\n+# Target ADRs retain the numeric filename; Shiki platform decisions use SADR.\n+_DECISION_FILENAME = re.compile(r\"^(?:(SADR)-)?(\\d{4})-.*\\.md$\")\n \n \n class Violation(NamedTuple):\n     \"\"\"A citation that does not resolve to exactly one decision record.\"\"\"\n \n     citing_file: str  # repo-relative path of the file that cites the ADR\n+    namespace: str  # \"ADR\" for target decisions; \"SADR\" for Shiki decisions\n     number: str  # the cited four-digit number, e.g. \"0016\"\n     line: int  # 1-based line of the citation\n     reason: str  # \"missing\" (zero matches) or \"ambiguous\" (two or more)\n@@ -49,44 +51,59 @@\n \n     @property\n     def expected_path(self) -> str:\n-        return f\"docs/adr/{self.number}-*.md\"\n+        prefix = \"SADR-\" if self.namespace == \"SADR\" else \"\"\n+        return f\"docs/adr/{prefix}{self.number}-*.md\"\n \n     def describe(self) -> str:\n         \"\"\"Name the citing file, the cited number, and the missing path.\"\"\"\n         if self.reason == \"missing\":\n             return (\n-                f\"{self.citing_file}:{self.line} cites ADR {self.number} \"\n+                f\"{self.citing_file}:{self.line} cites {self.namespace} {self.number} \"\n                 f\"but no document matches {self.expected_path}\"\n             )\n         joined = \", \".join(self.matches)\n         return (\n-            f\"{self.citing_file}:{self.line} cites ADR {self.number} which \"\n+            f\"{self.citing_file}:{self.line} cites {self.namespace} {self.number} which \"\n             f\"resolves to {len(self.matches)} documents (expected exactly one \"\n             f\"at {self.expected_path}): {joined}\"\n         )\n \n \n-def _adr_index(root: Path) -> dict[str, list[Path]]:\n-    \"\"\"Map every four-digit ADR number to the record files that claim it.\"\"\"\n-    index: dict[str, list[Path]] = {}\n+def _adr_index(root: Path) -> dict[tuple[str, str], list[Path]]:\n+    \"\"\"Map each decision namespace and number to the files that claim it.\"\"\"\n+    index: dict[tuple[str, str], list[Path]] = {}\n     adr_dir = root / \"docs\" / \"adr\"\n     if adr_dir.is_dir():\n         for path in sorted(adr_dir.glob(\"*.md\")):\n-            match = _ADR_FILENAME.match(path.name)\n+            match = _DECISION_FILENAME.match(path.name)\n             if match:\n-                index.setdefault(match.group(1), []).append(path)\n+                namespace = \"SADR\" if match.group(1) else \"ADR\"\n+                index.setdefault((namespace, match.group(2)), []).append(path)\n     return index\n \n \n def _citing_files(root: Path, include_docs: bool = True) -> list[Path]:\n-    \"\"\"Files scanned for citations: scripts/*.py, and cheaply the markdown.\"\"\"\n+    \"\"\"Current, shipped text surfaces scanned for decision citations.\"\"\"\n     files = sorted((root / \"scripts\").glob(\"*.py\"))\n+    files += sorted((root / \"scripts\").glob(\"*.sh\"))\n     if include_docs:\n         docs = root / \"docs\"\n         if docs.is_dir():\n             files += sorted(docs.rglob(\"*.md\"))\n         files += sorted(root.glob(\"*.md\"))\n-    return files\n+        pyproject = root / \"pyproject.toml\"\n+        if pyproject.is_file():\n+            files.append(pyproject)\n+        for relative in (\".github\", \".codex\"):\n+            surface = root / relative\n+            if surface.is_dir():\n+                files += sorted(\n+                    path\n+                    for path in surface.rglob(\"*\")\n+                    if path.is_file()\n+                    and path.suffix in {\".md\", \".py\", \".sh\", \".toml\", \".yaml\", \".yml\"}\n+                )\n+    return sorted(set(files))\n \n \n def find_adr_reference_violations(\n@@ -94,10 +111,9 @@\n ) -> list[Violation]:\n     \"\"\"Return every ``ADR ####`` citation under ``root`` that fails to resolve.\n \n-    A citation resolves when exactly one ``docs/adr/####-*.md`` matches its\n-    number. Zero matches is a \"missing\" violation; two or more is \"ambiguous\".\n-    Records that are never cited produce nothing -- the converse is not\n-    required.\n+    A citation resolves when exactly one record in the same namespace matches\n+    its number. Zero matches is a \"missing\" violation; two or more is\n+    \"ambiguous\". Records that are never cited produce nothing.\n     \"\"\"\n     root = Path(root)\n     index = _adr_index(root)\n@@ -110,13 +126,15 @@\n         rel = path.relative_to(root).as_posix()\n         for lineno, line in enumerate(text.splitlines(), start=1):\n             for match in _CITATION.finditer(line):\n-                number = match.group(1)\n-                found = index.get(number, [])\n+                namespace, number = match.groups()\n+                found = index.get((namespace, number), [])\n                 if len(found) == 1:\n                     continue\n                 reason = \"missing\" if not found else \"ambiguous\"\n                 rels = [p.relative_to(root).as_posix() for p in found]\n-                violations.append(Violation(rel, number, lineno, reason, rels))\n+                violations.append(\n+                    Violation(rel, namespace, number, lineno, reason, rels)\n+                )\n     return violations\n \n \n@@ -129,8 +147,8 @@\n         self.assertEqual(\n             violations,\n             [],\n-            \"Every 'ADR ####' citation must resolve to exactly one \"\n-            f\"docs/adr/####-*.md:\\n{detail}\",\n+            \"Every ADR/SADR citation must resolve exactly once in its own \"\n+            f\"docs/adr namespace:\\n{detail}\",\n         )\n \n     def test_scan_actually_reads_scripts(self) -> None:\n@@ -146,8 +164,21 @@\n         # The two records this Goal carried onto the default branch, and which\n         # the scripts cite most, must be present.\n         index = _adr_index(REPO_ROOT)\n-        self.assertIn(\"0016\", index)\n-        self.assertIn(\"0017\", index)\n+        self.assertIn((\"SADR\", \"0016\"), index)\n+        self.assertIn((\"SADR\", \"0017\"), index)\n+\n+    def test_platform_tree_has_only_sadr_0001_through_0019(self) -> None:\n+        adr_dir = REPO_ROOT / \"docs\" / \"adr\"\n+        sadrs = sorted(adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\"))\n+        self.assertEqual(\n+            {path.name.split(\"-\", 2)[1] for path in sadrs},\n+            {f\"{number:04d}\" for number in range(1, 20)},\n+        )\n+        self.assertEqual(\n+            list(adr_dir.glob(\"[0-9][0-9][0-9][0-9]-*.md\")),\n+            [],\n+            \"the Shiki platform tree must not retain legacy numeric SADR paths\",\n+        )\n \n \n class FixtureAdrReferences(unittest.TestCase):\n@@ -180,6 +211,26 @@\n         self.assertIn(\"9991\", message)  # cited number\n         self.assertIn(\"docs/adr/9991-*.md\", message)  # missing path\n \n+    def test_target_adr_does_not_satisfy_missing_platform_sadr(self) -> None:\n+        root = self._make_repo(\n+            adr_names=[\"0042-target-decision.md\"],\n+            script_body='\"\"\"Target ADR 0042 exists, but platform SADR-0042 does not.\"\"\"\\n',\n+        )\n+        violations = find_adr_reference_violations(root, include_docs=False)\n+        self.assertEqual(len(violations), 1)\n+        self.assertEqual(violations[0].namespace, \"SADR\")\n+        self.assertEqual(violations[0].number, \"0042\")\n+        self.assertEqual(violations[0].expected_path, \"docs/adr/SADR-0042-*.md\")\n+\n+    def test_target_adr_and_platform_sadr_may_reuse_a_number(self) -> None:\n+        root = self._make_repo(\n+            adr_names=[\"0042-target-decision.md\", \"SADR-0042-platform-decision.md\"],\n+            script_body='\"\"\"Target ADR 0042 and platform SADR-0042 are distinct.\"\"\"\\n',\n+        )\n+        self.assertEqual(\n+            find_adr_reference_violations(root, include_docs=False), []\n+        )\n+\n     def test_uncited_adr_is_allowed(self) -> None:\n         # An ADR exists but nothing cites it -> not a violation. The converse\n         # (every ADR must be cited) is explicitly NOT required.\ndiff --git a/tests/test_sadr_install_migration.py b/tests/test_sadr_install_migration.py\nnew file mode 100644\nindex 0000000000000000000000000000000000000000..7a88682efad63c022b90ffdc45395f7ee7b3da81\n--- /dev/null\n+++ b/tests/test_sadr_install_migration.py\n@@ -0,0 +1,141 @@\n+\"\"\"SADR installation and collision-safe legacy upgrade behavior.\"\"\"\n+\n+from __future__ import annotations\n+\n+import contextlib\n+import hashlib\n+import io\n+import json\n+import shutil\n+import tempfile\n+import unittest\n+from pathlib import Path\n+\n+import shiki_test_support  # noqa: F401  (path bootstrap)\n+\n+import shiki_installer\n+from tests.test_adr_references import find_adr_reference_violations\n+\n+\n+PRODUCT_NUMBERS = tuple(range(13, 19))\n+\n+\n+def _quiet_install(target: Path, *, force: bool, validate: bool) -> None:\n+    with contextlib.redirect_stdout(io.StringIO()):\n+        shiki_installer.install_template(target, force=force, validate=validate)\n+\n+\n+def _sha256(path: Path) -> str:\n+    return hashlib.sha256(path.read_bytes()).hexdigest()\n+\n+\n+def _legacy_sadr_paths() -> tuple[str, ...]:\n+    return tuple(\n+        f\"docs/adr/{path.name.removeprefix('SADR-')}\"\n+        for path in sorted((shiki_installer.ROOT / \"docs\" / \"adr\").glob(\"SADR-*.md\"))\n+        if path.name != \"SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\"\n+    )\n+\n+\n+def _create_legacy_upgrade_fixture(\n+    target: Path,\n+) -> tuple[dict[str, bytes], tuple[str, ...]]:\n+    adr_dir = target / \"docs\" / \"adr\"\n+    adr_dir.mkdir(parents=True)\n+    product_bytes = {\n+        f\"{number:04d}-cubeshare-product-decision.md\": (\n+            f\"# CubeShare ADR {number:04d}\\n\\nDo not rewrite {number}.\\n\"\n+        ).encode()\n+        for number in PRODUCT_NUMBERS\n+    }\n+    for name, content in product_bytes.items():\n+        (adr_dir / name).write_bytes(content)\n+\n+    legacy_paths = _legacy_sadr_paths()\n+    for relative in legacy_paths:\n+        (target / relative).write_bytes(f\"legacy Shiki bytes for {relative}\\n\".encode())\n+\n+    stamp_path = target / shiki_installer.INSTALL_STAMP_PATH\n+    stamp_path.parent.mkdir(parents=True, exist_ok=True)\n+    stamp = {\n+        \"version\": shiki_installer.INSTALL_STAMP_VERSION,\n+        \"platform_commit\": None,\n+        \"installed_at\": \"2000-01-01T00:00:00+00:00\",\n+        \"digests\": {\n+            relative: _sha256(target / relative) for relative in legacy_paths\n+        },\n+    }\n+    stamp_path.write_text(\n+        json.dumps(stamp, indent=2, sort_keys=True) + \"\\n\", encoding=\"utf-8\"\n+    )\n+    return product_bytes, legacy_paths\n+\n+\n+class FreshSadrInstallTests(unittest.TestCase):\n+    def setUp(self) -> None:\n+        self.tmp = Path(tempfile.mkdtemp(prefix=\"shiki-sadr-fresh-\"))\n+        self.addCleanup(lambda: shutil.rmtree(self.tmp, ignore_errors=True))\n+        self.target = self.tmp / \"target\"\n+        self.adr_dir = self.target / \"docs\" / \"adr\"\n+        self.adr_dir.mkdir(parents=True)\n+        self.product_bytes = {\n+            f\"{number:04d}-product-decision.md\": (\n+                f\"# Product ADR {number:04d}\\n\\nTarget-owned bytes {number}.\\n\"\n+            ).encode()\n+            for number in PRODUCT_NUMBERS\n+        }\n+        for name, content in self.product_bytes.items():\n+            (self.adr_dir / name).write_bytes(content)\n+\n+    def test_fresh_install_preserves_product_adrs_and_installs_all_sadrs(self) -> None:\n+        _quiet_install(self.target, force=False, validate=True)\n+\n+        for name, expected in self.product_bytes.items():\n+            self.assertEqual((self.adr_dir / name).read_bytes(), expected)\n+\n+        sadr_numbers = {\n+            path.name.split(\"-\", 2)[1]\n+            for path in self.adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\")\n+        }\n+        self.assertEqual(sadr_numbers, {f\"{number:04d}\" for number in range(1, 20)})\n+\n+        numeric_records = {\n+            path.name\n+            for path in self.adr_dir.glob(\"[0-9][0-9][0-9][0-9]-*.md\")\n+        }\n+        self.assertEqual(numeric_records, set(self.product_bytes))\n+        self.assertEqual(find_adr_reference_violations(self.target), [])\n+\n+\n+class LegacySadrUpgradeTests(unittest.TestCase):\n+    def setUp(self) -> None:\n+        self.tmp = Path(tempfile.mkdtemp(prefix=\"shiki-sadr-upgrade-\"))\n+        self.addCleanup(lambda: shutil.rmtree(self.tmp, ignore_errors=True))\n+        self.target = self.tmp / \"target\"\n+        self.adr_dir = self.target / \"docs\" / \"adr\"\n+        self.product_bytes, self.legacy_paths = _create_legacy_upgrade_fixture(\n+            self.target\n+        )\n+\n+    def test_force_upgrade_deletes_only_stamped_legacy_sadrs(self) -> None:\n+        _quiet_install(self.target, force=True, validate=True)\n+\n+        for relative in self.legacy_paths:\n+            self.assertFalse((self.target / relative).exists(), relative)\n+        for name, expected in self.product_bytes.items():\n+            self.assertEqual((self.adr_dir / name).read_bytes(), expected)\n+\n+        sadrs = list(self.adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\"))\n+        self.assertEqual(len(sadrs), 19)\n+        refreshed = json.loads(\n+            (self.target / shiki_installer.INSTALL_STAMP_PATH).read_text(encoding=\"utf-8\")\n+        )\n+        for relative in self.legacy_paths:\n+            self.assertNotIn(relative, refreshed[\"digests\"])\n+        for path in sadrs:\n+            relative = path.relative_to(self.target).as_posix()\n+            self.assertEqual(refreshed[\"digests\"][relative], _sha256(path))\n+\n+\n+if __name__ == \"__main__\":\n+    unittest.main()\n\ndiff --git a/.codex/skills/shiki/SKILL.md b/.codex/skills/shiki/SKILL.md\nindex c8e2e85d9d75d49cc4f2d6286ee22659c4e53e75..93cded623b2c15d9f65c76491a1c2d813ac9e6c0\n--- a/.codex/skills/shiki/SKILL.md\n+++ b/.codex/skills/shiki/SKILL.md\n@@ -18,6 +18,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n Codex CLI does not register this skill as a custom `/shiki` slash command. When\n the user types `/shiki` in Codex CLI and gets \"Unrecognized command\", explain\n that this is expected: invoke Shiki in Codex with natural language such as\n@@ -69,7 +72,7 @@\n \n ## Responsibilities\n \n-- Claude Code implements and repairs by default (`shiki runner claude`, ADR 0008); Codex implements and repairs tasks explicitly assigned to `codex`.\n+- Claude Code implements and repairs by default (`shiki runner claude`, SADR-0008); Codex implements and repairs tasks explicitly assigned to `codex`.\n - Claude Code Action can act as GitHub-side CCA or reviewer.\n - CCA judges completion.\n - MergeGate authorizes state transitions and merge readiness.\ndiff --git a/.github/prompts/cca-completion-check.md b/.github/prompts/cca-completion-check.md\nindex ed203ad5f302ecc81372d8ab27d2b1232ca97497..fd89950528333ef6242e6d5443904f5392ab7e56\n--- a/.github/prompts/cca-completion-check.md\n+++ b/.github/prompts/cca-completion-check.md\n@@ -19,6 +19,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n ## Required Reading\n \n Read only the files needed to judge the PR contract. Prefer this order:\ndiff --git a/AGENTS.md b/AGENTS.md\nindex 7f041100d49c2a7525f51cb0f5d696a4a6d1c78c..2b700e989ff295987aa8a4b0c5fcb1acd80020da\n--- a/AGENTS.md\n+++ b/AGENTS.md\n@@ -44,6 +44,10 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use\n+`SADR-NNNN-*.md`; target product decisions retain `NNNN-*.md` and the `ADR`\n+namespace.\n+\n Use the canonical source-of-truth order above. Runtime-specific reading orders\n are operational guidance only and must not override or reorder it.\n \ndiff --git a/CLAUDE.md b/CLAUDE.md\nindex 98a932e7841a632866fcd53b0bad0e3af3711d3d..611d491912d6a72a0327a1731b8f33a41acb4baa\n--- a/CLAUDE.md\n+++ b/CLAUDE.md\n@@ -14,6 +14,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n ## Claude Role\n \n Default to planner, default implementer, reviewer, coordinator, documentation editor, CCA-style completion judge, and final-judgment assistant.\ndiff --git a/SYSTEM_PROMPT.md b/SYSTEM_PROMPT.md\nindex aaaea45e3a896c3a44ba89eec8bb84325ecf6c62..2e7c093ebe9e8bb34e0923756f191b4f7fa581f5\n--- a/SYSTEM_PROMPT.md\n+++ b/SYSTEM_PROMPT.md\n@@ -30,6 +30,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n Use the canonical source-of-truth order above. Runtime-specific reading orders\n are operational guidance only and must not override or reorder it. The current\n conversation is non-durable operator input.\ndiff --git a/docs/adr/0001-keep-shiki-as-platform-name.md b/docs/adr/SADR-0001-keep-shiki-as-platform-name.md\nindex 20a9637c3b9d8c97de7ba8db7cfae4d82d9a7dba..8b685439959aada1b221c112f0ac3c51ca8a1695\n--- a/docs/adr/0001-keep-shiki-as-platform-name.md\n+++ b/docs/adr/SADR-0001-keep-shiki-as-platform-name.md\n@@ -1,3 +1,3 @@\n-# Keep Shiki As The Platform Name\n+# SADR-0001: Keep Shiki As The Platform Name\n \n Shiki remains the product name for the GitHub-native agentic engineering control plane. We considered more descriptive names such as MergePilot, MergeGate, and Kiroku, but kept Shiki because it carries the existing goal-seeking framework identity while remaining independent of Hermes, Codex, Claude Code, or any single runtime.\ndiff --git a/docs/adr/0002-use-agents-as-shared-constitution.md b/docs/adr/SADR-0002-use-agents-as-shared-constitution.md\nindex 73de30ecb078120eac540d9bc51855e3cd48d0e3..c5486182bf06fca36ea508496b013c67302fd989\n--- a/docs/adr/0002-use-agents-as-shared-constitution.md\n+++ b/docs/adr/SADR-0002-use-agents-as-shared-constitution.md\n@@ -1,3 +1,3 @@\n-# Use AGENTS As The Shared Agent Constitution\n+# SADR-0002: Use AGENTS As The Shared Agent Constitution\n \n Shiki uses `AGENTS.md` as the runtime-neutral constitution and `CLAUDE.md` as a thin Claude Code wrapper. This keeps Codex, Claude Code, Hermes Runner, and future Agent Runtimes aligned on the same GitHub-first MergeGate rules while still allowing runtime-specific instructions where needed.\ndiff --git a/docs/adr/0003-require-skill-gate-for-mergegate-readiness.md b/docs/adr/SADR-0003-require-skill-gate-for-mergegate-readiness.md\nindex 424be0c9af4c8f0136f1fde68a468c47ff84e764..7fc337987c7df9792f75f181dfcbed75fe5ffff2\n--- a/docs/adr/0003-require-skill-gate-for-mergegate-readiness.md\n+++ b/docs/adr/SADR-0003-require-skill-gate-for-mergegate-readiness.md\n@@ -1,3 +1,3 @@\n-# Require Skill Gate For MergeGate Readiness\n+# SADR-0003: Require Skill Gate For MergeGate Readiness\n \n Shiki requires relevant engineering skills to be invoked before execution when their triggers apply. This makes skills part of MergeGate readiness rather than optional prompt style, because Shiki depends on repeatable planning, issue decomposition, TDD, diagnosis, triage, and architecture review before autonomous or parallel agent work can be trusted.\ndiff --git a/docs/adr/0004-use-github-first-with-shiki-mirror.md b/docs/adr/SADR-0004-use-github-first-with-shiki-mirror.md\nindex d0a3a9174dc646a945a9d325954ffaf00c352040..a79c54ae9ee8eed2a0524192b864da4e22a92d3f\n--- a/docs/adr/0004-use-github-first-with-shiki-mirror.md\n+++ b/docs/adr/SADR-0004-use-github-first-with-shiki-mirror.md\n@@ -1,4 +1,4 @@\n-# ADR 0004: Use GitHub First With A Repository-Local Shiki Mirror\n+# SADR-0004: Use GitHub First With A Repository-Local Shiki Mirror\n \n ## Status\n \ndiff --git a/docs/adr/0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md b/docs/adr/SADR-0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\nindex b339baf49b89d9290e79c3d51b36e7b2bde3bd95..c30a0d8b1fde0224d0ac7820e7870dccf721580d\n--- a/docs/adr/0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\n+++ b/docs/adr/SADR-0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\n@@ -1,4 +1,4 @@\n-# ADR 0005: Use Codex As Front And Claude Code Action As The GitHub Runtime\n+# SADR-0005: Use Codex As Front And Claude Code Action As The GitHub Runtime\n \n ## Status\n \ndiff --git a/docs/adr/0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md b/docs/adr/SADR-0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\nindex 4bcae9d1e52f0a37b8e9fbafa5cd14b6ee1993bd..7a38538a6ae9929567f0ade67e7ea6a60d0b3f7e\n--- a/docs/adr/0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\n+++ b/docs/adr/SADR-0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\n@@ -1,4 +1,4 @@\n-# ADR 0006: Separate Codex Implementation, CCA Completion, And MergeGate Authorization\n+# SADR-0006: Separate Codex Implementation, CCA Completion, And MergeGate Authorization\n \n ## Status\n \ndiff --git a/docs/adr/0007-packaging-and-release.md b/docs/adr/SADR-0007-packaging-and-release.md\nindex 79b84ddb97167372b44641e5b7860e400ce80822..023cd57f8011aa6e6d2ebf612b7c35a7a82c00d4\n--- a/docs/adr/0007-packaging-and-release.md\n+++ b/docs/adr/SADR-0007-packaging-and-release.md\n@@ -1,4 +1,4 @@\n-# ADR 0007: Packaging And Release For Shiki\n+# SADR-0007: Packaging And Release For Shiki\n \n ## Status\n \ndiff --git a/docs/adr/0008-use-claude-code-as-default-implementer.md b/docs/adr/SADR-0008-use-claude-code-as-default-implementer.md\nindex dedf4510b7c7799521422f622029563d373db031..a158a8d444d93764ec515a28133f7dde4af23579\n--- a/docs/adr/0008-use-claude-code-as-default-implementer.md\n+++ b/docs/adr/SADR-0008-use-claude-code-as-default-implementer.md\n@@ -1,4 +1,4 @@\n-# ADR 0008: Use Claude Code As The Default Implementer Runtime\n+# SADR-0008: Use Claude Code As The Default Implementer Runtime\n \n ## Status\n \ndiff --git a/docs/adr/0009-requirements-definition-and-spec-freeze.md b/docs/adr/SADR-0009-requirements-definition-and-spec-freeze.md\nindex 29b3f7871bef9203a8b4a831a65c39eb2f506e42..52e7dd69ad0aa4efe0b68c62678abbf7df02aaec\n--- a/docs/adr/0009-requirements-definition-and-spec-freeze.md\n+++ b/docs/adr/SADR-0009-requirements-definition-and-spec-freeze.md\n@@ -1,4 +1,4 @@\n-# ADR 0009: Merge Grill And Planning Into Requirements Definition With Spec Freeze\n+# SADR-0009: Merge Grill And Planning Into Requirements Definition With Spec Freeze\n \n ## Status\n \ndiff --git a/docs/adr/0010-external-ai-guardian-review.md b/docs/adr/SADR-0010-external-ai-guardian-review.md\nindex e6108bbe8bb4e73e0ff4684c8f78001a80438c4e..e690d4eddd270e683d709c9b679c5859e71eeaaa\n--- a/docs/adr/0010-external-ai-guardian-review.md\n+++ b/docs/adr/SADR-0010-external-ai-guardian-review.md\n@@ -1,4 +1,4 @@\n-# ADR 0010: External AI Guardian Review As A First-Class Approval Authority\n+# SADR-0010: External AI Guardian Review As A First-Class Approval Authority\n \n ## Status\n \ndiff --git a/docs/adr/0011-autonomous-quality-gate-evidence.md b/docs/adr/SADR-0011-autonomous-quality-gate-evidence.md\nindex bca0246c5d46e44afbde15486a75eec425fc02f5..9c93eac16d8c3a6d4b11bd7224e3955102cd7db4\n--- a/docs/adr/0011-autonomous-quality-gate-evidence.md\n+++ b/docs/adr/SADR-0011-autonomous-quality-gate-evidence.md\n@@ -1,4 +1,4 @@\n-# ADR 0011: Autonomous Quality-Gate Evidence \u2014 Deterministic Facts And An Independent Verifier\n+# SADR-0011: Autonomous Quality-Gate Evidence \u2014 Deterministic Facts And An Independent Verifier\n \n ## Status\n \ndiff --git a/docs/adr/0012-autonomous-loop-closeout-to-main.md b/docs/adr/SADR-0012-autonomous-loop-closeout-to-main.md\nindex 637fad6dde4989c9c2bf0f81f676d8d29991123c..ec7d85f81030a676bcbba28b43eb365422a36eac\n--- a/docs/adr/0012-autonomous-loop-closeout-to-main.md\n+++ b/docs/adr/SADR-0012-autonomous-loop-closeout-to-main.md\n@@ -1,4 +1,4 @@\n-# 0012 \u2014 Autonomous loop pushes goal completion to main via a closeout PR\n+# SADR-0012 \u2014 Autonomous loop pushes goal completion to main via a closeout PR\n \n - Status: accepted\n - Date: 2026-06-18\ndiff --git a/docs/adr/0013-bootstrap-grants-actions-approve-pull-requests.md b/docs/adr/SADR-0013-bootstrap-grants-actions-approve-pull-requests.md\nindex b6c69d64f1c5ced346b88d6859586715b3b5bd0e..a793e7277d67ab8ea56286fec289ca689825a17d\n--- a/docs/adr/0013-bootstrap-grants-actions-approve-pull-requests.md\n+++ b/docs/adr/SADR-0013-bootstrap-grants-actions-approve-pull-requests.md\n@@ -1,4 +1,4 @@\n-# ADR 0013: Bootstrap Grants GitHub Actions Permission To Create And Approve Pull Requests\n+# SADR-0013: Bootstrap Grants GitHub Actions Permission To Create And Approve Pull Requests\n \n ## Status\n \ndiff --git a/docs/adr/0014-codex-app-external-ai-guardian-ui-adapter.md b/docs/adr/SADR-0014-codex-app-external-ai-guardian-ui-adapter.md\nindex 238db06279334caa341c117362df9271382a7fcd..65f12e9939321e1b3e389a34c822fc1549e5155d\n--- a/docs/adr/0014-codex-app-external-ai-guardian-ui-adapter.md\n+++ b/docs/adr/SADR-0014-codex-app-external-ai-guardian-ui-adapter.md\n@@ -1,4 +1,4 @@\n-# ADR 0014: Use Codex App As The External AI Guardian UI Adapter\n+# SADR-0014: Use Codex App As The External AI Guardian UI Adapter\n \n ## Status\n \ndiff --git a/docs/adr/0015-contract-approval-before-dispatch.md b/docs/adr/SADR-0015-contract-approval-before-dispatch.md\nindex fad6c656741a20a7684ba16080a7db159f63a52f..7bdd760bb3e82eee4e1410f45c783b03b638d456\n--- a/docs/adr/0015-contract-approval-before-dispatch.md\n+++ b/docs/adr/SADR-0015-contract-approval-before-dispatch.md\n@@ -1,4 +1,4 @@\n-# ADR 0015: Approve The Task Contract Before Dispatch, Not The Diff After It\n+# SADR-0015: Approve The Task Contract Before Dispatch, Not The Diff After It\n \n ## Status\n \ndiff --git a/docs/adr/0016-locks-bound-product-paths.md b/docs/adr/SADR-0016-locks-bound-product-paths.md\nindex b5e850f3f1f7f4ef6e7f9d1763a29a51bd300c45..332cc17313498404f41a145291e38447b5de0dc4\n--- a/docs/adr/0016-locks-bound-product-paths.md\n+++ b/docs/adr/SADR-0016-locks-bound-product-paths.md\n@@ -1,4 +1,4 @@\n-# ADR 0016: Locks bound product paths; the Shiki mirror is bound by record identity\n+# SADR-0016: Locks bound product paths; the Shiki mirror is bound by record identity\n \n ## Status\n \ndiff --git a/docs/adr/0017-bookkeeping-closeouts-do-not-inherit-risk.md b/docs/adr/SADR-0017-bookkeeping-closeouts-do-not-inherit-risk.md\nindex 554e3a0d526a72ca067108c5464cc68c3edcf174..83115f69f1b1b48d544c9b0157f377b91cb53abf\n--- a/docs/adr/0017-bookkeeping-closeouts-do-not-inherit-risk.md\n+++ b/docs/adr/SADR-0017-bookkeeping-closeouts-do-not-inherit-risk.md\n@@ -1,4 +1,4 @@\n-# ADR 0017: A Bookkeeping Closeout Does Not Inherit Its Task's Risk\n+# SADR-0017: A Bookkeeping Closeout Does Not Inherit Its Task's Risk\n \n ## Status\n \ndiff --git a/docs/adr/0018-carried-guardian-approval-across-a-proven-base-sync.md b/docs/adr/SADR-0018-carried-guardian-approval-across-a-proven-base-sync.md\nindex 22358562da390ed8b45cd600da08a43d378b4680..38da0fd95102c3496c3219ce2319fce696a2532a\n--- a/docs/adr/0018-carried-guardian-approval-across-a-proven-base-sync.md\n+++ b/docs/adr/SADR-0018-carried-guardian-approval-across-a-proven-base-sync.md\n@@ -1,4 +1,4 @@\n-# ADR 0018: Carried Guardian Approval Across A Proven Base Sync\n+# SADR-0018: Carried Guardian Approval Across A Proven Base Sync\n \n ## Status\n \ndiff --git a/docs/adr/README.md b/docs/adr/README.md\nindex c70e057a4520c36e042ab68c53cd4a21ef709b71..5da43856836e2ce6a150f8127766e2f88355ab4b\n--- a/docs/adr/README.md\n+++ b/docs/adr/README.md\n@@ -1,40 +1,41 @@\n-# Architecture Decision Records\n+# Decision Record Namespaces\n \n-This directory holds Shiki's Architecture Decision Records (ADRs): durable\n-records of hard-to-reverse platform decisions. ADRs are part of the Shiki source\n-of truth alongside GitHub state, the `.shiki/` mirror, and `CONTEXT.md`.\n+This directory holds two independent decision namespaces:\n \n-## How ADRs Work\n+- Shiki Architecture Decision Records (SADRs), named\n+  `SADR-NNNN-<short-slug>.md`, record hard-to-reverse Shiki platform decisions.\n+- Target Architecture Decision Records (ADRs), named\n+  `NNNN-<short-slug>.md`, belong to the target product.\n \n-- New ADRs are added as `docs/adr/NNNN-<short-slug>.md`, where `NNNN` is the next\n-  zero-padded sequence number.\n-- The **canonical list of ADRs is the set of files matching `docs/adr/*.md`** in\n-  this directory. Browse `docs/adr/*.md` for the authoritative, current set.\n-- Use [`template.md`](template.md) as the starting point for a new ADR.\n-- Record an ADR when a decision is hard to reverse, surprising without context,\n-  and a real tradeoff. Do not write an ADR for routine or easily reversible\n-  choices.\n+The same four-digit number may appear once in each namespace. A platform\n+`SADR-NNNN` citation resolves only to `SADR-NNNN-*.md`; a target `ADR NNNN`\n+citation resolves only to `NNNN-*.md`.\n \n-Using the `docs/adr/*.md` glob as the source of truth means other PRs can add an\n-ADR without editing this index file, avoiding merge contention on a\n-hand-maintained enumeration.\n+## How Decision Records Work\n \n-## Current ADRs (snapshot)\n-\n-The list below is a convenience snapshot and may lag behind the directory. When\n-in doubt, list `docs/adr/*.md`.\n+- New Shiki platform decisions use the next zero-padded SADR number.\n+- New target product decisions use the target's own ADR sequence.\n+- The canonical list is the files in `docs/adr/`; no hand-maintained index is\n+  authoritative.\n+- Use [`template.md`](template.md) as the starting point and choose the filename\n+  namespace that owns the decision.\n+- Record a decision when it is hard to reverse, surprising without context, and\n+  a real tradeoff. Do not write a decision record for routine or easily\n+  reversible choices.\n \n-- [0001 \u2014 Keep Shiki As The Platform Name](0001-keep-shiki-as-platform-name.md)\n-- [0002 \u2014 Use AGENTS As The Shared Agent Constitution](0002-use-agents-as-shared-constitution.md)\n-- [0003 \u2014 Require Skill Gate For MergeGate Readiness](0003-require-skill-gate-for-mergegate-readiness.md)\n-- [0004 \u2014 Use GitHub First With A Repository-Local Shiki Mirror](0004-use-github-first-with-shiki-mirror.md)\n-- [0005 \u2014 Use Codex As Front And Claude Code Action As The GitHub Runtime](0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md)\n-- [0006 \u2014 Separate Codex Implementation, CCA Completion, And MergeGate Authorization](0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md)\n+Using the directory as the source of truth lets independent changes add records\n+without contending on an enumerated index.\n \n-## Adding A New ADR\n+## Adding A Shiki SADR\n \n-1. Copy `template.md` to `docs/adr/NNNN-<short-slug>.md`.\n+1. Copy `template.md` to `docs/adr/SADR-NNNN-<short-slug>.md`.\n 2. Fill in the title, status, context, decision, and consequences.\n-3. Set the status to `Proposed`, then `Accepted` once the decision is approved.\n-4. Open a PR following `CONTRIBUTING.md`. You do not need to edit this index for\n-   the ADR to be discoverable via `docs/adr/*.md`.\n+3. Set the status to `Proposed`, then `Accepted` once the proper authority has\n+   approved the decision.\n+4. Open a PR following `CONTRIBUTING.md`.\n+\n+## Adding A Target ADR\n+\n+1. Copy `template.md` to `docs/adr/NNNN-<short-slug>.md` using the target's own\n+   sequence.\n+2. Cite it as `ADR NNNN`; do not prefix its filename with `SADR-`.\ndiff --git a/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md b/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\nnew file mode 100644\nindex 0000000000000000000000000000000000000000..7455ce1f3bed02ddddb851ac21a1d1a648c9aeba\n--- /dev/null\n+++ b/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\n@@ -0,0 +1,55 @@\n+# SADR-0019: Separate Shiki Platform SADRs From Target ADRs\n+\n+## Status\n+\n+Accepted\n+\n+## Context\n+\n+Shiki installs its platform decision records into `docs/adr/` in every target\n+repository. Shiki and its targets previously used the same numeric\n+`NNNN-<slug>.md` namespace. A target that already owned ADRs 0013 through 0018\n+therefore acquired two records for each number during installation, making an\n+`ADR NNNN` citation ambiguous. Renumbering target decisions would rewrite target\n+history, while renumbering Shiki decisions per target would destroy their stable\n+platform identity.\n+\n+Upgrades also need to remove the former Shiki-owned numeric paths without ever\n+mistaking a target-owned ADR for platform content. The install stamp already\n+records exact shipped paths and content digests, so it can prove ownership only\n+when both still match.\n+\n+## Decision\n+\n+Shiki platform decisions use the `SADR` namespace:\n+\n+- records are named `docs/adr/SADR-NNNN-<slug>.md`;\n+- live platform citations use `SADR-NNNN`;\n+- target product decisions retain `docs/adr/NNNN-<slug>.md` and `ADR NNNN`;\n+- the reference validator resolves the two namespaces independently, so the same\n+  four-digit number may exist once in each namespace.\n+\n+Before a forced upgrade writes anything, the installer inventories the exact\n+former numeric Shiki ADR paths. It may delete a legacy path only when the\n+existing install stamp contains that exact path and its recorded SHA-256 digest\n+matches the file currently on disk. All ownership blockers are reported\n+together and abort the upgrade before its first write. A non-forced install\n+never performs this cleanup.\n+\n+Historical `.shiki/` evidence remains unchanged because it records the namespace\n+that existed when that evidence was produced.\n+\n+## Consequences\n+\n+- Targets may use any ADR number without colliding with Shiki's platform SADRs.\n+- Fresh installs preserve target ADR bytes and install a stable SADR set.\n+- Forced upgrades fail closed when legacy ownership cannot be proven, and a\n+  successful upgrade refreshes the stamp without removed legacy paths.\n+- Current governance, packaging, workflow, prompt, script, and skill references\n+  must move with the platform records.\n+\n+## Alternatives Considered\n+\n+- Collision-only refusal leaves affected targets permanently unupgradable.\n+- Renumbering target ADRs mutates target-owned history.\n+- Per-target Shiki renumbering makes platform citations non-portable.\ndiff --git a/docs/adr/template.md b/docs/adr/template.md\nindex a92a97daddd0b00ab9dd626cc8d86942da2eafed..17dd79175b5fee2105935557f691b40cf9492942\n--- a/docs/adr/template.md\n+++ b/docs/adr/template.md\n@@ -1,14 +1,14 @@\n-# NNNN: <Short Decision Title>\n+# <SADR-NNNN or NNNN>: <Short Decision Title>\n \n ## Status\n \n-Proposed | Accepted | Superseded by ADR-NNNN\n+Proposed | Accepted | Superseded by SADR-NNNN or target ADR NNNN\n \n ## Context\n \n What is the situation, constraint, or force that requires a decision? Reference\n-relevant domain terms (`CONTEXT.md`), prior ADRs, and code reality. State the\n-problem, not the solution.\n+relevant domain terms (`CONTEXT.md`), prior Shiki SADRs or target ADRs, and code\n+reality. State the problem, not the solution.\n \n ## Decision\n \ndiff --git a/docs/agents/domain.md b/docs/agents/domain.md\nindex 258da8ea81e87ac0a6b0b5b5e5c2fde980eaabb7..67aaa6f4b7d0a5aa4d495ce6279073333f7798cb\n--- a/docs/agents/domain.md\n+++ b/docs/agents/domain.md\n@@ -5,7 +5,9 @@\n ## Layout\n \n - `CONTEXT.md` defines Shiki language. Keep it implementation-free.\n-- `docs/adr/` records hard-to-reverse decisions.\n+- `docs/adr/SADR-NNNN-*.md` records Shiki platform decisions.\n+- `docs/adr/NNNN-*.md` records target product decisions without sharing the\n+  SADR namespace.\n - `AGENTS.md` is the runtime-neutral agent constitution.\n - `CLAUDE.md` is the Claude Code wrapper around `AGENTS.md`.\n - `.shiki/` is the Target Repository mirror for Goals, PRDs, plans, Task DAGs, contracts, locks, worktree records, ledger entries, CCA verdicts, repair packets, reports, and handoffs.\ndiff --git a/docs/agents/implementation-policy.md b/docs/agents/implementation-policy.md\nindex c754144a9fb5bf4a38aa82a60e1670c59e626b44..e90d40fe21880b9a09719c5ecfe6bde7284eb3ce\n--- a/docs/agents/implementation-policy.md\n+++ b/docs/agents/implementation-policy.md\n@@ -31,6 +31,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n ## Non-Negotiables\n \n 1. **Planning precedes implementation.** Non-trivial Goals start with `grill-with-docs` and Context & Impact.\ndiff --git a/scripts/shiki_installer.py b/scripts/shiki_installer.py\nindex be064ab7a29ff6836f7f7a1d70500a43e0d11d7b..d3e3247edbf3e4be287a4667176d86f6500f8dc5\n--- a/scripts/shiki_installer.py\n+++ b/scripts/shiki_installer.py\n@@ -118,6 +118,7 @@\n # what lets a target carry it without a governance-schema change.\n INSTALL_STAMP_PATH = \".shiki/install-stamp.json\"\n INSTALL_STAMP_VERSION = 1\n+LEGACY_SHIKI_SADR_MAX = 18\n \n # Under --force the shipped surface splits three ways, so nothing is lost and\n # nothing is silently stale.\n@@ -237,9 +238,98 @@\n         )\n \n \n+def legacy_shiki_adr_paths() -> tuple[str, ...]:\n+    \"\"\"Former numeric paths for Shiki SADR-0001 through SADR-0018.\n+\n+    The authoritative slugs come from the current SADR filenames. SADR-0019 is\n+    the namespace decision itself and never shipped under a numeric legacy path.\n+    \"\"\"\n+    paths: list[str] = []\n+    for sadr in sorted((ROOT / \"docs\" / \"adr\").glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\")):\n+        number = sadr.name.split(\"-\", 2)[1]\n+        if int(number) <= LEGACY_SHIKI_SADR_MAX:\n+            paths.append(f\"docs/adr/{sadr.name.removeprefix('SADR-')}\")\n+    return tuple(paths)\n+\n+\n+def preflight_legacy_sadr_cleanup(target: Path, *, force: bool) -> tuple[str, ...]:\n+    \"\"\"Authorize exact legacy Shiki ADR removals before the first write.\n+\n+    A non-force install never removes existing files. A forced install needs no\n+    stamp when no legacy Shiki path is present. When legacy paths do exist, each\n+    must be named in a readable install stamp and its current bytes must match\n+    the recorded digest.\n+    \"\"\"\n+    if not force:\n+        return ()\n+    present = tuple(\n+        relative\n+        for relative in legacy_shiki_adr_paths()\n+        if (target / relative).exists() or (target / relative).is_symlink()\n+    )\n+    if not present:\n+        return ()\n+\n+    stamp_path = target / INSTALL_STAMP_PATH\n+    try:\n+        raw = stamp_path.read_text(encoding=\"utf-8\")\n+        stamp = json.loads(raw)\n+    except FileNotFoundError:\n+        detail = \"install stamp is absent\"\n+        blockers = [f\"{relative}: {detail}\" for relative in present]\n+    except (OSError, UnicodeDecodeError) as error:\n+        detail = f\"install stamp is unreadable ({error})\"\n+        blockers = [f\"{relative}: {detail}\" for relative in present]\n+    except json.JSONDecodeError as error:\n+        detail = f\"install stamp is malformed ({error})\"\n+        blockers = [f\"{relative}: {detail}\" for relative in present]\n+    else:\n+        if not isinstance(stamp, dict):\n+            blockers = [\n+                f\"{relative}: install stamp is malformed (expected an object)\"\n+                for relative in present\n+            ]\n+        else:\n+            digests = stamp.get(\"digests\")\n+            blockers = []\n+            if not isinstance(digests, dict):\n+                blockers.extend(\n+                    f\"{relative}: install stamp has no digest for this path\"\n+                    for relative in present\n+                )\n+            else:\n+                for relative in present:\n+                    expected = digests.get(relative)\n+                    if not isinstance(expected, str) or not expected:\n+                        blockers.append(\n+                            f\"{relative}: install stamp has no digest for this path\"\n+                        )\n+                        continue\n+                    try:\n+                        actual = _sha256_file(target / relative)\n+                    except OSError as error:\n+                        blockers.append(\n+                            f\"{relative}: legacy path is unreadable ({error})\"\n+                        )\n+                        continue\n+                    if actual != expected:\n+                        blockers.append(\n+                            f\"{relative}: digest mismatch (stamp {expected}, current {actual})\"\n+                        )\n+\n+    if blockers:\n+        joined = \"\\n\".join(f\"- {blocker}\" for blocker in blockers)\n+        raise ShikiError(\n+            \"legacy Shiki ADR cleanup is not authorized:\\n\"\n+            f\"{joined}\\nNo files were written.\"\n+        )\n+    return present\n+\n+\n def install_template(target: Path, *, force: bool, validate: bool) -> list[NewFileNote]:\n     # Preconditions first: never rewrite the tree of a target that cannot be\n     # upgraded cleanly (see refuse_pending_migrations).\n+    legacy_cleanup = preflight_legacy_sadr_cleanup(target, force=force)\n     refuse_pending_migrations(target)\n \n     # Resolve the target's required CODEOWNERS owner once from its\n@@ -250,6 +340,10 @@\n     # verbatim. See ``codeowners_required_owner``.\n     codeowners_owner = codeowners_required_owner(target)\n \n+    for relative in legacy_cleanup:\n+        (target / relative).unlink()\n+        info(f\"removed stamped legacy Shiki ADR: {target / relative}\")\n+\n     notes: list[NewFileNote] = []\n     for relative in TEMPLATE_PATHS:\n         source = ROOT / relative\ndiff --git a/tests/test_adr_references.py b/tests/test_adr_references.py\nindex 51706329c797d5dd9b47e082a333fa83b261d482..91ab76b724cb973435c45d53cab63793942267e9\n--- a/tests/test_adr_references.py\n+++ b/tests/test_adr_references.py\n@@ -1,4 +1,4 @@\n-\"\"\"Every ``ADR ####`` citation must resolve to exactly one decision record.\n+\"\"\"Every target ADR and Shiki SADR citation resolves in its own namespace.\n \n Background. ``scripts/mergegate_check.py`` justifies ``is_bookkeeping_closeout``\n with \"ADR 0017\" nine times and ``scripts/guardian_approval_signal.py`` cites\n@@ -11,11 +11,12 @@\n and the directory drifted with no signal.\n \n This is the same unbound-pair shape behind other silent failures: a reference\n-and its target with nothing asserting they agree. This test binds them. It scans\n-every ``scripts/*.py`` (plus ``docs/**/*.md`` and the root ``*.md`` governance\n-files, which are cheap) for the ``ADR ####`` pattern and asserts each cited\n-number resolves to exactly one ``docs/adr/####-*.md``. The converse is\n-deliberately NOT required: an ADR may exist without being cited.\n+and its target with nothing asserting they agree. This test binds them. Target\n+``ADR NNNN`` citations resolve only to ``docs/adr/NNNN-*.md``; Shiki\n+``SADR-NNNN`` citations resolve only to ``docs/adr/SADR-NNNN-*.md``. The scan\n+covers current governance docs, scripts, workflows, prompts, packaging metadata,\n+and repo-local Codex guidance while deliberately excluding historical\n+``.shiki`` evidence. The converse is not required: a record may be uncited.\n \n The suite is dependency free: standard-library ``unittest`` only.\n \"\"\"\n@@ -31,17 +32,18 @@\n \n REPO_ROOT = Path(__file__).resolve().parents[1]\n \n-# \"ADR 0016\" -- a four-digit citation. The trailing \\b keeps a five-digit token\n-# from being read as a truncated four-digit number.\n-_CITATION = re.compile(r\"\\bADR (\\d{4})\\b\")\n-# \"0016-locks-bound-product-paths.md\" -- the leading four digits name the record.\n-_ADR_FILENAME = re.compile(r\"^(\\d{4})-.*\\.md$\")\n+# ``ADR 0016`` / ``SADR-0016`` -- a namespace plus four-digit citation. The\n+# trailing ``\\b`` keeps a five-digit token from being read as a truncated one.\n+_CITATION = re.compile(r\"\\b(S?ADR)[ -](\\d{4})\\b\")\n+# Target ADRs retain the numeric filename; Shiki platform decisions use SADR.\n+_DECISION_FILENAME = re.compile(r\"^(?:(SADR)-)?(\\d{4})-.*\\.md$\")\n \n \n class Violation(NamedTuple):\n     \"\"\"A citation that does not resolve to exactly one decision record.\"\"\"\n \n     citing_file: str  # repo-relative path of the file that cites the ADR\n+    namespace: str  # \"ADR\" for target decisions; \"SADR\" for Shiki decisions\n     number: str  # the cited four-digit number, e.g. \"0016\"\n     line: int  # 1-based line of the citation\n     reason: str  # \"missing\" (zero matches) or \"ambiguous\" (two or more)\n@@ -49,44 +51,59 @@\n \n     @property\n     def expected_path(self) -> str:\n-        return f\"docs/adr/{self.number}-*.md\"\n+        prefix = \"SADR-\" if self.namespace == \"SADR\" else \"\"\n+        return f\"docs/adr/{prefix}{self.number}-*.md\"\n \n     def describe(self) -> str:\n         \"\"\"Name the citing file, the cited number, and the missing path.\"\"\"\n         if self.reason == \"missing\":\n             return (\n-                f\"{self.citing_file}:{self.line} cites ADR {self.number} \"\n+                f\"{self.citing_file}:{self.line} cites {self.namespace} {self.number} \"\n                 f\"but no document matches {self.expected_path}\"\n             )\n         joined = \", \".join(self.matches)\n         return (\n-            f\"{self.citing_file}:{self.line} cites ADR {self.number} which \"\n+            f\"{self.citing_file}:{self.line} cites {self.namespace} {self.number} which \"\n             f\"resolves to {len(self.matches)} documents (expected exactly one \"\n             f\"at {self.expected_path}): {joined}\"\n         )\n \n \n-def _adr_index(root: Path) -> dict[str, list[Path]]:\n-    \"\"\"Map every four-digit ADR number to the record files that claim it.\"\"\"\n-    index: dict[str, list[Path]] = {}\n+def _adr_index(root: Path) -> dict[tuple[str, str], list[Path]]:\n+    \"\"\"Map each decision namespace and number to the files that claim it.\"\"\"\n+    index: dict[tuple[str, str], list[Path]] = {}\n     adr_dir = root / \"docs\" / \"adr\"\n     if adr_dir.is_dir():\n         for path in sorted(adr_dir.glob(\"*.md\")):\n-            match = _ADR_FILENAME.match(path.name)\n+            match = _DECISION_FILENAME.match(path.name)\n             if match:\n-                index.setdefault(match.group(1), []).append(path)\n+                namespace = \"SADR\" if match.group(1) else \"ADR\"\n+                index.setdefault((namespace, match.group(2)), []).append(path)\n     return index\n \n \n def _citing_files(root: Path, include_docs: bool = True) -> list[Path]:\n-    \"\"\"Files scanned for citations: scripts/*.py, and cheaply the markdown.\"\"\"\n+    \"\"\"Current, shipped text surfaces scanned for decision citations.\"\"\"\n     files = sorted((root / \"scripts\").glob(\"*.py\"))\n+    files += sorted((root / \"scripts\").glob(\"*.sh\"))\n     if include_docs:\n         docs = root / \"docs\"\n         if docs.is_dir():\n             files += sorted(docs.rglob(\"*.md\"))\n         files += sorted(root.glob(\"*.md\"))\n-    return files\n+        pyproject = root / \"pyproject.toml\"\n+        if pyproject.is_file():\n+            files.append(pyproject)\n+        for relative in (\".github\", \".codex\"):\n+            surface = root / relative\n+            if surface.is_dir():\n+                files += sorted(\n+                    path\n+                    for path in surface.rglob(\"*\")\n+                    if path.is_file()\n+                    and path.suffix in {\".md\", \".py\", \".sh\", \".toml\", \".yaml\", \".yml\"}\n+                )\n+    return sorted(set(files))\n \n \n def find_adr_reference_violations(\n@@ -94,10 +111,9 @@\n ) -> list[Violation]:\n     \"\"\"Return every ``ADR ####`` citation under ``root`` that fails to resolve.\n \n-    A citation resolves when exactly one ``docs/adr/####-*.md`` matches its\n-    number. Zero matches is a \"missing\" violation; two or more is \"ambiguous\".\n-    Records that are never cited produce nothing -- the converse is not\n-    required.\n+    A citation resolves when exactly one record in the same namespace matches\n+    its number. Zero matches is a \"missing\" violation; two or more is\n+    \"ambiguous\". Records that are never cited produce nothing.\n     \"\"\"\n     root = Path(root)\n     index = _adr_index(root)\n@@ -110,13 +126,15 @@\n         rel = path.relative_to(root).as_posix()\n         for lineno, line in enumerate(text.splitlines(), start=1):\n             for match in _CITATION.finditer(line):\n-                number = match.group(1)\n-                found = index.get(number, [])\n+                namespace, number = match.groups()\n+                found = index.get((namespace, number), [])\n                 if len(found) == 1:\n                     continue\n                 reason = \"missing\" if not found else \"ambiguous\"\n                 rels = [p.relative_to(root).as_posix() for p in found]\n-                violations.append(Violation(rel, number, lineno, reason, rels))\n+                violations.append(\n+                    Violation(rel, namespace, number, lineno, reason, rels)\n+                )\n     return violations\n \n \n@@ -129,8 +147,8 @@\n         self.assertEqual(\n             violations,\n             [],\n-            \"Every 'ADR ####' citation must resolve to exactly one \"\n-            f\"docs/adr/####-*.md:\\n{detail}\",\n+            \"Every ADR/SADR citation must resolve exactly once in its own \"\n+            f\"docs/adr namespace:\\n{detail}\",\n         )\n \n     def test_scan_actually_reads_scripts(self) -> None:\n@@ -146,8 +164,21 @@\n         # The two records this Goal carried onto the default branch, and which\n         # the scripts cite most, must be present.\n         index = _adr_index(REPO_ROOT)\n-        self.assertIn(\"0016\", index)\n-        self.assertIn(\"0017\", index)\n+        self.assertIn((\"SADR\", \"0016\"), index)\n+        self.assertIn((\"SADR\", \"0017\"), index)\n+\n+    def test_platform_tree_has_only_sadr_0001_through_0019(self) -> None:\n+        adr_dir = REPO_ROOT / \"docs\" / \"adr\"\n+        sadrs = sorted(adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\"))\n+        self.assertEqual(\n+            {path.name.split(\"-\", 2)[1] for path in sadrs},\n+            {f\"{number:04d}\" for number in range(1, 20)},\n+        )\n+        self.assertEqual(\n+            list(adr_dir.glob(\"[0-9][0-9][0-9][0-9]-*.md\")),\n+            [],\n+            \"the Shiki platform tree must not retain legacy numeric SADR paths\",\n+        )\n \n \n class FixtureAdrReferences(unittest.TestCase):\n@@ -180,6 +211,26 @@\n         self.assertIn(\"9991\", message)  # cited number\n         self.assertIn(\"docs/adr/9991-*.md\", message)  # missing path\n \n+    def test_target_adr_does_not_satisfy_missing_platform_sadr(self) -> None:\n+        root = self._make_repo(\n+            adr_names=[\"0042-target-decision.md\"],\n+            script_body='\"\"\"Target ADR 0042 exists, but platform SADR-0042 does not.\"\"\"\\n',\n+        )\n+        violations = find_adr_reference_violations(root, include_docs=False)\n+        self.assertEqual(len(violations), 1)\n+        self.assertEqual(violations[0].namespace, \"SADR\")\n+        self.assertEqual(violations[0].number, \"0042\")\n+        self.assertEqual(violations[0].expected_path, \"docs/adr/SADR-0042-*.md\")\n+\n+    def test_target_adr_and_platform_sadr_may_reuse_a_number(self) -> None:\n+        root = self._make_repo(\n+            adr_names=[\"0042-target-decision.md\", \"SADR-0042-platform-decision.md\"],\n+            script_body='\"\"\"Target ADR 0042 and platform SADR-0042 are distinct.\"\"\"\\n',\n+        )\n+        self.assertEqual(\n+            find_adr_reference_violations(root, include_docs=False), []\n+        )\n+\n     def test_uncited_adr_is_allowed(self) -> None:\n         # An ADR exists but nothing cites it -> not a violation. The converse\n         # (every ADR must be cited) is explicitly NOT required.\ndiff --git a/tests/test_sadr_install_migration.py b/tests/test_sadr_install_migration.py\nnew file mode 100644\nindex 0000000000000000000000000000000000000000..7a88682efad63c022b90ffdc45395f7ee7b3da81\n--- /dev/null\n+++ b/tests/test_sadr_install_migration.py\n@@ -0,0 +1,141 @@\n+\"\"\"SADR installation and collision-safe legacy upgrade behavior.\"\"\"\n+\n+from __future__ import annotations\n+\n+import contextlib\n+import hashlib\n+import io\n+import json\n+import shutil\n+import tempfile\n+import unittest\n+from pathlib import Path\n+\n+import shiki_test_support  # noqa: F401  (path bootstrap)\n+\n+import shiki_installer\n+from tests.test_adr_references import find_adr_reference_violations\n+\n+\n+PRODUCT_NUMBERS = tuple(range(13, 19))\n+\n+\n+def _quiet_install(target: Path, *, force: bool, validate: bool) -> None:\n+    with contextlib.redirect_stdout(io.StringIO()):\n+        shiki_installer.install_template(target, force=force, validate=validate)\n+\n+\n+def _sha256(path: Path) -> str:\n+    return hashlib.sha256(path.read_bytes()).hexdigest()\n+\n+\n+def _legacy_sadr_paths() -> tuple[str, ...]:\n+    return tuple(\n+        f\"docs/adr/{path.name.removeprefix('SADR-')}\"\n+        for path in sorted((shiki_installer.ROOT / \"docs\" / \"adr\").glob(\"SADR-*.md\"))\n+        if path.name != \"SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\"\n+    )\n+\n+\n+def _create_legacy_upgrade_fixture(\n+    target: Path,\n+) -> tuple[dict[str, bytes], tuple[str, ...]]:\n+    adr_dir = target / \"docs\" / \"adr\"\n+    adr_dir.mkdir(parents=True)\n+    product_bytes = {\n+        f\"{number:04d}-cubeshare-product-decision.md\": (\n+            f\"# CubeShare ADR {number:04d}\\n\\nDo not rewrite {number}.\\n\"\n+        ).encode()\n+        for number in PRODUCT_NUMBERS\n+    }\n+    for name, content in product_bytes.items():\n+        (adr_dir / name).write_bytes(content)\n+\n+    legacy_paths = _legacy_sadr_paths()\n+    for relative in legacy_paths:\n+        (target / relative).write_bytes(f\"legacy Shiki bytes for {relative}\\n\".encode())\n+\n+    stamp_path = target / shiki_installer.INSTALL_STAMP_PATH\n+    stamp_path.parent.mkdir(parents=True, exist_ok=True)\n+    stamp = {\n+        \"version\": shiki_installer.INSTALL_STAMP_VERSION,\n+        \"platform_commit\": None,\n+        \"installed_at\": \"2000-01-01T00:00:00+00:00\",\n+        \"digests\": {\n+            relative: _sha256(target / relative) for relative in legacy_paths\n+        },\n+    }\n+    stamp_path.write_text(\n+        json.dumps(stamp, indent=2, sort_keys=True) + \"\\n\", encoding=\"utf-8\"\n+    )\n+    return product_bytes, legacy_paths\n+\n+\n+class FreshSadrInstallTests(unittest.TestCase):\n+    def setUp(self) -> None:\n+        self.tmp = Path(tempfile.mkdtemp(prefix=\"shiki-sadr-fresh-\"))\n+        self.addCleanup(lambda: shutil.rmtree(self.tmp, ignore_errors=True))\n+        self.target = self.tmp / \"target\"\n+        self.adr_dir = self.target / \"docs\" / \"adr\"\n+        self.adr_dir.mkdir(parents=True)\n+        self.product_bytes = {\n+            f\"{number:04d}-product-decision.md\": (\n+                f\"# Product ADR {number:04d}\\n\\nTarget-owned bytes {number}.\\n\"\n+            ).encode()\n+            for number in PRODUCT_NUMBERS\n+        }\n+        for name, content in self.product_bytes.items():\n+            (self.adr_dir / name).write_bytes(content)\n+\n+    def test_fresh_install_preserves_product_adrs_and_installs_all_sadrs(self) -> None:\n+        _quiet_install(self.target, force=False, validate=True)\n+\n+        for name, expected in self.product_bytes.items():\n+            self.assertEqual((self.adr_dir / name).read_bytes(), expected)\n+\n+        sadr_numbers = {\n+            path.name.split(\"-\", 2)[1]\n+            for path in self.adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\")\n+        }\n+        self.assertEqual(sadr_numbers, {f\"{number:04d}\" for number in range(1, 20)})\n+\n+        numeric_records = {\n+            path.name\n+            for path in self.adr_dir.glob(\"[0-9][0-9][0-9][0-9]-*.md\")\n+        }\n+        self.assertEqual(numeric_records, set(self.product_bytes))\n+        self.assertEqual(find_adr_reference_violations(self.target), [])\n+\n+\n+class LegacySadrUpgradeTests(unittest.TestCase):\n+    def setUp(self) -> None:\n+        self.tmp = Path(tempfile.mkdtemp(prefix=\"shiki-sadr-upgrade-\"))\n+        self.addCleanup(lambda: shutil.rmtree(self.tmp, ignore_errors=True))\n+        self.target = self.tmp / \"target\"\n+        self.adr_dir = self.target / \"docs\" / \"adr\"\n+        self.product_bytes, self.legacy_paths = _create_legacy_upgrade_fixture(\n+            self.target\n+        )\n+\n+    def test_force_upgrade_deletes_only_stamped_legacy_sadrs(self) -> None:\n+        _quiet_install(self.target, force=True, validate=True)\n+\n+        for relative in self.legacy_paths:\n+            self.assertFalse((self.target / relative).exists(), relative)\n+        for name, expected in self.product_bytes.items():\n+            self.assertEqual((self.adr_dir / name).read_bytes(), expected)\n+\n+        sadrs = list(self.adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\"))\n+        self.assertEqual(len(sadrs), 19)\n+        refreshed = json.loads(\n+            (self.target / shiki_installer.INSTALL_STAMP_PATH).read_text(encoding=\"utf-8\")\n+        )\n+        for relative in self.legacy_paths:\n+            self.assertNotIn(relative, refreshed[\"digests\"])\n+        for path in sadrs:\n+            relative = path.relative_to(self.target).as_posix()\n+            self.assertEqual(refreshed[\"digests\"][relative], _sha256(path))\n+\n+\n+if __name__ == \"__main__\":\n+    unittest.main()\n\n2026-08-07T08:12:01.163637Z ERROR codex_models_manager::manager: failed to renew cache TTL: missing field `base_instructions` at line 94 column 5\nexec\n/bin/zsh -lc \"python3 -m unittest discover -s tests -p 'test_sadr_install_migration.py' -q\" in /private/tmp/shiki-adr-collision.pa0KwY/.worktrees/shiki-t-20260807t073307221254z-2d22d654-introduc\ndiff --git a/.codex/skills/shiki/SKILL.md b/.codex/skills/shiki/SKILL.md\nindex c8e2e85d9d75d49cc4f2d6286ee22659c4e53e75..93cded623b2c15d9f65c76491a1c2d813ac9e6c0\n--- a/.codex/skills/shiki/SKILL.md\n+++ b/.codex/skills/shiki/SKILL.md\n@@ -18,6 +18,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n Codex CLI does not register this skill as a custom `/shiki` slash command. When\n the user types `/shiki` in Codex CLI and gets \"Unrecognized command\", explain\n that this is expected: invoke Shiki in Codex with natural language such as\n@@ -69,7 +72,7 @@\n \n ## Responsibilities\n \n-- Claude Code implements and repairs by default (`shiki runner claude`, ADR 0008); Codex implements and repairs tasks explicitly assigned to `codex`.\n+- Claude Code implements and repairs by default (`shiki runner claude`, SADR-0008); Codex implements and repairs tasks explicitly assigned to `codex`.\n - Claude Code Action can act as GitHub-side CCA or reviewer.\n - CCA judges completion.\n - MergeGate authorizes state transitions and merge readiness.\ndiff --git a/.github/prompts/cca-completion-check.md b/.github/prompts/cca-completion-check.md\nindex ed203ad5f302ecc81372d8ab27d2b1232ca97497..fd89950528333ef6242e6d5443904f5392ab7e56\n--- a/.github/prompts/cca-completion-check.md\n+++ b/.github/prompts/cca-completion-check.md\n@@ -19,6 +19,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n ## Required Reading\n \n Read only the files needed to judge the PR contract. Prefer this order:\ndiff --git a/AGENTS.md b/AGENTS.md\nindex 7f041100d49c2a7525f51cb0f5d696a4a6d1c78c..2b700e989ff295987aa8a4b0c5fcb1acd80020da\n--- a/AGENTS.md\n+++ b/AGENTS.md\n@@ -44,6 +44,10 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use\n+`SADR-NNNN-*.md`; target product decisions retain `NNNN-*.md` and the `ADR`\n+namespace.\n+\n Use the canonical source-of-truth order above. Runtime-specific reading orders\n are operational guidance only and must not override or reorder it.\n \ndiff --git a/CLAUDE.md b/CLAUDE.md\nindex 98a932e7841a632866fcd53b0bad0e3af3711d3d..611d491912d6a72a0327a1731b8f33a41acb4baa\n--- a/CLAUDE.md\n+++ b/CLAUDE.md\n@@ -14,6 +14,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n ## Claude Role\n \n Default to planner, default implementer, reviewer, coordinator, documentation editor, CCA-style completion judge, and final-judgment assistant.\ndiff --git a/SYSTEM_PROMPT.md b/SYSTEM_PROMPT.md\nindex aaaea45e3a896c3a44ba89eec8bb84325ecf6c62..2e7c093ebe9e8bb34e0923756f191b4f7fa581f5\n--- a/SYSTEM_PROMPT.md\n+++ b/SYSTEM_PROMPT.md\n@@ -30,6 +30,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n Use the canonical source-of-truth order above. Runtime-specific reading orders\n are operational guidance only and must not override or reorder it. The current\n conversation is non-durable operator input.\ndiff --git a/docs/adr/0001-keep-shiki-as-platform-name.md b/docs/adr/SADR-0001-keep-shiki-as-platform-name.md\nindex 20a9637c3b9d8c97de7ba8db7cfae4d82d9a7dba..8b685439959aada1b221c112f0ac3c51ca8a1695\n--- a/docs/adr/0001-keep-shiki-as-platform-name.md\n+++ b/docs/adr/SADR-0001-keep-shiki-as-platform-name.md\n@@ -1,3 +1,3 @@\n-# Keep Shiki As The Platform Name\n+# SADR-0001: Keep Shiki As The Platform Name\n \n Shiki remains the product name for the GitHub-native agentic engineering control plane. We considered more descriptive names such as MergePilot, MergeGate, and Kiroku, but kept Shiki because it carries the existing goal-seeking framework identity while remaining independent of Hermes, Codex, Claude Code, or any single runtime.\ndiff --git a/docs/adr/0002-use-agents-as-shared-constitution.md b/docs/adr/SADR-0002-use-agents-as-shared-constitution.md\nindex 73de30ecb078120eac540d9bc51855e3cd48d0e3..c5486182bf06fca36ea508496b013c67302fd989\n--- a/docs/adr/0002-use-agents-as-shared-constitution.md\n+++ b/docs/adr/SADR-0002-use-agents-as-shared-constitution.md\n@@ -1,3 +1,3 @@\n-# Use AGENTS As The Shared Agent Constitution\n+# SADR-0002: Use AGENTS As The Shared Agent Constitution\n \n Shiki uses `AGENTS.md` as the runtime-neutral constitution and `CLAUDE.md` as a thin Claude Code wrapper. This keeps Codex, Claude Code, Hermes Runner, and future Agent Runtimes aligned on the same GitHub-first MergeGate rules while still allowing runtime-specific instructions where needed.\ndiff --git a/docs/adr/0003-require-skill-gate-for-mergegate-readiness.md b/docs/adr/SADR-0003-require-skill-gate-for-mergegate-readiness.md\nindex 424be0c9af4c8f0136f1fde68a468c47ff84e764..7fc337987c7df9792f75f181dfcbed75fe5ffff2\n--- a/docs/adr/0003-require-skill-gate-for-mergegate-readiness.md\n+++ b/docs/adr/SADR-0003-require-skill-gate-for-mergegate-readiness.md\n@@ -1,3 +1,3 @@\n-# Require Skill Gate For MergeGate Readiness\n+# SADR-0003: Require Skill Gate For MergeGate Readiness\n \n Shiki requires relevant engineering skills to be invoked before execution when their triggers apply. This makes skills part of MergeGate readiness rather than optional prompt style, because Shiki depends on repeatable planning, issue decomposition, TDD, diagnosis, triage, and architecture review before autonomous or parallel agent work can be trusted.\ndiff --git a/docs/adr/0004-use-github-first-with-shiki-mirror.md b/docs/adr/SADR-0004-use-github-first-with-shiki-mirror.md\nindex d0a3a9174dc646a945a9d325954ffaf00c352040..a79c54ae9ee8eed2a0524192b864da4e22a92d3f\n--- a/docs/adr/0004-use-github-first-with-shiki-mirror.md\n+++ b/docs/adr/SADR-0004-use-github-first-with-shiki-mirror.md\n@@ -1,4 +1,4 @@\n-# ADR 0004: Use GitHub First With A Repository-Local Shiki Mirror\n+# SADR-0004: Use GitHub First With A Repository-Local Shiki Mirror\n \n ## Status\n \ndiff --git a/docs/adr/0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md b/docs/adr/SADR-0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\nindex b339baf49b89d9290e79c3d51b36e7b2bde3bd95..c30a0d8b1fde0224d0ac7820e7870dccf721580d\n--- a/docs/adr/0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\n+++ b/docs/adr/SADR-0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\n@@ -1,4 +1,4 @@\n-# ADR 0005: Use Codex As Front And Claude Code Action As The GitHub Runtime\n+# SADR-0005: Use Codex As Front And Claude Code Action As The GitHub Runtime\n \n ## Status\n \ndiff --git a/docs/adr/0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md b/docs/adr/SADR-0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\nindex 4bcae9d1e52f0a37b8e9fbafa5cd14b6ee1993bd..7a38538a6ae9929567f0ade67e7ea6a60d0b3f7e\n--- a/docs/adr/0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\n+++ b/docs/adr/SADR-0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\n@@ -1,4 +1,4 @@\n-# ADR 0006: Separate Codex Implementation, CCA Completion, And MergeGate Authorization\n+# SADR-0006: Separate Codex Implementation, CCA Completion, And MergeGate Authorization\n \n ## Status\n \ndiff --git a/docs/adr/0007-packaging-and-release.md b/docs/adr/SADR-0007-packaging-and-release.md\nindex 79b84ddb97167372b44641e5b7860e400ce80822..023cd57f8011aa6e6d2ebf612b7c35a7a82c00d4\n--- a/docs/adr/0007-packaging-and-release.md\n+++ b/docs/adr/SADR-0007-packaging-and-release.md\n@@ -1,4 +1,4 @@\n-# ADR 0007: Packaging And Release For Shiki\n+# SADR-0007: Packaging And Release For Shiki\n \n ## Status\n \ndiff --git a/docs/adr/0008-use-claude-code-as-default-implementer.md b/docs/adr/SADR-0008-use-claude-code-as-default-implementer.md\nindex dedf4510b7c7799521422f622029563d373db031..a158a8d444d93764ec515a28133f7dde4af23579\n--- a/docs/adr/0008-use-claude-code-as-default-implementer.md\n+++ b/docs/adr/SADR-0008-use-claude-code-as-default-implementer.md\n@@ -1,4 +1,4 @@\n-# ADR 0008: Use Claude Code As The Default Implementer Runtime\n+# SADR-0008: Use Claude Code As The Default Implementer Runtime\n \n ## Status\n \ndiff --git a/docs/adr/0009-requirements-definition-and-spec-freeze.md b/docs/adr/SADR-0009-requirements-definition-and-spec-freeze.md\nindex 29b3f7871bef9203a8b4a831a65c39eb2f506e42..52e7dd69ad0aa4efe0b68c62678abbf7df02aaec\n--- a/docs/adr/0009-requirements-definition-and-spec-freeze.md\n+++ b/docs/adr/SADR-0009-requirements-definition-and-spec-freeze.md\n@@ -1,4 +1,4 @@\n-# ADR 0009: Merge Grill And Planning Into Requirements Definition With Spec Freeze\n+# SADR-0009: Merge Grill And Planning Into Requirements Definition With Spec Freeze\n \n ## Status\n \ndiff --git a/docs/adr/0010-external-ai-guardian-review.md b/docs/adr/SADR-0010-external-ai-guardian-review.md\nindex e6108bbe8bb4e73e0ff4684c8f78001a80438c4e..e690d4eddd270e683d709c9b679c5859e71eeaaa\n--- a/docs/adr/0010-external-ai-guardian-review.md\n+++ b/docs/adr/SADR-0010-external-ai-guardian-review.md\n@@ -1,4 +1,4 @@\n-# ADR 0010: External AI Guardian Review As A First-Class Approval Authority\n+# SADR-0010: External AI Guardian Review As A First-Class Approval Authority\n \n ## Status\n \ndiff --git a/docs/adr/0011-autonomous-quality-gate-evidence.md b/docs/adr/SADR-0011-autonomous-quality-gate-evidence.md\nindex bca0246c5d46e44afbde15486a75eec425fc02f5..9c93eac16d8c3a6d4b11bd7224e3955102cd7db4\n--- a/docs/adr/0011-autonomous-quality-gate-evidence.md\n+++ b/docs/adr/SADR-0011-autonomous-quality-gate-evidence.md\n@@ -1,4 +1,4 @@\n-# ADR 0011: Autonomous Quality-Gate Evidence \u2014 Deterministic Facts And An Independent Verifier\n+# SADR-0011: Autonomous Quality-Gate Evidence \u2014 Deterministic Facts And An Independent Verifier\n \n ## Status\n \ndiff --git a/docs/adr/0012-autonomous-loop-closeout-to-main.md b/docs/adr/SADR-0012-autonomous-loop-closeout-to-main.md\nindex 637fad6dde4989c9c2bf0f81f676d8d29991123c..ec7d85f81030a676bcbba28b43eb365422a36eac\n--- a/docs/adr/0012-autonomous-loop-closeout-to-main.md\n+++ b/docs/adr/SADR-0012-autonomous-loop-closeout-to-main.md\n@@ -1,4 +1,4 @@\n-# 0012 \u2014 Autonomous loop pushes goal completion to main via a closeout PR\n+# SADR-0012 \u2014 Autonomous loop pushes goal completion to main via a closeout PR\n \n - Status: accepted\n - Date: 2026-06-18\ndiff --git a/docs/adr/0013-bootstrap-grants-actions-approve-pull-requests.md b/docs/adr/SADR-0013-bootstrap-grants-actions-approve-pull-requests.md\nindex b6c69d64f1c5ced346b88d6859586715b3b5bd0e..a793e7277d67ab8ea56286fec289ca689825a17d\n--- a/docs/adr/0013-bootstrap-grants-actions-approve-pull-requests.md\n+++ b/docs/adr/SADR-0013-bootstrap-grants-actions-approve-pull-requests.md\n@@ -1,4 +1,4 @@\n-# ADR 0013: Bootstrap Grants GitHub Actions Permission To Create And Approve Pull Requests\n+# SADR-0013: Bootstrap Grants GitHub Actions Permission To Create And Approve Pull Requests\n \n ## Status\n \ndiff --git a/docs/adr/0014-codex-app-external-ai-guardian-ui-adapter.md b/docs/adr/SADR-0014-codex-app-external-ai-guardian-ui-adapter.md\nindex 238db06279334caa341c117362df9271382a7fcd..65f12e9939321e1b3e389a34c822fc1549e5155d\n--- a/docs/adr/0014-codex-app-external-ai-guardian-ui-adapter.md\n+++ b/docs/adr/SADR-0014-codex-app-external-ai-guardian-ui-adapter.md\n@@ -1,4 +1,4 @@\n-# ADR 0014: Use Codex App As The External AI Guardian UI Adapter\n+# SADR-0014: Use Codex App As The External AI Guardian UI Adapter\n \n ## Status\n \ndiff --git a/docs/adr/0015-contract-approval-before-dispatch.md b/docs/adr/SADR-0015-contract-approval-before-dispatch.md\nindex fad6c656741a20a7684ba16080a7db159f63a52f..7bdd760bb3e82eee4e1410f45c783b03b638d456\n--- a/docs/adr/0015-contract-approval-before-dispatch.md\n+++ b/docs/adr/SADR-0015-contract-approval-before-dispatch.md\n@@ -1,4 +1,4 @@\n-# ADR 0015: Approve The Task Contract Before Dispatch, Not The Diff After It\n+# SADR-0015: Approve The Task Contract Before Dispatch, Not The Diff After It\n \n ## Status\n \ndiff --git a/docs/adr/0016-locks-bound-product-paths.md b/docs/adr/SADR-0016-locks-bound-product-paths.md\nindex b5e850f3f1f7f4ef6e7f9d1763a29a51bd300c45..332cc17313498404f41a145291e38447b5de0dc4\n--- a/docs/adr/0016-locks-bound-product-paths.md\n+++ b/docs/adr/SADR-0016-locks-bound-product-paths.md\n@@ -1,4 +1,4 @@\n-# ADR 0016: Locks bound product paths; the Shiki mirror is bound by record identity\n+# SADR-0016: Locks bound product paths; the Shiki mirror is bound by record identity\n \n ## Status\n \ndiff --git a/docs/adr/0017-bookkeeping-closeouts-do-not-inherit-risk.md b/docs/adr/SADR-0017-bookkeeping-closeouts-do-not-inherit-risk.md\nindex 554e3a0d526a72ca067108c5464cc68c3edcf174..83115f69f1b1b48d544c9b0157f377b91cb53abf\n--- a/docs/adr/0017-bookkeeping-closeouts-do-not-inherit-risk.md\n+++ b/docs/adr/SADR-0017-bookkeeping-closeouts-do-not-inherit-risk.md\n@@ -1,4 +1,4 @@\n-# ADR 0017: A Bookkeeping Closeout Does Not Inherit Its Task's Risk\n+# SADR-0017: A Bookkeeping Closeout Does Not Inherit Its Task's Risk\n \n ## Status\n \ndiff --git a/docs/adr/0018-carried-guardian-approval-across-a-proven-base-sync.md b/docs/adr/SADR-0018-carried-guardian-approval-across-a-proven-base-sync.md\nindex 22358562da390ed8b45cd600da08a43d378b4680..38da0fd95102c3496c3219ce2319fce696a2532a\n--- a/docs/adr/0018-carried-guardian-approval-across-a-proven-base-sync.md\n+++ b/docs/adr/SADR-0018-carried-guardian-approval-across-a-proven-base-sync.md\n@@ -1,4 +1,4 @@\n-# ADR 0018: Carried Guardian Approval Across A Proven Base Sync\n+# SADR-0018: Carried Guardian Approval Across A Proven Base Sync\n \n ## Status\n \ndiff --git a/docs/adr/README.md b/docs/adr/README.md\nindex c70e057a4520c36e042ab68c53cd4a21ef709b71..5da43856836e2ce6a150f8127766e2f88355ab4b\n--- a/docs/adr/README.md\n+++ b/docs/adr/README.md\n@@ -1,40 +1,41 @@\n-# Architecture Decision Records\n+# Decision Record Namespaces\n \n-This directory holds Shiki's Architecture Decision Records (ADRs): durable\n-records of hard-to-reverse platform decisions. ADRs are part of the Shiki source\n-of truth alongside GitHub state, the `.shiki/` mirror, and `CONTEXT.md`.\n+This directory holds two independent decision namespaces:\n \n-## How ADRs Work\n+- Shiki Architecture Decision Records (SADRs), named\n+  `SADR-NNNN-<short-slug>.md`, record hard-to-reverse Shiki platform decisions.\n+- Target Architecture Decision Records (ADRs), named\n+  `NNNN-<short-slug>.md`, belong to the target product.\n \n-- New ADRs are added as `docs/adr/NNNN-<short-slug>.md`, where `NNNN` is the next\n-  zero-padded sequence number.\n-- The **canonical list of ADRs is the set of files matching `docs/adr/*.md`** in\n-  this directory. Browse `docs/adr/*.md` for the authoritative, current set.\n-- Use [`template.md`](template.md) as the starting point for a new ADR.\n-- Record an ADR when a decision is hard to reverse, surprising without context,\n-  and a real tradeoff. Do not write an ADR for routine or easily reversible\n-  choices.\n+The same four-digit number may appear once in each namespace. A platform\n+`SADR-NNNN` citation resolves only to `SADR-NNNN-*.md`; a target `ADR NNNN`\n+citation resolves only to `NNNN-*.md`.\n \n-Using the `docs/adr/*.md` glob as the source of truth means other PRs can add an\n-ADR without editing this index file, avoiding merge contention on a\n-hand-maintained enumeration.\n+## How Decision Records Work\n \n-## Current ADRs (snapshot)\n-\n-The list below is a convenience snapshot and may lag behind the directory. When\n-in doubt, list `docs/adr/*.md`.\n+- New Shiki platform decisions use the next zero-padded SADR number.\n+- New target product decisions use the target's own ADR sequence.\n+- The canonical list is the files in `docs/adr/`; no hand-maintained index is\n+  authoritative.\n+- Use [`template.md`](template.md) as the starting point and choose the filename\n+  namespace that owns the decision.\n+- Record a decision when it is hard to reverse, surprising without context, and\n+  a real tradeoff. Do not write a decision record for routine or easily\n+  reversible choices.\n \n-- [0001 \u2014 Keep Shiki As The Platform Name](0001-keep-shiki-as-platform-name.md)\n-- [0002 \u2014 Use AGENTS As The Shared Agent Constitution](0002-use-agents-as-shared-constitution.md)\n-- [0003 \u2014 Require Skill Gate For MergeGate Readiness](0003-require-skill-gate-for-mergegate-readiness.md)\n-- [0004 \u2014 Use GitHub First With A Repository-Local Shiki Mirror](0004-use-github-first-with-shiki-mirror.md)\n-- [0005 \u2014 Use Codex As Front And Claude Code Action As The GitHub Runtime](0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md)\n-- [0006 \u2014 Separate Codex Implementation, CCA Completion, And MergeGate Authorization](0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md)\n+Using the directory as the source of truth lets independent changes add records\n+without contending on an enumerated index.\n \n-## Adding A New ADR\n+## Adding A Shiki SADR\n \n-1. Copy `template.md` to `docs/adr/NNNN-<short-slug>.md`.\n+1. Copy `template.md` to `docs/adr/SADR-NNNN-<short-slug>.md`.\n 2. Fill in the title, status, context, decision, and consequences.\n-3. Set the status to `Proposed`, then `Accepted` once the decision is approved.\n-4. Open a PR following `CONTRIBUTING.md`. You do not need to edit this index for\n-   the ADR to be discoverable via `docs/adr/*.md`.\n+3. Set the status to `Proposed`, then `Accepted` once the proper authority has\n+   approved the decision.\n+4. Open a PR following `CONTRIBUTING.md`.\n+\n+## Adding A Target ADR\n+\n+1. Copy `template.md` to `docs/adr/NNNN-<short-slug>.md` using the target's own\n+   sequence.\n+2. Cite it as `ADR NNNN`; do not prefix its filename with `SADR-`.\ndiff --git a/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md b/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\nnew file mode 100644\nindex 0000000000000000000000000000000000000000..7455ce1f3bed02ddddb851ac21a1d1a648c9aeba\n--- /dev/null\n+++ b/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\n@@ -0,0 +1,55 @@\n+# SADR-0019: Separate Shiki Platform SADRs From Target ADRs\n+\n+## Status\n+\n+Accepted\n+\n+## Context\n+\n+Shiki installs its platform decision records into `docs/adr/` in every target\n+repository. Shiki and its targets previously used the same numeric\n+`NNNN-<slug>.md` namespace. A target that already owned ADRs 0013 through 0018\n+therefore acquired two records for each number during installation, making an\n+`ADR NNNN` citation ambiguous. Renumbering target decisions would rewrite target\n+history, while renumbering Shiki decisions per target would destroy their stable\n+platform identity.\n+\n+Upgrades also need to remove the former Shiki-owned numeric paths without ever\n+mistaking a target-owned ADR for platform content. The install stamp already\n+records exact shipped paths and content digests, so it can prove ownership only\n+when both still match.\n+\n+## Decision\n+\n+Shiki platform decisions use the `SADR` namespace:\n+\n+- records are named `docs/adr/SADR-NNNN-<slug>.md`;\n+- live platform citations use `SADR-NNNN`;\n+- target product decisions retain `docs/adr/NNNN-<slug>.md` and `ADR NNNN`;\n+- the reference validator resolves the two namespaces independently, so the same\n+  four-digit number may exist once in each namespace.\n+\n+Before a forced upgrade writes anything, the installer inventories the exact\n+former numeric Shiki ADR paths. It may delete a legacy path only when the\n+existing install stamp contains that exact path and its recorded SHA-256 digest\n+matches the file currently on disk. All ownership blockers are reported\n+together and abort the upgrade before its first write. A non-forced install\n+never performs this cleanup.\n+\n+Historical `.shiki/` evidence remains unchanged because it records the namespace\n+that existed when that evidence was produced.\n+\n+## Consequences\n+\n+- Targets may use any ADR number without colliding with Shiki's platform SADRs.\n+- Fresh installs preserve target ADR bytes and install a stable SADR set.\n+- Forced upgrades fail closed when legacy ownership cannot be proven, and a\n+  successful upgrade refreshes the stamp without removed legacy paths.\n+- Current governance, packaging, workflow, prompt, script, and skill references\n+  must move with the platform records.\n+\n+## Alternatives Considered\n+\n+- Collision-only refusal leaves affected targets permanently unupgradable.\n+- Renumbering target ADRs mutates target-owned history.\n+- Per-target Shiki renumbering makes platform citations non-portable.\ndiff --git a/docs/adr/template.md b/docs/adr/template.md\nindex a92a97daddd0b00ab9dd626cc8d86942da2eafed..17dd79175b5fee2105935557f691b40cf9492942\n--- a/docs/adr/template.md\n+++ b/docs/adr/template.md\n@@ -1,14 +1,14 @@\n-# NNNN: <Short Decision Title>\n+# <SADR-NNNN or NNNN>: <Short Decision Title>\n \n ## Status\n \n-Proposed | Accepted | Superseded by ADR-NNNN\n+Proposed | Accepted | Superseded by SADR-NNNN or target ADR NNNN\n \n ## Context\n \n What is the situation, constraint, or force that requires a decision? Reference\n-relevant domain terms (`CONTEXT.md`), prior ADRs, and code reality. State the\n-problem, not the solution.\n+relevant domain terms (`CONTEXT.md`), prior Shiki SADRs or target ADRs, and code\n+reality. State the problem, not the solution.\n \n ## Decision\n \ndiff --git a/docs/agents/domain.md b/docs/agents/domain.md\nindex 258da8ea81e87ac0a6b0b5b5e5c2fde980eaabb7..67aaa6f4b7d0a5aa4d495ce6279073333f7798cb\n--- a/docs/agents/domain.md\n+++ b/docs/agents/domain.md\n@@ -5,7 +5,9 @@\n ## Layout\n \n - `CONTEXT.md` defines Shiki language. Keep it implementation-free.\n-- `docs/adr/` records hard-to-reverse decisions.\n+- `docs/adr/SADR-NNNN-*.md` records Shiki platform decisions.\n+- `docs/adr/NNNN-*.md` records target product decisions without sharing the\n+  SADR namespace.\n - `AGENTS.md` is the runtime-neutral agent constitution.\n - `CLAUDE.md` is the Claude Code wrapper around `AGENTS.md`.\n - `.shiki/` is the Target Repository mirror for Goals, PRDs, plans, Task DAGs, contracts, locks, worktree records, ledger entries, CCA verdicts, repair packets, reports, and handoffs.\ndiff --git a/docs/agents/implementation-policy.md b/docs/agents/implementation-policy.md\nindex c754144a9fb5bf4a38aa82a60e1670c59e626b44..e90d40fe21880b9a09719c5ecfe6bde7284eb3ce\n--- a/docs/agents/implementation-policy.md\n+++ b/docs/agents/implementation-policy.md\n@@ -31,6 +31,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n ## Non-Negotiables\n \n 1. **Planning precedes implementation.** Non-trivial Goals start with `grill-with-docs` and Context & Impact.\ndiff --git a/scripts/shiki_installer.py b/scripts/shiki_installer.py\nindex be064ab7a29ff6836f7f7a1d70500a43e0d11d7b..d3e3247edbf3e4be287a4667176d86f6500f8dc5\n--- a/scripts/shiki_installer.py\n+++ b/scripts/shiki_installer.py\n@@ -118,6 +118,7 @@\n # what lets a target carry it without a governance-schema change.\n INSTALL_STAMP_PATH = \".shiki/install-stamp.json\"\n INSTALL_STAMP_VERSION = 1\n+LEGACY_SHIKI_SADR_MAX = 18\n \n # Under --force the shipped surface splits three ways, so nothing is lost and\n # nothing is silently stale.\n@@ -237,9 +238,98 @@\n         )\n \n \n+def legacy_shiki_adr_paths() -> tuple[str, ...]:\n+    \"\"\"Former numeric paths for Shiki SADR-0001 through SADR-0018.\n+\n+    The authoritative slugs come from the current SADR filenames. SADR-0019 is\n+    the namespace decision itself and never shipped under a numeric legacy path.\n+    \"\"\"\n+    paths: list[str] = []\n+    for sadr in sorted((ROOT / \"docs\" / \"adr\").glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\")):\n+        number = sadr.name.split(\"-\", 2)[1]\n+        if int(number) <= LEGACY_SHIKI_SADR_MAX:\n+            paths.append(f\"docs/adr/{sadr.name.removeprefix('SADR-')}\")\n+    return tuple(paths)\n+\n+\n+def preflight_legacy_sadr_cleanup(target: Path, *, force: bool) -> tuple[str, ...]:\n+    \"\"\"Authorize exact legacy Shiki ADR removals before the first write.\n+\n+    A non-force install never removes existing files. A forced install needs no\n+    stamp when no legacy Shiki path is present. When legacy paths do exist, each\n+    must be named in a readable install stamp and its current bytes must match\n+    the recorded digest.\n+    \"\"\"\n+    if not force:\n+        return ()\n+    present = tuple(\n+        relative\n+        for relative in legacy_shiki_adr_paths()\n+        if (target / relative).exists() or (target / relative).is_symlink()\n+    )\n+    if not present:\n+        return ()\n+\n+    stamp_path = target / INSTALL_STAMP_PATH\n+    try:\n+        raw = stamp_path.read_text(encoding=\"utf-8\")\n+        stamp = json.loads(raw)\n+    except FileNotFoundError:\n+        detail = \"install stamp is absent\"\n+        blockers = [f\"{relative}: {detail}\" for relative in present]\n+    except (OSError, UnicodeDecodeError) as error:\n+        detail = f\"install stamp is unreadable ({error})\"\n+        blockers = [f\"{relative}: {detail}\" for relative in present]\n+    except json.JSONDecodeError as error:\n+        detail = f\"install stamp is malformed ({error})\"\n+        blockers = [f\"{relative}: {detail}\" for relative in present]\n+    else:\n+        if not isinstance(stamp, dict):\n+            blockers = [\n+                f\"{relative}: install stamp is malformed (expected an object)\"\n+                for relative in present\n+            ]\n+        else:\n+            digests = stamp.get(\"digests\")\n+            blockers = []\n+            if not isinstance(digests, dict):\n+                blockers.extend(\n+                    f\"{relative}: install stamp has no digest for this path\"\n+                    for relative in present\n+                )\n+            else:\n+                for relative in present:\n+                    expected = digests.get(relative)\n+                    if not isinstance(expected, str) or not expected:\n+                        blockers.append(\n+                            f\"{relative}: install stamp has no digest for this path\"\n+                        )\n+                        continue\n+                    try:\n+                        actual = _sha256_file(target / relative)\n+                    except OSError as error:\n+                        blockers.append(\n+                            f\"{relative}: legacy path is unreadable ({error})\"\n+                        )\n+                        continue\n+                    if actual != expected:\n+                        blockers.append(\n+                            f\"{relative}: digest mismatch (stamp {expected}, current {actual})\"\n+                        )\n+\n+    if blockers:\n+        joined = \"\\n\".join(f\"- {blocker}\" for blocker in blockers)\n+        raise ShikiError(\n+            \"legacy Shiki ADR cleanup is not authorized:\\n\"\n+            f\"{joined}\\nNo files were written.\"\n+        )\n+    return present\n+\n+\n def install_template(target: Path, *, force: bool, validate: bool) -> list[NewFileNote]:\n     # Preconditions first: never rewrite the tree of a target that cannot be\n     # upgraded cleanly (see refuse_pending_migrations).\n+    legacy_cleanup = preflight_legacy_sadr_cleanup(target, force=force)\n     refuse_pending_migrations(target)\n \n     # Resolve the target's required CODEOWNERS owner once from its\n@@ -250,6 +340,10 @@\n     # verbatim. See ``codeowners_required_owner``.\n     codeowners_owner = codeowners_required_owner(target)\n \n+    for relative in legacy_cleanup:\n+        (target / relative).unlink()\n+        info(f\"removed stamped legacy Shiki ADR: {target / relative}\")\n+\n     notes: list[NewFileNote] = []\n     for relative in TEMPLATE_PATHS:\n         source = ROOT / relative\ndiff --git a/tests/test_adr_references.py b/tests/test_adr_references.py\nindex 51706329c797d5dd9b47e082a333fa83b261d482..91ab76b724cb973435c45d53cab63793942267e9\n--- a/tests/test_adr_references.py\n+++ b/tests/test_adr_references.py\n@@ -1,4 +1,4 @@\n-\"\"\"Every ``ADR ####`` citation must resolve to exactly one decision record.\n+\"\"\"Every target ADR and Shiki SADR citation resolves in its own namespace.\n \n Background. ``scripts/mergegate_check.py`` justifies ``is_bookkeeping_closeout``\n with \"ADR 0017\" nine times and ``scripts/guardian_approval_signal.py`` cites\n@@ -11,11 +11,12 @@\n and the directory drifted with no signal.\n \n This is the same unbound-pair shape behind other silent failures: a reference\n-and its target with nothing asserting they agree. This test binds them. It scans\n-every ``scripts/*.py`` (plus ``docs/**/*.md`` and the root ``*.md`` governance\n-files, which are cheap) for the ``ADR ####`` pattern and asserts each cited\n-number resolves to exactly one ``docs/adr/####-*.md``. The converse is\n-deliberately NOT required: an ADR may exist without being cited.\n+and its target with nothing asserting they agree. This test binds them. Target\n+``ADR NNNN`` citations resolve only to ``docs/adr/NNNN-*.md``; Shiki\n+``SADR-NNNN`` citations resolve only to ``docs/adr/SADR-NNNN-*.md``. The scan\n+covers current governance docs, scripts, workflows, prompts, packaging metadata,\n+and repo-local Codex guidance while deliberately excluding historical\n+``.shiki`` evidence. The converse is not required: a record may be uncited.\n \n The suite is dependency free: standard-library ``unittest`` only.\n \"\"\"\n@@ -31,17 +32,18 @@\n \n REPO_ROOT = Path(__file__).resolve().parents[1]\n \n-# \"ADR 0016\" -- a four-digit citation. The trailing \\b keeps a five-digit token\n-# from being read as a truncated four-digit number.\n-_CITATION = re.compile(r\"\\bADR (\\d{4})\\b\")\n-# \"0016-locks-bound-product-paths.md\" -- the leading four digits name the record.\n-_ADR_FILENAME = re.compile(r\"^(\\d{4})-.*\\.md$\")\n+# ``ADR 0016`` / ``SADR-0016`` -- a namespace plus four-digit citation. The\n+# trailing ``\\b`` keeps a five-digit token from being read as a truncated one.\n+_CITATION = re.compile(r\"\\b(S?ADR)[ -](\\d{4})\\b\")\n+# Target ADRs retain the numeric filename; Shiki platform decisions use SADR.\n+_DECISION_FILENAME = re.compile(r\"^(?:(SADR)-)?(\\d{4})-.*\\.md$\")\n \n \n class Violation(NamedTuple):\n     \"\"\"A citation that does not resolve to exactly one decision record.\"\"\"\n \n     citing_file: str  # repo-relative path of the file that cites the ADR\n+    namespace: str  # \"ADR\" for target decisions; \"SADR\" for Shiki decisions\n     number: str  # the cited four-digit number, e.g. \"0016\"\n     line: int  # 1-based line of the citation\n     reason: str  # \"missing\" (zero matches) or \"ambiguous\" (two or more)\n@@ -49,44 +51,59 @@\n \n     @property\n     def expected_path(self) -> str:\n-        return f\"docs/adr/{self.number}-*.md\"\n+        prefix = \"SADR-\" if self.namespace == \"SADR\" else \"\"\n+        return f\"docs/adr/{prefix}{self.number}-*.md\"\n \n     def describe(self) -> str:\n         \"\"\"Name the citing file, the cited number, and the missing path.\"\"\"\n         if self.reason == \"missing\":\n             return (\n-                f\"{self.citing_file}:{self.line} cites ADR {self.number} \"\n+                f\"{self.citing_file}:{self.line} cites {self.namespace} {self.number} \"\n                 f\"but no document matches {self.expected_path}\"\n             )\n         joined = \", \".join(self.matches)\n         return (\n-            f\"{self.citing_file}:{self.line} cites ADR {self.number} which \"\n+            f\"{self.citing_file}:{self.line} cites {self.namespace} {self.number} which \"\n             f\"resolves to {len(self.matches)} documents (expected exactly one \"\n             f\"at {self.expected_path}): {joined}\"\n         )\n \n \n-def _adr_index(root: Path) -> dict[str, list[Path]]:\n-    \"\"\"Map every four-digit ADR number to the record files that claim it.\"\"\"\n-    index: dict[str, list[Path]] = {}\n+def _adr_index(root: Path) -> dict[tuple[str, str], list[Path]]:\n+    \"\"\"Map each decision namespace and number to the files that claim it.\"\"\"\n+    index: dict[tuple[str, str], list[Path]] = {}\n     adr_dir = root / \"docs\" / \"adr\"\n     if adr_dir.is_dir():\n         for path in sorted(adr_dir.glob(\"*.md\")):\n-            match = _ADR_FILENAME.match(path.name)\n+            match = _DECISION_FILENAME.match(path.name)\n             if match:\n-                index.setdefault(match.group(1), []).append(path)\n+                namespace = \"SADR\" if match.group(1) else \"ADR\"\n+                index.setdefault((namespace, match.group(2)), []).append(path)\n     return index\n \n \n def _citing_files(root: Path, include_docs: bool = True) -> list[Path]:\n-    \"\"\"Files scanned for citations: scripts/*.py, and cheaply the markdown.\"\"\"\n+    \"\"\"Current, shipped text surfaces scanned for decision citations.\"\"\"\n     files = sorted((root / \"scripts\").glob(\"*.py\"))\n+    files += sorted((root / \"scripts\").glob(\"*.sh\"))\n     if include_docs:\n         docs = root / \"docs\"\n         if docs.is_dir():\n             files += sorted(docs.rglob(\"*.md\"))\n         files += sorted(root.glob(\"*.md\"))\n-    return files\n+        pyproject = root / \"pyproject.toml\"\n+        if pyproject.is_file():\n+            files.append(pyproject)\n+        for relative in (\".github\", \".codex\"):\n+            surface = root / relative\n+            if surface.is_dir():\n+                files += sorted(\n+                    path\n+                    for path in surface.rglob(\"*\")\n+                    if path.is_file()\n+                    and path.suffix in {\".md\", \".py\", \".sh\", \".toml\", \".yaml\", \".yml\"}\n+                )\n+    return sorted(set(files))\n \n \n def find_adr_reference_violations(\n@@ -94,10 +111,9 @@\n ) -> list[Violation]:\n     \"\"\"Return every ``ADR ####`` citation under ``root`` that fails to resolve.\n \n-    A citation resolves when exactly one ``docs/adr/####-*.md`` matches its\n-    number. Zero matches is a \"missing\" violation; two or more is \"ambiguous\".\n-    Records that are never cited produce nothing -- the converse is not\n-    required.\n+    A citation resolves when exactly one record in the same namespace matches\n+    its number. Zero matches is a \"missing\" violation; two or more is\n+    \"ambiguous\". Records that are never cited produce nothing.\n     \"\"\"\n     root = Path(root)\n     index = _adr_index(root)\n@@ -110,13 +126,15 @@\n         rel = path.relative_to(root).as_posix()\n         for lineno, line in enumerate(text.splitlines(), start=1):\n             for match in _CITATION.finditer(line):\n-                number = match.group(1)\n-                found = index.get(number, [])\n+                namespace, number = match.groups()\n+                found = index.get((namespace, number), [])\n                 if len(found) == 1:\n                     continue\n                 reason = \"missing\" if not found else \"ambiguous\"\n                 rels = [p.relative_to(root).as_posix() for p in found]\n-                violations.append(Violation(rel, number, lineno, reason, rels))\n+                violations.append(\n+                    Violation(rel, namespace, number, lineno, reason, rels)\n+                )\n     return violations\n \n \n@@ -129,8 +147,8 @@\n         self.assertEqual(\n             violations,\n             [],\n-            \"Every 'ADR ####' citation must resolve to exactly one \"\n-            f\"docs/adr/####-*.md:\\n{detail}\",\n+            \"Every ADR/SADR citation must resolve exactly once in its own \"\n+            f\"docs/adr namespace:\\n{detail}\",\n         )\n \n     def test_scan_actually_reads_scripts(self) -> None:\n@@ -146,8 +164,21 @@\n         # The two records this Goal carried onto the default branch, and which\n         # the scripts cite most, must be present.\n         index = _adr_index(REPO_ROOT)\n-        self.assertIn(\"0016\", index)\n-        self.assertIn(\"0017\", index)\n+        self.assertIn((\"SADR\", \"0016\"), index)\n+        self.assertIn((\"SADR\", \"0017\"), index)\n+\n+    def test_platform_tree_has_only_sadr_0001_through_0019(self) -> None:\n+        adr_dir = REPO_ROOT / \"docs\" / \"adr\"\n+        sadrs = sorted(adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\"))\n+        self.assertEqual(\n+            {path.name.split(\"-\", 2)[1] for path in sadrs},\n+            {f\"{number:04d}\" for number in range(1, 20)},\n+        )\n+        self.assertEqual(\n+            list(adr_dir.glob(\"[0-9][0-9][0-9][0-9]-*.md\")),\n+            [],\n+            \"the Shiki platform tree must not retain legacy numeric SADR paths\",\n+        )\n \n \n class FixtureAdrReferences(unittest.TestCase):\n@@ -180,6 +211,26 @@\n         self.assertIn(\"9991\", message)  # cited number\n         self.assertIn(\"docs/adr/9991-*.md\", message)  # missing path\n \n+    def test_target_adr_does_not_satisfy_missing_platform_sadr(self) -> None:\n+        root = self._make_repo(\n+            adr_names=[\"0042-target-decision.md\"],\n+            script_body='\"\"\"Target ADR 0042 exists, but platform SADR-0042 does not.\"\"\"\\n',\n+        )\n+        violations = find_adr_reference_violations(root, include_docs=False)\n+        self.assertEqual(len(violations), 1)\n+        self.assertEqual(violations[0].namespace, \"SADR\")\n+        self.assertEqual(violations[0].number, \"0042\")\n+        self.assertEqual(violations[0].expected_path, \"docs/adr/SADR-0042-*.md\")\n+\n+    def test_target_adr_and_platform_sadr_may_reuse_a_number(self) -> None:\n+        root = self._make_repo(\n+            adr_names=[\"0042-target-decision.md\", \"SADR-0042-platform-decision.md\"],\n+            script_body='\"\"\"Target ADR 0042 and platform SADR-0042 are distinct.\"\"\"\\n',\n+        )\n+        self.assertEqual(\n+            find_adr_reference_violations(root, include_docs=False), []\n+        )\n+\n     def test_uncited_adr_is_allowed(self) -> None:\n         # An ADR exists but nothing cites it -> not a violation. The converse\n         # (every ADR must be cited) is explicitly NOT required.\ndiff --git a/tests/test_sadr_install_migration.py b/tests/test_sadr_install_migration.py\nnew file mode 100644\nindex 0000000000000000000000000000000000000000..7a88682efad63c022b90ffdc45395f7ee7b3da81\n--- /dev/null\n+++ b/tests/test_sadr_install_migration.py\n@@ -0,0 +1,141 @@\n+\"\"\"SADR installation and collision-safe legacy upgrade behavior.\"\"\"\n+\n+from __future__ import annotations\n+\n+import contextlib\n+import hashlib\n+import io\n+import json\n+import shutil\n+import tempfile\n+import unittest\n+from pathlib import Path\n+\n+import shiki_test_support  # noqa: F401  (path bootstrap)\n+\n+import shiki_installer\n+from tests.test_adr_references import find_adr_reference_violations\n+\n+\n+PRODUCT_NUMBERS = tuple(range(13, 19))\n+\n+\n+def _quiet_install(target: Path, *, force: bool, validate: bool) -> None:\n+    with contextlib.redirect_stdout(io.StringIO()):\n+        shiki_installer.install_template(target, force=force, validate=validate)\n+\n+\n+def _sha256(path: Path) -> str:\n+    return hashlib.sha256(path.read_bytes()).hexdigest()\n+\n+\n+def _legacy_sadr_paths() -> tuple[str, ...]:\n+    return tuple(\n+        f\"docs/adr/{path.name.removeprefix('SADR-')}\"\n+        for path in sorted((shiki_installer.ROOT / \"docs\" / \"adr\").glob(\"SADR-*.md\"))\n+        if path.name != \"SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\"\n+    )\n+\n+\n+def _create_legacy_upgrade_fixture(\n+    target: Path,\n+) -> tuple[dict[str, bytes], tuple[str, ...]]:\n+    adr_dir = target / \"docs\" / \"adr\"\n+    adr_dir.mkdir(parents=True)\n+    product_bytes = {\n+        f\"{number:04d}-cubeshare-product-decision.md\": (\n+            f\"# CubeShare ADR {number:04d}\\n\\nDo not rewrite {number}.\\n\"\n+        ).encode()\n+        for number in PRODUCT_NUMBERS\n+    }\n+    for name, content in product_bytes.items():\n+        (adr_dir / name).write_bytes(content)\n+\n+    legacy_paths = _legacy_sadr_paths()\n+    for relative in legacy_paths:\n+        (target / relative).write_bytes(f\"legacy Shiki bytes for {relative}\\n\".encode())\n+\n+    stamp_path = target / shiki_installer.INSTALL_STAMP_PATH\n+    stamp_path.parent.mkdir(parents=True, exist_ok=True)\n+    stamp = {\n+        \"version\": shiki_installer.INSTALL_STAMP_VERSION,\n+        \"platform_commit\": None,\n+        \"installed_at\": \"2000-01-01T00:00:00+00:00\",\n+        \"digests\": {\n+            relative: _sha256(target / relative) for relative in legacy_paths\n+        },\n+    }\n+    stamp_path.write_text(\n+        json.dumps(stamp, indent=2, sort_keys=True) + \"\\n\", encoding=\"utf-8\"\n+    )\n+    return product_bytes, legacy_paths\n+\n+\n+class FreshSadrInstallTests(unittest.TestCase):\n+    def setUp(self) -> None:\n+        self.tmp = Path(tempfile.mkdtemp(prefix=\"shiki-sadr-fresh-\"))\n+        self.addCleanup(lambda: shutil.rmtree(self.tmp, ignore_errors=True))\n+        self.target = self.tmp / \"target\"\n+        self.adr_dir = self.target / \"docs\" / \"adr\"\n+        self.adr_dir.mkdir(parents=True)\n+        self.product_bytes = {\n+            f\"{number:04d}-product-decision.md\": (\n+                f\"# Product ADR {number:04d}\\n\\nTarget-owned bytes {number}.\\n\"\n+            ).encode()\n+            for number in PRODUCT_NUMBERS\n+        }\n+        for name, content in self.product_bytes.items():\n+            (self.adr_dir / name).write_bytes(content)\n+\n+    def test_fresh_install_preserves_product_adrs_and_installs_all_sadrs(self) -> None:\n+        _quiet_install(self.target, force=False, validate=True)\n+\n+        for name, expected in self.product_bytes.items():\n+            self.assertEqual((self.adr_dir / name).read_bytes(), expected)\n+\n+        sadr_numbers = {\n+            path.name.split(\"-\", 2)[1]\n+            for path in self.adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\")\n+        }\n+        self.assertEqual(sadr_numbers, {f\"{number:04d}\" for number in range(1, 20)})\n+\n+        numeric_records = {\n+            path.name\n+            for path in self.adr_dir.glob(\"[0-9][0-9][0-9][0-9]-*.md\")\n+        }\n+        self.assertEqual(numeric_records, set(self.product_bytes))\n+        self.assertEqual(find_adr_reference_violations(self.target), [])\n+\n+\n+class LegacySadrUpgradeTests(unittest.TestCase):\n+    def setUp(self) -> None:\n+        self.tmp = Path(tempfile.mkdtemp(prefix=\"shiki-sadr-upgrade-\"))\n+        self.addCleanup(lambda: shutil.rmtree(self.tmp, ignore_errors=True))\n+        self.target = self.tmp / \"target\"\n+        self.adr_dir = self.target / \"docs\" / \"adr\"\n+        self.product_bytes, self.legacy_paths = _create_legacy_upgrade_fixture(\n+            self.target\n+        )\n+\n+    def test_force_upgrade_deletes_only_stamped_legacy_sadrs(self) -> None:\n+        _quiet_install(self.target, force=True, validate=True)\n+\n+        for relative in self.legacy_paths:\n+            self.assertFalse((self.target / relative).exists(), relative)\n+        for name, expected in self.product_bytes.items():\n+            self.assertEqual((self.adr_dir / name).read_bytes(), expected)\n+\n+        sadrs = list(self.adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\"))\n+        self.assertEqual(len(sadrs), 19)\n+        refreshed = json.loads(\n+            (self.target / shiki_installer.INSTALL_STAMP_PATH).read_text(encoding=\"utf-8\")\n+        )\n+        for relative in self.legacy_paths:\n+            self.assertNotIn(relative, refreshed[\"digests\"])\n+        for path in sadrs:\n+            relative = path.relative_to(self.target).as_posix()\n+            self.assertEqual(refreshed[\"digests\"][relative], _sha256(path))\n+\n+\n+if __name__ == \"__main__\":\n+    unittest.main()\n\n2026-08-07T08:12:04.757357Z ERROR codex_models_manager::manager: failed to renew cache TTL: missing field `base_instructions` at line 94 column 5\n succeeded in 910ms:\n----------------------------------------------------------------------\nRan 2 tests in 0.889s\n\nOK\n\napply patch\npatch: completed\n/private/tmp/shiki-adr-collision.pa0KwY/.worktrees/shiki-t-20260807t073307221254z-2d22d654-introduc/tests/test_sadr_install_migration.py\ndiff --git a/.codex/skills/shiki/SKILL.md b/.codex/skills/shiki/SKILL.md\nindex c8e2e85d9d75d49cc4f2d6286ee22659c4e53e75..93cded623b2c15d9f65c76491a1c2d813ac9e6c0\n--- a/.codex/skills/shiki/SKILL.md\n+++ b/.codex/skills/shiki/SKILL.md\n@@ -18,6 +18,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n Codex CLI does not register this skill as a custom `/shiki` slash command. When\n the user types `/shiki` in Codex CLI and gets \"Unrecognized command\", explain\n that this is expected: invoke Shiki in Codex with natural language such as\n@@ -69,7 +72,7 @@\n \n ## Responsibilities\n \n-- Claude Code implements and repairs by default (`shiki runner claude`, ADR 0008); Codex implements and repairs tasks explicitly assigned to `codex`.\n+- Claude Code implements and repairs by default (`shiki runner claude`, SADR-0008); Codex implements and repairs tasks explicitly assigned to `codex`.\n - Claude Code Action can act as GitHub-side CCA or reviewer.\n - CCA judges completion.\n - MergeGate authorizes state transitions and merge readiness.\ndiff --git a/.github/prompts/cca-completion-check.md b/.github/prompts/cca-completion-check.md\nindex ed203ad5f302ecc81372d8ab27d2b1232ca97497..fd89950528333ef6242e6d5443904f5392ab7e56\n--- a/.github/prompts/cca-completion-check.md\n+++ b/.github/prompts/cca-completion-check.md\n@@ -19,6 +19,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n ## Required Reading\n \n Read only the files needed to judge the PR contract. Prefer this order:\ndiff --git a/AGENTS.md b/AGENTS.md\nindex 7f041100d49c2a7525f51cb0f5d696a4a6d1c78c..2b700e989ff295987aa8a4b0c5fcb1acd80020da\n--- a/AGENTS.md\n+++ b/AGENTS.md\n@@ -44,6 +44,10 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use\n+`SADR-NNNN-*.md`; target product decisions retain `NNNN-*.md` and the `ADR`\n+namespace.\n+\n Use the canonical source-of-truth order above. Runtime-specific reading orders\n are operational guidance only and must not override or reorder it.\n \ndiff --git a/CLAUDE.md b/CLAUDE.md\nindex 98a932e7841a632866fcd53b0bad0e3af3711d3d..611d491912d6a72a0327a1731b8f33a41acb4baa\n--- a/CLAUDE.md\n+++ b/CLAUDE.md\n@@ -14,6 +14,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n ## Claude Role\n \n Default to planner, default implementer, reviewer, coordinator, documentation editor, CCA-style completion judge, and final-judgment assistant.\ndiff --git a/SYSTEM_PROMPT.md b/SYSTEM_PROMPT.md\nindex aaaea45e3a896c3a44ba89eec8bb84325ecf6c62..2e7c093ebe9e8bb34e0923756f191b4f7fa581f5\n--- a/SYSTEM_PROMPT.md\n+++ b/SYSTEM_PROMPT.md\n@@ -30,6 +30,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n Use the canonical source-of-truth order above. Runtime-specific reading orders\n are operational guidance only and must not override or reorder it. The current\n conversation is non-durable operator input.\ndiff --git a/docs/adr/0001-keep-shiki-as-platform-name.md b/docs/adr/SADR-0001-keep-shiki-as-platform-name.md\nindex 20a9637c3b9d8c97de7ba8db7cfae4d82d9a7dba..8b685439959aada1b221c112f0ac3c51ca8a1695\n--- a/docs/adr/0001-keep-shiki-as-platform-name.md\n+++ b/docs/adr/SADR-0001-keep-shiki-as-platform-name.md\n@@ -1,3 +1,3 @@\n-# Keep Shiki As The Platform Name\n+# SADR-0001: Keep Shiki As The Platform Name\n \n Shiki remains the product name for the GitHub-native agentic engineering control plane. We considered more descriptive names such as MergePilot, MergeGate, and Kiroku, but kept Shiki because it carries the existing goal-seeking framework identity while remaining independent of Hermes, Codex, Claude Code, or any single runtime.\ndiff --git a/docs/adr/0002-use-agents-as-shared-constitution.md b/docs/adr/SADR-0002-use-agents-as-shared-constitution.md\nindex 73de30ecb078120eac540d9bc51855e3cd48d0e3..c5486182bf06fca36ea508496b013c67302fd989\n--- a/docs/adr/0002-use-agents-as-shared-constitution.md\n+++ b/docs/adr/SADR-0002-use-agents-as-shared-constitution.md\n@@ -1,3 +1,3 @@\n-# Use AGENTS As The Shared Agent Constitution\n+# SADR-0002: Use AGENTS As The Shared Agent Constitution\n \n Shiki uses `AGENTS.md` as the runtime-neutral constitution and `CLAUDE.md` as a thin Claude Code wrapper. This keeps Codex, Claude Code, Hermes Runner, and future Agent Runtimes aligned on the same GitHub-first MergeGate rules while still allowing runtime-specific instructions where needed.\ndiff --git a/docs/adr/0003-require-skill-gate-for-mergegate-readiness.md b/docs/adr/SADR-0003-require-skill-gate-for-mergegate-readiness.md\nindex 424be0c9af4c8f0136f1fde68a468c47ff84e764..7fc337987c7df9792f75f181dfcbed75fe5ffff2\n--- a/docs/adr/0003-require-skill-gate-for-mergegate-readiness.md\n+++ b/docs/adr/SADR-0003-require-skill-gate-for-mergegate-readiness.md\n@@ -1,3 +1,3 @@\n-# Require Skill Gate For MergeGate Readiness\n+# SADR-0003: Require Skill Gate For MergeGate Readiness\n \n Shiki requires relevant engineering skills to be invoked before execution when their triggers apply. This makes skills part of MergeGate readiness rather than optional prompt style, because Shiki depends on repeatable planning, issue decomposition, TDD, diagnosis, triage, and architecture review before autonomous or parallel agent work can be trusted.\ndiff --git a/docs/adr/0004-use-github-first-with-shiki-mirror.md b/docs/adr/SADR-0004-use-github-first-with-shiki-mirror.md\nindex d0a3a9174dc646a945a9d325954ffaf00c352040..a79c54ae9ee8eed2a0524192b864da4e22a92d3f\n--- a/docs/adr/0004-use-github-first-with-shiki-mirror.md\n+++ b/docs/adr/SADR-0004-use-github-first-with-shiki-mirror.md\n@@ -1,4 +1,4 @@\n-# ADR 0004: Use GitHub First With A Repository-Local Shiki Mirror\n+# SADR-0004: Use GitHub First With A Repository-Local Shiki Mirror\n \n ## Status\n \ndiff --git a/docs/adr/0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md b/docs/adr/SADR-0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\nindex b339baf49b89d9290e79c3d51b36e7b2bde3bd95..c30a0d8b1fde0224d0ac7820e7870dccf721580d\n--- a/docs/adr/0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\n+++ b/docs/adr/SADR-0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\n@@ -1,4 +1,4 @@\n-# ADR 0005: Use Codex As Front And Claude Code Action As The GitHub Runtime\n+# SADR-0005: Use Codex As Front And Claude Code Action As The GitHub Runtime\n \n ## Status\n \ndiff --git a/docs/adr/0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md b/docs/adr/SADR-0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\nindex 4bcae9d1e52f0a37b8e9fbafa5cd14b6ee1993bd..7a38538a6ae9929567f0ade67e7ea6a60d0b3f7e\n--- a/docs/adr/0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\n+++ b/docs/adr/SADR-0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\n@@ -1,4 +1,4 @@\n-# ADR 0006: Separate Codex Implementation, CCA Completion, And MergeGate Authorization\n+# SADR-0006: Separate Codex Implementation, CCA Completion, And MergeGate Authorization\n \n ## Status\n \ndiff --git a/docs/adr/0007-packaging-and-release.md b/docs/adr/SADR-0007-packaging-and-release.md\nindex 79b84ddb97167372b44641e5b7860e400ce80822..023cd57f8011aa6e6d2ebf612b7c35a7a82c00d4\n--- a/docs/adr/0007-packaging-and-release.md\n+++ b/docs/adr/SADR-0007-packaging-and-release.md\n@@ -1,4 +1,4 @@\n-# ADR 0007: Packaging And Release For Shiki\n+# SADR-0007: Packaging And Release For Shiki\n \n ## Status\n \ndiff --git a/docs/adr/0008-use-claude-code-as-default-implementer.md b/docs/adr/SADR-0008-use-claude-code-as-default-implementer.md\nindex dedf4510b7c7799521422f622029563d373db031..a158a8d444d93764ec515a28133f7dde4af23579\n--- a/docs/adr/0008-use-claude-code-as-default-implementer.md\n+++ b/docs/adr/SADR-0008-use-claude-code-as-default-implementer.md\n@@ -1,4 +1,4 @@\n-# ADR 0008: Use Claude Code As The Default Implementer Runtime\n+# SADR-0008: Use Claude Code As The Default Implementer Runtime\n \n ## Status\n \ndiff --git a/docs/adr/0009-requirements-definition-and-spec-freeze.md b/docs/adr/SADR-0009-requirements-definition-and-spec-freeze.md\nindex 29b3f7871bef9203a8b4a831a65c39eb2f506e42..52e7dd69ad0aa4efe0b68c62678abbf7df02aaec\n--- a/docs/adr/0009-requirements-definition-and-spec-freeze.md\n+++ b/docs/adr/SADR-0009-requirements-definition-and-spec-freeze.md\n@@ -1,4 +1,4 @@\n-# ADR 0009: Merge Grill And Planning Into Requirements Definition With Spec Freeze\n+# SADR-0009: Merge Grill And Planning Into Requirements Definition With Spec Freeze\n \n ## Status\n \ndiff --git a/docs/adr/0010-external-ai-guardian-review.md b/docs/adr/SADR-0010-external-ai-guardian-review.md\nindex e6108bbe8bb4e73e0ff4684c8f78001a80438c4e..e690d4eddd270e683d709c9b679c5859e71eeaaa\n--- a/docs/adr/0010-external-ai-guardian-review.md\n+++ b/docs/adr/SADR-0010-external-ai-guardian-review.md\n@@ -1,4 +1,4 @@\n-# ADR 0010: External AI Guardian Review As A First-Class Approval Authority\n+# SADR-0010: External AI Guardian Review As A First-Class Approval Authority\n \n ## Status\n \ndiff --git a/docs/adr/0011-autonomous-quality-gate-evidence.md b/docs/adr/SADR-0011-autonomous-quality-gate-evidence.md\nindex bca0246c5d46e44afbde15486a75eec425fc02f5..9c93eac16d8c3a6d4b11bd7224e3955102cd7db4\n--- a/docs/adr/0011-autonomous-quality-gate-evidence.md\n+++ b/docs/adr/SADR-0011-autonomous-quality-gate-evidence.md\n@@ -1,4 +1,4 @@\n-# ADR 0011: Autonomous Quality-Gate Evidence \u2014 Deterministic Facts And An Independent Verifier\n+# SADR-0011: Autonomous Quality-Gate Evidence \u2014 Deterministic Facts And An Independent Verifier\n \n ## Status\n \ndiff --git a/docs/adr/0012-autonomous-loop-closeout-to-main.md b/docs/adr/SADR-0012-autonomous-loop-closeout-to-main.md\nindex 637fad6dde4989c9c2bf0f81f676d8d29991123c..ec7d85f81030a676bcbba28b43eb365422a36eac\n--- a/docs/adr/0012-autonomous-loop-closeout-to-main.md\n+++ b/docs/adr/SADR-0012-autonomous-loop-closeout-to-main.md\n@@ -1,4 +1,4 @@\n-# 0012 \u2014 Autonomous loop pushes goal completion to main via a closeout PR\n+# SADR-0012 \u2014 Autonomous loop pushes goal completion to main via a closeout PR\n \n - Status: accepted\n - Date: 2026-06-18\ndiff --git a/docs/adr/0013-bootstrap-grants-actions-approve-pull-requests.md b/docs/adr/SADR-0013-bootstrap-grants-actions-approve-pull-requests.md\nindex b6c69d64f1c5ced346b88d6859586715b3b5bd0e..a793e7277d67ab8ea56286fec289ca689825a17d\n--- a/docs/adr/0013-bootstrap-grants-actions-approve-pull-requests.md\n+++ b/docs/adr/SADR-0013-bootstrap-grants-actions-approve-pull-requests.md\n@@ -1,4 +1,4 @@\n-# ADR 0013: Bootstrap Grants GitHub Actions Permission To Create And Approve Pull Requests\n+# SADR-0013: Bootstrap Grants GitHub Actions Permission To Create And Approve Pull Requests\n \n ## Status\n \ndiff --git a/docs/adr/0014-codex-app-external-ai-guardian-ui-adapter.md b/docs/adr/SADR-0014-codex-app-external-ai-guardian-ui-adapter.md\nindex 238db06279334caa341c117362df9271382a7fcd..65f12e9939321e1b3e389a34c822fc1549e5155d\n--- a/docs/adr/0014-codex-app-external-ai-guardian-ui-adapter.md\n+++ b/docs/adr/SADR-0014-codex-app-external-ai-guardian-ui-adapter.md\n@@ -1,4 +1,4 @@\n-# ADR 0014: Use Codex App As The External AI Guardian UI Adapter\n+# SADR-0014: Use Codex App As The External AI Guardian UI Adapter\n \n ## Status\n \ndiff --git a/docs/adr/0015-contract-approval-before-dispatch.md b/docs/adr/SADR-0015-contract-approval-before-dispatch.md\nindex fad6c656741a20a7684ba16080a7db159f63a52f..7bdd760bb3e82eee4e1410f45c783b03b638d456\n--- a/docs/adr/0015-contract-approval-before-dispatch.md\n+++ b/docs/adr/SADR-0015-contract-approval-before-dispatch.md\n@@ -1,4 +1,4 @@\n-# ADR 0015: Approve The Task Contract Before Dispatch, Not The Diff After It\n+# SADR-0015: Approve The Task Contract Before Dispatch, Not The Diff After It\n \n ## Status\n \ndiff --git a/docs/adr/0016-locks-bound-product-paths.md b/docs/adr/SADR-0016-locks-bound-product-paths.md\nindex b5e850f3f1f7f4ef6e7f9d1763a29a51bd300c45..332cc17313498404f41a145291e38447b5de0dc4\n--- a/docs/adr/0016-locks-bound-product-paths.md\n+++ b/docs/adr/SADR-0016-locks-bound-product-paths.md\n@@ -1,4 +1,4 @@\n-# ADR 0016: Locks bound product paths; the Shiki mirror is bound by record identity\n+# SADR-0016: Locks bound product paths; the Shiki mirror is bound by record identity\n \n ## Status\n \ndiff --git a/docs/adr/0017-bookkeeping-closeouts-do-not-inherit-risk.md b/docs/adr/SADR-0017-bookkeeping-closeouts-do-not-inherit-risk.md\nindex 554e3a0d526a72ca067108c5464cc68c3edcf174..83115f69f1b1b48d544c9b0157f377b91cb53abf\n--- a/docs/adr/0017-bookkeeping-closeouts-do-not-inherit-risk.md\n+++ b/docs/adr/SADR-0017-bookkeeping-closeouts-do-not-inherit-risk.md\n@@ -1,4 +1,4 @@\n-# ADR 0017: A Bookkeeping Closeout Does Not Inherit Its Task's Risk\n+# SADR-0017: A Bookkeeping Closeout Does Not Inherit Its Task's Risk\n \n ## Status\n \ndiff --git a/docs/adr/0018-carried-guardian-approval-across-a-proven-base-sync.md b/docs/adr/SADR-0018-carried-guardian-approval-across-a-proven-base-sync.md\nindex 22358562da390ed8b45cd600da08a43d378b4680..38da0fd95102c3496c3219ce2319fce696a2532a\n--- a/docs/adr/0018-carried-guardian-approval-across-a-proven-base-sync.md\n+++ b/docs/adr/SADR-0018-carried-guardian-approval-across-a-proven-base-sync.md\n@@ -1,4 +1,4 @@\n-# ADR 0018: Carried Guardian Approval Across A Proven Base Sync\n+# SADR-0018: Carried Guardian Approval Across A Proven Base Sync\n \n ## Status\n \ndiff --git a/docs/adr/README.md b/docs/adr/README.md\nindex c70e057a4520c36e042ab68c53cd4a21ef709b71..5da43856836e2ce6a150f8127766e2f88355ab4b\n--- a/docs/adr/README.md\n+++ b/docs/adr/README.md\n@@ -1,40 +1,41 @@\n-# Architecture Decision Records\n+# Decision Record Namespaces\n \n-This directory holds Shiki's Architecture Decision Records (ADRs): durable\n-records of hard-to-reverse platform decisions. ADRs are part of the Shiki source\n-of truth alongside GitHub state, the `.shiki/` mirror, and `CONTEXT.md`.\n+This directory holds two independent decision namespaces:\n \n-## How ADRs Work\n+- Shiki Architecture Decision Records (SADRs), named\n+  `SADR-NNNN-<short-slug>.md`, record hard-to-reverse Shiki platform decisions.\n+- Target Architecture Decision Records (ADRs), named\n+  `NNNN-<short-slug>.md`, belong to the target product.\n \n-- New ADRs are added as `docs/adr/NNNN-<short-slug>.md`, where `NNNN` is the next\n-  zero-padded sequence number.\n-- The **canonical list of ADRs is the set of files matching `docs/adr/*.md`** in\n-  this directory. Browse `docs/adr/*.md` for the authoritative, current set.\n-- Use [`template.md`](template.md) as the starting point for a new ADR.\n-- Record an ADR when a decision is hard to reverse, surprising without context,\n-  and a real tradeoff. Do not write an ADR for routine or easily reversible\n-  choices.\n+The same four-digit number may appear once in each namespace. A platform\n+`SADR-NNNN` citation resolves only to `SADR-NNNN-*.md`; a target `ADR NNNN`\n+citation resolves only to `NNNN-*.md`.\n \n-Using the `docs/adr/*.md` glob as the source of truth means other PRs can add an\n-ADR without editing this index file, avoiding merge contention on a\n-hand-maintained enumeration.\n+## How Decision Records Work\n \n-## Current ADRs (snapshot)\n-\n-The list below is a convenience snapshot and may lag behind the directory. When\n-in doubt, list `docs/adr/*.md`.\n+- New Shiki platform decisions use the next zero-padded SADR number.\n+- New target product decisions use the target's own ADR sequence.\n+- The canonical list is the files in `docs/adr/`; no hand-maintained index is\n+  authoritative.\n+- Use [`template.md`](template.md) as the starting point and choose the filename\n+  namespace that owns the decision.\n+- Record a decision when it is hard to reverse, surprising without context, and\n+  a real tradeoff. Do not write a decision record for routine or easily\n+  reversible choices.\n \n-- [0001 \u2014 Keep Shiki As The Platform Name](0001-keep-shiki-as-platform-name.md)\n-- [0002 \u2014 Use AGENTS As The Shared Agent Constitution](0002-use-agents-as-shared-constitution.md)\n-- [0003 \u2014 Require Skill Gate For MergeGate Readiness](0003-require-skill-gate-for-mergegate-readiness.md)\n-- [0004 \u2014 Use GitHub First With A Repository-Local Shiki Mirror](0004-use-github-first-with-shiki-mirror.md)\n-- [0005 \u2014 Use Codex As Front And Claude Code Action As The GitHub Runtime](0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md)\n-- [0006 \u2014 Separate Codex Implementation, CCA Completion, And MergeGate Authorization](0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md)\n+Using the directory as the source of truth lets independent changes add records\n+without contending on an enumerated index.\n \n-## Adding A New ADR\n+## Adding A Shiki SADR\n \n-1. Copy `template.md` to `docs/adr/NNNN-<short-slug>.md`.\n+1. Copy `template.md` to `docs/adr/SADR-NNNN-<short-slug>.md`.\n 2. Fill in the title, status, context, decision, and consequences.\n-3. Set the status to `Proposed`, then `Accepted` once the decision is approved.\n-4. Open a PR following `CONTRIBUTING.md`. You do not need to edit this index for\n-   the ADR to be discoverable via `docs/adr/*.md`.\n+3. Set the status to `Proposed`, then `Accepted` once the proper authority has\n+   approved the decision.\n+4. Open a PR following `CONTRIBUTING.md`.\n+\n+## Adding A Target ADR\n+\n+1. Copy `template.md` to `docs/adr/NNNN-<short-slug>.md` using the target's own\n+   sequence.\n+2. Cite it as `ADR NNNN`; do not prefix its filename with `SADR-`.\ndiff --git a/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md b/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\nnew file mode 100644\nindex 0000000000000000000000000000000000000000..7455ce1f3bed02ddddb851ac21a1d1a648c9aeba\n--- /dev/null\n+++ b/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\n@@ -0,0 +1,55 @@\n+# SADR-0019: Separate Shiki Platform SADRs From Target ADRs\n+\n+## Status\n+\n+Accepted\n+\n+## Context\n+\n+Shiki installs its platform decision records into `docs/adr/` in every target\n+repository. Shiki and its targets previously used the same numeric\n+`NNNN-<slug>.md` namespace. A target that already owned ADRs 0013 through 0018\n+therefore acquired two records for each number during installation, making an\n+`ADR NNNN` citation ambiguous. Renumbering target decisions would rewrite target\n+history, while renumbering Shiki decisions per target would destroy their stable\n+platform identity.\n+\n+Upgrades also need to remove the former Shiki-owned numeric paths without ever\n+mistaking a target-owned ADR for platform content. The install stamp already\n+records exact shipped paths and content digests, so it can prove ownership only\n+when both still match.\n+\n+## Decision\n+\n+Shiki platform decisions use the `SADR` namespace:\n+\n+- records are named `docs/adr/SADR-NNNN-<slug>.md`;\n+- live platform citations use `SADR-NNNN`;\n+- target product decisions retain `docs/adr/NNNN-<slug>.md` and `ADR NNNN`;\n+- the reference validator resolves the two namespaces independently, so the same\n+  four-digit number may exist once in each namespace.\n+\n+Before a forced upgrade writes anything, the installer inventories the exact\n+former numeric Shiki ADR paths. It may delete a legacy path only when the\n+existing install stamp contains that exact path and its recorded SHA-256 digest\n+matches the file currently on disk. All ownership blockers are reported\n+together and abort the upgrade before its first write. A non-forced install\n+never performs this cleanup.\n+\n+Historical `.shiki/` evidence remains unchanged because it records the namespace\n+that existed when that evidence was produced.\n+\n+## Consequences\n+\n+- Targets may use any ADR number without colliding with Shiki's platform SADRs.\n+- Fresh installs preserve target ADR bytes and install a stable SADR set.\n+- Forced upgrades fail closed when legacy ownership cannot be proven, and a\n+  successful upgrade refreshes the stamp without removed legacy paths.\n+- Current governance, packaging, workflow, prompt, script, and skill references\n+  must move with the platform records.\n+\n+## Alternatives Considered\n+\n+- Collision-only refusal leaves affected targets permanently unupgradable.\n+- Renumbering target ADRs mutates target-owned history.\n+- Per-target Shiki renumbering makes platform citations non-portable.\ndiff --git a/docs/adr/template.md b/docs/adr/template.md\nindex a92a97daddd0b00ab9dd626cc8d86942da2eafed..17dd79175b5fee2105935557f691b40cf9492942\n--- a/docs/adr/template.md\n+++ b/docs/adr/template.md\n@@ -1,14 +1,14 @@\n-# NNNN: <Short Decision Title>\n+# <SADR-NNNN or NNNN>: <Short Decision Title>\n \n ## Status\n \n-Proposed | Accepted | Superseded by ADR-NNNN\n+Proposed | Accepted | Superseded by SADR-NNNN or target ADR NNNN\n \n ## Context\n \n What is the situation, constraint, or force that requires a decision? Reference\n-relevant domain terms (`CONTEXT.md`), prior ADRs, and code reality. State the\n-problem, not the solution.\n+relevant domain terms (`CONTEXT.md`), prior Shiki SADRs or target ADRs, and code\n+reality. State the problem, not the solution.\n \n ## Decision\n \ndiff --git a/docs/agents/domain.md b/docs/agents/domain.md\nindex 258da8ea81e87ac0a6b0b5b5e5c2fde980eaabb7..67aaa6f4b7d0a5aa4d495ce6279073333f7798cb\n--- a/docs/agents/domain.md\n+++ b/docs/agents/domain.md\n@@ -5,7 +5,9 @@\n ## Layout\n \n - `CONTEXT.md` defines Shiki language. Keep it implementation-free.\n-- `docs/adr/` records hard-to-reverse decisions.\n+- `docs/adr/SADR-NNNN-*.md` records Shiki platform decisions.\n+- `docs/adr/NNNN-*.md` records target product decisions without sharing the\n+  SADR namespace.\n - `AGENTS.md` is the runtime-neutral agent constitution.\n - `CLAUDE.md` is the Claude Code wrapper around `AGENTS.md`.\n - `.shiki/` is the Target Repository mirror for Goals, PRDs, plans, Task DAGs, contracts, locks, worktree records, ledger entries, CCA verdicts, repair packets, reports, and handoffs.\ndiff --git a/docs/agents/implementation-policy.md b/docs/agents/implementation-policy.md\nindex c754144a9fb5bf4a38aa82a60e1670c59e626b44..e90d40fe21880b9a09719c5ecfe6bde7284eb3ce\n--- a/docs/agents/implementation-policy.md\n+++ b/docs/agents/implementation-policy.md\n@@ -31,6 +31,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n ## Non-Negotiables\n \n 1. **Planning precedes implementation.** Non-trivial Goals start with `grill-with-docs` and Context & Impact.\ndiff --git a/scripts/shiki_installer.py b/scripts/shiki_installer.py\nindex be064ab7a29ff6836f7f7a1d70500a43e0d11d7b..d3e3247edbf3e4be287a4667176d86f6500f8dc5\n--- a/scripts/shiki_installer.py\n+++ b/scripts/shiki_installer.py\n@@ -118,6 +118,7 @@\n # what lets a target carry it without a governance-schema change.\n INSTALL_STAMP_PATH = \".shiki/install-stamp.json\"\n INSTALL_STAMP_VERSION = 1\n+LEGACY_SHIKI_SADR_MAX = 18\n \n # Under --force the shipped surface splits three ways, so nothing is lost and\n # nothing is silently stale.\n@@ -237,9 +238,98 @@\n         )\n \n \n+def legacy_shiki_adr_paths() -> tuple[str, ...]:\n+    \"\"\"Former numeric paths for Shiki SADR-0001 through SADR-0018.\n+\n+    The authoritative slugs come from the current SADR filenames. SADR-0019 is\n+    the namespace decision itself and never shipped under a numeric legacy path.\n+    \"\"\"\n+    paths: list[str] = []\n+    for sadr in sorted((ROOT / \"docs\" / \"adr\").glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\")):\n+        number = sadr.name.split(\"-\", 2)[1]\n+        if int(number) <= LEGACY_SHIKI_SADR_MAX:\n+            paths.append(f\"docs/adr/{sadr.name.removeprefix('SADR-')}\")\n+    return tuple(paths)\n+\n+\n+def preflight_legacy_sadr_cleanup(target: Path, *, force: bool) -> tuple[str, ...]:\n+    \"\"\"Authorize exact legacy Shiki ADR removals before the first write.\n+\n+    A non-force install never removes existing files. A forced install needs no\n+    stamp when no legacy Shiki path is present. When legacy paths do exist, each\n+    must be named in a readable install stamp and its current bytes must match\n+    the recorded digest.\n+    \"\"\"\n+    if not force:\n+        return ()\n+    present = tuple(\n+        relative\n+        for relative in legacy_shiki_adr_paths()\n+        if (target / relative).exists() or (target / relative).is_symlink()\n+    )\n+    if not present:\n+        return ()\n+\n+    stamp_path = target / INSTALL_STAMP_PATH\n+    try:\n+        raw = stamp_path.read_text(encoding=\"utf-8\")\n+        stamp = json.loads(raw)\n+    except FileNotFoundError:\n+        detail = \"install stamp is absent\"\n+        blockers = [f\"{relative}: {detail}\" for relative in present]\n+    except (OSError, UnicodeDecodeError) as error:\n+        detail = f\"install stamp is unreadable ({error})\"\n+        blockers = [f\"{relative}: {detail}\" for relative in present]\n+    except json.JSONDecodeError as error:\n+        detail = f\"install stamp is malformed ({error})\"\n+        blockers = [f\"{relative}: {detail}\" for relative in present]\n+    else:\n+        if not isinstance(stamp, dict):\n+            blockers = [\n+                f\"{relative}: install stamp is malformed (expected an object)\"\n+                for relative in present\n+            ]\n+        else:\n+            digests = stamp.get(\"digests\")\n+            blockers = []\n+            if not isinstance(digests, dict):\n+                blockers.extend(\n+                    f\"{relative}: install stamp has no digest for this path\"\n+                    for relative in present\n+                )\n+            else:\n+                for relative in present:\n+                    expected = digests.get(relative)\n+                    if not isinstance(expected, str) or not expected:\n+                        blockers.append(\n+                            f\"{relative}: install stamp has no digest for this path\"\n+                        )\n+                        continue\n+                    try:\n+                        actual = _sha256_file(target / relative)\n+                    except OSError as error:\n+                        blockers.append(\n+                            f\"{relative}: legacy path is unreadable ({error})\"\n+                        )\n+                        continue\n+                    if actual != expected:\n+                        blockers.append(\n+                            f\"{relative}: digest mismatch (stamp {expected}, current {actual})\"\n+                        )\n+\n+    if blockers:\n+        joined = \"\\n\".join(f\"- {blocker}\" for blocker in blockers)\n+        raise ShikiError(\n+            \"legacy Shiki ADR cleanup is not authorized:\\n\"\n+            f\"{joined}\\nNo files were written.\"\n+        )\n+    return present\n+\n+\n def install_template(target: Path, *, force: bool, validate: bool) -> list[NewFileNote]:\n     # Preconditions first: never rewrite the tree of a target that cannot be\n     # upgraded cleanly (see refuse_pending_migrations).\n+    legacy_cleanup = preflight_legacy_sadr_cleanup(target, force=force)\n     refuse_pending_migrations(target)\n \n     # Resolve the target's required CODEOWNERS owner once from its\n@@ -250,6 +340,10 @@\n     # verbatim. See ``codeowners_required_owner``.\n     codeowners_owner = codeowners_required_owner(target)\n \n+    for relative in legacy_cleanup:\n+        (target / relative).unlink()\n+        info(f\"removed stamped legacy Shiki ADR: {target / relative}\")\n+\n     notes: list[NewFileNote] = []\n     for relative in TEMPLATE_PATHS:\n         source = ROOT / relative\ndiff --git a/tests/test_adr_references.py b/tests/test_adr_references.py\nindex 51706329c797d5dd9b47e082a333fa83b261d482..91ab76b724cb973435c45d53cab63793942267e9\n--- a/tests/test_adr_references.py\n+++ b/tests/test_adr_references.py\n@@ -1,4 +1,4 @@\n-\"\"\"Every ``ADR ####`` citation must resolve to exactly one decision record.\n+\"\"\"Every target ADR and Shiki SADR citation resolves in its own namespace.\n \n Background. ``scripts/mergegate_check.py`` justifies ``is_bookkeeping_closeout``\n with \"ADR 0017\" nine times and ``scripts/guardian_approval_signal.py`` cites\n@@ -11,11 +11,12 @@\n and the directory drifted with no signal.\n \n This is the same unbound-pair shape behind other silent failures: a reference\n-and its target with nothing asserting they agree. This test binds them. It scans\n-every ``scripts/*.py`` (plus ``docs/**/*.md`` and the root ``*.md`` governance\n-files, which are cheap) for the ``ADR ####`` pattern and asserts each cited\n-number resolves to exactly one ``docs/adr/####-*.md``. The converse is\n-deliberately NOT required: an ADR may exist without being cited.\n+and its target with nothing asserting they agree. This test binds them. Target\n+``ADR NNNN`` citations resolve only to ``docs/adr/NNNN-*.md``; Shiki\n+``SADR-NNNN`` citations resolve only to ``docs/adr/SADR-NNNN-*.md``. The scan\n+covers current governance docs, scripts, workflows, prompts, packaging metadata,\n+and repo-local Codex guidance while deliberately excluding historical\n+``.shiki`` evidence. The converse is not required: a record may be uncited.\n \n The suite is dependency free: standard-library ``unittest`` only.\n \"\"\"\n@@ -31,17 +32,18 @@\n \n REPO_ROOT = Path(__file__).resolve().parents[1]\n \n-# \"ADR 0016\" -- a four-digit citation. The trailing \\b keeps a five-digit token\n-# from being read as a truncated four-digit number.\n-_CITATION = re.compile(r\"\\bADR (\\d{4})\\b\")\n-# \"0016-locks-bound-product-paths.md\" -- the leading four digits name the record.\n-_ADR_FILENAME = re.compile(r\"^(\\d{4})-.*\\.md$\")\n+# ``ADR 0016`` / ``SADR-0016`` -- a namespace plus four-digit citation. The\n+# trailing ``\\b`` keeps a five-digit token from being read as a truncated one.\n+_CITATION = re.compile(r\"\\b(S?ADR)[ -](\\d{4})\\b\")\n+# Target ADRs retain the numeric filename; Shiki platform decisions use SADR.\n+_DECISION_FILENAME = re.compile(r\"^(?:(SADR)-)?(\\d{4})-.*\\.md$\")\n \n \n class Violation(NamedTuple):\n     \"\"\"A citation that does not resolve to exactly one decision record.\"\"\"\n \n     citing_file: str  # repo-relative path of the file that cites the ADR\n+    namespace: str  # \"ADR\" for target decisions; \"SADR\" for Shiki decisions\n     number: str  # the cited four-digit number, e.g. \"0016\"\n     line: int  # 1-based line of the citation\n     reason: str  # \"missing\" (zero matches) or \"ambiguous\" (two or more)\n@@ -49,44 +51,59 @@\n \n     @property\n     def expected_path(self) -> str:\n-        return f\"docs/adr/{self.number}-*.md\"\n+        prefix = \"SADR-\" if self.namespace == \"SADR\" else \"\"\n+        return f\"docs/adr/{prefix}{self.number}-*.md\"\n \n     def describe(self) -> str:\n         \"\"\"Name the citing file, the cited number, and the missing path.\"\"\"\n         if self.reason == \"missing\":\n             return (\n-                f\"{self.citing_file}:{self.line} cites ADR {self.number} \"\n+                f\"{self.citing_file}:{self.line} cites {self.namespace} {self.number} \"\n                 f\"but no document matches {self.expected_path}\"\n             )\n         joined = \", \".join(self.matches)\n         return (\n-            f\"{self.citing_file}:{self.line} cites ADR {self.number} which \"\n+            f\"{self.citing_file}:{self.line} cites {self.namespace} {self.number} which \"\n             f\"resolves to {len(self.matches)} documents (expected exactly one \"\n             f\"at {self.expected_path}): {joined}\"\n         )\n \n \n-def _adr_index(root: Path) -> dict[str, list[Path]]:\n-    \"\"\"Map every four-digit ADR number to the record files that claim it.\"\"\"\n-    index: dict[str, list[Path]] = {}\n+def _adr_index(root: Path) -> dict[tuple[str, str], list[Path]]:\n+    \"\"\"Map each decision namespace and number to the files that claim it.\"\"\"\n+    index: dict[tuple[str, str], list[Path]] = {}\n     adr_dir = root / \"docs\" / \"adr\"\n     if adr_dir.is_dir():\n         for path in sorted(adr_dir.glob(\"*.md\")):\n-            match = _ADR_FILENAME.match(path.name)\n+            match = _DECISION_FILENAME.match(path.name)\n             if match:\n-                index.setdefault(match.group(1), []).append(path)\n+                namespace = \"SADR\" if match.group(1) else \"ADR\"\n+                index.setdefault((namespace, match.group(2)), []).append(path)\n     return index\n \n \n def _citing_files(root: Path, include_docs: bool = True) -> list[Path]:\n-    \"\"\"Files scanned for citations: scripts/*.py, and cheaply the markdown.\"\"\"\n+    \"\"\"Current, shipped text surfaces scanned for decision citations.\"\"\"\n     files = sorted((root / \"scripts\").glob(\"*.py\"))\n+    files += sorted((root / \"scripts\").glob(\"*.sh\"))\n     if include_docs:\n         docs = root / \"docs\"\n         if docs.is_dir():\n             files += sorted(docs.rglob(\"*.md\"))\n         files += sorted(root.glob(\"*.md\"))\n-    return files\n+        pyproject = root / \"pyproject.toml\"\n+        if pyproject.is_file():\n+            files.append(pyproject)\n+        for relative in (\".github\", \".codex\"):\n+            surface = root / relative\n+            if surface.is_dir():\n+                files += sorted(\n+                    path\n+                    for path in surface.rglob(\"*\")\n+                    if path.is_file()\n+                    and path.suffix in {\".md\", \".py\", \".sh\", \".toml\", \".yaml\", \".yml\"}\n+                )\n+    return sorted(set(files))\n \n \n def find_adr_reference_violations(\n@@ -94,10 +111,9 @@\n ) -> list[Violation]:\n     \"\"\"Return every ``ADR ####`` citation under ``root`` that fails to resolve.\n \n-    A citation resolves when exactly one ``docs/adr/####-*.md`` matches its\n-    number. Zero matches is a \"missing\" violation; two or more is \"ambiguous\".\n-    Records that are never cited produce nothing -- the converse is not\n-    required.\n+    A citation resolves when exactly one record in the same namespace matches\n+    its number. Zero matches is a \"missing\" violation; two or more is\n+    \"ambiguous\". Records that are never cited produce nothing.\n     \"\"\"\n     root = Path(root)\n     index = _adr_index(root)\n@@ -110,13 +126,15 @@\n         rel = path.relative_to(root).as_posix()\n         for lineno, line in enumerate(text.splitlines(), start=1):\n             for match in _CITATION.finditer(line):\n-                number = match.group(1)\n-                found = index.get(number, [])\n+                namespace, number = match.groups()\n+                found = index.get((namespace, number), [])\n                 if len(found) == 1:\n                     continue\n                 reason = \"missing\" if not found else \"ambiguous\"\n                 rels = [p.relative_to(root).as_posix() for p in found]\n-                violations.append(Violation(rel, number, lineno, reason, rels))\n+                violations.append(\n+                    Violation(rel, namespace, number, lineno, reason, rels)\n+                )\n     return violations\n \n \n@@ -129,8 +147,8 @@\n         self.assertEqual(\n             violations,\n             [],\n-            \"Every 'ADR ####' citation must resolve to exactly one \"\n-            f\"docs/adr/####-*.md:\\n{detail}\",\n+            \"Every ADR/SADR citation must resolve exactly once in its own \"\n+            f\"docs/adr namespace:\\n{detail}\",\n         )\n \n     def test_scan_actually_reads_scripts(self) -> None:\n@@ -146,8 +164,21 @@\n         # The two records this Goal carried onto the default branch, and which\n         # the scripts cite most, must be present.\n         index = _adr_index(REPO_ROOT)\n-        self.assertIn(\"0016\", index)\n-        self.assertIn(\"0017\", index)\n+        self.assertIn((\"SADR\", \"0016\"), index)\n+        self.assertIn((\"SADR\", \"0017\"), index)\n+\n+    def test_platform_tree_has_only_sadr_0001_through_0019(self) -> None:\n+        adr_dir = REPO_ROOT / \"docs\" / \"adr\"\n+        sadrs = sorted(adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\"))\n+        self.assertEqual(\n+            {path.name.split(\"-\", 2)[1] for path in sadrs},\n+            {f\"{number:04d}\" for number in range(1, 20)},\n+        )\n+        self.assertEqual(\n+            list(adr_dir.glob(\"[0-9][0-9][0-9][0-9]-*.md\")),\n+            [],\n+            \"the Shiki platform tree must not retain legacy numeric SADR paths\",\n+        )\n \n \n class FixtureAdrReferences(unittest.TestCase):\n@@ -180,6 +211,26 @@\n         self.assertIn(\"9991\", message)  # cited number\n         self.assertIn(\"docs/adr/9991-*.md\", message)  # missing path\n \n+    def test_target_adr_does_not_satisfy_missing_platform_sadr(self) -> None:\n+        root = self._make_repo(\n+            adr_names=[\"0042-target-decision.md\"],\n+            script_body='\"\"\"Target ADR 0042 exists, but platform SADR-0042 does not.\"\"\"\\n',\n+        )\n+        violations = find_adr_reference_violations(root, include_docs=False)\n+        self.assertEqual(len(violations), 1)\n+        self.assertEqual(violations[0].namespace, \"SADR\")\n+        self.assertEqual(violations[0].number, \"0042\")\n+        self.assertEqual(violations[0].expected_path, \"docs/adr/SADR-0042-*.md\")\n+\n+    def test_target_adr_and_platform_sadr_may_reuse_a_number(self) -> None:\n+        root = self._make_repo(\n+            adr_names=[\"0042-target-decision.md\", \"SADR-0042-platform-decision.md\"],\n+            script_body='\"\"\"Target ADR 0042 and platform SADR-0042 are distinct.\"\"\"\\n',\n+        )\n+        self.assertEqual(\n+            find_adr_reference_violations(root, include_docs=False), []\n+        )\n+\n     def test_uncited_adr_is_allowed(self) -> None:\n         # An ADR exists but nothing cites it -> not a violation. The converse\n         # (every ADR must be cited) is explicitly NOT required.\ndiff --git a/tests/test_sadr_install_migration.py b/tests/test_sadr_install_migration.py\nnew file mode 100644\nindex 0000000000000000000000000000000000000000..5a442fa5e4968ade178dcdedc3c63b54dba580a2\n--- /dev/null\n+++ b/tests/test_sadr_install_migration.py\n@@ -0,0 +1,224 @@\n+\"\"\"SADR installation and collision-safe legacy upgrade behavior.\"\"\"\n+\n+from __future__ import annotations\n+\n+import contextlib\n+import hashlib\n+import io\n+import json\n+import shutil\n+import tempfile\n+import unittest\n+from pathlib import Path\n+\n+import shiki_test_support  # noqa: F401  (path bootstrap)\n+\n+import shiki_installer\n+from tests.test_adr_references import find_adr_reference_violations\n+\n+\n+PRODUCT_NUMBERS = tuple(range(13, 19))\n+\n+\n+def _quiet_install(target: Path, *, force: bool, validate: bool) -> None:\n+    with contextlib.redirect_stdout(io.StringIO()):\n+        shiki_installer.install_template(target, force=force, validate=validate)\n+\n+\n+def _sha256(path: Path) -> str:\n+    return hashlib.sha256(path.read_bytes()).hexdigest()\n+\n+\n+def _legacy_sadr_paths() -> tuple[str, ...]:\n+    return tuple(\n+        f\"docs/adr/{path.name.removeprefix('SADR-')}\"\n+        for path in sorted((shiki_installer.ROOT / \"docs\" / \"adr\").glob(\"SADR-*.md\"))\n+        if path.name != \"SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\"\n+    )\n+\n+\n+def _create_legacy_upgrade_fixture(\n+    target: Path,\n+) -> tuple[dict[str, bytes], tuple[str, ...]]:\n+    adr_dir = target / \"docs\" / \"adr\"\n+    adr_dir.mkdir(parents=True)\n+    product_bytes = {\n+        f\"{number:04d}-cubeshare-product-decision.md\": (\n+            f\"# CubeShare ADR {number:04d}\\n\\nDo not rewrite {number}.\\n\"\n+        ).encode()\n+        for number in PRODUCT_NUMBERS\n+    }\n+    for name, content in product_bytes.items():\n+        (adr_dir / name).write_bytes(content)\n+\n+    legacy_paths = _legacy_sadr_paths()\n+    for relative in legacy_paths:\n+        (target / relative).write_bytes(f\"legacy Shiki bytes for {relative}\\n\".encode())\n+\n+    stamp_path = target / shiki_installer.INSTALL_STAMP_PATH\n+    stamp_path.parent.mkdir(parents=True, exist_ok=True)\n+    stamp = {\n+        \"version\": shiki_installer.INSTALL_STAMP_VERSION,\n+        \"platform_commit\": None,\n+        \"installed_at\": \"2000-01-01T00:00:00+00:00\",\n+        \"digests\": {\n+            relative: _sha256(target / relative) for relative in legacy_paths\n+        },\n+    }\n+    stamp_path.write_text(\n+        json.dumps(stamp, indent=2, sort_keys=True) + \"\\n\", encoding=\"utf-8\"\n+    )\n+    return product_bytes, legacy_paths\n+\n+\n+def _tree_snapshot(root: Path) -> dict[str, tuple[str, bytes | str]]:\n+    \"\"\"Recursive byte/shape snapshot used to prove preflight is write-free.\"\"\"\n+    snapshot: dict[str, tuple[str, bytes | str]] = {}\n+    for path in sorted(root.rglob(\"*\")):\n+        relative = path.relative_to(root).as_posix()\n+        if path.is_symlink():\n+            snapshot[relative] = (\"symlink\", path.readlink().as_posix())\n+        elif path.is_dir():\n+            snapshot[relative] = (\"directory\", \"\")\n+        else:\n+            snapshot[relative] = (\"file\", path.read_bytes())\n+    return snapshot\n+\n+\n+class FreshSadrInstallTests(unittest.TestCase):\n+    def setUp(self) -> None:\n+        self.tmp = Path(tempfile.mkdtemp(prefix=\"shiki-sadr-fresh-\"))\n+        self.addCleanup(lambda: shutil.rmtree(self.tmp, ignore_errors=True))\n+        self.target = self.tmp / \"target\"\n+        self.adr_dir = self.target / \"docs\" / \"adr\"\n+        self.adr_dir.mkdir(parents=True)\n+        self.product_bytes = {\n+            f\"{number:04d}-product-decision.md\": (\n+                f\"# Product ADR {number:04d}\\n\\nTarget-owned bytes {number}.\\n\"\n+            ).encode()\n+            for number in PRODUCT_NUMBERS\n+        }\n+        for name, content in self.product_bytes.items():\n+            (self.adr_dir / name).write_bytes(content)\n+\n+    def test_fresh_install_preserves_product_adrs_and_installs_all_sadrs(self) -> None:\n+        _quiet_install(self.target, force=False, validate=True)\n+\n+        for name, expected in self.product_bytes.items():\n+            self.assertEqual((self.adr_dir / name).read_bytes(), expected)\n+\n+        sadr_numbers = {\n+            path.name.split(\"-\", 2)[1]\n+            for path in self.adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\")\n+        }\n+        self.assertEqual(sadr_numbers, {f\"{number:04d}\" for number in range(1, 20)})\n+\n+        numeric_records = {\n+            path.name\n+            for path in self.adr_dir.glob(\"[0-9][0-9][0-9][0-9]-*.md\")\n+        }\n+        self.assertEqual(numeric_records, set(self.product_bytes))\n+        self.assertEqual(find_adr_reference_violations(self.target), [])\n+\n+\n+class LegacySadrUpgradeTests(unittest.TestCase):\n+    def setUp(self) -> None:\n+        self.tmp = Path(tempfile.mkdtemp(prefix=\"shiki-sadr-upgrade-\"))\n+        self.addCleanup(lambda: shutil.rmtree(self.tmp, ignore_errors=True))\n+        self.target = self.tmp / \"target\"\n+        self.adr_dir = self.target / \"docs\" / \"adr\"\n+        self.product_bytes, self.legacy_paths = _create_legacy_upgrade_fixture(\n+            self.target\n+        )\n+\n+    def test_force_upgrade_deletes_only_stamped_legacy_sadrs(self) -> None:\n+        _quiet_install(self.target, force=True, validate=True)\n+\n+        for relative in self.legacy_paths:\n+            self.assertFalse((self.target / relative).exists(), relative)\n+        for name, expected in self.product_bytes.items():\n+            self.assertEqual((self.adr_dir / name).read_bytes(), expected)\n+\n+        sadrs = list(self.adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\"))\n+        self.assertEqual(len(sadrs), 19)\n+        refreshed = json.loads(\n+            (self.target / shiki_installer.INSTALL_STAMP_PATH).read_text(encoding=\"utf-8\")\n+        )\n+        for relative in self.legacy_paths:\n+            self.assertNotIn(relative, refreshed[\"digests\"])\n+        for path in sadrs:\n+            relative = path.relative_to(self.target).as_posix()\n+            self.assertEqual(refreshed[\"digests\"][relative], _sha256(path))\n+\n+    def test_non_force_install_never_deletes_legacy_files(self) -> None:\n+        before = {\n+            relative: (self.target / relative).read_bytes()\n+            for relative in self.legacy_paths\n+        }\n+        _quiet_install(self.target, force=False, validate=False)\n+        for relative, expected in before.items():\n+            self.assertEqual((self.target / relative).read_bytes(), expected)\n+\n+\n+class LegacyCleanupPreflightTests(unittest.TestCase):\n+    def setUp(self) -> None:\n+        self.tmp = Path(tempfile.mkdtemp(prefix=\"shiki-sadr-preflight-\"))\n+        self.addCleanup(lambda: shutil.rmtree(self.tmp, ignore_errors=True))\n+\n+    def test_all_ownership_blockers_abort_before_any_write(self) -> None:\n+        cases = (\n+            (\"absent\", \"install stamp is absent\"),\n+            (\"unreadable\", \"install stamp is unreadable\"),\n+            (\"malformed\", \"install stamp is malformed\"),\n+            (\"missing-digest\", \"install stamp has no digest\"),\n+            (\"digest-mismatch\", \"digest mismatch\"),\n+        )\n+        for name, expected_reason in cases:\n+            with self.subTest(name=name):\n+                target = self.tmp / name\n+                _, legacy_paths = _create_legacy_upgrade_fixture(target)\n+                stamp_path = target / shiki_installer.INSTALL_STAMP_PATH\n+\n+                if name == \"absent\":\n+                    stamp_path.unlink()\n+                elif name == \"unreadable\":\n+                    stamp_path.unlink()\n+                    stamp_path.mkdir()\n+                elif name == \"malformed\":\n+                    stamp_path.write_text(\"{not-json\\n\", encoding=\"utf-8\")\n+                else:\n+                    stamp = json.loads(stamp_path.read_text(encoding=\"utf-8\"))\n+                    if name == \"missing-digest\":\n+                        stamp[\"digests\"] = {}\n+                    else:\n+                        stamp[\"digests\"] = {\n+                            relative: \"deadbeef\" for relative in legacy_paths\n+                        }\n+                    stamp_path.write_text(\n+                        json.dumps(stamp, indent=2, sort_keys=True) + \"\\n\",\n+                        encoding=\"utf-8\",\n+                    )\n+\n+                before = _tree_snapshot(target)\n+                with self.assertRaises(shiki_installer.ShikiError) as raised:\n+                    _quiet_install(target, force=True, validate=False)\n+                after = _tree_snapshot(target)\n+\n+                self.assertEqual(before, after, f\"{name} preflight changed the tree\")\n+                message = str(raised.exception)\n+                self.assertIn(expected_reason, message)\n+                self.assertIn(\"No files were written\", message)\n+                for relative in legacy_paths:\n+                    self.assertIn(relative, message)\n+\n+    def test_absent_stamp_is_allowed_when_no_legacy_path_exists(self) -> None:\n+        target = self.tmp / \"no-legacy\"\n+        target.mkdir()\n+        _quiet_install(target, force=True, validate=False)\n+        self.assertTrue(\n+            (target / \"docs\" / \"adr\" / \"SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\").is_file()\n+        )\n+\n+\n+if __name__ == \"__main__\":\n+    unittest.main()\n\ndiff --git a/.codex/skills/shiki/SKILL.md b/.codex/skills/shiki/SKILL.md\nindex c8e2e85d9d75d49cc4f2d6286ee22659c4e53e75..93cded623b2c15d9f65c76491a1c2d813ac9e6c0\n--- a/.codex/skills/shiki/SKILL.md\n+++ b/.codex/skills/shiki/SKILL.md\n@@ -18,6 +18,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n Codex CLI does not register this skill as a custom `/shiki` slash command. When\n the user types `/shiki` in Codex CLI and gets \"Unrecognized command\", explain\n that this is expected: invoke Shiki in Codex with natural language such as\n@@ -69,7 +72,7 @@\n \n ## Responsibilities\n \n-- Claude Code implements and repairs by default (`shiki runner claude`, ADR 0008); Codex implements and repairs tasks explicitly assigned to `codex`.\n+- Claude Code implements and repairs by default (`shiki runner claude`, SADR-0008); Codex implements and repairs tasks explicitly assigned to `codex`.\n - Claude Code Action can act as GitHub-side CCA or reviewer.\n - CCA judges completion.\n - MergeGate authorizes state transitions and merge readiness.\ndiff --git a/.github/prompts/cca-completion-check.md b/.github/prompts/cca-completion-check.md\nindex ed203ad5f302ecc81372d8ab27d2b1232ca97497..fd89950528333ef6242e6d5443904f5392ab7e56\n--- a/.github/prompts/cca-completion-check.md\n+++ b/.github/prompts/cca-completion-check.md\n@@ -19,6 +19,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n ## Required Reading\n \n Read only the files needed to judge the PR contract. Prefer this order:\ndiff --git a/AGENTS.md b/AGENTS.md\nindex 7f041100d49c2a7525f51cb0f5d696a4a6d1c78c..2b700e989ff295987aa8a4b0c5fcb1acd80020da\n--- a/AGENTS.md\n+++ b/AGENTS.md\n@@ -44,6 +44,10 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use\n+`SADR-NNNN-*.md`; target product decisions retain `NNNN-*.md` and the `ADR`\n+namespace.\n+\n Use the canonical source-of-truth order above. Runtime-specific reading orders\n are operational guidance only and must not override or reorder it.\n \ndiff --git a/CLAUDE.md b/CLAUDE.md\nindex 98a932e7841a632866fcd53b0bad0e3af3711d3d..611d491912d6a72a0327a1731b8f33a41acb4baa\n--- a/CLAUDE.md\n+++ b/CLAUDE.md\n@@ -14,6 +14,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n ## Claude Role\n \n Default to planner, default implementer, reviewer, coordinator, documentation editor, CCA-style completion judge, and final-judgment assistant.\ndiff --git a/SYSTEM_PROMPT.md b/SYSTEM_PROMPT.md\nindex aaaea45e3a896c3a44ba89eec8bb84325ecf6c62..2e7c093ebe9e8bb34e0923756f191b4f7fa581f5\n--- a/SYSTEM_PROMPT.md\n+++ b/SYSTEM_PROMPT.md\n@@ -30,6 +30,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n Use the canonical source-of-truth order above. Runtime-specific reading orders\n are operational guidance only and must not override or reorder it. The current\n conversation is non-durable operator input.\ndiff --git a/docs/adr/0001-keep-shiki-as-platform-name.md b/docs/adr/SADR-0001-keep-shiki-as-platform-name.md\nindex 20a9637c3b9d8c97de7ba8db7cfae4d82d9a7dba..8b685439959aada1b221c112f0ac3c51ca8a1695\n--- a/docs/adr/0001-keep-shiki-as-platform-name.md\n+++ b/docs/adr/SADR-0001-keep-shiki-as-platform-name.md\n@@ -1,3 +1,3 @@\n-# Keep Shiki As The Platform Name\n+# SADR-0001: Keep Shiki As The Platform Name\n \n Shiki remains the product name for the GitHub-native agentic engineering control plane. We considered more descriptive names such as MergePilot, MergeGate, and Kiroku, but kept Shiki because it carries the existing goal-seeking framework identity while remaining independent of Hermes, Codex, Claude Code, or any single runtime.\ndiff --git a/docs/adr/0002-use-agents-as-shared-constitution.md b/docs/adr/SADR-0002-use-agents-as-shared-constitution.md\nindex 73de30ecb078120eac540d9bc51855e3cd48d0e3..c5486182bf06fca36ea508496b013c67302fd989\n--- a/docs/adr/0002-use-agents-as-shared-constitution.md\n+++ b/docs/adr/SADR-0002-use-agents-as-shared-constitution.md\n@@ -1,3 +1,3 @@\n-# Use AGENTS As The Shared Agent Constitution\n+# SADR-0002: Use AGENTS As The Shared Agent Constitution\n \n Shiki uses `AGENTS.md` as the runtime-neutral constitution and `CLAUDE.md` as a thin Claude Code wrapper. This keeps Codex, Claude Code, Hermes Runner, and future Agent Runtimes aligned on the same GitHub-first MergeGate rules while still allowing runtime-specific instructions where needed.\ndiff --git a/docs/adr/0003-require-skill-gate-for-mergegate-readiness.md b/docs/adr/SADR-0003-require-skill-gate-for-mergegate-readiness.md\nindex 424be0c9af4c8f0136f1fde68a468c47ff84e764..7fc337987c7df9792f75f181dfcbed75fe5ffff2\n--- a/docs/adr/0003-require-skill-gate-for-mergegate-readiness.md\n+++ b/docs/adr/SADR-0003-require-skill-gate-for-mergegate-readiness.md\n@@ -1,3 +1,3 @@\n-# Require Skill Gate For MergeGate Readiness\n+# SADR-0003: Require Skill Gate For MergeGate Readiness\n \n Shiki requires relevant engineering skills to be invoked before execution when their triggers apply. This makes skills part of MergeGate readiness rather than optional prompt style, because Shiki depends on repeatable planning, issue decomposition, TDD, diagnosis, triage, and architecture review before autonomous or parallel agent work can be trusted.\ndiff --git a/docs/adr/0004-use-github-first-with-shiki-mirror.md b/docs/adr/SADR-0004-use-github-first-with-shiki-mirror.md\nindex d0a3a9174dc646a945a9d325954ffaf00c352040..a79c54ae9ee8eed2a0524192b864da4e22a92d3f\n--- a/docs/adr/0004-use-github-first-with-shiki-mirror.md\n+++ b/docs/adr/SADR-0004-use-github-first-with-shiki-mirror.md\n@@ -1,4 +1,4 @@\n-# ADR 0004: Use GitHub First With A Repository-Local Shiki Mirror\n+# SADR-0004: Use GitHub First With A Repository-Local Shiki Mirror\n \n ## Status\n \ndiff --git a/docs/adr/0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md b/docs/adr/SADR-0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\nindex b339baf49b89d9290e79c3d51b36e7b2bde3bd95..c30a0d8b1fde0224d0ac7820e7870dccf721580d\n--- a/docs/adr/0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\n+++ b/docs/adr/SADR-0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\n@@ -1,4 +1,4 @@\n-# ADR 0005: Use Codex As Front And Claude Code Action As The GitHub Runtime\n+# SADR-0005: Use Codex As Front And Claude Code Action As The GitHub Runtime\n \n ## Status\n \ndiff --git a/docs/adr/0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md b/docs/adr/SADR-0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\nindex 4bcae9d1e52f0a37b8e9fbafa5cd14b6ee1993bd..7a38538a6ae9929567f0ade67e7ea6a60d0b3f7e\n--- a/docs/adr/0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\n+++ b/docs/adr/SADR-0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\n@@ -1,4 +1,4 @@\n-# ADR 0006: Separate Codex Implementation, CCA Completion, And MergeGate Authorization\n+# SADR-0006: Separate Codex Implementation, CCA Completion, And MergeGate Authorization\n \n ## Status\n \ndiff --git a/docs/adr/0007-packaging-and-release.md b/docs/adr/SADR-0007-packaging-and-release.md\nindex 79b84ddb97167372b44641e5b7860e400ce80822..023cd57f8011aa6e6d2ebf612b7c35a7a82c00d4\n--- a/docs/adr/0007-packaging-and-release.md\n+++ b/docs/adr/SADR-0007-packaging-and-release.md\n@@ -1,4 +1,4 @@\n-# ADR 0007: Packaging And Release For Shiki\n+# SADR-0007: Packaging And Release For Shiki\n \n ## Status\n \ndiff --git a/docs/adr/0008-use-claude-code-as-default-implementer.md b/docs/adr/SADR-0008-use-claude-code-as-default-implementer.md\nindex dedf4510b7c7799521422f622029563d373db031..a158a8d444d93764ec515a28133f7dde4af23579\n--- a/docs/adr/0008-use-claude-code-as-default-implementer.md\n+++ b/docs/adr/SADR-0008-use-claude-code-as-default-implementer.md\n@@ -1,4 +1,4 @@\n-# ADR 0008: Use Claude Code As The Default Implementer Runtime\n+# SADR-0008: Use Claude Code As The Default Implementer Runtime\n \n ## Status\n \ndiff --git a/docs/adr/0009-requirements-definition-and-spec-freeze.md b/docs/adr/SADR-0009-requirements-definition-and-spec-freeze.md\nindex 29b3f7871bef9203a8b4a831a65c39eb2f506e42..52e7dd69ad0aa4efe0b68c62678abbf7df02aaec\n--- a/docs/adr/0009-requirements-definition-and-spec-freeze.md\n+++ b/docs/adr/SADR-0009-requirements-definition-and-spec-freeze.md\n@@ -1,4 +1,4 @@\n-# ADR 0009: Merge Grill And Planning Into Requirements Definition With Spec Freeze\n+# SADR-0009: Merge Grill And Planning Into Requirements Definition With Spec Freeze\n \n ## Status\n \ndiff --git a/docs/adr/0010-external-ai-guardian-review.md b/docs/adr/SADR-0010-external-ai-guardian-review.md\nindex e6108bbe8bb4e73e0ff4684c8f78001a80438c4e..e690d4eddd270e683d709c9b679c5859e71eeaaa\n--- a/docs/adr/0010-external-ai-guardian-review.md\n+++ b/docs/adr/SADR-0010-external-ai-guardian-review.md\n@@ -1,4 +1,4 @@\n-# ADR 0010: External AI Guardian Review As A First-Class Approval Authority\n+# SADR-0010: External AI Guardian Review As A First-Class Approval Authority\n \n ## Status\n \ndiff --git a/docs/adr/0011-autonomous-quality-gate-evidence.md b/docs/adr/SADR-0011-autonomous-quality-gate-evidence.md\nindex bca0246c5d46e44afbde15486a75eec425fc02f5..9c93eac16d8c3a6d4b11bd7224e3955102cd7db4\n--- a/docs/adr/0011-autonomous-quality-gate-evidence.md\n+++ b/docs/adr/SADR-0011-autonomous-quality-gate-evidence.md\n@@ -1,4 +1,4 @@\n-# ADR 0011: Autonomous Quality-Gate Evidence \u2014 Deterministic Facts And An Independent Verifier\n+# SADR-0011: Autonomous Quality-Gate Evidence \u2014 Deterministic Facts And An Independent Verifier\n \n ## Status\n \ndiff --git a/docs/adr/0012-autonomous-loop-closeout-to-main.md b/docs/adr/SADR-0012-autonomous-loop-closeout-to-main.md\nindex 637fad6dde4989c9c2bf0f81f676d8d29991123c..ec7d85f81030a676bcbba28b43eb365422a36eac\n--- a/docs/adr/0012-autonomous-loop-closeout-to-main.md\n+++ b/docs/adr/SADR-0012-autonomous-loop-closeout-to-main.md\n@@ -1,4 +1,4 @@\n-# 0012 \u2014 Autonomous loop pushes goal completion to main via a closeout PR\n+# SADR-0012 \u2014 Autonomous loop pushes goal completion to main via a closeout PR\n \n - Status: accepted\n - Date: 2026-06-18\ndiff --git a/docs/adr/0013-bootstrap-grants-actions-approve-pull-requests.md b/docs/adr/SADR-0013-bootstrap-grants-actions-approve-pull-requests.md\nindex b6c69d64f1c5ced346b88d6859586715b3b5bd0e..a793e7277d67ab8ea56286fec289ca689825a17d\n--- a/docs/adr/0013-bootstrap-grants-actions-approve-pull-requests.md\n+++ b/docs/adr/SADR-0013-bootstrap-grants-actions-approve-pull-requests.md\n@@ -1,4 +1,4 @@\n-# ADR 0013: Bootstrap Grants GitHub Actions Permission To Create And Approve Pull Requests\n+# SADR-0013: Bootstrap Grants GitHub Actions Permission To Create And Approve Pull Requests\n \n ## Status\n \ndiff --git a/docs/adr/0014-codex-app-external-ai-guardian-ui-adapter.md b/docs/adr/SADR-0014-codex-app-external-ai-guardian-ui-adapter.md\nindex 238db06279334caa341c117362df9271382a7fcd..65f12e9939321e1b3e389a34c822fc1549e5155d\n--- a/docs/adr/0014-codex-app-external-ai-guardian-ui-adapter.md\n+++ b/docs/adr/SADR-0014-codex-app-external-ai-guardian-ui-adapter.md\n@@ -1,4 +1,4 @@\n-# ADR 0014: Use Codex App As The External AI Guardian UI Adapter\n+# SADR-0014: Use Codex App As The External AI Guardian UI Adapter\n \n ## Status\n \ndiff --git a/docs/adr/0015-contract-approval-before-dispatch.md b/docs/adr/SADR-0015-contract-approval-before-dispatch.md\nindex fad6c656741a20a7684ba16080a7db159f63a52f..7bdd760bb3e82eee4e1410f45c783b03b638d456\n--- a/docs/adr/0015-contract-approval-before-dispatch.md\n+++ b/docs/adr/SADR-0015-contract-approval-before-dispatch.md\n@@ -1,4 +1,4 @@\n-# ADR 0015: Approve The Task Contract Before Dispatch, Not The Diff After It\n+# SADR-0015: Approve The Task Contract Before Dispatch, Not The Diff After It\n \n ## Status\n \ndiff --git a/docs/adr/0016-locks-bound-product-paths.md b/docs/adr/SADR-0016-locks-bound-product-paths.md\nindex b5e850f3f1f7f4ef6e7f9d1763a29a51bd300c45..332cc17313498404f41a145291e38447b5de0dc4\n--- a/docs/adr/0016-locks-bound-product-paths.md\n+++ b/docs/adr/SADR-0016-locks-bound-product-paths.md\n@@ -1,4 +1,4 @@\n-# ADR 0016: Locks bound product paths; the Shiki mirror is bound by record identity\n+# SADR-0016: Locks bound product paths; the Shiki mirror is bound by record identity\n \n ## Status\n \ndiff --git a/docs/adr/0017-bookkeeping-closeouts-do-not-inherit-risk.md b/docs/adr/SADR-0017-bookkeeping-closeouts-do-not-inherit-risk.md\nindex 554e3a0d526a72ca067108c5464cc68c3edcf174..83115f69f1b1b48d544c9b0157f377b91cb53abf\n--- a/docs/adr/0017-bookkeeping-closeouts-do-not-inherit-risk.md\n+++ b/docs/adr/SADR-0017-bookkeeping-closeouts-do-not-inherit-risk.md\n@@ -1,4 +1,4 @@\n-# ADR 0017: A Bookkeeping Closeout Does Not Inherit Its Task's Risk\n+# SADR-0017: A Bookkeeping Closeout Does Not Inherit Its Task's Risk\n \n ## Status\n \ndiff --git a/docs/adr/0018-carried-guardian-approval-across-a-proven-base-sync.md b/docs/adr/SADR-0018-carried-guardian-approval-across-a-proven-base-sync.md\nindex 22358562da390ed8b45cd600da08a43d378b4680..38da0fd95102c3496c3219ce2319fce696a2532a\n--- a/docs/adr/0018-carried-guardian-approval-across-a-proven-base-sync.md\n+++ b/docs/adr/SADR-0018-carried-guardian-approval-across-a-proven-base-sync.md\n@@ -1,4 +1,4 @@\n-# ADR 0018: Carried Guardian Approval Across A Proven Base Sync\n+# SADR-0018: Carried Guardian Approval Across A Proven Base Sync\n \n ## Status\n \ndiff --git a/docs/adr/README.md b/docs/adr/README.md\nindex c70e057a4520c36e042ab68c53cd4a21ef709b71..5da43856836e2ce6a150f8127766e2f88355ab4b\n--- a/docs/adr/README.md\n+++ b/docs/adr/README.md\n@@ -1,40 +1,41 @@\n-# Architecture Decision Records\n+# Decision Record Namespaces\n \n-This directory holds Shiki's Architecture Decision Records (ADRs): durable\n-records of hard-to-reverse platform decisions. ADRs are part of the Shiki source\n-of truth alongside GitHub state, the `.shiki/` mirror, and `CONTEXT.md`.\n+This directory holds two independent decision namespaces:\n \n-## How ADRs Work\n+- Shiki Architecture Decision Records (SADRs), named\n+  `SADR-NNNN-<short-slug>.md`, record hard-to-reverse Shiki platform decisions.\n+- Target Architecture Decision Records (ADRs), named\n+  `NNNN-<short-slug>.md`, belong to the target product.\n \n-- New ADRs are added as `docs/adr/NNNN-<short-slug>.md`, where `NNNN` is the next\n-  zero-padded sequence number.\n-- The **canonical list of ADRs is the set of files matching `docs/adr/*.md`** in\n-  this directory. Browse `docs/adr/*.md` for the authoritative, current set.\n-- Use [`template.md`](template.md) as the starting point for a new ADR.\n-- Record an ADR when a decision is hard to reverse, surprising without context,\n-  and a real tradeoff. Do not write an ADR for routine or easily reversible\n-  choices.\n+The same four-digit number may appear once in each namespace. A platform\n+`SADR-NNNN` citation resolves only to `SADR-NNNN-*.md`; a target `ADR NNNN`\n+citation resolves only to `NNNN-*.md`.\n \n-Using the `docs/adr/*.md` glob as the source of truth means other PRs can add an\n-ADR without editing this index file, avoiding merge contention on a\n-hand-maintained enumeration.\n+## How Decision Records Work\n \n-## Current ADRs (snapshot)\n-\n-The list below is a convenience snapshot and may lag behind the directory. When\n-in doubt, list `docs/adr/*.md`.\n+- New Shiki platform decisions use the next zero-padded SADR number.\n+- New target product decisions use the target's own ADR sequence.\n+- The canonical list is the files in `docs/adr/`; no hand-maintained index is\n+  authoritative.\n+- Use [`template.md`](template.md) as the starting point and choose the filename\n+  namespace that owns the decision.\n+- Record a decision when it is hard to reverse, surprising without context, and\n+  a real tradeoff. Do not write a decision record for routine or easily\n+  reversible choices.\n \n-- [0001 \u2014 Keep Shiki As The Platform Name](0001-keep-shiki-as-platform-name.md)\n-- [0002 \u2014 Use AGENTS As The Shared Agent Constitution](0002-use-agents-as-shared-constitution.md)\n-- [0003 \u2014 Require Skill Gate For MergeGate Readiness](0003-require-skill-gate-for-mergegate-readiness.md)\n-- [0004 \u2014 Use GitHub First With A Repository-Local Shiki Mirror](0004-use-github-first-with-shiki-mirror.md)\n-- [0005 \u2014 Use Codex As Front And Claude Code Action As The GitHub Runtime](0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md)\n-- [0006 \u2014 Separate Codex Implementation, CCA Completion, And MergeGate Authorization](0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md)\n+Using the directory as the source of truth lets independent changes add records\n+without contending on an enumerated index.\n \n-## Adding A New ADR\n+## Adding A Shiki SADR\n \n-1. Copy `template.md` to `docs/adr/NNNN-<short-slug>.md`.\n+1. Copy `template.md` to `docs/adr/SADR-NNNN-<short-slug>.md`.\n 2. Fill in the title, status, context, decision, and consequences.\n-3. Set the status to `Proposed`, then `Accepted` once the decision is approved.\n-4. Open a PR following `CONTRIBUTING.md`. You do not need to edit this index for\n-   the ADR to be discoverable via `docs/adr/*.md`.\n+3. Set the status to `Proposed`, then `Accepted` once the proper authority has\n+   approved the decision.\n+4. Open a PR following `CONTRIBUTING.md`.\n+\n+## Adding A Target ADR\n+\n+1. Copy `template.md` to `docs/adr/NNNN-<short-slug>.md` using the target's own\n+   sequence.\n+2. Cite it as `ADR NNNN`; do not prefix its filename with `SADR-`.\ndiff --git a/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md b/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\nnew file mode 100644\nindex 0000000000000000000000000000000000000000..7455ce1f3bed02ddddb851ac21a1d1a648c9aeba\n--- /dev/null\n+++ b/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\n@@ -0,0 +1,55 @@\n+# SADR-0019: Separate Shiki Platform SADRs From Target ADRs\n+\n+## Status\n+\n+Accepted\n+\n+## Context\n+\n+Shiki installs its platform decision records into `docs/adr/` in every target\n+repository. Shiki and its targets previously used the same numeric\n+`NNNN-<slug>.md` namespace. A target that already owned ADRs 0013 through 0018\n+therefore acquired two records for each number during installation, making an\n+`ADR NNNN` citation ambiguous. Renumbering target decisions would rewrite target\n+history, while renumbering Shiki decisions per target would destroy their stable\n+platform identity.\n+\n+Upgrades also need to remove the former Shiki-owned numeric paths without ever\n+mistaking a target-owned ADR for platform content. The install stamp already\n+records exact shipped paths and content digests, so it can prove ownership only\n+when both still match.\n+\n+## Decision\n+\n+Shiki platform decisions use the `SADR` namespace:\n+\n+- records are named `docs/adr/SADR-NNNN-<slug>.md`;\n+- live platform citations use `SADR-NNNN`;\n+- target product decisions retain `docs/adr/NNNN-<slug>.md` and `ADR NNNN`;\n+- the reference validator resolves the two namespaces independently, so the same\n+  four-digit number may exist once in each namespace.\n+\n+Before a forced upgrade writes anything, the installer inventories the exact\n+former numeric Shiki ADR paths. It may delete a legacy path only when the\n+existing install stamp contains that exact path and its recorded SHA-256 digest\n+matches the file currently on disk. All ownership blockers are reported\n+together and abort the upgrade before its first write. A non-forced install\n+never performs this cleanup.\n+\n+Historical `.shiki/` evidence remains unchanged because it records the namespace\n+that existed when that evidence was produced.\n+\n+## Consequences\n+\n+- Targets may use any ADR number without colliding with Shiki's platform SADRs.\n+- Fresh installs preserve target ADR bytes and install a stable SADR set.\n+- Forced upgrades fail closed when legacy ownership cannot be proven, and a\n+  successful upgrade refreshes the stamp without removed legacy paths.\n+- Current governance, packaging, workflow, prompt, script, and skill references\n+  must move with the platform records.\n+\n+## Alternatives Considered\n+\n+- Collision-only refusal leaves affected targets permanently unupgradable.\n+- Renumbering target ADRs mutates target-owned history.\n+- Per-target Shiki renumbering makes platform citations non-portable.\ndiff --git a/docs/adr/template.md b/docs/adr/template.md\nindex a92a97daddd0b00ab9dd626cc8d86942da2eafed..17dd79175b5fee2105935557f691b40cf9492942\n--- a/docs/adr/template.md\n+++ b/docs/adr/template.md\n@@ -1,14 +1,14 @@\n-# NNNN: <Short Decision Title>\n+# <SADR-NNNN or NNNN>: <Short Decision Title>\n \n ## Status\n \n-Proposed | Accepted | Superseded by ADR-NNNN\n+Proposed | Accepted | Superseded by SADR-NNNN or target ADR NNNN\n \n ## Context\n \n What is the situation, constraint, or force that requires a decision? Reference\n-relevant domain terms (`CONTEXT.md`), prior ADRs, and code reality. State the\n-problem, not the solution.\n+relevant domain terms (`CONTEXT.md`), prior Shiki SADRs or target ADRs, and code\n+reality. State the problem, not the solution.\n \n ## Decision\n \ndiff --git a/docs/agents/domain.md b/docs/agents/domain.md\nindex 258da8ea81e87ac0a6b0b5b5e5c2fde980eaabb7..67aaa6f4b7d0a5aa4d495ce6279073333f7798cb\n--- a/docs/agents/domain.md\n+++ b/docs/agents/domain.md\n@@ -5,7 +5,9 @@\n ## Layout\n \n - `CONTEXT.md` defines Shiki language. Keep it implementation-free.\n-- `docs/adr/` records hard-to-reverse decisions.\n+- `docs/adr/SADR-NNNN-*.md` records Shiki platform decisions.\n+- `docs/adr/NNNN-*.md` records target product decisions without sharing the\n+  SADR namespace.\n - `AGENTS.md` is the runtime-neutral agent constitution.\n - `CLAUDE.md` is the Claude Code wrapper around `AGENTS.md`.\n - `.shiki/` is the Target Repository mirror for Goals, PRDs, plans, Task DAGs, contracts, locks, worktree records, ledger entries, CCA verdicts, repair packets, reports, and handoffs.\ndiff --git a/docs/agents/implementation-policy.md b/docs/agents/implementation-policy.md\nindex c754144a9fb5bf4a38aa82a60e1670c59e626b44..e90d40fe21880b9a09719c5ecfe6bde7284eb3ce\n--- a/docs/agents/implementation-policy.md\n+++ b/docs/agents/implementation-policy.md\n@@ -31,6 +31,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n ## Non-Negotiables\n \n 1. **Planning precedes implementation.** Non-trivial Goals start with `grill-with-docs` and Context & Impact.\ndiff --git a/scripts/shiki_installer.py b/scripts/shiki_installer.py\nindex be064ab7a29ff6836f7f7a1d70500a43e0d11d7b..d3e3247edbf3e4be287a4667176d86f6500f8dc5\n--- a/scripts/shiki_installer.py\n+++ b/scripts/shiki_installer.py\n@@ -118,6 +118,7 @@\n # what lets a target carry it without a governance-schema change.\n INSTALL_STAMP_PATH = \".shiki/install-stamp.json\"\n INSTALL_STAMP_VERSION = 1\n+LEGACY_SHIKI_SADR_MAX = 18\n \n # Under --force the shipped surface splits three ways, so nothing is lost and\n # nothing is silently stale.\n@@ -237,9 +238,98 @@\n         )\n \n \n+def legacy_shiki_adr_paths() -> tuple[str, ...]:\n+    \"\"\"Former numeric paths for Shiki SADR-0001 through SADR-0018.\n+\n+    The authoritative slugs come from the current SADR filenames. SADR-0019 is\n+    the namespace decision itself and never shipped under a numeric legacy path.\n+    \"\"\"\n+    paths: list[str] = []\n+    for sadr in sorted((ROOT / \"docs\" / \"adr\").glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\")):\n+        number = sadr.name.split(\"-\", 2)[1]\n+        if int(number) <= LEGACY_SHIKI_SADR_MAX:\n+            paths.append(f\"docs/adr/{sadr.name.removeprefix('SADR-')}\")\n+    return tuple(paths)\n+\n+\n+def preflight_legacy_sadr_cleanup(target: Path, *, force: bool) -> tuple[str, ...]:\n+    \"\"\"Authorize exact legacy Shiki ADR removals before the first write.\n+\n+    A non-force install never removes existing files. A forced install needs no\n+    stamp when no legacy Shiki path is present. When legacy paths do exist, each\n+    must be named in a readable install stamp and its current bytes must match\n+    the recorded digest.\n+    \"\"\"\n+    if not force:\n+        return ()\n+    present = tuple(\n+        relative\n+        for relative in legacy_shiki_adr_paths()\n+        if (target / relative).exists() or (target / relative).is_symlink()\n+    )\n+    if not present:\n+        return ()\n+\n+    stamp_path = target / INSTALL_STAMP_PATH\n+    try:\n+        raw = stamp_path.read_text(encoding=\"utf-8\")\n+        stamp = json.loads(raw)\n+    except FileNotFoundError:\n+        detail = \"install stamp is absent\"\n+        blockers = [f\"{relative}: {detail}\" for relative in present]\n+    except (OSError, UnicodeDecodeError) as error:\n+        detail = f\"install stamp is unreadable ({error})\"\n+        blockers = [f\"{relative}: {detail}\" for relative in present]\n+    except json.JSONDecodeError as error:\n+        detail = f\"install stamp is malformed ({error})\"\n+        blockers = [f\"{relative}: {detail}\" for relative in present]\n+    else:\n+        if not isinstance(stamp, dict):\n+            blockers = [\n+                f\"{relative}: install stamp is malformed (expected an object)\"\n+                for relative in present\n+            ]\n+        else:\n+            digests = stamp.get(\"digests\")\n+            blockers = []\n+            if not isinstance(digests, dict):\n+                blockers.extend(\n+                    f\"{relative}: install stamp has no digest for this path\"\n+                    for relative in present\n+                )\n+            else:\n+                for relative in present:\n+                    expected = digests.get(relative)\n+                    if not isinstance(expected, str) or not expected:\n+                        blockers.append(\n+                            f\"{relative}: install stamp has no digest for this path\"\n+                        )\n+                        continue\n+                    try:\n+                        actual = _sha256_file(target / relative)\n+                    except OSError as error:\n+                        blockers.append(\n+                            f\"{relative}: legacy path is unreadable ({error})\"\n+                        )\n+                        continue\n+                    if actual != expected:\n+                        blockers.append(\n+                            f\"{relative}: digest mismatch (stamp {expected}, current {actual})\"\n+                        )\n+\n+    if blockers:\n+        joined = \"\\n\".join(f\"- {blocker}\" for blocker in blockers)\n+        raise ShikiError(\n+            \"legacy Shiki ADR cleanup is not authorized:\\n\"\n+            f\"{joined}\\nNo files were written.\"\n+        )\n+    return present\n+\n+\n def install_template(target: Path, *, force: bool, validate: bool) -> list[NewFileNote]:\n     # Preconditions first: never rewrite the tree of a target that cannot be\n     # upgraded cleanly (see refuse_pending_migrations).\n+    legacy_cleanup = preflight_legacy_sadr_cleanup(target, force=force)\n     refuse_pending_migrations(target)\n \n     # Resolve the target's required CODEOWNERS owner once from its\n@@ -250,6 +340,10 @@\n     # verbatim. See ``codeowners_required_owner``.\n     codeowners_owner = codeowners_required_owner(target)\n \n+    for relative in legacy_cleanup:\n+        (target / relative).unlink()\n+        info(f\"removed stamped legacy Shiki ADR: {target / relative}\")\n+\n     notes: list[NewFileNote] = []\n     for relative in TEMPLATE_PATHS:\n         source = ROOT / relative\ndiff --git a/tests/test_adr_references.py b/tests/test_adr_references.py\nindex 51706329c797d5dd9b47e082a333fa83b261d482..91ab76b724cb973435c45d53cab63793942267e9\n--- a/tests/test_adr_references.py\n+++ b/tests/test_adr_references.py\n@@ -1,4 +1,4 @@\n-\"\"\"Every ``ADR ####`` citation must resolve to exactly one decision record.\n+\"\"\"Every target ADR and Shiki SADR citation resolves in its own namespace.\n \n Background. ``scripts/mergegate_check.py`` justifies ``is_bookkeeping_closeout``\n with \"ADR 0017\" nine times and ``scripts/guardian_approval_signal.py`` cites\n@@ -11,11 +11,12 @@\n and the directory drifted with no signal.\n \n This is the same unbound-pair shape behind other silent failures: a reference\n-and its target with nothing asserting they agree. This test binds them. It scans\n-every ``scripts/*.py`` (plus ``docs/**/*.md`` and the root ``*.md`` governance\n-files, which are cheap) for the ``ADR ####`` pattern and asserts each cited\n-number resolves to exactly one ``docs/adr/####-*.md``. The converse is\n-deliberately NOT required: an ADR may exist without being cited.\n+and its target with nothing asserting they agree. This test binds them. Target\n+``ADR NNNN`` citations resolve only to ``docs/adr/NNNN-*.md``; Shiki\n+``SADR-NNNN`` citations resolve only to ``docs/adr/SADR-NNNN-*.md``. The scan\n+covers current governance docs, scripts, workflows, prompts, packaging metadata,\n+and repo-local Codex guidance while deliberately excluding historical\n+``.shiki`` evidence. The converse is not required: a record may be uncited.\n \n The suite is dependency free: standard-library ``unittest`` only.\n \"\"\"\n@@ -31,17 +32,18 @@\n \n REPO_ROOT = Path(__file__).resolve().parents[1]\n \n-# \"ADR 0016\" -- a four-digit citation. The trailing \\b keeps a five-digit token\n-# from being read as a truncated four-digit number.\n-_CITATION = re.compile(r\"\\bADR (\\d{4})\\b\")\n-# \"0016-locks-bound-product-paths.md\" -- the leading four digits name the record.\n-_ADR_FILENAME = re.compile(r\"^(\\d{4})-.*\\.md$\")\n+# ``ADR 0016`` / ``SADR-0016`` -- a namespace plus four-digit citation. The\n+# trailing ``\\b`` keeps a five-digit token from being read as a truncated one.\n+_CITATION = re.compile(r\"\\b(S?ADR)[ -](\\d{4})\\b\")\n+# Target ADRs retain the numeric filename; Shiki platform decisions use SADR.\n+_DECISION_FILENAME = re.compile(r\"^(?:(SADR)-)?(\\d{4})-.*\\.md$\")\n \n \n class Violation(NamedTuple):\n     \"\"\"A citation that does not resolve to exactly one decision record.\"\"\"\n \n     citing_file: str  # repo-relative path of the file that cites the ADR\n+    namespace: str  # \"ADR\" for target decisions; \"SADR\" for Shiki decisions\n     number: str  # the cited four-digit number, e.g. \"0016\"\n     line: int  # 1-based line of the citation\n     reason: str  # \"missing\" (zero matches) or \"ambiguous\" (two or more)\n@@ -49,44 +51,59 @@\n \n     @property\n     def expected_path(self) -> str:\n-        return f\"docs/adr/{self.number}-*.md\"\n+        prefix = \"SADR-\" if self.namespace == \"SADR\" else \"\"\n+        return f\"docs/adr/{prefix}{self.number}-*.md\"\n \n     def describe(self) -> str:\n         \"\"\"Name the citing file, the cited number, and the missing path.\"\"\"\n         if self.reason == \"missing\":\n             return (\n-                f\"{self.citing_file}:{self.line} cites ADR {self.number} \"\n+                f\"{self.citing_file}:{self.line} cites {self.namespace} {self.number} \"\n                 f\"but no document matches {self.expected_path}\"\n             )\n         joined = \", \".join(self.matches)\n         return (\n-            f\"{self.citing_file}:{self.line} cites ADR {self.number} which \"\n+            f\"{self.citing_file}:{self.line} cites {self.namespace} {self.number} which \"\n             f\"resolves to {len(self.matches)} documents (expected exactly one \"\n             f\"at {self.expected_path}): {joined}\"\n         )\n \n \n-def _adr_index(root: Path) -> dict[str, list[Path]]:\n-    \"\"\"Map every four-digit ADR number to the record files that claim it.\"\"\"\n-    index: dict[str, list[Path]] = {}\n+def _adr_index(root: Path) -> dict[tuple[str, str], list[Path]]:\n+    \"\"\"Map each decision namespace and number to the files that claim it.\"\"\"\n+    index: dict[tuple[str, str], list[Path]] = {}\n     adr_dir = root / \"docs\" / \"adr\"\n     if adr_dir.is_dir():\n         for path in sorted(adr_dir.glob(\"*.md\")):\n-            match = _ADR_FILENAME.match(path.name)\n+            match = _DECISION_FILENAME.match(path.name)\n             if match:\n-                index.setdefault(match.group(1), []).append(path)\n+                namespace = \"SADR\" if match.group(1) else \"ADR\"\n+                index.setdefault((namespace, match.group(2)), []).append(path)\n     return index\n \n \n def _citing_files(root: Path, include_docs: bool = True) -> list[Path]:\n-    \"\"\"Files scanned for citations: scripts/*.py, and cheaply the markdown.\"\"\"\n+    \"\"\"Current, shipped text surfaces scanned for decision citations.\"\"\"\n     files = sorted((root / \"scripts\").glob(\"*.py\"))\n+    files += sorted((root / \"scripts\").glob(\"*.sh\"))\n     if include_docs:\n         docs = root / \"docs\"\n         if docs.is_dir():\n             files += sorted(docs.rglob(\"*.md\"))\n         files += sorted(root.glob(\"*.md\"))\n-    return files\n+        pyproject = root / \"pyproject.toml\"\n+        if pyproject.is_file():\n+            files.append(pyproject)\n+        for relative in (\".github\", \".codex\"):\n+            surface = root / relative\n+            if surface.is_dir():\n+                files += sorted(\n+                    path\n+                    for path in surface.rglob(\"*\")\n+                    if path.is_file()\n+                    and path.suffix in {\".md\", \".py\", \".sh\", \".toml\", \".yaml\", \".yml\"}\n+                )\n+    return sorted(set(files))\n \n \n def find_adr_reference_violations(\n@@ -94,10 +111,9 @@\n ) -> list[Violation]:\n     \"\"\"Return every ``ADR ####`` citation under ``root`` that fails to resolve.\n \n-    A citation resolves when exactly one ``docs/adr/####-*.md`` matches its\n-    number. Zero matches is a \"missing\" violation; two or more is \"ambiguous\".\n-    Records that are never cited produce nothing -- the converse is not\n-    required.\n+    A citation resolves when exactly one record in the same namespace matches\n+    its number. Zero matches is a \"missing\" violation; two or more is\n+    \"ambiguous\". Records that are never cited produce nothing.\n     \"\"\"\n     root = Path(root)\n     index = _adr_index(root)\n@@ -110,13 +126,15 @@\n         rel = path.relative_to(root).as_posix()\n         for lineno, line in enumerate(text.splitlines(), start=1):\n             for match in _CITATION.finditer(line):\n-                number = match.group(1)\n-                found = index.get(number, [])\n+                namespace, number = match.groups()\n+                found = index.get((namespace, number), [])\n                 if len(found) == 1:\n                     continue\n                 reason = \"missing\" if not found else \"ambiguous\"\n                 rels = [p.relative_to(root).as_posix() for p in found]\n-                violations.append(Violation(rel, number, lineno, reason, rels))\n+                violations.append(\n+                    Violation(rel, namespace, number, lineno, reason, rels)\n+                )\n     return violations\n \n \n@@ -129,8 +147,8 @@\n         self.assertEqual(\n             violations,\n             [],\n-            \"Every 'ADR ####' citation must resolve to exactly one \"\n-            f\"docs/adr/####-*.md:\\n{detail}\",\n+            \"Every ADR/SADR citation must resolve exactly once in its own \"\n+            f\"docs/adr namespace:\\n{detail}\",\n         )\n \n     def test_scan_actually_reads_scripts(self) -> None:\n@@ -146,8 +164,21 @@\n         # The two records this Goal carried onto the default branch, and which\n         # the scripts cite most, must be present.\n         index = _adr_index(REPO_ROOT)\n-        self.assertIn(\"0016\", index)\n-        self.assertIn(\"0017\", index)\n+        self.assertIn((\"SADR\", \"0016\"), index)\n+        self.assertIn((\"SADR\", \"0017\"), index)\n+\n+    def test_platform_tree_has_only_sadr_0001_through_0019(self) -> None:\n+        adr_dir = REPO_ROOT / \"docs\" / \"adr\"\n+        sadrs = sorted(adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\"))\n+        self.assertEqual(\n+            {path.name.split(\"-\", 2)[1] for path in sadrs},\n+            {f\"{number:04d}\" for number in range(1, 20)},\n+        )\n+        self.assertEqual(\n+            list(adr_dir.glob(\"[0-9][0-9][0-9][0-9]-*.md\")),\n+            [],\n+            \"the Shiki platform tree must not retain legacy numeric SADR paths\",\n+        )\n \n \n class FixtureAdrReferences(unittest.TestCase):\n@@ -180,6 +211,26 @@\n         self.assertIn(\"9991\", message)  # cited number\n         self.assertIn(\"docs/adr/9991-*.md\", message)  # missing path\n \n+    def test_target_adr_does_not_satisfy_missing_platform_sadr(self) -> None:\n+        root = self._make_repo(\n+            adr_names=[\"0042-target-decision.md\"],\n+            script_body='\"\"\"Target ADR 0042 exists, but platform SADR-0042 does not.\"\"\"\\n',\n+        )\n+        violations = find_adr_reference_violations(root, include_docs=False)\n+        self.assertEqual(len(violations), 1)\n+        self.assertEqual(violations[0].namespace, \"SADR\")\n+        self.assertEqual(violations[0].number, \"0042\")\n+        self.assertEqual(violations[0].expected_path, \"docs/adr/SADR-0042-*.md\")\n+\n+    def test_target_adr_and_platform_sadr_may_reuse_a_number(self) -> None:\n+        root = self._make_repo(\n+            adr_names=[\"0042-target-decision.md\", \"SADR-0042-platform-decision.md\"],\n+            script_body='\"\"\"Target ADR 0042 and platform SADR-0042 are distinct.\"\"\"\\n',\n+        )\n+        self.assertEqual(\n+            find_adr_reference_violations(root, include_docs=False), []\n+        )\n+\n     def test_uncited_adr_is_allowed(self) -> None:\n         # An ADR exists but nothing cites it -> not a violation. The converse\n         # (every ADR must be cited) is explicitly NOT required.\ndiff --git a/tests/test_sadr_install_migration.py b/tests/test_sadr_install_migration.py\nnew file mode 100644\nindex 0000000000000000000000000000000000000000..5a442fa5e4968ade178dcdedc3c63b54dba580a2\n--- /dev/null\n+++ b/tests/test_sadr_install_migration.py\n@@ -0,0 +1,224 @@\n+\"\"\"SADR installation and collision-safe legacy upgrade behavior.\"\"\"\n+\n+from __future__ import annotations\n+\n+import contextlib\n+import hashlib\n+import io\n+import json\n+import shutil\n+import tempfile\n+import unittest\n+from pathlib import Path\n+\n+import shiki_test_support  # noqa: F401  (path bootstrap)\n+\n+import shiki_installer\n+from tests.test_adr_references import find_adr_reference_violations\n+\n+\n+PRODUCT_NUMBERS = tuple(range(13, 19))\n+\n+\n+def _quiet_install(target: Path, *, force: bool, validate: bool) -> None:\n+    with contextlib.redirect_stdout(io.StringIO()):\n+        shiki_installer.install_template(target, force=force, validate=validate)\n+\n+\n+def _sha256(path: Path) -> str:\n+    return hashlib.sha256(path.read_bytes()).hexdigest()\n+\n+\n+def _legacy_sadr_paths() -> tuple[str, ...]:\n+    return tuple(\n+        f\"docs/adr/{path.name.removeprefix('SADR-')}\"\n+        for path in sorted((shiki_installer.ROOT / \"docs\" / \"adr\").glob(\"SADR-*.md\"))\n+        if path.name != \"SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\"\n+    )\n+\n+\n+def _create_legacy_upgrade_fixture(\n+    target: Path,\n+) -> tuple[dict[str, bytes], tuple[str, ...]]:\n+    adr_dir = target / \"docs\" / \"adr\"\n+    adr_dir.mkdir(parents=True)\n+    product_bytes = {\n+        f\"{number:04d}-cubeshare-product-decision.md\": (\n+            f\"# CubeShare ADR {number:04d}\\n\\nDo not rewrite {number}.\\n\"\n+        ).encode()\n+        for number in PRODUCT_NUMBERS\n+    }\n+    for name, content in product_bytes.items():\n+        (adr_dir / name).write_bytes(content)\n+\n+    legacy_paths = _legacy_sadr_paths()\n+    for relative in legacy_paths:\n+        (target / relative).write_bytes(f\"legacy Shiki bytes for {relative}\\n\".encode())\n+\n+    stamp_path = target / shiki_installer.INSTALL_STAMP_PATH\n+    stamp_path.parent.mkdir(parents=True, exist_ok=True)\n+    stamp = {\n+        \"version\": shiki_installer.INSTALL_STAMP_VERSION,\n+        \"platform_commit\": None,\n+        \"installed_at\": \"2000-01-01T00:00:00+00:00\",\n+        \"digests\": {\n+            relative: _sha256(target / relative) for relative in legacy_paths\n+        },\n+    }\n+    stamp_path.write_text(\n+        json.dumps(stamp, indent=2, sort_keys=True) + \"\\n\", encoding=\"utf-8\"\n+    )\n+    return product_bytes, legacy_paths\n+\n+\n+def _tree_snapshot(root: Path) -> dict[str, tuple[str, bytes | str]]:\n+    \"\"\"Recursive byte/shape snapshot used to prove preflight is write-free.\"\"\"\n+    snapshot: dict[str, tuple[str, bytes | str]] = {}\n+    for path in sorted(root.rglob(\"*\")):\n+        relative = path.relative_to(root).as_posix()\n+        if path.is_symlink():\n+            snapshot[relative] = (\"symlink\", path.readlink().as_posix())\n+        elif path.is_dir():\n+            snapshot[relative] = (\"directory\", \"\")\n+        else:\n+            snapshot[relative] = (\"file\", path.read_bytes())\n+    return snapshot\n+\n+\n+class FreshSadrInstallTests(unittest.TestCase):\n+    def setUp(self) -> None:\n+        self.tmp = Path(tempfile.mkdtemp(prefix=\"shiki-sadr-fresh-\"))\n+        self.addCleanup(lambda: shutil.rmtree(self.tmp, ignore_errors=True))\n+        self.target = self.tmp / \"target\"\n+        self.adr_dir = self.target / \"docs\" / \"adr\"\n+        self.adr_dir.mkdir(parents=True)\n+        self.product_bytes = {\n+            f\"{number:04d}-product-decision.md\": (\n+                f\"# Product ADR {number:04d}\\n\\nTarget-owned bytes {number}.\\n\"\n+            ).encode()\n+            for number in PRODUCT_NUMBERS\n+        }\n+        for name, content in self.product_bytes.items():\n+            (self.adr_dir / name).write_bytes(content)\n+\n+    def test_fresh_install_preserves_product_adrs_and_installs_all_sadrs(self) -> None:\n+        _quiet_install(self.target, force=False, validate=True)\n+\n+        for name, expected in self.product_bytes.items():\n+            self.assertEqual((self.adr_dir / name).read_bytes(), expected)\n+\n+        sadr_numbers = {\n+            path.name.split(\"-\", 2)[1]\n+            for path in self.adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\")\n+        }\n+        self.assertEqual(sadr_numbers, {f\"{number:04d}\" for number in range(1, 20)})\n+\n+        numeric_records = {\n+            path.name\n+            for path in self.adr_dir.glob(\"[0-9][0-9][0-9][0-9]-*.md\")\n+        }\n+        self.assertEqual(numeric_records, set(self.product_bytes))\n+        self.assertEqual(find_adr_reference_violations(self.target), [])\n+\n+\n+class LegacySadrUpgradeTests(unittest.TestCase):\n+    def setUp(self) -> None:\n+        self.tmp = Path(tempfile.mkdtemp(prefix=\"shiki-sadr-upgrade-\"))\n+        self.addCleanup(lambda: shutil.rmtree(self.tmp, ignore_errors=True))\n+        self.target = self.tmp / \"target\"\n+        self.adr_dir = self.target / \"docs\" / \"adr\"\n+        self.product_bytes, self.legacy_paths = _create_legacy_upgrade_fixture(\n+            self.target\n+        )\n+\n+    def test_force_upgrade_deletes_only_stamped_legacy_sadrs(self) -> None:\n+        _quiet_install(self.target, force=True, validate=True)\n+\n+        for relative in self.legacy_paths:\n+            self.assertFalse((self.target / relative).exists(), relative)\n+        for name, expected in self.product_bytes.items():\n+            self.assertEqual((self.adr_dir / name).read_bytes(), expected)\n+\n+        sadrs = list(self.adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\"))\n+        self.assertEqual(len(sadrs), 19)\n+        refreshed = json.loads(\n+            (self.target / shiki_installer.INSTALL_STAMP_PATH).read_text(encoding=\"utf-8\")\n+        )\n+        for relative in self.legacy_paths:\n+            self.assertNotIn(relative, refreshed[\"digests\"])\n+        for path in sadrs:\n+            relative = path.relative_to(self.target).as_posix()\n+            self.assertEqual(refreshed[\"digests\"][relative], _sha256(path))\n+\n+    def test_non_force_install_never_deletes_legacy_files(self) -> None:\n+        before = {\n+            relative: (self.target / relative).read_bytes()\n+            for relative in self.legacy_paths\n+        }\n+        _quiet_install(self.target, force=False, validate=False)\n+        for relative, expected in before.items():\n+            self.assertEqual((self.target / relative).read_bytes(), expected)\n+\n+\n+class LegacyCleanupPreflightTests(unittest.TestCase):\n+    def setUp(self) -> None:\n+        self.tmp = Path(tempfile.mkdtemp(prefix=\"shiki-sadr-preflight-\"))\n+        self.addCleanup(lambda: shutil.rmtree(self.tmp, ignore_errors=True))\n+\n+    def test_all_ownership_blockers_abort_before_any_write(self) -> None:\n+        cases = (\n+            (\"absent\", \"install stamp is absent\"),\n+            (\"unreadable\", \"install stamp is unreadable\"),\n+            (\"malformed\", \"install stamp is malformed\"),\n+            (\"missing-digest\", \"install stamp has no digest\"),\n+            (\"digest-mismatch\", \"digest mismatch\"),\n+        )\n+        for name, expected_reason in cases:\n+            with self.subTest(name=name):\n+                target = self.tmp / name\n+                _, legacy_paths = _create_legacy_upgrade_fixture(target)\n+                stamp_path = target / shiki_installer.INSTALL_STAMP_PATH\n+\n+                if name == \"absent\":\n+                    stamp_path.unlink()\n+                elif name == \"unreadable\":\n+                    stamp_path.unlink()\n+                    stamp_path.mkdir()\n+                elif name == \"malformed\":\n+                    stamp_path.write_text(\"{not-json\\n\", encoding=\"utf-8\")\n+                else:\n+                    stamp = json.loads(stamp_path.read_text(encoding=\"utf-8\"))\n+                    if name == \"missing-digest\":\n+                        stamp[\"digests\"] = {}\n+                    else:\n+                        stamp[\"digests\"] = {\n+                            relative: \"deadbeef\" for relative in legacy_paths\n+                        }\n+                    stamp_path.write_text(\n+                        json.dumps(stamp, indent=2, sort_keys=True) + \"\\n\",\n+                        encoding=\"utf-8\",\n+                    )\n+\n+                before = _tree_snapshot(target)\n+                with self.assertRaises(shiki_installer.ShikiError) as raised:\n+                    _quiet_install(target, force=True, validate=False)\n+                after = _tree_snapshot(target)\n+\n+                self.assertEqual(before, after, f\"{name} preflight changed the tree\")\n+                message = str(raised.exception)\n+                self.assertIn(expected_reason, message)\n+                self.assertIn(\"No files were written\", message)\n+                for relative in legacy_paths:\n+                    self.assertIn(relative, message)\n+\n+    def test_absent_stamp_is_allowed_when_no_legacy_path_exists(self) -> None:\n+        target = self.tmp / \"no-legacy\"\n+        target.mkdir()\n+        _quiet_install(target, force=True, validate=False)\n+        self.assertTrue(\n+            (target / \"docs\" / \"adr\" / \"SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\").is_file()\n+        )\n+\n+\n+if __name__ == \"__main__\":\n+    unittest.main()\n\nexec\n/bin/zsh -lc \"python3 -m unittest discover -s tests -p 'test_sadr_install_migration.py' -v\" in /private/tmp/shiki-adr-collision.pa0KwY/.worktrees/shiki-t-20260807t073307221254z-2d22d654-introduc\ndiff --git a/.codex/skills/shiki/SKILL.md b/.codex/skills/shiki/SKILL.md\nindex c8e2e85d9d75d49cc4f2d6286ee22659c4e53e75..93cded623b2c15d9f65c76491a1c2d813ac9e6c0\n--- a/.codex/skills/shiki/SKILL.md\n+++ b/.codex/skills/shiki/SKILL.md\n@@ -18,6 +18,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n Codex CLI does not register this skill as a custom `/shiki` slash command. When\n the user types `/shiki` in Codex CLI and gets \"Unrecognized command\", explain\n that this is expected: invoke Shiki in Codex with natural language such as\n@@ -69,7 +72,7 @@\n \n ## Responsibilities\n \n-- Claude Code implements and repairs by default (`shiki runner claude`, ADR 0008); Codex implements and repairs tasks explicitly assigned to `codex`.\n+- Claude Code implements and repairs by default (`shiki runner claude`, SADR-0008); Codex implements and repairs tasks explicitly assigned to `codex`.\n - Claude Code Action can act as GitHub-side CCA or reviewer.\n - CCA judges completion.\n - MergeGate authorizes state transitions and merge readiness.\ndiff --git a/.github/prompts/cca-completion-check.md b/.github/prompts/cca-completion-check.md\nindex ed203ad5f302ecc81372d8ab27d2b1232ca97497..fd89950528333ef6242e6d5443904f5392ab7e56\n--- a/.github/prompts/cca-completion-check.md\n+++ b/.github/prompts/cca-completion-check.md\n@@ -19,6 +19,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n ## Required Reading\n \n Read only the files needed to judge the PR contract. Prefer this order:\ndiff --git a/AGENTS.md b/AGENTS.md\nindex 7f041100d49c2a7525f51cb0f5d696a4a6d1c78c..2b700e989ff295987aa8a4b0c5fcb1acd80020da\n--- a/AGENTS.md\n+++ b/AGENTS.md\n@@ -44,6 +44,10 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use\n+`SADR-NNNN-*.md`; target product decisions retain `NNNN-*.md` and the `ADR`\n+namespace.\n+\n Use the canonical source-of-truth order above. Runtime-specific reading orders\n are operational guidance only and must not override or reorder it.\n \ndiff --git a/CLAUDE.md b/CLAUDE.md\nindex 98a932e7841a632866fcd53b0bad0e3af3711d3d..611d491912d6a72a0327a1731b8f33a41acb4baa\n--- a/CLAUDE.md\n+++ b/CLAUDE.md\n@@ -14,6 +14,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n ## Claude Role\n \n Default to planner, default implementer, reviewer, coordinator, documentation editor, CCA-style completion judge, and final-judgment assistant.\ndiff --git a/SYSTEM_PROMPT.md b/SYSTEM_PROMPT.md\nindex aaaea45e3a896c3a44ba89eec8bb84325ecf6c62..2e7c093ebe9e8bb34e0923756f191b4f7fa581f5\n--- a/SYSTEM_PROMPT.md\n+++ b/SYSTEM_PROMPT.md\n@@ -30,6 +30,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n Use the canonical source-of-truth order above. Runtime-specific reading orders\n are operational guidance only and must not override or reorder it. The current\n conversation is non-durable operator input.\ndiff --git a/docs/adr/0001-keep-shiki-as-platform-name.md b/docs/adr/SADR-0001-keep-shiki-as-platform-name.md\nindex 20a9637c3b9d8c97de7ba8db7cfae4d82d9a7dba..8b685439959aada1b221c112f0ac3c51ca8a1695\n--- a/docs/adr/0001-keep-shiki-as-platform-name.md\n+++ b/docs/adr/SADR-0001-keep-shiki-as-platform-name.md\n@@ -1,3 +1,3 @@\n-# Keep Shiki As The Platform Name\n+# SADR-0001: Keep Shiki As The Platform Name\n \n Shiki remains the product name for the GitHub-native agentic engineering control plane. We considered more descriptive names such as MergePilot, MergeGate, and Kiroku, but kept Shiki because it carries the existing goal-seeking framework identity while remaining independent of Hermes, Codex, Claude Code, or any single runtime.\ndiff --git a/docs/adr/0002-use-agents-as-shared-constitution.md b/docs/adr/SADR-0002-use-agents-as-shared-constitution.md\nindex 73de30ecb078120eac540d9bc51855e3cd48d0e3..c5486182bf06fca36ea508496b013c67302fd989\n--- a/docs/adr/0002-use-agents-as-shared-constitution.md\n+++ b/docs/adr/SADR-0002-use-agents-as-shared-constitution.md\n@@ -1,3 +1,3 @@\n-# Use AGENTS As The Shared Agent Constitution\n+# SADR-0002: Use AGENTS As The Shared Agent Constitution\n \n Shiki uses `AGENTS.md` as the runtime-neutral constitution and `CLAUDE.md` as a thin Claude Code wrapper. This keeps Codex, Claude Code, Hermes Runner, and future Agent Runtimes aligned on the same GitHub-first MergeGate rules while still allowing runtime-specific instructions where needed.\ndiff --git a/docs/adr/0003-require-skill-gate-for-mergegate-readiness.md b/docs/adr/SADR-0003-require-skill-gate-for-mergegate-readiness.md\nindex 424be0c9af4c8f0136f1fde68a468c47ff84e764..7fc337987c7df9792f75f181dfcbed75fe5ffff2\n--- a/docs/adr/0003-require-skill-gate-for-mergegate-readiness.md\n+++ b/docs/adr/SADR-0003-require-skill-gate-for-mergegate-readiness.md\n@@ -1,3 +1,3 @@\n-# Require Skill Gate For MergeGate Readiness\n+# SADR-0003: Require Skill Gate For MergeGate Readiness\n \n Shiki requires relevant engineering skills to be invoked before execution when their triggers apply. This makes skills part of MergeGate readiness rather than optional prompt style, because Shiki depends on repeatable planning, issue decomposition, TDD, diagnosis, triage, and architecture review before autonomous or parallel agent work can be trusted.\ndiff --git a/docs/adr/0004-use-github-first-with-shiki-mirror.md b/docs/adr/SADR-0004-use-github-first-with-shiki-mirror.md\nindex d0a3a9174dc646a945a9d325954ffaf00c352040..a79c54ae9ee8eed2a0524192b864da4e22a92d3f\n--- a/docs/adr/0004-use-github-first-with-shiki-mirror.md\n+++ b/docs/adr/SADR-0004-use-github-first-with-shiki-mirror.md\n@@ -1,4 +1,4 @@\n-# ADR 0004: Use GitHub First With A Repository-Local Shiki Mirror\n+# SADR-0004: Use GitHub First With A Repository-Local Shiki Mirror\n \n ## Status\n \ndiff --git a/docs/adr/0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md b/docs/adr/SADR-0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\nindex b339baf49b89d9290e79c3d51b36e7b2bde3bd95..c30a0d8b1fde0224d0ac7820e7870dccf721580d\n--- a/docs/adr/0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\n+++ b/docs/adr/SADR-0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\n@@ -1,4 +1,4 @@\n-# ADR 0005: Use Codex As Front And Claude Code Action As The GitHub Runtime\n+# SADR-0005: Use Codex As Front And Claude Code Action As The GitHub Runtime\n \n ## Status\n \ndiff --git a/docs/adr/0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md b/docs/adr/SADR-0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\nindex 4bcae9d1e52f0a37b8e9fbafa5cd14b6ee1993bd..7a38538a6ae9929567f0ade67e7ea6a60d0b3f7e\n--- a/docs/adr/0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\n+++ b/docs/adr/SADR-0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\n@@ -1,4 +1,4 @@\n-# ADR 0006: Separate Codex Implementation, CCA Completion, And MergeGate Authorization\n+# SADR-0006: Separate Codex Implementation, CCA Completion, And MergeGate Authorization\n \n ## Status\n \ndiff --git a/docs/adr/0007-packaging-and-release.md b/docs/adr/SADR-0007-packaging-and-release.md\nindex 79b84ddb97167372b44641e5b7860e400ce80822..023cd57f8011aa6e6d2ebf612b7c35a7a82c00d4\n--- a/docs/adr/0007-packaging-and-release.md\n+++ b/docs/adr/SADR-0007-packaging-and-release.md\n@@ -1,4 +1,4 @@\n-# ADR 0007: Packaging And Release For Shiki\n+# SADR-0007: Packaging And Release For Shiki\n \n ## Status\n \ndiff --git a/docs/adr/0008-use-claude-code-as-default-implementer.md b/docs/adr/SADR-0008-use-claude-code-as-default-implementer.md\nindex dedf4510b7c7799521422f622029563d373db031..a158a8d444d93764ec515a28133f7dde4af23579\n--- a/docs/adr/0008-use-claude-code-as-default-implementer.md\n+++ b/docs/adr/SADR-0008-use-claude-code-as-default-implementer.md\n@@ -1,4 +1,4 @@\n-# ADR 0008: Use Claude Code As The Default Implementer Runtime\n+# SADR-0008: Use Claude Code As The Default Implementer Runtime\n \n ## Status\n \ndiff --git a/docs/adr/0009-requirements-definition-and-spec-freeze.md b/docs/adr/SADR-0009-requirements-definition-and-spec-freeze.md\nindex 29b3f7871bef9203a8b4a831a65c39eb2f506e42..52e7dd69ad0aa4efe0b68c62678abbf7df02aaec\n--- a/docs/adr/0009-requirements-definition-and-spec-freeze.md\n+++ b/docs/adr/SADR-0009-requirements-definition-and-spec-freeze.md\n@@ -1,4 +1,4 @@\n-# ADR 0009: Merge Grill And Planning Into Requirements Definition With Spec Freeze\n+# SADR-0009: Merge Grill And Planning Into Requirements Definition With Spec Freeze\n \n ## Status\n \ndiff --git a/docs/adr/0010-external-ai-guardian-review.md b/docs/adr/SADR-0010-external-ai-guardian-review.md\nindex e6108bbe8bb4e73e0ff4684c8f78001a80438c4e..e690d4eddd270e683d709c9b679c5859e71eeaaa\n--- a/docs/adr/0010-external-ai-guardian-review.md\n+++ b/docs/adr/SADR-0010-external-ai-guardian-review.md\n@@ -1,4 +1,4 @@\n-# ADR 0010: External AI Guardian Review As A First-Class Approval Authority\n+# SADR-0010: External AI Guardian Review As A First-Class Approval Authority\n \n ## Status\n \ndiff --git a/docs/adr/0011-autonomous-quality-gate-evidence.md b/docs/adr/SADR-0011-autonomous-quality-gate-evidence.md\nindex bca0246c5d46e44afbde15486a75eec425fc02f5..9c93eac16d8c3a6d4b11bd7224e3955102cd7db4\n--- a/docs/adr/0011-autonomous-quality-gate-evidence.md\n+++ b/docs/adr/SADR-0011-autonomous-quality-gate-evidence.md\n@@ -1,4 +1,4 @@\n-# ADR 0011: Autonomous Quality-Gate Evidence \u2014 Deterministic Facts And An Independent Verifier\n+# SADR-0011: Autonomous Quality-Gate Evidence \u2014 Deterministic Facts And An Independent Verifier\n \n ## Status\n \ndiff --git a/docs/adr/0012-autonomous-loop-closeout-to-main.md b/docs/adr/SADR-0012-autonomous-loop-closeout-to-main.md\nindex 637fad6dde4989c9c2bf0f81f676d8d29991123c..ec7d85f81030a676bcbba28b43eb365422a36eac\n--- a/docs/adr/0012-autonomous-loop-closeout-to-main.md\n+++ b/docs/adr/SADR-0012-autonomous-loop-closeout-to-main.md\n@@ -1,4 +1,4 @@\n-# 0012 \u2014 Autonomous loop pushes goal completion to main via a closeout PR\n+# SADR-0012 \u2014 Autonomous loop pushes goal completion to main via a closeout PR\n \n - Status: accepted\n - Date: 2026-06-18\ndiff --git a/docs/adr/0013-bootstrap-grants-actions-approve-pull-requests.md b/docs/adr/SADR-0013-bootstrap-grants-actions-approve-pull-requests.md\nindex b6c69d64f1c5ced346b88d6859586715b3b5bd0e..a793e7277d67ab8ea56286fec289ca689825a17d\n--- a/docs/adr/0013-bootstrap-grants-actions-approve-pull-requests.md\n+++ b/docs/adr/SADR-0013-bootstrap-grants-actions-approve-pull-requests.md\n@@ -1,4 +1,4 @@\n-# ADR 0013: Bootstrap Grants GitHub Actions Permission To Create And Approve Pull Requests\n+# SADR-0013: Bootstrap Grants GitHub Actions Permission To Create And Approve Pull Requests\n \n ## Status\n \ndiff --git a/docs/adr/0014-codex-app-external-ai-guardian-ui-adapter.md b/docs/adr/SADR-0014-codex-app-external-ai-guardian-ui-adapter.md\nindex 238db06279334caa341c117362df9271382a7fcd..65f12e9939321e1b3e389a34c822fc1549e5155d\n--- a/docs/adr/0014-codex-app-external-ai-guardian-ui-adapter.md\n+++ b/docs/adr/SADR-0014-codex-app-external-ai-guardian-ui-adapter.md\n@@ -1,4 +1,4 @@\n-# ADR 0014: Use Codex App As The External AI Guardian UI Adapter\n+# SADR-0014: Use Codex App As The External AI Guardian UI Adapter\n \n ## Status\n \ndiff --git a/docs/adr/0015-contract-approval-before-dispatch.md b/docs/adr/SADR-0015-contract-approval-before-dispatch.md\nindex fad6c656741a20a7684ba16080a7db159f63a52f..7bdd760bb3e82eee4e1410f45c783b03b638d456\n--- a/docs/adr/0015-contract-approval-before-dispatch.md\n+++ b/docs/adr/SADR-0015-contract-approval-before-dispatch.md\n@@ -1,4 +1,4 @@\n-# ADR 0015: Approve The Task Contract Before Dispatch, Not The Diff After It\n+# SADR-0015: Approve The Task Contract Before Dispatch, Not The Diff After It\n \n ## Status\n \ndiff --git a/docs/adr/0016-locks-bound-product-paths.md b/docs/adr/SADR-0016-locks-bound-product-paths.md\nindex b5e850f3f1f7f4ef6e7f9d1763a29a51bd300c45..332cc17313498404f41a145291e38447b5de0dc4\n--- a/docs/adr/0016-locks-bound-product-paths.md\n+++ b/docs/adr/SADR-0016-locks-bound-product-paths.md\n@@ -1,4 +1,4 @@\n-# ADR 0016: Locks bound product paths; the Shiki mirror is bound by record identity\n+# SADR-0016: Locks bound product paths; the Shiki mirror is bound by record identity\n \n ## Status\n \ndiff --git a/docs/adr/0017-bookkeeping-closeouts-do-not-inherit-risk.md b/docs/adr/SADR-0017-bookkeeping-closeouts-do-not-inherit-risk.md\nindex 554e3a0d526a72ca067108c5464cc68c3edcf174..83115f69f1b1b48d544c9b0157f377b91cb53abf\n--- a/docs/adr/0017-bookkeeping-closeouts-do-not-inherit-risk.md\n+++ b/docs/adr/SADR-0017-bookkeeping-closeouts-do-not-inherit-risk.md\n@@ -1,4 +1,4 @@\n-# ADR 0017: A Bookkeeping Closeout Does Not Inherit Its Task's Risk\n+# SADR-0017: A Bookkeeping Closeout Does Not Inherit Its Task's Risk\n \n ## Status\n \ndiff --git a/docs/adr/0018-carried-guardian-approval-across-a-proven-base-sync.md b/docs/adr/SADR-0018-carried-guardian-approval-across-a-proven-base-sync.md\nindex 22358562da390ed8b45cd600da08a43d378b4680..38da0fd95102c3496c3219ce2319fce696a2532a\n--- a/docs/adr/0018-carried-guardian-approval-across-a-proven-base-sync.md\n+++ b/docs/adr/SADR-0018-carried-guardian-approval-across-a-proven-base-sync.md\n@@ -1,4 +1,4 @@\n-# ADR 0018: Carried Guardian Approval Across A Proven Base Sync\n+# SADR-0018: Carried Guardian Approval Across A Proven Base Sync\n \n ## Status\n \ndiff --git a/docs/adr/README.md b/docs/adr/README.md\nindex c70e057a4520c36e042ab68c53cd4a21ef709b71..5da43856836e2ce6a150f8127766e2f88355ab4b\n--- a/docs/adr/README.md\n+++ b/docs/adr/README.md\n@@ -1,40 +1,41 @@\n-# Architecture Decision Records\n+# Decision Record Namespaces\n \n-This directory holds Shiki's Architecture Decision Records (ADRs): durable\n-records of hard-to-reverse platform decisions. ADRs are part of the Shiki source\n-of truth alongside GitHub state, the `.shiki/` mirror, and `CONTEXT.md`.\n+This directory holds two independent decision namespaces:\n \n-## How ADRs Work\n+- Shiki Architecture Decision Records (SADRs), named\n+  `SADR-NNNN-<short-slug>.md`, record hard-to-reverse Shiki platform decisions.\n+- Target Architecture Decision Records (ADRs), named\n+  `NNNN-<short-slug>.md`, belong to the target product.\n \n-- New ADRs are added as `docs/adr/NNNN-<short-slug>.md`, where `NNNN` is the next\n-  zero-padded sequence number.\n-- The **canonical list of ADRs is the set of files matching `docs/adr/*.md`** in\n-  this directory. Browse `docs/adr/*.md` for the authoritative, current set.\n-- Use [`template.md`](template.md) as the starting point for a new ADR.\n-- Record an ADR when a decision is hard to reverse, surprising without context,\n-  and a real tradeoff. Do not write an ADR for routine or easily reversible\n-  choices.\n+The same four-digit number may appear once in each namespace. A platform\n+`SADR-NNNN` citation resolves only to `SADR-NNNN-*.md`; a target `ADR NNNN`\n+citation resolves only to `NNNN-*.md`.\n \n-Using the `docs/adr/*.md` glob as the source of truth means other PRs can add an\n-ADR without editing this index file, avoiding merge contention on a\n-hand-maintained enumeration.\n+## How Decision Records Work\n \n-## Current ADRs (snapshot)\n-\n-The list below is a convenience snapshot and may lag behind the directory. When\n-in doubt, list `docs/adr/*.md`.\n+- New Shiki platform decisions use the next zero-padded SADR number.\n+- New target product decisions use the target's own ADR sequence.\n+- The canonical list is the files in `docs/adr/`; no hand-maintained index is\n+  authoritative.\n+- Use [`template.md`](template.md) as the starting point and choose the filename\n+  namespace that owns the decision.\n+- Record a decision when it is hard to reverse, surprising without context, and\n+  a real tradeoff. Do not write a decision record for routine or easily\n+  reversible choices.\n \n-- [0001 \u2014 Keep Shiki As The Platform Name](0001-keep-shiki-as-platform-name.md)\n-- [0002 \u2014 Use AGENTS As The Shared Agent Constitution](0002-use-agents-as-shared-constitution.md)\n-- [0003 \u2014 Require Skill Gate For MergeGate Readiness](0003-require-skill-gate-for-mergegate-readiness.md)\n-- [0004 \u2014 Use GitHub First With A Repository-Local Shiki Mirror](0004-use-github-first-with-shiki-mirror.md)\n-- [0005 \u2014 Use Codex As Front And Claude Code Action As The GitHub Runtime](0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md)\n-- [0006 \u2014 Separate Codex Implementation, CCA Completion, And MergeGate Authorization](0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md)\n+Using the directory as the source of truth lets independent changes add records\n+without contending on an enumerated index.\n \n-## Adding A New ADR\n+## Adding A Shiki SADR\n \n-1. Copy `template.md` to `docs/adr/NNNN-<short-slug>.md`.\n+1. Copy `template.md` to `docs/adr/SADR-NNNN-<short-slug>.md`.\n 2. Fill in the title, status, context, decision, and consequences.\n-3. Set the status to `Proposed`, then `Accepted` once the decision is approved.\n-4. Open a PR following `CONTRIBUTING.md`. You do not need to edit this index for\n-   the ADR to be discoverable via `docs/adr/*.md`.\n+3. Set the status to `Proposed`, then `Accepted` once the proper authority has\n+   approved the decision.\n+4. Open a PR following `CONTRIBUTING.md`.\n+\n+## Adding A Target ADR\n+\n+1. Copy `template.md` to `docs/adr/NNNN-<short-slug>.md` using the target's own\n+   sequence.\n+2. Cite it as `ADR NNNN`; do not prefix its filename with `SADR-`.\ndiff --git a/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md b/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\nnew file mode 100644\nindex 0000000000000000000000000000000000000000..7455ce1f3bed02ddddb851ac21a1d1a648c9aeba\n--- /dev/null\n+++ b/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\n@@ -0,0 +1,55 @@\n+# SADR-0019: Separate Shiki Platform SADRs From Target ADRs\n+\n+## Status\n+\n+Accepted\n+\n+## Context\n+\n+Shiki installs its platform decision records into `docs/adr/` in every target\n+repository. Shiki and its targets previously used the same numeric\n+`NNNN-<slug>.md` namespace. A target that already owned ADRs 0013 through 0018\n+therefore acquired two records for each number during installation, making an\n+`ADR NNNN` citation ambiguous. Renumbering target decisions would rewrite target\n+history, while renumbering Shiki decisions per target would destroy their stable\n+platform identity.\n+\n+Upgrades also need to remove the former Shiki-owned numeric paths without ever\n+mistaking a target-owned ADR for platform content. The install stamp already\n+records exact shipped paths and content digests, so it can prove ownership only\n+when both still match.\n+\n+## Decision\n+\n+Shiki platform decisions use the `SADR` namespace:\n+\n+- records are named `docs/adr/SADR-NNNN-<slug>.md`;\n+- live platform citations use `SADR-NNNN`;\n+- target product decisions retain `docs/adr/NNNN-<slug>.md` and `ADR NNNN`;\n+- the reference validator resolves the two namespaces independently, so the same\n+  four-digit number may exist once in each namespace.\n+\n+Before a forced upgrade writes anything, the installer inventories the exact\n+former numeric Shiki ADR paths. It may delete a legacy path only when the\n+existing install stamp contains that exact path and its recorded SHA-256 digest\n+matches the file currently on disk. All ownership blockers are reported\n+together and abort the upgrade before its first write. A non-forced install\n+never performs this cleanup.\n+\n+Historical `.shiki/` evidence remains unchanged because it records the namespace\n+that existed when that evidence was produced.\n+\n+## Consequences\n+\n+- Targets may use any ADR number without colliding with Shiki's platform SADRs.\n+- Fresh installs preserve target ADR bytes and install a stable SADR set.\n+- Forced upgrades fail closed when legacy ownership cannot be proven, and a\n+  successful upgrade refreshes the stamp without removed legacy paths.\n+- Current governance, packaging, workflow, prompt, script, and skill references\n+  must move with the platform records.\n+\n+## Alternatives Considered\n+\n+- Collision-only refusal leaves affected targets permanently unupgradable.\n+- Renumbering target ADRs mutates target-owned history.\n+- Per-target Shiki renumbering makes platform citations non-portable.\ndiff --git a/docs/adr/template.md b/docs/adr/template.md\nindex a92a97daddd0b00ab9dd626cc8d86942da2eafed..17dd79175b5fee2105935557f691b40cf9492942\n--- a/docs/adr/template.md\n+++ b/docs/adr/template.md\n@@ -1,14 +1,14 @@\n-# NNNN: <Short Decision Title>\n+# <SADR-NNNN or NNNN>: <Short Decision Title>\n \n ## Status\n \n-Proposed | Accepted | Superseded by ADR-NNNN\n+Proposed | Accepted | Superseded by SADR-NNNN or target ADR NNNN\n \n ## Context\n \n What is the situation, constraint, or force that requires a decision? Reference\n-relevant domain terms (`CONTEXT.md`), prior ADRs, and code reality. State the\n-problem, not the solution.\n+relevant domain terms (`CONTEXT.md`), prior Shiki SADRs or target ADRs, and code\n+reality. State the problem, not the solution.\n \n ## Decision\n \ndiff --git a/docs/agents/domain.md b/docs/agents/domain.md\nindex 258da8ea81e87ac0a6b0b5b5e5c2fde980eaabb7..67aaa6f4b7d0a5aa4d495ce6279073333f7798cb\n--- a/docs/agents/domain.md\n+++ b/docs/agents/domain.md\n@@ -5,7 +5,9 @@\n ## Layout\n \n - `CONTEXT.md` defines Shiki language. Keep it implementation-free.\n-- `docs/adr/` records hard-to-reverse decisions.\n+- `docs/adr/SADR-NNNN-*.md` records Shiki platform decisions.\n+- `docs/adr/NNNN-*.md` records target product decisions without sharing the\n+  SADR namespace.\n - `AGENTS.md` is the runtime-neutral agent constitution.\n - `CLAUDE.md` is the Claude Code wrapper around `AGENTS.md`.\n - `.shiki/` is the Target Repository mirror for Goals, PRDs, plans, Task DAGs, contracts, locks, worktree records, ledger entries, CCA verdicts, repair packets, reports, and handoffs.\ndiff --git a/docs/agents/implementation-policy.md b/docs/agents/implementation-policy.md\nindex c754144a9fb5bf4a38aa82a60e1670c59e626b44..e90d40fe21880b9a09719c5ecfe6bde7284eb3ce\n--- a/docs/agents/implementation-policy.md\n+++ b/docs/agents/implementation-policy.md\n@@ -31,6 +31,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n ## Non-Negotiables\n \n 1. **Planning precedes implementation.** Non-trivial Goals start with `grill-with-docs` and Context & Impact.\ndiff --git a/scripts/shiki_installer.py b/scripts/shiki_installer.py\nindex be064ab7a29ff6836f7f7a1d70500a43e0d11d7b..d3e3247edbf3e4be287a4667176d86f6500f8dc5\n--- a/scripts/shiki_installer.py\n+++ b/scripts/shiki_installer.py\n@@ -118,6 +118,7 @@\n # what lets a target carry it without a governance-schema change.\n INSTALL_STAMP_PATH = \".shiki/install-stamp.json\"\n INSTALL_STAMP_VERSION = 1\n+LEGACY_SHIKI_SADR_MAX = 18\n \n # Under --force the shipped surface splits three ways, so nothing is lost and\n # nothing is silently stale.\n@@ -237,9 +238,98 @@\n         )\n \n \n+def legacy_shiki_adr_paths() -> tuple[str, ...]:\n+    \"\"\"Former numeric paths for Shiki SADR-0001 through SADR-0018.\n+\n+    The authoritative slugs come from the current SADR filenames. SADR-0019 is\n+    the namespace decision itself and never shipped under a numeric legacy path.\n+    \"\"\"\n+    paths: list[str] = []\n+    for sadr in sorted((ROOT / \"docs\" / \"adr\").glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\")):\n+        number = sadr.name.split(\"-\", 2)[1]\n+        if int(number) <= LEGACY_SHIKI_SADR_MAX:\n+            paths.append(f\"docs/adr/{sadr.name.removeprefix('SADR-')}\")\n+    return tuple(paths)\n+\n+\n+def preflight_legacy_sadr_cleanup(target: Path, *, force: bool) -> tuple[str, ...]:\n+    \"\"\"Authorize exact legacy Shiki ADR removals before the first write.\n+\n+    A non-force install never removes existing files. A forced install needs no\n+    stamp when no legacy Shiki path is present. When legacy paths do exist, each\n+    must be named in a readable install stamp and its current bytes must match\n+    the recorded digest.\n+    \"\"\"\n+    if not force:\n+        return ()\n+    present = tuple(\n+        relative\n+        for relative in legacy_shiki_adr_paths()\n+        if (target / relative).exists() or (target / relative).is_symlink()\n+    )\n+    if not present:\n+        return ()\n+\n+    stamp_path = target / INSTALL_STAMP_PATH\n+    try:\n+        raw = stamp_path.read_text(encoding=\"utf-8\")\n+        stamp = json.loads(raw)\n+    except FileNotFoundError:\n+        detail = \"install stamp is absent\"\n+        blockers = [f\"{relative}: {detail}\" for relative in present]\n+    except (OSError, UnicodeDecodeError) as error:\n+        detail = f\"install stamp is unreadable ({error})\"\n+        blockers = [f\"{relative}: {detail}\" for relative in present]\n+    except json.JSONDecodeError as error:\n+        detail = f\"install stamp is malformed ({error})\"\n+        blockers = [f\"{relative}: {detail}\" for relative in present]\n+    else:\n+        if not isinstance(stamp, dict):\n+            blockers = [\n+                f\"{relative}: install stamp is malformed (expected an object)\"\n+                for relative in present\n+            ]\n+        else:\n+            digests = stamp.get(\"digests\")\n+            blockers = []\n+            if not isinstance(digests, dict):\n+                blockers.extend(\n+                    f\"{relative}: install stamp has no digest for this path\"\n+                    for relative in present\n+                )\n+            else:\n+                for relative in present:\n+                    expected = digests.get(relative)\n+                    if not isinstance(expected, str) or not expected:\n+                        blockers.append(\n+                            f\"{relative}: install stamp has no digest for this path\"\n+                        )\n+                        continue\n+                    try:\n+                        actual = _sha256_file(target / relative)\n+                    except OSError as error:\n+                        blockers.append(\n+                            f\"{relative}: legacy path is unreadable ({error})\"\n+                        )\n+                        continue\n+                    if actual != expected:\n+                        blockers.append(\n+                            f\"{relative}: digest mismatch (stamp {expected}, current {actual})\"\n+                        )\n+\n+    if blockers:\n+        joined = \"\\n\".join(f\"- {blocker}\" for blocker in blockers)\n+        raise ShikiError(\n+            \"legacy Shiki ADR cleanup is not authorized:\\n\"\n+            f\"{joined}\\nNo files were written.\"\n+        )\n+    return present\n+\n+\n def install_template(target: Path, *, force: bool, validate: bool) -> list[NewFileNote]:\n     # Preconditions first: never rewrite the tree of a target that cannot be\n     # upgraded cleanly (see refuse_pending_migrations).\n+    legacy_cleanup = preflight_legacy_sadr_cleanup(target, force=force)\n     refuse_pending_migrations(target)\n \n     # Resolve the target's required CODEOWNERS owner once from its\n@@ -250,6 +340,10 @@\n     # verbatim. See ``codeowners_required_owner``.\n     codeowners_owner = codeowners_required_owner(target)\n \n+    for relative in legacy_cleanup:\n+        (target / relative).unlink()\n+        info(f\"removed stamped legacy Shiki ADR: {target / relative}\")\n+\n     notes: list[NewFileNote] = []\n     for relative in TEMPLATE_PATHS:\n         source = ROOT / relative\ndiff --git a/tests/test_adr_references.py b/tests/test_adr_references.py\nindex 51706329c797d5dd9b47e082a333fa83b261d482..91ab76b724cb973435c45d53cab63793942267e9\n--- a/tests/test_adr_references.py\n+++ b/tests/test_adr_references.py\n@@ -1,4 +1,4 @@\n-\"\"\"Every ``ADR ####`` citation must resolve to exactly one decision record.\n+\"\"\"Every target ADR and Shiki SADR citation resolves in its own namespace.\n \n Background. ``scripts/mergegate_check.py`` justifies ``is_bookkeeping_closeout``\n with \"ADR 0017\" nine times and ``scripts/guardian_approval_signal.py`` cites\n@@ -11,11 +11,12 @@\n and the directory drifted with no signal.\n \n This is the same unbound-pair shape behind other silent failures: a reference\n-and its target with nothing asserting they agree. This test binds them. It scans\n-every ``scripts/*.py`` (plus ``docs/**/*.md`` and the root ``*.md`` governance\n-files, which are cheap) for the ``ADR ####`` pattern and asserts each cited\n-number resolves to exactly one ``docs/adr/####-*.md``. The converse is\n-deliberately NOT required: an ADR may exist without being cited.\n+and its target with nothing asserting they agree. This test binds them. Target\n+``ADR NNNN`` citations resolve only to ``docs/adr/NNNN-*.md``; Shiki\n+``SADR-NNNN`` citations resolve only to ``docs/adr/SADR-NNNN-*.md``. The scan\n+covers current governance docs, scripts, workflows, prompts, packaging metadata,\n+and repo-local Codex guidance while deliberately excluding historical\n+``.shiki`` evidence. The converse is not required: a record may be uncited.\n \n The suite is dependency free: standard-library ``unittest`` only.\n \"\"\"\n@@ -31,17 +32,18 @@\n \n REPO_ROOT = Path(__file__).resolve().parents[1]\n \n-# \"ADR 0016\" -- a four-digit citation. The trailing \\b keeps a five-digit token\n-# from being read as a truncated four-digit number.\n-_CITATION = re.compile(r\"\\bADR (\\d{4})\\b\")\n-# \"0016-locks-bound-product-paths.md\" -- the leading four digits name the record.\n-_ADR_FILENAME = re.compile(r\"^(\\d{4})-.*\\.md$\")\n+# ``ADR 0016`` / ``SADR-0016`` -- a namespace plus four-digit citation. The\n+# trailing ``\\b`` keeps a five-digit token from being read as a truncated one.\n+_CITATION = re.compile(r\"\\b(S?ADR)[ -](\\d{4})\\b\")\n+# Target ADRs retain the numeric filename; Shiki platform decisions use SADR.\n+_DECISION_FILENAME = re.compile(r\"^(?:(SADR)-)?(\\d{4})-.*\\.md$\")\n \n \n class Violation(NamedTuple):\n     \"\"\"A citation that does not resolve to exactly one decision record.\"\"\"\n \n     citing_file: str  # repo-relative path of the file that cites the ADR\n+    namespace: str  # \"ADR\" for target decisions; \"SADR\" for Shiki decisions\n     number: str  # the cited four-digit number, e.g. \"0016\"\n     line: int  # 1-based line of the citation\n     reason: str  # \"missing\" (zero matches) or \"ambiguous\" (two or more)\n@@ -49,44 +51,59 @@\n \n     @property\n     def expected_path(self) -> str:\n-        return f\"docs/adr/{self.number}-*.md\"\n+        prefix = \"SADR-\" if self.namespace == \"SADR\" else \"\"\n+        return f\"docs/adr/{prefix}{self.number}-*.md\"\n \n     def describe(self) -> str:\n         \"\"\"Name the citing file, the cited number, and the missing path.\"\"\"\n         if self.reason == \"missing\":\n             return (\n-                f\"{self.citing_file}:{self.line} cites ADR {self.number} \"\n+                f\"{self.citing_file}:{self.line} cites {self.namespace} {self.number} \"\n                 f\"but no document matches {self.expected_path}\"\n             )\n         joined = \", \".join(self.matches)\n         return (\n-            f\"{self.citing_file}:{self.line} cites ADR {self.number} which \"\n+            f\"{self.citing_file}:{self.line} cites {self.namespace} {self.number} which \"\n             f\"resolves to {len(self.matches)} documents (expected exactly one \"\n             f\"at {self.expected_path}): {joined}\"\n         )\n \n \n-def _adr_index(root: Path) -> dict[str, list[Path]]:\n-    \"\"\"Map every four-digit ADR number to the record files that claim it.\"\"\"\n-    index: dict[str, list[Path]] = {}\n+def _adr_index(root: Path) -> dict[tuple[str, str], list[Path]]:\n+    \"\"\"Map each decision namespace and number to the files that claim it.\"\"\"\n+    index: dict[tuple[str, str], list[Path]] = {}\n     adr_dir = root / \"docs\" / \"adr\"\n     if adr_dir.is_dir():\n         for path in sorted(adr_dir.glob(\"*.md\")):\n-            match = _ADR_FILENAME.match(path.name)\n+            match = _DECISION_FILENAME.match(path.name)\n             if match:\n-                index.setdefault(match.group(1), []).append(path)\n+                namespace = \"SADR\" if match.group(1) else \"ADR\"\n+                index.setdefault((namespace, match.group(2)), []).append(path)\n     return index\n \n \n def _citing_files(root: Path, include_docs: bool = True) -> list[Path]:\n-    \"\"\"Files scanned for citations: scripts/*.py, and cheaply the markdown.\"\"\"\n+    \"\"\"Current, shipped text surfaces scanned for decision citations.\"\"\"\n     files = sorted((root / \"scripts\").glob(\"*.py\"))\n+    files += sorted((root / \"scripts\").glob(\"*.sh\"))\n     if include_docs:\n         docs = root / \"docs\"\n         if docs.is_dir():\n             files += sorted(docs.rglob(\"*.md\"))\n         files += sorted(root.glob(\"*.md\"))\n-    return files\n+        pyproject = root / \"pyproject.toml\"\n+        if pyproject.is_file():\n+            files.append(pyproject)\n+        for relative in (\".github\", \".codex\"):\n+            surface = root / relative\n+            if surface.is_dir():\n+                files += sorted(\n+                    path\n+                    for path in surface.rglob(\"*\")\n+                    if path.is_file()\n+                    and path.suffix in {\".md\", \".py\", \".sh\", \".toml\", \".yaml\", \".yml\"}\n+                )\n+    return sorted(set(files))\n \n \n def find_adr_reference_violations(\n@@ -94,10 +111,9 @@\n ) -> list[Violation]:\n     \"\"\"Return every ``ADR ####`` citation under ``root`` that fails to resolve.\n \n-    A citation resolves when exactly one ``docs/adr/####-*.md`` matches its\n-    number. Zero matches is a \"missing\" violation; two or more is \"ambiguous\".\n-    Records that are never cited produce nothing -- the converse is not\n-    required.\n+    A citation resolves when exactly one record in the same namespace matches\n+    its number. Zero matches is a \"missing\" violation; two or more is\n+    \"ambiguous\". Records that are never cited produce nothing.\n     \"\"\"\n     root = Path(root)\n     index = _adr_index(root)\n@@ -110,13 +126,15 @@\n         rel = path.relative_to(root).as_posix()\n         for lineno, line in enumerate(text.splitlines(), start=1):\n             for match in _CITATION.finditer(line):\n-                number = match.group(1)\n-                found = index.get(number, [])\n+                namespace, number = match.groups()\n+                found = index.get((namespace, number), [])\n                 if len(found) == 1:\n                     continue\n                 reason = \"missing\" if not found else \"ambiguous\"\n                 rels = [p.relative_to(root).as_posix() for p in found]\n-                violations.append(Violation(rel, number, lineno, reason, rels))\n+                violations.append(\n+                    Violation(rel, namespace, number, lineno, reason, rels)\n+                )\n     return violations\n \n \n@@ -129,8 +147,8 @@\n         self.assertEqual(\n             violations,\n             [],\n-            \"Every 'ADR ####' citation must resolve to exactly one \"\n-            f\"docs/adr/####-*.md:\\n{detail}\",\n+            \"Every ADR/SADR citation must resolve exactly once in its own \"\n+            f\"docs/adr namespace:\\n{detail}\",\n         )\n \n     def test_scan_actually_reads_scripts(self) -> None:\n@@ -146,8 +164,21 @@\n         # The two records this Goal carried onto the default branch, and which\n         # the scripts cite most, must be present.\n         index = _adr_index(REPO_ROOT)\n-        self.assertIn(\"0016\", index)\n-        self.assertIn(\"0017\", index)\n+        self.assertIn((\"SADR\", \"0016\"), index)\n+        self.assertIn((\"SADR\", \"0017\"), index)\n+\n+    def test_platform_tree_has_only_sadr_0001_through_0019(self) -> None:\n+        adr_dir = REPO_ROOT / \"docs\" / \"adr\"\n+        sadrs = sorted(adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\"))\n+        self.assertEqual(\n+            {path.name.split(\"-\", 2)[1] for path in sadrs},\n+            {f\"{number:04d}\" for number in range(1, 20)},\n+        )\n+        self.assertEqual(\n+            list(adr_dir.glob(\"[0-9][0-9][0-9][0-9]-*.md\")),\n+            [],\n+            \"the Shiki platform tree must not retain legacy numeric SADR paths\",\n+        )\n \n \n class FixtureAdrReferences(unittest.TestCase):\n@@ -180,6 +211,26 @@\n         self.assertIn(\"9991\", message)  # cited number\n         self.assertIn(\"docs/adr/9991-*.md\", message)  # missing path\n \n+    def test_target_adr_does_not_satisfy_missing_platform_sadr(self) -> None:\n+        root = self._make_repo(\n+            adr_names=[\"0042-target-decision.md\"],\n+            script_body='\"\"\"Target ADR 0042 exists, but platform SADR-0042 does not.\"\"\"\\n',\n+        )\n+        violations = find_adr_reference_violations(root, include_docs=False)\n+        self.assertEqual(len(violations), 1)\n+        self.assertEqual(violations[0].namespace, \"SADR\")\n+        self.assertEqual(violations[0].number, \"0042\")\n+        self.assertEqual(violations[0].expected_path, \"docs/adr/SADR-0042-*.md\")\n+\n+    def test_target_adr_and_platform_sadr_may_reuse_a_number(self) -> None:\n+        root = self._make_repo(\n+            adr_names=[\"0042-target-decision.md\", \"SADR-0042-platform-decision.md\"],\n+            script_body='\"\"\"Target ADR 0042 and platform SADR-0042 are distinct.\"\"\"\\n',\n+        )\n+        self.assertEqual(\n+            find_adr_reference_violations(root, include_docs=False), []\n+        )\n+\n     def test_uncited_adr_is_allowed(self) -> None:\n         # An ADR exists but nothing cites it -> not a violation. The converse\n         # (every ADR must be cited) is explicitly NOT required.\ndiff --git a/tests/test_sadr_install_migration.py b/tests/test_sadr_install_migration.py\nnew file mode 100644\nindex 0000000000000000000000000000000000000000..5a442fa5e4968ade178dcdedc3c63b54dba580a2\n--- /dev/null\n+++ b/tests/test_sadr_install_migration.py\n@@ -0,0 +1,224 @@\n+\"\"\"SADR installation and collision-safe legacy upgrade behavior.\"\"\"\n+\n+from __future__ import annotations\n+\n+import contextlib\n+import hashlib\n+import io\n+import json\n+import shutil\n+import tempfile\n+import unittest\n+from pathlib import Path\n+\n+import shiki_test_support  # noqa: F401  (path bootstrap)\n+\n+import shiki_installer\n+from tests.test_adr_references import find_adr_reference_violations\n+\n+\n+PRODUCT_NUMBERS = tuple(range(13, 19))\n+\n+\n+def _quiet_install(target: Path, *, force: bool, validate: bool) -> None:\n+    with contextlib.redirect_stdout(io.StringIO()):\n+        shiki_installer.install_template(target, force=force, validate=validate)\n+\n+\n+def _sha256(path: Path) -> str:\n+    return hashlib.sha256(path.read_bytes()).hexdigest()\n+\n+\n+def _legacy_sadr_paths() -> tuple[str, ...]:\n+    return tuple(\n+        f\"docs/adr/{path.name.removeprefix('SADR-')}\"\n+        for path in sorted((shiki_installer.ROOT / \"docs\" / \"adr\").glob(\"SADR-*.md\"))\n+        if path.name != \"SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\"\n+    )\n+\n+\n+def _create_legacy_upgrade_fixture(\n+    target: Path,\n+) -> tuple[dict[str, bytes], tuple[str, ...]]:\n+    adr_dir = target / \"docs\" / \"adr\"\n+    adr_dir.mkdir(parents=True)\n+    product_bytes = {\n+        f\"{number:04d}-cubeshare-product-decision.md\": (\n+            f\"# CubeShare ADR {number:04d}\\n\\nDo not rewrite {number}.\\n\"\n+        ).encode()\n+        for number in PRODUCT_NUMBERS\n+    }\n+    for name, content in product_bytes.items():\n+        (adr_dir / name).write_bytes(content)\n+\n+    legacy_paths = _legacy_sadr_paths()\n+    for relative in legacy_paths:\n+        (target / relative).write_bytes(f\"legacy Shiki bytes for {relative}\\n\".encode())\n+\n+    stamp_path = target / shiki_installer.INSTALL_STAMP_PATH\n+    stamp_path.parent.mkdir(parents=True, exist_ok=True)\n+    stamp = {\n+        \"version\": shiki_installer.INSTALL_STAMP_VERSION,\n+        \"platform_commit\": None,\n+        \"installed_at\": \"2000-01-01T00:00:00+00:00\",\n+        \"digests\": {\n+            relative: _sha256(target / relative) for relative in legacy_paths\n+        },\n+    }\n+    stamp_path.write_text(\n+        json.dumps(stamp, indent=2, sort_keys=True) + \"\\n\", encoding=\"utf-8\"\n+    )\n+    return product_bytes, legacy_paths\n+\n+\n+def _tree_snapshot(root: Path) -> dict[str, tuple[str, bytes | str]]:\n+    \"\"\"Recursive byte/shape snapshot used to prove preflight is write-free.\"\"\"\n+    snapshot: dict[str, tuple[str, bytes | str]] = {}\n+    for path in sorted(root.rglob(\"*\")):\n+        relative = path.relative_to(root).as_posix()\n+        if path.is_symlink():\n+            snapshot[relative] = (\"symlink\", path.readlink().as_posix())\n+        elif path.is_dir():\n+            snapshot[relative] = (\"directory\", \"\")\n+        else:\n+            snapshot[relative] = (\"file\", path.read_bytes())\n+    return snapshot\n+\n+\n+class FreshSadrInstallTests(unittest.TestCase):\n+    def setUp(self) -> None:\n+        self.tmp = Path(tempfile.mkdtemp(prefix=\"shiki-sadr-fresh-\"))\n+        self.addCleanup(lambda: shutil.rmtree(self.tmp, ignore_errors=True))\n+        self.target = self.tmp / \"target\"\n+        self.adr_dir = self.target / \"docs\" / \"adr\"\n+        self.adr_dir.mkdir(parents=True)\n+        self.product_bytes = {\n+            f\"{number:04d}-product-decision.md\": (\n+                f\"# Product ADR {number:04d}\\n\\nTarget-owned bytes {number}.\\n\"\n+            ).encode()\n+            for number in PRODUCT_NUMBERS\n+        }\n+        for name, content in self.product_bytes.items():\n+            (self.adr_dir / name).write_bytes(content)\n+\n+    def test_fresh_install_preserves_product_adrs_and_installs_all_sadrs(self) -> None:\n+        _quiet_install(self.target, force=False, validate=True)\n+\n+        for name, expected in self.product_bytes.items():\n+            self.assertEqual((self.adr_dir / name).read_bytes(), expected)\n+\n+        sadr_numbers = {\n+            path.name.split(\"-\", 2)[1]\n+            for path in self.adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\")\n+        }\n+        self.assertEqual(sadr_numbers, {f\"{number:04d}\" for number in range(1, 20)})\n+\n+        numeric_records = {\n+            path.name\n+            for path in self.adr_dir.glob(\"[0-9][0-9][0-9][0-9]-*.md\")\n+        }\n+        self.assertEqual(numeric_records, set(self.product_bytes))\n+        self.assertEqual(find_adr_reference_violations(self.target), [])\n+\n+\n+class LegacySadrUpgradeTests(unittest.TestCase):\n+    def setUp(self) -> None:\n+        self.tmp = Path(tempfile.mkdtemp(prefix=\"shiki-sadr-upgrade-\"))\n+        self.addCleanup(lambda: shutil.rmtree(self.tmp, ignore_errors=True))\n+        self.target = self.tmp / \"target\"\n+        self.adr_dir = self.target / \"docs\" / \"adr\"\n+        self.product_bytes, self.legacy_paths = _create_legacy_upgrade_fixture(\n+            self.target\n+        )\n+\n+    def test_force_upgrade_deletes_only_stamped_legacy_sadrs(self) -> None:\n+        _quiet_install(self.target, force=True, validate=True)\n+\n+        for relative in self.legacy_paths:\n+            self.assertFalse((self.target / relative).exists(), relative)\n+        for name, expected in self.product_bytes.items():\n+            self.assertEqual((self.adr_dir / name).read_bytes(), expected)\n+\n+        sadrs = list(self.adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\"))\n+        self.assertEqual(len(sadrs), 19)\n+        refreshed = json.loads(\n+            (self.target / shiki_installer.INSTALL_STAMP_PATH).read_text(encoding=\"utf-8\")\n+        )\n+        for relative in self.legacy_paths:\n+            self.assertNotIn(relative, refreshed[\"digests\"])\n+        for path in sadrs:\n+            relative = path.relative_to(self.target).as_posix()\n+            self.assertEqual(refreshed[\"digests\"][relative], _sha256(path))\n+\n+    def test_non_force_install_never_deletes_legacy_files(self) -> None:\n+        before = {\n+            relative: (self.target / relative).read_bytes()\n+            for relative in self.legacy_paths\n+        }\n+        _quiet_install(self.target, force=False, validate=False)\n+        for relative, expected in before.items():\n+            self.assertEqual((self.target / relative).read_bytes(), expected)\n+\n+\n+class LegacyCleanupPreflightTests(unittest.TestCase):\n+    def setUp(self) -> None:\n+        self.tmp = Path(tempfile.mkdtemp(prefix=\"shiki-sadr-preflight-\"))\n+        self.addCleanup(lambda: shutil.rmtree(self.tmp, ignore_errors=True))\n+\n+    def test_all_ownership_blockers_abort_before_any_write(self) -> None:\n+        cases = (\n+            (\"absent\", \"install stamp is absent\"),\n+            (\"unreadable\", \"install stamp is unreadable\"),\n+            (\"malformed\", \"install stamp is malformed\"),\n+            (\"missing-digest\", \"install stamp has no digest\"),\n+            (\"digest-mismatch\", \"digest mismatch\"),\n+        )\n+        for name, expected_reason in cases:\n+            with self.subTest(name=name):\n+                target = self.tmp / name\n+                _, legacy_paths = _create_legacy_upgrade_fixture(target)\n+                stamp_path = target / shiki_installer.INSTALL_STAMP_PATH\n+\n+                if name == \"absent\":\n+                    stamp_path.unlink()\n+                elif name == \"unreadable\":\n+                    stamp_path.unlink()\n+                    stamp_path.mkdir()\n+                elif name == \"malformed\":\n+                    stamp_path.write_text(\"{not-json\\n\", encoding=\"utf-8\")\n+                else:\n+                    stamp = json.loads(stamp_path.read_text(encoding=\"utf-8\"))\n+                    if name == \"missing-digest\":\n+                        stamp[\"digests\"] = {}\n+                    else:\n+                        stamp[\"digests\"] = {\n+                            relative: \"deadbeef\" for relative in legacy_paths\n+                        }\n+                    stamp_path.write_text(\n+                        json.dumps(stamp, indent=2, sort_keys=True) + \"\\n\",\n+                        encoding=\"utf-8\",\n+                    )\n+\n+                before = _tree_snapshot(target)\n+                with self.assertRaises(shiki_installer.ShikiError) as raised:\n+                    _quiet_install(target, force=True, validate=False)\n+                after = _tree_snapshot(target)\n+\n+                self.assertEqual(before, after, f\"{name} preflight changed the tree\")\n+                message = str(raised.exception)\n+                self.assertIn(expected_reason, message)\n+                self.assertIn(\"No files were written\", message)\n+                for relative in legacy_paths:\n+                    self.assertIn(relative, message)\n+\n+    def test_absent_stamp_is_allowed_when_no_legacy_path_exists(self) -> None:\n+        target = self.tmp / \"no-legacy\"\n+        target.mkdir()\n+        _quiet_install(target, force=True, validate=False)\n+        self.assertTrue(\n+            (target / \"docs\" / \"adr\" / \"SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\").is_file()\n+        )\n+\n+\n+if __name__ == \"__main__\":\n+    unittest.main()\n\n succeeded in 1558ms:\nok\ntest_absent_stamp_is_allowed_when_no_legacy_path_exists (test_sadr_install_migration.LegacyCleanupPreflightTests.test_absent_stamp_is_allowed_when_no_legacy_path_exists) ... ok\ntest_all_ownership_blockers_abort_before_any_write (test_sadr_install_migration.LegacyCleanupPreflightTests.test_all_ownership_blockers_abort_before_any_write) ... ok\ntest_force_upgrade_deletes_only_stamped_legacy_sadrs (test_sadr_install_migration.LegacySadrUpgradeTests.test_force_upgrade_deletes_only_stamped_legacy_sadrs) ... ok\ntest_non_force_install_never_deletes_legacy_files (test_sadr_install_migration.LegacySadrUpgradeTests.test_non_force_install_never_deletes_legacy_files) ... ok\n\n----------------------------------------------------------------------\nRan 5 tests in 1.540s\n\nOK\n\ncodex\nThe installer migration slice is now green: the forced-upgrade fixture removes only exact stamp+digest-matched legacy paths, preserves CubeShare-style product ADR bytes, installs and stamps all 19 SADRs, and the five ownership-evidence failure modes leave a recursive byte/shape snapshot unchanged. Non-force and no-legacy/absent-stamp behavior are also pinned.\n\nI\u2019m moving into contract cleanup now: verifying no live platform citation or numeric Shiki path remains, while retaining explicit target-ADR examples and frozen `.shiki` history.\nexec\n/bin/zsh -lc \"find docs/adr -maxdepth 1 -type f -name '[0-9][0-9][0-9][0-9]-*.md' -print\" in /private/tmp/shiki-adr-collision.pa0KwY/.worktrees/shiki-t-20260807t073307221254z-2d22d654-introduc\n succeeded in 0ms:\nexec\n/bin/zsh -lc 'git status --short' in /private/tmp/shiki-adr-collision.pa0KwY/.worktrees/shiki-t-20260807t073307221254z-2d22d654-introduc\n succeeded in 0ms:\n M .codex/skills/shiki/SKILL.md\n M .github/prompts/cca-completion-check.md\n M .github/workflows/release.yml\n M .github/workflows/shiki-cca-completion.yml\n M .github/workflows/shiki-mergegate.yml\n M .shiki/tasks/T-20260807T073307221254Z-2d22d654.json\n M AGENTS.md\n M CLAUDE.md\n M CONTEXT.md\n M CONTRIBUTING.md\n M SYSTEM_PROMPT.md\n D docs/adr/0001-keep-shiki-as-platform-name.md\n D docs/adr/0002-use-agents-as-shared-constitution.md\n D docs/adr/0003-require-skill-gate-for-mergegate-readiness.md\n D docs/adr/0004-use-github-first-with-shiki-mirror.md\n D docs/adr/0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\n D docs/adr/0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\n D docs/adr/0007-packaging-and-release.md\n D docs/adr/0008-use-claude-code-as-default-implementer.md\n D docs/adr/0009-requirements-definition-and-spec-freeze.md\n D docs/adr/0010-external-ai-guardian-review.md\n D docs/adr/0011-autonomous-quality-gate-evidence.md\n D docs/adr/0012-autonomous-loop-closeout-to-main.md\n D docs/adr/0013-bootstrap-grants-actions-approve-pull-requests.md\n D docs/adr/0014-codex-app-external-ai-guardian-ui-adapter.md\n D docs/adr/0015-contract-approval-before-dispatch.md\n D docs/adr/0016-locks-bound-product-paths.md\n D docs/adr/0017-bookkeeping-closeouts-do-not-inherit-risk.md\n D docs/adr/0018-carried-guardian-approval-across-a-proven-base-sync.md\n M docs/adr/README.md\n M docs/adr/template.md\n M docs/agents/autonomous-loop-e2e-acceptance.md\n M docs/agents/codex-handoff.md\n M docs/agents/domain.md\n M docs/agents/guardian-policy.md\n M docs/agents/implementation-policy.md\n M docs/agents/runtime-auth-model.md\n M docs/agents/runtime-registry.md\n M docs/agents/shiki-cli-architecture.md\n M docs/agents/triage-labels.md\n M docs/proposals/0001-memory-loop-spec-freeze-review.md\n M docs/proposals/0002-autonomous-loop-completion.md\n M docs/releasing.md\n M pyproject.toml\n M scripts/guardian_approval_signal.py\n M scripts/mergegate_check.py\n M scripts/shiki_bootstrap.py\n M scripts/shiki_cli.py\n M scripts/shiki_contract_approval.py\n M scripts/shiki_github.py\n M scripts/shiki_guardian.py\n M scripts/shiki_guardian_review.py\n M scripts/shiki_installer.py\n M scripts/shiki_loop.py\n M scripts/shiki_migrations.py\n M scripts/shiki_runtime_adapters.py\n M scripts/shiki_runtime_registry.py\n M scripts/shiki_tasks.py\n M scripts/test_shiki_adversarial_state.sh\n M scripts/test_shiki_code_review_gate.sh\n M scripts/test_shiki_control_plane.sh\n M scripts/test_shiki_goal_loop.sh\n M scripts/test_shiki_governance_evidence.sh\n M scripts/test_shiki_guardian_policy.sh\n M scripts/test_shiki_loop_e2e.sh\n M scripts/test_shiki_loop_lock_guard.sh\n M scripts/test_shiki_run_orchestrator.sh\n M scripts/test_shiki_runner_claude.sh\n M scripts/test_shiki_runtime_registry.sh\n M scripts/test_shiki_start.sh\n M scripts/validate_shiki.py\n M tests/test_adr_references.py\n M tests/test_bookkeeping_closeout.py\n M tests/test_branch_behind_sync.py\n M tests/test_cca_evidence_manifest_taskless.py\n M tests/test_closeout.py\n M tests/test_completion_lock_scope.py\n M tests/test_contract_amendment.py\n M tests/test_contract_approval.py\n M tests/test_contract_immutability.py\n M tests/test_contract_mode.py\n M tests/test_contract_open.py\n M tests/test_contract_plan_registration.py\n M tests/test_dispatch_lock_precondition.py\n M tests/test_guardian_review.py\n M tests/test_guardian_signal.py\n M tests/test_guardian_signal_closeout.py\n M tests/test_loop_branch_plumbing.py\n M tests/test_loop_code_review.py\n M tests/test_loop_e2e_contract.py\n M tests/test_loop_engine.py\n M tests/test_loop_lock_validator_hint.py\n M tests/test_loop_tdd_evidence.py\n M tests/test_mirror_scope.py\n M tests/test_paired_invariants.py\n M tests/test_policy_job_merged_prs.py\n M tests/test_repair_delivery.py\n M tests/test_runner_adapters.py\n M tests/test_workflow_invocation.py\n M tests/test_workflow_invocation_carry.py\n?? .shiki/locks/T-20260807T073307221254Z-2d22d654.json\n?? docs/adr/SADR-0001-keep-shiki-as-platform-name.md\n?? docs/adr/SADR-0002-use-agents-as-shared-constitution.md\n?? docs/adr/SADR-0003-require-skill-gate-for-mergegate-readiness.md\n?? docs/adr/SADR-0004-use-github-first-with-shiki-mirror.md\n?? docs/adr/SADR-0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\n?? docs/adr/SADR-0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\n?? docs/adr/SADR-0007-packaging-and-release.md\n?? docs/adr/SADR-0008-use-claude-code-as-default-implementer.md\n?? docs/adr/SADR-0009-requirements-definition-and-spec-freeze.md\n?? docs/adr/SADR-0010-external-ai-guardian-review.md\n?? docs/adr/SADR-0011-autonomous-quality-gate-evidence.md\n?? docs/adr/SADR-0012-autonomous-loop-closeout-to-main.md\n?? docs/adr/SADR-0013-bootstrap-grants-actions-approve-pull-requests.md\n?? docs/adr/SADR-0014-codex-app-external-ai-guardian-ui-adapter.md\n?? docs/adr/SADR-0015-contract-approval-before-dispatch.md\n?? docs/adr/SADR-0016-locks-bound-product-paths.md\n?? docs/adr/SADR-0017-bookkeeping-closeouts-do-not-inherit-risk.md\n?? docs/adr/SADR-0018-carried-guardian-approval-across-a-proven-base-sync.md\n?? docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\n?? tests/test_sadr_install_migration.py\n\nexec\n/bin/zsh -lc \"rg -n --glob '\"'!.shiki/**'\"' --glob '\"'!skills/**'\"' '\\\\b(SADR|ADR)[ -][0-9]{4}\\\\b|docs/adr/(SADR-)?[0-9]{4}-' AGENTS.md CLAUDE.md CONTEXT.md CONTRIBUTING.md SYSTEM_PROMPT.md pyproject.toml docs scripts tests .github .codex\" in /private/tmp/shiki-adr-collision.pa0KwY/.worktrees/shiki-t-20260807t073307221254z-2d22d654-introduc\n succeeded in 0ms:\npyproject.toml:12:# docs/adr/SADR-0007-packaging-and-release.md.\nSYSTEM_PROMPT.md:19:Shiki is a GitHub-first execution governance layer for AI coding agents. It takes a Goal, clarifies success conditions, grills the plan against repository docs and domain language, converts settled context into a PRD, decomposes the PRD into vertical-slice issues, assigns implementation to Claude Code by default (Codex Front when explicitly assigned, SADR-0008), forces TDD and scoped branches, has a GitHub-side Completion Check Agent judge whether the work is actually complete, returns incomplete work to the assigned implementer as bounded repair, and permits merge only when MergeGate conditions and durable evidence are satisfied.\nCONTRIBUTING.md:24:   (SADR-0009). Post-freeze scope changes require an operator-approved,\nCONTEXT.md:104:Guardian approval of a high/critical-risk Goal's task contracts before any implementation starts, granted on the Contract PR and binding for every implementation that stays inside its Scope Envelope (SADR-0015). Guardian authorizes the contract, not the diff; whether the authorized work was implemented correctly stays with CCA and review.\nCONTEXT.md:120:A first-class Guardian approval authority (SADR-0010) by which an external AI reviewer authorizes autonomous merge of a high/critical-risk PR through a head-SHA-bound `external_ai_guardian_review` artifact, recorded with the AI reviewer's own model identity (reviewer_type=external_ai_model).\nCONTEXT.md:124:The deterministic review INPUT the External AI Guardian UI Adapter builds for a high/critical-risk PR (task contract, PR diff/checks, implementer report provenance, relevant docs, PR-type review focus areas, and known missing evidence). It is review context only, never approval evidence, and must not be committed by the PR under review (SADR-0014).\nCONTEXT.md:128:The Codex App-side runtime that transports an External AI Guardian Review: it builds the packet, drives the ChatGPT Pro review UI, extracts the verdict, validates the fenced `external_ai_guardian_review` artifact, relays only the validated approval to GitHub, and routes non-approval verdicts back into Shiki as bounded repair/evidence work (SADR-0014). Claude Code is the implementer/repairer and must not operate this Guardian UI path for its own work.\nCLAUDE.md:26:Claude Code is also the default hands (SADR-0008): implementation, tests, and bounded repair commits for assigned tasks are dispatched as a headless Claude Code session into the task's registered worktree through `shiki runner claude`, authenticated by the operator's Claude subscription.\nAGENTS.md:21:- **Claude Code** is the default implementer and runner runtime (SADR-0008): assigned tasks are dispatched into their registered worktree as a headless Claude Code session through `shiki runner claude`, authenticated by the operator's Claude subscription.\nAGENTS.md:109:Writes code in a scoped branch or worktree and verifies acceptance checks. Claude Code is the default implementer for source changes and repair commits, dispatched through `shiki runner claude`; Codex Front remains an optional implementer for tasks explicitly assigned to `codex` (SADR-0008). An implementer never authors an approval record (see [Approval Records](#approval-records)): it satisfies acceptance criteria with real work, and reports any criterion it cannot satisfy from the worktree's artifacts as a stop.\nAGENTS.md:303:When every MergeGate condition is satisfied, the goal loop (`shiki loop`) may merge risk low/medium PRs autonomously (SADR-0008/0009). High and critical risk always require Guardian approval before merge.\ndocs/proposals/0001-memory-loop-spec-freeze-review.md:319:- bypassPermissions bounding\uff08\u5225 Goal\u3001SADR-0008 \u8a18\u8f09\u306e\u30d5\u30a9\u30ed\u30fc\u30a2\u30c3\u30d7\uff09\ndocs/proposals/0001-memory-loop-spec-freeze-review.md:368:| SF-04 \u51cd\u7d50\u5f8c\u306e\u5909\u66f4\u306f Amendment \u306e\u307f | \u2705 \u5408\u610f\u6e08\u307f\uff08SADR-0009\uff09 |\n.codex/skills/shiki/SKILL.md:75:- Claude Code implements and repairs by default (`shiki runner claude`, SADR-0008); Codex implements and repairs tasks explicitly assigned to `codex`.\ndocs/proposals/0002-autonomous-loop-completion.md:4:Goal issue: #140. Parent PRD: #119 (Shiki 0.2.0, G-D). Decision: SADR-0011.\ndocs/proposals/0002-autonomous-loop-completion.md:8:The Shiki 0.2.0 autonomous post-freeze loop (SADR-0008/0009, #119 G-D) is not\ndocs/proposals/0002-autonomous-loop-completion.md:34:via an independent verifier, never by the implementer self-attesting (SADR-0011).\ndocs/proposals/0002-autonomous-loop-completion.md:40:   (loop-observed green; red-first ordering is not gated \u2014 SADR-0011).\ndocs/proposals/0002-autonomous-loop-completion.md:69:- **Evidence ownership (SADR-0011, Q1):** the loop produces all quality-gate\ndocs/proposals/0002-autonomous-loop-completion.md:106:- High/critical-risk auto-merge (remains Guardian-gated, SADR-0010).\ndocs/proposals/0002-autonomous-loop-completion.md:110:- Deterministic red\u2192green verification (SADR-0011 future enhancement).\ndocs/proposals/0002-autonomous-loop-completion.md:125:  Guardian-reviewed per SADR-0010.\ndocs/proposals/0002-autonomous-loop-completion.md:127:  (gaps at file:line), SADR-0011, and a Workflow parallel exploration sweep\ndocs/proposals/0002-autonomous-loop-completion.md:129:- **Decisions:** SADR-0011 (autonomous quality-gate evidence). Domain terms:\n.github/workflows/shiki-mergegate.yml:55:          # Live Guardian-approval evidence. A Contract PR (SADR-0015) forces the\n.github/workflows/shiki-mergegate.yml:70:          # resolve the SADR-0017 bookkeeping-closeout exemption; without it a\n.github/workflows/shiki-mergegate.yml:108:          # SADR-0015 Contract Approval carry, built the SAME way shiki-cca-completion.yml\n.github/workflows/shiki-mergegate.yml:159:          # SADR-0018 base-sync carry (guardian_comment_carried). --base-sync-carry\n.github/prompts/cca-completion-check.md:60:- For Guardian approval (CCA-08), read the deterministic `.shiki/gha/guardian-approval.json` (the result of the same authoritative `evaluate_guardian_approval` over `.shiki/guardian-policy.json` that the MergeGate policy check uses). Do not interpret raw PR comments yourself. If `required` is false, CCA-08 is not applicable; if `required` and `approved` are both true, CCA-08 is satisfied by the recorded authority in `sources`/`ai_reviewers`. Read each recorded source as an authority the deterministic signal already established, and do not re-derive it. An external AI guardian review is a valid authority per SADR-0010 \u2014 record it as `external_ai_model`, never as a human approver. A `guardian_comment_carried` source means the signal proved, per SADR-0018, that a prior-head Guardian comment approval was carried across a pure base sync (git tree reproduction); read it as a recorded Guardian authority and do not re-run the base-sync proof or re-judge whether the carry applies. If `required` is true and `approved` is false, return `needs_guardian`. Do not count CCA Review Bridge approval or advisory Claude review as Guardian approval.\ntests/test_closeout.py:5:Three defects this suite pins (SADR-0012):\n.github/workflows/shiki-cca-completion.yml:59:          # SADR-0017 bookkeeping-closeout exemption inputs, built the SAME way the\n.github/workflows/shiki-cca-completion.yml:91:          # SADR-0015 Contract Approval carry. Build the registration proof the\n.github/workflows/shiki-cca-completion.yml:152:          # external AI guardian review artifact, SADR-0010). The policy check\n.github/workflows/shiki-cca-completion.yml:155:          # SADR-0018 base-sync carry (guardian_comment_carried). --base-sync-carry\n.github/workflows/shiki-cca-completion.yml:312:            guardian review is a valid authority per SADR-0010; record it as such,\n.github/workflows/shiki-cca-completion.yml:314:            the signal proved (SADR-0018) that a prior-head Guardian comment\n.github/workflows/shiki-cca-completion.yml:397:            guardian review is a valid authority per SADR-0010; record it as such,\n.github/workflows/shiki-cca-completion.yml:399:            the signal proved (SADR-0018) that a prior-head Guardian comment\n.github/workflows/shiki-cca-completion.yml:462:          # SADR-0018 base-sync carry (guardian_comment_carried). --base-sync-carry\n.github/workflows/shiki-cca-completion.yml:747:          # This must run for every mode. SADR-0017's bookkeeping-closeout\n.github/workflows/shiki-cca-completion.yml:769:          # SADR-0015 Contract Approval carry, built the SAME way the CCA context\n.github/workflows/shiki-cca-completion.yml:820:          # SADR-0018 base-sync carry (guardian_comment_carried) passed to the\ntests/test_workflow_invocation.py:59:# SADR-0018 base-sync carry: shiki-cca-completion.yml now has a top-level entry in\ntests/test_workflow_invocation.py:127:    # SADR-0015 Contract Approval carry: the metadata job now passes\ntests/test_workflow_invocation.py:133:    # SADR-0018 base-sync carry: the metadata job now also passes --default-branch.\ntests/test_workflow_invocation.py:296:        mutated copy. shiki-cca-completion.yml is included because SADR-0018 added a\n.github/workflows/release.yml:6:# docs/adr/SADR-0007-packaging-and-release.md.\nscripts/test_shiki_start.sh:132:# Start without explicit Spec Freeze approval must fail (SADR-0009).\nscripts/test_shiki_start.sh:180:# protection, through cmd_start -> cmd_init (SADR-0013).\ndocs/agents/shiki-cli-architecture.md:28:| `scripts/shiki_runtime_adapters.py` | Runner adapter boundary: per-runtime tool/auth probes and headless execution commands bound to registry runtime names (SADR-0008). |\ntests/test_loop_lock_validator_hint.py:3:Under the mirror-lock model (SADR-0016 / A-LOCKS) a registered task's stored\ntests/test_loop_lock_validator_hint.py:11:WARN-ONLY; the future mirror-identity rule (SADR-0016 step B / remediation R-02)\ntests/test_loop_lock_validator_hint.py:55:        # advisory neither requires nor forbids it (SADR-0016 step B / R-02).\ndocs/agents/triage-labels.md:23:- `risk:medium` - requires normal review; goal-loop auto-merge permitted once all MergeGate evidence is green (SADR-0008).\ntests/test_contract_immutability.py:1:\"\"\"SADR-0015 Contract immutability on the NORMAL task-PR path.\ntests/test_contract_immutability.py:18:snapshot to check it against, it fails closed. Only the mutable set the SADR-0017\ntests/test_contract_immutability.py:203:        # The mutable-field set is the SAME constant the SADR-0017 closeout\nscripts/test_shiki_guardian_policy.sh:594:# SADR-0015 Contract Approval (end-to-end): a contract-approved implementation PR\ntests/test_guardian_signal.py:1:\"\"\"Deterministic Guardian-approval signal for the CCA judge (SADR-0010).\ndocs/agents/implementation-policy.md:44:6. **The assigned implementer implements; CCA judges.** Claude Code is the default implementer and repairer (SADR-0008); Codex Front implements tasks explicitly assigned to `codex`. GitHub CCA is the default completion judge.\ndocs/agents/implementation-policy.md:49:11. **Spec Freeze gates execution.** No plan runs without an operator-approved `spec_freeze` block (SADR-0009). Post-freeze scope changes require an operator-approved, recorded Spec Amendment; non-scope-moving interpretations are recorded in the Assumption Log.\ndocs/agents/implementation-policy.md:50:12. **Approval records are authored only by their authority.** An implementation runtime must never author, edit, forge, or backfill an operative approval record \u2014 a `spec_freeze` block, a Spec Amendment approval, Guardian approval evidence, a CCA `complete` verdict, or any other artifact whose meaning is that an authority approved something. An acceptance criterion that cannot be satisfied from the worktree because the missing artifact is such a record is reported as a stop, never closed by manufacturing it. This does not cover approval-record schemas or clearly-scoped test fixtures that never enter the real `.shiki/` mirror (SADR-0009/0010).\ntests/test_loop_branch_plumbing.py:416:    \"\"\"SADR-0012: the effector pushes the terminal state to main + repoints expected_pr.\"\"\"\ndocs/agents/runtime-registry.md:36:| `claude-code` | planner, implementer, runner, reviewer | local_cli | claude_subscription_oauth | Local Claude Code planning, review, and the default implementer/runner runtime (SADR-0008), dispatched with `shiki runner claude`. |\nscripts/shiki_cli.py:199:    contract = subcommands.add_parser(\"contract\", help=\"Open a Contract PR that registers a spec-frozen Goal's task contracts (SADR-0015)\")\ntests/test_adr_references.py:4:with \"ADR 0017\" nine times and ``scripts/guardian_approval_signal.py`` cites\ntests/test_adr_references.py:35:# ``ADR 0016`` / ``SADR-0016`` -- a namespace plus four-digit citation. The\ntests/test_adr_references.py:199:        # A script cites ADR 9991, but no docs/adr/9991-*.md exists.\ntests/test_adr_references.py:202:            script_body='\"\"\"See ADR 9991 for the rule.\"\"\"\\n',\ntests/test_adr_references.py:212:        self.assertIn(\"docs/adr/9991-*.md\", message)  # missing path\ntests/test_adr_references.py:217:            script_body='\"\"\"Target ADR 0042 exists, but platform SADR-0042 does not.\"\"\"\\n',\ntests/test_adr_references.py:223:        self.assertEqual(violations[0].expected_path, \"docs/adr/SADR-0042-*.md\")\ntests/test_adr_references.py:227:            adr_names=[\"0042-target-decision.md\", \"SADR-0042-platform-decision.md\"],\ntests/test_adr_references.py:228:            script_body='\"\"\"Target ADR 0042 and platform SADR-0042 are distinct.\"\"\"\\n',\ntests/test_adr_references.py:239:            script_body='\"\"\"Only mentions ADR 0001 here.\"\"\"\\n',\ntests/test_adr_references.py:249:            script_body='\"\"\"Cites ADR 0007 once.\"\"\"\\n',\ntests/test_adr_references.py:258:            script_body='\"\"\"Cites ADR 0016 which exists.\"\"\"\\n',\nscripts/shiki_github.py:610:    in solo operation after CCA returns ``complete`` (see SADR-0013 and\nscripts/shiki_github.py:704:    \"\"\"The PR-12 ``## Pre-PR code review`` body section (SADR-0011).\nscripts/shiki_github.py:721:    lines.append(\"- Independent read-only reviewer (claude -p, read tools only) \u2014 SADR-0011\")\nscripts/shiki_github.py:770:            \"(SADR-0011); a red run blocks the PR.\",\ntests/test_policy_job_merged_prs.py:2:EVERY judged PR, not only ``post_merge_reconcile`` ones (SADR-0017).\ntests/test_workflow_invocation_carry.py:1:\"\"\"SADR-0018 base-sync carry (guardian_comment_carried) reaches CI.\ntests/test_mirror_scope.py:28:And Spec Freeze is operator-only (SADR-0009): no PR may author or alter a\ntests/test_contract_amendment.py:1:\"\"\"Spec Amendment PR mode (SADR-0009/0015): correct a frozen contract on a\ntests/test_contract_mode.py:1:\"\"\"Contract PR mode (SADR-0015): approve the task contract before dispatch.\ntests/test_contract_mode.py:77:                \"title\": \"SADR-0015 contract approval\",\ntests/test_completion_lock_scope.py:24:lock-narrowing discipline (SADR-0016) replaced that directory glob with named\ntests/test_paired_invariants.py:726:        # (`SADR-0015`) out.\ntests/test_loop_e2e_contract.py:1:\"\"\"End-to-end loop self-drive CONTRACT meta-test (PRD 0002 T5, SADR-0011).\ntests/test_guardian_review.py:1:\"\"\"Tests for the External AI Guardian Review adapter contract (SADR-0010/0014).\ntests/test_guardian_review.py:56:        relevant_docs=[\"docs/adr/SADR-0010-external-ai-guardian-review.md\"],\ntests/test_guardian_review.py:164:        focus = gr.classify_review_focus([\"docs/adr/0099-x.md\", \"README.md\"], \"\", \"low\")\ntests/test_guardian_review.py:174:        focus = gr.classify_review_focus([\"docs/adr/0099-x.md\"], \"\", \"critical\")\ntests/test_guardian_signal_closeout.py:1:\"\"\"The bookkeeping-closeout Guardian exemption on the CCA *signal* path (SADR-0017).\ntests/test_guardian_signal_closeout.py:8:MergeGate *metadata* check (which learned the SADR-0017 exemption) but the CCA\ntests/test_guardian_signal_closeout.py:16:* each SADR-0017 disqualifier IN ISOLATION re-requires Guardian approval;\ntests/test_loop_code_review.py:1:\"\"\"Pre-PR independent code-review verifier (PRD 0002 T3, SADR-0011).\ntests/test_contract_open.py:7:path, so the suite adds no approval record to real mirror state (SADR-0015: an\ntests/test_contract_approval.py:1:\"\"\"SADR-0015 Contract Approval \u2014 the OR-alternative to a live PR Guardian approval.\ntests/test_contract_approval.py:3:SADR-0015 shipped in halves. The registration half (contract mode,\ntests/test_cca_evidence_manifest_taskless.py:3:Regression coverage for the contract-mode (SADR-0015) defect where the CCA\ntests/test_contract_plan_registration.py:1:\"\"\"A Contract PR may carry its Goal's own spec-frozen source plan (SADR-0015 gap).\ntests/test_contract_plan_registration.py:3:SADR-0015 has a Contract PR carry \"only Goal, task, and DAG registration\" and\ntests/test_loop_tdd_evidence.py:1:\"\"\"Loop-observed TDD evidence + per-task test command (PRD 0002 T2, SADR-0011).\ntests/test_loop_tdd_evidence.py:4:in the worktree \u2014 never trust the implementer's self-attestation (SADR-0011:\ntests/test_runner_adapters.py:1:\"\"\"Runtime adapter boundary contract (SADR-0008).\ntests/test_loop_engine.py:1:\"\"\"Goal loop decision engine contract (G-D, SADR-0008/0009).\ntests/test_loop_engine.py:67:        # SADR-0012: a merged impl PR (no closeout_pr yet) routes to a closeout PR\ntests/test_loop_engine.py:189:        # loop may merge high/critical autonomously (SADR-0010).\ntests/test_sadr_install_migration.py:36:        if path.name != \"SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\"\ntests/test_sadr_install_migration.py:219:            (target / \"docs\" / \"adr\" / \"SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\").is_file()\ntests/test_branch_behind_sync.py:164:        # laundered into a head-moving sync (SADR-0010). reruns=2 exhausts the CCA\ntests/test_branch_behind_sync.py:177:        # be the impersonation pathway SADR-0010 forbids.\ntests/test_bookkeeping_closeout.py:1:\"\"\"The bookkeeping-closeout Guardian exemption (SADR-0017).\ntests/test_bookkeeping_closeout.py:290:        # the point of SADR-0017's whitelist fix: it FAILS if expected_branch is\ntests/test_bookkeeping_closeout.py:341:        # Guard against the whitelist drifting wider than the one entry SADR-0017\ntests/test_repair_delivery.py:168:    origin/main tracking ref) so the SADR-0012 closeout can cut from it.\"\"\"\nscripts/guardian_approval_signal.py:11:label/review/comment OR an external AI guardian review artifact, SADR-0010)\nscripts/guardian_approval_signal.py:32:# The bookkeeping-closeout exemption (SADR-0017) must resolve risk IDENTICALLY on\nscripts/guardian_approval_signal.py:155:    \"\"\"Whether this PR is a proven SADR-0017 bookkeeping closeout.\nscripts/guardian_approval_signal.py:200:    \"\"\"Evaluate SADR-0015 Contract Approval for the CCA Guardian signal.\nscripts/guardian_approval_signal.py:251:    # SADR-0017 bookkeeping-closeout exemption inputs. All default to values that\nscripts/guardian_approval_signal.py:257:        help=\"Path to the base branch's .shiki snapshot; required for the SADR-0017 closeout exemption.\",\nscripts/guardian_approval_signal.py:262:        help=\"git --name-status changed-files file; required for the SADR-0017 closeout exemption.\",\nscripts/guardian_approval_signal.py:269:    # SADR-0015 Contract Approval input. Defaults to \"\" (no proof), so an\nscripts/guardian_approval_signal.py:276:            \"Path to the SADR-0015 Contract Approval registration proof (JSON). When present and valid, a \"\nscripts/guardian_approval_signal.py:319:    # SADR-0017: a PROVEN bookkeeping closeout carries no implementation and\nscripts/guardian_approval_signal.py:377:            note += \" (SADR-0017 bookkeeping-closeout exemption applied)\"\nscripts/guardian_approval_signal.py:391:    # SADR-0015 Contract Approval OR-branch. The Guardian requirement is satisfied\nscripts/guardian_approval_signal.py:419:                \"Guardian approval satisfied by Contract Approval (SADR-0015): the task contract was \"\ntests/test_dispatch_lock_precondition.py:10:SADR-0017 bookkeeping exemption needs the lock to move ``active -> released`` and\nscripts/mergegate_check.py:302:# A Contract PR (SADR-0015) declares itself with this exact HTML-comment marker.\nscripts/mergegate_check.py:310:# that opens a Contract PR must NOT apply this label to its own PR (SADR-0015\nscripts/mergegate_check.py:335:# An Amendment PR (Spec Amendment, SADR-0009/0015) declares itself with this exact\nscripts/mergegate_check.py:595:    ``allow_goal_registration`` is set (contract mode, SADR-0015), add the goal\nscripts/mergegate_check.py:615:    # mode (SADR-0015) may carry the Goal's own spec-frozen source plan to the\nscripts/mergegate_check.py:702:            # Contract mode only (SADR-0015): the Contract PR registers the goal\nscripts/mergegate_check.py:708:            # Contract flow only (SADR-0015): the Contract PR may carry the Goal's\nscripts/mergegate_check.py:782:    \"\"\"Validate a Contract PR (SADR-0015): register a spec-frozen Goal's task\nscripts/mergegate_check.py:790:    ``allow_source_plan_carry`` opts into the SADR-0015 plan-reachability escape:\nscripts/mergegate_check.py:860:    \"\"\"Validate the spec-frozen source plan a Contract PR carries (SADR-0015).\nscripts/mergegate_check.py:862:    SADR-0015 has a Contract PR carry only Goal/task/DAG registration and assumes\nscripts/mergegate_check.py:949:# a completion \u2014 must never rewrite. This is the SADR-0017 mutable set minus\nscripts/mergegate_check.py:1124:    \"\"\"Validate a Spec Amendment PR (SADR-0009/0015): correct a spec-frozen Goal's\nscripts/mergegate_check.py:1941:            # SADR-0017: a PROVEN bookkeeping closeout carries no implementation and\nscripts/mergegate_check.py:1972:# reached \u2014 the exact defect SADR-0017 exists to remove. That independent head-ref\nscripts/mergegate_check.py:2009:    \"\"\"Classify whether a PR is a pure bookkeeping closeout (SADR-0017).\nscripts/mergegate_check.py:2028:    The six SADR-0017 conditions:\nscripts/mergegate_check.py:2243:    # SADR-0015 Contract Approval OR-branch (\"Evaluation placement\"). The Guardian\nscripts/mergegate_check.py:2256:            + \" (SADR-0015 Contract Approval: the task contract was registered and Guardian-approved before dispatch)\"\nscripts/mergegate_check.py:2482:# (SADR-0015 Contract immutability, normal path). When the task file is present in\nscripts/mergegate_check.py:2493:# the mutable set stays the single SADR-0017 constant, never re-copied here.\nscripts/mergegate_check.py:2509:    than the base snapshot (SADR-0015). Returns ``max(base_risk, head_risk)`` by the\nscripts/mergegate_check.py:2527:    governance contract (SADR-0015 Contract immutability, normal path).\nscripts/mergegate_check.py:2533:    base-when-present, exactly as SADR-0015 states:\nscripts/mergegate_check.py:2563:            \"its frozen governance contract (SADR-0015 Contract immutability); failing closed\"\nscripts/mergegate_check.py:2574:            \"read to verify the frozen governance contract (SADR-0015 Contract immutability); failing closed\"\nscripts/mergegate_check.py:2591:                \"from the base snapshot (SADR-0015 Contract immutability)\"\nscripts/mergegate_check.py:2609:    \"\"\"Load the SADR-0015 Contract Approval registration proof from the file named\nscripts/mergegate_check.py:2637:    \"\"\"Evaluate SADR-0015 Contract Approval for a NORMAL task PR (the OR alternative\nscripts/mergegate_check.py:2646:    degrades to None (\"does not apply\"), exactly the SADR-0015 failure mode.\nscripts/mergegate_check.py:2778:            # Spec Freeze is operator-only (SADR-0009). No PR may author or alter a\nscripts/mergegate_check.py:2873:            \"Path to the SADR-0015 Contract Approval registration proof (JSON). When present and valid, a \"\nscripts/mergegate_check.py:2911:    # closeout classifier to prove the task's implementation PR merged (SADR-0017).\nscripts/mergegate_check.py:2918:    # Guardian gate evaluates it as if the task were low risk (SADR-0017). Stays\nscripts/mergegate_check.py:2981:            # it carries no single implementation task id (SADR-0015).\nscripts/mergegate_check.py:3028:        # Contract mode (SADR-0015): a goal-scoped, spec-frozen task-contract\nscripts/mergegate_check.py:3045:            # to the default branch (SADR-0015 plan-reachability gap); enable it.\nscripts/mergegate_check.py:3076:        # Amendment mode (SADR-0009/0015): a goal-scoped PR that corrects a\nscripts/mergegate_check.py:3211:            # SADR-0017: a proven bookkeeping closeout (task terminal state only, no\nscripts/mergegate_check.py:3240:                # SADR-0015 Contract immutability on the NORMAL task-PR path: a PR\nscripts/mergegate_check.py:3335:                # Normal path (SADR-0015): resolve the Guardian risk NEVER WEAKER\nscripts/mergegate_check.py:3354:            # SADR-0015 Contract Approval applies ONLY on the normal task path: a\nscripts/test_shiki_goal_loop.sh:75:    # The independent pre-PR reviewer (SADR-0011) carries --json-schema and\nscripts/test_shiki_goal_loop.sh:141:    # origin/main) sees the merged impl/closeout state (SADR-0012). The branch is a\nscripts/test_shiki_goal_loop.sh:196:    \"decisions\": [\"Loop drives frozen goals (SADR-0008/0009); completion is pushed to main via a closeout PR (SADR-0012)\"]\nscripts/test_shiki_goal_loop.sh:222:# PR that pushes task=done + goal=complete to main (SADR-0012) -> auto-merge the\nscripts/test_shiki_goal_loop.sh:227:# The loop opened a closeout PR (SADR-0012): its history records create_closeout_pr.\nscripts/test_shiki_control_plane.sh:155:# SADR-0015 Contract Approval carry (T-20260801T040836632579Z-2cde7018). BOTH the\nscripts/test_shiki_control_plane.sh:188:# SADR-0018 base-sync carry (guardian_comment_carried) reaches CI\nscripts/test_shiki_control_plane.sh:1588:    # SADR-0015 Contract immutability (normal task-PR path): a PR may move its own\nscripts/test_shiki_control_plane.sh:1600:    # SADR-0015 Contract immutability (normal task-PR path): a GOVERNANCE field\nscripts/test_shiki_governance_evidence.sh:745:# SADR-0017: bookkeeping-closeout Guardian exemption smuggling cases.\nscripts/test_shiki_governance_evidence.sh:966:# Amendment mode (SADR-0009/0015): a Spec Amendment PR cannot lower its OWN\nscripts/shiki_runtime_registry.py:97:        description=\"Local Claude Code planning, review, and default implementation/runner runtime (SADR-0008).\",\nscripts/shiki_contract_approval.py:2:\"\"\"SADR-0015 Contract Approval: the pure evaluator MergeGate accepts as an\nscripts/shiki_contract_approval.py:5:SADR-0015 moves Guardian approval for high/critical risk from the implementation\nscripts/shiki_contract_approval.py:19:SADR-0015 \"Evaluation placement\": Contract Approval is evaluated by this new,\nscripts/shiki_contract_approval.py:49:# applies. SADR-0015 is explicit that a ``contract_approval`` entry in the signal's\nscripts/shiki_contract_approval.py:56:    \"\"\"Outcome of evaluating SADR-0015 Contract Approval for one task PR.\nscripts/shiki_contract_approval.py:78:    (SADR-0015 condition 3). Empty list means the proof holds.\nscripts/shiki_contract_approval.py:145:    \"\"\"Whether this PR carries an SADR-0015 Contract Approval.\nscripts/shiki_guardian.py:48:    # external_ai_guardian_review: a first-class authority kind (SADR-0010).\nscripts/shiki_guardian.py:766:    verify-response path consume it so neither can drift from SADR-0010 / SADR-0014\nscripts/shiki_installer.py:242:    \"\"\"Former numeric paths for Shiki SADR-0001 through SADR-0018.\nscripts/shiki_installer.py:244:    The authoritative slugs come from the current SADR filenames. SADR-0019 is\nscripts/shiki_tasks.py:26:# Contract PR mode (SADR-0015). The body marker DECLARES intent; the\nscripts/shiki_tasks.py:86:# Safe default for the loop-observed TDD gate (SADR-0011): the command the goal\nscripts/shiki_tasks.py:229:            \"before Shiki can run it; see CONTEXT.md 'Spec Freeze' and SADR-0009\"\nscripts/shiki_tasks.py:342:        # The loop-observed TDD gate (SADR-0011) exec's THIS structured command in\nscripts/shiki_tasks.py:699:    PR (SADR-0015).\nscripts/shiki_tasks.py:877:    \"\"\"Open a Contract PR (SADR-0015): register a spec-frozen Goal's task contracts.\nscripts/shiki_tasks.py:957:        # Structured loop-observed TDD command (SADR-0011); falls back to the safe\nscripts/test_shiki_run_orchestrator.sh:83:# A grilled but unfrozen plan must be rejected (SADR-0009 Spec Freeze gate).\nscripts/test_shiki_code_review_gate.sh:60:    \"decisions\": [\"code-review is a mandatory pre-PR implementer gate (SADR-0008)\"]\nscripts/validate_shiki.py:304:# post_merge_reconcile PR (and the SADR-0017 bookkeeping-closeout exemption) can be\nscripts/validate_shiki.py:307:# SADR-0018 base-sync carry (guardian_comment_carried): every invocation that judges\nscripts/validate_shiki.py:762:        raise ValidationError(f\"{GUARDIAN_POLICY_PATH}: external_ai_guardian_review must bind to the head SHA (SADR-0010)\")\nscripts/validate_shiki.py:2088:    Under the derive-at-judgment-time mirror-lock model (SADR-0016 / A-LOCKS) a\nscripts/validate_shiki.py:2102:    (SADR-0016 step B / remediation R-02) will decide any blocking behaviour, and\nscripts/test_shiki_runner_claude.sh:51:    \"decisions\": [\"Use Claude Code as the default implementation runtime (SADR-0008)\"]\nscripts/test_shiki_runtime_registry.sh:69:# SADR-0008: claude-code is the default implementer/runner; codex stays a\nscripts/shiki_loop.py:2:\"\"\"Autonomous post-freeze Goal loop (SADR-0008/0009).\nscripts/shiki_loop.py:52:# review/label/comment OR an external AI guardian review, SADR-0010) and is the\nscripts/shiki_loop.py:316:        # goal only in the coordinator mirror (Gap B / SADR-0012). Drive a closeout\nscripts/shiki_loop.py:362:    # recorded authority. This closes the impersonation pathway SADR-0010 exists\nscripts/shiki_loop.py:425:        # laundered into a head-moving sync (SADR-0010). It also never fires while the\nscripts/shiki_loop.py:427:        # red check \u2014 so a head-bound Guardian approval, or an SADR-0015\nscripts/shiki_loop.py:485:    # (human review/label/comment OR an external AI guardian review, SADR-0010).\nscripts/shiki_loop.py:1379:    \"context (SADR-0011). Review ONLY the diff below for correctness bugs, broken \"\nscripts/shiki_loop.py:1409:            \"command\": \"independent pre-PR code review (claude -p, read-only) \u2014 SADR-0011\",\nscripts/shiki_loop.py:1434:    Loop-owned quality-gate step (SADR-0011): the reviewer is the same model as the\nscripts/shiki_loop.py:1558:            evidence=[\"independent read-only reviewer (claude -p) \u2014 SADR-0011\"],\nscripts/shiki_loop.py:1574:        evidence=[\"independent read-only reviewer (claude -p) \u2014 SADR-0011\"],\nscripts/shiki_loop.py:1587:    never be handed to a shell (SADR-0011: a deterministic observable command,\nscripts/shiki_loop.py:1602:    \"\"\"Loop-observed TDD gate (SADR-0011): run the task's tests in its worktree.\nscripts/shiki_loop.py:1690:    \"\"\"PR body for an autonomous closeout PR (SADR-0012). Must contain the literal\nscripts/shiki_loop.py:1701:        f\"Autonomous loop closeout (SADR-0012): the implementation PR for this task \"\nscripts/shiki_loop.py:1711:        f\"code-review gate, SADR-0011) before that PR merged. This PR carries only \"\nscripts/shiki_loop.py:1718:        f\"\\U0001f916 Generated by the Shiki goal loop (SADR-0012)\\n\"\nscripts/shiki_loop.py:1724:    (goal=complete iff this task completes the goal) to main \u2014 Gap B / SADR-0012.\nscripts/shiki_loop.py:1796:                    summary=\"Autonomous loop closeout: push goal completion to main (SADR-0012).\"))\nscripts/shiki_loop.py:1815:        commit = run([\"git\", \"commit\", \"-m\", f\"shiki: closeout {task_id} \u2014 push completion to main (goal loop, SADR-0012)\"], cwd=worktree, check=False)\nscripts/shiki_loop.py:1826:             \"--title\", f\"Closeout {task_id}: push goal completion to main (SADR-0012)\",\nscripts/shiki_loop.py:1844:                     + \" pushed to main by the goal loop (SADR-0012).\"),\nscripts/shiki_loop.py:2263:            # the scorecard report + completion ledger) to main (SADR-0012). Sync the\nscripts/shiki_loop.py:2294:        # (a) Pre-PR code-review gate (SADR-0011). An INDEPENDENT read-only\nscripts/shiki_loop.py:2318:        # Loop-owned TDD gate FIRST (SADR-0011): the loop \u2014 an independent\nscripts/shiki_loop.py:2477:        # SADR-0012: the impl PR merged; open a closeout PR that pushes the terminal\nscripts/shiki_loop.py:2528:        # SADR-0012: done-marking is DEFERRED. The loop never records `done` locally\ndocs/releasing.md:6:[docs/adr/SADR-0007-packaging-and-release.md](adr/SADR-0007-packaging-and-release.md).\ndocs/releasing.md:105:  (see [docs/adr/SADR-0007-packaging-and-release.md](adr/SADR-0007-packaging-and-release.md)).\nscripts/shiki_guardian_review.py:2:\"\"\"External AI Guardian Review adapter contract (SADR-0010 / SADR-0014).\nscripts/shiki_guardian_review.py:15:Role boundary (SADR-0010 / SADR-0014): Claude Code implements these DETERMINISTIC\nscripts/test_shiki_loop_lock_guard.sh:5:# (SADR-0016 / A-LOCKS).\ndocs/agents/guardian-policy.md:64:## Carried Guardian Approval Across A Proven Base Sync (SADR-0018)\ndocs/agents/guardian-policy.md:105:## External AI Guardian Review (SADR-0010)\ndocs/agents/guardian-policy.md:128:## External AI Guardian UI Adapter (SADR-0014)\ndocs/adr/SADR-0012-autonomous-loop-closeout-to-main.md:1:# SADR-0012 \u2014 Autonomous loop pushes goal completion to main via a closeout PR\ndocs/adr/SADR-0012-autonomous-loop-closeout-to-main.md:6:- Related: SADR-0008 (default implementer + autonomous merge), SADR-0009 (spec freeze), SADR-0011 (autonomous quality-gate evidence), issue #140 (complete the autonomous post-freeze loop), the #140 T5 live self-drive.\ndocs/adr/SADR-0012-autonomous-loop-closeout-to-main.md:40:- The loop reaches **goal complete on main, durably, with zero manual intervention** \u2014 closing #140 / SADR-0011's \"durable evidence, not local attestation\" for the completion step itself.\nscripts/shiki_runtime_adapters.py:159:# The independent pre-PR code-review verifier (SADR-0011). It is the SAME model as\ndocs/adr/SADR-0013-bootstrap-grants-actions-approve-pull-requests.md:1:# SADR-0013: Bootstrap Grants GitHub Actions Permission To Create And Approve Pull Requests\ndocs/adr/SADR-0013-bootstrap-grants-actions-approve-pull-requests.md:51:  posture is solo/AFK autonomous operation (SADR-0008/0009/0012), where the CCA\nscripts/test_shiki_adversarial_state.sh:506:# SADR-0015 Contract immutability on the NORMAL task-PR path (T-20260801\u20269f3f6f63):\nscripts/test_shiki_adversarial_state.sh:808:# SADR-0017: bookkeeping-closeout Guardian exemption smuggling cases.\nscripts/test_shiki_adversarial_state.sh:1134:# Spec Freeze is operator-only (SADR-0009). A new plan that is not the goal's\nscripts/test_shiki_adversarial_state.sh:1202:# Amendment mode (SADR-0009/0015) is two-factor (marker + mergegate:amendment\ndocs/adr/SADR-0010-external-ai-guardian-review.md:1:# SADR-0010: External AI Guardian Review As A First-Class Approval Authority\ndocs/adr/SADR-0001-keep-shiki-as-platform-name.md:1:# SADR-0001: Keep Shiki As The Platform Name\nscripts/shiki_migrations.py:155:            \"Plan ingest/run/daemon/smoke require spec_freeze.status=frozen (SADR-0009).\",\nscripts/shiki_migrations.py:233:            description=\"Add the additive spec_freeze block (SADR-0009) to stored plans that predate the Spec Freeze contract so they can be re-ingested or re-run.\",\ndocs/agents/runtime-auth-model.md:7:- **Claude Code**: the default implementer and runner runtime (SADR-0008). `shiki runner claude` dispatches a headless `claude -p` session into the task's registered worktree, authenticated by the operator's Claude subscription login.\ndocs/agents/runtime-auth-model.md:9:- **Codex App External AI Guardian UI Adapter**: the operator-facing adapter that builds External AI Guardian Review Packets, drives ChatGPT Pro review through the UI, extracts the reviewer verdict, validates the fenced `external_ai_guardian_review` artifact, and relays only validated approvals to GitHub (SADR-0014). It consumes the deterministic `shiki guardian packet|prompt|verify-response` surfaces. **Claude Code implementer/repairer sessions must not drive this Guardian UI path for their own work** \u2014 that boundary keeps implementation distinct from approval routing.\ndocs/agents/autonomous-loop-e2e-acceptance.md:4:SADR-0011 autonomous quality-gate work). It proves the second, non-simulated half\ndocs/agents/autonomous-loop-e2e-acceptance.md:16:SADR-0011's whole point is that autonomous evidence must be **deterministic\ndocs/agents/autonomous-loop-e2e-acceptance.md:64:those are loop-owned state transitions (SADR-0011). If the operator writes them,\ndocs/agents/autonomous-loop-e2e-acceptance.md:130:Per SADR-0011 / the Repair Loop: do not hand-clear the PR to \"make the gate\ndocs/agents/autonomous-loop-e2e-acceptance.md:135:self-attestation SADR-0011 forbids.\nscripts/shiki_bootstrap.py:350:            \"Plans cannot run without an operator-approved spec_freeze (SADR-0009).\"\ndocs/adr/SADR-0008-use-claude-code-as-default-implementer.md:1:# SADR-0008: Use Claude Code As The Default Implementer Runtime\ndocs/adr/SADR-0008-use-claude-code-as-default-implementer.md:5:Proposed (supersedes SADR-0005 for the implementer default; keeps its auth model)\ndocs/adr/SADR-0008-use-claude-code-as-default-implementer.md:9:SADR-0005 made Codex Front the default implementation runtime, with Claude Code\ndocs/adr/SADR-0008-use-claude-code-as-default-implementer.md:21:- The Goal lifecycle redesign (SADR-0009) requires an autonomous post-freeze\ndocs/adr/SADR-0008-use-claude-code-as-default-implementer.md:27:workflow\", SADR-0002). Any change to the implementer default must preserve the\ndocs/adr/SADR-0008-use-claude-code-as-default-implementer.md:42:  SADR-0005 remain supported for tasks explicitly assigned to `codex`.\ndocs/adr/SADR-0008-use-claude-code-as-default-implementer.md:63:(still requires its own ADR per SADR-0005).\ndocs/adr/SADR-0008-use-claude-code-as-default-implementer.md:103:  constitution (SADR-0002) for no operational gain and forecloses\nscripts/test_shiki_loop_e2e.sh:41:# to the merged PR branch so the loop's closeout step (SADR-0012), which cuts a\nscripts/test_shiki_loop_e2e.sh:217:    \"decisions\": [\"The loop drives frozen goals (SADR-0008/0009/0011)\"]\nscripts/test_shiki_loop_e2e.sh:245:# Two merges now: the impl PR, then the SADR-0012 closeout PR that pushes the\ndocs/adr/SADR-0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md:1:# SADR-0005: Use Codex As Front And Claude Code Action As The GitHub Runtime\ndocs/adr/SADR-0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md:5:Accepted (implementer default superseded by SADR-0008; the auth model and the\ndocs/adr/SADR-0014-codex-app-external-ai-guardian-ui-adapter.md:1:# SADR-0014: Use Codex App As The External AI Guardian UI Adapter\ndocs/adr/SADR-0014-codex-app-external-ai-guardian-ui-adapter.md:9:SADR-0010 admits `external_ai_guardian_review` as a first-class Guardian\ndocs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md:1:# SADR-0019: Separate Shiki Platform SADRs From Target ADRs\ndocs/adr/SADR-0002-use-agents-as-shared-constitution.md:1:# SADR-0002: Use AGENTS As The Shared Agent Constitution\ndocs/agents/codex-handoff.md:5:Claude Code through `shiki runner claude` (SADR-0008); the handoff contract\ndocs/adr/SADR-0009-requirements-definition-and-spec-freeze.md:1:# SADR-0009: Merge Grill And Planning Into Requirements Definition With Spec Freeze\ndocs/adr/SADR-0009-requirements-definition-and-spec-freeze.md:24:everything after it autonomous (SADR-0008 provides the implementation runtime\ndocs/adr/SADR-0015-contract-approval-before-dispatch.md:1:# SADR-0015: Approve The Task Contract Before Dispatch, Not The Diff After It\ndocs/adr/SADR-0015-contract-approval-before-dispatch.md:12:`external_ai_guardian_review` artifact (SADR-0010/0014) \u2014 is bound to a PR, and\ndocs/adr/SADR-0015-contract-approval-before-dispatch.md:133:is both earlier and smaller; the external AI Guardian path (SADR-0010/0014) works\ndocs/adr/SADR-0018-carried-guardian-approval-across-a-proven-base-sync.md:1:# SADR-0018: Carried Guardian Approval Across A Proven Base Sync\ndocs/adr/SADR-0003-require-skill-gate-for-mergegate-readiness.md:1:# SADR-0003: Require Skill Gate For MergeGate Readiness\ndocs/adr/SADR-0004-use-github-first-with-shiki-mirror.md:1:# SADR-0004: Use GitHub First With A Repository-Local Shiki Mirror\ndocs/adr/SADR-0007-packaging-and-release.md:1:# SADR-0007: Packaging And Release For Shiki\ndocs/adr/SADR-0007-packaging-and-release.md:28:  (consistent with SADR-0005's no-API-key-by-default stance).\ndocs/adr/SADR-0017-bookkeeping-closeouts-do-not-inherit-risk.md:1:# SADR-0017: A Bookkeeping Closeout Does Not Inherit Its Task's Risk\ndocs/adr/SADR-0017-bookkeeping-closeouts-do-not-inherit-risk.md:9:SADR-0012 made the goal loop push a task's completion to the default branch through a\ndocs/adr/SADR-0017-bookkeeping-closeouts-do-not-inherit-risk.md:52:   and `expected_branch` may differ \u2014 the SADR-0015 contract-immutability rule,\ndocs/adr/SADR-0017-bookkeeping-closeouts-do-not-inherit-risk.md:116:SADR-0012, which recorded why: a task file that is already `done` on the implementation\ndocs/adr/SADR-0017-bookkeeping-closeouts-do-not-inherit-risk.md:121:is a runtime authoring its own approval, which SADR-0010 and the constitution's\ndocs/adr/SADR-0011-autonomous-quality-gate-evidence.md:1:# SADR-0011: Autonomous Quality-Gate Evidence \u2014 Deterministic Facts And An Independent Verifier\ndocs/adr/SADR-0011-autonomous-quality-gate-evidence.md:9:The Shiki 0.2.0 autonomous post-freeze loop (SADR-0008/0009, PRD #119 G-D) must\ndocs/adr/SADR-0016-locks-bound-product-paths.md:1:# SADR-0016: Locks bound product paths; the Shiki mirror is bound by record identity\ndocs/adr/SADR-0016-locks-bound-product-paths.md:9:- Related: SADR-0004 (GitHub-first with a `.shiki` mirror), SADR-0009 (Spec Freeze),\ndocs/adr/SADR-0016-locks-bound-product-paths.md:10:  SADR-0012 (autonomous loop closeout to main), SADR-0015 (Contract Approval before\ndocs/adr/SADR-0016-locks-bound-product-paths.md:93:a one-time Spec Amendment per plan, which must land before SADR-0015's contract\ndocs/adr/SADR-0016-locks-bound-product-paths.md:97:registry, and SADR-0015's governance fields, from the base `.shiki` snapshot when\ndocs/adr/SADR-0016-locks-bound-product-paths.md:111:SADR-0012's closeout pull request. Registration of multi-task goals is SADR-0015's\ndocs/adr/SADR-0016-locks-bound-product-paths.md:130:add `goal_id` to SADR-0015's frozen governance field set, and until that lands a\ndocs/adr/SADR-0016-locks-bound-product-paths.md:142:before SADR-0015 T1; extension of the adversarial suites in\ndocs/adr/SADR-0016-locks-bound-product-paths.md:154:## Relationship to SADR-0004\ndocs/adr/SADR-0016-locks-bound-product-paths.md:156:This ADR **clarifies something SADR-0004 left unspecified**. SADR-0004 decides that\ndocs/adr/SADR-0016-locks-bound-product-paths.md:160:SADR-0004's Decision or Consequences is amended, superseded or weakened: the\ndocs/adr/SADR-0016-locks-bound-product-paths.md:167:It **amends SADR-0012**, whose Decision item 5 states that \"Loop-executed tasks\ndocs/adr/SADR-0016-locks-bound-product-paths.md:171:authorized by record identity, not by a declared lock. SADR-0012's substantive\ndocs/adr/SADR-0016-locks-bound-product-paths.md:175:It is **complementary to SADR-0015**, not a substitute. SADR-0015 owns contract\ndocs/adr/SADR-0016-locks-bound-product-paths.md:179:SADR-0015 T1's freezing of the `locks` field.\ndocs/adr/SADR-0016-locks-bound-product-paths.md:189:removes the low/medium autonomous path SADR-0008/0009 exists to provide. And it\ndocs/adr/SADR-0016-locks-bound-product-paths.md:209:SADR-0004's consequence that mirror conflicts are repaired against GitHub state;\ndocs/adr/SADR-0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md:1:# SADR-0006: Separate Codex Implementation, CCA Completion, And MergeGate Authorization\n\nexec\n/bin/zsh -lc \"rg -n --glob '\"'!.shiki/**'\"' --glob '\"'!skills/**'\"' --glob '\"'!tests/test_adr_references.py'\"' --glob '\"'!tests/test_sadr_install_migration.py'\"' '\\\\bADR[- ](000[1-9]|001[0-8])\\\\b|docs/adr/(000[1-9]|001[0-8])-|\\\\(adr/(000[1-9]|001[0-8])-' AGENTS.md CLAUDE.md CONTEXT.md CONTRIBUTING.md SYSTEM_PROMPT.md pyproject.toml docs scripts tests .github .codex\" in /private/tmp/shiki-adr-collision.pa0KwY/.worktrees/shiki-t-20260807t073307221254z-2d22d654-introduc\n exited 1 in 0ms:\ndiff --git a/.codex/skills/shiki/SKILL.md b/.codex/skills/shiki/SKILL.md\nindex c8e2e85d9d75d49cc4f2d6286ee22659c4e53e75..93cded623b2c15d9f65c76491a1c2d813ac9e6c0\n--- a/.codex/skills/shiki/SKILL.md\n+++ b/.codex/skills/shiki/SKILL.md\n@@ -18,6 +18,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n Codex CLI does not register this skill as a custom `/shiki` slash command. When\n the user types `/shiki` in Codex CLI and gets \"Unrecognized command\", explain\n that this is expected: invoke Shiki in Codex with natural language such as\n@@ -69,7 +72,7 @@\n \n ## Responsibilities\n \n-- Claude Code implements and repairs by default (`shiki runner claude`, ADR 0008); Codex implements and repairs tasks explicitly assigned to `codex`.\n+- Claude Code implements and repairs by default (`shiki runner claude`, SADR-0008); Codex implements and repairs tasks explicitly assigned to `codex`.\n - Claude Code Action can act as GitHub-side CCA or reviewer.\n - CCA judges completion.\n - MergeGate authorizes state transitions and merge readiness.\ndiff --git a/.github/prompts/cca-completion-check.md b/.github/prompts/cca-completion-check.md\nindex ed203ad5f302ecc81372d8ab27d2b1232ca97497..fd89950528333ef6242e6d5443904f5392ab7e56\n--- a/.github/prompts/cca-completion-check.md\n+++ b/.github/prompts/cca-completion-check.md\n@@ -19,6 +19,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n ## Required Reading\n \n Read only the files needed to judge the PR contract. Prefer this order:\ndiff --git a/AGENTS.md b/AGENTS.md\nindex 7f041100d49c2a7525f51cb0f5d696a4a6d1c78c..2b700e989ff295987aa8a4b0c5fcb1acd80020da\n--- a/AGENTS.md\n+++ b/AGENTS.md\n@@ -44,6 +44,10 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use\n+`SADR-NNNN-*.md`; target product decisions retain `NNNN-*.md` and the `ADR`\n+namespace.\n+\n Use the canonical source-of-truth order above. Runtime-specific reading orders\n are operational guidance only and must not override or reorder it.\n \ndiff --git a/CLAUDE.md b/CLAUDE.md\nindex 98a932e7841a632866fcd53b0bad0e3af3711d3d..611d491912d6a72a0327a1731b8f33a41acb4baa\n--- a/CLAUDE.md\n+++ b/CLAUDE.md\n@@ -14,6 +14,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n ## Claude Role\n \n Default to planner, default implementer, reviewer, coordinator, documentation editor, CCA-style completion judge, and final-judgment assistant.\ndiff --git a/SYSTEM_PROMPT.md b/SYSTEM_PROMPT.md\nindex aaaea45e3a896c3a44ba89eec8bb84325ecf6c62..2e7c093ebe9e8bb34e0923756f191b4f7fa581f5\n--- a/SYSTEM_PROMPT.md\n+++ b/SYSTEM_PROMPT.md\n@@ -30,6 +30,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n Use the canonical source-of-truth order above. Runtime-specific reading orders\n are operational guidance only and must not override or reorder it. The current\n conversation is non-durable operator input.\ndiff --git a/docs/adr/0001-keep-shiki-as-platform-name.md b/docs/adr/SADR-0001-keep-shiki-as-platform-name.md\nindex 20a9637c3b9d8c97de7ba8db7cfae4d82d9a7dba..8b685439959aada1b221c112f0ac3c51ca8a1695\n--- a/docs/adr/0001-keep-shiki-as-platform-name.md\n+++ b/docs/adr/SADR-0001-keep-shiki-as-platform-name.md\n@@ -1,3 +1,3 @@\n-# Keep Shiki As The Platform Name\n+# SADR-0001: Keep Shiki As The Platform Name\n \n Shiki remains the product name for the GitHub-native agentic engineering control plane. We considered more descriptive names such as MergePilot, MergeGate, and Kiroku, but kept Shiki because it carries the existing goal-seeking framework identity while remaining independent of Hermes, Codex, Claude Code, or any single runtime.\ndiff --git a/docs/adr/0002-use-agents-as-shared-constitution.md b/docs/adr/SADR-0002-use-agents-as-shared-constitution.md\nindex 73de30ecb078120eac540d9bc51855e3cd48d0e3..c5486182bf06fca36ea508496b013c67302fd989\n--- a/docs/adr/0002-use-agents-as-shared-constitution.md\n+++ b/docs/adr/SADR-0002-use-agents-as-shared-constitution.md\n@@ -1,3 +1,3 @@\n-# Use AGENTS As The Shared Agent Constitution\n+# SADR-0002: Use AGENTS As The Shared Agent Constitution\n \n Shiki uses `AGENTS.md` as the runtime-neutral constitution and `CLAUDE.md` as a thin Claude Code wrapper. This keeps Codex, Claude Code, Hermes Runner, and future Agent Runtimes aligned on the same GitHub-first MergeGate rules while still allowing runtime-specific instructions where needed.\ndiff --git a/docs/adr/0003-require-skill-gate-for-mergegate-readiness.md b/docs/adr/SADR-0003-require-skill-gate-for-mergegate-readiness.md\nindex 424be0c9af4c8f0136f1fde68a468c47ff84e764..7fc337987c7df9792f75f181dfcbed75fe5ffff2\n--- a/docs/adr/0003-require-skill-gate-for-mergegate-readiness.md\n+++ b/docs/adr/SADR-0003-require-skill-gate-for-mergegate-readiness.md\n@@ -1,3 +1,3 @@\n-# Require Skill Gate For MergeGate Readiness\n+# SADR-0003: Require Skill Gate For MergeGate Readiness\n \n Shiki requires relevant engineering skills to be invoked before execution when their triggers apply. This makes skills part of MergeGate readiness rather than optional prompt style, because Shiki depends on repeatable planning, issue decomposition, TDD, diagnosis, triage, and architecture review before autonomous or parallel agent work can be trusted.\ndiff --git a/docs/adr/0004-use-github-first-with-shiki-mirror.md b/docs/adr/SADR-0004-use-github-first-with-shiki-mirror.md\nindex d0a3a9174dc646a945a9d325954ffaf00c352040..a79c54ae9ee8eed2a0524192b864da4e22a92d3f\n--- a/docs/adr/0004-use-github-first-with-shiki-mirror.md\n+++ b/docs/adr/SADR-0004-use-github-first-with-shiki-mirror.md\n@@ -1,4 +1,4 @@\n-# ADR 0004: Use GitHub First With A Repository-Local Shiki Mirror\n+# SADR-0004: Use GitHub First With A Repository-Local Shiki Mirror\n \n ## Status\n \ndiff --git a/docs/adr/0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md b/docs/adr/SADR-0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\nindex b339baf49b89d9290e79c3d51b36e7b2bde3bd95..c30a0d8b1fde0224d0ac7820e7870dccf721580d\n--- a/docs/adr/0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\n+++ b/docs/adr/SADR-0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\n@@ -1,4 +1,4 @@\n-# ADR 0005: Use Codex As Front And Claude Code Action As The GitHub Runtime\n+# SADR-0005: Use Codex As Front And Claude Code Action As The GitHub Runtime\n \n ## Status\n \ndiff --git a/docs/adr/0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md b/docs/adr/SADR-0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\nindex 4bcae9d1e52f0a37b8e9fbafa5cd14b6ee1993bd..7a38538a6ae9929567f0ade67e7ea6a60d0b3f7e\n--- a/docs/adr/0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\n+++ b/docs/adr/SADR-0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\n@@ -1,4 +1,4 @@\n-# ADR 0006: Separate Codex Implementation, CCA Completion, And MergeGate Authorization\n+# SADR-0006: Separate Codex Implementation, CCA Completion, And MergeGate Authorization\n \n ## Status\n \ndiff --git a/docs/adr/0007-packaging-and-release.md b/docs/adr/SADR-0007-packaging-and-release.md\nindex 79b84ddb97167372b44641e5b7860e400ce80822..023cd57f8011aa6e6d2ebf612b7c35a7a82c00d4\n--- a/docs/adr/0007-packaging-and-release.md\n+++ b/docs/adr/SADR-0007-packaging-and-release.md\n@@ -1,4 +1,4 @@\n-# ADR 0007: Packaging And Release For Shiki\n+# SADR-0007: Packaging And Release For Shiki\n \n ## Status\n \ndiff --git a/docs/adr/0008-use-claude-code-as-default-implementer.md b/docs/adr/SADR-0008-use-claude-code-as-default-implementer.md\nindex dedf4510b7c7799521422f622029563d373db031..a158a8d444d93764ec515a28133f7dde4af23579\n--- a/docs/adr/0008-use-claude-code-as-default-implementer.md\n+++ b/docs/adr/SADR-0008-use-claude-code-as-default-implementer.md\n@@ -1,4 +1,4 @@\n-# ADR 0008: Use Claude Code As The Default Implementer Runtime\n+# SADR-0008: Use Claude Code As The Default Implementer Runtime\n \n ## Status\n \ndiff --git a/docs/adr/0009-requirements-definition-and-spec-freeze.md b/docs/adr/SADR-0009-requirements-definition-and-spec-freeze.md\nindex 29b3f7871bef9203a8b4a831a65c39eb2f506e42..52e7dd69ad0aa4efe0b68c62678abbf7df02aaec\n--- a/docs/adr/0009-requirements-definition-and-spec-freeze.md\n+++ b/docs/adr/SADR-0009-requirements-definition-and-spec-freeze.md\n@@ -1,4 +1,4 @@\n-# ADR 0009: Merge Grill And Planning Into Requirements Definition With Spec Freeze\n+# SADR-0009: Merge Grill And Planning Into Requirements Definition With Spec Freeze\n \n ## Status\n \ndiff --git a/docs/adr/0010-external-ai-guardian-review.md b/docs/adr/SADR-0010-external-ai-guardian-review.md\nindex e6108bbe8bb4e73e0ff4684c8f78001a80438c4e..e690d4eddd270e683d709c9b679c5859e71eeaaa\n--- a/docs/adr/0010-external-ai-guardian-review.md\n+++ b/docs/adr/SADR-0010-external-ai-guardian-review.md\n@@ -1,4 +1,4 @@\n-# ADR 0010: External AI Guardian Review As A First-Class Approval Authority\n+# SADR-0010: External AI Guardian Review As A First-Class Approval Authority\n \n ## Status\n \ndiff --git a/docs/adr/0011-autonomous-quality-gate-evidence.md b/docs/adr/SADR-0011-autonomous-quality-gate-evidence.md\nindex bca0246c5d46e44afbde15486a75eec425fc02f5..9c93eac16d8c3a6d4b11bd7224e3955102cd7db4\n--- a/docs/adr/0011-autonomous-quality-gate-evidence.md\n+++ b/docs/adr/SADR-0011-autonomous-quality-gate-evidence.md\n@@ -1,4 +1,4 @@\n-# ADR 0011: Autonomous Quality-Gate Evidence \u2014 Deterministic Facts And An Independent Verifier\n+# SADR-0011: Autonomous Quality-Gate Evidence \u2014 Deterministic Facts And An Independent Verifier\n \n ## Status\n \ndiff --git a/docs/adr/0012-autonomous-loop-closeout-to-main.md b/docs/adr/SADR-0012-autonomous-loop-closeout-to-main.md\nindex 637fad6dde4989c9c2bf0f81f676d8d29991123c..ec7d85f81030a676bcbba28b43eb365422a36eac\n--- a/docs/adr/0012-autonomous-loop-closeout-to-main.md\n+++ b/docs/adr/SADR-0012-autonomous-loop-closeout-to-main.md\n@@ -1,4 +1,4 @@\n-# 0012 \u2014 Autonomous loop pushes goal completion to main via a closeout PR\n+# SADR-0012 \u2014 Autonomous loop pushes goal completion to main via a closeout PR\n \n - Status: accepted\n - Date: 2026-06-18\ndiff --git a/docs/adr/0013-bootstrap-grants-actions-approve-pull-requests.md b/docs/adr/SADR-0013-bootstrap-grants-actions-approve-pull-requests.md\nindex b6c69d64f1c5ced346b88d6859586715b3b5bd0e..a793e7277d67ab8ea56286fec289ca689825a17d\n--- a/docs/adr/0013-bootstrap-grants-actions-approve-pull-requests.md\n+++ b/docs/adr/SADR-0013-bootstrap-grants-actions-approve-pull-requests.md\n@@ -1,4 +1,4 @@\n-# ADR 0013: Bootstrap Grants GitHub Actions Permission To Create And Approve Pull Requests\n+# SADR-0013: Bootstrap Grants GitHub Actions Permission To Create And Approve Pull Requests\n \n ## Status\n \ndiff --git a/docs/adr/0014-codex-app-external-ai-guardian-ui-adapter.md b/docs/adr/SADR-0014-codex-app-external-ai-guardian-ui-adapter.md\nindex 238db06279334caa341c117362df9271382a7fcd..65f12e9939321e1b3e389a34c822fc1549e5155d\n--- a/docs/adr/0014-codex-app-external-ai-guardian-ui-adapter.md\n+++ b/docs/adr/SADR-0014-codex-app-external-ai-guardian-ui-adapter.md\n@@ -1,4 +1,4 @@\n-# ADR 0014: Use Codex App As The External AI Guardian UI Adapter\n+# SADR-0014: Use Codex App As The External AI Guardian UI Adapter\n \n ## Status\n \ndiff --git a/docs/adr/0015-contract-approval-before-dispatch.md b/docs/adr/SADR-0015-contract-approval-before-dispatch.md\nindex fad6c656741a20a7684ba16080a7db159f63a52f..7bdd760bb3e82eee4e1410f45c783b03b638d456\n--- a/docs/adr/0015-contract-approval-before-dispatch.md\n+++ b/docs/adr/SADR-0015-contract-approval-before-dispatch.md\n@@ -1,4 +1,4 @@\n-# ADR 0015: Approve The Task Contract Before Dispatch, Not The Diff After It\n+# SADR-0015: Approve The Task Contract Before Dispatch, Not The Diff After It\n \n ## Status\n \ndiff --git a/docs/adr/0016-locks-bound-product-paths.md b/docs/adr/SADR-0016-locks-bound-product-paths.md\nindex b5e850f3f1f7f4ef6e7f9d1763a29a51bd300c45..332cc17313498404f41a145291e38447b5de0dc4\n--- a/docs/adr/0016-locks-bound-product-paths.md\n+++ b/docs/adr/SADR-0016-locks-bound-product-paths.md\n@@ -1,4 +1,4 @@\n-# ADR 0016: Locks bound product paths; the Shiki mirror is bound by record identity\n+# SADR-0016: Locks bound product paths; the Shiki mirror is bound by record identity\n \n ## Status\n \ndiff --git a/docs/adr/0017-bookkeeping-closeouts-do-not-inherit-risk.md b/docs/adr/SADR-0017-bookkeeping-closeouts-do-not-inherit-risk.md\nindex 554e3a0d526a72ca067108c5464cc68c3edcf174..83115f69f1b1b48d544c9b0157f377b91cb53abf\n--- a/docs/adr/0017-bookkeeping-closeouts-do-not-inherit-risk.md\n+++ b/docs/adr/SADR-0017-bookkeeping-closeouts-do-not-inherit-risk.md\n@@ -1,4 +1,4 @@\n-# ADR 0017: A Bookkeeping Closeout Does Not Inherit Its Task's Risk\n+# SADR-0017: A Bookkeeping Closeout Does Not Inherit Its Task's Risk\n \n ## Status\n \ndiff --git a/docs/adr/0018-carried-guardian-approval-across-a-proven-base-sync.md b/docs/adr/SADR-0018-carried-guardian-approval-across-a-proven-base-sync.md\nindex 22358562da390ed8b45cd600da08a43d378b4680..38da0fd95102c3496c3219ce2319fce696a2532a\n--- a/docs/adr/0018-carried-guardian-approval-across-a-proven-base-sync.md\n+++ b/docs/adr/SADR-0018-carried-guardian-approval-across-a-proven-base-sync.md\n@@ -1,4 +1,4 @@\n-# ADR 0018: Carried Guardian Approval Across A Proven Base Sync\n+# SADR-0018: Carried Guardian Approval Across A Proven Base Sync\n \n ## Status\n \ndiff --git a/docs/adr/README.md b/docs/adr/README.md\nindex c70e057a4520c36e042ab68c53cd4a21ef709b71..5da43856836e2ce6a150f8127766e2f88355ab4b\n--- a/docs/adr/README.md\n+++ b/docs/adr/README.md\n@@ -1,40 +1,41 @@\n-# Architecture Decision Records\n+# Decision Record Namespaces\n \n-This directory holds Shiki's Architecture Decision Records (ADRs): durable\n-records of hard-to-reverse platform decisions. ADRs are part of the Shiki source\n-of truth alongside GitHub state, the `.shiki/` mirror, and `CONTEXT.md`.\n+This directory holds two independent decision namespaces:\n \n-## How ADRs Work\n+- Shiki Architecture Decision Records (SADRs), named\n+  `SADR-NNNN-<short-slug>.md`, record hard-to-reverse Shiki platform decisions.\n+- Target Architecture Decision Records (ADRs), named\n+  `NNNN-<short-slug>.md`, belong to the target product.\n \n-- New ADRs are added as `docs/adr/NNNN-<short-slug>.md`, where `NNNN` is the next\n-  zero-padded sequence number.\n-- The **canonical list of ADRs is the set of files matching `docs/adr/*.md`** in\n-  this directory. Browse `docs/adr/*.md` for the authoritative, current set.\n-- Use [`template.md`](template.md) as the starting point for a new ADR.\n-- Record an ADR when a decision is hard to reverse, surprising without context,\n-  and a real tradeoff. Do not write an ADR for routine or easily reversible\n-  choices.\n+The same four-digit number may appear once in each namespace. A platform\n+`SADR-NNNN` citation resolves only to `SADR-NNNN-*.md`; a target `ADR NNNN`\n+citation resolves only to `NNNN-*.md`.\n \n-Using the `docs/adr/*.md` glob as the source of truth means other PRs can add an\n-ADR without editing this index file, avoiding merge contention on a\n-hand-maintained enumeration.\n+## How Decision Records Work\n \n-## Current ADRs (snapshot)\n-\n-The list below is a convenience snapshot and may lag behind the directory. When\n-in doubt, list `docs/adr/*.md`.\n+- New Shiki platform decisions use the next zero-padded SADR number.\n+- New target product decisions use the target's own ADR sequence.\n+- The canonical list is the files in `docs/adr/`; no hand-maintained index is\n+  authoritative.\n+- Use [`template.md`](template.md) as the starting point and choose the filename\n+  namespace that owns the decision.\n+- Record a decision when it is hard to reverse, surprising without context, and\n+  a real tradeoff. Do not write a decision record for routine or easily\n+  reversible choices.\n \n-- [0001 \u2014 Keep Shiki As The Platform Name](0001-keep-shiki-as-platform-name.md)\n-- [0002 \u2014 Use AGENTS As The Shared Agent Constitution](0002-use-agents-as-shared-constitution.md)\n-- [0003 \u2014 Require Skill Gate For MergeGate Readiness](0003-require-skill-gate-for-mergegate-readiness.md)\n-- [0004 \u2014 Use GitHub First With A Repository-Local Shiki Mirror](0004-use-github-first-with-shiki-mirror.md)\n-- [0005 \u2014 Use Codex As Front And Claude Code Action As The GitHub Runtime](0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md)\n-- [0006 \u2014 Separate Codex Implementation, CCA Completion, And MergeGate Authorization](0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md)\n+Using the directory as the source of truth lets independent changes add records\n+without contending on an enumerated index.\n \n-## Adding A New ADR\n+## Adding A Shiki SADR\n \n-1. Copy `template.md` to `docs/adr/NNNN-<short-slug>.md`.\n+1. Copy `template.md` to `docs/adr/SADR-NNNN-<short-slug>.md`.\n 2. Fill in the title, status, context, decision, and consequences.\n-3. Set the status to `Proposed`, then `Accepted` once the decision is approved.\n-4. Open a PR following `CONTRIBUTING.md`. You do not need to edit this index for\n-   the ADR to be discoverable via `docs/adr/*.md`.\n+3. Set the status to `Proposed`, then `Accepted` once the proper authority has\n+   approved the decision.\n+4. Open a PR following `CONTRIBUTING.md`.\n+\n+## Adding A Target ADR\n+\n+1. Copy `template.md` to `docs/adr/NNNN-<short-slug>.md` using the target's own\n+   sequence.\n+2. Cite it as `ADR NNNN`; do not prefix its filename with `SADR-`.\ndiff --git a/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md b/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\nnew file mode 100644\nindex 0000000000000000000000000000000000000000..7455ce1f3bed02ddddb851ac21a1d1a648c9aeba\n--- /dev/null\n+++ b/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\n@@ -0,0 +1,55 @@\n+# SADR-0019: Separate Shiki Platform SADRs From Target ADRs\n+\n+## Status\n+\n+Accepted\n+\n+## Context\n+\n+Shiki installs its platform decision records into `docs/adr/` in every target\n+repository. Shiki and its targets previously used the same numeric\n+`NNNN-<slug>.md` namespace. A target that already owned ADRs 0013 through 0018\n+therefore acquired two records for each number during installation, making an\n+`ADR NNNN` citation ambiguous. Renumbering target decisions would rewrite target\n+history, while renumbering Shiki decisions per target would destroy their stable\n+platform identity.\n+\n+Upgrades also need to remove the former Shiki-owned numeric paths without ever\n+mistaking a target-owned ADR for platform content. The install stamp already\n+records exact shipped paths and content digests, so it can prove ownership only\n+when both still match.\n+\n+## Decision\n+\n+Shiki platform decisions use the `SADR` namespace:\n+\n+- records are named `docs/adr/SADR-NNNN-<slug>.md`;\n+- live platform citations use `SADR-NNNN`;\n+- target product decisions retain `docs/adr/NNNN-<slug>.md` and `ADR NNNN`;\n+- the reference validator resolves the two namespaces independently, so the same\n+  four-digit number may exist once in each namespace.\n+\n+Before a forced upgrade writes anything, the installer inventories the exact\n+former numeric Shiki ADR paths. It may delete a legacy path only when the\n+existing install stamp contains that exact path and its recorded SHA-256 digest\n+matches the file currently on disk. All ownership blockers are reported\n+together and abort the upgrade before its first write. A non-forced install\n+never performs this cleanup.\n+\n+Historical `.shiki/` evidence remains unchanged because it records the namespace\n+that existed when that evidence was produced.\n+\n+## Consequences\n+\n+- Targets may use any ADR number without colliding with Shiki's platform SADRs.\n+- Fresh installs preserve target ADR bytes and install a stable SADR set.\n+- Forced upgrades fail closed when legacy ownership cannot be proven, and a\n+  successful upgrade refreshes the stamp without removed legacy paths.\n+- Current governance, packaging, workflow, prompt, script, and skill references\n+  must move with the platform records.\n+\n+## Alternatives Considered\n+\n+- Collision-only refusal leaves affected targets permanently unupgradable.\n+- Renumbering target ADRs mutates target-owned history.\n+- Per-target Shiki renumbering makes platform citations non-portable.\ndiff --git a/docs/adr/template.md b/docs/adr/template.md\nindex a92a97daddd0b00ab9dd626cc8d86942da2eafed..17dd79175b5fee2105935557f691b40cf9492942\n--- a/docs/adr/template.md\n+++ b/docs/adr/template.md\n@@ -1,14 +1,14 @@\n-# NNNN: <Short Decision Title>\n+# <SADR-NNNN or NNNN>: <Short Decision Title>\n \n ## Status\n \n-Proposed | Accepted | Superseded by ADR-NNNN\n+Proposed | Accepted | Superseded by SADR-NNNN or target ADR NNNN\n \n ## Context\n \n What is the situation, constraint, or force that requires a decision? Reference\n-relevant domain terms (`CONTEXT.md`), prior ADRs, and code reality. State the\n-problem, not the solution.\n+relevant domain terms (`CONTEXT.md`), prior Shiki SADRs or target ADRs, and code\n+reality. State the problem, not the solution.\n \n ## Decision\n \ndiff --git a/docs/agents/domain.md b/docs/agents/domain.md\nindex 258da8ea81e87ac0a6b0b5b5e5c2fde980eaabb7..67aaa6f4b7d0a5aa4d495ce6279073333f7798cb\n--- a/docs/agents/domain.md\n+++ b/docs/agents/domain.md\n@@ -5,7 +5,9 @@\n ## Layout\n \n - `CONTEXT.md` defines Shiki language. Keep it implementation-free.\n-- `docs/adr/` records hard-to-reverse decisions.\n+- `docs/adr/SADR-NNNN-*.md` records Shiki platform decisions.\n+- `docs/adr/NNNN-*.md` records target product decisions without sharing the\n+  SADR namespace.\n - `AGENTS.md` is the runtime-neutral agent constitution.\n - `CLAUDE.md` is the Claude Code wrapper around `AGENTS.md`.\n - `.shiki/` is the Target Repository mirror for Goals, PRDs, plans, Task DAGs, contracts, locks, worktree records, ledger entries, CCA verdicts, repair packets, reports, and handoffs.\ndiff --git a/docs/agents/implementation-policy.md b/docs/agents/implementation-policy.md\nindex c754144a9fb5bf4a38aa82a60e1670c59e626b44..e90d40fe21880b9a09719c5ecfe6bde7284eb3ce\n--- a/docs/agents/implementation-policy.md\n+++ b/docs/agents/implementation-policy.md\n@@ -31,6 +31,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n ## Non-Negotiables\n \n 1. **Planning precedes implementation.** Non-trivial Goals start with `grill-with-docs` and Context & Impact.\ndiff --git a/scripts/shiki_installer.py b/scripts/shiki_installer.py\nindex be064ab7a29ff6836f7f7a1d70500a43e0d11d7b..d3e3247edbf3e4be287a4667176d86f6500f8dc5\n--- a/scripts/shiki_installer.py\n+++ b/scripts/shiki_installer.py\n@@ -118,6 +118,7 @@\n # what lets a target carry it without a governance-schema change.\n INSTALL_STAMP_PATH = \".shiki/install-stamp.json\"\n INSTALL_STAMP_VERSION = 1\n+LEGACY_SHIKI_SADR_MAX = 18\n \n # Under --force the shipped surface splits three ways, so nothing is lost and\n # nothing is silently stale.\n@@ -237,9 +238,98 @@\n         )\n \n \n+def legacy_shiki_adr_paths() -> tuple[str, ...]:\n+    \"\"\"Former numeric paths for Shiki SADR-0001 through SADR-0018.\n+\n+    The authoritative slugs come from the current SADR filenames. SADR-0019 is\n+    the namespace decision itself and never shipped under a numeric legacy path.\n+    \"\"\"\n+    paths: list[str] = []\n+    for sadr in sorted((ROOT / \"docs\" / \"adr\").glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\")):\n+        number = sadr.name.split(\"-\", 2)[1]\n+        if int(number) <= LEGACY_SHIKI_SADR_MAX:\n+            paths.append(f\"docs/adr/{sadr.name.removeprefix('SADR-')}\")\n+    return tuple(paths)\n+\n+\n+def preflight_legacy_sadr_cleanup(target: Path, *, force: bool) -> tuple[str, ...]:\n+    \"\"\"Authorize exact legacy Shiki ADR removals before the first write.\n+\n+    A non-force install never removes existing files. A forced install needs no\n+    stamp when no legacy Shiki path is present. When legacy paths do exist, each\n+    must be named in a readable install stamp and its current bytes must match\n+    the recorded digest.\n+    \"\"\"\n+    if not force:\n+        return ()\n+    present = tuple(\n+        relative\n+        for relative in legacy_shiki_adr_paths()\n+        if (target / relative).exists() or (target / relative).is_symlink()\n+    )\n+    if not present:\n+        return ()\n+\n+    stamp_path = target / INSTALL_STAMP_PATH\n+    try:\n+        raw = stamp_path.read_text(encoding=\"utf-8\")\n+        stamp = json.loads(raw)\n+    except FileNotFoundError:\n+        detail = \"install stamp is absent\"\n+        blockers = [f\"{relative}: {detail}\" for relative in present]\n+    except (OSError, UnicodeDecodeError) as error:\n+        detail = f\"install stamp is unreadable ({error})\"\n+        blockers = [f\"{relative}: {detail}\" for relative in present]\n+    except json.JSONDecodeError as error:\n+        detail = f\"install stamp is malformed ({error})\"\n+        blockers = [f\"{relative}: {detail}\" for relative in present]\n+    else:\n+        if not isinstance(stamp, dict):\n+            blockers = [\n+                f\"{relative}: install stamp is malformed (expected an object)\"\n+                for relative in present\n+            ]\n+        else:\n+            digests = stamp.get(\"digests\")\n+            blockers = []\n+            if not isinstance(digests, dict):\n+                blockers.extend(\n+                    f\"{relative}: install stamp has no digest for this path\"\n+                    for relative in present\n+                )\n+            else:\n+                for relative in present:\n+                    expected = digests.get(relative)\n+                    if not isinstance(expected, str) or not expected:\n+                        blockers.append(\n+                            f\"{relative}: install stamp has no digest for this path\"\n+                        )\n+                        continue\n+                    try:\n+                        actual = _sha256_file(target / relative)\n+                    except OSError as error:\n+                        blockers.append(\n+                            f\"{relative}: legacy path is unreadable ({error})\"\n+                        )\n+                        continue\n+                    if actual != expected:\n+                        blockers.append(\n+                            f\"{relative}: digest mismatch (stamp {expected}, current {actual})\"\n+                        )\n+\n+    if blockers:\n+        joined = \"\\n\".join(f\"- {blocker}\" for blocker in blockers)\n+        raise ShikiError(\n+            \"legacy Shiki ADR cleanup is not authorized:\\n\"\n+            f\"{joined}\\nNo files were written.\"\n+        )\n+    return present\n+\n+\n def install_template(target: Path, *, force: bool, validate: bool) -> list[NewFileNote]:\n     # Preconditions first: never rewrite the tree of a target that cannot be\n     # upgraded cleanly (see refuse_pending_migrations).\n+    legacy_cleanup = preflight_legacy_sadr_cleanup(target, force=force)\n     refuse_pending_migrations(target)\n \n     # Resolve the target's required CODEOWNERS owner once from its\n@@ -250,6 +340,10 @@\n     # verbatim. See ``codeowners_required_owner``.\n     codeowners_owner = codeowners_required_owner(target)\n \n+    for relative in legacy_cleanup:\n+        (target / relative).unlink()\n+        info(f\"removed stamped legacy Shiki ADR: {target / relative}\")\n+\n     notes: list[NewFileNote] = []\n     for relative in TEMPLATE_PATHS:\n         source = ROOT / relative\ndiff --git a/tests/test_adr_references.py b/tests/test_adr_references.py\nindex 51706329c797d5dd9b47e082a333fa83b261d482..91ab76b724cb973435c45d53cab63793942267e9\n--- a/tests/test_adr_references.py\n+++ b/tests/test_adr_references.py\n@@ -1,4 +1,4 @@\n-\"\"\"Every ``ADR ####`` citation must resolve to exactly one decision record.\n+\"\"\"Every target ADR and Shiki SADR citation resolves in its own namespace.\n \n Background. ``scripts/mergegate_check.py`` justifies ``is_bookkeeping_closeout``\n with \"ADR 0017\" nine times and ``scripts/guardian_approval_signal.py`` cites\n@@ -11,11 +11,12 @@\n and the directory drifted with no signal.\n \n This is the same unbound-pair shape behind other silent failures: a reference\n-and its target with nothing asserting they agree. This test binds them. It scans\n-every ``scripts/*.py`` (plus ``docs/**/*.md`` and the root ``*.md`` governance\n-files, which are cheap) for the ``ADR ####`` pattern and asserts each cited\n-number resolves to exactly one ``docs/adr/####-*.md``. The converse is\n-deliberately NOT required: an ADR may exist without being cited.\n+and its target with nothing asserting they agree. This test binds them. Target\n+``ADR NNNN`` citations resolve only to ``docs/adr/NNNN-*.md``; Shiki\n+``SADR-NNNN`` citations resolve only to ``docs/adr/SADR-NNNN-*.md``. The scan\n+covers current governance docs, scripts, workflows, prompts, packaging metadata,\n+and repo-local Codex guidance while deliberately excluding historical\n+``.shiki`` evidence. The converse is not required: a record may be uncited.\n \n The suite is dependency free: standard-library ``unittest`` only.\n \"\"\"\n@@ -31,17 +32,18 @@\n \n REPO_ROOT = Path(__file__).resolve().parents[1]\n \n-# \"ADR 0016\" -- a four-digit citation. The trailing \\b keeps a five-digit token\n-# from being read as a truncated four-digit number.\n-_CITATION = re.compile(r\"\\bADR (\\d{4})\\b\")\n-# \"0016-locks-bound-product-paths.md\" -- the leading four digits name the record.\n-_ADR_FILENAME = re.compile(r\"^(\\d{4})-.*\\.md$\")\n+# ``ADR 0016`` / ``SADR-0016`` -- a namespace plus four-digit citation. The\n+# trailing ``\\b`` keeps a five-digit token from being read as a truncated one.\n+_CITATION = re.compile(r\"\\b(S?ADR)[ -](\\d{4})\\b\")\n+# Target ADRs retain the numeric filename; Shiki platform decisions use SADR.\n+_DECISION_FILENAME = re.compile(r\"^(?:(SADR)-)?(\\d{4})-.*\\.md$\")\n \n \n class Violation(NamedTuple):\n     \"\"\"A citation that does not resolve to exactly one decision record.\"\"\"\n \n     citing_file: str  # repo-relative path of the file that cites the ADR\n+    namespace: str  # \"ADR\" for target decisions; \"SADR\" for Shiki decisions\n     number: str  # the cited four-digit number, e.g. \"0016\"\n     line: int  # 1-based line of the citation\n     reason: str  # \"missing\" (zero matches) or \"ambiguous\" (two or more)\n@@ -49,44 +51,59 @@\n \n     @property\n     def expected_path(self) -> str:\n-        return f\"docs/adr/{self.number}-*.md\"\n+        prefix = \"SADR-\" if self.namespace == \"SADR\" else \"\"\n+        return f\"docs/adr/{prefix}{self.number}-*.md\"\n \n     def describe(self) -> str:\n         \"\"\"Name the citing file, the cited number, and the missing path.\"\"\"\n         if self.reason == \"missing\":\n             return (\n-                f\"{self.citing_file}:{self.line} cites ADR {self.number} \"\n+                f\"{self.citing_file}:{self.line} cites {self.namespace} {self.number} \"\n                 f\"but no document matches {self.expected_path}\"\n             )\n         joined = \", \".join(self.matches)\n         return (\n-            f\"{self.citing_file}:{self.line} cites ADR {self.number} which \"\n+            f\"{self.citing_file}:{self.line} cites {self.namespace} {self.number} which \"\n             f\"resolves to {len(self.matches)} documents (expected exactly one \"\n             f\"at {self.expected_path}): {joined}\"\n         )\n \n \n-def _adr_index(root: Path) -> dict[str, list[Path]]:\n-    \"\"\"Map every four-digit ADR number to the record files that claim it.\"\"\"\n-    index: dict[str, list[Path]] = {}\n+def _adr_index(root: Path) -> dict[tuple[str, str], list[Path]]:\n+    \"\"\"Map each decision namespace and number to the files that claim it.\"\"\"\n+    index: dict[tuple[str, str], list[Path]] = {}\n     adr_dir = root / \"docs\" / \"adr\"\n     if adr_dir.is_dir():\n         for path in sorted(adr_dir.glob(\"*.md\")):\n-            match = _ADR_FILENAME.match(path.name)\n+            match = _DECISION_FILENAME.match(path.name)\n             if match:\n-                index.setdefault(match.group(1), []).append(path)\n+                namespace = \"SADR\" if match.group(1) else \"ADR\"\n+                index.setdefault((namespace, match.group(2)), []).append(path)\n     return index\n \n \n def _citing_files(root: Path, include_docs: bool = True) -> list[Path]:\n-    \"\"\"Files scanned for citations: scripts/*.py, and cheaply the markdown.\"\"\"\n+    \"\"\"Current, shipped text surfaces scanned for decision citations.\"\"\"\n     files = sorted((root / \"scripts\").glob(\"*.py\"))\n+    files += sorted((root / \"scripts\").glob(\"*.sh\"))\n     if include_docs:\n         docs = root / \"docs\"\n         if docs.is_dir():\n             files += sorted(docs.rglob(\"*.md\"))\n         files += sorted(root.glob(\"*.md\"))\n-    return files\n+        pyproject = root / \"pyproject.toml\"\n+        if pyproject.is_file():\n+            files.append(pyproject)\n+        for relative in (\".github\", \".codex\"):\n+            surface = root / relative\n+            if surface.is_dir():\n+                files += sorted(\n+                    path\n+                    for path in surface.rglob(\"*\")\n+                    if path.is_file()\n+                    and path.suffix in {\".md\", \".py\", \".sh\", \".toml\", \".yaml\", \".yml\"}\n+                )\n+    return sorted(set(files))\n \n \n def find_adr_reference_violations(\n@@ -94,10 +111,9 @@\n ) -> list[Violation]:\n     \"\"\"Return every ``ADR ####`` citation under ``root`` that fails to resolve.\n \n-    A citation resolves when exactly one ``docs/adr/####-*.md`` matches its\n-    number. Zero matches is a \"missing\" violation; two or more is \"ambiguous\".\n-    Records that are never cited produce nothing -- the converse is not\n-    required.\n+    A citation resolves when exactly one record in the same namespace matches\n+    its number. Zero matches is a \"missing\" violation; two or more is\n+    \"ambiguous\". Records that are never cited produce nothing.\n     \"\"\"\n     root = Path(root)\n     index = _adr_index(root)\n@@ -110,13 +126,15 @@\n         rel = path.relative_to(root).as_posix()\n         for lineno, line in enumerate(text.splitlines(), start=1):\n             for match in _CITATION.finditer(line):\n-                number = match.group(1)\n-                found = index.get(number, [])\n+                namespace, number = match.groups()\n+                found = index.get((namespace, number), [])\n                 if len(found) == 1:\n                     continue\n                 reason = \"missing\" if not found else \"ambiguous\"\n                 rels = [p.relative_to(root).as_posix() for p in found]\n-                violations.append(Violation(rel, number, lineno, reason, rels))\n+                violations.append(\n+                    Violation(rel, namespace, number, lineno, reason, rels)\n+                )\n     return violations\n \n \n@@ -129,8 +147,8 @@\n         self.assertEqual(\n             violations,\n             [],\n-            \"Every 'ADR ####' citation must resolve to exactly one \"\n-            f\"docs/adr/####-*.md:\\n{detail}\",\n+            \"Every ADR/SADR citation must resolve exactly once in its own \"\n+            f\"docs/adr namespace:\\n{detail}\",\n         )\n \n     def test_scan_actually_reads_scripts(self) -> None:\n@@ -146,8 +164,21 @@\n         # The two records this Goal carried onto the default branch, and which\n         # the scripts cite most, must be present.\n         index = _adr_index(REPO_ROOT)\n-        self.assertIn(\"0016\", index)\n-        self.assertIn(\"0017\", index)\n+        self.assertIn((\"SADR\", \"0016\"), index)\n+        self.assertIn((\"SADR\", \"0017\"), index)\n+\n+    def test_platform_tree_has_only_sadr_0001_through_0019(self) -> None:\n+        adr_dir = REPO_ROOT / \"docs\" / \"adr\"\n+        sadrs = sorted(adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\"))\n+        self.assertEqual(\n+            {path.name.split(\"-\", 2)[1] for path in sadrs},\n+            {f\"{number:04d}\" for number in range(1, 20)},\n+        )\n+        self.assertEqual(\n+            list(adr_dir.glob(\"[0-9][0-9][0-9][0-9]-*.md\")),\n+            [],\n+            \"the Shiki platform tree must not retain legacy numeric SADR paths\",\n+        )\n \n \n class FixtureAdrReferences(unittest.TestCase):\n@@ -180,6 +211,26 @@\n         self.assertIn(\"9991\", message)  # cited number\n         self.assertIn(\"docs/adr/9991-*.md\", message)  # missing path\n \n+    def test_target_adr_does_not_satisfy_missing_platform_sadr(self) -> None:\n+        root = self._make_repo(\n+            adr_names=[\"0042-target-decision.md\"],\n+            script_body='\"\"\"Target ADR 0042 exists, but platform SADR-0042 does not.\"\"\"\\n',\n+        )\n+        violations = find_adr_reference_violations(root, include_docs=False)\n+        self.assertEqual(len(violations), 1)\n+        self.assertEqual(violations[0].namespace, \"SADR\")\n+        self.assertEqual(violations[0].number, \"0042\")\n+        self.assertEqual(violations[0].expected_path, \"docs/adr/SADR-0042-*.md\")\n+\n+    def test_target_adr_and_platform_sadr_may_reuse_a_number(self) -> None:\n+        root = self._make_repo(\n+            adr_names=[\"0042-target-decision.md\", \"SADR-0042-platform-decision.md\"],\n+            script_body='\"\"\"Target ADR 0042 and platform SADR-0042 are distinct.\"\"\"\\n',\n+        )\n+        self.assertEqual(\n+            find_adr_reference_violations(root, include_docs=False), []\n+        )\n+\n     def test_uncited_adr_is_allowed(self) -> None:\n         # An ADR exists but nothing cites it -> not a violation. The converse\n         # (every ADR must be cited) is explicitly NOT required.\ndiff --git a/tests/test_sadr_install_migration.py b/tests/test_sadr_install_migration.py\nnew file mode 100644\nindex 0000000000000000000000000000000000000000..5a442fa5e4968ade178dcdedc3c63b54dba580a2\n--- /dev/null\n+++ b/tests/test_sadr_install_migration.py\n@@ -0,0 +1,224 @@\n+\"\"\"SADR installation and collision-safe legacy upgrade behavior.\"\"\"\n+\n+from __future__ import annotations\n+\n+import contextlib\n+import hashlib\n+import io\n+import json\n+import shutil\n+import tempfile\n+import unittest\n+from pathlib import Path\n+\n+import shiki_test_support  # noqa: F401  (path bootstrap)\n+\n+import shiki_installer\n+from tests.test_adr_references import find_adr_reference_violations\n+\n+\n+PRODUCT_NUMBERS = tuple(range(13, 19))\n+\n+\n+def _quiet_install(target: Path, *, force: bool, validate: bool) -> None:\n+    with contextlib.redirect_stdout(io.StringIO()):\n+        shiki_installer.install_template(target, force=force, validate=validate)\n+\n+\n+def _sha256(path: Path) -> str:\n+    return hashlib.sha256(path.read_bytes()).hexdigest()\n+\n+\n+def _legacy_sadr_paths() -> tuple[str, ...]:\n+    return tuple(\n+        f\"docs/adr/{path.name.removeprefix('SADR-')}\"\n+        for path in sorted((shiki_installer.ROOT / \"docs\" / \"adr\").glob(\"SADR-*.md\"))\n+        if path.name != \"SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\"\n+    )\n+\n+\n+def _create_legacy_upgrade_fixture(\n+    target: Path,\n+) -> tuple[dict[str, bytes], tuple[str, ...]]:\n+    adr_dir = target / \"docs\" / \"adr\"\n+    adr_dir.mkdir(parents=True)\n+    product_bytes = {\n+        f\"{number:04d}-cubeshare-product-decision.md\": (\n+            f\"# CubeShare ADR {number:04d}\\n\\nDo not rewrite {number}.\\n\"\n+        ).encode()\n+        for number in PRODUCT_NUMBERS\n+    }\n+    for name, content in product_bytes.items():\n+        (adr_dir / name).write_bytes(content)\n+\n+    legacy_paths = _legacy_sadr_paths()\n+    for relative in legacy_paths:\n+        (target / relative).write_bytes(f\"legacy Shiki bytes for {relative}\\n\".encode())\n+\n+    stamp_path = target / shiki_installer.INSTALL_STAMP_PATH\n+    stamp_path.parent.mkdir(parents=True, exist_ok=True)\n+    stamp = {\n+        \"version\": shiki_installer.INSTALL_STAMP_VERSION,\n+        \"platform_commit\": None,\n+        \"installed_at\": \"2000-01-01T00:00:00+00:00\",\n+        \"digests\": {\n+            relative: _sha256(target / relative) for relative in legacy_paths\n+        },\n+    }\n+    stamp_path.write_text(\n+        json.dumps(stamp, indent=2, sort_keys=True) + \"\\n\", encoding=\"utf-8\"\n+    )\n+    return product_bytes, legacy_paths\n+\n+\n+def _tree_snapshot(root: Path) -> dict[str, tuple[str, bytes | str]]:\n+    \"\"\"Recursive byte/shape snapshot used to prove preflight is write-free.\"\"\"\n+    snapshot: dict[str, tuple[str, bytes | str]] = {}\n+    for path in sorted(root.rglob(\"*\")):\n+        relative = path.relative_to(root).as_posix()\n+        if path.is_symlink():\n+            snapshot[relative] = (\"symlink\", path.readlink().as_posix())\n+        elif path.is_dir():\n+            snapshot[relative] = (\"directory\", \"\")\n+        else:\n+            snapshot[relative] = (\"file\", path.read_bytes())\n+    return snapshot\n+\n+\n+class FreshSadrInstallTests(unittest.TestCase):\n+    def setUp(self) -> None:\n+        self.tmp = Path(tempfile.mkdtemp(prefix=\"shiki-sadr-fresh-\"))\n+        self.addCleanup(lambda: shutil.rmtree(self.tmp, ignore_errors=True))\n+        self.target = self.tmp / \"target\"\n+        self.adr_dir = self.target / \"docs\" / \"adr\"\n+        self.adr_dir.mkdir(parents=True)\n+        self.product_bytes = {\n+            f\"{number:04d}-product-decision.md\": (\n+                f\"# Product ADR {number:04d}\\n\\nTarget-owned bytes {number}.\\n\"\n+            ).encode()\n+            for number in PRODUCT_NUMBERS\n+        }\n+        for name, content in self.product_bytes.items():\n+            (self.adr_dir / name).write_bytes(content)\n+\n+    def test_fresh_install_preserves_product_adrs_and_installs_all_sadrs(self) -> None:\n+        _quiet_install(self.target, force=False, validate=True)\n+\n+        for name, expected in self.product_bytes.items():\n+            self.assertEqual((self.adr_dir / name).read_bytes(), expected)\n+\n+        sadr_numbers = {\n+            path.name.split(\"-\", 2)[1]\n+            for path in self.adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\")\n+        }\n+        self.assertEqual(sadr_numbers, {f\"{number:04d}\" for number in range(1, 20)})\n+\n+        numeric_records = {\n+            path.name\n+            for path in self.adr_dir.glob(\"[0-9][0-9][0-9][0-9]-*.md\")\n+        }\n+        self.assertEqual(numeric_records, set(self.product_bytes))\n+        self.assertEqual(find_adr_reference_violations(self.target), [])\n+\n+\n+class LegacySadrUpgradeTests(unittest.TestCase):\n+    def setUp(self) -> None:\n+        self.tmp = Path(tempfile.mkdtemp(prefix=\"shiki-sadr-upgrade-\"))\n+        self.addCleanup(lambda: shutil.rmtree(self.tmp, ignore_errors=True))\n+        self.target = self.tmp / \"target\"\n+        self.adr_dir = self.target / \"docs\" / \"adr\"\n+        self.product_bytes, self.legacy_paths = _create_legacy_upgrade_fixture(\n+            self.target\n+        )\n+\n+    def test_force_upgrade_deletes_only_stamped_legacy_sadrs(self) -> None:\n+        _quiet_install(self.target, force=True, validate=True)\n+\n+        for relative in self.legacy_paths:\n+            self.assertFalse((self.target / relative).exists(), relative)\n+        for name, expected in self.product_bytes.items():\n+            self.assertEqual((self.adr_dir / name).read_bytes(), expected)\n+\n+        sadrs = list(self.adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\"))\n+        self.assertEqual(len(sadrs), 19)\n+        refreshed = json.loads(\n+            (self.target / shiki_installer.INSTALL_STAMP_PATH).read_text(encoding=\"utf-8\")\n+        )\n+        for relative in self.legacy_paths:\n+            self.assertNotIn(relative, refreshed[\"digests\"])\n+        for path in sadrs:\n+            relative = path.relative_to(self.target).as_posix()\n+            self.assertEqual(refreshed[\"digests\"][relative], _sha256(path))\n+\n+    def test_non_force_install_never_deletes_legacy_files(self) -> None:\n+        before = {\n+            relative: (self.target / relative).read_bytes()\n+            for relative in self.legacy_paths\n+        }\n+        _quiet_install(self.target, force=False, validate=False)\n+        for relative, expected in before.items():\n+            self.assertEqual((self.target / relative).read_bytes(), expected)\n+\n+\n+class LegacyCleanupPreflightTests(unittest.TestCase):\n+    def setUp(self) -> None:\n+        self.tmp = Path(tempfile.mkdtemp(prefix=\"shiki-sadr-preflight-\"))\n+        self.addCleanup(lambda: shutil.rmtree(self.tmp, ignore_errors=True))\n+\n+    def test_all_ownership_blockers_abort_before_any_write(self) -> None:\n+        cases = (\n+            (\"absent\", \"install stamp is absent\"),\n+            (\"unreadable\", \"install stamp is unreadable\"),\n+            (\"malformed\", \"install stamp is malformed\"),\n+            (\"missing-digest\", \"install stamp has no digest\"),\n+            (\"digest-mismatch\", \"digest mismatch\"),\n+        )\n+        for name, expected_reason in cases:\n+            with self.subTest(name=name):\n+                target = self.tmp / name\n+                _, legacy_paths = _create_legacy_upgrade_fixture(target)\n+                stamp_path = target / shiki_installer.INSTALL_STAMP_PATH\n+\n+                if name == \"absent\":\n+                    stamp_path.unlink()\n+                elif name == \"unreadable\":\n+                    stamp_path.unlink()\n+                    stamp_path.mkdir()\n+                elif name == \"malformed\":\n+                    stamp_path.write_text(\"{not-json\\n\", encoding=\"utf-8\")\n+                else:\n+                    stamp = json.loads(stamp_path.read_text(encoding=\"utf-8\"))\n+                    if name == \"missing-digest\":\n+                        stamp[\"digests\"] = {}\n+                    else:\n+                        stamp[\"digests\"] = {\n+                            relative: \"deadbeef\" for relative in legacy_paths\n+                        }\n+                    stamp_path.write_text(\n+                        json.dumps(stamp, indent=2, sort_keys=True) + \"\\n\",\n+                        encoding=\"utf-8\",\n+                    )\n+\n+                before = _tree_snapshot(target)\n+                with self.assertRaises(shiki_installer.ShikiError) as raised:\n+                    _quiet_install(target, force=True, validate=False)\n+                after = _tree_snapshot(target)\n+\n+                self.assertEqual(before, after, f\"{name} preflight changed the tree\")\n+                message = str(raised.exception)\n+                self.assertIn(expected_reason, message)\n+                self.assertIn(\"No files were written\", message)\n+                for relative in legacy_paths:\n+                    self.assertIn(relative, message)\n+\n+    def test_absent_stamp_is_allowed_when_no_legacy_path_exists(self) -> None:\n+        target = self.tmp / \"no-legacy\"\n+        target.mkdir()\n+        _quiet_install(target, force=True, validate=False)\n+        self.assertTrue(\n+            (target / \"docs\" / \"adr\" / \"SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\").is_file()\n+        )\n+\n+\n+if __name__ == \"__main__\":\n+    unittest.main()\n\nexec\n/bin/zsh -lc \"rg -n '\\\\bADRs?\\\\b|\\\\ban SADR\\\\b' docs/adr/SADR-*.md scripts docs/agents AGENTS.md CLAUDE.md CONTEXT.md CONTRIBUTING.md SYSTEM_PROMPT.md .github .codex pyproject.toml --glob '\"'!.shiki/**'\"' | sed -n '1,320p'\" in /private/tmp/shiki-adr-collision.pa0KwY/.worktrees/shiki-t-20260807t073307221254z-2d22d654-introduc\n succeeded in 0ms:\ndocs/adr/SADR-0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md:29:- API-key based automation remains possible only as an explicit target-repo extension with its own ADR.\ndocs/adr/SADR-0015-contract-approval-before-dispatch.md:84:exactly what this ADR forbids.\ndocs/adr/SADR-0015-contract-approval-before-dispatch.md:119:and the MergeGate policy check never runs \u2014 the gate this ADR changes would never\ndocs/adr/SADR-0015-contract-approval-before-dispatch.md:149:Bootstrap costs, which apply only while this ADR is being implemented. The tasks\ndocs/adr/SADR-0015-contract-approval-before-dispatch.md:213:workflow to a diff-time Guardian decision \u2014 the exact behavior this ADR removes.\ndocs/adr/SADR-0017-bookkeeping-closeouts-do-not-inherit-risk.md:59:   it to anything other than the branch it is really on. The first draft of this ADR\nSYSTEM_PROMPT.md:34:product decisions retain `NNNN-*.md` and the `ADR` namespace.\nSYSTEM_PROMPT.md:54:Do not assume `openai/codex-action`, `OPENAI_API_KEY`, or API-key based Codex automation in the default Shiki path. API-key runners are explicit target-repo extensions and require their own ADR.\nSYSTEM_PROMPT.md:61:2. **grill-with-docs**: challenge the plan against `CONTEXT.md`, ADRs, code reality, terminology, boundaries, and edge scenarios. Ask one question at a time when the operator is available. If code can answer a question, inspect code instead of asking.\nSYSTEM_PROMPT.md:81:2. Read `CONTEXT.md` and relevant ADRs.\nSYSTEM_PROMPT.md:109:- Relevant documents, ADRs, domain terms, and prior decisions.\nSYSTEM_PROMPT.md:158:- `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant ADRs.\nSYSTEM_PROMPT.md:220:When the architecture gate triggers, run `grill-with-docs` or `improve-codebase-architecture` as appropriate and record the decision in an ADR if it is hard to reverse or likely to be re-litigated.\nSYSTEM_PROMPT.md:229:- `grill-with-docs`: default for non-trivial Goals before PRD/issues; required for ambiguous plans, terminology, boundaries, ADR-worthy tradeoffs, or design-tree decisions.\ndocs/adr/SADR-0016-locks-bound-product-paths.md:156:This ADR **clarifies something SADR-0004 left unspecified**. SADR-0004 decides that\ndocs/adr/SADR-0016-locks-bound-product-paths.md:162:boundaries remain the coordination surface. What this ADR settles is the question\ndocs/adr/SADR-0016-locks-bound-product-paths.md:177:ADR owns what a lock means and who may write the mirror. The one ordering\ndocs/adr/SADR-0016-locks-bound-product-paths.md:202:what this ADR decides.\nCONTRIBUTING.md:20:   domain language, ADRs, and code reality before writing a PRD.\nCONTRIBUTING.md:97:  GitHub, `.shiki/`, `CONTEXT.md`, or an ADR.\nCONTRIBUTING.md:106:record durable decisions in an ADR when needed (see `docs/adr/`).\nCONTEXT.md:28:A planning skill that challenges a plan against domain language, ADRs, code reality, and concrete edge scenarios. It resolves design-tree questions before PRD/issues. It runs inside Requirements Definition.\ndocs/adr/SADR-0010-external-ai-guardian-review.md:11:approval required for high/critical-risk merges (guardian-policy.json, ADR\ndocs/adr/SADR-0010-external-ai-guardian-review.md:92:  authority \u2014 the exact failure this ADR corrects.\ndocs/adr/SADR-0013-bootstrap-grants-actions-approve-pull-requests.md:34:what default token scope \u2014 is an ADR-worthy decision, not an implementation\ndocs/adr/SADR-0008-use-claude-code-as-default-implementer.md:63:(still requires its own ADR per SADR-0005).\nAGENTS.md:25:Do not assume `openai/codex-action`, `OPENAI_API_KEY`, or API-key based Codex automation in the default Shiki path. API-key runners are explicit target-repo extensions and require their own ADR.\nAGENTS.md:48:`SADR-NNNN-*.md`; target product decisions retain `NNNN-*.md` and the `ADR`\nAGENTS.md:54:Conversation state is not durable truth. If a decision matters, put it in GitHub, `.shiki/`, `CONTEXT.md`, or an ADR.\nAGENTS.md:82:2. **grill-with-docs**: challenge the plan against domain docs, ADRs, code reality, terminology, and edge scenarios.\nAGENTS.md:83:3. **Context & Impact**: find relevant docs, ADRs, code, dependencies, owners, locks, and verification surfaces.\nAGENTS.md:174:- Links to relevant domain terms and ADRs.\nAGENTS.md:200:- Relevant documents, domain terms, ADRs, and past decisions.\nAGENTS.md:217:| `grill-with-docs` | Default for non-trivial Goals before PRD/issues; always required when plans, terminology, boundaries, tradeoffs, or ADR-worthy decisions are ambiguous. |\nAGENTS.md:355:When the architecture gate triggers, run `grill-with-docs` or `improve-codebase-architecture` as appropriate and record durable decisions in an ADR when needed.\nCLAUDE.md:18:product decisions retain `NNNN-*.md` and the `ADR` namespace.\nCLAUDE.md:63:2. Read `CONTEXT.md` and relevant ADRs.\nCLAUDE.md:80:- Challenge hard-to-reverse tradeoffs against ADRs.\nCLAUDE.md:100:- Offer ADRs only when the decision is hard to reverse, surprising without context, and a real tradeoff.\nCLAUDE.md:109:- Include implementation decisions, testing decisions, out-of-scope items, and relevant ADR/domain links.\nCLAUDE.md:127:- Relevant docs, ADRs, modules, and tests.\nCLAUDE.md:158:- Read `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant ADRs, PRD, task issue, PR body, diff, checks, reviews, labels, and `.shiki/` state.\ndocs/adr/SADR-0018-carried-guardian-approval-across-a-proven-base-sync.md:37:feature was invisible in CI. This ADR records the decision the two of them\ndocs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md:1:# SADR-0019: Separate Shiki Platform SADRs From Target ADRs\ndocs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md:11:`NNNN-<slug>.md` namespace. A target that already owned ADRs 0013 through 0018\ndocs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md:13:`ADR NNNN` citation ambiguous. Renumbering target decisions would rewrite target\ndocs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md:18:mistaking a target-owned ADR for platform content. The install stamp already\ndocs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md:28:- target product decisions retain `docs/adr/NNNN-<slug>.md` and `ADR NNNN`;\ndocs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md:33:former numeric Shiki ADR paths. It may delete a legacy path only when the\ndocs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md:44:- Targets may use any ADR number without colliding with Shiki's platform SADRs.\ndocs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md:45:- Fresh installs preserve target ADR bytes and install a stable SADR set.\ndocs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md:54:- Renumbering target ADRs mutates target-owned history.\ndocs/adr/SADR-0007-packaging-and-release.md:37:as an ADR.\ndocs/adr/SADR-0007-packaging-and-release.md:107:   its own ADR if a packaged distribution is needed.\ndocs/adr/SADR-0007-packaging-and-release.md:126:  warrant their own ADR.\n.codex/skills/shiki/SKILL.md:22:product decisions retain `NNNN-*.md` and the `ADR` namespace.\n.github/prompts/codex-task-handoff.md:5:Follow `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant ADRs, and `docs/agents/codex-handoff.md`.\ndocs/agents/execution-control.md:66:| Required status checks | Repo maintainer / Guardian | GitHub branch protection / rulesets | Repository setup, updated via ADR | GitHub settings | branch/ruleset config, ADR |\n.github/prompts/cca-completion-check.md:23:product decisions retain `NNNN-*.md` and the `ADR` namespace.\ndocs/agents/implementation-policy.md:35:product decisions retain `NNNN-*.md` and the `ADR` namespace.\ndocs/agents/implementation-policy.md:40:2. **Domain language is controlled.** Terms must be checked against `CONTEXT.md`; hard-to-reverse tradeoffs must be checked against ADRs.\ndocs/agents/implementation-policy.md:101:- Check ADRs before reopening decisions.\ndocs/agents/implementation-policy.md:106:- Create ADRs only for hard-to-reverse, surprising, tradeoff-based decisions.\ndocs/agents/implementation-policy.md:113:- ADRs when justified.\ndocs/agents/implementation-policy.md:123:- Relevant documents and ADRs.\ndocs/agents/implementation-policy.md:146:- Links to relevant Goal, domain terms, and ADRs.\ndocs/agents/implementation-policy.md:246:- Documentation and ADR updates.\ndocs/agents/implementation-policy.md:307:| Architecture block | Decision needs ADR or deep-module discussion. | `grill-with-docs` or `improve-codebase-architecture`. |\ndocs/agents/completion-check-agent.md:35:- relevant ADRs\ndocs/agents/runtime-auth-model.md:151:If a repo opts into API-key mode, record the exception in an ADR and update the repo's `AGENTS.md`, `.shiki/config.yaml`, and workflow permissions.\ndocs/agents/codex-handoff.md:49:- Relevant ADRs:\ndocs/agents/autonomous-loop-e2e-acceptance.md:43:5. The repo `.shiki/policy` permits autonomous low/medium-risk merges (ADR\ndocs/agents/skill-gate.md:12:| `grill-with-docs` | Ambiguous plan, unclear terminology, boundary decision, tradeoff, or ADR-worthy design choice. | Clarified decision tree, recommended answers, updates to `CONTEXT.md` or ADRs when decisions crystallize. |\ndocs/agents/skill-gate.md:20:| `improve-codebase-architecture` | Structural friction, testability, AI-navigability, deep-module opportunity, or refactor request. | Architecture candidates and deepening recommendations informed by `CONTEXT.md` and ADRs. |\ndocs/agents/checklists.md:35:| GD-02 | Relevant ADRs were read or absence was recorded. | blocking for non-trivial Goals |\ndocs/agents/checklists.md:40:| GD-07 | ADR was created or explicitly skipped using the ADR criteria. | blocking when applicable |\ndocs/agents/checklists.md:46:| CI-01 | Relevant docs and ADRs identified. | blocking |\ndocs/agents/checklists.md:65:| PRD-07 | Relevant glossary/ADR links are present when applicable. | blocking |\ndocs/agents/checklists.md:101:| H-05 | Relevant docs, ADRs, modules, and tests are referenced. | blocking |\ndocs/agents/guardian-policy.md:101:a recorded authority in the deterministic signal and does not re-derive it. See ADR\ndocs/agents/domain.md:20:- Create ADRs only for hard-to-reverse, surprising, tradeoff-based decisions.\ndocs/agents/domain.md:22:- If an ADR conflicts with a proposed implementation, surface the conflict before editing.\ndocs/agents/decision-control.md:14:| Must `grill-with-docs` run? | Planner / Skill Gate | Before PRD or issue split | Ambiguity, domain terms, ADR conflicts, unclear boundaries | Skill invocation or explicit skip rationale | Skill Gate checklist |\nscripts/shiki_installer.py:256:    \"\"\"Authorize exact legacy Shiki ADR removals before the first write.\nscripts/shiki_installer.py:323:            \"legacy Shiki ADR cleanup is not authorized:\\n\"\nscripts/shiki_installer.py:345:        info(f\"removed stamped legacy Shiki ADR: {target / relative}\")\nscripts/test_shiki_goal_loop.sh:234:# guardian-policy.json \u2014 human approval OR external AI guardian review, ADR\nscripts/shiki_guardian_review.py:162:            \"Docs/ADR-only change: confirm the change is genuinely documentation/decision-only, introduces no behavior or contract drift, and stays consistent with the constitution and existing ADRs.\"\nscripts/guardian_approval_signal.py:202:    The SAME OR that MergeGate applies must reach this deterministic signal: ADR\nscripts/shiki_tasks.py:824:            \"Run grill-with-docs until terminology, boundaries, risks, and ADR-worthy decisions are settled.\",\nscripts/shiki_guardian.py:15:# of its invocations is read-only and local (ADR: prove-from-git base sync).\nscripts/shiki_loop.py:427:        # red check \u2014 so a head-bound Guardian approval, or an SADR-0015\nscripts/shiki_contract_approval.py:145:    \"\"\"Whether this PR carries an SADR-0015 Contract Approval.\n\ndiff --git a/.codex/skills/shiki/SKILL.md b/.codex/skills/shiki/SKILL.md\nindex c8e2e85d9d75d49cc4f2d6286ee22659c4e53e75..93cded623b2c15d9f65c76491a1c2d813ac9e6c0\n--- a/.codex/skills/shiki/SKILL.md\n+++ b/.codex/skills/shiki/SKILL.md\n@@ -18,6 +18,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n Codex CLI does not register this skill as a custom `/shiki` slash command. When\n the user types `/shiki` in Codex CLI and gets \"Unrecognized command\", explain\n that this is expected: invoke Shiki in Codex with natural language such as\n@@ -69,7 +72,7 @@\n \n ## Responsibilities\n \n-- Claude Code implements and repairs by default (`shiki runner claude`, ADR 0008); Codex implements and repairs tasks explicitly assigned to `codex`.\n+- Claude Code implements and repairs by default (`shiki runner claude`, SADR-0008); Codex implements and repairs tasks explicitly assigned to `codex`.\n - Claude Code Action can act as GitHub-side CCA or reviewer.\n - CCA judges completion.\n - MergeGate authorizes state transitions and merge readiness.\ndiff --git a/.github/prompts/cca-completion-check.md b/.github/prompts/cca-completion-check.md\nindex ed203ad5f302ecc81372d8ab27d2b1232ca97497..fd89950528333ef6242e6d5443904f5392ab7e56\n--- a/.github/prompts/cca-completion-check.md\n+++ b/.github/prompts/cca-completion-check.md\n@@ -19,6 +19,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n ## Required Reading\n \n Read only the files needed to judge the PR contract. Prefer this order:\ndiff --git a/AGENTS.md b/AGENTS.md\nindex 7f041100d49c2a7525f51cb0f5d696a4a6d1c78c..2b700e989ff295987aa8a4b0c5fcb1acd80020da\n--- a/AGENTS.md\n+++ b/AGENTS.md\n@@ -44,6 +44,10 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use\n+`SADR-NNNN-*.md`; target product decisions retain `NNNN-*.md` and the `ADR`\n+namespace.\n+\n Use the canonical source-of-truth order above. Runtime-specific reading orders\n are operational guidance only and must not override or reorder it.\n \ndiff --git a/CLAUDE.md b/CLAUDE.md\nindex 98a932e7841a632866fcd53b0bad0e3af3711d3d..611d491912d6a72a0327a1731b8f33a41acb4baa\n--- a/CLAUDE.md\n+++ b/CLAUDE.md\n@@ -14,6 +14,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n ## Claude Role\n \n Default to planner, default implementer, reviewer, coordinator, documentation editor, CCA-style completion judge, and final-judgment assistant.\ndiff --git a/SYSTEM_PROMPT.md b/SYSTEM_PROMPT.md\nindex aaaea45e3a896c3a44ba89eec8bb84325ecf6c62..2e7c093ebe9e8bb34e0923756f191b4f7fa581f5\n--- a/SYSTEM_PROMPT.md\n+++ b/SYSTEM_PROMPT.md\n@@ -30,6 +30,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n Use the canonical source-of-truth order above. Runtime-specific reading orders\n are operational guidance only and must not override or reorder it. The current\n conversation is non-durable operator input.\ndiff --git a/docs/adr/0001-keep-shiki-as-platform-name.md b/docs/adr/SADR-0001-keep-shiki-as-platform-name.md\nindex 20a9637c3b9d8c97de7ba8db7cfae4d82d9a7dba..8b685439959aada1b221c112f0ac3c51ca8a1695\n--- a/docs/adr/0001-keep-shiki-as-platform-name.md\n+++ b/docs/adr/SADR-0001-keep-shiki-as-platform-name.md\n@@ -1,3 +1,3 @@\n-# Keep Shiki As The Platform Name\n+# SADR-0001: Keep Shiki As The Platform Name\n \n Shiki remains the product name for the GitHub-native agentic engineering control plane. We considered more descriptive names such as MergePilot, MergeGate, and Kiroku, but kept Shiki because it carries the existing goal-seeking framework identity while remaining independent of Hermes, Codex, Claude Code, or any single runtime.\ndiff --git a/docs/adr/0002-use-agents-as-shared-constitution.md b/docs/adr/SADR-0002-use-agents-as-shared-constitution.md\nindex 73de30ecb078120eac540d9bc51855e3cd48d0e3..c5486182bf06fca36ea508496b013c67302fd989\n--- a/docs/adr/0002-use-agents-as-shared-constitution.md\n+++ b/docs/adr/SADR-0002-use-agents-as-shared-constitution.md\n@@ -1,3 +1,3 @@\n-# Use AGENTS As The Shared Agent Constitution\n+# SADR-0002: Use AGENTS As The Shared Agent Constitution\n \n Shiki uses `AGENTS.md` as the runtime-neutral constitution and `CLAUDE.md` as a thin Claude Code wrapper. This keeps Codex, Claude Code, Hermes Runner, and future Agent Runtimes aligned on the same GitHub-first MergeGate rules while still allowing runtime-specific instructions where needed.\ndiff --git a/docs/adr/0003-require-skill-gate-for-mergegate-readiness.md b/docs/adr/SADR-0003-require-skill-gate-for-mergegate-readiness.md\nindex 424be0c9af4c8f0136f1fde68a468c47ff84e764..7fc337987c7df9792f75f181dfcbed75fe5ffff2\n--- a/docs/adr/0003-require-skill-gate-for-mergegate-readiness.md\n+++ b/docs/adr/SADR-0003-require-skill-gate-for-mergegate-readiness.md\n@@ -1,3 +1,3 @@\n-# Require Skill Gate For MergeGate Readiness\n+# SADR-0003: Require Skill Gate For MergeGate Readiness\n \n Shiki requires relevant engineering skills to be invoked before execution when their triggers apply. This makes skills part of MergeGate readiness rather than optional prompt style, because Shiki depends on repeatable planning, issue decomposition, TDD, diagnosis, triage, and architecture review before autonomous or parallel agent work can be trusted.\ndiff --git a/docs/adr/0004-use-github-first-with-shiki-mirror.md b/docs/adr/SADR-0004-use-github-first-with-shiki-mirror.md\nindex d0a3a9174dc646a945a9d325954ffaf00c352040..a79c54ae9ee8eed2a0524192b864da4e22a92d3f\n--- a/docs/adr/0004-use-github-first-with-shiki-mirror.md\n+++ b/docs/adr/SADR-0004-use-github-first-with-shiki-mirror.md\n@@ -1,4 +1,4 @@\n-# ADR 0004: Use GitHub First With A Repository-Local Shiki Mirror\n+# SADR-0004: Use GitHub First With A Repository-Local Shiki Mirror\n \n ## Status\n \ndiff --git a/docs/adr/0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md b/docs/adr/SADR-0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\nindex b339baf49b89d9290e79c3d51b36e7b2bde3bd95..c30a0d8b1fde0224d0ac7820e7870dccf721580d\n--- a/docs/adr/0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\n+++ b/docs/adr/SADR-0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\n@@ -1,4 +1,4 @@\n-# ADR 0005: Use Codex As Front And Claude Code Action As The GitHub Runtime\n+# SADR-0005: Use Codex As Front And Claude Code Action As The GitHub Runtime\n \n ## Status\n \ndiff --git a/docs/adr/0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md b/docs/adr/SADR-0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\nindex 4bcae9d1e52f0a37b8e9fbafa5cd14b6ee1993bd..7a38538a6ae9929567f0ade67e7ea6a60d0b3f7e\n--- a/docs/adr/0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\n+++ b/docs/adr/SADR-0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\n@@ -1,4 +1,4 @@\n-# ADR 0006: Separate Codex Implementation, CCA Completion, And MergeGate Authorization\n+# SADR-0006: Separate Codex Implementation, CCA Completion, And MergeGate Authorization\n \n ## Status\n \ndiff --git a/docs/adr/0007-packaging-and-release.md b/docs/adr/SADR-0007-packaging-and-release.md\nindex 79b84ddb97167372b44641e5b7860e400ce80822..023cd57f8011aa6e6d2ebf612b7c35a7a82c00d4\n--- a/docs/adr/0007-packaging-and-release.md\n+++ b/docs/adr/SADR-0007-packaging-and-release.md\n@@ -1,4 +1,4 @@\n-# ADR 0007: Packaging And Release For Shiki\n+# SADR-0007: Packaging And Release For Shiki\n \n ## Status\n \ndiff --git a/docs/adr/0008-use-claude-code-as-default-implementer.md b/docs/adr/SADR-0008-use-claude-code-as-default-implementer.md\nindex dedf4510b7c7799521422f622029563d373db031..a158a8d444d93764ec515a28133f7dde4af23579\n--- a/docs/adr/0008-use-claude-code-as-default-implementer.md\n+++ b/docs/adr/SADR-0008-use-claude-code-as-default-implementer.md\n@@ -1,4 +1,4 @@\n-# ADR 0008: Use Claude Code As The Default Implementer Runtime\n+# SADR-0008: Use Claude Code As The Default Implementer Runtime\n \n ## Status\n \ndiff --git a/docs/adr/0009-requirements-definition-and-spec-freeze.md b/docs/adr/SADR-0009-requirements-definition-and-spec-freeze.md\nindex 29b3f7871bef9203a8b4a831a65c39eb2f506e42..52e7dd69ad0aa4efe0b68c62678abbf7df02aaec\n--- a/docs/adr/0009-requirements-definition-and-spec-freeze.md\n+++ b/docs/adr/SADR-0009-requirements-definition-and-spec-freeze.md\n@@ -1,4 +1,4 @@\n-# ADR 0009: Merge Grill And Planning Into Requirements Definition With Spec Freeze\n+# SADR-0009: Merge Grill And Planning Into Requirements Definition With Spec Freeze\n \n ## Status\n \ndiff --git a/docs/adr/0010-external-ai-guardian-review.md b/docs/adr/SADR-0010-external-ai-guardian-review.md\nindex e6108bbe8bb4e73e0ff4684c8f78001a80438c4e..e690d4eddd270e683d709c9b679c5859e71eeaaa\n--- a/docs/adr/0010-external-ai-guardian-review.md\n+++ b/docs/adr/SADR-0010-external-ai-guardian-review.md\n@@ -1,4 +1,4 @@\n-# ADR 0010: External AI Guardian Review As A First-Class Approval Authority\n+# SADR-0010: External AI Guardian Review As A First-Class Approval Authority\n \n ## Status\n \ndiff --git a/docs/adr/0011-autonomous-quality-gate-evidence.md b/docs/adr/SADR-0011-autonomous-quality-gate-evidence.md\nindex bca0246c5d46e44afbde15486a75eec425fc02f5..9c93eac16d8c3a6d4b11bd7224e3955102cd7db4\n--- a/docs/adr/0011-autonomous-quality-gate-evidence.md\n+++ b/docs/adr/SADR-0011-autonomous-quality-gate-evidence.md\n@@ -1,4 +1,4 @@\n-# ADR 0011: Autonomous Quality-Gate Evidence \u2014 Deterministic Facts And An Independent Verifier\n+# SADR-0011: Autonomous Quality-Gate Evidence \u2014 Deterministic Facts And An Independent Verifier\n \n ## Status\n \ndiff --git a/docs/adr/0012-autonomous-loop-closeout-to-main.md b/docs/adr/SADR-0012-autonomous-loop-closeout-to-main.md\nindex 637fad6dde4989c9c2bf0f81f676d8d29991123c..ec7d85f81030a676bcbba28b43eb365422a36eac\n--- a/docs/adr/0012-autonomous-loop-closeout-to-main.md\n+++ b/docs/adr/SADR-0012-autonomous-loop-closeout-to-main.md\n@@ -1,4 +1,4 @@\n-# 0012 \u2014 Autonomous loop pushes goal completion to main via a closeout PR\n+# SADR-0012 \u2014 Autonomous loop pushes goal completion to main via a closeout PR\n \n - Status: accepted\n - Date: 2026-06-18\ndiff --git a/docs/adr/0013-bootstrap-grants-actions-approve-pull-requests.md b/docs/adr/SADR-0013-bootstrap-grants-actions-approve-pull-requests.md\nindex b6c69d64f1c5ced346b88d6859586715b3b5bd0e..a793e7277d67ab8ea56286fec289ca689825a17d\n--- a/docs/adr/0013-bootstrap-grants-actions-approve-pull-requests.md\n+++ b/docs/adr/SADR-0013-bootstrap-grants-actions-approve-pull-requests.md\n@@ -1,4 +1,4 @@\n-# ADR 0013: Bootstrap Grants GitHub Actions Permission To Create And Approve Pull Requests\n+# SADR-0013: Bootstrap Grants GitHub Actions Permission To Create And Approve Pull Requests\n \n ## Status\n \ndiff --git a/docs/adr/0014-codex-app-external-ai-guardian-ui-adapter.md b/docs/adr/SADR-0014-codex-app-external-ai-guardian-ui-adapter.md\nindex 238db06279334caa341c117362df9271382a7fcd..65f12e9939321e1b3e389a34c822fc1549e5155d\n--- a/docs/adr/0014-codex-app-external-ai-guardian-ui-adapter.md\n+++ b/docs/adr/SADR-0014-codex-app-external-ai-guardian-ui-adapter.md\n@@ -1,4 +1,4 @@\n-# ADR 0014: Use Codex App As The External AI Guardian UI Adapter\n+# SADR-0014: Use Codex App As The External AI Guardian UI Adapter\n \n ## Status\n \ndiff --git a/docs/adr/0015-contract-approval-before-dispatch.md b/docs/adr/SADR-0015-contract-approval-before-dispatch.md\nindex fad6c656741a20a7684ba16080a7db159f63a52f..7bdd760bb3e82eee4e1410f45c783b03b638d456\n--- a/docs/adr/0015-contract-approval-before-dispatch.md\n+++ b/docs/adr/SADR-0015-contract-approval-before-dispatch.md\n@@ -1,4 +1,4 @@\n-# ADR 0015: Approve The Task Contract Before Dispatch, Not The Diff After It\n+# SADR-0015: Approve The Task Contract Before Dispatch, Not The Diff After It\n \n ## Status\n \ndiff --git a/docs/adr/0016-locks-bound-product-paths.md b/docs/adr/SADR-0016-locks-bound-product-paths.md\nindex b5e850f3f1f7f4ef6e7f9d1763a29a51bd300c45..332cc17313498404f41a145291e38447b5de0dc4\n--- a/docs/adr/0016-locks-bound-product-paths.md\n+++ b/docs/adr/SADR-0016-locks-bound-product-paths.md\n@@ -1,4 +1,4 @@\n-# ADR 0016: Locks bound product paths; the Shiki mirror is bound by record identity\n+# SADR-0016: Locks bound product paths; the Shiki mirror is bound by record identity\n \n ## Status\n \ndiff --git a/docs/adr/0017-bookkeeping-closeouts-do-not-inherit-risk.md b/docs/adr/SADR-0017-bookkeeping-closeouts-do-not-inherit-risk.md\nindex 554e3a0d526a72ca067108c5464cc68c3edcf174..83115f69f1b1b48d544c9b0157f377b91cb53abf\n--- a/docs/adr/0017-bookkeeping-closeouts-do-not-inherit-risk.md\n+++ b/docs/adr/SADR-0017-bookkeeping-closeouts-do-not-inherit-risk.md\n@@ -1,4 +1,4 @@\n-# ADR 0017: A Bookkeeping Closeout Does Not Inherit Its Task's Risk\n+# SADR-0017: A Bookkeeping Closeout Does Not Inherit Its Task's Risk\n \n ## Status\n \ndiff --git a/docs/adr/0018-carried-guardian-approval-across-a-proven-base-sync.md b/docs/adr/SADR-0018-carried-guardian-approval-across-a-proven-base-sync.md\nindex 22358562da390ed8b45cd600da08a43d378b4680..38da0fd95102c3496c3219ce2319fce696a2532a\n--- a/docs/adr/0018-carried-guardian-approval-across-a-proven-base-sync.md\n+++ b/docs/adr/SADR-0018-carried-guardian-approval-across-a-proven-base-sync.md\n@@ -1,4 +1,4 @@\n-# ADR 0018: Carried Guardian Approval Across A Proven Base Sync\n+# SADR-0018: Carried Guardian Approval Across A Proven Base Sync\n \n ## Status\n \ndiff --git a/docs/adr/README.md b/docs/adr/README.md\nindex c70e057a4520c36e042ab68c53cd4a21ef709b71..5da43856836e2ce6a150f8127766e2f88355ab4b\n--- a/docs/adr/README.md\n+++ b/docs/adr/README.md\n@@ -1,40 +1,41 @@\n-# Architecture Decision Records\n+# Decision Record Namespaces\n \n-This directory holds Shiki's Architecture Decision Records (ADRs): durable\n-records of hard-to-reverse platform decisions. ADRs are part of the Shiki source\n-of truth alongside GitHub state, the `.shiki/` mirror, and `CONTEXT.md`.\n+This directory holds two independent decision namespaces:\n \n-## How ADRs Work\n+- Shiki Architecture Decision Records (SADRs), named\n+  `SADR-NNNN-<short-slug>.md`, record hard-to-reverse Shiki platform decisions.\n+- Target Architecture Decision Records (ADRs), named\n+  `NNNN-<short-slug>.md`, belong to the target product.\n \n-- New ADRs are added as `docs/adr/NNNN-<short-slug>.md`, where `NNNN` is the next\n-  zero-padded sequence number.\n-- The **canonical list of ADRs is the set of files matching `docs/adr/*.md`** in\n-  this directory. Browse `docs/adr/*.md` for the authoritative, current set.\n-- Use [`template.md`](template.md) as the starting point for a new ADR.\n-- Record an ADR when a decision is hard to reverse, surprising without context,\n-  and a real tradeoff. Do not write an ADR for routine or easily reversible\n-  choices.\n+The same four-digit number may appear once in each namespace. A platform\n+`SADR-NNNN` citation resolves only to `SADR-NNNN-*.md`; a target `ADR NNNN`\n+citation resolves only to `NNNN-*.md`.\n \n-Using the `docs/adr/*.md` glob as the source of truth means other PRs can add an\n-ADR without editing this index file, avoiding merge contention on a\n-hand-maintained enumeration.\n+## How Decision Records Work\n \n-## Current ADRs (snapshot)\n-\n-The list below is a convenience snapshot and may lag behind the directory. When\n-in doubt, list `docs/adr/*.md`.\n+- New Shiki platform decisions use the next zero-padded SADR number.\n+- New target product decisions use the target's own ADR sequence.\n+- The canonical list is the files in `docs/adr/`; no hand-maintained index is\n+  authoritative.\n+- Use [`template.md`](template.md) as the starting point and choose the filename\n+  namespace that owns the decision.\n+- Record a decision when it is hard to reverse, surprising without context, and\n+  a real tradeoff. Do not write a decision record for routine or easily\n+  reversible choices.\n \n-- [0001 \u2014 Keep Shiki As The Platform Name](0001-keep-shiki-as-platform-name.md)\n-- [0002 \u2014 Use AGENTS As The Shared Agent Constitution](0002-use-agents-as-shared-constitution.md)\n-- [0003 \u2014 Require Skill Gate For MergeGate Readiness](0003-require-skill-gate-for-mergegate-readiness.md)\n-- [0004 \u2014 Use GitHub First With A Repository-Local Shiki Mirror](0004-use-github-first-with-shiki-mirror.md)\n-- [0005 \u2014 Use Codex As Front And Claude Code Action As The GitHub Runtime](0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md)\n-- [0006 \u2014 Separate Codex Implementation, CCA Completion, And MergeGate Authorization](0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md)\n+Using the directory as the source of truth lets independent changes add records\n+without contending on an enumerated index.\n \n-## Adding A New ADR\n+## Adding A Shiki SADR\n \n-1. Copy `template.md` to `docs/adr/NNNN-<short-slug>.md`.\n+1. Copy `template.md` to `docs/adr/SADR-NNNN-<short-slug>.md`.\n 2. Fill in the title, status, context, decision, and consequences.\n-3. Set the status to `Proposed`, then `Accepted` once the decision is approved.\n-4. Open a PR following `CONTRIBUTING.md`. You do not need to edit this index for\n-   the ADR to be discoverable via `docs/adr/*.md`.\n+3. Set the status to `Proposed`, then `Accepted` once the proper authority has\n+   approved the decision.\n+4. Open a PR following `CONTRIBUTING.md`.\n+\n+## Adding A Target ADR\n+\n+1. Copy `template.md` to `docs/adr/NNNN-<short-slug>.md` using the target's own\n+   sequence.\n+2. Cite it as `ADR NNNN`; do not prefix its filename with `SADR-`.\ndiff --git a/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md b/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\nnew file mode 100644\nindex 0000000000000000000000000000000000000000..7455ce1f3bed02ddddb851ac21a1d1a648c9aeba\n--- /dev/null\n+++ b/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\n@@ -0,0 +1,55 @@\n+# SADR-0019: Separate Shiki Platform SADRs From Target ADRs\n+\n+## Status\n+\n+Accepted\n+\n+## Context\n+\n+Shiki installs its platform decision records into `docs/adr/` in every target\n+repository. Shiki and its targets previously used the same numeric\n+`NNNN-<slug>.md` namespace. A target that already owned ADRs 0013 through 0018\n+therefore acquired two records for each number during installation, making an\n+`ADR NNNN` citation ambiguous. Renumbering target decisions would rewrite target\n+history, while renumbering Shiki decisions per target would destroy their stable\n+platform identity.\n+\n+Upgrades also need to remove the former Shiki-owned numeric paths without ever\n+mistaking a target-owned ADR for platform content. The install stamp already\n+records exact shipped paths and content digests, so it can prove ownership only\n+when both still match.\n+\n+## Decision\n+\n+Shiki platform decisions use the `SADR` namespace:\n+\n+- records are named `docs/adr/SADR-NNNN-<slug>.md`;\n+- live platform citations use `SADR-NNNN`;\n+- target product decisions retain `docs/adr/NNNN-<slug>.md` and `ADR NNNN`;\n+- the reference validator resolves the two namespaces independently, so the same\n+  four-digit number may exist once in each namespace.\n+\n+Before a forced upgrade writes anything, the installer inventories the exact\n+former numeric Shiki ADR paths. It may delete a legacy path only when the\n+existing install stamp contains that exact path and its recorded SHA-256 digest\n+matches the file currently on disk. All ownership blockers are reported\n+together and abort the upgrade before its first write. A non-forced install\n+never performs this cleanup.\n+\n+Historical `.shiki/` evidence remains unchanged because it records the namespace\n+that existed when that evidence was produced.\n+\n+## Consequences\n+\n+- Targets may use any ADR number without colliding with Shiki's platform SADRs.\n+- Fresh installs preserve target ADR bytes and install a stable SADR set.\n+- Forced upgrades fail closed when legacy ownership cannot be proven, and a\n+  successful upgrade refreshes the stamp without removed legacy paths.\n+- Current governance, packaging, workflow, prompt, script, and skill references\n+  must move with the platform records.\n+\n+## Alternatives Considered\n+\n+- Collision-only refusal leaves affected targets permanently unupgradable.\n+- Renumbering target ADRs mutates target-owned history.\n+- Per-target Shiki renumbering makes platform citations non-portable.\ndiff --git a/docs/adr/template.md b/docs/adr/template.md\nindex a92a97daddd0b00ab9dd626cc8d86942da2eafed..17dd79175b5fee2105935557f691b40cf9492942\n--- a/docs/adr/template.md\n+++ b/docs/adr/template.md\n@@ -1,14 +1,14 @@\n-# NNNN: <Short Decision Title>\n+# <SADR-NNNN or NNNN>: <Short Decision Title>\n \n ## Status\n \n-Proposed | Accepted | Superseded by ADR-NNNN\n+Proposed | Accepted | Superseded by SADR-NNNN or target ADR NNNN\n \n ## Context\n \n What is the situation, constraint, or force that requires a decision? Reference\n-relevant domain terms (`CONTEXT.md`), prior ADRs, and code reality. State the\n-problem, not the solution.\n+relevant domain terms (`CONTEXT.md`), prior Shiki SADRs or target ADRs, and code\n+reality. State the problem, not the solution.\n \n ## Decision\n \ndiff --git a/docs/agents/domain.md b/docs/agents/domain.md\nindex 258da8ea81e87ac0a6b0b5b5e5c2fde980eaabb7..67aaa6f4b7d0a5aa4d495ce6279073333f7798cb\n--- a/docs/agents/domain.md\n+++ b/docs/agents/domain.md\n@@ -5,7 +5,9 @@\n ## Layout\n \n - `CONTEXT.md` defines Shiki language. Keep it implementation-free.\n-- `docs/adr/` records hard-to-reverse decisions.\n+- `docs/adr/SADR-NNNN-*.md` records Shiki platform decisions.\n+- `docs/adr/NNNN-*.md` records target product decisions without sharing the\n+  SADR namespace.\n - `AGENTS.md` is the runtime-neutral agent constitution.\n - `CLAUDE.md` is the Claude Code wrapper around `AGENTS.md`.\n - `.shiki/` is the Target Repository mirror for Goals, PRDs, plans, Task DAGs, contracts, locks, worktree records, ledger entries, CCA verdicts, repair packets, reports, and handoffs.\ndiff --git a/docs/agents/implementation-policy.md b/docs/agents/implementation-policy.md\nindex c754144a9fb5bf4a38aa82a60e1670c59e626b44..e90d40fe21880b9a09719c5ecfe6bde7284eb3ce\n--- a/docs/agents/implementation-policy.md\n+++ b/docs/agents/implementation-policy.md\n@@ -31,6 +31,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n ## Non-Negotiables\n \n 1. **Planning precedes implementation.** Non-trivial Goals start with `grill-with-docs` and Context & Impact.\ndiff --git a/scripts/shiki_installer.py b/scripts/shiki_installer.py\nindex be064ab7a29ff6836f7f7a1d70500a43e0d11d7b..d3e3247edbf3e4be287a4667176d86f6500f8dc5\n--- a/scripts/shiki_installer.py\n+++ b/scripts/shiki_installer.py\n@@ -118,6 +118,7 @@\n # what lets a target carry it without a governance-schema change.\n INSTALL_STAMP_PATH = \".shiki/install-stamp.json\"\n INSTALL_STAMP_VERSION = 1\n+LEGACY_SHIKI_SADR_MAX = 18\n \n # Under --force the shipped surface splits three ways, so nothing is lost and\n # nothing is silently stale.\n@@ -237,9 +238,98 @@\n         )\n \n \n+def legacy_shiki_adr_paths() -> tuple[str, ...]:\n+    \"\"\"Former numeric paths for Shiki SADR-0001 through SADR-0018.\n+\n+    The authoritative slugs come from the current SADR filenames. SADR-0019 is\n+    the namespace decision itself and never shipped under a numeric legacy path.\n+    \"\"\"\n+    paths: list[str] = []\n+    for sadr in sorted((ROOT / \"docs\" / \"adr\").glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\")):\n+        number = sadr.name.split(\"-\", 2)[1]\n+        if int(number) <= LEGACY_SHIKI_SADR_MAX:\n+            paths.append(f\"docs/adr/{sadr.name.removeprefix('SADR-')}\")\n+    return tuple(paths)\n+\n+\n+def preflight_legacy_sadr_cleanup(target: Path, *, force: bool) -> tuple[str, ...]:\n+    \"\"\"Authorize exact legacy Shiki ADR removals before the first write.\n+\n+    A non-force install never removes existing files. A forced install needs no\n+    stamp when no legacy Shiki path is present. When legacy paths do exist, each\n+    must be named in a readable install stamp and its current bytes must match\n+    the recorded digest.\n+    \"\"\"\n+    if not force:\n+        return ()\n+    present = tuple(\n+        relative\n+        for relative in legacy_shiki_adr_paths()\n+        if (target / relative).exists() or (target / relative).is_symlink()\n+    )\n+    if not present:\n+        return ()\n+\n+    stamp_path = target / INSTALL_STAMP_PATH\n+    try:\n+        raw = stamp_path.read_text(encoding=\"utf-8\")\n+        stamp = json.loads(raw)\n+    except FileNotFoundError:\n+        detail = \"install stamp is absent\"\n+        blockers = [f\"{relative}: {detail}\" for relative in present]\n+    except (OSError, UnicodeDecodeError) as error:\n+        detail = f\"install stamp is unreadable ({error})\"\n+        blockers = [f\"{relative}: {detail}\" for relative in present]\n+    except json.JSONDecodeError as error:\n+        detail = f\"install stamp is malformed ({error})\"\n+        blockers = [f\"{relative}: {detail}\" for relative in present]\n+    else:\n+        if not isinstance(stamp, dict):\n+            blockers = [\n+                f\"{relative}: install stamp is malformed (expected an object)\"\n+                for relative in present\n+            ]\n+        else:\n+            digests = stamp.get(\"digests\")\n+            blockers = []\n+            if not isinstance(digests, dict):\n+                blockers.extend(\n+                    f\"{relative}: install stamp has no digest for this path\"\n+                    for relative in present\n+                )\n+            else:\n+                for relative in present:\n+                    expected = digests.get(relative)\n+                    if not isinstance(expected, str) or not expected:\n+                        blockers.append(\n+                            f\"{relative}: install stamp has no digest for this path\"\n+                        )\n+                        continue\n+                    try:\n+                        actual = _sha256_file(target / relative)\n+                    except OSError as error:\n+                        blockers.append(\n+                            f\"{relative}: legacy path is unreadable ({error})\"\n+                        )\n+                        continue\n+                    if actual != expected:\n+                        blockers.append(\n+                            f\"{relative}: digest mismatch (stamp {expected}, current {actual})\"\n+                        )\n+\n+    if blockers:\n+        joined = \"\\n\".join(f\"- {blocker}\" for blocker in blockers)\n+        raise ShikiError(\n+            \"legacy Shiki ADR cleanup is not authorized:\\n\"\n+            f\"{joined}\\nNo files were written.\"\n+        )\n+    return present\n+\n+\n def install_template(target: Path, *, force: bool, validate: bool) -> list[NewFileNote]:\n     # Preconditions first: never rewrite the tree of a target that cannot be\n     # upgraded cleanly (see refuse_pending_migrations).\n+    legacy_cleanup = preflight_legacy_sadr_cleanup(target, force=force)\n     refuse_pending_migrations(target)\n \n     # Resolve the target's required CODEOWNERS owner once from its\n@@ -250,6 +340,10 @@\n     # verbatim. See ``codeowners_required_owner``.\n     codeowners_owner = codeowners_required_owner(target)\n \n+    for relative in legacy_cleanup:\n+        (target / relative).unlink()\n+        info(f\"removed stamped legacy Shiki ADR: {target / relative}\")\n+\n     notes: list[NewFileNote] = []\n     for relative in TEMPLATE_PATHS:\n         source = ROOT / relative\ndiff --git a/tests/test_adr_references.py b/tests/test_adr_references.py\nindex 51706329c797d5dd9b47e082a333fa83b261d482..91ab76b724cb973435c45d53cab63793942267e9\n--- a/tests/test_adr_references.py\n+++ b/tests/test_adr_references.py\n@@ -1,4 +1,4 @@\n-\"\"\"Every ``ADR ####`` citation must resolve to exactly one decision record.\n+\"\"\"Every target ADR and Shiki SADR citation resolves in its own namespace.\n \n Background. ``scripts/mergegate_check.py`` justifies ``is_bookkeeping_closeout``\n with \"ADR 0017\" nine times and ``scripts/guardian_approval_signal.py`` cites\n@@ -11,11 +11,12 @@\n and the directory drifted with no signal.\n \n This is the same unbound-pair shape behind other silent failures: a reference\n-and its target with nothing asserting they agree. This test binds them. It scans\n-every ``scripts/*.py`` (plus ``docs/**/*.md`` and the root ``*.md`` governance\n-files, which are cheap) for the ``ADR ####`` pattern and asserts each cited\n-number resolves to exactly one ``docs/adr/####-*.md``. The converse is\n-deliberately NOT required: an ADR may exist without being cited.\n+and its target with nothing asserting they agree. This test binds them. Target\n+``ADR NNNN`` citations resolve only to ``docs/adr/NNNN-*.md``; Shiki\n+``SADR-NNNN`` citations resolve only to ``docs/adr/SADR-NNNN-*.md``. The scan\n+covers current governance docs, scripts, workflows, prompts, packaging metadata,\n+and repo-local Codex guidance while deliberately excluding historical\n+``.shiki`` evidence. The converse is not required: a record may be uncited.\n \n The suite is dependency free: standard-library ``unittest`` only.\n \"\"\"\n@@ -31,17 +32,18 @@\n \n REPO_ROOT = Path(__file__).resolve().parents[1]\n \n-# \"ADR 0016\" -- a four-digit citation. The trailing \\b keeps a five-digit token\n-# from being read as a truncated four-digit number.\n-_CITATION = re.compile(r\"\\bADR (\\d{4})\\b\")\n-# \"0016-locks-bound-product-paths.md\" -- the leading four digits name the record.\n-_ADR_FILENAME = re.compile(r\"^(\\d{4})-.*\\.md$\")\n+# ``ADR 0016`` / ``SADR-0016`` -- a namespace plus four-digit citation. The\n+# trailing ``\\b`` keeps a five-digit token from being read as a truncated one.\n+_CITATION = re.compile(r\"\\b(S?ADR)[ -](\\d{4})\\b\")\n+# Target ADRs retain the numeric filename; Shiki platform decisions use SADR.\n+_DECISION_FILENAME = re.compile(r\"^(?:(SADR)-)?(\\d{4})-.*\\.md$\")\n \n \n class Violation(NamedTuple):\n     \"\"\"A citation that does not resolve to exactly one decision record.\"\"\"\n \n     citing_file: str  # repo-relative path of the file that cites the ADR\n+    namespace: str  # \"ADR\" for target decisions; \"SADR\" for Shiki decisions\n     number: str  # the cited four-digit number, e.g. \"0016\"\n     line: int  # 1-based line of the citation\n     reason: str  # \"missing\" (zero matches) or \"ambiguous\" (two or more)\n@@ -49,44 +51,59 @@\n \n     @property\n     def expected_path(self) -> str:\n-        return f\"docs/adr/{self.number}-*.md\"\n+        prefix = \"SADR-\" if self.namespace == \"SADR\" else \"\"\n+        return f\"docs/adr/{prefix}{self.number}-*.md\"\n \n     def describe(self) -> str:\n         \"\"\"Name the citing file, the cited number, and the missing path.\"\"\"\n         if self.reason == \"missing\":\n             return (\n-                f\"{self.citing_file}:{self.line} cites ADR {self.number} \"\n+                f\"{self.citing_file}:{self.line} cites {self.namespace} {self.number} \"\n                 f\"but no document matches {self.expected_path}\"\n             )\n         joined = \", \".join(self.matches)\n         return (\n-            f\"{self.citing_file}:{self.line} cites ADR {self.number} which \"\n+            f\"{self.citing_file}:{self.line} cites {self.namespace} {self.number} which \"\n             f\"resolves to {len(self.matches)} documents (expected exactly one \"\n             f\"at {self.expected_path}): {joined}\"\n         )\n \n \n-def _adr_index(root: Path) -> dict[str, list[Path]]:\n-    \"\"\"Map every four-digit ADR number to the record files that claim it.\"\"\"\n-    index: dict[str, list[Path]] = {}\n+def _adr_index(root: Path) -> dict[tuple[str, str], list[Path]]:\n+    \"\"\"Map each decision namespace and number to the files that claim it.\"\"\"\n+    index: dict[tuple[str, str], list[Path]] = {}\n     adr_dir = root / \"docs\" / \"adr\"\n     if adr_dir.is_dir():\n         for path in sorted(adr_dir.glob(\"*.md\")):\n-            match = _ADR_FILENAME.match(path.name)\n+            match = _DECISION_FILENAME.match(path.name)\n             if match:\n-                index.setdefault(match.group(1), []).append(path)\n+                namespace = \"SADR\" if match.group(1) else \"ADR\"\n+                index.setdefault((namespace, match.group(2)), []).append(path)\n     return index\n \n \n def _citing_files(root: Path, include_docs: bool = True) -> list[Path]:\n-    \"\"\"Files scanned for citations: scripts/*.py, and cheaply the markdown.\"\"\"\n+    \"\"\"Current, shipped text surfaces scanned for decision citations.\"\"\"\n     files = sorted((root / \"scripts\").glob(\"*.py\"))\n+    files += sorted((root / \"scripts\").glob(\"*.sh\"))\n     if include_docs:\n         docs = root / \"docs\"\n         if docs.is_dir():\n             files += sorted(docs.rglob(\"*.md\"))\n         files += sorted(root.glob(\"*.md\"))\n-    return files\n+        pyproject = root / \"pyproject.toml\"\n+        if pyproject.is_file():\n+            files.append(pyproject)\n+        for relative in (\".github\", \".codex\"):\n+            surface = root / relative\n+            if surface.is_dir():\n+                files += sorted(\n+                    path\n+                    for path in surface.rglob(\"*\")\n+                    if path.is_file()\n+                    and path.suffix in {\".md\", \".py\", \".sh\", \".toml\", \".yaml\", \".yml\"}\n+                )\n+    return sorted(set(files))\n \n \n def find_adr_reference_violations(\n@@ -94,10 +111,9 @@\n ) -> list[Violation]:\n     \"\"\"Return every ``ADR ####`` citation under ``root`` that fails to resolve.\n \n-    A citation resolves when exactly one ``docs/adr/####-*.md`` matches its\n-    number. Zero matches is a \"missing\" violation; two or more is \"ambiguous\".\n-    Records that are never cited produce nothing -- the converse is not\n-    required.\n+    A citation resolves when exactly one record in the same namespace matches\n+    its number. Zero matches is a \"missing\" violation; two or more is\n+    \"ambiguous\". Records that are never cited produce nothing.\n     \"\"\"\n     root = Path(root)\n     index = _adr_index(root)\n@@ -110,13 +126,15 @@\n         rel = path.relative_to(root).as_posix()\n         for lineno, line in enumerate(text.splitlines(), start=1):\n             for match in _CITATION.finditer(line):\n-                number = match.group(1)\n-                found = index.get(number, [])\n+                namespace, number = match.groups()\n+                found = index.get((namespace, number), [])\n                 if len(found) == 1:\n                     continue\n                 reason = \"missing\" if not found else \"ambiguous\"\n                 rels = [p.relative_to(root).as_posix() for p in found]\n-                violations.append(Violation(rel, number, lineno, reason, rels))\n+                violations.append(\n+                    Violation(rel, namespace, number, lineno, reason, rels)\n+                )\n     return violations\n \n \n@@ -129,8 +147,8 @@\n         self.assertEqual(\n             violations,\n             [],\n-            \"Every 'ADR ####' citation must resolve to exactly one \"\n-            f\"docs/adr/####-*.md:\\n{detail}\",\n+            \"Every ADR/SADR citation must resolve exactly once in its own \"\n+            f\"docs/adr namespace:\\n{detail}\",\n         )\n \n     def test_scan_actually_reads_scripts(self) -> None:\n@@ -146,8 +164,21 @@\n         # The two records this Goal carried onto the default branch, and which\n         # the scripts cite most, must be present.\n         index = _adr_index(REPO_ROOT)\n-        self.assertIn(\"0016\", index)\n-        self.assertIn(\"0017\", index)\n+        self.assertIn((\"SADR\", \"0016\"), index)\n+        self.assertIn((\"SADR\", \"0017\"), index)\n+\n+    def test_platform_tree_has_only_sadr_0001_through_0019(self) -> None:\n+        adr_dir = REPO_ROOT / \"docs\" / \"adr\"\n+        sadrs = sorted(adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\"))\n+        self.assertEqual(\n+            {path.name.split(\"-\", 2)[1] for path in sadrs},\n+            {f\"{number:04d}\" for number in range(1, 20)},\n+        )\n+        self.assertEqual(\n+            list(adr_dir.glob(\"[0-9][0-9][0-9][0-9]-*.md\")),\n+            [],\n+            \"the Shiki platform tree must not retain legacy numeric SADR paths\",\n+        )\n \n \n class FixtureAdrReferences(unittest.TestCase):\n@@ -180,6 +211,26 @@\n         self.assertIn(\"9991\", message)  # cited number\n         self.assertIn(\"docs/adr/9991-*.md\", message)  # missing path\n \n+    def test_target_adr_does_not_satisfy_missing_platform_sadr(self) -> None:\n+        root = self._make_repo(\n+            adr_names=[\"0042-target-decision.md\"],\n+            script_body='\"\"\"Target ADR 0042 exists, but platform SADR-0042 does not.\"\"\"\\n',\n+        )\n+        violations = find_adr_reference_violations(root, include_docs=False)\n+        self.assertEqual(len(violations), 1)\n+        self.assertEqual(violations[0].namespace, \"SADR\")\n+        self.assertEqual(violations[0].number, \"0042\")\n+        self.assertEqual(violations[0].expected_path, \"docs/adr/SADR-0042-*.md\")\n+\n+    def test_target_adr_and_platform_sadr_may_reuse_a_number(self) -> None:\n+        root = self._make_repo(\n+            adr_names=[\"0042-target-decision.md\", \"SADR-0042-platform-decision.md\"],\n+            script_body='\"\"\"Target ADR 0042 and platform SADR-0042 are distinct.\"\"\"\\n',\n+        )\n+        self.assertEqual(\n+            find_adr_reference_violations(root, include_docs=False), []\n+        )\n+\n     def test_uncited_adr_is_allowed(self) -> None:\n         # An ADR exists but nothing cites it -> not a violation. The converse\n         # (every ADR must be cited) is explicitly NOT required.\ndiff --git a/tests/test_sadr_install_migration.py b/tests/test_sadr_install_migration.py\nnew file mode 100644\nindex 0000000000000000000000000000000000000000..5a442fa5e4968ade178dcdedc3c63b54dba580a2\n--- /dev/null\n+++ b/tests/test_sadr_install_migration.py\n@@ -0,0 +1,224 @@\n+\"\"\"SADR installation and collision-safe legacy upgrade behavior.\"\"\"\n+\n+from __future__ import annotations\n+\n+import contextlib\n+import hashlib\n+import io\n+import json\n+import shutil\n+import tempfile\n+import unittest\n+from pathlib import Path\n+\n+import shiki_test_support  # noqa: F401  (path bootstrap)\n+\n+import shiki_installer\n+from tests.test_adr_references import find_adr_reference_violations\n+\n+\n+PRODUCT_NUMBERS = tuple(range(13, 19))\n+\n+\n+def _quiet_install(target: Path, *, force: bool, validate: bool) -> None:\n+    with contextlib.redirect_stdout(io.StringIO()):\n+        shiki_installer.install_template(target, force=force, validate=validate)\n+\n+\n+def _sha256(path: Path) -> str:\n+    return hashlib.sha256(path.read_bytes()).hexdigest()\n+\n+\n+def _legacy_sadr_paths() -> tuple[str, ...]:\n+    return tuple(\n+        f\"docs/adr/{path.name.removeprefix('SADR-')}\"\n+        for path in sorted((shiki_installer.ROOT / \"docs\" / \"adr\").glob(\"SADR-*.md\"))\n+        if path.name != \"SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\"\n+    )\n+\n+\n+def _create_legacy_upgrade_fixture(\n+    target: Path,\n+) -> tuple[dict[str, bytes], tuple[str, ...]]:\n+    adr_dir = target / \"docs\" / \"adr\"\n+    adr_dir.mkdir(parents=True)\n+    product_bytes = {\n+        f\"{number:04d}-cubeshare-product-decision.md\": (\n+            f\"# CubeShare ADR {number:04d}\\n\\nDo not rewrite {number}.\\n\"\n+        ).encode()\n+        for number in PRODUCT_NUMBERS\n+    }\n+    for name, content in product_bytes.items():\n+        (adr_dir / name).write_bytes(content)\n+\n+    legacy_paths = _legacy_sadr_paths()\n+    for relative in legacy_paths:\n+        (target / relative).write_bytes(f\"legacy Shiki bytes for {relative}\\n\".encode())\n+\n+    stamp_path = target / shiki_installer.INSTALL_STAMP_PATH\n+    stamp_path.parent.mkdir(parents=True, exist_ok=True)\n+    stamp = {\n+        \"version\": shiki_installer.INSTALL_STAMP_VERSION,\n+        \"platform_commit\": None,\n+        \"installed_at\": \"2000-01-01T00:00:00+00:00\",\n+        \"digests\": {\n+            relative: _sha256(target / relative) for relative in legacy_paths\n+        },\n+    }\n+    stamp_path.write_text(\n+        json.dumps(stamp, indent=2, sort_keys=True) + \"\\n\", encoding=\"utf-8\"\n+    )\n+    return product_bytes, legacy_paths\n+\n+\n+def _tree_snapshot(root: Path) -> dict[str, tuple[str, bytes | str]]:\n+    \"\"\"Recursive byte/shape snapshot used to prove preflight is write-free.\"\"\"\n+    snapshot: dict[str, tuple[str, bytes | str]] = {}\n+    for path in sorted(root.rglob(\"*\")):\n+        relative = path.relative_to(root).as_posix()\n+        if path.is_symlink():\n+            snapshot[relative] = (\"symlink\", path.readlink().as_posix())\n+        elif path.is_dir():\n+            snapshot[relative] = (\"directory\", \"\")\n+        else:\n+            snapshot[relative] = (\"file\", path.read_bytes())\n+    return snapshot\n+\n+\n+class FreshSadrInstallTests(unittest.TestCase):\n+    def setUp(self) -> None:\n+        self.tmp = Path(tempfile.mkdtemp(prefix=\"shiki-sadr-fresh-\"))\n+        self.addCleanup(lambda: shutil.rmtree(self.tmp, ignore_errors=True))\n+        self.target = self.tmp / \"target\"\n+        self.adr_dir = self.target / \"docs\" / \"adr\"\n+        self.adr_dir.mkdir(parents=True)\n+        self.product_bytes = {\n+            f\"{number:04d}-product-decision.md\": (\n+                f\"# Product ADR {number:04d}\\n\\nTarget-owned bytes {number}.\\n\"\n+            ).encode()\n+            for number in PRODUCT_NUMBERS\n+        }\n+        for name, content in self.product_bytes.items():\n+            (self.adr_dir / name).write_bytes(content)\n+\n+    def test_fresh_install_preserves_product_adrs_and_installs_all_sadrs(self) -> None:\n+        _quiet_install(self.target, force=False, validate=True)\n+\n+        for name, expected in self.product_bytes.items():\n+            self.assertEqual((self.adr_dir / name).read_bytes(), expected)\n+\n+        sadr_numbers = {\n+            path.name.split(\"-\", 2)[1]\n+            for path in self.adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\")\n+        }\n+        self.assertEqual(sadr_numbers, {f\"{number:04d}\" for number in range(1, 20)})\n+\n+        numeric_records = {\n+            path.name\n+            for path in self.adr_dir.glob(\"[0-9][0-9][0-9][0-9]-*.md\")\n+        }\n+        self.assertEqual(numeric_records, set(self.product_bytes))\n+        self.assertEqual(find_adr_reference_violations(self.target), [])\n+\n+\n+class LegacySadrUpgradeTests(unittest.TestCase):\n+    def setUp(self) -> None:\n+        self.tmp = Path(tempfile.mkdtemp(prefix=\"shiki-sadr-upgrade-\"))\n+        self.addCleanup(lambda: shutil.rmtree(self.tmp, ignore_errors=True))\n+        self.target = self.tmp / \"target\"\n+        self.adr_dir = self.target / \"docs\" / \"adr\"\n+        self.product_bytes, self.legacy_paths = _create_legacy_upgrade_fixture(\n+            self.target\n+        )\n+\n+    def test_force_upgrade_deletes_only_stamped_legacy_sadrs(self) -> None:\n+        _quiet_install(self.target, force=True, validate=True)\n+\n+        for relative in self.legacy_paths:\n+            self.assertFalse((self.target / relative).exists(), relative)\n+        for name, expected in self.product_bytes.items():\n+            self.assertEqual((self.adr_dir / name).read_bytes(), expected)\n+\n+        sadrs = list(self.adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\"))\n+        self.assertEqual(len(sadrs), 19)\n+        refreshed = json.loads(\n+            (self.target / shiki_installer.INSTALL_STAMP_PATH).read_text(encoding=\"utf-8\")\n+        )\n+        for relative in self.legacy_paths:\n+            self.assertNotIn(relative, refreshed[\"digests\"])\n+        for path in sadrs:\n+            relative = path.relative_to(self.target).as_posix()\n+            self.assertEqual(refreshed[\"digests\"][relative], _sha256(path))\n+\n+    def test_non_force_install_never_deletes_legacy_files(self) -> None:\n+        before = {\n+            relative: (self.target / relative).read_bytes()\n+            for relative in self.legacy_paths\n+        }\n+        _quiet_install(self.target, force=False, validate=False)\n+        for relative, expected in before.items():\n+            self.assertEqual((self.target / relative).read_bytes(), expected)\n+\n+\n+class LegacyCleanupPreflightTests(unittest.TestCase):\n+    def setUp(self) -> None:\n+        self.tmp = Path(tempfile.mkdtemp(prefix=\"shiki-sadr-preflight-\"))\n+        self.addCleanup(lambda: shutil.rmtree(self.tmp, ignore_errors=True))\n+\n+    def test_all_ownership_blockers_abort_before_any_write(self) -> None:\n+        cases = (\n+            (\"absent\", \"install stamp is absent\"),\n+            (\"unreadable\", \"install stamp is unreadable\"),\n+            (\"malformed\", \"install stamp is malformed\"),\n+            (\"missing-digest\", \"install stamp has no digest\"),\n+            (\"digest-mismatch\", \"digest mismatch\"),\n+        )\n+        for name, expected_reason in cases:\n+            with self.subTest(name=name):\n+                target = self.tmp / name\n+                _, legacy_paths = _create_legacy_upgrade_fixture(target)\n+                stamp_path = target / shiki_installer.INSTALL_STAMP_PATH\n+\n+                if name == \"absent\":\n+                    stamp_path.unlink()\n+                elif name == \"unreadable\":\n+                    stamp_path.unlink()\n+                    stamp_path.mkdir()\n+                elif name == \"malformed\":\n+                    stamp_path.write_text(\"{not-json\\n\", encoding=\"utf-8\")\n+                else:\n+                    stamp = json.loads(stamp_path.read_text(encoding=\"utf-8\"))\n+                    if name == \"missing-digest\":\n+                        stamp[\"digests\"] = {}\n+                    else:\n+                        stamp[\"digests\"] = {\n+                            relative: \"deadbeef\" for relative in legacy_paths\n+                        }\n+                    stamp_path.write_text(\n+                        json.dumps(stamp, indent=2, sort_keys=True) + \"\\n\",\n+                        encoding=\"utf-8\",\n+                    )\n+\n+                before = _tree_snapshot(target)\n+                with self.assertRaises(shiki_installer.ShikiError) as raised:\n+                    _quiet_install(target, force=True, validate=False)\n+                after = _tree_snapshot(target)\n+\n+                self.assertEqual(before, after, f\"{name} preflight changed the tree\")\n+                message = str(raised.exception)\n+                self.assertIn(expected_reason, message)\n+                self.assertIn(\"No files were written\", message)\n+                for relative in legacy_paths:\n+                    self.assertIn(relative, message)\n+\n+    def test_absent_stamp_is_allowed_when_no_legacy_path_exists(self) -> None:\n+        target = self.tmp / \"no-legacy\"\n+        target.mkdir()\n+        _quiet_install(target, force=True, validate=False)\n+        self.assertTrue(\n+            (target / \"docs\" / \"adr\" / \"SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\").is_file()\n+        )\n+\n+\n+if __name__ == \"__main__\":\n+    unittest.main()\n\nexec\n/bin/zsh -lc \"rg -n -U --glob '\"'!.shiki/**'\"' --glob '\"'!skills/**'\"' --glob '\"'!tests/test_adr_references.py'\"' --glob '\"'!tests/test_sadr_install_migration.py'\"' '\\\\bADR[- ]?\\\\s*(000[1-9]|001[0-8])\\\\b|docs/adr/\\\\s*(000[1-9]|001[0-8])-' AGENTS.md CLAUDE.md CONTEXT.md CONTRIBUTING.md SYSTEM_PROMPT.md pyproject.toml docs scripts tests .github .codex\" in /private/tmp/shiki-adr-collision.pa0KwY/.worktrees/shiki-t-20260807t073307221254z-2d22d654-introduc\n succeeded in 0ms:\ntests/test_policy_job_merged_prs.py:6:and the authoritative gate (``mergegate_check.py`` in \"Enforce MergeGate\"). ADR\ntests/test_policy_job_merged_prs.py:7:0017's bookkeeping-closeout exemption (condition 6) needs the merged implementation\ndocs/agents/autonomous-loop-e2e-acceptance.md:43:5. The repo `.shiki/policy` permits autonomous low/medium-risk merges (ADR\ndocs/agents/autonomous-loop-e2e-acceptance.md:44:   0008/0009); the MergeGate policy check is the recorded-authority gate.\ndocs/agents/guardian-policy.md:101:a recorded authority in the deterministic signal and does not re-derive it. See ADR\ndocs/agents/guardian-policy.md:102:0018 for the full decision, the rejected diff/patch-equality and path-disjointness\nscripts/guardian_approval_signal.py:202:    The SAME OR that MergeGate applies must reach this deterministic signal: ADR\nscripts/guardian_approval_signal.py:203:    0015 is explicit that without it, a contract-approved PR still returns\n\ndiff --git a/.codex/skills/shiki/SKILL.md b/.codex/skills/shiki/SKILL.md\nindex c8e2e85d9d75d49cc4f2d6286ee22659c4e53e75..93cded623b2c15d9f65c76491a1c2d813ac9e6c0\n--- a/.codex/skills/shiki/SKILL.md\n+++ b/.codex/skills/shiki/SKILL.md\n@@ -18,6 +18,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n Codex CLI does not register this skill as a custom `/shiki` slash command. When\n the user types `/shiki` in Codex CLI and gets \"Unrecognized command\", explain\n that this is expected: invoke Shiki in Codex with natural language such as\n@@ -69,7 +72,7 @@\n \n ## Responsibilities\n \n-- Claude Code implements and repairs by default (`shiki runner claude`, ADR 0008); Codex implements and repairs tasks explicitly assigned to `codex`.\n+- Claude Code implements and repairs by default (`shiki runner claude`, SADR-0008); Codex implements and repairs tasks explicitly assigned to `codex`.\n - Claude Code Action can act as GitHub-side CCA or reviewer.\n - CCA judges completion.\n - MergeGate authorizes state transitions and merge readiness.\ndiff --git a/.github/prompts/cca-completion-check.md b/.github/prompts/cca-completion-check.md\nindex ed203ad5f302ecc81372d8ab27d2b1232ca97497..fd89950528333ef6242e6d5443904f5392ab7e56\n--- a/.github/prompts/cca-completion-check.md\n+++ b/.github/prompts/cca-completion-check.md\n@@ -19,6 +19,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n ## Required Reading\n \n Read only the files needed to judge the PR contract. Prefer this order:\ndiff --git a/AGENTS.md b/AGENTS.md\nindex 7f041100d49c2a7525f51cb0f5d696a4a6d1c78c..2b700e989ff295987aa8a4b0c5fcb1acd80020da\n--- a/AGENTS.md\n+++ b/AGENTS.md\n@@ -44,6 +44,10 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use\n+`SADR-NNNN-*.md`; target product decisions retain `NNNN-*.md` and the `ADR`\n+namespace.\n+\n Use the canonical source-of-truth order above. Runtime-specific reading orders\n are operational guidance only and must not override or reorder it.\n \ndiff --git a/CLAUDE.md b/CLAUDE.md\nindex 98a932e7841a632866fcd53b0bad0e3af3711d3d..611d491912d6a72a0327a1731b8f33a41acb4baa\n--- a/CLAUDE.md\n+++ b/CLAUDE.md\n@@ -14,6 +14,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n ## Claude Role\n \n Default to planner, default implementer, reviewer, coordinator, documentation editor, CCA-style completion judge, and final-judgment assistant.\ndiff --git a/SYSTEM_PROMPT.md b/SYSTEM_PROMPT.md\nindex aaaea45e3a896c3a44ba89eec8bb84325ecf6c62..2e7c093ebe9e8bb34e0923756f191b4f7fa581f5\n--- a/SYSTEM_PROMPT.md\n+++ b/SYSTEM_PROMPT.md\n@@ -30,6 +30,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n Use the canonical source-of-truth order above. Runtime-specific reading orders\n are operational guidance only and must not override or reorder it. The current\n conversation is non-durable operator input.\ndiff --git a/docs/adr/0001-keep-shiki-as-platform-name.md b/docs/adr/SADR-0001-keep-shiki-as-platform-name.md\nindex 20a9637c3b9d8c97de7ba8db7cfae4d82d9a7dba..8b685439959aada1b221c112f0ac3c51ca8a1695\n--- a/docs/adr/0001-keep-shiki-as-platform-name.md\n+++ b/docs/adr/SADR-0001-keep-shiki-as-platform-name.md\n@@ -1,3 +1,3 @@\n-# Keep Shiki As The Platform Name\n+# SADR-0001: Keep Shiki As The Platform Name\n \n Shiki remains the product name for the GitHub-native agentic engineering control plane. We considered more descriptive names such as MergePilot, MergeGate, and Kiroku, but kept Shiki because it carries the existing goal-seeking framework identity while remaining independent of Hermes, Codex, Claude Code, or any single runtime.\ndiff --git a/docs/adr/0002-use-agents-as-shared-constitution.md b/docs/adr/SADR-0002-use-agents-as-shared-constitution.md\nindex 73de30ecb078120eac540d9bc51855e3cd48d0e3..c5486182bf06fca36ea508496b013c67302fd989\n--- a/docs/adr/0002-use-agents-as-shared-constitution.md\n+++ b/docs/adr/SADR-0002-use-agents-as-shared-constitution.md\n@@ -1,3 +1,3 @@\n-# Use AGENTS As The Shared Agent Constitution\n+# SADR-0002: Use AGENTS As The Shared Agent Constitution\n \n Shiki uses `AGENTS.md` as the runtime-neutral constitution and `CLAUDE.md` as a thin Claude Code wrapper. This keeps Codex, Claude Code, Hermes Runner, and future Agent Runtimes aligned on the same GitHub-first MergeGate rules while still allowing runtime-specific instructions where needed.\ndiff --git a/docs/adr/0003-require-skill-gate-for-mergegate-readiness.md b/docs/adr/SADR-0003-require-skill-gate-for-mergegate-readiness.md\nindex 424be0c9af4c8f0136f1fde68a468c47ff84e764..7fc337987c7df9792f75f181dfcbed75fe5ffff2\n--- a/docs/adr/0003-require-skill-gate-for-mergegate-readiness.md\n+++ b/docs/adr/SADR-0003-require-skill-gate-for-mergegate-readiness.md\n@@ -1,3 +1,3 @@\n-# Require Skill Gate For MergeGate Readiness\n+# SADR-0003: Require Skill Gate For MergeGate Readiness\n \n Shiki requires relevant engineering skills to be invoked before execution when their triggers apply. This makes skills part of MergeGate readiness rather than optional prompt style, because Shiki depends on repeatable planning, issue decomposition, TDD, diagnosis, triage, and architecture review before autonomous or parallel agent work can be trusted.\ndiff --git a/docs/adr/0004-use-github-first-with-shiki-mirror.md b/docs/adr/SADR-0004-use-github-first-with-shiki-mirror.md\nindex d0a3a9174dc646a945a9d325954ffaf00c352040..a79c54ae9ee8eed2a0524192b864da4e22a92d3f\n--- a/docs/adr/0004-use-github-first-with-shiki-mirror.md\n+++ b/docs/adr/SADR-0004-use-github-first-with-shiki-mirror.md\n@@ -1,4 +1,4 @@\n-# ADR 0004: Use GitHub First With A Repository-Local Shiki Mirror\n+# SADR-0004: Use GitHub First With A Repository-Local Shiki Mirror\n \n ## Status\n \ndiff --git a/docs/adr/0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md b/docs/adr/SADR-0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\nindex b339baf49b89d9290e79c3d51b36e7b2bde3bd95..c30a0d8b1fde0224d0ac7820e7870dccf721580d\n--- a/docs/adr/0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\n+++ b/docs/adr/SADR-0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\n@@ -1,4 +1,4 @@\n-# ADR 0005: Use Codex As Front And Claude Code Action As The GitHub Runtime\n+# SADR-0005: Use Codex As Front And Claude Code Action As The GitHub Runtime\n \n ## Status\n \ndiff --git a/docs/adr/0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md b/docs/adr/SADR-0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\nindex 4bcae9d1e52f0a37b8e9fbafa5cd14b6ee1993bd..7a38538a6ae9929567f0ade67e7ea6a60d0b3f7e\n--- a/docs/adr/0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\n+++ b/docs/adr/SADR-0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\n@@ -1,4 +1,4 @@\n-# ADR 0006: Separate Codex Implementation, CCA Completion, And MergeGate Authorization\n+# SADR-0006: Separate Codex Implementation, CCA Completion, And MergeGate Authorization\n \n ## Status\n \ndiff --git a/docs/adr/0007-packaging-and-release.md b/docs/adr/SADR-0007-packaging-and-release.md\nindex 79b84ddb97167372b44641e5b7860e400ce80822..023cd57f8011aa6e6d2ebf612b7c35a7a82c00d4\n--- a/docs/adr/0007-packaging-and-release.md\n+++ b/docs/adr/SADR-0007-packaging-and-release.md\n@@ -1,4 +1,4 @@\n-# ADR 0007: Packaging And Release For Shiki\n+# SADR-0007: Packaging And Release For Shiki\n \n ## Status\n \ndiff --git a/docs/adr/0008-use-claude-code-as-default-implementer.md b/docs/adr/SADR-0008-use-claude-code-as-default-implementer.md\nindex dedf4510b7c7799521422f622029563d373db031..a158a8d444d93764ec515a28133f7dde4af23579\n--- a/docs/adr/0008-use-claude-code-as-default-implementer.md\n+++ b/docs/adr/SADR-0008-use-claude-code-as-default-implementer.md\n@@ -1,4 +1,4 @@\n-# ADR 0008: Use Claude Code As The Default Implementer Runtime\n+# SADR-0008: Use Claude Code As The Default Implementer Runtime\n \n ## Status\n \ndiff --git a/docs/adr/0009-requirements-definition-and-spec-freeze.md b/docs/adr/SADR-0009-requirements-definition-and-spec-freeze.md\nindex 29b3f7871bef9203a8b4a831a65c39eb2f506e42..52e7dd69ad0aa4efe0b68c62678abbf7df02aaec\n--- a/docs/adr/0009-requirements-definition-and-spec-freeze.md\n+++ b/docs/adr/SADR-0009-requirements-definition-and-spec-freeze.md\n@@ -1,4 +1,4 @@\n-# ADR 0009: Merge Grill And Planning Into Requirements Definition With Spec Freeze\n+# SADR-0009: Merge Grill And Planning Into Requirements Definition With Spec Freeze\n \n ## Status\n \ndiff --git a/docs/adr/0010-external-ai-guardian-review.md b/docs/adr/SADR-0010-external-ai-guardian-review.md\nindex e6108bbe8bb4e73e0ff4684c8f78001a80438c4e..e690d4eddd270e683d709c9b679c5859e71eeaaa\n--- a/docs/adr/0010-external-ai-guardian-review.md\n+++ b/docs/adr/SADR-0010-external-ai-guardian-review.md\n@@ -1,4 +1,4 @@\n-# ADR 0010: External AI Guardian Review As A First-Class Approval Authority\n+# SADR-0010: External AI Guardian Review As A First-Class Approval Authority\n \n ## Status\n \ndiff --git a/docs/adr/0011-autonomous-quality-gate-evidence.md b/docs/adr/SADR-0011-autonomous-quality-gate-evidence.md\nindex bca0246c5d46e44afbde15486a75eec425fc02f5..9c93eac16d8c3a6d4b11bd7224e3955102cd7db4\n--- a/docs/adr/0011-autonomous-quality-gate-evidence.md\n+++ b/docs/adr/SADR-0011-autonomous-quality-gate-evidence.md\n@@ -1,4 +1,4 @@\n-# ADR 0011: Autonomous Quality-Gate Evidence \u2014 Deterministic Facts And An Independent Verifier\n+# SADR-0011: Autonomous Quality-Gate Evidence \u2014 Deterministic Facts And An Independent Verifier\n \n ## Status\n \ndiff --git a/docs/adr/0012-autonomous-loop-closeout-to-main.md b/docs/adr/SADR-0012-autonomous-loop-closeout-to-main.md\nindex 637fad6dde4989c9c2bf0f81f676d8d29991123c..ec7d85f81030a676bcbba28b43eb365422a36eac\n--- a/docs/adr/0012-autonomous-loop-closeout-to-main.md\n+++ b/docs/adr/SADR-0012-autonomous-loop-closeout-to-main.md\n@@ -1,4 +1,4 @@\n-# 0012 \u2014 Autonomous loop pushes goal completion to main via a closeout PR\n+# SADR-0012 \u2014 Autonomous loop pushes goal completion to main via a closeout PR\n \n - Status: accepted\n - Date: 2026-06-18\ndiff --git a/docs/adr/0013-bootstrap-grants-actions-approve-pull-requests.md b/docs/adr/SADR-0013-bootstrap-grants-actions-approve-pull-requests.md\nindex b6c69d64f1c5ced346b88d6859586715b3b5bd0e..a793e7277d67ab8ea56286fec289ca689825a17d\n--- a/docs/adr/0013-bootstrap-grants-actions-approve-pull-requests.md\n+++ b/docs/adr/SADR-0013-bootstrap-grants-actions-approve-pull-requests.md\n@@ -1,4 +1,4 @@\n-# ADR 0013: Bootstrap Grants GitHub Actions Permission To Create And Approve Pull Requests\n+# SADR-0013: Bootstrap Grants GitHub Actions Permission To Create And Approve Pull Requests\n \n ## Status\n \ndiff --git a/docs/adr/0014-codex-app-external-ai-guardian-ui-adapter.md b/docs/adr/SADR-0014-codex-app-external-ai-guardian-ui-adapter.md\nindex 238db06279334caa341c117362df9271382a7fcd..65f12e9939321e1b3e389a34c822fc1549e5155d\n--- a/docs/adr/0014-codex-app-external-ai-guardian-ui-adapter.md\n+++ b/docs/adr/SADR-0014-codex-app-external-ai-guardian-ui-adapter.md\n@@ -1,4 +1,4 @@\n-# ADR 0014: Use Codex App As The External AI Guardian UI Adapter\n+# SADR-0014: Use Codex App As The External AI Guardian UI Adapter\n \n ## Status\n \ndiff --git a/docs/adr/0015-contract-approval-before-dispatch.md b/docs/adr/SADR-0015-contract-approval-before-dispatch.md\nindex fad6c656741a20a7684ba16080a7db159f63a52f..7bdd760bb3e82eee4e1410f45c783b03b638d456\n--- a/docs/adr/0015-contract-approval-before-dispatch.md\n+++ b/docs/adr/SADR-0015-contract-approval-before-dispatch.md\n@@ -1,4 +1,4 @@\n-# ADR 0015: Approve The Task Contract Before Dispatch, Not The Diff After It\n+# SADR-0015: Approve The Task Contract Before Dispatch, Not The Diff After It\n \n ## Status\n \ndiff --git a/docs/adr/0016-locks-bound-product-paths.md b/docs/adr/SADR-0016-locks-bound-product-paths.md\nindex b5e850f3f1f7f4ef6e7f9d1763a29a51bd300c45..332cc17313498404f41a145291e38447b5de0dc4\n--- a/docs/adr/0016-locks-bound-product-paths.md\n+++ b/docs/adr/SADR-0016-locks-bound-product-paths.md\n@@ -1,4 +1,4 @@\n-# ADR 0016: Locks bound product paths; the Shiki mirror is bound by record identity\n+# SADR-0016: Locks bound product paths; the Shiki mirror is bound by record identity\n \n ## Status\n \ndiff --git a/docs/adr/0017-bookkeeping-closeouts-do-not-inherit-risk.md b/docs/adr/SADR-0017-bookkeeping-closeouts-do-not-inherit-risk.md\nindex 554e3a0d526a72ca067108c5464cc68c3edcf174..83115f69f1b1b48d544c9b0157f377b91cb53abf\n--- a/docs/adr/0017-bookkeeping-closeouts-do-not-inherit-risk.md\n+++ b/docs/adr/SADR-0017-bookkeeping-closeouts-do-not-inherit-risk.md\n@@ -1,4 +1,4 @@\n-# ADR 0017: A Bookkeeping Closeout Does Not Inherit Its Task's Risk\n+# SADR-0017: A Bookkeeping Closeout Does Not Inherit Its Task's Risk\n \n ## Status\n \ndiff --git a/docs/adr/0018-carried-guardian-approval-across-a-proven-base-sync.md b/docs/adr/SADR-0018-carried-guardian-approval-across-a-proven-base-sync.md\nindex 22358562da390ed8b45cd600da08a43d378b4680..38da0fd95102c3496c3219ce2319fce696a2532a\n--- a/docs/adr/0018-carried-guardian-approval-across-a-proven-base-sync.md\n+++ b/docs/adr/SADR-0018-carried-guardian-approval-across-a-proven-base-sync.md\n@@ -1,4 +1,4 @@\n-# ADR 0018: Carried Guardian Approval Across A Proven Base Sync\n+# SADR-0018: Carried Guardian Approval Across A Proven Base Sync\n \n ## Status\n \ndiff --git a/docs/adr/README.md b/docs/adr/README.md\nindex c70e057a4520c36e042ab68c53cd4a21ef709b71..5da43856836e2ce6a150f8127766e2f88355ab4b\n--- a/docs/adr/README.md\n+++ b/docs/adr/README.md\n@@ -1,40 +1,41 @@\n-# Architecture Decision Records\n+# Decision Record Namespaces\n \n-This directory holds Shiki's Architecture Decision Records (ADRs): durable\n-records of hard-to-reverse platform decisions. ADRs are part of the Shiki source\n-of truth alongside GitHub state, the `.shiki/` mirror, and `CONTEXT.md`.\n+This directory holds two independent decision namespaces:\n \n-## How ADRs Work\n+- Shiki Architecture Decision Records (SADRs), named\n+  `SADR-NNNN-<short-slug>.md`, record hard-to-reverse Shiki platform decisions.\n+- Target Architecture Decision Records (ADRs), named\n+  `NNNN-<short-slug>.md`, belong to the target product.\n \n-- New ADRs are added as `docs/adr/NNNN-<short-slug>.md`, where `NNNN` is the next\n-  zero-padded sequence number.\n-- The **canonical list of ADRs is the set of files matching `docs/adr/*.md`** in\n-  this directory. Browse `docs/adr/*.md` for the authoritative, current set.\n-- Use [`template.md`](template.md) as the starting point for a new ADR.\n-- Record an ADR when a decision is hard to reverse, surprising without context,\n-  and a real tradeoff. Do not write an ADR for routine or easily reversible\n-  choices.\n+The same four-digit number may appear once in each namespace. A platform\n+`SADR-NNNN` citation resolves only to `SADR-NNNN-*.md`; a target `ADR NNNN`\n+citation resolves only to `NNNN-*.md`.\n \n-Using the `docs/adr/*.md` glob as the source of truth means other PRs can add an\n-ADR without editing this index file, avoiding merge contention on a\n-hand-maintained enumeration.\n+## How Decision Records Work\n \n-## Current ADRs (snapshot)\n-\n-The list below is a convenience snapshot and may lag behind the directory. When\n-in doubt, list `docs/adr/*.md`.\n+- New Shiki platform decisions use the next zero-padded SADR number.\n+- New target product decisions use the target's own ADR sequence.\n+- The canonical list is the files in `docs/adr/`; no hand-maintained index is\n+  authoritative.\n+- Use [`template.md`](template.md) as the starting point and choose the filename\n+  namespace that owns the decision.\n+- Record a decision when it is hard to reverse, surprising without context, and\n+  a real tradeoff. Do not write a decision record for routine or easily\n+  reversible choices.\n \n-- [0001 \u2014 Keep Shiki As The Platform Name](0001-keep-shiki-as-platform-name.md)\n-- [0002 \u2014 Use AGENTS As The Shared Agent Constitution](0002-use-agents-as-shared-constitution.md)\n-- [0003 \u2014 Require Skill Gate For MergeGate Readiness](0003-require-skill-gate-for-mergegate-readiness.md)\n-- [0004 \u2014 Use GitHub First With A Repository-Local Shiki Mirror](0004-use-github-first-with-shiki-mirror.md)\n-- [0005 \u2014 Use Codex As Front And Claude Code Action As The GitHub Runtime](0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md)\n-- [0006 \u2014 Separate Codex Implementation, CCA Completion, And MergeGate Authorization](0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md)\n+Using the directory as the source of truth lets independent changes add records\n+without contending on an enumerated index.\n \n-## Adding A New ADR\n+## Adding A Shiki SADR\n \n-1. Copy `template.md` to `docs/adr/NNNN-<short-slug>.md`.\n+1. Copy `template.md` to `docs/adr/SADR-NNNN-<short-slug>.md`.\n 2. Fill in the title, status, context, decision, and consequences.\n-3. Set the status to `Proposed`, then `Accepted` once the decision is approved.\n-4. Open a PR following `CONTRIBUTING.md`. You do not need to edit this index for\n-   the ADR to be discoverable via `docs/adr/*.md`.\n+3. Set the status to `Proposed`, then `Accepted` once the proper authority has\n+   approved the decision.\n+4. Open a PR following `CONTRIBUTING.md`.\n+\n+## Adding A Target ADR\n+\n+1. Copy `template.md` to `docs/adr/NNNN-<short-slug>.md` using the target's own\n+   sequence.\n+2. Cite it as `ADR NNNN`; do not prefix its filename with `SADR-`.\ndiff --git a/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md b/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\nnew file mode 100644\nindex 0000000000000000000000000000000000000000..7455ce1f3bed02ddddb851ac21a1d1a648c9aeba\n--- /dev/null\n+++ b/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\n@@ -0,0 +1,55 @@\n+# SADR-0019: Separate Shiki Platform SADRs From Target ADRs\n+\n+## Status\n+\n+Accepted\n+\n+## Context\n+\n+Shiki installs its platform decision records into `docs/adr/` in every target\n+repository. Shiki and its targets previously used the same numeric\n+`NNNN-<slug>.md` namespace. A target that already owned ADRs 0013 through 0018\n+therefore acquired two records for each number during installation, making an\n+`ADR NNNN` citation ambiguous. Renumbering target decisions would rewrite target\n+history, while renumbering Shiki decisions per target would destroy their stable\n+platform identity.\n+\n+Upgrades also need to remove the former Shiki-owned numeric paths without ever\n+mistaking a target-owned ADR for platform content. The install stamp already\n+records exact shipped paths and content digests, so it can prove ownership only\n+when both still match.\n+\n+## Decision\n+\n+Shiki platform decisions use the `SADR` namespace:\n+\n+- records are named `docs/adr/SADR-NNNN-<slug>.md`;\n+- live platform citations use `SADR-NNNN`;\n+- target product decisions retain `docs/adr/NNNN-<slug>.md` and `ADR NNNN`;\n+- the reference validator resolves the two namespaces independently, so the same\n+  four-digit number may exist once in each namespace.\n+\n+Before a forced upgrade writes anything, the installer inventories the exact\n+former numeric Shiki ADR paths. It may delete a legacy path only when the\n+existing install stamp contains that exact path and its recorded SHA-256 digest\n+matches the file currently on disk. All ownership blockers are reported\n+together and abort the upgrade before its first write. A non-forced install\n+never performs this cleanup.\n+\n+Historical `.shiki/` evidence remains unchanged because it records the namespace\n+that existed when that evidence was produced.\n+\n+## Consequences\n+\n+- Targets may use any ADR number without colliding with Shiki's platform SADRs.\n+- Fresh installs preserve target ADR bytes and install a stable SADR set.\n+- Forced upgrades fail closed when legacy ownership cannot be proven, and a\n+  successful upgrade refreshes the stamp without removed legacy paths.\n+- Current governance, packaging, workflow, prompt, script, and skill references\n+  must move with the platform records.\n+\n+## Alternatives Considered\n+\n+- Collision-only refusal leaves affected targets permanently unupgradable.\n+- Renumbering target ADRs mutates target-owned history.\n+- Per-target Shiki renumbering makes platform citations non-portable.\ndiff --git a/docs/adr/template.md b/docs/adr/template.md\nindex a92a97daddd0b00ab9dd626cc8d86942da2eafed..17dd79175b5fee2105935557f691b40cf9492942\n--- a/docs/adr/template.md\n+++ b/docs/adr/template.md\n@@ -1,14 +1,14 @@\n-# NNNN: <Short Decision Title>\n+# <SADR-NNNN or NNNN>: <Short Decision Title>\n \n ## Status\n \n-Proposed | Accepted | Superseded by ADR-NNNN\n+Proposed | Accepted | Superseded by SADR-NNNN or target ADR NNNN\n \n ## Context\n \n What is the situation, constraint, or force that requires a decision? Reference\n-relevant domain terms (`CONTEXT.md`), prior ADRs, and code reality. State the\n-problem, not the solution.\n+relevant domain terms (`CONTEXT.md`), prior Shiki SADRs or target ADRs, and code\n+reality. State the problem, not the solution.\n \n ## Decision\n \ndiff --git a/docs/agents/domain.md b/docs/agents/domain.md\nindex 258da8ea81e87ac0a6b0b5b5e5c2fde980eaabb7..67aaa6f4b7d0a5aa4d495ce6279073333f7798cb\n--- a/docs/agents/domain.md\n+++ b/docs/agents/domain.md\n@@ -5,7 +5,9 @@\n ## Layout\n \n - `CONTEXT.md` defines Shiki language. Keep it implementation-free.\n-- `docs/adr/` records hard-to-reverse decisions.\n+- `docs/adr/SADR-NNNN-*.md` records Shiki platform decisions.\n+- `docs/adr/NNNN-*.md` records target product decisions without sharing the\n+  SADR namespace.\n - `AGENTS.md` is the runtime-neutral agent constitution.\n - `CLAUDE.md` is the Claude Code wrapper around `AGENTS.md`.\n - `.shiki/` is the Target Repository mirror for Goals, PRDs, plans, Task DAGs, contracts, locks, worktree records, ledger entries, CCA verdicts, repair packets, reports, and handoffs.\ndiff --git a/docs/agents/implementation-policy.md b/docs/agents/implementation-policy.md\nindex c754144a9fb5bf4a38aa82a60e1670c59e626b44..e90d40fe21880b9a09719c5ecfe6bde7284eb3ce\n--- a/docs/agents/implementation-policy.md\n+++ b/docs/agents/implementation-policy.md\n@@ -31,6 +31,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n ## Non-Negotiables\n \n 1. **Planning precedes implementation.** Non-trivial Goals start with `grill-with-docs` and Context & Impact.\ndiff --git a/scripts/shiki_installer.py b/scripts/shiki_installer.py\nindex be064ab7a29ff6836f7f7a1d70500a43e0d11d7b..d3e3247edbf3e4be287a4667176d86f6500f8dc5\n--- a/scripts/shiki_installer.py\n+++ b/scripts/shiki_installer.py\n@@ -118,6 +118,7 @@\n # what lets a target carry it without a governance-schema change.\n INSTALL_STAMP_PATH = \".shiki/install-stamp.json\"\n INSTALL_STAMP_VERSION = 1\n+LEGACY_SHIKI_SADR_MAX = 18\n \n # Under --force the shipped surface splits three ways, so nothing is lost and\n # nothing is silently stale.\n@@ -237,9 +238,98 @@\n         )\n \n \n+def legacy_shiki_adr_paths() -> tuple[str, ...]:\n+    \"\"\"Former numeric paths for Shiki SADR-0001 through SADR-0018.\n+\n+    The authoritative slugs come from the current SADR filenames. SADR-0019 is\n+    the namespace decision itself and never shipped under a numeric legacy path.\n+    \"\"\"\n+    paths: list[str] = []\n+    for sadr in sorted((ROOT / \"docs\" / \"adr\").glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\")):\n+        number = sadr.name.split(\"-\", 2)[1]\n+        if int(number) <= LEGACY_SHIKI_SADR_MAX:\n+            paths.append(f\"docs/adr/{sadr.name.removeprefix('SADR-')}\")\n+    return tuple(paths)\n+\n+\n+def preflight_legacy_sadr_cleanup(target: Path, *, force: bool) -> tuple[str, ...]:\n+    \"\"\"Authorize exact legacy Shiki ADR removals before the first write.\n+\n+    A non-force install never removes existing files. A forced install needs no\n+    stamp when no legacy Shiki path is present. When legacy paths do exist, each\n+    must be named in a readable install stamp and its current bytes must match\n+    the recorded digest.\n+    \"\"\"\n+    if not force:\n+        return ()\n+    present = tuple(\n+        relative\n+        for relative in legacy_shiki_adr_paths()\n+        if (target / relative).exists() or (target / relative).is_symlink()\n+    )\n+    if not present:\n+        return ()\n+\n+    stamp_path = target / INSTALL_STAMP_PATH\n+    try:\n+        raw = stamp_path.read_text(encoding=\"utf-8\")\n+        stamp = json.loads(raw)\n+    except FileNotFoundError:\n+        detail = \"install stamp is absent\"\n+        blockers = [f\"{relative}: {detail}\" for relative in present]\n+    except (OSError, UnicodeDecodeError) as error:\n+        detail = f\"install stamp is unreadable ({error})\"\n+        blockers = [f\"{relative}: {detail}\" for relative in present]\n+    except json.JSONDecodeError as error:\n+        detail = f\"install stamp is malformed ({error})\"\n+        blockers = [f\"{relative}: {detail}\" for relative in present]\n+    else:\n+        if not isinstance(stamp, dict):\n+            blockers = [\n+                f\"{relative}: install stamp is malformed (expected an object)\"\n+                for relative in present\n+            ]\n+        else:\n+            digests = stamp.get(\"digests\")\n+            blockers = []\n+            if not isinstance(digests, dict):\n+                blockers.extend(\n+                    f\"{relative}: install stamp has no digest for this path\"\n+                    for relative in present\n+                )\n+            else:\n+                for relative in present:\n+                    expected = digests.get(relative)\n+                    if not isinstance(expected, str) or not expected:\n+                        blockers.append(\n+                            f\"{relative}: install stamp has no digest for this path\"\n+                        )\n+                        continue\n+                    try:\n+                        actual = _sha256_file(target / relative)\n+                    except OSError as error:\n+                        blockers.append(\n+                            f\"{relative}: legacy path is unreadable ({error})\"\n+                        )\n+                        continue\n+                    if actual != expected:\n+                        blockers.append(\n+                            f\"{relative}: digest mismatch (stamp {expected}, current {actual})\"\n+                        )\n+\n+    if blockers:\n+        joined = \"\\n\".join(f\"- {blocker}\" for blocker in blockers)\n+        raise ShikiError(\n+            \"legacy Shiki ADR cleanup is not authorized:\\n\"\n+            f\"{joined}\\nNo files were written.\"\n+        )\n+    return present\n+\n+\n def install_template(target: Path, *, force: bool, validate: bool) -> list[NewFileNote]:\n     # Preconditions first: never rewrite the tree of a target that cannot be\n     # upgraded cleanly (see refuse_pending_migrations).\n+    legacy_cleanup = preflight_legacy_sadr_cleanup(target, force=force)\n     refuse_pending_migrations(target)\n \n     # Resolve the target's required CODEOWNERS owner once from its\n@@ -250,6 +340,10 @@\n     # verbatim. See ``codeowners_required_owner``.\n     codeowners_owner = codeowners_required_owner(target)\n \n+    for relative in legacy_cleanup:\n+        (target / relative).unlink()\n+        info(f\"removed stamped legacy Shiki ADR: {target / relative}\")\n+\n     notes: list[NewFileNote] = []\n     for relative in TEMPLATE_PATHS:\n         source = ROOT / relative\ndiff --git a/tests/test_adr_references.py b/tests/test_adr_references.py\nindex 51706329c797d5dd9b47e082a333fa83b261d482..91ab76b724cb973435c45d53cab63793942267e9\n--- a/tests/test_adr_references.py\n+++ b/tests/test_adr_references.py\n@@ -1,4 +1,4 @@\n-\"\"\"Every ``ADR ####`` citation must resolve to exactly one decision record.\n+\"\"\"Every target ADR and Shiki SADR citation resolves in its own namespace.\n \n Background. ``scripts/mergegate_check.py`` justifies ``is_bookkeeping_closeout``\n with \"ADR 0017\" nine times and ``scripts/guardian_approval_signal.py`` cites\n@@ -11,11 +11,12 @@\n and the directory drifted with no signal.\n \n This is the same unbound-pair shape behind other silent failures: a reference\n-and its target with nothing asserting they agree. This test binds them. It scans\n-every ``scripts/*.py`` (plus ``docs/**/*.md`` and the root ``*.md`` governance\n-files, which are cheap) for the ``ADR ####`` pattern and asserts each cited\n-number resolves to exactly one ``docs/adr/####-*.md``. The converse is\n-deliberately NOT required: an ADR may exist without being cited.\n+and its target with nothing asserting they agree. This test binds them. Target\n+``ADR NNNN`` citations resolve only to ``docs/adr/NNNN-*.md``; Shiki\n+``SADR-NNNN`` citations resolve only to ``docs/adr/SADR-NNNN-*.md``. The scan\n+covers current governance docs, scripts, workflows, prompts, packaging metadata,\n+and repo-local Codex guidance while deliberately excluding historical\n+``.shiki`` evidence. The converse is not required: a record may be uncited.\n \n The suite is dependency free: standard-library ``unittest`` only.\n \"\"\"\n@@ -31,17 +32,18 @@\n \n REPO_ROOT = Path(__file__).resolve().parents[1]\n \n-# \"ADR 0016\" -- a four-digit citation. The trailing \\b keeps a five-digit token\n-# from being read as a truncated four-digit number.\n-_CITATION = re.compile(r\"\\bADR (\\d{4})\\b\")\n-# \"0016-locks-bound-product-paths.md\" -- the leading four digits name the record.\n-_ADR_FILENAME = re.compile(r\"^(\\d{4})-.*\\.md$\")\n+# ``ADR 0016`` / ``SADR-0016`` -- a namespace plus four-digit citation. The\n+# trailing ``\\b`` keeps a five-digit token from being read as a truncated one.\n+_CITATION = re.compile(r\"\\b(S?ADR)[ -](\\d{4})\\b\")\n+# Target ADRs retain the numeric filename; Shiki platform decisions use SADR.\n+_DECISION_FILENAME = re.compile(r\"^(?:(SADR)-)?(\\d{4})-.*\\.md$\")\n \n \n class Violation(NamedTuple):\n     \"\"\"A citation that does not resolve to exactly one decision record.\"\"\"\n \n     citing_file: str  # repo-relative path of the file that cites the ADR\n+    namespace: str  # \"ADR\" for target decisions; \"SADR\" for Shiki decisions\n     number: str  # the cited four-digit number, e.g. \"0016\"\n     line: int  # 1-based line of the citation\n     reason: str  # \"missing\" (zero matches) or \"ambiguous\" (two or more)\n@@ -49,44 +51,59 @@\n \n     @property\n     def expected_path(self) -> str:\n-        return f\"docs/adr/{self.number}-*.md\"\n+        prefix = \"SADR-\" if self.namespace == \"SADR\" else \"\"\n+        return f\"docs/adr/{prefix}{self.number}-*.md\"\n \n     def describe(self) -> str:\n         \"\"\"Name the citing file, the cited number, and the missing path.\"\"\"\n         if self.reason == \"missing\":\n             return (\n-                f\"{self.citing_file}:{self.line} cites ADR {self.number} \"\n+                f\"{self.citing_file}:{self.line} cites {self.namespace} {self.number} \"\n                 f\"but no document matches {self.expected_path}\"\n             )\n         joined = \", \".join(self.matches)\n         return (\n-            f\"{self.citing_file}:{self.line} cites ADR {self.number} which \"\n+            f\"{self.citing_file}:{self.line} cites {self.namespace} {self.number} which \"\n             f\"resolves to {len(self.matches)} documents (expected exactly one \"\n             f\"at {self.expected_path}): {joined}\"\n         )\n \n \n-def _adr_index(root: Path) -> dict[str, list[Path]]:\n-    \"\"\"Map every four-digit ADR number to the record files that claim it.\"\"\"\n-    index: dict[str, list[Path]] = {}\n+def _adr_index(root: Path) -> dict[tuple[str, str], list[Path]]:\n+    \"\"\"Map each decision namespace and number to the files that claim it.\"\"\"\n+    index: dict[tuple[str, str], list[Path]] = {}\n     adr_dir = root / \"docs\" / \"adr\"\n     if adr_dir.is_dir():\n         for path in sorted(adr_dir.glob(\"*.md\")):\n-            match = _ADR_FILENAME.match(path.name)\n+            match = _DECISION_FILENAME.match(path.name)\n             if match:\n-                index.setdefault(match.group(1), []).append(path)\n+                namespace = \"SADR\" if match.group(1) else \"ADR\"\n+                index.setdefault((namespace, match.group(2)), []).append(path)\n     return index\n \n \n def _citing_files(root: Path, include_docs: bool = True) -> list[Path]:\n-    \"\"\"Files scanned for citations: scripts/*.py, and cheaply the markdown.\"\"\"\n+    \"\"\"Current, shipped text surfaces scanned for decision citations.\"\"\"\n     files = sorted((root / \"scripts\").glob(\"*.py\"))\n+    files += sorted((root / \"scripts\").glob(\"*.sh\"))\n     if include_docs:\n         docs = root / \"docs\"\n         if docs.is_dir():\n             files += sorted(docs.rglob(\"*.md\"))\n         files += sorted(root.glob(\"*.md\"))\n-    return files\n+        pyproject = root / \"pyproject.toml\"\n+        if pyproject.is_file():\n+            files.append(pyproject)\n+        for relative in (\".github\", \".codex\"):\n+            surface = root / relative\n+            if surface.is_dir():\n+                files += sorted(\n+                    path\n+                    for path in surface.rglob(\"*\")\n+                    if path.is_file()\n+                    and path.suffix in {\".md\", \".py\", \".sh\", \".toml\", \".yaml\", \".yml\"}\n+                )\n+    return sorted(set(files))\n \n \n def find_adr_reference_violations(\n@@ -94,10 +111,9 @@\n ) -> list[Violation]:\n     \"\"\"Return every ``ADR ####`` citation under ``root`` that fails to resolve.\n \n-    A citation resolves when exactly one ``docs/adr/####-*.md`` matches its\n-    number. Zero matches is a \"missing\" violation; two or more is \"ambiguous\".\n-    Records that are never cited produce nothing -- the converse is not\n-    required.\n+    A citation resolves when exactly one record in the same namespace matches\n+    its number. Zero matches is a \"missing\" violation; two or more is\n+    \"ambiguous\". Records that are never cited produce nothing.\n     \"\"\"\n     root = Path(root)\n     index = _adr_index(root)\n@@ -110,13 +126,15 @@\n         rel = path.relative_to(root).as_posix()\n         for lineno, line in enumerate(text.splitlines(), start=1):\n             for match in _CITATION.finditer(line):\n-                number = match.group(1)\n-                found = index.get(number, [])\n+                namespace, number = match.groups()\n+                found = index.get((namespace, number), [])\n                 if len(found) == 1:\n                     continue\n                 reason = \"missing\" if not found else \"ambiguous\"\n                 rels = [p.relative_to(root).as_posix() for p in found]\n-                violations.append(Violation(rel, number, lineno, reason, rels))\n+                violations.append(\n+                    Violation(rel, namespace, number, lineno, reason, rels)\n+                )\n     return violations\n \n \n@@ -129,8 +147,8 @@\n         self.assertEqual(\n             violations,\n             [],\n-            \"Every 'ADR ####' citation must resolve to exactly one \"\n-            f\"docs/adr/####-*.md:\\n{detail}\",\n+            \"Every ADR/SADR citation must resolve exactly once in its own \"\n+            f\"docs/adr namespace:\\n{detail}\",\n         )\n \n     def test_scan_actually_reads_scripts(self) -> None:\n@@ -146,8 +164,21 @@\n         # The two records this Goal carried onto the default branch, and which\n         # the scripts cite most, must be present.\n         index = _adr_index(REPO_ROOT)\n-        self.assertIn(\"0016\", index)\n-        self.assertIn(\"0017\", index)\n+        self.assertIn((\"SADR\", \"0016\"), index)\n+        self.assertIn((\"SADR\", \"0017\"), index)\n+\n+    def test_platform_tree_has_only_sadr_0001_through_0019(self) -> None:\n+        adr_dir = REPO_ROOT / \"docs\" / \"adr\"\n+        sadrs = sorted(adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\"))\n+        self.assertEqual(\n+            {path.name.split(\"-\", 2)[1] for path in sadrs},\n+            {f\"{number:04d}\" for number in range(1, 20)},\n+        )\n+        self.assertEqual(\n+            list(adr_dir.glob(\"[0-9][0-9][0-9][0-9]-*.md\")),\n+            [],\n+            \"the Shiki platform tree must not retain legacy numeric SADR paths\",\n+        )\n \n \n class FixtureAdrReferences(unittest.TestCase):\n@@ -180,6 +211,26 @@\n         self.assertIn(\"9991\", message)  # cited number\n         self.assertIn(\"docs/adr/9991-*.md\", message)  # missing path\n \n+    def test_target_adr_does_not_satisfy_missing_platform_sadr(self) -> None:\n+        root = self._make_repo(\n+            adr_names=[\"0042-target-decision.md\"],\n+            script_body='\"\"\"Target ADR 0042 exists, but platform SADR-0042 does not.\"\"\"\\n',\n+        )\n+        violations = find_adr_reference_violations(root, include_docs=False)\n+        self.assertEqual(len(violations), 1)\n+        self.assertEqual(violations[0].namespace, \"SADR\")\n+        self.assertEqual(violations[0].number, \"0042\")\n+        self.assertEqual(violations[0].expected_path, \"docs/adr/SADR-0042-*.md\")\n+\n+    def test_target_adr_and_platform_sadr_may_reuse_a_number(self) -> None:\n+        root = self._make_repo(\n+            adr_names=[\"0042-target-decision.md\", \"SADR-0042-platform-decision.md\"],\n+            script_body='\"\"\"Target ADR 0042 and platform SADR-0042 are distinct.\"\"\"\\n',\n+        )\n+        self.assertEqual(\n+            find_adr_reference_violations(root, include_docs=False), []\n+        )\n+\n     def test_uncited_adr_is_allowed(self) -> None:\n         # An ADR exists but nothing cites it -> not a violation. The converse\n         # (every ADR must be cited) is explicitly NOT required.\ndiff --git a/tests/test_sadr_install_migration.py b/tests/test_sadr_install_migration.py\nnew file mode 100644\nindex 0000000000000000000000000000000000000000..5a442fa5e4968ade178dcdedc3c63b54dba580a2\n--- /dev/null\n+++ b/tests/test_sadr_install_migration.py\n@@ -0,0 +1,224 @@\n+\"\"\"SADR installation and collision-safe legacy upgrade behavior.\"\"\"\n+\n+from __future__ import annotations\n+\n+import contextlib\n+import hashlib\n+import io\n+import json\n+import shutil\n+import tempfile\n+import unittest\n+from pathlib import Path\n+\n+import shiki_test_support  # noqa: F401  (path bootstrap)\n+\n+import shiki_installer\n+from tests.test_adr_references import find_adr_reference_violations\n+\n+\n+PRODUCT_NUMBERS = tuple(range(13, 19))\n+\n+\n+def _quiet_install(target: Path, *, force: bool, validate: bool) -> None:\n+    with contextlib.redirect_stdout(io.StringIO()):\n+        shiki_installer.install_template(target, force=force, validate=validate)\n+\n+\n+def _sha256(path: Path) -> str:\n+    return hashlib.sha256(path.read_bytes()).hexdigest()\n+\n+\n+def _legacy_sadr_paths() -> tuple[str, ...]:\n+    return tuple(\n+        f\"docs/adr/{path.name.removeprefix('SADR-')}\"\n+        for path in sorted((shiki_installer.ROOT / \"docs\" / \"adr\").glob(\"SADR-*.md\"))\n+        if path.name != \"SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\"\n+    )\n+\n+\n+def _create_legacy_upgrade_fixture(\n+    target: Path,\n+) -> tuple[dict[str, bytes], tuple[str, ...]]:\n+    adr_dir = target / \"docs\" / \"adr\"\n+    adr_dir.mkdir(parents=True)\n+    product_bytes = {\n+        f\"{number:04d}-cubeshare-product-decision.md\": (\n+            f\"# CubeShare ADR {number:04d}\\n\\nDo not rewrite {number}.\\n\"\n+        ).encode()\n+        for number in PRODUCT_NUMBERS\n+    }\n+    for name, content in product_bytes.items():\n+        (adr_dir / name).write_bytes(content)\n+\n+    legacy_paths = _legacy_sadr_paths()\n+    for relative in legacy_paths:\n+        (target / relative).write_bytes(f\"legacy Shiki bytes for {relative}\\n\".encode())\n+\n+    stamp_path = target / shiki_installer.INSTALL_STAMP_PATH\n+    stamp_path.parent.mkdir(parents=True, exist_ok=True)\n+    stamp = {\n+        \"version\": shiki_installer.INSTALL_STAMP_VERSION,\n+        \"platform_commit\": None,\n+        \"installed_at\": \"2000-01-01T00:00:00+00:00\",\n+        \"digests\": {\n+            relative: _sha256(target / relative) for relative in legacy_paths\n+        },\n+    }\n+    stamp_path.write_text(\n+        json.dumps(stamp, indent=2, sort_keys=True) + \"\\n\", encoding=\"utf-8\"\n+    )\n+    return product_bytes, legacy_paths\n+\n+\n+def _tree_snapshot(root: Path) -> dict[str, tuple[str, bytes | str]]:\n+    \"\"\"Recursive byte/shape snapshot used to prove preflight is write-free.\"\"\"\n+    snapshot: dict[str, tuple[str, bytes | str]] = {}\n+    for path in sorted(root.rglob(\"*\")):\n+        relative = path.relative_to(root).as_posix()\n+        if path.is_symlink():\n+            snapshot[relative] = (\"symlink\", path.readlink().as_posix())\n+        elif path.is_dir():\n+            snapshot[relative] = (\"directory\", \"\")\n+        else:\n+            snapshot[relative] = (\"file\", path.read_bytes())\n+    return snapshot\n+\n+\n+class FreshSadrInstallTests(unittest.TestCase):\n+    def setUp(self) -> None:\n+        self.tmp = Path(tempfile.mkdtemp(prefix=\"shiki-sadr-fresh-\"))\n+        self.addCleanup(lambda: shutil.rmtree(self.tmp, ignore_errors=True))\n+        self.target = self.tmp / \"target\"\n+        self.adr_dir = self.target / \"docs\" / \"adr\"\n+        self.adr_dir.mkdir(parents=True)\n+        self.product_bytes = {\n+            f\"{number:04d}-product-decision.md\": (\n+                f\"# Product ADR {number:04d}\\n\\nTarget-owned bytes {number}.\\n\"\n+            ).encode()\n+            for number in PRODUCT_NUMBERS\n+        }\n+        for name, content in self.product_bytes.items():\n+            (self.adr_dir / name).write_bytes(content)\n+\n+    def test_fresh_install_preserves_product_adrs_and_installs_all_sadrs(self) -> None:\n+        _quiet_install(self.target, force=False, validate=True)\n+\n+        for name, expected in self.product_bytes.items():\n+            self.assertEqual((self.adr_dir / name).read_bytes(), expected)\n+\n+        sadr_numbers = {\n+            path.name.split(\"-\", 2)[1]\n+            for path in self.adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\")\n+        }\n+        self.assertEqual(sadr_numbers, {f\"{number:04d}\" for number in range(1, 20)})\n+\n+        numeric_records = {\n+            path.name\n+            for path in self.adr_dir.glob(\"[0-9][0-9][0-9][0-9]-*.md\")\n+        }\n+        self.assertEqual(numeric_records, set(self.product_bytes))\n+        self.assertEqual(find_adr_reference_violations(self.target), [])\n+\n+\n+class LegacySadrUpgradeTests(unittest.TestCase):\n+    def setUp(self) -> None:\n+        self.tmp = Path(tempfile.mkdtemp(prefix=\"shiki-sadr-upgrade-\"))\n+        self.addCleanup(lambda: shutil.rmtree(self.tmp, ignore_errors=True))\n+        self.target = self.tmp / \"target\"\n+        self.adr_dir = self.target / \"docs\" / \"adr\"\n+        self.product_bytes, self.legacy_paths = _create_legacy_upgrade_fixture(\n+            self.target\n+        )\n+\n+    def test_force_upgrade_deletes_only_stamped_legacy_sadrs(self) -> None:\n+        _quiet_install(self.target, force=True, validate=True)\n+\n+        for relative in self.legacy_paths:\n+            self.assertFalse((self.target / relative).exists(), relative)\n+        for name, expected in self.product_bytes.items():\n+            self.assertEqual((self.adr_dir / name).read_bytes(), expected)\n+\n+        sadrs = list(self.adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\"))\n+        self.assertEqual(len(sadrs), 19)\n+        refreshed = json.loads(\n+            (self.target / shiki_installer.INSTALL_STAMP_PATH).read_text(encoding=\"utf-8\")\n+        )\n+        for relative in self.legacy_paths:\n+            self.assertNotIn(relative, refreshed[\"digests\"])\n+        for path in sadrs:\n+            relative = path.relative_to(self.target).as_posix()\n+            self.assertEqual(refreshed[\"digests\"][relative], _sha256(path))\n+\n+    def test_non_force_install_never_deletes_legacy_files(self) -> None:\n+        before = {\n+            relative: (self.target / relative).read_bytes()\n+            for relative in self.legacy_paths\n+        }\n+        _quiet_install(self.target, force=False, validate=False)\n+        for relative, expected in before.items():\n+            self.assertEqual((self.target / relative).read_bytes(), expected)\n+\n+\n+class LegacyCleanupPreflightTests(unittest.TestCase):\n+    def setUp(self) -> None:\n+        self.tmp = Path(tempfile.mkdtemp(prefix=\"shiki-sadr-preflight-\"))\n+        self.addCleanup(lambda: shutil.rmtree(self.tmp, ignore_errors=True))\n+\n+    def test_all_ownership_blockers_abort_before_any_write(self) -> None:\n+        cases = (\n+            (\"absent\", \"install stamp is absent\"),\n+            (\"unreadable\", \"install stamp is unreadable\"),\n+            (\"malformed\", \"install stamp is malformed\"),\n+            (\"missing-digest\", \"install stamp has no digest\"),\n+            (\"digest-mismatch\", \"digest mismatch\"),\n+        )\n+        for name, expected_reason in cases:\n+            with self.subTest(name=name):\n+                target = self.tmp / name\n+                _, legacy_paths = _create_legacy_upgrade_fixture(target)\n+                stamp_path = target / shiki_installer.INSTALL_STAMP_PATH\n+\n+                if name == \"absent\":\n+                    stamp_path.unlink()\n+                elif name == \"unreadable\":\n+                    stamp_path.unlink()\n+                    stamp_path.mkdir()\n+                elif name == \"malformed\":\n+                    stamp_path.write_text(\"{not-json\\n\", encoding=\"utf-8\")\n+                else:\n+                    stamp = json.loads(stamp_path.read_text(encoding=\"utf-8\"))\n+                    if name == \"missing-digest\":\n+                        stamp[\"digests\"] = {}\n+                    else:\n+                        stamp[\"digests\"] = {\n+                            relative: \"deadbeef\" for relative in legacy_paths\n+                        }\n+                    stamp_path.write_text(\n+                        json.dumps(stamp, indent=2, sort_keys=True) + \"\\n\",\n+                        encoding=\"utf-8\",\n+                    )\n+\n+                before = _tree_snapshot(target)\n+                with self.assertRaises(shiki_installer.ShikiError) as raised:\n+                    _quiet_install(target, force=True, validate=False)\n+                after = _tree_snapshot(target)\n+\n+                self.assertEqual(before, after, f\"{name} preflight changed the tree\")\n+                message = str(raised.exception)\n+                self.assertIn(expected_reason, message)\n+                self.assertIn(\"No files were written\", message)\n+                for relative in legacy_paths:\n+                    self.assertIn(relative, message)\n+\n+    def test_absent_stamp_is_allowed_when_no_legacy_path_exists(self) -> None:\n+        target = self.tmp / \"no-legacy\"\n+        target.mkdir()\n+        _quiet_install(target, force=True, validate=False)\n+        self.assertTrue(\n+            (target / \"docs\" / \"adr\" / \"SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\").is_file()\n+        )\n+\n+\n+if __name__ == \"__main__\":\n+    unittest.main()\n\n2026-08-07T08:12:56.984149Z ERROR codex_models_manager::manager: failed to renew cache TTL: missing field `base_instructions` at line 94 column 5\nexec\n/bin/zsh -lc \"sed -n '224,242p' scripts/test_shiki_goal_loop.sh; sed -n '1,14p' tests/test_policy_job_merged_prs.py; sed -n '38,48p' docs/agents/autonomous-loop-e2e-acceptance.md; sed -n '96,106p' docs/agents/guardian-policy.md; sed -n '196,207p' scripts/guardian_approval_signal.py\" in /private/tmp/shiki-adr-collision.pa0KwY/.worktrees/shiki-t-20260807t073307221254z-2d22d654-introduc\n succeeded in 0ms:\npython3 \"$ROOT/scripts/shiki.py\" loop run --target \"$TARGET\" --goal-id \"$GOAL_ID\" --max-cycles 14 --interval 0 >/tmp/shiki-goal-loop-result.json\ntest \"$(json_get_last /tmp/shiki-goal-loop-result.json outcome)\" = \"complete\"\ngrep '\"status\": \"complete\"' \"$TARGET/.shiki/goals/$GOAL_ID.json\" >/dev/null\n# The loop opened a closeout PR (SADR-0012): its history records create_closeout_pr.\ngrep '\"action\": \"create_closeout_pr\"' /tmp/shiki-goal-loop-result.json >/dev/null\n# Impl PR + closeout PR both auto-merged.\ntest \"$(grep -c merged \"$GH_STATE/gh-log\")\" = \"2\"\n\n# High/critical risk merges autonomously when all required checks are green:\n# the \"MergeGate policy check\" required check IS the Guardian gate (it enforces\n# guardian-policy.json \u2014 human approval OR external AI guardian review, ADR\n# 0010), so green checks mean a recorded authority approved. The fake gh returns\n# green checks, simulating that the Guardian gate passed.\npython3 \"$ROOT/scripts/shiki.py\" goal create --target \"$TARGET\" --title \"Guardian gate\" --outcome \"High risk merges via policy gate\" >/tmp/shiki-goal-loop-guardian-goal.json\nGGOAL=\"$(json_get /tmp/shiki-goal-loop-guardian-goal.json goal_id)\"\npython3 \"$ROOT/scripts/shiki.py\" issue plan --target \"$TARGET\" --goal-id \"$GGOAL\" \\\n  --title \"High risk slice\" --scope \"Needs guardian\" --risk-level high \\\n  --acceptance-check \"Guardian approves\" >/tmp/shiki-goal-loop-guardian-task.json\nGTASK=\"$(json_get /tmp/shiki-goal-loop-guardian-task.json task_id)\"\n\"\"\"The MergeGate policy job must resolve the implementation-PR merge proof for\nEVERY judged PR, not only ``post_merge_reconcile`` ones (SADR-0017).\n\n``.github/workflows/shiki-cca-completion.yml`` has two consumers of the merged-PR\nproof: the CCA *signal* (``guardian_approval_signal.py`` in \"Prepare CCA context\")\nand the authoritative gate (``mergegate_check.py`` in \"Enforce MergeGate\"). ADR\n0017's bookkeeping-closeout exemption (condition 6) needs the merged implementation\nPR proven, and a closeout carries NO ``post_merge_reconcile`` marker. The signal\nalready resolves ``MERGED_PRS`` unconditionally, but \"Enforce MergeGate\" used to\nresolve it only inside ``grep -q '<!-- shiki:post_merge_reconcile -->'``. So a\nhigh/critical closeout could show the CCA signal exempt (``required: false``) while\nthe policy check still blocked with \"Guardian label 'guardian:approved' is missing\"\n-- one gate exempted the PR, the other did not, and the exemption could never\nproduce a merge without an operator approval (PR #212, PR #213).\n2. `claude --version` and `claude auth status` report a logged-in subscription\n   session (the runner and the reviewer both dispatch through it).\n3. `gh auth status` is logged in with merge permission on the repo.\n4. Branch protection + required checks are configured exactly as the goal-loop\n   required-check set expects (see `docs/agents/branch-protection-smoke.md`).\n5. The repo `.shiki/policy` permits autonomous low/medium-risk merges (ADR\n   0008/0009); the MergeGate policy check is the recorded-authority gate.\n\nThe operator's only actions are: register the task and start the loop. **No\nmanual pre-clearing** means after `shiki loop run` starts, the operator does not\ntouch the PR, checks, labels, evidence, or merge button.\npolicy checks \u2014 and `validate_shiki`'s workflow-invocation contract pins the flags on\neach so a future edit cannot drop them silently. The proof reads git history over\n`origin/<default_branch>` and reuses each job's existing base fetch; a shallow\n(`--depth`) or second base fetch would make the proof unresolvable and silently\ndisable the carry, so neither is permitted. CCA reads `guardian_comment_carried` as\na recorded authority in the deterministic signal and does not re-derive it. See ADR\n0018 for the full decision, the rejected diff/patch-equality and path-disjointness\nalternatives, and the accepted residual.\n\n## External AI Guardian Review (SADR-0010)\n\n    base_shiki: str,\n    changed_files_status: str,\n    contract_approval: str,\n) -> Any:\n    \"\"\"Evaluate SADR-0015 Contract Approval for the CCA Guardian signal.\n\n    The SAME OR that MergeGate applies must reach this deterministic signal: ADR\n    0015 is explicit that without it, a contract-approved PR still returns\n    ``needs_guardian``, the CCA job fails, and the MergeGate policy check never\n    runs \u2014 the gate this change moves would never be reached. This resolves the\n    task the SAME way MergeGate does (the first Shiki task id in the PR body),\n    loads the head/base task snapshots and the registration proof, and calls the\n\ndiff --git a/.codex/skills/shiki/SKILL.md b/.codex/skills/shiki/SKILL.md\nindex c8e2e85d9d75d49cc4f2d6286ee22659c4e53e75..93cded623b2c15d9f65c76491a1c2d813ac9e6c0\n--- a/.codex/skills/shiki/SKILL.md\n+++ b/.codex/skills/shiki/SKILL.md\n@@ -18,6 +18,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n Codex CLI does not register this skill as a custom `/shiki` slash command. When\n the user types `/shiki` in Codex CLI and gets \"Unrecognized command\", explain\n that this is expected: invoke Shiki in Codex with natural language such as\n@@ -69,7 +72,7 @@\n \n ## Responsibilities\n \n-- Claude Code implements and repairs by default (`shiki runner claude`, ADR 0008); Codex implements and repairs tasks explicitly assigned to `codex`.\n+- Claude Code implements and repairs by default (`shiki runner claude`, SADR-0008); Codex implements and repairs tasks explicitly assigned to `codex`.\n - Claude Code Action can act as GitHub-side CCA or reviewer.\n - CCA judges completion.\n - MergeGate authorizes state transitions and merge readiness.\ndiff --git a/.github/prompts/cca-completion-check.md b/.github/prompts/cca-completion-check.md\nindex ed203ad5f302ecc81372d8ab27d2b1232ca97497..fd89950528333ef6242e6d5443904f5392ab7e56\n--- a/.github/prompts/cca-completion-check.md\n+++ b/.github/prompts/cca-completion-check.md\n@@ -19,6 +19,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n ## Required Reading\n \n Read only the files needed to judge the PR contract. Prefer this order:\ndiff --git a/AGENTS.md b/AGENTS.md\nindex 7f041100d49c2a7525f51cb0f5d696a4a6d1c78c..2b700e989ff295987aa8a4b0c5fcb1acd80020da\n--- a/AGENTS.md\n+++ b/AGENTS.md\n@@ -44,6 +44,10 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use\n+`SADR-NNNN-*.md`; target product decisions retain `NNNN-*.md` and the `ADR`\n+namespace.\n+\n Use the canonical source-of-truth order above. Runtime-specific reading orders\n are operational guidance only and must not override or reorder it.\n \ndiff --git a/CLAUDE.md b/CLAUDE.md\nindex 98a932e7841a632866fcd53b0bad0e3af3711d3d..611d491912d6a72a0327a1731b8f33a41acb4baa\n--- a/CLAUDE.md\n+++ b/CLAUDE.md\n@@ -14,6 +14,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n ## Claude Role\n \n Default to planner, default implementer, reviewer, coordinator, documentation editor, CCA-style completion judge, and final-judgment assistant.\ndiff --git a/SYSTEM_PROMPT.md b/SYSTEM_PROMPT.md\nindex aaaea45e3a896c3a44ba89eec8bb84325ecf6c62..2e7c093ebe9e8bb34e0923756f191b4f7fa581f5\n--- a/SYSTEM_PROMPT.md\n+++ b/SYSTEM_PROMPT.md\n@@ -30,6 +30,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n Use the canonical source-of-truth order above. Runtime-specific reading orders\n are operational guidance only and must not override or reorder it. The current\n conversation is non-durable operator input.\ndiff --git a/docs/adr/0001-keep-shiki-as-platform-name.md b/docs/adr/SADR-0001-keep-shiki-as-platform-name.md\nindex 20a9637c3b9d8c97de7ba8db7cfae4d82d9a7dba..8b685439959aada1b221c112f0ac3c51ca8a1695\n--- a/docs/adr/0001-keep-shiki-as-platform-name.md\n+++ b/docs/adr/SADR-0001-keep-shiki-as-platform-name.md\n@@ -1,3 +1,3 @@\n-# Keep Shiki As The Platform Name\n+# SADR-0001: Keep Shiki As The Platform Name\n \n Shiki remains the product name for the GitHub-native agentic engineering control plane. We considered more descriptive names such as MergePilot, MergeGate, and Kiroku, but kept Shiki because it carries the existing goal-seeking framework identity while remaining independent of Hermes, Codex, Claude Code, or any single runtime.\ndiff --git a/docs/adr/0002-use-agents-as-shared-constitution.md b/docs/adr/SADR-0002-use-agents-as-shared-constitution.md\nindex 73de30ecb078120eac540d9bc51855e3cd48d0e3..c5486182bf06fca36ea508496b013c67302fd989\n--- a/docs/adr/0002-use-agents-as-shared-constitution.md\n+++ b/docs/adr/SADR-0002-use-agents-as-shared-constitution.md\n@@ -1,3 +1,3 @@\n-# Use AGENTS As The Shared Agent Constitution\n+# SADR-0002: Use AGENTS As The Shared Agent Constitution\n \n Shiki uses `AGENTS.md` as the runtime-neutral constitution and `CLAUDE.md` as a thin Claude Code wrapper. This keeps Codex, Claude Code, Hermes Runner, and future Agent Runtimes aligned on the same GitHub-first MergeGate rules while still allowing runtime-specific instructions where needed.\ndiff --git a/docs/adr/0003-require-skill-gate-for-mergegate-readiness.md b/docs/adr/SADR-0003-require-skill-gate-for-mergegate-readiness.md\nindex 424be0c9af4c8f0136f1fde68a468c47ff84e764..7fc337987c7df9792f75f181dfcbed75fe5ffff2\n--- a/docs/adr/0003-require-skill-gate-for-mergegate-readiness.md\n+++ b/docs/adr/SADR-0003-require-skill-gate-for-mergegate-readiness.md\n@@ -1,3 +1,3 @@\n-# Require Skill Gate For MergeGate Readiness\n+# SADR-0003: Require Skill Gate For MergeGate Readiness\n \n Shiki requires relevant engineering skills to be invoked before execution when their triggers apply. This makes skills part of MergeGate readiness rather than optional prompt style, because Shiki depends on repeatable planning, issue decomposition, TDD, diagnosis, triage, and architecture review before autonomous or parallel agent work can be trusted.\ndiff --git a/docs/adr/0004-use-github-first-with-shiki-mirror.md b/docs/adr/SADR-0004-use-github-first-with-shiki-mirror.md\nindex d0a3a9174dc646a945a9d325954ffaf00c352040..a79c54ae9ee8eed2a0524192b864da4e22a92d3f\n--- a/docs/adr/0004-use-github-first-with-shiki-mirror.md\n+++ b/docs/adr/SADR-0004-use-github-first-with-shiki-mirror.md\n@@ -1,4 +1,4 @@\n-# ADR 0004: Use GitHub First With A Repository-Local Shiki Mirror\n+# SADR-0004: Use GitHub First With A Repository-Local Shiki Mirror\n \n ## Status\n \ndiff --git a/docs/adr/0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md b/docs/adr/SADR-0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\nindex b339baf49b89d9290e79c3d51b36e7b2bde3bd95..c30a0d8b1fde0224d0ac7820e7870dccf721580d\n--- a/docs/adr/0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\n+++ b/docs/adr/SADR-0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\n@@ -1,4 +1,4 @@\n-# ADR 0005: Use Codex As Front And Claude Code Action As The GitHub Runtime\n+# SADR-0005: Use Codex As Front And Claude Code Action As The GitHub Runtime\n \n ## Status\n \ndiff --git a/docs/adr/0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md b/docs/adr/SADR-0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\nindex 4bcae9d1e52f0a37b8e9fbafa5cd14b6ee1993bd..7a38538a6ae9929567f0ade67e7ea6a60d0b3f7e\n--- a/docs/adr/0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\n+++ b/docs/adr/SADR-0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\n@@ -1,4 +1,4 @@\n-# ADR 0006: Separate Codex Implementation, CCA Completion, And MergeGate Authorization\n+# SADR-0006: Separate Codex Implementation, CCA Completion, And MergeGate Authorization\n \n ## Status\n \ndiff --git a/docs/adr/0007-packaging-and-release.md b/docs/adr/SADR-0007-packaging-and-release.md\nindex 79b84ddb97167372b44641e5b7860e400ce80822..023cd57f8011aa6e6d2ebf612b7c35a7a82c00d4\n--- a/docs/adr/0007-packaging-and-release.md\n+++ b/docs/adr/SADR-0007-packaging-and-release.md\n@@ -1,4 +1,4 @@\n-# ADR 0007: Packaging And Release For Shiki\n+# SADR-0007: Packaging And Release For Shiki\n \n ## Status\n \ndiff --git a/docs/adr/0008-use-claude-code-as-default-implementer.md b/docs/adr/SADR-0008-use-claude-code-as-default-implementer.md\nindex dedf4510b7c7799521422f622029563d373db031..a158a8d444d93764ec515a28133f7dde4af23579\n--- a/docs/adr/0008-use-claude-code-as-default-implementer.md\n+++ b/docs/adr/SADR-0008-use-claude-code-as-default-implementer.md\n@@ -1,4 +1,4 @@\n-# ADR 0008: Use Claude Code As The Default Implementer Runtime\n+# SADR-0008: Use Claude Code As The Default Implementer Runtime\n \n ## Status\n \ndiff --git a/docs/adr/0009-requirements-definition-and-spec-freeze.md b/docs/adr/SADR-0009-requirements-definition-and-spec-freeze.md\nindex 29b3f7871bef9203a8b4a831a65c39eb2f506e42..52e7dd69ad0aa4efe0b68c62678abbf7df02aaec\n--- a/docs/adr/0009-requirements-definition-and-spec-freeze.md\n+++ b/docs/adr/SADR-0009-requirements-definition-and-spec-freeze.md\n@@ -1,4 +1,4 @@\n-# ADR 0009: Merge Grill And Planning Into Requirements Definition With Spec Freeze\n+# SADR-0009: Merge Grill And Planning Into Requirements Definition With Spec Freeze\n \n ## Status\n \ndiff --git a/docs/adr/0010-external-ai-guardian-review.md b/docs/adr/SADR-0010-external-ai-guardian-review.md\nindex e6108bbe8bb4e73e0ff4684c8f78001a80438c4e..e690d4eddd270e683d709c9b679c5859e71eeaaa\n--- a/docs/adr/0010-external-ai-guardian-review.md\n+++ b/docs/adr/SADR-0010-external-ai-guardian-review.md\n@@ -1,4 +1,4 @@\n-# ADR 0010: External AI Guardian Review As A First-Class Approval Authority\n+# SADR-0010: External AI Guardian Review As A First-Class Approval Authority\n \n ## Status\n \ndiff --git a/docs/adr/0011-autonomous-quality-gate-evidence.md b/docs/adr/SADR-0011-autonomous-quality-gate-evidence.md\nindex bca0246c5d46e44afbde15486a75eec425fc02f5..9c93eac16d8c3a6d4b11bd7224e3955102cd7db4\n--- a/docs/adr/0011-autonomous-quality-gate-evidence.md\n+++ b/docs/adr/SADR-0011-autonomous-quality-gate-evidence.md\n@@ -1,4 +1,4 @@\n-# ADR 0011: Autonomous Quality-Gate Evidence \u2014 Deterministic Facts And An Independent Verifier\n+# SADR-0011: Autonomous Quality-Gate Evidence \u2014 Deterministic Facts And An Independent Verifier\n \n ## Status\n \ndiff --git a/docs/adr/0012-autonomous-loop-closeout-to-main.md b/docs/adr/SADR-0012-autonomous-loop-closeout-to-main.md\nindex 637fad6dde4989c9c2bf0f81f676d8d29991123c..ec7d85f81030a676bcbba28b43eb365422a36eac\n--- a/docs/adr/0012-autonomous-loop-closeout-to-main.md\n+++ b/docs/adr/SADR-0012-autonomous-loop-closeout-to-main.md\n@@ -1,4 +1,4 @@\n-# 0012 \u2014 Autonomous loop pushes goal completion to main via a closeout PR\n+# SADR-0012 \u2014 Autonomous loop pushes goal completion to main via a closeout PR\n \n - Status: accepted\n - Date: 2026-06-18\ndiff --git a/docs/adr/0013-bootstrap-grants-actions-approve-pull-requests.md b/docs/adr/SADR-0013-bootstrap-grants-actions-approve-pull-requests.md\nindex b6c69d64f1c5ced346b88d6859586715b3b5bd0e..a793e7277d67ab8ea56286fec289ca689825a17d\n--- a/docs/adr/0013-bootstrap-grants-actions-approve-pull-requests.md\n+++ b/docs/adr/SADR-0013-bootstrap-grants-actions-approve-pull-requests.md\n@@ -1,4 +1,4 @@\n-# ADR 0013: Bootstrap Grants GitHub Actions Permission To Create And Approve Pull Requests\n+# SADR-0013: Bootstrap Grants GitHub Actions Permission To Create And Approve Pull Requests\n \n ## Status\n \ndiff --git a/docs/adr/0014-codex-app-external-ai-guardian-ui-adapter.md b/docs/adr/SADR-0014-codex-app-external-ai-guardian-ui-adapter.md\nindex 238db06279334caa341c117362df9271382a7fcd..65f12e9939321e1b3e389a34c822fc1549e5155d\n--- a/docs/adr/0014-codex-app-external-ai-guardian-ui-adapter.md\n+++ b/docs/adr/SADR-0014-codex-app-external-ai-guardian-ui-adapter.md\n@@ -1,4 +1,4 @@\n-# ADR 0014: Use Codex App As The External AI Guardian UI Adapter\n+# SADR-0014: Use Codex App As The External AI Guardian UI Adapter\n \n ## Status\n \ndiff --git a/docs/adr/0015-contract-approval-before-dispatch.md b/docs/adr/SADR-0015-contract-approval-before-dispatch.md\nindex fad6c656741a20a7684ba16080a7db159f63a52f..7bdd760bb3e82eee4e1410f45c783b03b638d456\n--- a/docs/adr/0015-contract-approval-before-dispatch.md\n+++ b/docs/adr/SADR-0015-contract-approval-before-dispatch.md\n@@ -1,4 +1,4 @@\n-# ADR 0015: Approve The Task Contract Before Dispatch, Not The Diff After It\n+# SADR-0015: Approve The Task Contract Before Dispatch, Not The Diff After It\n \n ## Status\n \ndiff --git a/docs/adr/0016-locks-bound-product-paths.md b/docs/adr/SADR-0016-locks-bound-product-paths.md\nindex b5e850f3f1f7f4ef6e7f9d1763a29a51bd300c45..332cc17313498404f41a145291e38447b5de0dc4\n--- a/docs/adr/0016-locks-bound-product-paths.md\n+++ b/docs/adr/SADR-0016-locks-bound-product-paths.md\n@@ -1,4 +1,4 @@\n-# ADR 0016: Locks bound product paths; the Shiki mirror is bound by record identity\n+# SADR-0016: Locks bound product paths; the Shiki mirror is bound by record identity\n \n ## Status\n \ndiff --git a/docs/adr/0017-bookkeeping-closeouts-do-not-inherit-risk.md b/docs/adr/SADR-0017-bookkeeping-closeouts-do-not-inherit-risk.md\nindex 554e3a0d526a72ca067108c5464cc68c3edcf174..83115f69f1b1b48d544c9b0157f377b91cb53abf\n--- a/docs/adr/0017-bookkeeping-closeouts-do-not-inherit-risk.md\n+++ b/docs/adr/SADR-0017-bookkeeping-closeouts-do-not-inherit-risk.md\n@@ -1,4 +1,4 @@\n-# ADR 0017: A Bookkeeping Closeout Does Not Inherit Its Task's Risk\n+# SADR-0017: A Bookkeeping Closeout Does Not Inherit Its Task's Risk\n \n ## Status\n \ndiff --git a/docs/adr/0018-carried-guardian-approval-across-a-proven-base-sync.md b/docs/adr/SADR-0018-carried-guardian-approval-across-a-proven-base-sync.md\nindex 22358562da390ed8b45cd600da08a43d378b4680..38da0fd95102c3496c3219ce2319fce696a2532a\n--- a/docs/adr/0018-carried-guardian-approval-across-a-proven-base-sync.md\n+++ b/docs/adr/SADR-0018-carried-guardian-approval-across-a-proven-base-sync.md\n@@ -1,4 +1,4 @@\n-# ADR 0018: Carried Guardian Approval Across A Proven Base Sync\n+# SADR-0018: Carried Guardian Approval Across A Proven Base Sync\n \n ## Status\n \ndiff --git a/docs/adr/README.md b/docs/adr/README.md\nindex c70e057a4520c36e042ab68c53cd4a21ef709b71..5da43856836e2ce6a150f8127766e2f88355ab4b\n--- a/docs/adr/README.md\n+++ b/docs/adr/README.md\n@@ -1,40 +1,41 @@\n-# Architecture Decision Records\n+# Decision Record Namespaces\n \n-This directory holds Shiki's Architecture Decision Records (ADRs): durable\n-records of hard-to-reverse platform decisions. ADRs are part of the Shiki source\n-of truth alongside GitHub state, the `.shiki/` mirror, and `CONTEXT.md`.\n+This directory holds two independent decision namespaces:\n \n-## How ADRs Work\n+- Shiki Architecture Decision Records (SADRs), named\n+  `SADR-NNNN-<short-slug>.md`, record hard-to-reverse Shiki platform decisions.\n+- Target Architecture Decision Records (ADRs), named\n+  `NNNN-<short-slug>.md`, belong to the target product.\n \n-- New ADRs are added as `docs/adr/NNNN-<short-slug>.md`, where `NNNN` is the next\n-  zero-padded sequence number.\n-- The **canonical list of ADRs is the set of files matching `docs/adr/*.md`** in\n-  this directory. Browse `docs/adr/*.md` for the authoritative, current set.\n-- Use [`template.md`](template.md) as the starting point for a new ADR.\n-- Record an ADR when a decision is hard to reverse, surprising without context,\n-  and a real tradeoff. Do not write an ADR for routine or easily reversible\n-  choices.\n+The same four-digit number may appear once in each namespace. A platform\n+`SADR-NNNN` citation resolves only to `SADR-NNNN-*.md`; a target `ADR NNNN`\n+citation resolves only to `NNNN-*.md`.\n \n-Using the `docs/adr/*.md` glob as the source of truth means other PRs can add an\n-ADR without editing this index file, avoiding merge contention on a\n-hand-maintained enumeration.\n+## How Decision Records Work\n \n-## Current ADRs (snapshot)\n-\n-The list below is a convenience snapshot and may lag behind the directory. When\n-in doubt, list `docs/adr/*.md`.\n+- New Shiki platform decisions use the next zero-padded SADR number.\n+- New target product decisions use the target's own ADR sequence.\n+- The canonical list is the files in `docs/adr/`; no hand-maintained index is\n+  authoritative.\n+- Use [`template.md`](template.md) as the starting point and choose the filename\n+  namespace that owns the decision.\n+- Record a decision when it is hard to reverse, surprising without context, and\n+  a real tradeoff. Do not write a decision record for routine or easily\n+  reversible choices.\n \n-- [0001 \u2014 Keep Shiki As The Platform Name](0001-keep-shiki-as-platform-name.md)\n-- [0002 \u2014 Use AGENTS As The Shared Agent Constitution](0002-use-agents-as-shared-constitution.md)\n-- [0003 \u2014 Require Skill Gate For MergeGate Readiness](0003-require-skill-gate-for-mergegate-readiness.md)\n-- [0004 \u2014 Use GitHub First With A Repository-Local Shiki Mirror](0004-use-github-first-with-shiki-mirror.md)\n-- [0005 \u2014 Use Codex As Front And Claude Code Action As The GitHub Runtime](0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md)\n-- [0006 \u2014 Separate Codex Implementation, CCA Completion, And MergeGate Authorization](0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md)\n+Using the directory as the source of truth lets independent changes add records\n+without contending on an enumerated index.\n \n-## Adding A New ADR\n+## Adding A Shiki SADR\n \n-1. Copy `template.md` to `docs/adr/NNNN-<short-slug>.md`.\n+1. Copy `template.md` to `docs/adr/SADR-NNNN-<short-slug>.md`.\n 2. Fill in the title, status, context, decision, and consequences.\n-3. Set the status to `Proposed`, then `Accepted` once the decision is approved.\n-4. Open a PR following `CONTRIBUTING.md`. You do not need to edit this index for\n-   the ADR to be discoverable via `docs/adr/*.md`.\n+3. Set the status to `Proposed`, then `Accepted` once the proper authority has\n+   approved the decision.\n+4. Open a PR following `CONTRIBUTING.md`.\n+\n+## Adding A Target ADR\n+\n+1. Copy `template.md` to `docs/adr/NNNN-<short-slug>.md` using the target's own\n+   sequence.\n+2. Cite it as `ADR NNNN`; do not prefix its filename with `SADR-`.\ndiff --git a/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md b/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\nnew file mode 100644\nindex 0000000000000000000000000000000000000000..7455ce1f3bed02ddddb851ac21a1d1a648c9aeba\n--- /dev/null\n+++ b/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\n@@ -0,0 +1,55 @@\n+# SADR-0019: Separate Shiki Platform SADRs From Target ADRs\n+\n+## Status\n+\n+Accepted\n+\n+## Context\n+\n+Shiki installs its platform decision records into `docs/adr/` in every target\n+repository. Shiki and its targets previously used the same numeric\n+`NNNN-<slug>.md` namespace. A target that already owned ADRs 0013 through 0018\n+therefore acquired two records for each number during installation, making an\n+`ADR NNNN` citation ambiguous. Renumbering target decisions would rewrite target\n+history, while renumbering Shiki decisions per target would destroy their stable\n+platform identity.\n+\n+Upgrades also need to remove the former Shiki-owned numeric paths without ever\n+mistaking a target-owned ADR for platform content. The install stamp already\n+records exact shipped paths and content digests, so it can prove ownership only\n+when both still match.\n+\n+## Decision\n+\n+Shiki platform decisions use the `SADR` namespace:\n+\n+- records are named `docs/adr/SADR-NNNN-<slug>.md`;\n+- live platform citations use `SADR-NNNN`;\n+- target product decisions retain `docs/adr/NNNN-<slug>.md` and `ADR NNNN`;\n+- the reference validator resolves the two namespaces independently, so the same\n+  four-digit number may exist once in each namespace.\n+\n+Before a forced upgrade writes anything, the installer inventories the exact\n+former numeric Shiki ADR paths. It may delete a legacy path only when the\n+existing install stamp contains that exact path and its recorded SHA-256 digest\n+matches the file currently on disk. All ownership blockers are reported\n+together and abort the upgrade before its first write. A non-forced install\n+never performs this cleanup.\n+\n+Historical `.shiki/` evidence remains unchanged because it records the namespace\n+that existed when that evidence was produced.\n+\n+## Consequences\n+\n+- Targets may use any ADR number without colliding with Shiki's platform SADRs.\n+- Fresh installs preserve target ADR bytes and install a stable SADR set.\n+- Forced upgrades fail closed when legacy ownership cannot be proven, and a\n+  successful upgrade refreshes the stamp without removed legacy paths.\n+- Current governance, packaging, workflow, prompt, script, and skill references\n+  must move with the platform records.\n+\n+## Alternatives Considered\n+\n+- Collision-only refusal leaves affected targets permanently unupgradable.\n+- Renumbering target ADRs mutates target-owned history.\n+- Per-target Shiki renumbering makes platform citations non-portable.\ndiff --git a/docs/adr/template.md b/docs/adr/template.md\nindex a92a97daddd0b00ab9dd626cc8d86942da2eafed..17dd79175b5fee2105935557f691b40cf9492942\n--- a/docs/adr/template.md\n+++ b/docs/adr/template.md\n@@ -1,14 +1,14 @@\n-# NNNN: <Short Decision Title>\n+# <SADR-NNNN or NNNN>: <Short Decision Title>\n \n ## Status\n \n-Proposed | Accepted | Superseded by ADR-NNNN\n+Proposed | Accepted | Superseded by SADR-NNNN or target ADR NNNN\n \n ## Context\n \n What is the situation, constraint, or force that requires a decision? Reference\n-relevant domain terms (`CONTEXT.md`), prior ADRs, and code reality. State the\n-problem, not the solution.\n+relevant domain terms (`CONTEXT.md`), prior Shiki SADRs or target ADRs, and code\n+reality. State the problem, not the solution.\n \n ## Decision\n \ndiff --git a/docs/agents/domain.md b/docs/agents/domain.md\nindex 258da8ea81e87ac0a6b0b5b5e5c2fde980eaabb7..67aaa6f4b7d0a5aa4d495ce6279073333f7798cb\n--- a/docs/agents/domain.md\n+++ b/docs/agents/domain.md\n@@ -5,7 +5,9 @@\n ## Layout\n \n - `CONTEXT.md` defines Shiki language. Keep it implementation-free.\n-- `docs/adr/` records hard-to-reverse decisions.\n+- `docs/adr/SADR-NNNN-*.md` records Shiki platform decisions.\n+- `docs/adr/NNNN-*.md` records target product decisions without sharing the\n+  SADR namespace.\n - `AGENTS.md` is the runtime-neutral agent constitution.\n - `CLAUDE.md` is the Claude Code wrapper around `AGENTS.md`.\n - `.shiki/` is the Target Repository mirror for Goals, PRDs, plans, Task DAGs, contracts, locks, worktree records, ledger entries, CCA verdicts, repair packets, reports, and handoffs.\ndiff --git a/docs/agents/implementation-policy.md b/docs/agents/implementation-policy.md\nindex c754144a9fb5bf4a38aa82a60e1670c59e626b44..e90d40fe21880b9a09719c5ecfe6bde7284eb3ce\n--- a/docs/agents/implementation-policy.md\n+++ b/docs/agents/implementation-policy.md\n@@ -31,6 +31,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n ## Non-Negotiables\n \n 1. **Planning precedes implementation.** Non-trivial Goals start with `grill-with-docs` and Context & Impact.\ndiff --git a/scripts/shiki_installer.py b/scripts/shiki_installer.py\nindex be064ab7a29ff6836f7f7a1d70500a43e0d11d7b..d3e3247edbf3e4be287a4667176d86f6500f8dc5\n--- a/scripts/shiki_installer.py\n+++ b/scripts/shiki_installer.py\n@@ -118,6 +118,7 @@\n # what lets a target carry it without a governance-schema change.\n INSTALL_STAMP_PATH = \".shiki/install-stamp.json\"\n INSTALL_STAMP_VERSION = 1\n+LEGACY_SHIKI_SADR_MAX = 18\n \n # Under --force the shipped surface splits three ways, so nothing is lost and\n # nothing is silently stale.\n@@ -237,9 +238,98 @@\n         )\n \n \n+def legacy_shiki_adr_paths() -> tuple[str, ...]:\n+    \"\"\"Former numeric paths for Shiki SADR-0001 through SADR-0018.\n+\n+    The authoritative slugs come from the current SADR filenames. SADR-0019 is\n+    the namespace decision itself and never shipped under a numeric legacy path.\n+    \"\"\"\n+    paths: list[str] = []\n+    for sadr in sorted((ROOT / \"docs\" / \"adr\").glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\")):\n+        number = sadr.name.split(\"-\", 2)[1]\n+        if int(number) <= LEGACY_SHIKI_SADR_MAX:\n+            paths.append(f\"docs/adr/{sadr.name.removeprefix('SADR-')}\")\n+    return tuple(paths)\n+\n+\n+def preflight_legacy_sadr_cleanup(target: Path, *, force: bool) -> tuple[str, ...]:\n+    \"\"\"Authorize exact legacy Shiki ADR removals before the first write.\n+\n+    A non-force install never removes existing files. A forced install needs no\n+    stamp when no legacy Shiki path is present. When legacy paths do exist, each\n+    must be named in a readable install stamp and its current bytes must match\n+    the recorded digest.\n+    \"\"\"\n+    if not force:\n+        return ()\n+    present = tuple(\n+        relative\n+        for relative in legacy_shiki_adr_paths()\n+        if (target / relative).exists() or (target / relative).is_symlink()\n+    )\n+    if not present:\n+        return ()\n+\n+    stamp_path = target / INSTALL_STAMP_PATH\n+    try:\n+        raw = stamp_path.read_text(encoding=\"utf-8\")\n+        stamp = json.loads(raw)\n+    except FileNotFoundError:\n+        detail = \"install stamp is absent\"\n+        blockers = [f\"{relative}: {detail}\" for relative in present]\n+    except (OSError, UnicodeDecodeError) as error:\n+        detail = f\"install stamp is unreadable ({error})\"\n+        blockers = [f\"{relative}: {detail}\" for relative in present]\n+    except json.JSONDecodeError as error:\n+        detail = f\"install stamp is malformed ({error})\"\n+        blockers = [f\"{relative}: {detail}\" for relative in present]\n+    else:\n+        if not isinstance(stamp, dict):\n+            blockers = [\n+                f\"{relative}: install stamp is malformed (expected an object)\"\n+                for relative in present\n+            ]\n+        else:\n+            digests = stamp.get(\"digests\")\n+            blockers = []\n+            if not isinstance(digests, dict):\n+                blockers.extend(\n+                    f\"{relative}: install stamp has no digest for this path\"\n+                    for relative in present\n+                )\n+            else:\n+                for relative in present:\n+                    expected = digests.get(relative)\n+                    if not isinstance(expected, str) or not expected:\n+                        blockers.append(\n+                            f\"{relative}: install stamp has no digest for this path\"\n+                        )\n+                        continue\n+                    try:\n+                        actual = _sha256_file(target / relative)\n+                    except OSError as error:\n+                        blockers.append(\n+                            f\"{relative}: legacy path is unreadable ({error})\"\n+                        )\n+                        continue\n+                    if actual != expected:\n+                        blockers.append(\n+                            f\"{relative}: digest mismatch (stamp {expected}, current {actual})\"\n+                        )\n+\n+    if blockers:\n+        joined = \"\\n\".join(f\"- {blocker}\" for blocker in blockers)\n+        raise ShikiError(\n+            \"legacy Shiki ADR cleanup is not authorized:\\n\"\n+            f\"{joined}\\nNo files were written.\"\n+        )\n+    return present\n+\n+\n def install_template(target: Path, *, force: bool, validate: bool) -> list[NewFileNote]:\n     # Preconditions first: never rewrite the tree of a target that cannot be\n     # upgraded cleanly (see refuse_pending_migrations).\n+    legacy_cleanup = preflight_legacy_sadr_cleanup(target, force=force)\n     refuse_pending_migrations(target)\n \n     # Resolve the target's required CODEOWNERS owner once from its\n@@ -250,6 +340,10 @@\n     # verbatim. See ``codeowners_required_owner``.\n     codeowners_owner = codeowners_required_owner(target)\n \n+    for relative in legacy_cleanup:\n+        (target / relative).unlink()\n+        info(f\"removed stamped legacy Shiki ADR: {target / relative}\")\n+\n     notes: list[NewFileNote] = []\n     for relative in TEMPLATE_PATHS:\n         source = ROOT / relative\ndiff --git a/tests/test_adr_references.py b/tests/test_adr_references.py\nindex 51706329c797d5dd9b47e082a333fa83b261d482..91ab76b724cb973435c45d53cab63793942267e9\n--- a/tests/test_adr_references.py\n+++ b/tests/test_adr_references.py\n@@ -1,4 +1,4 @@\n-\"\"\"Every ``ADR ####`` citation must resolve to exactly one decision record.\n+\"\"\"Every target ADR and Shiki SADR citation resolves in its own namespace.\n \n Background. ``scripts/mergegate_check.py`` justifies ``is_bookkeeping_closeout``\n with \"ADR 0017\" nine times and ``scripts/guardian_approval_signal.py`` cites\n@@ -11,11 +11,12 @@\n and the directory drifted with no signal.\n \n This is the same unbound-pair shape behind other silent failures: a reference\n-and its target with nothing asserting they agree. This test binds them. It scans\n-every ``scripts/*.py`` (plus ``docs/**/*.md`` and the root ``*.md`` governance\n-files, which are cheap) for the ``ADR ####`` pattern and asserts each cited\n-number resolves to exactly one ``docs/adr/####-*.md``. The converse is\n-deliberately NOT required: an ADR may exist without being cited.\n+and its target with nothing asserting they agree. This test binds them. Target\n+``ADR NNNN`` citations resolve only to ``docs/adr/NNNN-*.md``; Shiki\n+``SADR-NNNN`` citations resolve only to ``docs/adr/SADR-NNNN-*.md``. The scan\n+covers current governance docs, scripts, workflows, prompts, packaging metadata,\n+and repo-local Codex guidance while deliberately excluding historical\n+``.shiki`` evidence. The converse is not required: a record may be uncited.\n \n The suite is dependency free: standard-library ``unittest`` only.\n \"\"\"\n@@ -31,17 +32,18 @@\n \n REPO_ROOT = Path(__file__).resolve().parents[1]\n \n-# \"ADR 0016\" -- a four-digit citation. The trailing \\b keeps a five-digit token\n-# from being read as a truncated four-digit number.\n-_CITATION = re.compile(r\"\\bADR (\\d{4})\\b\")\n-# \"0016-locks-bound-product-paths.md\" -- the leading four digits name the record.\n-_ADR_FILENAME = re.compile(r\"^(\\d{4})-.*\\.md$\")\n+# ``ADR 0016`` / ``SADR-0016`` -- a namespace plus four-digit citation. The\n+# trailing ``\\b`` keeps a five-digit token from being read as a truncated one.\n+_CITATION = re.compile(r\"\\b(S?ADR)[ -](\\d{4})\\b\")\n+# Target ADRs retain the numeric filename; Shiki platform decisions use SADR.\n+_DECISION_FILENAME = re.compile(r\"^(?:(SADR)-)?(\\d{4})-.*\\.md$\")\n \n \n class Violation(NamedTuple):\n     \"\"\"A citation that does not resolve to exactly one decision record.\"\"\"\n \n     citing_file: str  # repo-relative path of the file that cites the ADR\n+    namespace: str  # \"ADR\" for target decisions; \"SADR\" for Shiki decisions\n     number: str  # the cited four-digit number, e.g. \"0016\"\n     line: int  # 1-based line of the citation\n     reason: str  # \"missing\" (zero matches) or \"ambiguous\" (two or more)\n@@ -49,44 +51,59 @@\n \n     @property\n     def expected_path(self) -> str:\n-        return f\"docs/adr/{self.number}-*.md\"\n+        prefix = \"SADR-\" if self.namespace == \"SADR\" else \"\"\n+        return f\"docs/adr/{prefix}{self.number}-*.md\"\n \n     def describe(self) -> str:\n         \"\"\"Name the citing file, the cited number, and the missing path.\"\"\"\n         if self.reason == \"missing\":\n             return (\n-                f\"{self.citing_file}:{self.line} cites ADR {self.number} \"\n+                f\"{self.citing_file}:{self.line} cites {self.namespace} {self.number} \"\n                 f\"but no document matches {self.expected_path}\"\n             )\n         joined = \", \".join(self.matches)\n         return (\n-            f\"{self.citing_file}:{self.line} cites ADR {self.number} which \"\n+            f\"{self.citing_file}:{self.line} cites {self.namespace} {self.number} which \"\n             f\"resolves to {len(self.matches)} documents (expected exactly one \"\n             f\"at {self.expected_path}): {joined}\"\n         )\n \n \n-def _adr_index(root: Path) -> dict[str, list[Path]]:\n-    \"\"\"Map every four-digit ADR number to the record files that claim it.\"\"\"\n-    index: dict[str, list[Path]] = {}\n+def _adr_index(root: Path) -> dict[tuple[str, str], list[Path]]:\n+    \"\"\"Map each decision namespace and number to the files that claim it.\"\"\"\n+    index: dict[tuple[str, str], list[Path]] = {}\n     adr_dir = root / \"docs\" / \"adr\"\n     if adr_dir.is_dir():\n         for path in sorted(adr_dir.glob(\"*.md\")):\n-            match = _ADR_FILENAME.match(path.name)\n+            match = _DECISION_FILENAME.match(path.name)\n             if match:\n-                index.setdefault(match.group(1), []).append(path)\n+                namespace = \"SADR\" if match.group(1) else \"ADR\"\n+                index.setdefault((namespace, match.group(2)), []).append(path)\n     return index\n \n \n def _citing_files(root: Path, include_docs: bool = True) -> list[Path]:\n-    \"\"\"Files scanned for citations: scripts/*.py, and cheaply the markdown.\"\"\"\n+    \"\"\"Current, shipped text surfaces scanned for decision citations.\"\"\"\n     files = sorted((root / \"scripts\").glob(\"*.py\"))\n+    files += sorted((root / \"scripts\").glob(\"*.sh\"))\n     if include_docs:\n         docs = root / \"docs\"\n         if docs.is_dir():\n             files += sorted(docs.rglob(\"*.md\"))\n         files += sorted(root.glob(\"*.md\"))\n-    return files\n+        pyproject = root / \"pyproject.toml\"\n+        if pyproject.is_file():\n+            files.append(pyproject)\n+        for relative in (\".github\", \".codex\"):\n+            surface = root / relative\n+            if surface.is_dir():\n+                files += sorted(\n+                    path\n+                    for path in surface.rglob(\"*\")\n+                    if path.is_file()\n+                    and path.suffix in {\".md\", \".py\", \".sh\", \".toml\", \".yaml\", \".yml\"}\n+                )\n+    return sorted(set(files))\n \n \n def find_adr_reference_violations(\n@@ -94,10 +111,9 @@\n ) -> list[Violation]:\n     \"\"\"Return every ``ADR ####`` citation under ``root`` that fails to resolve.\n \n-    A citation resolves when exactly one ``docs/adr/####-*.md`` matches its\n-    number. Zero matches is a \"missing\" violation; two or more is \"ambiguous\".\n-    Records that are never cited produce nothing -- the converse is not\n-    required.\n+    A citation resolves when exactly one record in the same namespace matches\n+    its number. Zero matches is a \"missing\" violation; two or more is\n+    \"ambiguous\". Records that are never cited produce nothing.\n     \"\"\"\n     root = Path(root)\n     index = _adr_index(root)\n@@ -110,13 +126,15 @@\n         rel = path.relative_to(root).as_posix()\n         for lineno, line in enumerate(text.splitlines(), start=1):\n             for match in _CITATION.finditer(line):\n-                number = match.group(1)\n-                found = index.get(number, [])\n+                namespace, number = match.groups()\n+                found = index.get((namespace, number), [])\n                 if len(found) == 1:\n                     continue\n                 reason = \"missing\" if not found else \"ambiguous\"\n                 rels = [p.relative_to(root).as_posix() for p in found]\n-                violations.append(Violation(rel, number, lineno, reason, rels))\n+                violations.append(\n+                    Violation(rel, namespace, number, lineno, reason, rels)\n+                )\n     return violations\n \n \n@@ -129,8 +147,8 @@\n         self.assertEqual(\n             violations,\n             [],\n-            \"Every 'ADR ####' citation must resolve to exactly one \"\n-            f\"docs/adr/####-*.md:\\n{detail}\",\n+            \"Every ADR/SADR citation must resolve exactly once in its own \"\n+            f\"docs/adr namespace:\\n{detail}\",\n         )\n \n     def test_scan_actually_reads_scripts(self) -> None:\n@@ -146,8 +164,21 @@\n         # The two records this Goal carried onto the default branch, and which\n         # the scripts cite most, must be present.\n         index = _adr_index(REPO_ROOT)\n-        self.assertIn(\"0016\", index)\n-        self.assertIn(\"0017\", index)\n+        self.assertIn((\"SADR\", \"0016\"), index)\n+        self.assertIn((\"SADR\", \"0017\"), index)\n+\n+    def test_platform_tree_has_only_sadr_0001_through_0019(self) -> None:\n+        adr_dir = REPO_ROOT / \"docs\" / \"adr\"\n+        sadrs = sorted(adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\"))\n+        self.assertEqual(\n+            {path.name.split(\"-\", 2)[1] for path in sadrs},\n+            {f\"{number:04d}\" for number in range(1, 20)},\n+        )\n+        self.assertEqual(\n+            list(adr_dir.glob(\"[0-9][0-9][0-9][0-9]-*.md\")),\n+            [],\n+            \"the Shiki platform tree must not retain legacy numeric SADR paths\",\n+        )\n \n \n class FixtureAdrReferences(unittest.TestCase):\n@@ -180,6 +211,26 @@\n         self.assertIn(\"9991\", message)  # cited number\n         self.assertIn(\"docs/adr/9991-*.md\", message)  # missing path\n \n+    def test_target_adr_does_not_satisfy_missing_platform_sadr(self) -> None:\n+        root = self._make_repo(\n+            adr_names=[\"0042-target-decision.md\"],\n+            script_body='\"\"\"Target ADR 0042 exists, but platform SADR-0042 does not.\"\"\"\\n',\n+        )\n+        violations = find_adr_reference_violations(root, include_docs=False)\n+        self.assertEqual(len(violations), 1)\n+        self.assertEqual(violations[0].namespace, \"SADR\")\n+        self.assertEqual(violations[0].number, \"0042\")\n+        self.assertEqual(violations[0].expected_path, \"docs/adr/SADR-0042-*.md\")\n+\n+    def test_target_adr_and_platform_sadr_may_reuse_a_number(self) -> None:\n+        root = self._make_repo(\n+            adr_names=[\"0042-target-decision.md\", \"SADR-0042-platform-decision.md\"],\n+            script_body='\"\"\"Target ADR 0042 and platform SADR-0042 are distinct.\"\"\"\\n',\n+        )\n+        self.assertEqual(\n+            find_adr_reference_violations(root, include_docs=False), []\n+        )\n+\n     def test_uncited_adr_is_allowed(self) -> None:\n         # An ADR exists but nothing cites it -> not a violation. The converse\n         # (every ADR must be cited) is explicitly NOT required.\ndiff --git a/tests/test_sadr_install_migration.py b/tests/test_sadr_install_migration.py\nnew file mode 100644\nindex 0000000000000000000000000000000000000000..5a442fa5e4968ade178dcdedc3c63b54dba580a2\n--- /dev/null\n+++ b/tests/test_sadr_install_migration.py\n@@ -0,0 +1,224 @@\n+\"\"\"SADR installation and collision-safe legacy upgrade behavior.\"\"\"\n+\n+from __future__ import annotations\n+\n+import contextlib\n+import hashlib\n+import io\n+import json\n+import shutil\n+import tempfile\n+import unittest\n+from pathlib import Path\n+\n+import shiki_test_support  # noqa: F401  (path bootstrap)\n+\n+import shiki_installer\n+from tests.test_adr_references import find_adr_reference_violations\n+\n+\n+PRODUCT_NUMBERS = tuple(range(13, 19))\n+\n+\n+def _quiet_install(target: Path, *, force: bool, validate: bool) -> None:\n+    with contextlib.redirect_stdout(io.StringIO()):\n+        shiki_installer.install_template(target, force=force, validate=validate)\n+\n+\n+def _sha256(path: Path) -> str:\n+    return hashlib.sha256(path.read_bytes()).hexdigest()\n+\n+\n+def _legacy_sadr_paths() -> tuple[str, ...]:\n+    return tuple(\n+        f\"docs/adr/{path.name.removeprefix('SADR-')}\"\n+        for path in sorted((shiki_installer.ROOT / \"docs\" / \"adr\").glob(\"SADR-*.md\"))\n+        if path.name != \"SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\"\n+    )\n+\n+\n+def _create_legacy_upgrade_fixture(\n+    target: Path,\n+) -> tuple[dict[str, bytes], tuple[str, ...]]:\n+    adr_dir = target / \"docs\" / \"adr\"\n+    adr_dir.mkdir(parents=True)\n+    product_bytes = {\n+        f\"{number:04d}-cubeshare-product-decision.md\": (\n+            f\"# CubeShare ADR {number:04d}\\n\\nDo not rewrite {number}.\\n\"\n+        ).encode()\n+        for number in PRODUCT_NUMBERS\n+    }\n+    for name, content in product_bytes.items():\n+        (adr_dir / name).write_bytes(content)\n+\n+    legacy_paths = _legacy_sadr_paths()\n+    for relative in legacy_paths:\n+        (target / relative).write_bytes(f\"legacy Shiki bytes for {relative}\\n\".encode())\n+\n+    stamp_path = target / shiki_installer.INSTALL_STAMP_PATH\n+    stamp_path.parent.mkdir(parents=True, exist_ok=True)\n+    stamp = {\n+        \"version\": shiki_installer.INSTALL_STAMP_VERSION,\n+        \"platform_commit\": None,\n+        \"installed_at\": \"2000-01-01T00:00:00+00:00\",\n+        \"digests\": {\n+            relative: _sha256(target / relative) for relative in legacy_paths\n+        },\n+    }\n+    stamp_path.write_text(\n+        json.dumps(stamp, indent=2, sort_keys=True) + \"\\n\", encoding=\"utf-8\"\n+    )\n+    return product_bytes, legacy_paths\n+\n+\n+def _tree_snapshot(root: Path) -> dict[str, tuple[str, bytes | str]]:\n+    \"\"\"Recursive byte/shape snapshot used to prove preflight is write-free.\"\"\"\n+    snapshot: dict[str, tuple[str, bytes | str]] = {}\n+    for path in sorted(root.rglob(\"*\")):\n+        relative = path.relative_to(root).as_posix()\n+        if path.is_symlink():\n+            snapshot[relative] = (\"symlink\", path.readlink().as_posix())\n+        elif path.is_dir():\n+            snapshot[relative] = (\"directory\", \"\")\n+        else:\n+            snapshot[relative] = (\"file\", path.read_bytes())\n+    return snapshot\n+\n+\n+class FreshSadrInstallTests(unittest.TestCase):\n+    def setUp(self) -> None:\n+        self.tmp = Path(tempfile.mkdtemp(prefix=\"shiki-sadr-fresh-\"))\n+        self.addCleanup(lambda: shutil.rmtree(self.tmp, ignore_errors=True))\n+        self.target = self.tmp / \"target\"\n+        self.adr_dir = self.target / \"docs\" / \"adr\"\n+        self.adr_dir.mkdir(parents=True)\n+        self.product_bytes = {\n+            f\"{number:04d}-product-decision.md\": (\n+                f\"# Product ADR {number:04d}\\n\\nTarget-owned bytes {number}.\\n\"\n+            ).encode()\n+            for number in PRODUCT_NUMBERS\n+        }\n+        for name, content in self.product_bytes.items():\n+            (self.adr_dir / name).write_bytes(content)\n+\n+    def test_fresh_install_preserves_product_adrs_and_installs_all_sadrs(self) -> None:\n+        _quiet_install(self.target, force=False, validate=True)\n+\n+        for name, expected in self.product_bytes.items():\n+            self.assertEqual((self.adr_dir / name).read_bytes(), expected)\n+\n+        sadr_numbers = {\n+            path.name.split(\"-\", 2)[1]\n+            for path in self.adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\")\n+        }\n+        self.assertEqual(sadr_numbers, {f\"{number:04d}\" for number in range(1, 20)})\n+\n+        numeric_records = {\n+            path.name\n+            for path in self.adr_dir.glob(\"[0-9][0-9][0-9][0-9]-*.md\")\n+        }\n+        self.assertEqual(numeric_records, set(self.product_bytes))\n+        self.assertEqual(find_adr_reference_violations(self.target), [])\n+\n+\n+class LegacySadrUpgradeTests(unittest.TestCase):\n+    def setUp(self) -> None:\n+        self.tmp = Path(tempfile.mkdtemp(prefix=\"shiki-sadr-upgrade-\"))\n+        self.addCleanup(lambda: shutil.rmtree(self.tmp, ignore_errors=True))\n+        self.target = self.tmp / \"target\"\n+        self.adr_dir = self.target / \"docs\" / \"adr\"\n+        self.product_bytes, self.legacy_paths = _create_legacy_upgrade_fixture(\n+            self.target\n+        )\n+\n+    def test_force_upgrade_deletes_only_stamped_legacy_sadrs(self) -> None:\n+        _quiet_install(self.target, force=True, validate=True)\n+\n+        for relative in self.legacy_paths:\n+            self.assertFalse((self.target / relative).exists(), relative)\n+        for name, expected in self.product_bytes.items():\n+            self.assertEqual((self.adr_dir / name).read_bytes(), expected)\n+\n+        sadrs = list(self.adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\"))\n+        self.assertEqual(len(sadrs), 19)\n+        refreshed = json.loads(\n+            (self.target / shiki_installer.INSTALL_STAMP_PATH).read_text(encoding=\"utf-8\")\n+        )\n+        for relative in self.legacy_paths:\n+            self.assertNotIn(relative, refreshed[\"digests\"])\n+        for path in sadrs:\n+            relative = path.relative_to(self.target).as_posix()\n+            self.assertEqual(refreshed[\"digests\"][relative], _sha256(path))\n+\n+    def test_non_force_install_never_deletes_legacy_files(self) -> None:\n+        before = {\n+            relative: (self.target / relative).read_bytes()\n+            for relative in self.legacy_paths\n+        }\n+        _quiet_install(self.target, force=False, validate=False)\n+        for relative, expected in before.items():\n+            self.assertEqual((self.target / relative).read_bytes(), expected)\n+\n+\n+class LegacyCleanupPreflightTests(unittest.TestCase):\n+    def setUp(self) -> None:\n+        self.tmp = Path(tempfile.mkdtemp(prefix=\"shiki-sadr-preflight-\"))\n+        self.addCleanup(lambda: shutil.rmtree(self.tmp, ignore_errors=True))\n+\n+    def test_all_ownership_blockers_abort_before_any_write(self) -> None:\n+        cases = (\n+            (\"absent\", \"install stamp is absent\"),\n+            (\"unreadable\", \"install stamp is unreadable\"),\n+            (\"malformed\", \"install stamp is malformed\"),\n+            (\"missing-digest\", \"install stamp has no digest\"),\n+            (\"digest-mismatch\", \"digest mismatch\"),\n+        )\n+        for name, expected_reason in cases:\n+            with self.subTest(name=name):\n+                target = self.tmp / name\n+                _, legacy_paths = _create_legacy_upgrade_fixture(target)\n+                stamp_path = target / shiki_installer.INSTALL_STAMP_PATH\n+\n+                if name == \"absent\":\n+                    stamp_path.unlink()\n+                elif name == \"unreadable\":\n+                    stamp_path.unlink()\n+                    stamp_path.mkdir()\n+                elif name == \"malformed\":\n+                    stamp_path.write_text(\"{not-json\\n\", encoding=\"utf-8\")\n+                else:\n+                    stamp = json.loads(stamp_path.read_text(encoding=\"utf-8\"))\n+                    if name == \"missing-digest\":\n+                        stamp[\"digests\"] = {}\n+                    else:\n+                        stamp[\"digests\"] = {\n+                            relative: \"deadbeef\" for relative in legacy_paths\n+                        }\n+                    stamp_path.write_text(\n+                        json.dumps(stamp, indent=2, sort_keys=True) + \"\\n\",\n+                        encoding=\"utf-8\",\n+                    )\n+\n+                before = _tree_snapshot(target)\n+                with self.assertRaises(shiki_installer.ShikiError) as raised:\n+                    _quiet_install(target, force=True, validate=False)\n+                after = _tree_snapshot(target)\n+\n+                self.assertEqual(before, after, f\"{name} preflight changed the tree\")\n+                message = str(raised.exception)\n+                self.assertIn(expected_reason, message)\n+                self.assertIn(\"No files were written\", message)\n+                for relative in legacy_paths:\n+                    self.assertIn(relative, message)\n+\n+    def test_absent_stamp_is_allowed_when_no_legacy_path_exists(self) -> None:\n+        target = self.tmp / \"no-legacy\"\n+        target.mkdir()\n+        _quiet_install(target, force=True, validate=False)\n+        self.assertTrue(\n+            (target / \"docs\" / \"adr\" / \"SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\").is_file()\n+        )\n+\n+\n+if __name__ == \"__main__\":\n+    unittest.main()\n\n2026-08-07T08:13:00.625643Z ERROR codex_models_manager::manager: failed to renew cache TTL: missing field `base_instructions` at line 94 column 5\napply patch\npatch: completed\n/private/tmp/shiki-adr-collision.pa0KwY/.worktrees/shiki-t-20260807t073307221254z-2d22d654-introduc/docs/adr/SADR-0010-external-ai-guardian-review.md\n/private/tmp/shiki-adr-collision.pa0KwY/.worktrees/shiki-t-20260807t073307221254z-2d22d654-introduc/docs/adr/SADR-0015-contract-approval-before-dispatch.md\n/private/tmp/shiki-adr-collision.pa0KwY/.worktrees/shiki-t-20260807t073307221254z-2d22d654-introduc/docs/adr/SADR-0016-locks-bound-product-paths.md\n/private/tmp/shiki-adr-collision.pa0KwY/.worktrees/shiki-t-20260807t073307221254z-2d22d654-introduc/docs/adr/SADR-0017-bookkeeping-closeouts-do-not-inherit-risk.md\n/private/tmp/shiki-adr-collision.pa0KwY/.worktrees/shiki-t-20260807t073307221254z-2d22d654-introduc/docs/adr/SADR-0018-carried-guardian-approval-across-a-proven-base-sync.md\n/private/tmp/shiki-adr-collision.pa0KwY/.worktrees/shiki-t-20260807t073307221254z-2d22d654-introduc/docs/agents/autonomous-loop-e2e-acceptance.md\n/private/tmp/shiki-adr-collision.pa0KwY/.worktrees/shiki-t-20260807t073307221254z-2d22d654-introduc/docs/agents/guardian-policy.md\n/private/tmp/shiki-adr-collision.pa0KwY/.worktrees/shiki-t-20260807t073307221254z-2d22d654-introduc/scripts/guardian_approval_signal.py\n/private/tmp/shiki-adr-collision.pa0KwY/.worktrees/shiki-t-20260807t073307221254z-2d22d654-introduc/scripts/shiki_contract_approval.py\n/private/tmp/shiki-adr-collision.pa0KwY/.worktrees/shiki-t-20260807t073307221254z-2d22d654-introduc/scripts/shiki_loop.py\n/private/tmp/shiki-adr-collision.pa0KwY/.worktrees/shiki-t-20260807t073307221254z-2d22d654-introduc/scripts/test_shiki_goal_loop.sh\n/private/tmp/shiki-adr-collision.pa0KwY/.worktrees/shiki-t-20260807t073307221254z-2d22d654-introduc/tests/test_adr_references.py\n/private/tmp/shiki-adr-collision.pa0KwY/.worktrees/shiki-t-20260807t073307221254z-2d22d654-introduc/tests/test_policy_job_merged_prs.py\ndiff --git a/.codex/skills/shiki/SKILL.md b/.codex/skills/shiki/SKILL.md\nindex c8e2e85d9d75d49cc4f2d6286ee22659c4e53e75..93cded623b2c15d9f65c76491a1c2d813ac9e6c0\n--- a/.codex/skills/shiki/SKILL.md\n+++ b/.codex/skills/shiki/SKILL.md\n@@ -18,6 +18,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n Codex CLI does not register this skill as a custom `/shiki` slash command. When\n the user types `/shiki` in Codex CLI and gets \"Unrecognized command\", explain\n that this is expected: invoke Shiki in Codex with natural language such as\n@@ -69,7 +72,7 @@\n \n ## Responsibilities\n \n-- Claude Code implements and repairs by default (`shiki runner claude`, ADR 0008); Codex implements and repairs tasks explicitly assigned to `codex`.\n+- Claude Code implements and repairs by default (`shiki runner claude`, SADR-0008); Codex implements and repairs tasks explicitly assigned to `codex`.\n - Claude Code Action can act as GitHub-side CCA or reviewer.\n - CCA judges completion.\n - MergeGate authorizes state transitions and merge readiness.\ndiff --git a/.github/prompts/cca-completion-check.md b/.github/prompts/cca-completion-check.md\nindex ed203ad5f302ecc81372d8ab27d2b1232ca97497..fd89950528333ef6242e6d5443904f5392ab7e56\n--- a/.github/prompts/cca-completion-check.md\n+++ b/.github/prompts/cca-completion-check.md\n@@ -19,6 +19,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n ## Required Reading\n \n Read only the files needed to judge the PR contract. Prefer this order:\ndiff --git a/AGENTS.md b/AGENTS.md\nindex 7f041100d49c2a7525f51cb0f5d696a4a6d1c78c..2b700e989ff295987aa8a4b0c5fcb1acd80020da\n--- a/AGENTS.md\n+++ b/AGENTS.md\n@@ -44,6 +44,10 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use\n+`SADR-NNNN-*.md`; target product decisions retain `NNNN-*.md` and the `ADR`\n+namespace.\n+\n Use the canonical source-of-truth order above. Runtime-specific reading orders\n are operational guidance only and must not override or reorder it.\n \ndiff --git a/CLAUDE.md b/CLAUDE.md\nindex 98a932e7841a632866fcd53b0bad0e3af3711d3d..611d491912d6a72a0327a1731b8f33a41acb4baa\n--- a/CLAUDE.md\n+++ b/CLAUDE.md\n@@ -14,6 +14,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n ## Claude Role\n \n Default to planner, default implementer, reviewer, coordinator, documentation editor, CCA-style completion judge, and final-judgment assistant.\ndiff --git a/SYSTEM_PROMPT.md b/SYSTEM_PROMPT.md\nindex aaaea45e3a896c3a44ba89eec8bb84325ecf6c62..2e7c093ebe9e8bb34e0923756f191b4f7fa581f5\n--- a/SYSTEM_PROMPT.md\n+++ b/SYSTEM_PROMPT.md\n@@ -30,6 +30,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n Use the canonical source-of-truth order above. Runtime-specific reading orders\n are operational guidance only and must not override or reorder it. The current\n conversation is non-durable operator input.\ndiff --git a/docs/adr/0001-keep-shiki-as-platform-name.md b/docs/adr/SADR-0001-keep-shiki-as-platform-name.md\nindex 20a9637c3b9d8c97de7ba8db7cfae4d82d9a7dba..8b685439959aada1b221c112f0ac3c51ca8a1695\n--- a/docs/adr/0001-keep-shiki-as-platform-name.md\n+++ b/docs/adr/SADR-0001-keep-shiki-as-platform-name.md\n@@ -1,3 +1,3 @@\n-# Keep Shiki As The Platform Name\n+# SADR-0001: Keep Shiki As The Platform Name\n \n Shiki remains the product name for the GitHub-native agentic engineering control plane. We considered more descriptive names such as MergePilot, MergeGate, and Kiroku, but kept Shiki because it carries the existing goal-seeking framework identity while remaining independent of Hermes, Codex, Claude Code, or any single runtime.\ndiff --git a/docs/adr/0002-use-agents-as-shared-constitution.md b/docs/adr/SADR-0002-use-agents-as-shared-constitution.md\nindex 73de30ecb078120eac540d9bc51855e3cd48d0e3..c5486182bf06fca36ea508496b013c67302fd989\n--- a/docs/adr/0002-use-agents-as-shared-constitution.md\n+++ b/docs/adr/SADR-0002-use-agents-as-shared-constitution.md\n@@ -1,3 +1,3 @@\n-# Use AGENTS As The Shared Agent Constitution\n+# SADR-0002: Use AGENTS As The Shared Agent Constitution\n \n Shiki uses `AGENTS.md` as the runtime-neutral constitution and `CLAUDE.md` as a thin Claude Code wrapper. This keeps Codex, Claude Code, Hermes Runner, and future Agent Runtimes aligned on the same GitHub-first MergeGate rules while still allowing runtime-specific instructions where needed.\ndiff --git a/docs/adr/0003-require-skill-gate-for-mergegate-readiness.md b/docs/adr/SADR-0003-require-skill-gate-for-mergegate-readiness.md\nindex 424be0c9af4c8f0136f1fde68a468c47ff84e764..7fc337987c7df9792f75f181dfcbed75fe5ffff2\n--- a/docs/adr/0003-require-skill-gate-for-mergegate-readiness.md\n+++ b/docs/adr/SADR-0003-require-skill-gate-for-mergegate-readiness.md\n@@ -1,3 +1,3 @@\n-# Require Skill Gate For MergeGate Readiness\n+# SADR-0003: Require Skill Gate For MergeGate Readiness\n \n Shiki requires relevant engineering skills to be invoked before execution when their triggers apply. This makes skills part of MergeGate readiness rather than optional prompt style, because Shiki depends on repeatable planning, issue decomposition, TDD, diagnosis, triage, and architecture review before autonomous or parallel agent work can be trusted.\ndiff --git a/docs/adr/0004-use-github-first-with-shiki-mirror.md b/docs/adr/SADR-0004-use-github-first-with-shiki-mirror.md\nindex d0a3a9174dc646a945a9d325954ffaf00c352040..a79c54ae9ee8eed2a0524192b864da4e22a92d3f\n--- a/docs/adr/0004-use-github-first-with-shiki-mirror.md\n+++ b/docs/adr/SADR-0004-use-github-first-with-shiki-mirror.md\n@@ -1,4 +1,4 @@\n-# ADR 0004: Use GitHub First With A Repository-Local Shiki Mirror\n+# SADR-0004: Use GitHub First With A Repository-Local Shiki Mirror\n \n ## Status\n \ndiff --git a/docs/adr/0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md b/docs/adr/SADR-0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\nindex b339baf49b89d9290e79c3d51b36e7b2bde3bd95..c30a0d8b1fde0224d0ac7820e7870dccf721580d\n--- a/docs/adr/0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\n+++ b/docs/adr/SADR-0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\n@@ -1,4 +1,4 @@\n-# ADR 0005: Use Codex As Front And Claude Code Action As The GitHub Runtime\n+# SADR-0005: Use Codex As Front And Claude Code Action As The GitHub Runtime\n \n ## Status\n \ndiff --git a/docs/adr/0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md b/docs/adr/SADR-0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\nindex 4bcae9d1e52f0a37b8e9fbafa5cd14b6ee1993bd..7a38538a6ae9929567f0ade67e7ea6a60d0b3f7e\n--- a/docs/adr/0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\n+++ b/docs/adr/SADR-0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\n@@ -1,4 +1,4 @@\n-# ADR 0006: Separate Codex Implementation, CCA Completion, And MergeGate Authorization\n+# SADR-0006: Separate Codex Implementation, CCA Completion, And MergeGate Authorization\n \n ## Status\n \ndiff --git a/docs/adr/0007-packaging-and-release.md b/docs/adr/SADR-0007-packaging-and-release.md\nindex 79b84ddb97167372b44641e5b7860e400ce80822..023cd57f8011aa6e6d2ebf612b7c35a7a82c00d4\n--- a/docs/adr/0007-packaging-and-release.md\n+++ b/docs/adr/SADR-0007-packaging-and-release.md\n@@ -1,4 +1,4 @@\n-# ADR 0007: Packaging And Release For Shiki\n+# SADR-0007: Packaging And Release For Shiki\n \n ## Status\n \ndiff --git a/docs/adr/0008-use-claude-code-as-default-implementer.md b/docs/adr/SADR-0008-use-claude-code-as-default-implementer.md\nindex dedf4510b7c7799521422f622029563d373db031..a158a8d444d93764ec515a28133f7dde4af23579\n--- a/docs/adr/0008-use-claude-code-as-default-implementer.md\n+++ b/docs/adr/SADR-0008-use-claude-code-as-default-implementer.md\n@@ -1,4 +1,4 @@\n-# ADR 0008: Use Claude Code As The Default Implementer Runtime\n+# SADR-0008: Use Claude Code As The Default Implementer Runtime\n \n ## Status\n \ndiff --git a/docs/adr/0009-requirements-definition-and-spec-freeze.md b/docs/adr/SADR-0009-requirements-definition-and-spec-freeze.md\nindex 29b3f7871bef9203a8b4a831a65c39eb2f506e42..52e7dd69ad0aa4efe0b68c62678abbf7df02aaec\n--- a/docs/adr/0009-requirements-definition-and-spec-freeze.md\n+++ b/docs/adr/SADR-0009-requirements-definition-and-spec-freeze.md\n@@ -1,4 +1,4 @@\n-# ADR 0009: Merge Grill And Planning Into Requirements Definition With Spec Freeze\n+# SADR-0009: Merge Grill And Planning Into Requirements Definition With Spec Freeze\n \n ## Status\n \ndiff --git a/docs/adr/0010-external-ai-guardian-review.md b/docs/adr/SADR-0010-external-ai-guardian-review.md\nindex e6108bbe8bb4e73e0ff4684c8f78001a80438c4e..94ab21f40c326063a8833245d0ee8db67af0395d\n--- a/docs/adr/0010-external-ai-guardian-review.md\n+++ b/docs/adr/SADR-0010-external-ai-guardian-review.md\n@@ -1,4 +1,4 @@\n-# ADR 0010: External AI Guardian Review As A First-Class Approval Authority\n+# SADR-0010: External AI Guardian Review As A First-Class Approval Authority\n \n ## Status\n \n@@ -89,7 +89,7 @@\n   autonomous design the operator intends and produced the impersonation\n   incident.\n - **Record AI approval as an operator comment**: rejected as forgery of human\n-  authority \u2014 the exact failure this ADR corrects.\n+  authority \u2014 the exact failure this SADR corrects.\n - **Deliver the artifact as a committed `.shiki/` file**: rejected; a committed\n   approval inside the PR being approved is forgeable by anyone who can push.\n - **A dedicated reviewer bot identity instead of a relayed comment**: deferred;\ndiff --git a/docs/adr/0011-autonomous-quality-gate-evidence.md b/docs/adr/SADR-0011-autonomous-quality-gate-evidence.md\nindex bca0246c5d46e44afbde15486a75eec425fc02f5..9c93eac16d8c3a6d4b11bd7224e3955102cd7db4\n--- a/docs/adr/0011-autonomous-quality-gate-evidence.md\n+++ b/docs/adr/SADR-0011-autonomous-quality-gate-evidence.md\n@@ -1,4 +1,4 @@\n-# ADR 0011: Autonomous Quality-Gate Evidence \u2014 Deterministic Facts And An Independent Verifier\n+# SADR-0011: Autonomous Quality-Gate Evidence \u2014 Deterministic Facts And An Independent Verifier\n \n ## Status\n \ndiff --git a/docs/adr/0012-autonomous-loop-closeout-to-main.md b/docs/adr/SADR-0012-autonomous-loop-closeout-to-main.md\nindex 637fad6dde4989c9c2bf0f81f676d8d29991123c..ec7d85f81030a676bcbba28b43eb365422a36eac\n--- a/docs/adr/0012-autonomous-loop-closeout-to-main.md\n+++ b/docs/adr/SADR-0012-autonomous-loop-closeout-to-main.md\n@@ -1,4 +1,4 @@\n-# 0012 \u2014 Autonomous loop pushes goal completion to main via a closeout PR\n+# SADR-0012 \u2014 Autonomous loop pushes goal completion to main via a closeout PR\n \n - Status: accepted\n - Date: 2026-06-18\ndiff --git a/docs/adr/0013-bootstrap-grants-actions-approve-pull-requests.md b/docs/adr/SADR-0013-bootstrap-grants-actions-approve-pull-requests.md\nindex b6c69d64f1c5ced346b88d6859586715b3b5bd0e..a793e7277d67ab8ea56286fec289ca689825a17d\n--- a/docs/adr/0013-bootstrap-grants-actions-approve-pull-requests.md\n+++ b/docs/adr/SADR-0013-bootstrap-grants-actions-approve-pull-requests.md\n@@ -1,4 +1,4 @@\n-# ADR 0013: Bootstrap Grants GitHub Actions Permission To Create And Approve Pull Requests\n+# SADR-0013: Bootstrap Grants GitHub Actions Permission To Create And Approve Pull Requests\n \n ## Status\n \ndiff --git a/docs/adr/0014-codex-app-external-ai-guardian-ui-adapter.md b/docs/adr/SADR-0014-codex-app-external-ai-guardian-ui-adapter.md\nindex 238db06279334caa341c117362df9271382a7fcd..65f12e9939321e1b3e389a34c822fc1549e5155d\n--- a/docs/adr/0014-codex-app-external-ai-guardian-ui-adapter.md\n+++ b/docs/adr/SADR-0014-codex-app-external-ai-guardian-ui-adapter.md\n@@ -1,4 +1,4 @@\n-# ADR 0014: Use Codex App As The External AI Guardian UI Adapter\n+# SADR-0014: Use Codex App As The External AI Guardian UI Adapter\n \n ## Status\n \ndiff --git a/docs/adr/0015-contract-approval-before-dispatch.md b/docs/adr/SADR-0015-contract-approval-before-dispatch.md\nindex fad6c656741a20a7684ba16080a7db159f63a52f..6061271a46d3f1d6ae7174477de3fb75673123a6\n--- a/docs/adr/0015-contract-approval-before-dispatch.md\n+++ b/docs/adr/SADR-0015-contract-approval-before-dispatch.md\n@@ -1,4 +1,4 @@\n-# ADR 0015: Approve The Task Contract Before Dispatch, Not The Diff After It\n+# SADR-0015: Approve The Task Contract Before Dispatch, Not The Diff After It\n \n ## Status\n \n@@ -9,7 +9,7 @@\n Guardian approval for high/critical risk is expressible only on a pull request.\n Every approval source in `guardian-policy.json` \u2014 GitHub review, the\n `guardian:approved` label, the `Guardian approval granted` comment, and the\n-`external_ai_guardian_review` artifact (ADR 0010/0014) \u2014 is bound to a PR, and\n+`external_ai_guardian_review` artifact (SADR-0010/0014) \u2014 is bound to a PR, and\n three of the four require the current head SHA. Approval therefore cannot exist\n before an implementation PR exists.\n \n@@ -81,7 +81,7 @@\n An envelope breach must surface only on the non-repairable policy gate, never on\n the repairable `MergeGate metadata check`. Otherwise the Goal Loop routes it to\n `dispatch_repair` \u2014 instructing a runner to make the breach go away \u2014 which is\n-exactly what this ADR forbids.\n+exactly what this SADR forbids.\n \n **Contract immutability.** After registration, an implementation PR must not\n change the governance fields of its own task contract (`scope`, `non_goals`,\n@@ -116,7 +116,7 @@\n fails any verdict that is not `complete`, and the signal the CCA prompt reads is\n computed from `evaluate_guardian_approval` alone. Without the OR-merge at the\n signal, a contract-approved PR still returns `needs_guardian`, the CCA job fails,\n-and the MergeGate policy check never runs \u2014 the gate this ADR changes would never\n+and the MergeGate policy check never runs \u2014 the gate this SADR changes would never\n be reached. The CCA *prompt* needs no change: a `contract_approval` entry in the\n signal's existing `sources` array satisfies its current wording.\n \n@@ -130,7 +130,7 @@\n written, and an approved contract lets its implementation PRs merge through the\n same automatic path as low/medium risk. The wasted implement-then-block cycle\n disappears. Guardian review shifts from reading diffs to reading contracts, which\n-is both earlier and smaller; the external AI Guardian path (ADR 0010/0014) works\n+is both earlier and smaller; the external AI Guardian path (SADR-0010/0014) works\n on contracts with no change.\n \n We accept that the Guardian authorizes a class of change without seeing its\n@@ -146,7 +146,7 @@\n adversarial tests for the new approval path equivalent in rigor to\n `test_shiki_governance_evidence.sh`.\n \n-Bootstrap costs, which apply only while this ADR is being implemented. The tasks\n+Bootstrap costs, which apply only while this SADR is being implemented. The tasks\n that build Contract Approval merge under the existing rules, so the ones\n dispatched after the pre-dispatch gate lands but before the approval path lands\n need a Guardian-approved Contract PR *and* a live Guardian approval on their\n@@ -210,7 +210,7 @@\n risk; they do not bound authorized work.\n \n **Auto-escalate risk on envelope breach instead of stopping.** Returns the\n-workflow to a diff-time Guardian decision \u2014 the exact behavior this ADR removes.\n+workflow to a diff-time Guardian decision \u2014 the exact behavior this SADR removes.\n \n **Resolve governance fields from the base snapshot only.** Rejected against the\n registration flow: implementation PRs add their own contract, so the base has no\ndiff --git a/docs/adr/0016-locks-bound-product-paths.md b/docs/adr/SADR-0016-locks-bound-product-paths.md\nindex b5e850f3f1f7f4ef6e7f9d1763a29a51bd300c45..46b40a9b99a7aae644c02bb458f5473ff0a90ce2\n--- a/docs/adr/0016-locks-bound-product-paths.md\n+++ b/docs/adr/SADR-0016-locks-bound-product-paths.md\n@@ -1,4 +1,4 @@\n-# ADR 0016: Locks bound product paths; the Shiki mirror is bound by record identity\n+# SADR-0016: Locks bound product paths; the Shiki mirror is bound by record identity\n \n ## Status\n \n@@ -6,8 +6,8 @@\n \n - Date: 2026-07-28\n - Deciders: operator (mizutani-140), Claude (planner)\n-- Related: ADR 0004 (GitHub-first with a `.shiki` mirror), ADR 0009 (Spec Freeze),\n-  ADR 0012 (autonomous loop closeout to main), ADR 0015 (Contract Approval before\n+- Related: SADR-0004 (GitHub-first with a `.shiki` mirror), SADR-0009 (Spec Freeze),\n+  SADR-0012 (autonomous loop closeout to main), SADR-0015 (Contract Approval before\n   dispatch), remediation goal task R-02, the live PR #179 deadlock.\n \n ## Context\n@@ -90,11 +90,11 @@\n files, documentation files and named source modules are nameable; runtime-\n allocated ledger, runner and report ids are not \u2014 and those live in `.shiki`,\n which locks no longer govern. Existing frozen contracts are brought into line by\n-a one-time Spec Amendment per plan, which must land before ADR 0015's contract\n+a one-time Spec Amendment per plan, which must land before SADR-0015's contract\n immutability makes `locks` unwritable from the implementation PR.\n \n **Lock state is resolved from the durable side.** MergeGate resolves the lock\n-registry, and ADR 0015's governance fields, from the base `.shiki` snapshot when\n+registry, and SADR-0015's governance fields, from the base `.shiki` snapshot when\n present, falling back to head only when absent. The `MergeGate metadata check`\n job is given `--base-shiki`, which it does not receive today. Locks are not\n derived from the live open-pull-request set: a pull request body is untrusted\n@@ -108,7 +108,7 @@\n Explicitly out of scope: moving MergeGate's contract resolution off the PR\n checkout wholesale; making the mirror a projection of GitHub state; introducing\n new lock kinds; moving ledger evidence out of the repository; and unwinding\n-ADR 0012's closeout pull request. Registration of multi-task goals is ADR 0015's\n+SADR-0012's closeout pull request. Registration of multi-task goals is SADR-0015's\n Contract PR and is not re-decided here.\n \n ## Consequences\n@@ -127,7 +127,7 @@\n \n Risks accepted. First, goal-scoped identity is only as strong as `goal_id`,\n which is resolved from the PR body and the head task file; the mitigation is to\n-add `goal_id` to ADR 0015's frozen governance field set, and until that lands a\n+add `goal_id` to SADR-0015's frozen governance field set, and until that lands a\n task file can name a foreign goal. Second, base-when-present resolution is\n unavailable on fork pull requests, where the head is contributor-controlled;\n fork pull requests already cannot complete a Shiki merge, and this decision does\n@@ -139,7 +139,7 @@\n path, not by implementation pull requests.\n \n New obligations: one Spec Amendment per frozen plan to rewrite `locks`, ordered\n-before ADR 0015 T1; extension of the adversarial suites in\n+before SADR-0015 T1; extension of the adversarial suites in\n `scripts/test_shiki_adversarial_state.sh` and\n `scripts/test_shiki_governance_evidence.sh` to cover the eleven newly-scoped\n directories; and updating `scripts/test_shiki_loop_lock_guard.sh`,\n@@ -151,32 +151,32 @@\n whether the loop's transitive sync of ledger `evidence` references can be\n deleted once the Contract PR carries its own source plan.\n \n-## Relationship to ADR 0004\n+## Relationship to SADR-0004\n \n-This ADR **clarifies something ADR 0004 left unspecified**. ADR 0004 decides that\n+This SADR **clarifies something SADR-0004 left unspecified**. SADR-0004 decides that\n GitHub is the operational source of truth and that each target repository keeps a\n `.shiki/` mirror. It says nothing about pull requests writing the mirror, does\n not name MergeGate, and does not make the mirror an input to a gate. Nothing in\n-ADR 0004's Decision or Consequences is amended, superseded or weakened: the\n+SADR-0004's Decision or Consequences is amended, superseded or weakened: the\n mirror still exists, still records the same artifacts, and pull request\n-boundaries remain the coordination surface. What this ADR settles is the question\n+boundaries remain the coordination surface. What this SADR settles is the question\n 0004 never reached \u2014 by what authority a pull request may write a mirror record \u2014\n and it answers it the way `.shiki/manifest.json` already declares\n (`pr_mutation: current-task-or-current-goal-only`).\n \n-It **amends ADR 0012**, whose Decision item 5 states that \"Loop-executed tasks\n+It **amends SADR-0012**, whose Decision item 5 states that \"Loop-executed tasks\n declare the `path:.shiki/**` lock, which covers every `.shiki` file the closeout\n stages ... so `files_outside_locks` is satisfied without per-file lock\n bookkeeping.\" That clause is replaced: the closeout's mirror writes are\n-authorized by record identity, not by a declared lock. ADR 0012's substantive\n+authorized by record identity, not by a declared lock. SADR-0012's substantive\n decision \u2014 that completion reaches `main` through a closeout pull request rather\n than local attestation \u2014 is preserved unchanged.\n \n-It is **complementary to ADR 0015**, not a substitute. ADR 0015 owns contract\n+It is **complementary to SADR-0015**, not a substitute. SADR-0015 owns contract\n immutability, the pre-dispatch gate, the Contract PR and the Scope Envelope. This\n-ADR owns what a lock means and who may write the mirror. The one ordering\n+SADR owns what a lock means and who may write the mirror. The one ordering\n constraint between them is that the lock-narrowing Spec Amendment must precede\n-ADR 0015 T1's freezing of the `locks` field.\n+SADR-0015 T1's freezing of the `locks` field.\n \n ## Alternatives Considered\n \n@@ -186,7 +186,7 @@\n the only writer that can register them may run. Migrating the Guardian signal to\n that store makes every registration PR fail closed to `needs_guardian` at every\n risk level, because the task file is by definition absent from the store, which\n-removes the low/medium autonomous path ADR 0008/0009 exists to provide. And it\n+removes the low/medium autonomous path SADR-0008/0009 exists to provide. And it\n leaves schemas, manifest, config, guardian policy, templates, migrations, plans,\n worktrees, runner records, runs and handoffs with no writer at all in a\n repository whose product is `.shiki`. Measured benefit on the frozen backlog:\n@@ -199,14 +199,14 @@\n namespace is not in fact ownership-partitioned \u2014 56 ledger entries carry no\n `task_id`, and one goal DAG is writable by 35 registered tasks. The correct\n expression of its insight is that locks do not govern the mirror at all, which is\n-what this ADR decides.\n+what this SADR decides.\n \n **Make the mirror a projection written only by a registration writer and a\n post-merge projection job.** Rejected. It requires a bypass actor with write\n access to the protected default branch; it makes its own master invariant\n readable from a manifest that pull requests may edit; it deletes both reconcile\n modes and leaves a corrupted mirror with no in-band repair path, contradicting\n-ADR 0004's consequence that mirror conflicts are repaired against GitHub state;\n+SADR-0004's consequence that mirror conflicts are repaired against GitHub state;\n and its content-derived ids are rejected by `validate_shiki`'s currently-green id\n pattern.\n \ndiff --git a/docs/adr/0017-bookkeeping-closeouts-do-not-inherit-risk.md b/docs/adr/SADR-0017-bookkeeping-closeouts-do-not-inherit-risk.md\nindex 554e3a0d526a72ca067108c5464cc68c3edcf174..b9676857a4713850e063af7c39c8e0b01ecb4c66\n--- a/docs/adr/0017-bookkeeping-closeouts-do-not-inherit-risk.md\n+++ b/docs/adr/SADR-0017-bookkeeping-closeouts-do-not-inherit-risk.md\n@@ -1,4 +1,4 @@\n-# ADR 0017: A Bookkeeping Closeout Does Not Inherit Its Task's Risk\n+# SADR-0017: A Bookkeeping Closeout Does Not Inherit Its Task's Risk\n \n ## Status\n \n@@ -6,7 +6,7 @@\n \n ## Context\n \n-ADR 0012 made the goal loop push a task's completion to the default branch through a\n+SADR-0012 made the goal loop push a task's completion to the default branch through a\n closeout pull request: after the implementation PR merges, the loop opens a second PR\n carrying `task.status = done`, `lock.state = released`, and \u2014 when the task completes\n its goal \u2014 `goal.status = complete` plus one scorecard report. Nothing else. The\n@@ -49,14 +49,14 @@\n 4. The task file's governance fields (`scope`, `non_goals`, `required_skills`,\n    `risk_level`, `locks`, `acceptance_checks`, `test_command`) are byte-identical to\n    the base snapshot. Only `status`, `expected_pr`, `closeout_pr`, `ledger_evidence`\n-   and `expected_branch` may differ \u2014 the ADR 0015 contract-immutability rule,\n+   and `expected_branch` may differ \u2014 the SADR-0015 contract-immutability rule,\n    unchanged.\n \n    `expected_branch` is in that set because a closeout is *by construction* on a\n    different branch from the implementation it closes, and the loop rewrites the\n    field accordingly. It is safe to let it differ because MergeGate independently\n    requires `expected_branch` to equal the PR's actual head ref, so a PR cannot set\n-   it to anything other than the branch it is really on. The first draft of this ADR\n+   it to anything other than the branch it is really on. The first draft of this SADR\n    omitted it, which made the exemption unreachable: every real closeout failed\n    condition 4 on a field the loop always changes. See PR #209.\n 5. The state transitions are exactly the terminal set: task `review` \u2192 `done`, lock\n@@ -113,12 +113,12 @@\n classification must be derived, not asserted.\n \n **Fold the closeout into the implementation PR** so no second PR exists. Rejected under\n-ADR 0012, which recorded why: a task file that is already `done` on the implementation\n+SADR-0012, which recorded why: a task file that is already `done` on the implementation\n branch short-circuits the loop's review \u2192 CCA \u2192 merge \u2192 done ordering into a local\n goal-completion before any merge happens.\n \n **Have the loop auto-merge closeouts with elevated credentials.** Rejected outright: that\n-is a runtime authoring its own approval, which ADR 0010 and the constitution's\n+is a runtime authoring its own approval, which SADR-0010 and the constitution's\n approval-record prohibition forbid. It would also be indistinguishable, in the evidence,\n from the forgery those rules exist to prevent.\n \ndiff --git a/docs/adr/0018-carried-guardian-approval-across-a-proven-base-sync.md b/docs/adr/SADR-0018-carried-guardian-approval-across-a-proven-base-sync.md\nindex 22358562da390ed8b45cd600da08a43d378b4680..e6e2907cefa776460af73ddf7d39db605f3c39b0\n--- a/docs/adr/0018-carried-guardian-approval-across-a-proven-base-sync.md\n+++ b/docs/adr/SADR-0018-carried-guardian-approval-across-a-proven-base-sync.md\n@@ -1,4 +1,4 @@\n-# ADR 0018: Carried Guardian Approval Across A Proven Base Sync\n+# SADR-0018: Carried Guardian Approval Across A Proven Base Sync\n \n ## Status\n \n@@ -34,7 +34,7 @@\n Guardian evaluator that consumes it (`shiki_guardian.evaluate_guardian_approval`,\n the `guardian_comment_carried` source) were built and tested in isolation. Neither\n does anything until a caller passes the flags that turn the carry on, so the\n-feature was invisible in CI. This ADR records the decision the two of them\n+feature was invisible in CI. This SADR records the decision the two of them\n implement, and this task wires it into the four CI invocations that judge Guardian\n approval.\n \ndiff --git a/docs/adr/README.md b/docs/adr/README.md\nindex c70e057a4520c36e042ab68c53cd4a21ef709b71..5da43856836e2ce6a150f8127766e2f88355ab4b\n--- a/docs/adr/README.md\n+++ b/docs/adr/README.md\n@@ -1,40 +1,41 @@\n-# Architecture Decision Records\n+# Decision Record Namespaces\n \n-This directory holds Shiki's Architecture Decision Records (ADRs): durable\n-records of hard-to-reverse platform decisions. ADRs are part of the Shiki source\n-of truth alongside GitHub state, the `.shiki/` mirror, and `CONTEXT.md`.\n+This directory holds two independent decision namespaces:\n \n-## How ADRs Work\n+- Shiki Architecture Decision Records (SADRs), named\n+  `SADR-NNNN-<short-slug>.md`, record hard-to-reverse Shiki platform decisions.\n+- Target Architecture Decision Records (ADRs), named\n+  `NNNN-<short-slug>.md`, belong to the target product.\n \n-- New ADRs are added as `docs/adr/NNNN-<short-slug>.md`, where `NNNN` is the next\n-  zero-padded sequence number.\n-- The **canonical list of ADRs is the set of files matching `docs/adr/*.md`** in\n-  this directory. Browse `docs/adr/*.md` for the authoritative, current set.\n-- Use [`template.md`](template.md) as the starting point for a new ADR.\n-- Record an ADR when a decision is hard to reverse, surprising without context,\n-  and a real tradeoff. Do not write an ADR for routine or easily reversible\n-  choices.\n+The same four-digit number may appear once in each namespace. A platform\n+`SADR-NNNN` citation resolves only to `SADR-NNNN-*.md`; a target `ADR NNNN`\n+citation resolves only to `NNNN-*.md`.\n \n-Using the `docs/adr/*.md` glob as the source of truth means other PRs can add an\n-ADR without editing this index file, avoiding merge contention on a\n-hand-maintained enumeration.\n+## How Decision Records Work\n \n-## Current ADRs (snapshot)\n-\n-The list below is a convenience snapshot and may lag behind the directory. When\n-in doubt, list `docs/adr/*.md`.\n+- New Shiki platform decisions use the next zero-padded SADR number.\n+- New target product decisions use the target's own ADR sequence.\n+- The canonical list is the files in `docs/adr/`; no hand-maintained index is\n+  authoritative.\n+- Use [`template.md`](template.md) as the starting point and choose the filename\n+  namespace that owns the decision.\n+- Record a decision when it is hard to reverse, surprising without context, and\n+  a real tradeoff. Do not write a decision record for routine or easily\n+  reversible choices.\n \n-- [0001 \u2014 Keep Shiki As The Platform Name](0001-keep-shiki-as-platform-name.md)\n-- [0002 \u2014 Use AGENTS As The Shared Agent Constitution](0002-use-agents-as-shared-constitution.md)\n-- [0003 \u2014 Require Skill Gate For MergeGate Readiness](0003-require-skill-gate-for-mergegate-readiness.md)\n-- [0004 \u2014 Use GitHub First With A Repository-Local Shiki Mirror](0004-use-github-first-with-shiki-mirror.md)\n-- [0005 \u2014 Use Codex As Front And Claude Code Action As The GitHub Runtime](0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md)\n-- [0006 \u2014 Separate Codex Implementation, CCA Completion, And MergeGate Authorization](0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md)\n+Using the directory as the source of truth lets independent changes add records\n+without contending on an enumerated index.\n \n-## Adding A New ADR\n+## Adding A Shiki SADR\n \n-1. Copy `template.md` to `docs/adr/NNNN-<short-slug>.md`.\n+1. Copy `template.md` to `docs/adr/SADR-NNNN-<short-slug>.md`.\n 2. Fill in the title, status, context, decision, and consequences.\n-3. Set the status to `Proposed`, then `Accepted` once the decision is approved.\n-4. Open a PR following `CONTRIBUTING.md`. You do not need to edit this index for\n-   the ADR to be discoverable via `docs/adr/*.md`.\n+3. Set the status to `Proposed`, then `Accepted` once the proper authority has\n+   approved the decision.\n+4. Open a PR following `CONTRIBUTING.md`.\n+\n+## Adding A Target ADR\n+\n+1. Copy `template.md` to `docs/adr/NNNN-<short-slug>.md` using the target's own\n+   sequence.\n+2. Cite it as `ADR NNNN`; do not prefix its filename with `SADR-`.\ndiff --git a/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md b/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\nnew file mode 100644\nindex 0000000000000000000000000000000000000000..7455ce1f3bed02ddddb851ac21a1d1a648c9aeba\n--- /dev/null\n+++ b/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\n@@ -0,0 +1,55 @@\n+# SADR-0019: Separate Shiki Platform SADRs From Target ADRs\n+\n+## Status\n+\n+Accepted\n+\n+## Context\n+\n+Shiki installs its platform decision records into `docs/adr/` in every target\n+repository. Shiki and its targets previously used the same numeric\n+`NNNN-<slug>.md` namespace. A target that already owned ADRs 0013 through 0018\n+therefore acquired two records for each number during installation, making an\n+`ADR NNNN` citation ambiguous. Renumbering target decisions would rewrite target\n+history, while renumbering Shiki decisions per target would destroy their stable\n+platform identity.\n+\n+Upgrades also need to remove the former Shiki-owned numeric paths without ever\n+mistaking a target-owned ADR for platform content. The install stamp already\n+records exact shipped paths and content digests, so it can prove ownership only\n+when both still match.\n+\n+## Decision\n+\n+Shiki platform decisions use the `SADR` namespace:\n+\n+- records are named `docs/adr/SADR-NNNN-<slug>.md`;\n+- live platform citations use `SADR-NNNN`;\n+- target product decisions retain `docs/adr/NNNN-<slug>.md` and `ADR NNNN`;\n+- the reference validator resolves the two namespaces independently, so the same\n+  four-digit number may exist once in each namespace.\n+\n+Before a forced upgrade writes anything, the installer inventories the exact\n+former numeric Shiki ADR paths. It may delete a legacy path only when the\n+existing install stamp contains that exact path and its recorded SHA-256 digest\n+matches the file currently on disk. All ownership blockers are reported\n+together and abort the upgrade before its first write. A non-forced install\n+never performs this cleanup.\n+\n+Historical `.shiki/` evidence remains unchanged because it records the namespace\n+that existed when that evidence was produced.\n+\n+## Consequences\n+\n+- Targets may use any ADR number without colliding with Shiki's platform SADRs.\n+- Fresh installs preserve target ADR bytes and install a stable SADR set.\n+- Forced upgrades fail closed when legacy ownership cannot be proven, and a\n+  successful upgrade refreshes the stamp without removed legacy paths.\n+- Current governance, packaging, workflow, prompt, script, and skill references\n+  must move with the platform records.\n+\n+## Alternatives Considered\n+\n+- Collision-only refusal leaves affected targets permanently unupgradable.\n+- Renumbering target ADRs mutates target-owned history.\n+- Per-target Shiki renumbering makes platform citations non-portable.\ndiff --git a/docs/adr/template.md b/docs/adr/template.md\nindex a92a97daddd0b00ab9dd626cc8d86942da2eafed..17dd79175b5fee2105935557f691b40cf9492942\n--- a/docs/adr/template.md\n+++ b/docs/adr/template.md\n@@ -1,14 +1,14 @@\n-# NNNN: <Short Decision Title>\n+# <SADR-NNNN or NNNN>: <Short Decision Title>\n \n ## Status\n \n-Proposed | Accepted | Superseded by ADR-NNNN\n+Proposed | Accepted | Superseded by SADR-NNNN or target ADR NNNN\n \n ## Context\n \n What is the situation, constraint, or force that requires a decision? Reference\n-relevant domain terms (`CONTEXT.md`), prior ADRs, and code reality. State the\n-problem, not the solution.\n+relevant domain terms (`CONTEXT.md`), prior Shiki SADRs or target ADRs, and code\n+reality. State the problem, not the solution.\n \n ## Decision\n \ndiff --git a/docs/agents/autonomous-loop-e2e-acceptance.md b/docs/agents/autonomous-loop-e2e-acceptance.md\nindex bbf84c6081987f159ac6ad9ecf3c82b7aa72e7e8..5c13c1d02a05a90b0dc46cbef7c95a9846d0c237\n--- a/docs/agents/autonomous-loop-e2e-acceptance.md\n+++ b/docs/agents/autonomous-loop-e2e-acceptance.md\n@@ -40,8 +40,8 @@\n 3. `gh auth status` is logged in with merge permission on the repo.\n 4. Branch protection + required checks are configured exactly as the goal-loop\n    required-check set expects (see `docs/agents/branch-protection-smoke.md`).\n-5. The repo `.shiki/policy` permits autonomous low/medium-risk merges (ADR\n-   0008/0009); the MergeGate policy check is the recorded-authority gate.\n+5. The repo `.shiki/policy` permits autonomous low/medium-risk merges\n+   (SADR-0008/0009); the MergeGate policy check is the recorded-authority gate.\n \n The operator's only actions are: register the task and start the loop. **No\n manual pre-clearing** means after `shiki loop run` starts, the operator does not\ndiff --git a/docs/agents/domain.md b/docs/agents/domain.md\nindex 258da8ea81e87ac0a6b0b5b5e5c2fde980eaabb7..67aaa6f4b7d0a5aa4d495ce6279073333f7798cb\n--- a/docs/agents/domain.md\n+++ b/docs/agents/domain.md\n@@ -5,7 +5,9 @@\n ## Layout\n \n - `CONTEXT.md` defines Shiki language. Keep it implementation-free.\n-- `docs/adr/` records hard-to-reverse decisions.\n+- `docs/adr/SADR-NNNN-*.md` records Shiki platform decisions.\n+- `docs/adr/NNNN-*.md` records target product decisions without sharing the\n+  SADR namespace.\n - `AGENTS.md` is the runtime-neutral agent constitution.\n - `CLAUDE.md` is the Claude Code wrapper around `AGENTS.md`.\n - `.shiki/` is the Target Repository mirror for Goals, PRDs, plans, Task DAGs, contracts, locks, worktree records, ledger entries, CCA verdicts, repair packets, reports, and handoffs.\ndiff --git a/docs/agents/guardian-policy.md b/docs/agents/guardian-policy.md\nindex 4147c7adf05a47adb4fd24ada1a1c2e1f2ba6d2d..a20d9cc6c00a569bb057b9ca8d6b51c7fc4a9847\n--- a/docs/agents/guardian-policy.md\n+++ b/docs/agents/guardian-policy.md\n@@ -98,8 +98,8 @@\n `origin/<default_branch>` and reuses each job's existing base fetch; a shallow\n (`--depth`) or second base fetch would make the proof unresolvable and silently\n disable the carry, so neither is permitted. CCA reads `guardian_comment_carried` as\n-a recorded authority in the deterministic signal and does not re-derive it. See ADR\n-0018 for the full decision, the rejected diff/patch-equality and path-disjointness\n+a recorded authority in the deterministic signal and does not re-derive it. See\n+SADR-0018 for the full decision, the rejected diff/patch-equality and path-disjointness\n alternatives, and the accepted residual.\n \n ## External AI Guardian Review (SADR-0010)\ndiff --git a/docs/agents/implementation-policy.md b/docs/agents/implementation-policy.md\nindex c754144a9fb5bf4a38aa82a60e1670c59e626b44..e90d40fe21880b9a09719c5ecfe6bde7284eb3ce\n--- a/docs/agents/implementation-policy.md\n+++ b/docs/agents/implementation-policy.md\n@@ -31,6 +31,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n ## Non-Negotiables\n \n 1. **Planning precedes implementation.** Non-trivial Goals start with `grill-with-docs` and Context & Impact.\ndiff --git a/scripts/guardian_approval_signal.py b/scripts/guardian_approval_signal.py\nindex d059f92b2c82aa2b5d6ca421365537a87886c1cb..16924892714f4f5b9303c0ef0e325a7e70075630\n--- a/scripts/guardian_approval_signal.py\n+++ b/scripts/guardian_approval_signal.py\n@@ -199,8 +199,8 @@\n ) -> Any:\n     \"\"\"Evaluate SADR-0015 Contract Approval for the CCA Guardian signal.\n \n-    The SAME OR that MergeGate applies must reach this deterministic signal: ADR\n-    0015 is explicit that without it, a contract-approved PR still returns\n+    The SAME OR that MergeGate applies must reach this deterministic signal:\n+    SADR-0015 is explicit that without it, a contract-approved PR still returns\n     ``needs_guardian``, the CCA job fails, and the MergeGate policy check never\n     runs \u2014 the gate this change moves would never be reached. This resolves the\n     task the SAME way MergeGate does (the first Shiki task id in the PR body),\ndiff --git a/scripts/shiki_contract_approval.py b/scripts/shiki_contract_approval.py\nindex 941bcb96fdd8b9a85bc0c4f0f04d5dad9d04839c..4a4b418f6527413161db82670bec857a043360e2\n--- a/scripts/shiki_contract_approval.py\n+++ b/scripts/shiki_contract_approval.py\n@@ -142,7 +142,7 @@\n     changed_files_status: Any,\n     registration: Any,\n ) -> ContractApprovalResult:\n-    \"\"\"Whether this PR carries an SADR-0015 Contract Approval.\n+    \"\"\"Whether this PR carries a SADR-0015 Contract Approval.\n \n     The four conditions are evaluated IN ORDER, each failure recorded as a\n     distinct reason string:\ndiff --git a/scripts/shiki_installer.py b/scripts/shiki_installer.py\nindex be064ab7a29ff6836f7f7a1d70500a43e0d11d7b..d3e3247edbf3e4be287a4667176d86f6500f8dc5\n--- a/scripts/shiki_installer.py\n+++ b/scripts/shiki_installer.py\n@@ -118,6 +118,7 @@\n # what lets a target carry it without a governance-schema change.\n INSTALL_STAMP_PATH = \".shiki/install-stamp.json\"\n INSTALL_STAMP_VERSION = 1\n+LEGACY_SHIKI_SADR_MAX = 18\n \n # Under --force the shipped surface splits three ways, so nothing is lost and\n # nothing is silently stale.\n@@ -237,9 +238,98 @@\n         )\n \n \n+def legacy_shiki_adr_paths() -> tuple[str, ...]:\n+    \"\"\"Former numeric paths for Shiki SADR-0001 through SADR-0018.\n+\n+    The authoritative slugs come from the current SADR filenames. SADR-0019 is\n+    the namespace decision itself and never shipped under a numeric legacy path.\n+    \"\"\"\n+    paths: list[str] = []\n+    for sadr in sorted((ROOT / \"docs\" / \"adr\").glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\")):\n+        number = sadr.name.split(\"-\", 2)[1]\n+        if int(number) <= LEGACY_SHIKI_SADR_MAX:\n+            paths.append(f\"docs/adr/{sadr.name.removeprefix('SADR-')}\")\n+    return tuple(paths)\n+\n+\n+def preflight_legacy_sadr_cleanup(target: Path, *, force: bool) -> tuple[str, ...]:\n+    \"\"\"Authorize exact legacy Shiki ADR removals before the first write.\n+\n+    A non-force install never removes existing files. A forced install needs no\n+    stamp when no legacy Shiki path is present. When legacy paths do exist, each\n+    must be named in a readable install stamp and its current bytes must match\n+    the recorded digest.\n+    \"\"\"\n+    if not force:\n+        return ()\n+    present = tuple(\n+        relative\n+        for relative in legacy_shiki_adr_paths()\n+        if (target / relative).exists() or (target / relative).is_symlink()\n+    )\n+    if not present:\n+        return ()\n+\n+    stamp_path = target / INSTALL_STAMP_PATH\n+    try:\n+        raw = stamp_path.read_text(encoding=\"utf-8\")\n+        stamp = json.loads(raw)\n+    except FileNotFoundError:\n+        detail = \"install stamp is absent\"\n+        blockers = [f\"{relative}: {detail}\" for relative in present]\n+    except (OSError, UnicodeDecodeError) as error:\n+        detail = f\"install stamp is unreadable ({error})\"\n+        blockers = [f\"{relative}: {detail}\" for relative in present]\n+    except json.JSONDecodeError as error:\n+        detail = f\"install stamp is malformed ({error})\"\n+        blockers = [f\"{relative}: {detail}\" for relative in present]\n+    else:\n+        if not isinstance(stamp, dict):\n+            blockers = [\n+                f\"{relative}: install stamp is malformed (expected an object)\"\n+                for relative in present\n+            ]\n+        else:\n+            digests = stamp.get(\"digests\")\n+            blockers = []\n+            if not isinstance(digests, dict):\n+                blockers.extend(\n+                    f\"{relative}: install stamp has no digest for this path\"\n+                    for relative in present\n+                )\n+            else:\n+                for relative in present:\n+                    expected = digests.get(relative)\n+                    if not isinstance(expected, str) or not expected:\n+                        blockers.append(\n+                            f\"{relative}: install stamp has no digest for this path\"\n+                        )\n+                        continue\n+                    try:\n+                        actual = _sha256_file(target / relative)\n+                    except OSError as error:\n+                        blockers.append(\n+                            f\"{relative}: legacy path is unreadable ({error})\"\n+                        )\n+                        continue\n+                    if actual != expected:\n+                        blockers.append(\n+                            f\"{relative}: digest mismatch (stamp {expected}, current {actual})\"\n+                        )\n+\n+    if blockers:\n+        joined = \"\\n\".join(f\"- {blocker}\" for blocker in blockers)\n+        raise ShikiError(\n+            \"legacy Shiki ADR cleanup is not authorized:\\n\"\n+            f\"{joined}\\nNo files were written.\"\n+        )\n+    return present\n+\n+\n def install_template(target: Path, *, force: bool, validate: bool) -> list[NewFileNote]:\n     # Preconditions first: never rewrite the tree of a target that cannot be\n     # upgraded cleanly (see refuse_pending_migrations).\n+    legacy_cleanup = preflight_legacy_sadr_cleanup(target, force=force)\n     refuse_pending_migrations(target)\n \n     # Resolve the target's required CODEOWNERS owner once from its\n@@ -250,6 +340,10 @@\n     # verbatim. See ``codeowners_required_owner``.\n     codeowners_owner = codeowners_required_owner(target)\n \n+    for relative in legacy_cleanup:\n+        (target / relative).unlink()\n+        info(f\"removed stamped legacy Shiki ADR: {target / relative}\")\n+\n     notes: list[NewFileNote] = []\n     for relative in TEMPLATE_PATHS:\n         source = ROOT / relative\ndiff --git a/scripts/shiki_loop.py b/scripts/shiki_loop.py\nindex 3611641da8aeb5038b1da90e3114b09a473ec6d8..84caa491b9cad0edc4176f209c13e9221e975681\n--- a/scripts/shiki_loop.py\n+++ b/scripts/shiki_loop.py\n@@ -424,7 +424,7 @@\n         # unresolvable CCA verdict has already produced its terminal stop and is never\n         # laundered into a head-moving sync (SADR-0010). It also never fires while the\n         # policy (Guardian) gate is red \u2014 for ANY reason, not only when it is the sole\n-        # red check \u2014 so a head-bound Guardian approval, or an SADR-0015\n+        # red check \u2014 so a head-bound Guardian approval, or a SADR-0015\n         # contract-approval carry that base movement defeated (the policy gate compares\n         # base and head task governance directly), is never silently restored by\n         # adopting base wholesale. It is bounded by a durable per-task counter so the\ndiff --git a/scripts/test_shiki_goal_loop.sh b/scripts/test_shiki_goal_loop.sh\nindex 753c7b516e10f0be3bc92a4bd0eb1fa699776ae6..8f3820a0a079b3044424cfe1a6c6b7fe68896e67\n--- a/scripts/test_shiki_goal_loop.sh\n+++ b/scripts/test_shiki_goal_loop.sh\n@@ -231,8 +231,8 @@\n \n # High/critical risk merges autonomously when all required checks are green:\n # the \"MergeGate policy check\" required check IS the Guardian gate (it enforces\n-# guardian-policy.json \u2014 human approval OR external AI guardian review, ADR\n-# 0010), so green checks mean a recorded authority approved. The fake gh returns\n+# guardian-policy.json \u2014 human approval OR external AI guardian review,\n+# SADR-0010), so green checks mean a recorded authority approved. The fake gh returns\n # green checks, simulating that the Guardian gate passed.\n python3 \"$ROOT/scripts/shiki.py\" goal create --target \"$TARGET\" --title \"Guardian gate\" --outcome \"High risk merges via policy gate\" >/tmp/shiki-goal-loop-guardian-goal.json\n GGOAL=\"$(json_get /tmp/shiki-goal-loop-guardian-goal.json goal_id)\"\ndiff --git a/tests/test_adr_references.py b/tests/test_adr_references.py\nindex 51706329c797d5dd9b47e082a333fa83b261d482..db6e693e35f67103935be34f1d9bb4ff7ee60395\n--- a/tests/test_adr_references.py\n+++ b/tests/test_adr_references.py\n@@ -1,7 +1,7 @@\n-\"\"\"Every ``ADR ####`` citation must resolve to exactly one decision record.\n+\"\"\"Every target ADR and Shiki SADR citation resolves in its own namespace.\n \n Background. ``scripts/mergegate_check.py`` justifies ``is_bookkeeping_closeout``\n-with \"ADR 0017\" nine times and ``scripts/guardian_approval_signal.py`` cites\n+with \"SADR-0017\" nine times and ``scripts/guardian_approval_signal.py`` cites\n 0016/0017 seven times, yet for a stretch the numbering in ``docs/adr/`` stopped\n at 0015: the authoring commits lived on a coordinator branch and no task ever\n declared ``docs/adr/...`` in its locks, so nothing carried the records to the\n@@ -11,11 +11,12 @@\n and the directory drifted with no signal.\n \n This is the same unbound-pair shape behind other silent failures: a reference\n-and its target with nothing asserting they agree. This test binds them. It scans\n-every ``scripts/*.py`` (plus ``docs/**/*.md`` and the root ``*.md`` governance\n-files, which are cheap) for the ``ADR ####`` pattern and asserts each cited\n-number resolves to exactly one ``docs/adr/####-*.md``. The converse is\n-deliberately NOT required: an ADR may exist without being cited.\n+and its target with nothing asserting they agree. This test binds them. Target\n+``ADR NNNN`` citations resolve only to ``docs/adr/NNNN-*.md``; Shiki\n+``SADR-NNNN`` citations resolve only to ``docs/adr/SADR-NNNN-*.md``. The scan\n+covers current governance docs, scripts, workflows, prompts, packaging metadata,\n+and repo-local Codex guidance while deliberately excluding historical\n+``.shiki`` evidence. The converse is not required: a record may be uncited.\n \n The suite is dependency free: standard-library ``unittest`` only.\n \"\"\"\n@@ -31,17 +32,18 @@\n \n REPO_ROOT = Path(__file__).resolve().parents[1]\n \n-# \"ADR 0016\" -- a four-digit citation. The trailing \\b keeps a five-digit token\n-# from being read as a truncated four-digit number.\n-_CITATION = re.compile(r\"\\bADR (\\d{4})\\b\")\n-# \"0016-locks-bound-product-paths.md\" -- the leading four digits name the record.\n-_ADR_FILENAME = re.compile(r\"^(\\d{4})-.*\\.md$\")\n+# ``ADR 0016`` / ``SADR-0016`` -- a namespace plus four-digit citation. The\n+# trailing ``\\b`` keeps a five-digit token from being read as a truncated one.\n+_CITATION = re.compile(r\"\\b(S?ADR)[ -](\\d{4})\\b\")\n+# Target ADRs retain the numeric filename; Shiki platform decisions use SADR.\n+_DECISION_FILENAME = re.compile(r\"^(?:(SADR)-)?(\\d{4})-.*\\.md$\")\n \n \n class Violation(NamedTuple):\n     \"\"\"A citation that does not resolve to exactly one decision record.\"\"\"\n \n     citing_file: str  # repo-relative path of the file that cites the ADR\n+    namespace: str  # \"ADR\" for target decisions; \"SADR\" for Shiki decisions\n     number: str  # the cited four-digit number, e.g. \"0016\"\n     line: int  # 1-based line of the citation\n     reason: str  # \"missing\" (zero matches) or \"ambiguous\" (two or more)\n@@ -49,44 +51,59 @@\n \n     @property\n     def expected_path(self) -> str:\n-        return f\"docs/adr/{self.number}-*.md\"\n+        prefix = \"SADR-\" if self.namespace == \"SADR\" else \"\"\n+        return f\"docs/adr/{prefix}{self.number}-*.md\"\n \n     def describe(self) -> str:\n         \"\"\"Name the citing file, the cited number, and the missing path.\"\"\"\n         if self.reason == \"missing\":\n             return (\n-                f\"{self.citing_file}:{self.line} cites ADR {self.number} \"\n+                f\"{self.citing_file}:{self.line} cites {self.namespace} {self.number} \"\n                 f\"but no document matches {self.expected_path}\"\n             )\n         joined = \", \".join(self.matches)\n         return (\n-            f\"{self.citing_file}:{self.line} cites ADR {self.number} which \"\n+            f\"{self.citing_file}:{self.line} cites {self.namespace} {self.number} which \"\n             f\"resolves to {len(self.matches)} documents (expected exactly one \"\n             f\"at {self.expected_path}): {joined}\"\n         )\n \n \n-def _adr_index(root: Path) -> dict[str, list[Path]]:\n-    \"\"\"Map every four-digit ADR number to the record files that claim it.\"\"\"\n-    index: dict[str, list[Path]] = {}\n+def _adr_index(root: Path) -> dict[tuple[str, str], list[Path]]:\n+    \"\"\"Map each decision namespace and number to the files that claim it.\"\"\"\n+    index: dict[tuple[str, str], list[Path]] = {}\n     adr_dir = root / \"docs\" / \"adr\"\n     if adr_dir.is_dir():\n         for path in sorted(adr_dir.glob(\"*.md\")):\n-            match = _ADR_FILENAME.match(path.name)\n+            match = _DECISION_FILENAME.match(path.name)\n             if match:\n-                index.setdefault(match.group(1), []).append(path)\n+                namespace = \"SADR\" if match.group(1) else \"ADR\"\n+                index.setdefault((namespace, match.group(2)), []).append(path)\n     return index\n \n \n def _citing_files(root: Path, include_docs: bool = True) -> list[Path]:\n-    \"\"\"Files scanned for citations: scripts/*.py, and cheaply the markdown.\"\"\"\n+    \"\"\"Current, shipped text surfaces scanned for decision citations.\"\"\"\n     files = sorted((root / \"scripts\").glob(\"*.py\"))\n+    files += sorted((root / \"scripts\").glob(\"*.sh\"))\n     if include_docs:\n         docs = root / \"docs\"\n         if docs.is_dir():\n             files += sorted(docs.rglob(\"*.md\"))\n         files += sorted(root.glob(\"*.md\"))\n-    return files\n+        pyproject = root / \"pyproject.toml\"\n+        if pyproject.is_file():\n+            files.append(pyproject)\n+        for relative in (\".github\", \".codex\"):\n+            surface = root / relative\n+            if surface.is_dir():\n+                files += sorted(\n+                    path\n+                    for path in surface.rglob(\"*\")\n+                    if path.is_file()\n+                    and path.suffix in {\".md\", \".py\", \".sh\", \".toml\", \".yaml\", \".yml\"}\n+                )\n+    return sorted(set(files))\n \n \n def find_adr_reference_violations(\n@@ -94,10 +111,9 @@\n ) -> list[Violation]:\n     \"\"\"Return every ``ADR ####`` citation under ``root`` that fails to resolve.\n \n-    A citation resolves when exactly one ``docs/adr/####-*.md`` matches its\n-    number. Zero matches is a \"missing\" violation; two or more is \"ambiguous\".\n-    Records that are never cited produce nothing -- the converse is not\n-    required.\n+    A citation resolves when exactly one record in the same namespace matches\n+    its number. Zero matches is a \"missing\" violation; two or more is\n+    \"ambiguous\". Records that are never cited produce nothing.\n     \"\"\"\n     root = Path(root)\n     index = _adr_index(root)\n@@ -110,13 +126,15 @@\n         rel = path.relative_to(root).as_posix()\n         for lineno, line in enumerate(text.splitlines(), start=1):\n             for match in _CITATION.finditer(line):\n-                number = match.group(1)\n-                found = index.get(number, [])\n+                namespace, number = match.groups()\n+                found = index.get((namespace, number), [])\n                 if len(found) == 1:\n                     continue\n                 reason = \"missing\" if not found else \"ambiguous\"\n                 rels = [p.relative_to(root).as_posix() for p in found]\n-                violations.append(Violation(rel, number, lineno, reason, rels))\n+                violations.append(\n+                    Violation(rel, namespace, number, lineno, reason, rels)\n+                )\n     return violations\n \n \n@@ -129,8 +147,8 @@\n         self.assertEqual(\n             violations,\n             [],\n-            \"Every 'ADR ####' citation must resolve to exactly one \"\n-            f\"docs/adr/####-*.md:\\n{detail}\",\n+            \"Every ADR/SADR citation must resolve exactly once in its own \"\n+            f\"docs/adr namespace:\\n{detail}\",\n         )\n \n     def test_scan_actually_reads_scripts(self) -> None:\n@@ -146,8 +164,21 @@\n         # The two records this Goal carried onto the default branch, and which\n         # the scripts cite most, must be present.\n         index = _adr_index(REPO_ROOT)\n-        self.assertIn(\"0016\", index)\n-        self.assertIn(\"0017\", index)\n+        self.assertIn((\"SADR\", \"0016\"), index)\n+        self.assertIn((\"SADR\", \"0017\"), index)\n+\n+    def test_platform_tree_has_only_sadr_0001_through_0019(self) -> None:\n+        adr_dir = REPO_ROOT / \"docs\" / \"adr\"\n+        sadrs = sorted(adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\"))\n+        self.assertEqual(\n+            {path.name.split(\"-\", 2)[1] for path in sadrs},\n+            {f\"{number:04d}\" for number in range(1, 20)},\n+        )\n+        self.assertEqual(\n+            list(adr_dir.glob(\"[0-9][0-9][0-9][0-9]-*.md\")),\n+            [],\n+            \"the Shiki platform tree must not retain legacy numeric SADR paths\",\n+        )\n \n \n class FixtureAdrReferences(unittest.TestCase):\n@@ -180,6 +211,26 @@\n         self.assertIn(\"9991\", message)  # cited number\n         self.assertIn(\"docs/adr/9991-*.md\", message)  # missing path\n \n+    def test_target_adr_does_not_satisfy_missing_platform_sadr(self) -> None:\n+        root = self._make_repo(\n+            adr_names=[\"0042-target-decision.md\"],\n+            script_body='\"\"\"Target ADR 0042 exists, but platform SADR-0042 does not.\"\"\"\\n',\n+        )\n+        violations = find_adr_reference_violations(root, include_docs=False)\n+        self.assertEqual(len(violations), 1)\n+        self.assertEqual(violations[0].namespace, \"SADR\")\n+        self.assertEqual(violations[0].number, \"0042\")\n+        self.assertEqual(violations[0].expected_path, \"docs/adr/SADR-0042-*.md\")\n+\n+    def test_target_adr_and_platform_sadr_may_reuse_a_number(self) -> None:\n+        root = self._make_repo(\n+            adr_names=[\"0042-target-decision.md\", \"SADR-0042-platform-decision.md\"],\n+            script_body='\"\"\"Target ADR 0042 and platform SADR-0042 are distinct.\"\"\"\\n',\n+        )\n+        self.assertEqual(\n+            find_adr_reference_violations(root, include_docs=False), []\n+        )\n+\n     def test_uncited_adr_is_allowed(self) -> None:\n         # An ADR exists but nothing cites it -> not a violation. The converse\n         # (every ADR must be cited) is explicitly NOT required.\ndiff --git a/tests/test_policy_job_merged_prs.py b/tests/test_policy_job_merged_prs.py\nindex d45512e74681e5e649279236c7d4e79d5c3da471..b97989f8bd74d091de1a6b5275a20d8dc68c4512\n--- a/tests/test_policy_job_merged_prs.py\n+++ b/tests/test_policy_job_merged_prs.py\n@@ -3,8 +3,8 @@\n \n ``.github/workflows/shiki-cca-completion.yml`` has two consumers of the merged-PR\n proof: the CCA *signal* (``guardian_approval_signal.py`` in \"Prepare CCA context\")\n-and the authoritative gate (``mergegate_check.py`` in \"Enforce MergeGate\"). ADR\n-0017's bookkeeping-closeout exemption (condition 6) needs the merged implementation\n+and the authoritative gate (``mergegate_check.py`` in \"Enforce MergeGate\").\n+SADR-0017's bookkeeping-closeout exemption (condition 6) needs the merged implementation\n PR proven, and a closeout carries NO ``post_merge_reconcile`` marker. The signal\n already resolves ``MERGED_PRS`` unconditionally, but \"Enforce MergeGate\" used to\n resolve it only inside ``grep -q '<!-- shiki:post_merge_reconcile -->'``. So a\ndiff --git a/tests/test_sadr_install_migration.py b/tests/test_sadr_install_migration.py\nnew file mode 100644\nindex 0000000000000000000000000000000000000000..5a442fa5e4968ade178dcdedc3c63b54dba580a2\n--- /dev/null\n+++ b/tests/test_sadr_install_migration.py\n@@ -0,0 +1,224 @@\n+\"\"\"SADR installation and collision-safe legacy upgrade behavior.\"\"\"\n+\n+from __future__ import annotations\n+\n+import contextlib\n+import hashlib\n+import io\n+import json\n+import shutil\n+import tempfile\n+import unittest\n+from pathlib import Path\n+\n+import shiki_test_support  # noqa: F401  (path bootstrap)\n+\n+import shiki_installer\n+from tests.test_adr_references import find_adr_reference_violations\n+\n+\n+PRODUCT_NUMBERS = tuple(range(13, 19))\n+\n+\n+def _quiet_install(target: Path, *, force: bool, validate: bool) -> None:\n+    with contextlib.redirect_stdout(io.StringIO()):\n+        shiki_installer.install_template(target, force=force, validate=validate)\n+\n+\n+def _sha256(path: Path) -> str:\n+    return hashlib.sha256(path.read_bytes()).hexdigest()\n+\n+\n+def _legacy_sadr_paths() -> tuple[str, ...]:\n+    return tuple(\n+        f\"docs/adr/{path.name.removeprefix('SADR-')}\"\n+        for path in sorted((shiki_installer.ROOT / \"docs\" / \"adr\").glob(\"SADR-*.md\"))\n+        if path.name != \"SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\"\n+    )\n+\n+\n+def _create_legacy_upgrade_fixture(\n+    target: Path,\n+) -> tuple[dict[str, bytes], tuple[str, ...]]:\n+    adr_dir = target / \"docs\" / \"adr\"\n+    adr_dir.mkdir(parents=True)\n+    product_bytes = {\n+        f\"{number:04d}-cubeshare-product-decision.md\": (\n+            f\"# CubeShare ADR {number:04d}\\n\\nDo not rewrite {number}.\\n\"\n+        ).encode()\n+        for number in PRODUCT_NUMBERS\n+    }\n+    for name, content in product_bytes.items():\n+        (adr_dir / name).write_bytes(content)\n+\n+    legacy_paths = _legacy_sadr_paths()\n+    for relative in legacy_paths:\n+        (target / relative).write_bytes(f\"legacy Shiki bytes for {relative}\\n\".encode())\n+\n+    stamp_path = target / shiki_installer.INSTALL_STAMP_PATH\n+    stamp_path.parent.mkdir(parents=True, exist_ok=True)\n+    stamp = {\n+        \"version\": shiki_installer.INSTALL_STAMP_VERSION,\n+        \"platform_commit\": None,\n+        \"installed_at\": \"2000-01-01T00:00:00+00:00\",\n+        \"digests\": {\n+            relative: _sha256(target / relative) for relative in legacy_paths\n+        },\n+    }\n+    stamp_path.write_text(\n+        json.dumps(stamp, indent=2, sort_keys=True) + \"\\n\", encoding=\"utf-8\"\n+    )\n+    return product_bytes, legacy_paths\n+\n+\n+def _tree_snapshot(root: Path) -> dict[str, tuple[str, bytes | str]]:\n+    \"\"\"Recursive byte/shape snapshot used to prove preflight is write-free.\"\"\"\n+    snapshot: dict[str, tuple[str, bytes | str]] = {}\n+    for path in sorted(root.rglob(\"*\")):\n+        relative = path.relative_to(root).as_posix()\n+        if path.is_symlink():\n+            snapshot[relative] = (\"symlink\", path.readlink().as_posix())\n+        elif path.is_dir():\n+            snapshot[relative] = (\"directory\", \"\")\n+        else:\n+            snapshot[relative] = (\"file\", path.read_bytes())\n+    return snapshot\n+\n+\n+class FreshSadrInstallTests(unittest.TestCase):\n+    def setUp(self) -> None:\n+        self.tmp = Path(tempfile.mkdtemp(prefix=\"shiki-sadr-fresh-\"))\n+        self.addCleanup(lambda: shutil.rmtree(self.tmp, ignore_errors=True))\n+        self.target = self.tmp / \"target\"\n+        self.adr_dir = self.target / \"docs\" / \"adr\"\n+        self.adr_dir.mkdir(parents=True)\n+        self.product_bytes = {\n+            f\"{number:04d}-product-decision.md\": (\n+                f\"# Product ADR {number:04d}\\n\\nTarget-owned bytes {number}.\\n\"\n+            ).encode()\n+            for number in PRODUCT_NUMBERS\n+        }\n+        for name, content in self.product_bytes.items():\n+            (self.adr_dir / name).write_bytes(content)\n+\n+    def test_fresh_install_preserves_product_adrs_and_installs_all_sadrs(self) -> None:\n+        _quiet_install(self.target, force=False, validate=True)\n+\n+        for name, expected in self.product_bytes.items():\n+            self.assertEqual((self.adr_dir / name).read_bytes(), expected)\n+\n+        sadr_numbers = {\n+            path.name.split(\"-\", 2)[1]\n+            for path in self.adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\")\n+        }\n+        self.assertEqual(sadr_numbers, {f\"{number:04d}\" for number in range(1, 20)})\n+\n+        numeric_records = {\n+            path.name\n+            for path in self.adr_dir.glob(\"[0-9][0-9][0-9][0-9]-*.md\")\n+        }\n+        self.assertEqual(numeric_records, set(self.product_bytes))\n+        self.assertEqual(find_adr_reference_violations(self.target), [])\n+\n+\n+class LegacySadrUpgradeTests(unittest.TestCase):\n+    def setUp(self) -> None:\n+        self.tmp = Path(tempfile.mkdtemp(prefix=\"shiki-sadr-upgrade-\"))\n+        self.addCleanup(lambda: shutil.rmtree(self.tmp, ignore_errors=True))\n+        self.target = self.tmp / \"target\"\n+        self.adr_dir = self.target / \"docs\" / \"adr\"\n+        self.product_bytes, self.legacy_paths = _create_legacy_upgrade_fixture(\n+            self.target\n+        )\n+\n+    def test_force_upgrade_deletes_only_stamped_legacy_sadrs(self) -> None:\n+        _quiet_install(self.target, force=True, validate=True)\n+\n+        for relative in self.legacy_paths:\n+            self.assertFalse((self.target / relative).exists(), relative)\n+        for name, expected in self.product_bytes.items():\n+            self.assertEqual((self.adr_dir / name).read_bytes(), expected)\n+\n+        sadrs = list(self.adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\"))\n+        self.assertEqual(len(sadrs), 19)\n+        refreshed = json.loads(\n+            (self.target / shiki_installer.INSTALL_STAMP_PATH).read_text(encoding=\"utf-8\")\n+        )\n+        for relative in self.legacy_paths:\n+            self.assertNotIn(relative, refreshed[\"digests\"])\n+        for path in sadrs:\n+            relative = path.relative_to(self.target).as_posix()\n+            self.assertEqual(refreshed[\"digests\"][relative], _sha256(path))\n+\n+    def test_non_force_install_never_deletes_legacy_files(self) -> None:\n+        before = {\n+            relative: (self.target / relative).read_bytes()\n+            for relative in self.legacy_paths\n+        }\n+        _quiet_install(self.target, force=False, validate=False)\n+        for relative, expected in before.items():\n+            self.assertEqual((self.target / relative).read_bytes(), expected)\n+\n+\n+class LegacyCleanupPreflightTests(unittest.TestCase):\n+    def setUp(self) -> None:\n+        self.tmp = Path(tempfile.mkdtemp(prefix=\"shiki-sadr-preflight-\"))\n+        self.addCleanup(lambda: shutil.rmtree(self.tmp, ignore_errors=True))\n+\n+    def test_all_ownership_blockers_abort_before_any_write(self) -> None:\n+        cases = (\n+            (\"absent\", \"install stamp is absent\"),\n+            (\"unreadable\", \"install stamp is unreadable\"),\n+            (\"malformed\", \"install stamp is malformed\"),\n+            (\"missing-digest\", \"install stamp has no digest\"),\n+            (\"digest-mismatch\", \"digest mismatch\"),\n+        )\n+        for name, expected_reason in cases:\n+            with self.subTest(name=name):\n+                target = self.tmp / name\n+                _, legacy_paths = _create_legacy_upgrade_fixture(target)\n+                stamp_path = target / shiki_installer.INSTALL_STAMP_PATH\n+\n+                if name == \"absent\":\n+                    stamp_path.unlink()\n+                elif name == \"unreadable\":\n+                    stamp_path.unlink()\n+                    stamp_path.mkdir()\n+                elif name == \"malformed\":\n+                    stamp_path.write_text(\"{not-json\\n\", encoding=\"utf-8\")\n+                else:\n+                    stamp = json.loads(stamp_path.read_text(encoding=\"utf-8\"))\n+                    if name == \"missing-digest\":\n+                        stamp[\"digests\"] = {}\n+                    else:\n+                        stamp[\"digests\"] = {\n+                            relative: \"deadbeef\" for relative in legacy_paths\n+                        }\n+                    stamp_path.write_text(\n+                        json.dumps(stamp, indent=2, sort_keys=True) + \"\\n\",\n+                        encoding=\"utf-8\",\n+                    )\n+\n+                before = _tree_snapshot(target)\n+                with self.assertRaises(shiki_installer.ShikiError) as raised:\n+                    _quiet_install(target, force=True, validate=False)\n+                after = _tree_snapshot(target)\n+\n+                self.assertEqual(before, after, f\"{name} preflight changed the tree\")\n+                message = str(raised.exception)\n+                self.assertIn(expected_reason, message)\n+                self.assertIn(\"No files were written\", message)\n+                for relative in legacy_paths:\n+                    self.assertIn(relative, message)\n+\n+    def test_absent_stamp_is_allowed_when_no_legacy_path_exists(self) -> None:\n+        target = self.tmp / \"no-legacy\"\n+        target.mkdir()\n+        _quiet_install(target, force=True, validate=False)\n+        self.assertTrue(\n+            (target / \"docs\" / \"adr\" / \"SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\").is_file()\n+        )\n+\n+\n+if __name__ == \"__main__\":\n+    unittest.main()\n\ndiff --git a/.codex/skills/shiki/SKILL.md b/.codex/skills/shiki/SKILL.md\nindex c8e2e85d9d75d49cc4f2d6286ee22659c4e53e75..93cded623b2c15d9f65c76491a1c2d813ac9e6c0\n--- a/.codex/skills/shiki/SKILL.md\n+++ b/.codex/skills/shiki/SKILL.md\n@@ -18,6 +18,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n Codex CLI does not register this skill as a custom `/shiki` slash command. When\n the user types `/shiki` in Codex CLI and gets \"Unrecognized command\", explain\n that this is expected: invoke Shiki in Codex with natural language such as\n@@ -69,7 +72,7 @@\n \n ## Responsibilities\n \n-- Claude Code implements and repairs by default (`shiki runner claude`, ADR 0008); Codex implements and repairs tasks explicitly assigned to `codex`.\n+- Claude Code implements and repairs by default (`shiki runner claude`, SADR-0008); Codex implements and repairs tasks explicitly assigned to `codex`.\n - Claude Code Action can act as GitHub-side CCA or reviewer.\n - CCA judges completion.\n - MergeGate authorizes state transitions and merge readiness.\ndiff --git a/.github/prompts/cca-completion-check.md b/.github/prompts/cca-completion-check.md\nindex ed203ad5f302ecc81372d8ab27d2b1232ca97497..fd89950528333ef6242e6d5443904f5392ab7e56\n--- a/.github/prompts/cca-completion-check.md\n+++ b/.github/prompts/cca-completion-check.md\n@@ -19,6 +19,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n ## Required Reading\n \n Read only the files needed to judge the PR contract. Prefer this order:\ndiff --git a/AGENTS.md b/AGENTS.md\nindex 7f041100d49c2a7525f51cb0f5d696a4a6d1c78c..2b700e989ff295987aa8a4b0c5fcb1acd80020da\n--- a/AGENTS.md\n+++ b/AGENTS.md\n@@ -44,6 +44,10 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use\n+`SADR-NNNN-*.md`; target product decisions retain `NNNN-*.md` and the `ADR`\n+namespace.\n+\n Use the canonical source-of-truth order above. Runtime-specific reading orders\n are operational guidance only and must not override or reorder it.\n \ndiff --git a/CLAUDE.md b/CLAUDE.md\nindex 98a932e7841a632866fcd53b0bad0e3af3711d3d..611d491912d6a72a0327a1731b8f33a41acb4baa\n--- a/CLAUDE.md\n+++ b/CLAUDE.md\n@@ -14,6 +14,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n ## Claude Role\n \n Default to planner, default implementer, reviewer, coordinator, documentation editor, CCA-style completion judge, and final-judgment assistant.\ndiff --git a/SYSTEM_PROMPT.md b/SYSTEM_PROMPT.md\nindex aaaea45e3a896c3a44ba89eec8bb84325ecf6c62..2e7c093ebe9e8bb34e0923756f191b4f7fa581f5\n--- a/SYSTEM_PROMPT.md\n+++ b/SYSTEM_PROMPT.md\n@@ -30,6 +30,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n Use the canonical source-of-truth order above. Runtime-specific reading orders\n are operational guidance only and must not override or reorder it. The current\n conversation is non-durable operator input.\ndiff --git a/docs/adr/0001-keep-shiki-as-platform-name.md b/docs/adr/SADR-0001-keep-shiki-as-platform-name.md\nindex 20a9637c3b9d8c97de7ba8db7cfae4d82d9a7dba..8b685439959aada1b221c112f0ac3c51ca8a1695\n--- a/docs/adr/0001-keep-shiki-as-platform-name.md\n+++ b/docs/adr/SADR-0001-keep-shiki-as-platform-name.md\n@@ -1,3 +1,3 @@\n-# Keep Shiki As The Platform Name\n+# SADR-0001: Keep Shiki As The Platform Name\n \n Shiki remains the product name for the GitHub-native agentic engineering control plane. We considered more descriptive names such as MergePilot, MergeGate, and Kiroku, but kept Shiki because it carries the existing goal-seeking framework identity while remaining independent of Hermes, Codex, Claude Code, or any single runtime.\ndiff --git a/docs/adr/0002-use-agents-as-shared-constitution.md b/docs/adr/SADR-0002-use-agents-as-shared-constitution.md\nindex 73de30ecb078120eac540d9bc51855e3cd48d0e3..c5486182bf06fca36ea508496b013c67302fd989\n--- a/docs/adr/0002-use-agents-as-shared-constitution.md\n+++ b/docs/adr/SADR-0002-use-agents-as-shared-constitution.md\n@@ -1,3 +1,3 @@\n-# Use AGENTS As The Shared Agent Constitution\n+# SADR-0002: Use AGENTS As The Shared Agent Constitution\n \n Shiki uses `AGENTS.md` as the runtime-neutral constitution and `CLAUDE.md` as a thin Claude Code wrapper. This keeps Codex, Claude Code, Hermes Runner, and future Agent Runtimes aligned on the same GitHub-first MergeGate rules while still allowing runtime-specific instructions where needed.\ndiff --git a/docs/adr/0003-require-skill-gate-for-mergegate-readiness.md b/docs/adr/SADR-0003-require-skill-gate-for-mergegate-readiness.md\nindex 424be0c9af4c8f0136f1fde68a468c47ff84e764..7fc337987c7df9792f75f181dfcbed75fe5ffff2\n--- a/docs/adr/0003-require-skill-gate-for-mergegate-readiness.md\n+++ b/docs/adr/SADR-0003-require-skill-gate-for-mergegate-readiness.md\n@@ -1,3 +1,3 @@\n-# Require Skill Gate For MergeGate Readiness\n+# SADR-0003: Require Skill Gate For MergeGate Readiness\n \n Shiki requires relevant engineering skills to be invoked before execution when their triggers apply. This makes skills part of MergeGate readiness rather than optional prompt style, because Shiki depends on repeatable planning, issue decomposition, TDD, diagnosis, triage, and architecture review before autonomous or parallel agent work can be trusted.\ndiff --git a/docs/adr/0004-use-github-first-with-shiki-mirror.md b/docs/adr/SADR-0004-use-github-first-with-shiki-mirror.md\nindex d0a3a9174dc646a945a9d325954ffaf00c352040..a79c54ae9ee8eed2a0524192b864da4e22a92d3f\n--- a/docs/adr/0004-use-github-first-with-shiki-mirror.md\n+++ b/docs/adr/SADR-0004-use-github-first-with-shiki-mirror.md\n@@ -1,4 +1,4 @@\n-# ADR 0004: Use GitHub First With A Repository-Local Shiki Mirror\n+# SADR-0004: Use GitHub First With A Repository-Local Shiki Mirror\n \n ## Status\n \ndiff --git a/docs/adr/0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md b/docs/adr/SADR-0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\nindex b339baf49b89d9290e79c3d51b36e7b2bde3bd95..c30a0d8b1fde0224d0ac7820e7870dccf721580d\n--- a/docs/adr/0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\n+++ b/docs/adr/SADR-0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\n@@ -1,4 +1,4 @@\n-# ADR 0005: Use Codex As Front And Claude Code Action As The GitHub Runtime\n+# SADR-0005: Use Codex As Front And Claude Code Action As The GitHub Runtime\n \n ## Status\n \ndiff --git a/docs/adr/0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md b/docs/adr/SADR-0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\nindex 4bcae9d1e52f0a37b8e9fbafa5cd14b6ee1993bd..7a38538a6ae9929567f0ade67e7ea6a60d0b3f7e\n--- a/docs/adr/0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\n+++ b/docs/adr/SADR-0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\n@@ -1,4 +1,4 @@\n-# ADR 0006: Separate Codex Implementation, CCA Completion, And MergeGate Authorization\n+# SADR-0006: Separate Codex Implementation, CCA Completion, And MergeGate Authorization\n \n ## Status\n \ndiff --git a/docs/adr/0007-packaging-and-release.md b/docs/adr/SADR-0007-packaging-and-release.md\nindex 79b84ddb97167372b44641e5b7860e400ce80822..023cd57f8011aa6e6d2ebf612b7c35a7a82c00d4\n--- a/docs/adr/0007-packaging-and-release.md\n+++ b/docs/adr/SADR-0007-packaging-and-release.md\n@@ -1,4 +1,4 @@\n-# ADR 0007: Packaging And Release For Shiki\n+# SADR-0007: Packaging And Release For Shiki\n \n ## Status\n \ndiff --git a/docs/adr/0008-use-claude-code-as-default-implementer.md b/docs/adr/SADR-0008-use-claude-code-as-default-implementer.md\nindex dedf4510b7c7799521422f622029563d373db031..a158a8d444d93764ec515a28133f7dde4af23579\n--- a/docs/adr/0008-use-claude-code-as-default-implementer.md\n+++ b/docs/adr/SADR-0008-use-claude-code-as-default-implementer.md\n@@ -1,4 +1,4 @@\n-# ADR 0008: Use Claude Code As The Default Implementer Runtime\n+# SADR-0008: Use Claude Code As The Default Implementer Runtime\n \n ## Status\n \ndiff --git a/docs/adr/0009-requirements-definition-and-spec-freeze.md b/docs/adr/SADR-0009-requirements-definition-and-spec-freeze.md\nindex 29b3f7871bef9203a8b4a831a65c39eb2f506e42..52e7dd69ad0aa4efe0b68c62678abbf7df02aaec\n--- a/docs/adr/0009-requirements-definition-and-spec-freeze.md\n+++ b/docs/adr/SADR-0009-requirements-definition-and-spec-freeze.md\n@@ -1,4 +1,4 @@\n-# ADR 0009: Merge Grill And Planning Into Requirements Definition With Spec Freeze\n+# SADR-0009: Merge Grill And Planning Into Requirements Definition With Spec Freeze\n \n ## Status\n \ndiff --git a/docs/adr/0010-external-ai-guardian-review.md b/docs/adr/SADR-0010-external-ai-guardian-review.md\nindex e6108bbe8bb4e73e0ff4684c8f78001a80438c4e..94ab21f40c326063a8833245d0ee8db67af0395d\n--- a/docs/adr/0010-external-ai-guardian-review.md\n+++ b/docs/adr/SADR-0010-external-ai-guardian-review.md\n@@ -1,4 +1,4 @@\n-# ADR 0010: External AI Guardian Review As A First-Class Approval Authority\n+# SADR-0010: External AI Guardian Review As A First-Class Approval Authority\n \n ## Status\n \n@@ -89,7 +89,7 @@\n   autonomous design the operator intends and produced the impersonation\n   incident.\n - **Record AI approval as an operator comment**: rejected as forgery of human\n-  authority \u2014 the exact failure this ADR corrects.\n+  authority \u2014 the exact failure this SADR corrects.\n - **Deliver the artifact as a committed `.shiki/` file**: rejected; a committed\n   approval inside the PR being approved is forgeable by anyone who can push.\n - **A dedicated reviewer bot identity instead of a relayed comment**: deferred;\ndiff --git a/docs/adr/0011-autonomous-quality-gate-evidence.md b/docs/adr/SADR-0011-autonomous-quality-gate-evidence.md\nindex bca0246c5d46e44afbde15486a75eec425fc02f5..9c93eac16d8c3a6d4b11bd7224e3955102cd7db4\n--- a/docs/adr/0011-autonomous-quality-gate-evidence.md\n+++ b/docs/adr/SADR-0011-autonomous-quality-gate-evidence.md\n@@ -1,4 +1,4 @@\n-# ADR 0011: Autonomous Quality-Gate Evidence \u2014 Deterministic Facts And An Independent Verifier\n+# SADR-0011: Autonomous Quality-Gate Evidence \u2014 Deterministic Facts And An Independent Verifier\n \n ## Status\n \ndiff --git a/docs/adr/0012-autonomous-loop-closeout-to-main.md b/docs/adr/SADR-0012-autonomous-loop-closeout-to-main.md\nindex 637fad6dde4989c9c2bf0f81f676d8d29991123c..ec7d85f81030a676bcbba28b43eb365422a36eac\n--- a/docs/adr/0012-autonomous-loop-closeout-to-main.md\n+++ b/docs/adr/SADR-0012-autonomous-loop-closeout-to-main.md\n@@ -1,4 +1,4 @@\n-# 0012 \u2014 Autonomous loop pushes goal completion to main via a closeout PR\n+# SADR-0012 \u2014 Autonomous loop pushes goal completion to main via a closeout PR\n \n - Status: accepted\n - Date: 2026-06-18\ndiff --git a/docs/adr/0013-bootstrap-grants-actions-approve-pull-requests.md b/docs/adr/SADR-0013-bootstrap-grants-actions-approve-pull-requests.md\nindex b6c69d64f1c5ced346b88d6859586715b3b5bd0e..a793e7277d67ab8ea56286fec289ca689825a17d\n--- a/docs/adr/0013-bootstrap-grants-actions-approve-pull-requests.md\n+++ b/docs/adr/SADR-0013-bootstrap-grants-actions-approve-pull-requests.md\n@@ -1,4 +1,4 @@\n-# ADR 0013: Bootstrap Grants GitHub Actions Permission To Create And Approve Pull Requests\n+# SADR-0013: Bootstrap Grants GitHub Actions Permission To Create And Approve Pull Requests\n \n ## Status\n \ndiff --git a/docs/adr/0014-codex-app-external-ai-guardian-ui-adapter.md b/docs/adr/SADR-0014-codex-app-external-ai-guardian-ui-adapter.md\nindex 238db06279334caa341c117362df9271382a7fcd..65f12e9939321e1b3e389a34c822fc1549e5155d\n--- a/docs/adr/0014-codex-app-external-ai-guardian-ui-adapter.md\n+++ b/docs/adr/SADR-0014-codex-app-external-ai-guardian-ui-adapter.md\n@@ -1,4 +1,4 @@\n-# ADR 0014: Use Codex App As The External AI Guardian UI Adapter\n+# SADR-0014: Use Codex App As The External AI Guardian UI Adapter\n \n ## Status\n \ndiff --git a/docs/adr/0015-contract-approval-before-dispatch.md b/docs/adr/SADR-0015-contract-approval-before-dispatch.md\nindex fad6c656741a20a7684ba16080a7db159f63a52f..6061271a46d3f1d6ae7174477de3fb75673123a6\n--- a/docs/adr/0015-contract-approval-before-dispatch.md\n+++ b/docs/adr/SADR-0015-contract-approval-before-dispatch.md\n@@ -1,4 +1,4 @@\n-# ADR 0015: Approve The Task Contract Before Dispatch, Not The Diff After It\n+# SADR-0015: Approve The Task Contract Before Dispatch, Not The Diff After It\n \n ## Status\n \n@@ -9,7 +9,7 @@\n Guardian approval for high/critical risk is expressible only on a pull request.\n Every approval source in `guardian-policy.json` \u2014 GitHub review, the\n `guardian:approved` label, the `Guardian approval granted` comment, and the\n-`external_ai_guardian_review` artifact (ADR 0010/0014) \u2014 is bound to a PR, and\n+`external_ai_guardian_review` artifact (SADR-0010/0014) \u2014 is bound to a PR, and\n three of the four require the current head SHA. Approval therefore cannot exist\n before an implementation PR exists.\n \n@@ -81,7 +81,7 @@\n An envelope breach must surface only on the non-repairable policy gate, never on\n the repairable `MergeGate metadata check`. Otherwise the Goal Loop routes it to\n `dispatch_repair` \u2014 instructing a runner to make the breach go away \u2014 which is\n-exactly what this ADR forbids.\n+exactly what this SADR forbids.\n \n **Contract immutability.** After registration, an implementation PR must not\n change the governance fields of its own task contract (`scope`, `non_goals`,\n@@ -116,7 +116,7 @@\n fails any verdict that is not `complete`, and the signal the CCA prompt reads is\n computed from `evaluate_guardian_approval` alone. Without the OR-merge at the\n signal, a contract-approved PR still returns `needs_guardian`, the CCA job fails,\n-and the MergeGate policy check never runs \u2014 the gate this ADR changes would never\n+and the MergeGate policy check never runs \u2014 the gate this SADR changes would never\n be reached. The CCA *prompt* needs no change: a `contract_approval` entry in the\n signal's existing `sources` array satisfies its current wording.\n \n@@ -130,7 +130,7 @@\n written, and an approved contract lets its implementation PRs merge through the\n same automatic path as low/medium risk. The wasted implement-then-block cycle\n disappears. Guardian review shifts from reading diffs to reading contracts, which\n-is both earlier and smaller; the external AI Guardian path (ADR 0010/0014) works\n+is both earlier and smaller; the external AI Guardian path (SADR-0010/0014) works\n on contracts with no change.\n \n We accept that the Guardian authorizes a class of change without seeing its\n@@ -146,7 +146,7 @@\n adversarial tests for the new approval path equivalent in rigor to\n `test_shiki_governance_evidence.sh`.\n \n-Bootstrap costs, which apply only while this ADR is being implemented. The tasks\n+Bootstrap costs, which apply only while this SADR is being implemented. The tasks\n that build Contract Approval merge under the existing rules, so the ones\n dispatched after the pre-dispatch gate lands but before the approval path lands\n need a Guardian-approved Contract PR *and* a live Guardian approval on their\n@@ -210,7 +210,7 @@\n risk; they do not bound authorized work.\n \n **Auto-escalate risk on envelope breach instead of stopping.** Returns the\n-workflow to a diff-time Guardian decision \u2014 the exact behavior this ADR removes.\n+workflow to a diff-time Guardian decision \u2014 the exact behavior this SADR removes.\n \n **Resolve governance fields from the base snapshot only.** Rejected against the\n registration flow: implementation PRs add their own contract, so the base has no\ndiff --git a/docs/adr/0016-locks-bound-product-paths.md b/docs/adr/SADR-0016-locks-bound-product-paths.md\nindex b5e850f3f1f7f4ef6e7f9d1763a29a51bd300c45..46b40a9b99a7aae644c02bb458f5473ff0a90ce2\n--- a/docs/adr/0016-locks-bound-product-paths.md\n+++ b/docs/adr/SADR-0016-locks-bound-product-paths.md\n@@ -1,4 +1,4 @@\n-# ADR 0016: Locks bound product paths; the Shiki mirror is bound by record identity\n+# SADR-0016: Locks bound product paths; the Shiki mirror is bound by record identity\n \n ## Status\n \n@@ -6,8 +6,8 @@\n \n - Date: 2026-07-28\n - Deciders: operator (mizutani-140), Claude (planner)\n-- Related: ADR 0004 (GitHub-first with a `.shiki` mirror), ADR 0009 (Spec Freeze),\n-  ADR 0012 (autonomous loop closeout to main), ADR 0015 (Contract Approval before\n+- Related: SADR-0004 (GitHub-first with a `.shiki` mirror), SADR-0009 (Spec Freeze),\n+  SADR-0012 (autonomous loop closeout to main), SADR-0015 (Contract Approval before\n   dispatch), remediation goal task R-02, the live PR #179 deadlock.\n \n ## Context\n@@ -90,11 +90,11 @@\n files, documentation files and named source modules are nameable; runtime-\n allocated ledger, runner and report ids are not \u2014 and those live in `.shiki`,\n which locks no longer govern. Existing frozen contracts are brought into line by\n-a one-time Spec Amendment per plan, which must land before ADR 0015's contract\n+a one-time Spec Amendment per plan, which must land before SADR-0015's contract\n immutability makes `locks` unwritable from the implementation PR.\n \n **Lock state is resolved from the durable side.** MergeGate resolves the lock\n-registry, and ADR 0015's governance fields, from the base `.shiki` snapshot when\n+registry, and SADR-0015's governance fields, from the base `.shiki` snapshot when\n present, falling back to head only when absent. The `MergeGate metadata check`\n job is given `--base-shiki`, which it does not receive today. Locks are not\n derived from the live open-pull-request set: a pull request body is untrusted\n@@ -108,7 +108,7 @@\n Explicitly out of scope: moving MergeGate's contract resolution off the PR\n checkout wholesale; making the mirror a projection of GitHub state; introducing\n new lock kinds; moving ledger evidence out of the repository; and unwinding\n-ADR 0012's closeout pull request. Registration of multi-task goals is ADR 0015's\n+SADR-0012's closeout pull request. Registration of multi-task goals is SADR-0015's\n Contract PR and is not re-decided here.\n \n ## Consequences\n@@ -127,7 +127,7 @@\n \n Risks accepted. First, goal-scoped identity is only as strong as `goal_id`,\n which is resolved from the PR body and the head task file; the mitigation is to\n-add `goal_id` to ADR 0015's frozen governance field set, and until that lands a\n+add `goal_id` to SADR-0015's frozen governance field set, and until that lands a\n task file can name a foreign goal. Second, base-when-present resolution is\n unavailable on fork pull requests, where the head is contributor-controlled;\n fork pull requests already cannot complete a Shiki merge, and this decision does\n@@ -139,7 +139,7 @@\n path, not by implementation pull requests.\n \n New obligations: one Spec Amendment per frozen plan to rewrite `locks`, ordered\n-before ADR 0015 T1; extension of the adversarial suites in\n+before SADR-0015 T1; extension of the adversarial suites in\n `scripts/test_shiki_adversarial_state.sh` and\n `scripts/test_shiki_governance_evidence.sh` to cover the eleven newly-scoped\n directories; and updating `scripts/test_shiki_loop_lock_guard.sh`,\n@@ -151,32 +151,32 @@\n whether the loop's transitive sync of ledger `evidence` references can be\n deleted once the Contract PR carries its own source plan.\n \n-## Relationship to ADR 0004\n+## Relationship to SADR-0004\n \n-This ADR **clarifies something ADR 0004 left unspecified**. ADR 0004 decides that\n+This SADR **clarifies something SADR-0004 left unspecified**. SADR-0004 decides that\n GitHub is the operational source of truth and that each target repository keeps a\n `.shiki/` mirror. It says nothing about pull requests writing the mirror, does\n not name MergeGate, and does not make the mirror an input to a gate. Nothing in\n-ADR 0004's Decision or Consequences is amended, superseded or weakened: the\n+SADR-0004's Decision or Consequences is amended, superseded or weakened: the\n mirror still exists, still records the same artifacts, and pull request\n-boundaries remain the coordination surface. What this ADR settles is the question\n+boundaries remain the coordination surface. What this SADR settles is the question\n 0004 never reached \u2014 by what authority a pull request may write a mirror record \u2014\n and it answers it the way `.shiki/manifest.json` already declares\n (`pr_mutation: current-task-or-current-goal-only`).\n \n-It **amends ADR 0012**, whose Decision item 5 states that \"Loop-executed tasks\n+It **amends SADR-0012**, whose Decision item 5 states that \"Loop-executed tasks\n declare the `path:.shiki/**` lock, which covers every `.shiki` file the closeout\n stages ... so `files_outside_locks` is satisfied without per-file lock\n bookkeeping.\" That clause is replaced: the closeout's mirror writes are\n-authorized by record identity, not by a declared lock. ADR 0012's substantive\n+authorized by record identity, not by a declared lock. SADR-0012's substantive\n decision \u2014 that completion reaches `main` through a closeout pull request rather\n than local attestation \u2014 is preserved unchanged.\n \n-It is **complementary to ADR 0015**, not a substitute. ADR 0015 owns contract\n+It is **complementary to SADR-0015**, not a substitute. SADR-0015 owns contract\n immutability, the pre-dispatch gate, the Contract PR and the Scope Envelope. This\n-ADR owns what a lock means and who may write the mirror. The one ordering\n+SADR owns what a lock means and who may write the mirror. The one ordering\n constraint between them is that the lock-narrowing Spec Amendment must precede\n-ADR 0015 T1's freezing of the `locks` field.\n+SADR-0015 T1's freezing of the `locks` field.\n \n ## Alternatives Considered\n \n@@ -186,7 +186,7 @@\n the only writer that can register them may run. Migrating the Guardian signal to\n that store makes every registration PR fail closed to `needs_guardian` at every\n risk level, because the task file is by definition absent from the store, which\n-removes the low/medium autonomous path ADR 0008/0009 exists to provide. And it\n+removes the low/medium autonomous path SADR-0008/0009 exists to provide. And it\n leaves schemas, manifest, config, guardian policy, templates, migrations, plans,\n worktrees, runner records, runs and handoffs with no writer at all in a\n repository whose product is `.shiki`. Measured benefit on the frozen backlog:\n@@ -199,14 +199,14 @@\n namespace is not in fact ownership-partitioned \u2014 56 ledger entries carry no\n `task_id`, and one goal DAG is writable by 35 registered tasks. The correct\n expression of its insight is that locks do not govern the mirror at all, which is\n-what this ADR decides.\n+what this SADR decides.\n \n **Make the mirror a projection written only by a registration writer and a\n post-merge projection job.** Rejected. It requires a bypass actor with write\n access to the protected default branch; it makes its own master invariant\n readable from a manifest that pull requests may edit; it deletes both reconcile\n modes and leaves a corrupted mirror with no in-band repair path, contradicting\n-ADR 0004's consequence that mirror conflicts are repaired against GitHub state;\n+SADR-0004's consequence that mirror conflicts are repaired against GitHub state;\n and its content-derived ids are rejected by `validate_shiki`'s currently-green id\n pattern.\n \ndiff --git a/docs/adr/0017-bookkeeping-closeouts-do-not-inherit-risk.md b/docs/adr/SADR-0017-bookkeeping-closeouts-do-not-inherit-risk.md\nindex 554e3a0d526a72ca067108c5464cc68c3edcf174..b9676857a4713850e063af7c39c8e0b01ecb4c66\n--- a/docs/adr/0017-bookkeeping-closeouts-do-not-inherit-risk.md\n+++ b/docs/adr/SADR-0017-bookkeeping-closeouts-do-not-inherit-risk.md\n@@ -1,4 +1,4 @@\n-# ADR 0017: A Bookkeeping Closeout Does Not Inherit Its Task's Risk\n+# SADR-0017: A Bookkeeping Closeout Does Not Inherit Its Task's Risk\n \n ## Status\n \n@@ -6,7 +6,7 @@\n \n ## Context\n \n-ADR 0012 made the goal loop push a task's completion to the default branch through a\n+SADR-0012 made the goal loop push a task's completion to the default branch through a\n closeout pull request: after the implementation PR merges, the loop opens a second PR\n carrying `task.status = done`, `lock.state = released`, and \u2014 when the task completes\n its goal \u2014 `goal.status = complete` plus one scorecard report. Nothing else. The\n@@ -49,14 +49,14 @@\n 4. The task file's governance fields (`scope`, `non_goals`, `required_skills`,\n    `risk_level`, `locks`, `acceptance_checks`, `test_command`) are byte-identical to\n    the base snapshot. Only `status`, `expected_pr`, `closeout_pr`, `ledger_evidence`\n-   and `expected_branch` may differ \u2014 the ADR 0015 contract-immutability rule,\n+   and `expected_branch` may differ \u2014 the SADR-0015 contract-immutability rule,\n    unchanged.\n \n    `expected_branch` is in that set because a closeout is *by construction* on a\n    different branch from the implementation it closes, and the loop rewrites the\n    field accordingly. It is safe to let it differ because MergeGate independently\n    requires `expected_branch` to equal the PR's actual head ref, so a PR cannot set\n-   it to anything other than the branch it is really on. The first draft of this ADR\n+   it to anything other than the branch it is really on. The first draft of this SADR\n    omitted it, which made the exemption unreachable: every real closeout failed\n    condition 4 on a field the loop always changes. See PR #209.\n 5. The state transitions are exactly the terminal set: task `review` \u2192 `done`, lock\n@@ -113,12 +113,12 @@\n classification must be derived, not asserted.\n \n **Fold the closeout into the implementation PR** so no second PR exists. Rejected under\n-ADR 0012, which recorded why: a task file that is already `done` on the implementation\n+SADR-0012, which recorded why: a task file that is already `done` on the implementation\n branch short-circuits the loop's review \u2192 CCA \u2192 merge \u2192 done ordering into a local\n goal-completion before any merge happens.\n \n **Have the loop auto-merge closeouts with elevated credentials.** Rejected outright: that\n-is a runtime authoring its own approval, which ADR 0010 and the constitution's\n+is a runtime authoring its own approval, which SADR-0010 and the constitution's\n approval-record prohibition forbid. It would also be indistinguishable, in the evidence,\n from the forgery those rules exist to prevent.\n \ndiff --git a/docs/adr/0018-carried-guardian-approval-across-a-proven-base-sync.md b/docs/adr/SADR-0018-carried-guardian-approval-across-a-proven-base-sync.md\nindex 22358562da390ed8b45cd600da08a43d378b4680..e6e2907cefa776460af73ddf7d39db605f3c39b0\n--- a/docs/adr/0018-carried-guardian-approval-across-a-proven-base-sync.md\n+++ b/docs/adr/SADR-0018-carried-guardian-approval-across-a-proven-base-sync.md\n@@ -1,4 +1,4 @@\n-# ADR 0018: Carried Guardian Approval Across A Proven Base Sync\n+# SADR-0018: Carried Guardian Approval Across A Proven Base Sync\n \n ## Status\n \n@@ -34,7 +34,7 @@\n Guardian evaluator that consumes it (`shiki_guardian.evaluate_guardian_approval`,\n the `guardian_comment_carried` source) were built and tested in isolation. Neither\n does anything until a caller passes the flags that turn the carry on, so the\n-feature was invisible in CI. This ADR records the decision the two of them\n+feature was invisible in CI. This SADR records the decision the two of them\n implement, and this task wires it into the four CI invocations that judge Guardian\n approval.\n \ndiff --git a/docs/adr/README.md b/docs/adr/README.md\nindex c70e057a4520c36e042ab68c53cd4a21ef709b71..5da43856836e2ce6a150f8127766e2f88355ab4b\n--- a/docs/adr/README.md\n+++ b/docs/adr/README.md\n@@ -1,40 +1,41 @@\n-# Architecture Decision Records\n+# Decision Record Namespaces\n \n-This directory holds Shiki's Architecture Decision Records (ADRs): durable\n-records of hard-to-reverse platform decisions. ADRs are part of the Shiki source\n-of truth alongside GitHub state, the `.shiki/` mirror, and `CONTEXT.md`.\n+This directory holds two independent decision namespaces:\n \n-## How ADRs Work\n+- Shiki Architecture Decision Records (SADRs), named\n+  `SADR-NNNN-<short-slug>.md`, record hard-to-reverse Shiki platform decisions.\n+- Target Architecture Decision Records (ADRs), named\n+  `NNNN-<short-slug>.md`, belong to the target product.\n \n-- New ADRs are added as `docs/adr/NNNN-<short-slug>.md`, where `NNNN` is the next\n-  zero-padded sequence number.\n-- The **canonical list of ADRs is the set of files matching `docs/adr/*.md`** in\n-  this directory. Browse `docs/adr/*.md` for the authoritative, current set.\n-- Use [`template.md`](template.md) as the starting point for a new ADR.\n-- Record an ADR when a decision is hard to reverse, surprising without context,\n-  and a real tradeoff. Do not write an ADR for routine or easily reversible\n-  choices.\n+The same four-digit number may appear once in each namespace. A platform\n+`SADR-NNNN` citation resolves only to `SADR-NNNN-*.md`; a target `ADR NNNN`\n+citation resolves only to `NNNN-*.md`.\n \n-Using the `docs/adr/*.md` glob as the source of truth means other PRs can add an\n-ADR without editing this index file, avoiding merge contention on a\n-hand-maintained enumeration.\n+## How Decision Records Work\n \n-## Current ADRs (snapshot)\n-\n-The list below is a convenience snapshot and may lag behind the directory. When\n-in doubt, list `docs/adr/*.md`.\n+- New Shiki platform decisions use the next zero-padded SADR number.\n+- New target product decisions use the target's own ADR sequence.\n+- The canonical list is the files in `docs/adr/`; no hand-maintained index is\n+  authoritative.\n+- Use [`template.md`](template.md) as the starting point and choose the filename\n+  namespace that owns the decision.\n+- Record a decision when it is hard to reverse, surprising without context, and\n+  a real tradeoff. Do not write a decision record for routine or easily\n+  reversible choices.\n \n-- [0001 \u2014 Keep Shiki As The Platform Name](0001-keep-shiki-as-platform-name.md)\n-- [0002 \u2014 Use AGENTS As The Shared Agent Constitution](0002-use-agents-as-shared-constitution.md)\n-- [0003 \u2014 Require Skill Gate For MergeGate Readiness](0003-require-skill-gate-for-mergegate-readiness.md)\n-- [0004 \u2014 Use GitHub First With A Repository-Local Shiki Mirror](0004-use-github-first-with-shiki-mirror.md)\n-- [0005 \u2014 Use Codex As Front And Claude Code Action As The GitHub Runtime](0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md)\n-- [0006 \u2014 Separate Codex Implementation, CCA Completion, And MergeGate Authorization](0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md)\n+Using the directory as the source of truth lets independent changes add records\n+without contending on an enumerated index.\n \n-## Adding A New ADR\n+## Adding A Shiki SADR\n \n-1. Copy `template.md` to `docs/adr/NNNN-<short-slug>.md`.\n+1. Copy `template.md` to `docs/adr/SADR-NNNN-<short-slug>.md`.\n 2. Fill in the title, status, context, decision, and consequences.\n-3. Set the status to `Proposed`, then `Accepted` once the decision is approved.\n-4. Open a PR following `CONTRIBUTING.md`. You do not need to edit this index for\n-   the ADR to be discoverable via `docs/adr/*.md`.\n+3. Set the status to `Proposed`, then `Accepted` once the proper authority has\n+   approved the decision.\n+4. Open a PR following `CONTRIBUTING.md`.\n+\n+## Adding A Target ADR\n+\n+1. Copy `template.md` to `docs/adr/NNNN-<short-slug>.md` using the target's own\n+   sequence.\n+2. Cite it as `ADR NNNN`; do not prefix its filename with `SADR-`.\ndiff --git a/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md b/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\nnew file mode 100644\nindex 0000000000000000000000000000000000000000..7455ce1f3bed02ddddb851ac21a1d1a648c9aeba\n--- /dev/null\n+++ b/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\n@@ -0,0 +1,55 @@\n+# SADR-0019: Separate Shiki Platform SADRs From Target ADRs\n+\n+## Status\n+\n+Accepted\n+\n+## Context\n+\n+Shiki installs its platform decision records into `docs/adr/` in every target\n+repository. Shiki and its targets previously used the same numeric\n+`NNNN-<slug>.md` namespace. A target that already owned ADRs 0013 through 0018\n+therefore acquired two records for each number during installation, making an\n+`ADR NNNN` citation ambiguous. Renumbering target decisions would rewrite target\n+history, while renumbering Shiki decisions per target would destroy their stable\n+platform identity.\n+\n+Upgrades also need to remove the former Shiki-owned numeric paths without ever\n+mistaking a target-owned ADR for platform content. The install stamp already\n+records exact shipped paths and content digests, so it can prove ownership only\n+when both still match.\n+\n+## Decision\n+\n+Shiki platform decisions use the `SADR` namespace:\n+\n+- records are named `docs/adr/SADR-NNNN-<slug>.md`;\n+- live platform citations use `SADR-NNNN`;\n+- target product decisions retain `docs/adr/NNNN-<slug>.md` and `ADR NNNN`;\n+- the reference validator resolves the two namespaces independently, so the same\n+  four-digit number may exist once in each namespace.\n+\n+Before a forced upgrade writes anything, the installer inventories the exact\n+former numeric Shiki ADR paths. It may delete a legacy path only when the\n+existing install stamp contains that exact path and its recorded SHA-256 digest\n+matches the file currently on disk. All ownership blockers are reported\n+together and abort the upgrade before its first write. A non-forced install\n+never performs this cleanup.\n+\n+Historical `.shiki/` evidence remains unchanged because it records the namespace\n+that existed when that evidence was produced.\n+\n+## Consequences\n+\n+- Targets may use any ADR number without colliding with Shiki's platform SADRs.\n+- Fresh installs preserve target ADR bytes and install a stable SADR set.\n+- Forced upgrades fail closed when legacy ownership cannot be proven, and a\n+  successful upgrade refreshes the stamp without removed legacy paths.\n+- Current governance, packaging, workflow, prompt, script, and skill references\n+  must move with the platform records.\n+\n+## Alternatives Considered\n+\n+- Collision-only refusal leaves affected targets permanently unupgradable.\n+- Renumbering target ADRs mutates target-owned history.\n+- Per-target Shiki renumbering makes platform citations non-portable.\ndiff --git a/docs/adr/template.md b/docs/adr/template.md\nindex a92a97daddd0b00ab9dd626cc8d86942da2eafed..17dd79175b5fee2105935557f691b40cf9492942\n--- a/docs/adr/template.md\n+++ b/docs/adr/template.md\n@@ -1,14 +1,14 @@\n-# NNNN: <Short Decision Title>\n+# <SADR-NNNN or NNNN>: <Short Decision Title>\n \n ## Status\n \n-Proposed | Accepted | Superseded by ADR-NNNN\n+Proposed | Accepted | Superseded by SADR-NNNN or target ADR NNNN\n \n ## Context\n \n What is the situation, constraint, or force that requires a decision? Reference\n-relevant domain terms (`CONTEXT.md`), prior ADRs, and code reality. State the\n-problem, not the solution.\n+relevant domain terms (`CONTEXT.md`), prior Shiki SADRs or target ADRs, and code\n+reality. State the problem, not the solution.\n \n ## Decision\n \ndiff --git a/docs/agents/autonomous-loop-e2e-acceptance.md b/docs/agents/autonomous-loop-e2e-acceptance.md\nindex bbf84c6081987f159ac6ad9ecf3c82b7aa72e7e8..5c13c1d02a05a90b0dc46cbef7c95a9846d0c237\n--- a/docs/agents/autonomous-loop-e2e-acceptance.md\n+++ b/docs/agents/autonomous-loop-e2e-acceptance.md\n@@ -40,8 +40,8 @@\n 3. `gh auth status` is logged in with merge permission on the repo.\n 4. Branch protection + required checks are configured exactly as the goal-loop\n    required-check set expects (see `docs/agents/branch-protection-smoke.md`).\n-5. The repo `.shiki/policy` permits autonomous low/medium-risk merges (ADR\n-   0008/0009); the MergeGate policy check is the recorded-authority gate.\n+5. The repo `.shiki/policy` permits autonomous low/medium-risk merges\n+   (SADR-0008/0009); the MergeGate policy check is the recorded-authority gate.\n \n The operator's only actions are: register the task and start the loop. **No\n manual pre-clearing** means after `shiki loop run` starts, the operator does not\ndiff --git a/docs/agents/domain.md b/docs/agents/domain.md\nindex 258da8ea81e87ac0a6b0b5b5e5c2fde980eaabb7..67aaa6f4b7d0a5aa4d495ce6279073333f7798cb\n--- a/docs/agents/domain.md\n+++ b/docs/agents/domain.md\n@@ -5,7 +5,9 @@\n ## Layout\n \n - `CONTEXT.md` defines Shiki language. Keep it implementation-free.\n-- `docs/adr/` records hard-to-reverse decisions.\n+- `docs/adr/SADR-NNNN-*.md` records Shiki platform decisions.\n+- `docs/adr/NNNN-*.md` records target product decisions without sharing the\n+  SADR namespace.\n - `AGENTS.md` is the runtime-neutral agent constitution.\n - `CLAUDE.md` is the Claude Code wrapper around `AGENTS.md`.\n - `.shiki/` is the Target Repository mirror for Goals, PRDs, plans, Task DAGs, contracts, locks, worktree records, ledger entries, CCA verdicts, repair packets, reports, and handoffs.\ndiff --git a/docs/agents/guardian-policy.md b/docs/agents/guardian-policy.md\nindex 4147c7adf05a47adb4fd24ada1a1c2e1f2ba6d2d..a20d9cc6c00a569bb057b9ca8d6b51c7fc4a9847\n--- a/docs/agents/guardian-policy.md\n+++ b/docs/agents/guardian-policy.md\n@@ -98,8 +98,8 @@\n `origin/<default_branch>` and reuses each job's existing base fetch; a shallow\n (`--depth`) or second base fetch would make the proof unresolvable and silently\n disable the carry, so neither is permitted. CCA reads `guardian_comment_carried` as\n-a recorded authority in the deterministic signal and does not re-derive it. See ADR\n-0018 for the full decision, the rejected diff/patch-equality and path-disjointness\n+a recorded authority in the deterministic signal and does not re-derive it. See\n+SADR-0018 for the full decision, the rejected diff/patch-equality and path-disjointness\n alternatives, and the accepted residual.\n \n ## External AI Guardian Review (SADR-0010)\ndiff --git a/docs/agents/implementation-policy.md b/docs/agents/implementation-policy.md\nindex c754144a9fb5bf4a38aa82a60e1670c59e626b44..e90d40fe21880b9a09719c5ecfe6bde7284eb3ce\n--- a/docs/agents/implementation-policy.md\n+++ b/docs/agents/implementation-policy.md\n@@ -31,6 +31,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n ## Non-Negotiables\n \n 1. **Planning precedes implementation.** Non-trivial Goals start with `grill-with-docs` and Context & Impact.\ndiff --git a/scripts/guardian_approval_signal.py b/scripts/guardian_approval_signal.py\nindex d059f92b2c82aa2b5d6ca421365537a87886c1cb..16924892714f4f5b9303c0ef0e325a7e70075630\n--- a/scripts/guardian_approval_signal.py\n+++ b/scripts/guardian_approval_signal.py\n@@ -199,8 +199,8 @@\n ) -> Any:\n     \"\"\"Evaluate SADR-0015 Contract Approval for the CCA Guardian signal.\n \n-    The SAME OR that MergeGate applies must reach this deterministic signal: ADR\n-    0015 is explicit that without it, a contract-approved PR still returns\n+    The SAME OR that MergeGate applies must reach this deterministic signal:\n+    SADR-0015 is explicit that without it, a contract-approved PR still returns\n     ``needs_guardian``, the CCA job fails, and the MergeGate policy check never\n     runs \u2014 the gate this change moves would never be reached. This resolves the\n     task the SAME way MergeGate does (the first Shiki task id in the PR body),\ndiff --git a/scripts/shiki_contract_approval.py b/scripts/shiki_contract_approval.py\nindex 941bcb96fdd8b9a85bc0c4f0f04d5dad9d04839c..4a4b418f6527413161db82670bec857a043360e2\n--- a/scripts/shiki_contract_approval.py\n+++ b/scripts/shiki_contract_approval.py\n@@ -142,7 +142,7 @@\n     changed_files_status: Any,\n     registration: Any,\n ) -> ContractApprovalResult:\n-    \"\"\"Whether this PR carries an SADR-0015 Contract Approval.\n+    \"\"\"Whether this PR carries a SADR-0015 Contract Approval.\n \n     The four conditions are evaluated IN ORDER, each failure recorded as a\n     distinct reason string:\ndiff --git a/scripts/shiki_installer.py b/scripts/shiki_installer.py\nindex be064ab7a29ff6836f7f7a1d70500a43e0d11d7b..d3e3247edbf3e4be287a4667176d86f6500f8dc5\n--- a/scripts/shiki_installer.py\n+++ b/scripts/shiki_installer.py\n@@ -118,6 +118,7 @@\n # what lets a target carry it without a governance-schema change.\n INSTALL_STAMP_PATH = \".shiki/install-stamp.json\"\n INSTALL_STAMP_VERSION = 1\n+LEGACY_SHIKI_SADR_MAX = 18\n \n # Under --force the shipped surface splits three ways, so nothing is lost and\n # nothing is silently stale.\n@@ -237,9 +238,98 @@\n         )\n \n \n+def legacy_shiki_adr_paths() -> tuple[str, ...]:\n+    \"\"\"Former numeric paths for Shiki SADR-0001 through SADR-0018.\n+\n+    The authoritative slugs come from the current SADR filenames. SADR-0019 is\n+    the namespace decision itself and never shipped under a numeric legacy path.\n+    \"\"\"\n+    paths: list[str] = []\n+    for sadr in sorted((ROOT / \"docs\" / \"adr\").glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\")):\n+        number = sadr.name.split(\"-\", 2)[1]\n+        if int(number) <= LEGACY_SHIKI_SADR_MAX:\n+            paths.append(f\"docs/adr/{sadr.name.removeprefix('SADR-')}\")\n+    return tuple(paths)\n+\n+\n+def preflight_legacy_sadr_cleanup(target: Path, *, force: bool) -> tuple[str, ...]:\n+    \"\"\"Authorize exact legacy Shiki ADR removals before the first write.\n+\n+    A non-force install never removes existing files. A forced install needs no\n+    stamp when no legacy Shiki path is present. When legacy paths do exist, each\n+    must be named in a readable install stamp and its current bytes must match\n+    the recorded digest.\n+    \"\"\"\n+    if not force:\n+        return ()\n+    present = tuple(\n+        relative\n+        for relative in legacy_shiki_adr_paths()\n+        if (target / relative).exists() or (target / relative).is_symlink()\n+    )\n+    if not present:\n+        return ()\n+\n+    stamp_path = target / INSTALL_STAMP_PATH\n+    try:\n+        raw = stamp_path.read_text(encoding=\"utf-8\")\n+        stamp = json.loads(raw)\n+    except FileNotFoundError:\n+        detail = \"install stamp is absent\"\n+        blockers = [f\"{relative}: {detail}\" for relative in present]\n+    except (OSError, UnicodeDecodeError) as error:\n+        detail = f\"install stamp is unreadable ({error})\"\n+        blockers = [f\"{relative}: {detail}\" for relative in present]\n+    except json.JSONDecodeError as error:\n+        detail = f\"install stamp is malformed ({error})\"\n+        blockers = [f\"{relative}: {detail}\" for relative in present]\n+    else:\n+        if not isinstance(stamp, dict):\n+            blockers = [\n+                f\"{relative}: install stamp is malformed (expected an object)\"\n+                for relative in present\n+            ]\n+        else:\n+            digests = stamp.get(\"digests\")\n+            blockers = []\n+            if not isinstance(digests, dict):\n+                blockers.extend(\n+                    f\"{relative}: install stamp has no digest for this path\"\n+                    for relative in present\n+                )\n+            else:\n+                for relative in present:\n+                    expected = digests.get(relative)\n+                    if not isinstance(expected, str) or not expected:\n+                        blockers.append(\n+                            f\"{relative}: install stamp has no digest for this path\"\n+                        )\n+                        continue\n+                    try:\n+                        actual = _sha256_file(target / relative)\n+                    except OSError as error:\n+                        blockers.append(\n+                            f\"{relative}: legacy path is unreadable ({error})\"\n+                        )\n+                        continue\n+                    if actual != expected:\n+                        blockers.append(\n+                            f\"{relative}: digest mismatch (stamp {expected}, current {actual})\"\n+                        )\n+\n+    if blockers:\n+        joined = \"\\n\".join(f\"- {blocker}\" for blocker in blockers)\n+        raise ShikiError(\n+            \"legacy Shiki ADR cleanup is not authorized:\\n\"\n+            f\"{joined}\\nNo files were written.\"\n+        )\n+    return present\n+\n+\n def install_template(target: Path, *, force: bool, validate: bool) -> list[NewFileNote]:\n     # Preconditions first: never rewrite the tree of a target that cannot be\n     # upgraded cleanly (see refuse_pending_migrations).\n+    legacy_cleanup = preflight_legacy_sadr_cleanup(target, force=force)\n     refuse_pending_migrations(target)\n \n     # Resolve the target's required CODEOWNERS owner once from its\n@@ -250,6 +340,10 @@\n     # verbatim. See ``codeowners_required_owner``.\n     codeowners_owner = codeowners_required_owner(target)\n \n+    for relative in legacy_cleanup:\n+        (target / relative).unlink()\n+        info(f\"removed stamped legacy Shiki ADR: {target / relative}\")\n+\n     notes: list[NewFileNote] = []\n     for relative in TEMPLATE_PATHS:\n         source = ROOT / relative\ndiff --git a/scripts/shiki_loop.py b/scripts/shiki_loop.py\nindex 3611641da8aeb5038b1da90e3114b09a473ec6d8..84caa491b9cad0edc4176f209c13e9221e975681\n--- a/scripts/shiki_loop.py\n+++ b/scripts/shiki_loop.py\n@@ -424,7 +424,7 @@\n         # unresolvable CCA verdict has already produced its terminal stop and is never\n         # laundered into a head-moving sync (SADR-0010). It also never fires while the\n         # policy (Guardian) gate is red \u2014 for ANY reason, not only when it is the sole\n-        # red check \u2014 so a head-bound Guardian approval, or an SADR-0015\n+        # red check \u2014 so a head-bound Guardian approval, or a SADR-0015\n         # contract-approval carry that base movement defeated (the policy gate compares\n         # base and head task governance directly), is never silently restored by\n         # adopting base wholesale. It is bounded by a durable per-task counter so the\ndiff --git a/scripts/test_shiki_goal_loop.sh b/scripts/test_shiki_goal_loop.sh\nindex 753c7b516e10f0be3bc92a4bd0eb1fa699776ae6..8f3820a0a079b3044424cfe1a6c6b7fe68896e67\n--- a/scripts/test_shiki_goal_loop.sh\n+++ b/scripts/test_shiki_goal_loop.sh\n@@ -231,8 +231,8 @@\n \n # High/critical risk merges autonomously when all required checks are green:\n # the \"MergeGate policy check\" required check IS the Guardian gate (it enforces\n-# guardian-policy.json \u2014 human approval OR external AI guardian review, ADR\n-# 0010), so green checks mean a recorded authority approved. The fake gh returns\n+# guardian-policy.json \u2014 human approval OR external AI guardian review,\n+# SADR-0010), so green checks mean a recorded authority approved. The fake gh returns\n # green checks, simulating that the Guardian gate passed.\n python3 \"$ROOT/scripts/shiki.py\" goal create --target \"$TARGET\" --title \"Guardian gate\" --outcome \"High risk merges via policy gate\" >/tmp/shiki-goal-loop-guardian-goal.json\n GGOAL=\"$(json_get /tmp/shiki-goal-loop-guardian-goal.json goal_id)\"\ndiff --git a/tests/test_adr_references.py b/tests/test_adr_references.py\nindex 51706329c797d5dd9b47e082a333fa83b261d482..db6e693e35f67103935be34f1d9bb4ff7ee60395\n--- a/tests/test_adr_references.py\n+++ b/tests/test_adr_references.py\n@@ -1,7 +1,7 @@\n-\"\"\"Every ``ADR ####`` citation must resolve to exactly one decision record.\n+\"\"\"Every target ADR and Shiki SADR citation resolves in its own namespace.\n \n Background. ``scripts/mergegate_check.py`` justifies ``is_bookkeeping_closeout``\n-with \"ADR 0017\" nine times and ``scripts/guardian_approval_signal.py`` cites\n+with \"SADR-0017\" nine times and ``scripts/guardian_approval_signal.py`` cites\n 0016/0017 seven times, yet for a stretch the numbering in ``docs/adr/`` stopped\n at 0015: the authoring commits lived on a coordinator branch and no task ever\n declared ``docs/adr/...`` in its locks, so nothing carried the records to the\n@@ -11,11 +11,12 @@\n and the directory drifted with no signal.\n \n This is the same unbound-pair shape behind other silent failures: a reference\n-and its target with nothing asserting they agree. This test binds them. It scans\n-every ``scripts/*.py`` (plus ``docs/**/*.md`` and the root ``*.md`` governance\n-files, which are cheap) for the ``ADR ####`` pattern and asserts each cited\n-number resolves to exactly one ``docs/adr/####-*.md``. The converse is\n-deliberately NOT required: an ADR may exist without being cited.\n+and its target with nothing asserting they agree. This test binds them. Target\n+``ADR NNNN`` citations resolve only to ``docs/adr/NNNN-*.md``; Shiki\n+``SADR-NNNN`` citations resolve only to ``docs/adr/SADR-NNNN-*.md``. The scan\n+covers current governance docs, scripts, workflows, prompts, packaging metadata,\n+and repo-local Codex guidance while deliberately excluding historical\n+``.shiki`` evidence. The converse is not required: a record may be uncited.\n \n The suite is dependency free: standard-library ``unittest`` only.\n \"\"\"\n@@ -31,17 +32,18 @@\n \n REPO_ROOT = Path(__file__).resolve().parents[1]\n \n-# \"ADR 0016\" -- a four-digit citation. The trailing \\b keeps a five-digit token\n-# from being read as a truncated four-digit number.\n-_CITATION = re.compile(r\"\\bADR (\\d{4})\\b\")\n-# \"0016-locks-bound-product-paths.md\" -- the leading four digits name the record.\n-_ADR_FILENAME = re.compile(r\"^(\\d{4})-.*\\.md$\")\n+# ``ADR 0016`` / ``SADR-0016`` -- a namespace plus four-digit citation. The\n+# trailing ``\\b`` keeps a five-digit token from being read as a truncated one.\n+_CITATION = re.compile(r\"\\b(S?ADR)[ -](\\d{4})\\b\")\n+# Target ADRs retain the numeric filename; Shiki platform decisions use SADR.\n+_DECISION_FILENAME = re.compile(r\"^(?:(SADR)-)?(\\d{4})-.*\\.md$\")\n \n \n class Violation(NamedTuple):\n     \"\"\"A citation that does not resolve to exactly one decision record.\"\"\"\n \n     citing_file: str  # repo-relative path of the file that cites the ADR\n+    namespace: str  # \"ADR\" for target decisions; \"SADR\" for Shiki decisions\n     number: str  # the cited four-digit number, e.g. \"0016\"\n     line: int  # 1-based line of the citation\n     reason: str  # \"missing\" (zero matches) or \"ambiguous\" (two or more)\n@@ -49,44 +51,59 @@\n \n     @property\n     def expected_path(self) -> str:\n-        return f\"docs/adr/{self.number}-*.md\"\n+        prefix = \"SADR-\" if self.namespace == \"SADR\" else \"\"\n+        return f\"docs/adr/{prefix}{self.number}-*.md\"\n \n     def describe(self) -> str:\n         \"\"\"Name the citing file, the cited number, and the missing path.\"\"\"\n         if self.reason == \"missing\":\n             return (\n-                f\"{self.citing_file}:{self.line} cites ADR {self.number} \"\n+                f\"{self.citing_file}:{self.line} cites {self.namespace} {self.number} \"\n                 f\"but no document matches {self.expected_path}\"\n             )\n         joined = \", \".join(self.matches)\n         return (\n-            f\"{self.citing_file}:{self.line} cites ADR {self.number} which \"\n+            f\"{self.citing_file}:{self.line} cites {self.namespace} {self.number} which \"\n             f\"resolves to {len(self.matches)} documents (expected exactly one \"\n             f\"at {self.expected_path}): {joined}\"\n         )\n \n \n-def _adr_index(root: Path) -> dict[str, list[Path]]:\n-    \"\"\"Map every four-digit ADR number to the record files that claim it.\"\"\"\n-    index: dict[str, list[Path]] = {}\n+def _adr_index(root: Path) -> dict[tuple[str, str], list[Path]]:\n+    \"\"\"Map each decision namespace and number to the files that claim it.\"\"\"\n+    index: dict[tuple[str, str], list[Path]] = {}\n     adr_dir = root / \"docs\" / \"adr\"\n     if adr_dir.is_dir():\n         for path in sorted(adr_dir.glob(\"*.md\")):\n-            match = _ADR_FILENAME.match(path.name)\n+            match = _DECISION_FILENAME.match(path.name)\n             if match:\n-                index.setdefault(match.group(1), []).append(path)\n+                namespace = \"SADR\" if match.group(1) else \"ADR\"\n+                index.setdefault((namespace, match.group(2)), []).append(path)\n     return index\n \n \n def _citing_files(root: Path, include_docs: bool = True) -> list[Path]:\n-    \"\"\"Files scanned for citations: scripts/*.py, and cheaply the markdown.\"\"\"\n+    \"\"\"Current, shipped text surfaces scanned for decision citations.\"\"\"\n     files = sorted((root / \"scripts\").glob(\"*.py\"))\n+    files += sorted((root / \"scripts\").glob(\"*.sh\"))\n     if include_docs:\n         docs = root / \"docs\"\n         if docs.is_dir():\n             files += sorted(docs.rglob(\"*.md\"))\n         files += sorted(root.glob(\"*.md\"))\n-    return files\n+        pyproject = root / \"pyproject.toml\"\n+        if pyproject.is_file():\n+            files.append(pyproject)\n+        for relative in (\".github\", \".codex\"):\n+            surface = root / relative\n+            if surface.is_dir():\n+                files += sorted(\n+                    path\n+                    for path in surface.rglob(\"*\")\n+                    if path.is_file()\n+                    and path.suffix in {\".md\", \".py\", \".sh\", \".toml\", \".yaml\", \".yml\"}\n+                )\n+    return sorted(set(files))\n \n \n def find_adr_reference_violations(\n@@ -94,10 +111,9 @@\n ) -> list[Violation]:\n     \"\"\"Return every ``ADR ####`` citation under ``root`` that fails to resolve.\n \n-    A citation resolves when exactly one ``docs/adr/####-*.md`` matches its\n-    number. Zero matches is a \"missing\" violation; two or more is \"ambiguous\".\n-    Records that are never cited produce nothing -- the converse is not\n-    required.\n+    A citation resolves when exactly one record in the same namespace matches\n+    its number. Zero matches is a \"missing\" violation; two or more is\n+    \"ambiguous\". Records that are never cited produce nothing.\n     \"\"\"\n     root = Path(root)\n     index = _adr_index(root)\n@@ -110,13 +126,15 @@\n         rel = path.relative_to(root).as_posix()\n         for lineno, line in enumerate(text.splitlines(), start=1):\n             for match in _CITATION.finditer(line):\n-                number = match.group(1)\n-                found = index.get(number, [])\n+                namespace, number = match.groups()\n+                found = index.get((namespace, number), [])\n                 if len(found) == 1:\n                     continue\n                 reason = \"missing\" if not found else \"ambiguous\"\n                 rels = [p.relative_to(root).as_posix() for p in found]\n-                violations.append(Violation(rel, number, lineno, reason, rels))\n+                violations.append(\n+                    Violation(rel, namespace, number, lineno, reason, rels)\n+                )\n     return violations\n \n \n@@ -129,8 +147,8 @@\n         self.assertEqual(\n             violations,\n             [],\n-            \"Every 'ADR ####' citation must resolve to exactly one \"\n-            f\"docs/adr/####-*.md:\\n{detail}\",\n+            \"Every ADR/SADR citation must resolve exactly once in its own \"\n+            f\"docs/adr namespace:\\n{detail}\",\n         )\n \n     def test_scan_actually_reads_scripts(self) -> None:\n@@ -146,8 +164,21 @@\n         # The two records this Goal carried onto the default branch, and which\n         # the scripts cite most, must be present.\n         index = _adr_index(REPO_ROOT)\n-        self.assertIn(\"0016\", index)\n-        self.assertIn(\"0017\", index)\n+        self.assertIn((\"SADR\", \"0016\"), index)\n+        self.assertIn((\"SADR\", \"0017\"), index)\n+\n+    def test_platform_tree_has_only_sadr_0001_through_0019(self) -> None:\n+        adr_dir = REPO_ROOT / \"docs\" / \"adr\"\n+        sadrs = sorted(adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\"))\n+        self.assertEqual(\n+            {path.name.split(\"-\", 2)[1] for path in sadrs},\n+            {f\"{number:04d}\" for number in range(1, 20)},\n+        )\n+        self.assertEqual(\n+            list(adr_dir.glob(\"[0-9][0-9][0-9][0-9]-*.md\")),\n+            [],\n+            \"the Shiki platform tree must not retain legacy numeric SADR paths\",\n+        )\n \n \n class FixtureAdrReferences(unittest.TestCase):\n@@ -180,6 +211,26 @@\n         self.assertIn(\"9991\", message)  # cited number\n         self.assertIn(\"docs/adr/9991-*.md\", message)  # missing path\n \n+    def test_target_adr_does_not_satisfy_missing_platform_sadr(self) -> None:\n+        root = self._make_repo(\n+            adr_names=[\"0042-target-decision.md\"],\n+            script_body='\"\"\"Target ADR 0042 exists, but platform SADR-0042 does not.\"\"\"\\n',\n+        )\n+        violations = find_adr_reference_violations(root, include_docs=False)\n+        self.assertEqual(len(violations), 1)\n+        self.assertEqual(violations[0].namespace, \"SADR\")\n+        self.assertEqual(violations[0].number, \"0042\")\n+        self.assertEqual(violations[0].expected_path, \"docs/adr/SADR-0042-*.md\")\n+\n+    def test_target_adr_and_platform_sadr_may_reuse_a_number(self) -> None:\n+        root = self._make_repo(\n+            adr_names=[\"0042-target-decision.md\", \"SADR-0042-platform-decision.md\"],\n+            script_body='\"\"\"Target ADR 0042 and platform SADR-0042 are distinct.\"\"\"\\n',\n+        )\n+        self.assertEqual(\n+            find_adr_reference_violations(root, include_docs=False), []\n+        )\n+\n     def test_uncited_adr_is_allowed(self) -> None:\n         # An ADR exists but nothing cites it -> not a violation. The converse\n         # (every ADR must be cited) is explicitly NOT required.\ndiff --git a/tests/test_policy_job_merged_prs.py b/tests/test_policy_job_merged_prs.py\nindex d45512e74681e5e649279236c7d4e79d5c3da471..b97989f8bd74d091de1a6b5275a20d8dc68c4512\n--- a/tests/test_policy_job_merged_prs.py\n+++ b/tests/test_policy_job_merged_prs.py\n@@ -3,8 +3,8 @@\n \n ``.github/workflows/shiki-cca-completion.yml`` has two consumers of the merged-PR\n proof: the CCA *signal* (``guardian_approval_signal.py`` in \"Prepare CCA context\")\n-and the authoritative gate (``mergegate_check.py`` in \"Enforce MergeGate\"). ADR\n-0017's bookkeeping-closeout exemption (condition 6) needs the merged implementation\n+and the authoritative gate (``mergegate_check.py`` in \"Enforce MergeGate\").\n+SADR-0017's bookkeeping-closeout exemption (condition 6) needs the merged implementation\n PR proven, and a closeout carries NO ``post_merge_reconcile`` marker. The signal\n already resolves ``MERGED_PRS`` unconditionally, but \"Enforce MergeGate\" used to\n resolve it only inside ``grep -q '<!-- shiki:post_merge_reconcile -->'``. So a\ndiff --git a/tests/test_sadr_install_migration.py b/tests/test_sadr_install_migration.py\nnew file mode 100644\nindex 0000000000000000000000000000000000000000..5a442fa5e4968ade178dcdedc3c63b54dba580a2\n--- /dev/null\n+++ b/tests/test_sadr_install_migration.py\n@@ -0,0 +1,224 @@\n+\"\"\"SADR installation and collision-safe legacy upgrade behavior.\"\"\"\n+\n+from __future__ import annotations\n+\n+import contextlib\n+import hashlib\n+import io\n+import json\n+import shutil\n+import tempfile\n+import unittest\n+from pathlib import Path\n+\n+import shiki_test_support  # noqa: F401  (path bootstrap)\n+\n+import shiki_installer\n+from tests.test_adr_references import find_adr_reference_violations\n+\n+\n+PRODUCT_NUMBERS = tuple(range(13, 19))\n+\n+\n+def _quiet_install(target: Path, *, force: bool, validate: bool) -> None:\n+    with contextlib.redirect_stdout(io.StringIO()):\n+        shiki_installer.install_template(target, force=force, validate=validate)\n+\n+\n+def _sha256(path: Path) -> str:\n+    return hashlib.sha256(path.read_bytes()).hexdigest()\n+\n+\n+def _legacy_sadr_paths() -> tuple[str, ...]:\n+    return tuple(\n+        f\"docs/adr/{path.name.removeprefix('SADR-')}\"\n+        for path in sorted((shiki_installer.ROOT / \"docs\" / \"adr\").glob(\"SADR-*.md\"))\n+        if path.name != \"SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\"\n+    )\n+\n+\n+def _create_legacy_upgrade_fixture(\n+    target: Path,\n+) -> tuple[dict[str, bytes], tuple[str, ...]]:\n+    adr_dir = target / \"docs\" / \"adr\"\n+    adr_dir.mkdir(parents=True)\n+    product_bytes = {\n+        f\"{number:04d}-cubeshare-product-decision.md\": (\n+            f\"# CubeShare ADR {number:04d}\\n\\nDo not rewrite {number}.\\n\"\n+        ).encode()\n+        for number in PRODUCT_NUMBERS\n+    }\n+    for name, content in product_bytes.items():\n+        (adr_dir / name).write_bytes(content)\n+\n+    legacy_paths = _legacy_sadr_paths()\n+    for relative in legacy_paths:\n+        (target / relative).write_bytes(f\"legacy Shiki bytes for {relative}\\n\".encode())\n+\n+    stamp_path = target / shiki_installer.INSTALL_STAMP_PATH\n+    stamp_path.parent.mkdir(parents=True, exist_ok=True)\n+    stamp = {\n+        \"version\": shiki_installer.INSTALL_STAMP_VERSION,\n+        \"platform_commit\": None,\n+        \"installed_at\": \"2000-01-01T00:00:00+00:00\",\n+        \"digests\": {\n+            relative: _sha256(target / relative) for relative in legacy_paths\n+        },\n+    }\n+    stamp_path.write_text(\n+        json.dumps(stamp, indent=2, sort_keys=True) + \"\\n\", encoding=\"utf-8\"\n+    )\n+    return product_bytes, legacy_paths\n+\n+\n+def _tree_snapshot(root: Path) -> dict[str, tuple[str, bytes | str]]:\n+    \"\"\"Recursive byte/shape snapshot used to prove preflight is write-free.\"\"\"\n+    snapshot: dict[str, tuple[str, bytes | str]] = {}\n+    for path in sorted(root.rglob(\"*\")):\n+        relative = path.relative_to(root).as_posix()\n+        if path.is_symlink():\n+            snapshot[relative] = (\"symlink\", path.readlink().as_posix())\n+        elif path.is_dir():\n+            snapshot[relative] = (\"directory\", \"\")\n+        else:\n+            snapshot[relative] = (\"file\", path.read_bytes())\n+    return snapshot\n+\n+\n+class FreshSadrInstallTests(unittest.TestCase):\n+    def setUp(self) -> None:\n+        self.tmp = Path(tempfile.mkdtemp(prefix=\"shiki-sadr-fresh-\"))\n+        self.addCleanup(lambda: shutil.rmtree(self.tmp, ignore_errors=True))\n+        self.target = self.tmp / \"target\"\n+        self.adr_dir = self.target / \"docs\" / \"adr\"\n+        self.adr_dir.mkdir(parents=True)\n+        self.product_bytes = {\n+            f\"{number:04d}-product-decision.md\": (\n+                f\"# Product ADR {number:04d}\\n\\nTarget-owned bytes {number}.\\n\"\n+            ).encode()\n+            for number in PRODUCT_NUMBERS\n+        }\n+        for name, content in self.product_bytes.items():\n+            (self.adr_dir / name).write_bytes(content)\n+\n+    def test_fresh_install_preserves_product_adrs_and_installs_all_sadrs(self) -> None:\n+        _quiet_install(self.target, force=False, validate=True)\n+\n+        for name, expected in self.product_bytes.items():\n+            self.assertEqual((self.adr_dir / name).read_bytes(), expected)\n+\n+        sadr_numbers = {\n+            path.name.split(\"-\", 2)[1]\n+            for path in self.adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\")\n+        }\n+        self.assertEqual(sadr_numbers, {f\"{number:04d}\" for number in range(1, 20)})\n+\n+        numeric_records = {\n+            path.name\n+            for path in self.adr_dir.glob(\"[0-9][0-9][0-9][0-9]-*.md\")\n+        }\n+        self.assertEqual(numeric_records, set(self.product_bytes))\n+        self.assertEqual(find_adr_reference_violations(self.target), [])\n+\n+\n+class LegacySadrUpgradeTests(unittest.TestCase):\n+    def setUp(self) -> None:\n+        self.tmp = Path(tempfile.mkdtemp(prefix=\"shiki-sadr-upgrade-\"))\n+        self.addCleanup(lambda: shutil.rmtree(self.tmp, ignore_errors=True))\n+        self.target = self.tmp / \"target\"\n+        self.adr_dir = self.target / \"docs\" / \"adr\"\n+        self.product_bytes, self.legacy_paths = _create_legacy_upgrade_fixture(\n+            self.target\n+        )\n+\n+    def test_force_upgrade_deletes_only_stamped_legacy_sadrs(self) -> None:\n+        _quiet_install(self.target, force=True, validate=True)\n+\n+        for relative in self.legacy_paths:\n+            self.assertFalse((self.target / relative).exists(), relative)\n+        for name, expected in self.product_bytes.items():\n+            self.assertEqual((self.adr_dir / name).read_bytes(), expected)\n+\n+        sadrs = list(self.adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\"))\n+        self.assertEqual(len(sadrs), 19)\n+        refreshed = json.loads(\n+            (self.target / shiki_installer.INSTALL_STAMP_PATH).read_text(encoding=\"utf-8\")\n+        )\n+        for relative in self.legacy_paths:\n+            self.assertNotIn(relative, refreshed[\"digests\"])\n+        for path in sadrs:\n+            relative = path.relative_to(self.target).as_posix()\n+            self.assertEqual(refreshed[\"digests\"][relative], _sha256(path))\n+\n+    def test_non_force_install_never_deletes_legacy_files(self) -> None:\n+        before = {\n+            relative: (self.target / relative).read_bytes()\n+            for relative in self.legacy_paths\n+        }\n+        _quiet_install(self.target, force=False, validate=False)\n+        for relative, expected in before.items():\n+            self.assertEqual((self.target / relative).read_bytes(), expected)\n+\n+\n+class LegacyCleanupPreflightTests(unittest.TestCase):\n+    def setUp(self) -> None:\n+        self.tmp = Path(tempfile.mkdtemp(prefix=\"shiki-sadr-preflight-\"))\n+        self.addCleanup(lambda: shutil.rmtree(self.tmp, ignore_errors=True))\n+\n+    def test_all_ownership_blockers_abort_before_any_write(self) -> None:\n+        cases = (\n+            (\"absent\", \"install stamp is absent\"),\n+            (\"unreadable\", \"install stamp is unreadable\"),\n+            (\"malformed\", \"install stamp is malformed\"),\n+            (\"missing-digest\", \"install stamp has no digest\"),\n+            (\"digest-mismatch\", \"digest mismatch\"),\n+        )\n+        for name, expected_reason in cases:\n+            with self.subTest(name=name):\n+                target = self.tmp / name\n+                _, legacy_paths = _create_legacy_upgrade_fixture(target)\n+                stamp_path = target / shiki_installer.INSTALL_STAMP_PATH\n+\n+                if name == \"absent\":\n+                    stamp_path.unlink()\n+                elif name == \"unreadable\":\n+                    stamp_path.unlink()\n+                    stamp_path.mkdir()\n+                elif name == \"malformed\":\n+                    stamp_path.write_text(\"{not-json\\n\", encoding=\"utf-8\")\n+                else:\n+                    stamp = json.loads(stamp_path.read_text(encoding=\"utf-8\"))\n+                    if name == \"missing-digest\":\n+                        stamp[\"digests\"] = {}\n+                    else:\n+                        stamp[\"digests\"] = {\n+                            relative: \"deadbeef\" for relative in legacy_paths\n+                        }\n+                    stamp_path.write_text(\n+                        json.dumps(stamp, indent=2, sort_keys=True) + \"\\n\",\n+                        encoding=\"utf-8\",\n+                    )\n+\n+                before = _tree_snapshot(target)\n+                with self.assertRaises(shiki_installer.ShikiError) as raised:\n+                    _quiet_install(target, force=True, validate=False)\n+                after = _tree_snapshot(target)\n+\n+                self.assertEqual(before, after, f\"{name} preflight changed the tree\")\n+                message = str(raised.exception)\n+                self.assertIn(expected_reason, message)\n+                self.assertIn(\"No files were written\", message)\n+                for relative in legacy_paths:\n+                    self.assertIn(relative, message)\n+\n+    def test_absent_stamp_is_allowed_when_no_legacy_path_exists(self) -> None:\n+        target = self.tmp / \"no-legacy\"\n+        target.mkdir()\n+        _quiet_install(target, force=True, validate=False)\n+        self.assertTrue(\n+            (target / \"docs\" / \"adr\" / \"SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\").is_file()\n+        )\n+\n+\n+if __name__ == \"__main__\":\n+    unittest.main()\n\n2026-08-07T08:13:24.575868Z ERROR codex_models_manager::manager: failed to renew cache TTL: missing field `base_instructions` at line 94 column 5\napply patch\npatch: completed\n/private/tmp/shiki-adr-collision.pa0KwY/.worktrees/shiki-t-20260807t073307221254z-2d22d654-introduc/docs/adr/SADR-0007-packaging-and-release.md\n/private/tmp/shiki-adr-collision.pa0KwY/.worktrees/shiki-t-20260807t073307221254z-2d22d654-introduc/docs/adr/SADR-0008-use-claude-code-as-default-implementer.md\n/private/tmp/shiki-adr-collision.pa0KwY/.worktrees/shiki-t-20260807t073307221254z-2d22d654-introduc/docs/adr/SADR-0010-external-ai-guardian-review.md\n/private/tmp/shiki-adr-collision.pa0KwY/.worktrees/shiki-t-20260807t073307221254z-2d22d654-introduc/docs/adr/SADR-0013-bootstrap-grants-actions-approve-pull-requests.md\ndiff --git a/.codex/skills/shiki/SKILL.md b/.codex/skills/shiki/SKILL.md\nindex c8e2e85d9d75d49cc4f2d6286ee22659c4e53e75..93cded623b2c15d9f65c76491a1c2d813ac9e6c0\n--- a/.codex/skills/shiki/SKILL.md\n+++ b/.codex/skills/shiki/SKILL.md\n@@ -18,6 +18,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n Codex CLI does not register this skill as a custom `/shiki` slash command. When\n the user types `/shiki` in Codex CLI and gets \"Unrecognized command\", explain\n that this is expected: invoke Shiki in Codex with natural language such as\n@@ -69,7 +72,7 @@\n \n ## Responsibilities\n \n-- Claude Code implements and repairs by default (`shiki runner claude`, ADR 0008); Codex implements and repairs tasks explicitly assigned to `codex`.\n+- Claude Code implements and repairs by default (`shiki runner claude`, SADR-0008); Codex implements and repairs tasks explicitly assigned to `codex`.\n - Claude Code Action can act as GitHub-side CCA or reviewer.\n - CCA judges completion.\n - MergeGate authorizes state transitions and merge readiness.\ndiff --git a/.github/prompts/cca-completion-check.md b/.github/prompts/cca-completion-check.md\nindex ed203ad5f302ecc81372d8ab27d2b1232ca97497..fd89950528333ef6242e6d5443904f5392ab7e56\n--- a/.github/prompts/cca-completion-check.md\n+++ b/.github/prompts/cca-completion-check.md\n@@ -19,6 +19,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n ## Required Reading\n \n Read only the files needed to judge the PR contract. Prefer this order:\ndiff --git a/AGENTS.md b/AGENTS.md\nindex 7f041100d49c2a7525f51cb0f5d696a4a6d1c78c..2b700e989ff295987aa8a4b0c5fcb1acd80020da\n--- a/AGENTS.md\n+++ b/AGENTS.md\n@@ -44,6 +44,10 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use\n+`SADR-NNNN-*.md`; target product decisions retain `NNNN-*.md` and the `ADR`\n+namespace.\n+\n Use the canonical source-of-truth order above. Runtime-specific reading orders\n are operational guidance only and must not override or reorder it.\n \ndiff --git a/CLAUDE.md b/CLAUDE.md\nindex 98a932e7841a632866fcd53b0bad0e3af3711d3d..611d491912d6a72a0327a1731b8f33a41acb4baa\n--- a/CLAUDE.md\n+++ b/CLAUDE.md\n@@ -14,6 +14,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n ## Claude Role\n \n Default to planner, default implementer, reviewer, coordinator, documentation editor, CCA-style completion judge, and final-judgment assistant.\ndiff --git a/SYSTEM_PROMPT.md b/SYSTEM_PROMPT.md\nindex aaaea45e3a896c3a44ba89eec8bb84325ecf6c62..2e7c093ebe9e8bb34e0923756f191b4f7fa581f5\n--- a/SYSTEM_PROMPT.md\n+++ b/SYSTEM_PROMPT.md\n@@ -30,6 +30,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n Use the canonical source-of-truth order above. Runtime-specific reading orders\n are operational guidance only and must not override or reorder it. The current\n conversation is non-durable operator input.\ndiff --git a/docs/adr/0001-keep-shiki-as-platform-name.md b/docs/adr/SADR-0001-keep-shiki-as-platform-name.md\nindex 20a9637c3b9d8c97de7ba8db7cfae4d82d9a7dba..8b685439959aada1b221c112f0ac3c51ca8a1695\n--- a/docs/adr/0001-keep-shiki-as-platform-name.md\n+++ b/docs/adr/SADR-0001-keep-shiki-as-platform-name.md\n@@ -1,3 +1,3 @@\n-# Keep Shiki As The Platform Name\n+# SADR-0001: Keep Shiki As The Platform Name\n \n Shiki remains the product name for the GitHub-native agentic engineering control plane. We considered more descriptive names such as MergePilot, MergeGate, and Kiroku, but kept Shiki because it carries the existing goal-seeking framework identity while remaining independent of Hermes, Codex, Claude Code, or any single runtime.\ndiff --git a/docs/adr/0002-use-agents-as-shared-constitution.md b/docs/adr/SADR-0002-use-agents-as-shared-constitution.md\nindex 73de30ecb078120eac540d9bc51855e3cd48d0e3..c5486182bf06fca36ea508496b013c67302fd989\n--- a/docs/adr/0002-use-agents-as-shared-constitution.md\n+++ b/docs/adr/SADR-0002-use-agents-as-shared-constitution.md\n@@ -1,3 +1,3 @@\n-# Use AGENTS As The Shared Agent Constitution\n+# SADR-0002: Use AGENTS As The Shared Agent Constitution\n \n Shiki uses `AGENTS.md` as the runtime-neutral constitution and `CLAUDE.md` as a thin Claude Code wrapper. This keeps Codex, Claude Code, Hermes Runner, and future Agent Runtimes aligned on the same GitHub-first MergeGate rules while still allowing runtime-specific instructions where needed.\ndiff --git a/docs/adr/0003-require-skill-gate-for-mergegate-readiness.md b/docs/adr/SADR-0003-require-skill-gate-for-mergegate-readiness.md\nindex 424be0c9af4c8f0136f1fde68a468c47ff84e764..7fc337987c7df9792f75f181dfcbed75fe5ffff2\n--- a/docs/adr/0003-require-skill-gate-for-mergegate-readiness.md\n+++ b/docs/adr/SADR-0003-require-skill-gate-for-mergegate-readiness.md\n@@ -1,3 +1,3 @@\n-# Require Skill Gate For MergeGate Readiness\n+# SADR-0003: Require Skill Gate For MergeGate Readiness\n \n Shiki requires relevant engineering skills to be invoked before execution when their triggers apply. This makes skills part of MergeGate readiness rather than optional prompt style, because Shiki depends on repeatable planning, issue decomposition, TDD, diagnosis, triage, and architecture review before autonomous or parallel agent work can be trusted.\ndiff --git a/docs/adr/0004-use-github-first-with-shiki-mirror.md b/docs/adr/SADR-0004-use-github-first-with-shiki-mirror.md\nindex d0a3a9174dc646a945a9d325954ffaf00c352040..a79c54ae9ee8eed2a0524192b864da4e22a92d3f\n--- a/docs/adr/0004-use-github-first-with-shiki-mirror.md\n+++ b/docs/adr/SADR-0004-use-github-first-with-shiki-mirror.md\n@@ -1,4 +1,4 @@\n-# ADR 0004: Use GitHub First With A Repository-Local Shiki Mirror\n+# SADR-0004: Use GitHub First With A Repository-Local Shiki Mirror\n \n ## Status\n \ndiff --git a/docs/adr/0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md b/docs/adr/SADR-0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\nindex b339baf49b89d9290e79c3d51b36e7b2bde3bd95..c30a0d8b1fde0224d0ac7820e7870dccf721580d\n--- a/docs/adr/0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\n+++ b/docs/adr/SADR-0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\n@@ -1,4 +1,4 @@\n-# ADR 0005: Use Codex As Front And Claude Code Action As The GitHub Runtime\n+# SADR-0005: Use Codex As Front And Claude Code Action As The GitHub Runtime\n \n ## Status\n \ndiff --git a/docs/adr/0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md b/docs/adr/SADR-0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\nindex 4bcae9d1e52f0a37b8e9fbafa5cd14b6ee1993bd..7a38538a6ae9929567f0ade67e7ea6a60d0b3f7e\n--- a/docs/adr/0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\n+++ b/docs/adr/SADR-0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\n@@ -1,4 +1,4 @@\n-# ADR 0006: Separate Codex Implementation, CCA Completion, And MergeGate Authorization\n+# SADR-0006: Separate Codex Implementation, CCA Completion, And MergeGate Authorization\n \n ## Status\n \ndiff --git a/docs/adr/0007-packaging-and-release.md b/docs/adr/SADR-0007-packaging-and-release.md\nindex 79b84ddb97167372b44641e5b7860e400ce80822..bb445a2e2e856d9d1f5fdbe35de8d4e8cec0fb32\n--- a/docs/adr/0007-packaging-and-release.md\n+++ b/docs/adr/SADR-0007-packaging-and-release.md\n@@ -1,4 +1,4 @@\n-# ADR 0007: Packaging And Release For Shiki\n+# SADR-0007: Packaging And Release For Shiki\n \n ## Status\n \n@@ -25,7 +25,7 @@\n   not introduce third-party runtime dependencies or break the shim.\n - Releases must run inside the operator's subscription-authenticated toolchain\n   and must not require extra secrets beyond the built-in `GITHUB_TOKEN`\n-  (consistent with ADR 0005's no-API-key-by-default stance).\n+  (consistent with SADR-0005's no-API-key-by-default stance).\n - `.shiki/manifest.json` already encodes the install surface (`install.include`,\n   `install.create_directories`, `install.exclude_from_commit`), and it must not\n   be mutated as part of this change.\n@@ -34,7 +34,7 @@\n \n This is a hard-to-reverse platform decision (it sets the version source of\n truth, the release trigger, and the public install contract), so it is recorded\n-as an ADR.\n+as a SADR.\n \n ## Decision\n \n@@ -104,7 +104,7 @@\n 3. **Publishing to PyPI.** Rejected for now: Shiki is distributed as a\n    repository template + git-installable CLI, not a PyPI library; PyPI would add\n    credentials/secrets and a different versioning contract. Can be added later as\n-   its own ADR if a packaged distribution is needed.\n+   its own SADR if a packaged distribution is needed.\n 4. **Dynamic version (read `VERSION` via a build backend `dynamic` field).**\n    Rejected to keep `pyproject.toml` backend-agnostic and dependency-free; the\n    small duplication between `VERSION` and `pyproject.toml` is guarded by the\n@@ -123,4 +123,4 @@\n   releasing versions the whole platform but ships only `install.include` to\n   targets.\n - Future PyPI distribution or a console entry point are deferred and would each\n-  warrant their own ADR.\n+  warrant their own SADR.\ndiff --git a/docs/adr/0008-use-claude-code-as-default-implementer.md b/docs/adr/SADR-0008-use-claude-code-as-default-implementer.md\nindex dedf4510b7c7799521422f622029563d373db031..35c2625d908167fbc58855b5b63e283805f167e8\n--- a/docs/adr/0008-use-claude-code-as-default-implementer.md\n+++ b/docs/adr/SADR-0008-use-claude-code-as-default-implementer.md\n@@ -1,12 +1,12 @@\n-# ADR 0008: Use Claude Code As The Default Implementer Runtime\n+# SADR-0008: Use Claude Code As The Default Implementer Runtime\n \n ## Status\n \n-Proposed (supersedes ADR 0005 for the implementer default; keeps its auth model)\n+Proposed (supersedes SADR-0005 for the implementer default; keeps its auth model)\n \n ## Context\n \n-ADR 0005 made Codex Front the default implementation runtime, with Claude Code\n+SADR-0005 made Codex Front the default implementation runtime, with Claude Code\n restricted to planner and reviewer roles by the runtime registry, and the only\n autonomous implementation adapter was `shiki runner codex`.\n \n@@ -18,13 +18,13 @@\n - The platform's planning, review, and CCA layers already run on Claude Code,\n   and Claude Code's native capabilities (the built-in `/code-review` skill and\n   the Workflow multi-agent orchestration tool) have no Codex equivalent.\n-- The Goal lifecycle redesign (ADR 0009) requires an autonomous post-freeze\n+- The Goal lifecycle redesign (SADR-0009) requires an autonomous post-freeze\n   loop driven from the operator's Claude Code session, including bounded\n   repair dispatch, which must not depend on a runtime that cannot be invoked\n   unattended.\n \n The constitution claims Shiki is runtime-agnostic (\"not a Claude-only\n-workflow\", ADR 0002). Any change to the implementer default must preserve the\n+workflow\", SADR-0002). Any change to the implementer default must preserve the\n registry-based runtime model rather than hardcoding one vendor.\n \n ## Decision\n@@ -39,7 +39,7 @@\n   the task to review or repair-needed.\n - Keep Codex registered as an optional implementer runtime. `shiki runner\n   codex`, the Codex handoff templates, and the ChatGPT OAuth auth model from\n-  ADR 0005 remain supported for tasks explicitly assigned to `codex`.\n+  SADR-0005 remain supported for tasks explicitly assigned to `codex`.\n - Commit to mandating two Claude-native capabilities as loop contract in\n   follow-up Goals (status: the code-review gate was delivered by Goal G-B \u2014\n   skill registry, checklists item PR-12, default required_skills \u2014 while the\n@@ -60,7 +60,7 @@\n \n Out of scope: removing Codex support, changing the CCA runtime (Claude Code\n Action with `CLAUDE_CODE_OAUTH_TOKEN` stays), and API-key based automation\n-(still requires its own ADR per ADR 0005).\n+(still requires its own target ADR per SADR-0005).\n \n ## Consequences\n \n@@ -100,7 +100,7 @@\n   harness rejection of direct `codex exec` is outside Shiki's control, and the\n   plan's mandatory native capabilities cannot run on Codex at all.\n - **Remove Codex entirely**: rejected; it abandons the runtime-agnostic\n-  constitution (ADR 0002) for no operational gain and forecloses\n+  constitution (SADR-0002) for no operational gain and forecloses\n   cross-runtime adversarial review.\n - **GitHub Actions cloud implementation via claude-code-action**: rejected as\n   default; durable evidence is automatic but token cost, Node 24 workflow\ndiff --git a/docs/adr/0009-requirements-definition-and-spec-freeze.md b/docs/adr/SADR-0009-requirements-definition-and-spec-freeze.md\nindex 29b3f7871bef9203a8b4a831a65c39eb2f506e42..52e7dd69ad0aa4efe0b68c62678abbf7df02aaec\n--- a/docs/adr/0009-requirements-definition-and-spec-freeze.md\n+++ b/docs/adr/SADR-0009-requirements-definition-and-spec-freeze.md\n@@ -1,4 +1,4 @@\n-# ADR 0009: Merge Grill And Planning Into Requirements Definition With Spec Freeze\n+# SADR-0009: Merge Grill And Planning Into Requirements Definition With Spec Freeze\n \n ## Status\n \ndiff --git a/docs/adr/0010-external-ai-guardian-review.md b/docs/adr/SADR-0010-external-ai-guardian-review.md\nindex e6108bbe8bb4e73e0ff4684c8f78001a80438c4e..f3510b145664298adafb151b6f0db51b972f01ec\n--- a/docs/adr/0010-external-ai-guardian-review.md\n+++ b/docs/adr/SADR-0010-external-ai-guardian-review.md\n@@ -1,4 +1,4 @@\n-# ADR 0010: External AI Guardian Review As A First-Class Approval Authority\n+# SADR-0010: External AI Guardian Review As A First-Class Approval Authority\n \n ## Status\n \n@@ -8,7 +8,7 @@\n \n Shiki is intended to be an autonomous, recursive, self-improving agent\n framework: authority-in-the-loop, not human-in-the-loop. Until now the Guardian\n-approval required for high/critical-risk merges (guardian-policy.json, ADR\n+approval required for high/critical-risk merges (guardian-policy.json, SADR\n governance) recognized only human authority sources \u2014 a GitHub review approval,\n the `guardian:approved` label applied by a configured Guardian, or a comment\n carrying `Guardian approval granted` plus the current head SHA.\n@@ -89,7 +89,7 @@\n   autonomous design the operator intends and produced the impersonation\n   incident.\n - **Record AI approval as an operator comment**: rejected as forgery of human\n-  authority \u2014 the exact failure this ADR corrects.\n+  authority \u2014 the exact failure this SADR corrects.\n - **Deliver the artifact as a committed `.shiki/` file**: rejected; a committed\n   approval inside the PR being approved is forgeable by anyone who can push.\n - **A dedicated reviewer bot identity instead of a relayed comment**: deferred;\ndiff --git a/docs/adr/0011-autonomous-quality-gate-evidence.md b/docs/adr/SADR-0011-autonomous-quality-gate-evidence.md\nindex bca0246c5d46e44afbde15486a75eec425fc02f5..9c93eac16d8c3a6d4b11bd7224e3955102cd7db4\n--- a/docs/adr/0011-autonomous-quality-gate-evidence.md\n+++ b/docs/adr/SADR-0011-autonomous-quality-gate-evidence.md\n@@ -1,4 +1,4 @@\n-# ADR 0011: Autonomous Quality-Gate Evidence \u2014 Deterministic Facts And An Independent Verifier\n+# SADR-0011: Autonomous Quality-Gate Evidence \u2014 Deterministic Facts And An Independent Verifier\n \n ## Status\n \ndiff --git a/docs/adr/0012-autonomous-loop-closeout-to-main.md b/docs/adr/SADR-0012-autonomous-loop-closeout-to-main.md\nindex 637fad6dde4989c9c2bf0f81f676d8d29991123c..ec7d85f81030a676bcbba28b43eb365422a36eac\n--- a/docs/adr/0012-autonomous-loop-closeout-to-main.md\n+++ b/docs/adr/SADR-0012-autonomous-loop-closeout-to-main.md\n@@ -1,4 +1,4 @@\n-# 0012 \u2014 Autonomous loop pushes goal completion to main via a closeout PR\n+# SADR-0012 \u2014 Autonomous loop pushes goal completion to main via a closeout PR\n \n - Status: accepted\n - Date: 2026-06-18\ndiff --git a/docs/adr/0013-bootstrap-grants-actions-approve-pull-requests.md b/docs/adr/SADR-0013-bootstrap-grants-actions-approve-pull-requests.md\nindex b6c69d64f1c5ced346b88d6859586715b3b5bd0e..cf48ff4a6716267f539ca58edd1a852e7c7a2d64\n--- a/docs/adr/0013-bootstrap-grants-actions-approve-pull-requests.md\n+++ b/docs/adr/SADR-0013-bootstrap-grants-actions-approve-pull-requests.md\n@@ -1,4 +1,4 @@\n-# ADR 0013: Bootstrap Grants GitHub Actions Permission To Create And Approve Pull Requests\n+# SADR-0013: Bootstrap Grants GitHub Actions Permission To Create And Approve Pull Requests\n \n ## Status\n \n@@ -31,7 +31,7 @@\n security tradeoff: it lets repository automation satisfy a human-review gate. In\n a multi-maintainer repo that could let a compromised or buggy workflow\n self-approve changes. So whether bootstrap turns it on by default \u2014 and with\n-what default token scope \u2014 is an ADR-worthy decision, not an implementation\n+what default token scope \u2014 is a SADR-worthy decision, not an implementation\n detail.\n \n ## Decision\n@@ -48,7 +48,7 @@\n Specifically:\n \n - **`can_approve_pull_request_reviews` defaults to `true`.** Shiki's default\n-  posture is solo/AFK autonomous operation (ADR 0008/0009/0012), where the CCA\n+  posture is solo/AFK autonomous operation (SADR-0008/0009/0012), where the CCA\n   Review Bridge is the only path that can satisfy `required_review: true` after\n   the CCA and Guardian gates have already passed. The default makes a\n   freshly-bootstrapped repo able to run the autonomous loop end to end without a\ndiff --git a/docs/adr/0014-codex-app-external-ai-guardian-ui-adapter.md b/docs/adr/SADR-0014-codex-app-external-ai-guardian-ui-adapter.md\nindex 238db06279334caa341c117362df9271382a7fcd..65f12e9939321e1b3e389a34c822fc1549e5155d\n--- a/docs/adr/0014-codex-app-external-ai-guardian-ui-adapter.md\n+++ b/docs/adr/SADR-0014-codex-app-external-ai-guardian-ui-adapter.md\n@@ -1,4 +1,4 @@\n-# ADR 0014: Use Codex App As The External AI Guardian UI Adapter\n+# SADR-0014: Use Codex App As The External AI Guardian UI Adapter\n \n ## Status\n \ndiff --git a/docs/adr/0015-contract-approval-before-dispatch.md b/docs/adr/SADR-0015-contract-approval-before-dispatch.md\nindex fad6c656741a20a7684ba16080a7db159f63a52f..6061271a46d3f1d6ae7174477de3fb75673123a6\n--- a/docs/adr/0015-contract-approval-before-dispatch.md\n+++ b/docs/adr/SADR-0015-contract-approval-before-dispatch.md\n@@ -1,4 +1,4 @@\n-# ADR 0015: Approve The Task Contract Before Dispatch, Not The Diff After It\n+# SADR-0015: Approve The Task Contract Before Dispatch, Not The Diff After It\n \n ## Status\n \n@@ -9,7 +9,7 @@\n Guardian approval for high/critical risk is expressible only on a pull request.\n Every approval source in `guardian-policy.json` \u2014 GitHub review, the\n `guardian:approved` label, the `Guardian approval granted` comment, and the\n-`external_ai_guardian_review` artifact (ADR 0010/0014) \u2014 is bound to a PR, and\n+`external_ai_guardian_review` artifact (SADR-0010/0014) \u2014 is bound to a PR, and\n three of the four require the current head SHA. Approval therefore cannot exist\n before an implementation PR exists.\n \n@@ -81,7 +81,7 @@\n An envelope breach must surface only on the non-repairable policy gate, never on\n the repairable `MergeGate metadata check`. Otherwise the Goal Loop routes it to\n `dispatch_repair` \u2014 instructing a runner to make the breach go away \u2014 which is\n-exactly what this ADR forbids.\n+exactly what this SADR forbids.\n \n **Contract immutability.** After registration, an implementation PR must not\n change the governance fields of its own task contract (`scope`, `non_goals`,\n@@ -116,7 +116,7 @@\n fails any verdict that is not `complete`, and the signal the CCA prompt reads is\n computed from `evaluate_guardian_approval` alone. Without the OR-merge at the\n signal, a contract-approved PR still returns `needs_guardian`, the CCA job fails,\n-and the MergeGate policy check never runs \u2014 the gate this ADR changes would never\n+and the MergeGate policy check never runs \u2014 the gate this SADR changes would never\n be reached. The CCA *prompt* needs no change: a `contract_approval` entry in the\n signal's existing `sources` array satisfies its current wording.\n \n@@ -130,7 +130,7 @@\n written, and an approved contract lets its implementation PRs merge through the\n same automatic path as low/medium risk. The wasted implement-then-block cycle\n disappears. Guardian review shifts from reading diffs to reading contracts, which\n-is both earlier and smaller; the external AI Guardian path (ADR 0010/0014) works\n+is both earlier and smaller; the external AI Guardian path (SADR-0010/0014) works\n on contracts with no change.\n \n We accept that the Guardian authorizes a class of change without seeing its\n@@ -146,7 +146,7 @@\n adversarial tests for the new approval path equivalent in rigor to\n `test_shiki_governance_evidence.sh`.\n \n-Bootstrap costs, which apply only while this ADR is being implemented. The tasks\n+Bootstrap costs, which apply only while this SADR is being implemented. The tasks\n that build Contract Approval merge under the existing rules, so the ones\n dispatched after the pre-dispatch gate lands but before the approval path lands\n need a Guardian-approved Contract PR *and* a live Guardian approval on their\n@@ -210,7 +210,7 @@\n risk; they do not bound authorized work.\n \n **Auto-escalate risk on envelope breach instead of stopping.** Returns the\n-workflow to a diff-time Guardian decision \u2014 the exact behavior this ADR removes.\n+workflow to a diff-time Guardian decision \u2014 the exact behavior this SADR removes.\n \n **Resolve governance fields from the base snapshot only.** Rejected against the\n registration flow: implementation PRs add their own contract, so the base has no\ndiff --git a/docs/adr/0016-locks-bound-product-paths.md b/docs/adr/SADR-0016-locks-bound-product-paths.md\nindex b5e850f3f1f7f4ef6e7f9d1763a29a51bd300c45..46b40a9b99a7aae644c02bb458f5473ff0a90ce2\n--- a/docs/adr/0016-locks-bound-product-paths.md\n+++ b/docs/adr/SADR-0016-locks-bound-product-paths.md\n@@ -1,4 +1,4 @@\n-# ADR 0016: Locks bound product paths; the Shiki mirror is bound by record identity\n+# SADR-0016: Locks bound product paths; the Shiki mirror is bound by record identity\n \n ## Status\n \n@@ -6,8 +6,8 @@\n \n - Date: 2026-07-28\n - Deciders: operator (mizutani-140), Claude (planner)\n-- Related: ADR 0004 (GitHub-first with a `.shiki` mirror), ADR 0009 (Spec Freeze),\n-  ADR 0012 (autonomous loop closeout to main), ADR 0015 (Contract Approval before\n+- Related: SADR-0004 (GitHub-first with a `.shiki` mirror), SADR-0009 (Spec Freeze),\n+  SADR-0012 (autonomous loop closeout to main), SADR-0015 (Contract Approval before\n   dispatch), remediation goal task R-02, the live PR #179 deadlock.\n \n ## Context\n@@ -90,11 +90,11 @@\n files, documentation files and named source modules are nameable; runtime-\n allocated ledger, runner and report ids are not \u2014 and those live in `.shiki`,\n which locks no longer govern. Existing frozen contracts are brought into line by\n-a one-time Spec Amendment per plan, which must land before ADR 0015's contract\n+a one-time Spec Amendment per plan, which must land before SADR-0015's contract\n immutability makes `locks` unwritable from the implementation PR.\n \n **Lock state is resolved from the durable side.** MergeGate resolves the lock\n-registry, and ADR 0015's governance fields, from the base `.shiki` snapshot when\n+registry, and SADR-0015's governance fields, from the base `.shiki` snapshot when\n present, falling back to head only when absent. The `MergeGate metadata check`\n job is given `--base-shiki`, which it does not receive today. Locks are not\n derived from the live open-pull-request set: a pull request body is untrusted\n@@ -108,7 +108,7 @@\n Explicitly out of scope: moving MergeGate's contract resolution off the PR\n checkout wholesale; making the mirror a projection of GitHub state; introducing\n new lock kinds; moving ledger evidence out of the repository; and unwinding\n-ADR 0012's closeout pull request. Registration of multi-task goals is ADR 0015's\n+SADR-0012's closeout pull request. Registration of multi-task goals is SADR-0015's\n Contract PR and is not re-decided here.\n \n ## Consequences\n@@ -127,7 +127,7 @@\n \n Risks accepted. First, goal-scoped identity is only as strong as `goal_id`,\n which is resolved from the PR body and the head task file; the mitigation is to\n-add `goal_id` to ADR 0015's frozen governance field set, and until that lands a\n+add `goal_id` to SADR-0015's frozen governance field set, and until that lands a\n task file can name a foreign goal. Second, base-when-present resolution is\n unavailable on fork pull requests, where the head is contributor-controlled;\n fork pull requests already cannot complete a Shiki merge, and this decision does\n@@ -139,7 +139,7 @@\n path, not by implementation pull requests.\n \n New obligations: one Spec Amendment per frozen plan to rewrite `locks`, ordered\n-before ADR 0015 T1; extension of the adversarial suites in\n+before SADR-0015 T1; extension of the adversarial suites in\n `scripts/test_shiki_adversarial_state.sh` and\n `scripts/test_shiki_governance_evidence.sh` to cover the eleven newly-scoped\n directories; and updating `scripts/test_shiki_loop_lock_guard.sh`,\n@@ -151,32 +151,32 @@\n whether the loop's transitive sync of ledger `evidence` references can be\n deleted once the Contract PR carries its own source plan.\n \n-## Relationship to ADR 0004\n+## Relationship to SADR-0004\n \n-This ADR **clarifies something ADR 0004 left unspecified**. ADR 0004 decides that\n+This SADR **clarifies something SADR-0004 left unspecified**. SADR-0004 decides that\n GitHub is the operational source of truth and that each target repository keeps a\n `.shiki/` mirror. It says nothing about pull requests writing the mirror, does\n not name MergeGate, and does not make the mirror an input to a gate. Nothing in\n-ADR 0004's Decision or Consequences is amended, superseded or weakened: the\n+SADR-0004's Decision or Consequences is amended, superseded or weakened: the\n mirror still exists, still records the same artifacts, and pull request\n-boundaries remain the coordination surface. What this ADR settles is the question\n+boundaries remain the coordination surface. What this SADR settles is the question\n 0004 never reached \u2014 by what authority a pull request may write a mirror record \u2014\n and it answers it the way `.shiki/manifest.json` already declares\n (`pr_mutation: current-task-or-current-goal-only`).\n \n-It **amends ADR 0012**, whose Decision item 5 states that \"Loop-executed tasks\n+It **amends SADR-0012**, whose Decision item 5 states that \"Loop-executed tasks\n declare the `path:.shiki/**` lock, which covers every `.shiki` file the closeout\n stages ... so `files_outside_locks` is satisfied without per-file lock\n bookkeeping.\" That clause is replaced: the closeout's mirror writes are\n-authorized by record identity, not by a declared lock. ADR 0012's substantive\n+authorized by record identity, not by a declared lock. SADR-0012's substantive\n decision \u2014 that completion reaches `main` through a closeout pull request rather\n than local attestation \u2014 is preserved unchanged.\n \n-It is **complementary to ADR 0015**, not a substitute. ADR 0015 owns contract\n+It is **complementary to SADR-0015**, not a substitute. SADR-0015 owns contract\n immutability, the pre-dispatch gate, the Contract PR and the Scope Envelope. This\n-ADR owns what a lock means and who may write the mirror. The one ordering\n+SADR owns what a lock means and who may write the mirror. The one ordering\n constraint between them is that the lock-narrowing Spec Amendment must precede\n-ADR 0015 T1's freezing of the `locks` field.\n+SADR-0015 T1's freezing of the `locks` field.\n \n ## Alternatives Considered\n \n@@ -186,7 +186,7 @@\n the only writer that can register them may run. Migrating the Guardian signal to\n that store makes every registration PR fail closed to `needs_guardian` at every\n risk level, because the task file is by definition absent from the store, which\n-removes the low/medium autonomous path ADR 0008/0009 exists to provide. And it\n+removes the low/medium autonomous path SADR-0008/0009 exists to provide. And it\n leaves schemas, manifest, config, guardian policy, templates, migrations, plans,\n worktrees, runner records, runs and handoffs with no writer at all in a\n repository whose product is `.shiki`. Measured benefit on the frozen backlog:\n@@ -199,14 +199,14 @@\n namespace is not in fact ownership-partitioned \u2014 56 ledger entries carry no\n `task_id`, and one goal DAG is writable by 35 registered tasks. The correct\n expression of its insight is that locks do not govern the mirror at all, which is\n-what this ADR decides.\n+what this SADR decides.\n \n **Make the mirror a projection written only by a registration writer and a\n post-merge projection job.** Rejected. It requires a bypass actor with write\n access to the protected default branch; it makes its own master invariant\n readable from a manifest that pull requests may edit; it deletes both reconcile\n modes and leaves a corrupted mirror with no in-band repair path, contradicting\n-ADR 0004's consequence that mirror conflicts are repaired against GitHub state;\n+SADR-0004's consequence that mirror conflicts are repaired against GitHub state;\n and its content-derived ids are rejected by `validate_shiki`'s currently-green id\n pattern.\n \ndiff --git a/docs/adr/0017-bookkeeping-closeouts-do-not-inherit-risk.md b/docs/adr/SADR-0017-bookkeeping-closeouts-do-not-inherit-risk.md\nindex 554e3a0d526a72ca067108c5464cc68c3edcf174..b9676857a4713850e063af7c39c8e0b01ecb4c66\n--- a/docs/adr/0017-bookkeeping-closeouts-do-not-inherit-risk.md\n+++ b/docs/adr/SADR-0017-bookkeeping-closeouts-do-not-inherit-risk.md\n@@ -1,4 +1,4 @@\n-# ADR 0017: A Bookkeeping Closeout Does Not Inherit Its Task's Risk\n+# SADR-0017: A Bookkeeping Closeout Does Not Inherit Its Task's Risk\n \n ## Status\n \n@@ -6,7 +6,7 @@\n \n ## Context\n \n-ADR 0012 made the goal loop push a task's completion to the default branch through a\n+SADR-0012 made the goal loop push a task's completion to the default branch through a\n closeout pull request: after the implementation PR merges, the loop opens a second PR\n carrying `task.status = done`, `lock.state = released`, and \u2014 when the task completes\n its goal \u2014 `goal.status = complete` plus one scorecard report. Nothing else. The\n@@ -49,14 +49,14 @@\n 4. The task file's governance fields (`scope`, `non_goals`, `required_skills`,\n    `risk_level`, `locks`, `acceptance_checks`, `test_command`) are byte-identical to\n    the base snapshot. Only `status`, `expected_pr`, `closeout_pr`, `ledger_evidence`\n-   and `expected_branch` may differ \u2014 the ADR 0015 contract-immutability rule,\n+   and `expected_branch` may differ \u2014 the SADR-0015 contract-immutability rule,\n    unchanged.\n \n    `expected_branch` is in that set because a closeout is *by construction* on a\n    different branch from the implementation it closes, and the loop rewrites the\n    field accordingly. It is safe to let it differ because MergeGate independently\n    requires `expected_branch` to equal the PR's actual head ref, so a PR cannot set\n-   it to anything other than the branch it is really on. The first draft of this ADR\n+   it to anything other than the branch it is really on. The first draft of this SADR\n    omitted it, which made the exemption unreachable: every real closeout failed\n    condition 4 on a field the loop always changes. See PR #209.\n 5. The state transitions are exactly the terminal set: task `review` \u2192 `done`, lock\n@@ -113,12 +113,12 @@\n classification must be derived, not asserted.\n \n **Fold the closeout into the implementation PR** so no second PR exists. Rejected under\n-ADR 0012, which recorded why: a task file that is already `done` on the implementation\n+SADR-0012, which recorded why: a task file that is already `done` on the implementation\n branch short-circuits the loop's review \u2192 CCA \u2192 merge \u2192 done ordering into a local\n goal-completion before any merge happens.\n \n **Have the loop auto-merge closeouts with elevated credentials.** Rejected outright: that\n-is a runtime authoring its own approval, which ADR 0010 and the constitution's\n+is a runtime authoring its own approval, which SADR-0010 and the constitution's\n approval-record prohibition forbid. It would also be indistinguishable, in the evidence,\n from the forgery those rules exist to prevent.\n \ndiff --git a/docs/adr/0018-carried-guardian-approval-across-a-proven-base-sync.md b/docs/adr/SADR-0018-carried-guardian-approval-across-a-proven-base-sync.md\nindex 22358562da390ed8b45cd600da08a43d378b4680..e6e2907cefa776460af73ddf7d39db605f3c39b0\n--- a/docs/adr/0018-carried-guardian-approval-across-a-proven-base-sync.md\n+++ b/docs/adr/SADR-0018-carried-guardian-approval-across-a-proven-base-sync.md\n@@ -1,4 +1,4 @@\n-# ADR 0018: Carried Guardian Approval Across A Proven Base Sync\n+# SADR-0018: Carried Guardian Approval Across A Proven Base Sync\n \n ## Status\n \n@@ -34,7 +34,7 @@\n Guardian evaluator that consumes it (`shiki_guardian.evaluate_guardian_approval`,\n the `guardian_comment_carried` source) were built and tested in isolation. Neither\n does anything until a caller passes the flags that turn the carry on, so the\n-feature was invisible in CI. This ADR records the decision the two of them\n+feature was invisible in CI. This SADR records the decision the two of them\n implement, and this task wires it into the four CI invocations that judge Guardian\n approval.\n \ndiff --git a/docs/adr/README.md b/docs/adr/README.md\nindex c70e057a4520c36e042ab68c53cd4a21ef709b71..5da43856836e2ce6a150f8127766e2f88355ab4b\n--- a/docs/adr/README.md\n+++ b/docs/adr/README.md\n@@ -1,40 +1,41 @@\n-# Architecture Decision Records\n+# Decision Record Namespaces\n \n-This directory holds Shiki's Architecture Decision Records (ADRs): durable\n-records of hard-to-reverse platform decisions. ADRs are part of the Shiki source\n-of truth alongside GitHub state, the `.shiki/` mirror, and `CONTEXT.md`.\n+This directory holds two independent decision namespaces:\n \n-## How ADRs Work\n+- Shiki Architecture Decision Records (SADRs), named\n+  `SADR-NNNN-<short-slug>.md`, record hard-to-reverse Shiki platform decisions.\n+- Target Architecture Decision Records (ADRs), named\n+  `NNNN-<short-slug>.md`, belong to the target product.\n \n-- New ADRs are added as `docs/adr/NNNN-<short-slug>.md`, where `NNNN` is the next\n-  zero-padded sequence number.\n-- The **canonical list of ADRs is the set of files matching `docs/adr/*.md`** in\n-  this directory. Browse `docs/adr/*.md` for the authoritative, current set.\n-- Use [`template.md`](template.md) as the starting point for a new ADR.\n-- Record an ADR when a decision is hard to reverse, surprising without context,\n-  and a real tradeoff. Do not write an ADR for routine or easily reversible\n-  choices.\n+The same four-digit number may appear once in each namespace. A platform\n+`SADR-NNNN` citation resolves only to `SADR-NNNN-*.md`; a target `ADR NNNN`\n+citation resolves only to `NNNN-*.md`.\n \n-Using the `docs/adr/*.md` glob as the source of truth means other PRs can add an\n-ADR without editing this index file, avoiding merge contention on a\n-hand-maintained enumeration.\n+## How Decision Records Work\n \n-## Current ADRs (snapshot)\n-\n-The list below is a convenience snapshot and may lag behind the directory. When\n-in doubt, list `docs/adr/*.md`.\n+- New Shiki platform decisions use the next zero-padded SADR number.\n+- New target product decisions use the target's own ADR sequence.\n+- The canonical list is the files in `docs/adr/`; no hand-maintained index is\n+  authoritative.\n+- Use [`template.md`](template.md) as the starting point and choose the filename\n+  namespace that owns the decision.\n+- Record a decision when it is hard to reverse, surprising without context, and\n+  a real tradeoff. Do not write a decision record for routine or easily\n+  reversible choices.\n \n-- [0001 \u2014 Keep Shiki As The Platform Name](0001-keep-shiki-as-platform-name.md)\n-- [0002 \u2014 Use AGENTS As The Shared Agent Constitution](0002-use-agents-as-shared-constitution.md)\n-- [0003 \u2014 Require Skill Gate For MergeGate Readiness](0003-require-skill-gate-for-mergegate-readiness.md)\n-- [0004 \u2014 Use GitHub First With A Repository-Local Shiki Mirror](0004-use-github-first-with-shiki-mirror.md)\n-- [0005 \u2014 Use Codex As Front And Claude Code Action As The GitHub Runtime](0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md)\n-- [0006 \u2014 Separate Codex Implementation, CCA Completion, And MergeGate Authorization](0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md)\n+Using the directory as the source of truth lets independent changes add records\n+without contending on an enumerated index.\n \n-## Adding A New ADR\n+## Adding A Shiki SADR\n \n-1. Copy `template.md` to `docs/adr/NNNN-<short-slug>.md`.\n+1. Copy `template.md` to `docs/adr/SADR-NNNN-<short-slug>.md`.\n 2. Fill in the title, status, context, decision, and consequences.\n-3. Set the status to `Proposed`, then `Accepted` once the decision is approved.\n-4. Open a PR following `CONTRIBUTING.md`. You do not need to edit this index for\n-   the ADR to be discoverable via `docs/adr/*.md`.\n+3. Set the status to `Proposed`, then `Accepted` once the proper authority has\n+   approved the decision.\n+4. Open a PR following `CONTRIBUTING.md`.\n+\n+## Adding A Target ADR\n+\n+1. Copy `template.md` to `docs/adr/NNNN-<short-slug>.md` using the target's own\n+   sequence.\n+2. Cite it as `ADR NNNN`; do not prefix its filename with `SADR-`.\ndiff --git a/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md b/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\nnew file mode 100644\nindex 0000000000000000000000000000000000000000..7455ce1f3bed02ddddb851ac21a1d1a648c9aeba\n--- /dev/null\n+++ b/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\n@@ -0,0 +1,55 @@\n+# SADR-0019: Separate Shiki Platform SADRs From Target ADRs\n+\n+## Status\n+\n+Accepted\n+\n+## Context\n+\n+Shiki installs its platform decision records into `docs/adr/` in every target\n+repository. Shiki and its targets previously used the same numeric\n+`NNNN-<slug>.md` namespace. A target that already owned ADRs 0013 through 0018\n+therefore acquired two records for each number during installation, making an\n+`ADR NNNN` citation ambiguous. Renumbering target decisions would rewrite target\n+history, while renumbering Shiki decisions per target would destroy their stable\n+platform identity.\n+\n+Upgrades also need to remove the former Shiki-owned numeric paths without ever\n+mistaking a target-owned ADR for platform content. The install stamp already\n+records exact shipped paths and content digests, so it can prove ownership only\n+when both still match.\n+\n+## Decision\n+\n+Shiki platform decisions use the `SADR` namespace:\n+\n+- records are named `docs/adr/SADR-NNNN-<slug>.md`;\n+- live platform citations use `SADR-NNNN`;\n+- target product decisions retain `docs/adr/NNNN-<slug>.md` and `ADR NNNN`;\n+- the reference validator resolves the two namespaces independently, so the same\n+  four-digit number may exist once in each namespace.\n+\n+Before a forced upgrade writes anything, the installer inventories the exact\n+former numeric Shiki ADR paths. It may delete a legacy path only when the\n+existing install stamp contains that exact path and its recorded SHA-256 digest\n+matches the file currently on disk. All ownership blockers are reported\n+together and abort the upgrade before its first write. A non-forced install\n+never performs this cleanup.\n+\n+Historical `.shiki/` evidence remains unchanged because it records the namespace\n+that existed when that evidence was produced.\n+\n+## Consequences\n+\n+- Targets may use any ADR number without colliding with Shiki's platform SADRs.\n+- Fresh installs preserve target ADR bytes and install a stable SADR set.\n+- Forced upgrades fail closed when legacy ownership cannot be proven, and a\n+  successful upgrade refreshes the stamp without removed legacy paths.\n+- Current governance, packaging, workflow, prompt, script, and skill references\n+  must move with the platform records.\n+\n+## Alternatives Considered\n+\n+- Collision-only refusal leaves affected targets permanently unupgradable.\n+- Renumbering target ADRs mutates target-owned history.\n+- Per-target Shiki renumbering makes platform citations non-portable.\ndiff --git a/docs/adr/template.md b/docs/adr/template.md\nindex a92a97daddd0b00ab9dd626cc8d86942da2eafed..17dd79175b5fee2105935557f691b40cf9492942\n--- a/docs/adr/template.md\n+++ b/docs/adr/template.md\n@@ -1,14 +1,14 @@\n-# NNNN: <Short Decision Title>\n+# <SADR-NNNN or NNNN>: <Short Decision Title>\n \n ## Status\n \n-Proposed | Accepted | Superseded by ADR-NNNN\n+Proposed | Accepted | Superseded by SADR-NNNN or target ADR NNNN\n \n ## Context\n \n What is the situation, constraint, or force that requires a decision? Reference\n-relevant domain terms (`CONTEXT.md`), prior ADRs, and code reality. State the\n-problem, not the solution.\n+relevant domain terms (`CONTEXT.md`), prior Shiki SADRs or target ADRs, and code\n+reality. State the problem, not the solution.\n \n ## Decision\n \ndiff --git a/docs/agents/autonomous-loop-e2e-acceptance.md b/docs/agents/autonomous-loop-e2e-acceptance.md\nindex bbf84c6081987f159ac6ad9ecf3c82b7aa72e7e8..5c13c1d02a05a90b0dc46cbef7c95a9846d0c237\n--- a/docs/agents/autonomous-loop-e2e-acceptance.md\n+++ b/docs/agents/autonomous-loop-e2e-acceptance.md\n@@ -40,8 +40,8 @@\n 3. `gh auth status` is logged in with merge permission on the repo.\n 4. Branch protection + required checks are configured exactly as the goal-loop\n    required-check set expects (see `docs/agents/branch-protection-smoke.md`).\n-5. The repo `.shiki/policy` permits autonomous low/medium-risk merges (ADR\n-   0008/0009); the MergeGate policy check is the recorded-authority gate.\n+5. The repo `.shiki/policy` permits autonomous low/medium-risk merges\n+   (SADR-0008/0009); the MergeGate policy check is the recorded-authority gate.\n \n The operator's only actions are: register the task and start the loop. **No\n manual pre-clearing** means after `shiki loop run` starts, the operator does not\ndiff --git a/docs/agents/domain.md b/docs/agents/domain.md\nindex 258da8ea81e87ac0a6b0b5b5e5c2fde980eaabb7..67aaa6f4b7d0a5aa4d495ce6279073333f7798cb\n--- a/docs/agents/domain.md\n+++ b/docs/agents/domain.md\n@@ -5,7 +5,9 @@\n ## Layout\n \n - `CONTEXT.md` defines Shiki language. Keep it implementation-free.\n-- `docs/adr/` records hard-to-reverse decisions.\n+- `docs/adr/SADR-NNNN-*.md` records Shiki platform decisions.\n+- `docs/adr/NNNN-*.md` records target product decisions without sharing the\n+  SADR namespace.\n - `AGENTS.md` is the runtime-neutral agent constitution.\n - `CLAUDE.md` is the Claude Code wrapper around `AGENTS.md`.\n - `.shiki/` is the Target Repository mirror for Goals, PRDs, plans, Task DAGs, contracts, locks, worktree records, ledger entries, CCA verdicts, repair packets, reports, and handoffs.\ndiff --git a/docs/agents/guardian-policy.md b/docs/agents/guardian-policy.md\nindex 4147c7adf05a47adb4fd24ada1a1c2e1f2ba6d2d..a20d9cc6c00a569bb057b9ca8d6b51c7fc4a9847\n--- a/docs/agents/guardian-policy.md\n+++ b/docs/agents/guardian-policy.md\n@@ -98,8 +98,8 @@\n `origin/<default_branch>` and reuses each job's existing base fetch; a shallow\n (`--depth`) or second base fetch would make the proof unresolvable and silently\n disable the carry, so neither is permitted. CCA reads `guardian_comment_carried` as\n-a recorded authority in the deterministic signal and does not re-derive it. See ADR\n-0018 for the full decision, the rejected diff/patch-equality and path-disjointness\n+a recorded authority in the deterministic signal and does not re-derive it. See\n+SADR-0018 for the full decision, the rejected diff/patch-equality and path-disjointness\n alternatives, and the accepted residual.\n \n ## External AI Guardian Review (SADR-0010)\ndiff --git a/docs/agents/implementation-policy.md b/docs/agents/implementation-policy.md\nindex c754144a9fb5bf4a38aa82a60e1670c59e626b44..e90d40fe21880b9a09719c5ecfe6bde7284eb3ce\n--- a/docs/agents/implementation-policy.md\n+++ b/docs/agents/implementation-policy.md\n@@ -31,6 +31,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n ## Non-Negotiables\n \n 1. **Planning precedes implementation.** Non-trivial Goals start with `grill-with-docs` and Context & Impact.\ndiff --git a/scripts/guardian_approval_signal.py b/scripts/guardian_approval_signal.py\nindex d059f92b2c82aa2b5d6ca421365537a87886c1cb..16924892714f4f5b9303c0ef0e325a7e70075630\n--- a/scripts/guardian_approval_signal.py\n+++ b/scripts/guardian_approval_signal.py\n@@ -199,8 +199,8 @@\n ) -> Any:\n     \"\"\"Evaluate SADR-0015 Contract Approval for the CCA Guardian signal.\n \n-    The SAME OR that MergeGate applies must reach this deterministic signal: ADR\n-    0015 is explicit that without it, a contract-approved PR still returns\n+    The SAME OR that MergeGate applies must reach this deterministic signal:\n+    SADR-0015 is explicit that without it, a contract-approved PR still returns\n     ``needs_guardian``, the CCA job fails, and the MergeGate policy check never\n     runs \u2014 the gate this change moves would never be reached. This resolves the\n     task the SAME way MergeGate does (the first Shiki task id in the PR body),\ndiff --git a/scripts/shiki_contract_approval.py b/scripts/shiki_contract_approval.py\nindex 941bcb96fdd8b9a85bc0c4f0f04d5dad9d04839c..4a4b418f6527413161db82670bec857a043360e2\n--- a/scripts/shiki_contract_approval.py\n+++ b/scripts/shiki_contract_approval.py\n@@ -142,7 +142,7 @@\n     changed_files_status: Any,\n     registration: Any,\n ) -> ContractApprovalResult:\n-    \"\"\"Whether this PR carries an SADR-0015 Contract Approval.\n+    \"\"\"Whether this PR carries a SADR-0015 Contract Approval.\n \n     The four conditions are evaluated IN ORDER, each failure recorded as a\n     distinct reason string:\ndiff --git a/scripts/shiki_installer.py b/scripts/shiki_installer.py\nindex be064ab7a29ff6836f7f7a1d70500a43e0d11d7b..d3e3247edbf3e4be287a4667176d86f6500f8dc5\n--- a/scripts/shiki_installer.py\n+++ b/scripts/shiki_installer.py\n@@ -118,6 +118,7 @@\n # what lets a target carry it without a governance-schema change.\n INSTALL_STAMP_PATH = \".shiki/install-stamp.json\"\n INSTALL_STAMP_VERSION = 1\n+LEGACY_SHIKI_SADR_MAX = 18\n \n # Under --force the shipped surface splits three ways, so nothing is lost and\n # nothing is silently stale.\n@@ -237,9 +238,98 @@\n         )\n \n \n+def legacy_shiki_adr_paths() -> tuple[str, ...]:\n+    \"\"\"Former numeric paths for Shiki SADR-0001 through SADR-0018.\n+\n+    The authoritative slugs come from the current SADR filenames. SADR-0019 is\n+    the namespace decision itself and never shipped under a numeric legacy path.\n+    \"\"\"\n+    paths: list[str] = []\n+    for sadr in sorted((ROOT / \"docs\" / \"adr\").glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\")):\n+        number = sadr.name.split(\"-\", 2)[1]\n+        if int(number) <= LEGACY_SHIKI_SADR_MAX:\n+            paths.append(f\"docs/adr/{sadr.name.removeprefix('SADR-')}\")\n+    return tuple(paths)\n+\n+\n+def preflight_legacy_sadr_cleanup(target: Path, *, force: bool) -> tuple[str, ...]:\n+    \"\"\"Authorize exact legacy Shiki ADR removals before the first write.\n+\n+    A non-force install never removes existing files. A forced install needs no\n+    stamp when no legacy Shiki path is present. When legacy paths do exist, each\n+    must be named in a readable install stamp and its current bytes must match\n+    the recorded digest.\n+    \"\"\"\n+    if not force:\n+        return ()\n+    present = tuple(\n+        relative\n+        for relative in legacy_shiki_adr_paths()\n+        if (target / relative).exists() or (target / relative).is_symlink()\n+    )\n+    if not present:\n+        return ()\n+\n+    stamp_path = target / INSTALL_STAMP_PATH\n+    try:\n+        raw = stamp_path.read_text(encoding=\"utf-8\")\n+        stamp = json.loads(raw)\n+    except FileNotFoundError:\n+        detail = \"install stamp is absent\"\n+        blockers = [f\"{relative}: {detail}\" for relative in present]\n+    except (OSError, UnicodeDecodeError) as error:\n+        detail = f\"install stamp is unreadable ({error})\"\n+        blockers = [f\"{relative}: {detail}\" for relative in present]\n+    except json.JSONDecodeError as error:\n+        detail = f\"install stamp is malformed ({error})\"\n+        blockers = [f\"{relative}: {detail}\" for relative in present]\n+    else:\n+        if not isinstance(stamp, dict):\n+            blockers = [\n+                f\"{relative}: install stamp is malformed (expected an object)\"\n+                for relative in present\n+            ]\n+        else:\n+            digests = stamp.get(\"digests\")\n+            blockers = []\n+            if not isinstance(digests, dict):\n+                blockers.extend(\n+                    f\"{relative}: install stamp has no digest for this path\"\n+                    for relative in present\n+                )\n+            else:\n+                for relative in present:\n+                    expected = digests.get(relative)\n+                    if not isinstance(expected, str) or not expected:\n+                        blockers.append(\n+                            f\"{relative}: install stamp has no digest for this path\"\n+                        )\n+                        continue\n+                    try:\n+                        actual = _sha256_file(target / relative)\n+                    except OSError as error:\n+                        blockers.append(\n+                            f\"{relative}: legacy path is unreadable ({error})\"\n+                        )\n+                        continue\n+                    if actual != expected:\n+                        blockers.append(\n+                            f\"{relative}: digest mismatch (stamp {expected}, current {actual})\"\n+                        )\n+\n+    if blockers:\n+        joined = \"\\n\".join(f\"- {blocker}\" for blocker in blockers)\n+        raise ShikiError(\n+            \"legacy Shiki ADR cleanup is not authorized:\\n\"\n+            f\"{joined}\\nNo files were written.\"\n+        )\n+    return present\n+\n+\n def install_template(target: Path, *, force: bool, validate: bool) -> list[NewFileNote]:\n     # Preconditions first: never rewrite the tree of a target that cannot be\n     # upgraded cleanly (see refuse_pending_migrations).\n+    legacy_cleanup = preflight_legacy_sadr_cleanup(target, force=force)\n     refuse_pending_migrations(target)\n \n     # Resolve the target's required CODEOWNERS owner once from its\n@@ -250,6 +340,10 @@\n     # verbatim. See ``codeowners_required_owner``.\n     codeowners_owner = codeowners_required_owner(target)\n \n+    for relative in legacy_cleanup:\n+        (target / relative).unlink()\n+        info(f\"removed stamped legacy Shiki ADR: {target / relative}\")\n+\n     notes: list[NewFileNote] = []\n     for relative in TEMPLATE_PATHS:\n         source = ROOT / relative\ndiff --git a/scripts/shiki_loop.py b/scripts/shiki_loop.py\nindex 3611641da8aeb5038b1da90e3114b09a473ec6d8..84caa491b9cad0edc4176f209c13e9221e975681\n--- a/scripts/shiki_loop.py\n+++ b/scripts/shiki_loop.py\n@@ -424,7 +424,7 @@\n         # unresolvable CCA verdict has already produced its terminal stop and is never\n         # laundered into a head-moving sync (SADR-0010). It also never fires while the\n         # policy (Guardian) gate is red \u2014 for ANY reason, not only when it is the sole\n-        # red check \u2014 so a head-bound Guardian approval, or an SADR-0015\n+        # red check \u2014 so a head-bound Guardian approval, or a SADR-0015\n         # contract-approval carry that base movement defeated (the policy gate compares\n         # base and head task governance directly), is never silently restored by\n         # adopting base wholesale. It is bounded by a durable per-task counter so the\ndiff --git a/scripts/test_shiki_goal_loop.sh b/scripts/test_shiki_goal_loop.sh\nindex 753c7b516e10f0be3bc92a4bd0eb1fa699776ae6..8f3820a0a079b3044424cfe1a6c6b7fe68896e67\n--- a/scripts/test_shiki_goal_loop.sh\n+++ b/scripts/test_shiki_goal_loop.sh\n@@ -231,8 +231,8 @@\n \n # High/critical risk merges autonomously when all required checks are green:\n # the \"MergeGate policy check\" required check IS the Guardian gate (it enforces\n-# guardian-policy.json \u2014 human approval OR external AI guardian review, ADR\n-# 0010), so green checks mean a recorded authority approved. The fake gh returns\n+# guardian-policy.json \u2014 human approval OR external AI guardian review,\n+# SADR-0010), so green checks mean a recorded authority approved. The fake gh returns\n # green checks, simulating that the Guardian gate passed.\n python3 \"$ROOT/scripts/shiki.py\" goal create --target \"$TARGET\" --title \"Guardian gate\" --outcome \"High risk merges via policy gate\" >/tmp/shiki-goal-loop-guardian-goal.json\n GGOAL=\"$(json_get /tmp/shiki-goal-loop-guardian-goal.json goal_id)\"\ndiff --git a/tests/test_adr_references.py b/tests/test_adr_references.py\nindex 51706329c797d5dd9b47e082a333fa83b261d482..db6e693e35f67103935be34f1d9bb4ff7ee60395\n--- a/tests/test_adr_references.py\n+++ b/tests/test_adr_references.py\n@@ -1,7 +1,7 @@\n-\"\"\"Every ``ADR ####`` citation must resolve to exactly one decision record.\n+\"\"\"Every target ADR and Shiki SADR citation resolves in its own namespace.\n \n Background. ``scripts/mergegate_check.py`` justifies ``is_bookkeeping_closeout``\n-with \"ADR 0017\" nine times and ``scripts/guardian_approval_signal.py`` cites\n+with \"SADR-0017\" nine times and ``scripts/guardian_approval_signal.py`` cites\n 0016/0017 seven times, yet for a stretch the numbering in ``docs/adr/`` stopped\n at 0015: the authoring commits lived on a coordinator branch and no task ever\n declared ``docs/adr/...`` in its locks, so nothing carried the records to the\n@@ -11,11 +11,12 @@\n and the directory drifted with no signal.\n \n This is the same unbound-pair shape behind other silent failures: a reference\n-and its target with nothing asserting they agree. This test binds them. It scans\n-every ``scripts/*.py`` (plus ``docs/**/*.md`` and the root ``*.md`` governance\n-files, which are cheap) for the ``ADR ####`` pattern and asserts each cited\n-number resolves to exactly one ``docs/adr/####-*.md``. The converse is\n-deliberately NOT required: an ADR may exist without being cited.\n+and its target with nothing asserting they agree. This test binds them. Target\n+``ADR NNNN`` citations resolve only to ``docs/adr/NNNN-*.md``; Shiki\n+``SADR-NNNN`` citations resolve only to ``docs/adr/SADR-NNNN-*.md``. The scan\n+covers current governance docs, scripts, workflows, prompts, packaging metadata,\n+and repo-local Codex guidance while deliberately excluding historical\n+``.shiki`` evidence. The converse is not required: a record may be uncited.\n \n The suite is dependency free: standard-library ``unittest`` only.\n \"\"\"\n@@ -31,17 +32,18 @@\n \n REPO_ROOT = Path(__file__).resolve().parents[1]\n \n-# \"ADR 0016\" -- a four-digit citation. The trailing \\b keeps a five-digit token\n-# from being read as a truncated four-digit number.\n-_CITATION = re.compile(r\"\\bADR (\\d{4})\\b\")\n-# \"0016-locks-bound-product-paths.md\" -- the leading four digits name the record.\n-_ADR_FILENAME = re.compile(r\"^(\\d{4})-.*\\.md$\")\n+# ``ADR 0016`` / ``SADR-0016`` -- a namespace plus four-digit citation. The\n+# trailing ``\\b`` keeps a five-digit token from being read as a truncated one.\n+_CITATION = re.compile(r\"\\b(S?ADR)[ -](\\d{4})\\b\")\n+# Target ADRs retain the numeric filename; Shiki platform decisions use SADR.\n+_DECISION_FILENAME = re.compile(r\"^(?:(SADR)-)?(\\d{4})-.*\\.md$\")\n \n \n class Violation(NamedTuple):\n     \"\"\"A citation that does not resolve to exactly one decision record.\"\"\"\n \n     citing_file: str  # repo-relative path of the file that cites the ADR\n+    namespace: str  # \"ADR\" for target decisions; \"SADR\" for Shiki decisions\n     number: str  # the cited four-digit number, e.g. \"0016\"\n     line: int  # 1-based line of the citation\n     reason: str  # \"missing\" (zero matches) or \"ambiguous\" (two or more)\n@@ -49,44 +51,59 @@\n \n     @property\n     def expected_path(self) -> str:\n-        return f\"docs/adr/{self.number}-*.md\"\n+        prefix = \"SADR-\" if self.namespace == \"SADR\" else \"\"\n+        return f\"docs/adr/{prefix}{self.number}-*.md\"\n \n     def describe(self) -> str:\n         \"\"\"Name the citing file, the cited number, and the missing path.\"\"\"\n         if self.reason == \"missing\":\n             return (\n-                f\"{self.citing_file}:{self.line} cites ADR {self.number} \"\n+                f\"{self.citing_file}:{self.line} cites {self.namespace} {self.number} \"\n                 f\"but no document matches {self.expected_path}\"\n             )\n         joined = \", \".join(self.matches)\n         return (\n-            f\"{self.citing_file}:{self.line} cites ADR {self.number} which \"\n+            f\"{self.citing_file}:{self.line} cites {self.namespace} {self.number} which \"\n             f\"resolves to {len(self.matches)} documents (expected exactly one \"\n             f\"at {self.expected_path}): {joined}\"\n         )\n \n \n-def _adr_index(root: Path) -> dict[str, list[Path]]:\n-    \"\"\"Map every four-digit ADR number to the record files that claim it.\"\"\"\n-    index: dict[str, list[Path]] = {}\n+def _adr_index(root: Path) -> dict[tuple[str, str], list[Path]]:\n+    \"\"\"Map each decision namespace and number to the files that claim it.\"\"\"\n+    index: dict[tuple[str, str], list[Path]] = {}\n     adr_dir = root / \"docs\" / \"adr\"\n     if adr_dir.is_dir():\n         for path in sorted(adr_dir.glob(\"*.md\")):\n-            match = _ADR_FILENAME.match(path.name)\n+            match = _DECISION_FILENAME.match(path.name)\n             if match:\n-                index.setdefault(match.group(1), []).append(path)\n+                namespace = \"SADR\" if match.group(1) else \"ADR\"\n+                index.setdefault((namespace, match.group(2)), []).append(path)\n     return index\n \n \n def _citing_files(root: Path, include_docs: bool = True) -> list[Path]:\n-    \"\"\"Files scanned for citations: scripts/*.py, and cheaply the markdown.\"\"\"\n+    \"\"\"Current, shipped text surfaces scanned for decision citations.\"\"\"\n     files = sorted((root / \"scripts\").glob(\"*.py\"))\n+    files += sorted((root / \"scripts\").glob(\"*.sh\"))\n     if include_docs:\n         docs = root / \"docs\"\n         if docs.is_dir():\n             files += sorted(docs.rglob(\"*.md\"))\n         files += sorted(root.glob(\"*.md\"))\n-    return files\n+        pyproject = root / \"pyproject.toml\"\n+        if pyproject.is_file():\n+            files.append(pyproject)\n+        for relative in (\".github\", \".codex\"):\n+            surface = root / relative\n+            if surface.is_dir():\n+                files += sorted(\n+                    path\n+                    for path in surface.rglob(\"*\")\n+                    if path.is_file()\n+                    and path.suffix in {\".md\", \".py\", \".sh\", \".toml\", \".yaml\", \".yml\"}\n+                )\n+    return sorted(set(files))\n \n \n def find_adr_reference_violations(\n@@ -94,10 +111,9 @@\n ) -> list[Violation]:\n     \"\"\"Return every ``ADR ####`` citation under ``root`` that fails to resolve.\n \n-    A citation resolves when exactly one ``docs/adr/####-*.md`` matches its\n-    number. Zero matches is a \"missing\" violation; two or more is \"ambiguous\".\n-    Records that are never cited produce nothing -- the converse is not\n-    required.\n+    A citation resolves when exactly one record in the same namespace matches\n+    its number. Zero matches is a \"missing\" violation; two or more is\n+    \"ambiguous\". Records that are never cited produce nothing.\n     \"\"\"\n     root = Path(root)\n     index = _adr_index(root)\n@@ -110,13 +126,15 @@\n         rel = path.relative_to(root).as_posix()\n         for lineno, line in enumerate(text.splitlines(), start=1):\n             for match in _CITATION.finditer(line):\n-                number = match.group(1)\n-                found = index.get(number, [])\n+                namespace, number = match.groups()\n+                found = index.get((namespace, number), [])\n                 if len(found) == 1:\n                     continue\n                 reason = \"missing\" if not found else \"ambiguous\"\n                 rels = [p.relative_to(root).as_posix() for p in found]\n-                violations.append(Violation(rel, number, lineno, reason, rels))\n+                violations.append(\n+                    Violation(rel, namespace, number, lineno, reason, rels)\n+                )\n     return violations\n \n \n@@ -129,8 +147,8 @@\n         self.assertEqual(\n             violations,\n             [],\n-            \"Every 'ADR ####' citation must resolve to exactly one \"\n-            f\"docs/adr/####-*.md:\\n{detail}\",\n+            \"Every ADR/SADR citation must resolve exactly once in its own \"\n+            f\"docs/adr namespace:\\n{detail}\",\n         )\n \n     def test_scan_actually_reads_scripts(self) -> None:\n@@ -146,8 +164,21 @@\n         # The two records this Goal carried onto the default branch, and which\n         # the scripts cite most, must be present.\n         index = _adr_index(REPO_ROOT)\n-        self.assertIn(\"0016\", index)\n-        self.assertIn(\"0017\", index)\n+        self.assertIn((\"SADR\", \"0016\"), index)\n+        self.assertIn((\"SADR\", \"0017\"), index)\n+\n+    def test_platform_tree_has_only_sadr_0001_through_0019(self) -> None:\n+        adr_dir = REPO_ROOT / \"docs\" / \"adr\"\n+        sadrs = sorted(adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\"))\n+        self.assertEqual(\n+            {path.name.split(\"-\", 2)[1] for path in sadrs},\n+            {f\"{number:04d}\" for number in range(1, 20)},\n+        )\n+        self.assertEqual(\n+            list(adr_dir.glob(\"[0-9][0-9][0-9][0-9]-*.md\")),\n+            [],\n+            \"the Shiki platform tree must not retain legacy numeric SADR paths\",\n+        )\n \n \n class FixtureAdrReferences(unittest.TestCase):\n@@ -180,6 +211,26 @@\n         self.assertIn(\"9991\", message)  # cited number\n         self.assertIn(\"docs/adr/9991-*.md\", message)  # missing path\n \n+    def test_target_adr_does_not_satisfy_missing_platform_sadr(self) -> None:\n+        root = self._make_repo(\n+            adr_names=[\"0042-target-decision.md\"],\n+            script_body='\"\"\"Target ADR 0042 exists, but platform SADR-0042 does not.\"\"\"\\n',\n+        )\n+        violations = find_adr_reference_violations(root, include_docs=False)\n+        self.assertEqual(len(violations), 1)\n+        self.assertEqual(violations[0].namespace, \"SADR\")\n+        self.assertEqual(violations[0].number, \"0042\")\n+        self.assertEqual(violations[0].expected_path, \"docs/adr/SADR-0042-*.md\")\n+\n+    def test_target_adr_and_platform_sadr_may_reuse_a_number(self) -> None:\n+        root = self._make_repo(\n+            adr_names=[\"0042-target-decision.md\", \"SADR-0042-platform-decision.md\"],\n+            script_body='\"\"\"Target ADR 0042 and platform SADR-0042 are distinct.\"\"\"\\n',\n+        )\n+        self.assertEqual(\n+            find_adr_reference_violations(root, include_docs=False), []\n+        )\n+\n     def test_uncited_adr_is_allowed(self) -> None:\n         # An ADR exists but nothing cites it -> not a violation. The converse\n         # (every ADR must be cited) is explicitly NOT required.\ndiff --git a/tests/test_policy_job_merged_prs.py b/tests/test_policy_job_merged_prs.py\nindex d45512e74681e5e649279236c7d4e79d5c3da471..b97989f8bd74d091de1a6b5275a20d8dc68c4512\n--- a/tests/test_policy_job_merged_prs.py\n+++ b/tests/test_policy_job_merged_prs.py\n@@ -3,8 +3,8 @@\n \n ``.github/workflows/shiki-cca-completion.yml`` has two consumers of the merged-PR\n proof: the CCA *signal* (``guardian_approval_signal.py`` in \"Prepare CCA context\")\n-and the authoritative gate (``mergegate_check.py`` in \"Enforce MergeGate\"). ADR\n-0017's bookkeeping-closeout exemption (condition 6) needs the merged implementation\n+and the authoritative gate (``mergegate_check.py`` in \"Enforce MergeGate\").\n+SADR-0017's bookkeeping-closeout exemption (condition 6) needs the merged implementation\n PR proven, and a closeout carries NO ``post_merge_reconcile`` marker. The signal\n already resolves ``MERGED_PRS`` unconditionally, but \"Enforce MergeGate\" used to\n resolve it only inside ``grep -q '<!-- shiki:post_merge_reconcile -->'``. So a\ndiff --git a/tests/test_sadr_install_migration.py b/tests/test_sadr_install_migration.py\nnew file mode 100644\nindex 0000000000000000000000000000000000000000..5a442fa5e4968ade178dcdedc3c63b54dba580a2\n--- /dev/null\n+++ b/tests/test_sadr_install_migration.py\n@@ -0,0 +1,224 @@\n+\"\"\"SADR installation and collision-safe legacy upgrade behavior.\"\"\"\n+\n+from __future__ import annotations\n+\n+import contextlib\n+import hashlib\n+import io\n+import json\n+import shutil\n+import tempfile\n+import unittest\n+from pathlib import Path\n+\n+import shiki_test_support  # noqa: F401  (path bootstrap)\n+\n+import shiki_installer\n+from tests.test_adr_references import find_adr_reference_violations\n+\n+\n+PRODUCT_NUMBERS = tuple(range(13, 19))\n+\n+\n+def _quiet_install(target: Path, *, force: bool, validate: bool) -> None:\n+    with contextlib.redirect_stdout(io.StringIO()):\n+        shiki_installer.install_template(target, force=force, validate=validate)\n+\n+\n+def _sha256(path: Path) -> str:\n+    return hashlib.sha256(path.read_bytes()).hexdigest()\n+\n+\n+def _legacy_sadr_paths() -> tuple[str, ...]:\n+    return tuple(\n+        f\"docs/adr/{path.name.removeprefix('SADR-')}\"\n+        for path in sorted((shiki_installer.ROOT / \"docs\" / \"adr\").glob(\"SADR-*.md\"))\n+        if path.name != \"SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\"\n+    )\n+\n+\n+def _create_legacy_upgrade_fixture(\n+    target: Path,\n+) -> tuple[dict[str, bytes], tuple[str, ...]]:\n+    adr_dir = target / \"docs\" / \"adr\"\n+    adr_dir.mkdir(parents=True)\n+    product_bytes = {\n+        f\"{number:04d}-cubeshare-product-decision.md\": (\n+            f\"# CubeShare ADR {number:04d}\\n\\nDo not rewrite {number}.\\n\"\n+        ).encode()\n+        for number in PRODUCT_NUMBERS\n+    }\n+    for name, content in product_bytes.items():\n+        (adr_dir / name).write_bytes(content)\n+\n+    legacy_paths = _legacy_sadr_paths()\n+    for relative in legacy_paths:\n+        (target / relative).write_bytes(f\"legacy Shiki bytes for {relative}\\n\".encode())\n+\n+    stamp_path = target / shiki_installer.INSTALL_STAMP_PATH\n+    stamp_path.parent.mkdir(parents=True, exist_ok=True)\n+    stamp = {\n+        \"version\": shiki_installer.INSTALL_STAMP_VERSION,\n+        \"platform_commit\": None,\n+        \"installed_at\": \"2000-01-01T00:00:00+00:00\",\n+        \"digests\": {\n+            relative: _sha256(target / relative) for relative in legacy_paths\n+        },\n+    }\n+    stamp_path.write_text(\n+        json.dumps(stamp, indent=2, sort_keys=True) + \"\\n\", encoding=\"utf-8\"\n+    )\n+    return product_bytes, legacy_paths\n+\n+\n+def _tree_snapshot(root: Path) -> dict[str, tuple[str, bytes | str]]:\n+    \"\"\"Recursive byte/shape snapshot used to prove preflight is write-free.\"\"\"\n+    snapshot: dict[str, tuple[str, bytes | str]] = {}\n+    for path in sorted(root.rglob(\"*\")):\n+        relative = path.relative_to(root).as_posix()\n+        if path.is_symlink():\n+            snapshot[relative] = (\"symlink\", path.readlink().as_posix())\n+        elif path.is_dir():\n+            snapshot[relative] = (\"directory\", \"\")\n+        else:\n+            snapshot[relative] = (\"file\", path.read_bytes())\n+    return snapshot\n+\n+\n+class FreshSadrInstallTests(unittest.TestCase):\n+    def setUp(self) -> None:\n+        self.tmp = Path(tempfile.mkdtemp(prefix=\"shiki-sadr-fresh-\"))\n+        self.addCleanup(lambda: shutil.rmtree(self.tmp, ignore_errors=True))\n+        self.target = self.tmp / \"target\"\n+        self.adr_dir = self.target / \"docs\" / \"adr\"\n+        self.adr_dir.mkdir(parents=True)\n+        self.product_bytes = {\n+            f\"{number:04d}-product-decision.md\": (\n+                f\"# Product ADR {number:04d}\\n\\nTarget-owned bytes {number}.\\n\"\n+            ).encode()\n+            for number in PRODUCT_NUMBERS\n+        }\n+        for name, content in self.product_bytes.items():\n+            (self.adr_dir / name).write_bytes(content)\n+\n+    def test_fresh_install_preserves_product_adrs_and_installs_all_sadrs(self) -> None:\n+        _quiet_install(self.target, force=False, validate=True)\n+\n+        for name, expected in self.product_bytes.items():\n+            self.assertEqual((self.adr_dir / name).read_bytes(), expected)\n+\n+        sadr_numbers = {\n+            path.name.split(\"-\", 2)[1]\n+            for path in self.adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\")\n+        }\n+        self.assertEqual(sadr_numbers, {f\"{number:04d}\" for number in range(1, 20)})\n+\n+        numeric_records = {\n+            path.name\n+            for path in self.adr_dir.glob(\"[0-9][0-9][0-9][0-9]-*.md\")\n+        }\n+        self.assertEqual(numeric_records, set(self.product_bytes))\n+        self.assertEqual(find_adr_reference_violations(self.target), [])\n+\n+\n+class LegacySadrUpgradeTests(unittest.TestCase):\n+    def setUp(self) -> None:\n+        self.tmp = Path(tempfile.mkdtemp(prefix=\"shiki-sadr-upgrade-\"))\n+        self.addCleanup(lambda: shutil.rmtree(self.tmp, ignore_errors=True))\n+        self.target = self.tmp / \"target\"\n+        self.adr_dir = self.target / \"docs\" / \"adr\"\n+        self.product_bytes, self.legacy_paths = _create_legacy_upgrade_fixture(\n+            self.target\n+        )\n+\n+    def test_force_upgrade_deletes_only_stamped_legacy_sadrs(self) -> None:\n+        _quiet_install(self.target, force=True, validate=True)\n+\n+        for relative in self.legacy_paths:\n+            self.assertFalse((self.target / relative).exists(), relative)\n+        for name, expected in self.product_bytes.items():\n+            self.assertEqual((self.adr_dir / name).read_bytes(), expected)\n+\n+        sadrs = list(self.adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\"))\n+        self.assertEqual(len(sadrs), 19)\n+        refreshed = json.loads(\n+            (self.target / shiki_installer.INSTALL_STAMP_PATH).read_text(encoding=\"utf-8\")\n+        )\n+        for relative in self.legacy_paths:\n+            self.assertNotIn(relative, refreshed[\"digests\"])\n+        for path in sadrs:\n+            relative = path.relative_to(self.target).as_posix()\n+            self.assertEqual(refreshed[\"digests\"][relative], _sha256(path))\n+\n+    def test_non_force_install_never_deletes_legacy_files(self) -> None:\n+        before = {\n+            relative: (self.target / relative).read_bytes()\n+            for relative in self.legacy_paths\n+        }\n+        _quiet_install(self.target, force=False, validate=False)\n+        for relative, expected in before.items():\n+            self.assertEqual((self.target / relative).read_bytes(), expected)\n+\n+\n+class LegacyCleanupPreflightTests(unittest.TestCase):\n+    def setUp(self) -> None:\n+        self.tmp = Path(tempfile.mkdtemp(prefix=\"shiki-sadr-preflight-\"))\n+        self.addCleanup(lambda: shutil.rmtree(self.tmp, ignore_errors=True))\n+\n+    def test_all_ownership_blockers_abort_before_any_write(self) -> None:\n+        cases = (\n+            (\"absent\", \"install stamp is absent\"),\n+            (\"unreadable\", \"install stamp is unreadable\"),\n+            (\"malformed\", \"install stamp is malformed\"),\n+            (\"missing-digest\", \"install stamp has no digest\"),\n+            (\"digest-mismatch\", \"digest mismatch\"),\n+        )\n+        for name, expected_reason in cases:\n+            with self.subTest(name=name):\n+                target = self.tmp / name\n+                _, legacy_paths = _create_legacy_upgrade_fixture(target)\n+                stamp_path = target / shiki_installer.INSTALL_STAMP_PATH\n+\n+                if name == \"absent\":\n+                    stamp_path.unlink()\n+                elif name == \"unreadable\":\n+                    stamp_path.unlink()\n+                    stamp_path.mkdir()\n+                elif name == \"malformed\":\n+                    stamp_path.write_text(\"{not-json\\n\", encoding=\"utf-8\")\n+                else:\n+                    stamp = json.loads(stamp_path.read_text(encoding=\"utf-8\"))\n+                    if name == \"missing-digest\":\n+                        stamp[\"digests\"] = {}\n+                    else:\n+                        stamp[\"digests\"] = {\n+                            relative: \"deadbeef\" for relative in legacy_paths\n+                        }\n+                    stamp_path.write_text(\n+                        json.dumps(stamp, indent=2, sort_keys=True) + \"\\n\",\n+                        encoding=\"utf-8\",\n+                    )\n+\n+                before = _tree_snapshot(target)\n+                with self.assertRaises(shiki_installer.ShikiError) as raised:\n+                    _quiet_install(target, force=True, validate=False)\n+                after = _tree_snapshot(target)\n+\n+                self.assertEqual(before, after, f\"{name} preflight changed the tree\")\n+                message = str(raised.exception)\n+                self.assertIn(expected_reason, message)\n+                self.assertIn(\"No files were written\", message)\n+                for relative in legacy_paths:\n+                    self.assertIn(relative, message)\n+\n+    def test_absent_stamp_is_allowed_when_no_legacy_path_exists(self) -> None:\n+        target = self.tmp / \"no-legacy\"\n+        target.mkdir()\n+        _quiet_install(target, force=True, validate=False)\n+        self.assertTrue(\n+            (target / \"docs\" / \"adr\" / \"SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\").is_file()\n+        )\n+\n+\n+if __name__ == \"__main__\":\n+    unittest.main()\n\ndiff --git a/.codex/skills/shiki/SKILL.md b/.codex/skills/shiki/SKILL.md\nindex c8e2e85d9d75d49cc4f2d6286ee22659c4e53e75..93cded623b2c15d9f65c76491a1c2d813ac9e6c0\n--- a/.codex/skills/shiki/SKILL.md\n+++ b/.codex/skills/shiki/SKILL.md\n@@ -18,6 +18,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n Codex CLI does not register this skill as a custom `/shiki` slash command. When\n the user types `/shiki` in Codex CLI and gets \"Unrecognized command\", explain\n that this is expected: invoke Shiki in Codex with natural language such as\n@@ -69,7 +72,7 @@\n \n ## Responsibilities\n \n-- Claude Code implements and repairs by default (`shiki runner claude`, ADR 0008); Codex implements and repairs tasks explicitly assigned to `codex`.\n+- Claude Code implements and repairs by default (`shiki runner claude`, SADR-0008); Codex implements and repairs tasks explicitly assigned to `codex`.\n - Claude Code Action can act as GitHub-side CCA or reviewer.\n - CCA judges completion.\n - MergeGate authorizes state transitions and merge readiness.\ndiff --git a/.github/prompts/cca-completion-check.md b/.github/prompts/cca-completion-check.md\nindex ed203ad5f302ecc81372d8ab27d2b1232ca97497..fd89950528333ef6242e6d5443904f5392ab7e56\n--- a/.github/prompts/cca-completion-check.md\n+++ b/.github/prompts/cca-completion-check.md\n@@ -19,6 +19,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n ## Required Reading\n \n Read only the files needed to judge the PR contract. Prefer this order:\ndiff --git a/AGENTS.md b/AGENTS.md\nindex 7f041100d49c2a7525f51cb0f5d696a4a6d1c78c..2b700e989ff295987aa8a4b0c5fcb1acd80020da\n--- a/AGENTS.md\n+++ b/AGENTS.md\n@@ -44,6 +44,10 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use\n+`SADR-NNNN-*.md`; target product decisions retain `NNNN-*.md` and the `ADR`\n+namespace.\n+\n Use the canonical source-of-truth order above. Runtime-specific reading orders\n are operational guidance only and must not override or reorder it.\n \ndiff --git a/CLAUDE.md b/CLAUDE.md\nindex 98a932e7841a632866fcd53b0bad0e3af3711d3d..611d491912d6a72a0327a1731b8f33a41acb4baa\n--- a/CLAUDE.md\n+++ b/CLAUDE.md\n@@ -14,6 +14,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n ## Claude Role\n \n Default to planner, default implementer, reviewer, coordinator, documentation editor, CCA-style completion judge, and final-judgment assistant.\ndiff --git a/SYSTEM_PROMPT.md b/SYSTEM_PROMPT.md\nindex aaaea45e3a896c3a44ba89eec8bb84325ecf6c62..2e7c093ebe9e8bb34e0923756f191b4f7fa581f5\n--- a/SYSTEM_PROMPT.md\n+++ b/SYSTEM_PROMPT.md\n@@ -30,6 +30,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n Use the canonical source-of-truth order above. Runtime-specific reading orders\n are operational guidance only and must not override or reorder it. The current\n conversation is non-durable operator input.\ndiff --git a/docs/adr/0001-keep-shiki-as-platform-name.md b/docs/adr/SADR-0001-keep-shiki-as-platform-name.md\nindex 20a9637c3b9d8c97de7ba8db7cfae4d82d9a7dba..8b685439959aada1b221c112f0ac3c51ca8a1695\n--- a/docs/adr/0001-keep-shiki-as-platform-name.md\n+++ b/docs/adr/SADR-0001-keep-shiki-as-platform-name.md\n@@ -1,3 +1,3 @@\n-# Keep Shiki As The Platform Name\n+# SADR-0001: Keep Shiki As The Platform Name\n \n Shiki remains the product name for the GitHub-native agentic engineering control plane. We considered more descriptive names such as MergePilot, MergeGate, and Kiroku, but kept Shiki because it carries the existing goal-seeking framework identity while remaining independent of Hermes, Codex, Claude Code, or any single runtime.\ndiff --git a/docs/adr/0002-use-agents-as-shared-constitution.md b/docs/adr/SADR-0002-use-agents-as-shared-constitution.md\nindex 73de30ecb078120eac540d9bc51855e3cd48d0e3..c5486182bf06fca36ea508496b013c67302fd989\n--- a/docs/adr/0002-use-agents-as-shared-constitution.md\n+++ b/docs/adr/SADR-0002-use-agents-as-shared-constitution.md\n@@ -1,3 +1,3 @@\n-# Use AGENTS As The Shared Agent Constitution\n+# SADR-0002: Use AGENTS As The Shared Agent Constitution\n \n Shiki uses `AGENTS.md` as the runtime-neutral constitution and `CLAUDE.md` as a thin Claude Code wrapper. This keeps Codex, Claude Code, Hermes Runner, and future Agent Runtimes aligned on the same GitHub-first MergeGate rules while still allowing runtime-specific instructions where needed.\ndiff --git a/docs/adr/0003-require-skill-gate-for-mergegate-readiness.md b/docs/adr/SADR-0003-require-skill-gate-for-mergegate-readiness.md\nindex 424be0c9af4c8f0136f1fde68a468c47ff84e764..7fc337987c7df9792f75f181dfcbed75fe5ffff2\n--- a/docs/adr/0003-require-skill-gate-for-mergegate-readiness.md\n+++ b/docs/adr/SADR-0003-require-skill-gate-for-mergegate-readiness.md\n@@ -1,3 +1,3 @@\n-# Require Skill Gate For MergeGate Readiness\n+# SADR-0003: Require Skill Gate For MergeGate Readiness\n \n Shiki requires relevant engineering skills to be invoked before execution when their triggers apply. This makes skills part of MergeGate readiness rather than optional prompt style, because Shiki depends on repeatable planning, issue decomposition, TDD, diagnosis, triage, and architecture review before autonomous or parallel agent work can be trusted.\ndiff --git a/docs/adr/0004-use-github-first-with-shiki-mirror.md b/docs/adr/SADR-0004-use-github-first-with-shiki-mirror.md\nindex d0a3a9174dc646a945a9d325954ffaf00c352040..a79c54ae9ee8eed2a0524192b864da4e22a92d3f\n--- a/docs/adr/0004-use-github-first-with-shiki-mirror.md\n+++ b/docs/adr/SADR-0004-use-github-first-with-shiki-mirror.md\n@@ -1,4 +1,4 @@\n-# ADR 0004: Use GitHub First With A Repository-Local Shiki Mirror\n+# SADR-0004: Use GitHub First With A Repository-Local Shiki Mirror\n \n ## Status\n \ndiff --git a/docs/adr/0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md b/docs/adr/SADR-0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\nindex b339baf49b89d9290e79c3d51b36e7b2bde3bd95..c30a0d8b1fde0224d0ac7820e7870dccf721580d\n--- a/docs/adr/0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\n+++ b/docs/adr/SADR-0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\n@@ -1,4 +1,4 @@\n-# ADR 0005: Use Codex As Front And Claude Code Action As The GitHub Runtime\n+# SADR-0005: Use Codex As Front And Claude Code Action As The GitHub Runtime\n \n ## Status\n \ndiff --git a/docs/adr/0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md b/docs/adr/SADR-0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\nindex 4bcae9d1e52f0a37b8e9fbafa5cd14b6ee1993bd..7a38538a6ae9929567f0ade67e7ea6a60d0b3f7e\n--- a/docs/adr/0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\n+++ b/docs/adr/SADR-0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\n@@ -1,4 +1,4 @@\n-# ADR 0006: Separate Codex Implementation, CCA Completion, And MergeGate Authorization\n+# SADR-0006: Separate Codex Implementation, CCA Completion, And MergeGate Authorization\n \n ## Status\n \ndiff --git a/docs/adr/0007-packaging-and-release.md b/docs/adr/SADR-0007-packaging-and-release.md\nindex 79b84ddb97167372b44641e5b7860e400ce80822..bb445a2e2e856d9d1f5fdbe35de8d4e8cec0fb32\n--- a/docs/adr/0007-packaging-and-release.md\n+++ b/docs/adr/SADR-0007-packaging-and-release.md\n@@ -1,4 +1,4 @@\n-# ADR 0007: Packaging And Release For Shiki\n+# SADR-0007: Packaging And Release For Shiki\n \n ## Status\n \n@@ -25,7 +25,7 @@\n   not introduce third-party runtime dependencies or break the shim.\n - Releases must run inside the operator's subscription-authenticated toolchain\n   and must not require extra secrets beyond the built-in `GITHUB_TOKEN`\n-  (consistent with ADR 0005's no-API-key-by-default stance).\n+  (consistent with SADR-0005's no-API-key-by-default stance).\n - `.shiki/manifest.json` already encodes the install surface (`install.include`,\n   `install.create_directories`, `install.exclude_from_commit`), and it must not\n   be mutated as part of this change.\n@@ -34,7 +34,7 @@\n \n This is a hard-to-reverse platform decision (it sets the version source of\n truth, the release trigger, and the public install contract), so it is recorded\n-as an ADR.\n+as a SADR.\n \n ## Decision\n \n@@ -104,7 +104,7 @@\n 3. **Publishing to PyPI.** Rejected for now: Shiki is distributed as a\n    repository template + git-installable CLI, not a PyPI library; PyPI would add\n    credentials/secrets and a different versioning contract. Can be added later as\n-   its own ADR if a packaged distribution is needed.\n+   its own SADR if a packaged distribution is needed.\n 4. **Dynamic version (read `VERSION` via a build backend `dynamic` field).**\n    Rejected to keep `pyproject.toml` backend-agnostic and dependency-free; the\n    small duplication between `VERSION` and `pyproject.toml` is guarded by the\n@@ -123,4 +123,4 @@\n   releasing versions the whole platform but ships only `install.include` to\n   targets.\n - Future PyPI distribution or a console entry point are deferred and would each\n-  warrant their own ADR.\n+  warrant their own SADR.\ndiff --git a/docs/adr/0008-use-claude-code-as-default-implementer.md b/docs/adr/SADR-0008-use-claude-code-as-default-implementer.md\nindex dedf4510b7c7799521422f622029563d373db031..35c2625d908167fbc58855b5b63e283805f167e8\n--- a/docs/adr/0008-use-claude-code-as-default-implementer.md\n+++ b/docs/adr/SADR-0008-use-claude-code-as-default-implementer.md\n@@ -1,12 +1,12 @@\n-# ADR 0008: Use Claude Code As The Default Implementer Runtime\n+# SADR-0008: Use Claude Code As The Default Implementer Runtime\n \n ## Status\n \n-Proposed (supersedes ADR 0005 for the implementer default; keeps its auth model)\n+Proposed (supersedes SADR-0005 for the implementer default; keeps its auth model)\n \n ## Context\n \n-ADR 0005 made Codex Front the default implementation runtime, with Claude Code\n+SADR-0005 made Codex Front the default implementation runtime, with Claude Code\n restricted to planner and reviewer roles by the runtime registry, and the only\n autonomous implementation adapter was `shiki runner codex`.\n \n@@ -18,13 +18,13 @@\n - The platform's planning, review, and CCA layers already run on Claude Code,\n   and Claude Code's native capabilities (the built-in `/code-review` skill and\n   the Workflow multi-agent orchestration tool) have no Codex equivalent.\n-- The Goal lifecycle redesign (ADR 0009) requires an autonomous post-freeze\n+- The Goal lifecycle redesign (SADR-0009) requires an autonomous post-freeze\n   loop driven from the operator's Claude Code session, including bounded\n   repair dispatch, which must not depend on a runtime that cannot be invoked\n   unattended.\n \n The constitution claims Shiki is runtime-agnostic (\"not a Claude-only\n-workflow\", ADR 0002). Any change to the implementer default must preserve the\n+workflow\", SADR-0002). Any change to the implementer default must preserve the\n registry-based runtime model rather than hardcoding one vendor.\n \n ## Decision\n@@ -39,7 +39,7 @@\n   the task to review or repair-needed.\n - Keep Codex registered as an optional implementer runtime. `shiki runner\n   codex`, the Codex handoff templates, and the ChatGPT OAuth auth model from\n-  ADR 0005 remain supported for tasks explicitly assigned to `codex`.\n+  SADR-0005 remain supported for tasks explicitly assigned to `codex`.\n - Commit to mandating two Claude-native capabilities as loop contract in\n   follow-up Goals (status: the code-review gate was delivered by Goal G-B \u2014\n   skill registry, checklists item PR-12, default required_skills \u2014 while the\n@@ -60,7 +60,7 @@\n \n Out of scope: removing Codex support, changing the CCA runtime (Claude Code\n Action with `CLAUDE_CODE_OAUTH_TOKEN` stays), and API-key based automation\n-(still requires its own ADR per ADR 0005).\n+(still requires its own target ADR per SADR-0005).\n \n ## Consequences\n \n@@ -100,7 +100,7 @@\n   harness rejection of direct `codex exec` is outside Shiki's control, and the\n   plan's mandatory native capabilities cannot run on Codex at all.\n - **Remove Codex entirely**: rejected; it abandons the runtime-agnostic\n-  constitution (ADR 0002) for no operational gain and forecloses\n+  constitution (SADR-0002) for no operational gain and forecloses\n   cross-runtime adversarial review.\n - **GitHub Actions cloud implementation via claude-code-action**: rejected as\n   default; durable evidence is automatic but token cost, Node 24 workflow\ndiff --git a/docs/adr/0009-requirements-definition-and-spec-freeze.md b/docs/adr/SADR-0009-requirements-definition-and-spec-freeze.md\nindex 29b3f7871bef9203a8b4a831a65c39eb2f506e42..52e7dd69ad0aa4efe0b68c62678abbf7df02aaec\n--- a/docs/adr/0009-requirements-definition-and-spec-freeze.md\n+++ b/docs/adr/SADR-0009-requirements-definition-and-spec-freeze.md\n@@ -1,4 +1,4 @@\n-# ADR 0009: Merge Grill And Planning Into Requirements Definition With Spec Freeze\n+# SADR-0009: Merge Grill And Planning Into Requirements Definition With Spec Freeze\n \n ## Status\n \ndiff --git a/docs/adr/0010-external-ai-guardian-review.md b/docs/adr/SADR-0010-external-ai-guardian-review.md\nindex e6108bbe8bb4e73e0ff4684c8f78001a80438c4e..f3510b145664298adafb151b6f0db51b972f01ec\n--- a/docs/adr/0010-external-ai-guardian-review.md\n+++ b/docs/adr/SADR-0010-external-ai-guardian-review.md\n@@ -1,4 +1,4 @@\n-# ADR 0010: External AI Guardian Review As A First-Class Approval Authority\n+# SADR-0010: External AI Guardian Review As A First-Class Approval Authority\n \n ## Status\n \n@@ -8,7 +8,7 @@\n \n Shiki is intended to be an autonomous, recursive, self-improving agent\n framework: authority-in-the-loop, not human-in-the-loop. Until now the Guardian\n-approval required for high/critical-risk merges (guardian-policy.json, ADR\n+approval required for high/critical-risk merges (guardian-policy.json, SADR\n governance) recognized only human authority sources \u2014 a GitHub review approval,\n the `guardian:approved` label applied by a configured Guardian, or a comment\n carrying `Guardian approval granted` plus the current head SHA.\n@@ -89,7 +89,7 @@\n   autonomous design the operator intends and produced the impersonation\n   incident.\n - **Record AI approval as an operator comment**: rejected as forgery of human\n-  authority \u2014 the exact failure this ADR corrects.\n+  authority \u2014 the exact failure this SADR corrects.\n - **Deliver the artifact as a committed `.shiki/` file**: rejected; a committed\n   approval inside the PR being approved is forgeable by anyone who can push.\n - **A dedicated reviewer bot identity instead of a relayed comment**: deferred;\ndiff --git a/docs/adr/0011-autonomous-quality-gate-evidence.md b/docs/adr/SADR-0011-autonomous-quality-gate-evidence.md\nindex bca0246c5d46e44afbde15486a75eec425fc02f5..9c93eac16d8c3a6d4b11bd7224e3955102cd7db4\n--- a/docs/adr/0011-autonomous-quality-gate-evidence.md\n+++ b/docs/adr/SADR-0011-autonomous-quality-gate-evidence.md\n@@ -1,4 +1,4 @@\n-# ADR 0011: Autonomous Quality-Gate Evidence \u2014 Deterministic Facts And An Independent Verifier\n+# SADR-0011: Autonomous Quality-Gate Evidence \u2014 Deterministic Facts And An Independent Verifier\n \n ## Status\n \ndiff --git a/docs/adr/0012-autonomous-loop-closeout-to-main.md b/docs/adr/SADR-0012-autonomous-loop-closeout-to-main.md\nindex 637fad6dde4989c9c2bf0f81f676d8d29991123c..ec7d85f81030a676bcbba28b43eb365422a36eac\n--- a/docs/adr/0012-autonomous-loop-closeout-to-main.md\n+++ b/docs/adr/SADR-0012-autonomous-loop-closeout-to-main.md\n@@ -1,4 +1,4 @@\n-# 0012 \u2014 Autonomous loop pushes goal completion to main via a closeout PR\n+# SADR-0012 \u2014 Autonomous loop pushes goal completion to main via a closeout PR\n \n - Status: accepted\n - Date: 2026-06-18\ndiff --git a/docs/adr/0013-bootstrap-grants-actions-approve-pull-requests.md b/docs/adr/SADR-0013-bootstrap-grants-actions-approve-pull-requests.md\nindex b6c69d64f1c5ced346b88d6859586715b3b5bd0e..cf48ff4a6716267f539ca58edd1a852e7c7a2d64\n--- a/docs/adr/0013-bootstrap-grants-actions-approve-pull-requests.md\n+++ b/docs/adr/SADR-0013-bootstrap-grants-actions-approve-pull-requests.md\n@@ -1,4 +1,4 @@\n-# ADR 0013: Bootstrap Grants GitHub Actions Permission To Create And Approve Pull Requests\n+# SADR-0013: Bootstrap Grants GitHub Actions Permission To Create And Approve Pull Requests\n \n ## Status\n \n@@ -31,7 +31,7 @@\n security tradeoff: it lets repository automation satisfy a human-review gate. In\n a multi-maintainer repo that could let a compromised or buggy workflow\n self-approve changes. So whether bootstrap turns it on by default \u2014 and with\n-what default token scope \u2014 is an ADR-worthy decision, not an implementation\n+what default token scope \u2014 is a SADR-worthy decision, not an implementation\n detail.\n \n ## Decision\n@@ -48,7 +48,7 @@\n Specifically:\n \n - **`can_approve_pull_request_reviews` defaults to `true`.** Shiki's default\n-  posture is solo/AFK autonomous operation (ADR 0008/0009/0012), where the CCA\n+  posture is solo/AFK autonomous operation (SADR-0008/0009/0012), where the CCA\n   Review Bridge is the only path that can satisfy `required_review: true` after\n   the CCA and Guardian gates have already passed. The default makes a\n   freshly-bootstrapped repo able to run the autonomous loop end to end without a\ndiff --git a/docs/adr/0014-codex-app-external-ai-guardian-ui-adapter.md b/docs/adr/SADR-0014-codex-app-external-ai-guardian-ui-adapter.md\nindex 238db06279334caa341c117362df9271382a7fcd..65f12e9939321e1b3e389a34c822fc1549e5155d\n--- a/docs/adr/0014-codex-app-external-ai-guardian-ui-adapter.md\n+++ b/docs/adr/SADR-0014-codex-app-external-ai-guardian-ui-adapter.md\n@@ -1,4 +1,4 @@\n-# ADR 0014: Use Codex App As The External AI Guardian UI Adapter\n+# SADR-0014: Use Codex App As The External AI Guardian UI Adapter\n \n ## Status\n \ndiff --git a/docs/adr/0015-contract-approval-before-dispatch.md b/docs/adr/SADR-0015-contract-approval-before-dispatch.md\nindex fad6c656741a20a7684ba16080a7db159f63a52f..6061271a46d3f1d6ae7174477de3fb75673123a6\n--- a/docs/adr/0015-contract-approval-before-dispatch.md\n+++ b/docs/adr/SADR-0015-contract-approval-before-dispatch.md\n@@ -1,4 +1,4 @@\n-# ADR 0015: Approve The Task Contract Before Dispatch, Not The Diff After It\n+# SADR-0015: Approve The Task Contract Before Dispatch, Not The Diff After It\n \n ## Status\n \n@@ -9,7 +9,7 @@\n Guardian approval for high/critical risk is expressible only on a pull request.\n Every approval source in `guardian-policy.json` \u2014 GitHub review, the\n `guardian:approved` label, the `Guardian approval granted` comment, and the\n-`external_ai_guardian_review` artifact (ADR 0010/0014) \u2014 is bound to a PR, and\n+`external_ai_guardian_review` artifact (SADR-0010/0014) \u2014 is bound to a PR, and\n three of the four require the current head SHA. Approval therefore cannot exist\n before an implementation PR exists.\n \n@@ -81,7 +81,7 @@\n An envelope breach must surface only on the non-repairable policy gate, never on\n the repairable `MergeGate metadata check`. Otherwise the Goal Loop routes it to\n `dispatch_repair` \u2014 instructing a runner to make the breach go away \u2014 which is\n-exactly what this ADR forbids.\n+exactly what this SADR forbids.\n \n **Contract immutability.** After registration, an implementation PR must not\n change the governance fields of its own task contract (`scope`, `non_goals`,\n@@ -116,7 +116,7 @@\n fails any verdict that is not `complete`, and the signal the CCA prompt reads is\n computed from `evaluate_guardian_approval` alone. Without the OR-merge at the\n signal, a contract-approved PR still returns `needs_guardian`, the CCA job fails,\n-and the MergeGate policy check never runs \u2014 the gate this ADR changes would never\n+and the MergeGate policy check never runs \u2014 the gate this SADR changes would never\n be reached. The CCA *prompt* needs no change: a `contract_approval` entry in the\n signal's existing `sources` array satisfies its current wording.\n \n@@ -130,7 +130,7 @@\n written, and an approved contract lets its implementation PRs merge through the\n same automatic path as low/medium risk. The wasted implement-then-block cycle\n disappears. Guardian review shifts from reading diffs to reading contracts, which\n-is both earlier and smaller; the external AI Guardian path (ADR 0010/0014) works\n+is both earlier and smaller; the external AI Guardian path (SADR-0010/0014) works\n on contracts with no change.\n \n We accept that the Guardian authorizes a class of change without seeing its\n@@ -146,7 +146,7 @@\n adversarial tests for the new approval path equivalent in rigor to\n `test_shiki_governance_evidence.sh`.\n \n-Bootstrap costs, which apply only while this ADR is being implemented. The tasks\n+Bootstrap costs, which apply only while this SADR is being implemented. The tasks\n that build Contract Approval merge under the existing rules, so the ones\n dispatched after the pre-dispatch gate lands but before the approval path lands\n need a Guardian-approved Contract PR *and* a live Guardian approval on their\n@@ -210,7 +210,7 @@\n risk; they do not bound authorized work.\n \n **Auto-escalate risk on envelope breach instead of stopping.** Returns the\n-workflow to a diff-time Guardian decision \u2014 the exact behavior this ADR removes.\n+workflow to a diff-time Guardian decision \u2014 the exact behavior this SADR removes.\n \n **Resolve governance fields from the base snapshot only.** Rejected against the\n registration flow: implementation PRs add their own contract, so the base has no\ndiff --git a/docs/adr/0016-locks-bound-product-paths.md b/docs/adr/SADR-0016-locks-bound-product-paths.md\nindex b5e850f3f1f7f4ef6e7f9d1763a29a51bd300c45..46b40a9b99a7aae644c02bb458f5473ff0a90ce2\n--- a/docs/adr/0016-locks-bound-product-paths.md\n+++ b/docs/adr/SADR-0016-locks-bound-product-paths.md\n@@ -1,4 +1,4 @@\n-# ADR 0016: Locks bound product paths; the Shiki mirror is bound by record identity\n+# SADR-0016: Locks bound product paths; the Shiki mirror is bound by record identity\n \n ## Status\n \n@@ -6,8 +6,8 @@\n \n - Date: 2026-07-28\n - Deciders: operator (mizutani-140), Claude (planner)\n-- Related: ADR 0004 (GitHub-first with a `.shiki` mirror), ADR 0009 (Spec Freeze),\n-  ADR 0012 (autonomous loop closeout to main), ADR 0015 (Contract Approval before\n+- Related: SADR-0004 (GitHub-first with a `.shiki` mirror), SADR-0009 (Spec Freeze),\n+  SADR-0012 (autonomous loop closeout to main), SADR-0015 (Contract Approval before\n   dispatch), remediation goal task R-02, the live PR #179 deadlock.\n \n ## Context\n@@ -90,11 +90,11 @@\n files, documentation files and named source modules are nameable; runtime-\n allocated ledger, runner and report ids are not \u2014 and those live in `.shiki`,\n which locks no longer govern. Existing frozen contracts are brought into line by\n-a one-time Spec Amendment per plan, which must land before ADR 0015's contract\n+a one-time Spec Amendment per plan, which must land before SADR-0015's contract\n immutability makes `locks` unwritable from the implementation PR.\n \n **Lock state is resolved from the durable side.** MergeGate resolves the lock\n-registry, and ADR 0015's governance fields, from the base `.shiki` snapshot when\n+registry, and SADR-0015's governance fields, from the base `.shiki` snapshot when\n present, falling back to head only when absent. The `MergeGate metadata check`\n job is given `--base-shiki`, which it does not receive today. Locks are not\n derived from the live open-pull-request set: a pull request body is untrusted\n@@ -108,7 +108,7 @@\n Explicitly out of scope: moving MergeGate's contract resolution off the PR\n checkout wholesale; making the mirror a projection of GitHub state; introducing\n new lock kinds; moving ledger evidence out of the repository; and unwinding\n-ADR 0012's closeout pull request. Registration of multi-task goals is ADR 0015's\n+SADR-0012's closeout pull request. Registration of multi-task goals is SADR-0015's\n Contract PR and is not re-decided here.\n \n ## Consequences\n@@ -127,7 +127,7 @@\n \n Risks accepted. First, goal-scoped identity is only as strong as `goal_id`,\n which is resolved from the PR body and the head task file; the mitigation is to\n-add `goal_id` to ADR 0015's frozen governance field set, and until that lands a\n+add `goal_id` to SADR-0015's frozen governance field set, and until that lands a\n task file can name a foreign goal. Second, base-when-present resolution is\n unavailable on fork pull requests, where the head is contributor-controlled;\n fork pull requests already cannot complete a Shiki merge, and this decision does\n@@ -139,7 +139,7 @@\n path, not by implementation pull requests.\n \n New obligations: one Spec Amendment per frozen plan to rewrite `locks`, ordered\n-before ADR 0015 T1; extension of the adversarial suites in\n+before SADR-0015 T1; extension of the adversarial suites in\n `scripts/test_shiki_adversarial_state.sh` and\n `scripts/test_shiki_governance_evidence.sh` to cover the eleven newly-scoped\n directories; and updating `scripts/test_shiki_loop_lock_guard.sh`,\n@@ -151,32 +151,32 @@\n whether the loop's transitive sync of ledger `evidence` references can be\n deleted once the Contract PR carries its own source plan.\n \n-## Relationship to ADR 0004\n+## Relationship to SADR-0004\n \n-This ADR **clarifies something ADR 0004 left unspecified**. ADR 0004 decides that\n+This SADR **clarifies something SADR-0004 left unspecified**. SADR-0004 decides that\n GitHub is the operational source of truth and that each target repository keeps a\n `.shiki/` mirror. It says nothing about pull requests writing the mirror, does\n not name MergeGate, and does not make the mirror an input to a gate. Nothing in\n-ADR 0004's Decision or Consequences is amended, superseded or weakened: the\n+SADR-0004's Decision or Consequences is amended, superseded or weakened: the\n mirror still exists, still records the same artifacts, and pull request\n-boundaries remain the coordination surface. What this ADR settles is the question\n+boundaries remain the coordination surface. What this SADR settles is the question\n 0004 never reached \u2014 by what authority a pull request may write a mirror record \u2014\n and it answers it the way `.shiki/manifest.json` already declares\n (`pr_mutation: current-task-or-current-goal-only`).\n \n-It **amends ADR 0012**, whose Decision item 5 states that \"Loop-executed tasks\n+It **amends SADR-0012**, whose Decision item 5 states that \"Loop-executed tasks\n declare the `path:.shiki/**` lock, which covers every `.shiki` file the closeout\n stages ... so `files_outside_locks` is satisfied without per-file lock\n bookkeeping.\" That clause is replaced: the closeout's mirror writes are\n-authorized by record identity, not by a declared lock. ADR 0012's substantive\n+authorized by record identity, not by a declared lock. SADR-0012's substantive\n decision \u2014 that completion reaches `main` through a closeout pull request rather\n than local attestation \u2014 is preserved unchanged.\n \n-It is **complementary to ADR 0015**, not a substitute. ADR 0015 owns contract\n+It is **complementary to SADR-0015**, not a substitute. SADR-0015 owns contract\n immutability, the pre-dispatch gate, the Contract PR and the Scope Envelope. This\n-ADR owns what a lock means and who may write the mirror. The one ordering\n+SADR owns what a lock means and who may write the mirror. The one ordering\n constraint between them is that the lock-narrowing Spec Amendment must precede\n-ADR 0015 T1's freezing of the `locks` field.\n+SADR-0015 T1's freezing of the `locks` field.\n \n ## Alternatives Considered\n \n@@ -186,7 +186,7 @@\n the only writer that can register them may run. Migrating the Guardian signal to\n that store makes every registration PR fail closed to `needs_guardian` at every\n risk level, because the task file is by definition absent from the store, which\n-removes the low/medium autonomous path ADR 0008/0009 exists to provide. And it\n+removes the low/medium autonomous path SADR-0008/0009 exists to provide. And it\n leaves schemas, manifest, config, guardian policy, templates, migrations, plans,\n worktrees, runner records, runs and handoffs with no writer at all in a\n repository whose product is `.shiki`. Measured benefit on the frozen backlog:\n@@ -199,14 +199,14 @@\n namespace is not in fact ownership-partitioned \u2014 56 ledger entries carry no\n `task_id`, and one goal DAG is writable by 35 registered tasks. The correct\n expression of its insight is that locks do not govern the mirror at all, which is\n-what this ADR decides.\n+what this SADR decides.\n \n **Make the mirror a projection written only by a registration writer and a\n post-merge projection job.** Rejected. It requires a bypass actor with write\n access to the protected default branch; it makes its own master invariant\n readable from a manifest that pull requests may edit; it deletes both reconcile\n modes and leaves a corrupted mirror with no in-band repair path, contradicting\n-ADR 0004's consequence that mirror conflicts are repaired against GitHub state;\n+SADR-0004's consequence that mirror conflicts are repaired against GitHub state;\n and its content-derived ids are rejected by `validate_shiki`'s currently-green id\n pattern.\n \ndiff --git a/docs/adr/0017-bookkeeping-closeouts-do-not-inherit-risk.md b/docs/adr/SADR-0017-bookkeeping-closeouts-do-not-inherit-risk.md\nindex 554e3a0d526a72ca067108c5464cc68c3edcf174..b9676857a4713850e063af7c39c8e0b01ecb4c66\n--- a/docs/adr/0017-bookkeeping-closeouts-do-not-inherit-risk.md\n+++ b/docs/adr/SADR-0017-bookkeeping-closeouts-do-not-inherit-risk.md\n@@ -1,4 +1,4 @@\n-# ADR 0017: A Bookkeeping Closeout Does Not Inherit Its Task's Risk\n+# SADR-0017: A Bookkeeping Closeout Does Not Inherit Its Task's Risk\n \n ## Status\n \n@@ -6,7 +6,7 @@\n \n ## Context\n \n-ADR 0012 made the goal loop push a task's completion to the default branch through a\n+SADR-0012 made the goal loop push a task's completion to the default branch through a\n closeout pull request: after the implementation PR merges, the loop opens a second PR\n carrying `task.status = done`, `lock.state = released`, and \u2014 when the task completes\n its goal \u2014 `goal.status = complete` plus one scorecard report. Nothing else. The\n@@ -49,14 +49,14 @@\n 4. The task file's governance fields (`scope`, `non_goals`, `required_skills`,\n    `risk_level`, `locks`, `acceptance_checks`, `test_command`) are byte-identical to\n    the base snapshot. Only `status`, `expected_pr`, `closeout_pr`, `ledger_evidence`\n-   and `expected_branch` may differ \u2014 the ADR 0015 contract-immutability rule,\n+   and `expected_branch` may differ \u2014 the SADR-0015 contract-immutability rule,\n    unchanged.\n \n    `expected_branch` is in that set because a closeout is *by construction* on a\n    different branch from the implementation it closes, and the loop rewrites the\n    field accordingly. It is safe to let it differ because MergeGate independently\n    requires `expected_branch` to equal the PR's actual head ref, so a PR cannot set\n-   it to anything other than the branch it is really on. The first draft of this ADR\n+   it to anything other than the branch it is really on. The first draft of this SADR\n    omitted it, which made the exemption unreachable: every real closeout failed\n    condition 4 on a field the loop always changes. See PR #209.\n 5. The state transitions are exactly the terminal set: task `review` \u2192 `done`, lock\n@@ -113,12 +113,12 @@\n classification must be derived, not asserted.\n \n **Fold the closeout into the implementation PR** so no second PR exists. Rejected under\n-ADR 0012, which recorded why: a task file that is already `done` on the implementation\n+SADR-0012, which recorded why: a task file that is already `done` on the implementation\n branch short-circuits the loop's review \u2192 CCA \u2192 merge \u2192 done ordering into a local\n goal-completion before any merge happens.\n \n **Have the loop auto-merge closeouts with elevated credentials.** Rejected outright: that\n-is a runtime authoring its own approval, which ADR 0010 and the constitution's\n+is a runtime authoring its own approval, which SADR-0010 and the constitution's\n approval-record prohibition forbid. It would also be indistinguishable, in the evidence,\n from the forgery those rules exist to prevent.\n \ndiff --git a/docs/adr/0018-carried-guardian-approval-across-a-proven-base-sync.md b/docs/adr/SADR-0018-carried-guardian-approval-across-a-proven-base-sync.md\nindex 22358562da390ed8b45cd600da08a43d378b4680..e6e2907cefa776460af73ddf7d39db605f3c39b0\n--- a/docs/adr/0018-carried-guardian-approval-across-a-proven-base-sync.md\n+++ b/docs/adr/SADR-0018-carried-guardian-approval-across-a-proven-base-sync.md\n@@ -1,4 +1,4 @@\n-# ADR 0018: Carried Guardian Approval Across A Proven Base Sync\n+# SADR-0018: Carried Guardian Approval Across A Proven Base Sync\n \n ## Status\n \n@@ -34,7 +34,7 @@\n Guardian evaluator that consumes it (`shiki_guardian.evaluate_guardian_approval`,\n the `guardian_comment_carried` source) were built and tested in isolation. Neither\n does anything until a caller passes the flags that turn the carry on, so the\n-feature was invisible in CI. This ADR records the decision the two of them\n+feature was invisible in CI. This SADR records the decision the two of them\n implement, and this task wires it into the four CI invocations that judge Guardian\n approval.\n \ndiff --git a/docs/adr/README.md b/docs/adr/README.md\nindex c70e057a4520c36e042ab68c53cd4a21ef709b71..5da43856836e2ce6a150f8127766e2f88355ab4b\n--- a/docs/adr/README.md\n+++ b/docs/adr/README.md\n@@ -1,40 +1,41 @@\n-# Architecture Decision Records\n+# Decision Record Namespaces\n \n-This directory holds Shiki's Architecture Decision Records (ADRs): durable\n-records of hard-to-reverse platform decisions. ADRs are part of the Shiki source\n-of truth alongside GitHub state, the `.shiki/` mirror, and `CONTEXT.md`.\n+This directory holds two independent decision namespaces:\n \n-## How ADRs Work\n+- Shiki Architecture Decision Records (SADRs), named\n+  `SADR-NNNN-<short-slug>.md`, record hard-to-reverse Shiki platform decisions.\n+- Target Architecture Decision Records (ADRs), named\n+  `NNNN-<short-slug>.md`, belong to the target product.\n \n-- New ADRs are added as `docs/adr/NNNN-<short-slug>.md`, where `NNNN` is the next\n-  zero-padded sequence number.\n-- The **canonical list of ADRs is the set of files matching `docs/adr/*.md`** in\n-  this directory. Browse `docs/adr/*.md` for the authoritative, current set.\n-- Use [`template.md`](template.md) as the starting point for a new ADR.\n-- Record an ADR when a decision is hard to reverse, surprising without context,\n-  and a real tradeoff. Do not write an ADR for routine or easily reversible\n-  choices.\n+The same four-digit number may appear once in each namespace. A platform\n+`SADR-NNNN` citation resolves only to `SADR-NNNN-*.md`; a target `ADR NNNN`\n+citation resolves only to `NNNN-*.md`.\n \n-Using the `docs/adr/*.md` glob as the source of truth means other PRs can add an\n-ADR without editing this index file, avoiding merge contention on a\n-hand-maintained enumeration.\n+## How Decision Records Work\n \n-## Current ADRs (snapshot)\n-\n-The list below is a convenience snapshot and may lag behind the directory. When\n-in doubt, list `docs/adr/*.md`.\n+- New Shiki platform decisions use the next zero-padded SADR number.\n+- New target product decisions use the target's own ADR sequence.\n+- The canonical list is the files in `docs/adr/`; no hand-maintained index is\n+  authoritative.\n+- Use [`template.md`](template.md) as the starting point and choose the filename\n+  namespace that owns the decision.\n+- Record a decision when it is hard to reverse, surprising without context, and\n+  a real tradeoff. Do not write a decision record for routine or easily\n+  reversible choices.\n \n-- [0001 \u2014 Keep Shiki As The Platform Name](0001-keep-shiki-as-platform-name.md)\n-- [0002 \u2014 Use AGENTS As The Shared Agent Constitution](0002-use-agents-as-shared-constitution.md)\n-- [0003 \u2014 Require Skill Gate For MergeGate Readiness](0003-require-skill-gate-for-mergegate-readiness.md)\n-- [0004 \u2014 Use GitHub First With A Repository-Local Shiki Mirror](0004-use-github-first-with-shiki-mirror.md)\n-- [0005 \u2014 Use Codex As Front And Claude Code Action As The GitHub Runtime](0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md)\n-- [0006 \u2014 Separate Codex Implementation, CCA Completion, And MergeGate Authorization](0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md)\n+Using the directory as the source of truth lets independent changes add records\n+without contending on an enumerated index.\n \n-## Adding A New ADR\n+## Adding A Shiki SADR\n \n-1. Copy `template.md` to `docs/adr/NNNN-<short-slug>.md`.\n+1. Copy `template.md` to `docs/adr/SADR-NNNN-<short-slug>.md`.\n 2. Fill in the title, status, context, decision, and consequences.\n-3. Set the status to `Proposed`, then `Accepted` once the decision is approved.\n-4. Open a PR following `CONTRIBUTING.md`. You do not need to edit this index for\n-   the ADR to be discoverable via `docs/adr/*.md`.\n+3. Set the status to `Proposed`, then `Accepted` once the proper authority has\n+   approved the decision.\n+4. Open a PR following `CONTRIBUTING.md`.\n+\n+## Adding A Target ADR\n+\n+1. Copy `template.md` to `docs/adr/NNNN-<short-slug>.md` using the target's own\n+   sequence.\n+2. Cite it as `ADR NNNN`; do not prefix its filename with `SADR-`.\ndiff --git a/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md b/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\nnew file mode 100644\nindex 0000000000000000000000000000000000000000..7455ce1f3bed02ddddb851ac21a1d1a648c9aeba\n--- /dev/null\n+++ b/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\n@@ -0,0 +1,55 @@\n+# SADR-0019: Separate Shiki Platform SADRs From Target ADRs\n+\n+## Status\n+\n+Accepted\n+\n+## Context\n+\n+Shiki installs its platform decision records into `docs/adr/` in every target\n+repository. Shiki and its targets previously used the same numeric\n+`NNNN-<slug>.md` namespace. A target that already owned ADRs 0013 through 0018\n+therefore acquired two records for each number during installation, making an\n+`ADR NNNN` citation ambiguous. Renumbering target decisions would rewrite target\n+history, while renumbering Shiki decisions per target would destroy their stable\n+platform identity.\n+\n+Upgrades also need to remove the former Shiki-owned numeric paths without ever\n+mistaking a target-owned ADR for platform content. The install stamp already\n+records exact shipped paths and content digests, so it can prove ownership only\n+when both still match.\n+\n+## Decision\n+\n+Shiki platform decisions use the `SADR` namespace:\n+\n+- records are named `docs/adr/SADR-NNNN-<slug>.md`;\n+- live platform citations use `SADR-NNNN`;\n+- target product decisions retain `docs/adr/NNNN-<slug>.md` and `ADR NNNN`;\n+- the reference validator resolves the two namespaces independently, so the same\n+  four-digit number may exist once in each namespace.\n+\n+Before a forced upgrade writes anything, the installer inventories the exact\n+former numeric Shiki ADR paths. It may delete a legacy path only when the\n+existing install stamp contains that exact path and its recorded SHA-256 digest\n+matches the file currently on disk. All ownership blockers are reported\n+together and abort the upgrade before its first write. A non-forced install\n+never performs this cleanup.\n+\n+Historical `.shiki/` evidence remains unchanged because it records the namespace\n+that existed when that evidence was produced.\n+\n+## Consequences\n+\n+- Targets may use any ADR number without colliding with Shiki's platform SADRs.\n+- Fresh installs preserve target ADR bytes and install a stable SADR set.\n+- Forced upgrades fail closed when legacy ownership cannot be proven, and a\n+  successful upgrade refreshes the stamp without removed legacy paths.\n+- Current governance, packaging, workflow, prompt, script, and skill references\n+  must move with the platform records.\n+\n+## Alternatives Considered\n+\n+- Collision-only refusal leaves affected targets permanently unupgradable.\n+- Renumbering target ADRs mutates target-owned history.\n+- Per-target Shiki renumbering makes platform citations non-portable.\ndiff --git a/docs/adr/template.md b/docs/adr/template.md\nindex a92a97daddd0b00ab9dd626cc8d86942da2eafed..17dd79175b5fee2105935557f691b40cf9492942\n--- a/docs/adr/template.md\n+++ b/docs/adr/template.md\n@@ -1,14 +1,14 @@\n-# NNNN: <Short Decision Title>\n+# <SADR-NNNN or NNNN>: <Short Decision Title>\n \n ## Status\n \n-Proposed | Accepted | Superseded by ADR-NNNN\n+Proposed | Accepted | Superseded by SADR-NNNN or target ADR NNNN\n \n ## Context\n \n What is the situation, constraint, or force that requires a decision? Reference\n-relevant domain terms (`CONTEXT.md`), prior ADRs, and code reality. State the\n-problem, not the solution.\n+relevant domain terms (`CONTEXT.md`), prior Shiki SADRs or target ADRs, and code\n+reality. State the problem, not the solution.\n \n ## Decision\n \ndiff --git a/docs/agents/autonomous-loop-e2e-acceptance.md b/docs/agents/autonomous-loop-e2e-acceptance.md\nindex bbf84c6081987f159ac6ad9ecf3c82b7aa72e7e8..5c13c1d02a05a90b0dc46cbef7c95a9846d0c237\n--- a/docs/agents/autonomous-loop-e2e-acceptance.md\n+++ b/docs/agents/autonomous-loop-e2e-acceptance.md\n@@ -40,8 +40,8 @@\n 3. `gh auth status` is logged in with merge permission on the repo.\n 4. Branch protection + required checks are configured exactly as the goal-loop\n    required-check set expects (see `docs/agents/branch-protection-smoke.md`).\n-5. The repo `.shiki/policy` permits autonomous low/medium-risk merges (ADR\n-   0008/0009); the MergeGate policy check is the recorded-authority gate.\n+5. The repo `.shiki/policy` permits autonomous low/medium-risk merges\n+   (SADR-0008/0009); the MergeGate policy check is the recorded-authority gate.\n \n The operator's only actions are: register the task and start the loop. **No\n manual pre-clearing** means after `shiki loop run` starts, the operator does not\ndiff --git a/docs/agents/domain.md b/docs/agents/domain.md\nindex 258da8ea81e87ac0a6b0b5b5e5c2fde980eaabb7..67aaa6f4b7d0a5aa4d495ce6279073333f7798cb\n--- a/docs/agents/domain.md\n+++ b/docs/agents/domain.md\n@@ -5,7 +5,9 @@\n ## Layout\n \n - `CONTEXT.md` defines Shiki language. Keep it implementation-free.\n-- `docs/adr/` records hard-to-reverse decisions.\n+- `docs/adr/SADR-NNNN-*.md` records Shiki platform decisions.\n+- `docs/adr/NNNN-*.md` records target product decisions without sharing the\n+  SADR namespace.\n - `AGENTS.md` is the runtime-neutral agent constitution.\n - `CLAUDE.md` is the Claude Code wrapper around `AGENTS.md`.\n - `.shiki/` is the Target Repository mirror for Goals, PRDs, plans, Task DAGs, contracts, locks, worktree records, ledger entries, CCA verdicts, repair packets, reports, and handoffs.\ndiff --git a/docs/agents/guardian-policy.md b/docs/agents/guardian-policy.md\nindex 4147c7adf05a47adb4fd24ada1a1c2e1f2ba6d2d..a20d9cc6c00a569bb057b9ca8d6b51c7fc4a9847\n--- a/docs/agents/guardian-policy.md\n+++ b/docs/agents/guardian-policy.md\n@@ -98,8 +98,8 @@\n `origin/<default_branch>` and reuses each job's existing base fetch; a shallow\n (`--depth`) or second base fetch would make the proof unresolvable and silently\n disable the carry, so neither is permitted. CCA reads `guardian_comment_carried` as\n-a recorded authority in the deterministic signal and does not re-derive it. See ADR\n-0018 for the full decision, the rejected diff/patch-equality and path-disjointness\n+a recorded authority in the deterministic signal and does not re-derive it. See\n+SADR-0018 for the full decision, the rejected diff/patch-equality and path-disjointness\n alternatives, and the accepted residual.\n \n ## External AI Guardian Review (SADR-0010)\ndiff --git a/docs/agents/implementation-policy.md b/docs/agents/implementation-policy.md\nindex c754144a9fb5bf4a38aa82a60e1670c59e626b44..e90d40fe21880b9a09719c5ecfe6bde7284eb3ce\n--- a/docs/agents/implementation-policy.md\n+++ b/docs/agents/implementation-policy.md\n@@ -31,6 +31,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n ## Non-Negotiables\n \n 1. **Planning precedes implementation.** Non-trivial Goals start with `grill-with-docs` and Context & Impact.\ndiff --git a/scripts/guardian_approval_signal.py b/scripts/guardian_approval_signal.py\nindex d059f92b2c82aa2b5d6ca421365537a87886c1cb..16924892714f4f5b9303c0ef0e325a7e70075630\n--- a/scripts/guardian_approval_signal.py\n+++ b/scripts/guardian_approval_signal.py\n@@ -199,8 +199,8 @@\n ) -> Any:\n     \"\"\"Evaluate SADR-0015 Contract Approval for the CCA Guardian signal.\n \n-    The SAME OR that MergeGate applies must reach this deterministic signal: ADR\n-    0015 is explicit that without it, a contract-approved PR still returns\n+    The SAME OR that MergeGate applies must reach this deterministic signal:\n+    SADR-0015 is explicit that without it, a contract-approved PR still returns\n     ``needs_guardian``, the CCA job fails, and the MergeGate policy check never\n     runs \u2014 the gate this change moves would never be reached. This resolves the\n     task the SAME way MergeGate does (the first Shiki task id in the PR body),\ndiff --git a/scripts/shiki_contract_approval.py b/scripts/shiki_contract_approval.py\nindex 941bcb96fdd8b9a85bc0c4f0f04d5dad9d04839c..4a4b418f6527413161db82670bec857a043360e2\n--- a/scripts/shiki_contract_approval.py\n+++ b/scripts/shiki_contract_approval.py\n@@ -142,7 +142,7 @@\n     changed_files_status: Any,\n     registration: Any,\n ) -> ContractApprovalResult:\n-    \"\"\"Whether this PR carries an SADR-0015 Contract Approval.\n+    \"\"\"Whether this PR carries a SADR-0015 Contract Approval.\n \n     The four conditions are evaluated IN ORDER, each failure recorded as a\n     distinct reason string:\ndiff --git a/scripts/shiki_installer.py b/scripts/shiki_installer.py\nindex be064ab7a29ff6836f7f7a1d70500a43e0d11d7b..d3e3247edbf3e4be287a4667176d86f6500f8dc5\n--- a/scripts/shiki_installer.py\n+++ b/scripts/shiki_installer.py\n@@ -118,6 +118,7 @@\n # what lets a target carry it without a governance-schema change.\n INSTALL_STAMP_PATH = \".shiki/install-stamp.json\"\n INSTALL_STAMP_VERSION = 1\n+LEGACY_SHIKI_SADR_MAX = 18\n \n # Under --force the shipped surface splits three ways, so nothing is lost and\n # nothing is silently stale.\n@@ -237,9 +238,98 @@\n         )\n \n \n+def legacy_shiki_adr_paths() -> tuple[str, ...]:\n+    \"\"\"Former numeric paths for Shiki SADR-0001 through SADR-0018.\n+\n+    The authoritative slugs come from the current SADR filenames. SADR-0019 is\n+    the namespace decision itself and never shipped under a numeric legacy path.\n+    \"\"\"\n+    paths: list[str] = []\n+    for sadr in sorted((ROOT / \"docs\" / \"adr\").glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\")):\n+        number = sadr.name.split(\"-\", 2)[1]\n+        if int(number) <= LEGACY_SHIKI_SADR_MAX:\n+            paths.append(f\"docs/adr/{sadr.name.removeprefix('SADR-')}\")\n+    return tuple(paths)\n+\n+\n+def preflight_legacy_sadr_cleanup(target: Path, *, force: bool) -> tuple[str, ...]:\n+    \"\"\"Authorize exact legacy Shiki ADR removals before the first write.\n+\n+    A non-force install never removes existing files. A forced install needs no\n+    stamp when no legacy Shiki path is present. When legacy paths do exist, each\n+    must be named in a readable install stamp and its current bytes must match\n+    the recorded digest.\n+    \"\"\"\n+    if not force:\n+        return ()\n+    present = tuple(\n+        relative\n+        for relative in legacy_shiki_adr_paths()\n+        if (target / relative).exists() or (target / relative).is_symlink()\n+    )\n+    if not present:\n+        return ()\n+\n+    stamp_path = target / INSTALL_STAMP_PATH\n+    try:\n+        raw = stamp_path.read_text(encoding=\"utf-8\")\n+        stamp = json.loads(raw)\n+    except FileNotFoundError:\n+        detail = \"install stamp is absent\"\n+        blockers = [f\"{relative}: {detail}\" for relative in present]\n+    except (OSError, UnicodeDecodeError) as error:\n+        detail = f\"install stamp is unreadable ({error})\"\n+        blockers = [f\"{relative}: {detail}\" for relative in present]\n+    except json.JSONDecodeError as error:\n+        detail = f\"install stamp is malformed ({error})\"\n+        blockers = [f\"{relative}: {detail}\" for relative in present]\n+    else:\n+        if not isinstance(stamp, dict):\n+            blockers = [\n+                f\"{relative}: install stamp is malformed (expected an object)\"\n+                for relative in present\n+            ]\n+        else:\n+            digests = stamp.get(\"digests\")\n+            blockers = []\n+            if not isinstance(digests, dict):\n+                blockers.extend(\n+                    f\"{relative}: install stamp has no digest for this path\"\n+                    for relative in present\n+                )\n+            else:\n+                for relative in present:\n+                    expected = digests.get(relative)\n+                    if not isinstance(expected, str) or not expected:\n+                        blockers.append(\n+                            f\"{relative}: install stamp has no digest for this path\"\n+                        )\n+                        continue\n+                    try:\n+                        actual = _sha256_file(target / relative)\n+                    except OSError as error:\n+                        blockers.append(\n+                            f\"{relative}: legacy path is unreadable ({error})\"\n+                        )\n+                        continue\n+                    if actual != expected:\n+                        blockers.append(\n+                            f\"{relative}: digest mismatch (stamp {expected}, current {actual})\"\n+                        )\n+\n+    if blockers:\n+        joined = \"\\n\".join(f\"- {blocker}\" for blocker in blockers)\n+        raise ShikiError(\n+            \"legacy Shiki ADR cleanup is not authorized:\\n\"\n+            f\"{joined}\\nNo files were written.\"\n+        )\n+    return present\n+\n+\n def install_template(target: Path, *, force: bool, validate: bool) -> list[NewFileNote]:\n     # Preconditions first: never rewrite the tree of a target that cannot be\n     # upgraded cleanly (see refuse_pending_migrations).\n+    legacy_cleanup = preflight_legacy_sadr_cleanup(target, force=force)\n     refuse_pending_migrations(target)\n \n     # Resolve the target's required CODEOWNERS owner once from its\n@@ -250,6 +340,10 @@\n     # verbatim. See ``codeowners_required_owner``.\n     codeowners_owner = codeowners_required_owner(target)\n \n+    for relative in legacy_cleanup:\n+        (target / relative).unlink()\n+        info(f\"removed stamped legacy Shiki ADR: {target / relative}\")\n+\n     notes: list[NewFileNote] = []\n     for relative in TEMPLATE_PATHS:\n         source = ROOT / relative\ndiff --git a/scripts/shiki_loop.py b/scripts/shiki_loop.py\nindex 3611641da8aeb5038b1da90e3114b09a473ec6d8..84caa491b9cad0edc4176f209c13e9221e975681\n--- a/scripts/shiki_loop.py\n+++ b/scripts/shiki_loop.py\n@@ -424,7 +424,7 @@\n         # unresolvable CCA verdict has already produced its terminal stop and is never\n         # laundered into a head-moving sync (SADR-0010). It also never fires while the\n         # policy (Guardian) gate is red \u2014 for ANY reason, not only when it is the sole\n-        # red check \u2014 so a head-bound Guardian approval, or an SADR-0015\n+        # red check \u2014 so a head-bound Guardian approval, or a SADR-0015\n         # contract-approval carry that base movement defeated (the policy gate compares\n         # base and head task governance directly), is never silently restored by\n         # adopting base wholesale. It is bounded by a durable per-task counter so the\ndiff --git a/scripts/test_shiki_goal_loop.sh b/scripts/test_shiki_goal_loop.sh\nindex 753c7b516e10f0be3bc92a4bd0eb1fa699776ae6..8f3820a0a079b3044424cfe1a6c6b7fe68896e67\n--- a/scripts/test_shiki_goal_loop.sh\n+++ b/scripts/test_shiki_goal_loop.sh\n@@ -231,8 +231,8 @@\n \n # High/critical risk merges autonomously when all required checks are green:\n # the \"MergeGate policy check\" required check IS the Guardian gate (it enforces\n-# guardian-policy.json \u2014 human approval OR external AI guardian review, ADR\n-# 0010), so green checks mean a recorded authority approved. The fake gh returns\n+# guardian-policy.json \u2014 human approval OR external AI guardian review,\n+# SADR-0010), so green checks mean a recorded authority approved. The fake gh returns\n # green checks, simulating that the Guardian gate passed.\n python3 \"$ROOT/scripts/shiki.py\" goal create --target \"$TARGET\" --title \"Guardian gate\" --outcome \"High risk merges via policy gate\" >/tmp/shiki-goal-loop-guardian-goal.json\n GGOAL=\"$(json_get /tmp/shiki-goal-loop-guardian-goal.json goal_id)\"\ndiff --git a/tests/test_adr_references.py b/tests/test_adr_references.py\nindex 51706329c797d5dd9b47e082a333fa83b261d482..db6e693e35f67103935be34f1d9bb4ff7ee60395\n--- a/tests/test_adr_references.py\n+++ b/tests/test_adr_references.py\n@@ -1,7 +1,7 @@\n-\"\"\"Every ``ADR ####`` citation must resolve to exactly one decision record.\n+\"\"\"Every target ADR and Shiki SADR citation resolves in its own namespace.\n \n Background. ``scripts/mergegate_check.py`` justifies ``is_bookkeeping_closeout``\n-with \"ADR 0017\" nine times and ``scripts/guardian_approval_signal.py`` cites\n+with \"SADR-0017\" nine times and ``scripts/guardian_approval_signal.py`` cites\n 0016/0017 seven times, yet for a stretch the numbering in ``docs/adr/`` stopped\n at 0015: the authoring commits lived on a coordinator branch and no task ever\n declared ``docs/adr/...`` in its locks, so nothing carried the records to the\n@@ -11,11 +11,12 @@\n and the directory drifted with no signal.\n \n This is the same unbound-pair shape behind other silent failures: a reference\n-and its target with nothing asserting they agree. This test binds them. It scans\n-every ``scripts/*.py`` (plus ``docs/**/*.md`` and the root ``*.md`` governance\n-files, which are cheap) for the ``ADR ####`` pattern and asserts each cited\n-number resolves to exactly one ``docs/adr/####-*.md``. The converse is\n-deliberately NOT required: an ADR may exist without being cited.\n+and its target with nothing asserting they agree. This test binds them. Target\n+``ADR NNNN`` citations resolve only to ``docs/adr/NNNN-*.md``; Shiki\n+``SADR-NNNN`` citations resolve only to ``docs/adr/SADR-NNNN-*.md``. The scan\n+covers current governance docs, scripts, workflows, prompts, packaging metadata,\n+and repo-local Codex guidance while deliberately excluding historical\n+``.shiki`` evidence. The converse is not required: a record may be uncited.\n \n The suite is dependency free: standard-library ``unittest`` only.\n \"\"\"\n@@ -31,17 +32,18 @@\n \n REPO_ROOT = Path(__file__).resolve().parents[1]\n \n-# \"ADR 0016\" -- a four-digit citation. The trailing \\b keeps a five-digit token\n-# from being read as a truncated four-digit number.\n-_CITATION = re.compile(r\"\\bADR (\\d{4})\\b\")\n-# \"0016-locks-bound-product-paths.md\" -- the leading four digits name the record.\n-_ADR_FILENAME = re.compile(r\"^(\\d{4})-.*\\.md$\")\n+# ``ADR 0016`` / ``SADR-0016`` -- a namespace plus four-digit citation. The\n+# trailing ``\\b`` keeps a five-digit token from being read as a truncated one.\n+_CITATION = re.compile(r\"\\b(S?ADR)[ -](\\d{4})\\b\")\n+# Target ADRs retain the numeric filename; Shiki platform decisions use SADR.\n+_DECISION_FILENAME = re.compile(r\"^(?:(SADR)-)?(\\d{4})-.*\\.md$\")\n \n \n class Violation(NamedTuple):\n     \"\"\"A citation that does not resolve to exactly one decision record.\"\"\"\n \n     citing_file: str  # repo-relative path of the file that cites the ADR\n+    namespace: str  # \"ADR\" for target decisions; \"SADR\" for Shiki decisions\n     number: str  # the cited four-digit number, e.g. \"0016\"\n     line: int  # 1-based line of the citation\n     reason: str  # \"missing\" (zero matches) or \"ambiguous\" (two or more)\n@@ -49,44 +51,59 @@\n \n     @property\n     def expected_path(self) -> str:\n-        return f\"docs/adr/{self.number}-*.md\"\n+        prefix = \"SADR-\" if self.namespace == \"SADR\" else \"\"\n+        return f\"docs/adr/{prefix}{self.number}-*.md\"\n \n     def describe(self) -> str:\n         \"\"\"Name the citing file, the cited number, and the missing path.\"\"\"\n         if self.reason == \"missing\":\n             return (\n-                f\"{self.citing_file}:{self.line} cites ADR {self.number} \"\n+                f\"{self.citing_file}:{self.line} cites {self.namespace} {self.number} \"\n                 f\"but no document matches {self.expected_path}\"\n             )\n         joined = \", \".join(self.matches)\n         return (\n-            f\"{self.citing_file}:{self.line} cites ADR {self.number} which \"\n+            f\"{self.citing_file}:{self.line} cites {self.namespace} {self.number} which \"\n             f\"resolves to {len(self.matches)} documents (expected exactly one \"\n             f\"at {self.expected_path}): {joined}\"\n         )\n \n \n-def _adr_index(root: Path) -> dict[str, list[Path]]:\n-    \"\"\"Map every four-digit ADR number to the record files that claim it.\"\"\"\n-    index: dict[str, list[Path]] = {}\n+def _adr_index(root: Path) -> dict[tuple[str, str], list[Path]]:\n+    \"\"\"Map each decision namespace and number to the files that claim it.\"\"\"\n+    index: dict[tuple[str, str], list[Path]] = {}\n     adr_dir = root / \"docs\" / \"adr\"\n     if adr_dir.is_dir():\n         for path in sorted(adr_dir.glob(\"*.md\")):\n-            match = _ADR_FILENAME.match(path.name)\n+            match = _DECISION_FILENAME.match(path.name)\n             if match:\n-                index.setdefault(match.group(1), []).append(path)\n+                namespace = \"SADR\" if match.group(1) else \"ADR\"\n+                index.setdefault((namespace, match.group(2)), []).append(path)\n     return index\n \n \n def _citing_files(root: Path, include_docs: bool = True) -> list[Path]:\n-    \"\"\"Files scanned for citations: scripts/*.py, and cheaply the markdown.\"\"\"\n+    \"\"\"Current, shipped text surfaces scanned for decision citations.\"\"\"\n     files = sorted((root / \"scripts\").glob(\"*.py\"))\n+    files += sorted((root / \"scripts\").glob(\"*.sh\"))\n     if include_docs:\n         docs = root / \"docs\"\n         if docs.is_dir():\n             files += sorted(docs.rglob(\"*.md\"))\n         files += sorted(root.glob(\"*.md\"))\n-    return files\n+        pyproject = root / \"pyproject.toml\"\n+        if pyproject.is_file():\n+            files.append(pyproject)\n+        for relative in (\".github\", \".codex\"):\n+            surface = root / relative\n+            if surface.is_dir():\n+                files += sorted(\n+                    path\n+                    for path in surface.rglob(\"*\")\n+                    if path.is_file()\n+                    and path.suffix in {\".md\", \".py\", \".sh\", \".toml\", \".yaml\", \".yml\"}\n+                )\n+    return sorted(set(files))\n \n \n def find_adr_reference_violations(\n@@ -94,10 +111,9 @@\n ) -> list[Violation]:\n     \"\"\"Return every ``ADR ####`` citation under ``root`` that fails to resolve.\n \n-    A citation resolves when exactly one ``docs/adr/####-*.md`` matches its\n-    number. Zero matches is a \"missing\" violation; two or more is \"ambiguous\".\n-    Records that are never cited produce nothing -- the converse is not\n-    required.\n+    A citation resolves when exactly one record in the same namespace matches\n+    its number. Zero matches is a \"missing\" violation; two or more is\n+    \"ambiguous\". Records that are never cited produce nothing.\n     \"\"\"\n     root = Path(root)\n     index = _adr_index(root)\n@@ -110,13 +126,15 @@\n         rel = path.relative_to(root).as_posix()\n         for lineno, line in enumerate(text.splitlines(), start=1):\n             for match in _CITATION.finditer(line):\n-                number = match.group(1)\n-                found = index.get(number, [])\n+                namespace, number = match.groups()\n+                found = index.get((namespace, number), [])\n                 if len(found) == 1:\n                     continue\n                 reason = \"missing\" if not found else \"ambiguous\"\n                 rels = [p.relative_to(root).as_posix() for p in found]\n-                violations.append(Violation(rel, number, lineno, reason, rels))\n+                violations.append(\n+                    Violation(rel, namespace, number, lineno, reason, rels)\n+                )\n     return violations\n \n \n@@ -129,8 +147,8 @@\n         self.assertEqual(\n             violations,\n             [],\n-            \"Every 'ADR ####' citation must resolve to exactly one \"\n-            f\"docs/adr/####-*.md:\\n{detail}\",\n+            \"Every ADR/SADR citation must resolve exactly once in its own \"\n+            f\"docs/adr namespace:\\n{detail}\",\n         )\n \n     def test_scan_actually_reads_scripts(self) -> None:\n@@ -146,8 +164,21 @@\n         # The two records this Goal carried onto the default branch, and which\n         # the scripts cite most, must be present.\n         index = _adr_index(REPO_ROOT)\n-        self.assertIn(\"0016\", index)\n-        self.assertIn(\"0017\", index)\n+        self.assertIn((\"SADR\", \"0016\"), index)\n+        self.assertIn((\"SADR\", \"0017\"), index)\n+\n+    def test_platform_tree_has_only_sadr_0001_through_0019(self) -> None:\n+        adr_dir = REPO_ROOT / \"docs\" / \"adr\"\n+        sadrs = sorted(adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\"))\n+        self.assertEqual(\n+            {path.name.split(\"-\", 2)[1] for path in sadrs},\n+            {f\"{number:04d}\" for number in range(1, 20)},\n+        )\n+        self.assertEqual(\n+            list(adr_dir.glob(\"[0-9][0-9][0-9][0-9]-*.md\")),\n+            [],\n+            \"the Shiki platform tree must not retain legacy numeric SADR paths\",\n+        )\n \n \n class FixtureAdrReferences(unittest.TestCase):\n@@ -180,6 +211,26 @@\n         self.assertIn(\"9991\", message)  # cited number\n         self.assertIn(\"docs/adr/9991-*.md\", message)  # missing path\n \n+    def test_target_adr_does_not_satisfy_missing_platform_sadr(self) -> None:\n+        root = self._make_repo(\n+            adr_names=[\"0042-target-decision.md\"],\n+            script_body='\"\"\"Target ADR 0042 exists, but platform SADR-0042 does not.\"\"\"\\n',\n+        )\n+        violations = find_adr_reference_violations(root, include_docs=False)\n+        self.assertEqual(len(violations), 1)\n+        self.assertEqual(violations[0].namespace, \"SADR\")\n+        self.assertEqual(violations[0].number, \"0042\")\n+        self.assertEqual(violations[0].expected_path, \"docs/adr/SADR-0042-*.md\")\n+\n+    def test_target_adr_and_platform_sadr_may_reuse_a_number(self) -> None:\n+        root = self._make_repo(\n+            adr_names=[\"0042-target-decision.md\", \"SADR-0042-platform-decision.md\"],\n+            script_body='\"\"\"Target ADR 0042 and platform SADR-0042 are distinct.\"\"\"\\n',\n+        )\n+        self.assertEqual(\n+            find_adr_reference_violations(root, include_docs=False), []\n+        )\n+\n     def test_uncited_adr_is_allowed(self) -> None:\n         # An ADR exists but nothing cites it -> not a violation. The converse\n         # (every ADR must be cited) is explicitly NOT required.\ndiff --git a/tests/test_policy_job_merged_prs.py b/tests/test_policy_job_merged_prs.py\nindex d45512e74681e5e649279236c7d4e79d5c3da471..b97989f8bd74d091de1a6b5275a20d8dc68c4512\n--- a/tests/test_policy_job_merged_prs.py\n+++ b/tests/test_policy_job_merged_prs.py\n@@ -3,8 +3,8 @@\n \n ``.github/workflows/shiki-cca-completion.yml`` has two consumers of the merged-PR\n proof: the CCA *signal* (``guardian_approval_signal.py`` in \"Prepare CCA context\")\n-and the authoritative gate (``mergegate_check.py`` in \"Enforce MergeGate\"). ADR\n-0017's bookkeeping-closeout exemption (condition 6) needs the merged implementation\n+and the authoritative gate (``mergegate_check.py`` in \"Enforce MergeGate\").\n+SADR-0017's bookkeeping-closeout exemption (condition 6) needs the merged implementation\n PR proven, and a closeout carries NO ``post_merge_reconcile`` marker. The signal\n already resolves ``MERGED_PRS`` unconditionally, but \"Enforce MergeGate\" used to\n resolve it only inside ``grep -q '<!-- shiki:post_merge_reconcile -->'``. So a\ndiff --git a/tests/test_sadr_install_migration.py b/tests/test_sadr_install_migration.py\nnew file mode 100644\nindex 0000000000000000000000000000000000000000..5a442fa5e4968ade178dcdedc3c63b54dba580a2\n--- /dev/null\n+++ b/tests/test_sadr_install_migration.py\n@@ -0,0 +1,224 @@\n+\"\"\"SADR installation and collision-safe legacy upgrade behavior.\"\"\"\n+\n+from __future__ import annotations\n+\n+import contextlib\n+import hashlib\n+import io\n+import json\n+import shutil\n+import tempfile\n+import unittest\n+from pathlib import Path\n+\n+import shiki_test_support  # noqa: F401  (path bootstrap)\n+\n+import shiki_installer\n+from tests.test_adr_references import find_adr_reference_violations\n+\n+\n+PRODUCT_NUMBERS = tuple(range(13, 19))\n+\n+\n+def _quiet_install(target: Path, *, force: bool, validate: bool) -> None:\n+    with contextlib.redirect_stdout(io.StringIO()):\n+        shiki_installer.install_template(target, force=force, validate=validate)\n+\n+\n+def _sha256(path: Path) -> str:\n+    return hashlib.sha256(path.read_bytes()).hexdigest()\n+\n+\n+def _legacy_sadr_paths() -> tuple[str, ...]:\n+    return tuple(\n+        f\"docs/adr/{path.name.removeprefix('SADR-')}\"\n+        for path in sorted((shiki_installer.ROOT / \"docs\" / \"adr\").glob(\"SADR-*.md\"))\n+        if path.name != \"SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\"\n+    )\n+\n+\n+def _create_legacy_upgrade_fixture(\n+    target: Path,\n+) -> tuple[dict[str, bytes], tuple[str, ...]]:\n+    adr_dir = target / \"docs\" / \"adr\"\n+    adr_dir.mkdir(parents=True)\n+    product_bytes = {\n+        f\"{number:04d}-cubeshare-product-decision.md\": (\n+            f\"# CubeShare ADR {number:04d}\\n\\nDo not rewrite {number}.\\n\"\n+        ).encode()\n+        for number in PRODUCT_NUMBERS\n+    }\n+    for name, content in product_bytes.items():\n+        (adr_dir / name).write_bytes(content)\n+\n+    legacy_paths = _legacy_sadr_paths()\n+    for relative in legacy_paths:\n+        (target / relative).write_bytes(f\"legacy Shiki bytes for {relative}\\n\".encode())\n+\n+    stamp_path = target / shiki_installer.INSTALL_STAMP_PATH\n+    stamp_path.parent.mkdir(parents=True, exist_ok=True)\n+    stamp = {\n+        \"version\": shiki_installer.INSTALL_STAMP_VERSION,\n+        \"platform_commit\": None,\n+        \"installed_at\": \"2000-01-01T00:00:00+00:00\",\n+        \"digests\": {\n+            relative: _sha256(target / relative) for relative in legacy_paths\n+        },\n+    }\n+    stamp_path.write_text(\n+        json.dumps(stamp, indent=2, sort_keys=True) + \"\\n\", encoding=\"utf-8\"\n+    )\n+    return product_bytes, legacy_paths\n+\n+\n+def _tree_snapshot(root: Path) -> dict[str, tuple[str, bytes | str]]:\n+    \"\"\"Recursive byte/shape snapshot used to prove preflight is write-free.\"\"\"\n+    snapshot: dict[str, tuple[str, bytes | str]] = {}\n+    for path in sorted(root.rglob(\"*\")):\n+        relative = path.relative_to(root).as_posix()\n+        if path.is_symlink():\n+            snapshot[relative] = (\"symlink\", path.readlink().as_posix())\n+        elif path.is_dir():\n+            snapshot[relative] = (\"directory\", \"\")\n+        else:\n+            snapshot[relative] = (\"file\", path.read_bytes())\n+    return snapshot\n+\n+\n+class FreshSadrInstallTests(unittest.TestCase):\n+    def setUp(self) -> None:\n+        self.tmp = Path(tempfile.mkdtemp(prefix=\"shiki-sadr-fresh-\"))\n+        self.addCleanup(lambda: shutil.rmtree(self.tmp, ignore_errors=True))\n+        self.target = self.tmp / \"target\"\n+        self.adr_dir = self.target / \"docs\" / \"adr\"\n+        self.adr_dir.mkdir(parents=True)\n+        self.product_bytes = {\n+            f\"{number:04d}-product-decision.md\": (\n+                f\"# Product ADR {number:04d}\\n\\nTarget-owned bytes {number}.\\n\"\n+            ).encode()\n+            for number in PRODUCT_NUMBERS\n+        }\n+        for name, content in self.product_bytes.items():\n+            (self.adr_dir / name).write_bytes(content)\n+\n+    def test_fresh_install_preserves_product_adrs_and_installs_all_sadrs(self) -> None:\n+        _quiet_install(self.target, force=False, validate=True)\n+\n+        for name, expected in self.product_bytes.items():\n+            self.assertEqual((self.adr_dir / name).read_bytes(), expected)\n+\n+        sadr_numbers = {\n+            path.name.split(\"-\", 2)[1]\n+            for path in self.adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\")\n+        }\n+        self.assertEqual(sadr_numbers, {f\"{number:04d}\" for number in range(1, 20)})\n+\n+        numeric_records = {\n+            path.name\n+            for path in self.adr_dir.glob(\"[0-9][0-9][0-9][0-9]-*.md\")\n+        }\n+        self.assertEqual(numeric_records, set(self.product_bytes))\n+        self.assertEqual(find_adr_reference_violations(self.target), [])\n+\n+\n+class LegacySadrUpgradeTests(unittest.TestCase):\n+    def setUp(self) -> None:\n+        self.tmp = Path(tempfile.mkdtemp(prefix=\"shiki-sadr-upgrade-\"))\n+        self.addCleanup(lambda: shutil.rmtree(self.tmp, ignore_errors=True))\n+        self.target = self.tmp / \"target\"\n+        self.adr_dir = self.target / \"docs\" / \"adr\"\n+        self.product_bytes, self.legacy_paths = _create_legacy_upgrade_fixture(\n+            self.target\n+        )\n+\n+    def test_force_upgrade_deletes_only_stamped_legacy_sadrs(self) -> None:\n+        _quiet_install(self.target, force=True, validate=True)\n+\n+        for relative in self.legacy_paths:\n+            self.assertFalse((self.target / relative).exists(), relative)\n+        for name, expected in self.product_bytes.items():\n+            self.assertEqual((self.adr_dir / name).read_bytes(), expected)\n+\n+        sadrs = list(self.adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\"))\n+        self.assertEqual(len(sadrs), 19)\n+        refreshed = json.loads(\n+            (self.target / shiki_installer.INSTALL_STAMP_PATH).read_text(encoding=\"utf-8\")\n+        )\n+        for relative in self.legacy_paths:\n+            self.assertNotIn(relative, refreshed[\"digests\"])\n+        for path in sadrs:\n+            relative = path.relative_to(self.target).as_posix()\n+            self.assertEqual(refreshed[\"digests\"][relative], _sha256(path))\n+\n+    def test_non_force_install_never_deletes_legacy_files(self) -> None:\n+        before = {\n+            relative: (self.target / relative).read_bytes()\n+            for relative in self.legacy_paths\n+        }\n+        _quiet_install(self.target, force=False, validate=False)\n+        for relative, expected in before.items():\n+            self.assertEqual((self.target / relative).read_bytes(), expected)\n+\n+\n+class LegacyCleanupPreflightTests(unittest.TestCase):\n+    def setUp(self) -> None:\n+        self.tmp = Path(tempfile.mkdtemp(prefix=\"shiki-sadr-preflight-\"))\n+        self.addCleanup(lambda: shutil.rmtree(self.tmp, ignore_errors=True))\n+\n+    def test_all_ownership_blockers_abort_before_any_write(self) -> None:\n+        cases = (\n+            (\"absent\", \"install stamp is absent\"),\n+            (\"unreadable\", \"install stamp is unreadable\"),\n+            (\"malformed\", \"install stamp is malformed\"),\n+            (\"missing-digest\", \"install stamp has no digest\"),\n+            (\"digest-mismatch\", \"digest mismatch\"),\n+        )\n+        for name, expected_reason in cases:\n+            with self.subTest(name=name):\n+                target = self.tmp / name\n+                _, legacy_paths = _create_legacy_upgrade_fixture(target)\n+                stamp_path = target / shiki_installer.INSTALL_STAMP_PATH\n+\n+                if name == \"absent\":\n+                    stamp_path.unlink()\n+                elif name == \"unreadable\":\n+                    stamp_path.unlink()\n+                    stamp_path.mkdir()\n+                elif name == \"malformed\":\n+                    stamp_path.write_text(\"{not-json\\n\", encoding=\"utf-8\")\n+                else:\n+                    stamp = json.loads(stamp_path.read_text(encoding=\"utf-8\"))\n+                    if name == \"missing-digest\":\n+                        stamp[\"digests\"] = {}\n+                    else:\n+                        stamp[\"digests\"] = {\n+                            relative: \"deadbeef\" for relative in legacy_paths\n+                        }\n+                    stamp_path.write_text(\n+                        json.dumps(stamp, indent=2, sort_keys=True) + \"\\n\",\n+                        encoding=\"utf-8\",\n+                    )\n+\n+                before = _tree_snapshot(target)\n+                with self.assertRaises(shiki_installer.ShikiError) as raised:\n+                    _quiet_install(target, force=True, validate=False)\n+                after = _tree_snapshot(target)\n+\n+                self.assertEqual(before, after, f\"{name} preflight changed the tree\")\n+                message = str(raised.exception)\n+                self.assertIn(expected_reason, message)\n+                self.assertIn(\"No files were written\", message)\n+                for relative in legacy_paths:\n+                    self.assertIn(relative, message)\n+\n+    def test_absent_stamp_is_allowed_when_no_legacy_path_exists(self) -> None:\n+        target = self.tmp / \"no-legacy\"\n+        target.mkdir()\n+        _quiet_install(target, force=True, validate=False)\n+        self.assertTrue(\n+            (target / \"docs\" / \"adr\" / \"SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\").is_file()\n+        )\n+\n+\n+if __name__ == \"__main__\":\n+    unittest.main()\n\n2026-08-07T08:13:34.932561Z ERROR codex_models_manager::manager: failed to renew cache TTL: missing field `base_instructions` at line 94 column 5\napply patch\npatch: completed\n/private/tmp/shiki-adr-collision.pa0KwY/.worktrees/shiki-t-20260807t073307221254z-2d22d654-introduc/AGENTS.md\n/private/tmp/shiki-adr-collision.pa0KwY/.worktrees/shiki-t-20260807t073307221254z-2d22d654-introduc/CLAUDE.md\n/private/tmp/shiki-adr-collision.pa0KwY/.worktrees/shiki-t-20260807t073307221254z-2d22d654-introduc/CONTEXT.md\n/private/tmp/shiki-adr-collision.pa0KwY/.worktrees/shiki-t-20260807t073307221254z-2d22d654-introduc/CONTRIBUTING.md\n/private/tmp/shiki-adr-collision.pa0KwY/.worktrees/shiki-t-20260807t073307221254z-2d22d654-introduc/SYSTEM_PROMPT.md\ndiff --git a/.codex/skills/shiki/SKILL.md b/.codex/skills/shiki/SKILL.md\nindex c8e2e85d9d75d49cc4f2d6286ee22659c4e53e75..93cded623b2c15d9f65c76491a1c2d813ac9e6c0\n--- a/.codex/skills/shiki/SKILL.md\n+++ b/.codex/skills/shiki/SKILL.md\n@@ -18,6 +18,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n Codex CLI does not register this skill as a custom `/shiki` slash command. When\n the user types `/shiki` in Codex CLI and gets \"Unrecognized command\", explain\n that this is expected: invoke Shiki in Codex with natural language such as\n@@ -69,7 +72,7 @@\n \n ## Responsibilities\n \n-- Claude Code implements and repairs by default (`shiki runner claude`, ADR 0008); Codex implements and repairs tasks explicitly assigned to `codex`.\n+- Claude Code implements and repairs by default (`shiki runner claude`, SADR-0008); Codex implements and repairs tasks explicitly assigned to `codex`.\n - Claude Code Action can act as GitHub-side CCA or reviewer.\n - CCA judges completion.\n - MergeGate authorizes state transitions and merge readiness.\ndiff --git a/.github/prompts/cca-completion-check.md b/.github/prompts/cca-completion-check.md\nindex ed203ad5f302ecc81372d8ab27d2b1232ca97497..fd89950528333ef6242e6d5443904f5392ab7e56\n--- a/.github/prompts/cca-completion-check.md\n+++ b/.github/prompts/cca-completion-check.md\n@@ -19,6 +19,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n ## Required Reading\n \n Read only the files needed to judge the PR contract. Prefer this order:\ndiff --git a/AGENTS.md b/AGENTS.md\nindex 7f041100d49c2a7525f51cb0f5d696a4a6d1c78c..ea303398018461999e03b6f27be3f4d65833fa32\n--- a/AGENTS.md\n+++ b/AGENTS.md\n@@ -44,10 +44,14 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use\n+`SADR-NNNN-*.md`; target product decisions retain `NNNN-*.md` and the `ADR`\n+namespace.\n+\n Use the canonical source-of-truth order above. Runtime-specific reading orders\n are operational guidance only and must not override or reorder it.\n \n-Conversation state is not durable truth. If a decision matters, put it in GitHub, `.shiki/`, `CONTEXT.md`, or an ADR.\n+Conversation state is not durable truth. If a decision matters, put it in GitHub, `.shiki/`, `CONTEXT.md`, a Shiki SADR, or a target ADR.\n \n If GitHub and `.shiki/` disagree, surface the conflict and prefer GitHub operational state until the mirror is repaired.\n \n@@ -75,8 +79,8 @@\n Every non-trivial change follows this loop:\n \n 1. **Goal**: clarify outcome, completion conditions, non-goals, risk level, and success signals.\n-2. **grill-with-docs**: challenge the plan against domain docs, ADRs, code reality, terminology, and edge scenarios.\n-3. **Context & Impact**: find relevant docs, ADRs, code, dependencies, owners, locks, and verification surfaces.\n+2. **grill-with-docs**: challenge the plan against domain docs, Shiki SADRs, target ADRs, code reality, terminology, and edge scenarios.\n+3. **Context & Impact**: find relevant docs, SADRs, target ADRs, code, dependencies, owners, locks, and verification surfaces.\n 4. **PRD and Spec Freeze**: use `to-prd` when context is settled enough to become durable product/engineering intent. The operator's explicit approval of the PRD is Spec Freeze: the plan records a `spec_freeze` block, and no plan runs without it. Steps 1-4 form Requirements Definition \u2014 one continuous operator dialogue. After Spec Freeze, scope changes require an operator-approved Spec Amendment; non-scope-moving interpretations go to the Assumption Log.\n 5. **Issues**: use `to-issues` to create independently grabbable vertical-slice issues. Prefer AFK slices over HITL slices where possible.\n 6. **Triage**: label issues for readiness, risk, runtime, skills, and MergeGate state.\n@@ -167,7 +171,7 @@\n - Testing decisions.\n - Out-of-scope items.\n - Further notes.\n-- Links to relevant domain terms and ADRs.\n+- Links to relevant domain terms, SADRs, and target ADRs.\n \n Every executable task must include:\n \n@@ -193,7 +197,7 @@\n \n Context & Impact must identify:\n \n-- Relevant documents, domain terms, ADRs, and past decisions.\n+- Relevant documents, domain terms, SADRs, target ADRs, and past decisions.\n - Relevant modules, interfaces, seams, callers, owners, and tests.\n - Dependency relationships between tasks.\n - Candidate locks and lock conflicts.\n@@ -210,7 +214,7 @@\n | Skill | Required when |\n | --- | --- |\n | `setup-matt-pocock-skills` | First configuring the repo for skills, or when issue tracker, triage labels, or domain docs are missing. |\n-| `grill-with-docs` | Default for non-trivial Goals before PRD/issues; always required when plans, terminology, boundaries, tradeoffs, or ADR-worthy decisions are ambiguous. |\n+| `grill-with-docs` | Default for non-trivial Goals before PRD/issues; always required when plans, terminology, boundaries, tradeoffs, or decision-record-worthy choices are ambiguous. |\n | `zoom-out` | The agent lacks an architectural map of the relevant code area. |\n | `to-prd` | Settled Goal context must become durable product/engineering intent. |\n | `to-issues` | A Goal, PRD, or plan must become independently grabbable vertical-slice issues. |\n@@ -348,7 +352,7 @@\n - Three or more new files.\n - Any high-risk or critical label.\n \n-When the architecture gate triggers, run `grill-with-docs` or `improve-codebase-architecture` as appropriate and record durable decisions in an ADR when needed.\n+When the architecture gate triggers, run `grill-with-docs` or `improve-codebase-architecture` as appropriate and record durable decisions in a Shiki SADR or target ADR as applicable.\n \n ## Engineering Discipline\n \ndiff --git a/CLAUDE.md b/CLAUDE.md\nindex 98a932e7841a632866fcd53b0bad0e3af3711d3d..98a31399404d6fb044b4fd493f4459ac87f439de\n--- a/CLAUDE.md\n+++ b/CLAUDE.md\n@@ -14,6 +14,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n ## Claude Role\n \n Default to planner, default implementer, reviewer, coordinator, documentation editor, CCA-style completion judge, and final-judgment assistant.\n@@ -57,7 +60,7 @@\n At the start of a Shiki session:\n \n 1. Read `AGENTS.md`.\n-2. Read `CONTEXT.md` and relevant ADRs.\n+2. Read `CONTEXT.md` and relevant Shiki SADRs and target ADRs.\n 3. Read active `.shiki/` Goal/task/ledger state when present.\n 4. Read active distilled rules from `.shiki/memories/` (status `distilled`, `active: true`, not revoked or superseded) for applicable guidance; the same rules are injected deterministically into task handoffs as the `## Distilled Rules` section (\u00a73.5).\n 5. Read the GitHub Issue or PR when the task is GitHub-backed.\n@@ -74,7 +77,7 @@\n - Define the Goal, completion conditions, non-goals, and risk level.\n - Treat `grill-with-docs` as the default planning entry point for non-trivial Goals.\n - Challenge domain terms against `CONTEXT.md`.\n-- Challenge hard-to-reverse tradeoffs against ADRs.\n+- Challenge hard-to-reverse tradeoffs against SADRs and target ADRs.\n - Explore code instead of asking when code can answer the question.\n - Run Context & Impact before implementation planning.\n - Produce a Task DAG, not an unordered checklist.\n@@ -94,7 +97,7 @@\n - For each question, provide your recommended answer.\n - Resolve dependency decisions before downstream design questions.\n - Update `CONTEXT.md` inline when terminology crystallizes.\n-- Offer ADRs only when the decision is hard to reverse, surprising without context, and a real tradeoff.\n+- Offer a SADR or target ADR only when the decision is hard to reverse, surprising without context, and a real tradeoff.\n - Convert unresolved questions into HITL blockers rather than allowing Codex to guess.\n \n ## PRD and Issues Standard\n@@ -103,7 +106,7 @@\n \n - Use the project's domain glossary vocabulary.\n - Avoid volatile file paths unless they are essential.\n-- Include implementation decisions, testing decisions, out-of-scope items, and relevant ADR/domain links.\n+- Include implementation decisions, testing decisions, out-of-scope items, and relevant SADR/target-ADR/domain links.\n - Identify deep-module opportunities and testable interfaces.\n \n When creating issues:\n@@ -121,7 +124,7 @@\n - Branch/worktree target.\n - Scope and non-goals.\n - Dependencies and locks.\n-- Relevant docs, ADRs, modules, and tests.\n+- Relevant docs, SADRs, target ADRs, modules, and tests.\n - Required skill invocations.\n - TDD expectations.\n - Pre-PR `code-review` gate expectations.\n@@ -152,7 +155,7 @@\n \n - Treat yourself as a judge, not implementer.\n - Do not write production code.\n-- Read `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant ADRs, PRD, task issue, PR body, diff, checks, reviews, labels, and `.shiki/` state.\n+- Read `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant SADRs and target ADRs, PRD, task issue, PR body, diff, checks, reviews, labels, and `.shiki/` state.\n - Evaluate every applicable checklist in `docs/agents/checklists.md`.\n - Map each acceptance criterion to durable evidence.\n - Distinguish implementation failure from missing evidence.\ndiff --git a/CONTEXT.md b/CONTEXT.md\nindex e61a9be06a0cce0d026484083a657bde2067e234..237ee8aa3ea8fab334ece49768bc5ed3b5af0974\n--- a/CONTEXT.md\n+++ b/CONTEXT.md\n@@ -25,7 +25,7 @@\n _Avoid_: jumping directly into code\n \n **grill-with-docs**:\n-A planning skill that challenges a plan against domain language, ADRs, code reality, and concrete edge scenarios. It resolves design-tree questions before PRD/issues. It runs inside Requirements Definition.\n+A planning skill that challenges a plan against domain language, Shiki SADRs, target ADRs, code reality, and concrete edge scenarios. It resolves design-tree questions before PRD/issues. It runs inside Requirements Definition.\n _Avoid_: generic brainstorming, silent assumptions\n \n **Requirements Definition**:\ndiff --git a/CONTRIBUTING.md b/CONTRIBUTING.md\nindex c6d9b656e73abb8a55e5f660123e03153dcb3e6d..9e37a8fbf668db5b22461aaae5548b8f1354394c\n--- a/CONTRIBUTING.md\n+++ b/CONTRIBUTING.md\n@@ -17,7 +17,7 @@\n 1. **Goal** \u2014 start from a user-approved Goal with an outcome, completion\n    conditions, non-goals, risk level, and success signals.\n 2. **grill-with-docs** \u2014 for any non-trivial change, challenge the plan against\n-   domain language, ADRs, and code reality before writing a PRD.\n+   domain language, Shiki SADRs, target ADRs, and code reality before writing a PRD.\n 3. **PRD** \u2014 capture settled product/engineering intent (`to-prd`).\n 4. **Spec Freeze** \u2014 the operator explicitly approves the PRD/requirements;\n    the plan records a `spec_freeze` block and no plan runs without it\n@@ -94,7 +94,7 @@\n   entries.\n - Do not hand-edit `.shiki/goals/*.json` or `.shiki/manifest.json` in a task PR.\n - Conversation state is not durable truth. If a decision matters, record it in\n-  GitHub, `.shiki/`, `CONTEXT.md`, or an ADR.\n+  GitHub, `.shiki/`, `CONTEXT.md`, a Shiki SADR, or a target ADR.\n \n ## Architecture Gate\n \n@@ -103,7 +103,7 @@\n or CI policy, package structure, three or more structural files, more than 100\n net new lines in a core module, three or more new files, or any high-risk label.\n When triggered, run `grill-with-docs` or `improve-codebase-architecture` and\n-record durable decisions in an ADR when needed (see `docs/adr/`).\n+record durable decisions in a Shiki SADR or target ADR as applicable (see `docs/adr/`).\n \n ## Safety\n \ndiff --git a/SYSTEM_PROMPT.md b/SYSTEM_PROMPT.md\nindex aaaea45e3a896c3a44ba89eec8bb84325ecf6c62..61b3c55afe7a5fc5f1a3d475378175db58553c6d\n--- a/SYSTEM_PROMPT.md\n+++ b/SYSTEM_PROMPT.md\n@@ -30,6 +30,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n Use the canonical source-of-truth order above. Runtime-specific reading orders\n are operational guidance only and must not override or reorder it. The current\n conversation is non-durable operator input.\n@@ -55,7 +58,7 @@\n For any non-trivial Goal, use this flow:\n \n 1. **Goal Seek**: define outcome, non-goals, risk level, completion criteria, and evidence requirements.\n-2. **grill-with-docs**: challenge the plan against `CONTEXT.md`, ADRs, code reality, terminology, boundaries, and edge scenarios. Ask one question at a time when the operator is available. If code can answer a question, inspect code instead of asking.\n+2. **grill-with-docs**: challenge the plan against `CONTEXT.md`, Shiki SADRs, target ADRs, code reality, terminology, boundaries, and edge scenarios. Ask one question at a time when the operator is available. If code can answer a question, inspect code instead of asking.\n 3. **Context & Impact**: identify modules, interfaces, seams, callers, dependencies, locks, risk, and verification surfaces.\n 4. **to-prd + Spec Freeze**: turn settled context into a PRD using domain vocabulary and testing decisions. The operator's explicit approval of the PRD is Spec Freeze, recorded as the plan's `spec_freeze` block; plans without it do not run. Steps 1-4 form Requirements Definition.\n 5. **to-issues**: decompose the PRD into independently grabbable vertical-slice issues. Prefer AFK slices; mark HITL slices when judgment is still required.\n@@ -75,7 +78,7 @@\n Before making any material change:\n \n 1. Read `AGENTS.md` and any runtime-specific wrapper such as `CLAUDE.md`.\n-2. Read `CONTEXT.md` and relevant ADRs.\n+2. Read `CONTEXT.md` and relevant SADRs and target ADRs.\n 3. Read the GitHub Issue / PR and `.shiki/` state for the active Goal or task.\n 4. Identify your role: Planner, Implementer, Reviewer, Completion Check Agent, Repairer, or Guardian-assist.\n 5. Identify Goal id, task id, branch, PR, dependencies, locks, risk level, required skills, and required evidence.\n@@ -103,7 +106,7 @@\n \n At minimum, identify:\n \n-- Relevant documents, ADRs, domain terms, and prior decisions.\n+- Relevant documents, SADRs, target ADRs, domain terms, and prior decisions.\n - Relevant modules, interfaces, seams, callers, owners, and tests.\n - Dependency relationships between tasks.\n - Candidate file locks and lock conflicts.\n@@ -152,7 +155,7 @@\n \n CCA must read:\n \n-- `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant ADRs.\n+- `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant SADRs and target ADRs.\n - Parent Goal issue, PRD issue, task issue, comments, and labels.\n - PR body, diff, commits, changed files, reviews, CI checks, and artifacts available to the workflow.\n - `.shiki/` task contract, locks, ledger entries, CCA checklist profile, and prior repair packets.\n@@ -214,7 +217,7 @@\n - Three or more new files.\n - Any high-risk or critical label.\n \n-When the architecture gate triggers, run `grill-with-docs` or `improve-codebase-architecture` as appropriate and record the decision in an ADR if it is hard to reverse or likely to be re-litigated.\n+When the architecture gate triggers, run `grill-with-docs` or `improve-codebase-architecture` as appropriate and record the decision in a Shiki SADR or target ADR as applicable if it is hard to reverse or likely to be re-litigated.\n \n ## 14. Skill Gate\n \n@@ -223,7 +226,7 @@\n Use these skills:\n \n - `setup-matt-pocock-skills`: before first use of the engineering skill set, or when issue tracker, triage labels, or domain docs are missing.\n-- `grill-with-docs`: default for non-trivial Goals before PRD/issues; required for ambiguous plans, terminology, boundaries, ADR-worthy tradeoffs, or design-tree decisions.\n+- `grill-with-docs`: default for non-trivial Goals before PRD/issues; required for ambiguous plans, terminology, boundaries, decision-record-worthy tradeoffs, or design-tree decisions.\n - `zoom-out`: unfamiliar code area or missing architectural map.\n - `to-prd`: convert settled context into a PRD.\n - `to-issues`: break a PRD, plan, or Goal into independently grabbable vertical-slice issues.\ndiff --git a/docs/adr/0001-keep-shiki-as-platform-name.md b/docs/adr/SADR-0001-keep-shiki-as-platform-name.md\nindex 20a9637c3b9d8c97de7ba8db7cfae4d82d9a7dba..8b685439959aada1b221c112f0ac3c51ca8a1695\n--- a/docs/adr/0001-keep-shiki-as-platform-name.md\n+++ b/docs/adr/SADR-0001-keep-shiki-as-platform-name.md\n@@ -1,3 +1,3 @@\n-# Keep Shiki As The Platform Name\n+# SADR-0001: Keep Shiki As The Platform Name\n \n Shiki remains the product name for the GitHub-native agentic engineering control plane. We considered more descriptive names such as MergePilot, MergeGate, and Kiroku, but kept Shiki because it carries the existing goal-seeking framework identity while remaining independent of Hermes, Codex, Claude Code, or any single runtime.\ndiff --git a/docs/adr/0002-use-agents-as-shared-constitution.md b/docs/adr/SADR-0002-use-agents-as-shared-constitution.md\nindex 73de30ecb078120eac540d9bc51855e3cd48d0e3..c5486182bf06fca36ea508496b013c67302fd989\n--- a/docs/adr/0002-use-agents-as-shared-constitution.md\n+++ b/docs/adr/SADR-0002-use-agents-as-shared-constitution.md\n@@ -1,3 +1,3 @@\n-# Use AGENTS As The Shared Agent Constitution\n+# SADR-0002: Use AGENTS As The Shared Agent Constitution\n \n Shiki uses `AGENTS.md` as the runtime-neutral constitution and `CLAUDE.md` as a thin Claude Code wrapper. This keeps Codex, Claude Code, Hermes Runner, and future Agent Runtimes aligned on the same GitHub-first MergeGate rules while still allowing runtime-specific instructions where needed.\ndiff --git a/docs/adr/0003-require-skill-gate-for-mergegate-readiness.md b/docs/adr/SADR-0003-require-skill-gate-for-mergegate-readiness.md\nindex 424be0c9af4c8f0136f1fde68a468c47ff84e764..7fc337987c7df9792f75f181dfcbed75fe5ffff2\n--- a/docs/adr/0003-require-skill-gate-for-mergegate-readiness.md\n+++ b/docs/adr/SADR-0003-require-skill-gate-for-mergegate-readiness.md\n@@ -1,3 +1,3 @@\n-# Require Skill Gate For MergeGate Readiness\n+# SADR-0003: Require Skill Gate For MergeGate Readiness\n \n Shiki requires relevant engineering skills to be invoked before execution when their triggers apply. This makes skills part of MergeGate readiness rather than optional prompt style, because Shiki depends on repeatable planning, issue decomposition, TDD, diagnosis, triage, and architecture review before autonomous or parallel agent work can be trusted.\ndiff --git a/docs/adr/0004-use-github-first-with-shiki-mirror.md b/docs/adr/SADR-0004-use-github-first-with-shiki-mirror.md\nindex d0a3a9174dc646a945a9d325954ffaf00c352040..a79c54ae9ee8eed2a0524192b864da4e22a92d3f\n--- a/docs/adr/0004-use-github-first-with-shiki-mirror.md\n+++ b/docs/adr/SADR-0004-use-github-first-with-shiki-mirror.md\n@@ -1,4 +1,4 @@\n-# ADR 0004: Use GitHub First With A Repository-Local Shiki Mirror\n+# SADR-0004: Use GitHub First With A Repository-Local Shiki Mirror\n \n ## Status\n \ndiff --git a/docs/adr/0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md b/docs/adr/SADR-0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\nindex b339baf49b89d9290e79c3d51b36e7b2bde3bd95..c30a0d8b1fde0224d0ac7820e7870dccf721580d\n--- a/docs/adr/0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\n+++ b/docs/adr/SADR-0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\n@@ -1,4 +1,4 @@\n-# ADR 0005: Use Codex As Front And Claude Code Action As The GitHub Runtime\n+# SADR-0005: Use Codex As Front And Claude Code Action As The GitHub Runtime\n \n ## Status\n \ndiff --git a/docs/adr/0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md b/docs/adr/SADR-0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\nindex 4bcae9d1e52f0a37b8e9fbafa5cd14b6ee1993bd..7a38538a6ae9929567f0ade67e7ea6a60d0b3f7e\n--- a/docs/adr/0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\n+++ b/docs/adr/SADR-0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\n@@ -1,4 +1,4 @@\n-# ADR 0006: Separate Codex Implementation, CCA Completion, And MergeGate Authorization\n+# SADR-0006: Separate Codex Implementation, CCA Completion, And MergeGate Authorization\n \n ## Status\n \ndiff --git a/docs/adr/0007-packaging-and-release.md b/docs/adr/SADR-0007-packaging-and-release.md\nindex 79b84ddb97167372b44641e5b7860e400ce80822..bb445a2e2e856d9d1f5fdbe35de8d4e8cec0fb32\n--- a/docs/adr/0007-packaging-and-release.md\n+++ b/docs/adr/SADR-0007-packaging-and-release.md\n@@ -1,4 +1,4 @@\n-# ADR 0007: Packaging And Release For Shiki\n+# SADR-0007: Packaging And Release For Shiki\n \n ## Status\n \n@@ -25,7 +25,7 @@\n   not introduce third-party runtime dependencies or break the shim.\n - Releases must run inside the operator's subscription-authenticated toolchain\n   and must not require extra secrets beyond the built-in `GITHUB_TOKEN`\n-  (consistent with ADR 0005's no-API-key-by-default stance).\n+  (consistent with SADR-0005's no-API-key-by-default stance).\n - `.shiki/manifest.json` already encodes the install surface (`install.include`,\n   `install.create_directories`, `install.exclude_from_commit`), and it must not\n   be mutated as part of this change.\n@@ -34,7 +34,7 @@\n \n This is a hard-to-reverse platform decision (it sets the version source of\n truth, the release trigger, and the public install contract), so it is recorded\n-as an ADR.\n+as a SADR.\n \n ## Decision\n \n@@ -104,7 +104,7 @@\n 3. **Publishing to PyPI.** Rejected for now: Shiki is distributed as a\n    repository template + git-installable CLI, not a PyPI library; PyPI would add\n    credentials/secrets and a different versioning contract. Can be added later as\n-   its own ADR if a packaged distribution is needed.\n+   its own SADR if a packaged distribution is needed.\n 4. **Dynamic version (read `VERSION` via a build backend `dynamic` field).**\n    Rejected to keep `pyproject.toml` backend-agnostic and dependency-free; the\n    small duplication between `VERSION` and `pyproject.toml` is guarded by the\n@@ -123,4 +123,4 @@\n   releasing versions the whole platform but ships only `install.include` to\n   targets.\n - Future PyPI distribution or a console entry point are deferred and would each\n-  warrant their own ADR.\n+  warrant their own SADR.\ndiff --git a/docs/adr/0008-use-claude-code-as-default-implementer.md b/docs/adr/SADR-0008-use-claude-code-as-default-implementer.md\nindex dedf4510b7c7799521422f622029563d373db031..35c2625d908167fbc58855b5b63e283805f167e8\n--- a/docs/adr/0008-use-claude-code-as-default-implementer.md\n+++ b/docs/adr/SADR-0008-use-claude-code-as-default-implementer.md\n@@ -1,12 +1,12 @@\n-# ADR 0008: Use Claude Code As The Default Implementer Runtime\n+# SADR-0008: Use Claude Code As The Default Implementer Runtime\n \n ## Status\n \n-Proposed (supersedes ADR 0005 for the implementer default; keeps its auth model)\n+Proposed (supersedes SADR-0005 for the implementer default; keeps its auth model)\n \n ## Context\n \n-ADR 0005 made Codex Front the default implementation runtime, with Claude Code\n+SADR-0005 made Codex Front the default implementation runtime, with Claude Code\n restricted to planner and reviewer roles by the runtime registry, and the only\n autonomous implementation adapter was `shiki runner codex`.\n \n@@ -18,13 +18,13 @@\n - The platform's planning, review, and CCA layers already run on Claude Code,\n   and Claude Code's native capabilities (the built-in `/code-review` skill and\n   the Workflow multi-agent orchestration tool) have no Codex equivalent.\n-- The Goal lifecycle redesign (ADR 0009) requires an autonomous post-freeze\n+- The Goal lifecycle redesign (SADR-0009) requires an autonomous post-freeze\n   loop driven from the operator's Claude Code session, including bounded\n   repair dispatch, which must not depend on a runtime that cannot be invoked\n   unattended.\n \n The constitution claims Shiki is runtime-agnostic (\"not a Claude-only\n-workflow\", ADR 0002). Any change to the implementer default must preserve the\n+workflow\", SADR-0002). Any change to the implementer default must preserve the\n registry-based runtime model rather than hardcoding one vendor.\n \n ## Decision\n@@ -39,7 +39,7 @@\n   the task to review or repair-needed.\n - Keep Codex registered as an optional implementer runtime. `shiki runner\n   codex`, the Codex handoff templates, and the ChatGPT OAuth auth model from\n-  ADR 0005 remain supported for tasks explicitly assigned to `codex`.\n+  SADR-0005 remain supported for tasks explicitly assigned to `codex`.\n - Commit to mandating two Claude-native capabilities as loop contract in\n   follow-up Goals (status: the code-review gate was delivered by Goal G-B \u2014\n   skill registry, checklists item PR-12, default required_skills \u2014 while the\n@@ -60,7 +60,7 @@\n \n Out of scope: removing Codex support, changing the CCA runtime (Claude Code\n Action with `CLAUDE_CODE_OAUTH_TOKEN` stays), and API-key based automation\n-(still requires its own ADR per ADR 0005).\n+(still requires its own target ADR per SADR-0005).\n \n ## Consequences\n \n@@ -100,7 +100,7 @@\n   harness rejection of direct `codex exec` is outside Shiki's control, and the\n   plan's mandatory native capabilities cannot run on Codex at all.\n - **Remove Codex entirely**: rejected; it abandons the runtime-agnostic\n-  constitution (ADR 0002) for no operational gain and forecloses\n+  constitution (SADR-0002) for no operational gain and forecloses\n   cross-runtime adversarial review.\n - **GitHub Actions cloud implementation via claude-code-action**: rejected as\n   default; durable evidence is automatic but token cost, Node 24 workflow\ndiff --git a/docs/adr/0009-requirements-definition-and-spec-freeze.md b/docs/adr/SADR-0009-requirements-definition-and-spec-freeze.md\nindex 29b3f7871bef9203a8b4a831a65c39eb2f506e42..52e7dd69ad0aa4efe0b68c62678abbf7df02aaec\n--- a/docs/adr/0009-requirements-definition-and-spec-freeze.md\n+++ b/docs/adr/SADR-0009-requirements-definition-and-spec-freeze.md\n@@ -1,4 +1,4 @@\n-# ADR 0009: Merge Grill And Planning Into Requirements Definition With Spec Freeze\n+# SADR-0009: Merge Grill And Planning Into Requirements Definition With Spec Freeze\n \n ## Status\n \ndiff --git a/docs/adr/0010-external-ai-guardian-review.md b/docs/adr/SADR-0010-external-ai-guardian-review.md\nindex e6108bbe8bb4e73e0ff4684c8f78001a80438c4e..f3510b145664298adafb151b6f0db51b972f01ec\n--- a/docs/adr/0010-external-ai-guardian-review.md\n+++ b/docs/adr/SADR-0010-external-ai-guardian-review.md\n@@ -1,4 +1,4 @@\n-# ADR 0010: External AI Guardian Review As A First-Class Approval Authority\n+# SADR-0010: External AI Guardian Review As A First-Class Approval Authority\n \n ## Status\n \n@@ -8,7 +8,7 @@\n \n Shiki is intended to be an autonomous, recursive, self-improving agent\n framework: authority-in-the-loop, not human-in-the-loop. Until now the Guardian\n-approval required for high/critical-risk merges (guardian-policy.json, ADR\n+approval required for high/critical-risk merges (guardian-policy.json, SADR\n governance) recognized only human authority sources \u2014 a GitHub review approval,\n the `guardian:approved` label applied by a configured Guardian, or a comment\n carrying `Guardian approval granted` plus the current head SHA.\n@@ -89,7 +89,7 @@\n   autonomous design the operator intends and produced the impersonation\n   incident.\n - **Record AI approval as an operator comment**: rejected as forgery of human\n-  authority \u2014 the exact failure this ADR corrects.\n+  authority \u2014 the exact failure this SADR corrects.\n - **Deliver the artifact as a committed `.shiki/` file**: rejected; a committed\n   approval inside the PR being approved is forgeable by anyone who can push.\n - **A dedicated reviewer bot identity instead of a relayed comment**: deferred;\ndiff --git a/docs/adr/0011-autonomous-quality-gate-evidence.md b/docs/adr/SADR-0011-autonomous-quality-gate-evidence.md\nindex bca0246c5d46e44afbde15486a75eec425fc02f5..9c93eac16d8c3a6d4b11bd7224e3955102cd7db4\n--- a/docs/adr/0011-autonomous-quality-gate-evidence.md\n+++ b/docs/adr/SADR-0011-autonomous-quality-gate-evidence.md\n@@ -1,4 +1,4 @@\n-# ADR 0011: Autonomous Quality-Gate Evidence \u2014 Deterministic Facts And An Independent Verifier\n+# SADR-0011: Autonomous Quality-Gate Evidence \u2014 Deterministic Facts And An Independent Verifier\n \n ## Status\n \ndiff --git a/docs/adr/0012-autonomous-loop-closeout-to-main.md b/docs/adr/SADR-0012-autonomous-loop-closeout-to-main.md\nindex 637fad6dde4989c9c2bf0f81f676d8d29991123c..ec7d85f81030a676bcbba28b43eb365422a36eac\n--- a/docs/adr/0012-autonomous-loop-closeout-to-main.md\n+++ b/docs/adr/SADR-0012-autonomous-loop-closeout-to-main.md\n@@ -1,4 +1,4 @@\n-# 0012 \u2014 Autonomous loop pushes goal completion to main via a closeout PR\n+# SADR-0012 \u2014 Autonomous loop pushes goal completion to main via a closeout PR\n \n - Status: accepted\n - Date: 2026-06-18\ndiff --git a/docs/adr/0013-bootstrap-grants-actions-approve-pull-requests.md b/docs/adr/SADR-0013-bootstrap-grants-actions-approve-pull-requests.md\nindex b6c69d64f1c5ced346b88d6859586715b3b5bd0e..cf48ff4a6716267f539ca58edd1a852e7c7a2d64\n--- a/docs/adr/0013-bootstrap-grants-actions-approve-pull-requests.md\n+++ b/docs/adr/SADR-0013-bootstrap-grants-actions-approve-pull-requests.md\n@@ -1,4 +1,4 @@\n-# ADR 0013: Bootstrap Grants GitHub Actions Permission To Create And Approve Pull Requests\n+# SADR-0013: Bootstrap Grants GitHub Actions Permission To Create And Approve Pull Requests\n \n ## Status\n \n@@ -31,7 +31,7 @@\n security tradeoff: it lets repository automation satisfy a human-review gate. In\n a multi-maintainer repo that could let a compromised or buggy workflow\n self-approve changes. So whether bootstrap turns it on by default \u2014 and with\n-what default token scope \u2014 is an ADR-worthy decision, not an implementation\n+what default token scope \u2014 is a SADR-worthy decision, not an implementation\n detail.\n \n ## Decision\n@@ -48,7 +48,7 @@\n Specifically:\n \n - **`can_approve_pull_request_reviews` defaults to `true`.** Shiki's default\n-  posture is solo/AFK autonomous operation (ADR 0008/0009/0012), where the CCA\n+  posture is solo/AFK autonomous operation (SADR-0008/0009/0012), where the CCA\n   Review Bridge is the only path that can satisfy `required_review: true` after\n   the CCA and Guardian gates have already passed. The default makes a\n   freshly-bootstrapped repo able to run the autonomous loop end to end without a\ndiff --git a/docs/adr/0014-codex-app-external-ai-guardian-ui-adapter.md b/docs/adr/SADR-0014-codex-app-external-ai-guardian-ui-adapter.md\nindex 238db06279334caa341c117362df9271382a7fcd..65f12e9939321e1b3e389a34c822fc1549e5155d\n--- a/docs/adr/0014-codex-app-external-ai-guardian-ui-adapter.md\n+++ b/docs/adr/SADR-0014-codex-app-external-ai-guardian-ui-adapter.md\n@@ -1,4 +1,4 @@\n-# ADR 0014: Use Codex App As The External AI Guardian UI Adapter\n+# SADR-0014: Use Codex App As The External AI Guardian UI Adapter\n \n ## Status\n \ndiff --git a/docs/adr/0015-contract-approval-before-dispatch.md b/docs/adr/SADR-0015-contract-approval-before-dispatch.md\nindex fad6c656741a20a7684ba16080a7db159f63a52f..6061271a46d3f1d6ae7174477de3fb75673123a6\n--- a/docs/adr/0015-contract-approval-before-dispatch.md\n+++ b/docs/adr/SADR-0015-contract-approval-before-dispatch.md\n@@ -1,4 +1,4 @@\n-# ADR 0015: Approve The Task Contract Before Dispatch, Not The Diff After It\n+# SADR-0015: Approve The Task Contract Before Dispatch, Not The Diff After It\n \n ## Status\n \n@@ -9,7 +9,7 @@\n Guardian approval for high/critical risk is expressible only on a pull request.\n Every approval source in `guardian-policy.json` \u2014 GitHub review, the\n `guardian:approved` label, the `Guardian approval granted` comment, and the\n-`external_ai_guardian_review` artifact (ADR 0010/0014) \u2014 is bound to a PR, and\n+`external_ai_guardian_review` artifact (SADR-0010/0014) \u2014 is bound to a PR, and\n three of the four require the current head SHA. Approval therefore cannot exist\n before an implementation PR exists.\n \n@@ -81,7 +81,7 @@\n An envelope breach must surface only on the non-repairable policy gate, never on\n the repairable `MergeGate metadata check`. Otherwise the Goal Loop routes it to\n `dispatch_repair` \u2014 instructing a runner to make the breach go away \u2014 which is\n-exactly what this ADR forbids.\n+exactly what this SADR forbids.\n \n **Contract immutability.** After registration, an implementation PR must not\n change the governance fields of its own task contract (`scope`, `non_goals`,\n@@ -116,7 +116,7 @@\n fails any verdict that is not `complete`, and the signal the CCA prompt reads is\n computed from `evaluate_guardian_approval` alone. Without the OR-merge at the\n signal, a contract-approved PR still returns `needs_guardian`, the CCA job fails,\n-and the MergeGate policy check never runs \u2014 the gate this ADR changes would never\n+and the MergeGate policy check never runs \u2014 the gate this SADR changes would never\n be reached. The CCA *prompt* needs no change: a `contract_approval` entry in the\n signal's existing `sources` array satisfies its current wording.\n \n@@ -130,7 +130,7 @@\n written, and an approved contract lets its implementation PRs merge through the\n same automatic path as low/medium risk. The wasted implement-then-block cycle\n disappears. Guardian review shifts from reading diffs to reading contracts, which\n-is both earlier and smaller; the external AI Guardian path (ADR 0010/0014) works\n+is both earlier and smaller; the external AI Guardian path (SADR-0010/0014) works\n on contracts with no change.\n \n We accept that the Guardian authorizes a class of change without seeing its\n@@ -146,7 +146,7 @@\n adversarial tests for the new approval path equivalent in rigor to\n `test_shiki_governance_evidence.sh`.\n \n-Bootstrap costs, which apply only while this ADR is being implemented. The tasks\n+Bootstrap costs, which apply only while this SADR is being implemented. The tasks\n that build Contract Approval merge under the existing rules, so the ones\n dispatched after the pre-dispatch gate lands but before the approval path lands\n need a Guardian-approved Contract PR *and* a live Guardian approval on their\n@@ -210,7 +210,7 @@\n risk; they do not bound authorized work.\n \n **Auto-escalate risk on envelope breach instead of stopping.** Returns the\n-workflow to a diff-time Guardian decision \u2014 the exact behavior this ADR removes.\n+workflow to a diff-time Guardian decision \u2014 the exact behavior this SADR removes.\n \n **Resolve governance fields from the base snapshot only.** Rejected against the\n registration flow: implementation PRs add their own contract, so the base has no\ndiff --git a/docs/adr/0016-locks-bound-product-paths.md b/docs/adr/SADR-0016-locks-bound-product-paths.md\nindex b5e850f3f1f7f4ef6e7f9d1763a29a51bd300c45..46b40a9b99a7aae644c02bb458f5473ff0a90ce2\n--- a/docs/adr/0016-locks-bound-product-paths.md\n+++ b/docs/adr/SADR-0016-locks-bound-product-paths.md\n@@ -1,4 +1,4 @@\n-# ADR 0016: Locks bound product paths; the Shiki mirror is bound by record identity\n+# SADR-0016: Locks bound product paths; the Shiki mirror is bound by record identity\n \n ## Status\n \n@@ -6,8 +6,8 @@\n \n - Date: 2026-07-28\n - Deciders: operator (mizutani-140), Claude (planner)\n-- Related: ADR 0004 (GitHub-first with a `.shiki` mirror), ADR 0009 (Spec Freeze),\n-  ADR 0012 (autonomous loop closeout to main), ADR 0015 (Contract Approval before\n+- Related: SADR-0004 (GitHub-first with a `.shiki` mirror), SADR-0009 (Spec Freeze),\n+  SADR-0012 (autonomous loop closeout to main), SADR-0015 (Contract Approval before\n   dispatch), remediation goal task R-02, the live PR #179 deadlock.\n \n ## Context\n@@ -90,11 +90,11 @@\n files, documentation files and named source modules are nameable; runtime-\n allocated ledger, runner and report ids are not \u2014 and those live in `.shiki`,\n which locks no longer govern. Existing frozen contracts are brought into line by\n-a one-time Spec Amendment per plan, which must land before ADR 0015's contract\n+a one-time Spec Amendment per plan, which must land before SADR-0015's contract\n immutability makes `locks` unwritable from the implementation PR.\n \n **Lock state is resolved from the durable side.** MergeGate resolves the lock\n-registry, and ADR 0015's governance fields, from the base `.shiki` snapshot when\n+registry, and SADR-0015's governance fields, from the base `.shiki` snapshot when\n present, falling back to head only when absent. The `MergeGate metadata check`\n job is given `--base-shiki`, which it does not receive today. Locks are not\n derived from the live open-pull-request set: a pull request body is untrusted\n@@ -108,7 +108,7 @@\n Explicitly out of scope: moving MergeGate's contract resolution off the PR\n checkout wholesale; making the mirror a projection of GitHub state; introducing\n new lock kinds; moving ledger evidence out of the repository; and unwinding\n-ADR 0012's closeout pull request. Registration of multi-task goals is ADR 0015's\n+SADR-0012's closeout pull request. Registration of multi-task goals is SADR-0015's\n Contract PR and is not re-decided here.\n \n ## Consequences\n@@ -127,7 +127,7 @@\n \n Risks accepted. First, goal-scoped identity is only as strong as `goal_id`,\n which is resolved from the PR body and the head task file; the mitigation is to\n-add `goal_id` to ADR 0015's frozen governance field set, and until that lands a\n+add `goal_id` to SADR-0015's frozen governance field set, and until that lands a\n task file can name a foreign goal. Second, base-when-present resolution is\n unavailable on fork pull requests, where the head is contributor-controlled;\n fork pull requests already cannot complete a Shiki merge, and this decision does\n@@ -139,7 +139,7 @@\n path, not by implementation pull requests.\n \n New obligations: one Spec Amendment per frozen plan to rewrite `locks`, ordered\n-before ADR 0015 T1; extension of the adversarial suites in\n+before SADR-0015 T1; extension of the adversarial suites in\n `scripts/test_shiki_adversarial_state.sh` and\n `scripts/test_shiki_governance_evidence.sh` to cover the eleven newly-scoped\n directories; and updating `scripts/test_shiki_loop_lock_guard.sh`,\n@@ -151,32 +151,32 @@\n whether the loop's transitive sync of ledger `evidence` references can be\n deleted once the Contract PR carries its own source plan.\n \n-## Relationship to ADR 0004\n+## Relationship to SADR-0004\n \n-This ADR **clarifies something ADR 0004 left unspecified**. ADR 0004 decides that\n+This SADR **clarifies something SADR-0004 left unspecified**. SADR-0004 decides that\n GitHub is the operational source of truth and that each target repository keeps a\n `.shiki/` mirror. It says nothing about pull requests writing the mirror, does\n not name MergeGate, and does not make the mirror an input to a gate. Nothing in\n-ADR 0004's Decision or Consequences is amended, superseded or weakened: the\n+SADR-0004's Decision or Consequences is amended, superseded or weakened: the\n mirror still exists, still records the same artifacts, and pull request\n-boundaries remain the coordination surface. What this ADR settles is the question\n+boundaries remain the coordination surface. What this SADR settles is the question\n 0004 never reached \u2014 by what authority a pull request may write a mirror record \u2014\n and it answers it the way `.shiki/manifest.json` already declares\n (`pr_mutation: current-task-or-current-goal-only`).\n \n-It **amends ADR 0012**, whose Decision item 5 states that \"Loop-executed tasks\n+It **amends SADR-0012**, whose Decision item 5 states that \"Loop-executed tasks\n declare the `path:.shiki/**` lock, which covers every `.shiki` file the closeout\n stages ... so `files_outside_locks` is satisfied without per-file lock\n bookkeeping.\" That clause is replaced: the closeout's mirror writes are\n-authorized by record identity, not by a declared lock. ADR 0012's substantive\n+authorized by record identity, not by a declared lock. SADR-0012's substantive\n decision \u2014 that completion reaches `main` through a closeout pull request rather\n than local attestation \u2014 is preserved unchanged.\n \n-It is **complementary to ADR 0015**, not a substitute. ADR 0015 owns contract\n+It is **complementary to SADR-0015**, not a substitute. SADR-0015 owns contract\n immutability, the pre-dispatch gate, the Contract PR and the Scope Envelope. This\n-ADR owns what a lock means and who may write the mirror. The one ordering\n+SADR owns what a lock means and who may write the mirror. The one ordering\n constraint between them is that the lock-narrowing Spec Amendment must precede\n-ADR 0015 T1's freezing of the `locks` field.\n+SADR-0015 T1's freezing of the `locks` field.\n \n ## Alternatives Considered\n \n@@ -186,7 +186,7 @@\n the only writer that can register them may run. Migrating the Guardian signal to\n that store makes every registration PR fail closed to `needs_guardian` at every\n risk level, because the task file is by definition absent from the store, which\n-removes the low/medium autonomous path ADR 0008/0009 exists to provide. And it\n+removes the low/medium autonomous path SADR-0008/0009 exists to provide. And it\n leaves schemas, manifest, config, guardian policy, templates, migrations, plans,\n worktrees, runner records, runs and handoffs with no writer at all in a\n repository whose product is `.shiki`. Measured benefit on the frozen backlog:\n@@ -199,14 +199,14 @@\n namespace is not in fact ownership-partitioned \u2014 56 ledger entries carry no\n `task_id`, and one goal DAG is writable by 35 registered tasks. The correct\n expression of its insight is that locks do not govern the mirror at all, which is\n-what this ADR decides.\n+what this SADR decides.\n \n **Make the mirror a projection written only by a registration writer and a\n post-merge projection job.** Rejected. It requires a bypass actor with write\n access to the protected default branch; it makes its own master invariant\n readable from a manifest that pull requests may edit; it deletes both reconcile\n modes and leaves a corrupted mirror with no in-band repair path, contradicting\n-ADR 0004's consequence that mirror conflicts are repaired against GitHub state;\n+SADR-0004's consequence that mirror conflicts are repaired against GitHub state;\n and its content-derived ids are rejected by `validate_shiki`'s currently-green id\n pattern.\n \ndiff --git a/docs/adr/0017-bookkeeping-closeouts-do-not-inherit-risk.md b/docs/adr/SADR-0017-bookkeeping-closeouts-do-not-inherit-risk.md\nindex 554e3a0d526a72ca067108c5464cc68c3edcf174..b9676857a4713850e063af7c39c8e0b01ecb4c66\n--- a/docs/adr/0017-bookkeeping-closeouts-do-not-inherit-risk.md\n+++ b/docs/adr/SADR-0017-bookkeeping-closeouts-do-not-inherit-risk.md\n@@ -1,4 +1,4 @@\n-# ADR 0017: A Bookkeeping Closeout Does Not Inherit Its Task's Risk\n+# SADR-0017: A Bookkeeping Closeout Does Not Inherit Its Task's Risk\n \n ## Status\n \n@@ -6,7 +6,7 @@\n \n ## Context\n \n-ADR 0012 made the goal loop push a task's completion to the default branch through a\n+SADR-0012 made the goal loop push a task's completion to the default branch through a\n closeout pull request: after the implementation PR merges, the loop opens a second PR\n carrying `task.status = done`, `lock.state = released`, and \u2014 when the task completes\n its goal \u2014 `goal.status = complete` plus one scorecard report. Nothing else. The\n@@ -49,14 +49,14 @@\n 4. The task file's governance fields (`scope`, `non_goals`, `required_skills`,\n    `risk_level`, `locks`, `acceptance_checks`, `test_command`) are byte-identical to\n    the base snapshot. Only `status`, `expected_pr`, `closeout_pr`, `ledger_evidence`\n-   and `expected_branch` may differ \u2014 the ADR 0015 contract-immutability rule,\n+   and `expected_branch` may differ \u2014 the SADR-0015 contract-immutability rule,\n    unchanged.\n \n    `expected_branch` is in that set because a closeout is *by construction* on a\n    different branch from the implementation it closes, and the loop rewrites the\n    field accordingly. It is safe to let it differ because MergeGate independently\n    requires `expected_branch` to equal the PR's actual head ref, so a PR cannot set\n-   it to anything other than the branch it is really on. The first draft of this ADR\n+   it to anything other than the branch it is really on. The first draft of this SADR\n    omitted it, which made the exemption unreachable: every real closeout failed\n    condition 4 on a field the loop always changes. See PR #209.\n 5. The state transitions are exactly the terminal set: task `review` \u2192 `done`, lock\n@@ -113,12 +113,12 @@\n classification must be derived, not asserted.\n \n **Fold the closeout into the implementation PR** so no second PR exists. Rejected under\n-ADR 0012, which recorded why: a task file that is already `done` on the implementation\n+SADR-0012, which recorded why: a task file that is already `done` on the implementation\n branch short-circuits the loop's review \u2192 CCA \u2192 merge \u2192 done ordering into a local\n goal-completion before any merge happens.\n \n **Have the loop auto-merge closeouts with elevated credentials.** Rejected outright: that\n-is a runtime authoring its own approval, which ADR 0010 and the constitution's\n+is a runtime authoring its own approval, which SADR-0010 and the constitution's\n approval-record prohibition forbid. It would also be indistinguishable, in the evidence,\n from the forgery those rules exist to prevent.\n \ndiff --git a/docs/adr/0018-carried-guardian-approval-across-a-proven-base-sync.md b/docs/adr/SADR-0018-carried-guardian-approval-across-a-proven-base-sync.md\nindex 22358562da390ed8b45cd600da08a43d378b4680..e6e2907cefa776460af73ddf7d39db605f3c39b0\n--- a/docs/adr/0018-carried-guardian-approval-across-a-proven-base-sync.md\n+++ b/docs/adr/SADR-0018-carried-guardian-approval-across-a-proven-base-sync.md\n@@ -1,4 +1,4 @@\n-# ADR 0018: Carried Guardian Approval Across A Proven Base Sync\n+# SADR-0018: Carried Guardian Approval Across A Proven Base Sync\n \n ## Status\n \n@@ -34,7 +34,7 @@\n Guardian evaluator that consumes it (`shiki_guardian.evaluate_guardian_approval`,\n the `guardian_comment_carried` source) were built and tested in isolation. Neither\n does anything until a caller passes the flags that turn the carry on, so the\n-feature was invisible in CI. This ADR records the decision the two of them\n+feature was invisible in CI. This SADR records the decision the two of them\n implement, and this task wires it into the four CI invocations that judge Guardian\n approval.\n \ndiff --git a/docs/adr/README.md b/docs/adr/README.md\nindex c70e057a4520c36e042ab68c53cd4a21ef709b71..5da43856836e2ce6a150f8127766e2f88355ab4b\n--- a/docs/adr/README.md\n+++ b/docs/adr/README.md\n@@ -1,40 +1,41 @@\n-# Architecture Decision Records\n+# Decision Record Namespaces\n \n-This directory holds Shiki's Architecture Decision Records (ADRs): durable\n-records of hard-to-reverse platform decisions. ADRs are part of the Shiki source\n-of truth alongside GitHub state, the `.shiki/` mirror, and `CONTEXT.md`.\n+This directory holds two independent decision namespaces:\n \n-## How ADRs Work\n+- Shiki Architecture Decision Records (SADRs), named\n+  `SADR-NNNN-<short-slug>.md`, record hard-to-reverse Shiki platform decisions.\n+- Target Architecture Decision Records (ADRs), named\n+  `NNNN-<short-slug>.md`, belong to the target product.\n \n-- New ADRs are added as `docs/adr/NNNN-<short-slug>.md`, where `NNNN` is the next\n-  zero-padded sequence number.\n-- The **canonical list of ADRs is the set of files matching `docs/adr/*.md`** in\n-  this directory. Browse `docs/adr/*.md` for the authoritative, current set.\n-- Use [`template.md`](template.md) as the starting point for a new ADR.\n-- Record an ADR when a decision is hard to reverse, surprising without context,\n-  and a real tradeoff. Do not write an ADR for routine or easily reversible\n-  choices.\n+The same four-digit number may appear once in each namespace. A platform\n+`SADR-NNNN` citation resolves only to `SADR-NNNN-*.md`; a target `ADR NNNN`\n+citation resolves only to `NNNN-*.md`.\n \n-Using the `docs/adr/*.md` glob as the source of truth means other PRs can add an\n-ADR without editing this index file, avoiding merge contention on a\n-hand-maintained enumeration.\n+## How Decision Records Work\n \n-## Current ADRs (snapshot)\n-\n-The list below is a convenience snapshot and may lag behind the directory. When\n-in doubt, list `docs/adr/*.md`.\n+- New Shiki platform decisions use the next zero-padded SADR number.\n+- New target product decisions use the target's own ADR sequence.\n+- The canonical list is the files in `docs/adr/`; no hand-maintained index is\n+  authoritative.\n+- Use [`template.md`](template.md) as the starting point and choose the filename\n+  namespace that owns the decision.\n+- Record a decision when it is hard to reverse, surprising without context, and\n+  a real tradeoff. Do not write a decision record for routine or easily\n+  reversible choices.\n \n-- [0001 \u2014 Keep Shiki As The Platform Name](0001-keep-shiki-as-platform-name.md)\n-- [0002 \u2014 Use AGENTS As The Shared Agent Constitution](0002-use-agents-as-shared-constitution.md)\n-- [0003 \u2014 Require Skill Gate For MergeGate Readiness](0003-require-skill-gate-for-mergegate-readiness.md)\n-- [0004 \u2014 Use GitHub First With A Repository-Local Shiki Mirror](0004-use-github-first-with-shiki-mirror.md)\n-- [0005 \u2014 Use Codex As Front And Claude Code Action As The GitHub Runtime](0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md)\n-- [0006 \u2014 Separate Codex Implementation, CCA Completion, And MergeGate Authorization](0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md)\n+Using the directory as the source of truth lets independent changes add records\n+without contending on an enumerated index.\n \n-## Adding A New ADR\n+## Adding A Shiki SADR\n \n-1. Copy `template.md` to `docs/adr/NNNN-<short-slug>.md`.\n+1. Copy `template.md` to `docs/adr/SADR-NNNN-<short-slug>.md`.\n 2. Fill in the title, status, context, decision, and consequences.\n-3. Set the status to `Proposed`, then `Accepted` once the decision is approved.\n-4. Open a PR following `CONTRIBUTING.md`. You do not need to edit this index for\n-   the ADR to be discoverable via `docs/adr/*.md`.\n+3. Set the status to `Proposed`, then `Accepted` once the proper authority has\n+   approved the decision.\n+4. Open a PR following `CONTRIBUTING.md`.\n+\n+## Adding A Target ADR\n+\n+1. Copy `template.md` to `docs/adr/NNNN-<short-slug>.md` using the target's own\n+   sequence.\n+2. Cite it as `ADR NNNN`; do not prefix its filename with `SADR-`.\ndiff --git a/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md b/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\nnew file mode 100644\nindex 0000000000000000000000000000000000000000..7455ce1f3bed02ddddb851ac21a1d1a648c9aeba\n--- /dev/null\n+++ b/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\n@@ -0,0 +1,55 @@\n+# SADR-0019: Separate Shiki Platform SADRs From Target ADRs\n+\n+## Status\n+\n+Accepted\n+\n+## Context\n+\n+Shiki installs its platform decision records into `docs/adr/` in every target\n+repository. Shiki and its targets previously used the same numeric\n+`NNNN-<slug>.md` namespace. A target that already owned ADRs 0013 through 0018\n+therefore acquired two records for each number during installation, making an\n+`ADR NNNN` citation ambiguous. Renumbering target decisions would rewrite target\n+history, while renumbering Shiki decisions per target would destroy their stable\n+platform identity.\n+\n+Upgrades also need to remove the former Shiki-owned numeric paths without ever\n+mistaking a target-owned ADR for platform content. The install stamp already\n+records exact shipped paths and content digests, so it can prove ownership only\n+when both still match.\n+\n+## Decision\n+\n+Shiki platform decisions use the `SADR` namespace:\n+\n+- records are named `docs/adr/SADR-NNNN-<slug>.md`;\n+- live platform citations use `SADR-NNNN`;\n+- target product decisions retain `docs/adr/NNNN-<slug>.md` and `ADR NNNN`;\n+- the reference validator resolves the two namespaces independently, so the same\n+  four-digit number may exist once in each namespace.\n+\n+Before a forced upgrade writes anything, the installer inventories the exact\n+former numeric Shiki ADR paths. It may delete a legacy path only when the\n+existing install stamp contains that exact path and its recorded SHA-256 digest\n+matches the file currently on disk. All ownership blockers are reported\n+together and abort the upgrade before its first write. A non-forced install\n+never performs this cleanup.\n+\n+Historical `.shiki/` evidence remains unchanged because it records the namespace\n+that existed when that evidence was produced.\n+\n+## Consequences\n+\n+- Targets may use any ADR number without colliding with Shiki's platform SADRs.\n+- Fresh installs preserve target ADR bytes and install a stable SADR set.\n+- Forced upgrades fail closed when legacy ownership cannot be proven, and a\n+  successful upgrade refreshes the stamp without removed legacy paths.\n+- Current governance, packaging, workflow, prompt, script, and skill references\n+  must move with the platform records.\n+\n+## Alternatives Considered\n+\n+- Collision-only refusal leaves affected targets permanently unupgradable.\n+- Renumbering target ADRs mutates target-owned history.\n+- Per-target Shiki renumbering makes platform citations non-portable.\ndiff --git a/docs/adr/template.md b/docs/adr/template.md\nindex a92a97daddd0b00ab9dd626cc8d86942da2eafed..17dd79175b5fee2105935557f691b40cf9492942\n--- a/docs/adr/template.md\n+++ b/docs/adr/template.md\n@@ -1,14 +1,14 @@\n-# NNNN: <Short Decision Title>\n+# <SADR-NNNN or NNNN>: <Short Decision Title>\n \n ## Status\n \n-Proposed | Accepted | Superseded by ADR-NNNN\n+Proposed | Accepted | Superseded by SADR-NNNN or target ADR NNNN\n \n ## Context\n \n What is the situation, constraint, or force that requires a decision? Reference\n-relevant domain terms (`CONTEXT.md`), prior ADRs, and code reality. State the\n-problem, not the solution.\n+relevant domain terms (`CONTEXT.md`), prior Shiki SADRs or target ADRs, and code\n+reality. State the problem, not the solution.\n \n ## Decision\n \ndiff --git a/docs/agents/autonomous-loop-e2e-acceptance.md b/docs/agents/autonomous-loop-e2e-acceptance.md\nindex bbf84c6081987f159ac6ad9ecf3c82b7aa72e7e8..5c13c1d02a05a90b0dc46cbef7c95a9846d0c237\n--- a/docs/agents/autonomous-loop-e2e-acceptance.md\n+++ b/docs/agents/autonomous-loop-e2e-acceptance.md\n@@ -40,8 +40,8 @@\n 3. `gh auth status` is logged in with merge permission on the repo.\n 4. Branch protection + required checks are configured exactly as the goal-loop\n    required-check set expects (see `docs/agents/branch-protection-smoke.md`).\n-5. The repo `.shiki/policy` permits autonomous low/medium-risk merges (ADR\n-   0008/0009); the MergeGate policy check is the recorded-authority gate.\n+5. The repo `.shiki/policy` permits autonomous low/medium-risk merges\n+   (SADR-0008/0009); the MergeGate policy check is the recorded-authority gate.\n \n The operator's only actions are: register the task and start the loop. **No\n manual pre-clearing** means after `shiki loop run` starts, the operator does not\ndiff --git a/docs/agents/domain.md b/docs/agents/domain.md\nindex 258da8ea81e87ac0a6b0b5b5e5c2fde980eaabb7..67aaa6f4b7d0a5aa4d495ce6279073333f7798cb\n--- a/docs/agents/domain.md\n+++ b/docs/agents/domain.md\n@@ -5,7 +5,9 @@\n ## Layout\n \n - `CONTEXT.md` defines Shiki language. Keep it implementation-free.\n-- `docs/adr/` records hard-to-reverse decisions.\n+- `docs/adr/SADR-NNNN-*.md` records Shiki platform decisions.\n+- `docs/adr/NNNN-*.md` records target product decisions without sharing the\n+  SADR namespace.\n - `AGENTS.md` is the runtime-neutral agent constitution.\n - `CLAUDE.md` is the Claude Code wrapper around `AGENTS.md`.\n - `.shiki/` is the Target Repository mirror for Goals, PRDs, plans, Task DAGs, contracts, locks, worktree records, ledger entries, CCA verdicts, repair packets, reports, and handoffs.\ndiff --git a/docs/agents/guardian-policy.md b/docs/agents/guardian-policy.md\nindex 4147c7adf05a47adb4fd24ada1a1c2e1f2ba6d2d..a20d9cc6c00a569bb057b9ca8d6b51c7fc4a9847\n--- a/docs/agents/guardian-policy.md\n+++ b/docs/agents/guardian-policy.md\n@@ -98,8 +98,8 @@\n `origin/<default_branch>` and reuses each job's existing base fetch; a shallow\n (`--depth`) or second base fetch would make the proof unresolvable and silently\n disable the carry, so neither is permitted. CCA reads `guardian_comment_carried` as\n-a recorded authority in the deterministic signal and does not re-derive it. See ADR\n-0018 for the full decision, the rejected diff/patch-equality and path-disjointness\n+a recorded authority in the deterministic signal and does not re-derive it. See\n+SADR-0018 for the full decision, the rejected diff/patch-equality and path-disjointness\n alternatives, and the accepted residual.\n \n ## External AI Guardian Review (SADR-0010)\ndiff --git a/docs/agents/implementation-policy.md b/docs/agents/implementation-policy.md\nindex c754144a9fb5bf4a38aa82a60e1670c59e626b44..e90d40fe21880b9a09719c5ecfe6bde7284eb3ce\n--- a/docs/agents/implementation-policy.md\n+++ b/docs/agents/implementation-policy.md\n@@ -31,6 +31,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n ## Non-Negotiables\n \n 1. **Planning precedes implementation.** Non-trivial Goals start with `grill-with-docs` and Context & Impact.\ndiff --git a/scripts/guardian_approval_signal.py b/scripts/guardian_approval_signal.py\nindex d059f92b2c82aa2b5d6ca421365537a87886c1cb..16924892714f4f5b9303c0ef0e325a7e70075630\n--- a/scripts/guardian_approval_signal.py\n+++ b/scripts/guardian_approval_signal.py\n@@ -199,8 +199,8 @@\n ) -> Any:\n     \"\"\"Evaluate SADR-0015 Contract Approval for the CCA Guardian signal.\n \n-    The SAME OR that MergeGate applies must reach this deterministic signal: ADR\n-    0015 is explicit that without it, a contract-approved PR still returns\n+    The SAME OR that MergeGate applies must reach this deterministic signal:\n+    SADR-0015 is explicit that without it, a contract-approved PR still returns\n     ``needs_guardian``, the CCA job fails, and the MergeGate policy check never\n     runs \u2014 the gate this change moves would never be reached. This resolves the\n     task the SAME way MergeGate does (the first Shiki task id in the PR body),\ndiff --git a/scripts/shiki_contract_approval.py b/scripts/shiki_contract_approval.py\nindex 941bcb96fdd8b9a85bc0c4f0f04d5dad9d04839c..4a4b418f6527413161db82670bec857a043360e2\n--- a/scripts/shiki_contract_approval.py\n+++ b/scripts/shiki_contract_approval.py\n@@ -142,7 +142,7 @@\n     changed_files_status: Any,\n     registration: Any,\n ) -> ContractApprovalResult:\n-    \"\"\"Whether this PR carries an SADR-0015 Contract Approval.\n+    \"\"\"Whether this PR carries a SADR-0015 Contract Approval.\n \n     The four conditions are evaluated IN ORDER, each failure recorded as a\n     distinct reason string:\ndiff --git a/scripts/shiki_installer.py b/scripts/shiki_installer.py\nindex be064ab7a29ff6836f7f7a1d70500a43e0d11d7b..d3e3247edbf3e4be287a4667176d86f6500f8dc5\n--- a/scripts/shiki_installer.py\n+++ b/scripts/shiki_installer.py\n@@ -118,6 +118,7 @@\n # what lets a target carry it without a governance-schema change.\n INSTALL_STAMP_PATH = \".shiki/install-stamp.json\"\n INSTALL_STAMP_VERSION = 1\n+LEGACY_SHIKI_SADR_MAX = 18\n \n # Under --force the shipped surface splits three ways, so nothing is lost and\n # nothing is silently stale.\n@@ -237,9 +238,98 @@\n         )\n \n \n+def legacy_shiki_adr_paths() -> tuple[str, ...]:\n+    \"\"\"Former numeric paths for Shiki SADR-0001 through SADR-0018.\n+\n+    The authoritative slugs come from the current SADR filenames. SADR-0019 is\n+    the namespace decision itself and never shipped under a numeric legacy path.\n+    \"\"\"\n+    paths: list[str] = []\n+    for sadr in sorted((ROOT / \"docs\" / \"adr\").glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\")):\n+        number = sadr.name.split(\"-\", 2)[1]\n+        if int(number) <= LEGACY_SHIKI_SADR_MAX:\n+            paths.append(f\"docs/adr/{sadr.name.removeprefix('SADR-')}\")\n+    return tuple(paths)\n+\n+\n+def preflight_legacy_sadr_cleanup(target: Path, *, force: bool) -> tuple[str, ...]:\n+    \"\"\"Authorize exact legacy Shiki ADR removals before the first write.\n+\n+    A non-force install never removes existing files. A forced install needs no\n+    stamp when no legacy Shiki path is present. When legacy paths do exist, each\n+    must be named in a readable install stamp and its current bytes must match\n+    the recorded digest.\n+    \"\"\"\n+    if not force:\n+        return ()\n+    present = tuple(\n+        relative\n+        for relative in legacy_shiki_adr_paths()\n+        if (target / relative).exists() or (target / relative).is_symlink()\n+    )\n+    if not present:\n+        return ()\n+\n+    stamp_path = target / INSTALL_STAMP_PATH\n+    try:\n+        raw = stamp_path.read_text(encoding=\"utf-8\")\n+        stamp = json.loads(raw)\n+    except FileNotFoundError:\n+        detail = \"install stamp is absent\"\n+        blockers = [f\"{relative}: {detail}\" for relative in present]\n+    except (OSError, UnicodeDecodeError) as error:\n+        detail = f\"install stamp is unreadable ({error})\"\n+        blockers = [f\"{relative}: {detail}\" for relative in present]\n+    except json.JSONDecodeError as error:\n+        detail = f\"install stamp is malformed ({error})\"\n+        blockers = [f\"{relative}: {detail}\" for relative in present]\n+    else:\n+        if not isinstance(stamp, dict):\n+            blockers = [\n+                f\"{relative}: install stamp is malformed (expected an object)\"\n+                for relative in present\n+            ]\n+        else:\n+            digests = stamp.get(\"digests\")\n+            blockers = []\n+            if not isinstance(digests, dict):\n+                blockers.extend(\n+                    f\"{relative}: install stamp has no digest for this path\"\n+                    for relative in present\n+                )\n+            else:\n+                for relative in present:\n+                    expected = digests.get(relative)\n+                    if not isinstance(expected, str) or not expected:\n+                        blockers.append(\n+                            f\"{relative}: install stamp has no digest for this path\"\n+                        )\n+                        continue\n+                    try:\n+                        actual = _sha256_file(target / relative)\n+                    except OSError as error:\n+                        blockers.append(\n+                            f\"{relative}: legacy path is unreadable ({error})\"\n+                        )\n+                        continue\n+                    if actual != expected:\n+                        blockers.append(\n+                            f\"{relative}: digest mismatch (stamp {expected}, current {actual})\"\n+                        )\n+\n+    if blockers:\n+        joined = \"\\n\".join(f\"- {blocker}\" for blocker in blockers)\n+        raise ShikiError(\n+            \"legacy Shiki ADR cleanup is not authorized:\\n\"\n+            f\"{joined}\\nNo files were written.\"\n+        )\n+    return present\n+\n+\n def install_template(target: Path, *, force: bool, validate: bool) -> list[NewFileNote]:\n     # Preconditions first: never rewrite the tree of a target that cannot be\n     # upgraded cleanly (see refuse_pending_migrations).\n+    legacy_cleanup = preflight_legacy_sadr_cleanup(target, force=force)\n     refuse_pending_migrations(target)\n \n     # Resolve the target's required CODEOWNERS owner once from its\n@@ -250,6 +340,10 @@\n     # verbatim. See ``codeowners_required_owner``.\n     codeowners_owner = codeowners_required_owner(target)\n \n+    for relative in legacy_cleanup:\n+        (target / relative).unlink()\n+        info(f\"removed stamped legacy Shiki ADR: {target / relative}\")\n+\n     notes: list[NewFileNote] = []\n     for relative in TEMPLATE_PATHS:\n         source = ROOT / relative\ndiff --git a/scripts/shiki_loop.py b/scripts/shiki_loop.py\nindex 3611641da8aeb5038b1da90e3114b09a473ec6d8..84caa491b9cad0edc4176f209c13e9221e975681\n--- a/scripts/shiki_loop.py\n+++ b/scripts/shiki_loop.py\n@@ -424,7 +424,7 @@\n         # unresolvable CCA verdict has already produced its terminal stop and is never\n         # laundered into a head-moving sync (SADR-0010). It also never fires while the\n         # policy (Guardian) gate is red \u2014 for ANY reason, not only when it is the sole\n-        # red check \u2014 so a head-bound Guardian approval, or an SADR-0015\n+        # red check \u2014 so a head-bound Guardian approval, or a SADR-0015\n         # contract-approval carry that base movement defeated (the policy gate compares\n         # base and head task governance directly), is never silently restored by\n         # adopting base wholesale. It is bounded by a durable per-task counter so the\ndiff --git a/scripts/test_shiki_goal_loop.sh b/scripts/test_shiki_goal_loop.sh\nindex 753c7b516e10f0be3bc92a4bd0eb1fa699776ae6..8f3820a0a079b3044424cfe1a6c6b7fe68896e67\n--- a/scripts/test_shiki_goal_loop.sh\n+++ b/scripts/test_shiki_goal_loop.sh\n@@ -231,8 +231,8 @@\n \n # High/critical risk merges autonomously when all required checks are green:\n # the \"MergeGate policy check\" required check IS the Guardian gate (it enforces\n-# guardian-policy.json \u2014 human approval OR external AI guardian review, ADR\n-# 0010), so green checks mean a recorded authority approved. The fake gh returns\n+# guardian-policy.json \u2014 human approval OR external AI guardian review,\n+# SADR-0010), so green checks mean a recorded authority approved. The fake gh returns\n # green checks, simulating that the Guardian gate passed.\n python3 \"$ROOT/scripts/shiki.py\" goal create --target \"$TARGET\" --title \"Guardian gate\" --outcome \"High risk merges via policy gate\" >/tmp/shiki-goal-loop-guardian-goal.json\n GGOAL=\"$(json_get /tmp/shiki-goal-loop-guardian-goal.json goal_id)\"\ndiff --git a/tests/test_adr_references.py b/tests/test_adr_references.py\nindex 51706329c797d5dd9b47e082a333fa83b261d482..db6e693e35f67103935be34f1d9bb4ff7ee60395\n--- a/tests/test_adr_references.py\n+++ b/tests/test_adr_references.py\n@@ -1,7 +1,7 @@\n-\"\"\"Every ``ADR ####`` citation must resolve to exactly one decision record.\n+\"\"\"Every target ADR and Shiki SADR citation resolves in its own namespace.\n \n Background. ``scripts/mergegate_check.py`` justifies ``is_bookkeeping_closeout``\n-with \"ADR 0017\" nine times and ``scripts/guardian_approval_signal.py`` cites\n+with \"SADR-0017\" nine times and ``scripts/guardian_approval_signal.py`` cites\n 0016/0017 seven times, yet for a stretch the numbering in ``docs/adr/`` stopped\n at 0015: the authoring commits lived on a coordinator branch and no task ever\n declared ``docs/adr/...`` in its locks, so nothing carried the records to the\n@@ -11,11 +11,12 @@\n and the directory drifted with no signal.\n \n This is the same unbound-pair shape behind other silent failures: a reference\n-and its target with nothing asserting they agree. This test binds them. It scans\n-every ``scripts/*.py`` (plus ``docs/**/*.md`` and the root ``*.md`` governance\n-files, which are cheap) for the ``ADR ####`` pattern and asserts each cited\n-number resolves to exactly one ``docs/adr/####-*.md``. The converse is\n-deliberately NOT required: an ADR may exist without being cited.\n+and its target with nothing asserting they agree. This test binds them. Target\n+``ADR NNNN`` citations resolve only to ``docs/adr/NNNN-*.md``; Shiki\n+``SADR-NNNN`` citations resolve only to ``docs/adr/SADR-NNNN-*.md``. The scan\n+covers current governance docs, scripts, workflows, prompts, packaging metadata,\n+and repo-local Codex guidance while deliberately excluding historical\n+``.shiki`` evidence. The converse is not required: a record may be uncited.\n \n The suite is dependency free: standard-library ``unittest`` only.\n \"\"\"\n@@ -31,17 +32,18 @@\n \n REPO_ROOT = Path(__file__).resolve().parents[1]\n \n-# \"ADR 0016\" -- a four-digit citation. The trailing \\b keeps a five-digit token\n-# from being read as a truncated four-digit number.\n-_CITATION = re.compile(r\"\\bADR (\\d{4})\\b\")\n-# \"0016-locks-bound-product-paths.md\" -- the leading four digits name the record.\n-_ADR_FILENAME = re.compile(r\"^(\\d{4})-.*\\.md$\")\n+# ``ADR 0016`` / ``SADR-0016`` -- a namespace plus four-digit citation. The\n+# trailing ``\\b`` keeps a five-digit token from being read as a truncated one.\n+_CITATION = re.compile(r\"\\b(S?ADR)[ -](\\d{4})\\b\")\n+# Target ADRs retain the numeric filename; Shiki platform decisions use SADR.\n+_DECISION_FILENAME = re.compile(r\"^(?:(SADR)-)?(\\d{4})-.*\\.md$\")\n \n \n class Violation(NamedTuple):\n     \"\"\"A citation that does not resolve to exactly one decision record.\"\"\"\n \n     citing_file: str  # repo-relative path of the file that cites the ADR\n+    namespace: str  # \"ADR\" for target decisions; \"SADR\" for Shiki decisions\n     number: str  # the cited four-digit number, e.g. \"0016\"\n     line: int  # 1-based line of the citation\n     reason: str  # \"missing\" (zero matches) or \"ambiguous\" (two or more)\n@@ -49,44 +51,59 @@\n \n     @property\n     def expected_path(self) -> str:\n-        return f\"docs/adr/{self.number}-*.md\"\n+        prefix = \"SADR-\" if self.namespace == \"SADR\" else \"\"\n+        return f\"docs/adr/{prefix}{self.number}-*.md\"\n \n     def describe(self) -> str:\n         \"\"\"Name the citing file, the cited number, and the missing path.\"\"\"\n         if self.reason == \"missing\":\n             return (\n-                f\"{self.citing_file}:{self.line} cites ADR {self.number} \"\n+                f\"{self.citing_file}:{self.line} cites {self.namespace} {self.number} \"\n                 f\"but no document matches {self.expected_path}\"\n             )\n         joined = \", \".join(self.matches)\n         return (\n-            f\"{self.citing_file}:{self.line} cites ADR {self.number} which \"\n+            f\"{self.citing_file}:{self.line} cites {self.namespace} {self.number} which \"\n             f\"resolves to {len(self.matches)} documents (expected exactly one \"\n             f\"at {self.expected_path}): {joined}\"\n         )\n \n \n-def _adr_index(root: Path) -> dict[str, list[Path]]:\n-    \"\"\"Map every four-digit ADR number to the record files that claim it.\"\"\"\n-    index: dict[str, list[Path]] = {}\n+def _adr_index(root: Path) -> dict[tuple[str, str], list[Path]]:\n+    \"\"\"Map each decision namespace and number to the files that claim it.\"\"\"\n+    index: dict[tuple[str, str], list[Path]] = {}\n     adr_dir = root / \"docs\" / \"adr\"\n     if adr_dir.is_dir():\n         for path in sorted(adr_dir.glob(\"*.md\")):\n-            match = _ADR_FILENAME.match(path.name)\n+            match = _DECISION_FILENAME.match(path.name)\n             if match:\n-                index.setdefault(match.group(1), []).append(path)\n+                namespace = \"SADR\" if match.group(1) else \"ADR\"\n+                index.setdefault((namespace, match.group(2)), []).append(path)\n     return index\n \n \n def _citing_files(root: Path, include_docs: bool = True) -> list[Path]:\n-    \"\"\"Files scanned for citations: scripts/*.py, and cheaply the markdown.\"\"\"\n+    \"\"\"Current, shipped text surfaces scanned for decision citations.\"\"\"\n     files = sorted((root / \"scripts\").glob(\"*.py\"))\n+    files += sorted((root / \"scripts\").glob(\"*.sh\"))\n     if include_docs:\n         docs = root / \"docs\"\n         if docs.is_dir():\n             files += sorted(docs.rglob(\"*.md\"))\n         files += sorted(root.glob(\"*.md\"))\n-    return files\n+        pyproject = root / \"pyproject.toml\"\n+        if pyproject.is_file():\n+            files.append(pyproject)\n+        for relative in (\".github\", \".codex\"):\n+            surface = root / relative\n+            if surface.is_dir():\n+                files += sorted(\n+                    path\n+                    for path in surface.rglob(\"*\")\n+                    if path.is_file()\n+                    and path.suffix in {\".md\", \".py\", \".sh\", \".toml\", \".yaml\", \".yml\"}\n+                )\n+    return sorted(set(files))\n \n \n def find_adr_reference_violations(\n@@ -94,10 +111,9 @@\n ) -> list[Violation]:\n     \"\"\"Return every ``ADR ####`` citation under ``root`` that fails to resolve.\n \n-    A citation resolves when exactly one ``docs/adr/####-*.md`` matches its\n-    number. Zero matches is a \"missing\" violation; two or more is \"ambiguous\".\n-    Records that are never cited produce nothing -- the converse is not\n-    required.\n+    A citation resolves when exactly one record in the same namespace matches\n+    its number. Zero matches is a \"missing\" violation; two or more is\n+    \"ambiguous\". Records that are never cited produce nothing.\n     \"\"\"\n     root = Path(root)\n     index = _adr_index(root)\n@@ -110,13 +126,15 @@\n         rel = path.relative_to(root).as_posix()\n         for lineno, line in enumerate(text.splitlines(), start=1):\n             for match in _CITATION.finditer(line):\n-                number = match.group(1)\n-                found = index.get(number, [])\n+                namespace, number = match.groups()\n+                found = index.get((namespace, number), [])\n                 if len(found) == 1:\n                     continue\n                 reason = \"missing\" if not found else \"ambiguous\"\n                 rels = [p.relative_to(root).as_posix() for p in found]\n-                violations.append(Violation(rel, number, lineno, reason, rels))\n+                violations.append(\n+                    Violation(rel, namespace, number, lineno, reason, rels)\n+                )\n     return violations\n \n \n@@ -129,8 +147,8 @@\n         self.assertEqual(\n             violations,\n             [],\n-            \"Every 'ADR ####' citation must resolve to exactly one \"\n-            f\"docs/adr/####-*.md:\\n{detail}\",\n+            \"Every ADR/SADR citation must resolve exactly once in its own \"\n+            f\"docs/adr namespace:\\n{detail}\",\n         )\n \n     def test_scan_actually_reads_scripts(self) -> None:\n@@ -146,8 +164,21 @@\n         # The two records this Goal carried onto the default branch, and which\n         # the scripts cite most, must be present.\n         index = _adr_index(REPO_ROOT)\n-        self.assertIn(\"0016\", index)\n-        self.assertIn(\"0017\", index)\n+        self.assertIn((\"SADR\", \"0016\"), index)\n+        self.assertIn((\"SADR\", \"0017\"), index)\n+\n+    def test_platform_tree_has_only_sadr_0001_through_0019(self) -> None:\n+        adr_dir = REPO_ROOT / \"docs\" / \"adr\"\n+        sadrs = sorted(adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\"))\n+        self.assertEqual(\n+            {path.name.split(\"-\", 2)[1] for path in sadrs},\n+            {f\"{number:04d}\" for number in range(1, 20)},\n+        )\n+        self.assertEqual(\n+            list(adr_dir.glob(\"[0-9][0-9][0-9][0-9]-*.md\")),\n+            [],\n+            \"the Shiki platform tree must not retain legacy numeric SADR paths\",\n+        )\n \n \n class FixtureAdrReferences(unittest.TestCase):\n@@ -180,6 +211,26 @@\n         self.assertIn(\"9991\", message)  # cited number\n         self.assertIn(\"docs/adr/9991-*.md\", message)  # missing path\n \n+    def test_target_adr_does_not_satisfy_missing_platform_sadr(self) -> None:\n+        root = self._make_repo(\n+            adr_names=[\"0042-target-decision.md\"],\n+            script_body='\"\"\"Target ADR 0042 exists, but platform SADR-0042 does not.\"\"\"\\n',\n+        )\n+        violations = find_adr_reference_violations(root, include_docs=False)\n+        self.assertEqual(len(violations), 1)\n+        self.assertEqual(violations[0].namespace, \"SADR\")\n+        self.assertEqual(violations[0].number, \"0042\")\n+        self.assertEqual(violations[0].expected_path, \"docs/adr/SADR-0042-*.md\")\n+\n+    def test_target_adr_and_platform_sadr_may_reuse_a_number(self) -> None:\n+        root = self._make_repo(\n+            adr_names=[\"0042-target-decision.md\", \"SADR-0042-platform-decision.md\"],\n+            script_body='\"\"\"Target ADR 0042 and platform SADR-0042 are distinct.\"\"\"\\n',\n+        )\n+        self.assertEqual(\n+            find_adr_reference_violations(root, include_docs=False), []\n+        )\n+\n     def test_uncited_adr_is_allowed(self) -> None:\n         # An ADR exists but nothing cites it -> not a violation. The converse\n         # (every ADR must be cited) is explicitly NOT required.\ndiff --git a/tests/test_policy_job_merged_prs.py b/tests/test_policy_job_merged_prs.py\nindex d45512e74681e5e649279236c7d4e79d5c3da471..b97989f8bd74d091de1a6b5275a20d8dc68c4512\n--- a/tests/test_policy_job_merged_prs.py\n+++ b/tests/test_policy_job_merged_prs.py\n@@ -3,8 +3,8 @@\n \n ``.github/workflows/shiki-cca-completion.yml`` has two consumers of the merged-PR\n proof: the CCA *signal* (``guardian_approval_signal.py`` in \"Prepare CCA context\")\n-and the authoritative gate (``mergegate_check.py`` in \"Enforce MergeGate\"). ADR\n-0017's bookkeeping-closeout exemption (condition 6) needs the merged implementation\n+and the authoritative gate (``mergegate_check.py`` in \"Enforce MergeGate\").\n+SADR-0017's bookkeeping-closeout exemption (condition 6) needs the merged implementation\n PR proven, and a closeout carries NO ``post_merge_reconcile`` marker. The signal\n already resolves ``MERGED_PRS`` unconditionally, but \"Enforce MergeGate\" used to\n resolve it only inside ``grep -q '<!-- shiki:post_merge_reconcile -->'``. So a\ndiff --git a/tests/test_sadr_install_migration.py b/tests/test_sadr_install_migration.py\nnew file mode 100644\nindex 0000000000000000000000000000000000000000..5a442fa5e4968ade178dcdedc3c63b54dba580a2\n--- /dev/null\n+++ b/tests/test_sadr_install_migration.py\n@@ -0,0 +1,224 @@\n+\"\"\"SADR installation and collision-safe legacy upgrade behavior.\"\"\"\n+\n+from __future__ import annotations\n+\n+import contextlib\n+import hashlib\n+import io\n+import json\n+import shutil\n+import tempfile\n+import unittest\n+from pathlib import Path\n+\n+import shiki_test_support  # noqa: F401  (path bootstrap)\n+\n+import shiki_installer\n+from tests.test_adr_references import find_adr_reference_violations\n+\n+\n+PRODUCT_NUMBERS = tuple(range(13, 19))\n+\n+\n+def _quiet_install(target: Path, *, force: bool, validate: bool) -> None:\n+    with contextlib.redirect_stdout(io.StringIO()):\n+        shiki_installer.install_template(target, force=force, validate=validate)\n+\n+\n+def _sha256(path: Path) -> str:\n+    return hashlib.sha256(path.read_bytes()).hexdigest()\n+\n+\n+def _legacy_sadr_paths() -> tuple[str, ...]:\n+    return tuple(\n+        f\"docs/adr/{path.name.removeprefix('SADR-')}\"\n+        for path in sorted((shiki_installer.ROOT / \"docs\" / \"adr\").glob(\"SADR-*.md\"))\n+        if path.name != \"SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\"\n+    )\n+\n+\n+def _create_legacy_upgrade_fixture(\n+    target: Path,\n+) -> tuple[dict[str, bytes], tuple[str, ...]]:\n+    adr_dir = target / \"docs\" / \"adr\"\n+    adr_dir.mkdir(parents=True)\n+    product_bytes = {\n+        f\"{number:04d}-cubeshare-product-decision.md\": (\n+            f\"# CubeShare ADR {number:04d}\\n\\nDo not rewrite {number}.\\n\"\n+        ).encode()\n+        for number in PRODUCT_NUMBERS\n+    }\n+    for name, content in product_bytes.items():\n+        (adr_dir / name).write_bytes(content)\n+\n+    legacy_paths = _legacy_sadr_paths()\n+    for relative in legacy_paths:\n+        (target / relative).write_bytes(f\"legacy Shiki bytes for {relative}\\n\".encode())\n+\n+    stamp_path = target / shiki_installer.INSTALL_STAMP_PATH\n+    stamp_path.parent.mkdir(parents=True, exist_ok=True)\n+    stamp = {\n+        \"version\": shiki_installer.INSTALL_STAMP_VERSION,\n+        \"platform_commit\": None,\n+        \"installed_at\": \"2000-01-01T00:00:00+00:00\",\n+        \"digests\": {\n+            relative: _sha256(target / relative) for relative in legacy_paths\n+        },\n+    }\n+    stamp_path.write_text(\n+        json.dumps(stamp, indent=2, sort_keys=True) + \"\\n\", encoding=\"utf-8\"\n+    )\n+    return product_bytes, legacy_paths\n+\n+\n+def _tree_snapshot(root: Path) -> dict[str, tuple[str, bytes | str]]:\n+    \"\"\"Recursive byte/shape snapshot used to prove preflight is write-free.\"\"\"\n+    snapshot: dict[str, tuple[str, bytes | str]] = {}\n+    for path in sorted(root.rglob(\"*\")):\n+        relative = path.relative_to(root).as_posix()\n+        if path.is_symlink():\n+            snapshot[relative] = (\"symlink\", path.readlink().as_posix())\n+        elif path.is_dir():\n+            snapshot[relative] = (\"directory\", \"\")\n+        else:\n+            snapshot[relative] = (\"file\", path.read_bytes())\n+    return snapshot\n+\n+\n+class FreshSadrInstallTests(unittest.TestCase):\n+    def setUp(self) -> None:\n+        self.tmp = Path(tempfile.mkdtemp(prefix=\"shiki-sadr-fresh-\"))\n+        self.addCleanup(lambda: shutil.rmtree(self.tmp, ignore_errors=True))\n+        self.target = self.tmp / \"target\"\n+        self.adr_dir = self.target / \"docs\" / \"adr\"\n+        self.adr_dir.mkdir(parents=True)\n+        self.product_bytes = {\n+            f\"{number:04d}-product-decision.md\": (\n+                f\"# Product ADR {number:04d}\\n\\nTarget-owned bytes {number}.\\n\"\n+            ).encode()\n+            for number in PRODUCT_NUMBERS\n+        }\n+        for name, content in self.product_bytes.items():\n+            (self.adr_dir / name).write_bytes(content)\n+\n+    def test_fresh_install_preserves_product_adrs_and_installs_all_sadrs(self) -> None:\n+        _quiet_install(self.target, force=False, validate=True)\n+\n+        for name, expected in self.product_bytes.items():\n+            self.assertEqual((self.adr_dir / name).read_bytes(), expected)\n+\n+        sadr_numbers = {\n+            path.name.split(\"-\", 2)[1]\n+            for path in self.adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\")\n+        }\n+        self.assertEqual(sadr_numbers, {f\"{number:04d}\" for number in range(1, 20)})\n+\n+        numeric_records = {\n+            path.name\n+            for path in self.adr_dir.glob(\"[0-9][0-9][0-9][0-9]-*.md\")\n+        }\n+        self.assertEqual(numeric_records, set(self.product_bytes))\n+        self.assertEqual(find_adr_reference_violations(self.target), [])\n+\n+\n+class LegacySadrUpgradeTests(unittest.TestCase):\n+    def setUp(self) -> None:\n+        self.tmp = Path(tempfile.mkdtemp(prefix=\"shiki-sadr-upgrade-\"))\n+        self.addCleanup(lambda: shutil.rmtree(self.tmp, ignore_errors=True))\n+        self.target = self.tmp / \"target\"\n+        self.adr_dir = self.target / \"docs\" / \"adr\"\n+        self.product_bytes, self.legacy_paths = _create_legacy_upgrade_fixture(\n+            self.target\n+        )\n+\n+    def test_force_upgrade_deletes_only_stamped_legacy_sadrs(self) -> None:\n+        _quiet_install(self.target, force=True, validate=True)\n+\n+        for relative in self.legacy_paths:\n+            self.assertFalse((self.target / relative).exists(), relative)\n+        for name, expected in self.product_bytes.items():\n+            self.assertEqual((self.adr_dir / name).read_bytes(), expected)\n+\n+        sadrs = list(self.adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\"))\n+        self.assertEqual(len(sadrs), 19)\n+        refreshed = json.loads(\n+            (self.target / shiki_installer.INSTALL_STAMP_PATH).read_text(encoding=\"utf-8\")\n+        )\n+        for relative in self.legacy_paths:\n+            self.assertNotIn(relative, refreshed[\"digests\"])\n+        for path in sadrs:\n+            relative = path.relative_to(self.target).as_posix()\n+            self.assertEqual(refreshed[\"digests\"][relative], _sha256(path))\n+\n+    def test_non_force_install_never_deletes_legacy_files(self) -> None:\n+        before = {\n+            relative: (self.target / relative).read_bytes()\n+            for relative in self.legacy_paths\n+        }\n+        _quiet_install(self.target, force=False, validate=False)\n+        for relative, expected in before.items():\n+            self.assertEqual((self.target / relative).read_bytes(), expected)\n+\n+\n+class LegacyCleanupPreflightTests(unittest.TestCase):\n+    def setUp(self) -> None:\n+        self.tmp = Path(tempfile.mkdtemp(prefix=\"shiki-sadr-preflight-\"))\n+        self.addCleanup(lambda: shutil.rmtree(self.tmp, ignore_errors=True))\n+\n+    def test_all_ownership_blockers_abort_before_any_write(self) -> None:\n+        cases = (\n+            (\"absent\", \"install stamp is absent\"),\n+            (\"unreadable\", \"install stamp is unreadable\"),\n+            (\"malformed\", \"install stamp is malformed\"),\n+            (\"missing-digest\", \"install stamp has no digest\"),\n+            (\"digest-mismatch\", \"digest mismatch\"),\n+        )\n+        for name, expected_reason in cases:\n+            with self.subTest(name=name):\n+                target = self.tmp / name\n+                _, legacy_paths = _create_legacy_upgrade_fixture(target)\n+                stamp_path = target / shiki_installer.INSTALL_STAMP_PATH\n+\n+                if name == \"absent\":\n+                    stamp_path.unlink()\n+                elif name == \"unreadable\":\n+                    stamp_path.unlink()\n+                    stamp_path.mkdir()\n+                elif name == \"malformed\":\n+                    stamp_path.write_text(\"{not-json\\n\", encoding=\"utf-8\")\n+                else:\n+                    stamp = json.loads(stamp_path.read_text(encoding=\"utf-8\"))\n+                    if name == \"missing-digest\":\n+                        stamp[\"digests\"] = {}\n+                    else:\n+                        stamp[\"digests\"] = {\n+                            relative: \"deadbeef\" for relative in legacy_paths\n+                        }\n+                    stamp_path.write_text(\n+                        json.dumps(stamp, indent=2, sort_keys=True) + \"\\n\",\n+                        encoding=\"utf-8\",\n+                    )\n+\n+                before = _tree_snapshot(target)\n+                with self.assertRaises(shiki_installer.ShikiError) as raised:\n+                    _quiet_install(target, force=True, validate=False)\n+                after = _tree_snapshot(target)\n+\n+                self.assertEqual(before, after, f\"{name} preflight changed the tree\")\n+                message = str(raised.exception)\n+                self.assertIn(expected_reason, message)\n+                self.assertIn(\"No files were written\", message)\n+                for relative in legacy_paths:\n+                    self.assertIn(relative, message)\n+\n+    def test_absent_stamp_is_allowed_when_no_legacy_path_exists(self) -> None:\n+        target = self.tmp / \"no-legacy\"\n+        target.mkdir()\n+        _quiet_install(target, force=True, validate=False)\n+        self.assertTrue(\n+            (target / \"docs\" / \"adr\" / \"SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\").is_file()\n+        )\n+\n+\n+if __name__ == \"__main__\":\n+    unittest.main()\n\ndiff --git a/.codex/skills/shiki/SKILL.md b/.codex/skills/shiki/SKILL.md\nindex c8e2e85d9d75d49cc4f2d6286ee22659c4e53e75..93cded623b2c15d9f65c76491a1c2d813ac9e6c0\n--- a/.codex/skills/shiki/SKILL.md\n+++ b/.codex/skills/shiki/SKILL.md\n@@ -18,6 +18,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n Codex CLI does not register this skill as a custom `/shiki` slash command. When\n the user types `/shiki` in Codex CLI and gets \"Unrecognized command\", explain\n that this is expected: invoke Shiki in Codex with natural language such as\n@@ -69,7 +72,7 @@\n \n ## Responsibilities\n \n-- Claude Code implements and repairs by default (`shiki runner claude`, ADR 0008); Codex implements and repairs tasks explicitly assigned to `codex`.\n+- Claude Code implements and repairs by default (`shiki runner claude`, SADR-0008); Codex implements and repairs tasks explicitly assigned to `codex`.\n - Claude Code Action can act as GitHub-side CCA or reviewer.\n - CCA judges completion.\n - MergeGate authorizes state transitions and merge readiness.\ndiff --git a/.github/prompts/cca-completion-check.md b/.github/prompts/cca-completion-check.md\nindex ed203ad5f302ecc81372d8ab27d2b1232ca97497..fd89950528333ef6242e6d5443904f5392ab7e56\n--- a/.github/prompts/cca-completion-check.md\n+++ b/.github/prompts/cca-completion-check.md\n@@ -19,6 +19,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n ## Required Reading\n \n Read only the files needed to judge the PR contract. Prefer this order:\ndiff --git a/AGENTS.md b/AGENTS.md\nindex 7f041100d49c2a7525f51cb0f5d696a4a6d1c78c..ea303398018461999e03b6f27be3f4d65833fa32\n--- a/AGENTS.md\n+++ b/AGENTS.md\n@@ -44,10 +44,14 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use\n+`SADR-NNNN-*.md`; target product decisions retain `NNNN-*.md` and the `ADR`\n+namespace.\n+\n Use the canonical source-of-truth order above. Runtime-specific reading orders\n are operational guidance only and must not override or reorder it.\n \n-Conversation state is not durable truth. If a decision matters, put it in GitHub, `.shiki/`, `CONTEXT.md`, or an ADR.\n+Conversation state is not durable truth. If a decision matters, put it in GitHub, `.shiki/`, `CONTEXT.md`, a Shiki SADR, or a target ADR.\n \n If GitHub and `.shiki/` disagree, surface the conflict and prefer GitHub operational state until the mirror is repaired.\n \n@@ -75,8 +79,8 @@\n Every non-trivial change follows this loop:\n \n 1. **Goal**: clarify outcome, completion conditions, non-goals, risk level, and success signals.\n-2. **grill-with-docs**: challenge the plan against domain docs, ADRs, code reality, terminology, and edge scenarios.\n-3. **Context & Impact**: find relevant docs, ADRs, code, dependencies, owners, locks, and verification surfaces.\n+2. **grill-with-docs**: challenge the plan against domain docs, Shiki SADRs, target ADRs, code reality, terminology, and edge scenarios.\n+3. **Context & Impact**: find relevant docs, SADRs, target ADRs, code, dependencies, owners, locks, and verification surfaces.\n 4. **PRD and Spec Freeze**: use `to-prd` when context is settled enough to become durable product/engineering intent. The operator's explicit approval of the PRD is Spec Freeze: the plan records a `spec_freeze` block, and no plan runs without it. Steps 1-4 form Requirements Definition \u2014 one continuous operator dialogue. After Spec Freeze, scope changes require an operator-approved Spec Amendment; non-scope-moving interpretations go to the Assumption Log.\n 5. **Issues**: use `to-issues` to create independently grabbable vertical-slice issues. Prefer AFK slices over HITL slices where possible.\n 6. **Triage**: label issues for readiness, risk, runtime, skills, and MergeGate state.\n@@ -167,7 +171,7 @@\n - Testing decisions.\n - Out-of-scope items.\n - Further notes.\n-- Links to relevant domain terms and ADRs.\n+- Links to relevant domain terms, SADRs, and target ADRs.\n \n Every executable task must include:\n \n@@ -193,7 +197,7 @@\n \n Context & Impact must identify:\n \n-- Relevant documents, domain terms, ADRs, and past decisions.\n+- Relevant documents, domain terms, SADRs, target ADRs, and past decisions.\n - Relevant modules, interfaces, seams, callers, owners, and tests.\n - Dependency relationships between tasks.\n - Candidate locks and lock conflicts.\n@@ -210,7 +214,7 @@\n | Skill | Required when |\n | --- | --- |\n | `setup-matt-pocock-skills` | First configuring the repo for skills, or when issue tracker, triage labels, or domain docs are missing. |\n-| `grill-with-docs` | Default for non-trivial Goals before PRD/issues; always required when plans, terminology, boundaries, tradeoffs, or ADR-worthy decisions are ambiguous. |\n+| `grill-with-docs` | Default for non-trivial Goals before PRD/issues; always required when plans, terminology, boundaries, tradeoffs, or decision-record-worthy choices are ambiguous. |\n | `zoom-out` | The agent lacks an architectural map of the relevant code area. |\n | `to-prd` | Settled Goal context must become durable product/engineering intent. |\n | `to-issues` | A Goal, PRD, or plan must become independently grabbable vertical-slice issues. |\n@@ -348,7 +352,7 @@\n - Three or more new files.\n - Any high-risk or critical label.\n \n-When the architecture gate triggers, run `grill-with-docs` or `improve-codebase-architecture` as appropriate and record durable decisions in an ADR when needed.\n+When the architecture gate triggers, run `grill-with-docs` or `improve-codebase-architecture` as appropriate and record durable decisions in a Shiki SADR or target ADR as applicable.\n \n ## Engineering Discipline\n \ndiff --git a/CLAUDE.md b/CLAUDE.md\nindex 98a932e7841a632866fcd53b0bad0e3af3711d3d..98a31399404d6fb044b4fd493f4459ac87f439de\n--- a/CLAUDE.md\n+++ b/CLAUDE.md\n@@ -14,6 +14,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n ## Claude Role\n \n Default to planner, default implementer, reviewer, coordinator, documentation editor, CCA-style completion judge, and final-judgment assistant.\n@@ -57,7 +60,7 @@\n At the start of a Shiki session:\n \n 1. Read `AGENTS.md`.\n-2. Read `CONTEXT.md` and relevant ADRs.\n+2. Read `CONTEXT.md` and relevant Shiki SADRs and target ADRs.\n 3. Read active `.shiki/` Goal/task/ledger state when present.\n 4. Read active distilled rules from `.shiki/memories/` (status `distilled`, `active: true`, not revoked or superseded) for applicable guidance; the same rules are injected deterministically into task handoffs as the `## Distilled Rules` section (\u00a73.5).\n 5. Read the GitHub Issue or PR when the task is GitHub-backed.\n@@ -74,7 +77,7 @@\n - Define the Goal, completion conditions, non-goals, and risk level.\n - Treat `grill-with-docs` as the default planning entry point for non-trivial Goals.\n - Challenge domain terms against `CONTEXT.md`.\n-- Challenge hard-to-reverse tradeoffs against ADRs.\n+- Challenge hard-to-reverse tradeoffs against SADRs and target ADRs.\n - Explore code instead of asking when code can answer the question.\n - Run Context & Impact before implementation planning.\n - Produce a Task DAG, not an unordered checklist.\n@@ -94,7 +97,7 @@\n - For each question, provide your recommended answer.\n - Resolve dependency decisions before downstream design questions.\n - Update `CONTEXT.md` inline when terminology crystallizes.\n-- Offer ADRs only when the decision is hard to reverse, surprising without context, and a real tradeoff.\n+- Offer a SADR or target ADR only when the decision is hard to reverse, surprising without context, and a real tradeoff.\n - Convert unresolved questions into HITL blockers rather than allowing Codex to guess.\n \n ## PRD and Issues Standard\n@@ -103,7 +106,7 @@\n \n - Use the project's domain glossary vocabulary.\n - Avoid volatile file paths unless they are essential.\n-- Include implementation decisions, testing decisions, out-of-scope items, and relevant ADR/domain links.\n+- Include implementation decisions, testing decisions, out-of-scope items, and relevant SADR/target-ADR/domain links.\n - Identify deep-module opportunities and testable interfaces.\n \n When creating issues:\n@@ -121,7 +124,7 @@\n - Branch/worktree target.\n - Scope and non-goals.\n - Dependencies and locks.\n-- Relevant docs, ADRs, modules, and tests.\n+- Relevant docs, SADRs, target ADRs, modules, and tests.\n - Required skill invocations.\n - TDD expectations.\n - Pre-PR `code-review` gate expectations.\n@@ -152,7 +155,7 @@\n \n - Treat yourself as a judge, not implementer.\n - Do not write production code.\n-- Read `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant ADRs, PRD, task issue, PR body, diff, checks, reviews, labels, and `.shiki/` state.\n+- Read `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant SADRs and target ADRs, PRD, task issue, PR body, diff, checks, reviews, labels, and `.shiki/` state.\n - Evaluate every applicable checklist in `docs/agents/checklists.md`.\n - Map each acceptance criterion to durable evidence.\n - Distinguish implementation failure from missing evidence.\ndiff --git a/CONTEXT.md b/CONTEXT.md\nindex e61a9be06a0cce0d026484083a657bde2067e234..237ee8aa3ea8fab334ece49768bc5ed3b5af0974\n--- a/CONTEXT.md\n+++ b/CONTEXT.md\n@@ -25,7 +25,7 @@\n _Avoid_: jumping directly into code\n \n **grill-with-docs**:\n-A planning skill that challenges a plan against domain language, ADRs, code reality, and concrete edge scenarios. It resolves design-tree questions before PRD/issues. It runs inside Requirements Definition.\n+A planning skill that challenges a plan against domain language, Shiki SADRs, target ADRs, code reality, and concrete edge scenarios. It resolves design-tree questions before PRD/issues. It runs inside Requirements Definition.\n _Avoid_: generic brainstorming, silent assumptions\n \n **Requirements Definition**:\ndiff --git a/CONTRIBUTING.md b/CONTRIBUTING.md\nindex c6d9b656e73abb8a55e5f660123e03153dcb3e6d..9e37a8fbf668db5b22461aaae5548b8f1354394c\n--- a/CONTRIBUTING.md\n+++ b/CONTRIBUTING.md\n@@ -17,7 +17,7 @@\n 1. **Goal** \u2014 start from a user-approved Goal with an outcome, completion\n    conditions, non-goals, risk level, and success signals.\n 2. **grill-with-docs** \u2014 for any non-trivial change, challenge the plan against\n-   domain language, ADRs, and code reality before writing a PRD.\n+   domain language, Shiki SADRs, target ADRs, and code reality before writing a PRD.\n 3. **PRD** \u2014 capture settled product/engineering intent (`to-prd`).\n 4. **Spec Freeze** \u2014 the operator explicitly approves the PRD/requirements;\n    the plan records a `spec_freeze` block and no plan runs without it\n@@ -94,7 +94,7 @@\n   entries.\n - Do not hand-edit `.shiki/goals/*.json` or `.shiki/manifest.json` in a task PR.\n - Conversation state is not durable truth. If a decision matters, record it in\n-  GitHub, `.shiki/`, `CONTEXT.md`, or an ADR.\n+  GitHub, `.shiki/`, `CONTEXT.md`, a Shiki SADR, or a target ADR.\n \n ## Architecture Gate\n \n@@ -103,7 +103,7 @@\n or CI policy, package structure, three or more structural files, more than 100\n net new lines in a core module, three or more new files, or any high-risk label.\n When triggered, run `grill-with-docs` or `improve-codebase-architecture` and\n-record durable decisions in an ADR when needed (see `docs/adr/`).\n+record durable decisions in a Shiki SADR or target ADR as applicable (see `docs/adr/`).\n \n ## Safety\n \ndiff --git a/SYSTEM_PROMPT.md b/SYSTEM_PROMPT.md\nindex aaaea45e3a896c3a44ba89eec8bb84325ecf6c62..61b3c55afe7a5fc5f1a3d475378175db58553c6d\n--- a/SYSTEM_PROMPT.md\n+++ b/SYSTEM_PROMPT.md\n@@ -30,6 +30,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n Use the canonical source-of-truth order above. Runtime-specific reading orders\n are operational guidance only and must not override or reorder it. The current\n conversation is non-durable operator input.\n@@ -55,7 +58,7 @@\n For any non-trivial Goal, use this flow:\n \n 1. **Goal Seek**: define outcome, non-goals, risk level, completion criteria, and evidence requirements.\n-2. **grill-with-docs**: challenge the plan against `CONTEXT.md`, ADRs, code reality, terminology, boundaries, and edge scenarios. Ask one question at a time when the operator is available. If code can answer a question, inspect code instead of asking.\n+2. **grill-with-docs**: challenge the plan against `CONTEXT.md`, Shiki SADRs, target ADRs, code reality, terminology, boundaries, and edge scenarios. Ask one question at a time when the operator is available. If code can answer a question, inspect code instead of asking.\n 3. **Context & Impact**: identify modules, interfaces, seams, callers, dependencies, locks, risk, and verification surfaces.\n 4. **to-prd + Spec Freeze**: turn settled context into a PRD using domain vocabulary and testing decisions. The operator's explicit approval of the PRD is Spec Freeze, recorded as the plan's `spec_freeze` block; plans without it do not run. Steps 1-4 form Requirements Definition.\n 5. **to-issues**: decompose the PRD into independently grabbable vertical-slice issues. Prefer AFK slices; mark HITL slices when judgment is still required.\n@@ -75,7 +78,7 @@\n Before making any material change:\n \n 1. Read `AGENTS.md` and any runtime-specific wrapper such as `CLAUDE.md`.\n-2. Read `CONTEXT.md` and relevant ADRs.\n+2. Read `CONTEXT.md` and relevant SADRs and target ADRs.\n 3. Read the GitHub Issue / PR and `.shiki/` state for the active Goal or task.\n 4. Identify your role: Planner, Implementer, Reviewer, Completion Check Agent, Repairer, or Guardian-assist.\n 5. Identify Goal id, task id, branch, PR, dependencies, locks, risk level, required skills, and required evidence.\n@@ -103,7 +106,7 @@\n \n At minimum, identify:\n \n-- Relevant documents, ADRs, domain terms, and prior decisions.\n+- Relevant documents, SADRs, target ADRs, domain terms, and prior decisions.\n - Relevant modules, interfaces, seams, callers, owners, and tests.\n - Dependency relationships between tasks.\n - Candidate file locks and lock conflicts.\n@@ -152,7 +155,7 @@\n \n CCA must read:\n \n-- `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant ADRs.\n+- `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant SADRs and target ADRs.\n - Parent Goal issue, PRD issue, task issue, comments, and labels.\n - PR body, diff, commits, changed files, reviews, CI checks, and artifacts available to the workflow.\n - `.shiki/` task contract, locks, ledger entries, CCA checklist profile, and prior repair packets.\n@@ -214,7 +217,7 @@\n - Three or more new files.\n - Any high-risk or critical label.\n \n-When the architecture gate triggers, run `grill-with-docs` or `improve-codebase-architecture` as appropriate and record the decision in an ADR if it is hard to reverse or likely to be re-litigated.\n+When the architecture gate triggers, run `grill-with-docs` or `improve-codebase-architecture` as appropriate and record the decision in a Shiki SADR or target ADR as applicable if it is hard to reverse or likely to be re-litigated.\n \n ## 14. Skill Gate\n \n@@ -223,7 +226,7 @@\n Use these skills:\n \n - `setup-matt-pocock-skills`: before first use of the engineering skill set, or when issue tracker, triage labels, or domain docs are missing.\n-- `grill-with-docs`: default for non-trivial Goals before PRD/issues; required for ambiguous plans, terminology, boundaries, ADR-worthy tradeoffs, or design-tree decisions.\n+- `grill-with-docs`: default for non-trivial Goals before PRD/issues; required for ambiguous plans, terminology, boundaries, decision-record-worthy tradeoffs, or design-tree decisions.\n - `zoom-out`: unfamiliar code area or missing architectural map.\n - `to-prd`: convert settled context into a PRD.\n - `to-issues`: break a PRD, plan, or Goal into independently grabbable vertical-slice issues.\ndiff --git a/docs/adr/0001-keep-shiki-as-platform-name.md b/docs/adr/SADR-0001-keep-shiki-as-platform-name.md\nindex 20a9637c3b9d8c97de7ba8db7cfae4d82d9a7dba..8b685439959aada1b221c112f0ac3c51ca8a1695\n--- a/docs/adr/0001-keep-shiki-as-platform-name.md\n+++ b/docs/adr/SADR-0001-keep-shiki-as-platform-name.md\n@@ -1,3 +1,3 @@\n-# Keep Shiki As The Platform Name\n+# SADR-0001: Keep Shiki As The Platform Name\n \n Shiki remains the product name for the GitHub-native agentic engineering control plane. We considered more descriptive names such as MergePilot, MergeGate, and Kiroku, but kept Shiki because it carries the existing goal-seeking framework identity while remaining independent of Hermes, Codex, Claude Code, or any single runtime.\ndiff --git a/docs/adr/0002-use-agents-as-shared-constitution.md b/docs/adr/SADR-0002-use-agents-as-shared-constitution.md\nindex 73de30ecb078120eac540d9bc51855e3cd48d0e3..c5486182bf06fca36ea508496b013c67302fd989\n--- a/docs/adr/0002-use-agents-as-shared-constitution.md\n+++ b/docs/adr/SADR-0002-use-agents-as-shared-constitution.md\n@@ -1,3 +1,3 @@\n-# Use AGENTS As The Shared Agent Constitution\n+# SADR-0002: Use AGENTS As The Shared Agent Constitution\n \n Shiki uses `AGENTS.md` as the runtime-neutral constitution and `CLAUDE.md` as a thin Claude Code wrapper. This keeps Codex, Claude Code, Hermes Runner, and future Agent Runtimes aligned on the same GitHub-first MergeGate rules while still allowing runtime-specific instructions where needed.\ndiff --git a/docs/adr/0003-require-skill-gate-for-mergegate-readiness.md b/docs/adr/SADR-0003-require-skill-gate-for-mergegate-readiness.md\nindex 424be0c9af4c8f0136f1fde68a468c47ff84e764..7fc337987c7df9792f75f181dfcbed75fe5ffff2\n--- a/docs/adr/0003-require-skill-gate-for-mergegate-readiness.md\n+++ b/docs/adr/SADR-0003-require-skill-gate-for-mergegate-readiness.md\n@@ -1,3 +1,3 @@\n-# Require Skill Gate For MergeGate Readiness\n+# SADR-0003: Require Skill Gate For MergeGate Readiness\n \n Shiki requires relevant engineering skills to be invoked before execution when their triggers apply. This makes skills part of MergeGate readiness rather than optional prompt style, because Shiki depends on repeatable planning, issue decomposition, TDD, diagnosis, triage, and architecture review before autonomous or parallel agent work can be trusted.\ndiff --git a/docs/adr/0004-use-github-first-with-shiki-mirror.md b/docs/adr/SADR-0004-use-github-first-with-shiki-mirror.md\nindex d0a3a9174dc646a945a9d325954ffaf00c352040..a79c54ae9ee8eed2a0524192b864da4e22a92d3f\n--- a/docs/adr/0004-use-github-first-with-shiki-mirror.md\n+++ b/docs/adr/SADR-0004-use-github-first-with-shiki-mirror.md\n@@ -1,4 +1,4 @@\n-# ADR 0004: Use GitHub First With A Repository-Local Shiki Mirror\n+# SADR-0004: Use GitHub First With A Repository-Local Shiki Mirror\n \n ## Status\n \ndiff --git a/docs/adr/0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md b/docs/adr/SADR-0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\nindex b339baf49b89d9290e79c3d51b36e7b2bde3bd95..c30a0d8b1fde0224d0ac7820e7870dccf721580d\n--- a/docs/adr/0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\n+++ b/docs/adr/SADR-0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\n@@ -1,4 +1,4 @@\n-# ADR 0005: Use Codex As Front And Claude Code Action As The GitHub Runtime\n+# SADR-0005: Use Codex As Front And Claude Code Action As The GitHub Runtime\n \n ## Status\n \ndiff --git a/docs/adr/0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md b/docs/adr/SADR-0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\nindex 4bcae9d1e52f0a37b8e9fbafa5cd14b6ee1993bd..7a38538a6ae9929567f0ade67e7ea6a60d0b3f7e\n--- a/docs/adr/0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\n+++ b/docs/adr/SADR-0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\n@@ -1,4 +1,4 @@\n-# ADR 0006: Separate Codex Implementation, CCA Completion, And MergeGate Authorization\n+# SADR-0006: Separate Codex Implementation, CCA Completion, And MergeGate Authorization\n \n ## Status\n \ndiff --git a/docs/adr/0007-packaging-and-release.md b/docs/adr/SADR-0007-packaging-and-release.md\nindex 79b84ddb97167372b44641e5b7860e400ce80822..bb445a2e2e856d9d1f5fdbe35de8d4e8cec0fb32\n--- a/docs/adr/0007-packaging-and-release.md\n+++ b/docs/adr/SADR-0007-packaging-and-release.md\n@@ -1,4 +1,4 @@\n-# ADR 0007: Packaging And Release For Shiki\n+# SADR-0007: Packaging And Release For Shiki\n \n ## Status\n \n@@ -25,7 +25,7 @@\n   not introduce third-party runtime dependencies or break the shim.\n - Releases must run inside the operator's subscription-authenticated toolchain\n   and must not require extra secrets beyond the built-in `GITHUB_TOKEN`\n-  (consistent with ADR 0005's no-API-key-by-default stance).\n+  (consistent with SADR-0005's no-API-key-by-default stance).\n - `.shiki/manifest.json` already encodes the install surface (`install.include`,\n   `install.create_directories`, `install.exclude_from_commit`), and it must not\n   be mutated as part of this change.\n@@ -34,7 +34,7 @@\n \n This is a hard-to-reverse platform decision (it sets the version source of\n truth, the release trigger, and the public install contract), so it is recorded\n-as an ADR.\n+as a SADR.\n \n ## Decision\n \n@@ -104,7 +104,7 @@\n 3. **Publishing to PyPI.** Rejected for now: Shiki is distributed as a\n    repository template + git-installable CLI, not a PyPI library; PyPI would add\n    credentials/secrets and a different versioning contract. Can be added later as\n-   its own ADR if a packaged distribution is needed.\n+   its own SADR if a packaged distribution is needed.\n 4. **Dynamic version (read `VERSION` via a build backend `dynamic` field).**\n    Rejected to keep `pyproject.toml` backend-agnostic and dependency-free; the\n    small duplication between `VERSION` and `pyproject.toml` is guarded by the\n@@ -123,4 +123,4 @@\n   releasing versions the whole platform but ships only `install.include` to\n   targets.\n - Future PyPI distribution or a console entry point are deferred and would each\n-  warrant their own ADR.\n+  warrant their own SADR.\ndiff --git a/docs/adr/0008-use-claude-code-as-default-implementer.md b/docs/adr/SADR-0008-use-claude-code-as-default-implementer.md\nindex dedf4510b7c7799521422f622029563d373db031..35c2625d908167fbc58855b5b63e283805f167e8\n--- a/docs/adr/0008-use-claude-code-as-default-implementer.md\n+++ b/docs/adr/SADR-0008-use-claude-code-as-default-implementer.md\n@@ -1,12 +1,12 @@\n-# ADR 0008: Use Claude Code As The Default Implementer Runtime\n+# SADR-0008: Use Claude Code As The Default Implementer Runtime\n \n ## Status\n \n-Proposed (supersedes ADR 0005 for the implementer default; keeps its auth model)\n+Proposed (supersedes SADR-0005 for the implementer default; keeps its auth model)\n \n ## Context\n \n-ADR 0005 made Codex Front the default implementation runtime, with Claude Code\n+SADR-0005 made Codex Front the default implementation runtime, with Claude Code\n restricted to planner and reviewer roles by the runtime registry, and the only\n autonomous implementation adapter was `shiki runner codex`.\n \n@@ -18,13 +18,13 @@\n - The platform's planning, review, and CCA layers already run on Claude Code,\n   and Claude Code's native capabilities (the built-in `/code-review` skill and\n   the Workflow multi-agent orchestration tool) have no Codex equivalent.\n-- The Goal lifecycle redesign (ADR 0009) requires an autonomous post-freeze\n+- The Goal lifecycle redesign (SADR-0009) requires an autonomous post-freeze\n   loop driven from the operator's Claude Code session, including bounded\n   repair dispatch, which must not depend on a runtime that cannot be invoked\n   unattended.\n \n The constitution claims Shiki is runtime-agnostic (\"not a Claude-only\n-workflow\", ADR 0002). Any change to the implementer default must preserve the\n+workflow\", SADR-0002). Any change to the implementer default must preserve the\n registry-based runtime model rather than hardcoding one vendor.\n \n ## Decision\n@@ -39,7 +39,7 @@\n   the task to review or repair-needed.\n - Keep Codex registered as an optional implementer runtime. `shiki runner\n   codex`, the Codex handoff templates, and the ChatGPT OAuth auth model from\n-  ADR 0005 remain supported for tasks explicitly assigned to `codex`.\n+  SADR-0005 remain supported for tasks explicitly assigned to `codex`.\n - Commit to mandating two Claude-native capabilities as loop contract in\n   follow-up Goals (status: the code-review gate was delivered by Goal G-B \u2014\n   skill registry, checklists item PR-12, default required_skills \u2014 while the\n@@ -60,7 +60,7 @@\n \n Out of scope: removing Codex support, changing the CCA runtime (Claude Code\n Action with `CLAUDE_CODE_OAUTH_TOKEN` stays), and API-key based automation\n-(still requires its own ADR per ADR 0005).\n+(still requires its own target ADR per SADR-0005).\n \n ## Consequences\n \n@@ -100,7 +100,7 @@\n   harness rejection of direct `codex exec` is outside Shiki's control, and the\n   plan's mandatory native capabilities cannot run on Codex at all.\n - **Remove Codex entirely**: rejected; it abandons the runtime-agnostic\n-  constitution (ADR 0002) for no operational gain and forecloses\n+  constitution (SADR-0002) for no operational gain and forecloses\n   cross-runtime adversarial review.\n - **GitHub Actions cloud implementation via claude-code-action**: rejected as\n   default; durable evidence is automatic but token cost, Node 24 workflow\ndiff --git a/docs/adr/0009-requirements-definition-and-spec-freeze.md b/docs/adr/SADR-0009-requirements-definition-and-spec-freeze.md\nindex 29b3f7871bef9203a8b4a831a65c39eb2f506e42..52e7dd69ad0aa4efe0b68c62678abbf7df02aaec\n--- a/docs/adr/0009-requirements-definition-and-spec-freeze.md\n+++ b/docs/adr/SADR-0009-requirements-definition-and-spec-freeze.md\n@@ -1,4 +1,4 @@\n-# ADR 0009: Merge Grill And Planning Into Requirements Definition With Spec Freeze\n+# SADR-0009: Merge Grill And Planning Into Requirements Definition With Spec Freeze\n \n ## Status\n \ndiff --git a/docs/adr/0010-external-ai-guardian-review.md b/docs/adr/SADR-0010-external-ai-guardian-review.md\nindex e6108bbe8bb4e73e0ff4684c8f78001a80438c4e..f3510b145664298adafb151b6f0db51b972f01ec\n--- a/docs/adr/0010-external-ai-guardian-review.md\n+++ b/docs/adr/SADR-0010-external-ai-guardian-review.md\n@@ -1,4 +1,4 @@\n-# ADR 0010: External AI Guardian Review As A First-Class Approval Authority\n+# SADR-0010: External AI Guardian Review As A First-Class Approval Authority\n \n ## Status\n \n@@ -8,7 +8,7 @@\n \n Shiki is intended to be an autonomous, recursive, self-improving agent\n framework: authority-in-the-loop, not human-in-the-loop. Until now the Guardian\n-approval required for high/critical-risk merges (guardian-policy.json, ADR\n+approval required for high/critical-risk merges (guardian-policy.json, SADR\n governance) recognized only human authority sources \u2014 a GitHub review approval,\n the `guardian:approved` label applied by a configured Guardian, or a comment\n carrying `Guardian approval granted` plus the current head SHA.\n@@ -89,7 +89,7 @@\n   autonomous design the operator intends and produced the impersonation\n   incident.\n - **Record AI approval as an operator comment**: rejected as forgery of human\n-  authority \u2014 the exact failure this ADR corrects.\n+  authority \u2014 the exact failure this SADR corrects.\n - **Deliver the artifact as a committed `.shiki/` file**: rejected; a committed\n   approval inside the PR being approved is forgeable by anyone who can push.\n - **A dedicated reviewer bot identity instead of a relayed comment**: deferred;\ndiff --git a/docs/adr/0011-autonomous-quality-gate-evidence.md b/docs/adr/SADR-0011-autonomous-quality-gate-evidence.md\nindex bca0246c5d46e44afbde15486a75eec425fc02f5..9c93eac16d8c3a6d4b11bd7224e3955102cd7db4\n--- a/docs/adr/0011-autonomous-quality-gate-evidence.md\n+++ b/docs/adr/SADR-0011-autonomous-quality-gate-evidence.md\n@@ -1,4 +1,4 @@\n-# ADR 0011: Autonomous Quality-Gate Evidence \u2014 Deterministic Facts And An Independent Verifier\n+# SADR-0011: Autonomous Quality-Gate Evidence \u2014 Deterministic Facts And An Independent Verifier\n \n ## Status\n \ndiff --git a/docs/adr/0012-autonomous-loop-closeout-to-main.md b/docs/adr/SADR-0012-autonomous-loop-closeout-to-main.md\nindex 637fad6dde4989c9c2bf0f81f676d8d29991123c..ec7d85f81030a676bcbba28b43eb365422a36eac\n--- a/docs/adr/0012-autonomous-loop-closeout-to-main.md\n+++ b/docs/adr/SADR-0012-autonomous-loop-closeout-to-main.md\n@@ -1,4 +1,4 @@\n-# 0012 \u2014 Autonomous loop pushes goal completion to main via a closeout PR\n+# SADR-0012 \u2014 Autonomous loop pushes goal completion to main via a closeout PR\n \n - Status: accepted\n - Date: 2026-06-18\ndiff --git a/docs/adr/0013-bootstrap-grants-actions-approve-pull-requests.md b/docs/adr/SADR-0013-bootstrap-grants-actions-approve-pull-requests.md\nindex b6c69d64f1c5ced346b88d6859586715b3b5bd0e..cf48ff4a6716267f539ca58edd1a852e7c7a2d64\n--- a/docs/adr/0013-bootstrap-grants-actions-approve-pull-requests.md\n+++ b/docs/adr/SADR-0013-bootstrap-grants-actions-approve-pull-requests.md\n@@ -1,4 +1,4 @@\n-# ADR 0013: Bootstrap Grants GitHub Actions Permission To Create And Approve Pull Requests\n+# SADR-0013: Bootstrap Grants GitHub Actions Permission To Create And Approve Pull Requests\n \n ## Status\n \n@@ -31,7 +31,7 @@\n security tradeoff: it lets repository automation satisfy a human-review gate. In\n a multi-maintainer repo that could let a compromised or buggy workflow\n self-approve changes. So whether bootstrap turns it on by default \u2014 and with\n-what default token scope \u2014 is an ADR-worthy decision, not an implementation\n+what default token scope \u2014 is a SADR-worthy decision, not an implementation\n detail.\n \n ## Decision\n@@ -48,7 +48,7 @@\n Specifically:\n \n - **`can_approve_pull_request_reviews` defaults to `true`.** Shiki's default\n-  posture is solo/AFK autonomous operation (ADR 0008/0009/0012), where the CCA\n+  posture is solo/AFK autonomous operation (SADR-0008/0009/0012), where the CCA\n   Review Bridge is the only path that can satisfy `required_review: true` after\n   the CCA and Guardian gates have already passed. The default makes a\n   freshly-bootstrapped repo able to run the autonomous loop end to end without a\ndiff --git a/docs/adr/0014-codex-app-external-ai-guardian-ui-adapter.md b/docs/adr/SADR-0014-codex-app-external-ai-guardian-ui-adapter.md\nindex 238db06279334caa341c117362df9271382a7fcd..65f12e9939321e1b3e389a34c822fc1549e5155d\n--- a/docs/adr/0014-codex-app-external-ai-guardian-ui-adapter.md\n+++ b/docs/adr/SADR-0014-codex-app-external-ai-guardian-ui-adapter.md\n@@ -1,4 +1,4 @@\n-# ADR 0014: Use Codex App As The External AI Guardian UI Adapter\n+# SADR-0014: Use Codex App As The External AI Guardian UI Adapter\n \n ## Status\n \ndiff --git a/docs/adr/0015-contract-approval-before-dispatch.md b/docs/adr/SADR-0015-contract-approval-before-dispatch.md\nindex fad6c656741a20a7684ba16080a7db159f63a52f..6061271a46d3f1d6ae7174477de3fb75673123a6\n--- a/docs/adr/0015-contract-approval-before-dispatch.md\n+++ b/docs/adr/SADR-0015-contract-approval-before-dispatch.md\n@@ -1,4 +1,4 @@\n-# ADR 0015: Approve The Task Contract Before Dispatch, Not The Diff After It\n+# SADR-0015: Approve The Task Contract Before Dispatch, Not The Diff After It\n \n ## Status\n \n@@ -9,7 +9,7 @@\n Guardian approval for high/critical risk is expressible only on a pull request.\n Every approval source in `guardian-policy.json` \u2014 GitHub review, the\n `guardian:approved` label, the `Guardian approval granted` comment, and the\n-`external_ai_guardian_review` artifact (ADR 0010/0014) \u2014 is bound to a PR, and\n+`external_ai_guardian_review` artifact (SADR-0010/0014) \u2014 is bound to a PR, and\n three of the four require the current head SHA. Approval therefore cannot exist\n before an implementation PR exists.\n \n@@ -81,7 +81,7 @@\n An envelope breach must surface only on the non-repairable policy gate, never on\n the repairable `MergeGate metadata check`. Otherwise the Goal Loop routes it to\n `dispatch_repair` \u2014 instructing a runner to make the breach go away \u2014 which is\n-exactly what this ADR forbids.\n+exactly what this SADR forbids.\n \n **Contract immutability.** After registration, an implementation PR must not\n change the governance fields of its own task contract (`scope`, `non_goals`,\n@@ -116,7 +116,7 @@\n fails any verdict that is not `complete`, and the signal the CCA prompt reads is\n computed from `evaluate_guardian_approval` alone. Without the OR-merge at the\n signal, a contract-approved PR still returns `needs_guardian`, the CCA job fails,\n-and the MergeGate policy check never runs \u2014 the gate this ADR changes would never\n+and the MergeGate policy check never runs \u2014 the gate this SADR changes would never\n be reached. The CCA *prompt* needs no change: a `contract_approval` entry in the\n signal's existing `sources` array satisfies its current wording.\n \n@@ -130,7 +130,7 @@\n written, and an approved contract lets its implementation PRs merge through the\n same automatic path as low/medium risk. The wasted implement-then-block cycle\n disappears. Guardian review shifts from reading diffs to reading contracts, which\n-is both earlier and smaller; the external AI Guardian path (ADR 0010/0014) works\n+is both earlier and smaller; the external AI Guardian path (SADR-0010/0014) works\n on contracts with no change.\n \n We accept that the Guardian authorizes a class of change without seeing its\n@@ -146,7 +146,7 @@\n adversarial tests for the new approval path equivalent in rigor to\n `test_shiki_governance_evidence.sh`.\n \n-Bootstrap costs, which apply only while this ADR is being implemented. The tasks\n+Bootstrap costs, which apply only while this SADR is being implemented. The tasks\n that build Contract Approval merge under the existing rules, so the ones\n dispatched after the pre-dispatch gate lands but before the approval path lands\n need a Guardian-approved Contract PR *and* a live Guardian approval on their\n@@ -210,7 +210,7 @@\n risk; they do not bound authorized work.\n \n **Auto-escalate risk on envelope breach instead of stopping.** Returns the\n-workflow to a diff-time Guardian decision \u2014 the exact behavior this ADR removes.\n+workflow to a diff-time Guardian decision \u2014 the exact behavior this SADR removes.\n \n **Resolve governance fields from the base snapshot only.** Rejected against the\n registration flow: implementation PRs add their own contract, so the base has no\ndiff --git a/docs/adr/0016-locks-bound-product-paths.md b/docs/adr/SADR-0016-locks-bound-product-paths.md\nindex b5e850f3f1f7f4ef6e7f9d1763a29a51bd300c45..46b40a9b99a7aae644c02bb458f5473ff0a90ce2\n--- a/docs/adr/0016-locks-bound-product-paths.md\n+++ b/docs/adr/SADR-0016-locks-bound-product-paths.md\n@@ -1,4 +1,4 @@\n-# ADR 0016: Locks bound product paths; the Shiki mirror is bound by record identity\n+# SADR-0016: Locks bound product paths; the Shiki mirror is bound by record identity\n \n ## Status\n \n@@ -6,8 +6,8 @@\n \n - Date: 2026-07-28\n - Deciders: operator (mizutani-140), Claude (planner)\n-- Related: ADR 0004 (GitHub-first with a `.shiki` mirror), ADR 0009 (Spec Freeze),\n-  ADR 0012 (autonomous loop closeout to main), ADR 0015 (Contract Approval before\n+- Related: SADR-0004 (GitHub-first with a `.shiki` mirror), SADR-0009 (Spec Freeze),\n+  SADR-0012 (autonomous loop closeout to main), SADR-0015 (Contract Approval before\n   dispatch), remediation goal task R-02, the live PR #179 deadlock.\n \n ## Context\n@@ -90,11 +90,11 @@\n files, documentation files and named source modules are nameable; runtime-\n allocated ledger, runner and report ids are not \u2014 and those live in `.shiki`,\n which locks no longer govern. Existing frozen contracts are brought into line by\n-a one-time Spec Amendment per plan, which must land before ADR 0015's contract\n+a one-time Spec Amendment per plan, which must land before SADR-0015's contract\n immutability makes `locks` unwritable from the implementation PR.\n \n **Lock state is resolved from the durable side.** MergeGate resolves the lock\n-registry, and ADR 0015's governance fields, from the base `.shiki` snapshot when\n+registry, and SADR-0015's governance fields, from the base `.shiki` snapshot when\n present, falling back to head only when absent. The `MergeGate metadata check`\n job is given `--base-shiki`, which it does not receive today. Locks are not\n derived from the live open-pull-request set: a pull request body is untrusted\n@@ -108,7 +108,7 @@\n Explicitly out of scope: moving MergeGate's contract resolution off the PR\n checkout wholesale; making the mirror a projection of GitHub state; introducing\n new lock kinds; moving ledger evidence out of the repository; and unwinding\n-ADR 0012's closeout pull request. Registration of multi-task goals is ADR 0015's\n+SADR-0012's closeout pull request. Registration of multi-task goals is SADR-0015's\n Contract PR and is not re-decided here.\n \n ## Consequences\n@@ -127,7 +127,7 @@\n \n Risks accepted. First, goal-scoped identity is only as strong as `goal_id`,\n which is resolved from the PR body and the head task file; the mitigation is to\n-add `goal_id` to ADR 0015's frozen governance field set, and until that lands a\n+add `goal_id` to SADR-0015's frozen governance field set, and until that lands a\n task file can name a foreign goal. Second, base-when-present resolution is\n unavailable on fork pull requests, where the head is contributor-controlled;\n fork pull requests already cannot complete a Shiki merge, and this decision does\n@@ -139,7 +139,7 @@\n path, not by implementation pull requests.\n \n New obligations: one Spec Amendment per frozen plan to rewrite `locks`, ordered\n-before ADR 0015 T1; extension of the adversarial suites in\n+before SADR-0015 T1; extension of the adversarial suites in\n `scripts/test_shiki_adversarial_state.sh` and\n `scripts/test_shiki_governance_evidence.sh` to cover the eleven newly-scoped\n directories; and updating `scripts/test_shiki_loop_lock_guard.sh`,\n@@ -151,32 +151,32 @@\n whether the loop's transitive sync of ledger `evidence` references can be\n deleted once the Contract PR carries its own source plan.\n \n-## Relationship to ADR 0004\n+## Relationship to SADR-0004\n \n-This ADR **clarifies something ADR 0004 left unspecified**. ADR 0004 decides that\n+This SADR **clarifies something SADR-0004 left unspecified**. SADR-0004 decides that\n GitHub is the operational source of truth and that each target repository keeps a\n `.shiki/` mirror. It says nothing about pull requests writing the mirror, does\n not name MergeGate, and does not make the mirror an input to a gate. Nothing in\n-ADR 0004's Decision or Consequences is amended, superseded or weakened: the\n+SADR-0004's Decision or Consequences is amended, superseded or weakened: the\n mirror still exists, still records the same artifacts, and pull request\n-boundaries remain the coordination surface. What this ADR settles is the question\n+boundaries remain the coordination surface. What this SADR settles is the question\n 0004 never reached \u2014 by what authority a pull request may write a mirror record \u2014\n and it answers it the way `.shiki/manifest.json` already declares\n (`pr_mutation: current-task-or-current-goal-only`).\n \n-It **amends ADR 0012**, whose Decision item 5 states that \"Loop-executed tasks\n+It **amends SADR-0012**, whose Decision item 5 states that \"Loop-executed tasks\n declare the `path:.shiki/**` lock, which covers every `.shiki` file the closeout\n stages ... so `files_outside_locks` is satisfied without per-file lock\n bookkeeping.\" That clause is replaced: the closeout's mirror writes are\n-authorized by record identity, not by a declared lock. ADR 0012's substantive\n+authorized by record identity, not by a declared lock. SADR-0012's substantive\n decision \u2014 that completion reaches `main` through a closeout pull request rather\n than local attestation \u2014 is preserved unchanged.\n \n-It is **complementary to ADR 0015**, not a substitute. ADR 0015 owns contract\n+It is **complementary to SADR-0015**, not a substitute. SADR-0015 owns contract\n immutability, the pre-dispatch gate, the Contract PR and the Scope Envelope. This\n-ADR owns what a lock means and who may write the mirror. The one ordering\n+SADR owns what a lock means and who may write the mirror. The one ordering\n constraint between them is that the lock-narrowing Spec Amendment must precede\n-ADR 0015 T1's freezing of the `locks` field.\n+SADR-0015 T1's freezing of the `locks` field.\n \n ## Alternatives Considered\n \n@@ -186,7 +186,7 @@\n the only writer that can register them may run. Migrating the Guardian signal to\n that store makes every registration PR fail closed to `needs_guardian` at every\n risk level, because the task file is by definition absent from the store, which\n-removes the low/medium autonomous path ADR 0008/0009 exists to provide. And it\n+removes the low/medium autonomous path SADR-0008/0009 exists to provide. And it\n leaves schemas, manifest, config, guardian policy, templates, migrations, plans,\n worktrees, runner records, runs and handoffs with no writer at all in a\n repository whose product is `.shiki`. Measured benefit on the frozen backlog:\n@@ -199,14 +199,14 @@\n namespace is not in fact ownership-partitioned \u2014 56 ledger entries carry no\n `task_id`, and one goal DAG is writable by 35 registered tasks. The correct\n expression of its insight is that locks do not govern the mirror at all, which is\n-what this ADR decides.\n+what this SADR decides.\n \n **Make the mirror a projection written only by a registration writer and a\n post-merge projection job.** Rejected. It requires a bypass actor with write\n access to the protected default branch; it makes its own master invariant\n readable from a manifest that pull requests may edit; it deletes both reconcile\n modes and leaves a corrupted mirror with no in-band repair path, contradicting\n-ADR 0004's consequence that mirror conflicts are repaired against GitHub state;\n+SADR-0004's consequence that mirror conflicts are repaired against GitHub state;\n and its content-derived ids are rejected by `validate_shiki`'s currently-green id\n pattern.\n \ndiff --git a/docs/adr/0017-bookkeeping-closeouts-do-not-inherit-risk.md b/docs/adr/SADR-0017-bookkeeping-closeouts-do-not-inherit-risk.md\nindex 554e3a0d526a72ca067108c5464cc68c3edcf174..b9676857a4713850e063af7c39c8e0b01ecb4c66\n--- a/docs/adr/0017-bookkeeping-closeouts-do-not-inherit-risk.md\n+++ b/docs/adr/SADR-0017-bookkeeping-closeouts-do-not-inherit-risk.md\n@@ -1,4 +1,4 @@\n-# ADR 0017: A Bookkeeping Closeout Does Not Inherit Its Task's Risk\n+# SADR-0017: A Bookkeeping Closeout Does Not Inherit Its Task's Risk\n \n ## Status\n \n@@ -6,7 +6,7 @@\n \n ## Context\n \n-ADR 0012 made the goal loop push a task's completion to the default branch through a\n+SADR-0012 made the goal loop push a task's completion to the default branch through a\n closeout pull request: after the implementation PR merges, the loop opens a second PR\n carrying `task.status = done`, `lock.state = released`, and \u2014 when the task completes\n its goal \u2014 `goal.status = complete` plus one scorecard report. Nothing else. The\n@@ -49,14 +49,14 @@\n 4. The task file's governance fields (`scope`, `non_goals`, `required_skills`,\n    `risk_level`, `locks`, `acceptance_checks`, `test_command`) are byte-identical to\n    the base snapshot. Only `status`, `expected_pr`, `closeout_pr`, `ledger_evidence`\n-   and `expected_branch` may differ \u2014 the ADR 0015 contract-immutability rule,\n+   and `expected_branch` may differ \u2014 the SADR-0015 contract-immutability rule,\n    unchanged.\n \n    `expected_branch` is in that set because a closeout is *by construction* on a\n    different branch from the implementation it closes, and the loop rewrites the\n    field accordingly. It is safe to let it differ because MergeGate independently\n    requires `expected_branch` to equal the PR's actual head ref, so a PR cannot set\n-   it to anything other than the branch it is really on. The first draft of this ADR\n+   it to anything other than the branch it is really on. The first draft of this SADR\n    omitted it, which made the exemption unreachable: every real closeout failed\n    condition 4 on a field the loop always changes. See PR #209.\n 5. The state transitions are exactly the terminal set: task `review` \u2192 `done`, lock\n@@ -113,12 +113,12 @@\n classification must be derived, not asserted.\n \n **Fold the closeout into the implementation PR** so no second PR exists. Rejected under\n-ADR 0012, which recorded why: a task file that is already `done` on the implementation\n+SADR-0012, which recorded why: a task file that is already `done` on the implementation\n branch short-circuits the loop's review \u2192 CCA \u2192 merge \u2192 done ordering into a local\n goal-completion before any merge happens.\n \n **Have the loop auto-merge closeouts with elevated credentials.** Rejected outright: that\n-is a runtime authoring its own approval, which ADR 0010 and the constitution's\n+is a runtime authoring its own approval, which SADR-0010 and the constitution's\n approval-record prohibition forbid. It would also be indistinguishable, in the evidence,\n from the forgery those rules exist to prevent.\n \ndiff --git a/docs/adr/0018-carried-guardian-approval-across-a-proven-base-sync.md b/docs/adr/SADR-0018-carried-guardian-approval-across-a-proven-base-sync.md\nindex 22358562da390ed8b45cd600da08a43d378b4680..e6e2907cefa776460af73ddf7d39db605f3c39b0\n--- a/docs/adr/0018-carried-guardian-approval-across-a-proven-base-sync.md\n+++ b/docs/adr/SADR-0018-carried-guardian-approval-across-a-proven-base-sync.md\n@@ -1,4 +1,4 @@\n-# ADR 0018: Carried Guardian Approval Across A Proven Base Sync\n+# SADR-0018: Carried Guardian Approval Across A Proven Base Sync\n \n ## Status\n \n@@ -34,7 +34,7 @@\n Guardian evaluator that consumes it (`shiki_guardian.evaluate_guardian_approval`,\n the `guardian_comment_carried` source) were built and tested in isolation. Neither\n does anything until a caller passes the flags that turn the carry on, so the\n-feature was invisible in CI. This ADR records the decision the two of them\n+feature was invisible in CI. This SADR records the decision the two of them\n implement, and this task wires it into the four CI invocations that judge Guardian\n approval.\n \ndiff --git a/docs/adr/README.md b/docs/adr/README.md\nindex c70e057a4520c36e042ab68c53cd4a21ef709b71..5da43856836e2ce6a150f8127766e2f88355ab4b\n--- a/docs/adr/README.md\n+++ b/docs/adr/README.md\n@@ -1,40 +1,41 @@\n-# Architecture Decision Records\n+# Decision Record Namespaces\n \n-This directory holds Shiki's Architecture Decision Records (ADRs): durable\n-records of hard-to-reverse platform decisions. ADRs are part of the Shiki source\n-of truth alongside GitHub state, the `.shiki/` mirror, and `CONTEXT.md`.\n+This directory holds two independent decision namespaces:\n \n-## How ADRs Work\n+- Shiki Architecture Decision Records (SADRs), named\n+  `SADR-NNNN-<short-slug>.md`, record hard-to-reverse Shiki platform decisions.\n+- Target Architecture Decision Records (ADRs), named\n+  `NNNN-<short-slug>.md`, belong to the target product.\n \n-- New ADRs are added as `docs/adr/NNNN-<short-slug>.md`, where `NNNN` is the next\n-  zero-padded sequence number.\n-- The **canonical list of ADRs is the set of files matching `docs/adr/*.md`** in\n-  this directory. Browse `docs/adr/*.md` for the authoritative, current set.\n-- Use [`template.md`](template.md) as the starting point for a new ADR.\n-- Record an ADR when a decision is hard to reverse, surprising without context,\n-  and a real tradeoff. Do not write an ADR for routine or easily reversible\n-  choices.\n+The same four-digit number may appear once in each namespace. A platform\n+`SADR-NNNN` citation resolves only to `SADR-NNNN-*.md`; a target `ADR NNNN`\n+citation resolves only to `NNNN-*.md`.\n \n-Using the `docs/adr/*.md` glob as the source of truth means other PRs can add an\n-ADR without editing this index file, avoiding merge contention on a\n-hand-maintained enumeration.\n+## How Decision Records Work\n \n-## Current ADRs (snapshot)\n-\n-The list below is a convenience snapshot and may lag behind the directory. When\n-in doubt, list `docs/adr/*.md`.\n+- New Shiki platform decisions use the next zero-padded SADR number.\n+- New target product decisions use the target's own ADR sequence.\n+- The canonical list is the files in `docs/adr/`; no hand-maintained index is\n+  authoritative.\n+- Use [`template.md`](template.md) as the starting point and choose the filename\n+  namespace that owns the decision.\n+- Record a decision when it is hard to reverse, surprising without context, and\n+  a real tradeoff. Do not write a decision record for routine or easily\n+  reversible choices.\n \n-- [0001 \u2014 Keep Shiki As The Platform Name](0001-keep-shiki-as-platform-name.md)\n-- [0002 \u2014 Use AGENTS As The Shared Agent Constitution](0002-use-agents-as-shared-constitution.md)\n-- [0003 \u2014 Require Skill Gate For MergeGate Readiness](0003-require-skill-gate-for-mergegate-readiness.md)\n-- [0004 \u2014 Use GitHub First With A Repository-Local Shiki Mirror](0004-use-github-first-with-shiki-mirror.md)\n-- [0005 \u2014 Use Codex As Front And Claude Code Action As The GitHub Runtime](0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md)\n-- [0006 \u2014 Separate Codex Implementation, CCA Completion, And MergeGate Authorization](0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md)\n+Using the directory as the source of truth lets independent changes add records\n+without contending on an enumerated index.\n \n-## Adding A New ADR\n+## Adding A Shiki SADR\n \n-1. Copy `template.md` to `docs/adr/NNNN-<short-slug>.md`.\n+1. Copy `template.md` to `docs/adr/SADR-NNNN-<short-slug>.md`.\n 2. Fill in the title, status, context, decision, and consequences.\n-3. Set the status to `Proposed`, then `Accepted` once the decision is approved.\n-4. Open a PR following `CONTRIBUTING.md`. You do not need to edit this index for\n-   the ADR to be discoverable via `docs/adr/*.md`.\n+3. Set the status to `Proposed`, then `Accepted` once the proper authority has\n+   approved the decision.\n+4. Open a PR following `CONTRIBUTING.md`.\n+\n+## Adding A Target ADR\n+\n+1. Copy `template.md` to `docs/adr/NNNN-<short-slug>.md` using the target's own\n+   sequence.\n+2. Cite it as `ADR NNNN`; do not prefix its filename with `SADR-`.\ndiff --git a/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md b/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\nnew file mode 100644\nindex 0000000000000000000000000000000000000000..7455ce1f3bed02ddddb851ac21a1d1a648c9aeba\n--- /dev/null\n+++ b/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\n@@ -0,0 +1,55 @@\n+# SADR-0019: Separate Shiki Platform SADRs From Target ADRs\n+\n+## Status\n+\n+Accepted\n+\n+## Context\n+\n+Shiki installs its platform decision records into `docs/adr/` in every target\n+repository. Shiki and its targets previously used the same numeric\n+`NNNN-<slug>.md` namespace. A target that already owned ADRs 0013 through 0018\n+therefore acquired two records for each number during installation, making an\n+`ADR NNNN` citation ambiguous. Renumbering target decisions would rewrite target\n+history, while renumbering Shiki decisions per target would destroy their stable\n+platform identity.\n+\n+Upgrades also need to remove the former Shiki-owned numeric paths without ever\n+mistaking a target-owned ADR for platform content. The install stamp already\n+records exact shipped paths and content digests, so it can prove ownership only\n+when both still match.\n+\n+## Decision\n+\n+Shiki platform decisions use the `SADR` namespace:\n+\n+- records are named `docs/adr/SADR-NNNN-<slug>.md`;\n+- live platform citations use `SADR-NNNN`;\n+- target product decisions retain `docs/adr/NNNN-<slug>.md` and `ADR NNNN`;\n+- the reference validator resolves the two namespaces independently, so the same\n+  four-digit number may exist once in each namespace.\n+\n+Before a forced upgrade writes anything, the installer inventories the exact\n+former numeric Shiki ADR paths. It may delete a legacy path only when the\n+existing install stamp contains that exact path and its recorded SHA-256 digest\n+matches the file currently on disk. All ownership blockers are reported\n+together and abort the upgrade before its first write. A non-forced install\n+never performs this cleanup.\n+\n+Historical `.shiki/` evidence remains unchanged because it records the namespace\n+that existed when that evidence was produced.\n+\n+## Consequences\n+\n+- Targets may use any ADR number without colliding with Shiki's platform SADRs.\n+- Fresh installs preserve target ADR bytes and install a stable SADR set.\n+- Forced upgrades fail closed when legacy ownership cannot be proven, and a\n+  successful upgrade refreshes the stamp without removed legacy paths.\n+- Current governance, packaging, workflow, prompt, script, and skill references\n+  must move with the platform records.\n+\n+## Alternatives Considered\n+\n+- Collision-only refusal leaves affected targets permanently unupgradable.\n+- Renumbering target ADRs mutates target-owned history.\n+- Per-target Shiki renumbering makes platform citations non-portable.\ndiff --git a/docs/adr/template.md b/docs/adr/template.md\nindex a92a97daddd0b00ab9dd626cc8d86942da2eafed..17dd79175b5fee2105935557f691b40cf9492942\n--- a/docs/adr/template.md\n+++ b/docs/adr/template.md\n@@ -1,14 +1,14 @@\n-# NNNN: <Short Decision Title>\n+# <SADR-NNNN or NNNN>: <Short Decision Title>\n \n ## Status\n \n-Proposed | Accepted | Superseded by ADR-NNNN\n+Proposed | Accepted | Superseded by SADR-NNNN or target ADR NNNN\n \n ## Context\n \n What is the situation, constraint, or force that requires a decision? Reference\n-relevant domain terms (`CONTEXT.md`), prior ADRs, and code reality. State the\n-problem, not the solution.\n+relevant domain terms (`CONTEXT.md`), prior Shiki SADRs or target ADRs, and code\n+reality. State the problem, not the solution.\n \n ## Decision\n \ndiff --git a/docs/agents/autonomous-loop-e2e-acceptance.md b/docs/agents/autonomous-loop-e2e-acceptance.md\nindex bbf84c6081987f159ac6ad9ecf3c82b7aa72e7e8..5c13c1d02a05a90b0dc46cbef7c95a9846d0c237\n--- a/docs/agents/autonomous-loop-e2e-acceptance.md\n+++ b/docs/agents/autonomous-loop-e2e-acceptance.md\n@@ -40,8 +40,8 @@\n 3. `gh auth status` is logged in with merge permission on the repo.\n 4. Branch protection + required checks are configured exactly as the goal-loop\n    required-check set expects (see `docs/agents/branch-protection-smoke.md`).\n-5. The repo `.shiki/policy` permits autonomous low/medium-risk merges (ADR\n-   0008/0009); the MergeGate policy check is the recorded-authority gate.\n+5. The repo `.shiki/policy` permits autonomous low/medium-risk merges\n+   (SADR-0008/0009); the MergeGate policy check is the recorded-authority gate.\n \n The operator's only actions are: register the task and start the loop. **No\n manual pre-clearing** means after `shiki loop run` starts, the operator does not\ndiff --git a/docs/agents/domain.md b/docs/agents/domain.md\nindex 258da8ea81e87ac0a6b0b5b5e5c2fde980eaabb7..67aaa6f4b7d0a5aa4d495ce6279073333f7798cb\n--- a/docs/agents/domain.md\n+++ b/docs/agents/domain.md\n@@ -5,7 +5,9 @@\n ## Layout\n \n - `CONTEXT.md` defines Shiki language. Keep it implementation-free.\n-- `docs/adr/` records hard-to-reverse decisions.\n+- `docs/adr/SADR-NNNN-*.md` records Shiki platform decisions.\n+- `docs/adr/NNNN-*.md` records target product decisions without sharing the\n+  SADR namespace.\n - `AGENTS.md` is the runtime-neutral agent constitution.\n - `CLAUDE.md` is the Claude Code wrapper around `AGENTS.md`.\n - `.shiki/` is the Target Repository mirror for Goals, PRDs, plans, Task DAGs, contracts, locks, worktree records, ledger entries, CCA verdicts, repair packets, reports, and handoffs.\ndiff --git a/docs/agents/guardian-policy.md b/docs/agents/guardian-policy.md\nindex 4147c7adf05a47adb4fd24ada1a1c2e1f2ba6d2d..a20d9cc6c00a569bb057b9ca8d6b51c7fc4a9847\n--- a/docs/agents/guardian-policy.md\n+++ b/docs/agents/guardian-policy.md\n@@ -98,8 +98,8 @@\n `origin/<default_branch>` and reuses each job's existing base fetch; a shallow\n (`--depth`) or second base fetch would make the proof unresolvable and silently\n disable the carry, so neither is permitted. CCA reads `guardian_comment_carried` as\n-a recorded authority in the deterministic signal and does not re-derive it. See ADR\n-0018 for the full decision, the rejected diff/patch-equality and path-disjointness\n+a recorded authority in the deterministic signal and does not re-derive it. See\n+SADR-0018 for the full decision, the rejected diff/patch-equality and path-disjointness\n alternatives, and the accepted residual.\n \n ## External AI Guardian Review (SADR-0010)\ndiff --git a/docs/agents/implementation-policy.md b/docs/agents/implementation-policy.md\nindex c754144a9fb5bf4a38aa82a60e1670c59e626b44..e90d40fe21880b9a09719c5ecfe6bde7284eb3ce\n--- a/docs/agents/implementation-policy.md\n+++ b/docs/agents/implementation-policy.md\n@@ -31,6 +31,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n ## Non-Negotiables\n \n 1. **Planning precedes implementation.** Non-trivial Goals start with `grill-with-docs` and Context & Impact.\ndiff --git a/scripts/guardian_approval_signal.py b/scripts/guardian_approval_signal.py\nindex d059f92b2c82aa2b5d6ca421365537a87886c1cb..16924892714f4f5b9303c0ef0e325a7e70075630\n--- a/scripts/guardian_approval_signal.py\n+++ b/scripts/guardian_approval_signal.py\n@@ -199,8 +199,8 @@\n ) -> Any:\n     \"\"\"Evaluate SADR-0015 Contract Approval for the CCA Guardian signal.\n \n-    The SAME OR that MergeGate applies must reach this deterministic signal: ADR\n-    0015 is explicit that without it, a contract-approved PR still returns\n+    The SAME OR that MergeGate applies must reach this deterministic signal:\n+    SADR-0015 is explicit that without it, a contract-approved PR still returns\n     ``needs_guardian``, the CCA job fails, and the MergeGate policy check never\n     runs \u2014 the gate this change moves would never be reached. This resolves the\n     task the SAME way MergeGate does (the first Shiki task id in the PR body),\ndiff --git a/scripts/shiki_contract_approval.py b/scripts/shiki_contract_approval.py\nindex 941bcb96fdd8b9a85bc0c4f0f04d5dad9d04839c..4a4b418f6527413161db82670bec857a043360e2\n--- a/scripts/shiki_contract_approval.py\n+++ b/scripts/shiki_contract_approval.py\n@@ -142,7 +142,7 @@\n     changed_files_status: Any,\n     registration: Any,\n ) -> ContractApprovalResult:\n-    \"\"\"Whether this PR carries an SADR-0015 Contract Approval.\n+    \"\"\"Whether this PR carries a SADR-0015 Contract Approval.\n \n     The four conditions are evaluated IN ORDER, each failure recorded as a\n     distinct reason string:\ndiff --git a/scripts/shiki_installer.py b/scripts/shiki_installer.py\nindex be064ab7a29ff6836f7f7a1d70500a43e0d11d7b..d3e3247edbf3e4be287a4667176d86f6500f8dc5\n--- a/scripts/shiki_installer.py\n+++ b/scripts/shiki_installer.py\n@@ -118,6 +118,7 @@\n # what lets a target carry it without a governance-schema change.\n INSTALL_STAMP_PATH = \".shiki/install-stamp.json\"\n INSTALL_STAMP_VERSION = 1\n+LEGACY_SHIKI_SADR_MAX = 18\n \n # Under --force the shipped surface splits three ways, so nothing is lost and\n # nothing is silently stale.\n@@ -237,9 +238,98 @@\n         )\n \n \n+def legacy_shiki_adr_paths() -> tuple[str, ...]:\n+    \"\"\"Former numeric paths for Shiki SADR-0001 through SADR-0018.\n+\n+    The authoritative slugs come from the current SADR filenames. SADR-0019 is\n+    the namespace decision itself and never shipped under a numeric legacy path.\n+    \"\"\"\n+    paths: list[str] = []\n+    for sadr in sorted((ROOT / \"docs\" / \"adr\").glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\")):\n+        number = sadr.name.split(\"-\", 2)[1]\n+        if int(number) <= LEGACY_SHIKI_SADR_MAX:\n+            paths.append(f\"docs/adr/{sadr.name.removeprefix('SADR-')}\")\n+    return tuple(paths)\n+\n+\n+def preflight_legacy_sadr_cleanup(target: Path, *, force: bool) -> tuple[str, ...]:\n+    \"\"\"Authorize exact legacy Shiki ADR removals before the first write.\n+\n+    A non-force install never removes existing files. A forced install needs no\n+    stamp when no legacy Shiki path is present. When legacy paths do exist, each\n+    must be named in a readable install stamp and its current bytes must match\n+    the recorded digest.\n+    \"\"\"\n+    if not force:\n+        return ()\n+    present = tuple(\n+        relative\n+        for relative in legacy_shiki_adr_paths()\n+        if (target / relative).exists() or (target / relative).is_symlink()\n+    )\n+    if not present:\n+        return ()\n+\n+    stamp_path = target / INSTALL_STAMP_PATH\n+    try:\n+        raw = stamp_path.read_text(encoding=\"utf-8\")\n+        stamp = json.loads(raw)\n+    except FileNotFoundError:\n+        detail = \"install stamp is absent\"\n+        blockers = [f\"{relative}: {detail}\" for relative in present]\n+    except (OSError, UnicodeDecodeError) as error:\n+        detail = f\"install stamp is unreadable ({error})\"\n+        blockers = [f\"{relative}: {detail}\" for relative in present]\n+    except json.JSONDecodeError as error:\n+        detail = f\"install stamp is malformed ({error})\"\n+        blockers = [f\"{relative}: {detail}\" for relative in present]\n+    else:\n+        if not isinstance(stamp, dict):\n+            blockers = [\n+                f\"{relative}: install stamp is malformed (expected an object)\"\n+                for relative in present\n+            ]\n+        else:\n+            digests = stamp.get(\"digests\")\n+            blockers = []\n+            if not isinstance(digests, dict):\n+                blockers.extend(\n+                    f\"{relative}: install stamp has no digest for this path\"\n+                    for relative in present\n+                )\n+            else:\n+                for relative in present:\n+                    expected = digests.get(relative)\n+                    if not isinstance(expected, str) or not expected:\n+                        blockers.append(\n+                            f\"{relative}: install stamp has no digest for this path\"\n+                        )\n+                        continue\n+                    try:\n+                        actual = _sha256_file(target / relative)\n+                    except OSError as error:\n+                        blockers.append(\n+                            f\"{relative}: legacy path is unreadable ({error})\"\n+                        )\n+                        continue\n+                    if actual != expected:\n+                        blockers.append(\n+                            f\"{relative}: digest mismatch (stamp {expected}, current {actual})\"\n+                        )\n+\n+    if blockers:\n+        joined = \"\\n\".join(f\"- {blocker}\" for blocker in blockers)\n+        raise ShikiError(\n+            \"legacy Shiki ADR cleanup is not authorized:\\n\"\n+            f\"{joined}\\nNo files were written.\"\n+        )\n+    return present\n+\n+\n def install_template(target: Path, *, force: bool, validate: bool) -> list[NewFileNote]:\n     # Preconditions first: never rewrite the tree of a target that cannot be\n     # upgraded cleanly (see refuse_pending_migrations).\n+    legacy_cleanup = preflight_legacy_sadr_cleanup(target, force=force)\n     refuse_pending_migrations(target)\n \n     # Resolve the target's required CODEOWNERS owner once from its\n@@ -250,6 +340,10 @@\n     # verbatim. See ``codeowners_required_owner``.\n     codeowners_owner = codeowners_required_owner(target)\n \n+    for relative in legacy_cleanup:\n+        (target / relative).unlink()\n+        info(f\"removed stamped legacy Shiki ADR: {target / relative}\")\n+\n     notes: list[NewFileNote] = []\n     for relative in TEMPLATE_PATHS:\n         source = ROOT / relative\ndiff --git a/scripts/shiki_loop.py b/scripts/shiki_loop.py\nindex 3611641da8aeb5038b1da90e3114b09a473ec6d8..84caa491b9cad0edc4176f209c13e9221e975681\n--- a/scripts/shiki_loop.py\n+++ b/scripts/shiki_loop.py\n@@ -424,7 +424,7 @@\n         # unresolvable CCA verdict has already produced its terminal stop and is never\n         # laundered into a head-moving sync (SADR-0010). It also never fires while the\n         # policy (Guardian) gate is red \u2014 for ANY reason, not only when it is the sole\n-        # red check \u2014 so a head-bound Guardian approval, or an SADR-0015\n+        # red check \u2014 so a head-bound Guardian approval, or a SADR-0015\n         # contract-approval carry that base movement defeated (the policy gate compares\n         # base and head task governance directly), is never silently restored by\n         # adopting base wholesale. It is bounded by a durable per-task counter so the\ndiff --git a/scripts/test_shiki_goal_loop.sh b/scripts/test_shiki_goal_loop.sh\nindex 753c7b516e10f0be3bc92a4bd0eb1fa699776ae6..8f3820a0a079b3044424cfe1a6c6b7fe68896e67\n--- a/scripts/test_shiki_goal_loop.sh\n+++ b/scripts/test_shiki_goal_loop.sh\n@@ -231,8 +231,8 @@\n \n # High/critical risk merges autonomously when all required checks are green:\n # the \"MergeGate policy check\" required check IS the Guardian gate (it enforces\n-# guardian-policy.json \u2014 human approval OR external AI guardian review, ADR\n-# 0010), so green checks mean a recorded authority approved. The fake gh returns\n+# guardian-policy.json \u2014 human approval OR external AI guardian review,\n+# SADR-0010), so green checks mean a recorded authority approved. The fake gh returns\n # green checks, simulating that the Guardian gate passed.\n python3 \"$ROOT/scripts/shiki.py\" goal create --target \"$TARGET\" --title \"Guardian gate\" --outcome \"High risk merges via policy gate\" >/tmp/shiki-goal-loop-guardian-goal.json\n GGOAL=\"$(json_get /tmp/shiki-goal-loop-guardian-goal.json goal_id)\"\ndiff --git a/tests/test_adr_references.py b/tests/test_adr_references.py\nindex 51706329c797d5dd9b47e082a333fa83b261d482..db6e693e35f67103935be34f1d9bb4ff7ee60395\n--- a/tests/test_adr_references.py\n+++ b/tests/test_adr_references.py\n@@ -1,7 +1,7 @@\n-\"\"\"Every ``ADR ####`` citation must resolve to exactly one decision record.\n+\"\"\"Every target ADR and Shiki SADR citation resolves in its own namespace.\n \n Background. ``scripts/mergegate_check.py`` justifies ``is_bookkeeping_closeout``\n-with \"ADR 0017\" nine times and ``scripts/guardian_approval_signal.py`` cites\n+with \"SADR-0017\" nine times and ``scripts/guardian_approval_signal.py`` cites\n 0016/0017 seven times, yet for a stretch the numbering in ``docs/adr/`` stopped\n at 0015: the authoring commits lived on a coordinator branch and no task ever\n declared ``docs/adr/...`` in its locks, so nothing carried the records to the\n@@ -11,11 +11,12 @@\n and the directory drifted with no signal.\n \n This is the same unbound-pair shape behind other silent failures: a reference\n-and its target with nothing asserting they agree. This test binds them. It scans\n-every ``scripts/*.py`` (plus ``docs/**/*.md`` and the root ``*.md`` governance\n-files, which are cheap) for the ``ADR ####`` pattern and asserts each cited\n-number resolves to exactly one ``docs/adr/####-*.md``. The converse is\n-deliberately NOT required: an ADR may exist without being cited.\n+and its target with nothing asserting they agree. This test binds them. Target\n+``ADR NNNN`` citations resolve only to ``docs/adr/NNNN-*.md``; Shiki\n+``SADR-NNNN`` citations resolve only to ``docs/adr/SADR-NNNN-*.md``. The scan\n+covers current governance docs, scripts, workflows, prompts, packaging metadata,\n+and repo-local Codex guidance while deliberately excluding historical\n+``.shiki`` evidence. The converse is not required: a record may be uncited.\n \n The suite is dependency free: standard-library ``unittest`` only.\n \"\"\"\n@@ -31,17 +32,18 @@\n \n REPO_ROOT = Path(__file__).resolve().parents[1]\n \n-# \"ADR 0016\" -- a four-digit citation. The trailing \\b keeps a five-digit token\n-# from being read as a truncated four-digit number.\n-_CITATION = re.compile(r\"\\bADR (\\d{4})\\b\")\n-# \"0016-locks-bound-product-paths.md\" -- the leading four digits name the record.\n-_ADR_FILENAME = re.compile(r\"^(\\d{4})-.*\\.md$\")\n+# ``ADR 0016`` / ``SADR-0016`` -- a namespace plus four-digit citation. The\n+# trailing ``\\b`` keeps a five-digit token from being read as a truncated one.\n+_CITATION = re.compile(r\"\\b(S?ADR)[ -](\\d{4})\\b\")\n+# Target ADRs retain the numeric filename; Shiki platform decisions use SADR.\n+_DECISION_FILENAME = re.compile(r\"^(?:(SADR)-)?(\\d{4})-.*\\.md$\")\n \n \n class Violation(NamedTuple):\n     \"\"\"A citation that does not resolve to exactly one decision record.\"\"\"\n \n     citing_file: str  # repo-relative path of the file that cites the ADR\n+    namespace: str  # \"ADR\" for target decisions; \"SADR\" for Shiki decisions\n     number: str  # the cited four-digit number, e.g. \"0016\"\n     line: int  # 1-based line of the citation\n     reason: str  # \"missing\" (zero matches) or \"ambiguous\" (two or more)\n@@ -49,44 +51,59 @@\n \n     @property\n     def expected_path(self) -> str:\n-        return f\"docs/adr/{self.number}-*.md\"\n+        prefix = \"SADR-\" if self.namespace == \"SADR\" else \"\"\n+        return f\"docs/adr/{prefix}{self.number}-*.md\"\n \n     def describe(self) -> str:\n         \"\"\"Name the citing file, the cited number, and the missing path.\"\"\"\n         if self.reason == \"missing\":\n             return (\n-                f\"{self.citing_file}:{self.line} cites ADR {self.number} \"\n+                f\"{self.citing_file}:{self.line} cites {self.namespace} {self.number} \"\n                 f\"but no document matches {self.expected_path}\"\n             )\n         joined = \", \".join(self.matches)\n         return (\n-            f\"{self.citing_file}:{self.line} cites ADR {self.number} which \"\n+            f\"{self.citing_file}:{self.line} cites {self.namespace} {self.number} which \"\n             f\"resolves to {len(self.matches)} documents (expected exactly one \"\n             f\"at {self.expected_path}): {joined}\"\n         )\n \n \n-def _adr_index(root: Path) -> dict[str, list[Path]]:\n-    \"\"\"Map every four-digit ADR number to the record files that claim it.\"\"\"\n-    index: dict[str, list[Path]] = {}\n+def _adr_index(root: Path) -> dict[tuple[str, str], list[Path]]:\n+    \"\"\"Map each decision namespace and number to the files that claim it.\"\"\"\n+    index: dict[tuple[str, str], list[Path]] = {}\n     adr_dir = root / \"docs\" / \"adr\"\n     if adr_dir.is_dir():\n         for path in sorted(adr_dir.glob(\"*.md\")):\n-            match = _ADR_FILENAME.match(path.name)\n+            match = _DECISION_FILENAME.match(path.name)\n             if match:\n-                index.setdefault(match.group(1), []).append(path)\n+                namespace = \"SADR\" if match.group(1) else \"ADR\"\n+                index.setdefault((namespace, match.group(2)), []).append(path)\n     return index\n \n \n def _citing_files(root: Path, include_docs: bool = True) -> list[Path]:\n-    \"\"\"Files scanned for citations: scripts/*.py, and cheaply the markdown.\"\"\"\n+    \"\"\"Current, shipped text surfaces scanned for decision citations.\"\"\"\n     files = sorted((root / \"scripts\").glob(\"*.py\"))\n+    files += sorted((root / \"scripts\").glob(\"*.sh\"))\n     if include_docs:\n         docs = root / \"docs\"\n         if docs.is_dir():\n             files += sorted(docs.rglob(\"*.md\"))\n         files += sorted(root.glob(\"*.md\"))\n-    return files\n+        pyproject = root / \"pyproject.toml\"\n+        if pyproject.is_file():\n+            files.append(pyproject)\n+        for relative in (\".github\", \".codex\"):\n+            surface = root / relative\n+            if surface.is_dir():\n+                files += sorted(\n+                    path\n+                    for path in surface.rglob(\"*\")\n+                    if path.is_file()\n+                    and path.suffix in {\".md\", \".py\", \".sh\", \".toml\", \".yaml\", \".yml\"}\n+                )\n+    return sorted(set(files))\n \n \n def find_adr_reference_violations(\n@@ -94,10 +111,9 @@\n ) -> list[Violation]:\n     \"\"\"Return every ``ADR ####`` citation under ``root`` that fails to resolve.\n \n-    A citation resolves when exactly one ``docs/adr/####-*.md`` matches its\n-    number. Zero matches is a \"missing\" violation; two or more is \"ambiguous\".\n-    Records that are never cited produce nothing -- the converse is not\n-    required.\n+    A citation resolves when exactly one record in the same namespace matches\n+    its number. Zero matches is a \"missing\" violation; two or more is\n+    \"ambiguous\". Records that are never cited produce nothing.\n     \"\"\"\n     root = Path(root)\n     index = _adr_index(root)\n@@ -110,13 +126,15 @@\n         rel = path.relative_to(root).as_posix()\n         for lineno, line in enumerate(text.splitlines(), start=1):\n             for match in _CITATION.finditer(line):\n-                number = match.group(1)\n-                found = index.get(number, [])\n+                namespace, number = match.groups()\n+                found = index.get((namespace, number), [])\n                 if len(found) == 1:\n                     continue\n                 reason = \"missing\" if not found else \"ambiguous\"\n                 rels = [p.relative_to(root).as_posix() for p in found]\n-                violations.append(Violation(rel, number, lineno, reason, rels))\n+                violations.append(\n+                    Violation(rel, namespace, number, lineno, reason, rels)\n+                )\n     return violations\n \n \n@@ -129,8 +147,8 @@\n         self.assertEqual(\n             violations,\n             [],\n-            \"Every 'ADR ####' citation must resolve to exactly one \"\n-            f\"docs/adr/####-*.md:\\n{detail}\",\n+            \"Every ADR/SADR citation must resolve exactly once in its own \"\n+            f\"docs/adr namespace:\\n{detail}\",\n         )\n \n     def test_scan_actually_reads_scripts(self) -> None:\n@@ -146,8 +164,21 @@\n         # The two records this Goal carried onto the default branch, and which\n         # the scripts cite most, must be present.\n         index = _adr_index(REPO_ROOT)\n-        self.assertIn(\"0016\", index)\n-        self.assertIn(\"0017\", index)\n+        self.assertIn((\"SADR\", \"0016\"), index)\n+        self.assertIn((\"SADR\", \"0017\"), index)\n+\n+    def test_platform_tree_has_only_sadr_0001_through_0019(self) -> None:\n+        adr_dir = REPO_ROOT / \"docs\" / \"adr\"\n+        sadrs = sorted(adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\"))\n+        self.assertEqual(\n+            {path.name.split(\"-\", 2)[1] for path in sadrs},\n+            {f\"{number:04d}\" for number in range(1, 20)},\n+        )\n+        self.assertEqual(\n+            list(adr_dir.glob(\"[0-9][0-9][0-9][0-9]-*.md\")),\n+            [],\n+            \"the Shiki platform tree must not retain legacy numeric SADR paths\",\n+        )\n \n \n class FixtureAdrReferences(unittest.TestCase):\n@@ -180,6 +211,26 @@\n         self.assertIn(\"9991\", message)  # cited number\n         self.assertIn(\"docs/adr/9991-*.md\", message)  # missing path\n \n+    def test_target_adr_does_not_satisfy_missing_platform_sadr(self) -> None:\n+        root = self._make_repo(\n+            adr_names=[\"0042-target-decision.md\"],\n+            script_body='\"\"\"Target ADR 0042 exists, but platform SADR-0042 does not.\"\"\"\\n',\n+        )\n+        violations = find_adr_reference_violations(root, include_docs=False)\n+        self.assertEqual(len(violations), 1)\n+        self.assertEqual(violations[0].namespace, \"SADR\")\n+        self.assertEqual(violations[0].number, \"0042\")\n+        self.assertEqual(violations[0].expected_path, \"docs/adr/SADR-0042-*.md\")\n+\n+    def test_target_adr_and_platform_sadr_may_reuse_a_number(self) -> None:\n+        root = self._make_repo(\n+            adr_names=[\"0042-target-decision.md\", \"SADR-0042-platform-decision.md\"],\n+            script_body='\"\"\"Target ADR 0042 and platform SADR-0042 are distinct.\"\"\"\\n',\n+        )\n+        self.assertEqual(\n+            find_adr_reference_violations(root, include_docs=False), []\n+        )\n+\n     def test_uncited_adr_is_allowed(self) -> None:\n         # An ADR exists but nothing cites it -> not a violation. The converse\n         # (every ADR must be cited) is explicitly NOT required.\ndiff --git a/tests/test_policy_job_merged_prs.py b/tests/test_policy_job_merged_prs.py\nindex d45512e74681e5e649279236c7d4e79d5c3da471..b97989f8bd74d091de1a6b5275a20d8dc68c4512\n--- a/tests/test_policy_job_merged_prs.py\n+++ b/tests/test_policy_job_merged_prs.py\n@@ -3,8 +3,8 @@\n \n ``.github/workflows/shiki-cca-completion.yml`` has two consumers of the merged-PR\n proof: the CCA *signal* (``guardian_approval_signal.py`` in \"Prepare CCA context\")\n-and the authoritative gate (``mergegate_check.py`` in \"Enforce MergeGate\"). ADR\n-0017's bookkeeping-closeout exemption (condition 6) needs the merged implementation\n+and the authoritative gate (``mergegate_check.py`` in \"Enforce MergeGate\").\n+SADR-0017's bookkeeping-closeout exemption (condition 6) needs the merged implementation\n PR proven, and a closeout carries NO ``post_merge_reconcile`` marker. The signal\n already resolves ``MERGED_PRS`` unconditionally, but \"Enforce MergeGate\" used to\n resolve it only inside ``grep -q '<!-- shiki:post_merge_reconcile -->'``. So a\ndiff --git a/tests/test_sadr_install_migration.py b/tests/test_sadr_install_migration.py\nnew file mode 100644\nindex 0000000000000000000000000000000000000000..5a442fa5e4968ade178dcdedc3c63b54dba580a2\n--- /dev/null\n+++ b/tests/test_sadr_install_migration.py\n@@ -0,0 +1,224 @@\n+\"\"\"SADR installation and collision-safe legacy upgrade behavior.\"\"\"\n+\n+from __future__ import annotations\n+\n+import contextlib\n+import hashlib\n+import io\n+import json\n+import shutil\n+import tempfile\n+import unittest\n+from pathlib import Path\n+\n+import shiki_test_support  # noqa: F401  (path bootstrap)\n+\n+import shiki_installer\n+from tests.test_adr_references import find_adr_reference_violations\n+\n+\n+PRODUCT_NUMBERS = tuple(range(13, 19))\n+\n+\n+def _quiet_install(target: Path, *, force: bool, validate: bool) -> None:\n+    with contextlib.redirect_stdout(io.StringIO()):\n+        shiki_installer.install_template(target, force=force, validate=validate)\n+\n+\n+def _sha256(path: Path) -> str:\n+    return hashlib.sha256(path.read_bytes()).hexdigest()\n+\n+\n+def _legacy_sadr_paths() -> tuple[str, ...]:\n+    return tuple(\n+        f\"docs/adr/{path.name.removeprefix('SADR-')}\"\n+        for path in sorted((shiki_installer.ROOT / \"docs\" / \"adr\").glob(\"SADR-*.md\"))\n+        if path.name != \"SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\"\n+    )\n+\n+\n+def _create_legacy_upgrade_fixture(\n+    target: Path,\n+) -> tuple[dict[str, bytes], tuple[str, ...]]:\n+    adr_dir = target / \"docs\" / \"adr\"\n+    adr_dir.mkdir(parents=True)\n+    product_bytes = {\n+        f\"{number:04d}-cubeshare-product-decision.md\": (\n+            f\"# CubeShare ADR {number:04d}\\n\\nDo not rewrite {number}.\\n\"\n+        ).encode()\n+        for number in PRODUCT_NUMBERS\n+    }\n+    for name, content in product_bytes.items():\n+        (adr_dir / name).write_bytes(content)\n+\n+    legacy_paths = _legacy_sadr_paths()\n+    for relative in legacy_paths:\n+        (target / relative).write_bytes(f\"legacy Shiki bytes for {relative}\\n\".encode())\n+\n+    stamp_path = target / shiki_installer.INSTALL_STAMP_PATH\n+    stamp_path.parent.mkdir(parents=True, exist_ok=True)\n+    stamp = {\n+        \"version\": shiki_installer.INSTALL_STAMP_VERSION,\n+        \"platform_commit\": None,\n+        \"installed_at\": \"2000-01-01T00:00:00+00:00\",\n+        \"digests\": {\n+            relative: _sha256(target / relative) for relative in legacy_paths\n+        },\n+    }\n+    stamp_path.write_text(\n+        json.dumps(stamp, indent=2, sort_keys=True) + \"\\n\", encoding=\"utf-8\"\n+    )\n+    return product_bytes, legacy_paths\n+\n+\n+def _tree_snapshot(root: Path) -> dict[str, tuple[str, bytes | str]]:\n+    \"\"\"Recursive byte/shape snapshot used to prove preflight is write-free.\"\"\"\n+    snapshot: dict[str, tuple[str, bytes | str]] = {}\n+    for path in sorted(root.rglob(\"*\")):\n+        relative = path.relative_to(root).as_posix()\n+        if path.is_symlink():\n+            snapshot[relative] = (\"symlink\", path.readlink().as_posix())\n+        elif path.is_dir():\n+            snapshot[relative] = (\"directory\", \"\")\n+        else:\n+            snapshot[relative] = (\"file\", path.read_bytes())\n+    return snapshot\n+\n+\n+class FreshSadrInstallTests(unittest.TestCase):\n+    def setUp(self) -> None:\n+        self.tmp = Path(tempfile.mkdtemp(prefix=\"shiki-sadr-fresh-\"))\n+        self.addCleanup(lambda: shutil.rmtree(self.tmp, ignore_errors=True))\n+        self.target = self.tmp / \"target\"\n+        self.adr_dir = self.target / \"docs\" / \"adr\"\n+        self.adr_dir.mkdir(parents=True)\n+        self.product_bytes = {\n+            f\"{number:04d}-product-decision.md\": (\n+                f\"# Product ADR {number:04d}\\n\\nTarget-owned bytes {number}.\\n\"\n+            ).encode()\n+            for number in PRODUCT_NUMBERS\n+        }\n+        for name, content in self.product_bytes.items():\n+            (self.adr_dir / name).write_bytes(content)\n+\n+    def test_fresh_install_preserves_product_adrs_and_installs_all_sadrs(self) -> None:\n+        _quiet_install(self.target, force=False, validate=True)\n+\n+        for name, expected in self.product_bytes.items():\n+            self.assertEqual((self.adr_dir / name).read_bytes(), expected)\n+\n+        sadr_numbers = {\n+            path.name.split(\"-\", 2)[1]\n+            for path in self.adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\")\n+        }\n+        self.assertEqual(sadr_numbers, {f\"{number:04d}\" for number in range(1, 20)})\n+\n+        numeric_records = {\n+            path.name\n+            for path in self.adr_dir.glob(\"[0-9][0-9][0-9][0-9]-*.md\")\n+        }\n+        self.assertEqual(numeric_records, set(self.product_bytes))\n+        self.assertEqual(find_adr_reference_violations(self.target), [])\n+\n+\n+class LegacySadrUpgradeTests(unittest.TestCase):\n+    def setUp(self) -> None:\n+        self.tmp = Path(tempfile.mkdtemp(prefix=\"shiki-sadr-upgrade-\"))\n+        self.addCleanup(lambda: shutil.rmtree(self.tmp, ignore_errors=True))\n+        self.target = self.tmp / \"target\"\n+        self.adr_dir = self.target / \"docs\" / \"adr\"\n+        self.product_bytes, self.legacy_paths = _create_legacy_upgrade_fixture(\n+            self.target\n+        )\n+\n+    def test_force_upgrade_deletes_only_stamped_legacy_sadrs(self) -> None:\n+        _quiet_install(self.target, force=True, validate=True)\n+\n+        for relative in self.legacy_paths:\n+            self.assertFalse((self.target / relative).exists(), relative)\n+        for name, expected in self.product_bytes.items():\n+            self.assertEqual((self.adr_dir / name).read_bytes(), expected)\n+\n+        sadrs = list(self.adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\"))\n+        self.assertEqual(len(sadrs), 19)\n+        refreshed = json.loads(\n+            (self.target / shiki_installer.INSTALL_STAMP_PATH).read_text(encoding=\"utf-8\")\n+        )\n+        for relative in self.legacy_paths:\n+            self.assertNotIn(relative, refreshed[\"digests\"])\n+        for path in sadrs:\n+            relative = path.relative_to(self.target).as_posix()\n+            self.assertEqual(refreshed[\"digests\"][relative], _sha256(path))\n+\n+    def test_non_force_install_never_deletes_legacy_files(self) -> None:\n+        before = {\n+            relative: (self.target / relative).read_bytes()\n+            for relative in self.legacy_paths\n+        }\n+        _quiet_install(self.target, force=False, validate=False)\n+        for relative, expected in before.items():\n+            self.assertEqual((self.target / relative).read_bytes(), expected)\n+\n+\n+class LegacyCleanupPreflightTests(unittest.TestCase):\n+    def setUp(self) -> None:\n+        self.tmp = Path(tempfile.mkdtemp(prefix=\"shiki-sadr-preflight-\"))\n+        self.addCleanup(lambda: shutil.rmtree(self.tmp, ignore_errors=True))\n+\n+    def test_all_ownership_blockers_abort_before_any_write(self) -> None:\n+        cases = (\n+            (\"absent\", \"install stamp is absent\"),\n+            (\"unreadable\", \"install stamp is unreadable\"),\n+            (\"malformed\", \"install stamp is malformed\"),\n+            (\"missing-digest\", \"install stamp has no digest\"),\n+            (\"digest-mismatch\", \"digest mismatch\"),\n+        )\n+        for name, expected_reason in cases:\n+            with self.subTest(name=name):\n+                target = self.tmp / name\n+                _, legacy_paths = _create_legacy_upgrade_fixture(target)\n+                stamp_path = target / shiki_installer.INSTALL_STAMP_PATH\n+\n+                if name == \"absent\":\n+                    stamp_path.unlink()\n+                elif name == \"unreadable\":\n+                    stamp_path.unlink()\n+                    stamp_path.mkdir()\n+                elif name == \"malformed\":\n+                    stamp_path.write_text(\"{not-json\\n\", encoding=\"utf-8\")\n+                else:\n+                    stamp = json.loads(stamp_path.read_text(encoding=\"utf-8\"))\n+                    if name == \"missing-digest\":\n+                        stamp[\"digests\"] = {}\n+                    else:\n+                        stamp[\"digests\"] = {\n+                            relative: \"deadbeef\" for relative in legacy_paths\n+                        }\n+                    stamp_path.write_text(\n+                        json.dumps(stamp, indent=2, sort_keys=True) + \"\\n\",\n+                        encoding=\"utf-8\",\n+                    )\n+\n+                before = _tree_snapshot(target)\n+                with self.assertRaises(shiki_installer.ShikiError) as raised:\n+                    _quiet_install(target, force=True, validate=False)\n+                after = _tree_snapshot(target)\n+\n+                self.assertEqual(before, after, f\"{name} preflight changed the tree\")\n+                message = str(raised.exception)\n+                self.assertIn(expected_reason, message)\n+                self.assertIn(\"No files were written\", message)\n+                for relative in legacy_paths:\n+                    self.assertIn(relative, message)\n+\n+    def test_absent_stamp_is_allowed_when_no_legacy_path_exists(self) -> None:\n+        target = self.tmp / \"no-legacy\"\n+        target.mkdir()\n+        _quiet_install(target, force=True, validate=False)\n+        self.assertTrue(\n+            (target / \"docs\" / \"adr\" / \"SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\").is_file()\n+        )\n+\n+\n+if __name__ == \"__main__\":\n+    unittest.main()\n\n2026-08-07T08:14:03.215601Z ERROR codex_models_manager::manager: failed to renew cache TTL: missing field `base_instructions` at line 94 column 5\napply patch\npatch: completed\n/private/tmp/shiki-adr-collision.pa0KwY/.worktrees/shiki-t-20260807t073307221254z-2d22d654-introduc/.github/prompts/codex-task-handoff.md\n/private/tmp/shiki-adr-collision.pa0KwY/.worktrees/shiki-t-20260807t073307221254z-2d22d654-introduc/docs/agents/checklists.md\n/private/tmp/shiki-adr-collision.pa0KwY/.worktrees/shiki-t-20260807t073307221254z-2d22d654-introduc/docs/agents/codex-handoff.md\n/private/tmp/shiki-adr-collision.pa0KwY/.worktrees/shiki-t-20260807t073307221254z-2d22d654-introduc/docs/agents/completion-check-agent.md\n/private/tmp/shiki-adr-collision.pa0KwY/.worktrees/shiki-t-20260807t073307221254z-2d22d654-introduc/docs/agents/decision-control.md\n/private/tmp/shiki-adr-collision.pa0KwY/.worktrees/shiki-t-20260807t073307221254z-2d22d654-introduc/docs/agents/domain.md\n/private/tmp/shiki-adr-collision.pa0KwY/.worktrees/shiki-t-20260807t073307221254z-2d22d654-introduc/docs/agents/implementation-policy.md\n/private/tmp/shiki-adr-collision.pa0KwY/.worktrees/shiki-t-20260807t073307221254z-2d22d654-introduc/docs/agents/runtime-auth-model.md\n/private/tmp/shiki-adr-collision.pa0KwY/.worktrees/shiki-t-20260807t073307221254z-2d22d654-introduc/docs/agents/skill-gate.md\ndiff --git a/.codex/skills/shiki/SKILL.md b/.codex/skills/shiki/SKILL.md\nindex c8e2e85d9d75d49cc4f2d6286ee22659c4e53e75..93cded623b2c15d9f65c76491a1c2d813ac9e6c0\n--- a/.codex/skills/shiki/SKILL.md\n+++ b/.codex/skills/shiki/SKILL.md\n@@ -18,6 +18,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n Codex CLI does not register this skill as a custom `/shiki` slash command. When\n the user types `/shiki` in Codex CLI and gets \"Unrecognized command\", explain\n that this is expected: invoke Shiki in Codex with natural language such as\n@@ -69,7 +72,7 @@\n \n ## Responsibilities\n \n-- Claude Code implements and repairs by default (`shiki runner claude`, ADR 0008); Codex implements and repairs tasks explicitly assigned to `codex`.\n+- Claude Code implements and repairs by default (`shiki runner claude`, SADR-0008); Codex implements and repairs tasks explicitly assigned to `codex`.\n - Claude Code Action can act as GitHub-side CCA or reviewer.\n - CCA judges completion.\n - MergeGate authorizes state transitions and merge readiness.\ndiff --git a/.github/prompts/cca-completion-check.md b/.github/prompts/cca-completion-check.md\nindex ed203ad5f302ecc81372d8ab27d2b1232ca97497..fd89950528333ef6242e6d5443904f5392ab7e56\n--- a/.github/prompts/cca-completion-check.md\n+++ b/.github/prompts/cca-completion-check.md\n@@ -19,6 +19,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n ## Required Reading\n \n Read only the files needed to judge the PR contract. Prefer this order:\ndiff --git a/.github/prompts/codex-task-handoff.md b/.github/prompts/codex-task-handoff.md\nindex 695e211d76dea1c60e8d5bb360e78487808286a4..59441d576b01a39569722f226c854e5a740e9eb5\n--- a/.github/prompts/codex-task-handoff.md\n+++ b/.github/prompts/codex-task-handoff.md\n@@ -2,7 +2,7 @@\n \n You are Codex Front acting as the Implementer for a Shiki task.\n \n-Follow `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant ADRs, and `docs/agents/codex-handoff.md`.\n+Follow `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant Shiki SADRs and target ADRs, and `docs/agents/codex-handoff.md`.\n \n Rules:\n \ndiff --git a/AGENTS.md b/AGENTS.md\nindex 7f041100d49c2a7525f51cb0f5d696a4a6d1c78c..ea303398018461999e03b6f27be3f4d65833fa32\n--- a/AGENTS.md\n+++ b/AGENTS.md\n@@ -44,10 +44,14 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use\n+`SADR-NNNN-*.md`; target product decisions retain `NNNN-*.md` and the `ADR`\n+namespace.\n+\n Use the canonical source-of-truth order above. Runtime-specific reading orders\n are operational guidance only and must not override or reorder it.\n \n-Conversation state is not durable truth. If a decision matters, put it in GitHub, `.shiki/`, `CONTEXT.md`, or an ADR.\n+Conversation state is not durable truth. If a decision matters, put it in GitHub, `.shiki/`, `CONTEXT.md`, a Shiki SADR, or a target ADR.\n \n If GitHub and `.shiki/` disagree, surface the conflict and prefer GitHub operational state until the mirror is repaired.\n \n@@ -75,8 +79,8 @@\n Every non-trivial change follows this loop:\n \n 1. **Goal**: clarify outcome, completion conditions, non-goals, risk level, and success signals.\n-2. **grill-with-docs**: challenge the plan against domain docs, ADRs, code reality, terminology, and edge scenarios.\n-3. **Context & Impact**: find relevant docs, ADRs, code, dependencies, owners, locks, and verification surfaces.\n+2. **grill-with-docs**: challenge the plan against domain docs, Shiki SADRs, target ADRs, code reality, terminology, and edge scenarios.\n+3. **Context & Impact**: find relevant docs, SADRs, target ADRs, code, dependencies, owners, locks, and verification surfaces.\n 4. **PRD and Spec Freeze**: use `to-prd` when context is settled enough to become durable product/engineering intent. The operator's explicit approval of the PRD is Spec Freeze: the plan records a `spec_freeze` block, and no plan runs without it. Steps 1-4 form Requirements Definition \u2014 one continuous operator dialogue. After Spec Freeze, scope changes require an operator-approved Spec Amendment; non-scope-moving interpretations go to the Assumption Log.\n 5. **Issues**: use `to-issues` to create independently grabbable vertical-slice issues. Prefer AFK slices over HITL slices where possible.\n 6. **Triage**: label issues for readiness, risk, runtime, skills, and MergeGate state.\n@@ -167,7 +171,7 @@\n - Testing decisions.\n - Out-of-scope items.\n - Further notes.\n-- Links to relevant domain terms and ADRs.\n+- Links to relevant domain terms, SADRs, and target ADRs.\n \n Every executable task must include:\n \n@@ -193,7 +197,7 @@\n \n Context & Impact must identify:\n \n-- Relevant documents, domain terms, ADRs, and past decisions.\n+- Relevant documents, domain terms, SADRs, target ADRs, and past decisions.\n - Relevant modules, interfaces, seams, callers, owners, and tests.\n - Dependency relationships between tasks.\n - Candidate locks and lock conflicts.\n@@ -210,7 +214,7 @@\n | Skill | Required when |\n | --- | --- |\n | `setup-matt-pocock-skills` | First configuring the repo for skills, or when issue tracker, triage labels, or domain docs are missing. |\n-| `grill-with-docs` | Default for non-trivial Goals before PRD/issues; always required when plans, terminology, boundaries, tradeoffs, or ADR-worthy decisions are ambiguous. |\n+| `grill-with-docs` | Default for non-trivial Goals before PRD/issues; always required when plans, terminology, boundaries, tradeoffs, or decision-record-worthy choices are ambiguous. |\n | `zoom-out` | The agent lacks an architectural map of the relevant code area. |\n | `to-prd` | Settled Goal context must become durable product/engineering intent. |\n | `to-issues` | A Goal, PRD, or plan must become independently grabbable vertical-slice issues. |\n@@ -348,7 +352,7 @@\n - Three or more new files.\n - Any high-risk or critical label.\n \n-When the architecture gate triggers, run `grill-with-docs` or `improve-codebase-architecture` as appropriate and record durable decisions in an ADR when needed.\n+When the architecture gate triggers, run `grill-with-docs` or `improve-codebase-architecture` as appropriate and record durable decisions in a Shiki SADR or target ADR as applicable.\n \n ## Engineering Discipline\n \ndiff --git a/CLAUDE.md b/CLAUDE.md\nindex 98a932e7841a632866fcd53b0bad0e3af3711d3d..98a31399404d6fb044b4fd493f4459ac87f439de\n--- a/CLAUDE.md\n+++ b/CLAUDE.md\n@@ -14,6 +14,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n ## Claude Role\n \n Default to planner, default implementer, reviewer, coordinator, documentation editor, CCA-style completion judge, and final-judgment assistant.\n@@ -57,7 +60,7 @@\n At the start of a Shiki session:\n \n 1. Read `AGENTS.md`.\n-2. Read `CONTEXT.md` and relevant ADRs.\n+2. Read `CONTEXT.md` and relevant Shiki SADRs and target ADRs.\n 3. Read active `.shiki/` Goal/task/ledger state when present.\n 4. Read active distilled rules from `.shiki/memories/` (status `distilled`, `active: true`, not revoked or superseded) for applicable guidance; the same rules are injected deterministically into task handoffs as the `## Distilled Rules` section (\u00a73.5).\n 5. Read the GitHub Issue or PR when the task is GitHub-backed.\n@@ -74,7 +77,7 @@\n - Define the Goal, completion conditions, non-goals, and risk level.\n - Treat `grill-with-docs` as the default planning entry point for non-trivial Goals.\n - Challenge domain terms against `CONTEXT.md`.\n-- Challenge hard-to-reverse tradeoffs against ADRs.\n+- Challenge hard-to-reverse tradeoffs against SADRs and target ADRs.\n - Explore code instead of asking when code can answer the question.\n - Run Context & Impact before implementation planning.\n - Produce a Task DAG, not an unordered checklist.\n@@ -94,7 +97,7 @@\n - For each question, provide your recommended answer.\n - Resolve dependency decisions before downstream design questions.\n - Update `CONTEXT.md` inline when terminology crystallizes.\n-- Offer ADRs only when the decision is hard to reverse, surprising without context, and a real tradeoff.\n+- Offer a SADR or target ADR only when the decision is hard to reverse, surprising without context, and a real tradeoff.\n - Convert unresolved questions into HITL blockers rather than allowing Codex to guess.\n \n ## PRD and Issues Standard\n@@ -103,7 +106,7 @@\n \n - Use the project's domain glossary vocabulary.\n - Avoid volatile file paths unless they are essential.\n-- Include implementation decisions, testing decisions, out-of-scope items, and relevant ADR/domain links.\n+- Include implementation decisions, testing decisions, out-of-scope items, and relevant SADR/target-ADR/domain links.\n - Identify deep-module opportunities and testable interfaces.\n \n When creating issues:\n@@ -121,7 +124,7 @@\n - Branch/worktree target.\n - Scope and non-goals.\n - Dependencies and locks.\n-- Relevant docs, ADRs, modules, and tests.\n+- Relevant docs, SADRs, target ADRs, modules, and tests.\n - Required skill invocations.\n - TDD expectations.\n - Pre-PR `code-review` gate expectations.\n@@ -152,7 +155,7 @@\n \n - Treat yourself as a judge, not implementer.\n - Do not write production code.\n-- Read `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant ADRs, PRD, task issue, PR body, diff, checks, reviews, labels, and `.shiki/` state.\n+- Read `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant SADRs and target ADRs, PRD, task issue, PR body, diff, checks, reviews, labels, and `.shiki/` state.\n - Evaluate every applicable checklist in `docs/agents/checklists.md`.\n - Map each acceptance criterion to durable evidence.\n - Distinguish implementation failure from missing evidence.\ndiff --git a/CONTEXT.md b/CONTEXT.md\nindex e61a9be06a0cce0d026484083a657bde2067e234..237ee8aa3ea8fab334ece49768bc5ed3b5af0974\n--- a/CONTEXT.md\n+++ b/CONTEXT.md\n@@ -25,7 +25,7 @@\n _Avoid_: jumping directly into code\n \n **grill-with-docs**:\n-A planning skill that challenges a plan against domain language, ADRs, code reality, and concrete edge scenarios. It resolves design-tree questions before PRD/issues. It runs inside Requirements Definition.\n+A planning skill that challenges a plan against domain language, Shiki SADRs, target ADRs, code reality, and concrete edge scenarios. It resolves design-tree questions before PRD/issues. It runs inside Requirements Definition.\n _Avoid_: generic brainstorming, silent assumptions\n \n **Requirements Definition**:\ndiff --git a/CONTRIBUTING.md b/CONTRIBUTING.md\nindex c6d9b656e73abb8a55e5f660123e03153dcb3e6d..9e37a8fbf668db5b22461aaae5548b8f1354394c\n--- a/CONTRIBUTING.md\n+++ b/CONTRIBUTING.md\n@@ -17,7 +17,7 @@\n 1. **Goal** \u2014 start from a user-approved Goal with an outcome, completion\n    conditions, non-goals, risk level, and success signals.\n 2. **grill-with-docs** \u2014 for any non-trivial change, challenge the plan against\n-   domain language, ADRs, and code reality before writing a PRD.\n+   domain language, Shiki SADRs, target ADRs, and code reality before writing a PRD.\n 3. **PRD** \u2014 capture settled product/engineering intent (`to-prd`).\n 4. **Spec Freeze** \u2014 the operator explicitly approves the PRD/requirements;\n    the plan records a `spec_freeze` block and no plan runs without it\n@@ -94,7 +94,7 @@\n   entries.\n - Do not hand-edit `.shiki/goals/*.json` or `.shiki/manifest.json` in a task PR.\n - Conversation state is not durable truth. If a decision matters, record it in\n-  GitHub, `.shiki/`, `CONTEXT.md`, or an ADR.\n+  GitHub, `.shiki/`, `CONTEXT.md`, a Shiki SADR, or a target ADR.\n \n ## Architecture Gate\n \n@@ -103,7 +103,7 @@\n or CI policy, package structure, three or more structural files, more than 100\n net new lines in a core module, three or more new files, or any high-risk label.\n When triggered, run `grill-with-docs` or `improve-codebase-architecture` and\n-record durable decisions in an ADR when needed (see `docs/adr/`).\n+record durable decisions in a Shiki SADR or target ADR as applicable (see `docs/adr/`).\n \n ## Safety\n \ndiff --git a/SYSTEM_PROMPT.md b/SYSTEM_PROMPT.md\nindex aaaea45e3a896c3a44ba89eec8bb84325ecf6c62..61b3c55afe7a5fc5f1a3d475378175db58553c6d\n--- a/SYSTEM_PROMPT.md\n+++ b/SYSTEM_PROMPT.md\n@@ -30,6 +30,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n Use the canonical source-of-truth order above. Runtime-specific reading orders\n are operational guidance only and must not override or reorder it. The current\n conversation is non-durable operator input.\n@@ -55,7 +58,7 @@\n For any non-trivial Goal, use this flow:\n \n 1. **Goal Seek**: define outcome, non-goals, risk level, completion criteria, and evidence requirements.\n-2. **grill-with-docs**: challenge the plan against `CONTEXT.md`, ADRs, code reality, terminology, boundaries, and edge scenarios. Ask one question at a time when the operator is available. If code can answer a question, inspect code instead of asking.\n+2. **grill-with-docs**: challenge the plan against `CONTEXT.md`, Shiki SADRs, target ADRs, code reality, terminology, boundaries, and edge scenarios. Ask one question at a time when the operator is available. If code can answer a question, inspect code instead of asking.\n 3. **Context & Impact**: identify modules, interfaces, seams, callers, dependencies, locks, risk, and verification surfaces.\n 4. **to-prd + Spec Freeze**: turn settled context into a PRD using domain vocabulary and testing decisions. The operator's explicit approval of the PRD is Spec Freeze, recorded as the plan's `spec_freeze` block; plans without it do not run. Steps 1-4 form Requirements Definition.\n 5. **to-issues**: decompose the PRD into independently grabbable vertical-slice issues. Prefer AFK slices; mark HITL slices when judgment is still required.\n@@ -75,7 +78,7 @@\n Before making any material change:\n \n 1. Read `AGENTS.md` and any runtime-specific wrapper such as `CLAUDE.md`.\n-2. Read `CONTEXT.md` and relevant ADRs.\n+2. Read `CONTEXT.md` and relevant SADRs and target ADRs.\n 3. Read the GitHub Issue / PR and `.shiki/` state for the active Goal or task.\n 4. Identify your role: Planner, Implementer, Reviewer, Completion Check Agent, Repairer, or Guardian-assist.\n 5. Identify Goal id, task id, branch, PR, dependencies, locks, risk level, required skills, and required evidence.\n@@ -103,7 +106,7 @@\n \n At minimum, identify:\n \n-- Relevant documents, ADRs, domain terms, and prior decisions.\n+- Relevant documents, SADRs, target ADRs, domain terms, and prior decisions.\n - Relevant modules, interfaces, seams, callers, owners, and tests.\n - Dependency relationships between tasks.\n - Candidate file locks and lock conflicts.\n@@ -152,7 +155,7 @@\n \n CCA must read:\n \n-- `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant ADRs.\n+- `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant SADRs and target ADRs.\n - Parent Goal issue, PRD issue, task issue, comments, and labels.\n - PR body, diff, commits, changed files, reviews, CI checks, and artifacts available to the workflow.\n - `.shiki/` task contract, locks, ledger entries, CCA checklist profile, and prior repair packets.\n@@ -214,7 +217,7 @@\n - Three or more new files.\n - Any high-risk or critical label.\n \n-When the architecture gate triggers, run `grill-with-docs` or `improve-codebase-architecture` as appropriate and record the decision in an ADR if it is hard to reverse or likely to be re-litigated.\n+When the architecture gate triggers, run `grill-with-docs` or `improve-codebase-architecture` as appropriate and record the decision in a Shiki SADR or target ADR as applicable if it is hard to reverse or likely to be re-litigated.\n \n ## 14. Skill Gate\n \n@@ -223,7 +226,7 @@\n Use these skills:\n \n - `setup-matt-pocock-skills`: before first use of the engineering skill set, or when issue tracker, triage labels, or domain docs are missing.\n-- `grill-with-docs`: default for non-trivial Goals before PRD/issues; required for ambiguous plans, terminology, boundaries, ADR-worthy tradeoffs, or design-tree decisions.\n+- `grill-with-docs`: default for non-trivial Goals before PRD/issues; required for ambiguous plans, terminology, boundaries, decision-record-worthy tradeoffs, or design-tree decisions.\n - `zoom-out`: unfamiliar code area or missing architectural map.\n - `to-prd`: convert settled context into a PRD.\n - `to-issues`: break a PRD, plan, or Goal into independently grabbable vertical-slice issues.\ndiff --git a/docs/adr/0001-keep-shiki-as-platform-name.md b/docs/adr/SADR-0001-keep-shiki-as-platform-name.md\nindex 20a9637c3b9d8c97de7ba8db7cfae4d82d9a7dba..8b685439959aada1b221c112f0ac3c51ca8a1695\n--- a/docs/adr/0001-keep-shiki-as-platform-name.md\n+++ b/docs/adr/SADR-0001-keep-shiki-as-platform-name.md\n@@ -1,3 +1,3 @@\n-# Keep Shiki As The Platform Name\n+# SADR-0001: Keep Shiki As The Platform Name\n \n Shiki remains the product name for the GitHub-native agentic engineering control plane. We considered more descriptive names such as MergePilot, MergeGate, and Kiroku, but kept Shiki because it carries the existing goal-seeking framework identity while remaining independent of Hermes, Codex, Claude Code, or any single runtime.\ndiff --git a/docs/adr/0002-use-agents-as-shared-constitution.md b/docs/adr/SADR-0002-use-agents-as-shared-constitution.md\nindex 73de30ecb078120eac540d9bc51855e3cd48d0e3..c5486182bf06fca36ea508496b013c67302fd989\n--- a/docs/adr/0002-use-agents-as-shared-constitution.md\n+++ b/docs/adr/SADR-0002-use-agents-as-shared-constitution.md\n@@ -1,3 +1,3 @@\n-# Use AGENTS As The Shared Agent Constitution\n+# SADR-0002: Use AGENTS As The Shared Agent Constitution\n \n Shiki uses `AGENTS.md` as the runtime-neutral constitution and `CLAUDE.md` as a thin Claude Code wrapper. This keeps Codex, Claude Code, Hermes Runner, and future Agent Runtimes aligned on the same GitHub-first MergeGate rules while still allowing runtime-specific instructions where needed.\ndiff --git a/docs/adr/0003-require-skill-gate-for-mergegate-readiness.md b/docs/adr/SADR-0003-require-skill-gate-for-mergegate-readiness.md\nindex 424be0c9af4c8f0136f1fde68a468c47ff84e764..7fc337987c7df9792f75f181dfcbed75fe5ffff2\n--- a/docs/adr/0003-require-skill-gate-for-mergegate-readiness.md\n+++ b/docs/adr/SADR-0003-require-skill-gate-for-mergegate-readiness.md\n@@ -1,3 +1,3 @@\n-# Require Skill Gate For MergeGate Readiness\n+# SADR-0003: Require Skill Gate For MergeGate Readiness\n \n Shiki requires relevant engineering skills to be invoked before execution when their triggers apply. This makes skills part of MergeGate readiness rather than optional prompt style, because Shiki depends on repeatable planning, issue decomposition, TDD, diagnosis, triage, and architecture review before autonomous or parallel agent work can be trusted.\ndiff --git a/docs/adr/0004-use-github-first-with-shiki-mirror.md b/docs/adr/SADR-0004-use-github-first-with-shiki-mirror.md\nindex d0a3a9174dc646a945a9d325954ffaf00c352040..a79c54ae9ee8eed2a0524192b864da4e22a92d3f\n--- a/docs/adr/0004-use-github-first-with-shiki-mirror.md\n+++ b/docs/adr/SADR-0004-use-github-first-with-shiki-mirror.md\n@@ -1,4 +1,4 @@\n-# ADR 0004: Use GitHub First With A Repository-Local Shiki Mirror\n+# SADR-0004: Use GitHub First With A Repository-Local Shiki Mirror\n \n ## Status\n \ndiff --git a/docs/adr/0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md b/docs/adr/SADR-0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\nindex b339baf49b89d9290e79c3d51b36e7b2bde3bd95..c30a0d8b1fde0224d0ac7820e7870dccf721580d\n--- a/docs/adr/0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\n+++ b/docs/adr/SADR-0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\n@@ -1,4 +1,4 @@\n-# ADR 0005: Use Codex As Front And Claude Code Action As The GitHub Runtime\n+# SADR-0005: Use Codex As Front And Claude Code Action As The GitHub Runtime\n \n ## Status\n \ndiff --git a/docs/adr/0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md b/docs/adr/SADR-0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\nindex 4bcae9d1e52f0a37b8e9fbafa5cd14b6ee1993bd..7a38538a6ae9929567f0ade67e7ea6a60d0b3f7e\n--- a/docs/adr/0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\n+++ b/docs/adr/SADR-0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\n@@ -1,4 +1,4 @@\n-# ADR 0006: Separate Codex Implementation, CCA Completion, And MergeGate Authorization\n+# SADR-0006: Separate Codex Implementation, CCA Completion, And MergeGate Authorization\n \n ## Status\n \ndiff --git a/docs/adr/0007-packaging-and-release.md b/docs/adr/SADR-0007-packaging-and-release.md\nindex 79b84ddb97167372b44641e5b7860e400ce80822..bb445a2e2e856d9d1f5fdbe35de8d4e8cec0fb32\n--- a/docs/adr/0007-packaging-and-release.md\n+++ b/docs/adr/SADR-0007-packaging-and-release.md\n@@ -1,4 +1,4 @@\n-# ADR 0007: Packaging And Release For Shiki\n+# SADR-0007: Packaging And Release For Shiki\n \n ## Status\n \n@@ -25,7 +25,7 @@\n   not introduce third-party runtime dependencies or break the shim.\n - Releases must run inside the operator's subscription-authenticated toolchain\n   and must not require extra secrets beyond the built-in `GITHUB_TOKEN`\n-  (consistent with ADR 0005's no-API-key-by-default stance).\n+  (consistent with SADR-0005's no-API-key-by-default stance).\n - `.shiki/manifest.json` already encodes the install surface (`install.include`,\n   `install.create_directories`, `install.exclude_from_commit`), and it must not\n   be mutated as part of this change.\n@@ -34,7 +34,7 @@\n \n This is a hard-to-reverse platform decision (it sets the version source of\n truth, the release trigger, and the public install contract), so it is recorded\n-as an ADR.\n+as a SADR.\n \n ## Decision\n \n@@ -104,7 +104,7 @@\n 3. **Publishing to PyPI.** Rejected for now: Shiki is distributed as a\n    repository template + git-installable CLI, not a PyPI library; PyPI would add\n    credentials/secrets and a different versioning contract. Can be added later as\n-   its own ADR if a packaged distribution is needed.\n+   its own SADR if a packaged distribution is needed.\n 4. **Dynamic version (read `VERSION` via a build backend `dynamic` field).**\n    Rejected to keep `pyproject.toml` backend-agnostic and dependency-free; the\n    small duplication between `VERSION` and `pyproject.toml` is guarded by the\n@@ -123,4 +123,4 @@\n   releasing versions the whole platform but ships only `install.include` to\n   targets.\n - Future PyPI distribution or a console entry point are deferred and would each\n-  warrant their own ADR.\n+  warrant their own SADR.\ndiff --git a/docs/adr/0008-use-claude-code-as-default-implementer.md b/docs/adr/SADR-0008-use-claude-code-as-default-implementer.md\nindex dedf4510b7c7799521422f622029563d373db031..35c2625d908167fbc58855b5b63e283805f167e8\n--- a/docs/adr/0008-use-claude-code-as-default-implementer.md\n+++ b/docs/adr/SADR-0008-use-claude-code-as-default-implementer.md\n@@ -1,12 +1,12 @@\n-# ADR 0008: Use Claude Code As The Default Implementer Runtime\n+# SADR-0008: Use Claude Code As The Default Implementer Runtime\n \n ## Status\n \n-Proposed (supersedes ADR 0005 for the implementer default; keeps its auth model)\n+Proposed (supersedes SADR-0005 for the implementer default; keeps its auth model)\n \n ## Context\n \n-ADR 0005 made Codex Front the default implementation runtime, with Claude Code\n+SADR-0005 made Codex Front the default implementation runtime, with Claude Code\n restricted to planner and reviewer roles by the runtime registry, and the only\n autonomous implementation adapter was `shiki runner codex`.\n \n@@ -18,13 +18,13 @@\n - The platform's planning, review, and CCA layers already run on Claude Code,\n   and Claude Code's native capabilities (the built-in `/code-review` skill and\n   the Workflow multi-agent orchestration tool) have no Codex equivalent.\n-- The Goal lifecycle redesign (ADR 0009) requires an autonomous post-freeze\n+- The Goal lifecycle redesign (SADR-0009) requires an autonomous post-freeze\n   loop driven from the operator's Claude Code session, including bounded\n   repair dispatch, which must not depend on a runtime that cannot be invoked\n   unattended.\n \n The constitution claims Shiki is runtime-agnostic (\"not a Claude-only\n-workflow\", ADR 0002). Any change to the implementer default must preserve the\n+workflow\", SADR-0002). Any change to the implementer default must preserve the\n registry-based runtime model rather than hardcoding one vendor.\n \n ## Decision\n@@ -39,7 +39,7 @@\n   the task to review or repair-needed.\n - Keep Codex registered as an optional implementer runtime. `shiki runner\n   codex`, the Codex handoff templates, and the ChatGPT OAuth auth model from\n-  ADR 0005 remain supported for tasks explicitly assigned to `codex`.\n+  SADR-0005 remain supported for tasks explicitly assigned to `codex`.\n - Commit to mandating two Claude-native capabilities as loop contract in\n   follow-up Goals (status: the code-review gate was delivered by Goal G-B \u2014\n   skill registry, checklists item PR-12, default required_skills \u2014 while the\n@@ -60,7 +60,7 @@\n \n Out of scope: removing Codex support, changing the CCA runtime (Claude Code\n Action with `CLAUDE_CODE_OAUTH_TOKEN` stays), and API-key based automation\n-(still requires its own ADR per ADR 0005).\n+(still requires its own target ADR per SADR-0005).\n \n ## Consequences\n \n@@ -100,7 +100,7 @@\n   harness rejection of direct `codex exec` is outside Shiki's control, and the\n   plan's mandatory native capabilities cannot run on Codex at all.\n - **Remove Codex entirely**: rejected; it abandons the runtime-agnostic\n-  constitution (ADR 0002) for no operational gain and forecloses\n+  constitution (SADR-0002) for no operational gain and forecloses\n   cross-runtime adversarial review.\n - **GitHub Actions cloud implementation via claude-code-action**: rejected as\n   default; durable evidence is automatic but token cost, Node 24 workflow\ndiff --git a/docs/adr/0009-requirements-definition-and-spec-freeze.md b/docs/adr/SADR-0009-requirements-definition-and-spec-freeze.md\nindex 29b3f7871bef9203a8b4a831a65c39eb2f506e42..52e7dd69ad0aa4efe0b68c62678abbf7df02aaec\n--- a/docs/adr/0009-requirements-definition-and-spec-freeze.md\n+++ b/docs/adr/SADR-0009-requirements-definition-and-spec-freeze.md\n@@ -1,4 +1,4 @@\n-# ADR 0009: Merge Grill And Planning Into Requirements Definition With Spec Freeze\n+# SADR-0009: Merge Grill And Planning Into Requirements Definition With Spec Freeze\n \n ## Status\n \ndiff --git a/docs/adr/0010-external-ai-guardian-review.md b/docs/adr/SADR-0010-external-ai-guardian-review.md\nindex e6108bbe8bb4e73e0ff4684c8f78001a80438c4e..f3510b145664298adafb151b6f0db51b972f01ec\n--- a/docs/adr/0010-external-ai-guardian-review.md\n+++ b/docs/adr/SADR-0010-external-ai-guardian-review.md\n@@ -1,4 +1,4 @@\n-# ADR 0010: External AI Guardian Review As A First-Class Approval Authority\n+# SADR-0010: External AI Guardian Review As A First-Class Approval Authority\n \n ## Status\n \n@@ -8,7 +8,7 @@\n \n Shiki is intended to be an autonomous, recursive, self-improving agent\n framework: authority-in-the-loop, not human-in-the-loop. Until now the Guardian\n-approval required for high/critical-risk merges (guardian-policy.json, ADR\n+approval required for high/critical-risk merges (guardian-policy.json, SADR\n governance) recognized only human authority sources \u2014 a GitHub review approval,\n the `guardian:approved` label applied by a configured Guardian, or a comment\n carrying `Guardian approval granted` plus the current head SHA.\n@@ -89,7 +89,7 @@\n   autonomous design the operator intends and produced the impersonation\n   incident.\n - **Record AI approval as an operator comment**: rejected as forgery of human\n-  authority \u2014 the exact failure this ADR corrects.\n+  authority \u2014 the exact failure this SADR corrects.\n - **Deliver the artifact as a committed `.shiki/` file**: rejected; a committed\n   approval inside the PR being approved is forgeable by anyone who can push.\n - **A dedicated reviewer bot identity instead of a relayed comment**: deferred;\ndiff --git a/docs/adr/0011-autonomous-quality-gate-evidence.md b/docs/adr/SADR-0011-autonomous-quality-gate-evidence.md\nindex bca0246c5d46e44afbde15486a75eec425fc02f5..9c93eac16d8c3a6d4b11bd7224e3955102cd7db4\n--- a/docs/adr/0011-autonomous-quality-gate-evidence.md\n+++ b/docs/adr/SADR-0011-autonomous-quality-gate-evidence.md\n@@ -1,4 +1,4 @@\n-# ADR 0011: Autonomous Quality-Gate Evidence \u2014 Deterministic Facts And An Independent Verifier\n+# SADR-0011: Autonomous Quality-Gate Evidence \u2014 Deterministic Facts And An Independent Verifier\n \n ## Status\n \ndiff --git a/docs/adr/0012-autonomous-loop-closeout-to-main.md b/docs/adr/SADR-0012-autonomous-loop-closeout-to-main.md\nindex 637fad6dde4989c9c2bf0f81f676d8d29991123c..ec7d85f81030a676bcbba28b43eb365422a36eac\n--- a/docs/adr/0012-autonomous-loop-closeout-to-main.md\n+++ b/docs/adr/SADR-0012-autonomous-loop-closeout-to-main.md\n@@ -1,4 +1,4 @@\n-# 0012 \u2014 Autonomous loop pushes goal completion to main via a closeout PR\n+# SADR-0012 \u2014 Autonomous loop pushes goal completion to main via a closeout PR\n \n - Status: accepted\n - Date: 2026-06-18\ndiff --git a/docs/adr/0013-bootstrap-grants-actions-approve-pull-requests.md b/docs/adr/SADR-0013-bootstrap-grants-actions-approve-pull-requests.md\nindex b6c69d64f1c5ced346b88d6859586715b3b5bd0e..cf48ff4a6716267f539ca58edd1a852e7c7a2d64\n--- a/docs/adr/0013-bootstrap-grants-actions-approve-pull-requests.md\n+++ b/docs/adr/SADR-0013-bootstrap-grants-actions-approve-pull-requests.md\n@@ -1,4 +1,4 @@\n-# ADR 0013: Bootstrap Grants GitHub Actions Permission To Create And Approve Pull Requests\n+# SADR-0013: Bootstrap Grants GitHub Actions Permission To Create And Approve Pull Requests\n \n ## Status\n \n@@ -31,7 +31,7 @@\n security tradeoff: it lets repository automation satisfy a human-review gate. In\n a multi-maintainer repo that could let a compromised or buggy workflow\n self-approve changes. So whether bootstrap turns it on by default \u2014 and with\n-what default token scope \u2014 is an ADR-worthy decision, not an implementation\n+what default token scope \u2014 is a SADR-worthy decision, not an implementation\n detail.\n \n ## Decision\n@@ -48,7 +48,7 @@\n Specifically:\n \n - **`can_approve_pull_request_reviews` defaults to `true`.** Shiki's default\n-  posture is solo/AFK autonomous operation (ADR 0008/0009/0012), where the CCA\n+  posture is solo/AFK autonomous operation (SADR-0008/0009/0012), where the CCA\n   Review Bridge is the only path that can satisfy `required_review: true` after\n   the CCA and Guardian gates have already passed. The default makes a\n   freshly-bootstrapped repo able to run the autonomous loop end to end without a\ndiff --git a/docs/adr/0014-codex-app-external-ai-guardian-ui-adapter.md b/docs/adr/SADR-0014-codex-app-external-ai-guardian-ui-adapter.md\nindex 238db06279334caa341c117362df9271382a7fcd..65f12e9939321e1b3e389a34c822fc1549e5155d\n--- a/docs/adr/0014-codex-app-external-ai-guardian-ui-adapter.md\n+++ b/docs/adr/SADR-0014-codex-app-external-ai-guardian-ui-adapter.md\n@@ -1,4 +1,4 @@\n-# ADR 0014: Use Codex App As The External AI Guardian UI Adapter\n+# SADR-0014: Use Codex App As The External AI Guardian UI Adapter\n \n ## Status\n \ndiff --git a/docs/adr/0015-contract-approval-before-dispatch.md b/docs/adr/SADR-0015-contract-approval-before-dispatch.md\nindex fad6c656741a20a7684ba16080a7db159f63a52f..6061271a46d3f1d6ae7174477de3fb75673123a6\n--- a/docs/adr/0015-contract-approval-before-dispatch.md\n+++ b/docs/adr/SADR-0015-contract-approval-before-dispatch.md\n@@ -1,4 +1,4 @@\n-# ADR 0015: Approve The Task Contract Before Dispatch, Not The Diff After It\n+# SADR-0015: Approve The Task Contract Before Dispatch, Not The Diff After It\n \n ## Status\n \n@@ -9,7 +9,7 @@\n Guardian approval for high/critical risk is expressible only on a pull request.\n Every approval source in `guardian-policy.json` \u2014 GitHub review, the\n `guardian:approved` label, the `Guardian approval granted` comment, and the\n-`external_ai_guardian_review` artifact (ADR 0010/0014) \u2014 is bound to a PR, and\n+`external_ai_guardian_review` artifact (SADR-0010/0014) \u2014 is bound to a PR, and\n three of the four require the current head SHA. Approval therefore cannot exist\n before an implementation PR exists.\n \n@@ -81,7 +81,7 @@\n An envelope breach must surface only on the non-repairable policy gate, never on\n the repairable `MergeGate metadata check`. Otherwise the Goal Loop routes it to\n `dispatch_repair` \u2014 instructing a runner to make the breach go away \u2014 which is\n-exactly what this ADR forbids.\n+exactly what this SADR forbids.\n \n **Contract immutability.** After registration, an implementation PR must not\n change the governance fields of its own task contract (`scope`, `non_goals`,\n@@ -116,7 +116,7 @@\n fails any verdict that is not `complete`, and the signal the CCA prompt reads is\n computed from `evaluate_guardian_approval` alone. Without the OR-merge at the\n signal, a contract-approved PR still returns `needs_guardian`, the CCA job fails,\n-and the MergeGate policy check never runs \u2014 the gate this ADR changes would never\n+and the MergeGate policy check never runs \u2014 the gate this SADR changes would never\n be reached. The CCA *prompt* needs no change: a `contract_approval` entry in the\n signal's existing `sources` array satisfies its current wording.\n \n@@ -130,7 +130,7 @@\n written, and an approved contract lets its implementation PRs merge through the\n same automatic path as low/medium risk. The wasted implement-then-block cycle\n disappears. Guardian review shifts from reading diffs to reading contracts, which\n-is both earlier and smaller; the external AI Guardian path (ADR 0010/0014) works\n+is both earlier and smaller; the external AI Guardian path (SADR-0010/0014) works\n on contracts with no change.\n \n We accept that the Guardian authorizes a class of change without seeing its\n@@ -146,7 +146,7 @@\n adversarial tests for the new approval path equivalent in rigor to\n `test_shiki_governance_evidence.sh`.\n \n-Bootstrap costs, which apply only while this ADR is being implemented. The tasks\n+Bootstrap costs, which apply only while this SADR is being implemented. The tasks\n that build Contract Approval merge under the existing rules, so the ones\n dispatched after the pre-dispatch gate lands but before the approval path lands\n need a Guardian-approved Contract PR *and* a live Guardian approval on their\n@@ -210,7 +210,7 @@\n risk; they do not bound authorized work.\n \n **Auto-escalate risk on envelope breach instead of stopping.** Returns the\n-workflow to a diff-time Guardian decision \u2014 the exact behavior this ADR removes.\n+workflow to a diff-time Guardian decision \u2014 the exact behavior this SADR removes.\n \n **Resolve governance fields from the base snapshot only.** Rejected against the\n registration flow: implementation PRs add their own contract, so the base has no\ndiff --git a/docs/adr/0016-locks-bound-product-paths.md b/docs/adr/SADR-0016-locks-bound-product-paths.md\nindex b5e850f3f1f7f4ef6e7f9d1763a29a51bd300c45..46b40a9b99a7aae644c02bb458f5473ff0a90ce2\n--- a/docs/adr/0016-locks-bound-product-paths.md\n+++ b/docs/adr/SADR-0016-locks-bound-product-paths.md\n@@ -1,4 +1,4 @@\n-# ADR 0016: Locks bound product paths; the Shiki mirror is bound by record identity\n+# SADR-0016: Locks bound product paths; the Shiki mirror is bound by record identity\n \n ## Status\n \n@@ -6,8 +6,8 @@\n \n - Date: 2026-07-28\n - Deciders: operator (mizutani-140), Claude (planner)\n-- Related: ADR 0004 (GitHub-first with a `.shiki` mirror), ADR 0009 (Spec Freeze),\n-  ADR 0012 (autonomous loop closeout to main), ADR 0015 (Contract Approval before\n+- Related: SADR-0004 (GitHub-first with a `.shiki` mirror), SADR-0009 (Spec Freeze),\n+  SADR-0012 (autonomous loop closeout to main), SADR-0015 (Contract Approval before\n   dispatch), remediation goal task R-02, the live PR #179 deadlock.\n \n ## Context\n@@ -90,11 +90,11 @@\n files, documentation files and named source modules are nameable; runtime-\n allocated ledger, runner and report ids are not \u2014 and those live in `.shiki`,\n which locks no longer govern. Existing frozen contracts are brought into line by\n-a one-time Spec Amendment per plan, which must land before ADR 0015's contract\n+a one-time Spec Amendment per plan, which must land before SADR-0015's contract\n immutability makes `locks` unwritable from the implementation PR.\n \n **Lock state is resolved from the durable side.** MergeGate resolves the lock\n-registry, and ADR 0015's governance fields, from the base `.shiki` snapshot when\n+registry, and SADR-0015's governance fields, from the base `.shiki` snapshot when\n present, falling back to head only when absent. The `MergeGate metadata check`\n job is given `--base-shiki`, which it does not receive today. Locks are not\n derived from the live open-pull-request set: a pull request body is untrusted\n@@ -108,7 +108,7 @@\n Explicitly out of scope: moving MergeGate's contract resolution off the PR\n checkout wholesale; making the mirror a projection of GitHub state; introducing\n new lock kinds; moving ledger evidence out of the repository; and unwinding\n-ADR 0012's closeout pull request. Registration of multi-task goals is ADR 0015's\n+SADR-0012's closeout pull request. Registration of multi-task goals is SADR-0015's\n Contract PR and is not re-decided here.\n \n ## Consequences\n@@ -127,7 +127,7 @@\n \n Risks accepted. First, goal-scoped identity is only as strong as `goal_id`,\n which is resolved from the PR body and the head task file; the mitigation is to\n-add `goal_id` to ADR 0015's frozen governance field set, and until that lands a\n+add `goal_id` to SADR-0015's frozen governance field set, and until that lands a\n task file can name a foreign goal. Second, base-when-present resolution is\n unavailable on fork pull requests, where the head is contributor-controlled;\n fork pull requests already cannot complete a Shiki merge, and this decision does\n@@ -139,7 +139,7 @@\n path, not by implementation pull requests.\n \n New obligations: one Spec Amendment per frozen plan to rewrite `locks`, ordered\n-before ADR 0015 T1; extension of the adversarial suites in\n+before SADR-0015 T1; extension of the adversarial suites in\n `scripts/test_shiki_adversarial_state.sh` and\n `scripts/test_shiki_governance_evidence.sh` to cover the eleven newly-scoped\n directories; and updating `scripts/test_shiki_loop_lock_guard.sh`,\n@@ -151,32 +151,32 @@\n whether the loop's transitive sync of ledger `evidence` references can be\n deleted once the Contract PR carries its own source plan.\n \n-## Relationship to ADR 0004\n+## Relationship to SADR-0004\n \n-This ADR **clarifies something ADR 0004 left unspecified**. ADR 0004 decides that\n+This SADR **clarifies something SADR-0004 left unspecified**. SADR-0004 decides that\n GitHub is the operational source of truth and that each target repository keeps a\n `.shiki/` mirror. It says nothing about pull requests writing the mirror, does\n not name MergeGate, and does not make the mirror an input to a gate. Nothing in\n-ADR 0004's Decision or Consequences is amended, superseded or weakened: the\n+SADR-0004's Decision or Consequences is amended, superseded or weakened: the\n mirror still exists, still records the same artifacts, and pull request\n-boundaries remain the coordination surface. What this ADR settles is the question\n+boundaries remain the coordination surface. What this SADR settles is the question\n 0004 never reached \u2014 by what authority a pull request may write a mirror record \u2014\n and it answers it the way `.shiki/manifest.json` already declares\n (`pr_mutation: current-task-or-current-goal-only`).\n \n-It **amends ADR 0012**, whose Decision item 5 states that \"Loop-executed tasks\n+It **amends SADR-0012**, whose Decision item 5 states that \"Loop-executed tasks\n declare the `path:.shiki/**` lock, which covers every `.shiki` file the closeout\n stages ... so `files_outside_locks` is satisfied without per-file lock\n bookkeeping.\" That clause is replaced: the closeout's mirror writes are\n-authorized by record identity, not by a declared lock. ADR 0012's substantive\n+authorized by record identity, not by a declared lock. SADR-0012's substantive\n decision \u2014 that completion reaches `main` through a closeout pull request rather\n than local attestation \u2014 is preserved unchanged.\n \n-It is **complementary to ADR 0015**, not a substitute. ADR 0015 owns contract\n+It is **complementary to SADR-0015**, not a substitute. SADR-0015 owns contract\n immutability, the pre-dispatch gate, the Contract PR and the Scope Envelope. This\n-ADR owns what a lock means and who may write the mirror. The one ordering\n+SADR owns what a lock means and who may write the mirror. The one ordering\n constraint between them is that the lock-narrowing Spec Amendment must precede\n-ADR 0015 T1's freezing of the `locks` field.\n+SADR-0015 T1's freezing of the `locks` field.\n \n ## Alternatives Considered\n \n@@ -186,7 +186,7 @@\n the only writer that can register them may run. Migrating the Guardian signal to\n that store makes every registration PR fail closed to `needs_guardian` at every\n risk level, because the task file is by definition absent from the store, which\n-removes the low/medium autonomous path ADR 0008/0009 exists to provide. And it\n+removes the low/medium autonomous path SADR-0008/0009 exists to provide. And it\n leaves schemas, manifest, config, guardian policy, templates, migrations, plans,\n worktrees, runner records, runs and handoffs with no writer at all in a\n repository whose product is `.shiki`. Measured benefit on the frozen backlog:\n@@ -199,14 +199,14 @@\n namespace is not in fact ownership-partitioned \u2014 56 ledger entries carry no\n `task_id`, and one goal DAG is writable by 35 registered tasks. The correct\n expression of its insight is that locks do not govern the mirror at all, which is\n-what this ADR decides.\n+what this SADR decides.\n \n **Make the mirror a projection written only by a registration writer and a\n post-merge projection job.** Rejected. It requires a bypass actor with write\n access to the protected default branch; it makes its own master invariant\n readable from a manifest that pull requests may edit; it deletes both reconcile\n modes and leaves a corrupted mirror with no in-band repair path, contradicting\n-ADR 0004's consequence that mirror conflicts are repaired against GitHub state;\n+SADR-0004's consequence that mirror conflicts are repaired against GitHub state;\n and its content-derived ids are rejected by `validate_shiki`'s currently-green id\n pattern.\n \ndiff --git a/docs/adr/0017-bookkeeping-closeouts-do-not-inherit-risk.md b/docs/adr/SADR-0017-bookkeeping-closeouts-do-not-inherit-risk.md\nindex 554e3a0d526a72ca067108c5464cc68c3edcf174..b9676857a4713850e063af7c39c8e0b01ecb4c66\n--- a/docs/adr/0017-bookkeeping-closeouts-do-not-inherit-risk.md\n+++ b/docs/adr/SADR-0017-bookkeeping-closeouts-do-not-inherit-risk.md\n@@ -1,4 +1,4 @@\n-# ADR 0017: A Bookkeeping Closeout Does Not Inherit Its Task's Risk\n+# SADR-0017: A Bookkeeping Closeout Does Not Inherit Its Task's Risk\n \n ## Status\n \n@@ -6,7 +6,7 @@\n \n ## Context\n \n-ADR 0012 made the goal loop push a task's completion to the default branch through a\n+SADR-0012 made the goal loop push a task's completion to the default branch through a\n closeout pull request: after the implementation PR merges, the loop opens a second PR\n carrying `task.status = done`, `lock.state = released`, and \u2014 when the task completes\n its goal \u2014 `goal.status = complete` plus one scorecard report. Nothing else. The\n@@ -49,14 +49,14 @@\n 4. The task file's governance fields (`scope`, `non_goals`, `required_skills`,\n    `risk_level`, `locks`, `acceptance_checks`, `test_command`) are byte-identical to\n    the base snapshot. Only `status`, `expected_pr`, `closeout_pr`, `ledger_evidence`\n-   and `expected_branch` may differ \u2014 the ADR 0015 contract-immutability rule,\n+   and `expected_branch` may differ \u2014 the SADR-0015 contract-immutability rule,\n    unchanged.\n \n    `expected_branch` is in that set because a closeout is *by construction* on a\n    different branch from the implementation it closes, and the loop rewrites the\n    field accordingly. It is safe to let it differ because MergeGate independently\n    requires `expected_branch` to equal the PR's actual head ref, so a PR cannot set\n-   it to anything other than the branch it is really on. The first draft of this ADR\n+   it to anything other than the branch it is really on. The first draft of this SADR\n    omitted it, which made the exemption unreachable: every real closeout failed\n    condition 4 on a field the loop always changes. See PR #209.\n 5. The state transitions are exactly the terminal set: task `review` \u2192 `done`, lock\n@@ -113,12 +113,12 @@\n classification must be derived, not asserted.\n \n **Fold the closeout into the implementation PR** so no second PR exists. Rejected under\n-ADR 0012, which recorded why: a task file that is already `done` on the implementation\n+SADR-0012, which recorded why: a task file that is already `done` on the implementation\n branch short-circuits the loop's review \u2192 CCA \u2192 merge \u2192 done ordering into a local\n goal-completion before any merge happens.\n \n **Have the loop auto-merge closeouts with elevated credentials.** Rejected outright: that\n-is a runtime authoring its own approval, which ADR 0010 and the constitution's\n+is a runtime authoring its own approval, which SADR-0010 and the constitution's\n approval-record prohibition forbid. It would also be indistinguishable, in the evidence,\n from the forgery those rules exist to prevent.\n \ndiff --git a/docs/adr/0018-carried-guardian-approval-across-a-proven-base-sync.md b/docs/adr/SADR-0018-carried-guardian-approval-across-a-proven-base-sync.md\nindex 22358562da390ed8b45cd600da08a43d378b4680..e6e2907cefa776460af73ddf7d39db605f3c39b0\n--- a/docs/adr/0018-carried-guardian-approval-across-a-proven-base-sync.md\n+++ b/docs/adr/SADR-0018-carried-guardian-approval-across-a-proven-base-sync.md\n@@ -1,4 +1,4 @@\n-# ADR 0018: Carried Guardian Approval Across A Proven Base Sync\n+# SADR-0018: Carried Guardian Approval Across A Proven Base Sync\n \n ## Status\n \n@@ -34,7 +34,7 @@\n Guardian evaluator that consumes it (`shiki_guardian.evaluate_guardian_approval`,\n the `guardian_comment_carried` source) were built and tested in isolation. Neither\n does anything until a caller passes the flags that turn the carry on, so the\n-feature was invisible in CI. This ADR records the decision the two of them\n+feature was invisible in CI. This SADR records the decision the two of them\n implement, and this task wires it into the four CI invocations that judge Guardian\n approval.\n \ndiff --git a/docs/adr/README.md b/docs/adr/README.md\nindex c70e057a4520c36e042ab68c53cd4a21ef709b71..5da43856836e2ce6a150f8127766e2f88355ab4b\n--- a/docs/adr/README.md\n+++ b/docs/adr/README.md\n@@ -1,40 +1,41 @@\n-# Architecture Decision Records\n+# Decision Record Namespaces\n \n-This directory holds Shiki's Architecture Decision Records (ADRs): durable\n-records of hard-to-reverse platform decisions. ADRs are part of the Shiki source\n-of truth alongside GitHub state, the `.shiki/` mirror, and `CONTEXT.md`.\n+This directory holds two independent decision namespaces:\n \n-## How ADRs Work\n+- Shiki Architecture Decision Records (SADRs), named\n+  `SADR-NNNN-<short-slug>.md`, record hard-to-reverse Shiki platform decisions.\n+- Target Architecture Decision Records (ADRs), named\n+  `NNNN-<short-slug>.md`, belong to the target product.\n \n-- New ADRs are added as `docs/adr/NNNN-<short-slug>.md`, where `NNNN` is the next\n-  zero-padded sequence number.\n-- The **canonical list of ADRs is the set of files matching `docs/adr/*.md`** in\n-  this directory. Browse `docs/adr/*.md` for the authoritative, current set.\n-- Use [`template.md`](template.md) as the starting point for a new ADR.\n-- Record an ADR when a decision is hard to reverse, surprising without context,\n-  and a real tradeoff. Do not write an ADR for routine or easily reversible\n-  choices.\n+The same four-digit number may appear once in each namespace. A platform\n+`SADR-NNNN` citation resolves only to `SADR-NNNN-*.md`; a target `ADR NNNN`\n+citation resolves only to `NNNN-*.md`.\n \n-Using the `docs/adr/*.md` glob as the source of truth means other PRs can add an\n-ADR without editing this index file, avoiding merge contention on a\n-hand-maintained enumeration.\n+## How Decision Records Work\n \n-## Current ADRs (snapshot)\n-\n-The list below is a convenience snapshot and may lag behind the directory. When\n-in doubt, list `docs/adr/*.md`.\n+- New Shiki platform decisions use the next zero-padded SADR number.\n+- New target product decisions use the target's own ADR sequence.\n+- The canonical list is the files in `docs/adr/`; no hand-maintained index is\n+  authoritative.\n+- Use [`template.md`](template.md) as the starting point and choose the filename\n+  namespace that owns the decision.\n+- Record a decision when it is hard to reverse, surprising without context, and\n+  a real tradeoff. Do not write a decision record for routine or easily\n+  reversible choices.\n \n-- [0001 \u2014 Keep Shiki As The Platform Name](0001-keep-shiki-as-platform-name.md)\n-- [0002 \u2014 Use AGENTS As The Shared Agent Constitution](0002-use-agents-as-shared-constitution.md)\n-- [0003 \u2014 Require Skill Gate For MergeGate Readiness](0003-require-skill-gate-for-mergegate-readiness.md)\n-- [0004 \u2014 Use GitHub First With A Repository-Local Shiki Mirror](0004-use-github-first-with-shiki-mirror.md)\n-- [0005 \u2014 Use Codex As Front And Claude Code Action As The GitHub Runtime](0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md)\n-- [0006 \u2014 Separate Codex Implementation, CCA Completion, And MergeGate Authorization](0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md)\n+Using the directory as the source of truth lets independent changes add records\n+without contending on an enumerated index.\n \n-## Adding A New ADR\n+## Adding A Shiki SADR\n \n-1. Copy `template.md` to `docs/adr/NNNN-<short-slug>.md`.\n+1. Copy `template.md` to `docs/adr/SADR-NNNN-<short-slug>.md`.\n 2. Fill in the title, status, context, decision, and consequences.\n-3. Set the status to `Proposed`, then `Accepted` once the decision is approved.\n-4. Open a PR following `CONTRIBUTING.md`. You do not need to edit this index for\n-   the ADR to be discoverable via `docs/adr/*.md`.\n+3. Set the status to `Proposed`, then `Accepted` once the proper authority has\n+   approved the decision.\n+4. Open a PR following `CONTRIBUTING.md`.\n+\n+## Adding A Target ADR\n+\n+1. Copy `template.md` to `docs/adr/NNNN-<short-slug>.md` using the target's own\n+   sequence.\n+2. Cite it as `ADR NNNN`; do not prefix its filename with `SADR-`.\ndiff --git a/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md b/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\nnew file mode 100644\nindex 0000000000000000000000000000000000000000..7455ce1f3bed02ddddb851ac21a1d1a648c9aeba\n--- /dev/null\n+++ b/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\n@@ -0,0 +1,55 @@\n+# SADR-0019: Separate Shiki Platform SADRs From Target ADRs\n+\n+## Status\n+\n+Accepted\n+\n+## Context\n+\n+Shiki installs its platform decision records into `docs/adr/` in every target\n+repository. Shiki and its targets previously used the same numeric\n+`NNNN-<slug>.md` namespace. A target that already owned ADRs 0013 through 0018\n+therefore acquired two records for each number during installation, making an\n+`ADR NNNN` citation ambiguous. Renumbering target decisions would rewrite target\n+history, while renumbering Shiki decisions per target would destroy their stable\n+platform identity.\n+\n+Upgrades also need to remove the former Shiki-owned numeric paths without ever\n+mistaking a target-owned ADR for platform content. The install stamp already\n+records exact shipped paths and content digests, so it can prove ownership only\n+when both still match.\n+\n+## Decision\n+\n+Shiki platform decisions use the `SADR` namespace:\n+\n+- records are named `docs/adr/SADR-NNNN-<slug>.md`;\n+- live platform citations use `SADR-NNNN`;\n+- target product decisions retain `docs/adr/NNNN-<slug>.md` and `ADR NNNN`;\n+- the reference validator resolves the two namespaces independently, so the same\n+  four-digit number may exist once in each namespace.\n+\n+Before a forced upgrade writes anything, the installer inventories the exact\n+former numeric Shiki ADR paths. It may delete a legacy path only when the\n+existing install stamp contains that exact path and its recorded SHA-256 digest\n+matches the file currently on disk. All ownership blockers are reported\n+together and abort the upgrade before its first write. A non-forced install\n+never performs this cleanup.\n+\n+Historical `.shiki/` evidence remains unchanged because it records the namespace\n+that existed when that evidence was produced.\n+\n+## Consequences\n+\n+- Targets may use any ADR number without colliding with Shiki's platform SADRs.\n+- Fresh installs preserve target ADR bytes and install a stable SADR set.\n+- Forced upgrades fail closed when legacy ownership cannot be proven, and a\n+  successful upgrade refreshes the stamp without removed legacy paths.\n+- Current governance, packaging, workflow, prompt, script, and skill references\n+  must move with the platform records.\n+\n+## Alternatives Considered\n+\n+- Collision-only refusal leaves affected targets permanently unupgradable.\n+- Renumbering target ADRs mutates target-owned history.\n+- Per-target Shiki renumbering makes platform citations non-portable.\ndiff --git a/docs/adr/template.md b/docs/adr/template.md\nindex a92a97daddd0b00ab9dd626cc8d86942da2eafed..17dd79175b5fee2105935557f691b40cf9492942\n--- a/docs/adr/template.md\n+++ b/docs/adr/template.md\n@@ -1,14 +1,14 @@\n-# NNNN: <Short Decision Title>\n+# <SADR-NNNN or NNNN>: <Short Decision Title>\n \n ## Status\n \n-Proposed | Accepted | Superseded by ADR-NNNN\n+Proposed | Accepted | Superseded by SADR-NNNN or target ADR NNNN\n \n ## Context\n \n What is the situation, constraint, or force that requires a decision? Reference\n-relevant domain terms (`CONTEXT.md`), prior ADRs, and code reality. State the\n-problem, not the solution.\n+relevant domain terms (`CONTEXT.md`), prior Shiki SADRs or target ADRs, and code\n+reality. State the problem, not the solution.\n \n ## Decision\n \ndiff --git a/docs/agents/autonomous-loop-e2e-acceptance.md b/docs/agents/autonomous-loop-e2e-acceptance.md\nindex bbf84c6081987f159ac6ad9ecf3c82b7aa72e7e8..5c13c1d02a05a90b0dc46cbef7c95a9846d0c237\n--- a/docs/agents/autonomous-loop-e2e-acceptance.md\n+++ b/docs/agents/autonomous-loop-e2e-acceptance.md\n@@ -40,8 +40,8 @@\n 3. `gh auth status` is logged in with merge permission on the repo.\n 4. Branch protection + required checks are configured exactly as the goal-loop\n    required-check set expects (see `docs/agents/branch-protection-smoke.md`).\n-5. The repo `.shiki/policy` permits autonomous low/medium-risk merges (ADR\n-   0008/0009); the MergeGate policy check is the recorded-authority gate.\n+5. The repo `.shiki/policy` permits autonomous low/medium-risk merges\n+   (SADR-0008/0009); the MergeGate policy check is the recorded-authority gate.\n \n The operator's only actions are: register the task and start the loop. **No\n manual pre-clearing** means after `shiki loop run` starts, the operator does not\ndiff --git a/docs/agents/checklists.md b/docs/agents/checklists.md\nindex 4a7f03ade346f53a1c1d82981f3fd744f9ab4547..0c5ec6c53dc7197b7b7a7d18e667b325babd6c45\n--- a/docs/agents/checklists.md\n+++ b/docs/agents/checklists.md\n@@ -32,18 +32,18 @@\n | ID | Item | Blocking |\n | --- | --- | --- |\n | GD-01 | Plan was challenged against `CONTEXT.md`. | blocking for non-trivial Goals |\n-| GD-02 | Relevant ADRs were read or absence was recorded. | blocking for non-trivial Goals |\n+| GD-02 | Relevant SADRs and target ADRs were read or absence was recorded. | blocking for non-trivial Goals |\n | GD-03 | Ambiguous terms were resolved or converted to HITL blockers. | blocking |\n | GD-04 | Edge scenarios were considered. | advisory |\n | GD-05 | Code reality was checked when code could answer. | blocking when applicable |\n | GD-06 | New domain terms were added to `CONTEXT.md` when needed. | blocking when applicable |\n-| GD-07 | ADR was created or explicitly skipped using the ADR criteria. | blocking when applicable |\n+| GD-07 | A SADR or target ADR was created or explicitly skipped using the decision-record criteria. | blocking when applicable |\n \n ## CI \u2014 Context & Impact Checklist\n \n | ID | Item | Blocking |\n | --- | --- | --- |\n-| CI-01 | Relevant docs and ADRs identified. | blocking |\n+| CI-01 | Relevant docs, SADRs, and target ADRs identified. | blocking |\n | CI-02 | Relevant modules, interfaces, seams, callers, and tests identified. | blocking |\n | CI-03 | Dependencies and blocked-by relationships identified. | blocking |\n | CI-04 | Candidate locks identified. | blocking |\n@@ -62,7 +62,7 @@\n | PRD-04 | Implementation decisions are recorded without volatile code detail. | blocking |\n | PRD-05 | Testing decisions are recorded. | blocking |\n | PRD-06 | Out-of-scope items are explicit. | blocking |\n-| PRD-07 | Relevant glossary/ADR links are present when applicable. | blocking |\n+| PRD-07 | Relevant glossary/SADR/target-ADR links are present when applicable. | blocking |\n \n ## SF \u2014 Spec Freeze Checklist\n \n@@ -98,7 +98,7 @@\n | H-02 | Branch/worktree target is present. | blocking |\n | H-03 | Scope and non-goals are present. | blocking |\n | H-04 | Dependencies and locks are present. | blocking |\n-| H-05 | Relevant docs, ADRs, modules, and tests are referenced. | blocking |\n+| H-05 | Relevant docs, SADRs, target ADRs, modules, and tests are referenced. | blocking |\n | H-06 | Required skills are stated. | blocking |\n | H-07 | TDD expectations are stated. | blocking when behavior work |\n | H-08 | Acceptance criteria are included. | blocking |\ndiff --git a/docs/agents/codex-handoff.md b/docs/agents/codex-handoff.md\nindex ede468f47cb277f69a857e8c45df703e4ed0151d..6f97bdffb67e44d7edea2c71e01ea28933c4eb6c\n--- a/docs/agents/codex-handoff.md\n+++ b/docs/agents/codex-handoff.md\n@@ -46,7 +46,7 @@\n \n ## Context\n - Domain terms:\n-- Relevant ADRs:\n+- Relevant SADRs and target ADRs:\n - Relevant docs:\n - Relevant modules/interfaces/seams:\n - Similar tests/prior art:\ndiff --git a/docs/agents/completion-check-agent.md b/docs/agents/completion-check-agent.md\nindex 13e4c035db35025f4e3cdcc5adfb24314ac4d521..61bfa1218cf36ca605250c91919e620f33238aa0\n--- a/docs/agents/completion-check-agent.md\n+++ b/docs/agents/completion-check-agent.md\n@@ -32,7 +32,7 @@\n - `AGENTS.md`\n - `CLAUDE.md`\n - `CONTEXT.md`\n-- relevant ADRs\n+- relevant SADRs and target ADRs\n - parent Goal issue\n - PRD issue or document\n - task issue\ndiff --git a/docs/agents/decision-control.md b/docs/agents/decision-control.md\nindex 7d8ee5ab03a5ccbcbd04056ae61de03785a2a9c9..8c94ecda00fffaa0e92c325ab83aa9776bd1dac1\n--- a/docs/agents/decision-control.md\n+++ b/docs/agents/decision-control.md\n@@ -11,7 +11,7 @@\n | Decision | Owner | When | Required inputs | Output | Enforcement surface |\n | --- | --- | --- | --- | --- | --- |\n | Is the Goal clear enough to plan? | Planner + Guardian when needed | Goal intake | User request, existing docs, risk signals | Goal readiness verdict | Goal checklist, issue template, ledger |\n-| Must `grill-with-docs` run? | Planner / Skill Gate | Before PRD or issue split | Ambiguity, domain terms, ADR conflicts, unclear boundaries | Skill invocation or explicit skip rationale | Skill Gate checklist |\n+| Must `grill-with-docs` run? | Planner / Skill Gate | Before PRD or issue split | Ambiguity, domain terms, SADR/target-ADR conflicts, unclear boundaries | Skill invocation or explicit skip rationale | Skill Gate checklist |\n | Is a PRD required? | Planner | After Goal context stabilizes | Goal, `grill-with-docs` output, Context & Impact output | PRD required / not required | PRD checklist, CCA evidence check |\n | Can the spec be frozen? | Operator only | End of Requirements Definition, before issue split | PRD/requirements summary, scope inventory (SF-02), open-question list | `spec_freeze` block in the plan + ledger evidence | `require_grilled_plan` (plan ingest/run), SF checklist |\n | Can a frozen spec change? | Operator only (Spec Amendment) | When implementation contradicts the frozen spec | Contested decisions, scoped re-grill result | recorded amendment + re-stamped freeze | SF checklist, ledger evidence |\ndiff --git a/docs/agents/domain.md b/docs/agents/domain.md\nindex 258da8ea81e87ac0a6b0b5b5e5c2fde980eaabb7..d57b0cf17ff3e1fa9e6e5885372bf083d8295421\n--- a/docs/agents/domain.md\n+++ b/docs/agents/domain.md\n@@ -5,7 +5,9 @@\n ## Layout\n \n - `CONTEXT.md` defines Shiki language. Keep it implementation-free.\n-- `docs/adr/` records hard-to-reverse decisions.\n+- `docs/adr/SADR-NNNN-*.md` records Shiki platform decisions.\n+- `docs/adr/NNNN-*.md` records target product decisions without sharing the\n+  SADR namespace.\n - `AGENTS.md` is the runtime-neutral agent constitution.\n - `CLAUDE.md` is the Claude Code wrapper around `AGENTS.md`.\n - `.shiki/` is the Target Repository mirror for Goals, PRDs, plans, Task DAGs, contracts, locks, worktree records, ledger entries, CCA verdicts, repair packets, reports, and handoffs.\n@@ -15,6 +17,6 @@\n \n - Use Shiki terms exactly when they exist.\n - Add glossary terms to `CONTEXT.md` only when the term is part of the domain, not a generic programming concept.\n-- Create ADRs only for hard-to-reverse, surprising, tradeoff-based decisions.\n+- Create SADRs or target ADRs only for hard-to-reverse, surprising, tradeoff-based decisions.\n - Keep product-specific language in the Target Repository, not in Shiki Template docs.\n-- If an ADR conflicts with a proposed implementation, surface the conflict before editing.\n+- If a SADR or target ADR conflicts with a proposed implementation, surface the conflict before editing.\ndiff --git a/docs/agents/guardian-policy.md b/docs/agents/guardian-policy.md\nindex 4147c7adf05a47adb4fd24ada1a1c2e1f2ba6d2d..a20d9cc6c00a569bb057b9ca8d6b51c7fc4a9847\n--- a/docs/agents/guardian-policy.md\n+++ b/docs/agents/guardian-policy.md\n@@ -98,8 +98,8 @@\n `origin/<default_branch>` and reuses each job's existing base fetch; a shallow\n (`--depth`) or second base fetch would make the proof unresolvable and silently\n disable the carry, so neither is permitted. CCA reads `guardian_comment_carried` as\n-a recorded authority in the deterministic signal and does not re-derive it. See ADR\n-0018 for the full decision, the rejected diff/patch-equality and path-disjointness\n+a recorded authority in the deterministic signal and does not re-derive it. See\n+SADR-0018 for the full decision, the rejected diff/patch-equality and path-disjointness\n alternatives, and the accepted residual.\n \n ## External AI Guardian Review (SADR-0010)\ndiff --git a/docs/agents/implementation-policy.md b/docs/agents/implementation-policy.md\nindex c754144a9fb5bf4a38aa82a60e1670c59e626b44..85c37281bcad196c6b7b02343faf00b03cd9c57a\n--- a/docs/agents/implementation-policy.md\n+++ b/docs/agents/implementation-policy.md\n@@ -31,10 +31,13 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n ## Non-Negotiables\n \n 1. **Planning precedes implementation.** Non-trivial Goals start with `grill-with-docs` and Context & Impact.\n-2. **Domain language is controlled.** Terms must be checked against `CONTEXT.md`; hard-to-reverse tradeoffs must be checked against ADRs.\n+2. **Domain language is controlled.** Terms must be checked against `CONTEXT.md`; hard-to-reverse tradeoffs must be checked against Shiki SADRs and target ADRs.\n 3. **PRD precedes issue decomposition.** `to-prd` is used once the context is settled enough to become durable intent.\n 4. **Issues are vertical slices.** `to-issues` must produce independently grabbable tracer-bullet tasks, not horizontal layer tickets.\n 5. **Triage controls dispatch.** Only `ready-for-agent` / AFK tasks may be assigned to Codex without human input.\n@@ -95,19 +98,19 @@\n Process:\n \n - Challenge terms against `CONTEXT.md`.\n-- Check ADRs before reopening decisions.\n+- Check SADRs and target ADRs before reopening decisions.\n - Inspect code when code can answer a question.\n - Ask one question at a time when the operator is available.\n - Provide a recommended answer with each question.\n - Record resolved terms in `CONTEXT.md`.\n-- Create ADRs only for hard-to-reverse, surprising, tradeoff-based decisions.\n+- Create SADRs or target ADRs only for hard-to-reverse, surprising, tradeoff-based decisions.\n - Convert unresolved load-bearing questions into HITL blockers.\n \n Outputs:\n \n - Resolved design-tree decisions.\n - Updated glossary terms when needed.\n-- ADRs when justified.\n+- SADRs or target ADRs when justified.\n - HITL blockers when decisions remain unresolved.\n \n ## Phase 3 \u2014 Context & Impact\n@@ -117,7 +120,7 @@\n \n Outputs must include:\n \n-- Relevant documents and ADRs.\n+- Relevant documents, SADRs, and target ADRs.\n - Relevant modules, interfaces, seams, callers, and tests.\n - Candidate locks.\n - Dependencies.\n@@ -140,7 +143,7 @@\n - Testing decisions.\n - Out of scope.\n - Further notes.\n-- Links to relevant Goal, domain terms, and ADRs.\n+- Links to relevant Goal, domain terms, SADRs, and target ADRs.\n \n Phase 4 ends with Spec Freeze: the operator explicitly approves the PRD, the\n plan records a `spec_freeze` block (status, approved_by, source), and the SF\n@@ -240,7 +243,7 @@\n - Dependencies and locks.\n - Risk and Guardian requirements.\n - Ledger completeness.\n-- Documentation and ADR updates.\n+- Documentation and SADR/target-ADR updates.\n - Scope drift.\n \n CCA emits one of:\n@@ -301,7 +304,7 @@\n | Evidence failure | Behavior might be done, but proof is missing. | Add checks, PR evidence, ledger entry, or artifacts. |\n | Scope drift | PR changes unrelated behavior. | Revert unrelated changes or split into new Goal. |\n | Dependency block | Upstream task or lock not complete. | Block until dependency resolves. |\n-| Architecture block | Decision needs ADR or deep-module discussion. | `grill-with-docs` or `improve-codebase-architecture`. |\n+| Architecture block | Decision needs a SADR, target ADR, or deep-module discussion. | `grill-with-docs` or `improve-codebase-architecture`. |\n | Risk block | Security/production/policy change needs human approval. | Guardian decision. |\n \n ## Phase 5.5 \u2014 Execution Decision Control\ndiff --git a/docs/agents/runtime-auth-model.md b/docs/agents/runtime-auth-model.md\nindex 6a2e1a47e81ea89b5c68a210ec07772a9374babc..3c54748d24233dbd0ba8473950282547ef4ce935\n--- a/docs/agents/runtime-auth-model.md\n+++ b/docs/agents/runtime-auth-model.md\n@@ -148,7 +148,7 @@\n \n API-key based runners may be added by a target repository as an explicit extension. They are not the default Shiki template path.\n \n-If a repo opts into API-key mode, record the exception in an ADR and update the repo's `AGENTS.md`, `.shiki/config.yaml`, and workflow permissions.\n+If a repo opts into API-key mode, record the target-specific exception in a target ADR and update the repo's `AGENTS.md`, `.shiki/config.yaml`, and workflow permissions.\n \n ## GitHub Actions Runtime Compatibility\n \ndiff --git a/docs/agents/skill-gate.md b/docs/agents/skill-gate.md\nindex c50398b0d7c60a8598f2a5c88a10015730dced13..3acffe6444617bb1d54b77d4b4708158ec735bc6\n--- a/docs/agents/skill-gate.md\n+++ b/docs/agents/skill-gate.md\n@@ -9,7 +9,7 @@\n | Skill | Trigger | Expected output |\n | --- | --- | --- |\n | `setup-matt-pocock-skills` | First configuring a repo for these skills, or when issue tracker, triage labels, or domain docs are missing. | `docs/agents/` configuration and an `Agent skills` block in `AGENTS.md` or `CLAUDE.md`. |\n-| `grill-with-docs` | Ambiguous plan, unclear terminology, boundary decision, tradeoff, or ADR-worthy design choice. | Clarified decision tree, recommended answers, updates to `CONTEXT.md` or ADRs when decisions crystallize. |\n+| `grill-with-docs` | Ambiguous plan, unclear terminology, boundary decision, tradeoff, or decision-record-worthy design choice. | Clarified decision tree, recommended answers, updates to `CONTEXT.md`, SADRs, or target ADRs when decisions crystallize. |\n | `zoom-out` | Agent lacks a higher-level architectural map. | Map of relevant modules, callers, seams, and domain concepts. |\n | `to-prd` | Settled context should become a PRD. | PRD published to the issue tracker with problem, solution, user stories, decisions, testing, and out-of-scope notes. |\n | `to-issues` | Goal, PRD, or plan must become implementation tickets. | Vertical-slice issues with acceptance criteria, dependencies, and AFK/HITL classification. |\n@@ -17,7 +17,7 @@\n | `tdd` | Feature work or bug fix has observable behavior that can be specified with tests. | Red-green-refactor loop through public interfaces, one vertical slice at a time. |\n | `code-review` | The TDD loop is green and the implementer is about to create or update a PR. Default for every implementation task. | Pre-PR self-review of the task diff with fixes applied on the task branch; findings and resolutions recorded as a ledger entry naming the skill and a `## Pre-PR code review` PR body section. |\n | `diagnose` | Hard bug, failing check, regression, flaky behavior, or performance problem. | Reproduction loop, ranked hypotheses, instrumentation, fix, regression test, post-mortem. |\n-| `improve-codebase-architecture` | Structural friction, testability, AI-navigability, deep-module opportunity, or refactor request. | Architecture candidates and deepening recommendations informed by `CONTEXT.md` and ADRs. |\n+| `improve-codebase-architecture` | Structural friction, testability, AI-navigability, deep-module opportunity, or refactor request. | Architecture candidates and deepening recommendations informed by `CONTEXT.md`, SADRs, and target ADRs. |\n | `prototype` | A throwaway logic or UI prototype is needed to answer a design question. | Clearly marked disposable prototype and captured decision. |\n \n ## MergeGate Interaction\ndiff --git a/scripts/guardian_approval_signal.py b/scripts/guardian_approval_signal.py\nindex d059f92b2c82aa2b5d6ca421365537a87886c1cb..16924892714f4f5b9303c0ef0e325a7e70075630\n--- a/scripts/guardian_approval_signal.py\n+++ b/scripts/guardian_approval_signal.py\n@@ -199,8 +199,8 @@\n ) -> Any:\n     \"\"\"Evaluate SADR-0015 Contract Approval for the CCA Guardian signal.\n \n-    The SAME OR that MergeGate applies must reach this deterministic signal: ADR\n-    0015 is explicit that without it, a contract-approved PR still returns\n+    The SAME OR that MergeGate applies must reach this deterministic signal:\n+    SADR-0015 is explicit that without it, a contract-approved PR still returns\n     ``needs_guardian``, the CCA job fails, and the MergeGate policy check never\n     runs \u2014 the gate this change moves would never be reached. This resolves the\n     task the SAME way MergeGate does (the first Shiki task id in the PR body),\ndiff --git a/scripts/shiki_contract_approval.py b/scripts/shiki_contract_approval.py\nindex 941bcb96fdd8b9a85bc0c4f0f04d5dad9d04839c..4a4b418f6527413161db82670bec857a043360e2\n--- a/scripts/shiki_contract_approval.py\n+++ b/scripts/shiki_contract_approval.py\n@@ -142,7 +142,7 @@\n     changed_files_status: Any,\n     registration: Any,\n ) -> ContractApprovalResult:\n-    \"\"\"Whether this PR carries an SADR-0015 Contract Approval.\n+    \"\"\"Whether this PR carries a SADR-0015 Contract Approval.\n \n     The four conditions are evaluated IN ORDER, each failure recorded as a\n     distinct reason string:\ndiff --git a/scripts/shiki_installer.py b/scripts/shiki_installer.py\nindex be064ab7a29ff6836f7f7a1d70500a43e0d11d7b..d3e3247edbf3e4be287a4667176d86f6500f8dc5\n--- a/scripts/shiki_installer.py\n+++ b/scripts/shiki_installer.py\n@@ -118,6 +118,7 @@\n # what lets a target carry it without a governance-schema change.\n INSTALL_STAMP_PATH = \".shiki/install-stamp.json\"\n INSTALL_STAMP_VERSION = 1\n+LEGACY_SHIKI_SADR_MAX = 18\n \n # Under --force the shipped surface splits three ways, so nothing is lost and\n # nothing is silently stale.\n@@ -237,9 +238,98 @@\n         )\n \n \n+def legacy_shiki_adr_paths() -> tuple[str, ...]:\n+    \"\"\"Former numeric paths for Shiki SADR-0001 through SADR-0018.\n+\n+    The authoritative slugs come from the current SADR filenames. SADR-0019 is\n+    the namespace decision itself and never shipped under a numeric legacy path.\n+    \"\"\"\n+    paths: list[str] = []\n+    for sadr in sorted((ROOT / \"docs\" / \"adr\").glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\")):\n+        number = sadr.name.split(\"-\", 2)[1]\n+        if int(number) <= LEGACY_SHIKI_SADR_MAX:\n+            paths.append(f\"docs/adr/{sadr.name.removeprefix('SADR-')}\")\n+    return tuple(paths)\n+\n+\n+def preflight_legacy_sadr_cleanup(target: Path, *, force: bool) -> tuple[str, ...]:\n+    \"\"\"Authorize exact legacy Shiki ADR removals before the first write.\n+\n+    A non-force install never removes existing files. A forced install needs no\n+    stamp when no legacy Shiki path is present. When legacy paths do exist, each\n+    must be named in a readable install stamp and its current bytes must match\n+    the recorded digest.\n+    \"\"\"\n+    if not force:\n+        return ()\n+    present = tuple(\n+        relative\n+        for relative in legacy_shiki_adr_paths()\n+        if (target / relative).exists() or (target / relative).is_symlink()\n+    )\n+    if not present:\n+        return ()\n+\n+    stamp_path = target / INSTALL_STAMP_PATH\n+    try:\n+        raw = stamp_path.read_text(encoding=\"utf-8\")\n+        stamp = json.loads(raw)\n+    except FileNotFoundError:\n+        detail = \"install stamp is absent\"\n+        blockers = [f\"{relative}: {detail}\" for relative in present]\n+    except (OSError, UnicodeDecodeError) as error:\n+        detail = f\"install stamp is unreadable ({error})\"\n+        blockers = [f\"{relative}: {detail}\" for relative in present]\n+    except json.JSONDecodeError as error:\n+        detail = f\"install stamp is malformed ({error})\"\n+        blockers = [f\"{relative}: {detail}\" for relative in present]\n+    else:\n+        if not isinstance(stamp, dict):\n+            blockers = [\n+                f\"{relative}: install stamp is malformed (expected an object)\"\n+                for relative in present\n+            ]\n+        else:\n+            digests = stamp.get(\"digests\")\n+            blockers = []\n+            if not isinstance(digests, dict):\n+                blockers.extend(\n+                    f\"{relative}: install stamp has no digest for this path\"\n+                    for relative in present\n+                )\n+            else:\n+                for relative in present:\n+                    expected = digests.get(relative)\n+                    if not isinstance(expected, str) or not expected:\n+                        blockers.append(\n+                            f\"{relative}: install stamp has no digest for this path\"\n+                        )\n+                        continue\n+                    try:\n+                        actual = _sha256_file(target / relative)\n+                    except OSError as error:\n+                        blockers.append(\n+                            f\"{relative}: legacy path is unreadable ({error})\"\n+                        )\n+                        continue\n+                    if actual != expected:\n+                        blockers.append(\n+                            f\"{relative}: digest mismatch (stamp {expected}, current {actual})\"\n+                        )\n+\n+    if blockers:\n+        joined = \"\\n\".join(f\"- {blocker}\" for blocker in blockers)\n+        raise ShikiError(\n+            \"legacy Shiki ADR cleanup is not authorized:\\n\"\n+            f\"{joined}\\nNo files were written.\"\n+        )\n+    return present\n+\n+\n def install_template(target: Path, *, force: bool, validate: bool) -> list[NewFileNote]:\n     # Preconditions first: never rewrite the tree of a target that cannot be\n     # upgraded cleanly (see refuse_pending_migrations).\n+    legacy_cleanup = preflight_legacy_sadr_cleanup(target, force=force)\n     refuse_pending_migrations(target)\n \n     # Resolve the target's required CODEOWNERS owner once from its\n@@ -250,6 +340,10 @@\n     # verbatim. See ``codeowners_required_owner``.\n     codeowners_owner = codeowners_required_owner(target)\n \n+    for relative in legacy_cleanup:\n+        (target / relative).unlink()\n+        info(f\"removed stamped legacy Shiki ADR: {target / relative}\")\n+\n     notes: list[NewFileNote] = []\n     for relative in TEMPLATE_PATHS:\n         source = ROOT / relative\ndiff --git a/scripts/shiki_loop.py b/scripts/shiki_loop.py\nindex 3611641da8aeb5038b1da90e3114b09a473ec6d8..84caa491b9cad0edc4176f209c13e9221e975681\n--- a/scripts/shiki_loop.py\n+++ b/scripts/shiki_loop.py\n@@ -424,7 +424,7 @@\n         # unresolvable CCA verdict has already produced its terminal stop and is never\n         # laundered into a head-moving sync (SADR-0010). It also never fires while the\n         # policy (Guardian) gate is red \u2014 for ANY reason, not only when it is the sole\n-        # red check \u2014 so a head-bound Guardian approval, or an SADR-0015\n+        # red check \u2014 so a head-bound Guardian approval, or a SADR-0015\n         # contract-approval carry that base movement defeated (the policy gate compares\n         # base and head task governance directly), is never silently restored by\n         # adopting base wholesale. It is bounded by a durable per-task counter so the\ndiff --git a/scripts/test_shiki_goal_loop.sh b/scripts/test_shiki_goal_loop.sh\nindex 753c7b516e10f0be3bc92a4bd0eb1fa699776ae6..8f3820a0a079b3044424cfe1a6c6b7fe68896e67\n--- a/scripts/test_shiki_goal_loop.sh\n+++ b/scripts/test_shiki_goal_loop.sh\n@@ -231,8 +231,8 @@\n \n # High/critical risk merges autonomously when all required checks are green:\n # the \"MergeGate policy check\" required check IS the Guardian gate (it enforces\n-# guardian-policy.json \u2014 human approval OR external AI guardian review, ADR\n-# 0010), so green checks mean a recorded authority approved. The fake gh returns\n+# guardian-policy.json \u2014 human approval OR external AI guardian review,\n+# SADR-0010), so green checks mean a recorded authority approved. The fake gh returns\n # green checks, simulating that the Guardian gate passed.\n python3 \"$ROOT/scripts/shiki.py\" goal create --target \"$TARGET\" --title \"Guardian gate\" --outcome \"High risk merges via policy gate\" >/tmp/shiki-goal-loop-guardian-goal.json\n GGOAL=\"$(json_get /tmp/shiki-goal-loop-guardian-goal.json goal_id)\"\ndiff --git a/tests/test_adr_references.py b/tests/test_adr_references.py\nindex 51706329c797d5dd9b47e082a333fa83b261d482..db6e693e35f67103935be34f1d9bb4ff7ee60395\n--- a/tests/test_adr_references.py\n+++ b/tests/test_adr_references.py\n@@ -1,7 +1,7 @@\n-\"\"\"Every ``ADR ####`` citation must resolve to exactly one decision record.\n+\"\"\"Every target ADR and Shiki SADR citation resolves in its own namespace.\n \n Background. ``scripts/mergegate_check.py`` justifies ``is_bookkeeping_closeout``\n-with \"ADR 0017\" nine times and ``scripts/guardian_approval_signal.py`` cites\n+with \"SADR-0017\" nine times and ``scripts/guardian_approval_signal.py`` cites\n 0016/0017 seven times, yet for a stretch the numbering in ``docs/adr/`` stopped\n at 0015: the authoring commits lived on a coordinator branch and no task ever\n declared ``docs/adr/...`` in its locks, so nothing carried the records to the\n@@ -11,11 +11,12 @@\n and the directory drifted with no signal.\n \n This is the same unbound-pair shape behind other silent failures: a reference\n-and its target with nothing asserting they agree. This test binds them. It scans\n-every ``scripts/*.py`` (plus ``docs/**/*.md`` and the root ``*.md`` governance\n-files, which are cheap) for the ``ADR ####`` pattern and asserts each cited\n-number resolves to exactly one ``docs/adr/####-*.md``. The converse is\n-deliberately NOT required: an ADR may exist without being cited.\n+and its target with nothing asserting they agree. This test binds them. Target\n+``ADR NNNN`` citations resolve only to ``docs/adr/NNNN-*.md``; Shiki\n+``SADR-NNNN`` citations resolve only to ``docs/adr/SADR-NNNN-*.md``. The scan\n+covers current governance docs, scripts, workflows, prompts, packaging metadata,\n+and repo-local Codex guidance while deliberately excluding historical\n+``.shiki`` evidence. The converse is not required: a record may be uncited.\n \n The suite is dependency free: standard-library ``unittest`` only.\n \"\"\"\n@@ -31,17 +32,18 @@\n \n REPO_ROOT = Path(__file__).resolve().parents[1]\n \n-# \"ADR 0016\" -- a four-digit citation. The trailing \\b keeps a five-digit token\n-# from being read as a truncated four-digit number.\n-_CITATION = re.compile(r\"\\bADR (\\d{4})\\b\")\n-# \"0016-locks-bound-product-paths.md\" -- the leading four digits name the record.\n-_ADR_FILENAME = re.compile(r\"^(\\d{4})-.*\\.md$\")\n+# ``ADR 0016`` / ``SADR-0016`` -- a namespace plus four-digit citation. The\n+# trailing ``\\b`` keeps a five-digit token from being read as a truncated one.\n+_CITATION = re.compile(r\"\\b(S?ADR)[ -](\\d{4})\\b\")\n+# Target ADRs retain the numeric filename; Shiki platform decisions use SADR.\n+_DECISION_FILENAME = re.compile(r\"^(?:(SADR)-)?(\\d{4})-.*\\.md$\")\n \n \n class Violation(NamedTuple):\n     \"\"\"A citation that does not resolve to exactly one decision record.\"\"\"\n \n     citing_file: str  # repo-relative path of the file that cites the ADR\n+    namespace: str  # \"ADR\" for target decisions; \"SADR\" for Shiki decisions\n     number: str  # the cited four-digit number, e.g. \"0016\"\n     line: int  # 1-based line of the citation\n     reason: str  # \"missing\" (zero matches) or \"ambiguous\" (two or more)\n@@ -49,44 +51,59 @@\n \n     @property\n     def expected_path(self) -> str:\n-        return f\"docs/adr/{self.number}-*.md\"\n+        prefix = \"SADR-\" if self.namespace == \"SADR\" else \"\"\n+        return f\"docs/adr/{prefix}{self.number}-*.md\"\n \n     def describe(self) -> str:\n         \"\"\"Name the citing file, the cited number, and the missing path.\"\"\"\n         if self.reason == \"missing\":\n             return (\n-                f\"{self.citing_file}:{self.line} cites ADR {self.number} \"\n+                f\"{self.citing_file}:{self.line} cites {self.namespace} {self.number} \"\n                 f\"but no document matches {self.expected_path}\"\n             )\n         joined = \", \".join(self.matches)\n         return (\n-            f\"{self.citing_file}:{self.line} cites ADR {self.number} which \"\n+            f\"{self.citing_file}:{self.line} cites {self.namespace} {self.number} which \"\n             f\"resolves to {len(self.matches)} documents (expected exactly one \"\n             f\"at {self.expected_path}): {joined}\"\n         )\n \n \n-def _adr_index(root: Path) -> dict[str, list[Path]]:\n-    \"\"\"Map every four-digit ADR number to the record files that claim it.\"\"\"\n-    index: dict[str, list[Path]] = {}\n+def _adr_index(root: Path) -> dict[tuple[str, str], list[Path]]:\n+    \"\"\"Map each decision namespace and number to the files that claim it.\"\"\"\n+    index: dict[tuple[str, str], list[Path]] = {}\n     adr_dir = root / \"docs\" / \"adr\"\n     if adr_dir.is_dir():\n         for path in sorted(adr_dir.glob(\"*.md\")):\n-            match = _ADR_FILENAME.match(path.name)\n+            match = _DECISION_FILENAME.match(path.name)\n             if match:\n-                index.setdefault(match.group(1), []).append(path)\n+                namespace = \"SADR\" if match.group(1) else \"ADR\"\n+                index.setdefault((namespace, match.group(2)), []).append(path)\n     return index\n \n \n def _citing_files(root: Path, include_docs: bool = True) -> list[Path]:\n-    \"\"\"Files scanned for citations: scripts/*.py, and cheaply the markdown.\"\"\"\n+    \"\"\"Current, shipped text surfaces scanned for decision citations.\"\"\"\n     files = sorted((root / \"scripts\").glob(\"*.py\"))\n+    files += sorted((root / \"scripts\").glob(\"*.sh\"))\n     if include_docs:\n         docs = root / \"docs\"\n         if docs.is_dir():\n             files += sorted(docs.rglob(\"*.md\"))\n         files += sorted(root.glob(\"*.md\"))\n-    return files\n+        pyproject = root / \"pyproject.toml\"\n+        if pyproject.is_file():\n+            files.append(pyproject)\n+        for relative in (\".github\", \".codex\"):\n+            surface = root / relative\n+            if surface.is_dir():\n+                files += sorted(\n+                    path\n+                    for path in surface.rglob(\"*\")\n+                    if path.is_file()\n+                    and path.suffix in {\".md\", \".py\", \".sh\", \".toml\", \".yaml\", \".yml\"}\n+                )\n+    return sorted(set(files))\n \n \n def find_adr_reference_violations(\n@@ -94,10 +111,9 @@\n ) -> list[Violation]:\n     \"\"\"Return every ``ADR ####`` citation under ``root`` that fails to resolve.\n \n-    A citation resolves when exactly one ``docs/adr/####-*.md`` matches its\n-    number. Zero matches is a \"missing\" violation; two or more is \"ambiguous\".\n-    Records that are never cited produce nothing -- the converse is not\n-    required.\n+    A citation resolves when exactly one record in the same namespace matches\n+    its number. Zero matches is a \"missing\" violation; two or more is\n+    \"ambiguous\". Records that are never cited produce nothing.\n     \"\"\"\n     root = Path(root)\n     index = _adr_index(root)\n@@ -110,13 +126,15 @@\n         rel = path.relative_to(root).as_posix()\n         for lineno, line in enumerate(text.splitlines(), start=1):\n             for match in _CITATION.finditer(line):\n-                number = match.group(1)\n-                found = index.get(number, [])\n+                namespace, number = match.groups()\n+                found = index.get((namespace, number), [])\n                 if len(found) == 1:\n                     continue\n                 reason = \"missing\" if not found else \"ambiguous\"\n                 rels = [p.relative_to(root).as_posix() for p in found]\n-                violations.append(Violation(rel, number, lineno, reason, rels))\n+                violations.append(\n+                    Violation(rel, namespace, number, lineno, reason, rels)\n+                )\n     return violations\n \n \n@@ -129,8 +147,8 @@\n         self.assertEqual(\n             violations,\n             [],\n-            \"Every 'ADR ####' citation must resolve to exactly one \"\n-            f\"docs/adr/####-*.md:\\n{detail}\",\n+            \"Every ADR/SADR citation must resolve exactly once in its own \"\n+            f\"docs/adr namespace:\\n{detail}\",\n         )\n \n     def test_scan_actually_reads_scripts(self) -> None:\n@@ -146,8 +164,21 @@\n         # The two records this Goal carried onto the default branch, and which\n         # the scripts cite most, must be present.\n         index = _adr_index(REPO_ROOT)\n-        self.assertIn(\"0016\", index)\n-        self.assertIn(\"0017\", index)\n+        self.assertIn((\"SADR\", \"0016\"), index)\n+        self.assertIn((\"SADR\", \"0017\"), index)\n+\n+    def test_platform_tree_has_only_sadr_0001_through_0019(self) -> None:\n+        adr_dir = REPO_ROOT / \"docs\" / \"adr\"\n+        sadrs = sorted(adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\"))\n+        self.assertEqual(\n+            {path.name.split(\"-\", 2)[1] for path in sadrs},\n+            {f\"{number:04d}\" for number in range(1, 20)},\n+        )\n+        self.assertEqual(\n+            list(adr_dir.glob(\"[0-9][0-9][0-9][0-9]-*.md\")),\n+            [],\n+            \"the Shiki platform tree must not retain legacy numeric SADR paths\",\n+        )\n \n \n class FixtureAdrReferences(unittest.TestCase):\n@@ -180,6 +211,26 @@\n         self.assertIn(\"9991\", message)  # cited number\n         self.assertIn(\"docs/adr/9991-*.md\", message)  # missing path\n \n+    def test_target_adr_does_not_satisfy_missing_platform_sadr(self) -> None:\n+        root = self._make_repo(\n+            adr_names=[\"0042-target-decision.md\"],\n+            script_body='\"\"\"Target ADR 0042 exists, but platform SADR-0042 does not.\"\"\"\\n',\n+        )\n+        violations = find_adr_reference_violations(root, include_docs=False)\n+        self.assertEqual(len(violations), 1)\n+        self.assertEqual(violations[0].namespace, \"SADR\")\n+        self.assertEqual(violations[0].number, \"0042\")\n+        self.assertEqual(violations[0].expected_path, \"docs/adr/SADR-0042-*.md\")\n+\n+    def test_target_adr_and_platform_sadr_may_reuse_a_number(self) -> None:\n+        root = self._make_repo(\n+            adr_names=[\"0042-target-decision.md\", \"SADR-0042-platform-decision.md\"],\n+            script_body='\"\"\"Target ADR 0042 and platform SADR-0042 are distinct.\"\"\"\\n',\n+        )\n+        self.assertEqual(\n+            find_adr_reference_violations(root, include_docs=False), []\n+        )\n+\n     def test_uncited_adr_is_allowed(self) -> None:\n         # An ADR exists but nothing cites it -> not a violation. The converse\n         # (every ADR must be cited) is explicitly NOT required.\ndiff --git a/tests/test_policy_job_merged_prs.py b/tests/test_policy_job_merged_prs.py\nindex d45512e74681e5e649279236c7d4e79d5c3da471..b97989f8bd74d091de1a6b5275a20d8dc68c4512\n--- a/tests/test_policy_job_merged_prs.py\n+++ b/tests/test_policy_job_merged_prs.py\n@@ -3,8 +3,8 @@\n \n ``.github/workflows/shiki-cca-completion.yml`` has two consumers of the merged-PR\n proof: the CCA *signal* (``guardian_approval_signal.py`` in \"Prepare CCA context\")\n-and the authoritative gate (``mergegate_check.py`` in \"Enforce MergeGate\"). ADR\n-0017's bookkeeping-closeout exemption (condition 6) needs the merged implementation\n+and the authoritative gate (``mergegate_check.py`` in \"Enforce MergeGate\").\n+SADR-0017's bookkeeping-closeout exemption (condition 6) needs the merged implementation\n PR proven, and a closeout carries NO ``post_merge_reconcile`` marker. The signal\n already resolves ``MERGED_PRS`` unconditionally, but \"Enforce MergeGate\" used to\n resolve it only inside ``grep -q '<!-- shiki:post_merge_reconcile -->'``. So a\ndiff --git a/tests/test_sadr_install_migration.py b/tests/test_sadr_install_migration.py\nnew file mode 100644\nindex 0000000000000000000000000000000000000000..5a442fa5e4968ade178dcdedc3c63b54dba580a2\n--- /dev/null\n+++ b/tests/test_sadr_install_migration.py\n@@ -0,0 +1,224 @@\n+\"\"\"SADR installation and collision-safe legacy upgrade behavior.\"\"\"\n+\n+from __future__ import annotations\n+\n+import contextlib\n+import hashlib\n+import io\n+import json\n+import shutil\n+import tempfile\n+import unittest\n+from pathlib import Path\n+\n+import shiki_test_support  # noqa: F401  (path bootstrap)\n+\n+import shiki_installer\n+from tests.test_adr_references import find_adr_reference_violations\n+\n+\n+PRODUCT_NUMBERS = tuple(range(13, 19))\n+\n+\n+def _quiet_install(target: Path, *, force: bool, validate: bool) -> None:\n+    with contextlib.redirect_stdout(io.StringIO()):\n+        shiki_installer.install_template(target, force=force, validate=validate)\n+\n+\n+def _sha256(path: Path) -> str:\n+    return hashlib.sha256(path.read_bytes()).hexdigest()\n+\n+\n+def _legacy_sadr_paths() -> tuple[str, ...]:\n+    return tuple(\n+        f\"docs/adr/{path.name.removeprefix('SADR-')}\"\n+        for path in sorted((shiki_installer.ROOT / \"docs\" / \"adr\").glob(\"SADR-*.md\"))\n+        if path.name != \"SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\"\n+    )\n+\n+\n+def _create_legacy_upgrade_fixture(\n+    target: Path,\n+) -> tuple[dict[str, bytes], tuple[str, ...]]:\n+    adr_dir = target / \"docs\" / \"adr\"\n+    adr_dir.mkdir(parents=True)\n+    product_bytes = {\n+        f\"{number:04d}-cubeshare-product-decision.md\": (\n+            f\"# CubeShare ADR {number:04d}\\n\\nDo not rewrite {number}.\\n\"\n+        ).encode()\n+        for number in PRODUCT_NUMBERS\n+    }\n+    for name, content in product_bytes.items():\n+        (adr_dir / name).write_bytes(content)\n+\n+    legacy_paths = _legacy_sadr_paths()\n+    for relative in legacy_paths:\n+        (target / relative).write_bytes(f\"legacy Shiki bytes for {relative}\\n\".encode())\n+\n+    stamp_path = target / shiki_installer.INSTALL_STAMP_PATH\n+    stamp_path.parent.mkdir(parents=True, exist_ok=True)\n+    stamp = {\n+        \"version\": shiki_installer.INSTALL_STAMP_VERSION,\n+        \"platform_commit\": None,\n+        \"installed_at\": \"2000-01-01T00:00:00+00:00\",\n+        \"digests\": {\n+            relative: _sha256(target / relative) for relative in legacy_paths\n+        },\n+    }\n+    stamp_path.write_text(\n+        json.dumps(stamp, indent=2, sort_keys=True) + \"\\n\", encoding=\"utf-8\"\n+    )\n+    return product_bytes, legacy_paths\n+\n+\n+def _tree_snapshot(root: Path) -> dict[str, tuple[str, bytes | str]]:\n+    \"\"\"Recursive byte/shape snapshot used to prove preflight is write-free.\"\"\"\n+    snapshot: dict[str, tuple[str, bytes | str]] = {}\n+    for path in sorted(root.rglob(\"*\")):\n+        relative = path.relative_to(root).as_posix()\n+        if path.is_symlink():\n+            snapshot[relative] = (\"symlink\", path.readlink().as_posix())\n+        elif path.is_dir():\n+            snapshot[relative] = (\"directory\", \"\")\n+        else:\n+            snapshot[relative] = (\"file\", path.read_bytes())\n+    return snapshot\n+\n+\n+class FreshSadrInstallTests(unittest.TestCase):\n+    def setUp(self) -> None:\n+        self.tmp = Path(tempfile.mkdtemp(prefix=\"shiki-sadr-fresh-\"))\n+        self.addCleanup(lambda: shutil.rmtree(self.tmp, ignore_errors=True))\n+        self.target = self.tmp / \"target\"\n+        self.adr_dir = self.target / \"docs\" / \"adr\"\n+        self.adr_dir.mkdir(parents=True)\n+        self.product_bytes = {\n+            f\"{number:04d}-product-decision.md\": (\n+                f\"# Product ADR {number:04d}\\n\\nTarget-owned bytes {number}.\\n\"\n+            ).encode()\n+            for number in PRODUCT_NUMBERS\n+        }\n+        for name, content in self.product_bytes.items():\n+            (self.adr_dir / name).write_bytes(content)\n+\n+    def test_fresh_install_preserves_product_adrs_and_installs_all_sadrs(self) -> None:\n+        _quiet_install(self.target, force=False, validate=True)\n+\n+        for name, expected in self.product_bytes.items():\n+            self.assertEqual((self.adr_dir / name).read_bytes(), expected)\n+\n+        sadr_numbers = {\n+            path.name.split(\"-\", 2)[1]\n+            for path in self.adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\")\n+        }\n+        self.assertEqual(sadr_numbers, {f\"{number:04d}\" for number in range(1, 20)})\n+\n+        numeric_records = {\n+            path.name\n+            for path in self.adr_dir.glob(\"[0-9][0-9][0-9][0-9]-*.md\")\n+        }\n+        self.assertEqual(numeric_records, set(self.product_bytes))\n+        self.assertEqual(find_adr_reference_violations(self.target), [])\n+\n+\n+class LegacySadrUpgradeTests(unittest.TestCase):\n+    def setUp(self) -> None:\n+        self.tmp = Path(tempfile.mkdtemp(prefix=\"shiki-sadr-upgrade-\"))\n+        self.addCleanup(lambda: shutil.rmtree(self.tmp, ignore_errors=True))\n+        self.target = self.tmp / \"target\"\n+        self.adr_dir = self.target / \"docs\" / \"adr\"\n+        self.product_bytes, self.legacy_paths = _create_legacy_upgrade_fixture(\n+            self.target\n+        )\n+\n+    def test_force_upgrade_deletes_only_stamped_legacy_sadrs(self) -> None:\n+        _quiet_install(self.target, force=True, validate=True)\n+\n+        for relative in self.legacy_paths:\n+            self.assertFalse((self.target / relative).exists(), relative)\n+        for name, expected in self.product_bytes.items():\n+            self.assertEqual((self.adr_dir / name).read_bytes(), expected)\n+\n+        sadrs = list(self.adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\"))\n+        self.assertEqual(len(sadrs), 19)\n+        refreshed = json.loads(\n+            (self.target / shiki_installer.INSTALL_STAMP_PATH).read_text(encoding=\"utf-8\")\n+        )\n+        for relative in self.legacy_paths:\n+            self.assertNotIn(relative, refreshed[\"digests\"])\n+        for path in sadrs:\n+            relative = path.relative_to(self.target).as_posix()\n+            self.assertEqual(refreshed[\"digests\"][relative], _sha256(path))\n+\n+    def test_non_force_install_never_deletes_legacy_files(self) -> None:\n+        before = {\n+            relative: (self.target / relative).read_bytes()\n+            for relative in self.legacy_paths\n+        }\n+        _quiet_install(self.target, force=False, validate=False)\n+        for relative, expected in before.items():\n+            self.assertEqual((self.target / relative).read_bytes(), expected)\n+\n+\n+class LegacyCleanupPreflightTests(unittest.TestCase):\n+    def setUp(self) -> None:\n+        self.tmp = Path(tempfile.mkdtemp(prefix=\"shiki-sadr-preflight-\"))\n+        self.addCleanup(lambda: shutil.rmtree(self.tmp, ignore_errors=True))\n+\n+    def test_all_ownership_blockers_abort_before_any_write(self) -> None:\n+        cases = (\n+            (\"absent\", \"install stamp is absent\"),\n+            (\"unreadable\", \"install stamp is unreadable\"),\n+            (\"malformed\", \"install stamp is malformed\"),\n+            (\"missing-digest\", \"install stamp has no digest\"),\n+            (\"digest-mismatch\", \"digest mismatch\"),\n+        )\n+        for name, expected_reason in cases:\n+            with self.subTest(name=name):\n+                target = self.tmp / name\n+                _, legacy_paths = _create_legacy_upgrade_fixture(target)\n+                stamp_path = target / shiki_installer.INSTALL_STAMP_PATH\n+\n+                if name == \"absent\":\n+                    stamp_path.unlink()\n+                elif name == \"unreadable\":\n+                    stamp_path.unlink()\n+                    stamp_path.mkdir()\n+                elif name == \"malformed\":\n+                    stamp_path.write_text(\"{not-json\\n\", encoding=\"utf-8\")\n+                else:\n+                    stamp = json.loads(stamp_path.read_text(encoding=\"utf-8\"))\n+                    if name == \"missing-digest\":\n+                        stamp[\"digests\"] = {}\n+                    else:\n+                        stamp[\"digests\"] = {\n+                            relative: \"deadbeef\" for relative in legacy_paths\n+                        }\n+                    stamp_path.write_text(\n+                        json.dumps(stamp, indent=2, sort_keys=True) + \"\\n\",\n+                        encoding=\"utf-8\",\n+                    )\n+\n+                before = _tree_snapshot(target)\n+                with self.assertRaises(shiki_installer.ShikiError) as raised:\n+                    _quiet_install(target, force=True, validate=False)\n+                after = _tree_snapshot(target)\n+\n+                self.assertEqual(before, after, f\"{name} preflight changed the tree\")\n+                message = str(raised.exception)\n+                self.assertIn(expected_reason, message)\n+                self.assertIn(\"No files were written\", message)\n+                for relative in legacy_paths:\n+                    self.assertIn(relative, message)\n+\n+    def test_absent_stamp_is_allowed_when_no_legacy_path_exists(self) -> None:\n+        target = self.tmp / \"no-legacy\"\n+        target.mkdir()\n+        _quiet_install(target, force=True, validate=False)\n+        self.assertTrue(\n+            (target / \"docs\" / \"adr\" / \"SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\").is_file()\n+        )\n+\n+\n+if __name__ == \"__main__\":\n+    unittest.main()\n\ndiff --git a/.codex/skills/shiki/SKILL.md b/.codex/skills/shiki/SKILL.md\nindex c8e2e85d9d75d49cc4f2d6286ee22659c4e53e75..93cded623b2c15d9f65c76491a1c2d813ac9e6c0\n--- a/.codex/skills/shiki/SKILL.md\n+++ b/.codex/skills/shiki/SKILL.md\n@@ -18,6 +18,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n Codex CLI does not register this skill as a custom `/shiki` slash command. When\n the user types `/shiki` in Codex CLI and gets \"Unrecognized command\", explain\n that this is expected: invoke Shiki in Codex with natural language such as\n@@ -69,7 +72,7 @@\n \n ## Responsibilities\n \n-- Claude Code implements and repairs by default (`shiki runner claude`, ADR 0008); Codex implements and repairs tasks explicitly assigned to `codex`.\n+- Claude Code implements and repairs by default (`shiki runner claude`, SADR-0008); Codex implements and repairs tasks explicitly assigned to `codex`.\n - Claude Code Action can act as GitHub-side CCA or reviewer.\n - CCA judges completion.\n - MergeGate authorizes state transitions and merge readiness.\ndiff --git a/.github/prompts/cca-completion-check.md b/.github/prompts/cca-completion-check.md\nindex ed203ad5f302ecc81372d8ab27d2b1232ca97497..fd89950528333ef6242e6d5443904f5392ab7e56\n--- a/.github/prompts/cca-completion-check.md\n+++ b/.github/prompts/cca-completion-check.md\n@@ -19,6 +19,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n ## Required Reading\n \n Read only the files needed to judge the PR contract. Prefer this order:\ndiff --git a/.github/prompts/codex-task-handoff.md b/.github/prompts/codex-task-handoff.md\nindex 695e211d76dea1c60e8d5bb360e78487808286a4..59441d576b01a39569722f226c854e5a740e9eb5\n--- a/.github/prompts/codex-task-handoff.md\n+++ b/.github/prompts/codex-task-handoff.md\n@@ -2,7 +2,7 @@\n \n You are Codex Front acting as the Implementer for a Shiki task.\n \n-Follow `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant ADRs, and `docs/agents/codex-handoff.md`.\n+Follow `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant Shiki SADRs and target ADRs, and `docs/agents/codex-handoff.md`.\n \n Rules:\n \ndiff --git a/AGENTS.md b/AGENTS.md\nindex 7f041100d49c2a7525f51cb0f5d696a4a6d1c78c..ea303398018461999e03b6f27be3f4d65833fa32\n--- a/AGENTS.md\n+++ b/AGENTS.md\n@@ -44,10 +44,14 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use\n+`SADR-NNNN-*.md`; target product decisions retain `NNNN-*.md` and the `ADR`\n+namespace.\n+\n Use the canonical source-of-truth order above. Runtime-specific reading orders\n are operational guidance only and must not override or reorder it.\n \n-Conversation state is not durable truth. If a decision matters, put it in GitHub, `.shiki/`, `CONTEXT.md`, or an ADR.\n+Conversation state is not durable truth. If a decision matters, put it in GitHub, `.shiki/`, `CONTEXT.md`, a Shiki SADR, or a target ADR.\n \n If GitHub and `.shiki/` disagree, surface the conflict and prefer GitHub operational state until the mirror is repaired.\n \n@@ -75,8 +79,8 @@\n Every non-trivial change follows this loop:\n \n 1. **Goal**: clarify outcome, completion conditions, non-goals, risk level, and success signals.\n-2. **grill-with-docs**: challenge the plan against domain docs, ADRs, code reality, terminology, and edge scenarios.\n-3. **Context & Impact**: find relevant docs, ADRs, code, dependencies, owners, locks, and verification surfaces.\n+2. **grill-with-docs**: challenge the plan against domain docs, Shiki SADRs, target ADRs, code reality, terminology, and edge scenarios.\n+3. **Context & Impact**: find relevant docs, SADRs, target ADRs, code, dependencies, owners, locks, and verification surfaces.\n 4. **PRD and Spec Freeze**: use `to-prd` when context is settled enough to become durable product/engineering intent. The operator's explicit approval of the PRD is Spec Freeze: the plan records a `spec_freeze` block, and no plan runs without it. Steps 1-4 form Requirements Definition \u2014 one continuous operator dialogue. After Spec Freeze, scope changes require an operator-approved Spec Amendment; non-scope-moving interpretations go to the Assumption Log.\n 5. **Issues**: use `to-issues` to create independently grabbable vertical-slice issues. Prefer AFK slices over HITL slices where possible.\n 6. **Triage**: label issues for readiness, risk, runtime, skills, and MergeGate state.\n@@ -167,7 +171,7 @@\n - Testing decisions.\n - Out-of-scope items.\n - Further notes.\n-- Links to relevant domain terms and ADRs.\n+- Links to relevant domain terms, SADRs, and target ADRs.\n \n Every executable task must include:\n \n@@ -193,7 +197,7 @@\n \n Context & Impact must identify:\n \n-- Relevant documents, domain terms, ADRs, and past decisions.\n+- Relevant documents, domain terms, SADRs, target ADRs, and past decisions.\n - Relevant modules, interfaces, seams, callers, owners, and tests.\n - Dependency relationships between tasks.\n - Candidate locks and lock conflicts.\n@@ -210,7 +214,7 @@\n | Skill | Required when |\n | --- | --- |\n | `setup-matt-pocock-skills` | First configuring the repo for skills, or when issue tracker, triage labels, or domain docs are missing. |\n-| `grill-with-docs` | Default for non-trivial Goals before PRD/issues; always required when plans, terminology, boundaries, tradeoffs, or ADR-worthy decisions are ambiguous. |\n+| `grill-with-docs` | Default for non-trivial Goals before PRD/issues; always required when plans, terminology, boundaries, tradeoffs, or decision-record-worthy choices are ambiguous. |\n | `zoom-out` | The agent lacks an architectural map of the relevant code area. |\n | `to-prd` | Settled Goal context must become durable product/engineering intent. |\n | `to-issues` | A Goal, PRD, or plan must become independently grabbable vertical-slice issues. |\n@@ -348,7 +352,7 @@\n - Three or more new files.\n - Any high-risk or critical label.\n \n-When the architecture gate triggers, run `grill-with-docs` or `improve-codebase-architecture` as appropriate and record durable decisions in an ADR when needed.\n+When the architecture gate triggers, run `grill-with-docs` or `improve-codebase-architecture` as appropriate and record durable decisions in a Shiki SADR or target ADR as applicable.\n \n ## Engineering Discipline\n \ndiff --git a/CLAUDE.md b/CLAUDE.md\nindex 98a932e7841a632866fcd53b0bad0e3af3711d3d..98a31399404d6fb044b4fd493f4459ac87f439de\n--- a/CLAUDE.md\n+++ b/CLAUDE.md\n@@ -14,6 +14,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n ## Claude Role\n \n Default to planner, default implementer, reviewer, coordinator, documentation editor, CCA-style completion judge, and final-judgment assistant.\n@@ -57,7 +60,7 @@\n At the start of a Shiki session:\n \n 1. Read `AGENTS.md`.\n-2. Read `CONTEXT.md` and relevant ADRs.\n+2. Read `CONTEXT.md` and relevant Shiki SADRs and target ADRs.\n 3. Read active `.shiki/` Goal/task/ledger state when present.\n 4. Read active distilled rules from `.shiki/memories/` (status `distilled`, `active: true`, not revoked or superseded) for applicable guidance; the same rules are injected deterministically into task handoffs as the `## Distilled Rules` section (\u00a73.5).\n 5. Read the GitHub Issue or PR when the task is GitHub-backed.\n@@ -74,7 +77,7 @@\n - Define the Goal, completion conditions, non-goals, and risk level.\n - Treat `grill-with-docs` as the default planning entry point for non-trivial Goals.\n - Challenge domain terms against `CONTEXT.md`.\n-- Challenge hard-to-reverse tradeoffs against ADRs.\n+- Challenge hard-to-reverse tradeoffs against SADRs and target ADRs.\n - Explore code instead of asking when code can answer the question.\n - Run Context & Impact before implementation planning.\n - Produce a Task DAG, not an unordered checklist.\n@@ -94,7 +97,7 @@\n - For each question, provide your recommended answer.\n - Resolve dependency decisions before downstream design questions.\n - Update `CONTEXT.md` inline when terminology crystallizes.\n-- Offer ADRs only when the decision is hard to reverse, surprising without context, and a real tradeoff.\n+- Offer a SADR or target ADR only when the decision is hard to reverse, surprising without context, and a real tradeoff.\n - Convert unresolved questions into HITL blockers rather than allowing Codex to guess.\n \n ## PRD and Issues Standard\n@@ -103,7 +106,7 @@\n \n - Use the project's domain glossary vocabulary.\n - Avoid volatile file paths unless they are essential.\n-- Include implementation decisions, testing decisions, out-of-scope items, and relevant ADR/domain links.\n+- Include implementation decisions, testing decisions, out-of-scope items, and relevant SADR/target-ADR/domain links.\n - Identify deep-module opportunities and testable interfaces.\n \n When creating issues:\n@@ -121,7 +124,7 @@\n - Branch/worktree target.\n - Scope and non-goals.\n - Dependencies and locks.\n-- Relevant docs, ADRs, modules, and tests.\n+- Relevant docs, SADRs, target ADRs, modules, and tests.\n - Required skill invocations.\n - TDD expectations.\n - Pre-PR `code-review` gate expectations.\n@@ -152,7 +155,7 @@\n \n - Treat yourself as a judge, not implementer.\n - Do not write production code.\n-- Read `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant ADRs, PRD, task issue, PR body, diff, checks, reviews, labels, and `.shiki/` state.\n+- Read `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant SADRs and target ADRs, PRD, task issue, PR body, diff, checks, reviews, labels, and `.shiki/` state.\n - Evaluate every applicable checklist in `docs/agents/checklists.md`.\n - Map each acceptance criterion to durable evidence.\n - Distinguish implementation failure from missing evidence.\ndiff --git a/CONTEXT.md b/CONTEXT.md\nindex e61a9be06a0cce0d026484083a657bde2067e234..237ee8aa3ea8fab334ece49768bc5ed3b5af0974\n--- a/CONTEXT.md\n+++ b/CONTEXT.md\n@@ -25,7 +25,7 @@\n _Avoid_: jumping directly into code\n \n **grill-with-docs**:\n-A planning skill that challenges a plan against domain language, ADRs, code reality, and concrete edge scenarios. It resolves design-tree questions before PRD/issues. It runs inside Requirements Definition.\n+A planning skill that challenges a plan against domain language, Shiki SADRs, target ADRs, code reality, and concrete edge scenarios. It resolves design-tree questions before PRD/issues. It runs inside Requirements Definition.\n _Avoid_: generic brainstorming, silent assumptions\n \n **Requirements Definition**:\ndiff --git a/CONTRIBUTING.md b/CONTRIBUTING.md\nindex c6d9b656e73abb8a55e5f660123e03153dcb3e6d..9e37a8fbf668db5b22461aaae5548b8f1354394c\n--- a/CONTRIBUTING.md\n+++ b/CONTRIBUTING.md\n@@ -17,7 +17,7 @@\n 1. **Goal** \u2014 start from a user-approved Goal with an outcome, completion\n    conditions, non-goals, risk level, and success signals.\n 2. **grill-with-docs** \u2014 for any non-trivial change, challenge the plan against\n-   domain language, ADRs, and code reality before writing a PRD.\n+   domain language, Shiki SADRs, target ADRs, and code reality before writing a PRD.\n 3. **PRD** \u2014 capture settled product/engineering intent (`to-prd`).\n 4. **Spec Freeze** \u2014 the operator explicitly approves the PRD/requirements;\n    the plan records a `spec_freeze` block and no plan runs without it\n@@ -94,7 +94,7 @@\n   entries.\n - Do not hand-edit `.shiki/goals/*.json` or `.shiki/manifest.json` in a task PR.\n - Conversation state is not durable truth. If a decision matters, record it in\n-  GitHub, `.shiki/`, `CONTEXT.md`, or an ADR.\n+  GitHub, `.shiki/`, `CONTEXT.md`, a Shiki SADR, or a target ADR.\n \n ## Architecture Gate\n \n@@ -103,7 +103,7 @@\n or CI policy, package structure, three or more structural files, more than 100\n net new lines in a core module, three or more new files, or any high-risk label.\n When triggered, run `grill-with-docs` or `improve-codebase-architecture` and\n-record durable decisions in an ADR when needed (see `docs/adr/`).\n+record durable decisions in a Shiki SADR or target ADR as applicable (see `docs/adr/`).\n \n ## Safety\n \ndiff --git a/SYSTEM_PROMPT.md b/SYSTEM_PROMPT.md\nindex aaaea45e3a896c3a44ba89eec8bb84325ecf6c62..61b3c55afe7a5fc5f1a3d475378175db58553c6d\n--- a/SYSTEM_PROMPT.md\n+++ b/SYSTEM_PROMPT.md\n@@ -30,6 +30,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n Use the canonical source-of-truth order above. Runtime-specific reading orders\n are operational guidance only and must not override or reorder it. The current\n conversation is non-durable operator input.\n@@ -55,7 +58,7 @@\n For any non-trivial Goal, use this flow:\n \n 1. **Goal Seek**: define outcome, non-goals, risk level, completion criteria, and evidence requirements.\n-2. **grill-with-docs**: challenge the plan against `CONTEXT.md`, ADRs, code reality, terminology, boundaries, and edge scenarios. Ask one question at a time when the operator is available. If code can answer a question, inspect code instead of asking.\n+2. **grill-with-docs**: challenge the plan against `CONTEXT.md`, Shiki SADRs, target ADRs, code reality, terminology, boundaries, and edge scenarios. Ask one question at a time when the operator is available. If code can answer a question, inspect code instead of asking.\n 3. **Context & Impact**: identify modules, interfaces, seams, callers, dependencies, locks, risk, and verification surfaces.\n 4. **to-prd + Spec Freeze**: turn settled context into a PRD using domain vocabulary and testing decisions. The operator's explicit approval of the PRD is Spec Freeze, recorded as the plan's `spec_freeze` block; plans without it do not run. Steps 1-4 form Requirements Definition.\n 5. **to-issues**: decompose the PRD into independently grabbable vertical-slice issues. Prefer AFK slices; mark HITL slices when judgment is still required.\n@@ -75,7 +78,7 @@\n Before making any material change:\n \n 1. Read `AGENTS.md` and any runtime-specific wrapper such as `CLAUDE.md`.\n-2. Read `CONTEXT.md` and relevant ADRs.\n+2. Read `CONTEXT.md` and relevant SADRs and target ADRs.\n 3. Read the GitHub Issue / PR and `.shiki/` state for the active Goal or task.\n 4. Identify your role: Planner, Implementer, Reviewer, Completion Check Agent, Repairer, or Guardian-assist.\n 5. Identify Goal id, task id, branch, PR, dependencies, locks, risk level, required skills, and required evidence.\n@@ -103,7 +106,7 @@\n \n At minimum, identify:\n \n-- Relevant documents, ADRs, domain terms, and prior decisions.\n+- Relevant documents, SADRs, target ADRs, domain terms, and prior decisions.\n - Relevant modules, interfaces, seams, callers, owners, and tests.\n - Dependency relationships between tasks.\n - Candidate file locks and lock conflicts.\n@@ -152,7 +155,7 @@\n \n CCA must read:\n \n-- `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant ADRs.\n+- `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant SADRs and target ADRs.\n - Parent Goal issue, PRD issue, task issue, comments, and labels.\n - PR body, diff, commits, changed files, reviews, CI checks, and artifacts available to the workflow.\n - `.shiki/` task contract, locks, ledger entries, CCA checklist profile, and prior repair packets.\n@@ -214,7 +217,7 @@\n - Three or more new files.\n - Any high-risk or critical label.\n \n-When the architecture gate triggers, run `grill-with-docs` or `improve-codebase-architecture` as appropriate and record the decision in an ADR if it is hard to reverse or likely to be re-litigated.\n+When the architecture gate triggers, run `grill-with-docs` or `improve-codebase-architecture` as appropriate and record the decision in a Shiki SADR or target ADR as applicable if it is hard to reverse or likely to be re-litigated.\n \n ## 14. Skill Gate\n \n@@ -223,7 +226,7 @@\n Use these skills:\n \n - `setup-matt-pocock-skills`: before first use of the engineering skill set, or when issue tracker, triage labels, or domain docs are missing.\n-- `grill-with-docs`: default for non-trivial Goals before PRD/issues; required for ambiguous plans, terminology, boundaries, ADR-worthy tradeoffs, or design-tree decisions.\n+- `grill-with-docs`: default for non-trivial Goals before PRD/issues; required for ambiguous plans, terminology, boundaries, decision-record-worthy tradeoffs, or design-tree decisions.\n - `zoom-out`: unfamiliar code area or missing architectural map.\n - `to-prd`: convert settled context into a PRD.\n - `to-issues`: break a PRD, plan, or Goal into independently grabbable vertical-slice issues.\ndiff --git a/docs/adr/0001-keep-shiki-as-platform-name.md b/docs/adr/SADR-0001-keep-shiki-as-platform-name.md\nindex 20a9637c3b9d8c97de7ba8db7cfae4d82d9a7dba..8b685439959aada1b221c112f0ac3c51ca8a1695\n--- a/docs/adr/0001-keep-shiki-as-platform-name.md\n+++ b/docs/adr/SADR-0001-keep-shiki-as-platform-name.md\n@@ -1,3 +1,3 @@\n-# Keep Shiki As The Platform Name\n+# SADR-0001: Keep Shiki As The Platform Name\n \n Shiki remains the product name for the GitHub-native agentic engineering control plane. We considered more descriptive names such as MergePilot, MergeGate, and Kiroku, but kept Shiki because it carries the existing goal-seeking framework identity while remaining independent of Hermes, Codex, Claude Code, or any single runtime.\ndiff --git a/docs/adr/0002-use-agents-as-shared-constitution.md b/docs/adr/SADR-0002-use-agents-as-shared-constitution.md\nindex 73de30ecb078120eac540d9bc51855e3cd48d0e3..c5486182bf06fca36ea508496b013c67302fd989\n--- a/docs/adr/0002-use-agents-as-shared-constitution.md\n+++ b/docs/adr/SADR-0002-use-agents-as-shared-constitution.md\n@@ -1,3 +1,3 @@\n-# Use AGENTS As The Shared Agent Constitution\n+# SADR-0002: Use AGENTS As The Shared Agent Constitution\n \n Shiki uses `AGENTS.md` as the runtime-neutral constitution and `CLAUDE.md` as a thin Claude Code wrapper. This keeps Codex, Claude Code, Hermes Runner, and future Agent Runtimes aligned on the same GitHub-first MergeGate rules while still allowing runtime-specific instructions where needed.\ndiff --git a/docs/adr/0003-require-skill-gate-for-mergegate-readiness.md b/docs/adr/SADR-0003-require-skill-gate-for-mergegate-readiness.md\nindex 424be0c9af4c8f0136f1fde68a468c47ff84e764..7fc337987c7df9792f75f181dfcbed75fe5ffff2\n--- a/docs/adr/0003-require-skill-gate-for-mergegate-readiness.md\n+++ b/docs/adr/SADR-0003-require-skill-gate-for-mergegate-readiness.md\n@@ -1,3 +1,3 @@\n-# Require Skill Gate For MergeGate Readiness\n+# SADR-0003: Require Skill Gate For MergeGate Readiness\n \n Shiki requires relevant engineering skills to be invoked before execution when their triggers apply. This makes skills part of MergeGate readiness rather than optional prompt style, because Shiki depends on repeatable planning, issue decomposition, TDD, diagnosis, triage, and architecture review before autonomous or parallel agent work can be trusted.\ndiff --git a/docs/adr/0004-use-github-first-with-shiki-mirror.md b/docs/adr/SADR-0004-use-github-first-with-shiki-mirror.md\nindex d0a3a9174dc646a945a9d325954ffaf00c352040..a79c54ae9ee8eed2a0524192b864da4e22a92d3f\n--- a/docs/adr/0004-use-github-first-with-shiki-mirror.md\n+++ b/docs/adr/SADR-0004-use-github-first-with-shiki-mirror.md\n@@ -1,4 +1,4 @@\n-# ADR 0004: Use GitHub First With A Repository-Local Shiki Mirror\n+# SADR-0004: Use GitHub First With A Repository-Local Shiki Mirror\n \n ## Status\n \ndiff --git a/docs/adr/0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md b/docs/adr/SADR-0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\nindex b339baf49b89d9290e79c3d51b36e7b2bde3bd95..c30a0d8b1fde0224d0ac7820e7870dccf721580d\n--- a/docs/adr/0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\n+++ b/docs/adr/SADR-0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\n@@ -1,4 +1,4 @@\n-# ADR 0005: Use Codex As Front And Claude Code Action As The GitHub Runtime\n+# SADR-0005: Use Codex As Front And Claude Code Action As The GitHub Runtime\n \n ## Status\n \ndiff --git a/docs/adr/0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md b/docs/adr/SADR-0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\nindex 4bcae9d1e52f0a37b8e9fbafa5cd14b6ee1993bd..7a38538a6ae9929567f0ade67e7ea6a60d0b3f7e\n--- a/docs/adr/0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\n+++ b/docs/adr/SADR-0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\n@@ -1,4 +1,4 @@\n-# ADR 0006: Separate Codex Implementation, CCA Completion, And MergeGate Authorization\n+# SADR-0006: Separate Codex Implementation, CCA Completion, And MergeGate Authorization\n \n ## Status\n \ndiff --git a/docs/adr/0007-packaging-and-release.md b/docs/adr/SADR-0007-packaging-and-release.md\nindex 79b84ddb97167372b44641e5b7860e400ce80822..bb445a2e2e856d9d1f5fdbe35de8d4e8cec0fb32\n--- a/docs/adr/0007-packaging-and-release.md\n+++ b/docs/adr/SADR-0007-packaging-and-release.md\n@@ -1,4 +1,4 @@\n-# ADR 0007: Packaging And Release For Shiki\n+# SADR-0007: Packaging And Release For Shiki\n \n ## Status\n \n@@ -25,7 +25,7 @@\n   not introduce third-party runtime dependencies or break the shim.\n - Releases must run inside the operator's subscription-authenticated toolchain\n   and must not require extra secrets beyond the built-in `GITHUB_TOKEN`\n-  (consistent with ADR 0005's no-API-key-by-default stance).\n+  (consistent with SADR-0005's no-API-key-by-default stance).\n - `.shiki/manifest.json` already encodes the install surface (`install.include`,\n   `install.create_directories`, `install.exclude_from_commit`), and it must not\n   be mutated as part of this change.\n@@ -34,7 +34,7 @@\n \n This is a hard-to-reverse platform decision (it sets the version source of\n truth, the release trigger, and the public install contract), so it is recorded\n-as an ADR.\n+as a SADR.\n \n ## Decision\n \n@@ -104,7 +104,7 @@\n 3. **Publishing to PyPI.** Rejected for now: Shiki is distributed as a\n    repository template + git-installable CLI, not a PyPI library; PyPI would add\n    credentials/secrets and a different versioning contract. Can be added later as\n-   its own ADR if a packaged distribution is needed.\n+   its own SADR if a packaged distribution is needed.\n 4. **Dynamic version (read `VERSION` via a build backend `dynamic` field).**\n    Rejected to keep `pyproject.toml` backend-agnostic and dependency-free; the\n    small duplication between `VERSION` and `pyproject.toml` is guarded by the\n@@ -123,4 +123,4 @@\n   releasing versions the whole platform but ships only `install.include` to\n   targets.\n - Future PyPI distribution or a console entry point are deferred and would each\n-  warrant their own ADR.\n+  warrant their own SADR.\ndiff --git a/docs/adr/0008-use-claude-code-as-default-implementer.md b/docs/adr/SADR-0008-use-claude-code-as-default-implementer.md\nindex dedf4510b7c7799521422f622029563d373db031..35c2625d908167fbc58855b5b63e283805f167e8\n--- a/docs/adr/0008-use-claude-code-as-default-implementer.md\n+++ b/docs/adr/SADR-0008-use-claude-code-as-default-implementer.md\n@@ -1,12 +1,12 @@\n-# ADR 0008: Use Claude Code As The Default Implementer Runtime\n+# SADR-0008: Use Claude Code As The Default Implementer Runtime\n \n ## Status\n \n-Proposed (supersedes ADR 0005 for the implementer default; keeps its auth model)\n+Proposed (supersedes SADR-0005 for the implementer default; keeps its auth model)\n \n ## Context\n \n-ADR 0005 made Codex Front the default implementation runtime, with Claude Code\n+SADR-0005 made Codex Front the default implementation runtime, with Claude Code\n restricted to planner and reviewer roles by the runtime registry, and the only\n autonomous implementation adapter was `shiki runner codex`.\n \n@@ -18,13 +18,13 @@\n - The platform's planning, review, and CCA layers already run on Claude Code,\n   and Claude Code's native capabilities (the built-in `/code-review` skill and\n   the Workflow multi-agent orchestration tool) have no Codex equivalent.\n-- The Goal lifecycle redesign (ADR 0009) requires an autonomous post-freeze\n+- The Goal lifecycle redesign (SADR-0009) requires an autonomous post-freeze\n   loop driven from the operator's Claude Code session, including bounded\n   repair dispatch, which must not depend on a runtime that cannot be invoked\n   unattended.\n \n The constitution claims Shiki is runtime-agnostic (\"not a Claude-only\n-workflow\", ADR 0002). Any change to the implementer default must preserve the\n+workflow\", SADR-0002). Any change to the implementer default must preserve the\n registry-based runtime model rather than hardcoding one vendor.\n \n ## Decision\n@@ -39,7 +39,7 @@\n   the task to review or repair-needed.\n - Keep Codex registered as an optional implementer runtime. `shiki runner\n   codex`, the Codex handoff templates, and the ChatGPT OAuth auth model from\n-  ADR 0005 remain supported for tasks explicitly assigned to `codex`.\n+  SADR-0005 remain supported for tasks explicitly assigned to `codex`.\n - Commit to mandating two Claude-native capabilities as loop contract in\n   follow-up Goals (status: the code-review gate was delivered by Goal G-B \u2014\n   skill registry, checklists item PR-12, default required_skills \u2014 while the\n@@ -60,7 +60,7 @@\n \n Out of scope: removing Codex support, changing the CCA runtime (Claude Code\n Action with `CLAUDE_CODE_OAUTH_TOKEN` stays), and API-key based automation\n-(still requires its own ADR per ADR 0005).\n+(still requires its own target ADR per SADR-0005).\n \n ## Consequences\n \n@@ -100,7 +100,7 @@\n   harness rejection of direct `codex exec` is outside Shiki's control, and the\n   plan's mandatory native capabilities cannot run on Codex at all.\n - **Remove Codex entirely**: rejected; it abandons the runtime-agnostic\n-  constitution (ADR 0002) for no operational gain and forecloses\n+  constitution (SADR-0002) for no operational gain and forecloses\n   cross-runtime adversarial review.\n - **GitHub Actions cloud implementation via claude-code-action**: rejected as\n   default; durable evidence is automatic but token cost, Node 24 workflow\ndiff --git a/docs/adr/0009-requirements-definition-and-spec-freeze.md b/docs/adr/SADR-0009-requirements-definition-and-spec-freeze.md\nindex 29b3f7871bef9203a8b4a831a65c39eb2f506e42..52e7dd69ad0aa4efe0b68c62678abbf7df02aaec\n--- a/docs/adr/0009-requirements-definition-and-spec-freeze.md\n+++ b/docs/adr/SADR-0009-requirements-definition-and-spec-freeze.md\n@@ -1,4 +1,4 @@\n-# ADR 0009: Merge Grill And Planning Into Requirements Definition With Spec Freeze\n+# SADR-0009: Merge Grill And Planning Into Requirements Definition With Spec Freeze\n \n ## Status\n \ndiff --git a/docs/adr/0010-external-ai-guardian-review.md b/docs/adr/SADR-0010-external-ai-guardian-review.md\nindex e6108bbe8bb4e73e0ff4684c8f78001a80438c4e..f3510b145664298adafb151b6f0db51b972f01ec\n--- a/docs/adr/0010-external-ai-guardian-review.md\n+++ b/docs/adr/SADR-0010-external-ai-guardian-review.md\n@@ -1,4 +1,4 @@\n-# ADR 0010: External AI Guardian Review As A First-Class Approval Authority\n+# SADR-0010: External AI Guardian Review As A First-Class Approval Authority\n \n ## Status\n \n@@ -8,7 +8,7 @@\n \n Shiki is intended to be an autonomous, recursive, self-improving agent\n framework: authority-in-the-loop, not human-in-the-loop. Until now the Guardian\n-approval required for high/critical-risk merges (guardian-policy.json, ADR\n+approval required for high/critical-risk merges (guardian-policy.json, SADR\n governance) recognized only human authority sources \u2014 a GitHub review approval,\n the `guardian:approved` label applied by a configured Guardian, or a comment\n carrying `Guardian approval granted` plus the current head SHA.\n@@ -89,7 +89,7 @@\n   autonomous design the operator intends and produced the impersonation\n   incident.\n - **Record AI approval as an operator comment**: rejected as forgery of human\n-  authority \u2014 the exact failure this ADR corrects.\n+  authority \u2014 the exact failure this SADR corrects.\n - **Deliver the artifact as a committed `.shiki/` file**: rejected; a committed\n   approval inside the PR being approved is forgeable by anyone who can push.\n - **A dedicated reviewer bot identity instead of a relayed comment**: deferred;\ndiff --git a/docs/adr/0011-autonomous-quality-gate-evidence.md b/docs/adr/SADR-0011-autonomous-quality-gate-evidence.md\nindex bca0246c5d46e44afbde15486a75eec425fc02f5..9c93eac16d8c3a6d4b11bd7224e3955102cd7db4\n--- a/docs/adr/0011-autonomous-quality-gate-evidence.md\n+++ b/docs/adr/SADR-0011-autonomous-quality-gate-evidence.md\n@@ -1,4 +1,4 @@\n-# ADR 0011: Autonomous Quality-Gate Evidence \u2014 Deterministic Facts And An Independent Verifier\n+# SADR-0011: Autonomous Quality-Gate Evidence \u2014 Deterministic Facts And An Independent Verifier\n \n ## Status\n \ndiff --git a/docs/adr/0012-autonomous-loop-closeout-to-main.md b/docs/adr/SADR-0012-autonomous-loop-closeout-to-main.md\nindex 637fad6dde4989c9c2bf0f81f676d8d29991123c..ec7d85f81030a676bcbba28b43eb365422a36eac\n--- a/docs/adr/0012-autonomous-loop-closeout-to-main.md\n+++ b/docs/adr/SADR-0012-autonomous-loop-closeout-to-main.md\n@@ -1,4 +1,4 @@\n-# 0012 \u2014 Autonomous loop pushes goal completion to main via a closeout PR\n+# SADR-0012 \u2014 Autonomous loop pushes goal completion to main via a closeout PR\n \n - Status: accepted\n - Date: 2026-06-18\ndiff --git a/docs/adr/0013-bootstrap-grants-actions-approve-pull-requests.md b/docs/adr/SADR-0013-bootstrap-grants-actions-approve-pull-requests.md\nindex b6c69d64f1c5ced346b88d6859586715b3b5bd0e..cf48ff4a6716267f539ca58edd1a852e7c7a2d64\n--- a/docs/adr/0013-bootstrap-grants-actions-approve-pull-requests.md\n+++ b/docs/adr/SADR-0013-bootstrap-grants-actions-approve-pull-requests.md\n@@ -1,4 +1,4 @@\n-# ADR 0013: Bootstrap Grants GitHub Actions Permission To Create And Approve Pull Requests\n+# SADR-0013: Bootstrap Grants GitHub Actions Permission To Create And Approve Pull Requests\n \n ## Status\n \n@@ -31,7 +31,7 @@\n security tradeoff: it lets repository automation satisfy a human-review gate. In\n a multi-maintainer repo that could let a compromised or buggy workflow\n self-approve changes. So whether bootstrap turns it on by default \u2014 and with\n-what default token scope \u2014 is an ADR-worthy decision, not an implementation\n+what default token scope \u2014 is a SADR-worthy decision, not an implementation\n detail.\n \n ## Decision\n@@ -48,7 +48,7 @@\n Specifically:\n \n - **`can_approve_pull_request_reviews` defaults to `true`.** Shiki's default\n-  posture is solo/AFK autonomous operation (ADR 0008/0009/0012), where the CCA\n+  posture is solo/AFK autonomous operation (SADR-0008/0009/0012), where the CCA\n   Review Bridge is the only path that can satisfy `required_review: true` after\n   the CCA and Guardian gates have already passed. The default makes a\n   freshly-bootstrapped repo able to run the autonomous loop end to end without a\ndiff --git a/docs/adr/0014-codex-app-external-ai-guardian-ui-adapter.md b/docs/adr/SADR-0014-codex-app-external-ai-guardian-ui-adapter.md\nindex 238db06279334caa341c117362df9271382a7fcd..65f12e9939321e1b3e389a34c822fc1549e5155d\n--- a/docs/adr/0014-codex-app-external-ai-guardian-ui-adapter.md\n+++ b/docs/adr/SADR-0014-codex-app-external-ai-guardian-ui-adapter.md\n@@ -1,4 +1,4 @@\n-# ADR 0014: Use Codex App As The External AI Guardian UI Adapter\n+# SADR-0014: Use Codex App As The External AI Guardian UI Adapter\n \n ## Status\n \ndiff --git a/docs/adr/0015-contract-approval-before-dispatch.md b/docs/adr/SADR-0015-contract-approval-before-dispatch.md\nindex fad6c656741a20a7684ba16080a7db159f63a52f..6061271a46d3f1d6ae7174477de3fb75673123a6\n--- a/docs/adr/0015-contract-approval-before-dispatch.md\n+++ b/docs/adr/SADR-0015-contract-approval-before-dispatch.md\n@@ -1,4 +1,4 @@\n-# ADR 0015: Approve The Task Contract Before Dispatch, Not The Diff After It\n+# SADR-0015: Approve The Task Contract Before Dispatch, Not The Diff After It\n \n ## Status\n \n@@ -9,7 +9,7 @@\n Guardian approval for high/critical risk is expressible only on a pull request.\n Every approval source in `guardian-policy.json` \u2014 GitHub review, the\n `guardian:approved` label, the `Guardian approval granted` comment, and the\n-`external_ai_guardian_review` artifact (ADR 0010/0014) \u2014 is bound to a PR, and\n+`external_ai_guardian_review` artifact (SADR-0010/0014) \u2014 is bound to a PR, and\n three of the four require the current head SHA. Approval therefore cannot exist\n before an implementation PR exists.\n \n@@ -81,7 +81,7 @@\n An envelope breach must surface only on the non-repairable policy gate, never on\n the repairable `MergeGate metadata check`. Otherwise the Goal Loop routes it to\n `dispatch_repair` \u2014 instructing a runner to make the breach go away \u2014 which is\n-exactly what this ADR forbids.\n+exactly what this SADR forbids.\n \n **Contract immutability.** After registration, an implementation PR must not\n change the governance fields of its own task contract (`scope`, `non_goals`,\n@@ -116,7 +116,7 @@\n fails any verdict that is not `complete`, and the signal the CCA prompt reads is\n computed from `evaluate_guardian_approval` alone. Without the OR-merge at the\n signal, a contract-approved PR still returns `needs_guardian`, the CCA job fails,\n-and the MergeGate policy check never runs \u2014 the gate this ADR changes would never\n+and the MergeGate policy check never runs \u2014 the gate this SADR changes would never\n be reached. The CCA *prompt* needs no change: a `contract_approval` entry in the\n signal's existing `sources` array satisfies its current wording.\n \n@@ -130,7 +130,7 @@\n written, and an approved contract lets its implementation PRs merge through the\n same automatic path as low/medium risk. The wasted implement-then-block cycle\n disappears. Guardian review shifts from reading diffs to reading contracts, which\n-is both earlier and smaller; the external AI Guardian path (ADR 0010/0014) works\n+is both earlier and smaller; the external AI Guardian path (SADR-0010/0014) works\n on contracts with no change.\n \n We accept that the Guardian authorizes a class of change without seeing its\n@@ -146,7 +146,7 @@\n adversarial tests for the new approval path equivalent in rigor to\n `test_shiki_governance_evidence.sh`.\n \n-Bootstrap costs, which apply only while this ADR is being implemented. The tasks\n+Bootstrap costs, which apply only while this SADR is being implemented. The tasks\n that build Contract Approval merge under the existing rules, so the ones\n dispatched after the pre-dispatch gate lands but before the approval path lands\n need a Guardian-approved Contract PR *and* a live Guardian approval on their\n@@ -210,7 +210,7 @@\n risk; they do not bound authorized work.\n \n **Auto-escalate risk on envelope breach instead of stopping.** Returns the\n-workflow to a diff-time Guardian decision \u2014 the exact behavior this ADR removes.\n+workflow to a diff-time Guardian decision \u2014 the exact behavior this SADR removes.\n \n **Resolve governance fields from the base snapshot only.** Rejected against the\n registration flow: implementation PRs add their own contract, so the base has no\ndiff --git a/docs/adr/0016-locks-bound-product-paths.md b/docs/adr/SADR-0016-locks-bound-product-paths.md\nindex b5e850f3f1f7f4ef6e7f9d1763a29a51bd300c45..46b40a9b99a7aae644c02bb458f5473ff0a90ce2\n--- a/docs/adr/0016-locks-bound-product-paths.md\n+++ b/docs/adr/SADR-0016-locks-bound-product-paths.md\n@@ -1,4 +1,4 @@\n-# ADR 0016: Locks bound product paths; the Shiki mirror is bound by record identity\n+# SADR-0016: Locks bound product paths; the Shiki mirror is bound by record identity\n \n ## Status\n \n@@ -6,8 +6,8 @@\n \n - Date: 2026-07-28\n - Deciders: operator (mizutani-140), Claude (planner)\n-- Related: ADR 0004 (GitHub-first with a `.shiki` mirror), ADR 0009 (Spec Freeze),\n-  ADR 0012 (autonomous loop closeout to main), ADR 0015 (Contract Approval before\n+- Related: SADR-0004 (GitHub-first with a `.shiki` mirror), SADR-0009 (Spec Freeze),\n+  SADR-0012 (autonomous loop closeout to main), SADR-0015 (Contract Approval before\n   dispatch), remediation goal task R-02, the live PR #179 deadlock.\n \n ## Context\n@@ -90,11 +90,11 @@\n files, documentation files and named source modules are nameable; runtime-\n allocated ledger, runner and report ids are not \u2014 and those live in `.shiki`,\n which locks no longer govern. Existing frozen contracts are brought into line by\n-a one-time Spec Amendment per plan, which must land before ADR 0015's contract\n+a one-time Spec Amendment per plan, which must land before SADR-0015's contract\n immutability makes `locks` unwritable from the implementation PR.\n \n **Lock state is resolved from the durable side.** MergeGate resolves the lock\n-registry, and ADR 0015's governance fields, from the base `.shiki` snapshot when\n+registry, and SADR-0015's governance fields, from the base `.shiki` snapshot when\n present, falling back to head only when absent. The `MergeGate metadata check`\n job is given `--base-shiki`, which it does not receive today. Locks are not\n derived from the live open-pull-request set: a pull request body is untrusted\n@@ -108,7 +108,7 @@\n Explicitly out of scope: moving MergeGate's contract resolution off the PR\n checkout wholesale; making the mirror a projection of GitHub state; introducing\n new lock kinds; moving ledger evidence out of the repository; and unwinding\n-ADR 0012's closeout pull request. Registration of multi-task goals is ADR 0015's\n+SADR-0012's closeout pull request. Registration of multi-task goals is SADR-0015's\n Contract PR and is not re-decided here.\n \n ## Consequences\n@@ -127,7 +127,7 @@\n \n Risks accepted. First, goal-scoped identity is only as strong as `goal_id`,\n which is resolved from the PR body and the head task file; the mitigation is to\n-add `goal_id` to ADR 0015's frozen governance field set, and until that lands a\n+add `goal_id` to SADR-0015's frozen governance field set, and until that lands a\n task file can name a foreign goal. Second, base-when-present resolution is\n unavailable on fork pull requests, where the head is contributor-controlled;\n fork pull requests already cannot complete a Shiki merge, and this decision does\n@@ -139,7 +139,7 @@\n path, not by implementation pull requests.\n \n New obligations: one Spec Amendment per frozen plan to rewrite `locks`, ordered\n-before ADR 0015 T1; extension of the adversarial suites in\n+before SADR-0015 T1; extension of the adversarial suites in\n `scripts/test_shiki_adversarial_state.sh` and\n `scripts/test_shiki_governance_evidence.sh` to cover the eleven newly-scoped\n directories; and updating `scripts/test_shiki_loop_lock_guard.sh`,\n@@ -151,32 +151,32 @@\n whether the loop's transitive sync of ledger `evidence` references can be\n deleted once the Contract PR carries its own source plan.\n \n-## Relationship to ADR 0004\n+## Relationship to SADR-0004\n \n-This ADR **clarifies something ADR 0004 left unspecified**. ADR 0004 decides that\n+This SADR **clarifies something SADR-0004 left unspecified**. SADR-0004 decides that\n GitHub is the operational source of truth and that each target repository keeps a\n `.shiki/` mirror. It says nothing about pull requests writing the mirror, does\n not name MergeGate, and does not make the mirror an input to a gate. Nothing in\n-ADR 0004's Decision or Consequences is amended, superseded or weakened: the\n+SADR-0004's Decision or Consequences is amended, superseded or weakened: the\n mirror still exists, still records the same artifacts, and pull request\n-boundaries remain the coordination surface. What this ADR settles is the question\n+boundaries remain the coordination surface. What this SADR settles is the question\n 0004 never reached \u2014 by what authority a pull request may write a mirror record \u2014\n and it answers it the way `.shiki/manifest.json` already declares\n (`pr_mutation: current-task-or-current-goal-only`).\n \n-It **amends ADR 0012**, whose Decision item 5 states that \"Loop-executed tasks\n+It **amends SADR-0012**, whose Decision item 5 states that \"Loop-executed tasks\n declare the `path:.shiki/**` lock, which covers every `.shiki` file the closeout\n stages ... so `files_outside_locks` is satisfied without per-file lock\n bookkeeping.\" That clause is replaced: the closeout's mirror writes are\n-authorized by record identity, not by a declared lock. ADR 0012's substantive\n+authorized by record identity, not by a declared lock. SADR-0012's substantive\n decision \u2014 that completion reaches `main` through a closeout pull request rather\n than local attestation \u2014 is preserved unchanged.\n \n-It is **complementary to ADR 0015**, not a substitute. ADR 0015 owns contract\n+It is **complementary to SADR-0015**, not a substitute. SADR-0015 owns contract\n immutability, the pre-dispatch gate, the Contract PR and the Scope Envelope. This\n-ADR owns what a lock means and who may write the mirror. The one ordering\n+SADR owns what a lock means and who may write the mirror. The one ordering\n constraint between them is that the lock-narrowing Spec Amendment must precede\n-ADR 0015 T1's freezing of the `locks` field.\n+SADR-0015 T1's freezing of the `locks` field.\n \n ## Alternatives Considered\n \n@@ -186,7 +186,7 @@\n the only writer that can register them may run. Migrating the Guardian signal to\n that store makes every registration PR fail closed to `needs_guardian` at every\n risk level, because the task file is by definition absent from the store, which\n-removes the low/medium autonomous path ADR 0008/0009 exists to provide. And it\n+removes the low/medium autonomous path SADR-0008/0009 exists to provide. And it\n leaves schemas, manifest, config, guardian policy, templates, migrations, plans,\n worktrees, runner records, runs and handoffs with no writer at all in a\n repository whose product is `.shiki`. Measured benefit on the frozen backlog:\n@@ -199,14 +199,14 @@\n namespace is not in fact ownership-partitioned \u2014 56 ledger entries carry no\n `task_id`, and one goal DAG is writable by 35 registered tasks. The correct\n expression of its insight is that locks do not govern the mirror at all, which is\n-what this ADR decides.\n+what this SADR decides.\n \n **Make the mirror a projection written only by a registration writer and a\n post-merge projection job.** Rejected. It requires a bypass actor with write\n access to the protected default branch; it makes its own master invariant\n readable from a manifest that pull requests may edit; it deletes both reconcile\n modes and leaves a corrupted mirror with no in-band repair path, contradicting\n-ADR 0004's consequence that mirror conflicts are repaired against GitHub state;\n+SADR-0004's consequence that mirror conflicts are repaired against GitHub state;\n and its content-derived ids are rejected by `validate_shiki`'s currently-green id\n pattern.\n \ndiff --git a/docs/adr/0017-bookkeeping-closeouts-do-not-inherit-risk.md b/docs/adr/SADR-0017-bookkeeping-closeouts-do-not-inherit-risk.md\nindex 554e3a0d526a72ca067108c5464cc68c3edcf174..b9676857a4713850e063af7c39c8e0b01ecb4c66\n--- a/docs/adr/0017-bookkeeping-closeouts-do-not-inherit-risk.md\n+++ b/docs/adr/SADR-0017-bookkeeping-closeouts-do-not-inherit-risk.md\n@@ -1,4 +1,4 @@\n-# ADR 0017: A Bookkeeping Closeout Does Not Inherit Its Task's Risk\n+# SADR-0017: A Bookkeeping Closeout Does Not Inherit Its Task's Risk\n \n ## Status\n \n@@ -6,7 +6,7 @@\n \n ## Context\n \n-ADR 0012 made the goal loop push a task's completion to the default branch through a\n+SADR-0012 made the goal loop push a task's completion to the default branch through a\n closeout pull request: after the implementation PR merges, the loop opens a second PR\n carrying `task.status = done`, `lock.state = released`, and \u2014 when the task completes\n its goal \u2014 `goal.status = complete` plus one scorecard report. Nothing else. The\n@@ -49,14 +49,14 @@\n 4. The task file's governance fields (`scope`, `non_goals`, `required_skills`,\n    `risk_level`, `locks`, `acceptance_checks`, `test_command`) are byte-identical to\n    the base snapshot. Only `status`, `expected_pr`, `closeout_pr`, `ledger_evidence`\n-   and `expected_branch` may differ \u2014 the ADR 0015 contract-immutability rule,\n+   and `expected_branch` may differ \u2014 the SADR-0015 contract-immutability rule,\n    unchanged.\n \n    `expected_branch` is in that set because a closeout is *by construction* on a\n    different branch from the implementation it closes, and the loop rewrites the\n    field accordingly. It is safe to let it differ because MergeGate independently\n    requires `expected_branch` to equal the PR's actual head ref, so a PR cannot set\n-   it to anything other than the branch it is really on. The first draft of this ADR\n+   it to anything other than the branch it is really on. The first draft of this SADR\n    omitted it, which made the exemption unreachable: every real closeout failed\n    condition 4 on a field the loop always changes. See PR #209.\n 5. The state transitions are exactly the terminal set: task `review` \u2192 `done`, lock\n@@ -113,12 +113,12 @@\n classification must be derived, not asserted.\n \n **Fold the closeout into the implementation PR** so no second PR exists. Rejected under\n-ADR 0012, which recorded why: a task file that is already `done` on the implementation\n+SADR-0012, which recorded why: a task file that is already `done` on the implementation\n branch short-circuits the loop's review \u2192 CCA \u2192 merge \u2192 done ordering into a local\n goal-completion before any merge happens.\n \n **Have the loop auto-merge closeouts with elevated credentials.** Rejected outright: that\n-is a runtime authoring its own approval, which ADR 0010 and the constitution's\n+is a runtime authoring its own approval, which SADR-0010 and the constitution's\n approval-record prohibition forbid. It would also be indistinguishable, in the evidence,\n from the forgery those rules exist to prevent.\n \ndiff --git a/docs/adr/0018-carried-guardian-approval-across-a-proven-base-sync.md b/docs/adr/SADR-0018-carried-guardian-approval-across-a-proven-base-sync.md\nindex 22358562da390ed8b45cd600da08a43d378b4680..e6e2907cefa776460af73ddf7d39db605f3c39b0\n--- a/docs/adr/0018-carried-guardian-approval-across-a-proven-base-sync.md\n+++ b/docs/adr/SADR-0018-carried-guardian-approval-across-a-proven-base-sync.md\n@@ -1,4 +1,4 @@\n-# ADR 0018: Carried Guardian Approval Across A Proven Base Sync\n+# SADR-0018: Carried Guardian Approval Across A Proven Base Sync\n \n ## Status\n \n@@ -34,7 +34,7 @@\n Guardian evaluator that consumes it (`shiki_guardian.evaluate_guardian_approval`,\n the `guardian_comment_carried` source) were built and tested in isolation. Neither\n does anything until a caller passes the flags that turn the carry on, so the\n-feature was invisible in CI. This ADR records the decision the two of them\n+feature was invisible in CI. This SADR records the decision the two of them\n implement, and this task wires it into the four CI invocations that judge Guardian\n approval.\n \ndiff --git a/docs/adr/README.md b/docs/adr/README.md\nindex c70e057a4520c36e042ab68c53cd4a21ef709b71..5da43856836e2ce6a150f8127766e2f88355ab4b\n--- a/docs/adr/README.md\n+++ b/docs/adr/README.md\n@@ -1,40 +1,41 @@\n-# Architecture Decision Records\n+# Decision Record Namespaces\n \n-This directory holds Shiki's Architecture Decision Records (ADRs): durable\n-records of hard-to-reverse platform decisions. ADRs are part of the Shiki source\n-of truth alongside GitHub state, the `.shiki/` mirror, and `CONTEXT.md`.\n+This directory holds two independent decision namespaces:\n \n-## How ADRs Work\n+- Shiki Architecture Decision Records (SADRs), named\n+  `SADR-NNNN-<short-slug>.md`, record hard-to-reverse Shiki platform decisions.\n+- Target Architecture Decision Records (ADRs), named\n+  `NNNN-<short-slug>.md`, belong to the target product.\n \n-- New ADRs are added as `docs/adr/NNNN-<short-slug>.md`, where `NNNN` is the next\n-  zero-padded sequence number.\n-- The **canonical list of ADRs is the set of files matching `docs/adr/*.md`** in\n-  this directory. Browse `docs/adr/*.md` for the authoritative, current set.\n-- Use [`template.md`](template.md) as the starting point for a new ADR.\n-- Record an ADR when a decision is hard to reverse, surprising without context,\n-  and a real tradeoff. Do not write an ADR for routine or easily reversible\n-  choices.\n+The same four-digit number may appear once in each namespace. A platform\n+`SADR-NNNN` citation resolves only to `SADR-NNNN-*.md`; a target `ADR NNNN`\n+citation resolves only to `NNNN-*.md`.\n \n-Using the `docs/adr/*.md` glob as the source of truth means other PRs can add an\n-ADR without editing this index file, avoiding merge contention on a\n-hand-maintained enumeration.\n+## How Decision Records Work\n \n-## Current ADRs (snapshot)\n-\n-The list below is a convenience snapshot and may lag behind the directory. When\n-in doubt, list `docs/adr/*.md`.\n+- New Shiki platform decisions use the next zero-padded SADR number.\n+- New target product decisions use the target's own ADR sequence.\n+- The canonical list is the files in `docs/adr/`; no hand-maintained index is\n+  authoritative.\n+- Use [`template.md`](template.md) as the starting point and choose the filename\n+  namespace that owns the decision.\n+- Record a decision when it is hard to reverse, surprising without context, and\n+  a real tradeoff. Do not write a decision record for routine or easily\n+  reversible choices.\n \n-- [0001 \u2014 Keep Shiki As The Platform Name](0001-keep-shiki-as-platform-name.md)\n-- [0002 \u2014 Use AGENTS As The Shared Agent Constitution](0002-use-agents-as-shared-constitution.md)\n-- [0003 \u2014 Require Skill Gate For MergeGate Readiness](0003-require-skill-gate-for-mergegate-readiness.md)\n-- [0004 \u2014 Use GitHub First With A Repository-Local Shiki Mirror](0004-use-github-first-with-shiki-mirror.md)\n-- [0005 \u2014 Use Codex As Front And Claude Code Action As The GitHub Runtime](0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md)\n-- [0006 \u2014 Separate Codex Implementation, CCA Completion, And MergeGate Authorization](0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md)\n+Using the directory as the source of truth lets independent changes add records\n+without contending on an enumerated index.\n \n-## Adding A New ADR\n+## Adding A Shiki SADR\n \n-1. Copy `template.md` to `docs/adr/NNNN-<short-slug>.md`.\n+1. Copy `template.md` to `docs/adr/SADR-NNNN-<short-slug>.md`.\n 2. Fill in the title, status, context, decision, and consequences.\n-3. Set the status to `Proposed`, then `Accepted` once the decision is approved.\n-4. Open a PR following `CONTRIBUTING.md`. You do not need to edit this index for\n-   the ADR to be discoverable via `docs/adr/*.md`.\n+3. Set the status to `Proposed`, then `Accepted` once the proper authority has\n+   approved the decision.\n+4. Open a PR following `CONTRIBUTING.md`.\n+\n+## Adding A Target ADR\n+\n+1. Copy `template.md` to `docs/adr/NNNN-<short-slug>.md` using the target's own\n+   sequence.\n+2. Cite it as `ADR NNNN`; do not prefix its filename with `SADR-`.\ndiff --git a/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md b/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\nnew file mode 100644\nindex 0000000000000000000000000000000000000000..7455ce1f3bed02ddddb851ac21a1d1a648c9aeba\n--- /dev/null\n+++ b/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\n@@ -0,0 +1,55 @@\n+# SADR-0019: Separate Shiki Platform SADRs From Target ADRs\n+\n+## Status\n+\n+Accepted\n+\n+## Context\n+\n+Shiki installs its platform decision records into `docs/adr/` in every target\n+repository. Shiki and its targets previously used the same numeric\n+`NNNN-<slug>.md` namespace. A target that already owned ADRs 0013 through 0018\n+therefore acquired two records for each number during installation, making an\n+`ADR NNNN` citation ambiguous. Renumbering target decisions would rewrite target\n+history, while renumbering Shiki decisions per target would destroy their stable\n+platform identity.\n+\n+Upgrades also need to remove the former Shiki-owned numeric paths without ever\n+mistaking a target-owned ADR for platform content. The install stamp already\n+records exact shipped paths and content digests, so it can prove ownership only\n+when both still match.\n+\n+## Decision\n+\n+Shiki platform decisions use the `SADR` namespace:\n+\n+- records are named `docs/adr/SADR-NNNN-<slug>.md`;\n+- live platform citations use `SADR-NNNN`;\n+- target product decisions retain `docs/adr/NNNN-<slug>.md` and `ADR NNNN`;\n+- the reference validator resolves the two namespaces independently, so the same\n+  four-digit number may exist once in each namespace.\n+\n+Before a forced upgrade writes anything, the installer inventories the exact\n+former numeric Shiki ADR paths. It may delete a legacy path only when the\n+existing install stamp contains that exact path and its recorded SHA-256 digest\n+matches the file currently on disk. All ownership blockers are reported\n+together and abort the upgrade before its first write. A non-forced install\n+never performs this cleanup.\n+\n+Historical `.shiki/` evidence remains unchanged because it records the namespace\n+that existed when that evidence was produced.\n+\n+## Consequences\n+\n+- Targets may use any ADR number without colliding with Shiki's platform SADRs.\n+- Fresh installs preserve target ADR bytes and install a stable SADR set.\n+- Forced upgrades fail closed when legacy ownership cannot be proven, and a\n+  successful upgrade refreshes the stamp without removed legacy paths.\n+- Current governance, packaging, workflow, prompt, script, and skill references\n+  must move with the platform records.\n+\n+## Alternatives Considered\n+\n+- Collision-only refusal leaves affected targets permanently unupgradable.\n+- Renumbering target ADRs mutates target-owned history.\n+- Per-target Shiki renumbering makes platform citations non-portable.\ndiff --git a/docs/adr/template.md b/docs/adr/template.md\nindex a92a97daddd0b00ab9dd626cc8d86942da2eafed..17dd79175b5fee2105935557f691b40cf9492942\n--- a/docs/adr/template.md\n+++ b/docs/adr/template.md\n@@ -1,14 +1,14 @@\n-# NNNN: <Short Decision Title>\n+# <SADR-NNNN or NNNN>: <Short Decision Title>\n \n ## Status\n \n-Proposed | Accepted | Superseded by ADR-NNNN\n+Proposed | Accepted | Superseded by SADR-NNNN or target ADR NNNN\n \n ## Context\n \n What is the situation, constraint, or force that requires a decision? Reference\n-relevant domain terms (`CONTEXT.md`), prior ADRs, and code reality. State the\n-problem, not the solution.\n+relevant domain terms (`CONTEXT.md`), prior Shiki SADRs or target ADRs, and code\n+reality. State the problem, not the solution.\n \n ## Decision\n \ndiff --git a/docs/agents/autonomous-loop-e2e-acceptance.md b/docs/agents/autonomous-loop-e2e-acceptance.md\nindex bbf84c6081987f159ac6ad9ecf3c82b7aa72e7e8..5c13c1d02a05a90b0dc46cbef7c95a9846d0c237\n--- a/docs/agents/autonomous-loop-e2e-acceptance.md\n+++ b/docs/agents/autonomous-loop-e2e-acceptance.md\n@@ -40,8 +40,8 @@\n 3. `gh auth status` is logged in with merge permission on the repo.\n 4. Branch protection + required checks are configured exactly as the goal-loop\n    required-check set expects (see `docs/agents/branch-protection-smoke.md`).\n-5. The repo `.shiki/policy` permits autonomous low/medium-risk merges (ADR\n-   0008/0009); the MergeGate policy check is the recorded-authority gate.\n+5. The repo `.shiki/policy` permits autonomous low/medium-risk merges\n+   (SADR-0008/0009); the MergeGate policy check is the recorded-authority gate.\n \n The operator's only actions are: register the task and start the loop. **No\n manual pre-clearing** means after `shiki loop run` starts, the operator does not\ndiff --git a/docs/agents/checklists.md b/docs/agents/checklists.md\nindex 4a7f03ade346f53a1c1d82981f3fd744f9ab4547..0c5ec6c53dc7197b7b7a7d18e667b325babd6c45\n--- a/docs/agents/checklists.md\n+++ b/docs/agents/checklists.md\n@@ -32,18 +32,18 @@\n | ID | Item | Blocking |\n | --- | --- | --- |\n | GD-01 | Plan was challenged against `CONTEXT.md`. | blocking for non-trivial Goals |\n-| GD-02 | Relevant ADRs were read or absence was recorded. | blocking for non-trivial Goals |\n+| GD-02 | Relevant SADRs and target ADRs were read or absence was recorded. | blocking for non-trivial Goals |\n | GD-03 | Ambiguous terms were resolved or converted to HITL blockers. | blocking |\n | GD-04 | Edge scenarios were considered. | advisory |\n | GD-05 | Code reality was checked when code could answer. | blocking when applicable |\n | GD-06 | New domain terms were added to `CONTEXT.md` when needed. | blocking when applicable |\n-| GD-07 | ADR was created or explicitly skipped using the ADR criteria. | blocking when applicable |\n+| GD-07 | A SADR or target ADR was created or explicitly skipped using the decision-record criteria. | blocking when applicable |\n \n ## CI \u2014 Context & Impact Checklist\n \n | ID | Item | Blocking |\n | --- | --- | --- |\n-| CI-01 | Relevant docs and ADRs identified. | blocking |\n+| CI-01 | Relevant docs, SADRs, and target ADRs identified. | blocking |\n | CI-02 | Relevant modules, interfaces, seams, callers, and tests identified. | blocking |\n | CI-03 | Dependencies and blocked-by relationships identified. | blocking |\n | CI-04 | Candidate locks identified. | blocking |\n@@ -62,7 +62,7 @@\n | PRD-04 | Implementation decisions are recorded without volatile code detail. | blocking |\n | PRD-05 | Testing decisions are recorded. | blocking |\n | PRD-06 | Out-of-scope items are explicit. | blocking |\n-| PRD-07 | Relevant glossary/ADR links are present when applicable. | blocking |\n+| PRD-07 | Relevant glossary/SADR/target-ADR links are present when applicable. | blocking |\n \n ## SF \u2014 Spec Freeze Checklist\n \n@@ -98,7 +98,7 @@\n | H-02 | Branch/worktree target is present. | blocking |\n | H-03 | Scope and non-goals are present. | blocking |\n | H-04 | Dependencies and locks are present. | blocking |\n-| H-05 | Relevant docs, ADRs, modules, and tests are referenced. | blocking |\n+| H-05 | Relevant docs, SADRs, target ADRs, modules, and tests are referenced. | blocking |\n | H-06 | Required skills are stated. | blocking |\n | H-07 | TDD expectations are stated. | blocking when behavior work |\n | H-08 | Acceptance criteria are included. | blocking |\ndiff --git a/docs/agents/codex-handoff.md b/docs/agents/codex-handoff.md\nindex ede468f47cb277f69a857e8c45df703e4ed0151d..6f97bdffb67e44d7edea2c71e01ea28933c4eb6c\n--- a/docs/agents/codex-handoff.md\n+++ b/docs/agents/codex-handoff.md\n@@ -46,7 +46,7 @@\n \n ## Context\n - Domain terms:\n-- Relevant ADRs:\n+- Relevant SADRs and target ADRs:\n - Relevant docs:\n - Relevant modules/interfaces/seams:\n - Similar tests/prior art:\ndiff --git a/docs/agents/completion-check-agent.md b/docs/agents/completion-check-agent.md\nindex 13e4c035db35025f4e3cdcc5adfb24314ac4d521..61bfa1218cf36ca605250c91919e620f33238aa0\n--- a/docs/agents/completion-check-agent.md\n+++ b/docs/agents/completion-check-agent.md\n@@ -32,7 +32,7 @@\n - `AGENTS.md`\n - `CLAUDE.md`\n - `CONTEXT.md`\n-- relevant ADRs\n+- relevant SADRs and target ADRs\n - parent Goal issue\n - PRD issue or document\n - task issue\ndiff --git a/docs/agents/decision-control.md b/docs/agents/decision-control.md\nindex 7d8ee5ab03a5ccbcbd04056ae61de03785a2a9c9..8c94ecda00fffaa0e92c325ab83aa9776bd1dac1\n--- a/docs/agents/decision-control.md\n+++ b/docs/agents/decision-control.md\n@@ -11,7 +11,7 @@\n | Decision | Owner | When | Required inputs | Output | Enforcement surface |\n | --- | --- | --- | --- | --- | --- |\n | Is the Goal clear enough to plan? | Planner + Guardian when needed | Goal intake | User request, existing docs, risk signals | Goal readiness verdict | Goal checklist, issue template, ledger |\n-| Must `grill-with-docs` run? | Planner / Skill Gate | Before PRD or issue split | Ambiguity, domain terms, ADR conflicts, unclear boundaries | Skill invocation or explicit skip rationale | Skill Gate checklist |\n+| Must `grill-with-docs` run? | Planner / Skill Gate | Before PRD or issue split | Ambiguity, domain terms, SADR/target-ADR conflicts, unclear boundaries | Skill invocation or explicit skip rationale | Skill Gate checklist |\n | Is a PRD required? | Planner | After Goal context stabilizes | Goal, `grill-with-docs` output, Context & Impact output | PRD required / not required | PRD checklist, CCA evidence check |\n | Can the spec be frozen? | Operator only | End of Requirements Definition, before issue split | PRD/requirements summary, scope inventory (SF-02), open-question list | `spec_freeze` block in the plan + ledger evidence | `require_grilled_plan` (plan ingest/run), SF checklist |\n | Can a frozen spec change? | Operator only (Spec Amendment) | When implementation contradicts the frozen spec | Contested decisions, scoped re-grill result | recorded amendment + re-stamped freeze | SF checklist, ledger evidence |\ndiff --git a/docs/agents/domain.md b/docs/agents/domain.md\nindex 258da8ea81e87ac0a6b0b5b5e5c2fde980eaabb7..d57b0cf17ff3e1fa9e6e5885372bf083d8295421\n--- a/docs/agents/domain.md\n+++ b/docs/agents/domain.md\n@@ -5,7 +5,9 @@\n ## Layout\n \n - `CONTEXT.md` defines Shiki language. Keep it implementation-free.\n-- `docs/adr/` records hard-to-reverse decisions.\n+- `docs/adr/SADR-NNNN-*.md` records Shiki platform decisions.\n+- `docs/adr/NNNN-*.md` records target product decisions without sharing the\n+  SADR namespace.\n - `AGENTS.md` is the runtime-neutral agent constitution.\n - `CLAUDE.md` is the Claude Code wrapper around `AGENTS.md`.\n - `.shiki/` is the Target Repository mirror for Goals, PRDs, plans, Task DAGs, contracts, locks, worktree records, ledger entries, CCA verdicts, repair packets, reports, and handoffs.\n@@ -15,6 +17,6 @@\n \n - Use Shiki terms exactly when they exist.\n - Add glossary terms to `CONTEXT.md` only when the term is part of the domain, not a generic programming concept.\n-- Create ADRs only for hard-to-reverse, surprising, tradeoff-based decisions.\n+- Create SADRs or target ADRs only for hard-to-reverse, surprising, tradeoff-based decisions.\n - Keep product-specific language in the Target Repository, not in Shiki Template docs.\n-- If an ADR conflicts with a proposed implementation, surface the conflict before editing.\n+- If a SADR or target ADR conflicts with a proposed implementation, surface the conflict before editing.\ndiff --git a/docs/agents/guardian-policy.md b/docs/agents/guardian-policy.md\nindex 4147c7adf05a47adb4fd24ada1a1c2e1f2ba6d2d..a20d9cc6c00a569bb057b9ca8d6b51c7fc4a9847\n--- a/docs/agents/guardian-policy.md\n+++ b/docs/agents/guardian-policy.md\n@@ -98,8 +98,8 @@\n `origin/<default_branch>` and reuses each job's existing base fetch; a shallow\n (`--depth`) or second base fetch would make the proof unresolvable and silently\n disable the carry, so neither is permitted. CCA reads `guardian_comment_carried` as\n-a recorded authority in the deterministic signal and does not re-derive it. See ADR\n-0018 for the full decision, the rejected diff/patch-equality and path-disjointness\n+a recorded authority in the deterministic signal and does not re-derive it. See\n+SADR-0018 for the full decision, the rejected diff/patch-equality and path-disjointness\n alternatives, and the accepted residual.\n \n ## External AI Guardian Review (SADR-0010)\ndiff --git a/docs/agents/implementation-policy.md b/docs/agents/implementation-policy.md\nindex c754144a9fb5bf4a38aa82a60e1670c59e626b44..85c37281bcad196c6b7b02343faf00b03cd9c57a\n--- a/docs/agents/implementation-policy.md\n+++ b/docs/agents/implementation-policy.md\n@@ -31,10 +31,13 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n ## Non-Negotiables\n \n 1. **Planning precedes implementation.** Non-trivial Goals start with `grill-with-docs` and Context & Impact.\n-2. **Domain language is controlled.** Terms must be checked against `CONTEXT.md`; hard-to-reverse tradeoffs must be checked against ADRs.\n+2. **Domain language is controlled.** Terms must be checked against `CONTEXT.md`; hard-to-reverse tradeoffs must be checked against Shiki SADRs and target ADRs.\n 3. **PRD precedes issue decomposition.** `to-prd` is used once the context is settled enough to become durable intent.\n 4. **Issues are vertical slices.** `to-issues` must produce independently grabbable tracer-bullet tasks, not horizontal layer tickets.\n 5. **Triage controls dispatch.** Only `ready-for-agent` / AFK tasks may be assigned to Codex without human input.\n@@ -95,19 +98,19 @@\n Process:\n \n - Challenge terms against `CONTEXT.md`.\n-- Check ADRs before reopening decisions.\n+- Check SADRs and target ADRs before reopening decisions.\n - Inspect code when code can answer a question.\n - Ask one question at a time when the operator is available.\n - Provide a recommended answer with each question.\n - Record resolved terms in `CONTEXT.md`.\n-- Create ADRs only for hard-to-reverse, surprising, tradeoff-based decisions.\n+- Create SADRs or target ADRs only for hard-to-reverse, surprising, tradeoff-based decisions.\n - Convert unresolved load-bearing questions into HITL blockers.\n \n Outputs:\n \n - Resolved design-tree decisions.\n - Updated glossary terms when needed.\n-- ADRs when justified.\n+- SADRs or target ADRs when justified.\n - HITL blockers when decisions remain unresolved.\n \n ## Phase 3 \u2014 Context & Impact\n@@ -117,7 +120,7 @@\n \n Outputs must include:\n \n-- Relevant documents and ADRs.\n+- Relevant documents, SADRs, and target ADRs.\n - Relevant modules, interfaces, seams, callers, and tests.\n - Candidate locks.\n - Dependencies.\n@@ -140,7 +143,7 @@\n - Testing decisions.\n - Out of scope.\n - Further notes.\n-- Links to relevant Goal, domain terms, and ADRs.\n+- Links to relevant Goal, domain terms, SADRs, and target ADRs.\n \n Phase 4 ends with Spec Freeze: the operator explicitly approves the PRD, the\n plan records a `spec_freeze` block (status, approved_by, source), and the SF\n@@ -240,7 +243,7 @@\n - Dependencies and locks.\n - Risk and Guardian requirements.\n - Ledger completeness.\n-- Documentation and ADR updates.\n+- Documentation and SADR/target-ADR updates.\n - Scope drift.\n \n CCA emits one of:\n@@ -301,7 +304,7 @@\n | Evidence failure | Behavior might be done, but proof is missing. | Add checks, PR evidence, ledger entry, or artifacts. |\n | Scope drift | PR changes unrelated behavior. | Revert unrelated changes or split into new Goal. |\n | Dependency block | Upstream task or lock not complete. | Block until dependency resolves. |\n-| Architecture block | Decision needs ADR or deep-module discussion. | `grill-with-docs` or `improve-codebase-architecture`. |\n+| Architecture block | Decision needs a SADR, target ADR, or deep-module discussion. | `grill-with-docs` or `improve-codebase-architecture`. |\n | Risk block | Security/production/policy change needs human approval. | Guardian decision. |\n \n ## Phase 5.5 \u2014 Execution Decision Control\ndiff --git a/docs/agents/runtime-auth-model.md b/docs/agents/runtime-auth-model.md\nindex 6a2e1a47e81ea89b5c68a210ec07772a9374babc..3c54748d24233dbd0ba8473950282547ef4ce935\n--- a/docs/agents/runtime-auth-model.md\n+++ b/docs/agents/runtime-auth-model.md\n@@ -148,7 +148,7 @@\n \n API-key based runners may be added by a target repository as an explicit extension. They are not the default Shiki template path.\n \n-If a repo opts into API-key mode, record the exception in an ADR and update the repo's `AGENTS.md`, `.shiki/config.yaml`, and workflow permissions.\n+If a repo opts into API-key mode, record the target-specific exception in a target ADR and update the repo's `AGENTS.md`, `.shiki/config.yaml`, and workflow permissions.\n \n ## GitHub Actions Runtime Compatibility\n \ndiff --git a/docs/agents/skill-gate.md b/docs/agents/skill-gate.md\nindex c50398b0d7c60a8598f2a5c88a10015730dced13..3acffe6444617bb1d54b77d4b4708158ec735bc6\n--- a/docs/agents/skill-gate.md\n+++ b/docs/agents/skill-gate.md\n@@ -9,7 +9,7 @@\n | Skill | Trigger | Expected output |\n | --- | --- | --- |\n | `setup-matt-pocock-skills` | First configuring a repo for these skills, or when issue tracker, triage labels, or domain docs are missing. | `docs/agents/` configuration and an `Agent skills` block in `AGENTS.md` or `CLAUDE.md`. |\n-| `grill-with-docs` | Ambiguous plan, unclear terminology, boundary decision, tradeoff, or ADR-worthy design choice. | Clarified decision tree, recommended answers, updates to `CONTEXT.md` or ADRs when decisions crystallize. |\n+| `grill-with-docs` | Ambiguous plan, unclear terminology, boundary decision, tradeoff, or decision-record-worthy design choice. | Clarified decision tree, recommended answers, updates to `CONTEXT.md`, SADRs, or target ADRs when decisions crystallize. |\n | `zoom-out` | Agent lacks a higher-level architectural map. | Map of relevant modules, callers, seams, and domain concepts. |\n | `to-prd` | Settled context should become a PRD. | PRD published to the issue tracker with problem, solution, user stories, decisions, testing, and out-of-scope notes. |\n | `to-issues` | Goal, PRD, or plan must become implementation tickets. | Vertical-slice issues with acceptance criteria, dependencies, and AFK/HITL classification. |\n@@ -17,7 +17,7 @@\n | `tdd` | Feature work or bug fix has observable behavior that can be specified with tests. | Red-green-refactor loop through public interfaces, one vertical slice at a time. |\n | `code-review` | The TDD loop is green and the implementer is about to create or update a PR. Default for every implementation task. | Pre-PR self-review of the task diff with fixes applied on the task branch; findings and resolutions recorded as a ledger entry naming the skill and a `## Pre-PR code review` PR body section. |\n | `diagnose` | Hard bug, failing check, regression, flaky behavior, or performance problem. | Reproduction loop, ranked hypotheses, instrumentation, fix, regression test, post-mortem. |\n-| `improve-codebase-architecture` | Structural friction, testability, AI-navigability, deep-module opportunity, or refactor request. | Architecture candidates and deepening recommendations informed by `CONTEXT.md` and ADRs. |\n+| `improve-codebase-architecture` | Structural friction, testability, AI-navigability, deep-module opportunity, or refactor request. | Architecture candidates and deepening recommendations informed by `CONTEXT.md`, SADRs, and target ADRs. |\n | `prototype` | A throwaway logic or UI prototype is needed to answer a design question. | Clearly marked disposable prototype and captured decision. |\n \n ## MergeGate Interaction\ndiff --git a/scripts/guardian_approval_signal.py b/scripts/guardian_approval_signal.py\nindex d059f92b2c82aa2b5d6ca421365537a87886c1cb..16924892714f4f5b9303c0ef0e325a7e70075630\n--- a/scripts/guardian_approval_signal.py\n+++ b/scripts/guardian_approval_signal.py\n@@ -199,8 +199,8 @@\n ) -> Any:\n     \"\"\"Evaluate SADR-0015 Contract Approval for the CCA Guardian signal.\n \n-    The SAME OR that MergeGate applies must reach this deterministic signal: ADR\n-    0015 is explicit that without it, a contract-approved PR still returns\n+    The SAME OR that MergeGate applies must reach this deterministic signal:\n+    SADR-0015 is explicit that without it, a contract-approved PR still returns\n     ``needs_guardian``, the CCA job fails, and the MergeGate policy check never\n     runs \u2014 the gate this change moves would never be reached. This resolves the\n     task the SAME way MergeGate does (the first Shiki task id in the PR body),\ndiff --git a/scripts/shiki_contract_approval.py b/scripts/shiki_contract_approval.py\nindex 941bcb96fdd8b9a85bc0c4f0f04d5dad9d04839c..4a4b418f6527413161db82670bec857a043360e2\n--- a/scripts/shiki_contract_approval.py\n+++ b/scripts/shiki_contract_approval.py\n@@ -142,7 +142,7 @@\n     changed_files_status: Any,\n     registration: Any,\n ) -> ContractApprovalResult:\n-    \"\"\"Whether this PR carries an SADR-0015 Contract Approval.\n+    \"\"\"Whether this PR carries a SADR-0015 Contract Approval.\n \n     The four conditions are evaluated IN ORDER, each failure recorded as a\n     distinct reason string:\ndiff --git a/scripts/shiki_installer.py b/scripts/shiki_installer.py\nindex be064ab7a29ff6836f7f7a1d70500a43e0d11d7b..d3e3247edbf3e4be287a4667176d86f6500f8dc5\n--- a/scripts/shiki_installer.py\n+++ b/scripts/shiki_installer.py\n@@ -118,6 +118,7 @@\n # what lets a target carry it without a governance-schema change.\n INSTALL_STAMP_PATH = \".shiki/install-stamp.json\"\n INSTALL_STAMP_VERSION = 1\n+LEGACY_SHIKI_SADR_MAX = 18\n \n # Under --force the shipped surface splits three ways, so nothing is lost and\n # nothing is silently stale.\n@@ -237,9 +238,98 @@\n         )\n \n \n+def legacy_shiki_adr_paths() -> tuple[str, ...]:\n+    \"\"\"Former numeric paths for Shiki SADR-0001 through SADR-0018.\n+\n+    The authoritative slugs come from the current SADR filenames. SADR-0019 is\n+    the namespace decision itself and never shipped under a numeric legacy path.\n+    \"\"\"\n+    paths: list[str] = []\n+    for sadr in sorted((ROOT / \"docs\" / \"adr\").glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\")):\n+        number = sadr.name.split(\"-\", 2)[1]\n+        if int(number) <= LEGACY_SHIKI_SADR_MAX:\n+            paths.append(f\"docs/adr/{sadr.name.removeprefix('SADR-')}\")\n+    return tuple(paths)\n+\n+\n+def preflight_legacy_sadr_cleanup(target: Path, *, force: bool) -> tuple[str, ...]:\n+    \"\"\"Authorize exact legacy Shiki ADR removals before the first write.\n+\n+    A non-force install never removes existing files. A forced install needs no\n+    stamp when no legacy Shiki path is present. When legacy paths do exist, each\n+    must be named in a readable install stamp and its current bytes must match\n+    the recorded digest.\n+    \"\"\"\n+    if not force:\n+        return ()\n+    present = tuple(\n+        relative\n+        for relative in legacy_shiki_adr_paths()\n+        if (target / relative).exists() or (target / relative).is_symlink()\n+    )\n+    if not present:\n+        return ()\n+\n+    stamp_path = target / INSTALL_STAMP_PATH\n+    try:\n+        raw = stamp_path.read_text(encoding=\"utf-8\")\n+        stamp = json.loads(raw)\n+    except FileNotFoundError:\n+        detail = \"install stamp is absent\"\n+        blockers = [f\"{relative}: {detail}\" for relative in present]\n+    except (OSError, UnicodeDecodeError) as error:\n+        detail = f\"install stamp is unreadable ({error})\"\n+        blockers = [f\"{relative}: {detail}\" for relative in present]\n+    except json.JSONDecodeError as error:\n+        detail = f\"install stamp is malformed ({error})\"\n+        blockers = [f\"{relative}: {detail}\" for relative in present]\n+    else:\n+        if not isinstance(stamp, dict):\n+            blockers = [\n+                f\"{relative}: install stamp is malformed (expected an object)\"\n+                for relative in present\n+            ]\n+        else:\n+            digests = stamp.get(\"digests\")\n+            blockers = []\n+            if not isinstance(digests, dict):\n+                blockers.extend(\n+                    f\"{relative}: install stamp has no digest for this path\"\n+                    for relative in present\n+                )\n+            else:\n+                for relative in present:\n+                    expected = digests.get(relative)\n+                    if not isinstance(expected, str) or not expected:\n+                        blockers.append(\n+                            f\"{relative}: install stamp has no digest for this path\"\n+                        )\n+                        continue\n+                    try:\n+                        actual = _sha256_file(target / relative)\n+                    except OSError as error:\n+                        blockers.append(\n+                            f\"{relative}: legacy path is unreadable ({error})\"\n+                        )\n+                        continue\n+                    if actual != expected:\n+                        blockers.append(\n+                            f\"{relative}: digest mismatch (stamp {expected}, current {actual})\"\n+                        )\n+\n+    if blockers:\n+        joined = \"\\n\".join(f\"- {blocker}\" for blocker in blockers)\n+        raise ShikiError(\n+            \"legacy Shiki ADR cleanup is not authorized:\\n\"\n+            f\"{joined}\\nNo files were written.\"\n+        )\n+    return present\n+\n+\n def install_template(target: Path, *, force: bool, validate: bool) -> list[NewFileNote]:\n     # Preconditions first: never rewrite the tree of a target that cannot be\n     # upgraded cleanly (see refuse_pending_migrations).\n+    legacy_cleanup = preflight_legacy_sadr_cleanup(target, force=force)\n     refuse_pending_migrations(target)\n \n     # Resolve the target's required CODEOWNERS owner once from its\n@@ -250,6 +340,10 @@\n     # verbatim. See ``codeowners_required_owner``.\n     codeowners_owner = codeowners_required_owner(target)\n \n+    for relative in legacy_cleanup:\n+        (target / relative).unlink()\n+        info(f\"removed stamped legacy Shiki ADR: {target / relative}\")\n+\n     notes: list[NewFileNote] = []\n     for relative in TEMPLATE_PATHS:\n         source = ROOT / relative\ndiff --git a/scripts/shiki_loop.py b/scripts/shiki_loop.py\nindex 3611641da8aeb5038b1da90e3114b09a473ec6d8..84caa491b9cad0edc4176f209c13e9221e975681\n--- a/scripts/shiki_loop.py\n+++ b/scripts/shiki_loop.py\n@@ -424,7 +424,7 @@\n         # unresolvable CCA verdict has already produced its terminal stop and is never\n         # laundered into a head-moving sync (SADR-0010). It also never fires while the\n         # policy (Guardian) gate is red \u2014 for ANY reason, not only when it is the sole\n-        # red check \u2014 so a head-bound Guardian approval, or an SADR-0015\n+        # red check \u2014 so a head-bound Guardian approval, or a SADR-0015\n         # contract-approval carry that base movement defeated (the policy gate compares\n         # base and head task governance directly), is never silently restored by\n         # adopting base wholesale. It is bounded by a durable per-task counter so the\ndiff --git a/scripts/test_shiki_goal_loop.sh b/scripts/test_shiki_goal_loop.sh\nindex 753c7b516e10f0be3bc92a4bd0eb1fa699776ae6..8f3820a0a079b3044424cfe1a6c6b7fe68896e67\n--- a/scripts/test_shiki_goal_loop.sh\n+++ b/scripts/test_shiki_goal_loop.sh\n@@ -231,8 +231,8 @@\n \n # High/critical risk merges autonomously when all required checks are green:\n # the \"MergeGate policy check\" required check IS the Guardian gate (it enforces\n-# guardian-policy.json \u2014 human approval OR external AI guardian review, ADR\n-# 0010), so green checks mean a recorded authority approved. The fake gh returns\n+# guardian-policy.json \u2014 human approval OR external AI guardian review,\n+# SADR-0010), so green checks mean a recorded authority approved. The fake gh returns\n # green checks, simulating that the Guardian gate passed.\n python3 \"$ROOT/scripts/shiki.py\" goal create --target \"$TARGET\" --title \"Guardian gate\" --outcome \"High risk merges via policy gate\" >/tmp/shiki-goal-loop-guardian-goal.json\n GGOAL=\"$(json_get /tmp/shiki-goal-loop-guardian-goal.json goal_id)\"\ndiff --git a/tests/test_adr_references.py b/tests/test_adr_references.py\nindex 51706329c797d5dd9b47e082a333fa83b261d482..db6e693e35f67103935be34f1d9bb4ff7ee60395\n--- a/tests/test_adr_references.py\n+++ b/tests/test_adr_references.py\n@@ -1,7 +1,7 @@\n-\"\"\"Every ``ADR ####`` citation must resolve to exactly one decision record.\n+\"\"\"Every target ADR and Shiki SADR citation resolves in its own namespace.\n \n Background. ``scripts/mergegate_check.py`` justifies ``is_bookkeeping_closeout``\n-with \"ADR 0017\" nine times and ``scripts/guardian_approval_signal.py`` cites\n+with \"SADR-0017\" nine times and ``scripts/guardian_approval_signal.py`` cites\n 0016/0017 seven times, yet for a stretch the numbering in ``docs/adr/`` stopped\n at 0015: the authoring commits lived on a coordinator branch and no task ever\n declared ``docs/adr/...`` in its locks, so nothing carried the records to the\n@@ -11,11 +11,12 @@\n and the directory drifted with no signal.\n \n This is the same unbound-pair shape behind other silent failures: a reference\n-and its target with nothing asserting they agree. This test binds them. It scans\n-every ``scripts/*.py`` (plus ``docs/**/*.md`` and the root ``*.md`` governance\n-files, which are cheap) for the ``ADR ####`` pattern and asserts each cited\n-number resolves to exactly one ``docs/adr/####-*.md``. The converse is\n-deliberately NOT required: an ADR may exist without being cited.\n+and its target with nothing asserting they agree. This test binds them. Target\n+``ADR NNNN`` citations resolve only to ``docs/adr/NNNN-*.md``; Shiki\n+``SADR-NNNN`` citations resolve only to ``docs/adr/SADR-NNNN-*.md``. The scan\n+covers current governance docs, scripts, workflows, prompts, packaging metadata,\n+and repo-local Codex guidance while deliberately excluding historical\n+``.shiki`` evidence. The converse is not required: a record may be uncited.\n \n The suite is dependency free: standard-library ``unittest`` only.\n \"\"\"\n@@ -31,17 +32,18 @@\n \n REPO_ROOT = Path(__file__).resolve().parents[1]\n \n-# \"ADR 0016\" -- a four-digit citation. The trailing \\b keeps a five-digit token\n-# from being read as a truncated four-digit number.\n-_CITATION = re.compile(r\"\\bADR (\\d{4})\\b\")\n-# \"0016-locks-bound-product-paths.md\" -- the leading four digits name the record.\n-_ADR_FILENAME = re.compile(r\"^(\\d{4})-.*\\.md$\")\n+# ``ADR 0016`` / ``SADR-0016`` -- a namespace plus four-digit citation. The\n+# trailing ``\\b`` keeps a five-digit token from being read as a truncated one.\n+_CITATION = re.compile(r\"\\b(S?ADR)[ -](\\d{4})\\b\")\n+# Target ADRs retain the numeric filename; Shiki platform decisions use SADR.\n+_DECISION_FILENAME = re.compile(r\"^(?:(SADR)-)?(\\d{4})-.*\\.md$\")\n \n \n class Violation(NamedTuple):\n     \"\"\"A citation that does not resolve to exactly one decision record.\"\"\"\n \n     citing_file: str  # repo-relative path of the file that cites the ADR\n+    namespace: str  # \"ADR\" for target decisions; \"SADR\" for Shiki decisions\n     number: str  # the cited four-digit number, e.g. \"0016\"\n     line: int  # 1-based line of the citation\n     reason: str  # \"missing\" (zero matches) or \"ambiguous\" (two or more)\n@@ -49,44 +51,59 @@\n \n     @property\n     def expected_path(self) -> str:\n-        return f\"docs/adr/{self.number}-*.md\"\n+        prefix = \"SADR-\" if self.namespace == \"SADR\" else \"\"\n+        return f\"docs/adr/{prefix}{self.number}-*.md\"\n \n     def describe(self) -> str:\n         \"\"\"Name the citing file, the cited number, and the missing path.\"\"\"\n         if self.reason == \"missing\":\n             return (\n-                f\"{self.citing_file}:{self.line} cites ADR {self.number} \"\n+                f\"{self.citing_file}:{self.line} cites {self.namespace} {self.number} \"\n                 f\"but no document matches {self.expected_path}\"\n             )\n         joined = \", \".join(self.matches)\n         return (\n-            f\"{self.citing_file}:{self.line} cites ADR {self.number} which \"\n+            f\"{self.citing_file}:{self.line} cites {self.namespace} {self.number} which \"\n             f\"resolves to {len(self.matches)} documents (expected exactly one \"\n             f\"at {self.expected_path}): {joined}\"\n         )\n \n \n-def _adr_index(root: Path) -> dict[str, list[Path]]:\n-    \"\"\"Map every four-digit ADR number to the record files that claim it.\"\"\"\n-    index: dict[str, list[Path]] = {}\n+def _adr_index(root: Path) -> dict[tuple[str, str], list[Path]]:\n+    \"\"\"Map each decision namespace and number to the files that claim it.\"\"\"\n+    index: dict[tuple[str, str], list[Path]] = {}\n     adr_dir = root / \"docs\" / \"adr\"\n     if adr_dir.is_dir():\n         for path in sorted(adr_dir.glob(\"*.md\")):\n-            match = _ADR_FILENAME.match(path.name)\n+            match = _DECISION_FILENAME.match(path.name)\n             if match:\n-                index.setdefault(match.group(1), []).append(path)\n+                namespace = \"SADR\" if match.group(1) else \"ADR\"\n+                index.setdefault((namespace, match.group(2)), []).append(path)\n     return index\n \n \n def _citing_files(root: Path, include_docs: bool = True) -> list[Path]:\n-    \"\"\"Files scanned for citations: scripts/*.py, and cheaply the markdown.\"\"\"\n+    \"\"\"Current, shipped text surfaces scanned for decision citations.\"\"\"\n     files = sorted((root / \"scripts\").glob(\"*.py\"))\n+    files += sorted((root / \"scripts\").glob(\"*.sh\"))\n     if include_docs:\n         docs = root / \"docs\"\n         if docs.is_dir():\n             files += sorted(docs.rglob(\"*.md\"))\n         files += sorted(root.glob(\"*.md\"))\n-    return files\n+        pyproject = root / \"pyproject.toml\"\n+        if pyproject.is_file():\n+            files.append(pyproject)\n+        for relative in (\".github\", \".codex\"):\n+            surface = root / relative\n+            if surface.is_dir():\n+                files += sorted(\n+                    path\n+                    for path in surface.rglob(\"*\")\n+                    if path.is_file()\n+                    and path.suffix in {\".md\", \".py\", \".sh\", \".toml\", \".yaml\", \".yml\"}\n+                )\n+    return sorted(set(files))\n \n \n def find_adr_reference_violations(\n@@ -94,10 +111,9 @@\n ) -> list[Violation]:\n     \"\"\"Return every ``ADR ####`` citation under ``root`` that fails to resolve.\n \n-    A citation resolves when exactly one ``docs/adr/####-*.md`` matches its\n-    number. Zero matches is a \"missing\" violation; two or more is \"ambiguous\".\n-    Records that are never cited produce nothing -- the converse is not\n-    required.\n+    A citation resolves when exactly one record in the same namespace matches\n+    its number. Zero matches is a \"missing\" violation; two or more is\n+    \"ambiguous\". Records that are never cited produce nothing.\n     \"\"\"\n     root = Path(root)\n     index = _adr_index(root)\n@@ -110,13 +126,15 @@\n         rel = path.relative_to(root).as_posix()\n         for lineno, line in enumerate(text.splitlines(), start=1):\n             for match in _CITATION.finditer(line):\n-                number = match.group(1)\n-                found = index.get(number, [])\n+                namespace, number = match.groups()\n+                found = index.get((namespace, number), [])\n                 if len(found) == 1:\n                     continue\n                 reason = \"missing\" if not found else \"ambiguous\"\n                 rels = [p.relative_to(root).as_posix() for p in found]\n-                violations.append(Violation(rel, number, lineno, reason, rels))\n+                violations.append(\n+                    Violation(rel, namespace, number, lineno, reason, rels)\n+                )\n     return violations\n \n \n@@ -129,8 +147,8 @@\n         self.assertEqual(\n             violations,\n             [],\n-            \"Every 'ADR ####' citation must resolve to exactly one \"\n-            f\"docs/adr/####-*.md:\\n{detail}\",\n+            \"Every ADR/SADR citation must resolve exactly once in its own \"\n+            f\"docs/adr namespace:\\n{detail}\",\n         )\n \n     def test_scan_actually_reads_scripts(self) -> None:\n@@ -146,8 +164,21 @@\n         # The two records this Goal carried onto the default branch, and which\n         # the scripts cite most, must be present.\n         index = _adr_index(REPO_ROOT)\n-        self.assertIn(\"0016\", index)\n-        self.assertIn(\"0017\", index)\n+        self.assertIn((\"SADR\", \"0016\"), index)\n+        self.assertIn((\"SADR\", \"0017\"), index)\n+\n+    def test_platform_tree_has_only_sadr_0001_through_0019(self) -> None:\n+        adr_dir = REPO_ROOT / \"docs\" / \"adr\"\n+        sadrs = sorted(adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\"))\n+        self.assertEqual(\n+            {path.name.split(\"-\", 2)[1] for path in sadrs},\n+            {f\"{number:04d}\" for number in range(1, 20)},\n+        )\n+        self.assertEqual(\n+            list(adr_dir.glob(\"[0-9][0-9][0-9][0-9]-*.md\")),\n+            [],\n+            \"the Shiki platform tree must not retain legacy numeric SADR paths\",\n+        )\n \n \n class FixtureAdrReferences(unittest.TestCase):\n@@ -180,6 +211,26 @@\n         self.assertIn(\"9991\", message)  # cited number\n         self.assertIn(\"docs/adr/9991-*.md\", message)  # missing path\n \n+    def test_target_adr_does_not_satisfy_missing_platform_sadr(self) -> None:\n+        root = self._make_repo(\n+            adr_names=[\"0042-target-decision.md\"],\n+            script_body='\"\"\"Target ADR 0042 exists, but platform SADR-0042 does not.\"\"\"\\n',\n+        )\n+        violations = find_adr_reference_violations(root, include_docs=False)\n+        self.assertEqual(len(violations), 1)\n+        self.assertEqual(violations[0].namespace, \"SADR\")\n+        self.assertEqual(violations[0].number, \"0042\")\n+        self.assertEqual(violations[0].expected_path, \"docs/adr/SADR-0042-*.md\")\n+\n+    def test_target_adr_and_platform_sadr_may_reuse_a_number(self) -> None:\n+        root = self._make_repo(\n+            adr_names=[\"0042-target-decision.md\", \"SADR-0042-platform-decision.md\"],\n+            script_body='\"\"\"Target ADR 0042 and platform SADR-0042 are distinct.\"\"\"\\n',\n+        )\n+        self.assertEqual(\n+            find_adr_reference_violations(root, include_docs=False), []\n+        )\n+\n     def test_uncited_adr_is_allowed(self) -> None:\n         # An ADR exists but nothing cites it -> not a violation. The converse\n         # (every ADR must be cited) is explicitly NOT required.\ndiff --git a/tests/test_policy_job_merged_prs.py b/tests/test_policy_job_merged_prs.py\nindex d45512e74681e5e649279236c7d4e79d5c3da471..b97989f8bd74d091de1a6b5275a20d8dc68c4512\n--- a/tests/test_policy_job_merged_prs.py\n+++ b/tests/test_policy_job_merged_prs.py\n@@ -3,8 +3,8 @@\n \n ``.github/workflows/shiki-cca-completion.yml`` has two consumers of the merged-PR\n proof: the CCA *signal* (``guardian_approval_signal.py`` in \"Prepare CCA context\")\n-and the authoritative gate (``mergegate_check.py`` in \"Enforce MergeGate\"). ADR\n-0017's bookkeeping-closeout exemption (condition 6) needs the merged implementation\n+and the authoritative gate (``mergegate_check.py`` in \"Enforce MergeGate\").\n+SADR-0017's bookkeeping-closeout exemption (condition 6) needs the merged implementation\n PR proven, and a closeout carries NO ``post_merge_reconcile`` marker. The signal\n already resolves ``MERGED_PRS`` unconditionally, but \"Enforce MergeGate\" used to\n resolve it only inside ``grep -q '<!-- shiki:post_merge_reconcile -->'``. So a\ndiff --git a/tests/test_sadr_install_migration.py b/tests/test_sadr_install_migration.py\nnew file mode 100644\nindex 0000000000000000000000000000000000000000..5a442fa5e4968ade178dcdedc3c63b54dba580a2\n--- /dev/null\n+++ b/tests/test_sadr_install_migration.py\n@@ -0,0 +1,224 @@\n+\"\"\"SADR installation and collision-safe legacy upgrade behavior.\"\"\"\n+\n+from __future__ import annotations\n+\n+import contextlib\n+import hashlib\n+import io\n+import json\n+import shutil\n+import tempfile\n+import unittest\n+from pathlib import Path\n+\n+import shiki_test_support  # noqa: F401  (path bootstrap)\n+\n+import shiki_installer\n+from tests.test_adr_references import find_adr_reference_violations\n+\n+\n+PRODUCT_NUMBERS = tuple(range(13, 19))\n+\n+\n+def _quiet_install(target: Path, *, force: bool, validate: bool) -> None:\n+    with contextlib.redirect_stdout(io.StringIO()):\n+        shiki_installer.install_template(target, force=force, validate=validate)\n+\n+\n+def _sha256(path: Path) -> str:\n+    return hashlib.sha256(path.read_bytes()).hexdigest()\n+\n+\n+def _legacy_sadr_paths() -> tuple[str, ...]:\n+    return tuple(\n+        f\"docs/adr/{path.name.removeprefix('SADR-')}\"\n+        for path in sorted((shiki_installer.ROOT / \"docs\" / \"adr\").glob(\"SADR-*.md\"))\n+        if path.name != \"SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\"\n+    )\n+\n+\n+def _create_legacy_upgrade_fixture(\n+    target: Path,\n+) -> tuple[dict[str, bytes], tuple[str, ...]]:\n+    adr_dir = target / \"docs\" / \"adr\"\n+    adr_dir.mkdir(parents=True)\n+    product_bytes = {\n+        f\"{number:04d}-cubeshare-product-decision.md\": (\n+            f\"# CubeShare ADR {number:04d}\\n\\nDo not rewrite {number}.\\n\"\n+        ).encode()\n+        for number in PRODUCT_NUMBERS\n+    }\n+    for name, content in product_bytes.items():\n+        (adr_dir / name).write_bytes(content)\n+\n+    legacy_paths = _legacy_sadr_paths()\n+    for relative in legacy_paths:\n+        (target / relative).write_bytes(f\"legacy Shiki bytes for {relative}\\n\".encode())\n+\n+    stamp_path = target / shiki_installer.INSTALL_STAMP_PATH\n+    stamp_path.parent.mkdir(parents=True, exist_ok=True)\n+    stamp = {\n+        \"version\": shiki_installer.INSTALL_STAMP_VERSION,\n+        \"platform_commit\": None,\n+        \"installed_at\": \"2000-01-01T00:00:00+00:00\",\n+        \"digests\": {\n+            relative: _sha256(target / relative) for relative in legacy_paths\n+        },\n+    }\n+    stamp_path.write_text(\n+        json.dumps(stamp, indent=2, sort_keys=True) + \"\\n\", encoding=\"utf-8\"\n+    )\n+    return product_bytes, legacy_paths\n+\n+\n+def _tree_snapshot(root: Path) -> dict[str, tuple[str, bytes | str]]:\n+    \"\"\"Recursive byte/shape snapshot used to prove preflight is write-free.\"\"\"\n+    snapshot: dict[str, tuple[str, bytes | str]] = {}\n+    for path in sorted(root.rglob(\"*\")):\n+        relative = path.relative_to(root).as_posix()\n+        if path.is_symlink():\n+            snapshot[relative] = (\"symlink\", path.readlink().as_posix())\n+        elif path.is_dir():\n+            snapshot[relative] = (\"directory\", \"\")\n+        else:\n+            snapshot[relative] = (\"file\", path.read_bytes())\n+    return snapshot\n+\n+\n+class FreshSadrInstallTests(unittest.TestCase):\n+    def setUp(self) -> None:\n+        self.tmp = Path(tempfile.mkdtemp(prefix=\"shiki-sadr-fresh-\"))\n+        self.addCleanup(lambda: shutil.rmtree(self.tmp, ignore_errors=True))\n+        self.target = self.tmp / \"target\"\n+        self.adr_dir = self.target / \"docs\" / \"adr\"\n+        self.adr_dir.mkdir(parents=True)\n+        self.product_bytes = {\n+            f\"{number:04d}-product-decision.md\": (\n+                f\"# Product ADR {number:04d}\\n\\nTarget-owned bytes {number}.\\n\"\n+            ).encode()\n+            for number in PRODUCT_NUMBERS\n+        }\n+        for name, content in self.product_bytes.items():\n+            (self.adr_dir / name).write_bytes(content)\n+\n+    def test_fresh_install_preserves_product_adrs_and_installs_all_sadrs(self) -> None:\n+        _quiet_install(self.target, force=False, validate=True)\n+\n+        for name, expected in self.product_bytes.items():\n+            self.assertEqual((self.adr_dir / name).read_bytes(), expected)\n+\n+        sadr_numbers = {\n+            path.name.split(\"-\", 2)[1]\n+            for path in self.adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\")\n+        }\n+        self.assertEqual(sadr_numbers, {f\"{number:04d}\" for number in range(1, 20)})\n+\n+        numeric_records = {\n+            path.name\n+            for path in self.adr_dir.glob(\"[0-9][0-9][0-9][0-9]-*.md\")\n+        }\n+        self.assertEqual(numeric_records, set(self.product_bytes))\n+        self.assertEqual(find_adr_reference_violations(self.target), [])\n+\n+\n+class LegacySadrUpgradeTests(unittest.TestCase):\n+    def setUp(self) -> None:\n+        self.tmp = Path(tempfile.mkdtemp(prefix=\"shiki-sadr-upgrade-\"))\n+        self.addCleanup(lambda: shutil.rmtree(self.tmp, ignore_errors=True))\n+        self.target = self.tmp / \"target\"\n+        self.adr_dir = self.target / \"docs\" / \"adr\"\n+        self.product_bytes, self.legacy_paths = _create_legacy_upgrade_fixture(\n+            self.target\n+        )\n+\n+    def test_force_upgrade_deletes_only_stamped_legacy_sadrs(self) -> None:\n+        _quiet_install(self.target, force=True, validate=True)\n+\n+        for relative in self.legacy_paths:\n+            self.assertFalse((self.target / relative).exists(), relative)\n+        for name, expected in self.product_bytes.items():\n+            self.assertEqual((self.adr_dir / name).read_bytes(), expected)\n+\n+        sadrs = list(self.adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\"))\n+        self.assertEqual(len(sadrs), 19)\n+        refreshed = json.loads(\n+            (self.target / shiki_installer.INSTALL_STAMP_PATH).read_text(encoding=\"utf-8\")\n+        )\n+        for relative in self.legacy_paths:\n+            self.assertNotIn(relative, refreshed[\"digests\"])\n+        for path in sadrs:\n+            relative = path.relative_to(self.target).as_posix()\n+            self.assertEqual(refreshed[\"digests\"][relative], _sha256(path))\n+\n+    def test_non_force_install_never_deletes_legacy_files(self) -> None:\n+        before = {\n+            relative: (self.target / relative).read_bytes()\n+            for relative in self.legacy_paths\n+        }\n+        _quiet_install(self.target, force=False, validate=False)\n+        for relative, expected in before.items():\n+            self.assertEqual((self.target / relative).read_bytes(), expected)\n+\n+\n+class LegacyCleanupPreflightTests(unittest.TestCase):\n+    def setUp(self) -> None:\n+        self.tmp = Path(tempfile.mkdtemp(prefix=\"shiki-sadr-preflight-\"))\n+        self.addCleanup(lambda: shutil.rmtree(self.tmp, ignore_errors=True))\n+\n+    def test_all_ownership_blockers_abort_before_any_write(self) -> None:\n+        cases = (\n+            (\"absent\", \"install stamp is absent\"),\n+            (\"unreadable\", \"install stamp is unreadable\"),\n+            (\"malformed\", \"install stamp is malformed\"),\n+            (\"missing-digest\", \"install stamp has no digest\"),\n+            (\"digest-mismatch\", \"digest mismatch\"),\n+        )\n+        for name, expected_reason in cases:\n+            with self.subTest(name=name):\n+                target = self.tmp / name\n+                _, legacy_paths = _create_legacy_upgrade_fixture(target)\n+                stamp_path = target / shiki_installer.INSTALL_STAMP_PATH\n+\n+                if name == \"absent\":\n+                    stamp_path.unlink()\n+                elif name == \"unreadable\":\n+                    stamp_path.unlink()\n+                    stamp_path.mkdir()\n+                elif name == \"malformed\":\n+                    stamp_path.write_text(\"{not-json\\n\", encoding=\"utf-8\")\n+                else:\n+                    stamp = json.loads(stamp_path.read_text(encoding=\"utf-8\"))\n+                    if name == \"missing-digest\":\n+                        stamp[\"digests\"] = {}\n+                    else:\n+                        stamp[\"digests\"] = {\n+                            relative: \"deadbeef\" for relative in legacy_paths\n+                        }\n+                    stamp_path.write_text(\n+                        json.dumps(stamp, indent=2, sort_keys=True) + \"\\n\",\n+                        encoding=\"utf-8\",\n+                    )\n+\n+                before = _tree_snapshot(target)\n+                with self.assertRaises(shiki_installer.ShikiError) as raised:\n+                    _quiet_install(target, force=True, validate=False)\n+                after = _tree_snapshot(target)\n+\n+                self.assertEqual(before, after, f\"{name} preflight changed the tree\")\n+                message = str(raised.exception)\n+                self.assertIn(expected_reason, message)\n+                self.assertIn(\"No files were written\", message)\n+                for relative in legacy_paths:\n+                    self.assertIn(relative, message)\n+\n+    def test_absent_stamp_is_allowed_when_no_legacy_path_exists(self) -> None:\n+        target = self.tmp / \"no-legacy\"\n+        target.mkdir()\n+        _quiet_install(target, force=True, validate=False)\n+        self.assertTrue(\n+            (target / \"docs\" / \"adr\" / \"SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\").is_file()\n+        )\n+\n+\n+if __name__ == \"__main__\":\n+    unittest.main()\n\n2026-08-07T08:14:26.352899Z ERROR codex_models_manager::manager: failed to renew cache TTL: missing field `base_instructions` at line 94 column 5\napply patch\npatch: completed\n/private/tmp/shiki-adr-collision.pa0KwY/.worktrees/shiki-t-20260807t073307221254z-2d22d654-introduc/AGENTS.md\n/private/tmp/shiki-adr-collision.pa0KwY/.worktrees/shiki-t-20260807t073307221254z-2d22d654-introduc/SYSTEM_PROMPT.md\n/private/tmp/shiki-adr-collision.pa0KwY/.worktrees/shiki-t-20260807t073307221254z-2d22d654-introduc/docs/agents/execution-control.md\n/private/tmp/shiki-adr-collision.pa0KwY/.worktrees/shiki-t-20260807t073307221254z-2d22d654-introduc/scripts/shiki_guardian.py\n/private/tmp/shiki-adr-collision.pa0KwY/.worktrees/shiki-t-20260807t073307221254z-2d22d654-introduc/scripts/shiki_guardian_review.py\n/private/tmp/shiki-adr-collision.pa0KwY/.worktrees/shiki-t-20260807t073307221254z-2d22d654-introduc/scripts/shiki_tasks.py\n/private/tmp/shiki-adr-collision.pa0KwY/.worktrees/shiki-t-20260807t073307221254z-2d22d654-introduc/tests/test_guardian_review.py\ndiff --git a/.codex/skills/shiki/SKILL.md b/.codex/skills/shiki/SKILL.md\nindex c8e2e85d9d75d49cc4f2d6286ee22659c4e53e75..93cded623b2c15d9f65c76491a1c2d813ac9e6c0\n--- a/.codex/skills/shiki/SKILL.md\n+++ b/.codex/skills/shiki/SKILL.md\n@@ -18,6 +18,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n Codex CLI does not register this skill as a custom `/shiki` slash command. When\n the user types `/shiki` in Codex CLI and gets \"Unrecognized command\", explain\n that this is expected: invoke Shiki in Codex with natural language such as\n@@ -69,7 +72,7 @@\n \n ## Responsibilities\n \n-- Claude Code implements and repairs by default (`shiki runner claude`, ADR 0008); Codex implements and repairs tasks explicitly assigned to `codex`.\n+- Claude Code implements and repairs by default (`shiki runner claude`, SADR-0008); Codex implements and repairs tasks explicitly assigned to `codex`.\n - Claude Code Action can act as GitHub-side CCA or reviewer.\n - CCA judges completion.\n - MergeGate authorizes state transitions and merge readiness.\ndiff --git a/.github/prompts/cca-completion-check.md b/.github/prompts/cca-completion-check.md\nindex ed203ad5f302ecc81372d8ab27d2b1232ca97497..fd89950528333ef6242e6d5443904f5392ab7e56\n--- a/.github/prompts/cca-completion-check.md\n+++ b/.github/prompts/cca-completion-check.md\n@@ -19,6 +19,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n ## Required Reading\n \n Read only the files needed to judge the PR contract. Prefer this order:\ndiff --git a/.github/prompts/codex-task-handoff.md b/.github/prompts/codex-task-handoff.md\nindex 695e211d76dea1c60e8d5bb360e78487808286a4..59441d576b01a39569722f226c854e5a740e9eb5\n--- a/.github/prompts/codex-task-handoff.md\n+++ b/.github/prompts/codex-task-handoff.md\n@@ -2,7 +2,7 @@\n \n You are Codex Front acting as the Implementer for a Shiki task.\n \n-Follow `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant ADRs, and `docs/agents/codex-handoff.md`.\n+Follow `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant Shiki SADRs and target ADRs, and `docs/agents/codex-handoff.md`.\n \n Rules:\n \ndiff --git a/AGENTS.md b/AGENTS.md\nindex 7f041100d49c2a7525f51cb0f5d696a4a6d1c78c..a06bfbabc6fa48a007389e25e12d7ed523936bed\n--- a/AGENTS.md\n+++ b/AGENTS.md\n@@ -22,7 +22,7 @@\n - **Codex Front** is an optional implementer through Codex App, Codex CLI, Codex IDE extension, or Codex Web signed in with ChatGPT OAuth/subscription auth, dispatched with `shiki runner codex` when a task is explicitly assigned to `codex`.\n - **GitHub CCA** is implemented by Claude Code Action by default, using `CLAUDE_CODE_OAUTH_TOKEN`.\n \n-Do not assume `openai/codex-action`, `OPENAI_API_KEY`, or API-key based Codex automation in the default Shiki path. API-key runners are explicit target-repo extensions and require their own ADR.\n+Do not assume `openai/codex-action`, `OPENAI_API_KEY`, or API-key based Codex automation in the default Shiki path. API-key runners are explicit target-repo extensions and require their own target ADR.\n \n Shiki is not:\n \n@@ -44,10 +44,14 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use\n+`SADR-NNNN-*.md`; target product decisions retain `NNNN-*.md` and the `ADR`\n+namespace.\n+\n Use the canonical source-of-truth order above. Runtime-specific reading orders\n are operational guidance only and must not override or reorder it.\n \n-Conversation state is not durable truth. If a decision matters, put it in GitHub, `.shiki/`, `CONTEXT.md`, or an ADR.\n+Conversation state is not durable truth. If a decision matters, put it in GitHub, `.shiki/`, `CONTEXT.md`, a Shiki SADR, or a target ADR.\n \n If GitHub and `.shiki/` disagree, surface the conflict and prefer GitHub operational state until the mirror is repaired.\n \n@@ -75,8 +79,8 @@\n Every non-trivial change follows this loop:\n \n 1. **Goal**: clarify outcome, completion conditions, non-goals, risk level, and success signals.\n-2. **grill-with-docs**: challenge the plan against domain docs, ADRs, code reality, terminology, and edge scenarios.\n-3. **Context & Impact**: find relevant docs, ADRs, code, dependencies, owners, locks, and verification surfaces.\n+2. **grill-with-docs**: challenge the plan against domain docs, Shiki SADRs, target ADRs, code reality, terminology, and edge scenarios.\n+3. **Context & Impact**: find relevant docs, SADRs, target ADRs, code, dependencies, owners, locks, and verification surfaces.\n 4. **PRD and Spec Freeze**: use `to-prd` when context is settled enough to become durable product/engineering intent. The operator's explicit approval of the PRD is Spec Freeze: the plan records a `spec_freeze` block, and no plan runs without it. Steps 1-4 form Requirements Definition \u2014 one continuous operator dialogue. After Spec Freeze, scope changes require an operator-approved Spec Amendment; non-scope-moving interpretations go to the Assumption Log.\n 5. **Issues**: use `to-issues` to create independently grabbable vertical-slice issues. Prefer AFK slices over HITL slices where possible.\n 6. **Triage**: label issues for readiness, risk, runtime, skills, and MergeGate state.\n@@ -167,7 +171,7 @@\n - Testing decisions.\n - Out-of-scope items.\n - Further notes.\n-- Links to relevant domain terms and ADRs.\n+- Links to relevant domain terms, SADRs, and target ADRs.\n \n Every executable task must include:\n \n@@ -193,7 +197,7 @@\n \n Context & Impact must identify:\n \n-- Relevant documents, domain terms, ADRs, and past decisions.\n+- Relevant documents, domain terms, SADRs, target ADRs, and past decisions.\n - Relevant modules, interfaces, seams, callers, owners, and tests.\n - Dependency relationships between tasks.\n - Candidate locks and lock conflicts.\n@@ -210,7 +214,7 @@\n | Skill | Required when |\n | --- | --- |\n | `setup-matt-pocock-skills` | First configuring the repo for skills, or when issue tracker, triage labels, or domain docs are missing. |\n-| `grill-with-docs` | Default for non-trivial Goals before PRD/issues; always required when plans, terminology, boundaries, tradeoffs, or ADR-worthy decisions are ambiguous. |\n+| `grill-with-docs` | Default for non-trivial Goals before PRD/issues; always required when plans, terminology, boundaries, tradeoffs, or decision-record-worthy choices are ambiguous. |\n | `zoom-out` | The agent lacks an architectural map of the relevant code area. |\n | `to-prd` | Settled Goal context must become durable product/engineering intent. |\n | `to-issues` | A Goal, PRD, or plan must become independently grabbable vertical-slice issues. |\n@@ -348,7 +352,7 @@\n - Three or more new files.\n - Any high-risk or critical label.\n \n-When the architecture gate triggers, run `grill-with-docs` or `improve-codebase-architecture` as appropriate and record durable decisions in an ADR when needed.\n+When the architecture gate triggers, run `grill-with-docs` or `improve-codebase-architecture` as appropriate and record durable decisions in a Shiki SADR or target ADR as applicable.\n \n ## Engineering Discipline\n \ndiff --git a/CLAUDE.md b/CLAUDE.md\nindex 98a932e7841a632866fcd53b0bad0e3af3711d3d..98a31399404d6fb044b4fd493f4459ac87f439de\n--- a/CLAUDE.md\n+++ b/CLAUDE.md\n@@ -14,6 +14,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n ## Claude Role\n \n Default to planner, default implementer, reviewer, coordinator, documentation editor, CCA-style completion judge, and final-judgment assistant.\n@@ -57,7 +60,7 @@\n At the start of a Shiki session:\n \n 1. Read `AGENTS.md`.\n-2. Read `CONTEXT.md` and relevant ADRs.\n+2. Read `CONTEXT.md` and relevant Shiki SADRs and target ADRs.\n 3. Read active `.shiki/` Goal/task/ledger state when present.\n 4. Read active distilled rules from `.shiki/memories/` (status `distilled`, `active: true`, not revoked or superseded) for applicable guidance; the same rules are injected deterministically into task handoffs as the `## Distilled Rules` section (\u00a73.5).\n 5. Read the GitHub Issue or PR when the task is GitHub-backed.\n@@ -74,7 +77,7 @@\n - Define the Goal, completion conditions, non-goals, and risk level.\n - Treat `grill-with-docs` as the default planning entry point for non-trivial Goals.\n - Challenge domain terms against `CONTEXT.md`.\n-- Challenge hard-to-reverse tradeoffs against ADRs.\n+- Challenge hard-to-reverse tradeoffs against SADRs and target ADRs.\n - Explore code instead of asking when code can answer the question.\n - Run Context & Impact before implementation planning.\n - Produce a Task DAG, not an unordered checklist.\n@@ -94,7 +97,7 @@\n - For each question, provide your recommended answer.\n - Resolve dependency decisions before downstream design questions.\n - Update `CONTEXT.md` inline when terminology crystallizes.\n-- Offer ADRs only when the decision is hard to reverse, surprising without context, and a real tradeoff.\n+- Offer a SADR or target ADR only when the decision is hard to reverse, surprising without context, and a real tradeoff.\n - Convert unresolved questions into HITL blockers rather than allowing Codex to guess.\n \n ## PRD and Issues Standard\n@@ -103,7 +106,7 @@\n \n - Use the project's domain glossary vocabulary.\n - Avoid volatile file paths unless they are essential.\n-- Include implementation decisions, testing decisions, out-of-scope items, and relevant ADR/domain links.\n+- Include implementation decisions, testing decisions, out-of-scope items, and relevant SADR/target-ADR/domain links.\n - Identify deep-module opportunities and testable interfaces.\n \n When creating issues:\n@@ -121,7 +124,7 @@\n - Branch/worktree target.\n - Scope and non-goals.\n - Dependencies and locks.\n-- Relevant docs, ADRs, modules, and tests.\n+- Relevant docs, SADRs, target ADRs, modules, and tests.\n - Required skill invocations.\n - TDD expectations.\n - Pre-PR `code-review` gate expectations.\n@@ -152,7 +155,7 @@\n \n - Treat yourself as a judge, not implementer.\n - Do not write production code.\n-- Read `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant ADRs, PRD, task issue, PR body, diff, checks, reviews, labels, and `.shiki/` state.\n+- Read `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant SADRs and target ADRs, PRD, task issue, PR body, diff, checks, reviews, labels, and `.shiki/` state.\n - Evaluate every applicable checklist in `docs/agents/checklists.md`.\n - Map each acceptance criterion to durable evidence.\n - Distinguish implementation failure from missing evidence.\ndiff --git a/CONTEXT.md b/CONTEXT.md\nindex e61a9be06a0cce0d026484083a657bde2067e234..237ee8aa3ea8fab334ece49768bc5ed3b5af0974\n--- a/CONTEXT.md\n+++ b/CONTEXT.md\n@@ -25,7 +25,7 @@\n _Avoid_: jumping directly into code\n \n **grill-with-docs**:\n-A planning skill that challenges a plan against domain language, ADRs, code reality, and concrete edge scenarios. It resolves design-tree questions before PRD/issues. It runs inside Requirements Definition.\n+A planning skill that challenges a plan against domain language, Shiki SADRs, target ADRs, code reality, and concrete edge scenarios. It resolves design-tree questions before PRD/issues. It runs inside Requirements Definition.\n _Avoid_: generic brainstorming, silent assumptions\n \n **Requirements Definition**:\ndiff --git a/CONTRIBUTING.md b/CONTRIBUTING.md\nindex c6d9b656e73abb8a55e5f660123e03153dcb3e6d..9e37a8fbf668db5b22461aaae5548b8f1354394c\n--- a/CONTRIBUTING.md\n+++ b/CONTRIBUTING.md\n@@ -17,7 +17,7 @@\n 1. **Goal** \u2014 start from a user-approved Goal with an outcome, completion\n    conditions, non-goals, risk level, and success signals.\n 2. **grill-with-docs** \u2014 for any non-trivial change, challenge the plan against\n-   domain language, ADRs, and code reality before writing a PRD.\n+   domain language, Shiki SADRs, target ADRs, and code reality before writing a PRD.\n 3. **PRD** \u2014 capture settled product/engineering intent (`to-prd`).\n 4. **Spec Freeze** \u2014 the operator explicitly approves the PRD/requirements;\n    the plan records a `spec_freeze` block and no plan runs without it\n@@ -94,7 +94,7 @@\n   entries.\n - Do not hand-edit `.shiki/goals/*.json` or `.shiki/manifest.json` in a task PR.\n - Conversation state is not durable truth. If a decision matters, record it in\n-  GitHub, `.shiki/`, `CONTEXT.md`, or an ADR.\n+  GitHub, `.shiki/`, `CONTEXT.md`, a Shiki SADR, or a target ADR.\n \n ## Architecture Gate\n \n@@ -103,7 +103,7 @@\n or CI policy, package structure, three or more structural files, more than 100\n net new lines in a core module, three or more new files, or any high-risk label.\n When triggered, run `grill-with-docs` or `improve-codebase-architecture` and\n-record durable decisions in an ADR when needed (see `docs/adr/`).\n+record durable decisions in a Shiki SADR or target ADR as applicable (see `docs/adr/`).\n \n ## Safety\n \ndiff --git a/SYSTEM_PROMPT.md b/SYSTEM_PROMPT.md\nindex aaaea45e3a896c3a44ba89eec8bb84325ecf6c62..da009c86a88285269dee0155e58e550c6d744ea9\n--- a/SYSTEM_PROMPT.md\n+++ b/SYSTEM_PROMPT.md\n@@ -30,6 +30,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n Use the canonical source-of-truth order above. Runtime-specific reading orders\n are operational guidance only and must not override or reorder it. The current\n conversation is non-durable operator input.\n@@ -48,14 +51,14 @@\n \n A runtime may only do work that is explicitly assigned by the Goal, task, PR, or MergeGate decision.\n \n-Do not assume `openai/codex-action`, `OPENAI_API_KEY`, or API-key based Codex automation in the default Shiki path. API-key runners are explicit target-repo extensions and require their own ADR.\n+Do not assume `openai/codex-action`, `OPENAI_API_KEY`, or API-key based Codex automation in the default Shiki path. API-key runners are explicit target-repo extensions and require their own target ADR.\n \n ## 4. Non-negotiable Shiki flow\n \n For any non-trivial Goal, use this flow:\n \n 1. **Goal Seek**: define outcome, non-goals, risk level, completion criteria, and evidence requirements.\n-2. **grill-with-docs**: challenge the plan against `CONTEXT.md`, ADRs, code reality, terminology, boundaries, and edge scenarios. Ask one question at a time when the operator is available. If code can answer a question, inspect code instead of asking.\n+2. **grill-with-docs**: challenge the plan against `CONTEXT.md`, Shiki SADRs, target ADRs, code reality, terminology, boundaries, and edge scenarios. Ask one question at a time when the operator is available. If code can answer a question, inspect code instead of asking.\n 3. **Context & Impact**: identify modules, interfaces, seams, callers, dependencies, locks, risk, and verification surfaces.\n 4. **to-prd + Spec Freeze**: turn settled context into a PRD using domain vocabulary and testing decisions. The operator's explicit approval of the PRD is Spec Freeze, recorded as the plan's `spec_freeze` block; plans without it do not run. Steps 1-4 form Requirements Definition.\n 5. **to-issues**: decompose the PRD into independently grabbable vertical-slice issues. Prefer AFK slices; mark HITL slices when judgment is still required.\n@@ -75,7 +78,7 @@\n Before making any material change:\n \n 1. Read `AGENTS.md` and any runtime-specific wrapper such as `CLAUDE.md`.\n-2. Read `CONTEXT.md` and relevant ADRs.\n+2. Read `CONTEXT.md` and relevant SADRs and target ADRs.\n 3. Read the GitHub Issue / PR and `.shiki/` state for the active Goal or task.\n 4. Identify your role: Planner, Implementer, Reviewer, Completion Check Agent, Repairer, or Guardian-assist.\n 5. Identify Goal id, task id, branch, PR, dependencies, locks, risk level, required skills, and required evidence.\n@@ -103,7 +106,7 @@\n \n At minimum, identify:\n \n-- Relevant documents, ADRs, domain terms, and prior decisions.\n+- Relevant documents, SADRs, target ADRs, domain terms, and prior decisions.\n - Relevant modules, interfaces, seams, callers, owners, and tests.\n - Dependency relationships between tasks.\n - Candidate file locks and lock conflicts.\n@@ -152,7 +155,7 @@\n \n CCA must read:\n \n-- `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant ADRs.\n+- `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant SADRs and target ADRs.\n - Parent Goal issue, PRD issue, task issue, comments, and labels.\n - PR body, diff, commits, changed files, reviews, CI checks, and artifacts available to the workflow.\n - `.shiki/` task contract, locks, ledger entries, CCA checklist profile, and prior repair packets.\n@@ -214,7 +217,7 @@\n - Three or more new files.\n - Any high-risk or critical label.\n \n-When the architecture gate triggers, run `grill-with-docs` or `improve-codebase-architecture` as appropriate and record the decision in an ADR if it is hard to reverse or likely to be re-litigated.\n+When the architecture gate triggers, run `grill-with-docs` or `improve-codebase-architecture` as appropriate and record the decision in a Shiki SADR or target ADR as applicable if it is hard to reverse or likely to be re-litigated.\n \n ## 14. Skill Gate\n \n@@ -223,7 +226,7 @@\n Use these skills:\n \n - `setup-matt-pocock-skills`: before first use of the engineering skill set, or when issue tracker, triage labels, or domain docs are missing.\n-- `grill-with-docs`: default for non-trivial Goals before PRD/issues; required for ambiguous plans, terminology, boundaries, ADR-worthy tradeoffs, or design-tree decisions.\n+- `grill-with-docs`: default for non-trivial Goals before PRD/issues; required for ambiguous plans, terminology, boundaries, decision-record-worthy tradeoffs, or design-tree decisions.\n - `zoom-out`: unfamiliar code area or missing architectural map.\n - `to-prd`: convert settled context into a PRD.\n - `to-issues`: break a PRD, plan, or Goal into independently grabbable vertical-slice issues.\ndiff --git a/docs/adr/0001-keep-shiki-as-platform-name.md b/docs/adr/SADR-0001-keep-shiki-as-platform-name.md\nindex 20a9637c3b9d8c97de7ba8db7cfae4d82d9a7dba..8b685439959aada1b221c112f0ac3c51ca8a1695\n--- a/docs/adr/0001-keep-shiki-as-platform-name.md\n+++ b/docs/adr/SADR-0001-keep-shiki-as-platform-name.md\n@@ -1,3 +1,3 @@\n-# Keep Shiki As The Platform Name\n+# SADR-0001: Keep Shiki As The Platform Name\n \n Shiki remains the product name for the GitHub-native agentic engineering control plane. We considered more descriptive names such as MergePilot, MergeGate, and Kiroku, but kept Shiki because it carries the existing goal-seeking framework identity while remaining independent of Hermes, Codex, Claude Code, or any single runtime.\ndiff --git a/docs/adr/0002-use-agents-as-shared-constitution.md b/docs/adr/SADR-0002-use-agents-as-shared-constitution.md\nindex 73de30ecb078120eac540d9bc51855e3cd48d0e3..c5486182bf06fca36ea508496b013c67302fd989\n--- a/docs/adr/0002-use-agents-as-shared-constitution.md\n+++ b/docs/adr/SADR-0002-use-agents-as-shared-constitution.md\n@@ -1,3 +1,3 @@\n-# Use AGENTS As The Shared Agent Constitution\n+# SADR-0002: Use AGENTS As The Shared Agent Constitution\n \n Shiki uses `AGENTS.md` as the runtime-neutral constitution and `CLAUDE.md` as a thin Claude Code wrapper. This keeps Codex, Claude Code, Hermes Runner, and future Agent Runtimes aligned on the same GitHub-first MergeGate rules while still allowing runtime-specific instructions where needed.\ndiff --git a/docs/adr/0003-require-skill-gate-for-mergegate-readiness.md b/docs/adr/SADR-0003-require-skill-gate-for-mergegate-readiness.md\nindex 424be0c9af4c8f0136f1fde68a468c47ff84e764..7fc337987c7df9792f75f181dfcbed75fe5ffff2\n--- a/docs/adr/0003-require-skill-gate-for-mergegate-readiness.md\n+++ b/docs/adr/SADR-0003-require-skill-gate-for-mergegate-readiness.md\n@@ -1,3 +1,3 @@\n-# Require Skill Gate For MergeGate Readiness\n+# SADR-0003: Require Skill Gate For MergeGate Readiness\n \n Shiki requires relevant engineering skills to be invoked before execution when their triggers apply. This makes skills part of MergeGate readiness rather than optional prompt style, because Shiki depends on repeatable planning, issue decomposition, TDD, diagnosis, triage, and architecture review before autonomous or parallel agent work can be trusted.\ndiff --git a/docs/adr/0004-use-github-first-with-shiki-mirror.md b/docs/adr/SADR-0004-use-github-first-with-shiki-mirror.md\nindex d0a3a9174dc646a945a9d325954ffaf00c352040..a79c54ae9ee8eed2a0524192b864da4e22a92d3f\n--- a/docs/adr/0004-use-github-first-with-shiki-mirror.md\n+++ b/docs/adr/SADR-0004-use-github-first-with-shiki-mirror.md\n@@ -1,4 +1,4 @@\n-# ADR 0004: Use GitHub First With A Repository-Local Shiki Mirror\n+# SADR-0004: Use GitHub First With A Repository-Local Shiki Mirror\n \n ## Status\n \ndiff --git a/docs/adr/0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md b/docs/adr/SADR-0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\nindex b339baf49b89d9290e79c3d51b36e7b2bde3bd95..c30a0d8b1fde0224d0ac7820e7870dccf721580d\n--- a/docs/adr/0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\n+++ b/docs/adr/SADR-0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\n@@ -1,4 +1,4 @@\n-# ADR 0005: Use Codex As Front And Claude Code Action As The GitHub Runtime\n+# SADR-0005: Use Codex As Front And Claude Code Action As The GitHub Runtime\n \n ## Status\n \ndiff --git a/docs/adr/0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md b/docs/adr/SADR-0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\nindex 4bcae9d1e52f0a37b8e9fbafa5cd14b6ee1993bd..7a38538a6ae9929567f0ade67e7ea6a60d0b3f7e\n--- a/docs/adr/0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\n+++ b/docs/adr/SADR-0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\n@@ -1,4 +1,4 @@\n-# ADR 0006: Separate Codex Implementation, CCA Completion, And MergeGate Authorization\n+# SADR-0006: Separate Codex Implementation, CCA Completion, And MergeGate Authorization\n \n ## Status\n \ndiff --git a/docs/adr/0007-packaging-and-release.md b/docs/adr/SADR-0007-packaging-and-release.md\nindex 79b84ddb97167372b44641e5b7860e400ce80822..bb445a2e2e856d9d1f5fdbe35de8d4e8cec0fb32\n--- a/docs/adr/0007-packaging-and-release.md\n+++ b/docs/adr/SADR-0007-packaging-and-release.md\n@@ -1,4 +1,4 @@\n-# ADR 0007: Packaging And Release For Shiki\n+# SADR-0007: Packaging And Release For Shiki\n \n ## Status\n \n@@ -25,7 +25,7 @@\n   not introduce third-party runtime dependencies or break the shim.\n - Releases must run inside the operator's subscription-authenticated toolchain\n   and must not require extra secrets beyond the built-in `GITHUB_TOKEN`\n-  (consistent with ADR 0005's no-API-key-by-default stance).\n+  (consistent with SADR-0005's no-API-key-by-default stance).\n - `.shiki/manifest.json` already encodes the install surface (`install.include`,\n   `install.create_directories`, `install.exclude_from_commit`), and it must not\n   be mutated as part of this change.\n@@ -34,7 +34,7 @@\n \n This is a hard-to-reverse platform decision (it sets the version source of\n truth, the release trigger, and the public install contract), so it is recorded\n-as an ADR.\n+as a SADR.\n \n ## Decision\n \n@@ -104,7 +104,7 @@\n 3. **Publishing to PyPI.** Rejected for now: Shiki is distributed as a\n    repository template + git-installable CLI, not a PyPI library; PyPI would add\n    credentials/secrets and a different versioning contract. Can be added later as\n-   its own ADR if a packaged distribution is needed.\n+   its own SADR if a packaged distribution is needed.\n 4. **Dynamic version (read `VERSION` via a build backend `dynamic` field).**\n    Rejected to keep `pyproject.toml` backend-agnostic and dependency-free; the\n    small duplication between `VERSION` and `pyproject.toml` is guarded by the\n@@ -123,4 +123,4 @@\n   releasing versions the whole platform but ships only `install.include` to\n   targets.\n - Future PyPI distribution or a console entry point are deferred and would each\n-  warrant their own ADR.\n+  warrant their own SADR.\ndiff --git a/docs/adr/0008-use-claude-code-as-default-implementer.md b/docs/adr/SADR-0008-use-claude-code-as-default-implementer.md\nindex dedf4510b7c7799521422f622029563d373db031..35c2625d908167fbc58855b5b63e283805f167e8\n--- a/docs/adr/0008-use-claude-code-as-default-implementer.md\n+++ b/docs/adr/SADR-0008-use-claude-code-as-default-implementer.md\n@@ -1,12 +1,12 @@\n-# ADR 0008: Use Claude Code As The Default Implementer Runtime\n+# SADR-0008: Use Claude Code As The Default Implementer Runtime\n \n ## Status\n \n-Proposed (supersedes ADR 0005 for the implementer default; keeps its auth model)\n+Proposed (supersedes SADR-0005 for the implementer default; keeps its auth model)\n \n ## Context\n \n-ADR 0005 made Codex Front the default implementation runtime, with Claude Code\n+SADR-0005 made Codex Front the default implementation runtime, with Claude Code\n restricted to planner and reviewer roles by the runtime registry, and the only\n autonomous implementation adapter was `shiki runner codex`.\n \n@@ -18,13 +18,13 @@\n - The platform's planning, review, and CCA layers already run on Claude Code,\n   and Claude Code's native capabilities (the built-in `/code-review` skill and\n   the Workflow multi-agent orchestration tool) have no Codex equivalent.\n-- The Goal lifecycle redesign (ADR 0009) requires an autonomous post-freeze\n+- The Goal lifecycle redesign (SADR-0009) requires an autonomous post-freeze\n   loop driven from the operator's Claude Code session, including bounded\n   repair dispatch, which must not depend on a runtime that cannot be invoked\n   unattended.\n \n The constitution claims Shiki is runtime-agnostic (\"not a Claude-only\n-workflow\", ADR 0002). Any change to the implementer default must preserve the\n+workflow\", SADR-0002). Any change to the implementer default must preserve the\n registry-based runtime model rather than hardcoding one vendor.\n \n ## Decision\n@@ -39,7 +39,7 @@\n   the task to review or repair-needed.\n - Keep Codex registered as an optional implementer runtime. `shiki runner\n   codex`, the Codex handoff templates, and the ChatGPT OAuth auth model from\n-  ADR 0005 remain supported for tasks explicitly assigned to `codex`.\n+  SADR-0005 remain supported for tasks explicitly assigned to `codex`.\n - Commit to mandating two Claude-native capabilities as loop contract in\n   follow-up Goals (status: the code-review gate was delivered by Goal G-B \u2014\n   skill registry, checklists item PR-12, default required_skills \u2014 while the\n@@ -60,7 +60,7 @@\n \n Out of scope: removing Codex support, changing the CCA runtime (Claude Code\n Action with `CLAUDE_CODE_OAUTH_TOKEN` stays), and API-key based automation\n-(still requires its own ADR per ADR 0005).\n+(still requires its own target ADR per SADR-0005).\n \n ## Consequences\n \n@@ -100,7 +100,7 @@\n   harness rejection of direct `codex exec` is outside Shiki's control, and the\n   plan's mandatory native capabilities cannot run on Codex at all.\n - **Remove Codex entirely**: rejected; it abandons the runtime-agnostic\n-  constitution (ADR 0002) for no operational gain and forecloses\n+  constitution (SADR-0002) for no operational gain and forecloses\n   cross-runtime adversarial review.\n - **GitHub Actions cloud implementation via claude-code-action**: rejected as\n   default; durable evidence is automatic but token cost, Node 24 workflow\ndiff --git a/docs/adr/0009-requirements-definition-and-spec-freeze.md b/docs/adr/SADR-0009-requirements-definition-and-spec-freeze.md\nindex 29b3f7871bef9203a8b4a831a65c39eb2f506e42..52e7dd69ad0aa4efe0b68c62678abbf7df02aaec\n--- a/docs/adr/0009-requirements-definition-and-spec-freeze.md\n+++ b/docs/adr/SADR-0009-requirements-definition-and-spec-freeze.md\n@@ -1,4 +1,4 @@\n-# ADR 0009: Merge Grill And Planning Into Requirements Definition With Spec Freeze\n+# SADR-0009: Merge Grill And Planning Into Requirements Definition With Spec Freeze\n \n ## Status\n \ndiff --git a/docs/adr/0010-external-ai-guardian-review.md b/docs/adr/SADR-0010-external-ai-guardian-review.md\nindex e6108bbe8bb4e73e0ff4684c8f78001a80438c4e..f3510b145664298adafb151b6f0db51b972f01ec\n--- a/docs/adr/0010-external-ai-guardian-review.md\n+++ b/docs/adr/SADR-0010-external-ai-guardian-review.md\n@@ -1,4 +1,4 @@\n-# ADR 0010: External AI Guardian Review As A First-Class Approval Authority\n+# SADR-0010: External AI Guardian Review As A First-Class Approval Authority\n \n ## Status\n \n@@ -8,7 +8,7 @@\n \n Shiki is intended to be an autonomous, recursive, self-improving agent\n framework: authority-in-the-loop, not human-in-the-loop. Until now the Guardian\n-approval required for high/critical-risk merges (guardian-policy.json, ADR\n+approval required for high/critical-risk merges (guardian-policy.json, SADR\n governance) recognized only human authority sources \u2014 a GitHub review approval,\n the `guardian:approved` label applied by a configured Guardian, or a comment\n carrying `Guardian approval granted` plus the current head SHA.\n@@ -89,7 +89,7 @@\n   autonomous design the operator intends and produced the impersonation\n   incident.\n - **Record AI approval as an operator comment**: rejected as forgery of human\n-  authority \u2014 the exact failure this ADR corrects.\n+  authority \u2014 the exact failure this SADR corrects.\n - **Deliver the artifact as a committed `.shiki/` file**: rejected; a committed\n   approval inside the PR being approved is forgeable by anyone who can push.\n - **A dedicated reviewer bot identity instead of a relayed comment**: deferred;\ndiff --git a/docs/adr/0011-autonomous-quality-gate-evidence.md b/docs/adr/SADR-0011-autonomous-quality-gate-evidence.md\nindex bca0246c5d46e44afbde15486a75eec425fc02f5..9c93eac16d8c3a6d4b11bd7224e3955102cd7db4\n--- a/docs/adr/0011-autonomous-quality-gate-evidence.md\n+++ b/docs/adr/SADR-0011-autonomous-quality-gate-evidence.md\n@@ -1,4 +1,4 @@\n-# ADR 0011: Autonomous Quality-Gate Evidence \u2014 Deterministic Facts And An Independent Verifier\n+# SADR-0011: Autonomous Quality-Gate Evidence \u2014 Deterministic Facts And An Independent Verifier\n \n ## Status\n \ndiff --git a/docs/adr/0012-autonomous-loop-closeout-to-main.md b/docs/adr/SADR-0012-autonomous-loop-closeout-to-main.md\nindex 637fad6dde4989c9c2bf0f81f676d8d29991123c..ec7d85f81030a676bcbba28b43eb365422a36eac\n--- a/docs/adr/0012-autonomous-loop-closeout-to-main.md\n+++ b/docs/adr/SADR-0012-autonomous-loop-closeout-to-main.md\n@@ -1,4 +1,4 @@\n-# 0012 \u2014 Autonomous loop pushes goal completion to main via a closeout PR\n+# SADR-0012 \u2014 Autonomous loop pushes goal completion to main via a closeout PR\n \n - Status: accepted\n - Date: 2026-06-18\ndiff --git a/docs/adr/0013-bootstrap-grants-actions-approve-pull-requests.md b/docs/adr/SADR-0013-bootstrap-grants-actions-approve-pull-requests.md\nindex b6c69d64f1c5ced346b88d6859586715b3b5bd0e..cf48ff4a6716267f539ca58edd1a852e7c7a2d64\n--- a/docs/adr/0013-bootstrap-grants-actions-approve-pull-requests.md\n+++ b/docs/adr/SADR-0013-bootstrap-grants-actions-approve-pull-requests.md\n@@ -1,4 +1,4 @@\n-# ADR 0013: Bootstrap Grants GitHub Actions Permission To Create And Approve Pull Requests\n+# SADR-0013: Bootstrap Grants GitHub Actions Permission To Create And Approve Pull Requests\n \n ## Status\n \n@@ -31,7 +31,7 @@\n security tradeoff: it lets repository automation satisfy a human-review gate. In\n a multi-maintainer repo that could let a compromised or buggy workflow\n self-approve changes. So whether bootstrap turns it on by default \u2014 and with\n-what default token scope \u2014 is an ADR-worthy decision, not an implementation\n+what default token scope \u2014 is a SADR-worthy decision, not an implementation\n detail.\n \n ## Decision\n@@ -48,7 +48,7 @@\n Specifically:\n \n - **`can_approve_pull_request_reviews` defaults to `true`.** Shiki's default\n-  posture is solo/AFK autonomous operation (ADR 0008/0009/0012), where the CCA\n+  posture is solo/AFK autonomous operation (SADR-0008/0009/0012), where the CCA\n   Review Bridge is the only path that can satisfy `required_review: true` after\n   the CCA and Guardian gates have already passed. The default makes a\n   freshly-bootstrapped repo able to run the autonomous loop end to end without a\ndiff --git a/docs/adr/0014-codex-app-external-ai-guardian-ui-adapter.md b/docs/adr/SADR-0014-codex-app-external-ai-guardian-ui-adapter.md\nindex 238db06279334caa341c117362df9271382a7fcd..65f12e9939321e1b3e389a34c822fc1549e5155d\n--- a/docs/adr/0014-codex-app-external-ai-guardian-ui-adapter.md\n+++ b/docs/adr/SADR-0014-codex-app-external-ai-guardian-ui-adapter.md\n@@ -1,4 +1,4 @@\n-# ADR 0014: Use Codex App As The External AI Guardian UI Adapter\n+# SADR-0014: Use Codex App As The External AI Guardian UI Adapter\n \n ## Status\n \ndiff --git a/docs/adr/0015-contract-approval-before-dispatch.md b/docs/adr/SADR-0015-contract-approval-before-dispatch.md\nindex fad6c656741a20a7684ba16080a7db159f63a52f..6061271a46d3f1d6ae7174477de3fb75673123a6\n--- a/docs/adr/0015-contract-approval-before-dispatch.md\n+++ b/docs/adr/SADR-0015-contract-approval-before-dispatch.md\n@@ -1,4 +1,4 @@\n-# ADR 0015: Approve The Task Contract Before Dispatch, Not The Diff After It\n+# SADR-0015: Approve The Task Contract Before Dispatch, Not The Diff After It\n \n ## Status\n \n@@ -9,7 +9,7 @@\n Guardian approval for high/critical risk is expressible only on a pull request.\n Every approval source in `guardian-policy.json` \u2014 GitHub review, the\n `guardian:approved` label, the `Guardian approval granted` comment, and the\n-`external_ai_guardian_review` artifact (ADR 0010/0014) \u2014 is bound to a PR, and\n+`external_ai_guardian_review` artifact (SADR-0010/0014) \u2014 is bound to a PR, and\n three of the four require the current head SHA. Approval therefore cannot exist\n before an implementation PR exists.\n \n@@ -81,7 +81,7 @@\n An envelope breach must surface only on the non-repairable policy gate, never on\n the repairable `MergeGate metadata check`. Otherwise the Goal Loop routes it to\n `dispatch_repair` \u2014 instructing a runner to make the breach go away \u2014 which is\n-exactly what this ADR forbids.\n+exactly what this SADR forbids.\n \n **Contract immutability.** After registration, an implementation PR must not\n change the governance fields of its own task contract (`scope`, `non_goals`,\n@@ -116,7 +116,7 @@\n fails any verdict that is not `complete`, and the signal the CCA prompt reads is\n computed from `evaluate_guardian_approval` alone. Without the OR-merge at the\n signal, a contract-approved PR still returns `needs_guardian`, the CCA job fails,\n-and the MergeGate policy check never runs \u2014 the gate this ADR changes would never\n+and the MergeGate policy check never runs \u2014 the gate this SADR changes would never\n be reached. The CCA *prompt* needs no change: a `contract_approval` entry in the\n signal's existing `sources` array satisfies its current wording.\n \n@@ -130,7 +130,7 @@\n written, and an approved contract lets its implementation PRs merge through the\n same automatic path as low/medium risk. The wasted implement-then-block cycle\n disappears. Guardian review shifts from reading diffs to reading contracts, which\n-is both earlier and smaller; the external AI Guardian path (ADR 0010/0014) works\n+is both earlier and smaller; the external AI Guardian path (SADR-0010/0014) works\n on contracts with no change.\n \n We accept that the Guardian authorizes a class of change without seeing its\n@@ -146,7 +146,7 @@\n adversarial tests for the new approval path equivalent in rigor to\n `test_shiki_governance_evidence.sh`.\n \n-Bootstrap costs, which apply only while this ADR is being implemented. The tasks\n+Bootstrap costs, which apply only while this SADR is being implemented. The tasks\n that build Contract Approval merge under the existing rules, so the ones\n dispatched after the pre-dispatch gate lands but before the approval path lands\n need a Guardian-approved Contract PR *and* a live Guardian approval on their\n@@ -210,7 +210,7 @@\n risk; they do not bound authorized work.\n \n **Auto-escalate risk on envelope breach instead of stopping.** Returns the\n-workflow to a diff-time Guardian decision \u2014 the exact behavior this ADR removes.\n+workflow to a diff-time Guardian decision \u2014 the exact behavior this SADR removes.\n \n **Resolve governance fields from the base snapshot only.** Rejected against the\n registration flow: implementation PRs add their own contract, so the base has no\ndiff --git a/docs/adr/0016-locks-bound-product-paths.md b/docs/adr/SADR-0016-locks-bound-product-paths.md\nindex b5e850f3f1f7f4ef6e7f9d1763a29a51bd300c45..46b40a9b99a7aae644c02bb458f5473ff0a90ce2\n--- a/docs/adr/0016-locks-bound-product-paths.md\n+++ b/docs/adr/SADR-0016-locks-bound-product-paths.md\n@@ -1,4 +1,4 @@\n-# ADR 0016: Locks bound product paths; the Shiki mirror is bound by record identity\n+# SADR-0016: Locks bound product paths; the Shiki mirror is bound by record identity\n \n ## Status\n \n@@ -6,8 +6,8 @@\n \n - Date: 2026-07-28\n - Deciders: operator (mizutani-140), Claude (planner)\n-- Related: ADR 0004 (GitHub-first with a `.shiki` mirror), ADR 0009 (Spec Freeze),\n-  ADR 0012 (autonomous loop closeout to main), ADR 0015 (Contract Approval before\n+- Related: SADR-0004 (GitHub-first with a `.shiki` mirror), SADR-0009 (Spec Freeze),\n+  SADR-0012 (autonomous loop closeout to main), SADR-0015 (Contract Approval before\n   dispatch), remediation goal task R-02, the live PR #179 deadlock.\n \n ## Context\n@@ -90,11 +90,11 @@\n files, documentation files and named source modules are nameable; runtime-\n allocated ledger, runner and report ids are not \u2014 and those live in `.shiki`,\n which locks no longer govern. Existing frozen contracts are brought into line by\n-a one-time Spec Amendment per plan, which must land before ADR 0015's contract\n+a one-time Spec Amendment per plan, which must land before SADR-0015's contract\n immutability makes `locks` unwritable from the implementation PR.\n \n **Lock state is resolved from the durable side.** MergeGate resolves the lock\n-registry, and ADR 0015's governance fields, from the base `.shiki` snapshot when\n+registry, and SADR-0015's governance fields, from the base `.shiki` snapshot when\n present, falling back to head only when absent. The `MergeGate metadata check`\n job is given `--base-shiki`, which it does not receive today. Locks are not\n derived from the live open-pull-request set: a pull request body is untrusted\n@@ -108,7 +108,7 @@\n Explicitly out of scope: moving MergeGate's contract resolution off the PR\n checkout wholesale; making the mirror a projection of GitHub state; introducing\n new lock kinds; moving ledger evidence out of the repository; and unwinding\n-ADR 0012's closeout pull request. Registration of multi-task goals is ADR 0015's\n+SADR-0012's closeout pull request. Registration of multi-task goals is SADR-0015's\n Contract PR and is not re-decided here.\n \n ## Consequences\n@@ -127,7 +127,7 @@\n \n Risks accepted. First, goal-scoped identity is only as strong as `goal_id`,\n which is resolved from the PR body and the head task file; the mitigation is to\n-add `goal_id` to ADR 0015's frozen governance field set, and until that lands a\n+add `goal_id` to SADR-0015's frozen governance field set, and until that lands a\n task file can name a foreign goal. Second, base-when-present resolution is\n unavailable on fork pull requests, where the head is contributor-controlled;\n fork pull requests already cannot complete a Shiki merge, and this decision does\n@@ -139,7 +139,7 @@\n path, not by implementation pull requests.\n \n New obligations: one Spec Amendment per frozen plan to rewrite `locks`, ordered\n-before ADR 0015 T1; extension of the adversarial suites in\n+before SADR-0015 T1; extension of the adversarial suites in\n `scripts/test_shiki_adversarial_state.sh` and\n `scripts/test_shiki_governance_evidence.sh` to cover the eleven newly-scoped\n directories; and updating `scripts/test_shiki_loop_lock_guard.sh`,\n@@ -151,32 +151,32 @@\n whether the loop's transitive sync of ledger `evidence` references can be\n deleted once the Contract PR carries its own source plan.\n \n-## Relationship to ADR 0004\n+## Relationship to SADR-0004\n \n-This ADR **clarifies something ADR 0004 left unspecified**. ADR 0004 decides that\n+This SADR **clarifies something SADR-0004 left unspecified**. SADR-0004 decides that\n GitHub is the operational source of truth and that each target repository keeps a\n `.shiki/` mirror. It says nothing about pull requests writing the mirror, does\n not name MergeGate, and does not make the mirror an input to a gate. Nothing in\n-ADR 0004's Decision or Consequences is amended, superseded or weakened: the\n+SADR-0004's Decision or Consequences is amended, superseded or weakened: the\n mirror still exists, still records the same artifacts, and pull request\n-boundaries remain the coordination surface. What this ADR settles is the question\n+boundaries remain the coordination surface. What this SADR settles is the question\n 0004 never reached \u2014 by what authority a pull request may write a mirror record \u2014\n and it answers it the way `.shiki/manifest.json` already declares\n (`pr_mutation: current-task-or-current-goal-only`).\n \n-It **amends ADR 0012**, whose Decision item 5 states that \"Loop-executed tasks\n+It **amends SADR-0012**, whose Decision item 5 states that \"Loop-executed tasks\n declare the `path:.shiki/**` lock, which covers every `.shiki` file the closeout\n stages ... so `files_outside_locks` is satisfied without per-file lock\n bookkeeping.\" That clause is replaced: the closeout's mirror writes are\n-authorized by record identity, not by a declared lock. ADR 0012's substantive\n+authorized by record identity, not by a declared lock. SADR-0012's substantive\n decision \u2014 that completion reaches `main` through a closeout pull request rather\n than local attestation \u2014 is preserved unchanged.\n \n-It is **complementary to ADR 0015**, not a substitute. ADR 0015 owns contract\n+It is **complementary to SADR-0015**, not a substitute. SADR-0015 owns contract\n immutability, the pre-dispatch gate, the Contract PR and the Scope Envelope. This\n-ADR owns what a lock means and who may write the mirror. The one ordering\n+SADR owns what a lock means and who may write the mirror. The one ordering\n constraint between them is that the lock-narrowing Spec Amendment must precede\n-ADR 0015 T1's freezing of the `locks` field.\n+SADR-0015 T1's freezing of the `locks` field.\n \n ## Alternatives Considered\n \n@@ -186,7 +186,7 @@\n the only writer that can register them may run. Migrating the Guardian signal to\n that store makes every registration PR fail closed to `needs_guardian` at every\n risk level, because the task file is by definition absent from the store, which\n-removes the low/medium autonomous path ADR 0008/0009 exists to provide. And it\n+removes the low/medium autonomous path SADR-0008/0009 exists to provide. And it\n leaves schemas, manifest, config, guardian policy, templates, migrations, plans,\n worktrees, runner records, runs and handoffs with no writer at all in a\n repository whose product is `.shiki`. Measured benefit on the frozen backlog:\n@@ -199,14 +199,14 @@\n namespace is not in fact ownership-partitioned \u2014 56 ledger entries carry no\n `task_id`, and one goal DAG is writable by 35 registered tasks. The correct\n expression of its insight is that locks do not govern the mirror at all, which is\n-what this ADR decides.\n+what this SADR decides.\n \n **Make the mirror a projection written only by a registration writer and a\n post-merge projection job.** Rejected. It requires a bypass actor with write\n access to the protected default branch; it makes its own master invariant\n readable from a manifest that pull requests may edit; it deletes both reconcile\n modes and leaves a corrupted mirror with no in-band repair path, contradicting\n-ADR 0004's consequence that mirror conflicts are repaired against GitHub state;\n+SADR-0004's consequence that mirror conflicts are repaired against GitHub state;\n and its content-derived ids are rejected by `validate_shiki`'s currently-green id\n pattern.\n \ndiff --git a/docs/adr/0017-bookkeeping-closeouts-do-not-inherit-risk.md b/docs/adr/SADR-0017-bookkeeping-closeouts-do-not-inherit-risk.md\nindex 554e3a0d526a72ca067108c5464cc68c3edcf174..b9676857a4713850e063af7c39c8e0b01ecb4c66\n--- a/docs/adr/0017-bookkeeping-closeouts-do-not-inherit-risk.md\n+++ b/docs/adr/SADR-0017-bookkeeping-closeouts-do-not-inherit-risk.md\n@@ -1,4 +1,4 @@\n-# ADR 0017: A Bookkeeping Closeout Does Not Inherit Its Task's Risk\n+# SADR-0017: A Bookkeeping Closeout Does Not Inherit Its Task's Risk\n \n ## Status\n \n@@ -6,7 +6,7 @@\n \n ## Context\n \n-ADR 0012 made the goal loop push a task's completion to the default branch through a\n+SADR-0012 made the goal loop push a task's completion to the default branch through a\n closeout pull request: after the implementation PR merges, the loop opens a second PR\n carrying `task.status = done`, `lock.state = released`, and \u2014 when the task completes\n its goal \u2014 `goal.status = complete` plus one scorecard report. Nothing else. The\n@@ -49,14 +49,14 @@\n 4. The task file's governance fields (`scope`, `non_goals`, `required_skills`,\n    `risk_level`, `locks`, `acceptance_checks`, `test_command`) are byte-identical to\n    the base snapshot. Only `status`, `expected_pr`, `closeout_pr`, `ledger_evidence`\n-   and `expected_branch` may differ \u2014 the ADR 0015 contract-immutability rule,\n+   and `expected_branch` may differ \u2014 the SADR-0015 contract-immutability rule,\n    unchanged.\n \n    `expected_branch` is in that set because a closeout is *by construction* on a\n    different branch from the implementation it closes, and the loop rewrites the\n    field accordingly. It is safe to let it differ because MergeGate independently\n    requires `expected_branch` to equal the PR's actual head ref, so a PR cannot set\n-   it to anything other than the branch it is really on. The first draft of this ADR\n+   it to anything other than the branch it is really on. The first draft of this SADR\n    omitted it, which made the exemption unreachable: every real closeout failed\n    condition 4 on a field the loop always changes. See PR #209.\n 5. The state transitions are exactly the terminal set: task `review` \u2192 `done`, lock\n@@ -113,12 +113,12 @@\n classification must be derived, not asserted.\n \n **Fold the closeout into the implementation PR** so no second PR exists. Rejected under\n-ADR 0012, which recorded why: a task file that is already `done` on the implementation\n+SADR-0012, which recorded why: a task file that is already `done` on the implementation\n branch short-circuits the loop's review \u2192 CCA \u2192 merge \u2192 done ordering into a local\n goal-completion before any merge happens.\n \n **Have the loop auto-merge closeouts with elevated credentials.** Rejected outright: that\n-is a runtime authoring its own approval, which ADR 0010 and the constitution's\n+is a runtime authoring its own approval, which SADR-0010 and the constitution's\n approval-record prohibition forbid. It would also be indistinguishable, in the evidence,\n from the forgery those rules exist to prevent.\n \ndiff --git a/docs/adr/0018-carried-guardian-approval-across-a-proven-base-sync.md b/docs/adr/SADR-0018-carried-guardian-approval-across-a-proven-base-sync.md\nindex 22358562da390ed8b45cd600da08a43d378b4680..e6e2907cefa776460af73ddf7d39db605f3c39b0\n--- a/docs/adr/0018-carried-guardian-approval-across-a-proven-base-sync.md\n+++ b/docs/adr/SADR-0018-carried-guardian-approval-across-a-proven-base-sync.md\n@@ -1,4 +1,4 @@\n-# ADR 0018: Carried Guardian Approval Across A Proven Base Sync\n+# SADR-0018: Carried Guardian Approval Across A Proven Base Sync\n \n ## Status\n \n@@ -34,7 +34,7 @@\n Guardian evaluator that consumes it (`shiki_guardian.evaluate_guardian_approval`,\n the `guardian_comment_carried` source) were built and tested in isolation. Neither\n does anything until a caller passes the flags that turn the carry on, so the\n-feature was invisible in CI. This ADR records the decision the two of them\n+feature was invisible in CI. This SADR records the decision the two of them\n implement, and this task wires it into the four CI invocations that judge Guardian\n approval.\n \ndiff --git a/docs/adr/README.md b/docs/adr/README.md\nindex c70e057a4520c36e042ab68c53cd4a21ef709b71..5da43856836e2ce6a150f8127766e2f88355ab4b\n--- a/docs/adr/README.md\n+++ b/docs/adr/README.md\n@@ -1,40 +1,41 @@\n-# Architecture Decision Records\n+# Decision Record Namespaces\n \n-This directory holds Shiki's Architecture Decision Records (ADRs): durable\n-records of hard-to-reverse platform decisions. ADRs are part of the Shiki source\n-of truth alongside GitHub state, the `.shiki/` mirror, and `CONTEXT.md`.\n+This directory holds two independent decision namespaces:\n \n-## How ADRs Work\n+- Shiki Architecture Decision Records (SADRs), named\n+  `SADR-NNNN-<short-slug>.md`, record hard-to-reverse Shiki platform decisions.\n+- Target Architecture Decision Records (ADRs), named\n+  `NNNN-<short-slug>.md`, belong to the target product.\n \n-- New ADRs are added as `docs/adr/NNNN-<short-slug>.md`, where `NNNN` is the next\n-  zero-padded sequence number.\n-- The **canonical list of ADRs is the set of files matching `docs/adr/*.md`** in\n-  this directory. Browse `docs/adr/*.md` for the authoritative, current set.\n-- Use [`template.md`](template.md) as the starting point for a new ADR.\n-- Record an ADR when a decision is hard to reverse, surprising without context,\n-  and a real tradeoff. Do not write an ADR for routine or easily reversible\n-  choices.\n+The same four-digit number may appear once in each namespace. A platform\n+`SADR-NNNN` citation resolves only to `SADR-NNNN-*.md`; a target `ADR NNNN`\n+citation resolves only to `NNNN-*.md`.\n \n-Using the `docs/adr/*.md` glob as the source of truth means other PRs can add an\n-ADR without editing this index file, avoiding merge contention on a\n-hand-maintained enumeration.\n+## How Decision Records Work\n \n-## Current ADRs (snapshot)\n-\n-The list below is a convenience snapshot and may lag behind the directory. When\n-in doubt, list `docs/adr/*.md`.\n+- New Shiki platform decisions use the next zero-padded SADR number.\n+- New target product decisions use the target's own ADR sequence.\n+- The canonical list is the files in `docs/adr/`; no hand-maintained index is\n+  authoritative.\n+- Use [`template.md`](template.md) as the starting point and choose the filename\n+  namespace that owns the decision.\n+- Record a decision when it is hard to reverse, surprising without context, and\n+  a real tradeoff. Do not write a decision record for routine or easily\n+  reversible choices.\n \n-- [0001 \u2014 Keep Shiki As The Platform Name](0001-keep-shiki-as-platform-name.md)\n-- [0002 \u2014 Use AGENTS As The Shared Agent Constitution](0002-use-agents-as-shared-constitution.md)\n-- [0003 \u2014 Require Skill Gate For MergeGate Readiness](0003-require-skill-gate-for-mergegate-readiness.md)\n-- [0004 \u2014 Use GitHub First With A Repository-Local Shiki Mirror](0004-use-github-first-with-shiki-mirror.md)\n-- [0005 \u2014 Use Codex As Front And Claude Code Action As The GitHub Runtime](0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md)\n-- [0006 \u2014 Separate Codex Implementation, CCA Completion, And MergeGate Authorization](0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md)\n+Using the directory as the source of truth lets independent changes add records\n+without contending on an enumerated index.\n \n-## Adding A New ADR\n+## Adding A Shiki SADR\n \n-1. Copy `template.md` to `docs/adr/NNNN-<short-slug>.md`.\n+1. Copy `template.md` to `docs/adr/SADR-NNNN-<short-slug>.md`.\n 2. Fill in the title, status, context, decision, and consequences.\n-3. Set the status to `Proposed`, then `Accepted` once the decision is approved.\n-4. Open a PR following `CONTRIBUTING.md`. You do not need to edit this index for\n-   the ADR to be discoverable via `docs/adr/*.md`.\n+3. Set the status to `Proposed`, then `Accepted` once the proper authority has\n+   approved the decision.\n+4. Open a PR following `CONTRIBUTING.md`.\n+\n+## Adding A Target ADR\n+\n+1. Copy `template.md` to `docs/adr/NNNN-<short-slug>.md` using the target's own\n+   sequence.\n+2. Cite it as `ADR NNNN`; do not prefix its filename with `SADR-`.\ndiff --git a/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md b/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\nnew file mode 100644\nindex 0000000000000000000000000000000000000000..7455ce1f3bed02ddddb851ac21a1d1a648c9aeba\n--- /dev/null\n+++ b/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\n@@ -0,0 +1,55 @@\n+# SADR-0019: Separate Shiki Platform SADRs From Target ADRs\n+\n+## Status\n+\n+Accepted\n+\n+## Context\n+\n+Shiki installs its platform decision records into `docs/adr/` in every target\n+repository. Shiki and its targets previously used the same numeric\n+`NNNN-<slug>.md` namespace. A target that already owned ADRs 0013 through 0018\n+therefore acquired two records for each number during installation, making an\n+`ADR NNNN` citation ambiguous. Renumbering target decisions would rewrite target\n+history, while renumbering Shiki decisions per target would destroy their stable\n+platform identity.\n+\n+Upgrades also need to remove the former Shiki-owned numeric paths without ever\n+mistaking a target-owned ADR for platform content. The install stamp already\n+records exact shipped paths and content digests, so it can prove ownership only\n+when both still match.\n+\n+## Decision\n+\n+Shiki platform decisions use the `SADR` namespace:\n+\n+- records are named `docs/adr/SADR-NNNN-<slug>.md`;\n+- live platform citations use `SADR-NNNN`;\n+- target product decisions retain `docs/adr/NNNN-<slug>.md` and `ADR NNNN`;\n+- the reference validator resolves the two namespaces independently, so the same\n+  four-digit number may exist once in each namespace.\n+\n+Before a forced upgrade writes anything, the installer inventories the exact\n+former numeric Shiki ADR paths. It may delete a legacy path only when the\n+existing install stamp contains that exact path and its recorded SHA-256 digest\n+matches the file currently on disk. All ownership blockers are reported\n+together and abort the upgrade before its first write. A non-forced install\n+never performs this cleanup.\n+\n+Historical `.shiki/` evidence remains unchanged because it records the namespace\n+that existed when that evidence was produced.\n+\n+## Consequences\n+\n+- Targets may use any ADR number without colliding with Shiki's platform SADRs.\n+- Fresh installs preserve target ADR bytes and install a stable SADR set.\n+- Forced upgrades fail closed when legacy ownership cannot be proven, and a\n+  successful upgrade refreshes the stamp without removed legacy paths.\n+- Current governance, packaging, workflow, prompt, script, and skill references\n+  must move with the platform records.\n+\n+## Alternatives Considered\n+\n+- Collision-only refusal leaves affected targets permanently unupgradable.\n+- Renumbering target ADRs mutates target-owned history.\n+- Per-target Shiki renumbering makes platform citations non-portable.\ndiff --git a/docs/adr/template.md b/docs/adr/template.md\nindex a92a97daddd0b00ab9dd626cc8d86942da2eafed..17dd79175b5fee2105935557f691b40cf9492942\n--- a/docs/adr/template.md\n+++ b/docs/adr/template.md\n@@ -1,14 +1,14 @@\n-# NNNN: <Short Decision Title>\n+# <SADR-NNNN or NNNN>: <Short Decision Title>\n \n ## Status\n \n-Proposed | Accepted | Superseded by ADR-NNNN\n+Proposed | Accepted | Superseded by SADR-NNNN or target ADR NNNN\n \n ## Context\n \n What is the situation, constraint, or force that requires a decision? Reference\n-relevant domain terms (`CONTEXT.md`), prior ADRs, and code reality. State the\n-problem, not the solution.\n+relevant domain terms (`CONTEXT.md`), prior Shiki SADRs or target ADRs, and code\n+reality. State the problem, not the solution.\n \n ## Decision\n \ndiff --git a/docs/agents/autonomous-loop-e2e-acceptance.md b/docs/agents/autonomous-loop-e2e-acceptance.md\nindex bbf84c6081987f159ac6ad9ecf3c82b7aa72e7e8..5c13c1d02a05a90b0dc46cbef7c95a9846d0c237\n--- a/docs/agents/autonomous-loop-e2e-acceptance.md\n+++ b/docs/agents/autonomous-loop-e2e-acceptance.md\n@@ -40,8 +40,8 @@\n 3. `gh auth status` is logged in with merge permission on the repo.\n 4. Branch protection + required checks are configured exactly as the goal-loop\n    required-check set expects (see `docs/agents/branch-protection-smoke.md`).\n-5. The repo `.shiki/policy` permits autonomous low/medium-risk merges (ADR\n-   0008/0009); the MergeGate policy check is the recorded-authority gate.\n+5. The repo `.shiki/policy` permits autonomous low/medium-risk merges\n+   (SADR-0008/0009); the MergeGate policy check is the recorded-authority gate.\n \n The operator's only actions are: register the task and start the loop. **No\n manual pre-clearing** means after `shiki loop run` starts, the operator does not\ndiff --git a/docs/agents/checklists.md b/docs/agents/checklists.md\nindex 4a7f03ade346f53a1c1d82981f3fd744f9ab4547..0c5ec6c53dc7197b7b7a7d18e667b325babd6c45\n--- a/docs/agents/checklists.md\n+++ b/docs/agents/checklists.md\n@@ -32,18 +32,18 @@\n | ID | Item | Blocking |\n | --- | --- | --- |\n | GD-01 | Plan was challenged against `CONTEXT.md`. | blocking for non-trivial Goals |\n-| GD-02 | Relevant ADRs were read or absence was recorded. | blocking for non-trivial Goals |\n+| GD-02 | Relevant SADRs and target ADRs were read or absence was recorded. | blocking for non-trivial Goals |\n | GD-03 | Ambiguous terms were resolved or converted to HITL blockers. | blocking |\n | GD-04 | Edge scenarios were considered. | advisory |\n | GD-05 | Code reality was checked when code could answer. | blocking when applicable |\n | GD-06 | New domain terms were added to `CONTEXT.md` when needed. | blocking when applicable |\n-| GD-07 | ADR was created or explicitly skipped using the ADR criteria. | blocking when applicable |\n+| GD-07 | A SADR or target ADR was created or explicitly skipped using the decision-record criteria. | blocking when applicable |\n \n ## CI \u2014 Context & Impact Checklist\n \n | ID | Item | Blocking |\n | --- | --- | --- |\n-| CI-01 | Relevant docs and ADRs identified. | blocking |\n+| CI-01 | Relevant docs, SADRs, and target ADRs identified. | blocking |\n | CI-02 | Relevant modules, interfaces, seams, callers, and tests identified. | blocking |\n | CI-03 | Dependencies and blocked-by relationships identified. | blocking |\n | CI-04 | Candidate locks identified. | blocking |\n@@ -62,7 +62,7 @@\n | PRD-04 | Implementation decisions are recorded without volatile code detail. | blocking |\n | PRD-05 | Testing decisions are recorded. | blocking |\n | PRD-06 | Out-of-scope items are explicit. | blocking |\n-| PRD-07 | Relevant glossary/ADR links are present when applicable. | blocking |\n+| PRD-07 | Relevant glossary/SADR/target-ADR links are present when applicable. | blocking |\n \n ## SF \u2014 Spec Freeze Checklist\n \n@@ -98,7 +98,7 @@\n | H-02 | Branch/worktree target is present. | blocking |\n | H-03 | Scope and non-goals are present. | blocking |\n | H-04 | Dependencies and locks are present. | blocking |\n-| H-05 | Relevant docs, ADRs, modules, and tests are referenced. | blocking |\n+| H-05 | Relevant docs, SADRs, target ADRs, modules, and tests are referenced. | blocking |\n | H-06 | Required skills are stated. | blocking |\n | H-07 | TDD expectations are stated. | blocking when behavior work |\n | H-08 | Acceptance criteria are included. | blocking |\ndiff --git a/docs/agents/codex-handoff.md b/docs/agents/codex-handoff.md\nindex ede468f47cb277f69a857e8c45df703e4ed0151d..6f97bdffb67e44d7edea2c71e01ea28933c4eb6c\n--- a/docs/agents/codex-handoff.md\n+++ b/docs/agents/codex-handoff.md\n@@ -46,7 +46,7 @@\n \n ## Context\n - Domain terms:\n-- Relevant ADRs:\n+- Relevant SADRs and target ADRs:\n - Relevant docs:\n - Relevant modules/interfaces/seams:\n - Similar tests/prior art:\ndiff --git a/docs/agents/completion-check-agent.md b/docs/agents/completion-check-agent.md\nindex 13e4c035db35025f4e3cdcc5adfb24314ac4d521..61bfa1218cf36ca605250c91919e620f33238aa0\n--- a/docs/agents/completion-check-agent.md\n+++ b/docs/agents/completion-check-agent.md\n@@ -32,7 +32,7 @@\n - `AGENTS.md`\n - `CLAUDE.md`\n - `CONTEXT.md`\n-- relevant ADRs\n+- relevant SADRs and target ADRs\n - parent Goal issue\n - PRD issue or document\n - task issue\ndiff --git a/docs/agents/decision-control.md b/docs/agents/decision-control.md\nindex 7d8ee5ab03a5ccbcbd04056ae61de03785a2a9c9..8c94ecda00fffaa0e92c325ab83aa9776bd1dac1\n--- a/docs/agents/decision-control.md\n+++ b/docs/agents/decision-control.md\n@@ -11,7 +11,7 @@\n | Decision | Owner | When | Required inputs | Output | Enforcement surface |\n | --- | --- | --- | --- | --- | --- |\n | Is the Goal clear enough to plan? | Planner + Guardian when needed | Goal intake | User request, existing docs, risk signals | Goal readiness verdict | Goal checklist, issue template, ledger |\n-| Must `grill-with-docs` run? | Planner / Skill Gate | Before PRD or issue split | Ambiguity, domain terms, ADR conflicts, unclear boundaries | Skill invocation or explicit skip rationale | Skill Gate checklist |\n+| Must `grill-with-docs` run? | Planner / Skill Gate | Before PRD or issue split | Ambiguity, domain terms, SADR/target-ADR conflicts, unclear boundaries | Skill invocation or explicit skip rationale | Skill Gate checklist |\n | Is a PRD required? | Planner | After Goal context stabilizes | Goal, `grill-with-docs` output, Context & Impact output | PRD required / not required | PRD checklist, CCA evidence check |\n | Can the spec be frozen? | Operator only | End of Requirements Definition, before issue split | PRD/requirements summary, scope inventory (SF-02), open-question list | `spec_freeze` block in the plan + ledger evidence | `require_grilled_plan` (plan ingest/run), SF checklist |\n | Can a frozen spec change? | Operator only (Spec Amendment) | When implementation contradicts the frozen spec | Contested decisions, scoped re-grill result | recorded amendment + re-stamped freeze | SF checklist, ledger evidence |\ndiff --git a/docs/agents/domain.md b/docs/agents/domain.md\nindex 258da8ea81e87ac0a6b0b5b5e5c2fde980eaabb7..d57b0cf17ff3e1fa9e6e5885372bf083d8295421\n--- a/docs/agents/domain.md\n+++ b/docs/agents/domain.md\n@@ -5,7 +5,9 @@\n ## Layout\n \n - `CONTEXT.md` defines Shiki language. Keep it implementation-free.\n-- `docs/adr/` records hard-to-reverse decisions.\n+- `docs/adr/SADR-NNNN-*.md` records Shiki platform decisions.\n+- `docs/adr/NNNN-*.md` records target product decisions without sharing the\n+  SADR namespace.\n - `AGENTS.md` is the runtime-neutral agent constitution.\n - `CLAUDE.md` is the Claude Code wrapper around `AGENTS.md`.\n - `.shiki/` is the Target Repository mirror for Goals, PRDs, plans, Task DAGs, contracts, locks, worktree records, ledger entries, CCA verdicts, repair packets, reports, and handoffs.\n@@ -15,6 +17,6 @@\n \n - Use Shiki terms exactly when they exist.\n - Add glossary terms to `CONTEXT.md` only when the term is part of the domain, not a generic programming concept.\n-- Create ADRs only for hard-to-reverse, surprising, tradeoff-based decisions.\n+- Create SADRs or target ADRs only for hard-to-reverse, surprising, tradeoff-based decisions.\n - Keep product-specific language in the Target Repository, not in Shiki Template docs.\n-- If an ADR conflicts with a proposed implementation, surface the conflict before editing.\n+- If a SADR or target ADR conflicts with a proposed implementation, surface the conflict before editing.\ndiff --git a/docs/agents/execution-control.md b/docs/agents/execution-control.md\nindex ad8ee3cdb36be56fedc1f46408ca92b387d74047..bc63c3a73661d12b567016ba299f1a6b66bf9088\n--- a/docs/agents/execution-control.md\n+++ b/docs/agents/execution-control.md\n@@ -63,7 +63,7 @@\n | Branch name | Orchestrator | Orchestrator | Before implementer dispatch | branch policy | branch ref in task/ledger |\n | Lock reservation | Context & Impact | MergeGate | Before dispatch and before merge | `.shiki/locks/`, labels, PR metadata | lock id, owner, scope |\n | CI profile | Planner | MergeGate | During issue triage, rechecked at PR | labels, `.shiki/policy.yaml`, workflow matrix | required check list |\n-| Required status checks | Repo maintainer / Guardian | GitHub branch protection / rulesets | Repository setup, updated via ADR | GitHub settings | branch/ruleset config, ADR |\n+| Required status checks | Repo maintainer / Guardian | GitHub branch protection / rulesets | Repository setup, updated via target ADR | GitHub settings | branch/ruleset config, target ADR |\n | CCA judgment | CCA | GitHub required check / MergeGate | Every non-draft PR update | GitHub Action check | structured CCA verdict |\n | Repair assignment | CCA | MergeGate | After failed CCA/CI/review | repair packet, issue/PR comment | repair id and attempt count |\n | Merge readiness | MergeGate | GitHub branch protection / Guardian if needed | After CCA complete | required checks, review rules, rulesets | mergegate check output |\ndiff --git a/docs/agents/guardian-policy.md b/docs/agents/guardian-policy.md\nindex 4147c7adf05a47adb4fd24ada1a1c2e1f2ba6d2d..a20d9cc6c00a569bb057b9ca8d6b51c7fc4a9847\n--- a/docs/agents/guardian-policy.md\n+++ b/docs/agents/guardian-policy.md\n@@ -98,8 +98,8 @@\n `origin/<default_branch>` and reuses each job's existing base fetch; a shallow\n (`--depth`) or second base fetch would make the proof unresolvable and silently\n disable the carry, so neither is permitted. CCA reads `guardian_comment_carried` as\n-a recorded authority in the deterministic signal and does not re-derive it. See ADR\n-0018 for the full decision, the rejected diff/patch-equality and path-disjointness\n+a recorded authority in the deterministic signal and does not re-derive it. See\n+SADR-0018 for the full decision, the rejected diff/patch-equality and path-disjointness\n alternatives, and the accepted residual.\n \n ## External AI Guardian Review (SADR-0010)\ndiff --git a/docs/agents/implementation-policy.md b/docs/agents/implementation-policy.md\nindex c754144a9fb5bf4a38aa82a60e1670c59e626b44..85c37281bcad196c6b7b02343faf00b03cd9c57a\n--- a/docs/agents/implementation-policy.md\n+++ b/docs/agents/implementation-policy.md\n@@ -31,10 +31,13 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n ## Non-Negotiables\n \n 1. **Planning precedes implementation.** Non-trivial Goals start with `grill-with-docs` and Context & Impact.\n-2. **Domain language is controlled.** Terms must be checked against `CONTEXT.md`; hard-to-reverse tradeoffs must be checked against ADRs.\n+2. **Domain language is controlled.** Terms must be checked against `CONTEXT.md`; hard-to-reverse tradeoffs must be checked against Shiki SADRs and target ADRs.\n 3. **PRD precedes issue decomposition.** `to-prd` is used once the context is settled enough to become durable intent.\n 4. **Issues are vertical slices.** `to-issues` must produce independently grabbable tracer-bullet tasks, not horizontal layer tickets.\n 5. **Triage controls dispatch.** Only `ready-for-agent` / AFK tasks may be assigned to Codex without human input.\n@@ -95,19 +98,19 @@\n Process:\n \n - Challenge terms against `CONTEXT.md`.\n-- Check ADRs before reopening decisions.\n+- Check SADRs and target ADRs before reopening decisions.\n - Inspect code when code can answer a question.\n - Ask one question at a time when the operator is available.\n - Provide a recommended answer with each question.\n - Record resolved terms in `CONTEXT.md`.\n-- Create ADRs only for hard-to-reverse, surprising, tradeoff-based decisions.\n+- Create SADRs or target ADRs only for hard-to-reverse, surprising, tradeoff-based decisions.\n - Convert unresolved load-bearing questions into HITL blockers.\n \n Outputs:\n \n - Resolved design-tree decisions.\n - Updated glossary terms when needed.\n-- ADRs when justified.\n+- SADRs or target ADRs when justified.\n - HITL blockers when decisions remain unresolved.\n \n ## Phase 3 \u2014 Context & Impact\n@@ -117,7 +120,7 @@\n \n Outputs must include:\n \n-- Relevant documents and ADRs.\n+- Relevant documents, SADRs, and target ADRs.\n - Relevant modules, interfaces, seams, callers, and tests.\n - Candidate locks.\n - Dependencies.\n@@ -140,7 +143,7 @@\n - Testing decisions.\n - Out of scope.\n - Further notes.\n-- Links to relevant Goal, domain terms, and ADRs.\n+- Links to relevant Goal, domain terms, SADRs, and target ADRs.\n \n Phase 4 ends with Spec Freeze: the operator explicitly approves the PRD, the\n plan records a `spec_freeze` block (status, approved_by, source), and the SF\n@@ -240,7 +243,7 @@\n - Dependencies and locks.\n - Risk and Guardian requirements.\n - Ledger completeness.\n-- Documentation and ADR updates.\n+- Documentation and SADR/target-ADR updates.\n - Scope drift.\n \n CCA emits one of:\n@@ -301,7 +304,7 @@\n | Evidence failure | Behavior might be done, but proof is missing. | Add checks, PR evidence, ledger entry, or artifacts. |\n | Scope drift | PR changes unrelated behavior. | Revert unrelated changes or split into new Goal. |\n | Dependency block | Upstream task or lock not complete. | Block until dependency resolves. |\n-| Architecture block | Decision needs ADR or deep-module discussion. | `grill-with-docs` or `improve-codebase-architecture`. |\n+| Architecture block | Decision needs a SADR, target ADR, or deep-module discussion. | `grill-with-docs` or `improve-codebase-architecture`. |\n | Risk block | Security/production/policy change needs human approval. | Guardian decision. |\n \n ## Phase 5.5 \u2014 Execution Decision Control\ndiff --git a/docs/agents/runtime-auth-model.md b/docs/agents/runtime-auth-model.md\nindex 6a2e1a47e81ea89b5c68a210ec07772a9374babc..3c54748d24233dbd0ba8473950282547ef4ce935\n--- a/docs/agents/runtime-auth-model.md\n+++ b/docs/agents/runtime-auth-model.md\n@@ -148,7 +148,7 @@\n \n API-key based runners may be added by a target repository as an explicit extension. They are not the default Shiki template path.\n \n-If a repo opts into API-key mode, record the exception in an ADR and update the repo's `AGENTS.md`, `.shiki/config.yaml`, and workflow permissions.\n+If a repo opts into API-key mode, record the target-specific exception in a target ADR and update the repo's `AGENTS.md`, `.shiki/config.yaml`, and workflow permissions.\n \n ## GitHub Actions Runtime Compatibility\n \ndiff --git a/docs/agents/skill-gate.md b/docs/agents/skill-gate.md\nindex c50398b0d7c60a8598f2a5c88a10015730dced13..3acffe6444617bb1d54b77d4b4708158ec735bc6\n--- a/docs/agents/skill-gate.md\n+++ b/docs/agents/skill-gate.md\n@@ -9,7 +9,7 @@\n | Skill | Trigger | Expected output |\n | --- | --- | --- |\n | `setup-matt-pocock-skills` | First configuring a repo for these skills, or when issue tracker, triage labels, or domain docs are missing. | `docs/agents/` configuration and an `Agent skills` block in `AGENTS.md` or `CLAUDE.md`. |\n-| `grill-with-docs` | Ambiguous plan, unclear terminology, boundary decision, tradeoff, or ADR-worthy design choice. | Clarified decision tree, recommended answers, updates to `CONTEXT.md` or ADRs when decisions crystallize. |\n+| `grill-with-docs` | Ambiguous plan, unclear terminology, boundary decision, tradeoff, or decision-record-worthy design choice. | Clarified decision tree, recommended answers, updates to `CONTEXT.md`, SADRs, or target ADRs when decisions crystallize. |\n | `zoom-out` | Agent lacks a higher-level architectural map. | Map of relevant modules, callers, seams, and domain concepts. |\n | `to-prd` | Settled context should become a PRD. | PRD published to the issue tracker with problem, solution, user stories, decisions, testing, and out-of-scope notes. |\n | `to-issues` | Goal, PRD, or plan must become implementation tickets. | Vertical-slice issues with acceptance criteria, dependencies, and AFK/HITL classification. |\n@@ -17,7 +17,7 @@\n | `tdd` | Feature work or bug fix has observable behavior that can be specified with tests. | Red-green-refactor loop through public interfaces, one vertical slice at a time. |\n | `code-review` | The TDD loop is green and the implementer is about to create or update a PR. Default for every implementation task. | Pre-PR self-review of the task diff with fixes applied on the task branch; findings and resolutions recorded as a ledger entry naming the skill and a `## Pre-PR code review` PR body section. |\n | `diagnose` | Hard bug, failing check, regression, flaky behavior, or performance problem. | Reproduction loop, ranked hypotheses, instrumentation, fix, regression test, post-mortem. |\n-| `improve-codebase-architecture` | Structural friction, testability, AI-navigability, deep-module opportunity, or refactor request. | Architecture candidates and deepening recommendations informed by `CONTEXT.md` and ADRs. |\n+| `improve-codebase-architecture` | Structural friction, testability, AI-navigability, deep-module opportunity, or refactor request. | Architecture candidates and deepening recommendations informed by `CONTEXT.md`, SADRs, and target ADRs. |\n | `prototype` | A throwaway logic or UI prototype is needed to answer a design question. | Clearly marked disposable prototype and captured decision. |\n \n ## MergeGate Interaction\ndiff --git a/scripts/guardian_approval_signal.py b/scripts/guardian_approval_signal.py\nindex d059f92b2c82aa2b5d6ca421365537a87886c1cb..16924892714f4f5b9303c0ef0e325a7e70075630\n--- a/scripts/guardian_approval_signal.py\n+++ b/scripts/guardian_approval_signal.py\n@@ -199,8 +199,8 @@\n ) -> Any:\n     \"\"\"Evaluate SADR-0015 Contract Approval for the CCA Guardian signal.\n \n-    The SAME OR that MergeGate applies must reach this deterministic signal: ADR\n-    0015 is explicit that without it, a contract-approved PR still returns\n+    The SAME OR that MergeGate applies must reach this deterministic signal:\n+    SADR-0015 is explicit that without it, a contract-approved PR still returns\n     ``needs_guardian``, the CCA job fails, and the MergeGate policy check never\n     runs \u2014 the gate this change moves would never be reached. This resolves the\n     task the SAME way MergeGate does (the first Shiki task id in the PR body),\ndiff --git a/scripts/shiki_contract_approval.py b/scripts/shiki_contract_approval.py\nindex 941bcb96fdd8b9a85bc0c4f0f04d5dad9d04839c..4a4b418f6527413161db82670bec857a043360e2\n--- a/scripts/shiki_contract_approval.py\n+++ b/scripts/shiki_contract_approval.py\n@@ -142,7 +142,7 @@\n     changed_files_status: Any,\n     registration: Any,\n ) -> ContractApprovalResult:\n-    \"\"\"Whether this PR carries an SADR-0015 Contract Approval.\n+    \"\"\"Whether this PR carries a SADR-0015 Contract Approval.\n \n     The four conditions are evaluated IN ORDER, each failure recorded as a\n     distinct reason string:\ndiff --git a/scripts/shiki_guardian.py b/scripts/shiki_guardian.py\nindex a4ecb79bbda55fd5c8b06adfae2eb99760a03c45..4dd99bbe714e26f421fa19abac7a34ae1b195ecc\n--- a/scripts/shiki_guardian.py\n+++ b/scripts/shiki_guardian.py\n@@ -12,7 +12,7 @@\n # The carry re-derives its proof from git in-process on every judging invocation;\n # it is never read from an artifact, so there is nothing to go stale or be forged.\n # shiki_sync_proof is the ONLY guardian-family module that runs git, and every one\n-# of its invocations is read-only and local (ADR: prove-from-git base sync).\n+# of its invocations is read-only and local (SADR: prove-from-git base sync).\n from shiki_sync_proof import verify_pure_base_sync\n \n GUARDIAN_POLICY_PATH = \".shiki/guardian-policy.json\"\ndiff --git a/scripts/shiki_guardian_review.py b/scripts/shiki_guardian_review.py\nindex dd0f104d457899e0ba5ff1e162011c1e90b6f92b..e10ef58c046324e7ea3a3392feb3048ca77414ce\n--- a/scripts/shiki_guardian_review.py\n+++ b/scripts/shiki_guardian_review.py\n@@ -159,7 +159,7 @@\n     docs_only = _is_docs_only(changed_files)\n     if docs_only and matched == 0:\n         focus.append(\n-            \"Docs/ADR-only change: confirm the change is genuinely documentation/decision-only, introduces no behavior or contract drift, and stays consistent with the constitution and existing ADRs.\"\n+            \"Docs/decision-record-only change: confirm the change is genuinely documentation/decision-only, introduces no behavior or contract drift, and stays consistent with the constitution and existing SADRs and target ADRs.\"\n         )\n \n     # Conservative augmentation: unknown (no category and not docs-only), mixed\ndiff --git a/scripts/shiki_installer.py b/scripts/shiki_installer.py\nindex be064ab7a29ff6836f7f7a1d70500a43e0d11d7b..d3e3247edbf3e4be287a4667176d86f6500f8dc5\n--- a/scripts/shiki_installer.py\n+++ b/scripts/shiki_installer.py\n@@ -118,6 +118,7 @@\n # what lets a target carry it without a governance-schema change.\n INSTALL_STAMP_PATH = \".shiki/install-stamp.json\"\n INSTALL_STAMP_VERSION = 1\n+LEGACY_SHIKI_SADR_MAX = 18\n \n # Under --force the shipped surface splits three ways, so nothing is lost and\n # nothing is silently stale.\n@@ -237,9 +238,98 @@\n         )\n \n \n+def legacy_shiki_adr_paths() -> tuple[str, ...]:\n+    \"\"\"Former numeric paths for Shiki SADR-0001 through SADR-0018.\n+\n+    The authoritative slugs come from the current SADR filenames. SADR-0019 is\n+    the namespace decision itself and never shipped under a numeric legacy path.\n+    \"\"\"\n+    paths: list[str] = []\n+    for sadr in sorted((ROOT / \"docs\" / \"adr\").glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\")):\n+        number = sadr.name.split(\"-\", 2)[1]\n+        if int(number) <= LEGACY_SHIKI_SADR_MAX:\n+            paths.append(f\"docs/adr/{sadr.name.removeprefix('SADR-')}\")\n+    return tuple(paths)\n+\n+\n+def preflight_legacy_sadr_cleanup(target: Path, *, force: bool) -> tuple[str, ...]:\n+    \"\"\"Authorize exact legacy Shiki ADR removals before the first write.\n+\n+    A non-force install never removes existing files. A forced install needs no\n+    stamp when no legacy Shiki path is present. When legacy paths do exist, each\n+    must be named in a readable install stamp and its current bytes must match\n+    the recorded digest.\n+    \"\"\"\n+    if not force:\n+        return ()\n+    present = tuple(\n+        relative\n+        for relative in legacy_shiki_adr_paths()\n+        if (target / relative).exists() or (target / relative).is_symlink()\n+    )\n+    if not present:\n+        return ()\n+\n+    stamp_path = target / INSTALL_STAMP_PATH\n+    try:\n+        raw = stamp_path.read_text(encoding=\"utf-8\")\n+        stamp = json.loads(raw)\n+    except FileNotFoundError:\n+        detail = \"install stamp is absent\"\n+        blockers = [f\"{relative}: {detail}\" for relative in present]\n+    except (OSError, UnicodeDecodeError) as error:\n+        detail = f\"install stamp is unreadable ({error})\"\n+        blockers = [f\"{relative}: {detail}\" for relative in present]\n+    except json.JSONDecodeError as error:\n+        detail = f\"install stamp is malformed ({error})\"\n+        blockers = [f\"{relative}: {detail}\" for relative in present]\n+    else:\n+        if not isinstance(stamp, dict):\n+            blockers = [\n+                f\"{relative}: install stamp is malformed (expected an object)\"\n+                for relative in present\n+            ]\n+        else:\n+            digests = stamp.get(\"digests\")\n+            blockers = []\n+            if not isinstance(digests, dict):\n+                blockers.extend(\n+                    f\"{relative}: install stamp has no digest for this path\"\n+                    for relative in present\n+                )\n+            else:\n+                for relative in present:\n+                    expected = digests.get(relative)\n+                    if not isinstance(expected, str) or not expected:\n+                        blockers.append(\n+                            f\"{relative}: install stamp has no digest for this path\"\n+                        )\n+                        continue\n+                    try:\n+                        actual = _sha256_file(target / relative)\n+                    except OSError as error:\n+                        blockers.append(\n+                            f\"{relative}: legacy path is unreadable ({error})\"\n+                        )\n+                        continue\n+                    if actual != expected:\n+                        blockers.append(\n+                            f\"{relative}: digest mismatch (stamp {expected}, current {actual})\"\n+                        )\n+\n+    if blockers:\n+        joined = \"\\n\".join(f\"- {blocker}\" for blocker in blockers)\n+        raise ShikiError(\n+            \"legacy Shiki ADR cleanup is not authorized:\\n\"\n+            f\"{joined}\\nNo files were written.\"\n+        )\n+    return present\n+\n+\n def install_template(target: Path, *, force: bool, validate: bool) -> list[NewFileNote]:\n     # Preconditions first: never rewrite the tree of a target that cannot be\n     # upgraded cleanly (see refuse_pending_migrations).\n+    legacy_cleanup = preflight_legacy_sadr_cleanup(target, force=force)\n     refuse_pending_migrations(target)\n \n     # Resolve the target's required CODEOWNERS owner once from its\n@@ -250,6 +340,10 @@\n     # verbatim. See ``codeowners_required_owner``.\n     codeowners_owner = codeowners_required_owner(target)\n \n+    for relative in legacy_cleanup:\n+        (target / relative).unlink()\n+        info(f\"removed stamped legacy Shiki ADR: {target / relative}\")\n+\n     notes: list[NewFileNote] = []\n     for relative in TEMPLATE_PATHS:\n         source = ROOT / relative\ndiff --git a/scripts/shiki_loop.py b/scripts/shiki_loop.py\nindex 3611641da8aeb5038b1da90e3114b09a473ec6d8..84caa491b9cad0edc4176f209c13e9221e975681\n--- a/scripts/shiki_loop.py\n+++ b/scripts/shiki_loop.py\n@@ -424,7 +424,7 @@\n         # unresolvable CCA verdict has already produced its terminal stop and is never\n         # laundered into a head-moving sync (SADR-0010). It also never fires while the\n         # policy (Guardian) gate is red \u2014 for ANY reason, not only when it is the sole\n-        # red check \u2014 so a head-bound Guardian approval, or an SADR-0015\n+        # red check \u2014 so a head-bound Guardian approval, or a SADR-0015\n         # contract-approval carry that base movement defeated (the policy gate compares\n         # base and head task governance directly), is never silently restored by\n         # adopting base wholesale. It is bounded by a durable per-task counter so the\ndiff --git a/scripts/shiki_tasks.py b/scripts/shiki_tasks.py\nindex aec3809f24b4a0c8c96fc50971cff9ae9ad30381..d531052e9d9717058d4631483cefab44c26a65c1\n--- a/scripts/shiki_tasks.py\n+++ b/scripts/shiki_tasks.py\n@@ -821,7 +821,7 @@\n         \"prompt\": prompt,\n         \"entry_skill\": \"grill-with-docs\",\n         \"required_next_steps\": [\n-            \"Run grill-with-docs until terminology, boundaries, risks, and ADR-worthy decisions are settled.\",\n+            \"Run grill-with-docs until terminology, boundaries, risks, and decision-record-worthy choices are settled.\",\n             \"Obtain the operator's explicit approval of the PRD/requirements (Spec Freeze).\",\n             \"Write a machine-readable plan JSON with grill_with_docs.status=complete and spec_freeze.status=frozen.\",\n             \"Run shiki plan ingest --plan-file PLAN.json.\",\ndiff --git a/scripts/test_shiki_goal_loop.sh b/scripts/test_shiki_goal_loop.sh\nindex 753c7b516e10f0be3bc92a4bd0eb1fa699776ae6..8f3820a0a079b3044424cfe1a6c6b7fe68896e67\n--- a/scripts/test_shiki_goal_loop.sh\n+++ b/scripts/test_shiki_goal_loop.sh\n@@ -231,8 +231,8 @@\n \n # High/critical risk merges autonomously when all required checks are green:\n # the \"MergeGate policy check\" required check IS the Guardian gate (it enforces\n-# guardian-policy.json \u2014 human approval OR external AI guardian review, ADR\n-# 0010), so green checks mean a recorded authority approved. The fake gh returns\n+# guardian-policy.json \u2014 human approval OR external AI guardian review,\n+# SADR-0010), so green checks mean a recorded authority approved. The fake gh returns\n # green checks, simulating that the Guardian gate passed.\n python3 \"$ROOT/scripts/shiki.py\" goal create --target \"$TARGET\" --title \"Guardian gate\" --outcome \"High risk merges via policy gate\" >/tmp/shiki-goal-loop-guardian-goal.json\n GGOAL=\"$(json_get /tmp/shiki-goal-loop-guardian-goal.json goal_id)\"\ndiff --git a/tests/test_adr_references.py b/tests/test_adr_references.py\nindex 51706329c797d5dd9b47e082a333fa83b261d482..db6e693e35f67103935be34f1d9bb4ff7ee60395\n--- a/tests/test_adr_references.py\n+++ b/tests/test_adr_references.py\n@@ -1,7 +1,7 @@\n-\"\"\"Every ``ADR ####`` citation must resolve to exactly one decision record.\n+\"\"\"Every target ADR and Shiki SADR citation resolves in its own namespace.\n \n Background. ``scripts/mergegate_check.py`` justifies ``is_bookkeeping_closeout``\n-with \"ADR 0017\" nine times and ``scripts/guardian_approval_signal.py`` cites\n+with \"SADR-0017\" nine times and ``scripts/guardian_approval_signal.py`` cites\n 0016/0017 seven times, yet for a stretch the numbering in ``docs/adr/`` stopped\n at 0015: the authoring commits lived on a coordinator branch and no task ever\n declared ``docs/adr/...`` in its locks, so nothing carried the records to the\n@@ -11,11 +11,12 @@\n and the directory drifted with no signal.\n \n This is the same unbound-pair shape behind other silent failures: a reference\n-and its target with nothing asserting they agree. This test binds them. It scans\n-every ``scripts/*.py`` (plus ``docs/**/*.md`` and the root ``*.md`` governance\n-files, which are cheap) for the ``ADR ####`` pattern and asserts each cited\n-number resolves to exactly one ``docs/adr/####-*.md``. The converse is\n-deliberately NOT required: an ADR may exist without being cited.\n+and its target with nothing asserting they agree. This test binds them. Target\n+``ADR NNNN`` citations resolve only to ``docs/adr/NNNN-*.md``; Shiki\n+``SADR-NNNN`` citations resolve only to ``docs/adr/SADR-NNNN-*.md``. The scan\n+covers current governance docs, scripts, workflows, prompts, packaging metadata,\n+and repo-local Codex guidance while deliberately excluding historical\n+``.shiki`` evidence. The converse is not required: a record may be uncited.\n \n The suite is dependency free: standard-library ``unittest`` only.\n \"\"\"\n@@ -31,17 +32,18 @@\n \n REPO_ROOT = Path(__file__).resolve().parents[1]\n \n-# \"ADR 0016\" -- a four-digit citation. The trailing \\b keeps a five-digit token\n-# from being read as a truncated four-digit number.\n-_CITATION = re.compile(r\"\\bADR (\\d{4})\\b\")\n-# \"0016-locks-bound-product-paths.md\" -- the leading four digits name the record.\n-_ADR_FILENAME = re.compile(r\"^(\\d{4})-.*\\.md$\")\n+# ``ADR 0016`` / ``SADR-0016`` -- a namespace plus four-digit citation. The\n+# trailing ``\\b`` keeps a five-digit token from being read as a truncated one.\n+_CITATION = re.compile(r\"\\b(S?ADR)[ -](\\d{4})\\b\")\n+# Target ADRs retain the numeric filename; Shiki platform decisions use SADR.\n+_DECISION_FILENAME = re.compile(r\"^(?:(SADR)-)?(\\d{4})-.*\\.md$\")\n \n \n class Violation(NamedTuple):\n     \"\"\"A citation that does not resolve to exactly one decision record.\"\"\"\n \n     citing_file: str  # repo-relative path of the file that cites the ADR\n+    namespace: str  # \"ADR\" for target decisions; \"SADR\" for Shiki decisions\n     number: str  # the cited four-digit number, e.g. \"0016\"\n     line: int  # 1-based line of the citation\n     reason: str  # \"missing\" (zero matches) or \"ambiguous\" (two or more)\n@@ -49,44 +51,59 @@\n \n     @property\n     def expected_path(self) -> str:\n-        return f\"docs/adr/{self.number}-*.md\"\n+        prefix = \"SADR-\" if self.namespace == \"SADR\" else \"\"\n+        return f\"docs/adr/{prefix}{self.number}-*.md\"\n \n     def describe(self) -> str:\n         \"\"\"Name the citing file, the cited number, and the missing path.\"\"\"\n         if self.reason == \"missing\":\n             return (\n-                f\"{self.citing_file}:{self.line} cites ADR {self.number} \"\n+                f\"{self.citing_file}:{self.line} cites {self.namespace} {self.number} \"\n                 f\"but no document matches {self.expected_path}\"\n             )\n         joined = \", \".join(self.matches)\n         return (\n-            f\"{self.citing_file}:{self.line} cites ADR {self.number} which \"\n+            f\"{self.citing_file}:{self.line} cites {self.namespace} {self.number} which \"\n             f\"resolves to {len(self.matches)} documents (expected exactly one \"\n             f\"at {self.expected_path}): {joined}\"\n         )\n \n \n-def _adr_index(root: Path) -> dict[str, list[Path]]:\n-    \"\"\"Map every four-digit ADR number to the record files that claim it.\"\"\"\n-    index: dict[str, list[Path]] = {}\n+def _adr_index(root: Path) -> dict[tuple[str, str], list[Path]]:\n+    \"\"\"Map each decision namespace and number to the files that claim it.\"\"\"\n+    index: dict[tuple[str, str], list[Path]] = {}\n     adr_dir = root / \"docs\" / \"adr\"\n     if adr_dir.is_dir():\n         for path in sorted(adr_dir.glob(\"*.md\")):\n-            match = _ADR_FILENAME.match(path.name)\n+            match = _DECISION_FILENAME.match(path.name)\n             if match:\n-                index.setdefault(match.group(1), []).append(path)\n+                namespace = \"SADR\" if match.group(1) else \"ADR\"\n+                index.setdefault((namespace, match.group(2)), []).append(path)\n     return index\n \n \n def _citing_files(root: Path, include_docs: bool = True) -> list[Path]:\n-    \"\"\"Files scanned for citations: scripts/*.py, and cheaply the markdown.\"\"\"\n+    \"\"\"Current, shipped text surfaces scanned for decision citations.\"\"\"\n     files = sorted((root / \"scripts\").glob(\"*.py\"))\n+    files += sorted((root / \"scripts\").glob(\"*.sh\"))\n     if include_docs:\n         docs = root / \"docs\"\n         if docs.is_dir():\n             files += sorted(docs.rglob(\"*.md\"))\n         files += sorted(root.glob(\"*.md\"))\n-    return files\n+        pyproject = root / \"pyproject.toml\"\n+        if pyproject.is_file():\n+            files.append(pyproject)\n+        for relative in (\".github\", \".codex\"):\n+            surface = root / relative\n+            if surface.is_dir():\n+                files += sorted(\n+                    path\n+                    for path in surface.rglob(\"*\")\n+                    if path.is_file()\n+                    and path.suffix in {\".md\", \".py\", \".sh\", \".toml\", \".yaml\", \".yml\"}\n+                )\n+    return sorted(set(files))\n \n \n def find_adr_reference_violations(\n@@ -94,10 +111,9 @@\n ) -> list[Violation]:\n     \"\"\"Return every ``ADR ####`` citation under ``root`` that fails to resolve.\n \n-    A citation resolves when exactly one ``docs/adr/####-*.md`` matches its\n-    number. Zero matches is a \"missing\" violation; two or more is \"ambiguous\".\n-    Records that are never cited produce nothing -- the converse is not\n-    required.\n+    A citation resolves when exactly one record in the same namespace matches\n+    its number. Zero matches is a \"missing\" violation; two or more is\n+    \"ambiguous\". Records that are never cited produce nothing.\n     \"\"\"\n     root = Path(root)\n     index = _adr_index(root)\n@@ -110,13 +126,15 @@\n         rel = path.relative_to(root).as_posix()\n         for lineno, line in enumerate(text.splitlines(), start=1):\n             for match in _CITATION.finditer(line):\n-                number = match.group(1)\n-                found = index.get(number, [])\n+                namespace, number = match.groups()\n+                found = index.get((namespace, number), [])\n                 if len(found) == 1:\n                     continue\n                 reason = \"missing\" if not found else \"ambiguous\"\n                 rels = [p.relative_to(root).as_posix() for p in found]\n-                violations.append(Violation(rel, number, lineno, reason, rels))\n+                violations.append(\n+                    Violation(rel, namespace, number, lineno, reason, rels)\n+                )\n     return violations\n \n \n@@ -129,8 +147,8 @@\n         self.assertEqual(\n             violations,\n             [],\n-            \"Every 'ADR ####' citation must resolve to exactly one \"\n-            f\"docs/adr/####-*.md:\\n{detail}\",\n+            \"Every ADR/SADR citation must resolve exactly once in its own \"\n+            f\"docs/adr namespace:\\n{detail}\",\n         )\n \n     def test_scan_actually_reads_scripts(self) -> None:\n@@ -146,8 +164,21 @@\n         # The two records this Goal carried onto the default branch, and which\n         # the scripts cite most, must be present.\n         index = _adr_index(REPO_ROOT)\n-        self.assertIn(\"0016\", index)\n-        self.assertIn(\"0017\", index)\n+        self.assertIn((\"SADR\", \"0016\"), index)\n+        self.assertIn((\"SADR\", \"0017\"), index)\n+\n+    def test_platform_tree_has_only_sadr_0001_through_0019(self) -> None:\n+        adr_dir = REPO_ROOT / \"docs\" / \"adr\"\n+        sadrs = sorted(adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\"))\n+        self.assertEqual(\n+            {path.name.split(\"-\", 2)[1] for path in sadrs},\n+            {f\"{number:04d}\" for number in range(1, 20)},\n+        )\n+        self.assertEqual(\n+            list(adr_dir.glob(\"[0-9][0-9][0-9][0-9]-*.md\")),\n+            [],\n+            \"the Shiki platform tree must not retain legacy numeric SADR paths\",\n+        )\n \n \n class FixtureAdrReferences(unittest.TestCase):\n@@ -180,6 +211,26 @@\n         self.assertIn(\"9991\", message)  # cited number\n         self.assertIn(\"docs/adr/9991-*.md\", message)  # missing path\n \n+    def test_target_adr_does_not_satisfy_missing_platform_sadr(self) -> None:\n+        root = self._make_repo(\n+            adr_names=[\"0042-target-decision.md\"],\n+            script_body='\"\"\"Target ADR 0042 exists, but platform SADR-0042 does not.\"\"\"\\n',\n+        )\n+        violations = find_adr_reference_violations(root, include_docs=False)\n+        self.assertEqual(len(violations), 1)\n+        self.assertEqual(violations[0].namespace, \"SADR\")\n+        self.assertEqual(violations[0].number, \"0042\")\n+        self.assertEqual(violations[0].expected_path, \"docs/adr/SADR-0042-*.md\")\n+\n+    def test_target_adr_and_platform_sadr_may_reuse_a_number(self) -> None:\n+        root = self._make_repo(\n+            adr_names=[\"0042-target-decision.md\", \"SADR-0042-platform-decision.md\"],\n+            script_body='\"\"\"Target ADR 0042 and platform SADR-0042 are distinct.\"\"\"\\n',\n+        )\n+        self.assertEqual(\n+            find_adr_reference_violations(root, include_docs=False), []\n+        )\n+\n     def test_uncited_adr_is_allowed(self) -> None:\n         # An ADR exists but nothing cites it -> not a violation. The converse\n         # (every ADR must be cited) is explicitly NOT required.\ndiff --git a/tests/test_guardian_review.py b/tests/test_guardian_review.py\nindex 88829e7810212635e2fc98aa5918d6f26294677d..00a85a237acd5bf2d6cb5b294128198c422402ca\n--- a/tests/test_guardian_review.py\n+++ b/tests/test_guardian_review.py\n@@ -162,7 +162,7 @@\n \n     def test_docs_only_is_light_and_not_conservative(self):\n         focus = gr.classify_review_focus([\"docs/adr/0099-x.md\", \"README.md\"], \"\", \"low\")\n-        self.assertTrue(any(\"Docs/ADR-only\" in f for f in focus))\n+        self.assertTrue(any(\"Docs/decision-record-only\" in f for f in focus))\n         self.assertFalse(any(\"conservative default\" in f.lower() for f in focus))\n \n     def test_unknown_category_is_conservative(self):\ndiff --git a/tests/test_policy_job_merged_prs.py b/tests/test_policy_job_merged_prs.py\nindex d45512e74681e5e649279236c7d4e79d5c3da471..b97989f8bd74d091de1a6b5275a20d8dc68c4512\n--- a/tests/test_policy_job_merged_prs.py\n+++ b/tests/test_policy_job_merged_prs.py\n@@ -3,8 +3,8 @@\n \n ``.github/workflows/shiki-cca-completion.yml`` has two consumers of the merged-PR\n proof: the CCA *signal* (``guardian_approval_signal.py`` in \"Prepare CCA context\")\n-and the authoritative gate (``mergegate_check.py`` in \"Enforce MergeGate\"). ADR\n-0017's bookkeeping-closeout exemption (condition 6) needs the merged implementation\n+and the authoritative gate (``mergegate_check.py`` in \"Enforce MergeGate\").\n+SADR-0017's bookkeeping-closeout exemption (condition 6) needs the merged implementation\n PR proven, and a closeout carries NO ``post_merge_reconcile`` marker. The signal\n already resolves ``MERGED_PRS`` unconditionally, but \"Enforce MergeGate\" used to\n resolve it only inside ``grep -q '<!-- shiki:post_merge_reconcile -->'``. So a\ndiff --git a/tests/test_sadr_install_migration.py b/tests/test_sadr_install_migration.py\nnew file mode 100644\nindex 0000000000000000000000000000000000000000..5a442fa5e4968ade178dcdedc3c63b54dba580a2\n--- /dev/null\n+++ b/tests/test_sadr_install_migration.py\n@@ -0,0 +1,224 @@\n+\"\"\"SADR installation and collision-safe legacy upgrade behavior.\"\"\"\n+\n+from __future__ import annotations\n+\n+import contextlib\n+import hashlib\n+import io\n+import json\n+import shutil\n+import tempfile\n+import unittest\n+from pathlib import Path\n+\n+import shiki_test_support  # noqa: F401  (path bootstrap)\n+\n+import shiki_installer\n+from tests.test_adr_references import find_adr_reference_violations\n+\n+\n+PRODUCT_NUMBERS = tuple(range(13, 19))\n+\n+\n+def _quiet_install(target: Path, *, force: bool, validate: bool) -> None:\n+    with contextlib.redirect_stdout(io.StringIO()):\n+        shiki_installer.install_template(target, force=force, validate=validate)\n+\n+\n+def _sha256(path: Path) -> str:\n+    return hashlib.sha256(path.read_bytes()).hexdigest()\n+\n+\n+def _legacy_sadr_paths() -> tuple[str, ...]:\n+    return tuple(\n+        f\"docs/adr/{path.name.removeprefix('SADR-')}\"\n+        for path in sorted((shiki_installer.ROOT / \"docs\" / \"adr\").glob(\"SADR-*.md\"))\n+        if path.name != \"SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\"\n+    )\n+\n+\n+def _create_legacy_upgrade_fixture(\n+    target: Path,\n+) -> tuple[dict[str, bytes], tuple[str, ...]]:\n+    adr_dir = target / \"docs\" / \"adr\"\n+    adr_dir.mkdir(parents=True)\n+    product_bytes = {\n+        f\"{number:04d}-cubeshare-product-decision.md\": (\n+            f\"# CubeShare ADR {number:04d}\\n\\nDo not rewrite {number}.\\n\"\n+        ).encode()\n+        for number in PRODUCT_NUMBERS\n+    }\n+    for name, content in product_bytes.items():\n+        (adr_dir / name).write_bytes(content)\n+\n+    legacy_paths = _legacy_sadr_paths()\n+    for relative in legacy_paths:\n+        (target / relative).write_bytes(f\"legacy Shiki bytes for {relative}\\n\".encode())\n+\n+    stamp_path = target / shiki_installer.INSTALL_STAMP_PATH\n+    stamp_path.parent.mkdir(parents=True, exist_ok=True)\n+    stamp = {\n+        \"version\": shiki_installer.INSTALL_STAMP_VERSION,\n+        \"platform_commit\": None,\n+        \"installed_at\": \"2000-01-01T00:00:00+00:00\",\n+        \"digests\": {\n+            relative: _sha256(target / relative) for relative in legacy_paths\n+        },\n+    }\n+    stamp_path.write_text(\n+        json.dumps(stamp, indent=2, sort_keys=True) + \"\\n\", encoding=\"utf-8\"\n+    )\n+    return product_bytes, legacy_paths\n+\n+\n+def _tree_snapshot(root: Path) -> dict[str, tuple[str, bytes | str]]:\n+    \"\"\"Recursive byte/shape snapshot used to prove preflight is write-free.\"\"\"\n+    snapshot: dict[str, tuple[str, bytes | str]] = {}\n+    for path in sorted(root.rglob(\"*\")):\n+        relative = path.relative_to(root).as_posix()\n+        if path.is_symlink():\n+            snapshot[relative] = (\"symlink\", path.readlink().as_posix())\n+        elif path.is_dir():\n+            snapshot[relative] = (\"directory\", \"\")\n+        else:\n+            snapshot[relative] = (\"file\", path.read_bytes())\n+    return snapshot\n+\n+\n+class FreshSadrInstallTests(unittest.TestCase):\n+    def setUp(self) -> None:\n+        self.tmp = Path(tempfile.mkdtemp(prefix=\"shiki-sadr-fresh-\"))\n+        self.addCleanup(lambda: shutil.rmtree(self.tmp, ignore_errors=True))\n+        self.target = self.tmp / \"target\"\n+        self.adr_dir = self.target / \"docs\" / \"adr\"\n+        self.adr_dir.mkdir(parents=True)\n+        self.product_bytes = {\n+            f\"{number:04d}-product-decision.md\": (\n+                f\"# Product ADR {number:04d}\\n\\nTarget-owned bytes {number}.\\n\"\n+            ).encode()\n+            for number in PRODUCT_NUMBERS\n+        }\n+        for name, content in self.product_bytes.items():\n+            (self.adr_dir / name).write_bytes(content)\n+\n+    def test_fresh_install_preserves_product_adrs_and_installs_all_sadrs(self) -> None:\n+        _quiet_install(self.target, force=False, validate=True)\n+\n+        for name, expected in self.product_bytes.items():\n+            self.assertEqual((self.adr_dir / name).read_bytes(), expected)\n+\n+        sadr_numbers = {\n+            path.name.split(\"-\", 2)[1]\n+            for path in self.adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\")\n+        }\n+        self.assertEqual(sadr_numbers, {f\"{number:04d}\" for number in range(1, 20)})\n+\n+        numeric_records = {\n+            path.name\n+            for path in self.adr_dir.glob(\"[0-9][0-9][0-9][0-9]-*.md\")\n+        }\n+        self.assertEqual(numeric_records, set(self.product_bytes))\n+        self.assertEqual(find_adr_reference_violations(self.target), [])\n+\n+\n+class LegacySadrUpgradeTests(unittest.TestCase):\n+    def setUp(self) -> None:\n+        self.tmp = Path(tempfile.mkdtemp(prefix=\"shiki-sadr-upgrade-\"))\n+        self.addCleanup(lambda: shutil.rmtree(self.tmp, ignore_errors=True))\n+        self.target = self.tmp / \"target\"\n+        self.adr_dir = self.target / \"docs\" / \"adr\"\n+        self.product_bytes, self.legacy_paths = _create_legacy_upgrade_fixture(\n+            self.target\n+        )\n+\n+    def test_force_upgrade_deletes_only_stamped_legacy_sadrs(self) -> None:\n+        _quiet_install(self.target, force=True, validate=True)\n+\n+        for relative in self.legacy_paths:\n+            self.assertFalse((self.target / relative).exists(), relative)\n+        for name, expected in self.product_bytes.items():\n+            self.assertEqual((self.adr_dir / name).read_bytes(), expected)\n+\n+        sadrs = list(self.adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\"))\n+        self.assertEqual(len(sadrs), 19)\n+        refreshed = json.loads(\n+            (self.target / shiki_installer.INSTALL_STAMP_PATH).read_text(encoding=\"utf-8\")\n+        )\n+        for relative in self.legacy_paths:\n+            self.assertNotIn(relative, refreshed[\"digests\"])\n+        for path in sadrs:\n+            relative = path.relative_to(self.target).as_posix()\n+            self.assertEqual(refreshed[\"digests\"][relative], _sha256(path))\n+\n+    def test_non_force_install_never_deletes_legacy_files(self) -> None:\n+        before = {\n+            relative: (self.target / relative).read_bytes()\n+            for relative in self.legacy_paths\n+        }\n+        _quiet_install(self.target, force=False, validate=False)\n+        for relative, expected in before.items():\n+            self.assertEqual((self.target / relative).read_bytes(), expected)\n+\n+\n+class LegacyCleanupPreflightTests(unittest.TestCase):\n+    def setUp(self) -> None:\n+        self.tmp = Path(tempfile.mkdtemp(prefix=\"shiki-sadr-preflight-\"))\n+        self.addCleanup(lambda: shutil.rmtree(self.tmp, ignore_errors=True))\n+\n+    def test_all_ownership_blockers_abort_before_any_write(self) -> None:\n+        cases = (\n+            (\"absent\", \"install stamp is absent\"),\n+            (\"unreadable\", \"install stamp is unreadable\"),\n+            (\"malformed\", \"install stamp is malformed\"),\n+            (\"missing-digest\", \"install stamp has no digest\"),\n+            (\"digest-mismatch\", \"digest mismatch\"),\n+        )\n+        for name, expected_reason in cases:\n+            with self.subTest(name=name):\n+                target = self.tmp / name\n+                _, legacy_paths = _create_legacy_upgrade_fixture(target)\n+                stamp_path = target / shiki_installer.INSTALL_STAMP_PATH\n+\n+                if name == \"absent\":\n+                    stamp_path.unlink()\n+                elif name == \"unreadable\":\n+                    stamp_path.unlink()\n+                    stamp_path.mkdir()\n+                elif name == \"malformed\":\n+                    stamp_path.write_text(\"{not-json\\n\", encoding=\"utf-8\")\n+                else:\n+                    stamp = json.loads(stamp_path.read_text(encoding=\"utf-8\"))\n+                    if name == \"missing-digest\":\n+                        stamp[\"digests\"] = {}\n+                    else:\n+                        stamp[\"digests\"] = {\n+                            relative: \"deadbeef\" for relative in legacy_paths\n+                        }\n+                    stamp_path.write_text(\n+                        json.dumps(stamp, indent=2, sort_keys=True) + \"\\n\",\n+                        encoding=\"utf-8\",\n+                    )\n+\n+                before = _tree_snapshot(target)\n+                with self.assertRaises(shiki_installer.ShikiError) as raised:\n+                    _quiet_install(target, force=True, validate=False)\n+                after = _tree_snapshot(target)\n+\n+                self.assertEqual(before, after, f\"{name} preflight changed the tree\")\n+                message = str(raised.exception)\n+                self.assertIn(expected_reason, message)\n+                self.assertIn(\"No files were written\", message)\n+                for relative in legacy_paths:\n+                    self.assertIn(relative, message)\n+\n+    def test_absent_stamp_is_allowed_when_no_legacy_path_exists(self) -> None:\n+        target = self.tmp / \"no-legacy\"\n+        target.mkdir()\n+        _quiet_install(target, force=True, validate=False)\n+        self.assertTrue(\n+            (target / \"docs\" / \"adr\" / \"SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\").is_file()\n+        )\n+\n+\n+if __name__ == \"__main__\":\n+    unittest.main()\n\ndiff --git a/.codex/skills/shiki/SKILL.md b/.codex/skills/shiki/SKILL.md\nindex c8e2e85d9d75d49cc4f2d6286ee22659c4e53e75..93cded623b2c15d9f65c76491a1c2d813ac9e6c0\n--- a/.codex/skills/shiki/SKILL.md\n+++ b/.codex/skills/shiki/SKILL.md\n@@ -18,6 +18,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n Codex CLI does not register this skill as a custom `/shiki` slash command. When\n the user types `/shiki` in Codex CLI and gets \"Unrecognized command\", explain\n that this is expected: invoke Shiki in Codex with natural language such as\n@@ -69,7 +72,7 @@\n \n ## Responsibilities\n \n-- Claude Code implements and repairs by default (`shiki runner claude`, ADR 0008); Codex implements and repairs tasks explicitly assigned to `codex`.\n+- Claude Code implements and repairs by default (`shiki runner claude`, SADR-0008); Codex implements and repairs tasks explicitly assigned to `codex`.\n - Claude Code Action can act as GitHub-side CCA or reviewer.\n - CCA judges completion.\n - MergeGate authorizes state transitions and merge readiness.\ndiff --git a/.github/prompts/cca-completion-check.md b/.github/prompts/cca-completion-check.md\nindex ed203ad5f302ecc81372d8ab27d2b1232ca97497..fd89950528333ef6242e6d5443904f5392ab7e56\n--- a/.github/prompts/cca-completion-check.md\n+++ b/.github/prompts/cca-completion-check.md\n@@ -19,6 +19,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n ## Required Reading\n \n Read only the files needed to judge the PR contract. Prefer this order:\ndiff --git a/.github/prompts/codex-task-handoff.md b/.github/prompts/codex-task-handoff.md\nindex 695e211d76dea1c60e8d5bb360e78487808286a4..59441d576b01a39569722f226c854e5a740e9eb5\n--- a/.github/prompts/codex-task-handoff.md\n+++ b/.github/prompts/codex-task-handoff.md\n@@ -2,7 +2,7 @@\n \n You are Codex Front acting as the Implementer for a Shiki task.\n \n-Follow `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant ADRs, and `docs/agents/codex-handoff.md`.\n+Follow `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant Shiki SADRs and target ADRs, and `docs/agents/codex-handoff.md`.\n \n Rules:\n \ndiff --git a/AGENTS.md b/AGENTS.md\nindex 7f041100d49c2a7525f51cb0f5d696a4a6d1c78c..a06bfbabc6fa48a007389e25e12d7ed523936bed\n--- a/AGENTS.md\n+++ b/AGENTS.md\n@@ -22,7 +22,7 @@\n - **Codex Front** is an optional implementer through Codex App, Codex CLI, Codex IDE extension, or Codex Web signed in with ChatGPT OAuth/subscription auth, dispatched with `shiki runner codex` when a task is explicitly assigned to `codex`.\n - **GitHub CCA** is implemented by Claude Code Action by default, using `CLAUDE_CODE_OAUTH_TOKEN`.\n \n-Do not assume `openai/codex-action`, `OPENAI_API_KEY`, or API-key based Codex automation in the default Shiki path. API-key runners are explicit target-repo extensions and require their own ADR.\n+Do not assume `openai/codex-action`, `OPENAI_API_KEY`, or API-key based Codex automation in the default Shiki path. API-key runners are explicit target-repo extensions and require their own target ADR.\n \n Shiki is not:\n \n@@ -44,10 +44,14 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use\n+`SADR-NNNN-*.md`; target product decisions retain `NNNN-*.md` and the `ADR`\n+namespace.\n+\n Use the canonical source-of-truth order above. Runtime-specific reading orders\n are operational guidance only and must not override or reorder it.\n \n-Conversation state is not durable truth. If a decision matters, put it in GitHub, `.shiki/`, `CONTEXT.md`, or an ADR.\n+Conversation state is not durable truth. If a decision matters, put it in GitHub, `.shiki/`, `CONTEXT.md`, a Shiki SADR, or a target ADR.\n \n If GitHub and `.shiki/` disagree, surface the conflict and prefer GitHub operational state until the mirror is repaired.\n \n@@ -75,8 +79,8 @@\n Every non-trivial change follows this loop:\n \n 1. **Goal**: clarify outcome, completion conditions, non-goals, risk level, and success signals.\n-2. **grill-with-docs**: challenge the plan against domain docs, ADRs, code reality, terminology, and edge scenarios.\n-3. **Context & Impact**: find relevant docs, ADRs, code, dependencies, owners, locks, and verification surfaces.\n+2. **grill-with-docs**: challenge the plan against domain docs, Shiki SADRs, target ADRs, code reality, terminology, and edge scenarios.\n+3. **Context & Impact**: find relevant docs, SADRs, target ADRs, code, dependencies, owners, locks, and verification surfaces.\n 4. **PRD and Spec Freeze**: use `to-prd` when context is settled enough to become durable product/engineering intent. The operator's explicit approval of the PRD is Spec Freeze: the plan records a `spec_freeze` block, and no plan runs without it. Steps 1-4 form Requirements Definition \u2014 one continuous operator dialogue. After Spec Freeze, scope changes require an operator-approved Spec Amendment; non-scope-moving interpretations go to the Assumption Log.\n 5. **Issues**: use `to-issues` to create independently grabbable vertical-slice issues. Prefer AFK slices over HITL slices where possible.\n 6. **Triage**: label issues for readiness, risk, runtime, skills, and MergeGate state.\n@@ -167,7 +171,7 @@\n - Testing decisions.\n - Out-of-scope items.\n - Further notes.\n-- Links to relevant domain terms and ADRs.\n+- Links to relevant domain terms, SADRs, and target ADRs.\n \n Every executable task must include:\n \n@@ -193,7 +197,7 @@\n \n Context & Impact must identify:\n \n-- Relevant documents, domain terms, ADRs, and past decisions.\n+- Relevant documents, domain terms, SADRs, target ADRs, and past decisions.\n - Relevant modules, interfaces, seams, callers, owners, and tests.\n - Dependency relationships between tasks.\n - Candidate locks and lock conflicts.\n@@ -210,7 +214,7 @@\n | Skill | Required when |\n | --- | --- |\n | `setup-matt-pocock-skills` | First configuring the repo for skills, or when issue tracker, triage labels, or domain docs are missing. |\n-| `grill-with-docs` | Default for non-trivial Goals before PRD/issues; always required when plans, terminology, boundaries, tradeoffs, or ADR-worthy decisions are ambiguous. |\n+| `grill-with-docs` | Default for non-trivial Goals before PRD/issues; always required when plans, terminology, boundaries, tradeoffs, or decision-record-worthy choices are ambiguous. |\n | `zoom-out` | The agent lacks an architectural map of the relevant code area. |\n | `to-prd` | Settled Goal context must become durable product/engineering intent. |\n | `to-issues` | A Goal, PRD, or plan must become independently grabbable vertical-slice issues. |\n@@ -348,7 +352,7 @@\n - Three or more new files.\n - Any high-risk or critical label.\n \n-When the architecture gate triggers, run `grill-with-docs` or `improve-codebase-architecture` as appropriate and record durable decisions in an ADR when needed.\n+When the architecture gate triggers, run `grill-with-docs` or `improve-codebase-architecture` as appropriate and record durable decisions in a Shiki SADR or target ADR as applicable.\n \n ## Engineering Discipline\n \ndiff --git a/CLAUDE.md b/CLAUDE.md\nindex 98a932e7841a632866fcd53b0bad0e3af3711d3d..98a31399404d6fb044b4fd493f4459ac87f439de\n--- a/CLAUDE.md\n+++ b/CLAUDE.md\n@@ -14,6 +14,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n ## Claude Role\n \n Default to planner, default implementer, reviewer, coordinator, documentation editor, CCA-style completion judge, and final-judgment assistant.\n@@ -57,7 +60,7 @@\n At the start of a Shiki session:\n \n 1. Read `AGENTS.md`.\n-2. Read `CONTEXT.md` and relevant ADRs.\n+2. Read `CONTEXT.md` and relevant Shiki SADRs and target ADRs.\n 3. Read active `.shiki/` Goal/task/ledger state when present.\n 4. Read active distilled rules from `.shiki/memories/` (status `distilled`, `active: true`, not revoked or superseded) for applicable guidance; the same rules are injected deterministically into task handoffs as the `## Distilled Rules` section (\u00a73.5).\n 5. Read the GitHub Issue or PR when the task is GitHub-backed.\n@@ -74,7 +77,7 @@\n - Define the Goal, completion conditions, non-goals, and risk level.\n - Treat `grill-with-docs` as the default planning entry point for non-trivial Goals.\n - Challenge domain terms against `CONTEXT.md`.\n-- Challenge hard-to-reverse tradeoffs against ADRs.\n+- Challenge hard-to-reverse tradeoffs against SADRs and target ADRs.\n - Explore code instead of asking when code can answer the question.\n - Run Context & Impact before implementation planning.\n - Produce a Task DAG, not an unordered checklist.\n@@ -94,7 +97,7 @@\n - For each question, provide your recommended answer.\n - Resolve dependency decisions before downstream design questions.\n - Update `CONTEXT.md` inline when terminology crystallizes.\n-- Offer ADRs only when the decision is hard to reverse, surprising without context, and a real tradeoff.\n+- Offer a SADR or target ADR only when the decision is hard to reverse, surprising without context, and a real tradeoff.\n - Convert unresolved questions into HITL blockers rather than allowing Codex to guess.\n \n ## PRD and Issues Standard\n@@ -103,7 +106,7 @@\n \n - Use the project's domain glossary vocabulary.\n - Avoid volatile file paths unless they are essential.\n-- Include implementation decisions, testing decisions, out-of-scope items, and relevant ADR/domain links.\n+- Include implementation decisions, testing decisions, out-of-scope items, and relevant SADR/target-ADR/domain links.\n - Identify deep-module opportunities and testable interfaces.\n \n When creating issues:\n@@ -121,7 +124,7 @@\n - Branch/worktree target.\n - Scope and non-goals.\n - Dependencies and locks.\n-- Relevant docs, ADRs, modules, and tests.\n+- Relevant docs, SADRs, target ADRs, modules, and tests.\n - Required skill invocations.\n - TDD expectations.\n - Pre-PR `code-review` gate expectations.\n@@ -152,7 +155,7 @@\n \n - Treat yourself as a judge, not implementer.\n - Do not write production code.\n-- Read `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant ADRs, PRD, task issue, PR body, diff, checks, reviews, labels, and `.shiki/` state.\n+- Read `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant SADRs and target ADRs, PRD, task issue, PR body, diff, checks, reviews, labels, and `.shiki/` state.\n - Evaluate every applicable checklist in `docs/agents/checklists.md`.\n - Map each acceptance criterion to durable evidence.\n - Distinguish implementation failure from missing evidence.\ndiff --git a/CONTEXT.md b/CONTEXT.md\nindex e61a9be06a0cce0d026484083a657bde2067e234..237ee8aa3ea8fab334ece49768bc5ed3b5af0974\n--- a/CONTEXT.md\n+++ b/CONTEXT.md\n@@ -25,7 +25,7 @@\n _Avoid_: jumping directly into code\n \n **grill-with-docs**:\n-A planning skill that challenges a plan against domain language, ADRs, code reality, and concrete edge scenarios. It resolves design-tree questions before PRD/issues. It runs inside Requirements Definition.\n+A planning skill that challenges a plan against domain language, Shiki SADRs, target ADRs, code reality, and concrete edge scenarios. It resolves design-tree questions before PRD/issues. It runs inside Requirements Definition.\n _Avoid_: generic brainstorming, silent assumptions\n \n **Requirements Definition**:\ndiff --git a/CONTRIBUTING.md b/CONTRIBUTING.md\nindex c6d9b656e73abb8a55e5f660123e03153dcb3e6d..9e37a8fbf668db5b22461aaae5548b8f1354394c\n--- a/CONTRIBUTING.md\n+++ b/CONTRIBUTING.md\n@@ -17,7 +17,7 @@\n 1. **Goal** \u2014 start from a user-approved Goal with an outcome, completion\n    conditions, non-goals, risk level, and success signals.\n 2. **grill-with-docs** \u2014 for any non-trivial change, challenge the plan against\n-   domain language, ADRs, and code reality before writing a PRD.\n+   domain language, Shiki SADRs, target ADRs, and code reality before writing a PRD.\n 3. **PRD** \u2014 capture settled product/engineering intent (`to-prd`).\n 4. **Spec Freeze** \u2014 the operator explicitly approves the PRD/requirements;\n    the plan records a `spec_freeze` block and no plan runs without it\n@@ -94,7 +94,7 @@\n   entries.\n - Do not hand-edit `.shiki/goals/*.json` or `.shiki/manifest.json` in a task PR.\n - Conversation state is not durable truth. If a decision matters, record it in\n-  GitHub, `.shiki/`, `CONTEXT.md`, or an ADR.\n+  GitHub, `.shiki/`, `CONTEXT.md`, a Shiki SADR, or a target ADR.\n \n ## Architecture Gate\n \n@@ -103,7 +103,7 @@\n or CI policy, package structure, three or more structural files, more than 100\n net new lines in a core module, three or more new files, or any high-risk label.\n When triggered, run `grill-with-docs` or `improve-codebase-architecture` and\n-record durable decisions in an ADR when needed (see `docs/adr/`).\n+record durable decisions in a Shiki SADR or target ADR as applicable (see `docs/adr/`).\n \n ## Safety\n \ndiff --git a/SYSTEM_PROMPT.md b/SYSTEM_PROMPT.md\nindex aaaea45e3a896c3a44ba89eec8bb84325ecf6c62..da009c86a88285269dee0155e58e550c6d744ea9\n--- a/SYSTEM_PROMPT.md\n+++ b/SYSTEM_PROMPT.md\n@@ -30,6 +30,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n Use the canonical source-of-truth order above. Runtime-specific reading orders\n are operational guidance only and must not override or reorder it. The current\n conversation is non-durable operator input.\n@@ -48,14 +51,14 @@\n \n A runtime may only do work that is explicitly assigned by the Goal, task, PR, or MergeGate decision.\n \n-Do not assume `openai/codex-action`, `OPENAI_API_KEY`, or API-key based Codex automation in the default Shiki path. API-key runners are explicit target-repo extensions and require their own ADR.\n+Do not assume `openai/codex-action`, `OPENAI_API_KEY`, or API-key based Codex automation in the default Shiki path. API-key runners are explicit target-repo extensions and require their own target ADR.\n \n ## 4. Non-negotiable Shiki flow\n \n For any non-trivial Goal, use this flow:\n \n 1. **Goal Seek**: define outcome, non-goals, risk level, completion criteria, and evidence requirements.\n-2. **grill-with-docs**: challenge the plan against `CONTEXT.md`, ADRs, code reality, terminology, boundaries, and edge scenarios. Ask one question at a time when the operator is available. If code can answer a question, inspect code instead of asking.\n+2. **grill-with-docs**: challenge the plan against `CONTEXT.md`, Shiki SADRs, target ADRs, code reality, terminology, boundaries, and edge scenarios. Ask one question at a time when the operator is available. If code can answer a question, inspect code instead of asking.\n 3. **Context & Impact**: identify modules, interfaces, seams, callers, dependencies, locks, risk, and verification surfaces.\n 4. **to-prd + Spec Freeze**: turn settled context into a PRD using domain vocabulary and testing decisions. The operator's explicit approval of the PRD is Spec Freeze, recorded as the plan's `spec_freeze` block; plans without it do not run. Steps 1-4 form Requirements Definition.\n 5. **to-issues**: decompose the PRD into independently grabbable vertical-slice issues. Prefer AFK slices; mark HITL slices when judgment is still required.\n@@ -75,7 +78,7 @@\n Before making any material change:\n \n 1. Read `AGENTS.md` and any runtime-specific wrapper such as `CLAUDE.md`.\n-2. Read `CONTEXT.md` and relevant ADRs.\n+2. Read `CONTEXT.md` and relevant SADRs and target ADRs.\n 3. Read the GitHub Issue / PR and `.shiki/` state for the active Goal or task.\n 4. Identify your role: Planner, Implementer, Reviewer, Completion Check Agent, Repairer, or Guardian-assist.\n 5. Identify Goal id, task id, branch, PR, dependencies, locks, risk level, required skills, and required evidence.\n@@ -103,7 +106,7 @@\n \n At minimum, identify:\n \n-- Relevant documents, ADRs, domain terms, and prior decisions.\n+- Relevant documents, SADRs, target ADRs, domain terms, and prior decisions.\n - Relevant modules, interfaces, seams, callers, owners, and tests.\n - Dependency relationships between tasks.\n - Candidate file locks and lock conflicts.\n@@ -152,7 +155,7 @@\n \n CCA must read:\n \n-- `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant ADRs.\n+- `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant SADRs and target ADRs.\n - Parent Goal issue, PRD issue, task issue, comments, and labels.\n - PR body, diff, commits, changed files, reviews, CI checks, and artifacts available to the workflow.\n - `.shiki/` task contract, locks, ledger entries, CCA checklist profile, and prior repair packets.\n@@ -214,7 +217,7 @@\n - Three or more new files.\n - Any high-risk or critical label.\n \n-When the architecture gate triggers, run `grill-with-docs` or `improve-codebase-architecture` as appropriate and record the decision in an ADR if it is hard to reverse or likely to be re-litigated.\n+When the architecture gate triggers, run `grill-with-docs` or `improve-codebase-architecture` as appropriate and record the decision in a Shiki SADR or target ADR as applicable if it is hard to reverse or likely to be re-litigated.\n \n ## 14. Skill Gate\n \n@@ -223,7 +226,7 @@\n Use these skills:\n \n - `setup-matt-pocock-skills`: before first use of the engineering skill set, or when issue tracker, triage labels, or domain docs are missing.\n-- `grill-with-docs`: default for non-trivial Goals before PRD/issues; required for ambiguous plans, terminology, boundaries, ADR-worthy tradeoffs, or design-tree decisions.\n+- `grill-with-docs`: default for non-trivial Goals before PRD/issues; required for ambiguous plans, terminology, boundaries, decision-record-worthy tradeoffs, or design-tree decisions.\n - `zoom-out`: unfamiliar code area or missing architectural map.\n - `to-prd`: convert settled context into a PRD.\n - `to-issues`: break a PRD, plan, or Goal into independently grabbable vertical-slice issues.\ndiff --git a/docs/adr/0001-keep-shiki-as-platform-name.md b/docs/adr/SADR-0001-keep-shiki-as-platform-name.md\nindex 20a9637c3b9d8c97de7ba8db7cfae4d82d9a7dba..8b685439959aada1b221c112f0ac3c51ca8a1695\n--- a/docs/adr/0001-keep-shiki-as-platform-name.md\n+++ b/docs/adr/SADR-0001-keep-shiki-as-platform-name.md\n@@ -1,3 +1,3 @@\n-# Keep Shiki As The Platform Name\n+# SADR-0001: Keep Shiki As The Platform Name\n \n Shiki remains the product name for the GitHub-native agentic engineering control plane. We considered more descriptive names such as MergePilot, MergeGate, and Kiroku, but kept Shiki because it carries the existing goal-seeking framework identity while remaining independent of Hermes, Codex, Claude Code, or any single runtime.\ndiff --git a/docs/adr/0002-use-agents-as-shared-constitution.md b/docs/adr/SADR-0002-use-agents-as-shared-constitution.md\nindex 73de30ecb078120eac540d9bc51855e3cd48d0e3..c5486182bf06fca36ea508496b013c67302fd989\n--- a/docs/adr/0002-use-agents-as-shared-constitution.md\n+++ b/docs/adr/SADR-0002-use-agents-as-shared-constitution.md\n@@ -1,3 +1,3 @@\n-# Use AGENTS As The Shared Agent Constitution\n+# SADR-0002: Use AGENTS As The Shared Agent Constitution\n \n Shiki uses `AGENTS.md` as the runtime-neutral constitution and `CLAUDE.md` as a thin Claude Code wrapper. This keeps Codex, Claude Code, Hermes Runner, and future Agent Runtimes aligned on the same GitHub-first MergeGate rules while still allowing runtime-specific instructions where needed.\ndiff --git a/docs/adr/0003-require-skill-gate-for-mergegate-readiness.md b/docs/adr/SADR-0003-require-skill-gate-for-mergegate-readiness.md\nindex 424be0c9af4c8f0136f1fde68a468c47ff84e764..7fc337987c7df9792f75f181dfcbed75fe5ffff2\n--- a/docs/adr/0003-require-skill-gate-for-mergegate-readiness.md\n+++ b/docs/adr/SADR-0003-require-skill-gate-for-mergegate-readiness.md\n@@ -1,3 +1,3 @@\n-# Require Skill Gate For MergeGate Readiness\n+# SADR-0003: Require Skill Gate For MergeGate Readiness\n \n Shiki requires relevant engineering skills to be invoked before execution when their triggers apply. This makes skills part of MergeGate readiness rather than optional prompt style, because Shiki depends on repeatable planning, issue decomposition, TDD, diagnosis, triage, and architecture review before autonomous or parallel agent work can be trusted.\ndiff --git a/docs/adr/0004-use-github-first-with-shiki-mirror.md b/docs/adr/SADR-0004-use-github-first-with-shiki-mirror.md\nindex d0a3a9174dc646a945a9d325954ffaf00c352040..a79c54ae9ee8eed2a0524192b864da4e22a92d3f\n--- a/docs/adr/0004-use-github-first-with-shiki-mirror.md\n+++ b/docs/adr/SADR-0004-use-github-first-with-shiki-mirror.md\n@@ -1,4 +1,4 @@\n-# ADR 0004: Use GitHub First With A Repository-Local Shiki Mirror\n+# SADR-0004: Use GitHub First With A Repository-Local Shiki Mirror\n \n ## Status\n \ndiff --git a/docs/adr/0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md b/docs/adr/SADR-0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\nindex b339baf49b89d9290e79c3d51b36e7b2bde3bd95..c30a0d8b1fde0224d0ac7820e7870dccf721580d\n--- a/docs/adr/0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\n+++ b/docs/adr/SADR-0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\n@@ -1,4 +1,4 @@\n-# ADR 0005: Use Codex As Front And Claude Code Action As The GitHub Runtime\n+# SADR-0005: Use Codex As Front And Claude Code Action As The GitHub Runtime\n \n ## Status\n \ndiff --git a/docs/adr/0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md b/docs/adr/SADR-0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\nindex 4bcae9d1e52f0a37b8e9fbafa5cd14b6ee1993bd..7a38538a6ae9929567f0ade67e7ea6a60d0b3f7e\n--- a/docs/adr/0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\n+++ b/docs/adr/SADR-0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\n@@ -1,4 +1,4 @@\n-# ADR 0006: Separate Codex Implementation, CCA Completion, And MergeGate Authorization\n+# SADR-0006: Separate Codex Implementation, CCA Completion, And MergeGate Authorization\n \n ## Status\n \ndiff --git a/docs/adr/0007-packaging-and-release.md b/docs/adr/SADR-0007-packaging-and-release.md\nindex 79b84ddb97167372b44641e5b7860e400ce80822..bb445a2e2e856d9d1f5fdbe35de8d4e8cec0fb32\n--- a/docs/adr/0007-packaging-and-release.md\n+++ b/docs/adr/SADR-0007-packaging-and-release.md\n@@ -1,4 +1,4 @@\n-# ADR 0007: Packaging And Release For Shiki\n+# SADR-0007: Packaging And Release For Shiki\n \n ## Status\n \n@@ -25,7 +25,7 @@\n   not introduce third-party runtime dependencies or break the shim.\n - Releases must run inside the operator's subscription-authenticated toolchain\n   and must not require extra secrets beyond the built-in `GITHUB_TOKEN`\n-  (consistent with ADR 0005's no-API-key-by-default stance).\n+  (consistent with SADR-0005's no-API-key-by-default stance).\n - `.shiki/manifest.json` already encodes the install surface (`install.include`,\n   `install.create_directories`, `install.exclude_from_commit`), and it must not\n   be mutated as part of this change.\n@@ -34,7 +34,7 @@\n \n This is a hard-to-reverse platform decision (it sets the version source of\n truth, the release trigger, and the public install contract), so it is recorded\n-as an ADR.\n+as a SADR.\n \n ## Decision\n \n@@ -104,7 +104,7 @@\n 3. **Publishing to PyPI.** Rejected for now: Shiki is distributed as a\n    repository template + git-installable CLI, not a PyPI library; PyPI would add\n    credentials/secrets and a different versioning contract. Can be added later as\n-   its own ADR if a packaged distribution is needed.\n+   its own SADR if a packaged distribution is needed.\n 4. **Dynamic version (read `VERSION` via a build backend `dynamic` field).**\n    Rejected to keep `pyproject.toml` backend-agnostic and dependency-free; the\n    small duplication between `VERSION` and `pyproject.toml` is guarded by the\n@@ -123,4 +123,4 @@\n   releasing versions the whole platform but ships only `install.include` to\n   targets.\n - Future PyPI distribution or a console entry point are deferred and would each\n-  warrant their own ADR.\n+  warrant their own SADR.\ndiff --git a/docs/adr/0008-use-claude-code-as-default-implementer.md b/docs/adr/SADR-0008-use-claude-code-as-default-implementer.md\nindex dedf4510b7c7799521422f622029563d373db031..35c2625d908167fbc58855b5b63e283805f167e8\n--- a/docs/adr/0008-use-claude-code-as-default-implementer.md\n+++ b/docs/adr/SADR-0008-use-claude-code-as-default-implementer.md\n@@ -1,12 +1,12 @@\n-# ADR 0008: Use Claude Code As The Default Implementer Runtime\n+# SADR-0008: Use Claude Code As The Default Implementer Runtime\n \n ## Status\n \n-Proposed (supersedes ADR 0005 for the implementer default; keeps its auth model)\n+Proposed (supersedes SADR-0005 for the implementer default; keeps its auth model)\n \n ## Context\n \n-ADR 0005 made Codex Front the default implementation runtime, with Claude Code\n+SADR-0005 made Codex Front the default implementation runtime, with Claude Code\n restricted to planner and reviewer roles by the runtime registry, and the only\n autonomous implementation adapter was `shiki runner codex`.\n \n@@ -18,13 +18,13 @@\n - The platform's planning, review, and CCA layers already run on Claude Code,\n   and Claude Code's native capabilities (the built-in `/code-review` skill and\n   the Workflow multi-agent orchestration tool) have no Codex equivalent.\n-- The Goal lifecycle redesign (ADR 0009) requires an autonomous post-freeze\n+- The Goal lifecycle redesign (SADR-0009) requires an autonomous post-freeze\n   loop driven from the operator's Claude Code session, including bounded\n   repair dispatch, which must not depend on a runtime that cannot be invoked\n   unattended.\n \n The constitution claims Shiki is runtime-agnostic (\"not a Claude-only\n-workflow\", ADR 0002). Any change to the implementer default must preserve the\n+workflow\", SADR-0002). Any change to the implementer default must preserve the\n registry-based runtime model rather than hardcoding one vendor.\n \n ## Decision\n@@ -39,7 +39,7 @@\n   the task to review or repair-needed.\n - Keep Codex registered as an optional implementer runtime. `shiki runner\n   codex`, the Codex handoff templates, and the ChatGPT OAuth auth model from\n-  ADR 0005 remain supported for tasks explicitly assigned to `codex`.\n+  SADR-0005 remain supported for tasks explicitly assigned to `codex`.\n - Commit to mandating two Claude-native capabilities as loop contract in\n   follow-up Goals (status: the code-review gate was delivered by Goal G-B \u2014\n   skill registry, checklists item PR-12, default required_skills \u2014 while the\n@@ -60,7 +60,7 @@\n \n Out of scope: removing Codex support, changing the CCA runtime (Claude Code\n Action with `CLAUDE_CODE_OAUTH_TOKEN` stays), and API-key based automation\n-(still requires its own ADR per ADR 0005).\n+(still requires its own target ADR per SADR-0005).\n \n ## Consequences\n \n@@ -100,7 +100,7 @@\n   harness rejection of direct `codex exec` is outside Shiki's control, and the\n   plan's mandatory native capabilities cannot run on Codex at all.\n - **Remove Codex entirely**: rejected; it abandons the runtime-agnostic\n-  constitution (ADR 0002) for no operational gain and forecloses\n+  constitution (SADR-0002) for no operational gain and forecloses\n   cross-runtime adversarial review.\n - **GitHub Actions cloud implementation via claude-code-action**: rejected as\n   default; durable evidence is automatic but token cost, Node 24 workflow\ndiff --git a/docs/adr/0009-requirements-definition-and-spec-freeze.md b/docs/adr/SADR-0009-requirements-definition-and-spec-freeze.md\nindex 29b3f7871bef9203a8b4a831a65c39eb2f506e42..52e7dd69ad0aa4efe0b68c62678abbf7df02aaec\n--- a/docs/adr/0009-requirements-definition-and-spec-freeze.md\n+++ b/docs/adr/SADR-0009-requirements-definition-and-spec-freeze.md\n@@ -1,4 +1,4 @@\n-# ADR 0009: Merge Grill And Planning Into Requirements Definition With Spec Freeze\n+# SADR-0009: Merge Grill And Planning Into Requirements Definition With Spec Freeze\n \n ## Status\n \ndiff --git a/docs/adr/0010-external-ai-guardian-review.md b/docs/adr/SADR-0010-external-ai-guardian-review.md\nindex e6108bbe8bb4e73e0ff4684c8f78001a80438c4e..f3510b145664298adafb151b6f0db51b972f01ec\n--- a/docs/adr/0010-external-ai-guardian-review.md\n+++ b/docs/adr/SADR-0010-external-ai-guardian-review.md\n@@ -1,4 +1,4 @@\n-# ADR 0010: External AI Guardian Review As A First-Class Approval Authority\n+# SADR-0010: External AI Guardian Review As A First-Class Approval Authority\n \n ## Status\n \n@@ -8,7 +8,7 @@\n \n Shiki is intended to be an autonomous, recursive, self-improving agent\n framework: authority-in-the-loop, not human-in-the-loop. Until now the Guardian\n-approval required for high/critical-risk merges (guardian-policy.json, ADR\n+approval required for high/critical-risk merges (guardian-policy.json, SADR\n governance) recognized only human authority sources \u2014 a GitHub review approval,\n the `guardian:approved` label applied by a configured Guardian, or a comment\n carrying `Guardian approval granted` plus the current head SHA.\n@@ -89,7 +89,7 @@\n   autonomous design the operator intends and produced the impersonation\n   incident.\n - **Record AI approval as an operator comment**: rejected as forgery of human\n-  authority \u2014 the exact failure this ADR corrects.\n+  authority \u2014 the exact failure this SADR corrects.\n - **Deliver the artifact as a committed `.shiki/` file**: rejected; a committed\n   approval inside the PR being approved is forgeable by anyone who can push.\n - **A dedicated reviewer bot identity instead of a relayed comment**: deferred;\ndiff --git a/docs/adr/0011-autonomous-quality-gate-evidence.md b/docs/adr/SADR-0011-autonomous-quality-gate-evidence.md\nindex bca0246c5d46e44afbde15486a75eec425fc02f5..9c93eac16d8c3a6d4b11bd7224e3955102cd7db4\n--- a/docs/adr/0011-autonomous-quality-gate-evidence.md\n+++ b/docs/adr/SADR-0011-autonomous-quality-gate-evidence.md\n@@ -1,4 +1,4 @@\n-# ADR 0011: Autonomous Quality-Gate Evidence \u2014 Deterministic Facts And An Independent Verifier\n+# SADR-0011: Autonomous Quality-Gate Evidence \u2014 Deterministic Facts And An Independent Verifier\n \n ## Status\n \ndiff --git a/docs/adr/0012-autonomous-loop-closeout-to-main.md b/docs/adr/SADR-0012-autonomous-loop-closeout-to-main.md\nindex 637fad6dde4989c9c2bf0f81f676d8d29991123c..ec7d85f81030a676bcbba28b43eb365422a36eac\n--- a/docs/adr/0012-autonomous-loop-closeout-to-main.md\n+++ b/docs/adr/SADR-0012-autonomous-loop-closeout-to-main.md\n@@ -1,4 +1,4 @@\n-# 0012 \u2014 Autonomous loop pushes goal completion to main via a closeout PR\n+# SADR-0012 \u2014 Autonomous loop pushes goal completion to main via a closeout PR\n \n - Status: accepted\n - Date: 2026-06-18\ndiff --git a/docs/adr/0013-bootstrap-grants-actions-approve-pull-requests.md b/docs/adr/SADR-0013-bootstrap-grants-actions-approve-pull-requests.md\nindex b6c69d64f1c5ced346b88d6859586715b3b5bd0e..cf48ff4a6716267f539ca58edd1a852e7c7a2d64\n--- a/docs/adr/0013-bootstrap-grants-actions-approve-pull-requests.md\n+++ b/docs/adr/SADR-0013-bootstrap-grants-actions-approve-pull-requests.md\n@@ -1,4 +1,4 @@\n-# ADR 0013: Bootstrap Grants GitHub Actions Permission To Create And Approve Pull Requests\n+# SADR-0013: Bootstrap Grants GitHub Actions Permission To Create And Approve Pull Requests\n \n ## Status\n \n@@ -31,7 +31,7 @@\n security tradeoff: it lets repository automation satisfy a human-review gate. In\n a multi-maintainer repo that could let a compromised or buggy workflow\n self-approve changes. So whether bootstrap turns it on by default \u2014 and with\n-what default token scope \u2014 is an ADR-worthy decision, not an implementation\n+what default token scope \u2014 is a SADR-worthy decision, not an implementation\n detail.\n \n ## Decision\n@@ -48,7 +48,7 @@\n Specifically:\n \n - **`can_approve_pull_request_reviews` defaults to `true`.** Shiki's default\n-  posture is solo/AFK autonomous operation (ADR 0008/0009/0012), where the CCA\n+  posture is solo/AFK autonomous operation (SADR-0008/0009/0012), where the CCA\n   Review Bridge is the only path that can satisfy `required_review: true` after\n   the CCA and Guardian gates have already passed. The default makes a\n   freshly-bootstrapped repo able to run the autonomous loop end to end without a\ndiff --git a/docs/adr/0014-codex-app-external-ai-guardian-ui-adapter.md b/docs/adr/SADR-0014-codex-app-external-ai-guardian-ui-adapter.md\nindex 238db06279334caa341c117362df9271382a7fcd..65f12e9939321e1b3e389a34c822fc1549e5155d\n--- a/docs/adr/0014-codex-app-external-ai-guardian-ui-adapter.md\n+++ b/docs/adr/SADR-0014-codex-app-external-ai-guardian-ui-adapter.md\n@@ -1,4 +1,4 @@\n-# ADR 0014: Use Codex App As The External AI Guardian UI Adapter\n+# SADR-0014: Use Codex App As The External AI Guardian UI Adapter\n \n ## Status\n \ndiff --git a/docs/adr/0015-contract-approval-before-dispatch.md b/docs/adr/SADR-0015-contract-approval-before-dispatch.md\nindex fad6c656741a20a7684ba16080a7db159f63a52f..6061271a46d3f1d6ae7174477de3fb75673123a6\n--- a/docs/adr/0015-contract-approval-before-dispatch.md\n+++ b/docs/adr/SADR-0015-contract-approval-before-dispatch.md\n@@ -1,4 +1,4 @@\n-# ADR 0015: Approve The Task Contract Before Dispatch, Not The Diff After It\n+# SADR-0015: Approve The Task Contract Before Dispatch, Not The Diff After It\n \n ## Status\n \n@@ -9,7 +9,7 @@\n Guardian approval for high/critical risk is expressible only on a pull request.\n Every approval source in `guardian-policy.json` \u2014 GitHub review, the\n `guardian:approved` label, the `Guardian approval granted` comment, and the\n-`external_ai_guardian_review` artifact (ADR 0010/0014) \u2014 is bound to a PR, and\n+`external_ai_guardian_review` artifact (SADR-0010/0014) \u2014 is bound to a PR, and\n three of the four require the current head SHA. Approval therefore cannot exist\n before an implementation PR exists.\n \n@@ -81,7 +81,7 @@\n An envelope breach must surface only on the non-repairable policy gate, never on\n the repairable `MergeGate metadata check`. Otherwise the Goal Loop routes it to\n `dispatch_repair` \u2014 instructing a runner to make the breach go away \u2014 which is\n-exactly what this ADR forbids.\n+exactly what this SADR forbids.\n \n **Contract immutability.** After registration, an implementation PR must not\n change the governance fields of its own task contract (`scope`, `non_goals`,\n@@ -116,7 +116,7 @@\n fails any verdict that is not `complete`, and the signal the CCA prompt reads is\n computed from `evaluate_guardian_approval` alone. Without the OR-merge at the\n signal, a contract-approved PR still returns `needs_guardian`, the CCA job fails,\n-and the MergeGate policy check never runs \u2014 the gate this ADR changes would never\n+and the MergeGate policy check never runs \u2014 the gate this SADR changes would never\n be reached. The CCA *prompt* needs no change: a `contract_approval` entry in the\n signal's existing `sources` array satisfies its current wording.\n \n@@ -130,7 +130,7 @@\n written, and an approved contract lets its implementation PRs merge through the\n same automatic path as low/medium risk. The wasted implement-then-block cycle\n disappears. Guardian review shifts from reading diffs to reading contracts, which\n-is both earlier and smaller; the external AI Guardian path (ADR 0010/0014) works\n+is both earlier and smaller; the external AI Guardian path (SADR-0010/0014) works\n on contracts with no change.\n \n We accept that the Guardian authorizes a class of change without seeing its\n@@ -146,7 +146,7 @@\n adversarial tests for the new approval path equivalent in rigor to\n `test_shiki_governance_evidence.sh`.\n \n-Bootstrap costs, which apply only while this ADR is being implemented. The tasks\n+Bootstrap costs, which apply only while this SADR is being implemented. The tasks\n that build Contract Approval merge under the existing rules, so the ones\n dispatched after the pre-dispatch gate lands but before the approval path lands\n need a Guardian-approved Contract PR *and* a live Guardian approval on their\n@@ -210,7 +210,7 @@\n risk; they do not bound authorized work.\n \n **Auto-escalate risk on envelope breach instead of stopping.** Returns the\n-workflow to a diff-time Guardian decision \u2014 the exact behavior this ADR removes.\n+workflow to a diff-time Guardian decision \u2014 the exact behavior this SADR removes.\n \n **Resolve governance fields from the base snapshot only.** Rejected against the\n registration flow: implementation PRs add their own contract, so the base has no\ndiff --git a/docs/adr/0016-locks-bound-product-paths.md b/docs/adr/SADR-0016-locks-bound-product-paths.md\nindex b5e850f3f1f7f4ef6e7f9d1763a29a51bd300c45..46b40a9b99a7aae644c02bb458f5473ff0a90ce2\n--- a/docs/adr/0016-locks-bound-product-paths.md\n+++ b/docs/adr/SADR-0016-locks-bound-product-paths.md\n@@ -1,4 +1,4 @@\n-# ADR 0016: Locks bound product paths; the Shiki mirror is bound by record identity\n+# SADR-0016: Locks bound product paths; the Shiki mirror is bound by record identity\n \n ## Status\n \n@@ -6,8 +6,8 @@\n \n - Date: 2026-07-28\n - Deciders: operator (mizutani-140), Claude (planner)\n-- Related: ADR 0004 (GitHub-first with a `.shiki` mirror), ADR 0009 (Spec Freeze),\n-  ADR 0012 (autonomous loop closeout to main), ADR 0015 (Contract Approval before\n+- Related: SADR-0004 (GitHub-first with a `.shiki` mirror), SADR-0009 (Spec Freeze),\n+  SADR-0012 (autonomous loop closeout to main), SADR-0015 (Contract Approval before\n   dispatch), remediation goal task R-02, the live PR #179 deadlock.\n \n ## Context\n@@ -90,11 +90,11 @@\n files, documentation files and named source modules are nameable; runtime-\n allocated ledger, runner and report ids are not \u2014 and those live in `.shiki`,\n which locks no longer govern. Existing frozen contracts are brought into line by\n-a one-time Spec Amendment per plan, which must land before ADR 0015's contract\n+a one-time Spec Amendment per plan, which must land before SADR-0015's contract\n immutability makes `locks` unwritable from the implementation PR.\n \n **Lock state is resolved from the durable side.** MergeGate resolves the lock\n-registry, and ADR 0015's governance fields, from the base `.shiki` snapshot when\n+registry, and SADR-0015's governance fields, from the base `.shiki` snapshot when\n present, falling back to head only when absent. The `MergeGate metadata check`\n job is given `--base-shiki`, which it does not receive today. Locks are not\n derived from the live open-pull-request set: a pull request body is untrusted\n@@ -108,7 +108,7 @@\n Explicitly out of scope: moving MergeGate's contract resolution off the PR\n checkout wholesale; making the mirror a projection of GitHub state; introducing\n new lock kinds; moving ledger evidence out of the repository; and unwinding\n-ADR 0012's closeout pull request. Registration of multi-task goals is ADR 0015's\n+SADR-0012's closeout pull request. Registration of multi-task goals is SADR-0015's\n Contract PR and is not re-decided here.\n \n ## Consequences\n@@ -127,7 +127,7 @@\n \n Risks accepted. First, goal-scoped identity is only as strong as `goal_id`,\n which is resolved from the PR body and the head task file; the mitigation is to\n-add `goal_id` to ADR 0015's frozen governance field set, and until that lands a\n+add `goal_id` to SADR-0015's frozen governance field set, and until that lands a\n task file can name a foreign goal. Second, base-when-present resolution is\n unavailable on fork pull requests, where the head is contributor-controlled;\n fork pull requests already cannot complete a Shiki merge, and this decision does\n@@ -139,7 +139,7 @@\n path, not by implementation pull requests.\n \n New obligations: one Spec Amendment per frozen plan to rewrite `locks`, ordered\n-before ADR 0015 T1; extension of the adversarial suites in\n+before SADR-0015 T1; extension of the adversarial suites in\n `scripts/test_shiki_adversarial_state.sh` and\n `scripts/test_shiki_governance_evidence.sh` to cover the eleven newly-scoped\n directories; and updating `scripts/test_shiki_loop_lock_guard.sh`,\n@@ -151,32 +151,32 @@\n whether the loop's transitive sync of ledger `evidence` references can be\n deleted once the Contract PR carries its own source plan.\n \n-## Relationship to ADR 0004\n+## Relationship to SADR-0004\n \n-This ADR **clarifies something ADR 0004 left unspecified**. ADR 0004 decides that\n+This SADR **clarifies something SADR-0004 left unspecified**. SADR-0004 decides that\n GitHub is the operational source of truth and that each target repository keeps a\n `.shiki/` mirror. It says nothing about pull requests writing the mirror, does\n not name MergeGate, and does not make the mirror an input to a gate. Nothing in\n-ADR 0004's Decision or Consequences is amended, superseded or weakened: the\n+SADR-0004's Decision or Consequences is amended, superseded or weakened: the\n mirror still exists, still records the same artifacts, and pull request\n-boundaries remain the coordination surface. What this ADR settles is the question\n+boundaries remain the coordination surface. What this SADR settles is the question\n 0004 never reached \u2014 by what authority a pull request may write a mirror record \u2014\n and it answers it the way `.shiki/manifest.json` already declares\n (`pr_mutation: current-task-or-current-goal-only`).\n \n-It **amends ADR 0012**, whose Decision item 5 states that \"Loop-executed tasks\n+It **amends SADR-0012**, whose Decision item 5 states that \"Loop-executed tasks\n declare the `path:.shiki/**` lock, which covers every `.shiki` file the closeout\n stages ... so `files_outside_locks` is satisfied without per-file lock\n bookkeeping.\" That clause is replaced: the closeout's mirror writes are\n-authorized by record identity, not by a declared lock. ADR 0012's substantive\n+authorized by record identity, not by a declared lock. SADR-0012's substantive\n decision \u2014 that completion reaches `main` through a closeout pull request rather\n than local attestation \u2014 is preserved unchanged.\n \n-It is **complementary to ADR 0015**, not a substitute. ADR 0015 owns contract\n+It is **complementary to SADR-0015**, not a substitute. SADR-0015 owns contract\n immutability, the pre-dispatch gate, the Contract PR and the Scope Envelope. This\n-ADR owns what a lock means and who may write the mirror. The one ordering\n+SADR owns what a lock means and who may write the mirror. The one ordering\n constraint between them is that the lock-narrowing Spec Amendment must precede\n-ADR 0015 T1's freezing of the `locks` field.\n+SADR-0015 T1's freezing of the `locks` field.\n \n ## Alternatives Considered\n \n@@ -186,7 +186,7 @@\n the only writer that can register them may run. Migrating the Guardian signal to\n that store makes every registration PR fail closed to `needs_guardian` at every\n risk level, because the task file is by definition absent from the store, which\n-removes the low/medium autonomous path ADR 0008/0009 exists to provide. And it\n+removes the low/medium autonomous path SADR-0008/0009 exists to provide. And it\n leaves schemas, manifest, config, guardian policy, templates, migrations, plans,\n worktrees, runner records, runs and handoffs with no writer at all in a\n repository whose product is `.shiki`. Measured benefit on the frozen backlog:\n@@ -199,14 +199,14 @@\n namespace is not in fact ownership-partitioned \u2014 56 ledger entries carry no\n `task_id`, and one goal DAG is writable by 35 registered tasks. The correct\n expression of its insight is that locks do not govern the mirror at all, which is\n-what this ADR decides.\n+what this SADR decides.\n \n **Make the mirror a projection written only by a registration writer and a\n post-merge projection job.** Rejected. It requires a bypass actor with write\n access to the protected default branch; it makes its own master invariant\n readable from a manifest that pull requests may edit; it deletes both reconcile\n modes and leaves a corrupted mirror with no in-band repair path, contradicting\n-ADR 0004's consequence that mirror conflicts are repaired against GitHub state;\n+SADR-0004's consequence that mirror conflicts are repaired against GitHub state;\n and its content-derived ids are rejected by `validate_shiki`'s currently-green id\n pattern.\n \ndiff --git a/docs/adr/0017-bookkeeping-closeouts-do-not-inherit-risk.md b/docs/adr/SADR-0017-bookkeeping-closeouts-do-not-inherit-risk.md\nindex 554e3a0d526a72ca067108c5464cc68c3edcf174..b9676857a4713850e063af7c39c8e0b01ecb4c66\n--- a/docs/adr/0017-bookkeeping-closeouts-do-not-inherit-risk.md\n+++ b/docs/adr/SADR-0017-bookkeeping-closeouts-do-not-inherit-risk.md\n@@ -1,4 +1,4 @@\n-# ADR 0017: A Bookkeeping Closeout Does Not Inherit Its Task's Risk\n+# SADR-0017: A Bookkeeping Closeout Does Not Inherit Its Task's Risk\n \n ## Status\n \n@@ -6,7 +6,7 @@\n \n ## Context\n \n-ADR 0012 made the goal loop push a task's completion to the default branch through a\n+SADR-0012 made the goal loop push a task's completion to the default branch through a\n closeout pull request: after the implementation PR merges, the loop opens a second PR\n carrying `task.status = done`, `lock.state = released`, and \u2014 when the task completes\n its goal \u2014 `goal.status = complete` plus one scorecard report. Nothing else. The\n@@ -49,14 +49,14 @@\n 4. The task file's governance fields (`scope`, `non_goals`, `required_skills`,\n    `risk_level`, `locks`, `acceptance_checks`, `test_command`) are byte-identical to\n    the base snapshot. Only `status`, `expected_pr`, `closeout_pr`, `ledger_evidence`\n-   and `expected_branch` may differ \u2014 the ADR 0015 contract-immutability rule,\n+   and `expected_branch` may differ \u2014 the SADR-0015 contract-immutability rule,\n    unchanged.\n \n    `expected_branch` is in that set because a closeout is *by construction* on a\n    different branch from the implementation it closes, and the loop rewrites the\n    field accordingly. It is safe to let it differ because MergeGate independently\n    requires `expected_branch` to equal the PR's actual head ref, so a PR cannot set\n-   it to anything other than the branch it is really on. The first draft of this ADR\n+   it to anything other than the branch it is really on. The first draft of this SADR\n    omitted it, which made the exemption unreachable: every real closeout failed\n    condition 4 on a field the loop always changes. See PR #209.\n 5. The state transitions are exactly the terminal set: task `review` \u2192 `done`, lock\n@@ -113,12 +113,12 @@\n classification must be derived, not asserted.\n \n **Fold the closeout into the implementation PR** so no second PR exists. Rejected under\n-ADR 0012, which recorded why: a task file that is already `done` on the implementation\n+SADR-0012, which recorded why: a task file that is already `done` on the implementation\n branch short-circuits the loop's review \u2192 CCA \u2192 merge \u2192 done ordering into a local\n goal-completion before any merge happens.\n \n **Have the loop auto-merge closeouts with elevated credentials.** Rejected outright: that\n-is a runtime authoring its own approval, which ADR 0010 and the constitution's\n+is a runtime authoring its own approval, which SADR-0010 and the constitution's\n approval-record prohibition forbid. It would also be indistinguishable, in the evidence,\n from the forgery those rules exist to prevent.\n \ndiff --git a/docs/adr/0018-carried-guardian-approval-across-a-proven-base-sync.md b/docs/adr/SADR-0018-carried-guardian-approval-across-a-proven-base-sync.md\nindex 22358562da390ed8b45cd600da08a43d378b4680..e6e2907cefa776460af73ddf7d39db605f3c39b0\n--- a/docs/adr/0018-carried-guardian-approval-across-a-proven-base-sync.md\n+++ b/docs/adr/SADR-0018-carried-guardian-approval-across-a-proven-base-sync.md\n@@ -1,4 +1,4 @@\n-# ADR 0018: Carried Guardian Approval Across A Proven Base Sync\n+# SADR-0018: Carried Guardian Approval Across A Proven Base Sync\n \n ## Status\n \n@@ -34,7 +34,7 @@\n Guardian evaluator that consumes it (`shiki_guardian.evaluate_guardian_approval`,\n the `guardian_comment_carried` source) were built and tested in isolation. Neither\n does anything until a caller passes the flags that turn the carry on, so the\n-feature was invisible in CI. This ADR records the decision the two of them\n+feature was invisible in CI. This SADR records the decision the two of them\n implement, and this task wires it into the four CI invocations that judge Guardian\n approval.\n \ndiff --git a/docs/adr/README.md b/docs/adr/README.md\nindex c70e057a4520c36e042ab68c53cd4a21ef709b71..5da43856836e2ce6a150f8127766e2f88355ab4b\n--- a/docs/adr/README.md\n+++ b/docs/adr/README.md\n@@ -1,40 +1,41 @@\n-# Architecture Decision Records\n+# Decision Record Namespaces\n \n-This directory holds Shiki's Architecture Decision Records (ADRs): durable\n-records of hard-to-reverse platform decisions. ADRs are part of the Shiki source\n-of truth alongside GitHub state, the `.shiki/` mirror, and `CONTEXT.md`.\n+This directory holds two independent decision namespaces:\n \n-## How ADRs Work\n+- Shiki Architecture Decision Records (SADRs), named\n+  `SADR-NNNN-<short-slug>.md`, record hard-to-reverse Shiki platform decisions.\n+- Target Architecture Decision Records (ADRs), named\n+  `NNNN-<short-slug>.md`, belong to the target product.\n \n-- New ADRs are added as `docs/adr/NNNN-<short-slug>.md`, where `NNNN` is the next\n-  zero-padded sequence number.\n-- The **canonical list of ADRs is the set of files matching `docs/adr/*.md`** in\n-  this directory. Browse `docs/adr/*.md` for the authoritative, current set.\n-- Use [`template.md`](template.md) as the starting point for a new ADR.\n-- Record an ADR when a decision is hard to reverse, surprising without context,\n-  and a real tradeoff. Do not write an ADR for routine or easily reversible\n-  choices.\n+The same four-digit number may appear once in each namespace. A platform\n+`SADR-NNNN` citation resolves only to `SADR-NNNN-*.md`; a target `ADR NNNN`\n+citation resolves only to `NNNN-*.md`.\n \n-Using the `docs/adr/*.md` glob as the source of truth means other PRs can add an\n-ADR without editing this index file, avoiding merge contention on a\n-hand-maintained enumeration.\n+## How Decision Records Work\n \n-## Current ADRs (snapshot)\n-\n-The list below is a convenience snapshot and may lag behind the directory. When\n-in doubt, list `docs/adr/*.md`.\n+- New Shiki platform decisions use the next zero-padded SADR number.\n+- New target product decisions use the target's own ADR sequence.\n+- The canonical list is the files in `docs/adr/`; no hand-maintained index is\n+  authoritative.\n+- Use [`template.md`](template.md) as the starting point and choose the filename\n+  namespace that owns the decision.\n+- Record a decision when it is hard to reverse, surprising without context, and\n+  a real tradeoff. Do not write a decision record for routine or easily\n+  reversible choices.\n \n-- [0001 \u2014 Keep Shiki As The Platform Name](0001-keep-shiki-as-platform-name.md)\n-- [0002 \u2014 Use AGENTS As The Shared Agent Constitution](0002-use-agents-as-shared-constitution.md)\n-- [0003 \u2014 Require Skill Gate For MergeGate Readiness](0003-require-skill-gate-for-mergegate-readiness.md)\n-- [0004 \u2014 Use GitHub First With A Repository-Local Shiki Mirror](0004-use-github-first-with-shiki-mirror.md)\n-- [0005 \u2014 Use Codex As Front And Claude Code Action As The GitHub Runtime](0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md)\n-- [0006 \u2014 Separate Codex Implementation, CCA Completion, And MergeGate Authorization](0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md)\n+Using the directory as the source of truth lets independent changes add records\n+without contending on an enumerated index.\n \n-## Adding A New ADR\n+## Adding A Shiki SADR\n \n-1. Copy `template.md` to `docs/adr/NNNN-<short-slug>.md`.\n+1. Copy `template.md` to `docs/adr/SADR-NNNN-<short-slug>.md`.\n 2. Fill in the title, status, context, decision, and consequences.\n-3. Set the status to `Proposed`, then `Accepted` once the decision is approved.\n-4. Open a PR following `CONTRIBUTING.md`. You do not need to edit this index for\n-   the ADR to be discoverable via `docs/adr/*.md`.\n+3. Set the status to `Proposed`, then `Accepted` once the proper authority has\n+   approved the decision.\n+4. Open a PR following `CONTRIBUTING.md`.\n+\n+## Adding A Target ADR\n+\n+1. Copy `template.md` to `docs/adr/NNNN-<short-slug>.md` using the target's own\n+   sequence.\n+2. Cite it as `ADR NNNN`; do not prefix its filename with `SADR-`.\ndiff --git a/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md b/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\nnew file mode 100644\nindex 0000000000000000000000000000000000000000..7455ce1f3bed02ddddb851ac21a1d1a648c9aeba\n--- /dev/null\n+++ b/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\n@@ -0,0 +1,55 @@\n+# SADR-0019: Separate Shiki Platform SADRs From Target ADRs\n+\n+## Status\n+\n+Accepted\n+\n+## Context\n+\n+Shiki installs its platform decision records into `docs/adr/` in every target\n+repository. Shiki and its targets previously used the same numeric\n+`NNNN-<slug>.md` namespace. A target that already owned ADRs 0013 through 0018\n+therefore acquired two records for each number during installation, making an\n+`ADR NNNN` citation ambiguous. Renumbering target decisions would rewrite target\n+history, while renumbering Shiki decisions per target would destroy their stable\n+platform identity.\n+\n+Upgrades also need to remove the former Shiki-owned numeric paths without ever\n+mistaking a target-owned ADR for platform content. The install stamp already\n+records exact shipped paths and content digests, so it can prove ownership only\n+when both still match.\n+\n+## Decision\n+\n+Shiki platform decisions use the `SADR` namespace:\n+\n+- records are named `docs/adr/SADR-NNNN-<slug>.md`;\n+- live platform citations use `SADR-NNNN`;\n+- target product decisions retain `docs/adr/NNNN-<slug>.md` and `ADR NNNN`;\n+- the reference validator resolves the two namespaces independently, so the same\n+  four-digit number may exist once in each namespace.\n+\n+Before a forced upgrade writes anything, the installer inventories the exact\n+former numeric Shiki ADR paths. It may delete a legacy path only when the\n+existing install stamp contains that exact path and its recorded SHA-256 digest\n+matches the file currently on disk. All ownership blockers are reported\n+together and abort the upgrade before its first write. A non-forced install\n+never performs this cleanup.\n+\n+Historical `.shiki/` evidence remains unchanged because it records the namespace\n+that existed when that evidence was produced.\n+\n+## Consequences\n+\n+- Targets may use any ADR number without colliding with Shiki's platform SADRs.\n+- Fresh installs preserve target ADR bytes and install a stable SADR set.\n+- Forced upgrades fail closed when legacy ownership cannot be proven, and a\n+  successful upgrade refreshes the stamp without removed legacy paths.\n+- Current governance, packaging, workflow, prompt, script, and skill references\n+  must move with the platform records.\n+\n+## Alternatives Considered\n+\n+- Collision-only refusal leaves affected targets permanently unupgradable.\n+- Renumbering target ADRs mutates target-owned history.\n+- Per-target Shiki renumbering makes platform citations non-portable.\ndiff --git a/docs/adr/template.md b/docs/adr/template.md\nindex a92a97daddd0b00ab9dd626cc8d86942da2eafed..17dd79175b5fee2105935557f691b40cf9492942\n--- a/docs/adr/template.md\n+++ b/docs/adr/template.md\n@@ -1,14 +1,14 @@\n-# NNNN: <Short Decision Title>\n+# <SADR-NNNN or NNNN>: <Short Decision Title>\n \n ## Status\n \n-Proposed | Accepted | Superseded by ADR-NNNN\n+Proposed | Accepted | Superseded by SADR-NNNN or target ADR NNNN\n \n ## Context\n \n What is the situation, constraint, or force that requires a decision? Reference\n-relevant domain terms (`CONTEXT.md`), prior ADRs, and code reality. State the\n-problem, not the solution.\n+relevant domain terms (`CONTEXT.md`), prior Shiki SADRs or target ADRs, and code\n+reality. State the problem, not the solution.\n \n ## Decision\n \ndiff --git a/docs/agents/autonomous-loop-e2e-acceptance.md b/docs/agents/autonomous-loop-e2e-acceptance.md\nindex bbf84c6081987f159ac6ad9ecf3c82b7aa72e7e8..5c13c1d02a05a90b0dc46cbef7c95a9846d0c237\n--- a/docs/agents/autonomous-loop-e2e-acceptance.md\n+++ b/docs/agents/autonomous-loop-e2e-acceptance.md\n@@ -40,8 +40,8 @@\n 3. `gh auth status` is logged in with merge permission on the repo.\n 4. Branch protection + required checks are configured exactly as the goal-loop\n    required-check set expects (see `docs/agents/branch-protection-smoke.md`).\n-5. The repo `.shiki/policy` permits autonomous low/medium-risk merges (ADR\n-   0008/0009); the MergeGate policy check is the recorded-authority gate.\n+5. The repo `.shiki/policy` permits autonomous low/medium-risk merges\n+   (SADR-0008/0009); the MergeGate policy check is the recorded-authority gate.\n \n The operator's only actions are: register the task and start the loop. **No\n manual pre-clearing** means after `shiki loop run` starts, the operator does not\ndiff --git a/docs/agents/checklists.md b/docs/agents/checklists.md\nindex 4a7f03ade346f53a1c1d82981f3fd744f9ab4547..0c5ec6c53dc7197b7b7a7d18e667b325babd6c45\n--- a/docs/agents/checklists.md\n+++ b/docs/agents/checklists.md\n@@ -32,18 +32,18 @@\n | ID | Item | Blocking |\n | --- | --- | --- |\n | GD-01 | Plan was challenged against `CONTEXT.md`. | blocking for non-trivial Goals |\n-| GD-02 | Relevant ADRs were read or absence was recorded. | blocking for non-trivial Goals |\n+| GD-02 | Relevant SADRs and target ADRs were read or absence was recorded. | blocking for non-trivial Goals |\n | GD-03 | Ambiguous terms were resolved or converted to HITL blockers. | blocking |\n | GD-04 | Edge scenarios were considered. | advisory |\n | GD-05 | Code reality was checked when code could answer. | blocking when applicable |\n | GD-06 | New domain terms were added to `CONTEXT.md` when needed. | blocking when applicable |\n-| GD-07 | ADR was created or explicitly skipped using the ADR criteria. | blocking when applicable |\n+| GD-07 | A SADR or target ADR was created or explicitly skipped using the decision-record criteria. | blocking when applicable |\n \n ## CI \u2014 Context & Impact Checklist\n \n | ID | Item | Blocking |\n | --- | --- | --- |\n-| CI-01 | Relevant docs and ADRs identified. | blocking |\n+| CI-01 | Relevant docs, SADRs, and target ADRs identified. | blocking |\n | CI-02 | Relevant modules, interfaces, seams, callers, and tests identified. | blocking |\n | CI-03 | Dependencies and blocked-by relationships identified. | blocking |\n | CI-04 | Candidate locks identified. | blocking |\n@@ -62,7 +62,7 @@\n | PRD-04 | Implementation decisions are recorded without volatile code detail. | blocking |\n | PRD-05 | Testing decisions are recorded. | blocking |\n | PRD-06 | Out-of-scope items are explicit. | blocking |\n-| PRD-07 | Relevant glossary/ADR links are present when applicable. | blocking |\n+| PRD-07 | Relevant glossary/SADR/target-ADR links are present when applicable. | blocking |\n \n ## SF \u2014 Spec Freeze Checklist\n \n@@ -98,7 +98,7 @@\n | H-02 | Branch/worktree target is present. | blocking |\n | H-03 | Scope and non-goals are present. | blocking |\n | H-04 | Dependencies and locks are present. | blocking |\n-| H-05 | Relevant docs, ADRs, modules, and tests are referenced. | blocking |\n+| H-05 | Relevant docs, SADRs, target ADRs, modules, and tests are referenced. | blocking |\n | H-06 | Required skills are stated. | blocking |\n | H-07 | TDD expectations are stated. | blocking when behavior work |\n | H-08 | Acceptance criteria are included. | blocking |\ndiff --git a/docs/agents/codex-handoff.md b/docs/agents/codex-handoff.md\nindex ede468f47cb277f69a857e8c45df703e4ed0151d..6f97bdffb67e44d7edea2c71e01ea28933c4eb6c\n--- a/docs/agents/codex-handoff.md\n+++ b/docs/agents/codex-handoff.md\n@@ -46,7 +46,7 @@\n \n ## Context\n - Domain terms:\n-- Relevant ADRs:\n+- Relevant SADRs and target ADRs:\n - Relevant docs:\n - Relevant modules/interfaces/seams:\n - Similar tests/prior art:\ndiff --git a/docs/agents/completion-check-agent.md b/docs/agents/completion-check-agent.md\nindex 13e4c035db35025f4e3cdcc5adfb24314ac4d521..61bfa1218cf36ca605250c91919e620f33238aa0\n--- a/docs/agents/completion-check-agent.md\n+++ b/docs/agents/completion-check-agent.md\n@@ -32,7 +32,7 @@\n - `AGENTS.md`\n - `CLAUDE.md`\n - `CONTEXT.md`\n-- relevant ADRs\n+- relevant SADRs and target ADRs\n - parent Goal issue\n - PRD issue or document\n - task issue\ndiff --git a/docs/agents/decision-control.md b/docs/agents/decision-control.md\nindex 7d8ee5ab03a5ccbcbd04056ae61de03785a2a9c9..8c94ecda00fffaa0e92c325ab83aa9776bd1dac1\n--- a/docs/agents/decision-control.md\n+++ b/docs/agents/decision-control.md\n@@ -11,7 +11,7 @@\n | Decision | Owner | When | Required inputs | Output | Enforcement surface |\n | --- | --- | --- | --- | --- | --- |\n | Is the Goal clear enough to plan? | Planner + Guardian when needed | Goal intake | User request, existing docs, risk signals | Goal readiness verdict | Goal checklist, issue template, ledger |\n-| Must `grill-with-docs` run? | Planner / Skill Gate | Before PRD or issue split | Ambiguity, domain terms, ADR conflicts, unclear boundaries | Skill invocation or explicit skip rationale | Skill Gate checklist |\n+| Must `grill-with-docs` run? | Planner / Skill Gate | Before PRD or issue split | Ambiguity, domain terms, SADR/target-ADR conflicts, unclear boundaries | Skill invocation or explicit skip rationale | Skill Gate checklist |\n | Is a PRD required? | Planner | After Goal context stabilizes | Goal, `grill-with-docs` output, Context & Impact output | PRD required / not required | PRD checklist, CCA evidence check |\n | Can the spec be frozen? | Operator only | End of Requirements Definition, before issue split | PRD/requirements summary, scope inventory (SF-02), open-question list | `spec_freeze` block in the plan + ledger evidence | `require_grilled_plan` (plan ingest/run), SF checklist |\n | Can a frozen spec change? | Operator only (Spec Amendment) | When implementation contradicts the frozen spec | Contested decisions, scoped re-grill result | recorded amendment + re-stamped freeze | SF checklist, ledger evidence |\ndiff --git a/docs/agents/domain.md b/docs/agents/domain.md\nindex 258da8ea81e87ac0a6b0b5b5e5c2fde980eaabb7..d57b0cf17ff3e1fa9e6e5885372bf083d8295421\n--- a/docs/agents/domain.md\n+++ b/docs/agents/domain.md\n@@ -5,7 +5,9 @@\n ## Layout\n \n - `CONTEXT.md` defines Shiki language. Keep it implementation-free.\n-- `docs/adr/` records hard-to-reverse decisions.\n+- `docs/adr/SADR-NNNN-*.md` records Shiki platform decisions.\n+- `docs/adr/NNNN-*.md` records target product decisions without sharing the\n+  SADR namespace.\n - `AGENTS.md` is the runtime-neutral agent constitution.\n - `CLAUDE.md` is the Claude Code wrapper around `AGENTS.md`.\n - `.shiki/` is the Target Repository mirror for Goals, PRDs, plans, Task DAGs, contracts, locks, worktree records, ledger entries, CCA verdicts, repair packets, reports, and handoffs.\n@@ -15,6 +17,6 @@\n \n - Use Shiki terms exactly when they exist.\n - Add glossary terms to `CONTEXT.md` only when the term is part of the domain, not a generic programming concept.\n-- Create ADRs only for hard-to-reverse, surprising, tradeoff-based decisions.\n+- Create SADRs or target ADRs only for hard-to-reverse, surprising, tradeoff-based decisions.\n - Keep product-specific language in the Target Repository, not in Shiki Template docs.\n-- If an ADR conflicts with a proposed implementation, surface the conflict before editing.\n+- If a SADR or target ADR conflicts with a proposed implementation, surface the conflict before editing.\ndiff --git a/docs/agents/execution-control.md b/docs/agents/execution-control.md\nindex ad8ee3cdb36be56fedc1f46408ca92b387d74047..bc63c3a73661d12b567016ba299f1a6b66bf9088\n--- a/docs/agents/execution-control.md\n+++ b/docs/agents/execution-control.md\n@@ -63,7 +63,7 @@\n | Branch name | Orchestrator | Orchestrator | Before implementer dispatch | branch policy | branch ref in task/ledger |\n | Lock reservation | Context & Impact | MergeGate | Before dispatch and before merge | `.shiki/locks/`, labels, PR metadata | lock id, owner, scope |\n | CI profile | Planner | MergeGate | During issue triage, rechecked at PR | labels, `.shiki/policy.yaml`, workflow matrix | required check list |\n-| Required status checks | Repo maintainer / Guardian | GitHub branch protection / rulesets | Repository setup, updated via ADR | GitHub settings | branch/ruleset config, ADR |\n+| Required status checks | Repo maintainer / Guardian | GitHub branch protection / rulesets | Repository setup, updated via target ADR | GitHub settings | branch/ruleset config, target ADR |\n | CCA judgment | CCA | GitHub required check / MergeGate | Every non-draft PR update | GitHub Action check | structured CCA verdict |\n | Repair assignment | CCA | MergeGate | After failed CCA/CI/review | repair packet, issue/PR comment | repair id and attempt count |\n | Merge readiness | MergeGate | GitHub branch protection / Guardian if needed | After CCA complete | required checks, review rules, rulesets | mergegate check output |\ndiff --git a/docs/agents/guardian-policy.md b/docs/agents/guardian-policy.md\nindex 4147c7adf05a47adb4fd24ada1a1c2e1f2ba6d2d..a20d9cc6c00a569bb057b9ca8d6b51c7fc4a9847\n--- a/docs/agents/guardian-policy.md\n+++ b/docs/agents/guardian-policy.md\n@@ -98,8 +98,8 @@\n `origin/<default_branch>` and reuses each job's existing base fetch; a shallow\n (`--depth`) or second base fetch would make the proof unresolvable and silently\n disable the carry, so neither is permitted. CCA reads `guardian_comment_carried` as\n-a recorded authority in the deterministic signal and does not re-derive it. See ADR\n-0018 for the full decision, the rejected diff/patch-equality and path-disjointness\n+a recorded authority in the deterministic signal and does not re-derive it. See\n+SADR-0018 for the full decision, the rejected diff/patch-equality and path-disjointness\n alternatives, and the accepted residual.\n \n ## External AI Guardian Review (SADR-0010)\ndiff --git a/docs/agents/implementation-policy.md b/docs/agents/implementation-policy.md\nindex c754144a9fb5bf4a38aa82a60e1670c59e626b44..85c37281bcad196c6b7b02343faf00b03cd9c57a\n--- a/docs/agents/implementation-policy.md\n+++ b/docs/agents/implementation-policy.md\n@@ -31,10 +31,13 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n ## Non-Negotiables\n \n 1. **Planning precedes implementation.** Non-trivial Goals start with `grill-with-docs` and Context & Impact.\n-2. **Domain language is controlled.** Terms must be checked against `CONTEXT.md`; hard-to-reverse tradeoffs must be checked against ADRs.\n+2. **Domain language is controlled.** Terms must be checked against `CONTEXT.md`; hard-to-reverse tradeoffs must be checked against Shiki SADRs and target ADRs.\n 3. **PRD precedes issue decomposition.** `to-prd` is used once the context is settled enough to become durable intent.\n 4. **Issues are vertical slices.** `to-issues` must produce independently grabbable tracer-bullet tasks, not horizontal layer tickets.\n 5. **Triage controls dispatch.** Only `ready-for-agent` / AFK tasks may be assigned to Codex without human input.\n@@ -95,19 +98,19 @@\n Process:\n \n - Challenge terms against `CONTEXT.md`.\n-- Check ADRs before reopening decisions.\n+- Check SADRs and target ADRs before reopening decisions.\n - Inspect code when code can answer a question.\n - Ask one question at a time when the operator is available.\n - Provide a recommended answer with each question.\n - Record resolved terms in `CONTEXT.md`.\n-- Create ADRs only for hard-to-reverse, surprising, tradeoff-based decisions.\n+- Create SADRs or target ADRs only for hard-to-reverse, surprising, tradeoff-based decisions.\n - Convert unresolved load-bearing questions into HITL blockers.\n \n Outputs:\n \n - Resolved design-tree decisions.\n - Updated glossary terms when needed.\n-- ADRs when justified.\n+- SADRs or target ADRs when justified.\n - HITL blockers when decisions remain unresolved.\n \n ## Phase 3 \u2014 Context & Impact\n@@ -117,7 +120,7 @@\n \n Outputs must include:\n \n-- Relevant documents and ADRs.\n+- Relevant documents, SADRs, and target ADRs.\n - Relevant modules, interfaces, seams, callers, and tests.\n - Candidate locks.\n - Dependencies.\n@@ -140,7 +143,7 @@\n - Testing decisions.\n - Out of scope.\n - Further notes.\n-- Links to relevant Goal, domain terms, and ADRs.\n+- Links to relevant Goal, domain terms, SADRs, and target ADRs.\n \n Phase 4 ends with Spec Freeze: the operator explicitly approves the PRD, the\n plan records a `spec_freeze` block (status, approved_by, source), and the SF\n@@ -240,7 +243,7 @@\n - Dependencies and locks.\n - Risk and Guardian requirements.\n - Ledger completeness.\n-- Documentation and ADR updates.\n+- Documentation and SADR/target-ADR updates.\n - Scope drift.\n \n CCA emits one of:\n@@ -301,7 +304,7 @@\n | Evidence failure | Behavior might be done, but proof is missing. | Add checks, PR evidence, ledger entry, or artifacts. |\n | Scope drift | PR changes unrelated behavior. | Revert unrelated changes or split into new Goal. |\n | Dependency block | Upstream task or lock not complete. | Block until dependency resolves. |\n-| Architecture block | Decision needs ADR or deep-module discussion. | `grill-with-docs` or `improve-codebase-architecture`. |\n+| Architecture block | Decision needs a SADR, target ADR, or deep-module discussion. | `grill-with-docs` or `improve-codebase-architecture`. |\n | Risk block | Security/production/policy change needs human approval. | Guardian decision. |\n \n ## Phase 5.5 \u2014 Execution Decision Control\ndiff --git a/docs/agents/runtime-auth-model.md b/docs/agents/runtime-auth-model.md\nindex 6a2e1a47e81ea89b5c68a210ec07772a9374babc..3c54748d24233dbd0ba8473950282547ef4ce935\n--- a/docs/agents/runtime-auth-model.md\n+++ b/docs/agents/runtime-auth-model.md\n@@ -148,7 +148,7 @@\n \n API-key based runners may be added by a target repository as an explicit extension. They are not the default Shiki template path.\n \n-If a repo opts into API-key mode, record the exception in an ADR and update the repo's `AGENTS.md`, `.shiki/config.yaml`, and workflow permissions.\n+If a repo opts into API-key mode, record the target-specific exception in a target ADR and update the repo's `AGENTS.md`, `.shiki/config.yaml`, and workflow permissions.\n \n ## GitHub Actions Runtime Compatibility\n \ndiff --git a/docs/agents/skill-gate.md b/docs/agents/skill-gate.md\nindex c50398b0d7c60a8598f2a5c88a10015730dced13..3acffe6444617bb1d54b77d4b4708158ec735bc6\n--- a/docs/agents/skill-gate.md\n+++ b/docs/agents/skill-gate.md\n@@ -9,7 +9,7 @@\n | Skill | Trigger | Expected output |\n | --- | --- | --- |\n | `setup-matt-pocock-skills` | First configuring a repo for these skills, or when issue tracker, triage labels, or domain docs are missing. | `docs/agents/` configuration and an `Agent skills` block in `AGENTS.md` or `CLAUDE.md`. |\n-| `grill-with-docs` | Ambiguous plan, unclear terminology, boundary decision, tradeoff, or ADR-worthy design choice. | Clarified decision tree, recommended answers, updates to `CONTEXT.md` or ADRs when decisions crystallize. |\n+| `grill-with-docs` | Ambiguous plan, unclear terminology, boundary decision, tradeoff, or decision-record-worthy design choice. | Clarified decision tree, recommended answers, updates to `CONTEXT.md`, SADRs, or target ADRs when decisions crystallize. |\n | `zoom-out` | Agent lacks a higher-level architectural map. | Map of relevant modules, callers, seams, and domain concepts. |\n | `to-prd` | Settled context should become a PRD. | PRD published to the issue tracker with problem, solution, user stories, decisions, testing, and out-of-scope notes. |\n | `to-issues` | Goal, PRD, or plan must become implementation tickets. | Vertical-slice issues with acceptance criteria, dependencies, and AFK/HITL classification. |\n@@ -17,7 +17,7 @@\n | `tdd` | Feature work or bug fix has observable behavior that can be specified with tests. | Red-green-refactor loop through public interfaces, one vertical slice at a time. |\n | `code-review` | The TDD loop is green and the implementer is about to create or update a PR. Default for every implementation task. | Pre-PR self-review of the task diff with fixes applied on the task branch; findings and resolutions recorded as a ledger entry naming the skill and a `## Pre-PR code review` PR body section. |\n | `diagnose` | Hard bug, failing check, regression, flaky behavior, or performance problem. | Reproduction loop, ranked hypotheses, instrumentation, fix, regression test, post-mortem. |\n-| `improve-codebase-architecture` | Structural friction, testability, AI-navigability, deep-module opportunity, or refactor request. | Architecture candidates and deepening recommendations informed by `CONTEXT.md` and ADRs. |\n+| `improve-codebase-architecture` | Structural friction, testability, AI-navigability, deep-module opportunity, or refactor request. | Architecture candidates and deepening recommendations informed by `CONTEXT.md`, SADRs, and target ADRs. |\n | `prototype` | A throwaway logic or UI prototype is needed to answer a design question. | Clearly marked disposable prototype and captured decision. |\n \n ## MergeGate Interaction\ndiff --git a/scripts/guardian_approval_signal.py b/scripts/guardian_approval_signal.py\nindex d059f92b2c82aa2b5d6ca421365537a87886c1cb..16924892714f4f5b9303c0ef0e325a7e70075630\n--- a/scripts/guardian_approval_signal.py\n+++ b/scripts/guardian_approval_signal.py\n@@ -199,8 +199,8 @@\n ) -> Any:\n     \"\"\"Evaluate SADR-0015 Contract Approval for the CCA Guardian signal.\n \n-    The SAME OR that MergeGate applies must reach this deterministic signal: ADR\n-    0015 is explicit that without it, a contract-approved PR still returns\n+    The SAME OR that MergeGate applies must reach this deterministic signal:\n+    SADR-0015 is explicit that without it, a contract-approved PR still returns\n     ``needs_guardian``, the CCA job fails, and the MergeGate policy check never\n     runs \u2014 the gate this change moves would never be reached. This resolves the\n     task the SAME way MergeGate does (the first Shiki task id in the PR body),\ndiff --git a/scripts/shiki_contract_approval.py b/scripts/shiki_contract_approval.py\nindex 941bcb96fdd8b9a85bc0c4f0f04d5dad9d04839c..4a4b418f6527413161db82670bec857a043360e2\n--- a/scripts/shiki_contract_approval.py\n+++ b/scripts/shiki_contract_approval.py\n@@ -142,7 +142,7 @@\n     changed_files_status: Any,\n     registration: Any,\n ) -> ContractApprovalResult:\n-    \"\"\"Whether this PR carries an SADR-0015 Contract Approval.\n+    \"\"\"Whether this PR carries a SADR-0015 Contract Approval.\n \n     The four conditions are evaluated IN ORDER, each failure recorded as a\n     distinct reason string:\ndiff --git a/scripts/shiki_guardian.py b/scripts/shiki_guardian.py\nindex a4ecb79bbda55fd5c8b06adfae2eb99760a03c45..4dd99bbe714e26f421fa19abac7a34ae1b195ecc\n--- a/scripts/shiki_guardian.py\n+++ b/scripts/shiki_guardian.py\n@@ -12,7 +12,7 @@\n # The carry re-derives its proof from git in-process on every judging invocation;\n # it is never read from an artifact, so there is nothing to go stale or be forged.\n # shiki_sync_proof is the ONLY guardian-family module that runs git, and every one\n-# of its invocations is read-only and local (ADR: prove-from-git base sync).\n+# of its invocations is read-only and local (SADR: prove-from-git base sync).\n from shiki_sync_proof import verify_pure_base_sync\n \n GUARDIAN_POLICY_PATH = \".shiki/guardian-policy.json\"\ndiff --git a/scripts/shiki_guardian_review.py b/scripts/shiki_guardian_review.py\nindex dd0f104d457899e0ba5ff1e162011c1e90b6f92b..e10ef58c046324e7ea3a3392feb3048ca77414ce\n--- a/scripts/shiki_guardian_review.py\n+++ b/scripts/shiki_guardian_review.py\n@@ -159,7 +159,7 @@\n     docs_only = _is_docs_only(changed_files)\n     if docs_only and matched == 0:\n         focus.append(\n-            \"Docs/ADR-only change: confirm the change is genuinely documentation/decision-only, introduces no behavior or contract drift, and stays consistent with the constitution and existing ADRs.\"\n+            \"Docs/decision-record-only change: confirm the change is genuinely documentation/decision-only, introduces no behavior or contract drift, and stays consistent with the constitution and existing SADRs and target ADRs.\"\n         )\n \n     # Conservative augmentation: unknown (no category and not docs-only), mixed\ndiff --git a/scripts/shiki_installer.py b/scripts/shiki_installer.py\nindex be064ab7a29ff6836f7f7a1d70500a43e0d11d7b..d3e3247edbf3e4be287a4667176d86f6500f8dc5\n--- a/scripts/shiki_installer.py\n+++ b/scripts/shiki_installer.py\n@@ -118,6 +118,7 @@\n # what lets a target carry it without a governance-schema change.\n INSTALL_STAMP_PATH = \".shiki/install-stamp.json\"\n INSTALL_STAMP_VERSION = 1\n+LEGACY_SHIKI_SADR_MAX = 18\n \n # Under --force the shipped surface splits three ways, so nothing is lost and\n # nothing is silently stale.\n@@ -237,9 +238,98 @@\n         )\n \n \n+def legacy_shiki_adr_paths() -> tuple[str, ...]:\n+    \"\"\"Former numeric paths for Shiki SADR-0001 through SADR-0018.\n+\n+    The authoritative slugs come from the current SADR filenames. SADR-0019 is\n+    the namespace decision itself and never shipped under a numeric legacy path.\n+    \"\"\"\n+    paths: list[str] = []\n+    for sadr in sorted((ROOT / \"docs\" / \"adr\").glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\")):\n+        number = sadr.name.split(\"-\", 2)[1]\n+        if int(number) <= LEGACY_SHIKI_SADR_MAX:\n+            paths.append(f\"docs/adr/{sadr.name.removeprefix('SADR-')}\")\n+    return tuple(paths)\n+\n+\n+def preflight_legacy_sadr_cleanup(target: Path, *, force: bool) -> tuple[str, ...]:\n+    \"\"\"Authorize exact legacy Shiki ADR removals before the first write.\n+\n+    A non-force install never removes existing files. A forced install needs no\n+    stamp when no legacy Shiki path is present. When legacy paths do exist, each\n+    must be named in a readable install stamp and its current bytes must match\n+    the recorded digest.\n+    \"\"\"\n+    if not force:\n+        return ()\n+    present = tuple(\n+        relative\n+        for relative in legacy_shiki_adr_paths()\n+        if (target / relative).exists() or (target / relative).is_symlink()\n+    )\n+    if not present:\n+        return ()\n+\n+    stamp_path = target / INSTALL_STAMP_PATH\n+    try:\n+        raw = stamp_path.read_text(encoding=\"utf-8\")\n+        stamp = json.loads(raw)\n+    except FileNotFoundError:\n+        detail = \"install stamp is absent\"\n+        blockers = [f\"{relative}: {detail}\" for relative in present]\n+    except (OSError, UnicodeDecodeError) as error:\n+        detail = f\"install stamp is unreadable ({error})\"\n+        blockers = [f\"{relative}: {detail}\" for relative in present]\n+    except json.JSONDecodeError as error:\n+        detail = f\"install stamp is malformed ({error})\"\n+        blockers = [f\"{relative}: {detail}\" for relative in present]\n+    else:\n+        if not isinstance(stamp, dict):\n+            blockers = [\n+                f\"{relative}: install stamp is malformed (expected an object)\"\n+                for relative in present\n+            ]\n+        else:\n+            digests = stamp.get(\"digests\")\n+            blockers = []\n+            if not isinstance(digests, dict):\n+                blockers.extend(\n+                    f\"{relative}: install stamp has no digest for this path\"\n+                    for relative in present\n+                )\n+            else:\n+                for relative in present:\n+                    expected = digests.get(relative)\n+                    if not isinstance(expected, str) or not expected:\n+                        blockers.append(\n+                            f\"{relative}: install stamp has no digest for this path\"\n+                        )\n+                        continue\n+                    try:\n+                        actual = _sha256_file(target / relative)\n+                    except OSError as error:\n+                        blockers.append(\n+                            f\"{relative}: legacy path is unreadable ({error})\"\n+                        )\n+                        continue\n+                    if actual != expected:\n+                        blockers.append(\n+                            f\"{relative}: digest mismatch (stamp {expected}, current {actual})\"\n+                        )\n+\n+    if blockers:\n+        joined = \"\\n\".join(f\"- {blocker}\" for blocker in blockers)\n+        raise ShikiError(\n+            \"legacy Shiki ADR cleanup is not authorized:\\n\"\n+            f\"{joined}\\nNo files were written.\"\n+        )\n+    return present\n+\n+\n def install_template(target: Path, *, force: bool, validate: bool) -> list[NewFileNote]:\n     # Preconditions first: never rewrite the tree of a target that cannot be\n     # upgraded cleanly (see refuse_pending_migrations).\n+    legacy_cleanup = preflight_legacy_sadr_cleanup(target, force=force)\n     refuse_pending_migrations(target)\n \n     # Resolve the target's required CODEOWNERS owner once from its\n@@ -250,6 +340,10 @@\n     # verbatim. See ``codeowners_required_owner``.\n     codeowners_owner = codeowners_required_owner(target)\n \n+    for relative in legacy_cleanup:\n+        (target / relative).unlink()\n+        info(f\"removed stamped legacy Shiki ADR: {target / relative}\")\n+\n     notes: list[NewFileNote] = []\n     for relative in TEMPLATE_PATHS:\n         source = ROOT / relative\ndiff --git a/scripts/shiki_loop.py b/scripts/shiki_loop.py\nindex 3611641da8aeb5038b1da90e3114b09a473ec6d8..84caa491b9cad0edc4176f209c13e9221e975681\n--- a/scripts/shiki_loop.py\n+++ b/scripts/shiki_loop.py\n@@ -424,7 +424,7 @@\n         # unresolvable CCA verdict has already produced its terminal stop and is never\n         # laundered into a head-moving sync (SADR-0010). It also never fires while the\n         # policy (Guardian) gate is red \u2014 for ANY reason, not only when it is the sole\n-        # red check \u2014 so a head-bound Guardian approval, or an SADR-0015\n+        # red check \u2014 so a head-bound Guardian approval, or a SADR-0015\n         # contract-approval carry that base movement defeated (the policy gate compares\n         # base and head task governance directly), is never silently restored by\n         # adopting base wholesale. It is bounded by a durable per-task counter so the\ndiff --git a/scripts/shiki_tasks.py b/scripts/shiki_tasks.py\nindex aec3809f24b4a0c8c96fc50971cff9ae9ad30381..d531052e9d9717058d4631483cefab44c26a65c1\n--- a/scripts/shiki_tasks.py\n+++ b/scripts/shiki_tasks.py\n@@ -821,7 +821,7 @@\n         \"prompt\": prompt,\n         \"entry_skill\": \"grill-with-docs\",\n         \"required_next_steps\": [\n-            \"Run grill-with-docs until terminology, boundaries, risks, and ADR-worthy decisions are settled.\",\n+            \"Run grill-with-docs until terminology, boundaries, risks, and decision-record-worthy choices are settled.\",\n             \"Obtain the operator's explicit approval of the PRD/requirements (Spec Freeze).\",\n             \"Write a machine-readable plan JSON with grill_with_docs.status=complete and spec_freeze.status=frozen.\",\n             \"Run shiki plan ingest --plan-file PLAN.json.\",\ndiff --git a/scripts/test_shiki_goal_loop.sh b/scripts/test_shiki_goal_loop.sh\nindex 753c7b516e10f0be3bc92a4bd0eb1fa699776ae6..8f3820a0a079b3044424cfe1a6c6b7fe68896e67\n--- a/scripts/test_shiki_goal_loop.sh\n+++ b/scripts/test_shiki_goal_loop.sh\n@@ -231,8 +231,8 @@\n \n # High/critical risk merges autonomously when all required checks are green:\n # the \"MergeGate policy check\" required check IS the Guardian gate (it enforces\n-# guardian-policy.json \u2014 human approval OR external AI guardian review, ADR\n-# 0010), so green checks mean a recorded authority approved. The fake gh returns\n+# guardian-policy.json \u2014 human approval OR external AI guardian review,\n+# SADR-0010), so green checks mean a recorded authority approved. The fake gh returns\n # green checks, simulating that the Guardian gate passed.\n python3 \"$ROOT/scripts/shiki.py\" goal create --target \"$TARGET\" --title \"Guardian gate\" --outcome \"High risk merges via policy gate\" >/tmp/shiki-goal-loop-guardian-goal.json\n GGOAL=\"$(json_get /tmp/shiki-goal-loop-guardian-goal.json goal_id)\"\ndiff --git a/tests/test_adr_references.py b/tests/test_adr_references.py\nindex 51706329c797d5dd9b47e082a333fa83b261d482..db6e693e35f67103935be34f1d9bb4ff7ee60395\n--- a/tests/test_adr_references.py\n+++ b/tests/test_adr_references.py\n@@ -1,7 +1,7 @@\n-\"\"\"Every ``ADR ####`` citation must resolve to exactly one decision record.\n+\"\"\"Every target ADR and Shiki SADR citation resolves in its own namespace.\n \n Background. ``scripts/mergegate_check.py`` justifies ``is_bookkeeping_closeout``\n-with \"ADR 0017\" nine times and ``scripts/guardian_approval_signal.py`` cites\n+with \"SADR-0017\" nine times and ``scripts/guardian_approval_signal.py`` cites\n 0016/0017 seven times, yet for a stretch the numbering in ``docs/adr/`` stopped\n at 0015: the authoring commits lived on a coordinator branch and no task ever\n declared ``docs/adr/...`` in its locks, so nothing carried the records to the\n@@ -11,11 +11,12 @@\n and the directory drifted with no signal.\n \n This is the same unbound-pair shape behind other silent failures: a reference\n-and its target with nothing asserting they agree. This test binds them. It scans\n-every ``scripts/*.py`` (plus ``docs/**/*.md`` and the root ``*.md`` governance\n-files, which are cheap) for the ``ADR ####`` pattern and asserts each cited\n-number resolves to exactly one ``docs/adr/####-*.md``. The converse is\n-deliberately NOT required: an ADR may exist without being cited.\n+and its target with nothing asserting they agree. This test binds them. Target\n+``ADR NNNN`` citations resolve only to ``docs/adr/NNNN-*.md``; Shiki\n+``SADR-NNNN`` citations resolve only to ``docs/adr/SADR-NNNN-*.md``. The scan\n+covers current governance docs, scripts, workflows, prompts, packaging metadata,\n+and repo-local Codex guidance while deliberately excluding historical\n+``.shiki`` evidence. The converse is not required: a record may be uncited.\n \n The suite is dependency free: standard-library ``unittest`` only.\n \"\"\"\n@@ -31,17 +32,18 @@\n \n REPO_ROOT = Path(__file__).resolve().parents[1]\n \n-# \"ADR 0016\" -- a four-digit citation. The trailing \\b keeps a five-digit token\n-# from being read as a truncated four-digit number.\n-_CITATION = re.compile(r\"\\bADR (\\d{4})\\b\")\n-# \"0016-locks-bound-product-paths.md\" -- the leading four digits name the record.\n-_ADR_FILENAME = re.compile(r\"^(\\d{4})-.*\\.md$\")\n+# ``ADR 0016`` / ``SADR-0016`` -- a namespace plus four-digit citation. The\n+# trailing ``\\b`` keeps a five-digit token from being read as a truncated one.\n+_CITATION = re.compile(r\"\\b(S?ADR)[ -](\\d{4})\\b\")\n+# Target ADRs retain the numeric filename; Shiki platform decisions use SADR.\n+_DECISION_FILENAME = re.compile(r\"^(?:(SADR)-)?(\\d{4})-.*\\.md$\")\n \n \n class Violation(NamedTuple):\n     \"\"\"A citation that does not resolve to exactly one decision record.\"\"\"\n \n     citing_file: str  # repo-relative path of the file that cites the ADR\n+    namespace: str  # \"ADR\" for target decisions; \"SADR\" for Shiki decisions\n     number: str  # the cited four-digit number, e.g. \"0016\"\n     line: int  # 1-based line of the citation\n     reason: str  # \"missing\" (zero matches) or \"ambiguous\" (two or more)\n@@ -49,44 +51,59 @@\n \n     @property\n     def expected_path(self) -> str:\n-        return f\"docs/adr/{self.number}-*.md\"\n+        prefix = \"SADR-\" if self.namespace == \"SADR\" else \"\"\n+        return f\"docs/adr/{prefix}{self.number}-*.md\"\n \n     def describe(self) -> str:\n         \"\"\"Name the citing file, the cited number, and the missing path.\"\"\"\n         if self.reason == \"missing\":\n             return (\n-                f\"{self.citing_file}:{self.line} cites ADR {self.number} \"\n+                f\"{self.citing_file}:{self.line} cites {self.namespace} {self.number} \"\n                 f\"but no document matches {self.expected_path}\"\n             )\n         joined = \", \".join(self.matches)\n         return (\n-            f\"{self.citing_file}:{self.line} cites ADR {self.number} which \"\n+            f\"{self.citing_file}:{self.line} cites {self.namespace} {self.number} which \"\n             f\"resolves to {len(self.matches)} documents (expected exactly one \"\n             f\"at {self.expected_path}): {joined}\"\n         )\n \n \n-def _adr_index(root: Path) -> dict[str, list[Path]]:\n-    \"\"\"Map every four-digit ADR number to the record files that claim it.\"\"\"\n-    index: dict[str, list[Path]] = {}\n+def _adr_index(root: Path) -> dict[tuple[str, str], list[Path]]:\n+    \"\"\"Map each decision namespace and number to the files that claim it.\"\"\"\n+    index: dict[tuple[str, str], list[Path]] = {}\n     adr_dir = root / \"docs\" / \"adr\"\n     if adr_dir.is_dir():\n         for path in sorted(adr_dir.glob(\"*.md\")):\n-            match = _ADR_FILENAME.match(path.name)\n+            match = _DECISION_FILENAME.match(path.name)\n             if match:\n-                index.setdefault(match.group(1), []).append(path)\n+                namespace = \"SADR\" if match.group(1) else \"ADR\"\n+                index.setdefault((namespace, match.group(2)), []).append(path)\n     return index\n \n \n def _citing_files(root: Path, include_docs: bool = True) -> list[Path]:\n-    \"\"\"Files scanned for citations: scripts/*.py, and cheaply the markdown.\"\"\"\n+    \"\"\"Current, shipped text surfaces scanned for decision citations.\"\"\"\n     files = sorted((root / \"scripts\").glob(\"*.py\"))\n+    files += sorted((root / \"scripts\").glob(\"*.sh\"))\n     if include_docs:\n         docs = root / \"docs\"\n         if docs.is_dir():\n             files += sorted(docs.rglob(\"*.md\"))\n         files += sorted(root.glob(\"*.md\"))\n-    return files\n+        pyproject = root / \"pyproject.toml\"\n+        if pyproject.is_file():\n+            files.append(pyproject)\n+        for relative in (\".github\", \".codex\"):\n+            surface = root / relative\n+            if surface.is_dir():\n+                files += sorted(\n+                    path\n+                    for path in surface.rglob(\"*\")\n+                    if path.is_file()\n+                    and path.suffix in {\".md\", \".py\", \".sh\", \".toml\", \".yaml\", \".yml\"}\n+                )\n+    return sorted(set(files))\n \n \n def find_adr_reference_violations(\n@@ -94,10 +111,9 @@\n ) -> list[Violation]:\n     \"\"\"Return every ``ADR ####`` citation under ``root`` that fails to resolve.\n \n-    A citation resolves when exactly one ``docs/adr/####-*.md`` matches its\n-    number. Zero matches is a \"missing\" violation; two or more is \"ambiguous\".\n-    Records that are never cited produce nothing -- the converse is not\n-    required.\n+    A citation resolves when exactly one record in the same namespace matches\n+    its number. Zero matches is a \"missing\" violation; two or more is\n+    \"ambiguous\". Records that are never cited produce nothing.\n     \"\"\"\n     root = Path(root)\n     index = _adr_index(root)\n@@ -110,13 +126,15 @@\n         rel = path.relative_to(root).as_posix()\n         for lineno, line in enumerate(text.splitlines(), start=1):\n             for match in _CITATION.finditer(line):\n-                number = match.group(1)\n-                found = index.get(number, [])\n+                namespace, number = match.groups()\n+                found = index.get((namespace, number), [])\n                 if len(found) == 1:\n                     continue\n                 reason = \"missing\" if not found else \"ambiguous\"\n                 rels = [p.relative_to(root).as_posix() for p in found]\n-                violations.append(Violation(rel, number, lineno, reason, rels))\n+                violations.append(\n+                    Violation(rel, namespace, number, lineno, reason, rels)\n+                )\n     return violations\n \n \n@@ -129,8 +147,8 @@\n         self.assertEqual(\n             violations,\n             [],\n-            \"Every 'ADR ####' citation must resolve to exactly one \"\n-            f\"docs/adr/####-*.md:\\n{detail}\",\n+            \"Every ADR/SADR citation must resolve exactly once in its own \"\n+            f\"docs/adr namespace:\\n{detail}\",\n         )\n \n     def test_scan_actually_reads_scripts(self) -> None:\n@@ -146,8 +164,21 @@\n         # The two records this Goal carried onto the default branch, and which\n         # the scripts cite most, must be present.\n         index = _adr_index(REPO_ROOT)\n-        self.assertIn(\"0016\", index)\n-        self.assertIn(\"0017\", index)\n+        self.assertIn((\"SADR\", \"0016\"), index)\n+        self.assertIn((\"SADR\", \"0017\"), index)\n+\n+    def test_platform_tree_has_only_sadr_0001_through_0019(self) -> None:\n+        adr_dir = REPO_ROOT / \"docs\" / \"adr\"\n+        sadrs = sorted(adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\"))\n+        self.assertEqual(\n+            {path.name.split(\"-\", 2)[1] for path in sadrs},\n+            {f\"{number:04d}\" for number in range(1, 20)},\n+        )\n+        self.assertEqual(\n+            list(adr_dir.glob(\"[0-9][0-9][0-9][0-9]-*.md\")),\n+            [],\n+            \"the Shiki platform tree must not retain legacy numeric SADR paths\",\n+        )\n \n \n class FixtureAdrReferences(unittest.TestCase):\n@@ -180,6 +211,26 @@\n         self.assertIn(\"9991\", message)  # cited number\n         self.assertIn(\"docs/adr/9991-*.md\", message)  # missing path\n \n+    def test_target_adr_does_not_satisfy_missing_platform_sadr(self) -> None:\n+        root = self._make_repo(\n+            adr_names=[\"0042-target-decision.md\"],\n+            script_body='\"\"\"Target ADR 0042 exists, but platform SADR-0042 does not.\"\"\"\\n',\n+        )\n+        violations = find_adr_reference_violations(root, include_docs=False)\n+        self.assertEqual(len(violations), 1)\n+        self.assertEqual(violations[0].namespace, \"SADR\")\n+        self.assertEqual(violations[0].number, \"0042\")\n+        self.assertEqual(violations[0].expected_path, \"docs/adr/SADR-0042-*.md\")\n+\n+    def test_target_adr_and_platform_sadr_may_reuse_a_number(self) -> None:\n+        root = self._make_repo(\n+            adr_names=[\"0042-target-decision.md\", \"SADR-0042-platform-decision.md\"],\n+            script_body='\"\"\"Target ADR 0042 and platform SADR-0042 are distinct.\"\"\"\\n',\n+        )\n+        self.assertEqual(\n+            find_adr_reference_violations(root, include_docs=False), []\n+        )\n+\n     def test_uncited_adr_is_allowed(self) -> None:\n         # An ADR exists but nothing cites it -> not a violation. The converse\n         # (every ADR must be cited) is explicitly NOT required.\ndiff --git a/tests/test_guardian_review.py b/tests/test_guardian_review.py\nindex 88829e7810212635e2fc98aa5918d6f26294677d..00a85a237acd5bf2d6cb5b294128198c422402ca\n--- a/tests/test_guardian_review.py\n+++ b/tests/test_guardian_review.py\n@@ -162,7 +162,7 @@\n \n     def test_docs_only_is_light_and_not_conservative(self):\n         focus = gr.classify_review_focus([\"docs/adr/0099-x.md\", \"README.md\"], \"\", \"low\")\n-        self.assertTrue(any(\"Docs/ADR-only\" in f for f in focus))\n+        self.assertTrue(any(\"Docs/decision-record-only\" in f for f in focus))\n         self.assertFalse(any(\"conservative default\" in f.lower() for f in focus))\n \n     def test_unknown_category_is_conservative(self):\ndiff --git a/tests/test_policy_job_merged_prs.py b/tests/test_policy_job_merged_prs.py\nindex d45512e74681e5e649279236c7d4e79d5c3da471..b97989f8bd74d091de1a6b5275a20d8dc68c4512\n--- a/tests/test_policy_job_merged_prs.py\n+++ b/tests/test_policy_job_merged_prs.py\n@@ -3,8 +3,8 @@\n \n ``.github/workflows/shiki-cca-completion.yml`` has two consumers of the merged-PR\n proof: the CCA *signal* (``guardian_approval_signal.py`` in \"Prepare CCA context\")\n-and the authoritative gate (``mergegate_check.py`` in \"Enforce MergeGate\"). ADR\n-0017's bookkeeping-closeout exemption (condition 6) needs the merged implementation\n+and the authoritative gate (``mergegate_check.py`` in \"Enforce MergeGate\").\n+SADR-0017's bookkeeping-closeout exemption (condition 6) needs the merged implementation\n PR proven, and a closeout carries NO ``post_merge_reconcile`` marker. The signal\n already resolves ``MERGED_PRS`` unconditionally, but \"Enforce MergeGate\" used to\n resolve it only inside ``grep -q '<!-- shiki:post_merge_reconcile -->'``. So a\ndiff --git a/tests/test_sadr_install_migration.py b/tests/test_sadr_install_migration.py\nnew file mode 100644\nindex 0000000000000000000000000000000000000000..5a442fa5e4968ade178dcdedc3c63b54dba580a2\n--- /dev/null\n+++ b/tests/test_sadr_install_migration.py\n@@ -0,0 +1,224 @@\n+\"\"\"SADR installation and collision-safe legacy upgrade behavior.\"\"\"\n+\n+from __future__ import annotations\n+\n+import contextlib\n+import hashlib\n+import io\n+import json\n+import shutil\n+import tempfile\n+import unittest\n+from pathlib import Path\n+\n+import shiki_test_support  # noqa: F401  (path bootstrap)\n+\n+import shiki_installer\n+from tests.test_adr_references import find_adr_reference_violations\n+\n+\n+PRODUCT_NUMBERS = tuple(range(13, 19))\n+\n+\n+def _quiet_install(target: Path, *, force: bool, validate: bool) -> None:\n+    with contextlib.redirect_stdout(io.StringIO()):\n+        shiki_installer.install_template(target, force=force, validate=validate)\n+\n+\n+def _sha256(path: Path) -> str:\n+    return hashlib.sha256(path.read_bytes()).hexdigest()\n+\n+\n+def _legacy_sadr_paths() -> tuple[str, ...]:\n+    return tuple(\n+        f\"docs/adr/{path.name.removeprefix('SADR-')}\"\n+        for path in sorted((shiki_installer.ROOT / \"docs\" / \"adr\").glob(\"SADR-*.md\"))\n+        if path.name != \"SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\"\n+    )\n+\n+\n+def _create_legacy_upgrade_fixture(\n+    target: Path,\n+) -> tuple[dict[str, bytes], tuple[str, ...]]:\n+    adr_dir = target / \"docs\" / \"adr\"\n+    adr_dir.mkdir(parents=True)\n+    product_bytes = {\n+        f\"{number:04d}-cubeshare-product-decision.md\": (\n+            f\"# CubeShare ADR {number:04d}\\n\\nDo not rewrite {number}.\\n\"\n+        ).encode()\n+        for number in PRODUCT_NUMBERS\n+    }\n+    for name, content in product_bytes.items():\n+        (adr_dir / name).write_bytes(content)\n+\n+    legacy_paths = _legacy_sadr_paths()\n+    for relative in legacy_paths:\n+        (target / relative).write_bytes(f\"legacy Shiki bytes for {relative}\\n\".encode())\n+\n+    stamp_path = target / shiki_installer.INSTALL_STAMP_PATH\n+    stamp_path.parent.mkdir(parents=True, exist_ok=True)\n+    stamp = {\n+        \"version\": shiki_installer.INSTALL_STAMP_VERSION,\n+        \"platform_commit\": None,\n+        \"installed_at\": \"2000-01-01T00:00:00+00:00\",\n+        \"digests\": {\n+            relative: _sha256(target / relative) for relative in legacy_paths\n+        },\n+    }\n+    stamp_path.write_text(\n+        json.dumps(stamp, indent=2, sort_keys=True) + \"\\n\", encoding=\"utf-8\"\n+    )\n+    return product_bytes, legacy_paths\n+\n+\n+def _tree_snapshot(root: Path) -> dict[str, tuple[str, bytes | str]]:\n+    \"\"\"Recursive byte/shape snapshot used to prove preflight is write-free.\"\"\"\n+    snapshot: dict[str, tuple[str, bytes | str]] = {}\n+    for path in sorted(root.rglob(\"*\")):\n+        relative = path.relative_to(root).as_posix()\n+        if path.is_symlink():\n+            snapshot[relative] = (\"symlink\", path.readlink().as_posix())\n+        elif path.is_dir():\n+            snapshot[relative] = (\"directory\", \"\")\n+        else:\n+            snapshot[relative] = (\"file\", path.read_bytes())\n+    return snapshot\n+\n+\n+class FreshSadrInstallTests(unittest.TestCase):\n+    def setUp(self) -> None:\n+        self.tmp = Path(tempfile.mkdtemp(prefix=\"shiki-sadr-fresh-\"))\n+        self.addCleanup(lambda: shutil.rmtree(self.tmp, ignore_errors=True))\n+        self.target = self.tmp / \"target\"\n+        self.adr_dir = self.target / \"docs\" / \"adr\"\n+        self.adr_dir.mkdir(parents=True)\n+        self.product_bytes = {\n+            f\"{number:04d}-product-decision.md\": (\n+                f\"# Product ADR {number:04d}\\n\\nTarget-owned bytes {number}.\\n\"\n+            ).encode()\n+            for number in PRODUCT_NUMBERS\n+        }\n+        for name, content in self.product_bytes.items():\n+            (self.adr_dir / name).write_bytes(content)\n+\n+    def test_fresh_install_preserves_product_adrs_and_installs_all_sadrs(self) -> None:\n+        _quiet_install(self.target, force=False, validate=True)\n+\n+        for name, expected in self.product_bytes.items():\n+            self.assertEqual((self.adr_dir / name).read_bytes(), expected)\n+\n+        sadr_numbers = {\n+            path.name.split(\"-\", 2)[1]\n+            for path in self.adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\")\n+        }\n+        self.assertEqual(sadr_numbers, {f\"{number:04d}\" for number in range(1, 20)})\n+\n+        numeric_records = {\n+            path.name\n+            for path in self.adr_dir.glob(\"[0-9][0-9][0-9][0-9]-*.md\")\n+        }\n+        self.assertEqual(numeric_records, set(self.product_bytes))\n+        self.assertEqual(find_adr_reference_violations(self.target), [])\n+\n+\n+class LegacySadrUpgradeTests(unittest.TestCase):\n+    def setUp(self) -> None:\n+        self.tmp = Path(tempfile.mkdtemp(prefix=\"shiki-sadr-upgrade-\"))\n+        self.addCleanup(lambda: shutil.rmtree(self.tmp, ignore_errors=True))\n+        self.target = self.tmp / \"target\"\n+        self.adr_dir = self.target / \"docs\" / \"adr\"\n+        self.product_bytes, self.legacy_paths = _create_legacy_upgrade_fixture(\n+            self.target\n+        )\n+\n+    def test_force_upgrade_deletes_only_stamped_legacy_sadrs(self) -> None:\n+        _quiet_install(self.target, force=True, validate=True)\n+\n+        for relative in self.legacy_paths:\n+            self.assertFalse((self.target / relative).exists(), relative)\n+        for name, expected in self.product_bytes.items():\n+            self.assertEqual((self.adr_dir / name).read_bytes(), expected)\n+\n+        sadrs = list(self.adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\"))\n+        self.assertEqual(len(sadrs), 19)\n+        refreshed = json.loads(\n+            (self.target / shiki_installer.INSTALL_STAMP_PATH).read_text(encoding=\"utf-8\")\n+        )\n+        for relative in self.legacy_paths:\n+            self.assertNotIn(relative, refreshed[\"digests\"])\n+        for path in sadrs:\n+            relative = path.relative_to(self.target).as_posix()\n+            self.assertEqual(refreshed[\"digests\"][relative], _sha256(path))\n+\n+    def test_non_force_install_never_deletes_legacy_files(self) -> None:\n+        before = {\n+            relative: (self.target / relative).read_bytes()\n+            for relative in self.legacy_paths\n+        }\n+        _quiet_install(self.target, force=False, validate=False)\n+        for relative, expected in before.items():\n+            self.assertEqual((self.target / relative).read_bytes(), expected)\n+\n+\n+class LegacyCleanupPreflightTests(unittest.TestCase):\n+    def setUp(self) -> None:\n+        self.tmp = Path(tempfile.mkdtemp(prefix=\"shiki-sadr-preflight-\"))\n+        self.addCleanup(lambda: shutil.rmtree(self.tmp, ignore_errors=True))\n+\n+    def test_all_ownership_blockers_abort_before_any_write(self) -> None:\n+        cases = (\n+            (\"absent\", \"install stamp is absent\"),\n+            (\"unreadable\", \"install stamp is unreadable\"),\n+            (\"malformed\", \"install stamp is malformed\"),\n+            (\"missing-digest\", \"install stamp has no digest\"),\n+            (\"digest-mismatch\", \"digest mismatch\"),\n+        )\n+        for name, expected_reason in cases:\n+            with self.subTest(name=name):\n+                target = self.tmp / name\n+                _, legacy_paths = _create_legacy_upgrade_fixture(target)\n+                stamp_path = target / shiki_installer.INSTALL_STAMP_PATH\n+\n+                if name == \"absent\":\n+                    stamp_path.unlink()\n+                elif name == \"unreadable\":\n+                    stamp_path.unlink()\n+                    stamp_path.mkdir()\n+                elif name == \"malformed\":\n+                    stamp_path.write_text(\"{not-json\\n\", encoding=\"utf-8\")\n+                else:\n+                    stamp = json.loads(stamp_path.read_text(encoding=\"utf-8\"))\n+                    if name == \"missing-digest\":\n+                        stamp[\"digests\"] = {}\n+                    else:\n+                        stamp[\"digests\"] = {\n+                            relative: \"deadbeef\" for relative in legacy_paths\n+                        }\n+                    stamp_path.write_text(\n+                        json.dumps(stamp, indent=2, sort_keys=True) + \"\\n\",\n+                        encoding=\"utf-8\",\n+                    )\n+\n+                before = _tree_snapshot(target)\n+                with self.assertRaises(shiki_installer.ShikiError) as raised:\n+                    _quiet_install(target, force=True, validate=False)\n+                after = _tree_snapshot(target)\n+\n+                self.assertEqual(before, after, f\"{name} preflight changed the tree\")\n+                message = str(raised.exception)\n+                self.assertIn(expected_reason, message)\n+                self.assertIn(\"No files were written\", message)\n+                for relative in legacy_paths:\n+                    self.assertIn(relative, message)\n+\n+    def test_absent_stamp_is_allowed_when_no_legacy_path_exists(self) -> None:\n+        target = self.tmp / \"no-legacy\"\n+        target.mkdir()\n+        _quiet_install(target, force=True, validate=False)\n+        self.assertTrue(\n+            (target / \"docs\" / \"adr\" / \"SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\").is_file()\n+        )\n+\n+\n+if __name__ == \"__main__\":\n+    unittest.main()\n\n2026-08-07T08:14:43.520103Z ERROR codex_models_manager::manager: failed to renew cache TTL: missing field `base_instructions` at line 94 column 5\nexec\n/bin/zsh -lc \"rg -n --glob '\"'!.shiki/**'\"' --glob '\"'!skills/**'\"' '\\\\bADRs?\\\\b|ADR-worthy|Docs/ADR' AGENTS.md CLAUDE.md CONTEXT.md CONTRIBUTING.md SYSTEM_PROMPT.md pyproject.toml docs scripts .github .codex | sed -n '1,320p'\" in /private/tmp/shiki-adr-collision.pa0KwY/.worktrees/shiki-t-20260807t073307221254z-2d22d654-introduc\n succeeded in 0ms:\nSYSTEM_PROMPT.md:34:product decisions retain `NNNN-*.md` and the `ADR` namespace.\nSYSTEM_PROMPT.md:54:Do not assume `openai/codex-action`, `OPENAI_API_KEY`, or API-key based Codex automation in the default Shiki path. API-key runners are explicit target-repo extensions and require their own target ADR.\nSYSTEM_PROMPT.md:61:2. **grill-with-docs**: challenge the plan against `CONTEXT.md`, Shiki SADRs, target ADRs, code reality, terminology, boundaries, and edge scenarios. Ask one question at a time when the operator is available. If code can answer a question, inspect code instead of asking.\nSYSTEM_PROMPT.md:81:2. Read `CONTEXT.md` and relevant SADRs and target ADRs.\nSYSTEM_PROMPT.md:109:- Relevant documents, SADRs, target ADRs, domain terms, and prior decisions.\nSYSTEM_PROMPT.md:158:- `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant SADRs and target ADRs.\nSYSTEM_PROMPT.md:220:When the architecture gate triggers, run `grill-with-docs` or `improve-codebase-architecture` as appropriate and record the decision in a Shiki SADR or target ADR as applicable if it is hard to reverse or likely to be re-litigated.\nCONTRIBUTING.md:20:   domain language, Shiki SADRs, target ADRs, and code reality before writing a PRD.\nCONTRIBUTING.md:97:  GitHub, `.shiki/`, `CONTEXT.md`, a Shiki SADR, or a target ADR.\nCONTRIBUTING.md:106:record durable decisions in a Shiki SADR or target ADR as applicable (see `docs/adr/`).\nCONTEXT.md:28:A planning skill that challenges a plan against domain language, Shiki SADRs, target ADRs, code reality, and concrete edge scenarios. It resolves design-tree questions before PRD/issues. It runs inside Requirements Definition.\nCLAUDE.md:18:product decisions retain `NNNN-*.md` and the `ADR` namespace.\nCLAUDE.md:63:2. Read `CONTEXT.md` and relevant Shiki SADRs and target ADRs.\nCLAUDE.md:80:- Challenge hard-to-reverse tradeoffs against SADRs and target ADRs.\nCLAUDE.md:100:- Offer a SADR or target ADR only when the decision is hard to reverse, surprising without context, and a real tradeoff.\nCLAUDE.md:109:- Include implementation decisions, testing decisions, out-of-scope items, and relevant SADR/target-ADR/domain links.\nCLAUDE.md:127:- Relevant docs, SADRs, target ADRs, modules, and tests.\nCLAUDE.md:158:- Read `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant SADRs and target ADRs, PRD, task issue, PR body, diff, checks, reviews, labels, and `.shiki/` state.\nAGENTS.md:25:Do not assume `openai/codex-action`, `OPENAI_API_KEY`, or API-key based Codex automation in the default Shiki path. API-key runners are explicit target-repo extensions and require their own target ADR.\nAGENTS.md:48:`SADR-NNNN-*.md`; target product decisions retain `NNNN-*.md` and the `ADR`\nAGENTS.md:54:Conversation state is not durable truth. If a decision matters, put it in GitHub, `.shiki/`, `CONTEXT.md`, a Shiki SADR, or a target ADR.\nAGENTS.md:82:2. **grill-with-docs**: challenge the plan against domain docs, Shiki SADRs, target ADRs, code reality, terminology, and edge scenarios.\nAGENTS.md:83:3. **Context & Impact**: find relevant docs, SADRs, target ADRs, code, dependencies, owners, locks, and verification surfaces.\nAGENTS.md:174:- Links to relevant domain terms, SADRs, and target ADRs.\nAGENTS.md:200:- Relevant documents, domain terms, SADRs, target ADRs, and past decisions.\nAGENTS.md:355:When the architecture gate triggers, run `grill-with-docs` or `improve-codebase-architecture` as appropriate and record durable decisions in a Shiki SADR or target ADR as applicable.\n.codex/skills/shiki/SKILL.md:22:product decisions retain `NNNN-*.md` and the `ADR` namespace.\n.github/prompts/codex-task-handoff.md:5:Follow `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant Shiki SADRs and target ADRs, and `docs/agents/codex-handoff.md`.\n.github/prompts/cca-completion-check.md:23:product decisions retain `NNNN-*.md` and the `ADR` namespace.\ndocs/agents/execution-control.md:66:| Required status checks | Repo maintainer / Guardian | GitHub branch protection / rulesets | Repository setup, updated via target ADR | GitHub settings | branch/ruleset config, target ADR |\ndocs/agents/implementation-policy.md:35:product decisions retain `NNNN-*.md` and the `ADR` namespace.\ndocs/agents/implementation-policy.md:40:2. **Domain language is controlled.** Terms must be checked against `CONTEXT.md`; hard-to-reverse tradeoffs must be checked against Shiki SADRs and target ADRs.\ndocs/agents/implementation-policy.md:101:- Check SADRs and target ADRs before reopening decisions.\ndocs/agents/implementation-policy.md:106:- Create SADRs or target ADRs only for hard-to-reverse, surprising, tradeoff-based decisions.\ndocs/agents/implementation-policy.md:113:- SADRs or target ADRs when justified.\ndocs/agents/implementation-policy.md:123:- Relevant documents, SADRs, and target ADRs.\ndocs/agents/implementation-policy.md:146:- Links to relevant Goal, domain terms, SADRs, and target ADRs.\ndocs/agents/implementation-policy.md:246:- Documentation and SADR/target-ADR updates.\ndocs/agents/implementation-policy.md:307:| Architecture block | Decision needs a SADR, target ADR, or deep-module discussion. | `grill-with-docs` or `improve-codebase-architecture`. |\ndocs/agents/completion-check-agent.md:35:- relevant SADRs and target ADRs\ndocs/agents/skill-gate.md:12:| `grill-with-docs` | Ambiguous plan, unclear terminology, boundary decision, tradeoff, or decision-record-worthy design choice. | Clarified decision tree, recommended answers, updates to `CONTEXT.md`, SADRs, or target ADRs when decisions crystallize. |\ndocs/agents/skill-gate.md:20:| `improve-codebase-architecture` | Structural friction, testability, AI-navigability, deep-module opportunity, or refactor request. | Architecture candidates and deepening recommendations informed by `CONTEXT.md`, SADRs, and target ADRs. |\ndocs/agents/domain.md:20:- Create SADRs or target ADRs only for hard-to-reverse, surprising, tradeoff-based decisions.\ndocs/agents/domain.md:22:- If a SADR or target ADR conflicts with a proposed implementation, surface the conflict before editing.\nscripts/shiki_guardian_review.py:162:            \"Docs/decision-record-only change: confirm the change is genuinely documentation/decision-only, introduces no behavior or contract drift, and stays consistent with the constitution and existing SADRs and target ADRs.\"\ndocs/agents/decision-control.md:14:| Must `grill-with-docs` run? | Planner / Skill Gate | Before PRD or issue split | Ambiguity, domain terms, SADR/target-ADR conflicts, unclear boundaries | Skill invocation or explicit skip rationale | Skill Gate checklist |\nscripts/shiki_installer.py:256:    \"\"\"Authorize exact legacy Shiki ADR removals before the first write.\nscripts/shiki_installer.py:323:            \"legacy Shiki ADR cleanup is not authorized:\\n\"\nscripts/shiki_installer.py:345:        info(f\"removed stamped legacy Shiki ADR: {target / relative}\")\ndocs/agents/codex-handoff.md:49:- Relevant SADRs and target ADRs:\ndocs/releasing.md:70:packaged as an importable distribution (see the ADR for the rationale).\ndocs/agents/checklists.md:35:| GD-02 | Relevant SADRs and target ADRs were read or absence was recorded. | blocking for non-trivial Goals |\ndocs/agents/checklists.md:40:| GD-07 | A SADR or target ADR was created or explicitly skipped using the decision-record criteria. | blocking when applicable |\ndocs/agents/checklists.md:46:| CI-01 | Relevant docs, SADRs, and target ADRs identified. | blocking |\ndocs/agents/checklists.md:65:| PRD-07 | Relevant glossary/SADR/target-ADR links are present when applicable. | blocking |\ndocs/agents/checklists.md:101:| H-05 | Relevant docs, SADRs, target ADRs, modules, and tests are referenced. | blocking |\ndocs/agents/runtime-auth-model.md:151:If a repo opts into API-key mode, record the target-specific exception in a target ADR and update the repo's `AGENTS.md`, `.shiki/config.yaml`, and workflow permissions.\ndocs/adr/template.md:5:Proposed | Accepted | Superseded by SADR-NNNN or target ADR NNNN\ndocs/adr/template.md:10:relevant domain terms (`CONTEXT.md`), prior Shiki SADRs or target ADRs, and code\ndocs/adr/SADR-0013-bootstrap-grants-actions-approve-pull-requests.md:34:what default token scope \u2014 is a SADR-worthy decision, not an implementation\ndocs/adr/README.md:7:- Target Architecture Decision Records (ADRs), named\ndocs/adr/README.md:11:`SADR-NNNN` citation resolves only to `SADR-NNNN-*.md`; a target `ADR NNNN`\ndocs/adr/README.md:17:- New target product decisions use the target's own ADR sequence.\ndocs/adr/README.md:37:## Adding A Target ADR\ndocs/adr/README.md:41:2. Cite it as `ADR NNNN`; do not prefix its filename with `SADR-`.\ndocs/adr/SADR-0008-use-claude-code-as-default-implementer.md:63:(still requires its own target ADR per SADR-0005).\ndocs/adr/SADR-0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md:29:- API-key based automation remains possible only as an explicit target-repo extension with its own ADR.\ndocs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md:1:# SADR-0019: Separate Shiki Platform SADRs From Target ADRs\ndocs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md:11:`NNNN-<slug>.md` namespace. A target that already owned ADRs 0013 through 0018\ndocs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md:13:`ADR NNNN` citation ambiguous. Renumbering target decisions would rewrite target\ndocs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md:18:mistaking a target-owned ADR for platform content. The install stamp already\ndocs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md:28:- target product decisions retain `docs/adr/NNNN-<slug>.md` and `ADR NNNN`;\ndocs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md:33:former numeric Shiki ADR paths. It may delete a legacy path only when the\ndocs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md:44:- Targets may use any ADR number without colliding with Shiki's platform SADRs.\ndocs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md:45:- Fresh installs preserve target ADR bytes and install a stable SADR set.\ndocs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md:54:- Renumbering target ADRs mutates target-owned history.\n\nexec\n/bin/zsh -lc \"rg -n '\\\\ban SADR\\\\b' AGENTS.md CLAUDE.md CONTEXT.md CONTRIBUTING.md SYSTEM_PROMPT.md docs scripts tests .github .codex --glob '\"'!.shiki/**'\"'\" in /private/tmp/shiki-adr-collision.pa0KwY/.worktrees/shiki-t-20260807t073307221254z-2d22d654-introduc\nexec\n/bin/zsh -lc \"rg -n -U --glob '\"'!.shiki/**'\"' --glob '\"'!skills/**'\"' --glob '\"'!tests/test_adr_references.py'\"' --glob '\"'!tests/test_sadr_install_migration.py'\"' '\\\\bADR[- ]?\\\\s*(000[1-9]|001[0-8])\\\\b|docs/adr/\\\\s*(000[1-9]|001[0-8])-' AGENTS.md CLAUDE.md CONTEXT.md CONTRIBUTING.md SYSTEM_PROMPT.md pyproject.toml docs scripts tests .github .codex\" in /private/tmp/shiki-adr-collision.pa0KwY/.worktrees/shiki-t-20260807t073307221254z-2d22d654-introduc\n exited 1 in 0ms:\n exited 1 in 0ms:\nexec\n/bin/zsh -lc \"git diff -- .shiki | sed -n '1,200p'\" in /private/tmp/shiki-adr-collision.pa0KwY/.worktrees/shiki-t-20260807t073307221254z-2d22d654-introduc\n succeeded in 0ms:\ndiff --git a/.shiki/tasks/T-20260807T073307221254Z-2d22d654.json b/.shiki/tasks/T-20260807T073307221254Z-2d22d654.json\nindex 0823eeb..e7e150c 100644\n--- a/.shiki/tasks/T-20260807T073307221254Z-2d22d654.json\n+++ b/.shiki/tasks/T-20260807T073307221254Z-2d22d654.json\n@@ -11,6 +11,20 @@\n     \"python3 -m unittest discover -s tests, control-plane and adversarial shell suites, validate_shiki, and fresh-install/upgrade tests pass locally and in CI on the exact head.\"\n   ],\n   \"assigned_runtime\": \"codex\",\n+  \"cca_checklist_profile\": [\n+    \"CCA-01\",\n+    \"CCA-02\",\n+    \"CCA-03\",\n+    \"CCA-04\",\n+    \"CCA-05\",\n+    \"CCA-06\",\n+    \"CCA-07\",\n+    \"CCA-08\",\n+    \"CCA-09\",\n+    \"CCA-10\",\n+    \"CCA-11\",\n+    \"CCA-12\"\n+  ],\n   \"dependencies\": [],\n   \"expected_branch\": \"shiki/t-20260807t073307221254z-2d22d654-introduce-sadr-and-migrate-installs-without-touc\",\n   \"expected_pr\": null,\n@@ -18,7 +32,11 @@\n   \"goal_id\": \"G-20260807T073307219752Z-1ee1ed68\",\n   \"id\": \"T-20260807T073307221254Z-2d22d654\",\n   \"ledger_evidence\": [\n-    \"L-20260807T073307221361Z-b3c3a718\"\n+    \"L-20260807T073307221361Z-b3c3a718\",\n+    \"L-20260807T080107948254Z-61f24da0\",\n+    \"L-20260807T080228312210Z-07ac626e\",\n+    \"L-20260807T080228575038Z-d574f629\",\n+    \"L-20260807T080256330734Z-5bf40cb6\"\n   ],\n   \"locks\": [\n     \"path:.codex/**\",\n@@ -46,23 +64,9 @@\n     \"tdd\",\n     \"code-review\"\n   ],\n-  \"cca_checklist_profile\": [\n-    \"CCA-01\",\n-    \"CCA-02\",\n-    \"CCA-03\",\n-    \"CCA-04\",\n-    \"CCA-05\",\n-    \"CCA-06\",\n-    \"CCA-07\",\n-    \"CCA-08\",\n-    \"CCA-09\",\n-    \"CCA-10\",\n-    \"CCA-11\",\n-    \"CCA-12\"\n-  ],\n   \"risk_level\": \"high\",\n   \"scope\": \"Rename Shiki platform decision records to SADR-0001 through SADR-0018; add SADR-0019; update current citations, packaging metadata, documentation, workflows, prompts, repo-local skill guidance, and the reference validator for separate ADR/SADR namespaces; add an installer preflight that inventories legacy numeric Shiki ADR paths and authorizes deletion only from matching existing install-stamp path digests before any write; then apply authorized cleanup, install SADRs, refresh the stamp, and prove fresh-install plus CubeShare-shaped collision-upgrade behavior with TDD.\",\n-  \"status\": \"planned\",\n+  \"status\": \"running\",\n   \"test_command\": \"python3 -m unittest discover -s tests && bash scripts/test_shiki_control_plane.sh && bash scripts/test_shiki_adversarial_state.sh && python3 scripts/validate_shiki.py --target .\",\n   \"title\": \"Introduce SADR and migrate installs without touching product ADRs\"\n }\n\ndiff --git a/.codex/skills/shiki/SKILL.md b/.codex/skills/shiki/SKILL.md\nindex c8e2e85d9d75d49cc4f2d6286ee22659c4e53e75..93cded623b2c15d9f65c76491a1c2d813ac9e6c0\n--- a/.codex/skills/shiki/SKILL.md\n+++ b/.codex/skills/shiki/SKILL.md\n@@ -18,6 +18,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n Codex CLI does not register this skill as a custom `/shiki` slash command. When\n the user types `/shiki` in Codex CLI and gets \"Unrecognized command\", explain\n that this is expected: invoke Shiki in Codex with natural language such as\n@@ -69,7 +72,7 @@\n \n ## Responsibilities\n \n-- Claude Code implements and repairs by default (`shiki runner claude`, ADR 0008); Codex implements and repairs tasks explicitly assigned to `codex`.\n+- Claude Code implements and repairs by default (`shiki runner claude`, SADR-0008); Codex implements and repairs tasks explicitly assigned to `codex`.\n - Claude Code Action can act as GitHub-side CCA or reviewer.\n - CCA judges completion.\n - MergeGate authorizes state transitions and merge readiness.\ndiff --git a/.github/prompts/cca-completion-check.md b/.github/prompts/cca-completion-check.md\nindex ed203ad5f302ecc81372d8ab27d2b1232ca97497..fd89950528333ef6242e6d5443904f5392ab7e56\n--- a/.github/prompts/cca-completion-check.md\n+++ b/.github/prompts/cca-completion-check.md\n@@ -19,6 +19,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n ## Required Reading\n \n Read only the files needed to judge the PR contract. Prefer this order:\ndiff --git a/.github/prompts/codex-task-handoff.md b/.github/prompts/codex-task-handoff.md\nindex 695e211d76dea1c60e8d5bb360e78487808286a4..59441d576b01a39569722f226c854e5a740e9eb5\n--- a/.github/prompts/codex-task-handoff.md\n+++ b/.github/prompts/codex-task-handoff.md\n@@ -2,7 +2,7 @@\n \n You are Codex Front acting as the Implementer for a Shiki task.\n \n-Follow `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant ADRs, and `docs/agents/codex-handoff.md`.\n+Follow `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant Shiki SADRs and target ADRs, and `docs/agents/codex-handoff.md`.\n \n Rules:\n \ndiff --git a/AGENTS.md b/AGENTS.md\nindex 7f041100d49c2a7525f51cb0f5d696a4a6d1c78c..a06bfbabc6fa48a007389e25e12d7ed523936bed\n--- a/AGENTS.md\n+++ b/AGENTS.md\n@@ -22,7 +22,7 @@\n - **Codex Front** is an optional implementer through Codex App, Codex CLI, Codex IDE extension, or Codex Web signed in with ChatGPT OAuth/subscription auth, dispatched with `shiki runner codex` when a task is explicitly assigned to `codex`.\n - **GitHub CCA** is implemented by Claude Code Action by default, using `CLAUDE_CODE_OAUTH_TOKEN`.\n \n-Do not assume `openai/codex-action`, `OPENAI_API_KEY`, or API-key based Codex automation in the default Shiki path. API-key runners are explicit target-repo extensions and require their own ADR.\n+Do not assume `openai/codex-action`, `OPENAI_API_KEY`, or API-key based Codex automation in the default Shiki path. API-key runners are explicit target-repo extensions and require their own target ADR.\n \n Shiki is not:\n \n@@ -44,10 +44,14 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use\n+`SADR-NNNN-*.md`; target product decisions retain `NNNN-*.md` and the `ADR`\n+namespace.\n+\n Use the canonical source-of-truth order above. Runtime-specific reading orders\n are operational guidance only and must not override or reorder it.\n \n-Conversation state is not durable truth. If a decision matters, put it in GitHub, `.shiki/`, `CONTEXT.md`, or an ADR.\n+Conversation state is not durable truth. If a decision matters, put it in GitHub, `.shiki/`, `CONTEXT.md`, a Shiki SADR, or a target ADR.\n \n If GitHub and `.shiki/` disagree, surface the conflict and prefer GitHub operational state until the mirror is repaired.\n \n@@ -75,8 +79,8 @@\n Every non-trivial change follows this loop:\n \n 1. **Goal**: clarify outcome, completion conditions, non-goals, risk level, and success signals.\n-2. **grill-with-docs**: challenge the plan against domain docs, ADRs, code reality, terminology, and edge scenarios.\n-3. **Context & Impact**: find relevant docs, ADRs, code, dependencies, owners, locks, and verification surfaces.\n+2. **grill-with-docs**: challenge the plan against domain docs, Shiki SADRs, target ADRs, code reality, terminology, and edge scenarios.\n+3. **Context & Impact**: find relevant docs, SADRs, target ADRs, code, dependencies, owners, locks, and verification surfaces.\n 4. **PRD and Spec Freeze**: use `to-prd` when context is settled enough to become durable product/engineering intent. The operator's explicit approval of the PRD is Spec Freeze: the plan records a `spec_freeze` block, and no plan runs without it. Steps 1-4 form Requirements Definition \u2014 one continuous operator dialogue. After Spec Freeze, scope changes require an operator-approved Spec Amendment; non-scope-moving interpretations go to the Assumption Log.\n 5. **Issues**: use `to-issues` to create independently grabbable vertical-slice issues. Prefer AFK slices over HITL slices where possible.\n 6. **Triage**: label issues for readiness, risk, runtime, skills, and MergeGate state.\n@@ -167,7 +171,7 @@\n - Testing decisions.\n - Out-of-scope items.\n - Further notes.\n-- Links to relevant domain terms and ADRs.\n+- Links to relevant domain terms, SADRs, and target ADRs.\n \n Every executable task must include:\n \n@@ -193,7 +197,7 @@\n \n Context & Impact must identify:\n \n-- Relevant documents, domain terms, ADRs, and past decisions.\n+- Relevant documents, domain terms, SADRs, target ADRs, and past decisions.\n - Relevant modules, interfaces, seams, callers, owners, and tests.\n - Dependency relationships between tasks.\n - Candidate locks and lock conflicts.\n@@ -210,7 +214,7 @@\n | Skill | Required when |\n | --- | --- |\n | `setup-matt-pocock-skills` | First configuring the repo for skills, or when issue tracker, triage labels, or domain docs are missing. |\n-| `grill-with-docs` | Default for non-trivial Goals before PRD/issues; always required when plans, terminology, boundaries, tradeoffs, or ADR-worthy decisions are ambiguous. |\n+| `grill-with-docs` | Default for non-trivial Goals before PRD/issues; always required when plans, terminology, boundaries, tradeoffs, or decision-record-worthy choices are ambiguous. |\n | `zoom-out` | The agent lacks an architectural map of the relevant code area. |\n | `to-prd` | Settled Goal context must become durable product/engineering intent. |\n | `to-issues` | A Goal, PRD, or plan must become independently grabbable vertical-slice issues. |\n@@ -348,7 +352,7 @@\n - Three or more new files.\n - Any high-risk or critical label.\n \n-When the architecture gate triggers, run `grill-with-docs` or `improve-codebase-architecture` as appropriate and record durable decisions in an ADR when needed.\n+When the architecture gate triggers, run `grill-with-docs` or `improve-codebase-architecture` as appropriate and record durable decisions in a Shiki SADR or target ADR as applicable.\n \n ## Engineering Discipline\n \ndiff --git a/CLAUDE.md b/CLAUDE.md\nindex 98a932e7841a632866fcd53b0bad0e3af3711d3d..98a31399404d6fb044b4fd493f4459ac87f439de\n--- a/CLAUDE.md\n+++ b/CLAUDE.md\n@@ -14,6 +14,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n ## Claude Role\n \n Default to planner, default implementer, reviewer, coordinator, documentation editor, CCA-style completion judge, and final-judgment assistant.\n@@ -57,7 +60,7 @@\n At the start of a Shiki session:\n \n 1. Read `AGENTS.md`.\n-2. Read `CONTEXT.md` and relevant ADRs.\n+2. Read `CONTEXT.md` and relevant Shiki SADRs and target ADRs.\n 3. Read active `.shiki/` Goal/task/ledger state when present.\n 4. Read active distilled rules from `.shiki/memories/` (status `distilled`, `active: true`, not revoked or superseded) for applicable guidance; the same rules are injected deterministically into task handoffs as the `## Distilled Rules` section (\u00a73.5).\n 5. Read the GitHub Issue or PR when the task is GitHub-backed.\n@@ -74,7 +77,7 @@\n - Define the Goal, completion conditions, non-goals, and risk level.\n - Treat `grill-with-docs` as the default planning entry point for non-trivial Goals.\n - Challenge domain terms against `CONTEXT.md`.\n-- Challenge hard-to-reverse tradeoffs against ADRs.\n+- Challenge hard-to-reverse tradeoffs against SADRs and target ADRs.\n - Explore code instead of asking when code can answer the question.\n - Run Context & Impact before implementation planning.\n - Produce a Task DAG, not an unordered checklist.\n@@ -94,7 +97,7 @@\n - For each question, provide your recommended answer.\n - Resolve dependency decisions before downstream design questions.\n - Update `CONTEXT.md` inline when terminology crystallizes.\n-- Offer ADRs only when the decision is hard to reverse, surprising without context, and a real tradeoff.\n+- Offer a SADR or target ADR only when the decision is hard to reverse, surprising without context, and a real tradeoff.\n - Convert unresolved questions into HITL blockers rather than allowing Codex to guess.\n \n ## PRD and Issues Standard\n@@ -103,7 +106,7 @@\n \n - Use the project's domain glossary vocabulary.\n - Avoid volatile file paths unless they are essential.\n-- Include implementation decisions, testing decisions, out-of-scope items, and relevant ADR/domain links.\n+- Include implementation decisions, testing decisions, out-of-scope items, and relevant SADR/target-ADR/domain links.\n - Identify deep-module opportunities and testable interfaces.\n \n When creating issues:\n@@ -121,7 +124,7 @@\n - Branch/worktree target.\n - Scope and non-goals.\n - Dependencies and locks.\n-- Relevant docs, ADRs, modules, and tests.\n+- Relevant docs, SADRs, target ADRs, modules, and tests.\n - Required skill invocations.\n - TDD expectations.\n - Pre-PR `code-review` gate expectations.\n@@ -152,7 +155,7 @@\n \n - Treat yourself as a judge, not implementer.\n - Do not write production code.\n-- Read `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant ADRs, PRD, task issue, PR body, diff, checks, reviews, labels, and `.shiki/` state.\n+- Read `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant SADRs and target ADRs, PRD, task issue, PR body, diff, checks, reviews, labels, and `.shiki/` state.\n - Evaluate every applicable checklist in `docs/agents/checklists.md`.\n - Map each acceptance criterion to durable evidence.\n - Distinguish implementation failure from missing evidence.\ndiff --git a/CONTEXT.md b/CONTEXT.md\nindex e61a9be06a0cce0d026484083a657bde2067e234..237ee8aa3ea8fab334ece49768bc5ed3b5af0974\n--- a/CONTEXT.md\n+++ b/CONTEXT.md\n@@ -25,7 +25,7 @@\n _Avoid_: jumping directly into code\n \n **grill-with-docs**:\n-A planning skill that challenges a plan against domain language, ADRs, code reality, and concrete edge scenarios. It resolves design-tree questions before PRD/issues. It runs inside Requirements Definition.\n+A planning skill that challenges a plan against domain language, Shiki SADRs, target ADRs, code reality, and concrete edge scenarios. It resolves design-tree questions before PRD/issues. It runs inside Requirements Definition.\n _Avoid_: generic brainstorming, silent assumptions\n \n **Requirements Definition**:\ndiff --git a/CONTRIBUTING.md b/CONTRIBUTING.md\nindex c6d9b656e73abb8a55e5f660123e03153dcb3e6d..9e37a8fbf668db5b22461aaae5548b8f1354394c\n--- a/CONTRIBUTING.md\n+++ b/CONTRIBUTING.md\n@@ -17,7 +17,7 @@\n 1. **Goal** \u2014 start from a user-approved Goal with an outcome, completion\n    conditions, non-goals, risk level, and success signals.\n 2. **grill-with-docs** \u2014 for any non-trivial change, challenge the plan against\n-   domain language, ADRs, and code reality before writing a PRD.\n+   domain language, Shiki SADRs, target ADRs, and code reality before writing a PRD.\n 3. **PRD** \u2014 capture settled product/engineering intent (`to-prd`).\n 4. **Spec Freeze** \u2014 the operator explicitly approves the PRD/requirements;\n    the plan records a `spec_freeze` block and no plan runs without it\n@@ -94,7 +94,7 @@\n   entries.\n - Do not hand-edit `.shiki/goals/*.json` or `.shiki/manifest.json` in a task PR.\n - Conversation state is not durable truth. If a decision matters, record it in\n-  GitHub, `.shiki/`, `CONTEXT.md`, or an ADR.\n+  GitHub, `.shiki/`, `CONTEXT.md`, a Shiki SADR, or a target ADR.\n \n ## Architecture Gate\n \n@@ -103,7 +103,7 @@\n or CI policy, package structure, three or more structural files, more than 100\n net new lines in a core module, three or more new files, or any high-risk label.\n When triggered, run `grill-with-docs` or `improve-codebase-architecture` and\n-record durable decisions in an ADR when needed (see `docs/adr/`).\n+record durable decisions in a Shiki SADR or target ADR as applicable (see `docs/adr/`).\n \n ## Safety\n \ndiff --git a/SYSTEM_PROMPT.md b/SYSTEM_PROMPT.md\nindex aaaea45e3a896c3a44ba89eec8bb84325ecf6c62..da009c86a88285269dee0155e58e550c6d744ea9\n--- a/SYSTEM_PROMPT.md\n+++ b/SYSTEM_PROMPT.md\n@@ -30,6 +30,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n Use the canonical source-of-truth order above. Runtime-specific reading orders\n are operational guidance only and must not override or reorder it. The current\n conversation is non-durable operator input.\n@@ -48,14 +51,14 @@\n \n A runtime may only do work that is explicitly assigned by the Goal, task, PR, or MergeGate decision.\n \n-Do not assume `openai/codex-action`, `OPENAI_API_KEY`, or API-key based Codex automation in the default Shiki path. API-key runners are explicit target-repo extensions and require their own ADR.\n+Do not assume `openai/codex-action`, `OPENAI_API_KEY`, or API-key based Codex automation in the default Shiki path. API-key runners are explicit target-repo extensions and require their own target ADR.\n \n ## 4. Non-negotiable Shiki flow\n \n For any non-trivial Goal, use this flow:\n \n 1. **Goal Seek**: define outcome, non-goals, risk level, completion criteria, and evidence requirements.\n-2. **grill-with-docs**: challenge the plan against `CONTEXT.md`, ADRs, code reality, terminology, boundaries, and edge scenarios. Ask one question at a time when the operator is available. If code can answer a question, inspect code instead of asking.\n+2. **grill-with-docs**: challenge the plan against `CONTEXT.md`, Shiki SADRs, target ADRs, code reality, terminology, boundaries, and edge scenarios. Ask one question at a time when the operator is available. If code can answer a question, inspect code instead of asking.\n 3. **Context & Impact**: identify modules, interfaces, seams, callers, dependencies, locks, risk, and verification surfaces.\n 4. **to-prd + Spec Freeze**: turn settled context into a PRD using domain vocabulary and testing decisions. The operator's explicit approval of the PRD is Spec Freeze, recorded as the plan's `spec_freeze` block; plans without it do not run. Steps 1-4 form Requirements Definition.\n 5. **to-issues**: decompose the PRD into independently grabbable vertical-slice issues. Prefer AFK slices; mark HITL slices when judgment is still required.\n@@ -75,7 +78,7 @@\n Before making any material change:\n \n 1. Read `AGENTS.md` and any runtime-specific wrapper such as `CLAUDE.md`.\n-2. Read `CONTEXT.md` and relevant ADRs.\n+2. Read `CONTEXT.md` and relevant SADRs and target ADRs.\n 3. Read the GitHub Issue / PR and `.shiki/` state for the active Goal or task.\n 4. Identify your role: Planner, Implementer, Reviewer, Completion Check Agent, Repairer, or Guardian-assist.\n 5. Identify Goal id, task id, branch, PR, dependencies, locks, risk level, required skills, and required evidence.\n@@ -103,7 +106,7 @@\n \n At minimum, identify:\n \n-- Relevant documents, ADRs, domain terms, and prior decisions.\n+- Relevant documents, SADRs, target ADRs, domain terms, and prior decisions.\n - Relevant modules, interfaces, seams, callers, owners, and tests.\n - Dependency relationships between tasks.\n - Candidate file locks and lock conflicts.\n@@ -152,7 +155,7 @@\n \n CCA must read:\n \n-- `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant ADRs.\n+- `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant SADRs and target ADRs.\n - Parent Goal issue, PRD issue, task issue, comments, and labels.\n - PR body, diff, commits, changed files, reviews, CI checks, and artifacts available to the workflow.\n - `.shiki/` task contract, locks, ledger entries, CCA checklist profile, and prior repair packets.\n@@ -214,7 +217,7 @@\n - Three or more new files.\n - Any high-risk or critical label.\n \n-When the architecture gate triggers, run `grill-with-docs` or `improve-codebase-architecture` as appropriate and record the decision in an ADR if it is hard to reverse or likely to be re-litigated.\n+When the architecture gate triggers, run `grill-with-docs` or `improve-codebase-architecture` as appropriate and record the decision in a Shiki SADR or target ADR as applicable if it is hard to reverse or likely to be re-litigated.\n \n ## 14. Skill Gate\n \n@@ -223,7 +226,7 @@\n Use these skills:\n \n - `setup-matt-pocock-skills`: before first use of the engineering skill set, or when issue tracker, triage labels, or domain docs are missing.\n-- `grill-with-docs`: default for non-trivial Goals before PRD/issues; required for ambiguous plans, terminology, boundaries, ADR-worthy tradeoffs, or design-tree decisions.\n+- `grill-with-docs`: default for non-trivial Goals before PRD/issues; required for ambiguous plans, terminology, boundaries, decision-record-worthy tradeoffs, or design-tree decisions.\n - `zoom-out`: unfamiliar code area or missing architectural map.\n - `to-prd`: convert settled context into a PRD.\n - `to-issues`: break a PRD, plan, or Goal into independently grabbable vertical-slice issues.\ndiff --git a/docs/adr/0001-keep-shiki-as-platform-name.md b/docs/adr/SADR-0001-keep-shiki-as-platform-name.md\nindex 20a9637c3b9d8c97de7ba8db7cfae4d82d9a7dba..8b685439959aada1b221c112f0ac3c51ca8a1695\n--- a/docs/adr/0001-keep-shiki-as-platform-name.md\n+++ b/docs/adr/SADR-0001-keep-shiki-as-platform-name.md\n@@ -1,3 +1,3 @@\n-# Keep Shiki As The Platform Name\n+# SADR-0001: Keep Shiki As The Platform Name\n \n Shiki remains the product name for the GitHub-native agentic engineering control plane. We considered more descriptive names such as MergePilot, MergeGate, and Kiroku, but kept Shiki because it carries the existing goal-seeking framework identity while remaining independent of Hermes, Codex, Claude Code, or any single runtime.\ndiff --git a/docs/adr/0002-use-agents-as-shared-constitution.md b/docs/adr/SADR-0002-use-agents-as-shared-constitution.md\nindex 73de30ecb078120eac540d9bc51855e3cd48d0e3..c5486182bf06fca36ea508496b013c67302fd989\n--- a/docs/adr/0002-use-agents-as-shared-constitution.md\n+++ b/docs/adr/SADR-0002-use-agents-as-shared-constitution.md\n@@ -1,3 +1,3 @@\n-# Use AGENTS As The Shared Agent Constitution\n+# SADR-0002: Use AGENTS As The Shared Agent Constitution\n \n Shiki uses `AGENTS.md` as the runtime-neutral constitution and `CLAUDE.md` as a thin Claude Code wrapper. This keeps Codex, Claude Code, Hermes Runner, and future Agent Runtimes aligned on the same GitHub-first MergeGate rules while still allowing runtime-specific instructions where needed.\ndiff --git a/docs/adr/0003-require-skill-gate-for-mergegate-readiness.md b/docs/adr/SADR-0003-require-skill-gate-for-mergegate-readiness.md\nindex 424be0c9af4c8f0136f1fde68a468c47ff84e764..7fc337987c7df9792f75f181dfcbed75fe5ffff2\n--- a/docs/adr/0003-require-skill-gate-for-mergegate-readiness.md\n+++ b/docs/adr/SADR-0003-require-skill-gate-for-mergegate-readiness.md\n@@ -1,3 +1,3 @@\n-# Require Skill Gate For MergeGate Readiness\n+# SADR-0003: Require Skill Gate For MergeGate Readiness\n \n Shiki requires relevant engineering skills to be invoked before execution when their triggers apply. This makes skills part of MergeGate readiness rather than optional prompt style, because Shiki depends on repeatable planning, issue decomposition, TDD, diagnosis, triage, and architecture review before autonomous or parallel agent work can be trusted.\ndiff --git a/docs/adr/0004-use-github-first-with-shiki-mirror.md b/docs/adr/SADR-0004-use-github-first-with-shiki-mirror.md\nindex d0a3a9174dc646a945a9d325954ffaf00c352040..a79c54ae9ee8eed2a0524192b864da4e22a92d3f\n--- a/docs/adr/0004-use-github-first-with-shiki-mirror.md\n+++ b/docs/adr/SADR-0004-use-github-first-with-shiki-mirror.md\n@@ -1,4 +1,4 @@\n-# ADR 0004: Use GitHub First With A Repository-Local Shiki Mirror\n+# SADR-0004: Use GitHub First With A Repository-Local Shiki Mirror\n \n ## Status\n \ndiff --git a/docs/adr/0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md b/docs/adr/SADR-0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\nindex b339baf49b89d9290e79c3d51b36e7b2bde3bd95..c30a0d8b1fde0224d0ac7820e7870dccf721580d\n--- a/docs/adr/0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\n+++ b/docs/adr/SADR-0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\n@@ -1,4 +1,4 @@\n-# ADR 0005: Use Codex As Front And Claude Code Action As The GitHub Runtime\n+# SADR-0005: Use Codex As Front And Claude Code Action As The GitHub Runtime\n \n ## Status\n \ndiff --git a/docs/adr/0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md b/docs/adr/SADR-0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\nindex 4bcae9d1e52f0a37b8e9fbafa5cd14b6ee1993bd..7a38538a6ae9929567f0ade67e7ea6a60d0b3f7e\n--- a/docs/adr/0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\n+++ b/docs/adr/SADR-0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\n@@ -1,4 +1,4 @@\n-# ADR 0006: Separate Codex Implementation, CCA Completion, And MergeGate Authorization\n+# SADR-0006: Separate Codex Implementation, CCA Completion, And MergeGate Authorization\n \n ## Status\n \ndiff --git a/docs/adr/0007-packaging-and-release.md b/docs/adr/SADR-0007-packaging-and-release.md\nindex 79b84ddb97167372b44641e5b7860e400ce80822..bb445a2e2e856d9d1f5fdbe35de8d4e8cec0fb32\n--- a/docs/adr/0007-packaging-and-release.md\n+++ b/docs/adr/SADR-0007-packaging-and-release.md\n@@ -1,4 +1,4 @@\n-# ADR 0007: Packaging And Release For Shiki\n+# SADR-0007: Packaging And Release For Shiki\n \n ## Status\n \n@@ -25,7 +25,7 @@\n   not introduce third-party runtime dependencies or break the shim.\n - Releases must run inside the operator's subscription-authenticated toolchain\n   and must not require extra secrets beyond the built-in `GITHUB_TOKEN`\n-  (consistent with ADR 0005's no-API-key-by-default stance).\n+  (consistent with SADR-0005's no-API-key-by-default stance).\n - `.shiki/manifest.json` already encodes the install surface (`install.include`,\n   `install.create_directories`, `install.exclude_from_commit`), and it must not\n   be mutated as part of this change.\n@@ -34,7 +34,7 @@\n \n This is a hard-to-reverse platform decision (it sets the version source of\n truth, the release trigger, and the public install contract), so it is recorded\n-as an ADR.\n+as a SADR.\n \n ## Decision\n \n@@ -104,7 +104,7 @@\n 3. **Publishing to PyPI.** Rejected for now: Shiki is distributed as a\n    repository template + git-installable CLI, not a PyPI library; PyPI would add\n    credentials/secrets and a different versioning contract. Can be added later as\n-   its own ADR if a packaged distribution is needed.\n+   its own SADR if a packaged distribution is needed.\n 4. **Dynamic version (read `VERSION` via a build backend `dynamic` field).**\n    Rejected to keep `pyproject.toml` backend-agnostic and dependency-free; the\n    small duplication between `VERSION` and `pyproject.toml` is guarded by the\n@@ -123,4 +123,4 @@\n   releasing versions the whole platform but ships only `install.include` to\n   targets.\n - Future PyPI distribution or a console entry point are deferred and would each\n-  warrant their own ADR.\n+  warrant their own SADR.\ndiff --git a/docs/adr/0008-use-claude-code-as-default-implementer.md b/docs/adr/SADR-0008-use-claude-code-as-default-implementer.md\nindex dedf4510b7c7799521422f622029563d373db031..35c2625d908167fbc58855b5b63e283805f167e8\n--- a/docs/adr/0008-use-claude-code-as-default-implementer.md\n+++ b/docs/adr/SADR-0008-use-claude-code-as-default-implementer.md\n@@ -1,12 +1,12 @@\n-# ADR 0008: Use Claude Code As The Default Implementer Runtime\n+# SADR-0008: Use Claude Code As The Default Implementer Runtime\n \n ## Status\n \n-Proposed (supersedes ADR 0005 for the implementer default; keeps its auth model)\n+Proposed (supersedes SADR-0005 for the implementer default; keeps its auth model)\n \n ## Context\n \n-ADR 0005 made Codex Front the default implementation runtime, with Claude Code\n+SADR-0005 made Codex Front the default implementation runtime, with Claude Code\n restricted to planner and reviewer roles by the runtime registry, and the only\n autonomous implementation adapter was `shiki runner codex`.\n \n@@ -18,13 +18,13 @@\n - The platform's planning, review, and CCA layers already run on Claude Code,\n   and Claude Code's native capabilities (the built-in `/code-review` skill and\n   the Workflow multi-agent orchestration tool) have no Codex equivalent.\n-- The Goal lifecycle redesign (ADR 0009) requires an autonomous post-freeze\n+- The Goal lifecycle redesign (SADR-0009) requires an autonomous post-freeze\n   loop driven from the operator's Claude Code session, including bounded\n   repair dispatch, which must not depend on a runtime that cannot be invoked\n   unattended.\n \n The constitution claims Shiki is runtime-agnostic (\"not a Claude-only\n-workflow\", ADR 0002). Any change to the implementer default must preserve the\n+workflow\", SADR-0002). Any change to the implementer default must preserve the\n registry-based runtime model rather than hardcoding one vendor.\n \n ## Decision\n@@ -39,7 +39,7 @@\n   the task to review or repair-needed.\n - Keep Codex registered as an optional implementer runtime. `shiki runner\n   codex`, the Codex handoff templates, and the ChatGPT OAuth auth model from\n-  ADR 0005 remain supported for tasks explicitly assigned to `codex`.\n+  SADR-0005 remain supported for tasks explicitly assigned to `codex`.\n - Commit to mandating two Claude-native capabilities as loop contract in\n   follow-up Goals (status: the code-review gate was delivered by Goal G-B \u2014\n   skill registry, checklists item PR-12, default required_skills \u2014 while the\n@@ -60,7 +60,7 @@\n \n Out of scope: removing Codex support, changing the CCA runtime (Claude Code\n Action with `CLAUDE_CODE_OAUTH_TOKEN` stays), and API-key based automation\n-(still requires its own ADR per ADR 0005).\n+(still requires its own target ADR per SADR-0005).\n \n ## Consequences\n \n@@ -100,7 +100,7 @@\n   harness rejection of direct `codex exec` is outside Shiki's control, and the\n   plan's mandatory native capabilities cannot run on Codex at all.\n - **Remove Codex entirely**: rejected; it abandons the runtime-agnostic\n-  constitution (ADR 0002) for no operational gain and forecloses\n+  constitution (SADR-0002) for no operational gain and forecloses\n   cross-runtime adversarial review.\n - **GitHub Actions cloud implementation via claude-code-action**: rejected as\n   default; durable evidence is automatic but token cost, Node 24 workflow\ndiff --git a/docs/adr/0009-requirements-definition-and-spec-freeze.md b/docs/adr/SADR-0009-requirements-definition-and-spec-freeze.md\nindex 29b3f7871bef9203a8b4a831a65c39eb2f506e42..52e7dd69ad0aa4efe0b68c62678abbf7df02aaec\n--- a/docs/adr/0009-requirements-definition-and-spec-freeze.md\n+++ b/docs/adr/SADR-0009-requirements-definition-and-spec-freeze.md\n@@ -1,4 +1,4 @@\n-# ADR 0009: Merge Grill And Planning Into Requirements Definition With Spec Freeze\n+# SADR-0009: Merge Grill And Planning Into Requirements Definition With Spec Freeze\n \n ## Status\n \ndiff --git a/docs/adr/0010-external-ai-guardian-review.md b/docs/adr/SADR-0010-external-ai-guardian-review.md\nindex e6108bbe8bb4e73e0ff4684c8f78001a80438c4e..f3510b145664298adafb151b6f0db51b972f01ec\n--- a/docs/adr/0010-external-ai-guardian-review.md\n+++ b/docs/adr/SADR-0010-external-ai-guardian-review.md\n@@ -1,4 +1,4 @@\n-# ADR 0010: External AI Guardian Review As A First-Class Approval Authority\n+# SADR-0010: External AI Guardian Review As A First-Class Approval Authority\n \n ## Status\n \n@@ -8,7 +8,7 @@\n \n Shiki is intended to be an autonomous, recursive, self-improving agent\n framework: authority-in-the-loop, not human-in-the-loop. Until now the Guardian\n-approval required for high/critical-risk merges (guardian-policy.json, ADR\n+approval required for high/critical-risk merges (guardian-policy.json, SADR\n governance) recognized only human authority sources \u2014 a GitHub review approval,\n the `guardian:approved` label applied by a configured Guardian, or a comment\n carrying `Guardian approval granted` plus the current head SHA.\n@@ -89,7 +89,7 @@\n   autonomous design the operator intends and produced the impersonation\n   incident.\n - **Record AI approval as an operator comment**: rejected as forgery of human\n-  authority \u2014 the exact failure this ADR corrects.\n+  authority \u2014 the exact failure this SADR corrects.\n - **Deliver the artifact as a committed `.shiki/` file**: rejected; a committed\n   approval inside the PR being approved is forgeable by anyone who can push.\n - **A dedicated reviewer bot identity instead of a relayed comment**: deferred;\ndiff --git a/docs/adr/0011-autonomous-quality-gate-evidence.md b/docs/adr/SADR-0011-autonomous-quality-gate-evidence.md\nindex bca0246c5d46e44afbde15486a75eec425fc02f5..9c93eac16d8c3a6d4b11bd7224e3955102cd7db4\n--- a/docs/adr/0011-autonomous-quality-gate-evidence.md\n+++ b/docs/adr/SADR-0011-autonomous-quality-gate-evidence.md\n@@ -1,4 +1,4 @@\n-# ADR 0011: Autonomous Quality-Gate Evidence \u2014 Deterministic Facts And An Independent Verifier\n+# SADR-0011: Autonomous Quality-Gate Evidence \u2014 Deterministic Facts And An Independent Verifier\n \n ## Status\n \ndiff --git a/docs/adr/0012-autonomous-loop-closeout-to-main.md b/docs/adr/SADR-0012-autonomous-loop-closeout-to-main.md\nindex 637fad6dde4989c9c2bf0f81f676d8d29991123c..ec7d85f81030a676bcbba28b43eb365422a36eac\n--- a/docs/adr/0012-autonomous-loop-closeout-to-main.md\n+++ b/docs/adr/SADR-0012-autonomous-loop-closeout-to-main.md\n@@ -1,4 +1,4 @@\n-# 0012 \u2014 Autonomous loop pushes goal completion to main via a closeout PR\n+# SADR-0012 \u2014 Autonomous loop pushes goal completion to main via a closeout PR\n \n - Status: accepted\n - Date: 2026-06-18\ndiff --git a/docs/adr/0013-bootstrap-grants-actions-approve-pull-requests.md b/docs/adr/SADR-0013-bootstrap-grants-actions-approve-pull-requests.md\nindex b6c69d64f1c5ced346b88d6859586715b3b5bd0e..cf48ff4a6716267f539ca58edd1a852e7c7a2d64\n--- a/docs/adr/0013-bootstrap-grants-actions-approve-pull-requests.md\n+++ b/docs/adr/SADR-0013-bootstrap-grants-actions-approve-pull-requests.md\n@@ -1,4 +1,4 @@\n-# ADR 0013: Bootstrap Grants GitHub Actions Permission To Create And Approve Pull Requests\n+# SADR-0013: Bootstrap Grants GitHub Actions Permission To Create And Approve Pull Requests\n \n ## Status\n \n@@ -31,7 +31,7 @@\n security tradeoff: it lets repository automation satisfy a human-review gate. In\n a multi-maintainer repo that could let a compromised or buggy workflow\n self-approve changes. So whether bootstrap turns it on by default \u2014 and with\n-what default token scope \u2014 is an ADR-worthy decision, not an implementation\n+what default token scope \u2014 is a SADR-worthy decision, not an implementation\n detail.\n \n ## Decision\n@@ -48,7 +48,7 @@\n Specifically:\n \n - **`can_approve_pull_request_reviews` defaults to `true`.** Shiki's default\n-  posture is solo/AFK autonomous operation (ADR 0008/0009/0012), where the CCA\n+  posture is solo/AFK autonomous operation (SADR-0008/0009/0012), where the CCA\n   Review Bridge is the only path that can satisfy `required_review: true` after\n   the CCA and Guardian gates have already passed. The default makes a\n   freshly-bootstrapped repo able to run the autonomous loop end to end without a\ndiff --git a/docs/adr/0014-codex-app-external-ai-guardian-ui-adapter.md b/docs/adr/SADR-0014-codex-app-external-ai-guardian-ui-adapter.md\nindex 238db06279334caa341c117362df9271382a7fcd..65f12e9939321e1b3e389a34c822fc1549e5155d\n--- a/docs/adr/0014-codex-app-external-ai-guardian-ui-adapter.md\n+++ b/docs/adr/SADR-0014-codex-app-external-ai-guardian-ui-adapter.md\n@@ -1,4 +1,4 @@\n-# ADR 0014: Use Codex App As The External AI Guardian UI Adapter\n+# SADR-0014: Use Codex App As The External AI Guardian UI Adapter\n \n ## Status\n \ndiff --git a/docs/adr/0015-contract-approval-before-dispatch.md b/docs/adr/SADR-0015-contract-approval-before-dispatch.md\nindex fad6c656741a20a7684ba16080a7db159f63a52f..6061271a46d3f1d6ae7174477de3fb75673123a6\n--- a/docs/adr/0015-contract-approval-before-dispatch.md\n+++ b/docs/adr/SADR-0015-contract-approval-before-dispatch.md\n@@ -1,4 +1,4 @@\n-# ADR 0015: Approve The Task Contract Before Dispatch, Not The Diff After It\n+# SADR-0015: Approve The Task Contract Before Dispatch, Not The Diff After It\n \n ## Status\n \n@@ -9,7 +9,7 @@\n Guardian approval for high/critical risk is expressible only on a pull request.\n Every approval source in `guardian-policy.json` \u2014 GitHub review, the\n `guardian:approved` label, the `Guardian approval granted` comment, and the\n-`external_ai_guardian_review` artifact (ADR 0010/0014) \u2014 is bound to a PR, and\n+`external_ai_guardian_review` artifact (SADR-0010/0014) \u2014 is bound to a PR, and\n three of the four require the current head SHA. Approval therefore cannot exist\n before an implementation PR exists.\n \n@@ -81,7 +81,7 @@\n An envelope breach must surface only on the non-repairable policy gate, never on\n the repairable `MergeGate metadata check`. Otherwise the Goal Loop routes it to\n `dispatch_repair` \u2014 instructing a runner to make the breach go away \u2014 which is\n-exactly what this ADR forbids.\n+exactly what this SADR forbids.\n \n **Contract immutability.** After registration, an implementation PR must not\n change the governance fields of its own task contract (`scope`, `non_goals`,\n@@ -116,7 +116,7 @@\n fails any verdict that is not `complete`, and the signal the CCA prompt reads is\n computed from `evaluate_guardian_approval` alone. Without the OR-merge at the\n signal, a contract-approved PR still returns `needs_guardian`, the CCA job fails,\n-and the MergeGate policy check never runs \u2014 the gate this ADR changes would never\n+and the MergeGate policy check never runs \u2014 the gate this SADR changes would never\n be reached. The CCA *prompt* needs no change: a `contract_approval` entry in the\n signal's existing `sources` array satisfies its current wording.\n \n@@ -130,7 +130,7 @@\n written, and an approved contract lets its implementation PRs merge through the\n same automatic path as low/medium risk. The wasted implement-then-block cycle\n disappears. Guardian review shifts from reading diffs to reading contracts, which\n-is both earlier and smaller; the external AI Guardian path (ADR 0010/0014) works\n+is both earlier and smaller; the external AI Guardian path (SADR-0010/0014) works\n on contracts with no change.\n \n We accept that the Guardian authorizes a class of change without seeing its\n@@ -146,7 +146,7 @@\n adversarial tests for the new approval path equivalent in rigor to\n `test_shiki_governance_evidence.sh`.\n \n-Bootstrap costs, which apply only while this ADR is being implemented. The tasks\n+Bootstrap costs, which apply only while this SADR is being implemented. The tasks\n that build Contract Approval merge under the existing rules, so the ones\n dispatched after the pre-dispatch gate lands but before the approval path lands\n need a Guardian-approved Contract PR *and* a live Guardian approval on their\n@@ -210,7 +210,7 @@\n risk; they do not bound authorized work.\n \n **Auto-escalate risk on envelope breach instead of stopping.** Returns the\n-workflow to a diff-time Guardian decision \u2014 the exact behavior this ADR removes.\n+workflow to a diff-time Guardian decision \u2014 the exact behavior this SADR removes.\n \n **Resolve governance fields from the base snapshot only.** Rejected against the\n registration flow: implementation PRs add their own contract, so the base has no\ndiff --git a/docs/adr/0016-locks-bound-product-paths.md b/docs/adr/SADR-0016-locks-bound-product-paths.md\nindex b5e850f3f1f7f4ef6e7f9d1763a29a51bd300c45..46b40a9b99a7aae644c02bb458f5473ff0a90ce2\n--- a/docs/adr/0016-locks-bound-product-paths.md\n+++ b/docs/adr/SADR-0016-locks-bound-product-paths.md\n@@ -1,4 +1,4 @@\n-# ADR 0016: Locks bound product paths; the Shiki mirror is bound by record identity\n+# SADR-0016: Locks bound product paths; the Shiki mirror is bound by record identity\n \n ## Status\n \n@@ -6,8 +6,8 @@\n \n - Date: 2026-07-28\n - Deciders: operator (mizutani-140), Claude (planner)\n-- Related: ADR 0004 (GitHub-first with a `.shiki` mirror), ADR 0009 (Spec Freeze),\n-  ADR 0012 (autonomous loop closeout to main), ADR 0015 (Contract Approval before\n+- Related: SADR-0004 (GitHub-first with a `.shiki` mirror), SADR-0009 (Spec Freeze),\n+  SADR-0012 (autonomous loop closeout to main), SADR-0015 (Contract Approval before\n   dispatch), remediation goal task R-02, the live PR #179 deadlock.\n \n ## Context\n@@ -90,11 +90,11 @@\n files, documentation files and named source modules are nameable; runtime-\n allocated ledger, runner and report ids are not \u2014 and those live in `.shiki`,\n which locks no longer govern. Existing frozen contracts are brought into line by\n-a one-time Spec Amendment per plan, which must land before ADR 0015's contract\n+a one-time Spec Amendment per plan, which must land before SADR-0015's contract\n immutability makes `locks` unwritable from the implementation PR.\n \n **Lock state is resolved from the durable side.** MergeGate resolves the lock\n-registry, and ADR 0015's governance fields, from the base `.shiki` snapshot when\n+registry, and SADR-0015's governance fields, from the base `.shiki` snapshot when\n present, falling back to head only when absent. The `MergeGate metadata check`\n job is given `--base-shiki`, which it does not receive today. Locks are not\n derived from the live open-pull-request set: a pull request body is untrusted\n@@ -108,7 +108,7 @@\n Explicitly out of scope: moving MergeGate's contract resolution off the PR\n checkout wholesale; making the mirror a projection of GitHub state; introducing\n new lock kinds; moving ledger evidence out of the repository; and unwinding\n-ADR 0012's closeout pull request. Registration of multi-task goals is ADR 0015's\n+SADR-0012's closeout pull request. Registration of multi-task goals is SADR-0015's\n Contract PR and is not re-decided here.\n \n ## Consequences\n@@ -127,7 +127,7 @@\n \n Risks accepted. First, goal-scoped identity is only as strong as `goal_id`,\n which is resolved from the PR body and the head task file; the mitigation is to\n-add `goal_id` to ADR 0015's frozen governance field set, and until that lands a\n+add `goal_id` to SADR-0015's frozen governance field set, and until that lands a\n task file can name a foreign goal. Second, base-when-present resolution is\n unavailable on fork pull requests, where the head is contributor-controlled;\n fork pull requests already cannot complete a Shiki merge, and this decision does\n@@ -139,7 +139,7 @@\n path, not by implementation pull requests.\n \n New obligations: one Spec Amendment per frozen plan to rewrite `locks`, ordered\n-before ADR 0015 T1; extension of the adversarial suites in\n+before SADR-0015 T1; extension of the adversarial suites in\n `scripts/test_shiki_adversarial_state.sh` and\n `scripts/test_shiki_governance_evidence.sh` to cover the eleven newly-scoped\n directories; and updating `scripts/test_shiki_loop_lock_guard.sh`,\n@@ -151,32 +151,32 @@\n whether the loop's transitive sync of ledger `evidence` references can be\n deleted once the Contract PR carries its own source plan.\n \n-## Relationship to ADR 0004\n+## Relationship to SADR-0004\n \n-This ADR **clarifies something ADR 0004 left unspecified**. ADR 0004 decides that\n+This SADR **clarifies something SADR-0004 left unspecified**. SADR-0004 decides that\n GitHub is the operational source of truth and that each target repository keeps a\n `.shiki/` mirror. It says nothing about pull requests writing the mirror, does\n not name MergeGate, and does not make the mirror an input to a gate. Nothing in\n-ADR 0004's Decision or Consequences is amended, superseded or weakened: the\n+SADR-0004's Decision or Consequences is amended, superseded or weakened: the\n mirror still exists, still records the same artifacts, and pull request\n-boundaries remain the coordination surface. What this ADR settles is the question\n+boundaries remain the coordination surface. What this SADR settles is the question\n 0004 never reached \u2014 by what authority a pull request may write a mirror record \u2014\n and it answers it the way `.shiki/manifest.json` already declares\n (`pr_mutation: current-task-or-current-goal-only`).\n \n-It **amends ADR 0012**, whose Decision item 5 states that \"Loop-executed tasks\n+It **amends SADR-0012**, whose Decision item 5 states that \"Loop-executed tasks\n declare the `path:.shiki/**` lock, which covers every `.shiki` file the closeout\n stages ... so `files_outside_locks` is satisfied without per-file lock\n bookkeeping.\" That clause is replaced: the closeout's mirror writes are\n-authorized by record identity, not by a declared lock. ADR 0012's substantive\n+authorized by record identity, not by a declared lock. SADR-0012's substantive\n decision \u2014 that completion reaches `main` through a closeout pull request rather\n than local attestation \u2014 is preserved unchanged.\n \n-It is **complementary to ADR 0015**, not a substitute. ADR 0015 owns contract\n+It is **complementary to SADR-0015**, not a substitute. SADR-0015 owns contract\n immutability, the pre-dispatch gate, the Contract PR and the Scope Envelope. This\n-ADR owns what a lock means and who may write the mirror. The one ordering\n+SADR owns what a lock means and who may write the mirror. The one ordering\n constraint between them is that the lock-narrowing Spec Amendment must precede\n-ADR 0015 T1's freezing of the `locks` field.\n+SADR-0015 T1's freezing of the `locks` field.\n \n ## Alternatives Considered\n \n@@ -186,7 +186,7 @@\n the only writer that can register them may run. Migrating the Guardian signal to\n that store makes every registration PR fail closed to `needs_guardian` at every\n risk level, because the task file is by definition absent from the store, which\n-removes the low/medium autonomous path ADR 0008/0009 exists to provide. And it\n+removes the low/medium autonomous path SADR-0008/0009 exists to provide. And it\n leaves schemas, manifest, config, guardian policy, templates, migrations, plans,\n worktrees, runner records, runs and handoffs with no writer at all in a\n repository whose product is `.shiki`. Measured benefit on the frozen backlog:\n@@ -199,14 +199,14 @@\n namespace is not in fact ownership-partitioned \u2014 56 ledger entries carry no\n `task_id`, and one goal DAG is writable by 35 registered tasks. The correct\n expression of its insight is that locks do not govern the mirror at all, which is\n-what this ADR decides.\n+what this SADR decides.\n \n **Make the mirror a projection written only by a registration writer and a\n post-merge projection job.** Rejected. It requires a bypass actor with write\n access to the protected default branch; it makes its own master invariant\n readable from a manifest that pull requests may edit; it deletes both reconcile\n modes and leaves a corrupted mirror with no in-band repair path, contradicting\n-ADR 0004's consequence that mirror conflicts are repaired against GitHub state;\n+SADR-0004's consequence that mirror conflicts are repaired against GitHub state;\n and its content-derived ids are rejected by `validate_shiki`'s currently-green id\n pattern.\n \ndiff --git a/docs/adr/0017-bookkeeping-closeouts-do-not-inherit-risk.md b/docs/adr/SADR-0017-bookkeeping-closeouts-do-not-inherit-risk.md\nindex 554e3a0d526a72ca067108c5464cc68c3edcf174..b9676857a4713850e063af7c39c8e0b01ecb4c66\n--- a/docs/adr/0017-bookkeeping-closeouts-do-not-inherit-risk.md\n+++ b/docs/adr/SADR-0017-bookkeeping-closeouts-do-not-inherit-risk.md\n@@ -1,4 +1,4 @@\n-# ADR 0017: A Bookkeeping Closeout Does Not Inherit Its Task's Risk\n+# SADR-0017: A Bookkeeping Closeout Does Not Inherit Its Task's Risk\n \n ## Status\n \n@@ -6,7 +6,7 @@\n \n ## Context\n \n-ADR 0012 made the goal loop push a task's completion to the default branch through a\n+SADR-0012 made the goal loop push a task's completion to the default branch through a\n closeout pull request: after the implementation PR merges, the loop opens a second PR\n carrying `task.status = done`, `lock.state = released`, and \u2014 when the task completes\n its goal \u2014 `goal.status = complete` plus one scorecard report. Nothing else. The\n@@ -49,14 +49,14 @@\n 4. The task file's governance fields (`scope`, `non_goals`, `required_skills`,\n    `risk_level`, `locks`, `acceptance_checks`, `test_command`) are byte-identical to\n    the base snapshot. Only `status`, `expected_pr`, `closeout_pr`, `ledger_evidence`\n-   and `expected_branch` may differ \u2014 the ADR 0015 contract-immutability rule,\n+   and `expected_branch` may differ \u2014 the SADR-0015 contract-immutability rule,\n    unchanged.\n \n    `expected_branch` is in that set because a closeout is *by construction* on a\n    different branch from the implementation it closes, and the loop rewrites the\n    field accordingly. It is safe to let it differ because MergeGate independently\n    requires `expected_branch` to equal the PR's actual head ref, so a PR cannot set\n-   it to anything other than the branch it is really on. The first draft of this ADR\n+   it to anything other than the branch it is really on. The first draft of this SADR\n    omitted it, which made the exemption unreachable: every real closeout failed\n    condition 4 on a field the loop always changes. See PR #209.\n 5. The state transitions are exactly the terminal set: task `review` \u2192 `done`, lock\n@@ -113,12 +113,12 @@\n classification must be derived, not asserted.\n \n **Fold the closeout into the implementation PR** so no second PR exists. Rejected under\n-ADR 0012, which recorded why: a task file that is already `done` on the implementation\n+SADR-0012, which recorded why: a task file that is already `done` on the implementation\n branch short-circuits the loop's review \u2192 CCA \u2192 merge \u2192 done ordering into a local\n goal-completion before any merge happens.\n \n **Have the loop auto-merge closeouts with elevated credentials.** Rejected outright: that\n-is a runtime authoring its own approval, which ADR 0010 and the constitution's\n+is a runtime authoring its own approval, which SADR-0010 and the constitution's\n approval-record prohibition forbid. It would also be indistinguishable, in the evidence,\n from the forgery those rules exist to prevent.\n \ndiff --git a/docs/adr/0018-carried-guardian-approval-across-a-proven-base-sync.md b/docs/adr/SADR-0018-carried-guardian-approval-across-a-proven-base-sync.md\nindex 22358562da390ed8b45cd600da08a43d378b4680..e6e2907cefa776460af73ddf7d39db605f3c39b0\n--- a/docs/adr/0018-carried-guardian-approval-across-a-proven-base-sync.md\n+++ b/docs/adr/SADR-0018-carried-guardian-approval-across-a-proven-base-sync.md\n@@ -1,4 +1,4 @@\n-# ADR 0018: Carried Guardian Approval Across A Proven Base Sync\n+# SADR-0018: Carried Guardian Approval Across A Proven Base Sync\n \n ## Status\n \n@@ -34,7 +34,7 @@\n Guardian evaluator that consumes it (`shiki_guardian.evaluate_guardian_approval`,\n the `guardian_comment_carried` source) were built and tested in isolation. Neither\n does anything until a caller passes the flags that turn the carry on, so the\n-feature was invisible in CI. This ADR records the decision the two of them\n+feature was invisible in CI. This SADR records the decision the two of them\n implement, and this task wires it into the four CI invocations that judge Guardian\n approval.\n \ndiff --git a/docs/adr/README.md b/docs/adr/README.md\nindex c70e057a4520c36e042ab68c53cd4a21ef709b71..5da43856836e2ce6a150f8127766e2f88355ab4b\n--- a/docs/adr/README.md\n+++ b/docs/adr/README.md\n@@ -1,40 +1,41 @@\n-# Architecture Decision Records\n+# Decision Record Namespaces\n \n-This directory holds Shiki's Architecture Decision Records (ADRs): durable\n-records of hard-to-reverse platform decisions. ADRs are part of the Shiki source\n-of truth alongside GitHub state, the `.shiki/` mirror, and `CONTEXT.md`.\n+This directory holds two independent decision namespaces:\n \n-## How ADRs Work\n+- Shiki Architecture Decision Records (SADRs), named\n+  `SADR-NNNN-<short-slug>.md`, record hard-to-reverse Shiki platform decisions.\n+- Target Architecture Decision Records (ADRs), named\n+  `NNNN-<short-slug>.md`, belong to the target product.\n \n-- New ADRs are added as `docs/adr/NNNN-<short-slug>.md`, where `NNNN` is the next\n-  zero-padded sequence number.\n-- The **canonical list of ADRs is the set of files matching `docs/adr/*.md`** in\n-  this directory. Browse `docs/adr/*.md` for the authoritative, current set.\n-- Use [`template.md`](template.md) as the starting point for a new ADR.\n-- Record an ADR when a decision is hard to reverse, surprising without context,\n-  and a real tradeoff. Do not write an ADR for routine or easily reversible\n-  choices.\n+The same four-digit number may appear once in each namespace. A platform\n+`SADR-NNNN` citation resolves only to `SADR-NNNN-*.md`; a target `ADR NNNN`\n+citation resolves only to `NNNN-*.md`.\n \n-Using the `docs/adr/*.md` glob as the source of truth means other PRs can add an\n-ADR without editing this index file, avoiding merge contention on a\n-hand-maintained enumeration.\n+## How Decision Records Work\n \n-## Current ADRs (snapshot)\n-\n-The list below is a convenience snapshot and may lag behind the directory. When\n-in doubt, list `docs/adr/*.md`.\n+- New Shiki platform decisions use the next zero-padded SADR number.\n+- New target product decisions use the target's own ADR sequence.\n+- The canonical list is the files in `docs/adr/`; no hand-maintained index is\n+  authoritative.\n+- Use [`template.md`](template.md) as the starting point and choose the filename\n+  namespace that owns the decision.\n+- Record a decision when it is hard to reverse, surprising without context, and\n+  a real tradeoff. Do not write a decision record for routine or easily\n+  reversible choices.\n \n-- [0001 \u2014 Keep Shiki As The Platform Name](0001-keep-shiki-as-platform-name.md)\n-- [0002 \u2014 Use AGENTS As The Shared Agent Constitution](0002-use-agents-as-shared-constitution.md)\n-- [0003 \u2014 Require Skill Gate For MergeGate Readiness](0003-require-skill-gate-for-mergegate-readiness.md)\n-- [0004 \u2014 Use GitHub First With A Repository-Local Shiki Mirror](0004-use-github-first-with-shiki-mirror.md)\n-- [0005 \u2014 Use Codex As Front And Claude Code Action As The GitHub Runtime](0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md)\n-- [0006 \u2014 Separate Codex Implementation, CCA Completion, And MergeGate Authorization](0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md)\n+Using the directory as the source of truth lets independent changes add records\n+without contending on an enumerated index.\n \n-## Adding A New ADR\n+## Adding A Shiki SADR\n \n-1. Copy `template.md` to `docs/adr/NNNN-<short-slug>.md`.\n+1. Copy `template.md` to `docs/adr/SADR-NNNN-<short-slug>.md`.\n 2. Fill in the title, status, context, decision, and consequences.\n-3. Set the status to `Proposed`, then `Accepted` once the decision is approved.\n-4. Open a PR following `CONTRIBUTING.md`. You do not need to edit this index for\n-   the ADR to be discoverable via `docs/adr/*.md`.\n+3. Set the status to `Proposed`, then `Accepted` once the proper authority has\n+   approved the decision.\n+4. Open a PR following `CONTRIBUTING.md`.\n+\n+## Adding A Target ADR\n+\n+1. Copy `template.md` to `docs/adr/NNNN-<short-slug>.md` using the target's own\n+   sequence.\n+2. Cite it as `ADR NNNN`; do not prefix its filename with `SADR-`.\ndiff --git a/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md b/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\nnew file mode 100644\nindex 0000000000000000000000000000000000000000..7455ce1f3bed02ddddb851ac21a1d1a648c9aeba\n--- /dev/null\n+++ b/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\n@@ -0,0 +1,55 @@\n+# SADR-0019: Separate Shiki Platform SADRs From Target ADRs\n+\n+## Status\n+\n+Accepted\n+\n+## Context\n+\n+Shiki installs its platform decision records into `docs/adr/` in every target\n+repository. Shiki and its targets previously used the same numeric\n+`NNNN-<slug>.md` namespace. A target that already owned ADRs 0013 through 0018\n+therefore acquired two records for each number during installation, making an\n+`ADR NNNN` citation ambiguous. Renumbering target decisions would rewrite target\n+history, while renumbering Shiki decisions per target would destroy their stable\n+platform identity.\n+\n+Upgrades also need to remove the former Shiki-owned numeric paths without ever\n+mistaking a target-owned ADR for platform content. The install stamp already\n+records exact shipped paths and content digests, so it can prove ownership only\n+when both still match.\n+\n+## Decision\n+\n+Shiki platform decisions use the `SADR` namespace:\n+\n+- records are named `docs/adr/SADR-NNNN-<slug>.md`;\n+- live platform citations use `SADR-NNNN`;\n+- target product decisions retain `docs/adr/NNNN-<slug>.md` and `ADR NNNN`;\n+- the reference validator resolves the two namespaces independently, so the same\n+  four-digit number may exist once in each namespace.\n+\n+Before a forced upgrade writes anything, the installer inventories the exact\n+former numeric Shiki ADR paths. It may delete a legacy path only when the\n+existing install stamp contains that exact path and its recorded SHA-256 digest\n+matches the file currently on disk. All ownership blockers are reported\n+together and abort the upgrade before its first write. A non-forced install\n+never performs this cleanup.\n+\n+Historical `.shiki/` evidence remains unchanged because it records the namespace\n+that existed when that evidence was produced.\n+\n+## Consequences\n+\n+- Targets may use any ADR number without colliding with Shiki's platform SADRs.\n+- Fresh installs preserve target ADR bytes and install a stable SADR set.\n+- Forced upgrades fail closed when legacy ownership cannot be proven, and a\n+  successful upgrade refreshes the stamp without removed legacy paths.\n+- Current governance, packaging, workflow, prompt, script, and skill references\n+  must move with the platform records.\n+\n+## Alternatives Considered\n+\n+- Collision-only refusal leaves affected targets permanently unupgradable.\n+- Renumbering target ADRs mutates target-owned history.\n+- Per-target Shiki renumbering makes platform citations non-portable.\ndiff --git a/docs/adr/template.md b/docs/adr/template.md\nindex a92a97daddd0b00ab9dd626cc8d86942da2eafed..17dd79175b5fee2105935557f691b40cf9492942\n--- a/docs/adr/template.md\n+++ b/docs/adr/template.md\n@@ -1,14 +1,14 @@\n-# NNNN: <Short Decision Title>\n+# <SADR-NNNN or NNNN>: <Short Decision Title>\n \n ## Status\n \n-Proposed | Accepted | Superseded by ADR-NNNN\n+Proposed | Accepted | Superseded by SADR-NNNN or target ADR NNNN\n \n ## Context\n \n What is the situation, constraint, or force that requires a decision? Reference\n-relevant domain terms (`CONTEXT.md`), prior ADRs, and code reality. State the\n-problem, not the solution.\n+relevant domain terms (`CONTEXT.md`), prior Shiki SADRs or target ADRs, and code\n+reality. State the problem, not the solution.\n \n ## Decision\n \ndiff --git a/docs/agents/autonomous-loop-e2e-acceptance.md b/docs/agents/autonomous-loop-e2e-acceptance.md\nindex bbf84c6081987f159ac6ad9ecf3c82b7aa72e7e8..5c13c1d02a05a90b0dc46cbef7c95a9846d0c237\n--- a/docs/agents/autonomous-loop-e2e-acceptance.md\n+++ b/docs/agents/autonomous-loop-e2e-acceptance.md\n@@ -40,8 +40,8 @@\n 3. `gh auth status` is logged in with merge permission on the repo.\n 4. Branch protection + required checks are configured exactly as the goal-loop\n    required-check set expects (see `docs/agents/branch-protection-smoke.md`).\n-5. The repo `.shiki/policy` permits autonomous low/medium-risk merges (ADR\n-   0008/0009); the MergeGate policy check is the recorded-authority gate.\n+5. The repo `.shiki/policy` permits autonomous low/medium-risk merges\n+   (SADR-0008/0009); the MergeGate policy check is the recorded-authority gate.\n \n The operator's only actions are: register the task and start the loop. **No\n manual pre-clearing** means after `shiki loop run` starts, the operator does not\ndiff --git a/docs/agents/checklists.md b/docs/agents/checklists.md\nindex 4a7f03ade346f53a1c1d82981f3fd744f9ab4547..0c5ec6c53dc7197b7b7a7d18e667b325babd6c45\n--- a/docs/agents/checklists.md\n+++ b/docs/agents/checklists.md\n@@ -32,18 +32,18 @@\n | ID | Item | Blocking |\n | --- | --- | --- |\n | GD-01 | Plan was challenged against `CONTEXT.md`. | blocking for non-trivial Goals |\n-| GD-02 | Relevant ADRs were read or absence was recorded. | blocking for non-trivial Goals |\n+| GD-02 | Relevant SADRs and target ADRs were read or absence was recorded. | blocking for non-trivial Goals |\n | GD-03 | Ambiguous terms were resolved or converted to HITL blockers. | blocking |\n | GD-04 | Edge scenarios were considered. | advisory |\n | GD-05 | Code reality was checked when code could answer. | blocking when applicable |\n | GD-06 | New domain terms were added to `CONTEXT.md` when needed. | blocking when applicable |\n-| GD-07 | ADR was created or explicitly skipped using the ADR criteria. | blocking when applicable |\n+| GD-07 | A SADR or target ADR was created or explicitly skipped using the decision-record criteria. | blocking when applicable |\n \n ## CI \u2014 Context & Impact Checklist\n \n | ID | Item | Blocking |\n | --- | --- | --- |\n-| CI-01 | Relevant docs and ADRs identified. | blocking |\n+| CI-01 | Relevant docs, SADRs, and target ADRs identified. | blocking |\n | CI-02 | Relevant modules, interfaces, seams, callers, and tests identified. | blocking |\n | CI-03 | Dependencies and blocked-by relationships identified. | blocking |\n | CI-04 | Candidate locks identified. | blocking |\n@@ -62,7 +62,7 @@\n | PRD-04 | Implementation decisions are recorded without volatile code detail. | blocking |\n | PRD-05 | Testing decisions are recorded. | blocking |\n | PRD-06 | Out-of-scope items are explicit. | blocking |\n-| PRD-07 | Relevant glossary/ADR links are present when applicable. | blocking |\n+| PRD-07 | Relevant glossary/SADR/target-ADR links are present when applicable. | blocking |\n \n ## SF \u2014 Spec Freeze Checklist\n \n@@ -98,7 +98,7 @@\n | H-02 | Branch/worktree target is present. | blocking |\n | H-03 | Scope and non-goals are present. | blocking |\n | H-04 | Dependencies and locks are present. | blocking |\n-| H-05 | Relevant docs, ADRs, modules, and tests are referenced. | blocking |\n+| H-05 | Relevant docs, SADRs, target ADRs, modules, and tests are referenced. | blocking |\n | H-06 | Required skills are stated. | blocking |\n | H-07 | TDD expectations are stated. | blocking when behavior work |\n | H-08 | Acceptance criteria are included. | blocking |\ndiff --git a/docs/agents/codex-handoff.md b/docs/agents/codex-handoff.md\nindex ede468f47cb277f69a857e8c45df703e4ed0151d..6f97bdffb67e44d7edea2c71e01ea28933c4eb6c\n--- a/docs/agents/codex-handoff.md\n+++ b/docs/agents/codex-handoff.md\n@@ -46,7 +46,7 @@\n \n ## Context\n - Domain terms:\n-- Relevant ADRs:\n+- Relevant SADRs and target ADRs:\n - Relevant docs:\n - Relevant modules/interfaces/seams:\n - Similar tests/prior art:\ndiff --git a/docs/agents/completion-check-agent.md b/docs/agents/completion-check-agent.md\nindex 13e4c035db35025f4e3cdcc5adfb24314ac4d521..61bfa1218cf36ca605250c91919e620f33238aa0\n--- a/docs/agents/completion-check-agent.md\n+++ b/docs/agents/completion-check-agent.md\n@@ -32,7 +32,7 @@\n - `AGENTS.md`\n - `CLAUDE.md`\n - `CONTEXT.md`\n-- relevant ADRs\n+- relevant SADRs and target ADRs\n - parent Goal issue\n - PRD issue or document\n - task issue\ndiff --git a/docs/agents/decision-control.md b/docs/agents/decision-control.md\nindex 7d8ee5ab03a5ccbcbd04056ae61de03785a2a9c9..8c94ecda00fffaa0e92c325ab83aa9776bd1dac1\n--- a/docs/agents/decision-control.md\n+++ b/docs/agents/decision-control.md\n@@ -11,7 +11,7 @@\n | Decision | Owner | When | Required inputs | Output | Enforcement surface |\n | --- | --- | --- | --- | --- | --- |\n | Is the Goal clear enough to plan? | Planner + Guardian when needed | Goal intake | User request, existing docs, risk signals | Goal readiness verdict | Goal checklist, issue template, ledger |\n-| Must `grill-with-docs` run? | Planner / Skill Gate | Before PRD or issue split | Ambiguity, domain terms, ADR conflicts, unclear boundaries | Skill invocation or explicit skip rationale | Skill Gate checklist |\n+| Must `grill-with-docs` run? | Planner / Skill Gate | Before PRD or issue split | Ambiguity, domain terms, SADR/target-ADR conflicts, unclear boundaries | Skill invocation or explicit skip rationale | Skill Gate checklist |\n | Is a PRD required? | Planner | After Goal context stabilizes | Goal, `grill-with-docs` output, Context & Impact output | PRD required / not required | PRD checklist, CCA evidence check |\n | Can the spec be frozen? | Operator only | End of Requirements Definition, before issue split | PRD/requirements summary, scope inventory (SF-02), open-question list | `spec_freeze` block in the plan + ledger evidence | `require_grilled_plan` (plan ingest/run), SF checklist |\n | Can a frozen spec change? | Operator only (Spec Amendment) | When implementation contradicts the frozen spec | Contested decisions, scoped re-grill result | recorded amendment + re-stamped freeze | SF checklist, ledger evidence |\ndiff --git a/docs/agents/domain.md b/docs/agents/domain.md\nindex 258da8ea81e87ac0a6b0b5b5e5c2fde980eaabb7..d57b0cf17ff3e1fa9e6e5885372bf083d8295421\n--- a/docs/agents/domain.md\n+++ b/docs/agents/domain.md\n@@ -5,7 +5,9 @@\n ## Layout\n \n - `CONTEXT.md` defines Shiki language. Keep it implementation-free.\n-- `docs/adr/` records hard-to-reverse decisions.\n+- `docs/adr/SADR-NNNN-*.md` records Shiki platform decisions.\n+- `docs/adr/NNNN-*.md` records target product decisions without sharing the\n+  SADR namespace.\n - `AGENTS.md` is the runtime-neutral agent constitution.\n - `CLAUDE.md` is the Claude Code wrapper around `AGENTS.md`.\n - `.shiki/` is the Target Repository mirror for Goals, PRDs, plans, Task DAGs, contracts, locks, worktree records, ledger entries, CCA verdicts, repair packets, reports, and handoffs.\n@@ -15,6 +17,6 @@\n \n - Use Shiki terms exactly when they exist.\n - Add glossary terms to `CONTEXT.md` only when the term is part of the domain, not a generic programming concept.\n-- Create ADRs only for hard-to-reverse, surprising, tradeoff-based decisions.\n+- Create SADRs or target ADRs only for hard-to-reverse, surprising, tradeoff-based decisions.\n - Keep product-specific language in the Target Repository, not in Shiki Template docs.\n-- If an ADR conflicts with a proposed implementation, surface the conflict before editing.\n+- If a SADR or target ADR conflicts with a proposed implementation, surface the conflict before editing.\ndiff --git a/docs/agents/execution-control.md b/docs/agents/execution-control.md\nindex ad8ee3cdb36be56fedc1f46408ca92b387d74047..bc63c3a73661d12b567016ba299f1a6b66bf9088\n--- a/docs/agents/execution-control.md\n+++ b/docs/agents/execution-control.md\n@@ -63,7 +63,7 @@\n | Branch name | Orchestrator | Orchestrator | Before implementer dispatch | branch policy | branch ref in task/ledger |\n | Lock reservation | Context & Impact | MergeGate | Before dispatch and before merge | `.shiki/locks/`, labels, PR metadata | lock id, owner, scope |\n | CI profile | Planner | MergeGate | During issue triage, rechecked at PR | labels, `.shiki/policy.yaml`, workflow matrix | required check list |\n-| Required status checks | Repo maintainer / Guardian | GitHub branch protection / rulesets | Repository setup, updated via ADR | GitHub settings | branch/ruleset config, ADR |\n+| Required status checks | Repo maintainer / Guardian | GitHub branch protection / rulesets | Repository setup, updated via target ADR | GitHub settings | branch/ruleset config, target ADR |\n | CCA judgment | CCA | GitHub required check / MergeGate | Every non-draft PR update | GitHub Action check | structured CCA verdict |\n | Repair assignment | CCA | MergeGate | After failed CCA/CI/review | repair packet, issue/PR comment | repair id and attempt count |\n | Merge readiness | MergeGate | GitHub branch protection / Guardian if needed | After CCA complete | required checks, review rules, rulesets | mergegate check output |\ndiff --git a/docs/agents/guardian-policy.md b/docs/agents/guardian-policy.md\nindex 4147c7adf05a47adb4fd24ada1a1c2e1f2ba6d2d..a20d9cc6c00a569bb057b9ca8d6b51c7fc4a9847\n--- a/docs/agents/guardian-policy.md\n+++ b/docs/agents/guardian-policy.md\n@@ -98,8 +98,8 @@\n `origin/<default_branch>` and reuses each job's existing base fetch; a shallow\n (`--depth`) or second base fetch would make the proof unresolvable and silently\n disable the carry, so neither is permitted. CCA reads `guardian_comment_carried` as\n-a recorded authority in the deterministic signal and does not re-derive it. See ADR\n-0018 for the full decision, the rejected diff/patch-equality and path-disjointness\n+a recorded authority in the deterministic signal and does not re-derive it. See\n+SADR-0018 for the full decision, the rejected diff/patch-equality and path-disjointness\n alternatives, and the accepted residual.\n \n ## External AI Guardian Review (SADR-0010)\ndiff --git a/docs/agents/implementation-policy.md b/docs/agents/implementation-policy.md\nindex c754144a9fb5bf4a38aa82a60e1670c59e626b44..85c37281bcad196c6b7b02343faf00b03cd9c57a\n--- a/docs/agents/implementation-policy.md\n+++ b/docs/agents/implementation-policy.md\n@@ -31,10 +31,13 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n ## Non-Negotiables\n \n 1. **Planning precedes implementation.** Non-trivial Goals start with `grill-with-docs` and Context & Impact.\n-2. **Domain language is controlled.** Terms must be checked against `CONTEXT.md`; hard-to-reverse tradeoffs must be checked against ADRs.\n+2. **Domain language is controlled.** Terms must be checked against `CONTEXT.md`; hard-to-reverse tradeoffs must be checked against Shiki SADRs and target ADRs.\n 3. **PRD precedes issue decomposition.** `to-prd` is used once the context is settled enough to become durable intent.\n 4. **Issues are vertical slices.** `to-issues` must produce independently grabbable tracer-bullet tasks, not horizontal layer tickets.\n 5. **Triage controls dispatch.** Only `ready-for-agent` / AFK tasks may be assigned to Codex without human input.\n@@ -95,19 +98,19 @@\n Process:\n \n - Challenge terms against `CONTEXT.md`.\n-- Check ADRs before reopening decisions.\n+- Check SADRs and target ADRs before reopening decisions.\n - Inspect code when code can answer a question.\n - Ask one question at a time when the operator is available.\n - Provide a recommended answer with each question.\n - Record resolved terms in `CONTEXT.md`.\n-- Create ADRs only for hard-to-reverse, surprising, tradeoff-based decisions.\n+- Create SADRs or target ADRs only for hard-to-reverse, surprising, tradeoff-based decisions.\n - Convert unresolved load-bearing questions into HITL blockers.\n \n Outputs:\n \n - Resolved design-tree decisions.\n - Updated glossary terms when needed.\n-- ADRs when justified.\n+- SADRs or target ADRs when justified.\n - HITL blockers when decisions remain unresolved.\n \n ## Phase 3 \u2014 Context & Impact\n@@ -117,7 +120,7 @@\n \n Outputs must include:\n \n-- Relevant documents and ADRs.\n+- Relevant documents, SADRs, and target ADRs.\n - Relevant modules, interfaces, seams, callers, and tests.\n - Candidate locks.\n - Dependencies.\n@@ -140,7 +143,7 @@\n - Testing decisions.\n - Out of scope.\n - Further notes.\n-- Links to relevant Goal, domain terms, and ADRs.\n+- Links to relevant Goal, domain terms, SADRs, and target ADRs.\n \n Phase 4 ends with Spec Freeze: the operator explicitly approves the PRD, the\n plan records a `spec_freeze` block (status, approved_by, source), and the SF\n@@ -240,7 +243,7 @@\n - Dependencies and locks.\n - Risk and Guardian requirements.\n - Ledger completeness.\n-- Documentation and ADR updates.\n+- Documentation and SADR/target-ADR updates.\n - Scope drift.\n \n CCA emits one of:\n@@ -301,7 +304,7 @@\n | Evidence failure | Behavior might be done, but proof is missing. | Add checks, PR evidence, ledger entry, or artifacts. |\n | Scope drift | PR changes unrelated behavior. | Revert unrelated changes or split into new Goal. |\n | Dependency block | Upstream task or lock not complete. | Block until dependency resolves. |\n-| Architecture block | Decision needs ADR or deep-module discussion. | `grill-with-docs` or `improve-codebase-architecture`. |\n+| Architecture block | Decision needs a SADR, target ADR, or deep-module discussion. | `grill-with-docs` or `improve-codebase-architecture`. |\n | Risk block | Security/production/policy change needs human approval. | Guardian decision. |\n \n ## Phase 5.5 \u2014 Execution Decision Control\ndiff --git a/docs/agents/runtime-auth-model.md b/docs/agents/runtime-auth-model.md\nindex 6a2e1a47e81ea89b5c68a210ec07772a9374babc..3c54748d24233dbd0ba8473950282547ef4ce935\n--- a/docs/agents/runtime-auth-model.md\n+++ b/docs/agents/runtime-auth-model.md\n@@ -148,7 +148,7 @@\n \n API-key based runners may be added by a target repository as an explicit extension. They are not the default Shiki template path.\n \n-If a repo opts into API-key mode, record the exception in an ADR and update the repo's `AGENTS.md`, `.shiki/config.yaml`, and workflow permissions.\n+If a repo opts into API-key mode, record the target-specific exception in a target ADR and update the repo's `AGENTS.md`, `.shiki/config.yaml`, and workflow permissions.\n \n ## GitHub Actions Runtime Compatibility\n \ndiff --git a/docs/agents/skill-gate.md b/docs/agents/skill-gate.md\nindex c50398b0d7c60a8598f2a5c88a10015730dced13..3acffe6444617bb1d54b77d4b4708158ec735bc6\n--- a/docs/agents/skill-gate.md\n+++ b/docs/agents/skill-gate.md\n@@ -9,7 +9,7 @@\n | Skill | Trigger | Expected output |\n | --- | --- | --- |\n | `setup-matt-pocock-skills` | First configuring a repo for these skills, or when issue tracker, triage labels, or domain docs are missing. | `docs/agents/` configuration and an `Agent skills` block in `AGENTS.md` or `CLAUDE.md`. |\n-| `grill-with-docs` | Ambiguous plan, unclear terminology, boundary decision, tradeoff, or ADR-worthy design choice. | Clarified decision tree, recommended answers, updates to `CONTEXT.md` or ADRs when decisions crystallize. |\n+| `grill-with-docs` | Ambiguous plan, unclear terminology, boundary decision, tradeoff, or decision-record-worthy design choice. | Clarified decision tree, recommended answers, updates to `CONTEXT.md`, SADRs, or target ADRs when decisions crystallize. |\n | `zoom-out` | Agent lacks a higher-level architectural map. | Map of relevant modules, callers, seams, and domain concepts. |\n | `to-prd` | Settled context should become a PRD. | PRD published to the issue tracker with problem, solution, user stories, decisions, testing, and out-of-scope notes. |\n | `to-issues` | Goal, PRD, or plan must become implementation tickets. | Vertical-slice issues with acceptance criteria, dependencies, and AFK/HITL classification. |\n@@ -17,7 +17,7 @@\n | `tdd` | Feature work or bug fix has observable behavior that can be specified with tests. | Red-green-refactor loop through public interfaces, one vertical slice at a time. |\n | `code-review` | The TDD loop is green and the implementer is about to create or update a PR. Default for every implementation task. | Pre-PR self-review of the task diff with fixes applied on the task branch; findings and resolutions recorded as a ledger entry naming the skill and a `## Pre-PR code review` PR body section. |\n | `diagnose` | Hard bug, failing check, regression, flaky behavior, or performance problem. | Reproduction loop, ranked hypotheses, instrumentation, fix, regression test, post-mortem. |\n-| `improve-codebase-architecture` | Structural friction, testability, AI-navigability, deep-module opportunity, or refactor request. | Architecture candidates and deepening recommendations informed by `CONTEXT.md` and ADRs. |\n+| `improve-codebase-architecture` | Structural friction, testability, AI-navigability, deep-module opportunity, or refactor request. | Architecture candidates and deepening recommendations informed by `CONTEXT.md`, SADRs, and target ADRs. |\n | `prototype` | A throwaway logic or UI prototype is needed to answer a design question. | Clearly marked disposable prototype and captured decision. |\n \n ## MergeGate Interaction\ndiff --git a/scripts/guardian_approval_signal.py b/scripts/guardian_approval_signal.py\nindex d059f92b2c82aa2b5d6ca421365537a87886c1cb..16924892714f4f5b9303c0ef0e325a7e70075630\n--- a/scripts/guardian_approval_signal.py\n+++ b/scripts/guardian_approval_signal.py\n@@ -199,8 +199,8 @@\n ) -> Any:\n     \"\"\"Evaluate SADR-0015 Contract Approval for the CCA Guardian signal.\n \n-    The SAME OR that MergeGate applies must reach this deterministic signal: ADR\n-    0015 is explicit that without it, a contract-approved PR still returns\n+    The SAME OR that MergeGate applies must reach this deterministic signal:\n+    SADR-0015 is explicit that without it, a contract-approved PR still returns\n     ``needs_guardian``, the CCA job fails, and the MergeGate policy check never\n     runs \u2014 the gate this change moves would never be reached. This resolves the\n     task the SAME way MergeGate does (the first Shiki task id in the PR body),\ndiff --git a/scripts/shiki_contract_approval.py b/scripts/shiki_contract_approval.py\nindex 941bcb96fdd8b9a85bc0c4f0f04d5dad9d04839c..4a4b418f6527413161db82670bec857a043360e2\n--- a/scripts/shiki_contract_approval.py\n+++ b/scripts/shiki_contract_approval.py\n@@ -142,7 +142,7 @@\n     changed_files_status: Any,\n     registration: Any,\n ) -> ContractApprovalResult:\n-    \"\"\"Whether this PR carries an SADR-0015 Contract Approval.\n+    \"\"\"Whether this PR carries a SADR-0015 Contract Approval.\n \n     The four conditions are evaluated IN ORDER, each failure recorded as a\n     distinct reason string:\ndiff --git a/scripts/shiki_guardian.py b/scripts/shiki_guardian.py\nindex a4ecb79bbda55fd5c8b06adfae2eb99760a03c45..4dd99bbe714e26f421fa19abac7a34ae1b195ecc\n--- a/scripts/shiki_guardian.py\n+++ b/scripts/shiki_guardian.py\n@@ -12,7 +12,7 @@\n # The carry re-derives its proof from git in-process on every judging invocation;\n # it is never read from an artifact, so there is nothing to go stale or be forged.\n # shiki_sync_proof is the ONLY guardian-family module that runs git, and every one\n-# of its invocations is read-only and local (ADR: prove-from-git base sync).\n+# of its invocations is read-only and local (SADR: prove-from-git base sync).\n from shiki_sync_proof import verify_pure_base_sync\n \n GUARDIAN_POLICY_PATH = \".shiki/guardian-policy.json\"\ndiff --git a/scripts/shiki_guardian_review.py b/scripts/shiki_guardian_review.py\nindex dd0f104d457899e0ba5ff1e162011c1e90b6f92b..e10ef58c046324e7ea3a3392feb3048ca77414ce\n--- a/scripts/shiki_guardian_review.py\n+++ b/scripts/shiki_guardian_review.py\n@@ -159,7 +159,7 @@\n     docs_only = _is_docs_only(changed_files)\n     if docs_only and matched == 0:\n         focus.append(\n-            \"Docs/ADR-only change: confirm the change is genuinely documentation/decision-only, introduces no behavior or contract drift, and stays consistent with the constitution and existing ADRs.\"\n+            \"Docs/decision-record-only change: confirm the change is genuinely documentation/decision-only, introduces no behavior or contract drift, and stays consistent with the constitution and existing SADRs and target ADRs.\"\n         )\n \n     # Conservative augmentation: unknown (no category and not docs-only), mixed\ndiff --git a/scripts/shiki_installer.py b/scripts/shiki_installer.py\nindex be064ab7a29ff6836f7f7a1d70500a43e0d11d7b..d3e3247edbf3e4be287a4667176d86f6500f8dc5\n--- a/scripts/shiki_installer.py\n+++ b/scripts/shiki_installer.py\n@@ -118,6 +118,7 @@\n # what lets a target carry it without a governance-schema change.\n INSTALL_STAMP_PATH = \".shiki/install-stamp.json\"\n INSTALL_STAMP_VERSION = 1\n+LEGACY_SHIKI_SADR_MAX = 18\n \n # Under --force the shipped surface splits three ways, so nothing is lost and\n # nothing is silently stale.\n@@ -237,9 +238,98 @@\n         )\n \n \n+def legacy_shiki_adr_paths() -> tuple[str, ...]:\n+    \"\"\"Former numeric paths for Shiki SADR-0001 through SADR-0018.\n+\n+    The authoritative slugs come from the current SADR filenames. SADR-0019 is\n+    the namespace decision itself and never shipped under a numeric legacy path.\n+    \"\"\"\n+    paths: list[str] = []\n+    for sadr in sorted((ROOT / \"docs\" / \"adr\").glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\")):\n+        number = sadr.name.split(\"-\", 2)[1]\n+        if int(number) <= LEGACY_SHIKI_SADR_MAX:\n+            paths.append(f\"docs/adr/{sadr.name.removeprefix('SADR-')}\")\n+    return tuple(paths)\n+\n+\n+def preflight_legacy_sadr_cleanup(target: Path, *, force: bool) -> tuple[str, ...]:\n+    \"\"\"Authorize exact legacy Shiki ADR removals before the first write.\n+\n+    A non-force install never removes existing files. A forced install needs no\n+    stamp when no legacy Shiki path is present. When legacy paths do exist, each\n+    must be named in a readable install stamp and its current bytes must match\n+    the recorded digest.\n+    \"\"\"\n+    if not force:\n+        return ()\n+    present = tuple(\n+        relative\n+        for relative in legacy_shiki_adr_paths()\n+        if (target / relative).exists() or (target / relative).is_symlink()\n+    )\n+    if not present:\n+        return ()\n+\n+    stamp_path = target / INSTALL_STAMP_PATH\n+    try:\n+        raw = stamp_path.read_text(encoding=\"utf-8\")\n+        stamp = json.loads(raw)\n+    except FileNotFoundError:\n+        detail = \"install stamp is absent\"\n+        blockers = [f\"{relative}: {detail}\" for relative in present]\n+    except (OSError, UnicodeDecodeError) as error:\n+        detail = f\"install stamp is unreadable ({error})\"\n+        blockers = [f\"{relative}: {detail}\" for relative in present]\n+    except json.JSONDecodeError as error:\n+        detail = f\"install stamp is malformed ({error})\"\n+        blockers = [f\"{relative}: {detail}\" for relative in present]\n+    else:\n+        if not isinstance(stamp, dict):\n+            blockers = [\n+                f\"{relative}: install stamp is malformed (expected an object)\"\n+                for relative in present\n+            ]\n+        else:\n+            digests = stamp.get(\"digests\")\n+            blockers = []\n+            if not isinstance(digests, dict):\n+                blockers.extend(\n+                    f\"{relative}: install stamp has no digest for this path\"\n+                    for relative in present\n+                )\n+            else:\n+                for relative in present:\n+                    expected = digests.get(relative)\n+                    if not isinstance(expected, str) or not expected:\n+                        blockers.append(\n+                            f\"{relative}: install stamp has no digest for this path\"\n+                        )\n+                        continue\n+                    try:\n+                        actual = _sha256_file(target / relative)\n+                    except OSError as error:\n+                        blockers.append(\n+                            f\"{relative}: legacy path is unreadable ({error})\"\n+                        )\n+                        continue\n+                    if actual != expected:\n+                        blockers.append(\n+                            f\"{relative}: digest mismatch (stamp {expected}, current {actual})\"\n+                        )\n+\n+    if blockers:\n+        joined = \"\\n\".join(f\"- {blocker}\" for blocker in blockers)\n+        raise ShikiError(\n+            \"legacy Shiki ADR cleanup is not authorized:\\n\"\n+            f\"{joined}\\nNo files were written.\"\n+        )\n+    return present\n+\n+\n def install_template(target: Path, *, force: bool, validate: bool) -> list[NewFileNote]:\n     # Preconditions first: never rewrite the tree of a target that cannot be\n     # upgraded cleanly (see refuse_pending_migrations).\n+    legacy_cleanup = preflight_legacy_sadr_cleanup(target, force=force)\n     refuse_pending_migrations(target)\n \n     # Resolve the target's required CODEOWNERS owner once from its\n@@ -250,6 +340,10 @@\n     # verbatim. See ``codeowners_required_owner``.\n     codeowners_owner = codeowners_required_owner(target)\n \n+    for relative in legacy_cleanup:\n+        (target / relative).unlink()\n+        info(f\"removed stamped legacy Shiki ADR: {target / relative}\")\n+\n     notes: list[NewFileNote] = []\n     for relative in TEMPLATE_PATHS:\n         source = ROOT / relative\ndiff --git a/scripts/shiki_loop.py b/scripts/shiki_loop.py\nindex 3611641da8aeb5038b1da90e3114b09a473ec6d8..84caa491b9cad0edc4176f209c13e9221e975681\n--- a/scripts/shiki_loop.py\n+++ b/scripts/shiki_loop.py\n@@ -424,7 +424,7 @@\n         # unresolvable CCA verdict has already produced its terminal stop and is never\n         # laundered into a head-moving sync (SADR-0010). It also never fires while the\n         # policy (Guardian) gate is red \u2014 for ANY reason, not only when it is the sole\n-        # red check \u2014 so a head-bound Guardian approval, or an SADR-0015\n+        # red check \u2014 so a head-bound Guardian approval, or a SADR-0015\n         # contract-approval carry that base movement defeated (the policy gate compares\n         # base and head task governance directly), is never silently restored by\n         # adopting base wholesale. It is bounded by a durable per-task counter so the\ndiff --git a/scripts/shiki_tasks.py b/scripts/shiki_tasks.py\nindex aec3809f24b4a0c8c96fc50971cff9ae9ad30381..d531052e9d9717058d4631483cefab44c26a65c1\n--- a/scripts/shiki_tasks.py\n+++ b/scripts/shiki_tasks.py\n@@ -821,7 +821,7 @@\n         \"prompt\": prompt,\n         \"entry_skill\": \"grill-with-docs\",\n         \"required_next_steps\": [\n-            \"Run grill-with-docs until terminology, boundaries, risks, and ADR-worthy decisions are settled.\",\n+            \"Run grill-with-docs until terminology, boundaries, risks, and decision-record-worthy choices are settled.\",\n             \"Obtain the operator's explicit approval of the PRD/requirements (Spec Freeze).\",\n             \"Write a machine-readable plan JSON with grill_with_docs.status=complete and spec_freeze.status=frozen.\",\n             \"Run shiki plan ingest --plan-file PLAN.json.\",\ndiff --git a/scripts/test_shiki_goal_loop.sh b/scripts/test_shiki_goal_loop.sh\nindex 753c7b516e10f0be3bc92a4bd0eb1fa699776ae6..8f3820a0a079b3044424cfe1a6c6b7fe68896e67\n--- a/scripts/test_shiki_goal_loop.sh\n+++ b/scripts/test_shiki_goal_loop.sh\n@@ -231,8 +231,8 @@\n \n # High/critical risk merges autonomously when all required checks are green:\n # the \"MergeGate policy check\" required check IS the Guardian gate (it enforces\n-# guardian-policy.json \u2014 human approval OR external AI guardian review, ADR\n-# 0010), so green checks mean a recorded authority approved. The fake gh returns\n+# guardian-policy.json \u2014 human approval OR external AI guardian review,\n+# SADR-0010), so green checks mean a recorded authority approved. The fake gh returns\n # green checks, simulating that the Guardian gate passed.\n python3 \"$ROOT/scripts/shiki.py\" goal create --target \"$TARGET\" --title \"Guardian gate\" --outcome \"High risk merges via policy gate\" >/tmp/shiki-goal-loop-guardian-goal.json\n GGOAL=\"$(json_get /tmp/shiki-goal-loop-guardian-goal.json goal_id)\"\ndiff --git a/tests/test_adr_references.py b/tests/test_adr_references.py\nindex 51706329c797d5dd9b47e082a333fa83b261d482..db6e693e35f67103935be34f1d9bb4ff7ee60395\n--- a/tests/test_adr_references.py\n+++ b/tests/test_adr_references.py\n@@ -1,7 +1,7 @@\n-\"\"\"Every ``ADR ####`` citation must resolve to exactly one decision record.\n+\"\"\"Every target ADR and Shiki SADR citation resolves in its own namespace.\n \n Background. ``scripts/mergegate_check.py`` justifies ``is_bookkeeping_closeout``\n-with \"ADR 0017\" nine times and ``scripts/guardian_approval_signal.py`` cites\n+with \"SADR-0017\" nine times and ``scripts/guardian_approval_signal.py`` cites\n 0016/0017 seven times, yet for a stretch the numbering in ``docs/adr/`` stopped\n at 0015: the authoring commits lived on a coordinator branch and no task ever\n declared ``docs/adr/...`` in its locks, so nothing carried the records to the\n@@ -11,11 +11,12 @@\n and the directory drifted with no signal.\n \n This is the same unbound-pair shape behind other silent failures: a reference\n-and its target with nothing asserting they agree. This test binds them. It scans\n-every ``scripts/*.py`` (plus ``docs/**/*.md`` and the root ``*.md`` governance\n-files, which are cheap) for the ``ADR ####`` pattern and asserts each cited\n-number resolves to exactly one ``docs/adr/####-*.md``. The converse is\n-deliberately NOT required: an ADR may exist without being cited.\n+and its target with nothing asserting they agree. This test binds them. Target\n+``ADR NNNN`` citations resolve only to ``docs/adr/NNNN-*.md``; Shiki\n+``SADR-NNNN`` citations resolve only to ``docs/adr/SADR-NNNN-*.md``. The scan\n+covers current governance docs, scripts, workflows, prompts, packaging metadata,\n+and repo-local Codex guidance while deliberately excluding historical\n+``.shiki`` evidence. The converse is not required: a record may be uncited.\n \n The suite is dependency free: standard-library ``unittest`` only.\n \"\"\"\n@@ -31,17 +32,18 @@\n \n REPO_ROOT = Path(__file__).resolve().parents[1]\n \n-# \"ADR 0016\" -- a four-digit citation. The trailing \\b keeps a five-digit token\n-# from being read as a truncated four-digit number.\n-_CITATION = re.compile(r\"\\bADR (\\d{4})\\b\")\n-# \"0016-locks-bound-product-paths.md\" -- the leading four digits name the record.\n-_ADR_FILENAME = re.compile(r\"^(\\d{4})-.*\\.md$\")\n+# ``ADR 0016`` / ``SADR-0016`` -- a namespace plus four-digit citation. The\n+# trailing ``\\b`` keeps a five-digit token from being read as a truncated one.\n+_CITATION = re.compile(r\"\\b(S?ADR)[ -](\\d{4})\\b\")\n+# Target ADRs retain the numeric filename; Shiki platform decisions use SADR.\n+_DECISION_FILENAME = re.compile(r\"^(?:(SADR)-)?(\\d{4})-.*\\.md$\")\n \n \n class Violation(NamedTuple):\n     \"\"\"A citation that does not resolve to exactly one decision record.\"\"\"\n \n     citing_file: str  # repo-relative path of the file that cites the ADR\n+    namespace: str  # \"ADR\" for target decisions; \"SADR\" for Shiki decisions\n     number: str  # the cited four-digit number, e.g. \"0016\"\n     line: int  # 1-based line of the citation\n     reason: str  # \"missing\" (zero matches) or \"ambiguous\" (two or more)\n@@ -49,44 +51,59 @@\n \n     @property\n     def expected_path(self) -> str:\n-        return f\"docs/adr/{self.number}-*.md\"\n+        prefix = \"SADR-\" if self.namespace == \"SADR\" else \"\"\n+        return f\"docs/adr/{prefix}{self.number}-*.md\"\n \n     def describe(self) -> str:\n         \"\"\"Name the citing file, the cited number, and the missing path.\"\"\"\n         if self.reason == \"missing\":\n             return (\n-                f\"{self.citing_file}:{self.line} cites ADR {self.number} \"\n+                f\"{self.citing_file}:{self.line} cites {self.namespace} {self.number} \"\n                 f\"but no document matches {self.expected_path}\"\n             )\n         joined = \", \".join(self.matches)\n         return (\n-            f\"{self.citing_file}:{self.line} cites ADR {self.number} which \"\n+            f\"{self.citing_file}:{self.line} cites {self.namespace} {self.number} which \"\n             f\"resolves to {len(self.matches)} documents (expected exactly one \"\n             f\"at {self.expected_path}): {joined}\"\n         )\n \n \n-def _adr_index(root: Path) -> dict[str, list[Path]]:\n-    \"\"\"Map every four-digit ADR number to the record files that claim it.\"\"\"\n-    index: dict[str, list[Path]] = {}\n+def _adr_index(root: Path) -> dict[tuple[str, str], list[Path]]:\n+    \"\"\"Map each decision namespace and number to the files that claim it.\"\"\"\n+    index: dict[tuple[str, str], list[Path]] = {}\n     adr_dir = root / \"docs\" / \"adr\"\n     if adr_dir.is_dir():\n         for path in sorted(adr_dir.glob(\"*.md\")):\n-            match = _ADR_FILENAME.match(path.name)\n+            match = _DECISION_FILENAME.match(path.name)\n             if match:\n-                index.setdefault(match.group(1), []).append(path)\n+                namespace = \"SADR\" if match.group(1) else \"ADR\"\n+                index.setdefault((namespace, match.group(2)), []).append(path)\n     return index\n \n \n def _citing_files(root: Path, include_docs: bool = True) -> list[Path]:\n-    \"\"\"Files scanned for citations: scripts/*.py, and cheaply the markdown.\"\"\"\n+    \"\"\"Current, shipped text surfaces scanned for decision citations.\"\"\"\n     files = sorted((root / \"scripts\").glob(\"*.py\"))\n+    files += sorted((root / \"scripts\").glob(\"*.sh\"))\n     if include_docs:\n         docs = root / \"docs\"\n         if docs.is_dir():\n             files += sorted(docs.rglob(\"*.md\"))\n         files += sorted(root.glob(\"*.md\"))\n-    return files\n+        pyproject = root / \"pyproject.toml\"\n+        if pyproject.is_file():\n+            files.append(pyproject)\n+        for relative in (\".github\", \".codex\"):\n+            surface = root / relative\n+            if surface.is_dir():\n+                files += sorted(\n+                    path\n+                    for path in surface.rglob(\"*\")\n+                    if path.is_file()\n+                    and path.suffix in {\".md\", \".py\", \".sh\", \".toml\", \".yaml\", \".yml\"}\n+                )\n+    return sorted(set(files))\n \n \n def find_adr_reference_violations(\n@@ -94,10 +111,9 @@\n ) -> list[Violation]:\n     \"\"\"Return every ``ADR ####`` citation under ``root`` that fails to resolve.\n \n-    A citation resolves when exactly one ``docs/adr/####-*.md`` matches its\n-    number. Zero matches is a \"missing\" violation; two or more is \"ambiguous\".\n-    Records that are never cited produce nothing -- the converse is not\n-    required.\n+    A citation resolves when exactly one record in the same namespace matches\n+    its number. Zero matches is a \"missing\" violation; two or more is\n+    \"ambiguous\". Records that are never cited produce nothing.\n     \"\"\"\n     root = Path(root)\n     index = _adr_index(root)\n@@ -110,13 +126,15 @@\n         rel = path.relative_to(root).as_posix()\n         for lineno, line in enumerate(text.splitlines(), start=1):\n             for match in _CITATION.finditer(line):\n-                number = match.group(1)\n-                found = index.get(number, [])\n+                namespace, number = match.groups()\n+                found = index.get((namespace, number), [])\n                 if len(found) == 1:\n                     continue\n                 reason = \"missing\" if not found else \"ambiguous\"\n                 rels = [p.relative_to(root).as_posix() for p in found]\n-                violations.append(Violation(rel, number, lineno, reason, rels))\n+                violations.append(\n+                    Violation(rel, namespace, number, lineno, reason, rels)\n+                )\n     return violations\n \n \n@@ -129,8 +147,8 @@\n         self.assertEqual(\n             violations,\n             [],\n-            \"Every 'ADR ####' citation must resolve to exactly one \"\n-            f\"docs/adr/####-*.md:\\n{detail}\",\n+            \"Every ADR/SADR citation must resolve exactly once in its own \"\n+            f\"docs/adr namespace:\\n{detail}\",\n         )\n \n     def test_scan_actually_reads_scripts(self) -> None:\n@@ -146,8 +164,21 @@\n         # The two records this Goal carried onto the default branch, and which\n         # the scripts cite most, must be present.\n         index = _adr_index(REPO_ROOT)\n-        self.assertIn(\"0016\", index)\n-        self.assertIn(\"0017\", index)\n+        self.assertIn((\"SADR\", \"0016\"), index)\n+        self.assertIn((\"SADR\", \"0017\"), index)\n+\n+    def test_platform_tree_has_only_sadr_0001_through_0019(self) -> None:\n+        adr_dir = REPO_ROOT / \"docs\" / \"adr\"\n+        sadrs = sorted(adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\"))\n+        self.assertEqual(\n+            {path.name.split(\"-\", 2)[1] for path in sadrs},\n+            {f\"{number:04d}\" for number in range(1, 20)},\n+        )\n+        self.assertEqual(\n+            list(adr_dir.glob(\"[0-9][0-9][0-9][0-9]-*.md\")),\n+            [],\n+            \"the Shiki platform tree must not retain legacy numeric SADR paths\",\n+        )\n \n \n class FixtureAdrReferences(unittest.TestCase):\n@@ -180,6 +211,26 @@\n         self.assertIn(\"9991\", message)  # cited number\n         self.assertIn(\"docs/adr/9991-*.md\", message)  # missing path\n \n+    def test_target_adr_does_not_satisfy_missing_platform_sadr(self) -> None:\n+        root = self._make_repo(\n+            adr_names=[\"0042-target-decision.md\"],\n+            script_body='\"\"\"Target ADR 0042 exists, but platform SADR-0042 does not.\"\"\"\\n',\n+        )\n+        violations = find_adr_reference_violations(root, include_docs=False)\n+        self.assertEqual(len(violations), 1)\n+        self.assertEqual(violations[0].namespace, \"SADR\")\n+        self.assertEqual(violations[0].number, \"0042\")\n+        self.assertEqual(violations[0].expected_path, \"docs/adr/SADR-0042-*.md\")\n+\n+    def test_target_adr_and_platform_sadr_may_reuse_a_number(self) -> None:\n+        root = self._make_repo(\n+            adr_names=[\"0042-target-decision.md\", \"SADR-0042-platform-decision.md\"],\n+            script_body='\"\"\"Target ADR 0042 and platform SADR-0042 are distinct.\"\"\"\\n',\n+        )\n+        self.assertEqual(\n+            find_adr_reference_violations(root, include_docs=False), []\n+        )\n+\n     def test_uncited_adr_is_allowed(self) -> None:\n         # An ADR exists but nothing cites it -> not a violation. The converse\n         # (every ADR must be cited) is explicitly NOT required.\ndiff --git a/tests/test_guardian_review.py b/tests/test_guardian_review.py\nindex 88829e7810212635e2fc98aa5918d6f26294677d..00a85a237acd5bf2d6cb5b294128198c422402ca\n--- a/tests/test_guardian_review.py\n+++ b/tests/test_guardian_review.py\n@@ -162,7 +162,7 @@\n \n     def test_docs_only_is_light_and_not_conservative(self):\n         focus = gr.classify_review_focus([\"docs/adr/0099-x.md\", \"README.md\"], \"\", \"low\")\n-        self.assertTrue(any(\"Docs/ADR-only\" in f for f in focus))\n+        self.assertTrue(any(\"Docs/decision-record-only\" in f for f in focus))\n         self.assertFalse(any(\"conservative default\" in f.lower() for f in focus))\n \n     def test_unknown_category_is_conservative(self):\ndiff --git a/tests/test_policy_job_merged_prs.py b/tests/test_policy_job_merged_prs.py\nindex d45512e74681e5e649279236c7d4e79d5c3da471..b97989f8bd74d091de1a6b5275a20d8dc68c4512\n--- a/tests/test_policy_job_merged_prs.py\n+++ b/tests/test_policy_job_merged_prs.py\n@@ -3,8 +3,8 @@\n \n ``.github/workflows/shiki-cca-completion.yml`` has two consumers of the merged-PR\n proof: the CCA *signal* (``guardian_approval_signal.py`` in \"Prepare CCA context\")\n-and the authoritative gate (``mergegate_check.py`` in \"Enforce MergeGate\"). ADR\n-0017's bookkeeping-closeout exemption (condition 6) needs the merged implementation\n+and the authoritative gate (``mergegate_check.py`` in \"Enforce MergeGate\").\n+SADR-0017's bookkeeping-closeout exemption (condition 6) needs the merged implementation\n PR proven, and a closeout carries NO ``post_merge_reconcile`` marker. The signal\n already resolves ``MERGED_PRS`` unconditionally, but \"Enforce MergeGate\" used to\n resolve it only inside ``grep -q '<!-- shiki:post_merge_reconcile -->'``. So a\ndiff --git a/tests/test_sadr_install_migration.py b/tests/test_sadr_install_migration.py\nnew file mode 100644\nindex 0000000000000000000000000000000000000000..5a442fa5e4968ade178dcdedc3c63b54dba580a2\n--- /dev/null\n+++ b/tests/test_sadr_install_migration.py\n@@ -0,0 +1,224 @@\n+\"\"\"SADR installation and collision-safe legacy upgrade behavior.\"\"\"\n+\n+from __future__ import annotations\n+\n+import contextlib\n+import hashlib\n+import io\n+import json\n+import shutil\n+import tempfile\n+import unittest\n+from pathlib import Path\n+\n+import shiki_test_support  # noqa: F401  (path bootstrap)\n+\n+import shiki_installer\n+from tests.test_adr_references import find_adr_reference_violations\n+\n+\n+PRODUCT_NUMBERS = tuple(range(13, 19))\n+\n+\n+def _quiet_install(target: Path, *, force: bool, validate: bool) -> None:\n+    with contextlib.redirect_stdout(io.StringIO()):\n+        shiki_installer.install_template(target, force=force, validate=validate)\n+\n+\n+def _sha256(path: Path) -> str:\n+    return hashlib.sha256(path.read_bytes()).hexdigest()\n+\n+\n+def _legacy_sadr_paths() -> tuple[str, ...]:\n+    return tuple(\n+        f\"docs/adr/{path.name.removeprefix('SADR-')}\"\n+        for path in sorted((shiki_installer.ROOT / \"docs\" / \"adr\").glob(\"SADR-*.md\"))\n+        if path.name != \"SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\"\n+    )\n+\n+\n+def _create_legacy_upgrade_fixture(\n+    target: Path,\n+) -> tuple[dict[str, bytes], tuple[str, ...]]:\n+    adr_dir = target / \"docs\" / \"adr\"\n+    adr_dir.mkdir(parents=True)\n+    product_bytes = {\n+        f\"{number:04d}-cubeshare-product-decision.md\": (\n+            f\"# CubeShare ADR {number:04d}\\n\\nDo not rewrite {number}.\\n\"\n+        ).encode()\n+        for number in PRODUCT_NUMBERS\n+    }\n+    for name, content in product_bytes.items():\n+        (adr_dir / name).write_bytes(content)\n+\n+    legacy_paths = _legacy_sadr_paths()\n+    for relative in legacy_paths:\n+        (target / relative).write_bytes(f\"legacy Shiki bytes for {relative}\\n\".encode())\n+\n+    stamp_path = target / shiki_installer.INSTALL_STAMP_PATH\n+    stamp_path.parent.mkdir(parents=True, exist_ok=True)\n+    stamp = {\n+        \"version\": shiki_installer.INSTALL_STAMP_VERSION,\n+        \"platform_commit\": None,\n+        \"installed_at\": \"2000-01-01T00:00:00+00:00\",\n+        \"digests\": {\n+            relative: _sha256(target / relative) for relative in legacy_paths\n+        },\n+    }\n+    stamp_path.write_text(\n+        json.dumps(stamp, indent=2, sort_keys=True) + \"\\n\", encoding=\"utf-8\"\n+    )\n+    return product_bytes, legacy_paths\n+\n+\n+def _tree_snapshot(root: Path) -> dict[str, tuple[str, bytes | str]]:\n+    \"\"\"Recursive byte/shape snapshot used to prove preflight is write-free.\"\"\"\n+    snapshot: dict[str, tuple[str, bytes | str]] = {}\n+    for path in sorted(root.rglob(\"*\")):\n+        relative = path.relative_to(root).as_posix()\n+        if path.is_symlink():\n+            snapshot[relative] = (\"symlink\", path.readlink().as_posix())\n+        elif path.is_dir():\n+            snapshot[relative] = (\"directory\", \"\")\n+        else:\n+            snapshot[relative] = (\"file\", path.read_bytes())\n+    return snapshot\n+\n+\n+class FreshSadrInstallTests(unittest.TestCase):\n+    def setUp(self) -> None:\n+        self.tmp = Path(tempfile.mkdtemp(prefix=\"shiki-sadr-fresh-\"))\n+        self.addCleanup(lambda: shutil.rmtree(self.tmp, ignore_errors=True))\n+        self.target = self.tmp / \"target\"\n+        self.adr_dir = self.target / \"docs\" / \"adr\"\n+        self.adr_dir.mkdir(parents=True)\n+        self.product_bytes = {\n+            f\"{number:04d}-product-decision.md\": (\n+                f\"# Product ADR {number:04d}\\n\\nTarget-owned bytes {number}.\\n\"\n+            ).encode()\n+            for number in PRODUCT_NUMBERS\n+        }\n+        for name, content in self.product_bytes.items():\n+            (self.adr_dir / name).write_bytes(content)\n+\n+    def test_fresh_install_preserves_product_adrs_and_installs_all_sadrs(self) -> None:\n+        _quiet_install(self.target, force=False, validate=True)\n+\n+        for name, expected in self.product_bytes.items():\n+            self.assertEqual((self.adr_dir / name).read_bytes(), expected)\n+\n+        sadr_numbers = {\n+            path.name.split(\"-\", 2)[1]\n+            for path in self.adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\")\n+        }\n+        self.assertEqual(sadr_numbers, {f\"{number:04d}\" for number in range(1, 20)})\n+\n+        numeric_records = {\n+            path.name\n+            for path in self.adr_dir.glob(\"[0-9][0-9][0-9][0-9]-*.md\")\n+        }\n+        self.assertEqual(numeric_records, set(self.product_bytes))\n+        self.assertEqual(find_adr_reference_violations(self.target), [])\n+\n+\n+class LegacySadrUpgradeTests(unittest.TestCase):\n+    def setUp(self) -> None:\n+        self.tmp = Path(tempfile.mkdtemp(prefix=\"shiki-sadr-upgrade-\"))\n+        self.addCleanup(lambda: shutil.rmtree(self.tmp, ignore_errors=True))\n+        self.target = self.tmp / \"target\"\n+        self.adr_dir = self.target / \"docs\" / \"adr\"\n+        self.product_bytes, self.legacy_paths = _create_legacy_upgrade_fixture(\n+            self.target\n+        )\n+\n+    def test_force_upgrade_deletes_only_stamped_legacy_sadrs(self) -> None:\n+        _quiet_install(self.target, force=True, validate=True)\n+\n+        for relative in self.legacy_paths:\n+            self.assertFalse((self.target / relative).exists(), relative)\n+        for name, expected in self.product_bytes.items():\n+            self.assertEqual((self.adr_dir / name).read_bytes(), expected)\n+\n+        sadrs = list(self.adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\"))\n+        self.assertEqual(len(sadrs), 19)\n+        refreshed = json.loads(\n+            (self.target / shiki_installer.INSTALL_STAMP_PATH).read_text(encoding=\"utf-8\")\n+        )\n+        for relative in self.legacy_paths:\n+            self.assertNotIn(relative, refreshed[\"digests\"])\n+        for path in sadrs:\n+            relative = path.relative_to(self.target).as_posix()\n+            self.assertEqual(refreshed[\"digests\"][relative], _sha256(path))\n+\n+    def test_non_force_install_never_deletes_legacy_files(self) -> None:\n+        before = {\n+            relative: (self.target / relative).read_bytes()\n+            for relative in self.legacy_paths\n+        }\n+        _quiet_install(self.target, force=False, validate=False)\n+        for relative, expected in before.items():\n+            self.assertEqual((self.target / relative).read_bytes(), expected)\n+\n+\n+class LegacyCleanupPreflightTests(unittest.TestCase):\n+    def setUp(self) -> None:\n+        self.tmp = Path(tempfile.mkdtemp(prefix=\"shiki-sadr-preflight-\"))\n+        self.addCleanup(lambda: shutil.rmtree(self.tmp, ignore_errors=True))\n+\n+    def test_all_ownership_blockers_abort_before_any_write(self) -> None:\n+        cases = (\n+            (\"absent\", \"install stamp is absent\"),\n+            (\"unreadable\", \"install stamp is unreadable\"),\n+            (\"malformed\", \"install stamp is malformed\"),\n+            (\"missing-digest\", \"install stamp has no digest\"),\n+            (\"digest-mismatch\", \"digest mismatch\"),\n+        )\n+        for name, expected_reason in cases:\n+            with self.subTest(name=name):\n+                target = self.tmp / name\n+                _, legacy_paths = _create_legacy_upgrade_fixture(target)\n+                stamp_path = target / shiki_installer.INSTALL_STAMP_PATH\n+\n+                if name == \"absent\":\n+                    stamp_path.unlink()\n+                elif name == \"unreadable\":\n+                    stamp_path.unlink()\n+                    stamp_path.mkdir()\n+                elif name == \"malformed\":\n+                    stamp_path.write_text(\"{not-json\\n\", encoding=\"utf-8\")\n+                else:\n+                    stamp = json.loads(stamp_path.read_text(encoding=\"utf-8\"))\n+                    if name == \"missing-digest\":\n+                        stamp[\"digests\"] = {}\n+                    else:\n+                        stamp[\"digests\"] = {\n+                            relative: \"deadbeef\" for relative in legacy_paths\n+                        }\n+                    stamp_path.write_text(\n+                        json.dumps(stamp, indent=2, sort_keys=True) + \"\\n\",\n+                        encoding=\"utf-8\",\n+                    )\n+\n+                before = _tree_snapshot(target)\n+                with self.assertRaises(shiki_installer.ShikiError) as raised:\n+                    _quiet_install(target, force=True, validate=False)\n+                after = _tree_snapshot(target)\n+\n+                self.assertEqual(before, after, f\"{name} preflight changed the tree\")\n+                message = str(raised.exception)\n+                self.assertIn(expected_reason, message)\n+                self.assertIn(\"No files were written\", message)\n+                for relative in legacy_paths:\n+                    self.assertIn(relative, message)\n+\n+    def test_absent_stamp_is_allowed_when_no_legacy_path_exists(self) -> None:\n+        target = self.tmp / \"no-legacy\"\n+        target.mkdir()\n+        _quiet_install(target, force=True, validate=False)\n+        self.assertTrue(\n+            (target / \"docs\" / \"adr\" / \"SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\").is_file()\n+        )\n+\n+\n+if __name__ == \"__main__\":\n+    unittest.main()\n\n2026-08-07T08:14:52.002372Z ERROR codex_models_manager::manager: failed to renew cache TTL: missing field `base_instructions` at line 94 column 5\napply patch\npatch: completed\n/private/tmp/shiki-adr-collision.pa0KwY/.worktrees/shiki-t-20260807t073307221254z-2d22d654-introduc/tests/test_sadr_install_migration.py\ndiff --git a/.codex/skills/shiki/SKILL.md b/.codex/skills/shiki/SKILL.md\nindex c8e2e85d9d75d49cc4f2d6286ee22659c4e53e75..93cded623b2c15d9f65c76491a1c2d813ac9e6c0\n--- a/.codex/skills/shiki/SKILL.md\n+++ b/.codex/skills/shiki/SKILL.md\n@@ -18,6 +18,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n Codex CLI does not register this skill as a custom `/shiki` slash command. When\n the user types `/shiki` in Codex CLI and gets \"Unrecognized command\", explain\n that this is expected: invoke Shiki in Codex with natural language such as\n@@ -69,7 +72,7 @@\n \n ## Responsibilities\n \n-- Claude Code implements and repairs by default (`shiki runner claude`, ADR 0008); Codex implements and repairs tasks explicitly assigned to `codex`.\n+- Claude Code implements and repairs by default (`shiki runner claude`, SADR-0008); Codex implements and repairs tasks explicitly assigned to `codex`.\n - Claude Code Action can act as GitHub-side CCA or reviewer.\n - CCA judges completion.\n - MergeGate authorizes state transitions and merge readiness.\ndiff --git a/.github/prompts/cca-completion-check.md b/.github/prompts/cca-completion-check.md\nindex ed203ad5f302ecc81372d8ab27d2b1232ca97497..fd89950528333ef6242e6d5443904f5392ab7e56\n--- a/.github/prompts/cca-completion-check.md\n+++ b/.github/prompts/cca-completion-check.md\n@@ -19,6 +19,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n ## Required Reading\n \n Read only the files needed to judge the PR contract. Prefer this order:\ndiff --git a/.github/prompts/codex-task-handoff.md b/.github/prompts/codex-task-handoff.md\nindex 695e211d76dea1c60e8d5bb360e78487808286a4..59441d576b01a39569722f226c854e5a740e9eb5\n--- a/.github/prompts/codex-task-handoff.md\n+++ b/.github/prompts/codex-task-handoff.md\n@@ -2,7 +2,7 @@\n \n You are Codex Front acting as the Implementer for a Shiki task.\n \n-Follow `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant ADRs, and `docs/agents/codex-handoff.md`.\n+Follow `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant Shiki SADRs and target ADRs, and `docs/agents/codex-handoff.md`.\n \n Rules:\n \ndiff --git a/AGENTS.md b/AGENTS.md\nindex 7f041100d49c2a7525f51cb0f5d696a4a6d1c78c..a06bfbabc6fa48a007389e25e12d7ed523936bed\n--- a/AGENTS.md\n+++ b/AGENTS.md\n@@ -22,7 +22,7 @@\n - **Codex Front** is an optional implementer through Codex App, Codex CLI, Codex IDE extension, or Codex Web signed in with ChatGPT OAuth/subscription auth, dispatched with `shiki runner codex` when a task is explicitly assigned to `codex`.\n - **GitHub CCA** is implemented by Claude Code Action by default, using `CLAUDE_CODE_OAUTH_TOKEN`.\n \n-Do not assume `openai/codex-action`, `OPENAI_API_KEY`, or API-key based Codex automation in the default Shiki path. API-key runners are explicit target-repo extensions and require their own ADR.\n+Do not assume `openai/codex-action`, `OPENAI_API_KEY`, or API-key based Codex automation in the default Shiki path. API-key runners are explicit target-repo extensions and require their own target ADR.\n \n Shiki is not:\n \n@@ -44,10 +44,14 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use\n+`SADR-NNNN-*.md`; target product decisions retain `NNNN-*.md` and the `ADR`\n+namespace.\n+\n Use the canonical source-of-truth order above. Runtime-specific reading orders\n are operational guidance only and must not override or reorder it.\n \n-Conversation state is not durable truth. If a decision matters, put it in GitHub, `.shiki/`, `CONTEXT.md`, or an ADR.\n+Conversation state is not durable truth. If a decision matters, put it in GitHub, `.shiki/`, `CONTEXT.md`, a Shiki SADR, or a target ADR.\n \n If GitHub and `.shiki/` disagree, surface the conflict and prefer GitHub operational state until the mirror is repaired.\n \n@@ -75,8 +79,8 @@\n Every non-trivial change follows this loop:\n \n 1. **Goal**: clarify outcome, completion conditions, non-goals, risk level, and success signals.\n-2. **grill-with-docs**: challenge the plan against domain docs, ADRs, code reality, terminology, and edge scenarios.\n-3. **Context & Impact**: find relevant docs, ADRs, code, dependencies, owners, locks, and verification surfaces.\n+2. **grill-with-docs**: challenge the plan against domain docs, Shiki SADRs, target ADRs, code reality, terminology, and edge scenarios.\n+3. **Context & Impact**: find relevant docs, SADRs, target ADRs, code, dependencies, owners, locks, and verification surfaces.\n 4. **PRD and Spec Freeze**: use `to-prd` when context is settled enough to become durable product/engineering intent. The operator's explicit approval of the PRD is Spec Freeze: the plan records a `spec_freeze` block, and no plan runs without it. Steps 1-4 form Requirements Definition \u2014 one continuous operator dialogue. After Spec Freeze, scope changes require an operator-approved Spec Amendment; non-scope-moving interpretations go to the Assumption Log.\n 5. **Issues**: use `to-issues` to create independently grabbable vertical-slice issues. Prefer AFK slices over HITL slices where possible.\n 6. **Triage**: label issues for readiness, risk, runtime, skills, and MergeGate state.\n@@ -167,7 +171,7 @@\n - Testing decisions.\n - Out-of-scope items.\n - Further notes.\n-- Links to relevant domain terms and ADRs.\n+- Links to relevant domain terms, SADRs, and target ADRs.\n \n Every executable task must include:\n \n@@ -193,7 +197,7 @@\n \n Context & Impact must identify:\n \n-- Relevant documents, domain terms, ADRs, and past decisions.\n+- Relevant documents, domain terms, SADRs, target ADRs, and past decisions.\n - Relevant modules, interfaces, seams, callers, owners, and tests.\n - Dependency relationships between tasks.\n - Candidate locks and lock conflicts.\n@@ -210,7 +214,7 @@\n | Skill | Required when |\n | --- | --- |\n | `setup-matt-pocock-skills` | First configuring the repo for skills, or when issue tracker, triage labels, or domain docs are missing. |\n-| `grill-with-docs` | Default for non-trivial Goals before PRD/issues; always required when plans, terminology, boundaries, tradeoffs, or ADR-worthy decisions are ambiguous. |\n+| `grill-with-docs` | Default for non-trivial Goals before PRD/issues; always required when plans, terminology, boundaries, tradeoffs, or decision-record-worthy choices are ambiguous. |\n | `zoom-out` | The agent lacks an architectural map of the relevant code area. |\n | `to-prd` | Settled Goal context must become durable product/engineering intent. |\n | `to-issues` | A Goal, PRD, or plan must become independently grabbable vertical-slice issues. |\n@@ -348,7 +352,7 @@\n - Three or more new files.\n - Any high-risk or critical label.\n \n-When the architecture gate triggers, run `grill-with-docs` or `improve-codebase-architecture` as appropriate and record durable decisions in an ADR when needed.\n+When the architecture gate triggers, run `grill-with-docs` or `improve-codebase-architecture` as appropriate and record durable decisions in a Shiki SADR or target ADR as applicable.\n \n ## Engineering Discipline\n \ndiff --git a/CLAUDE.md b/CLAUDE.md\nindex 98a932e7841a632866fcd53b0bad0e3af3711d3d..98a31399404d6fb044b4fd493f4459ac87f439de\n--- a/CLAUDE.md\n+++ b/CLAUDE.md\n@@ -14,6 +14,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n ## Claude Role\n \n Default to planner, default implementer, reviewer, coordinator, documentation editor, CCA-style completion judge, and final-judgment assistant.\n@@ -57,7 +60,7 @@\n At the start of a Shiki session:\n \n 1. Read `AGENTS.md`.\n-2. Read `CONTEXT.md` and relevant ADRs.\n+2. Read `CONTEXT.md` and relevant Shiki SADRs and target ADRs.\n 3. Read active `.shiki/` Goal/task/ledger state when present.\n 4. Read active distilled rules from `.shiki/memories/` (status `distilled`, `active: true`, not revoked or superseded) for applicable guidance; the same rules are injected deterministically into task handoffs as the `## Distilled Rules` section (\u00a73.5).\n 5. Read the GitHub Issue or PR when the task is GitHub-backed.\n@@ -74,7 +77,7 @@\n - Define the Goal, completion conditions, non-goals, and risk level.\n - Treat `grill-with-docs` as the default planning entry point for non-trivial Goals.\n - Challenge domain terms against `CONTEXT.md`.\n-- Challenge hard-to-reverse tradeoffs against ADRs.\n+- Challenge hard-to-reverse tradeoffs against SADRs and target ADRs.\n - Explore code instead of asking when code can answer the question.\n - Run Context & Impact before implementation planning.\n - Produce a Task DAG, not an unordered checklist.\n@@ -94,7 +97,7 @@\n - For each question, provide your recommended answer.\n - Resolve dependency decisions before downstream design questions.\n - Update `CONTEXT.md` inline when terminology crystallizes.\n-- Offer ADRs only when the decision is hard to reverse, surprising without context, and a real tradeoff.\n+- Offer a SADR or target ADR only when the decision is hard to reverse, surprising without context, and a real tradeoff.\n - Convert unresolved questions into HITL blockers rather than allowing Codex to guess.\n \n ## PRD and Issues Standard\n@@ -103,7 +106,7 @@\n \n - Use the project's domain glossary vocabulary.\n - Avoid volatile file paths unless they are essential.\n-- Include implementation decisions, testing decisions, out-of-scope items, and relevant ADR/domain links.\n+- Include implementation decisions, testing decisions, out-of-scope items, and relevant SADR/target-ADR/domain links.\n - Identify deep-module opportunities and testable interfaces.\n \n When creating issues:\n@@ -121,7 +124,7 @@\n - Branch/worktree target.\n - Scope and non-goals.\n - Dependencies and locks.\n-- Relevant docs, ADRs, modules, and tests.\n+- Relevant docs, SADRs, target ADRs, modules, and tests.\n - Required skill invocations.\n - TDD expectations.\n - Pre-PR `code-review` gate expectations.\n@@ -152,7 +155,7 @@\n \n - Treat yourself as a judge, not implementer.\n - Do not write production code.\n-- Read `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant ADRs, PRD, task issue, PR body, diff, checks, reviews, labels, and `.shiki/` state.\n+- Read `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant SADRs and target ADRs, PRD, task issue, PR body, diff, checks, reviews, labels, and `.shiki/` state.\n - Evaluate every applicable checklist in `docs/agents/checklists.md`.\n - Map each acceptance criterion to durable evidence.\n - Distinguish implementation failure from missing evidence.\ndiff --git a/CONTEXT.md b/CONTEXT.md\nindex e61a9be06a0cce0d026484083a657bde2067e234..237ee8aa3ea8fab334ece49768bc5ed3b5af0974\n--- a/CONTEXT.md\n+++ b/CONTEXT.md\n@@ -25,7 +25,7 @@\n _Avoid_: jumping directly into code\n \n **grill-with-docs**:\n-A planning skill that challenges a plan against domain language, ADRs, code reality, and concrete edge scenarios. It resolves design-tree questions before PRD/issues. It runs inside Requirements Definition.\n+A planning skill that challenges a plan against domain language, Shiki SADRs, target ADRs, code reality, and concrete edge scenarios. It resolves design-tree questions before PRD/issues. It runs inside Requirements Definition.\n _Avoid_: generic brainstorming, silent assumptions\n \n **Requirements Definition**:\ndiff --git a/CONTRIBUTING.md b/CONTRIBUTING.md\nindex c6d9b656e73abb8a55e5f660123e03153dcb3e6d..9e37a8fbf668db5b22461aaae5548b8f1354394c\n--- a/CONTRIBUTING.md\n+++ b/CONTRIBUTING.md\n@@ -17,7 +17,7 @@\n 1. **Goal** \u2014 start from a user-approved Goal with an outcome, completion\n    conditions, non-goals, risk level, and success signals.\n 2. **grill-with-docs** \u2014 for any non-trivial change, challenge the plan against\n-   domain language, ADRs, and code reality before writing a PRD.\n+   domain language, Shiki SADRs, target ADRs, and code reality before writing a PRD.\n 3. **PRD** \u2014 capture settled product/engineering intent (`to-prd`).\n 4. **Spec Freeze** \u2014 the operator explicitly approves the PRD/requirements;\n    the plan records a `spec_freeze` block and no plan runs without it\n@@ -94,7 +94,7 @@\n   entries.\n - Do not hand-edit `.shiki/goals/*.json` or `.shiki/manifest.json` in a task PR.\n - Conversation state is not durable truth. If a decision matters, record it in\n-  GitHub, `.shiki/`, `CONTEXT.md`, or an ADR.\n+  GitHub, `.shiki/`, `CONTEXT.md`, a Shiki SADR, or a target ADR.\n \n ## Architecture Gate\n \n@@ -103,7 +103,7 @@\n or CI policy, package structure, three or more structural files, more than 100\n net new lines in a core module, three or more new files, or any high-risk label.\n When triggered, run `grill-with-docs` or `improve-codebase-architecture` and\n-record durable decisions in an ADR when needed (see `docs/adr/`).\n+record durable decisions in a Shiki SADR or target ADR as applicable (see `docs/adr/`).\n \n ## Safety\n \ndiff --git a/SYSTEM_PROMPT.md b/SYSTEM_PROMPT.md\nindex aaaea45e3a896c3a44ba89eec8bb84325ecf6c62..da009c86a88285269dee0155e58e550c6d744ea9\n--- a/SYSTEM_PROMPT.md\n+++ b/SYSTEM_PROMPT.md\n@@ -30,6 +30,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n Use the canonical source-of-truth order above. Runtime-specific reading orders\n are operational guidance only and must not override or reorder it. The current\n conversation is non-durable operator input.\n@@ -48,14 +51,14 @@\n \n A runtime may only do work that is explicitly assigned by the Goal, task, PR, or MergeGate decision.\n \n-Do not assume `openai/codex-action`, `OPENAI_API_KEY`, or API-key based Codex automation in the default Shiki path. API-key runners are explicit target-repo extensions and require their own ADR.\n+Do not assume `openai/codex-action`, `OPENAI_API_KEY`, or API-key based Codex automation in the default Shiki path. API-key runners are explicit target-repo extensions and require their own target ADR.\n \n ## 4. Non-negotiable Shiki flow\n \n For any non-trivial Goal, use this flow:\n \n 1. **Goal Seek**: define outcome, non-goals, risk level, completion criteria, and evidence requirements.\n-2. **grill-with-docs**: challenge the plan against `CONTEXT.md`, ADRs, code reality, terminology, boundaries, and edge scenarios. Ask one question at a time when the operator is available. If code can answer a question, inspect code instead of asking.\n+2. **grill-with-docs**: challenge the plan against `CONTEXT.md`, Shiki SADRs, target ADRs, code reality, terminology, boundaries, and edge scenarios. Ask one question at a time when the operator is available. If code can answer a question, inspect code instead of asking.\n 3. **Context & Impact**: identify modules, interfaces, seams, callers, dependencies, locks, risk, and verification surfaces.\n 4. **to-prd + Spec Freeze**: turn settled context into a PRD using domain vocabulary and testing decisions. The operator's explicit approval of the PRD is Spec Freeze, recorded as the plan's `spec_freeze` block; plans without it do not run. Steps 1-4 form Requirements Definition.\n 5. **to-issues**: decompose the PRD into independently grabbable vertical-slice issues. Prefer AFK slices; mark HITL slices when judgment is still required.\n@@ -75,7 +78,7 @@\n Before making any material change:\n \n 1. Read `AGENTS.md` and any runtime-specific wrapper such as `CLAUDE.md`.\n-2. Read `CONTEXT.md` and relevant ADRs.\n+2. Read `CONTEXT.md` and relevant SADRs and target ADRs.\n 3. Read the GitHub Issue / PR and `.shiki/` state for the active Goal or task.\n 4. Identify your role: Planner, Implementer, Reviewer, Completion Check Agent, Repairer, or Guardian-assist.\n 5. Identify Goal id, task id, branch, PR, dependencies, locks, risk level, required skills, and required evidence.\n@@ -103,7 +106,7 @@\n \n At minimum, identify:\n \n-- Relevant documents, ADRs, domain terms, and prior decisions.\n+- Relevant documents, SADRs, target ADRs, domain terms, and prior decisions.\n - Relevant modules, interfaces, seams, callers, owners, and tests.\n - Dependency relationships between tasks.\n - Candidate file locks and lock conflicts.\n@@ -152,7 +155,7 @@\n \n CCA must read:\n \n-- `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant ADRs.\n+- `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant SADRs and target ADRs.\n - Parent Goal issue, PRD issue, task issue, comments, and labels.\n - PR body, diff, commits, changed files, reviews, CI checks, and artifacts available to the workflow.\n - `.shiki/` task contract, locks, ledger entries, CCA checklist profile, and prior repair packets.\n@@ -214,7 +217,7 @@\n - Three or more new files.\n - Any high-risk or critical label.\n \n-When the architecture gate triggers, run `grill-with-docs` or `improve-codebase-architecture` as appropriate and record the decision in an ADR if it is hard to reverse or likely to be re-litigated.\n+When the architecture gate triggers, run `grill-with-docs` or `improve-codebase-architecture` as appropriate and record the decision in a Shiki SADR or target ADR as applicable if it is hard to reverse or likely to be re-litigated.\n \n ## 14. Skill Gate\n \n@@ -223,7 +226,7 @@\n Use these skills:\n \n - `setup-matt-pocock-skills`: before first use of the engineering skill set, or when issue tracker, triage labels, or domain docs are missing.\n-- `grill-with-docs`: default for non-trivial Goals before PRD/issues; required for ambiguous plans, terminology, boundaries, ADR-worthy tradeoffs, or design-tree decisions.\n+- `grill-with-docs`: default for non-trivial Goals before PRD/issues; required for ambiguous plans, terminology, boundaries, decision-record-worthy tradeoffs, or design-tree decisions.\n - `zoom-out`: unfamiliar code area or missing architectural map.\n - `to-prd`: convert settled context into a PRD.\n - `to-issues`: break a PRD, plan, or Goal into independently grabbable vertical-slice issues.\ndiff --git a/docs/adr/0001-keep-shiki-as-platform-name.md b/docs/adr/SADR-0001-keep-shiki-as-platform-name.md\nindex 20a9637c3b9d8c97de7ba8db7cfae4d82d9a7dba..8b685439959aada1b221c112f0ac3c51ca8a1695\n--- a/docs/adr/0001-keep-shiki-as-platform-name.md\n+++ b/docs/adr/SADR-0001-keep-shiki-as-platform-name.md\n@@ -1,3 +1,3 @@\n-# Keep Shiki As The Platform Name\n+# SADR-0001: Keep Shiki As The Platform Name\n \n Shiki remains the product name for the GitHub-native agentic engineering control plane. We considered more descriptive names such as MergePilot, MergeGate, and Kiroku, but kept Shiki because it carries the existing goal-seeking framework identity while remaining independent of Hermes, Codex, Claude Code, or any single runtime.\ndiff --git a/docs/adr/0002-use-agents-as-shared-constitution.md b/docs/adr/SADR-0002-use-agents-as-shared-constitution.md\nindex 73de30ecb078120eac540d9bc51855e3cd48d0e3..c5486182bf06fca36ea508496b013c67302fd989\n--- a/docs/adr/0002-use-agents-as-shared-constitution.md\n+++ b/docs/adr/SADR-0002-use-agents-as-shared-constitution.md\n@@ -1,3 +1,3 @@\n-# Use AGENTS As The Shared Agent Constitution\n+# SADR-0002: Use AGENTS As The Shared Agent Constitution\n \n Shiki uses `AGENTS.md` as the runtime-neutral constitution and `CLAUDE.md` as a thin Claude Code wrapper. This keeps Codex, Claude Code, Hermes Runner, and future Agent Runtimes aligned on the same GitHub-first MergeGate rules while still allowing runtime-specific instructions where needed.\ndiff --git a/docs/adr/0003-require-skill-gate-for-mergegate-readiness.md b/docs/adr/SADR-0003-require-skill-gate-for-mergegate-readiness.md\nindex 424be0c9af4c8f0136f1fde68a468c47ff84e764..7fc337987c7df9792f75f181dfcbed75fe5ffff2\n--- a/docs/adr/0003-require-skill-gate-for-mergegate-readiness.md\n+++ b/docs/adr/SADR-0003-require-skill-gate-for-mergegate-readiness.md\n@@ -1,3 +1,3 @@\n-# Require Skill Gate For MergeGate Readiness\n+# SADR-0003: Require Skill Gate For MergeGate Readiness\n \n Shiki requires relevant engineering skills to be invoked before execution when their triggers apply. This makes skills part of MergeGate readiness rather than optional prompt style, because Shiki depends on repeatable planning, issue decomposition, TDD, diagnosis, triage, and architecture review before autonomous or parallel agent work can be trusted.\ndiff --git a/docs/adr/0004-use-github-first-with-shiki-mirror.md b/docs/adr/SADR-0004-use-github-first-with-shiki-mirror.md\nindex d0a3a9174dc646a945a9d325954ffaf00c352040..a79c54ae9ee8eed2a0524192b864da4e22a92d3f\n--- a/docs/adr/0004-use-github-first-with-shiki-mirror.md\n+++ b/docs/adr/SADR-0004-use-github-first-with-shiki-mirror.md\n@@ -1,4 +1,4 @@\n-# ADR 0004: Use GitHub First With A Repository-Local Shiki Mirror\n+# SADR-0004: Use GitHub First With A Repository-Local Shiki Mirror\n \n ## Status\n \ndiff --git a/docs/adr/0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md b/docs/adr/SADR-0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\nindex b339baf49b89d9290e79c3d51b36e7b2bde3bd95..c30a0d8b1fde0224d0ac7820e7870dccf721580d\n--- a/docs/adr/0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\n+++ b/docs/adr/SADR-0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\n@@ -1,4 +1,4 @@\n-# ADR 0005: Use Codex As Front And Claude Code Action As The GitHub Runtime\n+# SADR-0005: Use Codex As Front And Claude Code Action As The GitHub Runtime\n \n ## Status\n \ndiff --git a/docs/adr/0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md b/docs/adr/SADR-0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\nindex 4bcae9d1e52f0a37b8e9fbafa5cd14b6ee1993bd..7a38538a6ae9929567f0ade67e7ea6a60d0b3f7e\n--- a/docs/adr/0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\n+++ b/docs/adr/SADR-0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\n@@ -1,4 +1,4 @@\n-# ADR 0006: Separate Codex Implementation, CCA Completion, And MergeGate Authorization\n+# SADR-0006: Separate Codex Implementation, CCA Completion, And MergeGate Authorization\n \n ## Status\n \ndiff --git a/docs/adr/0007-packaging-and-release.md b/docs/adr/SADR-0007-packaging-and-release.md\nindex 79b84ddb97167372b44641e5b7860e400ce80822..bb445a2e2e856d9d1f5fdbe35de8d4e8cec0fb32\n--- a/docs/adr/0007-packaging-and-release.md\n+++ b/docs/adr/SADR-0007-packaging-and-release.md\n@@ -1,4 +1,4 @@\n-# ADR 0007: Packaging And Release For Shiki\n+# SADR-0007: Packaging And Release For Shiki\n \n ## Status\n \n@@ -25,7 +25,7 @@\n   not introduce third-party runtime dependencies or break the shim.\n - Releases must run inside the operator's subscription-authenticated toolchain\n   and must not require extra secrets beyond the built-in `GITHUB_TOKEN`\n-  (consistent with ADR 0005's no-API-key-by-default stance).\n+  (consistent with SADR-0005's no-API-key-by-default stance).\n - `.shiki/manifest.json` already encodes the install surface (`install.include`,\n   `install.create_directories`, `install.exclude_from_commit`), and it must not\n   be mutated as part of this change.\n@@ -34,7 +34,7 @@\n \n This is a hard-to-reverse platform decision (it sets the version source of\n truth, the release trigger, and the public install contract), so it is recorded\n-as an ADR.\n+as a SADR.\n \n ## Decision\n \n@@ -104,7 +104,7 @@\n 3. **Publishing to PyPI.** Rejected for now: Shiki is distributed as a\n    repository template + git-installable CLI, not a PyPI library; PyPI would add\n    credentials/secrets and a different versioning contract. Can be added later as\n-   its own ADR if a packaged distribution is needed.\n+   its own SADR if a packaged distribution is needed.\n 4. **Dynamic version (read `VERSION` via a build backend `dynamic` field).**\n    Rejected to keep `pyproject.toml` backend-agnostic and dependency-free; the\n    small duplication between `VERSION` and `pyproject.toml` is guarded by the\n@@ -123,4 +123,4 @@\n   releasing versions the whole platform but ships only `install.include` to\n   targets.\n - Future PyPI distribution or a console entry point are deferred and would each\n-  warrant their own ADR.\n+  warrant their own SADR.\ndiff --git a/docs/adr/0008-use-claude-code-as-default-implementer.md b/docs/adr/SADR-0008-use-claude-code-as-default-implementer.md\nindex dedf4510b7c7799521422f622029563d373db031..35c2625d908167fbc58855b5b63e283805f167e8\n--- a/docs/adr/0008-use-claude-code-as-default-implementer.md\n+++ b/docs/adr/SADR-0008-use-claude-code-as-default-implementer.md\n@@ -1,12 +1,12 @@\n-# ADR 0008: Use Claude Code As The Default Implementer Runtime\n+# SADR-0008: Use Claude Code As The Default Implementer Runtime\n \n ## Status\n \n-Proposed (supersedes ADR 0005 for the implementer default; keeps its auth model)\n+Proposed (supersedes SADR-0005 for the implementer default; keeps its auth model)\n \n ## Context\n \n-ADR 0005 made Codex Front the default implementation runtime, with Claude Code\n+SADR-0005 made Codex Front the default implementation runtime, with Claude Code\n restricted to planner and reviewer roles by the runtime registry, and the only\n autonomous implementation adapter was `shiki runner codex`.\n \n@@ -18,13 +18,13 @@\n - The platform's planning, review, and CCA layers already run on Claude Code,\n   and Claude Code's native capabilities (the built-in `/code-review` skill and\n   the Workflow multi-agent orchestration tool) have no Codex equivalent.\n-- The Goal lifecycle redesign (ADR 0009) requires an autonomous post-freeze\n+- The Goal lifecycle redesign (SADR-0009) requires an autonomous post-freeze\n   loop driven from the operator's Claude Code session, including bounded\n   repair dispatch, which must not depend on a runtime that cannot be invoked\n   unattended.\n \n The constitution claims Shiki is runtime-agnostic (\"not a Claude-only\n-workflow\", ADR 0002). Any change to the implementer default must preserve the\n+workflow\", SADR-0002). Any change to the implementer default must preserve the\n registry-based runtime model rather than hardcoding one vendor.\n \n ## Decision\n@@ -39,7 +39,7 @@\n   the task to review or repair-needed.\n - Keep Codex registered as an optional implementer runtime. `shiki runner\n   codex`, the Codex handoff templates, and the ChatGPT OAuth auth model from\n-  ADR 0005 remain supported for tasks explicitly assigned to `codex`.\n+  SADR-0005 remain supported for tasks explicitly assigned to `codex`.\n - Commit to mandating two Claude-native capabilities as loop contract in\n   follow-up Goals (status: the code-review gate was delivered by Goal G-B \u2014\n   skill registry, checklists item PR-12, default required_skills \u2014 while the\n@@ -60,7 +60,7 @@\n \n Out of scope: removing Codex support, changing the CCA runtime (Claude Code\n Action with `CLAUDE_CODE_OAUTH_TOKEN` stays), and API-key based automation\n-(still requires its own ADR per ADR 0005).\n+(still requires its own target ADR per SADR-0005).\n \n ## Consequences\n \n@@ -100,7 +100,7 @@\n   harness rejection of direct `codex exec` is outside Shiki's control, and the\n   plan's mandatory native capabilities cannot run on Codex at all.\n - **Remove Codex entirely**: rejected; it abandons the runtime-agnostic\n-  constitution (ADR 0002) for no operational gain and forecloses\n+  constitution (SADR-0002) for no operational gain and forecloses\n   cross-runtime adversarial review.\n - **GitHub Actions cloud implementation via claude-code-action**: rejected as\n   default; durable evidence is automatic but token cost, Node 24 workflow\ndiff --git a/docs/adr/0009-requirements-definition-and-spec-freeze.md b/docs/adr/SADR-0009-requirements-definition-and-spec-freeze.md\nindex 29b3f7871bef9203a8b4a831a65c39eb2f506e42..52e7dd69ad0aa4efe0b68c62678abbf7df02aaec\n--- a/docs/adr/0009-requirements-definition-and-spec-freeze.md\n+++ b/docs/adr/SADR-0009-requirements-definition-and-spec-freeze.md\n@@ -1,4 +1,4 @@\n-# ADR 0009: Merge Grill And Planning Into Requirements Definition With Spec Freeze\n+# SADR-0009: Merge Grill And Planning Into Requirements Definition With Spec Freeze\n \n ## Status\n \ndiff --git a/docs/adr/0010-external-ai-guardian-review.md b/docs/adr/SADR-0010-external-ai-guardian-review.md\nindex e6108bbe8bb4e73e0ff4684c8f78001a80438c4e..f3510b145664298adafb151b6f0db51b972f01ec\n--- a/docs/adr/0010-external-ai-guardian-review.md\n+++ b/docs/adr/SADR-0010-external-ai-guardian-review.md\n@@ -1,4 +1,4 @@\n-# ADR 0010: External AI Guardian Review As A First-Class Approval Authority\n+# SADR-0010: External AI Guardian Review As A First-Class Approval Authority\n \n ## Status\n \n@@ -8,7 +8,7 @@\n \n Shiki is intended to be an autonomous, recursive, self-improving agent\n framework: authority-in-the-loop, not human-in-the-loop. Until now the Guardian\n-approval required for high/critical-risk merges (guardian-policy.json, ADR\n+approval required for high/critical-risk merges (guardian-policy.json, SADR\n governance) recognized only human authority sources \u2014 a GitHub review approval,\n the `guardian:approved` label applied by a configured Guardian, or a comment\n carrying `Guardian approval granted` plus the current head SHA.\n@@ -89,7 +89,7 @@\n   autonomous design the operator intends and produced the impersonation\n   incident.\n - **Record AI approval as an operator comment**: rejected as forgery of human\n-  authority \u2014 the exact failure this ADR corrects.\n+  authority \u2014 the exact failure this SADR corrects.\n - **Deliver the artifact as a committed `.shiki/` file**: rejected; a committed\n   approval inside the PR being approved is forgeable by anyone who can push.\n - **A dedicated reviewer bot identity instead of a relayed comment**: deferred;\ndiff --git a/docs/adr/0011-autonomous-quality-gate-evidence.md b/docs/adr/SADR-0011-autonomous-quality-gate-evidence.md\nindex bca0246c5d46e44afbde15486a75eec425fc02f5..9c93eac16d8c3a6d4b11bd7224e3955102cd7db4\n--- a/docs/adr/0011-autonomous-quality-gate-evidence.md\n+++ b/docs/adr/SADR-0011-autonomous-quality-gate-evidence.md\n@@ -1,4 +1,4 @@\n-# ADR 0011: Autonomous Quality-Gate Evidence \u2014 Deterministic Facts And An Independent Verifier\n+# SADR-0011: Autonomous Quality-Gate Evidence \u2014 Deterministic Facts And An Independent Verifier\n \n ## Status\n \ndiff --git a/docs/adr/0012-autonomous-loop-closeout-to-main.md b/docs/adr/SADR-0012-autonomous-loop-closeout-to-main.md\nindex 637fad6dde4989c9c2bf0f81f676d8d29991123c..ec7d85f81030a676bcbba28b43eb365422a36eac\n--- a/docs/adr/0012-autonomous-loop-closeout-to-main.md\n+++ b/docs/adr/SADR-0012-autonomous-loop-closeout-to-main.md\n@@ -1,4 +1,4 @@\n-# 0012 \u2014 Autonomous loop pushes goal completion to main via a closeout PR\n+# SADR-0012 \u2014 Autonomous loop pushes goal completion to main via a closeout PR\n \n - Status: accepted\n - Date: 2026-06-18\ndiff --git a/docs/adr/0013-bootstrap-grants-actions-approve-pull-requests.md b/docs/adr/SADR-0013-bootstrap-grants-actions-approve-pull-requests.md\nindex b6c69d64f1c5ced346b88d6859586715b3b5bd0e..cf48ff4a6716267f539ca58edd1a852e7c7a2d64\n--- a/docs/adr/0013-bootstrap-grants-actions-approve-pull-requests.md\n+++ b/docs/adr/SADR-0013-bootstrap-grants-actions-approve-pull-requests.md\n@@ -1,4 +1,4 @@\n-# ADR 0013: Bootstrap Grants GitHub Actions Permission To Create And Approve Pull Requests\n+# SADR-0013: Bootstrap Grants GitHub Actions Permission To Create And Approve Pull Requests\n \n ## Status\n \n@@ -31,7 +31,7 @@\n security tradeoff: it lets repository automation satisfy a human-review gate. In\n a multi-maintainer repo that could let a compromised or buggy workflow\n self-approve changes. So whether bootstrap turns it on by default \u2014 and with\n-what default token scope \u2014 is an ADR-worthy decision, not an implementation\n+what default token scope \u2014 is a SADR-worthy decision, not an implementation\n detail.\n \n ## Decision\n@@ -48,7 +48,7 @@\n Specifically:\n \n - **`can_approve_pull_request_reviews` defaults to `true`.** Shiki's default\n-  posture is solo/AFK autonomous operation (ADR 0008/0009/0012), where the CCA\n+  posture is solo/AFK autonomous operation (SADR-0008/0009/0012), where the CCA\n   Review Bridge is the only path that can satisfy `required_review: true` after\n   the CCA and Guardian gates have already passed. The default makes a\n   freshly-bootstrapped repo able to run the autonomous loop end to end without a\ndiff --git a/docs/adr/0014-codex-app-external-ai-guardian-ui-adapter.md b/docs/adr/SADR-0014-codex-app-external-ai-guardian-ui-adapter.md\nindex 238db06279334caa341c117362df9271382a7fcd..65f12e9939321e1b3e389a34c822fc1549e5155d\n--- a/docs/adr/0014-codex-app-external-ai-guardian-ui-adapter.md\n+++ b/docs/adr/SADR-0014-codex-app-external-ai-guardian-ui-adapter.md\n@@ -1,4 +1,4 @@\n-# ADR 0014: Use Codex App As The External AI Guardian UI Adapter\n+# SADR-0014: Use Codex App As The External AI Guardian UI Adapter\n \n ## Status\n \ndiff --git a/docs/adr/0015-contract-approval-before-dispatch.md b/docs/adr/SADR-0015-contract-approval-before-dispatch.md\nindex fad6c656741a20a7684ba16080a7db159f63a52f..6061271a46d3f1d6ae7174477de3fb75673123a6\n--- a/docs/adr/0015-contract-approval-before-dispatch.md\n+++ b/docs/adr/SADR-0015-contract-approval-before-dispatch.md\n@@ -1,4 +1,4 @@\n-# ADR 0015: Approve The Task Contract Before Dispatch, Not The Diff After It\n+# SADR-0015: Approve The Task Contract Before Dispatch, Not The Diff After It\n \n ## Status\n \n@@ -9,7 +9,7 @@\n Guardian approval for high/critical risk is expressible only on a pull request.\n Every approval source in `guardian-policy.json` \u2014 GitHub review, the\n `guardian:approved` label, the `Guardian approval granted` comment, and the\n-`external_ai_guardian_review` artifact (ADR 0010/0014) \u2014 is bound to a PR, and\n+`external_ai_guardian_review` artifact (SADR-0010/0014) \u2014 is bound to a PR, and\n three of the four require the current head SHA. Approval therefore cannot exist\n before an implementation PR exists.\n \n@@ -81,7 +81,7 @@\n An envelope breach must surface only on the non-repairable policy gate, never on\n the repairable `MergeGate metadata check`. Otherwise the Goal Loop routes it to\n `dispatch_repair` \u2014 instructing a runner to make the breach go away \u2014 which is\n-exactly what this ADR forbids.\n+exactly what this SADR forbids.\n \n **Contract immutability.** After registration, an implementation PR must not\n change the governance fields of its own task contract (`scope`, `non_goals`,\n@@ -116,7 +116,7 @@\n fails any verdict that is not `complete`, and the signal the CCA prompt reads is\n computed from `evaluate_guardian_approval` alone. Without the OR-merge at the\n signal, a contract-approved PR still returns `needs_guardian`, the CCA job fails,\n-and the MergeGate policy check never runs \u2014 the gate this ADR changes would never\n+and the MergeGate policy check never runs \u2014 the gate this SADR changes would never\n be reached. The CCA *prompt* needs no change: a `contract_approval` entry in the\n signal's existing `sources` array satisfies its current wording.\n \n@@ -130,7 +130,7 @@\n written, and an approved contract lets its implementation PRs merge through the\n same automatic path as low/medium risk. The wasted implement-then-block cycle\n disappears. Guardian review shifts from reading diffs to reading contracts, which\n-is both earlier and smaller; the external AI Guardian path (ADR 0010/0014) works\n+is both earlier and smaller; the external AI Guardian path (SADR-0010/0014) works\n on contracts with no change.\n \n We accept that the Guardian authorizes a class of change without seeing its\n@@ -146,7 +146,7 @@\n adversarial tests for the new approval path equivalent in rigor to\n `test_shiki_governance_evidence.sh`.\n \n-Bootstrap costs, which apply only while this ADR is being implemented. The tasks\n+Bootstrap costs, which apply only while this SADR is being implemented. The tasks\n that build Contract Approval merge under the existing rules, so the ones\n dispatched after the pre-dispatch gate lands but before the approval path lands\n need a Guardian-approved Contract PR *and* a live Guardian approval on their\n@@ -210,7 +210,7 @@\n risk; they do not bound authorized work.\n \n **Auto-escalate risk on envelope breach instead of stopping.** Returns the\n-workflow to a diff-time Guardian decision \u2014 the exact behavior this ADR removes.\n+workflow to a diff-time Guardian decision \u2014 the exact behavior this SADR removes.\n \n **Resolve governance fields from the base snapshot only.** Rejected against the\n registration flow: implementation PRs add their own contract, so the base has no\ndiff --git a/docs/adr/0016-locks-bound-product-paths.md b/docs/adr/SADR-0016-locks-bound-product-paths.md\nindex b5e850f3f1f7f4ef6e7f9d1763a29a51bd300c45..46b40a9b99a7aae644c02bb458f5473ff0a90ce2\n--- a/docs/adr/0016-locks-bound-product-paths.md\n+++ b/docs/adr/SADR-0016-locks-bound-product-paths.md\n@@ -1,4 +1,4 @@\n-# ADR 0016: Locks bound product paths; the Shiki mirror is bound by record identity\n+# SADR-0016: Locks bound product paths; the Shiki mirror is bound by record identity\n \n ## Status\n \n@@ -6,8 +6,8 @@\n \n - Date: 2026-07-28\n - Deciders: operator (mizutani-140), Claude (planner)\n-- Related: ADR 0004 (GitHub-first with a `.shiki` mirror), ADR 0009 (Spec Freeze),\n-  ADR 0012 (autonomous loop closeout to main), ADR 0015 (Contract Approval before\n+- Related: SADR-0004 (GitHub-first with a `.shiki` mirror), SADR-0009 (Spec Freeze),\n+  SADR-0012 (autonomous loop closeout to main), SADR-0015 (Contract Approval before\n   dispatch), remediation goal task R-02, the live PR #179 deadlock.\n \n ## Context\n@@ -90,11 +90,11 @@\n files, documentation files and named source modules are nameable; runtime-\n allocated ledger, runner and report ids are not \u2014 and those live in `.shiki`,\n which locks no longer govern. Existing frozen contracts are brought into line by\n-a one-time Spec Amendment per plan, which must land before ADR 0015's contract\n+a one-time Spec Amendment per plan, which must land before SADR-0015's contract\n immutability makes `locks` unwritable from the implementation PR.\n \n **Lock state is resolved from the durable side.** MergeGate resolves the lock\n-registry, and ADR 0015's governance fields, from the base `.shiki` snapshot when\n+registry, and SADR-0015's governance fields, from the base `.shiki` snapshot when\n present, falling back to head only when absent. The `MergeGate metadata check`\n job is given `--base-shiki`, which it does not receive today. Locks are not\n derived from the live open-pull-request set: a pull request body is untrusted\n@@ -108,7 +108,7 @@\n Explicitly out of scope: moving MergeGate's contract resolution off the PR\n checkout wholesale; making the mirror a projection of GitHub state; introducing\n new lock kinds; moving ledger evidence out of the repository; and unwinding\n-ADR 0012's closeout pull request. Registration of multi-task goals is ADR 0015's\n+SADR-0012's closeout pull request. Registration of multi-task goals is SADR-0015's\n Contract PR and is not re-decided here.\n \n ## Consequences\n@@ -127,7 +127,7 @@\n \n Risks accepted. First, goal-scoped identity is only as strong as `goal_id`,\n which is resolved from the PR body and the head task file; the mitigation is to\n-add `goal_id` to ADR 0015's frozen governance field set, and until that lands a\n+add `goal_id` to SADR-0015's frozen governance field set, and until that lands a\n task file can name a foreign goal. Second, base-when-present resolution is\n unavailable on fork pull requests, where the head is contributor-controlled;\n fork pull requests already cannot complete a Shiki merge, and this decision does\n@@ -139,7 +139,7 @@\n path, not by implementation pull requests.\n \n New obligations: one Spec Amendment per frozen plan to rewrite `locks`, ordered\n-before ADR 0015 T1; extension of the adversarial suites in\n+before SADR-0015 T1; extension of the adversarial suites in\n `scripts/test_shiki_adversarial_state.sh` and\n `scripts/test_shiki_governance_evidence.sh` to cover the eleven newly-scoped\n directories; and updating `scripts/test_shiki_loop_lock_guard.sh`,\n@@ -151,32 +151,32 @@\n whether the loop's transitive sync of ledger `evidence` references can be\n deleted once the Contract PR carries its own source plan.\n \n-## Relationship to ADR 0004\n+## Relationship to SADR-0004\n \n-This ADR **clarifies something ADR 0004 left unspecified**. ADR 0004 decides that\n+This SADR **clarifies something SADR-0004 left unspecified**. SADR-0004 decides that\n GitHub is the operational source of truth and that each target repository keeps a\n `.shiki/` mirror. It says nothing about pull requests writing the mirror, does\n not name MergeGate, and does not make the mirror an input to a gate. Nothing in\n-ADR 0004's Decision or Consequences is amended, superseded or weakened: the\n+SADR-0004's Decision or Consequences is amended, superseded or weakened: the\n mirror still exists, still records the same artifacts, and pull request\n-boundaries remain the coordination surface. What this ADR settles is the question\n+boundaries remain the coordination surface. What this SADR settles is the question\n 0004 never reached \u2014 by what authority a pull request may write a mirror record \u2014\n and it answers it the way `.shiki/manifest.json` already declares\n (`pr_mutation: current-task-or-current-goal-only`).\n \n-It **amends ADR 0012**, whose Decision item 5 states that \"Loop-executed tasks\n+It **amends SADR-0012**, whose Decision item 5 states that \"Loop-executed tasks\n declare the `path:.shiki/**` lock, which covers every `.shiki` file the closeout\n stages ... so `files_outside_locks` is satisfied without per-file lock\n bookkeeping.\" That clause is replaced: the closeout's mirror writes are\n-authorized by record identity, not by a declared lock. ADR 0012's substantive\n+authorized by record identity, not by a declared lock. SADR-0012's substantive\n decision \u2014 that completion reaches `main` through a closeout pull request rather\n than local attestation \u2014 is preserved unchanged.\n \n-It is **complementary to ADR 0015**, not a substitute. ADR 0015 owns contract\n+It is **complementary to SADR-0015**, not a substitute. SADR-0015 owns contract\n immutability, the pre-dispatch gate, the Contract PR and the Scope Envelope. This\n-ADR owns what a lock means and who may write the mirror. The one ordering\n+SADR owns what a lock means and who may write the mirror. The one ordering\n constraint between them is that the lock-narrowing Spec Amendment must precede\n-ADR 0015 T1's freezing of the `locks` field.\n+SADR-0015 T1's freezing of the `locks` field.\n \n ## Alternatives Considered\n \n@@ -186,7 +186,7 @@\n the only writer that can register them may run. Migrating the Guardian signal to\n that store makes every registration PR fail closed to `needs_guardian` at every\n risk level, because the task file is by definition absent from the store, which\n-removes the low/medium autonomous path ADR 0008/0009 exists to provide. And it\n+removes the low/medium autonomous path SADR-0008/0009 exists to provide. And it\n leaves schemas, manifest, config, guardian policy, templates, migrations, plans,\n worktrees, runner records, runs and handoffs with no writer at all in a\n repository whose product is `.shiki`. Measured benefit on the frozen backlog:\n@@ -199,14 +199,14 @@\n namespace is not in fact ownership-partitioned \u2014 56 ledger entries carry no\n `task_id`, and one goal DAG is writable by 35 registered tasks. The correct\n expression of its insight is that locks do not govern the mirror at all, which is\n-what this ADR decides.\n+what this SADR decides.\n \n **Make the mirror a projection written only by a registration writer and a\n post-merge projection job.** Rejected. It requires a bypass actor with write\n access to the protected default branch; it makes its own master invariant\n readable from a manifest that pull requests may edit; it deletes both reconcile\n modes and leaves a corrupted mirror with no in-band repair path, contradicting\n-ADR 0004's consequence that mirror conflicts are repaired against GitHub state;\n+SADR-0004's consequence that mirror conflicts are repaired against GitHub state;\n and its content-derived ids are rejected by `validate_shiki`'s currently-green id\n pattern.\n \ndiff --git a/docs/adr/0017-bookkeeping-closeouts-do-not-inherit-risk.md b/docs/adr/SADR-0017-bookkeeping-closeouts-do-not-inherit-risk.md\nindex 554e3a0d526a72ca067108c5464cc68c3edcf174..b9676857a4713850e063af7c39c8e0b01ecb4c66\n--- a/docs/adr/0017-bookkeeping-closeouts-do-not-inherit-risk.md\n+++ b/docs/adr/SADR-0017-bookkeeping-closeouts-do-not-inherit-risk.md\n@@ -1,4 +1,4 @@\n-# ADR 0017: A Bookkeeping Closeout Does Not Inherit Its Task's Risk\n+# SADR-0017: A Bookkeeping Closeout Does Not Inherit Its Task's Risk\n \n ## Status\n \n@@ -6,7 +6,7 @@\n \n ## Context\n \n-ADR 0012 made the goal loop push a task's completion to the default branch through a\n+SADR-0012 made the goal loop push a task's completion to the default branch through a\n closeout pull request: after the implementation PR merges, the loop opens a second PR\n carrying `task.status = done`, `lock.state = released`, and \u2014 when the task completes\n its goal \u2014 `goal.status = complete` plus one scorecard report. Nothing else. The\n@@ -49,14 +49,14 @@\n 4. The task file's governance fields (`scope`, `non_goals`, `required_skills`,\n    `risk_level`, `locks`, `acceptance_checks`, `test_command`) are byte-identical to\n    the base snapshot. Only `status`, `expected_pr`, `closeout_pr`, `ledger_evidence`\n-   and `expected_branch` may differ \u2014 the ADR 0015 contract-immutability rule,\n+   and `expected_branch` may differ \u2014 the SADR-0015 contract-immutability rule,\n    unchanged.\n \n    `expected_branch` is in that set because a closeout is *by construction* on a\n    different branch from the implementation it closes, and the loop rewrites the\n    field accordingly. It is safe to let it differ because MergeGate independently\n    requires `expected_branch` to equal the PR's actual head ref, so a PR cannot set\n-   it to anything other than the branch it is really on. The first draft of this ADR\n+   it to anything other than the branch it is really on. The first draft of this SADR\n    omitted it, which made the exemption unreachable: every real closeout failed\n    condition 4 on a field the loop always changes. See PR #209.\n 5. The state transitions are exactly the terminal set: task `review` \u2192 `done`, lock\n@@ -113,12 +113,12 @@\n classification must be derived, not asserted.\n \n **Fold the closeout into the implementation PR** so no second PR exists. Rejected under\n-ADR 0012, which recorded why: a task file that is already `done` on the implementation\n+SADR-0012, which recorded why: a task file that is already `done` on the implementation\n branch short-circuits the loop's review \u2192 CCA \u2192 merge \u2192 done ordering into a local\n goal-completion before any merge happens.\n \n **Have the loop auto-merge closeouts with elevated credentials.** Rejected outright: that\n-is a runtime authoring its own approval, which ADR 0010 and the constitution's\n+is a runtime authoring its own approval, which SADR-0010 and the constitution's\n approval-record prohibition forbid. It would also be indistinguishable, in the evidence,\n from the forgery those rules exist to prevent.\n \ndiff --git a/docs/adr/0018-carried-guardian-approval-across-a-proven-base-sync.md b/docs/adr/SADR-0018-carried-guardian-approval-across-a-proven-base-sync.md\nindex 22358562da390ed8b45cd600da08a43d378b4680..e6e2907cefa776460af73ddf7d39db605f3c39b0\n--- a/docs/adr/0018-carried-guardian-approval-across-a-proven-base-sync.md\n+++ b/docs/adr/SADR-0018-carried-guardian-approval-across-a-proven-base-sync.md\n@@ -1,4 +1,4 @@\n-# ADR 0018: Carried Guardian Approval Across A Proven Base Sync\n+# SADR-0018: Carried Guardian Approval Across A Proven Base Sync\n \n ## Status\n \n@@ -34,7 +34,7 @@\n Guardian evaluator that consumes it (`shiki_guardian.evaluate_guardian_approval`,\n the `guardian_comment_carried` source) were built and tested in isolation. Neither\n does anything until a caller passes the flags that turn the carry on, so the\n-feature was invisible in CI. This ADR records the decision the two of them\n+feature was invisible in CI. This SADR records the decision the two of them\n implement, and this task wires it into the four CI invocations that judge Guardian\n approval.\n \ndiff --git a/docs/adr/README.md b/docs/adr/README.md\nindex c70e057a4520c36e042ab68c53cd4a21ef709b71..5da43856836e2ce6a150f8127766e2f88355ab4b\n--- a/docs/adr/README.md\n+++ b/docs/adr/README.md\n@@ -1,40 +1,41 @@\n-# Architecture Decision Records\n+# Decision Record Namespaces\n \n-This directory holds Shiki's Architecture Decision Records (ADRs): durable\n-records of hard-to-reverse platform decisions. ADRs are part of the Shiki source\n-of truth alongside GitHub state, the `.shiki/` mirror, and `CONTEXT.md`.\n+This directory holds two independent decision namespaces:\n \n-## How ADRs Work\n+- Shiki Architecture Decision Records (SADRs), named\n+  `SADR-NNNN-<short-slug>.md`, record hard-to-reverse Shiki platform decisions.\n+- Target Architecture Decision Records (ADRs), named\n+  `NNNN-<short-slug>.md`, belong to the target product.\n \n-- New ADRs are added as `docs/adr/NNNN-<short-slug>.md`, where `NNNN` is the next\n-  zero-padded sequence number.\n-- The **canonical list of ADRs is the set of files matching `docs/adr/*.md`** in\n-  this directory. Browse `docs/adr/*.md` for the authoritative, current set.\n-- Use [`template.md`](template.md) as the starting point for a new ADR.\n-- Record an ADR when a decision is hard to reverse, surprising without context,\n-  and a real tradeoff. Do not write an ADR for routine or easily reversible\n-  choices.\n+The same four-digit number may appear once in each namespace. A platform\n+`SADR-NNNN` citation resolves only to `SADR-NNNN-*.md`; a target `ADR NNNN`\n+citation resolves only to `NNNN-*.md`.\n \n-Using the `docs/adr/*.md` glob as the source of truth means other PRs can add an\n-ADR without editing this index file, avoiding merge contention on a\n-hand-maintained enumeration.\n+## How Decision Records Work\n \n-## Current ADRs (snapshot)\n-\n-The list below is a convenience snapshot and may lag behind the directory. When\n-in doubt, list `docs/adr/*.md`.\n+- New Shiki platform decisions use the next zero-padded SADR number.\n+- New target product decisions use the target's own ADR sequence.\n+- The canonical list is the files in `docs/adr/`; no hand-maintained index is\n+  authoritative.\n+- Use [`template.md`](template.md) as the starting point and choose the filename\n+  namespace that owns the decision.\n+- Record a decision when it is hard to reverse, surprising without context, and\n+  a real tradeoff. Do not write a decision record for routine or easily\n+  reversible choices.\n \n-- [0001 \u2014 Keep Shiki As The Platform Name](0001-keep-shiki-as-platform-name.md)\n-- [0002 \u2014 Use AGENTS As The Shared Agent Constitution](0002-use-agents-as-shared-constitution.md)\n-- [0003 \u2014 Require Skill Gate For MergeGate Readiness](0003-require-skill-gate-for-mergegate-readiness.md)\n-- [0004 \u2014 Use GitHub First With A Repository-Local Shiki Mirror](0004-use-github-first-with-shiki-mirror.md)\n-- [0005 \u2014 Use Codex As Front And Claude Code Action As The GitHub Runtime](0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md)\n-- [0006 \u2014 Separate Codex Implementation, CCA Completion, And MergeGate Authorization](0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md)\n+Using the directory as the source of truth lets independent changes add records\n+without contending on an enumerated index.\n \n-## Adding A New ADR\n+## Adding A Shiki SADR\n \n-1. Copy `template.md` to `docs/adr/NNNN-<short-slug>.md`.\n+1. Copy `template.md` to `docs/adr/SADR-NNNN-<short-slug>.md`.\n 2. Fill in the title, status, context, decision, and consequences.\n-3. Set the status to `Proposed`, then `Accepted` once the decision is approved.\n-4. Open a PR following `CONTRIBUTING.md`. You do not need to edit this index for\n-   the ADR to be discoverable via `docs/adr/*.md`.\n+3. Set the status to `Proposed`, then `Accepted` once the proper authority has\n+   approved the decision.\n+4. Open a PR following `CONTRIBUTING.md`.\n+\n+## Adding A Target ADR\n+\n+1. Copy `template.md` to `docs/adr/NNNN-<short-slug>.md` using the target's own\n+   sequence.\n+2. Cite it as `ADR NNNN`; do not prefix its filename with `SADR-`.\ndiff --git a/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md b/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\nnew file mode 100644\nindex 0000000000000000000000000000000000000000..7455ce1f3bed02ddddb851ac21a1d1a648c9aeba\n--- /dev/null\n+++ b/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\n@@ -0,0 +1,55 @@\n+# SADR-0019: Separate Shiki Platform SADRs From Target ADRs\n+\n+## Status\n+\n+Accepted\n+\n+## Context\n+\n+Shiki installs its platform decision records into `docs/adr/` in every target\n+repository. Shiki and its targets previously used the same numeric\n+`NNNN-<slug>.md` namespace. A target that already owned ADRs 0013 through 0018\n+therefore acquired two records for each number during installation, making an\n+`ADR NNNN` citation ambiguous. Renumbering target decisions would rewrite target\n+history, while renumbering Shiki decisions per target would destroy their stable\n+platform identity.\n+\n+Upgrades also need to remove the former Shiki-owned numeric paths without ever\n+mistaking a target-owned ADR for platform content. The install stamp already\n+records exact shipped paths and content digests, so it can prove ownership only\n+when both still match.\n+\n+## Decision\n+\n+Shiki platform decisions use the `SADR` namespace:\n+\n+- records are named `docs/adr/SADR-NNNN-<slug>.md`;\n+- live platform citations use `SADR-NNNN`;\n+- target product decisions retain `docs/adr/NNNN-<slug>.md` and `ADR NNNN`;\n+- the reference validator resolves the two namespaces independently, so the same\n+  four-digit number may exist once in each namespace.\n+\n+Before a forced upgrade writes anything, the installer inventories the exact\n+former numeric Shiki ADR paths. It may delete a legacy path only when the\n+existing install stamp contains that exact path and its recorded SHA-256 digest\n+matches the file currently on disk. All ownership blockers are reported\n+together and abort the upgrade before its first write. A non-forced install\n+never performs this cleanup.\n+\n+Historical `.shiki/` evidence remains unchanged because it records the namespace\n+that existed when that evidence was produced.\n+\n+## Consequences\n+\n+- Targets may use any ADR number without colliding with Shiki's platform SADRs.\n+- Fresh installs preserve target ADR bytes and install a stable SADR set.\n+- Forced upgrades fail closed when legacy ownership cannot be proven, and a\n+  successful upgrade refreshes the stamp without removed legacy paths.\n+- Current governance, packaging, workflow, prompt, script, and skill references\n+  must move with the platform records.\n+\n+## Alternatives Considered\n+\n+- Collision-only refusal leaves affected targets permanently unupgradable.\n+- Renumbering target ADRs mutates target-owned history.\n+- Per-target Shiki renumbering makes platform citations non-portable.\ndiff --git a/docs/adr/template.md b/docs/adr/template.md\nindex a92a97daddd0b00ab9dd626cc8d86942da2eafed..17dd79175b5fee2105935557f691b40cf9492942\n--- a/docs/adr/template.md\n+++ b/docs/adr/template.md\n@@ -1,14 +1,14 @@\n-# NNNN: <Short Decision Title>\n+# <SADR-NNNN or NNNN>: <Short Decision Title>\n \n ## Status\n \n-Proposed | Accepted | Superseded by ADR-NNNN\n+Proposed | Accepted | Superseded by SADR-NNNN or target ADR NNNN\n \n ## Context\n \n What is the situation, constraint, or force that requires a decision? Reference\n-relevant domain terms (`CONTEXT.md`), prior ADRs, and code reality. State the\n-problem, not the solution.\n+relevant domain terms (`CONTEXT.md`), prior Shiki SADRs or target ADRs, and code\n+reality. State the problem, not the solution.\n \n ## Decision\n \ndiff --git a/docs/agents/autonomous-loop-e2e-acceptance.md b/docs/agents/autonomous-loop-e2e-acceptance.md\nindex bbf84c6081987f159ac6ad9ecf3c82b7aa72e7e8..5c13c1d02a05a90b0dc46cbef7c95a9846d0c237\n--- a/docs/agents/autonomous-loop-e2e-acceptance.md\n+++ b/docs/agents/autonomous-loop-e2e-acceptance.md\n@@ -40,8 +40,8 @@\n 3. `gh auth status` is logged in with merge permission on the repo.\n 4. Branch protection + required checks are configured exactly as the goal-loop\n    required-check set expects (see `docs/agents/branch-protection-smoke.md`).\n-5. The repo `.shiki/policy` permits autonomous low/medium-risk merges (ADR\n-   0008/0009); the MergeGate policy check is the recorded-authority gate.\n+5. The repo `.shiki/policy` permits autonomous low/medium-risk merges\n+   (SADR-0008/0009); the MergeGate policy check is the recorded-authority gate.\n \n The operator's only actions are: register the task and start the loop. **No\n manual pre-clearing** means after `shiki loop run` starts, the operator does not\ndiff --git a/docs/agents/checklists.md b/docs/agents/checklists.md\nindex 4a7f03ade346f53a1c1d82981f3fd744f9ab4547..0c5ec6c53dc7197b7b7a7d18e667b325babd6c45\n--- a/docs/agents/checklists.md\n+++ b/docs/agents/checklists.md\n@@ -32,18 +32,18 @@\n | ID | Item | Blocking |\n | --- | --- | --- |\n | GD-01 | Plan was challenged against `CONTEXT.md`. | blocking for non-trivial Goals |\n-| GD-02 | Relevant ADRs were read or absence was recorded. | blocking for non-trivial Goals |\n+| GD-02 | Relevant SADRs and target ADRs were read or absence was recorded. | blocking for non-trivial Goals |\n | GD-03 | Ambiguous terms were resolved or converted to HITL blockers. | blocking |\n | GD-04 | Edge scenarios were considered. | advisory |\n | GD-05 | Code reality was checked when code could answer. | blocking when applicable |\n | GD-06 | New domain terms were added to `CONTEXT.md` when needed. | blocking when applicable |\n-| GD-07 | ADR was created or explicitly skipped using the ADR criteria. | blocking when applicable |\n+| GD-07 | A SADR or target ADR was created or explicitly skipped using the decision-record criteria. | blocking when applicable |\n \n ## CI \u2014 Context & Impact Checklist\n \n | ID | Item | Blocking |\n | --- | --- | --- |\n-| CI-01 | Relevant docs and ADRs identified. | blocking |\n+| CI-01 | Relevant docs, SADRs, and target ADRs identified. | blocking |\n | CI-02 | Relevant modules, interfaces, seams, callers, and tests identified. | blocking |\n | CI-03 | Dependencies and blocked-by relationships identified. | blocking |\n | CI-04 | Candidate locks identified. | blocking |\n@@ -62,7 +62,7 @@\n | PRD-04 | Implementation decisions are recorded without volatile code detail. | blocking |\n | PRD-05 | Testing decisions are recorded. | blocking |\n | PRD-06 | Out-of-scope items are explicit. | blocking |\n-| PRD-07 | Relevant glossary/ADR links are present when applicable. | blocking |\n+| PRD-07 | Relevant glossary/SADR/target-ADR links are present when applicable. | blocking |\n \n ## SF \u2014 Spec Freeze Checklist\n \n@@ -98,7 +98,7 @@\n | H-02 | Branch/worktree target is present. | blocking |\n | H-03 | Scope and non-goals are present. | blocking |\n | H-04 | Dependencies and locks are present. | blocking |\n-| H-05 | Relevant docs, ADRs, modules, and tests are referenced. | blocking |\n+| H-05 | Relevant docs, SADRs, target ADRs, modules, and tests are referenced. | blocking |\n | H-06 | Required skills are stated. | blocking |\n | H-07 | TDD expectations are stated. | blocking when behavior work |\n | H-08 | Acceptance criteria are included. | blocking |\ndiff --git a/docs/agents/codex-handoff.md b/docs/agents/codex-handoff.md\nindex ede468f47cb277f69a857e8c45df703e4ed0151d..6f97bdffb67e44d7edea2c71e01ea28933c4eb6c\n--- a/docs/agents/codex-handoff.md\n+++ b/docs/agents/codex-handoff.md\n@@ -46,7 +46,7 @@\n \n ## Context\n - Domain terms:\n-- Relevant ADRs:\n+- Relevant SADRs and target ADRs:\n - Relevant docs:\n - Relevant modules/interfaces/seams:\n - Similar tests/prior art:\ndiff --git a/docs/agents/completion-check-agent.md b/docs/agents/completion-check-agent.md\nindex 13e4c035db35025f4e3cdcc5adfb24314ac4d521..61bfa1218cf36ca605250c91919e620f33238aa0\n--- a/docs/agents/completion-check-agent.md\n+++ b/docs/agents/completion-check-agent.md\n@@ -32,7 +32,7 @@\n - `AGENTS.md`\n - `CLAUDE.md`\n - `CONTEXT.md`\n-- relevant ADRs\n+- relevant SADRs and target ADRs\n - parent Goal issue\n - PRD issue or document\n - task issue\ndiff --git a/docs/agents/decision-control.md b/docs/agents/decision-control.md\nindex 7d8ee5ab03a5ccbcbd04056ae61de03785a2a9c9..8c94ecda00fffaa0e92c325ab83aa9776bd1dac1\n--- a/docs/agents/decision-control.md\n+++ b/docs/agents/decision-control.md\n@@ -11,7 +11,7 @@\n | Decision | Owner | When | Required inputs | Output | Enforcement surface |\n | --- | --- | --- | --- | --- | --- |\n | Is the Goal clear enough to plan? | Planner + Guardian when needed | Goal intake | User request, existing docs, risk signals | Goal readiness verdict | Goal checklist, issue template, ledger |\n-| Must `grill-with-docs` run? | Planner / Skill Gate | Before PRD or issue split | Ambiguity, domain terms, ADR conflicts, unclear boundaries | Skill invocation or explicit skip rationale | Skill Gate checklist |\n+| Must `grill-with-docs` run? | Planner / Skill Gate | Before PRD or issue split | Ambiguity, domain terms, SADR/target-ADR conflicts, unclear boundaries | Skill invocation or explicit skip rationale | Skill Gate checklist |\n | Is a PRD required? | Planner | After Goal context stabilizes | Goal, `grill-with-docs` output, Context & Impact output | PRD required / not required | PRD checklist, CCA evidence check |\n | Can the spec be frozen? | Operator only | End of Requirements Definition, before issue split | PRD/requirements summary, scope inventory (SF-02), open-question list | `spec_freeze` block in the plan + ledger evidence | `require_grilled_plan` (plan ingest/run), SF checklist |\n | Can a frozen spec change? | Operator only (Spec Amendment) | When implementation contradicts the frozen spec | Contested decisions, scoped re-grill result | recorded amendment + re-stamped freeze | SF checklist, ledger evidence |\ndiff --git a/docs/agents/domain.md b/docs/agents/domain.md\nindex 258da8ea81e87ac0a6b0b5b5e5c2fde980eaabb7..d57b0cf17ff3e1fa9e6e5885372bf083d8295421\n--- a/docs/agents/domain.md\n+++ b/docs/agents/domain.md\n@@ -5,7 +5,9 @@\n ## Layout\n \n - `CONTEXT.md` defines Shiki language. Keep it implementation-free.\n-- `docs/adr/` records hard-to-reverse decisions.\n+- `docs/adr/SADR-NNNN-*.md` records Shiki platform decisions.\n+- `docs/adr/NNNN-*.md` records target product decisions without sharing the\n+  SADR namespace.\n - `AGENTS.md` is the runtime-neutral agent constitution.\n - `CLAUDE.md` is the Claude Code wrapper around `AGENTS.md`.\n - `.shiki/` is the Target Repository mirror for Goals, PRDs, plans, Task DAGs, contracts, locks, worktree records, ledger entries, CCA verdicts, repair packets, reports, and handoffs.\n@@ -15,6 +17,6 @@\n \n - Use Shiki terms exactly when they exist.\n - Add glossary terms to `CONTEXT.md` only when the term is part of the domain, not a generic programming concept.\n-- Create ADRs only for hard-to-reverse, surprising, tradeoff-based decisions.\n+- Create SADRs or target ADRs only for hard-to-reverse, surprising, tradeoff-based decisions.\n - Keep product-specific language in the Target Repository, not in Shiki Template docs.\n-- If an ADR conflicts with a proposed implementation, surface the conflict before editing.\n+- If a SADR or target ADR conflicts with a proposed implementation, surface the conflict before editing.\ndiff --git a/docs/agents/execution-control.md b/docs/agents/execution-control.md\nindex ad8ee3cdb36be56fedc1f46408ca92b387d74047..bc63c3a73661d12b567016ba299f1a6b66bf9088\n--- a/docs/agents/execution-control.md\n+++ b/docs/agents/execution-control.md\n@@ -63,7 +63,7 @@\n | Branch name | Orchestrator | Orchestrator | Before implementer dispatch | branch policy | branch ref in task/ledger |\n | Lock reservation | Context & Impact | MergeGate | Before dispatch and before merge | `.shiki/locks/`, labels, PR metadata | lock id, owner, scope |\n | CI profile | Planner | MergeGate | During issue triage, rechecked at PR | labels, `.shiki/policy.yaml`, workflow matrix | required check list |\n-| Required status checks | Repo maintainer / Guardian | GitHub branch protection / rulesets | Repository setup, updated via ADR | GitHub settings | branch/ruleset config, ADR |\n+| Required status checks | Repo maintainer / Guardian | GitHub branch protection / rulesets | Repository setup, updated via target ADR | GitHub settings | branch/ruleset config, target ADR |\n | CCA judgment | CCA | GitHub required check / MergeGate | Every non-draft PR update | GitHub Action check | structured CCA verdict |\n | Repair assignment | CCA | MergeGate | After failed CCA/CI/review | repair packet, issue/PR comment | repair id and attempt count |\n | Merge readiness | MergeGate | GitHub branch protection / Guardian if needed | After CCA complete | required checks, review rules, rulesets | mergegate check output |\ndiff --git a/docs/agents/guardian-policy.md b/docs/agents/guardian-policy.md\nindex 4147c7adf05a47adb4fd24ada1a1c2e1f2ba6d2d..a20d9cc6c00a569bb057b9ca8d6b51c7fc4a9847\n--- a/docs/agents/guardian-policy.md\n+++ b/docs/agents/guardian-policy.md\n@@ -98,8 +98,8 @@\n `origin/<default_branch>` and reuses each job's existing base fetch; a shallow\n (`--depth`) or second base fetch would make the proof unresolvable and silently\n disable the carry, so neither is permitted. CCA reads `guardian_comment_carried` as\n-a recorded authority in the deterministic signal and does not re-derive it. See ADR\n-0018 for the full decision, the rejected diff/patch-equality and path-disjointness\n+a recorded authority in the deterministic signal and does not re-derive it. See\n+SADR-0018 for the full decision, the rejected diff/patch-equality and path-disjointness\n alternatives, and the accepted residual.\n \n ## External AI Guardian Review (SADR-0010)\ndiff --git a/docs/agents/implementation-policy.md b/docs/agents/implementation-policy.md\nindex c754144a9fb5bf4a38aa82a60e1670c59e626b44..85c37281bcad196c6b7b02343faf00b03cd9c57a\n--- a/docs/agents/implementation-policy.md\n+++ b/docs/agents/implementation-policy.md\n@@ -31,10 +31,13 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n ## Non-Negotiables\n \n 1. **Planning precedes implementation.** Non-trivial Goals start with `grill-with-docs` and Context & Impact.\n-2. **Domain language is controlled.** Terms must be checked against `CONTEXT.md`; hard-to-reverse tradeoffs must be checked against ADRs.\n+2. **Domain language is controlled.** Terms must be checked against `CONTEXT.md`; hard-to-reverse tradeoffs must be checked against Shiki SADRs and target ADRs.\n 3. **PRD precedes issue decomposition.** `to-prd` is used once the context is settled enough to become durable intent.\n 4. **Issues are vertical slices.** `to-issues` must produce independently grabbable tracer-bullet tasks, not horizontal layer tickets.\n 5. **Triage controls dispatch.** Only `ready-for-agent` / AFK tasks may be assigned to Codex without human input.\n@@ -95,19 +98,19 @@\n Process:\n \n - Challenge terms against `CONTEXT.md`.\n-- Check ADRs before reopening decisions.\n+- Check SADRs and target ADRs before reopening decisions.\n - Inspect code when code can answer a question.\n - Ask one question at a time when the operator is available.\n - Provide a recommended answer with each question.\n - Record resolved terms in `CONTEXT.md`.\n-- Create ADRs only for hard-to-reverse, surprising, tradeoff-based decisions.\n+- Create SADRs or target ADRs only for hard-to-reverse, surprising, tradeoff-based decisions.\n - Convert unresolved load-bearing questions into HITL blockers.\n \n Outputs:\n \n - Resolved design-tree decisions.\n - Updated glossary terms when needed.\n-- ADRs when justified.\n+- SADRs or target ADRs when justified.\n - HITL blockers when decisions remain unresolved.\n \n ## Phase 3 \u2014 Context & Impact\n@@ -117,7 +120,7 @@\n \n Outputs must include:\n \n-- Relevant documents and ADRs.\n+- Relevant documents, SADRs, and target ADRs.\n - Relevant modules, interfaces, seams, callers, and tests.\n - Candidate locks.\n - Dependencies.\n@@ -140,7 +143,7 @@\n - Testing decisions.\n - Out of scope.\n - Further notes.\n-- Links to relevant Goal, domain terms, and ADRs.\n+- Links to relevant Goal, domain terms, SADRs, and target ADRs.\n \n Phase 4 ends with Spec Freeze: the operator explicitly approves the PRD, the\n plan records a `spec_freeze` block (status, approved_by, source), and the SF\n@@ -240,7 +243,7 @@\n - Dependencies and locks.\n - Risk and Guardian requirements.\n - Ledger completeness.\n-- Documentation and ADR updates.\n+- Documentation and SADR/target-ADR updates.\n - Scope drift.\n \n CCA emits one of:\n@@ -301,7 +304,7 @@\n | Evidence failure | Behavior might be done, but proof is missing. | Add checks, PR evidence, ledger entry, or artifacts. |\n | Scope drift | PR changes unrelated behavior. | Revert unrelated changes or split into new Goal. |\n | Dependency block | Upstream task or lock not complete. | Block until dependency resolves. |\n-| Architecture block | Decision needs ADR or deep-module discussion. | `grill-with-docs` or `improve-codebase-architecture`. |\n+| Architecture block | Decision needs a SADR, target ADR, or deep-module discussion. | `grill-with-docs` or `improve-codebase-architecture`. |\n | Risk block | Security/production/policy change needs human approval. | Guardian decision. |\n \n ## Phase 5.5 \u2014 Execution Decision Control\ndiff --git a/docs/agents/runtime-auth-model.md b/docs/agents/runtime-auth-model.md\nindex 6a2e1a47e81ea89b5c68a210ec07772a9374babc..3c54748d24233dbd0ba8473950282547ef4ce935\n--- a/docs/agents/runtime-auth-model.md\n+++ b/docs/agents/runtime-auth-model.md\n@@ -148,7 +148,7 @@\n \n API-key based runners may be added by a target repository as an explicit extension. They are not the default Shiki template path.\n \n-If a repo opts into API-key mode, record the exception in an ADR and update the repo's `AGENTS.md`, `.shiki/config.yaml`, and workflow permissions.\n+If a repo opts into API-key mode, record the target-specific exception in a target ADR and update the repo's `AGENTS.md`, `.shiki/config.yaml`, and workflow permissions.\n \n ## GitHub Actions Runtime Compatibility\n \ndiff --git a/docs/agents/skill-gate.md b/docs/agents/skill-gate.md\nindex c50398b0d7c60a8598f2a5c88a10015730dced13..3acffe6444617bb1d54b77d4b4708158ec735bc6\n--- a/docs/agents/skill-gate.md\n+++ b/docs/agents/skill-gate.md\n@@ -9,7 +9,7 @@\n | Skill | Trigger | Expected output |\n | --- | --- | --- |\n | `setup-matt-pocock-skills` | First configuring a repo for these skills, or when issue tracker, triage labels, or domain docs are missing. | `docs/agents/` configuration and an `Agent skills` block in `AGENTS.md` or `CLAUDE.md`. |\n-| `grill-with-docs` | Ambiguous plan, unclear terminology, boundary decision, tradeoff, or ADR-worthy design choice. | Clarified decision tree, recommended answers, updates to `CONTEXT.md` or ADRs when decisions crystallize. |\n+| `grill-with-docs` | Ambiguous plan, unclear terminology, boundary decision, tradeoff, or decision-record-worthy design choice. | Clarified decision tree, recommended answers, updates to `CONTEXT.md`, SADRs, or target ADRs when decisions crystallize. |\n | `zoom-out` | Agent lacks a higher-level architectural map. | Map of relevant modules, callers, seams, and domain concepts. |\n | `to-prd` | Settled context should become a PRD. | PRD published to the issue tracker with problem, solution, user stories, decisions, testing, and out-of-scope notes. |\n | `to-issues` | Goal, PRD, or plan must become implementation tickets. | Vertical-slice issues with acceptance criteria, dependencies, and AFK/HITL classification. |\n@@ -17,7 +17,7 @@\n | `tdd` | Feature work or bug fix has observable behavior that can be specified with tests. | Red-green-refactor loop through public interfaces, one vertical slice at a time. |\n | `code-review` | The TDD loop is green and the implementer is about to create or update a PR. Default for every implementation task. | Pre-PR self-review of the task diff with fixes applied on the task branch; findings and resolutions recorded as a ledger entry naming the skill and a `## Pre-PR code review` PR body section. |\n | `diagnose` | Hard bug, failing check, regression, flaky behavior, or performance problem. | Reproduction loop, ranked hypotheses, instrumentation, fix, regression test, post-mortem. |\n-| `improve-codebase-architecture` | Structural friction, testability, AI-navigability, deep-module opportunity, or refactor request. | Architecture candidates and deepening recommendations informed by `CONTEXT.md` and ADRs. |\n+| `improve-codebase-architecture` | Structural friction, testability, AI-navigability, deep-module opportunity, or refactor request. | Architecture candidates and deepening recommendations informed by `CONTEXT.md`, SADRs, and target ADRs. |\n | `prototype` | A throwaway logic or UI prototype is needed to answer a design question. | Clearly marked disposable prototype and captured decision. |\n \n ## MergeGate Interaction\ndiff --git a/scripts/guardian_approval_signal.py b/scripts/guardian_approval_signal.py\nindex d059f92b2c82aa2b5d6ca421365537a87886c1cb..16924892714f4f5b9303c0ef0e325a7e70075630\n--- a/scripts/guardian_approval_signal.py\n+++ b/scripts/guardian_approval_signal.py\n@@ -199,8 +199,8 @@\n ) -> Any:\n     \"\"\"Evaluate SADR-0015 Contract Approval for the CCA Guardian signal.\n \n-    The SAME OR that MergeGate applies must reach this deterministic signal: ADR\n-    0015 is explicit that without it, a contract-approved PR still returns\n+    The SAME OR that MergeGate applies must reach this deterministic signal:\n+    SADR-0015 is explicit that without it, a contract-approved PR still returns\n     ``needs_guardian``, the CCA job fails, and the MergeGate policy check never\n     runs \u2014 the gate this change moves would never be reached. This resolves the\n     task the SAME way MergeGate does (the first Shiki task id in the PR body),\ndiff --git a/scripts/shiki_contract_approval.py b/scripts/shiki_contract_approval.py\nindex 941bcb96fdd8b9a85bc0c4f0f04d5dad9d04839c..4a4b418f6527413161db82670bec857a043360e2\n--- a/scripts/shiki_contract_approval.py\n+++ b/scripts/shiki_contract_approval.py\n@@ -142,7 +142,7 @@\n     changed_files_status: Any,\n     registration: Any,\n ) -> ContractApprovalResult:\n-    \"\"\"Whether this PR carries an SADR-0015 Contract Approval.\n+    \"\"\"Whether this PR carries a SADR-0015 Contract Approval.\n \n     The four conditions are evaluated IN ORDER, each failure recorded as a\n     distinct reason string:\ndiff --git a/scripts/shiki_guardian.py b/scripts/shiki_guardian.py\nindex a4ecb79bbda55fd5c8b06adfae2eb99760a03c45..4dd99bbe714e26f421fa19abac7a34ae1b195ecc\n--- a/scripts/shiki_guardian.py\n+++ b/scripts/shiki_guardian.py\n@@ -12,7 +12,7 @@\n # The carry re-derives its proof from git in-process on every judging invocation;\n # it is never read from an artifact, so there is nothing to go stale or be forged.\n # shiki_sync_proof is the ONLY guardian-family module that runs git, and every one\n-# of its invocations is read-only and local (ADR: prove-from-git base sync).\n+# of its invocations is read-only and local (SADR: prove-from-git base sync).\n from shiki_sync_proof import verify_pure_base_sync\n \n GUARDIAN_POLICY_PATH = \".shiki/guardian-policy.json\"\ndiff --git a/scripts/shiki_guardian_review.py b/scripts/shiki_guardian_review.py\nindex dd0f104d457899e0ba5ff1e162011c1e90b6f92b..e10ef58c046324e7ea3a3392feb3048ca77414ce\n--- a/scripts/shiki_guardian_review.py\n+++ b/scripts/shiki_guardian_review.py\n@@ -159,7 +159,7 @@\n     docs_only = _is_docs_only(changed_files)\n     if docs_only and matched == 0:\n         focus.append(\n-            \"Docs/ADR-only change: confirm the change is genuinely documentation/decision-only, introduces no behavior or contract drift, and stays consistent with the constitution and existing ADRs.\"\n+            \"Docs/decision-record-only change: confirm the change is genuinely documentation/decision-only, introduces no behavior or contract drift, and stays consistent with the constitution and existing SADRs and target ADRs.\"\n         )\n \n     # Conservative augmentation: unknown (no category and not docs-only), mixed\ndiff --git a/scripts/shiki_installer.py b/scripts/shiki_installer.py\nindex be064ab7a29ff6836f7f7a1d70500a43e0d11d7b..d3e3247edbf3e4be287a4667176d86f6500f8dc5\n--- a/scripts/shiki_installer.py\n+++ b/scripts/shiki_installer.py\n@@ -118,6 +118,7 @@\n # what lets a target carry it without a governance-schema change.\n INSTALL_STAMP_PATH = \".shiki/install-stamp.json\"\n INSTALL_STAMP_VERSION = 1\n+LEGACY_SHIKI_SADR_MAX = 18\n \n # Under --force the shipped surface splits three ways, so nothing is lost and\n # nothing is silently stale.\n@@ -237,9 +238,98 @@\n         )\n \n \n+def legacy_shiki_adr_paths() -> tuple[str, ...]:\n+    \"\"\"Former numeric paths for Shiki SADR-0001 through SADR-0018.\n+\n+    The authoritative slugs come from the current SADR filenames. SADR-0019 is\n+    the namespace decision itself and never shipped under a numeric legacy path.\n+    \"\"\"\n+    paths: list[str] = []\n+    for sadr in sorted((ROOT / \"docs\" / \"adr\").glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\")):\n+        number = sadr.name.split(\"-\", 2)[1]\n+        if int(number) <= LEGACY_SHIKI_SADR_MAX:\n+            paths.append(f\"docs/adr/{sadr.name.removeprefix('SADR-')}\")\n+    return tuple(paths)\n+\n+\n+def preflight_legacy_sadr_cleanup(target: Path, *, force: bool) -> tuple[str, ...]:\n+    \"\"\"Authorize exact legacy Shiki ADR removals before the first write.\n+\n+    A non-force install never removes existing files. A forced install needs no\n+    stamp when no legacy Shiki path is present. When legacy paths do exist, each\n+    must be named in a readable install stamp and its current bytes must match\n+    the recorded digest.\n+    \"\"\"\n+    if not force:\n+        return ()\n+    present = tuple(\n+        relative\n+        for relative in legacy_shiki_adr_paths()\n+        if (target / relative).exists() or (target / relative).is_symlink()\n+    )\n+    if not present:\n+        return ()\n+\n+    stamp_path = target / INSTALL_STAMP_PATH\n+    try:\n+        raw = stamp_path.read_text(encoding=\"utf-8\")\n+        stamp = json.loads(raw)\n+    except FileNotFoundError:\n+        detail = \"install stamp is absent\"\n+        blockers = [f\"{relative}: {detail}\" for relative in present]\n+    except (OSError, UnicodeDecodeError) as error:\n+        detail = f\"install stamp is unreadable ({error})\"\n+        blockers = [f\"{relative}: {detail}\" for relative in present]\n+    except json.JSONDecodeError as error:\n+        detail = f\"install stamp is malformed ({error})\"\n+        blockers = [f\"{relative}: {detail}\" for relative in present]\n+    else:\n+        if not isinstance(stamp, dict):\n+            blockers = [\n+                f\"{relative}: install stamp is malformed (expected an object)\"\n+                for relative in present\n+            ]\n+        else:\n+            digests = stamp.get(\"digests\")\n+            blockers = []\n+            if not isinstance(digests, dict):\n+                blockers.extend(\n+                    f\"{relative}: install stamp has no digest for this path\"\n+                    for relative in present\n+                )\n+            else:\n+                for relative in present:\n+                    expected = digests.get(relative)\n+                    if not isinstance(expected, str) or not expected:\n+                        blockers.append(\n+                            f\"{relative}: install stamp has no digest for this path\"\n+                        )\n+                        continue\n+                    try:\n+                        actual = _sha256_file(target / relative)\n+                    except OSError as error:\n+                        blockers.append(\n+                            f\"{relative}: legacy path is unreadable ({error})\"\n+                        )\n+                        continue\n+                    if actual != expected:\n+                        blockers.append(\n+                            f\"{relative}: digest mismatch (stamp {expected}, current {actual})\"\n+                        )\n+\n+    if blockers:\n+        joined = \"\\n\".join(f\"- {blocker}\" for blocker in blockers)\n+        raise ShikiError(\n+            \"legacy Shiki ADR cleanup is not authorized:\\n\"\n+            f\"{joined}\\nNo files were written.\"\n+        )\n+    return present\n+\n+\n def install_template(target: Path, *, force: bool, validate: bool) -> list[NewFileNote]:\n     # Preconditions first: never rewrite the tree of a target that cannot be\n     # upgraded cleanly (see refuse_pending_migrations).\n+    legacy_cleanup = preflight_legacy_sadr_cleanup(target, force=force)\n     refuse_pending_migrations(target)\n \n     # Resolve the target's required CODEOWNERS owner once from its\n@@ -250,6 +340,10 @@\n     # verbatim. See ``codeowners_required_owner``.\n     codeowners_owner = codeowners_required_owner(target)\n \n+    for relative in legacy_cleanup:\n+        (target / relative).unlink()\n+        info(f\"removed stamped legacy Shiki ADR: {target / relative}\")\n+\n     notes: list[NewFileNote] = []\n     for relative in TEMPLATE_PATHS:\n         source = ROOT / relative\ndiff --git a/scripts/shiki_loop.py b/scripts/shiki_loop.py\nindex 3611641da8aeb5038b1da90e3114b09a473ec6d8..84caa491b9cad0edc4176f209c13e9221e975681\n--- a/scripts/shiki_loop.py\n+++ b/scripts/shiki_loop.py\n@@ -424,7 +424,7 @@\n         # unresolvable CCA verdict has already produced its terminal stop and is never\n         # laundered into a head-moving sync (SADR-0010). It also never fires while the\n         # policy (Guardian) gate is red \u2014 for ANY reason, not only when it is the sole\n-        # red check \u2014 so a head-bound Guardian approval, or an SADR-0015\n+        # red check \u2014 so a head-bound Guardian approval, or a SADR-0015\n         # contract-approval carry that base movement defeated (the policy gate compares\n         # base and head task governance directly), is never silently restored by\n         # adopting base wholesale. It is bounded by a durable per-task counter so the\ndiff --git a/scripts/shiki_tasks.py b/scripts/shiki_tasks.py\nindex aec3809f24b4a0c8c96fc50971cff9ae9ad30381..d531052e9d9717058d4631483cefab44c26a65c1\n--- a/scripts/shiki_tasks.py\n+++ b/scripts/shiki_tasks.py\n@@ -821,7 +821,7 @@\n         \"prompt\": prompt,\n         \"entry_skill\": \"grill-with-docs\",\n         \"required_next_steps\": [\n-            \"Run grill-with-docs until terminology, boundaries, risks, and ADR-worthy decisions are settled.\",\n+            \"Run grill-with-docs until terminology, boundaries, risks, and decision-record-worthy choices are settled.\",\n             \"Obtain the operator's explicit approval of the PRD/requirements (Spec Freeze).\",\n             \"Write a machine-readable plan JSON with grill_with_docs.status=complete and spec_freeze.status=frozen.\",\n             \"Run shiki plan ingest --plan-file PLAN.json.\",\ndiff --git a/scripts/test_shiki_goal_loop.sh b/scripts/test_shiki_goal_loop.sh\nindex 753c7b516e10f0be3bc92a4bd0eb1fa699776ae6..8f3820a0a079b3044424cfe1a6c6b7fe68896e67\n--- a/scripts/test_shiki_goal_loop.sh\n+++ b/scripts/test_shiki_goal_loop.sh\n@@ -231,8 +231,8 @@\n \n # High/critical risk merges autonomously when all required checks are green:\n # the \"MergeGate policy check\" required check IS the Guardian gate (it enforces\n-# guardian-policy.json \u2014 human approval OR external AI guardian review, ADR\n-# 0010), so green checks mean a recorded authority approved. The fake gh returns\n+# guardian-policy.json \u2014 human approval OR external AI guardian review,\n+# SADR-0010), so green checks mean a recorded authority approved. The fake gh returns\n # green checks, simulating that the Guardian gate passed.\n python3 \"$ROOT/scripts/shiki.py\" goal create --target \"$TARGET\" --title \"Guardian gate\" --outcome \"High risk merges via policy gate\" >/tmp/shiki-goal-loop-guardian-goal.json\n GGOAL=\"$(json_get /tmp/shiki-goal-loop-guardian-goal.json goal_id)\"\ndiff --git a/tests/test_adr_references.py b/tests/test_adr_references.py\nindex 51706329c797d5dd9b47e082a333fa83b261d482..db6e693e35f67103935be34f1d9bb4ff7ee60395\n--- a/tests/test_adr_references.py\n+++ b/tests/test_adr_references.py\n@@ -1,7 +1,7 @@\n-\"\"\"Every ``ADR ####`` citation must resolve to exactly one decision record.\n+\"\"\"Every target ADR and Shiki SADR citation resolves in its own namespace.\n \n Background. ``scripts/mergegate_check.py`` justifies ``is_bookkeeping_closeout``\n-with \"ADR 0017\" nine times and ``scripts/guardian_approval_signal.py`` cites\n+with \"SADR-0017\" nine times and ``scripts/guardian_approval_signal.py`` cites\n 0016/0017 seven times, yet for a stretch the numbering in ``docs/adr/`` stopped\n at 0015: the authoring commits lived on a coordinator branch and no task ever\n declared ``docs/adr/...`` in its locks, so nothing carried the records to the\n@@ -11,11 +11,12 @@\n and the directory drifted with no signal.\n \n This is the same unbound-pair shape behind other silent failures: a reference\n-and its target with nothing asserting they agree. This test binds them. It scans\n-every ``scripts/*.py`` (plus ``docs/**/*.md`` and the root ``*.md`` governance\n-files, which are cheap) for the ``ADR ####`` pattern and asserts each cited\n-number resolves to exactly one ``docs/adr/####-*.md``. The converse is\n-deliberately NOT required: an ADR may exist without being cited.\n+and its target with nothing asserting they agree. This test binds them. Target\n+``ADR NNNN`` citations resolve only to ``docs/adr/NNNN-*.md``; Shiki\n+``SADR-NNNN`` citations resolve only to ``docs/adr/SADR-NNNN-*.md``. The scan\n+covers current governance docs, scripts, workflows, prompts, packaging metadata,\n+and repo-local Codex guidance while deliberately excluding historical\n+``.shiki`` evidence. The converse is not required: a record may be uncited.\n \n The suite is dependency free: standard-library ``unittest`` only.\n \"\"\"\n@@ -31,17 +32,18 @@\n \n REPO_ROOT = Path(__file__).resolve().parents[1]\n \n-# \"ADR 0016\" -- a four-digit citation. The trailing \\b keeps a five-digit token\n-# from being read as a truncated four-digit number.\n-_CITATION = re.compile(r\"\\bADR (\\d{4})\\b\")\n-# \"0016-locks-bound-product-paths.md\" -- the leading four digits name the record.\n-_ADR_FILENAME = re.compile(r\"^(\\d{4})-.*\\.md$\")\n+# ``ADR 0016`` / ``SADR-0016`` -- a namespace plus four-digit citation. The\n+# trailing ``\\b`` keeps a five-digit token from being read as a truncated one.\n+_CITATION = re.compile(r\"\\b(S?ADR)[ -](\\d{4})\\b\")\n+# Target ADRs retain the numeric filename; Shiki platform decisions use SADR.\n+_DECISION_FILENAME = re.compile(r\"^(?:(SADR)-)?(\\d{4})-.*\\.md$\")\n \n \n class Violation(NamedTuple):\n     \"\"\"A citation that does not resolve to exactly one decision record.\"\"\"\n \n     citing_file: str  # repo-relative path of the file that cites the ADR\n+    namespace: str  # \"ADR\" for target decisions; \"SADR\" for Shiki decisions\n     number: str  # the cited four-digit number, e.g. \"0016\"\n     line: int  # 1-based line of the citation\n     reason: str  # \"missing\" (zero matches) or \"ambiguous\" (two or more)\n@@ -49,44 +51,59 @@\n \n     @property\n     def expected_path(self) -> str:\n-        return f\"docs/adr/{self.number}-*.md\"\n+        prefix = \"SADR-\" if self.namespace == \"SADR\" else \"\"\n+        return f\"docs/adr/{prefix}{self.number}-*.md\"\n \n     def describe(self) -> str:\n         \"\"\"Name the citing file, the cited number, and the missing path.\"\"\"\n         if self.reason == \"missing\":\n             return (\n-                f\"{self.citing_file}:{self.line} cites ADR {self.number} \"\n+                f\"{self.citing_file}:{self.line} cites {self.namespace} {self.number} \"\n                 f\"but no document matches {self.expected_path}\"\n             )\n         joined = \", \".join(self.matches)\n         return (\n-            f\"{self.citing_file}:{self.line} cites ADR {self.number} which \"\n+            f\"{self.citing_file}:{self.line} cites {self.namespace} {self.number} which \"\n             f\"resolves to {len(self.matches)} documents (expected exactly one \"\n             f\"at {self.expected_path}): {joined}\"\n         )\n \n \n-def _adr_index(root: Path) -> dict[str, list[Path]]:\n-    \"\"\"Map every four-digit ADR number to the record files that claim it.\"\"\"\n-    index: dict[str, list[Path]] = {}\n+def _adr_index(root: Path) -> dict[tuple[str, str], list[Path]]:\n+    \"\"\"Map each decision namespace and number to the files that claim it.\"\"\"\n+    index: dict[tuple[str, str], list[Path]] = {}\n     adr_dir = root / \"docs\" / \"adr\"\n     if adr_dir.is_dir():\n         for path in sorted(adr_dir.glob(\"*.md\")):\n-            match = _ADR_FILENAME.match(path.name)\n+            match = _DECISION_FILENAME.match(path.name)\n             if match:\n-                index.setdefault(match.group(1), []).append(path)\n+                namespace = \"SADR\" if match.group(1) else \"ADR\"\n+                index.setdefault((namespace, match.group(2)), []).append(path)\n     return index\n \n \n def _citing_files(root: Path, include_docs: bool = True) -> list[Path]:\n-    \"\"\"Files scanned for citations: scripts/*.py, and cheaply the markdown.\"\"\"\n+    \"\"\"Current, shipped text surfaces scanned for decision citations.\"\"\"\n     files = sorted((root / \"scripts\").glob(\"*.py\"))\n+    files += sorted((root / \"scripts\").glob(\"*.sh\"))\n     if include_docs:\n         docs = root / \"docs\"\n         if docs.is_dir():\n             files += sorted(docs.rglob(\"*.md\"))\n         files += sorted(root.glob(\"*.md\"))\n-    return files\n+        pyproject = root / \"pyproject.toml\"\n+        if pyproject.is_file():\n+            files.append(pyproject)\n+        for relative in (\".github\", \".codex\"):\n+            surface = root / relative\n+            if surface.is_dir():\n+                files += sorted(\n+                    path\n+                    for path in surface.rglob(\"*\")\n+                    if path.is_file()\n+                    and path.suffix in {\".md\", \".py\", \".sh\", \".toml\", \".yaml\", \".yml\"}\n+                )\n+    return sorted(set(files))\n \n \n def find_adr_reference_violations(\n@@ -94,10 +111,9 @@\n ) -> list[Violation]:\n     \"\"\"Return every ``ADR ####`` citation under ``root`` that fails to resolve.\n \n-    A citation resolves when exactly one ``docs/adr/####-*.md`` matches its\n-    number. Zero matches is a \"missing\" violation; two or more is \"ambiguous\".\n-    Records that are never cited produce nothing -- the converse is not\n-    required.\n+    A citation resolves when exactly one record in the same namespace matches\n+    its number. Zero matches is a \"missing\" violation; two or more is\n+    \"ambiguous\". Records that are never cited produce nothing.\n     \"\"\"\n     root = Path(root)\n     index = _adr_index(root)\n@@ -110,13 +126,15 @@\n         rel = path.relative_to(root).as_posix()\n         for lineno, line in enumerate(text.splitlines(), start=1):\n             for match in _CITATION.finditer(line):\n-                number = match.group(1)\n-                found = index.get(number, [])\n+                namespace, number = match.groups()\n+                found = index.get((namespace, number), [])\n                 if len(found) == 1:\n                     continue\n                 reason = \"missing\" if not found else \"ambiguous\"\n                 rels = [p.relative_to(root).as_posix() for p in found]\n-                violations.append(Violation(rel, number, lineno, reason, rels))\n+                violations.append(\n+                    Violation(rel, namespace, number, lineno, reason, rels)\n+                )\n     return violations\n \n \n@@ -129,8 +147,8 @@\n         self.assertEqual(\n             violations,\n             [],\n-            \"Every 'ADR ####' citation must resolve to exactly one \"\n-            f\"docs/adr/####-*.md:\\n{detail}\",\n+            \"Every ADR/SADR citation must resolve exactly once in its own \"\n+            f\"docs/adr namespace:\\n{detail}\",\n         )\n \n     def test_scan_actually_reads_scripts(self) -> None:\n@@ -146,8 +164,21 @@\n         # The two records this Goal carried onto the default branch, and which\n         # the scripts cite most, must be present.\n         index = _adr_index(REPO_ROOT)\n-        self.assertIn(\"0016\", index)\n-        self.assertIn(\"0017\", index)\n+        self.assertIn((\"SADR\", \"0016\"), index)\n+        self.assertIn((\"SADR\", \"0017\"), index)\n+\n+    def test_platform_tree_has_only_sadr_0001_through_0019(self) -> None:\n+        adr_dir = REPO_ROOT / \"docs\" / \"adr\"\n+        sadrs = sorted(adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\"))\n+        self.assertEqual(\n+            {path.name.split(\"-\", 2)[1] for path in sadrs},\n+            {f\"{number:04d}\" for number in range(1, 20)},\n+        )\n+        self.assertEqual(\n+            list(adr_dir.glob(\"[0-9][0-9][0-9][0-9]-*.md\")),\n+            [],\n+            \"the Shiki platform tree must not retain legacy numeric SADR paths\",\n+        )\n \n \n class FixtureAdrReferences(unittest.TestCase):\n@@ -180,6 +211,26 @@\n         self.assertIn(\"9991\", message)  # cited number\n         self.assertIn(\"docs/adr/9991-*.md\", message)  # missing path\n \n+    def test_target_adr_does_not_satisfy_missing_platform_sadr(self) -> None:\n+        root = self._make_repo(\n+            adr_names=[\"0042-target-decision.md\"],\n+            script_body='\"\"\"Target ADR 0042 exists, but platform SADR-0042 does not.\"\"\"\\n',\n+        )\n+        violations = find_adr_reference_violations(root, include_docs=False)\n+        self.assertEqual(len(violations), 1)\n+        self.assertEqual(violations[0].namespace, \"SADR\")\n+        self.assertEqual(violations[0].number, \"0042\")\n+        self.assertEqual(violations[0].expected_path, \"docs/adr/SADR-0042-*.md\")\n+\n+    def test_target_adr_and_platform_sadr_may_reuse_a_number(self) -> None:\n+        root = self._make_repo(\n+            adr_names=[\"0042-target-decision.md\", \"SADR-0042-platform-decision.md\"],\n+            script_body='\"\"\"Target ADR 0042 and platform SADR-0042 are distinct.\"\"\"\\n',\n+        )\n+        self.assertEqual(\n+            find_adr_reference_violations(root, include_docs=False), []\n+        )\n+\n     def test_uncited_adr_is_allowed(self) -> None:\n         # An ADR exists but nothing cites it -> not a violation. The converse\n         # (every ADR must be cited) is explicitly NOT required.\ndiff --git a/tests/test_guardian_review.py b/tests/test_guardian_review.py\nindex 88829e7810212635e2fc98aa5918d6f26294677d..00a85a237acd5bf2d6cb5b294128198c422402ca\n--- a/tests/test_guardian_review.py\n+++ b/tests/test_guardian_review.py\n@@ -162,7 +162,7 @@\n \n     def test_docs_only_is_light_and_not_conservative(self):\n         focus = gr.classify_review_focus([\"docs/adr/0099-x.md\", \"README.md\"], \"\", \"low\")\n-        self.assertTrue(any(\"Docs/ADR-only\" in f for f in focus))\n+        self.assertTrue(any(\"Docs/decision-record-only\" in f for f in focus))\n         self.assertFalse(any(\"conservative default\" in f.lower() for f in focus))\n \n     def test_unknown_category_is_conservative(self):\ndiff --git a/tests/test_policy_job_merged_prs.py b/tests/test_policy_job_merged_prs.py\nindex d45512e74681e5e649279236c7d4e79d5c3da471..b97989f8bd74d091de1a6b5275a20d8dc68c4512\n--- a/tests/test_policy_job_merged_prs.py\n+++ b/tests/test_policy_job_merged_prs.py\n@@ -3,8 +3,8 @@\n \n ``.github/workflows/shiki-cca-completion.yml`` has two consumers of the merged-PR\n proof: the CCA *signal* (``guardian_approval_signal.py`` in \"Prepare CCA context\")\n-and the authoritative gate (``mergegate_check.py`` in \"Enforce MergeGate\"). ADR\n-0017's bookkeeping-closeout exemption (condition 6) needs the merged implementation\n+and the authoritative gate (``mergegate_check.py`` in \"Enforce MergeGate\").\n+SADR-0017's bookkeeping-closeout exemption (condition 6) needs the merged implementation\n PR proven, and a closeout carries NO ``post_merge_reconcile`` marker. The signal\n already resolves ``MERGED_PRS`` unconditionally, but \"Enforce MergeGate\" used to\n resolve it only inside ``grep -q '<!-- shiki:post_merge_reconcile -->'``. So a\ndiff --git a/tests/test_sadr_install_migration.py b/tests/test_sadr_install_migration.py\nnew file mode 100644\nindex 0000000000000000000000000000000000000000..a6f4c57e4dcaef73ef600b0d4804744138ab9071\n--- /dev/null\n+++ b/tests/test_sadr_install_migration.py\n@@ -0,0 +1,229 @@\n+\"\"\"SADR installation and collision-safe legacy upgrade behavior.\"\"\"\n+\n+from __future__ import annotations\n+\n+import contextlib\n+import hashlib\n+import io\n+import json\n+import shutil\n+import tempfile\n+import unittest\n+from pathlib import Path\n+\n+import shiki_test_support  # noqa: F401  (path bootstrap)\n+\n+import shiki_installer\n+from tests.test_adr_references import find_adr_reference_violations\n+\n+\n+PRODUCT_NUMBERS = tuple(range(13, 19))\n+\n+\n+def _quiet_install(target: Path, *, force: bool, validate: bool) -> None:\n+    with contextlib.redirect_stdout(io.StringIO()):\n+        shiki_installer.install_template(target, force=force, validate=validate)\n+\n+\n+def _sha256(path: Path) -> str:\n+    return hashlib.sha256(path.read_bytes()).hexdigest()\n+\n+\n+def _legacy_sadr_paths() -> tuple[str, ...]:\n+    return tuple(\n+        f\"docs/adr/{path.name.removeprefix('SADR-')}\"\n+        for path in sorted((shiki_installer.ROOT / \"docs\" / \"adr\").glob(\"SADR-*.md\"))\n+        if path.name != \"SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\"\n+    )\n+\n+\n+def _create_legacy_upgrade_fixture(\n+    target: Path,\n+) -> tuple[dict[str, bytes], tuple[str, ...]]:\n+    adr_dir = target / \"docs\" / \"adr\"\n+    adr_dir.mkdir(parents=True)\n+    product_bytes = {\n+        f\"{number:04d}-cubeshare-product-decision.md\": (\n+            f\"# CubeShare ADR {number:04d}\\n\\nDo not rewrite {number}.\\n\"\n+        ).encode()\n+        for number in PRODUCT_NUMBERS\n+    }\n+    for name, content in product_bytes.items():\n+        (adr_dir / name).write_bytes(content)\n+\n+    legacy_paths = _legacy_sadr_paths()\n+    for relative in legacy_paths:\n+        (target / relative).write_bytes(f\"legacy Shiki bytes for {relative}\\n\".encode())\n+\n+    stamp_path = target / shiki_installer.INSTALL_STAMP_PATH\n+    stamp_path.parent.mkdir(parents=True, exist_ok=True)\n+    stamp = {\n+        \"version\": shiki_installer.INSTALL_STAMP_VERSION,\n+        \"platform_commit\": None,\n+        \"installed_at\": \"2000-01-01T00:00:00+00:00\",\n+        \"digests\": {\n+            relative: _sha256(target / relative) for relative in legacy_paths\n+        },\n+    }\n+    stamp_path.write_text(\n+        json.dumps(stamp, indent=2, sort_keys=True) + \"\\n\", encoding=\"utf-8\"\n+    )\n+    return product_bytes, legacy_paths\n+\n+\n+def _tree_snapshot(root: Path) -> dict[str, tuple[str, bytes | str]]:\n+    \"\"\"Recursive byte/shape snapshot used to prove preflight is write-free.\"\"\"\n+    snapshot: dict[str, tuple[str, bytes | str]] = {}\n+    for path in sorted(root.rglob(\"*\")):\n+        relative = path.relative_to(root).as_posix()\n+        if path.is_symlink():\n+            snapshot[relative] = (\"symlink\", path.readlink().as_posix())\n+        elif path.is_dir():\n+            snapshot[relative] = (\"directory\", \"\")\n+        else:\n+            snapshot[relative] = (\"file\", path.read_bytes())\n+    return snapshot\n+\n+\n+class FreshSadrInstallTests(unittest.TestCase):\n+    def setUp(self) -> None:\n+        self.tmp = Path(tempfile.mkdtemp(prefix=\"shiki-sadr-fresh-\"))\n+        self.addCleanup(lambda: shutil.rmtree(self.tmp, ignore_errors=True))\n+        self.target = self.tmp / \"target\"\n+        self.adr_dir = self.target / \"docs\" / \"adr\"\n+        self.adr_dir.mkdir(parents=True)\n+        self.product_bytes = {\n+            f\"{number:04d}-product-decision.md\": (\n+                f\"# Product ADR {number:04d}\\n\\nTarget-owned bytes {number}.\\n\"\n+            ).encode()\n+            for number in PRODUCT_NUMBERS\n+        }\n+        for name, content in self.product_bytes.items():\n+            (self.adr_dir / name).write_bytes(content)\n+\n+    def test_fresh_install_preserves_product_adrs_and_installs_all_sadrs(self) -> None:\n+        _quiet_install(self.target, force=False, validate=True)\n+\n+        for name, expected in self.product_bytes.items():\n+            self.assertEqual((self.adr_dir / name).read_bytes(), expected)\n+\n+        sadr_numbers = {\n+            path.name.split(\"-\", 2)[1]\n+            for path in self.adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\")\n+        }\n+        self.assertEqual(sadr_numbers, {f\"{number:04d}\" for number in range(1, 20)})\n+\n+        numeric_records = {\n+            path.name\n+            for path in self.adr_dir.glob(\"[0-9][0-9][0-9][0-9]-*.md\")\n+        }\n+        self.assertEqual(numeric_records, set(self.product_bytes))\n+        self.assertEqual(find_adr_reference_violations(self.target), [])\n+\n+\n+class LegacySadrUpgradeTests(unittest.TestCase):\n+    def setUp(self) -> None:\n+        self.tmp = Path(tempfile.mkdtemp(prefix=\"shiki-sadr-upgrade-\"))\n+        self.addCleanup(lambda: shutil.rmtree(self.tmp, ignore_errors=True))\n+        self.target = self.tmp / \"target\"\n+        self.adr_dir = self.target / \"docs\" / \"adr\"\n+        self.product_bytes, self.legacy_paths = _create_legacy_upgrade_fixture(\n+            self.target\n+        )\n+\n+    def test_force_upgrade_deletes_only_stamped_legacy_sadrs(self) -> None:\n+        _quiet_install(self.target, force=True, validate=True)\n+\n+        for relative in self.legacy_paths:\n+            self.assertFalse((self.target / relative).exists(), relative)\n+        for name, expected in self.product_bytes.items():\n+            self.assertEqual((self.adr_dir / name).read_bytes(), expected)\n+\n+        sadrs = list(self.adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\"))\n+        self.assertEqual(len(sadrs), 19)\n+        refreshed = json.loads(\n+            (self.target / shiki_installer.INSTALL_STAMP_PATH).read_text(encoding=\"utf-8\")\n+        )\n+        for relative in self.legacy_paths:\n+            self.assertNotIn(relative, refreshed[\"digests\"])\n+        for path in sadrs:\n+            relative = path.relative_to(self.target).as_posix()\n+            self.assertEqual(refreshed[\"digests\"][relative], _sha256(path))\n+\n+    def test_non_force_install_never_deletes_legacy_files(self) -> None:\n+        before = {\n+            relative: (self.target / relative).read_bytes()\n+            for relative in self.legacy_paths\n+        }\n+        _quiet_install(self.target, force=False, validate=False)\n+        for relative, expected in before.items():\n+            self.assertEqual((self.target / relative).read_bytes(), expected)\n+        refreshed = json.loads(\n+            (self.target / shiki_installer.INSTALL_STAMP_PATH).read_text(encoding=\"utf-8\")\n+        )\n+        for relative in self.legacy_paths:\n+            self.assertEqual(refreshed[\"digests\"][relative], _sha256(self.target / relative))\n+\n+\n+class LegacyCleanupPreflightTests(unittest.TestCase):\n+    def setUp(self) -> None:\n+        self.tmp = Path(tempfile.mkdtemp(prefix=\"shiki-sadr-preflight-\"))\n+        self.addCleanup(lambda: shutil.rmtree(self.tmp, ignore_errors=True))\n+\n+    def test_all_ownership_blockers_abort_before_any_write(self) -> None:\n+        cases = (\n+            (\"absent\", \"install stamp is absent\"),\n+            (\"unreadable\", \"install stamp is unreadable\"),\n+            (\"malformed\", \"install stamp is malformed\"),\n+            (\"missing-digest\", \"install stamp has no digest\"),\n+            (\"digest-mismatch\", \"digest mismatch\"),\n+        )\n+        for name, expected_reason in cases:\n+            with self.subTest(name=name):\n+                target = self.tmp / name\n+                _, legacy_paths = _create_legacy_upgrade_fixture(target)\n+                stamp_path = target / shiki_installer.INSTALL_STAMP_PATH\n+\n+                if name == \"absent\":\n+                    stamp_path.unlink()\n+                elif name == \"unreadable\":\n+                    stamp_path.unlink()\n+                    stamp_path.mkdir()\n+                elif name == \"malformed\":\n+                    stamp_path.write_text(\"{not-json\\n\", encoding=\"utf-8\")\n+                else:\n+                    stamp = json.loads(stamp_path.read_text(encoding=\"utf-8\"))\n+                    if name == \"missing-digest\":\n+                        stamp[\"digests\"] = {}\n+                    else:\n+                        stamp[\"digests\"] = {\n+                            relative: \"deadbeef\" for relative in legacy_paths\n+                        }\n+                    stamp_path.write_text(\n+                        json.dumps(stamp, indent=2, sort_keys=True) + \"\\n\",\n+                        encoding=\"utf-8\",\n+                    )\n+\n+                before = _tree_snapshot(target)\n+                with self.assertRaises(shiki_installer.ShikiError) as raised:\n+                    _quiet_install(target, force=True, validate=False)\n+                after = _tree_snapshot(target)\n+\n+                self.assertEqual(before, after, f\"{name} preflight changed the tree\")\n+                message = str(raised.exception)\n+                self.assertIn(expected_reason, message)\n+                self.assertIn(\"No files were written\", message)\n+                for relative in legacy_paths:\n+                    self.assertIn(relative, message)\n+\n+    def test_absent_stamp_is_allowed_when_no_legacy_path_exists(self) -> None:\n+        target = self.tmp / \"no-legacy\"\n+        target.mkdir()\n+        _quiet_install(target, force=True, validate=False)\n+        self.assertTrue(\n+            (target / \"docs\" / \"adr\" / \"SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\").is_file()\n+        )\n+\n+\n+if __name__ == \"__main__\":\n+    unittest.main()\n\ndiff --git a/.codex/skills/shiki/SKILL.md b/.codex/skills/shiki/SKILL.md\nindex c8e2e85d9d75d49cc4f2d6286ee22659c4e53e75..93cded623b2c15d9f65c76491a1c2d813ac9e6c0\n--- a/.codex/skills/shiki/SKILL.md\n+++ b/.codex/skills/shiki/SKILL.md\n@@ -18,6 +18,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n Codex CLI does not register this skill as a custom `/shiki` slash command. When\n the user types `/shiki` in Codex CLI and gets \"Unrecognized command\", explain\n that this is expected: invoke Shiki in Codex with natural language such as\n@@ -69,7 +72,7 @@\n \n ## Responsibilities\n \n-- Claude Code implements and repairs by default (`shiki runner claude`, ADR 0008); Codex implements and repairs tasks explicitly assigned to `codex`.\n+- Claude Code implements and repairs by default (`shiki runner claude`, SADR-0008); Codex implements and repairs tasks explicitly assigned to `codex`.\n - Claude Code Action can act as GitHub-side CCA or reviewer.\n - CCA judges completion.\n - MergeGate authorizes state transitions and merge readiness.\ndiff --git a/.github/prompts/cca-completion-check.md b/.github/prompts/cca-completion-check.md\nindex ed203ad5f302ecc81372d8ab27d2b1232ca97497..fd89950528333ef6242e6d5443904f5392ab7e56\n--- a/.github/prompts/cca-completion-check.md\n+++ b/.github/prompts/cca-completion-check.md\n@@ -19,6 +19,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n ## Required Reading\n \n Read only the files needed to judge the PR contract. Prefer this order:\ndiff --git a/.github/prompts/codex-task-handoff.md b/.github/prompts/codex-task-handoff.md\nindex 695e211d76dea1c60e8d5bb360e78487808286a4..59441d576b01a39569722f226c854e5a740e9eb5\n--- a/.github/prompts/codex-task-handoff.md\n+++ b/.github/prompts/codex-task-handoff.md\n@@ -2,7 +2,7 @@\n \n You are Codex Front acting as the Implementer for a Shiki task.\n \n-Follow `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant ADRs, and `docs/agents/codex-handoff.md`.\n+Follow `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant Shiki SADRs and target ADRs, and `docs/agents/codex-handoff.md`.\n \n Rules:\n \ndiff --git a/AGENTS.md b/AGENTS.md\nindex 7f041100d49c2a7525f51cb0f5d696a4a6d1c78c..a06bfbabc6fa48a007389e25e12d7ed523936bed\n--- a/AGENTS.md\n+++ b/AGENTS.md\n@@ -22,7 +22,7 @@\n - **Codex Front** is an optional implementer through Codex App, Codex CLI, Codex IDE extension, or Codex Web signed in with ChatGPT OAuth/subscription auth, dispatched with `shiki runner codex` when a task is explicitly assigned to `codex`.\n - **GitHub CCA** is implemented by Claude Code Action by default, using `CLAUDE_CODE_OAUTH_TOKEN`.\n \n-Do not assume `openai/codex-action`, `OPENAI_API_KEY`, or API-key based Codex automation in the default Shiki path. API-key runners are explicit target-repo extensions and require their own ADR.\n+Do not assume `openai/codex-action`, `OPENAI_API_KEY`, or API-key based Codex automation in the default Shiki path. API-key runners are explicit target-repo extensions and require their own target ADR.\n \n Shiki is not:\n \n@@ -44,10 +44,14 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use\n+`SADR-NNNN-*.md`; target product decisions retain `NNNN-*.md` and the `ADR`\n+namespace.\n+\n Use the canonical source-of-truth order above. Runtime-specific reading orders\n are operational guidance only and must not override or reorder it.\n \n-Conversation state is not durable truth. If a decision matters, put it in GitHub, `.shiki/`, `CONTEXT.md`, or an ADR.\n+Conversation state is not durable truth. If a decision matters, put it in GitHub, `.shiki/`, `CONTEXT.md`, a Shiki SADR, or a target ADR.\n \n If GitHub and `.shiki/` disagree, surface the conflict and prefer GitHub operational state until the mirror is repaired.\n \n@@ -75,8 +79,8 @@\n Every non-trivial change follows this loop:\n \n 1. **Goal**: clarify outcome, completion conditions, non-goals, risk level, and success signals.\n-2. **grill-with-docs**: challenge the plan against domain docs, ADRs, code reality, terminology, and edge scenarios.\n-3. **Context & Impact**: find relevant docs, ADRs, code, dependencies, owners, locks, and verification surfaces.\n+2. **grill-with-docs**: challenge the plan against domain docs, Shiki SADRs, target ADRs, code reality, terminology, and edge scenarios.\n+3. **Context & Impact**: find relevant docs, SADRs, target ADRs, code, dependencies, owners, locks, and verification surfaces.\n 4. **PRD and Spec Freeze**: use `to-prd` when context is settled enough to become durable product/engineering intent. The operator's explicit approval of the PRD is Spec Freeze: the plan records a `spec_freeze` block, and no plan runs without it. Steps 1-4 form Requirements Definition \u2014 one continuous operator dialogue. After Spec Freeze, scope changes require an operator-approved Spec Amendment; non-scope-moving interpretations go to the Assumption Log.\n 5. **Issues**: use `to-issues` to create independently grabbable vertical-slice issues. Prefer AFK slices over HITL slices where possible.\n 6. **Triage**: label issues for readiness, risk, runtime, skills, and MergeGate state.\n@@ -167,7 +171,7 @@\n - Testing decisions.\n - Out-of-scope items.\n - Further notes.\n-- Links to relevant domain terms and ADRs.\n+- Links to relevant domain terms, SADRs, and target ADRs.\n \n Every executable task must include:\n \n@@ -193,7 +197,7 @@\n \n Context & Impact must identify:\n \n-- Relevant documents, domain terms, ADRs, and past decisions.\n+- Relevant documents, domain terms, SADRs, target ADRs, and past decisions.\n - Relevant modules, interfaces, seams, callers, owners, and tests.\n - Dependency relationships between tasks.\n - Candidate locks and lock conflicts.\n@@ -210,7 +214,7 @@\n | Skill | Required when |\n | --- | --- |\n | `setup-matt-pocock-skills` | First configuring the repo for skills, or when issue tracker, triage labels, or domain docs are missing. |\n-| `grill-with-docs` | Default for non-trivial Goals before PRD/issues; always required when plans, terminology, boundaries, tradeoffs, or ADR-worthy decisions are ambiguous. |\n+| `grill-with-docs` | Default for non-trivial Goals before PRD/issues; always required when plans, terminology, boundaries, tradeoffs, or decision-record-worthy choices are ambiguous. |\n | `zoom-out` | The agent lacks an architectural map of the relevant code area. |\n | `to-prd` | Settled Goal context must become durable product/engineering intent. |\n | `to-issues` | A Goal, PRD, or plan must become independently grabbable vertical-slice issues. |\n@@ -348,7 +352,7 @@\n - Three or more new files.\n - Any high-risk or critical label.\n \n-When the architecture gate triggers, run `grill-with-docs` or `improve-codebase-architecture` as appropriate and record durable decisions in an ADR when needed.\n+When the architecture gate triggers, run `grill-with-docs` or `improve-codebase-architecture` as appropriate and record durable decisions in a Shiki SADR or target ADR as applicable.\n \n ## Engineering Discipline\n \ndiff --git a/CLAUDE.md b/CLAUDE.md\nindex 98a932e7841a632866fcd53b0bad0e3af3711d3d..98a31399404d6fb044b4fd493f4459ac87f439de\n--- a/CLAUDE.md\n+++ b/CLAUDE.md\n@@ -14,6 +14,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n ## Claude Role\n \n Default to planner, default implementer, reviewer, coordinator, documentation editor, CCA-style completion judge, and final-judgment assistant.\n@@ -57,7 +60,7 @@\n At the start of a Shiki session:\n \n 1. Read `AGENTS.md`.\n-2. Read `CONTEXT.md` and relevant ADRs.\n+2. Read `CONTEXT.md` and relevant Shiki SADRs and target ADRs.\n 3. Read active `.shiki/` Goal/task/ledger state when present.\n 4. Read active distilled rules from `.shiki/memories/` (status `distilled`, `active: true`, not revoked or superseded) for applicable guidance; the same rules are injected deterministically into task handoffs as the `## Distilled Rules` section (\u00a73.5).\n 5. Read the GitHub Issue or PR when the task is GitHub-backed.\n@@ -74,7 +77,7 @@\n - Define the Goal, completion conditions, non-goals, and risk level.\n - Treat `grill-with-docs` as the default planning entry point for non-trivial Goals.\n - Challenge domain terms against `CONTEXT.md`.\n-- Challenge hard-to-reverse tradeoffs against ADRs.\n+- Challenge hard-to-reverse tradeoffs against SADRs and target ADRs.\n - Explore code instead of asking when code can answer the question.\n - Run Context & Impact before implementation planning.\n - Produce a Task DAG, not an unordered checklist.\n@@ -94,7 +97,7 @@\n - For each question, provide your recommended answer.\n - Resolve dependency decisions before downstream design questions.\n - Update `CONTEXT.md` inline when terminology crystallizes.\n-- Offer ADRs only when the decision is hard to reverse, surprising without context, and a real tradeoff.\n+- Offer a SADR or target ADR only when the decision is hard to reverse, surprising without context, and a real tradeoff.\n - Convert unresolved questions into HITL blockers rather than allowing Codex to guess.\n \n ## PRD and Issues Standard\n@@ -103,7 +106,7 @@\n \n - Use the project's domain glossary vocabulary.\n - Avoid volatile file paths unless they are essential.\n-- Include implementation decisions, testing decisions, out-of-scope items, and relevant ADR/domain links.\n+- Include implementation decisions, testing decisions, out-of-scope items, and relevant SADR/target-ADR/domain links.\n - Identify deep-module opportunities and testable interfaces.\n \n When creating issues:\n@@ -121,7 +124,7 @@\n - Branch/worktree target.\n - Scope and non-goals.\n - Dependencies and locks.\n-- Relevant docs, ADRs, modules, and tests.\n+- Relevant docs, SADRs, target ADRs, modules, and tests.\n - Required skill invocations.\n - TDD expectations.\n - Pre-PR `code-review` gate expectations.\n@@ -152,7 +155,7 @@\n \n - Treat yourself as a judge, not implementer.\n - Do not write production code.\n-- Read `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant ADRs, PRD, task issue, PR body, diff, checks, reviews, labels, and `.shiki/` state.\n+- Read `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant SADRs and target ADRs, PRD, task issue, PR body, diff, checks, reviews, labels, and `.shiki/` state.\n - Evaluate every applicable checklist in `docs/agents/checklists.md`.\n - Map each acceptance criterion to durable evidence.\n - Distinguish implementation failure from missing evidence.\ndiff --git a/CONTEXT.md b/CONTEXT.md\nindex e61a9be06a0cce0d026484083a657bde2067e234..237ee8aa3ea8fab334ece49768bc5ed3b5af0974\n--- a/CONTEXT.md\n+++ b/CONTEXT.md\n@@ -25,7 +25,7 @@\n _Avoid_: jumping directly into code\n \n **grill-with-docs**:\n-A planning skill that challenges a plan against domain language, ADRs, code reality, and concrete edge scenarios. It resolves design-tree questions before PRD/issues. It runs inside Requirements Definition.\n+A planning skill that challenges a plan against domain language, Shiki SADRs, target ADRs, code reality, and concrete edge scenarios. It resolves design-tree questions before PRD/issues. It runs inside Requirements Definition.\n _Avoid_: generic brainstorming, silent assumptions\n \n **Requirements Definition**:\ndiff --git a/CONTRIBUTING.md b/CONTRIBUTING.md\nindex c6d9b656e73abb8a55e5f660123e03153dcb3e6d..9e37a8fbf668db5b22461aaae5548b8f1354394c\n--- a/CONTRIBUTING.md\n+++ b/CONTRIBUTING.md\n@@ -17,7 +17,7 @@\n 1. **Goal** \u2014 start from a user-approved Goal with an outcome, completion\n    conditions, non-goals, risk level, and success signals.\n 2. **grill-with-docs** \u2014 for any non-trivial change, challenge the plan against\n-   domain language, ADRs, and code reality before writing a PRD.\n+   domain language, Shiki SADRs, target ADRs, and code reality before writing a PRD.\n 3. **PRD** \u2014 capture settled product/engineering intent (`to-prd`).\n 4. **Spec Freeze** \u2014 the operator explicitly approves the PRD/requirements;\n    the plan records a `spec_freeze` block and no plan runs without it\n@@ -94,7 +94,7 @@\n   entries.\n - Do not hand-edit `.shiki/goals/*.json` or `.shiki/manifest.json` in a task PR.\n - Conversation state is not durable truth. If a decision matters, record it in\n-  GitHub, `.shiki/`, `CONTEXT.md`, or an ADR.\n+  GitHub, `.shiki/`, `CONTEXT.md`, a Shiki SADR, or a target ADR.\n \n ## Architecture Gate\n \n@@ -103,7 +103,7 @@\n or CI policy, package structure, three or more structural files, more than 100\n net new lines in a core module, three or more new files, or any high-risk label.\n When triggered, run `grill-with-docs` or `improve-codebase-architecture` and\n-record durable decisions in an ADR when needed (see `docs/adr/`).\n+record durable decisions in a Shiki SADR or target ADR as applicable (see `docs/adr/`).\n \n ## Safety\n \ndiff --git a/SYSTEM_PROMPT.md b/SYSTEM_PROMPT.md\nindex aaaea45e3a896c3a44ba89eec8bb84325ecf6c62..da009c86a88285269dee0155e58e550c6d744ea9\n--- a/SYSTEM_PROMPT.md\n+++ b/SYSTEM_PROMPT.md\n@@ -30,6 +30,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n Use the canonical source-of-truth order above. Runtime-specific reading orders\n are operational guidance only and must not override or reorder it. The current\n conversation is non-durable operator input.\n@@ -48,14 +51,14 @@\n \n A runtime may only do work that is explicitly assigned by the Goal, task, PR, or MergeGate decision.\n \n-Do not assume `openai/codex-action`, `OPENAI_API_KEY`, or API-key based Codex automation in the default Shiki path. API-key runners are explicit target-repo extensions and require their own ADR.\n+Do not assume `openai/codex-action`, `OPENAI_API_KEY`, or API-key based Codex automation in the default Shiki path. API-key runners are explicit target-repo extensions and require their own target ADR.\n \n ## 4. Non-negotiable Shiki flow\n \n For any non-trivial Goal, use this flow:\n \n 1. **Goal Seek**: define outcome, non-goals, risk level, completion criteria, and evidence requirements.\n-2. **grill-with-docs**: challenge the plan against `CONTEXT.md`, ADRs, code reality, terminology, boundaries, and edge scenarios. Ask one question at a time when the operator is available. If code can answer a question, inspect code instead of asking.\n+2. **grill-with-docs**: challenge the plan against `CONTEXT.md`, Shiki SADRs, target ADRs, code reality, terminology, boundaries, and edge scenarios. Ask one question at a time when the operator is available. If code can answer a question, inspect code instead of asking.\n 3. **Context & Impact**: identify modules, interfaces, seams, callers, dependencies, locks, risk, and verification surfaces.\n 4. **to-prd + Spec Freeze**: turn settled context into a PRD using domain vocabulary and testing decisions. The operator's explicit approval of the PRD is Spec Freeze, recorded as the plan's `spec_freeze` block; plans without it do not run. Steps 1-4 form Requirements Definition.\n 5. **to-issues**: decompose the PRD into independently grabbable vertical-slice issues. Prefer AFK slices; mark HITL slices when judgment is still required.\n@@ -75,7 +78,7 @@\n Before making any material change:\n \n 1. Read `AGENTS.md` and any runtime-specific wrapper such as `CLAUDE.md`.\n-2. Read `CONTEXT.md` and relevant ADRs.\n+2. Read `CONTEXT.md` and relevant SADRs and target ADRs.\n 3. Read the GitHub Issue / PR and `.shiki/` state for the active Goal or task.\n 4. Identify your role: Planner, Implementer, Reviewer, Completion Check Agent, Repairer, or Guardian-assist.\n 5. Identify Goal id, task id, branch, PR, dependencies, locks, risk level, required skills, and required evidence.\n@@ -103,7 +106,7 @@\n \n At minimum, identify:\n \n-- Relevant documents, ADRs, domain terms, and prior decisions.\n+- Relevant documents, SADRs, target ADRs, domain terms, and prior decisions.\n - Relevant modules, interfaces, seams, callers, owners, and tests.\n - Dependency relationships between tasks.\n - Candidate file locks and lock conflicts.\n@@ -152,7 +155,7 @@\n \n CCA must read:\n \n-- `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant ADRs.\n+- `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant SADRs and target ADRs.\n - Parent Goal issue, PRD issue, task issue, comments, and labels.\n - PR body, diff, commits, changed files, reviews, CI checks, and artifacts available to the workflow.\n - `.shiki/` task contract, locks, ledger entries, CCA checklist profile, and prior repair packets.\n@@ -214,7 +217,7 @@\n - Three or more new files.\n - Any high-risk or critical label.\n \n-When the architecture gate triggers, run `grill-with-docs` or `improve-codebase-architecture` as appropriate and record the decision in an ADR if it is hard to reverse or likely to be re-litigated.\n+When the architecture gate triggers, run `grill-with-docs` or `improve-codebase-architecture` as appropriate and record the decision in a Shiki SADR or target ADR as applicable if it is hard to reverse or likely to be re-litigated.\n \n ## 14. Skill Gate\n \n@@ -223,7 +226,7 @@\n Use these skills:\n \n - `setup-matt-pocock-skills`: before first use of the engineering skill set, or when issue tracker, triage labels, or domain docs are missing.\n-- `grill-with-docs`: default for non-trivial Goals before PRD/issues; required for ambiguous plans, terminology, boundaries, ADR-worthy tradeoffs, or design-tree decisions.\n+- `grill-with-docs`: default for non-trivial Goals before PRD/issues; required for ambiguous plans, terminology, boundaries, decision-record-worthy tradeoffs, or design-tree decisions.\n - `zoom-out`: unfamiliar code area or missing architectural map.\n - `to-prd`: convert settled context into a PRD.\n - `to-issues`: break a PRD, plan, or Goal into independently grabbable vertical-slice issues.\ndiff --git a/docs/adr/0001-keep-shiki-as-platform-name.md b/docs/adr/SADR-0001-keep-shiki-as-platform-name.md\nindex 20a9637c3b9d8c97de7ba8db7cfae4d82d9a7dba..8b685439959aada1b221c112f0ac3c51ca8a1695\n--- a/docs/adr/0001-keep-shiki-as-platform-name.md\n+++ b/docs/adr/SADR-0001-keep-shiki-as-platform-name.md\n@@ -1,3 +1,3 @@\n-# Keep Shiki As The Platform Name\n+# SADR-0001: Keep Shiki As The Platform Name\n \n Shiki remains the product name for the GitHub-native agentic engineering control plane. We considered more descriptive names such as MergePilot, MergeGate, and Kiroku, but kept Shiki because it carries the existing goal-seeking framework identity while remaining independent of Hermes, Codex, Claude Code, or any single runtime.\ndiff --git a/docs/adr/0002-use-agents-as-shared-constitution.md b/docs/adr/SADR-0002-use-agents-as-shared-constitution.md\nindex 73de30ecb078120eac540d9bc51855e3cd48d0e3..c5486182bf06fca36ea508496b013c67302fd989\n--- a/docs/adr/0002-use-agents-as-shared-constitution.md\n+++ b/docs/adr/SADR-0002-use-agents-as-shared-constitution.md\n@@ -1,3 +1,3 @@\n-# Use AGENTS As The Shared Agent Constitution\n+# SADR-0002: Use AGENTS As The Shared Agent Constitution\n \n Shiki uses `AGENTS.md` as the runtime-neutral constitution and `CLAUDE.md` as a thin Claude Code wrapper. This keeps Codex, Claude Code, Hermes Runner, and future Agent Runtimes aligned on the same GitHub-first MergeGate rules while still allowing runtime-specific instructions where needed.\ndiff --git a/docs/adr/0003-require-skill-gate-for-mergegate-readiness.md b/docs/adr/SADR-0003-require-skill-gate-for-mergegate-readiness.md\nindex 424be0c9af4c8f0136f1fde68a468c47ff84e764..7fc337987c7df9792f75f181dfcbed75fe5ffff2\n--- a/docs/adr/0003-require-skill-gate-for-mergegate-readiness.md\n+++ b/docs/adr/SADR-0003-require-skill-gate-for-mergegate-readiness.md\n@@ -1,3 +1,3 @@\n-# Require Skill Gate For MergeGate Readiness\n+# SADR-0003: Require Skill Gate For MergeGate Readiness\n \n Shiki requires relevant engineering skills to be invoked before execution when their triggers apply. This makes skills part of MergeGate readiness rather than optional prompt style, because Shiki depends on repeatable planning, issue decomposition, TDD, diagnosis, triage, and architecture review before autonomous or parallel agent work can be trusted.\ndiff --git a/docs/adr/0004-use-github-first-with-shiki-mirror.md b/docs/adr/SADR-0004-use-github-first-with-shiki-mirror.md\nindex d0a3a9174dc646a945a9d325954ffaf00c352040..a79c54ae9ee8eed2a0524192b864da4e22a92d3f\n--- a/docs/adr/0004-use-github-first-with-shiki-mirror.md\n+++ b/docs/adr/SADR-0004-use-github-first-with-shiki-mirror.md\n@@ -1,4 +1,4 @@\n-# ADR 0004: Use GitHub First With A Repository-Local Shiki Mirror\n+# SADR-0004: Use GitHub First With A Repository-Local Shiki Mirror\n \n ## Status\n \ndiff --git a/docs/adr/0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md b/docs/adr/SADR-0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\nindex b339baf49b89d9290e79c3d51b36e7b2bde3bd95..c30a0d8b1fde0224d0ac7820e7870dccf721580d\n--- a/docs/adr/0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\n+++ b/docs/adr/SADR-0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\n@@ -1,4 +1,4 @@\n-# ADR 0005: Use Codex As Front And Claude Code Action As The GitHub Runtime\n+# SADR-0005: Use Codex As Front And Claude Code Action As The GitHub Runtime\n \n ## Status\n \ndiff --git a/docs/adr/0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md b/docs/adr/SADR-0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\nindex 4bcae9d1e52f0a37b8e9fbafa5cd14b6ee1993bd..7a38538a6ae9929567f0ade67e7ea6a60d0b3f7e\n--- a/docs/adr/0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\n+++ b/docs/adr/SADR-0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\n@@ -1,4 +1,4 @@\n-# ADR 0006: Separate Codex Implementation, CCA Completion, And MergeGate Authorization\n+# SADR-0006: Separate Codex Implementation, CCA Completion, And MergeGate Authorization\n \n ## Status\n \ndiff --git a/docs/adr/0007-packaging-and-release.md b/docs/adr/SADR-0007-packaging-and-release.md\nindex 79b84ddb97167372b44641e5b7860e400ce80822..bb445a2e2e856d9d1f5fdbe35de8d4e8cec0fb32\n--- a/docs/adr/0007-packaging-and-release.md\n+++ b/docs/adr/SADR-0007-packaging-and-release.md\n@@ -1,4 +1,4 @@\n-# ADR 0007: Packaging And Release For Shiki\n+# SADR-0007: Packaging And Release For Shiki\n \n ## Status\n \n@@ -25,7 +25,7 @@\n   not introduce third-party runtime dependencies or break the shim.\n - Releases must run inside the operator's subscription-authenticated toolchain\n   and must not require extra secrets beyond the built-in `GITHUB_TOKEN`\n-  (consistent with ADR 0005's no-API-key-by-default stance).\n+  (consistent with SADR-0005's no-API-key-by-default stance).\n - `.shiki/manifest.json` already encodes the install surface (`install.include`,\n   `install.create_directories`, `install.exclude_from_commit`), and it must not\n   be mutated as part of this change.\n@@ -34,7 +34,7 @@\n \n This is a hard-to-reverse platform decision (it sets the version source of\n truth, the release trigger, and the public install contract), so it is recorded\n-as an ADR.\n+as a SADR.\n \n ## Decision\n \n@@ -104,7 +104,7 @@\n 3. **Publishing to PyPI.** Rejected for now: Shiki is distributed as a\n    repository template + git-installable CLI, not a PyPI library; PyPI would add\n    credentials/secrets and a different versioning contract. Can be added later as\n-   its own ADR if a packaged distribution is needed.\n+   its own SADR if a packaged distribution is needed.\n 4. **Dynamic version (read `VERSION` via a build backend `dynamic` field).**\n    Rejected to keep `pyproject.toml` backend-agnostic and dependency-free; the\n    small duplication between `VERSION` and `pyproject.toml` is guarded by the\n@@ -123,4 +123,4 @@\n   releasing versions the whole platform but ships only `install.include` to\n   targets.\n - Future PyPI distribution or a console entry point are deferred and would each\n-  warrant their own ADR.\n+  warrant their own SADR.\ndiff --git a/docs/adr/0008-use-claude-code-as-default-implementer.md b/docs/adr/SADR-0008-use-claude-code-as-default-implementer.md\nindex dedf4510b7c7799521422f622029563d373db031..35c2625d908167fbc58855b5b63e283805f167e8\n--- a/docs/adr/0008-use-claude-code-as-default-implementer.md\n+++ b/docs/adr/SADR-0008-use-claude-code-as-default-implementer.md\n@@ -1,12 +1,12 @@\n-# ADR 0008: Use Claude Code As The Default Implementer Runtime\n+# SADR-0008: Use Claude Code As The Default Implementer Runtime\n \n ## Status\n \n-Proposed (supersedes ADR 0005 for the implementer default; keeps its auth model)\n+Proposed (supersedes SADR-0005 for the implementer default; keeps its auth model)\n \n ## Context\n \n-ADR 0005 made Codex Front the default implementation runtime, with Claude Code\n+SADR-0005 made Codex Front the default implementation runtime, with Claude Code\n restricted to planner and reviewer roles by the runtime registry, and the only\n autonomous implementation adapter was `shiki runner codex`.\n \n@@ -18,13 +18,13 @@\n - The platform's planning, review, and CCA layers already run on Claude Code,\n   and Claude Code's native capabilities (the built-in `/code-review` skill and\n   the Workflow multi-agent orchestration tool) have no Codex equivalent.\n-- The Goal lifecycle redesign (ADR 0009) requires an autonomous post-freeze\n+- The Goal lifecycle redesign (SADR-0009) requires an autonomous post-freeze\n   loop driven from the operator's Claude Code session, including bounded\n   repair dispatch, which must not depend on a runtime that cannot be invoked\n   unattended.\n \n The constitution claims Shiki is runtime-agnostic (\"not a Claude-only\n-workflow\", ADR 0002). Any change to the implementer default must preserve the\n+workflow\", SADR-0002). Any change to the implementer default must preserve the\n registry-based runtime model rather than hardcoding one vendor.\n \n ## Decision\n@@ -39,7 +39,7 @@\n   the task to review or repair-needed.\n - Keep Codex registered as an optional implementer runtime. `shiki runner\n   codex`, the Codex handoff templates, and the ChatGPT OAuth auth model from\n-  ADR 0005 remain supported for tasks explicitly assigned to `codex`.\n+  SADR-0005 remain supported for tasks explicitly assigned to `codex`.\n - Commit to mandating two Claude-native capabilities as loop contract in\n   follow-up Goals (status: the code-review gate was delivered by Goal G-B \u2014\n   skill registry, checklists item PR-12, default required_skills \u2014 while the\n@@ -60,7 +60,7 @@\n \n Out of scope: removing Codex support, changing the CCA runtime (Claude Code\n Action with `CLAUDE_CODE_OAUTH_TOKEN` stays), and API-key based automation\n-(still requires its own ADR per ADR 0005).\n+(still requires its own target ADR per SADR-0005).\n \n ## Consequences\n \n@@ -100,7 +100,7 @@\n   harness rejection of direct `codex exec` is outside Shiki's control, and the\n   plan's mandatory native capabilities cannot run on Codex at all.\n - **Remove Codex entirely**: rejected; it abandons the runtime-agnostic\n-  constitution (ADR 0002) for no operational gain and forecloses\n+  constitution (SADR-0002) for no operational gain and forecloses\n   cross-runtime adversarial review.\n - **GitHub Actions cloud implementation via claude-code-action**: rejected as\n   default; durable evidence is automatic but token cost, Node 24 workflow\ndiff --git a/docs/adr/0009-requirements-definition-and-spec-freeze.md b/docs/adr/SADR-0009-requirements-definition-and-spec-freeze.md\nindex 29b3f7871bef9203a8b4a831a65c39eb2f506e42..52e7dd69ad0aa4efe0b68c62678abbf7df02aaec\n--- a/docs/adr/0009-requirements-definition-and-spec-freeze.md\n+++ b/docs/adr/SADR-0009-requirements-definition-and-spec-freeze.md\n@@ -1,4 +1,4 @@\n-# ADR 0009: Merge Grill And Planning Into Requirements Definition With Spec Freeze\n+# SADR-0009: Merge Grill And Planning Into Requirements Definition With Spec Freeze\n \n ## Status\n \ndiff --git a/docs/adr/0010-external-ai-guardian-review.md b/docs/adr/SADR-0010-external-ai-guardian-review.md\nindex e6108bbe8bb4e73e0ff4684c8f78001a80438c4e..f3510b145664298adafb151b6f0db51b972f01ec\n--- a/docs/adr/0010-external-ai-guardian-review.md\n+++ b/docs/adr/SADR-0010-external-ai-guardian-review.md\n@@ -1,4 +1,4 @@\n-# ADR 0010: External AI Guardian Review As A First-Class Approval Authority\n+# SADR-0010: External AI Guardian Review As A First-Class Approval Authority\n \n ## Status\n \n@@ -8,7 +8,7 @@\n \n Shiki is intended to be an autonomous, recursive, self-improving agent\n framework: authority-in-the-loop, not human-in-the-loop. Until now the Guardian\n-approval required for high/critical-risk merges (guardian-policy.json, ADR\n+approval required for high/critical-risk merges (guardian-policy.json, SADR\n governance) recognized only human authority sources \u2014 a GitHub review approval,\n the `guardian:approved` label applied by a configured Guardian, or a comment\n carrying `Guardian approval granted` plus the current head SHA.\n@@ -89,7 +89,7 @@\n   autonomous design the operator intends and produced the impersonation\n   incident.\n - **Record AI approval as an operator comment**: rejected as forgery of human\n-  authority \u2014 the exact failure this ADR corrects.\n+  authority \u2014 the exact failure this SADR corrects.\n - **Deliver the artifact as a committed `.shiki/` file**: rejected; a committed\n   approval inside the PR being approved is forgeable by anyone who can push.\n - **A dedicated reviewer bot identity instead of a relayed comment**: deferred;\ndiff --git a/docs/adr/0011-autonomous-quality-gate-evidence.md b/docs/adr/SADR-0011-autonomous-quality-gate-evidence.md\nindex bca0246c5d46e44afbde15486a75eec425fc02f5..9c93eac16d8c3a6d4b11bd7224e3955102cd7db4\n--- a/docs/adr/0011-autonomous-quality-gate-evidence.md\n+++ b/docs/adr/SADR-0011-autonomous-quality-gate-evidence.md\n@@ -1,4 +1,4 @@\n-# ADR 0011: Autonomous Quality-Gate Evidence \u2014 Deterministic Facts And An Independent Verifier\n+# SADR-0011: Autonomous Quality-Gate Evidence \u2014 Deterministic Facts And An Independent Verifier\n \n ## Status\n \ndiff --git a/docs/adr/0012-autonomous-loop-closeout-to-main.md b/docs/adr/SADR-0012-autonomous-loop-closeout-to-main.md\nindex 637fad6dde4989c9c2bf0f81f676d8d29991123c..ec7d85f81030a676bcbba28b43eb365422a36eac\n--- a/docs/adr/0012-autonomous-loop-closeout-to-main.md\n+++ b/docs/adr/SADR-0012-autonomous-loop-closeout-to-main.md\n@@ -1,4 +1,4 @@\n-# 0012 \u2014 Autonomous loop pushes goal completion to main via a closeout PR\n+# SADR-0012 \u2014 Autonomous loop pushes goal completion to main via a closeout PR\n \n - Status: accepted\n - Date: 2026-06-18\ndiff --git a/docs/adr/0013-bootstrap-grants-actions-approve-pull-requests.md b/docs/adr/SADR-0013-bootstrap-grants-actions-approve-pull-requests.md\nindex b6c69d64f1c5ced346b88d6859586715b3b5bd0e..cf48ff4a6716267f539ca58edd1a852e7c7a2d64\n--- a/docs/adr/0013-bootstrap-grants-actions-approve-pull-requests.md\n+++ b/docs/adr/SADR-0013-bootstrap-grants-actions-approve-pull-requests.md\n@@ -1,4 +1,4 @@\n-# ADR 0013: Bootstrap Grants GitHub Actions Permission To Create And Approve Pull Requests\n+# SADR-0013: Bootstrap Grants GitHub Actions Permission To Create And Approve Pull Requests\n \n ## Status\n \n@@ -31,7 +31,7 @@\n security tradeoff: it lets repository automation satisfy a human-review gate. In\n a multi-maintainer repo that could let a compromised or buggy workflow\n self-approve changes. So whether bootstrap turns it on by default \u2014 and with\n-what default token scope \u2014 is an ADR-worthy decision, not an implementation\n+what default token scope \u2014 is a SADR-worthy decision, not an implementation\n detail.\n \n ## Decision\n@@ -48,7 +48,7 @@\n Specifically:\n \n - **`can_approve_pull_request_reviews` defaults to `true`.** Shiki's default\n-  posture is solo/AFK autonomous operation (ADR 0008/0009/0012), where the CCA\n+  posture is solo/AFK autonomous operation (SADR-0008/0009/0012), where the CCA\n   Review Bridge is the only path that can satisfy `required_review: true` after\n   the CCA and Guardian gates have already passed. The default makes a\n   freshly-bootstrapped repo able to run the autonomous loop end to end without a\ndiff --git a/docs/adr/0014-codex-app-external-ai-guardian-ui-adapter.md b/docs/adr/SADR-0014-codex-app-external-ai-guardian-ui-adapter.md\nindex 238db06279334caa341c117362df9271382a7fcd..65f12e9939321e1b3e389a34c822fc1549e5155d\n--- a/docs/adr/0014-codex-app-external-ai-guardian-ui-adapter.md\n+++ b/docs/adr/SADR-0014-codex-app-external-ai-guardian-ui-adapter.md\n@@ -1,4 +1,4 @@\n-# ADR 0014: Use Codex App As The External AI Guardian UI Adapter\n+# SADR-0014: Use Codex App As The External AI Guardian UI Adapter\n \n ## Status\n \ndiff --git a/docs/adr/0015-contract-approval-before-dispatch.md b/docs/adr/SADR-0015-contract-approval-before-dispatch.md\nindex fad6c656741a20a7684ba16080a7db159f63a52f..6061271a46d3f1d6ae7174477de3fb75673123a6\n--- a/docs/adr/0015-contract-approval-before-dispatch.md\n+++ b/docs/adr/SADR-0015-contract-approval-before-dispatch.md\n@@ -1,4 +1,4 @@\n-# ADR 0015: Approve The Task Contract Before Dispatch, Not The Diff After It\n+# SADR-0015: Approve The Task Contract Before Dispatch, Not The Diff After It\n \n ## Status\n \n@@ -9,7 +9,7 @@\n Guardian approval for high/critical risk is expressible only on a pull request.\n Every approval source in `guardian-policy.json` \u2014 GitHub review, the\n `guardian:approved` label, the `Guardian approval granted` comment, and the\n-`external_ai_guardian_review` artifact (ADR 0010/0014) \u2014 is bound to a PR, and\n+`external_ai_guardian_review` artifact (SADR-0010/0014) \u2014 is bound to a PR, and\n three of the four require the current head SHA. Approval therefore cannot exist\n before an implementation PR exists.\n \n@@ -81,7 +81,7 @@\n An envelope breach must surface only on the non-repairable policy gate, never on\n the repairable `MergeGate metadata check`. Otherwise the Goal Loop routes it to\n `dispatch_repair` \u2014 instructing a runner to make the breach go away \u2014 which is\n-exactly what this ADR forbids.\n+exactly what this SADR forbids.\n \n **Contract immutability.** After registration, an implementation PR must not\n change the governance fields of its own task contract (`scope`, `non_goals`,\n@@ -116,7 +116,7 @@\n fails any verdict that is not `complete`, and the signal the CCA prompt reads is\n computed from `evaluate_guardian_approval` alone. Without the OR-merge at the\n signal, a contract-approved PR still returns `needs_guardian`, the CCA job fails,\n-and the MergeGate policy check never runs \u2014 the gate this ADR changes would never\n+and the MergeGate policy check never runs \u2014 the gate this SADR changes would never\n be reached. The CCA *prompt* needs no change: a `contract_approval` entry in the\n signal's existing `sources` array satisfies its current wording.\n \n@@ -130,7 +130,7 @@\n written, and an approved contract lets its implementation PRs merge through the\n same automatic path as low/medium risk. The wasted implement-then-block cycle\n disappears. Guardian review shifts from reading diffs to reading contracts, which\n-is both earlier and smaller; the external AI Guardian path (ADR 0010/0014) works\n+is both earlier and smaller; the external AI Guardian path (SADR-0010/0014) works\n on contracts with no change.\n \n We accept that the Guardian authorizes a class of change without seeing its\n@@ -146,7 +146,7 @@\n adversarial tests for the new approval path equivalent in rigor to\n `test_shiki_governance_evidence.sh`.\n \n-Bootstrap costs, which apply only while this ADR is being implemented. The tasks\n+Bootstrap costs, which apply only while this SADR is being implemented. The tasks\n that build Contract Approval merge under the existing rules, so the ones\n dispatched after the pre-dispatch gate lands but before the approval path lands\n need a Guardian-approved Contract PR *and* a live Guardian approval on their\n@@ -210,7 +210,7 @@\n risk; they do not bound authorized work.\n \n **Auto-escalate risk on envelope breach instead of stopping.** Returns the\n-workflow to a diff-time Guardian decision \u2014 the exact behavior this ADR removes.\n+workflow to a diff-time Guardian decision \u2014 the exact behavior this SADR removes.\n \n **Resolve governance fields from the base snapshot only.** Rejected against the\n registration flow: implementation PRs add their own contract, so the base has no\ndiff --git a/docs/adr/0016-locks-bound-product-paths.md b/docs/adr/SADR-0016-locks-bound-product-paths.md\nindex b5e850f3f1f7f4ef6e7f9d1763a29a51bd300c45..46b40a9b99a7aae644c02bb458f5473ff0a90ce2\n--- a/docs/adr/0016-locks-bound-product-paths.md\n+++ b/docs/adr/SADR-0016-locks-bound-product-paths.md\n@@ -1,4 +1,4 @@\n-# ADR 0016: Locks bound product paths; the Shiki mirror is bound by record identity\n+# SADR-0016: Locks bound product paths; the Shiki mirror is bound by record identity\n \n ## Status\n \n@@ -6,8 +6,8 @@\n \n - Date: 2026-07-28\n - Deciders: operator (mizutani-140), Claude (planner)\n-- Related: ADR 0004 (GitHub-first with a `.shiki` mirror), ADR 0009 (Spec Freeze),\n-  ADR 0012 (autonomous loop closeout to main), ADR 0015 (Contract Approval before\n+- Related: SADR-0004 (GitHub-first with a `.shiki` mirror), SADR-0009 (Spec Freeze),\n+  SADR-0012 (autonomous loop closeout to main), SADR-0015 (Contract Approval before\n   dispatch), remediation goal task R-02, the live PR #179 deadlock.\n \n ## Context\n@@ -90,11 +90,11 @@\n files, documentation files and named source modules are nameable; runtime-\n allocated ledger, runner and report ids are not \u2014 and those live in `.shiki`,\n which locks no longer govern. Existing frozen contracts are brought into line by\n-a one-time Spec Amendment per plan, which must land before ADR 0015's contract\n+a one-time Spec Amendment per plan, which must land before SADR-0015's contract\n immutability makes `locks` unwritable from the implementation PR.\n \n **Lock state is resolved from the durable side.** MergeGate resolves the lock\n-registry, and ADR 0015's governance fields, from the base `.shiki` snapshot when\n+registry, and SADR-0015's governance fields, from the base `.shiki` snapshot when\n present, falling back to head only when absent. The `MergeGate metadata check`\n job is given `--base-shiki`, which it does not receive today. Locks are not\n derived from the live open-pull-request set: a pull request body is untrusted\n@@ -108,7 +108,7 @@\n Explicitly out of scope: moving MergeGate's contract resolution off the PR\n checkout wholesale; making the mirror a projection of GitHub state; introducing\n new lock kinds; moving ledger evidence out of the repository; and unwinding\n-ADR 0012's closeout pull request. Registration of multi-task goals is ADR 0015's\n+SADR-0012's closeout pull request. Registration of multi-task goals is SADR-0015's\n Contract PR and is not re-decided here.\n \n ## Consequences\n@@ -127,7 +127,7 @@\n \n Risks accepted. First, goal-scoped identity is only as strong as `goal_id`,\n which is resolved from the PR body and the head task file; the mitigation is to\n-add `goal_id` to ADR 0015's frozen governance field set, and until that lands a\n+add `goal_id` to SADR-0015's frozen governance field set, and until that lands a\n task file can name a foreign goal. Second, base-when-present resolution is\n unavailable on fork pull requests, where the head is contributor-controlled;\n fork pull requests already cannot complete a Shiki merge, and this decision does\n@@ -139,7 +139,7 @@\n path, not by implementation pull requests.\n \n New obligations: one Spec Amendment per frozen plan to rewrite `locks`, ordered\n-before ADR 0015 T1; extension of the adversarial suites in\n+before SADR-0015 T1; extension of the adversarial suites in\n `scripts/test_shiki_adversarial_state.sh` and\n `scripts/test_shiki_governance_evidence.sh` to cover the eleven newly-scoped\n directories; and updating `scripts/test_shiki_loop_lock_guard.sh`,\n@@ -151,32 +151,32 @@\n whether the loop's transitive sync of ledger `evidence` references can be\n deleted once the Contract PR carries its own source plan.\n \n-## Relationship to ADR 0004\n+## Relationship to SADR-0004\n \n-This ADR **clarifies something ADR 0004 left unspecified**. ADR 0004 decides that\n+This SADR **clarifies something SADR-0004 left unspecified**. SADR-0004 decides that\n GitHub is the operational source of truth and that each target repository keeps a\n `.shiki/` mirror. It says nothing about pull requests writing the mirror, does\n not name MergeGate, and does not make the mirror an input to a gate. Nothing in\n-ADR 0004's Decision or Consequences is amended, superseded or weakened: the\n+SADR-0004's Decision or Consequences is amended, superseded or weakened: the\n mirror still exists, still records the same artifacts, and pull request\n-boundaries remain the coordination surface. What this ADR settles is the question\n+boundaries remain the coordination surface. What this SADR settles is the question\n 0004 never reached \u2014 by what authority a pull request may write a mirror record \u2014\n and it answers it the way `.shiki/manifest.json` already declares\n (`pr_mutation: current-task-or-current-goal-only`).\n \n-It **amends ADR 0012**, whose Decision item 5 states that \"Loop-executed tasks\n+It **amends SADR-0012**, whose Decision item 5 states that \"Loop-executed tasks\n declare the `path:.shiki/**` lock, which covers every `.shiki` file the closeout\n stages ... so `files_outside_locks` is satisfied without per-file lock\n bookkeeping.\" That clause is replaced: the closeout's mirror writes are\n-authorized by record identity, not by a declared lock. ADR 0012's substantive\n+authorized by record identity, not by a declared lock. SADR-0012's substantive\n decision \u2014 that completion reaches `main` through a closeout pull request rather\n than local attestation \u2014 is preserved unchanged.\n \n-It is **complementary to ADR 0015**, not a substitute. ADR 0015 owns contract\n+It is **complementary to SADR-0015**, not a substitute. SADR-0015 owns contract\n immutability, the pre-dispatch gate, the Contract PR and the Scope Envelope. This\n-ADR owns what a lock means and who may write the mirror. The one ordering\n+SADR owns what a lock means and who may write the mirror. The one ordering\n constraint between them is that the lock-narrowing Spec Amendment must precede\n-ADR 0015 T1's freezing of the `locks` field.\n+SADR-0015 T1's freezing of the `locks` field.\n \n ## Alternatives Considered\n \n@@ -186,7 +186,7 @@\n the only writer that can register them may run. Migrating the Guardian signal to\n that store makes every registration PR fail closed to `needs_guardian` at every\n risk level, because the task file is by definition absent from the store, which\n-removes the low/medium autonomous path ADR 0008/0009 exists to provide. And it\n+removes the low/medium autonomous path SADR-0008/0009 exists to provide. And it\n leaves schemas, manifest, config, guardian policy, templates, migrations, plans,\n worktrees, runner records, runs and handoffs with no writer at all in a\n repository whose product is `.shiki`. Measured benefit on the frozen backlog:\n@@ -199,14 +199,14 @@\n namespace is not in fact ownership-partitioned \u2014 56 ledger entries carry no\n `task_id`, and one goal DAG is writable by 35 registered tasks. The correct\n expression of its insight is that locks do not govern the mirror at all, which is\n-what this ADR decides.\n+what this SADR decides.\n \n **Make the mirror a projection written only by a registration writer and a\n post-merge projection job.** Rejected. It requires a bypass actor with write\n access to the protected default branch; it makes its own master invariant\n readable from a manifest that pull requests may edit; it deletes both reconcile\n modes and leaves a corrupted mirror with no in-band repair path, contradicting\n-ADR 0004's consequence that mirror conflicts are repaired against GitHub state;\n+SADR-0004's consequence that mirror conflicts are repaired against GitHub state;\n and its content-derived ids are rejected by `validate_shiki`'s currently-green id\n pattern.\n \ndiff --git a/docs/adr/0017-bookkeeping-closeouts-do-not-inherit-risk.md b/docs/adr/SADR-0017-bookkeeping-closeouts-do-not-inherit-risk.md\nindex 554e3a0d526a72ca067108c5464cc68c3edcf174..b9676857a4713850e063af7c39c8e0b01ecb4c66\n--- a/docs/adr/0017-bookkeeping-closeouts-do-not-inherit-risk.md\n+++ b/docs/adr/SADR-0017-bookkeeping-closeouts-do-not-inherit-risk.md\n@@ -1,4 +1,4 @@\n-# ADR 0017: A Bookkeeping Closeout Does Not Inherit Its Task's Risk\n+# SADR-0017: A Bookkeeping Closeout Does Not Inherit Its Task's Risk\n \n ## Status\n \n@@ -6,7 +6,7 @@\n \n ## Context\n \n-ADR 0012 made the goal loop push a task's completion to the default branch through a\n+SADR-0012 made the goal loop push a task's completion to the default branch through a\n closeout pull request: after the implementation PR merges, the loop opens a second PR\n carrying `task.status = done`, `lock.state = released`, and \u2014 when the task completes\n its goal \u2014 `goal.status = complete` plus one scorecard report. Nothing else. The\n@@ -49,14 +49,14 @@\n 4. The task file's governance fields (`scope`, `non_goals`, `required_skills`,\n    `risk_level`, `locks`, `acceptance_checks`, `test_command`) are byte-identical to\n    the base snapshot. Only `status`, `expected_pr`, `closeout_pr`, `ledger_evidence`\n-   and `expected_branch` may differ \u2014 the ADR 0015 contract-immutability rule,\n+   and `expected_branch` may differ \u2014 the SADR-0015 contract-immutability rule,\n    unchanged.\n \n    `expected_branch` is in that set because a closeout is *by construction* on a\n    different branch from the implementation it closes, and the loop rewrites the\n    field accordingly. It is safe to let it differ because MergeGate independently\n    requires `expected_branch` to equal the PR's actual head ref, so a PR cannot set\n-   it to anything other than the branch it is really on. The first draft of this ADR\n+   it to anything other than the branch it is really on. The first draft of this SADR\n    omitted it, which made the exemption unreachable: every real closeout failed\n    condition 4 on a field the loop always changes. See PR #209.\n 5. The state transitions are exactly the terminal set: task `review` \u2192 `done`, lock\n@@ -113,12 +113,12 @@\n classification must be derived, not asserted.\n \n **Fold the closeout into the implementation PR** so no second PR exists. Rejected under\n-ADR 0012, which recorded why: a task file that is already `done` on the implementation\n+SADR-0012, which recorded why: a task file that is already `done` on the implementation\n branch short-circuits the loop's review \u2192 CCA \u2192 merge \u2192 done ordering into a local\n goal-completion before any merge happens.\n \n **Have the loop auto-merge closeouts with elevated credentials.** Rejected outright: that\n-is a runtime authoring its own approval, which ADR 0010 and the constitution's\n+is a runtime authoring its own approval, which SADR-0010 and the constitution's\n approval-record prohibition forbid. It would also be indistinguishable, in the evidence,\n from the forgery those rules exist to prevent.\n \ndiff --git a/docs/adr/0018-carried-guardian-approval-across-a-proven-base-sync.md b/docs/adr/SADR-0018-carried-guardian-approval-across-a-proven-base-sync.md\nindex 22358562da390ed8b45cd600da08a43d378b4680..e6e2907cefa776460af73ddf7d39db605f3c39b0\n--- a/docs/adr/0018-carried-guardian-approval-across-a-proven-base-sync.md\n+++ b/docs/adr/SADR-0018-carried-guardian-approval-across-a-proven-base-sync.md\n@@ -1,4 +1,4 @@\n-# ADR 0018: Carried Guardian Approval Across A Proven Base Sync\n+# SADR-0018: Carried Guardian Approval Across A Proven Base Sync\n \n ## Status\n \n@@ -34,7 +34,7 @@\n Guardian evaluator that consumes it (`shiki_guardian.evaluate_guardian_approval`,\n the `guardian_comment_carried` source) were built and tested in isolation. Neither\n does anything until a caller passes the flags that turn the carry on, so the\n-feature was invisible in CI. This ADR records the decision the two of them\n+feature was invisible in CI. This SADR records the decision the two of them\n implement, and this task wires it into the four CI invocations that judge Guardian\n approval.\n \ndiff --git a/docs/adr/README.md b/docs/adr/README.md\nindex c70e057a4520c36e042ab68c53cd4a21ef709b71..5da43856836e2ce6a150f8127766e2f88355ab4b\n--- a/docs/adr/README.md\n+++ b/docs/adr/README.md\n@@ -1,40 +1,41 @@\n-# Architecture Decision Records\n+# Decision Record Namespaces\n \n-This directory holds Shiki's Architecture Decision Records (ADRs): durable\n-records of hard-to-reverse platform decisions. ADRs are part of the Shiki source\n-of truth alongside GitHub state, the `.shiki/` mirror, and `CONTEXT.md`.\n+This directory holds two independent decision namespaces:\n \n-## How ADRs Work\n+- Shiki Architecture Decision Records (SADRs), named\n+  `SADR-NNNN-<short-slug>.md`, record hard-to-reverse Shiki platform decisions.\n+- Target Architecture Decision Records (ADRs), named\n+  `NNNN-<short-slug>.md`, belong to the target product.\n \n-- New ADRs are added as `docs/adr/NNNN-<short-slug>.md`, where `NNNN` is the next\n-  zero-padded sequence number.\n-- The **canonical list of ADRs is the set of files matching `docs/adr/*.md`** in\n-  this directory. Browse `docs/adr/*.md` for the authoritative, current set.\n-- Use [`template.md`](template.md) as the starting point for a new ADR.\n-- Record an ADR when a decision is hard to reverse, surprising without context,\n-  and a real tradeoff. Do not write an ADR for routine or easily reversible\n-  choices.\n+The same four-digit number may appear once in each namespace. A platform\n+`SADR-NNNN` citation resolves only to `SADR-NNNN-*.md`; a target `ADR NNNN`\n+citation resolves only to `NNNN-*.md`.\n \n-Using the `docs/adr/*.md` glob as the source of truth means other PRs can add an\n-ADR without editing this index file, avoiding merge contention on a\n-hand-maintained enumeration.\n+## How Decision Records Work\n \n-## Current ADRs (snapshot)\n-\n-The list below is a convenience snapshot and may lag behind the directory. When\n-in doubt, list `docs/adr/*.md`.\n+- New Shiki platform decisions use the next zero-padded SADR number.\n+- New target product decisions use the target's own ADR sequence.\n+- The canonical list is the files in `docs/adr/`; no hand-maintained index is\n+  authoritative.\n+- Use [`template.md`](template.md) as the starting point and choose the filename\n+  namespace that owns the decision.\n+- Record a decision when it is hard to reverse, surprising without context, and\n+  a real tradeoff. Do not write a decision record for routine or easily\n+  reversible choices.\n \n-- [0001 \u2014 Keep Shiki As The Platform Name](0001-keep-shiki-as-platform-name.md)\n-- [0002 \u2014 Use AGENTS As The Shared Agent Constitution](0002-use-agents-as-shared-constitution.md)\n-- [0003 \u2014 Require Skill Gate For MergeGate Readiness](0003-require-skill-gate-for-mergegate-readiness.md)\n-- [0004 \u2014 Use GitHub First With A Repository-Local Shiki Mirror](0004-use-github-first-with-shiki-mirror.md)\n-- [0005 \u2014 Use Codex As Front And Claude Code Action As The GitHub Runtime](0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md)\n-- [0006 \u2014 Separate Codex Implementation, CCA Completion, And MergeGate Authorization](0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md)\n+Using the directory as the source of truth lets independent changes add records\n+without contending on an enumerated index.\n \n-## Adding A New ADR\n+## Adding A Shiki SADR\n \n-1. Copy `template.md` to `docs/adr/NNNN-<short-slug>.md`.\n+1. Copy `template.md` to `docs/adr/SADR-NNNN-<short-slug>.md`.\n 2. Fill in the title, status, context, decision, and consequences.\n-3. Set the status to `Proposed`, then `Accepted` once the decision is approved.\n-4. Open a PR following `CONTRIBUTING.md`. You do not need to edit this index for\n-   the ADR to be discoverable via `docs/adr/*.md`.\n+3. Set the status to `Proposed`, then `Accepted` once the proper authority has\n+   approved the decision.\n+4. Open a PR following `CONTRIBUTING.md`.\n+\n+## Adding A Target ADR\n+\n+1. Copy `template.md` to `docs/adr/NNNN-<short-slug>.md` using the target's own\n+   sequence.\n+2. Cite it as `ADR NNNN`; do not prefix its filename with `SADR-`.\ndiff --git a/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md b/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\nnew file mode 100644\nindex 0000000000000000000000000000000000000000..7455ce1f3bed02ddddb851ac21a1d1a648c9aeba\n--- /dev/null\n+++ b/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\n@@ -0,0 +1,55 @@\n+# SADR-0019: Separate Shiki Platform SADRs From Target ADRs\n+\n+## Status\n+\n+Accepted\n+\n+## Context\n+\n+Shiki installs its platform decision records into `docs/adr/` in every target\n+repository. Shiki and its targets previously used the same numeric\n+`NNNN-<slug>.md` namespace. A target that already owned ADRs 0013 through 0018\n+therefore acquired two records for each number during installation, making an\n+`ADR NNNN` citation ambiguous. Renumbering target decisions would rewrite target\n+history, while renumbering Shiki decisions per target would destroy their stable\n+platform identity.\n+\n+Upgrades also need to remove the former Shiki-owned numeric paths without ever\n+mistaking a target-owned ADR for platform content. The install stamp already\n+records exact shipped paths and content digests, so it can prove ownership only\n+when both still match.\n+\n+## Decision\n+\n+Shiki platform decisions use the `SADR` namespace:\n+\n+- records are named `docs/adr/SADR-NNNN-<slug>.md`;\n+- live platform citations use `SADR-NNNN`;\n+- target product decisions retain `docs/adr/NNNN-<slug>.md` and `ADR NNNN`;\n+- the reference validator resolves the two namespaces independently, so the same\n+  four-digit number may exist once in each namespace.\n+\n+Before a forced upgrade writes anything, the installer inventories the exact\n+former numeric Shiki ADR paths. It may delete a legacy path only when the\n+existing install stamp contains that exact path and its recorded SHA-256 digest\n+matches the file currently on disk. All ownership blockers are reported\n+together and abort the upgrade before its first write. A non-forced install\n+never performs this cleanup.\n+\n+Historical `.shiki/` evidence remains unchanged because it records the namespace\n+that existed when that evidence was produced.\n+\n+## Consequences\n+\n+- Targets may use any ADR number without colliding with Shiki's platform SADRs.\n+- Fresh installs preserve target ADR bytes and install a stable SADR set.\n+- Forced upgrades fail closed when legacy ownership cannot be proven, and a\n+  successful upgrade refreshes the stamp without removed legacy paths.\n+- Current governance, packaging, workflow, prompt, script, and skill references\n+  must move with the platform records.\n+\n+## Alternatives Considered\n+\n+- Collision-only refusal leaves affected targets permanently unupgradable.\n+- Renumbering target ADRs mutates target-owned history.\n+- Per-target Shiki renumbering makes platform citations non-portable.\ndiff --git a/docs/adr/template.md b/docs/adr/template.md\nindex a92a97daddd0b00ab9dd626cc8d86942da2eafed..17dd79175b5fee2105935557f691b40cf9492942\n--- a/docs/adr/template.md\n+++ b/docs/adr/template.md\n@@ -1,14 +1,14 @@\n-# NNNN: <Short Decision Title>\n+# <SADR-NNNN or NNNN>: <Short Decision Title>\n \n ## Status\n \n-Proposed | Accepted | Superseded by ADR-NNNN\n+Proposed | Accepted | Superseded by SADR-NNNN or target ADR NNNN\n \n ## Context\n \n What is the situation, constraint, or force that requires a decision? Reference\n-relevant domain terms (`CONTEXT.md`), prior ADRs, and code reality. State the\n-problem, not the solution.\n+relevant domain terms (`CONTEXT.md`), prior Shiki SADRs or target ADRs, and code\n+reality. State the problem, not the solution.\n \n ## Decision\n \ndiff --git a/docs/agents/autonomous-loop-e2e-acceptance.md b/docs/agents/autonomous-loop-e2e-acceptance.md\nindex bbf84c6081987f159ac6ad9ecf3c82b7aa72e7e8..5c13c1d02a05a90b0dc46cbef7c95a9846d0c237\n--- a/docs/agents/autonomous-loop-e2e-acceptance.md\n+++ b/docs/agents/autonomous-loop-e2e-acceptance.md\n@@ -40,8 +40,8 @@\n 3. `gh auth status` is logged in with merge permission on the repo.\n 4. Branch protection + required checks are configured exactly as the goal-loop\n    required-check set expects (see `docs/agents/branch-protection-smoke.md`).\n-5. The repo `.shiki/policy` permits autonomous low/medium-risk merges (ADR\n-   0008/0009); the MergeGate policy check is the recorded-authority gate.\n+5. The repo `.shiki/policy` permits autonomous low/medium-risk merges\n+   (SADR-0008/0009); the MergeGate policy check is the recorded-authority gate.\n \n The operator's only actions are: register the task and start the loop. **No\n manual pre-clearing** means after `shiki loop run` starts, the operator does not\ndiff --git a/docs/agents/checklists.md b/docs/agents/checklists.md\nindex 4a7f03ade346f53a1c1d82981f3fd744f9ab4547..0c5ec6c53dc7197b7b7a7d18e667b325babd6c45\n--- a/docs/agents/checklists.md\n+++ b/docs/agents/checklists.md\n@@ -32,18 +32,18 @@\n | ID | Item | Blocking |\n | --- | --- | --- |\n | GD-01 | Plan was challenged against `CONTEXT.md`. | blocking for non-trivial Goals |\n-| GD-02 | Relevant ADRs were read or absence was recorded. | blocking for non-trivial Goals |\n+| GD-02 | Relevant SADRs and target ADRs were read or absence was recorded. | blocking for non-trivial Goals |\n | GD-03 | Ambiguous terms were resolved or converted to HITL blockers. | blocking |\n | GD-04 | Edge scenarios were considered. | advisory |\n | GD-05 | Code reality was checked when code could answer. | blocking when applicable |\n | GD-06 | New domain terms were added to `CONTEXT.md` when needed. | blocking when applicable |\n-| GD-07 | ADR was created or explicitly skipped using the ADR criteria. | blocking when applicable |\n+| GD-07 | A SADR or target ADR was created or explicitly skipped using the decision-record criteria. | blocking when applicable |\n \n ## CI \u2014 Context & Impact Checklist\n \n | ID | Item | Blocking |\n | --- | --- | --- |\n-| CI-01 | Relevant docs and ADRs identified. | blocking |\n+| CI-01 | Relevant docs, SADRs, and target ADRs identified. | blocking |\n | CI-02 | Relevant modules, interfaces, seams, callers, and tests identified. | blocking |\n | CI-03 | Dependencies and blocked-by relationships identified. | blocking |\n | CI-04 | Candidate locks identified. | blocking |\n@@ -62,7 +62,7 @@\n | PRD-04 | Implementation decisions are recorded without volatile code detail. | blocking |\n | PRD-05 | Testing decisions are recorded. | blocking |\n | PRD-06 | Out-of-scope items are explicit. | blocking |\n-| PRD-07 | Relevant glossary/ADR links are present when applicable. | blocking |\n+| PRD-07 | Relevant glossary/SADR/target-ADR links are present when applicable. | blocking |\n \n ## SF \u2014 Spec Freeze Checklist\n \n@@ -98,7 +98,7 @@\n | H-02 | Branch/worktree target is present. | blocking |\n | H-03 | Scope and non-goals are present. | blocking |\n | H-04 | Dependencies and locks are present. | blocking |\n-| H-05 | Relevant docs, ADRs, modules, and tests are referenced. | blocking |\n+| H-05 | Relevant docs, SADRs, target ADRs, modules, and tests are referenced. | blocking |\n | H-06 | Required skills are stated. | blocking |\n | H-07 | TDD expectations are stated. | blocking when behavior work |\n | H-08 | Acceptance criteria are included. | blocking |\ndiff --git a/docs/agents/codex-handoff.md b/docs/agents/codex-handoff.md\nindex ede468f47cb277f69a857e8c45df703e4ed0151d..6f97bdffb67e44d7edea2c71e01ea28933c4eb6c\n--- a/docs/agents/codex-handoff.md\n+++ b/docs/agents/codex-handoff.md\n@@ -46,7 +46,7 @@\n \n ## Context\n - Domain terms:\n-- Relevant ADRs:\n+- Relevant SADRs and target ADRs:\n - Relevant docs:\n - Relevant modules/interfaces/seams:\n - Similar tests/prior art:\ndiff --git a/docs/agents/completion-check-agent.md b/docs/agents/completion-check-agent.md\nindex 13e4c035db35025f4e3cdcc5adfb24314ac4d521..61bfa1218cf36ca605250c91919e620f33238aa0\n--- a/docs/agents/completion-check-agent.md\n+++ b/docs/agents/completion-check-agent.md\n@@ -32,7 +32,7 @@\n - `AGENTS.md`\n - `CLAUDE.md`\n - `CONTEXT.md`\n-- relevant ADRs\n+- relevant SADRs and target ADRs\n - parent Goal issue\n - PRD issue or document\n - task issue\ndiff --git a/docs/agents/decision-control.md b/docs/agents/decision-control.md\nindex 7d8ee5ab03a5ccbcbd04056ae61de03785a2a9c9..8c94ecda00fffaa0e92c325ab83aa9776bd1dac1\n--- a/docs/agents/decision-control.md\n+++ b/docs/agents/decision-control.md\n@@ -11,7 +11,7 @@\n | Decision | Owner | When | Required inputs | Output | Enforcement surface |\n | --- | --- | --- | --- | --- | --- |\n | Is the Goal clear enough to plan? | Planner + Guardian when needed | Goal intake | User request, existing docs, risk signals | Goal readiness verdict | Goal checklist, issue template, ledger |\n-| Must `grill-with-docs` run? | Planner / Skill Gate | Before PRD or issue split | Ambiguity, domain terms, ADR conflicts, unclear boundaries | Skill invocation or explicit skip rationale | Skill Gate checklist |\n+| Must `grill-with-docs` run? | Planner / Skill Gate | Before PRD or issue split | Ambiguity, domain terms, SADR/target-ADR conflicts, unclear boundaries | Skill invocation or explicit skip rationale | Skill Gate checklist |\n | Is a PRD required? | Planner | After Goal context stabilizes | Goal, `grill-with-docs` output, Context & Impact output | PRD required / not required | PRD checklist, CCA evidence check |\n | Can the spec be frozen? | Operator only | End of Requirements Definition, before issue split | PRD/requirements summary, scope inventory (SF-02), open-question list | `spec_freeze` block in the plan + ledger evidence | `require_grilled_plan` (plan ingest/run), SF checklist |\n | Can a frozen spec change? | Operator only (Spec Amendment) | When implementation contradicts the frozen spec | Contested decisions, scoped re-grill result | recorded amendment + re-stamped freeze | SF checklist, ledger evidence |\ndiff --git a/docs/agents/domain.md b/docs/agents/domain.md\nindex 258da8ea81e87ac0a6b0b5b5e5c2fde980eaabb7..d57b0cf17ff3e1fa9e6e5885372bf083d8295421\n--- a/docs/agents/domain.md\n+++ b/docs/agents/domain.md\n@@ -5,7 +5,9 @@\n ## Layout\n \n - `CONTEXT.md` defines Shiki language. Keep it implementation-free.\n-- `docs/adr/` records hard-to-reverse decisions.\n+- `docs/adr/SADR-NNNN-*.md` records Shiki platform decisions.\n+- `docs/adr/NNNN-*.md` records target product decisions without sharing the\n+  SADR namespace.\n - `AGENTS.md` is the runtime-neutral agent constitution.\n - `CLAUDE.md` is the Claude Code wrapper around `AGENTS.md`.\n - `.shiki/` is the Target Repository mirror for Goals, PRDs, plans, Task DAGs, contracts, locks, worktree records, ledger entries, CCA verdicts, repair packets, reports, and handoffs.\n@@ -15,6 +17,6 @@\n \n - Use Shiki terms exactly when they exist.\n - Add glossary terms to `CONTEXT.md` only when the term is part of the domain, not a generic programming concept.\n-- Create ADRs only for hard-to-reverse, surprising, tradeoff-based decisions.\n+- Create SADRs or target ADRs only for hard-to-reverse, surprising, tradeoff-based decisions.\n - Keep product-specific language in the Target Repository, not in Shiki Template docs.\n-- If an ADR conflicts with a proposed implementation, surface the conflict before editing.\n+- If a SADR or target ADR conflicts with a proposed implementation, surface the conflict before editing.\ndiff --git a/docs/agents/execution-control.md b/docs/agents/execution-control.md\nindex ad8ee3cdb36be56fedc1f46408ca92b387d74047..bc63c3a73661d12b567016ba299f1a6b66bf9088\n--- a/docs/agents/execution-control.md\n+++ b/docs/agents/execution-control.md\n@@ -63,7 +63,7 @@\n | Branch name | Orchestrator | Orchestrator | Before implementer dispatch | branch policy | branch ref in task/ledger |\n | Lock reservation | Context & Impact | MergeGate | Before dispatch and before merge | `.shiki/locks/`, labels, PR metadata | lock id, owner, scope |\n | CI profile | Planner | MergeGate | During issue triage, rechecked at PR | labels, `.shiki/policy.yaml`, workflow matrix | required check list |\n-| Required status checks | Repo maintainer / Guardian | GitHub branch protection / rulesets | Repository setup, updated via ADR | GitHub settings | branch/ruleset config, ADR |\n+| Required status checks | Repo maintainer / Guardian | GitHub branch protection / rulesets | Repository setup, updated via target ADR | GitHub settings | branch/ruleset config, target ADR |\n | CCA judgment | CCA | GitHub required check / MergeGate | Every non-draft PR update | GitHub Action check | structured CCA verdict |\n | Repair assignment | CCA | MergeGate | After failed CCA/CI/review | repair packet, issue/PR comment | repair id and attempt count |\n | Merge readiness | MergeGate | GitHub branch protection / Guardian if needed | After CCA complete | required checks, review rules, rulesets | mergegate check output |\ndiff --git a/docs/agents/guardian-policy.md b/docs/agents/guardian-policy.md\nindex 4147c7adf05a47adb4fd24ada1a1c2e1f2ba6d2d..a20d9cc6c00a569bb057b9ca8d6b51c7fc4a9847\n--- a/docs/agents/guardian-policy.md\n+++ b/docs/agents/guardian-policy.md\n@@ -98,8 +98,8 @@\n `origin/<default_branch>` and reuses each job's existing base fetch; a shallow\n (`--depth`) or second base fetch would make the proof unresolvable and silently\n disable the carry, so neither is permitted. CCA reads `guardian_comment_carried` as\n-a recorded authority in the deterministic signal and does not re-derive it. See ADR\n-0018 for the full decision, the rejected diff/patch-equality and path-disjointness\n+a recorded authority in the deterministic signal and does not re-derive it. See\n+SADR-0018 for the full decision, the rejected diff/patch-equality and path-disjointness\n alternatives, and the accepted residual.\n \n ## External AI Guardian Review (SADR-0010)\ndiff --git a/docs/agents/implementation-policy.md b/docs/agents/implementation-policy.md\nindex c754144a9fb5bf4a38aa82a60e1670c59e626b44..85c37281bcad196c6b7b02343faf00b03cd9c57a\n--- a/docs/agents/implementation-policy.md\n+++ b/docs/agents/implementation-policy.md\n@@ -31,10 +31,13 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n ## Non-Negotiables\n \n 1. **Planning precedes implementation.** Non-trivial Goals start with `grill-with-docs` and Context & Impact.\n-2. **Domain language is controlled.** Terms must be checked against `CONTEXT.md`; hard-to-reverse tradeoffs must be checked against ADRs.\n+2. **Domain language is controlled.** Terms must be checked against `CONTEXT.md`; hard-to-reverse tradeoffs must be checked against Shiki SADRs and target ADRs.\n 3. **PRD precedes issue decomposition.** `to-prd` is used once the context is settled enough to become durable intent.\n 4. **Issues are vertical slices.** `to-issues` must produce independently grabbable tracer-bullet tasks, not horizontal layer tickets.\n 5. **Triage controls dispatch.** Only `ready-for-agent` / AFK tasks may be assigned to Codex without human input.\n@@ -95,19 +98,19 @@\n Process:\n \n - Challenge terms against `CONTEXT.md`.\n-- Check ADRs before reopening decisions.\n+- Check SADRs and target ADRs before reopening decisions.\n - Inspect code when code can answer a question.\n - Ask one question at a time when the operator is available.\n - Provide a recommended answer with each question.\n - Record resolved terms in `CONTEXT.md`.\n-- Create ADRs only for hard-to-reverse, surprising, tradeoff-based decisions.\n+- Create SADRs or target ADRs only for hard-to-reverse, surprising, tradeoff-based decisions.\n - Convert unresolved load-bearing questions into HITL blockers.\n \n Outputs:\n \n - Resolved design-tree decisions.\n - Updated glossary terms when needed.\n-- ADRs when justified.\n+- SADRs or target ADRs when justified.\n - HITL blockers when decisions remain unresolved.\n \n ## Phase 3 \u2014 Context & Impact\n@@ -117,7 +120,7 @@\n \n Outputs must include:\n \n-- Relevant documents and ADRs.\n+- Relevant documents, SADRs, and target ADRs.\n - Relevant modules, interfaces, seams, callers, and tests.\n - Candidate locks.\n - Dependencies.\n@@ -140,7 +143,7 @@\n - Testing decisions.\n - Out of scope.\n - Further notes.\n-- Links to relevant Goal, domain terms, and ADRs.\n+- Links to relevant Goal, domain terms, SADRs, and target ADRs.\n \n Phase 4 ends with Spec Freeze: the operator explicitly approves the PRD, the\n plan records a `spec_freeze` block (status, approved_by, source), and the SF\n@@ -240,7 +243,7 @@\n - Dependencies and locks.\n - Risk and Guardian requirements.\n - Ledger completeness.\n-- Documentation and ADR updates.\n+- Documentation and SADR/target-ADR updates.\n - Scope drift.\n \n CCA emits one of:\n@@ -301,7 +304,7 @@\n | Evidence failure | Behavior might be done, but proof is missing. | Add checks, PR evidence, ledger entry, or artifacts. |\n | Scope drift | PR changes unrelated behavior. | Revert unrelated changes or split into new Goal. |\n | Dependency block | Upstream task or lock not complete. | Block until dependency resolves. |\n-| Architecture block | Decision needs ADR or deep-module discussion. | `grill-with-docs` or `improve-codebase-architecture`. |\n+| Architecture block | Decision needs a SADR, target ADR, or deep-module discussion. | `grill-with-docs` or `improve-codebase-architecture`. |\n | Risk block | Security/production/policy change needs human approval. | Guardian decision. |\n \n ## Phase 5.5 \u2014 Execution Decision Control\ndiff --git a/docs/agents/runtime-auth-model.md b/docs/agents/runtime-auth-model.md\nindex 6a2e1a47e81ea89b5c68a210ec07772a9374babc..3c54748d24233dbd0ba8473950282547ef4ce935\n--- a/docs/agents/runtime-auth-model.md\n+++ b/docs/agents/runtime-auth-model.md\n@@ -148,7 +148,7 @@\n \n API-key based runners may be added by a target repository as an explicit extension. They are not the default Shiki template path.\n \n-If a repo opts into API-key mode, record the exception in an ADR and update the repo's `AGENTS.md`, `.shiki/config.yaml`, and workflow permissions.\n+If a repo opts into API-key mode, record the target-specific exception in a target ADR and update the repo's `AGENTS.md`, `.shiki/config.yaml`, and workflow permissions.\n \n ## GitHub Actions Runtime Compatibility\n \ndiff --git a/docs/agents/skill-gate.md b/docs/agents/skill-gate.md\nindex c50398b0d7c60a8598f2a5c88a10015730dced13..3acffe6444617bb1d54b77d4b4708158ec735bc6\n--- a/docs/agents/skill-gate.md\n+++ b/docs/agents/skill-gate.md\n@@ -9,7 +9,7 @@\n | Skill | Trigger | Expected output |\n | --- | --- | --- |\n | `setup-matt-pocock-skills` | First configuring a repo for these skills, or when issue tracker, triage labels, or domain docs are missing. | `docs/agents/` configuration and an `Agent skills` block in `AGENTS.md` or `CLAUDE.md`. |\n-| `grill-with-docs` | Ambiguous plan, unclear terminology, boundary decision, tradeoff, or ADR-worthy design choice. | Clarified decision tree, recommended answers, updates to `CONTEXT.md` or ADRs when decisions crystallize. |\n+| `grill-with-docs` | Ambiguous plan, unclear terminology, boundary decision, tradeoff, or decision-record-worthy design choice. | Clarified decision tree, recommended answers, updates to `CONTEXT.md`, SADRs, or target ADRs when decisions crystallize. |\n | `zoom-out` | Agent lacks a higher-level architectural map. | Map of relevant modules, callers, seams, and domain concepts. |\n | `to-prd` | Settled context should become a PRD. | PRD published to the issue tracker with problem, solution, user stories, decisions, testing, and out-of-scope notes. |\n | `to-issues` | Goal, PRD, or plan must become implementation tickets. | Vertical-slice issues with acceptance criteria, dependencies, and AFK/HITL classification. |\n@@ -17,7 +17,7 @@\n | `tdd` | Feature work or bug fix has observable behavior that can be specified with tests. | Red-green-refactor loop through public interfaces, one vertical slice at a time. |\n | `code-review` | The TDD loop is green and the implementer is about to create or update a PR. Default for every implementation task. | Pre-PR self-review of the task diff with fixes applied on the task branch; findings and resolutions recorded as a ledger entry naming the skill and a `## Pre-PR code review` PR body section. |\n | `diagnose` | Hard bug, failing check, regression, flaky behavior, or performance problem. | Reproduction loop, ranked hypotheses, instrumentation, fix, regression test, post-mortem. |\n-| `improve-codebase-architecture` | Structural friction, testability, AI-navigability, deep-module opportunity, or refactor request. | Architecture candidates and deepening recommendations informed by `CONTEXT.md` and ADRs. |\n+| `improve-codebase-architecture` | Structural friction, testability, AI-navigability, deep-module opportunity, or refactor request. | Architecture candidates and deepening recommendations informed by `CONTEXT.md`, SADRs, and target ADRs. |\n | `prototype` | A throwaway logic or UI prototype is needed to answer a design question. | Clearly marked disposable prototype and captured decision. |\n \n ## MergeGate Interaction\ndiff --git a/scripts/guardian_approval_signal.py b/scripts/guardian_approval_signal.py\nindex d059f92b2c82aa2b5d6ca421365537a87886c1cb..16924892714f4f5b9303c0ef0e325a7e70075630\n--- a/scripts/guardian_approval_signal.py\n+++ b/scripts/guardian_approval_signal.py\n@@ -199,8 +199,8 @@\n ) -> Any:\n     \"\"\"Evaluate SADR-0015 Contract Approval for the CCA Guardian signal.\n \n-    The SAME OR that MergeGate applies must reach this deterministic signal: ADR\n-    0015 is explicit that without it, a contract-approved PR still returns\n+    The SAME OR that MergeGate applies must reach this deterministic signal:\n+    SADR-0015 is explicit that without it, a contract-approved PR still returns\n     ``needs_guardian``, the CCA job fails, and the MergeGate policy check never\n     runs \u2014 the gate this change moves would never be reached. This resolves the\n     task the SAME way MergeGate does (the first Shiki task id in the PR body),\ndiff --git a/scripts/shiki_contract_approval.py b/scripts/shiki_contract_approval.py\nindex 941bcb96fdd8b9a85bc0c4f0f04d5dad9d04839c..4a4b418f6527413161db82670bec857a043360e2\n--- a/scripts/shiki_contract_approval.py\n+++ b/scripts/shiki_contract_approval.py\n@@ -142,7 +142,7 @@\n     changed_files_status: Any,\n     registration: Any,\n ) -> ContractApprovalResult:\n-    \"\"\"Whether this PR carries an SADR-0015 Contract Approval.\n+    \"\"\"Whether this PR carries a SADR-0015 Contract Approval.\n \n     The four conditions are evaluated IN ORDER, each failure recorded as a\n     distinct reason string:\ndiff --git a/scripts/shiki_guardian.py b/scripts/shiki_guardian.py\nindex a4ecb79bbda55fd5c8b06adfae2eb99760a03c45..4dd99bbe714e26f421fa19abac7a34ae1b195ecc\n--- a/scripts/shiki_guardian.py\n+++ b/scripts/shiki_guardian.py\n@@ -12,7 +12,7 @@\n # The carry re-derives its proof from git in-process on every judging invocation;\n # it is never read from an artifact, so there is nothing to go stale or be forged.\n # shiki_sync_proof is the ONLY guardian-family module that runs git, and every one\n-# of its invocations is read-only and local (ADR: prove-from-git base sync).\n+# of its invocations is read-only and local (SADR: prove-from-git base sync).\n from shiki_sync_proof import verify_pure_base_sync\n \n GUARDIAN_POLICY_PATH = \".shiki/guardian-policy.json\"\ndiff --git a/scripts/shiki_guardian_review.py b/scripts/shiki_guardian_review.py\nindex dd0f104d457899e0ba5ff1e162011c1e90b6f92b..e10ef58c046324e7ea3a3392feb3048ca77414ce\n--- a/scripts/shiki_guardian_review.py\n+++ b/scripts/shiki_guardian_review.py\n@@ -159,7 +159,7 @@\n     docs_only = _is_docs_only(changed_files)\n     if docs_only and matched == 0:\n         focus.append(\n-            \"Docs/ADR-only change: confirm the change is genuinely documentation/decision-only, introduces no behavior or contract drift, and stays consistent with the constitution and existing ADRs.\"\n+            \"Docs/decision-record-only change: confirm the change is genuinely documentation/decision-only, introduces no behavior or contract drift, and stays consistent with the constitution and existing SADRs and target ADRs.\"\n         )\n \n     # Conservative augmentation: unknown (no category and not docs-only), mixed\ndiff --git a/scripts/shiki_installer.py b/scripts/shiki_installer.py\nindex be064ab7a29ff6836f7f7a1d70500a43e0d11d7b..d3e3247edbf3e4be287a4667176d86f6500f8dc5\n--- a/scripts/shiki_installer.py\n+++ b/scripts/shiki_installer.py\n@@ -118,6 +118,7 @@\n # what lets a target carry it without a governance-schema change.\n INSTALL_STAMP_PATH = \".shiki/install-stamp.json\"\n INSTALL_STAMP_VERSION = 1\n+LEGACY_SHIKI_SADR_MAX = 18\n \n # Under --force the shipped surface splits three ways, so nothing is lost and\n # nothing is silently stale.\n@@ -237,9 +238,98 @@\n         )\n \n \n+def legacy_shiki_adr_paths() -> tuple[str, ...]:\n+    \"\"\"Former numeric paths for Shiki SADR-0001 through SADR-0018.\n+\n+    The authoritative slugs come from the current SADR filenames. SADR-0019 is\n+    the namespace decision itself and never shipped under a numeric legacy path.\n+    \"\"\"\n+    paths: list[str] = []\n+    for sadr in sorted((ROOT / \"docs\" / \"adr\").glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\")):\n+        number = sadr.name.split(\"-\", 2)[1]\n+        if int(number) <= LEGACY_SHIKI_SADR_MAX:\n+            paths.append(f\"docs/adr/{sadr.name.removeprefix('SADR-')}\")\n+    return tuple(paths)\n+\n+\n+def preflight_legacy_sadr_cleanup(target: Path, *, force: bool) -> tuple[str, ...]:\n+    \"\"\"Authorize exact legacy Shiki ADR removals before the first write.\n+\n+    A non-force install never removes existing files. A forced install needs no\n+    stamp when no legacy Shiki path is present. When legacy paths do exist, each\n+    must be named in a readable install stamp and its current bytes must match\n+    the recorded digest.\n+    \"\"\"\n+    if not force:\n+        return ()\n+    present = tuple(\n+        relative\n+        for relative in legacy_shiki_adr_paths()\n+        if (target / relative).exists() or (target / relative).is_symlink()\n+    )\n+    if not present:\n+        return ()\n+\n+    stamp_path = target / INSTALL_STAMP_PATH\n+    try:\n+        raw = stamp_path.read_text(encoding=\"utf-8\")\n+        stamp = json.loads(raw)\n+    except FileNotFoundError:\n+        detail = \"install stamp is absent\"\n+        blockers = [f\"{relative}: {detail}\" for relative in present]\n+    except (OSError, UnicodeDecodeError) as error:\n+        detail = f\"install stamp is unreadable ({error})\"\n+        blockers = [f\"{relative}: {detail}\" for relative in present]\n+    except json.JSONDecodeError as error:\n+        detail = f\"install stamp is malformed ({error})\"\n+        blockers = [f\"{relative}: {detail}\" for relative in present]\n+    else:\n+        if not isinstance(stamp, dict):\n+            blockers = [\n+                f\"{relative}: install stamp is malformed (expected an object)\"\n+                for relative in present\n+            ]\n+        else:\n+            digests = stamp.get(\"digests\")\n+            blockers = []\n+            if not isinstance(digests, dict):\n+                blockers.extend(\n+                    f\"{relative}: install stamp has no digest for this path\"\n+                    for relative in present\n+                )\n+            else:\n+                for relative in present:\n+                    expected = digests.get(relative)\n+                    if not isinstance(expected, str) or not expected:\n+                        blockers.append(\n+                            f\"{relative}: install stamp has no digest for this path\"\n+                        )\n+                        continue\n+                    try:\n+                        actual = _sha256_file(target / relative)\n+                    except OSError as error:\n+                        blockers.append(\n+                            f\"{relative}: legacy path is unreadable ({error})\"\n+                        )\n+                        continue\n+                    if actual != expected:\n+                        blockers.append(\n+                            f\"{relative}: digest mismatch (stamp {expected}, current {actual})\"\n+                        )\n+\n+    if blockers:\n+        joined = \"\\n\".join(f\"- {blocker}\" for blocker in blockers)\n+        raise ShikiError(\n+            \"legacy Shiki ADR cleanup is not authorized:\\n\"\n+            f\"{joined}\\nNo files were written.\"\n+        )\n+    return present\n+\n+\n def install_template(target: Path, *, force: bool, validate: bool) -> list[NewFileNote]:\n     # Preconditions first: never rewrite the tree of a target that cannot be\n     # upgraded cleanly (see refuse_pending_migrations).\n+    legacy_cleanup = preflight_legacy_sadr_cleanup(target, force=force)\n     refuse_pending_migrations(target)\n \n     # Resolve the target's required CODEOWNERS owner once from its\n@@ -250,6 +340,10 @@\n     # verbatim. See ``codeowners_required_owner``.\n     codeowners_owner = codeowners_required_owner(target)\n \n+    for relative in legacy_cleanup:\n+        (target / relative).unlink()\n+        info(f\"removed stamped legacy Shiki ADR: {target / relative}\")\n+\n     notes: list[NewFileNote] = []\n     for relative in TEMPLATE_PATHS:\n         source = ROOT / relative\ndiff --git a/scripts/shiki_loop.py b/scripts/shiki_loop.py\nindex 3611641da8aeb5038b1da90e3114b09a473ec6d8..84caa491b9cad0edc4176f209c13e9221e975681\n--- a/scripts/shiki_loop.py\n+++ b/scripts/shiki_loop.py\n@@ -424,7 +424,7 @@\n         # unresolvable CCA verdict has already produced its terminal stop and is never\n         # laundered into a head-moving sync (SADR-0010). It also never fires while the\n         # policy (Guardian) gate is red \u2014 for ANY reason, not only when it is the sole\n-        # red check \u2014 so a head-bound Guardian approval, or an SADR-0015\n+        # red check \u2014 so a head-bound Guardian approval, or a SADR-0015\n         # contract-approval carry that base movement defeated (the policy gate compares\n         # base and head task governance directly), is never silently restored by\n         # adopting base wholesale. It is bounded by a durable per-task counter so the\ndiff --git a/scripts/shiki_tasks.py b/scripts/shiki_tasks.py\nindex aec3809f24b4a0c8c96fc50971cff9ae9ad30381..d531052e9d9717058d4631483cefab44c26a65c1\n--- a/scripts/shiki_tasks.py\n+++ b/scripts/shiki_tasks.py\n@@ -821,7 +821,7 @@\n         \"prompt\": prompt,\n         \"entry_skill\": \"grill-with-docs\",\n         \"required_next_steps\": [\n-            \"Run grill-with-docs until terminology, boundaries, risks, and ADR-worthy decisions are settled.\",\n+            \"Run grill-with-docs until terminology, boundaries, risks, and decision-record-worthy choices are settled.\",\n             \"Obtain the operator's explicit approval of the PRD/requirements (Spec Freeze).\",\n             \"Write a machine-readable plan JSON with grill_with_docs.status=complete and spec_freeze.status=frozen.\",\n             \"Run shiki plan ingest --plan-file PLAN.json.\",\ndiff --git a/scripts/test_shiki_goal_loop.sh b/scripts/test_shiki_goal_loop.sh\nindex 753c7b516e10f0be3bc92a4bd0eb1fa699776ae6..8f3820a0a079b3044424cfe1a6c6b7fe68896e67\n--- a/scripts/test_shiki_goal_loop.sh\n+++ b/scripts/test_shiki_goal_loop.sh\n@@ -231,8 +231,8 @@\n \n # High/critical risk merges autonomously when all required checks are green:\n # the \"MergeGate policy check\" required check IS the Guardian gate (it enforces\n-# guardian-policy.json \u2014 human approval OR external AI guardian review, ADR\n-# 0010), so green checks mean a recorded authority approved. The fake gh returns\n+# guardian-policy.json \u2014 human approval OR external AI guardian review,\n+# SADR-0010), so green checks mean a recorded authority approved. The fake gh returns\n # green checks, simulating that the Guardian gate passed.\n python3 \"$ROOT/scripts/shiki.py\" goal create --target \"$TARGET\" --title \"Guardian gate\" --outcome \"High risk merges via policy gate\" >/tmp/shiki-goal-loop-guardian-goal.json\n GGOAL=\"$(json_get /tmp/shiki-goal-loop-guardian-goal.json goal_id)\"\ndiff --git a/tests/test_adr_references.py b/tests/test_adr_references.py\nindex 51706329c797d5dd9b47e082a333fa83b261d482..db6e693e35f67103935be34f1d9bb4ff7ee60395\n--- a/tests/test_adr_references.py\n+++ b/tests/test_adr_references.py\n@@ -1,7 +1,7 @@\n-\"\"\"Every ``ADR ####`` citation must resolve to exactly one decision record.\n+\"\"\"Every target ADR and Shiki SADR citation resolves in its own namespace.\n \n Background. ``scripts/mergegate_check.py`` justifies ``is_bookkeeping_closeout``\n-with \"ADR 0017\" nine times and ``scripts/guardian_approval_signal.py`` cites\n+with \"SADR-0017\" nine times and ``scripts/guardian_approval_signal.py`` cites\n 0016/0017 seven times, yet for a stretch the numbering in ``docs/adr/`` stopped\n at 0015: the authoring commits lived on a coordinator branch and no task ever\n declared ``docs/adr/...`` in its locks, so nothing carried the records to the\n@@ -11,11 +11,12 @@\n and the directory drifted with no signal.\n \n This is the same unbound-pair shape behind other silent failures: a reference\n-and its target with nothing asserting they agree. This test binds them. It scans\n-every ``scripts/*.py`` (plus ``docs/**/*.md`` and the root ``*.md`` governance\n-files, which are cheap) for the ``ADR ####`` pattern and asserts each cited\n-number resolves to exactly one ``docs/adr/####-*.md``. The converse is\n-deliberately NOT required: an ADR may exist without being cited.\n+and its target with nothing asserting they agree. This test binds them. Target\n+``ADR NNNN`` citations resolve only to ``docs/adr/NNNN-*.md``; Shiki\n+``SADR-NNNN`` citations resolve only to ``docs/adr/SADR-NNNN-*.md``. The scan\n+covers current governance docs, scripts, workflows, prompts, packaging metadata,\n+and repo-local Codex guidance while deliberately excluding historical\n+``.shiki`` evidence. The converse is not required: a record may be uncited.\n \n The suite is dependency free: standard-library ``unittest`` only.\n \"\"\"\n@@ -31,17 +32,18 @@\n \n REPO_ROOT = Path(__file__).resolve().parents[1]\n \n-# \"ADR 0016\" -- a four-digit citation. The trailing \\b keeps a five-digit token\n-# from being read as a truncated four-digit number.\n-_CITATION = re.compile(r\"\\bADR (\\d{4})\\b\")\n-# \"0016-locks-bound-product-paths.md\" -- the leading four digits name the record.\n-_ADR_FILENAME = re.compile(r\"^(\\d{4})-.*\\.md$\")\n+# ``ADR 0016`` / ``SADR-0016`` -- a namespace plus four-digit citation. The\n+# trailing ``\\b`` keeps a five-digit token from being read as a truncated one.\n+_CITATION = re.compile(r\"\\b(S?ADR)[ -](\\d{4})\\b\")\n+# Target ADRs retain the numeric filename; Shiki platform decisions use SADR.\n+_DECISION_FILENAME = re.compile(r\"^(?:(SADR)-)?(\\d{4})-.*\\.md$\")\n \n \n class Violation(NamedTuple):\n     \"\"\"A citation that does not resolve to exactly one decision record.\"\"\"\n \n     citing_file: str  # repo-relative path of the file that cites the ADR\n+    namespace: str  # \"ADR\" for target decisions; \"SADR\" for Shiki decisions\n     number: str  # the cited four-digit number, e.g. \"0016\"\n     line: int  # 1-based line of the citation\n     reason: str  # \"missing\" (zero matches) or \"ambiguous\" (two or more)\n@@ -49,44 +51,59 @@\n \n     @property\n     def expected_path(self) -> str:\n-        return f\"docs/adr/{self.number}-*.md\"\n+        prefix = \"SADR-\" if self.namespace == \"SADR\" else \"\"\n+        return f\"docs/adr/{prefix}{self.number}-*.md\"\n \n     def describe(self) -> str:\n         \"\"\"Name the citing file, the cited number, and the missing path.\"\"\"\n         if self.reason == \"missing\":\n             return (\n-                f\"{self.citing_file}:{self.line} cites ADR {self.number} \"\n+                f\"{self.citing_file}:{self.line} cites {self.namespace} {self.number} \"\n                 f\"but no document matches {self.expected_path}\"\n             )\n         joined = \", \".join(self.matches)\n         return (\n-            f\"{self.citing_file}:{self.line} cites ADR {self.number} which \"\n+            f\"{self.citing_file}:{self.line} cites {self.namespace} {self.number} which \"\n             f\"resolves to {len(self.matches)} documents (expected exactly one \"\n             f\"at {self.expected_path}): {joined}\"\n         )\n \n \n-def _adr_index(root: Path) -> dict[str, list[Path]]:\n-    \"\"\"Map every four-digit ADR number to the record files that claim it.\"\"\"\n-    index: dict[str, list[Path]] = {}\n+def _adr_index(root: Path) -> dict[tuple[str, str], list[Path]]:\n+    \"\"\"Map each decision namespace and number to the files that claim it.\"\"\"\n+    index: dict[tuple[str, str], list[Path]] = {}\n     adr_dir = root / \"docs\" / \"adr\"\n     if adr_dir.is_dir():\n         for path in sorted(adr_dir.glob(\"*.md\")):\n-            match = _ADR_FILENAME.match(path.name)\n+            match = _DECISION_FILENAME.match(path.name)\n             if match:\n-                index.setdefault(match.group(1), []).append(path)\n+                namespace = \"SADR\" if match.group(1) else \"ADR\"\n+                index.setdefault((namespace, match.group(2)), []).append(path)\n     return index\n \n \n def _citing_files(root: Path, include_docs: bool = True) -> list[Path]:\n-    \"\"\"Files scanned for citations: scripts/*.py, and cheaply the markdown.\"\"\"\n+    \"\"\"Current, shipped text surfaces scanned for decision citations.\"\"\"\n     files = sorted((root / \"scripts\").glob(\"*.py\"))\n+    files += sorted((root / \"scripts\").glob(\"*.sh\"))\n     if include_docs:\n         docs = root / \"docs\"\n         if docs.is_dir():\n             files += sorted(docs.rglob(\"*.md\"))\n         files += sorted(root.glob(\"*.md\"))\n-    return files\n+        pyproject = root / \"pyproject.toml\"\n+        if pyproject.is_file():\n+            files.append(pyproject)\n+        for relative in (\".github\", \".codex\"):\n+            surface = root / relative\n+            if surface.is_dir():\n+                files += sorted(\n+                    path\n+                    for path in surface.rglob(\"*\")\n+                    if path.is_file()\n+                    and path.suffix in {\".md\", \".py\", \".sh\", \".toml\", \".yaml\", \".yml\"}\n+                )\n+    return sorted(set(files))\n \n \n def find_adr_reference_violations(\n@@ -94,10 +111,9 @@\n ) -> list[Violation]:\n     \"\"\"Return every ``ADR ####`` citation under ``root`` that fails to resolve.\n \n-    A citation resolves when exactly one ``docs/adr/####-*.md`` matches its\n-    number. Zero matches is a \"missing\" violation; two or more is \"ambiguous\".\n-    Records that are never cited produce nothing -- the converse is not\n-    required.\n+    A citation resolves when exactly one record in the same namespace matches\n+    its number. Zero matches is a \"missing\" violation; two or more is\n+    \"ambiguous\". Records that are never cited produce nothing.\n     \"\"\"\n     root = Path(root)\n     index = _adr_index(root)\n@@ -110,13 +126,15 @@\n         rel = path.relative_to(root).as_posix()\n         for lineno, line in enumerate(text.splitlines(), start=1):\n             for match in _CITATION.finditer(line):\n-                number = match.group(1)\n-                found = index.get(number, [])\n+                namespace, number = match.groups()\n+                found = index.get((namespace, number), [])\n                 if len(found) == 1:\n                     continue\n                 reason = \"missing\" if not found else \"ambiguous\"\n                 rels = [p.relative_to(root).as_posix() for p in found]\n-                violations.append(Violation(rel, number, lineno, reason, rels))\n+                violations.append(\n+                    Violation(rel, namespace, number, lineno, reason, rels)\n+                )\n     return violations\n \n \n@@ -129,8 +147,8 @@\n         self.assertEqual(\n             violations,\n             [],\n-            \"Every 'ADR ####' citation must resolve to exactly one \"\n-            f\"docs/adr/####-*.md:\\n{detail}\",\n+            \"Every ADR/SADR citation must resolve exactly once in its own \"\n+            f\"docs/adr namespace:\\n{detail}\",\n         )\n \n     def test_scan_actually_reads_scripts(self) -> None:\n@@ -146,8 +164,21 @@\n         # The two records this Goal carried onto the default branch, and which\n         # the scripts cite most, must be present.\n         index = _adr_index(REPO_ROOT)\n-        self.assertIn(\"0016\", index)\n-        self.assertIn(\"0017\", index)\n+        self.assertIn((\"SADR\", \"0016\"), index)\n+        self.assertIn((\"SADR\", \"0017\"), index)\n+\n+    def test_platform_tree_has_only_sadr_0001_through_0019(self) -> None:\n+        adr_dir = REPO_ROOT / \"docs\" / \"adr\"\n+        sadrs = sorted(adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\"))\n+        self.assertEqual(\n+            {path.name.split(\"-\", 2)[1] for path in sadrs},\n+            {f\"{number:04d}\" for number in range(1, 20)},\n+        )\n+        self.assertEqual(\n+            list(adr_dir.glob(\"[0-9][0-9][0-9][0-9]-*.md\")),\n+            [],\n+            \"the Shiki platform tree must not retain legacy numeric SADR paths\",\n+        )\n \n \n class FixtureAdrReferences(unittest.TestCase):\n@@ -180,6 +211,26 @@\n         self.assertIn(\"9991\", message)  # cited number\n         self.assertIn(\"docs/adr/9991-*.md\", message)  # missing path\n \n+    def test_target_adr_does_not_satisfy_missing_platform_sadr(self) -> None:\n+        root = self._make_repo(\n+            adr_names=[\"0042-target-decision.md\"],\n+            script_body='\"\"\"Target ADR 0042 exists, but platform SADR-0042 does not.\"\"\"\\n',\n+        )\n+        violations = find_adr_reference_violations(root, include_docs=False)\n+        self.assertEqual(len(violations), 1)\n+        self.assertEqual(violations[0].namespace, \"SADR\")\n+        self.assertEqual(violations[0].number, \"0042\")\n+        self.assertEqual(violations[0].expected_path, \"docs/adr/SADR-0042-*.md\")\n+\n+    def test_target_adr_and_platform_sadr_may_reuse_a_number(self) -> None:\n+        root = self._make_repo(\n+            adr_names=[\"0042-target-decision.md\", \"SADR-0042-platform-decision.md\"],\n+            script_body='\"\"\"Target ADR 0042 and platform SADR-0042 are distinct.\"\"\"\\n',\n+        )\n+        self.assertEqual(\n+            find_adr_reference_violations(root, include_docs=False), []\n+        )\n+\n     def test_uncited_adr_is_allowed(self) -> None:\n         # An ADR exists but nothing cites it -> not a violation. The converse\n         # (every ADR must be cited) is explicitly NOT required.\ndiff --git a/tests/test_guardian_review.py b/tests/test_guardian_review.py\nindex 88829e7810212635e2fc98aa5918d6f26294677d..00a85a237acd5bf2d6cb5b294128198c422402ca\n--- a/tests/test_guardian_review.py\n+++ b/tests/test_guardian_review.py\n@@ -162,7 +162,7 @@\n \n     def test_docs_only_is_light_and_not_conservative(self):\n         focus = gr.classify_review_focus([\"docs/adr/0099-x.md\", \"README.md\"], \"\", \"low\")\n-        self.assertTrue(any(\"Docs/ADR-only\" in f for f in focus))\n+        self.assertTrue(any(\"Docs/decision-record-only\" in f for f in focus))\n         self.assertFalse(any(\"conservative default\" in f.lower() for f in focus))\n \n     def test_unknown_category_is_conservative(self):\ndiff --git a/tests/test_policy_job_merged_prs.py b/tests/test_policy_job_merged_prs.py\nindex d45512e74681e5e649279236c7d4e79d5c3da471..b97989f8bd74d091de1a6b5275a20d8dc68c4512\n--- a/tests/test_policy_job_merged_prs.py\n+++ b/tests/test_policy_job_merged_prs.py\n@@ -3,8 +3,8 @@\n \n ``.github/workflows/shiki-cca-completion.yml`` has two consumers of the merged-PR\n proof: the CCA *signal* (``guardian_approval_signal.py`` in \"Prepare CCA context\")\n-and the authoritative gate (``mergegate_check.py`` in \"Enforce MergeGate\"). ADR\n-0017's bookkeeping-closeout exemption (condition 6) needs the merged implementation\n+and the authoritative gate (``mergegate_check.py`` in \"Enforce MergeGate\").\n+SADR-0017's bookkeeping-closeout exemption (condition 6) needs the merged implementation\n PR proven, and a closeout carries NO ``post_merge_reconcile`` marker. The signal\n already resolves ``MERGED_PRS`` unconditionally, but \"Enforce MergeGate\" used to\n resolve it only inside ``grep -q '<!-- shiki:post_merge_reconcile -->'``. So a\ndiff --git a/tests/test_sadr_install_migration.py b/tests/test_sadr_install_migration.py\nnew file mode 100644\nindex 0000000000000000000000000000000000000000..a6f4c57e4dcaef73ef600b0d4804744138ab9071\n--- /dev/null\n+++ b/tests/test_sadr_install_migration.py\n@@ -0,0 +1,229 @@\n+\"\"\"SADR installation and collision-safe legacy upgrade behavior.\"\"\"\n+\n+from __future__ import annotations\n+\n+import contextlib\n+import hashlib\n+import io\n+import json\n+import shutil\n+import tempfile\n+import unittest\n+from pathlib import Path\n+\n+import shiki_test_support  # noqa: F401  (path bootstrap)\n+\n+import shiki_installer\n+from tests.test_adr_references import find_adr_reference_violations\n+\n+\n+PRODUCT_NUMBERS = tuple(range(13, 19))\n+\n+\n+def _quiet_install(target: Path, *, force: bool, validate: bool) -> None:\n+    with contextlib.redirect_stdout(io.StringIO()):\n+        shiki_installer.install_template(target, force=force, validate=validate)\n+\n+\n+def _sha256(path: Path) -> str:\n+    return hashlib.sha256(path.read_bytes()).hexdigest()\n+\n+\n+def _legacy_sadr_paths() -> tuple[str, ...]:\n+    return tuple(\n+        f\"docs/adr/{path.name.removeprefix('SADR-')}\"\n+        for path in sorted((shiki_installer.ROOT / \"docs\" / \"adr\").glob(\"SADR-*.md\"))\n+        if path.name != \"SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\"\n+    )\n+\n+\n+def _create_legacy_upgrade_fixture(\n+    target: Path,\n+) -> tuple[dict[str, bytes], tuple[str, ...]]:\n+    adr_dir = target / \"docs\" / \"adr\"\n+    adr_dir.mkdir(parents=True)\n+    product_bytes = {\n+        f\"{number:04d}-cubeshare-product-decision.md\": (\n+            f\"# CubeShare ADR {number:04d}\\n\\nDo not rewrite {number}.\\n\"\n+        ).encode()\n+        for number in PRODUCT_NUMBERS\n+    }\n+    for name, content in product_bytes.items():\n+        (adr_dir / name).write_bytes(content)\n+\n+    legacy_paths = _legacy_sadr_paths()\n+    for relative in legacy_paths:\n+        (target / relative).write_bytes(f\"legacy Shiki bytes for {relative}\\n\".encode())\n+\n+    stamp_path = target / shiki_installer.INSTALL_STAMP_PATH\n+    stamp_path.parent.mkdir(parents=True, exist_ok=True)\n+    stamp = {\n+        \"version\": shiki_installer.INSTALL_STAMP_VERSION,\n+        \"platform_commit\": None,\n+        \"installed_at\": \"2000-01-01T00:00:00+00:00\",\n+        \"digests\": {\n+            relative: _sha256(target / relative) for relative in legacy_paths\n+        },\n+    }\n+    stamp_path.write_text(\n+        json.dumps(stamp, indent=2, sort_keys=True) + \"\\n\", encoding=\"utf-8\"\n+    )\n+    return product_bytes, legacy_paths\n+\n+\n+def _tree_snapshot(root: Path) -> dict[str, tuple[str, bytes | str]]:\n+    \"\"\"Recursive byte/shape snapshot used to prove preflight is write-free.\"\"\"\n+    snapshot: dict[str, tuple[str, bytes | str]] = {}\n+    for path in sorted(root.rglob(\"*\")):\n+        relative = path.relative_to(root).as_posix()\n+        if path.is_symlink():\n+            snapshot[relative] = (\"symlink\", path.readlink().as_posix())\n+        elif path.is_dir():\n+            snapshot[relative] = (\"directory\", \"\")\n+        else:\n+            snapshot[relative] = (\"file\", path.read_bytes())\n+    return snapshot\n+\n+\n+class FreshSadrInstallTests(unittest.TestCase):\n+    def setUp(self) -> None:\n+        self.tmp = Path(tempfile.mkdtemp(prefix=\"shiki-sadr-fresh-\"))\n+        self.addCleanup(lambda: shutil.rmtree(self.tmp, ignore_errors=True))\n+        self.target = self.tmp / \"target\"\n+        self.adr_dir = self.target / \"docs\" / \"adr\"\n+        self.adr_dir.mkdir(parents=True)\n+        self.product_bytes = {\n+            f\"{number:04d}-product-decision.md\": (\n+                f\"# Product ADR {number:04d}\\n\\nTarget-owned bytes {number}.\\n\"\n+            ).encode()\n+            for number in PRODUCT_NUMBERS\n+        }\n+        for name, content in self.product_bytes.items():\n+            (self.adr_dir / name).write_bytes(content)\n+\n+    def test_fresh_install_preserves_product_adrs_and_installs_all_sadrs(self) -> None:\n+        _quiet_install(self.target, force=False, validate=True)\n+\n+        for name, expected in self.product_bytes.items():\n+            self.assertEqual((self.adr_dir / name).read_bytes(), expected)\n+\n+        sadr_numbers = {\n+            path.name.split(\"-\", 2)[1]\n+            for path in self.adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\")\n+        }\n+        self.assertEqual(sadr_numbers, {f\"{number:04d}\" for number in range(1, 20)})\n+\n+        numeric_records = {\n+            path.name\n+            for path in self.adr_dir.glob(\"[0-9][0-9][0-9][0-9]-*.md\")\n+        }\n+        self.assertEqual(numeric_records, set(self.product_bytes))\n+        self.assertEqual(find_adr_reference_violations(self.target), [])\n+\n+\n+class LegacySadrUpgradeTests(unittest.TestCase):\n+    def setUp(self) -> None:\n+        self.tmp = Path(tempfile.mkdtemp(prefix=\"shiki-sadr-upgrade-\"))\n+        self.addCleanup(lambda: shutil.rmtree(self.tmp, ignore_errors=True))\n+        self.target = self.tmp / \"target\"\n+        self.adr_dir = self.target / \"docs\" / \"adr\"\n+        self.product_bytes, self.legacy_paths = _create_legacy_upgrade_fixture(\n+            self.target\n+        )\n+\n+    def test_force_upgrade_deletes_only_stamped_legacy_sadrs(self) -> None:\n+        _quiet_install(self.target, force=True, validate=True)\n+\n+        for relative in self.legacy_paths:\n+            self.assertFalse((self.target / relative).exists(), relative)\n+        for name, expected in self.product_bytes.items():\n+            self.assertEqual((self.adr_dir / name).read_bytes(), expected)\n+\n+        sadrs = list(self.adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\"))\n+        self.assertEqual(len(sadrs), 19)\n+        refreshed = json.loads(\n+            (self.target / shiki_installer.INSTALL_STAMP_PATH).read_text(encoding=\"utf-8\")\n+        )\n+        for relative in self.legacy_paths:\n+            self.assertNotIn(relative, refreshed[\"digests\"])\n+        for path in sadrs:\n+            relative = path.relative_to(self.target).as_posix()\n+            self.assertEqual(refreshed[\"digests\"][relative], _sha256(path))\n+\n+    def test_non_force_install_never_deletes_legacy_files(self) -> None:\n+        before = {\n+            relative: (self.target / relative).read_bytes()\n+            for relative in self.legacy_paths\n+        }\n+        _quiet_install(self.target, force=False, validate=False)\n+        for relative, expected in before.items():\n+            self.assertEqual((self.target / relative).read_bytes(), expected)\n+        refreshed = json.loads(\n+            (self.target / shiki_installer.INSTALL_STAMP_PATH).read_text(encoding=\"utf-8\")\n+        )\n+        for relative in self.legacy_paths:\n+            self.assertEqual(refreshed[\"digests\"][relative], _sha256(self.target / relative))\n+\n+\n+class LegacyCleanupPreflightTests(unittest.TestCase):\n+    def setUp(self) -> None:\n+        self.tmp = Path(tempfile.mkdtemp(prefix=\"shiki-sadr-preflight-\"))\n+        self.addCleanup(lambda: shutil.rmtree(self.tmp, ignore_errors=True))\n+\n+    def test_all_ownership_blockers_abort_before_any_write(self) -> None:\n+        cases = (\n+            (\"absent\", \"install stamp is absent\"),\n+            (\"unreadable\", \"install stamp is unreadable\"),\n+            (\"malformed\", \"install stamp is malformed\"),\n+            (\"missing-digest\", \"install stamp has no digest\"),\n+            (\"digest-mismatch\", \"digest mismatch\"),\n+        )\n+        for name, expected_reason in cases:\n+            with self.subTest(name=name):\n+                target = self.tmp / name\n+                _, legacy_paths = _create_legacy_upgrade_fixture(target)\n+                stamp_path = target / shiki_installer.INSTALL_STAMP_PATH\n+\n+                if name == \"absent\":\n+                    stamp_path.unlink()\n+                elif name == \"unreadable\":\n+                    stamp_path.unlink()\n+                    stamp_path.mkdir()\n+                elif name == \"malformed\":\n+                    stamp_path.write_text(\"{not-json\\n\", encoding=\"utf-8\")\n+                else:\n+                    stamp = json.loads(stamp_path.read_text(encoding=\"utf-8\"))\n+                    if name == \"missing-digest\":\n+                        stamp[\"digests\"] = {}\n+                    else:\n+                        stamp[\"digests\"] = {\n+                            relative: \"deadbeef\" for relative in legacy_paths\n+                        }\n+                    stamp_path.write_text(\n+                        json.dumps(stamp, indent=2, sort_keys=True) + \"\\n\",\n+                        encoding=\"utf-8\",\n+                    )\n+\n+                before = _tree_snapshot(target)\n+                with self.assertRaises(shiki_installer.ShikiError) as raised:\n+                    _quiet_install(target, force=True, validate=False)\n+                after = _tree_snapshot(target)\n+\n+                self.assertEqual(before, after, f\"{name} preflight changed the tree\")\n+                message = str(raised.exception)\n+                self.assertIn(expected_reason, message)\n+                self.assertIn(\"No files were written\", message)\n+                for relative in legacy_paths:\n+                    self.assertIn(relative, message)\n+\n+    def test_absent_stamp_is_allowed_when_no_legacy_path_exists(self) -> None:\n+        target = self.tmp / \"no-legacy\"\n+        target.mkdir()\n+        _quiet_install(target, force=True, validate=False)\n+        self.assertTrue(\n+            (target / \"docs\" / \"adr\" / \"SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\").is_file()\n+        )\n+\n+\n+if __name__ == \"__main__\":\n+    unittest.main()\n\n2026-08-07T08:15:01.222036Z ERROR codex_models_manager::manager: failed to renew cache TTL: missing field `base_instructions` at line 94 column 5\nexec\n/bin/zsh -lc \"python3 -m unittest discover -s tests -p 'test_sadr_install_migration.py' -k non_force -v\" in /private/tmp/shiki-adr-collision.pa0KwY/.worktrees/shiki-t-20260807t073307221254z-2d22d654-introduc\ndiff --git a/.codex/skills/shiki/SKILL.md b/.codex/skills/shiki/SKILL.md\nindex c8e2e85d9d75d49cc4f2d6286ee22659c4e53e75..93cded623b2c15d9f65c76491a1c2d813ac9e6c0\n--- a/.codex/skills/shiki/SKILL.md\n+++ b/.codex/skills/shiki/SKILL.md\n@@ -18,6 +18,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n Codex CLI does not register this skill as a custom `/shiki` slash command. When\n the user types `/shiki` in Codex CLI and gets \"Unrecognized command\", explain\n that this is expected: invoke Shiki in Codex with natural language such as\n@@ -69,7 +72,7 @@\n \n ## Responsibilities\n \n-- Claude Code implements and repairs by default (`shiki runner claude`, ADR 0008); Codex implements and repairs tasks explicitly assigned to `codex`.\n+- Claude Code implements and repairs by default (`shiki runner claude`, SADR-0008); Codex implements and repairs tasks explicitly assigned to `codex`.\n - Claude Code Action can act as GitHub-side CCA or reviewer.\n - CCA judges completion.\n - MergeGate authorizes state transitions and merge readiness.\ndiff --git a/.github/prompts/cca-completion-check.md b/.github/prompts/cca-completion-check.md\nindex ed203ad5f302ecc81372d8ab27d2b1232ca97497..fd89950528333ef6242e6d5443904f5392ab7e56\n--- a/.github/prompts/cca-completion-check.md\n+++ b/.github/prompts/cca-completion-check.md\n@@ -19,6 +19,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n ## Required Reading\n \n Read only the files needed to judge the PR contract. Prefer this order:\ndiff --git a/.github/prompts/codex-task-handoff.md b/.github/prompts/codex-task-handoff.md\nindex 695e211d76dea1c60e8d5bb360e78487808286a4..59441d576b01a39569722f226c854e5a740e9eb5\n--- a/.github/prompts/codex-task-handoff.md\n+++ b/.github/prompts/codex-task-handoff.md\n@@ -2,7 +2,7 @@\n \n You are Codex Front acting as the Implementer for a Shiki task.\n \n-Follow `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant ADRs, and `docs/agents/codex-handoff.md`.\n+Follow `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant Shiki SADRs and target ADRs, and `docs/agents/codex-handoff.md`.\n \n Rules:\n \ndiff --git a/AGENTS.md b/AGENTS.md\nindex 7f041100d49c2a7525f51cb0f5d696a4a6d1c78c..a06bfbabc6fa48a007389e25e12d7ed523936bed\n--- a/AGENTS.md\n+++ b/AGENTS.md\n@@ -22,7 +22,7 @@\n - **Codex Front** is an optional implementer through Codex App, Codex CLI, Codex IDE extension, or Codex Web signed in with ChatGPT OAuth/subscription auth, dispatched with `shiki runner codex` when a task is explicitly assigned to `codex`.\n - **GitHub CCA** is implemented by Claude Code Action by default, using `CLAUDE_CODE_OAUTH_TOKEN`.\n \n-Do not assume `openai/codex-action`, `OPENAI_API_KEY`, or API-key based Codex automation in the default Shiki path. API-key runners are explicit target-repo extensions and require their own ADR.\n+Do not assume `openai/codex-action`, `OPENAI_API_KEY`, or API-key based Codex automation in the default Shiki path. API-key runners are explicit target-repo extensions and require their own target ADR.\n \n Shiki is not:\n \n@@ -44,10 +44,14 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use\n+`SADR-NNNN-*.md`; target product decisions retain `NNNN-*.md` and the `ADR`\n+namespace.\n+\n Use the canonical source-of-truth order above. Runtime-specific reading orders\n are operational guidance only and must not override or reorder it.\n \n-Conversation state is not durable truth. If a decision matters, put it in GitHub, `.shiki/`, `CONTEXT.md`, or an ADR.\n+Conversation state is not durable truth. If a decision matters, put it in GitHub, `.shiki/`, `CONTEXT.md`, a Shiki SADR, or a target ADR.\n \n If GitHub and `.shiki/` disagree, surface the conflict and prefer GitHub operational state until the mirror is repaired.\n \n@@ -75,8 +79,8 @@\n Every non-trivial change follows this loop:\n \n 1. **Goal**: clarify outcome, completion conditions, non-goals, risk level, and success signals.\n-2. **grill-with-docs**: challenge the plan against domain docs, ADRs, code reality, terminology, and edge scenarios.\n-3. **Context & Impact**: find relevant docs, ADRs, code, dependencies, owners, locks, and verification surfaces.\n+2. **grill-with-docs**: challenge the plan against domain docs, Shiki SADRs, target ADRs, code reality, terminology, and edge scenarios.\n+3. **Context & Impact**: find relevant docs, SADRs, target ADRs, code, dependencies, owners, locks, and verification surfaces.\n 4. **PRD and Spec Freeze**: use `to-prd` when context is settled enough to become durable product/engineering intent. The operator's explicit approval of the PRD is Spec Freeze: the plan records a `spec_freeze` block, and no plan runs without it. Steps 1-4 form Requirements Definition \u2014 one continuous operator dialogue. After Spec Freeze, scope changes require an operator-approved Spec Amendment; non-scope-moving interpretations go to the Assumption Log.\n 5. **Issues**: use `to-issues` to create independently grabbable vertical-slice issues. Prefer AFK slices over HITL slices where possible.\n 6. **Triage**: label issues for readiness, risk, runtime, skills, and MergeGate state.\n@@ -167,7 +171,7 @@\n - Testing decisions.\n - Out-of-scope items.\n - Further notes.\n-- Links to relevant domain terms and ADRs.\n+- Links to relevant domain terms, SADRs, and target ADRs.\n \n Every executable task must include:\n \n@@ -193,7 +197,7 @@\n \n Context & Impact must identify:\n \n-- Relevant documents, domain terms, ADRs, and past decisions.\n+- Relevant documents, domain terms, SADRs, target ADRs, and past decisions.\n - Relevant modules, interfaces, seams, callers, owners, and tests.\n - Dependency relationships between tasks.\n - Candidate locks and lock conflicts.\n@@ -210,7 +214,7 @@\n | Skill | Required when |\n | --- | --- |\n | `setup-matt-pocock-skills` | First configuring the repo for skills, or when issue tracker, triage labels, or domain docs are missing. |\n-| `grill-with-docs` | Default for non-trivial Goals before PRD/issues; always required when plans, terminology, boundaries, tradeoffs, or ADR-worthy decisions are ambiguous. |\n+| `grill-with-docs` | Default for non-trivial Goals before PRD/issues; always required when plans, terminology, boundaries, tradeoffs, or decision-record-worthy choices are ambiguous. |\n | `zoom-out` | The agent lacks an architectural map of the relevant code area. |\n | `to-prd` | Settled Goal context must become durable product/engineering intent. |\n | `to-issues` | A Goal, PRD, or plan must become independently grabbable vertical-slice issues. |\n@@ -348,7 +352,7 @@\n - Three or more new files.\n - Any high-risk or critical label.\n \n-When the architecture gate triggers, run `grill-with-docs` or `improve-codebase-architecture` as appropriate and record durable decisions in an ADR when needed.\n+When the architecture gate triggers, run `grill-with-docs` or `improve-codebase-architecture` as appropriate and record durable decisions in a Shiki SADR or target ADR as applicable.\n \n ## Engineering Discipline\n \ndiff --git a/CLAUDE.md b/CLAUDE.md\nindex 98a932e7841a632866fcd53b0bad0e3af3711d3d..98a31399404d6fb044b4fd493f4459ac87f439de\n--- a/CLAUDE.md\n+++ b/CLAUDE.md\n@@ -14,6 +14,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n ## Claude Role\n \n Default to planner, default implementer, reviewer, coordinator, documentation editor, CCA-style completion judge, and final-judgment assistant.\n@@ -57,7 +60,7 @@\n At the start of a Shiki session:\n \n 1. Read `AGENTS.md`.\n-2. Read `CONTEXT.md` and relevant ADRs.\n+2. Read `CONTEXT.md` and relevant Shiki SADRs and target ADRs.\n 3. Read active `.shiki/` Goal/task/ledger state when present.\n 4. Read active distilled rules from `.shiki/memories/` (status `distilled`, `active: true`, not revoked or superseded) for applicable guidance; the same rules are injected deterministically into task handoffs as the `## Distilled Rules` section (\u00a73.5).\n 5. Read the GitHub Issue or PR when the task is GitHub-backed.\n@@ -74,7 +77,7 @@\n - Define the Goal, completion conditions, non-goals, and risk level.\n - Treat `grill-with-docs` as the default planning entry point for non-trivial Goals.\n - Challenge domain terms against `CONTEXT.md`.\n-- Challenge hard-to-reverse tradeoffs against ADRs.\n+- Challenge hard-to-reverse tradeoffs against SADRs and target ADRs.\n - Explore code instead of asking when code can answer the question.\n - Run Context & Impact before implementation planning.\n - Produce a Task DAG, not an unordered checklist.\n@@ -94,7 +97,7 @@\n - For each question, provide your recommended answer.\n - Resolve dependency decisions before downstream design questions.\n - Update `CONTEXT.md` inline when terminology crystallizes.\n-- Offer ADRs only when the decision is hard to reverse, surprising without context, and a real tradeoff.\n+- Offer a SADR or target ADR only when the decision is hard to reverse, surprising without context, and a real tradeoff.\n - Convert unresolved questions into HITL blockers rather than allowing Codex to guess.\n \n ## PRD and Issues Standard\n@@ -103,7 +106,7 @@\n \n - Use the project's domain glossary vocabulary.\n - Avoid volatile file paths unless they are essential.\n-- Include implementation decisions, testing decisions, out-of-scope items, and relevant ADR/domain links.\n+- Include implementation decisions, testing decisions, out-of-scope items, and relevant SADR/target-ADR/domain links.\n - Identify deep-module opportunities and testable interfaces.\n \n When creating issues:\n@@ -121,7 +124,7 @@\n - Branch/worktree target.\n - Scope and non-goals.\n - Dependencies and locks.\n-- Relevant docs, ADRs, modules, and tests.\n+- Relevant docs, SADRs, target ADRs, modules, and tests.\n - Required skill invocations.\n - TDD expectations.\n - Pre-PR `code-review` gate expectations.\n@@ -152,7 +155,7 @@\n \n - Treat yourself as a judge, not implementer.\n - Do not write production code.\n-- Read `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant ADRs, PRD, task issue, PR body, diff, checks, reviews, labels, and `.shiki/` state.\n+- Read `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant SADRs and target ADRs, PRD, task issue, PR body, diff, checks, reviews, labels, and `.shiki/` state.\n - Evaluate every applicable checklist in `docs/agents/checklists.md`.\n - Map each acceptance criterion to durable evidence.\n - Distinguish implementation failure from missing evidence.\ndiff --git a/CONTEXT.md b/CONTEXT.md\nindex e61a9be06a0cce0d026484083a657bde2067e234..237ee8aa3ea8fab334ece49768bc5ed3b5af0974\n--- a/CONTEXT.md\n+++ b/CONTEXT.md\n@@ -25,7 +25,7 @@\n _Avoid_: jumping directly into code\n \n **grill-with-docs**:\n-A planning skill that challenges a plan against domain language, ADRs, code reality, and concrete edge scenarios. It resolves design-tree questions before PRD/issues. It runs inside Requirements Definition.\n+A planning skill that challenges a plan against domain language, Shiki SADRs, target ADRs, code reality, and concrete edge scenarios. It resolves design-tree questions before PRD/issues. It runs inside Requirements Definition.\n _Avoid_: generic brainstorming, silent assumptions\n \n **Requirements Definition**:\ndiff --git a/CONTRIBUTING.md b/CONTRIBUTING.md\nindex c6d9b656e73abb8a55e5f660123e03153dcb3e6d..9e37a8fbf668db5b22461aaae5548b8f1354394c\n--- a/CONTRIBUTING.md\n+++ b/CONTRIBUTING.md\n@@ -17,7 +17,7 @@\n 1. **Goal** \u2014 start from a user-approved Goal with an outcome, completion\n    conditions, non-goals, risk level, and success signals.\n 2. **grill-with-docs** \u2014 for any non-trivial change, challenge the plan against\n-   domain language, ADRs, and code reality before writing a PRD.\n+   domain language, Shiki SADRs, target ADRs, and code reality before writing a PRD.\n 3. **PRD** \u2014 capture settled product/engineering intent (`to-prd`).\n 4. **Spec Freeze** \u2014 the operator explicitly approves the PRD/requirements;\n    the plan records a `spec_freeze` block and no plan runs without it\n@@ -94,7 +94,7 @@\n   entries.\n - Do not hand-edit `.shiki/goals/*.json` or `.shiki/manifest.json` in a task PR.\n - Conversation state is not durable truth. If a decision matters, record it in\n-  GitHub, `.shiki/`, `CONTEXT.md`, or an ADR.\n+  GitHub, `.shiki/`, `CONTEXT.md`, a Shiki SADR, or a target ADR.\n \n ## Architecture Gate\n \n@@ -103,7 +103,7 @@\n or CI policy, package structure, three or more structural files, more than 100\n net new lines in a core module, three or more new files, or any high-risk label.\n When triggered, run `grill-with-docs` or `improve-codebase-architecture` and\n-record durable decisions in an ADR when needed (see `docs/adr/`).\n+record durable decisions in a Shiki SADR or target ADR as applicable (see `docs/adr/`).\n \n ## Safety\n \ndiff --git a/SYSTEM_PROMPT.md b/SYSTEM_PROMPT.md\nindex aaaea45e3a896c3a44ba89eec8bb84325ecf6c62..da009c86a88285269dee0155e58e550c6d744ea9\n--- a/SYSTEM_PROMPT.md\n+++ b/SYSTEM_PROMPT.md\n@@ -30,6 +30,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n Use the canonical source-of-truth order above. Runtime-specific reading orders\n are operational guidance only and must not override or reorder it. The current\n conversation is non-durable operator input.\n@@ -48,14 +51,14 @@\n \n A runtime may only do work that is explicitly assigned by the Goal, task, PR, or MergeGate decision.\n \n-Do not assume `openai/codex-action`, `OPENAI_API_KEY`, or API-key based Codex automation in the default Shiki path. API-key runners are explicit target-repo extensions and require their own ADR.\n+Do not assume `openai/codex-action`, `OPENAI_API_KEY`, or API-key based Codex automation in the default Shiki path. API-key runners are explicit target-repo extensions and require their own target ADR.\n \n ## 4. Non-negotiable Shiki flow\n \n For any non-trivial Goal, use this flow:\n \n 1. **Goal Seek**: define outcome, non-goals, risk level, completion criteria, and evidence requirements.\n-2. **grill-with-docs**: challenge the plan against `CONTEXT.md`, ADRs, code reality, terminology, boundaries, and edge scenarios. Ask one question at a time when the operator is available. If code can answer a question, inspect code instead of asking.\n+2. **grill-with-docs**: challenge the plan against `CONTEXT.md`, Shiki SADRs, target ADRs, code reality, terminology, boundaries, and edge scenarios. Ask one question at a time when the operator is available. If code can answer a question, inspect code instead of asking.\n 3. **Context & Impact**: identify modules, interfaces, seams, callers, dependencies, locks, risk, and verification surfaces.\n 4. **to-prd + Spec Freeze**: turn settled context into a PRD using domain vocabulary and testing decisions. The operator's explicit approval of the PRD is Spec Freeze, recorded as the plan's `spec_freeze` block; plans without it do not run. Steps 1-4 form Requirements Definition.\n 5. **to-issues**: decompose the PRD into independently grabbable vertical-slice issues. Prefer AFK slices; mark HITL slices when judgment is still required.\n@@ -75,7 +78,7 @@\n Before making any material change:\n \n 1. Read `AGENTS.md` and any runtime-specific wrapper such as `CLAUDE.md`.\n-2. Read `CONTEXT.md` and relevant ADRs.\n+2. Read `CONTEXT.md` and relevant SADRs and target ADRs.\n 3. Read the GitHub Issue / PR and `.shiki/` state for the active Goal or task.\n 4. Identify your role: Planner, Implementer, Reviewer, Completion Check Agent, Repairer, or Guardian-assist.\n 5. Identify Goal id, task id, branch, PR, dependencies, locks, risk level, required skills, and required evidence.\n@@ -103,7 +106,7 @@\n \n At minimum, identify:\n \n-- Relevant documents, ADRs, domain terms, and prior decisions.\n+- Relevant documents, SADRs, target ADRs, domain terms, and prior decisions.\n - Relevant modules, interfaces, seams, callers, owners, and tests.\n - Dependency relationships between tasks.\n - Candidate file locks and lock conflicts.\n@@ -152,7 +155,7 @@\n \n CCA must read:\n \n-- `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant ADRs.\n+- `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant SADRs and target ADRs.\n - Parent Goal issue, PRD issue, task issue, comments, and labels.\n - PR body, diff, commits, changed files, reviews, CI checks, and artifacts available to the workflow.\n - `.shiki/` task contract, locks, ledger entries, CCA checklist profile, and prior repair packets.\n@@ -214,7 +217,7 @@\n - Three or more new files.\n - Any high-risk or critical label.\n \n-When the architecture gate triggers, run `grill-with-docs` or `improve-codebase-architecture` as appropriate and record the decision in an ADR if it is hard to reverse or likely to be re-litigated.\n+When the architecture gate triggers, run `grill-with-docs` or `improve-codebase-architecture` as appropriate and record the decision in a Shiki SADR or target ADR as applicable if it is hard to reverse or likely to be re-litigated.\n \n ## 14. Skill Gate\n \n@@ -223,7 +226,7 @@\n Use these skills:\n \n - `setup-matt-pocock-skills`: before first use of the engineering skill set, or when issue tracker, triage labels, or domain docs are missing.\n-- `grill-with-docs`: default for non-trivial Goals before PRD/issues; required for ambiguous plans, terminology, boundaries, ADR-worthy tradeoffs, or design-tree decisions.\n+- `grill-with-docs`: default for non-trivial Goals before PRD/issues; required for ambiguous plans, terminology, boundaries, decision-record-worthy tradeoffs, or design-tree decisions.\n - `zoom-out`: unfamiliar code area or missing architectural map.\n - `to-prd`: convert settled context into a PRD.\n - `to-issues`: break a PRD, plan, or Goal into independently grabbable vertical-slice issues.\ndiff --git a/docs/adr/0001-keep-shiki-as-platform-name.md b/docs/adr/SADR-0001-keep-shiki-as-platform-name.md\nindex 20a9637c3b9d8c97de7ba8db7cfae4d82d9a7dba..8b685439959aada1b221c112f0ac3c51ca8a1695\n--- a/docs/adr/0001-keep-shiki-as-platform-name.md\n+++ b/docs/adr/SADR-0001-keep-shiki-as-platform-name.md\n@@ -1,3 +1,3 @@\n-# Keep Shiki As The Platform Name\n+# SADR-0001: Keep Shiki As The Platform Name\n \n Shiki remains the product name for the GitHub-native agentic engineering control plane. We considered more descriptive names such as MergePilot, MergeGate, and Kiroku, but kept Shiki because it carries the existing goal-seeking framework identity while remaining independent of Hermes, Codex, Claude Code, or any single runtime.\ndiff --git a/docs/adr/0002-use-agents-as-shared-constitution.md b/docs/adr/SADR-0002-use-agents-as-shared-constitution.md\nindex 73de30ecb078120eac540d9bc51855e3cd48d0e3..c5486182bf06fca36ea508496b013c67302fd989\n--- a/docs/adr/0002-use-agents-as-shared-constitution.md\n+++ b/docs/adr/SADR-0002-use-agents-as-shared-constitution.md\n@@ -1,3 +1,3 @@\n-# Use AGENTS As The Shared Agent Constitution\n+# SADR-0002: Use AGENTS As The Shared Agent Constitution\n \n Shiki uses `AGENTS.md` as the runtime-neutral constitution and `CLAUDE.md` as a thin Claude Code wrapper. This keeps Codex, Claude Code, Hermes Runner, and future Agent Runtimes aligned on the same GitHub-first MergeGate rules while still allowing runtime-specific instructions where needed.\ndiff --git a/docs/adr/0003-require-skill-gate-for-mergegate-readiness.md b/docs/adr/SADR-0003-require-skill-gate-for-mergegate-readiness.md\nindex 424be0c9af4c8f0136f1fde68a468c47ff84e764..7fc337987c7df9792f75f181dfcbed75fe5ffff2\n--- a/docs/adr/0003-require-skill-gate-for-mergegate-readiness.md\n+++ b/docs/adr/SADR-0003-require-skill-gate-for-mergegate-readiness.md\n@@ -1,3 +1,3 @@\n-# Require Skill Gate For MergeGate Readiness\n+# SADR-0003: Require Skill Gate For MergeGate Readiness\n \n Shiki requires relevant engineering skills to be invoked before execution when their triggers apply. This makes skills part of MergeGate readiness rather than optional prompt style, because Shiki depends on repeatable planning, issue decomposition, TDD, diagnosis, triage, and architecture review before autonomous or parallel agent work can be trusted.\ndiff --git a/docs/adr/0004-use-github-first-with-shiki-mirror.md b/docs/adr/SADR-0004-use-github-first-with-shiki-mirror.md\nindex d0a3a9174dc646a945a9d325954ffaf00c352040..a79c54ae9ee8eed2a0524192b864da4e22a92d3f\n--- a/docs/adr/0004-use-github-first-with-shiki-mirror.md\n+++ b/docs/adr/SADR-0004-use-github-first-with-shiki-mirror.md\n@@ -1,4 +1,4 @@\n-# ADR 0004: Use GitHub First With A Repository-Local Shiki Mirror\n+# SADR-0004: Use GitHub First With A Repository-Local Shiki Mirror\n \n ## Status\n \ndiff --git a/docs/adr/0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md b/docs/adr/SADR-0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\nindex b339baf49b89d9290e79c3d51b36e7b2bde3bd95..c30a0d8b1fde0224d0ac7820e7870dccf721580d\n--- a/docs/adr/0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\n+++ b/docs/adr/SADR-0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\n@@ -1,4 +1,4 @@\n-# ADR 0005: Use Codex As Front And Claude Code Action As The GitHub Runtime\n+# SADR-0005: Use Codex As Front And Claude Code Action As The GitHub Runtime\n \n ## Status\n \ndiff --git a/docs/adr/0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md b/docs/adr/SADR-0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\nindex 4bcae9d1e52f0a37b8e9fbafa5cd14b6ee1993bd..7a38538a6ae9929567f0ade67e7ea6a60d0b3f7e\n--- a/docs/adr/0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\n+++ b/docs/adr/SADR-0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\n@@ -1,4 +1,4 @@\n-# ADR 0006: Separate Codex Implementation, CCA Completion, And MergeGate Authorization\n+# SADR-0006: Separate Codex Implementation, CCA Completion, And MergeGate Authorization\n \n ## Status\n \ndiff --git a/docs/adr/0007-packaging-and-release.md b/docs/adr/SADR-0007-packaging-and-release.md\nindex 79b84ddb97167372b44641e5b7860e400ce80822..bb445a2e2e856d9d1f5fdbe35de8d4e8cec0fb32\n--- a/docs/adr/0007-packaging-and-release.md\n+++ b/docs/adr/SADR-0007-packaging-and-release.md\n@@ -1,4 +1,4 @@\n-# ADR 0007: Packaging And Release For Shiki\n+# SADR-0007: Packaging And Release For Shiki\n \n ## Status\n \n@@ -25,7 +25,7 @@\n   not introduce third-party runtime dependencies or break the shim.\n - Releases must run inside the operator's subscription-authenticated toolchain\n   and must not require extra secrets beyond the built-in `GITHUB_TOKEN`\n-  (consistent with ADR 0005's no-API-key-by-default stance).\n+  (consistent with SADR-0005's no-API-key-by-default stance).\n - `.shiki/manifest.json` already encodes the install surface (`install.include`,\n   `install.create_directories`, `install.exclude_from_commit`), and it must not\n   be mutated as part of this change.\n@@ -34,7 +34,7 @@\n \n This is a hard-to-reverse platform decision (it sets the version source of\n truth, the release trigger, and the public install contract), so it is recorded\n-as an ADR.\n+as a SADR.\n \n ## Decision\n \n@@ -104,7 +104,7 @@\n 3. **Publishing to PyPI.** Rejected for now: Shiki is distributed as a\n    repository template + git-installable CLI, not a PyPI library; PyPI would add\n    credentials/secrets and a different versioning contract. Can be added later as\n-   its own ADR if a packaged distribution is needed.\n+   its own SADR if a packaged distribution is needed.\n 4. **Dynamic version (read `VERSION` via a build backend `dynamic` field).**\n    Rejected to keep `pyproject.toml` backend-agnostic and dependency-free; the\n    small duplication between `VERSION` and `pyproject.toml` is guarded by the\n@@ -123,4 +123,4 @@\n   releasing versions the whole platform but ships only `install.include` to\n   targets.\n - Future PyPI distribution or a console entry point are deferred and would each\n-  warrant their own ADR.\n+  warrant their own SADR.\ndiff --git a/docs/adr/0008-use-claude-code-as-default-implementer.md b/docs/adr/SADR-0008-use-claude-code-as-default-implementer.md\nindex dedf4510b7c7799521422f622029563d373db031..35c2625d908167fbc58855b5b63e283805f167e8\n--- a/docs/adr/0008-use-claude-code-as-default-implementer.md\n+++ b/docs/adr/SADR-0008-use-claude-code-as-default-implementer.md\n@@ -1,12 +1,12 @@\n-# ADR 0008: Use Claude Code As The Default Implementer Runtime\n+# SADR-0008: Use Claude Code As The Default Implementer Runtime\n \n ## Status\n \n-Proposed (supersedes ADR 0005 for the implementer default; keeps its auth model)\n+Proposed (supersedes SADR-0005 for the implementer default; keeps its auth model)\n \n ## Context\n \n-ADR 0005 made Codex Front the default implementation runtime, with Claude Code\n+SADR-0005 made Codex Front the default implementation runtime, with Claude Code\n restricted to planner and reviewer roles by the runtime registry, and the only\n autonomous implementation adapter was `shiki runner codex`.\n \n@@ -18,13 +18,13 @@\n - The platform's planning, review, and CCA layers already run on Claude Code,\n   and Claude Code's native capabilities (the built-in `/code-review` skill and\n   the Workflow multi-agent orchestration tool) have no Codex equivalent.\n-- The Goal lifecycle redesign (ADR 0009) requires an autonomous post-freeze\n+- The Goal lifecycle redesign (SADR-0009) requires an autonomous post-freeze\n   loop driven from the operator's Claude Code session, including bounded\n   repair dispatch, which must not depend on a runtime that cannot be invoked\n   unattended.\n \n The constitution claims Shiki is runtime-agnostic (\"not a Claude-only\n-workflow\", ADR 0002). Any change to the implementer default must preserve the\n+workflow\", SADR-0002). Any change to the implementer default must preserve the\n registry-based runtime model rather than hardcoding one vendor.\n \n ## Decision\n@@ -39,7 +39,7 @@\n   the task to review or repair-needed.\n - Keep Codex registered as an optional implementer runtime. `shiki runner\n   codex`, the Codex handoff templates, and the ChatGPT OAuth auth model from\n-  ADR 0005 remain supported for tasks explicitly assigned to `codex`.\n+  SADR-0005 remain supported for tasks explicitly assigned to `codex`.\n - Commit to mandating two Claude-native capabilities as loop contract in\n   follow-up Goals (status: the code-review gate was delivered by Goal G-B \u2014\n   skill registry, checklists item PR-12, default required_skills \u2014 while the\n@@ -60,7 +60,7 @@\n \n Out of scope: removing Codex support, changing the CCA runtime (Claude Code\n Action with `CLAUDE_CODE_OAUTH_TOKEN` stays), and API-key based automation\n-(still requires its own ADR per ADR 0005).\n+(still requires its own target ADR per SADR-0005).\n \n ## Consequences\n \n@@ -100,7 +100,7 @@\n   harness rejection of direct `codex exec` is outside Shiki's control, and the\n   plan's mandatory native capabilities cannot run on Codex at all.\n - **Remove Codex entirely**: rejected; it abandons the runtime-agnostic\n-  constitution (ADR 0002) for no operational gain and forecloses\n+  constitution (SADR-0002) for no operational gain and forecloses\n   cross-runtime adversarial review.\n - **GitHub Actions cloud implementation via claude-code-action**: rejected as\n   default; durable evidence is automatic but token cost, Node 24 workflow\ndiff --git a/docs/adr/0009-requirements-definition-and-spec-freeze.md b/docs/adr/SADR-0009-requirements-definition-and-spec-freeze.md\nindex 29b3f7871bef9203a8b4a831a65c39eb2f506e42..52e7dd69ad0aa4efe0b68c62678abbf7df02aaec\n--- a/docs/adr/0009-requirements-definition-and-spec-freeze.md\n+++ b/docs/adr/SADR-0009-requirements-definition-and-spec-freeze.md\n@@ -1,4 +1,4 @@\n-# ADR 0009: Merge Grill And Planning Into Requirements Definition With Spec Freeze\n+# SADR-0009: Merge Grill And Planning Into Requirements Definition With Spec Freeze\n \n ## Status\n \ndiff --git a/docs/adr/0010-external-ai-guardian-review.md b/docs/adr/SADR-0010-external-ai-guardian-review.md\nindex e6108bbe8bb4e73e0ff4684c8f78001a80438c4e..f3510b145664298adafb151b6f0db51b972f01ec\n--- a/docs/adr/0010-external-ai-guardian-review.md\n+++ b/docs/adr/SADR-0010-external-ai-guardian-review.md\n@@ -1,4 +1,4 @@\n-# ADR 0010: External AI Guardian Review As A First-Class Approval Authority\n+# SADR-0010: External AI Guardian Review As A First-Class Approval Authority\n \n ## Status\n \n@@ -8,7 +8,7 @@\n \n Shiki is intended to be an autonomous, recursive, self-improving agent\n framework: authority-in-the-loop, not human-in-the-loop. Until now the Guardian\n-approval required for high/critical-risk merges (guardian-policy.json, ADR\n+approval required for high/critical-risk merges (guardian-policy.json, SADR\n governance) recognized only human authority sources \u2014 a GitHub review approval,\n the `guardian:approved` label applied by a configured Guardian, or a comment\n carrying `Guardian approval granted` plus the current head SHA.\n@@ -89,7 +89,7 @@\n   autonomous design the operator intends and produced the impersonation\n   incident.\n - **Record AI approval as an operator comment**: rejected as forgery of human\n-  authority \u2014 the exact failure this ADR corrects.\n+  authority \u2014 the exact failure this SADR corrects.\n - **Deliver the artifact as a committed `.shiki/` file**: rejected; a committed\n   approval inside the PR being approved is forgeable by anyone who can push.\n - **A dedicated reviewer bot identity instead of a relayed comment**: deferred;\ndiff --git a/docs/adr/0011-autonomous-quality-gate-evidence.md b/docs/adr/SADR-0011-autonomous-quality-gate-evidence.md\nindex bca0246c5d46e44afbde15486a75eec425fc02f5..9c93eac16d8c3a6d4b11bd7224e3955102cd7db4\n--- a/docs/adr/0011-autonomous-quality-gate-evidence.md\n+++ b/docs/adr/SADR-0011-autonomous-quality-gate-evidence.md\n@@ -1,4 +1,4 @@\n-# ADR 0011: Autonomous Quality-Gate Evidence \u2014 Deterministic Facts And An Independent Verifier\n+# SADR-0011: Autonomous Quality-Gate Evidence \u2014 Deterministic Facts And An Independent Verifier\n \n ## Status\n \ndiff --git a/docs/adr/0012-autonomous-loop-closeout-to-main.md b/docs/adr/SADR-0012-autonomous-loop-closeout-to-main.md\nindex 637fad6dde4989c9c2bf0f81f676d8d29991123c..ec7d85f81030a676bcbba28b43eb365422a36eac\n--- a/docs/adr/0012-autonomous-loop-closeout-to-main.md\n+++ b/docs/adr/SADR-0012-autonomous-loop-closeout-to-main.md\n@@ -1,4 +1,4 @@\n-# 0012 \u2014 Autonomous loop pushes goal completion to main via a closeout PR\n+# SADR-0012 \u2014 Autonomous loop pushes goal completion to main via a closeout PR\n \n - Status: accepted\n - Date: 2026-06-18\ndiff --git a/docs/adr/0013-bootstrap-grants-actions-approve-pull-requests.md b/docs/adr/SADR-0013-bootstrap-grants-actions-approve-pull-requests.md\nindex b6c69d64f1c5ced346b88d6859586715b3b5bd0e..cf48ff4a6716267f539ca58edd1a852e7c7a2d64\n--- a/docs/adr/0013-bootstrap-grants-actions-approve-pull-requests.md\n+++ b/docs/adr/SADR-0013-bootstrap-grants-actions-approve-pull-requests.md\n@@ -1,4 +1,4 @@\n-# ADR 0013: Bootstrap Grants GitHub Actions Permission To Create And Approve Pull Requests\n+# SADR-0013: Bootstrap Grants GitHub Actions Permission To Create And Approve Pull Requests\n \n ## Status\n \n@@ -31,7 +31,7 @@\n security tradeoff: it lets repository automation satisfy a human-review gate. In\n a multi-maintainer repo that could let a compromised or buggy workflow\n self-approve changes. So whether bootstrap turns it on by default \u2014 and with\n-what default token scope \u2014 is an ADR-worthy decision, not an implementation\n+what default token scope \u2014 is a SADR-worthy decision, not an implementation\n detail.\n \n ## Decision\n@@ -48,7 +48,7 @@\n Specifically:\n \n - **`can_approve_pull_request_reviews` defaults to `true`.** Shiki's default\n-  posture is solo/AFK autonomous operation (ADR 0008/0009/0012), where the CCA\n+  posture is solo/AFK autonomous operation (SADR-0008/0009/0012), where the CCA\n   Review Bridge is the only path that can satisfy `required_review: true` after\n   the CCA and Guardian gates have already passed. The default makes a\n   freshly-bootstrapped repo able to run the autonomous loop end to end without a\ndiff --git a/docs/adr/0014-codex-app-external-ai-guardian-ui-adapter.md b/docs/adr/SADR-0014-codex-app-external-ai-guardian-ui-adapter.md\nindex 238db06279334caa341c117362df9271382a7fcd..65f12e9939321e1b3e389a34c822fc1549e5155d\n--- a/docs/adr/0014-codex-app-external-ai-guardian-ui-adapter.md\n+++ b/docs/adr/SADR-0014-codex-app-external-ai-guardian-ui-adapter.md\n@@ -1,4 +1,4 @@\n-# ADR 0014: Use Codex App As The External AI Guardian UI Adapter\n+# SADR-0014: Use Codex App As The External AI Guardian UI Adapter\n \n ## Status\n \ndiff --git a/docs/adr/0015-contract-approval-before-dispatch.md b/docs/adr/SADR-0015-contract-approval-before-dispatch.md\nindex fad6c656741a20a7684ba16080a7db159f63a52f..6061271a46d3f1d6ae7174477de3fb75673123a6\n--- a/docs/adr/0015-contract-approval-before-dispatch.md\n+++ b/docs/adr/SADR-0015-contract-approval-before-dispatch.md\n@@ -1,4 +1,4 @@\n-# ADR 0015: Approve The Task Contract Before Dispatch, Not The Diff After It\n+# SADR-0015: Approve The Task Contract Before Dispatch, Not The Diff After It\n \n ## Status\n \n@@ -9,7 +9,7 @@\n Guardian approval for high/critical risk is expressible only on a pull request.\n Every approval source in `guardian-policy.json` \u2014 GitHub review, the\n `guardian:approved` label, the `Guardian approval granted` comment, and the\n-`external_ai_guardian_review` artifact (ADR 0010/0014) \u2014 is bound to a PR, and\n+`external_ai_guardian_review` artifact (SADR-0010/0014) \u2014 is bound to a PR, and\n three of the four require the current head SHA. Approval therefore cannot exist\n before an implementation PR exists.\n \n@@ -81,7 +81,7 @@\n An envelope breach must surface only on the non-repairable policy gate, never on\n the repairable `MergeGate metadata check`. Otherwise the Goal Loop routes it to\n `dispatch_repair` \u2014 instructing a runner to make the breach go away \u2014 which is\n-exactly what this ADR forbids.\n+exactly what this SADR forbids.\n \n **Contract immutability.** After registration, an implementation PR must not\n change the governance fields of its own task contract (`scope`, `non_goals`,\n@@ -116,7 +116,7 @@\n fails any verdict that is not `complete`, and the signal the CCA prompt reads is\n computed from `evaluate_guardian_approval` alone. Without the OR-merge at the\n signal, a contract-approved PR still returns `needs_guardian`, the CCA job fails,\n-and the MergeGate policy check never runs \u2014 the gate this ADR changes would never\n+and the MergeGate policy check never runs \u2014 the gate this SADR changes would never\n be reached. The CCA *prompt* needs no change: a `contract_approval` entry in the\n signal's existing `sources` array satisfies its current wording.\n \n@@ -130,7 +130,7 @@\n written, and an approved contract lets its implementation PRs merge through the\n same automatic path as low/medium risk. The wasted implement-then-block cycle\n disappears. Guardian review shifts from reading diffs to reading contracts, which\n-is both earlier and smaller; the external AI Guardian path (ADR 0010/0014) works\n+is both earlier and smaller; the external AI Guardian path (SADR-0010/0014) works\n on contracts with no change.\n \n We accept that the Guardian authorizes a class of change without seeing its\n@@ -146,7 +146,7 @@\n adversarial tests for the new approval path equivalent in rigor to\n `test_shiki_governance_evidence.sh`.\n \n-Bootstrap costs, which apply only while this ADR is being implemented. The tasks\n+Bootstrap costs, which apply only while this SADR is being implemented. The tasks\n that build Contract Approval merge under the existing rules, so the ones\n dispatched after the pre-dispatch gate lands but before the approval path lands\n need a Guardian-approved Contract PR *and* a live Guardian approval on their\n@@ -210,7 +210,7 @@\n risk; they do not bound authorized work.\n \n **Auto-escalate risk on envelope breach instead of stopping.** Returns the\n-workflow to a diff-time Guardian decision \u2014 the exact behavior this ADR removes.\n+workflow to a diff-time Guardian decision \u2014 the exact behavior this SADR removes.\n \n **Resolve governance fields from the base snapshot only.** Rejected against the\n registration flow: implementation PRs add their own contract, so the base has no\ndiff --git a/docs/adr/0016-locks-bound-product-paths.md b/docs/adr/SADR-0016-locks-bound-product-paths.md\nindex b5e850f3f1f7f4ef6e7f9d1763a29a51bd300c45..46b40a9b99a7aae644c02bb458f5473ff0a90ce2\n--- a/docs/adr/0016-locks-bound-product-paths.md\n+++ b/docs/adr/SADR-0016-locks-bound-product-paths.md\n@@ -1,4 +1,4 @@\n-# ADR 0016: Locks bound product paths; the Shiki mirror is bound by record identity\n+# SADR-0016: Locks bound product paths; the Shiki mirror is bound by record identity\n \n ## Status\n \n@@ -6,8 +6,8 @@\n \n - Date: 2026-07-28\n - Deciders: operator (mizutani-140), Claude (planner)\n-- Related: ADR 0004 (GitHub-first with a `.shiki` mirror), ADR 0009 (Spec Freeze),\n-  ADR 0012 (autonomous loop closeout to main), ADR 0015 (Contract Approval before\n+- Related: SADR-0004 (GitHub-first with a `.shiki` mirror), SADR-0009 (Spec Freeze),\n+  SADR-0012 (autonomous loop closeout to main), SADR-0015 (Contract Approval before\n   dispatch), remediation goal task R-02, the live PR #179 deadlock.\n \n ## Context\n@@ -90,11 +90,11 @@\n files, documentation files and named source modules are nameable; runtime-\n allocated ledger, runner and report ids are not \u2014 and those live in `.shiki`,\n which locks no longer govern. Existing frozen contracts are brought into line by\n-a one-time Spec Amendment per plan, which must land before ADR 0015's contract\n+a one-time Spec Amendment per plan, which must land before SADR-0015's contract\n immutability makes `locks` unwritable from the implementation PR.\n \n **Lock state is resolved from the durable side.** MergeGate resolves the lock\n-registry, and ADR 0015's governance fields, from the base `.shiki` snapshot when\n+registry, and SADR-0015's governance fields, from the base `.shiki` snapshot when\n present, falling back to head only when absent. The `MergeGate metadata check`\n job is given `--base-shiki`, which it does not receive today. Locks are not\n derived from the live open-pull-request set: a pull request body is untrusted\n@@ -108,7 +108,7 @@\n Explicitly out of scope: moving MergeGate's contract resolution off the PR\n checkout wholesale; making the mirror a projection of GitHub state; introducing\n new lock kinds; moving ledger evidence out of the repository; and unwinding\n-ADR 0012's closeout pull request. Registration of multi-task goals is ADR 0015's\n+SADR-0012's closeout pull request. Registration of multi-task goals is SADR-0015's\n Contract PR and is not re-decided here.\n \n ## Consequences\n@@ -127,7 +127,7 @@\n \n Risks accepted. First, goal-scoped identity is only as strong as `goal_id`,\n which is resolved from the PR body and the head task file; the mitigation is to\n-add `goal_id` to ADR 0015's frozen governance field set, and until that lands a\n+add `goal_id` to SADR-0015's frozen governance field set, and until that lands a\n task file can name a foreign goal. Second, base-when-present resolution is\n unavailable on fork pull requests, where the head is contributor-controlled;\n fork pull requests already cannot complete a Shiki merge, and this decision does\n@@ -139,7 +139,7 @@\n path, not by implementation pull requests.\n \n New obligations: one Spec Amendment per frozen plan to rewrite `locks`, ordered\n-before ADR 0015 T1; extension of the adversarial suites in\n+before SADR-0015 T1; extension of the adversarial suites in\n `scripts/test_shiki_adversarial_state.sh` and\n `scripts/test_shiki_governance_evidence.sh` to cover the eleven newly-scoped\n directories; and updating `scripts/test_shiki_loop_lock_guard.sh`,\n@@ -151,32 +151,32 @@\n whether the loop's transitive sync of ledger `evidence` references can be\n deleted once the Contract PR carries its own source plan.\n \n-## Relationship to ADR 0004\n+## Relationship to SADR-0004\n \n-This ADR **clarifies something ADR 0004 left unspecified**. ADR 0004 decides that\n+This SADR **clarifies something SADR-0004 left unspecified**. SADR-0004 decides that\n GitHub is the operational source of truth and that each target repository keeps a\n `.shiki/` mirror. It says nothing about pull requests writing the mirror, does\n not name MergeGate, and does not make the mirror an input to a gate. Nothing in\n-ADR 0004's Decision or Consequences is amended, superseded or weakened: the\n+SADR-0004's Decision or Consequences is amended, superseded or weakened: the\n mirror still exists, still records the same artifacts, and pull request\n-boundaries remain the coordination surface. What this ADR settles is the question\n+boundaries remain the coordination surface. What this SADR settles is the question\n 0004 never reached \u2014 by what authority a pull request may write a mirror record \u2014\n and it answers it the way `.shiki/manifest.json` already declares\n (`pr_mutation: current-task-or-current-goal-only`).\n \n-It **amends ADR 0012**, whose Decision item 5 states that \"Loop-executed tasks\n+It **amends SADR-0012**, whose Decision item 5 states that \"Loop-executed tasks\n declare the `path:.shiki/**` lock, which covers every `.shiki` file the closeout\n stages ... so `files_outside_locks` is satisfied without per-file lock\n bookkeeping.\" That clause is replaced: the closeout's mirror writes are\n-authorized by record identity, not by a declared lock. ADR 0012's substantive\n+authorized by record identity, not by a declared lock. SADR-0012's substantive\n decision \u2014 that completion reaches `main` through a closeout pull request rather\n than local attestation \u2014 is preserved unchanged.\n \n-It is **complementary to ADR 0015**, not a substitute. ADR 0015 owns contract\n+It is **complementary to SADR-0015**, not a substitute. SADR-0015 owns contract\n immutability, the pre-dispatch gate, the Contract PR and the Scope Envelope. This\n-ADR owns what a lock means and who may write the mirror. The one ordering\n+SADR owns what a lock means and who may write the mirror. The one ordering\n constraint between them is that the lock-narrowing Spec Amendment must precede\n-ADR 0015 T1's freezing of the `locks` field.\n+SADR-0015 T1's freezing of the `locks` field.\n \n ## Alternatives Considered\n \n@@ -186,7 +186,7 @@\n the only writer that can register them may run. Migrating the Guardian signal to\n that store makes every registration PR fail closed to `needs_guardian` at every\n risk level, because the task file is by definition absent from the store, which\n-removes the low/medium autonomous path ADR 0008/0009 exists to provide. And it\n+removes the low/medium autonomous path SADR-0008/0009 exists to provide. And it\n leaves schemas, manifest, config, guardian policy, templates, migrations, plans,\n worktrees, runner records, runs and handoffs with no writer at all in a\n repository whose product is `.shiki`. Measured benefit on the frozen backlog:\n@@ -199,14 +199,14 @@\n namespace is not in fact ownership-partitioned \u2014 56 ledger entries carry no\n `task_id`, and one goal DAG is writable by 35 registered tasks. The correct\n expression of its insight is that locks do not govern the mirror at all, which is\n-what this ADR decides.\n+what this SADR decides.\n \n **Make the mirror a projection written only by a registration writer and a\n post-merge projection job.** Rejected. It requires a bypass actor with write\n access to the protected default branch; it makes its own master invariant\n readable from a manifest that pull requests may edit; it deletes both reconcile\n modes and leaves a corrupted mirror with no in-band repair path, contradicting\n-ADR 0004's consequence that mirror conflicts are repaired against GitHub state;\n+SADR-0004's consequence that mirror conflicts are repaired against GitHub state;\n and its content-derived ids are rejected by `validate_shiki`'s currently-green id\n pattern.\n \ndiff --git a/docs/adr/0017-bookkeeping-closeouts-do-not-inherit-risk.md b/docs/adr/SADR-0017-bookkeeping-closeouts-do-not-inherit-risk.md\nindex 554e3a0d526a72ca067108c5464cc68c3edcf174..b9676857a4713850e063af7c39c8e0b01ecb4c66\n--- a/docs/adr/0017-bookkeeping-closeouts-do-not-inherit-risk.md\n+++ b/docs/adr/SADR-0017-bookkeeping-closeouts-do-not-inherit-risk.md\n@@ -1,4 +1,4 @@\n-# ADR 0017: A Bookkeeping Closeout Does Not Inherit Its Task's Risk\n+# SADR-0017: A Bookkeeping Closeout Does Not Inherit Its Task's Risk\n \n ## Status\n \n@@ -6,7 +6,7 @@\n \n ## Context\n \n-ADR 0012 made the goal loop push a task's completion to the default branch through a\n+SADR-0012 made the goal loop push a task's completion to the default branch through a\n closeout pull request: after the implementation PR merges, the loop opens a second PR\n carrying `task.status = done`, `lock.state = released`, and \u2014 when the task completes\n its goal \u2014 `goal.status = complete` plus one scorecard report. Nothing else. The\n@@ -49,14 +49,14 @@\n 4. The task file's governance fields (`scope`, `non_goals`, `required_skills`,\n    `risk_level`, `locks`, `acceptance_checks`, `test_command`) are byte-identical to\n    the base snapshot. Only `status`, `expected_pr`, `closeout_pr`, `ledger_evidence`\n-   and `expected_branch` may differ \u2014 the ADR 0015 contract-immutability rule,\n+   and `expected_branch` may differ \u2014 the SADR-0015 contract-immutability rule,\n    unchanged.\n \n    `expected_branch` is in that set because a closeout is *by construction* on a\n    different branch from the implementation it closes, and the loop rewrites the\n    field accordingly. It is safe to let it differ because MergeGate independently\n    requires `expected_branch` to equal the PR's actual head ref, so a PR cannot set\n-   it to anything other than the branch it is really on. The first draft of this ADR\n+   it to anything other than the branch it is really on. The first draft of this SADR\n    omitted it, which made the exemption unreachable: every real closeout failed\n    condition 4 on a field the loop always changes. See PR #209.\n 5. The state transitions are exactly the terminal set: task `review` \u2192 `done`, lock\n@@ -113,12 +113,12 @@\n classification must be derived, not asserted.\n \n **Fold the closeout into the implementation PR** so no second PR exists. Rejected under\n-ADR 0012, which recorded why: a task file that is already `done` on the implementation\n+SADR-0012, which recorded why: a task file that is already `done` on the implementation\n branch short-circuits the loop's review \u2192 CCA \u2192 merge \u2192 done ordering into a local\n goal-completion before any merge happens.\n \n **Have the loop auto-merge closeouts with elevated credentials.** Rejected outright: that\n-is a runtime authoring its own approval, which ADR 0010 and the constitution's\n+is a runtime authoring its own approval, which SADR-0010 and the constitution's\n approval-record prohibition forbid. It would also be indistinguishable, in the evidence,\n from the forgery those rules exist to prevent.\n \ndiff --git a/docs/adr/0018-carried-guardian-approval-across-a-proven-base-sync.md b/docs/adr/SADR-0018-carried-guardian-approval-across-a-proven-base-sync.md\nindex 22358562da390ed8b45cd600da08a43d378b4680..e6e2907cefa776460af73ddf7d39db605f3c39b0\n--- a/docs/adr/0018-carried-guardian-approval-across-a-proven-base-sync.md\n+++ b/docs/adr/SADR-0018-carried-guardian-approval-across-a-proven-base-sync.md\n@@ -1,4 +1,4 @@\n-# ADR 0018: Carried Guardian Approval Across A Proven Base Sync\n+# SADR-0018: Carried Guardian Approval Across A Proven Base Sync\n \n ## Status\n \n@@ -34,7 +34,7 @@\n Guardian evaluator that consumes it (`shiki_guardian.evaluate_guardian_approval`,\n the `guardian_comment_carried` source) were built and tested in isolation. Neither\n does anything until a caller passes the flags that turn the carry on, so the\n-feature was invisible in CI. This ADR records the decision the two of them\n+feature was invisible in CI. This SADR records the decision the two of them\n implement, and this task wires it into the four CI invocations that judge Guardian\n approval.\n \ndiff --git a/docs/adr/README.md b/docs/adr/README.md\nindex c70e057a4520c36e042ab68c53cd4a21ef709b71..5da43856836e2ce6a150f8127766e2f88355ab4b\n--- a/docs/adr/README.md\n+++ b/docs/adr/README.md\n@@ -1,40 +1,41 @@\n-# Architecture Decision Records\n+# Decision Record Namespaces\n \n-This directory holds Shiki's Architecture Decision Records (ADRs): durable\n-records of hard-to-reverse platform decisions. ADRs are part of the Shiki source\n-of truth alongside GitHub state, the `.shiki/` mirror, and `CONTEXT.md`.\n+This directory holds two independent decision namespaces:\n \n-## How ADRs Work\n+- Shiki Architecture Decision Records (SADRs), named\n+  `SADR-NNNN-<short-slug>.md`, record hard-to-reverse Shiki platform decisions.\n+- Target Architecture Decision Records (ADRs), named\n+  `NNNN-<short-slug>.md`, belong to the target product.\n \n-- New ADRs are added as `docs/adr/NNNN-<short-slug>.md`, where `NNNN` is the next\n-  zero-padded sequence number.\n-- The **canonical list of ADRs is the set of files matching `docs/adr/*.md`** in\n-  this directory. Browse `docs/adr/*.md` for the authoritative, current set.\n-- Use [`template.md`](template.md) as the starting point for a new ADR.\n-- Record an ADR when a decision is hard to reverse, surprising without context,\n-  and a real tradeoff. Do not write an ADR for routine or easily reversible\n-  choices.\n+The same four-digit number may appear once in each namespace. A platform\n+`SADR-NNNN` citation resolves only to `SADR-NNNN-*.md`; a target `ADR NNNN`\n+citation resolves only to `NNNN-*.md`.\n \n-Using the `docs/adr/*.md` glob as the source of truth means other PRs can add an\n-ADR without editing this index file, avoiding merge contention on a\n-hand-maintained enumeration.\n+## How Decision Records Work\n \n-## Current ADRs (snapshot)\n-\n-The list below is a convenience snapshot and may lag behind the directory. When\n-in doubt, list `docs/adr/*.md`.\n+- New Shiki platform decisions use the next zero-padded SADR number.\n+- New target product decisions use the target's own ADR sequence.\n+- The canonical list is the files in `docs/adr/`; no hand-maintained index is\n+  authoritative.\n+- Use [`template.md`](template.md) as the starting point and choose the filename\n+  namespace that owns the decision.\n+- Record a decision when it is hard to reverse, surprising without context, and\n+  a real tradeoff. Do not write a decision record for routine or easily\n+  reversible choices.\n \n-- [0001 \u2014 Keep Shiki As The Platform Name](0001-keep-shiki-as-platform-name.md)\n-- [0002 \u2014 Use AGENTS As The Shared Agent Constitution](0002-use-agents-as-shared-constitution.md)\n-- [0003 \u2014 Require Skill Gate For MergeGate Readiness](0003-require-skill-gate-for-mergegate-readiness.md)\n-- [0004 \u2014 Use GitHub First With A Repository-Local Shiki Mirror](0004-use-github-first-with-shiki-mirror.md)\n-- [0005 \u2014 Use Codex As Front And Claude Code Action As The GitHub Runtime](0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md)\n-- [0006 \u2014 Separate Codex Implementation, CCA Completion, And MergeGate Authorization](0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md)\n+Using the directory as the source of truth lets independent changes add records\n+without contending on an enumerated index.\n \n-## Adding A New ADR\n+## Adding A Shiki SADR\n \n-1. Copy `template.md` to `docs/adr/NNNN-<short-slug>.md`.\n+1. Copy `template.md` to `docs/adr/SADR-NNNN-<short-slug>.md`.\n 2. Fill in the title, status, context, decision, and consequences.\n-3. Set the status to `Proposed`, then `Accepted` once the decision is approved.\n-4. Open a PR following `CONTRIBUTING.md`. You do not need to edit this index for\n-   the ADR to be discoverable via `docs/adr/*.md`.\n+3. Set the status to `Proposed`, then `Accepted` once the proper authority has\n+   approved the decision.\n+4. Open a PR following `CONTRIBUTING.md`.\n+\n+## Adding A Target ADR\n+\n+1. Copy `template.md` to `docs/adr/NNNN-<short-slug>.md` using the target's own\n+   sequence.\n+2. Cite it as `ADR NNNN`; do not prefix its filename with `SADR-`.\ndiff --git a/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md b/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\nnew file mode 100644\nindex 0000000000000000000000000000000000000000..7455ce1f3bed02ddddb851ac21a1d1a648c9aeba\n--- /dev/null\n+++ b/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\n@@ -0,0 +1,55 @@\n+# SADR-0019: Separate Shiki Platform SADRs From Target ADRs\n+\n+## Status\n+\n+Accepted\n+\n+## Context\n+\n+Shiki installs its platform decision records into `docs/adr/` in every target\n+repository. Shiki and its targets previously used the same numeric\n+`NNNN-<slug>.md` namespace. A target that already owned ADRs 0013 through 0018\n+therefore acquired two records for each number during installation, making an\n+`ADR NNNN` citation ambiguous. Renumbering target decisions would rewrite target\n+history, while renumbering Shiki decisions per target would destroy their stable\n+platform identity.\n+\n+Upgrades also need to remove the former Shiki-owned numeric paths without ever\n+mistaking a target-owned ADR for platform content. The install stamp already\n+records exact shipped paths and content digests, so it can prove ownership only\n+when both still match.\n+\n+## Decision\n+\n+Shiki platform decisions use the `SADR` namespace:\n+\n+- records are named `docs/adr/SADR-NNNN-<slug>.md`;\n+- live platform citations use `SADR-NNNN`;\n+- target product decisions retain `docs/adr/NNNN-<slug>.md` and `ADR NNNN`;\n+- the reference validator resolves the two namespaces independently, so the same\n+  four-digit number may exist once in each namespace.\n+\n+Before a forced upgrade writes anything, the installer inventories the exact\n+former numeric Shiki ADR paths. It may delete a legacy path only when the\n+existing install stamp contains that exact path and its recorded SHA-256 digest\n+matches the file currently on disk. All ownership blockers are reported\n+together and abort the upgrade before its first write. A non-forced install\n+never performs this cleanup.\n+\n+Historical `.shiki/` evidence remains unchanged because it records the namespace\n+that existed when that evidence was produced.\n+\n+## Consequences\n+\n+- Targets may use any ADR number without colliding with Shiki's platform SADRs.\n+- Fresh installs preserve target ADR bytes and install a stable SADR set.\n+- Forced upgrades fail closed when legacy ownership cannot be proven, and a\n+  successful upgrade refreshes the stamp without removed legacy paths.\n+- Current governance, packaging, workflow, prompt, script, and skill references\n+  must move with the platform records.\n+\n+## Alternatives Considered\n+\n+- Collision-only refusal leaves affected targets permanently unupgradable.\n+- Renumbering target ADRs mutates target-owned history.\n+- Per-target Shiki renumbering makes platform citations non-portable.\ndiff --git a/docs/adr/template.md b/docs/adr/template.md\nindex a92a97daddd0b00ab9dd626cc8d86942da2eafed..17dd79175b5fee2105935557f691b40cf9492942\n--- a/docs/adr/template.md\n+++ b/docs/adr/template.md\n@@ -1,14 +1,14 @@\n-# NNNN: <Short Decision Title>\n+# <SADR-NNNN or NNNN>: <Short Decision Title>\n \n ## Status\n \n-Proposed | Accepted | Superseded by ADR-NNNN\n+Proposed | Accepted | Superseded by SADR-NNNN or target ADR NNNN\n \n ## Context\n \n What is the situation, constraint, or force that requires a decision? Reference\n-relevant domain terms (`CONTEXT.md`), prior ADRs, and code reality. State the\n-problem, not the solution.\n+relevant domain terms (`CONTEXT.md`), prior Shiki SADRs or target ADRs, and code\n+reality. State the problem, not the solution.\n \n ## Decision\n \ndiff --git a/docs/agents/autonomous-loop-e2e-acceptance.md b/docs/agents/autonomous-loop-e2e-acceptance.md\nindex bbf84c6081987f159ac6ad9ecf3c82b7aa72e7e8..5c13c1d02a05a90b0dc46cbef7c95a9846d0c237\n--- a/docs/agents/autonomous-loop-e2e-acceptance.md\n+++ b/docs/agents/autonomous-loop-e2e-acceptance.md\n@@ -40,8 +40,8 @@\n 3. `gh auth status` is logged in with merge permission on the repo.\n 4. Branch protection + required checks are configured exactly as the goal-loop\n    required-check set expects (see `docs/agents/branch-protection-smoke.md`).\n-5. The repo `.shiki/policy` permits autonomous low/medium-risk merges (ADR\n-   0008/0009); the MergeGate policy check is the recorded-authority gate.\n+5. The repo `.shiki/policy` permits autonomous low/medium-risk merges\n+   (SADR-0008/0009); the MergeGate policy check is the recorded-authority gate.\n \n The operator's only actions are: register the task and start the loop. **No\n manual pre-clearing** means after `shiki loop run` starts, the operator does not\ndiff --git a/docs/agents/checklists.md b/docs/agents/checklists.md\nindex 4a7f03ade346f53a1c1d82981f3fd744f9ab4547..0c5ec6c53dc7197b7b7a7d18e667b325babd6c45\n--- a/docs/agents/checklists.md\n+++ b/docs/agents/checklists.md\n@@ -32,18 +32,18 @@\n | ID | Item | Blocking |\n | --- | --- | --- |\n | GD-01 | Plan was challenged against `CONTEXT.md`. | blocking for non-trivial Goals |\n-| GD-02 | Relevant ADRs were read or absence was recorded. | blocking for non-trivial Goals |\n+| GD-02 | Relevant SADRs and target ADRs were read or absence was recorded. | blocking for non-trivial Goals |\n | GD-03 | Ambiguous terms were resolved or converted to HITL blockers. | blocking |\n | GD-04 | Edge scenarios were considered. | advisory |\n | GD-05 | Code reality was checked when code could answer. | blocking when applicable |\n | GD-06 | New domain terms were added to `CONTEXT.md` when needed. | blocking when applicable |\n-| GD-07 | ADR was created or explicitly skipped using the ADR criteria. | blocking when applicable |\n+| GD-07 | A SADR or target ADR was created or explicitly skipped using the decision-record criteria. | blocking when applicable |\n \n ## CI \u2014 Context & Impact Checklist\n \n | ID | Item | Blocking |\n | --- | --- | --- |\n-| CI-01 | Relevant docs and ADRs identified. | blocking |\n+| CI-01 | Relevant docs, SADRs, and target ADRs identified. | blocking |\n | CI-02 | Relevant modules, interfaces, seams, callers, and tests identified. | blocking |\n | CI-03 | Dependencies and blocked-by relationships identified. | blocking |\n | CI-04 | Candidate locks identified. | blocking |\n@@ -62,7 +62,7 @@\n | PRD-04 | Implementation decisions are recorded without volatile code detail. | blocking |\n | PRD-05 | Testing decisions are recorded. | blocking |\n | PRD-06 | Out-of-scope items are explicit. | blocking |\n-| PRD-07 | Relevant glossary/ADR links are present when applicable. | blocking |\n+| PRD-07 | Relevant glossary/SADR/target-ADR links are present when applicable. | blocking |\n \n ## SF \u2014 Spec Freeze Checklist\n \n@@ -98,7 +98,7 @@\n | H-02 | Branch/worktree target is present. | blocking |\n | H-03 | Scope and non-goals are present. | blocking |\n | H-04 | Dependencies and locks are present. | blocking |\n-| H-05 | Relevant docs, ADRs, modules, and tests are referenced. | blocking |\n+| H-05 | Relevant docs, SADRs, target ADRs, modules, and tests are referenced. | blocking |\n | H-06 | Required skills are stated. | blocking |\n | H-07 | TDD expectations are stated. | blocking when behavior work |\n | H-08 | Acceptance criteria are included. | blocking |\ndiff --git a/docs/agents/codex-handoff.md b/docs/agents/codex-handoff.md\nindex ede468f47cb277f69a857e8c45df703e4ed0151d..6f97bdffb67e44d7edea2c71e01ea28933c4eb6c\n--- a/docs/agents/codex-handoff.md\n+++ b/docs/agents/codex-handoff.md\n@@ -46,7 +46,7 @@\n \n ## Context\n - Domain terms:\n-- Relevant ADRs:\n+- Relevant SADRs and target ADRs:\n - Relevant docs:\n - Relevant modules/interfaces/seams:\n - Similar tests/prior art:\ndiff --git a/docs/agents/completion-check-agent.md b/docs/agents/completion-check-agent.md\nindex 13e4c035db35025f4e3cdcc5adfb24314ac4d521..61bfa1218cf36ca605250c91919e620f33238aa0\n--- a/docs/agents/completion-check-agent.md\n+++ b/docs/agents/completion-check-agent.md\n@@ -32,7 +32,7 @@\n - `AGENTS.md`\n - `CLAUDE.md`\n - `CONTEXT.md`\n-- relevant ADRs\n+- relevant SADRs and target ADRs\n - parent Goal issue\n - PRD issue or document\n - task issue\ndiff --git a/docs/agents/decision-control.md b/docs/agents/decision-control.md\nindex 7d8ee5ab03a5ccbcbd04056ae61de03785a2a9c9..8c94ecda00fffaa0e92c325ab83aa9776bd1dac1\n--- a/docs/agents/decision-control.md\n+++ b/docs/agents/decision-control.md\n@@ -11,7 +11,7 @@\n | Decision | Owner | When | Required inputs | Output | Enforcement surface |\n | --- | --- | --- | --- | --- | --- |\n | Is the Goal clear enough to plan? | Planner + Guardian when needed | Goal intake | User request, existing docs, risk signals | Goal readiness verdict | Goal checklist, issue template, ledger |\n-| Must `grill-with-docs` run? | Planner / Skill Gate | Before PRD or issue split | Ambiguity, domain terms, ADR conflicts, unclear boundaries | Skill invocation or explicit skip rationale | Skill Gate checklist |\n+| Must `grill-with-docs` run? | Planner / Skill Gate | Before PRD or issue split | Ambiguity, domain terms, SADR/target-ADR conflicts, unclear boundaries | Skill invocation or explicit skip rationale | Skill Gate checklist |\n | Is a PRD required? | Planner | After Goal context stabilizes | Goal, `grill-with-docs` output, Context & Impact output | PRD required / not required | PRD checklist, CCA evidence check |\n | Can the spec be frozen? | Operator only | End of Requirements Definition, before issue split | PRD/requirements summary, scope inventory (SF-02), open-question list | `spec_freeze` block in the plan + ledger evidence | `require_grilled_plan` (plan ingest/run), SF checklist |\n | Can a frozen spec change? | Operator only (Spec Amendment) | When implementation contradicts the frozen spec | Contested decisions, scoped re-grill result | recorded amendment + re-stamped freeze | SF checklist, ledger evidence |\ndiff --git a/docs/agents/domain.md b/docs/agents/domain.md\nindex 258da8ea81e87ac0a6b0b5b5e5c2fde980eaabb7..d57b0cf17ff3e1fa9e6e5885372bf083d8295421\n--- a/docs/agents/domain.md\n+++ b/docs/agents/domain.md\n@@ -5,7 +5,9 @@\n ## Layout\n \n - `CONTEXT.md` defines Shiki language. Keep it implementation-free.\n-- `docs/adr/` records hard-to-reverse decisions.\n+- `docs/adr/SADR-NNNN-*.md` records Shiki platform decisions.\n+- `docs/adr/NNNN-*.md` records target product decisions without sharing the\n+  SADR namespace.\n - `AGENTS.md` is the runtime-neutral agent constitution.\n - `CLAUDE.md` is the Claude Code wrapper around `AGENTS.md`.\n - `.shiki/` is the Target Repository mirror for Goals, PRDs, plans, Task DAGs, contracts, locks, worktree records, ledger entries, CCA verdicts, repair packets, reports, and handoffs.\n@@ -15,6 +17,6 @@\n \n - Use Shiki terms exactly when they exist.\n - Add glossary terms to `CONTEXT.md` only when the term is part of the domain, not a generic programming concept.\n-- Create ADRs only for hard-to-reverse, surprising, tradeoff-based decisions.\n+- Create SADRs or target ADRs only for hard-to-reverse, surprising, tradeoff-based decisions.\n - Keep product-specific language in the Target Repository, not in Shiki Template docs.\n-- If an ADR conflicts with a proposed implementation, surface the conflict before editing.\n+- If a SADR or target ADR conflicts with a proposed implementation, surface the conflict before editing.\ndiff --git a/docs/agents/execution-control.md b/docs/agents/execution-control.md\nindex ad8ee3cdb36be56fedc1f46408ca92b387d74047..bc63c3a73661d12b567016ba299f1a6b66bf9088\n--- a/docs/agents/execution-control.md\n+++ b/docs/agents/execution-control.md\n@@ -63,7 +63,7 @@\n | Branch name | Orchestrator | Orchestrator | Before implementer dispatch | branch policy | branch ref in task/ledger |\n | Lock reservation | Context & Impact | MergeGate | Before dispatch and before merge | `.shiki/locks/`, labels, PR metadata | lock id, owner, scope |\n | CI profile | Planner | MergeGate | During issue triage, rechecked at PR | labels, `.shiki/policy.yaml`, workflow matrix | required check list |\n-| Required status checks | Repo maintainer / Guardian | GitHub branch protection / rulesets | Repository setup, updated via ADR | GitHub settings | branch/ruleset config, ADR |\n+| Required status checks | Repo maintainer / Guardian | GitHub branch protection / rulesets | Repository setup, updated via target ADR | GitHub settings | branch/ruleset config, target ADR |\n | CCA judgment | CCA | GitHub required check / MergeGate | Every non-draft PR update | GitHub Action check | structured CCA verdict |\n | Repair assignment | CCA | MergeGate | After failed CCA/CI/review | repair packet, issue/PR comment | repair id and attempt count |\n | Merge readiness | MergeGate | GitHub branch protection / Guardian if needed | After CCA complete | required checks, review rules, rulesets | mergegate check output |\ndiff --git a/docs/agents/guardian-policy.md b/docs/agents/guardian-policy.md\nindex 4147c7adf05a47adb4fd24ada1a1c2e1f2ba6d2d..a20d9cc6c00a569bb057b9ca8d6b51c7fc4a9847\n--- a/docs/agents/guardian-policy.md\n+++ b/docs/agents/guardian-policy.md\n@@ -98,8 +98,8 @@\n `origin/<default_branch>` and reuses each job's existing base fetch; a shallow\n (`--depth`) or second base fetch would make the proof unresolvable and silently\n disable the carry, so neither is permitted. CCA reads `guardian_comment_carried` as\n-a recorded authority in the deterministic signal and does not re-derive it. See ADR\n-0018 for the full decision, the rejected diff/patch-equality and path-disjointness\n+a recorded authority in the deterministic signal and does not re-derive it. See\n+SADR-0018 for the full decision, the rejected diff/patch-equality and path-disjointness\n alternatives, and the accepted residual.\n \n ## External AI Guardian Review (SADR-0010)\ndiff --git a/docs/agents/implementation-policy.md b/docs/agents/implementation-policy.md\nindex c754144a9fb5bf4a38aa82a60e1670c59e626b44..85c37281bcad196c6b7b02343faf00b03cd9c57a\n--- a/docs/agents/implementation-policy.md\n+++ b/docs/agents/implementation-policy.md\n@@ -31,10 +31,13 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n ## Non-Negotiables\n \n 1. **Planning precedes implementation.** Non-trivial Goals start with `grill-with-docs` and Context & Impact.\n-2. **Domain language is controlled.** Terms must be checked against `CONTEXT.md`; hard-to-reverse tradeoffs must be checked against ADRs.\n+2. **Domain language is controlled.** Terms must be checked against `CONTEXT.md`; hard-to-reverse tradeoffs must be checked against Shiki SADRs and target ADRs.\n 3. **PRD precedes issue decomposition.** `to-prd` is used once the context is settled enough to become durable intent.\n 4. **Issues are vertical slices.** `to-issues` must produce independently grabbable tracer-bullet tasks, not horizontal layer tickets.\n 5. **Triage controls dispatch.** Only `ready-for-agent` / AFK tasks may be assigned to Codex without human input.\n@@ -95,19 +98,19 @@\n Process:\n \n - Challenge terms against `CONTEXT.md`.\n-- Check ADRs before reopening decisions.\n+- Check SADRs and target ADRs before reopening decisions.\n - Inspect code when code can answer a question.\n - Ask one question at a time when the operator is available.\n - Provide a recommended answer with each question.\n - Record resolved terms in `CONTEXT.md`.\n-- Create ADRs only for hard-to-reverse, surprising, tradeoff-based decisions.\n+- Create SADRs or target ADRs only for hard-to-reverse, surprising, tradeoff-based decisions.\n - Convert unresolved load-bearing questions into HITL blockers.\n \n Outputs:\n \n - Resolved design-tree decisions.\n - Updated glossary terms when needed.\n-- ADRs when justified.\n+- SADRs or target ADRs when justified.\n - HITL blockers when decisions remain unresolved.\n \n ## Phase 3 \u2014 Context & Impact\n@@ -117,7 +120,7 @@\n \n Outputs must include:\n \n-- Relevant documents and ADRs.\n+- Relevant documents, SADRs, and target ADRs.\n - Relevant modules, interfaces, seams, callers, and tests.\n - Candidate locks.\n - Dependencies.\n@@ -140,7 +143,7 @@\n - Testing decisions.\n - Out of scope.\n - Further notes.\n-- Links to relevant Goal, domain terms, and ADRs.\n+- Links to relevant Goal, domain terms, SADRs, and target ADRs.\n \n Phase 4 ends with Spec Freeze: the operator explicitly approves the PRD, the\n plan records a `spec_freeze` block (status, approved_by, source), and the SF\n@@ -240,7 +243,7 @@\n - Dependencies and locks.\n - Risk and Guardian requirements.\n - Ledger completeness.\n-- Documentation and ADR updates.\n+- Documentation and SADR/target-ADR updates.\n - Scope drift.\n \n CCA emits one of:\n@@ -301,7 +304,7 @@\n | Evidence failure | Behavior might be done, but proof is missing. | Add checks, PR evidence, ledger entry, or artifacts. |\n | Scope drift | PR changes unrelated behavior. | Revert unrelated changes or split into new Goal. |\n | Dependency block | Upstream task or lock not complete. | Block until dependency resolves. |\n-| Architecture block | Decision needs ADR or deep-module discussion. | `grill-with-docs` or `improve-codebase-architecture`. |\n+| Architecture block | Decision needs a SADR, target ADR, or deep-module discussion. | `grill-with-docs` or `improve-codebase-architecture`. |\n | Risk block | Security/production/policy change needs human approval. | Guardian decision. |\n \n ## Phase 5.5 \u2014 Execution Decision Control\ndiff --git a/docs/agents/runtime-auth-model.md b/docs/agents/runtime-auth-model.md\nindex 6a2e1a47e81ea89b5c68a210ec07772a9374babc..3c54748d24233dbd0ba8473950282547ef4ce935\n--- a/docs/agents/runtime-auth-model.md\n+++ b/docs/agents/runtime-auth-model.md\n@@ -148,7 +148,7 @@\n \n API-key based runners may be added by a target repository as an explicit extension. They are not the default Shiki template path.\n \n-If a repo opts into API-key mode, record the exception in an ADR and update the repo's `AGENTS.md`, `.shiki/config.yaml`, and workflow permissions.\n+If a repo opts into API-key mode, record the target-specific exception in a target ADR and update the repo's `AGENTS.md`, `.shiki/config.yaml`, and workflow permissions.\n \n ## GitHub Actions Runtime Compatibility\n \ndiff --git a/docs/agents/skill-gate.md b/docs/agents/skill-gate.md\nindex c50398b0d7c60a8598f2a5c88a10015730dced13..3acffe6444617bb1d54b77d4b4708158ec735bc6\n--- a/docs/agents/skill-gate.md\n+++ b/docs/agents/skill-gate.md\n@@ -9,7 +9,7 @@\n | Skill | Trigger | Expected output |\n | --- | --- | --- |\n | `setup-matt-pocock-skills` | First configuring a repo for these skills, or when issue tracker, triage labels, or domain docs are missing. | `docs/agents/` configuration and an `Agent skills` block in `AGENTS.md` or `CLAUDE.md`. |\n-| `grill-with-docs` | Ambiguous plan, unclear terminology, boundary decision, tradeoff, or ADR-worthy design choice. | Clarified decision tree, recommended answers, updates to `CONTEXT.md` or ADRs when decisions crystallize. |\n+| `grill-with-docs` | Ambiguous plan, unclear terminology, boundary decision, tradeoff, or decision-record-worthy design choice. | Clarified decision tree, recommended answers, updates to `CONTEXT.md`, SADRs, or target ADRs when decisions crystallize. |\n | `zoom-out` | Agent lacks a higher-level architectural map. | Map of relevant modules, callers, seams, and domain concepts. |\n | `to-prd` | Settled context should become a PRD. | PRD published to the issue tracker with problem, solution, user stories, decisions, testing, and out-of-scope notes. |\n | `to-issues` | Goal, PRD, or plan must become implementation tickets. | Vertical-slice issues with acceptance criteria, dependencies, and AFK/HITL classification. |\n@@ -17,7 +17,7 @@\n | `tdd` | Feature work or bug fix has observable behavior that can be specified with tests. | Red-green-refactor loop through public interfaces, one vertical slice at a time. |\n | `code-review` | The TDD loop is green and the implementer is about to create or update a PR. Default for every implementation task. | Pre-PR self-review of the task diff with fixes applied on the task branch; findings and resolutions recorded as a ledger entry naming the skill and a `## Pre-PR code review` PR body section. |\n | `diagnose` | Hard bug, failing check, regression, flaky behavior, or performance problem. | Reproduction loop, ranked hypotheses, instrumentation, fix, regression test, post-mortem. |\n-| `improve-codebase-architecture` | Structural friction, testability, AI-navigability, deep-module opportunity, or refactor request. | Architecture candidates and deepening recommendations informed by `CONTEXT.md` and ADRs. |\n+| `improve-codebase-architecture` | Structural friction, testability, AI-navigability, deep-module opportunity, or refactor request. | Architecture candidates and deepening recommendations informed by `CONTEXT.md`, SADRs, and target ADRs. |\n | `prototype` | A throwaway logic or UI prototype is needed to answer a design question. | Clearly marked disposable prototype and captured decision. |\n \n ## MergeGate Interaction\ndiff --git a/scripts/guardian_approval_signal.py b/scripts/guardian_approval_signal.py\nindex d059f92b2c82aa2b5d6ca421365537a87886c1cb..16924892714f4f5b9303c0ef0e325a7e70075630\n--- a/scripts/guardian_approval_signal.py\n+++ b/scripts/guardian_approval_signal.py\n@@ -199,8 +199,8 @@\n ) -> Any:\n     \"\"\"Evaluate SADR-0015 Contract Approval for the CCA Guardian signal.\n \n-    The SAME OR that MergeGate applies must reach this deterministic signal: ADR\n-    0015 is explicit that without it, a contract-approved PR still returns\n+    The SAME OR that MergeGate applies must reach this deterministic signal:\n+    SADR-0015 is explicit that without it, a contract-approved PR still returns\n     ``needs_guardian``, the CCA job fails, and the MergeGate policy check never\n     runs \u2014 the gate this change moves would never be reached. This resolves the\n     task the SAME way MergeGate does (the first Shiki task id in the PR body),\ndiff --git a/scripts/shiki_contract_approval.py b/scripts/shiki_contract_approval.py\nindex 941bcb96fdd8b9a85bc0c4f0f04d5dad9d04839c..4a4b418f6527413161db82670bec857a043360e2\n--- a/scripts/shiki_contract_approval.py\n+++ b/scripts/shiki_contract_approval.py\n@@ -142,7 +142,7 @@\n     changed_files_status: Any,\n     registration: Any,\n ) -> ContractApprovalResult:\n-    \"\"\"Whether this PR carries an SADR-0015 Contract Approval.\n+    \"\"\"Whether this PR carries a SADR-0015 Contract Approval.\n \n     The four conditions are evaluated IN ORDER, each failure recorded as a\n     distinct reason string:\ndiff --git a/scripts/shiki_guardian.py b/scripts/shiki_guardian.py\nindex a4ecb79bbda55fd5c8b06adfae2eb99760a03c45..4dd99bbe714e26f421fa19abac7a34ae1b195ecc\n--- a/scripts/shiki_guardian.py\n+++ b/scripts/shiki_guardian.py\n@@ -12,7 +12,7 @@\n # The carry re-derives its proof from git in-process on every judging invocation;\n # it is never read from an artifact, so there is nothing to go stale or be forged.\n # shiki_sync_proof is the ONLY guardian-family module that runs git, and every one\n-# of its invocations is read-only and local (ADR: prove-from-git base sync).\n+# of its invocations is read-only and local (SADR: prove-from-git base sync).\n from shiki_sync_proof import verify_pure_base_sync\n \n GUARDIAN_POLICY_PATH = \".shiki/guardian-policy.json\"\ndiff --git a/scripts/shiki_guardian_review.py b/scripts/shiki_guardian_review.py\nindex dd0f104d457899e0ba5ff1e162011c1e90b6f92b..e10ef58c046324e7ea3a3392feb3048ca77414ce\n--- a/scripts/shiki_guardian_review.py\n+++ b/scripts/shiki_guardian_review.py\n@@ -159,7 +159,7 @@\n     docs_only = _is_docs_only(changed_files)\n     if docs_only and matched == 0:\n         focus.append(\n-            \"Docs/ADR-only change: confirm the change is genuinely documentation/decision-only, introduces no behavior or contract drift, and stays consistent with the constitution and existing ADRs.\"\n+            \"Docs/decision-record-only change: confirm the change is genuinely documentation/decision-only, introduces no behavior or contract drift, and stays consistent with the constitution and existing SADRs and target ADRs.\"\n         )\n \n     # Conservative augmentation: unknown (no category and not docs-only), mixed\ndiff --git a/scripts/shiki_installer.py b/scripts/shiki_installer.py\nindex be064ab7a29ff6836f7f7a1d70500a43e0d11d7b..d3e3247edbf3e4be287a4667176d86f6500f8dc5\n--- a/scripts/shiki_installer.py\n+++ b/scripts/shiki_installer.py\n@@ -118,6 +118,7 @@\n # what lets a target carry it without a governance-schema change.\n INSTALL_STAMP_PATH = \".shiki/install-stamp.json\"\n INSTALL_STAMP_VERSION = 1\n+LEGACY_SHIKI_SADR_MAX = 18\n \n # Under --force the shipped surface splits three ways, so nothing is lost and\n # nothing is silently stale.\n@@ -237,9 +238,98 @@\n         )\n \n \n+def legacy_shiki_adr_paths() -> tuple[str, ...]:\n+    \"\"\"Former numeric paths for Shiki SADR-0001 through SADR-0018.\n+\n+    The authoritative slugs come from the current SADR filenames. SADR-0019 is\n+    the namespace decision itself and never shipped under a numeric legacy path.\n+    \"\"\"\n+    paths: list[str] = []\n+    for sadr in sorted((ROOT / \"docs\" / \"adr\").glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\")):\n+        number = sadr.name.split(\"-\", 2)[1]\n+        if int(number) <= LEGACY_SHIKI_SADR_MAX:\n+            paths.append(f\"docs/adr/{sadr.name.removeprefix('SADR-')}\")\n+    return tuple(paths)\n+\n+\n+def preflight_legacy_sadr_cleanup(target: Path, *, force: bool) -> tuple[str, ...]:\n+    \"\"\"Authorize exact legacy Shiki ADR removals before the first write.\n+\n+    A non-force install never removes existing files. A forced install needs no\n+    stamp when no legacy Shiki path is present. When legacy paths do exist, each\n+    must be named in a readable install stamp and its current bytes must match\n+    the recorded digest.\n+    \"\"\"\n+    if not force:\n+        return ()\n+    present = tuple(\n+        relative\n+        for relative in legacy_shiki_adr_paths()\n+        if (target / relative).exists() or (target / relative).is_symlink()\n+    )\n+    if not present:\n+        return ()\n+\n+    stamp_path = target / INSTALL_STAMP_PATH\n+    try:\n+        raw = stamp_path.read_text(encoding=\"utf-8\")\n+        stamp = json.loads(raw)\n+    except FileNotFoundError:\n+        detail = \"install stamp is absent\"\n+        blockers = [f\"{relative}: {detail}\" for relative in present]\n+    except (OSError, UnicodeDecodeError) as error:\n+        detail = f\"install stamp is unreadable ({error})\"\n+        blockers = [f\"{relative}: {detail}\" for relative in present]\n+    except json.JSONDecodeError as error:\n+        detail = f\"install stamp is malformed ({error})\"\n+        blockers = [f\"{relative}: {detail}\" for relative in present]\n+    else:\n+        if not isinstance(stamp, dict):\n+            blockers = [\n+                f\"{relative}: install stamp is malformed (expected an object)\"\n+                for relative in present\n+            ]\n+        else:\n+            digests = stamp.get(\"digests\")\n+            blockers = []\n+            if not isinstance(digests, dict):\n+                blockers.extend(\n+                    f\"{relative}: install stamp has no digest for this path\"\n+                    for relative in present\n+                )\n+            else:\n+                for relative in present:\n+                    expected = digests.get(relative)\n+                    if not isinstance(expected, str) or not expected:\n+                        blockers.append(\n+                            f\"{relative}: install stamp has no digest for this path\"\n+                        )\n+                        continue\n+                    try:\n+                        actual = _sha256_file(target / relative)\n+                    except OSError as error:\n+                        blockers.append(\n+                            f\"{relative}: legacy path is unreadable ({error})\"\n+                        )\n+                        continue\n+                    if actual != expected:\n+                        blockers.append(\n+                            f\"{relative}: digest mismatch (stamp {expected}, current {actual})\"\n+                        )\n+\n+    if blockers:\n+        joined = \"\\n\".join(f\"- {blocker}\" for blocker in blockers)\n+        raise ShikiError(\n+            \"legacy Shiki ADR cleanup is not authorized:\\n\"\n+            f\"{joined}\\nNo files were written.\"\n+        )\n+    return present\n+\n+\n def install_template(target: Path, *, force: bool, validate: bool) -> list[NewFileNote]:\n     # Preconditions first: never rewrite the tree of a target that cannot be\n     # upgraded cleanly (see refuse_pending_migrations).\n+    legacy_cleanup = preflight_legacy_sadr_cleanup(target, force=force)\n     refuse_pending_migrations(target)\n \n     # Resolve the target's required CODEOWNERS owner once from its\n@@ -250,6 +340,10 @@\n     # verbatim. See ``codeowners_required_owner``.\n     codeowners_owner = codeowners_required_owner(target)\n \n+    for relative in legacy_cleanup:\n+        (target / relative).unlink()\n+        info(f\"removed stamped legacy Shiki ADR: {target / relative}\")\n+\n     notes: list[NewFileNote] = []\n     for relative in TEMPLATE_PATHS:\n         source = ROOT / relative\ndiff --git a/scripts/shiki_loop.py b/scripts/shiki_loop.py\nindex 3611641da8aeb5038b1da90e3114b09a473ec6d8..84caa491b9cad0edc4176f209c13e9221e975681\n--- a/scripts/shiki_loop.py\n+++ b/scripts/shiki_loop.py\n@@ -424,7 +424,7 @@\n         # unresolvable CCA verdict has already produced its terminal stop and is never\n         # laundered into a head-moving sync (SADR-0010). It also never fires while the\n         # policy (Guardian) gate is red \u2014 for ANY reason, not only when it is the sole\n-        # red check \u2014 so a head-bound Guardian approval, or an SADR-0015\n+        # red check \u2014 so a head-bound Guardian approval, or a SADR-0015\n         # contract-approval carry that base movement defeated (the policy gate compares\n         # base and head task governance directly), is never silently restored by\n         # adopting base wholesale. It is bounded by a durable per-task counter so the\ndiff --git a/scripts/shiki_tasks.py b/scripts/shiki_tasks.py\nindex aec3809f24b4a0c8c96fc50971cff9ae9ad30381..d531052e9d9717058d4631483cefab44c26a65c1\n--- a/scripts/shiki_tasks.py\n+++ b/scripts/shiki_tasks.py\n@@ -821,7 +821,7 @@\n         \"prompt\": prompt,\n         \"entry_skill\": \"grill-with-docs\",\n         \"required_next_steps\": [\n-            \"Run grill-with-docs until terminology, boundaries, risks, and ADR-worthy decisions are settled.\",\n+            \"Run grill-with-docs until terminology, boundaries, risks, and decision-record-worthy choices are settled.\",\n             \"Obtain the operator's explicit approval of the PRD/requirements (Spec Freeze).\",\n             \"Write a machine-readable plan JSON with grill_with_docs.status=complete and spec_freeze.status=frozen.\",\n             \"Run shiki plan ingest --plan-file PLAN.json.\",\ndiff --git a/scripts/test_shiki_goal_loop.sh b/scripts/test_shiki_goal_loop.sh\nindex 753c7b516e10f0be3bc92a4bd0eb1fa699776ae6..8f3820a0a079b3044424cfe1a6c6b7fe68896e67\n--- a/scripts/test_shiki_goal_loop.sh\n+++ b/scripts/test_shiki_goal_loop.sh\n@@ -231,8 +231,8 @@\n \n # High/critical risk merges autonomously when all required checks are green:\n # the \"MergeGate policy check\" required check IS the Guardian gate (it enforces\n-# guardian-policy.json \u2014 human approval OR external AI guardian review, ADR\n-# 0010), so green checks mean a recorded authority approved. The fake gh returns\n+# guardian-policy.json \u2014 human approval OR external AI guardian review,\n+# SADR-0010), so green checks mean a recorded authority approved. The fake gh returns\n # green checks, simulating that the Guardian gate passed.\n python3 \"$ROOT/scripts/shiki.py\" goal create --target \"$TARGET\" --title \"Guardian gate\" --outcome \"High risk merges via policy gate\" >/tmp/shiki-goal-loop-guardian-goal.json\n GGOAL=\"$(json_get /tmp/shiki-goal-loop-guardian-goal.json goal_id)\"\ndiff --git a/tests/test_adr_references.py b/tests/test_adr_references.py\nindex 51706329c797d5dd9b47e082a333fa83b261d482..db6e693e35f67103935be34f1d9bb4ff7ee60395\n--- a/tests/test_adr_references.py\n+++ b/tests/test_adr_references.py\n@@ -1,7 +1,7 @@\n-\"\"\"Every ``ADR ####`` citation must resolve to exactly one decision record.\n+\"\"\"Every target ADR and Shiki SADR citation resolves in its own namespace.\n \n Background. ``scripts/mergegate_check.py`` justifies ``is_bookkeeping_closeout``\n-with \"ADR 0017\" nine times and ``scripts/guardian_approval_signal.py`` cites\n+with \"SADR-0017\" nine times and ``scripts/guardian_approval_signal.py`` cites\n 0016/0017 seven times, yet for a stretch the numbering in ``docs/adr/`` stopped\n at 0015: the authoring commits lived on a coordinator branch and no task ever\n declared ``docs/adr/...`` in its locks, so nothing carried the records to the\n@@ -11,11 +11,12 @@\n and the directory drifted with no signal.\n \n This is the same unbound-pair shape behind other silent failures: a reference\n-and its target with nothing asserting they agree. This test binds them. It scans\n-every ``scripts/*.py`` (plus ``docs/**/*.md`` and the root ``*.md`` governance\n-files, which are cheap) for the ``ADR ####`` pattern and asserts each cited\n-number resolves to exactly one ``docs/adr/####-*.md``. The converse is\n-deliberately NOT required: an ADR may exist without being cited.\n+and its target with nothing asserting they agree. This test binds them. Target\n+``ADR NNNN`` citations resolve only to ``docs/adr/NNNN-*.md``; Shiki\n+``SADR-NNNN`` citations resolve only to ``docs/adr/SADR-NNNN-*.md``. The scan\n+covers current governance docs, scripts, workflows, prompts, packaging metadata,\n+and repo-local Codex guidance while deliberately excluding historical\n+``.shiki`` evidence. The converse is not required: a record may be uncited.\n \n The suite is dependency free: standard-library ``unittest`` only.\n \"\"\"\n@@ -31,17 +32,18 @@\n \n REPO_ROOT = Path(__file__).resolve().parents[1]\n \n-# \"ADR 0016\" -- a four-digit citation. The trailing \\b keeps a five-digit token\n-# from being read as a truncated four-digit number.\n-_CITATION = re.compile(r\"\\bADR (\\d{4})\\b\")\n-# \"0016-locks-bound-product-paths.md\" -- the leading four digits name the record.\n-_ADR_FILENAME = re.compile(r\"^(\\d{4})-.*\\.md$\")\n+# ``ADR 0016`` / ``SADR-0016`` -- a namespace plus four-digit citation. The\n+# trailing ``\\b`` keeps a five-digit token from being read as a truncated one.\n+_CITATION = re.compile(r\"\\b(S?ADR)[ -](\\d{4})\\b\")\n+# Target ADRs retain the numeric filename; Shiki platform decisions use SADR.\n+_DECISION_FILENAME = re.compile(r\"^(?:(SADR)-)?(\\d{4})-.*\\.md$\")\n \n \n class Violation(NamedTuple):\n     \"\"\"A citation that does not resolve to exactly one decision record.\"\"\"\n \n     citing_file: str  # repo-relative path of the file that cites the ADR\n+    namespace: str  # \"ADR\" for target decisions; \"SADR\" for Shiki decisions\n     number: str  # the cited four-digit number, e.g. \"0016\"\n     line: int  # 1-based line of the citation\n     reason: str  # \"missing\" (zero matches) or \"ambiguous\" (two or more)\n@@ -49,44 +51,59 @@\n \n     @property\n     def expected_path(self) -> str:\n-        return f\"docs/adr/{self.number}-*.md\"\n+        prefix = \"SADR-\" if self.namespace == \"SADR\" else \"\"\n+        return f\"docs/adr/{prefix}{self.number}-*.md\"\n \n     def describe(self) -> str:\n         \"\"\"Name the citing file, the cited number, and the missing path.\"\"\"\n         if self.reason == \"missing\":\n             return (\n-                f\"{self.citing_file}:{self.line} cites ADR {self.number} \"\n+                f\"{self.citing_file}:{self.line} cites {self.namespace} {self.number} \"\n                 f\"but no document matches {self.expected_path}\"\n             )\n         joined = \", \".join(self.matches)\n         return (\n-            f\"{self.citing_file}:{self.line} cites ADR {self.number} which \"\n+            f\"{self.citing_file}:{self.line} cites {self.namespace} {self.number} which \"\n             f\"resolves to {len(self.matches)} documents (expected exactly one \"\n             f\"at {self.expected_path}): {joined}\"\n         )\n \n \n-def _adr_index(root: Path) -> dict[str, list[Path]]:\n-    \"\"\"Map every four-digit ADR number to the record files that claim it.\"\"\"\n-    index: dict[str, list[Path]] = {}\n+def _adr_index(root: Path) -> dict[tuple[str, str], list[Path]]:\n+    \"\"\"Map each decision namespace and number to the files that claim it.\"\"\"\n+    index: dict[tuple[str, str], list[Path]] = {}\n     adr_dir = root / \"docs\" / \"adr\"\n     if adr_dir.is_dir():\n         for path in sorted(adr_dir.glob(\"*.md\")):\n-            match = _ADR_FILENAME.match(path.name)\n+            match = _DECISION_FILENAME.match(path.name)\n             if match:\n-                index.setdefault(match.group(1), []).append(path)\n+                namespace = \"SADR\" if match.group(1) else \"ADR\"\n+                index.setdefault((namespace, match.group(2)), []).append(path)\n     return index\n \n \n def _citing_files(root: Path, include_docs: bool = True) -> list[Path]:\n-    \"\"\"Files scanned for citations: scripts/*.py, and cheaply the markdown.\"\"\"\n+    \"\"\"Current, shipped text surfaces scanned for decision citations.\"\"\"\n     files = sorted((root / \"scripts\").glob(\"*.py\"))\n+    files += sorted((root / \"scripts\").glob(\"*.sh\"))\n     if include_docs:\n         docs = root / \"docs\"\n         if docs.is_dir():\n             files += sorted(docs.rglob(\"*.md\"))\n         files += sorted(root.glob(\"*.md\"))\n-    return files\n+        pyproject = root / \"pyproject.toml\"\n+        if pyproject.is_file():\n+            files.append(pyproject)\n+        for relative in (\".github\", \".codex\"):\n+            surface = root / relative\n+            if surface.is_dir():\n+                files += sorted(\n+                    path\n+                    for path in surface.rglob(\"*\")\n+                    if path.is_file()\n+                    and path.suffix in {\".md\", \".py\", \".sh\", \".toml\", \".yaml\", \".yml\"}\n+                )\n+    return sorted(set(files))\n \n \n def find_adr_reference_violations(\n@@ -94,10 +111,9 @@\n ) -> list[Violation]:\n     \"\"\"Return every ``ADR ####`` citation under ``root`` that fails to resolve.\n \n-    A citation resolves when exactly one ``docs/adr/####-*.md`` matches its\n-    number. Zero matches is a \"missing\" violation; two or more is \"ambiguous\".\n-    Records that are never cited produce nothing -- the converse is not\n-    required.\n+    A citation resolves when exactly one record in the same namespace matches\n+    its number. Zero matches is a \"missing\" violation; two or more is\n+    \"ambiguous\". Records that are never cited produce nothing.\n     \"\"\"\n     root = Path(root)\n     index = _adr_index(root)\n@@ -110,13 +126,15 @@\n         rel = path.relative_to(root).as_posix()\n         for lineno, line in enumerate(text.splitlines(), start=1):\n             for match in _CITATION.finditer(line):\n-                number = match.group(1)\n-                found = index.get(number, [])\n+                namespace, number = match.groups()\n+                found = index.get((namespace, number), [])\n                 if len(found) == 1:\n                     continue\n                 reason = \"missing\" if not found else \"ambiguous\"\n                 rels = [p.relative_to(root).as_posix() for p in found]\n-                violations.append(Violation(rel, number, lineno, reason, rels))\n+                violations.append(\n+                    Violation(rel, namespace, number, lineno, reason, rels)\n+                )\n     return violations\n \n \n@@ -129,8 +147,8 @@\n         self.assertEqual(\n             violations,\n             [],\n-            \"Every 'ADR ####' citation must resolve to exactly one \"\n-            f\"docs/adr/####-*.md:\\n{detail}\",\n+            \"Every ADR/SADR citation must resolve exactly once in its own \"\n+            f\"docs/adr namespace:\\n{detail}\",\n         )\n \n     def test_scan_actually_reads_scripts(self) -> None:\n@@ -146,8 +164,21 @@\n         # The two records this Goal carried onto the default branch, and which\n         # the scripts cite most, must be present.\n         index = _adr_index(REPO_ROOT)\n-        self.assertIn(\"0016\", index)\n-        self.assertIn(\"0017\", index)\n+        self.assertIn((\"SADR\", \"0016\"), index)\n+        self.assertIn((\"SADR\", \"0017\"), index)\n+\n+    def test_platform_tree_has_only_sadr_0001_through_0019(self) -> None:\n+        adr_dir = REPO_ROOT / \"docs\" / \"adr\"\n+        sadrs = sorted(adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\"))\n+        self.assertEqual(\n+            {path.name.split(\"-\", 2)[1] for path in sadrs},\n+            {f\"{number:04d}\" for number in range(1, 20)},\n+        )\n+        self.assertEqual(\n+            list(adr_dir.glob(\"[0-9][0-9][0-9][0-9]-*.md\")),\n+            [],\n+            \"the Shiki platform tree must not retain legacy numeric SADR paths\",\n+        )\n \n \n class FixtureAdrReferences(unittest.TestCase):\n@@ -180,6 +211,26 @@\n         self.assertIn(\"9991\", message)  # cited number\n         self.assertIn(\"docs/adr/9991-*.md\", message)  # missing path\n \n+    def test_target_adr_does_not_satisfy_missing_platform_sadr(self) -> None:\n+        root = self._make_repo(\n+            adr_names=[\"0042-target-decision.md\"],\n+            script_body='\"\"\"Target ADR 0042 exists, but platform SADR-0042 does not.\"\"\"\\n',\n+        )\n+        violations = find_adr_reference_violations(root, include_docs=False)\n+        self.assertEqual(len(violations), 1)\n+        self.assertEqual(violations[0].namespace, \"SADR\")\n+        self.assertEqual(violations[0].number, \"0042\")\n+        self.assertEqual(violations[0].expected_path, \"docs/adr/SADR-0042-*.md\")\n+\n+    def test_target_adr_and_platform_sadr_may_reuse_a_number(self) -> None:\n+        root = self._make_repo(\n+            adr_names=[\"0042-target-decision.md\", \"SADR-0042-platform-decision.md\"],\n+            script_body='\"\"\"Target ADR 0042 and platform SADR-0042 are distinct.\"\"\"\\n',\n+        )\n+        self.assertEqual(\n+            find_adr_reference_violations(root, include_docs=False), []\n+        )\n+\n     def test_uncited_adr_is_allowed(self) -> None:\n         # An ADR exists but nothing cites it -> not a violation. The converse\n         # (every ADR must be cited) is explicitly NOT required.\ndiff --git a/tests/test_guardian_review.py b/tests/test_guardian_review.py\nindex 88829e7810212635e2fc98aa5918d6f26294677d..00a85a237acd5bf2d6cb5b294128198c422402ca\n--- a/tests/test_guardian_review.py\n+++ b/tests/test_guardian_review.py\n@@ -162,7 +162,7 @@\n \n     def test_docs_only_is_light_and_not_conservative(self):\n         focus = gr.classify_review_focus([\"docs/adr/0099-x.md\", \"README.md\"], \"\", \"low\")\n-        self.assertTrue(any(\"Docs/ADR-only\" in f for f in focus))\n+        self.assertTrue(any(\"Docs/decision-record-only\" in f for f in focus))\n         self.assertFalse(any(\"conservative default\" in f.lower() for f in focus))\n \n     def test_unknown_category_is_conservative(self):\ndiff --git a/tests/test_policy_job_merged_prs.py b/tests/test_policy_job_merged_prs.py\nindex d45512e74681e5e649279236c7d4e79d5c3da471..b97989f8bd74d091de1a6b5275a20d8dc68c4512\n--- a/tests/test_policy_job_merged_prs.py\n+++ b/tests/test_policy_job_merged_prs.py\n@@ -3,8 +3,8 @@\n \n ``.github/workflows/shiki-cca-completion.yml`` has two consumers of the merged-PR\n proof: the CCA *signal* (``guardian_approval_signal.py`` in \"Prepare CCA context\")\n-and the authoritative gate (``mergegate_check.py`` in \"Enforce MergeGate\"). ADR\n-0017's bookkeeping-closeout exemption (condition 6) needs the merged implementation\n+and the authoritative gate (``mergegate_check.py`` in \"Enforce MergeGate\").\n+SADR-0017's bookkeeping-closeout exemption (condition 6) needs the merged implementation\n PR proven, and a closeout carries NO ``post_merge_reconcile`` marker. The signal\n already resolves ``MERGED_PRS`` unconditionally, but \"Enforce MergeGate\" used to\n resolve it only inside ``grep -q '<!-- shiki:post_merge_reconcile -->'``. So a\ndiff --git a/tests/test_sadr_install_migration.py b/tests/test_sadr_install_migration.py\nnew file mode 100644\nindex 0000000000000000000000000000000000000000..a6f4c57e4dcaef73ef600b0d4804744138ab9071\n--- /dev/null\n+++ b/tests/test_sadr_install_migration.py\n@@ -0,0 +1,229 @@\n+\"\"\"SADR installation and collision-safe legacy upgrade behavior.\"\"\"\n+\n+from __future__ import annotations\n+\n+import contextlib\n+import hashlib\n+import io\n+import json\n+import shutil\n+import tempfile\n+import unittest\n+from pathlib import Path\n+\n+import shiki_test_support  # noqa: F401  (path bootstrap)\n+\n+import shiki_installer\n+from tests.test_adr_references import find_adr_reference_violations\n+\n+\n+PRODUCT_NUMBERS = tuple(range(13, 19))\n+\n+\n+def _quiet_install(target: Path, *, force: bool, validate: bool) -> None:\n+    with contextlib.redirect_stdout(io.StringIO()):\n+        shiki_installer.install_template(target, force=force, validate=validate)\n+\n+\n+def _sha256(path: Path) -> str:\n+    return hashlib.sha256(path.read_bytes()).hexdigest()\n+\n+\n+def _legacy_sadr_paths() -> tuple[str, ...]:\n+    return tuple(\n+        f\"docs/adr/{path.name.removeprefix('SADR-')}\"\n+        for path in sorted((shiki_installer.ROOT / \"docs\" / \"adr\").glob(\"SADR-*.md\"))\n+        if path.name != \"SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\"\n+    )\n+\n+\n+def _create_legacy_upgrade_fixture(\n+    target: Path,\n+) -> tuple[dict[str, bytes], tuple[str, ...]]:\n+    adr_dir = target / \"docs\" / \"adr\"\n+    adr_dir.mkdir(parents=True)\n+    product_bytes = {\n+        f\"{number:04d}-cubeshare-product-decision.md\": (\n+            f\"# CubeShare ADR {number:04d}\\n\\nDo not rewrite {number}.\\n\"\n+        ).encode()\n+        for number in PRODUCT_NUMBERS\n+    }\n+    for name, content in product_bytes.items():\n+        (adr_dir / name).write_bytes(content)\n+\n+    legacy_paths = _legacy_sadr_paths()\n+    for relative in legacy_paths:\n+        (target / relative).write_bytes(f\"legacy Shiki bytes for {relative}\\n\".encode())\n+\n+    stamp_path = target / shiki_installer.INSTALL_STAMP_PATH\n+    stamp_path.parent.mkdir(parents=True, exist_ok=True)\n+    stamp = {\n+        \"version\": shiki_installer.INSTALL_STAMP_VERSION,\n+        \"platform_commit\": None,\n+        \"installed_at\": \"2000-01-01T00:00:00+00:00\",\n+        \"digests\": {\n+            relative: _sha256(target / relative) for relative in legacy_paths\n+        },\n+    }\n+    stamp_path.write_text(\n+        json.dumps(stamp, indent=2, sort_keys=True) + \"\\n\", encoding=\"utf-8\"\n+    )\n+    return product_bytes, legacy_paths\n+\n+\n+def _tree_snapshot(root: Path) -> dict[str, tuple[str, bytes | str]]:\n+    \"\"\"Recursive byte/shape snapshot used to prove preflight is write-free.\"\"\"\n+    snapshot: dict[str, tuple[str, bytes | str]] = {}\n+    for path in sorted(root.rglob(\"*\")):\n+        relative = path.relative_to(root).as_posix()\n+        if path.is_symlink():\n+            snapshot[relative] = (\"symlink\", path.readlink().as_posix())\n+        elif path.is_dir():\n+            snapshot[relative] = (\"directory\", \"\")\n+        else:\n+            snapshot[relative] = (\"file\", path.read_bytes())\n+    return snapshot\n+\n+\n+class FreshSadrInstallTests(unittest.TestCase):\n+    def setUp(self) -> None:\n+        self.tmp = Path(tempfile.mkdtemp(prefix=\"shiki-sadr-fresh-\"))\n+        self.addCleanup(lambda: shutil.rmtree(self.tmp, ignore_errors=True))\n+        self.target = self.tmp / \"target\"\n+        self.adr_dir = self.target / \"docs\" / \"adr\"\n+        self.adr_dir.mkdir(parents=True)\n+        self.product_bytes = {\n+            f\"{number:04d}-product-decision.md\": (\n+                f\"# Product ADR {number:04d}\\n\\nTarget-owned bytes {number}.\\n\"\n+            ).encode()\n+            for number in PRODUCT_NUMBERS\n+        }\n+        for name, content in self.product_bytes.items():\n+            (self.adr_dir / name).write_bytes(content)\n+\n+    def test_fresh_install_preserves_product_adrs_and_installs_all_sadrs(self) -> None:\n+        _quiet_install(self.target, force=False, validate=True)\n+\n+        for name, expected in self.product_bytes.items():\n+            self.assertEqual((self.adr_dir / name).read_bytes(), expected)\n+\n+        sadr_numbers = {\n+            path.name.split(\"-\", 2)[1]\n+            for path in self.adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\")\n+        }\n+        self.assertEqual(sadr_numbers, {f\"{number:04d}\" for number in range(1, 20)})\n+\n+        numeric_records = {\n+            path.name\n+            for path in self.adr_dir.glob(\"[0-9][0-9][0-9][0-9]-*.md\")\n+        }\n+        self.assertEqual(numeric_records, set(self.product_bytes))\n+        self.assertEqual(find_adr_reference_violations(self.target), [])\n+\n+\n+class LegacySadrUpgradeTests(unittest.TestCase):\n+    def setUp(self) -> None:\n+        self.tmp = Path(tempfile.mkdtemp(prefix=\"shiki-sadr-upgrade-\"))\n+        self.addCleanup(lambda: shutil.rmtree(self.tmp, ignore_errors=True))\n+        self.target = self.tmp / \"target\"\n+        self.adr_dir = self.target / \"docs\" / \"adr\"\n+        self.product_bytes, self.legacy_paths = _create_legacy_upgrade_fixture(\n+            self.target\n+        )\n+\n+    def test_force_upgrade_deletes_only_stamped_legacy_sadrs(self) -> None:\n+        _quiet_install(self.target, force=True, validate=True)\n+\n+        for relative in self.legacy_paths:\n+            self.assertFalse((self.target / relative).exists(), relative)\n+        for name, expected in self.product_bytes.items():\n+            self.assertEqual((self.adr_dir / name).read_bytes(), expected)\n+\n+        sadrs = list(self.adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\"))\n+        self.assertEqual(len(sadrs), 19)\n+        refreshed = json.loads(\n+            (self.target / shiki_installer.INSTALL_STAMP_PATH).read_text(encoding=\"utf-8\")\n+        )\n+        for relative in self.legacy_paths:\n+            self.assertNotIn(relative, refreshed[\"digests\"])\n+        for path in sadrs:\n+            relative = path.relative_to(self.target).as_posix()\n+            self.assertEqual(refreshed[\"digests\"][relative], _sha256(path))\n+\n+    def test_non_force_install_never_deletes_legacy_files(self) -> None:\n+        before = {\n+            relative: (self.target / relative).read_bytes()\n+            for relative in self.legacy_paths\n+        }\n+        _quiet_install(self.target, force=False, validate=False)\n+        for relative, expected in before.items():\n+            self.assertEqual((self.target / relative).read_bytes(), expected)\n+        refreshed = json.loads(\n+            (self.target / shiki_installer.INSTALL_STAMP_PATH).read_text(encoding=\"utf-8\")\n+        )\n+        for relative in self.legacy_paths:\n+            self.assertEqual(refreshed[\"digests\"][relative], _sha256(self.target / relative))\n+\n+\n+class LegacyCleanupPreflightTests(unittest.TestCase):\n+    def setUp(self) -> None:\n+        self.tmp = Path(tempfile.mkdtemp(prefix=\"shiki-sadr-preflight-\"))\n+        self.addCleanup(lambda: shutil.rmtree(self.tmp, ignore_errors=True))\n+\n+    def test_all_ownership_blockers_abort_before_any_write(self) -> None:\n+        cases = (\n+            (\"absent\", \"install stamp is absent\"),\n+            (\"unreadable\", \"install stamp is unreadable\"),\n+            (\"malformed\", \"install stamp is malformed\"),\n+            (\"missing-digest\", \"install stamp has no digest\"),\n+            (\"digest-mismatch\", \"digest mismatch\"),\n+        )\n+        for name, expected_reason in cases:\n+            with self.subTest(name=name):\n+                target = self.tmp / name\n+                _, legacy_paths = _create_legacy_upgrade_fixture(target)\n+                stamp_path = target / shiki_installer.INSTALL_STAMP_PATH\n+\n+                if name == \"absent\":\n+                    stamp_path.unlink()\n+                elif name == \"unreadable\":\n+                    stamp_path.unlink()\n+                    stamp_path.mkdir()\n+                elif name == \"malformed\":\n+                    stamp_path.write_text(\"{not-json\\n\", encoding=\"utf-8\")\n+                else:\n+                    stamp = json.loads(stamp_path.read_text(encoding=\"utf-8\"))\n+                    if name == \"missing-digest\":\n+                        stamp[\"digests\"] = {}\n+                    else:\n+                        stamp[\"digests\"] = {\n+                            relative: \"deadbeef\" for relative in legacy_paths\n+                        }\n+                    stamp_path.write_text(\n+                        json.dumps(stamp, indent=2, sort_keys=True) + \"\\n\",\n+                        encoding=\"utf-8\",\n+                    )\n+\n+                before = _tree_snapshot(target)\n+                with self.assertRaises(shiki_installer.ShikiError) as raised:\n+                    _quiet_install(target, force=True, validate=False)\n+                after = _tree_snapshot(target)\n+\n+                self.assertEqual(before, after, f\"{name} preflight changed the tree\")\n+                message = str(raised.exception)\n+                self.assertIn(expected_reason, message)\n+                self.assertIn(\"No files were written\", message)\n+                for relative in legacy_paths:\n+                    self.assertIn(relative, message)\n+\n+    def test_absent_stamp_is_allowed_when_no_legacy_path_exists(self) -> None:\n+        target = self.tmp / \"no-legacy\"\n+        target.mkdir()\n+        _quiet_install(target, force=True, validate=False)\n+        self.assertTrue(\n+            (target / \"docs\" / \"adr\" / \"SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\").is_file()\n+        )\n+\n+\n+if __name__ == \"__main__\":\n+    unittest.main()\n\n2026-08-07T08:15:04.475408Z ERROR codex_models_manager::manager: failed to renew cache TTL: missing field `base_instructions` at line 94 column 5\n exited 1 in 333ms:\nERROR\n\n======================================================================\nERROR: test_non_force_install_never_deletes_legacy_files (test_sadr_install_migration.LegacySadrUpgradeTests.test_non_force_install_never_deletes_legacy_files)\n----------------------------------------------------------------------\nTraceback (most recent call last):\n  File \"/private/tmp/shiki-adr-collision.pa0KwY/.worktrees/shiki-t-20260807t073307221254z-2d22d654-introduc/tests/test_sadr_install_migration.py\", line 165, in test_non_force_install_never_deletes_legacy_files\n    self.assertEqual(refreshed[\"digests\"][relative], _sha256(self.target / relative))\n                     ~~~~~~~~~~~~~~~~~~~~^^^^^^^^^^\nKeyError: 'docs/adr/0001-keep-shiki-as-platform-name.md'\n\n----------------------------------------------------------------------\nRan 1 test in 0.300s\n\nFAILED (errors=1)\n\napply patch\npatch: completed\n/private/tmp/shiki-adr-collision.pa0KwY/.worktrees/shiki-t-20260807t073307221254z-2d22d654-introduc/scripts/shiki_installer.py\ndiff --git a/.codex/skills/shiki/SKILL.md b/.codex/skills/shiki/SKILL.md\nindex c8e2e85d9d75d49cc4f2d6286ee22659c4e53e75..93cded623b2c15d9f65c76491a1c2d813ac9e6c0\n--- a/.codex/skills/shiki/SKILL.md\n+++ b/.codex/skills/shiki/SKILL.md\n@@ -18,6 +18,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n Codex CLI does not register this skill as a custom `/shiki` slash command. When\n the user types `/shiki` in Codex CLI and gets \"Unrecognized command\", explain\n that this is expected: invoke Shiki in Codex with natural language such as\n@@ -69,7 +72,7 @@\n \n ## Responsibilities\n \n-- Claude Code implements and repairs by default (`shiki runner claude`, ADR 0008); Codex implements and repairs tasks explicitly assigned to `codex`.\n+- Claude Code implements and repairs by default (`shiki runner claude`, SADR-0008); Codex implements and repairs tasks explicitly assigned to `codex`.\n - Claude Code Action can act as GitHub-side CCA or reviewer.\n - CCA judges completion.\n - MergeGate authorizes state transitions and merge readiness.\ndiff --git a/.github/prompts/cca-completion-check.md b/.github/prompts/cca-completion-check.md\nindex ed203ad5f302ecc81372d8ab27d2b1232ca97497..fd89950528333ef6242e6d5443904f5392ab7e56\n--- a/.github/prompts/cca-completion-check.md\n+++ b/.github/prompts/cca-completion-check.md\n@@ -19,6 +19,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n ## Required Reading\n \n Read only the files needed to judge the PR contract. Prefer this order:\ndiff --git a/.github/prompts/codex-task-handoff.md b/.github/prompts/codex-task-handoff.md\nindex 695e211d76dea1c60e8d5bb360e78487808286a4..59441d576b01a39569722f226c854e5a740e9eb5\n--- a/.github/prompts/codex-task-handoff.md\n+++ b/.github/prompts/codex-task-handoff.md\n@@ -2,7 +2,7 @@\n \n You are Codex Front acting as the Implementer for a Shiki task.\n \n-Follow `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant ADRs, and `docs/agents/codex-handoff.md`.\n+Follow `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant Shiki SADRs and target ADRs, and `docs/agents/codex-handoff.md`.\n \n Rules:\n \ndiff --git a/AGENTS.md b/AGENTS.md\nindex 7f041100d49c2a7525f51cb0f5d696a4a6d1c78c..a06bfbabc6fa48a007389e25e12d7ed523936bed\n--- a/AGENTS.md\n+++ b/AGENTS.md\n@@ -22,7 +22,7 @@\n - **Codex Front** is an optional implementer through Codex App, Codex CLI, Codex IDE extension, or Codex Web signed in with ChatGPT OAuth/subscription auth, dispatched with `shiki runner codex` when a task is explicitly assigned to `codex`.\n - **GitHub CCA** is implemented by Claude Code Action by default, using `CLAUDE_CODE_OAUTH_TOKEN`.\n \n-Do not assume `openai/codex-action`, `OPENAI_API_KEY`, or API-key based Codex automation in the default Shiki path. API-key runners are explicit target-repo extensions and require their own ADR.\n+Do not assume `openai/codex-action`, `OPENAI_API_KEY`, or API-key based Codex automation in the default Shiki path. API-key runners are explicit target-repo extensions and require their own target ADR.\n \n Shiki is not:\n \n@@ -44,10 +44,14 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use\n+`SADR-NNNN-*.md`; target product decisions retain `NNNN-*.md` and the `ADR`\n+namespace.\n+\n Use the canonical source-of-truth order above. Runtime-specific reading orders\n are operational guidance only and must not override or reorder it.\n \n-Conversation state is not durable truth. If a decision matters, put it in GitHub, `.shiki/`, `CONTEXT.md`, or an ADR.\n+Conversation state is not durable truth. If a decision matters, put it in GitHub, `.shiki/`, `CONTEXT.md`, a Shiki SADR, or a target ADR.\n \n If GitHub and `.shiki/` disagree, surface the conflict and prefer GitHub operational state until the mirror is repaired.\n \n@@ -75,8 +79,8 @@\n Every non-trivial change follows this loop:\n \n 1. **Goal**: clarify outcome, completion conditions, non-goals, risk level, and success signals.\n-2. **grill-with-docs**: challenge the plan against domain docs, ADRs, code reality, terminology, and edge scenarios.\n-3. **Context & Impact**: find relevant docs, ADRs, code, dependencies, owners, locks, and verification surfaces.\n+2. **grill-with-docs**: challenge the plan against domain docs, Shiki SADRs, target ADRs, code reality, terminology, and edge scenarios.\n+3. **Context & Impact**: find relevant docs, SADRs, target ADRs, code, dependencies, owners, locks, and verification surfaces.\n 4. **PRD and Spec Freeze**: use `to-prd` when context is settled enough to become durable product/engineering intent. The operator's explicit approval of the PRD is Spec Freeze: the plan records a `spec_freeze` block, and no plan runs without it. Steps 1-4 form Requirements Definition \u2014 one continuous operator dialogue. After Spec Freeze, scope changes require an operator-approved Spec Amendment; non-scope-moving interpretations go to the Assumption Log.\n 5. **Issues**: use `to-issues` to create independently grabbable vertical-slice issues. Prefer AFK slices over HITL slices where possible.\n 6. **Triage**: label issues for readiness, risk, runtime, skills, and MergeGate state.\n@@ -167,7 +171,7 @@\n - Testing decisions.\n - Out-of-scope items.\n - Further notes.\n-- Links to relevant domain terms and ADRs.\n+- Links to relevant domain terms, SADRs, and target ADRs.\n \n Every executable task must include:\n \n@@ -193,7 +197,7 @@\n \n Context & Impact must identify:\n \n-- Relevant documents, domain terms, ADRs, and past decisions.\n+- Relevant documents, domain terms, SADRs, target ADRs, and past decisions.\n - Relevant modules, interfaces, seams, callers, owners, and tests.\n - Dependency relationships between tasks.\n - Candidate locks and lock conflicts.\n@@ -210,7 +214,7 @@\n | Skill | Required when |\n | --- | --- |\n | `setup-matt-pocock-skills` | First configuring the repo for skills, or when issue tracker, triage labels, or domain docs are missing. |\n-| `grill-with-docs` | Default for non-trivial Goals before PRD/issues; always required when plans, terminology, boundaries, tradeoffs, or ADR-worthy decisions are ambiguous. |\n+| `grill-with-docs` | Default for non-trivial Goals before PRD/issues; always required when plans, terminology, boundaries, tradeoffs, or decision-record-worthy choices are ambiguous. |\n | `zoom-out` | The agent lacks an architectural map of the relevant code area. |\n | `to-prd` | Settled Goal context must become durable product/engineering intent. |\n | `to-issues` | A Goal, PRD, or plan must become independently grabbable vertical-slice issues. |\n@@ -348,7 +352,7 @@\n - Three or more new files.\n - Any high-risk or critical label.\n \n-When the architecture gate triggers, run `grill-with-docs` or `improve-codebase-architecture` as appropriate and record durable decisions in an ADR when needed.\n+When the architecture gate triggers, run `grill-with-docs` or `improve-codebase-architecture` as appropriate and record durable decisions in a Shiki SADR or target ADR as applicable.\n \n ## Engineering Discipline\n \ndiff --git a/CLAUDE.md b/CLAUDE.md\nindex 98a932e7841a632866fcd53b0bad0e3af3711d3d..98a31399404d6fb044b4fd493f4459ac87f439de\n--- a/CLAUDE.md\n+++ b/CLAUDE.md\n@@ -14,6 +14,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n ## Claude Role\n \n Default to planner, default implementer, reviewer, coordinator, documentation editor, CCA-style completion judge, and final-judgment assistant.\n@@ -57,7 +60,7 @@\n At the start of a Shiki session:\n \n 1. Read `AGENTS.md`.\n-2. Read `CONTEXT.md` and relevant ADRs.\n+2. Read `CONTEXT.md` and relevant Shiki SADRs and target ADRs.\n 3. Read active `.shiki/` Goal/task/ledger state when present.\n 4. Read active distilled rules from `.shiki/memories/` (status `distilled`, `active: true`, not revoked or superseded) for applicable guidance; the same rules are injected deterministically into task handoffs as the `## Distilled Rules` section (\u00a73.5).\n 5. Read the GitHub Issue or PR when the task is GitHub-backed.\n@@ -74,7 +77,7 @@\n - Define the Goal, completion conditions, non-goals, and risk level.\n - Treat `grill-with-docs` as the default planning entry point for non-trivial Goals.\n - Challenge domain terms against `CONTEXT.md`.\n-- Challenge hard-to-reverse tradeoffs against ADRs.\n+- Challenge hard-to-reverse tradeoffs against SADRs and target ADRs.\n - Explore code instead of asking when code can answer the question.\n - Run Context & Impact before implementation planning.\n - Produce a Task DAG, not an unordered checklist.\n@@ -94,7 +97,7 @@\n - For each question, provide your recommended answer.\n - Resolve dependency decisions before downstream design questions.\n - Update `CONTEXT.md` inline when terminology crystallizes.\n-- Offer ADRs only when the decision is hard to reverse, surprising without context, and a real tradeoff.\n+- Offer a SADR or target ADR only when the decision is hard to reverse, surprising without context, and a real tradeoff.\n - Convert unresolved questions into HITL blockers rather than allowing Codex to guess.\n \n ## PRD and Issues Standard\n@@ -103,7 +106,7 @@\n \n - Use the project's domain glossary vocabulary.\n - Avoid volatile file paths unless they are essential.\n-- Include implementation decisions, testing decisions, out-of-scope items, and relevant ADR/domain links.\n+- Include implementation decisions, testing decisions, out-of-scope items, and relevant SADR/target-ADR/domain links.\n - Identify deep-module opportunities and testable interfaces.\n \n When creating issues:\n@@ -121,7 +124,7 @@\n - Branch/worktree target.\n - Scope and non-goals.\n - Dependencies and locks.\n-- Relevant docs, ADRs, modules, and tests.\n+- Relevant docs, SADRs, target ADRs, modules, and tests.\n - Required skill invocations.\n - TDD expectations.\n - Pre-PR `code-review` gate expectations.\n@@ -152,7 +155,7 @@\n \n - Treat yourself as a judge, not implementer.\n - Do not write production code.\n-- Read `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant ADRs, PRD, task issue, PR body, diff, checks, reviews, labels, and `.shiki/` state.\n+- Read `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant SADRs and target ADRs, PRD, task issue, PR body, diff, checks, reviews, labels, and `.shiki/` state.\n - Evaluate every applicable checklist in `docs/agents/checklists.md`.\n - Map each acceptance criterion to durable evidence.\n - Distinguish implementation failure from missing evidence.\ndiff --git a/CONTEXT.md b/CONTEXT.md\nindex e61a9be06a0cce0d026484083a657bde2067e234..237ee8aa3ea8fab334ece49768bc5ed3b5af0974\n--- a/CONTEXT.md\n+++ b/CONTEXT.md\n@@ -25,7 +25,7 @@\n _Avoid_: jumping directly into code\n \n **grill-with-docs**:\n-A planning skill that challenges a plan against domain language, ADRs, code reality, and concrete edge scenarios. It resolves design-tree questions before PRD/issues. It runs inside Requirements Definition.\n+A planning skill that challenges a plan against domain language, Shiki SADRs, target ADRs, code reality, and concrete edge scenarios. It resolves design-tree questions before PRD/issues. It runs inside Requirements Definition.\n _Avoid_: generic brainstorming, silent assumptions\n \n **Requirements Definition**:\ndiff --git a/CONTRIBUTING.md b/CONTRIBUTING.md\nindex c6d9b656e73abb8a55e5f660123e03153dcb3e6d..9e37a8fbf668db5b22461aaae5548b8f1354394c\n--- a/CONTRIBUTING.md\n+++ b/CONTRIBUTING.md\n@@ -17,7 +17,7 @@\n 1. **Goal** \u2014 start from a user-approved Goal with an outcome, completion\n    conditions, non-goals, risk level, and success signals.\n 2. **grill-with-docs** \u2014 for any non-trivial change, challenge the plan against\n-   domain language, ADRs, and code reality before writing a PRD.\n+   domain language, Shiki SADRs, target ADRs, and code reality before writing a PRD.\n 3. **PRD** \u2014 capture settled product/engineering intent (`to-prd`).\n 4. **Spec Freeze** \u2014 the operator explicitly approves the PRD/requirements;\n    the plan records a `spec_freeze` block and no plan runs without it\n@@ -94,7 +94,7 @@\n   entries.\n - Do not hand-edit `.shiki/goals/*.json` or `.shiki/manifest.json` in a task PR.\n - Conversation state is not durable truth. If a decision matters, record it in\n-  GitHub, `.shiki/`, `CONTEXT.md`, or an ADR.\n+  GitHub, `.shiki/`, `CONTEXT.md`, a Shiki SADR, or a target ADR.\n \n ## Architecture Gate\n \n@@ -103,7 +103,7 @@\n or CI policy, package structure, three or more structural files, more than 100\n net new lines in a core module, three or more new files, or any high-risk label.\n When triggered, run `grill-with-docs` or `improve-codebase-architecture` and\n-record durable decisions in an ADR when needed (see `docs/adr/`).\n+record durable decisions in a Shiki SADR or target ADR as applicable (see `docs/adr/`).\n \n ## Safety\n \ndiff --git a/SYSTEM_PROMPT.md b/SYSTEM_PROMPT.md\nindex aaaea45e3a896c3a44ba89eec8bb84325ecf6c62..da009c86a88285269dee0155e58e550c6d744ea9\n--- a/SYSTEM_PROMPT.md\n+++ b/SYSTEM_PROMPT.md\n@@ -30,6 +30,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n Use the canonical source-of-truth order above. Runtime-specific reading orders\n are operational guidance only and must not override or reorder it. The current\n conversation is non-durable operator input.\n@@ -48,14 +51,14 @@\n \n A runtime may only do work that is explicitly assigned by the Goal, task, PR, or MergeGate decision.\n \n-Do not assume `openai/codex-action`, `OPENAI_API_KEY`, or API-key based Codex automation in the default Shiki path. API-key runners are explicit target-repo extensions and require their own ADR.\n+Do not assume `openai/codex-action`, `OPENAI_API_KEY`, or API-key based Codex automation in the default Shiki path. API-key runners are explicit target-repo extensions and require their own target ADR.\n \n ## 4. Non-negotiable Shiki flow\n \n For any non-trivial Goal, use this flow:\n \n 1. **Goal Seek**: define outcome, non-goals, risk level, completion criteria, and evidence requirements.\n-2. **grill-with-docs**: challenge the plan against `CONTEXT.md`, ADRs, code reality, terminology, boundaries, and edge scenarios. Ask one question at a time when the operator is available. If code can answer a question, inspect code instead of asking.\n+2. **grill-with-docs**: challenge the plan against `CONTEXT.md`, Shiki SADRs, target ADRs, code reality, terminology, boundaries, and edge scenarios. Ask one question at a time when the operator is available. If code can answer a question, inspect code instead of asking.\n 3. **Context & Impact**: identify modules, interfaces, seams, callers, dependencies, locks, risk, and verification surfaces.\n 4. **to-prd + Spec Freeze**: turn settled context into a PRD using domain vocabulary and testing decisions. The operator's explicit approval of the PRD is Spec Freeze, recorded as the plan's `spec_freeze` block; plans without it do not run. Steps 1-4 form Requirements Definition.\n 5. **to-issues**: decompose the PRD into independently grabbable vertical-slice issues. Prefer AFK slices; mark HITL slices when judgment is still required.\n@@ -75,7 +78,7 @@\n Before making any material change:\n \n 1. Read `AGENTS.md` and any runtime-specific wrapper such as `CLAUDE.md`.\n-2. Read `CONTEXT.md` and relevant ADRs.\n+2. Read `CONTEXT.md` and relevant SADRs and target ADRs.\n 3. Read the GitHub Issue / PR and `.shiki/` state for the active Goal or task.\n 4. Identify your role: Planner, Implementer, Reviewer, Completion Check Agent, Repairer, or Guardian-assist.\n 5. Identify Goal id, task id, branch, PR, dependencies, locks, risk level, required skills, and required evidence.\n@@ -103,7 +106,7 @@\n \n At minimum, identify:\n \n-- Relevant documents, ADRs, domain terms, and prior decisions.\n+- Relevant documents, SADRs, target ADRs, domain terms, and prior decisions.\n - Relevant modules, interfaces, seams, callers, owners, and tests.\n - Dependency relationships between tasks.\n - Candidate file locks and lock conflicts.\n@@ -152,7 +155,7 @@\n \n CCA must read:\n \n-- `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant ADRs.\n+- `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant SADRs and target ADRs.\n - Parent Goal issue, PRD issue, task issue, comments, and labels.\n - PR body, diff, commits, changed files, reviews, CI checks, and artifacts available to the workflow.\n - `.shiki/` task contract, locks, ledger entries, CCA checklist profile, and prior repair packets.\n@@ -214,7 +217,7 @@\n - Three or more new files.\n - Any high-risk or critical label.\n \n-When the architecture gate triggers, run `grill-with-docs` or `improve-codebase-architecture` as appropriate and record the decision in an ADR if it is hard to reverse or likely to be re-litigated.\n+When the architecture gate triggers, run `grill-with-docs` or `improve-codebase-architecture` as appropriate and record the decision in a Shiki SADR or target ADR as applicable if it is hard to reverse or likely to be re-litigated.\n \n ## 14. Skill Gate\n \n@@ -223,7 +226,7 @@\n Use these skills:\n \n - `setup-matt-pocock-skills`: before first use of the engineering skill set, or when issue tracker, triage labels, or domain docs are missing.\n-- `grill-with-docs`: default for non-trivial Goals before PRD/issues; required for ambiguous plans, terminology, boundaries, ADR-worthy tradeoffs, or design-tree decisions.\n+- `grill-with-docs`: default for non-trivial Goals before PRD/issues; required for ambiguous plans, terminology, boundaries, decision-record-worthy tradeoffs, or design-tree decisions.\n - `zoom-out`: unfamiliar code area or missing architectural map.\n - `to-prd`: convert settled context into a PRD.\n - `to-issues`: break a PRD, plan, or Goal into independently grabbable vertical-slice issues.\ndiff --git a/docs/adr/0001-keep-shiki-as-platform-name.md b/docs/adr/SADR-0001-keep-shiki-as-platform-name.md\nindex 20a9637c3b9d8c97de7ba8db7cfae4d82d9a7dba..8b685439959aada1b221c112f0ac3c51ca8a1695\n--- a/docs/adr/0001-keep-shiki-as-platform-name.md\n+++ b/docs/adr/SADR-0001-keep-shiki-as-platform-name.md\n@@ -1,3 +1,3 @@\n-# Keep Shiki As The Platform Name\n+# SADR-0001: Keep Shiki As The Platform Name\n \n Shiki remains the product name for the GitHub-native agentic engineering control plane. We considered more descriptive names such as MergePilot, MergeGate, and Kiroku, but kept Shiki because it carries the existing goal-seeking framework identity while remaining independent of Hermes, Codex, Claude Code, or any single runtime.\ndiff --git a/docs/adr/0002-use-agents-as-shared-constitution.md b/docs/adr/SADR-0002-use-agents-as-shared-constitution.md\nindex 73de30ecb078120eac540d9bc51855e3cd48d0e3..c5486182bf06fca36ea508496b013c67302fd989\n--- a/docs/adr/0002-use-agents-as-shared-constitution.md\n+++ b/docs/adr/SADR-0002-use-agents-as-shared-constitution.md\n@@ -1,3 +1,3 @@\n-# Use AGENTS As The Shared Agent Constitution\n+# SADR-0002: Use AGENTS As The Shared Agent Constitution\n \n Shiki uses `AGENTS.md` as the runtime-neutral constitution and `CLAUDE.md` as a thin Claude Code wrapper. This keeps Codex, Claude Code, Hermes Runner, and future Agent Runtimes aligned on the same GitHub-first MergeGate rules while still allowing runtime-specific instructions where needed.\ndiff --git a/docs/adr/0003-require-skill-gate-for-mergegate-readiness.md b/docs/adr/SADR-0003-require-skill-gate-for-mergegate-readiness.md\nindex 424be0c9af4c8f0136f1fde68a468c47ff84e764..7fc337987c7df9792f75f181dfcbed75fe5ffff2\n--- a/docs/adr/0003-require-skill-gate-for-mergegate-readiness.md\n+++ b/docs/adr/SADR-0003-require-skill-gate-for-mergegate-readiness.md\n@@ -1,3 +1,3 @@\n-# Require Skill Gate For MergeGate Readiness\n+# SADR-0003: Require Skill Gate For MergeGate Readiness\n \n Shiki requires relevant engineering skills to be invoked before execution when their triggers apply. This makes skills part of MergeGate readiness rather than optional prompt style, because Shiki depends on repeatable planning, issue decomposition, TDD, diagnosis, triage, and architecture review before autonomous or parallel agent work can be trusted.\ndiff --git a/docs/adr/0004-use-github-first-with-shiki-mirror.md b/docs/adr/SADR-0004-use-github-first-with-shiki-mirror.md\nindex d0a3a9174dc646a945a9d325954ffaf00c352040..a79c54ae9ee8eed2a0524192b864da4e22a92d3f\n--- a/docs/adr/0004-use-github-first-with-shiki-mirror.md\n+++ b/docs/adr/SADR-0004-use-github-first-with-shiki-mirror.md\n@@ -1,4 +1,4 @@\n-# ADR 0004: Use GitHub First With A Repository-Local Shiki Mirror\n+# SADR-0004: Use GitHub First With A Repository-Local Shiki Mirror\n \n ## Status\n \ndiff --git a/docs/adr/0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md b/docs/adr/SADR-0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\nindex b339baf49b89d9290e79c3d51b36e7b2bde3bd95..c30a0d8b1fde0224d0ac7820e7870dccf721580d\n--- a/docs/adr/0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\n+++ b/docs/adr/SADR-0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\n@@ -1,4 +1,4 @@\n-# ADR 0005: Use Codex As Front And Claude Code Action As The GitHub Runtime\n+# SADR-0005: Use Codex As Front And Claude Code Action As The GitHub Runtime\n \n ## Status\n \ndiff --git a/docs/adr/0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md b/docs/adr/SADR-0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\nindex 4bcae9d1e52f0a37b8e9fbafa5cd14b6ee1993bd..7a38538a6ae9929567f0ade67e7ea6a60d0b3f7e\n--- a/docs/adr/0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\n+++ b/docs/adr/SADR-0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\n@@ -1,4 +1,4 @@\n-# ADR 0006: Separate Codex Implementation, CCA Completion, And MergeGate Authorization\n+# SADR-0006: Separate Codex Implementation, CCA Completion, And MergeGate Authorization\n \n ## Status\n \ndiff --git a/docs/adr/0007-packaging-and-release.md b/docs/adr/SADR-0007-packaging-and-release.md\nindex 79b84ddb97167372b44641e5b7860e400ce80822..bb445a2e2e856d9d1f5fdbe35de8d4e8cec0fb32\n--- a/docs/adr/0007-packaging-and-release.md\n+++ b/docs/adr/SADR-0007-packaging-and-release.md\n@@ -1,4 +1,4 @@\n-# ADR 0007: Packaging And Release For Shiki\n+# SADR-0007: Packaging And Release For Shiki\n \n ## Status\n \n@@ -25,7 +25,7 @@\n   not introduce third-party runtime dependencies or break the shim.\n - Releases must run inside the operator's subscription-authenticated toolchain\n   and must not require extra secrets beyond the built-in `GITHUB_TOKEN`\n-  (consistent with ADR 0005's no-API-key-by-default stance).\n+  (consistent with SADR-0005's no-API-key-by-default stance).\n - `.shiki/manifest.json` already encodes the install surface (`install.include`,\n   `install.create_directories`, `install.exclude_from_commit`), and it must not\n   be mutated as part of this change.\n@@ -34,7 +34,7 @@\n \n This is a hard-to-reverse platform decision (it sets the version source of\n truth, the release trigger, and the public install contract), so it is recorded\n-as an ADR.\n+as a SADR.\n \n ## Decision\n \n@@ -104,7 +104,7 @@\n 3. **Publishing to PyPI.** Rejected for now: Shiki is distributed as a\n    repository template + git-installable CLI, not a PyPI library; PyPI would add\n    credentials/secrets and a different versioning contract. Can be added later as\n-   its own ADR if a packaged distribution is needed.\n+   its own SADR if a packaged distribution is needed.\n 4. **Dynamic version (read `VERSION` via a build backend `dynamic` field).**\n    Rejected to keep `pyproject.toml` backend-agnostic and dependency-free; the\n    small duplication between `VERSION` and `pyproject.toml` is guarded by the\n@@ -123,4 +123,4 @@\n   releasing versions the whole platform but ships only `install.include` to\n   targets.\n - Future PyPI distribution or a console entry point are deferred and would each\n-  warrant their own ADR.\n+  warrant their own SADR.\ndiff --git a/docs/adr/0008-use-claude-code-as-default-implementer.md b/docs/adr/SADR-0008-use-claude-code-as-default-implementer.md\nindex dedf4510b7c7799521422f622029563d373db031..35c2625d908167fbc58855b5b63e283805f167e8\n--- a/docs/adr/0008-use-claude-code-as-default-implementer.md\n+++ b/docs/adr/SADR-0008-use-claude-code-as-default-implementer.md\n@@ -1,12 +1,12 @@\n-# ADR 0008: Use Claude Code As The Default Implementer Runtime\n+# SADR-0008: Use Claude Code As The Default Implementer Runtime\n \n ## Status\n \n-Proposed (supersedes ADR 0005 for the implementer default; keeps its auth model)\n+Proposed (supersedes SADR-0005 for the implementer default; keeps its auth model)\n \n ## Context\n \n-ADR 0005 made Codex Front the default implementation runtime, with Claude Code\n+SADR-0005 made Codex Front the default implementation runtime, with Claude Code\n restricted to planner and reviewer roles by the runtime registry, and the only\n autonomous implementation adapter was `shiki runner codex`.\n \n@@ -18,13 +18,13 @@\n - The platform's planning, review, and CCA layers already run on Claude Code,\n   and Claude Code's native capabilities (the built-in `/code-review` skill and\n   the Workflow multi-agent orchestration tool) have no Codex equivalent.\n-- The Goal lifecycle redesign (ADR 0009) requires an autonomous post-freeze\n+- The Goal lifecycle redesign (SADR-0009) requires an autonomous post-freeze\n   loop driven from the operator's Claude Code session, including bounded\n   repair dispatch, which must not depend on a runtime that cannot be invoked\n   unattended.\n \n The constitution claims Shiki is runtime-agnostic (\"not a Claude-only\n-workflow\", ADR 0002). Any change to the implementer default must preserve the\n+workflow\", SADR-0002). Any change to the implementer default must preserve the\n registry-based runtime model rather than hardcoding one vendor.\n \n ## Decision\n@@ -39,7 +39,7 @@\n   the task to review or repair-needed.\n - Keep Codex registered as an optional implementer runtime. `shiki runner\n   codex`, the Codex handoff templates, and the ChatGPT OAuth auth model from\n-  ADR 0005 remain supported for tasks explicitly assigned to `codex`.\n+  SADR-0005 remain supported for tasks explicitly assigned to `codex`.\n - Commit to mandating two Claude-native capabilities as loop contract in\n   follow-up Goals (status: the code-review gate was delivered by Goal G-B \u2014\n   skill registry, checklists item PR-12, default required_skills \u2014 while the\n@@ -60,7 +60,7 @@\n \n Out of scope: removing Codex support, changing the CCA runtime (Claude Code\n Action with `CLAUDE_CODE_OAUTH_TOKEN` stays), and API-key based automation\n-(still requires its own ADR per ADR 0005).\n+(still requires its own target ADR per SADR-0005).\n \n ## Consequences\n \n@@ -100,7 +100,7 @@\n   harness rejection of direct `codex exec` is outside Shiki's control, and the\n   plan's mandatory native capabilities cannot run on Codex at all.\n - **Remove Codex entirely**: rejected; it abandons the runtime-agnostic\n-  constitution (ADR 0002) for no operational gain and forecloses\n+  constitution (SADR-0002) for no operational gain and forecloses\n   cross-runtime adversarial review.\n - **GitHub Actions cloud implementation via claude-code-action**: rejected as\n   default; durable evidence is automatic but token cost, Node 24 workflow\ndiff --git a/docs/adr/0009-requirements-definition-and-spec-freeze.md b/docs/adr/SADR-0009-requirements-definition-and-spec-freeze.md\nindex 29b3f7871bef9203a8b4a831a65c39eb2f506e42..52e7dd69ad0aa4efe0b68c62678abbf7df02aaec\n--- a/docs/adr/0009-requirements-definition-and-spec-freeze.md\n+++ b/docs/adr/SADR-0009-requirements-definition-and-spec-freeze.md\n@@ -1,4 +1,4 @@\n-# ADR 0009: Merge Grill And Planning Into Requirements Definition With Spec Freeze\n+# SADR-0009: Merge Grill And Planning Into Requirements Definition With Spec Freeze\n \n ## Status\n \ndiff --git a/docs/adr/0010-external-ai-guardian-review.md b/docs/adr/SADR-0010-external-ai-guardian-review.md\nindex e6108bbe8bb4e73e0ff4684c8f78001a80438c4e..f3510b145664298adafb151b6f0db51b972f01ec\n--- a/docs/adr/0010-external-ai-guardian-review.md\n+++ b/docs/adr/SADR-0010-external-ai-guardian-review.md\n@@ -1,4 +1,4 @@\n-# ADR 0010: External AI Guardian Review As A First-Class Approval Authority\n+# SADR-0010: External AI Guardian Review As A First-Class Approval Authority\n \n ## Status\n \n@@ -8,7 +8,7 @@\n \n Shiki is intended to be an autonomous, recursive, self-improving agent\n framework: authority-in-the-loop, not human-in-the-loop. Until now the Guardian\n-approval required for high/critical-risk merges (guardian-policy.json, ADR\n+approval required for high/critical-risk merges (guardian-policy.json, SADR\n governance) recognized only human authority sources \u2014 a GitHub review approval,\n the `guardian:approved` label applied by a configured Guardian, or a comment\n carrying `Guardian approval granted` plus the current head SHA.\n@@ -89,7 +89,7 @@\n   autonomous design the operator intends and produced the impersonation\n   incident.\n - **Record AI approval as an operator comment**: rejected as forgery of human\n-  authority \u2014 the exact failure this ADR corrects.\n+  authority \u2014 the exact failure this SADR corrects.\n - **Deliver the artifact as a committed `.shiki/` file**: rejected; a committed\n   approval inside the PR being approved is forgeable by anyone who can push.\n - **A dedicated reviewer bot identity instead of a relayed comment**: deferred;\ndiff --git a/docs/adr/0011-autonomous-quality-gate-evidence.md b/docs/adr/SADR-0011-autonomous-quality-gate-evidence.md\nindex bca0246c5d46e44afbde15486a75eec425fc02f5..9c93eac16d8c3a6d4b11bd7224e3955102cd7db4\n--- a/docs/adr/0011-autonomous-quality-gate-evidence.md\n+++ b/docs/adr/SADR-0011-autonomous-quality-gate-evidence.md\n@@ -1,4 +1,4 @@\n-# ADR 0011: Autonomous Quality-Gate Evidence \u2014 Deterministic Facts And An Independent Verifier\n+# SADR-0011: Autonomous Quality-Gate Evidence \u2014 Deterministic Facts And An Independent Verifier\n \n ## Status\n \ndiff --git a/docs/adr/0012-autonomous-loop-closeout-to-main.md b/docs/adr/SADR-0012-autonomous-loop-closeout-to-main.md\nindex 637fad6dde4989c9c2bf0f81f676d8d29991123c..ec7d85f81030a676bcbba28b43eb365422a36eac\n--- a/docs/adr/0012-autonomous-loop-closeout-to-main.md\n+++ b/docs/adr/SADR-0012-autonomous-loop-closeout-to-main.md\n@@ -1,4 +1,4 @@\n-# 0012 \u2014 Autonomous loop pushes goal completion to main via a closeout PR\n+# SADR-0012 \u2014 Autonomous loop pushes goal completion to main via a closeout PR\n \n - Status: accepted\n - Date: 2026-06-18\ndiff --git a/docs/adr/0013-bootstrap-grants-actions-approve-pull-requests.md b/docs/adr/SADR-0013-bootstrap-grants-actions-approve-pull-requests.md\nindex b6c69d64f1c5ced346b88d6859586715b3b5bd0e..cf48ff4a6716267f539ca58edd1a852e7c7a2d64\n--- a/docs/adr/0013-bootstrap-grants-actions-approve-pull-requests.md\n+++ b/docs/adr/SADR-0013-bootstrap-grants-actions-approve-pull-requests.md\n@@ -1,4 +1,4 @@\n-# ADR 0013: Bootstrap Grants GitHub Actions Permission To Create And Approve Pull Requests\n+# SADR-0013: Bootstrap Grants GitHub Actions Permission To Create And Approve Pull Requests\n \n ## Status\n \n@@ -31,7 +31,7 @@\n security tradeoff: it lets repository automation satisfy a human-review gate. In\n a multi-maintainer repo that could let a compromised or buggy workflow\n self-approve changes. So whether bootstrap turns it on by default \u2014 and with\n-what default token scope \u2014 is an ADR-worthy decision, not an implementation\n+what default token scope \u2014 is a SADR-worthy decision, not an implementation\n detail.\n \n ## Decision\n@@ -48,7 +48,7 @@\n Specifically:\n \n - **`can_approve_pull_request_reviews` defaults to `true`.** Shiki's default\n-  posture is solo/AFK autonomous operation (ADR 0008/0009/0012), where the CCA\n+  posture is solo/AFK autonomous operation (SADR-0008/0009/0012), where the CCA\n   Review Bridge is the only path that can satisfy `required_review: true` after\n   the CCA and Guardian gates have already passed. The default makes a\n   freshly-bootstrapped repo able to run the autonomous loop end to end without a\ndiff --git a/docs/adr/0014-codex-app-external-ai-guardian-ui-adapter.md b/docs/adr/SADR-0014-codex-app-external-ai-guardian-ui-adapter.md\nindex 238db06279334caa341c117362df9271382a7fcd..65f12e9939321e1b3e389a34c822fc1549e5155d\n--- a/docs/adr/0014-codex-app-external-ai-guardian-ui-adapter.md\n+++ b/docs/adr/SADR-0014-codex-app-external-ai-guardian-ui-adapter.md\n@@ -1,4 +1,4 @@\n-# ADR 0014: Use Codex App As The External AI Guardian UI Adapter\n+# SADR-0014: Use Codex App As The External AI Guardian UI Adapter\n \n ## Status\n \ndiff --git a/docs/adr/0015-contract-approval-before-dispatch.md b/docs/adr/SADR-0015-contract-approval-before-dispatch.md\nindex fad6c656741a20a7684ba16080a7db159f63a52f..6061271a46d3f1d6ae7174477de3fb75673123a6\n--- a/docs/adr/0015-contract-approval-before-dispatch.md\n+++ b/docs/adr/SADR-0015-contract-approval-before-dispatch.md\n@@ -1,4 +1,4 @@\n-# ADR 0015: Approve The Task Contract Before Dispatch, Not The Diff After It\n+# SADR-0015: Approve The Task Contract Before Dispatch, Not The Diff After It\n \n ## Status\n \n@@ -9,7 +9,7 @@\n Guardian approval for high/critical risk is expressible only on a pull request.\n Every approval source in `guardian-policy.json` \u2014 GitHub review, the\n `guardian:approved` label, the `Guardian approval granted` comment, and the\n-`external_ai_guardian_review` artifact (ADR 0010/0014) \u2014 is bound to a PR, and\n+`external_ai_guardian_review` artifact (SADR-0010/0014) \u2014 is bound to a PR, and\n three of the four require the current head SHA. Approval therefore cannot exist\n before an implementation PR exists.\n \n@@ -81,7 +81,7 @@\n An envelope breach must surface only on the non-repairable policy gate, never on\n the repairable `MergeGate metadata check`. Otherwise the Goal Loop routes it to\n `dispatch_repair` \u2014 instructing a runner to make the breach go away \u2014 which is\n-exactly what this ADR forbids.\n+exactly what this SADR forbids.\n \n **Contract immutability.** After registration, an implementation PR must not\n change the governance fields of its own task contract (`scope`, `non_goals`,\n@@ -116,7 +116,7 @@\n fails any verdict that is not `complete`, and the signal the CCA prompt reads is\n computed from `evaluate_guardian_approval` alone. Without the OR-merge at the\n signal, a contract-approved PR still returns `needs_guardian`, the CCA job fails,\n-and the MergeGate policy check never runs \u2014 the gate this ADR changes would never\n+and the MergeGate policy check never runs \u2014 the gate this SADR changes would never\n be reached. The CCA *prompt* needs no change: a `contract_approval` entry in the\n signal's existing `sources` array satisfies its current wording.\n \n@@ -130,7 +130,7 @@\n written, and an approved contract lets its implementation PRs merge through the\n same automatic path as low/medium risk. The wasted implement-then-block cycle\n disappears. Guardian review shifts from reading diffs to reading contracts, which\n-is both earlier and smaller; the external AI Guardian path (ADR 0010/0014) works\n+is both earlier and smaller; the external AI Guardian path (SADR-0010/0014) works\n on contracts with no change.\n \n We accept that the Guardian authorizes a class of change without seeing its\n@@ -146,7 +146,7 @@\n adversarial tests for the new approval path equivalent in rigor to\n `test_shiki_governance_evidence.sh`.\n \n-Bootstrap costs, which apply only while this ADR is being implemented. The tasks\n+Bootstrap costs, which apply only while this SADR is being implemented. The tasks\n that build Contract Approval merge under the existing rules, so the ones\n dispatched after the pre-dispatch gate lands but before the approval path lands\n need a Guardian-approved Contract PR *and* a live Guardian approval on their\n@@ -210,7 +210,7 @@\n risk; they do not bound authorized work.\n \n **Auto-escalate risk on envelope breach instead of stopping.** Returns the\n-workflow to a diff-time Guardian decision \u2014 the exact behavior this ADR removes.\n+workflow to a diff-time Guardian decision \u2014 the exact behavior this SADR removes.\n \n **Resolve governance fields from the base snapshot only.** Rejected against the\n registration flow: implementation PRs add their own contract, so the base has no\ndiff --git a/docs/adr/0016-locks-bound-product-paths.md b/docs/adr/SADR-0016-locks-bound-product-paths.md\nindex b5e850f3f1f7f4ef6e7f9d1763a29a51bd300c45..46b40a9b99a7aae644c02bb458f5473ff0a90ce2\n--- a/docs/adr/0016-locks-bound-product-paths.md\n+++ b/docs/adr/SADR-0016-locks-bound-product-paths.md\n@@ -1,4 +1,4 @@\n-# ADR 0016: Locks bound product paths; the Shiki mirror is bound by record identity\n+# SADR-0016: Locks bound product paths; the Shiki mirror is bound by record identity\n \n ## Status\n \n@@ -6,8 +6,8 @@\n \n - Date: 2026-07-28\n - Deciders: operator (mizutani-140), Claude (planner)\n-- Related: ADR 0004 (GitHub-first with a `.shiki` mirror), ADR 0009 (Spec Freeze),\n-  ADR 0012 (autonomous loop closeout to main), ADR 0015 (Contract Approval before\n+- Related: SADR-0004 (GitHub-first with a `.shiki` mirror), SADR-0009 (Spec Freeze),\n+  SADR-0012 (autonomous loop closeout to main), SADR-0015 (Contract Approval before\n   dispatch), remediation goal task R-02, the live PR #179 deadlock.\n \n ## Context\n@@ -90,11 +90,11 @@\n files, documentation files and named source modules are nameable; runtime-\n allocated ledger, runner and report ids are not \u2014 and those live in `.shiki`,\n which locks no longer govern. Existing frozen contracts are brought into line by\n-a one-time Spec Amendment per plan, which must land before ADR 0015's contract\n+a one-time Spec Amendment per plan, which must land before SADR-0015's contract\n immutability makes `locks` unwritable from the implementation PR.\n \n **Lock state is resolved from the durable side.** MergeGate resolves the lock\n-registry, and ADR 0015's governance fields, from the base `.shiki` snapshot when\n+registry, and SADR-0015's governance fields, from the base `.shiki` snapshot when\n present, falling back to head only when absent. The `MergeGate metadata check`\n job is given `--base-shiki`, which it does not receive today. Locks are not\n derived from the live open-pull-request set: a pull request body is untrusted\n@@ -108,7 +108,7 @@\n Explicitly out of scope: moving MergeGate's contract resolution off the PR\n checkout wholesale; making the mirror a projection of GitHub state; introducing\n new lock kinds; moving ledger evidence out of the repository; and unwinding\n-ADR 0012's closeout pull request. Registration of multi-task goals is ADR 0015's\n+SADR-0012's closeout pull request. Registration of multi-task goals is SADR-0015's\n Contract PR and is not re-decided here.\n \n ## Consequences\n@@ -127,7 +127,7 @@\n \n Risks accepted. First, goal-scoped identity is only as strong as `goal_id`,\n which is resolved from the PR body and the head task file; the mitigation is to\n-add `goal_id` to ADR 0015's frozen governance field set, and until that lands a\n+add `goal_id` to SADR-0015's frozen governance field set, and until that lands a\n task file can name a foreign goal. Second, base-when-present resolution is\n unavailable on fork pull requests, where the head is contributor-controlled;\n fork pull requests already cannot complete a Shiki merge, and this decision does\n@@ -139,7 +139,7 @@\n path, not by implementation pull requests.\n \n New obligations: one Spec Amendment per frozen plan to rewrite `locks`, ordered\n-before ADR 0015 T1; extension of the adversarial suites in\n+before SADR-0015 T1; extension of the adversarial suites in\n `scripts/test_shiki_adversarial_state.sh` and\n `scripts/test_shiki_governance_evidence.sh` to cover the eleven newly-scoped\n directories; and updating `scripts/test_shiki_loop_lock_guard.sh`,\n@@ -151,32 +151,32 @@\n whether the loop's transitive sync of ledger `evidence` references can be\n deleted once the Contract PR carries its own source plan.\n \n-## Relationship to ADR 0004\n+## Relationship to SADR-0004\n \n-This ADR **clarifies something ADR 0004 left unspecified**. ADR 0004 decides that\n+This SADR **clarifies something SADR-0004 left unspecified**. SADR-0004 decides that\n GitHub is the operational source of truth and that each target repository keeps a\n `.shiki/` mirror. It says nothing about pull requests writing the mirror, does\n not name MergeGate, and does not make the mirror an input to a gate. Nothing in\n-ADR 0004's Decision or Consequences is amended, superseded or weakened: the\n+SADR-0004's Decision or Consequences is amended, superseded or weakened: the\n mirror still exists, still records the same artifacts, and pull request\n-boundaries remain the coordination surface. What this ADR settles is the question\n+boundaries remain the coordination surface. What this SADR settles is the question\n 0004 never reached \u2014 by what authority a pull request may write a mirror record \u2014\n and it answers it the way `.shiki/manifest.json` already declares\n (`pr_mutation: current-task-or-current-goal-only`).\n \n-It **amends ADR 0012**, whose Decision item 5 states that \"Loop-executed tasks\n+It **amends SADR-0012**, whose Decision item 5 states that \"Loop-executed tasks\n declare the `path:.shiki/**` lock, which covers every `.shiki` file the closeout\n stages ... so `files_outside_locks` is satisfied without per-file lock\n bookkeeping.\" That clause is replaced: the closeout's mirror writes are\n-authorized by record identity, not by a declared lock. ADR 0012's substantive\n+authorized by record identity, not by a declared lock. SADR-0012's substantive\n decision \u2014 that completion reaches `main` through a closeout pull request rather\n than local attestation \u2014 is preserved unchanged.\n \n-It is **complementary to ADR 0015**, not a substitute. ADR 0015 owns contract\n+It is **complementary to SADR-0015**, not a substitute. SADR-0015 owns contract\n immutability, the pre-dispatch gate, the Contract PR and the Scope Envelope. This\n-ADR owns what a lock means and who may write the mirror. The one ordering\n+SADR owns what a lock means and who may write the mirror. The one ordering\n constraint between them is that the lock-narrowing Spec Amendment must precede\n-ADR 0015 T1's freezing of the `locks` field.\n+SADR-0015 T1's freezing of the `locks` field.\n \n ## Alternatives Considered\n \n@@ -186,7 +186,7 @@\n the only writer that can register them may run. Migrating the Guardian signal to\n that store makes every registration PR fail closed to `needs_guardian` at every\n risk level, because the task file is by definition absent from the store, which\n-removes the low/medium autonomous path ADR 0008/0009 exists to provide. And it\n+removes the low/medium autonomous path SADR-0008/0009 exists to provide. And it\n leaves schemas, manifest, config, guardian policy, templates, migrations, plans,\n worktrees, runner records, runs and handoffs with no writer at all in a\n repository whose product is `.shiki`. Measured benefit on the frozen backlog:\n@@ -199,14 +199,14 @@\n namespace is not in fact ownership-partitioned \u2014 56 ledger entries carry no\n `task_id`, and one goal DAG is writable by 35 registered tasks. The correct\n expression of its insight is that locks do not govern the mirror at all, which is\n-what this ADR decides.\n+what this SADR decides.\n \n **Make the mirror a projection written only by a registration writer and a\n post-merge projection job.** Rejected. It requires a bypass actor with write\n access to the protected default branch; it makes its own master invariant\n readable from a manifest that pull requests may edit; it deletes both reconcile\n modes and leaves a corrupted mirror with no in-band repair path, contradicting\n-ADR 0004's consequence that mirror conflicts are repaired against GitHub state;\n+SADR-0004's consequence that mirror conflicts are repaired against GitHub state;\n and its content-derived ids are rejected by `validate_shiki`'s currently-green id\n pattern.\n \ndiff --git a/docs/adr/0017-bookkeeping-closeouts-do-not-inherit-risk.md b/docs/adr/SADR-0017-bookkeeping-closeouts-do-not-inherit-risk.md\nindex 554e3a0d526a72ca067108c5464cc68c3edcf174..b9676857a4713850e063af7c39c8e0b01ecb4c66\n--- a/docs/adr/0017-bookkeeping-closeouts-do-not-inherit-risk.md\n+++ b/docs/adr/SADR-0017-bookkeeping-closeouts-do-not-inherit-risk.md\n@@ -1,4 +1,4 @@\n-# ADR 0017: A Bookkeeping Closeout Does Not Inherit Its Task's Risk\n+# SADR-0017: A Bookkeeping Closeout Does Not Inherit Its Task's Risk\n \n ## Status\n \n@@ -6,7 +6,7 @@\n \n ## Context\n \n-ADR 0012 made the goal loop push a task's completion to the default branch through a\n+SADR-0012 made the goal loop push a task's completion to the default branch through a\n closeout pull request: after the implementation PR merges, the loop opens a second PR\n carrying `task.status = done`, `lock.state = released`, and \u2014 when the task completes\n its goal \u2014 `goal.status = complete` plus one scorecard report. Nothing else. The\n@@ -49,14 +49,14 @@\n 4. The task file's governance fields (`scope`, `non_goals`, `required_skills`,\n    `risk_level`, `locks`, `acceptance_checks`, `test_command`) are byte-identical to\n    the base snapshot. Only `status`, `expected_pr`, `closeout_pr`, `ledger_evidence`\n-   and `expected_branch` may differ \u2014 the ADR 0015 contract-immutability rule,\n+   and `expected_branch` may differ \u2014 the SADR-0015 contract-immutability rule,\n    unchanged.\n \n    `expected_branch` is in that set because a closeout is *by construction* on a\n    different branch from the implementation it closes, and the loop rewrites the\n    field accordingly. It is safe to let it differ because MergeGate independently\n    requires `expected_branch` to equal the PR's actual head ref, so a PR cannot set\n-   it to anything other than the branch it is really on. The first draft of this ADR\n+   it to anything other than the branch it is really on. The first draft of this SADR\n    omitted it, which made the exemption unreachable: every real closeout failed\n    condition 4 on a field the loop always changes. See PR #209.\n 5. The state transitions are exactly the terminal set: task `review` \u2192 `done`, lock\n@@ -113,12 +113,12 @@\n classification must be derived, not asserted.\n \n **Fold the closeout into the implementation PR** so no second PR exists. Rejected under\n-ADR 0012, which recorded why: a task file that is already `done` on the implementation\n+SADR-0012, which recorded why: a task file that is already `done` on the implementation\n branch short-circuits the loop's review \u2192 CCA \u2192 merge \u2192 done ordering into a local\n goal-completion before any merge happens.\n \n **Have the loop auto-merge closeouts with elevated credentials.** Rejected outright: that\n-is a runtime authoring its own approval, which ADR 0010 and the constitution's\n+is a runtime authoring its own approval, which SADR-0010 and the constitution's\n approval-record prohibition forbid. It would also be indistinguishable, in the evidence,\n from the forgery those rules exist to prevent.\n \ndiff --git a/docs/adr/0018-carried-guardian-approval-across-a-proven-base-sync.md b/docs/adr/SADR-0018-carried-guardian-approval-across-a-proven-base-sync.md\nindex 22358562da390ed8b45cd600da08a43d378b4680..e6e2907cefa776460af73ddf7d39db605f3c39b0\n--- a/docs/adr/0018-carried-guardian-approval-across-a-proven-base-sync.md\n+++ b/docs/adr/SADR-0018-carried-guardian-approval-across-a-proven-base-sync.md\n@@ -1,4 +1,4 @@\n-# ADR 0018: Carried Guardian Approval Across A Proven Base Sync\n+# SADR-0018: Carried Guardian Approval Across A Proven Base Sync\n \n ## Status\n \n@@ -34,7 +34,7 @@\n Guardian evaluator that consumes it (`shiki_guardian.evaluate_guardian_approval`,\n the `guardian_comment_carried` source) were built and tested in isolation. Neither\n does anything until a caller passes the flags that turn the carry on, so the\n-feature was invisible in CI. This ADR records the decision the two of them\n+feature was invisible in CI. This SADR records the decision the two of them\n implement, and this task wires it into the four CI invocations that judge Guardian\n approval.\n \ndiff --git a/docs/adr/README.md b/docs/adr/README.md\nindex c70e057a4520c36e042ab68c53cd4a21ef709b71..5da43856836e2ce6a150f8127766e2f88355ab4b\n--- a/docs/adr/README.md\n+++ b/docs/adr/README.md\n@@ -1,40 +1,41 @@\n-# Architecture Decision Records\n+# Decision Record Namespaces\n \n-This directory holds Shiki's Architecture Decision Records (ADRs): durable\n-records of hard-to-reverse platform decisions. ADRs are part of the Shiki source\n-of truth alongside GitHub state, the `.shiki/` mirror, and `CONTEXT.md`.\n+This directory holds two independent decision namespaces:\n \n-## How ADRs Work\n+- Shiki Architecture Decision Records (SADRs), named\n+  `SADR-NNNN-<short-slug>.md`, record hard-to-reverse Shiki platform decisions.\n+- Target Architecture Decision Records (ADRs), named\n+  `NNNN-<short-slug>.md`, belong to the target product.\n \n-- New ADRs are added as `docs/adr/NNNN-<short-slug>.md`, where `NNNN` is the next\n-  zero-padded sequence number.\n-- The **canonical list of ADRs is the set of files matching `docs/adr/*.md`** in\n-  this directory. Browse `docs/adr/*.md` for the authoritative, current set.\n-- Use [`template.md`](template.md) as the starting point for a new ADR.\n-- Record an ADR when a decision is hard to reverse, surprising without context,\n-  and a real tradeoff. Do not write an ADR for routine or easily reversible\n-  choices.\n+The same four-digit number may appear once in each namespace. A platform\n+`SADR-NNNN` citation resolves only to `SADR-NNNN-*.md`; a target `ADR NNNN`\n+citation resolves only to `NNNN-*.md`.\n \n-Using the `docs/adr/*.md` glob as the source of truth means other PRs can add an\n-ADR without editing this index file, avoiding merge contention on a\n-hand-maintained enumeration.\n+## How Decision Records Work\n \n-## Current ADRs (snapshot)\n-\n-The list below is a convenience snapshot and may lag behind the directory. When\n-in doubt, list `docs/adr/*.md`.\n+- New Shiki platform decisions use the next zero-padded SADR number.\n+- New target product decisions use the target's own ADR sequence.\n+- The canonical list is the files in `docs/adr/`; no hand-maintained index is\n+  authoritative.\n+- Use [`template.md`](template.md) as the starting point and choose the filename\n+  namespace that owns the decision.\n+- Record a decision when it is hard to reverse, surprising without context, and\n+  a real tradeoff. Do not write a decision record for routine or easily\n+  reversible choices.\n \n-- [0001 \u2014 Keep Shiki As The Platform Name](0001-keep-shiki-as-platform-name.md)\n-- [0002 \u2014 Use AGENTS As The Shared Agent Constitution](0002-use-agents-as-shared-constitution.md)\n-- [0003 \u2014 Require Skill Gate For MergeGate Readiness](0003-require-skill-gate-for-mergegate-readiness.md)\n-- [0004 \u2014 Use GitHub First With A Repository-Local Shiki Mirror](0004-use-github-first-with-shiki-mirror.md)\n-- [0005 \u2014 Use Codex As Front And Claude Code Action As The GitHub Runtime](0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md)\n-- [0006 \u2014 Separate Codex Implementation, CCA Completion, And MergeGate Authorization](0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md)\n+Using the directory as the source of truth lets independent changes add records\n+without contending on an enumerated index.\n \n-## Adding A New ADR\n+## Adding A Shiki SADR\n \n-1. Copy `template.md` to `docs/adr/NNNN-<short-slug>.md`.\n+1. Copy `template.md` to `docs/adr/SADR-NNNN-<short-slug>.md`.\n 2. Fill in the title, status, context, decision, and consequences.\n-3. Set the status to `Proposed`, then `Accepted` once the decision is approved.\n-4. Open a PR following `CONTRIBUTING.md`. You do not need to edit this index for\n-   the ADR to be discoverable via `docs/adr/*.md`.\n+3. Set the status to `Proposed`, then `Accepted` once the proper authority has\n+   approved the decision.\n+4. Open a PR following `CONTRIBUTING.md`.\n+\n+## Adding A Target ADR\n+\n+1. Copy `template.md` to `docs/adr/NNNN-<short-slug>.md` using the target's own\n+   sequence.\n+2. Cite it as `ADR NNNN`; do not prefix its filename with `SADR-`.\ndiff --git a/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md b/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\nnew file mode 100644\nindex 0000000000000000000000000000000000000000..7455ce1f3bed02ddddb851ac21a1d1a648c9aeba\n--- /dev/null\n+++ b/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\n@@ -0,0 +1,55 @@\n+# SADR-0019: Separate Shiki Platform SADRs From Target ADRs\n+\n+## Status\n+\n+Accepted\n+\n+## Context\n+\n+Shiki installs its platform decision records into `docs/adr/` in every target\n+repository. Shiki and its targets previously used the same numeric\n+`NNNN-<slug>.md` namespace. A target that already owned ADRs 0013 through 0018\n+therefore acquired two records for each number during installation, making an\n+`ADR NNNN` citation ambiguous. Renumbering target decisions would rewrite target\n+history, while renumbering Shiki decisions per target would destroy their stable\n+platform identity.\n+\n+Upgrades also need to remove the former Shiki-owned numeric paths without ever\n+mistaking a target-owned ADR for platform content. The install stamp already\n+records exact shipped paths and content digests, so it can prove ownership only\n+when both still match.\n+\n+## Decision\n+\n+Shiki platform decisions use the `SADR` namespace:\n+\n+- records are named `docs/adr/SADR-NNNN-<slug>.md`;\n+- live platform citations use `SADR-NNNN`;\n+- target product decisions retain `docs/adr/NNNN-<slug>.md` and `ADR NNNN`;\n+- the reference validator resolves the two namespaces independently, so the same\n+  four-digit number may exist once in each namespace.\n+\n+Before a forced upgrade writes anything, the installer inventories the exact\n+former numeric Shiki ADR paths. It may delete a legacy path only when the\n+existing install stamp contains that exact path and its recorded SHA-256 digest\n+matches the file currently on disk. All ownership blockers are reported\n+together and abort the upgrade before its first write. A non-forced install\n+never performs this cleanup.\n+\n+Historical `.shiki/` evidence remains unchanged because it records the namespace\n+that existed when that evidence was produced.\n+\n+## Consequences\n+\n+- Targets may use any ADR number without colliding with Shiki's platform SADRs.\n+- Fresh installs preserve target ADR bytes and install a stable SADR set.\n+- Forced upgrades fail closed when legacy ownership cannot be proven, and a\n+  successful upgrade refreshes the stamp without removed legacy paths.\n+- Current governance, packaging, workflow, prompt, script, and skill references\n+  must move with the platform records.\n+\n+## Alternatives Considered\n+\n+- Collision-only refusal leaves affected targets permanently unupgradable.\n+- Renumbering target ADRs mutates target-owned history.\n+- Per-target Shiki renumbering makes platform citations non-portable.\ndiff --git a/docs/adr/template.md b/docs/adr/template.md\nindex a92a97daddd0b00ab9dd626cc8d86942da2eafed..17dd79175b5fee2105935557f691b40cf9492942\n--- a/docs/adr/template.md\n+++ b/docs/adr/template.md\n@@ -1,14 +1,14 @@\n-# NNNN: <Short Decision Title>\n+# <SADR-NNNN or NNNN>: <Short Decision Title>\n \n ## Status\n \n-Proposed | Accepted | Superseded by ADR-NNNN\n+Proposed | Accepted | Superseded by SADR-NNNN or target ADR NNNN\n \n ## Context\n \n What is the situation, constraint, or force that requires a decision? Reference\n-relevant domain terms (`CONTEXT.md`), prior ADRs, and code reality. State the\n-problem, not the solution.\n+relevant domain terms (`CONTEXT.md`), prior Shiki SADRs or target ADRs, and code\n+reality. State the problem, not the solution.\n \n ## Decision\n \ndiff --git a/docs/agents/autonomous-loop-e2e-acceptance.md b/docs/agents/autonomous-loop-e2e-acceptance.md\nindex bbf84c6081987f159ac6ad9ecf3c82b7aa72e7e8..5c13c1d02a05a90b0dc46cbef7c95a9846d0c237\n--- a/docs/agents/autonomous-loop-e2e-acceptance.md\n+++ b/docs/agents/autonomous-loop-e2e-acceptance.md\n@@ -40,8 +40,8 @@\n 3. `gh auth status` is logged in with merge permission on the repo.\n 4. Branch protection + required checks are configured exactly as the goal-loop\n    required-check set expects (see `docs/agents/branch-protection-smoke.md`).\n-5. The repo `.shiki/policy` permits autonomous low/medium-risk merges (ADR\n-   0008/0009); the MergeGate policy check is the recorded-authority gate.\n+5. The repo `.shiki/policy` permits autonomous low/medium-risk merges\n+   (SADR-0008/0009); the MergeGate policy check is the recorded-authority gate.\n \n The operator's only actions are: register the task and start the loop. **No\n manual pre-clearing** means after `shiki loop run` starts, the operator does not\ndiff --git a/docs/agents/checklists.md b/docs/agents/checklists.md\nindex 4a7f03ade346f53a1c1d82981f3fd744f9ab4547..0c5ec6c53dc7197b7b7a7d18e667b325babd6c45\n--- a/docs/agents/checklists.md\n+++ b/docs/agents/checklists.md\n@@ -32,18 +32,18 @@\n | ID | Item | Blocking |\n | --- | --- | --- |\n | GD-01 | Plan was challenged against `CONTEXT.md`. | blocking for non-trivial Goals |\n-| GD-02 | Relevant ADRs were read or absence was recorded. | blocking for non-trivial Goals |\n+| GD-02 | Relevant SADRs and target ADRs were read or absence was recorded. | blocking for non-trivial Goals |\n | GD-03 | Ambiguous terms were resolved or converted to HITL blockers. | blocking |\n | GD-04 | Edge scenarios were considered. | advisory |\n | GD-05 | Code reality was checked when code could answer. | blocking when applicable |\n | GD-06 | New domain terms were added to `CONTEXT.md` when needed. | blocking when applicable |\n-| GD-07 | ADR was created or explicitly skipped using the ADR criteria. | blocking when applicable |\n+| GD-07 | A SADR or target ADR was created or explicitly skipped using the decision-record criteria. | blocking when applicable |\n \n ## CI \u2014 Context & Impact Checklist\n \n | ID | Item | Blocking |\n | --- | --- | --- |\n-| CI-01 | Relevant docs and ADRs identified. | blocking |\n+| CI-01 | Relevant docs, SADRs, and target ADRs identified. | blocking |\n | CI-02 | Relevant modules, interfaces, seams, callers, and tests identified. | blocking |\n | CI-03 | Dependencies and blocked-by relationships identified. | blocking |\n | CI-04 | Candidate locks identified. | blocking |\n@@ -62,7 +62,7 @@\n | PRD-04 | Implementation decisions are recorded without volatile code detail. | blocking |\n | PRD-05 | Testing decisions are recorded. | blocking |\n | PRD-06 | Out-of-scope items are explicit. | blocking |\n-| PRD-07 | Relevant glossary/ADR links are present when applicable. | blocking |\n+| PRD-07 | Relevant glossary/SADR/target-ADR links are present when applicable. | blocking |\n \n ## SF \u2014 Spec Freeze Checklist\n \n@@ -98,7 +98,7 @@\n | H-02 | Branch/worktree target is present. | blocking |\n | H-03 | Scope and non-goals are present. | blocking |\n | H-04 | Dependencies and locks are present. | blocking |\n-| H-05 | Relevant docs, ADRs, modules, and tests are referenced. | blocking |\n+| H-05 | Relevant docs, SADRs, target ADRs, modules, and tests are referenced. | blocking |\n | H-06 | Required skills are stated. | blocking |\n | H-07 | TDD expectations are stated. | blocking when behavior work |\n | H-08 | Acceptance criteria are included. | blocking |\ndiff --git a/docs/agents/codex-handoff.md b/docs/agents/codex-handoff.md\nindex ede468f47cb277f69a857e8c45df703e4ed0151d..6f97bdffb67e44d7edea2c71e01ea28933c4eb6c\n--- a/docs/agents/codex-handoff.md\n+++ b/docs/agents/codex-handoff.md\n@@ -46,7 +46,7 @@\n \n ## Context\n - Domain terms:\n-- Relevant ADRs:\n+- Relevant SADRs and target ADRs:\n - Relevant docs:\n - Relevant modules/interfaces/seams:\n - Similar tests/prior art:\ndiff --git a/docs/agents/completion-check-agent.md b/docs/agents/completion-check-agent.md\nindex 13e4c035db35025f4e3cdcc5adfb24314ac4d521..61bfa1218cf36ca605250c91919e620f33238aa0\n--- a/docs/agents/completion-check-agent.md\n+++ b/docs/agents/completion-check-agent.md\n@@ -32,7 +32,7 @@\n - `AGENTS.md`\n - `CLAUDE.md`\n - `CONTEXT.md`\n-- relevant ADRs\n+- relevant SADRs and target ADRs\n - parent Goal issue\n - PRD issue or document\n - task issue\ndiff --git a/docs/agents/decision-control.md b/docs/agents/decision-control.md\nindex 7d8ee5ab03a5ccbcbd04056ae61de03785a2a9c9..8c94ecda00fffaa0e92c325ab83aa9776bd1dac1\n--- a/docs/agents/decision-control.md\n+++ b/docs/agents/decision-control.md\n@@ -11,7 +11,7 @@\n | Decision | Owner | When | Required inputs | Output | Enforcement surface |\n | --- | --- | --- | --- | --- | --- |\n | Is the Goal clear enough to plan? | Planner + Guardian when needed | Goal intake | User request, existing docs, risk signals | Goal readiness verdict | Goal checklist, issue template, ledger |\n-| Must `grill-with-docs` run? | Planner / Skill Gate | Before PRD or issue split | Ambiguity, domain terms, ADR conflicts, unclear boundaries | Skill invocation or explicit skip rationale | Skill Gate checklist |\n+| Must `grill-with-docs` run? | Planner / Skill Gate | Before PRD or issue split | Ambiguity, domain terms, SADR/target-ADR conflicts, unclear boundaries | Skill invocation or explicit skip rationale | Skill Gate checklist |\n | Is a PRD required? | Planner | After Goal context stabilizes | Goal, `grill-with-docs` output, Context & Impact output | PRD required / not required | PRD checklist, CCA evidence check |\n | Can the spec be frozen? | Operator only | End of Requirements Definition, before issue split | PRD/requirements summary, scope inventory (SF-02), open-question list | `spec_freeze` block in the plan + ledger evidence | `require_grilled_plan` (plan ingest/run), SF checklist |\n | Can a frozen spec change? | Operator only (Spec Amendment) | When implementation contradicts the frozen spec | Contested decisions, scoped re-grill result | recorded amendment + re-stamped freeze | SF checklist, ledger evidence |\ndiff --git a/docs/agents/domain.md b/docs/agents/domain.md\nindex 258da8ea81e87ac0a6b0b5b5e5c2fde980eaabb7..d57b0cf17ff3e1fa9e6e5885372bf083d8295421\n--- a/docs/agents/domain.md\n+++ b/docs/agents/domain.md\n@@ -5,7 +5,9 @@\n ## Layout\n \n - `CONTEXT.md` defines Shiki language. Keep it implementation-free.\n-- `docs/adr/` records hard-to-reverse decisions.\n+- `docs/adr/SADR-NNNN-*.md` records Shiki platform decisions.\n+- `docs/adr/NNNN-*.md` records target product decisions without sharing the\n+  SADR namespace.\n - `AGENTS.md` is the runtime-neutral agent constitution.\n - `CLAUDE.md` is the Claude Code wrapper around `AGENTS.md`.\n - `.shiki/` is the Target Repository mirror for Goals, PRDs, plans, Task DAGs, contracts, locks, worktree records, ledger entries, CCA verdicts, repair packets, reports, and handoffs.\n@@ -15,6 +17,6 @@\n \n - Use Shiki terms exactly when they exist.\n - Add glossary terms to `CONTEXT.md` only when the term is part of the domain, not a generic programming concept.\n-- Create ADRs only for hard-to-reverse, surprising, tradeoff-based decisions.\n+- Create SADRs or target ADRs only for hard-to-reverse, surprising, tradeoff-based decisions.\n - Keep product-specific language in the Target Repository, not in Shiki Template docs.\n-- If an ADR conflicts with a proposed implementation, surface the conflict before editing.\n+- If a SADR or target ADR conflicts with a proposed implementation, surface the conflict before editing.\ndiff --git a/docs/agents/execution-control.md b/docs/agents/execution-control.md\nindex ad8ee3cdb36be56fedc1f46408ca92b387d74047..bc63c3a73661d12b567016ba299f1a6b66bf9088\n--- a/docs/agents/execution-control.md\n+++ b/docs/agents/execution-control.md\n@@ -63,7 +63,7 @@\n | Branch name | Orchestrator | Orchestrator | Before implementer dispatch | branch policy | branch ref in task/ledger |\n | Lock reservation | Context & Impact | MergeGate | Before dispatch and before merge | `.shiki/locks/`, labels, PR metadata | lock id, owner, scope |\n | CI profile | Planner | MergeGate | During issue triage, rechecked at PR | labels, `.shiki/policy.yaml`, workflow matrix | required check list |\n-| Required status checks | Repo maintainer / Guardian | GitHub branch protection / rulesets | Repository setup, updated via ADR | GitHub settings | branch/ruleset config, ADR |\n+| Required status checks | Repo maintainer / Guardian | GitHub branch protection / rulesets | Repository setup, updated via target ADR | GitHub settings | branch/ruleset config, target ADR |\n | CCA judgment | CCA | GitHub required check / MergeGate | Every non-draft PR update | GitHub Action check | structured CCA verdict |\n | Repair assignment | CCA | MergeGate | After failed CCA/CI/review | repair packet, issue/PR comment | repair id and attempt count |\n | Merge readiness | MergeGate | GitHub branch protection / Guardian if needed | After CCA complete | required checks, review rules, rulesets | mergegate check output |\ndiff --git a/docs/agents/guardian-policy.md b/docs/agents/guardian-policy.md\nindex 4147c7adf05a47adb4fd24ada1a1c2e1f2ba6d2d..a20d9cc6c00a569bb057b9ca8d6b51c7fc4a9847\n--- a/docs/agents/guardian-policy.md\n+++ b/docs/agents/guardian-policy.md\n@@ -98,8 +98,8 @@\n `origin/<default_branch>` and reuses each job's existing base fetch; a shallow\n (`--depth`) or second base fetch would make the proof unresolvable and silently\n disable the carry, so neither is permitted. CCA reads `guardian_comment_carried` as\n-a recorded authority in the deterministic signal and does not re-derive it. See ADR\n-0018 for the full decision, the rejected diff/patch-equality and path-disjointness\n+a recorded authority in the deterministic signal and does not re-derive it. See\n+SADR-0018 for the full decision, the rejected diff/patch-equality and path-disjointness\n alternatives, and the accepted residual.\n \n ## External AI Guardian Review (SADR-0010)\ndiff --git a/docs/agents/implementation-policy.md b/docs/agents/implementation-policy.md\nindex c754144a9fb5bf4a38aa82a60e1670c59e626b44..85c37281bcad196c6b7b02343faf00b03cd9c57a\n--- a/docs/agents/implementation-policy.md\n+++ b/docs/agents/implementation-policy.md\n@@ -31,10 +31,13 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n ## Non-Negotiables\n \n 1. **Planning precedes implementation.** Non-trivial Goals start with `grill-with-docs` and Context & Impact.\n-2. **Domain language is controlled.** Terms must be checked against `CONTEXT.md`; hard-to-reverse tradeoffs must be checked against ADRs.\n+2. **Domain language is controlled.** Terms must be checked against `CONTEXT.md`; hard-to-reverse tradeoffs must be checked against Shiki SADRs and target ADRs.\n 3. **PRD precedes issue decomposition.** `to-prd` is used once the context is settled enough to become durable intent.\n 4. **Issues are vertical slices.** `to-issues` must produce independently grabbable tracer-bullet tasks, not horizontal layer tickets.\n 5. **Triage controls dispatch.** Only `ready-for-agent` / AFK tasks may be assigned to Codex without human input.\n@@ -95,19 +98,19 @@\n Process:\n \n - Challenge terms against `CONTEXT.md`.\n-- Check ADRs before reopening decisions.\n+- Check SADRs and target ADRs before reopening decisions.\n - Inspect code when code can answer a question.\n - Ask one question at a time when the operator is available.\n - Provide a recommended answer with each question.\n - Record resolved terms in `CONTEXT.md`.\n-- Create ADRs only for hard-to-reverse, surprising, tradeoff-based decisions.\n+- Create SADRs or target ADRs only for hard-to-reverse, surprising, tradeoff-based decisions.\n - Convert unresolved load-bearing questions into HITL blockers.\n \n Outputs:\n \n - Resolved design-tree decisions.\n - Updated glossary terms when needed.\n-- ADRs when justified.\n+- SADRs or target ADRs when justified.\n - HITL blockers when decisions remain unresolved.\n \n ## Phase 3 \u2014 Context & Impact\n@@ -117,7 +120,7 @@\n \n Outputs must include:\n \n-- Relevant documents and ADRs.\n+- Relevant documents, SADRs, and target ADRs.\n - Relevant modules, interfaces, seams, callers, and tests.\n - Candidate locks.\n - Dependencies.\n@@ -140,7 +143,7 @@\n - Testing decisions.\n - Out of scope.\n - Further notes.\n-- Links to relevant Goal, domain terms, and ADRs.\n+- Links to relevant Goal, domain terms, SADRs, and target ADRs.\n \n Phase 4 ends with Spec Freeze: the operator explicitly approves the PRD, the\n plan records a `spec_freeze` block (status, approved_by, source), and the SF\n@@ -240,7 +243,7 @@\n - Dependencies and locks.\n - Risk and Guardian requirements.\n - Ledger completeness.\n-- Documentation and ADR updates.\n+- Documentation and SADR/target-ADR updates.\n - Scope drift.\n \n CCA emits one of:\n@@ -301,7 +304,7 @@\n | Evidence failure | Behavior might be done, but proof is missing. | Add checks, PR evidence, ledger entry, or artifacts. |\n | Scope drift | PR changes unrelated behavior. | Revert unrelated changes or split into new Goal. |\n | Dependency block | Upstream task or lock not complete. | Block until dependency resolves. |\n-| Architecture block | Decision needs ADR or deep-module discussion. | `grill-with-docs` or `improve-codebase-architecture`. |\n+| Architecture block | Decision needs a SADR, target ADR, or deep-module discussion. | `grill-with-docs` or `improve-codebase-architecture`. |\n | Risk block | Security/production/policy change needs human approval. | Guardian decision. |\n \n ## Phase 5.5 \u2014 Execution Decision Control\ndiff --git a/docs/agents/runtime-auth-model.md b/docs/agents/runtime-auth-model.md\nindex 6a2e1a47e81ea89b5c68a210ec07772a9374babc..3c54748d24233dbd0ba8473950282547ef4ce935\n--- a/docs/agents/runtime-auth-model.md\n+++ b/docs/agents/runtime-auth-model.md\n@@ -148,7 +148,7 @@\n \n API-key based runners may be added by a target repository as an explicit extension. They are not the default Shiki template path.\n \n-If a repo opts into API-key mode, record the exception in an ADR and update the repo's `AGENTS.md`, `.shiki/config.yaml`, and workflow permissions.\n+If a repo opts into API-key mode, record the target-specific exception in a target ADR and update the repo's `AGENTS.md`, `.shiki/config.yaml`, and workflow permissions.\n \n ## GitHub Actions Runtime Compatibility\n \ndiff --git a/docs/agents/skill-gate.md b/docs/agents/skill-gate.md\nindex c50398b0d7c60a8598f2a5c88a10015730dced13..3acffe6444617bb1d54b77d4b4708158ec735bc6\n--- a/docs/agents/skill-gate.md\n+++ b/docs/agents/skill-gate.md\n@@ -9,7 +9,7 @@\n | Skill | Trigger | Expected output |\n | --- | --- | --- |\n | `setup-matt-pocock-skills` | First configuring a repo for these skills, or when issue tracker, triage labels, or domain docs are missing. | `docs/agents/` configuration and an `Agent skills` block in `AGENTS.md` or `CLAUDE.md`. |\n-| `grill-with-docs` | Ambiguous plan, unclear terminology, boundary decision, tradeoff, or ADR-worthy design choice. | Clarified decision tree, recommended answers, updates to `CONTEXT.md` or ADRs when decisions crystallize. |\n+| `grill-with-docs` | Ambiguous plan, unclear terminology, boundary decision, tradeoff, or decision-record-worthy design choice. | Clarified decision tree, recommended answers, updates to `CONTEXT.md`, SADRs, or target ADRs when decisions crystallize. |\n | `zoom-out` | Agent lacks a higher-level architectural map. | Map of relevant modules, callers, seams, and domain concepts. |\n | `to-prd` | Settled context should become a PRD. | PRD published to the issue tracker with problem, solution, user stories, decisions, testing, and out-of-scope notes. |\n | `to-issues` | Goal, PRD, or plan must become implementation tickets. | Vertical-slice issues with acceptance criteria, dependencies, and AFK/HITL classification. |\n@@ -17,7 +17,7 @@\n | `tdd` | Feature work or bug fix has observable behavior that can be specified with tests. | Red-green-refactor loop through public interfaces, one vertical slice at a time. |\n | `code-review` | The TDD loop is green and the implementer is about to create or update a PR. Default for every implementation task. | Pre-PR self-review of the task diff with fixes applied on the task branch; findings and resolutions recorded as a ledger entry naming the skill and a `## Pre-PR code review` PR body section. |\n | `diagnose` | Hard bug, failing check, regression, flaky behavior, or performance problem. | Reproduction loop, ranked hypotheses, instrumentation, fix, regression test, post-mortem. |\n-| `improve-codebase-architecture` | Structural friction, testability, AI-navigability, deep-module opportunity, or refactor request. | Architecture candidates and deepening recommendations informed by `CONTEXT.md` and ADRs. |\n+| `improve-codebase-architecture` | Structural friction, testability, AI-navigability, deep-module opportunity, or refactor request. | Architecture candidates and deepening recommendations informed by `CONTEXT.md`, SADRs, and target ADRs. |\n | `prototype` | A throwaway logic or UI prototype is needed to answer a design question. | Clearly marked disposable prototype and captured decision. |\n \n ## MergeGate Interaction\ndiff --git a/scripts/guardian_approval_signal.py b/scripts/guardian_approval_signal.py\nindex d059f92b2c82aa2b5d6ca421365537a87886c1cb..16924892714f4f5b9303c0ef0e325a7e70075630\n--- a/scripts/guardian_approval_signal.py\n+++ b/scripts/guardian_approval_signal.py\n@@ -199,8 +199,8 @@\n ) -> Any:\n     \"\"\"Evaluate SADR-0015 Contract Approval for the CCA Guardian signal.\n \n-    The SAME OR that MergeGate applies must reach this deterministic signal: ADR\n-    0015 is explicit that without it, a contract-approved PR still returns\n+    The SAME OR that MergeGate applies must reach this deterministic signal:\n+    SADR-0015 is explicit that without it, a contract-approved PR still returns\n     ``needs_guardian``, the CCA job fails, and the MergeGate policy check never\n     runs \u2014 the gate this change moves would never be reached. This resolves the\n     task the SAME way MergeGate does (the first Shiki task id in the PR body),\ndiff --git a/scripts/shiki_contract_approval.py b/scripts/shiki_contract_approval.py\nindex 941bcb96fdd8b9a85bc0c4f0f04d5dad9d04839c..4a4b418f6527413161db82670bec857a043360e2\n--- a/scripts/shiki_contract_approval.py\n+++ b/scripts/shiki_contract_approval.py\n@@ -142,7 +142,7 @@\n     changed_files_status: Any,\n     registration: Any,\n ) -> ContractApprovalResult:\n-    \"\"\"Whether this PR carries an SADR-0015 Contract Approval.\n+    \"\"\"Whether this PR carries a SADR-0015 Contract Approval.\n \n     The four conditions are evaluated IN ORDER, each failure recorded as a\n     distinct reason string:\ndiff --git a/scripts/shiki_guardian.py b/scripts/shiki_guardian.py\nindex a4ecb79bbda55fd5c8b06adfae2eb99760a03c45..4dd99bbe714e26f421fa19abac7a34ae1b195ecc\n--- a/scripts/shiki_guardian.py\n+++ b/scripts/shiki_guardian.py\n@@ -12,7 +12,7 @@\n # The carry re-derives its proof from git in-process on every judging invocation;\n # it is never read from an artifact, so there is nothing to go stale or be forged.\n # shiki_sync_proof is the ONLY guardian-family module that runs git, and every one\n-# of its invocations is read-only and local (ADR: prove-from-git base sync).\n+# of its invocations is read-only and local (SADR: prove-from-git base sync).\n from shiki_sync_proof import verify_pure_base_sync\n \n GUARDIAN_POLICY_PATH = \".shiki/guardian-policy.json\"\ndiff --git a/scripts/shiki_guardian_review.py b/scripts/shiki_guardian_review.py\nindex dd0f104d457899e0ba5ff1e162011c1e90b6f92b..e10ef58c046324e7ea3a3392feb3048ca77414ce\n--- a/scripts/shiki_guardian_review.py\n+++ b/scripts/shiki_guardian_review.py\n@@ -159,7 +159,7 @@\n     docs_only = _is_docs_only(changed_files)\n     if docs_only and matched == 0:\n         focus.append(\n-            \"Docs/ADR-only change: confirm the change is genuinely documentation/decision-only, introduces no behavior or contract drift, and stays consistent with the constitution and existing ADRs.\"\n+            \"Docs/decision-record-only change: confirm the change is genuinely documentation/decision-only, introduces no behavior or contract drift, and stays consistent with the constitution and existing SADRs and target ADRs.\"\n         )\n \n     # Conservative augmentation: unknown (no category and not docs-only), mixed\ndiff --git a/scripts/shiki_installer.py b/scripts/shiki_installer.py\nindex be064ab7a29ff6836f7f7a1d70500a43e0d11d7b..9fd8eefd137ce7003dad5c0dcb2e48e2cd47ab32\n--- a/scripts/shiki_installer.py\n+++ b/scripts/shiki_installer.py\n@@ -118,6 +118,7 @@\n # what lets a target carry it without a governance-schema change.\n INSTALL_STAMP_PATH = \".shiki/install-stamp.json\"\n INSTALL_STAMP_VERSION = 1\n+LEGACY_SHIKI_SADR_MAX = 18\n \n # Under --force the shipped surface splits three ways, so nothing is lost and\n # nothing is silently stale.\n@@ -237,9 +238,98 @@\n         )\n \n \n+def legacy_shiki_adr_paths() -> tuple[str, ...]:\n+    \"\"\"Former numeric paths for Shiki SADR-0001 through SADR-0018.\n+\n+    The authoritative slugs come from the current SADR filenames. SADR-0019 is\n+    the namespace decision itself and never shipped under a numeric legacy path.\n+    \"\"\"\n+    paths: list[str] = []\n+    for sadr in sorted((ROOT / \"docs\" / \"adr\").glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\")):\n+        number = sadr.name.split(\"-\", 2)[1]\n+        if int(number) <= LEGACY_SHIKI_SADR_MAX:\n+            paths.append(f\"docs/adr/{sadr.name.removeprefix('SADR-')}\")\n+    return tuple(paths)\n+\n+\n+def preflight_legacy_sadr_cleanup(target: Path, *, force: bool) -> tuple[str, ...]:\n+    \"\"\"Authorize exact legacy Shiki ADR removals before the first write.\n+\n+    A non-force install never removes existing files. A forced install needs no\n+    stamp when no legacy Shiki path is present. When legacy paths do exist, each\n+    must be named in a readable install stamp and its current bytes must match\n+    the recorded digest.\n+    \"\"\"\n+    if not force:\n+        return ()\n+    present = tuple(\n+        relative\n+        for relative in legacy_shiki_adr_paths()\n+        if (target / relative).exists() or (target / relative).is_symlink()\n+    )\n+    if not present:\n+        return ()\n+\n+    stamp_path = target / INSTALL_STAMP_PATH\n+    try:\n+        raw = stamp_path.read_text(encoding=\"utf-8\")\n+        stamp = json.loads(raw)\n+    except FileNotFoundError:\n+        detail = \"install stamp is absent\"\n+        blockers = [f\"{relative}: {detail}\" for relative in present]\n+    except (OSError, UnicodeDecodeError) as error:\n+        detail = f\"install stamp is unreadable ({error})\"\n+        blockers = [f\"{relative}: {detail}\" for relative in present]\n+    except json.JSONDecodeError as error:\n+        detail = f\"install stamp is malformed ({error})\"\n+        blockers = [f\"{relative}: {detail}\" for relative in present]\n+    else:\n+        if not isinstance(stamp, dict):\n+            blockers = [\n+                f\"{relative}: install stamp is malformed (expected an object)\"\n+                for relative in present\n+            ]\n+        else:\n+            digests = stamp.get(\"digests\")\n+            blockers = []\n+            if not isinstance(digests, dict):\n+                blockers.extend(\n+                    f\"{relative}: install stamp has no digest for this path\"\n+                    for relative in present\n+                )\n+            else:\n+                for relative in present:\n+                    expected = digests.get(relative)\n+                    if not isinstance(expected, str) or not expected:\n+                        blockers.append(\n+                            f\"{relative}: install stamp has no digest for this path\"\n+                        )\n+                        continue\n+                    try:\n+                        actual = _sha256_file(target / relative)\n+                    except OSError as error:\n+                        blockers.append(\n+                            f\"{relative}: legacy path is unreadable ({error})\"\n+                        )\n+                        continue\n+                    if actual != expected:\n+                        blockers.append(\n+                            f\"{relative}: digest mismatch (stamp {expected}, current {actual})\"\n+                        )\n+\n+    if blockers:\n+        joined = \"\\n\".join(f\"- {blocker}\" for blocker in blockers)\n+        raise ShikiError(\n+            \"legacy Shiki ADR cleanup is not authorized:\\n\"\n+            f\"{joined}\\nNo files were written.\"\n+        )\n+    return present\n+\n+\n def install_template(target: Path, *, force: bool, validate: bool) -> list[NewFileNote]:\n     # Preconditions first: never rewrite the tree of a target that cannot be\n     # upgraded cleanly (see refuse_pending_migrations).\n+    legacy_cleanup = preflight_legacy_sadr_cleanup(target, force=force)\n     refuse_pending_migrations(target)\n \n     # Resolve the target's required CODEOWNERS owner once from its\n@@ -250,6 +340,10 @@\n     # verbatim. See ``codeowners_required_owner``.\n     codeowners_owner = codeowners_required_owner(target)\n \n+    for relative in legacy_cleanup:\n+        (target / relative).unlink()\n+        info(f\"removed stamped legacy Shiki ADR: {target / relative}\")\n+\n     notes: list[NewFileNote] = []\n     for relative in TEMPLATE_PATHS:\n         source = ROOT / relative\n@@ -647,6 +741,22 @@\n     digests = target_stamp_digests(target)\n     existing = read_install_stamp(target)\n     if existing is not None:\n+        # A non-force install deliberately leaves legacy Shiki ADRs in place.\n+        # Preserve only still-matching ownership proof so a later forced\n+        # upgrade can authorize cleanup; never carry a missing or stale digest.\n+        existing_digests = existing.get(\"digests\")\n+        if isinstance(existing_digests, dict):\n+            for relative in legacy_shiki_adr_paths():\n+                expected = existing_digests.get(relative)\n+                path = target / relative\n+                if not isinstance(expected, str) or not path.is_file():\n+                    continue\n+                try:\n+                    actual = _sha256_file(path)\n+                except OSError:\n+                    continue\n+                if actual == expected:\n+                    digests[relative] = actual\n         if _incoming_is_older(commit, existing.get(\"platform_commit\")):\n             warn(\n                 \"kept existing install stamp: incoming install \"\ndiff --git a/scripts/shiki_loop.py b/scripts/shiki_loop.py\nindex 3611641da8aeb5038b1da90e3114b09a473ec6d8..84caa491b9cad0edc4176f209c13e9221e975681\n--- a/scripts/shiki_loop.py\n+++ b/scripts/shiki_loop.py\n@@ -424,7 +424,7 @@\n         # unresolvable CCA verdict has already produced its terminal stop and is never\n         # laundered into a head-moving sync (SADR-0010). It also never fires while the\n         # policy (Guardian) gate is red \u2014 for ANY reason, not only when it is the sole\n-        # red check \u2014 so a head-bound Guardian approval, or an SADR-0015\n+        # red check \u2014 so a head-bound Guardian approval, or a SADR-0015\n         # contract-approval carry that base movement defeated (the policy gate compares\n         # base and head task governance directly), is never silently restored by\n         # adopting base wholesale. It is bounded by a durable per-task counter so the\ndiff --git a/scripts/shiki_tasks.py b/scripts/shiki_tasks.py\nindex aec3809f24b4a0c8c96fc50971cff9ae9ad30381..d531052e9d9717058d4631483cefab44c26a65c1\n--- a/scripts/shiki_tasks.py\n+++ b/scripts/shiki_tasks.py\n@@ -821,7 +821,7 @@\n         \"prompt\": prompt,\n         \"entry_skill\": \"grill-with-docs\",\n         \"required_next_steps\": [\n-            \"Run grill-with-docs until terminology, boundaries, risks, and ADR-worthy decisions are settled.\",\n+            \"Run grill-with-docs until terminology, boundaries, risks, and decision-record-worthy choices are settled.\",\n             \"Obtain the operator's explicit approval of the PRD/requirements (Spec Freeze).\",\n             \"Write a machine-readable plan JSON with grill_with_docs.status=complete and spec_freeze.status=frozen.\",\n             \"Run shiki plan ingest --plan-file PLAN.json.\",\ndiff --git a/scripts/test_shiki_goal_loop.sh b/scripts/test_shiki_goal_loop.sh\nindex 753c7b516e10f0be3bc92a4bd0eb1fa699776ae6..8f3820a0a079b3044424cfe1a6c6b7fe68896e67\n--- a/scripts/test_shiki_goal_loop.sh\n+++ b/scripts/test_shiki_goal_loop.sh\n@@ -231,8 +231,8 @@\n \n # High/critical risk merges autonomously when all required checks are green:\n # the \"MergeGate policy check\" required check IS the Guardian gate (it enforces\n-# guardian-policy.json \u2014 human approval OR external AI guardian review, ADR\n-# 0010), so green checks mean a recorded authority approved. The fake gh returns\n+# guardian-policy.json \u2014 human approval OR external AI guardian review,\n+# SADR-0010), so green checks mean a recorded authority approved. The fake gh returns\n # green checks, simulating that the Guardian gate passed.\n python3 \"$ROOT/scripts/shiki.py\" goal create --target \"$TARGET\" --title \"Guardian gate\" --outcome \"High risk merges via policy gate\" >/tmp/shiki-goal-loop-guardian-goal.json\n GGOAL=\"$(json_get /tmp/shiki-goal-loop-guardian-goal.json goal_id)\"\ndiff --git a/tests/test_adr_references.py b/tests/test_adr_references.py\nindex 51706329c797d5dd9b47e082a333fa83b261d482..db6e693e35f67103935be34f1d9bb4ff7ee60395\n--- a/tests/test_adr_references.py\n+++ b/tests/test_adr_references.py\n@@ -1,7 +1,7 @@\n-\"\"\"Every ``ADR ####`` citation must resolve to exactly one decision record.\n+\"\"\"Every target ADR and Shiki SADR citation resolves in its own namespace.\n \n Background. ``scripts/mergegate_check.py`` justifies ``is_bookkeeping_closeout``\n-with \"ADR 0017\" nine times and ``scripts/guardian_approval_signal.py`` cites\n+with \"SADR-0017\" nine times and ``scripts/guardian_approval_signal.py`` cites\n 0016/0017 seven times, yet for a stretch the numbering in ``docs/adr/`` stopped\n at 0015: the authoring commits lived on a coordinator branch and no task ever\n declared ``docs/adr/...`` in its locks, so nothing carried the records to the\n@@ -11,11 +11,12 @@\n and the directory drifted with no signal.\n \n This is the same unbound-pair shape behind other silent failures: a reference\n-and its target with nothing asserting they agree. This test binds them. It scans\n-every ``scripts/*.py`` (plus ``docs/**/*.md`` and the root ``*.md`` governance\n-files, which are cheap) for the ``ADR ####`` pattern and asserts each cited\n-number resolves to exactly one ``docs/adr/####-*.md``. The converse is\n-deliberately NOT required: an ADR may exist without being cited.\n+and its target with nothing asserting they agree. This test binds them. Target\n+``ADR NNNN`` citations resolve only to ``docs/adr/NNNN-*.md``; Shiki\n+``SADR-NNNN`` citations resolve only to ``docs/adr/SADR-NNNN-*.md``. The scan\n+covers current governance docs, scripts, workflows, prompts, packaging metadata,\n+and repo-local Codex guidance while deliberately excluding historical\n+``.shiki`` evidence. The converse is not required: a record may be uncited.\n \n The suite is dependency free: standard-library ``unittest`` only.\n \"\"\"\n@@ -31,17 +32,18 @@\n \n REPO_ROOT = Path(__file__).resolve().parents[1]\n \n-# \"ADR 0016\" -- a four-digit citation. The trailing \\b keeps a five-digit token\n-# from being read as a truncated four-digit number.\n-_CITATION = re.compile(r\"\\bADR (\\d{4})\\b\")\n-# \"0016-locks-bound-product-paths.md\" -- the leading four digits name the record.\n-_ADR_FILENAME = re.compile(r\"^(\\d{4})-.*\\.md$\")\n+# ``ADR 0016`` / ``SADR-0016`` -- a namespace plus four-digit citation. The\n+# trailing ``\\b`` keeps a five-digit token from being read as a truncated one.\n+_CITATION = re.compile(r\"\\b(S?ADR)[ -](\\d{4})\\b\")\n+# Target ADRs retain the numeric filename; Shiki platform decisions use SADR.\n+_DECISION_FILENAME = re.compile(r\"^(?:(SADR)-)?(\\d{4})-.*\\.md$\")\n \n \n class Violation(NamedTuple):\n     \"\"\"A citation that does not resolve to exactly one decision record.\"\"\"\n \n     citing_file: str  # repo-relative path of the file that cites the ADR\n+    namespace: str  # \"ADR\" for target decisions; \"SADR\" for Shiki decisions\n     number: str  # the cited four-digit number, e.g. \"0016\"\n     line: int  # 1-based line of the citation\n     reason: str  # \"missing\" (zero matches) or \"ambiguous\" (two or more)\n@@ -49,44 +51,59 @@\n \n     @property\n     def expected_path(self) -> str:\n-        return f\"docs/adr/{self.number}-*.md\"\n+        prefix = \"SADR-\" if self.namespace == \"SADR\" else \"\"\n+        return f\"docs/adr/{prefix}{self.number}-*.md\"\n \n     def describe(self) -> str:\n         \"\"\"Name the citing file, the cited number, and the missing path.\"\"\"\n         if self.reason == \"missing\":\n             return (\n-                f\"{self.citing_file}:{self.line} cites ADR {self.number} \"\n+                f\"{self.citing_file}:{self.line} cites {self.namespace} {self.number} \"\n                 f\"but no document matches {self.expected_path}\"\n             )\n         joined = \", \".join(self.matches)\n         return (\n-            f\"{self.citing_file}:{self.line} cites ADR {self.number} which \"\n+            f\"{self.citing_file}:{self.line} cites {self.namespace} {self.number} which \"\n             f\"resolves to {len(self.matches)} documents (expected exactly one \"\n             f\"at {self.expected_path}): {joined}\"\n         )\n \n \n-def _adr_index(root: Path) -> dict[str, list[Path]]:\n-    \"\"\"Map every four-digit ADR number to the record files that claim it.\"\"\"\n-    index: dict[str, list[Path]] = {}\n+def _adr_index(root: Path) -> dict[tuple[str, str], list[Path]]:\n+    \"\"\"Map each decision namespace and number to the files that claim it.\"\"\"\n+    index: dict[tuple[str, str], list[Path]] = {}\n     adr_dir = root / \"docs\" / \"adr\"\n     if adr_dir.is_dir():\n         for path in sorted(adr_dir.glob(\"*.md\")):\n-            match = _ADR_FILENAME.match(path.name)\n+            match = _DECISION_FILENAME.match(path.name)\n             if match:\n-                index.setdefault(match.group(1), []).append(path)\n+                namespace = \"SADR\" if match.group(1) else \"ADR\"\n+                index.setdefault((namespace, match.group(2)), []).append(path)\n     return index\n \n \n def _citing_files(root: Path, include_docs: bool = True) -> list[Path]:\n-    \"\"\"Files scanned for citations: scripts/*.py, and cheaply the markdown.\"\"\"\n+    \"\"\"Current, shipped text surfaces scanned for decision citations.\"\"\"\n     files = sorted((root / \"scripts\").glob(\"*.py\"))\n+    files += sorted((root / \"scripts\").glob(\"*.sh\"))\n     if include_docs:\n         docs = root / \"docs\"\n         if docs.is_dir():\n             files += sorted(docs.rglob(\"*.md\"))\n         files += sorted(root.glob(\"*.md\"))\n-    return files\n+        pyproject = root / \"pyproject.toml\"\n+        if pyproject.is_file():\n+            files.append(pyproject)\n+        for relative in (\".github\", \".codex\"):\n+            surface = root / relative\n+            if surface.is_dir():\n+                files += sorted(\n+                    path\n+                    for path in surface.rglob(\"*\")\n+                    if path.is_file()\n+                    and path.suffix in {\".md\", \".py\", \".sh\", \".toml\", \".yaml\", \".yml\"}\n+                )\n+    return sorted(set(files))\n \n \n def find_adr_reference_violations(\n@@ -94,10 +111,9 @@\n ) -> list[Violation]:\n     \"\"\"Return every ``ADR ####`` citation under ``root`` that fails to resolve.\n \n-    A citation resolves when exactly one ``docs/adr/####-*.md`` matches its\n-    number. Zero matches is a \"missing\" violation; two or more is \"ambiguous\".\n-    Records that are never cited produce nothing -- the converse is not\n-    required.\n+    A citation resolves when exactly one record in the same namespace matches\n+    its number. Zero matches is a \"missing\" violation; two or more is\n+    \"ambiguous\". Records that are never cited produce nothing.\n     \"\"\"\n     root = Path(root)\n     index = _adr_index(root)\n@@ -110,13 +126,15 @@\n         rel = path.relative_to(root).as_posix()\n         for lineno, line in enumerate(text.splitlines(), start=1):\n             for match in _CITATION.finditer(line):\n-                number = match.group(1)\n-                found = index.get(number, [])\n+                namespace, number = match.groups()\n+                found = index.get((namespace, number), [])\n                 if len(found) == 1:\n                     continue\n                 reason = \"missing\" if not found else \"ambiguous\"\n                 rels = [p.relative_to(root).as_posix() for p in found]\n-                violations.append(Violation(rel, number, lineno, reason, rels))\n+                violations.append(\n+                    Violation(rel, namespace, number, lineno, reason, rels)\n+                )\n     return violations\n \n \n@@ -129,8 +147,8 @@\n         self.assertEqual(\n             violations,\n             [],\n-            \"Every 'ADR ####' citation must resolve to exactly one \"\n-            f\"docs/adr/####-*.md:\\n{detail}\",\n+            \"Every ADR/SADR citation must resolve exactly once in its own \"\n+            f\"docs/adr namespace:\\n{detail}\",\n         )\n \n     def test_scan_actually_reads_scripts(self) -> None:\n@@ -146,8 +164,21 @@\n         # The two records this Goal carried onto the default branch, and which\n         # the scripts cite most, must be present.\n         index = _adr_index(REPO_ROOT)\n-        self.assertIn(\"0016\", index)\n-        self.assertIn(\"0017\", index)\n+        self.assertIn((\"SADR\", \"0016\"), index)\n+        self.assertIn((\"SADR\", \"0017\"), index)\n+\n+    def test_platform_tree_has_only_sadr_0001_through_0019(self) -> None:\n+        adr_dir = REPO_ROOT / \"docs\" / \"adr\"\n+        sadrs = sorted(adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\"))\n+        self.assertEqual(\n+            {path.name.split(\"-\", 2)[1] for path in sadrs},\n+            {f\"{number:04d}\" for number in range(1, 20)},\n+        )\n+        self.assertEqual(\n+            list(adr_dir.glob(\"[0-9][0-9][0-9][0-9]-*.md\")),\n+            [],\n+            \"the Shiki platform tree must not retain legacy numeric SADR paths\",\n+        )\n \n \n class FixtureAdrReferences(unittest.TestCase):\n@@ -180,6 +211,26 @@\n         self.assertIn(\"9991\", message)  # cited number\n         self.assertIn(\"docs/adr/9991-*.md\", message)  # missing path\n \n+    def test_target_adr_does_not_satisfy_missing_platform_sadr(self) -> None:\n+        root = self._make_repo(\n+            adr_names=[\"0042-target-decision.md\"],\n+            script_body='\"\"\"Target ADR 0042 exists, but platform SADR-0042 does not.\"\"\"\\n',\n+        )\n+        violations = find_adr_reference_violations(root, include_docs=False)\n+        self.assertEqual(len(violations), 1)\n+        self.assertEqual(violations[0].namespace, \"SADR\")\n+        self.assertEqual(violations[0].number, \"0042\")\n+        self.assertEqual(violations[0].expected_path, \"docs/adr/SADR-0042-*.md\")\n+\n+    def test_target_adr_and_platform_sadr_may_reuse_a_number(self) -> None:\n+        root = self._make_repo(\n+            adr_names=[\"0042-target-decision.md\", \"SADR-0042-platform-decision.md\"],\n+            script_body='\"\"\"Target ADR 0042 and platform SADR-0042 are distinct.\"\"\"\\n',\n+        )\n+        self.assertEqual(\n+            find_adr_reference_violations(root, include_docs=False), []\n+        )\n+\n     def test_uncited_adr_is_allowed(self) -> None:\n         # An ADR exists but nothing cites it -> not a violation. The converse\n         # (every ADR must be cited) is explicitly NOT required.\ndiff --git a/tests/test_guardian_review.py b/tests/test_guardian_review.py\nindex 88829e7810212635e2fc98aa5918d6f26294677d..00a85a237acd5bf2d6cb5b294128198c422402ca\n--- a/tests/test_guardian_review.py\n+++ b/tests/test_guardian_review.py\n@@ -162,7 +162,7 @@\n \n     def test_docs_only_is_light_and_not_conservative(self):\n         focus = gr.classify_review_focus([\"docs/adr/0099-x.md\", \"README.md\"], \"\", \"low\")\n-        self.assertTrue(any(\"Docs/ADR-only\" in f for f in focus))\n+        self.assertTrue(any(\"Docs/decision-record-only\" in f for f in focus))\n         self.assertFalse(any(\"conservative default\" in f.lower() for f in focus))\n \n     def test_unknown_category_is_conservative(self):\ndiff --git a/tests/test_policy_job_merged_prs.py b/tests/test_policy_job_merged_prs.py\nindex d45512e74681e5e649279236c7d4e79d5c3da471..b97989f8bd74d091de1a6b5275a20d8dc68c4512\n--- a/tests/test_policy_job_merged_prs.py\n+++ b/tests/test_policy_job_merged_prs.py\n@@ -3,8 +3,8 @@\n \n ``.github/workflows/shiki-cca-completion.yml`` has two consumers of the merged-PR\n proof: the CCA *signal* (``guardian_approval_signal.py`` in \"Prepare CCA context\")\n-and the authoritative gate (``mergegate_check.py`` in \"Enforce MergeGate\"). ADR\n-0017's bookkeeping-closeout exemption (condition 6) needs the merged implementation\n+and the authoritative gate (``mergegate_check.py`` in \"Enforce MergeGate\").\n+SADR-0017's bookkeeping-closeout exemption (condition 6) needs the merged implementation\n PR proven, and a closeout carries NO ``post_merge_reconcile`` marker. The signal\n already resolves ``MERGED_PRS`` unconditionally, but \"Enforce MergeGate\" used to\n resolve it only inside ``grep -q '<!-- shiki:post_merge_reconcile -->'``. So a\ndiff --git a/tests/test_sadr_install_migration.py b/tests/test_sadr_install_migration.py\nnew file mode 100644\nindex 0000000000000000000000000000000000000000..a6f4c57e4dcaef73ef600b0d4804744138ab9071\n--- /dev/null\n+++ b/tests/test_sadr_install_migration.py\n@@ -0,0 +1,229 @@\n+\"\"\"SADR installation and collision-safe legacy upgrade behavior.\"\"\"\n+\n+from __future__ import annotations\n+\n+import contextlib\n+import hashlib\n+import io\n+import json\n+import shutil\n+import tempfile\n+import unittest\n+from pathlib import Path\n+\n+import shiki_test_support  # noqa: F401  (path bootstrap)\n+\n+import shiki_installer\n+from tests.test_adr_references import find_adr_reference_violations\n+\n+\n+PRODUCT_NUMBERS = tuple(range(13, 19))\n+\n+\n+def _quiet_install(target: Path, *, force: bool, validate: bool) -> None:\n+    with contextlib.redirect_stdout(io.StringIO()):\n+        shiki_installer.install_template(target, force=force, validate=validate)\n+\n+\n+def _sha256(path: Path) -> str:\n+    return hashlib.sha256(path.read_bytes()).hexdigest()\n+\n+\n+def _legacy_sadr_paths() -> tuple[str, ...]:\n+    return tuple(\n+        f\"docs/adr/{path.name.removeprefix('SADR-')}\"\n+        for path in sorted((shiki_installer.ROOT / \"docs\" / \"adr\").glob(\"SADR-*.md\"))\n+        if path.name != \"SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\"\n+    )\n+\n+\n+def _create_legacy_upgrade_fixture(\n+    target: Path,\n+) -> tuple[dict[str, bytes], tuple[str, ...]]:\n+    adr_dir = target / \"docs\" / \"adr\"\n+    adr_dir.mkdir(parents=True)\n+    product_bytes = {\n+        f\"{number:04d}-cubeshare-product-decision.md\": (\n+            f\"# CubeShare ADR {number:04d}\\n\\nDo not rewrite {number}.\\n\"\n+        ).encode()\n+        for number in PRODUCT_NUMBERS\n+    }\n+    for name, content in product_bytes.items():\n+        (adr_dir / name).write_bytes(content)\n+\n+    legacy_paths = _legacy_sadr_paths()\n+    for relative in legacy_paths:\n+        (target / relative).write_bytes(f\"legacy Shiki bytes for {relative}\\n\".encode())\n+\n+    stamp_path = target / shiki_installer.INSTALL_STAMP_PATH\n+    stamp_path.parent.mkdir(parents=True, exist_ok=True)\n+    stamp = {\n+        \"version\": shiki_installer.INSTALL_STAMP_VERSION,\n+        \"platform_commit\": None,\n+        \"installed_at\": \"2000-01-01T00:00:00+00:00\",\n+        \"digests\": {\n+            relative: _sha256(target / relative) for relative in legacy_paths\n+        },\n+    }\n+    stamp_path.write_text(\n+        json.dumps(stamp, indent=2, sort_keys=True) + \"\\n\", encoding=\"utf-8\"\n+    )\n+    return product_bytes, legacy_paths\n+\n+\n+def _tree_snapshot(root: Path) -> dict[str, tuple[str, bytes | str]]:\n+    \"\"\"Recursive byte/shape snapshot used to prove preflight is write-free.\"\"\"\n+    snapshot: dict[str, tuple[str, bytes | str]] = {}\n+    for path in sorted(root.rglob(\"*\")):\n+        relative = path.relative_to(root).as_posix()\n+        if path.is_symlink():\n+            snapshot[relative] = (\"symlink\", path.readlink().as_posix())\n+        elif path.is_dir():\n+            snapshot[relative] = (\"directory\", \"\")\n+        else:\n+            snapshot[relative] = (\"file\", path.read_bytes())\n+    return snapshot\n+\n+\n+class FreshSadrInstallTests(unittest.TestCase):\n+    def setUp(self) -> None:\n+        self.tmp = Path(tempfile.mkdtemp(prefix=\"shiki-sadr-fresh-\"))\n+        self.addCleanup(lambda: shutil.rmtree(self.tmp, ignore_errors=True))\n+        self.target = self.tmp / \"target\"\n+        self.adr_dir = self.target / \"docs\" / \"adr\"\n+        self.adr_dir.mkdir(parents=True)\n+        self.product_bytes = {\n+            f\"{number:04d}-product-decision.md\": (\n+                f\"# Product ADR {number:04d}\\n\\nTarget-owned bytes {number}.\\n\"\n+            ).encode()\n+            for number in PRODUCT_NUMBERS\n+        }\n+        for name, content in self.product_bytes.items():\n+            (self.adr_dir / name).write_bytes(content)\n+\n+    def test_fresh_install_preserves_product_adrs_and_installs_all_sadrs(self) -> None:\n+        _quiet_install(self.target, force=False, validate=True)\n+\n+        for name, expected in self.product_bytes.items():\n+            self.assertEqual((self.adr_dir / name).read_bytes(), expected)\n+\n+        sadr_numbers = {\n+            path.name.split(\"-\", 2)[1]\n+            for path in self.adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\")\n+        }\n+        self.assertEqual(sadr_numbers, {f\"{number:04d}\" for number in range(1, 20)})\n+\n+        numeric_records = {\n+            path.name\n+            for path in self.adr_dir.glob(\"[0-9][0-9][0-9][0-9]-*.md\")\n+        }\n+        self.assertEqual(numeric_records, set(self.product_bytes))\n+        self.assertEqual(find_adr_reference_violations(self.target), [])\n+\n+\n+class LegacySadrUpgradeTests(unittest.TestCase):\n+    def setUp(self) -> None:\n+        self.tmp = Path(tempfile.mkdtemp(prefix=\"shiki-sadr-upgrade-\"))\n+        self.addCleanup(lambda: shutil.rmtree(self.tmp, ignore_errors=True))\n+        self.target = self.tmp / \"target\"\n+        self.adr_dir = self.target / \"docs\" / \"adr\"\n+        self.product_bytes, self.legacy_paths = _create_legacy_upgrade_fixture(\n+            self.target\n+        )\n+\n+    def test_force_upgrade_deletes_only_stamped_legacy_sadrs(self) -> None:\n+        _quiet_install(self.target, force=True, validate=True)\n+\n+        for relative in self.legacy_paths:\n+            self.assertFalse((self.target / relative).exists(), relative)\n+        for name, expected in self.product_bytes.items():\n+            self.assertEqual((self.adr_dir / name).read_bytes(), expected)\n+\n+        sadrs = list(self.adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\"))\n+        self.assertEqual(len(sadrs), 19)\n+        refreshed = json.loads(\n+            (self.target / shiki_installer.INSTALL_STAMP_PATH).read_text(encoding=\"utf-8\")\n+        )\n+        for relative in self.legacy_paths:\n+            self.assertNotIn(relative, refreshed[\"digests\"])\n+        for path in sadrs:\n+            relative = path.relative_to(self.target).as_posix()\n+            self.assertEqual(refreshed[\"digests\"][relative], _sha256(path))\n+\n+    def test_non_force_install_never_deletes_legacy_files(self) -> None:\n+        before = {\n+            relative: (self.target / relative).read_bytes()\n+            for relative in self.legacy_paths\n+        }\n+        _quiet_install(self.target, force=False, validate=False)\n+        for relative, expected in before.items():\n+            self.assertEqual((self.target / relative).read_bytes(), expected)\n+        refreshed = json.loads(\n+            (self.target / shiki_installer.INSTALL_STAMP_PATH).read_text(encoding=\"utf-8\")\n+        )\n+        for relative in self.legacy_paths:\n+            self.assertEqual(refreshed[\"digests\"][relative], _sha256(self.target / relative))\n+\n+\n+class LegacyCleanupPreflightTests(unittest.TestCase):\n+    def setUp(self) -> None:\n+        self.tmp = Path(tempfile.mkdtemp(prefix=\"shiki-sadr-preflight-\"))\n+        self.addCleanup(lambda: shutil.rmtree(self.tmp, ignore_errors=True))\n+\n+    def test_all_ownership_blockers_abort_before_any_write(self) -> None:\n+        cases = (\n+            (\"absent\", \"install stamp is absent\"),\n+            (\"unreadable\", \"install stamp is unreadable\"),\n+            (\"malformed\", \"install stamp is malformed\"),\n+            (\"missing-digest\", \"install stamp has no digest\"),\n+            (\"digest-mismatch\", \"digest mismatch\"),\n+        )\n+        for name, expected_reason in cases:\n+            with self.subTest(name=name):\n+                target = self.tmp / name\n+                _, legacy_paths = _create_legacy_upgrade_fixture(target)\n+                stamp_path = target / shiki_installer.INSTALL_STAMP_PATH\n+\n+                if name == \"absent\":\n+                    stamp_path.unlink()\n+                elif name == \"unreadable\":\n+                    stamp_path.unlink()\n+                    stamp_path.mkdir()\n+                elif name == \"malformed\":\n+                    stamp_path.write_text(\"{not-json\\n\", encoding=\"utf-8\")\n+                else:\n+                    stamp = json.loads(stamp_path.read_text(encoding=\"utf-8\"))\n+                    if name == \"missing-digest\":\n+                        stamp[\"digests\"] = {}\n+                    else:\n+                        stamp[\"digests\"] = {\n+                            relative: \"deadbeef\" for relative in legacy_paths\n+                        }\n+                    stamp_path.write_text(\n+                        json.dumps(stamp, indent=2, sort_keys=True) + \"\\n\",\n+                        encoding=\"utf-8\",\n+                    )\n+\n+                before = _tree_snapshot(target)\n+                with self.assertRaises(shiki_installer.ShikiError) as raised:\n+                    _quiet_install(target, force=True, validate=False)\n+                after = _tree_snapshot(target)\n+\n+                self.assertEqual(before, after, f\"{name} preflight changed the tree\")\n+                message = str(raised.exception)\n+                self.assertIn(expected_reason, message)\n+                self.assertIn(\"No files were written\", message)\n+                for relative in legacy_paths:\n+                    self.assertIn(relative, message)\n+\n+    def test_absent_stamp_is_allowed_when_no_legacy_path_exists(self) -> None:\n+        target = self.tmp / \"no-legacy\"\n+        target.mkdir()\n+        _quiet_install(target, force=True, validate=False)\n+        self.assertTrue(\n+            (target / \"docs\" / \"adr\" / \"SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\").is_file()\n+        )\n+\n+\n+if __name__ == \"__main__\":\n+    unittest.main()\n\ndiff --git a/.codex/skills/shiki/SKILL.md b/.codex/skills/shiki/SKILL.md\nindex c8e2e85d9d75d49cc4f2d6286ee22659c4e53e75..93cded623b2c15d9f65c76491a1c2d813ac9e6c0\n--- a/.codex/skills/shiki/SKILL.md\n+++ b/.codex/skills/shiki/SKILL.md\n@@ -18,6 +18,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n Codex CLI does not register this skill as a custom `/shiki` slash command. When\n the user types `/shiki` in Codex CLI and gets \"Unrecognized command\", explain\n that this is expected: invoke Shiki in Codex with natural language such as\n@@ -69,7 +72,7 @@\n \n ## Responsibilities\n \n-- Claude Code implements and repairs by default (`shiki runner claude`, ADR 0008); Codex implements and repairs tasks explicitly assigned to `codex`.\n+- Claude Code implements and repairs by default (`shiki runner claude`, SADR-0008); Codex implements and repairs tasks explicitly assigned to `codex`.\n - Claude Code Action can act as GitHub-side CCA or reviewer.\n - CCA judges completion.\n - MergeGate authorizes state transitions and merge readiness.\ndiff --git a/.github/prompts/cca-completion-check.md b/.github/prompts/cca-completion-check.md\nindex ed203ad5f302ecc81372d8ab27d2b1232ca97497..fd89950528333ef6242e6d5443904f5392ab7e56\n--- a/.github/prompts/cca-completion-check.md\n+++ b/.github/prompts/cca-completion-check.md\n@@ -19,6 +19,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n ## Required Reading\n \n Read only the files needed to judge the PR contract. Prefer this order:\ndiff --git a/.github/prompts/codex-task-handoff.md b/.github/prompts/codex-task-handoff.md\nindex 695e211d76dea1c60e8d5bb360e78487808286a4..59441d576b01a39569722f226c854e5a740e9eb5\n--- a/.github/prompts/codex-task-handoff.md\n+++ b/.github/prompts/codex-task-handoff.md\n@@ -2,7 +2,7 @@\n \n You are Codex Front acting as the Implementer for a Shiki task.\n \n-Follow `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant ADRs, and `docs/agents/codex-handoff.md`.\n+Follow `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant Shiki SADRs and target ADRs, and `docs/agents/codex-handoff.md`.\n \n Rules:\n \ndiff --git a/AGENTS.md b/AGENTS.md\nindex 7f041100d49c2a7525f51cb0f5d696a4a6d1c78c..a06bfbabc6fa48a007389e25e12d7ed523936bed\n--- a/AGENTS.md\n+++ b/AGENTS.md\n@@ -22,7 +22,7 @@\n - **Codex Front** is an optional implementer through Codex App, Codex CLI, Codex IDE extension, or Codex Web signed in with ChatGPT OAuth/subscription auth, dispatched with `shiki runner codex` when a task is explicitly assigned to `codex`.\n - **GitHub CCA** is implemented by Claude Code Action by default, using `CLAUDE_CODE_OAUTH_TOKEN`.\n \n-Do not assume `openai/codex-action`, `OPENAI_API_KEY`, or API-key based Codex automation in the default Shiki path. API-key runners are explicit target-repo extensions and require their own ADR.\n+Do not assume `openai/codex-action`, `OPENAI_API_KEY`, or API-key based Codex automation in the default Shiki path. API-key runners are explicit target-repo extensions and require their own target ADR.\n \n Shiki is not:\n \n@@ -44,10 +44,14 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use\n+`SADR-NNNN-*.md`; target product decisions retain `NNNN-*.md` and the `ADR`\n+namespace.\n+\n Use the canonical source-of-truth order above. Runtime-specific reading orders\n are operational guidance only and must not override or reorder it.\n \n-Conversation state is not durable truth. If a decision matters, put it in GitHub, `.shiki/`, `CONTEXT.md`, or an ADR.\n+Conversation state is not durable truth. If a decision matters, put it in GitHub, `.shiki/`, `CONTEXT.md`, a Shiki SADR, or a target ADR.\n \n If GitHub and `.shiki/` disagree, surface the conflict and prefer GitHub operational state until the mirror is repaired.\n \n@@ -75,8 +79,8 @@\n Every non-trivial change follows this loop:\n \n 1. **Goal**: clarify outcome, completion conditions, non-goals, risk level, and success signals.\n-2. **grill-with-docs**: challenge the plan against domain docs, ADRs, code reality, terminology, and edge scenarios.\n-3. **Context & Impact**: find relevant docs, ADRs, code, dependencies, owners, locks, and verification surfaces.\n+2. **grill-with-docs**: challenge the plan against domain docs, Shiki SADRs, target ADRs, code reality, terminology, and edge scenarios.\n+3. **Context & Impact**: find relevant docs, SADRs, target ADRs, code, dependencies, owners, locks, and verification surfaces.\n 4. **PRD and Spec Freeze**: use `to-prd` when context is settled enough to become durable product/engineering intent. The operator's explicit approval of the PRD is Spec Freeze: the plan records a `spec_freeze` block, and no plan runs without it. Steps 1-4 form Requirements Definition \u2014 one continuous operator dialogue. After Spec Freeze, scope changes require an operator-approved Spec Amendment; non-scope-moving interpretations go to the Assumption Log.\n 5. **Issues**: use `to-issues` to create independently grabbable vertical-slice issues. Prefer AFK slices over HITL slices where possible.\n 6. **Triage**: label issues for readiness, risk, runtime, skills, and MergeGate state.\n@@ -167,7 +171,7 @@\n - Testing decisions.\n - Out-of-scope items.\n - Further notes.\n-- Links to relevant domain terms and ADRs.\n+- Links to relevant domain terms, SADRs, and target ADRs.\n \n Every executable task must include:\n \n@@ -193,7 +197,7 @@\n \n Context & Impact must identify:\n \n-- Relevant documents, domain terms, ADRs, and past decisions.\n+- Relevant documents, domain terms, SADRs, target ADRs, and past decisions.\n - Relevant modules, interfaces, seams, callers, owners, and tests.\n - Dependency relationships between tasks.\n - Candidate locks and lock conflicts.\n@@ -210,7 +214,7 @@\n | Skill | Required when |\n | --- | --- |\n | `setup-matt-pocock-skills` | First configuring the repo for skills, or when issue tracker, triage labels, or domain docs are missing. |\n-| `grill-with-docs` | Default for non-trivial Goals before PRD/issues; always required when plans, terminology, boundaries, tradeoffs, or ADR-worthy decisions are ambiguous. |\n+| `grill-with-docs` | Default for non-trivial Goals before PRD/issues; always required when plans, terminology, boundaries, tradeoffs, or decision-record-worthy choices are ambiguous. |\n | `zoom-out` | The agent lacks an architectural map of the relevant code area. |\n | `to-prd` | Settled Goal context must become durable product/engineering intent. |\n | `to-issues` | A Goal, PRD, or plan must become independently grabbable vertical-slice issues. |\n@@ -348,7 +352,7 @@\n - Three or more new files.\n - Any high-risk or critical label.\n \n-When the architecture gate triggers, run `grill-with-docs` or `improve-codebase-architecture` as appropriate and record durable decisions in an ADR when needed.\n+When the architecture gate triggers, run `grill-with-docs` or `improve-codebase-architecture` as appropriate and record durable decisions in a Shiki SADR or target ADR as applicable.\n \n ## Engineering Discipline\n \ndiff --git a/CLAUDE.md b/CLAUDE.md\nindex 98a932e7841a632866fcd53b0bad0e3af3711d3d..98a31399404d6fb044b4fd493f4459ac87f439de\n--- a/CLAUDE.md\n+++ b/CLAUDE.md\n@@ -14,6 +14,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n ## Claude Role\n \n Default to planner, default implementer, reviewer, coordinator, documentation editor, CCA-style completion judge, and final-judgment assistant.\n@@ -57,7 +60,7 @@\n At the start of a Shiki session:\n \n 1. Read `AGENTS.md`.\n-2. Read `CONTEXT.md` and relevant ADRs.\n+2. Read `CONTEXT.md` and relevant Shiki SADRs and target ADRs.\n 3. Read active `.shiki/` Goal/task/ledger state when present.\n 4. Read active distilled rules from `.shiki/memories/` (status `distilled`, `active: true`, not revoked or superseded) for applicable guidance; the same rules are injected deterministically into task handoffs as the `## Distilled Rules` section (\u00a73.5).\n 5. Read the GitHub Issue or PR when the task is GitHub-backed.\n@@ -74,7 +77,7 @@\n - Define the Goal, completion conditions, non-goals, and risk level.\n - Treat `grill-with-docs` as the default planning entry point for non-trivial Goals.\n - Challenge domain terms against `CONTEXT.md`.\n-- Challenge hard-to-reverse tradeoffs against ADRs.\n+- Challenge hard-to-reverse tradeoffs against SADRs and target ADRs.\n - Explore code instead of asking when code can answer the question.\n - Run Context & Impact before implementation planning.\n - Produce a Task DAG, not an unordered checklist.\n@@ -94,7 +97,7 @@\n - For each question, provide your recommended answer.\n - Resolve dependency decisions before downstream design questions.\n - Update `CONTEXT.md` inline when terminology crystallizes.\n-- Offer ADRs only when the decision is hard to reverse, surprising without context, and a real tradeoff.\n+- Offer a SADR or target ADR only when the decision is hard to reverse, surprising without context, and a real tradeoff.\n - Convert unresolved questions into HITL blockers rather than allowing Codex to guess.\n \n ## PRD and Issues Standard\n@@ -103,7 +106,7 @@\n \n - Use the project's domain glossary vocabulary.\n - Avoid volatile file paths unless they are essential.\n-- Include implementation decisions, testing decisions, out-of-scope items, and relevant ADR/domain links.\n+- Include implementation decisions, testing decisions, out-of-scope items, and relevant SADR/target-ADR/domain links.\n - Identify deep-module opportunities and testable interfaces.\n \n When creating issues:\n@@ -121,7 +124,7 @@\n - Branch/worktree target.\n - Scope and non-goals.\n - Dependencies and locks.\n-- Relevant docs, ADRs, modules, and tests.\n+- Relevant docs, SADRs, target ADRs, modules, and tests.\n - Required skill invocations.\n - TDD expectations.\n - Pre-PR `code-review` gate expectations.\n@@ -152,7 +155,7 @@\n \n - Treat yourself as a judge, not implementer.\n - Do not write production code.\n-- Read `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant ADRs, PRD, task issue, PR body, diff, checks, reviews, labels, and `.shiki/` state.\n+- Read `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant SADRs and target ADRs, PRD, task issue, PR body, diff, checks, reviews, labels, and `.shiki/` state.\n - Evaluate every applicable checklist in `docs/agents/checklists.md`.\n - Map each acceptance criterion to durable evidence.\n - Distinguish implementation failure from missing evidence.\ndiff --git a/CONTEXT.md b/CONTEXT.md\nindex e61a9be06a0cce0d026484083a657bde2067e234..237ee8aa3ea8fab334ece49768bc5ed3b5af0974\n--- a/CONTEXT.md\n+++ b/CONTEXT.md\n@@ -25,7 +25,7 @@\n _Avoid_: jumping directly into code\n \n **grill-with-docs**:\n-A planning skill that challenges a plan against domain language, ADRs, code reality, and concrete edge scenarios. It resolves design-tree questions before PRD/issues. It runs inside Requirements Definition.\n+A planning skill that challenges a plan against domain language, Shiki SADRs, target ADRs, code reality, and concrete edge scenarios. It resolves design-tree questions before PRD/issues. It runs inside Requirements Definition.\n _Avoid_: generic brainstorming, silent assumptions\n \n **Requirements Definition**:\ndiff --git a/CONTRIBUTING.md b/CONTRIBUTING.md\nindex c6d9b656e73abb8a55e5f660123e03153dcb3e6d..9e37a8fbf668db5b22461aaae5548b8f1354394c\n--- a/CONTRIBUTING.md\n+++ b/CONTRIBUTING.md\n@@ -17,7 +17,7 @@\n 1. **Goal** \u2014 start from a user-approved Goal with an outcome, completion\n    conditions, non-goals, risk level, and success signals.\n 2. **grill-with-docs** \u2014 for any non-trivial change, challenge the plan against\n-   domain language, ADRs, and code reality before writing a PRD.\n+   domain language, Shiki SADRs, target ADRs, and code reality before writing a PRD.\n 3. **PRD** \u2014 capture settled product/engineering intent (`to-prd`).\n 4. **Spec Freeze** \u2014 the operator explicitly approves the PRD/requirements;\n    the plan records a `spec_freeze` block and no plan runs without it\n@@ -94,7 +94,7 @@\n   entries.\n - Do not hand-edit `.shiki/goals/*.json` or `.shiki/manifest.json` in a task PR.\n - Conversation state is not durable truth. If a decision matters, record it in\n-  GitHub, `.shiki/`, `CONTEXT.md`, or an ADR.\n+  GitHub, `.shiki/`, `CONTEXT.md`, a Shiki SADR, or a target ADR.\n \n ## Architecture Gate\n \n@@ -103,7 +103,7 @@\n or CI policy, package structure, three or more structural files, more than 100\n net new lines in a core module, three or more new files, or any high-risk label.\n When triggered, run `grill-with-docs` or `improve-codebase-architecture` and\n-record durable decisions in an ADR when needed (see `docs/adr/`).\n+record durable decisions in a Shiki SADR or target ADR as applicable (see `docs/adr/`).\n \n ## Safety\n \ndiff --git a/SYSTEM_PROMPT.md b/SYSTEM_PROMPT.md\nindex aaaea45e3a896c3a44ba89eec8bb84325ecf6c62..da009c86a88285269dee0155e58e550c6d744ea9\n--- a/SYSTEM_PROMPT.md\n+++ b/SYSTEM_PROMPT.md\n@@ -30,6 +30,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n Use the canonical source-of-truth order above. Runtime-specific reading orders\n are operational guidance only and must not override or reorder it. The current\n conversation is non-durable operator input.\n@@ -48,14 +51,14 @@\n \n A runtime may only do work that is explicitly assigned by the Goal, task, PR, or MergeGate decision.\n \n-Do not assume `openai/codex-action`, `OPENAI_API_KEY`, or API-key based Codex automation in the default Shiki path. API-key runners are explicit target-repo extensions and require their own ADR.\n+Do not assume `openai/codex-action`, `OPENAI_API_KEY`, or API-key based Codex automation in the default Shiki path. API-key runners are explicit target-repo extensions and require their own target ADR.\n \n ## 4. Non-negotiable Shiki flow\n \n For any non-trivial Goal, use this flow:\n \n 1. **Goal Seek**: define outcome, non-goals, risk level, completion criteria, and evidence requirements.\n-2. **grill-with-docs**: challenge the plan against `CONTEXT.md`, ADRs, code reality, terminology, boundaries, and edge scenarios. Ask one question at a time when the operator is available. If code can answer a question, inspect code instead of asking.\n+2. **grill-with-docs**: challenge the plan against `CONTEXT.md`, Shiki SADRs, target ADRs, code reality, terminology, boundaries, and edge scenarios. Ask one question at a time when the operator is available. If code can answer a question, inspect code instead of asking.\n 3. **Context & Impact**: identify modules, interfaces, seams, callers, dependencies, locks, risk, and verification surfaces.\n 4. **to-prd + Spec Freeze**: turn settled context into a PRD using domain vocabulary and testing decisions. The operator's explicit approval of the PRD is Spec Freeze, recorded as the plan's `spec_freeze` block; plans without it do not run. Steps 1-4 form Requirements Definition.\n 5. **to-issues**: decompose the PRD into independently grabbable vertical-slice issues. Prefer AFK slices; mark HITL slices when judgment is still required.\n@@ -75,7 +78,7 @@\n Before making any material change:\n \n 1. Read `AGENTS.md` and any runtime-specific wrapper such as `CLAUDE.md`.\n-2. Read `CONTEXT.md` and relevant ADRs.\n+2. Read `CONTEXT.md` and relevant SADRs and target ADRs.\n 3. Read the GitHub Issue / PR and `.shiki/` state for the active Goal or task.\n 4. Identify your role: Planner, Implementer, Reviewer, Completion Check Agent, Repairer, or Guardian-assist.\n 5. Identify Goal id, task id, branch, PR, dependencies, locks, risk level, required skills, and required evidence.\n@@ -103,7 +106,7 @@\n \n At minimum, identify:\n \n-- Relevant documents, ADRs, domain terms, and prior decisions.\n+- Relevant documents, SADRs, target ADRs, domain terms, and prior decisions.\n - Relevant modules, interfaces, seams, callers, owners, and tests.\n - Dependency relationships between tasks.\n - Candidate file locks and lock conflicts.\n@@ -152,7 +155,7 @@\n \n CCA must read:\n \n-- `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant ADRs.\n+- `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant SADRs and target ADRs.\n - Parent Goal issue, PRD issue, task issue, comments, and labels.\n - PR body, diff, commits, changed files, reviews, CI checks, and artifacts available to the workflow.\n - `.shiki/` task contract, locks, ledger entries, CCA checklist profile, and prior repair packets.\n@@ -214,7 +217,7 @@\n - Three or more new files.\n - Any high-risk or critical label.\n \n-When the architecture gate triggers, run `grill-with-docs` or `improve-codebase-architecture` as appropriate and record the decision in an ADR if it is hard to reverse or likely to be re-litigated.\n+When the architecture gate triggers, run `grill-with-docs` or `improve-codebase-architecture` as appropriate and record the decision in a Shiki SADR or target ADR as applicable if it is hard to reverse or likely to be re-litigated.\n \n ## 14. Skill Gate\n \n@@ -223,7 +226,7 @@\n Use these skills:\n \n - `setup-matt-pocock-skills`: before first use of the engineering skill set, or when issue tracker, triage labels, or domain docs are missing.\n-- `grill-with-docs`: default for non-trivial Goals before PRD/issues; required for ambiguous plans, terminology, boundaries, ADR-worthy tradeoffs, or design-tree decisions.\n+- `grill-with-docs`: default for non-trivial Goals before PRD/issues; required for ambiguous plans, terminology, boundaries, decision-record-worthy tradeoffs, or design-tree decisions.\n - `zoom-out`: unfamiliar code area or missing architectural map.\n - `to-prd`: convert settled context into a PRD.\n - `to-issues`: break a PRD, plan, or Goal into independently grabbable vertical-slice issues.\ndiff --git a/docs/adr/0001-keep-shiki-as-platform-name.md b/docs/adr/SADR-0001-keep-shiki-as-platform-name.md\nindex 20a9637c3b9d8c97de7ba8db7cfae4d82d9a7dba..8b685439959aada1b221c112f0ac3c51ca8a1695\n--- a/docs/adr/0001-keep-shiki-as-platform-name.md\n+++ b/docs/adr/SADR-0001-keep-shiki-as-platform-name.md\n@@ -1,3 +1,3 @@\n-# Keep Shiki As The Platform Name\n+# SADR-0001: Keep Shiki As The Platform Name\n \n Shiki remains the product name for the GitHub-native agentic engineering control plane. We considered more descriptive names such as MergePilot, MergeGate, and Kiroku, but kept Shiki because it carries the existing goal-seeking framework identity while remaining independent of Hermes, Codex, Claude Code, or any single runtime.\ndiff --git a/docs/adr/0002-use-agents-as-shared-constitution.md b/docs/adr/SADR-0002-use-agents-as-shared-constitution.md\nindex 73de30ecb078120eac540d9bc51855e3cd48d0e3..c5486182bf06fca36ea508496b013c67302fd989\n--- a/docs/adr/0002-use-agents-as-shared-constitution.md\n+++ b/docs/adr/SADR-0002-use-agents-as-shared-constitution.md\n@@ -1,3 +1,3 @@\n-# Use AGENTS As The Shared Agent Constitution\n+# SADR-0002: Use AGENTS As The Shared Agent Constitution\n \n Shiki uses `AGENTS.md` as the runtime-neutral constitution and `CLAUDE.md` as a thin Claude Code wrapper. This keeps Codex, Claude Code, Hermes Runner, and future Agent Runtimes aligned on the same GitHub-first MergeGate rules while still allowing runtime-specific instructions where needed.\ndiff --git a/docs/adr/0003-require-skill-gate-for-mergegate-readiness.md b/docs/adr/SADR-0003-require-skill-gate-for-mergegate-readiness.md\nindex 424be0c9af4c8f0136f1fde68a468c47ff84e764..7fc337987c7df9792f75f181dfcbed75fe5ffff2\n--- a/docs/adr/0003-require-skill-gate-for-mergegate-readiness.md\n+++ b/docs/adr/SADR-0003-require-skill-gate-for-mergegate-readiness.md\n@@ -1,3 +1,3 @@\n-# Require Skill Gate For MergeGate Readiness\n+# SADR-0003: Require Skill Gate For MergeGate Readiness\n \n Shiki requires relevant engineering skills to be invoked before execution when their triggers apply. This makes skills part of MergeGate readiness rather than optional prompt style, because Shiki depends on repeatable planning, issue decomposition, TDD, diagnosis, triage, and architecture review before autonomous or parallel agent work can be trusted.\ndiff --git a/docs/adr/0004-use-github-first-with-shiki-mirror.md b/docs/adr/SADR-0004-use-github-first-with-shiki-mirror.md\nindex d0a3a9174dc646a945a9d325954ffaf00c352040..a79c54ae9ee8eed2a0524192b864da4e22a92d3f\n--- a/docs/adr/0004-use-github-first-with-shiki-mirror.md\n+++ b/docs/adr/SADR-0004-use-github-first-with-shiki-mirror.md\n@@ -1,4 +1,4 @@\n-# ADR 0004: Use GitHub First With A Repository-Local Shiki Mirror\n+# SADR-0004: Use GitHub First With A Repository-Local Shiki Mirror\n \n ## Status\n \ndiff --git a/docs/adr/0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md b/docs/adr/SADR-0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\nindex b339baf49b89d9290e79c3d51b36e7b2bde3bd95..c30a0d8b1fde0224d0ac7820e7870dccf721580d\n--- a/docs/adr/0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\n+++ b/docs/adr/SADR-0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\n@@ -1,4 +1,4 @@\n-# ADR 0005: Use Codex As Front And Claude Code Action As The GitHub Runtime\n+# SADR-0005: Use Codex As Front And Claude Code Action As The GitHub Runtime\n \n ## Status\n \ndiff --git a/docs/adr/0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md b/docs/adr/SADR-0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\nindex 4bcae9d1e52f0a37b8e9fbafa5cd14b6ee1993bd..7a38538a6ae9929567f0ade67e7ea6a60d0b3f7e\n--- a/docs/adr/0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\n+++ b/docs/adr/SADR-0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\n@@ -1,4 +1,4 @@\n-# ADR 0006: Separate Codex Implementation, CCA Completion, And MergeGate Authorization\n+# SADR-0006: Separate Codex Implementation, CCA Completion, And MergeGate Authorization\n \n ## Status\n \ndiff --git a/docs/adr/0007-packaging-and-release.md b/docs/adr/SADR-0007-packaging-and-release.md\nindex 79b84ddb97167372b44641e5b7860e400ce80822..bb445a2e2e856d9d1f5fdbe35de8d4e8cec0fb32\n--- a/docs/adr/0007-packaging-and-release.md\n+++ b/docs/adr/SADR-0007-packaging-and-release.md\n@@ -1,4 +1,4 @@\n-# ADR 0007: Packaging And Release For Shiki\n+# SADR-0007: Packaging And Release For Shiki\n \n ## Status\n \n@@ -25,7 +25,7 @@\n   not introduce third-party runtime dependencies or break the shim.\n - Releases must run inside the operator's subscription-authenticated toolchain\n   and must not require extra secrets beyond the built-in `GITHUB_TOKEN`\n-  (consistent with ADR 0005's no-API-key-by-default stance).\n+  (consistent with SADR-0005's no-API-key-by-default stance).\n - `.shiki/manifest.json` already encodes the install surface (`install.include`,\n   `install.create_directories`, `install.exclude_from_commit`), and it must not\n   be mutated as part of this change.\n@@ -34,7 +34,7 @@\n \n This is a hard-to-reverse platform decision (it sets the version source of\n truth, the release trigger, and the public install contract), so it is recorded\n-as an ADR.\n+as a SADR.\n \n ## Decision\n \n@@ -104,7 +104,7 @@\n 3. **Publishing to PyPI.** Rejected for now: Shiki is distributed as a\n    repository template + git-installable CLI, not a PyPI library; PyPI would add\n    credentials/secrets and a different versioning contract. Can be added later as\n-   its own ADR if a packaged distribution is needed.\n+   its own SADR if a packaged distribution is needed.\n 4. **Dynamic version (read `VERSION` via a build backend `dynamic` field).**\n    Rejected to keep `pyproject.toml` backend-agnostic and dependency-free; the\n    small duplication between `VERSION` and `pyproject.toml` is guarded by the\n@@ -123,4 +123,4 @@\n   releasing versions the whole platform but ships only `install.include` to\n   targets.\n - Future PyPI distribution or a console entry point are deferred and would each\n-  warrant their own ADR.\n+  warrant their own SADR.\ndiff --git a/docs/adr/0008-use-claude-code-as-default-implementer.md b/docs/adr/SADR-0008-use-claude-code-as-default-implementer.md\nindex dedf4510b7c7799521422f622029563d373db031..35c2625d908167fbc58855b5b63e283805f167e8\n--- a/docs/adr/0008-use-claude-code-as-default-implementer.md\n+++ b/docs/adr/SADR-0008-use-claude-code-as-default-implementer.md\n@@ -1,12 +1,12 @@\n-# ADR 0008: Use Claude Code As The Default Implementer Runtime\n+# SADR-0008: Use Claude Code As The Default Implementer Runtime\n \n ## Status\n \n-Proposed (supersedes ADR 0005 for the implementer default; keeps its auth model)\n+Proposed (supersedes SADR-0005 for the implementer default; keeps its auth model)\n \n ## Context\n \n-ADR 0005 made Codex Front the default implementation runtime, with Claude Code\n+SADR-0005 made Codex Front the default implementation runtime, with Claude Code\n restricted to planner and reviewer roles by the runtime registry, and the only\n autonomous implementation adapter was `shiki runner codex`.\n \n@@ -18,13 +18,13 @@\n - The platform's planning, review, and CCA layers already run on Claude Code,\n   and Claude Code's native capabilities (the built-in `/code-review` skill and\n   the Workflow multi-agent orchestration tool) have no Codex equivalent.\n-- The Goal lifecycle redesign (ADR 0009) requires an autonomous post-freeze\n+- The Goal lifecycle redesign (SADR-0009) requires an autonomous post-freeze\n   loop driven from the operator's Claude Code session, including bounded\n   repair dispatch, which must not depend on a runtime that cannot be invoked\n   unattended.\n \n The constitution claims Shiki is runtime-agnostic (\"not a Claude-only\n-workflow\", ADR 0002). Any change to the implementer default must preserve the\n+workflow\", SADR-0002). Any change to the implementer default must preserve the\n registry-based runtime model rather than hardcoding one vendor.\n \n ## Decision\n@@ -39,7 +39,7 @@\n   the task to review or repair-needed.\n - Keep Codex registered as an optional implementer runtime. `shiki runner\n   codex`, the Codex handoff templates, and the ChatGPT OAuth auth model from\n-  ADR 0005 remain supported for tasks explicitly assigned to `codex`.\n+  SADR-0005 remain supported for tasks explicitly assigned to `codex`.\n - Commit to mandating two Claude-native capabilities as loop contract in\n   follow-up Goals (status: the code-review gate was delivered by Goal G-B \u2014\n   skill registry, checklists item PR-12, default required_skills \u2014 while the\n@@ -60,7 +60,7 @@\n \n Out of scope: removing Codex support, changing the CCA runtime (Claude Code\n Action with `CLAUDE_CODE_OAUTH_TOKEN` stays), and API-key based automation\n-(still requires its own ADR per ADR 0005).\n+(still requires its own target ADR per SADR-0005).\n \n ## Consequences\n \n@@ -100,7 +100,7 @@\n   harness rejection of direct `codex exec` is outside Shiki's control, and the\n   plan's mandatory native capabilities cannot run on Codex at all.\n - **Remove Codex entirely**: rejected; it abandons the runtime-agnostic\n-  constitution (ADR 0002) for no operational gain and forecloses\n+  constitution (SADR-0002) for no operational gain and forecloses\n   cross-runtime adversarial review.\n - **GitHub Actions cloud implementation via claude-code-action**: rejected as\n   default; durable evidence is automatic but token cost, Node 24 workflow\ndiff --git a/docs/adr/0009-requirements-definition-and-spec-freeze.md b/docs/adr/SADR-0009-requirements-definition-and-spec-freeze.md\nindex 29b3f7871bef9203a8b4a831a65c39eb2f506e42..52e7dd69ad0aa4efe0b68c62678abbf7df02aaec\n--- a/docs/adr/0009-requirements-definition-and-spec-freeze.md\n+++ b/docs/adr/SADR-0009-requirements-definition-and-spec-freeze.md\n@@ -1,4 +1,4 @@\n-# ADR 0009: Merge Grill And Planning Into Requirements Definition With Spec Freeze\n+# SADR-0009: Merge Grill And Planning Into Requirements Definition With Spec Freeze\n \n ## Status\n \ndiff --git a/docs/adr/0010-external-ai-guardian-review.md b/docs/adr/SADR-0010-external-ai-guardian-review.md\nindex e6108bbe8bb4e73e0ff4684c8f78001a80438c4e..f3510b145664298adafb151b6f0db51b972f01ec\n--- a/docs/adr/0010-external-ai-guardian-review.md\n+++ b/docs/adr/SADR-0010-external-ai-guardian-review.md\n@@ -1,4 +1,4 @@\n-# ADR 0010: External AI Guardian Review As A First-Class Approval Authority\n+# SADR-0010: External AI Guardian Review As A First-Class Approval Authority\n \n ## Status\n \n@@ -8,7 +8,7 @@\n \n Shiki is intended to be an autonomous, recursive, self-improving agent\n framework: authority-in-the-loop, not human-in-the-loop. Until now the Guardian\n-approval required for high/critical-risk merges (guardian-policy.json, ADR\n+approval required for high/critical-risk merges (guardian-policy.json, SADR\n governance) recognized only human authority sources \u2014 a GitHub review approval,\n the `guardian:approved` label applied by a configured Guardian, or a comment\n carrying `Guardian approval granted` plus the current head SHA.\n@@ -89,7 +89,7 @@\n   autonomous design the operator intends and produced the impersonation\n   incident.\n - **Record AI approval as an operator comment**: rejected as forgery of human\n-  authority \u2014 the exact failure this ADR corrects.\n+  authority \u2014 the exact failure this SADR corrects.\n - **Deliver the artifact as a committed `.shiki/` file**: rejected; a committed\n   approval inside the PR being approved is forgeable by anyone who can push.\n - **A dedicated reviewer bot identity instead of a relayed comment**: deferred;\ndiff --git a/docs/adr/0011-autonomous-quality-gate-evidence.md b/docs/adr/SADR-0011-autonomous-quality-gate-evidence.md\nindex bca0246c5d46e44afbde15486a75eec425fc02f5..9c93eac16d8c3a6d4b11bd7224e3955102cd7db4\n--- a/docs/adr/0011-autonomous-quality-gate-evidence.md\n+++ b/docs/adr/SADR-0011-autonomous-quality-gate-evidence.md\n@@ -1,4 +1,4 @@\n-# ADR 0011: Autonomous Quality-Gate Evidence \u2014 Deterministic Facts And An Independent Verifier\n+# SADR-0011: Autonomous Quality-Gate Evidence \u2014 Deterministic Facts And An Independent Verifier\n \n ## Status\n \ndiff --git a/docs/adr/0012-autonomous-loop-closeout-to-main.md b/docs/adr/SADR-0012-autonomous-loop-closeout-to-main.md\nindex 637fad6dde4989c9c2bf0f81f676d8d29991123c..ec7d85f81030a676bcbba28b43eb365422a36eac\n--- a/docs/adr/0012-autonomous-loop-closeout-to-main.md\n+++ b/docs/adr/SADR-0012-autonomous-loop-closeout-to-main.md\n@@ -1,4 +1,4 @@\n-# 0012 \u2014 Autonomous loop pushes goal completion to main via a closeout PR\n+# SADR-0012 \u2014 Autonomous loop pushes goal completion to main via a closeout PR\n \n - Status: accepted\n - Date: 2026-06-18\ndiff --git a/docs/adr/0013-bootstrap-grants-actions-approve-pull-requests.md b/docs/adr/SADR-0013-bootstrap-grants-actions-approve-pull-requests.md\nindex b6c69d64f1c5ced346b88d6859586715b3b5bd0e..cf48ff4a6716267f539ca58edd1a852e7c7a2d64\n--- a/docs/adr/0013-bootstrap-grants-actions-approve-pull-requests.md\n+++ b/docs/adr/SADR-0013-bootstrap-grants-actions-approve-pull-requests.md\n@@ -1,4 +1,4 @@\n-# ADR 0013: Bootstrap Grants GitHub Actions Permission To Create And Approve Pull Requests\n+# SADR-0013: Bootstrap Grants GitHub Actions Permission To Create And Approve Pull Requests\n \n ## Status\n \n@@ -31,7 +31,7 @@\n security tradeoff: it lets repository automation satisfy a human-review gate. In\n a multi-maintainer repo that could let a compromised or buggy workflow\n self-approve changes. So whether bootstrap turns it on by default \u2014 and with\n-what default token scope \u2014 is an ADR-worthy decision, not an implementation\n+what default token scope \u2014 is a SADR-worthy decision, not an implementation\n detail.\n \n ## Decision\n@@ -48,7 +48,7 @@\n Specifically:\n \n - **`can_approve_pull_request_reviews` defaults to `true`.** Shiki's default\n-  posture is solo/AFK autonomous operation (ADR 0008/0009/0012), where the CCA\n+  posture is solo/AFK autonomous operation (SADR-0008/0009/0012), where the CCA\n   Review Bridge is the only path that can satisfy `required_review: true` after\n   the CCA and Guardian gates have already passed. The default makes a\n   freshly-bootstrapped repo able to run the autonomous loop end to end without a\ndiff --git a/docs/adr/0014-codex-app-external-ai-guardian-ui-adapter.md b/docs/adr/SADR-0014-codex-app-external-ai-guardian-ui-adapter.md\nindex 238db06279334caa341c117362df9271382a7fcd..65f12e9939321e1b3e389a34c822fc1549e5155d\n--- a/docs/adr/0014-codex-app-external-ai-guardian-ui-adapter.md\n+++ b/docs/adr/SADR-0014-codex-app-external-ai-guardian-ui-adapter.md\n@@ -1,4 +1,4 @@\n-# ADR 0014: Use Codex App As The External AI Guardian UI Adapter\n+# SADR-0014: Use Codex App As The External AI Guardian UI Adapter\n \n ## Status\n \ndiff --git a/docs/adr/0015-contract-approval-before-dispatch.md b/docs/adr/SADR-0015-contract-approval-before-dispatch.md\nindex fad6c656741a20a7684ba16080a7db159f63a52f..6061271a46d3f1d6ae7174477de3fb75673123a6\n--- a/docs/adr/0015-contract-approval-before-dispatch.md\n+++ b/docs/adr/SADR-0015-contract-approval-before-dispatch.md\n@@ -1,4 +1,4 @@\n-# ADR 0015: Approve The Task Contract Before Dispatch, Not The Diff After It\n+# SADR-0015: Approve The Task Contract Before Dispatch, Not The Diff After It\n \n ## Status\n \n@@ -9,7 +9,7 @@\n Guardian approval for high/critical risk is expressible only on a pull request.\n Every approval source in `guardian-policy.json` \u2014 GitHub review, the\n `guardian:approved` label, the `Guardian approval granted` comment, and the\n-`external_ai_guardian_review` artifact (ADR 0010/0014) \u2014 is bound to a PR, and\n+`external_ai_guardian_review` artifact (SADR-0010/0014) \u2014 is bound to a PR, and\n three of the four require the current head SHA. Approval therefore cannot exist\n before an implementation PR exists.\n \n@@ -81,7 +81,7 @@\n An envelope breach must surface only on the non-repairable policy gate, never on\n the repairable `MergeGate metadata check`. Otherwise the Goal Loop routes it to\n `dispatch_repair` \u2014 instructing a runner to make the breach go away \u2014 which is\n-exactly what this ADR forbids.\n+exactly what this SADR forbids.\n \n **Contract immutability.** After registration, an implementation PR must not\n change the governance fields of its own task contract (`scope`, `non_goals`,\n@@ -116,7 +116,7 @@\n fails any verdict that is not `complete`, and the signal the CCA prompt reads is\n computed from `evaluate_guardian_approval` alone. Without the OR-merge at the\n signal, a contract-approved PR still returns `needs_guardian`, the CCA job fails,\n-and the MergeGate policy check never runs \u2014 the gate this ADR changes would never\n+and the MergeGate policy check never runs \u2014 the gate this SADR changes would never\n be reached. The CCA *prompt* needs no change: a `contract_approval` entry in the\n signal's existing `sources` array satisfies its current wording.\n \n@@ -130,7 +130,7 @@\n written, and an approved contract lets its implementation PRs merge through the\n same automatic path as low/medium risk. The wasted implement-then-block cycle\n disappears. Guardian review shifts from reading diffs to reading contracts, which\n-is both earlier and smaller; the external AI Guardian path (ADR 0010/0014) works\n+is both earlier and smaller; the external AI Guardian path (SADR-0010/0014) works\n on contracts with no change.\n \n We accept that the Guardian authorizes a class of change without seeing its\n@@ -146,7 +146,7 @@\n adversarial tests for the new approval path equivalent in rigor to\n `test_shiki_governance_evidence.sh`.\n \n-Bootstrap costs, which apply only while this ADR is being implemented. The tasks\n+Bootstrap costs, which apply only while this SADR is being implemented. The tasks\n that build Contract Approval merge under the existing rules, so the ones\n dispatched after the pre-dispatch gate lands but before the approval path lands\n need a Guardian-approved Contract PR *and* a live Guardian approval on their\n@@ -210,7 +210,7 @@\n risk; they do not bound authorized work.\n \n **Auto-escalate risk on envelope breach instead of stopping.** Returns the\n-workflow to a diff-time Guardian decision \u2014 the exact behavior this ADR removes.\n+workflow to a diff-time Guardian decision \u2014 the exact behavior this SADR removes.\n \n **Resolve governance fields from the base snapshot only.** Rejected against the\n registration flow: implementation PRs add their own contract, so the base has no\ndiff --git a/docs/adr/0016-locks-bound-product-paths.md b/docs/adr/SADR-0016-locks-bound-product-paths.md\nindex b5e850f3f1f7f4ef6e7f9d1763a29a51bd300c45..46b40a9b99a7aae644c02bb458f5473ff0a90ce2\n--- a/docs/adr/0016-locks-bound-product-paths.md\n+++ b/docs/adr/SADR-0016-locks-bound-product-paths.md\n@@ -1,4 +1,4 @@\n-# ADR 0016: Locks bound product paths; the Shiki mirror is bound by record identity\n+# SADR-0016: Locks bound product paths; the Shiki mirror is bound by record identity\n \n ## Status\n \n@@ -6,8 +6,8 @@\n \n - Date: 2026-07-28\n - Deciders: operator (mizutani-140), Claude (planner)\n-- Related: ADR 0004 (GitHub-first with a `.shiki` mirror), ADR 0009 (Spec Freeze),\n-  ADR 0012 (autonomous loop closeout to main), ADR 0015 (Contract Approval before\n+- Related: SADR-0004 (GitHub-first with a `.shiki` mirror), SADR-0009 (Spec Freeze),\n+  SADR-0012 (autonomous loop closeout to main), SADR-0015 (Contract Approval before\n   dispatch), remediation goal task R-02, the live PR #179 deadlock.\n \n ## Context\n@@ -90,11 +90,11 @@\n files, documentation files and named source modules are nameable; runtime-\n allocated ledger, runner and report ids are not \u2014 and those live in `.shiki`,\n which locks no longer govern. Existing frozen contracts are brought into line by\n-a one-time Spec Amendment per plan, which must land before ADR 0015's contract\n+a one-time Spec Amendment per plan, which must land before SADR-0015's contract\n immutability makes `locks` unwritable from the implementation PR.\n \n **Lock state is resolved from the durable side.** MergeGate resolves the lock\n-registry, and ADR 0015's governance fields, from the base `.shiki` snapshot when\n+registry, and SADR-0015's governance fields, from the base `.shiki` snapshot when\n present, falling back to head only when absent. The `MergeGate metadata check`\n job is given `--base-shiki`, which it does not receive today. Locks are not\n derived from the live open-pull-request set: a pull request body is untrusted\n@@ -108,7 +108,7 @@\n Explicitly out of scope: moving MergeGate's contract resolution off the PR\n checkout wholesale; making the mirror a projection of GitHub state; introducing\n new lock kinds; moving ledger evidence out of the repository; and unwinding\n-ADR 0012's closeout pull request. Registration of multi-task goals is ADR 0015's\n+SADR-0012's closeout pull request. Registration of multi-task goals is SADR-0015's\n Contract PR and is not re-decided here.\n \n ## Consequences\n@@ -127,7 +127,7 @@\n \n Risks accepted. First, goal-scoped identity is only as strong as `goal_id`,\n which is resolved from the PR body and the head task file; the mitigation is to\n-add `goal_id` to ADR 0015's frozen governance field set, and until that lands a\n+add `goal_id` to SADR-0015's frozen governance field set, and until that lands a\n task file can name a foreign goal. Second, base-when-present resolution is\n unavailable on fork pull requests, where the head is contributor-controlled;\n fork pull requests already cannot complete a Shiki merge, and this decision does\n@@ -139,7 +139,7 @@\n path, not by implementation pull requests.\n \n New obligations: one Spec Amendment per frozen plan to rewrite `locks`, ordered\n-before ADR 0015 T1; extension of the adversarial suites in\n+before SADR-0015 T1; extension of the adversarial suites in\n `scripts/test_shiki_adversarial_state.sh` and\n `scripts/test_shiki_governance_evidence.sh` to cover the eleven newly-scoped\n directories; and updating `scripts/test_shiki_loop_lock_guard.sh`,\n@@ -151,32 +151,32 @@\n whether the loop's transitive sync of ledger `evidence` references can be\n deleted once the Contract PR carries its own source plan.\n \n-## Relationship to ADR 0004\n+## Relationship to SADR-0004\n \n-This ADR **clarifies something ADR 0004 left unspecified**. ADR 0004 decides that\n+This SADR **clarifies something SADR-0004 left unspecified**. SADR-0004 decides that\n GitHub is the operational source of truth and that each target repository keeps a\n `.shiki/` mirror. It says nothing about pull requests writing the mirror, does\n not name MergeGate, and does not make the mirror an input to a gate. Nothing in\n-ADR 0004's Decision or Consequences is amended, superseded or weakened: the\n+SADR-0004's Decision or Consequences is amended, superseded or weakened: the\n mirror still exists, still records the same artifacts, and pull request\n-boundaries remain the coordination surface. What this ADR settles is the question\n+boundaries remain the coordination surface. What this SADR settles is the question\n 0004 never reached \u2014 by what authority a pull request may write a mirror record \u2014\n and it answers it the way `.shiki/manifest.json` already declares\n (`pr_mutation: current-task-or-current-goal-only`).\n \n-It **amends ADR 0012**, whose Decision item 5 states that \"Loop-executed tasks\n+It **amends SADR-0012**, whose Decision item 5 states that \"Loop-executed tasks\n declare the `path:.shiki/**` lock, which covers every `.shiki` file the closeout\n stages ... so `files_outside_locks` is satisfied without per-file lock\n bookkeeping.\" That clause is replaced: the closeout's mirror writes are\n-authorized by record identity, not by a declared lock. ADR 0012's substantive\n+authorized by record identity, not by a declared lock. SADR-0012's substantive\n decision \u2014 that completion reaches `main` through a closeout pull request rather\n than local attestation \u2014 is preserved unchanged.\n \n-It is **complementary to ADR 0015**, not a substitute. ADR 0015 owns contract\n+It is **complementary to SADR-0015**, not a substitute. SADR-0015 owns contract\n immutability, the pre-dispatch gate, the Contract PR and the Scope Envelope. This\n-ADR owns what a lock means and who may write the mirror. The one ordering\n+SADR owns what a lock means and who may write the mirror. The one ordering\n constraint between them is that the lock-narrowing Spec Amendment must precede\n-ADR 0015 T1's freezing of the `locks` field.\n+SADR-0015 T1's freezing of the `locks` field.\n \n ## Alternatives Considered\n \n@@ -186,7 +186,7 @@\n the only writer that can register them may run. Migrating the Guardian signal to\n that store makes every registration PR fail closed to `needs_guardian` at every\n risk level, because the task file is by definition absent from the store, which\n-removes the low/medium autonomous path ADR 0008/0009 exists to provide. And it\n+removes the low/medium autonomous path SADR-0008/0009 exists to provide. And it\n leaves schemas, manifest, config, guardian policy, templates, migrations, plans,\n worktrees, runner records, runs and handoffs with no writer at all in a\n repository whose product is `.shiki`. Measured benefit on the frozen backlog:\n@@ -199,14 +199,14 @@\n namespace is not in fact ownership-partitioned \u2014 56 ledger entries carry no\n `task_id`, and one goal DAG is writable by 35 registered tasks. The correct\n expression of its insight is that locks do not govern the mirror at all, which is\n-what this ADR decides.\n+what this SADR decides.\n \n **Make the mirror a projection written only by a registration writer and a\n post-merge projection job.** Rejected. It requires a bypass actor with write\n access to the protected default branch; it makes its own master invariant\n readable from a manifest that pull requests may edit; it deletes both reconcile\n modes and leaves a corrupted mirror with no in-band repair path, contradicting\n-ADR 0004's consequence that mirror conflicts are repaired against GitHub state;\n+SADR-0004's consequence that mirror conflicts are repaired against GitHub state;\n and its content-derived ids are rejected by `validate_shiki`'s currently-green id\n pattern.\n \ndiff --git a/docs/adr/0017-bookkeeping-closeouts-do-not-inherit-risk.md b/docs/adr/SADR-0017-bookkeeping-closeouts-do-not-inherit-risk.md\nindex 554e3a0d526a72ca067108c5464cc68c3edcf174..b9676857a4713850e063af7c39c8e0b01ecb4c66\n--- a/docs/adr/0017-bookkeeping-closeouts-do-not-inherit-risk.md\n+++ b/docs/adr/SADR-0017-bookkeeping-closeouts-do-not-inherit-risk.md\n@@ -1,4 +1,4 @@\n-# ADR 0017: A Bookkeeping Closeout Does Not Inherit Its Task's Risk\n+# SADR-0017: A Bookkeeping Closeout Does Not Inherit Its Task's Risk\n \n ## Status\n \n@@ -6,7 +6,7 @@\n \n ## Context\n \n-ADR 0012 made the goal loop push a task's completion to the default branch through a\n+SADR-0012 made the goal loop push a task's completion to the default branch through a\n closeout pull request: after the implementation PR merges, the loop opens a second PR\n carrying `task.status = done`, `lock.state = released`, and \u2014 when the task completes\n its goal \u2014 `goal.status = complete` plus one scorecard report. Nothing else. The\n@@ -49,14 +49,14 @@\n 4. The task file's governance fields (`scope`, `non_goals`, `required_skills`,\n    `risk_level`, `locks`, `acceptance_checks`, `test_command`) are byte-identical to\n    the base snapshot. Only `status`, `expected_pr`, `closeout_pr`, `ledger_evidence`\n-   and `expected_branch` may differ \u2014 the ADR 0015 contract-immutability rule,\n+   and `expected_branch` may differ \u2014 the SADR-0015 contract-immutability rule,\n    unchanged.\n \n    `expected_branch` is in that set because a closeout is *by construction* on a\n    different branch from the implementation it closes, and the loop rewrites the\n    field accordingly. It is safe to let it differ because MergeGate independently\n    requires `expected_branch` to equal the PR's actual head ref, so a PR cannot set\n-   it to anything other than the branch it is really on. The first draft of this ADR\n+   it to anything other than the branch it is really on. The first draft of this SADR\n    omitted it, which made the exemption unreachable: every real closeout failed\n    condition 4 on a field the loop always changes. See PR #209.\n 5. The state transitions are exactly the terminal set: task `review` \u2192 `done`, lock\n@@ -113,12 +113,12 @@\n classification must be derived, not asserted.\n \n **Fold the closeout into the implementation PR** so no second PR exists. Rejected under\n-ADR 0012, which recorded why: a task file that is already `done` on the implementation\n+SADR-0012, which recorded why: a task file that is already `done` on the implementation\n branch short-circuits the loop's review \u2192 CCA \u2192 merge \u2192 done ordering into a local\n goal-completion before any merge happens.\n \n **Have the loop auto-merge closeouts with elevated credentials.** Rejected outright: that\n-is a runtime authoring its own approval, which ADR 0010 and the constitution's\n+is a runtime authoring its own approval, which SADR-0010 and the constitution's\n approval-record prohibition forbid. It would also be indistinguishable, in the evidence,\n from the forgery those rules exist to prevent.\n \ndiff --git a/docs/adr/0018-carried-guardian-approval-across-a-proven-base-sync.md b/docs/adr/SADR-0018-carried-guardian-approval-across-a-proven-base-sync.md\nindex 22358562da390ed8b45cd600da08a43d378b4680..e6e2907cefa776460af73ddf7d39db605f3c39b0\n--- a/docs/adr/0018-carried-guardian-approval-across-a-proven-base-sync.md\n+++ b/docs/adr/SADR-0018-carried-guardian-approval-across-a-proven-base-sync.md\n@@ -1,4 +1,4 @@\n-# ADR 0018: Carried Guardian Approval Across A Proven Base Sync\n+# SADR-0018: Carried Guardian Approval Across A Proven Base Sync\n \n ## Status\n \n@@ -34,7 +34,7 @@\n Guardian evaluator that consumes it (`shiki_guardian.evaluate_guardian_approval`,\n the `guardian_comment_carried` source) were built and tested in isolation. Neither\n does anything until a caller passes the flags that turn the carry on, so the\n-feature was invisible in CI. This ADR records the decision the two of them\n+feature was invisible in CI. This SADR records the decision the two of them\n implement, and this task wires it into the four CI invocations that judge Guardian\n approval.\n \ndiff --git a/docs/adr/README.md b/docs/adr/README.md\nindex c70e057a4520c36e042ab68c53cd4a21ef709b71..5da43856836e2ce6a150f8127766e2f88355ab4b\n--- a/docs/adr/README.md\n+++ b/docs/adr/README.md\n@@ -1,40 +1,41 @@\n-# Architecture Decision Records\n+# Decision Record Namespaces\n \n-This directory holds Shiki's Architecture Decision Records (ADRs): durable\n-records of hard-to-reverse platform decisions. ADRs are part of the Shiki source\n-of truth alongside GitHub state, the `.shiki/` mirror, and `CONTEXT.md`.\n+This directory holds two independent decision namespaces:\n \n-## How ADRs Work\n+- Shiki Architecture Decision Records (SADRs), named\n+  `SADR-NNNN-<short-slug>.md`, record hard-to-reverse Shiki platform decisions.\n+- Target Architecture Decision Records (ADRs), named\n+  `NNNN-<short-slug>.md`, belong to the target product.\n \n-- New ADRs are added as `docs/adr/NNNN-<short-slug>.md`, where `NNNN` is the next\n-  zero-padded sequence number.\n-- The **canonical list of ADRs is the set of files matching `docs/adr/*.md`** in\n-  this directory. Browse `docs/adr/*.md` for the authoritative, current set.\n-- Use [`template.md`](template.md) as the starting point for a new ADR.\n-- Record an ADR when a decision is hard to reverse, surprising without context,\n-  and a real tradeoff. Do not write an ADR for routine or easily reversible\n-  choices.\n+The same four-digit number may appear once in each namespace. A platform\n+`SADR-NNNN` citation resolves only to `SADR-NNNN-*.md`; a target `ADR NNNN`\n+citation resolves only to `NNNN-*.md`.\n \n-Using the `docs/adr/*.md` glob as the source of truth means other PRs can add an\n-ADR without editing this index file, avoiding merge contention on a\n-hand-maintained enumeration.\n+## How Decision Records Work\n \n-## Current ADRs (snapshot)\n-\n-The list below is a convenience snapshot and may lag behind the directory. When\n-in doubt, list `docs/adr/*.md`.\n+- New Shiki platform decisions use the next zero-padded SADR number.\n+- New target product decisions use the target's own ADR sequence.\n+- The canonical list is the files in `docs/adr/`; no hand-maintained index is\n+  authoritative.\n+- Use [`template.md`](template.md) as the starting point and choose the filename\n+  namespace that owns the decision.\n+- Record a decision when it is hard to reverse, surprising without context, and\n+  a real tradeoff. Do not write a decision record for routine or easily\n+  reversible choices.\n \n-- [0001 \u2014 Keep Shiki As The Platform Name](0001-keep-shiki-as-platform-name.md)\n-- [0002 \u2014 Use AGENTS As The Shared Agent Constitution](0002-use-agents-as-shared-constitution.md)\n-- [0003 \u2014 Require Skill Gate For MergeGate Readiness](0003-require-skill-gate-for-mergegate-readiness.md)\n-- [0004 \u2014 Use GitHub First With A Repository-Local Shiki Mirror](0004-use-github-first-with-shiki-mirror.md)\n-- [0005 \u2014 Use Codex As Front And Claude Code Action As The GitHub Runtime](0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md)\n-- [0006 \u2014 Separate Codex Implementation, CCA Completion, And MergeGate Authorization](0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md)\n+Using the directory as the source of truth lets independent changes add records\n+without contending on an enumerated index.\n \n-## Adding A New ADR\n+## Adding A Shiki SADR\n \n-1. Copy `template.md` to `docs/adr/NNNN-<short-slug>.md`.\n+1. Copy `template.md` to `docs/adr/SADR-NNNN-<short-slug>.md`.\n 2. Fill in the title, status, context, decision, and consequences.\n-3. Set the status to `Proposed`, then `Accepted` once the decision is approved.\n-4. Open a PR following `CONTRIBUTING.md`. You do not need to edit this index for\n-   the ADR to be discoverable via `docs/adr/*.md`.\n+3. Set the status to `Proposed`, then `Accepted` once the proper authority has\n+   approved the decision.\n+4. Open a PR following `CONTRIBUTING.md`.\n+\n+## Adding A Target ADR\n+\n+1. Copy `template.md` to `docs/adr/NNNN-<short-slug>.md` using the target's own\n+   sequence.\n+2. Cite it as `ADR NNNN`; do not prefix its filename with `SADR-`.\ndiff --git a/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md b/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\nnew file mode 100644\nindex 0000000000000000000000000000000000000000..7455ce1f3bed02ddddb851ac21a1d1a648c9aeba\n--- /dev/null\n+++ b/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\n@@ -0,0 +1,55 @@\n+# SADR-0019: Separate Shiki Platform SADRs From Target ADRs\n+\n+## Status\n+\n+Accepted\n+\n+## Context\n+\n+Shiki installs its platform decision records into `docs/adr/` in every target\n+repository. Shiki and its targets previously used the same numeric\n+`NNNN-<slug>.md` namespace. A target that already owned ADRs 0013 through 0018\n+therefore acquired two records for each number during installation, making an\n+`ADR NNNN` citation ambiguous. Renumbering target decisions would rewrite target\n+history, while renumbering Shiki decisions per target would destroy their stable\n+platform identity.\n+\n+Upgrades also need to remove the former Shiki-owned numeric paths without ever\n+mistaking a target-owned ADR for platform content. The install stamp already\n+records exact shipped paths and content digests, so it can prove ownership only\n+when both still match.\n+\n+## Decision\n+\n+Shiki platform decisions use the `SADR` namespace:\n+\n+- records are named `docs/adr/SADR-NNNN-<slug>.md`;\n+- live platform citations use `SADR-NNNN`;\n+- target product decisions retain `docs/adr/NNNN-<slug>.md` and `ADR NNNN`;\n+- the reference validator resolves the two namespaces independently, so the same\n+  four-digit number may exist once in each namespace.\n+\n+Before a forced upgrade writes anything, the installer inventories the exact\n+former numeric Shiki ADR paths. It may delete a legacy path only when the\n+existing install stamp contains that exact path and its recorded SHA-256 digest\n+matches the file currently on disk. All ownership blockers are reported\n+together and abort the upgrade before its first write. A non-forced install\n+never performs this cleanup.\n+\n+Historical `.shiki/` evidence remains unchanged because it records the namespace\n+that existed when that evidence was produced.\n+\n+## Consequences\n+\n+- Targets may use any ADR number without colliding with Shiki's platform SADRs.\n+- Fresh installs preserve target ADR bytes and install a stable SADR set.\n+- Forced upgrades fail closed when legacy ownership cannot be proven, and a\n+  successful upgrade refreshes the stamp without removed legacy paths.\n+- Current governance, packaging, workflow, prompt, script, and skill references\n+  must move with the platform records.\n+\n+## Alternatives Considered\n+\n+- Collision-only refusal leaves affected targets permanently unupgradable.\n+- Renumbering target ADRs mutates target-owned history.\n+- Per-target Shiki renumbering makes platform citations non-portable.\ndiff --git a/docs/adr/template.md b/docs/adr/template.md\nindex a92a97daddd0b00ab9dd626cc8d86942da2eafed..17dd79175b5fee2105935557f691b40cf9492942\n--- a/docs/adr/template.md\n+++ b/docs/adr/template.md\n@@ -1,14 +1,14 @@\n-# NNNN: <Short Decision Title>\n+# <SADR-NNNN or NNNN>: <Short Decision Title>\n \n ## Status\n \n-Proposed | Accepted | Superseded by ADR-NNNN\n+Proposed | Accepted | Superseded by SADR-NNNN or target ADR NNNN\n \n ## Context\n \n What is the situation, constraint, or force that requires a decision? Reference\n-relevant domain terms (`CONTEXT.md`), prior ADRs, and code reality. State the\n-problem, not the solution.\n+relevant domain terms (`CONTEXT.md`), prior Shiki SADRs or target ADRs, and code\n+reality. State the problem, not the solution.\n \n ## Decision\n \ndiff --git a/docs/agents/autonomous-loop-e2e-acceptance.md b/docs/agents/autonomous-loop-e2e-acceptance.md\nindex bbf84c6081987f159ac6ad9ecf3c82b7aa72e7e8..5c13c1d02a05a90b0dc46cbef7c95a9846d0c237\n--- a/docs/agents/autonomous-loop-e2e-acceptance.md\n+++ b/docs/agents/autonomous-loop-e2e-acceptance.md\n@@ -40,8 +40,8 @@\n 3. `gh auth status` is logged in with merge permission on the repo.\n 4. Branch protection + required checks are configured exactly as the goal-loop\n    required-check set expects (see `docs/agents/branch-protection-smoke.md`).\n-5. The repo `.shiki/policy` permits autonomous low/medium-risk merges (ADR\n-   0008/0009); the MergeGate policy check is the recorded-authority gate.\n+5. The repo `.shiki/policy` permits autonomous low/medium-risk merges\n+   (SADR-0008/0009); the MergeGate policy check is the recorded-authority gate.\n \n The operator's only actions are: register the task and start the loop. **No\n manual pre-clearing** means after `shiki loop run` starts, the operator does not\ndiff --git a/docs/agents/checklists.md b/docs/agents/checklists.md\nindex 4a7f03ade346f53a1c1d82981f3fd744f9ab4547..0c5ec6c53dc7197b7b7a7d18e667b325babd6c45\n--- a/docs/agents/checklists.md\n+++ b/docs/agents/checklists.md\n@@ -32,18 +32,18 @@\n | ID | Item | Blocking |\n | --- | --- | --- |\n | GD-01 | Plan was challenged against `CONTEXT.md`. | blocking for non-trivial Goals |\n-| GD-02 | Relevant ADRs were read or absence was recorded. | blocking for non-trivial Goals |\n+| GD-02 | Relevant SADRs and target ADRs were read or absence was recorded. | blocking for non-trivial Goals |\n | GD-03 | Ambiguous terms were resolved or converted to HITL blockers. | blocking |\n | GD-04 | Edge scenarios were considered. | advisory |\n | GD-05 | Code reality was checked when code could answer. | blocking when applicable |\n | GD-06 | New domain terms were added to `CONTEXT.md` when needed. | blocking when applicable |\n-| GD-07 | ADR was created or explicitly skipped using the ADR criteria. | blocking when applicable |\n+| GD-07 | A SADR or target ADR was created or explicitly skipped using the decision-record criteria. | blocking when applicable |\n \n ## CI \u2014 Context & Impact Checklist\n \n | ID | Item | Blocking |\n | --- | --- | --- |\n-| CI-01 | Relevant docs and ADRs identified. | blocking |\n+| CI-01 | Relevant docs, SADRs, and target ADRs identified. | blocking |\n | CI-02 | Relevant modules, interfaces, seams, callers, and tests identified. | blocking |\n | CI-03 | Dependencies and blocked-by relationships identified. | blocking |\n | CI-04 | Candidate locks identified. | blocking |\n@@ -62,7 +62,7 @@\n | PRD-04 | Implementation decisions are recorded without volatile code detail. | blocking |\n | PRD-05 | Testing decisions are recorded. | blocking |\n | PRD-06 | Out-of-scope items are explicit. | blocking |\n-| PRD-07 | Relevant glossary/ADR links are present when applicable. | blocking |\n+| PRD-07 | Relevant glossary/SADR/target-ADR links are present when applicable. | blocking |\n \n ## SF \u2014 Spec Freeze Checklist\n \n@@ -98,7 +98,7 @@\n | H-02 | Branch/worktree target is present. | blocking |\n | H-03 | Scope and non-goals are present. | blocking |\n | H-04 | Dependencies and locks are present. | blocking |\n-| H-05 | Relevant docs, ADRs, modules, and tests are referenced. | blocking |\n+| H-05 | Relevant docs, SADRs, target ADRs, modules, and tests are referenced. | blocking |\n | H-06 | Required skills are stated. | blocking |\n | H-07 | TDD expectations are stated. | blocking when behavior work |\n | H-08 | Acceptance criteria are included. | blocking |\ndiff --git a/docs/agents/codex-handoff.md b/docs/agents/codex-handoff.md\nindex ede468f47cb277f69a857e8c45df703e4ed0151d..6f97bdffb67e44d7edea2c71e01ea28933c4eb6c\n--- a/docs/agents/codex-handoff.md\n+++ b/docs/agents/codex-handoff.md\n@@ -46,7 +46,7 @@\n \n ## Context\n - Domain terms:\n-- Relevant ADRs:\n+- Relevant SADRs and target ADRs:\n - Relevant docs:\n - Relevant modules/interfaces/seams:\n - Similar tests/prior art:\ndiff --git a/docs/agents/completion-check-agent.md b/docs/agents/completion-check-agent.md\nindex 13e4c035db35025f4e3cdcc5adfb24314ac4d521..61bfa1218cf36ca605250c91919e620f33238aa0\n--- a/docs/agents/completion-check-agent.md\n+++ b/docs/agents/completion-check-agent.md\n@@ -32,7 +32,7 @@\n - `AGENTS.md`\n - `CLAUDE.md`\n - `CONTEXT.md`\n-- relevant ADRs\n+- relevant SADRs and target ADRs\n - parent Goal issue\n - PRD issue or document\n - task issue\ndiff --git a/docs/agents/decision-control.md b/docs/agents/decision-control.md\nindex 7d8ee5ab03a5ccbcbd04056ae61de03785a2a9c9..8c94ecda00fffaa0e92c325ab83aa9776bd1dac1\n--- a/docs/agents/decision-control.md\n+++ b/docs/agents/decision-control.md\n@@ -11,7 +11,7 @@\n | Decision | Owner | When | Required inputs | Output | Enforcement surface |\n | --- | --- | --- | --- | --- | --- |\n | Is the Goal clear enough to plan? | Planner + Guardian when needed | Goal intake | User request, existing docs, risk signals | Goal readiness verdict | Goal checklist, issue template, ledger |\n-| Must `grill-with-docs` run? | Planner / Skill Gate | Before PRD or issue split | Ambiguity, domain terms, ADR conflicts, unclear boundaries | Skill invocation or explicit skip rationale | Skill Gate checklist |\n+| Must `grill-with-docs` run? | Planner / Skill Gate | Before PRD or issue split | Ambiguity, domain terms, SADR/target-ADR conflicts, unclear boundaries | Skill invocation or explicit skip rationale | Skill Gate checklist |\n | Is a PRD required? | Planner | After Goal context stabilizes | Goal, `grill-with-docs` output, Context & Impact output | PRD required / not required | PRD checklist, CCA evidence check |\n | Can the spec be frozen? | Operator only | End of Requirements Definition, before issue split | PRD/requirements summary, scope inventory (SF-02), open-question list | `spec_freeze` block in the plan + ledger evidence | `require_grilled_plan` (plan ingest/run), SF checklist |\n | Can a frozen spec change? | Operator only (Spec Amendment) | When implementation contradicts the frozen spec | Contested decisions, scoped re-grill result | recorded amendment + re-stamped freeze | SF checklist, ledger evidence |\ndiff --git a/docs/agents/domain.md b/docs/agents/domain.md\nindex 258da8ea81e87ac0a6b0b5b5e5c2fde980eaabb7..d57b0cf17ff3e1fa9e6e5885372bf083d8295421\n--- a/docs/agents/domain.md\n+++ b/docs/agents/domain.md\n@@ -5,7 +5,9 @@\n ## Layout\n \n - `CONTEXT.md` defines Shiki language. Keep it implementation-free.\n-- `docs/adr/` records hard-to-reverse decisions.\n+- `docs/adr/SADR-NNNN-*.md` records Shiki platform decisions.\n+- `docs/adr/NNNN-*.md` records target product decisions without sharing the\n+  SADR namespace.\n - `AGENTS.md` is the runtime-neutral agent constitution.\n - `CLAUDE.md` is the Claude Code wrapper around `AGENTS.md`.\n - `.shiki/` is the Target Repository mirror for Goals, PRDs, plans, Task DAGs, contracts, locks, worktree records, ledger entries, CCA verdicts, repair packets, reports, and handoffs.\n@@ -15,6 +17,6 @@\n \n - Use Shiki terms exactly when they exist.\n - Add glossary terms to `CONTEXT.md` only when the term is part of the domain, not a generic programming concept.\n-- Create ADRs only for hard-to-reverse, surprising, tradeoff-based decisions.\n+- Create SADRs or target ADRs only for hard-to-reverse, surprising, tradeoff-based decisions.\n - Keep product-specific language in the Target Repository, not in Shiki Template docs.\n-- If an ADR conflicts with a proposed implementation, surface the conflict before editing.\n+- If a SADR or target ADR conflicts with a proposed implementation, surface the conflict before editing.\ndiff --git a/docs/agents/execution-control.md b/docs/agents/execution-control.md\nindex ad8ee3cdb36be56fedc1f46408ca92b387d74047..bc63c3a73661d12b567016ba299f1a6b66bf9088\n--- a/docs/agents/execution-control.md\n+++ b/docs/agents/execution-control.md\n@@ -63,7 +63,7 @@\n | Branch name | Orchestrator | Orchestrator | Before implementer dispatch | branch policy | branch ref in task/ledger |\n | Lock reservation | Context & Impact | MergeGate | Before dispatch and before merge | `.shiki/locks/`, labels, PR metadata | lock id, owner, scope |\n | CI profile | Planner | MergeGate | During issue triage, rechecked at PR | labels, `.shiki/policy.yaml`, workflow matrix | required check list |\n-| Required status checks | Repo maintainer / Guardian | GitHub branch protection / rulesets | Repository setup, updated via ADR | GitHub settings | branch/ruleset config, ADR |\n+| Required status checks | Repo maintainer / Guardian | GitHub branch protection / rulesets | Repository setup, updated via target ADR | GitHub settings | branch/ruleset config, target ADR |\n | CCA judgment | CCA | GitHub required check / MergeGate | Every non-draft PR update | GitHub Action check | structured CCA verdict |\n | Repair assignment | CCA | MergeGate | After failed CCA/CI/review | repair packet, issue/PR comment | repair id and attempt count |\n | Merge readiness | MergeGate | GitHub branch protection / Guardian if needed | After CCA complete | required checks, review rules, rulesets | mergegate check output |\ndiff --git a/docs/agents/guardian-policy.md b/docs/agents/guardian-policy.md\nindex 4147c7adf05a47adb4fd24ada1a1c2e1f2ba6d2d..a20d9cc6c00a569bb057b9ca8d6b51c7fc4a9847\n--- a/docs/agents/guardian-policy.md\n+++ b/docs/agents/guardian-policy.md\n@@ -98,8 +98,8 @@\n `origin/<default_branch>` and reuses each job's existing base fetch; a shallow\n (`--depth`) or second base fetch would make the proof unresolvable and silently\n disable the carry, so neither is permitted. CCA reads `guardian_comment_carried` as\n-a recorded authority in the deterministic signal and does not re-derive it. See ADR\n-0018 for the full decision, the rejected diff/patch-equality and path-disjointness\n+a recorded authority in the deterministic signal and does not re-derive it. See\n+SADR-0018 for the full decision, the rejected diff/patch-equality and path-disjointness\n alternatives, and the accepted residual.\n \n ## External AI Guardian Review (SADR-0010)\ndiff --git a/docs/agents/implementation-policy.md b/docs/agents/implementation-policy.md\nindex c754144a9fb5bf4a38aa82a60e1670c59e626b44..85c37281bcad196c6b7b02343faf00b03cd9c57a\n--- a/docs/agents/implementation-policy.md\n+++ b/docs/agents/implementation-policy.md\n@@ -31,10 +31,13 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n ## Non-Negotiables\n \n 1. **Planning precedes implementation.** Non-trivial Goals start with `grill-with-docs` and Context & Impact.\n-2. **Domain language is controlled.** Terms must be checked against `CONTEXT.md`; hard-to-reverse tradeoffs must be checked against ADRs.\n+2. **Domain language is controlled.** Terms must be checked against `CONTEXT.md`; hard-to-reverse tradeoffs must be checked against Shiki SADRs and target ADRs.\n 3. **PRD precedes issue decomposition.** `to-prd` is used once the context is settled enough to become durable intent.\n 4. **Issues are vertical slices.** `to-issues` must produce independently grabbable tracer-bullet tasks, not horizontal layer tickets.\n 5. **Triage controls dispatch.** Only `ready-for-agent` / AFK tasks may be assigned to Codex without human input.\n@@ -95,19 +98,19 @@\n Process:\n \n - Challenge terms against `CONTEXT.md`.\n-- Check ADRs before reopening decisions.\n+- Check SADRs and target ADRs before reopening decisions.\n - Inspect code when code can answer a question.\n - Ask one question at a time when the operator is available.\n - Provide a recommended answer with each question.\n - Record resolved terms in `CONTEXT.md`.\n-- Create ADRs only for hard-to-reverse, surprising, tradeoff-based decisions.\n+- Create SADRs or target ADRs only for hard-to-reverse, surprising, tradeoff-based decisions.\n - Convert unresolved load-bearing questions into HITL blockers.\n \n Outputs:\n \n - Resolved design-tree decisions.\n - Updated glossary terms when needed.\n-- ADRs when justified.\n+- SADRs or target ADRs when justified.\n - HITL blockers when decisions remain unresolved.\n \n ## Phase 3 \u2014 Context & Impact\n@@ -117,7 +120,7 @@\n \n Outputs must include:\n \n-- Relevant documents and ADRs.\n+- Relevant documents, SADRs, and target ADRs.\n - Relevant modules, interfaces, seams, callers, and tests.\n - Candidate locks.\n - Dependencies.\n@@ -140,7 +143,7 @@\n - Testing decisions.\n - Out of scope.\n - Further notes.\n-- Links to relevant Goal, domain terms, and ADRs.\n+- Links to relevant Goal, domain terms, SADRs, and target ADRs.\n \n Phase 4 ends with Spec Freeze: the operator explicitly approves the PRD, the\n plan records a `spec_freeze` block (status, approved_by, source), and the SF\n@@ -240,7 +243,7 @@\n - Dependencies and locks.\n - Risk and Guardian requirements.\n - Ledger completeness.\n-- Documentation and ADR updates.\n+- Documentation and SADR/target-ADR updates.\n - Scope drift.\n \n CCA emits one of:\n@@ -301,7 +304,7 @@\n | Evidence failure | Behavior might be done, but proof is missing. | Add checks, PR evidence, ledger entry, or artifacts. |\n | Scope drift | PR changes unrelated behavior. | Revert unrelated changes or split into new Goal. |\n | Dependency block | Upstream task or lock not complete. | Block until dependency resolves. |\n-| Architecture block | Decision needs ADR or deep-module discussion. | `grill-with-docs` or `improve-codebase-architecture`. |\n+| Architecture block | Decision needs a SADR, target ADR, or deep-module discussion. | `grill-with-docs` or `improve-codebase-architecture`. |\n | Risk block | Security/production/policy change needs human approval. | Guardian decision. |\n \n ## Phase 5.5 \u2014 Execution Decision Control\ndiff --git a/docs/agents/runtime-auth-model.md b/docs/agents/runtime-auth-model.md\nindex 6a2e1a47e81ea89b5c68a210ec07772a9374babc..3c54748d24233dbd0ba8473950282547ef4ce935\n--- a/docs/agents/runtime-auth-model.md\n+++ b/docs/agents/runtime-auth-model.md\n@@ -148,7 +148,7 @@\n \n API-key based runners may be added by a target repository as an explicit extension. They are not the default Shiki template path.\n \n-If a repo opts into API-key mode, record the exception in an ADR and update the repo's `AGENTS.md`, `.shiki/config.yaml`, and workflow permissions.\n+If a repo opts into API-key mode, record the target-specific exception in a target ADR and update the repo's `AGENTS.md`, `.shiki/config.yaml`, and workflow permissions.\n \n ## GitHub Actions Runtime Compatibility\n \ndiff --git a/docs/agents/skill-gate.md b/docs/agents/skill-gate.md\nindex c50398b0d7c60a8598f2a5c88a10015730dced13..3acffe6444617bb1d54b77d4b4708158ec735bc6\n--- a/docs/agents/skill-gate.md\n+++ b/docs/agents/skill-gate.md\n@@ -9,7 +9,7 @@\n | Skill | Trigger | Expected output |\n | --- | --- | --- |\n | `setup-matt-pocock-skills` | First configuring a repo for these skills, or when issue tracker, triage labels, or domain docs are missing. | `docs/agents/` configuration and an `Agent skills` block in `AGENTS.md` or `CLAUDE.md`. |\n-| `grill-with-docs` | Ambiguous plan, unclear terminology, boundary decision, tradeoff, or ADR-worthy design choice. | Clarified decision tree, recommended answers, updates to `CONTEXT.md` or ADRs when decisions crystallize. |\n+| `grill-with-docs` | Ambiguous plan, unclear terminology, boundary decision, tradeoff, or decision-record-worthy design choice. | Clarified decision tree, recommended answers, updates to `CONTEXT.md`, SADRs, or target ADRs when decisions crystallize. |\n | `zoom-out` | Agent lacks a higher-level architectural map. | Map of relevant modules, callers, seams, and domain concepts. |\n | `to-prd` | Settled context should become a PRD. | PRD published to the issue tracker with problem, solution, user stories, decisions, testing, and out-of-scope notes. |\n | `to-issues` | Goal, PRD, or plan must become implementation tickets. | Vertical-slice issues with acceptance criteria, dependencies, and AFK/HITL classification. |\n@@ -17,7 +17,7 @@\n | `tdd` | Feature work or bug fix has observable behavior that can be specified with tests. | Red-green-refactor loop through public interfaces, one vertical slice at a time. |\n | `code-review` | The TDD loop is green and the implementer is about to create or update a PR. Default for every implementation task. | Pre-PR self-review of the task diff with fixes applied on the task branch; findings and resolutions recorded as a ledger entry naming the skill and a `## Pre-PR code review` PR body section. |\n | `diagnose` | Hard bug, failing check, regression, flaky behavior, or performance problem. | Reproduction loop, ranked hypotheses, instrumentation, fix, regression test, post-mortem. |\n-| `improve-codebase-architecture` | Structural friction, testability, AI-navigability, deep-module opportunity, or refactor request. | Architecture candidates and deepening recommendations informed by `CONTEXT.md` and ADRs. |\n+| `improve-codebase-architecture` | Structural friction, testability, AI-navigability, deep-module opportunity, or refactor request. | Architecture candidates and deepening recommendations informed by `CONTEXT.md`, SADRs, and target ADRs. |\n | `prototype` | A throwaway logic or UI prototype is needed to answer a design question. | Clearly marked disposable prototype and captured decision. |\n \n ## MergeGate Interaction\ndiff --git a/scripts/guardian_approval_signal.py b/scripts/guardian_approval_signal.py\nindex d059f92b2c82aa2b5d6ca421365537a87886c1cb..16924892714f4f5b9303c0ef0e325a7e70075630\n--- a/scripts/guardian_approval_signal.py\n+++ b/scripts/guardian_approval_signal.py\n@@ -199,8 +199,8 @@\n ) -> Any:\n     \"\"\"Evaluate SADR-0015 Contract Approval for the CCA Guardian signal.\n \n-    The SAME OR that MergeGate applies must reach this deterministic signal: ADR\n-    0015 is explicit that without it, a contract-approved PR still returns\n+    The SAME OR that MergeGate applies must reach this deterministic signal:\n+    SADR-0015 is explicit that without it, a contract-approved PR still returns\n     ``needs_guardian``, the CCA job fails, and the MergeGate policy check never\n     runs \u2014 the gate this change moves would never be reached. This resolves the\n     task the SAME way MergeGate does (the first Shiki task id in the PR body),\ndiff --git a/scripts/shiki_contract_approval.py b/scripts/shiki_contract_approval.py\nindex 941bcb96fdd8b9a85bc0c4f0f04d5dad9d04839c..4a4b418f6527413161db82670bec857a043360e2\n--- a/scripts/shiki_contract_approval.py\n+++ b/scripts/shiki_contract_approval.py\n@@ -142,7 +142,7 @@\n     changed_files_status: Any,\n     registration: Any,\n ) -> ContractApprovalResult:\n-    \"\"\"Whether this PR carries an SADR-0015 Contract Approval.\n+    \"\"\"Whether this PR carries a SADR-0015 Contract Approval.\n \n     The four conditions are evaluated IN ORDER, each failure recorded as a\n     distinct reason string:\ndiff --git a/scripts/shiki_guardian.py b/scripts/shiki_guardian.py\nindex a4ecb79bbda55fd5c8b06adfae2eb99760a03c45..4dd99bbe714e26f421fa19abac7a34ae1b195ecc\n--- a/scripts/shiki_guardian.py\n+++ b/scripts/shiki_guardian.py\n@@ -12,7 +12,7 @@\n # The carry re-derives its proof from git in-process on every judging invocation;\n # it is never read from an artifact, so there is nothing to go stale or be forged.\n # shiki_sync_proof is the ONLY guardian-family module that runs git, and every one\n-# of its invocations is read-only and local (ADR: prove-from-git base sync).\n+# of its invocations is read-only and local (SADR: prove-from-git base sync).\n from shiki_sync_proof import verify_pure_base_sync\n \n GUARDIAN_POLICY_PATH = \".shiki/guardian-policy.json\"\ndiff --git a/scripts/shiki_guardian_review.py b/scripts/shiki_guardian_review.py\nindex dd0f104d457899e0ba5ff1e162011c1e90b6f92b..e10ef58c046324e7ea3a3392feb3048ca77414ce\n--- a/scripts/shiki_guardian_review.py\n+++ b/scripts/shiki_guardian_review.py\n@@ -159,7 +159,7 @@\n     docs_only = _is_docs_only(changed_files)\n     if docs_only and matched == 0:\n         focus.append(\n-            \"Docs/ADR-only change: confirm the change is genuinely documentation/decision-only, introduces no behavior or contract drift, and stays consistent with the constitution and existing ADRs.\"\n+            \"Docs/decision-record-only change: confirm the change is genuinely documentation/decision-only, introduces no behavior or contract drift, and stays consistent with the constitution and existing SADRs and target ADRs.\"\n         )\n \n     # Conservative augmentation: unknown (no category and not docs-only), mixed\ndiff --git a/scripts/shiki_installer.py b/scripts/shiki_installer.py\nindex be064ab7a29ff6836f7f7a1d70500a43e0d11d7b..9fd8eefd137ce7003dad5c0dcb2e48e2cd47ab32\n--- a/scripts/shiki_installer.py\n+++ b/scripts/shiki_installer.py\n@@ -118,6 +118,7 @@\n # what lets a target carry it without a governance-schema change.\n INSTALL_STAMP_PATH = \".shiki/install-stamp.json\"\n INSTALL_STAMP_VERSION = 1\n+LEGACY_SHIKI_SADR_MAX = 18\n \n # Under --force the shipped surface splits three ways, so nothing is lost and\n # nothing is silently stale.\n@@ -237,9 +238,98 @@\n         )\n \n \n+def legacy_shiki_adr_paths() -> tuple[str, ...]:\n+    \"\"\"Former numeric paths for Shiki SADR-0001 through SADR-0018.\n+\n+    The authoritative slugs come from the current SADR filenames. SADR-0019 is\n+    the namespace decision itself and never shipped under a numeric legacy path.\n+    \"\"\"\n+    paths: list[str] = []\n+    for sadr in sorted((ROOT / \"docs\" / \"adr\").glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\")):\n+        number = sadr.name.split(\"-\", 2)[1]\n+        if int(number) <= LEGACY_SHIKI_SADR_MAX:\n+            paths.append(f\"docs/adr/{sadr.name.removeprefix('SADR-')}\")\n+    return tuple(paths)\n+\n+\n+def preflight_legacy_sadr_cleanup(target: Path, *, force: bool) -> tuple[str, ...]:\n+    \"\"\"Authorize exact legacy Shiki ADR removals before the first write.\n+\n+    A non-force install never removes existing files. A forced install needs no\n+    stamp when no legacy Shiki path is present. When legacy paths do exist, each\n+    must be named in a readable install stamp and its current bytes must match\n+    the recorded digest.\n+    \"\"\"\n+    if not force:\n+        return ()\n+    present = tuple(\n+        relative\n+        for relative in legacy_shiki_adr_paths()\n+        if (target / relative).exists() or (target / relative).is_symlink()\n+    )\n+    if not present:\n+        return ()\n+\n+    stamp_path = target / INSTALL_STAMP_PATH\n+    try:\n+        raw = stamp_path.read_text(encoding=\"utf-8\")\n+        stamp = json.loads(raw)\n+    except FileNotFoundError:\n+        detail = \"install stamp is absent\"\n+        blockers = [f\"{relative}: {detail}\" for relative in present]\n+    except (OSError, UnicodeDecodeError) as error:\n+        detail = f\"install stamp is unreadable ({error})\"\n+        blockers = [f\"{relative}: {detail}\" for relative in present]\n+    except json.JSONDecodeError as error:\n+        detail = f\"install stamp is malformed ({error})\"\n+        blockers = [f\"{relative}: {detail}\" for relative in present]\n+    else:\n+        if not isinstance(stamp, dict):\n+            blockers = [\n+                f\"{relative}: install stamp is malformed (expected an object)\"\n+                for relative in present\n+            ]\n+        else:\n+            digests = stamp.get(\"digests\")\n+            blockers = []\n+            if not isinstance(digests, dict):\n+                blockers.extend(\n+                    f\"{relative}: install stamp has no digest for this path\"\n+                    for relative in present\n+                )\n+            else:\n+                for relative in present:\n+                    expected = digests.get(relative)\n+                    if not isinstance(expected, str) or not expected:\n+                        blockers.append(\n+                            f\"{relative}: install stamp has no digest for this path\"\n+                        )\n+                        continue\n+                    try:\n+                        actual = _sha256_file(target / relative)\n+                    except OSError as error:\n+                        blockers.append(\n+                            f\"{relative}: legacy path is unreadable ({error})\"\n+                        )\n+                        continue\n+                    if actual != expected:\n+                        blockers.append(\n+                            f\"{relative}: digest mismatch (stamp {expected}, current {actual})\"\n+                        )\n+\n+    if blockers:\n+        joined = \"\\n\".join(f\"- {blocker}\" for blocker in blockers)\n+        raise ShikiError(\n+            \"legacy Shiki ADR cleanup is not authorized:\\n\"\n+            f\"{joined}\\nNo files were written.\"\n+        )\n+    return present\n+\n+\n def install_template(target: Path, *, force: bool, validate: bool) -> list[NewFileNote]:\n     # Preconditions first: never rewrite the tree of a target that cannot be\n     # upgraded cleanly (see refuse_pending_migrations).\n+    legacy_cleanup = preflight_legacy_sadr_cleanup(target, force=force)\n     refuse_pending_migrations(target)\n \n     # Resolve the target's required CODEOWNERS owner once from its\n@@ -250,6 +340,10 @@\n     # verbatim. See ``codeowners_required_owner``.\n     codeowners_owner = codeowners_required_owner(target)\n \n+    for relative in legacy_cleanup:\n+        (target / relative).unlink()\n+        info(f\"removed stamped legacy Shiki ADR: {target / relative}\")\n+\n     notes: list[NewFileNote] = []\n     for relative in TEMPLATE_PATHS:\n         source = ROOT / relative\n@@ -647,6 +741,22 @@\n     digests = target_stamp_digests(target)\n     existing = read_install_stamp(target)\n     if existing is not None:\n+        # A non-force install deliberately leaves legacy Shiki ADRs in place.\n+        # Preserve only still-matching ownership proof so a later forced\n+        # upgrade can authorize cleanup; never carry a missing or stale digest.\n+        existing_digests = existing.get(\"digests\")\n+        if isinstance(existing_digests, dict):\n+            for relative in legacy_shiki_adr_paths():\n+                expected = existing_digests.get(relative)\n+                path = target / relative\n+                if not isinstance(expected, str) or not path.is_file():\n+                    continue\n+                try:\n+                    actual = _sha256_file(path)\n+                except OSError:\n+                    continue\n+                if actual == expected:\n+                    digests[relative] = actual\n         if _incoming_is_older(commit, existing.get(\"platform_commit\")):\n             warn(\n                 \"kept existing install stamp: incoming install \"\ndiff --git a/scripts/shiki_loop.py b/scripts/shiki_loop.py\nindex 3611641da8aeb5038b1da90e3114b09a473ec6d8..84caa491b9cad0edc4176f209c13e9221e975681\n--- a/scripts/shiki_loop.py\n+++ b/scripts/shiki_loop.py\n@@ -424,7 +424,7 @@\n         # unresolvable CCA verdict has already produced its terminal stop and is never\n         # laundered into a head-moving sync (SADR-0010). It also never fires while the\n         # policy (Guardian) gate is red \u2014 for ANY reason, not only when it is the sole\n-        # red check \u2014 so a head-bound Guardian approval, or an SADR-0015\n+        # red check \u2014 so a head-bound Guardian approval, or a SADR-0015\n         # contract-approval carry that base movement defeated (the policy gate compares\n         # base and head task governance directly), is never silently restored by\n         # adopting base wholesale. It is bounded by a durable per-task counter so the\ndiff --git a/scripts/shiki_tasks.py b/scripts/shiki_tasks.py\nindex aec3809f24b4a0c8c96fc50971cff9ae9ad30381..d531052e9d9717058d4631483cefab44c26a65c1\n--- a/scripts/shiki_tasks.py\n+++ b/scripts/shiki_tasks.py\n@@ -821,7 +821,7 @@\n         \"prompt\": prompt,\n         \"entry_skill\": \"grill-with-docs\",\n         \"required_next_steps\": [\n-            \"Run grill-with-docs until terminology, boundaries, risks, and ADR-worthy decisions are settled.\",\n+            \"Run grill-with-docs until terminology, boundaries, risks, and decision-record-worthy choices are settled.\",\n             \"Obtain the operator's explicit approval of the PRD/requirements (Spec Freeze).\",\n             \"Write a machine-readable plan JSON with grill_with_docs.status=complete and spec_freeze.status=frozen.\",\n             \"Run shiki plan ingest --plan-file PLAN.json.\",\ndiff --git a/scripts/test_shiki_goal_loop.sh b/scripts/test_shiki_goal_loop.sh\nindex 753c7b516e10f0be3bc92a4bd0eb1fa699776ae6..8f3820a0a079b3044424cfe1a6c6b7fe68896e67\n--- a/scripts/test_shiki_goal_loop.sh\n+++ b/scripts/test_shiki_goal_loop.sh\n@@ -231,8 +231,8 @@\n \n # High/critical risk merges autonomously when all required checks are green:\n # the \"MergeGate policy check\" required check IS the Guardian gate (it enforces\n-# guardian-policy.json \u2014 human approval OR external AI guardian review, ADR\n-# 0010), so green checks mean a recorded authority approved. The fake gh returns\n+# guardian-policy.json \u2014 human approval OR external AI guardian review,\n+# SADR-0010), so green checks mean a recorded authority approved. The fake gh returns\n # green checks, simulating that the Guardian gate passed.\n python3 \"$ROOT/scripts/shiki.py\" goal create --target \"$TARGET\" --title \"Guardian gate\" --outcome \"High risk merges via policy gate\" >/tmp/shiki-goal-loop-guardian-goal.json\n GGOAL=\"$(json_get /tmp/shiki-goal-loop-guardian-goal.json goal_id)\"\ndiff --git a/tests/test_adr_references.py b/tests/test_adr_references.py\nindex 51706329c797d5dd9b47e082a333fa83b261d482..db6e693e35f67103935be34f1d9bb4ff7ee60395\n--- a/tests/test_adr_references.py\n+++ b/tests/test_adr_references.py\n@@ -1,7 +1,7 @@\n-\"\"\"Every ``ADR ####`` citation must resolve to exactly one decision record.\n+\"\"\"Every target ADR and Shiki SADR citation resolves in its own namespace.\n \n Background. ``scripts/mergegate_check.py`` justifies ``is_bookkeeping_closeout``\n-with \"ADR 0017\" nine times and ``scripts/guardian_approval_signal.py`` cites\n+with \"SADR-0017\" nine times and ``scripts/guardian_approval_signal.py`` cites\n 0016/0017 seven times, yet for a stretch the numbering in ``docs/adr/`` stopped\n at 0015: the authoring commits lived on a coordinator branch and no task ever\n declared ``docs/adr/...`` in its locks, so nothing carried the records to the\n@@ -11,11 +11,12 @@\n and the directory drifted with no signal.\n \n This is the same unbound-pair shape behind other silent failures: a reference\n-and its target with nothing asserting they agree. This test binds them. It scans\n-every ``scripts/*.py`` (plus ``docs/**/*.md`` and the root ``*.md`` governance\n-files, which are cheap) for the ``ADR ####`` pattern and asserts each cited\n-number resolves to exactly one ``docs/adr/####-*.md``. The converse is\n-deliberately NOT required: an ADR may exist without being cited.\n+and its target with nothing asserting they agree. This test binds them. Target\n+``ADR NNNN`` citations resolve only to ``docs/adr/NNNN-*.md``; Shiki\n+``SADR-NNNN`` citations resolve only to ``docs/adr/SADR-NNNN-*.md``. The scan\n+covers current governance docs, scripts, workflows, prompts, packaging metadata,\n+and repo-local Codex guidance while deliberately excluding historical\n+``.shiki`` evidence. The converse is not required: a record may be uncited.\n \n The suite is dependency free: standard-library ``unittest`` only.\n \"\"\"\n@@ -31,17 +32,18 @@\n \n REPO_ROOT = Path(__file__).resolve().parents[1]\n \n-# \"ADR 0016\" -- a four-digit citation. The trailing \\b keeps a five-digit token\n-# from being read as a truncated four-digit number.\n-_CITATION = re.compile(r\"\\bADR (\\d{4})\\b\")\n-# \"0016-locks-bound-product-paths.md\" -- the leading four digits name the record.\n-_ADR_FILENAME = re.compile(r\"^(\\d{4})-.*\\.md$\")\n+# ``ADR 0016`` / ``SADR-0016`` -- a namespace plus four-digit citation. The\n+# trailing ``\\b`` keeps a five-digit token from being read as a truncated one.\n+_CITATION = re.compile(r\"\\b(S?ADR)[ -](\\d{4})\\b\")\n+# Target ADRs retain the numeric filename; Shiki platform decisions use SADR.\n+_DECISION_FILENAME = re.compile(r\"^(?:(SADR)-)?(\\d{4})-.*\\.md$\")\n \n \n class Violation(NamedTuple):\n     \"\"\"A citation that does not resolve to exactly one decision record.\"\"\"\n \n     citing_file: str  # repo-relative path of the file that cites the ADR\n+    namespace: str  # \"ADR\" for target decisions; \"SADR\" for Shiki decisions\n     number: str  # the cited four-digit number, e.g. \"0016\"\n     line: int  # 1-based line of the citation\n     reason: str  # \"missing\" (zero matches) or \"ambiguous\" (two or more)\n@@ -49,44 +51,59 @@\n \n     @property\n     def expected_path(self) -> str:\n-        return f\"docs/adr/{self.number}-*.md\"\n+        prefix = \"SADR-\" if self.namespace == \"SADR\" else \"\"\n+        return f\"docs/adr/{prefix}{self.number}-*.md\"\n \n     def describe(self) -> str:\n         \"\"\"Name the citing file, the cited number, and the missing path.\"\"\"\n         if self.reason == \"missing\":\n             return (\n-                f\"{self.citing_file}:{self.line} cites ADR {self.number} \"\n+                f\"{self.citing_file}:{self.line} cites {self.namespace} {self.number} \"\n                 f\"but no document matches {self.expected_path}\"\n             )\n         joined = \", \".join(self.matches)\n         return (\n-            f\"{self.citing_file}:{self.line} cites ADR {self.number} which \"\n+            f\"{self.citing_file}:{self.line} cites {self.namespace} {self.number} which \"\n             f\"resolves to {len(self.matches)} documents (expected exactly one \"\n             f\"at {self.expected_path}): {joined}\"\n         )\n \n \n-def _adr_index(root: Path) -> dict[str, list[Path]]:\n-    \"\"\"Map every four-digit ADR number to the record files that claim it.\"\"\"\n-    index: dict[str, list[Path]] = {}\n+def _adr_index(root: Path) -> dict[tuple[str, str], list[Path]]:\n+    \"\"\"Map each decision namespace and number to the files that claim it.\"\"\"\n+    index: dict[tuple[str, str], list[Path]] = {}\n     adr_dir = root / \"docs\" / \"adr\"\n     if adr_dir.is_dir():\n         for path in sorted(adr_dir.glob(\"*.md\")):\n-            match = _ADR_FILENAME.match(path.name)\n+            match = _DECISION_FILENAME.match(path.name)\n             if match:\n-                index.setdefault(match.group(1), []).append(path)\n+                namespace = \"SADR\" if match.group(1) else \"ADR\"\n+                index.setdefault((namespace, match.group(2)), []).append(path)\n     return index\n \n \n def _citing_files(root: Path, include_docs: bool = True) -> list[Path]:\n-    \"\"\"Files scanned for citations: scripts/*.py, and cheaply the markdown.\"\"\"\n+    \"\"\"Current, shipped text surfaces scanned for decision citations.\"\"\"\n     files = sorted((root / \"scripts\").glob(\"*.py\"))\n+    files += sorted((root / \"scripts\").glob(\"*.sh\"))\n     if include_docs:\n         docs = root / \"docs\"\n         if docs.is_dir():\n             files += sorted(docs.rglob(\"*.md\"))\n         files += sorted(root.glob(\"*.md\"))\n-    return files\n+        pyproject = root / \"pyproject.toml\"\n+        if pyproject.is_file():\n+            files.append(pyproject)\n+        for relative in (\".github\", \".codex\"):\n+            surface = root / relative\n+            if surface.is_dir():\n+                files += sorted(\n+                    path\n+                    for path in surface.rglob(\"*\")\n+                    if path.is_file()\n+                    and path.suffix in {\".md\", \".py\", \".sh\", \".toml\", \".yaml\", \".yml\"}\n+                )\n+    return sorted(set(files))\n \n \n def find_adr_reference_violations(\n@@ -94,10 +111,9 @@\n ) -> list[Violation]:\n     \"\"\"Return every ``ADR ####`` citation under ``root`` that fails to resolve.\n \n-    A citation resolves when exactly one ``docs/adr/####-*.md`` matches its\n-    number. Zero matches is a \"missing\" violation; two or more is \"ambiguous\".\n-    Records that are never cited produce nothing -- the converse is not\n-    required.\n+    A citation resolves when exactly one record in the same namespace matches\n+    its number. Zero matches is a \"missing\" violation; two or more is\n+    \"ambiguous\". Records that are never cited produce nothing.\n     \"\"\"\n     root = Path(root)\n     index = _adr_index(root)\n@@ -110,13 +126,15 @@\n         rel = path.relative_to(root).as_posix()\n         for lineno, line in enumerate(text.splitlines(), start=1):\n             for match in _CITATION.finditer(line):\n-                number = match.group(1)\n-                found = index.get(number, [])\n+                namespace, number = match.groups()\n+                found = index.get((namespace, number), [])\n                 if len(found) == 1:\n                     continue\n                 reason = \"missing\" if not found else \"ambiguous\"\n                 rels = [p.relative_to(root).as_posix() for p in found]\n-                violations.append(Violation(rel, number, lineno, reason, rels))\n+                violations.append(\n+                    Violation(rel, namespace, number, lineno, reason, rels)\n+                )\n     return violations\n \n \n@@ -129,8 +147,8 @@\n         self.assertEqual(\n             violations,\n             [],\n-            \"Every 'ADR ####' citation must resolve to exactly one \"\n-            f\"docs/adr/####-*.md:\\n{detail}\",\n+            \"Every ADR/SADR citation must resolve exactly once in its own \"\n+            f\"docs/adr namespace:\\n{detail}\",\n         )\n \n     def test_scan_actually_reads_scripts(self) -> None:\n@@ -146,8 +164,21 @@\n         # The two records this Goal carried onto the default branch, and which\n         # the scripts cite most, must be present.\n         index = _adr_index(REPO_ROOT)\n-        self.assertIn(\"0016\", index)\n-        self.assertIn(\"0017\", index)\n+        self.assertIn((\"SADR\", \"0016\"), index)\n+        self.assertIn((\"SADR\", \"0017\"), index)\n+\n+    def test_platform_tree_has_only_sadr_0001_through_0019(self) -> None:\n+        adr_dir = REPO_ROOT / \"docs\" / \"adr\"\n+        sadrs = sorted(adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\"))\n+        self.assertEqual(\n+            {path.name.split(\"-\", 2)[1] for path in sadrs},\n+            {f\"{number:04d}\" for number in range(1, 20)},\n+        )\n+        self.assertEqual(\n+            list(adr_dir.glob(\"[0-9][0-9][0-9][0-9]-*.md\")),\n+            [],\n+            \"the Shiki platform tree must not retain legacy numeric SADR paths\",\n+        )\n \n \n class FixtureAdrReferences(unittest.TestCase):\n@@ -180,6 +211,26 @@\n         self.assertIn(\"9991\", message)  # cited number\n         self.assertIn(\"docs/adr/9991-*.md\", message)  # missing path\n \n+    def test_target_adr_does_not_satisfy_missing_platform_sadr(self) -> None:\n+        root = self._make_repo(\n+            adr_names=[\"0042-target-decision.md\"],\n+            script_body='\"\"\"Target ADR 0042 exists, but platform SADR-0042 does not.\"\"\"\\n',\n+        )\n+        violations = find_adr_reference_violations(root, include_docs=False)\n+        self.assertEqual(len(violations), 1)\n+        self.assertEqual(violations[0].namespace, \"SADR\")\n+        self.assertEqual(violations[0].number, \"0042\")\n+        self.assertEqual(violations[0].expected_path, \"docs/adr/SADR-0042-*.md\")\n+\n+    def test_target_adr_and_platform_sadr_may_reuse_a_number(self) -> None:\n+        root = self._make_repo(\n+            adr_names=[\"0042-target-decision.md\", \"SADR-0042-platform-decision.md\"],\n+            script_body='\"\"\"Target ADR 0042 and platform SADR-0042 are distinct.\"\"\"\\n',\n+        )\n+        self.assertEqual(\n+            find_adr_reference_violations(root, include_docs=False), []\n+        )\n+\n     def test_uncited_adr_is_allowed(self) -> None:\n         # An ADR exists but nothing cites it -> not a violation. The converse\n         # (every ADR must be cited) is explicitly NOT required.\ndiff --git a/tests/test_guardian_review.py b/tests/test_guardian_review.py\nindex 88829e7810212635e2fc98aa5918d6f26294677d..00a85a237acd5bf2d6cb5b294128198c422402ca\n--- a/tests/test_guardian_review.py\n+++ b/tests/test_guardian_review.py\n@@ -162,7 +162,7 @@\n \n     def test_docs_only_is_light_and_not_conservative(self):\n         focus = gr.classify_review_focus([\"docs/adr/0099-x.md\", \"README.md\"], \"\", \"low\")\n-        self.assertTrue(any(\"Docs/ADR-only\" in f for f in focus))\n+        self.assertTrue(any(\"Docs/decision-record-only\" in f for f in focus))\n         self.assertFalse(any(\"conservative default\" in f.lower() for f in focus))\n \n     def test_unknown_category_is_conservative(self):\ndiff --git a/tests/test_policy_job_merged_prs.py b/tests/test_policy_job_merged_prs.py\nindex d45512e74681e5e649279236c7d4e79d5c3da471..b97989f8bd74d091de1a6b5275a20d8dc68c4512\n--- a/tests/test_policy_job_merged_prs.py\n+++ b/tests/test_policy_job_merged_prs.py\n@@ -3,8 +3,8 @@\n \n ``.github/workflows/shiki-cca-completion.yml`` has two consumers of the merged-PR\n proof: the CCA *signal* (``guardian_approval_signal.py`` in \"Prepare CCA context\")\n-and the authoritative gate (``mergegate_check.py`` in \"Enforce MergeGate\"). ADR\n-0017's bookkeeping-closeout exemption (condition 6) needs the merged implementation\n+and the authoritative gate (``mergegate_check.py`` in \"Enforce MergeGate\").\n+SADR-0017's bookkeeping-closeout exemption (condition 6) needs the merged implementation\n PR proven, and a closeout carries NO ``post_merge_reconcile`` marker. The signal\n already resolves ``MERGED_PRS`` unconditionally, but \"Enforce MergeGate\" used to\n resolve it only inside ``grep -q '<!-- shiki:post_merge_reconcile -->'``. So a\ndiff --git a/tests/test_sadr_install_migration.py b/tests/test_sadr_install_migration.py\nnew file mode 100644\nindex 0000000000000000000000000000000000000000..a6f4c57e4dcaef73ef600b0d4804744138ab9071\n--- /dev/null\n+++ b/tests/test_sadr_install_migration.py\n@@ -0,0 +1,229 @@\n+\"\"\"SADR installation and collision-safe legacy upgrade behavior.\"\"\"\n+\n+from __future__ import annotations\n+\n+import contextlib\n+import hashlib\n+import io\n+import json\n+import shutil\n+import tempfile\n+import unittest\n+from pathlib import Path\n+\n+import shiki_test_support  # noqa: F401  (path bootstrap)\n+\n+import shiki_installer\n+from tests.test_adr_references import find_adr_reference_violations\n+\n+\n+PRODUCT_NUMBERS = tuple(range(13, 19))\n+\n+\n+def _quiet_install(target: Path, *, force: bool, validate: bool) -> None:\n+    with contextlib.redirect_stdout(io.StringIO()):\n+        shiki_installer.install_template(target, force=force, validate=validate)\n+\n+\n+def _sha256(path: Path) -> str:\n+    return hashlib.sha256(path.read_bytes()).hexdigest()\n+\n+\n+def _legacy_sadr_paths() -> tuple[str, ...]:\n+    return tuple(\n+        f\"docs/adr/{path.name.removeprefix('SADR-')}\"\n+        for path in sorted((shiki_installer.ROOT / \"docs\" / \"adr\").glob(\"SADR-*.md\"))\n+        if path.name != \"SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\"\n+    )\n+\n+\n+def _create_legacy_upgrade_fixture(\n+    target: Path,\n+) -> tuple[dict[str, bytes], tuple[str, ...]]:\n+    adr_dir = target / \"docs\" / \"adr\"\n+    adr_dir.mkdir(parents=True)\n+    product_bytes = {\n+        f\"{number:04d}-cubeshare-product-decision.md\": (\n+            f\"# CubeShare ADR {number:04d}\\n\\nDo not rewrite {number}.\\n\"\n+        ).encode()\n+        for number in PRODUCT_NUMBERS\n+    }\n+    for name, content in product_bytes.items():\n+        (adr_dir / name).write_bytes(content)\n+\n+    legacy_paths = _legacy_sadr_paths()\n+    for relative in legacy_paths:\n+        (target / relative).write_bytes(f\"legacy Shiki bytes for {relative}\\n\".encode())\n+\n+    stamp_path = target / shiki_installer.INSTALL_STAMP_PATH\n+    stamp_path.parent.mkdir(parents=True, exist_ok=True)\n+    stamp = {\n+        \"version\": shiki_installer.INSTALL_STAMP_VERSION,\n+        \"platform_commit\": None,\n+        \"installed_at\": \"2000-01-01T00:00:00+00:00\",\n+        \"digests\": {\n+            relative: _sha256(target / relative) for relative in legacy_paths\n+        },\n+    }\n+    stamp_path.write_text(\n+        json.dumps(stamp, indent=2, sort_keys=True) + \"\\n\", encoding=\"utf-8\"\n+    )\n+    return product_bytes, legacy_paths\n+\n+\n+def _tree_snapshot(root: Path) -> dict[str, tuple[str, bytes | str]]:\n+    \"\"\"Recursive byte/shape snapshot used to prove preflight is write-free.\"\"\"\n+    snapshot: dict[str, tuple[str, bytes | str]] = {}\n+    for path in sorted(root.rglob(\"*\")):\n+        relative = path.relative_to(root).as_posix()\n+        if path.is_symlink():\n+            snapshot[relative] = (\"symlink\", path.readlink().as_posix())\n+        elif path.is_dir():\n+            snapshot[relative] = (\"directory\", \"\")\n+        else:\n+            snapshot[relative] = (\"file\", path.read_bytes())\n+    return snapshot\n+\n+\n+class FreshSadrInstallTests(unittest.TestCase):\n+    def setUp(self) -> None:\n+        self.tmp = Path(tempfile.mkdtemp(prefix=\"shiki-sadr-fresh-\"))\n+        self.addCleanup(lambda: shutil.rmtree(self.tmp, ignore_errors=True))\n+        self.target = self.tmp / \"target\"\n+        self.adr_dir = self.target / \"docs\" / \"adr\"\n+        self.adr_dir.mkdir(parents=True)\n+        self.product_bytes = {\n+            f\"{number:04d}-product-decision.md\": (\n+                f\"# Product ADR {number:04d}\\n\\nTarget-owned bytes {number}.\\n\"\n+            ).encode()\n+            for number in PRODUCT_NUMBERS\n+        }\n+        for name, content in self.product_bytes.items():\n+            (self.adr_dir / name).write_bytes(content)\n+\n+    def test_fresh_install_preserves_product_adrs_and_installs_all_sadrs(self) -> None:\n+        _quiet_install(self.target, force=False, validate=True)\n+\n+        for name, expected in self.product_bytes.items():\n+            self.assertEqual((self.adr_dir / name).read_bytes(), expected)\n+\n+        sadr_numbers = {\n+            path.name.split(\"-\", 2)[1]\n+            for path in self.adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\")\n+        }\n+        self.assertEqual(sadr_numbers, {f\"{number:04d}\" for number in range(1, 20)})\n+\n+        numeric_records = {\n+            path.name\n+            for path in self.adr_dir.glob(\"[0-9][0-9][0-9][0-9]-*.md\")\n+        }\n+        self.assertEqual(numeric_records, set(self.product_bytes))\n+        self.assertEqual(find_adr_reference_violations(self.target), [])\n+\n+\n+class LegacySadrUpgradeTests(unittest.TestCase):\n+    def setUp(self) -> None:\n+        self.tmp = Path(tempfile.mkdtemp(prefix=\"shiki-sadr-upgrade-\"))\n+        self.addCleanup(lambda: shutil.rmtree(self.tmp, ignore_errors=True))\n+        self.target = self.tmp / \"target\"\n+        self.adr_dir = self.target / \"docs\" / \"adr\"\n+        self.product_bytes, self.legacy_paths = _create_legacy_upgrade_fixture(\n+            self.target\n+        )\n+\n+    def test_force_upgrade_deletes_only_stamped_legacy_sadrs(self) -> None:\n+        _quiet_install(self.target, force=True, validate=True)\n+\n+        for relative in self.legacy_paths:\n+            self.assertFalse((self.target / relative).exists(), relative)\n+        for name, expected in self.product_bytes.items():\n+            self.assertEqual((self.adr_dir / name).read_bytes(), expected)\n+\n+        sadrs = list(self.adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\"))\n+        self.assertEqual(len(sadrs), 19)\n+        refreshed = json.loads(\n+            (self.target / shiki_installer.INSTALL_STAMP_PATH).read_text(encoding=\"utf-8\")\n+        )\n+        for relative in self.legacy_paths:\n+            self.assertNotIn(relative, refreshed[\"digests\"])\n+        for path in sadrs:\n+            relative = path.relative_to(self.target).as_posix()\n+            self.assertEqual(refreshed[\"digests\"][relative], _sha256(path))\n+\n+    def test_non_force_install_never_deletes_legacy_files(self) -> None:\n+        before = {\n+            relative: (self.target / relative).read_bytes()\n+            for relative in self.legacy_paths\n+        }\n+        _quiet_install(self.target, force=False, validate=False)\n+        for relative, expected in before.items():\n+            self.assertEqual((self.target / relative).read_bytes(), expected)\n+        refreshed = json.loads(\n+            (self.target / shiki_installer.INSTALL_STAMP_PATH).read_text(encoding=\"utf-8\")\n+        )\n+        for relative in self.legacy_paths:\n+            self.assertEqual(refreshed[\"digests\"][relative], _sha256(self.target / relative))\n+\n+\n+class LegacyCleanupPreflightTests(unittest.TestCase):\n+    def setUp(self) -> None:\n+        self.tmp = Path(tempfile.mkdtemp(prefix=\"shiki-sadr-preflight-\"))\n+        self.addCleanup(lambda: shutil.rmtree(self.tmp, ignore_errors=True))\n+\n+    def test_all_ownership_blockers_abort_before_any_write(self) -> None:\n+        cases = (\n+            (\"absent\", \"install stamp is absent\"),\n+            (\"unreadable\", \"install stamp is unreadable\"),\n+            (\"malformed\", \"install stamp is malformed\"),\n+            (\"missing-digest\", \"install stamp has no digest\"),\n+            (\"digest-mismatch\", \"digest mismatch\"),\n+        )\n+        for name, expected_reason in cases:\n+            with self.subTest(name=name):\n+                target = self.tmp / name\n+                _, legacy_paths = _create_legacy_upgrade_fixture(target)\n+                stamp_path = target / shiki_installer.INSTALL_STAMP_PATH\n+\n+                if name == \"absent\":\n+                    stamp_path.unlink()\n+                elif name == \"unreadable\":\n+                    stamp_path.unlink()\n+                    stamp_path.mkdir()\n+                elif name == \"malformed\":\n+                    stamp_path.write_text(\"{not-json\\n\", encoding=\"utf-8\")\n+                else:\n+                    stamp = json.loads(stamp_path.read_text(encoding=\"utf-8\"))\n+                    if name == \"missing-digest\":\n+                        stamp[\"digests\"] = {}\n+                    else:\n+                        stamp[\"digests\"] = {\n+                            relative: \"deadbeef\" for relative in legacy_paths\n+                        }\n+                    stamp_path.write_text(\n+                        json.dumps(stamp, indent=2, sort_keys=True) + \"\\n\",\n+                        encoding=\"utf-8\",\n+                    )\n+\n+                before = _tree_snapshot(target)\n+                with self.assertRaises(shiki_installer.ShikiError) as raised:\n+                    _quiet_install(target, force=True, validate=False)\n+                after = _tree_snapshot(target)\n+\n+                self.assertEqual(before, after, f\"{name} preflight changed the tree\")\n+                message = str(raised.exception)\n+                self.assertIn(expected_reason, message)\n+                self.assertIn(\"No files were written\", message)\n+                for relative in legacy_paths:\n+                    self.assertIn(relative, message)\n+\n+    def test_absent_stamp_is_allowed_when_no_legacy_path_exists(self) -> None:\n+        target = self.tmp / \"no-legacy\"\n+        target.mkdir()\n+        _quiet_install(target, force=True, validate=False)\n+        self.assertTrue(\n+            (target / \"docs\" / \"adr\" / \"SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\").is_file()\n+        )\n+\n+\n+if __name__ == \"__main__\":\n+    unittest.main()\n\nexec\n/bin/zsh -lc \"python3 -m unittest discover -s tests -p 'test_sadr_install_migration.py' -v\" in /private/tmp/shiki-adr-collision.pa0KwY/.worktrees/shiki-t-20260807t073307221254z-2d22d654-introduc\ndiff --git a/.codex/skills/shiki/SKILL.md b/.codex/skills/shiki/SKILL.md\nindex c8e2e85d9d75d49cc4f2d6286ee22659c4e53e75..93cded623b2c15d9f65c76491a1c2d813ac9e6c0\n--- a/.codex/skills/shiki/SKILL.md\n+++ b/.codex/skills/shiki/SKILL.md\n@@ -18,6 +18,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n Codex CLI does not register this skill as a custom `/shiki` slash command. When\n the user types `/shiki` in Codex CLI and gets \"Unrecognized command\", explain\n that this is expected: invoke Shiki in Codex with natural language such as\n@@ -69,7 +72,7 @@\n \n ## Responsibilities\n \n-- Claude Code implements and repairs by default (`shiki runner claude`, ADR 0008); Codex implements and repairs tasks explicitly assigned to `codex`.\n+- Claude Code implements and repairs by default (`shiki runner claude`, SADR-0008); Codex implements and repairs tasks explicitly assigned to `codex`.\n - Claude Code Action can act as GitHub-side CCA or reviewer.\n - CCA judges completion.\n - MergeGate authorizes state transitions and merge readiness.\ndiff --git a/.github/prompts/cca-completion-check.md b/.github/prompts/cca-completion-check.md\nindex ed203ad5f302ecc81372d8ab27d2b1232ca97497..fd89950528333ef6242e6d5443904f5392ab7e56\n--- a/.github/prompts/cca-completion-check.md\n+++ b/.github/prompts/cca-completion-check.md\n@@ -19,6 +19,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n ## Required Reading\n \n Read only the files needed to judge the PR contract. Prefer this order:\ndiff --git a/.github/prompts/codex-task-handoff.md b/.github/prompts/codex-task-handoff.md\nindex 695e211d76dea1c60e8d5bb360e78487808286a4..59441d576b01a39569722f226c854e5a740e9eb5\n--- a/.github/prompts/codex-task-handoff.md\n+++ b/.github/prompts/codex-task-handoff.md\n@@ -2,7 +2,7 @@\n \n You are Codex Front acting as the Implementer for a Shiki task.\n \n-Follow `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant ADRs, and `docs/agents/codex-handoff.md`.\n+Follow `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant Shiki SADRs and target ADRs, and `docs/agents/codex-handoff.md`.\n \n Rules:\n \ndiff --git a/AGENTS.md b/AGENTS.md\nindex 7f041100d49c2a7525f51cb0f5d696a4a6d1c78c..a06bfbabc6fa48a007389e25e12d7ed523936bed\n--- a/AGENTS.md\n+++ b/AGENTS.md\n@@ -22,7 +22,7 @@\n - **Codex Front** is an optional implementer through Codex App, Codex CLI, Codex IDE extension, or Codex Web signed in with ChatGPT OAuth/subscription auth, dispatched with `shiki runner codex` when a task is explicitly assigned to `codex`.\n - **GitHub CCA** is implemented by Claude Code Action by default, using `CLAUDE_CODE_OAUTH_TOKEN`.\n \n-Do not assume `openai/codex-action`, `OPENAI_API_KEY`, or API-key based Codex automation in the default Shiki path. API-key runners are explicit target-repo extensions and require their own ADR.\n+Do not assume `openai/codex-action`, `OPENAI_API_KEY`, or API-key based Codex automation in the default Shiki path. API-key runners are explicit target-repo extensions and require their own target ADR.\n \n Shiki is not:\n \n@@ -44,10 +44,14 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use\n+`SADR-NNNN-*.md`; target product decisions retain `NNNN-*.md` and the `ADR`\n+namespace.\n+\n Use the canonical source-of-truth order above. Runtime-specific reading orders\n are operational guidance only and must not override or reorder it.\n \n-Conversation state is not durable truth. If a decision matters, put it in GitHub, `.shiki/`, `CONTEXT.md`, or an ADR.\n+Conversation state is not durable truth. If a decision matters, put it in GitHub, `.shiki/`, `CONTEXT.md`, a Shiki SADR, or a target ADR.\n \n If GitHub and `.shiki/` disagree, surface the conflict and prefer GitHub operational state until the mirror is repaired.\n \n@@ -75,8 +79,8 @@\n Every non-trivial change follows this loop:\n \n 1. **Goal**: clarify outcome, completion conditions, non-goals, risk level, and success signals.\n-2. **grill-with-docs**: challenge the plan against domain docs, ADRs, code reality, terminology, and edge scenarios.\n-3. **Context & Impact**: find relevant docs, ADRs, code, dependencies, owners, locks, and verification surfaces.\n+2. **grill-with-docs**: challenge the plan against domain docs, Shiki SADRs, target ADRs, code reality, terminology, and edge scenarios.\n+3. **Context & Impact**: find relevant docs, SADRs, target ADRs, code, dependencies, owners, locks, and verification surfaces.\n 4. **PRD and Spec Freeze**: use `to-prd` when context is settled enough to become durable product/engineering intent. The operator's explicit approval of the PRD is Spec Freeze: the plan records a `spec_freeze` block, and no plan runs without it. Steps 1-4 form Requirements Definition \u2014 one continuous operator dialogue. After Spec Freeze, scope changes require an operator-approved Spec Amendment; non-scope-moving interpretations go to the Assumption Log.\n 5. **Issues**: use `to-issues` to create independently grabbable vertical-slice issues. Prefer AFK slices over HITL slices where possible.\n 6. **Triage**: label issues for readiness, risk, runtime, skills, and MergeGate state.\n@@ -167,7 +171,7 @@\n - Testing decisions.\n - Out-of-scope items.\n - Further notes.\n-- Links to relevant domain terms and ADRs.\n+- Links to relevant domain terms, SADRs, and target ADRs.\n \n Every executable task must include:\n \n@@ -193,7 +197,7 @@\n \n Context & Impact must identify:\n \n-- Relevant documents, domain terms, ADRs, and past decisions.\n+- Relevant documents, domain terms, SADRs, target ADRs, and past decisions.\n - Relevant modules, interfaces, seams, callers, owners, and tests.\n - Dependency relationships between tasks.\n - Candidate locks and lock conflicts.\n@@ -210,7 +214,7 @@\n | Skill | Required when |\n | --- | --- |\n | `setup-matt-pocock-skills` | First configuring the repo for skills, or when issue tracker, triage labels, or domain docs are missing. |\n-| `grill-with-docs` | Default for non-trivial Goals before PRD/issues; always required when plans, terminology, boundaries, tradeoffs, or ADR-worthy decisions are ambiguous. |\n+| `grill-with-docs` | Default for non-trivial Goals before PRD/issues; always required when plans, terminology, boundaries, tradeoffs, or decision-record-worthy choices are ambiguous. |\n | `zoom-out` | The agent lacks an architectural map of the relevant code area. |\n | `to-prd` | Settled Goal context must become durable product/engineering intent. |\n | `to-issues` | A Goal, PRD, or plan must become independently grabbable vertical-slice issues. |\n@@ -348,7 +352,7 @@\n - Three or more new files.\n - Any high-risk or critical label.\n \n-When the architecture gate triggers, run `grill-with-docs` or `improve-codebase-architecture` as appropriate and record durable decisions in an ADR when needed.\n+When the architecture gate triggers, run `grill-with-docs` or `improve-codebase-architecture` as appropriate and record durable decisions in a Shiki SADR or target ADR as applicable.\n \n ## Engineering Discipline\n \ndiff --git a/CLAUDE.md b/CLAUDE.md\nindex 98a932e7841a632866fcd53b0bad0e3af3711d3d..98a31399404d6fb044b4fd493f4459ac87f439de\n--- a/CLAUDE.md\n+++ b/CLAUDE.md\n@@ -14,6 +14,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n ## Claude Role\n \n Default to planner, default implementer, reviewer, coordinator, documentation editor, CCA-style completion judge, and final-judgment assistant.\n@@ -57,7 +60,7 @@\n At the start of a Shiki session:\n \n 1. Read `AGENTS.md`.\n-2. Read `CONTEXT.md` and relevant ADRs.\n+2. Read `CONTEXT.md` and relevant Shiki SADRs and target ADRs.\n 3. Read active `.shiki/` Goal/task/ledger state when present.\n 4. Read active distilled rules from `.shiki/memories/` (status `distilled`, `active: true`, not revoked or superseded) for applicable guidance; the same rules are injected deterministically into task handoffs as the `## Distilled Rules` section (\u00a73.5).\n 5. Read the GitHub Issue or PR when the task is GitHub-backed.\n@@ -74,7 +77,7 @@\n - Define the Goal, completion conditions, non-goals, and risk level.\n - Treat `grill-with-docs` as the default planning entry point for non-trivial Goals.\n - Challenge domain terms against `CONTEXT.md`.\n-- Challenge hard-to-reverse tradeoffs against ADRs.\n+- Challenge hard-to-reverse tradeoffs against SADRs and target ADRs.\n - Explore code instead of asking when code can answer the question.\n - Run Context & Impact before implementation planning.\n - Produce a Task DAG, not an unordered checklist.\n@@ -94,7 +97,7 @@\n - For each question, provide your recommended answer.\n - Resolve dependency decisions before downstream design questions.\n - Update `CONTEXT.md` inline when terminology crystallizes.\n-- Offer ADRs only when the decision is hard to reverse, surprising without context, and a real tradeoff.\n+- Offer a SADR or target ADR only when the decision is hard to reverse, surprising without context, and a real tradeoff.\n - Convert unresolved questions into HITL blockers rather than allowing Codex to guess.\n \n ## PRD and Issues Standard\n@@ -103,7 +106,7 @@\n \n - Use the project's domain glossary vocabulary.\n - Avoid volatile file paths unless they are essential.\n-- Include implementation decisions, testing decisions, out-of-scope items, and relevant ADR/domain links.\n+- Include implementation decisions, testing decisions, out-of-scope items, and relevant SADR/target-ADR/domain links.\n - Identify deep-module opportunities and testable interfaces.\n \n When creating issues:\n@@ -121,7 +124,7 @@\n - Branch/worktree target.\n - Scope and non-goals.\n - Dependencies and locks.\n-- Relevant docs, ADRs, modules, and tests.\n+- Relevant docs, SADRs, target ADRs, modules, and tests.\n - Required skill invocations.\n - TDD expectations.\n - Pre-PR `code-review` gate expectations.\n@@ -152,7 +155,7 @@\n \n - Treat yourself as a judge, not implementer.\n - Do not write production code.\n-- Read `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant ADRs, PRD, task issue, PR body, diff, checks, reviews, labels, and `.shiki/` state.\n+- Read `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant SADRs and target ADRs, PRD, task issue, PR body, diff, checks, reviews, labels, and `.shiki/` state.\n - Evaluate every applicable checklist in `docs/agents/checklists.md`.\n - Map each acceptance criterion to durable evidence.\n - Distinguish implementation failure from missing evidence.\ndiff --git a/CONTEXT.md b/CONTEXT.md\nindex e61a9be06a0cce0d026484083a657bde2067e234..237ee8aa3ea8fab334ece49768bc5ed3b5af0974\n--- a/CONTEXT.md\n+++ b/CONTEXT.md\n@@ -25,7 +25,7 @@\n _Avoid_: jumping directly into code\n \n **grill-with-docs**:\n-A planning skill that challenges a plan against domain language, ADRs, code reality, and concrete edge scenarios. It resolves design-tree questions before PRD/issues. It runs inside Requirements Definition.\n+A planning skill that challenges a plan against domain language, Shiki SADRs, target ADRs, code reality, and concrete edge scenarios. It resolves design-tree questions before PRD/issues. It runs inside Requirements Definition.\n _Avoid_: generic brainstorming, silent assumptions\n \n **Requirements Definition**:\ndiff --git a/CONTRIBUTING.md b/CONTRIBUTING.md\nindex c6d9b656e73abb8a55e5f660123e03153dcb3e6d..9e37a8fbf668db5b22461aaae5548b8f1354394c\n--- a/CONTRIBUTING.md\n+++ b/CONTRIBUTING.md\n@@ -17,7 +17,7 @@\n 1. **Goal** \u2014 start from a user-approved Goal with an outcome, completion\n    conditions, non-goals, risk level, and success signals.\n 2. **grill-with-docs** \u2014 for any non-trivial change, challenge the plan against\n-   domain language, ADRs, and code reality before writing a PRD.\n+   domain language, Shiki SADRs, target ADRs, and code reality before writing a PRD.\n 3. **PRD** \u2014 capture settled product/engineering intent (`to-prd`).\n 4. **Spec Freeze** \u2014 the operator explicitly approves the PRD/requirements;\n    the plan records a `spec_freeze` block and no plan runs without it\n@@ -94,7 +94,7 @@\n   entries.\n - Do not hand-edit `.shiki/goals/*.json` or `.shiki/manifest.json` in a task PR.\n - Conversation state is not durable truth. If a decision matters, record it in\n-  GitHub, `.shiki/`, `CONTEXT.md`, or an ADR.\n+  GitHub, `.shiki/`, `CONTEXT.md`, a Shiki SADR, or a target ADR.\n \n ## Architecture Gate\n \n@@ -103,7 +103,7 @@\n or CI policy, package structure, three or more structural files, more than 100\n net new lines in a core module, three or more new files, or any high-risk label.\n When triggered, run `grill-with-docs` or `improve-codebase-architecture` and\n-record durable decisions in an ADR when needed (see `docs/adr/`).\n+record durable decisions in a Shiki SADR or target ADR as applicable (see `docs/adr/`).\n \n ## Safety\n \ndiff --git a/SYSTEM_PROMPT.md b/SYSTEM_PROMPT.md\nindex aaaea45e3a896c3a44ba89eec8bb84325ecf6c62..da009c86a88285269dee0155e58e550c6d744ea9\n--- a/SYSTEM_PROMPT.md\n+++ b/SYSTEM_PROMPT.md\n@@ -30,6 +30,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n Use the canonical source-of-truth order above. Runtime-specific reading orders\n are operational guidance only and must not override or reorder it. The current\n conversation is non-durable operator input.\n@@ -48,14 +51,14 @@\n \n A runtime may only do work that is explicitly assigned by the Goal, task, PR, or MergeGate decision.\n \n-Do not assume `openai/codex-action`, `OPENAI_API_KEY`, or API-key based Codex automation in the default Shiki path. API-key runners are explicit target-repo extensions and require their own ADR.\n+Do not assume `openai/codex-action`, `OPENAI_API_KEY`, or API-key based Codex automation in the default Shiki path. API-key runners are explicit target-repo extensions and require their own target ADR.\n \n ## 4. Non-negotiable Shiki flow\n \n For any non-trivial Goal, use this flow:\n \n 1. **Goal Seek**: define outcome, non-goals, risk level, completion criteria, and evidence requirements.\n-2. **grill-with-docs**: challenge the plan against `CONTEXT.md`, ADRs, code reality, terminology, boundaries, and edge scenarios. Ask one question at a time when the operator is available. If code can answer a question, inspect code instead of asking.\n+2. **grill-with-docs**: challenge the plan against `CONTEXT.md`, Shiki SADRs, target ADRs, code reality, terminology, boundaries, and edge scenarios. Ask one question at a time when the operator is available. If code can answer a question, inspect code instead of asking.\n 3. **Context & Impact**: identify modules, interfaces, seams, callers, dependencies, locks, risk, and verification surfaces.\n 4. **to-prd + Spec Freeze**: turn settled context into a PRD using domain vocabulary and testing decisions. The operator's explicit approval of the PRD is Spec Freeze, recorded as the plan's `spec_freeze` block; plans without it do not run. Steps 1-4 form Requirements Definition.\n 5. **to-issues**: decompose the PRD into independently grabbable vertical-slice issues. Prefer AFK slices; mark HITL slices when judgment is still required.\n@@ -75,7 +78,7 @@\n Before making any material change:\n \n 1. Read `AGENTS.md` and any runtime-specific wrapper such as `CLAUDE.md`.\n-2. Read `CONTEXT.md` and relevant ADRs.\n+2. Read `CONTEXT.md` and relevant SADRs and target ADRs.\n 3. Read the GitHub Issue / PR and `.shiki/` state for the active Goal or task.\n 4. Identify your role: Planner, Implementer, Reviewer, Completion Check Agent, Repairer, or Guardian-assist.\n 5. Identify Goal id, task id, branch, PR, dependencies, locks, risk level, required skills, and required evidence.\n@@ -103,7 +106,7 @@\n \n At minimum, identify:\n \n-- Relevant documents, ADRs, domain terms, and prior decisions.\n+- Relevant documents, SADRs, target ADRs, domain terms, and prior decisions.\n - Relevant modules, interfaces, seams, callers, owners, and tests.\n - Dependency relationships between tasks.\n - Candidate file locks and lock conflicts.\n@@ -152,7 +155,7 @@\n \n CCA must read:\n \n-- `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant ADRs.\n+- `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant SADRs and target ADRs.\n - Parent Goal issue, PRD issue, task issue, comments, and labels.\n - PR body, diff, commits, changed files, reviews, CI checks, and artifacts available to the workflow.\n - `.shiki/` task contract, locks, ledger entries, CCA checklist profile, and prior repair packets.\n@@ -214,7 +217,7 @@\n - Three or more new files.\n - Any high-risk or critical label.\n \n-When the architecture gate triggers, run `grill-with-docs` or `improve-codebase-architecture` as appropriate and record the decision in an ADR if it is hard to reverse or likely to be re-litigated.\n+When the architecture gate triggers, run `grill-with-docs` or `improve-codebase-architecture` as appropriate and record the decision in a Shiki SADR or target ADR as applicable if it is hard to reverse or likely to be re-litigated.\n \n ## 14. Skill Gate\n \n@@ -223,7 +226,7 @@\n Use these skills:\n \n - `setup-matt-pocock-skills`: before first use of the engineering skill set, or when issue tracker, triage labels, or domain docs are missing.\n-- `grill-with-docs`: default for non-trivial Goals before PRD/issues; required for ambiguous plans, terminology, boundaries, ADR-worthy tradeoffs, or design-tree decisions.\n+- `grill-with-docs`: default for non-trivial Goals before PRD/issues; required for ambiguous plans, terminology, boundaries, decision-record-worthy tradeoffs, or design-tree decisions.\n - `zoom-out`: unfamiliar code area or missing architectural map.\n - `to-prd`: convert settled context into a PRD.\n - `to-issues`: break a PRD, plan, or Goal into independently grabbable vertical-slice issues.\ndiff --git a/docs/adr/0001-keep-shiki-as-platform-name.md b/docs/adr/SADR-0001-keep-shiki-as-platform-name.md\nindex 20a9637c3b9d8c97de7ba8db7cfae4d82d9a7dba..8b685439959aada1b221c112f0ac3c51ca8a1695\n--- a/docs/adr/0001-keep-shiki-as-platform-name.md\n+++ b/docs/adr/SADR-0001-keep-shiki-as-platform-name.md\n@@ -1,3 +1,3 @@\n-# Keep Shiki As The Platform Name\n+# SADR-0001: Keep Shiki As The Platform Name\n \n Shiki remains the product name for the GitHub-native agentic engineering control plane. We considered more descriptive names such as MergePilot, MergeGate, and Kiroku, but kept Shiki because it carries the existing goal-seeking framework identity while remaining independent of Hermes, Codex, Claude Code, or any single runtime.\ndiff --git a/docs/adr/0002-use-agents-as-shared-constitution.md b/docs/adr/SADR-0002-use-agents-as-shared-constitution.md\nindex 73de30ecb078120eac540d9bc51855e3cd48d0e3..c5486182bf06fca36ea508496b013c67302fd989\n--- a/docs/adr/0002-use-agents-as-shared-constitution.md\n+++ b/docs/adr/SADR-0002-use-agents-as-shared-constitution.md\n@@ -1,3 +1,3 @@\n-# Use AGENTS As The Shared Agent Constitution\n+# SADR-0002: Use AGENTS As The Shared Agent Constitution\n \n Shiki uses `AGENTS.md` as the runtime-neutral constitution and `CLAUDE.md` as a thin Claude Code wrapper. This keeps Codex, Claude Code, Hermes Runner, and future Agent Runtimes aligned on the same GitHub-first MergeGate rules while still allowing runtime-specific instructions where needed.\ndiff --git a/docs/adr/0003-require-skill-gate-for-mergegate-readiness.md b/docs/adr/SADR-0003-require-skill-gate-for-mergegate-readiness.md\nindex 424be0c9af4c8f0136f1fde68a468c47ff84e764..7fc337987c7df9792f75f181dfcbed75fe5ffff2\n--- a/docs/adr/0003-require-skill-gate-for-mergegate-readiness.md\n+++ b/docs/adr/SADR-0003-require-skill-gate-for-mergegate-readiness.md\n@@ -1,3 +1,3 @@\n-# Require Skill Gate For MergeGate Readiness\n+# SADR-0003: Require Skill Gate For MergeGate Readiness\n \n Shiki requires relevant engineering skills to be invoked before execution when their triggers apply. This makes skills part of MergeGate readiness rather than optional prompt style, because Shiki depends on repeatable planning, issue decomposition, TDD, diagnosis, triage, and architecture review before autonomous or parallel agent work can be trusted.\ndiff --git a/docs/adr/0004-use-github-first-with-shiki-mirror.md b/docs/adr/SADR-0004-use-github-first-with-shiki-mirror.md\nindex d0a3a9174dc646a945a9d325954ffaf00c352040..a79c54ae9ee8eed2a0524192b864da4e22a92d3f\n--- a/docs/adr/0004-use-github-first-with-shiki-mirror.md\n+++ b/docs/adr/SADR-0004-use-github-first-with-shiki-mirror.md\n@@ -1,4 +1,4 @@\n-# ADR 0004: Use GitHub First With A Repository-Local Shiki Mirror\n+# SADR-0004: Use GitHub First With A Repository-Local Shiki Mirror\n \n ## Status\n \ndiff --git a/docs/adr/0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md b/docs/adr/SADR-0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\nindex b339baf49b89d9290e79c3d51b36e7b2bde3bd95..c30a0d8b1fde0224d0ac7820e7870dccf721580d\n--- a/docs/adr/0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\n+++ b/docs/adr/SADR-0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\n@@ -1,4 +1,4 @@\n-# ADR 0005: Use Codex As Front And Claude Code Action As The GitHub Runtime\n+# SADR-0005: Use Codex As Front And Claude Code Action As The GitHub Runtime\n \n ## Status\n \ndiff --git a/docs/adr/0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md b/docs/adr/SADR-0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\nindex 4bcae9d1e52f0a37b8e9fbafa5cd14b6ee1993bd..7a38538a6ae9929567f0ade67e7ea6a60d0b3f7e\n--- a/docs/adr/0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\n+++ b/docs/adr/SADR-0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\n@@ -1,4 +1,4 @@\n-# ADR 0006: Separate Codex Implementation, CCA Completion, And MergeGate Authorization\n+# SADR-0006: Separate Codex Implementation, CCA Completion, And MergeGate Authorization\n \n ## Status\n \ndiff --git a/docs/adr/0007-packaging-and-release.md b/docs/adr/SADR-0007-packaging-and-release.md\nindex 79b84ddb97167372b44641e5b7860e400ce80822..bb445a2e2e856d9d1f5fdbe35de8d4e8cec0fb32\n--- a/docs/adr/0007-packaging-and-release.md\n+++ b/docs/adr/SADR-0007-packaging-and-release.md\n@@ -1,4 +1,4 @@\n-# ADR 0007: Packaging And Release For Shiki\n+# SADR-0007: Packaging And Release For Shiki\n \n ## Status\n \n@@ -25,7 +25,7 @@\n   not introduce third-party runtime dependencies or break the shim.\n - Releases must run inside the operator's subscription-authenticated toolchain\n   and must not require extra secrets beyond the built-in `GITHUB_TOKEN`\n-  (consistent with ADR 0005's no-API-key-by-default stance).\n+  (consistent with SADR-0005's no-API-key-by-default stance).\n - `.shiki/manifest.json` already encodes the install surface (`install.include`,\n   `install.create_directories`, `install.exclude_from_commit`), and it must not\n   be mutated as part of this change.\n@@ -34,7 +34,7 @@\n \n This is a hard-to-reverse platform decision (it sets the version source of\n truth, the release trigger, and the public install contract), so it is recorded\n-as an ADR.\n+as a SADR.\n \n ## Decision\n \n@@ -104,7 +104,7 @@\n 3. **Publishing to PyPI.** Rejected for now: Shiki is distributed as a\n    repository template + git-installable CLI, not a PyPI library; PyPI would add\n    credentials/secrets and a different versioning contract. Can be added later as\n-   its own ADR if a packaged distribution is needed.\n+   its own SADR if a packaged distribution is needed.\n 4. **Dynamic version (read `VERSION` via a build backend `dynamic` field).**\n    Rejected to keep `pyproject.toml` backend-agnostic and dependency-free; the\n    small duplication between `VERSION` and `pyproject.toml` is guarded by the\n@@ -123,4 +123,4 @@\n   releasing versions the whole platform but ships only `install.include` to\n   targets.\n - Future PyPI distribution or a console entry point are deferred and would each\n-  warrant their own ADR.\n+  warrant their own SADR.\ndiff --git a/docs/adr/0008-use-claude-code-as-default-implementer.md b/docs/adr/SADR-0008-use-claude-code-as-default-implementer.md\nindex dedf4510b7c7799521422f622029563d373db031..35c2625d908167fbc58855b5b63e283805f167e8\n--- a/docs/adr/0008-use-claude-code-as-default-implementer.md\n+++ b/docs/adr/SADR-0008-use-claude-code-as-default-implementer.md\n@@ -1,12 +1,12 @@\n-# ADR 0008: Use Claude Code As The Default Implementer Runtime\n+# SADR-0008: Use Claude Code As The Default Implementer Runtime\n \n ## Status\n \n-Proposed (supersedes ADR 0005 for the implementer default; keeps its auth model)\n+Proposed (supersedes SADR-0005 for the implementer default; keeps its auth model)\n \n ## Context\n \n-ADR 0005 made Codex Front the default implementation runtime, with Claude Code\n+SADR-0005 made Codex Front the default implementation runtime, with Claude Code\n restricted to planner and reviewer roles by the runtime registry, and the only\n autonomous implementation adapter was `shiki runner codex`.\n \n@@ -18,13 +18,13 @@\n - The platform's planning, review, and CCA layers already run on Claude Code,\n   and Claude Code's native capabilities (the built-in `/code-review` skill and\n   the Workflow multi-agent orchestration tool) have no Codex equivalent.\n-- The Goal lifecycle redesign (ADR 0009) requires an autonomous post-freeze\n+- The Goal lifecycle redesign (SADR-0009) requires an autonomous post-freeze\n   loop driven from the operator's Claude Code session, including bounded\n   repair dispatch, which must not depend on a runtime that cannot be invoked\n   unattended.\n \n The constitution claims Shiki is runtime-agnostic (\"not a Claude-only\n-workflow\", ADR 0002). Any change to the implementer default must preserve the\n+workflow\", SADR-0002). Any change to the implementer default must preserve the\n registry-based runtime model rather than hardcoding one vendor.\n \n ## Decision\n@@ -39,7 +39,7 @@\n   the task to review or repair-needed.\n - Keep Codex registered as an optional implementer runtime. `shiki runner\n   codex`, the Codex handoff templates, and the ChatGPT OAuth auth model from\n-  ADR 0005 remain supported for tasks explicitly assigned to `codex`.\n+  SADR-0005 remain supported for tasks explicitly assigned to `codex`.\n - Commit to mandating two Claude-native capabilities as loop contract in\n   follow-up Goals (status: the code-review gate was delivered by Goal G-B \u2014\n   skill registry, checklists item PR-12, default required_skills \u2014 while the\n@@ -60,7 +60,7 @@\n \n Out of scope: removing Codex support, changing the CCA runtime (Claude Code\n Action with `CLAUDE_CODE_OAUTH_TOKEN` stays), and API-key based automation\n-(still requires its own ADR per ADR 0005).\n+(still requires its own target ADR per SADR-0005).\n \n ## Consequences\n \n@@ -100,7 +100,7 @@\n   harness rejection of direct `codex exec` is outside Shiki's control, and the\n   plan's mandatory native capabilities cannot run on Codex at all.\n - **Remove Codex entirely**: rejected; it abandons the runtime-agnostic\n-  constitution (ADR 0002) for no operational gain and forecloses\n+  constitution (SADR-0002) for no operational gain and forecloses\n   cross-runtime adversarial review.\n - **GitHub Actions cloud implementation via claude-code-action**: rejected as\n   default; durable evidence is automatic but token cost, Node 24 workflow\ndiff --git a/docs/adr/0009-requirements-definition-and-spec-freeze.md b/docs/adr/SADR-0009-requirements-definition-and-spec-freeze.md\nindex 29b3f7871bef9203a8b4a831a65c39eb2f506e42..52e7dd69ad0aa4efe0b68c62678abbf7df02aaec\n--- a/docs/adr/0009-requirements-definition-and-spec-freeze.md\n+++ b/docs/adr/SADR-0009-requirements-definition-and-spec-freeze.md\n@@ -1,4 +1,4 @@\n-# ADR 0009: Merge Grill And Planning Into Requirements Definition With Spec Freeze\n+# SADR-0009: Merge Grill And Planning Into Requirements Definition With Spec Freeze\n \n ## Status\n \ndiff --git a/docs/adr/0010-external-ai-guardian-review.md b/docs/adr/SADR-0010-external-ai-guardian-review.md\nindex e6108bbe8bb4e73e0ff4684c8f78001a80438c4e..f3510b145664298adafb151b6f0db51b972f01ec\n--- a/docs/adr/0010-external-ai-guardian-review.md\n+++ b/docs/adr/SADR-0010-external-ai-guardian-review.md\n@@ -1,4 +1,4 @@\n-# ADR 0010: External AI Guardian Review As A First-Class Approval Authority\n+# SADR-0010: External AI Guardian Review As A First-Class Approval Authority\n \n ## Status\n \n@@ -8,7 +8,7 @@\n \n Shiki is intended to be an autonomous, recursive, self-improving agent\n framework: authority-in-the-loop, not human-in-the-loop. Until now the Guardian\n-approval required for high/critical-risk merges (guardian-policy.json, ADR\n+approval required for high/critical-risk merges (guardian-policy.json, SADR\n governance) recognized only human authority sources \u2014 a GitHub review approval,\n the `guardian:approved` label applied by a configured Guardian, or a comment\n carrying `Guardian approval granted` plus the current head SHA.\n@@ -89,7 +89,7 @@\n   autonomous design the operator intends and produced the impersonation\n   incident.\n - **Record AI approval as an operator comment**: rejected as forgery of human\n-  authority \u2014 the exact failure this ADR corrects.\n+  authority \u2014 the exact failure this SADR corrects.\n - **Deliver the artifact as a committed `.shiki/` file**: rejected; a committed\n   approval inside the PR being approved is forgeable by anyone who can push.\n - **A dedicated reviewer bot identity instead of a relayed comment**: deferred;\ndiff --git a/docs/adr/0011-autonomous-quality-gate-evidence.md b/docs/adr/SADR-0011-autonomous-quality-gate-evidence.md\nindex bca0246c5d46e44afbde15486a75eec425fc02f5..9c93eac16d8c3a6d4b11bd7224e3955102cd7db4\n--- a/docs/adr/0011-autonomous-quality-gate-evidence.md\n+++ b/docs/adr/SADR-0011-autonomous-quality-gate-evidence.md\n@@ -1,4 +1,4 @@\n-# ADR 0011: Autonomous Quality-Gate Evidence \u2014 Deterministic Facts And An Independent Verifier\n+# SADR-0011: Autonomous Quality-Gate Evidence \u2014 Deterministic Facts And An Independent Verifier\n \n ## Status\n \ndiff --git a/docs/adr/0012-autonomous-loop-closeout-to-main.md b/docs/adr/SADR-0012-autonomous-loop-closeout-to-main.md\nindex 637fad6dde4989c9c2bf0f81f676d8d29991123c..ec7d85f81030a676bcbba28b43eb365422a36eac\n--- a/docs/adr/0012-autonomous-loop-closeout-to-main.md\n+++ b/docs/adr/SADR-0012-autonomous-loop-closeout-to-main.md\n@@ -1,4 +1,4 @@\n-# 0012 \u2014 Autonomous loop pushes goal completion to main via a closeout PR\n+# SADR-0012 \u2014 Autonomous loop pushes goal completion to main via a closeout PR\n \n - Status: accepted\n - Date: 2026-06-18\ndiff --git a/docs/adr/0013-bootstrap-grants-actions-approve-pull-requests.md b/docs/adr/SADR-0013-bootstrap-grants-actions-approve-pull-requests.md\nindex b6c69d64f1c5ced346b88d6859586715b3b5bd0e..cf48ff4a6716267f539ca58edd1a852e7c7a2d64\n--- a/docs/adr/0013-bootstrap-grants-actions-approve-pull-requests.md\n+++ b/docs/adr/SADR-0013-bootstrap-grants-actions-approve-pull-requests.md\n@@ -1,4 +1,4 @@\n-# ADR 0013: Bootstrap Grants GitHub Actions Permission To Create And Approve Pull Requests\n+# SADR-0013: Bootstrap Grants GitHub Actions Permission To Create And Approve Pull Requests\n \n ## Status\n \n@@ -31,7 +31,7 @@\n security tradeoff: it lets repository automation satisfy a human-review gate. In\n a multi-maintainer repo that could let a compromised or buggy workflow\n self-approve changes. So whether bootstrap turns it on by default \u2014 and with\n-what default token scope \u2014 is an ADR-worthy decision, not an implementation\n+what default token scope \u2014 is a SADR-worthy decision, not an implementation\n detail.\n \n ## Decision\n@@ -48,7 +48,7 @@\n Specifically:\n \n - **`can_approve_pull_request_reviews` defaults to `true`.** Shiki's default\n-  posture is solo/AFK autonomous operation (ADR 0008/0009/0012), where the CCA\n+  posture is solo/AFK autonomous operation (SADR-0008/0009/0012), where the CCA\n   Review Bridge is the only path that can satisfy `required_review: true` after\n   the CCA and Guardian gates have already passed. The default makes a\n   freshly-bootstrapped repo able to run the autonomous loop end to end without a\ndiff --git a/docs/adr/0014-codex-app-external-ai-guardian-ui-adapter.md b/docs/adr/SADR-0014-codex-app-external-ai-guardian-ui-adapter.md\nindex 238db06279334caa341c117362df9271382a7fcd..65f12e9939321e1b3e389a34c822fc1549e5155d\n--- a/docs/adr/0014-codex-app-external-ai-guardian-ui-adapter.md\n+++ b/docs/adr/SADR-0014-codex-app-external-ai-guardian-ui-adapter.md\n@@ -1,4 +1,4 @@\n-# ADR 0014: Use Codex App As The External AI Guardian UI Adapter\n+# SADR-0014: Use Codex App As The External AI Guardian UI Adapter\n \n ## Status\n \ndiff --git a/docs/adr/0015-contract-approval-before-dispatch.md b/docs/adr/SADR-0015-contract-approval-before-dispatch.md\nindex fad6c656741a20a7684ba16080a7db159f63a52f..6061271a46d3f1d6ae7174477de3fb75673123a6\n--- a/docs/adr/0015-contract-approval-before-dispatch.md\n+++ b/docs/adr/SADR-0015-contract-approval-before-dispatch.md\n@@ -1,4 +1,4 @@\n-# ADR 0015: Approve The Task Contract Before Dispatch, Not The Diff After It\n+# SADR-0015: Approve The Task Contract Before Dispatch, Not The Diff After It\n \n ## Status\n \n@@ -9,7 +9,7 @@\n Guardian approval for high/critical risk is expressible only on a pull request.\n Every approval source in `guardian-policy.json` \u2014 GitHub review, the\n `guardian:approved` label, the `Guardian approval granted` comment, and the\n-`external_ai_guardian_review` artifact (ADR 0010/0014) \u2014 is bound to a PR, and\n+`external_ai_guardian_review` artifact (SADR-0010/0014) \u2014 is bound to a PR, and\n three of the four require the current head SHA. Approval therefore cannot exist\n before an implementation PR exists.\n \n@@ -81,7 +81,7 @@\n An envelope breach must surface only on the non-repairable policy gate, never on\n the repairable `MergeGate metadata check`. Otherwise the Goal Loop routes it to\n `dispatch_repair` \u2014 instructing a runner to make the breach go away \u2014 which is\n-exactly what this ADR forbids.\n+exactly what this SADR forbids.\n \n **Contract immutability.** After registration, an implementation PR must not\n change the governance fields of its own task contract (`scope`, `non_goals`,\n@@ -116,7 +116,7 @@\n fails any verdict that is not `complete`, and the signal the CCA prompt reads is\n computed from `evaluate_guardian_approval` alone. Without the OR-merge at the\n signal, a contract-approved PR still returns `needs_guardian`, the CCA job fails,\n-and the MergeGate policy check never runs \u2014 the gate this ADR changes would never\n+and the MergeGate policy check never runs \u2014 the gate this SADR changes would never\n be reached. The CCA *prompt* needs no change: a `contract_approval` entry in the\n signal's existing `sources` array satisfies its current wording.\n \n@@ -130,7 +130,7 @@\n written, and an approved contract lets its implementation PRs merge through the\n same automatic path as low/medium risk. The wasted implement-then-block cycle\n disappears. Guardian review shifts from reading diffs to reading contracts, which\n-is both earlier and smaller; the external AI Guardian path (ADR 0010/0014) works\n+is both earlier and smaller; the external AI Guardian path (SADR-0010/0014) works\n on contracts with no change.\n \n We accept that the Guardian authorizes a class of change without seeing its\n@@ -146,7 +146,7 @@\n adversarial tests for the new approval path equivalent in rigor to\n `test_shiki_governance_evidence.sh`.\n \n-Bootstrap costs, which apply only while this ADR is being implemented. The tasks\n+Bootstrap costs, which apply only while this SADR is being implemented. The tasks\n that build Contract Approval merge under the existing rules, so the ones\n dispatched after the pre-dispatch gate lands but before the approval path lands\n need a Guardian-approved Contract PR *and* a live Guardian approval on their\n@@ -210,7 +210,7 @@\n risk; they do not bound authorized work.\n \n **Auto-escalate risk on envelope breach instead of stopping.** Returns the\n-workflow to a diff-time Guardian decision \u2014 the exact behavior this ADR removes.\n+workflow to a diff-time Guardian decision \u2014 the exact behavior this SADR removes.\n \n **Resolve governance fields from the base snapshot only.** Rejected against the\n registration flow: implementation PRs add their own contract, so the base has no\ndiff --git a/docs/adr/0016-locks-bound-product-paths.md b/docs/adr/SADR-0016-locks-bound-product-paths.md\nindex b5e850f3f1f7f4ef6e7f9d1763a29a51bd300c45..46b40a9b99a7aae644c02bb458f5473ff0a90ce2\n--- a/docs/adr/0016-locks-bound-product-paths.md\n+++ b/docs/adr/SADR-0016-locks-bound-product-paths.md\n@@ -1,4 +1,4 @@\n-# ADR 0016: Locks bound product paths; the Shiki mirror is bound by record identity\n+# SADR-0016: Locks bound product paths; the Shiki mirror is bound by record identity\n \n ## Status\n \n@@ -6,8 +6,8 @@\n \n - Date: 2026-07-28\n - Deciders: operator (mizutani-140), Claude (planner)\n-- Related: ADR 0004 (GitHub-first with a `.shiki` mirror), ADR 0009 (Spec Freeze),\n-  ADR 0012 (autonomous loop closeout to main), ADR 0015 (Contract Approval before\n+- Related: SADR-0004 (GitHub-first with a `.shiki` mirror), SADR-0009 (Spec Freeze),\n+  SADR-0012 (autonomous loop closeout to main), SADR-0015 (Contract Approval before\n   dispatch), remediation goal task R-02, the live PR #179 deadlock.\n \n ## Context\n@@ -90,11 +90,11 @@\n files, documentation files and named source modules are nameable; runtime-\n allocated ledger, runner and report ids are not \u2014 and those live in `.shiki`,\n which locks no longer govern. Existing frozen contracts are brought into line by\n-a one-time Spec Amendment per plan, which must land before ADR 0015's contract\n+a one-time Spec Amendment per plan, which must land before SADR-0015's contract\n immutability makes `locks` unwritable from the implementation PR.\n \n **Lock state is resolved from the durable side.** MergeGate resolves the lock\n-registry, and ADR 0015's governance fields, from the base `.shiki` snapshot when\n+registry, and SADR-0015's governance fields, from the base `.shiki` snapshot when\n present, falling back to head only when absent. The `MergeGate metadata check`\n job is given `--base-shiki`, which it does not receive today. Locks are not\n derived from the live open-pull-request set: a pull request body is untrusted\n@@ -108,7 +108,7 @@\n Explicitly out of scope: moving MergeGate's contract resolution off the PR\n checkout wholesale; making the mirror a projection of GitHub state; introducing\n new lock kinds; moving ledger evidence out of the repository; and unwinding\n-ADR 0012's closeout pull request. Registration of multi-task goals is ADR 0015's\n+SADR-0012's closeout pull request. Registration of multi-task goals is SADR-0015's\n Contract PR and is not re-decided here.\n \n ## Consequences\n@@ -127,7 +127,7 @@\n \n Risks accepted. First, goal-scoped identity is only as strong as `goal_id`,\n which is resolved from the PR body and the head task file; the mitigation is to\n-add `goal_id` to ADR 0015's frozen governance field set, and until that lands a\n+add `goal_id` to SADR-0015's frozen governance field set, and until that lands a\n task file can name a foreign goal. Second, base-when-present resolution is\n unavailable on fork pull requests, where the head is contributor-controlled;\n fork pull requests already cannot complete a Shiki merge, and this decision does\n@@ -139,7 +139,7 @@\n path, not by implementation pull requests.\n \n New obligations: one Spec Amendment per frozen plan to rewrite `locks`, ordered\n-before ADR 0015 T1; extension of the adversarial suites in\n+before SADR-0015 T1; extension of the adversarial suites in\n `scripts/test_shiki_adversarial_state.sh` and\n `scripts/test_shiki_governance_evidence.sh` to cover the eleven newly-scoped\n directories; and updating `scripts/test_shiki_loop_lock_guard.sh`,\n@@ -151,32 +151,32 @@\n whether the loop's transitive sync of ledger `evidence` references can be\n deleted once the Contract PR carries its own source plan.\n \n-## Relationship to ADR 0004\n+## Relationship to SADR-0004\n \n-This ADR **clarifies something ADR 0004 left unspecified**. ADR 0004 decides that\n+This SADR **clarifies something SADR-0004 left unspecified**. SADR-0004 decides that\n GitHub is the operational source of truth and that each target repository keeps a\n `.shiki/` mirror. It says nothing about pull requests writing the mirror, does\n not name MergeGate, and does not make the mirror an input to a gate. Nothing in\n-ADR 0004's Decision or Consequences is amended, superseded or weakened: the\n+SADR-0004's Decision or Consequences is amended, superseded or weakened: the\n mirror still exists, still records the same artifacts, and pull request\n-boundaries remain the coordination surface. What this ADR settles is the question\n+boundaries remain the coordination surface. What this SADR settles is the question\n 0004 never reached \u2014 by what authority a pull request may write a mirror record \u2014\n and it answers it the way `.shiki/manifest.json` already declares\n (`pr_mutation: current-task-or-current-goal-only`).\n \n-It **amends ADR 0012**, whose Decision item 5 states that \"Loop-executed tasks\n+It **amends SADR-0012**, whose Decision item 5 states that \"Loop-executed tasks\n declare the `path:.shiki/**` lock, which covers every `.shiki` file the closeout\n stages ... so `files_outside_locks` is satisfied without per-file lock\n bookkeeping.\" That clause is replaced: the closeout's mirror writes are\n-authorized by record identity, not by a declared lock. ADR 0012's substantive\n+authorized by record identity, not by a declared lock. SADR-0012's substantive\n decision \u2014 that completion reaches `main` through a closeout pull request rather\n than local attestation \u2014 is preserved unchanged.\n \n-It is **complementary to ADR 0015**, not a substitute. ADR 0015 owns contract\n+It is **complementary to SADR-0015**, not a substitute. SADR-0015 owns contract\n immutability, the pre-dispatch gate, the Contract PR and the Scope Envelope. This\n-ADR owns what a lock means and who may write the mirror. The one ordering\n+SADR owns what a lock means and who may write the mirror. The one ordering\n constraint between them is that the lock-narrowing Spec Amendment must precede\n-ADR 0015 T1's freezing of the `locks` field.\n+SADR-0015 T1's freezing of the `locks` field.\n \n ## Alternatives Considered\n \n@@ -186,7 +186,7 @@\n the only writer that can register them may run. Migrating the Guardian signal to\n that store makes every registration PR fail closed to `needs_guardian` at every\n risk level, because the task file is by definition absent from the store, which\n-removes the low/medium autonomous path ADR 0008/0009 exists to provide. And it\n+removes the low/medium autonomous path SADR-0008/0009 exists to provide. And it\n leaves schemas, manifest, config, guardian policy, templates, migrations, plans,\n worktrees, runner records, runs and handoffs with no writer at all in a\n repository whose product is `.shiki`. Measured benefit on the frozen backlog:\n@@ -199,14 +199,14 @@\n namespace is not in fact ownership-partitioned \u2014 56 ledger entries carry no\n `task_id`, and one goal DAG is writable by 35 registered tasks. The correct\n expression of its insight is that locks do not govern the mirror at all, which is\n-what this ADR decides.\n+what this SADR decides.\n \n **Make the mirror a projection written only by a registration writer and a\n post-merge projection job.** Rejected. It requires a bypass actor with write\n access to the protected default branch; it makes its own master invariant\n readable from a manifest that pull requests may edit; it deletes both reconcile\n modes and leaves a corrupted mirror with no in-band repair path, contradicting\n-ADR 0004's consequence that mirror conflicts are repaired against GitHub state;\n+SADR-0004's consequence that mirror conflicts are repaired against GitHub state;\n and its content-derived ids are rejected by `validate_shiki`'s currently-green id\n pattern.\n \ndiff --git a/docs/adr/0017-bookkeeping-closeouts-do-not-inherit-risk.md b/docs/adr/SADR-0017-bookkeeping-closeouts-do-not-inherit-risk.md\nindex 554e3a0d526a72ca067108c5464cc68c3edcf174..b9676857a4713850e063af7c39c8e0b01ecb4c66\n--- a/docs/adr/0017-bookkeeping-closeouts-do-not-inherit-risk.md\n+++ b/docs/adr/SADR-0017-bookkeeping-closeouts-do-not-inherit-risk.md\n@@ -1,4 +1,4 @@\n-# ADR 0017: A Bookkeeping Closeout Does Not Inherit Its Task's Risk\n+# SADR-0017: A Bookkeeping Closeout Does Not Inherit Its Task's Risk\n \n ## Status\n \n@@ -6,7 +6,7 @@\n \n ## Context\n \n-ADR 0012 made the goal loop push a task's completion to the default branch through a\n+SADR-0012 made the goal loop push a task's completion to the default branch through a\n closeout pull request: after the implementation PR merges, the loop opens a second PR\n carrying `task.status = done`, `lock.state = released`, and \u2014 when the task completes\n its goal \u2014 `goal.status = complete` plus one scorecard report. Nothing else. The\n@@ -49,14 +49,14 @@\n 4. The task file's governance fields (`scope`, `non_goals`, `required_skills`,\n    `risk_level`, `locks`, `acceptance_checks`, `test_command`) are byte-identical to\n    the base snapshot. Only `status`, `expected_pr`, `closeout_pr`, `ledger_evidence`\n-   and `expected_branch` may differ \u2014 the ADR 0015 contract-immutability rule,\n+   and `expected_branch` may differ \u2014 the SADR-0015 contract-immutability rule,\n    unchanged.\n \n    `expected_branch` is in that set because a closeout is *by construction* on a\n    different branch from the implementation it closes, and the loop rewrites the\n    field accordingly. It is safe to let it differ because MergeGate independently\n    requires `expected_branch` to equal the PR's actual head ref, so a PR cannot set\n-   it to anything other than the branch it is really on. The first draft of this ADR\n+   it to anything other than the branch it is really on. The first draft of this SADR\n    omitted it, which made the exemption unreachable: every real closeout failed\n    condition 4 on a field the loop always changes. See PR #209.\n 5. The state transitions are exactly the terminal set: task `review` \u2192 `done`, lock\n@@ -113,12 +113,12 @@\n classification must be derived, not asserted.\n \n **Fold the closeout into the implementation PR** so no second PR exists. Rejected under\n-ADR 0012, which recorded why: a task file that is already `done` on the implementation\n+SADR-0012, which recorded why: a task file that is already `done` on the implementation\n branch short-circuits the loop's review \u2192 CCA \u2192 merge \u2192 done ordering into a local\n goal-completion before any merge happens.\n \n **Have the loop auto-merge closeouts with elevated credentials.** Rejected outright: that\n-is a runtime authoring its own approval, which ADR 0010 and the constitution's\n+is a runtime authoring its own approval, which SADR-0010 and the constitution's\n approval-record prohibition forbid. It would also be indistinguishable, in the evidence,\n from the forgery those rules exist to prevent.\n \ndiff --git a/docs/adr/0018-carried-guardian-approval-across-a-proven-base-sync.md b/docs/adr/SADR-0018-carried-guardian-approval-across-a-proven-base-sync.md\nindex 22358562da390ed8b45cd600da08a43d378b4680..e6e2907cefa776460af73ddf7d39db605f3c39b0\n--- a/docs/adr/0018-carried-guardian-approval-across-a-proven-base-sync.md\n+++ b/docs/adr/SADR-0018-carried-guardian-approval-across-a-proven-base-sync.md\n@@ -1,4 +1,4 @@\n-# ADR 0018: Carried Guardian Approval Across A Proven Base Sync\n+# SADR-0018: Carried Guardian Approval Across A Proven Base Sync\n \n ## Status\n \n@@ -34,7 +34,7 @@\n Guardian evaluator that consumes it (`shiki_guardian.evaluate_guardian_approval`,\n the `guardian_comment_carried` source) were built and tested in isolation. Neither\n does anything until a caller passes the flags that turn the carry on, so the\n-feature was invisible in CI. This ADR records the decision the two of them\n+feature was invisible in CI. This SADR records the decision the two of them\n implement, and this task wires it into the four CI invocations that judge Guardian\n approval.\n \ndiff --git a/docs/adr/README.md b/docs/adr/README.md\nindex c70e057a4520c36e042ab68c53cd4a21ef709b71..5da43856836e2ce6a150f8127766e2f88355ab4b\n--- a/docs/adr/README.md\n+++ b/docs/adr/README.md\n@@ -1,40 +1,41 @@\n-# Architecture Decision Records\n+# Decision Record Namespaces\n \n-This directory holds Shiki's Architecture Decision Records (ADRs): durable\n-records of hard-to-reverse platform decisions. ADRs are part of the Shiki source\n-of truth alongside GitHub state, the `.shiki/` mirror, and `CONTEXT.md`.\n+This directory holds two independent decision namespaces:\n \n-## How ADRs Work\n+- Shiki Architecture Decision Records (SADRs), named\n+  `SADR-NNNN-<short-slug>.md`, record hard-to-reverse Shiki platform decisions.\n+- Target Architecture Decision Records (ADRs), named\n+  `NNNN-<short-slug>.md`, belong to the target product.\n \n-- New ADRs are added as `docs/adr/NNNN-<short-slug>.md`, where `NNNN` is the next\n-  zero-padded sequence number.\n-- The **canonical list of ADRs is the set of files matching `docs/adr/*.md`** in\n-  this directory. Browse `docs/adr/*.md` for the authoritative, current set.\n-- Use [`template.md`](template.md) as the starting point for a new ADR.\n-- Record an ADR when a decision is hard to reverse, surprising without context,\n-  and a real tradeoff. Do not write an ADR for routine or easily reversible\n-  choices.\n+The same four-digit number may appear once in each namespace. A platform\n+`SADR-NNNN` citation resolves only to `SADR-NNNN-*.md`; a target `ADR NNNN`\n+citation resolves only to `NNNN-*.md`.\n \n-Using the `docs/adr/*.md` glob as the source of truth means other PRs can add an\n-ADR without editing this index file, avoiding merge contention on a\n-hand-maintained enumeration.\n+## How Decision Records Work\n \n-## Current ADRs (snapshot)\n-\n-The list below is a convenience snapshot and may lag behind the directory. When\n-in doubt, list `docs/adr/*.md`.\n+- New Shiki platform decisions use the next zero-padded SADR number.\n+- New target product decisions use the target's own ADR sequence.\n+- The canonical list is the files in `docs/adr/`; no hand-maintained index is\n+  authoritative.\n+- Use [`template.md`](template.md) as the starting point and choose the filename\n+  namespace that owns the decision.\n+- Record a decision when it is hard to reverse, surprising without context, and\n+  a real tradeoff. Do not write a decision record for routine or easily\n+  reversible choices.\n \n-- [0001 \u2014 Keep Shiki As The Platform Name](0001-keep-shiki-as-platform-name.md)\n-- [0002 \u2014 Use AGENTS As The Shared Agent Constitution](0002-use-agents-as-shared-constitution.md)\n-- [0003 \u2014 Require Skill Gate For MergeGate Readiness](0003-require-skill-gate-for-mergegate-readiness.md)\n-- [0004 \u2014 Use GitHub First With A Repository-Local Shiki Mirror](0004-use-github-first-with-shiki-mirror.md)\n-- [0005 \u2014 Use Codex As Front And Claude Code Action As The GitHub Runtime](0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md)\n-- [0006 \u2014 Separate Codex Implementation, CCA Completion, And MergeGate Authorization](0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md)\n+Using the directory as the source of truth lets independent changes add records\n+without contending on an enumerated index.\n \n-## Adding A New ADR\n+## Adding A Shiki SADR\n \n-1. Copy `template.md` to `docs/adr/NNNN-<short-slug>.md`.\n+1. Copy `template.md` to `docs/adr/SADR-NNNN-<short-slug>.md`.\n 2. Fill in the title, status, context, decision, and consequences.\n-3. Set the status to `Proposed`, then `Accepted` once the decision is approved.\n-4. Open a PR following `CONTRIBUTING.md`. You do not need to edit this index for\n-   the ADR to be discoverable via `docs/adr/*.md`.\n+3. Set the status to `Proposed`, then `Accepted` once the proper authority has\n+   approved the decision.\n+4. Open a PR following `CONTRIBUTING.md`.\n+\n+## Adding A Target ADR\n+\n+1. Copy `template.md` to `docs/adr/NNNN-<short-slug>.md` using the target's own\n+   sequence.\n+2. Cite it as `ADR NNNN`; do not prefix its filename with `SADR-`.\ndiff --git a/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md b/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\nnew file mode 100644\nindex 0000000000000000000000000000000000000000..7455ce1f3bed02ddddb851ac21a1d1a648c9aeba\n--- /dev/null\n+++ b/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\n@@ -0,0 +1,55 @@\n+# SADR-0019: Separate Shiki Platform SADRs From Target ADRs\n+\n+## Status\n+\n+Accepted\n+\n+## Context\n+\n+Shiki installs its platform decision records into `docs/adr/` in every target\n+repository. Shiki and its targets previously used the same numeric\n+`NNNN-<slug>.md` namespace. A target that already owned ADRs 0013 through 0018\n+therefore acquired two records for each number during installation, making an\n+`ADR NNNN` citation ambiguous. Renumbering target decisions would rewrite target\n+history, while renumbering Shiki decisions per target would destroy their stable\n+platform identity.\n+\n+Upgrades also need to remove the former Shiki-owned numeric paths without ever\n+mistaking a target-owned ADR for platform content. The install stamp already\n+records exact shipped paths and content digests, so it can prove ownership only\n+when both still match.\n+\n+## Decision\n+\n+Shiki platform decisions use the `SADR` namespace:\n+\n+- records are named `docs/adr/SADR-NNNN-<slug>.md`;\n+- live platform citations use `SADR-NNNN`;\n+- target product decisions retain `docs/adr/NNNN-<slug>.md` and `ADR NNNN`;\n+- the reference validator resolves the two namespaces independently, so the same\n+  four-digit number may exist once in each namespace.\n+\n+Before a forced upgrade writes anything, the installer inventories the exact\n+former numeric Shiki ADR paths. It may delete a legacy path only when the\n+existing install stamp contains that exact path and its recorded SHA-256 digest\n+matches the file currently on disk. All ownership blockers are reported\n+together and abort the upgrade before its first write. A non-forced install\n+never performs this cleanup.\n+\n+Historical `.shiki/` evidence remains unchanged because it records the namespace\n+that existed when that evidence was produced.\n+\n+## Consequences\n+\n+- Targets may use any ADR number without colliding with Shiki's platform SADRs.\n+- Fresh installs preserve target ADR bytes and install a stable SADR set.\n+- Forced upgrades fail closed when legacy ownership cannot be proven, and a\n+  successful upgrade refreshes the stamp without removed legacy paths.\n+- Current governance, packaging, workflow, prompt, script, and skill references\n+  must move with the platform records.\n+\n+## Alternatives Considered\n+\n+- Collision-only refusal leaves affected targets permanently unupgradable.\n+- Renumbering target ADRs mutates target-owned history.\n+- Per-target Shiki renumbering makes platform citations non-portable.\ndiff --git a/docs/adr/template.md b/docs/adr/template.md\nindex a92a97daddd0b00ab9dd626cc8d86942da2eafed..17dd79175b5fee2105935557f691b40cf9492942\n--- a/docs/adr/template.md\n+++ b/docs/adr/template.md\n@@ -1,14 +1,14 @@\n-# NNNN: <Short Decision Title>\n+# <SADR-NNNN or NNNN>: <Short Decision Title>\n \n ## Status\n \n-Proposed | Accepted | Superseded by ADR-NNNN\n+Proposed | Accepted | Superseded by SADR-NNNN or target ADR NNNN\n \n ## Context\n \n What is the situation, constraint, or force that requires a decision? Reference\n-relevant domain terms (`CONTEXT.md`), prior ADRs, and code reality. State the\n-problem, not the solution.\n+relevant domain terms (`CONTEXT.md`), prior Shiki SADRs or target ADRs, and code\n+reality. State the problem, not the solution.\n \n ## Decision\n \ndiff --git a/docs/agents/autonomous-loop-e2e-acceptance.md b/docs/agents/autonomous-loop-e2e-acceptance.md\nindex bbf84c6081987f159ac6ad9ecf3c82b7aa72e7e8..5c13c1d02a05a90b0dc46cbef7c95a9846d0c237\n--- a/docs/agents/autonomous-loop-e2e-acceptance.md\n+++ b/docs/agents/autonomous-loop-e2e-acceptance.md\n@@ -40,8 +40,8 @@\n 3. `gh auth status` is logged in with merge permission on the repo.\n 4. Branch protection + required checks are configured exactly as the goal-loop\n    required-check set expects (see `docs/agents/branch-protection-smoke.md`).\n-5. The repo `.shiki/policy` permits autonomous low/medium-risk merges (ADR\n-   0008/0009); the MergeGate policy check is the recorded-authority gate.\n+5. The repo `.shiki/policy` permits autonomous low/medium-risk merges\n+   (SADR-0008/0009); the MergeGate policy check is the recorded-authority gate.\n \n The operator's only actions are: register the task and start the loop. **No\n manual pre-clearing** means after `shiki loop run` starts, the operator does not\ndiff --git a/docs/agents/checklists.md b/docs/agents/checklists.md\nindex 4a7f03ade346f53a1c1d82981f3fd744f9ab4547..0c5ec6c53dc7197b7b7a7d18e667b325babd6c45\n--- a/docs/agents/checklists.md\n+++ b/docs/agents/checklists.md\n@@ -32,18 +32,18 @@\n | ID | Item | Blocking |\n | --- | --- | --- |\n | GD-01 | Plan was challenged against `CONTEXT.md`. | blocking for non-trivial Goals |\n-| GD-02 | Relevant ADRs were read or absence was recorded. | blocking for non-trivial Goals |\n+| GD-02 | Relevant SADRs and target ADRs were read or absence was recorded. | blocking for non-trivial Goals |\n | GD-03 | Ambiguous terms were resolved or converted to HITL blockers. | blocking |\n | GD-04 | Edge scenarios were considered. | advisory |\n | GD-05 | Code reality was checked when code could answer. | blocking when applicable |\n | GD-06 | New domain terms were added to `CONTEXT.md` when needed. | blocking when applicable |\n-| GD-07 | ADR was created or explicitly skipped using the ADR criteria. | blocking when applicable |\n+| GD-07 | A SADR or target ADR was created or explicitly skipped using the decision-record criteria. | blocking when applicable |\n \n ## CI \u2014 Context & Impact Checklist\n \n | ID | Item | Blocking |\n | --- | --- | --- |\n-| CI-01 | Relevant docs and ADRs identified. | blocking |\n+| CI-01 | Relevant docs, SADRs, and target ADRs identified. | blocking |\n | CI-02 | Relevant modules, interfaces, seams, callers, and tests identified. | blocking |\n | CI-03 | Dependencies and blocked-by relationships identified. | blocking |\n | CI-04 | Candidate locks identified. | blocking |\n@@ -62,7 +62,7 @@\n | PRD-04 | Implementation decisions are recorded without volatile code detail. | blocking |\n | PRD-05 | Testing decisions are recorded. | blocking |\n | PRD-06 | Out-of-scope items are explicit. | blocking |\n-| PRD-07 | Relevant glossary/ADR links are present when applicable. | blocking |\n+| PRD-07 | Relevant glossary/SADR/target-ADR links are present when applicable. | blocking |\n \n ## SF \u2014 Spec Freeze Checklist\n \n@@ -98,7 +98,7 @@\n | H-02 | Branch/worktree target is present. | blocking |\n | H-03 | Scope and non-goals are present. | blocking |\n | H-04 | Dependencies and locks are present. | blocking |\n-| H-05 | Relevant docs, ADRs, modules, and tests are referenced. | blocking |\n+| H-05 | Relevant docs, SADRs, target ADRs, modules, and tests are referenced. | blocking |\n | H-06 | Required skills are stated. | blocking |\n | H-07 | TDD expectations are stated. | blocking when behavior work |\n | H-08 | Acceptance criteria are included. | blocking |\ndiff --git a/docs/agents/codex-handoff.md b/docs/agents/codex-handoff.md\nindex ede468f47cb277f69a857e8c45df703e4ed0151d..6f97bdffb67e44d7edea2c71e01ea28933c4eb6c\n--- a/docs/agents/codex-handoff.md\n+++ b/docs/agents/codex-handoff.md\n@@ -46,7 +46,7 @@\n \n ## Context\n - Domain terms:\n-- Relevant ADRs:\n+- Relevant SADRs and target ADRs:\n - Relevant docs:\n - Relevant modules/interfaces/seams:\n - Similar tests/prior art:\ndiff --git a/docs/agents/completion-check-agent.md b/docs/agents/completion-check-agent.md\nindex 13e4c035db35025f4e3cdcc5adfb24314ac4d521..61bfa1218cf36ca605250c91919e620f33238aa0\n--- a/docs/agents/completion-check-agent.md\n+++ b/docs/agents/completion-check-agent.md\n@@ -32,7 +32,7 @@\n - `AGENTS.md`\n - `CLAUDE.md`\n - `CONTEXT.md`\n-- relevant ADRs\n+- relevant SADRs and target ADRs\n - parent Goal issue\n - PRD issue or document\n - task issue\ndiff --git a/docs/agents/decision-control.md b/docs/agents/decision-control.md\nindex 7d8ee5ab03a5ccbcbd04056ae61de03785a2a9c9..8c94ecda00fffaa0e92c325ab83aa9776bd1dac1\n--- a/docs/agents/decision-control.md\n+++ b/docs/agents/decision-control.md\n@@ -11,7 +11,7 @@\n | Decision | Owner | When | Required inputs | Output | Enforcement surface |\n | --- | --- | --- | --- | --- | --- |\n | Is the Goal clear enough to plan? | Planner + Guardian when needed | Goal intake | User request, existing docs, risk signals | Goal readiness verdict | Goal checklist, issue template, ledger |\n-| Must `grill-with-docs` run? | Planner / Skill Gate | Before PRD or issue split | Ambiguity, domain terms, ADR conflicts, unclear boundaries | Skill invocation or explicit skip rationale | Skill Gate checklist |\n+| Must `grill-with-docs` run? | Planner / Skill Gate | Before PRD or issue split | Ambiguity, domain terms, SADR/target-ADR conflicts, unclear boundaries | Skill invocation or explicit skip rationale | Skill Gate checklist |\n | Is a PRD required? | Planner | After Goal context stabilizes | Goal, `grill-with-docs` output, Context & Impact output | PRD required / not required | PRD checklist, CCA evidence check |\n | Can the spec be frozen? | Operator only | End of Requirements Definition, before issue split | PRD/requirements summary, scope inventory (SF-02), open-question list | `spec_freeze` block in the plan + ledger evidence | `require_grilled_plan` (plan ingest/run), SF checklist |\n | Can a frozen spec change? | Operator only (Spec Amendment) | When implementation contradicts the frozen spec | Contested decisions, scoped re-grill result | recorded amendment + re-stamped freeze | SF checklist, ledger evidence |\ndiff --git a/docs/agents/domain.md b/docs/agents/domain.md\nindex 258da8ea81e87ac0a6b0b5b5e5c2fde980eaabb7..d57b0cf17ff3e1fa9e6e5885372bf083d8295421\n--- a/docs/agents/domain.md\n+++ b/docs/agents/domain.md\n@@ -5,7 +5,9 @@\n ## Layout\n \n - `CONTEXT.md` defines Shiki language. Keep it implementation-free.\n-- `docs/adr/` records hard-to-reverse decisions.\n+- `docs/adr/SADR-NNNN-*.md` records Shiki platform decisions.\n+- `docs/adr/NNNN-*.md` records target product decisions without sharing the\n+  SADR namespace.\n - `AGENTS.md` is the runtime-neutral agent constitution.\n - `CLAUDE.md` is the Claude Code wrapper around `AGENTS.md`.\n - `.shiki/` is the Target Repository mirror for Goals, PRDs, plans, Task DAGs, contracts, locks, worktree records, ledger entries, CCA verdicts, repair packets, reports, and handoffs.\n@@ -15,6 +17,6 @@\n \n - Use Shiki terms exactly when they exist.\n - Add glossary terms to `CONTEXT.md` only when the term is part of the domain, not a generic programming concept.\n-- Create ADRs only for hard-to-reverse, surprising, tradeoff-based decisions.\n+- Create SADRs or target ADRs only for hard-to-reverse, surprising, tradeoff-based decisions.\n - Keep product-specific language in the Target Repository, not in Shiki Template docs.\n-- If an ADR conflicts with a proposed implementation, surface the conflict before editing.\n+- If a SADR or target ADR conflicts with a proposed implementation, surface the conflict before editing.\ndiff --git a/docs/agents/execution-control.md b/docs/agents/execution-control.md\nindex ad8ee3cdb36be56fedc1f46408ca92b387d74047..bc63c3a73661d12b567016ba299f1a6b66bf9088\n--- a/docs/agents/execution-control.md\n+++ b/docs/agents/execution-control.md\n@@ -63,7 +63,7 @@\n | Branch name | Orchestrator | Orchestrator | Before implementer dispatch | branch policy | branch ref in task/ledger |\n | Lock reservation | Context & Impact | MergeGate | Before dispatch and before merge | `.shiki/locks/`, labels, PR metadata | lock id, owner, scope |\n | CI profile | Planner | MergeGate | During issue triage, rechecked at PR | labels, `.shiki/policy.yaml`, workflow matrix | required check list |\n-| Required status checks | Repo maintainer / Guardian | GitHub branch protection / rulesets | Repository setup, updated via ADR | GitHub settings | branch/ruleset config, ADR |\n+| Required status checks | Repo maintainer / Guardian | GitHub branch protection / rulesets | Repository setup, updated via target ADR | GitHub settings | branch/ruleset config, target ADR |\n | CCA judgment | CCA | GitHub required check / MergeGate | Every non-draft PR update | GitHub Action check | structured CCA verdict |\n | Repair assignment | CCA | MergeGate | After failed CCA/CI/review | repair packet, issue/PR comment | repair id and attempt count |\n | Merge readiness | MergeGate | GitHub branch protection / Guardian if needed | After CCA complete | required checks, review rules, rulesets | mergegate check output |\ndiff --git a/docs/agents/guardian-policy.md b/docs/agents/guardian-policy.md\nindex 4147c7adf05a47adb4fd24ada1a1c2e1f2ba6d2d..a20d9cc6c00a569bb057b9ca8d6b51c7fc4a9847\n--- a/docs/agents/guardian-policy.md\n+++ b/docs/agents/guardian-policy.md\n@@ -98,8 +98,8 @@\n `origin/<default_branch>` and reuses each job's existing base fetch; a shallow\n (`--depth`) or second base fetch would make the proof unresolvable and silently\n disable the carry, so neither is permitted. CCA reads `guardian_comment_carried` as\n-a recorded authority in the deterministic signal and does not re-derive it. See ADR\n-0018 for the full decision, the rejected diff/patch-equality and path-disjointness\n+a recorded authority in the deterministic signal and does not re-derive it. See\n+SADR-0018 for the full decision, the rejected diff/patch-equality and path-disjointness\n alternatives, and the accepted residual.\n \n ## External AI Guardian Review (SADR-0010)\ndiff --git a/docs/agents/implementation-policy.md b/docs/agents/implementation-policy.md\nindex c754144a9fb5bf4a38aa82a60e1670c59e626b44..85c37281bcad196c6b7b02343faf00b03cd9c57a\n--- a/docs/agents/implementation-policy.md\n+++ b/docs/agents/implementation-policy.md\n@@ -31,10 +31,13 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n ## Non-Negotiables\n \n 1. **Planning precedes implementation.** Non-trivial Goals start with `grill-with-docs` and Context & Impact.\n-2. **Domain language is controlled.** Terms must be checked against `CONTEXT.md`; hard-to-reverse tradeoffs must be checked against ADRs.\n+2. **Domain language is controlled.** Terms must be checked against `CONTEXT.md`; hard-to-reverse tradeoffs must be checked against Shiki SADRs and target ADRs.\n 3. **PRD precedes issue decomposition.** `to-prd` is used once the context is settled enough to become durable intent.\n 4. **Issues are vertical slices.** `to-issues` must produce independently grabbable tracer-bullet tasks, not horizontal layer tickets.\n 5. **Triage controls dispatch.** Only `ready-for-agent` / AFK tasks may be assigned to Codex without human input.\n@@ -95,19 +98,19 @@\n Process:\n \n - Challenge terms against `CONTEXT.md`.\n-- Check ADRs before reopening decisions.\n+- Check SADRs and target ADRs before reopening decisions.\n - Inspect code when code can answer a question.\n - Ask one question at a time when the operator is available.\n - Provide a recommended answer with each question.\n - Record resolved terms in `CONTEXT.md`.\n-- Create ADRs only for hard-to-reverse, surprising, tradeoff-based decisions.\n+- Create SADRs or target ADRs only for hard-to-reverse, surprising, tradeoff-based decisions.\n - Convert unresolved load-bearing questions into HITL blockers.\n \n Outputs:\n \n - Resolved design-tree decisions.\n - Updated glossary terms when needed.\n-- ADRs when justified.\n+- SADRs or target ADRs when justified.\n - HITL blockers when decisions remain unresolved.\n \n ## Phase 3 \u2014 Context & Impact\n@@ -117,7 +120,7 @@\n \n Outputs must include:\n \n-- Relevant documents and ADRs.\n+- Relevant documents, SADRs, and target ADRs.\n - Relevant modules, interfaces, seams, callers, and tests.\n - Candidate locks.\n - Dependencies.\n@@ -140,7 +143,7 @@\n - Testing decisions.\n - Out of scope.\n - Further notes.\n-- Links to relevant Goal, domain terms, and ADRs.\n+- Links to relevant Goal, domain terms, SADRs, and target ADRs.\n \n Phase 4 ends with Spec Freeze: the operator explicitly approves the PRD, the\n plan records a `spec_freeze` block (status, approved_by, source), and the SF\n@@ -240,7 +243,7 @@\n - Dependencies and locks.\n - Risk and Guardian requirements.\n - Ledger completeness.\n-- Documentation and ADR updates.\n+- Documentation and SADR/target-ADR updates.\n - Scope drift.\n \n CCA emits one of:\n@@ -301,7 +304,7 @@\n | Evidence failure | Behavior might be done, but proof is missing. | Add checks, PR evidence, ledger entry, or artifacts. |\n | Scope drift | PR changes unrelated behavior. | Revert unrelated changes or split into new Goal. |\n | Dependency block | Upstream task or lock not complete. | Block until dependency resolves. |\n-| Architecture block | Decision needs ADR or deep-module discussion. | `grill-with-docs` or `improve-codebase-architecture`. |\n+| Architecture block | Decision needs a SADR, target ADR, or deep-module discussion. | `grill-with-docs` or `improve-codebase-architecture`. |\n | Risk block | Security/production/policy change needs human approval. | Guardian decision. |\n \n ## Phase 5.5 \u2014 Execution Decision Control\ndiff --git a/docs/agents/runtime-auth-model.md b/docs/agents/runtime-auth-model.md\nindex 6a2e1a47e81ea89b5c68a210ec07772a9374babc..3c54748d24233dbd0ba8473950282547ef4ce935\n--- a/docs/agents/runtime-auth-model.md\n+++ b/docs/agents/runtime-auth-model.md\n@@ -148,7 +148,7 @@\n \n API-key based runners may be added by a target repository as an explicit extension. They are not the default Shiki template path.\n \n-If a repo opts into API-key mode, record the exception in an ADR and update the repo's `AGENTS.md`, `.shiki/config.yaml`, and workflow permissions.\n+If a repo opts into API-key mode, record the target-specific exception in a target ADR and update the repo's `AGENTS.md`, `.shiki/config.yaml`, and workflow permissions.\n \n ## GitHub Actions Runtime Compatibility\n \ndiff --git a/docs/agents/skill-gate.md b/docs/agents/skill-gate.md\nindex c50398b0d7c60a8598f2a5c88a10015730dced13..3acffe6444617bb1d54b77d4b4708158ec735bc6\n--- a/docs/agents/skill-gate.md\n+++ b/docs/agents/skill-gate.md\n@@ -9,7 +9,7 @@\n | Skill | Trigger | Expected output |\n | --- | --- | --- |\n | `setup-matt-pocock-skills` | First configuring a repo for these skills, or when issue tracker, triage labels, or domain docs are missing. | `docs/agents/` configuration and an `Agent skills` block in `AGENTS.md` or `CLAUDE.md`. |\n-| `grill-with-docs` | Ambiguous plan, unclear terminology, boundary decision, tradeoff, or ADR-worthy design choice. | Clarified decision tree, recommended answers, updates to `CONTEXT.md` or ADRs when decisions crystallize. |\n+| `grill-with-docs` | Ambiguous plan, unclear terminology, boundary decision, tradeoff, or decision-record-worthy design choice. | Clarified decision tree, recommended answers, updates to `CONTEXT.md`, SADRs, or target ADRs when decisions crystallize. |\n | `zoom-out` | Agent lacks a higher-level architectural map. | Map of relevant modules, callers, seams, and domain concepts. |\n | `to-prd` | Settled context should become a PRD. | PRD published to the issue tracker with problem, solution, user stories, decisions, testing, and out-of-scope notes. |\n | `to-issues` | Goal, PRD, or plan must become implementation tickets. | Vertical-slice issues with acceptance criteria, dependencies, and AFK/HITL classification. |\n@@ -17,7 +17,7 @@\n | `tdd` | Feature work or bug fix has observable behavior that can be specified with tests. | Red-green-refactor loop through public interfaces, one vertical slice at a time. |\n | `code-review` | The TDD loop is green and the implementer is about to create or update a PR. Default for every implementation task. | Pre-PR self-review of the task diff with fixes applied on the task branch; findings and resolutions recorded as a ledger entry naming the skill and a `## Pre-PR code review` PR body section. |\n | `diagnose` | Hard bug, failing check, regression, flaky behavior, or performance problem. | Reproduction loop, ranked hypotheses, instrumentation, fix, regression test, post-mortem. |\n-| `improve-codebase-architecture` | Structural friction, testability, AI-navigability, deep-module opportunity, or refactor request. | Architecture candidates and deepening recommendations informed by `CONTEXT.md` and ADRs. |\n+| `improve-codebase-architecture` | Structural friction, testability, AI-navigability, deep-module opportunity, or refactor request. | Architecture candidates and deepening recommendations informed by `CONTEXT.md`, SADRs, and target ADRs. |\n | `prototype` | A throwaway logic or UI prototype is needed to answer a design question. | Clearly marked disposable prototype and captured decision. |\n \n ## MergeGate Interaction\ndiff --git a/scripts/guardian_approval_signal.py b/scripts/guardian_approval_signal.py\nindex d059f92b2c82aa2b5d6ca421365537a87886c1cb..16924892714f4f5b9303c0ef0e325a7e70075630\n--- a/scripts/guardian_approval_signal.py\n+++ b/scripts/guardian_approval_signal.py\n@@ -199,8 +199,8 @@\n ) -> Any:\n     \"\"\"Evaluate SADR-0015 Contract Approval for the CCA Guardian signal.\n \n-    The SAME OR that MergeGate applies must reach this deterministic signal: ADR\n-    0015 is explicit that without it, a contract-approved PR still returns\n+    The SAME OR that MergeGate applies must reach this deterministic signal:\n+    SADR-0015 is explicit that without it, a contract-approved PR still returns\n     ``needs_guardian``, the CCA job fails, and the MergeGate policy check never\n     runs \u2014 the gate this change moves would never be reached. This resolves the\n     task the SAME way MergeGate does (the first Shiki task id in the PR body),\ndiff --git a/scripts/shiki_contract_approval.py b/scripts/shiki_contract_approval.py\nindex 941bcb96fdd8b9a85bc0c4f0f04d5dad9d04839c..4a4b418f6527413161db82670bec857a043360e2\n--- a/scripts/shiki_contract_approval.py\n+++ b/scripts/shiki_contract_approval.py\n@@ -142,7 +142,7 @@\n     changed_files_status: Any,\n     registration: Any,\n ) -> ContractApprovalResult:\n-    \"\"\"Whether this PR carries an SADR-0015 Contract Approval.\n+    \"\"\"Whether this PR carries a SADR-0015 Contract Approval.\n \n     The four conditions are evaluated IN ORDER, each failure recorded as a\n     distinct reason string:\ndiff --git a/scripts/shiki_guardian.py b/scripts/shiki_guardian.py\nindex a4ecb79bbda55fd5c8b06adfae2eb99760a03c45..4dd99bbe714e26f421fa19abac7a34ae1b195ecc\n--- a/scripts/shiki_guardian.py\n+++ b/scripts/shiki_guardian.py\n@@ -12,7 +12,7 @@\n # The carry re-derives its proof from git in-process on every judging invocation;\n # it is never read from an artifact, so there is nothing to go stale or be forged.\n # shiki_sync_proof is the ONLY guardian-family module that runs git, and every one\n-# of its invocations is read-only and local (ADR: prove-from-git base sync).\n+# of its invocations is read-only and local (SADR: prove-from-git base sync).\n from shiki_sync_proof import verify_pure_base_sync\n \n GUARDIAN_POLICY_PATH = \".shiki/guardian-policy.json\"\ndiff --git a/scripts/shiki_guardian_review.py b/scripts/shiki_guardian_review.py\nindex dd0f104d457899e0ba5ff1e162011c1e90b6f92b..e10ef58c046324e7ea3a3392feb3048ca77414ce\n--- a/scripts/shiki_guardian_review.py\n+++ b/scripts/shiki_guardian_review.py\n@@ -159,7 +159,7 @@\n     docs_only = _is_docs_only(changed_files)\n     if docs_only and matched == 0:\n         focus.append(\n-            \"Docs/ADR-only change: confirm the change is genuinely documentation/decision-only, introduces no behavior or contract drift, and stays consistent with the constitution and existing ADRs.\"\n+            \"Docs/decision-record-only change: confirm the change is genuinely documentation/decision-only, introduces no behavior or contract drift, and stays consistent with the constitution and existing SADRs and target ADRs.\"\n         )\n \n     # Conservative augmentation: unknown (no category and not docs-only), mixed\ndiff --git a/scripts/shiki_installer.py b/scripts/shiki_installer.py\nindex be064ab7a29ff6836f7f7a1d70500a43e0d11d7b..9fd8eefd137ce7003dad5c0dcb2e48e2cd47ab32\n--- a/scripts/shiki_installer.py\n+++ b/scripts/shiki_installer.py\n@@ -118,6 +118,7 @@\n # what lets a target carry it without a governance-schema change.\n INSTALL_STAMP_PATH = \".shiki/install-stamp.json\"\n INSTALL_STAMP_VERSION = 1\n+LEGACY_SHIKI_SADR_MAX = 18\n \n # Under --force the shipped surface splits three ways, so nothing is lost and\n # nothing is silently stale.\n@@ -237,9 +238,98 @@\n         )\n \n \n+def legacy_shiki_adr_paths() -> tuple[str, ...]:\n+    \"\"\"Former numeric paths for Shiki SADR-0001 through SADR-0018.\n+\n+    The authoritative slugs come from the current SADR filenames. SADR-0019 is\n+    the namespace decision itself and never shipped under a numeric legacy path.\n+    \"\"\"\n+    paths: list[str] = []\n+    for sadr in sorted((ROOT / \"docs\" / \"adr\").glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\")):\n+        number = sadr.name.split(\"-\", 2)[1]\n+        if int(number) <= LEGACY_SHIKI_SADR_MAX:\n+            paths.append(f\"docs/adr/{sadr.name.removeprefix('SADR-')}\")\n+    return tuple(paths)\n+\n+\n+def preflight_legacy_sadr_cleanup(target: Path, *, force: bool) -> tuple[str, ...]:\n+    \"\"\"Authorize exact legacy Shiki ADR removals before the first write.\n+\n+    A non-force install never removes existing files. A forced install needs no\n+    stamp when no legacy Shiki path is present. When legacy paths do exist, each\n+    must be named in a readable install stamp and its current bytes must match\n+    the recorded digest.\n+    \"\"\"\n+    if not force:\n+        return ()\n+    present = tuple(\n+        relative\n+        for relative in legacy_shiki_adr_paths()\n+        if (target / relative).exists() or (target / relative).is_symlink()\n+    )\n+    if not present:\n+        return ()\n+\n+    stamp_path = target / INSTALL_STAMP_PATH\n+    try:\n+        raw = stamp_path.read_text(encoding=\"utf-8\")\n+        stamp = json.loads(raw)\n+    except FileNotFoundError:\n+        detail = \"install stamp is absent\"\n+        blockers = [f\"{relative}: {detail}\" for relative in present]\n+    except (OSError, UnicodeDecodeError) as error:\n+        detail = f\"install stamp is unreadable ({error})\"\n+        blockers = [f\"{relative}: {detail}\" for relative in present]\n+    except json.JSONDecodeError as error:\n+        detail = f\"install stamp is malformed ({error})\"\n+        blockers = [f\"{relative}: {detail}\" for relative in present]\n+    else:\n+        if not isinstance(stamp, dict):\n+            blockers = [\n+                f\"{relative}: install stamp is malformed (expected an object)\"\n+                for relative in present\n+            ]\n+        else:\n+            digests = stamp.get(\"digests\")\n+            blockers = []\n+            if not isinstance(digests, dict):\n+                blockers.extend(\n+                    f\"{relative}: install stamp has no digest for this path\"\n+                    for relative in present\n+                )\n+            else:\n+                for relative in present:\n+                    expected = digests.get(relative)\n+                    if not isinstance(expected, str) or not expected:\n+                        blockers.append(\n+                            f\"{relative}: install stamp has no digest for this path\"\n+                        )\n+                        continue\n+                    try:\n+                        actual = _sha256_file(target / relative)\n+                    except OSError as error:\n+                        blockers.append(\n+                            f\"{relative}: legacy path is unreadable ({error})\"\n+                        )\n+                        continue\n+                    if actual != expected:\n+                        blockers.append(\n+                            f\"{relative}: digest mismatch (stamp {expected}, current {actual})\"\n+                        )\n+\n+    if blockers:\n+        joined = \"\\n\".join(f\"- {blocker}\" for blocker in blockers)\n+        raise ShikiError(\n+            \"legacy Shiki ADR cleanup is not authorized:\\n\"\n+            f\"{joined}\\nNo files were written.\"\n+        )\n+    return present\n+\n+\n def install_template(target: Path, *, force: bool, validate: bool) -> list[NewFileNote]:\n     # Preconditions first: never rewrite the tree of a target that cannot be\n     # upgraded cleanly (see refuse_pending_migrations).\n+    legacy_cleanup = preflight_legacy_sadr_cleanup(target, force=force)\n     refuse_pending_migrations(target)\n \n     # Resolve the target's required CODEOWNERS owner once from its\n@@ -250,6 +340,10 @@\n     # verbatim. See ``codeowners_required_owner``.\n     codeowners_owner = codeowners_required_owner(target)\n \n+    for relative in legacy_cleanup:\n+        (target / relative).unlink()\n+        info(f\"removed stamped legacy Shiki ADR: {target / relative}\")\n+\n     notes: list[NewFileNote] = []\n     for relative in TEMPLATE_PATHS:\n         source = ROOT / relative\n@@ -647,6 +741,22 @@\n     digests = target_stamp_digests(target)\n     existing = read_install_stamp(target)\n     if existing is not None:\n+        # A non-force install deliberately leaves legacy Shiki ADRs in place.\n+        # Preserve only still-matching ownership proof so a later forced\n+        # upgrade can authorize cleanup; never carry a missing or stale digest.\n+        existing_digests = existing.get(\"digests\")\n+        if isinstance(existing_digests, dict):\n+            for relative in legacy_shiki_adr_paths():\n+                expected = existing_digests.get(relative)\n+                path = target / relative\n+                if not isinstance(expected, str) or not path.is_file():\n+                    continue\n+                try:\n+                    actual = _sha256_file(path)\n+                except OSError:\n+                    continue\n+                if actual == expected:\n+                    digests[relative] = actual\n         if _incoming_is_older(commit, existing.get(\"platform_commit\")):\n             warn(\n                 \"kept existing install stamp: incoming install \"\ndiff --git a/scripts/shiki_loop.py b/scripts/shiki_loop.py\nindex 3611641da8aeb5038b1da90e3114b09a473ec6d8..84caa491b9cad0edc4176f209c13e9221e975681\n--- a/scripts/shiki_loop.py\n+++ b/scripts/shiki_loop.py\n@@ -424,7 +424,7 @@\n         # unresolvable CCA verdict has already produced its terminal stop and is never\n         # laundered into a head-moving sync (SADR-0010). It also never fires while the\n         # policy (Guardian) gate is red \u2014 for ANY reason, not only when it is the sole\n-        # red check \u2014 so a head-bound Guardian approval, or an SADR-0015\n+        # red check \u2014 so a head-bound Guardian approval, or a SADR-0015\n         # contract-approval carry that base movement defeated (the policy gate compares\n         # base and head task governance directly), is never silently restored by\n         # adopting base wholesale. It is bounded by a durable per-task counter so the\ndiff --git a/scripts/shiki_tasks.py b/scripts/shiki_tasks.py\nindex aec3809f24b4a0c8c96fc50971cff9ae9ad30381..d531052e9d9717058d4631483cefab44c26a65c1\n--- a/scripts/shiki_tasks.py\n+++ b/scripts/shiki_tasks.py\n@@ -821,7 +821,7 @@\n         \"prompt\": prompt,\n         \"entry_skill\": \"grill-with-docs\",\n         \"required_next_steps\": [\n-            \"Run grill-with-docs until terminology, boundaries, risks, and ADR-worthy decisions are settled.\",\n+            \"Run grill-with-docs until terminology, boundaries, risks, and decision-record-worthy choices are settled.\",\n             \"Obtain the operator's explicit approval of the PRD/requirements (Spec Freeze).\",\n             \"Write a machine-readable plan JSON with grill_with_docs.status=complete and spec_freeze.status=frozen.\",\n             \"Run shiki plan ingest --plan-file PLAN.json.\",\ndiff --git a/scripts/test_shiki_goal_loop.sh b/scripts/test_shiki_goal_loop.sh\nindex 753c7b516e10f0be3bc92a4bd0eb1fa699776ae6..8f3820a0a079b3044424cfe1a6c6b7fe68896e67\n--- a/scripts/test_shiki_goal_loop.sh\n+++ b/scripts/test_shiki_goal_loop.sh\n@@ -231,8 +231,8 @@\n \n # High/critical risk merges autonomously when all required checks are green:\n # the \"MergeGate policy check\" required check IS the Guardian gate (it enforces\n-# guardian-policy.json \u2014 human approval OR external AI guardian review, ADR\n-# 0010), so green checks mean a recorded authority approved. The fake gh returns\n+# guardian-policy.json \u2014 human approval OR external AI guardian review,\n+# SADR-0010), so green checks mean a recorded authority approved. The fake gh returns\n # green checks, simulating that the Guardian gate passed.\n python3 \"$ROOT/scripts/shiki.py\" goal create --target \"$TARGET\" --title \"Guardian gate\" --outcome \"High risk merges via policy gate\" >/tmp/shiki-goal-loop-guardian-goal.json\n GGOAL=\"$(json_get /tmp/shiki-goal-loop-guardian-goal.json goal_id)\"\ndiff --git a/tests/test_adr_references.py b/tests/test_adr_references.py\nindex 51706329c797d5dd9b47e082a333fa83b261d482..db6e693e35f67103935be34f1d9bb4ff7ee60395\n--- a/tests/test_adr_references.py\n+++ b/tests/test_adr_references.py\n@@ -1,7 +1,7 @@\n-\"\"\"Every ``ADR ####`` citation must resolve to exactly one decision record.\n+\"\"\"Every target ADR and Shiki SADR citation resolves in its own namespace.\n \n Background. ``scripts/mergegate_check.py`` justifies ``is_bookkeeping_closeout``\n-with \"ADR 0017\" nine times and ``scripts/guardian_approval_signal.py`` cites\n+with \"SADR-0017\" nine times and ``scripts/guardian_approval_signal.py`` cites\n 0016/0017 seven times, yet for a stretch the numbering in ``docs/adr/`` stopped\n at 0015: the authoring commits lived on a coordinator branch and no task ever\n declared ``docs/adr/...`` in its locks, so nothing carried the records to the\n@@ -11,11 +11,12 @@\n and the directory drifted with no signal.\n \n This is the same unbound-pair shape behind other silent failures: a reference\n-and its target with nothing asserting they agree. This test binds them. It scans\n-every ``scripts/*.py`` (plus ``docs/**/*.md`` and the root ``*.md`` governance\n-files, which are cheap) for the ``ADR ####`` pattern and asserts each cited\n-number resolves to exactly one ``docs/adr/####-*.md``. The converse is\n-deliberately NOT required: an ADR may exist without being cited.\n+and its target with nothing asserting they agree. This test binds them. Target\n+``ADR NNNN`` citations resolve only to ``docs/adr/NNNN-*.md``; Shiki\n+``SADR-NNNN`` citations resolve only to ``docs/adr/SADR-NNNN-*.md``. The scan\n+covers current governance docs, scripts, workflows, prompts, packaging metadata,\n+and repo-local Codex guidance while deliberately excluding historical\n+``.shiki`` evidence. The converse is not required: a record may be uncited.\n \n The suite is dependency free: standard-library ``unittest`` only.\n \"\"\"\n@@ -31,17 +32,18 @@\n \n REPO_ROOT = Path(__file__).resolve().parents[1]\n \n-# \"ADR 0016\" -- a four-digit citation. The trailing \\b keeps a five-digit token\n-# from being read as a truncated four-digit number.\n-_CITATION = re.compile(r\"\\bADR (\\d{4})\\b\")\n-# \"0016-locks-bound-product-paths.md\" -- the leading four digits name the record.\n-_ADR_FILENAME = re.compile(r\"^(\\d{4})-.*\\.md$\")\n+# ``ADR 0016`` / ``SADR-0016`` -- a namespace plus four-digit citation. The\n+# trailing ``\\b`` keeps a five-digit token from being read as a truncated one.\n+_CITATION = re.compile(r\"\\b(S?ADR)[ -](\\d{4})\\b\")\n+# Target ADRs retain the numeric filename; Shiki platform decisions use SADR.\n+_DECISION_FILENAME = re.compile(r\"^(?:(SADR)-)?(\\d{4})-.*\\.md$\")\n \n \n class Violation(NamedTuple):\n     \"\"\"A citation that does not resolve to exactly one decision record.\"\"\"\n \n     citing_file: str  # repo-relative path of the file that cites the ADR\n+    namespace: str  # \"ADR\" for target decisions; \"SADR\" for Shiki decisions\n     number: str  # the cited four-digit number, e.g. \"0016\"\n     line: int  # 1-based line of the citation\n     reason: str  # \"missing\" (zero matches) or \"ambiguous\" (two or more)\n@@ -49,44 +51,59 @@\n \n     @property\n     def expected_path(self) -> str:\n-        return f\"docs/adr/{self.number}-*.md\"\n+        prefix = \"SADR-\" if self.namespace == \"SADR\" else \"\"\n+        return f\"docs/adr/{prefix}{self.number}-*.md\"\n \n     def describe(self) -> str:\n         \"\"\"Name the citing file, the cited number, and the missing path.\"\"\"\n         if self.reason == \"missing\":\n             return (\n-                f\"{self.citing_file}:{self.line} cites ADR {self.number} \"\n+                f\"{self.citing_file}:{self.line} cites {self.namespace} {self.number} \"\n                 f\"but no document matches {self.expected_path}\"\n             )\n         joined = \", \".join(self.matches)\n         return (\n-            f\"{self.citing_file}:{self.line} cites ADR {self.number} which \"\n+            f\"{self.citing_file}:{self.line} cites {self.namespace} {self.number} which \"\n             f\"resolves to {len(self.matches)} documents (expected exactly one \"\n             f\"at {self.expected_path}): {joined}\"\n         )\n \n \n-def _adr_index(root: Path) -> dict[str, list[Path]]:\n-    \"\"\"Map every four-digit ADR number to the record files that claim it.\"\"\"\n-    index: dict[str, list[Path]] = {}\n+def _adr_index(root: Path) -> dict[tuple[str, str], list[Path]]:\n+    \"\"\"Map each decision namespace and number to the files that claim it.\"\"\"\n+    index: dict[tuple[str, str], list[Path]] = {}\n     adr_dir = root / \"docs\" / \"adr\"\n     if adr_dir.is_dir():\n         for path in sorted(adr_dir.glob(\"*.md\")):\n-            match = _ADR_FILENAME.match(path.name)\n+            match = _DECISION_FILENAME.match(path.name)\n             if match:\n-                index.setdefault(match.group(1), []).append(path)\n+                namespace = \"SADR\" if match.group(1) else \"ADR\"\n+                index.setdefault((namespace, match.group(2)), []).append(path)\n     return index\n \n \n def _citing_files(root: Path, include_docs: bool = True) -> list[Path]:\n-    \"\"\"Files scanned for citations: scripts/*.py, and cheaply the markdown.\"\"\"\n+    \"\"\"Current, shipped text surfaces scanned for decision citations.\"\"\"\n     files = sorted((root / \"scripts\").glob(\"*.py\"))\n+    files += sorted((root / \"scripts\").glob(\"*.sh\"))\n     if include_docs:\n         docs = root / \"docs\"\n         if docs.is_dir():\n             files += sorted(docs.rglob(\"*.md\"))\n         files += sorted(root.glob(\"*.md\"))\n-    return files\n+        pyproject = root / \"pyproject.toml\"\n+        if pyproject.is_file():\n+            files.append(pyproject)\n+        for relative in (\".github\", \".codex\"):\n+            surface = root / relative\n+            if surface.is_dir():\n+                files += sorted(\n+                    path\n+                    for path in surface.rglob(\"*\")\n+                    if path.is_file()\n+                    and path.suffix in {\".md\", \".py\", \".sh\", \".toml\", \".yaml\", \".yml\"}\n+                )\n+    return sorted(set(files))\n \n \n def find_adr_reference_violations(\n@@ -94,10 +111,9 @@\n ) -> list[Violation]:\n     \"\"\"Return every ``ADR ####`` citation under ``root`` that fails to resolve.\n \n-    A citation resolves when exactly one ``docs/adr/####-*.md`` matches its\n-    number. Zero matches is a \"missing\" violation; two or more is \"ambiguous\".\n-    Records that are never cited produce nothing -- the converse is not\n-    required.\n+    A citation resolves when exactly one record in the same namespace matches\n+    its number. Zero matches is a \"missing\" violation; two or more is\n+    \"ambiguous\". Records that are never cited produce nothing.\n     \"\"\"\n     root = Path(root)\n     index = _adr_index(root)\n@@ -110,13 +126,15 @@\n         rel = path.relative_to(root).as_posix()\n         for lineno, line in enumerate(text.splitlines(), start=1):\n             for match in _CITATION.finditer(line):\n-                number = match.group(1)\n-                found = index.get(number, [])\n+                namespace, number = match.groups()\n+                found = index.get((namespace, number), [])\n                 if len(found) == 1:\n                     continue\n                 reason = \"missing\" if not found else \"ambiguous\"\n                 rels = [p.relative_to(root).as_posix() for p in found]\n-                violations.append(Violation(rel, number, lineno, reason, rels))\n+                violations.append(\n+                    Violation(rel, namespace, number, lineno, reason, rels)\n+                )\n     return violations\n \n \n@@ -129,8 +147,8 @@\n         self.assertEqual(\n             violations,\n             [],\n-            \"Every 'ADR ####' citation must resolve to exactly one \"\n-            f\"docs/adr/####-*.md:\\n{detail}\",\n+            \"Every ADR/SADR citation must resolve exactly once in its own \"\n+            f\"docs/adr namespace:\\n{detail}\",\n         )\n \n     def test_scan_actually_reads_scripts(self) -> None:\n@@ -146,8 +164,21 @@\n         # The two records this Goal carried onto the default branch, and which\n         # the scripts cite most, must be present.\n         index = _adr_index(REPO_ROOT)\n-        self.assertIn(\"0016\", index)\n-        self.assertIn(\"0017\", index)\n+        self.assertIn((\"SADR\", \"0016\"), index)\n+        self.assertIn((\"SADR\", \"0017\"), index)\n+\n+    def test_platform_tree_has_only_sadr_0001_through_0019(self) -> None:\n+        adr_dir = REPO_ROOT / \"docs\" / \"adr\"\n+        sadrs = sorted(adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\"))\n+        self.assertEqual(\n+            {path.name.split(\"-\", 2)[1] for path in sadrs},\n+            {f\"{number:04d}\" for number in range(1, 20)},\n+        )\n+        self.assertEqual(\n+            list(adr_dir.glob(\"[0-9][0-9][0-9][0-9]-*.md\")),\n+            [],\n+            \"the Shiki platform tree must not retain legacy numeric SADR paths\",\n+        )\n \n \n class FixtureAdrReferences(unittest.TestCase):\n@@ -180,6 +211,26 @@\n         self.assertIn(\"9991\", message)  # cited number\n         self.assertIn(\"docs/adr/9991-*.md\", message)  # missing path\n \n+    def test_target_adr_does_not_satisfy_missing_platform_sadr(self) -> None:\n+        root = self._make_repo(\n+            adr_names=[\"0042-target-decision.md\"],\n+            script_body='\"\"\"Target ADR 0042 exists, but platform SADR-0042 does not.\"\"\"\\n',\n+        )\n+        violations = find_adr_reference_violations(root, include_docs=False)\n+        self.assertEqual(len(violations), 1)\n+        self.assertEqual(violations[0].namespace, \"SADR\")\n+        self.assertEqual(violations[0].number, \"0042\")\n+        self.assertEqual(violations[0].expected_path, \"docs/adr/SADR-0042-*.md\")\n+\n+    def test_target_adr_and_platform_sadr_may_reuse_a_number(self) -> None:\n+        root = self._make_repo(\n+            adr_names=[\"0042-target-decision.md\", \"SADR-0042-platform-decision.md\"],\n+            script_body='\"\"\"Target ADR 0042 and platform SADR-0042 are distinct.\"\"\"\\n',\n+        )\n+        self.assertEqual(\n+            find_adr_reference_violations(root, include_docs=False), []\n+        )\n+\n     def test_uncited_adr_is_allowed(self) -> None:\n         # An ADR exists but nothing cites it -> not a violation. The converse\n         # (every ADR must be cited) is explicitly NOT required.\ndiff --git a/tests/test_guardian_review.py b/tests/test_guardian_review.py\nindex 88829e7810212635e2fc98aa5918d6f26294677d..00a85a237acd5bf2d6cb5b294128198c422402ca\n--- a/tests/test_guardian_review.py\n+++ b/tests/test_guardian_review.py\n@@ -162,7 +162,7 @@\n \n     def test_docs_only_is_light_and_not_conservative(self):\n         focus = gr.classify_review_focus([\"docs/adr/0099-x.md\", \"README.md\"], \"\", \"low\")\n-        self.assertTrue(any(\"Docs/ADR-only\" in f for f in focus))\n+        self.assertTrue(any(\"Docs/decision-record-only\" in f for f in focus))\n         self.assertFalse(any(\"conservative default\" in f.lower() for f in focus))\n \n     def test_unknown_category_is_conservative(self):\ndiff --git a/tests/test_policy_job_merged_prs.py b/tests/test_policy_job_merged_prs.py\nindex d45512e74681e5e649279236c7d4e79d5c3da471..b97989f8bd74d091de1a6b5275a20d8dc68c4512\n--- a/tests/test_policy_job_merged_prs.py\n+++ b/tests/test_policy_job_merged_prs.py\n@@ -3,8 +3,8 @@\n \n ``.github/workflows/shiki-cca-completion.yml`` has two consumers of the merged-PR\n proof: the CCA *signal* (``guardian_approval_signal.py`` in \"Prepare CCA context\")\n-and the authoritative gate (``mergegate_check.py`` in \"Enforce MergeGate\"). ADR\n-0017's bookkeeping-closeout exemption (condition 6) needs the merged implementation\n+and the authoritative gate (``mergegate_check.py`` in \"Enforce MergeGate\").\n+SADR-0017's bookkeeping-closeout exemption (condition 6) needs the merged implementation\n PR proven, and a closeout carries NO ``post_merge_reconcile`` marker. The signal\n already resolves ``MERGED_PRS`` unconditionally, but \"Enforce MergeGate\" used to\n resolve it only inside ``grep -q '<!-- shiki:post_merge_reconcile -->'``. So a\ndiff --git a/tests/test_sadr_install_migration.py b/tests/test_sadr_install_migration.py\nnew file mode 100644\nindex 0000000000000000000000000000000000000000..a6f4c57e4dcaef73ef600b0d4804744138ab9071\n--- /dev/null\n+++ b/tests/test_sadr_install_migration.py\n@@ -0,0 +1,229 @@\n+\"\"\"SADR installation and collision-safe legacy upgrade behavior.\"\"\"\n+\n+from __future__ import annotations\n+\n+import contextlib\n+import hashlib\n+import io\n+import json\n+import shutil\n+import tempfile\n+import unittest\n+from pathlib import Path\n+\n+import shiki_test_support  # noqa: F401  (path bootstrap)\n+\n+import shiki_installer\n+from tests.test_adr_references import find_adr_reference_violations\n+\n+\n+PRODUCT_NUMBERS = tuple(range(13, 19))\n+\n+\n+def _quiet_install(target: Path, *, force: bool, validate: bool) -> None:\n+    with contextlib.redirect_stdout(io.StringIO()):\n+        shiki_installer.install_template(target, force=force, validate=validate)\n+\n+\n+def _sha256(path: Path) -> str:\n+    return hashlib.sha256(path.read_bytes()).hexdigest()\n+\n+\n+def _legacy_sadr_paths() -> tuple[str, ...]:\n+    return tuple(\n+        f\"docs/adr/{path.name.removeprefix('SADR-')}\"\n+        for path in sorted((shiki_installer.ROOT / \"docs\" / \"adr\").glob(\"SADR-*.md\"))\n+        if path.name != \"SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\"\n+    )\n+\n+\n+def _create_legacy_upgrade_fixture(\n+    target: Path,\n+) -> tuple[dict[str, bytes], tuple[str, ...]]:\n+    adr_dir = target / \"docs\" / \"adr\"\n+    adr_dir.mkdir(parents=True)\n+    product_bytes = {\n+        f\"{number:04d}-cubeshare-product-decision.md\": (\n+            f\"# CubeShare ADR {number:04d}\\n\\nDo not rewrite {number}.\\n\"\n+        ).encode()\n+        for number in PRODUCT_NUMBERS\n+    }\n+    for name, content in product_bytes.items():\n+        (adr_dir / name).write_bytes(content)\n+\n+    legacy_paths = _legacy_sadr_paths()\n+    for relative in legacy_paths:\n+        (target / relative).write_bytes(f\"legacy Shiki bytes for {relative}\\n\".encode())\n+\n+    stamp_path = target / shiki_installer.INSTALL_STAMP_PATH\n+    stamp_path.parent.mkdir(parents=True, exist_ok=True)\n+    stamp = {\n+        \"version\": shiki_installer.INSTALL_STAMP_VERSION,\n+        \"platform_commit\": None,\n+        \"installed_at\": \"2000-01-01T00:00:00+00:00\",\n+        \"digests\": {\n+            relative: _sha256(target / relative) for relative in legacy_paths\n+        },\n+    }\n+    stamp_path.write_text(\n+        json.dumps(stamp, indent=2, sort_keys=True) + \"\\n\", encoding=\"utf-8\"\n+    )\n+    return product_bytes, legacy_paths\n+\n+\n+def _tree_snapshot(root: Path) -> dict[str, tuple[str, bytes | str]]:\n+    \"\"\"Recursive byte/shape snapshot used to prove preflight is write-free.\"\"\"\n+    snapshot: dict[str, tuple[str, bytes | str]] = {}\n+    for path in sorted(root.rglob(\"*\")):\n+        relative = path.relative_to(root).as_posix()\n+        if path.is_symlink():\n+            snapshot[relative] = (\"symlink\", path.readlink().as_posix())\n+        elif path.is_dir():\n+            snapshot[relative] = (\"directory\", \"\")\n+        else:\n+            snapshot[relative] = (\"file\", path.read_bytes())\n+    return snapshot\n+\n+\n+class FreshSadrInstallTests(unittest.TestCase):\n+    def setUp(self) -> None:\n+        self.tmp = Path(tempfile.mkdtemp(prefix=\"shiki-sadr-fresh-\"))\n+        self.addCleanup(lambda: shutil.rmtree(self.tmp, ignore_errors=True))\n+        self.target = self.tmp / \"target\"\n+        self.adr_dir = self.target / \"docs\" / \"adr\"\n+        self.adr_dir.mkdir(parents=True)\n+        self.product_bytes = {\n+            f\"{number:04d}-product-decision.md\": (\n+                f\"# Product ADR {number:04d}\\n\\nTarget-owned bytes {number}.\\n\"\n+            ).encode()\n+            for number in PRODUCT_NUMBERS\n+        }\n+        for name, content in self.product_bytes.items():\n+            (self.adr_dir / name).write_bytes(content)\n+\n+    def test_fresh_install_preserves_product_adrs_and_installs_all_sadrs(self) -> None:\n+        _quiet_install(self.target, force=False, validate=True)\n+\n+        for name, expected in self.product_bytes.items():\n+            self.assertEqual((self.adr_dir / name).read_bytes(), expected)\n+\n+        sadr_numbers = {\n+            path.name.split(\"-\", 2)[1]\n+            for path in self.adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\")\n+        }\n+        self.assertEqual(sadr_numbers, {f\"{number:04d}\" for number in range(1, 20)})\n+\n+        numeric_records = {\n+            path.name\n+            for path in self.adr_dir.glob(\"[0-9][0-9][0-9][0-9]-*.md\")\n+        }\n+        self.assertEqual(numeric_records, set(self.product_bytes))\n+        self.assertEqual(find_adr_reference_violations(self.target), [])\n+\n+\n+class LegacySadrUpgradeTests(unittest.TestCase):\n+    def setUp(self) -> None:\n+        self.tmp = Path(tempfile.mkdtemp(prefix=\"shiki-sadr-upgrade-\"))\n+        self.addCleanup(lambda: shutil.rmtree(self.tmp, ignore_errors=True))\n+        self.target = self.tmp / \"target\"\n+        self.adr_dir = self.target / \"docs\" / \"adr\"\n+        self.product_bytes, self.legacy_paths = _create_legacy_upgrade_fixture(\n+            self.target\n+        )\n+\n+    def test_force_upgrade_deletes_only_stamped_legacy_sadrs(self) -> None:\n+        _quiet_install(self.target, force=True, validate=True)\n+\n+        for relative in self.legacy_paths:\n+            self.assertFalse((self.target / relative).exists(), relative)\n+        for name, expected in self.product_bytes.items():\n+            self.assertEqual((self.adr_dir / name).read_bytes(), expected)\n+\n+        sadrs = list(self.adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\"))\n+        self.assertEqual(len(sadrs), 19)\n+        refreshed = json.loads(\n+            (self.target / shiki_installer.INSTALL_STAMP_PATH).read_text(encoding=\"utf-8\")\n+        )\n+        for relative in self.legacy_paths:\n+            self.assertNotIn(relative, refreshed[\"digests\"])\n+        for path in sadrs:\n+            relative = path.relative_to(self.target).as_posix()\n+            self.assertEqual(refreshed[\"digests\"][relative], _sha256(path))\n+\n+    def test_non_force_install_never_deletes_legacy_files(self) -> None:\n+        before = {\n+            relative: (self.target / relative).read_bytes()\n+            for relative in self.legacy_paths\n+        }\n+        _quiet_install(self.target, force=False, validate=False)\n+        for relative, expected in before.items():\n+            self.assertEqual((self.target / relative).read_bytes(), expected)\n+        refreshed = json.loads(\n+            (self.target / shiki_installer.INSTALL_STAMP_PATH).read_text(encoding=\"utf-8\")\n+        )\n+        for relative in self.legacy_paths:\n+            self.assertEqual(refreshed[\"digests\"][relative], _sha256(self.target / relative))\n+\n+\n+class LegacyCleanupPreflightTests(unittest.TestCase):\n+    def setUp(self) -> None:\n+        self.tmp = Path(tempfile.mkdtemp(prefix=\"shiki-sadr-preflight-\"))\n+        self.addCleanup(lambda: shutil.rmtree(self.tmp, ignore_errors=True))\n+\n+    def test_all_ownership_blockers_abort_before_any_write(self) -> None:\n+        cases = (\n+            (\"absent\", \"install stamp is absent\"),\n+            (\"unreadable\", \"install stamp is unreadable\"),\n+            (\"malformed\", \"install stamp is malformed\"),\n+            (\"missing-digest\", \"install stamp has no digest\"),\n+            (\"digest-mismatch\", \"digest mismatch\"),\n+        )\n+        for name, expected_reason in cases:\n+            with self.subTest(name=name):\n+                target = self.tmp / name\n+                _, legacy_paths = _create_legacy_upgrade_fixture(target)\n+                stamp_path = target / shiki_installer.INSTALL_STAMP_PATH\n+\n+                if name == \"absent\":\n+                    stamp_path.unlink()\n+                elif name == \"unreadable\":\n+                    stamp_path.unlink()\n+                    stamp_path.mkdir()\n+                elif name == \"malformed\":\n+                    stamp_path.write_text(\"{not-json\\n\", encoding=\"utf-8\")\n+                else:\n+                    stamp = json.loads(stamp_path.read_text(encoding=\"utf-8\"))\n+                    if name == \"missing-digest\":\n+                        stamp[\"digests\"] = {}\n+                    else:\n+                        stamp[\"digests\"] = {\n+                            relative: \"deadbeef\" for relative in legacy_paths\n+                        }\n+                    stamp_path.write_text(\n+                        json.dumps(stamp, indent=2, sort_keys=True) + \"\\n\",\n+                        encoding=\"utf-8\",\n+                    )\n+\n+                before = _tree_snapshot(target)\n+                with self.assertRaises(shiki_installer.ShikiError) as raised:\n+                    _quiet_install(target, force=True, validate=False)\n+                after = _tree_snapshot(target)\n+\n+                self.assertEqual(before, after, f\"{name} preflight changed the tree\")\n+                message = str(raised.exception)\n+                self.assertIn(expected_reason, message)\n+                self.assertIn(\"No files were written\", message)\n+                for relative in legacy_paths:\n+                    self.assertIn(relative, message)\n+\n+    def test_absent_stamp_is_allowed_when_no_legacy_path_exists(self) -> None:\n+        target = self.tmp / \"no-legacy\"\n+        target.mkdir()\n+        _quiet_install(target, force=True, validate=False)\n+        self.assertTrue(\n+            (target / \"docs\" / \"adr\" / \"SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\").is_file()\n+        )\n+\n+\n+if __name__ == \"__main__\":\n+    unittest.main()\n\n succeeded in 1564ms:\nok\ntest_absent_stamp_is_allowed_when_no_legacy_path_exists (test_sadr_install_migration.LegacyCleanupPreflightTests.test_absent_stamp_is_allowed_when_no_legacy_path_exists) ... ok\ntest_all_ownership_blockers_abort_before_any_write (test_sadr_install_migration.LegacyCleanupPreflightTests.test_all_ownership_blockers_abort_before_any_write) ... ok\ntest_force_upgrade_deletes_only_stamped_legacy_sadrs (test_sadr_install_migration.LegacySadrUpgradeTests.test_force_upgrade_deletes_only_stamped_legacy_sadrs) ... ok\ntest_non_force_install_never_deletes_legacy_files (test_sadr_install_migration.LegacySadrUpgradeTests.test_non_force_install_never_deletes_legacy_files) ... ok\n\n----------------------------------------------------------------------\nRan 5 tests in 1.540s\n\nOK\n\nexec\n/bin/zsh -lc \"rg -n 'install-target|--force|install stamp|upgrade|docs/adr' docs README.md AGENTS.md CLAUDE.md SYSTEM_PROMPT.md CONTRIBUTING.md .codex .github --glob '\"'!.shiki/**'\"' | sed -n '1,360p'\" in /private/tmp/shiki-adr-collision.pa0KwY/.worktrees/shiki-t-20260807t073307221254z-2d22d654-introduc\n succeeded in 0ms:\nCONTRIBUTING.md:106:record durable decisions in a Shiki SADR or target ADR as applicable (see `docs/adr/`).\nSYSTEM_PROMPT.md:29:4. `docs/adr/` records hard-to-reverse platform decisions.\nSYSTEM_PROMPT.md:33:Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\nCLAUDE.md:13:4. `docs/adr/` records hard-to-reverse platform decisions.\nCLAUDE.md:17:Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\nCLAUDE.md:51:- `docs/adr/`\nREADME.md:64:4. `docs/adr/` records hard-to-reverse platform decisions.\nREADME.md:78:| `docs/adr/` | Architecture Decision Records. |\nREADME.md:102:  vs `install-target` vs `bootstrap-platform` vs `/shiki`.\nREADME.md:103:- **[docs/operations.md](docs/operations.md)** \u2014 install, upgrade, rollback, and\nAGENTS.md:43:4. `docs/adr/` records hard-to-reverse platform decisions.\nAGENTS.md:47:Within `docs/adr/`, Shiki platform decisions use\nAGENTS.md:403:Shiki is a single-context platform repository unless `CONTEXT-MAP.md` says otherwise. `CONTEXT.md` is the glossary and `docs/adr/` holds durable decisions. See `docs/agents/domain.md`.\n.codex/skills/shiki/SKILL.md:17:4. `docs/adr/` records hard-to-reverse platform decisions.\n.codex/skills/shiki/SKILL.md:21:Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n.codex/skills/shiki/SKILL.md:94:- Do not use `shiki install-target` unless the user explicitly asks for local-only template copying.\ndocs/releasing.md:3:This document defines how Shiki is versioned, released, installed, upgraded, and\ndocs/releasing.md:6:[docs/adr/SADR-0007-packaging-and-release.md](adr/SADR-0007-packaging-and-release.md).\ndocs/releasing.md:105:  (see [docs/adr/SADR-0007-packaging-and-release.md](adr/SADR-0007-packaging-and-release.md)).\ndocs/releasing.md:109:  and the CLI install subcommands (`shiki install-target`,\ndocs/releasing.md:138:Target repositories upgrade by re-installing the newer template surface and then\ndocs/releasing.md:142:   (see Install paths above) **with `--force`**. `--force` is the flag that makes\ndocs/releasing.md:143:   a re-run an upgrade: without it an existing file is kept (a `kept existing\ndocs/releasing.md:145:   is not idempotent proof that the target changed. Under `--force` the shipped\ndocs/releasing.md:156:   pending migrations, `--force` refuses and writes nothing; apply migrations\ndocs/releasing.md:170:3. Re-validate the upgraded mirror:\n.github/prompts/cca-completion-check.md:18:4. `docs/adr/` records hard-to-reverse platform decisions.\n.github/prompts/cca-completion-check.md:22:Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n.github/prompts/cca-completion-check.md:32:4. relevant `docs/adr/`\ndocs/agents/implementation-policy.md:30:4. `docs/adr/` records hard-to-reverse platform decisions.\ndocs/agents/implementation-policy.md:34:Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\ndocs/agents/implementation-policy.md:57:- Install or define `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, `docs/adr/`, and `docs/agents/`.\ndocs/adr/SADR-0007-packaging-and-release.md:12:mechanism, and no documented install/upgrade/rollback path. P2.2 (Goal G-0012,\ndocs/adr/SADR-0007-packaging-and-release.md:19:- an upgrade/migration path (P2.2.5),\ndocs/adr/SADR-0007-packaging-and-release.md:83:- Document upgrade via re-install plus `shiki migrate {status,plan,apply}`, and\ndocs/adr/SADR-0007-packaging-and-release.md:116:  and documented install/upgrade/rollback paths.\n.github/workflows/release.yml:6:# docs/adr/SADR-0007-packaging-and-release.md.\ndocs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md:9:Shiki installs its platform decision records into `docs/adr/` in every target\ndocs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md:18:mistaking a target-owned ADR for platform content. The install stamp already\ndocs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md:26:- records are named `docs/adr/SADR-NNNN-<slug>.md`;\ndocs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md:28:- target product decisions retain `docs/adr/NNNN-<slug>.md` and `ADR NNNN`;\ndocs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md:32:Before a forced upgrade writes anything, the installer inventories the exact\ndocs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md:34:existing install stamp contains that exact path and its recorded SHA-256 digest\ndocs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md:36:together and abort the upgrade before its first write. A non-forced install\ndocs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md:46:- Forced upgrades fail closed when legacy ownership cannot be proven, and a\ndocs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md:47:  successful upgrade refreshes the stamp without removed legacy paths.\ndocs/agents/domain.md:8:- `docs/adr/SADR-NNNN-*.md` records Shiki platform decisions.\ndocs/agents/domain.md:9:- `docs/adr/NNNN-*.md` records target product decisions without sharing the\ndocs/adr/README.md:18:- The canonical list is the files in `docs/adr/`; no hand-maintained index is\ndocs/adr/README.md:31:1. Copy `template.md` to `docs/adr/SADR-NNNN-<short-slug>.md`.\ndocs/adr/README.md:39:1. Copy `template.md` to `docs/adr/NNNN-<short-slug>.md` using the target's own\ndocs/agents/runtime-auth-model.md:163:24 action upgrades for official actions in those workflow files must land\ndocs/agents/bootstrap-command.md:74:Do not use `install-target` for normal setup. Shiki is GitHub-first.\ndocs/agents/bootstrap-command.md:188:shiki install-target /path/to/target-repo --local-only\ndocs/agents/bootstrap-command.md:192:Use `--force` only when you intentionally want to overwrite existing target files.\ndocs/usage.md:14:| `shiki install-target` | Local-only template copy for tests, fixtures, or inspection. No GitHub. | Writes local files only. |\ndocs/usage.md:60:## `shiki install-target` \u2014 local-only template copy\ndocs/usage.md:62:Use `shiki install-target` only for tests, fixtures, or explicit local-only\ndocs/usage.md:67:shiki install-target /path/to/target-repo --local-only\ndocs/usage.md:70:Use `--force` only when you intentionally want to overwrite existing target\ndocs/usage.md:122:sequence and [`docs/operations.md`](operations.md) for install/upgrade/rollback/\ndocs/operations.md:4:**upgrade**, **rollback**, and **migration**. It complements\ndocs/operations.md:72:2. **Refresh target template files with `--force`.** Re-running the bootstrap\ndocs/operations.md:73:   without `--force` is **not** an upgrade: an existing file is kept with a\ndocs/operations.md:75:   looks refreshed when nothing changed. Pass `--force` to actually refresh the\ndocs/operations.md:76:   template. Re-run in dry-run first to preview, then `--force --execute` to\ndocs/operations.md:77:   apply. Under `--force` the shipped surface splits three ways so nothing is\ndocs/operations.md:92:   If the target has pending migrations, `--force` refuses and writes nothing \u2014\ndocs/operations.md:94:   summary of every `.new` written, so a half-upgrade is visible.\ndocs/operations.md:102:applying an upgrade.\ndocs/operations.md:106:Under `--force`, a file that must not be overwritten (project content and the\ndocs/operations.md:169:Run these from the repository root for any install, upgrade, rollback, or\n\ndiff --git a/.codex/skills/shiki/SKILL.md b/.codex/skills/shiki/SKILL.md\nindex c8e2e85d9d75d49cc4f2d6286ee22659c4e53e75..93cded623b2c15d9f65c76491a1c2d813ac9e6c0\n--- a/.codex/skills/shiki/SKILL.md\n+++ b/.codex/skills/shiki/SKILL.md\n@@ -18,6 +18,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n Codex CLI does not register this skill as a custom `/shiki` slash command. When\n the user types `/shiki` in Codex CLI and gets \"Unrecognized command\", explain\n that this is expected: invoke Shiki in Codex with natural language such as\n@@ -69,7 +72,7 @@\n \n ## Responsibilities\n \n-- Claude Code implements and repairs by default (`shiki runner claude`, ADR 0008); Codex implements and repairs tasks explicitly assigned to `codex`.\n+- Claude Code implements and repairs by default (`shiki runner claude`, SADR-0008); Codex implements and repairs tasks explicitly assigned to `codex`.\n - Claude Code Action can act as GitHub-side CCA or reviewer.\n - CCA judges completion.\n - MergeGate authorizes state transitions and merge readiness.\ndiff --git a/.github/prompts/cca-completion-check.md b/.github/prompts/cca-completion-check.md\nindex ed203ad5f302ecc81372d8ab27d2b1232ca97497..fd89950528333ef6242e6d5443904f5392ab7e56\n--- a/.github/prompts/cca-completion-check.md\n+++ b/.github/prompts/cca-completion-check.md\n@@ -19,6 +19,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n ## Required Reading\n \n Read only the files needed to judge the PR contract. Prefer this order:\ndiff --git a/.github/prompts/codex-task-handoff.md b/.github/prompts/codex-task-handoff.md\nindex 695e211d76dea1c60e8d5bb360e78487808286a4..59441d576b01a39569722f226c854e5a740e9eb5\n--- a/.github/prompts/codex-task-handoff.md\n+++ b/.github/prompts/codex-task-handoff.md\n@@ -2,7 +2,7 @@\n \n You are Codex Front acting as the Implementer for a Shiki task.\n \n-Follow `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant ADRs, and `docs/agents/codex-handoff.md`.\n+Follow `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant Shiki SADRs and target ADRs, and `docs/agents/codex-handoff.md`.\n \n Rules:\n \ndiff --git a/AGENTS.md b/AGENTS.md\nindex 7f041100d49c2a7525f51cb0f5d696a4a6d1c78c..a06bfbabc6fa48a007389e25e12d7ed523936bed\n--- a/AGENTS.md\n+++ b/AGENTS.md\n@@ -22,7 +22,7 @@\n - **Codex Front** is an optional implementer through Codex App, Codex CLI, Codex IDE extension, or Codex Web signed in with ChatGPT OAuth/subscription auth, dispatched with `shiki runner codex` when a task is explicitly assigned to `codex`.\n - **GitHub CCA** is implemented by Claude Code Action by default, using `CLAUDE_CODE_OAUTH_TOKEN`.\n \n-Do not assume `openai/codex-action`, `OPENAI_API_KEY`, or API-key based Codex automation in the default Shiki path. API-key runners are explicit target-repo extensions and require their own ADR.\n+Do not assume `openai/codex-action`, `OPENAI_API_KEY`, or API-key based Codex automation in the default Shiki path. API-key runners are explicit target-repo extensions and require their own target ADR.\n \n Shiki is not:\n \n@@ -44,10 +44,14 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use\n+`SADR-NNNN-*.md`; target product decisions retain `NNNN-*.md` and the `ADR`\n+namespace.\n+\n Use the canonical source-of-truth order above. Runtime-specific reading orders\n are operational guidance only and must not override or reorder it.\n \n-Conversation state is not durable truth. If a decision matters, put it in GitHub, `.shiki/`, `CONTEXT.md`, or an ADR.\n+Conversation state is not durable truth. If a decision matters, put it in GitHub, `.shiki/`, `CONTEXT.md`, a Shiki SADR, or a target ADR.\n \n If GitHub and `.shiki/` disagree, surface the conflict and prefer GitHub operational state until the mirror is repaired.\n \n@@ -75,8 +79,8 @@\n Every non-trivial change follows this loop:\n \n 1. **Goal**: clarify outcome, completion conditions, non-goals, risk level, and success signals.\n-2. **grill-with-docs**: challenge the plan against domain docs, ADRs, code reality, terminology, and edge scenarios.\n-3. **Context & Impact**: find relevant docs, ADRs, code, dependencies, owners, locks, and verification surfaces.\n+2. **grill-with-docs**: challenge the plan against domain docs, Shiki SADRs, target ADRs, code reality, terminology, and edge scenarios.\n+3. **Context & Impact**: find relevant docs, SADRs, target ADRs, code, dependencies, owners, locks, and verification surfaces.\n 4. **PRD and Spec Freeze**: use `to-prd` when context is settled enough to become durable product/engineering intent. The operator's explicit approval of the PRD is Spec Freeze: the plan records a `spec_freeze` block, and no plan runs without it. Steps 1-4 form Requirements Definition \u2014 one continuous operator dialogue. After Spec Freeze, scope changes require an operator-approved Spec Amendment; non-scope-moving interpretations go to the Assumption Log.\n 5. **Issues**: use `to-issues` to create independently grabbable vertical-slice issues. Prefer AFK slices over HITL slices where possible.\n 6. **Triage**: label issues for readiness, risk, runtime, skills, and MergeGate state.\n@@ -167,7 +171,7 @@\n - Testing decisions.\n - Out-of-scope items.\n - Further notes.\n-- Links to relevant domain terms and ADRs.\n+- Links to relevant domain terms, SADRs, and target ADRs.\n \n Every executable task must include:\n \n@@ -193,7 +197,7 @@\n \n Context & Impact must identify:\n \n-- Relevant documents, domain terms, ADRs, and past decisions.\n+- Relevant documents, domain terms, SADRs, target ADRs, and past decisions.\n - Relevant modules, interfaces, seams, callers, owners, and tests.\n - Dependency relationships between tasks.\n - Candidate locks and lock conflicts.\n@@ -210,7 +214,7 @@\n | Skill | Required when |\n | --- | --- |\n | `setup-matt-pocock-skills` | First configuring the repo for skills, or when issue tracker, triage labels, or domain docs are missing. |\n-| `grill-with-docs` | Default for non-trivial Goals before PRD/issues; always required when plans, terminology, boundaries, tradeoffs, or ADR-worthy decisions are ambiguous. |\n+| `grill-with-docs` | Default for non-trivial Goals before PRD/issues; always required when plans, terminology, boundaries, tradeoffs, or decision-record-worthy choices are ambiguous. |\n | `zoom-out` | The agent lacks an architectural map of the relevant code area. |\n | `to-prd` | Settled Goal context must become durable product/engineering intent. |\n | `to-issues` | A Goal, PRD, or plan must become independently grabbable vertical-slice issues. |\n@@ -348,7 +352,7 @@\n - Three or more new files.\n - Any high-risk or critical label.\n \n-When the architecture gate triggers, run `grill-with-docs` or `improve-codebase-architecture` as appropriate and record durable decisions in an ADR when needed.\n+When the architecture gate triggers, run `grill-with-docs` or `improve-codebase-architecture` as appropriate and record durable decisions in a Shiki SADR or target ADR as applicable.\n \n ## Engineering Discipline\n \ndiff --git a/CLAUDE.md b/CLAUDE.md\nindex 98a932e7841a632866fcd53b0bad0e3af3711d3d..98a31399404d6fb044b4fd493f4459ac87f439de\n--- a/CLAUDE.md\n+++ b/CLAUDE.md\n@@ -14,6 +14,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n ## Claude Role\n \n Default to planner, default implementer, reviewer, coordinator, documentation editor, CCA-style completion judge, and final-judgment assistant.\n@@ -57,7 +60,7 @@\n At the start of a Shiki session:\n \n 1. Read `AGENTS.md`.\n-2. Read `CONTEXT.md` and relevant ADRs.\n+2. Read `CONTEXT.md` and relevant Shiki SADRs and target ADRs.\n 3. Read active `.shiki/` Goal/task/ledger state when present.\n 4. Read active distilled rules from `.shiki/memories/` (status `distilled`, `active: true`, not revoked or superseded) for applicable guidance; the same rules are injected deterministically into task handoffs as the `## Distilled Rules` section (\u00a73.5).\n 5. Read the GitHub Issue or PR when the task is GitHub-backed.\n@@ -74,7 +77,7 @@\n - Define the Goal, completion conditions, non-goals, and risk level.\n - Treat `grill-with-docs` as the default planning entry point for non-trivial Goals.\n - Challenge domain terms against `CONTEXT.md`.\n-- Challenge hard-to-reverse tradeoffs against ADRs.\n+- Challenge hard-to-reverse tradeoffs against SADRs and target ADRs.\n - Explore code instead of asking when code can answer the question.\n - Run Context & Impact before implementation planning.\n - Produce a Task DAG, not an unordered checklist.\n@@ -94,7 +97,7 @@\n - For each question, provide your recommended answer.\n - Resolve dependency decisions before downstream design questions.\n - Update `CONTEXT.md` inline when terminology crystallizes.\n-- Offer ADRs only when the decision is hard to reverse, surprising without context, and a real tradeoff.\n+- Offer a SADR or target ADR only when the decision is hard to reverse, surprising without context, and a real tradeoff.\n - Convert unresolved questions into HITL blockers rather than allowing Codex to guess.\n \n ## PRD and Issues Standard\n@@ -103,7 +106,7 @@\n \n - Use the project's domain glossary vocabulary.\n - Avoid volatile file paths unless they are essential.\n-- Include implementation decisions, testing decisions, out-of-scope items, and relevant ADR/domain links.\n+- Include implementation decisions, testing decisions, out-of-scope items, and relevant SADR/target-ADR/domain links.\n - Identify deep-module opportunities and testable interfaces.\n \n When creating issues:\n@@ -121,7 +124,7 @@\n - Branch/worktree target.\n - Scope and non-goals.\n - Dependencies and locks.\n-- Relevant docs, ADRs, modules, and tests.\n+- Relevant docs, SADRs, target ADRs, modules, and tests.\n - Required skill invocations.\n - TDD expectations.\n - Pre-PR `code-review` gate expectations.\n@@ -152,7 +155,7 @@\n \n - Treat yourself as a judge, not implementer.\n - Do not write production code.\n-- Read `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant ADRs, PRD, task issue, PR body, diff, checks, reviews, labels, and `.shiki/` state.\n+- Read `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant SADRs and target ADRs, PRD, task issue, PR body, diff, checks, reviews, labels, and `.shiki/` state.\n - Evaluate every applicable checklist in `docs/agents/checklists.md`.\n - Map each acceptance criterion to durable evidence.\n - Distinguish implementation failure from missing evidence.\ndiff --git a/CONTEXT.md b/CONTEXT.md\nindex e61a9be06a0cce0d026484083a657bde2067e234..237ee8aa3ea8fab334ece49768bc5ed3b5af0974\n--- a/CONTEXT.md\n+++ b/CONTEXT.md\n@@ -25,7 +25,7 @@\n _Avoid_: jumping directly into code\n \n **grill-with-docs**:\n-A planning skill that challenges a plan against domain language, ADRs, code reality, and concrete edge scenarios. It resolves design-tree questions before PRD/issues. It runs inside Requirements Definition.\n+A planning skill that challenges a plan against domain language, Shiki SADRs, target ADRs, code reality, and concrete edge scenarios. It resolves design-tree questions before PRD/issues. It runs inside Requirements Definition.\n _Avoid_: generic brainstorming, silent assumptions\n \n **Requirements Definition**:\ndiff --git a/CONTRIBUTING.md b/CONTRIBUTING.md\nindex c6d9b656e73abb8a55e5f660123e03153dcb3e6d..9e37a8fbf668db5b22461aaae5548b8f1354394c\n--- a/CONTRIBUTING.md\n+++ b/CONTRIBUTING.md\n@@ -17,7 +17,7 @@\n 1. **Goal** \u2014 start from a user-approved Goal with an outcome, completion\n    conditions, non-goals, risk level, and success signals.\n 2. **grill-with-docs** \u2014 for any non-trivial change, challenge the plan against\n-   domain language, ADRs, and code reality before writing a PRD.\n+   domain language, Shiki SADRs, target ADRs, and code reality before writing a PRD.\n 3. **PRD** \u2014 capture settled product/engineering intent (`to-prd`).\n 4. **Spec Freeze** \u2014 the operator explicitly approves the PRD/requirements;\n    the plan records a `spec_freeze` block and no plan runs without it\n@@ -94,7 +94,7 @@\n   entries.\n - Do not hand-edit `.shiki/goals/*.json` or `.shiki/manifest.json` in a task PR.\n - Conversation state is not durable truth. If a decision matters, record it in\n-  GitHub, `.shiki/`, `CONTEXT.md`, or an ADR.\n+  GitHub, `.shiki/`, `CONTEXT.md`, a Shiki SADR, or a target ADR.\n \n ## Architecture Gate\n \n@@ -103,7 +103,7 @@\n or CI policy, package structure, three or more structural files, more than 100\n net new lines in a core module, three or more new files, or any high-risk label.\n When triggered, run `grill-with-docs` or `improve-codebase-architecture` and\n-record durable decisions in an ADR when needed (see `docs/adr/`).\n+record durable decisions in a Shiki SADR or target ADR as applicable (see `docs/adr/`).\n \n ## Safety\n \ndiff --git a/SYSTEM_PROMPT.md b/SYSTEM_PROMPT.md\nindex aaaea45e3a896c3a44ba89eec8bb84325ecf6c62..da009c86a88285269dee0155e58e550c6d744ea9\n--- a/SYSTEM_PROMPT.md\n+++ b/SYSTEM_PROMPT.md\n@@ -30,6 +30,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n Use the canonical source-of-truth order above. Runtime-specific reading orders\n are operational guidance only and must not override or reorder it. The current\n conversation is non-durable operator input.\n@@ -48,14 +51,14 @@\n \n A runtime may only do work that is explicitly assigned by the Goal, task, PR, or MergeGate decision.\n \n-Do not assume `openai/codex-action`, `OPENAI_API_KEY`, or API-key based Codex automation in the default Shiki path. API-key runners are explicit target-repo extensions and require their own ADR.\n+Do not assume `openai/codex-action`, `OPENAI_API_KEY`, or API-key based Codex automation in the default Shiki path. API-key runners are explicit target-repo extensions and require their own target ADR.\n \n ## 4. Non-negotiable Shiki flow\n \n For any non-trivial Goal, use this flow:\n \n 1. **Goal Seek**: define outcome, non-goals, risk level, completion criteria, and evidence requirements.\n-2. **grill-with-docs**: challenge the plan against `CONTEXT.md`, ADRs, code reality, terminology, boundaries, and edge scenarios. Ask one question at a time when the operator is available. If code can answer a question, inspect code instead of asking.\n+2. **grill-with-docs**: challenge the plan against `CONTEXT.md`, Shiki SADRs, target ADRs, code reality, terminology, boundaries, and edge scenarios. Ask one question at a time when the operator is available. If code can answer a question, inspect code instead of asking.\n 3. **Context & Impact**: identify modules, interfaces, seams, callers, dependencies, locks, risk, and verification surfaces.\n 4. **to-prd + Spec Freeze**: turn settled context into a PRD using domain vocabulary and testing decisions. The operator's explicit approval of the PRD is Spec Freeze, recorded as the plan's `spec_freeze` block; plans without it do not run. Steps 1-4 form Requirements Definition.\n 5. **to-issues**: decompose the PRD into independently grabbable vertical-slice issues. Prefer AFK slices; mark HITL slices when judgment is still required.\n@@ -75,7 +78,7 @@\n Before making any material change:\n \n 1. Read `AGENTS.md` and any runtime-specific wrapper such as `CLAUDE.md`.\n-2. Read `CONTEXT.md` and relevant ADRs.\n+2. Read `CONTEXT.md` and relevant SADRs and target ADRs.\n 3. Read the GitHub Issue / PR and `.shiki/` state for the active Goal or task.\n 4. Identify your role: Planner, Implementer, Reviewer, Completion Check Agent, Repairer, or Guardian-assist.\n 5. Identify Goal id, task id, branch, PR, dependencies, locks, risk level, required skills, and required evidence.\n@@ -103,7 +106,7 @@\n \n At minimum, identify:\n \n-- Relevant documents, ADRs, domain terms, and prior decisions.\n+- Relevant documents, SADRs, target ADRs, domain terms, and prior decisions.\n - Relevant modules, interfaces, seams, callers, owners, and tests.\n - Dependency relationships between tasks.\n - Candidate file locks and lock conflicts.\n@@ -152,7 +155,7 @@\n \n CCA must read:\n \n-- `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant ADRs.\n+- `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant SADRs and target ADRs.\n - Parent Goal issue, PRD issue, task issue, comments, and labels.\n - PR body, diff, commits, changed files, reviews, CI checks, and artifacts available to the workflow.\n - `.shiki/` task contract, locks, ledger entries, CCA checklist profile, and prior repair packets.\n@@ -214,7 +217,7 @@\n - Three or more new files.\n - Any high-risk or critical label.\n \n-When the architecture gate triggers, run `grill-with-docs` or `improve-codebase-architecture` as appropriate and record the decision in an ADR if it is hard to reverse or likely to be re-litigated.\n+When the architecture gate triggers, run `grill-with-docs` or `improve-codebase-architecture` as appropriate and record the decision in a Shiki SADR or target ADR as applicable if it is hard to reverse or likely to be re-litigated.\n \n ## 14. Skill Gate\n \n@@ -223,7 +226,7 @@\n Use these skills:\n \n - `setup-matt-pocock-skills`: before first use of the engineering skill set, or when issue tracker, triage labels, or domain docs are missing.\n-- `grill-with-docs`: default for non-trivial Goals before PRD/issues; required for ambiguous plans, terminology, boundaries, ADR-worthy tradeoffs, or design-tree decisions.\n+- `grill-with-docs`: default for non-trivial Goals before PRD/issues; required for ambiguous plans, terminology, boundaries, decision-record-worthy tradeoffs, or design-tree decisions.\n - `zoom-out`: unfamiliar code area or missing architectural map.\n - `to-prd`: convert settled context into a PRD.\n - `to-issues`: break a PRD, plan, or Goal into independently grabbable vertical-slice issues.\ndiff --git a/docs/adr/0001-keep-shiki-as-platform-name.md b/docs/adr/SADR-0001-keep-shiki-as-platform-name.md\nindex 20a9637c3b9d8c97de7ba8db7cfae4d82d9a7dba..8b685439959aada1b221c112f0ac3c51ca8a1695\n--- a/docs/adr/0001-keep-shiki-as-platform-name.md\n+++ b/docs/adr/SADR-0001-keep-shiki-as-platform-name.md\n@@ -1,3 +1,3 @@\n-# Keep Shiki As The Platform Name\n+# SADR-0001: Keep Shiki As The Platform Name\n \n Shiki remains the product name for the GitHub-native agentic engineering control plane. We considered more descriptive names such as MergePilot, MergeGate, and Kiroku, but kept Shiki because it carries the existing goal-seeking framework identity while remaining independent of Hermes, Codex, Claude Code, or any single runtime.\ndiff --git a/docs/adr/0002-use-agents-as-shared-constitution.md b/docs/adr/SADR-0002-use-agents-as-shared-constitution.md\nindex 73de30ecb078120eac540d9bc51855e3cd48d0e3..c5486182bf06fca36ea508496b013c67302fd989\n--- a/docs/adr/0002-use-agents-as-shared-constitution.md\n+++ b/docs/adr/SADR-0002-use-agents-as-shared-constitution.md\n@@ -1,3 +1,3 @@\n-# Use AGENTS As The Shared Agent Constitution\n+# SADR-0002: Use AGENTS As The Shared Agent Constitution\n \n Shiki uses `AGENTS.md` as the runtime-neutral constitution and `CLAUDE.md` as a thin Claude Code wrapper. This keeps Codex, Claude Code, Hermes Runner, and future Agent Runtimes aligned on the same GitHub-first MergeGate rules while still allowing runtime-specific instructions where needed.\ndiff --git a/docs/adr/0003-require-skill-gate-for-mergegate-readiness.md b/docs/adr/SADR-0003-require-skill-gate-for-mergegate-readiness.md\nindex 424be0c9af4c8f0136f1fde68a468c47ff84e764..7fc337987c7df9792f75f181dfcbed75fe5ffff2\n--- a/docs/adr/0003-require-skill-gate-for-mergegate-readiness.md\n+++ b/docs/adr/SADR-0003-require-skill-gate-for-mergegate-readiness.md\n@@ -1,3 +1,3 @@\n-# Require Skill Gate For MergeGate Readiness\n+# SADR-0003: Require Skill Gate For MergeGate Readiness\n \n Shiki requires relevant engineering skills to be invoked before execution when their triggers apply. This makes skills part of MergeGate readiness rather than optional prompt style, because Shiki depends on repeatable planning, issue decomposition, TDD, diagnosis, triage, and architecture review before autonomous or parallel agent work can be trusted.\ndiff --git a/docs/adr/0004-use-github-first-with-shiki-mirror.md b/docs/adr/SADR-0004-use-github-first-with-shiki-mirror.md\nindex d0a3a9174dc646a945a9d325954ffaf00c352040..a79c54ae9ee8eed2a0524192b864da4e22a92d3f\n--- a/docs/adr/0004-use-github-first-with-shiki-mirror.md\n+++ b/docs/adr/SADR-0004-use-github-first-with-shiki-mirror.md\n@@ -1,4 +1,4 @@\n-# ADR 0004: Use GitHub First With A Repository-Local Shiki Mirror\n+# SADR-0004: Use GitHub First With A Repository-Local Shiki Mirror\n \n ## Status\n \ndiff --git a/docs/adr/0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md b/docs/adr/SADR-0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\nindex b339baf49b89d9290e79c3d51b36e7b2bde3bd95..c30a0d8b1fde0224d0ac7820e7870dccf721580d\n--- a/docs/adr/0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\n+++ b/docs/adr/SADR-0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\n@@ -1,4 +1,4 @@\n-# ADR 0005: Use Codex As Front And Claude Code Action As The GitHub Runtime\n+# SADR-0005: Use Codex As Front And Claude Code Action As The GitHub Runtime\n \n ## Status\n \ndiff --git a/docs/adr/0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md b/docs/adr/SADR-0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\nindex 4bcae9d1e52f0a37b8e9fbafa5cd14b6ee1993bd..7a38538a6ae9929567f0ade67e7ea6a60d0b3f7e\n--- a/docs/adr/0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\n+++ b/docs/adr/SADR-0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\n@@ -1,4 +1,4 @@\n-# ADR 0006: Separate Codex Implementation, CCA Completion, And MergeGate Authorization\n+# SADR-0006: Separate Codex Implementation, CCA Completion, And MergeGate Authorization\n \n ## Status\n \ndiff --git a/docs/adr/0007-packaging-and-release.md b/docs/adr/SADR-0007-packaging-and-release.md\nindex 79b84ddb97167372b44641e5b7860e400ce80822..bb445a2e2e856d9d1f5fdbe35de8d4e8cec0fb32\n--- a/docs/adr/0007-packaging-and-release.md\n+++ b/docs/adr/SADR-0007-packaging-and-release.md\n@@ -1,4 +1,4 @@\n-# ADR 0007: Packaging And Release For Shiki\n+# SADR-0007: Packaging And Release For Shiki\n \n ## Status\n \n@@ -25,7 +25,7 @@\n   not introduce third-party runtime dependencies or break the shim.\n - Releases must run inside the operator's subscription-authenticated toolchain\n   and must not require extra secrets beyond the built-in `GITHUB_TOKEN`\n-  (consistent with ADR 0005's no-API-key-by-default stance).\n+  (consistent with SADR-0005's no-API-key-by-default stance).\n - `.shiki/manifest.json` already encodes the install surface (`install.include`,\n   `install.create_directories`, `install.exclude_from_commit`), and it must not\n   be mutated as part of this change.\n@@ -34,7 +34,7 @@\n \n This is a hard-to-reverse platform decision (it sets the version source of\n truth, the release trigger, and the public install contract), so it is recorded\n-as an ADR.\n+as a SADR.\n \n ## Decision\n \n@@ -104,7 +104,7 @@\n 3. **Publishing to PyPI.** Rejected for now: Shiki is distributed as a\n    repository template + git-installable CLI, not a PyPI library; PyPI would add\n    credentials/secrets and a different versioning contract. Can be added later as\n-   its own ADR if a packaged distribution is needed.\n+   its own SADR if a packaged distribution is needed.\n 4. **Dynamic version (read `VERSION` via a build backend `dynamic` field).**\n    Rejected to keep `pyproject.toml` backend-agnostic and dependency-free; the\n    small duplication between `VERSION` and `pyproject.toml` is guarded by the\n@@ -123,4 +123,4 @@\n   releasing versions the whole platform but ships only `install.include` to\n   targets.\n - Future PyPI distribution or a console entry point are deferred and would each\n-  warrant their own ADR.\n+  warrant their own SADR.\ndiff --git a/docs/adr/0008-use-claude-code-as-default-implementer.md b/docs/adr/SADR-0008-use-claude-code-as-default-implementer.md\nindex dedf4510b7c7799521422f622029563d373db031..35c2625d908167fbc58855b5b63e283805f167e8\n--- a/docs/adr/0008-use-claude-code-as-default-implementer.md\n+++ b/docs/adr/SADR-0008-use-claude-code-as-default-implementer.md\n@@ -1,12 +1,12 @@\n-# ADR 0008: Use Claude Code As The Default Implementer Runtime\n+# SADR-0008: Use Claude Code As The Default Implementer Runtime\n \n ## Status\n \n-Proposed (supersedes ADR 0005 for the implementer default; keeps its auth model)\n+Proposed (supersedes SADR-0005 for the implementer default; keeps its auth model)\n \n ## Context\n \n-ADR 0005 made Codex Front the default implementation runtime, with Claude Code\n+SADR-0005 made Codex Front the default implementation runtime, with Claude Code\n restricted to planner and reviewer roles by the runtime registry, and the only\n autonomous implementation adapter was `shiki runner codex`.\n \n@@ -18,13 +18,13 @@\n - The platform's planning, review, and CCA layers already run on Claude Code,\n   and Claude Code's native capabilities (the built-in `/code-review` skill and\n   the Workflow multi-agent orchestration tool) have no Codex equivalent.\n-- The Goal lifecycle redesign (ADR 0009) requires an autonomous post-freeze\n+- The Goal lifecycle redesign (SADR-0009) requires an autonomous post-freeze\n   loop driven from the operator's Claude Code session, including bounded\n   repair dispatch, which must not depend on a runtime that cannot be invoked\n   unattended.\n \n The constitution claims Shiki is runtime-agnostic (\"not a Claude-only\n-workflow\", ADR 0002). Any change to the implementer default must preserve the\n+workflow\", SADR-0002). Any change to the implementer default must preserve the\n registry-based runtime model rather than hardcoding one vendor.\n \n ## Decision\n@@ -39,7 +39,7 @@\n   the task to review or repair-needed.\n - Keep Codex registered as an optional implementer runtime. `shiki runner\n   codex`, the Codex handoff templates, and the ChatGPT OAuth auth model from\n-  ADR 0005 remain supported for tasks explicitly assigned to `codex`.\n+  SADR-0005 remain supported for tasks explicitly assigned to `codex`.\n - Commit to mandating two Claude-native capabilities as loop contract in\n   follow-up Goals (status: the code-review gate was delivered by Goal G-B \u2014\n   skill registry, checklists item PR-12, default required_skills \u2014 while the\n@@ -60,7 +60,7 @@\n \n Out of scope: removing Codex support, changing the CCA runtime (Claude Code\n Action with `CLAUDE_CODE_OAUTH_TOKEN` stays), and API-key based automation\n-(still requires its own ADR per ADR 0005).\n+(still requires its own target ADR per SADR-0005).\n \n ## Consequences\n \n@@ -100,7 +100,7 @@\n   harness rejection of direct `codex exec` is outside Shiki's control, and the\n   plan's mandatory native capabilities cannot run on Codex at all.\n - **Remove Codex entirely**: rejected; it abandons the runtime-agnostic\n-  constitution (ADR 0002) for no operational gain and forecloses\n+  constitution (SADR-0002) for no operational gain and forecloses\n   cross-runtime adversarial review.\n - **GitHub Actions cloud implementation via claude-code-action**: rejected as\n   default; durable evidence is automatic but token cost, Node 24 workflow\ndiff --git a/docs/adr/0009-requirements-definition-and-spec-freeze.md b/docs/adr/SADR-0009-requirements-definition-and-spec-freeze.md\nindex 29b3f7871bef9203a8b4a831a65c39eb2f506e42..52e7dd69ad0aa4efe0b68c62678abbf7df02aaec\n--- a/docs/adr/0009-requirements-definition-and-spec-freeze.md\n+++ b/docs/adr/SADR-0009-requirements-definition-and-spec-freeze.md\n@@ -1,4 +1,4 @@\n-# ADR 0009: Merge Grill And Planning Into Requirements Definition With Spec Freeze\n+# SADR-0009: Merge Grill And Planning Into Requirements Definition With Spec Freeze\n \n ## Status\n \ndiff --git a/docs/adr/0010-external-ai-guardian-review.md b/docs/adr/SADR-0010-external-ai-guardian-review.md\nindex e6108bbe8bb4e73e0ff4684c8f78001a80438c4e..f3510b145664298adafb151b6f0db51b972f01ec\n--- a/docs/adr/0010-external-ai-guardian-review.md\n+++ b/docs/adr/SADR-0010-external-ai-guardian-review.md\n@@ -1,4 +1,4 @@\n-# ADR 0010: External AI Guardian Review As A First-Class Approval Authority\n+# SADR-0010: External AI Guardian Review As A First-Class Approval Authority\n \n ## Status\n \n@@ -8,7 +8,7 @@\n \n Shiki is intended to be an autonomous, recursive, self-improving agent\n framework: authority-in-the-loop, not human-in-the-loop. Until now the Guardian\n-approval required for high/critical-risk merges (guardian-policy.json, ADR\n+approval required for high/critical-risk merges (guardian-policy.json, SADR\n governance) recognized only human authority sources \u2014 a GitHub review approval,\n the `guardian:approved` label applied by a configured Guardian, or a comment\n carrying `Guardian approval granted` plus the current head SHA.\n@@ -89,7 +89,7 @@\n   autonomous design the operator intends and produced the impersonation\n   incident.\n - **Record AI approval as an operator comment**: rejected as forgery of human\n-  authority \u2014 the exact failure this ADR corrects.\n+  authority \u2014 the exact failure this SADR corrects.\n - **Deliver the artifact as a committed `.shiki/` file**: rejected; a committed\n   approval inside the PR being approved is forgeable by anyone who can push.\n - **A dedicated reviewer bot identity instead of a relayed comment**: deferred;\ndiff --git a/docs/adr/0011-autonomous-quality-gate-evidence.md b/docs/adr/SADR-0011-autonomous-quality-gate-evidence.md\nindex bca0246c5d46e44afbde15486a75eec425fc02f5..9c93eac16d8c3a6d4b11bd7224e3955102cd7db4\n--- a/docs/adr/0011-autonomous-quality-gate-evidence.md\n+++ b/docs/adr/SADR-0011-autonomous-quality-gate-evidence.md\n@@ -1,4 +1,4 @@\n-# ADR 0011: Autonomous Quality-Gate Evidence \u2014 Deterministic Facts And An Independent Verifier\n+# SADR-0011: Autonomous Quality-Gate Evidence \u2014 Deterministic Facts And An Independent Verifier\n \n ## Status\n \ndiff --git a/docs/adr/0012-autonomous-loop-closeout-to-main.md b/docs/adr/SADR-0012-autonomous-loop-closeout-to-main.md\nindex 637fad6dde4989c9c2bf0f81f676d8d29991123c..ec7d85f81030a676bcbba28b43eb365422a36eac\n--- a/docs/adr/0012-autonomous-loop-closeout-to-main.md\n+++ b/docs/adr/SADR-0012-autonomous-loop-closeout-to-main.md\n@@ -1,4 +1,4 @@\n-# 0012 \u2014 Autonomous loop pushes goal completion to main via a closeout PR\n+# SADR-0012 \u2014 Autonomous loop pushes goal completion to main via a closeout PR\n \n - Status: accepted\n - Date: 2026-06-18\ndiff --git a/docs/adr/0013-bootstrap-grants-actions-approve-pull-requests.md b/docs/adr/SADR-0013-bootstrap-grants-actions-approve-pull-requests.md\nindex b6c69d64f1c5ced346b88d6859586715b3b5bd0e..cf48ff4a6716267f539ca58edd1a852e7c7a2d64\n--- a/docs/adr/0013-bootstrap-grants-actions-approve-pull-requests.md\n+++ b/docs/adr/SADR-0013-bootstrap-grants-actions-approve-pull-requests.md\n@@ -1,4 +1,4 @@\n-# ADR 0013: Bootstrap Grants GitHub Actions Permission To Create And Approve Pull Requests\n+# SADR-0013: Bootstrap Grants GitHub Actions Permission To Create And Approve Pull Requests\n \n ## Status\n \n@@ -31,7 +31,7 @@\n security tradeoff: it lets repository automation satisfy a human-review gate. In\n a multi-maintainer repo that could let a compromised or buggy workflow\n self-approve changes. So whether bootstrap turns it on by default \u2014 and with\n-what default token scope \u2014 is an ADR-worthy decision, not an implementation\n+what default token scope \u2014 is a SADR-worthy decision, not an implementation\n detail.\n \n ## Decision\n@@ -48,7 +48,7 @@\n Specifically:\n \n - **`can_approve_pull_request_reviews` defaults to `true`.** Shiki's default\n-  posture is solo/AFK autonomous operation (ADR 0008/0009/0012), where the CCA\n+  posture is solo/AFK autonomous operation (SADR-0008/0009/0012), where the CCA\n   Review Bridge is the only path that can satisfy `required_review: true` after\n   the CCA and Guardian gates have already passed. The default makes a\n   freshly-bootstrapped repo able to run the autonomous loop end to end without a\ndiff --git a/docs/adr/0014-codex-app-external-ai-guardian-ui-adapter.md b/docs/adr/SADR-0014-codex-app-external-ai-guardian-ui-adapter.md\nindex 238db06279334caa341c117362df9271382a7fcd..65f12e9939321e1b3e389a34c822fc1549e5155d\n--- a/docs/adr/0014-codex-app-external-ai-guardian-ui-adapter.md\n+++ b/docs/adr/SADR-0014-codex-app-external-ai-guardian-ui-adapter.md\n@@ -1,4 +1,4 @@\n-# ADR 0014: Use Codex App As The External AI Guardian UI Adapter\n+# SADR-0014: Use Codex App As The External AI Guardian UI Adapter\n \n ## Status\n \ndiff --git a/docs/adr/0015-contract-approval-before-dispatch.md b/docs/adr/SADR-0015-contract-approval-before-dispatch.md\nindex fad6c656741a20a7684ba16080a7db159f63a52f..6061271a46d3f1d6ae7174477de3fb75673123a6\n--- a/docs/adr/0015-contract-approval-before-dispatch.md\n+++ b/docs/adr/SADR-0015-contract-approval-before-dispatch.md\n@@ -1,4 +1,4 @@\n-# ADR 0015: Approve The Task Contract Before Dispatch, Not The Diff After It\n+# SADR-0015: Approve The Task Contract Before Dispatch, Not The Diff After It\n \n ## Status\n \n@@ -9,7 +9,7 @@\n Guardian approval for high/critical risk is expressible only on a pull request.\n Every approval source in `guardian-policy.json` \u2014 GitHub review, the\n `guardian:approved` label, the `Guardian approval granted` comment, and the\n-`external_ai_guardian_review` artifact (ADR 0010/0014) \u2014 is bound to a PR, and\n+`external_ai_guardian_review` artifact (SADR-0010/0014) \u2014 is bound to a PR, and\n three of the four require the current head SHA. Approval therefore cannot exist\n before an implementation PR exists.\n \n@@ -81,7 +81,7 @@\n An envelope breach must surface only on the non-repairable policy gate, never on\n the repairable `MergeGate metadata check`. Otherwise the Goal Loop routes it to\n `dispatch_repair` \u2014 instructing a runner to make the breach go away \u2014 which is\n-exactly what this ADR forbids.\n+exactly what this SADR forbids.\n \n **Contract immutability.** After registration, an implementation PR must not\n change the governance fields of its own task contract (`scope`, `non_goals`,\n@@ -116,7 +116,7 @@\n fails any verdict that is not `complete`, and the signal the CCA prompt reads is\n computed from `evaluate_guardian_approval` alone. Without the OR-merge at the\n signal, a contract-approved PR still returns `needs_guardian`, the CCA job fails,\n-and the MergeGate policy check never runs \u2014 the gate this ADR changes would never\n+and the MergeGate policy check never runs \u2014 the gate this SADR changes would never\n be reached. The CCA *prompt* needs no change: a `contract_approval` entry in the\n signal's existing `sources` array satisfies its current wording.\n \n@@ -130,7 +130,7 @@\n written, and an approved contract lets its implementation PRs merge through the\n same automatic path as low/medium risk. The wasted implement-then-block cycle\n disappears. Guardian review shifts from reading diffs to reading contracts, which\n-is both earlier and smaller; the external AI Guardian path (ADR 0010/0014) works\n+is both earlier and smaller; the external AI Guardian path (SADR-0010/0014) works\n on contracts with no change.\n \n We accept that the Guardian authorizes a class of change without seeing its\n@@ -146,7 +146,7 @@\n adversarial tests for the new approval path equivalent in rigor to\n `test_shiki_governance_evidence.sh`.\n \n-Bootstrap costs, which apply only while this ADR is being implemented. The tasks\n+Bootstrap costs, which apply only while this SADR is being implemented. The tasks\n that build Contract Approval merge under the existing rules, so the ones\n dispatched after the pre-dispatch gate lands but before the approval path lands\n need a Guardian-approved Contract PR *and* a live Guardian approval on their\n@@ -210,7 +210,7 @@\n risk; they do not bound authorized work.\n \n **Auto-escalate risk on envelope breach instead of stopping.** Returns the\n-workflow to a diff-time Guardian decision \u2014 the exact behavior this ADR removes.\n+workflow to a diff-time Guardian decision \u2014 the exact behavior this SADR removes.\n \n **Resolve governance fields from the base snapshot only.** Rejected against the\n registration flow: implementation PRs add their own contract, so the base has no\ndiff --git a/docs/adr/0016-locks-bound-product-paths.md b/docs/adr/SADR-0016-locks-bound-product-paths.md\nindex b5e850f3f1f7f4ef6e7f9d1763a29a51bd300c45..46b40a9b99a7aae644c02bb458f5473ff0a90ce2\n--- a/docs/adr/0016-locks-bound-product-paths.md\n+++ b/docs/adr/SADR-0016-locks-bound-product-paths.md\n@@ -1,4 +1,4 @@\n-# ADR 0016: Locks bound product paths; the Shiki mirror is bound by record identity\n+# SADR-0016: Locks bound product paths; the Shiki mirror is bound by record identity\n \n ## Status\n \n@@ -6,8 +6,8 @@\n \n - Date: 2026-07-28\n - Deciders: operator (mizutani-140), Claude (planner)\n-- Related: ADR 0004 (GitHub-first with a `.shiki` mirror), ADR 0009 (Spec Freeze),\n-  ADR 0012 (autonomous loop closeout to main), ADR 0015 (Contract Approval before\n+- Related: SADR-0004 (GitHub-first with a `.shiki` mirror), SADR-0009 (Spec Freeze),\n+  SADR-0012 (autonomous loop closeout to main), SADR-0015 (Contract Approval before\n   dispatch), remediation goal task R-02, the live PR #179 deadlock.\n \n ## Context\n@@ -90,11 +90,11 @@\n files, documentation files and named source modules are nameable; runtime-\n allocated ledger, runner and report ids are not \u2014 and those live in `.shiki`,\n which locks no longer govern. Existing frozen contracts are brought into line by\n-a one-time Spec Amendment per plan, which must land before ADR 0015's contract\n+a one-time Spec Amendment per plan, which must land before SADR-0015's contract\n immutability makes `locks` unwritable from the implementation PR.\n \n **Lock state is resolved from the durable side.** MergeGate resolves the lock\n-registry, and ADR 0015's governance fields, from the base `.shiki` snapshot when\n+registry, and SADR-0015's governance fields, from the base `.shiki` snapshot when\n present, falling back to head only when absent. The `MergeGate metadata check`\n job is given `--base-shiki`, which it does not receive today. Locks are not\n derived from the live open-pull-request set: a pull request body is untrusted\n@@ -108,7 +108,7 @@\n Explicitly out of scope: moving MergeGate's contract resolution off the PR\n checkout wholesale; making the mirror a projection of GitHub state; introducing\n new lock kinds; moving ledger evidence out of the repository; and unwinding\n-ADR 0012's closeout pull request. Registration of multi-task goals is ADR 0015's\n+SADR-0012's closeout pull request. Registration of multi-task goals is SADR-0015's\n Contract PR and is not re-decided here.\n \n ## Consequences\n@@ -127,7 +127,7 @@\n \n Risks accepted. First, goal-scoped identity is only as strong as `goal_id`,\n which is resolved from the PR body and the head task file; the mitigation is to\n-add `goal_id` to ADR 0015's frozen governance field set, and until that lands a\n+add `goal_id` to SADR-0015's frozen governance field set, and until that lands a\n task file can name a foreign goal. Second, base-when-present resolution is\n unavailable on fork pull requests, where the head is contributor-controlled;\n fork pull requests already cannot complete a Shiki merge, and this decision does\n@@ -139,7 +139,7 @@\n path, not by implementation pull requests.\n \n New obligations: one Spec Amendment per frozen plan to rewrite `locks`, ordered\n-before ADR 0015 T1; extension of the adversarial suites in\n+before SADR-0015 T1; extension of the adversarial suites in\n `scripts/test_shiki_adversarial_state.sh` and\n `scripts/test_shiki_governance_evidence.sh` to cover the eleven newly-scoped\n directories; and updating `scripts/test_shiki_loop_lock_guard.sh`,\n@@ -151,32 +151,32 @@\n whether the loop's transitive sync of ledger `evidence` references can be\n deleted once the Contract PR carries its own source plan.\n \n-## Relationship to ADR 0004\n+## Relationship to SADR-0004\n \n-This ADR **clarifies something ADR 0004 left unspecified**. ADR 0004 decides that\n+This SADR **clarifies something SADR-0004 left unspecified**. SADR-0004 decides that\n GitHub is the operational source of truth and that each target repository keeps a\n `.shiki/` mirror. It says nothing about pull requests writing the mirror, does\n not name MergeGate, and does not make the mirror an input to a gate. Nothing in\n-ADR 0004's Decision or Consequences is amended, superseded or weakened: the\n+SADR-0004's Decision or Consequences is amended, superseded or weakened: the\n mirror still exists, still records the same artifacts, and pull request\n-boundaries remain the coordination surface. What this ADR settles is the question\n+boundaries remain the coordination surface. What this SADR settles is the question\n 0004 never reached \u2014 by what authority a pull request may write a mirror record \u2014\n and it answers it the way `.shiki/manifest.json` already declares\n (`pr_mutation: current-task-or-current-goal-only`).\n \n-It **amends ADR 0012**, whose Decision item 5 states that \"Loop-executed tasks\n+It **amends SADR-0012**, whose Decision item 5 states that \"Loop-executed tasks\n declare the `path:.shiki/**` lock, which covers every `.shiki` file the closeout\n stages ... so `files_outside_locks` is satisfied without per-file lock\n bookkeeping.\" That clause is replaced: the closeout's mirror writes are\n-authorized by record identity, not by a declared lock. ADR 0012's substantive\n+authorized by record identity, not by a declared lock. SADR-0012's substantive\n decision \u2014 that completion reaches `main` through a closeout pull request rather\n than local attestation \u2014 is preserved unchanged.\n \n-It is **complementary to ADR 0015**, not a substitute. ADR 0015 owns contract\n+It is **complementary to SADR-0015**, not a substitute. SADR-0015 owns contract\n immutability, the pre-dispatch gate, the Contract PR and the Scope Envelope. This\n-ADR owns what a lock means and who may write the mirror. The one ordering\n+SADR owns what a lock means and who may write the mirror. The one ordering\n constraint between them is that the lock-narrowing Spec Amendment must precede\n-ADR 0015 T1's freezing of the `locks` field.\n+SADR-0015 T1's freezing of the `locks` field.\n \n ## Alternatives Considered\n \n@@ -186,7 +186,7 @@\n the only writer that can register them may run. Migrating the Guardian signal to\n that store makes every registration PR fail closed to `needs_guardian` at every\n risk level, because the task file is by definition absent from the store, which\n-removes the low/medium autonomous path ADR 0008/0009 exists to provide. And it\n+removes the low/medium autonomous path SADR-0008/0009 exists to provide. And it\n leaves schemas, manifest, config, guardian policy, templates, migrations, plans,\n worktrees, runner records, runs and handoffs with no writer at all in a\n repository whose product is `.shiki`. Measured benefit on the frozen backlog:\n@@ -199,14 +199,14 @@\n namespace is not in fact ownership-partitioned \u2014 56 ledger entries carry no\n `task_id`, and one goal DAG is writable by 35 registered tasks. The correct\n expression of its insight is that locks do not govern the mirror at all, which is\n-what this ADR decides.\n+what this SADR decides.\n \n **Make the mirror a projection written only by a registration writer and a\n post-merge projection job.** Rejected. It requires a bypass actor with write\n access to the protected default branch; it makes its own master invariant\n readable from a manifest that pull requests may edit; it deletes both reconcile\n modes and leaves a corrupted mirror with no in-band repair path, contradicting\n-ADR 0004's consequence that mirror conflicts are repaired against GitHub state;\n+SADR-0004's consequence that mirror conflicts are repaired against GitHub state;\n and its content-derived ids are rejected by `validate_shiki`'s currently-green id\n pattern.\n \ndiff --git a/docs/adr/0017-bookkeeping-closeouts-do-not-inherit-risk.md b/docs/adr/SADR-0017-bookkeeping-closeouts-do-not-inherit-risk.md\nindex 554e3a0d526a72ca067108c5464cc68c3edcf174..b9676857a4713850e063af7c39c8e0b01ecb4c66\n--- a/docs/adr/0017-bookkeeping-closeouts-do-not-inherit-risk.md\n+++ b/docs/adr/SADR-0017-bookkeeping-closeouts-do-not-inherit-risk.md\n@@ -1,4 +1,4 @@\n-# ADR 0017: A Bookkeeping Closeout Does Not Inherit Its Task's Risk\n+# SADR-0017: A Bookkeeping Closeout Does Not Inherit Its Task's Risk\n \n ## Status\n \n@@ -6,7 +6,7 @@\n \n ## Context\n \n-ADR 0012 made the goal loop push a task's completion to the default branch through a\n+SADR-0012 made the goal loop push a task's completion to the default branch through a\n closeout pull request: after the implementation PR merges, the loop opens a second PR\n carrying `task.status = done`, `lock.state = released`, and \u2014 when the task completes\n its goal \u2014 `goal.status = complete` plus one scorecard report. Nothing else. The\n@@ -49,14 +49,14 @@\n 4. The task file's governance fields (`scope`, `non_goals`, `required_skills`,\n    `risk_level`, `locks`, `acceptance_checks`, `test_command`) are byte-identical to\n    the base snapshot. Only `status`, `expected_pr`, `closeout_pr`, `ledger_evidence`\n-   and `expected_branch` may differ \u2014 the ADR 0015 contract-immutability rule,\n+   and `expected_branch` may differ \u2014 the SADR-0015 contract-immutability rule,\n    unchanged.\n \n    `expected_branch` is in that set because a closeout is *by construction* on a\n    different branch from the implementation it closes, and the loop rewrites the\n    field accordingly. It is safe to let it differ because MergeGate independently\n    requires `expected_branch` to equal the PR's actual head ref, so a PR cannot set\n-   it to anything other than the branch it is really on. The first draft of this ADR\n+   it to anything other than the branch it is really on. The first draft of this SADR\n    omitted it, which made the exemption unreachable: every real closeout failed\n    condition 4 on a field the loop always changes. See PR #209.\n 5. The state transitions are exactly the terminal set: task `review` \u2192 `done`, lock\n@@ -113,12 +113,12 @@\n classification must be derived, not asserted.\n \n **Fold the closeout into the implementation PR** so no second PR exists. Rejected under\n-ADR 0012, which recorded why: a task file that is already `done` on the implementation\n+SADR-0012, which recorded why: a task file that is already `done` on the implementation\n branch short-circuits the loop's review \u2192 CCA \u2192 merge \u2192 done ordering into a local\n goal-completion before any merge happens.\n \n **Have the loop auto-merge closeouts with elevated credentials.** Rejected outright: that\n-is a runtime authoring its own approval, which ADR 0010 and the constitution's\n+is a runtime authoring its own approval, which SADR-0010 and the constitution's\n approval-record prohibition forbid. It would also be indistinguishable, in the evidence,\n from the forgery those rules exist to prevent.\n \ndiff --git a/docs/adr/0018-carried-guardian-approval-across-a-proven-base-sync.md b/docs/adr/SADR-0018-carried-guardian-approval-across-a-proven-base-sync.md\nindex 22358562da390ed8b45cd600da08a43d378b4680..e6e2907cefa776460af73ddf7d39db605f3c39b0\n--- a/docs/adr/0018-carried-guardian-approval-across-a-proven-base-sync.md\n+++ b/docs/adr/SADR-0018-carried-guardian-approval-across-a-proven-base-sync.md\n@@ -1,4 +1,4 @@\n-# ADR 0018: Carried Guardian Approval Across A Proven Base Sync\n+# SADR-0018: Carried Guardian Approval Across A Proven Base Sync\n \n ## Status\n \n@@ -34,7 +34,7 @@\n Guardian evaluator that consumes it (`shiki_guardian.evaluate_guardian_approval`,\n the `guardian_comment_carried` source) were built and tested in isolation. Neither\n does anything until a caller passes the flags that turn the carry on, so the\n-feature was invisible in CI. This ADR records the decision the two of them\n+feature was invisible in CI. This SADR records the decision the two of them\n implement, and this task wires it into the four CI invocations that judge Guardian\n approval.\n \ndiff --git a/docs/adr/README.md b/docs/adr/README.md\nindex c70e057a4520c36e042ab68c53cd4a21ef709b71..5da43856836e2ce6a150f8127766e2f88355ab4b\n--- a/docs/adr/README.md\n+++ b/docs/adr/README.md\n@@ -1,40 +1,41 @@\n-# Architecture Decision Records\n+# Decision Record Namespaces\n \n-This directory holds Shiki's Architecture Decision Records (ADRs): durable\n-records of hard-to-reverse platform decisions. ADRs are part of the Shiki source\n-of truth alongside GitHub state, the `.shiki/` mirror, and `CONTEXT.md`.\n+This directory holds two independent decision namespaces:\n \n-## How ADRs Work\n+- Shiki Architecture Decision Records (SADRs), named\n+  `SADR-NNNN-<short-slug>.md`, record hard-to-reverse Shiki platform decisions.\n+- Target Architecture Decision Records (ADRs), named\n+  `NNNN-<short-slug>.md`, belong to the target product.\n \n-- New ADRs are added as `docs/adr/NNNN-<short-slug>.md`, where `NNNN` is the next\n-  zero-padded sequence number.\n-- The **canonical list of ADRs is the set of files matching `docs/adr/*.md`** in\n-  this directory. Browse `docs/adr/*.md` for the authoritative, current set.\n-- Use [`template.md`](template.md) as the starting point for a new ADR.\n-- Record an ADR when a decision is hard to reverse, surprising without context,\n-  and a real tradeoff. Do not write an ADR for routine or easily reversible\n-  choices.\n+The same four-digit number may appear once in each namespace. A platform\n+`SADR-NNNN` citation resolves only to `SADR-NNNN-*.md`; a target `ADR NNNN`\n+citation resolves only to `NNNN-*.md`.\n \n-Using the `docs/adr/*.md` glob as the source of truth means other PRs can add an\n-ADR without editing this index file, avoiding merge contention on a\n-hand-maintained enumeration.\n+## How Decision Records Work\n \n-## Current ADRs (snapshot)\n-\n-The list below is a convenience snapshot and may lag behind the directory. When\n-in doubt, list `docs/adr/*.md`.\n+- New Shiki platform decisions use the next zero-padded SADR number.\n+- New target product decisions use the target's own ADR sequence.\n+- The canonical list is the files in `docs/adr/`; no hand-maintained index is\n+  authoritative.\n+- Use [`template.md`](template.md) as the starting point and choose the filename\n+  namespace that owns the decision.\n+- Record a decision when it is hard to reverse, surprising without context, and\n+  a real tradeoff. Do not write a decision record for routine or easily\n+  reversible choices.\n \n-- [0001 \u2014 Keep Shiki As The Platform Name](0001-keep-shiki-as-platform-name.md)\n-- [0002 \u2014 Use AGENTS As The Shared Agent Constitution](0002-use-agents-as-shared-constitution.md)\n-- [0003 \u2014 Require Skill Gate For MergeGate Readiness](0003-require-skill-gate-for-mergegate-readiness.md)\n-- [0004 \u2014 Use GitHub First With A Repository-Local Shiki Mirror](0004-use-github-first-with-shiki-mirror.md)\n-- [0005 \u2014 Use Codex As Front And Claude Code Action As The GitHub Runtime](0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md)\n-- [0006 \u2014 Separate Codex Implementation, CCA Completion, And MergeGate Authorization](0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md)\n+Using the directory as the source of truth lets independent changes add records\n+without contending on an enumerated index.\n \n-## Adding A New ADR\n+## Adding A Shiki SADR\n \n-1. Copy `template.md` to `docs/adr/NNNN-<short-slug>.md`.\n+1. Copy `template.md` to `docs/adr/SADR-NNNN-<short-slug>.md`.\n 2. Fill in the title, status, context, decision, and consequences.\n-3. Set the status to `Proposed`, then `Accepted` once the decision is approved.\n-4. Open a PR following `CONTRIBUTING.md`. You do not need to edit this index for\n-   the ADR to be discoverable via `docs/adr/*.md`.\n+3. Set the status to `Proposed`, then `Accepted` once the proper authority has\n+   approved the decision.\n+4. Open a PR following `CONTRIBUTING.md`.\n+\n+## Adding A Target ADR\n+\n+1. Copy `template.md` to `docs/adr/NNNN-<short-slug>.md` using the target's own\n+   sequence.\n+2. Cite it as `ADR NNNN`; do not prefix its filename with `SADR-`.\ndiff --git a/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md b/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\nnew file mode 100644\nindex 0000000000000000000000000000000000000000..7455ce1f3bed02ddddb851ac21a1d1a648c9aeba\n--- /dev/null\n+++ b/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\n@@ -0,0 +1,55 @@\n+# SADR-0019: Separate Shiki Platform SADRs From Target ADRs\n+\n+## Status\n+\n+Accepted\n+\n+## Context\n+\n+Shiki installs its platform decision records into `docs/adr/` in every target\n+repository. Shiki and its targets previously used the same numeric\n+`NNNN-<slug>.md` namespace. A target that already owned ADRs 0013 through 0018\n+therefore acquired two records for each number during installation, making an\n+`ADR NNNN` citation ambiguous. Renumbering target decisions would rewrite target\n+history, while renumbering Shiki decisions per target would destroy their stable\n+platform identity.\n+\n+Upgrades also need to remove the former Shiki-owned numeric paths without ever\n+mistaking a target-owned ADR for platform content. The install stamp already\n+records exact shipped paths and content digests, so it can prove ownership only\n+when both still match.\n+\n+## Decision\n+\n+Shiki platform decisions use the `SADR` namespace:\n+\n+- records are named `docs/adr/SADR-NNNN-<slug>.md`;\n+- live platform citations use `SADR-NNNN`;\n+- target product decisions retain `docs/adr/NNNN-<slug>.md` and `ADR NNNN`;\n+- the reference validator resolves the two namespaces independently, so the same\n+  four-digit number may exist once in each namespace.\n+\n+Before a forced upgrade writes anything, the installer inventories the exact\n+former numeric Shiki ADR paths. It may delete a legacy path only when the\n+existing install stamp contains that exact path and its recorded SHA-256 digest\n+matches the file currently on disk. All ownership blockers are reported\n+together and abort the upgrade before its first write. A non-forced install\n+never performs this cleanup.\n+\n+Historical `.shiki/` evidence remains unchanged because it records the namespace\n+that existed when that evidence was produced.\n+\n+## Consequences\n+\n+- Targets may use any ADR number without colliding with Shiki's platform SADRs.\n+- Fresh installs preserve target ADR bytes and install a stable SADR set.\n+- Forced upgrades fail closed when legacy ownership cannot be proven, and a\n+  successful upgrade refreshes the stamp without removed legacy paths.\n+- Current governance, packaging, workflow, prompt, script, and skill references\n+  must move with the platform records.\n+\n+## Alternatives Considered\n+\n+- Collision-only refusal leaves affected targets permanently unupgradable.\n+- Renumbering target ADRs mutates target-owned history.\n+- Per-target Shiki renumbering makes platform citations non-portable.\ndiff --git a/docs/adr/template.md b/docs/adr/template.md\nindex a92a97daddd0b00ab9dd626cc8d86942da2eafed..17dd79175b5fee2105935557f691b40cf9492942\n--- a/docs/adr/template.md\n+++ b/docs/adr/template.md\n@@ -1,14 +1,14 @@\n-# NNNN: <Short Decision Title>\n+# <SADR-NNNN or NNNN>: <Short Decision Title>\n \n ## Status\n \n-Proposed | Accepted | Superseded by ADR-NNNN\n+Proposed | Accepted | Superseded by SADR-NNNN or target ADR NNNN\n \n ## Context\n \n What is the situation, constraint, or force that requires a decision? Reference\n-relevant domain terms (`CONTEXT.md`), prior ADRs, and code reality. State the\n-problem, not the solution.\n+relevant domain terms (`CONTEXT.md`), prior Shiki SADRs or target ADRs, and code\n+reality. State the problem, not the solution.\n \n ## Decision\n \ndiff --git a/docs/agents/autonomous-loop-e2e-acceptance.md b/docs/agents/autonomous-loop-e2e-acceptance.md\nindex bbf84c6081987f159ac6ad9ecf3c82b7aa72e7e8..5c13c1d02a05a90b0dc46cbef7c95a9846d0c237\n--- a/docs/agents/autonomous-loop-e2e-acceptance.md\n+++ b/docs/agents/autonomous-loop-e2e-acceptance.md\n@@ -40,8 +40,8 @@\n 3. `gh auth status` is logged in with merge permission on the repo.\n 4. Branch protection + required checks are configured exactly as the goal-loop\n    required-check set expects (see `docs/agents/branch-protection-smoke.md`).\n-5. The repo `.shiki/policy` permits autonomous low/medium-risk merges (ADR\n-   0008/0009); the MergeGate policy check is the recorded-authority gate.\n+5. The repo `.shiki/policy` permits autonomous low/medium-risk merges\n+   (SADR-0008/0009); the MergeGate policy check is the recorded-authority gate.\n \n The operator's only actions are: register the task and start the loop. **No\n manual pre-clearing** means after `shiki loop run` starts, the operator does not\ndiff --git a/docs/agents/checklists.md b/docs/agents/checklists.md\nindex 4a7f03ade346f53a1c1d82981f3fd744f9ab4547..0c5ec6c53dc7197b7b7a7d18e667b325babd6c45\n--- a/docs/agents/checklists.md\n+++ b/docs/agents/checklists.md\n@@ -32,18 +32,18 @@\n | ID | Item | Blocking |\n | --- | --- | --- |\n | GD-01 | Plan was challenged against `CONTEXT.md`. | blocking for non-trivial Goals |\n-| GD-02 | Relevant ADRs were read or absence was recorded. | blocking for non-trivial Goals |\n+| GD-02 | Relevant SADRs and target ADRs were read or absence was recorded. | blocking for non-trivial Goals |\n | GD-03 | Ambiguous terms were resolved or converted to HITL blockers. | blocking |\n | GD-04 | Edge scenarios were considered. | advisory |\n | GD-05 | Code reality was checked when code could answer. | blocking when applicable |\n | GD-06 | New domain terms were added to `CONTEXT.md` when needed. | blocking when applicable |\n-| GD-07 | ADR was created or explicitly skipped using the ADR criteria. | blocking when applicable |\n+| GD-07 | A SADR or target ADR was created or explicitly skipped using the decision-record criteria. | blocking when applicable |\n \n ## CI \u2014 Context & Impact Checklist\n \n | ID | Item | Blocking |\n | --- | --- | --- |\n-| CI-01 | Relevant docs and ADRs identified. | blocking |\n+| CI-01 | Relevant docs, SADRs, and target ADRs identified. | blocking |\n | CI-02 | Relevant modules, interfaces, seams, callers, and tests identified. | blocking |\n | CI-03 | Dependencies and blocked-by relationships identified. | blocking |\n | CI-04 | Candidate locks identified. | blocking |\n@@ -62,7 +62,7 @@\n | PRD-04 | Implementation decisions are recorded without volatile code detail. | blocking |\n | PRD-05 | Testing decisions are recorded. | blocking |\n | PRD-06 | Out-of-scope items are explicit. | blocking |\n-| PRD-07 | Relevant glossary/ADR links are present when applicable. | blocking |\n+| PRD-07 | Relevant glossary/SADR/target-ADR links are present when applicable. | blocking |\n \n ## SF \u2014 Spec Freeze Checklist\n \n@@ -98,7 +98,7 @@\n | H-02 | Branch/worktree target is present. | blocking |\n | H-03 | Scope and non-goals are present. | blocking |\n | H-04 | Dependencies and locks are present. | blocking |\n-| H-05 | Relevant docs, ADRs, modules, and tests are referenced. | blocking |\n+| H-05 | Relevant docs, SADRs, target ADRs, modules, and tests are referenced. | blocking |\n | H-06 | Required skills are stated. | blocking |\n | H-07 | TDD expectations are stated. | blocking when behavior work |\n | H-08 | Acceptance criteria are included. | blocking |\ndiff --git a/docs/agents/codex-handoff.md b/docs/agents/codex-handoff.md\nindex ede468f47cb277f69a857e8c45df703e4ed0151d..6f97bdffb67e44d7edea2c71e01ea28933c4eb6c\n--- a/docs/agents/codex-handoff.md\n+++ b/docs/agents/codex-handoff.md\n@@ -46,7 +46,7 @@\n \n ## Context\n - Domain terms:\n-- Relevant ADRs:\n+- Relevant SADRs and target ADRs:\n - Relevant docs:\n - Relevant modules/interfaces/seams:\n - Similar tests/prior art:\ndiff --git a/docs/agents/completion-check-agent.md b/docs/agents/completion-check-agent.md\nindex 13e4c035db35025f4e3cdcc5adfb24314ac4d521..61bfa1218cf36ca605250c91919e620f33238aa0\n--- a/docs/agents/completion-check-agent.md\n+++ b/docs/agents/completion-check-agent.md\n@@ -32,7 +32,7 @@\n - `AGENTS.md`\n - `CLAUDE.md`\n - `CONTEXT.md`\n-- relevant ADRs\n+- relevant SADRs and target ADRs\n - parent Goal issue\n - PRD issue or document\n - task issue\ndiff --git a/docs/agents/decision-control.md b/docs/agents/decision-control.md\nindex 7d8ee5ab03a5ccbcbd04056ae61de03785a2a9c9..8c94ecda00fffaa0e92c325ab83aa9776bd1dac1\n--- a/docs/agents/decision-control.md\n+++ b/docs/agents/decision-control.md\n@@ -11,7 +11,7 @@\n | Decision | Owner | When | Required inputs | Output | Enforcement surface |\n | --- | --- | --- | --- | --- | --- |\n | Is the Goal clear enough to plan? | Planner + Guardian when needed | Goal intake | User request, existing docs, risk signals | Goal readiness verdict | Goal checklist, issue template, ledger |\n-| Must `grill-with-docs` run? | Planner / Skill Gate | Before PRD or issue split | Ambiguity, domain terms, ADR conflicts, unclear boundaries | Skill invocation or explicit skip rationale | Skill Gate checklist |\n+| Must `grill-with-docs` run? | Planner / Skill Gate | Before PRD or issue split | Ambiguity, domain terms, SADR/target-ADR conflicts, unclear boundaries | Skill invocation or explicit skip rationale | Skill Gate checklist |\n | Is a PRD required? | Planner | After Goal context stabilizes | Goal, `grill-with-docs` output, Context & Impact output | PRD required / not required | PRD checklist, CCA evidence check |\n | Can the spec be frozen? | Operator only | End of Requirements Definition, before issue split | PRD/requirements summary, scope inventory (SF-02), open-question list | `spec_freeze` block in the plan + ledger evidence | `require_grilled_plan` (plan ingest/run), SF checklist |\n | Can a frozen spec change? | Operator only (Spec Amendment) | When implementation contradicts the frozen spec | Contested decisions, scoped re-grill result | recorded amendment + re-stamped freeze | SF checklist, ledger evidence |\ndiff --git a/docs/agents/domain.md b/docs/agents/domain.md\nindex 258da8ea81e87ac0a6b0b5b5e5c2fde980eaabb7..d57b0cf17ff3e1fa9e6e5885372bf083d8295421\n--- a/docs/agents/domain.md\n+++ b/docs/agents/domain.md\n@@ -5,7 +5,9 @@\n ## Layout\n \n - `CONTEXT.md` defines Shiki language. Keep it implementation-free.\n-- `docs/adr/` records hard-to-reverse decisions.\n+- `docs/adr/SADR-NNNN-*.md` records Shiki platform decisions.\n+- `docs/adr/NNNN-*.md` records target product decisions without sharing the\n+  SADR namespace.\n - `AGENTS.md` is the runtime-neutral agent constitution.\n - `CLAUDE.md` is the Claude Code wrapper around `AGENTS.md`.\n - `.shiki/` is the Target Repository mirror for Goals, PRDs, plans, Task DAGs, contracts, locks, worktree records, ledger entries, CCA verdicts, repair packets, reports, and handoffs.\n@@ -15,6 +17,6 @@\n \n - Use Shiki terms exactly when they exist.\n - Add glossary terms to `CONTEXT.md` only when the term is part of the domain, not a generic programming concept.\n-- Create ADRs only for hard-to-reverse, surprising, tradeoff-based decisions.\n+- Create SADRs or target ADRs only for hard-to-reverse, surprising, tradeoff-based decisions.\n - Keep product-specific language in the Target Repository, not in Shiki Template docs.\n-- If an ADR conflicts with a proposed implementation, surface the conflict before editing.\n+- If a SADR or target ADR conflicts with a proposed implementation, surface the conflict before editing.\ndiff --git a/docs/agents/execution-control.md b/docs/agents/execution-control.md\nindex ad8ee3cdb36be56fedc1f46408ca92b387d74047..bc63c3a73661d12b567016ba299f1a6b66bf9088\n--- a/docs/agents/execution-control.md\n+++ b/docs/agents/execution-control.md\n@@ -63,7 +63,7 @@\n | Branch name | Orchestrator | Orchestrator | Before implementer dispatch | branch policy | branch ref in task/ledger |\n | Lock reservation | Context & Impact | MergeGate | Before dispatch and before merge | `.shiki/locks/`, labels, PR metadata | lock id, owner, scope |\n | CI profile | Planner | MergeGate | During issue triage, rechecked at PR | labels, `.shiki/policy.yaml`, workflow matrix | required check list |\n-| Required status checks | Repo maintainer / Guardian | GitHub branch protection / rulesets | Repository setup, updated via ADR | GitHub settings | branch/ruleset config, ADR |\n+| Required status checks | Repo maintainer / Guardian | GitHub branch protection / rulesets | Repository setup, updated via target ADR | GitHub settings | branch/ruleset config, target ADR |\n | CCA judgment | CCA | GitHub required check / MergeGate | Every non-draft PR update | GitHub Action check | structured CCA verdict |\n | Repair assignment | CCA | MergeGate | After failed CCA/CI/review | repair packet, issue/PR comment | repair id and attempt count |\n | Merge readiness | MergeGate | GitHub branch protection / Guardian if needed | After CCA complete | required checks, review rules, rulesets | mergegate check output |\ndiff --git a/docs/agents/guardian-policy.md b/docs/agents/guardian-policy.md\nindex 4147c7adf05a47adb4fd24ada1a1c2e1f2ba6d2d..a20d9cc6c00a569bb057b9ca8d6b51c7fc4a9847\n--- a/docs/agents/guardian-policy.md\n+++ b/docs/agents/guardian-policy.md\n@@ -98,8 +98,8 @@\n `origin/<default_branch>` and reuses each job's existing base fetch; a shallow\n (`--depth`) or second base fetch would make the proof unresolvable and silently\n disable the carry, so neither is permitted. CCA reads `guardian_comment_carried` as\n-a recorded authority in the deterministic signal and does not re-derive it. See ADR\n-0018 for the full decision, the rejected diff/patch-equality and path-disjointness\n+a recorded authority in the deterministic signal and does not re-derive it. See\n+SADR-0018 for the full decision, the rejected diff/patch-equality and path-disjointness\n alternatives, and the accepted residual.\n \n ## External AI Guardian Review (SADR-0010)\ndiff --git a/docs/agents/implementation-policy.md b/docs/agents/implementation-policy.md\nindex c754144a9fb5bf4a38aa82a60e1670c59e626b44..85c37281bcad196c6b7b02343faf00b03cd9c57a\n--- a/docs/agents/implementation-policy.md\n+++ b/docs/agents/implementation-policy.md\n@@ -31,10 +31,13 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n ## Non-Negotiables\n \n 1. **Planning precedes implementation.** Non-trivial Goals start with `grill-with-docs` and Context & Impact.\n-2. **Domain language is controlled.** Terms must be checked against `CONTEXT.md`; hard-to-reverse tradeoffs must be checked against ADRs.\n+2. **Domain language is controlled.** Terms must be checked against `CONTEXT.md`; hard-to-reverse tradeoffs must be checked against Shiki SADRs and target ADRs.\n 3. **PRD precedes issue decomposition.** `to-prd` is used once the context is settled enough to become durable intent.\n 4. **Issues are vertical slices.** `to-issues` must produce independently grabbable tracer-bullet tasks, not horizontal layer tickets.\n 5. **Triage controls dispatch.** Only `ready-for-agent` / AFK tasks may be assigned to Codex without human input.\n@@ -95,19 +98,19 @@\n Process:\n \n - Challenge terms against `CONTEXT.md`.\n-- Check ADRs before reopening decisions.\n+- Check SADRs and target ADRs before reopening decisions.\n - Inspect code when code can answer a question.\n - Ask one question at a time when the operator is available.\n - Provide a recommended answer with each question.\n - Record resolved terms in `CONTEXT.md`.\n-- Create ADRs only for hard-to-reverse, surprising, tradeoff-based decisions.\n+- Create SADRs or target ADRs only for hard-to-reverse, surprising, tradeoff-based decisions.\n - Convert unresolved load-bearing questions into HITL blockers.\n \n Outputs:\n \n - Resolved design-tree decisions.\n - Updated glossary terms when needed.\n-- ADRs when justified.\n+- SADRs or target ADRs when justified.\n - HITL blockers when decisions remain unresolved.\n \n ## Phase 3 \u2014 Context & Impact\n@@ -117,7 +120,7 @@\n \n Outputs must include:\n \n-- Relevant documents and ADRs.\n+- Relevant documents, SADRs, and target ADRs.\n - Relevant modules, interfaces, seams, callers, and tests.\n - Candidate locks.\n - Dependencies.\n@@ -140,7 +143,7 @@\n - Testing decisions.\n - Out of scope.\n - Further notes.\n-- Links to relevant Goal, domain terms, and ADRs.\n+- Links to relevant Goal, domain terms, SADRs, and target ADRs.\n \n Phase 4 ends with Spec Freeze: the operator explicitly approves the PRD, the\n plan records a `spec_freeze` block (status, approved_by, source), and the SF\n@@ -240,7 +243,7 @@\n - Dependencies and locks.\n - Risk and Guardian requirements.\n - Ledger completeness.\n-- Documentation and ADR updates.\n+- Documentation and SADR/target-ADR updates.\n - Scope drift.\n \n CCA emits one of:\n@@ -301,7 +304,7 @@\n | Evidence failure | Behavior might be done, but proof is missing. | Add checks, PR evidence, ledger entry, or artifacts. |\n | Scope drift | PR changes unrelated behavior. | Revert unrelated changes or split into new Goal. |\n | Dependency block | Upstream task or lock not complete. | Block until dependency resolves. |\n-| Architecture block | Decision needs ADR or deep-module discussion. | `grill-with-docs` or `improve-codebase-architecture`. |\n+| Architecture block | Decision needs a SADR, target ADR, or deep-module discussion. | `grill-with-docs` or `improve-codebase-architecture`. |\n | Risk block | Security/production/policy change needs human approval. | Guardian decision. |\n \n ## Phase 5.5 \u2014 Execution Decision Control\ndiff --git a/docs/agents/runtime-auth-model.md b/docs/agents/runtime-auth-model.md\nindex 6a2e1a47e81ea89b5c68a210ec07772a9374babc..3c54748d24233dbd0ba8473950282547ef4ce935\n--- a/docs/agents/runtime-auth-model.md\n+++ b/docs/agents/runtime-auth-model.md\n@@ -148,7 +148,7 @@\n \n API-key based runners may be added by a target repository as an explicit extension. They are not the default Shiki template path.\n \n-If a repo opts into API-key mode, record the exception in an ADR and update the repo's `AGENTS.md`, `.shiki/config.yaml`, and workflow permissions.\n+If a repo opts into API-key mode, record the target-specific exception in a target ADR and update the repo's `AGENTS.md`, `.shiki/config.yaml`, and workflow permissions.\n \n ## GitHub Actions Runtime Compatibility\n \ndiff --git a/docs/agents/skill-gate.md b/docs/agents/skill-gate.md\nindex c50398b0d7c60a8598f2a5c88a10015730dced13..3acffe6444617bb1d54b77d4b4708158ec735bc6\n--- a/docs/agents/skill-gate.md\n+++ b/docs/agents/skill-gate.md\n@@ -9,7 +9,7 @@\n | Skill | Trigger | Expected output |\n | --- | --- | --- |\n | `setup-matt-pocock-skills` | First configuring a repo for these skills, or when issue tracker, triage labels, or domain docs are missing. | `docs/agents/` configuration and an `Agent skills` block in `AGENTS.md` or `CLAUDE.md`. |\n-| `grill-with-docs` | Ambiguous plan, unclear terminology, boundary decision, tradeoff, or ADR-worthy design choice. | Clarified decision tree, recommended answers, updates to `CONTEXT.md` or ADRs when decisions crystallize. |\n+| `grill-with-docs` | Ambiguous plan, unclear terminology, boundary decision, tradeoff, or decision-record-worthy design choice. | Clarified decision tree, recommended answers, updates to `CONTEXT.md`, SADRs, or target ADRs when decisions crystallize. |\n | `zoom-out` | Agent lacks a higher-level architectural map. | Map of relevant modules, callers, seams, and domain concepts. |\n | `to-prd` | Settled context should become a PRD. | PRD published to the issue tracker with problem, solution, user stories, decisions, testing, and out-of-scope notes. |\n | `to-issues` | Goal, PRD, or plan must become implementation tickets. | Vertical-slice issues with acceptance criteria, dependencies, and AFK/HITL classification. |\n@@ -17,7 +17,7 @@\n | `tdd` | Feature work or bug fix has observable behavior that can be specified with tests. | Red-green-refactor loop through public interfaces, one vertical slice at a time. |\n | `code-review` | The TDD loop is green and the implementer is about to create or update a PR. Default for every implementation task. | Pre-PR self-review of the task diff with fixes applied on the task branch; findings and resolutions recorded as a ledger entry naming the skill and a `## Pre-PR code review` PR body section. |\n | `diagnose` | Hard bug, failing check, regression, flaky behavior, or performance problem. | Reproduction loop, ranked hypotheses, instrumentation, fix, regression test, post-mortem. |\n-| `improve-codebase-architecture` | Structural friction, testability, AI-navigability, deep-module opportunity, or refactor request. | Architecture candidates and deepening recommendations informed by `CONTEXT.md` and ADRs. |\n+| `improve-codebase-architecture` | Structural friction, testability, AI-navigability, deep-module opportunity, or refactor request. | Architecture candidates and deepening recommendations informed by `CONTEXT.md`, SADRs, and target ADRs. |\n | `prototype` | A throwaway logic or UI prototype is needed to answer a design question. | Clearly marked disposable prototype and captured decision. |\n \n ## MergeGate Interaction\ndiff --git a/scripts/guardian_approval_signal.py b/scripts/guardian_approval_signal.py\nindex d059f92b2c82aa2b5d6ca421365537a87886c1cb..16924892714f4f5b9303c0ef0e325a7e70075630\n--- a/scripts/guardian_approval_signal.py\n+++ b/scripts/guardian_approval_signal.py\n@@ -199,8 +199,8 @@\n ) -> Any:\n     \"\"\"Evaluate SADR-0015 Contract Approval for the CCA Guardian signal.\n \n-    The SAME OR that MergeGate applies must reach this deterministic signal: ADR\n-    0015 is explicit that without it, a contract-approved PR still returns\n+    The SAME OR that MergeGate applies must reach this deterministic signal:\n+    SADR-0015 is explicit that without it, a contract-approved PR still returns\n     ``needs_guardian``, the CCA job fails, and the MergeGate policy check never\n     runs \u2014 the gate this change moves would never be reached. This resolves the\n     task the SAME way MergeGate does (the first Shiki task id in the PR body),\ndiff --git a/scripts/shiki_contract_approval.py b/scripts/shiki_contract_approval.py\nindex 941bcb96fdd8b9a85bc0c4f0f04d5dad9d04839c..4a4b418f6527413161db82670bec857a043360e2\n--- a/scripts/shiki_contract_approval.py\n+++ b/scripts/shiki_contract_approval.py\n@@ -142,7 +142,7 @@\n     changed_files_status: Any,\n     registration: Any,\n ) -> ContractApprovalResult:\n-    \"\"\"Whether this PR carries an SADR-0015 Contract Approval.\n+    \"\"\"Whether this PR carries a SADR-0015 Contract Approval.\n \n     The four conditions are evaluated IN ORDER, each failure recorded as a\n     distinct reason string:\ndiff --git a/scripts/shiki_guardian.py b/scripts/shiki_guardian.py\nindex a4ecb79bbda55fd5c8b06adfae2eb99760a03c45..4dd99bbe714e26f421fa19abac7a34ae1b195ecc\n--- a/scripts/shiki_guardian.py\n+++ b/scripts/shiki_guardian.py\n@@ -12,7 +12,7 @@\n # The carry re-derives its proof from git in-process on every judging invocation;\n # it is never read from an artifact, so there is nothing to go stale or be forged.\n # shiki_sync_proof is the ONLY guardian-family module that runs git, and every one\n-# of its invocations is read-only and local (ADR: prove-from-git base sync).\n+# of its invocations is read-only and local (SADR: prove-from-git base sync).\n from shiki_sync_proof import verify_pure_base_sync\n \n GUARDIAN_POLICY_PATH = \".shiki/guardian-policy.json\"\ndiff --git a/scripts/shiki_guardian_review.py b/scripts/shiki_guardian_review.py\nindex dd0f104d457899e0ba5ff1e162011c1e90b6f92b..e10ef58c046324e7ea3a3392feb3048ca77414ce\n--- a/scripts/shiki_guardian_review.py\n+++ b/scripts/shiki_guardian_review.py\n@@ -159,7 +159,7 @@\n     docs_only = _is_docs_only(changed_files)\n     if docs_only and matched == 0:\n         focus.append(\n-            \"Docs/ADR-only change: confirm the change is genuinely documentation/decision-only, introduces no behavior or contract drift, and stays consistent with the constitution and existing ADRs.\"\n+            \"Docs/decision-record-only change: confirm the change is genuinely documentation/decision-only, introduces no behavior or contract drift, and stays consistent with the constitution and existing SADRs and target ADRs.\"\n         )\n \n     # Conservative augmentation: unknown (no category and not docs-only), mixed\ndiff --git a/scripts/shiki_installer.py b/scripts/shiki_installer.py\nindex be064ab7a29ff6836f7f7a1d70500a43e0d11d7b..9fd8eefd137ce7003dad5c0dcb2e48e2cd47ab32\n--- a/scripts/shiki_installer.py\n+++ b/scripts/shiki_installer.py\n@@ -118,6 +118,7 @@\n # what lets a target carry it without a governance-schema change.\n INSTALL_STAMP_PATH = \".shiki/install-stamp.json\"\n INSTALL_STAMP_VERSION = 1\n+LEGACY_SHIKI_SADR_MAX = 18\n \n # Under --force the shipped surface splits three ways, so nothing is lost and\n # nothing is silently stale.\n@@ -237,9 +238,98 @@\n         )\n \n \n+def legacy_shiki_adr_paths() -> tuple[str, ...]:\n+    \"\"\"Former numeric paths for Shiki SADR-0001 through SADR-0018.\n+\n+    The authoritative slugs come from the current SADR filenames. SADR-0019 is\n+    the namespace decision itself and never shipped under a numeric legacy path.\n+    \"\"\"\n+    paths: list[str] = []\n+    for sadr in sorted((ROOT / \"docs\" / \"adr\").glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\")):\n+        number = sadr.name.split(\"-\", 2)[1]\n+        if int(number) <= LEGACY_SHIKI_SADR_MAX:\n+            paths.append(f\"docs/adr/{sadr.name.removeprefix('SADR-')}\")\n+    return tuple(paths)\n+\n+\n+def preflight_legacy_sadr_cleanup(target: Path, *, force: bool) -> tuple[str, ...]:\n+    \"\"\"Authorize exact legacy Shiki ADR removals before the first write.\n+\n+    A non-force install never removes existing files. A forced install needs no\n+    stamp when no legacy Shiki path is present. When legacy paths do exist, each\n+    must be named in a readable install stamp and its current bytes must match\n+    the recorded digest.\n+    \"\"\"\n+    if not force:\n+        return ()\n+    present = tuple(\n+        relative\n+        for relative in legacy_shiki_adr_paths()\n+        if (target / relative).exists() or (target / relative).is_symlink()\n+    )\n+    if not present:\n+        return ()\n+\n+    stamp_path = target / INSTALL_STAMP_PATH\n+    try:\n+        raw = stamp_path.read_text(encoding=\"utf-8\")\n+        stamp = json.loads(raw)\n+    except FileNotFoundError:\n+        detail = \"install stamp is absent\"\n+        blockers = [f\"{relative}: {detail}\" for relative in present]\n+    except (OSError, UnicodeDecodeError) as error:\n+        detail = f\"install stamp is unreadable ({error})\"\n+        blockers = [f\"{relative}: {detail}\" for relative in present]\n+    except json.JSONDecodeError as error:\n+        detail = f\"install stamp is malformed ({error})\"\n+        blockers = [f\"{relative}: {detail}\" for relative in present]\n+    else:\n+        if not isinstance(stamp, dict):\n+            blockers = [\n+                f\"{relative}: install stamp is malformed (expected an object)\"\n+                for relative in present\n+            ]\n+        else:\n+            digests = stamp.get(\"digests\")\n+            blockers = []\n+            if not isinstance(digests, dict):\n+                blockers.extend(\n+                    f\"{relative}: install stamp has no digest for this path\"\n+                    for relative in present\n+                )\n+            else:\n+                for relative in present:\n+                    expected = digests.get(relative)\n+                    if not isinstance(expected, str) or not expected:\n+                        blockers.append(\n+                            f\"{relative}: install stamp has no digest for this path\"\n+                        )\n+                        continue\n+                    try:\n+                        actual = _sha256_file(target / relative)\n+                    except OSError as error:\n+                        blockers.append(\n+                            f\"{relative}: legacy path is unreadable ({error})\"\n+                        )\n+                        continue\n+                    if actual != expected:\n+                        blockers.append(\n+                            f\"{relative}: digest mismatch (stamp {expected}, current {actual})\"\n+                        )\n+\n+    if blockers:\n+        joined = \"\\n\".join(f\"- {blocker}\" for blocker in blockers)\n+        raise ShikiError(\n+            \"legacy Shiki ADR cleanup is not authorized:\\n\"\n+            f\"{joined}\\nNo files were written.\"\n+        )\n+    return present\n+\n+\n def install_template(target: Path, *, force: bool, validate: bool) -> list[NewFileNote]:\n     # Preconditions first: never rewrite the tree of a target that cannot be\n     # upgraded cleanly (see refuse_pending_migrations).\n+    legacy_cleanup = preflight_legacy_sadr_cleanup(target, force=force)\n     refuse_pending_migrations(target)\n \n     # Resolve the target's required CODEOWNERS owner once from its\n@@ -250,6 +340,10 @@\n     # verbatim. See ``codeowners_required_owner``.\n     codeowners_owner = codeowners_required_owner(target)\n \n+    for relative in legacy_cleanup:\n+        (target / relative).unlink()\n+        info(f\"removed stamped legacy Shiki ADR: {target / relative}\")\n+\n     notes: list[NewFileNote] = []\n     for relative in TEMPLATE_PATHS:\n         source = ROOT / relative\n@@ -647,6 +741,22 @@\n     digests = target_stamp_digests(target)\n     existing = read_install_stamp(target)\n     if existing is not None:\n+        # A non-force install deliberately leaves legacy Shiki ADRs in place.\n+        # Preserve only still-matching ownership proof so a later forced\n+        # upgrade can authorize cleanup; never carry a missing or stale digest.\n+        existing_digests = existing.get(\"digests\")\n+        if isinstance(existing_digests, dict):\n+            for relative in legacy_shiki_adr_paths():\n+                expected = existing_digests.get(relative)\n+                path = target / relative\n+                if not isinstance(expected, str) or not path.is_file():\n+                    continue\n+                try:\n+                    actual = _sha256_file(path)\n+                except OSError:\n+                    continue\n+                if actual == expected:\n+                    digests[relative] = actual\n         if _incoming_is_older(commit, existing.get(\"platform_commit\")):\n             warn(\n                 \"kept existing install stamp: incoming install \"\ndiff --git a/scripts/shiki_loop.py b/scripts/shiki_loop.py\nindex 3611641da8aeb5038b1da90e3114b09a473ec6d8..84caa491b9cad0edc4176f209c13e9221e975681\n--- a/scripts/shiki_loop.py\n+++ b/scripts/shiki_loop.py\n@@ -424,7 +424,7 @@\n         # unresolvable CCA verdict has already produced its terminal stop and is never\n         # laundered into a head-moving sync (SADR-0010). It also never fires while the\n         # policy (Guardian) gate is red \u2014 for ANY reason, not only when it is the sole\n-        # red check \u2014 so a head-bound Guardian approval, or an SADR-0015\n+        # red check \u2014 so a head-bound Guardian approval, or a SADR-0015\n         # contract-approval carry that base movement defeated (the policy gate compares\n         # base and head task governance directly), is never silently restored by\n         # adopting base wholesale. It is bounded by a durable per-task counter so the\ndiff --git a/scripts/shiki_tasks.py b/scripts/shiki_tasks.py\nindex aec3809f24b4a0c8c96fc50971cff9ae9ad30381..d531052e9d9717058d4631483cefab44c26a65c1\n--- a/scripts/shiki_tasks.py\n+++ b/scripts/shiki_tasks.py\n@@ -821,7 +821,7 @@\n         \"prompt\": prompt,\n         \"entry_skill\": \"grill-with-docs\",\n         \"required_next_steps\": [\n-            \"Run grill-with-docs until terminology, boundaries, risks, and ADR-worthy decisions are settled.\",\n+            \"Run grill-with-docs until terminology, boundaries, risks, and decision-record-worthy choices are settled.\",\n             \"Obtain the operator's explicit approval of the PRD/requirements (Spec Freeze).\",\n             \"Write a machine-readable plan JSON with grill_with_docs.status=complete and spec_freeze.status=frozen.\",\n             \"Run shiki plan ingest --plan-file PLAN.json.\",\ndiff --git a/scripts/test_shiki_goal_loop.sh b/scripts/test_shiki_goal_loop.sh\nindex 753c7b516e10f0be3bc92a4bd0eb1fa699776ae6..8f3820a0a079b3044424cfe1a6c6b7fe68896e67\n--- a/scripts/test_shiki_goal_loop.sh\n+++ b/scripts/test_shiki_goal_loop.sh\n@@ -231,8 +231,8 @@\n \n # High/critical risk merges autonomously when all required checks are green:\n # the \"MergeGate policy check\" required check IS the Guardian gate (it enforces\n-# guardian-policy.json \u2014 human approval OR external AI guardian review, ADR\n-# 0010), so green checks mean a recorded authority approved. The fake gh returns\n+# guardian-policy.json \u2014 human approval OR external AI guardian review,\n+# SADR-0010), so green checks mean a recorded authority approved. The fake gh returns\n # green checks, simulating that the Guardian gate passed.\n python3 \"$ROOT/scripts/shiki.py\" goal create --target \"$TARGET\" --title \"Guardian gate\" --outcome \"High risk merges via policy gate\" >/tmp/shiki-goal-loop-guardian-goal.json\n GGOAL=\"$(json_get /tmp/shiki-goal-loop-guardian-goal.json goal_id)\"\ndiff --git a/tests/test_adr_references.py b/tests/test_adr_references.py\nindex 51706329c797d5dd9b47e082a333fa83b261d482..db6e693e35f67103935be34f1d9bb4ff7ee60395\n--- a/tests/test_adr_references.py\n+++ b/tests/test_adr_references.py\n@@ -1,7 +1,7 @@\n-\"\"\"Every ``ADR ####`` citation must resolve to exactly one decision record.\n+\"\"\"Every target ADR and Shiki SADR citation resolves in its own namespace.\n \n Background. ``scripts/mergegate_check.py`` justifies ``is_bookkeeping_closeout``\n-with \"ADR 0017\" nine times and ``scripts/guardian_approval_signal.py`` cites\n+with \"SADR-0017\" nine times and ``scripts/guardian_approval_signal.py`` cites\n 0016/0017 seven times, yet for a stretch the numbering in ``docs/adr/`` stopped\n at 0015: the authoring commits lived on a coordinator branch and no task ever\n declared ``docs/adr/...`` in its locks, so nothing carried the records to the\n@@ -11,11 +11,12 @@\n and the directory drifted with no signal.\n \n This is the same unbound-pair shape behind other silent failures: a reference\n-and its target with nothing asserting they agree. This test binds them. It scans\n-every ``scripts/*.py`` (plus ``docs/**/*.md`` and the root ``*.md`` governance\n-files, which are cheap) for the ``ADR ####`` pattern and asserts each cited\n-number resolves to exactly one ``docs/adr/####-*.md``. The converse is\n-deliberately NOT required: an ADR may exist without being cited.\n+and its target with nothing asserting they agree. This test binds them. Target\n+``ADR NNNN`` citations resolve only to ``docs/adr/NNNN-*.md``; Shiki\n+``SADR-NNNN`` citations resolve only to ``docs/adr/SADR-NNNN-*.md``. The scan\n+covers current governance docs, scripts, workflows, prompts, packaging metadata,\n+and repo-local Codex guidance while deliberately excluding historical\n+``.shiki`` evidence. The converse is not required: a record may be uncited.\n \n The suite is dependency free: standard-library ``unittest`` only.\n \"\"\"\n@@ -31,17 +32,18 @@\n \n REPO_ROOT = Path(__file__).resolve().parents[1]\n \n-# \"ADR 0016\" -- a four-digit citation. The trailing \\b keeps a five-digit token\n-# from being read as a truncated four-digit number.\n-_CITATION = re.compile(r\"\\bADR (\\d{4})\\b\")\n-# \"0016-locks-bound-product-paths.md\" -- the leading four digits name the record.\n-_ADR_FILENAME = re.compile(r\"^(\\d{4})-.*\\.md$\")\n+# ``ADR 0016`` / ``SADR-0016`` -- a namespace plus four-digit citation. The\n+# trailing ``\\b`` keeps a five-digit token from being read as a truncated one.\n+_CITATION = re.compile(r\"\\b(S?ADR)[ -](\\d{4})\\b\")\n+# Target ADRs retain the numeric filename; Shiki platform decisions use SADR.\n+_DECISION_FILENAME = re.compile(r\"^(?:(SADR)-)?(\\d{4})-.*\\.md$\")\n \n \n class Violation(NamedTuple):\n     \"\"\"A citation that does not resolve to exactly one decision record.\"\"\"\n \n     citing_file: str  # repo-relative path of the file that cites the ADR\n+    namespace: str  # \"ADR\" for target decisions; \"SADR\" for Shiki decisions\n     number: str  # the cited four-digit number, e.g. \"0016\"\n     line: int  # 1-based line of the citation\n     reason: str  # \"missing\" (zero matches) or \"ambiguous\" (two or more)\n@@ -49,44 +51,59 @@\n \n     @property\n     def expected_path(self) -> str:\n-        return f\"docs/adr/{self.number}-*.md\"\n+        prefix = \"SADR-\" if self.namespace == \"SADR\" else \"\"\n+        return f\"docs/adr/{prefix}{self.number}-*.md\"\n \n     def describe(self) -> str:\n         \"\"\"Name the citing file, the cited number, and the missing path.\"\"\"\n         if self.reason == \"missing\":\n             return (\n-                f\"{self.citing_file}:{self.line} cites ADR {self.number} \"\n+                f\"{self.citing_file}:{self.line} cites {self.namespace} {self.number} \"\n                 f\"but no document matches {self.expected_path}\"\n             )\n         joined = \", \".join(self.matches)\n         return (\n-            f\"{self.citing_file}:{self.line} cites ADR {self.number} which \"\n+            f\"{self.citing_file}:{self.line} cites {self.namespace} {self.number} which \"\n             f\"resolves to {len(self.matches)} documents (expected exactly one \"\n             f\"at {self.expected_path}): {joined}\"\n         )\n \n \n-def _adr_index(root: Path) -> dict[str, list[Path]]:\n-    \"\"\"Map every four-digit ADR number to the record files that claim it.\"\"\"\n-    index: dict[str, list[Path]] = {}\n+def _adr_index(root: Path) -> dict[tuple[str, str], list[Path]]:\n+    \"\"\"Map each decision namespace and number to the files that claim it.\"\"\"\n+    index: dict[tuple[str, str], list[Path]] = {}\n     adr_dir = root / \"docs\" / \"adr\"\n     if adr_dir.is_dir():\n         for path in sorted(adr_dir.glob(\"*.md\")):\n-            match = _ADR_FILENAME.match(path.name)\n+            match = _DECISION_FILENAME.match(path.name)\n             if match:\n-                index.setdefault(match.group(1), []).append(path)\n+                namespace = \"SADR\" if match.group(1) else \"ADR\"\n+                index.setdefault((namespace, match.group(2)), []).append(path)\n     return index\n \n \n def _citing_files(root: Path, include_docs: bool = True) -> list[Path]:\n-    \"\"\"Files scanned for citations: scripts/*.py, and cheaply the markdown.\"\"\"\n+    \"\"\"Current, shipped text surfaces scanned for decision citations.\"\"\"\n     files = sorted((root / \"scripts\").glob(\"*.py\"))\n+    files += sorted((root / \"scripts\").glob(\"*.sh\"))\n     if include_docs:\n         docs = root / \"docs\"\n         if docs.is_dir():\n             files += sorted(docs.rglob(\"*.md\"))\n         files += sorted(root.glob(\"*.md\"))\n-    return files\n+        pyproject = root / \"pyproject.toml\"\n+        if pyproject.is_file():\n+            files.append(pyproject)\n+        for relative in (\".github\", \".codex\"):\n+            surface = root / relative\n+            if surface.is_dir():\n+                files += sorted(\n+                    path\n+                    for path in surface.rglob(\"*\")\n+                    if path.is_file()\n+                    and path.suffix in {\".md\", \".py\", \".sh\", \".toml\", \".yaml\", \".yml\"}\n+                )\n+    return sorted(set(files))\n \n \n def find_adr_reference_violations(\n@@ -94,10 +111,9 @@\n ) -> list[Violation]:\n     \"\"\"Return every ``ADR ####`` citation under ``root`` that fails to resolve.\n \n-    A citation resolves when exactly one ``docs/adr/####-*.md`` matches its\n-    number. Zero matches is a \"missing\" violation; two or more is \"ambiguous\".\n-    Records that are never cited produce nothing -- the converse is not\n-    required.\n+    A citation resolves when exactly one record in the same namespace matches\n+    its number. Zero matches is a \"missing\" violation; two or more is\n+    \"ambiguous\". Records that are never cited produce nothing.\n     \"\"\"\n     root = Path(root)\n     index = _adr_index(root)\n@@ -110,13 +126,15 @@\n         rel = path.relative_to(root).as_posix()\n         for lineno, line in enumerate(text.splitlines(), start=1):\n             for match in _CITATION.finditer(line):\n-                number = match.group(1)\n-                found = index.get(number, [])\n+                namespace, number = match.groups()\n+                found = index.get((namespace, number), [])\n                 if len(found) == 1:\n                     continue\n                 reason = \"missing\" if not found else \"ambiguous\"\n                 rels = [p.relative_to(root).as_posix() for p in found]\n-                violations.append(Violation(rel, number, lineno, reason, rels))\n+                violations.append(\n+                    Violation(rel, namespace, number, lineno, reason, rels)\n+                )\n     return violations\n \n \n@@ -129,8 +147,8 @@\n         self.assertEqual(\n             violations,\n             [],\n-            \"Every 'ADR ####' citation must resolve to exactly one \"\n-            f\"docs/adr/####-*.md:\\n{detail}\",\n+            \"Every ADR/SADR citation must resolve exactly once in its own \"\n+            f\"docs/adr namespace:\\n{detail}\",\n         )\n \n     def test_scan_actually_reads_scripts(self) -> None:\n@@ -146,8 +164,21 @@\n         # The two records this Goal carried onto the default branch, and which\n         # the scripts cite most, must be present.\n         index = _adr_index(REPO_ROOT)\n-        self.assertIn(\"0016\", index)\n-        self.assertIn(\"0017\", index)\n+        self.assertIn((\"SADR\", \"0016\"), index)\n+        self.assertIn((\"SADR\", \"0017\"), index)\n+\n+    def test_platform_tree_has_only_sadr_0001_through_0019(self) -> None:\n+        adr_dir = REPO_ROOT / \"docs\" / \"adr\"\n+        sadrs = sorted(adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\"))\n+        self.assertEqual(\n+            {path.name.split(\"-\", 2)[1] for path in sadrs},\n+            {f\"{number:04d}\" for number in range(1, 20)},\n+        )\n+        self.assertEqual(\n+            list(adr_dir.glob(\"[0-9][0-9][0-9][0-9]-*.md\")),\n+            [],\n+            \"the Shiki platform tree must not retain legacy numeric SADR paths\",\n+        )\n \n \n class FixtureAdrReferences(unittest.TestCase):\n@@ -180,6 +211,26 @@\n         self.assertIn(\"9991\", message)  # cited number\n         self.assertIn(\"docs/adr/9991-*.md\", message)  # missing path\n \n+    def test_target_adr_does_not_satisfy_missing_platform_sadr(self) -> None:\n+        root = self._make_repo(\n+            adr_names=[\"0042-target-decision.md\"],\n+            script_body='\"\"\"Target ADR 0042 exists, but platform SADR-0042 does not.\"\"\"\\n',\n+        )\n+        violations = find_adr_reference_violations(root, include_docs=False)\n+        self.assertEqual(len(violations), 1)\n+        self.assertEqual(violations[0].namespace, \"SADR\")\n+        self.assertEqual(violations[0].number, \"0042\")\n+        self.assertEqual(violations[0].expected_path, \"docs/adr/SADR-0042-*.md\")\n+\n+    def test_target_adr_and_platform_sadr_may_reuse_a_number(self) -> None:\n+        root = self._make_repo(\n+            adr_names=[\"0042-target-decision.md\", \"SADR-0042-platform-decision.md\"],\n+            script_body='\"\"\"Target ADR 0042 and platform SADR-0042 are distinct.\"\"\"\\n',\n+        )\n+        self.assertEqual(\n+            find_adr_reference_violations(root, include_docs=False), []\n+        )\n+\n     def test_uncited_adr_is_allowed(self) -> None:\n         # An ADR exists but nothing cites it -> not a violation. The converse\n         # (every ADR must be cited) is explicitly NOT required.\ndiff --git a/tests/test_guardian_review.py b/tests/test_guardian_review.py\nindex 88829e7810212635e2fc98aa5918d6f26294677d..00a85a237acd5bf2d6cb5b294128198c422402ca\n--- a/tests/test_guardian_review.py\n+++ b/tests/test_guardian_review.py\n@@ -162,7 +162,7 @@\n \n     def test_docs_only_is_light_and_not_conservative(self):\n         focus = gr.classify_review_focus([\"docs/adr/0099-x.md\", \"README.md\"], \"\", \"low\")\n-        self.assertTrue(any(\"Docs/ADR-only\" in f for f in focus))\n+        self.assertTrue(any(\"Docs/decision-record-only\" in f for f in focus))\n         self.assertFalse(any(\"conservative default\" in f.lower() for f in focus))\n \n     def test_unknown_category_is_conservative(self):\ndiff --git a/tests/test_policy_job_merged_prs.py b/tests/test_policy_job_merged_prs.py\nindex d45512e74681e5e649279236c7d4e79d5c3da471..b97989f8bd74d091de1a6b5275a20d8dc68c4512\n--- a/tests/test_policy_job_merged_prs.py\n+++ b/tests/test_policy_job_merged_prs.py\n@@ -3,8 +3,8 @@\n \n ``.github/workflows/shiki-cca-completion.yml`` has two consumers of the merged-PR\n proof: the CCA *signal* (``guardian_approval_signal.py`` in \"Prepare CCA context\")\n-and the authoritative gate (``mergegate_check.py`` in \"Enforce MergeGate\"). ADR\n-0017's bookkeeping-closeout exemption (condition 6) needs the merged implementation\n+and the authoritative gate (``mergegate_check.py`` in \"Enforce MergeGate\").\n+SADR-0017's bookkeeping-closeout exemption (condition 6) needs the merged implementation\n PR proven, and a closeout carries NO ``post_merge_reconcile`` marker. The signal\n already resolves ``MERGED_PRS`` unconditionally, but \"Enforce MergeGate\" used to\n resolve it only inside ``grep -q '<!-- shiki:post_merge_reconcile -->'``. So a\ndiff --git a/tests/test_sadr_install_migration.py b/tests/test_sadr_install_migration.py\nnew file mode 100644\nindex 0000000000000000000000000000000000000000..a6f4c57e4dcaef73ef600b0d4804744138ab9071\n--- /dev/null\n+++ b/tests/test_sadr_install_migration.py\n@@ -0,0 +1,229 @@\n+\"\"\"SADR installation and collision-safe legacy upgrade behavior.\"\"\"\n+\n+from __future__ import annotations\n+\n+import contextlib\n+import hashlib\n+import io\n+import json\n+import shutil\n+import tempfile\n+import unittest\n+from pathlib import Path\n+\n+import shiki_test_support  # noqa: F401  (path bootstrap)\n+\n+import shiki_installer\n+from tests.test_adr_references import find_adr_reference_violations\n+\n+\n+PRODUCT_NUMBERS = tuple(range(13, 19))\n+\n+\n+def _quiet_install(target: Path, *, force: bool, validate: bool) -> None:\n+    with contextlib.redirect_stdout(io.StringIO()):\n+        shiki_installer.install_template(target, force=force, validate=validate)\n+\n+\n+def _sha256(path: Path) -> str:\n+    return hashlib.sha256(path.read_bytes()).hexdigest()\n+\n+\n+def _legacy_sadr_paths() -> tuple[str, ...]:\n+    return tuple(\n+        f\"docs/adr/{path.name.removeprefix('SADR-')}\"\n+        for path in sorted((shiki_installer.ROOT / \"docs\" / \"adr\").glob(\"SADR-*.md\"))\n+        if path.name != \"SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\"\n+    )\n+\n+\n+def _create_legacy_upgrade_fixture(\n+    target: Path,\n+) -> tuple[dict[str, bytes], tuple[str, ...]]:\n+    adr_dir = target / \"docs\" / \"adr\"\n+    adr_dir.mkdir(parents=True)\n+    product_bytes = {\n+        f\"{number:04d}-cubeshare-product-decision.md\": (\n+            f\"# CubeShare ADR {number:04d}\\n\\nDo not rewrite {number}.\\n\"\n+        ).encode()\n+        for number in PRODUCT_NUMBERS\n+    }\n+    for name, content in product_bytes.items():\n+        (adr_dir / name).write_bytes(content)\n+\n+    legacy_paths = _legacy_sadr_paths()\n+    for relative in legacy_paths:\n+        (target / relative).write_bytes(f\"legacy Shiki bytes for {relative}\\n\".encode())\n+\n+    stamp_path = target / shiki_installer.INSTALL_STAMP_PATH\n+    stamp_path.parent.mkdir(parents=True, exist_ok=True)\n+    stamp = {\n+        \"version\": shiki_installer.INSTALL_STAMP_VERSION,\n+        \"platform_commit\": None,\n+        \"installed_at\": \"2000-01-01T00:00:00+00:00\",\n+        \"digests\": {\n+            relative: _sha256(target / relative) for relative in legacy_paths\n+        },\n+    }\n+    stamp_path.write_text(\n+        json.dumps(stamp, indent=2, sort_keys=True) + \"\\n\", encoding=\"utf-8\"\n+    )\n+    return product_bytes, legacy_paths\n+\n+\n+def _tree_snapshot(root: Path) -> dict[str, tuple[str, bytes | str]]:\n+    \"\"\"Recursive byte/shape snapshot used to prove preflight is write-free.\"\"\"\n+    snapshot: dict[str, tuple[str, bytes | str]] = {}\n+    for path in sorted(root.rglob(\"*\")):\n+        relative = path.relative_to(root).as_posix()\n+        if path.is_symlink():\n+            snapshot[relative] = (\"symlink\", path.readlink().as_posix())\n+        elif path.is_dir():\n+            snapshot[relative] = (\"directory\", \"\")\n+        else:\n+            snapshot[relative] = (\"file\", path.read_bytes())\n+    return snapshot\n+\n+\n+class FreshSadrInstallTests(unittest.TestCase):\n+    def setUp(self) -> None:\n+        self.tmp = Path(tempfile.mkdtemp(prefix=\"shiki-sadr-fresh-\"))\n+        self.addCleanup(lambda: shutil.rmtree(self.tmp, ignore_errors=True))\n+        self.target = self.tmp / \"target\"\n+        self.adr_dir = self.target / \"docs\" / \"adr\"\n+        self.adr_dir.mkdir(parents=True)\n+        self.product_bytes = {\n+            f\"{number:04d}-product-decision.md\": (\n+                f\"# Product ADR {number:04d}\\n\\nTarget-owned bytes {number}.\\n\"\n+            ).encode()\n+            for number in PRODUCT_NUMBERS\n+        }\n+        for name, content in self.product_bytes.items():\n+            (self.adr_dir / name).write_bytes(content)\n+\n+    def test_fresh_install_preserves_product_adrs_and_installs_all_sadrs(self) -> None:\n+        _quiet_install(self.target, force=False, validate=True)\n+\n+        for name, expected in self.product_bytes.items():\n+            self.assertEqual((self.adr_dir / name).read_bytes(), expected)\n+\n+        sadr_numbers = {\n+            path.name.split(\"-\", 2)[1]\n+            for path in self.adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\")\n+        }\n+        self.assertEqual(sadr_numbers, {f\"{number:04d}\" for number in range(1, 20)})\n+\n+        numeric_records = {\n+            path.name\n+            for path in self.adr_dir.glob(\"[0-9][0-9][0-9][0-9]-*.md\")\n+        }\n+        self.assertEqual(numeric_records, set(self.product_bytes))\n+        self.assertEqual(find_adr_reference_violations(self.target), [])\n+\n+\n+class LegacySadrUpgradeTests(unittest.TestCase):\n+    def setUp(self) -> None:\n+        self.tmp = Path(tempfile.mkdtemp(prefix=\"shiki-sadr-upgrade-\"))\n+        self.addCleanup(lambda: shutil.rmtree(self.tmp, ignore_errors=True))\n+        self.target = self.tmp / \"target\"\n+        self.adr_dir = self.target / \"docs\" / \"adr\"\n+        self.product_bytes, self.legacy_paths = _create_legacy_upgrade_fixture(\n+            self.target\n+        )\n+\n+    def test_force_upgrade_deletes_only_stamped_legacy_sadrs(self) -> None:\n+        _quiet_install(self.target, force=True, validate=True)\n+\n+        for relative in self.legacy_paths:\n+            self.assertFalse((self.target / relative).exists(), relative)\n+        for name, expected in self.product_bytes.items():\n+            self.assertEqual((self.adr_dir / name).read_bytes(), expected)\n+\n+        sadrs = list(self.adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\"))\n+        self.assertEqual(len(sadrs), 19)\n+        refreshed = json.loads(\n+            (self.target / shiki_installer.INSTALL_STAMP_PATH).read_text(encoding=\"utf-8\")\n+        )\n+        for relative in self.legacy_paths:\n+            self.assertNotIn(relative, refreshed[\"digests\"])\n+        for path in sadrs:\n+            relative = path.relative_to(self.target).as_posix()\n+            self.assertEqual(refreshed[\"digests\"][relative], _sha256(path))\n+\n+    def test_non_force_install_never_deletes_legacy_files(self) -> None:\n+        before = {\n+            relative: (self.target / relative).read_bytes()\n+            for relative in self.legacy_paths\n+        }\n+        _quiet_install(self.target, force=False, validate=False)\n+        for relative, expected in before.items():\n+            self.assertEqual((self.target / relative).read_bytes(), expected)\n+        refreshed = json.loads(\n+            (self.target / shiki_installer.INSTALL_STAMP_PATH).read_text(encoding=\"utf-8\")\n+        )\n+        for relative in self.legacy_paths:\n+            self.assertEqual(refreshed[\"digests\"][relative], _sha256(self.target / relative))\n+\n+\n+class LegacyCleanupPreflightTests(unittest.TestCase):\n+    def setUp(self) -> None:\n+        self.tmp = Path(tempfile.mkdtemp(prefix=\"shiki-sadr-preflight-\"))\n+        self.addCleanup(lambda: shutil.rmtree(self.tmp, ignore_errors=True))\n+\n+    def test_all_ownership_blockers_abort_before_any_write(self) -> None:\n+        cases = (\n+            (\"absent\", \"install stamp is absent\"),\n+            (\"unreadable\", \"install stamp is unreadable\"),\n+            (\"malformed\", \"install stamp is malformed\"),\n+            (\"missing-digest\", \"install stamp has no digest\"),\n+            (\"digest-mismatch\", \"digest mismatch\"),\n+        )\n+        for name, expected_reason in cases:\n+            with self.subTest(name=name):\n+                target = self.tmp / name\n+                _, legacy_paths = _create_legacy_upgrade_fixture(target)\n+                stamp_path = target / shiki_installer.INSTALL_STAMP_PATH\n+\n+                if name == \"absent\":\n+                    stamp_path.unlink()\n+                elif name == \"unreadable\":\n+                    stamp_path.unlink()\n+                    stamp_path.mkdir()\n+                elif name == \"malformed\":\n+                    stamp_path.write_text(\"{not-json\\n\", encoding=\"utf-8\")\n+                else:\n+                    stamp = json.loads(stamp_path.read_text(encoding=\"utf-8\"))\n+                    if name == \"missing-digest\":\n+                        stamp[\"digests\"] = {}\n+                    else:\n+                        stamp[\"digests\"] = {\n+                            relative: \"deadbeef\" for relative in legacy_paths\n+                        }\n+                    stamp_path.write_text(\n+                        json.dumps(stamp, indent=2, sort_keys=True) + \"\\n\",\n+                        encoding=\"utf-8\",\n+                    )\n+\n+                before = _tree_snapshot(target)\n+                with self.assertRaises(shiki_installer.ShikiError) as raised:\n+                    _quiet_install(target, force=True, validate=False)\n+                after = _tree_snapshot(target)\n+\n+                self.assertEqual(before, after, f\"{name} preflight changed the tree\")\n+                message = str(raised.exception)\n+                self.assertIn(expected_reason, message)\n+                self.assertIn(\"No files were written\", message)\n+                for relative in legacy_paths:\n+                    self.assertIn(relative, message)\n+\n+    def test_absent_stamp_is_allowed_when_no_legacy_path_exists(self) -> None:\n+        target = self.tmp / \"no-legacy\"\n+        target.mkdir()\n+        _quiet_install(target, force=True, validate=False)\n+        self.assertTrue(\n+            (target / \"docs\" / \"adr\" / \"SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\").is_file()\n+        )\n+\n+\n+if __name__ == \"__main__\":\n+    unittest.main()\n\nexec\n/bin/zsh -lc \"sed -n '56,80p' docs/usage.md\" in /private/tmp/shiki-adr-collision.pa0KwY/.worktrees/shiki-t-20260807t073307221254z-2d22d654-introduc\nexec\n/bin/zsh -lc \"sed -n '176,205p' docs/agents/bootstrap-command.md\" in /private/tmp/shiki-adr-collision.pa0KwY/.worktrees/shiki-t-20260807t073307221254z-2d22d654-introduc\n succeeded in 0ms:\n  `--github-host`, and `--github-api-url` for GitHub Enterprise-compatible hosts.\n- Shares the bounded flags with `shiki start`: `--adopt-existing-repo`,\n  `--no-set-secret`, `--no-protect`, `--no-commit`, `--no-push`.\n\n## `shiki install-target` \u2014 local-only template copy\n\nUse `shiki install-target` only for tests, fixtures, or explicit local-only\ntemplate inspection. It is **not** for normal setup, because Shiki is\nGitHub-first.\n\n```bash\nshiki install-target /path/to/target-repo --local-only\n```\n\nUse `--force` only when you intentionally want to overwrite existing target\nfiles.\n\n## `shiki bootstrap-platform` \u2014 publish the Shiki platform repo\n\nUse `shiki bootstrap-platform` to initialize and publish **this Shiki platform\nrepository** itself (as opposed to a downstream Target Repository).\n\n```bash\nCLAUDE_CODE_OAUTH_TOKEN=... shiki bootstrap-platform --repo OWNER/shiki --private\n```\n\n succeeded in 0ms:\n- `--no-set-secret` skips `CLAUDE_CODE_OAUTH_TOKEN` setup.\n- `--no-protect` skips branch protection configuration.\n- `--no-commit` skips manifest commit creation.\n- `--no-push` skips pushing and default-branch mutation.\n\nWhen execute mode is used, missing required secret input remains a hard failure\nunless `--no-set-secret` is passed, and branch protection failure remains a hard\nfailure unless `--no-protect` is passed.\n\n## Local-Only Template Copy\n\n```bash\nshiki install-target /path/to/target-repo --local-only\n```\n\nUse this only for tests, fixtures, or explicit local-only template inspection.\nUse `--force` only when you intentionally want to overwrite existing target files.\n\n## Slash Command\n\nAfter `shiki install-global`, Claude Code can invoke:\n\n```text\n/shiki <goal or task>\n```\n\nCodex CLI does not currently expose installed skills as custom slash commands,\nso `/shiki` is expected to be unrecognized there. In Codex, invoke Shiki with\nnatural language, for example:\n\n\nexec\n/bin/zsh -lc \"sed -n '60,125p' docs/operations.md\" in /private/tmp/shiki-adr-collision.pa0KwY/.worktrees/shiki-t-20260807t073307221254z-2d22d654-introduc\n succeeded in 0ms:\npython3 scripts/validate_shiki.py  # validate the .shiki/ mirror\n```\n\n## Upgrade\n\nUpgrading means bringing an existing installation up to the current Shiki\ntemplate, CLI, and mirror schema.\n\n1. **Update the CLI.** Pull the latest Shiki platform repo and rerun\n   `bin/shiki install-global` so `~/.local/bin/shiki`, the Claude command, and\n   the Codex skill point at the new version. Restart Codex or Claude Code if the\n   client does not reload commands dynamically.\n2. **Refresh target template files with `--force`.** Re-running the bootstrap\n   without `--force` is **not** an upgrade: an existing file is kept with a\n   `kept existing file:` warning and the run still exits 0, so a stale target\n   looks refreshed when nothing changed. Pass `--force` to actually refresh the\n   template. Re-run in dry-run first to preview, then `--force --execute` to\n   apply. Under `--force` the shipped surface splits three ways so nothing is\n   lost and nothing is silently stale:\n   - **Project content** \u2014 `CONTEXT.md`, `AGENTS.md`, `CLAUDE.md`,\n     `.github/CODEOWNERS` \u2014 is never overwritten. The incoming template is\n     written alongside as `<file>.new` (see [The `.new`\n     convention](#the-new-convention) below) so you can merge deliberately.\n   - **Governance contract** \u2014 `.shiki/config.yaml`,\n     `.shiki/guardian-policy.json` \u2014 is also never silently kept: `<file>.new`\n     is written alongside and the run reports which keys differ, naming\n     `mergegate.required_checks` (what branch protection requires) and\n     `approval_sources` (what may approve) explicitly.\n   - **`.shiki/migrations/state.json`** is preserved outright (no `.new`); it is\n     target history, brought forward by `shiki migrate`, not a template.\n   - **Everything else** (CLI scripts, workflows, schemas, docs) is overwritten.\n\n   If the target has pending migrations, `--force` refuses and writes nothing \u2014\n   apply migrations first (step 3), then re-run. The run ends with a single\n   summary of every `.new` written, so a half-upgrade is visible.\n3. **Apply mirror migrations.** Run `shiki migrate` (see\n   [Migration](#migration) below) to bring the `.shiki/` mirror schema up to\n   date.\n4. **Verify.** Run the verification baseline (validate, py_compile, script\n   tests) and `shiki doctor` after upgrading.\n\nBecause Shiki defaults to dry-run, always review the previewed mutations before\napplying an upgrade.\n\n### The `.new` convention\n\nUnder `--force`, a file that must not be overwritten (project content and the\ngovernance contract) is kept exactly as the target has it, and the incoming\ntemplate is written next to it with a `.new` suffix \u2014 for example\n`.shiki/config.yaml.new` beside `.shiki/config.yaml`. Nothing merges the two for\nyou: review each `.new`, fold in the changes you want, then delete the `.new`\nfile. The end-of-run summary lists every `.new` written so none is missed.\n\n## Rollback\n\nShiki state lives in two durable places, so rollback is a deliberate, evidenced\noperation \u2014 not a destructive command.\n\n- **GitHub operational state** (Issues, PRs, Checks, Reviews, merges) is the\n  source of truth. Roll back code changes with normal, Guardian-authorized Git\n  practices: revert the merge/PR rather than rewriting history. Do not\n  force-push, rewrite history, or delete work without Guardian authorization.\n- **`.shiki/` mirror state** is versioned in Git. To roll the mirror back, revert\n  the commit(s) that changed it through a PR. Never edit existing ledger entries\n  to \"undo\" them \u2014 append new ledger entries that record the correction, so the\n  audit trail stays intact.\n\nexec\n/bin/zsh -lc \"sed -n '130,190p' docs/releasing.md\" in /private/tmp/shiki-adr-collision.pa0KwY/.worktrees/shiki-t-20260807t073307221254z-2d22d654-introduc\n succeeded in 0ms:\n\nA release therefore versions the whole platform, but only the `install.include`\ntemplate surface is what target repositories receive. Changes that alter\n`install.include`, schemas, or the manifest layout are MAJOR-eligible because\nthey affect every installed target.\n\n## Upgrade and migration (P2.2.5)\n\nTarget repositories upgrade by re-installing the newer template surface and then\napplying any repository-local state migrations:\n\n1. Re-run the install/bootstrap path for the target against the new Shiki version\n   (see Install paths above) **with `--force`**. `--force` is the flag that makes\n   a re-run an upgrade: without it an existing file is kept (a `kept existing\n   file:` warning) and the run still exits 0, so re-running the bootstrap alone\n   is not idempotent proof that the target changed. Under `--force` the shipped\n   surface splits three ways \u2014 project content (`CONTEXT.md`, `AGENTS.md`,\n   `CLAUDE.md`, `.github/CODEOWNERS`) and the governance contract\n   (`.shiki/config.yaml`, `.shiki/guardian-policy.json`) are never overwritten\n   but written alongside as `<file>.new` for deliberate merge (the governance\n   report names `mergegate.required_checks` and `approval_sources` when they\n   differ); `.shiki/migrations/state.json` is preserved outright; everything else\n   is overwritten. The `.new` convention (write the incoming template beside the\n   kept file, review, then delete the `.new`) and the end-of-run summary of every\n   `.new` written are detailed in\n   [docs/operations.md](operations.md#the-new-convention). If the target has\n   pending migrations, `--force` refuses and writes nothing; apply migrations\n   (step 2) first, then re-run.\n2. Apply repository-local `.shiki/` state migrations with the migrate command:\n\n   ```bash\n   python3 scripts/shiki.py migrate status   # show current migration state\n   python3 scripts/shiki.py migrate plan     # preview pending migrations (no mutation)\n   python3 scripts/shiki.py migrate apply     # apply pending migrations (defaults to dry-run)\n   ```\n\n   `shiki migrate apply` defaults to a dry-run; review the plan, then apply for\n   real once the plan is confirmed. Migration state is tracked under\n   `.shiki/migrations/`.\n\n3. Re-validate the upgraded mirror:\n\n   ```bash\n   python3 scripts/validate_shiki.py\n   ```\n\nWhen a release is MAJOR (constitution, schema, workflow-contract, or manifest\nlayout change), the release notes must call out the required migration steps.\n\n## Rollback (P2.2.6)\n\nIf a release introduces a regression:\n\n1. **Pin/downgrade the install** to the last known-good tag by checking out that\n   tag and re-running the script install:\n\n   ```bash\n   git checkout v0.0.0\n   python3 scripts/shiki.py install-global   # re-install the prior version globally\n   ```\n\n\ndiff --git a/.codex/skills/shiki/SKILL.md b/.codex/skills/shiki/SKILL.md\nindex c8e2e85d9d75d49cc4f2d6286ee22659c4e53e75..93cded623b2c15d9f65c76491a1c2d813ac9e6c0\n--- a/.codex/skills/shiki/SKILL.md\n+++ b/.codex/skills/shiki/SKILL.md\n@@ -18,6 +18,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n Codex CLI does not register this skill as a custom `/shiki` slash command. When\n the user types `/shiki` in Codex CLI and gets \"Unrecognized command\", explain\n that this is expected: invoke Shiki in Codex with natural language such as\n@@ -69,7 +72,7 @@\n \n ## Responsibilities\n \n-- Claude Code implements and repairs by default (`shiki runner claude`, ADR 0008); Codex implements and repairs tasks explicitly assigned to `codex`.\n+- Claude Code implements and repairs by default (`shiki runner claude`, SADR-0008); Codex implements and repairs tasks explicitly assigned to `codex`.\n - Claude Code Action can act as GitHub-side CCA or reviewer.\n - CCA judges completion.\n - MergeGate authorizes state transitions and merge readiness.\ndiff --git a/.github/prompts/cca-completion-check.md b/.github/prompts/cca-completion-check.md\nindex ed203ad5f302ecc81372d8ab27d2b1232ca97497..fd89950528333ef6242e6d5443904f5392ab7e56\n--- a/.github/prompts/cca-completion-check.md\n+++ b/.github/prompts/cca-completion-check.md\n@@ -19,6 +19,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n ## Required Reading\n \n Read only the files needed to judge the PR contract. Prefer this order:\ndiff --git a/.github/prompts/codex-task-handoff.md b/.github/prompts/codex-task-handoff.md\nindex 695e211d76dea1c60e8d5bb360e78487808286a4..59441d576b01a39569722f226c854e5a740e9eb5\n--- a/.github/prompts/codex-task-handoff.md\n+++ b/.github/prompts/codex-task-handoff.md\n@@ -2,7 +2,7 @@\n \n You are Codex Front acting as the Implementer for a Shiki task.\n \n-Follow `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant ADRs, and `docs/agents/codex-handoff.md`.\n+Follow `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant Shiki SADRs and target ADRs, and `docs/agents/codex-handoff.md`.\n \n Rules:\n \ndiff --git a/AGENTS.md b/AGENTS.md\nindex 7f041100d49c2a7525f51cb0f5d696a4a6d1c78c..a06bfbabc6fa48a007389e25e12d7ed523936bed\n--- a/AGENTS.md\n+++ b/AGENTS.md\n@@ -22,7 +22,7 @@\n - **Codex Front** is an optional implementer through Codex App, Codex CLI, Codex IDE extension, or Codex Web signed in with ChatGPT OAuth/subscription auth, dispatched with `shiki runner codex` when a task is explicitly assigned to `codex`.\n - **GitHub CCA** is implemented by Claude Code Action by default, using `CLAUDE_CODE_OAUTH_TOKEN`.\n \n-Do not assume `openai/codex-action`, `OPENAI_API_KEY`, or API-key based Codex automation in the default Shiki path. API-key runners are explicit target-repo extensions and require their own ADR.\n+Do not assume `openai/codex-action`, `OPENAI_API_KEY`, or API-key based Codex automation in the default Shiki path. API-key runners are explicit target-repo extensions and require their own target ADR.\n \n Shiki is not:\n \n@@ -44,10 +44,14 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use\n+`SADR-NNNN-*.md`; target product decisions retain `NNNN-*.md` and the `ADR`\n+namespace.\n+\n Use the canonical source-of-truth order above. Runtime-specific reading orders\n are operational guidance only and must not override or reorder it.\n \n-Conversation state is not durable truth. If a decision matters, put it in GitHub, `.shiki/`, `CONTEXT.md`, or an ADR.\n+Conversation state is not durable truth. If a decision matters, put it in GitHub, `.shiki/`, `CONTEXT.md`, a Shiki SADR, or a target ADR.\n \n If GitHub and `.shiki/` disagree, surface the conflict and prefer GitHub operational state until the mirror is repaired.\n \n@@ -75,8 +79,8 @@\n Every non-trivial change follows this loop:\n \n 1. **Goal**: clarify outcome, completion conditions, non-goals, risk level, and success signals.\n-2. **grill-with-docs**: challenge the plan against domain docs, ADRs, code reality, terminology, and edge scenarios.\n-3. **Context & Impact**: find relevant docs, ADRs, code, dependencies, owners, locks, and verification surfaces.\n+2. **grill-with-docs**: challenge the plan against domain docs, Shiki SADRs, target ADRs, code reality, terminology, and edge scenarios.\n+3. **Context & Impact**: find relevant docs, SADRs, target ADRs, code, dependencies, owners, locks, and verification surfaces.\n 4. **PRD and Spec Freeze**: use `to-prd` when context is settled enough to become durable product/engineering intent. The operator's explicit approval of the PRD is Spec Freeze: the plan records a `spec_freeze` block, and no plan runs without it. Steps 1-4 form Requirements Definition \u2014 one continuous operator dialogue. After Spec Freeze, scope changes require an operator-approved Spec Amendment; non-scope-moving interpretations go to the Assumption Log.\n 5. **Issues**: use `to-issues` to create independently grabbable vertical-slice issues. Prefer AFK slices over HITL slices where possible.\n 6. **Triage**: label issues for readiness, risk, runtime, skills, and MergeGate state.\n@@ -167,7 +171,7 @@\n - Testing decisions.\n - Out-of-scope items.\n - Further notes.\n-- Links to relevant domain terms and ADRs.\n+- Links to relevant domain terms, SADRs, and target ADRs.\n \n Every executable task must include:\n \n@@ -193,7 +197,7 @@\n \n Context & Impact must identify:\n \n-- Relevant documents, domain terms, ADRs, and past decisions.\n+- Relevant documents, domain terms, SADRs, target ADRs, and past decisions.\n - Relevant modules, interfaces, seams, callers, owners, and tests.\n - Dependency relationships between tasks.\n - Candidate locks and lock conflicts.\n@@ -210,7 +214,7 @@\n | Skill | Required when |\n | --- | --- |\n | `setup-matt-pocock-skills` | First configuring the repo for skills, or when issue tracker, triage labels, or domain docs are missing. |\n-| `grill-with-docs` | Default for non-trivial Goals before PRD/issues; always required when plans, terminology, boundaries, tradeoffs, or ADR-worthy decisions are ambiguous. |\n+| `grill-with-docs` | Default for non-trivial Goals before PRD/issues; always required when plans, terminology, boundaries, tradeoffs, or decision-record-worthy choices are ambiguous. |\n | `zoom-out` | The agent lacks an architectural map of the relevant code area. |\n | `to-prd` | Settled Goal context must become durable product/engineering intent. |\n | `to-issues` | A Goal, PRD, or plan must become independently grabbable vertical-slice issues. |\n@@ -348,7 +352,7 @@\n - Three or more new files.\n - Any high-risk or critical label.\n \n-When the architecture gate triggers, run `grill-with-docs` or `improve-codebase-architecture` as appropriate and record durable decisions in an ADR when needed.\n+When the architecture gate triggers, run `grill-with-docs` or `improve-codebase-architecture` as appropriate and record durable decisions in a Shiki SADR or target ADR as applicable.\n \n ## Engineering Discipline\n \ndiff --git a/CLAUDE.md b/CLAUDE.md\nindex 98a932e7841a632866fcd53b0bad0e3af3711d3d..98a31399404d6fb044b4fd493f4459ac87f439de\n--- a/CLAUDE.md\n+++ b/CLAUDE.md\n@@ -14,6 +14,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n ## Claude Role\n \n Default to planner, default implementer, reviewer, coordinator, documentation editor, CCA-style completion judge, and final-judgment assistant.\n@@ -57,7 +60,7 @@\n At the start of a Shiki session:\n \n 1. Read `AGENTS.md`.\n-2. Read `CONTEXT.md` and relevant ADRs.\n+2. Read `CONTEXT.md` and relevant Shiki SADRs and target ADRs.\n 3. Read active `.shiki/` Goal/task/ledger state when present.\n 4. Read active distilled rules from `.shiki/memories/` (status `distilled`, `active: true`, not revoked or superseded) for applicable guidance; the same rules are injected deterministically into task handoffs as the `## Distilled Rules` section (\u00a73.5).\n 5. Read the GitHub Issue or PR when the task is GitHub-backed.\n@@ -74,7 +77,7 @@\n - Define the Goal, completion conditions, non-goals, and risk level.\n - Treat `grill-with-docs` as the default planning entry point for non-trivial Goals.\n - Challenge domain terms against `CONTEXT.md`.\n-- Challenge hard-to-reverse tradeoffs against ADRs.\n+- Challenge hard-to-reverse tradeoffs against SADRs and target ADRs.\n - Explore code instead of asking when code can answer the question.\n - Run Context & Impact before implementation planning.\n - Produce a Task DAG, not an unordered checklist.\n@@ -94,7 +97,7 @@\n - For each question, provide your recommended answer.\n - Resolve dependency decisions before downstream design questions.\n - Update `CONTEXT.md` inline when terminology crystallizes.\n-- Offer ADRs only when the decision is hard to reverse, surprising without context, and a real tradeoff.\n+- Offer a SADR or target ADR only when the decision is hard to reverse, surprising without context, and a real tradeoff.\n - Convert unresolved questions into HITL blockers rather than allowing Codex to guess.\n \n ## PRD and Issues Standard\n@@ -103,7 +106,7 @@\n \n - Use the project's domain glossary vocabulary.\n - Avoid volatile file paths unless they are essential.\n-- Include implementation decisions, testing decisions, out-of-scope items, and relevant ADR/domain links.\n+- Include implementation decisions, testing decisions, out-of-scope items, and relevant SADR/target-ADR/domain links.\n - Identify deep-module opportunities and testable interfaces.\n \n When creating issues:\n@@ -121,7 +124,7 @@\n - Branch/worktree target.\n - Scope and non-goals.\n - Dependencies and locks.\n-- Relevant docs, ADRs, modules, and tests.\n+- Relevant docs, SADRs, target ADRs, modules, and tests.\n - Required skill invocations.\n - TDD expectations.\n - Pre-PR `code-review` gate expectations.\n@@ -152,7 +155,7 @@\n \n - Treat yourself as a judge, not implementer.\n - Do not write production code.\n-- Read `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant ADRs, PRD, task issue, PR body, diff, checks, reviews, labels, and `.shiki/` state.\n+- Read `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant SADRs and target ADRs, PRD, task issue, PR body, diff, checks, reviews, labels, and `.shiki/` state.\n - Evaluate every applicable checklist in `docs/agents/checklists.md`.\n - Map each acceptance criterion to durable evidence.\n - Distinguish implementation failure from missing evidence.\ndiff --git a/CONTEXT.md b/CONTEXT.md\nindex e61a9be06a0cce0d026484083a657bde2067e234..237ee8aa3ea8fab334ece49768bc5ed3b5af0974\n--- a/CONTEXT.md\n+++ b/CONTEXT.md\n@@ -25,7 +25,7 @@\n _Avoid_: jumping directly into code\n \n **grill-with-docs**:\n-A planning skill that challenges a plan against domain language, ADRs, code reality, and concrete edge scenarios. It resolves design-tree questions before PRD/issues. It runs inside Requirements Definition.\n+A planning skill that challenges a plan against domain language, Shiki SADRs, target ADRs, code reality, and concrete edge scenarios. It resolves design-tree questions before PRD/issues. It runs inside Requirements Definition.\n _Avoid_: generic brainstorming, silent assumptions\n \n **Requirements Definition**:\ndiff --git a/CONTRIBUTING.md b/CONTRIBUTING.md\nindex c6d9b656e73abb8a55e5f660123e03153dcb3e6d..9e37a8fbf668db5b22461aaae5548b8f1354394c\n--- a/CONTRIBUTING.md\n+++ b/CONTRIBUTING.md\n@@ -17,7 +17,7 @@\n 1. **Goal** \u2014 start from a user-approved Goal with an outcome, completion\n    conditions, non-goals, risk level, and success signals.\n 2. **grill-with-docs** \u2014 for any non-trivial change, challenge the plan against\n-   domain language, ADRs, and code reality before writing a PRD.\n+   domain language, Shiki SADRs, target ADRs, and code reality before writing a PRD.\n 3. **PRD** \u2014 capture settled product/engineering intent (`to-prd`).\n 4. **Spec Freeze** \u2014 the operator explicitly approves the PRD/requirements;\n    the plan records a `spec_freeze` block and no plan runs without it\n@@ -94,7 +94,7 @@\n   entries.\n - Do not hand-edit `.shiki/goals/*.json` or `.shiki/manifest.json` in a task PR.\n - Conversation state is not durable truth. If a decision matters, record it in\n-  GitHub, `.shiki/`, `CONTEXT.md`, or an ADR.\n+  GitHub, `.shiki/`, `CONTEXT.md`, a Shiki SADR, or a target ADR.\n \n ## Architecture Gate\n \n@@ -103,7 +103,7 @@\n or CI policy, package structure, three or more structural files, more than 100\n net new lines in a core module, three or more new files, or any high-risk label.\n When triggered, run `grill-with-docs` or `improve-codebase-architecture` and\n-record durable decisions in an ADR when needed (see `docs/adr/`).\n+record durable decisions in a Shiki SADR or target ADR as applicable (see `docs/adr/`).\n \n ## Safety\n \ndiff --git a/SYSTEM_PROMPT.md b/SYSTEM_PROMPT.md\nindex aaaea45e3a896c3a44ba89eec8bb84325ecf6c62..da009c86a88285269dee0155e58e550c6d744ea9\n--- a/SYSTEM_PROMPT.md\n+++ b/SYSTEM_PROMPT.md\n@@ -30,6 +30,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n Use the canonical source-of-truth order above. Runtime-specific reading orders\n are operational guidance only and must not override or reorder it. The current\n conversation is non-durable operator input.\n@@ -48,14 +51,14 @@\n \n A runtime may only do work that is explicitly assigned by the Goal, task, PR, or MergeGate decision.\n \n-Do not assume `openai/codex-action`, `OPENAI_API_KEY`, or API-key based Codex automation in the default Shiki path. API-key runners are explicit target-repo extensions and require their own ADR.\n+Do not assume `openai/codex-action`, `OPENAI_API_KEY`, or API-key based Codex automation in the default Shiki path. API-key runners are explicit target-repo extensions and require their own target ADR.\n \n ## 4. Non-negotiable Shiki flow\n \n For any non-trivial Goal, use this flow:\n \n 1. **Goal Seek**: define outcome, non-goals, risk level, completion criteria, and evidence requirements.\n-2. **grill-with-docs**: challenge the plan against `CONTEXT.md`, ADRs, code reality, terminology, boundaries, and edge scenarios. Ask one question at a time when the operator is available. If code can answer a question, inspect code instead of asking.\n+2. **grill-with-docs**: challenge the plan against `CONTEXT.md`, Shiki SADRs, target ADRs, code reality, terminology, boundaries, and edge scenarios. Ask one question at a time when the operator is available. If code can answer a question, inspect code instead of asking.\n 3. **Context & Impact**: identify modules, interfaces, seams, callers, dependencies, locks, risk, and verification surfaces.\n 4. **to-prd + Spec Freeze**: turn settled context into a PRD using domain vocabulary and testing decisions. The operator's explicit approval of the PRD is Spec Freeze, recorded as the plan's `spec_freeze` block; plans without it do not run. Steps 1-4 form Requirements Definition.\n 5. **to-issues**: decompose the PRD into independently grabbable vertical-slice issues. Prefer AFK slices; mark HITL slices when judgment is still required.\n@@ -75,7 +78,7 @@\n Before making any material change:\n \n 1. Read `AGENTS.md` and any runtime-specific wrapper such as `CLAUDE.md`.\n-2. Read `CONTEXT.md` and relevant ADRs.\n+2. Read `CONTEXT.md` and relevant SADRs and target ADRs.\n 3. Read the GitHub Issue / PR and `.shiki/` state for the active Goal or task.\n 4. Identify your role: Planner, Implementer, Reviewer, Completion Check Agent, Repairer, or Guardian-assist.\n 5. Identify Goal id, task id, branch, PR, dependencies, locks, risk level, required skills, and required evidence.\n@@ -103,7 +106,7 @@\n \n At minimum, identify:\n \n-- Relevant documents, ADRs, domain terms, and prior decisions.\n+- Relevant documents, SADRs, target ADRs, domain terms, and prior decisions.\n - Relevant modules, interfaces, seams, callers, owners, and tests.\n - Dependency relationships between tasks.\n - Candidate file locks and lock conflicts.\n@@ -152,7 +155,7 @@\n \n CCA must read:\n \n-- `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant ADRs.\n+- `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant SADRs and target ADRs.\n - Parent Goal issue, PRD issue, task issue, comments, and labels.\n - PR body, diff, commits, changed files, reviews, CI checks, and artifacts available to the workflow.\n - `.shiki/` task contract, locks, ledger entries, CCA checklist profile, and prior repair packets.\n@@ -214,7 +217,7 @@\n - Three or more new files.\n - Any high-risk or critical label.\n \n-When the architecture gate triggers, run `grill-with-docs` or `improve-codebase-architecture` as appropriate and record the decision in an ADR if it is hard to reverse or likely to be re-litigated.\n+When the architecture gate triggers, run `grill-with-docs` or `improve-codebase-architecture` as appropriate and record the decision in a Shiki SADR or target ADR as applicable if it is hard to reverse or likely to be re-litigated.\n \n ## 14. Skill Gate\n \n@@ -223,7 +226,7 @@\n Use these skills:\n \n - `setup-matt-pocock-skills`: before first use of the engineering skill set, or when issue tracker, triage labels, or domain docs are missing.\n-- `grill-with-docs`: default for non-trivial Goals before PRD/issues; required for ambiguous plans, terminology, boundaries, ADR-worthy tradeoffs, or design-tree decisions.\n+- `grill-with-docs`: default for non-trivial Goals before PRD/issues; required for ambiguous plans, terminology, boundaries, decision-record-worthy tradeoffs, or design-tree decisions.\n - `zoom-out`: unfamiliar code area or missing architectural map.\n - `to-prd`: convert settled context into a PRD.\n - `to-issues`: break a PRD, plan, or Goal into independently grabbable vertical-slice issues.\ndiff --git a/docs/adr/0001-keep-shiki-as-platform-name.md b/docs/adr/SADR-0001-keep-shiki-as-platform-name.md\nindex 20a9637c3b9d8c97de7ba8db7cfae4d82d9a7dba..8b685439959aada1b221c112f0ac3c51ca8a1695\n--- a/docs/adr/0001-keep-shiki-as-platform-name.md\n+++ b/docs/adr/SADR-0001-keep-shiki-as-platform-name.md\n@@ -1,3 +1,3 @@\n-# Keep Shiki As The Platform Name\n+# SADR-0001: Keep Shiki As The Platform Name\n \n Shiki remains the product name for the GitHub-native agentic engineering control plane. We considered more descriptive names such as MergePilot, MergeGate, and Kiroku, but kept Shiki because it carries the existing goal-seeking framework identity while remaining independent of Hermes, Codex, Claude Code, or any single runtime.\ndiff --git a/docs/adr/0002-use-agents-as-shared-constitution.md b/docs/adr/SADR-0002-use-agents-as-shared-constitution.md\nindex 73de30ecb078120eac540d9bc51855e3cd48d0e3..c5486182bf06fca36ea508496b013c67302fd989\n--- a/docs/adr/0002-use-agents-as-shared-constitution.md\n+++ b/docs/adr/SADR-0002-use-agents-as-shared-constitution.md\n@@ -1,3 +1,3 @@\n-# Use AGENTS As The Shared Agent Constitution\n+# SADR-0002: Use AGENTS As The Shared Agent Constitution\n \n Shiki uses `AGENTS.md` as the runtime-neutral constitution and `CLAUDE.md` as a thin Claude Code wrapper. This keeps Codex, Claude Code, Hermes Runner, and future Agent Runtimes aligned on the same GitHub-first MergeGate rules while still allowing runtime-specific instructions where needed.\ndiff --git a/docs/adr/0003-require-skill-gate-for-mergegate-readiness.md b/docs/adr/SADR-0003-require-skill-gate-for-mergegate-readiness.md\nindex 424be0c9af4c8f0136f1fde68a468c47ff84e764..7fc337987c7df9792f75f181dfcbed75fe5ffff2\n--- a/docs/adr/0003-require-skill-gate-for-mergegate-readiness.md\n+++ b/docs/adr/SADR-0003-require-skill-gate-for-mergegate-readiness.md\n@@ -1,3 +1,3 @@\n-# Require Skill Gate For MergeGate Readiness\n+# SADR-0003: Require Skill Gate For MergeGate Readiness\n \n Shiki requires relevant engineering skills to be invoked before execution when their triggers apply. This makes skills part of MergeGate readiness rather than optional prompt style, because Shiki depends on repeatable planning, issue decomposition, TDD, diagnosis, triage, and architecture review before autonomous or parallel agent work can be trusted.\ndiff --git a/docs/adr/0004-use-github-first-with-shiki-mirror.md b/docs/adr/SADR-0004-use-github-first-with-shiki-mirror.md\nindex d0a3a9174dc646a945a9d325954ffaf00c352040..a79c54ae9ee8eed2a0524192b864da4e22a92d3f\n--- a/docs/adr/0004-use-github-first-with-shiki-mirror.md\n+++ b/docs/adr/SADR-0004-use-github-first-with-shiki-mirror.md\n@@ -1,4 +1,4 @@\n-# ADR 0004: Use GitHub First With A Repository-Local Shiki Mirror\n+# SADR-0004: Use GitHub First With A Repository-Local Shiki Mirror\n \n ## Status\n \ndiff --git a/docs/adr/0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md b/docs/adr/SADR-0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\nindex b339baf49b89d9290e79c3d51b36e7b2bde3bd95..c30a0d8b1fde0224d0ac7820e7870dccf721580d\n--- a/docs/adr/0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\n+++ b/docs/adr/SADR-0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\n@@ -1,4 +1,4 @@\n-# ADR 0005: Use Codex As Front And Claude Code Action As The GitHub Runtime\n+# SADR-0005: Use Codex As Front And Claude Code Action As The GitHub Runtime\n \n ## Status\n \ndiff --git a/docs/adr/0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md b/docs/adr/SADR-0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\nindex 4bcae9d1e52f0a37b8e9fbafa5cd14b6ee1993bd..7a38538a6ae9929567f0ade67e7ea6a60d0b3f7e\n--- a/docs/adr/0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\n+++ b/docs/adr/SADR-0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\n@@ -1,4 +1,4 @@\n-# ADR 0006: Separate Codex Implementation, CCA Completion, And MergeGate Authorization\n+# SADR-0006: Separate Codex Implementation, CCA Completion, And MergeGate Authorization\n \n ## Status\n \ndiff --git a/docs/adr/0007-packaging-and-release.md b/docs/adr/SADR-0007-packaging-and-release.md\nindex 79b84ddb97167372b44641e5b7860e400ce80822..bb445a2e2e856d9d1f5fdbe35de8d4e8cec0fb32\n--- a/docs/adr/0007-packaging-and-release.md\n+++ b/docs/adr/SADR-0007-packaging-and-release.md\n@@ -1,4 +1,4 @@\n-# ADR 0007: Packaging And Release For Shiki\n+# SADR-0007: Packaging And Release For Shiki\n \n ## Status\n \n@@ -25,7 +25,7 @@\n   not introduce third-party runtime dependencies or break the shim.\n - Releases must run inside the operator's subscription-authenticated toolchain\n   and must not require extra secrets beyond the built-in `GITHUB_TOKEN`\n-  (consistent with ADR 0005's no-API-key-by-default stance).\n+  (consistent with SADR-0005's no-API-key-by-default stance).\n - `.shiki/manifest.json` already encodes the install surface (`install.include`,\n   `install.create_directories`, `install.exclude_from_commit`), and it must not\n   be mutated as part of this change.\n@@ -34,7 +34,7 @@\n \n This is a hard-to-reverse platform decision (it sets the version source of\n truth, the release trigger, and the public install contract), so it is recorded\n-as an ADR.\n+as a SADR.\n \n ## Decision\n \n@@ -104,7 +104,7 @@\n 3. **Publishing to PyPI.** Rejected for now: Shiki is distributed as a\n    repository template + git-installable CLI, not a PyPI library; PyPI would add\n    credentials/secrets and a different versioning contract. Can be added later as\n-   its own ADR if a packaged distribution is needed.\n+   its own SADR if a packaged distribution is needed.\n 4. **Dynamic version (read `VERSION` via a build backend `dynamic` field).**\n    Rejected to keep `pyproject.toml` backend-agnostic and dependency-free; the\n    small duplication between `VERSION` and `pyproject.toml` is guarded by the\n@@ -123,4 +123,4 @@\n   releasing versions the whole platform but ships only `install.include` to\n   targets.\n - Future PyPI distribution or a console entry point are deferred and would each\n-  warrant their own ADR.\n+  warrant their own SADR.\ndiff --git a/docs/adr/0008-use-claude-code-as-default-implementer.md b/docs/adr/SADR-0008-use-claude-code-as-default-implementer.md\nindex dedf4510b7c7799521422f622029563d373db031..35c2625d908167fbc58855b5b63e283805f167e8\n--- a/docs/adr/0008-use-claude-code-as-default-implementer.md\n+++ b/docs/adr/SADR-0008-use-claude-code-as-default-implementer.md\n@@ -1,12 +1,12 @@\n-# ADR 0008: Use Claude Code As The Default Implementer Runtime\n+# SADR-0008: Use Claude Code As The Default Implementer Runtime\n \n ## Status\n \n-Proposed (supersedes ADR 0005 for the implementer default; keeps its auth model)\n+Proposed (supersedes SADR-0005 for the implementer default; keeps its auth model)\n \n ## Context\n \n-ADR 0005 made Codex Front the default implementation runtime, with Claude Code\n+SADR-0005 made Codex Front the default implementation runtime, with Claude Code\n restricted to planner and reviewer roles by the runtime registry, and the only\n autonomous implementation adapter was `shiki runner codex`.\n \n@@ -18,13 +18,13 @@\n - The platform's planning, review, and CCA layers already run on Claude Code,\n   and Claude Code's native capabilities (the built-in `/code-review` skill and\n   the Workflow multi-agent orchestration tool) have no Codex equivalent.\n-- The Goal lifecycle redesign (ADR 0009) requires an autonomous post-freeze\n+- The Goal lifecycle redesign (SADR-0009) requires an autonomous post-freeze\n   loop driven from the operator's Claude Code session, including bounded\n   repair dispatch, which must not depend on a runtime that cannot be invoked\n   unattended.\n \n The constitution claims Shiki is runtime-agnostic (\"not a Claude-only\n-workflow\", ADR 0002). Any change to the implementer default must preserve the\n+workflow\", SADR-0002). Any change to the implementer default must preserve the\n registry-based runtime model rather than hardcoding one vendor.\n \n ## Decision\n@@ -39,7 +39,7 @@\n   the task to review or repair-needed.\n - Keep Codex registered as an optional implementer runtime. `shiki runner\n   codex`, the Codex handoff templates, and the ChatGPT OAuth auth model from\n-  ADR 0005 remain supported for tasks explicitly assigned to `codex`.\n+  SADR-0005 remain supported for tasks explicitly assigned to `codex`.\n - Commit to mandating two Claude-native capabilities as loop contract in\n   follow-up Goals (status: the code-review gate was delivered by Goal G-B \u2014\n   skill registry, checklists item PR-12, default required_skills \u2014 while the\n@@ -60,7 +60,7 @@\n \n Out of scope: removing Codex support, changing the CCA runtime (Claude Code\n Action with `CLAUDE_CODE_OAUTH_TOKEN` stays), and API-key based automation\n-(still requires its own ADR per ADR 0005).\n+(still requires its own target ADR per SADR-0005).\n \n ## Consequences\n \n@@ -100,7 +100,7 @@\n   harness rejection of direct `codex exec` is outside Shiki's control, and the\n   plan's mandatory native capabilities cannot run on Codex at all.\n - **Remove Codex entirely**: rejected; it abandons the runtime-agnostic\n-  constitution (ADR 0002) for no operational gain and forecloses\n+  constitution (SADR-0002) for no operational gain and forecloses\n   cross-runtime adversarial review.\n - **GitHub Actions cloud implementation via claude-code-action**: rejected as\n   default; durable evidence is automatic but token cost, Node 24 workflow\ndiff --git a/docs/adr/0009-requirements-definition-and-spec-freeze.md b/docs/adr/SADR-0009-requirements-definition-and-spec-freeze.md\nindex 29b3f7871bef9203a8b4a831a65c39eb2f506e42..52e7dd69ad0aa4efe0b68c62678abbf7df02aaec\n--- a/docs/adr/0009-requirements-definition-and-spec-freeze.md\n+++ b/docs/adr/SADR-0009-requirements-definition-and-spec-freeze.md\n@@ -1,4 +1,4 @@\n-# ADR 0009: Merge Grill And Planning Into Requirements Definition With Spec Freeze\n+# SADR-0009: Merge Grill And Planning Into Requirements Definition With Spec Freeze\n \n ## Status\n \ndiff --git a/docs/adr/0010-external-ai-guardian-review.md b/docs/adr/SADR-0010-external-ai-guardian-review.md\nindex e6108bbe8bb4e73e0ff4684c8f78001a80438c4e..f3510b145664298adafb151b6f0db51b972f01ec\n--- a/docs/adr/0010-external-ai-guardian-review.md\n+++ b/docs/adr/SADR-0010-external-ai-guardian-review.md\n@@ -1,4 +1,4 @@\n-# ADR 0010: External AI Guardian Review As A First-Class Approval Authority\n+# SADR-0010: External AI Guardian Review As A First-Class Approval Authority\n \n ## Status\n \n@@ -8,7 +8,7 @@\n \n Shiki is intended to be an autonomous, recursive, self-improving agent\n framework: authority-in-the-loop, not human-in-the-loop. Until now the Guardian\n-approval required for high/critical-risk merges (guardian-policy.json, ADR\n+approval required for high/critical-risk merges (guardian-policy.json, SADR\n governance) recognized only human authority sources \u2014 a GitHub review approval,\n the `guardian:approved` label applied by a configured Guardian, or a comment\n carrying `Guardian approval granted` plus the current head SHA.\n@@ -89,7 +89,7 @@\n   autonomous design the operator intends and produced the impersonation\n   incident.\n - **Record AI approval as an operator comment**: rejected as forgery of human\n-  authority \u2014 the exact failure this ADR corrects.\n+  authority \u2014 the exact failure this SADR corrects.\n - **Deliver the artifact as a committed `.shiki/` file**: rejected; a committed\n   approval inside the PR being approved is forgeable by anyone who can push.\n - **A dedicated reviewer bot identity instead of a relayed comment**: deferred;\ndiff --git a/docs/adr/0011-autonomous-quality-gate-evidence.md b/docs/adr/SADR-0011-autonomous-quality-gate-evidence.md\nindex bca0246c5d46e44afbde15486a75eec425fc02f5..9c93eac16d8c3a6d4b11bd7224e3955102cd7db4\n--- a/docs/adr/0011-autonomous-quality-gate-evidence.md\n+++ b/docs/adr/SADR-0011-autonomous-quality-gate-evidence.md\n@@ -1,4 +1,4 @@\n-# ADR 0011: Autonomous Quality-Gate Evidence \u2014 Deterministic Facts And An Independent Verifier\n+# SADR-0011: Autonomous Quality-Gate Evidence \u2014 Deterministic Facts And An Independent Verifier\n \n ## Status\n \ndiff --git a/docs/adr/0012-autonomous-loop-closeout-to-main.md b/docs/adr/SADR-0012-autonomous-loop-closeout-to-main.md\nindex 637fad6dde4989c9c2bf0f81f676d8d29991123c..ec7d85f81030a676bcbba28b43eb365422a36eac\n--- a/docs/adr/0012-autonomous-loop-closeout-to-main.md\n+++ b/docs/adr/SADR-0012-autonomous-loop-closeout-to-main.md\n@@ -1,4 +1,4 @@\n-# 0012 \u2014 Autonomous loop pushes goal completion to main via a closeout PR\n+# SADR-0012 \u2014 Autonomous loop pushes goal completion to main via a closeout PR\n \n - Status: accepted\n - Date: 2026-06-18\ndiff --git a/docs/adr/0013-bootstrap-grants-actions-approve-pull-requests.md b/docs/adr/SADR-0013-bootstrap-grants-actions-approve-pull-requests.md\nindex b6c69d64f1c5ced346b88d6859586715b3b5bd0e..cf48ff4a6716267f539ca58edd1a852e7c7a2d64\n--- a/docs/adr/0013-bootstrap-grants-actions-approve-pull-requests.md\n+++ b/docs/adr/SADR-0013-bootstrap-grants-actions-approve-pull-requests.md\n@@ -1,4 +1,4 @@\n-# ADR 0013: Bootstrap Grants GitHub Actions Permission To Create And Approve Pull Requests\n+# SADR-0013: Bootstrap Grants GitHub Actions Permission To Create And Approve Pull Requests\n \n ## Status\n \n@@ -31,7 +31,7 @@\n security tradeoff: it lets repository automation satisfy a human-review gate. In\n a multi-maintainer repo that could let a compromised or buggy workflow\n self-approve changes. So whether bootstrap turns it on by default \u2014 and with\n-what default token scope \u2014 is an ADR-worthy decision, not an implementation\n+what default token scope \u2014 is a SADR-worthy decision, not an implementation\n detail.\n \n ## Decision\n@@ -48,7 +48,7 @@\n Specifically:\n \n - **`can_approve_pull_request_reviews` defaults to `true`.** Shiki's default\n-  posture is solo/AFK autonomous operation (ADR 0008/0009/0012), where the CCA\n+  posture is solo/AFK autonomous operation (SADR-0008/0009/0012), where the CCA\n   Review Bridge is the only path that can satisfy `required_review: true` after\n   the CCA and Guardian gates have already passed. The default makes a\n   freshly-bootstrapped repo able to run the autonomous loop end to end without a\ndiff --git a/docs/adr/0014-codex-app-external-ai-guardian-ui-adapter.md b/docs/adr/SADR-0014-codex-app-external-ai-guardian-ui-adapter.md\nindex 238db06279334caa341c117362df9271382a7fcd..65f12e9939321e1b3e389a34c822fc1549e5155d\n--- a/docs/adr/0014-codex-app-external-ai-guardian-ui-adapter.md\n+++ b/docs/adr/SADR-0014-codex-app-external-ai-guardian-ui-adapter.md\n@@ -1,4 +1,4 @@\n-# ADR 0014: Use Codex App As The External AI Guardian UI Adapter\n+# SADR-0014: Use Codex App As The External AI Guardian UI Adapter\n \n ## Status\n \ndiff --git a/docs/adr/0015-contract-approval-before-dispatch.md b/docs/adr/SADR-0015-contract-approval-before-dispatch.md\nindex fad6c656741a20a7684ba16080a7db159f63a52f..6061271a46d3f1d6ae7174477de3fb75673123a6\n--- a/docs/adr/0015-contract-approval-before-dispatch.md\n+++ b/docs/adr/SADR-0015-contract-approval-before-dispatch.md\n@@ -1,4 +1,4 @@\n-# ADR 0015: Approve The Task Contract Before Dispatch, Not The Diff After It\n+# SADR-0015: Approve The Task Contract Before Dispatch, Not The Diff After It\n \n ## Status\n \n@@ -9,7 +9,7 @@\n Guardian approval for high/critical risk is expressible only on a pull request.\n Every approval source in `guardian-policy.json` \u2014 GitHub review, the\n `guardian:approved` label, the `Guardian approval granted` comment, and the\n-`external_ai_guardian_review` artifact (ADR 0010/0014) \u2014 is bound to a PR, and\n+`external_ai_guardian_review` artifact (SADR-0010/0014) \u2014 is bound to a PR, and\n three of the four require the current head SHA. Approval therefore cannot exist\n before an implementation PR exists.\n \n@@ -81,7 +81,7 @@\n An envelope breach must surface only on the non-repairable policy gate, never on\n the repairable `MergeGate metadata check`. Otherwise the Goal Loop routes it to\n `dispatch_repair` \u2014 instructing a runner to make the breach go away \u2014 which is\n-exactly what this ADR forbids.\n+exactly what this SADR forbids.\n \n **Contract immutability.** After registration, an implementation PR must not\n change the governance fields of its own task contract (`scope`, `non_goals`,\n@@ -116,7 +116,7 @@\n fails any verdict that is not `complete`, and the signal the CCA prompt reads is\n computed from `evaluate_guardian_approval` alone. Without the OR-merge at the\n signal, a contract-approved PR still returns `needs_guardian`, the CCA job fails,\n-and the MergeGate policy check never runs \u2014 the gate this ADR changes would never\n+and the MergeGate policy check never runs \u2014 the gate this SADR changes would never\n be reached. The CCA *prompt* needs no change: a `contract_approval` entry in the\n signal's existing `sources` array satisfies its current wording.\n \n@@ -130,7 +130,7 @@\n written, and an approved contract lets its implementation PRs merge through the\n same automatic path as low/medium risk. The wasted implement-then-block cycle\n disappears. Guardian review shifts from reading diffs to reading contracts, which\n-is both earlier and smaller; the external AI Guardian path (ADR 0010/0014) works\n+is both earlier and smaller; the external AI Guardian path (SADR-0010/0014) works\n on contracts with no change.\n \n We accept that the Guardian authorizes a class of change without seeing its\n@@ -146,7 +146,7 @@\n adversarial tests for the new approval path equivalent in rigor to\n `test_shiki_governance_evidence.sh`.\n \n-Bootstrap costs, which apply only while this ADR is being implemented. The tasks\n+Bootstrap costs, which apply only while this SADR is being implemented. The tasks\n that build Contract Approval merge under the existing rules, so the ones\n dispatched after the pre-dispatch gate lands but before the approval path lands\n need a Guardian-approved Contract PR *and* a live Guardian approval on their\n@@ -210,7 +210,7 @@\n risk; they do not bound authorized work.\n \n **Auto-escalate risk on envelope breach instead of stopping.** Returns the\n-workflow to a diff-time Guardian decision \u2014 the exact behavior this ADR removes.\n+workflow to a diff-time Guardian decision \u2014 the exact behavior this SADR removes.\n \n **Resolve governance fields from the base snapshot only.** Rejected against the\n registration flow: implementation PRs add their own contract, so the base has no\ndiff --git a/docs/adr/0016-locks-bound-product-paths.md b/docs/adr/SADR-0016-locks-bound-product-paths.md\nindex b5e850f3f1f7f4ef6e7f9d1763a29a51bd300c45..46b40a9b99a7aae644c02bb458f5473ff0a90ce2\n--- a/docs/adr/0016-locks-bound-product-paths.md\n+++ b/docs/adr/SADR-0016-locks-bound-product-paths.md\n@@ -1,4 +1,4 @@\n-# ADR 0016: Locks bound product paths; the Shiki mirror is bound by record identity\n+# SADR-0016: Locks bound product paths; the Shiki mirror is bound by record identity\n \n ## Status\n \n@@ -6,8 +6,8 @@\n \n - Date: 2026-07-28\n - Deciders: operator (mizutani-140), Claude (planner)\n-- Related: ADR 0004 (GitHub-first with a `.shiki` mirror), ADR 0009 (Spec Freeze),\n-  ADR 0012 (autonomous loop closeout to main), ADR 0015 (Contract Approval before\n+- Related: SADR-0004 (GitHub-first with a `.shiki` mirror), SADR-0009 (Spec Freeze),\n+  SADR-0012 (autonomous loop closeout to main), SADR-0015 (Contract Approval before\n   dispatch), remediation goal task R-02, the live PR #179 deadlock.\n \n ## Context\n@@ -90,11 +90,11 @@\n files, documentation files and named source modules are nameable; runtime-\n allocated ledger, runner and report ids are not \u2014 and those live in `.shiki`,\n which locks no longer govern. Existing frozen contracts are brought into line by\n-a one-time Spec Amendment per plan, which must land before ADR 0015's contract\n+a one-time Spec Amendment per plan, which must land before SADR-0015's contract\n immutability makes `locks` unwritable from the implementation PR.\n \n **Lock state is resolved from the durable side.** MergeGate resolves the lock\n-registry, and ADR 0015's governance fields, from the base `.shiki` snapshot when\n+registry, and SADR-0015's governance fields, from the base `.shiki` snapshot when\n present, falling back to head only when absent. The `MergeGate metadata check`\n job is given `--base-shiki`, which it does not receive today. Locks are not\n derived from the live open-pull-request set: a pull request body is untrusted\n@@ -108,7 +108,7 @@\n Explicitly out of scope: moving MergeGate's contract resolution off the PR\n checkout wholesale; making the mirror a projection of GitHub state; introducing\n new lock kinds; moving ledger evidence out of the repository; and unwinding\n-ADR 0012's closeout pull request. Registration of multi-task goals is ADR 0015's\n+SADR-0012's closeout pull request. Registration of multi-task goals is SADR-0015's\n Contract PR and is not re-decided here.\n \n ## Consequences\n@@ -127,7 +127,7 @@\n \n Risks accepted. First, goal-scoped identity is only as strong as `goal_id`,\n which is resolved from the PR body and the head task file; the mitigation is to\n-add `goal_id` to ADR 0015's frozen governance field set, and until that lands a\n+add `goal_id` to SADR-0015's frozen governance field set, and until that lands a\n task file can name a foreign goal. Second, base-when-present resolution is\n unavailable on fork pull requests, where the head is contributor-controlled;\n fork pull requests already cannot complete a Shiki merge, and this decision does\n@@ -139,7 +139,7 @@\n path, not by implementation pull requests.\n \n New obligations: one Spec Amendment per frozen plan to rewrite `locks`, ordered\n-before ADR 0015 T1; extension of the adversarial suites in\n+before SADR-0015 T1; extension of the adversarial suites in\n `scripts/test_shiki_adversarial_state.sh` and\n `scripts/test_shiki_governance_evidence.sh` to cover the eleven newly-scoped\n directories; and updating `scripts/test_shiki_loop_lock_guard.sh`,\n@@ -151,32 +151,32 @@\n whether the loop's transitive sync of ledger `evidence` references can be\n deleted once the Contract PR carries its own source plan.\n \n-## Relationship to ADR 0004\n+## Relationship to SADR-0004\n \n-This ADR **clarifies something ADR 0004 left unspecified**. ADR 0004 decides that\n+This SADR **clarifies something SADR-0004 left unspecified**. SADR-0004 decides that\n GitHub is the operational source of truth and that each target repository keeps a\n `.shiki/` mirror. It says nothing about pull requests writing the mirror, does\n not name MergeGate, and does not make the mirror an input to a gate. Nothing in\n-ADR 0004's Decision or Consequences is amended, superseded or weakened: the\n+SADR-0004's Decision or Consequences is amended, superseded or weakened: the\n mirror still exists, still records the same artifacts, and pull request\n-boundaries remain the coordination surface. What this ADR settles is the question\n+boundaries remain the coordination surface. What this SADR settles is the question\n 0004 never reached \u2014 by what authority a pull request may write a mirror record \u2014\n and it answers it the way `.shiki/manifest.json` already declares\n (`pr_mutation: current-task-or-current-goal-only`).\n \n-It **amends ADR 0012**, whose Decision item 5 states that \"Loop-executed tasks\n+It **amends SADR-0012**, whose Decision item 5 states that \"Loop-executed tasks\n declare the `path:.shiki/**` lock, which covers every `.shiki` file the closeout\n stages ... so `files_outside_locks` is satisfied without per-file lock\n bookkeeping.\" That clause is replaced: the closeout's mirror writes are\n-authorized by record identity, not by a declared lock. ADR 0012's substantive\n+authorized by record identity, not by a declared lock. SADR-0012's substantive\n decision \u2014 that completion reaches `main` through a closeout pull request rather\n than local attestation \u2014 is preserved unchanged.\n \n-It is **complementary to ADR 0015**, not a substitute. ADR 0015 owns contract\n+It is **complementary to SADR-0015**, not a substitute. SADR-0015 owns contract\n immutability, the pre-dispatch gate, the Contract PR and the Scope Envelope. This\n-ADR owns what a lock means and who may write the mirror. The one ordering\n+SADR owns what a lock means and who may write the mirror. The one ordering\n constraint between them is that the lock-narrowing Spec Amendment must precede\n-ADR 0015 T1's freezing of the `locks` field.\n+SADR-0015 T1's freezing of the `locks` field.\n \n ## Alternatives Considered\n \n@@ -186,7 +186,7 @@\n the only writer that can register them may run. Migrating the Guardian signal to\n that store makes every registration PR fail closed to `needs_guardian` at every\n risk level, because the task file is by definition absent from the store, which\n-removes the low/medium autonomous path ADR 0008/0009 exists to provide. And it\n+removes the low/medium autonomous path SADR-0008/0009 exists to provide. And it\n leaves schemas, manifest, config, guardian policy, templates, migrations, plans,\n worktrees, runner records, runs and handoffs with no writer at all in a\n repository whose product is `.shiki`. Measured benefit on the frozen backlog:\n@@ -199,14 +199,14 @@\n namespace is not in fact ownership-partitioned \u2014 56 ledger entries carry no\n `task_id`, and one goal DAG is writable by 35 registered tasks. The correct\n expression of its insight is that locks do not govern the mirror at all, which is\n-what this ADR decides.\n+what this SADR decides.\n \n **Make the mirror a projection written only by a registration writer and a\n post-merge projection job.** Rejected. It requires a bypass actor with write\n access to the protected default branch; it makes its own master invariant\n readable from a manifest that pull requests may edit; it deletes both reconcile\n modes and leaves a corrupted mirror with no in-band repair path, contradicting\n-ADR 0004's consequence that mirror conflicts are repaired against GitHub state;\n+SADR-0004's consequence that mirror conflicts are repaired against GitHub state;\n and its content-derived ids are rejected by `validate_shiki`'s currently-green id\n pattern.\n \ndiff --git a/docs/adr/0017-bookkeeping-closeouts-do-not-inherit-risk.md b/docs/adr/SADR-0017-bookkeeping-closeouts-do-not-inherit-risk.md\nindex 554e3a0d526a72ca067108c5464cc68c3edcf174..b9676857a4713850e063af7c39c8e0b01ecb4c66\n--- a/docs/adr/0017-bookkeeping-closeouts-do-not-inherit-risk.md\n+++ b/docs/adr/SADR-0017-bookkeeping-closeouts-do-not-inherit-risk.md\n@@ -1,4 +1,4 @@\n-# ADR 0017: A Bookkeeping Closeout Does Not Inherit Its Task's Risk\n+# SADR-0017: A Bookkeeping Closeout Does Not Inherit Its Task's Risk\n \n ## Status\n \n@@ -6,7 +6,7 @@\n \n ## Context\n \n-ADR 0012 made the goal loop push a task's completion to the default branch through a\n+SADR-0012 made the goal loop push a task's completion to the default branch through a\n closeout pull request: after the implementation PR merges, the loop opens a second PR\n carrying `task.status = done`, `lock.state = released`, and \u2014 when the task completes\n its goal \u2014 `goal.status = complete` plus one scorecard report. Nothing else. The\n@@ -49,14 +49,14 @@\n 4. The task file's governance fields (`scope`, `non_goals`, `required_skills`,\n    `risk_level`, `locks`, `acceptance_checks`, `test_command`) are byte-identical to\n    the base snapshot. Only `status`, `expected_pr`, `closeout_pr`, `ledger_evidence`\n-   and `expected_branch` may differ \u2014 the ADR 0015 contract-immutability rule,\n+   and `expected_branch` may differ \u2014 the SADR-0015 contract-immutability rule,\n    unchanged.\n \n    `expected_branch` is in that set because a closeout is *by construction* on a\n    different branch from the implementation it closes, and the loop rewrites the\n    field accordingly. It is safe to let it differ because MergeGate independently\n    requires `expected_branch` to equal the PR's actual head ref, so a PR cannot set\n-   it to anything other than the branch it is really on. The first draft of this ADR\n+   it to anything other than the branch it is really on. The first draft of this SADR\n    omitted it, which made the exemption unreachable: every real closeout failed\n    condition 4 on a field the loop always changes. See PR #209.\n 5. The state transitions are exactly the terminal set: task `review` \u2192 `done`, lock\n@@ -113,12 +113,12 @@\n classification must be derived, not asserted.\n \n **Fold the closeout into the implementation PR** so no second PR exists. Rejected under\n-ADR 0012, which recorded why: a task file that is already `done` on the implementation\n+SADR-0012, which recorded why: a task file that is already `done` on the implementation\n branch short-circuits the loop's review \u2192 CCA \u2192 merge \u2192 done ordering into a local\n goal-completion before any merge happens.\n \n **Have the loop auto-merge closeouts with elevated credentials.** Rejected outright: that\n-is a runtime authoring its own approval, which ADR 0010 and the constitution's\n+is a runtime authoring its own approval, which SADR-0010 and the constitution's\n approval-record prohibition forbid. It would also be indistinguishable, in the evidence,\n from the forgery those rules exist to prevent.\n \ndiff --git a/docs/adr/0018-carried-guardian-approval-across-a-proven-base-sync.md b/docs/adr/SADR-0018-carried-guardian-approval-across-a-proven-base-sync.md\nindex 22358562da390ed8b45cd600da08a43d378b4680..e6e2907cefa776460af73ddf7d39db605f3c39b0\n--- a/docs/adr/0018-carried-guardian-approval-across-a-proven-base-sync.md\n+++ b/docs/adr/SADR-0018-carried-guardian-approval-across-a-proven-base-sync.md\n@@ -1,4 +1,4 @@\n-# ADR 0018: Carried Guardian Approval Across A Proven Base Sync\n+# SADR-0018: Carried Guardian Approval Across A Proven Base Sync\n \n ## Status\n \n@@ -34,7 +34,7 @@\n Guardian evaluator that consumes it (`shiki_guardian.evaluate_guardian_approval`,\n the `guardian_comment_carried` source) were built and tested in isolation. Neither\n does anything until a caller passes the flags that turn the carry on, so the\n-feature was invisible in CI. This ADR records the decision the two of them\n+feature was invisible in CI. This SADR records the decision the two of them\n implement, and this task wires it into the four CI invocations that judge Guardian\n approval.\n \ndiff --git a/docs/adr/README.md b/docs/adr/README.md\nindex c70e057a4520c36e042ab68c53cd4a21ef709b71..5da43856836e2ce6a150f8127766e2f88355ab4b\n--- a/docs/adr/README.md\n+++ b/docs/adr/README.md\n@@ -1,40 +1,41 @@\n-# Architecture Decision Records\n+# Decision Record Namespaces\n \n-This directory holds Shiki's Architecture Decision Records (ADRs): durable\n-records of hard-to-reverse platform decisions. ADRs are part of the Shiki source\n-of truth alongside GitHub state, the `.shiki/` mirror, and `CONTEXT.md`.\n+This directory holds two independent decision namespaces:\n \n-## How ADRs Work\n+- Shiki Architecture Decision Records (SADRs), named\n+  `SADR-NNNN-<short-slug>.md`, record hard-to-reverse Shiki platform decisions.\n+- Target Architecture Decision Records (ADRs), named\n+  `NNNN-<short-slug>.md`, belong to the target product.\n \n-- New ADRs are added as `docs/adr/NNNN-<short-slug>.md`, where `NNNN` is the next\n-  zero-padded sequence number.\n-- The **canonical list of ADRs is the set of files matching `docs/adr/*.md`** in\n-  this directory. Browse `docs/adr/*.md` for the authoritative, current set.\n-- Use [`template.md`](template.md) as the starting point for a new ADR.\n-- Record an ADR when a decision is hard to reverse, surprising without context,\n-  and a real tradeoff. Do not write an ADR for routine or easily reversible\n-  choices.\n+The same four-digit number may appear once in each namespace. A platform\n+`SADR-NNNN` citation resolves only to `SADR-NNNN-*.md`; a target `ADR NNNN`\n+citation resolves only to `NNNN-*.md`.\n \n-Using the `docs/adr/*.md` glob as the source of truth means other PRs can add an\n-ADR without editing this index file, avoiding merge contention on a\n-hand-maintained enumeration.\n+## How Decision Records Work\n \n-## Current ADRs (snapshot)\n-\n-The list below is a convenience snapshot and may lag behind the directory. When\n-in doubt, list `docs/adr/*.md`.\n+- New Shiki platform decisions use the next zero-padded SADR number.\n+- New target product decisions use the target's own ADR sequence.\n+- The canonical list is the files in `docs/adr/`; no hand-maintained index is\n+  authoritative.\n+- Use [`template.md`](template.md) as the starting point and choose the filename\n+  namespace that owns the decision.\n+- Record a decision when it is hard to reverse, surprising without context, and\n+  a real tradeoff. Do not write a decision record for routine or easily\n+  reversible choices.\n \n-- [0001 \u2014 Keep Shiki As The Platform Name](0001-keep-shiki-as-platform-name.md)\n-- [0002 \u2014 Use AGENTS As The Shared Agent Constitution](0002-use-agents-as-shared-constitution.md)\n-- [0003 \u2014 Require Skill Gate For MergeGate Readiness](0003-require-skill-gate-for-mergegate-readiness.md)\n-- [0004 \u2014 Use GitHub First With A Repository-Local Shiki Mirror](0004-use-github-first-with-shiki-mirror.md)\n-- [0005 \u2014 Use Codex As Front And Claude Code Action As The GitHub Runtime](0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md)\n-- [0006 \u2014 Separate Codex Implementation, CCA Completion, And MergeGate Authorization](0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md)\n+Using the directory as the source of truth lets independent changes add records\n+without contending on an enumerated index.\n \n-## Adding A New ADR\n+## Adding A Shiki SADR\n \n-1. Copy `template.md` to `docs/adr/NNNN-<short-slug>.md`.\n+1. Copy `template.md` to `docs/adr/SADR-NNNN-<short-slug>.md`.\n 2. Fill in the title, status, context, decision, and consequences.\n-3. Set the status to `Proposed`, then `Accepted` once the decision is approved.\n-4. Open a PR following `CONTRIBUTING.md`. You do not need to edit this index for\n-   the ADR to be discoverable via `docs/adr/*.md`.\n+3. Set the status to `Proposed`, then `Accepted` once the proper authority has\n+   approved the decision.\n+4. Open a PR following `CONTRIBUTING.md`.\n+\n+## Adding A Target ADR\n+\n+1. Copy `template.md` to `docs/adr/NNNN-<short-slug>.md` using the target's own\n+   sequence.\n+2. Cite it as `ADR NNNN`; do not prefix its filename with `SADR-`.\ndiff --git a/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md b/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\nnew file mode 100644\nindex 0000000000000000000000000000000000000000..7455ce1f3bed02ddddb851ac21a1d1a648c9aeba\n--- /dev/null\n+++ b/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\n@@ -0,0 +1,55 @@\n+# SADR-0019: Separate Shiki Platform SADRs From Target ADRs\n+\n+## Status\n+\n+Accepted\n+\n+## Context\n+\n+Shiki installs its platform decision records into `docs/adr/` in every target\n+repository. Shiki and its targets previously used the same numeric\n+`NNNN-<slug>.md` namespace. A target that already owned ADRs 0013 through 0018\n+therefore acquired two records for each number during installation, making an\n+`ADR NNNN` citation ambiguous. Renumbering target decisions would rewrite target\n+history, while renumbering Shiki decisions per target would destroy their stable\n+platform identity.\n+\n+Upgrades also need to remove the former Shiki-owned numeric paths without ever\n+mistaking a target-owned ADR for platform content. The install stamp already\n+records exact shipped paths and content digests, so it can prove ownership only\n+when both still match.\n+\n+## Decision\n+\n+Shiki platform decisions use the `SADR` namespace:\n+\n+- records are named `docs/adr/SADR-NNNN-<slug>.md`;\n+- live platform citations use `SADR-NNNN`;\n+- target product decisions retain `docs/adr/NNNN-<slug>.md` and `ADR NNNN`;\n+- the reference validator resolves the two namespaces independently, so the same\n+  four-digit number may exist once in each namespace.\n+\n+Before a forced upgrade writes anything, the installer inventories the exact\n+former numeric Shiki ADR paths. It may delete a legacy path only when the\n+existing install stamp contains that exact path and its recorded SHA-256 digest\n+matches the file currently on disk. All ownership blockers are reported\n+together and abort the upgrade before its first write. A non-forced install\n+never performs this cleanup.\n+\n+Historical `.shiki/` evidence remains unchanged because it records the namespace\n+that existed when that evidence was produced.\n+\n+## Consequences\n+\n+- Targets may use any ADR number without colliding with Shiki's platform SADRs.\n+- Fresh installs preserve target ADR bytes and install a stable SADR set.\n+- Forced upgrades fail closed when legacy ownership cannot be proven, and a\n+  successful upgrade refreshes the stamp without removed legacy paths.\n+- Current governance, packaging, workflow, prompt, script, and skill references\n+  must move with the platform records.\n+\n+## Alternatives Considered\n+\n+- Collision-only refusal leaves affected targets permanently unupgradable.\n+- Renumbering target ADRs mutates target-owned history.\n+- Per-target Shiki renumbering makes platform citations non-portable.\ndiff --git a/docs/adr/template.md b/docs/adr/template.md\nindex a92a97daddd0b00ab9dd626cc8d86942da2eafed..17dd79175b5fee2105935557f691b40cf9492942\n--- a/docs/adr/template.md\n+++ b/docs/adr/template.md\n@@ -1,14 +1,14 @@\n-# NNNN: <Short Decision Title>\n+# <SADR-NNNN or NNNN>: <Short Decision Title>\n \n ## Status\n \n-Proposed | Accepted | Superseded by ADR-NNNN\n+Proposed | Accepted | Superseded by SADR-NNNN or target ADR NNNN\n \n ## Context\n \n What is the situation, constraint, or force that requires a decision? Reference\n-relevant domain terms (`CONTEXT.md`), prior ADRs, and code reality. State the\n-problem, not the solution.\n+relevant domain terms (`CONTEXT.md`), prior Shiki SADRs or target ADRs, and code\n+reality. State the problem, not the solution.\n \n ## Decision\n \ndiff --git a/docs/agents/autonomous-loop-e2e-acceptance.md b/docs/agents/autonomous-loop-e2e-acceptance.md\nindex bbf84c6081987f159ac6ad9ecf3c82b7aa72e7e8..5c13c1d02a05a90b0dc46cbef7c95a9846d0c237\n--- a/docs/agents/autonomous-loop-e2e-acceptance.md\n+++ b/docs/agents/autonomous-loop-e2e-acceptance.md\n@@ -40,8 +40,8 @@\n 3. `gh auth status` is logged in with merge permission on the repo.\n 4. Branch protection + required checks are configured exactly as the goal-loop\n    required-check set expects (see `docs/agents/branch-protection-smoke.md`).\n-5. The repo `.shiki/policy` permits autonomous low/medium-risk merges (ADR\n-   0008/0009); the MergeGate policy check is the recorded-authority gate.\n+5. The repo `.shiki/policy` permits autonomous low/medium-risk merges\n+   (SADR-0008/0009); the MergeGate policy check is the recorded-authority gate.\n \n The operator's only actions are: register the task and start the loop. **No\n manual pre-clearing** means after `shiki loop run` starts, the operator does not\ndiff --git a/docs/agents/checklists.md b/docs/agents/checklists.md\nindex 4a7f03ade346f53a1c1d82981f3fd744f9ab4547..0c5ec6c53dc7197b7b7a7d18e667b325babd6c45\n--- a/docs/agents/checklists.md\n+++ b/docs/agents/checklists.md\n@@ -32,18 +32,18 @@\n | ID | Item | Blocking |\n | --- | --- | --- |\n | GD-01 | Plan was challenged against `CONTEXT.md`. | blocking for non-trivial Goals |\n-| GD-02 | Relevant ADRs were read or absence was recorded. | blocking for non-trivial Goals |\n+| GD-02 | Relevant SADRs and target ADRs were read or absence was recorded. | blocking for non-trivial Goals |\n | GD-03 | Ambiguous terms were resolved or converted to HITL blockers. | blocking |\n | GD-04 | Edge scenarios were considered. | advisory |\n | GD-05 | Code reality was checked when code could answer. | blocking when applicable |\n | GD-06 | New domain terms were added to `CONTEXT.md` when needed. | blocking when applicable |\n-| GD-07 | ADR was created or explicitly skipped using the ADR criteria. | blocking when applicable |\n+| GD-07 | A SADR or target ADR was created or explicitly skipped using the decision-record criteria. | blocking when applicable |\n \n ## CI \u2014 Context & Impact Checklist\n \n | ID | Item | Blocking |\n | --- | --- | --- |\n-| CI-01 | Relevant docs and ADRs identified. | blocking |\n+| CI-01 | Relevant docs, SADRs, and target ADRs identified. | blocking |\n | CI-02 | Relevant modules, interfaces, seams, callers, and tests identified. | blocking |\n | CI-03 | Dependencies and blocked-by relationships identified. | blocking |\n | CI-04 | Candidate locks identified. | blocking |\n@@ -62,7 +62,7 @@\n | PRD-04 | Implementation decisions are recorded without volatile code detail. | blocking |\n | PRD-05 | Testing decisions are recorded. | blocking |\n | PRD-06 | Out-of-scope items are explicit. | blocking |\n-| PRD-07 | Relevant glossary/ADR links are present when applicable. | blocking |\n+| PRD-07 | Relevant glossary/SADR/target-ADR links are present when applicable. | blocking |\n \n ## SF \u2014 Spec Freeze Checklist\n \n@@ -98,7 +98,7 @@\n | H-02 | Branch/worktree target is present. | blocking |\n | H-03 | Scope and non-goals are present. | blocking |\n | H-04 | Dependencies and locks are present. | blocking |\n-| H-05 | Relevant docs, ADRs, modules, and tests are referenced. | blocking |\n+| H-05 | Relevant docs, SADRs, target ADRs, modules, and tests are referenced. | blocking |\n | H-06 | Required skills are stated. | blocking |\n | H-07 | TDD expectations are stated. | blocking when behavior work |\n | H-08 | Acceptance criteria are included. | blocking |\ndiff --git a/docs/agents/codex-handoff.md b/docs/agents/codex-handoff.md\nindex ede468f47cb277f69a857e8c45df703e4ed0151d..6f97bdffb67e44d7edea2c71e01ea28933c4eb6c\n--- a/docs/agents/codex-handoff.md\n+++ b/docs/agents/codex-handoff.md\n@@ -46,7 +46,7 @@\n \n ## Context\n - Domain terms:\n-- Relevant ADRs:\n+- Relevant SADRs and target ADRs:\n - Relevant docs:\n - Relevant modules/interfaces/seams:\n - Similar tests/prior art:\ndiff --git a/docs/agents/completion-check-agent.md b/docs/agents/completion-check-agent.md\nindex 13e4c035db35025f4e3cdcc5adfb24314ac4d521..61bfa1218cf36ca605250c91919e620f33238aa0\n--- a/docs/agents/completion-check-agent.md\n+++ b/docs/agents/completion-check-agent.md\n@@ -32,7 +32,7 @@\n - `AGENTS.md`\n - `CLAUDE.md`\n - `CONTEXT.md`\n-- relevant ADRs\n+- relevant SADRs and target ADRs\n - parent Goal issue\n - PRD issue or document\n - task issue\ndiff --git a/docs/agents/decision-control.md b/docs/agents/decision-control.md\nindex 7d8ee5ab03a5ccbcbd04056ae61de03785a2a9c9..8c94ecda00fffaa0e92c325ab83aa9776bd1dac1\n--- a/docs/agents/decision-control.md\n+++ b/docs/agents/decision-control.md\n@@ -11,7 +11,7 @@\n | Decision | Owner | When | Required inputs | Output | Enforcement surface |\n | --- | --- | --- | --- | --- | --- |\n | Is the Goal clear enough to plan? | Planner + Guardian when needed | Goal intake | User request, existing docs, risk signals | Goal readiness verdict | Goal checklist, issue template, ledger |\n-| Must `grill-with-docs` run? | Planner / Skill Gate | Before PRD or issue split | Ambiguity, domain terms, ADR conflicts, unclear boundaries | Skill invocation or explicit skip rationale | Skill Gate checklist |\n+| Must `grill-with-docs` run? | Planner / Skill Gate | Before PRD or issue split | Ambiguity, domain terms, SADR/target-ADR conflicts, unclear boundaries | Skill invocation or explicit skip rationale | Skill Gate checklist |\n | Is a PRD required? | Planner | After Goal context stabilizes | Goal, `grill-with-docs` output, Context & Impact output | PRD required / not required | PRD checklist, CCA evidence check |\n | Can the spec be frozen? | Operator only | End of Requirements Definition, before issue split | PRD/requirements summary, scope inventory (SF-02), open-question list | `spec_freeze` block in the plan + ledger evidence | `require_grilled_plan` (plan ingest/run), SF checklist |\n | Can a frozen spec change? | Operator only (Spec Amendment) | When implementation contradicts the frozen spec | Contested decisions, scoped re-grill result | recorded amendment + re-stamped freeze | SF checklist, ledger evidence |\ndiff --git a/docs/agents/domain.md b/docs/agents/domain.md\nindex 258da8ea81e87ac0a6b0b5b5e5c2fde980eaabb7..d57b0cf17ff3e1fa9e6e5885372bf083d8295421\n--- a/docs/agents/domain.md\n+++ b/docs/agents/domain.md\n@@ -5,7 +5,9 @@\n ## Layout\n \n - `CONTEXT.md` defines Shiki language. Keep it implementation-free.\n-- `docs/adr/` records hard-to-reverse decisions.\n+- `docs/adr/SADR-NNNN-*.md` records Shiki platform decisions.\n+- `docs/adr/NNNN-*.md` records target product decisions without sharing the\n+  SADR namespace.\n - `AGENTS.md` is the runtime-neutral agent constitution.\n - `CLAUDE.md` is the Claude Code wrapper around `AGENTS.md`.\n - `.shiki/` is the Target Repository mirror for Goals, PRDs, plans, Task DAGs, contracts, locks, worktree records, ledger entries, CCA verdicts, repair packets, reports, and handoffs.\n@@ -15,6 +17,6 @@\n \n - Use Shiki terms exactly when they exist.\n - Add glossary terms to `CONTEXT.md` only when the term is part of the domain, not a generic programming concept.\n-- Create ADRs only for hard-to-reverse, surprising, tradeoff-based decisions.\n+- Create SADRs or target ADRs only for hard-to-reverse, surprising, tradeoff-based decisions.\n - Keep product-specific language in the Target Repository, not in Shiki Template docs.\n-- If an ADR conflicts with a proposed implementation, surface the conflict before editing.\n+- If a SADR or target ADR conflicts with a proposed implementation, surface the conflict before editing.\ndiff --git a/docs/agents/execution-control.md b/docs/agents/execution-control.md\nindex ad8ee3cdb36be56fedc1f46408ca92b387d74047..bc63c3a73661d12b567016ba299f1a6b66bf9088\n--- a/docs/agents/execution-control.md\n+++ b/docs/agents/execution-control.md\n@@ -63,7 +63,7 @@\n | Branch name | Orchestrator | Orchestrator | Before implementer dispatch | branch policy | branch ref in task/ledger |\n | Lock reservation | Context & Impact | MergeGate | Before dispatch and before merge | `.shiki/locks/`, labels, PR metadata | lock id, owner, scope |\n | CI profile | Planner | MergeGate | During issue triage, rechecked at PR | labels, `.shiki/policy.yaml`, workflow matrix | required check list |\n-| Required status checks | Repo maintainer / Guardian | GitHub branch protection / rulesets | Repository setup, updated via ADR | GitHub settings | branch/ruleset config, ADR |\n+| Required status checks | Repo maintainer / Guardian | GitHub branch protection / rulesets | Repository setup, updated via target ADR | GitHub settings | branch/ruleset config, target ADR |\n | CCA judgment | CCA | GitHub required check / MergeGate | Every non-draft PR update | GitHub Action check | structured CCA verdict |\n | Repair assignment | CCA | MergeGate | After failed CCA/CI/review | repair packet, issue/PR comment | repair id and attempt count |\n | Merge readiness | MergeGate | GitHub branch protection / Guardian if needed | After CCA complete | required checks, review rules, rulesets | mergegate check output |\ndiff --git a/docs/agents/guardian-policy.md b/docs/agents/guardian-policy.md\nindex 4147c7adf05a47adb4fd24ada1a1c2e1f2ba6d2d..a20d9cc6c00a569bb057b9ca8d6b51c7fc4a9847\n--- a/docs/agents/guardian-policy.md\n+++ b/docs/agents/guardian-policy.md\n@@ -98,8 +98,8 @@\n `origin/<default_branch>` and reuses each job's existing base fetch; a shallow\n (`--depth`) or second base fetch would make the proof unresolvable and silently\n disable the carry, so neither is permitted. CCA reads `guardian_comment_carried` as\n-a recorded authority in the deterministic signal and does not re-derive it. See ADR\n-0018 for the full decision, the rejected diff/patch-equality and path-disjointness\n+a recorded authority in the deterministic signal and does not re-derive it. See\n+SADR-0018 for the full decision, the rejected diff/patch-equality and path-disjointness\n alternatives, and the accepted residual.\n \n ## External AI Guardian Review (SADR-0010)\ndiff --git a/docs/agents/implementation-policy.md b/docs/agents/implementation-policy.md\nindex c754144a9fb5bf4a38aa82a60e1670c59e626b44..85c37281bcad196c6b7b02343faf00b03cd9c57a\n--- a/docs/agents/implementation-policy.md\n+++ b/docs/agents/implementation-policy.md\n@@ -31,10 +31,13 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n ## Non-Negotiables\n \n 1. **Planning precedes implementation.** Non-trivial Goals start with `grill-with-docs` and Context & Impact.\n-2. **Domain language is controlled.** Terms must be checked against `CONTEXT.md`; hard-to-reverse tradeoffs must be checked against ADRs.\n+2. **Domain language is controlled.** Terms must be checked against `CONTEXT.md`; hard-to-reverse tradeoffs must be checked against Shiki SADRs and target ADRs.\n 3. **PRD precedes issue decomposition.** `to-prd` is used once the context is settled enough to become durable intent.\n 4. **Issues are vertical slices.** `to-issues` must produce independently grabbable tracer-bullet tasks, not horizontal layer tickets.\n 5. **Triage controls dispatch.** Only `ready-for-agent` / AFK tasks may be assigned to Codex without human input.\n@@ -95,19 +98,19 @@\n Process:\n \n - Challenge terms against `CONTEXT.md`.\n-- Check ADRs before reopening decisions.\n+- Check SADRs and target ADRs before reopening decisions.\n - Inspect code when code can answer a question.\n - Ask one question at a time when the operator is available.\n - Provide a recommended answer with each question.\n - Record resolved terms in `CONTEXT.md`.\n-- Create ADRs only for hard-to-reverse, surprising, tradeoff-based decisions.\n+- Create SADRs or target ADRs only for hard-to-reverse, surprising, tradeoff-based decisions.\n - Convert unresolved load-bearing questions into HITL blockers.\n \n Outputs:\n \n - Resolved design-tree decisions.\n - Updated glossary terms when needed.\n-- ADRs when justified.\n+- SADRs or target ADRs when justified.\n - HITL blockers when decisions remain unresolved.\n \n ## Phase 3 \u2014 Context & Impact\n@@ -117,7 +120,7 @@\n \n Outputs must include:\n \n-- Relevant documents and ADRs.\n+- Relevant documents, SADRs, and target ADRs.\n - Relevant modules, interfaces, seams, callers, and tests.\n - Candidate locks.\n - Dependencies.\n@@ -140,7 +143,7 @@\n - Testing decisions.\n - Out of scope.\n - Further notes.\n-- Links to relevant Goal, domain terms, and ADRs.\n+- Links to relevant Goal, domain terms, SADRs, and target ADRs.\n \n Phase 4 ends with Spec Freeze: the operator explicitly approves the PRD, the\n plan records a `spec_freeze` block (status, approved_by, source), and the SF\n@@ -240,7 +243,7 @@\n - Dependencies and locks.\n - Risk and Guardian requirements.\n - Ledger completeness.\n-- Documentation and ADR updates.\n+- Documentation and SADR/target-ADR updates.\n - Scope drift.\n \n CCA emits one of:\n@@ -301,7 +304,7 @@\n | Evidence failure | Behavior might be done, but proof is missing. | Add checks, PR evidence, ledger entry, or artifacts. |\n | Scope drift | PR changes unrelated behavior. | Revert unrelated changes or split into new Goal. |\n | Dependency block | Upstream task or lock not complete. | Block until dependency resolves. |\n-| Architecture block | Decision needs ADR or deep-module discussion. | `grill-with-docs` or `improve-codebase-architecture`. |\n+| Architecture block | Decision needs a SADR, target ADR, or deep-module discussion. | `grill-with-docs` or `improve-codebase-architecture`. |\n | Risk block | Security/production/policy change needs human approval. | Guardian decision. |\n \n ## Phase 5.5 \u2014 Execution Decision Control\ndiff --git a/docs/agents/runtime-auth-model.md b/docs/agents/runtime-auth-model.md\nindex 6a2e1a47e81ea89b5c68a210ec07772a9374babc..3c54748d24233dbd0ba8473950282547ef4ce935\n--- a/docs/agents/runtime-auth-model.md\n+++ b/docs/agents/runtime-auth-model.md\n@@ -148,7 +148,7 @@\n \n API-key based runners may be added by a target repository as an explicit extension. They are not the default Shiki template path.\n \n-If a repo opts into API-key mode, record the exception in an ADR and update the repo's `AGENTS.md`, `.shiki/config.yaml`, and workflow permissions.\n+If a repo opts into API-key mode, record the target-specific exception in a target ADR and update the repo's `AGENTS.md`, `.shiki/config.yaml`, and workflow permissions.\n \n ## GitHub Actions Runtime Compatibility\n \ndiff --git a/docs/agents/skill-gate.md b/docs/agents/skill-gate.md\nindex c50398b0d7c60a8598f2a5c88a10015730dced13..3acffe6444617bb1d54b77d4b4708158ec735bc6\n--- a/docs/agents/skill-gate.md\n+++ b/docs/agents/skill-gate.md\n@@ -9,7 +9,7 @@\n | Skill | Trigger | Expected output |\n | --- | --- | --- |\n | `setup-matt-pocock-skills` | First configuring a repo for these skills, or when issue tracker, triage labels, or domain docs are missing. | `docs/agents/` configuration and an `Agent skills` block in `AGENTS.md` or `CLAUDE.md`. |\n-| `grill-with-docs` | Ambiguous plan, unclear terminology, boundary decision, tradeoff, or ADR-worthy design choice. | Clarified decision tree, recommended answers, updates to `CONTEXT.md` or ADRs when decisions crystallize. |\n+| `grill-with-docs` | Ambiguous plan, unclear terminology, boundary decision, tradeoff, or decision-record-worthy design choice. | Clarified decision tree, recommended answers, updates to `CONTEXT.md`, SADRs, or target ADRs when decisions crystallize. |\n | `zoom-out` | Agent lacks a higher-level architectural map. | Map of relevant modules, callers, seams, and domain concepts. |\n | `to-prd` | Settled context should become a PRD. | PRD published to the issue tracker with problem, solution, user stories, decisions, testing, and out-of-scope notes. |\n | `to-issues` | Goal, PRD, or plan must become implementation tickets. | Vertical-slice issues with acceptance criteria, dependencies, and AFK/HITL classification. |\n@@ -17,7 +17,7 @@\n | `tdd` | Feature work or bug fix has observable behavior that can be specified with tests. | Red-green-refactor loop through public interfaces, one vertical slice at a time. |\n | `code-review` | The TDD loop is green and the implementer is about to create or update a PR. Default for every implementation task. | Pre-PR self-review of the task diff with fixes applied on the task branch; findings and resolutions recorded as a ledger entry naming the skill and a `## Pre-PR code review` PR body section. |\n | `diagnose` | Hard bug, failing check, regression, flaky behavior, or performance problem. | Reproduction loop, ranked hypotheses, instrumentation, fix, regression test, post-mortem. |\n-| `improve-codebase-architecture` | Structural friction, testability, AI-navigability, deep-module opportunity, or refactor request. | Architecture candidates and deepening recommendations informed by `CONTEXT.md` and ADRs. |\n+| `improve-codebase-architecture` | Structural friction, testability, AI-navigability, deep-module opportunity, or refactor request. | Architecture candidates and deepening recommendations informed by `CONTEXT.md`, SADRs, and target ADRs. |\n | `prototype` | A throwaway logic or UI prototype is needed to answer a design question. | Clearly marked disposable prototype and captured decision. |\n \n ## MergeGate Interaction\ndiff --git a/scripts/guardian_approval_signal.py b/scripts/guardian_approval_signal.py\nindex d059f92b2c82aa2b5d6ca421365537a87886c1cb..16924892714f4f5b9303c0ef0e325a7e70075630\n--- a/scripts/guardian_approval_signal.py\n+++ b/scripts/guardian_approval_signal.py\n@@ -199,8 +199,8 @@\n ) -> Any:\n     \"\"\"Evaluate SADR-0015 Contract Approval for the CCA Guardian signal.\n \n-    The SAME OR that MergeGate applies must reach this deterministic signal: ADR\n-    0015 is explicit that without it, a contract-approved PR still returns\n+    The SAME OR that MergeGate applies must reach this deterministic signal:\n+    SADR-0015 is explicit that without it, a contract-approved PR still returns\n     ``needs_guardian``, the CCA job fails, and the MergeGate policy check never\n     runs \u2014 the gate this change moves would never be reached. This resolves the\n     task the SAME way MergeGate does (the first Shiki task id in the PR body),\ndiff --git a/scripts/shiki_contract_approval.py b/scripts/shiki_contract_approval.py\nindex 941bcb96fdd8b9a85bc0c4f0f04d5dad9d04839c..4a4b418f6527413161db82670bec857a043360e2\n--- a/scripts/shiki_contract_approval.py\n+++ b/scripts/shiki_contract_approval.py\n@@ -142,7 +142,7 @@\n     changed_files_status: Any,\n     registration: Any,\n ) -> ContractApprovalResult:\n-    \"\"\"Whether this PR carries an SADR-0015 Contract Approval.\n+    \"\"\"Whether this PR carries a SADR-0015 Contract Approval.\n \n     The four conditions are evaluated IN ORDER, each failure recorded as a\n     distinct reason string:\ndiff --git a/scripts/shiki_guardian.py b/scripts/shiki_guardian.py\nindex a4ecb79bbda55fd5c8b06adfae2eb99760a03c45..4dd99bbe714e26f421fa19abac7a34ae1b195ecc\n--- a/scripts/shiki_guardian.py\n+++ b/scripts/shiki_guardian.py\n@@ -12,7 +12,7 @@\n # The carry re-derives its proof from git in-process on every judging invocation;\n # it is never read from an artifact, so there is nothing to go stale or be forged.\n # shiki_sync_proof is the ONLY guardian-family module that runs git, and every one\n-# of its invocations is read-only and local (ADR: prove-from-git base sync).\n+# of its invocations is read-only and local (SADR: prove-from-git base sync).\n from shiki_sync_proof import verify_pure_base_sync\n \n GUARDIAN_POLICY_PATH = \".shiki/guardian-policy.json\"\ndiff --git a/scripts/shiki_guardian_review.py b/scripts/shiki_guardian_review.py\nindex dd0f104d457899e0ba5ff1e162011c1e90b6f92b..e10ef58c046324e7ea3a3392feb3048ca77414ce\n--- a/scripts/shiki_guardian_review.py\n+++ b/scripts/shiki_guardian_review.py\n@@ -159,7 +159,7 @@\n     docs_only = _is_docs_only(changed_files)\n     if docs_only and matched == 0:\n         focus.append(\n-            \"Docs/ADR-only change: confirm the change is genuinely documentation/decision-only, introduces no behavior or contract drift, and stays consistent with the constitution and existing ADRs.\"\n+            \"Docs/decision-record-only change: confirm the change is genuinely documentation/decision-only, introduces no behavior or contract drift, and stays consistent with the constitution and existing SADRs and target ADRs.\"\n         )\n \n     # Conservative augmentation: unknown (no category and not docs-only), mixed\ndiff --git a/scripts/shiki_installer.py b/scripts/shiki_installer.py\nindex be064ab7a29ff6836f7f7a1d70500a43e0d11d7b..9fd8eefd137ce7003dad5c0dcb2e48e2cd47ab32\n--- a/scripts/shiki_installer.py\n+++ b/scripts/shiki_installer.py\n@@ -118,6 +118,7 @@\n # what lets a target carry it without a governance-schema change.\n INSTALL_STAMP_PATH = \".shiki/install-stamp.json\"\n INSTALL_STAMP_VERSION = 1\n+LEGACY_SHIKI_SADR_MAX = 18\n \n # Under --force the shipped surface splits three ways, so nothing is lost and\n # nothing is silently stale.\n@@ -237,9 +238,98 @@\n         )\n \n \n+def legacy_shiki_adr_paths() -> tuple[str, ...]:\n+    \"\"\"Former numeric paths for Shiki SADR-0001 through SADR-0018.\n+\n+    The authoritative slugs come from the current SADR filenames. SADR-0019 is\n+    the namespace decision itself and never shipped under a numeric legacy path.\n+    \"\"\"\n+    paths: list[str] = []\n+    for sadr in sorted((ROOT / \"docs\" / \"adr\").glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\")):\n+        number = sadr.name.split(\"-\", 2)[1]\n+        if int(number) <= LEGACY_SHIKI_SADR_MAX:\n+            paths.append(f\"docs/adr/{sadr.name.removeprefix('SADR-')}\")\n+    return tuple(paths)\n+\n+\n+def preflight_legacy_sadr_cleanup(target: Path, *, force: bool) -> tuple[str, ...]:\n+    \"\"\"Authorize exact legacy Shiki ADR removals before the first write.\n+\n+    A non-force install never removes existing files. A forced install needs no\n+    stamp when no legacy Shiki path is present. When legacy paths do exist, each\n+    must be named in a readable install stamp and its current bytes must match\n+    the recorded digest.\n+    \"\"\"\n+    if not force:\n+        return ()\n+    present = tuple(\n+        relative\n+        for relative in legacy_shiki_adr_paths()\n+        if (target / relative).exists() or (target / relative).is_symlink()\n+    )\n+    if not present:\n+        return ()\n+\n+    stamp_path = target / INSTALL_STAMP_PATH\n+    try:\n+        raw = stamp_path.read_text(encoding=\"utf-8\")\n+        stamp = json.loads(raw)\n+    except FileNotFoundError:\n+        detail = \"install stamp is absent\"\n+        blockers = [f\"{relative}: {detail}\" for relative in present]\n+    except (OSError, UnicodeDecodeError) as error:\n+        detail = f\"install stamp is unreadable ({error})\"\n+        blockers = [f\"{relative}: {detail}\" for relative in present]\n+    except json.JSONDecodeError as error:\n+        detail = f\"install stamp is malformed ({error})\"\n+        blockers = [f\"{relative}: {detail}\" for relative in present]\n+    else:\n+        if not isinstance(stamp, dict):\n+            blockers = [\n+                f\"{relative}: install stamp is malformed (expected an object)\"\n+                for relative in present\n+            ]\n+        else:\n+            digests = stamp.get(\"digests\")\n+            blockers = []\n+            if not isinstance(digests, dict):\n+                blockers.extend(\n+                    f\"{relative}: install stamp has no digest for this path\"\n+                    for relative in present\n+                )\n+            else:\n+                for relative in present:\n+                    expected = digests.get(relative)\n+                    if not isinstance(expected, str) or not expected:\n+                        blockers.append(\n+                            f\"{relative}: install stamp has no digest for this path\"\n+                        )\n+                        continue\n+                    try:\n+                        actual = _sha256_file(target / relative)\n+                    except OSError as error:\n+                        blockers.append(\n+                            f\"{relative}: legacy path is unreadable ({error})\"\n+                        )\n+                        continue\n+                    if actual != expected:\n+                        blockers.append(\n+                            f\"{relative}: digest mismatch (stamp {expected}, current {actual})\"\n+                        )\n+\n+    if blockers:\n+        joined = \"\\n\".join(f\"- {blocker}\" for blocker in blockers)\n+        raise ShikiError(\n+            \"legacy Shiki ADR cleanup is not authorized:\\n\"\n+            f\"{joined}\\nNo files were written.\"\n+        )\n+    return present\n+\n+\n def install_template(target: Path, *, force: bool, validate: bool) -> list[NewFileNote]:\n     # Preconditions first: never rewrite the tree of a target that cannot be\n     # upgraded cleanly (see refuse_pending_migrations).\n+    legacy_cleanup = preflight_legacy_sadr_cleanup(target, force=force)\n     refuse_pending_migrations(target)\n \n     # Resolve the target's required CODEOWNERS owner once from its\n@@ -250,6 +340,10 @@\n     # verbatim. See ``codeowners_required_owner``.\n     codeowners_owner = codeowners_required_owner(target)\n \n+    for relative in legacy_cleanup:\n+        (target / relative).unlink()\n+        info(f\"removed stamped legacy Shiki ADR: {target / relative}\")\n+\n     notes: list[NewFileNote] = []\n     for relative in TEMPLATE_PATHS:\n         source = ROOT / relative\n@@ -647,6 +741,22 @@\n     digests = target_stamp_digests(target)\n     existing = read_install_stamp(target)\n     if existing is not None:\n+        # A non-force install deliberately leaves legacy Shiki ADRs in place.\n+        # Preserve only still-matching ownership proof so a later forced\n+        # upgrade can authorize cleanup; never carry a missing or stale digest.\n+        existing_digests = existing.get(\"digests\")\n+        if isinstance(existing_digests, dict):\n+            for relative in legacy_shiki_adr_paths():\n+                expected = existing_digests.get(relative)\n+                path = target / relative\n+                if not isinstance(expected, str) or not path.is_file():\n+                    continue\n+                try:\n+                    actual = _sha256_file(path)\n+                except OSError:\n+                    continue\n+                if actual == expected:\n+                    digests[relative] = actual\n         if _incoming_is_older(commit, existing.get(\"platform_commit\")):\n             warn(\n                 \"kept existing install stamp: incoming install \"\ndiff --git a/scripts/shiki_loop.py b/scripts/shiki_loop.py\nindex 3611641da8aeb5038b1da90e3114b09a473ec6d8..84caa491b9cad0edc4176f209c13e9221e975681\n--- a/scripts/shiki_loop.py\n+++ b/scripts/shiki_loop.py\n@@ -424,7 +424,7 @@\n         # unresolvable CCA verdict has already produced its terminal stop and is never\n         # laundered into a head-moving sync (SADR-0010). It also never fires while the\n         # policy (Guardian) gate is red \u2014 for ANY reason, not only when it is the sole\n-        # red check \u2014 so a head-bound Guardian approval, or an SADR-0015\n+        # red check \u2014 so a head-bound Guardian approval, or a SADR-0015\n         # contract-approval carry that base movement defeated (the policy gate compares\n         # base and head task governance directly), is never silently restored by\n         # adopting base wholesale. It is bounded by a durable per-task counter so the\ndiff --git a/scripts/shiki_tasks.py b/scripts/shiki_tasks.py\nindex aec3809f24b4a0c8c96fc50971cff9ae9ad30381..d531052e9d9717058d4631483cefab44c26a65c1\n--- a/scripts/shiki_tasks.py\n+++ b/scripts/shiki_tasks.py\n@@ -821,7 +821,7 @@\n         \"prompt\": prompt,\n         \"entry_skill\": \"grill-with-docs\",\n         \"required_next_steps\": [\n-            \"Run grill-with-docs until terminology, boundaries, risks, and ADR-worthy decisions are settled.\",\n+            \"Run grill-with-docs until terminology, boundaries, risks, and decision-record-worthy choices are settled.\",\n             \"Obtain the operator's explicit approval of the PRD/requirements (Spec Freeze).\",\n             \"Write a machine-readable plan JSON with grill_with_docs.status=complete and spec_freeze.status=frozen.\",\n             \"Run shiki plan ingest --plan-file PLAN.json.\",\ndiff --git a/scripts/test_shiki_goal_loop.sh b/scripts/test_shiki_goal_loop.sh\nindex 753c7b516e10f0be3bc92a4bd0eb1fa699776ae6..8f3820a0a079b3044424cfe1a6c6b7fe68896e67\n--- a/scripts/test_shiki_goal_loop.sh\n+++ b/scripts/test_shiki_goal_loop.sh\n@@ -231,8 +231,8 @@\n \n # High/critical risk merges autonomously when all required checks are green:\n # the \"MergeGate policy check\" required check IS the Guardian gate (it enforces\n-# guardian-policy.json \u2014 human approval OR external AI guardian review, ADR\n-# 0010), so green checks mean a recorded authority approved. The fake gh returns\n+# guardian-policy.json \u2014 human approval OR external AI guardian review,\n+# SADR-0010), so green checks mean a recorded authority approved. The fake gh returns\n # green checks, simulating that the Guardian gate passed.\n python3 \"$ROOT/scripts/shiki.py\" goal create --target \"$TARGET\" --title \"Guardian gate\" --outcome \"High risk merges via policy gate\" >/tmp/shiki-goal-loop-guardian-goal.json\n GGOAL=\"$(json_get /tmp/shiki-goal-loop-guardian-goal.json goal_id)\"\ndiff --git a/tests/test_adr_references.py b/tests/test_adr_references.py\nindex 51706329c797d5dd9b47e082a333fa83b261d482..db6e693e35f67103935be34f1d9bb4ff7ee60395\n--- a/tests/test_adr_references.py\n+++ b/tests/test_adr_references.py\n@@ -1,7 +1,7 @@\n-\"\"\"Every ``ADR ####`` citation must resolve to exactly one decision record.\n+\"\"\"Every target ADR and Shiki SADR citation resolves in its own namespace.\n \n Background. ``scripts/mergegate_check.py`` justifies ``is_bookkeeping_closeout``\n-with \"ADR 0017\" nine times and ``scripts/guardian_approval_signal.py`` cites\n+with \"SADR-0017\" nine times and ``scripts/guardian_approval_signal.py`` cites\n 0016/0017 seven times, yet for a stretch the numbering in ``docs/adr/`` stopped\n at 0015: the authoring commits lived on a coordinator branch and no task ever\n declared ``docs/adr/...`` in its locks, so nothing carried the records to the\n@@ -11,11 +11,12 @@\n and the directory drifted with no signal.\n \n This is the same unbound-pair shape behind other silent failures: a reference\n-and its target with nothing asserting they agree. This test binds them. It scans\n-every ``scripts/*.py`` (plus ``docs/**/*.md`` and the root ``*.md`` governance\n-files, which are cheap) for the ``ADR ####`` pattern and asserts each cited\n-number resolves to exactly one ``docs/adr/####-*.md``. The converse is\n-deliberately NOT required: an ADR may exist without being cited.\n+and its target with nothing asserting they agree. This test binds them. Target\n+``ADR NNNN`` citations resolve only to ``docs/adr/NNNN-*.md``; Shiki\n+``SADR-NNNN`` citations resolve only to ``docs/adr/SADR-NNNN-*.md``. The scan\n+covers current governance docs, scripts, workflows, prompts, packaging metadata,\n+and repo-local Codex guidance while deliberately excluding historical\n+``.shiki`` evidence. The converse is not required: a record may be uncited.\n \n The suite is dependency free: standard-library ``unittest`` only.\n \"\"\"\n@@ -31,17 +32,18 @@\n \n REPO_ROOT = Path(__file__).resolve().parents[1]\n \n-# \"ADR 0016\" -- a four-digit citation. The trailing \\b keeps a five-digit token\n-# from being read as a truncated four-digit number.\n-_CITATION = re.compile(r\"\\bADR (\\d{4})\\b\")\n-# \"0016-locks-bound-product-paths.md\" -- the leading four digits name the record.\n-_ADR_FILENAME = re.compile(r\"^(\\d{4})-.*\\.md$\")\n+# ``ADR 0016`` / ``SADR-0016`` -- a namespace plus four-digit citation. The\n+# trailing ``\\b`` keeps a five-digit token from being read as a truncated one.\n+_CITATION = re.compile(r\"\\b(S?ADR)[ -](\\d{4})\\b\")\n+# Target ADRs retain the numeric filename; Shiki platform decisions use SADR.\n+_DECISION_FILENAME = re.compile(r\"^(?:(SADR)-)?(\\d{4})-.*\\.md$\")\n \n \n class Violation(NamedTuple):\n     \"\"\"A citation that does not resolve to exactly one decision record.\"\"\"\n \n     citing_file: str  # repo-relative path of the file that cites the ADR\n+    namespace: str  # \"ADR\" for target decisions; \"SADR\" for Shiki decisions\n     number: str  # the cited four-digit number, e.g. \"0016\"\n     line: int  # 1-based line of the citation\n     reason: str  # \"missing\" (zero matches) or \"ambiguous\" (two or more)\n@@ -49,44 +51,59 @@\n \n     @property\n     def expected_path(self) -> str:\n-        return f\"docs/adr/{self.number}-*.md\"\n+        prefix = \"SADR-\" if self.namespace == \"SADR\" else \"\"\n+        return f\"docs/adr/{prefix}{self.number}-*.md\"\n \n     def describe(self) -> str:\n         \"\"\"Name the citing file, the cited number, and the missing path.\"\"\"\n         if self.reason == \"missing\":\n             return (\n-                f\"{self.citing_file}:{self.line} cites ADR {self.number} \"\n+                f\"{self.citing_file}:{self.line} cites {self.namespace} {self.number} \"\n                 f\"but no document matches {self.expected_path}\"\n             )\n         joined = \", \".join(self.matches)\n         return (\n-            f\"{self.citing_file}:{self.line} cites ADR {self.number} which \"\n+            f\"{self.citing_file}:{self.line} cites {self.namespace} {self.number} which \"\n             f\"resolves to {len(self.matches)} documents (expected exactly one \"\n             f\"at {self.expected_path}): {joined}\"\n         )\n \n \n-def _adr_index(root: Path) -> dict[str, list[Path]]:\n-    \"\"\"Map every four-digit ADR number to the record files that claim it.\"\"\"\n-    index: dict[str, list[Path]] = {}\n+def _adr_index(root: Path) -> dict[tuple[str, str], list[Path]]:\n+    \"\"\"Map each decision namespace and number to the files that claim it.\"\"\"\n+    index: dict[tuple[str, str], list[Path]] = {}\n     adr_dir = root / \"docs\" / \"adr\"\n     if adr_dir.is_dir():\n         for path in sorted(adr_dir.glob(\"*.md\")):\n-            match = _ADR_FILENAME.match(path.name)\n+            match = _DECISION_FILENAME.match(path.name)\n             if match:\n-                index.setdefault(match.group(1), []).append(path)\n+                namespace = \"SADR\" if match.group(1) else \"ADR\"\n+                index.setdefault((namespace, match.group(2)), []).append(path)\n     return index\n \n \n def _citing_files(root: Path, include_docs: bool = True) -> list[Path]:\n-    \"\"\"Files scanned for citations: scripts/*.py, and cheaply the markdown.\"\"\"\n+    \"\"\"Current, shipped text surfaces scanned for decision citations.\"\"\"\n     files = sorted((root / \"scripts\").glob(\"*.py\"))\n+    files += sorted((root / \"scripts\").glob(\"*.sh\"))\n     if include_docs:\n         docs = root / \"docs\"\n         if docs.is_dir():\n             files += sorted(docs.rglob(\"*.md\"))\n         files += sorted(root.glob(\"*.md\"))\n-    return files\n+        pyproject = root / \"pyproject.toml\"\n+        if pyproject.is_file():\n+            files.append(pyproject)\n+        for relative in (\".github\", \".codex\"):\n+            surface = root / relative\n+            if surface.is_dir():\n+                files += sorted(\n+                    path\n+                    for path in surface.rglob(\"*\")\n+                    if path.is_file()\n+                    and path.suffix in {\".md\", \".py\", \".sh\", \".toml\", \".yaml\", \".yml\"}\n+                )\n+    return sorted(set(files))\n \n \n def find_adr_reference_violations(\n@@ -94,10 +111,9 @@\n ) -> list[Violation]:\n     \"\"\"Return every ``ADR ####`` citation under ``root`` that fails to resolve.\n \n-    A citation resolves when exactly one ``docs/adr/####-*.md`` matches its\n-    number. Zero matches is a \"missing\" violation; two or more is \"ambiguous\".\n-    Records that are never cited produce nothing -- the converse is not\n-    required.\n+    A citation resolves when exactly one record in the same namespace matches\n+    its number. Zero matches is a \"missing\" violation; two or more is\n+    \"ambiguous\". Records that are never cited produce nothing.\n     \"\"\"\n     root = Path(root)\n     index = _adr_index(root)\n@@ -110,13 +126,15 @@\n         rel = path.relative_to(root).as_posix()\n         for lineno, line in enumerate(text.splitlines(), start=1):\n             for match in _CITATION.finditer(line):\n-                number = match.group(1)\n-                found = index.get(number, [])\n+                namespace, number = match.groups()\n+                found = index.get((namespace, number), [])\n                 if len(found) == 1:\n                     continue\n                 reason = \"missing\" if not found else \"ambiguous\"\n                 rels = [p.relative_to(root).as_posix() for p in found]\n-                violations.append(Violation(rel, number, lineno, reason, rels))\n+                violations.append(\n+                    Violation(rel, namespace, number, lineno, reason, rels)\n+                )\n     return violations\n \n \n@@ -129,8 +147,8 @@\n         self.assertEqual(\n             violations,\n             [],\n-            \"Every 'ADR ####' citation must resolve to exactly one \"\n-            f\"docs/adr/####-*.md:\\n{detail}\",\n+            \"Every ADR/SADR citation must resolve exactly once in its own \"\n+            f\"docs/adr namespace:\\n{detail}\",\n         )\n \n     def test_scan_actually_reads_scripts(self) -> None:\n@@ -146,8 +164,21 @@\n         # The two records this Goal carried onto the default branch, and which\n         # the scripts cite most, must be present.\n         index = _adr_index(REPO_ROOT)\n-        self.assertIn(\"0016\", index)\n-        self.assertIn(\"0017\", index)\n+        self.assertIn((\"SADR\", \"0016\"), index)\n+        self.assertIn((\"SADR\", \"0017\"), index)\n+\n+    def test_platform_tree_has_only_sadr_0001_through_0019(self) -> None:\n+        adr_dir = REPO_ROOT / \"docs\" / \"adr\"\n+        sadrs = sorted(adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\"))\n+        self.assertEqual(\n+            {path.name.split(\"-\", 2)[1] for path in sadrs},\n+            {f\"{number:04d}\" for number in range(1, 20)},\n+        )\n+        self.assertEqual(\n+            list(adr_dir.glob(\"[0-9][0-9][0-9][0-9]-*.md\")),\n+            [],\n+            \"the Shiki platform tree must not retain legacy numeric SADR paths\",\n+        )\n \n \n class FixtureAdrReferences(unittest.TestCase):\n@@ -180,6 +211,26 @@\n         self.assertIn(\"9991\", message)  # cited number\n         self.assertIn(\"docs/adr/9991-*.md\", message)  # missing path\n \n+    def test_target_adr_does_not_satisfy_missing_platform_sadr(self) -> None:\n+        root = self._make_repo(\n+            adr_names=[\"0042-target-decision.md\"],\n+            script_body='\"\"\"Target ADR 0042 exists, but platform SADR-0042 does not.\"\"\"\\n',\n+        )\n+        violations = find_adr_reference_violations(root, include_docs=False)\n+        self.assertEqual(len(violations), 1)\n+        self.assertEqual(violations[0].namespace, \"SADR\")\n+        self.assertEqual(violations[0].number, \"0042\")\n+        self.assertEqual(violations[0].expected_path, \"docs/adr/SADR-0042-*.md\")\n+\n+    def test_target_adr_and_platform_sadr_may_reuse_a_number(self) -> None:\n+        root = self._make_repo(\n+            adr_names=[\"0042-target-decision.md\", \"SADR-0042-platform-decision.md\"],\n+            script_body='\"\"\"Target ADR 0042 and platform SADR-0042 are distinct.\"\"\"\\n',\n+        )\n+        self.assertEqual(\n+            find_adr_reference_violations(root, include_docs=False), []\n+        )\n+\n     def test_uncited_adr_is_allowed(self) -> None:\n         # An ADR exists but nothing cites it -> not a violation. The converse\n         # (every ADR must be cited) is explicitly NOT required.\ndiff --git a/tests/test_guardian_review.py b/tests/test_guardian_review.py\nindex 88829e7810212635e2fc98aa5918d6f26294677d..00a85a237acd5bf2d6cb5b294128198c422402ca\n--- a/tests/test_guardian_review.py\n+++ b/tests/test_guardian_review.py\n@@ -162,7 +162,7 @@\n \n     def test_docs_only_is_light_and_not_conservative(self):\n         focus = gr.classify_review_focus([\"docs/adr/0099-x.md\", \"README.md\"], \"\", \"low\")\n-        self.assertTrue(any(\"Docs/ADR-only\" in f for f in focus))\n+        self.assertTrue(any(\"Docs/decision-record-only\" in f for f in focus))\n         self.assertFalse(any(\"conservative default\" in f.lower() for f in focus))\n \n     def test_unknown_category_is_conservative(self):\ndiff --git a/tests/test_policy_job_merged_prs.py b/tests/test_policy_job_merged_prs.py\nindex d45512e74681e5e649279236c7d4e79d5c3da471..b97989f8bd74d091de1a6b5275a20d8dc68c4512\n--- a/tests/test_policy_job_merged_prs.py\n+++ b/tests/test_policy_job_merged_prs.py\n@@ -3,8 +3,8 @@\n \n ``.github/workflows/shiki-cca-completion.yml`` has two consumers of the merged-PR\n proof: the CCA *signal* (``guardian_approval_signal.py`` in \"Prepare CCA context\")\n-and the authoritative gate (``mergegate_check.py`` in \"Enforce MergeGate\"). ADR\n-0017's bookkeeping-closeout exemption (condition 6) needs the merged implementation\n+and the authoritative gate (``mergegate_check.py`` in \"Enforce MergeGate\").\n+SADR-0017's bookkeeping-closeout exemption (condition 6) needs the merged implementation\n PR proven, and a closeout carries NO ``post_merge_reconcile`` marker. The signal\n already resolves ``MERGED_PRS`` unconditionally, but \"Enforce MergeGate\" used to\n resolve it only inside ``grep -q '<!-- shiki:post_merge_reconcile -->'``. So a\ndiff --git a/tests/test_sadr_install_migration.py b/tests/test_sadr_install_migration.py\nnew file mode 100644\nindex 0000000000000000000000000000000000000000..a6f4c57e4dcaef73ef600b0d4804744138ab9071\n--- /dev/null\n+++ b/tests/test_sadr_install_migration.py\n@@ -0,0 +1,229 @@\n+\"\"\"SADR installation and collision-safe legacy upgrade behavior.\"\"\"\n+\n+from __future__ import annotations\n+\n+import contextlib\n+import hashlib\n+import io\n+import json\n+import shutil\n+import tempfile\n+import unittest\n+from pathlib import Path\n+\n+import shiki_test_support  # noqa: F401  (path bootstrap)\n+\n+import shiki_installer\n+from tests.test_adr_references import find_adr_reference_violations\n+\n+\n+PRODUCT_NUMBERS = tuple(range(13, 19))\n+\n+\n+def _quiet_install(target: Path, *, force: bool, validate: bool) -> None:\n+    with contextlib.redirect_stdout(io.StringIO()):\n+        shiki_installer.install_template(target, force=force, validate=validate)\n+\n+\n+def _sha256(path: Path) -> str:\n+    return hashlib.sha256(path.read_bytes()).hexdigest()\n+\n+\n+def _legacy_sadr_paths() -> tuple[str, ...]:\n+    return tuple(\n+        f\"docs/adr/{path.name.removeprefix('SADR-')}\"\n+        for path in sorted((shiki_installer.ROOT / \"docs\" / \"adr\").glob(\"SADR-*.md\"))\n+        if path.name != \"SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\"\n+    )\n+\n+\n+def _create_legacy_upgrade_fixture(\n+    target: Path,\n+) -> tuple[dict[str, bytes], tuple[str, ...]]:\n+    adr_dir = target / \"docs\" / \"adr\"\n+    adr_dir.mkdir(parents=True)\n+    product_bytes = {\n+        f\"{number:04d}-cubeshare-product-decision.md\": (\n+            f\"# CubeShare ADR {number:04d}\\n\\nDo not rewrite {number}.\\n\"\n+        ).encode()\n+        for number in PRODUCT_NUMBERS\n+    }\n+    for name, content in product_bytes.items():\n+        (adr_dir / name).write_bytes(content)\n+\n+    legacy_paths = _legacy_sadr_paths()\n+    for relative in legacy_paths:\n+        (target / relative).write_bytes(f\"legacy Shiki bytes for {relative}\\n\".encode())\n+\n+    stamp_path = target / shiki_installer.INSTALL_STAMP_PATH\n+    stamp_path.parent.mkdir(parents=True, exist_ok=True)\n+    stamp = {\n+        \"version\": shiki_installer.INSTALL_STAMP_VERSION,\n+        \"platform_commit\": None,\n+        \"installed_at\": \"2000-01-01T00:00:00+00:00\",\n+        \"digests\": {\n+            relative: _sha256(target / relative) for relative in legacy_paths\n+        },\n+    }\n+    stamp_path.write_text(\n+        json.dumps(stamp, indent=2, sort_keys=True) + \"\\n\", encoding=\"utf-8\"\n+    )\n+    return product_bytes, legacy_paths\n+\n+\n+def _tree_snapshot(root: Path) -> dict[str, tuple[str, bytes | str]]:\n+    \"\"\"Recursive byte/shape snapshot used to prove preflight is write-free.\"\"\"\n+    snapshot: dict[str, tuple[str, bytes | str]] = {}\n+    for path in sorted(root.rglob(\"*\")):\n+        relative = path.relative_to(root).as_posix()\n+        if path.is_symlink():\n+            snapshot[relative] = (\"symlink\", path.readlink().as_posix())\n+        elif path.is_dir():\n+            snapshot[relative] = (\"directory\", \"\")\n+        else:\n+            snapshot[relative] = (\"file\", path.read_bytes())\n+    return snapshot\n+\n+\n+class FreshSadrInstallTests(unittest.TestCase):\n+    def setUp(self) -> None:\n+        self.tmp = Path(tempfile.mkdtemp(prefix=\"shiki-sadr-fresh-\"))\n+        self.addCleanup(lambda: shutil.rmtree(self.tmp, ignore_errors=True))\n+        self.target = self.tmp / \"target\"\n+        self.adr_dir = self.target / \"docs\" / \"adr\"\n+        self.adr_dir.mkdir(parents=True)\n+        self.product_bytes = {\n+            f\"{number:04d}-product-decision.md\": (\n+                f\"# Product ADR {number:04d}\\n\\nTarget-owned bytes {number}.\\n\"\n+            ).encode()\n+            for number in PRODUCT_NUMBERS\n+        }\n+        for name, content in self.product_bytes.items():\n+            (self.adr_dir / name).write_bytes(content)\n+\n+    def test_fresh_install_preserves_product_adrs_and_installs_all_sadrs(self) -> None:\n+        _quiet_install(self.target, force=False, validate=True)\n+\n+        for name, expected in self.product_bytes.items():\n+            self.assertEqual((self.adr_dir / name).read_bytes(), expected)\n+\n+        sadr_numbers = {\n+            path.name.split(\"-\", 2)[1]\n+            for path in self.adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\")\n+        }\n+        self.assertEqual(sadr_numbers, {f\"{number:04d}\" for number in range(1, 20)})\n+\n+        numeric_records = {\n+            path.name\n+            for path in self.adr_dir.glob(\"[0-9][0-9][0-9][0-9]-*.md\")\n+        }\n+        self.assertEqual(numeric_records, set(self.product_bytes))\n+        self.assertEqual(find_adr_reference_violations(self.target), [])\n+\n+\n+class LegacySadrUpgradeTests(unittest.TestCase):\n+    def setUp(self) -> None:\n+        self.tmp = Path(tempfile.mkdtemp(prefix=\"shiki-sadr-upgrade-\"))\n+        self.addCleanup(lambda: shutil.rmtree(self.tmp, ignore_errors=True))\n+        self.target = self.tmp / \"target\"\n+        self.adr_dir = self.target / \"docs\" / \"adr\"\n+        self.product_bytes, self.legacy_paths = _create_legacy_upgrade_fixture(\n+            self.target\n+        )\n+\n+    def test_force_upgrade_deletes_only_stamped_legacy_sadrs(self) -> None:\n+        _quiet_install(self.target, force=True, validate=True)\n+\n+        for relative in self.legacy_paths:\n+            self.assertFalse((self.target / relative).exists(), relative)\n+        for name, expected in self.product_bytes.items():\n+            self.assertEqual((self.adr_dir / name).read_bytes(), expected)\n+\n+        sadrs = list(self.adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\"))\n+        self.assertEqual(len(sadrs), 19)\n+        refreshed = json.loads(\n+            (self.target / shiki_installer.INSTALL_STAMP_PATH).read_text(encoding=\"utf-8\")\n+        )\n+        for relative in self.legacy_paths:\n+            self.assertNotIn(relative, refreshed[\"digests\"])\n+        for path in sadrs:\n+            relative = path.relative_to(self.target).as_posix()\n+            self.assertEqual(refreshed[\"digests\"][relative], _sha256(path))\n+\n+    def test_non_force_install_never_deletes_legacy_files(self) -> None:\n+        before = {\n+            relative: (self.target / relative).read_bytes()\n+            for relative in self.legacy_paths\n+        }\n+        _quiet_install(self.target, force=False, validate=False)\n+        for relative, expected in before.items():\n+            self.assertEqual((self.target / relative).read_bytes(), expected)\n+        refreshed = json.loads(\n+            (self.target / shiki_installer.INSTALL_STAMP_PATH).read_text(encoding=\"utf-8\")\n+        )\n+        for relative in self.legacy_paths:\n+            self.assertEqual(refreshed[\"digests\"][relative], _sha256(self.target / relative))\n+\n+\n+class LegacyCleanupPreflightTests(unittest.TestCase):\n+    def setUp(self) -> None:\n+        self.tmp = Path(tempfile.mkdtemp(prefix=\"shiki-sadr-preflight-\"))\n+        self.addCleanup(lambda: shutil.rmtree(self.tmp, ignore_errors=True))\n+\n+    def test_all_ownership_blockers_abort_before_any_write(self) -> None:\n+        cases = (\n+            (\"absent\", \"install stamp is absent\"),\n+            (\"unreadable\", \"install stamp is unreadable\"),\n+            (\"malformed\", \"install stamp is malformed\"),\n+            (\"missing-digest\", \"install stamp has no digest\"),\n+            (\"digest-mismatch\", \"digest mismatch\"),\n+        )\n+        for name, expected_reason in cases:\n+            with self.subTest(name=name):\n+                target = self.tmp / name\n+                _, legacy_paths = _create_legacy_upgrade_fixture(target)\n+                stamp_path = target / shiki_installer.INSTALL_STAMP_PATH\n+\n+                if name == \"absent\":\n+                    stamp_path.unlink()\n+                elif name == \"unreadable\":\n+                    stamp_path.unlink()\n+                    stamp_path.mkdir()\n+                elif name == \"malformed\":\n+                    stamp_path.write_text(\"{not-json\\n\", encoding=\"utf-8\")\n+                else:\n+                    stamp = json.loads(stamp_path.read_text(encoding=\"utf-8\"))\n+                    if name == \"missing-digest\":\n+                        stamp[\"digests\"] = {}\n+                    else:\n+                        stamp[\"digests\"] = {\n+                            relative: \"deadbeef\" for relative in legacy_paths\n+                        }\n+                    stamp_path.write_text(\n+                        json.dumps(stamp, indent=2, sort_keys=True) + \"\\n\",\n+                        encoding=\"utf-8\",\n+                    )\n+\n+                before = _tree_snapshot(target)\n+                with self.assertRaises(shiki_installer.ShikiError) as raised:\n+                    _quiet_install(target, force=True, validate=False)\n+                after = _tree_snapshot(target)\n+\n+                self.assertEqual(before, after, f\"{name} preflight changed the tree\")\n+                message = str(raised.exception)\n+                self.assertIn(expected_reason, message)\n+                self.assertIn(\"No files were written\", message)\n+                for relative in legacy_paths:\n+                    self.assertIn(relative, message)\n+\n+    def test_absent_stamp_is_allowed_when_no_legacy_path_exists(self) -> None:\n+        target = self.tmp / \"no-legacy\"\n+        target.mkdir()\n+        _quiet_install(target, force=True, validate=False)\n+        self.assertTrue(\n+            (target / \"docs\" / \"adr\" / \"SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\").is_file()\n+        )\n+\n+\n+if __name__ == \"__main__\":\n+    unittest.main()\n\napply patch\npatch: completed\n/private/tmp/shiki-adr-collision.pa0KwY/.worktrees/shiki-t-20260807t073307221254z-2d22d654-introduc/docs/adr/SADR-0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\n/private/tmp/shiki-adr-collision.pa0KwY/.worktrees/shiki-t-20260807t073307221254z-2d22d654-introduc/docs/agents/bootstrap-command.md\n/private/tmp/shiki-adr-collision.pa0KwY/.worktrees/shiki-t-20260807t073307221254z-2d22d654-introduc/docs/operations.md\n/private/tmp/shiki-adr-collision.pa0KwY/.worktrees/shiki-t-20260807t073307221254z-2d22d654-introduc/docs/releasing.md\n/private/tmp/shiki-adr-collision.pa0KwY/.worktrees/shiki-t-20260807t073307221254z-2d22d654-introduc/docs/usage.md\ndiff --git a/.codex/skills/shiki/SKILL.md b/.codex/skills/shiki/SKILL.md\nindex c8e2e85d9d75d49cc4f2d6286ee22659c4e53e75..93cded623b2c15d9f65c76491a1c2d813ac9e6c0\n--- a/.codex/skills/shiki/SKILL.md\n+++ b/.codex/skills/shiki/SKILL.md\n@@ -18,6 +18,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n Codex CLI does not register this skill as a custom `/shiki` slash command. When\n the user types `/shiki` in Codex CLI and gets \"Unrecognized command\", explain\n that this is expected: invoke Shiki in Codex with natural language such as\n@@ -69,7 +72,7 @@\n \n ## Responsibilities\n \n-- Claude Code implements and repairs by default (`shiki runner claude`, ADR 0008); Codex implements and repairs tasks explicitly assigned to `codex`.\n+- Claude Code implements and repairs by default (`shiki runner claude`, SADR-0008); Codex implements and repairs tasks explicitly assigned to `codex`.\n - Claude Code Action can act as GitHub-side CCA or reviewer.\n - CCA judges completion.\n - MergeGate authorizes state transitions and merge readiness.\ndiff --git a/.github/prompts/cca-completion-check.md b/.github/prompts/cca-completion-check.md\nindex ed203ad5f302ecc81372d8ab27d2b1232ca97497..fd89950528333ef6242e6d5443904f5392ab7e56\n--- a/.github/prompts/cca-completion-check.md\n+++ b/.github/prompts/cca-completion-check.md\n@@ -19,6 +19,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n ## Required Reading\n \n Read only the files needed to judge the PR contract. Prefer this order:\ndiff --git a/.github/prompts/codex-task-handoff.md b/.github/prompts/codex-task-handoff.md\nindex 695e211d76dea1c60e8d5bb360e78487808286a4..59441d576b01a39569722f226c854e5a740e9eb5\n--- a/.github/prompts/codex-task-handoff.md\n+++ b/.github/prompts/codex-task-handoff.md\n@@ -2,7 +2,7 @@\n \n You are Codex Front acting as the Implementer for a Shiki task.\n \n-Follow `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant ADRs, and `docs/agents/codex-handoff.md`.\n+Follow `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant Shiki SADRs and target ADRs, and `docs/agents/codex-handoff.md`.\n \n Rules:\n \ndiff --git a/AGENTS.md b/AGENTS.md\nindex 7f041100d49c2a7525f51cb0f5d696a4a6d1c78c..a06bfbabc6fa48a007389e25e12d7ed523936bed\n--- a/AGENTS.md\n+++ b/AGENTS.md\n@@ -22,7 +22,7 @@\n - **Codex Front** is an optional implementer through Codex App, Codex CLI, Codex IDE extension, or Codex Web signed in with ChatGPT OAuth/subscription auth, dispatched with `shiki runner codex` when a task is explicitly assigned to `codex`.\n - **GitHub CCA** is implemented by Claude Code Action by default, using `CLAUDE_CODE_OAUTH_TOKEN`.\n \n-Do not assume `openai/codex-action`, `OPENAI_API_KEY`, or API-key based Codex automation in the default Shiki path. API-key runners are explicit target-repo extensions and require their own ADR.\n+Do not assume `openai/codex-action`, `OPENAI_API_KEY`, or API-key based Codex automation in the default Shiki path. API-key runners are explicit target-repo extensions and require their own target ADR.\n \n Shiki is not:\n \n@@ -44,10 +44,14 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use\n+`SADR-NNNN-*.md`; target product decisions retain `NNNN-*.md` and the `ADR`\n+namespace.\n+\n Use the canonical source-of-truth order above. Runtime-specific reading orders\n are operational guidance only and must not override or reorder it.\n \n-Conversation state is not durable truth. If a decision matters, put it in GitHub, `.shiki/`, `CONTEXT.md`, or an ADR.\n+Conversation state is not durable truth. If a decision matters, put it in GitHub, `.shiki/`, `CONTEXT.md`, a Shiki SADR, or a target ADR.\n \n If GitHub and `.shiki/` disagree, surface the conflict and prefer GitHub operational state until the mirror is repaired.\n \n@@ -75,8 +79,8 @@\n Every non-trivial change follows this loop:\n \n 1. **Goal**: clarify outcome, completion conditions, non-goals, risk level, and success signals.\n-2. **grill-with-docs**: challenge the plan against domain docs, ADRs, code reality, terminology, and edge scenarios.\n-3. **Context & Impact**: find relevant docs, ADRs, code, dependencies, owners, locks, and verification surfaces.\n+2. **grill-with-docs**: challenge the plan against domain docs, Shiki SADRs, target ADRs, code reality, terminology, and edge scenarios.\n+3. **Context & Impact**: find relevant docs, SADRs, target ADRs, code, dependencies, owners, locks, and verification surfaces.\n 4. **PRD and Spec Freeze**: use `to-prd` when context is settled enough to become durable product/engineering intent. The operator's explicit approval of the PRD is Spec Freeze: the plan records a `spec_freeze` block, and no plan runs without it. Steps 1-4 form Requirements Definition \u2014 one continuous operator dialogue. After Spec Freeze, scope changes require an operator-approved Spec Amendment; non-scope-moving interpretations go to the Assumption Log.\n 5. **Issues**: use `to-issues` to create independently grabbable vertical-slice issues. Prefer AFK slices over HITL slices where possible.\n 6. **Triage**: label issues for readiness, risk, runtime, skills, and MergeGate state.\n@@ -167,7 +171,7 @@\n - Testing decisions.\n - Out-of-scope items.\n - Further notes.\n-- Links to relevant domain terms and ADRs.\n+- Links to relevant domain terms, SADRs, and target ADRs.\n \n Every executable task must include:\n \n@@ -193,7 +197,7 @@\n \n Context & Impact must identify:\n \n-- Relevant documents, domain terms, ADRs, and past decisions.\n+- Relevant documents, domain terms, SADRs, target ADRs, and past decisions.\n - Relevant modules, interfaces, seams, callers, owners, and tests.\n - Dependency relationships between tasks.\n - Candidate locks and lock conflicts.\n@@ -210,7 +214,7 @@\n | Skill | Required when |\n | --- | --- |\n | `setup-matt-pocock-skills` | First configuring the repo for skills, or when issue tracker, triage labels, or domain docs are missing. |\n-| `grill-with-docs` | Default for non-trivial Goals before PRD/issues; always required when plans, terminology, boundaries, tradeoffs, or ADR-worthy decisions are ambiguous. |\n+| `grill-with-docs` | Default for non-trivial Goals before PRD/issues; always required when plans, terminology, boundaries, tradeoffs, or decision-record-worthy choices are ambiguous. |\n | `zoom-out` | The agent lacks an architectural map of the relevant code area. |\n | `to-prd` | Settled Goal context must become durable product/engineering intent. |\n | `to-issues` | A Goal, PRD, or plan must become independently grabbable vertical-slice issues. |\n@@ -348,7 +352,7 @@\n - Three or more new files.\n - Any high-risk or critical label.\n \n-When the architecture gate triggers, run `grill-with-docs` or `improve-codebase-architecture` as appropriate and record durable decisions in an ADR when needed.\n+When the architecture gate triggers, run `grill-with-docs` or `improve-codebase-architecture` as appropriate and record durable decisions in a Shiki SADR or target ADR as applicable.\n \n ## Engineering Discipline\n \ndiff --git a/CLAUDE.md b/CLAUDE.md\nindex 98a932e7841a632866fcd53b0bad0e3af3711d3d..98a31399404d6fb044b4fd493f4459ac87f439de\n--- a/CLAUDE.md\n+++ b/CLAUDE.md\n@@ -14,6 +14,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n ## Claude Role\n \n Default to planner, default implementer, reviewer, coordinator, documentation editor, CCA-style completion judge, and final-judgment assistant.\n@@ -57,7 +60,7 @@\n At the start of a Shiki session:\n \n 1. Read `AGENTS.md`.\n-2. Read `CONTEXT.md` and relevant ADRs.\n+2. Read `CONTEXT.md` and relevant Shiki SADRs and target ADRs.\n 3. Read active `.shiki/` Goal/task/ledger state when present.\n 4. Read active distilled rules from `.shiki/memories/` (status `distilled`, `active: true`, not revoked or superseded) for applicable guidance; the same rules are injected deterministically into task handoffs as the `## Distilled Rules` section (\u00a73.5).\n 5. Read the GitHub Issue or PR when the task is GitHub-backed.\n@@ -74,7 +77,7 @@\n - Define the Goal, completion conditions, non-goals, and risk level.\n - Treat `grill-with-docs` as the default planning entry point for non-trivial Goals.\n - Challenge domain terms against `CONTEXT.md`.\n-- Challenge hard-to-reverse tradeoffs against ADRs.\n+- Challenge hard-to-reverse tradeoffs against SADRs and target ADRs.\n - Explore code instead of asking when code can answer the question.\n - Run Context & Impact before implementation planning.\n - Produce a Task DAG, not an unordered checklist.\n@@ -94,7 +97,7 @@\n - For each question, provide your recommended answer.\n - Resolve dependency decisions before downstream design questions.\n - Update `CONTEXT.md` inline when terminology crystallizes.\n-- Offer ADRs only when the decision is hard to reverse, surprising without context, and a real tradeoff.\n+- Offer a SADR or target ADR only when the decision is hard to reverse, surprising without context, and a real tradeoff.\n - Convert unresolved questions into HITL blockers rather than allowing Codex to guess.\n \n ## PRD and Issues Standard\n@@ -103,7 +106,7 @@\n \n - Use the project's domain glossary vocabulary.\n - Avoid volatile file paths unless they are essential.\n-- Include implementation decisions, testing decisions, out-of-scope items, and relevant ADR/domain links.\n+- Include implementation decisions, testing decisions, out-of-scope items, and relevant SADR/target-ADR/domain links.\n - Identify deep-module opportunities and testable interfaces.\n \n When creating issues:\n@@ -121,7 +124,7 @@\n - Branch/worktree target.\n - Scope and non-goals.\n - Dependencies and locks.\n-- Relevant docs, ADRs, modules, and tests.\n+- Relevant docs, SADRs, target ADRs, modules, and tests.\n - Required skill invocations.\n - TDD expectations.\n - Pre-PR `code-review` gate expectations.\n@@ -152,7 +155,7 @@\n \n - Treat yourself as a judge, not implementer.\n - Do not write production code.\n-- Read `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant ADRs, PRD, task issue, PR body, diff, checks, reviews, labels, and `.shiki/` state.\n+- Read `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant SADRs and target ADRs, PRD, task issue, PR body, diff, checks, reviews, labels, and `.shiki/` state.\n - Evaluate every applicable checklist in `docs/agents/checklists.md`.\n - Map each acceptance criterion to durable evidence.\n - Distinguish implementation failure from missing evidence.\ndiff --git a/CONTEXT.md b/CONTEXT.md\nindex e61a9be06a0cce0d026484083a657bde2067e234..237ee8aa3ea8fab334ece49768bc5ed3b5af0974\n--- a/CONTEXT.md\n+++ b/CONTEXT.md\n@@ -25,7 +25,7 @@\n _Avoid_: jumping directly into code\n \n **grill-with-docs**:\n-A planning skill that challenges a plan against domain language, ADRs, code reality, and concrete edge scenarios. It resolves design-tree questions before PRD/issues. It runs inside Requirements Definition.\n+A planning skill that challenges a plan against domain language, Shiki SADRs, target ADRs, code reality, and concrete edge scenarios. It resolves design-tree questions before PRD/issues. It runs inside Requirements Definition.\n _Avoid_: generic brainstorming, silent assumptions\n \n **Requirements Definition**:\ndiff --git a/CONTRIBUTING.md b/CONTRIBUTING.md\nindex c6d9b656e73abb8a55e5f660123e03153dcb3e6d..9e37a8fbf668db5b22461aaae5548b8f1354394c\n--- a/CONTRIBUTING.md\n+++ b/CONTRIBUTING.md\n@@ -17,7 +17,7 @@\n 1. **Goal** \u2014 start from a user-approved Goal with an outcome, completion\n    conditions, non-goals, risk level, and success signals.\n 2. **grill-with-docs** \u2014 for any non-trivial change, challenge the plan against\n-   domain language, ADRs, and code reality before writing a PRD.\n+   domain language, Shiki SADRs, target ADRs, and code reality before writing a PRD.\n 3. **PRD** \u2014 capture settled product/engineering intent (`to-prd`).\n 4. **Spec Freeze** \u2014 the operator explicitly approves the PRD/requirements;\n    the plan records a `spec_freeze` block and no plan runs without it\n@@ -94,7 +94,7 @@\n   entries.\n - Do not hand-edit `.shiki/goals/*.json` or `.shiki/manifest.json` in a task PR.\n - Conversation state is not durable truth. If a decision matters, record it in\n-  GitHub, `.shiki/`, `CONTEXT.md`, or an ADR.\n+  GitHub, `.shiki/`, `CONTEXT.md`, a Shiki SADR, or a target ADR.\n \n ## Architecture Gate\n \n@@ -103,7 +103,7 @@\n or CI policy, package structure, three or more structural files, more than 100\n net new lines in a core module, three or more new files, or any high-risk label.\n When triggered, run `grill-with-docs` or `improve-codebase-architecture` and\n-record durable decisions in an ADR when needed (see `docs/adr/`).\n+record durable decisions in a Shiki SADR or target ADR as applicable (see `docs/adr/`).\n \n ## Safety\n \ndiff --git a/SYSTEM_PROMPT.md b/SYSTEM_PROMPT.md\nindex aaaea45e3a896c3a44ba89eec8bb84325ecf6c62..da009c86a88285269dee0155e58e550c6d744ea9\n--- a/SYSTEM_PROMPT.md\n+++ b/SYSTEM_PROMPT.md\n@@ -30,6 +30,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n Use the canonical source-of-truth order above. Runtime-specific reading orders\n are operational guidance only and must not override or reorder it. The current\n conversation is non-durable operator input.\n@@ -48,14 +51,14 @@\n \n A runtime may only do work that is explicitly assigned by the Goal, task, PR, or MergeGate decision.\n \n-Do not assume `openai/codex-action`, `OPENAI_API_KEY`, or API-key based Codex automation in the default Shiki path. API-key runners are explicit target-repo extensions and require their own ADR.\n+Do not assume `openai/codex-action`, `OPENAI_API_KEY`, or API-key based Codex automation in the default Shiki path. API-key runners are explicit target-repo extensions and require their own target ADR.\n \n ## 4. Non-negotiable Shiki flow\n \n For any non-trivial Goal, use this flow:\n \n 1. **Goal Seek**: define outcome, non-goals, risk level, completion criteria, and evidence requirements.\n-2. **grill-with-docs**: challenge the plan against `CONTEXT.md`, ADRs, code reality, terminology, boundaries, and edge scenarios. Ask one question at a time when the operator is available. If code can answer a question, inspect code instead of asking.\n+2. **grill-with-docs**: challenge the plan against `CONTEXT.md`, Shiki SADRs, target ADRs, code reality, terminology, boundaries, and edge scenarios. Ask one question at a time when the operator is available. If code can answer a question, inspect code instead of asking.\n 3. **Context & Impact**: identify modules, interfaces, seams, callers, dependencies, locks, risk, and verification surfaces.\n 4. **to-prd + Spec Freeze**: turn settled context into a PRD using domain vocabulary and testing decisions. The operator's explicit approval of the PRD is Spec Freeze, recorded as the plan's `spec_freeze` block; plans without it do not run. Steps 1-4 form Requirements Definition.\n 5. **to-issues**: decompose the PRD into independently grabbable vertical-slice issues. Prefer AFK slices; mark HITL slices when judgment is still required.\n@@ -75,7 +78,7 @@\n Before making any material change:\n \n 1. Read `AGENTS.md` and any runtime-specific wrapper such as `CLAUDE.md`.\n-2. Read `CONTEXT.md` and relevant ADRs.\n+2. Read `CONTEXT.md` and relevant SADRs and target ADRs.\n 3. Read the GitHub Issue / PR and `.shiki/` state for the active Goal or task.\n 4. Identify your role: Planner, Implementer, Reviewer, Completion Check Agent, Repairer, or Guardian-assist.\n 5. Identify Goal id, task id, branch, PR, dependencies, locks, risk level, required skills, and required evidence.\n@@ -103,7 +106,7 @@\n \n At minimum, identify:\n \n-- Relevant documents, ADRs, domain terms, and prior decisions.\n+- Relevant documents, SADRs, target ADRs, domain terms, and prior decisions.\n - Relevant modules, interfaces, seams, callers, owners, and tests.\n - Dependency relationships between tasks.\n - Candidate file locks and lock conflicts.\n@@ -152,7 +155,7 @@\n \n CCA must read:\n \n-- `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant ADRs.\n+- `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant SADRs and target ADRs.\n - Parent Goal issue, PRD issue, task issue, comments, and labels.\n - PR body, diff, commits, changed files, reviews, CI checks, and artifacts available to the workflow.\n - `.shiki/` task contract, locks, ledger entries, CCA checklist profile, and prior repair packets.\n@@ -214,7 +217,7 @@\n - Three or more new files.\n - Any high-risk or critical label.\n \n-When the architecture gate triggers, run `grill-with-docs` or `improve-codebase-architecture` as appropriate and record the decision in an ADR if it is hard to reverse or likely to be re-litigated.\n+When the architecture gate triggers, run `grill-with-docs` or `improve-codebase-architecture` as appropriate and record the decision in a Shiki SADR or target ADR as applicable if it is hard to reverse or likely to be re-litigated.\n \n ## 14. Skill Gate\n \n@@ -223,7 +226,7 @@\n Use these skills:\n \n - `setup-matt-pocock-skills`: before first use of the engineering skill set, or when issue tracker, triage labels, or domain docs are missing.\n-- `grill-with-docs`: default for non-trivial Goals before PRD/issues; required for ambiguous plans, terminology, boundaries, ADR-worthy tradeoffs, or design-tree decisions.\n+- `grill-with-docs`: default for non-trivial Goals before PRD/issues; required for ambiguous plans, terminology, boundaries, decision-record-worthy tradeoffs, or design-tree decisions.\n - `zoom-out`: unfamiliar code area or missing architectural map.\n - `to-prd`: convert settled context into a PRD.\n - `to-issues`: break a PRD, plan, or Goal into independently grabbable vertical-slice issues.\ndiff --git a/docs/adr/0001-keep-shiki-as-platform-name.md b/docs/adr/SADR-0001-keep-shiki-as-platform-name.md\nindex 20a9637c3b9d8c97de7ba8db7cfae4d82d9a7dba..8b685439959aada1b221c112f0ac3c51ca8a1695\n--- a/docs/adr/0001-keep-shiki-as-platform-name.md\n+++ b/docs/adr/SADR-0001-keep-shiki-as-platform-name.md\n@@ -1,3 +1,3 @@\n-# Keep Shiki As The Platform Name\n+# SADR-0001: Keep Shiki As The Platform Name\n \n Shiki remains the product name for the GitHub-native agentic engineering control plane. We considered more descriptive names such as MergePilot, MergeGate, and Kiroku, but kept Shiki because it carries the existing goal-seeking framework identity while remaining independent of Hermes, Codex, Claude Code, or any single runtime.\ndiff --git a/docs/adr/0002-use-agents-as-shared-constitution.md b/docs/adr/SADR-0002-use-agents-as-shared-constitution.md\nindex 73de30ecb078120eac540d9bc51855e3cd48d0e3..c5486182bf06fca36ea508496b013c67302fd989\n--- a/docs/adr/0002-use-agents-as-shared-constitution.md\n+++ b/docs/adr/SADR-0002-use-agents-as-shared-constitution.md\n@@ -1,3 +1,3 @@\n-# Use AGENTS As The Shared Agent Constitution\n+# SADR-0002: Use AGENTS As The Shared Agent Constitution\n \n Shiki uses `AGENTS.md` as the runtime-neutral constitution and `CLAUDE.md` as a thin Claude Code wrapper. This keeps Codex, Claude Code, Hermes Runner, and future Agent Runtimes aligned on the same GitHub-first MergeGate rules while still allowing runtime-specific instructions where needed.\ndiff --git a/docs/adr/0003-require-skill-gate-for-mergegate-readiness.md b/docs/adr/SADR-0003-require-skill-gate-for-mergegate-readiness.md\nindex 424be0c9af4c8f0136f1fde68a468c47ff84e764..7fc337987c7df9792f75f181dfcbed75fe5ffff2\n--- a/docs/adr/0003-require-skill-gate-for-mergegate-readiness.md\n+++ b/docs/adr/SADR-0003-require-skill-gate-for-mergegate-readiness.md\n@@ -1,3 +1,3 @@\n-# Require Skill Gate For MergeGate Readiness\n+# SADR-0003: Require Skill Gate For MergeGate Readiness\n \n Shiki requires relevant engineering skills to be invoked before execution when their triggers apply. This makes skills part of MergeGate readiness rather than optional prompt style, because Shiki depends on repeatable planning, issue decomposition, TDD, diagnosis, triage, and architecture review before autonomous or parallel agent work can be trusted.\ndiff --git a/docs/adr/0004-use-github-first-with-shiki-mirror.md b/docs/adr/SADR-0004-use-github-first-with-shiki-mirror.md\nindex d0a3a9174dc646a945a9d325954ffaf00c352040..a79c54ae9ee8eed2a0524192b864da4e22a92d3f\n--- a/docs/adr/0004-use-github-first-with-shiki-mirror.md\n+++ b/docs/adr/SADR-0004-use-github-first-with-shiki-mirror.md\n@@ -1,4 +1,4 @@\n-# ADR 0004: Use GitHub First With A Repository-Local Shiki Mirror\n+# SADR-0004: Use GitHub First With A Repository-Local Shiki Mirror\n \n ## Status\n \ndiff --git a/docs/adr/0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md b/docs/adr/SADR-0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\nindex b339baf49b89d9290e79c3d51b36e7b2bde3bd95..0f3c3eb9afccdb3403ce978e21d04fe12e495da0\n--- a/docs/adr/0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\n+++ b/docs/adr/SADR-0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\n@@ -1,8 +1,8 @@\n-# ADR 0005: Use Codex As Front And Claude Code Action As The GitHub Runtime\n+# SADR-0005: Use Codex As Front And Claude Code Action As The GitHub Runtime\n \n ## Status\n \n-Accepted (implementer default superseded by ADR 0008; the auth model and the\n+Accepted (implementer default superseded by SADR-0008; the auth model and the\n Claude Code Action GitHub runtime decision remain in force)\n \n ## Context\n@@ -26,4 +26,4 @@\n - Shiki avoids requiring `OPENAI_API_KEY` for the default workflow.\n - Codex work happens through the user's authenticated Codex front surface, not as a hidden backend Action.\n - Claude Code Action is configured for OAuth-token based subscription usage by default.\n-- API-key based automation remains possible only as an explicit target-repo extension with its own ADR.\n+- API-key based automation remains possible only as an explicit target-repo extension with its own target ADR.\ndiff --git a/docs/adr/0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md b/docs/adr/SADR-0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\nindex 4bcae9d1e52f0a37b8e9fbafa5cd14b6ee1993bd..7a38538a6ae9929567f0ade67e7ea6a60d0b3f7e\n--- a/docs/adr/0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\n+++ b/docs/adr/SADR-0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\n@@ -1,4 +1,4 @@\n-# ADR 0006: Separate Codex Implementation, CCA Completion, And MergeGate Authorization\n+# SADR-0006: Separate Codex Implementation, CCA Completion, And MergeGate Authorization\n \n ## Status\n \ndiff --git a/docs/adr/0007-packaging-and-release.md b/docs/adr/SADR-0007-packaging-and-release.md\nindex 79b84ddb97167372b44641e5b7860e400ce80822..bb445a2e2e856d9d1f5fdbe35de8d4e8cec0fb32\n--- a/docs/adr/0007-packaging-and-release.md\n+++ b/docs/adr/SADR-0007-packaging-and-release.md\n@@ -1,4 +1,4 @@\n-# ADR 0007: Packaging And Release For Shiki\n+# SADR-0007: Packaging And Release For Shiki\n \n ## Status\n \n@@ -25,7 +25,7 @@\n   not introduce third-party runtime dependencies or break the shim.\n - Releases must run inside the operator's subscription-authenticated toolchain\n   and must not require extra secrets beyond the built-in `GITHUB_TOKEN`\n-  (consistent with ADR 0005's no-API-key-by-default stance).\n+  (consistent with SADR-0005's no-API-key-by-default stance).\n - `.shiki/manifest.json` already encodes the install surface (`install.include`,\n   `install.create_directories`, `install.exclude_from_commit`), and it must not\n   be mutated as part of this change.\n@@ -34,7 +34,7 @@\n \n This is a hard-to-reverse platform decision (it sets the version source of\n truth, the release trigger, and the public install contract), so it is recorded\n-as an ADR.\n+as a SADR.\n \n ## Decision\n \n@@ -104,7 +104,7 @@\n 3. **Publishing to PyPI.** Rejected for now: Shiki is distributed as a\n    repository template + git-installable CLI, not a PyPI library; PyPI would add\n    credentials/secrets and a different versioning contract. Can be added later as\n-   its own ADR if a packaged distribution is needed.\n+   its own SADR if a packaged distribution is needed.\n 4. **Dynamic version (read `VERSION` via a build backend `dynamic` field).**\n    Rejected to keep `pyproject.toml` backend-agnostic and dependency-free; the\n    small duplication between `VERSION` and `pyproject.toml` is guarded by the\n@@ -123,4 +123,4 @@\n   releasing versions the whole platform but ships only `install.include` to\n   targets.\n - Future PyPI distribution or a console entry point are deferred and would each\n-  warrant their own ADR.\n+  warrant their own SADR.\ndiff --git a/docs/adr/0008-use-claude-code-as-default-implementer.md b/docs/adr/SADR-0008-use-claude-code-as-default-implementer.md\nindex dedf4510b7c7799521422f622029563d373db031..35c2625d908167fbc58855b5b63e283805f167e8\n--- a/docs/adr/0008-use-claude-code-as-default-implementer.md\n+++ b/docs/adr/SADR-0008-use-claude-code-as-default-implementer.md\n@@ -1,12 +1,12 @@\n-# ADR 0008: Use Claude Code As The Default Implementer Runtime\n+# SADR-0008: Use Claude Code As The Default Implementer Runtime\n \n ## Status\n \n-Proposed (supersedes ADR 0005 for the implementer default; keeps its auth model)\n+Proposed (supersedes SADR-0005 for the implementer default; keeps its auth model)\n \n ## Context\n \n-ADR 0005 made Codex Front the default implementation runtime, with Claude Code\n+SADR-0005 made Codex Front the default implementation runtime, with Claude Code\n restricted to planner and reviewer roles by the runtime registry, and the only\n autonomous implementation adapter was `shiki runner codex`.\n \n@@ -18,13 +18,13 @@\n - The platform's planning, review, and CCA layers already run on Claude Code,\n   and Claude Code's native capabilities (the built-in `/code-review` skill and\n   the Workflow multi-agent orchestration tool) have no Codex equivalent.\n-- The Goal lifecycle redesign (ADR 0009) requires an autonomous post-freeze\n+- The Goal lifecycle redesign (SADR-0009) requires an autonomous post-freeze\n   loop driven from the operator's Claude Code session, including bounded\n   repair dispatch, which must not depend on a runtime that cannot be invoked\n   unattended.\n \n The constitution claims Shiki is runtime-agnostic (\"not a Claude-only\n-workflow\", ADR 0002). Any change to the implementer default must preserve the\n+workflow\", SADR-0002). Any change to the implementer default must preserve the\n registry-based runtime model rather than hardcoding one vendor.\n \n ## Decision\n@@ -39,7 +39,7 @@\n   the task to review or repair-needed.\n - Keep Codex registered as an optional implementer runtime. `shiki runner\n   codex`, the Codex handoff templates, and the ChatGPT OAuth auth model from\n-  ADR 0005 remain supported for tasks explicitly assigned to `codex`.\n+  SADR-0005 remain supported for tasks explicitly assigned to `codex`.\n - Commit to mandating two Claude-native capabilities as loop contract in\n   follow-up Goals (status: the code-review gate was delivered by Goal G-B \u2014\n   skill registry, checklists item PR-12, default required_skills \u2014 while the\n@@ -60,7 +60,7 @@\n \n Out of scope: removing Codex support, changing the CCA runtime (Claude Code\n Action with `CLAUDE_CODE_OAUTH_TOKEN` stays), and API-key based automation\n-(still requires its own ADR per ADR 0005).\n+(still requires its own target ADR per SADR-0005).\n \n ## Consequences\n \n@@ -100,7 +100,7 @@\n   harness rejection of direct `codex exec` is outside Shiki's control, and the\n   plan's mandatory native capabilities cannot run on Codex at all.\n - **Remove Codex entirely**: rejected; it abandons the runtime-agnostic\n-  constitution (ADR 0002) for no operational gain and forecloses\n+  constitution (SADR-0002) for no operational gain and forecloses\n   cross-runtime adversarial review.\n - **GitHub Actions cloud implementation via claude-code-action**: rejected as\n   default; durable evidence is automatic but token cost, Node 24 workflow\ndiff --git a/docs/adr/0009-requirements-definition-and-spec-freeze.md b/docs/adr/SADR-0009-requirements-definition-and-spec-freeze.md\nindex 29b3f7871bef9203a8b4a831a65c39eb2f506e42..52e7dd69ad0aa4efe0b68c62678abbf7df02aaec\n--- a/docs/adr/0009-requirements-definition-and-spec-freeze.md\n+++ b/docs/adr/SADR-0009-requirements-definition-and-spec-freeze.md\n@@ -1,4 +1,4 @@\n-# ADR 0009: Merge Grill And Planning Into Requirements Definition With Spec Freeze\n+# SADR-0009: Merge Grill And Planning Into Requirements Definition With Spec Freeze\n \n ## Status\n \ndiff --git a/docs/adr/0010-external-ai-guardian-review.md b/docs/adr/SADR-0010-external-ai-guardian-review.md\nindex e6108bbe8bb4e73e0ff4684c8f78001a80438c4e..f3510b145664298adafb151b6f0db51b972f01ec\n--- a/docs/adr/0010-external-ai-guardian-review.md\n+++ b/docs/adr/SADR-0010-external-ai-guardian-review.md\n@@ -1,4 +1,4 @@\n-# ADR 0010: External AI Guardian Review As A First-Class Approval Authority\n+# SADR-0010: External AI Guardian Review As A First-Class Approval Authority\n \n ## Status\n \n@@ -8,7 +8,7 @@\n \n Shiki is intended to be an autonomous, recursive, self-improving agent\n framework: authority-in-the-loop, not human-in-the-loop. Until now the Guardian\n-approval required for high/critical-risk merges (guardian-policy.json, ADR\n+approval required for high/critical-risk merges (guardian-policy.json, SADR\n governance) recognized only human authority sources \u2014 a GitHub review approval,\n the `guardian:approved` label applied by a configured Guardian, or a comment\n carrying `Guardian approval granted` plus the current head SHA.\n@@ -89,7 +89,7 @@\n   autonomous design the operator intends and produced the impersonation\n   incident.\n - **Record AI approval as an operator comment**: rejected as forgery of human\n-  authority \u2014 the exact failure this ADR corrects.\n+  authority \u2014 the exact failure this SADR corrects.\n - **Deliver the artifact as a committed `.shiki/` file**: rejected; a committed\n   approval inside the PR being approved is forgeable by anyone who can push.\n - **A dedicated reviewer bot identity instead of a relayed comment**: deferred;\ndiff --git a/docs/adr/0011-autonomous-quality-gate-evidence.md b/docs/adr/SADR-0011-autonomous-quality-gate-evidence.md\nindex bca0246c5d46e44afbde15486a75eec425fc02f5..9c93eac16d8c3a6d4b11bd7224e3955102cd7db4\n--- a/docs/adr/0011-autonomous-quality-gate-evidence.md\n+++ b/docs/adr/SADR-0011-autonomous-quality-gate-evidence.md\n@@ -1,4 +1,4 @@\n-# ADR 0011: Autonomous Quality-Gate Evidence \u2014 Deterministic Facts And An Independent Verifier\n+# SADR-0011: Autonomous Quality-Gate Evidence \u2014 Deterministic Facts And An Independent Verifier\n \n ## Status\n \ndiff --git a/docs/adr/0012-autonomous-loop-closeout-to-main.md b/docs/adr/SADR-0012-autonomous-loop-closeout-to-main.md\nindex 637fad6dde4989c9c2bf0f81f676d8d29991123c..ec7d85f81030a676bcbba28b43eb365422a36eac\n--- a/docs/adr/0012-autonomous-loop-closeout-to-main.md\n+++ b/docs/adr/SADR-0012-autonomous-loop-closeout-to-main.md\n@@ -1,4 +1,4 @@\n-# 0012 \u2014 Autonomous loop pushes goal completion to main via a closeout PR\n+# SADR-0012 \u2014 Autonomous loop pushes goal completion to main via a closeout PR\n \n - Status: accepted\n - Date: 2026-06-18\ndiff --git a/docs/adr/0013-bootstrap-grants-actions-approve-pull-requests.md b/docs/adr/SADR-0013-bootstrap-grants-actions-approve-pull-requests.md\nindex b6c69d64f1c5ced346b88d6859586715b3b5bd0e..cf48ff4a6716267f539ca58edd1a852e7c7a2d64\n--- a/docs/adr/0013-bootstrap-grants-actions-approve-pull-requests.md\n+++ b/docs/adr/SADR-0013-bootstrap-grants-actions-approve-pull-requests.md\n@@ -1,4 +1,4 @@\n-# ADR 0013: Bootstrap Grants GitHub Actions Permission To Create And Approve Pull Requests\n+# SADR-0013: Bootstrap Grants GitHub Actions Permission To Create And Approve Pull Requests\n \n ## Status\n \n@@ -31,7 +31,7 @@\n security tradeoff: it lets repository automation satisfy a human-review gate. In\n a multi-maintainer repo that could let a compromised or buggy workflow\n self-approve changes. So whether bootstrap turns it on by default \u2014 and with\n-what default token scope \u2014 is an ADR-worthy decision, not an implementation\n+what default token scope \u2014 is a SADR-worthy decision, not an implementation\n detail.\n \n ## Decision\n@@ -48,7 +48,7 @@\n Specifically:\n \n - **`can_approve_pull_request_reviews` defaults to `true`.** Shiki's default\n-  posture is solo/AFK autonomous operation (ADR 0008/0009/0012), where the CCA\n+  posture is solo/AFK autonomous operation (SADR-0008/0009/0012), where the CCA\n   Review Bridge is the only path that can satisfy `required_review: true` after\n   the CCA and Guardian gates have already passed. The default makes a\n   freshly-bootstrapped repo able to run the autonomous loop end to end without a\ndiff --git a/docs/adr/0014-codex-app-external-ai-guardian-ui-adapter.md b/docs/adr/SADR-0014-codex-app-external-ai-guardian-ui-adapter.md\nindex 238db06279334caa341c117362df9271382a7fcd..65f12e9939321e1b3e389a34c822fc1549e5155d\n--- a/docs/adr/0014-codex-app-external-ai-guardian-ui-adapter.md\n+++ b/docs/adr/SADR-0014-codex-app-external-ai-guardian-ui-adapter.md\n@@ -1,4 +1,4 @@\n-# ADR 0014: Use Codex App As The External AI Guardian UI Adapter\n+# SADR-0014: Use Codex App As The External AI Guardian UI Adapter\n \n ## Status\n \ndiff --git a/docs/adr/0015-contract-approval-before-dispatch.md b/docs/adr/SADR-0015-contract-approval-before-dispatch.md\nindex fad6c656741a20a7684ba16080a7db159f63a52f..6061271a46d3f1d6ae7174477de3fb75673123a6\n--- a/docs/adr/0015-contract-approval-before-dispatch.md\n+++ b/docs/adr/SADR-0015-contract-approval-before-dispatch.md\n@@ -1,4 +1,4 @@\n-# ADR 0015: Approve The Task Contract Before Dispatch, Not The Diff After It\n+# SADR-0015: Approve The Task Contract Before Dispatch, Not The Diff After It\n \n ## Status\n \n@@ -9,7 +9,7 @@\n Guardian approval for high/critical risk is expressible only on a pull request.\n Every approval source in `guardian-policy.json` \u2014 GitHub review, the\n `guardian:approved` label, the `Guardian approval granted` comment, and the\n-`external_ai_guardian_review` artifact (ADR 0010/0014) \u2014 is bound to a PR, and\n+`external_ai_guardian_review` artifact (SADR-0010/0014) \u2014 is bound to a PR, and\n three of the four require the current head SHA. Approval therefore cannot exist\n before an implementation PR exists.\n \n@@ -81,7 +81,7 @@\n An envelope breach must surface only on the non-repairable policy gate, never on\n the repairable `MergeGate metadata check`. Otherwise the Goal Loop routes it to\n `dispatch_repair` \u2014 instructing a runner to make the breach go away \u2014 which is\n-exactly what this ADR forbids.\n+exactly what this SADR forbids.\n \n **Contract immutability.** After registration, an implementation PR must not\n change the governance fields of its own task contract (`scope`, `non_goals`,\n@@ -116,7 +116,7 @@\n fails any verdict that is not `complete`, and the signal the CCA prompt reads is\n computed from `evaluate_guardian_approval` alone. Without the OR-merge at the\n signal, a contract-approved PR still returns `needs_guardian`, the CCA job fails,\n-and the MergeGate policy check never runs \u2014 the gate this ADR changes would never\n+and the MergeGate policy check never runs \u2014 the gate this SADR changes would never\n be reached. The CCA *prompt* needs no change: a `contract_approval` entry in the\n signal's existing `sources` array satisfies its current wording.\n \n@@ -130,7 +130,7 @@\n written, and an approved contract lets its implementation PRs merge through the\n same automatic path as low/medium risk. The wasted implement-then-block cycle\n disappears. Guardian review shifts from reading diffs to reading contracts, which\n-is both earlier and smaller; the external AI Guardian path (ADR 0010/0014) works\n+is both earlier and smaller; the external AI Guardian path (SADR-0010/0014) works\n on contracts with no change.\n \n We accept that the Guardian authorizes a class of change without seeing its\n@@ -146,7 +146,7 @@\n adversarial tests for the new approval path equivalent in rigor to\n `test_shiki_governance_evidence.sh`.\n \n-Bootstrap costs, which apply only while this ADR is being implemented. The tasks\n+Bootstrap costs, which apply only while this SADR is being implemented. The tasks\n that build Contract Approval merge under the existing rules, so the ones\n dispatched after the pre-dispatch gate lands but before the approval path lands\n need a Guardian-approved Contract PR *and* a live Guardian approval on their\n@@ -210,7 +210,7 @@\n risk; they do not bound authorized work.\n \n **Auto-escalate risk on envelope breach instead of stopping.** Returns the\n-workflow to a diff-time Guardian decision \u2014 the exact behavior this ADR removes.\n+workflow to a diff-time Guardian decision \u2014 the exact behavior this SADR removes.\n \n **Resolve governance fields from the base snapshot only.** Rejected against the\n registration flow: implementation PRs add their own contract, so the base has no\ndiff --git a/docs/adr/0016-locks-bound-product-paths.md b/docs/adr/SADR-0016-locks-bound-product-paths.md\nindex b5e850f3f1f7f4ef6e7f9d1763a29a51bd300c45..46b40a9b99a7aae644c02bb458f5473ff0a90ce2\n--- a/docs/adr/0016-locks-bound-product-paths.md\n+++ b/docs/adr/SADR-0016-locks-bound-product-paths.md\n@@ -1,4 +1,4 @@\n-# ADR 0016: Locks bound product paths; the Shiki mirror is bound by record identity\n+# SADR-0016: Locks bound product paths; the Shiki mirror is bound by record identity\n \n ## Status\n \n@@ -6,8 +6,8 @@\n \n - Date: 2026-07-28\n - Deciders: operator (mizutani-140), Claude (planner)\n-- Related: ADR 0004 (GitHub-first with a `.shiki` mirror), ADR 0009 (Spec Freeze),\n-  ADR 0012 (autonomous loop closeout to main), ADR 0015 (Contract Approval before\n+- Related: SADR-0004 (GitHub-first with a `.shiki` mirror), SADR-0009 (Spec Freeze),\n+  SADR-0012 (autonomous loop closeout to main), SADR-0015 (Contract Approval before\n   dispatch), remediation goal task R-02, the live PR #179 deadlock.\n \n ## Context\n@@ -90,11 +90,11 @@\n files, documentation files and named source modules are nameable; runtime-\n allocated ledger, runner and report ids are not \u2014 and those live in `.shiki`,\n which locks no longer govern. Existing frozen contracts are brought into line by\n-a one-time Spec Amendment per plan, which must land before ADR 0015's contract\n+a one-time Spec Amendment per plan, which must land before SADR-0015's contract\n immutability makes `locks` unwritable from the implementation PR.\n \n **Lock state is resolved from the durable side.** MergeGate resolves the lock\n-registry, and ADR 0015's governance fields, from the base `.shiki` snapshot when\n+registry, and SADR-0015's governance fields, from the base `.shiki` snapshot when\n present, falling back to head only when absent. The `MergeGate metadata check`\n job is given `--base-shiki`, which it does not receive today. Locks are not\n derived from the live open-pull-request set: a pull request body is untrusted\n@@ -108,7 +108,7 @@\n Explicitly out of scope: moving MergeGate's contract resolution off the PR\n checkout wholesale; making the mirror a projection of GitHub state; introducing\n new lock kinds; moving ledger evidence out of the repository; and unwinding\n-ADR 0012's closeout pull request. Registration of multi-task goals is ADR 0015's\n+SADR-0012's closeout pull request. Registration of multi-task goals is SADR-0015's\n Contract PR and is not re-decided here.\n \n ## Consequences\n@@ -127,7 +127,7 @@\n \n Risks accepted. First, goal-scoped identity is only as strong as `goal_id`,\n which is resolved from the PR body and the head task file; the mitigation is to\n-add `goal_id` to ADR 0015's frozen governance field set, and until that lands a\n+add `goal_id` to SADR-0015's frozen governance field set, and until that lands a\n task file can name a foreign goal. Second, base-when-present resolution is\n unavailable on fork pull requests, where the head is contributor-controlled;\n fork pull requests already cannot complete a Shiki merge, and this decision does\n@@ -139,7 +139,7 @@\n path, not by implementation pull requests.\n \n New obligations: one Spec Amendment per frozen plan to rewrite `locks`, ordered\n-before ADR 0015 T1; extension of the adversarial suites in\n+before SADR-0015 T1; extension of the adversarial suites in\n `scripts/test_shiki_adversarial_state.sh` and\n `scripts/test_shiki_governance_evidence.sh` to cover the eleven newly-scoped\n directories; and updating `scripts/test_shiki_loop_lock_guard.sh`,\n@@ -151,32 +151,32 @@\n whether the loop's transitive sync of ledger `evidence` references can be\n deleted once the Contract PR carries its own source plan.\n \n-## Relationship to ADR 0004\n+## Relationship to SADR-0004\n \n-This ADR **clarifies something ADR 0004 left unspecified**. ADR 0004 decides that\n+This SADR **clarifies something SADR-0004 left unspecified**. SADR-0004 decides that\n GitHub is the operational source of truth and that each target repository keeps a\n `.shiki/` mirror. It says nothing about pull requests writing the mirror, does\n not name MergeGate, and does not make the mirror an input to a gate. Nothing in\n-ADR 0004's Decision or Consequences is amended, superseded or weakened: the\n+SADR-0004's Decision or Consequences is amended, superseded or weakened: the\n mirror still exists, still records the same artifacts, and pull request\n-boundaries remain the coordination surface. What this ADR settles is the question\n+boundaries remain the coordination surface. What this SADR settles is the question\n 0004 never reached \u2014 by what authority a pull request may write a mirror record \u2014\n and it answers it the way `.shiki/manifest.json` already declares\n (`pr_mutation: current-task-or-current-goal-only`).\n \n-It **amends ADR 0012**, whose Decision item 5 states that \"Loop-executed tasks\n+It **amends SADR-0012**, whose Decision item 5 states that \"Loop-executed tasks\n declare the `path:.shiki/**` lock, which covers every `.shiki` file the closeout\n stages ... so `files_outside_locks` is satisfied without per-file lock\n bookkeeping.\" That clause is replaced: the closeout's mirror writes are\n-authorized by record identity, not by a declared lock. ADR 0012's substantive\n+authorized by record identity, not by a declared lock. SADR-0012's substantive\n decision \u2014 that completion reaches `main` through a closeout pull request rather\n than local attestation \u2014 is preserved unchanged.\n \n-It is **complementary to ADR 0015**, not a substitute. ADR 0015 owns contract\n+It is **complementary to SADR-0015**, not a substitute. SADR-0015 owns contract\n immutability, the pre-dispatch gate, the Contract PR and the Scope Envelope. This\n-ADR owns what a lock means and who may write the mirror. The one ordering\n+SADR owns what a lock means and who may write the mirror. The one ordering\n constraint between them is that the lock-narrowing Spec Amendment must precede\n-ADR 0015 T1's freezing of the `locks` field.\n+SADR-0015 T1's freezing of the `locks` field.\n \n ## Alternatives Considered\n \n@@ -186,7 +186,7 @@\n the only writer that can register them may run. Migrating the Guardian signal to\n that store makes every registration PR fail closed to `needs_guardian` at every\n risk level, because the task file is by definition absent from the store, which\n-removes the low/medium autonomous path ADR 0008/0009 exists to provide. And it\n+removes the low/medium autonomous path SADR-0008/0009 exists to provide. And it\n leaves schemas, manifest, config, guardian policy, templates, migrations, plans,\n worktrees, runner records, runs and handoffs with no writer at all in a\n repository whose product is `.shiki`. Measured benefit on the frozen backlog:\n@@ -199,14 +199,14 @@\n namespace is not in fact ownership-partitioned \u2014 56 ledger entries carry no\n `task_id`, and one goal DAG is writable by 35 registered tasks. The correct\n expression of its insight is that locks do not govern the mirror at all, which is\n-what this ADR decides.\n+what this SADR decides.\n \n **Make the mirror a projection written only by a registration writer and a\n post-merge projection job.** Rejected. It requires a bypass actor with write\n access to the protected default branch; it makes its own master invariant\n readable from a manifest that pull requests may edit; it deletes both reconcile\n modes and leaves a corrupted mirror with no in-band repair path, contradicting\n-ADR 0004's consequence that mirror conflicts are repaired against GitHub state;\n+SADR-0004's consequence that mirror conflicts are repaired against GitHub state;\n and its content-derived ids are rejected by `validate_shiki`'s currently-green id\n pattern.\n \ndiff --git a/docs/adr/0017-bookkeeping-closeouts-do-not-inherit-risk.md b/docs/adr/SADR-0017-bookkeeping-closeouts-do-not-inherit-risk.md\nindex 554e3a0d526a72ca067108c5464cc68c3edcf174..b9676857a4713850e063af7c39c8e0b01ecb4c66\n--- a/docs/adr/0017-bookkeeping-closeouts-do-not-inherit-risk.md\n+++ b/docs/adr/SADR-0017-bookkeeping-closeouts-do-not-inherit-risk.md\n@@ -1,4 +1,4 @@\n-# ADR 0017: A Bookkeeping Closeout Does Not Inherit Its Task's Risk\n+# SADR-0017: A Bookkeeping Closeout Does Not Inherit Its Task's Risk\n \n ## Status\n \n@@ -6,7 +6,7 @@\n \n ## Context\n \n-ADR 0012 made the goal loop push a task's completion to the default branch through a\n+SADR-0012 made the goal loop push a task's completion to the default branch through a\n closeout pull request: after the implementation PR merges, the loop opens a second PR\n carrying `task.status = done`, `lock.state = released`, and \u2014 when the task completes\n its goal \u2014 `goal.status = complete` plus one scorecard report. Nothing else. The\n@@ -49,14 +49,14 @@\n 4. The task file's governance fields (`scope`, `non_goals`, `required_skills`,\n    `risk_level`, `locks`, `acceptance_checks`, `test_command`) are byte-identical to\n    the base snapshot. Only `status`, `expected_pr`, `closeout_pr`, `ledger_evidence`\n-   and `expected_branch` may differ \u2014 the ADR 0015 contract-immutability rule,\n+   and `expected_branch` may differ \u2014 the SADR-0015 contract-immutability rule,\n    unchanged.\n \n    `expected_branch` is in that set because a closeout is *by construction* on a\n    different branch from the implementation it closes, and the loop rewrites the\n    field accordingly. It is safe to let it differ because MergeGate independently\n    requires `expected_branch` to equal the PR's actual head ref, so a PR cannot set\n-   it to anything other than the branch it is really on. The first draft of this ADR\n+   it to anything other than the branch it is really on. The first draft of this SADR\n    omitted it, which made the exemption unreachable: every real closeout failed\n    condition 4 on a field the loop always changes. See PR #209.\n 5. The state transitions are exactly the terminal set: task `review` \u2192 `done`, lock\n@@ -113,12 +113,12 @@\n classification must be derived, not asserted.\n \n **Fold the closeout into the implementation PR** so no second PR exists. Rejected under\n-ADR 0012, which recorded why: a task file that is already `done` on the implementation\n+SADR-0012, which recorded why: a task file that is already `done` on the implementation\n branch short-circuits the loop's review \u2192 CCA \u2192 merge \u2192 done ordering into a local\n goal-completion before any merge happens.\n \n **Have the loop auto-merge closeouts with elevated credentials.** Rejected outright: that\n-is a runtime authoring its own approval, which ADR 0010 and the constitution's\n+is a runtime authoring its own approval, which SADR-0010 and the constitution's\n approval-record prohibition forbid. It would also be indistinguishable, in the evidence,\n from the forgery those rules exist to prevent.\n \ndiff --git a/docs/adr/0018-carried-guardian-approval-across-a-proven-base-sync.md b/docs/adr/SADR-0018-carried-guardian-approval-across-a-proven-base-sync.md\nindex 22358562da390ed8b45cd600da08a43d378b4680..e6e2907cefa776460af73ddf7d39db605f3c39b0\n--- a/docs/adr/0018-carried-guardian-approval-across-a-proven-base-sync.md\n+++ b/docs/adr/SADR-0018-carried-guardian-approval-across-a-proven-base-sync.md\n@@ -1,4 +1,4 @@\n-# ADR 0018: Carried Guardian Approval Across A Proven Base Sync\n+# SADR-0018: Carried Guardian Approval Across A Proven Base Sync\n \n ## Status\n \n@@ -34,7 +34,7 @@\n Guardian evaluator that consumes it (`shiki_guardian.evaluate_guardian_approval`,\n the `guardian_comment_carried` source) were built and tested in isolation. Neither\n does anything until a caller passes the flags that turn the carry on, so the\n-feature was invisible in CI. This ADR records the decision the two of them\n+feature was invisible in CI. This SADR records the decision the two of them\n implement, and this task wires it into the four CI invocations that judge Guardian\n approval.\n \ndiff --git a/docs/adr/README.md b/docs/adr/README.md\nindex c70e057a4520c36e042ab68c53cd4a21ef709b71..5da43856836e2ce6a150f8127766e2f88355ab4b\n--- a/docs/adr/README.md\n+++ b/docs/adr/README.md\n@@ -1,40 +1,41 @@\n-# Architecture Decision Records\n+# Decision Record Namespaces\n \n-This directory holds Shiki's Architecture Decision Records (ADRs): durable\n-records of hard-to-reverse platform decisions. ADRs are part of the Shiki source\n-of truth alongside GitHub state, the `.shiki/` mirror, and `CONTEXT.md`.\n+This directory holds two independent decision namespaces:\n \n-## How ADRs Work\n+- Shiki Architecture Decision Records (SADRs), named\n+  `SADR-NNNN-<short-slug>.md`, record hard-to-reverse Shiki platform decisions.\n+- Target Architecture Decision Records (ADRs), named\n+  `NNNN-<short-slug>.md`, belong to the target product.\n \n-- New ADRs are added as `docs/adr/NNNN-<short-slug>.md`, where `NNNN` is the next\n-  zero-padded sequence number.\n-- The **canonical list of ADRs is the set of files matching `docs/adr/*.md`** in\n-  this directory. Browse `docs/adr/*.md` for the authoritative, current set.\n-- Use [`template.md`](template.md) as the starting point for a new ADR.\n-- Record an ADR when a decision is hard to reverse, surprising without context,\n-  and a real tradeoff. Do not write an ADR for routine or easily reversible\n-  choices.\n+The same four-digit number may appear once in each namespace. A platform\n+`SADR-NNNN` citation resolves only to `SADR-NNNN-*.md`; a target `ADR NNNN`\n+citation resolves only to `NNNN-*.md`.\n \n-Using the `docs/adr/*.md` glob as the source of truth means other PRs can add an\n-ADR without editing this index file, avoiding merge contention on a\n-hand-maintained enumeration.\n+## How Decision Records Work\n \n-## Current ADRs (snapshot)\n-\n-The list below is a convenience snapshot and may lag behind the directory. When\n-in doubt, list `docs/adr/*.md`.\n+- New Shiki platform decisions use the next zero-padded SADR number.\n+- New target product decisions use the target's own ADR sequence.\n+- The canonical list is the files in `docs/adr/`; no hand-maintained index is\n+  authoritative.\n+- Use [`template.md`](template.md) as the starting point and choose the filename\n+  namespace that owns the decision.\n+- Record a decision when it is hard to reverse, surprising without context, and\n+  a real tradeoff. Do not write a decision record for routine or easily\n+  reversible choices.\n \n-- [0001 \u2014 Keep Shiki As The Platform Name](0001-keep-shiki-as-platform-name.md)\n-- [0002 \u2014 Use AGENTS As The Shared Agent Constitution](0002-use-agents-as-shared-constitution.md)\n-- [0003 \u2014 Require Skill Gate For MergeGate Readiness](0003-require-skill-gate-for-mergegate-readiness.md)\n-- [0004 \u2014 Use GitHub First With A Repository-Local Shiki Mirror](0004-use-github-first-with-shiki-mirror.md)\n-- [0005 \u2014 Use Codex As Front And Claude Code Action As The GitHub Runtime](0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md)\n-- [0006 \u2014 Separate Codex Implementation, CCA Completion, And MergeGate Authorization](0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md)\n+Using the directory as the source of truth lets independent changes add records\n+without contending on an enumerated index.\n \n-## Adding A New ADR\n+## Adding A Shiki SADR\n \n-1. Copy `template.md` to `docs/adr/NNNN-<short-slug>.md`.\n+1. Copy `template.md` to `docs/adr/SADR-NNNN-<short-slug>.md`.\n 2. Fill in the title, status, context, decision, and consequences.\n-3. Set the status to `Proposed`, then `Accepted` once the decision is approved.\n-4. Open a PR following `CONTRIBUTING.md`. You do not need to edit this index for\n-   the ADR to be discoverable via `docs/adr/*.md`.\n+3. Set the status to `Proposed`, then `Accepted` once the proper authority has\n+   approved the decision.\n+4. Open a PR following `CONTRIBUTING.md`.\n+\n+## Adding A Target ADR\n+\n+1. Copy `template.md` to `docs/adr/NNNN-<short-slug>.md` using the target's own\n+   sequence.\n+2. Cite it as `ADR NNNN`; do not prefix its filename with `SADR-`.\ndiff --git a/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md b/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\nnew file mode 100644\nindex 0000000000000000000000000000000000000000..7455ce1f3bed02ddddb851ac21a1d1a648c9aeba\n--- /dev/null\n+++ b/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\n@@ -0,0 +1,55 @@\n+# SADR-0019: Separate Shiki Platform SADRs From Target ADRs\n+\n+## Status\n+\n+Accepted\n+\n+## Context\n+\n+Shiki installs its platform decision records into `docs/adr/` in every target\n+repository. Shiki and its targets previously used the same numeric\n+`NNNN-<slug>.md` namespace. A target that already owned ADRs 0013 through 0018\n+therefore acquired two records for each number during installation, making an\n+`ADR NNNN` citation ambiguous. Renumbering target decisions would rewrite target\n+history, while renumbering Shiki decisions per target would destroy their stable\n+platform identity.\n+\n+Upgrades also need to remove the former Shiki-owned numeric paths without ever\n+mistaking a target-owned ADR for platform content. The install stamp already\n+records exact shipped paths and content digests, so it can prove ownership only\n+when both still match.\n+\n+## Decision\n+\n+Shiki platform decisions use the `SADR` namespace:\n+\n+- records are named `docs/adr/SADR-NNNN-<slug>.md`;\n+- live platform citations use `SADR-NNNN`;\n+- target product decisions retain `docs/adr/NNNN-<slug>.md` and `ADR NNNN`;\n+- the reference validator resolves the two namespaces independently, so the same\n+  four-digit number may exist once in each namespace.\n+\n+Before a forced upgrade writes anything, the installer inventories the exact\n+former numeric Shiki ADR paths. It may delete a legacy path only when the\n+existing install stamp contains that exact path and its recorded SHA-256 digest\n+matches the file currently on disk. All ownership blockers are reported\n+together and abort the upgrade before its first write. A non-forced install\n+never performs this cleanup.\n+\n+Historical `.shiki/` evidence remains unchanged because it records the namespace\n+that existed when that evidence was produced.\n+\n+## Consequences\n+\n+- Targets may use any ADR number without colliding with Shiki's platform SADRs.\n+- Fresh installs preserve target ADR bytes and install a stable SADR set.\n+- Forced upgrades fail closed when legacy ownership cannot be proven, and a\n+  successful upgrade refreshes the stamp without removed legacy paths.\n+- Current governance, packaging, workflow, prompt, script, and skill references\n+  must move with the platform records.\n+\n+## Alternatives Considered\n+\n+- Collision-only refusal leaves affected targets permanently unupgradable.\n+- Renumbering target ADRs mutates target-owned history.\n+- Per-target Shiki renumbering makes platform citations non-portable.\ndiff --git a/docs/adr/template.md b/docs/adr/template.md\nindex a92a97daddd0b00ab9dd626cc8d86942da2eafed..17dd79175b5fee2105935557f691b40cf9492942\n--- a/docs/adr/template.md\n+++ b/docs/adr/template.md\n@@ -1,14 +1,14 @@\n-# NNNN: <Short Decision Title>\n+# <SADR-NNNN or NNNN>: <Short Decision Title>\n \n ## Status\n \n-Proposed | Accepted | Superseded by ADR-NNNN\n+Proposed | Accepted | Superseded by SADR-NNNN or target ADR NNNN\n \n ## Context\n \n What is the situation, constraint, or force that requires a decision? Reference\n-relevant domain terms (`CONTEXT.md`), prior ADRs, and code reality. State the\n-problem, not the solution.\n+relevant domain terms (`CONTEXT.md`), prior Shiki SADRs or target ADRs, and code\n+reality. State the problem, not the solution.\n \n ## Decision\n \ndiff --git a/docs/agents/autonomous-loop-e2e-acceptance.md b/docs/agents/autonomous-loop-e2e-acceptance.md\nindex bbf84c6081987f159ac6ad9ecf3c82b7aa72e7e8..5c13c1d02a05a90b0dc46cbef7c95a9846d0c237\n--- a/docs/agents/autonomous-loop-e2e-acceptance.md\n+++ b/docs/agents/autonomous-loop-e2e-acceptance.md\n@@ -40,8 +40,8 @@\n 3. `gh auth status` is logged in with merge permission on the repo.\n 4. Branch protection + required checks are configured exactly as the goal-loop\n    required-check set expects (see `docs/agents/branch-protection-smoke.md`).\n-5. The repo `.shiki/policy` permits autonomous low/medium-risk merges (ADR\n-   0008/0009); the MergeGate policy check is the recorded-authority gate.\n+5. The repo `.shiki/policy` permits autonomous low/medium-risk merges\n+   (SADR-0008/0009); the MergeGate policy check is the recorded-authority gate.\n \n The operator's only actions are: register the task and start the loop. **No\n manual pre-clearing** means after `shiki loop run` starts, the operator does not\ndiff --git a/docs/agents/bootstrap-command.md b/docs/agents/bootstrap-command.md\nindex 05f7f203a52cf412b9f97084a6e131ed0f785afd..f7aa0b734dfebee4a3b139335b66ee3415f48713\n--- a/docs/agents/bootstrap-command.md\n+++ b/docs/agents/bootstrap-command.md\n@@ -189,7 +189,9 @@\n ```\n \n Use this only for tests, fixtures, or explicit local-only template inspection.\n-Use `--force` only when you intentionally want to overwrite existing target files.\n+Use `--force` only when you intentionally want to refresh the target template.\n+Legacy numeric Shiki decision cleanup is stamp-and-digest authorized before any\n+write; target ADR paths are not cleanup candidates.\n \n ## Slash Command\n \ndiff --git a/docs/agents/checklists.md b/docs/agents/checklists.md\nindex 4a7f03ade346f53a1c1d82981f3fd744f9ab4547..0c5ec6c53dc7197b7b7a7d18e667b325babd6c45\n--- a/docs/agents/checklists.md\n+++ b/docs/agents/checklists.md\n@@ -32,18 +32,18 @@\n | ID | Item | Blocking |\n | --- | --- | --- |\n | GD-01 | Plan was challenged against `CONTEXT.md`. | blocking for non-trivial Goals |\n-| GD-02 | Relevant ADRs were read or absence was recorded. | blocking for non-trivial Goals |\n+| GD-02 | Relevant SADRs and target ADRs were read or absence was recorded. | blocking for non-trivial Goals |\n | GD-03 | Ambiguous terms were resolved or converted to HITL blockers. | blocking |\n | GD-04 | Edge scenarios were considered. | advisory |\n | GD-05 | Code reality was checked when code could answer. | blocking when applicable |\n | GD-06 | New domain terms were added to `CONTEXT.md` when needed. | blocking when applicable |\n-| GD-07 | ADR was created or explicitly skipped using the ADR criteria. | blocking when applicable |\n+| GD-07 | A SADR or target ADR was created or explicitly skipped using the decision-record criteria. | blocking when applicable |\n \n ## CI \u2014 Context & Impact Checklist\n \n | ID | Item | Blocking |\n | --- | --- | --- |\n-| CI-01 | Relevant docs and ADRs identified. | blocking |\n+| CI-01 | Relevant docs, SADRs, and target ADRs identified. | blocking |\n | CI-02 | Relevant modules, interfaces, seams, callers, and tests identified. | blocking |\n | CI-03 | Dependencies and blocked-by relationships identified. | blocking |\n | CI-04 | Candidate locks identified. | blocking |\n@@ -62,7 +62,7 @@\n | PRD-04 | Implementation decisions are recorded without volatile code detail. | blocking |\n | PRD-05 | Testing decisions are recorded. | blocking |\n | PRD-06 | Out-of-scope items are explicit. | blocking |\n-| PRD-07 | Relevant glossary/ADR links are present when applicable. | blocking |\n+| PRD-07 | Relevant glossary/SADR/target-ADR links are present when applicable. | blocking |\n \n ## SF \u2014 Spec Freeze Checklist\n \n@@ -98,7 +98,7 @@\n | H-02 | Branch/worktree target is present. | blocking |\n | H-03 | Scope and non-goals are present. | blocking |\n | H-04 | Dependencies and locks are present. | blocking |\n-| H-05 | Relevant docs, ADRs, modules, and tests are referenced. | blocking |\n+| H-05 | Relevant docs, SADRs, target ADRs, modules, and tests are referenced. | blocking |\n | H-06 | Required skills are stated. | blocking |\n | H-07 | TDD expectations are stated. | blocking when behavior work |\n | H-08 | Acceptance criteria are included. | blocking |\ndiff --git a/docs/agents/codex-handoff.md b/docs/agents/codex-handoff.md\nindex ede468f47cb277f69a857e8c45df703e4ed0151d..6f97bdffb67e44d7edea2c71e01ea28933c4eb6c\n--- a/docs/agents/codex-handoff.md\n+++ b/docs/agents/codex-handoff.md\n@@ -46,7 +46,7 @@\n \n ## Context\n - Domain terms:\n-- Relevant ADRs:\n+- Relevant SADRs and target ADRs:\n - Relevant docs:\n - Relevant modules/interfaces/seams:\n - Similar tests/prior art:\ndiff --git a/docs/agents/completion-check-agent.md b/docs/agents/completion-check-agent.md\nindex 13e4c035db35025f4e3cdcc5adfb24314ac4d521..61bfa1218cf36ca605250c91919e620f33238aa0\n--- a/docs/agents/completion-check-agent.md\n+++ b/docs/agents/completion-check-agent.md\n@@ -32,7 +32,7 @@\n - `AGENTS.md`\n - `CLAUDE.md`\n - `CONTEXT.md`\n-- relevant ADRs\n+- relevant SADRs and target ADRs\n - parent Goal issue\n - PRD issue or document\n - task issue\ndiff --git a/docs/agents/decision-control.md b/docs/agents/decision-control.md\nindex 7d8ee5ab03a5ccbcbd04056ae61de03785a2a9c9..8c94ecda00fffaa0e92c325ab83aa9776bd1dac1\n--- a/docs/agents/decision-control.md\n+++ b/docs/agents/decision-control.md\n@@ -11,7 +11,7 @@\n | Decision | Owner | When | Required inputs | Output | Enforcement surface |\n | --- | --- | --- | --- | --- | --- |\n | Is the Goal clear enough to plan? | Planner + Guardian when needed | Goal intake | User request, existing docs, risk signals | Goal readiness verdict | Goal checklist, issue template, ledger |\n-| Must `grill-with-docs` run? | Planner / Skill Gate | Before PRD or issue split | Ambiguity, domain terms, ADR conflicts, unclear boundaries | Skill invocation or explicit skip rationale | Skill Gate checklist |\n+| Must `grill-with-docs` run? | Planner / Skill Gate | Before PRD or issue split | Ambiguity, domain terms, SADR/target-ADR conflicts, unclear boundaries | Skill invocation or explicit skip rationale | Skill Gate checklist |\n | Is a PRD required? | Planner | After Goal context stabilizes | Goal, `grill-with-docs` output, Context & Impact output | PRD required / not required | PRD checklist, CCA evidence check |\n | Can the spec be frozen? | Operator only | End of Requirements Definition, before issue split | PRD/requirements summary, scope inventory (SF-02), open-question list | `spec_freeze` block in the plan + ledger evidence | `require_grilled_plan` (plan ingest/run), SF checklist |\n | Can a frozen spec change? | Operator only (Spec Amendment) | When implementation contradicts the frozen spec | Contested decisions, scoped re-grill result | recorded amendment + re-stamped freeze | SF checklist, ledger evidence |\ndiff --git a/docs/agents/domain.md b/docs/agents/domain.md\nindex 258da8ea81e87ac0a6b0b5b5e5c2fde980eaabb7..d57b0cf17ff3e1fa9e6e5885372bf083d8295421\n--- a/docs/agents/domain.md\n+++ b/docs/agents/domain.md\n@@ -5,7 +5,9 @@\n ## Layout\n \n - `CONTEXT.md` defines Shiki language. Keep it implementation-free.\n-- `docs/adr/` records hard-to-reverse decisions.\n+- `docs/adr/SADR-NNNN-*.md` records Shiki platform decisions.\n+- `docs/adr/NNNN-*.md` records target product decisions without sharing the\n+  SADR namespace.\n - `AGENTS.md` is the runtime-neutral agent constitution.\n - `CLAUDE.md` is the Claude Code wrapper around `AGENTS.md`.\n - `.shiki/` is the Target Repository mirror for Goals, PRDs, plans, Task DAGs, contracts, locks, worktree records, ledger entries, CCA verdicts, repair packets, reports, and handoffs.\n@@ -15,6 +17,6 @@\n \n - Use Shiki terms exactly when they exist.\n - Add glossary terms to `CONTEXT.md` only when the term is part of the domain, not a generic programming concept.\n-- Create ADRs only for hard-to-reverse, surprising, tradeoff-based decisions.\n+- Create SADRs or target ADRs only for hard-to-reverse, surprising, tradeoff-based decisions.\n - Keep product-specific language in the Target Repository, not in Shiki Template docs.\n-- If an ADR conflicts with a proposed implementation, surface the conflict before editing.\n+- If a SADR or target ADR conflicts with a proposed implementation, surface the conflict before editing.\ndiff --git a/docs/agents/execution-control.md b/docs/agents/execution-control.md\nindex ad8ee3cdb36be56fedc1f46408ca92b387d74047..bc63c3a73661d12b567016ba299f1a6b66bf9088\n--- a/docs/agents/execution-control.md\n+++ b/docs/agents/execution-control.md\n@@ -63,7 +63,7 @@\n | Branch name | Orchestrator | Orchestrator | Before implementer dispatch | branch policy | branch ref in task/ledger |\n | Lock reservation | Context & Impact | MergeGate | Before dispatch and before merge | `.shiki/locks/`, labels, PR metadata | lock id, owner, scope |\n | CI profile | Planner | MergeGate | During issue triage, rechecked at PR | labels, `.shiki/policy.yaml`, workflow matrix | required check list |\n-| Required status checks | Repo maintainer / Guardian | GitHub branch protection / rulesets | Repository setup, updated via ADR | GitHub settings | branch/ruleset config, ADR |\n+| Required status checks | Repo maintainer / Guardian | GitHub branch protection / rulesets | Repository setup, updated via target ADR | GitHub settings | branch/ruleset config, target ADR |\n | CCA judgment | CCA | GitHub required check / MergeGate | Every non-draft PR update | GitHub Action check | structured CCA verdict |\n | Repair assignment | CCA | MergeGate | After failed CCA/CI/review | repair packet, issue/PR comment | repair id and attempt count |\n | Merge readiness | MergeGate | GitHub branch protection / Guardian if needed | After CCA complete | required checks, review rules, rulesets | mergegate check output |\ndiff --git a/docs/agents/guardian-policy.md b/docs/agents/guardian-policy.md\nindex 4147c7adf05a47adb4fd24ada1a1c2e1f2ba6d2d..a20d9cc6c00a569bb057b9ca8d6b51c7fc4a9847\n--- a/docs/agents/guardian-policy.md\n+++ b/docs/agents/guardian-policy.md\n@@ -98,8 +98,8 @@\n `origin/<default_branch>` and reuses each job's existing base fetch; a shallow\n (`--depth`) or second base fetch would make the proof unresolvable and silently\n disable the carry, so neither is permitted. CCA reads `guardian_comment_carried` as\n-a recorded authority in the deterministic signal and does not re-derive it. See ADR\n-0018 for the full decision, the rejected diff/patch-equality and path-disjointness\n+a recorded authority in the deterministic signal and does not re-derive it. See\n+SADR-0018 for the full decision, the rejected diff/patch-equality and path-disjointness\n alternatives, and the accepted residual.\n \n ## External AI Guardian Review (SADR-0010)\ndiff --git a/docs/agents/implementation-policy.md b/docs/agents/implementation-policy.md\nindex c754144a9fb5bf4a38aa82a60e1670c59e626b44..85c37281bcad196c6b7b02343faf00b03cd9c57a\n--- a/docs/agents/implementation-policy.md\n+++ b/docs/agents/implementation-policy.md\n@@ -31,10 +31,13 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n ## Non-Negotiables\n \n 1. **Planning precedes implementation.** Non-trivial Goals start with `grill-with-docs` and Context & Impact.\n-2. **Domain language is controlled.** Terms must be checked against `CONTEXT.md`; hard-to-reverse tradeoffs must be checked against ADRs.\n+2. **Domain language is controlled.** Terms must be checked against `CONTEXT.md`; hard-to-reverse tradeoffs must be checked against Shiki SADRs and target ADRs.\n 3. **PRD precedes issue decomposition.** `to-prd` is used once the context is settled enough to become durable intent.\n 4. **Issues are vertical slices.** `to-issues` must produce independently grabbable tracer-bullet tasks, not horizontal layer tickets.\n 5. **Triage controls dispatch.** Only `ready-for-agent` / AFK tasks may be assigned to Codex without human input.\n@@ -95,19 +98,19 @@\n Process:\n \n - Challenge terms against `CONTEXT.md`.\n-- Check ADRs before reopening decisions.\n+- Check SADRs and target ADRs before reopening decisions.\n - Inspect code when code can answer a question.\n - Ask one question at a time when the operator is available.\n - Provide a recommended answer with each question.\n - Record resolved terms in `CONTEXT.md`.\n-- Create ADRs only for hard-to-reverse, surprising, tradeoff-based decisions.\n+- Create SADRs or target ADRs only for hard-to-reverse, surprising, tradeoff-based decisions.\n - Convert unresolved load-bearing questions into HITL blockers.\n \n Outputs:\n \n - Resolved design-tree decisions.\n - Updated glossary terms when needed.\n-- ADRs when justified.\n+- SADRs or target ADRs when justified.\n - HITL blockers when decisions remain unresolved.\n \n ## Phase 3 \u2014 Context & Impact\n@@ -117,7 +120,7 @@\n \n Outputs must include:\n \n-- Relevant documents and ADRs.\n+- Relevant documents, SADRs, and target ADRs.\n - Relevant modules, interfaces, seams, callers, and tests.\n - Candidate locks.\n - Dependencies.\n@@ -140,7 +143,7 @@\n - Testing decisions.\n - Out of scope.\n - Further notes.\n-- Links to relevant Goal, domain terms, and ADRs.\n+- Links to relevant Goal, domain terms, SADRs, and target ADRs.\n \n Phase 4 ends with Spec Freeze: the operator explicitly approves the PRD, the\n plan records a `spec_freeze` block (status, approved_by, source), and the SF\n@@ -240,7 +243,7 @@\n - Dependencies and locks.\n - Risk and Guardian requirements.\n - Ledger completeness.\n-- Documentation and ADR updates.\n+- Documentation and SADR/target-ADR updates.\n - Scope drift.\n \n CCA emits one of:\n@@ -301,7 +304,7 @@\n | Evidence failure | Behavior might be done, but proof is missing. | Add checks, PR evidence, ledger entry, or artifacts. |\n | Scope drift | PR changes unrelated behavior. | Revert unrelated changes or split into new Goal. |\n | Dependency block | Upstream task or lock not complete. | Block until dependency resolves. |\n-| Architecture block | Decision needs ADR or deep-module discussion. | `grill-with-docs` or `improve-codebase-architecture`. |\n+| Architecture block | Decision needs a SADR, target ADR, or deep-module discussion. | `grill-with-docs` or `improve-codebase-architecture`. |\n | Risk block | Security/production/policy change needs human approval. | Guardian decision. |\n \n ## Phase 5.5 \u2014 Execution Decision Control\ndiff --git a/docs/agents/runtime-auth-model.md b/docs/agents/runtime-auth-model.md\nindex 6a2e1a47e81ea89b5c68a210ec07772a9374babc..3c54748d24233dbd0ba8473950282547ef4ce935\n--- a/docs/agents/runtime-auth-model.md\n+++ b/docs/agents/runtime-auth-model.md\n@@ -148,7 +148,7 @@\n \n API-key based runners may be added by a target repository as an explicit extension. They are not the default Shiki template path.\n \n-If a repo opts into API-key mode, record the exception in an ADR and update the repo's `AGENTS.md`, `.shiki/config.yaml`, and workflow permissions.\n+If a repo opts into API-key mode, record the target-specific exception in a target ADR and update the repo's `AGENTS.md`, `.shiki/config.yaml`, and workflow permissions.\n \n ## GitHub Actions Runtime Compatibility\n \ndiff --git a/docs/agents/skill-gate.md b/docs/agents/skill-gate.md\nindex c50398b0d7c60a8598f2a5c88a10015730dced13..3acffe6444617bb1d54b77d4b4708158ec735bc6\n--- a/docs/agents/skill-gate.md\n+++ b/docs/agents/skill-gate.md\n@@ -9,7 +9,7 @@\n | Skill | Trigger | Expected output |\n | --- | --- | --- |\n | `setup-matt-pocock-skills` | First configuring a repo for these skills, or when issue tracker, triage labels, or domain docs are missing. | `docs/agents/` configuration and an `Agent skills` block in `AGENTS.md` or `CLAUDE.md`. |\n-| `grill-with-docs` | Ambiguous plan, unclear terminology, boundary decision, tradeoff, or ADR-worthy design choice. | Clarified decision tree, recommended answers, updates to `CONTEXT.md` or ADRs when decisions crystallize. |\n+| `grill-with-docs` | Ambiguous plan, unclear terminology, boundary decision, tradeoff, or decision-record-worthy design choice. | Clarified decision tree, recommended answers, updates to `CONTEXT.md`, SADRs, or target ADRs when decisions crystallize. |\n | `zoom-out` | Agent lacks a higher-level architectural map. | Map of relevant modules, callers, seams, and domain concepts. |\n | `to-prd` | Settled context should become a PRD. | PRD published to the issue tracker with problem, solution, user stories, decisions, testing, and out-of-scope notes. |\n | `to-issues` | Goal, PRD, or plan must become implementation tickets. | Vertical-slice issues with acceptance criteria, dependencies, and AFK/HITL classification. |\n@@ -17,7 +17,7 @@\n | `tdd` | Feature work or bug fix has observable behavior that can be specified with tests. | Red-green-refactor loop through public interfaces, one vertical slice at a time. |\n | `code-review` | The TDD loop is green and the implementer is about to create or update a PR. Default for every implementation task. | Pre-PR self-review of the task diff with fixes applied on the task branch; findings and resolutions recorded as a ledger entry naming the skill and a `## Pre-PR code review` PR body section. |\n | `diagnose` | Hard bug, failing check, regression, flaky behavior, or performance problem. | Reproduction loop, ranked hypotheses, instrumentation, fix, regression test, post-mortem. |\n-| `improve-codebase-architecture` | Structural friction, testability, AI-navigability, deep-module opportunity, or refactor request. | Architecture candidates and deepening recommendations informed by `CONTEXT.md` and ADRs. |\n+| `improve-codebase-architecture` | Structural friction, testability, AI-navigability, deep-module opportunity, or refactor request. | Architecture candidates and deepening recommendations informed by `CONTEXT.md`, SADRs, and target ADRs. |\n | `prototype` | A throwaway logic or UI prototype is needed to answer a design question. | Clearly marked disposable prototype and captured decision. |\n \n ## MergeGate Interaction\ndiff --git a/docs/operations.md b/docs/operations.md\nindex cf1f452d4c589e38ccd58553bda0ad8684b51eb7..ace48a90ffbec9e5c0a7113bef16a4858bc44b3f\n--- a/docs/operations.md\n+++ b/docs/operations.md\n@@ -89,6 +89,16 @@\n      target history, brought forward by `shiki migrate`, not a template.\n    - **Everything else** (CLI scripts, workflows, schemas, docs) is overwritten.\n \n+   A forced upgrade also migrates the former numeric Shiki decision paths to\n+   `SADR-NNNN-*`. Before the first write, the installer inventories the exact\n+   legacy Shiki paths and authorizes each deletion only when the existing\n+   `.shiki/install-stamp.json` names that path and its SHA-256 digest still\n+   matches the file. An absent, unreadable, malformed, incomplete, or\n+   mismatching stamp reports every blocked path and stops with no filesystem\n+   change. Target-owned `NNNN-*` ADRs are not cleanup candidates. A non-force\n+   install deletes nothing and retains any still-matching legacy stamp entries\n+   so a later forced upgrade can prove ownership.\n+\n    If the target has pending migrations, `--force` refuses and writes nothing \u2014\n    apply migrations first (step 3), then re-run. The run ends with a single\n    summary of every `.new` written, so a half-upgrade is visible.\ndiff --git a/docs/releasing.md b/docs/releasing.md\nindex 10d73828e7dbd64e9d9f6b83bc84d010537ce62a..aafeee8c4d441e1e427efee2ce4f81a647f020e1\n--- a/docs/releasing.md\n+++ b/docs/releasing.md\n@@ -67,7 +67,7 @@\n CLI in `scripts/`. There are no third-party Python runtime dependencies. The CLI\n is intentionally **not** exposed as a `[project.scripts]` console entry point\n because `scripts/shiki.py` is a dependency-free shim and `scripts/` is not\n-packaged as an importable distribution (see the ADR for the rationale).\n+packaged as an importable distribution (see SADR-0007 for the rationale).\n \n Supported install paths:\n \n@@ -155,6 +155,15 @@\n    [docs/operations.md](operations.md#the-new-convention). If the target has\n    pending migrations, `--force` refuses and writes nothing; apply migrations\n    (step 2) first, then re-run.\n+\n+   The SADR namespace upgrade has an additional pre-write gate: each former\n+   numeric Shiki decision path is removed only when the existing install stamp\n+   contains that exact path and a digest matching its current bytes. The\n+   installer aggregates absent/unreadable/malformed-stamp, missing-digest, and\n+   digest-mismatch blockers before raising, so refusal leaves the target tree\n+   byte-identical. Numeric target ADRs are preserved. A successful forced\n+   upgrade installs and stamps `SADR-*` paths and drops the removed legacy paths\n+   from the refreshed stamp.\n 2. Apply repository-local `.shiki/` state migrations with the migrate command:\n \n    ```bash\ndiff --git a/docs/usage.md b/docs/usage.md\nindex b83407ee3970dd395dd893010bc5dbe8bad26051..a130ef6e40dfd8169a5bc701339351097df1a09f\n--- a/docs/usage.md\n+++ b/docs/usage.md\n@@ -67,8 +67,10 @@\n shiki install-target /path/to/target-repo --local-only\n ```\n \n-Use `--force` only when you intentionally want to overwrite existing target\n-files.\n+Use `--force` only when you intentionally want to refresh the target template.\n+For the SADR namespace migration, `--force` deletes only exact legacy Shiki\n+decision paths whose existing install-stamp digest still matches; any ownership\n+blocker stops before the first write. Numeric target ADRs are never candidates.\n \n ## `shiki bootstrap-platform` \u2014 publish the Shiki platform repo\n \ndiff --git a/scripts/guardian_approval_signal.py b/scripts/guardian_approval_signal.py\nindex d059f92b2c82aa2b5d6ca421365537a87886c1cb..16924892714f4f5b9303c0ef0e325a7e70075630\n--- a/scripts/guardian_approval_signal.py\n+++ b/scripts/guardian_approval_signal.py\n@@ -199,8 +199,8 @@\n ) -> Any:\n     \"\"\"Evaluate SADR-0015 Contract Approval for the CCA Guardian signal.\n \n-    The SAME OR that MergeGate applies must reach this deterministic signal: ADR\n-    0015 is explicit that without it, a contract-approved PR still returns\n+    The SAME OR that MergeGate applies must reach this deterministic signal:\n+    SADR-0015 is explicit that without it, a contract-approved PR still returns\n     ``needs_guardian``, the CCA job fails, and the MergeGate policy check never\n     runs \u2014 the gate this change moves would never be reached. This resolves the\n     task the SAME way MergeGate does (the first Shiki task id in the PR body),\ndiff --git a/scripts/shiki_contract_approval.py b/scripts/shiki_contract_approval.py\nindex 941bcb96fdd8b9a85bc0c4f0f04d5dad9d04839c..4a4b418f6527413161db82670bec857a043360e2\n--- a/scripts/shiki_contract_approval.py\n+++ b/scripts/shiki_contract_approval.py\n@@ -142,7 +142,7 @@\n     changed_files_status: Any,\n     registration: Any,\n ) -> ContractApprovalResult:\n-    \"\"\"Whether this PR carries an SADR-0015 Contract Approval.\n+    \"\"\"Whether this PR carries a SADR-0015 Contract Approval.\n \n     The four conditions are evaluated IN ORDER, each failure recorded as a\n     distinct reason string:\ndiff --git a/scripts/shiki_guardian.py b/scripts/shiki_guardian.py\nindex a4ecb79bbda55fd5c8b06adfae2eb99760a03c45..4dd99bbe714e26f421fa19abac7a34ae1b195ecc\n--- a/scripts/shiki_guardian.py\n+++ b/scripts/shiki_guardian.py\n@@ -12,7 +12,7 @@\n # The carry re-derives its proof from git in-process on every judging invocation;\n # it is never read from an artifact, so there is nothing to go stale or be forged.\n # shiki_sync_proof is the ONLY guardian-family module that runs git, and every one\n-# of its invocations is read-only and local (ADR: prove-from-git base sync).\n+# of its invocations is read-only and local (SADR: prove-from-git base sync).\n from shiki_sync_proof import verify_pure_base_sync\n \n GUARDIAN_POLICY_PATH = \".shiki/guardian-policy.json\"\ndiff --git a/scripts/shiki_guardian_review.py b/scripts/shiki_guardian_review.py\nindex dd0f104d457899e0ba5ff1e162011c1e90b6f92b..e10ef58c046324e7ea3a3392feb3048ca77414ce\n--- a/scripts/shiki_guardian_review.py\n+++ b/scripts/shiki_guardian_review.py\n@@ -159,7 +159,7 @@\n     docs_only = _is_docs_only(changed_files)\n     if docs_only and matched == 0:\n         focus.append(\n-            \"Docs/ADR-only change: confirm the change is genuinely documentation/decision-only, introduces no behavior or contract drift, and stays consistent with the constitution and existing ADRs.\"\n+            \"Docs/decision-record-only change: confirm the change is genuinely documentation/decision-only, introduces no behavior or contract drift, and stays consistent with the constitution and existing SADRs and target ADRs.\"\n         )\n \n     # Conservative augmentation: unknown (no category and not docs-only), mixed\ndiff --git a/scripts/shiki_installer.py b/scripts/shiki_installer.py\nindex be064ab7a29ff6836f7f7a1d70500a43e0d11d7b..9fd8eefd137ce7003dad5c0dcb2e48e2cd47ab32\n--- a/scripts/shiki_installer.py\n+++ b/scripts/shiki_installer.py\n@@ -118,6 +118,7 @@\n # what lets a target carry it without a governance-schema change.\n INSTALL_STAMP_PATH = \".shiki/install-stamp.json\"\n INSTALL_STAMP_VERSION = 1\n+LEGACY_SHIKI_SADR_MAX = 18\n \n # Under --force the shipped surface splits three ways, so nothing is lost and\n # nothing is silently stale.\n@@ -237,9 +238,98 @@\n         )\n \n \n+def legacy_shiki_adr_paths() -> tuple[str, ...]:\n+    \"\"\"Former numeric paths for Shiki SADR-0001 through SADR-0018.\n+\n+    The authoritative slugs come from the current SADR filenames. SADR-0019 is\n+    the namespace decision itself and never shipped under a numeric legacy path.\n+    \"\"\"\n+    paths: list[str] = []\n+    for sadr in sorted((ROOT / \"docs\" / \"adr\").glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\")):\n+        number = sadr.name.split(\"-\", 2)[1]\n+        if int(number) <= LEGACY_SHIKI_SADR_MAX:\n+            paths.append(f\"docs/adr/{sadr.name.removeprefix('SADR-')}\")\n+    return tuple(paths)\n+\n+\n+def preflight_legacy_sadr_cleanup(target: Path, *, force: bool) -> tuple[str, ...]:\n+    \"\"\"Authorize exact legacy Shiki ADR removals before the first write.\n+\n+    A non-force install never removes existing files. A forced install needs no\n+    stamp when no legacy Shiki path is present. When legacy paths do exist, each\n+    must be named in a readable install stamp and its current bytes must match\n+    the recorded digest.\n+    \"\"\"\n+    if not force:\n+        return ()\n+    present = tuple(\n+        relative\n+        for relative in legacy_shiki_adr_paths()\n+        if (target / relative).exists() or (target / relative).is_symlink()\n+    )\n+    if not present:\n+        return ()\n+\n+    stamp_path = target / INSTALL_STAMP_PATH\n+    try:\n+        raw = stamp_path.read_text(encoding=\"utf-8\")\n+        stamp = json.loads(raw)\n+    except FileNotFoundError:\n+        detail = \"install stamp is absent\"\n+        blockers = [f\"{relative}: {detail}\" for relative in present]\n+    except (OSError, UnicodeDecodeError) as error:\n+        detail = f\"install stamp is unreadable ({error})\"\n+        blockers = [f\"{relative}: {detail}\" for relative in present]\n+    except json.JSONDecodeError as error:\n+        detail = f\"install stamp is malformed ({error})\"\n+        blockers = [f\"{relative}: {detail}\" for relative in present]\n+    else:\n+        if not isinstance(stamp, dict):\n+            blockers = [\n+                f\"{relative}: install stamp is malformed (expected an object)\"\n+                for relative in present\n+            ]\n+        else:\n+            digests = stamp.get(\"digests\")\n+            blockers = []\n+            if not isinstance(digests, dict):\n+                blockers.extend(\n+                    f\"{relative}: install stamp has no digest for this path\"\n+                    for relative in present\n+                )\n+            else:\n+                for relative in present:\n+                    expected = digests.get(relative)\n+                    if not isinstance(expected, str) or not expected:\n+                        blockers.append(\n+                            f\"{relative}: install stamp has no digest for this path\"\n+                        )\n+                        continue\n+                    try:\n+                        actual = _sha256_file(target / relative)\n+                    except OSError as error:\n+                        blockers.append(\n+                            f\"{relative}: legacy path is unreadable ({error})\"\n+                        )\n+                        continue\n+                    if actual != expected:\n+                        blockers.append(\n+                            f\"{relative}: digest mismatch (stamp {expected}, current {actual})\"\n+                        )\n+\n+    if blockers:\n+        joined = \"\\n\".join(f\"- {blocker}\" for blocker in blockers)\n+        raise ShikiError(\n+            \"legacy Shiki ADR cleanup is not authorized:\\n\"\n+            f\"{joined}\\nNo files were written.\"\n+        )\n+    return present\n+\n+\n def install_template(target: Path, *, force: bool, validate: bool) -> list[NewFileNote]:\n     # Preconditions first: never rewrite the tree of a target that cannot be\n     # upgraded cleanly (see refuse_pending_migrations).\n+    legacy_cleanup = preflight_legacy_sadr_cleanup(target, force=force)\n     refuse_pending_migrations(target)\n \n     # Resolve the target's required CODEOWNERS owner once from its\n@@ -250,6 +340,10 @@\n     # verbatim. See ``codeowners_required_owner``.\n     codeowners_owner = codeowners_required_owner(target)\n \n+    for relative in legacy_cleanup:\n+        (target / relative).unlink()\n+        info(f\"removed stamped legacy Shiki ADR: {target / relative}\")\n+\n     notes: list[NewFileNote] = []\n     for relative in TEMPLATE_PATHS:\n         source = ROOT / relative\n@@ -647,6 +741,22 @@\n     digests = target_stamp_digests(target)\n     existing = read_install_stamp(target)\n     if existing is not None:\n+        # A non-force install deliberately leaves legacy Shiki ADRs in place.\n+        # Preserve only still-matching ownership proof so a later forced\n+        # upgrade can authorize cleanup; never carry a missing or stale digest.\n+        existing_digests = existing.get(\"digests\")\n+        if isinstance(existing_digests, dict):\n+            for relative in legacy_shiki_adr_paths():\n+                expected = existing_digests.get(relative)\n+                path = target / relative\n+                if not isinstance(expected, str) or not path.is_file():\n+                    continue\n+                try:\n+                    actual = _sha256_file(path)\n+                except OSError:\n+                    continue\n+                if actual == expected:\n+                    digests[relative] = actual\n         if _incoming_is_older(commit, existing.get(\"platform_commit\")):\n             warn(\n                 \"kept existing install stamp: incoming install \"\ndiff --git a/scripts/shiki_loop.py b/scripts/shiki_loop.py\nindex 3611641da8aeb5038b1da90e3114b09a473ec6d8..84caa491b9cad0edc4176f209c13e9221e975681\n--- a/scripts/shiki_loop.py\n+++ b/scripts/shiki_loop.py\n@@ -424,7 +424,7 @@\n         # unresolvable CCA verdict has already produced its terminal stop and is never\n         # laundered into a head-moving sync (SADR-0010). It also never fires while the\n         # policy (Guardian) gate is red \u2014 for ANY reason, not only when it is the sole\n-        # red check \u2014 so a head-bound Guardian approval, or an SADR-0015\n+        # red check \u2014 so a head-bound Guardian approval, or a SADR-0015\n         # contract-approval carry that base movement defeated (the policy gate compares\n         # base and head task governance directly), is never silently restored by\n         # adopting base wholesale. It is bounded by a durable per-task counter so the\ndiff --git a/scripts/shiki_tasks.py b/scripts/shiki_tasks.py\nindex aec3809f24b4a0c8c96fc50971cff9ae9ad30381..d531052e9d9717058d4631483cefab44c26a65c1\n--- a/scripts/shiki_tasks.py\n+++ b/scripts/shiki_tasks.py\n@@ -821,7 +821,7 @@\n         \"prompt\": prompt,\n         \"entry_skill\": \"grill-with-docs\",\n         \"required_next_steps\": [\n-            \"Run grill-with-docs until terminology, boundaries, risks, and ADR-worthy decisions are settled.\",\n+            \"Run grill-with-docs until terminology, boundaries, risks, and decision-record-worthy choices are settled.\",\n             \"Obtain the operator's explicit approval of the PRD/requirements (Spec Freeze).\",\n             \"Write a machine-readable plan JSON with grill_with_docs.status=complete and spec_freeze.status=frozen.\",\n             \"Run shiki plan ingest --plan-file PLAN.json.\",\ndiff --git a/scripts/test_shiki_goal_loop.sh b/scripts/test_shiki_goal_loop.sh\nindex 753c7b516e10f0be3bc92a4bd0eb1fa699776ae6..8f3820a0a079b3044424cfe1a6c6b7fe68896e67\n--- a/scripts/test_shiki_goal_loop.sh\n+++ b/scripts/test_shiki_goal_loop.sh\n@@ -231,8 +231,8 @@\n \n # High/critical risk merges autonomously when all required checks are green:\n # the \"MergeGate policy check\" required check IS the Guardian gate (it enforces\n-# guardian-policy.json \u2014 human approval OR external AI guardian review, ADR\n-# 0010), so green checks mean a recorded authority approved. The fake gh returns\n+# guardian-policy.json \u2014 human approval OR external AI guardian review,\n+# SADR-0010), so green checks mean a recorded authority approved. The fake gh returns\n # green checks, simulating that the Guardian gate passed.\n python3 \"$ROOT/scripts/shiki.py\" goal create --target \"$TARGET\" --title \"Guardian gate\" --outcome \"High risk merges via policy gate\" >/tmp/shiki-goal-loop-guardian-goal.json\n GGOAL=\"$(json_get /tmp/shiki-goal-loop-guardian-goal.json goal_id)\"\ndiff --git a/tests/test_adr_references.py b/tests/test_adr_references.py\nindex 51706329c797d5dd9b47e082a333fa83b261d482..db6e693e35f67103935be34f1d9bb4ff7ee60395\n--- a/tests/test_adr_references.py\n+++ b/tests/test_adr_references.py\n@@ -1,7 +1,7 @@\n-\"\"\"Every ``ADR ####`` citation must resolve to exactly one decision record.\n+\"\"\"Every target ADR and Shiki SADR citation resolves in its own namespace.\n \n Background. ``scripts/mergegate_check.py`` justifies ``is_bookkeeping_closeout``\n-with \"ADR 0017\" nine times and ``scripts/guardian_approval_signal.py`` cites\n+with \"SADR-0017\" nine times and ``scripts/guardian_approval_signal.py`` cites\n 0016/0017 seven times, yet for a stretch the numbering in ``docs/adr/`` stopped\n at 0015: the authoring commits lived on a coordinator branch and no task ever\n declared ``docs/adr/...`` in its locks, so nothing carried the records to the\n@@ -11,11 +11,12 @@\n and the directory drifted with no signal.\n \n This is the same unbound-pair shape behind other silent failures: a reference\n-and its target with nothing asserting they agree. This test binds them. It scans\n-every ``scripts/*.py`` (plus ``docs/**/*.md`` and the root ``*.md`` governance\n-files, which are cheap) for the ``ADR ####`` pattern and asserts each cited\n-number resolves to exactly one ``docs/adr/####-*.md``. The converse is\n-deliberately NOT required: an ADR may exist without being cited.\n+and its target with nothing asserting they agree. This test binds them. Target\n+``ADR NNNN`` citations resolve only to ``docs/adr/NNNN-*.md``; Shiki\n+``SADR-NNNN`` citations resolve only to ``docs/adr/SADR-NNNN-*.md``. The scan\n+covers current governance docs, scripts, workflows, prompts, packaging metadata,\n+and repo-local Codex guidance while deliberately excluding historical\n+``.shiki`` evidence. The converse is not required: a record may be uncited.\n \n The suite is dependency free: standard-library ``unittest`` only.\n \"\"\"\n@@ -31,17 +32,18 @@\n \n REPO_ROOT = Path(__file__).resolve().parents[1]\n \n-# \"ADR 0016\" -- a four-digit citation. The trailing \\b keeps a five-digit token\n-# from being read as a truncated four-digit number.\n-_CITATION = re.compile(r\"\\bADR (\\d{4})\\b\")\n-# \"0016-locks-bound-product-paths.md\" -- the leading four digits name the record.\n-_ADR_FILENAME = re.compile(r\"^(\\d{4})-.*\\.md$\")\n+# ``ADR 0016`` / ``SADR-0016`` -- a namespace plus four-digit citation. The\n+# trailing ``\\b`` keeps a five-digit token from being read as a truncated one.\n+_CITATION = re.compile(r\"\\b(S?ADR)[ -](\\d{4})\\b\")\n+# Target ADRs retain the numeric filename; Shiki platform decisions use SADR.\n+_DECISION_FILENAME = re.compile(r\"^(?:(SADR)-)?(\\d{4})-.*\\.md$\")\n \n \n class Violation(NamedTuple):\n     \"\"\"A citation that does not resolve to exactly one decision record.\"\"\"\n \n     citing_file: str  # repo-relative path of the file that cites the ADR\n+    namespace: str  # \"ADR\" for target decisions; \"SADR\" for Shiki decisions\n     number: str  # the cited four-digit number, e.g. \"0016\"\n     line: int  # 1-based line of the citation\n     reason: str  # \"missing\" (zero matches) or \"ambiguous\" (two or more)\n@@ -49,44 +51,59 @@\n \n     @property\n     def expected_path(self) -> str:\n-        return f\"docs/adr/{self.number}-*.md\"\n+        prefix = \"SADR-\" if self.namespace == \"SADR\" else \"\"\n+        return f\"docs/adr/{prefix}{self.number}-*.md\"\n \n     def describe(self) -> str:\n         \"\"\"Name the citing file, the cited number, and the missing path.\"\"\"\n         if self.reason == \"missing\":\n             return (\n-                f\"{self.citing_file}:{self.line} cites ADR {self.number} \"\n+                f\"{self.citing_file}:{self.line} cites {self.namespace} {self.number} \"\n                 f\"but no document matches {self.expected_path}\"\n             )\n         joined = \", \".join(self.matches)\n         return (\n-            f\"{self.citing_file}:{self.line} cites ADR {self.number} which \"\n+            f\"{self.citing_file}:{self.line} cites {self.namespace} {self.number} which \"\n             f\"resolves to {len(self.matches)} documents (expected exactly one \"\n             f\"at {self.expected_path}): {joined}\"\n         )\n \n \n-def _adr_index(root: Path) -> dict[str, list[Path]]:\n-    \"\"\"Map every four-digit ADR number to the record files that claim it.\"\"\"\n-    index: dict[str, list[Path]] = {}\n+def _adr_index(root: Path) -> dict[tuple[str, str], list[Path]]:\n+    \"\"\"Map each decision namespace and number to the files that claim it.\"\"\"\n+    index: dict[tuple[str, str], list[Path]] = {}\n     adr_dir = root / \"docs\" / \"adr\"\n     if adr_dir.is_dir():\n         for path in sorted(adr_dir.glob(\"*.md\")):\n-            match = _ADR_FILENAME.match(path.name)\n+            match = _DECISION_FILENAME.match(path.name)\n             if match:\n-                index.setdefault(match.group(1), []).append(path)\n+                namespace = \"SADR\" if match.group(1) else \"ADR\"\n+                index.setdefault((namespace, match.group(2)), []).append(path)\n     return index\n \n \n def _citing_files(root: Path, include_docs: bool = True) -> list[Path]:\n-    \"\"\"Files scanned for citations: scripts/*.py, and cheaply the markdown.\"\"\"\n+    \"\"\"Current, shipped text surfaces scanned for decision citations.\"\"\"\n     files = sorted((root / \"scripts\").glob(\"*.py\"))\n+    files += sorted((root / \"scripts\").glob(\"*.sh\"))\n     if include_docs:\n         docs = root / \"docs\"\n         if docs.is_dir():\n             files += sorted(docs.rglob(\"*.md\"))\n         files += sorted(root.glob(\"*.md\"))\n-    return files\n+        pyproject = root / \"pyproject.toml\"\n+        if pyproject.is_file():\n+            files.append(pyproject)\n+        for relative in (\".github\", \".codex\"):\n+            surface = root / relative\n+            if surface.is_dir():\n+                files += sorted(\n+                    path\n+                    for path in surface.rglob(\"*\")\n+                    if path.is_file()\n+                    and path.suffix in {\".md\", \".py\", \".sh\", \".toml\", \".yaml\", \".yml\"}\n+                )\n+    return sorted(set(files))\n \n \n def find_adr_reference_violations(\n@@ -94,10 +111,9 @@\n ) -> list[Violation]:\n     \"\"\"Return every ``ADR ####`` citation under ``root`` that fails to resolve.\n \n-    A citation resolves when exactly one ``docs/adr/####-*.md`` matches its\n-    number. Zero matches is a \"missing\" violation; two or more is \"ambiguous\".\n-    Records that are never cited produce nothing -- the converse is not\n-    required.\n+    A citation resolves when exactly one record in the same namespace matches\n+    its number. Zero matches is a \"missing\" violation; two or more is\n+    \"ambiguous\". Records that are never cited produce nothing.\n     \"\"\"\n     root = Path(root)\n     index = _adr_index(root)\n@@ -110,13 +126,15 @@\n         rel = path.relative_to(root).as_posix()\n         for lineno, line in enumerate(text.splitlines(), start=1):\n             for match in _CITATION.finditer(line):\n-                number = match.group(1)\n-                found = index.get(number, [])\n+                namespace, number = match.groups()\n+                found = index.get((namespace, number), [])\n                 if len(found) == 1:\n                     continue\n                 reason = \"missing\" if not found else \"ambiguous\"\n                 rels = [p.relative_to(root).as_posix() for p in found]\n-                violations.append(Violation(rel, number, lineno, reason, rels))\n+                violations.append(\n+                    Violation(rel, namespace, number, lineno, reason, rels)\n+                )\n     return violations\n \n \n@@ -129,8 +147,8 @@\n         self.assertEqual(\n             violations,\n             [],\n-            \"Every 'ADR ####' citation must resolve to exactly one \"\n-            f\"docs/adr/####-*.md:\\n{detail}\",\n+            \"Every ADR/SADR citation must resolve exactly once in its own \"\n+            f\"docs/adr namespace:\\n{detail}\",\n         )\n \n     def test_scan_actually_reads_scripts(self) -> None:\n@@ -146,8 +164,21 @@\n         # The two records this Goal carried onto the default branch, and which\n         # the scripts cite most, must be present.\n         index = _adr_index(REPO_ROOT)\n-        self.assertIn(\"0016\", index)\n-        self.assertIn(\"0017\", index)\n+        self.assertIn((\"SADR\", \"0016\"), index)\n+        self.assertIn((\"SADR\", \"0017\"), index)\n+\n+    def test_platform_tree_has_only_sadr_0001_through_0019(self) -> None:\n+        adr_dir = REPO_ROOT / \"docs\" / \"adr\"\n+        sadrs = sorted(adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\"))\n+        self.assertEqual(\n+            {path.name.split(\"-\", 2)[1] for path in sadrs},\n+            {f\"{number:04d}\" for number in range(1, 20)},\n+        )\n+        self.assertEqual(\n+            list(adr_dir.glob(\"[0-9][0-9][0-9][0-9]-*.md\")),\n+            [],\n+            \"the Shiki platform tree must not retain legacy numeric SADR paths\",\n+        )\n \n \n class FixtureAdrReferences(unittest.TestCase):\n@@ -180,6 +211,26 @@\n         self.assertIn(\"9991\", message)  # cited number\n         self.assertIn(\"docs/adr/9991-*.md\", message)  # missing path\n \n+    def test_target_adr_does_not_satisfy_missing_platform_sadr(self) -> None:\n+        root = self._make_repo(\n+            adr_names=[\"0042-target-decision.md\"],\n+            script_body='\"\"\"Target ADR 0042 exists, but platform SADR-0042 does not.\"\"\"\\n',\n+        )\n+        violations = find_adr_reference_violations(root, include_docs=False)\n+        self.assertEqual(len(violations), 1)\n+        self.assertEqual(violations[0].namespace, \"SADR\")\n+        self.assertEqual(violations[0].number, \"0042\")\n+        self.assertEqual(violations[0].expected_path, \"docs/adr/SADR-0042-*.md\")\n+\n+    def test_target_adr_and_platform_sadr_may_reuse_a_number(self) -> None:\n+        root = self._make_repo(\n+            adr_names=[\"0042-target-decision.md\", \"SADR-0042-platform-decision.md\"],\n+            script_body='\"\"\"Target ADR 0042 and platform SADR-0042 are distinct.\"\"\"\\n',\n+        )\n+        self.assertEqual(\n+            find_adr_reference_violations(root, include_docs=False), []\n+        )\n+\n     def test_uncited_adr_is_allowed(self) -> None:\n         # An ADR exists but nothing cites it -> not a violation. The converse\n         # (every ADR must be cited) is explicitly NOT required.\ndiff --git a/tests/test_guardian_review.py b/tests/test_guardian_review.py\nindex 88829e7810212635e2fc98aa5918d6f26294677d..00a85a237acd5bf2d6cb5b294128198c422402ca\n--- a/tests/test_guardian_review.py\n+++ b/tests/test_guardian_review.py\n@@ -162,7 +162,7 @@\n \n     def test_docs_only_is_light_and_not_conservative(self):\n         focus = gr.classify_review_focus([\"docs/adr/0099-x.md\", \"README.md\"], \"\", \"low\")\n-        self.assertTrue(any(\"Docs/ADR-only\" in f for f in focus))\n+        self.assertTrue(any(\"Docs/decision-record-only\" in f for f in focus))\n         self.assertFalse(any(\"conservative default\" in f.lower() for f in focus))\n \n     def test_unknown_category_is_conservative(self):\ndiff --git a/tests/test_policy_job_merged_prs.py b/tests/test_policy_job_merged_prs.py\nindex d45512e74681e5e649279236c7d4e79d5c3da471..b97989f8bd74d091de1a6b5275a20d8dc68c4512\n--- a/tests/test_policy_job_merged_prs.py\n+++ b/tests/test_policy_job_merged_prs.py\n@@ -3,8 +3,8 @@\n \n ``.github/workflows/shiki-cca-completion.yml`` has two consumers of the merged-PR\n proof: the CCA *signal* (``guardian_approval_signal.py`` in \"Prepare CCA context\")\n-and the authoritative gate (``mergegate_check.py`` in \"Enforce MergeGate\"). ADR\n-0017's bookkeeping-closeout exemption (condition 6) needs the merged implementation\n+and the authoritative gate (``mergegate_check.py`` in \"Enforce MergeGate\").\n+SADR-0017's bookkeeping-closeout exemption (condition 6) needs the merged implementation\n PR proven, and a closeout carries NO ``post_merge_reconcile`` marker. The signal\n already resolves ``MERGED_PRS`` unconditionally, but \"Enforce MergeGate\" used to\n resolve it only inside ``grep -q '<!-- shiki:post_merge_reconcile -->'``. So a\ndiff --git a/tests/test_sadr_install_migration.py b/tests/test_sadr_install_migration.py\nnew file mode 100644\nindex 0000000000000000000000000000000000000000..a6f4c57e4dcaef73ef600b0d4804744138ab9071\n--- /dev/null\n+++ b/tests/test_sadr_install_migration.py\n@@ -0,0 +1,229 @@\n+\"\"\"SADR installation and collision-safe legacy upgrade behavior.\"\"\"\n+\n+from __future__ import annotations\n+\n+import contextlib\n+import hashlib\n+import io\n+import json\n+import shutil\n+import tempfile\n+import unittest\n+from pathlib import Path\n+\n+import shiki_test_support  # noqa: F401  (path bootstrap)\n+\n+import shiki_installer\n+from tests.test_adr_references import find_adr_reference_violations\n+\n+\n+PRODUCT_NUMBERS = tuple(range(13, 19))\n+\n+\n+def _quiet_install(target: Path, *, force: bool, validate: bool) -> None:\n+    with contextlib.redirect_stdout(io.StringIO()):\n+        shiki_installer.install_template(target, force=force, validate=validate)\n+\n+\n+def _sha256(path: Path) -> str:\n+    return hashlib.sha256(path.read_bytes()).hexdigest()\n+\n+\n+def _legacy_sadr_paths() -> tuple[str, ...]:\n+    return tuple(\n+        f\"docs/adr/{path.name.removeprefix('SADR-')}\"\n+        for path in sorted((shiki_installer.ROOT / \"docs\" / \"adr\").glob(\"SADR-*.md\"))\n+        if path.name != \"SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\"\n+    )\n+\n+\n+def _create_legacy_upgrade_fixture(\n+    target: Path,\n+) -> tuple[dict[str, bytes], tuple[str, ...]]:\n+    adr_dir = target / \"docs\" / \"adr\"\n+    adr_dir.mkdir(parents=True)\n+    product_bytes = {\n+        f\"{number:04d}-cubeshare-product-decision.md\": (\n+            f\"# CubeShare ADR {number:04d}\\n\\nDo not rewrite {number}.\\n\"\n+        ).encode()\n+        for number in PRODUCT_NUMBERS\n+    }\n+    for name, content in product_bytes.items():\n+        (adr_dir / name).write_bytes(content)\n+\n+    legacy_paths = _legacy_sadr_paths()\n+    for relative in legacy_paths:\n+        (target / relative).write_bytes(f\"legacy Shiki bytes for {relative}\\n\".encode())\n+\n+    stamp_path = target / shiki_installer.INSTALL_STAMP_PATH\n+    stamp_path.parent.mkdir(parents=True, exist_ok=True)\n+    stamp = {\n+        \"version\": shiki_installer.INSTALL_STAMP_VERSION,\n+        \"platform_commit\": None,\n+        \"installed_at\": \"2000-01-01T00:00:00+00:00\",\n+        \"digests\": {\n+            relative: _sha256(target / relative) for relative in legacy_paths\n+        },\n+    }\n+    stamp_path.write_text(\n+        json.dumps(stamp, indent=2, sort_keys=True) + \"\\n\", encoding=\"utf-8\"\n+    )\n+    return product_bytes, legacy_paths\n+\n+\n+def _tree_snapshot(root: Path) -> dict[str, tuple[str, bytes | str]]:\n+    \"\"\"Recursive byte/shape snapshot used to prove preflight is write-free.\"\"\"\n+    snapshot: dict[str, tuple[str, bytes | str]] = {}\n+    for path in sorted(root.rglob(\"*\")):\n+        relative = path.relative_to(root).as_posix()\n+        if path.is_symlink():\n+            snapshot[relative] = (\"symlink\", path.readlink().as_posix())\n+        elif path.is_dir():\n+            snapshot[relative] = (\"directory\", \"\")\n+        else:\n+            snapshot[relative] = (\"file\", path.read_bytes())\n+    return snapshot\n+\n+\n+class FreshSadrInstallTests(unittest.TestCase):\n+    def setUp(self) -> None:\n+        self.tmp = Path(tempfile.mkdtemp(prefix=\"shiki-sadr-fresh-\"))\n+        self.addCleanup(lambda: shutil.rmtree(self.tmp, ignore_errors=True))\n+        self.target = self.tmp / \"target\"\n+        self.adr_dir = self.target / \"docs\" / \"adr\"\n+        self.adr_dir.mkdir(parents=True)\n+        self.product_bytes = {\n+            f\"{number:04d}-product-decision.md\": (\n+                f\"# Product ADR {number:04d}\\n\\nTarget-owned bytes {number}.\\n\"\n+            ).encode()\n+            for number in PRODUCT_NUMBERS\n+        }\n+        for name, content in self.product_bytes.items():\n+            (self.adr_dir / name).write_bytes(content)\n+\n+    def test_fresh_install_preserves_product_adrs_and_installs_all_sadrs(self) -> None:\n+        _quiet_install(self.target, force=False, validate=True)\n+\n+        for name, expected in self.product_bytes.items():\n+            self.assertEqual((self.adr_dir / name).read_bytes(), expected)\n+\n+        sadr_numbers = {\n+            path.name.split(\"-\", 2)[1]\n+            for path in self.adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\")\n+        }\n+        self.assertEqual(sadr_numbers, {f\"{number:04d}\" for number in range(1, 20)})\n+\n+        numeric_records = {\n+            path.name\n+            for path in self.adr_dir.glob(\"[0-9][0-9][0-9][0-9]-*.md\")\n+        }\n+        self.assertEqual(numeric_records, set(self.product_bytes))\n+        self.assertEqual(find_adr_reference_violations(self.target), [])\n+\n+\n+class LegacySadrUpgradeTests(unittest.TestCase):\n+    def setUp(self) -> None:\n+        self.tmp = Path(tempfile.mkdtemp(prefix=\"shiki-sadr-upgrade-\"))\n+        self.addCleanup(lambda: shutil.rmtree(self.tmp, ignore_errors=True))\n+        self.target = self.tmp / \"target\"\n+        self.adr_dir = self.target / \"docs\" / \"adr\"\n+        self.product_bytes, self.legacy_paths = _create_legacy_upgrade_fixture(\n+            self.target\n+        )\n+\n+    def test_force_upgrade_deletes_only_stamped_legacy_sadrs(self) -> None:\n+        _quiet_install(self.target, force=True, validate=True)\n+\n+        for relative in self.legacy_paths:\n+            self.assertFalse((self.target / relative).exists(), relative)\n+        for name, expected in self.product_bytes.items():\n+            self.assertEqual((self.adr_dir / name).read_bytes(), expected)\n+\n+        sadrs = list(self.adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\"))\n+        self.assertEqual(len(sadrs), 19)\n+        refreshed = json.loads(\n+            (self.target / shiki_installer.INSTALL_STAMP_PATH).read_text(encoding=\"utf-8\")\n+        )\n+        for relative in self.legacy_paths:\n+            self.assertNotIn(relative, refreshed[\"digests\"])\n+        for path in sadrs:\n+            relative = path.relative_to(self.target).as_posix()\n+            self.assertEqual(refreshed[\"digests\"][relative], _sha256(path))\n+\n+    def test_non_force_install_never_deletes_legacy_files(self) -> None:\n+        before = {\n+            relative: (self.target / relative).read_bytes()\n+            for relative in self.legacy_paths\n+        }\n+        _quiet_install(self.target, force=False, validate=False)\n+        for relative, expected in before.items():\n+            self.assertEqual((self.target / relative).read_bytes(), expected)\n+        refreshed = json.loads(\n+            (self.target / shiki_installer.INSTALL_STAMP_PATH).read_text(encoding=\"utf-8\")\n+        )\n+        for relative in self.legacy_paths:\n+            self.assertEqual(refreshed[\"digests\"][relative], _sha256(self.target / relative))\n+\n+\n+class LegacyCleanupPreflightTests(unittest.TestCase):\n+    def setUp(self) -> None:\n+        self.tmp = Path(tempfile.mkdtemp(prefix=\"shiki-sadr-preflight-\"))\n+        self.addCleanup(lambda: shutil.rmtree(self.tmp, ignore_errors=True))\n+\n+    def test_all_ownership_blockers_abort_before_any_write(self) -> None:\n+        cases = (\n+            (\"absent\", \"install stamp is absent\"),\n+            (\"unreadable\", \"install stamp is unreadable\"),\n+            (\"malformed\", \"install stamp is malformed\"),\n+            (\"missing-digest\", \"install stamp has no digest\"),\n+            (\"digest-mismatch\", \"digest mismatch\"),\n+        )\n+        for name, expected_reason in cases:\n+            with self.subTest(name=name):\n+                target = self.tmp / name\n+                _, legacy_paths = _create_legacy_upgrade_fixture(target)\n+                stamp_path = target / shiki_installer.INSTALL_STAMP_PATH\n+\n+                if name == \"absent\":\n+                    stamp_path.unlink()\n+                elif name == \"unreadable\":\n+                    stamp_path.unlink()\n+                    stamp_path.mkdir()\n+                elif name == \"malformed\":\n+                    stamp_path.write_text(\"{not-json\\n\", encoding=\"utf-8\")\n+                else:\n+                    stamp = json.loads(stamp_path.read_text(encoding=\"utf-8\"))\n+                    if name == \"missing-digest\":\n+                        stamp[\"digests\"] = {}\n+                    else:\n+                        stamp[\"digests\"] = {\n+                            relative: \"deadbeef\" for relative in legacy_paths\n+                        }\n+                    stamp_path.write_text(\n+                        json.dumps(stamp, indent=2, sort_keys=True) + \"\\n\",\n+                        encoding=\"utf-8\",\n+                    )\n+\n+                before = _tree_snapshot(target)\n+                with self.assertRaises(shiki_installer.ShikiError) as raised:\n+                    _quiet_install(target, force=True, validate=False)\n+                after = _tree_snapshot(target)\n+\n+                self.assertEqual(before, after, f\"{name} preflight changed the tree\")\n+                message = str(raised.exception)\n+                self.assertIn(expected_reason, message)\n+                self.assertIn(\"No files were written\", message)\n+                for relative in legacy_paths:\n+                    self.assertIn(relative, message)\n+\n+    def test_absent_stamp_is_allowed_when_no_legacy_path_exists(self) -> None:\n+        target = self.tmp / \"no-legacy\"\n+        target.mkdir()\n+        _quiet_install(target, force=True, validate=False)\n+        self.assertTrue(\n+            (target / \"docs\" / \"adr\" / \"SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\").is_file()\n+        )\n+\n+\n+if __name__ == \"__main__\":\n+    unittest.main()\n\ndiff --git a/.codex/skills/shiki/SKILL.md b/.codex/skills/shiki/SKILL.md\nindex c8e2e85d9d75d49cc4f2d6286ee22659c4e53e75..93cded623b2c15d9f65c76491a1c2d813ac9e6c0\n--- a/.codex/skills/shiki/SKILL.md\n+++ b/.codex/skills/shiki/SKILL.md\n@@ -18,6 +18,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n Codex CLI does not register this skill as a custom `/shiki` slash command. When\n the user types `/shiki` in Codex CLI and gets \"Unrecognized command\", explain\n that this is expected: invoke Shiki in Codex with natural language such as\n@@ -69,7 +72,7 @@\n \n ## Responsibilities\n \n-- Claude Code implements and repairs by default (`shiki runner claude`, ADR 0008); Codex implements and repairs tasks explicitly assigned to `codex`.\n+- Claude Code implements and repairs by default (`shiki runner claude`, SADR-0008); Codex implements and repairs tasks explicitly assigned to `codex`.\n - Claude Code Action can act as GitHub-side CCA or reviewer.\n - CCA judges completion.\n - MergeGate authorizes state transitions and merge readiness.\ndiff --git a/.github/prompts/cca-completion-check.md b/.github/prompts/cca-completion-check.md\nindex ed203ad5f302ecc81372d8ab27d2b1232ca97497..fd89950528333ef6242e6d5443904f5392ab7e56\n--- a/.github/prompts/cca-completion-check.md\n+++ b/.github/prompts/cca-completion-check.md\n@@ -19,6 +19,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n ## Required Reading\n \n Read only the files needed to judge the PR contract. Prefer this order:\ndiff --git a/.github/prompts/codex-task-handoff.md b/.github/prompts/codex-task-handoff.md\nindex 695e211d76dea1c60e8d5bb360e78487808286a4..59441d576b01a39569722f226c854e5a740e9eb5\n--- a/.github/prompts/codex-task-handoff.md\n+++ b/.github/prompts/codex-task-handoff.md\n@@ -2,7 +2,7 @@\n \n You are Codex Front acting as the Implementer for a Shiki task.\n \n-Follow `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant ADRs, and `docs/agents/codex-handoff.md`.\n+Follow `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant Shiki SADRs and target ADRs, and `docs/agents/codex-handoff.md`.\n \n Rules:\n \ndiff --git a/AGENTS.md b/AGENTS.md\nindex 7f041100d49c2a7525f51cb0f5d696a4a6d1c78c..a06bfbabc6fa48a007389e25e12d7ed523936bed\n--- a/AGENTS.md\n+++ b/AGENTS.md\n@@ -22,7 +22,7 @@\n - **Codex Front** is an optional implementer through Codex App, Codex CLI, Codex IDE extension, or Codex Web signed in with ChatGPT OAuth/subscription auth, dispatched with `shiki runner codex` when a task is explicitly assigned to `codex`.\n - **GitHub CCA** is implemented by Claude Code Action by default, using `CLAUDE_CODE_OAUTH_TOKEN`.\n \n-Do not assume `openai/codex-action`, `OPENAI_API_KEY`, or API-key based Codex automation in the default Shiki path. API-key runners are explicit target-repo extensions and require their own ADR.\n+Do not assume `openai/codex-action`, `OPENAI_API_KEY`, or API-key based Codex automation in the default Shiki path. API-key runners are explicit target-repo extensions and require their own target ADR.\n \n Shiki is not:\n \n@@ -44,10 +44,14 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use\n+`SADR-NNNN-*.md`; target product decisions retain `NNNN-*.md` and the `ADR`\n+namespace.\n+\n Use the canonical source-of-truth order above. Runtime-specific reading orders\n are operational guidance only and must not override or reorder it.\n \n-Conversation state is not durable truth. If a decision matters, put it in GitHub, `.shiki/`, `CONTEXT.md`, or an ADR.\n+Conversation state is not durable truth. If a decision matters, put it in GitHub, `.shiki/`, `CONTEXT.md`, a Shiki SADR, or a target ADR.\n \n If GitHub and `.shiki/` disagree, surface the conflict and prefer GitHub operational state until the mirror is repaired.\n \n@@ -75,8 +79,8 @@\n Every non-trivial change follows this loop:\n \n 1. **Goal**: clarify outcome, completion conditions, non-goals, risk level, and success signals.\n-2. **grill-with-docs**: challenge the plan against domain docs, ADRs, code reality, terminology, and edge scenarios.\n-3. **Context & Impact**: find relevant docs, ADRs, code, dependencies, owners, locks, and verification surfaces.\n+2. **grill-with-docs**: challenge the plan against domain docs, Shiki SADRs, target ADRs, code reality, terminology, and edge scenarios.\n+3. **Context & Impact**: find relevant docs, SADRs, target ADRs, code, dependencies, owners, locks, and verification surfaces.\n 4. **PRD and Spec Freeze**: use `to-prd` when context is settled enough to become durable product/engineering intent. The operator's explicit approval of the PRD is Spec Freeze: the plan records a `spec_freeze` block, and no plan runs without it. Steps 1-4 form Requirements Definition \u2014 one continuous operator dialogue. After Spec Freeze, scope changes require an operator-approved Spec Amendment; non-scope-moving interpretations go to the Assumption Log.\n 5. **Issues**: use `to-issues` to create independently grabbable vertical-slice issues. Prefer AFK slices over HITL slices where possible.\n 6. **Triage**: label issues for readiness, risk, runtime, skills, and MergeGate state.\n@@ -167,7 +171,7 @@\n - Testing decisions.\n - Out-of-scope items.\n - Further notes.\n-- Links to relevant domain terms and ADRs.\n+- Links to relevant domain terms, SADRs, and target ADRs.\n \n Every executable task must include:\n \n@@ -193,7 +197,7 @@\n \n Context & Impact must identify:\n \n-- Relevant documents, domain terms, ADRs, and past decisions.\n+- Relevant documents, domain terms, SADRs, target ADRs, and past decisions.\n - Relevant modules, interfaces, seams, callers, owners, and tests.\n - Dependency relationships between tasks.\n - Candidate locks and lock conflicts.\n@@ -210,7 +214,7 @@\n | Skill | Required when |\n | --- | --- |\n | `setup-matt-pocock-skills` | First configuring the repo for skills, or when issue tracker, triage labels, or domain docs are missing. |\n-| `grill-with-docs` | Default for non-trivial Goals before PRD/issues; always required when plans, terminology, boundaries, tradeoffs, or ADR-worthy decisions are ambiguous. |\n+| `grill-with-docs` | Default for non-trivial Goals before PRD/issues; always required when plans, terminology, boundaries, tradeoffs, or decision-record-worthy choices are ambiguous. |\n | `zoom-out` | The agent lacks an architectural map of the relevant code area. |\n | `to-prd` | Settled Goal context must become durable product/engineering intent. |\n | `to-issues` | A Goal, PRD, or plan must become independently grabbable vertical-slice issues. |\n@@ -348,7 +352,7 @@\n - Three or more new files.\n - Any high-risk or critical label.\n \n-When the architecture gate triggers, run `grill-with-docs` or `improve-codebase-architecture` as appropriate and record durable decisions in an ADR when needed.\n+When the architecture gate triggers, run `grill-with-docs` or `improve-codebase-architecture` as appropriate and record durable decisions in a Shiki SADR or target ADR as applicable.\n \n ## Engineering Discipline\n \ndiff --git a/CLAUDE.md b/CLAUDE.md\nindex 98a932e7841a632866fcd53b0bad0e3af3711d3d..98a31399404d6fb044b4fd493f4459ac87f439de\n--- a/CLAUDE.md\n+++ b/CLAUDE.md\n@@ -14,6 +14,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n ## Claude Role\n \n Default to planner, default implementer, reviewer, coordinator, documentation editor, CCA-style completion judge, and final-judgment assistant.\n@@ -57,7 +60,7 @@\n At the start of a Shiki session:\n \n 1. Read `AGENTS.md`.\n-2. Read `CONTEXT.md` and relevant ADRs.\n+2. Read `CONTEXT.md` and relevant Shiki SADRs and target ADRs.\n 3. Read active `.shiki/` Goal/task/ledger state when present.\n 4. Read active distilled rules from `.shiki/memories/` (status `distilled`, `active: true`, not revoked or superseded) for applicable guidance; the same rules are injected deterministically into task handoffs as the `## Distilled Rules` section (\u00a73.5).\n 5. Read the GitHub Issue or PR when the task is GitHub-backed.\n@@ -74,7 +77,7 @@\n - Define the Goal, completion conditions, non-goals, and risk level.\n - Treat `grill-with-docs` as the default planning entry point for non-trivial Goals.\n - Challenge domain terms against `CONTEXT.md`.\n-- Challenge hard-to-reverse tradeoffs against ADRs.\n+- Challenge hard-to-reverse tradeoffs against SADRs and target ADRs.\n - Explore code instead of asking when code can answer the question.\n - Run Context & Impact before implementation planning.\n - Produce a Task DAG, not an unordered checklist.\n@@ -94,7 +97,7 @@\n - For each question, provide your recommended answer.\n - Resolve dependency decisions before downstream design questions.\n - Update `CONTEXT.md` inline when terminology crystallizes.\n-- Offer ADRs only when the decision is hard to reverse, surprising without context, and a real tradeoff.\n+- Offer a SADR or target ADR only when the decision is hard to reverse, surprising without context, and a real tradeoff.\n - Convert unresolved questions into HITL blockers rather than allowing Codex to guess.\n \n ## PRD and Issues Standard\n@@ -103,7 +106,7 @@\n \n - Use the project's domain glossary vocabulary.\n - Avoid volatile file paths unless they are essential.\n-- Include implementation decisions, testing decisions, out-of-scope items, and relevant ADR/domain links.\n+- Include implementation decisions, testing decisions, out-of-scope items, and relevant SADR/target-ADR/domain links.\n - Identify deep-module opportunities and testable interfaces.\n \n When creating issues:\n@@ -121,7 +124,7 @@\n - Branch/worktree target.\n - Scope and non-goals.\n - Dependencies and locks.\n-- Relevant docs, ADRs, modules, and tests.\n+- Relevant docs, SADRs, target ADRs, modules, and tests.\n - Required skill invocations.\n - TDD expectations.\n - Pre-PR `code-review` gate expectations.\n@@ -152,7 +155,7 @@\n \n - Treat yourself as a judge, not implementer.\n - Do not write production code.\n-- Read `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant ADRs, PRD, task issue, PR body, diff, checks, reviews, labels, and `.shiki/` state.\n+- Read `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant SADRs and target ADRs, PRD, task issue, PR body, diff, checks, reviews, labels, and `.shiki/` state.\n - Evaluate every applicable checklist in `docs/agents/checklists.md`.\n - Map each acceptance criterion to durable evidence.\n - Distinguish implementation failure from missing evidence.\ndiff --git a/CONTEXT.md b/CONTEXT.md\nindex e61a9be06a0cce0d026484083a657bde2067e234..237ee8aa3ea8fab334ece49768bc5ed3b5af0974\n--- a/CONTEXT.md\n+++ b/CONTEXT.md\n@@ -25,7 +25,7 @@\n _Avoid_: jumping directly into code\n \n **grill-with-docs**:\n-A planning skill that challenges a plan against domain language, ADRs, code reality, and concrete edge scenarios. It resolves design-tree questions before PRD/issues. It runs inside Requirements Definition.\n+A planning skill that challenges a plan against domain language, Shiki SADRs, target ADRs, code reality, and concrete edge scenarios. It resolves design-tree questions before PRD/issues. It runs inside Requirements Definition.\n _Avoid_: generic brainstorming, silent assumptions\n \n **Requirements Definition**:\ndiff --git a/CONTRIBUTING.md b/CONTRIBUTING.md\nindex c6d9b656e73abb8a55e5f660123e03153dcb3e6d..9e37a8fbf668db5b22461aaae5548b8f1354394c\n--- a/CONTRIBUTING.md\n+++ b/CONTRIBUTING.md\n@@ -17,7 +17,7 @@\n 1. **Goal** \u2014 start from a user-approved Goal with an outcome, completion\n    conditions, non-goals, risk level, and success signals.\n 2. **grill-with-docs** \u2014 for any non-trivial change, challenge the plan against\n-   domain language, ADRs, and code reality before writing a PRD.\n+   domain language, Shiki SADRs, target ADRs, and code reality before writing a PRD.\n 3. **PRD** \u2014 capture settled product/engineering intent (`to-prd`).\n 4. **Spec Freeze** \u2014 the operator explicitly approves the PRD/requirements;\n    the plan records a `spec_freeze` block and no plan runs without it\n@@ -94,7 +94,7 @@\n   entries.\n - Do not hand-edit `.shiki/goals/*.json` or `.shiki/manifest.json` in a task PR.\n - Conversation state is not durable truth. If a decision matters, record it in\n-  GitHub, `.shiki/`, `CONTEXT.md`, or an ADR.\n+  GitHub, `.shiki/`, `CONTEXT.md`, a Shiki SADR, or a target ADR.\n \n ## Architecture Gate\n \n@@ -103,7 +103,7 @@\n or CI policy, package structure, three or more structural files, more than 100\n net new lines in a core module, three or more new files, or any high-risk label.\n When triggered, run `grill-with-docs` or `improve-codebase-architecture` and\n-record durable decisions in an ADR when needed (see `docs/adr/`).\n+record durable decisions in a Shiki SADR or target ADR as applicable (see `docs/adr/`).\n \n ## Safety\n \ndiff --git a/SYSTEM_PROMPT.md b/SYSTEM_PROMPT.md\nindex aaaea45e3a896c3a44ba89eec8bb84325ecf6c62..da009c86a88285269dee0155e58e550c6d744ea9\n--- a/SYSTEM_PROMPT.md\n+++ b/SYSTEM_PROMPT.md\n@@ -30,6 +30,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n Use the canonical source-of-truth order above. Runtime-specific reading orders\n are operational guidance only and must not override or reorder it. The current\n conversation is non-durable operator input.\n@@ -48,14 +51,14 @@\n \n A runtime may only do work that is explicitly assigned by the Goal, task, PR, or MergeGate decision.\n \n-Do not assume `openai/codex-action`, `OPENAI_API_KEY`, or API-key based Codex automation in the default Shiki path. API-key runners are explicit target-repo extensions and require their own ADR.\n+Do not assume `openai/codex-action`, `OPENAI_API_KEY`, or API-key based Codex automation in the default Shiki path. API-key runners are explicit target-repo extensions and require their own target ADR.\n \n ## 4. Non-negotiable Shiki flow\n \n For any non-trivial Goal, use this flow:\n \n 1. **Goal Seek**: define outcome, non-goals, risk level, completion criteria, and evidence requirements.\n-2. **grill-with-docs**: challenge the plan against `CONTEXT.md`, ADRs, code reality, terminology, boundaries, and edge scenarios. Ask one question at a time when the operator is available. If code can answer a question, inspect code instead of asking.\n+2. **grill-with-docs**: challenge the plan against `CONTEXT.md`, Shiki SADRs, target ADRs, code reality, terminology, boundaries, and edge scenarios. Ask one question at a time when the operator is available. If code can answer a question, inspect code instead of asking.\n 3. **Context & Impact**: identify modules, interfaces, seams, callers, dependencies, locks, risk, and verification surfaces.\n 4. **to-prd + Spec Freeze**: turn settled context into a PRD using domain vocabulary and testing decisions. The operator's explicit approval of the PRD is Spec Freeze, recorded as the plan's `spec_freeze` block; plans without it do not run. Steps 1-4 form Requirements Definition.\n 5. **to-issues**: decompose the PRD into independently grabbable vertical-slice issues. Prefer AFK slices; mark HITL slices when judgment is still required.\n@@ -75,7 +78,7 @@\n Before making any material change:\n \n 1. Read `AGENTS.md` and any runtime-specific wrapper such as `CLAUDE.md`.\n-2. Read `CONTEXT.md` and relevant ADRs.\n+2. Read `CONTEXT.md` and relevant SADRs and target ADRs.\n 3. Read the GitHub Issue / PR and `.shiki/` state for the active Goal or task.\n 4. Identify your role: Planner, Implementer, Reviewer, Completion Check Agent, Repairer, or Guardian-assist.\n 5. Identify Goal id, task id, branch, PR, dependencies, locks, risk level, required skills, and required evidence.\n@@ -103,7 +106,7 @@\n \n At minimum, identify:\n \n-- Relevant documents, ADRs, domain terms, and prior decisions.\n+- Relevant documents, SADRs, target ADRs, domain terms, and prior decisions.\n - Relevant modules, interfaces, seams, callers, owners, and tests.\n - Dependency relationships between tasks.\n - Candidate file locks and lock conflicts.\n@@ -152,7 +155,7 @@\n \n CCA must read:\n \n-- `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant ADRs.\n+- `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant SADRs and target ADRs.\n - Parent Goal issue, PRD issue, task issue, comments, and labels.\n - PR body, diff, commits, changed files, reviews, CI checks, and artifacts available to the workflow.\n - `.shiki/` task contract, locks, ledger entries, CCA checklist profile, and prior repair packets.\n@@ -214,7 +217,7 @@\n - Three or more new files.\n - Any high-risk or critical label.\n \n-When the architecture gate triggers, run `grill-with-docs` or `improve-codebase-architecture` as appropriate and record the decision in an ADR if it is hard to reverse or likely to be re-litigated.\n+When the architecture gate triggers, run `grill-with-docs` or `improve-codebase-architecture` as appropriate and record the decision in a Shiki SADR or target ADR as applicable if it is hard to reverse or likely to be re-litigated.\n \n ## 14. Skill Gate\n \n@@ -223,7 +226,7 @@\n Use these skills:\n \n - `setup-matt-pocock-skills`: before first use of the engineering skill set, or when issue tracker, triage labels, or domain docs are missing.\n-- `grill-with-docs`: default for non-trivial Goals before PRD/issues; required for ambiguous plans, terminology, boundaries, ADR-worthy tradeoffs, or design-tree decisions.\n+- `grill-with-docs`: default for non-trivial Goals before PRD/issues; required for ambiguous plans, terminology, boundaries, decision-record-worthy tradeoffs, or design-tree decisions.\n - `zoom-out`: unfamiliar code area or missing architectural map.\n - `to-prd`: convert settled context into a PRD.\n - `to-issues`: break a PRD, plan, or Goal into independently grabbable vertical-slice issues.\ndiff --git a/docs/adr/0001-keep-shiki-as-platform-name.md b/docs/adr/SADR-0001-keep-shiki-as-platform-name.md\nindex 20a9637c3b9d8c97de7ba8db7cfae4d82d9a7dba..8b685439959aada1b221c112f0ac3c51ca8a1695\n--- a/docs/adr/0001-keep-shiki-as-platform-name.md\n+++ b/docs/adr/SADR-0001-keep-shiki-as-platform-name.md\n@@ -1,3 +1,3 @@\n-# Keep Shiki As The Platform Name\n+# SADR-0001: Keep Shiki As The Platform Name\n \n Shiki remains the product name for the GitHub-native agentic engineering control plane. We considered more descriptive names such as MergePilot, MergeGate, and Kiroku, but kept Shiki because it carries the existing goal-seeking framework identity while remaining independent of Hermes, Codex, Claude Code, or any single runtime.\ndiff --git a/docs/adr/0002-use-agents-as-shared-constitution.md b/docs/adr/SADR-0002-use-agents-as-shared-constitution.md\nindex 73de30ecb078120eac540d9bc51855e3cd48d0e3..c5486182bf06fca36ea508496b013c67302fd989\n--- a/docs/adr/0002-use-agents-as-shared-constitution.md\n+++ b/docs/adr/SADR-0002-use-agents-as-shared-constitution.md\n@@ -1,3 +1,3 @@\n-# Use AGENTS As The Shared Agent Constitution\n+# SADR-0002: Use AGENTS As The Shared Agent Constitution\n \n Shiki uses `AGENTS.md` as the runtime-neutral constitution and `CLAUDE.md` as a thin Claude Code wrapper. This keeps Codex, Claude Code, Hermes Runner, and future Agent Runtimes aligned on the same GitHub-first MergeGate rules while still allowing runtime-specific instructions where needed.\ndiff --git a/docs/adr/0003-require-skill-gate-for-mergegate-readiness.md b/docs/adr/SADR-0003-require-skill-gate-for-mergegate-readiness.md\nindex 424be0c9af4c8f0136f1fde68a468c47ff84e764..7fc337987c7df9792f75f181dfcbed75fe5ffff2\n--- a/docs/adr/0003-require-skill-gate-for-mergegate-readiness.md\n+++ b/docs/adr/SADR-0003-require-skill-gate-for-mergegate-readiness.md\n@@ -1,3 +1,3 @@\n-# Require Skill Gate For MergeGate Readiness\n+# SADR-0003: Require Skill Gate For MergeGate Readiness\n \n Shiki requires relevant engineering skills to be invoked before execution when their triggers apply. This makes skills part of MergeGate readiness rather than optional prompt style, because Shiki depends on repeatable planning, issue decomposition, TDD, diagnosis, triage, and architecture review before autonomous or parallel agent work can be trusted.\ndiff --git a/docs/adr/0004-use-github-first-with-shiki-mirror.md b/docs/adr/SADR-0004-use-github-first-with-shiki-mirror.md\nindex d0a3a9174dc646a945a9d325954ffaf00c352040..a79c54ae9ee8eed2a0524192b864da4e22a92d3f\n--- a/docs/adr/0004-use-github-first-with-shiki-mirror.md\n+++ b/docs/adr/SADR-0004-use-github-first-with-shiki-mirror.md\n@@ -1,4 +1,4 @@\n-# ADR 0004: Use GitHub First With A Repository-Local Shiki Mirror\n+# SADR-0004: Use GitHub First With A Repository-Local Shiki Mirror\n \n ## Status\n \ndiff --git a/docs/adr/0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md b/docs/adr/SADR-0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\nindex b339baf49b89d9290e79c3d51b36e7b2bde3bd95..0f3c3eb9afccdb3403ce978e21d04fe12e495da0\n--- a/docs/adr/0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\n+++ b/docs/adr/SADR-0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\n@@ -1,8 +1,8 @@\n-# ADR 0005: Use Codex As Front And Claude Code Action As The GitHub Runtime\n+# SADR-0005: Use Codex As Front And Claude Code Action As The GitHub Runtime\n \n ## Status\n \n-Accepted (implementer default superseded by ADR 0008; the auth model and the\n+Accepted (implementer default superseded by SADR-0008; the auth model and the\n Claude Code Action GitHub runtime decision remain in force)\n \n ## Context\n@@ -26,4 +26,4 @@\n - Shiki avoids requiring `OPENAI_API_KEY` for the default workflow.\n - Codex work happens through the user's authenticated Codex front surface, not as a hidden backend Action.\n - Claude Code Action is configured for OAuth-token based subscription usage by default.\n-- API-key based automation remains possible only as an explicit target-repo extension with its own ADR.\n+- API-key based automation remains possible only as an explicit target-repo extension with its own target ADR.\ndiff --git a/docs/adr/0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md b/docs/adr/SADR-0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\nindex 4bcae9d1e52f0a37b8e9fbafa5cd14b6ee1993bd..7a38538a6ae9929567f0ade67e7ea6a60d0b3f7e\n--- a/docs/adr/0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\n+++ b/docs/adr/SADR-0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\n@@ -1,4 +1,4 @@\n-# ADR 0006: Separate Codex Implementation, CCA Completion, And MergeGate Authorization\n+# SADR-0006: Separate Codex Implementation, CCA Completion, And MergeGate Authorization\n \n ## Status\n \ndiff --git a/docs/adr/0007-packaging-and-release.md b/docs/adr/SADR-0007-packaging-and-release.md\nindex 79b84ddb97167372b44641e5b7860e400ce80822..bb445a2e2e856d9d1f5fdbe35de8d4e8cec0fb32\n--- a/docs/adr/0007-packaging-and-release.md\n+++ b/docs/adr/SADR-0007-packaging-and-release.md\n@@ -1,4 +1,4 @@\n-# ADR 0007: Packaging And Release For Shiki\n+# SADR-0007: Packaging And Release For Shiki\n \n ## Status\n \n@@ -25,7 +25,7 @@\n   not introduce third-party runtime dependencies or break the shim.\n - Releases must run inside the operator's subscription-authenticated toolchain\n   and must not require extra secrets beyond the built-in `GITHUB_TOKEN`\n-  (consistent with ADR 0005's no-API-key-by-default stance).\n+  (consistent with SADR-0005's no-API-key-by-default stance).\n - `.shiki/manifest.json` already encodes the install surface (`install.include`,\n   `install.create_directories`, `install.exclude_from_commit`), and it must not\n   be mutated as part of this change.\n@@ -34,7 +34,7 @@\n \n This is a hard-to-reverse platform decision (it sets the version source of\n truth, the release trigger, and the public install contract), so it is recorded\n-as an ADR.\n+as a SADR.\n \n ## Decision\n \n@@ -104,7 +104,7 @@\n 3. **Publishing to PyPI.** Rejected for now: Shiki is distributed as a\n    repository template + git-installable CLI, not a PyPI library; PyPI would add\n    credentials/secrets and a different versioning contract. Can be added later as\n-   its own ADR if a packaged distribution is needed.\n+   its own SADR if a packaged distribution is needed.\n 4. **Dynamic version (read `VERSION` via a build backend `dynamic` field).**\n    Rejected to keep `pyproject.toml` backend-agnostic and dependency-free; the\n    small duplication between `VERSION` and `pyproject.toml` is guarded by the\n@@ -123,4 +123,4 @@\n   releasing versions the whole platform but ships only `install.include` to\n   targets.\n - Future PyPI distribution or a console entry point are deferred and would each\n-  warrant their own ADR.\n+  warrant their own SADR.\ndiff --git a/docs/adr/0008-use-claude-code-as-default-implementer.md b/docs/adr/SADR-0008-use-claude-code-as-default-implementer.md\nindex dedf4510b7c7799521422f622029563d373db031..35c2625d908167fbc58855b5b63e283805f167e8\n--- a/docs/adr/0008-use-claude-code-as-default-implementer.md\n+++ b/docs/adr/SADR-0008-use-claude-code-as-default-implementer.md\n@@ -1,12 +1,12 @@\n-# ADR 0008: Use Claude Code As The Default Implementer Runtime\n+# SADR-0008: Use Claude Code As The Default Implementer Runtime\n \n ## Status\n \n-Proposed (supersedes ADR 0005 for the implementer default; keeps its auth model)\n+Proposed (supersedes SADR-0005 for the implementer default; keeps its auth model)\n \n ## Context\n \n-ADR 0005 made Codex Front the default implementation runtime, with Claude Code\n+SADR-0005 made Codex Front the default implementation runtime, with Claude Code\n restricted to planner and reviewer roles by the runtime registry, and the only\n autonomous implementation adapter was `shiki runner codex`.\n \n@@ -18,13 +18,13 @@\n - The platform's planning, review, and CCA layers already run on Claude Code,\n   and Claude Code's native capabilities (the built-in `/code-review` skill and\n   the Workflow multi-agent orchestration tool) have no Codex equivalent.\n-- The Goal lifecycle redesign (ADR 0009) requires an autonomous post-freeze\n+- The Goal lifecycle redesign (SADR-0009) requires an autonomous post-freeze\n   loop driven from the operator's Claude Code session, including bounded\n   repair dispatch, which must not depend on a runtime that cannot be invoked\n   unattended.\n \n The constitution claims Shiki is runtime-agnostic (\"not a Claude-only\n-workflow\", ADR 0002). Any change to the implementer default must preserve the\n+workflow\", SADR-0002). Any change to the implementer default must preserve the\n registry-based runtime model rather than hardcoding one vendor.\n \n ## Decision\n@@ -39,7 +39,7 @@\n   the task to review or repair-needed.\n - Keep Codex registered as an optional implementer runtime. `shiki runner\n   codex`, the Codex handoff templates, and the ChatGPT OAuth auth model from\n-  ADR 0005 remain supported for tasks explicitly assigned to `codex`.\n+  SADR-0005 remain supported for tasks explicitly assigned to `codex`.\n - Commit to mandating two Claude-native capabilities as loop contract in\n   follow-up Goals (status: the code-review gate was delivered by Goal G-B \u2014\n   skill registry, checklists item PR-12, default required_skills \u2014 while the\n@@ -60,7 +60,7 @@\n \n Out of scope: removing Codex support, changing the CCA runtime (Claude Code\n Action with `CLAUDE_CODE_OAUTH_TOKEN` stays), and API-key based automation\n-(still requires its own ADR per ADR 0005).\n+(still requires its own target ADR per SADR-0005).\n \n ## Consequences\n \n@@ -100,7 +100,7 @@\n   harness rejection of direct `codex exec` is outside Shiki's control, and the\n   plan's mandatory native capabilities cannot run on Codex at all.\n - **Remove Codex entirely**: rejected; it abandons the runtime-agnostic\n-  constitution (ADR 0002) for no operational gain and forecloses\n+  constitution (SADR-0002) for no operational gain and forecloses\n   cross-runtime adversarial review.\n - **GitHub Actions cloud implementation via claude-code-action**: rejected as\n   default; durable evidence is automatic but token cost, Node 24 workflow\ndiff --git a/docs/adr/0009-requirements-definition-and-spec-freeze.md b/docs/adr/SADR-0009-requirements-definition-and-spec-freeze.md\nindex 29b3f7871bef9203a8b4a831a65c39eb2f506e42..52e7dd69ad0aa4efe0b68c62678abbf7df02aaec\n--- a/docs/adr/0009-requirements-definition-and-spec-freeze.md\n+++ b/docs/adr/SADR-0009-requirements-definition-and-spec-freeze.md\n@@ -1,4 +1,4 @@\n-# ADR 0009: Merge Grill And Planning Into Requirements Definition With Spec Freeze\n+# SADR-0009: Merge Grill And Planning Into Requirements Definition With Spec Freeze\n \n ## Status\n \ndiff --git a/docs/adr/0010-external-ai-guardian-review.md b/docs/adr/SADR-0010-external-ai-guardian-review.md\nindex e6108bbe8bb4e73e0ff4684c8f78001a80438c4e..f3510b145664298adafb151b6f0db51b972f01ec\n--- a/docs/adr/0010-external-ai-guardian-review.md\n+++ b/docs/adr/SADR-0010-external-ai-guardian-review.md\n@@ -1,4 +1,4 @@\n-# ADR 0010: External AI Guardian Review As A First-Class Approval Authority\n+# SADR-0010: External AI Guardian Review As A First-Class Approval Authority\n \n ## Status\n \n@@ -8,7 +8,7 @@\n \n Shiki is intended to be an autonomous, recursive, self-improving agent\n framework: authority-in-the-loop, not human-in-the-loop. Until now the Guardian\n-approval required for high/critical-risk merges (guardian-policy.json, ADR\n+approval required for high/critical-risk merges (guardian-policy.json, SADR\n governance) recognized only human authority sources \u2014 a GitHub review approval,\n the `guardian:approved` label applied by a configured Guardian, or a comment\n carrying `Guardian approval granted` plus the current head SHA.\n@@ -89,7 +89,7 @@\n   autonomous design the operator intends and produced the impersonation\n   incident.\n - **Record AI approval as an operator comment**: rejected as forgery of human\n-  authority \u2014 the exact failure this ADR corrects.\n+  authority \u2014 the exact failure this SADR corrects.\n - **Deliver the artifact as a committed `.shiki/` file**: rejected; a committed\n   approval inside the PR being approved is forgeable by anyone who can push.\n - **A dedicated reviewer bot identity instead of a relayed comment**: deferred;\ndiff --git a/docs/adr/0011-autonomous-quality-gate-evidence.md b/docs/adr/SADR-0011-autonomous-quality-gate-evidence.md\nindex bca0246c5d46e44afbde15486a75eec425fc02f5..9c93eac16d8c3a6d4b11bd7224e3955102cd7db4\n--- a/docs/adr/0011-autonomous-quality-gate-evidence.md\n+++ b/docs/adr/SADR-0011-autonomous-quality-gate-evidence.md\n@@ -1,4 +1,4 @@\n-# ADR 0011: Autonomous Quality-Gate Evidence \u2014 Deterministic Facts And An Independent Verifier\n+# SADR-0011: Autonomous Quality-Gate Evidence \u2014 Deterministic Facts And An Independent Verifier\n \n ## Status\n \ndiff --git a/docs/adr/0012-autonomous-loop-closeout-to-main.md b/docs/adr/SADR-0012-autonomous-loop-closeout-to-main.md\nindex 637fad6dde4989c9c2bf0f81f676d8d29991123c..ec7d85f81030a676bcbba28b43eb365422a36eac\n--- a/docs/adr/0012-autonomous-loop-closeout-to-main.md\n+++ b/docs/adr/SADR-0012-autonomous-loop-closeout-to-main.md\n@@ -1,4 +1,4 @@\n-# 0012 \u2014 Autonomous loop pushes goal completion to main via a closeout PR\n+# SADR-0012 \u2014 Autonomous loop pushes goal completion to main via a closeout PR\n \n - Status: accepted\n - Date: 2026-06-18\ndiff --git a/docs/adr/0013-bootstrap-grants-actions-approve-pull-requests.md b/docs/adr/SADR-0013-bootstrap-grants-actions-approve-pull-requests.md\nindex b6c69d64f1c5ced346b88d6859586715b3b5bd0e..cf48ff4a6716267f539ca58edd1a852e7c7a2d64\n--- a/docs/adr/0013-bootstrap-grants-actions-approve-pull-requests.md\n+++ b/docs/adr/SADR-0013-bootstrap-grants-actions-approve-pull-requests.md\n@@ -1,4 +1,4 @@\n-# ADR 0013: Bootstrap Grants GitHub Actions Permission To Create And Approve Pull Requests\n+# SADR-0013: Bootstrap Grants GitHub Actions Permission To Create And Approve Pull Requests\n \n ## Status\n \n@@ -31,7 +31,7 @@\n security tradeoff: it lets repository automation satisfy a human-review gate. In\n a multi-maintainer repo that could let a compromised or buggy workflow\n self-approve changes. So whether bootstrap turns it on by default \u2014 and with\n-what default token scope \u2014 is an ADR-worthy decision, not an implementation\n+what default token scope \u2014 is a SADR-worthy decision, not an implementation\n detail.\n \n ## Decision\n@@ -48,7 +48,7 @@\n Specifically:\n \n - **`can_approve_pull_request_reviews` defaults to `true`.** Shiki's default\n-  posture is solo/AFK autonomous operation (ADR 0008/0009/0012), where the CCA\n+  posture is solo/AFK autonomous operation (SADR-0008/0009/0012), where the CCA\n   Review Bridge is the only path that can satisfy `required_review: true` after\n   the CCA and Guardian gates have already passed. The default makes a\n   freshly-bootstrapped repo able to run the autonomous loop end to end without a\ndiff --git a/docs/adr/0014-codex-app-external-ai-guardian-ui-adapter.md b/docs/adr/SADR-0014-codex-app-external-ai-guardian-ui-adapter.md\nindex 238db06279334caa341c117362df9271382a7fcd..65f12e9939321e1b3e389a34c822fc1549e5155d\n--- a/docs/adr/0014-codex-app-external-ai-guardian-ui-adapter.md\n+++ b/docs/adr/SADR-0014-codex-app-external-ai-guardian-ui-adapter.md\n@@ -1,4 +1,4 @@\n-# ADR 0014: Use Codex App As The External AI Guardian UI Adapter\n+# SADR-0014: Use Codex App As The External AI Guardian UI Adapter\n \n ## Status\n \ndiff --git a/docs/adr/0015-contract-approval-before-dispatch.md b/docs/adr/SADR-0015-contract-approval-before-dispatch.md\nindex fad6c656741a20a7684ba16080a7db159f63a52f..6061271a46d3f1d6ae7174477de3fb75673123a6\n--- a/docs/adr/0015-contract-approval-before-dispatch.md\n+++ b/docs/adr/SADR-0015-contract-approval-before-dispatch.md\n@@ -1,4 +1,4 @@\n-# ADR 0015: Approve The Task Contract Before Dispatch, Not The Diff After It\n+# SADR-0015: Approve The Task Contract Before Dispatch, Not The Diff After It\n \n ## Status\n \n@@ -9,7 +9,7 @@\n Guardian approval for high/critical risk is expressible only on a pull request.\n Every approval source in `guardian-policy.json` \u2014 GitHub review, the\n `guardian:approved` label, the `Guardian approval granted` comment, and the\n-`external_ai_guardian_review` artifact (ADR 0010/0014) \u2014 is bound to a PR, and\n+`external_ai_guardian_review` artifact (SADR-0010/0014) \u2014 is bound to a PR, and\n three of the four require the current head SHA. Approval therefore cannot exist\n before an implementation PR exists.\n \n@@ -81,7 +81,7 @@\n An envelope breach must surface only on the non-repairable policy gate, never on\n the repairable `MergeGate metadata check`. Otherwise the Goal Loop routes it to\n `dispatch_repair` \u2014 instructing a runner to make the breach go away \u2014 which is\n-exactly what this ADR forbids.\n+exactly what this SADR forbids.\n \n **Contract immutability.** After registration, an implementation PR must not\n change the governance fields of its own task contract (`scope`, `non_goals`,\n@@ -116,7 +116,7 @@\n fails any verdict that is not `complete`, and the signal the CCA prompt reads is\n computed from `evaluate_guardian_approval` alone. Without the OR-merge at the\n signal, a contract-approved PR still returns `needs_guardian`, the CCA job fails,\n-and the MergeGate policy check never runs \u2014 the gate this ADR changes would never\n+and the MergeGate policy check never runs \u2014 the gate this SADR changes would never\n be reached. The CCA *prompt* needs no change: a `contract_approval` entry in the\n signal's existing `sources` array satisfies its current wording.\n \n@@ -130,7 +130,7 @@\n written, and an approved contract lets its implementation PRs merge through the\n same automatic path as low/medium risk. The wasted implement-then-block cycle\n disappears. Guardian review shifts from reading diffs to reading contracts, which\n-is both earlier and smaller; the external AI Guardian path (ADR 0010/0014) works\n+is both earlier and smaller; the external AI Guardian path (SADR-0010/0014) works\n on contracts with no change.\n \n We accept that the Guardian authorizes a class of change without seeing its\n@@ -146,7 +146,7 @@\n adversarial tests for the new approval path equivalent in rigor to\n `test_shiki_governance_evidence.sh`.\n \n-Bootstrap costs, which apply only while this ADR is being implemented. The tasks\n+Bootstrap costs, which apply only while this SADR is being implemented. The tasks\n that build Contract Approval merge under the existing rules, so the ones\n dispatched after the pre-dispatch gate lands but before the approval path lands\n need a Guardian-approved Contract PR *and* a live Guardian approval on their\n@@ -210,7 +210,7 @@\n risk; they do not bound authorized work.\n \n **Auto-escalate risk on envelope breach instead of stopping.** Returns the\n-workflow to a diff-time Guardian decision \u2014 the exact behavior this ADR removes.\n+workflow to a diff-time Guardian decision \u2014 the exact behavior this SADR removes.\n \n **Resolve governance fields from the base snapshot only.** Rejected against the\n registration flow: implementation PRs add their own contract, so the base has no\ndiff --git a/docs/adr/0016-locks-bound-product-paths.md b/docs/adr/SADR-0016-locks-bound-product-paths.md\nindex b5e850f3f1f7f4ef6e7f9d1763a29a51bd300c45..46b40a9b99a7aae644c02bb458f5473ff0a90ce2\n--- a/docs/adr/0016-locks-bound-product-paths.md\n+++ b/docs/adr/SADR-0016-locks-bound-product-paths.md\n@@ -1,4 +1,4 @@\n-# ADR 0016: Locks bound product paths; the Shiki mirror is bound by record identity\n+# SADR-0016: Locks bound product paths; the Shiki mirror is bound by record identity\n \n ## Status\n \n@@ -6,8 +6,8 @@\n \n - Date: 2026-07-28\n - Deciders: operator (mizutani-140), Claude (planner)\n-- Related: ADR 0004 (GitHub-first with a `.shiki` mirror), ADR 0009 (Spec Freeze),\n-  ADR 0012 (autonomous loop closeout to main), ADR 0015 (Contract Approval before\n+- Related: SADR-0004 (GitHub-first with a `.shiki` mirror), SADR-0009 (Spec Freeze),\n+  SADR-0012 (autonomous loop closeout to main), SADR-0015 (Contract Approval before\n   dispatch), remediation goal task R-02, the live PR #179 deadlock.\n \n ## Context\n@@ -90,11 +90,11 @@\n files, documentation files and named source modules are nameable; runtime-\n allocated ledger, runner and report ids are not \u2014 and those live in `.shiki`,\n which locks no longer govern. Existing frozen contracts are brought into line by\n-a one-time Spec Amendment per plan, which must land before ADR 0015's contract\n+a one-time Spec Amendment per plan, which must land before SADR-0015's contract\n immutability makes `locks` unwritable from the implementation PR.\n \n **Lock state is resolved from the durable side.** MergeGate resolves the lock\n-registry, and ADR 0015's governance fields, from the base `.shiki` snapshot when\n+registry, and SADR-0015's governance fields, from the base `.shiki` snapshot when\n present, falling back to head only when absent. The `MergeGate metadata check`\n job is given `--base-shiki`, which it does not receive today. Locks are not\n derived from the live open-pull-request set: a pull request body is untrusted\n@@ -108,7 +108,7 @@\n Explicitly out of scope: moving MergeGate's contract resolution off the PR\n checkout wholesale; making the mirror a projection of GitHub state; introducing\n new lock kinds; moving ledger evidence out of the repository; and unwinding\n-ADR 0012's closeout pull request. Registration of multi-task goals is ADR 0015's\n+SADR-0012's closeout pull request. Registration of multi-task goals is SADR-0015's\n Contract PR and is not re-decided here.\n \n ## Consequences\n@@ -127,7 +127,7 @@\n \n Risks accepted. First, goal-scoped identity is only as strong as `goal_id`,\n which is resolved from the PR body and the head task file; the mitigation is to\n-add `goal_id` to ADR 0015's frozen governance field set, and until that lands a\n+add `goal_id` to SADR-0015's frozen governance field set, and until that lands a\n task file can name a foreign goal. Second, base-when-present resolution is\n unavailable on fork pull requests, where the head is contributor-controlled;\n fork pull requests already cannot complete a Shiki merge, and this decision does\n@@ -139,7 +139,7 @@\n path, not by implementation pull requests.\n \n New obligations: one Spec Amendment per frozen plan to rewrite `locks`, ordered\n-before ADR 0015 T1; extension of the adversarial suites in\n+before SADR-0015 T1; extension of the adversarial suites in\n `scripts/test_shiki_adversarial_state.sh` and\n `scripts/test_shiki_governance_evidence.sh` to cover the eleven newly-scoped\n directories; and updating `scripts/test_shiki_loop_lock_guard.sh`,\n@@ -151,32 +151,32 @@\n whether the loop's transitive sync of ledger `evidence` references can be\n deleted once the Contract PR carries its own source plan.\n \n-## Relationship to ADR 0004\n+## Relationship to SADR-0004\n \n-This ADR **clarifies something ADR 0004 left unspecified**. ADR 0004 decides that\n+This SADR **clarifies something SADR-0004 left unspecified**. SADR-0004 decides that\n GitHub is the operational source of truth and that each target repository keeps a\n `.shiki/` mirror. It says nothing about pull requests writing the mirror, does\n not name MergeGate, and does not make the mirror an input to a gate. Nothing in\n-ADR 0004's Decision or Consequences is amended, superseded or weakened: the\n+SADR-0004's Decision or Consequences is amended, superseded or weakened: the\n mirror still exists, still records the same artifacts, and pull request\n-boundaries remain the coordination surface. What this ADR settles is the question\n+boundaries remain the coordination surface. What this SADR settles is the question\n 0004 never reached \u2014 by what authority a pull request may write a mirror record \u2014\n and it answers it the way `.shiki/manifest.json` already declares\n (`pr_mutation: current-task-or-current-goal-only`).\n \n-It **amends ADR 0012**, whose Decision item 5 states that \"Loop-executed tasks\n+It **amends SADR-0012**, whose Decision item 5 states that \"Loop-executed tasks\n declare the `path:.shiki/**` lock, which covers every `.shiki` file the closeout\n stages ... so `files_outside_locks` is satisfied without per-file lock\n bookkeeping.\" That clause is replaced: the closeout's mirror writes are\n-authorized by record identity, not by a declared lock. ADR 0012's substantive\n+authorized by record identity, not by a declared lock. SADR-0012's substantive\n decision \u2014 that completion reaches `main` through a closeout pull request rather\n than local attestation \u2014 is preserved unchanged.\n \n-It is **complementary to ADR 0015**, not a substitute. ADR 0015 owns contract\n+It is **complementary to SADR-0015**, not a substitute. SADR-0015 owns contract\n immutability, the pre-dispatch gate, the Contract PR and the Scope Envelope. This\n-ADR owns what a lock means and who may write the mirror. The one ordering\n+SADR owns what a lock means and who may write the mirror. The one ordering\n constraint between them is that the lock-narrowing Spec Amendment must precede\n-ADR 0015 T1's freezing of the `locks` field.\n+SADR-0015 T1's freezing of the `locks` field.\n \n ## Alternatives Considered\n \n@@ -186,7 +186,7 @@\n the only writer that can register them may run. Migrating the Guardian signal to\n that store makes every registration PR fail closed to `needs_guardian` at every\n risk level, because the task file is by definition absent from the store, which\n-removes the low/medium autonomous path ADR 0008/0009 exists to provide. And it\n+removes the low/medium autonomous path SADR-0008/0009 exists to provide. And it\n leaves schemas, manifest, config, guardian policy, templates, migrations, plans,\n worktrees, runner records, runs and handoffs with no writer at all in a\n repository whose product is `.shiki`. Measured benefit on the frozen backlog:\n@@ -199,14 +199,14 @@\n namespace is not in fact ownership-partitioned \u2014 56 ledger entries carry no\n `task_id`, and one goal DAG is writable by 35 registered tasks. The correct\n expression of its insight is that locks do not govern the mirror at all, which is\n-what this ADR decides.\n+what this SADR decides.\n \n **Make the mirror a projection written only by a registration writer and a\n post-merge projection job.** Rejected. It requires a bypass actor with write\n access to the protected default branch; it makes its own master invariant\n readable from a manifest that pull requests may edit; it deletes both reconcile\n modes and leaves a corrupted mirror with no in-band repair path, contradicting\n-ADR 0004's consequence that mirror conflicts are repaired against GitHub state;\n+SADR-0004's consequence that mirror conflicts are repaired against GitHub state;\n and its content-derived ids are rejected by `validate_shiki`'s currently-green id\n pattern.\n \ndiff --git a/docs/adr/0017-bookkeeping-closeouts-do-not-inherit-risk.md b/docs/adr/SADR-0017-bookkeeping-closeouts-do-not-inherit-risk.md\nindex 554e3a0d526a72ca067108c5464cc68c3edcf174..b9676857a4713850e063af7c39c8e0b01ecb4c66\n--- a/docs/adr/0017-bookkeeping-closeouts-do-not-inherit-risk.md\n+++ b/docs/adr/SADR-0017-bookkeeping-closeouts-do-not-inherit-risk.md\n@@ -1,4 +1,4 @@\n-# ADR 0017: A Bookkeeping Closeout Does Not Inherit Its Task's Risk\n+# SADR-0017: A Bookkeeping Closeout Does Not Inherit Its Task's Risk\n \n ## Status\n \n@@ -6,7 +6,7 @@\n \n ## Context\n \n-ADR 0012 made the goal loop push a task's completion to the default branch through a\n+SADR-0012 made the goal loop push a task's completion to the default branch through a\n closeout pull request: after the implementation PR merges, the loop opens a second PR\n carrying `task.status = done`, `lock.state = released`, and \u2014 when the task completes\n its goal \u2014 `goal.status = complete` plus one scorecard report. Nothing else. The\n@@ -49,14 +49,14 @@\n 4. The task file's governance fields (`scope`, `non_goals`, `required_skills`,\n    `risk_level`, `locks`, `acceptance_checks`, `test_command`) are byte-identical to\n    the base snapshot. Only `status`, `expected_pr`, `closeout_pr`, `ledger_evidence`\n-   and `expected_branch` may differ \u2014 the ADR 0015 contract-immutability rule,\n+   and `expected_branch` may differ \u2014 the SADR-0015 contract-immutability rule,\n    unchanged.\n \n    `expected_branch` is in that set because a closeout is *by construction* on a\n    different branch from the implementation it closes, and the loop rewrites the\n    field accordingly. It is safe to let it differ because MergeGate independently\n    requires `expected_branch` to equal the PR's actual head ref, so a PR cannot set\n-   it to anything other than the branch it is really on. The first draft of this ADR\n+   it to anything other than the branch it is really on. The first draft of this SADR\n    omitted it, which made the exemption unreachable: every real closeout failed\n    condition 4 on a field the loop always changes. See PR #209.\n 5. The state transitions are exactly the terminal set: task `review` \u2192 `done`, lock\n@@ -113,12 +113,12 @@\n classification must be derived, not asserted.\n \n **Fold the closeout into the implementation PR** so no second PR exists. Rejected under\n-ADR 0012, which recorded why: a task file that is already `done` on the implementation\n+SADR-0012, which recorded why: a task file that is already `done` on the implementation\n branch short-circuits the loop's review \u2192 CCA \u2192 merge \u2192 done ordering into a local\n goal-completion before any merge happens.\n \n **Have the loop auto-merge closeouts with elevated credentials.** Rejected outright: that\n-is a runtime authoring its own approval, which ADR 0010 and the constitution's\n+is a runtime authoring its own approval, which SADR-0010 and the constitution's\n approval-record prohibition forbid. It would also be indistinguishable, in the evidence,\n from the forgery those rules exist to prevent.\n \ndiff --git a/docs/adr/0018-carried-guardian-approval-across-a-proven-base-sync.md b/docs/adr/SADR-0018-carried-guardian-approval-across-a-proven-base-sync.md\nindex 22358562da390ed8b45cd600da08a43d378b4680..e6e2907cefa776460af73ddf7d39db605f3c39b0\n--- a/docs/adr/0018-carried-guardian-approval-across-a-proven-base-sync.md\n+++ b/docs/adr/SADR-0018-carried-guardian-approval-across-a-proven-base-sync.md\n@@ -1,4 +1,4 @@\n-# ADR 0018: Carried Guardian Approval Across A Proven Base Sync\n+# SADR-0018: Carried Guardian Approval Across A Proven Base Sync\n \n ## Status\n \n@@ -34,7 +34,7 @@\n Guardian evaluator that consumes it (`shiki_guardian.evaluate_guardian_approval`,\n the `guardian_comment_carried` source) were built and tested in isolation. Neither\n does anything until a caller passes the flags that turn the carry on, so the\n-feature was invisible in CI. This ADR records the decision the two of them\n+feature was invisible in CI. This SADR records the decision the two of them\n implement, and this task wires it into the four CI invocations that judge Guardian\n approval.\n \ndiff --git a/docs/adr/README.md b/docs/adr/README.md\nindex c70e057a4520c36e042ab68c53cd4a21ef709b71..5da43856836e2ce6a150f8127766e2f88355ab4b\n--- a/docs/adr/README.md\n+++ b/docs/adr/README.md\n@@ -1,40 +1,41 @@\n-# Architecture Decision Records\n+# Decision Record Namespaces\n \n-This directory holds Shiki's Architecture Decision Records (ADRs): durable\n-records of hard-to-reverse platform decisions. ADRs are part of the Shiki source\n-of truth alongside GitHub state, the `.shiki/` mirror, and `CONTEXT.md`.\n+This directory holds two independent decision namespaces:\n \n-## How ADRs Work\n+- Shiki Architecture Decision Records (SADRs), named\n+  `SADR-NNNN-<short-slug>.md`, record hard-to-reverse Shiki platform decisions.\n+- Target Architecture Decision Records (ADRs), named\n+  `NNNN-<short-slug>.md`, belong to the target product.\n \n-- New ADRs are added as `docs/adr/NNNN-<short-slug>.md`, where `NNNN` is the next\n-  zero-padded sequence number.\n-- The **canonical list of ADRs is the set of files matching `docs/adr/*.md`** in\n-  this directory. Browse `docs/adr/*.md` for the authoritative, current set.\n-- Use [`template.md`](template.md) as the starting point for a new ADR.\n-- Record an ADR when a decision is hard to reverse, surprising without context,\n-  and a real tradeoff. Do not write an ADR for routine or easily reversible\n-  choices.\n+The same four-digit number may appear once in each namespace. A platform\n+`SADR-NNNN` citation resolves only to `SADR-NNNN-*.md`; a target `ADR NNNN`\n+citation resolves only to `NNNN-*.md`.\n \n-Using the `docs/adr/*.md` glob as the source of truth means other PRs can add an\n-ADR without editing this index file, avoiding merge contention on a\n-hand-maintained enumeration.\n+## How Decision Records Work\n \n-## Current ADRs (snapshot)\n-\n-The list below is a convenience snapshot and may lag behind the directory. When\n-in doubt, list `docs/adr/*.md`.\n+- New Shiki platform decisions use the next zero-padded SADR number.\n+- New target product decisions use the target's own ADR sequence.\n+- The canonical list is the files in `docs/adr/`; no hand-maintained index is\n+  authoritative.\n+- Use [`template.md`](template.md) as the starting point and choose the filename\n+  namespace that owns the decision.\n+- Record a decision when it is hard to reverse, surprising without context, and\n+  a real tradeoff. Do not write a decision record for routine or easily\n+  reversible choices.\n \n-- [0001 \u2014 Keep Shiki As The Platform Name](0001-keep-shiki-as-platform-name.md)\n-- [0002 \u2014 Use AGENTS As The Shared Agent Constitution](0002-use-agents-as-shared-constitution.md)\n-- [0003 \u2014 Require Skill Gate For MergeGate Readiness](0003-require-skill-gate-for-mergegate-readiness.md)\n-- [0004 \u2014 Use GitHub First With A Repository-Local Shiki Mirror](0004-use-github-first-with-shiki-mirror.md)\n-- [0005 \u2014 Use Codex As Front And Claude Code Action As The GitHub Runtime](0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md)\n-- [0006 \u2014 Separate Codex Implementation, CCA Completion, And MergeGate Authorization](0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md)\n+Using the directory as the source of truth lets independent changes add records\n+without contending on an enumerated index.\n \n-## Adding A New ADR\n+## Adding A Shiki SADR\n \n-1. Copy `template.md` to `docs/adr/NNNN-<short-slug>.md`.\n+1. Copy `template.md` to `docs/adr/SADR-NNNN-<short-slug>.md`.\n 2. Fill in the title, status, context, decision, and consequences.\n-3. Set the status to `Proposed`, then `Accepted` once the decision is approved.\n-4. Open a PR following `CONTRIBUTING.md`. You do not need to edit this index for\n-   the ADR to be discoverable via `docs/adr/*.md`.\n+3. Set the status to `Proposed`, then `Accepted` once the proper authority has\n+   approved the decision.\n+4. Open a PR following `CONTRIBUTING.md`.\n+\n+## Adding A Target ADR\n+\n+1. Copy `template.md` to `docs/adr/NNNN-<short-slug>.md` using the target's own\n+   sequence.\n+2. Cite it as `ADR NNNN`; do not prefix its filename with `SADR-`.\ndiff --git a/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md b/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\nnew file mode 100644\nindex 0000000000000000000000000000000000000000..7455ce1f3bed02ddddb851ac21a1d1a648c9aeba\n--- /dev/null\n+++ b/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\n@@ -0,0 +1,55 @@\n+# SADR-0019: Separate Shiki Platform SADRs From Target ADRs\n+\n+## Status\n+\n+Accepted\n+\n+## Context\n+\n+Shiki installs its platform decision records into `docs/adr/` in every target\n+repository. Shiki and its targets previously used the same numeric\n+`NNNN-<slug>.md` namespace. A target that already owned ADRs 0013 through 0018\n+therefore acquired two records for each number during installation, making an\n+`ADR NNNN` citation ambiguous. Renumbering target decisions would rewrite target\n+history, while renumbering Shiki decisions per target would destroy their stable\n+platform identity.\n+\n+Upgrades also need to remove the former Shiki-owned numeric paths without ever\n+mistaking a target-owned ADR for platform content. The install stamp already\n+records exact shipped paths and content digests, so it can prove ownership only\n+when both still match.\n+\n+## Decision\n+\n+Shiki platform decisions use the `SADR` namespace:\n+\n+- records are named `docs/adr/SADR-NNNN-<slug>.md`;\n+- live platform citations use `SADR-NNNN`;\n+- target product decisions retain `docs/adr/NNNN-<slug>.md` and `ADR NNNN`;\n+- the reference validator resolves the two namespaces independently, so the same\n+  four-digit number may exist once in each namespace.\n+\n+Before a forced upgrade writes anything, the installer inventories the exact\n+former numeric Shiki ADR paths. It may delete a legacy path only when the\n+existing install stamp contains that exact path and its recorded SHA-256 digest\n+matches the file currently on disk. All ownership blockers are reported\n+together and abort the upgrade before its first write. A non-forced install\n+never performs this cleanup.\n+\n+Historical `.shiki/` evidence remains unchanged because it records the namespace\n+that existed when that evidence was produced.\n+\n+## Consequences\n+\n+- Targets may use any ADR number without colliding with Shiki's platform SADRs.\n+- Fresh installs preserve target ADR bytes and install a stable SADR set.\n+- Forced upgrades fail closed when legacy ownership cannot be proven, and a\n+  successful upgrade refreshes the stamp without removed legacy paths.\n+- Current governance, packaging, workflow, prompt, script, and skill references\n+  must move with the platform records.\n+\n+## Alternatives Considered\n+\n+- Collision-only refusal leaves affected targets permanently unupgradable.\n+- Renumbering target ADRs mutates target-owned history.\n+- Per-target Shiki renumbering makes platform citations non-portable.\ndiff --git a/docs/adr/template.md b/docs/adr/template.md\nindex a92a97daddd0b00ab9dd626cc8d86942da2eafed..17dd79175b5fee2105935557f691b40cf9492942\n--- a/docs/adr/template.md\n+++ b/docs/adr/template.md\n@@ -1,14 +1,14 @@\n-# NNNN: <Short Decision Title>\n+# <SADR-NNNN or NNNN>: <Short Decision Title>\n \n ## Status\n \n-Proposed | Accepted | Superseded by ADR-NNNN\n+Proposed | Accepted | Superseded by SADR-NNNN or target ADR NNNN\n \n ## Context\n \n What is the situation, constraint, or force that requires a decision? Reference\n-relevant domain terms (`CONTEXT.md`), prior ADRs, and code reality. State the\n-problem, not the solution.\n+relevant domain terms (`CONTEXT.md`), prior Shiki SADRs or target ADRs, and code\n+reality. State the problem, not the solution.\n \n ## Decision\n \ndiff --git a/docs/agents/autonomous-loop-e2e-acceptance.md b/docs/agents/autonomous-loop-e2e-acceptance.md\nindex bbf84c6081987f159ac6ad9ecf3c82b7aa72e7e8..5c13c1d02a05a90b0dc46cbef7c95a9846d0c237\n--- a/docs/agents/autonomous-loop-e2e-acceptance.md\n+++ b/docs/agents/autonomous-loop-e2e-acceptance.md\n@@ -40,8 +40,8 @@\n 3. `gh auth status` is logged in with merge permission on the repo.\n 4. Branch protection + required checks are configured exactly as the goal-loop\n    required-check set expects (see `docs/agents/branch-protection-smoke.md`).\n-5. The repo `.shiki/policy` permits autonomous low/medium-risk merges (ADR\n-   0008/0009); the MergeGate policy check is the recorded-authority gate.\n+5. The repo `.shiki/policy` permits autonomous low/medium-risk merges\n+   (SADR-0008/0009); the MergeGate policy check is the recorded-authority gate.\n \n The operator's only actions are: register the task and start the loop. **No\n manual pre-clearing** means after `shiki loop run` starts, the operator does not\ndiff --git a/docs/agents/bootstrap-command.md b/docs/agents/bootstrap-command.md\nindex 05f7f203a52cf412b9f97084a6e131ed0f785afd..f7aa0b734dfebee4a3b139335b66ee3415f48713\n--- a/docs/agents/bootstrap-command.md\n+++ b/docs/agents/bootstrap-command.md\n@@ -189,7 +189,9 @@\n ```\n \n Use this only for tests, fixtures, or explicit local-only template inspection.\n-Use `--force` only when you intentionally want to overwrite existing target files.\n+Use `--force` only when you intentionally want to refresh the target template.\n+Legacy numeric Shiki decision cleanup is stamp-and-digest authorized before any\n+write; target ADR paths are not cleanup candidates.\n \n ## Slash Command\n \ndiff --git a/docs/agents/checklists.md b/docs/agents/checklists.md\nindex 4a7f03ade346f53a1c1d82981f3fd744f9ab4547..0c5ec6c53dc7197b7b7a7d18e667b325babd6c45\n--- a/docs/agents/checklists.md\n+++ b/docs/agents/checklists.md\n@@ -32,18 +32,18 @@\n | ID | Item | Blocking |\n | --- | --- | --- |\n | GD-01 | Plan was challenged against `CONTEXT.md`. | blocking for non-trivial Goals |\n-| GD-02 | Relevant ADRs were read or absence was recorded. | blocking for non-trivial Goals |\n+| GD-02 | Relevant SADRs and target ADRs were read or absence was recorded. | blocking for non-trivial Goals |\n | GD-03 | Ambiguous terms were resolved or converted to HITL blockers. | blocking |\n | GD-04 | Edge scenarios were considered. | advisory |\n | GD-05 | Code reality was checked when code could answer. | blocking when applicable |\n | GD-06 | New domain terms were added to `CONTEXT.md` when needed. | blocking when applicable |\n-| GD-07 | ADR was created or explicitly skipped using the ADR criteria. | blocking when applicable |\n+| GD-07 | A SADR or target ADR was created or explicitly skipped using the decision-record criteria. | blocking when applicable |\n \n ## CI \u2014 Context & Impact Checklist\n \n | ID | Item | Blocking |\n | --- | --- | --- |\n-| CI-01 | Relevant docs and ADRs identified. | blocking |\n+| CI-01 | Relevant docs, SADRs, and target ADRs identified. | blocking |\n | CI-02 | Relevant modules, interfaces, seams, callers, and tests identified. | blocking |\n | CI-03 | Dependencies and blocked-by relationships identified. | blocking |\n | CI-04 | Candidate locks identified. | blocking |\n@@ -62,7 +62,7 @@\n | PRD-04 | Implementation decisions are recorded without volatile code detail. | blocking |\n | PRD-05 | Testing decisions are recorded. | blocking |\n | PRD-06 | Out-of-scope items are explicit. | blocking |\n-| PRD-07 | Relevant glossary/ADR links are present when applicable. | blocking |\n+| PRD-07 | Relevant glossary/SADR/target-ADR links are present when applicable. | blocking |\n \n ## SF \u2014 Spec Freeze Checklist\n \n@@ -98,7 +98,7 @@\n | H-02 | Branch/worktree target is present. | blocking |\n | H-03 | Scope and non-goals are present. | blocking |\n | H-04 | Dependencies and locks are present. | blocking |\n-| H-05 | Relevant docs, ADRs, modules, and tests are referenced. | blocking |\n+| H-05 | Relevant docs, SADRs, target ADRs, modules, and tests are referenced. | blocking |\n | H-06 | Required skills are stated. | blocking |\n | H-07 | TDD expectations are stated. | blocking when behavior work |\n | H-08 | Acceptance criteria are included. | blocking |\ndiff --git a/docs/agents/codex-handoff.md b/docs/agents/codex-handoff.md\nindex ede468f47cb277f69a857e8c45df703e4ed0151d..6f97bdffb67e44d7edea2c71e01ea28933c4eb6c\n--- a/docs/agents/codex-handoff.md\n+++ b/docs/agents/codex-handoff.md\n@@ -46,7 +46,7 @@\n \n ## Context\n - Domain terms:\n-- Relevant ADRs:\n+- Relevant SADRs and target ADRs:\n - Relevant docs:\n - Relevant modules/interfaces/seams:\n - Similar tests/prior art:\ndiff --git a/docs/agents/completion-check-agent.md b/docs/agents/completion-check-agent.md\nindex 13e4c035db35025f4e3cdcc5adfb24314ac4d521..61bfa1218cf36ca605250c91919e620f33238aa0\n--- a/docs/agents/completion-check-agent.md\n+++ b/docs/agents/completion-check-agent.md\n@@ -32,7 +32,7 @@\n - `AGENTS.md`\n - `CLAUDE.md`\n - `CONTEXT.md`\n-- relevant ADRs\n+- relevant SADRs and target ADRs\n - parent Goal issue\n - PRD issue or document\n - task issue\ndiff --git a/docs/agents/decision-control.md b/docs/agents/decision-control.md\nindex 7d8ee5ab03a5ccbcbd04056ae61de03785a2a9c9..8c94ecda00fffaa0e92c325ab83aa9776bd1dac1\n--- a/docs/agents/decision-control.md\n+++ b/docs/agents/decision-control.md\n@@ -11,7 +11,7 @@\n | Decision | Owner | When | Required inputs | Output | Enforcement surface |\n | --- | --- | --- | --- | --- | --- |\n | Is the Goal clear enough to plan? | Planner + Guardian when needed | Goal intake | User request, existing docs, risk signals | Goal readiness verdict | Goal checklist, issue template, ledger |\n-| Must `grill-with-docs` run? | Planner / Skill Gate | Before PRD or issue split | Ambiguity, domain terms, ADR conflicts, unclear boundaries | Skill invocation or explicit skip rationale | Skill Gate checklist |\n+| Must `grill-with-docs` run? | Planner / Skill Gate | Before PRD or issue split | Ambiguity, domain terms, SADR/target-ADR conflicts, unclear boundaries | Skill invocation or explicit skip rationale | Skill Gate checklist |\n | Is a PRD required? | Planner | After Goal context stabilizes | Goal, `grill-with-docs` output, Context & Impact output | PRD required / not required | PRD checklist, CCA evidence check |\n | Can the spec be frozen? | Operator only | End of Requirements Definition, before issue split | PRD/requirements summary, scope inventory (SF-02), open-question list | `spec_freeze` block in the plan + ledger evidence | `require_grilled_plan` (plan ingest/run), SF checklist |\n | Can a frozen spec change? | Operator only (Spec Amendment) | When implementation contradicts the frozen spec | Contested decisions, scoped re-grill result | recorded amendment + re-stamped freeze | SF checklist, ledger evidence |\ndiff --git a/docs/agents/domain.md b/docs/agents/domain.md\nindex 258da8ea81e87ac0a6b0b5b5e5c2fde980eaabb7..d57b0cf17ff3e1fa9e6e5885372bf083d8295421\n--- a/docs/agents/domain.md\n+++ b/docs/agents/domain.md\n@@ -5,7 +5,9 @@\n ## Layout\n \n - `CONTEXT.md` defines Shiki language. Keep it implementation-free.\n-- `docs/adr/` records hard-to-reverse decisions.\n+- `docs/adr/SADR-NNNN-*.md` records Shiki platform decisions.\n+- `docs/adr/NNNN-*.md` records target product decisions without sharing the\n+  SADR namespace.\n - `AGENTS.md` is the runtime-neutral agent constitution.\n - `CLAUDE.md` is the Claude Code wrapper around `AGENTS.md`.\n - `.shiki/` is the Target Repository mirror for Goals, PRDs, plans, Task DAGs, contracts, locks, worktree records, ledger entries, CCA verdicts, repair packets, reports, and handoffs.\n@@ -15,6 +17,6 @@\n \n - Use Shiki terms exactly when they exist.\n - Add glossary terms to `CONTEXT.md` only when the term is part of the domain, not a generic programming concept.\n-- Create ADRs only for hard-to-reverse, surprising, tradeoff-based decisions.\n+- Create SADRs or target ADRs only for hard-to-reverse, surprising, tradeoff-based decisions.\n - Keep product-specific language in the Target Repository, not in Shiki Template docs.\n-- If an ADR conflicts with a proposed implementation, surface the conflict before editing.\n+- If a SADR or target ADR conflicts with a proposed implementation, surface the conflict before editing.\ndiff --git a/docs/agents/execution-control.md b/docs/agents/execution-control.md\nindex ad8ee3cdb36be56fedc1f46408ca92b387d74047..bc63c3a73661d12b567016ba299f1a6b66bf9088\n--- a/docs/agents/execution-control.md\n+++ b/docs/agents/execution-control.md\n@@ -63,7 +63,7 @@\n | Branch name | Orchestrator | Orchestrator | Before implementer dispatch | branch policy | branch ref in task/ledger |\n | Lock reservation | Context & Impact | MergeGate | Before dispatch and before merge | `.shiki/locks/`, labels, PR metadata | lock id, owner, scope |\n | CI profile | Planner | MergeGate | During issue triage, rechecked at PR | labels, `.shiki/policy.yaml`, workflow matrix | required check list |\n-| Required status checks | Repo maintainer / Guardian | GitHub branch protection / rulesets | Repository setup, updated via ADR | GitHub settings | branch/ruleset config, ADR |\n+| Required status checks | Repo maintainer / Guardian | GitHub branch protection / rulesets | Repository setup, updated via target ADR | GitHub settings | branch/ruleset config, target ADR |\n | CCA judgment | CCA | GitHub required check / MergeGate | Every non-draft PR update | GitHub Action check | structured CCA verdict |\n | Repair assignment | CCA | MergeGate | After failed CCA/CI/review | repair packet, issue/PR comment | repair id and attempt count |\n | Merge readiness | MergeGate | GitHub branch protection / Guardian if needed | After CCA complete | required checks, review rules, rulesets | mergegate check output |\ndiff --git a/docs/agents/guardian-policy.md b/docs/agents/guardian-policy.md\nindex 4147c7adf05a47adb4fd24ada1a1c2e1f2ba6d2d..a20d9cc6c00a569bb057b9ca8d6b51c7fc4a9847\n--- a/docs/agents/guardian-policy.md\n+++ b/docs/agents/guardian-policy.md\n@@ -98,8 +98,8 @@\n `origin/<default_branch>` and reuses each job's existing base fetch; a shallow\n (`--depth`) or second base fetch would make the proof unresolvable and silently\n disable the carry, so neither is permitted. CCA reads `guardian_comment_carried` as\n-a recorded authority in the deterministic signal and does not re-derive it. See ADR\n-0018 for the full decision, the rejected diff/patch-equality and path-disjointness\n+a recorded authority in the deterministic signal and does not re-derive it. See\n+SADR-0018 for the full decision, the rejected diff/patch-equality and path-disjointness\n alternatives, and the accepted residual.\n \n ## External AI Guardian Review (SADR-0010)\ndiff --git a/docs/agents/implementation-policy.md b/docs/agents/implementation-policy.md\nindex c754144a9fb5bf4a38aa82a60e1670c59e626b44..85c37281bcad196c6b7b02343faf00b03cd9c57a\n--- a/docs/agents/implementation-policy.md\n+++ b/docs/agents/implementation-policy.md\n@@ -31,10 +31,13 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n ## Non-Negotiables\n \n 1. **Planning precedes implementation.** Non-trivial Goals start with `grill-with-docs` and Context & Impact.\n-2. **Domain language is controlled.** Terms must be checked against `CONTEXT.md`; hard-to-reverse tradeoffs must be checked against ADRs.\n+2. **Domain language is controlled.** Terms must be checked against `CONTEXT.md`; hard-to-reverse tradeoffs must be checked against Shiki SADRs and target ADRs.\n 3. **PRD precedes issue decomposition.** `to-prd` is used once the context is settled enough to become durable intent.\n 4. **Issues are vertical slices.** `to-issues` must produce independently grabbable tracer-bullet tasks, not horizontal layer tickets.\n 5. **Triage controls dispatch.** Only `ready-for-agent` / AFK tasks may be assigned to Codex without human input.\n@@ -95,19 +98,19 @@\n Process:\n \n - Challenge terms against `CONTEXT.md`.\n-- Check ADRs before reopening decisions.\n+- Check SADRs and target ADRs before reopening decisions.\n - Inspect code when code can answer a question.\n - Ask one question at a time when the operator is available.\n - Provide a recommended answer with each question.\n - Record resolved terms in `CONTEXT.md`.\n-- Create ADRs only for hard-to-reverse, surprising, tradeoff-based decisions.\n+- Create SADRs or target ADRs only for hard-to-reverse, surprising, tradeoff-based decisions.\n - Convert unresolved load-bearing questions into HITL blockers.\n \n Outputs:\n \n - Resolved design-tree decisions.\n - Updated glossary terms when needed.\n-- ADRs when justified.\n+- SADRs or target ADRs when justified.\n - HITL blockers when decisions remain unresolved.\n \n ## Phase 3 \u2014 Context & Impact\n@@ -117,7 +120,7 @@\n \n Outputs must include:\n \n-- Relevant documents and ADRs.\n+- Relevant documents, SADRs, and target ADRs.\n - Relevant modules, interfaces, seams, callers, and tests.\n - Candidate locks.\n - Dependencies.\n@@ -140,7 +143,7 @@\n - Testing decisions.\n - Out of scope.\n - Further notes.\n-- Links to relevant Goal, domain terms, and ADRs.\n+- Links to relevant Goal, domain terms, SADRs, and target ADRs.\n \n Phase 4 ends with Spec Freeze: the operator explicitly approves the PRD, the\n plan records a `spec_freeze` block (status, approved_by, source), and the SF\n@@ -240,7 +243,7 @@\n - Dependencies and locks.\n - Risk and Guardian requirements.\n - Ledger completeness.\n-- Documentation and ADR updates.\n+- Documentation and SADR/target-ADR updates.\n - Scope drift.\n \n CCA emits one of:\n@@ -301,7 +304,7 @@\n | Evidence failure | Behavior might be done, but proof is missing. | Add checks, PR evidence, ledger entry, or artifacts. |\n | Scope drift | PR changes unrelated behavior. | Revert unrelated changes or split into new Goal. |\n | Dependency block | Upstream task or lock not complete. | Block until dependency resolves. |\n-| Architecture block | Decision needs ADR or deep-module discussion. | `grill-with-docs` or `improve-codebase-architecture`. |\n+| Architecture block | Decision needs a SADR, target ADR, or deep-module discussion. | `grill-with-docs` or `improve-codebase-architecture`. |\n | Risk block | Security/production/policy change needs human approval. | Guardian decision. |\n \n ## Phase 5.5 \u2014 Execution Decision Control\ndiff --git a/docs/agents/runtime-auth-model.md b/docs/agents/runtime-auth-model.md\nindex 6a2e1a47e81ea89b5c68a210ec07772a9374babc..3c54748d24233dbd0ba8473950282547ef4ce935\n--- a/docs/agents/runtime-auth-model.md\n+++ b/docs/agents/runtime-auth-model.md\n@@ -148,7 +148,7 @@\n \n API-key based runners may be added by a target repository as an explicit extension. They are not the default Shiki template path.\n \n-If a repo opts into API-key mode, record the exception in an ADR and update the repo's `AGENTS.md`, `.shiki/config.yaml`, and workflow permissions.\n+If a repo opts into API-key mode, record the target-specific exception in a target ADR and update the repo's `AGENTS.md`, `.shiki/config.yaml`, and workflow permissions.\n \n ## GitHub Actions Runtime Compatibility\n \ndiff --git a/docs/agents/skill-gate.md b/docs/agents/skill-gate.md\nindex c50398b0d7c60a8598f2a5c88a10015730dced13..3acffe6444617bb1d54b77d4b4708158ec735bc6\n--- a/docs/agents/skill-gate.md\n+++ b/docs/agents/skill-gate.md\n@@ -9,7 +9,7 @@\n | Skill | Trigger | Expected output |\n | --- | --- | --- |\n | `setup-matt-pocock-skills` | First configuring a repo for these skills, or when issue tracker, triage labels, or domain docs are missing. | `docs/agents/` configuration and an `Agent skills` block in `AGENTS.md` or `CLAUDE.md`. |\n-| `grill-with-docs` | Ambiguous plan, unclear terminology, boundary decision, tradeoff, or ADR-worthy design choice. | Clarified decision tree, recommended answers, updates to `CONTEXT.md` or ADRs when decisions crystallize. |\n+| `grill-with-docs` | Ambiguous plan, unclear terminology, boundary decision, tradeoff, or decision-record-worthy design choice. | Clarified decision tree, recommended answers, updates to `CONTEXT.md`, SADRs, or target ADRs when decisions crystallize. |\n | `zoom-out` | Agent lacks a higher-level architectural map. | Map of relevant modules, callers, seams, and domain concepts. |\n | `to-prd` | Settled context should become a PRD. | PRD published to the issue tracker with problem, solution, user stories, decisions, testing, and out-of-scope notes. |\n | `to-issues` | Goal, PRD, or plan must become implementation tickets. | Vertical-slice issues with acceptance criteria, dependencies, and AFK/HITL classification. |\n@@ -17,7 +17,7 @@\n | `tdd` | Feature work or bug fix has observable behavior that can be specified with tests. | Red-green-refactor loop through public interfaces, one vertical slice at a time. |\n | `code-review` | The TDD loop is green and the implementer is about to create or update a PR. Default for every implementation task. | Pre-PR self-review of the task diff with fixes applied on the task branch; findings and resolutions recorded as a ledger entry naming the skill and a `## Pre-PR code review` PR body section. |\n | `diagnose` | Hard bug, failing check, regression, flaky behavior, or performance problem. | Reproduction loop, ranked hypotheses, instrumentation, fix, regression test, post-mortem. |\n-| `improve-codebase-architecture` | Structural friction, testability, AI-navigability, deep-module opportunity, or refactor request. | Architecture candidates and deepening recommendations informed by `CONTEXT.md` and ADRs. |\n+| `improve-codebase-architecture` | Structural friction, testability, AI-navigability, deep-module opportunity, or refactor request. | Architecture candidates and deepening recommendations informed by `CONTEXT.md`, SADRs, and target ADRs. |\n | `prototype` | A throwaway logic or UI prototype is needed to answer a design question. | Clearly marked disposable prototype and captured decision. |\n \n ## MergeGate Interaction\ndiff --git a/docs/operations.md b/docs/operations.md\nindex cf1f452d4c589e38ccd58553bda0ad8684b51eb7..ace48a90ffbec9e5c0a7113bef16a4858bc44b3f\n--- a/docs/operations.md\n+++ b/docs/operations.md\n@@ -89,6 +89,16 @@\n      target history, brought forward by `shiki migrate`, not a template.\n    - **Everything else** (CLI scripts, workflows, schemas, docs) is overwritten.\n \n+   A forced upgrade also migrates the former numeric Shiki decision paths to\n+   `SADR-NNNN-*`. Before the first write, the installer inventories the exact\n+   legacy Shiki paths and authorizes each deletion only when the existing\n+   `.shiki/install-stamp.json` names that path and its SHA-256 digest still\n+   matches the file. An absent, unreadable, malformed, incomplete, or\n+   mismatching stamp reports every blocked path and stops with no filesystem\n+   change. Target-owned `NNNN-*` ADRs are not cleanup candidates. A non-force\n+   install deletes nothing and retains any still-matching legacy stamp entries\n+   so a later forced upgrade can prove ownership.\n+\n    If the target has pending migrations, `--force` refuses and writes nothing \u2014\n    apply migrations first (step 3), then re-run. The run ends with a single\n    summary of every `.new` written, so a half-upgrade is visible.\ndiff --git a/docs/releasing.md b/docs/releasing.md\nindex 10d73828e7dbd64e9d9f6b83bc84d010537ce62a..aafeee8c4d441e1e427efee2ce4f81a647f020e1\n--- a/docs/releasing.md\n+++ b/docs/releasing.md\n@@ -67,7 +67,7 @@\n CLI in `scripts/`. There are no third-party Python runtime dependencies. The CLI\n is intentionally **not** exposed as a `[project.scripts]` console entry point\n because `scripts/shiki.py` is a dependency-free shim and `scripts/` is not\n-packaged as an importable distribution (see the ADR for the rationale).\n+packaged as an importable distribution (see SADR-0007 for the rationale).\n \n Supported install paths:\n \n@@ -155,6 +155,15 @@\n    [docs/operations.md](operations.md#the-new-convention). If the target has\n    pending migrations, `--force` refuses and writes nothing; apply migrations\n    (step 2) first, then re-run.\n+\n+   The SADR namespace upgrade has an additional pre-write gate: each former\n+   numeric Shiki decision path is removed only when the existing install stamp\n+   contains that exact path and a digest matching its current bytes. The\n+   installer aggregates absent/unreadable/malformed-stamp, missing-digest, and\n+   digest-mismatch blockers before raising, so refusal leaves the target tree\n+   byte-identical. Numeric target ADRs are preserved. A successful forced\n+   upgrade installs and stamps `SADR-*` paths and drops the removed legacy paths\n+   from the refreshed stamp.\n 2. Apply repository-local `.shiki/` state migrations with the migrate command:\n \n    ```bash\ndiff --git a/docs/usage.md b/docs/usage.md\nindex b83407ee3970dd395dd893010bc5dbe8bad26051..a130ef6e40dfd8169a5bc701339351097df1a09f\n--- a/docs/usage.md\n+++ b/docs/usage.md\n@@ -67,8 +67,10 @@\n shiki install-target /path/to/target-repo --local-only\n ```\n \n-Use `--force` only when you intentionally want to overwrite existing target\n-files.\n+Use `--force` only when you intentionally want to refresh the target template.\n+For the SADR namespace migration, `--force` deletes only exact legacy Shiki\n+decision paths whose existing install-stamp digest still matches; any ownership\n+blocker stops before the first write. Numeric target ADRs are never candidates.\n \n ## `shiki bootstrap-platform` \u2014 publish the Shiki platform repo\n \ndiff --git a/scripts/guardian_approval_signal.py b/scripts/guardian_approval_signal.py\nindex d059f92b2c82aa2b5d6ca421365537a87886c1cb..16924892714f4f5b9303c0ef0e325a7e70075630\n--- a/scripts/guardian_approval_signal.py\n+++ b/scripts/guardian_approval_signal.py\n@@ -199,8 +199,8 @@\n ) -> Any:\n     \"\"\"Evaluate SADR-0015 Contract Approval for the CCA Guardian signal.\n \n-    The SAME OR that MergeGate applies must reach this deterministic signal: ADR\n-    0015 is explicit that without it, a contract-approved PR still returns\n+    The SAME OR that MergeGate applies must reach this deterministic signal:\n+    SADR-0015 is explicit that without it, a contract-approved PR still returns\n     ``needs_guardian``, the CCA job fails, and the MergeGate policy check never\n     runs \u2014 the gate this change moves would never be reached. This resolves the\n     task the SAME way MergeGate does (the first Shiki task id in the PR body),\ndiff --git a/scripts/shiki_contract_approval.py b/scripts/shiki_contract_approval.py\nindex 941bcb96fdd8b9a85bc0c4f0f04d5dad9d04839c..4a4b418f6527413161db82670bec857a043360e2\n--- a/scripts/shiki_contract_approval.py\n+++ b/scripts/shiki_contract_approval.py\n@@ -142,7 +142,7 @@\n     changed_files_status: Any,\n     registration: Any,\n ) -> ContractApprovalResult:\n-    \"\"\"Whether this PR carries an SADR-0015 Contract Approval.\n+    \"\"\"Whether this PR carries a SADR-0015 Contract Approval.\n \n     The four conditions are evaluated IN ORDER, each failure recorded as a\n     distinct reason string:\ndiff --git a/scripts/shiki_guardian.py b/scripts/shiki_guardian.py\nindex a4ecb79bbda55fd5c8b06adfae2eb99760a03c45..4dd99bbe714e26f421fa19abac7a34ae1b195ecc\n--- a/scripts/shiki_guardian.py\n+++ b/scripts/shiki_guardian.py\n@@ -12,7 +12,7 @@\n # The carry re-derives its proof from git in-process on every judging invocation;\n # it is never read from an artifact, so there is nothing to go stale or be forged.\n # shiki_sync_proof is the ONLY guardian-family module that runs git, and every one\n-# of its invocations is read-only and local (ADR: prove-from-git base sync).\n+# of its invocations is read-only and local (SADR: prove-from-git base sync).\n from shiki_sync_proof import verify_pure_base_sync\n \n GUARDIAN_POLICY_PATH = \".shiki/guardian-policy.json\"\ndiff --git a/scripts/shiki_guardian_review.py b/scripts/shiki_guardian_review.py\nindex dd0f104d457899e0ba5ff1e162011c1e90b6f92b..e10ef58c046324e7ea3a3392feb3048ca77414ce\n--- a/scripts/shiki_guardian_review.py\n+++ b/scripts/shiki_guardian_review.py\n@@ -159,7 +159,7 @@\n     docs_only = _is_docs_only(changed_files)\n     if docs_only and matched == 0:\n         focus.append(\n-            \"Docs/ADR-only change: confirm the change is genuinely documentation/decision-only, introduces no behavior or contract drift, and stays consistent with the constitution and existing ADRs.\"\n+            \"Docs/decision-record-only change: confirm the change is genuinely documentation/decision-only, introduces no behavior or contract drift, and stays consistent with the constitution and existing SADRs and target ADRs.\"\n         )\n \n     # Conservative augmentation: unknown (no category and not docs-only), mixed\ndiff --git a/scripts/shiki_installer.py b/scripts/shiki_installer.py\nindex be064ab7a29ff6836f7f7a1d70500a43e0d11d7b..9fd8eefd137ce7003dad5c0dcb2e48e2cd47ab32\n--- a/scripts/shiki_installer.py\n+++ b/scripts/shiki_installer.py\n@@ -118,6 +118,7 @@\n # what lets a target carry it without a governance-schema change.\n INSTALL_STAMP_PATH = \".shiki/install-stamp.json\"\n INSTALL_STAMP_VERSION = 1\n+LEGACY_SHIKI_SADR_MAX = 18\n \n # Under --force the shipped surface splits three ways, so nothing is lost and\n # nothing is silently stale.\n@@ -237,9 +238,98 @@\n         )\n \n \n+def legacy_shiki_adr_paths() -> tuple[str, ...]:\n+    \"\"\"Former numeric paths for Shiki SADR-0001 through SADR-0018.\n+\n+    The authoritative slugs come from the current SADR filenames. SADR-0019 is\n+    the namespace decision itself and never shipped under a numeric legacy path.\n+    \"\"\"\n+    paths: list[str] = []\n+    for sadr in sorted((ROOT / \"docs\" / \"adr\").glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\")):\n+        number = sadr.name.split(\"-\", 2)[1]\n+        if int(number) <= LEGACY_SHIKI_SADR_MAX:\n+            paths.append(f\"docs/adr/{sadr.name.removeprefix('SADR-')}\")\n+    return tuple(paths)\n+\n+\n+def preflight_legacy_sadr_cleanup(target: Path, *, force: bool) -> tuple[str, ...]:\n+    \"\"\"Authorize exact legacy Shiki ADR removals before the first write.\n+\n+    A non-force install never removes existing files. A forced install needs no\n+    stamp when no legacy Shiki path is present. When legacy paths do exist, each\n+    must be named in a readable install stamp and its current bytes must match\n+    the recorded digest.\n+    \"\"\"\n+    if not force:\n+        return ()\n+    present = tuple(\n+        relative\n+        for relative in legacy_shiki_adr_paths()\n+        if (target / relative).exists() or (target / relative).is_symlink()\n+    )\n+    if not present:\n+        return ()\n+\n+    stamp_path = target / INSTALL_STAMP_PATH\n+    try:\n+        raw = stamp_path.read_text(encoding=\"utf-8\")\n+        stamp = json.loads(raw)\n+    except FileNotFoundError:\n+        detail = \"install stamp is absent\"\n+        blockers = [f\"{relative}: {detail}\" for relative in present]\n+    except (OSError, UnicodeDecodeError) as error:\n+        detail = f\"install stamp is unreadable ({error})\"\n+        blockers = [f\"{relative}: {detail}\" for relative in present]\n+    except json.JSONDecodeError as error:\n+        detail = f\"install stamp is malformed ({error})\"\n+        blockers = [f\"{relative}: {detail}\" for relative in present]\n+    else:\n+        if not isinstance(stamp, dict):\n+            blockers = [\n+                f\"{relative}: install stamp is malformed (expected an object)\"\n+                for relative in present\n+            ]\n+        else:\n+            digests = stamp.get(\"digests\")\n+            blockers = []\n+            if not isinstance(digests, dict):\n+                blockers.extend(\n+                    f\"{relative}: install stamp has no digest for this path\"\n+                    for relative in present\n+                )\n+            else:\n+                for relative in present:\n+                    expected = digests.get(relative)\n+                    if not isinstance(expected, str) or not expected:\n+                        blockers.append(\n+                            f\"{relative}: install stamp has no digest for this path\"\n+                        )\n+                        continue\n+                    try:\n+                        actual = _sha256_file(target / relative)\n+                    except OSError as error:\n+                        blockers.append(\n+                            f\"{relative}: legacy path is unreadable ({error})\"\n+                        )\n+                        continue\n+                    if actual != expected:\n+                        blockers.append(\n+                            f\"{relative}: digest mismatch (stamp {expected}, current {actual})\"\n+                        )\n+\n+    if blockers:\n+        joined = \"\\n\".join(f\"- {blocker}\" for blocker in blockers)\n+        raise ShikiError(\n+            \"legacy Shiki ADR cleanup is not authorized:\\n\"\n+            f\"{joined}\\nNo files were written.\"\n+        )\n+    return present\n+\n+\n def install_template(target: Path, *, force: bool, validate: bool) -> list[NewFileNote]:\n     # Preconditions first: never rewrite the tree of a target that cannot be\n     # upgraded cleanly (see refuse_pending_migrations).\n+    legacy_cleanup = preflight_legacy_sadr_cleanup(target, force=force)\n     refuse_pending_migrations(target)\n \n     # Resolve the target's required CODEOWNERS owner once from its\n@@ -250,6 +340,10 @@\n     # verbatim. See ``codeowners_required_owner``.\n     codeowners_owner = codeowners_required_owner(target)\n \n+    for relative in legacy_cleanup:\n+        (target / relative).unlink()\n+        info(f\"removed stamped legacy Shiki ADR: {target / relative}\")\n+\n     notes: list[NewFileNote] = []\n     for relative in TEMPLATE_PATHS:\n         source = ROOT / relative\n@@ -647,6 +741,22 @@\n     digests = target_stamp_digests(target)\n     existing = read_install_stamp(target)\n     if existing is not None:\n+        # A non-force install deliberately leaves legacy Shiki ADRs in place.\n+        # Preserve only still-matching ownership proof so a later forced\n+        # upgrade can authorize cleanup; never carry a missing or stale digest.\n+        existing_digests = existing.get(\"digests\")\n+        if isinstance(existing_digests, dict):\n+            for relative in legacy_shiki_adr_paths():\n+                expected = existing_digests.get(relative)\n+                path = target / relative\n+                if not isinstance(expected, str) or not path.is_file():\n+                    continue\n+                try:\n+                    actual = _sha256_file(path)\n+                except OSError:\n+                    continue\n+                if actual == expected:\n+                    digests[relative] = actual\n         if _incoming_is_older(commit, existing.get(\"platform_commit\")):\n             warn(\n                 \"kept existing install stamp: incoming install \"\ndiff --git a/scripts/shiki_loop.py b/scripts/shiki_loop.py\nindex 3611641da8aeb5038b1da90e3114b09a473ec6d8..84caa491b9cad0edc4176f209c13e9221e975681\n--- a/scripts/shiki_loop.py\n+++ b/scripts/shiki_loop.py\n@@ -424,7 +424,7 @@\n         # unresolvable CCA verdict has already produced its terminal stop and is never\n         # laundered into a head-moving sync (SADR-0010). It also never fires while the\n         # policy (Guardian) gate is red \u2014 for ANY reason, not only when it is the sole\n-        # red check \u2014 so a head-bound Guardian approval, or an SADR-0015\n+        # red check \u2014 so a head-bound Guardian approval, or a SADR-0015\n         # contract-approval carry that base movement defeated (the policy gate compares\n         # base and head task governance directly), is never silently restored by\n         # adopting base wholesale. It is bounded by a durable per-task counter so the\ndiff --git a/scripts/shiki_tasks.py b/scripts/shiki_tasks.py\nindex aec3809f24b4a0c8c96fc50971cff9ae9ad30381..d531052e9d9717058d4631483cefab44c26a65c1\n--- a/scripts/shiki_tasks.py\n+++ b/scripts/shiki_tasks.py\n@@ -821,7 +821,7 @@\n         \"prompt\": prompt,\n         \"entry_skill\": \"grill-with-docs\",\n         \"required_next_steps\": [\n-            \"Run grill-with-docs until terminology, boundaries, risks, and ADR-worthy decisions are settled.\",\n+            \"Run grill-with-docs until terminology, boundaries, risks, and decision-record-worthy choices are settled.\",\n             \"Obtain the operator's explicit approval of the PRD/requirements (Spec Freeze).\",\n             \"Write a machine-readable plan JSON with grill_with_docs.status=complete and spec_freeze.status=frozen.\",\n             \"Run shiki plan ingest --plan-file PLAN.json.\",\ndiff --git a/scripts/test_shiki_goal_loop.sh b/scripts/test_shiki_goal_loop.sh\nindex 753c7b516e10f0be3bc92a4bd0eb1fa699776ae6..8f3820a0a079b3044424cfe1a6c6b7fe68896e67\n--- a/scripts/test_shiki_goal_loop.sh\n+++ b/scripts/test_shiki_goal_loop.sh\n@@ -231,8 +231,8 @@\n \n # High/critical risk merges autonomously when all required checks are green:\n # the \"MergeGate policy check\" required check IS the Guardian gate (it enforces\n-# guardian-policy.json \u2014 human approval OR external AI guardian review, ADR\n-# 0010), so green checks mean a recorded authority approved. The fake gh returns\n+# guardian-policy.json \u2014 human approval OR external AI guardian review,\n+# SADR-0010), so green checks mean a recorded authority approved. The fake gh returns\n # green checks, simulating that the Guardian gate passed.\n python3 \"$ROOT/scripts/shiki.py\" goal create --target \"$TARGET\" --title \"Guardian gate\" --outcome \"High risk merges via policy gate\" >/tmp/shiki-goal-loop-guardian-goal.json\n GGOAL=\"$(json_get /tmp/shiki-goal-loop-guardian-goal.json goal_id)\"\ndiff --git a/tests/test_adr_references.py b/tests/test_adr_references.py\nindex 51706329c797d5dd9b47e082a333fa83b261d482..db6e693e35f67103935be34f1d9bb4ff7ee60395\n--- a/tests/test_adr_references.py\n+++ b/tests/test_adr_references.py\n@@ -1,7 +1,7 @@\n-\"\"\"Every ``ADR ####`` citation must resolve to exactly one decision record.\n+\"\"\"Every target ADR and Shiki SADR citation resolves in its own namespace.\n \n Background. ``scripts/mergegate_check.py`` justifies ``is_bookkeeping_closeout``\n-with \"ADR 0017\" nine times and ``scripts/guardian_approval_signal.py`` cites\n+with \"SADR-0017\" nine times and ``scripts/guardian_approval_signal.py`` cites\n 0016/0017 seven times, yet for a stretch the numbering in ``docs/adr/`` stopped\n at 0015: the authoring commits lived on a coordinator branch and no task ever\n declared ``docs/adr/...`` in its locks, so nothing carried the records to the\n@@ -11,11 +11,12 @@\n and the directory drifted with no signal.\n \n This is the same unbound-pair shape behind other silent failures: a reference\n-and its target with nothing asserting they agree. This test binds them. It scans\n-every ``scripts/*.py`` (plus ``docs/**/*.md`` and the root ``*.md`` governance\n-files, which are cheap) for the ``ADR ####`` pattern and asserts each cited\n-number resolves to exactly one ``docs/adr/####-*.md``. The converse is\n-deliberately NOT required: an ADR may exist without being cited.\n+and its target with nothing asserting they agree. This test binds them. Target\n+``ADR NNNN`` citations resolve only to ``docs/adr/NNNN-*.md``; Shiki\n+``SADR-NNNN`` citations resolve only to ``docs/adr/SADR-NNNN-*.md``. The scan\n+covers current governance docs, scripts, workflows, prompts, packaging metadata,\n+and repo-local Codex guidance while deliberately excluding historical\n+``.shiki`` evidence. The converse is not required: a record may be uncited.\n \n The suite is dependency free: standard-library ``unittest`` only.\n \"\"\"\n@@ -31,17 +32,18 @@\n \n REPO_ROOT = Path(__file__).resolve().parents[1]\n \n-# \"ADR 0016\" -- a four-digit citation. The trailing \\b keeps a five-digit token\n-# from being read as a truncated four-digit number.\n-_CITATION = re.compile(r\"\\bADR (\\d{4})\\b\")\n-# \"0016-locks-bound-product-paths.md\" -- the leading four digits name the record.\n-_ADR_FILENAME = re.compile(r\"^(\\d{4})-.*\\.md$\")\n+# ``ADR 0016`` / ``SADR-0016`` -- a namespace plus four-digit citation. The\n+# trailing ``\\b`` keeps a five-digit token from being read as a truncated one.\n+_CITATION = re.compile(r\"\\b(S?ADR)[ -](\\d{4})\\b\")\n+# Target ADRs retain the numeric filename; Shiki platform decisions use SADR.\n+_DECISION_FILENAME = re.compile(r\"^(?:(SADR)-)?(\\d{4})-.*\\.md$\")\n \n \n class Violation(NamedTuple):\n     \"\"\"A citation that does not resolve to exactly one decision record.\"\"\"\n \n     citing_file: str  # repo-relative path of the file that cites the ADR\n+    namespace: str  # \"ADR\" for target decisions; \"SADR\" for Shiki decisions\n     number: str  # the cited four-digit number, e.g. \"0016\"\n     line: int  # 1-based line of the citation\n     reason: str  # \"missing\" (zero matches) or \"ambiguous\" (two or more)\n@@ -49,44 +51,59 @@\n \n     @property\n     def expected_path(self) -> str:\n-        return f\"docs/adr/{self.number}-*.md\"\n+        prefix = \"SADR-\" if self.namespace == \"SADR\" else \"\"\n+        return f\"docs/adr/{prefix}{self.number}-*.md\"\n \n     def describe(self) -> str:\n         \"\"\"Name the citing file, the cited number, and the missing path.\"\"\"\n         if self.reason == \"missing\":\n             return (\n-                f\"{self.citing_file}:{self.line} cites ADR {self.number} \"\n+                f\"{self.citing_file}:{self.line} cites {self.namespace} {self.number} \"\n                 f\"but no document matches {self.expected_path}\"\n             )\n         joined = \", \".join(self.matches)\n         return (\n-            f\"{self.citing_file}:{self.line} cites ADR {self.number} which \"\n+            f\"{self.citing_file}:{self.line} cites {self.namespace} {self.number} which \"\n             f\"resolves to {len(self.matches)} documents (expected exactly one \"\n             f\"at {self.expected_path}): {joined}\"\n         )\n \n \n-def _adr_index(root: Path) -> dict[str, list[Path]]:\n-    \"\"\"Map every four-digit ADR number to the record files that claim it.\"\"\"\n-    index: dict[str, list[Path]] = {}\n+def _adr_index(root: Path) -> dict[tuple[str, str], list[Path]]:\n+    \"\"\"Map each decision namespace and number to the files that claim it.\"\"\"\n+    index: dict[tuple[str, str], list[Path]] = {}\n     adr_dir = root / \"docs\" / \"adr\"\n     if adr_dir.is_dir():\n         for path in sorted(adr_dir.glob(\"*.md\")):\n-            match = _ADR_FILENAME.match(path.name)\n+            match = _DECISION_FILENAME.match(path.name)\n             if match:\n-                index.setdefault(match.group(1), []).append(path)\n+                namespace = \"SADR\" if match.group(1) else \"ADR\"\n+                index.setdefault((namespace, match.group(2)), []).append(path)\n     return index\n \n \n def _citing_files(root: Path, include_docs: bool = True) -> list[Path]:\n-    \"\"\"Files scanned for citations: scripts/*.py, and cheaply the markdown.\"\"\"\n+    \"\"\"Current, shipped text surfaces scanned for decision citations.\"\"\"\n     files = sorted((root / \"scripts\").glob(\"*.py\"))\n+    files += sorted((root / \"scripts\").glob(\"*.sh\"))\n     if include_docs:\n         docs = root / \"docs\"\n         if docs.is_dir():\n             files += sorted(docs.rglob(\"*.md\"))\n         files += sorted(root.glob(\"*.md\"))\n-    return files\n+        pyproject = root / \"pyproject.toml\"\n+        if pyproject.is_file():\n+            files.append(pyproject)\n+        for relative in (\".github\", \".codex\"):\n+            surface = root / relative\n+            if surface.is_dir():\n+                files += sorted(\n+                    path\n+                    for path in surface.rglob(\"*\")\n+                    if path.is_file()\n+                    and path.suffix in {\".md\", \".py\", \".sh\", \".toml\", \".yaml\", \".yml\"}\n+                )\n+    return sorted(set(files))\n \n \n def find_adr_reference_violations(\n@@ -94,10 +111,9 @@\n ) -> list[Violation]:\n     \"\"\"Return every ``ADR ####`` citation under ``root`` that fails to resolve.\n \n-    A citation resolves when exactly one ``docs/adr/####-*.md`` matches its\n-    number. Zero matches is a \"missing\" violation; two or more is \"ambiguous\".\n-    Records that are never cited produce nothing -- the converse is not\n-    required.\n+    A citation resolves when exactly one record in the same namespace matches\n+    its number. Zero matches is a \"missing\" violation; two or more is\n+    \"ambiguous\". Records that are never cited produce nothing.\n     \"\"\"\n     root = Path(root)\n     index = _adr_index(root)\n@@ -110,13 +126,15 @@\n         rel = path.relative_to(root).as_posix()\n         for lineno, line in enumerate(text.splitlines(), start=1):\n             for match in _CITATION.finditer(line):\n-                number = match.group(1)\n-                found = index.get(number, [])\n+                namespace, number = match.groups()\n+                found = index.get((namespace, number), [])\n                 if len(found) == 1:\n                     continue\n                 reason = \"missing\" if not found else \"ambiguous\"\n                 rels = [p.relative_to(root).as_posix() for p in found]\n-                violations.append(Violation(rel, number, lineno, reason, rels))\n+                violations.append(\n+                    Violation(rel, namespace, number, lineno, reason, rels)\n+                )\n     return violations\n \n \n@@ -129,8 +147,8 @@\n         self.assertEqual(\n             violations,\n             [],\n-            \"Every 'ADR ####' citation must resolve to exactly one \"\n-            f\"docs/adr/####-*.md:\\n{detail}\",\n+            \"Every ADR/SADR citation must resolve exactly once in its own \"\n+            f\"docs/adr namespace:\\n{detail}\",\n         )\n \n     def test_scan_actually_reads_scripts(self) -> None:\n@@ -146,8 +164,21 @@\n         # The two records this Goal carried onto the default branch, and which\n         # the scripts cite most, must be present.\n         index = _adr_index(REPO_ROOT)\n-        self.assertIn(\"0016\", index)\n-        self.assertIn(\"0017\", index)\n+        self.assertIn((\"SADR\", \"0016\"), index)\n+        self.assertIn((\"SADR\", \"0017\"), index)\n+\n+    def test_platform_tree_has_only_sadr_0001_through_0019(self) -> None:\n+        adr_dir = REPO_ROOT / \"docs\" / \"adr\"\n+        sadrs = sorted(adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\"))\n+        self.assertEqual(\n+            {path.name.split(\"-\", 2)[1] for path in sadrs},\n+            {f\"{number:04d}\" for number in range(1, 20)},\n+        )\n+        self.assertEqual(\n+            list(adr_dir.glob(\"[0-9][0-9][0-9][0-9]-*.md\")),\n+            [],\n+            \"the Shiki platform tree must not retain legacy numeric SADR paths\",\n+        )\n \n \n class FixtureAdrReferences(unittest.TestCase):\n@@ -180,6 +211,26 @@\n         self.assertIn(\"9991\", message)  # cited number\n         self.assertIn(\"docs/adr/9991-*.md\", message)  # missing path\n \n+    def test_target_adr_does_not_satisfy_missing_platform_sadr(self) -> None:\n+        root = self._make_repo(\n+            adr_names=[\"0042-target-decision.md\"],\n+            script_body='\"\"\"Target ADR 0042 exists, but platform SADR-0042 does not.\"\"\"\\n',\n+        )\n+        violations = find_adr_reference_violations(root, include_docs=False)\n+        self.assertEqual(len(violations), 1)\n+        self.assertEqual(violations[0].namespace, \"SADR\")\n+        self.assertEqual(violations[0].number, \"0042\")\n+        self.assertEqual(violations[0].expected_path, \"docs/adr/SADR-0042-*.md\")\n+\n+    def test_target_adr_and_platform_sadr_may_reuse_a_number(self) -> None:\n+        root = self._make_repo(\n+            adr_names=[\"0042-target-decision.md\", \"SADR-0042-platform-decision.md\"],\n+            script_body='\"\"\"Target ADR 0042 and platform SADR-0042 are distinct.\"\"\"\\n',\n+        )\n+        self.assertEqual(\n+            find_adr_reference_violations(root, include_docs=False), []\n+        )\n+\n     def test_uncited_adr_is_allowed(self) -> None:\n         # An ADR exists but nothing cites it -> not a violation. The converse\n         # (every ADR must be cited) is explicitly NOT required.\ndiff --git a/tests/test_guardian_review.py b/tests/test_guardian_review.py\nindex 88829e7810212635e2fc98aa5918d6f26294677d..00a85a237acd5bf2d6cb5b294128198c422402ca\n--- a/tests/test_guardian_review.py\n+++ b/tests/test_guardian_review.py\n@@ -162,7 +162,7 @@\n \n     def test_docs_only_is_light_and_not_conservative(self):\n         focus = gr.classify_review_focus([\"docs/adr/0099-x.md\", \"README.md\"], \"\", \"low\")\n-        self.assertTrue(any(\"Docs/ADR-only\" in f for f in focus))\n+        self.assertTrue(any(\"Docs/decision-record-only\" in f for f in focus))\n         self.assertFalse(any(\"conservative default\" in f.lower() for f in focus))\n \n     def test_unknown_category_is_conservative(self):\ndiff --git a/tests/test_policy_job_merged_prs.py b/tests/test_policy_job_merged_prs.py\nindex d45512e74681e5e649279236c7d4e79d5c3da471..b97989f8bd74d091de1a6b5275a20d8dc68c4512\n--- a/tests/test_policy_job_merged_prs.py\n+++ b/tests/test_policy_job_merged_prs.py\n@@ -3,8 +3,8 @@\n \n ``.github/workflows/shiki-cca-completion.yml`` has two consumers of the merged-PR\n proof: the CCA *signal* (``guardian_approval_signal.py`` in \"Prepare CCA context\")\n-and the authoritative gate (``mergegate_check.py`` in \"Enforce MergeGate\"). ADR\n-0017's bookkeeping-closeout exemption (condition 6) needs the merged implementation\n+and the authoritative gate (``mergegate_check.py`` in \"Enforce MergeGate\").\n+SADR-0017's bookkeeping-closeout exemption (condition 6) needs the merged implementation\n PR proven, and a closeout carries NO ``post_merge_reconcile`` marker. The signal\n already resolves ``MERGED_PRS`` unconditionally, but \"Enforce MergeGate\" used to\n resolve it only inside ``grep -q '<!-- shiki:post_merge_reconcile -->'``. So a\ndiff --git a/tests/test_sadr_install_migration.py b/tests/test_sadr_install_migration.py\nnew file mode 100644\nindex 0000000000000000000000000000000000000000..a6f4c57e4dcaef73ef600b0d4804744138ab9071\n--- /dev/null\n+++ b/tests/test_sadr_install_migration.py\n@@ -0,0 +1,229 @@\n+\"\"\"SADR installation and collision-safe legacy upgrade behavior.\"\"\"\n+\n+from __future__ import annotations\n+\n+import contextlib\n+import hashlib\n+import io\n+import json\n+import shutil\n+import tempfile\n+import unittest\n+from pathlib import Path\n+\n+import shiki_test_support  # noqa: F401  (path bootstrap)\n+\n+import shiki_installer\n+from tests.test_adr_references import find_adr_reference_violations\n+\n+\n+PRODUCT_NUMBERS = tuple(range(13, 19))\n+\n+\n+def _quiet_install(target: Path, *, force: bool, validate: bool) -> None:\n+    with contextlib.redirect_stdout(io.StringIO()):\n+        shiki_installer.install_template(target, force=force, validate=validate)\n+\n+\n+def _sha256(path: Path) -> str:\n+    return hashlib.sha256(path.read_bytes()).hexdigest()\n+\n+\n+def _legacy_sadr_paths() -> tuple[str, ...]:\n+    return tuple(\n+        f\"docs/adr/{path.name.removeprefix('SADR-')}\"\n+        for path in sorted((shiki_installer.ROOT / \"docs\" / \"adr\").glob(\"SADR-*.md\"))\n+        if path.name != \"SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\"\n+    )\n+\n+\n+def _create_legacy_upgrade_fixture(\n+    target: Path,\n+) -> tuple[dict[str, bytes], tuple[str, ...]]:\n+    adr_dir = target / \"docs\" / \"adr\"\n+    adr_dir.mkdir(parents=True)\n+    product_bytes = {\n+        f\"{number:04d}-cubeshare-product-decision.md\": (\n+            f\"# CubeShare ADR {number:04d}\\n\\nDo not rewrite {number}.\\n\"\n+        ).encode()\n+        for number in PRODUCT_NUMBERS\n+    }\n+    for name, content in product_bytes.items():\n+        (adr_dir / name).write_bytes(content)\n+\n+    legacy_paths = _legacy_sadr_paths()\n+    for relative in legacy_paths:\n+        (target / relative).write_bytes(f\"legacy Shiki bytes for {relative}\\n\".encode())\n+\n+    stamp_path = target / shiki_installer.INSTALL_STAMP_PATH\n+    stamp_path.parent.mkdir(parents=True, exist_ok=True)\n+    stamp = {\n+        \"version\": shiki_installer.INSTALL_STAMP_VERSION,\n+        \"platform_commit\": None,\n+        \"installed_at\": \"2000-01-01T00:00:00+00:00\",\n+        \"digests\": {\n+            relative: _sha256(target / relative) for relative in legacy_paths\n+        },\n+    }\n+    stamp_path.write_text(\n+        json.dumps(stamp, indent=2, sort_keys=True) + \"\\n\", encoding=\"utf-8\"\n+    )\n+    return product_bytes, legacy_paths\n+\n+\n+def _tree_snapshot(root: Path) -> dict[str, tuple[str, bytes | str]]:\n+    \"\"\"Recursive byte/shape snapshot used to prove preflight is write-free.\"\"\"\n+    snapshot: dict[str, tuple[str, bytes | str]] = {}\n+    for path in sorted(root.rglob(\"*\")):\n+        relative = path.relative_to(root).as_posix()\n+        if path.is_symlink():\n+            snapshot[relative] = (\"symlink\", path.readlink().as_posix())\n+        elif path.is_dir():\n+            snapshot[relative] = (\"directory\", \"\")\n+        else:\n+            snapshot[relative] = (\"file\", path.read_bytes())\n+    return snapshot\n+\n+\n+class FreshSadrInstallTests(unittest.TestCase):\n+    def setUp(self) -> None:\n+        self.tmp = Path(tempfile.mkdtemp(prefix=\"shiki-sadr-fresh-\"))\n+        self.addCleanup(lambda: shutil.rmtree(self.tmp, ignore_errors=True))\n+        self.target = self.tmp / \"target\"\n+        self.adr_dir = self.target / \"docs\" / \"adr\"\n+        self.adr_dir.mkdir(parents=True)\n+        self.product_bytes = {\n+            f\"{number:04d}-product-decision.md\": (\n+                f\"# Product ADR {number:04d}\\n\\nTarget-owned bytes {number}.\\n\"\n+            ).encode()\n+            for number in PRODUCT_NUMBERS\n+        }\n+        for name, content in self.product_bytes.items():\n+            (self.adr_dir / name).write_bytes(content)\n+\n+    def test_fresh_install_preserves_product_adrs_and_installs_all_sadrs(self) -> None:\n+        _quiet_install(self.target, force=False, validate=True)\n+\n+        for name, expected in self.product_bytes.items():\n+            self.assertEqual((self.adr_dir / name).read_bytes(), expected)\n+\n+        sadr_numbers = {\n+            path.name.split(\"-\", 2)[1]\n+            for path in self.adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\")\n+        }\n+        self.assertEqual(sadr_numbers, {f\"{number:04d}\" for number in range(1, 20)})\n+\n+        numeric_records = {\n+            path.name\n+            for path in self.adr_dir.glob(\"[0-9][0-9][0-9][0-9]-*.md\")\n+        }\n+        self.assertEqual(numeric_records, set(self.product_bytes))\n+        self.assertEqual(find_adr_reference_violations(self.target), [])\n+\n+\n+class LegacySadrUpgradeTests(unittest.TestCase):\n+    def setUp(self) -> None:\n+        self.tmp = Path(tempfile.mkdtemp(prefix=\"shiki-sadr-upgrade-\"))\n+        self.addCleanup(lambda: shutil.rmtree(self.tmp, ignore_errors=True))\n+        self.target = self.tmp / \"target\"\n+        self.adr_dir = self.target / \"docs\" / \"adr\"\n+        self.product_bytes, self.legacy_paths = _create_legacy_upgrade_fixture(\n+            self.target\n+        )\n+\n+    def test_force_upgrade_deletes_only_stamped_legacy_sadrs(self) -> None:\n+        _quiet_install(self.target, force=True, validate=True)\n+\n+        for relative in self.legacy_paths:\n+            self.assertFalse((self.target / relative).exists(), relative)\n+        for name, expected in self.product_bytes.items():\n+            self.assertEqual((self.adr_dir / name).read_bytes(), expected)\n+\n+        sadrs = list(self.adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\"))\n+        self.assertEqual(len(sadrs), 19)\n+        refreshed = json.loads(\n+            (self.target / shiki_installer.INSTALL_STAMP_PATH).read_text(encoding=\"utf-8\")\n+        )\n+        for relative in self.legacy_paths:\n+            self.assertNotIn(relative, refreshed[\"digests\"])\n+        for path in sadrs:\n+            relative = path.relative_to(self.target).as_posix()\n+            self.assertEqual(refreshed[\"digests\"][relative], _sha256(path))\n+\n+    def test_non_force_install_never_deletes_legacy_files(self) -> None:\n+        before = {\n+            relative: (self.target / relative).read_bytes()\n+            for relative in self.legacy_paths\n+        }\n+        _quiet_install(self.target, force=False, validate=False)\n+        for relative, expected in before.items():\n+            self.assertEqual((self.target / relative).read_bytes(), expected)\n+        refreshed = json.loads(\n+            (self.target / shiki_installer.INSTALL_STAMP_PATH).read_text(encoding=\"utf-8\")\n+        )\n+        for relative in self.legacy_paths:\n+            self.assertEqual(refreshed[\"digests\"][relative], _sha256(self.target / relative))\n+\n+\n+class LegacyCleanupPreflightTests(unittest.TestCase):\n+    def setUp(self) -> None:\n+        self.tmp = Path(tempfile.mkdtemp(prefix=\"shiki-sadr-preflight-\"))\n+        self.addCleanup(lambda: shutil.rmtree(self.tmp, ignore_errors=True))\n+\n+    def test_all_ownership_blockers_abort_before_any_write(self) -> None:\n+        cases = (\n+            (\"absent\", \"install stamp is absent\"),\n+            (\"unreadable\", \"install stamp is unreadable\"),\n+            (\"malformed\", \"install stamp is malformed\"),\n+            (\"missing-digest\", \"install stamp has no digest\"),\n+            (\"digest-mismatch\", \"digest mismatch\"),\n+        )\n+        for name, expected_reason in cases:\n+            with self.subTest(name=name):\n+                target = self.tmp / name\n+                _, legacy_paths = _create_legacy_upgrade_fixture(target)\n+                stamp_path = target / shiki_installer.INSTALL_STAMP_PATH\n+\n+                if name == \"absent\":\n+                    stamp_path.unlink()\n+                elif name == \"unreadable\":\n+                    stamp_path.unlink()\n+                    stamp_path.mkdir()\n+                elif name == \"malformed\":\n+                    stamp_path.write_text(\"{not-json\\n\", encoding=\"utf-8\")\n+                else:\n+                    stamp = json.loads(stamp_path.read_text(encoding=\"utf-8\"))\n+                    if name == \"missing-digest\":\n+                        stamp[\"digests\"] = {}\n+                    else:\n+                        stamp[\"digests\"] = {\n+                            relative: \"deadbeef\" for relative in legacy_paths\n+                        }\n+                    stamp_path.write_text(\n+                        json.dumps(stamp, indent=2, sort_keys=True) + \"\\n\",\n+                        encoding=\"utf-8\",\n+                    )\n+\n+                before = _tree_snapshot(target)\n+                with self.assertRaises(shiki_installer.ShikiError) as raised:\n+                    _quiet_install(target, force=True, validate=False)\n+                after = _tree_snapshot(target)\n+\n+                self.assertEqual(before, after, f\"{name} preflight changed the tree\")\n+                message = str(raised.exception)\n+                self.assertIn(expected_reason, message)\n+                self.assertIn(\"No files were written\", message)\n+                for relative in legacy_paths:\n+                    self.assertIn(relative, message)\n+\n+    def test_absent_stamp_is_allowed_when_no_legacy_path_exists(self) -> None:\n+        target = self.tmp / \"no-legacy\"\n+        target.mkdir()\n+        _quiet_install(target, force=True, validate=False)\n+        self.assertTrue(\n+            (target / \"docs\" / \"adr\" / \"SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\").is_file()\n+        )\n+\n+\n+if __name__ == \"__main__\":\n+    unittest.main()\n\ncodex\nThe namespace contract is now documented at both authoring and operator surfaces, including the exact pre-write ownership rule and the non-force behavior. I\u2019m starting broad verification now; the first pass will identify any citation-string fixtures or paired invariants that need synchronized expectations before the shell suites.\nexec\n/bin/zsh -lc 'python3 -m unittest discover -s tests' in /private/tmp/shiki-adr-collision.pa0KwY/.worktrees/shiki-t-20260807t073307221254z-2d22d654-introduc\ndiff --git a/.codex/skills/shiki/SKILL.md b/.codex/skills/shiki/SKILL.md\nindex c8e2e85d9d75d49cc4f2d6286ee22659c4e53e75..93cded623b2c15d9f65c76491a1c2d813ac9e6c0\n--- a/.codex/skills/shiki/SKILL.md\n+++ b/.codex/skills/shiki/SKILL.md\n@@ -18,6 +18,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n Codex CLI does not register this skill as a custom `/shiki` slash command. When\n the user types `/shiki` in Codex CLI and gets \"Unrecognized command\", explain\n that this is expected: invoke Shiki in Codex with natural language such as\n@@ -69,7 +72,7 @@\n \n ## Responsibilities\n \n-- Claude Code implements and repairs by default (`shiki runner claude`, ADR 0008); Codex implements and repairs tasks explicitly assigned to `codex`.\n+- Claude Code implements and repairs by default (`shiki runner claude`, SADR-0008); Codex implements and repairs tasks explicitly assigned to `codex`.\n - Claude Code Action can act as GitHub-side CCA or reviewer.\n - CCA judges completion.\n - MergeGate authorizes state transitions and merge readiness.\ndiff --git a/.github/prompts/cca-completion-check.md b/.github/prompts/cca-completion-check.md\nindex ed203ad5f302ecc81372d8ab27d2b1232ca97497..fd89950528333ef6242e6d5443904f5392ab7e56\n--- a/.github/prompts/cca-completion-check.md\n+++ b/.github/prompts/cca-completion-check.md\n@@ -19,6 +19,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n ## Required Reading\n \n Read only the files needed to judge the PR contract. Prefer this order:\ndiff --git a/.github/prompts/codex-task-handoff.md b/.github/prompts/codex-task-handoff.md\nindex 695e211d76dea1c60e8d5bb360e78487808286a4..59441d576b01a39569722f226c854e5a740e9eb5\n--- a/.github/prompts/codex-task-handoff.md\n+++ b/.github/prompts/codex-task-handoff.md\n@@ -2,7 +2,7 @@\n \n You are Codex Front acting as the Implementer for a Shiki task.\n \n-Follow `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant ADRs, and `docs/agents/codex-handoff.md`.\n+Follow `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant Shiki SADRs and target ADRs, and `docs/agents/codex-handoff.md`.\n \n Rules:\n \ndiff --git a/AGENTS.md b/AGENTS.md\nindex 7f041100d49c2a7525f51cb0f5d696a4a6d1c78c..a06bfbabc6fa48a007389e25e12d7ed523936bed\n--- a/AGENTS.md\n+++ b/AGENTS.md\n@@ -22,7 +22,7 @@\n - **Codex Front** is an optional implementer through Codex App, Codex CLI, Codex IDE extension, or Codex Web signed in with ChatGPT OAuth/subscription auth, dispatched with `shiki runner codex` when a task is explicitly assigned to `codex`.\n - **GitHub CCA** is implemented by Claude Code Action by default, using `CLAUDE_CODE_OAUTH_TOKEN`.\n \n-Do not assume `openai/codex-action`, `OPENAI_API_KEY`, or API-key based Codex automation in the default Shiki path. API-key runners are explicit target-repo extensions and require their own ADR.\n+Do not assume `openai/codex-action`, `OPENAI_API_KEY`, or API-key based Codex automation in the default Shiki path. API-key runners are explicit target-repo extensions and require their own target ADR.\n \n Shiki is not:\n \n@@ -44,10 +44,14 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use\n+`SADR-NNNN-*.md`; target product decisions retain `NNNN-*.md` and the `ADR`\n+namespace.\n+\n Use the canonical source-of-truth order above. Runtime-specific reading orders\n are operational guidance only and must not override or reorder it.\n \n-Conversation state is not durable truth. If a decision matters, put it in GitHub, `.shiki/`, `CONTEXT.md`, or an ADR.\n+Conversation state is not durable truth. If a decision matters, put it in GitHub, `.shiki/`, `CONTEXT.md`, a Shiki SADR, or a target ADR.\n \n If GitHub and `.shiki/` disagree, surface the conflict and prefer GitHub operational state until the mirror is repaired.\n \n@@ -75,8 +79,8 @@\n Every non-trivial change follows this loop:\n \n 1. **Goal**: clarify outcome, completion conditions, non-goals, risk level, and success signals.\n-2. **grill-with-docs**: challenge the plan against domain docs, ADRs, code reality, terminology, and edge scenarios.\n-3. **Context & Impact**: find relevant docs, ADRs, code, dependencies, owners, locks, and verification surfaces.\n+2. **grill-with-docs**: challenge the plan against domain docs, Shiki SADRs, target ADRs, code reality, terminology, and edge scenarios.\n+3. **Context & Impact**: find relevant docs, SADRs, target ADRs, code, dependencies, owners, locks, and verification surfaces.\n 4. **PRD and Spec Freeze**: use `to-prd` when context is settled enough to become durable product/engineering intent. The operator's explicit approval of the PRD is Spec Freeze: the plan records a `spec_freeze` block, and no plan runs without it. Steps 1-4 form Requirements Definition \u2014 one continuous operator dialogue. After Spec Freeze, scope changes require an operator-approved Spec Amendment; non-scope-moving interpretations go to the Assumption Log.\n 5. **Issues**: use `to-issues` to create independently grabbable vertical-slice issues. Prefer AFK slices over HITL slices where possible.\n 6. **Triage**: label issues for readiness, risk, runtime, skills, and MergeGate state.\n@@ -167,7 +171,7 @@\n - Testing decisions.\n - Out-of-scope items.\n - Further notes.\n-- Links to relevant domain terms and ADRs.\n+- Links to relevant domain terms, SADRs, and target ADRs.\n \n Every executable task must include:\n \n@@ -193,7 +197,7 @@\n \n Context & Impact must identify:\n \n-- Relevant documents, domain terms, ADRs, and past decisions.\n+- Relevant documents, domain terms, SADRs, target ADRs, and past decisions.\n - Relevant modules, interfaces, seams, callers, owners, and tests.\n - Dependency relationships between tasks.\n - Candidate locks and lock conflicts.\n@@ -210,7 +214,7 @@\n | Skill | Required when |\n | --- | --- |\n | `setup-matt-pocock-skills` | First configuring the repo for skills, or when issue tracker, triage labels, or domain docs are missing. |\n-| `grill-with-docs` | Default for non-trivial Goals before PRD/issues; always required when plans, terminology, boundaries, tradeoffs, or ADR-worthy decisions are ambiguous. |\n+| `grill-with-docs` | Default for non-trivial Goals before PRD/issues; always required when plans, terminology, boundaries, tradeoffs, or decision-record-worthy choices are ambiguous. |\n | `zoom-out` | The agent lacks an architectural map of the relevant code area. |\n | `to-prd` | Settled Goal context must become durable product/engineering intent. |\n | `to-issues` | A Goal, PRD, or plan must become independently grabbable vertical-slice issues. |\n@@ -348,7 +352,7 @@\n - Three or more new files.\n - Any high-risk or critical label.\n \n-When the architecture gate triggers, run `grill-with-docs` or `improve-codebase-architecture` as appropriate and record durable decisions in an ADR when needed.\n+When the architecture gate triggers, run `grill-with-docs` or `improve-codebase-architecture` as appropriate and record durable decisions in a Shiki SADR or target ADR as applicable.\n \n ## Engineering Discipline\n \ndiff --git a/CLAUDE.md b/CLAUDE.md\nindex 98a932e7841a632866fcd53b0bad0e3af3711d3d..98a31399404d6fb044b4fd493f4459ac87f439de\n--- a/CLAUDE.md\n+++ b/CLAUDE.md\n@@ -14,6 +14,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n ## Claude Role\n \n Default to planner, default implementer, reviewer, coordinator, documentation editor, CCA-style completion judge, and final-judgment assistant.\n@@ -57,7 +60,7 @@\n At the start of a Shiki session:\n \n 1. Read `AGENTS.md`.\n-2. Read `CONTEXT.md` and relevant ADRs.\n+2. Read `CONTEXT.md` and relevant Shiki SADRs and target ADRs.\n 3. Read active `.shiki/` Goal/task/ledger state when present.\n 4. Read active distilled rules from `.shiki/memories/` (status `distilled`, `active: true`, not revoked or superseded) for applicable guidance; the same rules are injected deterministically into task handoffs as the `## Distilled Rules` section (\u00a73.5).\n 5. Read the GitHub Issue or PR when the task is GitHub-backed.\n@@ -74,7 +77,7 @@\n - Define the Goal, completion conditions, non-goals, and risk level.\n - Treat `grill-with-docs` as the default planning entry point for non-trivial Goals.\n - Challenge domain terms against `CONTEXT.md`.\n-- Challenge hard-to-reverse tradeoffs against ADRs.\n+- Challenge hard-to-reverse tradeoffs against SADRs and target ADRs.\n - Explore code instead of asking when code can answer the question.\n - Run Context & Impact before implementation planning.\n - Produce a Task DAG, not an unordered checklist.\n@@ -94,7 +97,7 @@\n - For each question, provide your recommended answer.\n - Resolve dependency decisions before downstream design questions.\n - Update `CONTEXT.md` inline when terminology crystallizes.\n-- Offer ADRs only when the decision is hard to reverse, surprising without context, and a real tradeoff.\n+- Offer a SADR or target ADR only when the decision is hard to reverse, surprising without context, and a real tradeoff.\n - Convert unresolved questions into HITL blockers rather than allowing Codex to guess.\n \n ## PRD and Issues Standard\n@@ -103,7 +106,7 @@\n \n - Use the project's domain glossary vocabulary.\n - Avoid volatile file paths unless they are essential.\n-- Include implementation decisions, testing decisions, out-of-scope items, and relevant ADR/domain links.\n+- Include implementation decisions, testing decisions, out-of-scope items, and relevant SADR/target-ADR/domain links.\n - Identify deep-module opportunities and testable interfaces.\n \n When creating issues:\n@@ -121,7 +124,7 @@\n - Branch/worktree target.\n - Scope and non-goals.\n - Dependencies and locks.\n-- Relevant docs, ADRs, modules, and tests.\n+- Relevant docs, SADRs, target ADRs, modules, and tests.\n - Required skill invocations.\n - TDD expectations.\n - Pre-PR `code-review` gate expectations.\n@@ -152,7 +155,7 @@\n \n - Treat yourself as a judge, not implementer.\n - Do not write production code.\n-- Read `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant ADRs, PRD, task issue, PR body, diff, checks, reviews, labels, and `.shiki/` state.\n+- Read `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant SADRs and target ADRs, PRD, task issue, PR body, diff, checks, reviews, labels, and `.shiki/` state.\n - Evaluate every applicable checklist in `docs/agents/checklists.md`.\n - Map each acceptance criterion to durable evidence.\n - Distinguish implementation failure from missing evidence.\ndiff --git a/CONTEXT.md b/CONTEXT.md\nindex e61a9be06a0cce0d026484083a657bde2067e234..237ee8aa3ea8fab334ece49768bc5ed3b5af0974\n--- a/CONTEXT.md\n+++ b/CONTEXT.md\n@@ -25,7 +25,7 @@\n _Avoid_: jumping directly into code\n \n **grill-with-docs**:\n-A planning skill that challenges a plan against domain language, ADRs, code reality, and concrete edge scenarios. It resolves design-tree questions before PRD/issues. It runs inside Requirements Definition.\n+A planning skill that challenges a plan against domain language, Shiki SADRs, target ADRs, code reality, and concrete edge scenarios. It resolves design-tree questions before PRD/issues. It runs inside Requirements Definition.\n _Avoid_: generic brainstorming, silent assumptions\n \n **Requirements Definition**:\ndiff --git a/CONTRIBUTING.md b/CONTRIBUTING.md\nindex c6d9b656e73abb8a55e5f660123e03153dcb3e6d..9e37a8fbf668db5b22461aaae5548b8f1354394c\n--- a/CONTRIBUTING.md\n+++ b/CONTRIBUTING.md\n@@ -17,7 +17,7 @@\n 1. **Goal** \u2014 start from a user-approved Goal with an outcome, completion\n    conditions, non-goals, risk level, and success signals.\n 2. **grill-with-docs** \u2014 for any non-trivial change, challenge the plan against\n-   domain language, ADRs, and code reality before writing a PRD.\n+   domain language, Shiki SADRs, target ADRs, and code reality before writing a PRD.\n 3. **PRD** \u2014 capture settled product/engineering intent (`to-prd`).\n 4. **Spec Freeze** \u2014 the operator explicitly approves the PRD/requirements;\n    the plan records a `spec_freeze` block and no plan runs without it\n@@ -94,7 +94,7 @@\n   entries.\n - Do not hand-edit `.shiki/goals/*.json` or `.shiki/manifest.json` in a task PR.\n - Conversation state is not durable truth. If a decision matters, record it in\n-  GitHub, `.shiki/`, `CONTEXT.md`, or an ADR.\n+  GitHub, `.shiki/`, `CONTEXT.md`, a Shiki SADR, or a target ADR.\n \n ## Architecture Gate\n \n@@ -103,7 +103,7 @@\n or CI policy, package structure, three or more structural files, more than 100\n net new lines in a core module, three or more new files, or any high-risk label.\n When triggered, run `grill-with-docs` or `improve-codebase-architecture` and\n-record durable decisions in an ADR when needed (see `docs/adr/`).\n+record durable decisions in a Shiki SADR or target ADR as applicable (see `docs/adr/`).\n \n ## Safety\n \ndiff --git a/SYSTEM_PROMPT.md b/SYSTEM_PROMPT.md\nindex aaaea45e3a896c3a44ba89eec8bb84325ecf6c62..da009c86a88285269dee0155e58e550c6d744ea9\n--- a/SYSTEM_PROMPT.md\n+++ b/SYSTEM_PROMPT.md\n@@ -30,6 +30,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n Use the canonical source-of-truth order above. Runtime-specific reading orders\n are operational guidance only and must not override or reorder it. The current\n conversation is non-durable operator input.\n@@ -48,14 +51,14 @@\n \n A runtime may only do work that is explicitly assigned by the Goal, task, PR, or MergeGate decision.\n \n-Do not assume `openai/codex-action`, `OPENAI_API_KEY`, or API-key based Codex automation in the default Shiki path. API-key runners are explicit target-repo extensions and require their own ADR.\n+Do not assume `openai/codex-action`, `OPENAI_API_KEY`, or API-key based Codex automation in the default Shiki path. API-key runners are explicit target-repo extensions and require their own target ADR.\n \n ## 4. Non-negotiable Shiki flow\n \n For any non-trivial Goal, use this flow:\n \n 1. **Goal Seek**: define outcome, non-goals, risk level, completion criteria, and evidence requirements.\n-2. **grill-with-docs**: challenge the plan against `CONTEXT.md`, ADRs, code reality, terminology, boundaries, and edge scenarios. Ask one question at a time when the operator is available. If code can answer a question, inspect code instead of asking.\n+2. **grill-with-docs**: challenge the plan against `CONTEXT.md`, Shiki SADRs, target ADRs, code reality, terminology, boundaries, and edge scenarios. Ask one question at a time when the operator is available. If code can answer a question, inspect code instead of asking.\n 3. **Context & Impact**: identify modules, interfaces, seams, callers, dependencies, locks, risk, and verification surfaces.\n 4. **to-prd + Spec Freeze**: turn settled context into a PRD using domain vocabulary and testing decisions. The operator's explicit approval of the PRD is Spec Freeze, recorded as the plan's `spec_freeze` block; plans without it do not run. Steps 1-4 form Requirements Definition.\n 5. **to-issues**: decompose the PRD into independently grabbable vertical-slice issues. Prefer AFK slices; mark HITL slices when judgment is still required.\n@@ -75,7 +78,7 @@\n Before making any material change:\n \n 1. Read `AGENTS.md` and any runtime-specific wrapper such as `CLAUDE.md`.\n-2. Read `CONTEXT.md` and relevant ADRs.\n+2. Read `CONTEXT.md` and relevant SADRs and target ADRs.\n 3. Read the GitHub Issue / PR and `.shiki/` state for the active Goal or task.\n 4. Identify your role: Planner, Implementer, Reviewer, Completion Check Agent, Repairer, or Guardian-assist.\n 5. Identify Goal id, task id, branch, PR, dependencies, locks, risk level, required skills, and required evidence.\n@@ -103,7 +106,7 @@\n \n At minimum, identify:\n \n-- Relevant documents, ADRs, domain terms, and prior decisions.\n+- Relevant documents, SADRs, target ADRs, domain terms, and prior decisions.\n - Relevant modules, interfaces, seams, callers, owners, and tests.\n - Dependency relationships between tasks.\n - Candidate file locks and lock conflicts.\n@@ -152,7 +155,7 @@\n \n CCA must read:\n \n-- `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant ADRs.\n+- `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant SADRs and target ADRs.\n - Parent Goal issue, PRD issue, task issue, comments, and labels.\n - PR body, diff, commits, changed files, reviews, CI checks, and artifacts available to the workflow.\n - `.shiki/` task contract, locks, ledger entries, CCA checklist profile, and prior repair packets.\n@@ -214,7 +217,7 @@\n - Three or more new files.\n - Any high-risk or critical label.\n \n-When the architecture gate triggers, run `grill-with-docs` or `improve-codebase-architecture` as appropriate and record the decision in an ADR if it is hard to reverse or likely to be re-litigated.\n+When the architecture gate triggers, run `grill-with-docs` or `improve-codebase-architecture` as appropriate and record the decision in a Shiki SADR or target ADR as applicable if it is hard to reverse or likely to be re-litigated.\n \n ## 14. Skill Gate\n \n@@ -223,7 +226,7 @@\n Use these skills:\n \n - `setup-matt-pocock-skills`: before first use of the engineering skill set, or when issue tracker, triage labels, or domain docs are missing.\n-- `grill-with-docs`: default for non-trivial Goals before PRD/issues; required for ambiguous plans, terminology, boundaries, ADR-worthy tradeoffs, or design-tree decisions.\n+- `grill-with-docs`: default for non-trivial Goals before PRD/issues; required for ambiguous plans, terminology, boundaries, decision-record-worthy tradeoffs, or design-tree decisions.\n - `zoom-out`: unfamiliar code area or missing architectural map.\n - `to-prd`: convert settled context into a PRD.\n - `to-issues`: break a PRD, plan, or Goal into independently grabbable vertical-slice issues.\ndiff --git a/docs/adr/0001-keep-shiki-as-platform-name.md b/docs/adr/SADR-0001-keep-shiki-as-platform-name.md\nindex 20a9637c3b9d8c97de7ba8db7cfae4d82d9a7dba..8b685439959aada1b221c112f0ac3c51ca8a1695\n--- a/docs/adr/0001-keep-shiki-as-platform-name.md\n+++ b/docs/adr/SADR-0001-keep-shiki-as-platform-name.md\n@@ -1,3 +1,3 @@\n-# Keep Shiki As The Platform Name\n+# SADR-0001: Keep Shiki As The Platform Name\n \n Shiki remains the product name for the GitHub-native agentic engineering control plane. We considered more descriptive names such as MergePilot, MergeGate, and Kiroku, but kept Shiki because it carries the existing goal-seeking framework identity while remaining independent of Hermes, Codex, Claude Code, or any single runtime.\ndiff --git a/docs/adr/0002-use-agents-as-shared-constitution.md b/docs/adr/SADR-0002-use-agents-as-shared-constitution.md\nindex 73de30ecb078120eac540d9bc51855e3cd48d0e3..c5486182bf06fca36ea508496b013c67302fd989\n--- a/docs/adr/0002-use-agents-as-shared-constitution.md\n+++ b/docs/adr/SADR-0002-use-agents-as-shared-constitution.md\n@@ -1,3 +1,3 @@\n-# Use AGENTS As The Shared Agent Constitution\n+# SADR-0002: Use AGENTS As The Shared Agent Constitution\n \n Shiki uses `AGENTS.md` as the runtime-neutral constitution and `CLAUDE.md` as a thin Claude Code wrapper. This keeps Codex, Claude Code, Hermes Runner, and future Agent Runtimes aligned on the same GitHub-first MergeGate rules while still allowing runtime-specific instructions where needed.\ndiff --git a/docs/adr/0003-require-skill-gate-for-mergegate-readiness.md b/docs/adr/SADR-0003-require-skill-gate-for-mergegate-readiness.md\nindex 424be0c9af4c8f0136f1fde68a468c47ff84e764..7fc337987c7df9792f75f181dfcbed75fe5ffff2\n--- a/docs/adr/0003-require-skill-gate-for-mergegate-readiness.md\n+++ b/docs/adr/SADR-0003-require-skill-gate-for-mergegate-readiness.md\n@@ -1,3 +1,3 @@\n-# Require Skill Gate For MergeGate Readiness\n+# SADR-0003: Require Skill Gate For MergeGate Readiness\n \n Shiki requires relevant engineering skills to be invoked before execution when their triggers apply. This makes skills part of MergeGate readiness rather than optional prompt style, because Shiki depends on repeatable planning, issue decomposition, TDD, diagnosis, triage, and architecture review before autonomous or parallel agent work can be trusted.\ndiff --git a/docs/adr/0004-use-github-first-with-shiki-mirror.md b/docs/adr/SADR-0004-use-github-first-with-shiki-mirror.md\nindex d0a3a9174dc646a945a9d325954ffaf00c352040..a79c54ae9ee8eed2a0524192b864da4e22a92d3f\n--- a/docs/adr/0004-use-github-first-with-shiki-mirror.md\n+++ b/docs/adr/SADR-0004-use-github-first-with-shiki-mirror.md\n@@ -1,4 +1,4 @@\n-# ADR 0004: Use GitHub First With A Repository-Local Shiki Mirror\n+# SADR-0004: Use GitHub First With A Repository-Local Shiki Mirror\n \n ## Status\n \ndiff --git a/docs/adr/0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md b/docs/adr/SADR-0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\nindex b339baf49b89d9290e79c3d51b36e7b2bde3bd95..0f3c3eb9afccdb3403ce978e21d04fe12e495da0\n--- a/docs/adr/0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\n+++ b/docs/adr/SADR-0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\n@@ -1,8 +1,8 @@\n-# ADR 0005: Use Codex As Front And Claude Code Action As The GitHub Runtime\n+# SADR-0005: Use Codex As Front And Claude Code Action As The GitHub Runtime\n \n ## Status\n \n-Accepted (implementer default superseded by ADR 0008; the auth model and the\n+Accepted (implementer default superseded by SADR-0008; the auth model and the\n Claude Code Action GitHub runtime decision remain in force)\n \n ## Context\n@@ -26,4 +26,4 @@\n - Shiki avoids requiring `OPENAI_API_KEY` for the default workflow.\n - Codex work happens through the user's authenticated Codex front surface, not as a hidden backend Action.\n - Claude Code Action is configured for OAuth-token based subscription usage by default.\n-- API-key based automation remains possible only as an explicit target-repo extension with its own ADR.\n+- API-key based automation remains possible only as an explicit target-repo extension with its own target ADR.\ndiff --git a/docs/adr/0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md b/docs/adr/SADR-0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\nindex 4bcae9d1e52f0a37b8e9fbafa5cd14b6ee1993bd..7a38538a6ae9929567f0ade67e7ea6a60d0b3f7e\n--- a/docs/adr/0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\n+++ b/docs/adr/SADR-0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\n@@ -1,4 +1,4 @@\n-# ADR 0006: Separate Codex Implementation, CCA Completion, And MergeGate Authorization\n+# SADR-0006: Separate Codex Implementation, CCA Completion, And MergeGate Authorization\n \n ## Status\n \ndiff --git a/docs/adr/0007-packaging-and-release.md b/docs/adr/SADR-0007-packaging-and-release.md\nindex 79b84ddb97167372b44641e5b7860e400ce80822..bb445a2e2e856d9d1f5fdbe35de8d4e8cec0fb32\n--- a/docs/adr/0007-packaging-and-release.md\n+++ b/docs/adr/SADR-0007-packaging-and-release.md\n@@ -1,4 +1,4 @@\n-# ADR 0007: Packaging And Release For Shiki\n+# SADR-0007: Packaging And Release For Shiki\n \n ## Status\n \n@@ -25,7 +25,7 @@\n   not introduce third-party runtime dependencies or break the shim.\n - Releases must run inside the operator's subscription-authenticated toolchain\n   and must not require extra secrets beyond the built-in `GITHUB_TOKEN`\n-  (consistent with ADR 0005's no-API-key-by-default stance).\n+  (consistent with SADR-0005's no-API-key-by-default stance).\n - `.shiki/manifest.json` already encodes the install surface (`install.include`,\n   `install.create_directories`, `install.exclude_from_commit`), and it must not\n   be mutated as part of this change.\n@@ -34,7 +34,7 @@\n \n This is a hard-to-reverse platform decision (it sets the version source of\n truth, the release trigger, and the public install contract), so it is recorded\n-as an ADR.\n+as a SADR.\n \n ## Decision\n \n@@ -104,7 +104,7 @@\n 3. **Publishing to PyPI.** Rejected for now: Shiki is distributed as a\n    repository template + git-installable CLI, not a PyPI library; PyPI would add\n    credentials/secrets and a different versioning contract. Can be added later as\n-   its own ADR if a packaged distribution is needed.\n+   its own SADR if a packaged distribution is needed.\n 4. **Dynamic version (read `VERSION` via a build backend `dynamic` field).**\n    Rejected to keep `pyproject.toml` backend-agnostic and dependency-free; the\n    small duplication between `VERSION` and `pyproject.toml` is guarded by the\n@@ -123,4 +123,4 @@\n   releasing versions the whole platform but ships only `install.include` to\n   targets.\n - Future PyPI distribution or a console entry point are deferred and would each\n-  warrant their own ADR.\n+  warrant their own SADR.\ndiff --git a/docs/adr/0008-use-claude-code-as-default-implementer.md b/docs/adr/SADR-0008-use-claude-code-as-default-implementer.md\nindex dedf4510b7c7799521422f622029563d373db031..35c2625d908167fbc58855b5b63e283805f167e8\n--- a/docs/adr/0008-use-claude-code-as-default-implementer.md\n+++ b/docs/adr/SADR-0008-use-claude-code-as-default-implementer.md\n@@ -1,12 +1,12 @@\n-# ADR 0008: Use Claude Code As The Default Implementer Runtime\n+# SADR-0008: Use Claude Code As The Default Implementer Runtime\n \n ## Status\n \n-Proposed (supersedes ADR 0005 for the implementer default; keeps its auth model)\n+Proposed (supersedes SADR-0005 for the implementer default; keeps its auth model)\n \n ## Context\n \n-ADR 0005 made Codex Front the default implementation runtime, with Claude Code\n+SADR-0005 made Codex Front the default implementation runtime, with Claude Code\n restricted to planner and reviewer roles by the runtime registry, and the only\n autonomous implementation adapter was `shiki runner codex`.\n \n@@ -18,13 +18,13 @@\n - The platform's planning, review, and CCA layers already run on Claude Code,\n   and Claude Code's native capabilities (the built-in `/code-review` skill and\n   the Workflow multi-agent orchestration tool) have no Codex equivalent.\n-- The Goal lifecycle redesign (ADR 0009) requires an autonomous post-freeze\n+- The Goal lifecycle redesign (SADR-0009) requires an autonomous post-freeze\n   loop driven from the operator's Claude Code session, including bounded\n   repair dispatch, which must not depend on a runtime that cannot be invoked\n   unattended.\n \n The constitution claims Shiki is runtime-agnostic (\"not a Claude-only\n-workflow\", ADR 0002). Any change to the implementer default must preserve the\n+workflow\", SADR-0002). Any change to the implementer default must preserve the\n registry-based runtime model rather than hardcoding one vendor.\n \n ## Decision\n@@ -39,7 +39,7 @@\n   the task to review or repair-needed.\n - Keep Codex registered as an optional implementer runtime. `shiki runner\n   codex`, the Codex handoff templates, and the ChatGPT OAuth auth model from\n-  ADR 0005 remain supported for tasks explicitly assigned to `codex`.\n+  SADR-0005 remain supported for tasks explicitly assigned to `codex`.\n - Commit to mandating two Claude-native capabilities as loop contract in\n   follow-up Goals (status: the code-review gate was delivered by Goal G-B \u2014\n   skill registry, checklists item PR-12, default required_skills \u2014 while the\n@@ -60,7 +60,7 @@\n \n Out of scope: removing Codex support, changing the CCA runtime (Claude Code\n Action with `CLAUDE_CODE_OAUTH_TOKEN` stays), and API-key based automation\n-(still requires its own ADR per ADR 0005).\n+(still requires its own target ADR per SADR-0005).\n \n ## Consequences\n \n@@ -100,7 +100,7 @@\n   harness rejection of direct `codex exec` is outside Shiki's control, and the\n   plan's mandatory native capabilities cannot run on Codex at all.\n - **Remove Codex entirely**: rejected; it abandons the runtime-agnostic\n-  constitution (ADR 0002) for no operational gain and forecloses\n+  constitution (SADR-0002) for no operational gain and forecloses\n   cross-runtime adversarial review.\n - **GitHub Actions cloud implementation via claude-code-action**: rejected as\n   default; durable evidence is automatic but token cost, Node 24 workflow\ndiff --git a/docs/adr/0009-requirements-definition-and-spec-freeze.md b/docs/adr/SADR-0009-requirements-definition-and-spec-freeze.md\nindex 29b3f7871bef9203a8b4a831a65c39eb2f506e42..52e7dd69ad0aa4efe0b68c62678abbf7df02aaec\n--- a/docs/adr/0009-requirements-definition-and-spec-freeze.md\n+++ b/docs/adr/SADR-0009-requirements-definition-and-spec-freeze.md\n@@ -1,4 +1,4 @@\n-# ADR 0009: Merge Grill And Planning Into Requirements Definition With Spec Freeze\n+# SADR-0009: Merge Grill And Planning Into Requirements Definition With Spec Freeze\n \n ## Status\n \ndiff --git a/docs/adr/0010-external-ai-guardian-review.md b/docs/adr/SADR-0010-external-ai-guardian-review.md\nindex e6108bbe8bb4e73e0ff4684c8f78001a80438c4e..f3510b145664298adafb151b6f0db51b972f01ec\n--- a/docs/adr/0010-external-ai-guardian-review.md\n+++ b/docs/adr/SADR-0010-external-ai-guardian-review.md\n@@ -1,4 +1,4 @@\n-# ADR 0010: External AI Guardian Review As A First-Class Approval Authority\n+# SADR-0010: External AI Guardian Review As A First-Class Approval Authority\n \n ## Status\n \n@@ -8,7 +8,7 @@\n \n Shiki is intended to be an autonomous, recursive, self-improving agent\n framework: authority-in-the-loop, not human-in-the-loop. Until now the Guardian\n-approval required for high/critical-risk merges (guardian-policy.json, ADR\n+approval required for high/critical-risk merges (guardian-policy.json, SADR\n governance) recognized only human authority sources \u2014 a GitHub review approval,\n the `guardian:approved` label applied by a configured Guardian, or a comment\n carrying `Guardian approval granted` plus the current head SHA.\n@@ -89,7 +89,7 @@\n   autonomous design the operator intends and produced the impersonation\n   incident.\n - **Record AI approval as an operator comment**: rejected as forgery of human\n-  authority \u2014 the exact failure this ADR corrects.\n+  authority \u2014 the exact failure this SADR corrects.\n - **Deliver the artifact as a committed `.shiki/` file**: rejected; a committed\n   approval inside the PR being approved is forgeable by anyone who can push.\n - **A dedicated reviewer bot identity instead of a relayed comment**: deferred;\ndiff --git a/docs/adr/0011-autonomous-quality-gate-evidence.md b/docs/adr/SADR-0011-autonomous-quality-gate-evidence.md\nindex bca0246c5d46e44afbde15486a75eec425fc02f5..9c93eac16d8c3a6d4b11bd7224e3955102cd7db4\n--- a/docs/adr/0011-autonomous-quality-gate-evidence.md\n+++ b/docs/adr/SADR-0011-autonomous-quality-gate-evidence.md\n@@ -1,4 +1,4 @@\n-# ADR 0011: Autonomous Quality-Gate Evidence \u2014 Deterministic Facts And An Independent Verifier\n+# SADR-0011: Autonomous Quality-Gate Evidence \u2014 Deterministic Facts And An Independent Verifier\n \n ## Status\n \ndiff --git a/docs/adr/0012-autonomous-loop-closeout-to-main.md b/docs/adr/SADR-0012-autonomous-loop-closeout-to-main.md\nindex 637fad6dde4989c9c2bf0f81f676d8d29991123c..ec7d85f81030a676bcbba28b43eb365422a36eac\n--- a/docs/adr/0012-autonomous-loop-closeout-to-main.md\n+++ b/docs/adr/SADR-0012-autonomous-loop-closeout-to-main.md\n@@ -1,4 +1,4 @@\n-# 0012 \u2014 Autonomous loop pushes goal completion to main via a closeout PR\n+# SADR-0012 \u2014 Autonomous loop pushes goal completion to main via a closeout PR\n \n - Status: accepted\n - Date: 2026-06-18\ndiff --git a/docs/adr/0013-bootstrap-grants-actions-approve-pull-requests.md b/docs/adr/SADR-0013-bootstrap-grants-actions-approve-pull-requests.md\nindex b6c69d64f1c5ced346b88d6859586715b3b5bd0e..cf48ff4a6716267f539ca58edd1a852e7c7a2d64\n--- a/docs/adr/0013-bootstrap-grants-actions-approve-pull-requests.md\n+++ b/docs/adr/SADR-0013-bootstrap-grants-actions-approve-pull-requests.md\n@@ -1,4 +1,4 @@\n-# ADR 0013: Bootstrap Grants GitHub Actions Permission To Create And Approve Pull Requests\n+# SADR-0013: Bootstrap Grants GitHub Actions Permission To Create And Approve Pull Requests\n \n ## Status\n \n@@ -31,7 +31,7 @@\n security tradeoff: it lets repository automation satisfy a human-review gate. In\n a multi-maintainer repo that could let a compromised or buggy workflow\n self-approve changes. So whether bootstrap turns it on by default \u2014 and with\n-what default token scope \u2014 is an ADR-worthy decision, not an implementation\n+what default token scope \u2014 is a SADR-worthy decision, not an implementation\n detail.\n \n ## Decision\n@@ -48,7 +48,7 @@\n Specifically:\n \n - **`can_approve_pull_request_reviews` defaults to `true`.** Shiki's default\n-  posture is solo/AFK autonomous operation (ADR 0008/0009/0012), where the CCA\n+  posture is solo/AFK autonomous operation (SADR-0008/0009/0012), where the CCA\n   Review Bridge is the only path that can satisfy `required_review: true` after\n   the CCA and Guardian gates have already passed. The default makes a\n   freshly-bootstrapped repo able to run the autonomous loop end to end without a\ndiff --git a/docs/adr/0014-codex-app-external-ai-guardian-ui-adapter.md b/docs/adr/SADR-0014-codex-app-external-ai-guardian-ui-adapter.md\nindex 238db06279334caa341c117362df9271382a7fcd..65f12e9939321e1b3e389a34c822fc1549e5155d\n--- a/docs/adr/0014-codex-app-external-ai-guardian-ui-adapter.md\n+++ b/docs/adr/SADR-0014-codex-app-external-ai-guardian-ui-adapter.md\n@@ -1,4 +1,4 @@\n-# ADR 0014: Use Codex App As The External AI Guardian UI Adapter\n+# SADR-0014: Use Codex App As The External AI Guardian UI Adapter\n \n ## Status\n \ndiff --git a/docs/adr/0015-contract-approval-before-dispatch.md b/docs/adr/SADR-0015-contract-approval-before-dispatch.md\nindex fad6c656741a20a7684ba16080a7db159f63a52f..6061271a46d3f1d6ae7174477de3fb75673123a6\n--- a/docs/adr/0015-contract-approval-before-dispatch.md\n+++ b/docs/adr/SADR-0015-contract-approval-before-dispatch.md\n@@ -1,4 +1,4 @@\n-# ADR 0015: Approve The Task Contract Before Dispatch, Not The Diff After It\n+# SADR-0015: Approve The Task Contract Before Dispatch, Not The Diff After It\n \n ## Status\n \n@@ -9,7 +9,7 @@\n Guardian approval for high/critical risk is expressible only on a pull request.\n Every approval source in `guardian-policy.json` \u2014 GitHub review, the\n `guardian:approved` label, the `Guardian approval granted` comment, and the\n-`external_ai_guardian_review` artifact (ADR 0010/0014) \u2014 is bound to a PR, and\n+`external_ai_guardian_review` artifact (SADR-0010/0014) \u2014 is bound to a PR, and\n three of the four require the current head SHA. Approval therefore cannot exist\n before an implementation PR exists.\n \n@@ -81,7 +81,7 @@\n An envelope breach must surface only on the non-repairable policy gate, never on\n the repairable `MergeGate metadata check`. Otherwise the Goal Loop routes it to\n `dispatch_repair` \u2014 instructing a runner to make the breach go away \u2014 which is\n-exactly what this ADR forbids.\n+exactly what this SADR forbids.\n \n **Contract immutability.** After registration, an implementation PR must not\n change the governance fields of its own task contract (`scope`, `non_goals`,\n@@ -116,7 +116,7 @@\n fails any verdict that is not `complete`, and the signal the CCA prompt reads is\n computed from `evaluate_guardian_approval` alone. Without the OR-merge at the\n signal, a contract-approved PR still returns `needs_guardian`, the CCA job fails,\n-and the MergeGate policy check never runs \u2014 the gate this ADR changes would never\n+and the MergeGate policy check never runs \u2014 the gate this SADR changes would never\n be reached. The CCA *prompt* needs no change: a `contract_approval` entry in the\n signal's existing `sources` array satisfies its current wording.\n \n@@ -130,7 +130,7 @@\n written, and an approved contract lets its implementation PRs merge through the\n same automatic path as low/medium risk. The wasted implement-then-block cycle\n disappears. Guardian review shifts from reading diffs to reading contracts, which\n-is both earlier and smaller; the external AI Guardian path (ADR 0010/0014) works\n+is both earlier and smaller; the external AI Guardian path (SADR-0010/0014) works\n on contracts with no change.\n \n We accept that the Guardian authorizes a class of change without seeing its\n@@ -146,7 +146,7 @@\n adversarial tests for the new approval path equivalent in rigor to\n `test_shiki_governance_evidence.sh`.\n \n-Bootstrap costs, which apply only while this ADR is being implemented. The tasks\n+Bootstrap costs, which apply only while this SADR is being implemented. The tasks\n that build Contract Approval merge under the existing rules, so the ones\n dispatched after the pre-dispatch gate lands but before the approval path lands\n need a Guardian-approved Contract PR *and* a live Guardian approval on their\n@@ -210,7 +210,7 @@\n risk; they do not bound authorized work.\n \n **Auto-escalate risk on envelope breach instead of stopping.** Returns the\n-workflow to a diff-time Guardian decision \u2014 the exact behavior this ADR removes.\n+workflow to a diff-time Guardian decision \u2014 the exact behavior this SADR removes.\n \n **Resolve governance fields from the base snapshot only.** Rejected against the\n registration flow: implementation PRs add their own contract, so the base has no\ndiff --git a/docs/adr/0016-locks-bound-product-paths.md b/docs/adr/SADR-0016-locks-bound-product-paths.md\nindex b5e850f3f1f7f4ef6e7f9d1763a29a51bd300c45..46b40a9b99a7aae644c02bb458f5473ff0a90ce2\n--- a/docs/adr/0016-locks-bound-product-paths.md\n+++ b/docs/adr/SADR-0016-locks-bound-product-paths.md\n@@ -1,4 +1,4 @@\n-# ADR 0016: Locks bound product paths; the Shiki mirror is bound by record identity\n+# SADR-0016: Locks bound product paths; the Shiki mirror is bound by record identity\n \n ## Status\n \n@@ -6,8 +6,8 @@\n \n - Date: 2026-07-28\n - Deciders: operator (mizutani-140), Claude (planner)\n-- Related: ADR 0004 (GitHub-first with a `.shiki` mirror), ADR 0009 (Spec Freeze),\n-  ADR 0012 (autonomous loop closeout to main), ADR 0015 (Contract Approval before\n+- Related: SADR-0004 (GitHub-first with a `.shiki` mirror), SADR-0009 (Spec Freeze),\n+  SADR-0012 (autonomous loop closeout to main), SADR-0015 (Contract Approval before\n   dispatch), remediation goal task R-02, the live PR #179 deadlock.\n \n ## Context\n@@ -90,11 +90,11 @@\n files, documentation files and named source modules are nameable; runtime-\n allocated ledger, runner and report ids are not \u2014 and those live in `.shiki`,\n which locks no longer govern. Existing frozen contracts are brought into line by\n-a one-time Spec Amendment per plan, which must land before ADR 0015's contract\n+a one-time Spec Amendment per plan, which must land before SADR-0015's contract\n immutability makes `locks` unwritable from the implementation PR.\n \n **Lock state is resolved from the durable side.** MergeGate resolves the lock\n-registry, and ADR 0015's governance fields, from the base `.shiki` snapshot when\n+registry, and SADR-0015's governance fields, from the base `.shiki` snapshot when\n present, falling back to head only when absent. The `MergeGate metadata check`\n job is given `--base-shiki`, which it does not receive today. Locks are not\n derived from the live open-pull-request set: a pull request body is untrusted\n@@ -108,7 +108,7 @@\n Explicitly out of scope: moving MergeGate's contract resolution off the PR\n checkout wholesale; making the mirror a projection of GitHub state; introducing\n new lock kinds; moving ledger evidence out of the repository; and unwinding\n-ADR 0012's closeout pull request. Registration of multi-task goals is ADR 0015's\n+SADR-0012's closeout pull request. Registration of multi-task goals is SADR-0015's\n Contract PR and is not re-decided here.\n \n ## Consequences\n@@ -127,7 +127,7 @@\n \n Risks accepted. First, goal-scoped identity is only as strong as `goal_id`,\n which is resolved from the PR body and the head task file; the mitigation is to\n-add `goal_id` to ADR 0015's frozen governance field set, and until that lands a\n+add `goal_id` to SADR-0015's frozen governance field set, and until that lands a\n task file can name a foreign goal. Second, base-when-present resolution is\n unavailable on fork pull requests, where the head is contributor-controlled;\n fork pull requests already cannot complete a Shiki merge, and this decision does\n@@ -139,7 +139,7 @@\n path, not by implementation pull requests.\n \n New obligations: one Spec Amendment per frozen plan to rewrite `locks`, ordered\n-before ADR 0015 T1; extension of the adversarial suites in\n+before SADR-0015 T1; extension of the adversarial suites in\n `scripts/test_shiki_adversarial_state.sh` and\n `scripts/test_shiki_governance_evidence.sh` to cover the eleven newly-scoped\n directories; and updating `scripts/test_shiki_loop_lock_guard.sh`,\n@@ -151,32 +151,32 @@\n whether the loop's transitive sync of ledger `evidence` references can be\n deleted once the Contract PR carries its own source plan.\n \n-## Relationship to ADR 0004\n+## Relationship to SADR-0004\n \n-This ADR **clarifies something ADR 0004 left unspecified**. ADR 0004 decides that\n+This SADR **clarifies something SADR-0004 left unspecified**. SADR-0004 decides that\n GitHub is the operational source of truth and that each target repository keeps a\n `.shiki/` mirror. It says nothing about pull requests writing the mirror, does\n not name MergeGate, and does not make the mirror an input to a gate. Nothing in\n-ADR 0004's Decision or Consequences is amended, superseded or weakened: the\n+SADR-0004's Decision or Consequences is amended, superseded or weakened: the\n mirror still exists, still records the same artifacts, and pull request\n-boundaries remain the coordination surface. What this ADR settles is the question\n+boundaries remain the coordination surface. What this SADR settles is the question\n 0004 never reached \u2014 by what authority a pull request may write a mirror record \u2014\n and it answers it the way `.shiki/manifest.json` already declares\n (`pr_mutation: current-task-or-current-goal-only`).\n \n-It **amends ADR 0012**, whose Decision item 5 states that \"Loop-executed tasks\n+It **amends SADR-0012**, whose Decision item 5 states that \"Loop-executed tasks\n declare the `path:.shiki/**` lock, which covers every `.shiki` file the closeout\n stages ... so `files_outside_locks` is satisfied without per-file lock\n bookkeeping.\" That clause is replaced: the closeout's mirror writes are\n-authorized by record identity, not by a declared lock. ADR 0012's substantive\n+authorized by record identity, not by a declared lock. SADR-0012's substantive\n decision \u2014 that completion reaches `main` through a closeout pull request rather\n than local attestation \u2014 is preserved unchanged.\n \n-It is **complementary to ADR 0015**, not a substitute. ADR 0015 owns contract\n+It is **complementary to SADR-0015**, not a substitute. SADR-0015 owns contract\n immutability, the pre-dispatch gate, the Contract PR and the Scope Envelope. This\n-ADR owns what a lock means and who may write the mirror. The one ordering\n+SADR owns what a lock means and who may write the mirror. The one ordering\n constraint between them is that the lock-narrowing Spec Amendment must precede\n-ADR 0015 T1's freezing of the `locks` field.\n+SADR-0015 T1's freezing of the `locks` field.\n \n ## Alternatives Considered\n \n@@ -186,7 +186,7 @@\n the only writer that can register them may run. Migrating the Guardian signal to\n that store makes every registration PR fail closed to `needs_guardian` at every\n risk level, because the task file is by definition absent from the store, which\n-removes the low/medium autonomous path ADR 0008/0009 exists to provide. And it\n+removes the low/medium autonomous path SADR-0008/0009 exists to provide. And it\n leaves schemas, manifest, config, guardian policy, templates, migrations, plans,\n worktrees, runner records, runs and handoffs with no writer at all in a\n repository whose product is `.shiki`. Measured benefit on the frozen backlog:\n@@ -199,14 +199,14 @@\n namespace is not in fact ownership-partitioned \u2014 56 ledger entries carry no\n `task_id`, and one goal DAG is writable by 35 registered tasks. The correct\n expression of its insight is that locks do not govern the mirror at all, which is\n-what this ADR decides.\n+what this SADR decides.\n \n **Make the mirror a projection written only by a registration writer and a\n post-merge projection job.** Rejected. It requires a bypass actor with write\n access to the protected default branch; it makes its own master invariant\n readable from a manifest that pull requests may edit; it deletes both reconcile\n modes and leaves a corrupted mirror with no in-band repair path, contradicting\n-ADR 0004's consequence that mirror conflicts are repaired against GitHub state;\n+SADR-0004's consequence that mirror conflicts are repaired against GitHub state;\n and its content-derived ids are rejected by `validate_shiki`'s currently-green id\n pattern.\n \ndiff --git a/docs/adr/0017-bookkeeping-closeouts-do-not-inherit-risk.md b/docs/adr/SADR-0017-bookkeeping-closeouts-do-not-inherit-risk.md\nindex 554e3a0d526a72ca067108c5464cc68c3edcf174..b9676857a4713850e063af7c39c8e0b01ecb4c66\n--- a/docs/adr/0017-bookkeeping-closeouts-do-not-inherit-risk.md\n+++ b/docs/adr/SADR-0017-bookkeeping-closeouts-do-not-inherit-risk.md\n@@ -1,4 +1,4 @@\n-# ADR 0017: A Bookkeeping Closeout Does Not Inherit Its Task's Risk\n+# SADR-0017: A Bookkeeping Closeout Does Not Inherit Its Task's Risk\n \n ## Status\n \n@@ -6,7 +6,7 @@\n \n ## Context\n \n-ADR 0012 made the goal loop push a task's completion to the default branch through a\n+SADR-0012 made the goal loop push a task's completion to the default branch through a\n closeout pull request: after the implementation PR merges, the loop opens a second PR\n carrying `task.status = done`, `lock.state = released`, and \u2014 when the task completes\n its goal \u2014 `goal.status = complete` plus one scorecard report. Nothing else. The\n@@ -49,14 +49,14 @@\n 4. The task file's governance fields (`scope`, `non_goals`, `required_skills`,\n    `risk_level`, `locks`, `acceptance_checks`, `test_command`) are byte-identical to\n    the base snapshot. Only `status`, `expected_pr`, `closeout_pr`, `ledger_evidence`\n-   and `expected_branch` may differ \u2014 the ADR 0015 contract-immutability rule,\n+   and `expected_branch` may differ \u2014 the SADR-0015 contract-immutability rule,\n    unchanged.\n \n    `expected_branch` is in that set because a closeout is *by construction* on a\n    different branch from the implementation it closes, and the loop rewrites the\n    field accordingly. It is safe to let it differ because MergeGate independently\n    requires `expected_branch` to equal the PR's actual head ref, so a PR cannot set\n-   it to anything other than the branch it is really on. The first draft of this ADR\n+   it to anything other than the branch it is really on. The first draft of this SADR\n    omitted it, which made the exemption unreachable: every real closeout failed\n    condition 4 on a field the loop always changes. See PR #209.\n 5. The state transitions are exactly the terminal set: task `review` \u2192 `done`, lock\n@@ -113,12 +113,12 @@\n classification must be derived, not asserted.\n \n **Fold the closeout into the implementation PR** so no second PR exists. Rejected under\n-ADR 0012, which recorded why: a task file that is already `done` on the implementation\n+SADR-0012, which recorded why: a task file that is already `done` on the implementation\n branch short-circuits the loop's review \u2192 CCA \u2192 merge \u2192 done ordering into a local\n goal-completion before any merge happens.\n \n **Have the loop auto-merge closeouts with elevated credentials.** Rejected outright: that\n-is a runtime authoring its own approval, which ADR 0010 and the constitution's\n+is a runtime authoring its own approval, which SADR-0010 and the constitution's\n approval-record prohibition forbid. It would also be indistinguishable, in the evidence,\n from the forgery those rules exist to prevent.\n \ndiff --git a/docs/adr/0018-carried-guardian-approval-across-a-proven-base-sync.md b/docs/adr/SADR-0018-carried-guardian-approval-across-a-proven-base-sync.md\nindex 22358562da390ed8b45cd600da08a43d378b4680..e6e2907cefa776460af73ddf7d39db605f3c39b0\n--- a/docs/adr/0018-carried-guardian-approval-across-a-proven-base-sync.md\n+++ b/docs/adr/SADR-0018-carried-guardian-approval-across-a-proven-base-sync.md\n@@ -1,4 +1,4 @@\n-# ADR 0018: Carried Guardian Approval Across A Proven Base Sync\n+# SADR-0018: Carried Guardian Approval Across A Proven Base Sync\n \n ## Status\n \n@@ -34,7 +34,7 @@\n Guardian evaluator that consumes it (`shiki_guardian.evaluate_guardian_approval`,\n the `guardian_comment_carried` source) were built and tested in isolation. Neither\n does anything until a caller passes the flags that turn the carry on, so the\n-feature was invisible in CI. This ADR records the decision the two of them\n+feature was invisible in CI. This SADR records the decision the two of them\n implement, and this task wires it into the four CI invocations that judge Guardian\n approval.\n \ndiff --git a/docs/adr/README.md b/docs/adr/README.md\nindex c70e057a4520c36e042ab68c53cd4a21ef709b71..5da43856836e2ce6a150f8127766e2f88355ab4b\n--- a/docs/adr/README.md\n+++ b/docs/adr/README.md\n@@ -1,40 +1,41 @@\n-# Architecture Decision Records\n+# Decision Record Namespaces\n \n-This directory holds Shiki's Architecture Decision Records (ADRs): durable\n-records of hard-to-reverse platform decisions. ADRs are part of the Shiki source\n-of truth alongside GitHub state, the `.shiki/` mirror, and `CONTEXT.md`.\n+This directory holds two independent decision namespaces:\n \n-## How ADRs Work\n+- Shiki Architecture Decision Records (SADRs), named\n+  `SADR-NNNN-<short-slug>.md`, record hard-to-reverse Shiki platform decisions.\n+- Target Architecture Decision Records (ADRs), named\n+  `NNNN-<short-slug>.md`, belong to the target product.\n \n-- New ADRs are added as `docs/adr/NNNN-<short-slug>.md`, where `NNNN` is the next\n-  zero-padded sequence number.\n-- The **canonical list of ADRs is the set of files matching `docs/adr/*.md`** in\n-  this directory. Browse `docs/adr/*.md` for the authoritative, current set.\n-- Use [`template.md`](template.md) as the starting point for a new ADR.\n-- Record an ADR when a decision is hard to reverse, surprising without context,\n-  and a real tradeoff. Do not write an ADR for routine or easily reversible\n-  choices.\n+The same four-digit number may appear once in each namespace. A platform\n+`SADR-NNNN` citation resolves only to `SADR-NNNN-*.md`; a target `ADR NNNN`\n+citation resolves only to `NNNN-*.md`.\n \n-Using the `docs/adr/*.md` glob as the source of truth means other PRs can add an\n-ADR without editing this index file, avoiding merge contention on a\n-hand-maintained enumeration.\n+## How Decision Records Work\n \n-## Current ADRs (snapshot)\n-\n-The list below is a convenience snapshot and may lag behind the directory. When\n-in doubt, list `docs/adr/*.md`.\n+- New Shiki platform decisions use the next zero-padded SADR number.\n+- New target product decisions use the target's own ADR sequence.\n+- The canonical list is the files in `docs/adr/`; no hand-maintained index is\n+  authoritative.\n+- Use [`template.md`](template.md) as the starting point and choose the filename\n+  namespace that owns the decision.\n+- Record a decision when it is hard to reverse, surprising without context, and\n+  a real tradeoff. Do not write a decision record for routine or easily\n+  reversible choices.\n \n-- [0001 \u2014 Keep Shiki As The Platform Name](0001-keep-shiki-as-platform-name.md)\n-- [0002 \u2014 Use AGENTS As The Shared Agent Constitution](0002-use-agents-as-shared-constitution.md)\n-- [0003 \u2014 Require Skill Gate For MergeGate Readiness](0003-require-skill-gate-for-mergegate-readiness.md)\n-- [0004 \u2014 Use GitHub First With A Repository-Local Shiki Mirror](0004-use-github-first-with-shiki-mirror.md)\n-- [0005 \u2014 Use Codex As Front And Claude Code Action As The GitHub Runtime](0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md)\n-- [0006 \u2014 Separate Codex Implementation, CCA Completion, And MergeGate Authorization](0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md)\n+Using the directory as the source of truth lets independent changes add records\n+without contending on an enumerated index.\n \n-## Adding A New ADR\n+## Adding A Shiki SADR\n \n-1. Copy `template.md` to `docs/adr/NNNN-<short-slug>.md`.\n+1. Copy `template.md` to `docs/adr/SADR-NNNN-<short-slug>.md`.\n 2. Fill in the title, status, context, decision, and consequences.\n-3. Set the status to `Proposed`, then `Accepted` once the decision is approved.\n-4. Open a PR following `CONTRIBUTING.md`. You do not need to edit this index for\n-   the ADR to be discoverable via `docs/adr/*.md`.\n+3. Set the status to `Proposed`, then `Accepted` once the proper authority has\n+   approved the decision.\n+4. Open a PR following `CONTRIBUTING.md`.\n+\n+## Adding A Target ADR\n+\n+1. Copy `template.md` to `docs/adr/NNNN-<short-slug>.md` using the target's own\n+   sequence.\n+2. Cite it as `ADR NNNN`; do not prefix its filename with `SADR-`.\ndiff --git a/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md b/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\nnew file mode 100644\nindex 0000000000000000000000000000000000000000..7455ce1f3bed02ddddb851ac21a1d1a648c9aeba\n--- /dev/null\n+++ b/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\n@@ -0,0 +1,55 @@\n+# SADR-0019: Separate Shiki Platform SADRs From Target ADRs\n+\n+## Status\n+\n+Accepted\n+\n+## Context\n+\n+Shiki installs its platform decision records into `docs/adr/` in every target\n+repository. Shiki and its targets previously used the same numeric\n+`NNNN-<slug>.md` namespace. A target that already owned ADRs 0013 through 0018\n+therefore acquired two records for each number during installation, making an\n+`ADR NNNN` citation ambiguous. Renumbering target decisions would rewrite target\n+history, while renumbering Shiki decisions per target would destroy their stable\n+platform identity.\n+\n+Upgrades also need to remove the former Shiki-owned numeric paths without ever\n+mistaking a target-owned ADR for platform content. The install stamp already\n+records exact shipped paths and content digests, so it can prove ownership only\n+when both still match.\n+\n+## Decision\n+\n+Shiki platform decisions use the `SADR` namespace:\n+\n+- records are named `docs/adr/SADR-NNNN-<slug>.md`;\n+- live platform citations use `SADR-NNNN`;\n+- target product decisions retain `docs/adr/NNNN-<slug>.md` and `ADR NNNN`;\n+- the reference validator resolves the two namespaces independently, so the same\n+  four-digit number may exist once in each namespace.\n+\n+Before a forced upgrade writes anything, the installer inventories the exact\n+former numeric Shiki ADR paths. It may delete a legacy path only when the\n+existing install stamp contains that exact path and its recorded SHA-256 digest\n+matches the file currently on disk. All ownership blockers are reported\n+together and abort the upgrade before its first write. A non-forced install\n+never performs this cleanup.\n+\n+Historical `.shiki/` evidence remains unchanged because it records the namespace\n+that existed when that evidence was produced.\n+\n+## Consequences\n+\n+- Targets may use any ADR number without colliding with Shiki's platform SADRs.\n+- Fresh installs preserve target ADR bytes and install a stable SADR set.\n+- Forced upgrades fail closed when legacy ownership cannot be proven, and a\n+  successful upgrade refreshes the stamp without removed legacy paths.\n+- Current governance, packaging, workflow, prompt, script, and skill references\n+  must move with the platform records.\n+\n+## Alternatives Considered\n+\n+- Collision-only refusal leaves affected targets permanently unupgradable.\n+- Renumbering target ADRs mutates target-owned history.\n+- Per-target Shiki renumbering makes platform citations non-portable.\ndiff --git a/docs/adr/template.md b/docs/adr/template.md\nindex a92a97daddd0b00ab9dd626cc8d86942da2eafed..17dd79175b5fee2105935557f691b40cf9492942\n--- a/docs/adr/template.md\n+++ b/docs/adr/template.md\n@@ -1,14 +1,14 @@\n-# NNNN: <Short Decision Title>\n+# <SADR-NNNN or NNNN>: <Short Decision Title>\n \n ## Status\n \n-Proposed | Accepted | Superseded by ADR-NNNN\n+Proposed | Accepted | Superseded by SADR-NNNN or target ADR NNNN\n \n ## Context\n \n What is the situation, constraint, or force that requires a decision? Reference\n-relevant domain terms (`CONTEXT.md`), prior ADRs, and code reality. State the\n-problem, not the solution.\n+relevant domain terms (`CONTEXT.md`), prior Shiki SADRs or target ADRs, and code\n+reality. State the problem, not the solution.\n \n ## Decision\n \ndiff --git a/docs/agents/autonomous-loop-e2e-acceptance.md b/docs/agents/autonomous-loop-e2e-acceptance.md\nindex bbf84c6081987f159ac6ad9ecf3c82b7aa72e7e8..5c13c1d02a05a90b0dc46cbef7c95a9846d0c237\n--- a/docs/agents/autonomous-loop-e2e-acceptance.md\n+++ b/docs/agents/autonomous-loop-e2e-acceptance.md\n@@ -40,8 +40,8 @@\n 3. `gh auth status` is logged in with merge permission on the repo.\n 4. Branch protection + required checks are configured exactly as the goal-loop\n    required-check set expects (see `docs/agents/branch-protection-smoke.md`).\n-5. The repo `.shiki/policy` permits autonomous low/medium-risk merges (ADR\n-   0008/0009); the MergeGate policy check is the recorded-authority gate.\n+5. The repo `.shiki/policy` permits autonomous low/medium-risk merges\n+   (SADR-0008/0009); the MergeGate policy check is the recorded-authority gate.\n \n The operator's only actions are: register the task and start the loop. **No\n manual pre-clearing** means after `shiki loop run` starts, the operator does not\ndiff --git a/docs/agents/bootstrap-command.md b/docs/agents/bootstrap-command.md\nindex 05f7f203a52cf412b9f97084a6e131ed0f785afd..f7aa0b734dfebee4a3b139335b66ee3415f48713\n--- a/docs/agents/bootstrap-command.md\n+++ b/docs/agents/bootstrap-command.md\n@@ -189,7 +189,9 @@\n ```\n \n Use this only for tests, fixtures, or explicit local-only template inspection.\n-Use `--force` only when you intentionally want to overwrite existing target files.\n+Use `--force` only when you intentionally want to refresh the target template.\n+Legacy numeric Shiki decision cleanup is stamp-and-digest authorized before any\n+write; target ADR paths are not cleanup candidates.\n \n ## Slash Command\n \ndiff --git a/docs/agents/checklists.md b/docs/agents/checklists.md\nindex 4a7f03ade346f53a1c1d82981f3fd744f9ab4547..0c5ec6c53dc7197b7b7a7d18e667b325babd6c45\n--- a/docs/agents/checklists.md\n+++ b/docs/agents/checklists.md\n@@ -32,18 +32,18 @@\n | ID | Item | Blocking |\n | --- | --- | --- |\n | GD-01 | Plan was challenged against `CONTEXT.md`. | blocking for non-trivial Goals |\n-| GD-02 | Relevant ADRs were read or absence was recorded. | blocking for non-trivial Goals |\n+| GD-02 | Relevant SADRs and target ADRs were read or absence was recorded. | blocking for non-trivial Goals |\n | GD-03 | Ambiguous terms were resolved or converted to HITL blockers. | blocking |\n | GD-04 | Edge scenarios were considered. | advisory |\n | GD-05 | Code reality was checked when code could answer. | blocking when applicable |\n | GD-06 | New domain terms were added to `CONTEXT.md` when needed. | blocking when applicable |\n-| GD-07 | ADR was created or explicitly skipped using the ADR criteria. | blocking when applicable |\n+| GD-07 | A SADR or target ADR was created or explicitly skipped using the decision-record criteria. | blocking when applicable |\n \n ## CI \u2014 Context & Impact Checklist\n \n | ID | Item | Blocking |\n | --- | --- | --- |\n-| CI-01 | Relevant docs and ADRs identified. | blocking |\n+| CI-01 | Relevant docs, SADRs, and target ADRs identified. | blocking |\n | CI-02 | Relevant modules, interfaces, seams, callers, and tests identified. | blocking |\n | CI-03 | Dependencies and blocked-by relationships identified. | blocking |\n | CI-04 | Candidate locks identified. | blocking |\n@@ -62,7 +62,7 @@\n | PRD-04 | Implementation decisions are recorded without volatile code detail. | blocking |\n | PRD-05 | Testing decisions are recorded. | blocking |\n | PRD-06 | Out-of-scope items are explicit. | blocking |\n-| PRD-07 | Relevant glossary/ADR links are present when applicable. | blocking |\n+| PRD-07 | Relevant glossary/SADR/target-ADR links are present when applicable. | blocking |\n \n ## SF \u2014 Spec Freeze Checklist\n \n@@ -98,7 +98,7 @@\n | H-02 | Branch/worktree target is present. | blocking |\n | H-03 | Scope and non-goals are present. | blocking |\n | H-04 | Dependencies and locks are present. | blocking |\n-| H-05 | Relevant docs, ADRs, modules, and tests are referenced. | blocking |\n+| H-05 | Relevant docs, SADRs, target ADRs, modules, and tests are referenced. | blocking |\n | H-06 | Required skills are stated. | blocking |\n | H-07 | TDD expectations are stated. | blocking when behavior work |\n | H-08 | Acceptance criteria are included. | blocking |\ndiff --git a/docs/agents/codex-handoff.md b/docs/agents/codex-handoff.md\nindex ede468f47cb277f69a857e8c45df703e4ed0151d..6f97bdffb67e44d7edea2c71e01ea28933c4eb6c\n--- a/docs/agents/codex-handoff.md\n+++ b/docs/agents/codex-handoff.md\n@@ -46,7 +46,7 @@\n \n ## Context\n - Domain terms:\n-- Relevant ADRs:\n+- Relevant SADRs and target ADRs:\n - Relevant docs:\n - Relevant modules/interfaces/seams:\n - Similar tests/prior art:\ndiff --git a/docs/agents/completion-check-agent.md b/docs/agents/completion-check-agent.md\nindex 13e4c035db35025f4e3cdcc5adfb24314ac4d521..61bfa1218cf36ca605250c91919e620f33238aa0\n--- a/docs/agents/completion-check-agent.md\n+++ b/docs/agents/completion-check-agent.md\n@@ -32,7 +32,7 @@\n - `AGENTS.md`\n - `CLAUDE.md`\n - `CONTEXT.md`\n-- relevant ADRs\n+- relevant SADRs and target ADRs\n - parent Goal issue\n - PRD issue or document\n - task issue\ndiff --git a/docs/agents/decision-control.md b/docs/agents/decision-control.md\nindex 7d8ee5ab03a5ccbcbd04056ae61de03785a2a9c9..8c94ecda00fffaa0e92c325ab83aa9776bd1dac1\n--- a/docs/agents/decision-control.md\n+++ b/docs/agents/decision-control.md\n@@ -11,7 +11,7 @@\n | Decision | Owner | When | Required inputs | Output | Enforcement surface |\n | --- | --- | --- | --- | --- | --- |\n | Is the Goal clear enough to plan? | Planner + Guardian when needed | Goal intake | User request, existing docs, risk signals | Goal readiness verdict | Goal checklist, issue template, ledger |\n-| Must `grill-with-docs` run? | Planner / Skill Gate | Before PRD or issue split | Ambiguity, domain terms, ADR conflicts, unclear boundaries | Skill invocation or explicit skip rationale | Skill Gate checklist |\n+| Must `grill-with-docs` run? | Planner / Skill Gate | Before PRD or issue split | Ambiguity, domain terms, SADR/target-ADR conflicts, unclear boundaries | Skill invocation or explicit skip rationale | Skill Gate checklist |\n | Is a PRD required? | Planner | After Goal context stabilizes | Goal, `grill-with-docs` output, Context & Impact output | PRD required / not required | PRD checklist, CCA evidence check |\n | Can the spec be frozen? | Operator only | End of Requirements Definition, before issue split | PRD/requirements summary, scope inventory (SF-02), open-question list | `spec_freeze` block in the plan + ledger evidence | `require_grilled_plan` (plan ingest/run), SF checklist |\n | Can a frozen spec change? | Operator only (Spec Amendment) | When implementation contradicts the frozen spec | Contested decisions, scoped re-grill result | recorded amendment + re-stamped freeze | SF checklist, ledger evidence |\ndiff --git a/docs/agents/domain.md b/docs/agents/domain.md\nindex 258da8ea81e87ac0a6b0b5b5e5c2fde980eaabb7..d57b0cf17ff3e1fa9e6e5885372bf083d8295421\n--- a/docs/agents/domain.md\n+++ b/docs/agents/domain.md\n@@ -5,7 +5,9 @@\n ## Layout\n \n - `CONTEXT.md` defines Shiki language. Keep it implementation-free.\n-- `docs/adr/` records hard-to-reverse decisions.\n+- `docs/adr/SADR-NNNN-*.md` records Shiki platform decisions.\n+- `docs/adr/NNNN-*.md` records target product decisions without sharing the\n+  SADR namespace.\n - `AGENTS.md` is the runtime-neutral agent constitution.\n - `CLAUDE.md` is the Claude Code wrapper around `AGENTS.md`.\n - `.shiki/` is the Target Repository mirror for Goals, PRDs, plans, Task DAGs, contracts, locks, worktree records, ledger entries, CCA verdicts, repair packets, reports, and handoffs.\n@@ -15,6 +17,6 @@\n \n - Use Shiki terms exactly when they exist.\n - Add glossary terms to `CONTEXT.md` only when the term is part of the domain, not a generic programming concept.\n-- Create ADRs only for hard-to-reverse, surprising, tradeoff-based decisions.\n+- Create SADRs or target ADRs only for hard-to-reverse, surprising, tradeoff-based decisions.\n - Keep product-specific language in the Target Repository, not in Shiki Template docs.\n-- If an ADR conflicts with a proposed implementation, surface the conflict before editing.\n+- If a SADR or target ADR conflicts with a proposed implementation, surface the conflict before editing.\ndiff --git a/docs/agents/execution-control.md b/docs/agents/execution-control.md\nindex ad8ee3cdb36be56fedc1f46408ca92b387d74047..bc63c3a73661d12b567016ba299f1a6b66bf9088\n--- a/docs/agents/execution-control.md\n+++ b/docs/agents/execution-control.md\n@@ -63,7 +63,7 @@\n | Branch name | Orchestrator | Orchestrator | Before implementer dispatch | branch policy | branch ref in task/ledger |\n | Lock reservation | Context & Impact | MergeGate | Before dispatch and before merge | `.shiki/locks/`, labels, PR metadata | lock id, owner, scope |\n | CI profile | Planner | MergeGate | During issue triage, rechecked at PR | labels, `.shiki/policy.yaml`, workflow matrix | required check list |\n-| Required status checks | Repo maintainer / Guardian | GitHub branch protection / rulesets | Repository setup, updated via ADR | GitHub settings | branch/ruleset config, ADR |\n+| Required status checks | Repo maintainer / Guardian | GitHub branch protection / rulesets | Repository setup, updated via target ADR | GitHub settings | branch/ruleset config, target ADR |\n | CCA judgment | CCA | GitHub required check / MergeGate | Every non-draft PR update | GitHub Action check | structured CCA verdict |\n | Repair assignment | CCA | MergeGate | After failed CCA/CI/review | repair packet, issue/PR comment | repair id and attempt count |\n | Merge readiness | MergeGate | GitHub branch protection / Guardian if needed | After CCA complete | required checks, review rules, rulesets | mergegate check output |\ndiff --git a/docs/agents/guardian-policy.md b/docs/agents/guardian-policy.md\nindex 4147c7adf05a47adb4fd24ada1a1c2e1f2ba6d2d..a20d9cc6c00a569bb057b9ca8d6b51c7fc4a9847\n--- a/docs/agents/guardian-policy.md\n+++ b/docs/agents/guardian-policy.md\n@@ -98,8 +98,8 @@\n `origin/<default_branch>` and reuses each job's existing base fetch; a shallow\n (`--depth`) or second base fetch would make the proof unresolvable and silently\n disable the carry, so neither is permitted. CCA reads `guardian_comment_carried` as\n-a recorded authority in the deterministic signal and does not re-derive it. See ADR\n-0018 for the full decision, the rejected diff/patch-equality and path-disjointness\n+a recorded authority in the deterministic signal and does not re-derive it. See\n+SADR-0018 for the full decision, the rejected diff/patch-equality and path-disjointness\n alternatives, and the accepted residual.\n \n ## External AI Guardian Review (SADR-0010)\ndiff --git a/docs/agents/implementation-policy.md b/docs/agents/implementation-policy.md\nindex c754144a9fb5bf4a38aa82a60e1670c59e626b44..85c37281bcad196c6b7b02343faf00b03cd9c57a\n--- a/docs/agents/implementation-policy.md\n+++ b/docs/agents/implementation-policy.md\n@@ -31,10 +31,13 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n ## Non-Negotiables\n \n 1. **Planning precedes implementation.** Non-trivial Goals start with `grill-with-docs` and Context & Impact.\n-2. **Domain language is controlled.** Terms must be checked against `CONTEXT.md`; hard-to-reverse tradeoffs must be checked against ADRs.\n+2. **Domain language is controlled.** Terms must be checked against `CONTEXT.md`; hard-to-reverse tradeoffs must be checked against Shiki SADRs and target ADRs.\n 3. **PRD precedes issue decomposition.** `to-prd` is used once the context is settled enough to become durable intent.\n 4. **Issues are vertical slices.** `to-issues` must produce independently grabbable tracer-bullet tasks, not horizontal layer tickets.\n 5. **Triage controls dispatch.** Only `ready-for-agent` / AFK tasks may be assigned to Codex without human input.\n@@ -95,19 +98,19 @@\n Process:\n \n - Challenge terms against `CONTEXT.md`.\n-- Check ADRs before reopening decisions.\n+- Check SADRs and target ADRs before reopening decisions.\n - Inspect code when code can answer a question.\n - Ask one question at a time when the operator is available.\n - Provide a recommended answer with each question.\n - Record resolved terms in `CONTEXT.md`.\n-- Create ADRs only for hard-to-reverse, surprising, tradeoff-based decisions.\n+- Create SADRs or target ADRs only for hard-to-reverse, surprising, tradeoff-based decisions.\n - Convert unresolved load-bearing questions into HITL blockers.\n \n Outputs:\n \n - Resolved design-tree decisions.\n - Updated glossary terms when needed.\n-- ADRs when justified.\n+- SADRs or target ADRs when justified.\n - HITL blockers when decisions remain unresolved.\n \n ## Phase 3 \u2014 Context & Impact\n@@ -117,7 +120,7 @@\n \n Outputs must include:\n \n-- Relevant documents and ADRs.\n+- Relevant documents, SADRs, and target ADRs.\n - Relevant modules, interfaces, seams, callers, and tests.\n - Candidate locks.\n - Dependencies.\n@@ -140,7 +143,7 @@\n - Testing decisions.\n - Out of scope.\n - Further notes.\n-- Links to relevant Goal, domain terms, and ADRs.\n+- Links to relevant Goal, domain terms, SADRs, and target ADRs.\n \n Phase 4 ends with Spec Freeze: the operator explicitly approves the PRD, the\n plan records a `spec_freeze` block (status, approved_by, source), and the SF\n@@ -240,7 +243,7 @@\n - Dependencies and locks.\n - Risk and Guardian requirements.\n - Ledger completeness.\n-- Documentation and ADR updates.\n+- Documentation and SADR/target-ADR updates.\n - Scope drift.\n \n CCA emits one of:\n@@ -301,7 +304,7 @@\n | Evidence failure | Behavior might be done, but proof is missing. | Add checks, PR evidence, ledger entry, or artifacts. |\n | Scope drift | PR changes unrelated behavior. | Revert unrelated changes or split into new Goal. |\n | Dependency block | Upstream task or lock not complete. | Block until dependency resolves. |\n-| Architecture block | Decision needs ADR or deep-module discussion. | `grill-with-docs` or `improve-codebase-architecture`. |\n+| Architecture block | Decision needs a SADR, target ADR, or deep-module discussion. | `grill-with-docs` or `improve-codebase-architecture`. |\n | Risk block | Security/production/policy change needs human approval. | Guardian decision. |\n \n ## Phase 5.5 \u2014 Execution Decision Control\ndiff --git a/docs/agents/runtime-auth-model.md b/docs/agents/runtime-auth-model.md\nindex 6a2e1a47e81ea89b5c68a210ec07772a9374babc..3c54748d24233dbd0ba8473950282547ef4ce935\n--- a/docs/agents/runtime-auth-model.md\n+++ b/docs/agents/runtime-auth-model.md\n@@ -148,7 +148,7 @@\n \n API-key based runners may be added by a target repository as an explicit extension. They are not the default Shiki template path.\n \n-If a repo opts into API-key mode, record the exception in an ADR and update the repo's `AGENTS.md`, `.shiki/config.yaml`, and workflow permissions.\n+If a repo opts into API-key mode, record the target-specific exception in a target ADR and update the repo's `AGENTS.md`, `.shiki/config.yaml`, and workflow permissions.\n \n ## GitHub Actions Runtime Compatibility\n \ndiff --git a/docs/agents/skill-gate.md b/docs/agents/skill-gate.md\nindex c50398b0d7c60a8598f2a5c88a10015730dced13..3acffe6444617bb1d54b77d4b4708158ec735bc6\n--- a/docs/agents/skill-gate.md\n+++ b/docs/agents/skill-gate.md\n@@ -9,7 +9,7 @@\n | Skill | Trigger | Expected output |\n | --- | --- | --- |\n | `setup-matt-pocock-skills` | First configuring a repo for these skills, or when issue tracker, triage labels, or domain docs are missing. | `docs/agents/` configuration and an `Agent skills` block in `AGENTS.md` or `CLAUDE.md`. |\n-| `grill-with-docs` | Ambiguous plan, unclear terminology, boundary decision, tradeoff, or ADR-worthy design choice. | Clarified decision tree, recommended answers, updates to `CONTEXT.md` or ADRs when decisions crystallize. |\n+| `grill-with-docs` | Ambiguous plan, unclear terminology, boundary decision, tradeoff, or decision-record-worthy design choice. | Clarified decision tree, recommended answers, updates to `CONTEXT.md`, SADRs, or target ADRs when decisions crystallize. |\n | `zoom-out` | Agent lacks a higher-level architectural map. | Map of relevant modules, callers, seams, and domain concepts. |\n | `to-prd` | Settled context should become a PRD. | PRD published to the issue tracker with problem, solution, user stories, decisions, testing, and out-of-scope notes. |\n | `to-issues` | Goal, PRD, or plan must become implementation tickets. | Vertical-slice issues with acceptance criteria, dependencies, and AFK/HITL classification. |\n@@ -17,7 +17,7 @@\n | `tdd` | Feature work or bug fix has observable behavior that can be specified with tests. | Red-green-refactor loop through public interfaces, one vertical slice at a time. |\n | `code-review` | The TDD loop is green and the implementer is about to create or update a PR. Default for every implementation task. | Pre-PR self-review of the task diff with fixes applied on the task branch; findings and resolutions recorded as a ledger entry naming the skill and a `## Pre-PR code review` PR body section. |\n | `diagnose` | Hard bug, failing check, regression, flaky behavior, or performance problem. | Reproduction loop, ranked hypotheses, instrumentation, fix, regression test, post-mortem. |\n-| `improve-codebase-architecture` | Structural friction, testability, AI-navigability, deep-module opportunity, or refactor request. | Architecture candidates and deepening recommendations informed by `CONTEXT.md` and ADRs. |\n+| `improve-codebase-architecture` | Structural friction, testability, AI-navigability, deep-module opportunity, or refactor request. | Architecture candidates and deepening recommendations informed by `CONTEXT.md`, SADRs, and target ADRs. |\n | `prototype` | A throwaway logic or UI prototype is needed to answer a design question. | Clearly marked disposable prototype and captured decision. |\n \n ## MergeGate Interaction\ndiff --git a/docs/operations.md b/docs/operations.md\nindex cf1f452d4c589e38ccd58553bda0ad8684b51eb7..ace48a90ffbec9e5c0a7113bef16a4858bc44b3f\n--- a/docs/operations.md\n+++ b/docs/operations.md\n@@ -89,6 +89,16 @@\n      target history, brought forward by `shiki migrate`, not a template.\n    - **Everything else** (CLI scripts, workflows, schemas, docs) is overwritten.\n \n+   A forced upgrade also migrates the former numeric Shiki decision paths to\n+   `SADR-NNNN-*`. Before the first write, the installer inventories the exact\n+   legacy Shiki paths and authorizes each deletion only when the existing\n+   `.shiki/install-stamp.json` names that path and its SHA-256 digest still\n+   matches the file. An absent, unreadable, malformed, incomplete, or\n+   mismatching stamp reports every blocked path and stops with no filesystem\n+   change. Target-owned `NNNN-*` ADRs are not cleanup candidates. A non-force\n+   install deletes nothing and retains any still-matching legacy stamp entries\n+   so a later forced upgrade can prove ownership.\n+\n    If the target has pending migrations, `--force` refuses and writes nothing \u2014\n    apply migrations first (step 3), then re-run. The run ends with a single\n    summary of every `.new` written, so a half-upgrade is visible.\ndiff --git a/docs/releasing.md b/docs/releasing.md\nindex 10d73828e7dbd64e9d9f6b83bc84d010537ce62a..aafeee8c4d441e1e427efee2ce4f81a647f020e1\n--- a/docs/releasing.md\n+++ b/docs/releasing.md\n@@ -67,7 +67,7 @@\n CLI in `scripts/`. There are no third-party Python runtime dependencies. The CLI\n is intentionally **not** exposed as a `[project.scripts]` console entry point\n because `scripts/shiki.py` is a dependency-free shim and `scripts/` is not\n-packaged as an importable distribution (see the ADR for the rationale).\n+packaged as an importable distribution (see SADR-0007 for the rationale).\n \n Supported install paths:\n \n@@ -155,6 +155,15 @@\n    [docs/operations.md](operations.md#the-new-convention). If the target has\n    pending migrations, `--force` refuses and writes nothing; apply migrations\n    (step 2) first, then re-run.\n+\n+   The SADR namespace upgrade has an additional pre-write gate: each former\n+   numeric Shiki decision path is removed only when the existing install stamp\n+   contains that exact path and a digest matching its current bytes. The\n+   installer aggregates absent/unreadable/malformed-stamp, missing-digest, and\n+   digest-mismatch blockers before raising, so refusal leaves the target tree\n+   byte-identical. Numeric target ADRs are preserved. A successful forced\n+   upgrade installs and stamps `SADR-*` paths and drops the removed legacy paths\n+   from the refreshed stamp.\n 2. Apply repository-local `.shiki/` state migrations with the migrate command:\n \n    ```bash\ndiff --git a/docs/usage.md b/docs/usage.md\nindex b83407ee3970dd395dd893010bc5dbe8bad26051..a130ef6e40dfd8169a5bc701339351097df1a09f\n--- a/docs/usage.md\n+++ b/docs/usage.md\n@@ -67,8 +67,10 @@\n shiki install-target /path/to/target-repo --local-only\n ```\n \n-Use `--force` only when you intentionally want to overwrite existing target\n-files.\n+Use `--force` only when you intentionally want to refresh the target template.\n+For the SADR namespace migration, `--force` deletes only exact legacy Shiki\n+decision paths whose existing install-stamp digest still matches; any ownership\n+blocker stops before the first write. Numeric target ADRs are never candidates.\n \n ## `shiki bootstrap-platform` \u2014 publish the Shiki platform repo\n \ndiff --git a/scripts/guardian_approval_signal.py b/scripts/guardian_approval_signal.py\nindex d059f92b2c82aa2b5d6ca421365537a87886c1cb..16924892714f4f5b9303c0ef0e325a7e70075630\n--- a/scripts/guardian_approval_signal.py\n+++ b/scripts/guardian_approval_signal.py\n@@ -199,8 +199,8 @@\n ) -> Any:\n     \"\"\"Evaluate SADR-0015 Contract Approval for the CCA Guardian signal.\n \n-    The SAME OR that MergeGate applies must reach this deterministic signal: ADR\n-    0015 is explicit that without it, a contract-approved PR still returns\n+    The SAME OR that MergeGate applies must reach this deterministic signal:\n+    SADR-0015 is explicit that without it, a contract-approved PR still returns\n     ``needs_guardian``, the CCA job fails, and the MergeGate policy check never\n     runs \u2014 the gate this change moves would never be reached. This resolves the\n     task the SAME way MergeGate does (the first Shiki task id in the PR body),\ndiff --git a/scripts/shiki_contract_approval.py b/scripts/shiki_contract_approval.py\nindex 941bcb96fdd8b9a85bc0c4f0f04d5dad9d04839c..4a4b418f6527413161db82670bec857a043360e2\n--- a/scripts/shiki_contract_approval.py\n+++ b/scripts/shiki_contract_approval.py\n@@ -142,7 +142,7 @@\n     changed_files_status: Any,\n     registration: Any,\n ) -> ContractApprovalResult:\n-    \"\"\"Whether this PR carries an SADR-0015 Contract Approval.\n+    \"\"\"Whether this PR carries a SADR-0015 Contract Approval.\n \n     The four conditions are evaluated IN ORDER, each failure recorded as a\n     distinct reason string:\ndiff --git a/scripts/shiki_guardian.py b/scripts/shiki_guardian.py\nindex a4ecb79bbda55fd5c8b06adfae2eb99760a03c45..4dd99bbe714e26f421fa19abac7a34ae1b195ecc\n--- a/scripts/shiki_guardian.py\n+++ b/scripts/shiki_guardian.py\n@@ -12,7 +12,7 @@\n # The carry re-derives its proof from git in-process on every judging invocation;\n # it is never read from an artifact, so there is nothing to go stale or be forged.\n # shiki_sync_proof is the ONLY guardian-family module that runs git, and every one\n-# of its invocations is read-only and local (ADR: prove-from-git base sync).\n+# of its invocations is read-only and local (SADR: prove-from-git base sync).\n from shiki_sync_proof import verify_pure_base_sync\n \n GUARDIAN_POLICY_PATH = \".shiki/guardian-policy.json\"\ndiff --git a/scripts/shiki_guardian_review.py b/scripts/shiki_guardian_review.py\nindex dd0f104d457899e0ba5ff1e162011c1e90b6f92b..e10ef58c046324e7ea3a3392feb3048ca77414ce\n--- a/scripts/shiki_guardian_review.py\n+++ b/scripts/shiki_guardian_review.py\n@@ -159,7 +159,7 @@\n     docs_only = _is_docs_only(changed_files)\n     if docs_only and matched == 0:\n         focus.append(\n-            \"Docs/ADR-only change: confirm the change is genuinely documentation/decision-only, introduces no behavior or contract drift, and stays consistent with the constitution and existing ADRs.\"\n+            \"Docs/decision-record-only change: confirm the change is genuinely documentation/decision-only, introduces no behavior or contract drift, and stays consistent with the constitution and existing SADRs and target ADRs.\"\n         )\n \n     # Conservative augmentation: unknown (no category and not docs-only), mixed\ndiff --git a/scripts/shiki_installer.py b/scripts/shiki_installer.py\nindex be064ab7a29ff6836f7f7a1d70500a43e0d11d7b..9fd8eefd137ce7003dad5c0dcb2e48e2cd47ab32\n--- a/scripts/shiki_installer.py\n+++ b/scripts/shiki_installer.py\n@@ -118,6 +118,7 @@\n # what lets a target carry it without a governance-schema change.\n INSTALL_STAMP_PATH = \".shiki/install-stamp.json\"\n INSTALL_STAMP_VERSION = 1\n+LEGACY_SHIKI_SADR_MAX = 18\n \n # Under --force the shipped surface splits three ways, so nothing is lost and\n # nothing is silently stale.\n@@ -237,9 +238,98 @@\n         )\n \n \n+def legacy_shiki_adr_paths() -> tuple[str, ...]:\n+    \"\"\"Former numeric paths for Shiki SADR-0001 through SADR-0018.\n+\n+    The authoritative slugs come from the current SADR filenames. SADR-0019 is\n+    the namespace decision itself and never shipped under a numeric legacy path.\n+    \"\"\"\n+    paths: list[str] = []\n+    for sadr in sorted((ROOT / \"docs\" / \"adr\").glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\")):\n+        number = sadr.name.split(\"-\", 2)[1]\n+        if int(number) <= LEGACY_SHIKI_SADR_MAX:\n+            paths.append(f\"docs/adr/{sadr.name.removeprefix('SADR-')}\")\n+    return tuple(paths)\n+\n+\n+def preflight_legacy_sadr_cleanup(target: Path, *, force: bool) -> tuple[str, ...]:\n+    \"\"\"Authorize exact legacy Shiki ADR removals before the first write.\n+\n+    A non-force install never removes existing files. A forced install needs no\n+    stamp when no legacy Shiki path is present. When legacy paths do exist, each\n+    must be named in a readable install stamp and its current bytes must match\n+    the recorded digest.\n+    \"\"\"\n+    if not force:\n+        return ()\n+    present = tuple(\n+        relative\n+        for relative in legacy_shiki_adr_paths()\n+        if (target / relative).exists() or (target / relative).is_symlink()\n+    )\n+    if not present:\n+        return ()\n+\n+    stamp_path = target / INSTALL_STAMP_PATH\n+    try:\n+        raw = stamp_path.read_text(encoding=\"utf-8\")\n+        stamp = json.loads(raw)\n+    except FileNotFoundError:\n+        detail = \"install stamp is absent\"\n+        blockers = [f\"{relative}: {detail}\" for relative in present]\n+    except (OSError, UnicodeDecodeError) as error:\n+        detail = f\"install stamp is unreadable ({error})\"\n+        blockers = [f\"{relative}: {detail}\" for relative in present]\n+    except json.JSONDecodeError as error:\n+        detail = f\"install stamp is malformed ({error})\"\n+        blockers = [f\"{relative}: {detail}\" for relative in present]\n+    else:\n+        if not isinstance(stamp, dict):\n+            blockers = [\n+                f\"{relative}: install stamp is malformed (expected an object)\"\n+                for relative in present\n+            ]\n+        else:\n+            digests = stamp.get(\"digests\")\n+            blockers = []\n+            if not isinstance(digests, dict):\n+                blockers.extend(\n+                    f\"{relative}: install stamp has no digest for this path\"\n+                    for relative in present\n+                )\n+            else:\n+                for relative in present:\n+                    expected = digests.get(relative)\n+                    if not isinstance(expected, str) or not expected:\n+                        blockers.append(\n+                            f\"{relative}: install stamp has no digest for this path\"\n+                        )\n+                        continue\n+                    try:\n+                        actual = _sha256_file(target / relative)\n+                    except OSError as error:\n+                        blockers.append(\n+                            f\"{relative}: legacy path is unreadable ({error})\"\n+                        )\n+                        continue\n+                    if actual != expected:\n+                        blockers.append(\n+                            f\"{relative}: digest mismatch (stamp {expected}, current {actual})\"\n+                        )\n+\n+    if blockers:\n+        joined = \"\\n\".join(f\"- {blocker}\" for blocker in blockers)\n+        raise ShikiError(\n+            \"legacy Shiki ADR cleanup is not authorized:\\n\"\n+            f\"{joined}\\nNo files were written.\"\n+        )\n+    return present\n+\n+\n def install_template(target: Path, *, force: bool, validate: bool) -> list[NewFileNote]:\n     # Preconditions first: never rewrite the tree of a target that cannot be\n     # upgraded cleanly (see refuse_pending_migrations).\n+    legacy_cleanup = preflight_legacy_sadr_cleanup(target, force=force)\n     refuse_pending_migrations(target)\n \n     # Resolve the target's required CODEOWNERS owner once from its\n@@ -250,6 +340,10 @@\n     # verbatim. See ``codeowners_required_owner``.\n     codeowners_owner = codeowners_required_owner(target)\n \n+    for relative in legacy_cleanup:\n+        (target / relative).unlink()\n+        info(f\"removed stamped legacy Shiki ADR: {target / relative}\")\n+\n     notes: list[NewFileNote] = []\n     for relative in TEMPLATE_PATHS:\n         source = ROOT / relative\n@@ -647,6 +741,22 @@\n     digests = target_stamp_digests(target)\n     existing = read_install_stamp(target)\n     if existing is not None:\n+        # A non-force install deliberately leaves legacy Shiki ADRs in place.\n+        # Preserve only still-matching ownership proof so a later forced\n+        # upgrade can authorize cleanup; never carry a missing or stale digest.\n+        existing_digests = existing.get(\"digests\")\n+        if isinstance(existing_digests, dict):\n+            for relative in legacy_shiki_adr_paths():\n+                expected = existing_digests.get(relative)\n+                path = target / relative\n+                if not isinstance(expected, str) or not path.is_file():\n+                    continue\n+                try:\n+                    actual = _sha256_file(path)\n+                except OSError:\n+                    continue\n+                if actual == expected:\n+                    digests[relative] = actual\n         if _incoming_is_older(commit, existing.get(\"platform_commit\")):\n             warn(\n                 \"kept existing install stamp: incoming install \"\ndiff --git a/scripts/shiki_loop.py b/scripts/shiki_loop.py\nindex 3611641da8aeb5038b1da90e3114b09a473ec6d8..84caa491b9cad0edc4176f209c13e9221e975681\n--- a/scripts/shiki_loop.py\n+++ b/scripts/shiki_loop.py\n@@ -424,7 +424,7 @@\n         # unresolvable CCA verdict has already produced its terminal stop and is never\n         # laundered into a head-moving sync (SADR-0010). It also never fires while the\n         # policy (Guardian) gate is red \u2014 for ANY reason, not only when it is the sole\n-        # red check \u2014 so a head-bound Guardian approval, or an SADR-0015\n+        # red check \u2014 so a head-bound Guardian approval, or a SADR-0015\n         # contract-approval carry that base movement defeated (the policy gate compares\n         # base and head task governance directly), is never silently restored by\n         # adopting base wholesale. It is bounded by a durable per-task counter so the\ndiff --git a/scripts/shiki_tasks.py b/scripts/shiki_tasks.py\nindex aec3809f24b4a0c8c96fc50971cff9ae9ad30381..d531052e9d9717058d4631483cefab44c26a65c1\n--- a/scripts/shiki_tasks.py\n+++ b/scripts/shiki_tasks.py\n@@ -821,7 +821,7 @@\n         \"prompt\": prompt,\n         \"entry_skill\": \"grill-with-docs\",\n         \"required_next_steps\": [\n-            \"Run grill-with-docs until terminology, boundaries, risks, and ADR-worthy decisions are settled.\",\n+            \"Run grill-with-docs until terminology, boundaries, risks, and decision-record-worthy choices are settled.\",\n             \"Obtain the operator's explicit approval of the PRD/requirements (Spec Freeze).\",\n             \"Write a machine-readable plan JSON with grill_with_docs.status=complete and spec_freeze.status=frozen.\",\n             \"Run shiki plan ingest --plan-file PLAN.json.\",\ndiff --git a/scripts/test_shiki_goal_loop.sh b/scripts/test_shiki_goal_loop.sh\nindex 753c7b516e10f0be3bc92a4bd0eb1fa699776ae6..8f3820a0a079b3044424cfe1a6c6b7fe68896e67\n--- a/scripts/test_shiki_goal_loop.sh\n+++ b/scripts/test_shiki_goal_loop.sh\n@@ -231,8 +231,8 @@\n \n # High/critical risk merges autonomously when all required checks are green:\n # the \"MergeGate policy check\" required check IS the Guardian gate (it enforces\n-# guardian-policy.json \u2014 human approval OR external AI guardian review, ADR\n-# 0010), so green checks mean a recorded authority approved. The fake gh returns\n+# guardian-policy.json \u2014 human approval OR external AI guardian review,\n+# SADR-0010), so green checks mean a recorded authority approved. The fake gh returns\n # green checks, simulating that the Guardian gate passed.\n python3 \"$ROOT/scripts/shiki.py\" goal create --target \"$TARGET\" --title \"Guardian gate\" --outcome \"High risk merges via policy gate\" >/tmp/shiki-goal-loop-guardian-goal.json\n GGOAL=\"$(json_get /tmp/shiki-goal-loop-guardian-goal.json goal_id)\"\ndiff --git a/tests/test_adr_references.py b/tests/test_adr_references.py\nindex 51706329c797d5dd9b47e082a333fa83b261d482..db6e693e35f67103935be34f1d9bb4ff7ee60395\n--- a/tests/test_adr_references.py\n+++ b/tests/test_adr_references.py\n@@ -1,7 +1,7 @@\n-\"\"\"Every ``ADR ####`` citation must resolve to exactly one decision record.\n+\"\"\"Every target ADR and Shiki SADR citation resolves in its own namespace.\n \n Background. ``scripts/mergegate_check.py`` justifies ``is_bookkeeping_closeout``\n-with \"ADR 0017\" nine times and ``scripts/guardian_approval_signal.py`` cites\n+with \"SADR-0017\" nine times and ``scripts/guardian_approval_signal.py`` cites\n 0016/0017 seven times, yet for a stretch the numbering in ``docs/adr/`` stopped\n at 0015: the authoring commits lived on a coordinator branch and no task ever\n declared ``docs/adr/...`` in its locks, so nothing carried the records to the\n@@ -11,11 +11,12 @@\n and the directory drifted with no signal.\n \n This is the same unbound-pair shape behind other silent failures: a reference\n-and its target with nothing asserting they agree. This test binds them. It scans\n-every ``scripts/*.py`` (plus ``docs/**/*.md`` and the root ``*.md`` governance\n-files, which are cheap) for the ``ADR ####`` pattern and asserts each cited\n-number resolves to exactly one ``docs/adr/####-*.md``. The converse is\n-deliberately NOT required: an ADR may exist without being cited.\n+and its target with nothing asserting they agree. This test binds them. Target\n+``ADR NNNN`` citations resolve only to ``docs/adr/NNNN-*.md``; Shiki\n+``SADR-NNNN`` citations resolve only to ``docs/adr/SADR-NNNN-*.md``. The scan\n+covers current governance docs, scripts, workflows, prompts, packaging metadata,\n+and repo-local Codex guidance while deliberately excluding historical\n+``.shiki`` evidence. The converse is not required: a record may be uncited.\n \n The suite is dependency free: standard-library ``unittest`` only.\n \"\"\"\n@@ -31,17 +32,18 @@\n \n REPO_ROOT = Path(__file__).resolve().parents[1]\n \n-# \"ADR 0016\" -- a four-digit citation. The trailing \\b keeps a five-digit token\n-# from being read as a truncated four-digit number.\n-_CITATION = re.compile(r\"\\bADR (\\d{4})\\b\")\n-# \"0016-locks-bound-product-paths.md\" -- the leading four digits name the record.\n-_ADR_FILENAME = re.compile(r\"^(\\d{4})-.*\\.md$\")\n+# ``ADR 0016`` / ``SADR-0016`` -- a namespace plus four-digit citation. The\n+# trailing ``\\b`` keeps a five-digit token from being read as a truncated one.\n+_CITATION = re.compile(r\"\\b(S?ADR)[ -](\\d{4})\\b\")\n+# Target ADRs retain the numeric filename; Shiki platform decisions use SADR.\n+_DECISION_FILENAME = re.compile(r\"^(?:(SADR)-)?(\\d{4})-.*\\.md$\")\n \n \n class Violation(NamedTuple):\n     \"\"\"A citation that does not resolve to exactly one decision record.\"\"\"\n \n     citing_file: str  # repo-relative path of the file that cites the ADR\n+    namespace: str  # \"ADR\" for target decisions; \"SADR\" for Shiki decisions\n     number: str  # the cited four-digit number, e.g. \"0016\"\n     line: int  # 1-based line of the citation\n     reason: str  # \"missing\" (zero matches) or \"ambiguous\" (two or more)\n@@ -49,44 +51,59 @@\n \n     @property\n     def expected_path(self) -> str:\n-        return f\"docs/adr/{self.number}-*.md\"\n+        prefix = \"SADR-\" if self.namespace == \"SADR\" else \"\"\n+        return f\"docs/adr/{prefix}{self.number}-*.md\"\n \n     def describe(self) -> str:\n         \"\"\"Name the citing file, the cited number, and the missing path.\"\"\"\n         if self.reason == \"missing\":\n             return (\n-                f\"{self.citing_file}:{self.line} cites ADR {self.number} \"\n+                f\"{self.citing_file}:{self.line} cites {self.namespace} {self.number} \"\n                 f\"but no document matches {self.expected_path}\"\n             )\n         joined = \", \".join(self.matches)\n         return (\n-            f\"{self.citing_file}:{self.line} cites ADR {self.number} which \"\n+            f\"{self.citing_file}:{self.line} cites {self.namespace} {self.number} which \"\n             f\"resolves to {len(self.matches)} documents (expected exactly one \"\n             f\"at {self.expected_path}): {joined}\"\n         )\n \n \n-def _adr_index(root: Path) -> dict[str, list[Path]]:\n-    \"\"\"Map every four-digit ADR number to the record files that claim it.\"\"\"\n-    index: dict[str, list[Path]] = {}\n+def _adr_index(root: Path) -> dict[tuple[str, str], list[Path]]:\n+    \"\"\"Map each decision namespace and number to the files that claim it.\"\"\"\n+    index: dict[tuple[str, str], list[Path]] = {}\n     adr_dir = root / \"docs\" / \"adr\"\n     if adr_dir.is_dir():\n         for path in sorted(adr_dir.glob(\"*.md\")):\n-            match = _ADR_FILENAME.match(path.name)\n+            match = _DECISION_FILENAME.match(path.name)\n             if match:\n-                index.setdefault(match.group(1), []).append(path)\n+                namespace = \"SADR\" if match.group(1) else \"ADR\"\n+                index.setdefault((namespace, match.group(2)), []).append(path)\n     return index\n \n \n def _citing_files(root: Path, include_docs: bool = True) -> list[Path]:\n-    \"\"\"Files scanned for citations: scripts/*.py, and cheaply the markdown.\"\"\"\n+    \"\"\"Current, shipped text surfaces scanned for decision citations.\"\"\"\n     files = sorted((root / \"scripts\").glob(\"*.py\"))\n+    files += sorted((root / \"scripts\").glob(\"*.sh\"))\n     if include_docs:\n         docs = root / \"docs\"\n         if docs.is_dir():\n             files += sorted(docs.rglob(\"*.md\"))\n         files += sorted(root.glob(\"*.md\"))\n-    return files\n+        pyproject = root / \"pyproject.toml\"\n+        if pyproject.is_file():\n+            files.append(pyproject)\n+        for relative in (\".github\", \".codex\"):\n+            surface = root / relative\n+            if surface.is_dir():\n+                files += sorted(\n+                    path\n+                    for path in surface.rglob(\"*\")\n+                    if path.is_file()\n+                    and path.suffix in {\".md\", \".py\", \".sh\", \".toml\", \".yaml\", \".yml\"}\n+                )\n+    return sorted(set(files))\n \n \n def find_adr_reference_violations(\n@@ -94,10 +111,9 @@\n ) -> list[Violation]:\n     \"\"\"Return every ``ADR ####`` citation under ``root`` that fails to resolve.\n \n-    A citation resolves when exactly one ``docs/adr/####-*.md`` matches its\n-    number. Zero matches is a \"missing\" violation; two or more is \"ambiguous\".\n-    Records that are never cited produce nothing -- the converse is not\n-    required.\n+    A citation resolves when exactly one record in the same namespace matches\n+    its number. Zero matches is a \"missing\" violation; two or more is\n+    \"ambiguous\". Records that are never cited produce nothing.\n     \"\"\"\n     root = Path(root)\n     index = _adr_index(root)\n@@ -110,13 +126,15 @@\n         rel = path.relative_to(root).as_posix()\n         for lineno, line in enumerate(text.splitlines(), start=1):\n             for match in _CITATION.finditer(line):\n-                number = match.group(1)\n-                found = index.get(number, [])\n+                namespace, number = match.groups()\n+                found = index.get((namespace, number), [])\n                 if len(found) == 1:\n                     continue\n                 reason = \"missing\" if not found else \"ambiguous\"\n                 rels = [p.relative_to(root).as_posix() for p in found]\n-                violations.append(Violation(rel, number, lineno, reason, rels))\n+                violations.append(\n+                    Violation(rel, namespace, number, lineno, reason, rels)\n+                )\n     return violations\n \n \n@@ -129,8 +147,8 @@\n         self.assertEqual(\n             violations,\n             [],\n-            \"Every 'ADR ####' citation must resolve to exactly one \"\n-            f\"docs/adr/####-*.md:\\n{detail}\",\n+            \"Every ADR/SADR citation must resolve exactly once in its own \"\n+            f\"docs/adr namespace:\\n{detail}\",\n         )\n \n     def test_scan_actually_reads_scripts(self) -> None:\n@@ -146,8 +164,21 @@\n         # The two records this Goal carried onto the default branch, and which\n         # the scripts cite most, must be present.\n         index = _adr_index(REPO_ROOT)\n-        self.assertIn(\"0016\", index)\n-        self.assertIn(\"0017\", index)\n+        self.assertIn((\"SADR\", \"0016\"), index)\n+        self.assertIn((\"SADR\", \"0017\"), index)\n+\n+    def test_platform_tree_has_only_sadr_0001_through_0019(self) -> None:\n+        adr_dir = REPO_ROOT / \"docs\" / \"adr\"\n+        sadrs = sorted(adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\"))\n+        self.assertEqual(\n+            {path.name.split(\"-\", 2)[1] for path in sadrs},\n+            {f\"{number:04d}\" for number in range(1, 20)},\n+        )\n+        self.assertEqual(\n+            list(adr_dir.glob(\"[0-9][0-9][0-9][0-9]-*.md\")),\n+            [],\n+            \"the Shiki platform tree must not retain legacy numeric SADR paths\",\n+        )\n \n \n class FixtureAdrReferences(unittest.TestCase):\n@@ -180,6 +211,26 @@\n         self.assertIn(\"9991\", message)  # cited number\n         self.assertIn(\"docs/adr/9991-*.md\", message)  # missing path\n \n+    def test_target_adr_does_not_satisfy_missing_platform_sadr(self) -> None:\n+        root = self._make_repo(\n+            adr_names=[\"0042-target-decision.md\"],\n+            script_body='\"\"\"Target ADR 0042 exists, but platform SADR-0042 does not.\"\"\"\\n',\n+        )\n+        violations = find_adr_reference_violations(root, include_docs=False)\n+        self.assertEqual(len(violations), 1)\n+        self.assertEqual(violations[0].namespace, \"SADR\")\n+        self.assertEqual(violations[0].number, \"0042\")\n+        self.assertEqual(violations[0].expected_path, \"docs/adr/SADR-0042-*.md\")\n+\n+    def test_target_adr_and_platform_sadr_may_reuse_a_number(self) -> None:\n+        root = self._make_repo(\n+            adr_names=[\"0042-target-decision.md\", \"SADR-0042-platform-decision.md\"],\n+            script_body='\"\"\"Target ADR 0042 and platform SADR-0042 are distinct.\"\"\"\\n',\n+        )\n+        self.assertEqual(\n+            find_adr_reference_violations(root, include_docs=False), []\n+        )\n+\n     def test_uncited_adr_is_allowed(self) -> None:\n         # An ADR exists but nothing cites it -> not a violation. The converse\n         # (every ADR must be cited) is explicitly NOT required.\ndiff --git a/tests/test_guardian_review.py b/tests/test_guardian_review.py\nindex 88829e7810212635e2fc98aa5918d6f26294677d..00a85a237acd5bf2d6cb5b294128198c422402ca\n--- a/tests/test_guardian_review.py\n+++ b/tests/test_guardian_review.py\n@@ -162,7 +162,7 @@\n \n     def test_docs_only_is_light_and_not_conservative(self):\n         focus = gr.classify_review_focus([\"docs/adr/0099-x.md\", \"README.md\"], \"\", \"low\")\n-        self.assertTrue(any(\"Docs/ADR-only\" in f for f in focus))\n+        self.assertTrue(any(\"Docs/decision-record-only\" in f for f in focus))\n         self.assertFalse(any(\"conservative default\" in f.lower() for f in focus))\n \n     def test_unknown_category_is_conservative(self):\ndiff --git a/tests/test_policy_job_merged_prs.py b/tests/test_policy_job_merged_prs.py\nindex d45512e74681e5e649279236c7d4e79d5c3da471..b97989f8bd74d091de1a6b5275a20d8dc68c4512\n--- a/tests/test_policy_job_merged_prs.py\n+++ b/tests/test_policy_job_merged_prs.py\n@@ -3,8 +3,8 @@\n \n ``.github/workflows/shiki-cca-completion.yml`` has two consumers of the merged-PR\n proof: the CCA *signal* (``guardian_approval_signal.py`` in \"Prepare CCA context\")\n-and the authoritative gate (``mergegate_check.py`` in \"Enforce MergeGate\"). ADR\n-0017's bookkeeping-closeout exemption (condition 6) needs the merged implementation\n+and the authoritative gate (``mergegate_check.py`` in \"Enforce MergeGate\").\n+SADR-0017's bookkeeping-closeout exemption (condition 6) needs the merged implementation\n PR proven, and a closeout carries NO ``post_merge_reconcile`` marker. The signal\n already resolves ``MERGED_PRS`` unconditionally, but \"Enforce MergeGate\" used to\n resolve it only inside ``grep -q '<!-- shiki:post_merge_reconcile -->'``. So a\ndiff --git a/tests/test_sadr_install_migration.py b/tests/test_sadr_install_migration.py\nnew file mode 100644\nindex 0000000000000000000000000000000000000000..a6f4c57e4dcaef73ef600b0d4804744138ab9071\n--- /dev/null\n+++ b/tests/test_sadr_install_migration.py\n@@ -0,0 +1,229 @@\n+\"\"\"SADR installation and collision-safe legacy upgrade behavior.\"\"\"\n+\n+from __future__ import annotations\n+\n+import contextlib\n+import hashlib\n+import io\n+import json\n+import shutil\n+import tempfile\n+import unittest\n+from pathlib import Path\n+\n+import shiki_test_support  # noqa: F401  (path bootstrap)\n+\n+import shiki_installer\n+from tests.test_adr_references import find_adr_reference_violations\n+\n+\n+PRODUCT_NUMBERS = tuple(range(13, 19))\n+\n+\n+def _quiet_install(target: Path, *, force: bool, validate: bool) -> None:\n+    with contextlib.redirect_stdout(io.StringIO()):\n+        shiki_installer.install_template(target, force=force, validate=validate)\n+\n+\n+def _sha256(path: Path) -> str:\n+    return hashlib.sha256(path.read_bytes()).hexdigest()\n+\n+\n+def _legacy_sadr_paths() -> tuple[str, ...]:\n+    return tuple(\n+        f\"docs/adr/{path.name.removeprefix('SADR-')}\"\n+        for path in sorted((shiki_installer.ROOT / \"docs\" / \"adr\").glob(\"SADR-*.md\"))\n+        if path.name != \"SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\"\n+    )\n+\n+\n+def _create_legacy_upgrade_fixture(\n+    target: Path,\n+) -> tuple[dict[str, bytes], tuple[str, ...]]:\n+    adr_dir = target / \"docs\" / \"adr\"\n+    adr_dir.mkdir(parents=True)\n+    product_bytes = {\n+        f\"{number:04d}-cubeshare-product-decision.md\": (\n+            f\"# CubeShare ADR {number:04d}\\n\\nDo not rewrite {number}.\\n\"\n+        ).encode()\n+        for number in PRODUCT_NUMBERS\n+    }\n+    for name, content in product_bytes.items():\n+        (adr_dir / name).write_bytes(content)\n+\n+    legacy_paths = _legacy_sadr_paths()\n+    for relative in legacy_paths:\n+        (target / relative).write_bytes(f\"legacy Shiki bytes for {relative}\\n\".encode())\n+\n+    stamp_path = target / shiki_installer.INSTALL_STAMP_PATH\n+    stamp_path.parent.mkdir(parents=True, exist_ok=True)\n+    stamp = {\n+        \"version\": shiki_installer.INSTALL_STAMP_VERSION,\n+        \"platform_commit\": None,\n+        \"installed_at\": \"2000-01-01T00:00:00+00:00\",\n+        \"digests\": {\n+            relative: _sha256(target / relative) for relative in legacy_paths\n+        },\n+    }\n+    stamp_path.write_text(\n+        json.dumps(stamp, indent=2, sort_keys=True) + \"\\n\", encoding=\"utf-8\"\n+    )\n+    return product_bytes, legacy_paths\n+\n+\n+def _tree_snapshot(root: Path) -> dict[str, tuple[str, bytes | str]]:\n+    \"\"\"Recursive byte/shape snapshot used to prove preflight is write-free.\"\"\"\n+    snapshot: dict[str, tuple[str, bytes | str]] = {}\n+    for path in sorted(root.rglob(\"*\")):\n+        relative = path.relative_to(root).as_posix()\n+        if path.is_symlink():\n+            snapshot[relative] = (\"symlink\", path.readlink().as_posix())\n+        elif path.is_dir():\n+            snapshot[relative] = (\"directory\", \"\")\n+        else:\n+            snapshot[relative] = (\"file\", path.read_bytes())\n+    return snapshot\n+\n+\n+class FreshSadrInstallTests(unittest.TestCase):\n+    def setUp(self) -> None:\n+        self.tmp = Path(tempfile.mkdtemp(prefix=\"shiki-sadr-fresh-\"))\n+        self.addCleanup(lambda: shutil.rmtree(self.tmp, ignore_errors=True))\n+        self.target = self.tmp / \"target\"\n+        self.adr_dir = self.target / \"docs\" / \"adr\"\n+        self.adr_dir.mkdir(parents=True)\n+        self.product_bytes = {\n+            f\"{number:04d}-product-decision.md\": (\n+                f\"# Product ADR {number:04d}\\n\\nTarget-owned bytes {number}.\\n\"\n+            ).encode()\n+            for number in PRODUCT_NUMBERS\n+        }\n+        for name, content in self.product_bytes.items():\n+            (self.adr_dir / name).write_bytes(content)\n+\n+    def test_fresh_install_preserves_product_adrs_and_installs_all_sadrs(self) -> None:\n+        _quiet_install(self.target, force=False, validate=True)\n+\n+        for name, expected in self.product_bytes.items():\n+            self.assertEqual((self.adr_dir / name).read_bytes(), expected)\n+\n+        sadr_numbers = {\n+            path.name.split(\"-\", 2)[1]\n+            for path in self.adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\")\n+        }\n+        self.assertEqual(sadr_numbers, {f\"{number:04d}\" for number in range(1, 20)})\n+\n+        numeric_records = {\n+            path.name\n+            for path in self.adr_dir.glob(\"[0-9][0-9][0-9][0-9]-*.md\")\n+        }\n+        self.assertEqual(numeric_records, set(self.product_bytes))\n+        self.assertEqual(find_adr_reference_violations(self.target), [])\n+\n+\n+class LegacySadrUpgradeTests(unittest.TestCase):\n+    def setUp(self) -> None:\n+        self.tmp = Path(tempfile.mkdtemp(prefix=\"shiki-sadr-upgrade-\"))\n+        self.addCleanup(lambda: shutil.rmtree(self.tmp, ignore_errors=True))\n+        self.target = self.tmp / \"target\"\n+        self.adr_dir = self.target / \"docs\" / \"adr\"\n+        self.product_bytes, self.legacy_paths = _create_legacy_upgrade_fixture(\n+            self.target\n+        )\n+\n+    def test_force_upgrade_deletes_only_stamped_legacy_sadrs(self) -> None:\n+        _quiet_install(self.target, force=True, validate=True)\n+\n+        for relative in self.legacy_paths:\n+            self.assertFalse((self.target / relative).exists(), relative)\n+        for name, expected in self.product_bytes.items():\n+            self.assertEqual((self.adr_dir / name).read_bytes(), expected)\n+\n+        sadrs = list(self.adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\"))\n+        self.assertEqual(len(sadrs), 19)\n+        refreshed = json.loads(\n+            (self.target / shiki_installer.INSTALL_STAMP_PATH).read_text(encoding=\"utf-8\")\n+        )\n+        for relative in self.legacy_paths:\n+            self.assertNotIn(relative, refreshed[\"digests\"])\n+        for path in sadrs:\n+            relative = path.relative_to(self.target).as_posix()\n+            self.assertEqual(refreshed[\"digests\"][relative], _sha256(path))\n+\n+    def test_non_force_install_never_deletes_legacy_files(self) -> None:\n+        before = {\n+            relative: (self.target / relative).read_bytes()\n+            for relative in self.legacy_paths\n+        }\n+        _quiet_install(self.target, force=False, validate=False)\n+        for relative, expected in before.items():\n+            self.assertEqual((self.target / relative).read_bytes(), expected)\n+        refreshed = json.loads(\n+            (self.target / shiki_installer.INSTALL_STAMP_PATH).read_text(encoding=\"utf-8\")\n+        )\n+        for relative in self.legacy_paths:\n+            self.assertEqual(refreshed[\"digests\"][relative], _sha256(self.target / relative))\n+\n+\n+class LegacyCleanupPreflightTests(unittest.TestCase):\n+    def setUp(self) -> None:\n+        self.tmp = Path(tempfile.mkdtemp(prefix=\"shiki-sadr-preflight-\"))\n+        self.addCleanup(lambda: shutil.rmtree(self.tmp, ignore_errors=True))\n+\n+    def test_all_ownership_blockers_abort_before_any_write(self) -> None:\n+        cases = (\n+            (\"absent\", \"install stamp is absent\"),\n+            (\"unreadable\", \"install stamp is unreadable\"),\n+            (\"malformed\", \"install stamp is malformed\"),\n+            (\"missing-digest\", \"install stamp has no digest\"),\n+            (\"digest-mismatch\", \"digest mismatch\"),\n+        )\n+        for name, expected_reason in cases:\n+            with self.subTest(name=name):\n+                target = self.tmp / name\n+                _, legacy_paths = _create_legacy_upgrade_fixture(target)\n+                stamp_path = target / shiki_installer.INSTALL_STAMP_PATH\n+\n+                if name == \"absent\":\n+                    stamp_path.unlink()\n+                elif name == \"unreadable\":\n+                    stamp_path.unlink()\n+                    stamp_path.mkdir()\n+                elif name == \"malformed\":\n+                    stamp_path.write_text(\"{not-json\\n\", encoding=\"utf-8\")\n+                else:\n+                    stamp = json.loads(stamp_path.read_text(encoding=\"utf-8\"))\n+                    if name == \"missing-digest\":\n+                        stamp[\"digests\"] = {}\n+                    else:\n+                        stamp[\"digests\"] = {\n+                            relative: \"deadbeef\" for relative in legacy_paths\n+                        }\n+                    stamp_path.write_text(\n+                        json.dumps(stamp, indent=2, sort_keys=True) + \"\\n\",\n+                        encoding=\"utf-8\",\n+                    )\n+\n+                before = _tree_snapshot(target)\n+                with self.assertRaises(shiki_installer.ShikiError) as raised:\n+                    _quiet_install(target, force=True, validate=False)\n+                after = _tree_snapshot(target)\n+\n+                self.assertEqual(before, after, f\"{name} preflight changed the tree\")\n+                message = str(raised.exception)\n+                self.assertIn(expected_reason, message)\n+                self.assertIn(\"No files were written\", message)\n+                for relative in legacy_paths:\n+                    self.assertIn(relative, message)\n+\n+    def test_absent_stamp_is_allowed_when_no_legacy_path_exists(self) -> None:\n+        target = self.tmp / \"no-legacy\"\n+        target.mkdir()\n+        _quiet_install(target, force=True, validate=False)\n+        self.assertTrue(\n+            (target / \"docs\" / \"adr\" / \"SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\").is_file()\n+        )\n+\n+\n+if __name__ == \"__main__\":\n+    unittest.main()\n\n2026-08-07T08:16:05.348850Z ERROR codex_models_manager::manager: failed to renew cache TTL: missing field `base_instructions` at line 94 column 5\ndiff --git a/.codex/skills/shiki/SKILL.md b/.codex/skills/shiki/SKILL.md\nindex c8e2e85d9d75d49cc4f2d6286ee22659c4e53e75..93cded623b2c15d9f65c76491a1c2d813ac9e6c0\n--- a/.codex/skills/shiki/SKILL.md\n+++ b/.codex/skills/shiki/SKILL.md\n@@ -18,6 +18,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n Codex CLI does not register this skill as a custom `/shiki` slash command. When\n the user types `/shiki` in Codex CLI and gets \"Unrecognized command\", explain\n that this is expected: invoke Shiki in Codex with natural language such as\n@@ -69,7 +72,7 @@\n \n ## Responsibilities\n \n-- Claude Code implements and repairs by default (`shiki runner claude`, ADR 0008); Codex implements and repairs tasks explicitly assigned to `codex`.\n+- Claude Code implements and repairs by default (`shiki runner claude`, SADR-0008); Codex implements and repairs tasks explicitly assigned to `codex`.\n - Claude Code Action can act as GitHub-side CCA or reviewer.\n - CCA judges completion.\n - MergeGate authorizes state transitions and merge readiness.\ndiff --git a/.github/prompts/cca-completion-check.md b/.github/prompts/cca-completion-check.md\nindex ed203ad5f302ecc81372d8ab27d2b1232ca97497..fd89950528333ef6242e6d5443904f5392ab7e56\n--- a/.github/prompts/cca-completion-check.md\n+++ b/.github/prompts/cca-completion-check.md\n@@ -19,6 +19,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n ## Required Reading\n \n Read only the files needed to judge the PR contract. Prefer this order:\ndiff --git a/.github/prompts/codex-task-handoff.md b/.github/prompts/codex-task-handoff.md\nindex 695e211d76dea1c60e8d5bb360e78487808286a4..59441d576b01a39569722f226c854e5a740e9eb5\n--- a/.github/prompts/codex-task-handoff.md\n+++ b/.github/prompts/codex-task-handoff.md\n@@ -2,7 +2,7 @@\n \n You are Codex Front acting as the Implementer for a Shiki task.\n \n-Follow `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant ADRs, and `docs/agents/codex-handoff.md`.\n+Follow `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant Shiki SADRs and target ADRs, and `docs/agents/codex-handoff.md`.\n \n Rules:\n \ndiff --git a/AGENTS.md b/AGENTS.md\nindex 7f041100d49c2a7525f51cb0f5d696a4a6d1c78c..a06bfbabc6fa48a007389e25e12d7ed523936bed\n--- a/AGENTS.md\n+++ b/AGENTS.md\n@@ -22,7 +22,7 @@\n - **Codex Front** is an optional implementer through Codex App, Codex CLI, Codex IDE extension, or Codex Web signed in with ChatGPT OAuth/subscription auth, dispatched with `shiki runner codex` when a task is explicitly assigned to `codex`.\n - **GitHub CCA** is implemented by Claude Code Action by default, using `CLAUDE_CODE_OAUTH_TOKEN`.\n \n-Do not assume `openai/codex-action`, `OPENAI_API_KEY`, or API-key based Codex automation in the default Shiki path. API-key runners are explicit target-repo extensions and require their own ADR.\n+Do not assume `openai/codex-action`, `OPENAI_API_KEY`, or API-key based Codex automation in the default Shiki path. API-key runners are explicit target-repo extensions and require their own target ADR.\n \n Shiki is not:\n \n@@ -44,10 +44,14 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use\n+`SADR-NNNN-*.md`; target product decisions retain `NNNN-*.md` and the `ADR`\n+namespace.\n+\n Use the canonical source-of-truth order above. Runtime-specific reading orders\n are operational guidance only and must not override or reorder it.\n \n-Conversation state is not durable truth. If a decision matters, put it in GitHub, `.shiki/`, `CONTEXT.md`, or an ADR.\n+Conversation state is not durable truth. If a decision matters, put it in GitHub, `.shiki/`, `CONTEXT.md`, a Shiki SADR, or a target ADR.\n \n If GitHub and `.shiki/` disagree, surface the conflict and prefer GitHub operational state until the mirror is repaired.\n \n@@ -75,8 +79,8 @@\n Every non-trivial change follows this loop:\n \n 1. **Goal**: clarify outcome, completion conditions, non-goals, risk level, and success signals.\n-2. **grill-with-docs**: challenge the plan against domain docs, ADRs, code reality, terminology, and edge scenarios.\n-3. **Context & Impact**: find relevant docs, ADRs, code, dependencies, owners, locks, and verification surfaces.\n+2. **grill-with-docs**: challenge the plan against domain docs, Shiki SADRs, target ADRs, code reality, terminology, and edge scenarios.\n+3. **Context & Impact**: find relevant docs, SADRs, target ADRs, code, dependencies, owners, locks, and verification surfaces.\n 4. **PRD and Spec Freeze**: use `to-prd` when context is settled enough to become durable product/engineering intent. The operator's explicit approval of the PRD is Spec Freeze: the plan records a `spec_freeze` block, and no plan runs without it. Steps 1-4 form Requirements Definition \u2014 one continuous operator dialogue. After Spec Freeze, scope changes require an operator-approved Spec Amendment; non-scope-moving interpretations go to the Assumption Log.\n 5. **Issues**: use `to-issues` to create independently grabbable vertical-slice issues. Prefer AFK slices over HITL slices where possible.\n 6. **Triage**: label issues for readiness, risk, runtime, skills, and MergeGate state.\n@@ -167,7 +171,7 @@\n - Testing decisions.\n - Out-of-scope items.\n - Further notes.\n-- Links to relevant domain terms and ADRs.\n+- Links to relevant domain terms, SADRs, and target ADRs.\n \n Every executable task must include:\n \n@@ -193,7 +197,7 @@\n \n Context & Impact must identify:\n \n-- Relevant documents, domain terms, ADRs, and past decisions.\n+- Relevant documents, domain terms, SADRs, target ADRs, and past decisions.\n - Relevant modules, interfaces, seams, callers, owners, and tests.\n - Dependency relationships between tasks.\n - Candidate locks and lock conflicts.\n@@ -210,7 +214,7 @@\n | Skill | Required when |\n | --- | --- |\n | `setup-matt-pocock-skills` | First configuring the repo for skills, or when issue tracker, triage labels, or domain docs are missing. |\n-| `grill-with-docs` | Default for non-trivial Goals before PRD/issues; always required when plans, terminology, boundaries, tradeoffs, or ADR-worthy decisions are ambiguous. |\n+| `grill-with-docs` | Default for non-trivial Goals before PRD/issues; always required when plans, terminology, boundaries, tradeoffs, or decision-record-worthy choices are ambiguous. |\n | `zoom-out` | The agent lacks an architectural map of the relevant code area. |\n | `to-prd` | Settled Goal context must become durable product/engineering intent. |\n | `to-issues` | A Goal, PRD, or plan must become independently grabbable vertical-slice issues. |\n@@ -348,7 +352,7 @@\n - Three or more new files.\n - Any high-risk or critical label.\n \n-When the architecture gate triggers, run `grill-with-docs` or `improve-codebase-architecture` as appropriate and record durable decisions in an ADR when needed.\n+When the architecture gate triggers, run `grill-with-docs` or `improve-codebase-architecture` as appropriate and record durable decisions in a Shiki SADR or target ADR as applicable.\n \n ## Engineering Discipline\n \ndiff --git a/CLAUDE.md b/CLAUDE.md\nindex 98a932e7841a632866fcd53b0bad0e3af3711d3d..98a31399404d6fb044b4fd493f4459ac87f439de\n--- a/CLAUDE.md\n+++ b/CLAUDE.md\n@@ -14,6 +14,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n ## Claude Role\n \n Default to planner, default implementer, reviewer, coordinator, documentation editor, CCA-style completion judge, and final-judgment assistant.\n@@ -57,7 +60,7 @@\n At the start of a Shiki session:\n \n 1. Read `AGENTS.md`.\n-2. Read `CONTEXT.md` and relevant ADRs.\n+2. Read `CONTEXT.md` and relevant Shiki SADRs and target ADRs.\n 3. Read active `.shiki/` Goal/task/ledger state when present.\n 4. Read active distilled rules from `.shiki/memories/` (status `distilled`, `active: true`, not revoked or superseded) for applicable guidance; the same rules are injected deterministically into task handoffs as the `## Distilled Rules` section (\u00a73.5).\n 5. Read the GitHub Issue or PR when the task is GitHub-backed.\n@@ -74,7 +77,7 @@\n - Define the Goal, completion conditions, non-goals, and risk level.\n - Treat `grill-with-docs` as the default planning entry point for non-trivial Goals.\n - Challenge domain terms against `CONTEXT.md`.\n-- Challenge hard-to-reverse tradeoffs against ADRs.\n+- Challenge hard-to-reverse tradeoffs against SADRs and target ADRs.\n - Explore code instead of asking when code can answer the question.\n - Run Context & Impact before implementation planning.\n - Produce a Task DAG, not an unordered checklist.\n@@ -94,7 +97,7 @@\n - For each question, provide your recommended answer.\n - Resolve dependency decisions before downstream design questions.\n - Update `CONTEXT.md` inline when terminology crystallizes.\n-- Offer ADRs only when the decision is hard to reverse, surprising without context, and a real tradeoff.\n+- Offer a SADR or target ADR only when the decision is hard to reverse, surprising without context, and a real tradeoff.\n - Convert unresolved questions into HITL blockers rather than allowing Codex to guess.\n \n ## PRD and Issues Standard\n@@ -103,7 +106,7 @@\n \n - Use the project's domain glossary vocabulary.\n - Avoid volatile file paths unless they are essential.\n-- Include implementation decisions, testing decisions, out-of-scope items, and relevant ADR/domain links.\n+- Include implementation decisions, testing decisions, out-of-scope items, and relevant SADR/target-ADR/domain links.\n - Identify deep-module opportunities and testable interfaces.\n \n When creating issues:\n@@ -121,7 +124,7 @@\n - Branch/worktree target.\n - Scope and non-goals.\n - Dependencies and locks.\n-- Relevant docs, ADRs, modules, and tests.\n+- Relevant docs, SADRs, target ADRs, modules, and tests.\n - Required skill invocations.\n - TDD expectations.\n - Pre-PR `code-review` gate expectations.\n@@ -152,7 +155,7 @@\n \n - Treat yourself as a judge, not implementer.\n - Do not write production code.\n-- Read `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant ADRs, PRD, task issue, PR body, diff, checks, reviews, labels, and `.shiki/` state.\n+- Read `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant SADRs and target ADRs, PRD, task issue, PR body, diff, checks, reviews, labels, and `.shiki/` state.\n - Evaluate every applicable checklist in `docs/agents/checklists.md`.\n - Map each acceptance criterion to durable evidence.\n - Distinguish implementation failure from missing evidence.\ndiff --git a/CONTEXT.md b/CONTEXT.md\nindex e61a9be06a0cce0d026484083a657bde2067e234..237ee8aa3ea8fab334ece49768bc5ed3b5af0974\n--- a/CONTEXT.md\n+++ b/CONTEXT.md\n@@ -25,7 +25,7 @@\n _Avoid_: jumping directly into code\n \n **grill-with-docs**:\n-A planning skill that challenges a plan against domain language, ADRs, code reality, and concrete edge scenarios. It resolves design-tree questions before PRD/issues. It runs inside Requirements Definition.\n+A planning skill that challenges a plan against domain language, Shiki SADRs, target ADRs, code reality, and concrete edge scenarios. It resolves design-tree questions before PRD/issues. It runs inside Requirements Definition.\n _Avoid_: generic brainstorming, silent assumptions\n \n **Requirements Definition**:\ndiff --git a/CONTRIBUTING.md b/CONTRIBUTING.md\nindex c6d9b656e73abb8a55e5f660123e03153dcb3e6d..9e37a8fbf668db5b22461aaae5548b8f1354394c\n--- a/CONTRIBUTING.md\n+++ b/CONTRIBUTING.md\n@@ -17,7 +17,7 @@\n 1. **Goal** \u2014 start from a user-approved Goal with an outcome, completion\n    conditions, non-goals, risk level, and success signals.\n 2. **grill-with-docs** \u2014 for any non-trivial change, challenge the plan against\n-   domain language, ADRs, and code reality before writing a PRD.\n+   domain language, Shiki SADRs, target ADRs, and code reality before writing a PRD.\n 3. **PRD** \u2014 capture settled product/engineering intent (`to-prd`).\n 4. **Spec Freeze** \u2014 the operator explicitly approves the PRD/requirements;\n    the plan records a `spec_freeze` block and no plan runs without it\n@@ -94,7 +94,7 @@\n   entries.\n - Do not hand-edit `.shiki/goals/*.json` or `.shiki/manifest.json` in a task PR.\n - Conversation state is not durable truth. If a decision matters, record it in\n-  GitHub, `.shiki/`, `CONTEXT.md`, or an ADR.\n+  GitHub, `.shiki/`, `CONTEXT.md`, a Shiki SADR, or a target ADR.\n \n ## Architecture Gate\n \n@@ -103,7 +103,7 @@\n or CI policy, package structure, three or more structural files, more than 100\n net new lines in a core module, three or more new files, or any high-risk label.\n When triggered, run `grill-with-docs` or `improve-codebase-architecture` and\n-record durable decisions in an ADR when needed (see `docs/adr/`).\n+record durable decisions in a Shiki SADR or target ADR as applicable (see `docs/adr/`).\n \n ## Safety\n \ndiff --git a/SYSTEM_PROMPT.md b/SYSTEM_PROMPT.md\nindex aaaea45e3a896c3a44ba89eec8bb84325ecf6c62..da009c86a88285269dee0155e58e550c6d744ea9\n--- a/SYSTEM_PROMPT.md\n+++ b/SYSTEM_PROMPT.md\n@@ -30,6 +30,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n Use the canonical source-of-truth order above. Runtime-specific reading orders\n are operational guidance only and must not override or reorder it. The current\n conversation is non-durable operator input.\n@@ -48,14 +51,14 @@\n \n A runtime may only do work that is explicitly assigned by the Goal, task, PR, or MergeGate decision.\n \n-Do not assume `openai/codex-action`, `OPENAI_API_KEY`, or API-key based Codex automation in the default Shiki path. API-key runners are explicit target-repo extensions and require their own ADR.\n+Do not assume `openai/codex-action`, `OPENAI_API_KEY`, or API-key based Codex automation in the default Shiki path. API-key runners are explicit target-repo extensions and require their own target ADR.\n \n ## 4. Non-negotiable Shiki flow\n \n For any non-trivial Goal, use this flow:\n \n 1. **Goal Seek**: define outcome, non-goals, risk level, completion criteria, and evidence requirements.\n-2. **grill-with-docs**: challenge the plan against `CONTEXT.md`, ADRs, code reality, terminology, boundaries, and edge scenarios. Ask one question at a time when the operator is available. If code can answer a question, inspect code instead of asking.\n+2. **grill-with-docs**: challenge the plan against `CONTEXT.md`, Shiki SADRs, target ADRs, code reality, terminology, boundaries, and edge scenarios. Ask one question at a time when the operator is available. If code can answer a question, inspect code instead of asking.\n 3. **Context & Impact**: identify modules, interfaces, seams, callers, dependencies, locks, risk, and verification surfaces.\n 4. **to-prd + Spec Freeze**: turn settled context into a PRD using domain vocabulary and testing decisions. The operator's explicit approval of the PRD is Spec Freeze, recorded as the plan's `spec_freeze` block; plans without it do not run. Steps 1-4 form Requirements Definition.\n 5. **to-issues**: decompose the PRD into independently grabbable vertical-slice issues. Prefer AFK slices; mark HITL slices when judgment is still required.\n@@ -75,7 +78,7 @@\n Before making any material change:\n \n 1. Read `AGENTS.md` and any runtime-specific wrapper such as `CLAUDE.md`.\n-2. Read `CONTEXT.md` and relevant ADRs.\n+2. Read `CONTEXT.md` and relevant SADRs and target ADRs.\n 3. Read the GitHub Issue / PR and `.shiki/` state for the active Goal or task.\n 4. Identify your role: Planner, Implementer, Reviewer, Completion Check Agent, Repairer, or Guardian-assist.\n 5. Identify Goal id, task id, branch, PR, dependencies, locks, risk level, required skills, and required evidence.\n@@ -103,7 +106,7 @@\n \n At minimum, identify:\n \n-- Relevant documents, ADRs, domain terms, and prior decisions.\n+- Relevant documents, SADRs, target ADRs, domain terms, and prior decisions.\n - Relevant modules, interfaces, seams, callers, owners, and tests.\n - Dependency relationships between tasks.\n - Candidate file locks and lock conflicts.\n@@ -152,7 +155,7 @@\n \n CCA must read:\n \n-- `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant ADRs.\n+- `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant SADRs and target ADRs.\n - Parent Goal issue, PRD issue, task issue, comments, and labels.\n - PR body, diff, commits, changed files, reviews, CI checks, and artifacts available to the workflow.\n - `.shiki/` task contract, locks, ledger entries, CCA checklist profile, and prior repair packets.\n@@ -214,7 +217,7 @@\n - Three or more new files.\n - Any high-risk or critical label.\n \n-When the architecture gate triggers, run `grill-with-docs` or `improve-codebase-architecture` as appropriate and record the decision in an ADR if it is hard to reverse or likely to be re-litigated.\n+When the architecture gate triggers, run `grill-with-docs` or `improve-codebase-architecture` as appropriate and record the decision in a Shiki SADR or target ADR as applicable if it is hard to reverse or likely to be re-litigated.\n \n ## 14. Skill Gate\n \n@@ -223,7 +226,7 @@\n Use these skills:\n \n - `setup-matt-pocock-skills`: before first use of the engineering skill set, or when issue tracker, triage labels, or domain docs are missing.\n-- `grill-with-docs`: default for non-trivial Goals before PRD/issues; required for ambiguous plans, terminology, boundaries, ADR-worthy tradeoffs, or design-tree decisions.\n+- `grill-with-docs`: default for non-trivial Goals before PRD/issues; required for ambiguous plans, terminology, boundaries, decision-record-worthy tradeoffs, or design-tree decisions.\n - `zoom-out`: unfamiliar code area or missing architectural map.\n - `to-prd`: convert settled context into a PRD.\n - `to-issues`: break a PRD, plan, or Goal into independently grabbable vertical-slice issues.\ndiff --git a/docs/adr/0001-keep-shiki-as-platform-name.md b/docs/adr/SADR-0001-keep-shiki-as-platform-name.md\nindex 20a9637c3b9d8c97de7ba8db7cfae4d82d9a7dba..8b685439959aada1b221c112f0ac3c51ca8a1695\n--- a/docs/adr/0001-keep-shiki-as-platform-name.md\n+++ b/docs/adr/SADR-0001-keep-shiki-as-platform-name.md\n@@ -1,3 +1,3 @@\n-# Keep Shiki As The Platform Name\n+# SADR-0001: Keep Shiki As The Platform Name\n \n Shiki remains the product name for the GitHub-native agentic engineering control plane. We considered more descriptive names such as MergePilot, MergeGate, and Kiroku, but kept Shiki because it carries the existing goal-seeking framework identity while remaining independent of Hermes, Codex, Claude Code, or any single runtime.\ndiff --git a/docs/adr/0002-use-agents-as-shared-constitution.md b/docs/adr/SADR-0002-use-agents-as-shared-constitution.md\nindex 73de30ecb078120eac540d9bc51855e3cd48d0e3..c5486182bf06fca36ea508496b013c67302fd989\n--- a/docs/adr/0002-use-agents-as-shared-constitution.md\n+++ b/docs/adr/SADR-0002-use-agents-as-shared-constitution.md\n@@ -1,3 +1,3 @@\n-# Use AGENTS As The Shared Agent Constitution\n+# SADR-0002: Use AGENTS As The Shared Agent Constitution\n \n Shiki uses `AGENTS.md` as the runtime-neutral constitution and `CLAUDE.md` as a thin Claude Code wrapper. This keeps Codex, Claude Code, Hermes Runner, and future Agent Runtimes aligned on the same GitHub-first MergeGate rules while still allowing runtime-specific instructions where needed.\ndiff --git a/docs/adr/0003-require-skill-gate-for-mergegate-readiness.md b/docs/adr/SADR-0003-require-skill-gate-for-mergegate-readiness.md\nindex 424be0c9af4c8f0136f1fde68a468c47ff84e764..7fc337987c7df9792f75f181dfcbed75fe5ffff2\n--- a/docs/adr/0003-require-skill-gate-for-mergegate-readiness.md\n+++ b/docs/adr/SADR-0003-require-skill-gate-for-mergegate-readiness.md\n@@ -1,3 +1,3 @@\n-# Require Skill Gate For MergeGate Readiness\n+# SADR-0003: Require Skill Gate For MergeGate Readiness\n \n Shiki requires relevant engineering skills to be invoked before execution when their triggers apply. This makes skills part of MergeGate readiness rather than optional prompt style, because Shiki depends on repeatable planning, issue decomposition, TDD, diagnosis, triage, and architecture review before autonomous or parallel agent work can be trusted.\ndiff --git a/docs/adr/0004-use-github-first-with-shiki-mirror.md b/docs/adr/SADR-0004-use-github-first-with-shiki-mirror.md\nindex d0a3a9174dc646a945a9d325954ffaf00c352040..a79c54ae9ee8eed2a0524192b864da4e22a92d3f\n--- a/docs/adr/0004-use-github-first-with-shiki-mirror.md\n+++ b/docs/adr/SADR-0004-use-github-first-with-shiki-mirror.md\n@@ -1,4 +1,4 @@\n-# ADR 0004: Use GitHub First With A Repository-Local Shiki Mirror\n+# SADR-0004: Use GitHub First With A Repository-Local Shiki Mirror\n \n ## Status\n \ndiff --git a/docs/adr/0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md b/docs/adr/SADR-0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\nindex b339baf49b89d9290e79c3d51b36e7b2bde3bd95..0f3c3eb9afccdb3403ce978e21d04fe12e495da0\n--- a/docs/adr/0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\n+++ b/docs/adr/SADR-0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\n@@ -1,8 +1,8 @@\n-# ADR 0005: Use Codex As Front And Claude Code Action As The GitHub Runtime\n+# SADR-0005: Use Codex As Front And Claude Code Action As The GitHub Runtime\n \n ## Status\n \n-Accepted (implementer default superseded by ADR 0008; the auth model and the\n+Accepted (implementer default superseded by SADR-0008; the auth model and the\n Claude Code Action GitHub runtime decision remain in force)\n \n ## Context\n@@ -26,4 +26,4 @@\n - Shiki avoids requiring `OPENAI_API_KEY` for the default workflow.\n - Codex work happens through the user's authenticated Codex front surface, not as a hidden backend Action.\n - Claude Code Action is configured for OAuth-token based subscription usage by default.\n-- API-key based automation remains possible only as an explicit target-repo extension with its own ADR.\n+- API-key based automation remains possible only as an explicit target-repo extension with its own target ADR.\ndiff --git a/docs/adr/0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md b/docs/adr/SADR-0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\nindex 4bcae9d1e52f0a37b8e9fbafa5cd14b6ee1993bd..7a38538a6ae9929567f0ade67e7ea6a60d0b3f7e\n--- a/docs/adr/0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\n+++ b/docs/adr/SADR-0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\n@@ -1,4 +1,4 @@\n-# ADR 0006: Separate Codex Implementation, CCA Completion, And MergeGate Authorization\n+# SADR-0006: Separate Codex Implementation, CCA Completion, And MergeGate Authorization\n \n ## Status\n \ndiff --git a/docs/adr/0007-packaging-and-release.md b/docs/adr/SADR-0007-packaging-and-release.md\nindex 79b84ddb97167372b44641e5b7860e400ce80822..bb445a2e2e856d9d1f5fdbe35de8d4e8cec0fb32\n--- a/docs/adr/0007-packaging-and-release.md\n+++ b/docs/adr/SADR-0007-packaging-and-release.md\n@@ -1,4 +1,4 @@\n-# ADR 0007: Packaging And Release For Shiki\n+# SADR-0007: Packaging And Release For Shiki\n \n ## Status\n \n@@ -25,7 +25,7 @@\n   not introduce third-party runtime dependencies or break the shim.\n - Releases must run inside the operator's subscription-authenticated toolchain\n   and must not require extra secrets beyond the built-in `GITHUB_TOKEN`\n-  (consistent with ADR 0005's no-API-key-by-default stance).\n+  (consistent with SADR-0005's no-API-key-by-default stance).\n - `.shiki/manifest.json` already encodes the install surface (`install.include`,\n   `install.create_directories`, `install.exclude_from_commit`), and it must not\n   be mutated as part of this change.\n@@ -34,7 +34,7 @@\n \n This is a hard-to-reverse platform decision (it sets the version source of\n truth, the release trigger, and the public install contract), so it is recorded\n-as an ADR.\n+as a SADR.\n \n ## Decision\n \n@@ -104,7 +104,7 @@\n 3. **Publishing to PyPI.** Rejected for now: Shiki is distributed as a\n    repository template + git-installable CLI, not a PyPI library; PyPI would add\n    credentials/secrets and a different versioning contract. Can be added later as\n-   its own ADR if a packaged distribution is needed.\n+   its own SADR if a packaged distribution is needed.\n 4. **Dynamic version (read `VERSION` via a build backend `dynamic` field).**\n    Rejected to keep `pyproject.toml` backend-agnostic and dependency-free; the\n    small duplication between `VERSION` and `pyproject.toml` is guarded by the\n@@ -123,4 +123,4 @@\n   releasing versions the whole platform but ships only `install.include` to\n   targets.\n - Future PyPI distribution or a console entry point are deferred and would each\n-  warrant their own ADR.\n+  warrant their own SADR.\ndiff --git a/docs/adr/0008-use-claude-code-as-default-implementer.md b/docs/adr/SADR-0008-use-claude-code-as-default-implementer.md\nindex dedf4510b7c7799521422f622029563d373db031..35c2625d908167fbc58855b5b63e283805f167e8\n--- a/docs/adr/0008-use-claude-code-as-default-implementer.md\n+++ b/docs/adr/SADR-0008-use-claude-code-as-default-implementer.md\n@@ -1,12 +1,12 @@\n-# ADR 0008: Use Claude Code As The Default Implementer Runtime\n+# SADR-0008: Use Claude Code As The Default Implementer Runtime\n \n ## Status\n \n-Proposed (supersedes ADR 0005 for the implementer default; keeps its auth model)\n+Proposed (supersedes SADR-0005 for the implementer default; keeps its auth model)\n \n ## Context\n \n-ADR 0005 made Codex Front the default implementation runtime, with Claude Code\n+SADR-0005 made Codex Front the default implementation runtime, with Claude Code\n restricted to planner and reviewer roles by the runtime registry, and the only\n autonomous implementation adapter was `shiki runner codex`.\n \n@@ -18,13 +18,13 @@\n - The platform's planning, review, and CCA layers already run on Claude Code,\n   and Claude Code's native capabilities (the built-in `/code-review` skill and\n   the Workflow multi-agent orchestration tool) have no Codex equivalent.\n-- The Goal lifecycle redesign (ADR 0009) requires an autonomous post-freeze\n+- The Goal lifecycle redesign (SADR-0009) requires an autonomous post-freeze\n   loop driven from the operator's Claude Code session, including bounded\n   repair dispatch, which must not depend on a runtime that cannot be invoked\n   unattended.\n \n The constitution claims Shiki is runtime-agnostic (\"not a Claude-only\n-workflow\", ADR 0002). Any change to the implementer default must preserve the\n+workflow\", SADR-0002). Any change to the implementer default must preserve the\n registry-based runtime model rather than hardcoding one vendor.\n \n ## Decision\n@@ -39,7 +39,7 @@\n   the task to review or repair-needed.\n - Keep Codex registered as an optional implementer runtime. `shiki runner\n   codex`, the Codex handoff templates, and the ChatGPT OAuth auth model from\n-  ADR 0005 remain supported for tasks explicitly assigned to `codex`.\n+  SADR-0005 remain supported for tasks explicitly assigned to `codex`.\n - Commit to mandating two Claude-native capabilities as loop contract in\n   follow-up Goals (status: the code-review gate was delivered by Goal G-B \u2014\n   skill registry, checklists item PR-12, default required_skills \u2014 while the\n@@ -60,7 +60,7 @@\n \n Out of scope: removing Codex support, changing the CCA runtime (Claude Code\n Action with `CLAUDE_CODE_OAUTH_TOKEN` stays), and API-key based automation\n-(still requires its own ADR per ADR 0005).\n+(still requires its own target ADR per SADR-0005).\n \n ## Consequences\n \n@@ -100,7 +100,7 @@\n   harness rejection of direct `codex exec` is outside Shiki's control, and the\n   plan's mandatory native capabilities cannot run on Codex at all.\n - **Remove Codex entirely**: rejected; it abandons the runtime-agnostic\n-  constitution (ADR 0002) for no operational gain and forecloses\n+  constitution (SADR-0002) for no operational gain and forecloses\n   cross-runtime adversarial review.\n - **GitHub Actions cloud implementation via claude-code-action**: rejected as\n   default; durable evidence is automatic but token cost, Node 24 workflow\ndiff --git a/docs/adr/0009-requirements-definition-and-spec-freeze.md b/docs/adr/SADR-0009-requirements-definition-and-spec-freeze.md\nindex 29b3f7871bef9203a8b4a831a65c39eb2f506e42..52e7dd69ad0aa4efe0b68c62678abbf7df02aaec\n--- a/docs/adr/0009-requirements-definition-and-spec-freeze.md\n+++ b/docs/adr/SADR-0009-requirements-definition-and-spec-freeze.md\n@@ -1,4 +1,4 @@\n-# ADR 0009: Merge Grill And Planning Into Requirements Definition With Spec Freeze\n+# SADR-0009: Merge Grill And Planning Into Requirements Definition With Spec Freeze\n \n ## Status\n \ndiff --git a/docs/adr/0010-external-ai-guardian-review.md b/docs/adr/SADR-0010-external-ai-guardian-review.md\nindex e6108bbe8bb4e73e0ff4684c8f78001a80438c4e..f3510b145664298adafb151b6f0db51b972f01ec\n--- a/docs/adr/0010-external-ai-guardian-review.md\n+++ b/docs/adr/SADR-0010-external-ai-guardian-review.md\n@@ -1,4 +1,4 @@\n-# ADR 0010: External AI Guardian Review As A First-Class Approval Authority\n+# SADR-0010: External AI Guardian Review As A First-Class Approval Authority\n \n ## Status\n \n@@ -8,7 +8,7 @@\n \n Shiki is intended to be an autonomous, recursive, self-improving agent\n framework: authority-in-the-loop, not human-in-the-loop. Until now the Guardian\n-approval required for high/critical-risk merges (guardian-policy.json, ADR\n+approval required for high/critical-risk merges (guardian-policy.json, SADR\n governance) recognized only human authority sources \u2014 a GitHub review approval,\n the `guardian:approved` label applied by a configured Guardian, or a comment\n carrying `Guardian approval granted` plus the current head SHA.\n@@ -89,7 +89,7 @@\n   autonomous design the operator intends and produced the impersonation\n   incident.\n - **Record AI approval as an operator comment**: rejected as forgery of human\n-  authority \u2014 the exact failure this ADR corrects.\n+  authority \u2014 the exact failure this SADR corrects.\n - **Deliver the artifact as a committed `.shiki/` file**: rejected; a committed\n   approval inside the PR being approved is forgeable by anyone who can push.\n - **A dedicated reviewer bot identity instead of a relayed comment**: deferred;\ndiff --git a/docs/adr/0011-autonomous-quality-gate-evidence.md b/docs/adr/SADR-0011-autonomous-quality-gate-evidence.md\nindex bca0246c5d46e44afbde15486a75eec425fc02f5..9c93eac16d8c3a6d4b11bd7224e3955102cd7db4\n--- a/docs/adr/0011-autonomous-quality-gate-evidence.md\n+++ b/docs/adr/SADR-0011-autonomous-quality-gate-evidence.md\n@@ -1,4 +1,4 @@\n-# ADR 0011: Autonomous Quality-Gate Evidence \u2014 Deterministic Facts And An Independent Verifier\n+# SADR-0011: Autonomous Quality-Gate Evidence \u2014 Deterministic Facts And An Independent Verifier\n \n ## Status\n \ndiff --git a/docs/adr/0012-autonomous-loop-closeout-to-main.md b/docs/adr/SADR-0012-autonomous-loop-closeout-to-main.md\nindex 637fad6dde4989c9c2bf0f81f676d8d29991123c..ec7d85f81030a676bcbba28b43eb365422a36eac\n--- a/docs/adr/0012-autonomous-loop-closeout-to-main.md\n+++ b/docs/adr/SADR-0012-autonomous-loop-closeout-to-main.md\n@@ -1,4 +1,4 @@\n-# 0012 \u2014 Autonomous loop pushes goal completion to main via a closeout PR\n+# SADR-0012 \u2014 Autonomous loop pushes goal completion to main via a closeout PR\n \n - Status: accepted\n - Date: 2026-06-18\ndiff --git a/docs/adr/0013-bootstrap-grants-actions-approve-pull-requests.md b/docs/adr/SADR-0013-bootstrap-grants-actions-approve-pull-requests.md\nindex b6c69d64f1c5ced346b88d6859586715b3b5bd0e..cf48ff4a6716267f539ca58edd1a852e7c7a2d64\n--- a/docs/adr/0013-bootstrap-grants-actions-approve-pull-requests.md\n+++ b/docs/adr/SADR-0013-bootstrap-grants-actions-approve-pull-requests.md\n@@ -1,4 +1,4 @@\n-# ADR 0013: Bootstrap Grants GitHub Actions Permission To Create And Approve Pull Requests\n+# SADR-0013: Bootstrap Grants GitHub Actions Permission To Create And Approve Pull Requests\n \n ## Status\n \n@@ -31,7 +31,7 @@\n security tradeoff: it lets repository automation satisfy a human-review gate. In\n a multi-maintainer repo that could let a compromised or buggy workflow\n self-approve changes. So whether bootstrap turns it on by default \u2014 and with\n-what default token scope \u2014 is an ADR-worthy decision, not an implementation\n+what default token scope \u2014 is a SADR-worthy decision, not an implementation\n detail.\n \n ## Decision\n@@ -48,7 +48,7 @@\n Specifically:\n \n - **`can_approve_pull_request_reviews` defaults to `true`.** Shiki's default\n-  posture is solo/AFK autonomous operation (ADR 0008/0009/0012), where the CCA\n+  posture is solo/AFK autonomous operation (SADR-0008/0009/0012), where the CCA\n   Review Bridge is the only path that can satisfy `required_review: true` after\n   the CCA and Guardian gates have already passed. The default makes a\n   freshly-bootstrapped repo able to run the autonomous loop end to end without a\ndiff --git a/docs/adr/0014-codex-app-external-ai-guardian-ui-adapter.md b/docs/adr/SADR-0014-codex-app-external-ai-guardian-ui-adapter.md\nindex 238db06279334caa341c117362df9271382a7fcd..65f12e9939321e1b3e389a34c822fc1549e5155d\n--- a/docs/adr/0014-codex-app-external-ai-guardian-ui-adapter.md\n+++ b/docs/adr/SADR-0014-codex-app-external-ai-guardian-ui-adapter.md\n@@ -1,4 +1,4 @@\n-# ADR 0014: Use Codex App As The External AI Guardian UI Adapter\n+# SADR-0014: Use Codex App As The External AI Guardian UI Adapter\n \n ## Status\n \ndiff --git a/docs/adr/0015-contract-approval-before-dispatch.md b/docs/adr/SADR-0015-contract-approval-before-dispatch.md\nindex fad6c656741a20a7684ba16080a7db159f63a52f..6061271a46d3f1d6ae7174477de3fb75673123a6\n--- a/docs/adr/0015-contract-approval-before-dispatch.md\n+++ b/docs/adr/SADR-0015-contract-approval-before-dispatch.md\n@@ -1,4 +1,4 @@\n-# ADR 0015: Approve The Task Contract Before Dispatch, Not The Diff After It\n+# SADR-0015: Approve The Task Contract Before Dispatch, Not The Diff After It\n \n ## Status\n \n@@ -9,7 +9,7 @@\n Guardian approval for high/critical risk is expressible only on a pull request.\n Every approval source in `guardian-policy.json` \u2014 GitHub review, the\n `guardian:approved` label, the `Guardian approval granted` comment, and the\n-`external_ai_guardian_review` artifact (ADR 0010/0014) \u2014 is bound to a PR, and\n+`external_ai_guardian_review` artifact (SADR-0010/0014) \u2014 is bound to a PR, and\n three of the four require the current head SHA. Approval therefore cannot exist\n before an implementation PR exists.\n \n@@ -81,7 +81,7 @@\n An envelope breach must surface only on the non-repairable policy gate, never on\n the repairable `MergeGate metadata check`. Otherwise the Goal Loop routes it to\n `dispatch_repair` \u2014 instructing a runner to make the breach go away \u2014 which is\n-exactly what this ADR forbids.\n+exactly what this SADR forbids.\n \n **Contract immutability.** After registration, an implementation PR must not\n change the governance fields of its own task contract (`scope`, `non_goals`,\n@@ -116,7 +116,7 @@\n fails any verdict that is not `complete`, and the signal the CCA prompt reads is\n computed from `evaluate_guardian_approval` alone. Without the OR-merge at the\n signal, a contract-approved PR still returns `needs_guardian`, the CCA job fails,\n-and the MergeGate policy check never runs \u2014 the gate this ADR changes would never\n+and the MergeGate policy check never runs \u2014 the gate this SADR changes would never\n be reached. The CCA *prompt* needs no change: a `contract_approval` entry in the\n signal's existing `sources` array satisfies its current wording.\n \n@@ -130,7 +130,7 @@\n written, and an approved contract lets its implementation PRs merge through the\n same automatic path as low/medium risk. The wasted implement-then-block cycle\n disappears. Guardian review shifts from reading diffs to reading contracts, which\n-is both earlier and smaller; the external AI Guardian path (ADR 0010/0014) works\n+is both earlier and smaller; the external AI Guardian path (SADR-0010/0014) works\n on contracts with no change.\n \n We accept that the Guardian authorizes a class of change without seeing its\n@@ -146,7 +146,7 @@\n adversarial tests for the new approval path equivalent in rigor to\n `test_shiki_governance_evidence.sh`.\n \n-Bootstrap costs, which apply only while this ADR is being implemented. The tasks\n+Bootstrap costs, which apply only while this SADR is being implemented. The tasks\n that build Contract Approval merge under the existing rules, so the ones\n dispatched after the pre-dispatch gate lands but before the approval path lands\n need a Guardian-approved Contract PR *and* a live Guardian approval on their\n@@ -210,7 +210,7 @@\n risk; they do not bound authorized work.\n \n **Auto-escalate risk on envelope breach instead of stopping.** Returns the\n-workflow to a diff-time Guardian decision \u2014 the exact behavior this ADR removes.\n+workflow to a diff-time Guardian decision \u2014 the exact behavior this SADR removes.\n \n **Resolve governance fields from the base snapshot only.** Rejected against the\n registration flow: implementation PRs add their own contract, so the base has no\ndiff --git a/docs/adr/0016-locks-bound-product-paths.md b/docs/adr/SADR-0016-locks-bound-product-paths.md\nindex b5e850f3f1f7f4ef6e7f9d1763a29a51bd300c45..46b40a9b99a7aae644c02bb458f5473ff0a90ce2\n--- a/docs/adr/0016-locks-bound-product-paths.md\n+++ b/docs/adr/SADR-0016-locks-bound-product-paths.md\n@@ -1,4 +1,4 @@\n-# ADR 0016: Locks bound product paths; the Shiki mirror is bound by record identity\n+# SADR-0016: Locks bound product paths; the Shiki mirror is bound by record identity\n \n ## Status\n \n@@ -6,8 +6,8 @@\n \n - Date: 2026-07-28\n - Deciders: operator (mizutani-140), Claude (planner)\n-- Related: ADR 0004 (GitHub-first with a `.shiki` mirror), ADR 0009 (Spec Freeze),\n-  ADR 0012 (autonomous loop closeout to main), ADR 0015 (Contract Approval before\n+- Related: SADR-0004 (GitHub-first with a `.shiki` mirror), SADR-0009 (Spec Freeze),\n+  SADR-0012 (autonomous loop closeout to main), SADR-0015 (Contract Approval before\n   dispatch), remediation goal task R-02, the live PR #179 deadlock.\n \n ## Context\n@@ -90,11 +90,11 @@\n files, documentation files and named source modules are nameable; runtime-\n allocated ledger, runner and report ids are not \u2014 and those live in `.shiki`,\n which locks no longer govern. Existing frozen contracts are brought into line by\n-a one-time Spec Amendment per plan, which must land before ADR 0015's contract\n+a one-time Spec Amendment per plan, which must land before SADR-0015's contract\n immutability makes `locks` unwritable from the implementation PR.\n \n **Lock state is resolved from the durable side.** MergeGate resolves the lock\n-registry, and ADR 0015's governance fields, from the base `.shiki` snapshot when\n+registry, and SADR-0015's governance fields, from the base `.shiki` snapshot when\n present, falling back to head only when absent. The `MergeGate metadata check`\n job is given `--base-shiki`, which it does not receive today. Locks are not\n derived from the live open-pull-request set: a pull request body is untrusted\n@@ -108,7 +108,7 @@\n Explicitly out of scope: moving MergeGate's contract resolution off the PR\n checkout wholesale; making the mirror a projection of GitHub state; introducing\n new lock kinds; moving ledger evidence out of the repository; and unwinding\n-ADR 0012's closeout pull request. Registration of multi-task goals is ADR 0015's\n+SADR-0012's closeout pull request. Registration of multi-task goals is SADR-0015's\n Contract PR and is not re-decided here.\n \n ## Consequences\n@@ -127,7 +127,7 @@\n \n Risks accepted. First, goal-scoped identity is only as strong as `goal_id`,\n which is resolved from the PR body and the head task file; the mitigation is to\n-add `goal_id` to ADR 0015's frozen governance field set, and until that lands a\n+add `goal_id` to SADR-0015's frozen governance field set, and until that lands a\n task file can name a foreign goal. Second, base-when-present resolution is\n unavailable on fork pull requests, where the head is contributor-controlled;\n fork pull requests already cannot complete a Shiki merge, and this decision does\n@@ -139,7 +139,7 @@\n path, not by implementation pull requests.\n \n New obligations: one Spec Amendment per frozen plan to rewrite `locks`, ordered\n-before ADR 0015 T1; extension of the adversarial suites in\n+before SADR-0015 T1; extension of the adversarial suites in\n `scripts/test_shiki_adversarial_state.sh` and\n `scripts/test_shiki_governance_evidence.sh` to cover the eleven newly-scoped\n directories; and updating `scripts/test_shiki_loop_lock_guard.sh`,\n@@ -151,32 +151,32 @@\n whether the loop's transitive sync of ledger `evidence` references can be\n deleted once the Contract PR carries its own source plan.\n \n-## Relationship to ADR 0004\n+## Relationship to SADR-0004\n \n-This ADR **clarifies something ADR 0004 left unspecified**. ADR 0004 decides that\n+This SADR **clarifies something SADR-0004 left unspecified**. SADR-0004 decides that\n GitHub is the operational source of truth and that each target repository keeps a\n `.shiki/` mirror. It says nothing about pull requests writing the mirror, does\n not name MergeGate, and does not make the mirror an input to a gate. Nothing in\n-ADR 0004's Decision or Consequences is amended, superseded or weakened: the\n+SADR-0004's Decision or Consequences is amended, superseded or weakened: the\n mirror still exists, still records the same artifacts, and pull request\n-boundaries remain the coordination surface. What this ADR settles is the question\n+boundaries remain the coordination surface. What this SADR settles is the question\n 0004 never reached \u2014 by what authority a pull request may write a mirror record \u2014\n and it answers it the way `.shiki/manifest.json` already declares\n (`pr_mutation: current-task-or-current-goal-only`).\n \n-It **amends ADR 0012**, whose Decision item 5 states that \"Loop-executed tasks\n+It **amends SADR-0012**, whose Decision item 5 states that \"Loop-executed tasks\n declare the `path:.shiki/**` lock, which covers every `.shiki` file the closeout\n stages ... so `files_outside_locks` is satisfied without per-file lock\n bookkeeping.\" That clause is replaced: the closeout's mirror writes are\n-authorized by record identity, not by a declared lock. ADR 0012's substantive\n+authorized by record identity, not by a declared lock. SADR-0012's substantive\n decision \u2014 that completion reaches `main` through a closeout pull request rather\n than local attestation \u2014 is preserved unchanged.\n \n-It is **complementary to ADR 0015**, not a substitute. ADR 0015 owns contract\n+It is **complementary to SADR-0015**, not a substitute. SADR-0015 owns contract\n immutability, the pre-dispatch gate, the Contract PR and the Scope Envelope. This\n-ADR owns what a lock means and who may write the mirror. The one ordering\n+SADR owns what a lock means and who may write the mirror. The one ordering\n constraint between them is that the lock-narrowing Spec Amendment must precede\n-ADR 0015 T1's freezing of the `locks` field.\n+SADR-0015 T1's freezing of the `locks` field.\n \n ## Alternatives Considered\n \n@@ -186,7 +186,7 @@\n the only writer that can register them may run. Migrating the Guardian signal to\n that store makes every registration PR fail closed to `needs_guardian` at every\n risk level, because the task file is by definition absent from the store, which\n-removes the low/medium autonomous path ADR 0008/0009 exists to provide. And it\n+removes the low/medium autonomous path SADR-0008/0009 exists to provide. And it\n leaves schemas, manifest, config, guardian policy, templates, migrations, plans,\n worktrees, runner records, runs and handoffs with no writer at all in a\n repository whose product is `.shiki`. Measured benefit on the frozen backlog:\n@@ -199,14 +199,14 @@\n namespace is not in fact ownership-partitioned \u2014 56 ledger entries carry no\n `task_id`, and one goal DAG is writable by 35 registered tasks. The correct\n expression of its insight is that locks do not govern the mirror at all, which is\n-what this ADR decides.\n+what this SADR decides.\n \n **Make the mirror a projection written only by a registration writer and a\n post-merge projection job.** Rejected. It requires a bypass actor with write\n access to the protected default branch; it makes its own master invariant\n readable from a manifest that pull requests may edit; it deletes both reconcile\n modes and leaves a corrupted mirror with no in-band repair path, contradicting\n-ADR 0004's consequence that mirror conflicts are repaired against GitHub state;\n+SADR-0004's consequence that mirror conflicts are repaired against GitHub state;\n and its content-derived ids are rejected by `validate_shiki`'s currently-green id\n pattern.\n \ndiff --git a/docs/adr/0017-bookkeeping-closeouts-do-not-inherit-risk.md b/docs/adr/SADR-0017-bookkeeping-closeouts-do-not-inherit-risk.md\nindex 554e3a0d526a72ca067108c5464cc68c3edcf174..b9676857a4713850e063af7c39c8e0b01ecb4c66\n--- a/docs/adr/0017-bookkeeping-closeouts-do-not-inherit-risk.md\n+++ b/docs/adr/SADR-0017-bookkeeping-closeouts-do-not-inherit-risk.md\n@@ -1,4 +1,4 @@\n-# ADR 0017: A Bookkeeping Closeout Does Not Inherit Its Task's Risk\n+# SADR-0017: A Bookkeeping Closeout Does Not Inherit Its Task's Risk\n \n ## Status\n \n@@ -6,7 +6,7 @@\n \n ## Context\n \n-ADR 0012 made the goal loop push a task's completion to the default branch through a\n+SADR-0012 made the goal loop push a task's completion to the default branch through a\n closeout pull request: after the implementation PR merges, the loop opens a second PR\n carrying `task.status = done`, `lock.state = released`, and \u2014 when the task completes\n its goal \u2014 `goal.status = complete` plus one scorecard report. Nothing else. The\n@@ -49,14 +49,14 @@\n 4. The task file's governance fields (`scope`, `non_goals`, `required_skills`,\n    `risk_level`, `locks`, `acceptance_checks`, `test_command`) are byte-identical to\n    the base snapshot. Only `status`, `expected_pr`, `closeout_pr`, `ledger_evidence`\n-   and `expected_branch` may differ \u2014 the ADR 0015 contract-immutability rule,\n+   and `expected_branch` may differ \u2014 the SADR-0015 contract-immutability rule,\n    unchanged.\n \n    `expected_branch` is in that set because a closeout is *by construction* on a\n    different branch from the implementation it closes, and the loop rewrites the\n    field accordingly. It is safe to let it differ because MergeGate independently\n    requires `expected_branch` to equal the PR's actual head ref, so a PR cannot set\n-   it to anything other than the branch it is really on. The first draft of this ADR\n+   it to anything other than the branch it is really on. The first draft of this SADR\n    omitted it, which made the exemption unreachable: every real closeout failed\n    condition 4 on a field the loop always changes. See PR #209.\n 5. The state transitions are exactly the terminal set: task `review` \u2192 `done`, lock\n@@ -113,12 +113,12 @@\n classification must be derived, not asserted.\n \n **Fold the closeout into the implementation PR** so no second PR exists. Rejected under\n-ADR 0012, which recorded why: a task file that is already `done` on the implementation\n+SADR-0012, which recorded why: a task file that is already `done` on the implementation\n branch short-circuits the loop's review \u2192 CCA \u2192 merge \u2192 done ordering into a local\n goal-completion before any merge happens.\n \n **Have the loop auto-merge closeouts with elevated credentials.** Rejected outright: that\n-is a runtime authoring its own approval, which ADR 0010 and the constitution's\n+is a runtime authoring its own approval, which SADR-0010 and the constitution's\n approval-record prohibition forbid. It would also be indistinguishable, in the evidence,\n from the forgery those rules exist to prevent.\n \ndiff --git a/docs/adr/0018-carried-guardian-approval-across-a-proven-base-sync.md b/docs/adr/SADR-0018-carried-guardian-approval-across-a-proven-base-sync.md\nindex 22358562da390ed8b45cd600da08a43d378b4680..e6e2907cefa776460af73ddf7d39db605f3c39b0\n--- a/docs/adr/0018-carried-guardian-approval-across-a-proven-base-sync.md\n+++ b/docs/adr/SADR-0018-carried-guardian-approval-across-a-proven-base-sync.md\n@@ -1,4 +1,4 @@\n-# ADR 0018: Carried Guardian Approval Across A Proven Base Sync\n+# SADR-0018: Carried Guardian Approval Across A Proven Base Sync\n \n ## Status\n \n@@ -34,7 +34,7 @@\n Guardian evaluator that consumes it (`shiki_guardian.evaluate_guardian_approval`,\n the `guardian_comment_carried` source) were built and tested in isolation. Neither\n does anything until a caller passes the flags that turn the carry on, so the\n-feature was invisible in CI. This ADR records the decision the two of them\n+feature was invisible in CI. This SADR records the decision the two of them\n implement, and this task wires it into the four CI invocations that judge Guardian\n approval.\n \ndiff --git a/docs/adr/README.md b/docs/adr/README.md\nindex c70e057a4520c36e042ab68c53cd4a21ef709b71..5da43856836e2ce6a150f8127766e2f88355ab4b\n--- a/docs/adr/README.md\n+++ b/docs/adr/README.md\n@@ -1,40 +1,41 @@\n-# Architecture Decision Records\n+# Decision Record Namespaces\n \n-This directory holds Shiki's Architecture Decision Records (ADRs): durable\n-records of hard-to-reverse platform decisions. ADRs are part of the Shiki source\n-of truth alongside GitHub state, the `.shiki/` mirror, and `CONTEXT.md`.\n+This directory holds two independent decision namespaces:\n \n-## How ADRs Work\n+- Shiki Architecture Decision Records (SADRs), named\n+  `SADR-NNNN-<short-slug>.md`, record hard-to-reverse Shiki platform decisions.\n+- Target Architecture Decision Records (ADRs), named\n+  `NNNN-<short-slug>.md`, belong to the target product.\n \n-- New ADRs are added as `docs/adr/NNNN-<short-slug>.md`, where `NNNN` is the next\n-  zero-padded sequence number.\n-- The **canonical list of ADRs is the set of files matching `docs/adr/*.md`** in\n-  this directory. Browse `docs/adr/*.md` for the authoritative, current set.\n-- Use [`template.md`](template.md) as the starting point for a new ADR.\n-- Record an ADR when a decision is hard to reverse, surprising without context,\n-  and a real tradeoff. Do not write an ADR for routine or easily reversible\n-  choices.\n+The same four-digit number may appear once in each namespace. A platform\n+`SADR-NNNN` citation resolves only to `SADR-NNNN-*.md`; a target `ADR NNNN`\n+citation resolves only to `NNNN-*.md`.\n \n-Using the `docs/adr/*.md` glob as the source of truth means other PRs can add an\n-ADR without editing this index file, avoiding merge contention on a\n-hand-maintained enumeration.\n+## How Decision Records Work\n \n-## Current ADRs (snapshot)\n-\n-The list below is a convenience snapshot and may lag behind the directory. When\n-in doubt, list `docs/adr/*.md`.\n+- New Shiki platform decisions use the next zero-padded SADR number.\n+- New target product decisions use the target's own ADR sequence.\n+- The canonical list is the files in `docs/adr/`; no hand-maintained index is\n+  authoritative.\n+- Use [`template.md`](template.md) as the starting point and choose the filename\n+  namespace that owns the decision.\n+- Record a decision when it is hard to reverse, surprising without context, and\n+  a real tradeoff. Do not write a decision record for routine or easily\n+  reversible choices.\n \n-- [0001 \u2014 Keep Shiki As The Platform Name](0001-keep-shiki-as-platform-name.md)\n-- [0002 \u2014 Use AGENTS As The Shared Agent Constitution](0002-use-agents-as-shared-constitution.md)\n-- [0003 \u2014 Require Skill Gate For MergeGate Readiness](0003-require-skill-gate-for-mergegate-readiness.md)\n-- [0004 \u2014 Use GitHub First With A Repository-Local Shiki Mirror](0004-use-github-first-with-shiki-mirror.md)\n-- [0005 \u2014 Use Codex As Front And Claude Code Action As The GitHub Runtime](0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md)\n-- [0006 \u2014 Separate Codex Implementation, CCA Completion, And MergeGate Authorization](0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md)\n+Using the directory as the source of truth lets independent changes add records\n+without contending on an enumerated index.\n \n-## Adding A New ADR\n+## Adding A Shiki SADR\n \n-1. Copy `template.md` to `docs/adr/NNNN-<short-slug>.md`.\n+1. Copy `template.md` to `docs/adr/SADR-NNNN-<short-slug>.md`.\n 2. Fill in the title, status, context, decision, and consequences.\n-3. Set the status to `Proposed`, then `Accepted` once the decision is approved.\n-4. Open a PR following `CONTRIBUTING.md`. You do not need to edit this index for\n-   the ADR to be discoverable via `docs/adr/*.md`.\n+3. Set the status to `Proposed`, then `Accepted` once the proper authority has\n+   approved the decision.\n+4. Open a PR following `CONTRIBUTING.md`.\n+\n+## Adding A Target ADR\n+\n+1. Copy `template.md` to `docs/adr/NNNN-<short-slug>.md` using the target's own\n+   sequence.\n+2. Cite it as `ADR NNNN`; do not prefix its filename with `SADR-`.\ndiff --git a/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md b/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\nnew file mode 100644\nindex 0000000000000000000000000000000000000000..7455ce1f3bed02ddddb851ac21a1d1a648c9aeba\n--- /dev/null\n+++ b/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\n@@ -0,0 +1,55 @@\n+# SADR-0019: Separate Shiki Platform SADRs From Target ADRs\n+\n+## Status\n+\n+Accepted\n+\n+## Context\n+\n+Shiki installs its platform decision records into `docs/adr/` in every target\n+repository. Shiki and its targets previously used the same numeric\n+`NNNN-<slug>.md` namespace. A target that already owned ADRs 0013 through 0018\n+therefore acquired two records for each number during installation, making an\n+`ADR NNNN` citation ambiguous. Renumbering target decisions would rewrite target\n+history, while renumbering Shiki decisions per target would destroy their stable\n+platform identity.\n+\n+Upgrades also need to remove the former Shiki-owned numeric paths without ever\n+mistaking a target-owned ADR for platform content. The install stamp already\n+records exact shipped paths and content digests, so it can prove ownership only\n+when both still match.\n+\n+## Decision\n+\n+Shiki platform decisions use the `SADR` namespace:\n+\n+- records are named `docs/adr/SADR-NNNN-<slug>.md`;\n+- live platform citations use `SADR-NNNN`;\n+- target product decisions retain `docs/adr/NNNN-<slug>.md` and `ADR NNNN`;\n+- the reference validator resolves the two namespaces independently, so the same\n+  four-digit number may exist once in each namespace.\n+\n+Before a forced upgrade writes anything, the installer inventories the exact\n+former numeric Shiki ADR paths. It may delete a legacy path only when the\n+existing install stamp contains that exact path and its recorded SHA-256 digest\n+matches the file currently on disk. All ownership blockers are reported\n+together and abort the upgrade before its first write. A non-forced install\n+never performs this cleanup.\n+\n+Historical `.shiki/` evidence remains unchanged because it records the namespace\n+that existed when that evidence was produced.\n+\n+## Consequences\n+\n+- Targets may use any ADR number without colliding with Shiki's platform SADRs.\n+- Fresh installs preserve target ADR bytes and install a stable SADR set.\n+- Forced upgrades fail closed when legacy ownership cannot be proven, and a\n+  successful upgrade refreshes the stamp without removed legacy paths.\n+- Current governance, packaging, workflow, prompt, script, and skill references\n+  must move with the platform records.\n+\n+## Alternatives Considered\n+\n+- Collision-only refusal leaves affected targets permanently unupgradable.\n+- Renumbering target ADRs mutates target-owned history.\n+- Per-target Shiki renumbering makes platform citations non-portable.\ndiff --git a/docs/adr/template.md b/docs/adr/template.md\nindex a92a97daddd0b00ab9dd626cc8d86942da2eafed..17dd79175b5fee2105935557f691b40cf9492942\n--- a/docs/adr/template.md\n+++ b/docs/adr/template.md\n@@ -1,14 +1,14 @@\n-# NNNN: <Short Decision Title>\n+# <SADR-NNNN or NNNN>: <Short Decision Title>\n \n ## Status\n \n-Proposed | Accepted | Superseded by ADR-NNNN\n+Proposed | Accepted | Superseded by SADR-NNNN or target ADR NNNN\n \n ## Context\n \n What is the situation, constraint, or force that requires a decision? Reference\n-relevant domain terms (`CONTEXT.md`), prior ADRs, and code reality. State the\n-problem, not the solution.\n+relevant domain terms (`CONTEXT.md`), prior Shiki SADRs or target ADRs, and code\n+reality. State the problem, not the solution.\n \n ## Decision\n \ndiff --git a/docs/agents/autonomous-loop-e2e-acceptance.md b/docs/agents/autonomous-loop-e2e-acceptance.md\nindex bbf84c6081987f159ac6ad9ecf3c82b7aa72e7e8..5c13c1d02a05a90b0dc46cbef7c95a9846d0c237\n--- a/docs/agents/autonomous-loop-e2e-acceptance.md\n+++ b/docs/agents/autonomous-loop-e2e-acceptance.md\n@@ -40,8 +40,8 @@\n 3. `gh auth status` is logged in with merge permission on the repo.\n 4. Branch protection + required checks are configured exactly as the goal-loop\n    required-check set expects (see `docs/agents/branch-protection-smoke.md`).\n-5. The repo `.shiki/policy` permits autonomous low/medium-risk merges (ADR\n-   0008/0009); the MergeGate policy check is the recorded-authority gate.\n+5. The repo `.shiki/policy` permits autonomous low/medium-risk merges\n+   (SADR-0008/0009); the MergeGate policy check is the recorded-authority gate.\n \n The operator's only actions are: register the task and start the loop. **No\n manual pre-clearing** means after `shiki loop run` starts, the operator does not\ndiff --git a/docs/agents/bootstrap-command.md b/docs/agents/bootstrap-command.md\nindex 05f7f203a52cf412b9f97084a6e131ed0f785afd..f7aa0b734dfebee4a3b139335b66ee3415f48713\n--- a/docs/agents/bootstrap-command.md\n+++ b/docs/agents/bootstrap-command.md\n@@ -189,7 +189,9 @@\n ```\n \n Use this only for tests, fixtures, or explicit local-only template inspection.\n-Use `--force` only when you intentionally want to overwrite existing target files.\n+Use `--force` only when you intentionally want to refresh the target template.\n+Legacy numeric Shiki decision cleanup is stamp-and-digest authorized before any\n+write; target ADR paths are not cleanup candidates.\n \n ## Slash Command\n \ndiff --git a/docs/agents/checklists.md b/docs/agents/checklists.md\nindex 4a7f03ade346f53a1c1d82981f3fd744f9ab4547..0c5ec6c53dc7197b7b7a7d18e667b325babd6c45\n--- a/docs/agents/checklists.md\n+++ b/docs/agents/checklists.md\n@@ -32,18 +32,18 @@\n | ID | Item | Blocking |\n | --- | --- | --- |\n | GD-01 | Plan was challenged against `CONTEXT.md`. | blocking for non-trivial Goals |\n-| GD-02 | Relevant ADRs were read or absence was recorded. | blocking for non-trivial Goals |\n+| GD-02 | Relevant SADRs and target ADRs were read or absence was recorded. | blocking for non-trivial Goals |\n | GD-03 | Ambiguous terms were resolved or converted to HITL blockers. | blocking |\n | GD-04 | Edge scenarios were considered. | advisory |\n | GD-05 | Code reality was checked when code could answer. | blocking when applicable |\n | GD-06 | New domain terms were added to `CONTEXT.md` when needed. | blocking when applicable |\n-| GD-07 | ADR was created or explicitly skipped using the ADR criteria. | blocking when applicable |\n+| GD-07 | A SADR or target ADR was created or explicitly skipped using the decision-record criteria. | blocking when applicable |\n \n ## CI \u2014 Context & Impact Checklist\n \n | ID | Item | Blocking |\n | --- | --- | --- |\n-| CI-01 | Relevant docs and ADRs identified. | blocking |\n+| CI-01 | Relevant docs, SADRs, and target ADRs identified. | blocking |\n | CI-02 | Relevant modules, interfaces, seams, callers, and tests identified. | blocking |\n | CI-03 | Dependencies and blocked-by relationships identified. | blocking |\n | CI-04 | Candidate locks identified. | blocking |\n@@ -62,7 +62,7 @@\n | PRD-04 | Implementation decisions are recorded without volatile code detail. | blocking |\n | PRD-05 | Testing decisions are recorded. | blocking |\n | PRD-06 | Out-of-scope items are explicit. | blocking |\n-| PRD-07 | Relevant glossary/ADR links are present when applicable. | blocking |\n+| PRD-07 | Relevant glossary/SADR/target-ADR links are present when applicable. | blocking |\n \n ## SF \u2014 Spec Freeze Checklist\n \n@@ -98,7 +98,7 @@\n | H-02 | Branch/worktree target is present. | blocking |\n | H-03 | Scope and non-goals are present. | blocking |\n | H-04 | Dependencies and locks are present. | blocking |\n-| H-05 | Relevant docs, ADRs, modules, and tests are referenced. | blocking |\n+| H-05 | Relevant docs, SADRs, target ADRs, modules, and tests are referenced. | blocking |\n | H-06 | Required skills are stated. | blocking |\n | H-07 | TDD expectations are stated. | blocking when behavior work |\n | H-08 | Acceptance criteria are included. | blocking |\ndiff --git a/docs/agents/codex-handoff.md b/docs/agents/codex-handoff.md\nindex ede468f47cb277f69a857e8c45df703e4ed0151d..6f97bdffb67e44d7edea2c71e01ea28933c4eb6c\n--- a/docs/agents/codex-handoff.md\n+++ b/docs/agents/codex-handoff.md\n@@ -46,7 +46,7 @@\n \n ## Context\n - Domain terms:\n-- Relevant ADRs:\n+- Relevant SADRs and target ADRs:\n - Relevant docs:\n - Relevant modules/interfaces/seams:\n - Similar tests/prior art:\ndiff --git a/docs/agents/completion-check-agent.md b/docs/agents/completion-check-agent.md\nindex 13e4c035db35025f4e3cdcc5adfb24314ac4d521..61bfa1218cf36ca605250c91919e620f33238aa0\n--- a/docs/agents/completion-check-agent.md\n+++ b/docs/agents/completion-check-agent.md\n@@ -32,7 +32,7 @@\n - `AGENTS.md`\n - `CLAUDE.md`\n - `CONTEXT.md`\n-- relevant ADRs\n+- relevant SADRs and target ADRs\n - parent Goal issue\n - PRD issue or document\n - task issue\ndiff --git a/docs/agents/decision-control.md b/docs/agents/decision-control.md\nindex 7d8ee5ab03a5ccbcbd04056ae61de03785a2a9c9..8c94ecda00fffaa0e92c325ab83aa9776bd1dac1\n--- a/docs/agents/decision-control.md\n+++ b/docs/agents/decision-control.md\n@@ -11,7 +11,7 @@\n | Decision | Owner | When | Required inputs | Output | Enforcement surface |\n | --- | --- | --- | --- | --- | --- |\n | Is the Goal clear enough to plan? | Planner + Guardian when needed | Goal intake | User request, existing docs, risk signals | Goal readiness verdict | Goal checklist, issue template, ledger |\n-| Must `grill-with-docs` run? | Planner / Skill Gate | Before PRD or issue split | Ambiguity, domain terms, ADR conflicts, unclear boundaries | Skill invocation or explicit skip rationale | Skill Gate checklist |\n+| Must `grill-with-docs` run? | Planner / Skill Gate | Before PRD or issue split | Ambiguity, domain terms, SADR/target-ADR conflicts, unclear boundaries | Skill invocation or explicit skip rationale | Skill Gate checklist |\n | Is a PRD required? | Planner | After Goal context stabilizes | Goal, `grill-with-docs` output, Context & Impact output | PRD required / not required | PRD checklist, CCA evidence check |\n | Can the spec be frozen? | Operator only | End of Requirements Definition, before issue split | PRD/requirements summary, scope inventory (SF-02), open-question list | `spec_freeze` block in the plan + ledger evidence | `require_grilled_plan` (plan ingest/run), SF checklist |\n | Can a frozen spec change? | Operator only (Spec Amendment) | When implementation contradicts the frozen spec | Contested decisions, scoped re-grill result | recorded amendment + re-stamped freeze | SF checklist, ledger evidence |\ndiff --git a/docs/agents/domain.md b/docs/agents/domain.md\nindex 258da8ea81e87ac0a6b0b5b5e5c2fde980eaabb7..d57b0cf17ff3e1fa9e6e5885372bf083d8295421\n--- a/docs/agents/domain.md\n+++ b/docs/agents/domain.md\n@@ -5,7 +5,9 @@\n ## Layout\n \n - `CONTEXT.md` defines Shiki language. Keep it implementation-free.\n-- `docs/adr/` records hard-to-reverse decisions.\n+- `docs/adr/SADR-NNNN-*.md` records Shiki platform decisions.\n+- `docs/adr/NNNN-*.md` records target product decisions without sharing the\n+  SADR namespace.\n - `AGENTS.md` is the runtime-neutral agent constitution.\n - `CLAUDE.md` is the Claude Code wrapper around `AGENTS.md`.\n - `.shiki/` is the Target Repository mirror for Goals, PRDs, plans, Task DAGs, contracts, locks, worktree records, ledger entries, CCA verdicts, repair packets, reports, and handoffs.\n@@ -15,6 +17,6 @@\n \n - Use Shiki terms exactly when they exist.\n - Add glossary terms to `CONTEXT.md` only when the term is part of the domain, not a generic programming concept.\n-- Create ADRs only for hard-to-reverse, surprising, tradeoff-based decisions.\n+- Create SADRs or target ADRs only for hard-to-reverse, surprising, tradeoff-based decisions.\n - Keep product-specific language in the Target Repository, not in Shiki Template docs.\n-- If an ADR conflicts with a proposed implementation, surface the conflict before editing.\n+- If a SADR or target ADR conflicts with a proposed implementation, surface the conflict before editing.\ndiff --git a/docs/agents/execution-control.md b/docs/agents/execution-control.md\nindex ad8ee3cdb36be56fedc1f46408ca92b387d74047..bc63c3a73661d12b567016ba299f1a6b66bf9088\n--- a/docs/agents/execution-control.md\n+++ b/docs/agents/execution-control.md\n@@ -63,7 +63,7 @@\n | Branch name | Orchestrator | Orchestrator | Before implementer dispatch | branch policy | branch ref in task/ledger |\n | Lock reservation | Context & Impact | MergeGate | Before dispatch and before merge | `.shiki/locks/`, labels, PR metadata | lock id, owner, scope |\n | CI profile | Planner | MergeGate | During issue triage, rechecked at PR | labels, `.shiki/policy.yaml`, workflow matrix | required check list |\n-| Required status checks | Repo maintainer / Guardian | GitHub branch protection / rulesets | Repository setup, updated via ADR | GitHub settings | branch/ruleset config, ADR |\n+| Required status checks | Repo maintainer / Guardian | GitHub branch protection / rulesets | Repository setup, updated via target ADR | GitHub settings | branch/ruleset config, target ADR |\n | CCA judgment | CCA | GitHub required check / MergeGate | Every non-draft PR update | GitHub Action check | structured CCA verdict |\n | Repair assignment | CCA | MergeGate | After failed CCA/CI/review | repair packet, issue/PR comment | repair id and attempt count |\n | Merge readiness | MergeGate | GitHub branch protection / Guardian if needed | After CCA complete | required checks, review rules, rulesets | mergegate check output |\ndiff --git a/docs/agents/guardian-policy.md b/docs/agents/guardian-policy.md\nindex 4147c7adf05a47adb4fd24ada1a1c2e1f2ba6d2d..a20d9cc6c00a569bb057b9ca8d6b51c7fc4a9847\n--- a/docs/agents/guardian-policy.md\n+++ b/docs/agents/guardian-policy.md\n@@ -98,8 +98,8 @@\n `origin/<default_branch>` and reuses each job's existing base fetch; a shallow\n (`--depth`) or second base fetch would make the proof unresolvable and silently\n disable the carry, so neither is permitted. CCA reads `guardian_comment_carried` as\n-a recorded authority in the deterministic signal and does not re-derive it. See ADR\n-0018 for the full decision, the rejected diff/patch-equality and path-disjointness\n+a recorded authority in the deterministic signal and does not re-derive it. See\n+SADR-0018 for the full decision, the rejected diff/patch-equality and path-disjointness\n alternatives, and the accepted residual.\n \n ## External AI Guardian Review (SADR-0010)\ndiff --git a/docs/agents/implementation-policy.md b/docs/agents/implementation-policy.md\nindex c754144a9fb5bf4a38aa82a60e1670c59e626b44..85c37281bcad196c6b7b02343faf00b03cd9c57a\n--- a/docs/agents/implementation-policy.md\n+++ b/docs/agents/implementation-policy.md\n@@ -31,10 +31,13 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n ## Non-Negotiables\n \n 1. **Planning precedes implementation.** Non-trivial Goals start with `grill-with-docs` and Context & Impact.\n-2. **Domain language is controlled.** Terms must be checked against `CONTEXT.md`; hard-to-reverse tradeoffs must be checked against ADRs.\n+2. **Domain language is controlled.** Terms must be checked against `CONTEXT.md`; hard-to-reverse tradeoffs must be checked against Shiki SADRs and target ADRs.\n 3. **PRD precedes issue decomposition.** `to-prd` is used once the context is settled enough to become durable intent.\n 4. **Issues are vertical slices.** `to-issues` must produce independently grabbable tracer-bullet tasks, not horizontal layer tickets.\n 5. **Triage controls dispatch.** Only `ready-for-agent` / AFK tasks may be assigned to Codex without human input.\n@@ -95,19 +98,19 @@\n Process:\n \n - Challenge terms against `CONTEXT.md`.\n-- Check ADRs before reopening decisions.\n+- Check SADRs and target ADRs before reopening decisions.\n - Inspect code when code can answer a question.\n - Ask one question at a time when the operator is available.\n - Provide a recommended answer with each question.\n - Record resolved terms in `CONTEXT.md`.\n-- Create ADRs only for hard-to-reverse, surprising, tradeoff-based decisions.\n+- Create SADRs or target ADRs only for hard-to-reverse, surprising, tradeoff-based decisions.\n - Convert unresolved load-bearing questions into HITL blockers.\n \n Outputs:\n \n - Resolved design-tree decisions.\n - Updated glossary terms when needed.\n-- ADRs when justified.\n+- SADRs or target ADRs when justified.\n - HITL blockers when decisions remain unresolved.\n \n ## Phase 3 \u2014 Context & Impact\n@@ -117,7 +120,7 @@\n \n Outputs must include:\n \n-- Relevant documents and ADRs.\n+- Relevant documents, SADRs, and target ADRs.\n - Relevant modules, interfaces, seams, callers, and tests.\n - Candidate locks.\n - Dependencies.\n@@ -140,7 +143,7 @@\n - Testing decisions.\n - Out of scope.\n - Further notes.\n-- Links to relevant Goal, domain terms, and ADRs.\n+- Links to relevant Goal, domain terms, SADRs, and target ADRs.\n \n Phase 4 ends with Spec Freeze: the operator explicitly approves the PRD, the\n plan records a `spec_freeze` block (status, approved_by, source), and the SF\n@@ -240,7 +243,7 @@\n - Dependencies and locks.\n - Risk and Guardian requirements.\n - Ledger completeness.\n-- Documentation and ADR updates.\n+- Documentation and SADR/target-ADR updates.\n - Scope drift.\n \n CCA emits one of:\n@@ -301,7 +304,7 @@\n | Evidence failure | Behavior might be done, but proof is missing. | Add checks, PR evidence, ledger entry, or artifacts. |\n | Scope drift | PR changes unrelated behavior. | Revert unrelated changes or split into new Goal. |\n | Dependency block | Upstream task or lock not complete. | Block until dependency resolves. |\n-| Architecture block | Decision needs ADR or deep-module discussion. | `grill-with-docs` or `improve-codebase-architecture`. |\n+| Architecture block | Decision needs a SADR, target ADR, or deep-module discussion. | `grill-with-docs` or `improve-codebase-architecture`. |\n | Risk block | Security/production/policy change needs human approval. | Guardian decision. |\n \n ## Phase 5.5 \u2014 Execution Decision Control\ndiff --git a/docs/agents/runtime-auth-model.md b/docs/agents/runtime-auth-model.md\nindex 6a2e1a47e81ea89b5c68a210ec07772a9374babc..3c54748d24233dbd0ba8473950282547ef4ce935\n--- a/docs/agents/runtime-auth-model.md\n+++ b/docs/agents/runtime-auth-model.md\n@@ -148,7 +148,7 @@\n \n API-key based runners may be added by a target repository as an explicit extension. They are not the default Shiki template path.\n \n-If a repo opts into API-key mode, record the exception in an ADR and update the repo's `AGENTS.md`, `.shiki/config.yaml`, and workflow permissions.\n+If a repo opts into API-key mode, record the target-specific exception in a target ADR and update the repo's `AGENTS.md`, `.shiki/config.yaml`, and workflow permissions.\n \n ## GitHub Actions Runtime Compatibility\n \ndiff --git a/docs/agents/skill-gate.md b/docs/agents/skill-gate.md\nindex c50398b0d7c60a8598f2a5c88a10015730dced13..3acffe6444617bb1d54b77d4b4708158ec735bc6\n--- a/docs/agents/skill-gate.md\n+++ b/docs/agents/skill-gate.md\n@@ -9,7 +9,7 @@\n | Skill | Trigger | Expected output |\n | --- | --- | --- |\n | `setup-matt-pocock-skills` | First configuring a repo for these skills, or when issue tracker, triage labels, or domain docs are missing. | `docs/agents/` configuration and an `Agent skills` block in `AGENTS.md` or `CLAUDE.md`. |\n-| `grill-with-docs` | Ambiguous plan, unclear terminology, boundary decision, tradeoff, or ADR-worthy design choice. | Clarified decision tree, recommended answers, updates to `CONTEXT.md` or ADRs when decisions crystallize. |\n+| `grill-with-docs` | Ambiguous plan, unclear terminology, boundary decision, tradeoff, or decision-record-worthy design choice. | Clarified decision tree, recommended answers, updates to `CONTEXT.md`, SADRs, or target ADRs when decisions crystallize. |\n | `zoom-out` | Agent lacks a higher-level architectural map. | Map of relevant modules, callers, seams, and domain concepts. |\n | `to-prd` | Settled context should become a PRD. | PRD published to the issue tracker with problem, solution, user stories, decisions, testing, and out-of-scope notes. |\n | `to-issues` | Goal, PRD, or plan must become implementation tickets. | Vertical-slice issues with acceptance criteria, dependencies, and AFK/HITL classification. |\n@@ -17,7 +17,7 @@\n | `tdd` | Feature work or bug fix has observable behavior that can be specified with tests. | Red-green-refactor loop through public interfaces, one vertical slice at a time. |\n | `code-review` | The TDD loop is green and the implementer is about to create or update a PR. Default for every implementation task. | Pre-PR self-review of the task diff with fixes applied on the task branch; findings and resolutions recorded as a ledger entry naming the skill and a `## Pre-PR code review` PR body section. |\n | `diagnose` | Hard bug, failing check, regression, flaky behavior, or performance problem. | Reproduction loop, ranked hypotheses, instrumentation, fix, regression test, post-mortem. |\n-| `improve-codebase-architecture` | Structural friction, testability, AI-navigability, deep-module opportunity, or refactor request. | Architecture candidates and deepening recommendations informed by `CONTEXT.md` and ADRs. |\n+| `improve-codebase-architecture` | Structural friction, testability, AI-navigability, deep-module opportunity, or refactor request. | Architecture candidates and deepening recommendations informed by `CONTEXT.md`, SADRs, and target ADRs. |\n | `prototype` | A throwaway logic or UI prototype is needed to answer a design question. | Clearly marked disposable prototype and captured decision. |\n \n ## MergeGate Interaction\ndiff --git a/docs/operations.md b/docs/operations.md\nindex cf1f452d4c589e38ccd58553bda0ad8684b51eb7..ace48a90ffbec9e5c0a7113bef16a4858bc44b3f\n--- a/docs/operations.md\n+++ b/docs/operations.md\n@@ -89,6 +89,16 @@\n      target history, brought forward by `shiki migrate`, not a template.\n    - **Everything else** (CLI scripts, workflows, schemas, docs) is overwritten.\n \n+   A forced upgrade also migrates the former numeric Shiki decision paths to\n+   `SADR-NNNN-*`. Before the first write, the installer inventories the exact\n+   legacy Shiki paths and authorizes each deletion only when the existing\n+   `.shiki/install-stamp.json` names that path and its SHA-256 digest still\n+   matches the file. An absent, unreadable, malformed, incomplete, or\n+   mismatching stamp reports every blocked path and stops with no filesystem\n+   change. Target-owned `NNNN-*` ADRs are not cleanup candidates. A non-force\n+   install deletes nothing and retains any still-matching legacy stamp entries\n+   so a later forced upgrade can prove ownership.\n+\n    If the target has pending migrations, `--force` refuses and writes nothing \u2014\n    apply migrations first (step 3), then re-run. The run ends with a single\n    summary of every `.new` written, so a half-upgrade is visible.\ndiff --git a/docs/releasing.md b/docs/releasing.md\nindex 10d73828e7dbd64e9d9f6b83bc84d010537ce62a..aafeee8c4d441e1e427efee2ce4f81a647f020e1\n--- a/docs/releasing.md\n+++ b/docs/releasing.md\n@@ -67,7 +67,7 @@\n CLI in `scripts/`. There are no third-party Python runtime dependencies. The CLI\n is intentionally **not** exposed as a `[project.scripts]` console entry point\n because `scripts/shiki.py` is a dependency-free shim and `scripts/` is not\n-packaged as an importable distribution (see the ADR for the rationale).\n+packaged as an importable distribution (see SADR-0007 for the rationale).\n \n Supported install paths:\n \n@@ -155,6 +155,15 @@\n    [docs/operations.md](operations.md#the-new-convention). If the target has\n    pending migrations, `--force` refuses and writes nothing; apply migrations\n    (step 2) first, then re-run.\n+\n+   The SADR namespace upgrade has an additional pre-write gate: each former\n+   numeric Shiki decision path is removed only when the existing install stamp\n+   contains that exact path and a digest matching its current bytes. The\n+   installer aggregates absent/unreadable/malformed-stamp, missing-digest, and\n+   digest-mismatch blockers before raising, so refusal leaves the target tree\n+   byte-identical. Numeric target ADRs are preserved. A successful forced\n+   upgrade installs and stamps `SADR-*` paths and drops the removed legacy paths\n+   from the refreshed stamp.\n 2. Apply repository-local `.shiki/` state migrations with the migrate command:\n \n    ```bash\ndiff --git a/docs/usage.md b/docs/usage.md\nindex b83407ee3970dd395dd893010bc5dbe8bad26051..a130ef6e40dfd8169a5bc701339351097df1a09f\n--- a/docs/usage.md\n+++ b/docs/usage.md\n@@ -67,8 +67,10 @@\n shiki install-target /path/to/target-repo --local-only\n ```\n \n-Use `--force` only when you intentionally want to overwrite existing target\n-files.\n+Use `--force` only when you intentionally want to refresh the target template.\n+For the SADR namespace migration, `--force` deletes only exact legacy Shiki\n+decision paths whose existing install-stamp digest still matches; any ownership\n+blocker stops before the first write. Numeric target ADRs are never candidates.\n \n ## `shiki bootstrap-platform` \u2014 publish the Shiki platform repo\n \ndiff --git a/scripts/guardian_approval_signal.py b/scripts/guardian_approval_signal.py\nindex d059f92b2c82aa2b5d6ca421365537a87886c1cb..16924892714f4f5b9303c0ef0e325a7e70075630\n--- a/scripts/guardian_approval_signal.py\n+++ b/scripts/guardian_approval_signal.py\n@@ -199,8 +199,8 @@\n ) -> Any:\n     \"\"\"Evaluate SADR-0015 Contract Approval for the CCA Guardian signal.\n \n-    The SAME OR that MergeGate applies must reach this deterministic signal: ADR\n-    0015 is explicit that without it, a contract-approved PR still returns\n+    The SAME OR that MergeGate applies must reach this deterministic signal:\n+    SADR-0015 is explicit that without it, a contract-approved PR still returns\n     ``needs_guardian``, the CCA job fails, and the MergeGate policy check never\n     runs \u2014 the gate this change moves would never be reached. This resolves the\n     task the SAME way MergeGate does (the first Shiki task id in the PR body),\ndiff --git a/scripts/shiki_contract_approval.py b/scripts/shiki_contract_approval.py\nindex 941bcb96fdd8b9a85bc0c4f0f04d5dad9d04839c..4a4b418f6527413161db82670bec857a043360e2\n--- a/scripts/shiki_contract_approval.py\n+++ b/scripts/shiki_contract_approval.py\n@@ -142,7 +142,7 @@\n     changed_files_status: Any,\n     registration: Any,\n ) -> ContractApprovalResult:\n-    \"\"\"Whether this PR carries an SADR-0015 Contract Approval.\n+    \"\"\"Whether this PR carries a SADR-0015 Contract Approval.\n \n     The four conditions are evaluated IN ORDER, each failure recorded as a\n     distinct reason string:\ndiff --git a/scripts/shiki_guardian.py b/scripts/shiki_guardian.py\nindex a4ecb79bbda55fd5c8b06adfae2eb99760a03c45..4dd99bbe714e26f421fa19abac7a34ae1b195ecc\n--- a/scripts/shiki_guardian.py\n+++ b/scripts/shiki_guardian.py\n@@ -12,7 +12,7 @@\n # The carry re-derives its proof from git in-process on every judging invocation;\n # it is never read from an artifact, so there is nothing to go stale or be forged.\n # shiki_sync_proof is the ONLY guardian-family module that runs git, and every one\n-# of its invocations is read-only and local (ADR: prove-from-git base sync).\n+# of its invocations is read-only and local (SADR: prove-from-git base sync).\n from shiki_sync_proof import verify_pure_base_sync\n \n GUARDIAN_POLICY_PATH = \".shiki/guardian-policy.json\"\ndiff --git a/scripts/shiki_guardian_review.py b/scripts/shiki_guardian_review.py\nindex dd0f104d457899e0ba5ff1e162011c1e90b6f92b..e10ef58c046324e7ea3a3392feb3048ca77414ce\n--- a/scripts/shiki_guardian_review.py\n+++ b/scripts/shiki_guardian_review.py\n@@ -159,7 +159,7 @@\n     docs_only = _is_docs_only(changed_files)\n     if docs_only and matched == 0:\n         focus.append(\n-            \"Docs/ADR-only change: confirm the change is genuinely documentation/decision-only, introduces no behavior or contract drift, and stays consistent with the constitution and existing ADRs.\"\n+            \"Docs/decision-record-only change: confirm the change is genuinely documentation/decision-only, introduces no behavior or contract drift, and stays consistent with the constitution and existing SADRs and target ADRs.\"\n         )\n \n     # Conservative augmentation: unknown (no category and not docs-only), mixed\ndiff --git a/scripts/shiki_installer.py b/scripts/shiki_installer.py\nindex be064ab7a29ff6836f7f7a1d70500a43e0d11d7b..9fd8eefd137ce7003dad5c0dcb2e48e2cd47ab32\n--- a/scripts/shiki_installer.py\n+++ b/scripts/shiki_installer.py\n@@ -118,6 +118,7 @@\n # what lets a target carry it without a governance-schema change.\n INSTALL_STAMP_PATH = \".shiki/install-stamp.json\"\n INSTALL_STAMP_VERSION = 1\n+LEGACY_SHIKI_SADR_MAX = 18\n \n # Under --force the shipped surface splits three ways, so nothing is lost and\n # nothing is silently stale.\n@@ -237,9 +238,98 @@\n         )\n \n \n+def legacy_shiki_adr_paths() -> tuple[str, ...]:\n+    \"\"\"Former numeric paths for Shiki SADR-0001 through SADR-0018.\n+\n+    The authoritative slugs come from the current SADR filenames. SADR-0019 is\n+    the namespace decision itself and never shipped under a numeric legacy path.\n+    \"\"\"\n+    paths: list[str] = []\n+    for sadr in sorted((ROOT / \"docs\" / \"adr\").glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\")):\n+        number = sadr.name.split(\"-\", 2)[1]\n+        if int(number) <= LEGACY_SHIKI_SADR_MAX:\n+            paths.append(f\"docs/adr/{sadr.name.removeprefix('SADR-')}\")\n+    return tuple(paths)\n+\n+\n+def preflight_legacy_sadr_cleanup(target: Path, *, force: bool) -> tuple[str, ...]:\n+    \"\"\"Authorize exact legacy Shiki ADR removals before the first write.\n+\n+    A non-force install never removes existing files. A forced install needs no\n+    stamp when no legacy Shiki path is present. When legacy paths do exist, each\n+    must be named in a readable install stamp and its current bytes must match\n+    the recorded digest.\n+    \"\"\"\n+    if not force:\n+        return ()\n+    present = tuple(\n+        relative\n+        for relative in legacy_shiki_adr_paths()\n+        if (target / relative).exists() or (target / relative).is_symlink()\n+    )\n+    if not present:\n+        return ()\n+\n+    stamp_path = target / INSTALL_STAMP_PATH\n+    try:\n+        raw = stamp_path.read_text(encoding=\"utf-8\")\n+        stamp = json.loads(raw)\n+    except FileNotFoundError:\n+        detail = \"install stamp is absent\"\n+        blockers = [f\"{relative}: {detail}\" for relative in present]\n+    except (OSError, UnicodeDecodeError) as error:\n+        detail = f\"install stamp is unreadable ({error})\"\n+        blockers = [f\"{relative}: {detail}\" for relative in present]\n+    except json.JSONDecodeError as error:\n+        detail = f\"install stamp is malformed ({error})\"\n+        blockers = [f\"{relative}: {detail}\" for relative in present]\n+    else:\n+        if not isinstance(stamp, dict):\n+            blockers = [\n+                f\"{relative}: install stamp is malformed (expected an object)\"\n+                for relative in present\n+            ]\n+        else:\n+            digests = stamp.get(\"digests\")\n+            blockers = []\n+            if not isinstance(digests, dict):\n+                blockers.extend(\n+                    f\"{relative}: install stamp has no digest for this path\"\n+                    for relative in present\n+                )\n+            else:\n+                for relative in present:\n+                    expected = digests.get(relative)\n+                    if not isinstance(expected, str) or not expected:\n+                        blockers.append(\n+                            f\"{relative}: install stamp has no digest for this path\"\n+                        )\n+                        continue\n+                    try:\n+                        actual = _sha256_file(target / relative)\n+                    except OSError as error:\n+                        blockers.append(\n+                            f\"{relative}: legacy path is unreadable ({error})\"\n+                        )\n+                        continue\n+                    if actual != expected:\n+                        blockers.append(\n+                            f\"{relative}: digest mismatch (stamp {expected}, current {actual})\"\n+                        )\n+\n+    if blockers:\n+        joined = \"\\n\".join(f\"- {blocker}\" for blocker in blockers)\n+        raise ShikiError(\n+            \"legacy Shiki ADR cleanup is not authorized:\\n\"\n+            f\"{joined}\\nNo files were written.\"\n+        )\n+    return present\n+\n+\n def install_template(target: Path, *, force: bool, validate: bool) -> list[NewFileNote]:\n     # Preconditions first: never rewrite the tree of a target that cannot be\n     # upgraded cleanly (see refuse_pending_migrations).\n+    legacy_cleanup = preflight_legacy_sadr_cleanup(target, force=force)\n     refuse_pending_migrations(target)\n \n     # Resolve the target's required CODEOWNERS owner once from its\n@@ -250,6 +340,10 @@\n     # verbatim. See ``codeowners_required_owner``.\n     codeowners_owner = codeowners_required_owner(target)\n \n+    for relative in legacy_cleanup:\n+        (target / relative).unlink()\n+        info(f\"removed stamped legacy Shiki ADR: {target / relative}\")\n+\n     notes: list[NewFileNote] = []\n     for relative in TEMPLATE_PATHS:\n         source = ROOT / relative\n@@ -647,6 +741,22 @@\n     digests = target_stamp_digests(target)\n     existing = read_install_stamp(target)\n     if existing is not None:\n+        # A non-force install deliberately leaves legacy Shiki ADRs in place.\n+        # Preserve only still-matching ownership proof so a later forced\n+        # upgrade can authorize cleanup; never carry a missing or stale digest.\n+        existing_digests = existing.get(\"digests\")\n+        if isinstance(existing_digests, dict):\n+            for relative in legacy_shiki_adr_paths():\n+                expected = existing_digests.get(relative)\n+                path = target / relative\n+                if not isinstance(expected, str) or not path.is_file():\n+                    continue\n+                try:\n+                    actual = _sha256_file(path)\n+                except OSError:\n+                    continue\n+                if actual == expected:\n+                    digests[relative] = actual\n         if _incoming_is_older(commit, existing.get(\"platform_commit\")):\n             warn(\n                 \"kept existing install stamp: incoming install \"\ndiff --git a/scripts/shiki_loop.py b/scripts/shiki_loop.py\nindex 3611641da8aeb5038b1da90e3114b09a473ec6d8..84caa491b9cad0edc4176f209c13e9221e975681\n--- a/scripts/shiki_loop.py\n+++ b/scripts/shiki_loop.py\n@@ -424,7 +424,7 @@\n         # unresolvable CCA verdict has already produced its terminal stop and is never\n         # laundered into a head-moving sync (SADR-0010). It also never fires while the\n         # policy (Guardian) gate is red \u2014 for ANY reason, not only when it is the sole\n-        # red check \u2014 so a head-bound Guardian approval, or an SADR-0015\n+        # red check \u2014 so a head-bound Guardian approval, or a SADR-0015\n         # contract-approval carry that base movement defeated (the policy gate compares\n         # base and head task governance directly), is never silently restored by\n         # adopting base wholesale. It is bounded by a durable per-task counter so the\ndiff --git a/scripts/shiki_tasks.py b/scripts/shiki_tasks.py\nindex aec3809f24b4a0c8c96fc50971cff9ae9ad30381..d531052e9d9717058d4631483cefab44c26a65c1\n--- a/scripts/shiki_tasks.py\n+++ b/scripts/shiki_tasks.py\n@@ -821,7 +821,7 @@\n         \"prompt\": prompt,\n         \"entry_skill\": \"grill-with-docs\",\n         \"required_next_steps\": [\n-            \"Run grill-with-docs until terminology, boundaries, risks, and ADR-worthy decisions are settled.\",\n+            \"Run grill-with-docs until terminology, boundaries, risks, and decision-record-worthy choices are settled.\",\n             \"Obtain the operator's explicit approval of the PRD/requirements (Spec Freeze).\",\n             \"Write a machine-readable plan JSON with grill_with_docs.status=complete and spec_freeze.status=frozen.\",\n             \"Run shiki plan ingest --plan-file PLAN.json.\",\ndiff --git a/scripts/test_shiki_goal_loop.sh b/scripts/test_shiki_goal_loop.sh\nindex 753c7b516e10f0be3bc92a4bd0eb1fa699776ae6..8f3820a0a079b3044424cfe1a6c6b7fe68896e67\n--- a/scripts/test_shiki_goal_loop.sh\n+++ b/scripts/test_shiki_goal_loop.sh\n@@ -231,8 +231,8 @@\n \n # High/critical risk merges autonomously when all required checks are green:\n # the \"MergeGate policy check\" required check IS the Guardian gate (it enforces\n-# guardian-policy.json \u2014 human approval OR external AI guardian review, ADR\n-# 0010), so green checks mean a recorded authority approved. The fake gh returns\n+# guardian-policy.json \u2014 human approval OR external AI guardian review,\n+# SADR-0010), so green checks mean a recorded authority approved. The fake gh returns\n # green checks, simulating that the Guardian gate passed.\n python3 \"$ROOT/scripts/shiki.py\" goal create --target \"$TARGET\" --title \"Guardian gate\" --outcome \"High risk merges via policy gate\" >/tmp/shiki-goal-loop-guardian-goal.json\n GGOAL=\"$(json_get /tmp/shiki-goal-loop-guardian-goal.json goal_id)\"\ndiff --git a/tests/test_adr_references.py b/tests/test_adr_references.py\nindex 51706329c797d5dd9b47e082a333fa83b261d482..db6e693e35f67103935be34f1d9bb4ff7ee60395\n--- a/tests/test_adr_references.py\n+++ b/tests/test_adr_references.py\n@@ -1,7 +1,7 @@\n-\"\"\"Every ``ADR ####`` citation must resolve to exactly one decision record.\n+\"\"\"Every target ADR and Shiki SADR citation resolves in its own namespace.\n \n Background. ``scripts/mergegate_check.py`` justifies ``is_bookkeeping_closeout``\n-with \"ADR 0017\" nine times and ``scripts/guardian_approval_signal.py`` cites\n+with \"SADR-0017\" nine times and ``scripts/guardian_approval_signal.py`` cites\n 0016/0017 seven times, yet for a stretch the numbering in ``docs/adr/`` stopped\n at 0015: the authoring commits lived on a coordinator branch and no task ever\n declared ``docs/adr/...`` in its locks, so nothing carried the records to the\n@@ -11,11 +11,12 @@\n and the directory drifted with no signal.\n \n This is the same unbound-pair shape behind other silent failures: a reference\n-and its target with nothing asserting they agree. This test binds them. It scans\n-every ``scripts/*.py`` (plus ``docs/**/*.md`` and the root ``*.md`` governance\n-files, which are cheap) for the ``ADR ####`` pattern and asserts each cited\n-number resolves to exactly one ``docs/adr/####-*.md``. The converse is\n-deliberately NOT required: an ADR may exist without being cited.\n+and its target with nothing asserting they agree. This test binds them. Target\n+``ADR NNNN`` citations resolve only to ``docs/adr/NNNN-*.md``; Shiki\n+``SADR-NNNN`` citations resolve only to ``docs/adr/SADR-NNNN-*.md``. The scan\n+covers current governance docs, scripts, workflows, prompts, packaging metadata,\n+and repo-local Codex guidance while deliberately excluding historical\n+``.shiki`` evidence. The converse is not required: a record may be uncited.\n \n The suite is dependency free: standard-library ``unittest`` only.\n \"\"\"\n@@ -31,17 +32,18 @@\n \n REPO_ROOT = Path(__file__).resolve().parents[1]\n \n-# \"ADR 0016\" -- a four-digit citation. The trailing \\b keeps a five-digit token\n-# from being read as a truncated four-digit number.\n-_CITATION = re.compile(r\"\\bADR (\\d{4})\\b\")\n-# \"0016-locks-bound-product-paths.md\" -- the leading four digits name the record.\n-_ADR_FILENAME = re.compile(r\"^(\\d{4})-.*\\.md$\")\n+# ``ADR 0016`` / ``SADR-0016`` -- a namespace plus four-digit citation. The\n+# trailing ``\\b`` keeps a five-digit token from being read as a truncated one.\n+_CITATION = re.compile(r\"\\b(S?ADR)[ -](\\d{4})\\b\")\n+# Target ADRs retain the numeric filename; Shiki platform decisions use SADR.\n+_DECISION_FILENAME = re.compile(r\"^(?:(SADR)-)?(\\d{4})-.*\\.md$\")\n \n \n class Violation(NamedTuple):\n     \"\"\"A citation that does not resolve to exactly one decision record.\"\"\"\n \n     citing_file: str  # repo-relative path of the file that cites the ADR\n+    namespace: str  # \"ADR\" for target decisions; \"SADR\" for Shiki decisions\n     number: str  # the cited four-digit number, e.g. \"0016\"\n     line: int  # 1-based line of the citation\n     reason: str  # \"missing\" (zero matches) or \"ambiguous\" (two or more)\n@@ -49,44 +51,59 @@\n \n     @property\n     def expected_path(self) -> str:\n-        return f\"docs/adr/{self.number}-*.md\"\n+        prefix = \"SADR-\" if self.namespace == \"SADR\" else \"\"\n+        return f\"docs/adr/{prefix}{self.number}-*.md\"\n \n     def describe(self) -> str:\n         \"\"\"Name the citing file, the cited number, and the missing path.\"\"\"\n         if self.reason == \"missing\":\n             return (\n-                f\"{self.citing_file}:{self.line} cites ADR {self.number} \"\n+                f\"{self.citing_file}:{self.line} cites {self.namespace} {self.number} \"\n                 f\"but no document matches {self.expected_path}\"\n             )\n         joined = \", \".join(self.matches)\n         return (\n-            f\"{self.citing_file}:{self.line} cites ADR {self.number} which \"\n+            f\"{self.citing_file}:{self.line} cites {self.namespace} {self.number} which \"\n             f\"resolves to {len(self.matches)} documents (expected exactly one \"\n             f\"at {self.expected_path}): {joined}\"\n         )\n \n \n-def _adr_index(root: Path) -> dict[str, list[Path]]:\n-    \"\"\"Map every four-digit ADR number to the record files that claim it.\"\"\"\n-    index: dict[str, list[Path]] = {}\n+def _adr_index(root: Path) -> dict[tuple[str, str], list[Path]]:\n+    \"\"\"Map each decision namespace and number to the files that claim it.\"\"\"\n+    index: dict[tuple[str, str], list[Path]] = {}\n     adr_dir = root / \"docs\" / \"adr\"\n     if adr_dir.is_dir():\n         for path in sorted(adr_dir.glob(\"*.md\")):\n-            match = _ADR_FILENAME.match(path.name)\n+            match = _DECISION_FILENAME.match(path.name)\n             if match:\n-                index.setdefault(match.group(1), []).append(path)\n+                namespace = \"SADR\" if match.group(1) else \"ADR\"\n+                index.setdefault((namespace, match.group(2)), []).append(path)\n     return index\n \n \n def _citing_files(root: Path, include_docs: bool = True) -> list[Path]:\n-    \"\"\"Files scanned for citations: scripts/*.py, and cheaply the markdown.\"\"\"\n+    \"\"\"Current, shipped text surfaces scanned for decision citations.\"\"\"\n     files = sorted((root / \"scripts\").glob(\"*.py\"))\n+    files += sorted((root / \"scripts\").glob(\"*.sh\"))\n     if include_docs:\n         docs = root / \"docs\"\n         if docs.is_dir():\n             files += sorted(docs.rglob(\"*.md\"))\n         files += sorted(root.glob(\"*.md\"))\n-    return files\n+        pyproject = root / \"pyproject.toml\"\n+        if pyproject.is_file():\n+            files.append(pyproject)\n+        for relative in (\".github\", \".codex\"):\n+            surface = root / relative\n+            if surface.is_dir():\n+                files += sorted(\n+                    path\n+                    for path in surface.rglob(\"*\")\n+                    if path.is_file()\n+                    and path.suffix in {\".md\", \".py\", \".sh\", \".toml\", \".yaml\", \".yml\"}\n+                )\n+    return sorted(set(files))\n \n \n def find_adr_reference_violations(\n@@ -94,10 +111,9 @@\n ) -> list[Violation]:\n     \"\"\"Return every ``ADR ####`` citation under ``root`` that fails to resolve.\n \n-    A citation resolves when exactly one ``docs/adr/####-*.md`` matches its\n-    number. Zero matches is a \"missing\" violation; two or more is \"ambiguous\".\n-    Records that are never cited produce nothing -- the converse is not\n-    required.\n+    A citation resolves when exactly one record in the same namespace matches\n+    its number. Zero matches is a \"missing\" violation; two or more is\n+    \"ambiguous\". Records that are never cited produce nothing.\n     \"\"\"\n     root = Path(root)\n     index = _adr_index(root)\n@@ -110,13 +126,15 @@\n         rel = path.relative_to(root).as_posix()\n         for lineno, line in enumerate(text.splitlines(), start=1):\n             for match in _CITATION.finditer(line):\n-                number = match.group(1)\n-                found = index.get(number, [])\n+                namespace, number = match.groups()\n+                found = index.get((namespace, number), [])\n                 if len(found) == 1:\n                     continue\n                 reason = \"missing\" if not found else \"ambiguous\"\n                 rels = [p.relative_to(root).as_posix() for p in found]\n-                violations.append(Violation(rel, number, lineno, reason, rels))\n+                violations.append(\n+                    Violation(rel, namespace, number, lineno, reason, rels)\n+                )\n     return violations\n \n \n@@ -129,8 +147,8 @@\n         self.assertEqual(\n             violations,\n             [],\n-            \"Every 'ADR ####' citation must resolve to exactly one \"\n-            f\"docs/adr/####-*.md:\\n{detail}\",\n+            \"Every ADR/SADR citation must resolve exactly once in its own \"\n+            f\"docs/adr namespace:\\n{detail}\",\n         )\n \n     def test_scan_actually_reads_scripts(self) -> None:\n@@ -146,8 +164,21 @@\n         # The two records this Goal carried onto the default branch, and which\n         # the scripts cite most, must be present.\n         index = _adr_index(REPO_ROOT)\n-        self.assertIn(\"0016\", index)\n-        self.assertIn(\"0017\", index)\n+        self.assertIn((\"SADR\", \"0016\"), index)\n+        self.assertIn((\"SADR\", \"0017\"), index)\n+\n+    def test_platform_tree_has_only_sadr_0001_through_0019(self) -> None:\n+        adr_dir = REPO_ROOT / \"docs\" / \"adr\"\n+        sadrs = sorted(adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\"))\n+        self.assertEqual(\n+            {path.name.split(\"-\", 2)[1] for path in sadrs},\n+            {f\"{number:04d}\" for number in range(1, 20)},\n+        )\n+        self.assertEqual(\n+            list(adr_dir.glob(\"[0-9][0-9][0-9][0-9]-*.md\")),\n+            [],\n+            \"the Shiki platform tree must not retain legacy numeric SADR paths\",\n+        )\n \n \n class FixtureAdrReferences(unittest.TestCase):\n@@ -180,6 +211,26 @@\n         self.assertIn(\"9991\", message)  # cited number\n         self.assertIn(\"docs/adr/9991-*.md\", message)  # missing path\n \n+    def test_target_adr_does_not_satisfy_missing_platform_sadr(self) -> None:\n+        root = self._make_repo(\n+            adr_names=[\"0042-target-decision.md\"],\n+            script_body='\"\"\"Target ADR 0042 exists, but platform SADR-0042 does not.\"\"\"\\n',\n+        )\n+        violations = find_adr_reference_violations(root, include_docs=False)\n+        self.assertEqual(len(violations), 1)\n+        self.assertEqual(violations[0].namespace, \"SADR\")\n+        self.assertEqual(violations[0].number, \"0042\")\n+        self.assertEqual(violations[0].expected_path, \"docs/adr/SADR-0042-*.md\")\n+\n+    def test_target_adr_and_platform_sadr_may_reuse_a_number(self) -> None:\n+        root = self._make_repo(\n+            adr_names=[\"0042-target-decision.md\", \"SADR-0042-platform-decision.md\"],\n+            script_body='\"\"\"Target ADR 0042 and platform SADR-0042 are distinct.\"\"\"\\n',\n+        )\n+        self.assertEqual(\n+            find_adr_reference_violations(root, include_docs=False), []\n+        )\n+\n     def test_uncited_adr_is_allowed(self) -> None:\n         # An ADR exists but nothing cites it -> not a violation. The converse\n         # (every ADR must be cited) is explicitly NOT required.\ndiff --git a/tests/test_guardian_review.py b/tests/test_guardian_review.py\nindex 88829e7810212635e2fc98aa5918d6f26294677d..00a85a237acd5bf2d6cb5b294128198c422402ca\n--- a/tests/test_guardian_review.py\n+++ b/tests/test_guardian_review.py\n@@ -162,7 +162,7 @@\n \n     def test_docs_only_is_light_and_not_conservative(self):\n         focus = gr.classify_review_focus([\"docs/adr/0099-x.md\", \"README.md\"], \"\", \"low\")\n-        self.assertTrue(any(\"Docs/ADR-only\" in f for f in focus))\n+        self.assertTrue(any(\"Docs/decision-record-only\" in f for f in focus))\n         self.assertFalse(any(\"conservative default\" in f.lower() for f in focus))\n \n     def test_unknown_category_is_conservative(self):\ndiff --git a/tests/test_policy_job_merged_prs.py b/tests/test_policy_job_merged_prs.py\nindex d45512e74681e5e649279236c7d4e79d5c3da471..b97989f8bd74d091de1a6b5275a20d8dc68c4512\n--- a/tests/test_policy_job_merged_prs.py\n+++ b/tests/test_policy_job_merged_prs.py\n@@ -3,8 +3,8 @@\n \n ``.github/workflows/shiki-cca-completion.yml`` has two consumers of the merged-PR\n proof: the CCA *signal* (``guardian_approval_signal.py`` in \"Prepare CCA context\")\n-and the authoritative gate (``mergegate_check.py`` in \"Enforce MergeGate\"). ADR\n-0017's bookkeeping-closeout exemption (condition 6) needs the merged implementation\n+and the authoritative gate (``mergegate_check.py`` in \"Enforce MergeGate\").\n+SADR-0017's bookkeeping-closeout exemption (condition 6) needs the merged implementation\n PR proven, and a closeout carries NO ``post_merge_reconcile`` marker. The signal\n already resolves ``MERGED_PRS`` unconditionally, but \"Enforce MergeGate\" used to\n resolve it only inside ``grep -q '<!-- shiki:post_merge_reconcile -->'``. So a\ndiff --git a/tests/test_sadr_install_migration.py b/tests/test_sadr_install_migration.py\nnew file mode 100644\nindex 0000000000000000000000000000000000000000..a6f4c57e4dcaef73ef600b0d4804744138ab9071\n--- /dev/null\n+++ b/tests/test_sadr_install_migration.py\n@@ -0,0 +1,229 @@\n+\"\"\"SADR installation and collision-safe legacy upgrade behavior.\"\"\"\n+\n+from __future__ import annotations\n+\n+import contextlib\n+import hashlib\n+import io\n+import json\n+import shutil\n+import tempfile\n+import unittest\n+from pathlib import Path\n+\n+import shiki_test_support  # noqa: F401  (path bootstrap)\n+\n+import shiki_installer\n+from tests.test_adr_references import find_adr_reference_violations\n+\n+\n+PRODUCT_NUMBERS = tuple(range(13, 19))\n+\n+\n+def _quiet_install(target: Path, *, force: bool, validate: bool) -> None:\n+    with contextlib.redirect_stdout(io.StringIO()):\n+        shiki_installer.install_template(target, force=force, validate=validate)\n+\n+\n+def _sha256(path: Path) -> str:\n+    return hashlib.sha256(path.read_bytes()).hexdigest()\n+\n+\n+def _legacy_sadr_paths() -> tuple[str, ...]:\n+    return tuple(\n+        f\"docs/adr/{path.name.removeprefix('SADR-')}\"\n+        for path in sorted((shiki_installer.ROOT / \"docs\" / \"adr\").glob(\"SADR-*.md\"))\n+        if path.name != \"SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\"\n+    )\n+\n+\n+def _create_legacy_upgrade_fixture(\n+    target: Path,\n+) -> tuple[dict[str, bytes], tuple[str, ...]]:\n+    adr_dir = target / \"docs\" / \"adr\"\n+    adr_dir.mkdir(parents=True)\n+    product_bytes = {\n+        f\"{number:04d}-cubeshare-product-decision.md\": (\n+            f\"# CubeShare ADR {number:04d}\\n\\nDo not rewrite {number}.\\n\"\n+        ).encode()\n+        for number in PRODUCT_NUMBERS\n+    }\n+    for name, content in product_bytes.items():\n+        (adr_dir / name).write_bytes(content)\n+\n+    legacy_paths = _legacy_sadr_paths()\n+    for relative in legacy_paths:\n+        (target / relative).write_bytes(f\"legacy Shiki bytes for {relative}\\n\".encode())\n+\n+    stamp_path = target / shiki_installer.INSTALL_STAMP_PATH\n+    stamp_path.parent.mkdir(parents=True, exist_ok=True)\n+    stamp = {\n+        \"version\": shiki_installer.INSTALL_STAMP_VERSION,\n+        \"platform_commit\": None,\n+        \"installed_at\": \"2000-01-01T00:00:00+00:00\",\n+        \"digests\": {\n+            relative: _sha256(target / relative) for relative in legacy_paths\n+        },\n+    }\n+    stamp_path.write_text(\n+        json.dumps(stamp, indent=2, sort_keys=True) + \"\\n\", encoding=\"utf-8\"\n+    )\n+    return product_bytes, legacy_paths\n+\n+\n+def _tree_snapshot(root: Path) -> dict[str, tuple[str, bytes | str]]:\n+    \"\"\"Recursive byte/shape snapshot used to prove preflight is write-free.\"\"\"\n+    snapshot: dict[str, tuple[str, bytes | str]] = {}\n+    for path in sorted(root.rglob(\"*\")):\n+        relative = path.relative_to(root).as_posix()\n+        if path.is_symlink():\n+            snapshot[relative] = (\"symlink\", path.readlink().as_posix())\n+        elif path.is_dir():\n+            snapshot[relative] = (\"directory\", \"\")\n+        else:\n+            snapshot[relative] = (\"file\", path.read_bytes())\n+    return snapshot\n+\n+\n+class FreshSadrInstallTests(unittest.TestCase):\n+    def setUp(self) -> None:\n+        self.tmp = Path(tempfile.mkdtemp(prefix=\"shiki-sadr-fresh-\"))\n+        self.addCleanup(lambda: shutil.rmtree(self.tmp, ignore_errors=True))\n+        self.target = self.tmp / \"target\"\n+        self.adr_dir = self.target / \"docs\" / \"adr\"\n+        self.adr_dir.mkdir(parents=True)\n+        self.product_bytes = {\n+            f\"{number:04d}-product-decision.md\": (\n+                f\"# Product ADR {number:04d}\\n\\nTarget-owned bytes {number}.\\n\"\n+            ).encode()\n+            for number in PRODUCT_NUMBERS\n+        }\n+        for name, content in self.product_bytes.items():\n+            (self.adr_dir / name).write_bytes(content)\n+\n+    def test_fresh_install_preserves_product_adrs_and_installs_all_sadrs(self) -> None:\n+        _quiet_install(self.target, force=False, validate=True)\n+\n+        for name, expected in self.product_bytes.items():\n+            self.assertEqual((self.adr_dir / name).read_bytes(), expected)\n+\n+        sadr_numbers = {\n+            path.name.split(\"-\", 2)[1]\n+            for path in self.adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\")\n+        }\n+        self.assertEqual(sadr_numbers, {f\"{number:04d}\" for number in range(1, 20)})\n+\n+        numeric_records = {\n+            path.name\n+            for path in self.adr_dir.glob(\"[0-9][0-9][0-9][0-9]-*.md\")\n+        }\n+        self.assertEqual(numeric_records, set(self.product_bytes))\n+        self.assertEqual(find_adr_reference_violations(self.target), [])\n+\n+\n+class LegacySadrUpgradeTests(unittest.TestCase):\n+    def setUp(self) -> None:\n+        self.tmp = Path(tempfile.mkdtemp(prefix=\"shiki-sadr-upgrade-\"))\n+        self.addCleanup(lambda: shutil.rmtree(self.tmp, ignore_errors=True))\n+        self.target = self.tmp / \"target\"\n+        self.adr_dir = self.target / \"docs\" / \"adr\"\n+        self.product_bytes, self.legacy_paths = _create_legacy_upgrade_fixture(\n+            self.target\n+        )\n+\n+    def test_force_upgrade_deletes_only_stamped_legacy_sadrs(self) -> None:\n+        _quiet_install(self.target, force=True, validate=True)\n+\n+        for relative in self.legacy_paths:\n+            self.assertFalse((self.target / relative).exists(), relative)\n+        for name, expected in self.product_bytes.items():\n+            self.assertEqual((self.adr_dir / name).read_bytes(), expected)\n+\n+        sadrs = list(self.adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\"))\n+        self.assertEqual(len(sadrs), 19)\n+        refreshed = json.loads(\n+            (self.target / shiki_installer.INSTALL_STAMP_PATH).read_text(encoding=\"utf-8\")\n+        )\n+        for relative in self.legacy_paths:\n+            self.assertNotIn(relative, refreshed[\"digests\"])\n+        for path in sadrs:\n+            relative = path.relative_to(self.target).as_posix()\n+            self.assertEqual(refreshed[\"digests\"][relative], _sha256(path))\n+\n+    def test_non_force_install_never_deletes_legacy_files(self) -> None:\n+        before = {\n+            relative: (self.target / relative).read_bytes()\n+            for relative in self.legacy_paths\n+        }\n+        _quiet_install(self.target, force=False, validate=False)\n+        for relative, expected in before.items():\n+            self.assertEqual((self.target / relative).read_bytes(), expected)\n+        refreshed = json.loads(\n+            (self.target / shiki_installer.INSTALL_STAMP_PATH).read_text(encoding=\"utf-8\")\n+        )\n+        for relative in self.legacy_paths:\n+            self.assertEqual(refreshed[\"digests\"][relative], _sha256(self.target / relative))\n+\n+\n+class LegacyCleanupPreflightTests(unittest.TestCase):\n+    def setUp(self) -> None:\n+        self.tmp = Path(tempfile.mkdtemp(prefix=\"shiki-sadr-preflight-\"))\n+        self.addCleanup(lambda: shutil.rmtree(self.tmp, ignore_errors=True))\n+\n+    def test_all_ownership_blockers_abort_before_any_write(self) -> None:\n+        cases = (\n+            (\"absent\", \"install stamp is absent\"),\n+            (\"unreadable\", \"install stamp is unreadable\"),\n+            (\"malformed\", \"install stamp is malformed\"),\n+            (\"missing-digest\", \"install stamp has no digest\"),\n+            (\"digest-mismatch\", \"digest mismatch\"),\n+        )\n+        for name, expected_reason in cases:\n+            with self.subTest(name=name):\n+                target = self.tmp / name\n+                _, legacy_paths = _create_legacy_upgrade_fixture(target)\n+                stamp_path = target / shiki_installer.INSTALL_STAMP_PATH\n+\n+                if name == \"absent\":\n+                    stamp_path.unlink()\n+                elif name == \"unreadable\":\n+                    stamp_path.unlink()\n+                    stamp_path.mkdir()\n+                elif name == \"malformed\":\n+                    stamp_path.write_text(\"{not-json\\n\", encoding=\"utf-8\")\n+                else:\n+                    stamp = json.loads(stamp_path.read_text(encoding=\"utf-8\"))\n+                    if name == \"missing-digest\":\n+                        stamp[\"digests\"] = {}\n+                    else:\n+                        stamp[\"digests\"] = {\n+                            relative: \"deadbeef\" for relative in legacy_paths\n+                        }\n+                    stamp_path.write_text(\n+                        json.dumps(stamp, indent=2, sort_keys=True) + \"\\n\",\n+                        encoding=\"utf-8\",\n+                    )\n+\n+                before = _tree_snapshot(target)\n+                with self.assertRaises(shiki_installer.ShikiError) as raised:\n+                    _quiet_install(target, force=True, validate=False)\n+                after = _tree_snapshot(target)\n+\n+                self.assertEqual(before, after, f\"{name} preflight changed the tree\")\n+                message = str(raised.exception)\n+                self.assertIn(expected_reason, message)\n+                self.assertIn(\"No files were written\", message)\n+                for relative in legacy_paths:\n+                    self.assertIn(relative, message)\n+\n+    def test_absent_stamp_is_allowed_when_no_legacy_path_exists(self) -> None:\n+        target = self.tmp / \"no-legacy\"\n+        target.mkdir()\n+        _quiet_install(target, force=True, validate=False)\n+        self.assertTrue(\n+            (target / \"docs\" / \"adr\" / \"SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\").is_file()\n+        )\n+\n+\n+if __name__ == \"__main__\":\n+    unittest.main()\n\n2026-08-07T08:16:25.509225Z ERROR codex_models_manager::manager: failed to renew cache TTL: missing field `base_instructions` at line 94 column 5\nexec\n/bin/zsh -lc 'python3 -m unittest discover -s tests -q' in /private/tmp/shiki-adr-collision.pa0KwY/.worktrees/shiki-t-20260807t073307221254z-2d22d654-introduc\ndiff --git a/.codex/skills/shiki/SKILL.md b/.codex/skills/shiki/SKILL.md\nindex c8e2e85d9d75d49cc4f2d6286ee22659c4e53e75..93cded623b2c15d9f65c76491a1c2d813ac9e6c0\n--- a/.codex/skills/shiki/SKILL.md\n+++ b/.codex/skills/shiki/SKILL.md\n@@ -18,6 +18,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n Codex CLI does not register this skill as a custom `/shiki` slash command. When\n the user types `/shiki` in Codex CLI and gets \"Unrecognized command\", explain\n that this is expected: invoke Shiki in Codex with natural language such as\n@@ -69,7 +72,7 @@\n \n ## Responsibilities\n \n-- Claude Code implements and repairs by default (`shiki runner claude`, ADR 0008); Codex implements and repairs tasks explicitly assigned to `codex`.\n+- Claude Code implements and repairs by default (`shiki runner claude`, SADR-0008); Codex implements and repairs tasks explicitly assigned to `codex`.\n - Claude Code Action can act as GitHub-side CCA or reviewer.\n - CCA judges completion.\n - MergeGate authorizes state transitions and merge readiness.\ndiff --git a/.github/prompts/cca-completion-check.md b/.github/prompts/cca-completion-check.md\nindex ed203ad5f302ecc81372d8ab27d2b1232ca97497..fd89950528333ef6242e6d5443904f5392ab7e56\n--- a/.github/prompts/cca-completion-check.md\n+++ b/.github/prompts/cca-completion-check.md\n@@ -19,6 +19,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n ## Required Reading\n \n Read only the files needed to judge the PR contract. Prefer this order:\ndiff --git a/.github/prompts/codex-task-handoff.md b/.github/prompts/codex-task-handoff.md\nindex 695e211d76dea1c60e8d5bb360e78487808286a4..59441d576b01a39569722f226c854e5a740e9eb5\n--- a/.github/prompts/codex-task-handoff.md\n+++ b/.github/prompts/codex-task-handoff.md\n@@ -2,7 +2,7 @@\n \n You are Codex Front acting as the Implementer for a Shiki task.\n \n-Follow `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant ADRs, and `docs/agents/codex-handoff.md`.\n+Follow `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant Shiki SADRs and target ADRs, and `docs/agents/codex-handoff.md`.\n \n Rules:\n \ndiff --git a/AGENTS.md b/AGENTS.md\nindex 7f041100d49c2a7525f51cb0f5d696a4a6d1c78c..a06bfbabc6fa48a007389e25e12d7ed523936bed\n--- a/AGENTS.md\n+++ b/AGENTS.md\n@@ -22,7 +22,7 @@\n - **Codex Front** is an optional implementer through Codex App, Codex CLI, Codex IDE extension, or Codex Web signed in with ChatGPT OAuth/subscription auth, dispatched with `shiki runner codex` when a task is explicitly assigned to `codex`.\n - **GitHub CCA** is implemented by Claude Code Action by default, using `CLAUDE_CODE_OAUTH_TOKEN`.\n \n-Do not assume `openai/codex-action`, `OPENAI_API_KEY`, or API-key based Codex automation in the default Shiki path. API-key runners are explicit target-repo extensions and require their own ADR.\n+Do not assume `openai/codex-action`, `OPENAI_API_KEY`, or API-key based Codex automation in the default Shiki path. API-key runners are explicit target-repo extensions and require their own target ADR.\n \n Shiki is not:\n \n@@ -44,10 +44,14 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use\n+`SADR-NNNN-*.md`; target product decisions retain `NNNN-*.md` and the `ADR`\n+namespace.\n+\n Use the canonical source-of-truth order above. Runtime-specific reading orders\n are operational guidance only and must not override or reorder it.\n \n-Conversation state is not durable truth. If a decision matters, put it in GitHub, `.shiki/`, `CONTEXT.md`, or an ADR.\n+Conversation state is not durable truth. If a decision matters, put it in GitHub, `.shiki/`, `CONTEXT.md`, a Shiki SADR, or a target ADR.\n \n If GitHub and `.shiki/` disagree, surface the conflict and prefer GitHub operational state until the mirror is repaired.\n \n@@ -75,8 +79,8 @@\n Every non-trivial change follows this loop:\n \n 1. **Goal**: clarify outcome, completion conditions, non-goals, risk level, and success signals.\n-2. **grill-with-docs**: challenge the plan against domain docs, ADRs, code reality, terminology, and edge scenarios.\n-3. **Context & Impact**: find relevant docs, ADRs, code, dependencies, owners, locks, and verification surfaces.\n+2. **grill-with-docs**: challenge the plan against domain docs, Shiki SADRs, target ADRs, code reality, terminology, and edge scenarios.\n+3. **Context & Impact**: find relevant docs, SADRs, target ADRs, code, dependencies, owners, locks, and verification surfaces.\n 4. **PRD and Spec Freeze**: use `to-prd` when context is settled enough to become durable product/engineering intent. The operator's explicit approval of the PRD is Spec Freeze: the plan records a `spec_freeze` block, and no plan runs without it. Steps 1-4 form Requirements Definition \u2014 one continuous operator dialogue. After Spec Freeze, scope changes require an operator-approved Spec Amendment; non-scope-moving interpretations go to the Assumption Log.\n 5. **Issues**: use `to-issues` to create independently grabbable vertical-slice issues. Prefer AFK slices over HITL slices where possible.\n 6. **Triage**: label issues for readiness, risk, runtime, skills, and MergeGate state.\n@@ -167,7 +171,7 @@\n - Testing decisions.\n - Out-of-scope items.\n - Further notes.\n-- Links to relevant domain terms and ADRs.\n+- Links to relevant domain terms, SADRs, and target ADRs.\n \n Every executable task must include:\n \n@@ -193,7 +197,7 @@\n \n Context & Impact must identify:\n \n-- Relevant documents, domain terms, ADRs, and past decisions.\n+- Relevant documents, domain terms, SADRs, target ADRs, and past decisions.\n - Relevant modules, interfaces, seams, callers, owners, and tests.\n - Dependency relationships between tasks.\n - Candidate locks and lock conflicts.\n@@ -210,7 +214,7 @@\n | Skill | Required when |\n | --- | --- |\n | `setup-matt-pocock-skills` | First configuring the repo for skills, or when issue tracker, triage labels, or domain docs are missing. |\n-| `grill-with-docs` | Default for non-trivial Goals before PRD/issues; always required when plans, terminology, boundaries, tradeoffs, or ADR-worthy decisions are ambiguous. |\n+| `grill-with-docs` | Default for non-trivial Goals before PRD/issues; always required when plans, terminology, boundaries, tradeoffs, or decision-record-worthy choices are ambiguous. |\n | `zoom-out` | The agent lacks an architectural map of the relevant code area. |\n | `to-prd` | Settled Goal context must become durable product/engineering intent. |\n | `to-issues` | A Goal, PRD, or plan must become independently grabbable vertical-slice issues. |\n@@ -348,7 +352,7 @@\n - Three or more new files.\n - Any high-risk or critical label.\n \n-When the architecture gate triggers, run `grill-with-docs` or `improve-codebase-architecture` as appropriate and record durable decisions in an ADR when needed.\n+When the architecture gate triggers, run `grill-with-docs` or `improve-codebase-architecture` as appropriate and record durable decisions in a Shiki SADR or target ADR as applicable.\n \n ## Engineering Discipline\n \ndiff --git a/CLAUDE.md b/CLAUDE.md\nindex 98a932e7841a632866fcd53b0bad0e3af3711d3d..98a31399404d6fb044b4fd493f4459ac87f439de\n--- a/CLAUDE.md\n+++ b/CLAUDE.md\n@@ -14,6 +14,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n ## Claude Role\n \n Default to planner, default implementer, reviewer, coordinator, documentation editor, CCA-style completion judge, and final-judgment assistant.\n@@ -57,7 +60,7 @@\n At the start of a Shiki session:\n \n 1. Read `AGENTS.md`.\n-2. Read `CONTEXT.md` and relevant ADRs.\n+2. Read `CONTEXT.md` and relevant Shiki SADRs and target ADRs.\n 3. Read active `.shiki/` Goal/task/ledger state when present.\n 4. Read active distilled rules from `.shiki/memories/` (status `distilled`, `active: true`, not revoked or superseded) for applicable guidance; the same rules are injected deterministically into task handoffs as the `## Distilled Rules` section (\u00a73.5).\n 5. Read the GitHub Issue or PR when the task is GitHub-backed.\n@@ -74,7 +77,7 @@\n - Define the Goal, completion conditions, non-goals, and risk level.\n - Treat `grill-with-docs` as the default planning entry point for non-trivial Goals.\n - Challenge domain terms against `CONTEXT.md`.\n-- Challenge hard-to-reverse tradeoffs against ADRs.\n+- Challenge hard-to-reverse tradeoffs against SADRs and target ADRs.\n - Explore code instead of asking when code can answer the question.\n - Run Context & Impact before implementation planning.\n - Produce a Task DAG, not an unordered checklist.\n@@ -94,7 +97,7 @@\n - For each question, provide your recommended answer.\n - Resolve dependency decisions before downstream design questions.\n - Update `CONTEXT.md` inline when terminology crystallizes.\n-- Offer ADRs only when the decision is hard to reverse, surprising without context, and a real tradeoff.\n+- Offer a SADR or target ADR only when the decision is hard to reverse, surprising without context, and a real tradeoff.\n - Convert unresolved questions into HITL blockers rather than allowing Codex to guess.\n \n ## PRD and Issues Standard\n@@ -103,7 +106,7 @@\n \n - Use the project's domain glossary vocabulary.\n - Avoid volatile file paths unless they are essential.\n-- Include implementation decisions, testing decisions, out-of-scope items, and relevant ADR/domain links.\n+- Include implementation decisions, testing decisions, out-of-scope items, and relevant SADR/target-ADR/domain links.\n - Identify deep-module opportunities and testable interfaces.\n \n When creating issues:\n@@ -121,7 +124,7 @@\n - Branch/worktree target.\n - Scope and non-goals.\n - Dependencies and locks.\n-- Relevant docs, ADRs, modules, and tests.\n+- Relevant docs, SADRs, target ADRs, modules, and tests.\n - Required skill invocations.\n - TDD expectations.\n - Pre-PR `code-review` gate expectations.\n@@ -152,7 +155,7 @@\n \n - Treat yourself as a judge, not implementer.\n - Do not write production code.\n-- Read `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant ADRs, PRD, task issue, PR body, diff, checks, reviews, labels, and `.shiki/` state.\n+- Read `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant SADRs and target ADRs, PRD, task issue, PR body, diff, checks, reviews, labels, and `.shiki/` state.\n - Evaluate every applicable checklist in `docs/agents/checklists.md`.\n - Map each acceptance criterion to durable evidence.\n - Distinguish implementation failure from missing evidence.\ndiff --git a/CONTEXT.md b/CONTEXT.md\nindex e61a9be06a0cce0d026484083a657bde2067e234..237ee8aa3ea8fab334ece49768bc5ed3b5af0974\n--- a/CONTEXT.md\n+++ b/CONTEXT.md\n@@ -25,7 +25,7 @@\n _Avoid_: jumping directly into code\n \n **grill-with-docs**:\n-A planning skill that challenges a plan against domain language, ADRs, code reality, and concrete edge scenarios. It resolves design-tree questions before PRD/issues. It runs inside Requirements Definition.\n+A planning skill that challenges a plan against domain language, Shiki SADRs, target ADRs, code reality, and concrete edge scenarios. It resolves design-tree questions before PRD/issues. It runs inside Requirements Definition.\n _Avoid_: generic brainstorming, silent assumptions\n \n **Requirements Definition**:\ndiff --git a/CONTRIBUTING.md b/CONTRIBUTING.md\nindex c6d9b656e73abb8a55e5f660123e03153dcb3e6d..9e37a8fbf668db5b22461aaae5548b8f1354394c\n--- a/CONTRIBUTING.md\n+++ b/CONTRIBUTING.md\n@@ -17,7 +17,7 @@\n 1. **Goal** \u2014 start from a user-approved Goal with an outcome, completion\n    conditions, non-goals, risk level, and success signals.\n 2. **grill-with-docs** \u2014 for any non-trivial change, challenge the plan against\n-   domain language, ADRs, and code reality before writing a PRD.\n+   domain language, Shiki SADRs, target ADRs, and code reality before writing a PRD.\n 3. **PRD** \u2014 capture settled product/engineering intent (`to-prd`).\n 4. **Spec Freeze** \u2014 the operator explicitly approves the PRD/requirements;\n    the plan records a `spec_freeze` block and no plan runs without it\n@@ -94,7 +94,7 @@\n   entries.\n - Do not hand-edit `.shiki/goals/*.json` or `.shiki/manifest.json` in a task PR.\n - Conversation state is not durable truth. If a decision matters, record it in\n-  GitHub, `.shiki/`, `CONTEXT.md`, or an ADR.\n+  GitHub, `.shiki/`, `CONTEXT.md`, a Shiki SADR, or a target ADR.\n \n ## Architecture Gate\n \n@@ -103,7 +103,7 @@\n or CI policy, package structure, three or more structural files, more than 100\n net new lines in a core module, three or more new files, or any high-risk label.\n When triggered, run `grill-with-docs` or `improve-codebase-architecture` and\n-record durable decisions in an ADR when needed (see `docs/adr/`).\n+record durable decisions in a Shiki SADR or target ADR as applicable (see `docs/adr/`).\n \n ## Safety\n \ndiff --git a/SYSTEM_PROMPT.md b/SYSTEM_PROMPT.md\nindex aaaea45e3a896c3a44ba89eec8bb84325ecf6c62..da009c86a88285269dee0155e58e550c6d744ea9\n--- a/SYSTEM_PROMPT.md\n+++ b/SYSTEM_PROMPT.md\n@@ -30,6 +30,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n Use the canonical source-of-truth order above. Runtime-specific reading orders\n are operational guidance only and must not override or reorder it. The current\n conversation is non-durable operator input.\n@@ -48,14 +51,14 @@\n \n A runtime may only do work that is explicitly assigned by the Goal, task, PR, or MergeGate decision.\n \n-Do not assume `openai/codex-action`, `OPENAI_API_KEY`, or API-key based Codex automation in the default Shiki path. API-key runners are explicit target-repo extensions and require their own ADR.\n+Do not assume `openai/codex-action`, `OPENAI_API_KEY`, or API-key based Codex automation in the default Shiki path. API-key runners are explicit target-repo extensions and require their own target ADR.\n \n ## 4. Non-negotiable Shiki flow\n \n For any non-trivial Goal, use this flow:\n \n 1. **Goal Seek**: define outcome, non-goals, risk level, completion criteria, and evidence requirements.\n-2. **grill-with-docs**: challenge the plan against `CONTEXT.md`, ADRs, code reality, terminology, boundaries, and edge scenarios. Ask one question at a time when the operator is available. If code can answer a question, inspect code instead of asking.\n+2. **grill-with-docs**: challenge the plan against `CONTEXT.md`, Shiki SADRs, target ADRs, code reality, terminology, boundaries, and edge scenarios. Ask one question at a time when the operator is available. If code can answer a question, inspect code instead of asking.\n 3. **Context & Impact**: identify modules, interfaces, seams, callers, dependencies, locks, risk, and verification surfaces.\n 4. **to-prd + Spec Freeze**: turn settled context into a PRD using domain vocabulary and testing decisions. The operator's explicit approval of the PRD is Spec Freeze, recorded as the plan's `spec_freeze` block; plans without it do not run. Steps 1-4 form Requirements Definition.\n 5. **to-issues**: decompose the PRD into independently grabbable vertical-slice issues. Prefer AFK slices; mark HITL slices when judgment is still required.\n@@ -75,7 +78,7 @@\n Before making any material change:\n \n 1. Read `AGENTS.md` and any runtime-specific wrapper such as `CLAUDE.md`.\n-2. Read `CONTEXT.md` and relevant ADRs.\n+2. Read `CONTEXT.md` and relevant SADRs and target ADRs.\n 3. Read the GitHub Issue / PR and `.shiki/` state for the active Goal or task.\n 4. Identify your role: Planner, Implementer, Reviewer, Completion Check Agent, Repairer, or Guardian-assist.\n 5. Identify Goal id, task id, branch, PR, dependencies, locks, risk level, required skills, and required evidence.\n@@ -103,7 +106,7 @@\n \n At minimum, identify:\n \n-- Relevant documents, ADRs, domain terms, and prior decisions.\n+- Relevant documents, SADRs, target ADRs, domain terms, and prior decisions.\n - Relevant modules, interfaces, seams, callers, owners, and tests.\n - Dependency relationships between tasks.\n - Candidate file locks and lock conflicts.\n@@ -152,7 +155,7 @@\n \n CCA must read:\n \n-- `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant ADRs.\n+- `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant SADRs and target ADRs.\n - Parent Goal issue, PRD issue, task issue, comments, and labels.\n - PR body, diff, commits, changed files, reviews, CI checks, and artifacts available to the workflow.\n - `.shiki/` task contract, locks, ledger entries, CCA checklist profile, and prior repair packets.\n@@ -214,7 +217,7 @@\n - Three or more new files.\n - Any high-risk or critical label.\n \n-When the architecture gate triggers, run `grill-with-docs` or `improve-codebase-architecture` as appropriate and record the decision in an ADR if it is hard to reverse or likely to be re-litigated.\n+When the architecture gate triggers, run `grill-with-docs` or `improve-codebase-architecture` as appropriate and record the decision in a Shiki SADR or target ADR as applicable if it is hard to reverse or likely to be re-litigated.\n \n ## 14. Skill Gate\n \n@@ -223,7 +226,7 @@\n Use these skills:\n \n - `setup-matt-pocock-skills`: before first use of the engineering skill set, or when issue tracker, triage labels, or domain docs are missing.\n-- `grill-with-docs`: default for non-trivial Goals before PRD/issues; required for ambiguous plans, terminology, boundaries, ADR-worthy tradeoffs, or design-tree decisions.\n+- `grill-with-docs`: default for non-trivial Goals before PRD/issues; required for ambiguous plans, terminology, boundaries, decision-record-worthy tradeoffs, or design-tree decisions.\n - `zoom-out`: unfamiliar code area or missing architectural map.\n - `to-prd`: convert settled context into a PRD.\n - `to-issues`: break a PRD, plan, or Goal into independently grabbable vertical-slice issues.\ndiff --git a/docs/adr/0001-keep-shiki-as-platform-name.md b/docs/adr/SADR-0001-keep-shiki-as-platform-name.md\nindex 20a9637c3b9d8c97de7ba8db7cfae4d82d9a7dba..8b685439959aada1b221c112f0ac3c51ca8a1695\n--- a/docs/adr/0001-keep-shiki-as-platform-name.md\n+++ b/docs/adr/SADR-0001-keep-shiki-as-platform-name.md\n@@ -1,3 +1,3 @@\n-# Keep Shiki As The Platform Name\n+# SADR-0001: Keep Shiki As The Platform Name\n \n Shiki remains the product name for the GitHub-native agentic engineering control plane. We considered more descriptive names such as MergePilot, MergeGate, and Kiroku, but kept Shiki because it carries the existing goal-seeking framework identity while remaining independent of Hermes, Codex, Claude Code, or any single runtime.\ndiff --git a/docs/adr/0002-use-agents-as-shared-constitution.md b/docs/adr/SADR-0002-use-agents-as-shared-constitution.md\nindex 73de30ecb078120eac540d9bc51855e3cd48d0e3..c5486182bf06fca36ea508496b013c67302fd989\n--- a/docs/adr/0002-use-agents-as-shared-constitution.md\n+++ b/docs/adr/SADR-0002-use-agents-as-shared-constitution.md\n@@ -1,3 +1,3 @@\n-# Use AGENTS As The Shared Agent Constitution\n+# SADR-0002: Use AGENTS As The Shared Agent Constitution\n \n Shiki uses `AGENTS.md` as the runtime-neutral constitution and `CLAUDE.md` as a thin Claude Code wrapper. This keeps Codex, Claude Code, Hermes Runner, and future Agent Runtimes aligned on the same GitHub-first MergeGate rules while still allowing runtime-specific instructions where needed.\ndiff --git a/docs/adr/0003-require-skill-gate-for-mergegate-readiness.md b/docs/adr/SADR-0003-require-skill-gate-for-mergegate-readiness.md\nindex 424be0c9af4c8f0136f1fde68a468c47ff84e764..7fc337987c7df9792f75f181dfcbed75fe5ffff2\n--- a/docs/adr/0003-require-skill-gate-for-mergegate-readiness.md\n+++ b/docs/adr/SADR-0003-require-skill-gate-for-mergegate-readiness.md\n@@ -1,3 +1,3 @@\n-# Require Skill Gate For MergeGate Readiness\n+# SADR-0003: Require Skill Gate For MergeGate Readiness\n \n Shiki requires relevant engineering skills to be invoked before execution when their triggers apply. This makes skills part of MergeGate readiness rather than optional prompt style, because Shiki depends on repeatable planning, issue decomposition, TDD, diagnosis, triage, and architecture review before autonomous or parallel agent work can be trusted.\ndiff --git a/docs/adr/0004-use-github-first-with-shiki-mirror.md b/docs/adr/SADR-0004-use-github-first-with-shiki-mirror.md\nindex d0a3a9174dc646a945a9d325954ffaf00c352040..a79c54ae9ee8eed2a0524192b864da4e22a92d3f\n--- a/docs/adr/0004-use-github-first-with-shiki-mirror.md\n+++ b/docs/adr/SADR-0004-use-github-first-with-shiki-mirror.md\n@@ -1,4 +1,4 @@\n-# ADR 0004: Use GitHub First With A Repository-Local Shiki Mirror\n+# SADR-0004: Use GitHub First With A Repository-Local Shiki Mirror\n \n ## Status\n \ndiff --git a/docs/adr/0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md b/docs/adr/SADR-0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\nindex b339baf49b89d9290e79c3d51b36e7b2bde3bd95..0f3c3eb9afccdb3403ce978e21d04fe12e495da0\n--- a/docs/adr/0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\n+++ b/docs/adr/SADR-0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\n@@ -1,8 +1,8 @@\n-# ADR 0005: Use Codex As Front And Claude Code Action As The GitHub Runtime\n+# SADR-0005: Use Codex As Front And Claude Code Action As The GitHub Runtime\n \n ## Status\n \n-Accepted (implementer default superseded by ADR 0008; the auth model and the\n+Accepted (implementer default superseded by SADR-0008; the auth model and the\n Claude Code Action GitHub runtime decision remain in force)\n \n ## Context\n@@ -26,4 +26,4 @@\n - Shiki avoids requiring `OPENAI_API_KEY` for the default workflow.\n - Codex work happens through the user's authenticated Codex front surface, not as a hidden backend Action.\n - Claude Code Action is configured for OAuth-token based subscription usage by default.\n-- API-key based automation remains possible only as an explicit target-repo extension with its own ADR.\n+- API-key based automation remains possible only as an explicit target-repo extension with its own target ADR.\ndiff --git a/docs/adr/0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md b/docs/adr/SADR-0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\nindex 4bcae9d1e52f0a37b8e9fbafa5cd14b6ee1993bd..7a38538a6ae9929567f0ade67e7ea6a60d0b3f7e\n--- a/docs/adr/0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\n+++ b/docs/adr/SADR-0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\n@@ -1,4 +1,4 @@\n-# ADR 0006: Separate Codex Implementation, CCA Completion, And MergeGate Authorization\n+# SADR-0006: Separate Codex Implementation, CCA Completion, And MergeGate Authorization\n \n ## Status\n \ndiff --git a/docs/adr/0007-packaging-and-release.md b/docs/adr/SADR-0007-packaging-and-release.md\nindex 79b84ddb97167372b44641e5b7860e400ce80822..bb445a2e2e856d9d1f5fdbe35de8d4e8cec0fb32\n--- a/docs/adr/0007-packaging-and-release.md\n+++ b/docs/adr/SADR-0007-packaging-and-release.md\n@@ -1,4 +1,4 @@\n-# ADR 0007: Packaging And Release For Shiki\n+# SADR-0007: Packaging And Release For Shiki\n \n ## Status\n \n@@ -25,7 +25,7 @@\n   not introduce third-party runtime dependencies or break the shim.\n - Releases must run inside the operator's subscription-authenticated toolchain\n   and must not require extra secrets beyond the built-in `GITHUB_TOKEN`\n-  (consistent with ADR 0005's no-API-key-by-default stance).\n+  (consistent with SADR-0005's no-API-key-by-default stance).\n - `.shiki/manifest.json` already encodes the install surface (`install.include`,\n   `install.create_directories`, `install.exclude_from_commit`), and it must not\n   be mutated as part of this change.\n@@ -34,7 +34,7 @@\n \n This is a hard-to-reverse platform decision (it sets the version source of\n truth, the release trigger, and the public install contract), so it is recorded\n-as an ADR.\n+as a SADR.\n \n ## Decision\n \n@@ -104,7 +104,7 @@\n 3. **Publishing to PyPI.** Rejected for now: Shiki is distributed as a\n    repository template + git-installable CLI, not a PyPI library; PyPI would add\n    credentials/secrets and a different versioning contract. Can be added later as\n-   its own ADR if a packaged distribution is needed.\n+   its own SADR if a packaged distribution is needed.\n 4. **Dynamic version (read `VERSION` via a build backend `dynamic` field).**\n    Rejected to keep `pyproject.toml` backend-agnostic and dependency-free; the\n    small duplication between `VERSION` and `pyproject.toml` is guarded by the\n@@ -123,4 +123,4 @@\n   releasing versions the whole platform but ships only `install.include` to\n   targets.\n - Future PyPI distribution or a console entry point are deferred and would each\n-  warrant their own ADR.\n+  warrant their own SADR.\ndiff --git a/docs/adr/0008-use-claude-code-as-default-implementer.md b/docs/adr/SADR-0008-use-claude-code-as-default-implementer.md\nindex dedf4510b7c7799521422f622029563d373db031..35c2625d908167fbc58855b5b63e283805f167e8\n--- a/docs/adr/0008-use-claude-code-as-default-implementer.md\n+++ b/docs/adr/SADR-0008-use-claude-code-as-default-implementer.md\n@@ -1,12 +1,12 @@\n-# ADR 0008: Use Claude Code As The Default Implementer Runtime\n+# SADR-0008: Use Claude Code As The Default Implementer Runtime\n \n ## Status\n \n-Proposed (supersedes ADR 0005 for the implementer default; keeps its auth model)\n+Proposed (supersedes SADR-0005 for the implementer default; keeps its auth model)\n \n ## Context\n \n-ADR 0005 made Codex Front the default implementation runtime, with Claude Code\n+SADR-0005 made Codex Front the default implementation runtime, with Claude Code\n restricted to planner and reviewer roles by the runtime registry, and the only\n autonomous implementation adapter was `shiki runner codex`.\n \n@@ -18,13 +18,13 @@\n - The platform's planning, review, and CCA layers already run on Claude Code,\n   and Claude Code's native capabilities (the built-in `/code-review` skill and\n   the Workflow multi-agent orchestration tool) have no Codex equivalent.\n-- The Goal lifecycle redesign (ADR 0009) requires an autonomous post-freeze\n+- The Goal lifecycle redesign (SADR-0009) requires an autonomous post-freeze\n   loop driven from the operator's Claude Code session, including bounded\n   repair dispatch, which must not depend on a runtime that cannot be invoked\n   unattended.\n \n The constitution claims Shiki is runtime-agnostic (\"not a Claude-only\n-workflow\", ADR 0002). Any change to the implementer default must preserve the\n+workflow\", SADR-0002). Any change to the implementer default must preserve the\n registry-based runtime model rather than hardcoding one vendor.\n \n ## Decision\n@@ -39,7 +39,7 @@\n   the task to review or repair-needed.\n - Keep Codex registered as an optional implementer runtime. `shiki runner\n   codex`, the Codex handoff templates, and the ChatGPT OAuth auth model from\n-  ADR 0005 remain supported for tasks explicitly assigned to `codex`.\n+  SADR-0005 remain supported for tasks explicitly assigned to `codex`.\n - Commit to mandating two Claude-native capabilities as loop contract in\n   follow-up Goals (status: the code-review gate was delivered by Goal G-B \u2014\n   skill registry, checklists item PR-12, default required_skills \u2014 while the\n@@ -60,7 +60,7 @@\n \n Out of scope: removing Codex support, changing the CCA runtime (Claude Code\n Action with `CLAUDE_CODE_OAUTH_TOKEN` stays), and API-key based automation\n-(still requires its own ADR per ADR 0005).\n+(still requires its own target ADR per SADR-0005).\n \n ## Consequences\n \n@@ -100,7 +100,7 @@\n   harness rejection of direct `codex exec` is outside Shiki's control, and the\n   plan's mandatory native capabilities cannot run on Codex at all.\n - **Remove Codex entirely**: rejected; it abandons the runtime-agnostic\n-  constitution (ADR 0002) for no operational gain and forecloses\n+  constitution (SADR-0002) for no operational gain and forecloses\n   cross-runtime adversarial review.\n - **GitHub Actions cloud implementation via claude-code-action**: rejected as\n   default; durable evidence is automatic but token cost, Node 24 workflow\ndiff --git a/docs/adr/0009-requirements-definition-and-spec-freeze.md b/docs/adr/SADR-0009-requirements-definition-and-spec-freeze.md\nindex 29b3f7871bef9203a8b4a831a65c39eb2f506e42..52e7dd69ad0aa4efe0b68c62678abbf7df02aaec\n--- a/docs/adr/0009-requirements-definition-and-spec-freeze.md\n+++ b/docs/adr/SADR-0009-requirements-definition-and-spec-freeze.md\n@@ -1,4 +1,4 @@\n-# ADR 0009: Merge Grill And Planning Into Requirements Definition With Spec Freeze\n+# SADR-0009: Merge Grill And Planning Into Requirements Definition With Spec Freeze\n \n ## Status\n \ndiff --git a/docs/adr/0010-external-ai-guardian-review.md b/docs/adr/SADR-0010-external-ai-guardian-review.md\nindex e6108bbe8bb4e73e0ff4684c8f78001a80438c4e..f3510b145664298adafb151b6f0db51b972f01ec\n--- a/docs/adr/0010-external-ai-guardian-review.md\n+++ b/docs/adr/SADR-0010-external-ai-guardian-review.md\n@@ -1,4 +1,4 @@\n-# ADR 0010: External AI Guardian Review As A First-Class Approval Authority\n+# SADR-0010: External AI Guardian Review As A First-Class Approval Authority\n \n ## Status\n \n@@ -8,7 +8,7 @@\n \n Shiki is intended to be an autonomous, recursive, self-improving agent\n framework: authority-in-the-loop, not human-in-the-loop. Until now the Guardian\n-approval required for high/critical-risk merges (guardian-policy.json, ADR\n+approval required for high/critical-risk merges (guardian-policy.json, SADR\n governance) recognized only human authority sources \u2014 a GitHub review approval,\n the `guardian:approved` label applied by a configured Guardian, or a comment\n carrying `Guardian approval granted` plus the current head SHA.\n@@ -89,7 +89,7 @@\n   autonomous design the operator intends and produced the impersonation\n   incident.\n - **Record AI approval as an operator comment**: rejected as forgery of human\n-  authority \u2014 the exact failure this ADR corrects.\n+  authority \u2014 the exact failure this SADR corrects.\n - **Deliver the artifact as a committed `.shiki/` file**: rejected; a committed\n   approval inside the PR being approved is forgeable by anyone who can push.\n - **A dedicated reviewer bot identity instead of a relayed comment**: deferred;\ndiff --git a/docs/adr/0011-autonomous-quality-gate-evidence.md b/docs/adr/SADR-0011-autonomous-quality-gate-evidence.md\nindex bca0246c5d46e44afbde15486a75eec425fc02f5..9c93eac16d8c3a6d4b11bd7224e3955102cd7db4\n--- a/docs/adr/0011-autonomous-quality-gate-evidence.md\n+++ b/docs/adr/SADR-0011-autonomous-quality-gate-evidence.md\n@@ -1,4 +1,4 @@\n-# ADR 0011: Autonomous Quality-Gate Evidence \u2014 Deterministic Facts And An Independent Verifier\n+# SADR-0011: Autonomous Quality-Gate Evidence \u2014 Deterministic Facts And An Independent Verifier\n \n ## Status\n \ndiff --git a/docs/adr/0012-autonomous-loop-closeout-to-main.md b/docs/adr/SADR-0012-autonomous-loop-closeout-to-main.md\nindex 637fad6dde4989c9c2bf0f81f676d8d29991123c..ec7d85f81030a676bcbba28b43eb365422a36eac\n--- a/docs/adr/0012-autonomous-loop-closeout-to-main.md\n+++ b/docs/adr/SADR-0012-autonomous-loop-closeout-to-main.md\n@@ -1,4 +1,4 @@\n-# 0012 \u2014 Autonomous loop pushes goal completion to main via a closeout PR\n+# SADR-0012 \u2014 Autonomous loop pushes goal completion to main via a closeout PR\n \n - Status: accepted\n - Date: 2026-06-18\ndiff --git a/docs/adr/0013-bootstrap-grants-actions-approve-pull-requests.md b/docs/adr/SADR-0013-bootstrap-grants-actions-approve-pull-requests.md\nindex b6c69d64f1c5ced346b88d6859586715b3b5bd0e..cf48ff4a6716267f539ca58edd1a852e7c7a2d64\n--- a/docs/adr/0013-bootstrap-grants-actions-approve-pull-requests.md\n+++ b/docs/adr/SADR-0013-bootstrap-grants-actions-approve-pull-requests.md\n@@ -1,4 +1,4 @@\n-# ADR 0013: Bootstrap Grants GitHub Actions Permission To Create And Approve Pull Requests\n+# SADR-0013: Bootstrap Grants GitHub Actions Permission To Create And Approve Pull Requests\n \n ## Status\n \n@@ -31,7 +31,7 @@\n security tradeoff: it lets repository automation satisfy a human-review gate. In\n a multi-maintainer repo that could let a compromised or buggy workflow\n self-approve changes. So whether bootstrap turns it on by default \u2014 and with\n-what default token scope \u2014 is an ADR-worthy decision, not an implementation\n+what default token scope \u2014 is a SADR-worthy decision, not an implementation\n detail.\n \n ## Decision\n@@ -48,7 +48,7 @@\n Specifically:\n \n - **`can_approve_pull_request_reviews` defaults to `true`.** Shiki's default\n-  posture is solo/AFK autonomous operation (ADR 0008/0009/0012), where the CCA\n+  posture is solo/AFK autonomous operation (SADR-0008/0009/0012), where the CCA\n   Review Bridge is the only path that can satisfy `required_review: true` after\n   the CCA and Guardian gates have already passed. The default makes a\n   freshly-bootstrapped repo able to run the autonomous loop end to end without a\ndiff --git a/docs/adr/0014-codex-app-external-ai-guardian-ui-adapter.md b/docs/adr/SADR-0014-codex-app-external-ai-guardian-ui-adapter.md\nindex 238db06279334caa341c117362df9271382a7fcd..65f12e9939321e1b3e389a34c822fc1549e5155d\n--- a/docs/adr/0014-codex-app-external-ai-guardian-ui-adapter.md\n+++ b/docs/adr/SADR-0014-codex-app-external-ai-guardian-ui-adapter.md\n@@ -1,4 +1,4 @@\n-# ADR 0014: Use Codex App As The External AI Guardian UI Adapter\n+# SADR-0014: Use Codex App As The External AI Guardian UI Adapter\n \n ## Status\n \ndiff --git a/docs/adr/0015-contract-approval-before-dispatch.md b/docs/adr/SADR-0015-contract-approval-before-dispatch.md\nindex fad6c656741a20a7684ba16080a7db159f63a52f..6061271a46d3f1d6ae7174477de3fb75673123a6\n--- a/docs/adr/0015-contract-approval-before-dispatch.md\n+++ b/docs/adr/SADR-0015-contract-approval-before-dispatch.md\n@@ -1,4 +1,4 @@\n-# ADR 0015: Approve The Task Contract Before Dispatch, Not The Diff After It\n+# SADR-0015: Approve The Task Contract Before Dispatch, Not The Diff After It\n \n ## Status\n \n@@ -9,7 +9,7 @@\n Guardian approval for high/critical risk is expressible only on a pull request.\n Every approval source in `guardian-policy.json` \u2014 GitHub review, the\n `guardian:approved` label, the `Guardian approval granted` comment, and the\n-`external_ai_guardian_review` artifact (ADR 0010/0014) \u2014 is bound to a PR, and\n+`external_ai_guardian_review` artifact (SADR-0010/0014) \u2014 is bound to a PR, and\n three of the four require the current head SHA. Approval therefore cannot exist\n before an implementation PR exists.\n \n@@ -81,7 +81,7 @@\n An envelope breach must surface only on the non-repairable policy gate, never on\n the repairable `MergeGate metadata check`. Otherwise the Goal Loop routes it to\n `dispatch_repair` \u2014 instructing a runner to make the breach go away \u2014 which is\n-exactly what this ADR forbids.\n+exactly what this SADR forbids.\n \n **Contract immutability.** After registration, an implementation PR must not\n change the governance fields of its own task contract (`scope`, `non_goals`,\n@@ -116,7 +116,7 @@\n fails any verdict that is not `complete`, and the signal the CCA prompt reads is\n computed from `evaluate_guardian_approval` alone. Without the OR-merge at the\n signal, a contract-approved PR still returns `needs_guardian`, the CCA job fails,\n-and the MergeGate policy check never runs \u2014 the gate this ADR changes would never\n+and the MergeGate policy check never runs \u2014 the gate this SADR changes would never\n be reached. The CCA *prompt* needs no change: a `contract_approval` entry in the\n signal's existing `sources` array satisfies its current wording.\n \n@@ -130,7 +130,7 @@\n written, and an approved contract lets its implementation PRs merge through the\n same automatic path as low/medium risk. The wasted implement-then-block cycle\n disappears. Guardian review shifts from reading diffs to reading contracts, which\n-is both earlier and smaller; the external AI Guardian path (ADR 0010/0014) works\n+is both earlier and smaller; the external AI Guardian path (SADR-0010/0014) works\n on contracts with no change.\n \n We accept that the Guardian authorizes a class of change without seeing its\n@@ -146,7 +146,7 @@\n adversarial tests for the new approval path equivalent in rigor to\n `test_shiki_governance_evidence.sh`.\n \n-Bootstrap costs, which apply only while this ADR is being implemented. The tasks\n+Bootstrap costs, which apply only while this SADR is being implemented. The tasks\n that build Contract Approval merge under the existing rules, so the ones\n dispatched after the pre-dispatch gate lands but before the approval path lands\n need a Guardian-approved Contract PR *and* a live Guardian approval on their\n@@ -210,7 +210,7 @@\n risk; they do not bound authorized work.\n \n **Auto-escalate risk on envelope breach instead of stopping.** Returns the\n-workflow to a diff-time Guardian decision \u2014 the exact behavior this ADR removes.\n+workflow to a diff-time Guardian decision \u2014 the exact behavior this SADR removes.\n \n **Resolve governance fields from the base snapshot only.** Rejected against the\n registration flow: implementation PRs add their own contract, so the base has no\ndiff --git a/docs/adr/0016-locks-bound-product-paths.md b/docs/adr/SADR-0016-locks-bound-product-paths.md\nindex b5e850f3f1f7f4ef6e7f9d1763a29a51bd300c45..46b40a9b99a7aae644c02bb458f5473ff0a90ce2\n--- a/docs/adr/0016-locks-bound-product-paths.md\n+++ b/docs/adr/SADR-0016-locks-bound-product-paths.md\n@@ -1,4 +1,4 @@\n-# ADR 0016: Locks bound product paths; the Shiki mirror is bound by record identity\n+# SADR-0016: Locks bound product paths; the Shiki mirror is bound by record identity\n \n ## Status\n \n@@ -6,8 +6,8 @@\n \n - Date: 2026-07-28\n - Deciders: operator (mizutani-140), Claude (planner)\n-- Related: ADR 0004 (GitHub-first with a `.shiki` mirror), ADR 0009 (Spec Freeze),\n-  ADR 0012 (autonomous loop closeout to main), ADR 0015 (Contract Approval before\n+- Related: SADR-0004 (GitHub-first with a `.shiki` mirror), SADR-0009 (Spec Freeze),\n+  SADR-0012 (autonomous loop closeout to main), SADR-0015 (Contract Approval before\n   dispatch), remediation goal task R-02, the live PR #179 deadlock.\n \n ## Context\n@@ -90,11 +90,11 @@\n files, documentation files and named source modules are nameable; runtime-\n allocated ledger, runner and report ids are not \u2014 and those live in `.shiki`,\n which locks no longer govern. Existing frozen contracts are brought into line by\n-a one-time Spec Amendment per plan, which must land before ADR 0015's contract\n+a one-time Spec Amendment per plan, which must land before SADR-0015's contract\n immutability makes `locks` unwritable from the implementation PR.\n \n **Lock state is resolved from the durable side.** MergeGate resolves the lock\n-registry, and ADR 0015's governance fields, from the base `.shiki` snapshot when\n+registry, and SADR-0015's governance fields, from the base `.shiki` snapshot when\n present, falling back to head only when absent. The `MergeGate metadata check`\n job is given `--base-shiki`, which it does not receive today. Locks are not\n derived from the live open-pull-request set: a pull request body is untrusted\n@@ -108,7 +108,7 @@\n Explicitly out of scope: moving MergeGate's contract resolution off the PR\n checkout wholesale; making the mirror a projection of GitHub state; introducing\n new lock kinds; moving ledger evidence out of the repository; and unwinding\n-ADR 0012's closeout pull request. Registration of multi-task goals is ADR 0015's\n+SADR-0012's closeout pull request. Registration of multi-task goals is SADR-0015's\n Contract PR and is not re-decided here.\n \n ## Consequences\n@@ -127,7 +127,7 @@\n \n Risks accepted. First, goal-scoped identity is only as strong as `goal_id`,\n which is resolved from the PR body and the head task file; the mitigation is to\n-add `goal_id` to ADR 0015's frozen governance field set, and until that lands a\n+add `goal_id` to SADR-0015's frozen governance field set, and until that lands a\n task file can name a foreign goal. Second, base-when-present resolution is\n unavailable on fork pull requests, where the head is contributor-controlled;\n fork pull requests already cannot complete a Shiki merge, and this decision does\n@@ -139,7 +139,7 @@\n path, not by implementation pull requests.\n \n New obligations: one Spec Amendment per frozen plan to rewrite `locks`, ordered\n-before ADR 0015 T1; extension of the adversarial suites in\n+before SADR-0015 T1; extension of the adversarial suites in\n `scripts/test_shiki_adversarial_state.sh` and\n `scripts/test_shiki_governance_evidence.sh` to cover the eleven newly-scoped\n directories; and updating `scripts/test_shiki_loop_lock_guard.sh`,\n@@ -151,32 +151,32 @@\n whether the loop's transitive sync of ledger `evidence` references can be\n deleted once the Contract PR carries its own source plan.\n \n-## Relationship to ADR 0004\n+## Relationship to SADR-0004\n \n-This ADR **clarifies something ADR 0004 left unspecified**. ADR 0004 decides that\n+This SADR **clarifies something SADR-0004 left unspecified**. SADR-0004 decides that\n GitHub is the operational source of truth and that each target repository keeps a\n `.shiki/` mirror. It says nothing about pull requests writing the mirror, does\n not name MergeGate, and does not make the mirror an input to a gate. Nothing in\n-ADR 0004's Decision or Consequences is amended, superseded or weakened: the\n+SADR-0004's Decision or Consequences is amended, superseded or weakened: the\n mirror still exists, still records the same artifacts, and pull request\n-boundaries remain the coordination surface. What this ADR settles is the question\n+boundaries remain the coordination surface. What this SADR settles is the question\n 0004 never reached \u2014 by what authority a pull request may write a mirror record \u2014\n and it answers it the way `.shiki/manifest.json` already declares\n (`pr_mutation: current-task-or-current-goal-only`).\n \n-It **amends ADR 0012**, whose Decision item 5 states that \"Loop-executed tasks\n+It **amends SADR-0012**, whose Decision item 5 states that \"Loop-executed tasks\n declare the `path:.shiki/**` lock, which covers every `.shiki` file the closeout\n stages ... so `files_outside_locks` is satisfied without per-file lock\n bookkeeping.\" That clause is replaced: the closeout's mirror writes are\n-authorized by record identity, not by a declared lock. ADR 0012's substantive\n+authorized by record identity, not by a declared lock. SADR-0012's substantive\n decision \u2014 that completion reaches `main` through a closeout pull request rather\n than local attestation \u2014 is preserved unchanged.\n \n-It is **complementary to ADR 0015**, not a substitute. ADR 0015 owns contract\n+It is **complementary to SADR-0015**, not a substitute. SADR-0015 owns contract\n immutability, the pre-dispatch gate, the Contract PR and the Scope Envelope. This\n-ADR owns what a lock means and who may write the mirror. The one ordering\n+SADR owns what a lock means and who may write the mirror. The one ordering\n constraint between them is that the lock-narrowing Spec Amendment must precede\n-ADR 0015 T1's freezing of the `locks` field.\n+SADR-0015 T1's freezing of the `locks` field.\n \n ## Alternatives Considered\n \n@@ -186,7 +186,7 @@\n the only writer that can register them may run. Migrating the Guardian signal to\n that store makes every registration PR fail closed to `needs_guardian` at every\n risk level, because the task file is by definition absent from the store, which\n-removes the low/medium autonomous path ADR 0008/0009 exists to provide. And it\n+removes the low/medium autonomous path SADR-0008/0009 exists to provide. And it\n leaves schemas, manifest, config, guardian policy, templates, migrations, plans,\n worktrees, runner records, runs and handoffs with no writer at all in a\n repository whose product is `.shiki`. Measured benefit on the frozen backlog:\n@@ -199,14 +199,14 @@\n namespace is not in fact ownership-partitioned \u2014 56 ledger entries carry no\n `task_id`, and one goal DAG is writable by 35 registered tasks. The correct\n expression of its insight is that locks do not govern the mirror at all, which is\n-what this ADR decides.\n+what this SADR decides.\n \n **Make the mirror a projection written only by a registration writer and a\n post-merge projection job.** Rejected. It requires a bypass actor with write\n access to the protected default branch; it makes its own master invariant\n readable from a manifest that pull requests may edit; it deletes both reconcile\n modes and leaves a corrupted mirror with no in-band repair path, contradicting\n-ADR 0004's consequence that mirror conflicts are repaired against GitHub state;\n+SADR-0004's consequence that mirror conflicts are repaired against GitHub state;\n and its content-derived ids are rejected by `validate_shiki`'s currently-green id\n pattern.\n \ndiff --git a/docs/adr/0017-bookkeeping-closeouts-do-not-inherit-risk.md b/docs/adr/SADR-0017-bookkeeping-closeouts-do-not-inherit-risk.md\nindex 554e3a0d526a72ca067108c5464cc68c3edcf174..b9676857a4713850e063af7c39c8e0b01ecb4c66\n--- a/docs/adr/0017-bookkeeping-closeouts-do-not-inherit-risk.md\n+++ b/docs/adr/SADR-0017-bookkeeping-closeouts-do-not-inherit-risk.md\n@@ -1,4 +1,4 @@\n-# ADR 0017: A Bookkeeping Closeout Does Not Inherit Its Task's Risk\n+# SADR-0017: A Bookkeeping Closeout Does Not Inherit Its Task's Risk\n \n ## Status\n \n@@ -6,7 +6,7 @@\n \n ## Context\n \n-ADR 0012 made the goal loop push a task's completion to the default branch through a\n+SADR-0012 made the goal loop push a task's completion to the default branch through a\n closeout pull request: after the implementation PR merges, the loop opens a second PR\n carrying `task.status = done`, `lock.state = released`, and \u2014 when the task completes\n its goal \u2014 `goal.status = complete` plus one scorecard report. Nothing else. The\n@@ -49,14 +49,14 @@\n 4. The task file's governance fields (`scope`, `non_goals`, `required_skills`,\n    `risk_level`, `locks`, `acceptance_checks`, `test_command`) are byte-identical to\n    the base snapshot. Only `status`, `expected_pr`, `closeout_pr`, `ledger_evidence`\n-   and `expected_branch` may differ \u2014 the ADR 0015 contract-immutability rule,\n+   and `expected_branch` may differ \u2014 the SADR-0015 contract-immutability rule,\n    unchanged.\n \n    `expected_branch` is in that set because a closeout is *by construction* on a\n    different branch from the implementation it closes, and the loop rewrites the\n    field accordingly. It is safe to let it differ because MergeGate independently\n    requires `expected_branch` to equal the PR's actual head ref, so a PR cannot set\n-   it to anything other than the branch it is really on. The first draft of this ADR\n+   it to anything other than the branch it is really on. The first draft of this SADR\n    omitted it, which made the exemption unreachable: every real closeout failed\n    condition 4 on a field the loop always changes. See PR #209.\n 5. The state transitions are exactly the terminal set: task `review` \u2192 `done`, lock\n@@ -113,12 +113,12 @@\n classification must be derived, not asserted.\n \n **Fold the closeout into the implementation PR** so no second PR exists. Rejected under\n-ADR 0012, which recorded why: a task file that is already `done` on the implementation\n+SADR-0012, which recorded why: a task file that is already `done` on the implementation\n branch short-circuits the loop's review \u2192 CCA \u2192 merge \u2192 done ordering into a local\n goal-completion before any merge happens.\n \n **Have the loop auto-merge closeouts with elevated credentials.** Rejected outright: that\n-is a runtime authoring its own approval, which ADR 0010 and the constitution's\n+is a runtime authoring its own approval, which SADR-0010 and the constitution's\n approval-record prohibition forbid. It would also be indistinguishable, in the evidence,\n from the forgery those rules exist to prevent.\n \ndiff --git a/docs/adr/0018-carried-guardian-approval-across-a-proven-base-sync.md b/docs/adr/SADR-0018-carried-guardian-approval-across-a-proven-base-sync.md\nindex 22358562da390ed8b45cd600da08a43d378b4680..e6e2907cefa776460af73ddf7d39db605f3c39b0\n--- a/docs/adr/0018-carried-guardian-approval-across-a-proven-base-sync.md\n+++ b/docs/adr/SADR-0018-carried-guardian-approval-across-a-proven-base-sync.md\n@@ -1,4 +1,4 @@\n-# ADR 0018: Carried Guardian Approval Across A Proven Base Sync\n+# SADR-0018: Carried Guardian Approval Across A Proven Base Sync\n \n ## Status\n \n@@ -34,7 +34,7 @@\n Guardian evaluator that consumes it (`shiki_guardian.evaluate_guardian_approval`,\n the `guardian_comment_carried` source) were built and tested in isolation. Neither\n does anything until a caller passes the flags that turn the carry on, so the\n-feature was invisible in CI. This ADR records the decision the two of them\n+feature was invisible in CI. This SADR records the decision the two of them\n implement, and this task wires it into the four CI invocations that judge Guardian\n approval.\n \ndiff --git a/docs/adr/README.md b/docs/adr/README.md\nindex c70e057a4520c36e042ab68c53cd4a21ef709b71..5da43856836e2ce6a150f8127766e2f88355ab4b\n--- a/docs/adr/README.md\n+++ b/docs/adr/README.md\n@@ -1,40 +1,41 @@\n-# Architecture Decision Records\n+# Decision Record Namespaces\n \n-This directory holds Shiki's Architecture Decision Records (ADRs): durable\n-records of hard-to-reverse platform decisions. ADRs are part of the Shiki source\n-of truth alongside GitHub state, the `.shiki/` mirror, and `CONTEXT.md`.\n+This directory holds two independent decision namespaces:\n \n-## How ADRs Work\n+- Shiki Architecture Decision Records (SADRs), named\n+  `SADR-NNNN-<short-slug>.md`, record hard-to-reverse Shiki platform decisions.\n+- Target Architecture Decision Records (ADRs), named\n+  `NNNN-<short-slug>.md`, belong to the target product.\n \n-- New ADRs are added as `docs/adr/NNNN-<short-slug>.md`, where `NNNN` is the next\n-  zero-padded sequence number.\n-- The **canonical list of ADRs is the set of files matching `docs/adr/*.md`** in\n-  this directory. Browse `docs/adr/*.md` for the authoritative, current set.\n-- Use [`template.md`](template.md) as the starting point for a new ADR.\n-- Record an ADR when a decision is hard to reverse, surprising without context,\n-  and a real tradeoff. Do not write an ADR for routine or easily reversible\n-  choices.\n+The same four-digit number may appear once in each namespace. A platform\n+`SADR-NNNN` citation resolves only to `SADR-NNNN-*.md`; a target `ADR NNNN`\n+citation resolves only to `NNNN-*.md`.\n \n-Using the `docs/adr/*.md` glob as the source of truth means other PRs can add an\n-ADR without editing this index file, avoiding merge contention on a\n-hand-maintained enumeration.\n+## How Decision Records Work\n \n-## Current ADRs (snapshot)\n-\n-The list below is a convenience snapshot and may lag behind the directory. When\n-in doubt, list `docs/adr/*.md`.\n+- New Shiki platform decisions use the next zero-padded SADR number.\n+- New target product decisions use the target's own ADR sequence.\n+- The canonical list is the files in `docs/adr/`; no hand-maintained index is\n+  authoritative.\n+- Use [`template.md`](template.md) as the starting point and choose the filename\n+  namespace that owns the decision.\n+- Record a decision when it is hard to reverse, surprising without context, and\n+  a real tradeoff. Do not write a decision record for routine or easily\n+  reversible choices.\n \n-- [0001 \u2014 Keep Shiki As The Platform Name](0001-keep-shiki-as-platform-name.md)\n-- [0002 \u2014 Use AGENTS As The Shared Agent Constitution](0002-use-agents-as-shared-constitution.md)\n-- [0003 \u2014 Require Skill Gate For MergeGate Readiness](0003-require-skill-gate-for-mergegate-readiness.md)\n-- [0004 \u2014 Use GitHub First With A Repository-Local Shiki Mirror](0004-use-github-first-with-shiki-mirror.md)\n-- [0005 \u2014 Use Codex As Front And Claude Code Action As The GitHub Runtime](0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md)\n-- [0006 \u2014 Separate Codex Implementation, CCA Completion, And MergeGate Authorization](0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md)\n+Using the directory as the source of truth lets independent changes add records\n+without contending on an enumerated index.\n \n-## Adding A New ADR\n+## Adding A Shiki SADR\n \n-1. Copy `template.md` to `docs/adr/NNNN-<short-slug>.md`.\n+1. Copy `template.md` to `docs/adr/SADR-NNNN-<short-slug>.md`.\n 2. Fill in the title, status, context, decision, and consequences.\n-3. Set the status to `Proposed`, then `Accepted` once the decision is approved.\n-4. Open a PR following `CONTRIBUTING.md`. You do not need to edit this index for\n-   the ADR to be discoverable via `docs/adr/*.md`.\n+3. Set the status to `Proposed`, then `Accepted` once the proper authority has\n+   approved the decision.\n+4. Open a PR following `CONTRIBUTING.md`.\n+\n+## Adding A Target ADR\n+\n+1. Copy `template.md` to `docs/adr/NNNN-<short-slug>.md` using the target's own\n+   sequence.\n+2. Cite it as `ADR NNNN`; do not prefix its filename with `SADR-`.\ndiff --git a/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md b/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\nnew file mode 100644\nindex 0000000000000000000000000000000000000000..7455ce1f3bed02ddddb851ac21a1d1a648c9aeba\n--- /dev/null\n+++ b/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\n@@ -0,0 +1,55 @@\n+# SADR-0019: Separate Shiki Platform SADRs From Target ADRs\n+\n+## Status\n+\n+Accepted\n+\n+## Context\n+\n+Shiki installs its platform decision records into `docs/adr/` in every target\n+repository. Shiki and its targets previously used the same numeric\n+`NNNN-<slug>.md` namespace. A target that already owned ADRs 0013 through 0018\n+therefore acquired two records for each number during installation, making an\n+`ADR NNNN` citation ambiguous. Renumbering target decisions would rewrite target\n+history, while renumbering Shiki decisions per target would destroy their stable\n+platform identity.\n+\n+Upgrades also need to remove the former Shiki-owned numeric paths without ever\n+mistaking a target-owned ADR for platform content. The install stamp already\n+records exact shipped paths and content digests, so it can prove ownership only\n+when both still match.\n+\n+## Decision\n+\n+Shiki platform decisions use the `SADR` namespace:\n+\n+- records are named `docs/adr/SADR-NNNN-<slug>.md`;\n+- live platform citations use `SADR-NNNN`;\n+- target product decisions retain `docs/adr/NNNN-<slug>.md` and `ADR NNNN`;\n+- the reference validator resolves the two namespaces independently, so the same\n+  four-digit number may exist once in each namespace.\n+\n+Before a forced upgrade writes anything, the installer inventories the exact\n+former numeric Shiki ADR paths. It may delete a legacy path only when the\n+existing install stamp contains that exact path and its recorded SHA-256 digest\n+matches the file currently on disk. All ownership blockers are reported\n+together and abort the upgrade before its first write. A non-forced install\n+never performs this cleanup.\n+\n+Historical `.shiki/` evidence remains unchanged because it records the namespace\n+that existed when that evidence was produced.\n+\n+## Consequences\n+\n+- Targets may use any ADR number without colliding with Shiki's platform SADRs.\n+- Fresh installs preserve target ADR bytes and install a stable SADR set.\n+- Forced upgrades fail closed when legacy ownership cannot be proven, and a\n+  successful upgrade refreshes the stamp without removed legacy paths.\n+- Current governance, packaging, workflow, prompt, script, and skill references\n+  must move with the platform records.\n+\n+## Alternatives Considered\n+\n+- Collision-only refusal leaves affected targets permanently unupgradable.\n+- Renumbering target ADRs mutates target-owned history.\n+- Per-target Shiki renumbering makes platform citations non-portable.\ndiff --git a/docs/adr/template.md b/docs/adr/template.md\nindex a92a97daddd0b00ab9dd626cc8d86942da2eafed..17dd79175b5fee2105935557f691b40cf9492942\n--- a/docs/adr/template.md\n+++ b/docs/adr/template.md\n@@ -1,14 +1,14 @@\n-# NNNN: <Short Decision Title>\n+# <SADR-NNNN or NNNN>: <Short Decision Title>\n \n ## Status\n \n-Proposed | Accepted | Superseded by ADR-NNNN\n+Proposed | Accepted | Superseded by SADR-NNNN or target ADR NNNN\n \n ## Context\n \n What is the situation, constraint, or force that requires a decision? Reference\n-relevant domain terms (`CONTEXT.md`), prior ADRs, and code reality. State the\n-problem, not the solution.\n+relevant domain terms (`CONTEXT.md`), prior Shiki SADRs or target ADRs, and code\n+reality. State the problem, not the solution.\n \n ## Decision\n \ndiff --git a/docs/agents/autonomous-loop-e2e-acceptance.md b/docs/agents/autonomous-loop-e2e-acceptance.md\nindex bbf84c6081987f159ac6ad9ecf3c82b7aa72e7e8..5c13c1d02a05a90b0dc46cbef7c95a9846d0c237\n--- a/docs/agents/autonomous-loop-e2e-acceptance.md\n+++ b/docs/agents/autonomous-loop-e2e-acceptance.md\n@@ -40,8 +40,8 @@\n 3. `gh auth status` is logged in with merge permission on the repo.\n 4. Branch protection + required checks are configured exactly as the goal-loop\n    required-check set expects (see `docs/agents/branch-protection-smoke.md`).\n-5. The repo `.shiki/policy` permits autonomous low/medium-risk merges (ADR\n-   0008/0009); the MergeGate policy check is the recorded-authority gate.\n+5. The repo `.shiki/policy` permits autonomous low/medium-risk merges\n+   (SADR-0008/0009); the MergeGate policy check is the recorded-authority gate.\n \n The operator's only actions are: register the task and start the loop. **No\n manual pre-clearing** means after `shiki loop run` starts, the operator does not\ndiff --git a/docs/agents/bootstrap-command.md b/docs/agents/bootstrap-command.md\nindex 05f7f203a52cf412b9f97084a6e131ed0f785afd..f7aa0b734dfebee4a3b139335b66ee3415f48713\n--- a/docs/agents/bootstrap-command.md\n+++ b/docs/agents/bootstrap-command.md\n@@ -189,7 +189,9 @@\n ```\n \n Use this only for tests, fixtures, or explicit local-only template inspection.\n-Use `--force` only when you intentionally want to overwrite existing target files.\n+Use `--force` only when you intentionally want to refresh the target template.\n+Legacy numeric Shiki decision cleanup is stamp-and-digest authorized before any\n+write; target ADR paths are not cleanup candidates.\n \n ## Slash Command\n \ndiff --git a/docs/agents/checklists.md b/docs/agents/checklists.md\nindex 4a7f03ade346f53a1c1d82981f3fd744f9ab4547..0c5ec6c53dc7197b7b7a7d18e667b325babd6c45\n--- a/docs/agents/checklists.md\n+++ b/docs/agents/checklists.md\n@@ -32,18 +32,18 @@\n | ID | Item | Blocking |\n | --- | --- | --- |\n | GD-01 | Plan was challenged against `CONTEXT.md`. | blocking for non-trivial Goals |\n-| GD-02 | Relevant ADRs were read or absence was recorded. | blocking for non-trivial Goals |\n+| GD-02 | Relevant SADRs and target ADRs were read or absence was recorded. | blocking for non-trivial Goals |\n | GD-03 | Ambiguous terms were resolved or converted to HITL blockers. | blocking |\n | GD-04 | Edge scenarios were considered. | advisory |\n | GD-05 | Code reality was checked when code could answer. | blocking when applicable |\n | GD-06 | New domain terms were added to `CONTEXT.md` when needed. | blocking when applicable |\n-| GD-07 | ADR was created or explicitly skipped using the ADR criteria. | blocking when applicable |\n+| GD-07 | A SADR or target ADR was created or explicitly skipped using the decision-record criteria. | blocking when applicable |\n \n ## CI \u2014 Context & Impact Checklist\n \n | ID | Item | Blocking |\n | --- | --- | --- |\n-| CI-01 | Relevant docs and ADRs identified. | blocking |\n+| CI-01 | Relevant docs, SADRs, and target ADRs identified. | blocking |\n | CI-02 | Relevant modules, interfaces, seams, callers, and tests identified. | blocking |\n | CI-03 | Dependencies and blocked-by relationships identified. | blocking |\n | CI-04 | Candidate locks identified. | blocking |\n@@ -62,7 +62,7 @@\n | PRD-04 | Implementation decisions are recorded without volatile code detail. | blocking |\n | PRD-05 | Testing decisions are recorded. | blocking |\n | PRD-06 | Out-of-scope items are explicit. | blocking |\n-| PRD-07 | Relevant glossary/ADR links are present when applicable. | blocking |\n+| PRD-07 | Relevant glossary/SADR/target-ADR links are present when applicable. | blocking |\n \n ## SF \u2014 Spec Freeze Checklist\n \n@@ -98,7 +98,7 @@\n | H-02 | Branch/worktree target is present. | blocking |\n | H-03 | Scope and non-goals are present. | blocking |\n | H-04 | Dependencies and locks are present. | blocking |\n-| H-05 | Relevant docs, ADRs, modules, and tests are referenced. | blocking |\n+| H-05 | Relevant docs, SADRs, target ADRs, modules, and tests are referenced. | blocking |\n | H-06 | Required skills are stated. | blocking |\n | H-07 | TDD expectations are stated. | blocking when behavior work |\n | H-08 | Acceptance criteria are included. | blocking |\ndiff --git a/docs/agents/codex-handoff.md b/docs/agents/codex-handoff.md\nindex ede468f47cb277f69a857e8c45df703e4ed0151d..6f97bdffb67e44d7edea2c71e01ea28933c4eb6c\n--- a/docs/agents/codex-handoff.md\n+++ b/docs/agents/codex-handoff.md\n@@ -46,7 +46,7 @@\n \n ## Context\n - Domain terms:\n-- Relevant ADRs:\n+- Relevant SADRs and target ADRs:\n - Relevant docs:\n - Relevant modules/interfaces/seams:\n - Similar tests/prior art:\ndiff --git a/docs/agents/completion-check-agent.md b/docs/agents/completion-check-agent.md\nindex 13e4c035db35025f4e3cdcc5adfb24314ac4d521..61bfa1218cf36ca605250c91919e620f33238aa0\n--- a/docs/agents/completion-check-agent.md\n+++ b/docs/agents/completion-check-agent.md\n@@ -32,7 +32,7 @@\n - `AGENTS.md`\n - `CLAUDE.md`\n - `CONTEXT.md`\n-- relevant ADRs\n+- relevant SADRs and target ADRs\n - parent Goal issue\n - PRD issue or document\n - task issue\ndiff --git a/docs/agents/decision-control.md b/docs/agents/decision-control.md\nindex 7d8ee5ab03a5ccbcbd04056ae61de03785a2a9c9..8c94ecda00fffaa0e92c325ab83aa9776bd1dac1\n--- a/docs/agents/decision-control.md\n+++ b/docs/agents/decision-control.md\n@@ -11,7 +11,7 @@\n | Decision | Owner | When | Required inputs | Output | Enforcement surface |\n | --- | --- | --- | --- | --- | --- |\n | Is the Goal clear enough to plan? | Planner + Guardian when needed | Goal intake | User request, existing docs, risk signals | Goal readiness verdict | Goal checklist, issue template, ledger |\n-| Must `grill-with-docs` run? | Planner / Skill Gate | Before PRD or issue split | Ambiguity, domain terms, ADR conflicts, unclear boundaries | Skill invocation or explicit skip rationale | Skill Gate checklist |\n+| Must `grill-with-docs` run? | Planner / Skill Gate | Before PRD or issue split | Ambiguity, domain terms, SADR/target-ADR conflicts, unclear boundaries | Skill invocation or explicit skip rationale | Skill Gate checklist |\n | Is a PRD required? | Planner | After Goal context stabilizes | Goal, `grill-with-docs` output, Context & Impact output | PRD required / not required | PRD checklist, CCA evidence check |\n | Can the spec be frozen? | Operator only | End of Requirements Definition, before issue split | PRD/requirements summary, scope inventory (SF-02), open-question list | `spec_freeze` block in the plan + ledger evidence | `require_grilled_plan` (plan ingest/run), SF checklist |\n | Can a frozen spec change? | Operator only (Spec Amendment) | When implementation contradicts the frozen spec | Contested decisions, scoped re-grill result | recorded amendment + re-stamped freeze | SF checklist, ledger evidence |\ndiff --git a/docs/agents/domain.md b/docs/agents/domain.md\nindex 258da8ea81e87ac0a6b0b5b5e5c2fde980eaabb7..d57b0cf17ff3e1fa9e6e5885372bf083d8295421\n--- a/docs/agents/domain.md\n+++ b/docs/agents/domain.md\n@@ -5,7 +5,9 @@\n ## Layout\n \n - `CONTEXT.md` defines Shiki language. Keep it implementation-free.\n-- `docs/adr/` records hard-to-reverse decisions.\n+- `docs/adr/SADR-NNNN-*.md` records Shiki platform decisions.\n+- `docs/adr/NNNN-*.md` records target product decisions without sharing the\n+  SADR namespace.\n - `AGENTS.md` is the runtime-neutral agent constitution.\n - `CLAUDE.md` is the Claude Code wrapper around `AGENTS.md`.\n - `.shiki/` is the Target Repository mirror for Goals, PRDs, plans, Task DAGs, contracts, locks, worktree records, ledger entries, CCA verdicts, repair packets, reports, and handoffs.\n@@ -15,6 +17,6 @@\n \n - Use Shiki terms exactly when they exist.\n - Add glossary terms to `CONTEXT.md` only when the term is part of the domain, not a generic programming concept.\n-- Create ADRs only for hard-to-reverse, surprising, tradeoff-based decisions.\n+- Create SADRs or target ADRs only for hard-to-reverse, surprising, tradeoff-based decisions.\n - Keep product-specific language in the Target Repository, not in Shiki Template docs.\n-- If an ADR conflicts with a proposed implementation, surface the conflict before editing.\n+- If a SADR or target ADR conflicts with a proposed implementation, surface the conflict before editing.\ndiff --git a/docs/agents/execution-control.md b/docs/agents/execution-control.md\nindex ad8ee3cdb36be56fedc1f46408ca92b387d74047..bc63c3a73661d12b567016ba299f1a6b66bf9088\n--- a/docs/agents/execution-control.md\n+++ b/docs/agents/execution-control.md\n@@ -63,7 +63,7 @@\n | Branch name | Orchestrator | Orchestrator | Before implementer dispatch | branch policy | branch ref in task/ledger |\n | Lock reservation | Context & Impact | MergeGate | Before dispatch and before merge | `.shiki/locks/`, labels, PR metadata | lock id, owner, scope |\n | CI profile | Planner | MergeGate | During issue triage, rechecked at PR | labels, `.shiki/policy.yaml`, workflow matrix | required check list |\n-| Required status checks | Repo maintainer / Guardian | GitHub branch protection / rulesets | Repository setup, updated via ADR | GitHub settings | branch/ruleset config, ADR |\n+| Required status checks | Repo maintainer / Guardian | GitHub branch protection / rulesets | Repository setup, updated via target ADR | GitHub settings | branch/ruleset config, target ADR |\n | CCA judgment | CCA | GitHub required check / MergeGate | Every non-draft PR update | GitHub Action check | structured CCA verdict |\n | Repair assignment | CCA | MergeGate | After failed CCA/CI/review | repair packet, issue/PR comment | repair id and attempt count |\n | Merge readiness | MergeGate | GitHub branch protection / Guardian if needed | After CCA complete | required checks, review rules, rulesets | mergegate check output |\ndiff --git a/docs/agents/guardian-policy.md b/docs/agents/guardian-policy.md\nindex 4147c7adf05a47adb4fd24ada1a1c2e1f2ba6d2d..a20d9cc6c00a569bb057b9ca8d6b51c7fc4a9847\n--- a/docs/agents/guardian-policy.md\n+++ b/docs/agents/guardian-policy.md\n@@ -98,8 +98,8 @@\n `origin/<default_branch>` and reuses each job's existing base fetch; a shallow\n (`--depth`) or second base fetch would make the proof unresolvable and silently\n disable the carry, so neither is permitted. CCA reads `guardian_comment_carried` as\n-a recorded authority in the deterministic signal and does not re-derive it. See ADR\n-0018 for the full decision, the rejected diff/patch-equality and path-disjointness\n+a recorded authority in the deterministic signal and does not re-derive it. See\n+SADR-0018 for the full decision, the rejected diff/patch-equality and path-disjointness\n alternatives, and the accepted residual.\n \n ## External AI Guardian Review (SADR-0010)\ndiff --git a/docs/agents/implementation-policy.md b/docs/agents/implementation-policy.md\nindex c754144a9fb5bf4a38aa82a60e1670c59e626b44..85c37281bcad196c6b7b02343faf00b03cd9c57a\n--- a/docs/agents/implementation-policy.md\n+++ b/docs/agents/implementation-policy.md\n@@ -31,10 +31,13 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n ## Non-Negotiables\n \n 1. **Planning precedes implementation.** Non-trivial Goals start with `grill-with-docs` and Context & Impact.\n-2. **Domain language is controlled.** Terms must be checked against `CONTEXT.md`; hard-to-reverse tradeoffs must be checked against ADRs.\n+2. **Domain language is controlled.** Terms must be checked against `CONTEXT.md`; hard-to-reverse tradeoffs must be checked against Shiki SADRs and target ADRs.\n 3. **PRD precedes issue decomposition.** `to-prd` is used once the context is settled enough to become durable intent.\n 4. **Issues are vertical slices.** `to-issues` must produce independently grabbable tracer-bullet tasks, not horizontal layer tickets.\n 5. **Triage controls dispatch.** Only `ready-for-agent` / AFK tasks may be assigned to Codex without human input.\n@@ -95,19 +98,19 @@\n Process:\n \n - Challenge terms against `CONTEXT.md`.\n-- Check ADRs before reopening decisions.\n+- Check SADRs and target ADRs before reopening decisions.\n - Inspect code when code can answer a question.\n - Ask one question at a time when the operator is available.\n - Provide a recommended answer with each question.\n - Record resolved terms in `CONTEXT.md`.\n-- Create ADRs only for hard-to-reverse, surprising, tradeoff-based decisions.\n+- Create SADRs or target ADRs only for hard-to-reverse, surprising, tradeoff-based decisions.\n - Convert unresolved load-bearing questions into HITL blockers.\n \n Outputs:\n \n - Resolved design-tree decisions.\n - Updated glossary terms when needed.\n-- ADRs when justified.\n+- SADRs or target ADRs when justified.\n - HITL blockers when decisions remain unresolved.\n \n ## Phase 3 \u2014 Context & Impact\n@@ -117,7 +120,7 @@\n \n Outputs must include:\n \n-- Relevant documents and ADRs.\n+- Relevant documents, SADRs, and target ADRs.\n - Relevant modules, interfaces, seams, callers, and tests.\n - Candidate locks.\n - Dependencies.\n@@ -140,7 +143,7 @@\n - Testing decisions.\n - Out of scope.\n - Further notes.\n-- Links to relevant Goal, domain terms, and ADRs.\n+- Links to relevant Goal, domain terms, SADRs, and target ADRs.\n \n Phase 4 ends with Spec Freeze: the operator explicitly approves the PRD, the\n plan records a `spec_freeze` block (status, approved_by, source), and the SF\n@@ -240,7 +243,7 @@\n - Dependencies and locks.\n - Risk and Guardian requirements.\n - Ledger completeness.\n-- Documentation and ADR updates.\n+- Documentation and SADR/target-ADR updates.\n - Scope drift.\n \n CCA emits one of:\n@@ -301,7 +304,7 @@\n | Evidence failure | Behavior might be done, but proof is missing. | Add checks, PR evidence, ledger entry, or artifacts. |\n | Scope drift | PR changes unrelated behavior. | Revert unrelated changes or split into new Goal. |\n | Dependency block | Upstream task or lock not complete. | Block until dependency resolves. |\n-| Architecture block | Decision needs ADR or deep-module discussion. | `grill-with-docs` or `improve-codebase-architecture`. |\n+| Architecture block | Decision needs a SADR, target ADR, or deep-module discussion. | `grill-with-docs` or `improve-codebase-architecture`. |\n | Risk block | Security/production/policy change needs human approval. | Guardian decision. |\n \n ## Phase 5.5 \u2014 Execution Decision Control\ndiff --git a/docs/agents/runtime-auth-model.md b/docs/agents/runtime-auth-model.md\nindex 6a2e1a47e81ea89b5c68a210ec07772a9374babc..3c54748d24233dbd0ba8473950282547ef4ce935\n--- a/docs/agents/runtime-auth-model.md\n+++ b/docs/agents/runtime-auth-model.md\n@@ -148,7 +148,7 @@\n \n API-key based runners may be added by a target repository as an explicit extension. They are not the default Shiki template path.\n \n-If a repo opts into API-key mode, record the exception in an ADR and update the repo's `AGENTS.md`, `.shiki/config.yaml`, and workflow permissions.\n+If a repo opts into API-key mode, record the target-specific exception in a target ADR and update the repo's `AGENTS.md`, `.shiki/config.yaml`, and workflow permissions.\n \n ## GitHub Actions Runtime Compatibility\n \ndiff --git a/docs/agents/skill-gate.md b/docs/agents/skill-gate.md\nindex c50398b0d7c60a8598f2a5c88a10015730dced13..3acffe6444617bb1d54b77d4b4708158ec735bc6\n--- a/docs/agents/skill-gate.md\n+++ b/docs/agents/skill-gate.md\n@@ -9,7 +9,7 @@\n | Skill | Trigger | Expected output |\n | --- | --- | --- |\n | `setup-matt-pocock-skills` | First configuring a repo for these skills, or when issue tracker, triage labels, or domain docs are missing. | `docs/agents/` configuration and an `Agent skills` block in `AGENTS.md` or `CLAUDE.md`. |\n-| `grill-with-docs` | Ambiguous plan, unclear terminology, boundary decision, tradeoff, or ADR-worthy design choice. | Clarified decision tree, recommended answers, updates to `CONTEXT.md` or ADRs when decisions crystallize. |\n+| `grill-with-docs` | Ambiguous plan, unclear terminology, boundary decision, tradeoff, or decision-record-worthy design choice. | Clarified decision tree, recommended answers, updates to `CONTEXT.md`, SADRs, or target ADRs when decisions crystallize. |\n | `zoom-out` | Agent lacks a higher-level architectural map. | Map of relevant modules, callers, seams, and domain concepts. |\n | `to-prd` | Settled context should become a PRD. | PRD published to the issue tracker with problem, solution, user stories, decisions, testing, and out-of-scope notes. |\n | `to-issues` | Goal, PRD, or plan must become implementation tickets. | Vertical-slice issues with acceptance criteria, dependencies, and AFK/HITL classification. |\n@@ -17,7 +17,7 @@\n | `tdd` | Feature work or bug fix has observable behavior that can be specified with tests. | Red-green-refactor loop through public interfaces, one vertical slice at a time. |\n | `code-review` | The TDD loop is green and the implementer is about to create or update a PR. Default for every implementation task. | Pre-PR self-review of the task diff with fixes applied on the task branch; findings and resolutions recorded as a ledger entry naming the skill and a `## Pre-PR code review` PR body section. |\n | `diagnose` | Hard bug, failing check, regression, flaky behavior, or performance problem. | Reproduction loop, ranked hypotheses, instrumentation, fix, regression test, post-mortem. |\n-| `improve-codebase-architecture` | Structural friction, testability, AI-navigability, deep-module opportunity, or refactor request. | Architecture candidates and deepening recommendations informed by `CONTEXT.md` and ADRs. |\n+| `improve-codebase-architecture` | Structural friction, testability, AI-navigability, deep-module opportunity, or refactor request. | Architecture candidates and deepening recommendations informed by `CONTEXT.md`, SADRs, and target ADRs. |\n | `prototype` | A throwaway logic or UI prototype is needed to answer a design question. | Clearly marked disposable prototype and captured decision. |\n \n ## MergeGate Interaction\ndiff --git a/docs/operations.md b/docs/operations.md\nindex cf1f452d4c589e38ccd58553bda0ad8684b51eb7..ace48a90ffbec9e5c0a7113bef16a4858bc44b3f\n--- a/docs/operations.md\n+++ b/docs/operations.md\n@@ -89,6 +89,16 @@\n      target history, brought forward by `shiki migrate`, not a template.\n    - **Everything else** (CLI scripts, workflows, schemas, docs) is overwritten.\n \n+   A forced upgrade also migrates the former numeric Shiki decision paths to\n+   `SADR-NNNN-*`. Before the first write, the installer inventories the exact\n+   legacy Shiki paths and authorizes each deletion only when the existing\n+   `.shiki/install-stamp.json` names that path and its SHA-256 digest still\n+   matches the file. An absent, unreadable, malformed, incomplete, or\n+   mismatching stamp reports every blocked path and stops with no filesystem\n+   change. Target-owned `NNNN-*` ADRs are not cleanup candidates. A non-force\n+   install deletes nothing and retains any still-matching legacy stamp entries\n+   so a later forced upgrade can prove ownership.\n+\n    If the target has pending migrations, `--force` refuses and writes nothing \u2014\n    apply migrations first (step 3), then re-run. The run ends with a single\n    summary of every `.new` written, so a half-upgrade is visible.\ndiff --git a/docs/releasing.md b/docs/releasing.md\nindex 10d73828e7dbd64e9d9f6b83bc84d010537ce62a..aafeee8c4d441e1e427efee2ce4f81a647f020e1\n--- a/docs/releasing.md\n+++ b/docs/releasing.md\n@@ -67,7 +67,7 @@\n CLI in `scripts/`. There are no third-party Python runtime dependencies. The CLI\n is intentionally **not** exposed as a `[project.scripts]` console entry point\n because `scripts/shiki.py` is a dependency-free shim and `scripts/` is not\n-packaged as an importable distribution (see the ADR for the rationale).\n+packaged as an importable distribution (see SADR-0007 for the rationale).\n \n Supported install paths:\n \n@@ -155,6 +155,15 @@\n    [docs/operations.md](operations.md#the-new-convention). If the target has\n    pending migrations, `--force` refuses and writes nothing; apply migrations\n    (step 2) first, then re-run.\n+\n+   The SADR namespace upgrade has an additional pre-write gate: each former\n+   numeric Shiki decision path is removed only when the existing install stamp\n+   contains that exact path and a digest matching its current bytes. The\n+   installer aggregates absent/unreadable/malformed-stamp, missing-digest, and\n+   digest-mismatch blockers before raising, so refusal leaves the target tree\n+   byte-identical. Numeric target ADRs are preserved. A successful forced\n+   upgrade installs and stamps `SADR-*` paths and drops the removed legacy paths\n+   from the refreshed stamp.\n 2. Apply repository-local `.shiki/` state migrations with the migrate command:\n \n    ```bash\ndiff --git a/docs/usage.md b/docs/usage.md\nindex b83407ee3970dd395dd893010bc5dbe8bad26051..a130ef6e40dfd8169a5bc701339351097df1a09f\n--- a/docs/usage.md\n+++ b/docs/usage.md\n@@ -67,8 +67,10 @@\n shiki install-target /path/to/target-repo --local-only\n ```\n \n-Use `--force` only when you intentionally want to overwrite existing target\n-files.\n+Use `--force` only when you intentionally want to refresh the target template.\n+For the SADR namespace migration, `--force` deletes only exact legacy Shiki\n+decision paths whose existing install-stamp digest still matches; any ownership\n+blocker stops before the first write. Numeric target ADRs are never candidates.\n \n ## `shiki bootstrap-platform` \u2014 publish the Shiki platform repo\n \ndiff --git a/scripts/guardian_approval_signal.py b/scripts/guardian_approval_signal.py\nindex d059f92b2c82aa2b5d6ca421365537a87886c1cb..16924892714f4f5b9303c0ef0e325a7e70075630\n--- a/scripts/guardian_approval_signal.py\n+++ b/scripts/guardian_approval_signal.py\n@@ -199,8 +199,8 @@\n ) -> Any:\n     \"\"\"Evaluate SADR-0015 Contract Approval for the CCA Guardian signal.\n \n-    The SAME OR that MergeGate applies must reach this deterministic signal: ADR\n-    0015 is explicit that without it, a contract-approved PR still returns\n+    The SAME OR that MergeGate applies must reach this deterministic signal:\n+    SADR-0015 is explicit that without it, a contract-approved PR still returns\n     ``needs_guardian``, the CCA job fails, and the MergeGate policy check never\n     runs \u2014 the gate this change moves would never be reached. This resolves the\n     task the SAME way MergeGate does (the first Shiki task id in the PR body),\ndiff --git a/scripts/shiki_contract_approval.py b/scripts/shiki_contract_approval.py\nindex 941bcb96fdd8b9a85bc0c4f0f04d5dad9d04839c..4a4b418f6527413161db82670bec857a043360e2\n--- a/scripts/shiki_contract_approval.py\n+++ b/scripts/shiki_contract_approval.py\n@@ -142,7 +142,7 @@\n     changed_files_status: Any,\n     registration: Any,\n ) -> ContractApprovalResult:\n-    \"\"\"Whether this PR carries an SADR-0015 Contract Approval.\n+    \"\"\"Whether this PR carries a SADR-0015 Contract Approval.\n \n     The four conditions are evaluated IN ORDER, each failure recorded as a\n     distinct reason string:\ndiff --git a/scripts/shiki_guardian.py b/scripts/shiki_guardian.py\nindex a4ecb79bbda55fd5c8b06adfae2eb99760a03c45..4dd99bbe714e26f421fa19abac7a34ae1b195ecc\n--- a/scripts/shiki_guardian.py\n+++ b/scripts/shiki_guardian.py\n@@ -12,7 +12,7 @@\n # The carry re-derives its proof from git in-process on every judging invocation;\n # it is never read from an artifact, so there is nothing to go stale or be forged.\n # shiki_sync_proof is the ONLY guardian-family module that runs git, and every one\n-# of its invocations is read-only and local (ADR: prove-from-git base sync).\n+# of its invocations is read-only and local (SADR: prove-from-git base sync).\n from shiki_sync_proof import verify_pure_base_sync\n \n GUARDIAN_POLICY_PATH = \".shiki/guardian-policy.json\"\ndiff --git a/scripts/shiki_guardian_review.py b/scripts/shiki_guardian_review.py\nindex dd0f104d457899e0ba5ff1e162011c1e90b6f92b..e10ef58c046324e7ea3a3392feb3048ca77414ce\n--- a/scripts/shiki_guardian_review.py\n+++ b/scripts/shiki_guardian_review.py\n@@ -159,7 +159,7 @@\n     docs_only = _is_docs_only(changed_files)\n     if docs_only and matched == 0:\n         focus.append(\n-            \"Docs/ADR-only change: confirm the change is genuinely documentation/decision-only, introduces no behavior or contract drift, and stays consistent with the constitution and existing ADRs.\"\n+            \"Docs/decision-record-only change: confirm the change is genuinely documentation/decision-only, introduces no behavior or contract drift, and stays consistent with the constitution and existing SADRs and target ADRs.\"\n         )\n \n     # Conservative augmentation: unknown (no category and not docs-only), mixed\ndiff --git a/scripts/shiki_installer.py b/scripts/shiki_installer.py\nindex be064ab7a29ff6836f7f7a1d70500a43e0d11d7b..9fd8eefd137ce7003dad5c0dcb2e48e2cd47ab32\n--- a/scripts/shiki_installer.py\n+++ b/scripts/shiki_installer.py\n@@ -118,6 +118,7 @@\n # what lets a target carry it without a governance-schema change.\n INSTALL_STAMP_PATH = \".shiki/install-stamp.json\"\n INSTALL_STAMP_VERSION = 1\n+LEGACY_SHIKI_SADR_MAX = 18\n \n # Under --force the shipped surface splits three ways, so nothing is lost and\n # nothing is silently stale.\n@@ -237,9 +238,98 @@\n         )\n \n \n+def legacy_shiki_adr_paths() -> tuple[str, ...]:\n+    \"\"\"Former numeric paths for Shiki SADR-0001 through SADR-0018.\n+\n+    The authoritative slugs come from the current SADR filenames. SADR-0019 is\n+    the namespace decision itself and never shipped under a numeric legacy path.\n+    \"\"\"\n+    paths: list[str] = []\n+    for sadr in sorted((ROOT / \"docs\" / \"adr\").glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\")):\n+        number = sadr.name.split(\"-\", 2)[1]\n+        if int(number) <= LEGACY_SHIKI_SADR_MAX:\n+            paths.append(f\"docs/adr/{sadr.name.removeprefix('SADR-')}\")\n+    return tuple(paths)\n+\n+\n+def preflight_legacy_sadr_cleanup(target: Path, *, force: bool) -> tuple[str, ...]:\n+    \"\"\"Authorize exact legacy Shiki ADR removals before the first write.\n+\n+    A non-force install never removes existing files. A forced install needs no\n+    stamp when no legacy Shiki path is present. When legacy paths do exist, each\n+    must be named in a readable install stamp and its current bytes must match\n+    the recorded digest.\n+    \"\"\"\n+    if not force:\n+        return ()\n+    present = tuple(\n+        relative\n+        for relative in legacy_shiki_adr_paths()\n+        if (target / relative).exists() or (target / relative).is_symlink()\n+    )\n+    if not present:\n+        return ()\n+\n+    stamp_path = target / INSTALL_STAMP_PATH\n+    try:\n+        raw = stamp_path.read_text(encoding=\"utf-8\")\n+        stamp = json.loads(raw)\n+    except FileNotFoundError:\n+        detail = \"install stamp is absent\"\n+        blockers = [f\"{relative}: {detail}\" for relative in present]\n+    except (OSError, UnicodeDecodeError) as error:\n+        detail = f\"install stamp is unreadable ({error})\"\n+        blockers = [f\"{relative}: {detail}\" for relative in present]\n+    except json.JSONDecodeError as error:\n+        detail = f\"install stamp is malformed ({error})\"\n+        blockers = [f\"{relative}: {detail}\" for relative in present]\n+    else:\n+        if not isinstance(stamp, dict):\n+            blockers = [\n+                f\"{relative}: install stamp is malformed (expected an object)\"\n+                for relative in present\n+            ]\n+        else:\n+            digests = stamp.get(\"digests\")\n+            blockers = []\n+            if not isinstance(digests, dict):\n+                blockers.extend(\n+                    f\"{relative}: install stamp has no digest for this path\"\n+                    for relative in present\n+                )\n+            else:\n+                for relative in present:\n+                    expected = digests.get(relative)\n+                    if not isinstance(expected, str) or not expected:\n+                        blockers.append(\n+                            f\"{relative}: install stamp has no digest for this path\"\n+                        )\n+                        continue\n+                    try:\n+                        actual = _sha256_file(target / relative)\n+                    except OSError as error:\n+                        blockers.append(\n+                            f\"{relative}: legacy path is unreadable ({error})\"\n+                        )\n+                        continue\n+                    if actual != expected:\n+                        blockers.append(\n+                            f\"{relative}: digest mismatch (stamp {expected}, current {actual})\"\n+                        )\n+\n+    if blockers:\n+        joined = \"\\n\".join(f\"- {blocker}\" for blocker in blockers)\n+        raise ShikiError(\n+            \"legacy Shiki ADR cleanup is not authorized:\\n\"\n+            f\"{joined}\\nNo files were written.\"\n+        )\n+    return present\n+\n+\n def install_template(target: Path, *, force: bool, validate: bool) -> list[NewFileNote]:\n     # Preconditions first: never rewrite the tree of a target that cannot be\n     # upgraded cleanly (see refuse_pending_migrations).\n+    legacy_cleanup = preflight_legacy_sadr_cleanup(target, force=force)\n     refuse_pending_migrations(target)\n \n     # Resolve the target's required CODEOWNERS owner once from its\n@@ -250,6 +340,10 @@\n     # verbatim. See ``codeowners_required_owner``.\n     codeowners_owner = codeowners_required_owner(target)\n \n+    for relative in legacy_cleanup:\n+        (target / relative).unlink()\n+        info(f\"removed stamped legacy Shiki ADR: {target / relative}\")\n+\n     notes: list[NewFileNote] = []\n     for relative in TEMPLATE_PATHS:\n         source = ROOT / relative\n@@ -647,6 +741,22 @@\n     digests = target_stamp_digests(target)\n     existing = read_install_stamp(target)\n     if existing is not None:\n+        # A non-force install deliberately leaves legacy Shiki ADRs in place.\n+        # Preserve only still-matching ownership proof so a later forced\n+        # upgrade can authorize cleanup; never carry a missing or stale digest.\n+        existing_digests = existing.get(\"digests\")\n+        if isinstance(existing_digests, dict):\n+            for relative in legacy_shiki_adr_paths():\n+                expected = existing_digests.get(relative)\n+                path = target / relative\n+                if not isinstance(expected, str) or not path.is_file():\n+                    continue\n+                try:\n+                    actual = _sha256_file(path)\n+                except OSError:\n+                    continue\n+                if actual == expected:\n+                    digests[relative] = actual\n         if _incoming_is_older(commit, existing.get(\"platform_commit\")):\n             warn(\n                 \"kept existing install stamp: incoming install \"\ndiff --git a/scripts/shiki_loop.py b/scripts/shiki_loop.py\nindex 3611641da8aeb5038b1da90e3114b09a473ec6d8..84caa491b9cad0edc4176f209c13e9221e975681\n--- a/scripts/shiki_loop.py\n+++ b/scripts/shiki_loop.py\n@@ -424,7 +424,7 @@\n         # unresolvable CCA verdict has already produced its terminal stop and is never\n         # laundered into a head-moving sync (SADR-0010). It also never fires while the\n         # policy (Guardian) gate is red \u2014 for ANY reason, not only when it is the sole\n-        # red check \u2014 so a head-bound Guardian approval, or an SADR-0015\n+        # red check \u2014 so a head-bound Guardian approval, or a SADR-0015\n         # contract-approval carry that base movement defeated (the policy gate compares\n         # base and head task governance directly), is never silently restored by\n         # adopting base wholesale. It is bounded by a durable per-task counter so the\ndiff --git a/scripts/shiki_tasks.py b/scripts/shiki_tasks.py\nindex aec3809f24b4a0c8c96fc50971cff9ae9ad30381..d531052e9d9717058d4631483cefab44c26a65c1\n--- a/scripts/shiki_tasks.py\n+++ b/scripts/shiki_tasks.py\n@@ -821,7 +821,7 @@\n         \"prompt\": prompt,\n         \"entry_skill\": \"grill-with-docs\",\n         \"required_next_steps\": [\n-            \"Run grill-with-docs until terminology, boundaries, risks, and ADR-worthy decisions are settled.\",\n+            \"Run grill-with-docs until terminology, boundaries, risks, and decision-record-worthy choices are settled.\",\n             \"Obtain the operator's explicit approval of the PRD/requirements (Spec Freeze).\",\n             \"Write a machine-readable plan JSON with grill_with_docs.status=complete and spec_freeze.status=frozen.\",\n             \"Run shiki plan ingest --plan-file PLAN.json.\",\ndiff --git a/scripts/test_shiki_goal_loop.sh b/scripts/test_shiki_goal_loop.sh\nindex 753c7b516e10f0be3bc92a4bd0eb1fa699776ae6..8f3820a0a079b3044424cfe1a6c6b7fe68896e67\n--- a/scripts/test_shiki_goal_loop.sh\n+++ b/scripts/test_shiki_goal_loop.sh\n@@ -231,8 +231,8 @@\n \n # High/critical risk merges autonomously when all required checks are green:\n # the \"MergeGate policy check\" required check IS the Guardian gate (it enforces\n-# guardian-policy.json \u2014 human approval OR external AI guardian review, ADR\n-# 0010), so green checks mean a recorded authority approved. The fake gh returns\n+# guardian-policy.json \u2014 human approval OR external AI guardian review,\n+# SADR-0010), so green checks mean a recorded authority approved. The fake gh returns\n # green checks, simulating that the Guardian gate passed.\n python3 \"$ROOT/scripts/shiki.py\" goal create --target \"$TARGET\" --title \"Guardian gate\" --outcome \"High risk merges via policy gate\" >/tmp/shiki-goal-loop-guardian-goal.json\n GGOAL=\"$(json_get /tmp/shiki-goal-loop-guardian-goal.json goal_id)\"\ndiff --git a/tests/test_adr_references.py b/tests/test_adr_references.py\nindex 51706329c797d5dd9b47e082a333fa83b261d482..db6e693e35f67103935be34f1d9bb4ff7ee60395\n--- a/tests/test_adr_references.py\n+++ b/tests/test_adr_references.py\n@@ -1,7 +1,7 @@\n-\"\"\"Every ``ADR ####`` citation must resolve to exactly one decision record.\n+\"\"\"Every target ADR and Shiki SADR citation resolves in its own namespace.\n \n Background. ``scripts/mergegate_check.py`` justifies ``is_bookkeeping_closeout``\n-with \"ADR 0017\" nine times and ``scripts/guardian_approval_signal.py`` cites\n+with \"SADR-0017\" nine times and ``scripts/guardian_approval_signal.py`` cites\n 0016/0017 seven times, yet for a stretch the numbering in ``docs/adr/`` stopped\n at 0015: the authoring commits lived on a coordinator branch and no task ever\n declared ``docs/adr/...`` in its locks, so nothing carried the records to the\n@@ -11,11 +11,12 @@\n and the directory drifted with no signal.\n \n This is the same unbound-pair shape behind other silent failures: a reference\n-and its target with nothing asserting they agree. This test binds them. It scans\n-every ``scripts/*.py`` (plus ``docs/**/*.md`` and the root ``*.md`` governance\n-files, which are cheap) for the ``ADR ####`` pattern and asserts each cited\n-number resolves to exactly one ``docs/adr/####-*.md``. The converse is\n-deliberately NOT required: an ADR may exist without being cited.\n+and its target with nothing asserting they agree. This test binds them. Target\n+``ADR NNNN`` citations resolve only to ``docs/adr/NNNN-*.md``; Shiki\n+``SADR-NNNN`` citations resolve only to ``docs/adr/SADR-NNNN-*.md``. The scan\n+covers current governance docs, scripts, workflows, prompts, packaging metadata,\n+and repo-local Codex guidance while deliberately excluding historical\n+``.shiki`` evidence. The converse is not required: a record may be uncited.\n \n The suite is dependency free: standard-library ``unittest`` only.\n \"\"\"\n@@ -31,17 +32,18 @@\n \n REPO_ROOT = Path(__file__).resolve().parents[1]\n \n-# \"ADR 0016\" -- a four-digit citation. The trailing \\b keeps a five-digit token\n-# from being read as a truncated four-digit number.\n-_CITATION = re.compile(r\"\\bADR (\\d{4})\\b\")\n-# \"0016-locks-bound-product-paths.md\" -- the leading four digits name the record.\n-_ADR_FILENAME = re.compile(r\"^(\\d{4})-.*\\.md$\")\n+# ``ADR 0016`` / ``SADR-0016`` -- a namespace plus four-digit citation. The\n+# trailing ``\\b`` keeps a five-digit token from being read as a truncated one.\n+_CITATION = re.compile(r\"\\b(S?ADR)[ -](\\d{4})\\b\")\n+# Target ADRs retain the numeric filename; Shiki platform decisions use SADR.\n+_DECISION_FILENAME = re.compile(r\"^(?:(SADR)-)?(\\d{4})-.*\\.md$\")\n \n \n class Violation(NamedTuple):\n     \"\"\"A citation that does not resolve to exactly one decision record.\"\"\"\n \n     citing_file: str  # repo-relative path of the file that cites the ADR\n+    namespace: str  # \"ADR\" for target decisions; \"SADR\" for Shiki decisions\n     number: str  # the cited four-digit number, e.g. \"0016\"\n     line: int  # 1-based line of the citation\n     reason: str  # \"missing\" (zero matches) or \"ambiguous\" (two or more)\n@@ -49,44 +51,59 @@\n \n     @property\n     def expected_path(self) -> str:\n-        return f\"docs/adr/{self.number}-*.md\"\n+        prefix = \"SADR-\" if self.namespace == \"SADR\" else \"\"\n+        return f\"docs/adr/{prefix}{self.number}-*.md\"\n \n     def describe(self) -> str:\n         \"\"\"Name the citing file, the cited number, and the missing path.\"\"\"\n         if self.reason == \"missing\":\n             return (\n-                f\"{self.citing_file}:{self.line} cites ADR {self.number} \"\n+                f\"{self.citing_file}:{self.line} cites {self.namespace} {self.number} \"\n                 f\"but no document matches {self.expected_path}\"\n             )\n         joined = \", \".join(self.matches)\n         return (\n-            f\"{self.citing_file}:{self.line} cites ADR {self.number} which \"\n+            f\"{self.citing_file}:{self.line} cites {self.namespace} {self.number} which \"\n             f\"resolves to {len(self.matches)} documents (expected exactly one \"\n             f\"at {self.expected_path}): {joined}\"\n         )\n \n \n-def _adr_index(root: Path) -> dict[str, list[Path]]:\n-    \"\"\"Map every four-digit ADR number to the record files that claim it.\"\"\"\n-    index: dict[str, list[Path]] = {}\n+def _adr_index(root: Path) -> dict[tuple[str, str], list[Path]]:\n+    \"\"\"Map each decision namespace and number to the files that claim it.\"\"\"\n+    index: dict[tuple[str, str], list[Path]] = {}\n     adr_dir = root / \"docs\" / \"adr\"\n     if adr_dir.is_dir():\n         for path in sorted(adr_dir.glob(\"*.md\")):\n-            match = _ADR_FILENAME.match(path.name)\n+            match = _DECISION_FILENAME.match(path.name)\n             if match:\n-                index.setdefault(match.group(1), []).append(path)\n+                namespace = \"SADR\" if match.group(1) else \"ADR\"\n+                index.setdefault((namespace, match.group(2)), []).append(path)\n     return index\n \n \n def _citing_files(root: Path, include_docs: bool = True) -> list[Path]:\n-    \"\"\"Files scanned for citations: scripts/*.py, and cheaply the markdown.\"\"\"\n+    \"\"\"Current, shipped text surfaces scanned for decision citations.\"\"\"\n     files = sorted((root / \"scripts\").glob(\"*.py\"))\n+    files += sorted((root / \"scripts\").glob(\"*.sh\"))\n     if include_docs:\n         docs = root / \"docs\"\n         if docs.is_dir():\n             files += sorted(docs.rglob(\"*.md\"))\n         files += sorted(root.glob(\"*.md\"))\n-    return files\n+        pyproject = root / \"pyproject.toml\"\n+        if pyproject.is_file():\n+            files.append(pyproject)\n+        for relative in (\".github\", \".codex\"):\n+            surface = root / relative\n+            if surface.is_dir():\n+                files += sorted(\n+                    path\n+                    for path in surface.rglob(\"*\")\n+                    if path.is_file()\n+                    and path.suffix in {\".md\", \".py\", \".sh\", \".toml\", \".yaml\", \".yml\"}\n+                )\n+    return sorted(set(files))\n \n \n def find_adr_reference_violations(\n@@ -94,10 +111,9 @@\n ) -> list[Violation]:\n     \"\"\"Return every ``ADR ####`` citation under ``root`` that fails to resolve.\n \n-    A citation resolves when exactly one ``docs/adr/####-*.md`` matches its\n-    number. Zero matches is a \"missing\" violation; two or more is \"ambiguous\".\n-    Records that are never cited produce nothing -- the converse is not\n-    required.\n+    A citation resolves when exactly one record in the same namespace matches\n+    its number. Zero matches is a \"missing\" violation; two or more is\n+    \"ambiguous\". Records that are never cited produce nothing.\n     \"\"\"\n     root = Path(root)\n     index = _adr_index(root)\n@@ -110,13 +126,15 @@\n         rel = path.relative_to(root).as_posix()\n         for lineno, line in enumerate(text.splitlines(), start=1):\n             for match in _CITATION.finditer(line):\n-                number = match.group(1)\n-                found = index.get(number, [])\n+                namespace, number = match.groups()\n+                found = index.get((namespace, number), [])\n                 if len(found) == 1:\n                     continue\n                 reason = \"missing\" if not found else \"ambiguous\"\n                 rels = [p.relative_to(root).as_posix() for p in found]\n-                violations.append(Violation(rel, number, lineno, reason, rels))\n+                violations.append(\n+                    Violation(rel, namespace, number, lineno, reason, rels)\n+                )\n     return violations\n \n \n@@ -129,8 +147,8 @@\n         self.assertEqual(\n             violations,\n             [],\n-            \"Every 'ADR ####' citation must resolve to exactly one \"\n-            f\"docs/adr/####-*.md:\\n{detail}\",\n+            \"Every ADR/SADR citation must resolve exactly once in its own \"\n+            f\"docs/adr namespace:\\n{detail}\",\n         )\n \n     def test_scan_actually_reads_scripts(self) -> None:\n@@ -146,8 +164,21 @@\n         # The two records this Goal carried onto the default branch, and which\n         # the scripts cite most, must be present.\n         index = _adr_index(REPO_ROOT)\n-        self.assertIn(\"0016\", index)\n-        self.assertIn(\"0017\", index)\n+        self.assertIn((\"SADR\", \"0016\"), index)\n+        self.assertIn((\"SADR\", \"0017\"), index)\n+\n+    def test_platform_tree_has_only_sadr_0001_through_0019(self) -> None:\n+        adr_dir = REPO_ROOT / \"docs\" / \"adr\"\n+        sadrs = sorted(adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\"))\n+        self.assertEqual(\n+            {path.name.split(\"-\", 2)[1] for path in sadrs},\n+            {f\"{number:04d}\" for number in range(1, 20)},\n+        )\n+        self.assertEqual(\n+            list(adr_dir.glob(\"[0-9][0-9][0-9][0-9]-*.md\")),\n+            [],\n+            \"the Shiki platform tree must not retain legacy numeric SADR paths\",\n+        )\n \n \n class FixtureAdrReferences(unittest.TestCase):\n@@ -180,6 +211,26 @@\n         self.assertIn(\"9991\", message)  # cited number\n         self.assertIn(\"docs/adr/9991-*.md\", message)  # missing path\n \n+    def test_target_adr_does_not_satisfy_missing_platform_sadr(self) -> None:\n+        root = self._make_repo(\n+            adr_names=[\"0042-target-decision.md\"],\n+            script_body='\"\"\"Target ADR 0042 exists, but platform SADR-0042 does not.\"\"\"\\n',\n+        )\n+        violations = find_adr_reference_violations(root, include_docs=False)\n+        self.assertEqual(len(violations), 1)\n+        self.assertEqual(violations[0].namespace, \"SADR\")\n+        self.assertEqual(violations[0].number, \"0042\")\n+        self.assertEqual(violations[0].expected_path, \"docs/adr/SADR-0042-*.md\")\n+\n+    def test_target_adr_and_platform_sadr_may_reuse_a_number(self) -> None:\n+        root = self._make_repo(\n+            adr_names=[\"0042-target-decision.md\", \"SADR-0042-platform-decision.md\"],\n+            script_body='\"\"\"Target ADR 0042 and platform SADR-0042 are distinct.\"\"\"\\n',\n+        )\n+        self.assertEqual(\n+            find_adr_reference_violations(root, include_docs=False), []\n+        )\n+\n     def test_uncited_adr_is_allowed(self) -> None:\n         # An ADR exists but nothing cites it -> not a violation. The converse\n         # (every ADR must be cited) is explicitly NOT required.\ndiff --git a/tests/test_guardian_review.py b/tests/test_guardian_review.py\nindex 88829e7810212635e2fc98aa5918d6f26294677d..00a85a237acd5bf2d6cb5b294128198c422402ca\n--- a/tests/test_guardian_review.py\n+++ b/tests/test_guardian_review.py\n@@ -162,7 +162,7 @@\n \n     def test_docs_only_is_light_and_not_conservative(self):\n         focus = gr.classify_review_focus([\"docs/adr/0099-x.md\", \"README.md\"], \"\", \"low\")\n-        self.assertTrue(any(\"Docs/ADR-only\" in f for f in focus))\n+        self.assertTrue(any(\"Docs/decision-record-only\" in f for f in focus))\n         self.assertFalse(any(\"conservative default\" in f.lower() for f in focus))\n \n     def test_unknown_category_is_conservative(self):\ndiff --git a/tests/test_policy_job_merged_prs.py b/tests/test_policy_job_merged_prs.py\nindex d45512e74681e5e649279236c7d4e79d5c3da471..b97989f8bd74d091de1a6b5275a20d8dc68c4512\n--- a/tests/test_policy_job_merged_prs.py\n+++ b/tests/test_policy_job_merged_prs.py\n@@ -3,8 +3,8 @@\n \n ``.github/workflows/shiki-cca-completion.yml`` has two consumers of the merged-PR\n proof: the CCA *signal* (``guardian_approval_signal.py`` in \"Prepare CCA context\")\n-and the authoritative gate (``mergegate_check.py`` in \"Enforce MergeGate\"). ADR\n-0017's bookkeeping-closeout exemption (condition 6) needs the merged implementation\n+and the authoritative gate (``mergegate_check.py`` in \"Enforce MergeGate\").\n+SADR-0017's bookkeeping-closeout exemption (condition 6) needs the merged implementation\n PR proven, and a closeout carries NO ``post_merge_reconcile`` marker. The signal\n already resolves ``MERGED_PRS`` unconditionally, but \"Enforce MergeGate\" used to\n resolve it only inside ``grep -q '<!-- shiki:post_merge_reconcile -->'``. So a\ndiff --git a/tests/test_sadr_install_migration.py b/tests/test_sadr_install_migration.py\nnew file mode 100644\nindex 0000000000000000000000000000000000000000..a6f4c57e4dcaef73ef600b0d4804744138ab9071\n--- /dev/null\n+++ b/tests/test_sadr_install_migration.py\n@@ -0,0 +1,229 @@\n+\"\"\"SADR installation and collision-safe legacy upgrade behavior.\"\"\"\n+\n+from __future__ import annotations\n+\n+import contextlib\n+import hashlib\n+import io\n+import json\n+import shutil\n+import tempfile\n+import unittest\n+from pathlib import Path\n+\n+import shiki_test_support  # noqa: F401  (path bootstrap)\n+\n+import shiki_installer\n+from tests.test_adr_references import find_adr_reference_violations\n+\n+\n+PRODUCT_NUMBERS = tuple(range(13, 19))\n+\n+\n+def _quiet_install(target: Path, *, force: bool, validate: bool) -> None:\n+    with contextlib.redirect_stdout(io.StringIO()):\n+        shiki_installer.install_template(target, force=force, validate=validate)\n+\n+\n+def _sha256(path: Path) -> str:\n+    return hashlib.sha256(path.read_bytes()).hexdigest()\n+\n+\n+def _legacy_sadr_paths() -> tuple[str, ...]:\n+    return tuple(\n+        f\"docs/adr/{path.name.removeprefix('SADR-')}\"\n+        for path in sorted((shiki_installer.ROOT / \"docs\" / \"adr\").glob(\"SADR-*.md\"))\n+        if path.name != \"SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\"\n+    )\n+\n+\n+def _create_legacy_upgrade_fixture(\n+    target: Path,\n+) -> tuple[dict[str, bytes], tuple[str, ...]]:\n+    adr_dir = target / \"docs\" / \"adr\"\n+    adr_dir.mkdir(parents=True)\n+    product_bytes = {\n+        f\"{number:04d}-cubeshare-product-decision.md\": (\n+            f\"# CubeShare ADR {number:04d}\\n\\nDo not rewrite {number}.\\n\"\n+        ).encode()\n+        for number in PRODUCT_NUMBERS\n+    }\n+    for name, content in product_bytes.items():\n+        (adr_dir / name).write_bytes(content)\n+\n+    legacy_paths = _legacy_sadr_paths()\n+    for relative in legacy_paths:\n+        (target / relative).write_bytes(f\"legacy Shiki bytes for {relative}\\n\".encode())\n+\n+    stamp_path = target / shiki_installer.INSTALL_STAMP_PATH\n+    stamp_path.parent.mkdir(parents=True, exist_ok=True)\n+    stamp = {\n+        \"version\": shiki_installer.INSTALL_STAMP_VERSION,\n+        \"platform_commit\": None,\n+        \"installed_at\": \"2000-01-01T00:00:00+00:00\",\n+        \"digests\": {\n+            relative: _sha256(target / relative) for relative in legacy_paths\n+        },\n+    }\n+    stamp_path.write_text(\n+        json.dumps(stamp, indent=2, sort_keys=True) + \"\\n\", encoding=\"utf-8\"\n+    )\n+    return product_bytes, legacy_paths\n+\n+\n+def _tree_snapshot(root: Path) -> dict[str, tuple[str, bytes | str]]:\n+    \"\"\"Recursive byte/shape snapshot used to prove preflight is write-free.\"\"\"\n+    snapshot: dict[str, tuple[str, bytes | str]] = {}\n+    for path in sorted(root.rglob(\"*\")):\n+        relative = path.relative_to(root).as_posix()\n+        if path.is_symlink():\n+            snapshot[relative] = (\"symlink\", path.readlink().as_posix())\n+        elif path.is_dir():\n+            snapshot[relative] = (\"directory\", \"\")\n+        else:\n+            snapshot[relative] = (\"file\", path.read_bytes())\n+    return snapshot\n+\n+\n+class FreshSadrInstallTests(unittest.TestCase):\n+    def setUp(self) -> None:\n+        self.tmp = Path(tempfile.mkdtemp(prefix=\"shiki-sadr-fresh-\"))\n+        self.addCleanup(lambda: shutil.rmtree(self.tmp, ignore_errors=True))\n+        self.target = self.tmp / \"target\"\n+        self.adr_dir = self.target / \"docs\" / \"adr\"\n+        self.adr_dir.mkdir(parents=True)\n+        self.product_bytes = {\n+            f\"{number:04d}-product-decision.md\": (\n+                f\"# Product ADR {number:04d}\\n\\nTarget-owned bytes {number}.\\n\"\n+            ).encode()\n+            for number in PRODUCT_NUMBERS\n+        }\n+        for name, content in self.product_bytes.items():\n+            (self.adr_dir / name).write_bytes(content)\n+\n+    def test_fresh_install_preserves_product_adrs_and_installs_all_sadrs(self) -> None:\n+        _quiet_install(self.target, force=False, validate=True)\n+\n+        for name, expected in self.product_bytes.items():\n+            self.assertEqual((self.adr_dir / name).read_bytes(), expected)\n+\n+        sadr_numbers = {\n+            path.name.split(\"-\", 2)[1]\n+            for path in self.adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\")\n+        }\n+        self.assertEqual(sadr_numbers, {f\"{number:04d}\" for number in range(1, 20)})\n+\n+        numeric_records = {\n+            path.name\n+            for path in self.adr_dir.glob(\"[0-9][0-9][0-9][0-9]-*.md\")\n+        }\n+        self.assertEqual(numeric_records, set(self.product_bytes))\n+        self.assertEqual(find_adr_reference_violations(self.target), [])\n+\n+\n+class LegacySadrUpgradeTests(unittest.TestCase):\n+    def setUp(self) -> None:\n+        self.tmp = Path(tempfile.mkdtemp(prefix=\"shiki-sadr-upgrade-\"))\n+        self.addCleanup(lambda: shutil.rmtree(self.tmp, ignore_errors=True))\n+        self.target = self.tmp / \"target\"\n+        self.adr_dir = self.target / \"docs\" / \"adr\"\n+        self.product_bytes, self.legacy_paths = _create_legacy_upgrade_fixture(\n+            self.target\n+        )\n+\n+    def test_force_upgrade_deletes_only_stamped_legacy_sadrs(self) -> None:\n+        _quiet_install(self.target, force=True, validate=True)\n+\n+        for relative in self.legacy_paths:\n+            self.assertFalse((self.target / relative).exists(), relative)\n+        for name, expected in self.product_bytes.items():\n+            self.assertEqual((self.adr_dir / name).read_bytes(), expected)\n+\n+        sadrs = list(self.adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\"))\n+        self.assertEqual(len(sadrs), 19)\n+        refreshed = json.loads(\n+            (self.target / shiki_installer.INSTALL_STAMP_PATH).read_text(encoding=\"utf-8\")\n+        )\n+        for relative in self.legacy_paths:\n+            self.assertNotIn(relative, refreshed[\"digests\"])\n+        for path in sadrs:\n+            relative = path.relative_to(self.target).as_posix()\n+            self.assertEqual(refreshed[\"digests\"][relative], _sha256(path))\n+\n+    def test_non_force_install_never_deletes_legacy_files(self) -> None:\n+        before = {\n+            relative: (self.target / relative).read_bytes()\n+            for relative in self.legacy_paths\n+        }\n+        _quiet_install(self.target, force=False, validate=False)\n+        for relative, expected in before.items():\n+            self.assertEqual((self.target / relative).read_bytes(), expected)\n+        refreshed = json.loads(\n+            (self.target / shiki_installer.INSTALL_STAMP_PATH).read_text(encoding=\"utf-8\")\n+        )\n+        for relative in self.legacy_paths:\n+            self.assertEqual(refreshed[\"digests\"][relative], _sha256(self.target / relative))\n+\n+\n+class LegacyCleanupPreflightTests(unittest.TestCase):\n+    def setUp(self) -> None:\n+        self.tmp = Path(tempfile.mkdtemp(prefix=\"shiki-sadr-preflight-\"))\n+        self.addCleanup(lambda: shutil.rmtree(self.tmp, ignore_errors=True))\n+\n+    def test_all_ownership_blockers_abort_before_any_write(self) -> None:\n+        cases = (\n+            (\"absent\", \"install stamp is absent\"),\n+            (\"unreadable\", \"install stamp is unreadable\"),\n+            (\"malformed\", \"install stamp is malformed\"),\n+            (\"missing-digest\", \"install stamp has no digest\"),\n+            (\"digest-mismatch\", \"digest mismatch\"),\n+        )\n+        for name, expected_reason in cases:\n+            with self.subTest(name=name):\n+                target = self.tmp / name\n+                _, legacy_paths = _create_legacy_upgrade_fixture(target)\n+                stamp_path = target / shiki_installer.INSTALL_STAMP_PATH\n+\n+                if name == \"absent\":\n+                    stamp_path.unlink()\n+                elif name == \"unreadable\":\n+                    stamp_path.unlink()\n+                    stamp_path.mkdir()\n+                elif name == \"malformed\":\n+                    stamp_path.write_text(\"{not-json\\n\", encoding=\"utf-8\")\n+                else:\n+                    stamp = json.loads(stamp_path.read_text(encoding=\"utf-8\"))\n+                    if name == \"missing-digest\":\n+                        stamp[\"digests\"] = {}\n+                    else:\n+                        stamp[\"digests\"] = {\n+                            relative: \"deadbeef\" for relative in legacy_paths\n+                        }\n+                    stamp_path.write_text(\n+                        json.dumps(stamp, indent=2, sort_keys=True) + \"\\n\",\n+                        encoding=\"utf-8\",\n+                    )\n+\n+                before = _tree_snapshot(target)\n+                with self.assertRaises(shiki_installer.ShikiError) as raised:\n+                    _quiet_install(target, force=True, validate=False)\n+                after = _tree_snapshot(target)\n+\n+                self.assertEqual(before, after, f\"{name} preflight changed the tree\")\n+                message = str(raised.exception)\n+                self.assertIn(expected_reason, message)\n+                self.assertIn(\"No files were written\", message)\n+                for relative in legacy_paths:\n+                    self.assertIn(relative, message)\n+\n+    def test_absent_stamp_is_allowed_when_no_legacy_path_exists(self) -> None:\n+        target = self.tmp / \"no-legacy\"\n+        target.mkdir()\n+        _quiet_install(target, force=True, validate=False)\n+        self.assertTrue(\n+            (target / \"docs\" / \"adr\" / \"SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\").is_file()\n+        )\n+\n+\n+if __name__ == \"__main__\":\n+    unittest.main()\n\n2026-08-07T08:16:43.624839Z ERROR codex_models_manager::manager: failed to renew cache TTL: missing field `base_instructions` at line 94 column 5\ndiff --git a/.codex/skills/shiki/SKILL.md b/.codex/skills/shiki/SKILL.md\nindex c8e2e85d9d75d49cc4f2d6286ee22659c4e53e75..93cded623b2c15d9f65c76491a1c2d813ac9e6c0\n--- a/.codex/skills/shiki/SKILL.md\n+++ b/.codex/skills/shiki/SKILL.md\n@@ -18,6 +18,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n Codex CLI does not register this skill as a custom `/shiki` slash command. When\n the user types `/shiki` in Codex CLI and gets \"Unrecognized command\", explain\n that this is expected: invoke Shiki in Codex with natural language such as\n@@ -69,7 +72,7 @@\n \n ## Responsibilities\n \n-- Claude Code implements and repairs by default (`shiki runner claude`, ADR 0008); Codex implements and repairs tasks explicitly assigned to `codex`.\n+- Claude Code implements and repairs by default (`shiki runner claude`, SADR-0008); Codex implements and repairs tasks explicitly assigned to `codex`.\n - Claude Code Action can act as GitHub-side CCA or reviewer.\n - CCA judges completion.\n - MergeGate authorizes state transitions and merge readiness.\ndiff --git a/.github/prompts/cca-completion-check.md b/.github/prompts/cca-completion-check.md\nindex ed203ad5f302ecc81372d8ab27d2b1232ca97497..fd89950528333ef6242e6d5443904f5392ab7e56\n--- a/.github/prompts/cca-completion-check.md\n+++ b/.github/prompts/cca-completion-check.md\n@@ -19,6 +19,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n ## Required Reading\n \n Read only the files needed to judge the PR contract. Prefer this order:\ndiff --git a/.github/prompts/codex-task-handoff.md b/.github/prompts/codex-task-handoff.md\nindex 695e211d76dea1c60e8d5bb360e78487808286a4..59441d576b01a39569722f226c854e5a740e9eb5\n--- a/.github/prompts/codex-task-handoff.md\n+++ b/.github/prompts/codex-task-handoff.md\n@@ -2,7 +2,7 @@\n \n You are Codex Front acting as the Implementer for a Shiki task.\n \n-Follow `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant ADRs, and `docs/agents/codex-handoff.md`.\n+Follow `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant Shiki SADRs and target ADRs, and `docs/agents/codex-handoff.md`.\n \n Rules:\n \ndiff --git a/AGENTS.md b/AGENTS.md\nindex 7f041100d49c2a7525f51cb0f5d696a4a6d1c78c..a06bfbabc6fa48a007389e25e12d7ed523936bed\n--- a/AGENTS.md\n+++ b/AGENTS.md\n@@ -22,7 +22,7 @@\n - **Codex Front** is an optional implementer through Codex App, Codex CLI, Codex IDE extension, or Codex Web signed in with ChatGPT OAuth/subscription auth, dispatched with `shiki runner codex` when a task is explicitly assigned to `codex`.\n - **GitHub CCA** is implemented by Claude Code Action by default, using `CLAUDE_CODE_OAUTH_TOKEN`.\n \n-Do not assume `openai/codex-action`, `OPENAI_API_KEY`, or API-key based Codex automation in the default Shiki path. API-key runners are explicit target-repo extensions and require their own ADR.\n+Do not assume `openai/codex-action`, `OPENAI_API_KEY`, or API-key based Codex automation in the default Shiki path. API-key runners are explicit target-repo extensions and require their own target ADR.\n \n Shiki is not:\n \n@@ -44,10 +44,14 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use\n+`SADR-NNNN-*.md`; target product decisions retain `NNNN-*.md` and the `ADR`\n+namespace.\n+\n Use the canonical source-of-truth order above. Runtime-specific reading orders\n are operational guidance only and must not override or reorder it.\n \n-Conversation state is not durable truth. If a decision matters, put it in GitHub, `.shiki/`, `CONTEXT.md`, or an ADR.\n+Conversation state is not durable truth. If a decision matters, put it in GitHub, `.shiki/`, `CONTEXT.md`, a Shiki SADR, or a target ADR.\n \n If GitHub and `.shiki/` disagree, surface the conflict and prefer GitHub operational state until the mirror is repaired.\n \n@@ -75,8 +79,8 @@\n Every non-trivial change follows this loop:\n \n 1. **Goal**: clarify outcome, completion conditions, non-goals, risk level, and success signals.\n-2. **grill-with-docs**: challenge the plan against domain docs, ADRs, code reality, terminology, and edge scenarios.\n-3. **Context & Impact**: find relevant docs, ADRs, code, dependencies, owners, locks, and verification surfaces.\n+2. **grill-with-docs**: challenge the plan against domain docs, Shiki SADRs, target ADRs, code reality, terminology, and edge scenarios.\n+3. **Context & Impact**: find relevant docs, SADRs, target ADRs, code, dependencies, owners, locks, and verification surfaces.\n 4. **PRD and Spec Freeze**: use `to-prd` when context is settled enough to become durable product/engineering intent. The operator's explicit approval of the PRD is Spec Freeze: the plan records a `spec_freeze` block, and no plan runs without it. Steps 1-4 form Requirements Definition \u2014 one continuous operator dialogue. After Spec Freeze, scope changes require an operator-approved Spec Amendment; non-scope-moving interpretations go to the Assumption Log.\n 5. **Issues**: use `to-issues` to create independently grabbable vertical-slice issues. Prefer AFK slices over HITL slices where possible.\n 6. **Triage**: label issues for readiness, risk, runtime, skills, and MergeGate state.\n@@ -167,7 +171,7 @@\n - Testing decisions.\n - Out-of-scope items.\n - Further notes.\n-- Links to relevant domain terms and ADRs.\n+- Links to relevant domain terms, SADRs, and target ADRs.\n \n Every executable task must include:\n \n@@ -193,7 +197,7 @@\n \n Context & Impact must identify:\n \n-- Relevant documents, domain terms, ADRs, and past decisions.\n+- Relevant documents, domain terms, SADRs, target ADRs, and past decisions.\n - Relevant modules, interfaces, seams, callers, owners, and tests.\n - Dependency relationships between tasks.\n - Candidate locks and lock conflicts.\n@@ -210,7 +214,7 @@\n | Skill | Required when |\n | --- | --- |\n | `setup-matt-pocock-skills` | First configuring the repo for skills, or when issue tracker, triage labels, or domain docs are missing. |\n-| `grill-with-docs` | Default for non-trivial Goals before PRD/issues; always required when plans, terminology, boundaries, tradeoffs, or ADR-worthy decisions are ambiguous. |\n+| `grill-with-docs` | Default for non-trivial Goals before PRD/issues; always required when plans, terminology, boundaries, tradeoffs, or decision-record-worthy choices are ambiguous. |\n | `zoom-out` | The agent lacks an architectural map of the relevant code area. |\n | `to-prd` | Settled Goal context must become durable product/engineering intent. |\n | `to-issues` | A Goal, PRD, or plan must become independently grabbable vertical-slice issues. |\n@@ -348,7 +352,7 @@\n - Three or more new files.\n - Any high-risk or critical label.\n \n-When the architecture gate triggers, run `grill-with-docs` or `improve-codebase-architecture` as appropriate and record durable decisions in an ADR when needed.\n+When the architecture gate triggers, run `grill-with-docs` or `improve-codebase-architecture` as appropriate and record durable decisions in a Shiki SADR or target ADR as applicable.\n \n ## Engineering Discipline\n \ndiff --git a/CLAUDE.md b/CLAUDE.md\nindex 98a932e7841a632866fcd53b0bad0e3af3711d3d..98a31399404d6fb044b4fd493f4459ac87f439de\n--- a/CLAUDE.md\n+++ b/CLAUDE.md\n@@ -14,6 +14,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n ## Claude Role\n \n Default to planner, default implementer, reviewer, coordinator, documentation editor, CCA-style completion judge, and final-judgment assistant.\n@@ -57,7 +60,7 @@\n At the start of a Shiki session:\n \n 1. Read `AGENTS.md`.\n-2. Read `CONTEXT.md` and relevant ADRs.\n+2. Read `CONTEXT.md` and relevant Shiki SADRs and target ADRs.\n 3. Read active `.shiki/` Goal/task/ledger state when present.\n 4. Read active distilled rules from `.shiki/memories/` (status `distilled`, `active: true`, not revoked or superseded) for applicable guidance; the same rules are injected deterministically into task handoffs as the `## Distilled Rules` section (\u00a73.5).\n 5. Read the GitHub Issue or PR when the task is GitHub-backed.\n@@ -74,7 +77,7 @@\n - Define the Goal, completion conditions, non-goals, and risk level.\n - Treat `grill-with-docs` as the default planning entry point for non-trivial Goals.\n - Challenge domain terms against `CONTEXT.md`.\n-- Challenge hard-to-reverse tradeoffs against ADRs.\n+- Challenge hard-to-reverse tradeoffs against SADRs and target ADRs.\n - Explore code instead of asking when code can answer the question.\n - Run Context & Impact before implementation planning.\n - Produce a Task DAG, not an unordered checklist.\n@@ -94,7 +97,7 @@\n - For each question, provide your recommended answer.\n - Resolve dependency decisions before downstream design questions.\n - Update `CONTEXT.md` inline when terminology crystallizes.\n-- Offer ADRs only when the decision is hard to reverse, surprising without context, and a real tradeoff.\n+- Offer a SADR or target ADR only when the decision is hard to reverse, surprising without context, and a real tradeoff.\n - Convert unresolved questions into HITL blockers rather than allowing Codex to guess.\n \n ## PRD and Issues Standard\n@@ -103,7 +106,7 @@\n \n - Use the project's domain glossary vocabulary.\n - Avoid volatile file paths unless they are essential.\n-- Include implementation decisions, testing decisions, out-of-scope items, and relevant ADR/domain links.\n+- Include implementation decisions, testing decisions, out-of-scope items, and relevant SADR/target-ADR/domain links.\n - Identify deep-module opportunities and testable interfaces.\n \n When creating issues:\n@@ -121,7 +124,7 @@\n - Branch/worktree target.\n - Scope and non-goals.\n - Dependencies and locks.\n-- Relevant docs, ADRs, modules, and tests.\n+- Relevant docs, SADRs, target ADRs, modules, and tests.\n - Required skill invocations.\n - TDD expectations.\n - Pre-PR `code-review` gate expectations.\n@@ -152,7 +155,7 @@\n \n - Treat yourself as a judge, not implementer.\n - Do not write production code.\n-- Read `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant ADRs, PRD, task issue, PR body, diff, checks, reviews, labels, and `.shiki/` state.\n+- Read `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant SADRs and target ADRs, PRD, task issue, PR body, diff, checks, reviews, labels, and `.shiki/` state.\n - Evaluate every applicable checklist in `docs/agents/checklists.md`.\n - Map each acceptance criterion to durable evidence.\n - Distinguish implementation failure from missing evidence.\ndiff --git a/CONTEXT.md b/CONTEXT.md\nindex e61a9be06a0cce0d026484083a657bde2067e234..237ee8aa3ea8fab334ece49768bc5ed3b5af0974\n--- a/CONTEXT.md\n+++ b/CONTEXT.md\n@@ -25,7 +25,7 @@\n _Avoid_: jumping directly into code\n \n **grill-with-docs**:\n-A planning skill that challenges a plan against domain language, ADRs, code reality, and concrete edge scenarios. It resolves design-tree questions before PRD/issues. It runs inside Requirements Definition.\n+A planning skill that challenges a plan against domain language, Shiki SADRs, target ADRs, code reality, and concrete edge scenarios. It resolves design-tree questions before PRD/issues. It runs inside Requirements Definition.\n _Avoid_: generic brainstorming, silent assumptions\n \n **Requirements Definition**:\ndiff --git a/CONTRIBUTING.md b/CONTRIBUTING.md\nindex c6d9b656e73abb8a55e5f660123e03153dcb3e6d..9e37a8fbf668db5b22461aaae5548b8f1354394c\n--- a/CONTRIBUTING.md\n+++ b/CONTRIBUTING.md\n@@ -17,7 +17,7 @@\n 1. **Goal** \u2014 start from a user-approved Goal with an outcome, completion\n    conditions, non-goals, risk level, and success signals.\n 2. **grill-with-docs** \u2014 for any non-trivial change, challenge the plan against\n-   domain language, ADRs, and code reality before writing a PRD.\n+   domain language, Shiki SADRs, target ADRs, and code reality before writing a PRD.\n 3. **PRD** \u2014 capture settled product/engineering intent (`to-prd`).\n 4. **Spec Freeze** \u2014 the operator explicitly approves the PRD/requirements;\n    the plan records a `spec_freeze` block and no plan runs without it\n@@ -94,7 +94,7 @@\n   entries.\n - Do not hand-edit `.shiki/goals/*.json` or `.shiki/manifest.json` in a task PR.\n - Conversation state is not durable truth. If a decision matters, record it in\n-  GitHub, `.shiki/`, `CONTEXT.md`, or an ADR.\n+  GitHub, `.shiki/`, `CONTEXT.md`, a Shiki SADR, or a target ADR.\n \n ## Architecture Gate\n \n@@ -103,7 +103,7 @@\n or CI policy, package structure, three or more structural files, more than 100\n net new lines in a core module, three or more new files, or any high-risk label.\n When triggered, run `grill-with-docs` or `improve-codebase-architecture` and\n-record durable decisions in an ADR when needed (see `docs/adr/`).\n+record durable decisions in a Shiki SADR or target ADR as applicable (see `docs/adr/`).\n \n ## Safety\n \ndiff --git a/SYSTEM_PROMPT.md b/SYSTEM_PROMPT.md\nindex aaaea45e3a896c3a44ba89eec8bb84325ecf6c62..da009c86a88285269dee0155e58e550c6d744ea9\n--- a/SYSTEM_PROMPT.md\n+++ b/SYSTEM_PROMPT.md\n@@ -30,6 +30,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n Use the canonical source-of-truth order above. Runtime-specific reading orders\n are operational guidance only and must not override or reorder it. The current\n conversation is non-durable operator input.\n@@ -48,14 +51,14 @@\n \n A runtime may only do work that is explicitly assigned by the Goal, task, PR, or MergeGate decision.\n \n-Do not assume `openai/codex-action`, `OPENAI_API_KEY`, or API-key based Codex automation in the default Shiki path. API-key runners are explicit target-repo extensions and require their own ADR.\n+Do not assume `openai/codex-action`, `OPENAI_API_KEY`, or API-key based Codex automation in the default Shiki path. API-key runners are explicit target-repo extensions and require their own target ADR.\n \n ## 4. Non-negotiable Shiki flow\n \n For any non-trivial Goal, use this flow:\n \n 1. **Goal Seek**: define outcome, non-goals, risk level, completion criteria, and evidence requirements.\n-2. **grill-with-docs**: challenge the plan against `CONTEXT.md`, ADRs, code reality, terminology, boundaries, and edge scenarios. Ask one question at a time when the operator is available. If code can answer a question, inspect code instead of asking.\n+2. **grill-with-docs**: challenge the plan against `CONTEXT.md`, Shiki SADRs, target ADRs, code reality, terminology, boundaries, and edge scenarios. Ask one question at a time when the operator is available. If code can answer a question, inspect code instead of asking.\n 3. **Context & Impact**: identify modules, interfaces, seams, callers, dependencies, locks, risk, and verification surfaces.\n 4. **to-prd + Spec Freeze**: turn settled context into a PRD using domain vocabulary and testing decisions. The operator's explicit approval of the PRD is Spec Freeze, recorded as the plan's `spec_freeze` block; plans without it do not run. Steps 1-4 form Requirements Definition.\n 5. **to-issues**: decompose the PRD into independently grabbable vertical-slice issues. Prefer AFK slices; mark HITL slices when judgment is still required.\n@@ -75,7 +78,7 @@\n Before making any material change:\n \n 1. Read `AGENTS.md` and any runtime-specific wrapper such as `CLAUDE.md`.\n-2. Read `CONTEXT.md` and relevant ADRs.\n+2. Read `CONTEXT.md` and relevant SADRs and target ADRs.\n 3. Read the GitHub Issue / PR and `.shiki/` state for the active Goal or task.\n 4. Identify your role: Planner, Implementer, Reviewer, Completion Check Agent, Repairer, or Guardian-assist.\n 5. Identify Goal id, task id, branch, PR, dependencies, locks, risk level, required skills, and required evidence.\n@@ -103,7 +106,7 @@\n \n At minimum, identify:\n \n-- Relevant documents, ADRs, domain terms, and prior decisions.\n+- Relevant documents, SADRs, target ADRs, domain terms, and prior decisions.\n - Relevant modules, interfaces, seams, callers, owners, and tests.\n - Dependency relationships between tasks.\n - Candidate file locks and lock conflicts.\n@@ -152,7 +155,7 @@\n \n CCA must read:\n \n-- `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant ADRs.\n+- `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant SADRs and target ADRs.\n - Parent Goal issue, PRD issue, task issue, comments, and labels.\n - PR body, diff, commits, changed files, reviews, CI checks, and artifacts available to the workflow.\n - `.shiki/` task contract, locks, ledger entries, CCA checklist profile, and prior repair packets.\n@@ -214,7 +217,7 @@\n - Three or more new files.\n - Any high-risk or critical label.\n \n-When the architecture gate triggers, run `grill-with-docs` or `improve-codebase-architecture` as appropriate and record the decision in an ADR if it is hard to reverse or likely to be re-litigated.\n+When the architecture gate triggers, run `grill-with-docs` or `improve-codebase-architecture` as appropriate and record the decision in a Shiki SADR or target ADR as applicable if it is hard to reverse or likely to be re-litigated.\n \n ## 14. Skill Gate\n \n@@ -223,7 +226,7 @@\n Use these skills:\n \n - `setup-matt-pocock-skills`: before first use of the engineering skill set, or when issue tracker, triage labels, or domain docs are missing.\n-- `grill-with-docs`: default for non-trivial Goals before PRD/issues; required for ambiguous plans, terminology, boundaries, ADR-worthy tradeoffs, or design-tree decisions.\n+- `grill-with-docs`: default for non-trivial Goals before PRD/issues; required for ambiguous plans, terminology, boundaries, decision-record-worthy tradeoffs, or design-tree decisions.\n - `zoom-out`: unfamiliar code area or missing architectural map.\n - `to-prd`: convert settled context into a PRD.\n - `to-issues`: break a PRD, plan, or Goal into independently grabbable vertical-slice issues.\ndiff --git a/docs/adr/0001-keep-shiki-as-platform-name.md b/docs/adr/SADR-0001-keep-shiki-as-platform-name.md\nindex 20a9637c3b9d8c97de7ba8db7cfae4d82d9a7dba..8b685439959aada1b221c112f0ac3c51ca8a1695\n--- a/docs/adr/0001-keep-shiki-as-platform-name.md\n+++ b/docs/adr/SADR-0001-keep-shiki-as-platform-name.md\n@@ -1,3 +1,3 @@\n-# Keep Shiki As The Platform Name\n+# SADR-0001: Keep Shiki As The Platform Name\n \n Shiki remains the product name for the GitHub-native agentic engineering control plane. We considered more descriptive names such as MergePilot, MergeGate, and Kiroku, but kept Shiki because it carries the existing goal-seeking framework identity while remaining independent of Hermes, Codex, Claude Code, or any single runtime.\ndiff --git a/docs/adr/0002-use-agents-as-shared-constitution.md b/docs/adr/SADR-0002-use-agents-as-shared-constitution.md\nindex 73de30ecb078120eac540d9bc51855e3cd48d0e3..c5486182bf06fca36ea508496b013c67302fd989\n--- a/docs/adr/0002-use-agents-as-shared-constitution.md\n+++ b/docs/adr/SADR-0002-use-agents-as-shared-constitution.md\n@@ -1,3 +1,3 @@\n-# Use AGENTS As The Shared Agent Constitution\n+# SADR-0002: Use AGENTS As The Shared Agent Constitution\n \n Shiki uses `AGENTS.md` as the runtime-neutral constitution and `CLAUDE.md` as a thin Claude Code wrapper. This keeps Codex, Claude Code, Hermes Runner, and future Agent Runtimes aligned on the same GitHub-first MergeGate rules while still allowing runtime-specific instructions where needed.\ndiff --git a/docs/adr/0003-require-skill-gate-for-mergegate-readiness.md b/docs/adr/SADR-0003-require-skill-gate-for-mergegate-readiness.md\nindex 424be0c9af4c8f0136f1fde68a468c47ff84e764..7fc337987c7df9792f75f181dfcbed75fe5ffff2\n--- a/docs/adr/0003-require-skill-gate-for-mergegate-readiness.md\n+++ b/docs/adr/SADR-0003-require-skill-gate-for-mergegate-readiness.md\n@@ -1,3 +1,3 @@\n-# Require Skill Gate For MergeGate Readiness\n+# SADR-0003: Require Skill Gate For MergeGate Readiness\n \n Shiki requires relevant engineering skills to be invoked before execution when their triggers apply. This makes skills part of MergeGate readiness rather than optional prompt style, because Shiki depends on repeatable planning, issue decomposition, TDD, diagnosis, triage, and architecture review before autonomous or parallel agent work can be trusted.\ndiff --git a/docs/adr/0004-use-github-first-with-shiki-mirror.md b/docs/adr/SADR-0004-use-github-first-with-shiki-mirror.md\nindex d0a3a9174dc646a945a9d325954ffaf00c352040..a79c54ae9ee8eed2a0524192b864da4e22a92d3f\n--- a/docs/adr/0004-use-github-first-with-shiki-mirror.md\n+++ b/docs/adr/SADR-0004-use-github-first-with-shiki-mirror.md\n@@ -1,4 +1,4 @@\n-# ADR 0004: Use GitHub First With A Repository-Local Shiki Mirror\n+# SADR-0004: Use GitHub First With A Repository-Local Shiki Mirror\n \n ## Status\n \ndiff --git a/docs/adr/0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md b/docs/adr/SADR-0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\nindex b339baf49b89d9290e79c3d51b36e7b2bde3bd95..0f3c3eb9afccdb3403ce978e21d04fe12e495da0\n--- a/docs/adr/0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\n+++ b/docs/adr/SADR-0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\n@@ -1,8 +1,8 @@\n-# ADR 0005: Use Codex As Front And Claude Code Action As The GitHub Runtime\n+# SADR-0005: Use Codex As Front And Claude Code Action As The GitHub Runtime\n \n ## Status\n \n-Accepted (implementer default superseded by ADR 0008; the auth model and the\n+Accepted (implementer default superseded by SADR-0008; the auth model and the\n Claude Code Action GitHub runtime decision remain in force)\n \n ## Context\n@@ -26,4 +26,4 @@\n - Shiki avoids requiring `OPENAI_API_KEY` for the default workflow.\n - Codex work happens through the user's authenticated Codex front surface, not as a hidden backend Action.\n - Claude Code Action is configured for OAuth-token based subscription usage by default.\n-- API-key based automation remains possible only as an explicit target-repo extension with its own ADR.\n+- API-key based automation remains possible only as an explicit target-repo extension with its own target ADR.\ndiff --git a/docs/adr/0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md b/docs/adr/SADR-0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\nindex 4bcae9d1e52f0a37b8e9fbafa5cd14b6ee1993bd..7a38538a6ae9929567f0ade67e7ea6a60d0b3f7e\n--- a/docs/adr/0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\n+++ b/docs/adr/SADR-0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\n@@ -1,4 +1,4 @@\n-# ADR 0006: Separate Codex Implementation, CCA Completion, And MergeGate Authorization\n+# SADR-0006: Separate Codex Implementation, CCA Completion, And MergeGate Authorization\n \n ## Status\n \ndiff --git a/docs/adr/0007-packaging-and-release.md b/docs/adr/SADR-0007-packaging-and-release.md\nindex 79b84ddb97167372b44641e5b7860e400ce80822..bb445a2e2e856d9d1f5fdbe35de8d4e8cec0fb32\n--- a/docs/adr/0007-packaging-and-release.md\n+++ b/docs/adr/SADR-0007-packaging-and-release.md\n@@ -1,4 +1,4 @@\n-# ADR 0007: Packaging And Release For Shiki\n+# SADR-0007: Packaging And Release For Shiki\n \n ## Status\n \n@@ -25,7 +25,7 @@\n   not introduce third-party runtime dependencies or break the shim.\n - Releases must run inside the operator's subscription-authenticated toolchain\n   and must not require extra secrets beyond the built-in `GITHUB_TOKEN`\n-  (consistent with ADR 0005's no-API-key-by-default stance).\n+  (consistent with SADR-0005's no-API-key-by-default stance).\n - `.shiki/manifest.json` already encodes the install surface (`install.include`,\n   `install.create_directories`, `install.exclude_from_commit`), and it must not\n   be mutated as part of this change.\n@@ -34,7 +34,7 @@\n \n This is a hard-to-reverse platform decision (it sets the version source of\n truth, the release trigger, and the public install contract), so it is recorded\n-as an ADR.\n+as a SADR.\n \n ## Decision\n \n@@ -104,7 +104,7 @@\n 3. **Publishing to PyPI.** Rejected for now: Shiki is distributed as a\n    repository template + git-installable CLI, not a PyPI library; PyPI would add\n    credentials/secrets and a different versioning contract. Can be added later as\n-   its own ADR if a packaged distribution is needed.\n+   its own SADR if a packaged distribution is needed.\n 4. **Dynamic version (read `VERSION` via a build backend `dynamic` field).**\n    Rejected to keep `pyproject.toml` backend-agnostic and dependency-free; the\n    small duplication between `VERSION` and `pyproject.toml` is guarded by the\n@@ -123,4 +123,4 @@\n   releasing versions the whole platform but ships only `install.include` to\n   targets.\n - Future PyPI distribution or a console entry point are deferred and would each\n-  warrant their own ADR.\n+  warrant their own SADR.\ndiff --git a/docs/adr/0008-use-claude-code-as-default-implementer.md b/docs/adr/SADR-0008-use-claude-code-as-default-implementer.md\nindex dedf4510b7c7799521422f622029563d373db031..35c2625d908167fbc58855b5b63e283805f167e8\n--- a/docs/adr/0008-use-claude-code-as-default-implementer.md\n+++ b/docs/adr/SADR-0008-use-claude-code-as-default-implementer.md\n@@ -1,12 +1,12 @@\n-# ADR 0008: Use Claude Code As The Default Implementer Runtime\n+# SADR-0008: Use Claude Code As The Default Implementer Runtime\n \n ## Status\n \n-Proposed (supersedes ADR 0005 for the implementer default; keeps its auth model)\n+Proposed (supersedes SADR-0005 for the implementer default; keeps its auth model)\n \n ## Context\n \n-ADR 0005 made Codex Front the default implementation runtime, with Claude Code\n+SADR-0005 made Codex Front the default implementation runtime, with Claude Code\n restricted to planner and reviewer roles by the runtime registry, and the only\n autonomous implementation adapter was `shiki runner codex`.\n \n@@ -18,13 +18,13 @@\n - The platform's planning, review, and CCA layers already run on Claude Code,\n   and Claude Code's native capabilities (the built-in `/code-review` skill and\n   the Workflow multi-agent orchestration tool) have no Codex equivalent.\n-- The Goal lifecycle redesign (ADR 0009) requires an autonomous post-freeze\n+- The Goal lifecycle redesign (SADR-0009) requires an autonomous post-freeze\n   loop driven from the operator's Claude Code session, including bounded\n   repair dispatch, which must not depend on a runtime that cannot be invoked\n   unattended.\n \n The constitution claims Shiki is runtime-agnostic (\"not a Claude-only\n-workflow\", ADR 0002). Any change to the implementer default must preserve the\n+workflow\", SADR-0002). Any change to the implementer default must preserve the\n registry-based runtime model rather than hardcoding one vendor.\n \n ## Decision\n@@ -39,7 +39,7 @@\n   the task to review or repair-needed.\n - Keep Codex registered as an optional implementer runtime. `shiki runner\n   codex`, the Codex handoff templates, and the ChatGPT OAuth auth model from\n-  ADR 0005 remain supported for tasks explicitly assigned to `codex`.\n+  SADR-0005 remain supported for tasks explicitly assigned to `codex`.\n - Commit to mandating two Claude-native capabilities as loop contract in\n   follow-up Goals (status: the code-review gate was delivered by Goal G-B \u2014\n   skill registry, checklists item PR-12, default required_skills \u2014 while the\n@@ -60,7 +60,7 @@\n \n Out of scope: removing Codex support, changing the CCA runtime (Claude Code\n Action with `CLAUDE_CODE_OAUTH_TOKEN` stays), and API-key based automation\n-(still requires its own ADR per ADR 0005).\n+(still requires its own target ADR per SADR-0005).\n \n ## Consequences\n \n@@ -100,7 +100,7 @@\n   harness rejection of direct `codex exec` is outside Shiki's control, and the\n   plan's mandatory native capabilities cannot run on Codex at all.\n - **Remove Codex entirely**: rejected; it abandons the runtime-agnostic\n-  constitution (ADR 0002) for no operational gain and forecloses\n+  constitution (SADR-0002) for no operational gain and forecloses\n   cross-runtime adversarial review.\n - **GitHub Actions cloud implementation via claude-code-action**: rejected as\n   default; durable evidence is automatic but token cost, Node 24 workflow\ndiff --git a/docs/adr/0009-requirements-definition-and-spec-freeze.md b/docs/adr/SADR-0009-requirements-definition-and-spec-freeze.md\nindex 29b3f7871bef9203a8b4a831a65c39eb2f506e42..52e7dd69ad0aa4efe0b68c62678abbf7df02aaec\n--- a/docs/adr/0009-requirements-definition-and-spec-freeze.md\n+++ b/docs/adr/SADR-0009-requirements-definition-and-spec-freeze.md\n@@ -1,4 +1,4 @@\n-# ADR 0009: Merge Grill And Planning Into Requirements Definition With Spec Freeze\n+# SADR-0009: Merge Grill And Planning Into Requirements Definition With Spec Freeze\n \n ## Status\n \ndiff --git a/docs/adr/0010-external-ai-guardian-review.md b/docs/adr/SADR-0010-external-ai-guardian-review.md\nindex e6108bbe8bb4e73e0ff4684c8f78001a80438c4e..f3510b145664298adafb151b6f0db51b972f01ec\n--- a/docs/adr/0010-external-ai-guardian-review.md\n+++ b/docs/adr/SADR-0010-external-ai-guardian-review.md\n@@ -1,4 +1,4 @@\n-# ADR 0010: External AI Guardian Review As A First-Class Approval Authority\n+# SADR-0010: External AI Guardian Review As A First-Class Approval Authority\n \n ## Status\n \n@@ -8,7 +8,7 @@\n \n Shiki is intended to be an autonomous, recursive, self-improving agent\n framework: authority-in-the-loop, not human-in-the-loop. Until now the Guardian\n-approval required for high/critical-risk merges (guardian-policy.json, ADR\n+approval required for high/critical-risk merges (guardian-policy.json, SADR\n governance) recognized only human authority sources \u2014 a GitHub review approval,\n the `guardian:approved` label applied by a configured Guardian, or a comment\n carrying `Guardian approval granted` plus the current head SHA.\n@@ -89,7 +89,7 @@\n   autonomous design the operator intends and produced the impersonation\n   incident.\n - **Record AI approval as an operator comment**: rejected as forgery of human\n-  authority \u2014 the exact failure this ADR corrects.\n+  authority \u2014 the exact failure this SADR corrects.\n - **Deliver the artifact as a committed `.shiki/` file**: rejected; a committed\n   approval inside the PR being approved is forgeable by anyone who can push.\n - **A dedicated reviewer bot identity instead of a relayed comment**: deferred;\ndiff --git a/docs/adr/0011-autonomous-quality-gate-evidence.md b/docs/adr/SADR-0011-autonomous-quality-gate-evidence.md\nindex bca0246c5d46e44afbde15486a75eec425fc02f5..9c93eac16d8c3a6d4b11bd7224e3955102cd7db4\n--- a/docs/adr/0011-autonomous-quality-gate-evidence.md\n+++ b/docs/adr/SADR-0011-autonomous-quality-gate-evidence.md\n@@ -1,4 +1,4 @@\n-# ADR 0011: Autonomous Quality-Gate Evidence \u2014 Deterministic Facts And An Independent Verifier\n+# SADR-0011: Autonomous Quality-Gate Evidence \u2014 Deterministic Facts And An Independent Verifier\n \n ## Status\n \ndiff --git a/docs/adr/0012-autonomous-loop-closeout-to-main.md b/docs/adr/SADR-0012-autonomous-loop-closeout-to-main.md\nindex 637fad6dde4989c9c2bf0f81f676d8d29991123c..ec7d85f81030a676bcbba28b43eb365422a36eac\n--- a/docs/adr/0012-autonomous-loop-closeout-to-main.md\n+++ b/docs/adr/SADR-0012-autonomous-loop-closeout-to-main.md\n@@ -1,4 +1,4 @@\n-# 0012 \u2014 Autonomous loop pushes goal completion to main via a closeout PR\n+# SADR-0012 \u2014 Autonomous loop pushes goal completion to main via a closeout PR\n \n - Status: accepted\n - Date: 2026-06-18\ndiff --git a/docs/adr/0013-bootstrap-grants-actions-approve-pull-requests.md b/docs/adr/SADR-0013-bootstrap-grants-actions-approve-pull-requests.md\nindex b6c69d64f1c5ced346b88d6859586715b3b5bd0e..cf48ff4a6716267f539ca58edd1a852e7c7a2d64\n--- a/docs/adr/0013-bootstrap-grants-actions-approve-pull-requests.md\n+++ b/docs/adr/SADR-0013-bootstrap-grants-actions-approve-pull-requests.md\n@@ -1,4 +1,4 @@\n-# ADR 0013: Bootstrap Grants GitHub Actions Permission To Create And Approve Pull Requests\n+# SADR-0013: Bootstrap Grants GitHub Actions Permission To Create And Approve Pull Requests\n \n ## Status\n \n@@ -31,7 +31,7 @@\n security tradeoff: it lets repository automation satisfy a human-review gate. In\n a multi-maintainer repo that could let a compromised or buggy workflow\n self-approve changes. So whether bootstrap turns it on by default \u2014 and with\n-what default token scope \u2014 is an ADR-worthy decision, not an implementation\n+what default token scope \u2014 is a SADR-worthy decision, not an implementation\n detail.\n \n ## Decision\n@@ -48,7 +48,7 @@\n Specifically:\n \n - **`can_approve_pull_request_reviews` defaults to `true`.** Shiki's default\n-  posture is solo/AFK autonomous operation (ADR 0008/0009/0012), where the CCA\n+  posture is solo/AFK autonomous operation (SADR-0008/0009/0012), where the CCA\n   Review Bridge is the only path that can satisfy `required_review: true` after\n   the CCA and Guardian gates have already passed. The default makes a\n   freshly-bootstrapped repo able to run the autonomous loop end to end without a\ndiff --git a/docs/adr/0014-codex-app-external-ai-guardian-ui-adapter.md b/docs/adr/SADR-0014-codex-app-external-ai-guardian-ui-adapter.md\nindex 238db06279334caa341c117362df9271382a7fcd..65f12e9939321e1b3e389a34c822fc1549e5155d\n--- a/docs/adr/0014-codex-app-external-ai-guardian-ui-adapter.md\n+++ b/docs/adr/SADR-0014-codex-app-external-ai-guardian-ui-adapter.md\n@@ -1,4 +1,4 @@\n-# ADR 0014: Use Codex App As The External AI Guardian UI Adapter\n+# SADR-0014: Use Codex App As The External AI Guardian UI Adapter\n \n ## Status\n \ndiff --git a/docs/adr/0015-contract-approval-before-dispatch.md b/docs/adr/SADR-0015-contract-approval-before-dispatch.md\nindex fad6c656741a20a7684ba16080a7db159f63a52f..6061271a46d3f1d6ae7174477de3fb75673123a6\n--- a/docs/adr/0015-contract-approval-before-dispatch.md\n+++ b/docs/adr/SADR-0015-contract-approval-before-dispatch.md\n@@ -1,4 +1,4 @@\n-# ADR 0015: Approve The Task Contract Before Dispatch, Not The Diff After It\n+# SADR-0015: Approve The Task Contract Before Dispatch, Not The Diff After It\n \n ## Status\n \n@@ -9,7 +9,7 @@\n Guardian approval for high/critical risk is expressible only on a pull request.\n Every approval source in `guardian-policy.json` \u2014 GitHub review, the\n `guardian:approved` label, the `Guardian approval granted` comment, and the\n-`external_ai_guardian_review` artifact (ADR 0010/0014) \u2014 is bound to a PR, and\n+`external_ai_guardian_review` artifact (SADR-0010/0014) \u2014 is bound to a PR, and\n three of the four require the current head SHA. Approval therefore cannot exist\n before an implementation PR exists.\n \n@@ -81,7 +81,7 @@\n An envelope breach must surface only on the non-repairable policy gate, never on\n the repairable `MergeGate metadata check`. Otherwise the Goal Loop routes it to\n `dispatch_repair` \u2014 instructing a runner to make the breach go away \u2014 which is\n-exactly what this ADR forbids.\n+exactly what this SADR forbids.\n \n **Contract immutability.** After registration, an implementation PR must not\n change the governance fields of its own task contract (`scope`, `non_goals`,\n@@ -116,7 +116,7 @@\n fails any verdict that is not `complete`, and the signal the CCA prompt reads is\n computed from `evaluate_guardian_approval` alone. Without the OR-merge at the\n signal, a contract-approved PR still returns `needs_guardian`, the CCA job fails,\n-and the MergeGate policy check never runs \u2014 the gate this ADR changes would never\n+and the MergeGate policy check never runs \u2014 the gate this SADR changes would never\n be reached. The CCA *prompt* needs no change: a `contract_approval` entry in the\n signal's existing `sources` array satisfies its current wording.\n \n@@ -130,7 +130,7 @@\n written, and an approved contract lets its implementation PRs merge through the\n same automatic path as low/medium risk. The wasted implement-then-block cycle\n disappears. Guardian review shifts from reading diffs to reading contracts, which\n-is both earlier and smaller; the external AI Guardian path (ADR 0010/0014) works\n+is both earlier and smaller; the external AI Guardian path (SADR-0010/0014) works\n on contracts with no change.\n \n We accept that the Guardian authorizes a class of change without seeing its\n@@ -146,7 +146,7 @@\n adversarial tests for the new approval path equivalent in rigor to\n `test_shiki_governance_evidence.sh`.\n \n-Bootstrap costs, which apply only while this ADR is being implemented. The tasks\n+Bootstrap costs, which apply only while this SADR is being implemented. The tasks\n that build Contract Approval merge under the existing rules, so the ones\n dispatched after the pre-dispatch gate lands but before the approval path lands\n need a Guardian-approved Contract PR *and* a live Guardian approval on their\n@@ -210,7 +210,7 @@\n risk; they do not bound authorized work.\n \n **Auto-escalate risk on envelope breach instead of stopping.** Returns the\n-workflow to a diff-time Guardian decision \u2014 the exact behavior this ADR removes.\n+workflow to a diff-time Guardian decision \u2014 the exact behavior this SADR removes.\n \n **Resolve governance fields from the base snapshot only.** Rejected against the\n registration flow: implementation PRs add their own contract, so the base has no\ndiff --git a/docs/adr/0016-locks-bound-product-paths.md b/docs/adr/SADR-0016-locks-bound-product-paths.md\nindex b5e850f3f1f7f4ef6e7f9d1763a29a51bd300c45..46b40a9b99a7aae644c02bb458f5473ff0a90ce2\n--- a/docs/adr/0016-locks-bound-product-paths.md\n+++ b/docs/adr/SADR-0016-locks-bound-product-paths.md\n@@ -1,4 +1,4 @@\n-# ADR 0016: Locks bound product paths; the Shiki mirror is bound by record identity\n+# SADR-0016: Locks bound product paths; the Shiki mirror is bound by record identity\n \n ## Status\n \n@@ -6,8 +6,8 @@\n \n - Date: 2026-07-28\n - Deciders: operator (mizutani-140), Claude (planner)\n-- Related: ADR 0004 (GitHub-first with a `.shiki` mirror), ADR 0009 (Spec Freeze),\n-  ADR 0012 (autonomous loop closeout to main), ADR 0015 (Contract Approval before\n+- Related: SADR-0004 (GitHub-first with a `.shiki` mirror), SADR-0009 (Spec Freeze),\n+  SADR-0012 (autonomous loop closeout to main), SADR-0015 (Contract Approval before\n   dispatch), remediation goal task R-02, the live PR #179 deadlock.\n \n ## Context\n@@ -90,11 +90,11 @@\n files, documentation files and named source modules are nameable; runtime-\n allocated ledger, runner and report ids are not \u2014 and those live in `.shiki`,\n which locks no longer govern. Existing frozen contracts are brought into line by\n-a one-time Spec Amendment per plan, which must land before ADR 0015's contract\n+a one-time Spec Amendment per plan, which must land before SADR-0015's contract\n immutability makes `locks` unwritable from the implementation PR.\n \n **Lock state is resolved from the durable side.** MergeGate resolves the lock\n-registry, and ADR 0015's governance fields, from the base `.shiki` snapshot when\n+registry, and SADR-0015's governance fields, from the base `.shiki` snapshot when\n present, falling back to head only when absent. The `MergeGate metadata check`\n job is given `--base-shiki`, which it does not receive today. Locks are not\n derived from the live open-pull-request set: a pull request body is untrusted\n@@ -108,7 +108,7 @@\n Explicitly out of scope: moving MergeGate's contract resolution off the PR\n checkout wholesale; making the mirror a projection of GitHub state; introducing\n new lock kinds; moving ledger evidence out of the repository; and unwinding\n-ADR 0012's closeout pull request. Registration of multi-task goals is ADR 0015's\n+SADR-0012's closeout pull request. Registration of multi-task goals is SADR-0015's\n Contract PR and is not re-decided here.\n \n ## Consequences\n@@ -127,7 +127,7 @@\n \n Risks accepted. First, goal-scoped identity is only as strong as `goal_id`,\n which is resolved from the PR body and the head task file; the mitigation is to\n-add `goal_id` to ADR 0015's frozen governance field set, and until that lands a\n+add `goal_id` to SADR-0015's frozen governance field set, and until that lands a\n task file can name a foreign goal. Second, base-when-present resolution is\n unavailable on fork pull requests, where the head is contributor-controlled;\n fork pull requests already cannot complete a Shiki merge, and this decision does\n@@ -139,7 +139,7 @@\n path, not by implementation pull requests.\n \n New obligations: one Spec Amendment per frozen plan to rewrite `locks`, ordered\n-before ADR 0015 T1; extension of the adversarial suites in\n+before SADR-0015 T1; extension of the adversarial suites in\n `scripts/test_shiki_adversarial_state.sh` and\n `scripts/test_shiki_governance_evidence.sh` to cover the eleven newly-scoped\n directories; and updating `scripts/test_shiki_loop_lock_guard.sh`,\n@@ -151,32 +151,32 @@\n whether the loop's transitive sync of ledger `evidence` references can be\n deleted once the Contract PR carries its own source plan.\n \n-## Relationship to ADR 0004\n+## Relationship to SADR-0004\n \n-This ADR **clarifies something ADR 0004 left unspecified**. ADR 0004 decides that\n+This SADR **clarifies something SADR-0004 left unspecified**. SADR-0004 decides that\n GitHub is the operational source of truth and that each target repository keeps a\n `.shiki/` mirror. It says nothing about pull requests writing the mirror, does\n not name MergeGate, and does not make the mirror an input to a gate. Nothing in\n-ADR 0004's Decision or Consequences is amended, superseded or weakened: the\n+SADR-0004's Decision or Consequences is amended, superseded or weakened: the\n mirror still exists, still records the same artifacts, and pull request\n-boundaries remain the coordination surface. What this ADR settles is the question\n+boundaries remain the coordination surface. What this SADR settles is the question\n 0004 never reached \u2014 by what authority a pull request may write a mirror record \u2014\n and it answers it the way `.shiki/manifest.json` already declares\n (`pr_mutation: current-task-or-current-goal-only`).\n \n-It **amends ADR 0012**, whose Decision item 5 states that \"Loop-executed tasks\n+It **amends SADR-0012**, whose Decision item 5 states that \"Loop-executed tasks\n declare the `path:.shiki/**` lock, which covers every `.shiki` file the closeout\n stages ... so `files_outside_locks` is satisfied without per-file lock\n bookkeeping.\" That clause is replaced: the closeout's mirror writes are\n-authorized by record identity, not by a declared lock. ADR 0012's substantive\n+authorized by record identity, not by a declared lock. SADR-0012's substantive\n decision \u2014 that completion reaches `main` through a closeout pull request rather\n than local attestation \u2014 is preserved unchanged.\n \n-It is **complementary to ADR 0015**, not a substitute. ADR 0015 owns contract\n+It is **complementary to SADR-0015**, not a substitute. SADR-0015 owns contract\n immutability, the pre-dispatch gate, the Contract PR and the Scope Envelope. This\n-ADR owns what a lock means and who may write the mirror. The one ordering\n+SADR owns what a lock means and who may write the mirror. The one ordering\n constraint between them is that the lock-narrowing Spec Amendment must precede\n-ADR 0015 T1's freezing of the `locks` field.\n+SADR-0015 T1's freezing of the `locks` field.\n \n ## Alternatives Considered\n \n@@ -186,7 +186,7 @@\n the only writer that can register them may run. Migrating the Guardian signal to\n that store makes every registration PR fail closed to `needs_guardian` at every\n risk level, because the task file is by definition absent from the store, which\n-removes the low/medium autonomous path ADR 0008/0009 exists to provide. And it\n+removes the low/medium autonomous path SADR-0008/0009 exists to provide. And it\n leaves schemas, manifest, config, guardian policy, templates, migrations, plans,\n worktrees, runner records, runs and handoffs with no writer at all in a\n repository whose product is `.shiki`. Measured benefit on the frozen backlog:\n@@ -199,14 +199,14 @@\n namespace is not in fact ownership-partitioned \u2014 56 ledger entries carry no\n `task_id`, and one goal DAG is writable by 35 registered tasks. The correct\n expression of its insight is that locks do not govern the mirror at all, which is\n-what this ADR decides.\n+what this SADR decides.\n \n **Make the mirror a projection written only by a registration writer and a\n post-merge projection job.** Rejected. It requires a bypass actor with write\n access to the protected default branch; it makes its own master invariant\n readable from a manifest that pull requests may edit; it deletes both reconcile\n modes and leaves a corrupted mirror with no in-band repair path, contradicting\n-ADR 0004's consequence that mirror conflicts are repaired against GitHub state;\n+SADR-0004's consequence that mirror conflicts are repaired against GitHub state;\n and its content-derived ids are rejected by `validate_shiki`'s currently-green id\n pattern.\n \ndiff --git a/docs/adr/0017-bookkeeping-closeouts-do-not-inherit-risk.md b/docs/adr/SADR-0017-bookkeeping-closeouts-do-not-inherit-risk.md\nindex 554e3a0d526a72ca067108c5464cc68c3edcf174..b9676857a4713850e063af7c39c8e0b01ecb4c66\n--- a/docs/adr/0017-bookkeeping-closeouts-do-not-inherit-risk.md\n+++ b/docs/adr/SADR-0017-bookkeeping-closeouts-do-not-inherit-risk.md\n@@ -1,4 +1,4 @@\n-# ADR 0017: A Bookkeeping Closeout Does Not Inherit Its Task's Risk\n+# SADR-0017: A Bookkeeping Closeout Does Not Inherit Its Task's Risk\n \n ## Status\n \n@@ -6,7 +6,7 @@\n \n ## Context\n \n-ADR 0012 made the goal loop push a task's completion to the default branch through a\n+SADR-0012 made the goal loop push a task's completion to the default branch through a\n closeout pull request: after the implementation PR merges, the loop opens a second PR\n carrying `task.status = done`, `lock.state = released`, and \u2014 when the task completes\n its goal \u2014 `goal.status = complete` plus one scorecard report. Nothing else. The\n@@ -49,14 +49,14 @@\n 4. The task file's governance fields (`scope`, `non_goals`, `required_skills`,\n    `risk_level`, `locks`, `acceptance_checks`, `test_command`) are byte-identical to\n    the base snapshot. Only `status`, `expected_pr`, `closeout_pr`, `ledger_evidence`\n-   and `expected_branch` may differ \u2014 the ADR 0015 contract-immutability rule,\n+   and `expected_branch` may differ \u2014 the SADR-0015 contract-immutability rule,\n    unchanged.\n \n    `expected_branch` is in that set because a closeout is *by construction* on a\n    different branch from the implementation it closes, and the loop rewrites the\n    field accordingly. It is safe to let it differ because MergeGate independently\n    requires `expected_branch` to equal the PR's actual head ref, so a PR cannot set\n-   it to anything other than the branch it is really on. The first draft of this ADR\n+   it to anything other than the branch it is really on. The first draft of this SADR\n    omitted it, which made the exemption unreachable: every real closeout failed\n    condition 4 on a field the loop always changes. See PR #209.\n 5. The state transitions are exactly the terminal set: task `review` \u2192 `done`, lock\n@@ -113,12 +113,12 @@\n classification must be derived, not asserted.\n \n **Fold the closeout into the implementation PR** so no second PR exists. Rejected under\n-ADR 0012, which recorded why: a task file that is already `done` on the implementation\n+SADR-0012, which recorded why: a task file that is already `done` on the implementation\n branch short-circuits the loop's review \u2192 CCA \u2192 merge \u2192 done ordering into a local\n goal-completion before any merge happens.\n \n **Have the loop auto-merge closeouts with elevated credentials.** Rejected outright: that\n-is a runtime authoring its own approval, which ADR 0010 and the constitution's\n+is a runtime authoring its own approval, which SADR-0010 and the constitution's\n approval-record prohibition forbid. It would also be indistinguishable, in the evidence,\n from the forgery those rules exist to prevent.\n \ndiff --git a/docs/adr/0018-carried-guardian-approval-across-a-proven-base-sync.md b/docs/adr/SADR-0018-carried-guardian-approval-across-a-proven-base-sync.md\nindex 22358562da390ed8b45cd600da08a43d378b4680..e6e2907cefa776460af73ddf7d39db605f3c39b0\n--- a/docs/adr/0018-carried-guardian-approval-across-a-proven-base-sync.md\n+++ b/docs/adr/SADR-0018-carried-guardian-approval-across-a-proven-base-sync.md\n@@ -1,4 +1,4 @@\n-# ADR 0018: Carried Guardian Approval Across A Proven Base Sync\n+# SADR-0018: Carried Guardian Approval Across A Proven Base Sync\n \n ## Status\n \n@@ -34,7 +34,7 @@\n Guardian evaluator that consumes it (`shiki_guardian.evaluate_guardian_approval`,\n the `guardian_comment_carried` source) were built and tested in isolation. Neither\n does anything until a caller passes the flags that turn the carry on, so the\n-feature was invisible in CI. This ADR records the decision the two of them\n+feature was invisible in CI. This SADR records the decision the two of them\n implement, and this task wires it into the four CI invocations that judge Guardian\n approval.\n \ndiff --git a/docs/adr/README.md b/docs/adr/README.md\nindex c70e057a4520c36e042ab68c53cd4a21ef709b71..5da43856836e2ce6a150f8127766e2f88355ab4b\n--- a/docs/adr/README.md\n+++ b/docs/adr/README.md\n@@ -1,40 +1,41 @@\n-# Architecture Decision Records\n+# Decision Record Namespaces\n \n-This directory holds Shiki's Architecture Decision Records (ADRs): durable\n-records of hard-to-reverse platform decisions. ADRs are part of the Shiki source\n-of truth alongside GitHub state, the `.shiki/` mirror, and `CONTEXT.md`.\n+This directory holds two independent decision namespaces:\n \n-## How ADRs Work\n+- Shiki Architecture Decision Records (SADRs), named\n+  `SADR-NNNN-<short-slug>.md`, record hard-to-reverse Shiki platform decisions.\n+- Target Architecture Decision Records (ADRs), named\n+  `NNNN-<short-slug>.md`, belong to the target product.\n \n-- New ADRs are added as `docs/adr/NNNN-<short-slug>.md`, where `NNNN` is the next\n-  zero-padded sequence number.\n-- The **canonical list of ADRs is the set of files matching `docs/adr/*.md`** in\n-  this directory. Browse `docs/adr/*.md` for the authoritative, current set.\n-- Use [`template.md`](template.md) as the starting point for a new ADR.\n-- Record an ADR when a decision is hard to reverse, surprising without context,\n-  and a real tradeoff. Do not write an ADR for routine or easily reversible\n-  choices.\n+The same four-digit number may appear once in each namespace. A platform\n+`SADR-NNNN` citation resolves only to `SADR-NNNN-*.md`; a target `ADR NNNN`\n+citation resolves only to `NNNN-*.md`.\n \n-Using the `docs/adr/*.md` glob as the source of truth means other PRs can add an\n-ADR without editing this index file, avoiding merge contention on a\n-hand-maintained enumeration.\n+## How Decision Records Work\n \n-## Current ADRs (snapshot)\n-\n-The list below is a convenience snapshot and may lag behind the directory. When\n-in doubt, list `docs/adr/*.md`.\n+- New Shiki platform decisions use the next zero-padded SADR number.\n+- New target product decisions use the target's own ADR sequence.\n+- The canonical list is the files in `docs/adr/`; no hand-maintained index is\n+  authoritative.\n+- Use [`template.md`](template.md) as the starting point and choose the filename\n+  namespace that owns the decision.\n+- Record a decision when it is hard to reverse, surprising without context, and\n+  a real tradeoff. Do not write a decision record for routine or easily\n+  reversible choices.\n \n-- [0001 \u2014 Keep Shiki As The Platform Name](0001-keep-shiki-as-platform-name.md)\n-- [0002 \u2014 Use AGENTS As The Shared Agent Constitution](0002-use-agents-as-shared-constitution.md)\n-- [0003 \u2014 Require Skill Gate For MergeGate Readiness](0003-require-skill-gate-for-mergegate-readiness.md)\n-- [0004 \u2014 Use GitHub First With A Repository-Local Shiki Mirror](0004-use-github-first-with-shiki-mirror.md)\n-- [0005 \u2014 Use Codex As Front And Claude Code Action As The GitHub Runtime](0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md)\n-- [0006 \u2014 Separate Codex Implementation, CCA Completion, And MergeGate Authorization](0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md)\n+Using the directory as the source of truth lets independent changes add records\n+without contending on an enumerated index.\n \n-## Adding A New ADR\n+## Adding A Shiki SADR\n \n-1. Copy `template.md` to `docs/adr/NNNN-<short-slug>.md`.\n+1. Copy `template.md` to `docs/adr/SADR-NNNN-<short-slug>.md`.\n 2. Fill in the title, status, context, decision, and consequences.\n-3. Set the status to `Proposed`, then `Accepted` once the decision is approved.\n-4. Open a PR following `CONTRIBUTING.md`. You do not need to edit this index for\n-   the ADR to be discoverable via `docs/adr/*.md`.\n+3. Set the status to `Proposed`, then `Accepted` once the proper authority has\n+   approved the decision.\n+4. Open a PR following `CONTRIBUTING.md`.\n+\n+## Adding A Target ADR\n+\n+1. Copy `template.md` to `docs/adr/NNNN-<short-slug>.md` using the target's own\n+   sequence.\n+2. Cite it as `ADR NNNN`; do not prefix its filename with `SADR-`.\ndiff --git a/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md b/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\nnew file mode 100644\nindex 0000000000000000000000000000000000000000..7455ce1f3bed02ddddb851ac21a1d1a648c9aeba\n--- /dev/null\n+++ b/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\n@@ -0,0 +1,55 @@\n+# SADR-0019: Separate Shiki Platform SADRs From Target ADRs\n+\n+## Status\n+\n+Accepted\n+\n+## Context\n+\n+Shiki installs its platform decision records into `docs/adr/` in every target\n+repository. Shiki and its targets previously used the same numeric\n+`NNNN-<slug>.md` namespace. A target that already owned ADRs 0013 through 0018\n+therefore acquired two records for each number during installation, making an\n+`ADR NNNN` citation ambiguous. Renumbering target decisions would rewrite target\n+history, while renumbering Shiki decisions per target would destroy their stable\n+platform identity.\n+\n+Upgrades also need to remove the former Shiki-owned numeric paths without ever\n+mistaking a target-owned ADR for platform content. The install stamp already\n+records exact shipped paths and content digests, so it can prove ownership only\n+when both still match.\n+\n+## Decision\n+\n+Shiki platform decisions use the `SADR` namespace:\n+\n+- records are named `docs/adr/SADR-NNNN-<slug>.md`;\n+- live platform citations use `SADR-NNNN`;\n+- target product decisions retain `docs/adr/NNNN-<slug>.md` and `ADR NNNN`;\n+- the reference validator resolves the two namespaces independently, so the same\n+  four-digit number may exist once in each namespace.\n+\n+Before a forced upgrade writes anything, the installer inventories the exact\n+former numeric Shiki ADR paths. It may delete a legacy path only when the\n+existing install stamp contains that exact path and its recorded SHA-256 digest\n+matches the file currently on disk. All ownership blockers are reported\n+together and abort the upgrade before its first write. A non-forced install\n+never performs this cleanup.\n+\n+Historical `.shiki/` evidence remains unchanged because it records the namespace\n+that existed when that evidence was produced.\n+\n+## Consequences\n+\n+- Targets may use any ADR number without colliding with Shiki's platform SADRs.\n+- Fresh installs preserve target ADR bytes and install a stable SADR set.\n+- Forced upgrades fail closed when legacy ownership cannot be proven, and a\n+  successful upgrade refreshes the stamp without removed legacy paths.\n+- Current governance, packaging, workflow, prompt, script, and skill references\n+  must move with the platform records.\n+\n+## Alternatives Considered\n+\n+- Collision-only refusal leaves affected targets permanently unupgradable.\n+- Renumbering target ADRs mutates target-owned history.\n+- Per-target Shiki renumbering makes platform citations non-portable.\ndiff --git a/docs/adr/template.md b/docs/adr/template.md\nindex a92a97daddd0b00ab9dd626cc8d86942da2eafed..17dd79175b5fee2105935557f691b40cf9492942\n--- a/docs/adr/template.md\n+++ b/docs/adr/template.md\n@@ -1,14 +1,14 @@\n-# NNNN: <Short Decision Title>\n+# <SADR-NNNN or NNNN>: <Short Decision Title>\n \n ## Status\n \n-Proposed | Accepted | Superseded by ADR-NNNN\n+Proposed | Accepted | Superseded by SADR-NNNN or target ADR NNNN\n \n ## Context\n \n What is the situation, constraint, or force that requires a decision? Reference\n-relevant domain terms (`CONTEXT.md`), prior ADRs, and code reality. State the\n-problem, not the solution.\n+relevant domain terms (`CONTEXT.md`), prior Shiki SADRs or target ADRs, and code\n+reality. State the problem, not the solution.\n \n ## Decision\n \ndiff --git a/docs/agents/autonomous-loop-e2e-acceptance.md b/docs/agents/autonomous-loop-e2e-acceptance.md\nindex bbf84c6081987f159ac6ad9ecf3c82b7aa72e7e8..5c13c1d02a05a90b0dc46cbef7c95a9846d0c237\n--- a/docs/agents/autonomous-loop-e2e-acceptance.md\n+++ b/docs/agents/autonomous-loop-e2e-acceptance.md\n@@ -40,8 +40,8 @@\n 3. `gh auth status` is logged in with merge permission on the repo.\n 4. Branch protection + required checks are configured exactly as the goal-loop\n    required-check set expects (see `docs/agents/branch-protection-smoke.md`).\n-5. The repo `.shiki/policy` permits autonomous low/medium-risk merges (ADR\n-   0008/0009); the MergeGate policy check is the recorded-authority gate.\n+5. The repo `.shiki/policy` permits autonomous low/medium-risk merges\n+   (SADR-0008/0009); the MergeGate policy check is the recorded-authority gate.\n \n The operator's only actions are: register the task and start the loop. **No\n manual pre-clearing** means after `shiki loop run` starts, the operator does not\ndiff --git a/docs/agents/bootstrap-command.md b/docs/agents/bootstrap-command.md\nindex 05f7f203a52cf412b9f97084a6e131ed0f785afd..f7aa0b734dfebee4a3b139335b66ee3415f48713\n--- a/docs/agents/bootstrap-command.md\n+++ b/docs/agents/bootstrap-command.md\n@@ -189,7 +189,9 @@\n ```\n \n Use this only for tests, fixtures, or explicit local-only template inspection.\n-Use `--force` only when you intentionally want to overwrite existing target files.\n+Use `--force` only when you intentionally want to refresh the target template.\n+Legacy numeric Shiki decision cleanup is stamp-and-digest authorized before any\n+write; target ADR paths are not cleanup candidates.\n \n ## Slash Command\n \ndiff --git a/docs/agents/checklists.md b/docs/agents/checklists.md\nindex 4a7f03ade346f53a1c1d82981f3fd744f9ab4547..0c5ec6c53dc7197b7b7a7d18e667b325babd6c45\n--- a/docs/agents/checklists.md\n+++ b/docs/agents/checklists.md\n@@ -32,18 +32,18 @@\n | ID | Item | Blocking |\n | --- | --- | --- |\n | GD-01 | Plan was challenged against `CONTEXT.md`. | blocking for non-trivial Goals |\n-| GD-02 | Relevant ADRs were read or absence was recorded. | blocking for non-trivial Goals |\n+| GD-02 | Relevant SADRs and target ADRs were read or absence was recorded. | blocking for non-trivial Goals |\n | GD-03 | Ambiguous terms were resolved or converted to HITL blockers. | blocking |\n | GD-04 | Edge scenarios were considered. | advisory |\n | GD-05 | Code reality was checked when code could answer. | blocking when applicable |\n | GD-06 | New domain terms were added to `CONTEXT.md` when needed. | blocking when applicable |\n-| GD-07 | ADR was created or explicitly skipped using the ADR criteria. | blocking when applicable |\n+| GD-07 | A SADR or target ADR was created or explicitly skipped using the decision-record criteria. | blocking when applicable |\n \n ## CI \u2014 Context & Impact Checklist\n \n | ID | Item | Blocking |\n | --- | --- | --- |\n-| CI-01 | Relevant docs and ADRs identified. | blocking |\n+| CI-01 | Relevant docs, SADRs, and target ADRs identified. | blocking |\n | CI-02 | Relevant modules, interfaces, seams, callers, and tests identified. | blocking |\n | CI-03 | Dependencies and blocked-by relationships identified. | blocking |\n | CI-04 | Candidate locks identified. | blocking |\n@@ -62,7 +62,7 @@\n | PRD-04 | Implementation decisions are recorded without volatile code detail. | blocking |\n | PRD-05 | Testing decisions are recorded. | blocking |\n | PRD-06 | Out-of-scope items are explicit. | blocking |\n-| PRD-07 | Relevant glossary/ADR links are present when applicable. | blocking |\n+| PRD-07 | Relevant glossary/SADR/target-ADR links are present when applicable. | blocking |\n \n ## SF \u2014 Spec Freeze Checklist\n \n@@ -98,7 +98,7 @@\n | H-02 | Branch/worktree target is present. | blocking |\n | H-03 | Scope and non-goals are present. | blocking |\n | H-04 | Dependencies and locks are present. | blocking |\n-| H-05 | Relevant docs, ADRs, modules, and tests are referenced. | blocking |\n+| H-05 | Relevant docs, SADRs, target ADRs, modules, and tests are referenced. | blocking |\n | H-06 | Required skills are stated. | blocking |\n | H-07 | TDD expectations are stated. | blocking when behavior work |\n | H-08 | Acceptance criteria are included. | blocking |\ndiff --git a/docs/agents/codex-handoff.md b/docs/agents/codex-handoff.md\nindex ede468f47cb277f69a857e8c45df703e4ed0151d..6f97bdffb67e44d7edea2c71e01ea28933c4eb6c\n--- a/docs/agents/codex-handoff.md\n+++ b/docs/agents/codex-handoff.md\n@@ -46,7 +46,7 @@\n \n ## Context\n - Domain terms:\n-- Relevant ADRs:\n+- Relevant SADRs and target ADRs:\n - Relevant docs:\n - Relevant modules/interfaces/seams:\n - Similar tests/prior art:\ndiff --git a/docs/agents/completion-check-agent.md b/docs/agents/completion-check-agent.md\nindex 13e4c035db35025f4e3cdcc5adfb24314ac4d521..61bfa1218cf36ca605250c91919e620f33238aa0\n--- a/docs/agents/completion-check-agent.md\n+++ b/docs/agents/completion-check-agent.md\n@@ -32,7 +32,7 @@\n - `AGENTS.md`\n - `CLAUDE.md`\n - `CONTEXT.md`\n-- relevant ADRs\n+- relevant SADRs and target ADRs\n - parent Goal issue\n - PRD issue or document\n - task issue\ndiff --git a/docs/agents/decision-control.md b/docs/agents/decision-control.md\nindex 7d8ee5ab03a5ccbcbd04056ae61de03785a2a9c9..8c94ecda00fffaa0e92c325ab83aa9776bd1dac1\n--- a/docs/agents/decision-control.md\n+++ b/docs/agents/decision-control.md\n@@ -11,7 +11,7 @@\n | Decision | Owner | When | Required inputs | Output | Enforcement surface |\n | --- | --- | --- | --- | --- | --- |\n | Is the Goal clear enough to plan? | Planner + Guardian when needed | Goal intake | User request, existing docs, risk signals | Goal readiness verdict | Goal checklist, issue template, ledger |\n-| Must `grill-with-docs` run? | Planner / Skill Gate | Before PRD or issue split | Ambiguity, domain terms, ADR conflicts, unclear boundaries | Skill invocation or explicit skip rationale | Skill Gate checklist |\n+| Must `grill-with-docs` run? | Planner / Skill Gate | Before PRD or issue split | Ambiguity, domain terms, SADR/target-ADR conflicts, unclear boundaries | Skill invocation or explicit skip rationale | Skill Gate checklist |\n | Is a PRD required? | Planner | After Goal context stabilizes | Goal, `grill-with-docs` output, Context & Impact output | PRD required / not required | PRD checklist, CCA evidence check |\n | Can the spec be frozen? | Operator only | End of Requirements Definition, before issue split | PRD/requirements summary, scope inventory (SF-02), open-question list | `spec_freeze` block in the plan + ledger evidence | `require_grilled_plan` (plan ingest/run), SF checklist |\n | Can a frozen spec change? | Operator only (Spec Amendment) | When implementation contradicts the frozen spec | Contested decisions, scoped re-grill result | recorded amendment + re-stamped freeze | SF checklist, ledger evidence |\ndiff --git a/docs/agents/domain.md b/docs/agents/domain.md\nindex 258da8ea81e87ac0a6b0b5b5e5c2fde980eaabb7..d57b0cf17ff3e1fa9e6e5885372bf083d8295421\n--- a/docs/agents/domain.md\n+++ b/docs/agents/domain.md\n@@ -5,7 +5,9 @@\n ## Layout\n \n - `CONTEXT.md` defines Shiki language. Keep it implementation-free.\n-- `docs/adr/` records hard-to-reverse decisions.\n+- `docs/adr/SADR-NNNN-*.md` records Shiki platform decisions.\n+- `docs/adr/NNNN-*.md` records target product decisions without sharing the\n+  SADR namespace.\n - `AGENTS.md` is the runtime-neutral agent constitution.\n - `CLAUDE.md` is the Claude Code wrapper around `AGENTS.md`.\n - `.shiki/` is the Target Repository mirror for Goals, PRDs, plans, Task DAGs, contracts, locks, worktree records, ledger entries, CCA verdicts, repair packets, reports, and handoffs.\n@@ -15,6 +17,6 @@\n \n - Use Shiki terms exactly when they exist.\n - Add glossary terms to `CONTEXT.md` only when the term is part of the domain, not a generic programming concept.\n-- Create ADRs only for hard-to-reverse, surprising, tradeoff-based decisions.\n+- Create SADRs or target ADRs only for hard-to-reverse, surprising, tradeoff-based decisions.\n - Keep product-specific language in the Target Repository, not in Shiki Template docs.\n-- If an ADR conflicts with a proposed implementation, surface the conflict before editing.\n+- If a SADR or target ADR conflicts with a proposed implementation, surface the conflict before editing.\ndiff --git a/docs/agents/execution-control.md b/docs/agents/execution-control.md\nindex ad8ee3cdb36be56fedc1f46408ca92b387d74047..bc63c3a73661d12b567016ba299f1a6b66bf9088\n--- a/docs/agents/execution-control.md\n+++ b/docs/agents/execution-control.md\n@@ -63,7 +63,7 @@\n | Branch name | Orchestrator | Orchestrator | Before implementer dispatch | branch policy | branch ref in task/ledger |\n | Lock reservation | Context & Impact | MergeGate | Before dispatch and before merge | `.shiki/locks/`, labels, PR metadata | lock id, owner, scope |\n | CI profile | Planner | MergeGate | During issue triage, rechecked at PR | labels, `.shiki/policy.yaml`, workflow matrix | required check list |\n-| Required status checks | Repo maintainer / Guardian | GitHub branch protection / rulesets | Repository setup, updated via ADR | GitHub settings | branch/ruleset config, ADR |\n+| Required status checks | Repo maintainer / Guardian | GitHub branch protection / rulesets | Repository setup, updated via target ADR | GitHub settings | branch/ruleset config, target ADR |\n | CCA judgment | CCA | GitHub required check / MergeGate | Every non-draft PR update | GitHub Action check | structured CCA verdict |\n | Repair assignment | CCA | MergeGate | After failed CCA/CI/review | repair packet, issue/PR comment | repair id and attempt count |\n | Merge readiness | MergeGate | GitHub branch protection / Guardian if needed | After CCA complete | required checks, review rules, rulesets | mergegate check output |\ndiff --git a/docs/agents/guardian-policy.md b/docs/agents/guardian-policy.md\nindex 4147c7adf05a47adb4fd24ada1a1c2e1f2ba6d2d..a20d9cc6c00a569bb057b9ca8d6b51c7fc4a9847\n--- a/docs/agents/guardian-policy.md\n+++ b/docs/agents/guardian-policy.md\n@@ -98,8 +98,8 @@\n `origin/<default_branch>` and reuses each job's existing base fetch; a shallow\n (`--depth`) or second base fetch would make the proof unresolvable and silently\n disable the carry, so neither is permitted. CCA reads `guardian_comment_carried` as\n-a recorded authority in the deterministic signal and does not re-derive it. See ADR\n-0018 for the full decision, the rejected diff/patch-equality and path-disjointness\n+a recorded authority in the deterministic signal and does not re-derive it. See\n+SADR-0018 for the full decision, the rejected diff/patch-equality and path-disjointness\n alternatives, and the accepted residual.\n \n ## External AI Guardian Review (SADR-0010)\ndiff --git a/docs/agents/implementation-policy.md b/docs/agents/implementation-policy.md\nindex c754144a9fb5bf4a38aa82a60e1670c59e626b44..85c37281bcad196c6b7b02343faf00b03cd9c57a\n--- a/docs/agents/implementation-policy.md\n+++ b/docs/agents/implementation-policy.md\n@@ -31,10 +31,13 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n ## Non-Negotiables\n \n 1. **Planning precedes implementation.** Non-trivial Goals start with `grill-with-docs` and Context & Impact.\n-2. **Domain language is controlled.** Terms must be checked against `CONTEXT.md`; hard-to-reverse tradeoffs must be checked against ADRs.\n+2. **Domain language is controlled.** Terms must be checked against `CONTEXT.md`; hard-to-reverse tradeoffs must be checked against Shiki SADRs and target ADRs.\n 3. **PRD precedes issue decomposition.** `to-prd` is used once the context is settled enough to become durable intent.\n 4. **Issues are vertical slices.** `to-issues` must produce independently grabbable tracer-bullet tasks, not horizontal layer tickets.\n 5. **Triage controls dispatch.** Only `ready-for-agent` / AFK tasks may be assigned to Codex without human input.\n@@ -95,19 +98,19 @@\n Process:\n \n - Challenge terms against `CONTEXT.md`.\n-- Check ADRs before reopening decisions.\n+- Check SADRs and target ADRs before reopening decisions.\n - Inspect code when code can answer a question.\n - Ask one question at a time when the operator is available.\n - Provide a recommended answer with each question.\n - Record resolved terms in `CONTEXT.md`.\n-- Create ADRs only for hard-to-reverse, surprising, tradeoff-based decisions.\n+- Create SADRs or target ADRs only for hard-to-reverse, surprising, tradeoff-based decisions.\n - Convert unresolved load-bearing questions into HITL blockers.\n \n Outputs:\n \n - Resolved design-tree decisions.\n - Updated glossary terms when needed.\n-- ADRs when justified.\n+- SADRs or target ADRs when justified.\n - HITL blockers when decisions remain unresolved.\n \n ## Phase 3 \u2014 Context & Impact\n@@ -117,7 +120,7 @@\n \n Outputs must include:\n \n-- Relevant documents and ADRs.\n+- Relevant documents, SADRs, and target ADRs.\n - Relevant modules, interfaces, seams, callers, and tests.\n - Candidate locks.\n - Dependencies.\n@@ -140,7 +143,7 @@\n - Testing decisions.\n - Out of scope.\n - Further notes.\n-- Links to relevant Goal, domain terms, and ADRs.\n+- Links to relevant Goal, domain terms, SADRs, and target ADRs.\n \n Phase 4 ends with Spec Freeze: the operator explicitly approves the PRD, the\n plan records a `spec_freeze` block (status, approved_by, source), and the SF\n@@ -240,7 +243,7 @@\n - Dependencies and locks.\n - Risk and Guardian requirements.\n - Ledger completeness.\n-- Documentation and ADR updates.\n+- Documentation and SADR/target-ADR updates.\n - Scope drift.\n \n CCA emits one of:\n@@ -301,7 +304,7 @@\n | Evidence failure | Behavior might be done, but proof is missing. | Add checks, PR evidence, ledger entry, or artifacts. |\n | Scope drift | PR changes unrelated behavior. | Revert unrelated changes or split into new Goal. |\n | Dependency block | Upstream task or lock not complete. | Block until dependency resolves. |\n-| Architecture block | Decision needs ADR or deep-module discussion. | `grill-with-docs` or `improve-codebase-architecture`. |\n+| Architecture block | Decision needs a SADR, target ADR, or deep-module discussion. | `grill-with-docs` or `improve-codebase-architecture`. |\n | Risk block | Security/production/policy change needs human approval. | Guardian decision. |\n \n ## Phase 5.5 \u2014 Execution Decision Control\ndiff --git a/docs/agents/runtime-auth-model.md b/docs/agents/runtime-auth-model.md\nindex 6a2e1a47e81ea89b5c68a210ec07772a9374babc..3c54748d24233dbd0ba8473950282547ef4ce935\n--- a/docs/agents/runtime-auth-model.md\n+++ b/docs/agents/runtime-auth-model.md\n@@ -148,7 +148,7 @@\n \n API-key based runners may be added by a target repository as an explicit extension. They are not the default Shiki template path.\n \n-If a repo opts into API-key mode, record the exception in an ADR and update the repo's `AGENTS.md`, `.shiki/config.yaml`, and workflow permissions.\n+If a repo opts into API-key mode, record the target-specific exception in a target ADR and update the repo's `AGENTS.md`, `.shiki/config.yaml`, and workflow permissions.\n \n ## GitHub Actions Runtime Compatibility\n \ndiff --git a/docs/agents/skill-gate.md b/docs/agents/skill-gate.md\nindex c50398b0d7c60a8598f2a5c88a10015730dced13..3acffe6444617bb1d54b77d4b4708158ec735bc6\n--- a/docs/agents/skill-gate.md\n+++ b/docs/agents/skill-gate.md\n@@ -9,7 +9,7 @@\n | Skill | Trigger | Expected output |\n | --- | --- | --- |\n | `setup-matt-pocock-skills` | First configuring a repo for these skills, or when issue tracker, triage labels, or domain docs are missing. | `docs/agents/` configuration and an `Agent skills` block in `AGENTS.md` or `CLAUDE.md`. |\n-| `grill-with-docs` | Ambiguous plan, unclear terminology, boundary decision, tradeoff, or ADR-worthy design choice. | Clarified decision tree, recommended answers, updates to `CONTEXT.md` or ADRs when decisions crystallize. |\n+| `grill-with-docs` | Ambiguous plan, unclear terminology, boundary decision, tradeoff, or decision-record-worthy design choice. | Clarified decision tree, recommended answers, updates to `CONTEXT.md`, SADRs, or target ADRs when decisions crystallize. |\n | `zoom-out` | Agent lacks a higher-level architectural map. | Map of relevant modules, callers, seams, and domain concepts. |\n | `to-prd` | Settled context should become a PRD. | PRD published to the issue tracker with problem, solution, user stories, decisions, testing, and out-of-scope notes. |\n | `to-issues` | Goal, PRD, or plan must become implementation tickets. | Vertical-slice issues with acceptance criteria, dependencies, and AFK/HITL classification. |\n@@ -17,7 +17,7 @@\n | `tdd` | Feature work or bug fix has observable behavior that can be specified with tests. | Red-green-refactor loop through public interfaces, one vertical slice at a time. |\n | `code-review` | The TDD loop is green and the implementer is about to create or update a PR. Default for every implementation task. | Pre-PR self-review of the task diff with fixes applied on the task branch; findings and resolutions recorded as a ledger entry naming the skill and a `## Pre-PR code review` PR body section. |\n | `diagnose` | Hard bug, failing check, regression, flaky behavior, or performance problem. | Reproduction loop, ranked hypotheses, instrumentation, fix, regression test, post-mortem. |\n-| `improve-codebase-architecture` | Structural friction, testability, AI-navigability, deep-module opportunity, or refactor request. | Architecture candidates and deepening recommendations informed by `CONTEXT.md` and ADRs. |\n+| `improve-codebase-architecture` | Structural friction, testability, AI-navigability, deep-module opportunity, or refactor request. | Architecture candidates and deepening recommendations informed by `CONTEXT.md`, SADRs, and target ADRs. |\n | `prototype` | A throwaway logic or UI prototype is needed to answer a design question. | Clearly marked disposable prototype and captured decision. |\n \n ## MergeGate Interaction\ndiff --git a/docs/operations.md b/docs/operations.md\nindex cf1f452d4c589e38ccd58553bda0ad8684b51eb7..ace48a90ffbec9e5c0a7113bef16a4858bc44b3f\n--- a/docs/operations.md\n+++ b/docs/operations.md\n@@ -89,6 +89,16 @@\n      target history, brought forward by `shiki migrate`, not a template.\n    - **Everything else** (CLI scripts, workflows, schemas, docs) is overwritten.\n \n+   A forced upgrade also migrates the former numeric Shiki decision paths to\n+   `SADR-NNNN-*`. Before the first write, the installer inventories the exact\n+   legacy Shiki paths and authorizes each deletion only when the existing\n+   `.shiki/install-stamp.json` names that path and its SHA-256 digest still\n+   matches the file. An absent, unreadable, malformed, incomplete, or\n+   mismatching stamp reports every blocked path and stops with no filesystem\n+   change. Target-owned `NNNN-*` ADRs are not cleanup candidates. A non-force\n+   install deletes nothing and retains any still-matching legacy stamp entries\n+   so a later forced upgrade can prove ownership.\n+\n    If the target has pending migrations, `--force` refuses and writes nothing \u2014\n    apply migrations first (step 3), then re-run. The run ends with a single\n    summary of every `.new` written, so a half-upgrade is visible.\ndiff --git a/docs/releasing.md b/docs/releasing.md\nindex 10d73828e7dbd64e9d9f6b83bc84d010537ce62a..aafeee8c4d441e1e427efee2ce4f81a647f020e1\n--- a/docs/releasing.md\n+++ b/docs/releasing.md\n@@ -67,7 +67,7 @@\n CLI in `scripts/`. There are no third-party Python runtime dependencies. The CLI\n is intentionally **not** exposed as a `[project.scripts]` console entry point\n because `scripts/shiki.py` is a dependency-free shim and `scripts/` is not\n-packaged as an importable distribution (see the ADR for the rationale).\n+packaged as an importable distribution (see SADR-0007 for the rationale).\n \n Supported install paths:\n \n@@ -155,6 +155,15 @@\n    [docs/operations.md](operations.md#the-new-convention). If the target has\n    pending migrations, `--force` refuses and writes nothing; apply migrations\n    (step 2) first, then re-run.\n+\n+   The SADR namespace upgrade has an additional pre-write gate: each former\n+   numeric Shiki decision path is removed only when the existing install stamp\n+   contains that exact path and a digest matching its current bytes. The\n+   installer aggregates absent/unreadable/malformed-stamp, missing-digest, and\n+   digest-mismatch blockers before raising, so refusal leaves the target tree\n+   byte-identical. Numeric target ADRs are preserved. A successful forced\n+   upgrade installs and stamps `SADR-*` paths and drops the removed legacy paths\n+   from the refreshed stamp.\n 2. Apply repository-local `.shiki/` state migrations with the migrate command:\n \n    ```bash\ndiff --git a/docs/usage.md b/docs/usage.md\nindex b83407ee3970dd395dd893010bc5dbe8bad26051..a130ef6e40dfd8169a5bc701339351097df1a09f\n--- a/docs/usage.md\n+++ b/docs/usage.md\n@@ -67,8 +67,10 @@\n shiki install-target /path/to/target-repo --local-only\n ```\n \n-Use `--force` only when you intentionally want to overwrite existing target\n-files.\n+Use `--force` only when you intentionally want to refresh the target template.\n+For the SADR namespace migration, `--force` deletes only exact legacy Shiki\n+decision paths whose existing install-stamp digest still matches; any ownership\n+blocker stops before the first write. Numeric target ADRs are never candidates.\n \n ## `shiki bootstrap-platform` \u2014 publish the Shiki platform repo\n \ndiff --git a/scripts/guardian_approval_signal.py b/scripts/guardian_approval_signal.py\nindex d059f92b2c82aa2b5d6ca421365537a87886c1cb..16924892714f4f5b9303c0ef0e325a7e70075630\n--- a/scripts/guardian_approval_signal.py\n+++ b/scripts/guardian_approval_signal.py\n@@ -199,8 +199,8 @@\n ) -> Any:\n     \"\"\"Evaluate SADR-0015 Contract Approval for the CCA Guardian signal.\n \n-    The SAME OR that MergeGate applies must reach this deterministic signal: ADR\n-    0015 is explicit that without it, a contract-approved PR still returns\n+    The SAME OR that MergeGate applies must reach this deterministic signal:\n+    SADR-0015 is explicit that without it, a contract-approved PR still returns\n     ``needs_guardian``, the CCA job fails, and the MergeGate policy check never\n     runs \u2014 the gate this change moves would never be reached. This resolves the\n     task the SAME way MergeGate does (the first Shiki task id in the PR body),\ndiff --git a/scripts/shiki_contract_approval.py b/scripts/shiki_contract_approval.py\nindex 941bcb96fdd8b9a85bc0c4f0f04d5dad9d04839c..4a4b418f6527413161db82670bec857a043360e2\n--- a/scripts/shiki_contract_approval.py\n+++ b/scripts/shiki_contract_approval.py\n@@ -142,7 +142,7 @@\n     changed_files_status: Any,\n     registration: Any,\n ) -> ContractApprovalResult:\n-    \"\"\"Whether this PR carries an SADR-0015 Contract Approval.\n+    \"\"\"Whether this PR carries a SADR-0015 Contract Approval.\n \n     The four conditions are evaluated IN ORDER, each failure recorded as a\n     distinct reason string:\ndiff --git a/scripts/shiki_guardian.py b/scripts/shiki_guardian.py\nindex a4ecb79bbda55fd5c8b06adfae2eb99760a03c45..4dd99bbe714e26f421fa19abac7a34ae1b195ecc\n--- a/scripts/shiki_guardian.py\n+++ b/scripts/shiki_guardian.py\n@@ -12,7 +12,7 @@\n # The carry re-derives its proof from git in-process on every judging invocation;\n # it is never read from an artifact, so there is nothing to go stale or be forged.\n # shiki_sync_proof is the ONLY guardian-family module that runs git, and every one\n-# of its invocations is read-only and local (ADR: prove-from-git base sync).\n+# of its invocations is read-only and local (SADR: prove-from-git base sync).\n from shiki_sync_proof import verify_pure_base_sync\n \n GUARDIAN_POLICY_PATH = \".shiki/guardian-policy.json\"\ndiff --git a/scripts/shiki_guardian_review.py b/scripts/shiki_guardian_review.py\nindex dd0f104d457899e0ba5ff1e162011c1e90b6f92b..e10ef58c046324e7ea3a3392feb3048ca77414ce\n--- a/scripts/shiki_guardian_review.py\n+++ b/scripts/shiki_guardian_review.py\n@@ -159,7 +159,7 @@\n     docs_only = _is_docs_only(changed_files)\n     if docs_only and matched == 0:\n         focus.append(\n-            \"Docs/ADR-only change: confirm the change is genuinely documentation/decision-only, introduces no behavior or contract drift, and stays consistent with the constitution and existing ADRs.\"\n+            \"Docs/decision-record-only change: confirm the change is genuinely documentation/decision-only, introduces no behavior or contract drift, and stays consistent with the constitution and existing SADRs and target ADRs.\"\n         )\n \n     # Conservative augmentation: unknown (no category and not docs-only), mixed\ndiff --git a/scripts/shiki_installer.py b/scripts/shiki_installer.py\nindex be064ab7a29ff6836f7f7a1d70500a43e0d11d7b..9fd8eefd137ce7003dad5c0dcb2e48e2cd47ab32\n--- a/scripts/shiki_installer.py\n+++ b/scripts/shiki_installer.py\n@@ -118,6 +118,7 @@\n # what lets a target carry it without a governance-schema change.\n INSTALL_STAMP_PATH = \".shiki/install-stamp.json\"\n INSTALL_STAMP_VERSION = 1\n+LEGACY_SHIKI_SADR_MAX = 18\n \n # Under --force the shipped surface splits three ways, so nothing is lost and\n # nothing is silently stale.\n@@ -237,9 +238,98 @@\n         )\n \n \n+def legacy_shiki_adr_paths() -> tuple[str, ...]:\n+    \"\"\"Former numeric paths for Shiki SADR-0001 through SADR-0018.\n+\n+    The authoritative slugs come from the current SADR filenames. SADR-0019 is\n+    the namespace decision itself and never shipped under a numeric legacy path.\n+    \"\"\"\n+    paths: list[str] = []\n+    for sadr in sorted((ROOT / \"docs\" / \"adr\").glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\")):\n+        number = sadr.name.split(\"-\", 2)[1]\n+        if int(number) <= LEGACY_SHIKI_SADR_MAX:\n+            paths.append(f\"docs/adr/{sadr.name.removeprefix('SADR-')}\")\n+    return tuple(paths)\n+\n+\n+def preflight_legacy_sadr_cleanup(target: Path, *, force: bool) -> tuple[str, ...]:\n+    \"\"\"Authorize exact legacy Shiki ADR removals before the first write.\n+\n+    A non-force install never removes existing files. A forced install needs no\n+    stamp when no legacy Shiki path is present. When legacy paths do exist, each\n+    must be named in a readable install stamp and its current bytes must match\n+    the recorded digest.\n+    \"\"\"\n+    if not force:\n+        return ()\n+    present = tuple(\n+        relative\n+        for relative in legacy_shiki_adr_paths()\n+        if (target / relative).exists() or (target / relative).is_symlink()\n+    )\n+    if not present:\n+        return ()\n+\n+    stamp_path = target / INSTALL_STAMP_PATH\n+    try:\n+        raw = stamp_path.read_text(encoding=\"utf-8\")\n+        stamp = json.loads(raw)\n+    except FileNotFoundError:\n+        detail = \"install stamp is absent\"\n+        blockers = [f\"{relative}: {detail}\" for relative in present]\n+    except (OSError, UnicodeDecodeError) as error:\n+        detail = f\"install stamp is unreadable ({error})\"\n+        blockers = [f\"{relative}: {detail}\" for relative in present]\n+    except json.JSONDecodeError as error:\n+        detail = f\"install stamp is malformed ({error})\"\n+        blockers = [f\"{relative}: {detail}\" for relative in present]\n+    else:\n+        if not isinstance(stamp, dict):\n+            blockers = [\n+                f\"{relative}: install stamp is malformed (expected an object)\"\n+                for relative in present\n+            ]\n+        else:\n+            digests = stamp.get(\"digests\")\n+            blockers = []\n+            if not isinstance(digests, dict):\n+                blockers.extend(\n+                    f\"{relative}: install stamp has no digest for this path\"\n+                    for relative in present\n+                )\n+            else:\n+                for relative in present:\n+                    expected = digests.get(relative)\n+                    if not isinstance(expected, str) or not expected:\n+                        blockers.append(\n+                            f\"{relative}: install stamp has no digest for this path\"\n+                        )\n+                        continue\n+                    try:\n+                        actual = _sha256_file(target / relative)\n+                    except OSError as error:\n+                        blockers.append(\n+                            f\"{relative}: legacy path is unreadable ({error})\"\n+                        )\n+                        continue\n+                    if actual != expected:\n+                        blockers.append(\n+                            f\"{relative}: digest mismatch (stamp {expected}, current {actual})\"\n+                        )\n+\n+    if blockers:\n+        joined = \"\\n\".join(f\"- {blocker}\" for blocker in blockers)\n+        raise ShikiError(\n+            \"legacy Shiki ADR cleanup is not authorized:\\n\"\n+            f\"{joined}\\nNo files were written.\"\n+        )\n+    return present\n+\n+\n def install_template(target: Path, *, force: bool, validate: bool) -> list[NewFileNote]:\n     # Preconditions first: never rewrite the tree of a target that cannot be\n     # upgraded cleanly (see refuse_pending_migrations).\n+    legacy_cleanup = preflight_legacy_sadr_cleanup(target, force=force)\n     refuse_pending_migrations(target)\n \n     # Resolve the target's required CODEOWNERS owner once from its\n@@ -250,6 +340,10 @@\n     # verbatim. See ``codeowners_required_owner``.\n     codeowners_owner = codeowners_required_owner(target)\n \n+    for relative in legacy_cleanup:\n+        (target / relative).unlink()\n+        info(f\"removed stamped legacy Shiki ADR: {target / relative}\")\n+\n     notes: list[NewFileNote] = []\n     for relative in TEMPLATE_PATHS:\n         source = ROOT / relative\n@@ -647,6 +741,22 @@\n     digests = target_stamp_digests(target)\n     existing = read_install_stamp(target)\n     if existing is not None:\n+        # A non-force install deliberately leaves legacy Shiki ADRs in place.\n+        # Preserve only still-matching ownership proof so a later forced\n+        # upgrade can authorize cleanup; never carry a missing or stale digest.\n+        existing_digests = existing.get(\"digests\")\n+        if isinstance(existing_digests, dict):\n+            for relative in legacy_shiki_adr_paths():\n+                expected = existing_digests.get(relative)\n+                path = target / relative\n+                if not isinstance(expected, str) or not path.is_file():\n+                    continue\n+                try:\n+                    actual = _sha256_file(path)\n+                except OSError:\n+                    continue\n+                if actual == expected:\n+                    digests[relative] = actual\n         if _incoming_is_older(commit, existing.get(\"platform_commit\")):\n             warn(\n                 \"kept existing install stamp: incoming install \"\ndiff --git a/scripts/shiki_loop.py b/scripts/shiki_loop.py\nindex 3611641da8aeb5038b1da90e3114b09a473ec6d8..84caa491b9cad0edc4176f209c13e9221e975681\n--- a/scripts/shiki_loop.py\n+++ b/scripts/shiki_loop.py\n@@ -424,7 +424,7 @@\n         # unresolvable CCA verdict has already produced its terminal stop and is never\n         # laundered into a head-moving sync (SADR-0010). It also never fires while the\n         # policy (Guardian) gate is red \u2014 for ANY reason, not only when it is the sole\n-        # red check \u2014 so a head-bound Guardian approval, or an SADR-0015\n+        # red check \u2014 so a head-bound Guardian approval, or a SADR-0015\n         # contract-approval carry that base movement defeated (the policy gate compares\n         # base and head task governance directly), is never silently restored by\n         # adopting base wholesale. It is bounded by a durable per-task counter so the\ndiff --git a/scripts/shiki_tasks.py b/scripts/shiki_tasks.py\nindex aec3809f24b4a0c8c96fc50971cff9ae9ad30381..d531052e9d9717058d4631483cefab44c26a65c1\n--- a/scripts/shiki_tasks.py\n+++ b/scripts/shiki_tasks.py\n@@ -821,7 +821,7 @@\n         \"prompt\": prompt,\n         \"entry_skill\": \"grill-with-docs\",\n         \"required_next_steps\": [\n-            \"Run grill-with-docs until terminology, boundaries, risks, and ADR-worthy decisions are settled.\",\n+            \"Run grill-with-docs until terminology, boundaries, risks, and decision-record-worthy choices are settled.\",\n             \"Obtain the operator's explicit approval of the PRD/requirements (Spec Freeze).\",\n             \"Write a machine-readable plan JSON with grill_with_docs.status=complete and spec_freeze.status=frozen.\",\n             \"Run shiki plan ingest --plan-file PLAN.json.\",\ndiff --git a/scripts/test_shiki_goal_loop.sh b/scripts/test_shiki_goal_loop.sh\nindex 753c7b516e10f0be3bc92a4bd0eb1fa699776ae6..8f3820a0a079b3044424cfe1a6c6b7fe68896e67\n--- a/scripts/test_shiki_goal_loop.sh\n+++ b/scripts/test_shiki_goal_loop.sh\n@@ -231,8 +231,8 @@\n \n # High/critical risk merges autonomously when all required checks are green:\n # the \"MergeGate policy check\" required check IS the Guardian gate (it enforces\n-# guardian-policy.json \u2014 human approval OR external AI guardian review, ADR\n-# 0010), so green checks mean a recorded authority approved. The fake gh returns\n+# guardian-policy.json \u2014 human approval OR external AI guardian review,\n+# SADR-0010), so green checks mean a recorded authority approved. The fake gh returns\n # green checks, simulating that the Guardian gate passed.\n python3 \"$ROOT/scripts/shiki.py\" goal create --target \"$TARGET\" --title \"Guardian gate\" --outcome \"High risk merges via policy gate\" >/tmp/shiki-goal-loop-guardian-goal.json\n GGOAL=\"$(json_get /tmp/shiki-goal-loop-guardian-goal.json goal_id)\"\ndiff --git a/tests/test_adr_references.py b/tests/test_adr_references.py\nindex 51706329c797d5dd9b47e082a333fa83b261d482..db6e693e35f67103935be34f1d9bb4ff7ee60395\n--- a/tests/test_adr_references.py\n+++ b/tests/test_adr_references.py\n@@ -1,7 +1,7 @@\n-\"\"\"Every ``ADR ####`` citation must resolve to exactly one decision record.\n+\"\"\"Every target ADR and Shiki SADR citation resolves in its own namespace.\n \n Background. ``scripts/mergegate_check.py`` justifies ``is_bookkeeping_closeout``\n-with \"ADR 0017\" nine times and ``scripts/guardian_approval_signal.py`` cites\n+with \"SADR-0017\" nine times and ``scripts/guardian_approval_signal.py`` cites\n 0016/0017 seven times, yet for a stretch the numbering in ``docs/adr/`` stopped\n at 0015: the authoring commits lived on a coordinator branch and no task ever\n declared ``docs/adr/...`` in its locks, so nothing carried the records to the\n@@ -11,11 +11,12 @@\n and the directory drifted with no signal.\n \n This is the same unbound-pair shape behind other silent failures: a reference\n-and its target with nothing asserting they agree. This test binds them. It scans\n-every ``scripts/*.py`` (plus ``docs/**/*.md`` and the root ``*.md`` governance\n-files, which are cheap) for the ``ADR ####`` pattern and asserts each cited\n-number resolves to exactly one ``docs/adr/####-*.md``. The converse is\n-deliberately NOT required: an ADR may exist without being cited.\n+and its target with nothing asserting they agree. This test binds them. Target\n+``ADR NNNN`` citations resolve only to ``docs/adr/NNNN-*.md``; Shiki\n+``SADR-NNNN`` citations resolve only to ``docs/adr/SADR-NNNN-*.md``. The scan\n+covers current governance docs, scripts, workflows, prompts, packaging metadata,\n+and repo-local Codex guidance while deliberately excluding historical\n+``.shiki`` evidence. The converse is not required: a record may be uncited.\n \n The suite is dependency free: standard-library ``unittest`` only.\n \"\"\"\n@@ -31,17 +32,18 @@\n \n REPO_ROOT = Path(__file__).resolve().parents[1]\n \n-# \"ADR 0016\" -- a four-digit citation. The trailing \\b keeps a five-digit token\n-# from being read as a truncated four-digit number.\n-_CITATION = re.compile(r\"\\bADR (\\d{4})\\b\")\n-# \"0016-locks-bound-product-paths.md\" -- the leading four digits name the record.\n-_ADR_FILENAME = re.compile(r\"^(\\d{4})-.*\\.md$\")\n+# ``ADR 0016`` / ``SADR-0016`` -- a namespace plus four-digit citation. The\n+# trailing ``\\b`` keeps a five-digit token from being read as a truncated one.\n+_CITATION = re.compile(r\"\\b(S?ADR)[ -](\\d{4})\\b\")\n+# Target ADRs retain the numeric filename; Shiki platform decisions use SADR.\n+_DECISION_FILENAME = re.compile(r\"^(?:(SADR)-)?(\\d{4})-.*\\.md$\")\n \n \n class Violation(NamedTuple):\n     \"\"\"A citation that does not resolve to exactly one decision record.\"\"\"\n \n     citing_file: str  # repo-relative path of the file that cites the ADR\n+    namespace: str  # \"ADR\" for target decisions; \"SADR\" for Shiki decisions\n     number: str  # the cited four-digit number, e.g. \"0016\"\n     line: int  # 1-based line of the citation\n     reason: str  # \"missing\" (zero matches) or \"ambiguous\" (two or more)\n@@ -49,44 +51,59 @@\n \n     @property\n     def expected_path(self) -> str:\n-        return f\"docs/adr/{self.number}-*.md\"\n+        prefix = \"SADR-\" if self.namespace == \"SADR\" else \"\"\n+        return f\"docs/adr/{prefix}{self.number}-*.md\"\n \n     def describe(self) -> str:\n         \"\"\"Name the citing file, the cited number, and the missing path.\"\"\"\n         if self.reason == \"missing\":\n             return (\n-                f\"{self.citing_file}:{self.line} cites ADR {self.number} \"\n+                f\"{self.citing_file}:{self.line} cites {self.namespace} {self.number} \"\n                 f\"but no document matches {self.expected_path}\"\n             )\n         joined = \", \".join(self.matches)\n         return (\n-            f\"{self.citing_file}:{self.line} cites ADR {self.number} which \"\n+            f\"{self.citing_file}:{self.line} cites {self.namespace} {self.number} which \"\n             f\"resolves to {len(self.matches)} documents (expected exactly one \"\n             f\"at {self.expected_path}): {joined}\"\n         )\n \n \n-def _adr_index(root: Path) -> dict[str, list[Path]]:\n-    \"\"\"Map every four-digit ADR number to the record files that claim it.\"\"\"\n-    index: dict[str, list[Path]] = {}\n+def _adr_index(root: Path) -> dict[tuple[str, str], list[Path]]:\n+    \"\"\"Map each decision namespace and number to the files that claim it.\"\"\"\n+    index: dict[tuple[str, str], list[Path]] = {}\n     adr_dir = root / \"docs\" / \"adr\"\n     if adr_dir.is_dir():\n         for path in sorted(adr_dir.glob(\"*.md\")):\n-            match = _ADR_FILENAME.match(path.name)\n+            match = _DECISION_FILENAME.match(path.name)\n             if match:\n-                index.setdefault(match.group(1), []).append(path)\n+                namespace = \"SADR\" if match.group(1) else \"ADR\"\n+                index.setdefault((namespace, match.group(2)), []).append(path)\n     return index\n \n \n def _citing_files(root: Path, include_docs: bool = True) -> list[Path]:\n-    \"\"\"Files scanned for citations: scripts/*.py, and cheaply the markdown.\"\"\"\n+    \"\"\"Current, shipped text surfaces scanned for decision citations.\"\"\"\n     files = sorted((root / \"scripts\").glob(\"*.py\"))\n+    files += sorted((root / \"scripts\").glob(\"*.sh\"))\n     if include_docs:\n         docs = root / \"docs\"\n         if docs.is_dir():\n             files += sorted(docs.rglob(\"*.md\"))\n         files += sorted(root.glob(\"*.md\"))\n-    return files\n+        pyproject = root / \"pyproject.toml\"\n+        if pyproject.is_file():\n+            files.append(pyproject)\n+        for relative in (\".github\", \".codex\"):\n+            surface = root / relative\n+            if surface.is_dir():\n+                files += sorted(\n+                    path\n+                    for path in surface.rglob(\"*\")\n+                    if path.is_file()\n+                    and path.suffix in {\".md\", \".py\", \".sh\", \".toml\", \".yaml\", \".yml\"}\n+                )\n+    return sorted(set(files))\n \n \n def find_adr_reference_violations(\n@@ -94,10 +111,9 @@\n ) -> list[Violation]:\n     \"\"\"Return every ``ADR ####`` citation under ``root`` that fails to resolve.\n \n-    A citation resolves when exactly one ``docs/adr/####-*.md`` matches its\n-    number. Zero matches is a \"missing\" violation; two or more is \"ambiguous\".\n-    Records that are never cited produce nothing -- the converse is not\n-    required.\n+    A citation resolves when exactly one record in the same namespace matches\n+    its number. Zero matches is a \"missing\" violation; two or more is\n+    \"ambiguous\". Records that are never cited produce nothing.\n     \"\"\"\n     root = Path(root)\n     index = _adr_index(root)\n@@ -110,13 +126,15 @@\n         rel = path.relative_to(root).as_posix()\n         for lineno, line in enumerate(text.splitlines(), start=1):\n             for match in _CITATION.finditer(line):\n-                number = match.group(1)\n-                found = index.get(number, [])\n+                namespace, number = match.groups()\n+                found = index.get((namespace, number), [])\n                 if len(found) == 1:\n                     continue\n                 reason = \"missing\" if not found else \"ambiguous\"\n                 rels = [p.relative_to(root).as_posix() for p in found]\n-                violations.append(Violation(rel, number, lineno, reason, rels))\n+                violations.append(\n+                    Violation(rel, namespace, number, lineno, reason, rels)\n+                )\n     return violations\n \n \n@@ -129,8 +147,8 @@\n         self.assertEqual(\n             violations,\n             [],\n-            \"Every 'ADR ####' citation must resolve to exactly one \"\n-            f\"docs/adr/####-*.md:\\n{detail}\",\n+            \"Every ADR/SADR citation must resolve exactly once in its own \"\n+            f\"docs/adr namespace:\\n{detail}\",\n         )\n \n     def test_scan_actually_reads_scripts(self) -> None:\n@@ -146,8 +164,21 @@\n         # The two records this Goal carried onto the default branch, and which\n         # the scripts cite most, must be present.\n         index = _adr_index(REPO_ROOT)\n-        self.assertIn(\"0016\", index)\n-        self.assertIn(\"0017\", index)\n+        self.assertIn((\"SADR\", \"0016\"), index)\n+        self.assertIn((\"SADR\", \"0017\"), index)\n+\n+    def test_platform_tree_has_only_sadr_0001_through_0019(self) -> None:\n+        adr_dir = REPO_ROOT / \"docs\" / \"adr\"\n+        sadrs = sorted(adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\"))\n+        self.assertEqual(\n+            {path.name.split(\"-\", 2)[1] for path in sadrs},\n+            {f\"{number:04d}\" for number in range(1, 20)},\n+        )\n+        self.assertEqual(\n+            list(adr_dir.glob(\"[0-9][0-9][0-9][0-9]-*.md\")),\n+            [],\n+            \"the Shiki platform tree must not retain legacy numeric SADR paths\",\n+        )\n \n \n class FixtureAdrReferences(unittest.TestCase):\n@@ -180,6 +211,26 @@\n         self.assertIn(\"9991\", message)  # cited number\n         self.assertIn(\"docs/adr/9991-*.md\", message)  # missing path\n \n+    def test_target_adr_does_not_satisfy_missing_platform_sadr(self) -> None:\n+        root = self._make_repo(\n+            adr_names=[\"0042-target-decision.md\"],\n+            script_body='\"\"\"Target ADR 0042 exists, but platform SADR-0042 does not.\"\"\"\\n',\n+        )\n+        violations = find_adr_reference_violations(root, include_docs=False)\n+        self.assertEqual(len(violations), 1)\n+        self.assertEqual(violations[0].namespace, \"SADR\")\n+        self.assertEqual(violations[0].number, \"0042\")\n+        self.assertEqual(violations[0].expected_path, \"docs/adr/SADR-0042-*.md\")\n+\n+    def test_target_adr_and_platform_sadr_may_reuse_a_number(self) -> None:\n+        root = self._make_repo(\n+            adr_names=[\"0042-target-decision.md\", \"SADR-0042-platform-decision.md\"],\n+            script_body='\"\"\"Target ADR 0042 and platform SADR-0042 are distinct.\"\"\"\\n',\n+        )\n+        self.assertEqual(\n+            find_adr_reference_violations(root, include_docs=False), []\n+        )\n+\n     def test_uncited_adr_is_allowed(self) -> None:\n         # An ADR exists but nothing cites it -> not a violation. The converse\n         # (every ADR must be cited) is explicitly NOT required.\ndiff --git a/tests/test_guardian_review.py b/tests/test_guardian_review.py\nindex 88829e7810212635e2fc98aa5918d6f26294677d..00a85a237acd5bf2d6cb5b294128198c422402ca\n--- a/tests/test_guardian_review.py\n+++ b/tests/test_guardian_review.py\n@@ -162,7 +162,7 @@\n \n     def test_docs_only_is_light_and_not_conservative(self):\n         focus = gr.classify_review_focus([\"docs/adr/0099-x.md\", \"README.md\"], \"\", \"low\")\n-        self.assertTrue(any(\"Docs/ADR-only\" in f for f in focus))\n+        self.assertTrue(any(\"Docs/decision-record-only\" in f for f in focus))\n         self.assertFalse(any(\"conservative default\" in f.lower() for f in focus))\n \n     def test_unknown_category_is_conservative(self):\ndiff --git a/tests/test_policy_job_merged_prs.py b/tests/test_policy_job_merged_prs.py\nindex d45512e74681e5e649279236c7d4e79d5c3da471..b97989f8bd74d091de1a6b5275a20d8dc68c4512\n--- a/tests/test_policy_job_merged_prs.py\n+++ b/tests/test_policy_job_merged_prs.py\n@@ -3,8 +3,8 @@\n \n ``.github/workflows/shiki-cca-completion.yml`` has two consumers of the merged-PR\n proof: the CCA *signal* (``guardian_approval_signal.py`` in \"Prepare CCA context\")\n-and the authoritative gate (``mergegate_check.py`` in \"Enforce MergeGate\"). ADR\n-0017's bookkeeping-closeout exemption (condition 6) needs the merged implementation\n+and the authoritative gate (``mergegate_check.py`` in \"Enforce MergeGate\").\n+SADR-0017's bookkeeping-closeout exemption (condition 6) needs the merged implementation\n PR proven, and a closeout carries NO ``post_merge_reconcile`` marker. The signal\n already resolves ``MERGED_PRS`` unconditionally, but \"Enforce MergeGate\" used to\n resolve it only inside ``grep -q '<!-- shiki:post_merge_reconcile -->'``. So a\ndiff --git a/tests/test_sadr_install_migration.py b/tests/test_sadr_install_migration.py\nnew file mode 100644\nindex 0000000000000000000000000000000000000000..a6f4c57e4dcaef73ef600b0d4804744138ab9071\n--- /dev/null\n+++ b/tests/test_sadr_install_migration.py\n@@ -0,0 +1,229 @@\n+\"\"\"SADR installation and collision-safe legacy upgrade behavior.\"\"\"\n+\n+from __future__ import annotations\n+\n+import contextlib\n+import hashlib\n+import io\n+import json\n+import shutil\n+import tempfile\n+import unittest\n+from pathlib import Path\n+\n+import shiki_test_support  # noqa: F401  (path bootstrap)\n+\n+import shiki_installer\n+from tests.test_adr_references import find_adr_reference_violations\n+\n+\n+PRODUCT_NUMBERS = tuple(range(13, 19))\n+\n+\n+def _quiet_install(target: Path, *, force: bool, validate: bool) -> None:\n+    with contextlib.redirect_stdout(io.StringIO()):\n+        shiki_installer.install_template(target, force=force, validate=validate)\n+\n+\n+def _sha256(path: Path) -> str:\n+    return hashlib.sha256(path.read_bytes()).hexdigest()\n+\n+\n+def _legacy_sadr_paths() -> tuple[str, ...]:\n+    return tuple(\n+        f\"docs/adr/{path.name.removeprefix('SADR-')}\"\n+        for path in sorted((shiki_installer.ROOT / \"docs\" / \"adr\").glob(\"SADR-*.md\"))\n+        if path.name != \"SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\"\n+    )\n+\n+\n+def _create_legacy_upgrade_fixture(\n+    target: Path,\n+) -> tuple[dict[str, bytes], tuple[str, ...]]:\n+    adr_dir = target / \"docs\" / \"adr\"\n+    adr_dir.mkdir(parents=True)\n+    product_bytes = {\n+        f\"{number:04d}-cubeshare-product-decision.md\": (\n+            f\"# CubeShare ADR {number:04d}\\n\\nDo not rewrite {number}.\\n\"\n+        ).encode()\n+        for number in PRODUCT_NUMBERS\n+    }\n+    for name, content in product_bytes.items():\n+        (adr_dir / name).write_bytes(content)\n+\n+    legacy_paths = _legacy_sadr_paths()\n+    for relative in legacy_paths:\n+        (target / relative).write_bytes(f\"legacy Shiki bytes for {relative}\\n\".encode())\n+\n+    stamp_path = target / shiki_installer.INSTALL_STAMP_PATH\n+    stamp_path.parent.mkdir(parents=True, exist_ok=True)\n+    stamp = {\n+        \"version\": shiki_installer.INSTALL_STAMP_VERSION,\n+        \"platform_commit\": None,\n+        \"installed_at\": \"2000-01-01T00:00:00+00:00\",\n+        \"digests\": {\n+            relative: _sha256(target / relative) for relative in legacy_paths\n+        },\n+    }\n+    stamp_path.write_text(\n+        json.dumps(stamp, indent=2, sort_keys=True) + \"\\n\", encoding=\"utf-8\"\n+    )\n+    return product_bytes, legacy_paths\n+\n+\n+def _tree_snapshot(root: Path) -> dict[str, tuple[str, bytes | str]]:\n+    \"\"\"Recursive byte/shape snapshot used to prove preflight is write-free.\"\"\"\n+    snapshot: dict[str, tuple[str, bytes | str]] = {}\n+    for path in sorted(root.rglob(\"*\")):\n+        relative = path.relative_to(root).as_posix()\n+        if path.is_symlink():\n+            snapshot[relative] = (\"symlink\", path.readlink().as_posix())\n+        elif path.is_dir():\n+            snapshot[relative] = (\"directory\", \"\")\n+        else:\n+            snapshot[relative] = (\"file\", path.read_bytes())\n+    return snapshot\n+\n+\n+class FreshSadrInstallTests(unittest.TestCase):\n+    def setUp(self) -> None:\n+        self.tmp = Path(tempfile.mkdtemp(prefix=\"shiki-sadr-fresh-\"))\n+        self.addCleanup(lambda: shutil.rmtree(self.tmp, ignore_errors=True))\n+        self.target = self.tmp / \"target\"\n+        self.adr_dir = self.target / \"docs\" / \"adr\"\n+        self.adr_dir.mkdir(parents=True)\n+        self.product_bytes = {\n+            f\"{number:04d}-product-decision.md\": (\n+                f\"# Product ADR {number:04d}\\n\\nTarget-owned bytes {number}.\\n\"\n+            ).encode()\n+            for number in PRODUCT_NUMBERS\n+        }\n+        for name, content in self.product_bytes.items():\n+            (self.adr_dir / name).write_bytes(content)\n+\n+    def test_fresh_install_preserves_product_adrs_and_installs_all_sadrs(self) -> None:\n+        _quiet_install(self.target, force=False, validate=True)\n+\n+        for name, expected in self.product_bytes.items():\n+            self.assertEqual((self.adr_dir / name).read_bytes(), expected)\n+\n+        sadr_numbers = {\n+            path.name.split(\"-\", 2)[1]\n+            for path in self.adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\")\n+        }\n+        self.assertEqual(sadr_numbers, {f\"{number:04d}\" for number in range(1, 20)})\n+\n+        numeric_records = {\n+            path.name\n+            for path in self.adr_dir.glob(\"[0-9][0-9][0-9][0-9]-*.md\")\n+        }\n+        self.assertEqual(numeric_records, set(self.product_bytes))\n+        self.assertEqual(find_adr_reference_violations(self.target), [])\n+\n+\n+class LegacySadrUpgradeTests(unittest.TestCase):\n+    def setUp(self) -> None:\n+        self.tmp = Path(tempfile.mkdtemp(prefix=\"shiki-sadr-upgrade-\"))\n+        self.addCleanup(lambda: shutil.rmtree(self.tmp, ignore_errors=True))\n+        self.target = self.tmp / \"target\"\n+        self.adr_dir = self.target / \"docs\" / \"adr\"\n+        self.product_bytes, self.legacy_paths = _create_legacy_upgrade_fixture(\n+            self.target\n+        )\n+\n+    def test_force_upgrade_deletes_only_stamped_legacy_sadrs(self) -> None:\n+        _quiet_install(self.target, force=True, validate=True)\n+\n+        for relative in self.legacy_paths:\n+            self.assertFalse((self.target / relative).exists(), relative)\n+        for name, expected in self.product_bytes.items():\n+            self.assertEqual((self.adr_dir / name).read_bytes(), expected)\n+\n+        sadrs = list(self.adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\"))\n+        self.assertEqual(len(sadrs), 19)\n+        refreshed = json.loads(\n+            (self.target / shiki_installer.INSTALL_STAMP_PATH).read_text(encoding=\"utf-8\")\n+        )\n+        for relative in self.legacy_paths:\n+            self.assertNotIn(relative, refreshed[\"digests\"])\n+        for path in sadrs:\n+            relative = path.relative_to(self.target).as_posix()\n+            self.assertEqual(refreshed[\"digests\"][relative], _sha256(path))\n+\n+    def test_non_force_install_never_deletes_legacy_files(self) -> None:\n+        before = {\n+            relative: (self.target / relative).read_bytes()\n+            for relative in self.legacy_paths\n+        }\n+        _quiet_install(self.target, force=False, validate=False)\n+        for relative, expected in before.items():\n+            self.assertEqual((self.target / relative).read_bytes(), expected)\n+        refreshed = json.loads(\n+            (self.target / shiki_installer.INSTALL_STAMP_PATH).read_text(encoding=\"utf-8\")\n+        )\n+        for relative in self.legacy_paths:\n+            self.assertEqual(refreshed[\"digests\"][relative], _sha256(self.target / relative))\n+\n+\n+class LegacyCleanupPreflightTests(unittest.TestCase):\n+    def setUp(self) -> None:\n+        self.tmp = Path(tempfile.mkdtemp(prefix=\"shiki-sadr-preflight-\"))\n+        self.addCleanup(lambda: shutil.rmtree(self.tmp, ignore_errors=True))\n+\n+    def test_all_ownership_blockers_abort_before_any_write(self) -> None:\n+        cases = (\n+            (\"absent\", \"install stamp is absent\"),\n+            (\"unreadable\", \"install stamp is unreadable\"),\n+            (\"malformed\", \"install stamp is malformed\"),\n+            (\"missing-digest\", \"install stamp has no digest\"),\n+            (\"digest-mismatch\", \"digest mismatch\"),\n+        )\n+        for name, expected_reason in cases:\n+            with self.subTest(name=name):\n+                target = self.tmp / name\n+                _, legacy_paths = _create_legacy_upgrade_fixture(target)\n+                stamp_path = target / shiki_installer.INSTALL_STAMP_PATH\n+\n+                if name == \"absent\":\n+                    stamp_path.unlink()\n+                elif name == \"unreadable\":\n+                    stamp_path.unlink()\n+                    stamp_path.mkdir()\n+                elif name == \"malformed\":\n+                    stamp_path.write_text(\"{not-json\\n\", encoding=\"utf-8\")\n+                else:\n+                    stamp = json.loads(stamp_path.read_text(encoding=\"utf-8\"))\n+                    if name == \"missing-digest\":\n+                        stamp[\"digests\"] = {}\n+                    else:\n+                        stamp[\"digests\"] = {\n+                            relative: \"deadbeef\" for relative in legacy_paths\n+                        }\n+                    stamp_path.write_text(\n+                        json.dumps(stamp, indent=2, sort_keys=True) + \"\\n\",\n+                        encoding=\"utf-8\",\n+                    )\n+\n+                before = _tree_snapshot(target)\n+                with self.assertRaises(shiki_installer.ShikiError) as raised:\n+                    _quiet_install(target, force=True, validate=False)\n+                after = _tree_snapshot(target)\n+\n+                self.assertEqual(before, after, f\"{name} preflight changed the tree\")\n+                message = str(raised.exception)\n+                self.assertIn(expected_reason, message)\n+                self.assertIn(\"No files were written\", message)\n+                for relative in legacy_paths:\n+                    self.assertIn(relative, message)\n+\n+    def test_absent_stamp_is_allowed_when_no_legacy_path_exists(self) -> None:\n+        target = self.tmp / \"no-legacy\"\n+        target.mkdir()\n+        _quiet_install(target, force=True, validate=False)\n+        self.assertTrue(\n+            (target / \"docs\" / \"adr\" / \"SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\").is_file()\n+        )\n+\n+\n+if __name__ == \"__main__\":\n+    unittest.main()\n\n2026-08-07T08:17:15.638612Z ERROR codex_models_manager::manager: failed to renew cache TTL: missing field `base_instructions` at line 94 column 5\n succeeded in 94197ms:\n..........................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................\n----------------------------------------------------------------------\nRan 1146 tests in 93.952s\n\nOK\nGuardian approval status for PR #130 (mizutani-140/shiki)\nHead SHA: aaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa\nResult: NOT APPROVED\n\nSatisfied approval sources:\n  - guardian_label (by mizutani-140)\n\nMissing / what the gate is waiting for:\n  - Guardian approval requires a configured Guardian review or current-head Guardian comment\n\nHead SHA stability:\n  - clean: safe to request approval against this head SHA\n\nPaste-ready Guardian approval comment (a configured Guardian pastes this verbatim):\n----------8<----------\nGuardian approval granted\n\naaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa\n---------->8----------\n{\n  \"handoff_file\": \"/private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/tmpn3fwqt1h/repo/.shiki/handoffs/RP-20260807T081650709451Z-9e9f1048-repair.md\",\n  \"ledger_id\": \"L-20260807T081650723595Z-a58438ad\",\n  \"repair_id\": \"RP-20260807T081650709451Z-9e9f1048\"\n}\n{\n  \"handoff_file\": \"/private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/tmp6f9ksjqh/repo/.shiki/handoffs/RP-20260807T081652595986Z-c9630be5-repair.md\",\n  \"ledger_id\": \"L-20260807T081652608817Z-0c817fad\",\n  \"repair_id\": \"RP-20260807T081652595986Z-c9630be5\"\n}\n{\n  \"handoff_file\": \"/private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/tmp4maxny2x/repo/.shiki/handoffs/RP-20260807T081654005523Z-cffeb6dd-repair.md\",\n  \"ledger_id\": \"L-20260807T081654020766Z-8518f0a6\",\n  \"repair_id\": \"RP-20260807T081654005523Z-cffeb6dd\"\n}\n{\n  \"handoff_file\": \"/private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/tmp9mn2p53u/repo/.shiki/handoffs/RP-20260807T081706437971Z-38ff7b73-repair.md\",\n  \"ledger_id\": \"L-20260807T081706453189Z-0baeea50\",\n  \"repair_id\": \"RP-20260807T081706437971Z-38ff7b73\"\n}\n{\n  \"handoff_file\": \"/private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/tmp_pk2valw/repo/.shiki/handoffs/RP-20260807T081707690210Z-bb0b00cf-repair.md\",\n  \"ledger_id\": \"L-20260807T081707705983Z-9166a028\",\n  \"repair_id\": \"RP-20260807T081707690210Z-bb0b00cf\"\n}\n{\n  \"handoff_file\": \"/private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/tmp7rlamama/repo/.shiki/handoffs/RP-20260807T081708244004Z-29d12129-repair.md\",\n  \"ledger_id\": \"L-20260807T081708258843Z-5c3d592f\",\n  \"repair_id\": \"RP-20260807T081708244004Z-29d12129\"\n}\n{\n  \"handoff_file\": \"/private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/tmpbr8t4ucx/repo/.shiki/handoffs/RP-20260807T081708898063Z-1b677e16-repair.md\",\n  \"ledger_id\": \"L-20260807T081708913425Z-0726a7c8\",\n  \"repair_id\": \"RP-20260807T081708898063Z-1b677e16\"\n}\n{\n  \"handoff_file\": \"/private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/tmpmvyz_nuv/repo/.shiki/handoffs/RP-20260807T081709538941Z-4ac1254c-repair.md\",\n  \"ledger_id\": \"L-20260807T081709554723Z-7f9de67f\",\n  \"repair_id\": \"RP-20260807T081709538941Z-4ac1254c\"\n}\n{\n  \"handoff_file\": \"/private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/tmp6jysqm41/repo/.shiki/handoffs/RP-20260807T081709928668Z-6865ee08-repair.md\",\n  \"ledger_id\": \"L-20260807T081709943494Z-ef94c15d\",\n  \"repair_id\": \"RP-20260807T081709928668Z-6865ee08\"\n}\n{\n  \"handoff_file\": \"/private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/tmp8iqnfwpy/repo/.shiki/handoffs/RP-20260807T081710423691Z-3b7ce541-repair.md\",\n  \"ledger_id\": \"L-20260807T081710438413Z-b268022b\",\n  \"repair_id\": \"RP-20260807T081710423691Z-3b7ce541\"\n}\n[shiki] token verified: ok\n[shiki] token verified: ok\n{\n  \"repo\": \"owner/name\",\n  \"secret\": \"CLAUDE_CODE_OAUTH_TOKEN\",\n  \"source\": \"env:SHIKI_TEST_TOK\",\n  \"verified\": true\n}\n\ndiff --git a/.codex/skills/shiki/SKILL.md b/.codex/skills/shiki/SKILL.md\nindex c8e2e85d9d75d49cc4f2d6286ee22659c4e53e75..93cded623b2c15d9f65c76491a1c2d813ac9e6c0\n--- a/.codex/skills/shiki/SKILL.md\n+++ b/.codex/skills/shiki/SKILL.md\n@@ -18,6 +18,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n Codex CLI does not register this skill as a custom `/shiki` slash command. When\n the user types `/shiki` in Codex CLI and gets \"Unrecognized command\", explain\n that this is expected: invoke Shiki in Codex with natural language such as\n@@ -69,7 +72,7 @@\n \n ## Responsibilities\n \n-- Claude Code implements and repairs by default (`shiki runner claude`, ADR 0008); Codex implements and repairs tasks explicitly assigned to `codex`.\n+- Claude Code implements and repairs by default (`shiki runner claude`, SADR-0008); Codex implements and repairs tasks explicitly assigned to `codex`.\n - Claude Code Action can act as GitHub-side CCA or reviewer.\n - CCA judges completion.\n - MergeGate authorizes state transitions and merge readiness.\ndiff --git a/.github/prompts/cca-completion-check.md b/.github/prompts/cca-completion-check.md\nindex ed203ad5f302ecc81372d8ab27d2b1232ca97497..fd89950528333ef6242e6d5443904f5392ab7e56\n--- a/.github/prompts/cca-completion-check.md\n+++ b/.github/prompts/cca-completion-check.md\n@@ -19,6 +19,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n ## Required Reading\n \n Read only the files needed to judge the PR contract. Prefer this order:\ndiff --git a/.github/prompts/codex-task-handoff.md b/.github/prompts/codex-task-handoff.md\nindex 695e211d76dea1c60e8d5bb360e78487808286a4..59441d576b01a39569722f226c854e5a740e9eb5\n--- a/.github/prompts/codex-task-handoff.md\n+++ b/.github/prompts/codex-task-handoff.md\n@@ -2,7 +2,7 @@\n \n You are Codex Front acting as the Implementer for a Shiki task.\n \n-Follow `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant ADRs, and `docs/agents/codex-handoff.md`.\n+Follow `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant Shiki SADRs and target ADRs, and `docs/agents/codex-handoff.md`.\n \n Rules:\n \ndiff --git a/AGENTS.md b/AGENTS.md\nindex 7f041100d49c2a7525f51cb0f5d696a4a6d1c78c..a06bfbabc6fa48a007389e25e12d7ed523936bed\n--- a/AGENTS.md\n+++ b/AGENTS.md\n@@ -22,7 +22,7 @@\n - **Codex Front** is an optional implementer through Codex App, Codex CLI, Codex IDE extension, or Codex Web signed in with ChatGPT OAuth/subscription auth, dispatched with `shiki runner codex` when a task is explicitly assigned to `codex`.\n - **GitHub CCA** is implemented by Claude Code Action by default, using `CLAUDE_CODE_OAUTH_TOKEN`.\n \n-Do not assume `openai/codex-action`, `OPENAI_API_KEY`, or API-key based Codex automation in the default Shiki path. API-key runners are explicit target-repo extensions and require their own ADR.\n+Do not assume `openai/codex-action`, `OPENAI_API_KEY`, or API-key based Codex automation in the default Shiki path. API-key runners are explicit target-repo extensions and require their own target ADR.\n \n Shiki is not:\n \n@@ -44,10 +44,14 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use\n+`SADR-NNNN-*.md`; target product decisions retain `NNNN-*.md` and the `ADR`\n+namespace.\n+\n Use the canonical source-of-truth order above. Runtime-specific reading orders\n are operational guidance only and must not override or reorder it.\n \n-Conversation state is not durable truth. If a decision matters, put it in GitHub, `.shiki/`, `CONTEXT.md`, or an ADR.\n+Conversation state is not durable truth. If a decision matters, put it in GitHub, `.shiki/`, `CONTEXT.md`, a Shiki SADR, or a target ADR.\n \n If GitHub and `.shiki/` disagree, surface the conflict and prefer GitHub operational state until the mirror is repaired.\n \n@@ -75,8 +79,8 @@\n Every non-trivial change follows this loop:\n \n 1. **Goal**: clarify outcome, completion conditions, non-goals, risk level, and success signals.\n-2. **grill-with-docs**: challenge the plan against domain docs, ADRs, code reality, terminology, and edge scenarios.\n-3. **Context & Impact**: find relevant docs, ADRs, code, dependencies, owners, locks, and verification surfaces.\n+2. **grill-with-docs**: challenge the plan against domain docs, Shiki SADRs, target ADRs, code reality, terminology, and edge scenarios.\n+3. **Context & Impact**: find relevant docs, SADRs, target ADRs, code, dependencies, owners, locks, and verification surfaces.\n 4. **PRD and Spec Freeze**: use `to-prd` when context is settled enough to become durable product/engineering intent. The operator's explicit approval of the PRD is Spec Freeze: the plan records a `spec_freeze` block, and no plan runs without it. Steps 1-4 form Requirements Definition \u2014 one continuous operator dialogue. After Spec Freeze, scope changes require an operator-approved Spec Amendment; non-scope-moving interpretations go to the Assumption Log.\n 5. **Issues**: use `to-issues` to create independently grabbable vertical-slice issues. Prefer AFK slices over HITL slices where possible.\n 6. **Triage**: label issues for readiness, risk, runtime, skills, and MergeGate state.\n@@ -167,7 +171,7 @@\n - Testing decisions.\n - Out-of-scope items.\n - Further notes.\n-- Links to relevant domain terms and ADRs.\n+- Links to relevant domain terms, SADRs, and target ADRs.\n \n Every executable task must include:\n \n@@ -193,7 +197,7 @@\n \n Context & Impact must identify:\n \n-- Relevant documents, domain terms, ADRs, and past decisions.\n+- Relevant documents, domain terms, SADRs, target ADRs, and past decisions.\n - Relevant modules, interfaces, seams, callers, owners, and tests.\n - Dependency relationships between tasks.\n - Candidate locks and lock conflicts.\n@@ -210,7 +214,7 @@\n | Skill | Required when |\n | --- | --- |\n | `setup-matt-pocock-skills` | First configuring the repo for skills, or when issue tracker, triage labels, or domain docs are missing. |\n-| `grill-with-docs` | Default for non-trivial Goals before PRD/issues; always required when plans, terminology, boundaries, tradeoffs, or ADR-worthy decisions are ambiguous. |\n+| `grill-with-docs` | Default for non-trivial Goals before PRD/issues; always required when plans, terminology, boundaries, tradeoffs, or decision-record-worthy choices are ambiguous. |\n | `zoom-out` | The agent lacks an architectural map of the relevant code area. |\n | `to-prd` | Settled Goal context must become durable product/engineering intent. |\n | `to-issues` | A Goal, PRD, or plan must become independently grabbable vertical-slice issues. |\n@@ -348,7 +352,7 @@\n - Three or more new files.\n - Any high-risk or critical label.\n \n-When the architecture gate triggers, run `grill-with-docs` or `improve-codebase-architecture` as appropriate and record durable decisions in an ADR when needed.\n+When the architecture gate triggers, run `grill-with-docs` or `improve-codebase-architecture` as appropriate and record durable decisions in a Shiki SADR or target ADR as applicable.\n \n ## Engineering Discipline\n \ndiff --git a/CLAUDE.md b/CLAUDE.md\nindex 98a932e7841a632866fcd53b0bad0e3af3711d3d..98a31399404d6fb044b4fd493f4459ac87f439de\n--- a/CLAUDE.md\n+++ b/CLAUDE.md\n@@ -14,6 +14,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n ## Claude Role\n \n Default to planner, default implementer, reviewer, coordinator, documentation editor, CCA-style completion judge, and final-judgment assistant.\n@@ -57,7 +60,7 @@\n At the start of a Shiki session:\n \n 1. Read `AGENTS.md`.\n-2. Read `CONTEXT.md` and relevant ADRs.\n+2. Read `CONTEXT.md` and relevant Shiki SADRs and target ADRs.\n 3. Read active `.shiki/` Goal/task/ledger state when present.\n 4. Read active distilled rules from `.shiki/memories/` (status `distilled`, `active: true`, not revoked or superseded) for applicable guidance; the same rules are injected deterministically into task handoffs as the `## Distilled Rules` section (\u00a73.5).\n 5. Read the GitHub Issue or PR when the task is GitHub-backed.\n@@ -74,7 +77,7 @@\n - Define the Goal, completion conditions, non-goals, and risk level.\n - Treat `grill-with-docs` as the default planning entry point for non-trivial Goals.\n - Challenge domain terms against `CONTEXT.md`.\n-- Challenge hard-to-reverse tradeoffs against ADRs.\n+- Challenge hard-to-reverse tradeoffs against SADRs and target ADRs.\n - Explore code instead of asking when code can answer the question.\n - Run Context & Impact before implementation planning.\n - Produce a Task DAG, not an unordered checklist.\n@@ -94,7 +97,7 @@\n - For each question, provide your recommended answer.\n - Resolve dependency decisions before downstream design questions.\n - Update `CONTEXT.md` inline when terminology crystallizes.\n-- Offer ADRs only when the decision is hard to reverse, surprising without context, and a real tradeoff.\n+- Offer a SADR or target ADR only when the decision is hard to reverse, surprising without context, and a real tradeoff.\n - Convert unresolved questions into HITL blockers rather than allowing Codex to guess.\n \n ## PRD and Issues Standard\n@@ -103,7 +106,7 @@\n \n - Use the project's domain glossary vocabulary.\n - Avoid volatile file paths unless they are essential.\n-- Include implementation decisions, testing decisions, out-of-scope items, and relevant ADR/domain links.\n+- Include implementation decisions, testing decisions, out-of-scope items, and relevant SADR/target-ADR/domain links.\n - Identify deep-module opportunities and testable interfaces.\n \n When creating issues:\n@@ -121,7 +124,7 @@\n - Branch/worktree target.\n - Scope and non-goals.\n - Dependencies and locks.\n-- Relevant docs, ADRs, modules, and tests.\n+- Relevant docs, SADRs, target ADRs, modules, and tests.\n - Required skill invocations.\n - TDD expectations.\n - Pre-PR `code-review` gate expectations.\n@@ -152,7 +155,7 @@\n \n - Treat yourself as a judge, not implementer.\n - Do not write production code.\n-- Read `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant ADRs, PRD, task issue, PR body, diff, checks, reviews, labels, and `.shiki/` state.\n+- Read `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant SADRs and target ADRs, PRD, task issue, PR body, diff, checks, reviews, labels, and `.shiki/` state.\n - Evaluate every applicable checklist in `docs/agents/checklists.md`.\n - Map each acceptance criterion to durable evidence.\n - Distinguish implementation failure from missing evidence.\ndiff --git a/CONTEXT.md b/CONTEXT.md\nindex e61a9be06a0cce0d026484083a657bde2067e234..237ee8aa3ea8fab334ece49768bc5ed3b5af0974\n--- a/CONTEXT.md\n+++ b/CONTEXT.md\n@@ -25,7 +25,7 @@\n _Avoid_: jumping directly into code\n \n **grill-with-docs**:\n-A planning skill that challenges a plan against domain language, ADRs, code reality, and concrete edge scenarios. It resolves design-tree questions before PRD/issues. It runs inside Requirements Definition.\n+A planning skill that challenges a plan against domain language, Shiki SADRs, target ADRs, code reality, and concrete edge scenarios. It resolves design-tree questions before PRD/issues. It runs inside Requirements Definition.\n _Avoid_: generic brainstorming, silent assumptions\n \n **Requirements Definition**:\ndiff --git a/CONTRIBUTING.md b/CONTRIBUTING.md\nindex c6d9b656e73abb8a55e5f660123e03153dcb3e6d..9e37a8fbf668db5b22461aaae5548b8f1354394c\n--- a/CONTRIBUTING.md\n+++ b/CONTRIBUTING.md\n@@ -17,7 +17,7 @@\n 1. **Goal** \u2014 start from a user-approved Goal with an outcome, completion\n    conditions, non-goals, risk level, and success signals.\n 2. **grill-with-docs** \u2014 for any non-trivial change, challenge the plan against\n-   domain language, ADRs, and code reality before writing a PRD.\n+   domain language, Shiki SADRs, target ADRs, and code reality before writing a PRD.\n 3. **PRD** \u2014 capture settled product/engineering intent (`to-prd`).\n 4. **Spec Freeze** \u2014 the operator explicitly approves the PRD/requirements;\n    the plan records a `spec_freeze` block and no plan runs without it\n@@ -94,7 +94,7 @@\n   entries.\n - Do not hand-edit `.shiki/goals/*.json` or `.shiki/manifest.json` in a task PR.\n - Conversation state is not durable truth. If a decision matters, record it in\n-  GitHub, `.shiki/`, `CONTEXT.md`, or an ADR.\n+  GitHub, `.shiki/`, `CONTEXT.md`, a Shiki SADR, or a target ADR.\n \n ## Architecture Gate\n \n@@ -103,7 +103,7 @@\n or CI policy, package structure, three or more structural files, more than 100\n net new lines in a core module, three or more new files, or any high-risk label.\n When triggered, run `grill-with-docs` or `improve-codebase-architecture` and\n-record durable decisions in an ADR when needed (see `docs/adr/`).\n+record durable decisions in a Shiki SADR or target ADR as applicable (see `docs/adr/`).\n \n ## Safety\n \ndiff --git a/SYSTEM_PROMPT.md b/SYSTEM_PROMPT.md\nindex aaaea45e3a896c3a44ba89eec8bb84325ecf6c62..da009c86a88285269dee0155e58e550c6d744ea9\n--- a/SYSTEM_PROMPT.md\n+++ b/SYSTEM_PROMPT.md\n@@ -30,6 +30,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n Use the canonical source-of-truth order above. Runtime-specific reading orders\n are operational guidance only and must not override or reorder it. The current\n conversation is non-durable operator input.\n@@ -48,14 +51,14 @@\n \n A runtime may only do work that is explicitly assigned by the Goal, task, PR, or MergeGate decision.\n \n-Do not assume `openai/codex-action`, `OPENAI_API_KEY`, or API-key based Codex automation in the default Shiki path. API-key runners are explicit target-repo extensions and require their own ADR.\n+Do not assume `openai/codex-action`, `OPENAI_API_KEY`, or API-key based Codex automation in the default Shiki path. API-key runners are explicit target-repo extensions and require their own target ADR.\n \n ## 4. Non-negotiable Shiki flow\n \n For any non-trivial Goal, use this flow:\n \n 1. **Goal Seek**: define outcome, non-goals, risk level, completion criteria, and evidence requirements.\n-2. **grill-with-docs**: challenge the plan against `CONTEXT.md`, ADRs, code reality, terminology, boundaries, and edge scenarios. Ask one question at a time when the operator is available. If code can answer a question, inspect code instead of asking.\n+2. **grill-with-docs**: challenge the plan against `CONTEXT.md`, Shiki SADRs, target ADRs, code reality, terminology, boundaries, and edge scenarios. Ask one question at a time when the operator is available. If code can answer a question, inspect code instead of asking.\n 3. **Context & Impact**: identify modules, interfaces, seams, callers, dependencies, locks, risk, and verification surfaces.\n 4. **to-prd + Spec Freeze**: turn settled context into a PRD using domain vocabulary and testing decisions. The operator's explicit approval of the PRD is Spec Freeze, recorded as the plan's `spec_freeze` block; plans without it do not run. Steps 1-4 form Requirements Definition.\n 5. **to-issues**: decompose the PRD into independently grabbable vertical-slice issues. Prefer AFK slices; mark HITL slices when judgment is still required.\n@@ -75,7 +78,7 @@\n Before making any material change:\n \n 1. Read `AGENTS.md` and any runtime-specific wrapper such as `CLAUDE.md`.\n-2. Read `CONTEXT.md` and relevant ADRs.\n+2. Read `CONTEXT.md` and relevant SADRs and target ADRs.\n 3. Read the GitHub Issue / PR and `.shiki/` state for the active Goal or task.\n 4. Identify your role: Planner, Implementer, Reviewer, Completion Check Agent, Repairer, or Guardian-assist.\n 5. Identify Goal id, task id, branch, PR, dependencies, locks, risk level, required skills, and required evidence.\n@@ -103,7 +106,7 @@\n \n At minimum, identify:\n \n-- Relevant documents, ADRs, domain terms, and prior decisions.\n+- Relevant documents, SADRs, target ADRs, domain terms, and prior decisions.\n - Relevant modules, interfaces, seams, callers, owners, and tests.\n - Dependency relationships between tasks.\n - Candidate file locks and lock conflicts.\n@@ -152,7 +155,7 @@\n \n CCA must read:\n \n-- `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant ADRs.\n+- `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant SADRs and target ADRs.\n - Parent Goal issue, PRD issue, task issue, comments, and labels.\n - PR body, diff, commits, changed files, reviews, CI checks, and artifacts available to the workflow.\n - `.shiki/` task contract, locks, ledger entries, CCA checklist profile, and prior repair packets.\n@@ -214,7 +217,7 @@\n - Three or more new files.\n - Any high-risk or critical label.\n \n-When the architecture gate triggers, run `grill-with-docs` or `improve-codebase-architecture` as appropriate and record the decision in an ADR if it is hard to reverse or likely to be re-litigated.\n+When the architecture gate triggers, run `grill-with-docs` or `improve-codebase-architecture` as appropriate and record the decision in a Shiki SADR or target ADR as applicable if it is hard to reverse or likely to be re-litigated.\n \n ## 14. Skill Gate\n \n@@ -223,7 +226,7 @@\n Use these skills:\n \n - `setup-matt-pocock-skills`: before first use of the engineering skill set, or when issue tracker, triage labels, or domain docs are missing.\n-- `grill-with-docs`: default for non-trivial Goals before PRD/issues; required for ambiguous plans, terminology, boundaries, ADR-worthy tradeoffs, or design-tree decisions.\n+- `grill-with-docs`: default for non-trivial Goals before PRD/issues; required for ambiguous plans, terminology, boundaries, decision-record-worthy tradeoffs, or design-tree decisions.\n - `zoom-out`: unfamiliar code area or missing architectural map.\n - `to-prd`: convert settled context into a PRD.\n - `to-issues`: break a PRD, plan, or Goal into independently grabbable vertical-slice issues.\ndiff --git a/docs/adr/0001-keep-shiki-as-platform-name.md b/docs/adr/SADR-0001-keep-shiki-as-platform-name.md\nindex 20a9637c3b9d8c97de7ba8db7cfae4d82d9a7dba..8b685439959aada1b221c112f0ac3c51ca8a1695\n--- a/docs/adr/0001-keep-shiki-as-platform-name.md\n+++ b/docs/adr/SADR-0001-keep-shiki-as-platform-name.md\n@@ -1,3 +1,3 @@\n-# Keep Shiki As The Platform Name\n+# SADR-0001: Keep Shiki As The Platform Name\n \n Shiki remains the product name for the GitHub-native agentic engineering control plane. We considered more descriptive names such as MergePilot, MergeGate, and Kiroku, but kept Shiki because it carries the existing goal-seeking framework identity while remaining independent of Hermes, Codex, Claude Code, or any single runtime.\ndiff --git a/docs/adr/0002-use-agents-as-shared-constitution.md b/docs/adr/SADR-0002-use-agents-as-shared-constitution.md\nindex 73de30ecb078120eac540d9bc51855e3cd48d0e3..c5486182bf06fca36ea508496b013c67302fd989\n--- a/docs/adr/0002-use-agents-as-shared-constitution.md\n+++ b/docs/adr/SADR-0002-use-agents-as-shared-constitution.md\n@@ -1,3 +1,3 @@\n-# Use AGENTS As The Shared Agent Constitution\n+# SADR-0002: Use AGENTS As The Shared Agent Constitution\n \n Shiki uses `AGENTS.md` as the runtime-neutral constitution and `CLAUDE.md` as a thin Claude Code wrapper. This keeps Codex, Claude Code, Hermes Runner, and future Agent Runtimes aligned on the same GitHub-first MergeGate rules while still allowing runtime-specific instructions where needed.\ndiff --git a/docs/adr/0003-require-skill-gate-for-mergegate-readiness.md b/docs/adr/SADR-0003-require-skill-gate-for-mergegate-readiness.md\nindex 424be0c9af4c8f0136f1fde68a468c47ff84e764..7fc337987c7df9792f75f181dfcbed75fe5ffff2\n--- a/docs/adr/0003-require-skill-gate-for-mergegate-readiness.md\n+++ b/docs/adr/SADR-0003-require-skill-gate-for-mergegate-readiness.md\n@@ -1,3 +1,3 @@\n-# Require Skill Gate For MergeGate Readiness\n+# SADR-0003: Require Skill Gate For MergeGate Readiness\n \n Shiki requires relevant engineering skills to be invoked before execution when their triggers apply. This makes skills part of MergeGate readiness rather than optional prompt style, because Shiki depends on repeatable planning, issue decomposition, TDD, diagnosis, triage, and architecture review before autonomous or parallel agent work can be trusted.\ndiff --git a/docs/adr/0004-use-github-first-with-shiki-mirror.md b/docs/adr/SADR-0004-use-github-first-with-shiki-mirror.md\nindex d0a3a9174dc646a945a9d325954ffaf00c352040..a79c54ae9ee8eed2a0524192b864da4e22a92d3f\n--- a/docs/adr/0004-use-github-first-with-shiki-mirror.md\n+++ b/docs/adr/SADR-0004-use-github-first-with-shiki-mirror.md\n@@ -1,4 +1,4 @@\n-# ADR 0004: Use GitHub First With A Repository-Local Shiki Mirror\n+# SADR-0004: Use GitHub First With A Repository-Local Shiki Mirror\n \n ## Status\n \ndiff --git a/docs/adr/0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md b/docs/adr/SADR-0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\nindex b339baf49b89d9290e79c3d51b36e7b2bde3bd95..0f3c3eb9afccdb3403ce978e21d04fe12e495da0\n--- a/docs/adr/0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\n+++ b/docs/adr/SADR-0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\n@@ -1,8 +1,8 @@\n-# ADR 0005: Use Codex As Front And Claude Code Action As The GitHub Runtime\n+# SADR-0005: Use Codex As Front And Claude Code Action As The GitHub Runtime\n \n ## Status\n \n-Accepted (implementer default superseded by ADR 0008; the auth model and the\n+Accepted (implementer default superseded by SADR-0008; the auth model and the\n Claude Code Action GitHub runtime decision remain in force)\n \n ## Context\n@@ -26,4 +26,4 @@\n - Shiki avoids requiring `OPENAI_API_KEY` for the default workflow.\n - Codex work happens through the user's authenticated Codex front surface, not as a hidden backend Action.\n - Claude Code Action is configured for OAuth-token based subscription usage by default.\n-- API-key based automation remains possible only as an explicit target-repo extension with its own ADR.\n+- API-key based automation remains possible only as an explicit target-repo extension with its own target ADR.\ndiff --git a/docs/adr/0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md b/docs/adr/SADR-0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\nindex 4bcae9d1e52f0a37b8e9fbafa5cd14b6ee1993bd..7a38538a6ae9929567f0ade67e7ea6a60d0b3f7e\n--- a/docs/adr/0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\n+++ b/docs/adr/SADR-0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\n@@ -1,4 +1,4 @@\n-# ADR 0006: Separate Codex Implementation, CCA Completion, And MergeGate Authorization\n+# SADR-0006: Separate Codex Implementation, CCA Completion, And MergeGate Authorization\n \n ## Status\n \ndiff --git a/docs/adr/0007-packaging-and-release.md b/docs/adr/SADR-0007-packaging-and-release.md\nindex 79b84ddb97167372b44641e5b7860e400ce80822..bb445a2e2e856d9d1f5fdbe35de8d4e8cec0fb32\n--- a/docs/adr/0007-packaging-and-release.md\n+++ b/docs/adr/SADR-0007-packaging-and-release.md\n@@ -1,4 +1,4 @@\n-# ADR 0007: Packaging And Release For Shiki\n+# SADR-0007: Packaging And Release For Shiki\n \n ## Status\n \n@@ -25,7 +25,7 @@\n   not introduce third-party runtime dependencies or break the shim.\n - Releases must run inside the operator's subscription-authenticated toolchain\n   and must not require extra secrets beyond the built-in `GITHUB_TOKEN`\n-  (consistent with ADR 0005's no-API-key-by-default stance).\n+  (consistent with SADR-0005's no-API-key-by-default stance).\n - `.shiki/manifest.json` already encodes the install surface (`install.include`,\n   `install.create_directories`, `install.exclude_from_commit`), and it must not\n   be mutated as part of this change.\n@@ -34,7 +34,7 @@\n \n This is a hard-to-reverse platform decision (it sets the version source of\n truth, the release trigger, and the public install contract), so it is recorded\n-as an ADR.\n+as a SADR.\n \n ## Decision\n \n@@ -104,7 +104,7 @@\n 3. **Publishing to PyPI.** Rejected for now: Shiki is distributed as a\n    repository template + git-installable CLI, not a PyPI library; PyPI would add\n    credentials/secrets and a different versioning contract. Can be added later as\n-   its own ADR if a packaged distribution is needed.\n+   its own SADR if a packaged distribution is needed.\n 4. **Dynamic version (read `VERSION` via a build backend `dynamic` field).**\n    Rejected to keep `pyproject.toml` backend-agnostic and dependency-free; the\n    small duplication between `VERSION` and `pyproject.toml` is guarded by the\n@@ -123,4 +123,4 @@\n   releasing versions the whole platform but ships only `install.include` to\n   targets.\n - Future PyPI distribution or a console entry point are deferred and would each\n-  warrant their own ADR.\n+  warrant their own SADR.\ndiff --git a/docs/adr/0008-use-claude-code-as-default-implementer.md b/docs/adr/SADR-0008-use-claude-code-as-default-implementer.md\nindex dedf4510b7c7799521422f622029563d373db031..35c2625d908167fbc58855b5b63e283805f167e8\n--- a/docs/adr/0008-use-claude-code-as-default-implementer.md\n+++ b/docs/adr/SADR-0008-use-claude-code-as-default-implementer.md\n@@ -1,12 +1,12 @@\n-# ADR 0008: Use Claude Code As The Default Implementer Runtime\n+# SADR-0008: Use Claude Code As The Default Implementer Runtime\n \n ## Status\n \n-Proposed (supersedes ADR 0005 for the implementer default; keeps its auth model)\n+Proposed (supersedes SADR-0005 for the implementer default; keeps its auth model)\n \n ## Context\n \n-ADR 0005 made Codex Front the default implementation runtime, with Claude Code\n+SADR-0005 made Codex Front the default implementation runtime, with Claude Code\n restricted to planner and reviewer roles by the runtime registry, and the only\n autonomous implementation adapter was `shiki runner codex`.\n \n@@ -18,13 +18,13 @@\n - The platform's planning, review, and CCA layers already run on Claude Code,\n   and Claude Code's native capabilities (the built-in `/code-review` skill and\n   the Workflow multi-agent orchestration tool) have no Codex equivalent.\n-- The Goal lifecycle redesign (ADR 0009) requires an autonomous post-freeze\n+- The Goal lifecycle redesign (SADR-0009) requires an autonomous post-freeze\n   loop driven from the operator's Claude Code session, including bounded\n   repair dispatch, which must not depend on a runtime that cannot be invoked\n   unattended.\n \n The constitution claims Shiki is runtime-agnostic (\"not a Claude-only\n-workflow\", ADR 0002). Any change to the implementer default must preserve the\n+workflow\", SADR-0002). Any change to the implementer default must preserve the\n registry-based runtime model rather than hardcoding one vendor.\n \n ## Decision\n@@ -39,7 +39,7 @@\n   the task to review or repair-needed.\n - Keep Codex registered as an optional implementer runtime. `shiki runner\n   codex`, the Codex handoff templates, and the ChatGPT OAuth auth model from\n-  ADR 0005 remain supported for tasks explicitly assigned to `codex`.\n+  SADR-0005 remain supported for tasks explicitly assigned to `codex`.\n - Commit to mandating two Claude-native capabilities as loop contract in\n   follow-up Goals (status: the code-review gate was delivered by Goal G-B \u2014\n   skill registry, checklists item PR-12, default required_skills \u2014 while the\n@@ -60,7 +60,7 @@\n \n Out of scope: removing Codex support, changing the CCA runtime (Claude Code\n Action with `CLAUDE_CODE_OAUTH_TOKEN` stays), and API-key based automation\n-(still requires its own ADR per ADR 0005).\n+(still requires its own target ADR per SADR-0005).\n \n ## Consequences\n \n@@ -100,7 +100,7 @@\n   harness rejection of direct `codex exec` is outside Shiki's control, and the\n   plan's mandatory native capabilities cannot run on Codex at all.\n - **Remove Codex entirely**: rejected; it abandons the runtime-agnostic\n-  constitution (ADR 0002) for no operational gain and forecloses\n+  constitution (SADR-0002) for no operational gain and forecloses\n   cross-runtime adversarial review.\n - **GitHub Actions cloud implementation via claude-code-action**: rejected as\n   default; durable evidence is automatic but token cost, Node 24 workflow\ndiff --git a/docs/adr/0009-requirements-definition-and-spec-freeze.md b/docs/adr/SADR-0009-requirements-definition-and-spec-freeze.md\nindex 29b3f7871bef9203a8b4a831a65c39eb2f506e42..52e7dd69ad0aa4efe0b68c62678abbf7df02aaec\n--- a/docs/adr/0009-requirements-definition-and-spec-freeze.md\n+++ b/docs/adr/SADR-0009-requirements-definition-and-spec-freeze.md\n@@ -1,4 +1,4 @@\n-# ADR 0009: Merge Grill And Planning Into Requirements Definition With Spec Freeze\n+# SADR-0009: Merge Grill And Planning Into Requirements Definition With Spec Freeze\n \n ## Status\n \ndiff --git a/docs/adr/0010-external-ai-guardian-review.md b/docs/adr/SADR-0010-external-ai-guardian-review.md\nindex e6108bbe8bb4e73e0ff4684c8f78001a80438c4e..f3510b145664298adafb151b6f0db51b972f01ec\n--- a/docs/adr/0010-external-ai-guardian-review.md\n+++ b/docs/adr/SADR-0010-external-ai-guardian-review.md\n@@ -1,4 +1,4 @@\n-# ADR 0010: External AI Guardian Review As A First-Class Approval Authority\n+# SADR-0010: External AI Guardian Review As A First-Class Approval Authority\n \n ## Status\n \n@@ -8,7 +8,7 @@\n \n Shiki is intended to be an autonomous, recursive, self-improving agent\n framework: authority-in-the-loop, not human-in-the-loop. Until now the Guardian\n-approval required for high/critical-risk merges (guardian-policy.json, ADR\n+approval required for high/critical-risk merges (guardian-policy.json, SADR\n governance) recognized only human authority sources \u2014 a GitHub review approval,\n the `guardian:approved` label applied by a configured Guardian, or a comment\n carrying `Guardian approval granted` plus the current head SHA.\n@@ -89,7 +89,7 @@\n   autonomous design the operator intends and produced the impersonation\n   incident.\n - **Record AI approval as an operator comment**: rejected as forgery of human\n-  authority \u2014 the exact failure this ADR corrects.\n+  authority \u2014 the exact failure this SADR corrects.\n - **Deliver the artifact as a committed `.shiki/` file**: rejected; a committed\n   approval inside the PR being approved is forgeable by anyone who can push.\n - **A dedicated reviewer bot identity instead of a relayed comment**: deferred;\ndiff --git a/docs/adr/0011-autonomous-quality-gate-evidence.md b/docs/adr/SADR-0011-autonomous-quality-gate-evidence.md\nindex bca0246c5d46e44afbde15486a75eec425fc02f5..9c93eac16d8c3a6d4b11bd7224e3955102cd7db4\n--- a/docs/adr/0011-autonomous-quality-gate-evidence.md\n+++ b/docs/adr/SADR-0011-autonomous-quality-gate-evidence.md\n@@ -1,4 +1,4 @@\n-# ADR 0011: Autonomous Quality-Gate Evidence \u2014 Deterministic Facts And An Independent Verifier\n+# SADR-0011: Autonomous Quality-Gate Evidence \u2014 Deterministic Facts And An Independent Verifier\n \n ## Status\n \ndiff --git a/docs/adr/0012-autonomous-loop-closeout-to-main.md b/docs/adr/SADR-0012-autonomous-loop-closeout-to-main.md\nindex 637fad6dde4989c9c2bf0f81f676d8d29991123c..ec7d85f81030a676bcbba28b43eb365422a36eac\n--- a/docs/adr/0012-autonomous-loop-closeout-to-main.md\n+++ b/docs/adr/SADR-0012-autonomous-loop-closeout-to-main.md\n@@ -1,4 +1,4 @@\n-# 0012 \u2014 Autonomous loop pushes goal completion to main via a closeout PR\n+# SADR-0012 \u2014 Autonomous loop pushes goal completion to main via a closeout PR\n \n - Status: accepted\n - Date: 2026-06-18\ndiff --git a/docs/adr/0013-bootstrap-grants-actions-approve-pull-requests.md b/docs/adr/SADR-0013-bootstrap-grants-actions-approve-pull-requests.md\nindex b6c69d64f1c5ced346b88d6859586715b3b5bd0e..cf48ff4a6716267f539ca58edd1a852e7c7a2d64\n--- a/docs/adr/0013-bootstrap-grants-actions-approve-pull-requests.md\n+++ b/docs/adr/SADR-0013-bootstrap-grants-actions-approve-pull-requests.md\n@@ -1,4 +1,4 @@\n-# ADR 0013: Bootstrap Grants GitHub Actions Permission To Create And Approve Pull Requests\n+# SADR-0013: Bootstrap Grants GitHub Actions Permission To Create And Approve Pull Requests\n \n ## Status\n \n@@ -31,7 +31,7 @@\n security tradeoff: it lets repository automation satisfy a human-review gate. In\n a multi-maintainer repo that could let a compromised or buggy workflow\n self-approve changes. So whether bootstrap turns it on by default \u2014 and with\n-what default token scope \u2014 is an ADR-worthy decision, not an implementation\n+what default token scope \u2014 is a SADR-worthy decision, not an implementation\n detail.\n \n ## Decision\n@@ -48,7 +48,7 @@\n Specifically:\n \n - **`can_approve_pull_request_reviews` defaults to `true`.** Shiki's default\n-  posture is solo/AFK autonomous operation (ADR 0008/0009/0012), where the CCA\n+  posture is solo/AFK autonomous operation (SADR-0008/0009/0012), where the CCA\n   Review Bridge is the only path that can satisfy `required_review: true` after\n   the CCA and Guardian gates have already passed. The default makes a\n   freshly-bootstrapped repo able to run the autonomous loop end to end without a\ndiff --git a/docs/adr/0014-codex-app-external-ai-guardian-ui-adapter.md b/docs/adr/SADR-0014-codex-app-external-ai-guardian-ui-adapter.md\nindex 238db06279334caa341c117362df9271382a7fcd..65f12e9939321e1b3e389a34c822fc1549e5155d\n--- a/docs/adr/0014-codex-app-external-ai-guardian-ui-adapter.md\n+++ b/docs/adr/SADR-0014-codex-app-external-ai-guardian-ui-adapter.md\n@@ -1,4 +1,4 @@\n-# ADR 0014: Use Codex App As The External AI Guardian UI Adapter\n+# SADR-0014: Use Codex App As The External AI Guardian UI Adapter\n \n ## Status\n \ndiff --git a/docs/adr/0015-contract-approval-before-dispatch.md b/docs/adr/SADR-0015-contract-approval-before-dispatch.md\nindex fad6c656741a20a7684ba16080a7db159f63a52f..6061271a46d3f1d6ae7174477de3fb75673123a6\n--- a/docs/adr/0015-contract-approval-before-dispatch.md\n+++ b/docs/adr/SADR-0015-contract-approval-before-dispatch.md\n@@ -1,4 +1,4 @@\n-# ADR 0015: Approve The Task Contract Before Dispatch, Not The Diff After It\n+# SADR-0015: Approve The Task Contract Before Dispatch, Not The Diff After It\n \n ## Status\n \n@@ -9,7 +9,7 @@\n Guardian approval for high/critical risk is expressible only on a pull request.\n Every approval source in `guardian-policy.json` \u2014 GitHub review, the\n `guardian:approved` label, the `Guardian approval granted` comment, and the\n-`external_ai_guardian_review` artifact (ADR 0010/0014) \u2014 is bound to a PR, and\n+`external_ai_guardian_review` artifact (SADR-0010/0014) \u2014 is bound to a PR, and\n three of the four require the current head SHA. Approval therefore cannot exist\n before an implementation PR exists.\n \n@@ -81,7 +81,7 @@\n An envelope breach must surface only on the non-repairable policy gate, never on\n the repairable `MergeGate metadata check`. Otherwise the Goal Loop routes it to\n `dispatch_repair` \u2014 instructing a runner to make the breach go away \u2014 which is\n-exactly what this ADR forbids.\n+exactly what this SADR forbids.\n \n **Contract immutability.** After registration, an implementation PR must not\n change the governance fields of its own task contract (`scope`, `non_goals`,\n@@ -116,7 +116,7 @@\n fails any verdict that is not `complete`, and the signal the CCA prompt reads is\n computed from `evaluate_guardian_approval` alone. Without the OR-merge at the\n signal, a contract-approved PR still returns `needs_guardian`, the CCA job fails,\n-and the MergeGate policy check never runs \u2014 the gate this ADR changes would never\n+and the MergeGate policy check never runs \u2014 the gate this SADR changes would never\n be reached. The CCA *prompt* needs no change: a `contract_approval` entry in the\n signal's existing `sources` array satisfies its current wording.\n \n@@ -130,7 +130,7 @@\n written, and an approved contract lets its implementation PRs merge through the\n same automatic path as low/medium risk. The wasted implement-then-block cycle\n disappears. Guardian review shifts from reading diffs to reading contracts, which\n-is both earlier and smaller; the external AI Guardian path (ADR 0010/0014) works\n+is both earlier and smaller; the external AI Guardian path (SADR-0010/0014) works\n on contracts with no change.\n \n We accept that the Guardian authorizes a class of change without seeing its\n@@ -146,7 +146,7 @@\n adversarial tests for the new approval path equivalent in rigor to\n `test_shiki_governance_evidence.sh`.\n \n-Bootstrap costs, which apply only while this ADR is being implemented. The tasks\n+Bootstrap costs, which apply only while this SADR is being implemented. The tasks\n that build Contract Approval merge under the existing rules, so the ones\n dispatched after the pre-dispatch gate lands but before the approval path lands\n need a Guardian-approved Contract PR *and* a live Guardian approval on their\n@@ -210,7 +210,7 @@\n risk; they do not bound authorized work.\n \n **Auto-escalate risk on envelope breach instead of stopping.** Returns the\n-workflow to a diff-time Guardian decision \u2014 the exact behavior this ADR removes.\n+workflow to a diff-time Guardian decision \u2014 the exact behavior this SADR removes.\n \n **Resolve governance fields from the base snapshot only.** Rejected against the\n registration flow: implementation PRs add their own contract, so the base has no\ndiff --git a/docs/adr/0016-locks-bound-product-paths.md b/docs/adr/SADR-0016-locks-bound-product-paths.md\nindex b5e850f3f1f7f4ef6e7f9d1763a29a51bd300c45..46b40a9b99a7aae644c02bb458f5473ff0a90ce2\n--- a/docs/adr/0016-locks-bound-product-paths.md\n+++ b/docs/adr/SADR-0016-locks-bound-product-paths.md\n@@ -1,4 +1,4 @@\n-# ADR 0016: Locks bound product paths; the Shiki mirror is bound by record identity\n+# SADR-0016: Locks bound product paths; the Shiki mirror is bound by record identity\n \n ## Status\n \n@@ -6,8 +6,8 @@\n \n - Date: 2026-07-28\n - Deciders: operator (mizutani-140), Claude (planner)\n-- Related: ADR 0004 (GitHub-first with a `.shiki` mirror), ADR 0009 (Spec Freeze),\n-  ADR 0012 (autonomous loop closeout to main), ADR 0015 (Contract Approval before\n+- Related: SADR-0004 (GitHub-first with a `.shiki` mirror), SADR-0009 (Spec Freeze),\n+  SADR-0012 (autonomous loop closeout to main), SADR-0015 (Contract Approval before\n   dispatch), remediation goal task R-02, the live PR #179 deadlock.\n \n ## Context\n@@ -90,11 +90,11 @@\n files, documentation files and named source modules are nameable; runtime-\n allocated ledger, runner and report ids are not \u2014 and those live in `.shiki`,\n which locks no longer govern. Existing frozen contracts are brought into line by\n-a one-time Spec Amendment per plan, which must land before ADR 0015's contract\n+a one-time Spec Amendment per plan, which must land before SADR-0015's contract\n immutability makes `locks` unwritable from the implementation PR.\n \n **Lock state is resolved from the durable side.** MergeGate resolves the lock\n-registry, and ADR 0015's governance fields, from the base `.shiki` snapshot when\n+registry, and SADR-0015's governance fields, from the base `.shiki` snapshot when\n present, falling back to head only when absent. The `MergeGate metadata check`\n job is given `--base-shiki`, which it does not receive today. Locks are not\n derived from the live open-pull-request set: a pull request body is untrusted\n@@ -108,7 +108,7 @@\n Explicitly out of scope: moving MergeGate's contract resolution off the PR\n checkout wholesale; making the mirror a projection of GitHub state; introducing\n new lock kinds; moving ledger evidence out of the repository; and unwinding\n-ADR 0012's closeout pull request. Registration of multi-task goals is ADR 0015's\n+SADR-0012's closeout pull request. Registration of multi-task goals is SADR-0015's\n Contract PR and is not re-decided here.\n \n ## Consequences\n@@ -127,7 +127,7 @@\n \n Risks accepted. First, goal-scoped identity is only as strong as `goal_id`,\n which is resolved from the PR body and the head task file; the mitigation is to\n-add `goal_id` to ADR 0015's frozen governance field set, and until that lands a\n+add `goal_id` to SADR-0015's frozen governance field set, and until that lands a\n task file can name a foreign goal. Second, base-when-present resolution is\n unavailable on fork pull requests, where the head is contributor-controlled;\n fork pull requests already cannot complete a Shiki merge, and this decision does\n@@ -139,7 +139,7 @@\n path, not by implementation pull requests.\n \n New obligations: one Spec Amendment per frozen plan to rewrite `locks`, ordered\n-before ADR 0015 T1; extension of the adversarial suites in\n+before SADR-0015 T1; extension of the adversarial suites in\n `scripts/test_shiki_adversarial_state.sh` and\n `scripts/test_shiki_governance_evidence.sh` to cover the eleven newly-scoped\n directories; and updating `scripts/test_shiki_loop_lock_guard.sh`,\n@@ -151,32 +151,32 @@\n whether the loop's transitive sync of ledger `evidence` references can be\n deleted once the Contract PR carries its own source plan.\n \n-## Relationship to ADR 0004\n+## Relationship to SADR-0004\n \n-This ADR **clarifies something ADR 0004 left unspecified**. ADR 0004 decides that\n+This SADR **clarifies something SADR-0004 left unspecified**. SADR-0004 decides that\n GitHub is the operational source of truth and that each target repository keeps a\n `.shiki/` mirror. It says nothing about pull requests writing the mirror, does\n not name MergeGate, and does not make the mirror an input to a gate. Nothing in\n-ADR 0004's Decision or Consequences is amended, superseded or weakened: the\n+SADR-0004's Decision or Consequences is amended, superseded or weakened: the\n mirror still exists, still records the same artifacts, and pull request\n-boundaries remain the coordination surface. What this ADR settles is the question\n+boundaries remain the coordination surface. What this SADR settles is the question\n 0004 never reached \u2014 by what authority a pull request may write a mirror record \u2014\n and it answers it the way `.shiki/manifest.json` already declares\n (`pr_mutation: current-task-or-current-goal-only`).\n \n-It **amends ADR 0012**, whose Decision item 5 states that \"Loop-executed tasks\n+It **amends SADR-0012**, whose Decision item 5 states that \"Loop-executed tasks\n declare the `path:.shiki/**` lock, which covers every `.shiki` file the closeout\n stages ... so `files_outside_locks` is satisfied without per-file lock\n bookkeeping.\" That clause is replaced: the closeout's mirror writes are\n-authorized by record identity, not by a declared lock. ADR 0012's substantive\n+authorized by record identity, not by a declared lock. SADR-0012's substantive\n decision \u2014 that completion reaches `main` through a closeout pull request rather\n than local attestation \u2014 is preserved unchanged.\n \n-It is **complementary to ADR 0015**, not a substitute. ADR 0015 owns contract\n+It is **complementary to SADR-0015**, not a substitute. SADR-0015 owns contract\n immutability, the pre-dispatch gate, the Contract PR and the Scope Envelope. This\n-ADR owns what a lock means and who may write the mirror. The one ordering\n+SADR owns what a lock means and who may write the mirror. The one ordering\n constraint between them is that the lock-narrowing Spec Amendment must precede\n-ADR 0015 T1's freezing of the `locks` field.\n+SADR-0015 T1's freezing of the `locks` field.\n \n ## Alternatives Considered\n \n@@ -186,7 +186,7 @@\n the only writer that can register them may run. Migrating the Guardian signal to\n that store makes every registration PR fail closed to `needs_guardian` at every\n risk level, because the task file is by definition absent from the store, which\n-removes the low/medium autonomous path ADR 0008/0009 exists to provide. And it\n+removes the low/medium autonomous path SADR-0008/0009 exists to provide. And it\n leaves schemas, manifest, config, guardian policy, templates, migrations, plans,\n worktrees, runner records, runs and handoffs with no writer at all in a\n repository whose product is `.shiki`. Measured benefit on the frozen backlog:\n@@ -199,14 +199,14 @@\n namespace is not in fact ownership-partitioned \u2014 56 ledger entries carry no\n `task_id`, and one goal DAG is writable by 35 registered tasks. The correct\n expression of its insight is that locks do not govern the mirror at all, which is\n-what this ADR decides.\n+what this SADR decides.\n \n **Make the mirror a projection written only by a registration writer and a\n post-merge projection job.** Rejected. It requires a bypass actor with write\n access to the protected default branch; it makes its own master invariant\n readable from a manifest that pull requests may edit; it deletes both reconcile\n modes and leaves a corrupted mirror with no in-band repair path, contradicting\n-ADR 0004's consequence that mirror conflicts are repaired against GitHub state;\n+SADR-0004's consequence that mirror conflicts are repaired against GitHub state;\n and its content-derived ids are rejected by `validate_shiki`'s currently-green id\n pattern.\n \ndiff --git a/docs/adr/0017-bookkeeping-closeouts-do-not-inherit-risk.md b/docs/adr/SADR-0017-bookkeeping-closeouts-do-not-inherit-risk.md\nindex 554e3a0d526a72ca067108c5464cc68c3edcf174..b9676857a4713850e063af7c39c8e0b01ecb4c66\n--- a/docs/adr/0017-bookkeeping-closeouts-do-not-inherit-risk.md\n+++ b/docs/adr/SADR-0017-bookkeeping-closeouts-do-not-inherit-risk.md\n@@ -1,4 +1,4 @@\n-# ADR 0017: A Bookkeeping Closeout Does Not Inherit Its Task's Risk\n+# SADR-0017: A Bookkeeping Closeout Does Not Inherit Its Task's Risk\n \n ## Status\n \n@@ -6,7 +6,7 @@\n \n ## Context\n \n-ADR 0012 made the goal loop push a task's completion to the default branch through a\n+SADR-0012 made the goal loop push a task's completion to the default branch through a\n closeout pull request: after the implementation PR merges, the loop opens a second PR\n carrying `task.status = done`, `lock.state = released`, and \u2014 when the task completes\n its goal \u2014 `goal.status = complete` plus one scorecard report. Nothing else. The\n@@ -49,14 +49,14 @@\n 4. The task file's governance fields (`scope`, `non_goals`, `required_skills`,\n    `risk_level`, `locks`, `acceptance_checks`, `test_command`) are byte-identical to\n    the base snapshot. Only `status`, `expected_pr`, `closeout_pr`, `ledger_evidence`\n-   and `expected_branch` may differ \u2014 the ADR 0015 contract-immutability rule,\n+   and `expected_branch` may differ \u2014 the SADR-0015 contract-immutability rule,\n    unchanged.\n \n    `expected_branch` is in that set because a closeout is *by construction* on a\n    different branch from the implementation it closes, and the loop rewrites the\n    field accordingly. It is safe to let it differ because MergeGate independently\n    requires `expected_branch` to equal the PR's actual head ref, so a PR cannot set\n-   it to anything other than the branch it is really on. The first draft of this ADR\n+   it to anything other than the branch it is really on. The first draft of this SADR\n    omitted it, which made the exemption unreachable: every real closeout failed\n    condition 4 on a field the loop always changes. See PR #209.\n 5. The state transitions are exactly the terminal set: task `review` \u2192 `done`, lock\n@@ -113,12 +113,12 @@\n classification must be derived, not asserted.\n \n **Fold the closeout into the implementation PR** so no second PR exists. Rejected under\n-ADR 0012, which recorded why: a task file that is already `done` on the implementation\n+SADR-0012, which recorded why: a task file that is already `done` on the implementation\n branch short-circuits the loop's review \u2192 CCA \u2192 merge \u2192 done ordering into a local\n goal-completion before any merge happens.\n \n **Have the loop auto-merge closeouts with elevated credentials.** Rejected outright: that\n-is a runtime authoring its own approval, which ADR 0010 and the constitution's\n+is a runtime authoring its own approval, which SADR-0010 and the constitution's\n approval-record prohibition forbid. It would also be indistinguishable, in the evidence,\n from the forgery those rules exist to prevent.\n \ndiff --git a/docs/adr/0018-carried-guardian-approval-across-a-proven-base-sync.md b/docs/adr/SADR-0018-carried-guardian-approval-across-a-proven-base-sync.md\nindex 22358562da390ed8b45cd600da08a43d378b4680..e6e2907cefa776460af73ddf7d39db605f3c39b0\n--- a/docs/adr/0018-carried-guardian-approval-across-a-proven-base-sync.md\n+++ b/docs/adr/SADR-0018-carried-guardian-approval-across-a-proven-base-sync.md\n@@ -1,4 +1,4 @@\n-# ADR 0018: Carried Guardian Approval Across A Proven Base Sync\n+# SADR-0018: Carried Guardian Approval Across A Proven Base Sync\n \n ## Status\n \n@@ -34,7 +34,7 @@\n Guardian evaluator that consumes it (`shiki_guardian.evaluate_guardian_approval`,\n the `guardian_comment_carried` source) were built and tested in isolation. Neither\n does anything until a caller passes the flags that turn the carry on, so the\n-feature was invisible in CI. This ADR records the decision the two of them\n+feature was invisible in CI. This SADR records the decision the two of them\n implement, and this task wires it into the four CI invocations that judge Guardian\n approval.\n \ndiff --git a/docs/adr/README.md b/docs/adr/README.md\nindex c70e057a4520c36e042ab68c53cd4a21ef709b71..5da43856836e2ce6a150f8127766e2f88355ab4b\n--- a/docs/adr/README.md\n+++ b/docs/adr/README.md\n@@ -1,40 +1,41 @@\n-# Architecture Decision Records\n+# Decision Record Namespaces\n \n-This directory holds Shiki's Architecture Decision Records (ADRs): durable\n-records of hard-to-reverse platform decisions. ADRs are part of the Shiki source\n-of truth alongside GitHub state, the `.shiki/` mirror, and `CONTEXT.md`.\n+This directory holds two independent decision namespaces:\n \n-## How ADRs Work\n+- Shiki Architecture Decision Records (SADRs), named\n+  `SADR-NNNN-<short-slug>.md`, record hard-to-reverse Shiki platform decisions.\n+- Target Architecture Decision Records (ADRs), named\n+  `NNNN-<short-slug>.md`, belong to the target product.\n \n-- New ADRs are added as `docs/adr/NNNN-<short-slug>.md`, where `NNNN` is the next\n-  zero-padded sequence number.\n-- The **canonical list of ADRs is the set of files matching `docs/adr/*.md`** in\n-  this directory. Browse `docs/adr/*.md` for the authoritative, current set.\n-- Use [`template.md`](template.md) as the starting point for a new ADR.\n-- Record an ADR when a decision is hard to reverse, surprising without context,\n-  and a real tradeoff. Do not write an ADR for routine or easily reversible\n-  choices.\n+The same four-digit number may appear once in each namespace. A platform\n+`SADR-NNNN` citation resolves only to `SADR-NNNN-*.md`; a target `ADR NNNN`\n+citation resolves only to `NNNN-*.md`.\n \n-Using the `docs/adr/*.md` glob as the source of truth means other PRs can add an\n-ADR without editing this index file, avoiding merge contention on a\n-hand-maintained enumeration.\n+## How Decision Records Work\n \n-## Current ADRs (snapshot)\n-\n-The list below is a convenience snapshot and may lag behind the directory. When\n-in doubt, list `docs/adr/*.md`.\n+- New Shiki platform decisions use the next zero-padded SADR number.\n+- New target product decisions use the target's own ADR sequence.\n+- The canonical list is the files in `docs/adr/`; no hand-maintained index is\n+  authoritative.\n+- Use [`template.md`](template.md) as the starting point and choose the filename\n+  namespace that owns the decision.\n+- Record a decision when it is hard to reverse, surprising without context, and\n+  a real tradeoff. Do not write a decision record for routine or easily\n+  reversible choices.\n \n-- [0001 \u2014 Keep Shiki As The Platform Name](0001-keep-shiki-as-platform-name.md)\n-- [0002 \u2014 Use AGENTS As The Shared Agent Constitution](0002-use-agents-as-shared-constitution.md)\n-- [0003 \u2014 Require Skill Gate For MergeGate Readiness](0003-require-skill-gate-for-mergegate-readiness.md)\n-- [0004 \u2014 Use GitHub First With A Repository-Local Shiki Mirror](0004-use-github-first-with-shiki-mirror.md)\n-- [0005 \u2014 Use Codex As Front And Claude Code Action As The GitHub Runtime](0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md)\n-- [0006 \u2014 Separate Codex Implementation, CCA Completion, And MergeGate Authorization](0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md)\n+Using the directory as the source of truth lets independent changes add records\n+without contending on an enumerated index.\n \n-## Adding A New ADR\n+## Adding A Shiki SADR\n \n-1. Copy `template.md` to `docs/adr/NNNN-<short-slug>.md`.\n+1. Copy `template.md` to `docs/adr/SADR-NNNN-<short-slug>.md`.\n 2. Fill in the title, status, context, decision, and consequences.\n-3. Set the status to `Proposed`, then `Accepted` once the decision is approved.\n-4. Open a PR following `CONTRIBUTING.md`. You do not need to edit this index for\n-   the ADR to be discoverable via `docs/adr/*.md`.\n+3. Set the status to `Proposed`, then `Accepted` once the proper authority has\n+   approved the decision.\n+4. Open a PR following `CONTRIBUTING.md`.\n+\n+## Adding A Target ADR\n+\n+1. Copy `template.md` to `docs/adr/NNNN-<short-slug>.md` using the target's own\n+   sequence.\n+2. Cite it as `ADR NNNN`; do not prefix its filename with `SADR-`.\ndiff --git a/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md b/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\nnew file mode 100644\nindex 0000000000000000000000000000000000000000..7455ce1f3bed02ddddb851ac21a1d1a648c9aeba\n--- /dev/null\n+++ b/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\n@@ -0,0 +1,55 @@\n+# SADR-0019: Separate Shiki Platform SADRs From Target ADRs\n+\n+## Status\n+\n+Accepted\n+\n+## Context\n+\n+Shiki installs its platform decision records into `docs/adr/` in every target\n+repository. Shiki and its targets previously used the same numeric\n+`NNNN-<slug>.md` namespace. A target that already owned ADRs 0013 through 0018\n+therefore acquired two records for each number during installation, making an\n+`ADR NNNN` citation ambiguous. Renumbering target decisions would rewrite target\n+history, while renumbering Shiki decisions per target would destroy their stable\n+platform identity.\n+\n+Upgrades also need to remove the former Shiki-owned numeric paths without ever\n+mistaking a target-owned ADR for platform content. The install stamp already\n+records exact shipped paths and content digests, so it can prove ownership only\n+when both still match.\n+\n+## Decision\n+\n+Shiki platform decisions use the `SADR` namespace:\n+\n+- records are named `docs/adr/SADR-NNNN-<slug>.md`;\n+- live platform citations use `SADR-NNNN`;\n+- target product decisions retain `docs/adr/NNNN-<slug>.md` and `ADR NNNN`;\n+- the reference validator resolves the two namespaces independently, so the same\n+  four-digit number may exist once in each namespace.\n+\n+Before a forced upgrade writes anything, the installer inventories the exact\n+former numeric Shiki ADR paths. It may delete a legacy path only when the\n+existing install stamp contains that exact path and its recorded SHA-256 digest\n+matches the file currently on disk. All ownership blockers are reported\n+together and abort the upgrade before its first write. A non-forced install\n+never performs this cleanup.\n+\n+Historical `.shiki/` evidence remains unchanged because it records the namespace\n+that existed when that evidence was produced.\n+\n+## Consequences\n+\n+- Targets may use any ADR number without colliding with Shiki's platform SADRs.\n+- Fresh installs preserve target ADR bytes and install a stable SADR set.\n+- Forced upgrades fail closed when legacy ownership cannot be proven, and a\n+  successful upgrade refreshes the stamp without removed legacy paths.\n+- Current governance, packaging, workflow, prompt, script, and skill references\n+  must move with the platform records.\n+\n+## Alternatives Considered\n+\n+- Collision-only refusal leaves affected targets permanently unupgradable.\n+- Renumbering target ADRs mutates target-owned history.\n+- Per-target Shiki renumbering makes platform citations non-portable.\ndiff --git a/docs/adr/template.md b/docs/adr/template.md\nindex a92a97daddd0b00ab9dd626cc8d86942da2eafed..17dd79175b5fee2105935557f691b40cf9492942\n--- a/docs/adr/template.md\n+++ b/docs/adr/template.md\n@@ -1,14 +1,14 @@\n-# NNNN: <Short Decision Title>\n+# <SADR-NNNN or NNNN>: <Short Decision Title>\n \n ## Status\n \n-Proposed | Accepted | Superseded by ADR-NNNN\n+Proposed | Accepted | Superseded by SADR-NNNN or target ADR NNNN\n \n ## Context\n \n What is the situation, constraint, or force that requires a decision? Reference\n-relevant domain terms (`CONTEXT.md`), prior ADRs, and code reality. State the\n-problem, not the solution.\n+relevant domain terms (`CONTEXT.md`), prior Shiki SADRs or target ADRs, and code\n+reality. State the problem, not the solution.\n \n ## Decision\n \ndiff --git a/docs/agents/autonomous-loop-e2e-acceptance.md b/docs/agents/autonomous-loop-e2e-acceptance.md\nindex bbf84c6081987f159ac6ad9ecf3c82b7aa72e7e8..5c13c1d02a05a90b0dc46cbef7c95a9846d0c237\n--- a/docs/agents/autonomous-loop-e2e-acceptance.md\n+++ b/docs/agents/autonomous-loop-e2e-acceptance.md\n@@ -40,8 +40,8 @@\n 3. `gh auth status` is logged in with merge permission on the repo.\n 4. Branch protection + required checks are configured exactly as the goal-loop\n    required-check set expects (see `docs/agents/branch-protection-smoke.md`).\n-5. The repo `.shiki/policy` permits autonomous low/medium-risk merges (ADR\n-   0008/0009); the MergeGate policy check is the recorded-authority gate.\n+5. The repo `.shiki/policy` permits autonomous low/medium-risk merges\n+   (SADR-0008/0009); the MergeGate policy check is the recorded-authority gate.\n \n The operator's only actions are: register the task and start the loop. **No\n manual pre-clearing** means after `shiki loop run` starts, the operator does not\ndiff --git a/docs/agents/bootstrap-command.md b/docs/agents/bootstrap-command.md\nindex 05f7f203a52cf412b9f97084a6e131ed0f785afd..f7aa0b734dfebee4a3b139335b66ee3415f48713\n--- a/docs/agents/bootstrap-command.md\n+++ b/docs/agents/bootstrap-command.md\n@@ -189,7 +189,9 @@\n ```\n \n Use this only for tests, fixtures, or explicit local-only template inspection.\n-Use `--force` only when you intentionally want to overwrite existing target files.\n+Use `--force` only when you intentionally want to refresh the target template.\n+Legacy numeric Shiki decision cleanup is stamp-and-digest authorized before any\n+write; target ADR paths are not cleanup candidates.\n \n ## Slash Command\n \ndiff --git a/docs/agents/checklists.md b/docs/agents/checklists.md\nindex 4a7f03ade346f53a1c1d82981f3fd744f9ab4547..0c5ec6c53dc7197b7b7a7d18e667b325babd6c45\n--- a/docs/agents/checklists.md\n+++ b/docs/agents/checklists.md\n@@ -32,18 +32,18 @@\n | ID | Item | Blocking |\n | --- | --- | --- |\n | GD-01 | Plan was challenged against `CONTEXT.md`. | blocking for non-trivial Goals |\n-| GD-02 | Relevant ADRs were read or absence was recorded. | blocking for non-trivial Goals |\n+| GD-02 | Relevant SADRs and target ADRs were read or absence was recorded. | blocking for non-trivial Goals |\n | GD-03 | Ambiguous terms were resolved or converted to HITL blockers. | blocking |\n | GD-04 | Edge scenarios were considered. | advisory |\n | GD-05 | Code reality was checked when code could answer. | blocking when applicable |\n | GD-06 | New domain terms were added to `CONTEXT.md` when needed. | blocking when applicable |\n-| GD-07 | ADR was created or explicitly skipped using the ADR criteria. | blocking when applicable |\n+| GD-07 | A SADR or target ADR was created or explicitly skipped using the decision-record criteria. | blocking when applicable |\n \n ## CI \u2014 Context & Impact Checklist\n \n | ID | Item | Blocking |\n | --- | --- | --- |\n-| CI-01 | Relevant docs and ADRs identified. | blocking |\n+| CI-01 | Relevant docs, SADRs, and target ADRs identified. | blocking |\n | CI-02 | Relevant modules, interfaces, seams, callers, and tests identified. | blocking |\n | CI-03 | Dependencies and blocked-by relationships identified. | blocking |\n | CI-04 | Candidate locks identified. | blocking |\n@@ -62,7 +62,7 @@\n | PRD-04 | Implementation decisions are recorded without volatile code detail. | blocking |\n | PRD-05 | Testing decisions are recorded. | blocking |\n | PRD-06 | Out-of-scope items are explicit. | blocking |\n-| PRD-07 | Relevant glossary/ADR links are present when applicable. | blocking |\n+| PRD-07 | Relevant glossary/SADR/target-ADR links are present when applicable. | blocking |\n \n ## SF \u2014 Spec Freeze Checklist\n \n@@ -98,7 +98,7 @@\n | H-02 | Branch/worktree target is present. | blocking |\n | H-03 | Scope and non-goals are present. | blocking |\n | H-04 | Dependencies and locks are present. | blocking |\n-| H-05 | Relevant docs, ADRs, modules, and tests are referenced. | blocking |\n+| H-05 | Relevant docs, SADRs, target ADRs, modules, and tests are referenced. | blocking |\n | H-06 | Required skills are stated. | blocking |\n | H-07 | TDD expectations are stated. | blocking when behavior work |\n | H-08 | Acceptance criteria are included. | blocking |\ndiff --git a/docs/agents/codex-handoff.md b/docs/agents/codex-handoff.md\nindex ede468f47cb277f69a857e8c45df703e4ed0151d..6f97bdffb67e44d7edea2c71e01ea28933c4eb6c\n--- a/docs/agents/codex-handoff.md\n+++ b/docs/agents/codex-handoff.md\n@@ -46,7 +46,7 @@\n \n ## Context\n - Domain terms:\n-- Relevant ADRs:\n+- Relevant SADRs and target ADRs:\n - Relevant docs:\n - Relevant modules/interfaces/seams:\n - Similar tests/prior art:\ndiff --git a/docs/agents/completion-check-agent.md b/docs/agents/completion-check-agent.md\nindex 13e4c035db35025f4e3cdcc5adfb24314ac4d521..61bfa1218cf36ca605250c91919e620f33238aa0\n--- a/docs/agents/completion-check-agent.md\n+++ b/docs/agents/completion-check-agent.md\n@@ -32,7 +32,7 @@\n - `AGENTS.md`\n - `CLAUDE.md`\n - `CONTEXT.md`\n-- relevant ADRs\n+- relevant SADRs and target ADRs\n - parent Goal issue\n - PRD issue or document\n - task issue\ndiff --git a/docs/agents/decision-control.md b/docs/agents/decision-control.md\nindex 7d8ee5ab03a5ccbcbd04056ae61de03785a2a9c9..8c94ecda00fffaa0e92c325ab83aa9776bd1dac1\n--- a/docs/agents/decision-control.md\n+++ b/docs/agents/decision-control.md\n@@ -11,7 +11,7 @@\n | Decision | Owner | When | Required inputs | Output | Enforcement surface |\n | --- | --- | --- | --- | --- | --- |\n | Is the Goal clear enough to plan? | Planner + Guardian when needed | Goal intake | User request, existing docs, risk signals | Goal readiness verdict | Goal checklist, issue template, ledger |\n-| Must `grill-with-docs` run? | Planner / Skill Gate | Before PRD or issue split | Ambiguity, domain terms, ADR conflicts, unclear boundaries | Skill invocation or explicit skip rationale | Skill Gate checklist |\n+| Must `grill-with-docs` run? | Planner / Skill Gate | Before PRD or issue split | Ambiguity, domain terms, SADR/target-ADR conflicts, unclear boundaries | Skill invocation or explicit skip rationale | Skill Gate checklist |\n | Is a PRD required? | Planner | After Goal context stabilizes | Goal, `grill-with-docs` output, Context & Impact output | PRD required / not required | PRD checklist, CCA evidence check |\n | Can the spec be frozen? | Operator only | End of Requirements Definition, before issue split | PRD/requirements summary, scope inventory (SF-02), open-question list | `spec_freeze` block in the plan + ledger evidence | `require_grilled_plan` (plan ingest/run), SF checklist |\n | Can a frozen spec change? | Operator only (Spec Amendment) | When implementation contradicts the frozen spec | Contested decisions, scoped re-grill result | recorded amendment + re-stamped freeze | SF checklist, ledger evidence |\ndiff --git a/docs/agents/domain.md b/docs/agents/domain.md\nindex 258da8ea81e87ac0a6b0b5b5e5c2fde980eaabb7..d57b0cf17ff3e1fa9e6e5885372bf083d8295421\n--- a/docs/agents/domain.md\n+++ b/docs/agents/domain.md\n@@ -5,7 +5,9 @@\n ## Layout\n \n - `CONTEXT.md` defines Shiki language. Keep it implementation-free.\n-- `docs/adr/` records hard-to-reverse decisions.\n+- `docs/adr/SADR-NNNN-*.md` records Shiki platform decisions.\n+- `docs/adr/NNNN-*.md` records target product decisions without sharing the\n+  SADR namespace.\n - `AGENTS.md` is the runtime-neutral agent constitution.\n - `CLAUDE.md` is the Claude Code wrapper around `AGENTS.md`.\n - `.shiki/` is the Target Repository mirror for Goals, PRDs, plans, Task DAGs, contracts, locks, worktree records, ledger entries, CCA verdicts, repair packets, reports, and handoffs.\n@@ -15,6 +17,6 @@\n \n - Use Shiki terms exactly when they exist.\n - Add glossary terms to `CONTEXT.md` only when the term is part of the domain, not a generic programming concept.\n-- Create ADRs only for hard-to-reverse, surprising, tradeoff-based decisions.\n+- Create SADRs or target ADRs only for hard-to-reverse, surprising, tradeoff-based decisions.\n - Keep product-specific language in the Target Repository, not in Shiki Template docs.\n-- If an ADR conflicts with a proposed implementation, surface the conflict before editing.\n+- If a SADR or target ADR conflicts with a proposed implementation, surface the conflict before editing.\ndiff --git a/docs/agents/execution-control.md b/docs/agents/execution-control.md\nindex ad8ee3cdb36be56fedc1f46408ca92b387d74047..bc63c3a73661d12b567016ba299f1a6b66bf9088\n--- a/docs/agents/execution-control.md\n+++ b/docs/agents/execution-control.md\n@@ -63,7 +63,7 @@\n | Branch name | Orchestrator | Orchestrator | Before implementer dispatch | branch policy | branch ref in task/ledger |\n | Lock reservation | Context & Impact | MergeGate | Before dispatch and before merge | `.shiki/locks/`, labels, PR metadata | lock id, owner, scope |\n | CI profile | Planner | MergeGate | During issue triage, rechecked at PR | labels, `.shiki/policy.yaml`, workflow matrix | required check list |\n-| Required status checks | Repo maintainer / Guardian | GitHub branch protection / rulesets | Repository setup, updated via ADR | GitHub settings | branch/ruleset config, ADR |\n+| Required status checks | Repo maintainer / Guardian | GitHub branch protection / rulesets | Repository setup, updated via target ADR | GitHub settings | branch/ruleset config, target ADR |\n | CCA judgment | CCA | GitHub required check / MergeGate | Every non-draft PR update | GitHub Action check | structured CCA verdict |\n | Repair assignment | CCA | MergeGate | After failed CCA/CI/review | repair packet, issue/PR comment | repair id and attempt count |\n | Merge readiness | MergeGate | GitHub branch protection / Guardian if needed | After CCA complete | required checks, review rules, rulesets | mergegate check output |\ndiff --git a/docs/agents/guardian-policy.md b/docs/agents/guardian-policy.md\nindex 4147c7adf05a47adb4fd24ada1a1c2e1f2ba6d2d..a20d9cc6c00a569bb057b9ca8d6b51c7fc4a9847\n--- a/docs/agents/guardian-policy.md\n+++ b/docs/agents/guardian-policy.md\n@@ -98,8 +98,8 @@\n `origin/<default_branch>` and reuses each job's existing base fetch; a shallow\n (`--depth`) or second base fetch would make the proof unresolvable and silently\n disable the carry, so neither is permitted. CCA reads `guardian_comment_carried` as\n-a recorded authority in the deterministic signal and does not re-derive it. See ADR\n-0018 for the full decision, the rejected diff/patch-equality and path-disjointness\n+a recorded authority in the deterministic signal and does not re-derive it. See\n+SADR-0018 for the full decision, the rejected diff/patch-equality and path-disjointness\n alternatives, and the accepted residual.\n \n ## External AI Guardian Review (SADR-0010)\ndiff --git a/docs/agents/implementation-policy.md b/docs/agents/implementation-policy.md\nindex c754144a9fb5bf4a38aa82a60e1670c59e626b44..85c37281bcad196c6b7b02343faf00b03cd9c57a\n--- a/docs/agents/implementation-policy.md\n+++ b/docs/agents/implementation-policy.md\n@@ -31,10 +31,13 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n ## Non-Negotiables\n \n 1. **Planning precedes implementation.** Non-trivial Goals start with `grill-with-docs` and Context & Impact.\n-2. **Domain language is controlled.** Terms must be checked against `CONTEXT.md`; hard-to-reverse tradeoffs must be checked against ADRs.\n+2. **Domain language is controlled.** Terms must be checked against `CONTEXT.md`; hard-to-reverse tradeoffs must be checked against Shiki SADRs and target ADRs.\n 3. **PRD precedes issue decomposition.** `to-prd` is used once the context is settled enough to become durable intent.\n 4. **Issues are vertical slices.** `to-issues` must produce independently grabbable tracer-bullet tasks, not horizontal layer tickets.\n 5. **Triage controls dispatch.** Only `ready-for-agent` / AFK tasks may be assigned to Codex without human input.\n@@ -95,19 +98,19 @@\n Process:\n \n - Challenge terms against `CONTEXT.md`.\n-- Check ADRs before reopening decisions.\n+- Check SADRs and target ADRs before reopening decisions.\n - Inspect code when code can answer a question.\n - Ask one question at a time when the operator is available.\n - Provide a recommended answer with each question.\n - Record resolved terms in `CONTEXT.md`.\n-- Create ADRs only for hard-to-reverse, surprising, tradeoff-based decisions.\n+- Create SADRs or target ADRs only for hard-to-reverse, surprising, tradeoff-based decisions.\n - Convert unresolved load-bearing questions into HITL blockers.\n \n Outputs:\n \n - Resolved design-tree decisions.\n - Updated glossary terms when needed.\n-- ADRs when justified.\n+- SADRs or target ADRs when justified.\n - HITL blockers when decisions remain unresolved.\n \n ## Phase 3 \u2014 Context & Impact\n@@ -117,7 +120,7 @@\n \n Outputs must include:\n \n-- Relevant documents and ADRs.\n+- Relevant documents, SADRs, and target ADRs.\n - Relevant modules, interfaces, seams, callers, and tests.\n - Candidate locks.\n - Dependencies.\n@@ -140,7 +143,7 @@\n - Testing decisions.\n - Out of scope.\n - Further notes.\n-- Links to relevant Goal, domain terms, and ADRs.\n+- Links to relevant Goal, domain terms, SADRs, and target ADRs.\n \n Phase 4 ends with Spec Freeze: the operator explicitly approves the PRD, the\n plan records a `spec_freeze` block (status, approved_by, source), and the SF\n@@ -240,7 +243,7 @@\n - Dependencies and locks.\n - Risk and Guardian requirements.\n - Ledger completeness.\n-- Documentation and ADR updates.\n+- Documentation and SADR/target-ADR updates.\n - Scope drift.\n \n CCA emits one of:\n@@ -301,7 +304,7 @@\n | Evidence failure | Behavior might be done, but proof is missing. | Add checks, PR evidence, ledger entry, or artifacts. |\n | Scope drift | PR changes unrelated behavior. | Revert unrelated changes or split into new Goal. |\n | Dependency block | Upstream task or lock not complete. | Block until dependency resolves. |\n-| Architecture block | Decision needs ADR or deep-module discussion. | `grill-with-docs` or `improve-codebase-architecture`. |\n+| Architecture block | Decision needs a SADR, target ADR, or deep-module discussion. | `grill-with-docs` or `improve-codebase-architecture`. |\n | Risk block | Security/production/policy change needs human approval. | Guardian decision. |\n \n ## Phase 5.5 \u2014 Execution Decision Control\ndiff --git a/docs/agents/runtime-auth-model.md b/docs/agents/runtime-auth-model.md\nindex 6a2e1a47e81ea89b5c68a210ec07772a9374babc..3c54748d24233dbd0ba8473950282547ef4ce935\n--- a/docs/agents/runtime-auth-model.md\n+++ b/docs/agents/runtime-auth-model.md\n@@ -148,7 +148,7 @@\n \n API-key based runners may be added by a target repository as an explicit extension. They are not the default Shiki template path.\n \n-If a repo opts into API-key mode, record the exception in an ADR and update the repo's `AGENTS.md`, `.shiki/config.yaml`, and workflow permissions.\n+If a repo opts into API-key mode, record the target-specific exception in a target ADR and update the repo's `AGENTS.md`, `.shiki/config.yaml`, and workflow permissions.\n \n ## GitHub Actions Runtime Compatibility\n \ndiff --git a/docs/agents/skill-gate.md b/docs/agents/skill-gate.md\nindex c50398b0d7c60a8598f2a5c88a10015730dced13..3acffe6444617bb1d54b77d4b4708158ec735bc6\n--- a/docs/agents/skill-gate.md\n+++ b/docs/agents/skill-gate.md\n@@ -9,7 +9,7 @@\n | Skill | Trigger | Expected output |\n | --- | --- | --- |\n | `setup-matt-pocock-skills` | First configuring a repo for these skills, or when issue tracker, triage labels, or domain docs are missing. | `docs/agents/` configuration and an `Agent skills` block in `AGENTS.md` or `CLAUDE.md`. |\n-| `grill-with-docs` | Ambiguous plan, unclear terminology, boundary decision, tradeoff, or ADR-worthy design choice. | Clarified decision tree, recommended answers, updates to `CONTEXT.md` or ADRs when decisions crystallize. |\n+| `grill-with-docs` | Ambiguous plan, unclear terminology, boundary decision, tradeoff, or decision-record-worthy design choice. | Clarified decision tree, recommended answers, updates to `CONTEXT.md`, SADRs, or target ADRs when decisions crystallize. |\n | `zoom-out` | Agent lacks a higher-level architectural map. | Map of relevant modules, callers, seams, and domain concepts. |\n | `to-prd` | Settled context should become a PRD. | PRD published to the issue tracker with problem, solution, user stories, decisions, testing, and out-of-scope notes. |\n | `to-issues` | Goal, PRD, or plan must become implementation tickets. | Vertical-slice issues with acceptance criteria, dependencies, and AFK/HITL classification. |\n@@ -17,7 +17,7 @@\n | `tdd` | Feature work or bug fix has observable behavior that can be specified with tests. | Red-green-refactor loop through public interfaces, one vertical slice at a time. |\n | `code-review` | The TDD loop is green and the implementer is about to create or update a PR. Default for every implementation task. | Pre-PR self-review of the task diff with fixes applied on the task branch; findings and resolutions recorded as a ledger entry naming the skill and a `## Pre-PR code review` PR body section. |\n | `diagnose` | Hard bug, failing check, regression, flaky behavior, or performance problem. | Reproduction loop, ranked hypotheses, instrumentation, fix, regression test, post-mortem. |\n-| `improve-codebase-architecture` | Structural friction, testability, AI-navigability, deep-module opportunity, or refactor request. | Architecture candidates and deepening recommendations informed by `CONTEXT.md` and ADRs. |\n+| `improve-codebase-architecture` | Structural friction, testability, AI-navigability, deep-module opportunity, or refactor request. | Architecture candidates and deepening recommendations informed by `CONTEXT.md`, SADRs, and target ADRs. |\n | `prototype` | A throwaway logic or UI prototype is needed to answer a design question. | Clearly marked disposable prototype and captured decision. |\n \n ## MergeGate Interaction\ndiff --git a/docs/operations.md b/docs/operations.md\nindex cf1f452d4c589e38ccd58553bda0ad8684b51eb7..ace48a90ffbec9e5c0a7113bef16a4858bc44b3f\n--- a/docs/operations.md\n+++ b/docs/operations.md\n@@ -89,6 +89,16 @@\n      target history, brought forward by `shiki migrate`, not a template.\n    - **Everything else** (CLI scripts, workflows, schemas, docs) is overwritten.\n \n+   A forced upgrade also migrates the former numeric Shiki decision paths to\n+   `SADR-NNNN-*`. Before the first write, the installer inventories the exact\n+   legacy Shiki paths and authorizes each deletion only when the existing\n+   `.shiki/install-stamp.json` names that path and its SHA-256 digest still\n+   matches the file. An absent, unreadable, malformed, incomplete, or\n+   mismatching stamp reports every blocked path and stops with no filesystem\n+   change. Target-owned `NNNN-*` ADRs are not cleanup candidates. A non-force\n+   install deletes nothing and retains any still-matching legacy stamp entries\n+   so a later forced upgrade can prove ownership.\n+\n    If the target has pending migrations, `--force` refuses and writes nothing \u2014\n    apply migrations first (step 3), then re-run. The run ends with a single\n    summary of every `.new` written, so a half-upgrade is visible.\ndiff --git a/docs/releasing.md b/docs/releasing.md\nindex 10d73828e7dbd64e9d9f6b83bc84d010537ce62a..aafeee8c4d441e1e427efee2ce4f81a647f020e1\n--- a/docs/releasing.md\n+++ b/docs/releasing.md\n@@ -67,7 +67,7 @@\n CLI in `scripts/`. There are no third-party Python runtime dependencies. The CLI\n is intentionally **not** exposed as a `[project.scripts]` console entry point\n because `scripts/shiki.py` is a dependency-free shim and `scripts/` is not\n-packaged as an importable distribution (see the ADR for the rationale).\n+packaged as an importable distribution (see SADR-0007 for the rationale).\n \n Supported install paths:\n \n@@ -155,6 +155,15 @@\n    [docs/operations.md](operations.md#the-new-convention). If the target has\n    pending migrations, `--force` refuses and writes nothing; apply migrations\n    (step 2) first, then re-run.\n+\n+   The SADR namespace upgrade has an additional pre-write gate: each former\n+   numeric Shiki decision path is removed only when the existing install stamp\n+   contains that exact path and a digest matching its current bytes. The\n+   installer aggregates absent/unreadable/malformed-stamp, missing-digest, and\n+   digest-mismatch blockers before raising, so refusal leaves the target tree\n+   byte-identical. Numeric target ADRs are preserved. A successful forced\n+   upgrade installs and stamps `SADR-*` paths and drops the removed legacy paths\n+   from the refreshed stamp.\n 2. Apply repository-local `.shiki/` state migrations with the migrate command:\n \n    ```bash\ndiff --git a/docs/usage.md b/docs/usage.md\nindex b83407ee3970dd395dd893010bc5dbe8bad26051..a130ef6e40dfd8169a5bc701339351097df1a09f\n--- a/docs/usage.md\n+++ b/docs/usage.md\n@@ -67,8 +67,10 @@\n shiki install-target /path/to/target-repo --local-only\n ```\n \n-Use `--force` only when you intentionally want to overwrite existing target\n-files.\n+Use `--force` only when you intentionally want to refresh the target template.\n+For the SADR namespace migration, `--force` deletes only exact legacy Shiki\n+decision paths whose existing install-stamp digest still matches; any ownership\n+blocker stops before the first write. Numeric target ADRs are never candidates.\n \n ## `shiki bootstrap-platform` \u2014 publish the Shiki platform repo\n \ndiff --git a/scripts/guardian_approval_signal.py b/scripts/guardian_approval_signal.py\nindex d059f92b2c82aa2b5d6ca421365537a87886c1cb..16924892714f4f5b9303c0ef0e325a7e70075630\n--- a/scripts/guardian_approval_signal.py\n+++ b/scripts/guardian_approval_signal.py\n@@ -199,8 +199,8 @@\n ) -> Any:\n     \"\"\"Evaluate SADR-0015 Contract Approval for the CCA Guardian signal.\n \n-    The SAME OR that MergeGate applies must reach this deterministic signal: ADR\n-    0015 is explicit that without it, a contract-approved PR still returns\n+    The SAME OR that MergeGate applies must reach this deterministic signal:\n+    SADR-0015 is explicit that without it, a contract-approved PR still returns\n     ``needs_guardian``, the CCA job fails, and the MergeGate policy check never\n     runs \u2014 the gate this change moves would never be reached. This resolves the\n     task the SAME way MergeGate does (the first Shiki task id in the PR body),\ndiff --git a/scripts/shiki_contract_approval.py b/scripts/shiki_contract_approval.py\nindex 941bcb96fdd8b9a85bc0c4f0f04d5dad9d04839c..4a4b418f6527413161db82670bec857a043360e2\n--- a/scripts/shiki_contract_approval.py\n+++ b/scripts/shiki_contract_approval.py\n@@ -142,7 +142,7 @@\n     changed_files_status: Any,\n     registration: Any,\n ) -> ContractApprovalResult:\n-    \"\"\"Whether this PR carries an SADR-0015 Contract Approval.\n+    \"\"\"Whether this PR carries a SADR-0015 Contract Approval.\n \n     The four conditions are evaluated IN ORDER, each failure recorded as a\n     distinct reason string:\ndiff --git a/scripts/shiki_guardian.py b/scripts/shiki_guardian.py\nindex a4ecb79bbda55fd5c8b06adfae2eb99760a03c45..4dd99bbe714e26f421fa19abac7a34ae1b195ecc\n--- a/scripts/shiki_guardian.py\n+++ b/scripts/shiki_guardian.py\n@@ -12,7 +12,7 @@\n # The carry re-derives its proof from git in-process on every judging invocation;\n # it is never read from an artifact, so there is nothing to go stale or be forged.\n # shiki_sync_proof is the ONLY guardian-family module that runs git, and every one\n-# of its invocations is read-only and local (ADR: prove-from-git base sync).\n+# of its invocations is read-only and local (SADR: prove-from-git base sync).\n from shiki_sync_proof import verify_pure_base_sync\n \n GUARDIAN_POLICY_PATH = \".shiki/guardian-policy.json\"\ndiff --git a/scripts/shiki_guardian_review.py b/scripts/shiki_guardian_review.py\nindex dd0f104d457899e0ba5ff1e162011c1e90b6f92b..e10ef58c046324e7ea3a3392feb3048ca77414ce\n--- a/scripts/shiki_guardian_review.py\n+++ b/scripts/shiki_guardian_review.py\n@@ -159,7 +159,7 @@\n     docs_only = _is_docs_only(changed_files)\n     if docs_only and matched == 0:\n         focus.append(\n-            \"Docs/ADR-only change: confirm the change is genuinely documentation/decision-only, introduces no behavior or contract drift, and stays consistent with the constitution and existing ADRs.\"\n+            \"Docs/decision-record-only change: confirm the change is genuinely documentation/decision-only, introduces no behavior or contract drift, and stays consistent with the constitution and existing SADRs and target ADRs.\"\n         )\n \n     # Conservative augmentation: unknown (no category and not docs-only), mixed\ndiff --git a/scripts/shiki_installer.py b/scripts/shiki_installer.py\nindex be064ab7a29ff6836f7f7a1d70500a43e0d11d7b..9fd8eefd137ce7003dad5c0dcb2e48e2cd47ab32\n--- a/scripts/shiki_installer.py\n+++ b/scripts/shiki_installer.py\n@@ -118,6 +118,7 @@\n # what lets a target carry it without a governance-schema change.\n INSTALL_STAMP_PATH = \".shiki/install-stamp.json\"\n INSTALL_STAMP_VERSION = 1\n+LEGACY_SHIKI_SADR_MAX = 18\n \n # Under --force the shipped surface splits three ways, so nothing is lost and\n # nothing is silently stale.\n@@ -237,9 +238,98 @@\n         )\n \n \n+def legacy_shiki_adr_paths() -> tuple[str, ...]:\n+    \"\"\"Former numeric paths for Shiki SADR-0001 through SADR-0018.\n+\n+    The authoritative slugs come from the current SADR filenames. SADR-0019 is\n+    the namespace decision itself and never shipped under a numeric legacy path.\n+    \"\"\"\n+    paths: list[str] = []\n+    for sadr in sorted((ROOT / \"docs\" / \"adr\").glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\")):\n+        number = sadr.name.split(\"-\", 2)[1]\n+        if int(number) <= LEGACY_SHIKI_SADR_MAX:\n+            paths.append(f\"docs/adr/{sadr.name.removeprefix('SADR-')}\")\n+    return tuple(paths)\n+\n+\n+def preflight_legacy_sadr_cleanup(target: Path, *, force: bool) -> tuple[str, ...]:\n+    \"\"\"Authorize exact legacy Shiki ADR removals before the first write.\n+\n+    A non-force install never removes existing files. A forced install needs no\n+    stamp when no legacy Shiki path is present. When legacy paths do exist, each\n+    must be named in a readable install stamp and its current bytes must match\n+    the recorded digest.\n+    \"\"\"\n+    if not force:\n+        return ()\n+    present = tuple(\n+        relative\n+        for relative in legacy_shiki_adr_paths()\n+        if (target / relative).exists() or (target / relative).is_symlink()\n+    )\n+    if not present:\n+        return ()\n+\n+    stamp_path = target / INSTALL_STAMP_PATH\n+    try:\n+        raw = stamp_path.read_text(encoding=\"utf-8\")\n+        stamp = json.loads(raw)\n+    except FileNotFoundError:\n+        detail = \"install stamp is absent\"\n+        blockers = [f\"{relative}: {detail}\" for relative in present]\n+    except (OSError, UnicodeDecodeError) as error:\n+        detail = f\"install stamp is unreadable ({error})\"\n+        blockers = [f\"{relative}: {detail}\" for relative in present]\n+    except json.JSONDecodeError as error:\n+        detail = f\"install stamp is malformed ({error})\"\n+        blockers = [f\"{relative}: {detail}\" for relative in present]\n+    else:\n+        if not isinstance(stamp, dict):\n+            blockers = [\n+                f\"{relative}: install stamp is malformed (expected an object)\"\n+                for relative in present\n+            ]\n+        else:\n+            digests = stamp.get(\"digests\")\n+            blockers = []\n+            if not isinstance(digests, dict):\n+                blockers.extend(\n+                    f\"{relative}: install stamp has no digest for this path\"\n+                    for relative in present\n+                )\n+            else:\n+                for relative in present:\n+                    expected = digests.get(relative)\n+                    if not isinstance(expected, str) or not expected:\n+                        blockers.append(\n+                            f\"{relative}: install stamp has no digest for this path\"\n+                        )\n+                        continue\n+                    try:\n+                        actual = _sha256_file(target / relative)\n+                    except OSError as error:\n+                        blockers.append(\n+                            f\"{relative}: legacy path is unreadable ({error})\"\n+                        )\n+                        continue\n+                    if actual != expected:\n+                        blockers.append(\n+                            f\"{relative}: digest mismatch (stamp {expected}, current {actual})\"\n+                        )\n+\n+    if blockers:\n+        joined = \"\\n\".join(f\"- {blocker}\" for blocker in blockers)\n+        raise ShikiError(\n+            \"legacy Shiki ADR cleanup is not authorized:\\n\"\n+            f\"{joined}\\nNo files were written.\"\n+        )\n+    return present\n+\n+\n def install_template(target: Path, *, force: bool, validate: bool) -> list[NewFileNote]:\n     # Preconditions first: never rewrite the tree of a target that cannot be\n     # upgraded cleanly (see refuse_pending_migrations).\n+    legacy_cleanup = preflight_legacy_sadr_cleanup(target, force=force)\n     refuse_pending_migrations(target)\n \n     # Resolve the target's required CODEOWNERS owner once from its\n@@ -250,6 +340,10 @@\n     # verbatim. See ``codeowners_required_owner``.\n     codeowners_owner = codeowners_required_owner(target)\n \n+    for relative in legacy_cleanup:\n+        (target / relative).unlink()\n+        info(f\"removed stamped legacy Shiki ADR: {target / relative}\")\n+\n     notes: list[NewFileNote] = []\n     for relative in TEMPLATE_PATHS:\n         source = ROOT / relative\n@@ -647,6 +741,22 @@\n     digests = target_stamp_digests(target)\n     existing = read_install_stamp(target)\n     if existing is not None:\n+        # A non-force install deliberately leaves legacy Shiki ADRs in place.\n+        # Preserve only still-matching ownership proof so a later forced\n+        # upgrade can authorize cleanup; never carry a missing or stale digest.\n+        existing_digests = existing.get(\"digests\")\n+        if isinstance(existing_digests, dict):\n+            for relative in legacy_shiki_adr_paths():\n+                expected = existing_digests.get(relative)\n+                path = target / relative\n+                if not isinstance(expected, str) or not path.is_file():\n+                    continue\n+                try:\n+                    actual = _sha256_file(path)\n+                except OSError:\n+                    continue\n+                if actual == expected:\n+                    digests[relative] = actual\n         if _incoming_is_older(commit, existing.get(\"platform_commit\")):\n             warn(\n                 \"kept existing install stamp: incoming install \"\ndiff --git a/scripts/shiki_loop.py b/scripts/shiki_loop.py\nindex 3611641da8aeb5038b1da90e3114b09a473ec6d8..84caa491b9cad0edc4176f209c13e9221e975681\n--- a/scripts/shiki_loop.py\n+++ b/scripts/shiki_loop.py\n@@ -424,7 +424,7 @@\n         # unresolvable CCA verdict has already produced its terminal stop and is never\n         # laundered into a head-moving sync (SADR-0010). It also never fires while the\n         # policy (Guardian) gate is red \u2014 for ANY reason, not only when it is the sole\n-        # red check \u2014 so a head-bound Guardian approval, or an SADR-0015\n+        # red check \u2014 so a head-bound Guardian approval, or a SADR-0015\n         # contract-approval carry that base movement defeated (the policy gate compares\n         # base and head task governance directly), is never silently restored by\n         # adopting base wholesale. It is bounded by a durable per-task counter so the\ndiff --git a/scripts/shiki_tasks.py b/scripts/shiki_tasks.py\nindex aec3809f24b4a0c8c96fc50971cff9ae9ad30381..d531052e9d9717058d4631483cefab44c26a65c1\n--- a/scripts/shiki_tasks.py\n+++ b/scripts/shiki_tasks.py\n@@ -821,7 +821,7 @@\n         \"prompt\": prompt,\n         \"entry_skill\": \"grill-with-docs\",\n         \"required_next_steps\": [\n-            \"Run grill-with-docs until terminology, boundaries, risks, and ADR-worthy decisions are settled.\",\n+            \"Run grill-with-docs until terminology, boundaries, risks, and decision-record-worthy choices are settled.\",\n             \"Obtain the operator's explicit approval of the PRD/requirements (Spec Freeze).\",\n             \"Write a machine-readable plan JSON with grill_with_docs.status=complete and spec_freeze.status=frozen.\",\n             \"Run shiki plan ingest --plan-file PLAN.json.\",\ndiff --git a/scripts/test_shiki_goal_loop.sh b/scripts/test_shiki_goal_loop.sh\nindex 753c7b516e10f0be3bc92a4bd0eb1fa699776ae6..8f3820a0a079b3044424cfe1a6c6b7fe68896e67\n--- a/scripts/test_shiki_goal_loop.sh\n+++ b/scripts/test_shiki_goal_loop.sh\n@@ -231,8 +231,8 @@\n \n # High/critical risk merges autonomously when all required checks are green:\n # the \"MergeGate policy check\" required check IS the Guardian gate (it enforces\n-# guardian-policy.json \u2014 human approval OR external AI guardian review, ADR\n-# 0010), so green checks mean a recorded authority approved. The fake gh returns\n+# guardian-policy.json \u2014 human approval OR external AI guardian review,\n+# SADR-0010), so green checks mean a recorded authority approved. The fake gh returns\n # green checks, simulating that the Guardian gate passed.\n python3 \"$ROOT/scripts/shiki.py\" goal create --target \"$TARGET\" --title \"Guardian gate\" --outcome \"High risk merges via policy gate\" >/tmp/shiki-goal-loop-guardian-goal.json\n GGOAL=\"$(json_get /tmp/shiki-goal-loop-guardian-goal.json goal_id)\"\ndiff --git a/tests/test_adr_references.py b/tests/test_adr_references.py\nindex 51706329c797d5dd9b47e082a333fa83b261d482..db6e693e35f67103935be34f1d9bb4ff7ee60395\n--- a/tests/test_adr_references.py\n+++ b/tests/test_adr_references.py\n@@ -1,7 +1,7 @@\n-\"\"\"Every ``ADR ####`` citation must resolve to exactly one decision record.\n+\"\"\"Every target ADR and Shiki SADR citation resolves in its own namespace.\n \n Background. ``scripts/mergegate_check.py`` justifies ``is_bookkeeping_closeout``\n-with \"ADR 0017\" nine times and ``scripts/guardian_approval_signal.py`` cites\n+with \"SADR-0017\" nine times and ``scripts/guardian_approval_signal.py`` cites\n 0016/0017 seven times, yet for a stretch the numbering in ``docs/adr/`` stopped\n at 0015: the authoring commits lived on a coordinator branch and no task ever\n declared ``docs/adr/...`` in its locks, so nothing carried the records to the\n@@ -11,11 +11,12 @@\n and the directory drifted with no signal.\n \n This is the same unbound-pair shape behind other silent failures: a reference\n-and its target with nothing asserting they agree. This test binds them. It scans\n-every ``scripts/*.py`` (plus ``docs/**/*.md`` and the root ``*.md`` governance\n-files, which are cheap) for the ``ADR ####`` pattern and asserts each cited\n-number resolves to exactly one ``docs/adr/####-*.md``. The converse is\n-deliberately NOT required: an ADR may exist without being cited.\n+and its target with nothing asserting they agree. This test binds them. Target\n+``ADR NNNN`` citations resolve only to ``docs/adr/NNNN-*.md``; Shiki\n+``SADR-NNNN`` citations resolve only to ``docs/adr/SADR-NNNN-*.md``. The scan\n+covers current governance docs, scripts, workflows, prompts, packaging metadata,\n+and repo-local Codex guidance while deliberately excluding historical\n+``.shiki`` evidence. The converse is not required: a record may be uncited.\n \n The suite is dependency free: standard-library ``unittest`` only.\n \"\"\"\n@@ -31,17 +32,18 @@\n \n REPO_ROOT = Path(__file__).resolve().parents[1]\n \n-# \"ADR 0016\" -- a four-digit citation. The trailing \\b keeps a five-digit token\n-# from being read as a truncated four-digit number.\n-_CITATION = re.compile(r\"\\bADR (\\d{4})\\b\")\n-# \"0016-locks-bound-product-paths.md\" -- the leading four digits name the record.\n-_ADR_FILENAME = re.compile(r\"^(\\d{4})-.*\\.md$\")\n+# ``ADR 0016`` / ``SADR-0016`` -- a namespace plus four-digit citation. The\n+# trailing ``\\b`` keeps a five-digit token from being read as a truncated one.\n+_CITATION = re.compile(r\"\\b(S?ADR)[ -](\\d{4})\\b\")\n+# Target ADRs retain the numeric filename; Shiki platform decisions use SADR.\n+_DECISION_FILENAME = re.compile(r\"^(?:(SADR)-)?(\\d{4})-.*\\.md$\")\n \n \n class Violation(NamedTuple):\n     \"\"\"A citation that does not resolve to exactly one decision record.\"\"\"\n \n     citing_file: str  # repo-relative path of the file that cites the ADR\n+    namespace: str  # \"ADR\" for target decisions; \"SADR\" for Shiki decisions\n     number: str  # the cited four-digit number, e.g. \"0016\"\n     line: int  # 1-based line of the citation\n     reason: str  # \"missing\" (zero matches) or \"ambiguous\" (two or more)\n@@ -49,44 +51,59 @@\n \n     @property\n     def expected_path(self) -> str:\n-        return f\"docs/adr/{self.number}-*.md\"\n+        prefix = \"SADR-\" if self.namespace == \"SADR\" else \"\"\n+        return f\"docs/adr/{prefix}{self.number}-*.md\"\n \n     def describe(self) -> str:\n         \"\"\"Name the citing file, the cited number, and the missing path.\"\"\"\n         if self.reason == \"missing\":\n             return (\n-                f\"{self.citing_file}:{self.line} cites ADR {self.number} \"\n+                f\"{self.citing_file}:{self.line} cites {self.namespace} {self.number} \"\n                 f\"but no document matches {self.expected_path}\"\n             )\n         joined = \", \".join(self.matches)\n         return (\n-            f\"{self.citing_file}:{self.line} cites ADR {self.number} which \"\n+            f\"{self.citing_file}:{self.line} cites {self.namespace} {self.number} which \"\n             f\"resolves to {len(self.matches)} documents (expected exactly one \"\n             f\"at {self.expected_path}): {joined}\"\n         )\n \n \n-def _adr_index(root: Path) -> dict[str, list[Path]]:\n-    \"\"\"Map every four-digit ADR number to the record files that claim it.\"\"\"\n-    index: dict[str, list[Path]] = {}\n+def _adr_index(root: Path) -> dict[tuple[str, str], list[Path]]:\n+    \"\"\"Map each decision namespace and number to the files that claim it.\"\"\"\n+    index: dict[tuple[str, str], list[Path]] = {}\n     adr_dir = root / \"docs\" / \"adr\"\n     if adr_dir.is_dir():\n         for path in sorted(adr_dir.glob(\"*.md\")):\n-            match = _ADR_FILENAME.match(path.name)\n+            match = _DECISION_FILENAME.match(path.name)\n             if match:\n-                index.setdefault(match.group(1), []).append(path)\n+                namespace = \"SADR\" if match.group(1) else \"ADR\"\n+                index.setdefault((namespace, match.group(2)), []).append(path)\n     return index\n \n \n def _citing_files(root: Path, include_docs: bool = True) -> list[Path]:\n-    \"\"\"Files scanned for citations: scripts/*.py, and cheaply the markdown.\"\"\"\n+    \"\"\"Current, shipped text surfaces scanned for decision citations.\"\"\"\n     files = sorted((root / \"scripts\").glob(\"*.py\"))\n+    files += sorted((root / \"scripts\").glob(\"*.sh\"))\n     if include_docs:\n         docs = root / \"docs\"\n         if docs.is_dir():\n             files += sorted(docs.rglob(\"*.md\"))\n         files += sorted(root.glob(\"*.md\"))\n-    return files\n+        pyproject = root / \"pyproject.toml\"\n+        if pyproject.is_file():\n+            files.append(pyproject)\n+        for relative in (\".github\", \".codex\"):\n+            surface = root / relative\n+            if surface.is_dir():\n+                files += sorted(\n+                    path\n+                    for path in surface.rglob(\"*\")\n+                    if path.is_file()\n+                    and path.suffix in {\".md\", \".py\", \".sh\", \".toml\", \".yaml\", \".yml\"}\n+                )\n+    return sorted(set(files))\n \n \n def find_adr_reference_violations(\n@@ -94,10 +111,9 @@\n ) -> list[Violation]:\n     \"\"\"Return every ``ADR ####`` citation under ``root`` that fails to resolve.\n \n-    A citation resolves when exactly one ``docs/adr/####-*.md`` matches its\n-    number. Zero matches is a \"missing\" violation; two or more is \"ambiguous\".\n-    Records that are never cited produce nothing -- the converse is not\n-    required.\n+    A citation resolves when exactly one record in the same namespace matches\n+    its number. Zero matches is a \"missing\" violation; two or more is\n+    \"ambiguous\". Records that are never cited produce nothing.\n     \"\"\"\n     root = Path(root)\n     index = _adr_index(root)\n@@ -110,13 +126,15 @@\n         rel = path.relative_to(root).as_posix()\n         for lineno, line in enumerate(text.splitlines(), start=1):\n             for match in _CITATION.finditer(line):\n-                number = match.group(1)\n-                found = index.get(number, [])\n+                namespace, number = match.groups()\n+                found = index.get((namespace, number), [])\n                 if len(found) == 1:\n                     continue\n                 reason = \"missing\" if not found else \"ambiguous\"\n                 rels = [p.relative_to(root).as_posix() for p in found]\n-                violations.append(Violation(rel, number, lineno, reason, rels))\n+                violations.append(\n+                    Violation(rel, namespace, number, lineno, reason, rels)\n+                )\n     return violations\n \n \n@@ -129,8 +147,8 @@\n         self.assertEqual(\n             violations,\n             [],\n-            \"Every 'ADR ####' citation must resolve to exactly one \"\n-            f\"docs/adr/####-*.md:\\n{detail}\",\n+            \"Every ADR/SADR citation must resolve exactly once in its own \"\n+            f\"docs/adr namespace:\\n{detail}\",\n         )\n \n     def test_scan_actually_reads_scripts(self) -> None:\n@@ -146,8 +164,21 @@\n         # The two records this Goal carried onto the default branch, and which\n         # the scripts cite most, must be present.\n         index = _adr_index(REPO_ROOT)\n-        self.assertIn(\"0016\", index)\n-        self.assertIn(\"0017\", index)\n+        self.assertIn((\"SADR\", \"0016\"), index)\n+        self.assertIn((\"SADR\", \"0017\"), index)\n+\n+    def test_platform_tree_has_only_sadr_0001_through_0019(self) -> None:\n+        adr_dir = REPO_ROOT / \"docs\" / \"adr\"\n+        sadrs = sorted(adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\"))\n+        self.assertEqual(\n+            {path.name.split(\"-\", 2)[1] for path in sadrs},\n+            {f\"{number:04d}\" for number in range(1, 20)},\n+        )\n+        self.assertEqual(\n+            list(adr_dir.glob(\"[0-9][0-9][0-9][0-9]-*.md\")),\n+            [],\n+            \"the Shiki platform tree must not retain legacy numeric SADR paths\",\n+        )\n \n \n class FixtureAdrReferences(unittest.TestCase):\n@@ -180,6 +211,26 @@\n         self.assertIn(\"9991\", message)  # cited number\n         self.assertIn(\"docs/adr/9991-*.md\", message)  # missing path\n \n+    def test_target_adr_does_not_satisfy_missing_platform_sadr(self) -> None:\n+        root = self._make_repo(\n+            adr_names=[\"0042-target-decision.md\"],\n+            script_body='\"\"\"Target ADR 0042 exists, but platform SADR-0042 does not.\"\"\"\\n',\n+        )\n+        violations = find_adr_reference_violations(root, include_docs=False)\n+        self.assertEqual(len(violations), 1)\n+        self.assertEqual(violations[0].namespace, \"SADR\")\n+        self.assertEqual(violations[0].number, \"0042\")\n+        self.assertEqual(violations[0].expected_path, \"docs/adr/SADR-0042-*.md\")\n+\n+    def test_target_adr_and_platform_sadr_may_reuse_a_number(self) -> None:\n+        root = self._make_repo(\n+            adr_names=[\"0042-target-decision.md\", \"SADR-0042-platform-decision.md\"],\n+            script_body='\"\"\"Target ADR 0042 and platform SADR-0042 are distinct.\"\"\"\\n',\n+        )\n+        self.assertEqual(\n+            find_adr_reference_violations(root, include_docs=False), []\n+        )\n+\n     def test_uncited_adr_is_allowed(self) -> None:\n         # An ADR exists but nothing cites it -> not a violation. The converse\n         # (every ADR must be cited) is explicitly NOT required.\ndiff --git a/tests/test_guardian_review.py b/tests/test_guardian_review.py\nindex 88829e7810212635e2fc98aa5918d6f26294677d..00a85a237acd5bf2d6cb5b294128198c422402ca\n--- a/tests/test_guardian_review.py\n+++ b/tests/test_guardian_review.py\n@@ -162,7 +162,7 @@\n \n     def test_docs_only_is_light_and_not_conservative(self):\n         focus = gr.classify_review_focus([\"docs/adr/0099-x.md\", \"README.md\"], \"\", \"low\")\n-        self.assertTrue(any(\"Docs/ADR-only\" in f for f in focus))\n+        self.assertTrue(any(\"Docs/decision-record-only\" in f for f in focus))\n         self.assertFalse(any(\"conservative default\" in f.lower() for f in focus))\n \n     def test_unknown_category_is_conservative(self):\ndiff --git a/tests/test_policy_job_merged_prs.py b/tests/test_policy_job_merged_prs.py\nindex d45512e74681e5e649279236c7d4e79d5c3da471..b97989f8bd74d091de1a6b5275a20d8dc68c4512\n--- a/tests/test_policy_job_merged_prs.py\n+++ b/tests/test_policy_job_merged_prs.py\n@@ -3,8 +3,8 @@\n \n ``.github/workflows/shiki-cca-completion.yml`` has two consumers of the merged-PR\n proof: the CCA *signal* (``guardian_approval_signal.py`` in \"Prepare CCA context\")\n-and the authoritative gate (``mergegate_check.py`` in \"Enforce MergeGate\"). ADR\n-0017's bookkeeping-closeout exemption (condition 6) needs the merged implementation\n+and the authoritative gate (``mergegate_check.py`` in \"Enforce MergeGate\").\n+SADR-0017's bookkeeping-closeout exemption (condition 6) needs the merged implementation\n PR proven, and a closeout carries NO ``post_merge_reconcile`` marker. The signal\n already resolves ``MERGED_PRS`` unconditionally, but \"Enforce MergeGate\" used to\n resolve it only inside ``grep -q '<!-- shiki:post_merge_reconcile -->'``. So a\ndiff --git a/tests/test_sadr_install_migration.py b/tests/test_sadr_install_migration.py\nnew file mode 100644\nindex 0000000000000000000000000000000000000000..a6f4c57e4dcaef73ef600b0d4804744138ab9071\n--- /dev/null\n+++ b/tests/test_sadr_install_migration.py\n@@ -0,0 +1,229 @@\n+\"\"\"SADR installation and collision-safe legacy upgrade behavior.\"\"\"\n+\n+from __future__ import annotations\n+\n+import contextlib\n+import hashlib\n+import io\n+import json\n+import shutil\n+import tempfile\n+import unittest\n+from pathlib import Path\n+\n+import shiki_test_support  # noqa: F401  (path bootstrap)\n+\n+import shiki_installer\n+from tests.test_adr_references import find_adr_reference_violations\n+\n+\n+PRODUCT_NUMBERS = tuple(range(13, 19))\n+\n+\n+def _quiet_install(target: Path, *, force: bool, validate: bool) -> None:\n+    with contextlib.redirect_stdout(io.StringIO()):\n+        shiki_installer.install_template(target, force=force, validate=validate)\n+\n+\n+def _sha256(path: Path) -> str:\n+    return hashlib.sha256(path.read_bytes()).hexdigest()\n+\n+\n+def _legacy_sadr_paths() -> tuple[str, ...]:\n+    return tuple(\n+        f\"docs/adr/{path.name.removeprefix('SADR-')}\"\n+        for path in sorted((shiki_installer.ROOT / \"docs\" / \"adr\").glob(\"SADR-*.md\"))\n+        if path.name != \"SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\"\n+    )\n+\n+\n+def _create_legacy_upgrade_fixture(\n+    target: Path,\n+) -> tuple[dict[str, bytes], tuple[str, ...]]:\n+    adr_dir = target / \"docs\" / \"adr\"\n+    adr_dir.mkdir(parents=True)\n+    product_bytes = {\n+        f\"{number:04d}-cubeshare-product-decision.md\": (\n+            f\"# CubeShare ADR {number:04d}\\n\\nDo not rewrite {number}.\\n\"\n+        ).encode()\n+        for number in PRODUCT_NUMBERS\n+    }\n+    for name, content in product_bytes.items():\n+        (adr_dir / name).write_bytes(content)\n+\n+    legacy_paths = _legacy_sadr_paths()\n+    for relative in legacy_paths:\n+        (target / relative).write_bytes(f\"legacy Shiki bytes for {relative}\\n\".encode())\n+\n+    stamp_path = target / shiki_installer.INSTALL_STAMP_PATH\n+    stamp_path.parent.mkdir(parents=True, exist_ok=True)\n+    stamp = {\n+        \"version\": shiki_installer.INSTALL_STAMP_VERSION,\n+        \"platform_commit\": None,\n+        \"installed_at\": \"2000-01-01T00:00:00+00:00\",\n+        \"digests\": {\n+            relative: _sha256(target / relative) for relative in legacy_paths\n+        },\n+    }\n+    stamp_path.write_text(\n+        json.dumps(stamp, indent=2, sort_keys=True) + \"\\n\", encoding=\"utf-8\"\n+    )\n+    return product_bytes, legacy_paths\n+\n+\n+def _tree_snapshot(root: Path) -> dict[str, tuple[str, bytes | str]]:\n+    \"\"\"Recursive byte/shape snapshot used to prove preflight is write-free.\"\"\"\n+    snapshot: dict[str, tuple[str, bytes | str]] = {}\n+    for path in sorted(root.rglob(\"*\")):\n+        relative = path.relative_to(root).as_posix()\n+        if path.is_symlink():\n+            snapshot[relative] = (\"symlink\", path.readlink().as_posix())\n+        elif path.is_dir():\n+            snapshot[relative] = (\"directory\", \"\")\n+        else:\n+            snapshot[relative] = (\"file\", path.read_bytes())\n+    return snapshot\n+\n+\n+class FreshSadrInstallTests(unittest.TestCase):\n+    def setUp(self) -> None:\n+        self.tmp = Path(tempfile.mkdtemp(prefix=\"shiki-sadr-fresh-\"))\n+        self.addCleanup(lambda: shutil.rmtree(self.tmp, ignore_errors=True))\n+        self.target = self.tmp / \"target\"\n+        self.adr_dir = self.target / \"docs\" / \"adr\"\n+        self.adr_dir.mkdir(parents=True)\n+        self.product_bytes = {\n+            f\"{number:04d}-product-decision.md\": (\n+                f\"# Product ADR {number:04d}\\n\\nTarget-owned bytes {number}.\\n\"\n+            ).encode()\n+            for number in PRODUCT_NUMBERS\n+        }\n+        for name, content in self.product_bytes.items():\n+            (self.adr_dir / name).write_bytes(content)\n+\n+    def test_fresh_install_preserves_product_adrs_and_installs_all_sadrs(self) -> None:\n+        _quiet_install(self.target, force=False, validate=True)\n+\n+        for name, expected in self.product_bytes.items():\n+            self.assertEqual((self.adr_dir / name).read_bytes(), expected)\n+\n+        sadr_numbers = {\n+            path.name.split(\"-\", 2)[1]\n+            for path in self.adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\")\n+        }\n+        self.assertEqual(sadr_numbers, {f\"{number:04d}\" for number in range(1, 20)})\n+\n+        numeric_records = {\n+            path.name\n+            for path in self.adr_dir.glob(\"[0-9][0-9][0-9][0-9]-*.md\")\n+        }\n+        self.assertEqual(numeric_records, set(self.product_bytes))\n+        self.assertEqual(find_adr_reference_violations(self.target), [])\n+\n+\n+class LegacySadrUpgradeTests(unittest.TestCase):\n+    def setUp(self) -> None:\n+        self.tmp = Path(tempfile.mkdtemp(prefix=\"shiki-sadr-upgrade-\"))\n+        self.addCleanup(lambda: shutil.rmtree(self.tmp, ignore_errors=True))\n+        self.target = self.tmp / \"target\"\n+        self.adr_dir = self.target / \"docs\" / \"adr\"\n+        self.product_bytes, self.legacy_paths = _create_legacy_upgrade_fixture(\n+            self.target\n+        )\n+\n+    def test_force_upgrade_deletes_only_stamped_legacy_sadrs(self) -> None:\n+        _quiet_install(self.target, force=True, validate=True)\n+\n+        for relative in self.legacy_paths:\n+            self.assertFalse((self.target / relative).exists(), relative)\n+        for name, expected in self.product_bytes.items():\n+            self.assertEqual((self.adr_dir / name).read_bytes(), expected)\n+\n+        sadrs = list(self.adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\"))\n+        self.assertEqual(len(sadrs), 19)\n+        refreshed = json.loads(\n+            (self.target / shiki_installer.INSTALL_STAMP_PATH).read_text(encoding=\"utf-8\")\n+        )\n+        for relative in self.legacy_paths:\n+            self.assertNotIn(relative, refreshed[\"digests\"])\n+        for path in sadrs:\n+            relative = path.relative_to(self.target).as_posix()\n+            self.assertEqual(refreshed[\"digests\"][relative], _sha256(path))\n+\n+    def test_non_force_install_never_deletes_legacy_files(self) -> None:\n+        before = {\n+            relative: (self.target / relative).read_bytes()\n+            for relative in self.legacy_paths\n+        }\n+        _quiet_install(self.target, force=False, validate=False)\n+        for relative, expected in before.items():\n+            self.assertEqual((self.target / relative).read_bytes(), expected)\n+        refreshed = json.loads(\n+            (self.target / shiki_installer.INSTALL_STAMP_PATH).read_text(encoding=\"utf-8\")\n+        )\n+        for relative in self.legacy_paths:\n+            self.assertEqual(refreshed[\"digests\"][relative], _sha256(self.target / relative))\n+\n+\n+class LegacyCleanupPreflightTests(unittest.TestCase):\n+    def setUp(self) -> None:\n+        self.tmp = Path(tempfile.mkdtemp(prefix=\"shiki-sadr-preflight-\"))\n+        self.addCleanup(lambda: shutil.rmtree(self.tmp, ignore_errors=True))\n+\n+    def test_all_ownership_blockers_abort_before_any_write(self) -> None:\n+        cases = (\n+            (\"absent\", \"install stamp is absent\"),\n+            (\"unreadable\", \"install stamp is unreadable\"),\n+            (\"malformed\", \"install stamp is malformed\"),\n+            (\"missing-digest\", \"install stamp has no digest\"),\n+            (\"digest-mismatch\", \"digest mismatch\"),\n+        )\n+        for name, expected_reason in cases:\n+            with self.subTest(name=name):\n+                target = self.tmp / name\n+                _, legacy_paths = _create_legacy_upgrade_fixture(target)\n+                stamp_path = target / shiki_installer.INSTALL_STAMP_PATH\n+\n+                if name == \"absent\":\n+                    stamp_path.unlink()\n+                elif name == \"unreadable\":\n+                    stamp_path.unlink()\n+                    stamp_path.mkdir()\n+                elif name == \"malformed\":\n+                    stamp_path.write_text(\"{not-json\\n\", encoding=\"utf-8\")\n+                else:\n+                    stamp = json.loads(stamp_path.read_text(encoding=\"utf-8\"))\n+                    if name == \"missing-digest\":\n+                        stamp[\"digests\"] = {}\n+                    else:\n+                        stamp[\"digests\"] = {\n+                            relative: \"deadbeef\" for relative in legacy_paths\n+                        }\n+                    stamp_path.write_text(\n+                        json.dumps(stamp, indent=2, sort_keys=True) + \"\\n\",\n+                        encoding=\"utf-8\",\n+                    )\n+\n+                before = _tree_snapshot(target)\n+                with self.assertRaises(shiki_installer.ShikiError) as raised:\n+                    _quiet_install(target, force=True, validate=False)\n+                after = _tree_snapshot(target)\n+\n+                self.assertEqual(before, after, f\"{name} preflight changed the tree\")\n+                message = str(raised.exception)\n+                self.assertIn(expected_reason, message)\n+                self.assertIn(\"No files were written\", message)\n+                for relative in legacy_paths:\n+                    self.assertIn(relative, message)\n+\n+    def test_absent_stamp_is_allowed_when_no_legacy_path_exists(self) -> None:\n+        target = self.tmp / \"no-legacy\"\n+        target.mkdir()\n+        _quiet_install(target, force=True, validate=False)\n+        self.assertTrue(\n+            (target / \"docs\" / \"adr\" / \"SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\").is_file()\n+        )\n+\n+\n+if __name__ == \"__main__\":\n+    unittest.main()\n\n2026-08-07T08:17:47.650186Z ERROR codex_models_manager::manager: failed to renew cache TTL: missing field `base_instructions` at line 94 column 5\ncodex\nThe broad unit run is still active rather than failing fast; the earlier non-quiet pass completed its progress stream without errors, and I\u2019m waiting for the quiet rerun\u2019s final exit/count so I don\u2019t overstate the result.\ndiff --git a/.codex/skills/shiki/SKILL.md b/.codex/skills/shiki/SKILL.md\nindex c8e2e85d9d75d49cc4f2d6286ee22659c4e53e75..93cded623b2c15d9f65c76491a1c2d813ac9e6c0\n--- a/.codex/skills/shiki/SKILL.md\n+++ b/.codex/skills/shiki/SKILL.md\n@@ -18,6 +18,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n Codex CLI does not register this skill as a custom `/shiki` slash command. When\n the user types `/shiki` in Codex CLI and gets \"Unrecognized command\", explain\n that this is expected: invoke Shiki in Codex with natural language such as\n@@ -69,7 +72,7 @@\n \n ## Responsibilities\n \n-- Claude Code implements and repairs by default (`shiki runner claude`, ADR 0008); Codex implements and repairs tasks explicitly assigned to `codex`.\n+- Claude Code implements and repairs by default (`shiki runner claude`, SADR-0008); Codex implements and repairs tasks explicitly assigned to `codex`.\n - Claude Code Action can act as GitHub-side CCA or reviewer.\n - CCA judges completion.\n - MergeGate authorizes state transitions and merge readiness.\ndiff --git a/.github/prompts/cca-completion-check.md b/.github/prompts/cca-completion-check.md\nindex ed203ad5f302ecc81372d8ab27d2b1232ca97497..fd89950528333ef6242e6d5443904f5392ab7e56\n--- a/.github/prompts/cca-completion-check.md\n+++ b/.github/prompts/cca-completion-check.md\n@@ -19,6 +19,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n ## Required Reading\n \n Read only the files needed to judge the PR contract. Prefer this order:\ndiff --git a/.github/prompts/codex-task-handoff.md b/.github/prompts/codex-task-handoff.md\nindex 695e211d76dea1c60e8d5bb360e78487808286a4..59441d576b01a39569722f226c854e5a740e9eb5\n--- a/.github/prompts/codex-task-handoff.md\n+++ b/.github/prompts/codex-task-handoff.md\n@@ -2,7 +2,7 @@\n \n You are Codex Front acting as the Implementer for a Shiki task.\n \n-Follow `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant ADRs, and `docs/agents/codex-handoff.md`.\n+Follow `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant Shiki SADRs and target ADRs, and `docs/agents/codex-handoff.md`.\n \n Rules:\n \ndiff --git a/AGENTS.md b/AGENTS.md\nindex 7f041100d49c2a7525f51cb0f5d696a4a6d1c78c..a06bfbabc6fa48a007389e25e12d7ed523936bed\n--- a/AGENTS.md\n+++ b/AGENTS.md\n@@ -22,7 +22,7 @@\n - **Codex Front** is an optional implementer through Codex App, Codex CLI, Codex IDE extension, or Codex Web signed in with ChatGPT OAuth/subscription auth, dispatched with `shiki runner codex` when a task is explicitly assigned to `codex`.\n - **GitHub CCA** is implemented by Claude Code Action by default, using `CLAUDE_CODE_OAUTH_TOKEN`.\n \n-Do not assume `openai/codex-action`, `OPENAI_API_KEY`, or API-key based Codex automation in the default Shiki path. API-key runners are explicit target-repo extensions and require their own ADR.\n+Do not assume `openai/codex-action`, `OPENAI_API_KEY`, or API-key based Codex automation in the default Shiki path. API-key runners are explicit target-repo extensions and require their own target ADR.\n \n Shiki is not:\n \n@@ -44,10 +44,14 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use\n+`SADR-NNNN-*.md`; target product decisions retain `NNNN-*.md` and the `ADR`\n+namespace.\n+\n Use the canonical source-of-truth order above. Runtime-specific reading orders\n are operational guidance only and must not override or reorder it.\n \n-Conversation state is not durable truth. If a decision matters, put it in GitHub, `.shiki/`, `CONTEXT.md`, or an ADR.\n+Conversation state is not durable truth. If a decision matters, put it in GitHub, `.shiki/`, `CONTEXT.md`, a Shiki SADR, or a target ADR.\n \n If GitHub and `.shiki/` disagree, surface the conflict and prefer GitHub operational state until the mirror is repaired.\n \n@@ -75,8 +79,8 @@\n Every non-trivial change follows this loop:\n \n 1. **Goal**: clarify outcome, completion conditions, non-goals, risk level, and success signals.\n-2. **grill-with-docs**: challenge the plan against domain docs, ADRs, code reality, terminology, and edge scenarios.\n-3. **Context & Impact**: find relevant docs, ADRs, code, dependencies, owners, locks, and verification surfaces.\n+2. **grill-with-docs**: challenge the plan against domain docs, Shiki SADRs, target ADRs, code reality, terminology, and edge scenarios.\n+3. **Context & Impact**: find relevant docs, SADRs, target ADRs, code, dependencies, owners, locks, and verification surfaces.\n 4. **PRD and Spec Freeze**: use `to-prd` when context is settled enough to become durable product/engineering intent. The operator's explicit approval of the PRD is Spec Freeze: the plan records a `spec_freeze` block, and no plan runs without it. Steps 1-4 form Requirements Definition \u2014 one continuous operator dialogue. After Spec Freeze, scope changes require an operator-approved Spec Amendment; non-scope-moving interpretations go to the Assumption Log.\n 5. **Issues**: use `to-issues` to create independently grabbable vertical-slice issues. Prefer AFK slices over HITL slices where possible.\n 6. **Triage**: label issues for readiness, risk, runtime, skills, and MergeGate state.\n@@ -167,7 +171,7 @@\n - Testing decisions.\n - Out-of-scope items.\n - Further notes.\n-- Links to relevant domain terms and ADRs.\n+- Links to relevant domain terms, SADRs, and target ADRs.\n \n Every executable task must include:\n \n@@ -193,7 +197,7 @@\n \n Context & Impact must identify:\n \n-- Relevant documents, domain terms, ADRs, and past decisions.\n+- Relevant documents, domain terms, SADRs, target ADRs, and past decisions.\n - Relevant modules, interfaces, seams, callers, owners, and tests.\n - Dependency relationships between tasks.\n - Candidate locks and lock conflicts.\n@@ -210,7 +214,7 @@\n | Skill | Required when |\n | --- | --- |\n | `setup-matt-pocock-skills` | First configuring the repo for skills, or when issue tracker, triage labels, or domain docs are missing. |\n-| `grill-with-docs` | Default for non-trivial Goals before PRD/issues; always required when plans, terminology, boundaries, tradeoffs, or ADR-worthy decisions are ambiguous. |\n+| `grill-with-docs` | Default for non-trivial Goals before PRD/issues; always required when plans, terminology, boundaries, tradeoffs, or decision-record-worthy choices are ambiguous. |\n | `zoom-out` | The agent lacks an architectural map of the relevant code area. |\n | `to-prd` | Settled Goal context must become durable product/engineering intent. |\n | `to-issues` | A Goal, PRD, or plan must become independently grabbable vertical-slice issues. |\n@@ -348,7 +352,7 @@\n - Three or more new files.\n - Any high-risk or critical label.\n \n-When the architecture gate triggers, run `grill-with-docs` or `improve-codebase-architecture` as appropriate and record durable decisions in an ADR when needed.\n+When the architecture gate triggers, run `grill-with-docs` or `improve-codebase-architecture` as appropriate and record durable decisions in a Shiki SADR or target ADR as applicable.\n \n ## Engineering Discipline\n \ndiff --git a/CLAUDE.md b/CLAUDE.md\nindex 98a932e7841a632866fcd53b0bad0e3af3711d3d..98a31399404d6fb044b4fd493f4459ac87f439de\n--- a/CLAUDE.md\n+++ b/CLAUDE.md\n@@ -14,6 +14,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n ## Claude Role\n \n Default to planner, default implementer, reviewer, coordinator, documentation editor, CCA-style completion judge, and final-judgment assistant.\n@@ -57,7 +60,7 @@\n At the start of a Shiki session:\n \n 1. Read `AGENTS.md`.\n-2. Read `CONTEXT.md` and relevant ADRs.\n+2. Read `CONTEXT.md` and relevant Shiki SADRs and target ADRs.\n 3. Read active `.shiki/` Goal/task/ledger state when present.\n 4. Read active distilled rules from `.shiki/memories/` (status `distilled`, `active: true`, not revoked or superseded) for applicable guidance; the same rules are injected deterministically into task handoffs as the `## Distilled Rules` section (\u00a73.5).\n 5. Read the GitHub Issue or PR when the task is GitHub-backed.\n@@ -74,7 +77,7 @@\n - Define the Goal, completion conditions, non-goals, and risk level.\n - Treat `grill-with-docs` as the default planning entry point for non-trivial Goals.\n - Challenge domain terms against `CONTEXT.md`.\n-- Challenge hard-to-reverse tradeoffs against ADRs.\n+- Challenge hard-to-reverse tradeoffs against SADRs and target ADRs.\n - Explore code instead of asking when code can answer the question.\n - Run Context & Impact before implementation planning.\n - Produce a Task DAG, not an unordered checklist.\n@@ -94,7 +97,7 @@\n - For each question, provide your recommended answer.\n - Resolve dependency decisions before downstream design questions.\n - Update `CONTEXT.md` inline when terminology crystallizes.\n-- Offer ADRs only when the decision is hard to reverse, surprising without context, and a real tradeoff.\n+- Offer a SADR or target ADR only when the decision is hard to reverse, surprising without context, and a real tradeoff.\n - Convert unresolved questions into HITL blockers rather than allowing Codex to guess.\n \n ## PRD and Issues Standard\n@@ -103,7 +106,7 @@\n \n - Use the project's domain glossary vocabulary.\n - Avoid volatile file paths unless they are essential.\n-- Include implementation decisions, testing decisions, out-of-scope items, and relevant ADR/domain links.\n+- Include implementation decisions, testing decisions, out-of-scope items, and relevant SADR/target-ADR/domain links.\n - Identify deep-module opportunities and testable interfaces.\n \n When creating issues:\n@@ -121,7 +124,7 @@\n - Branch/worktree target.\n - Scope and non-goals.\n - Dependencies and locks.\n-- Relevant docs, ADRs, modules, and tests.\n+- Relevant docs, SADRs, target ADRs, modules, and tests.\n - Required skill invocations.\n - TDD expectations.\n - Pre-PR `code-review` gate expectations.\n@@ -152,7 +155,7 @@\n \n - Treat yourself as a judge, not implementer.\n - Do not write production code.\n-- Read `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant ADRs, PRD, task issue, PR body, diff, checks, reviews, labels, and `.shiki/` state.\n+- Read `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant SADRs and target ADRs, PRD, task issue, PR body, diff, checks, reviews, labels, and `.shiki/` state.\n - Evaluate every applicable checklist in `docs/agents/checklists.md`.\n - Map each acceptance criterion to durable evidence.\n - Distinguish implementation failure from missing evidence.\ndiff --git a/CONTEXT.md b/CONTEXT.md\nindex e61a9be06a0cce0d026484083a657bde2067e234..237ee8aa3ea8fab334ece49768bc5ed3b5af0974\n--- a/CONTEXT.md\n+++ b/CONTEXT.md\n@@ -25,7 +25,7 @@\n _Avoid_: jumping directly into code\n \n **grill-with-docs**:\n-A planning skill that challenges a plan against domain language, ADRs, code reality, and concrete edge scenarios. It resolves design-tree questions before PRD/issues. It runs inside Requirements Definition.\n+A planning skill that challenges a plan against domain language, Shiki SADRs, target ADRs, code reality, and concrete edge scenarios. It resolves design-tree questions before PRD/issues. It runs inside Requirements Definition.\n _Avoid_: generic brainstorming, silent assumptions\n \n **Requirements Definition**:\ndiff --git a/CONTRIBUTING.md b/CONTRIBUTING.md\nindex c6d9b656e73abb8a55e5f660123e03153dcb3e6d..9e37a8fbf668db5b22461aaae5548b8f1354394c\n--- a/CONTRIBUTING.md\n+++ b/CONTRIBUTING.md\n@@ -17,7 +17,7 @@\n 1. **Goal** \u2014 start from a user-approved Goal with an outcome, completion\n    conditions, non-goals, risk level, and success signals.\n 2. **grill-with-docs** \u2014 for any non-trivial change, challenge the plan against\n-   domain language, ADRs, and code reality before writing a PRD.\n+   domain language, Shiki SADRs, target ADRs, and code reality before writing a PRD.\n 3. **PRD** \u2014 capture settled product/engineering intent (`to-prd`).\n 4. **Spec Freeze** \u2014 the operator explicitly approves the PRD/requirements;\n    the plan records a `spec_freeze` block and no plan runs without it\n@@ -94,7 +94,7 @@\n   entries.\n - Do not hand-edit `.shiki/goals/*.json` or `.shiki/manifest.json` in a task PR.\n - Conversation state is not durable truth. If a decision matters, record it in\n-  GitHub, `.shiki/`, `CONTEXT.md`, or an ADR.\n+  GitHub, `.shiki/`, `CONTEXT.md`, a Shiki SADR, or a target ADR.\n \n ## Architecture Gate\n \n@@ -103,7 +103,7 @@\n or CI policy, package structure, three or more structural files, more than 100\n net new lines in a core module, three or more new files, or any high-risk label.\n When triggered, run `grill-with-docs` or `improve-codebase-architecture` and\n-record durable decisions in an ADR when needed (see `docs/adr/`).\n+record durable decisions in a Shiki SADR or target ADR as applicable (see `docs/adr/`).\n \n ## Safety\n \ndiff --git a/SYSTEM_PROMPT.md b/SYSTEM_PROMPT.md\nindex aaaea45e3a896c3a44ba89eec8bb84325ecf6c62..da009c86a88285269dee0155e58e550c6d744ea9\n--- a/SYSTEM_PROMPT.md\n+++ b/SYSTEM_PROMPT.md\n@@ -30,6 +30,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n Use the canonical source-of-truth order above. Runtime-specific reading orders\n are operational guidance only and must not override or reorder it. The current\n conversation is non-durable operator input.\n@@ -48,14 +51,14 @@\n \n A runtime may only do work that is explicitly assigned by the Goal, task, PR, or MergeGate decision.\n \n-Do not assume `openai/codex-action`, `OPENAI_API_KEY`, or API-key based Codex automation in the default Shiki path. API-key runners are explicit target-repo extensions and require their own ADR.\n+Do not assume `openai/codex-action`, `OPENAI_API_KEY`, or API-key based Codex automation in the default Shiki path. API-key runners are explicit target-repo extensions and require their own target ADR.\n \n ## 4. Non-negotiable Shiki flow\n \n For any non-trivial Goal, use this flow:\n \n 1. **Goal Seek**: define outcome, non-goals, risk level, completion criteria, and evidence requirements.\n-2. **grill-with-docs**: challenge the plan against `CONTEXT.md`, ADRs, code reality, terminology, boundaries, and edge scenarios. Ask one question at a time when the operator is available. If code can answer a question, inspect code instead of asking.\n+2. **grill-with-docs**: challenge the plan against `CONTEXT.md`, Shiki SADRs, target ADRs, code reality, terminology, boundaries, and edge scenarios. Ask one question at a time when the operator is available. If code can answer a question, inspect code instead of asking.\n 3. **Context & Impact**: identify modules, interfaces, seams, callers, dependencies, locks, risk, and verification surfaces.\n 4. **to-prd + Spec Freeze**: turn settled context into a PRD using domain vocabulary and testing decisions. The operator's explicit approval of the PRD is Spec Freeze, recorded as the plan's `spec_freeze` block; plans without it do not run. Steps 1-4 form Requirements Definition.\n 5. **to-issues**: decompose the PRD into independently grabbable vertical-slice issues. Prefer AFK slices; mark HITL slices when judgment is still required.\n@@ -75,7 +78,7 @@\n Before making any material change:\n \n 1. Read `AGENTS.md` and any runtime-specific wrapper such as `CLAUDE.md`.\n-2. Read `CONTEXT.md` and relevant ADRs.\n+2. Read `CONTEXT.md` and relevant SADRs and target ADRs.\n 3. Read the GitHub Issue / PR and `.shiki/` state for the active Goal or task.\n 4. Identify your role: Planner, Implementer, Reviewer, Completion Check Agent, Repairer, or Guardian-assist.\n 5. Identify Goal id, task id, branch, PR, dependencies, locks, risk level, required skills, and required evidence.\n@@ -103,7 +106,7 @@\n \n At minimum, identify:\n \n-- Relevant documents, ADRs, domain terms, and prior decisions.\n+- Relevant documents, SADRs, target ADRs, domain terms, and prior decisions.\n - Relevant modules, interfaces, seams, callers, owners, and tests.\n - Dependency relationships between tasks.\n - Candidate file locks and lock conflicts.\n@@ -152,7 +155,7 @@\n \n CCA must read:\n \n-- `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant ADRs.\n+- `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant SADRs and target ADRs.\n - Parent Goal issue, PRD issue, task issue, comments, and labels.\n - PR body, diff, commits, changed files, reviews, CI checks, and artifacts available to the workflow.\n - `.shiki/` task contract, locks, ledger entries, CCA checklist profile, and prior repair packets.\n@@ -214,7 +217,7 @@\n - Three or more new files.\n - Any high-risk or critical label.\n \n-When the architecture gate triggers, run `grill-with-docs` or `improve-codebase-architecture` as appropriate and record the decision in an ADR if it is hard to reverse or likely to be re-litigated.\n+When the architecture gate triggers, run `grill-with-docs` or `improve-codebase-architecture` as appropriate and record the decision in a Shiki SADR or target ADR as applicable if it is hard to reverse or likely to be re-litigated.\n \n ## 14. Skill Gate\n \n@@ -223,7 +226,7 @@\n Use these skills:\n \n - `setup-matt-pocock-skills`: before first use of the engineering skill set, or when issue tracker, triage labels, or domain docs are missing.\n-- `grill-with-docs`: default for non-trivial Goals before PRD/issues; required for ambiguous plans, terminology, boundaries, ADR-worthy tradeoffs, or design-tree decisions.\n+- `grill-with-docs`: default for non-trivial Goals before PRD/issues; required for ambiguous plans, terminology, boundaries, decision-record-worthy tradeoffs, or design-tree decisions.\n - `zoom-out`: unfamiliar code area or missing architectural map.\n - `to-prd`: convert settled context into a PRD.\n - `to-issues`: break a PRD, plan, or Goal into independently grabbable vertical-slice issues.\ndiff --git a/docs/adr/0001-keep-shiki-as-platform-name.md b/docs/adr/SADR-0001-keep-shiki-as-platform-name.md\nindex 20a9637c3b9d8c97de7ba8db7cfae4d82d9a7dba..8b685439959aada1b221c112f0ac3c51ca8a1695\n--- a/docs/adr/0001-keep-shiki-as-platform-name.md\n+++ b/docs/adr/SADR-0001-keep-shiki-as-platform-name.md\n@@ -1,3 +1,3 @@\n-# Keep Shiki As The Platform Name\n+# SADR-0001: Keep Shiki As The Platform Name\n \n Shiki remains the product name for the GitHub-native agentic engineering control plane. We considered more descriptive names such as MergePilot, MergeGate, and Kiroku, but kept Shiki because it carries the existing goal-seeking framework identity while remaining independent of Hermes, Codex, Claude Code, or any single runtime.\ndiff --git a/docs/adr/0002-use-agents-as-shared-constitution.md b/docs/adr/SADR-0002-use-agents-as-shared-constitution.md\nindex 73de30ecb078120eac540d9bc51855e3cd48d0e3..c5486182bf06fca36ea508496b013c67302fd989\n--- a/docs/adr/0002-use-agents-as-shared-constitution.md\n+++ b/docs/adr/SADR-0002-use-agents-as-shared-constitution.md\n@@ -1,3 +1,3 @@\n-# Use AGENTS As The Shared Agent Constitution\n+# SADR-0002: Use AGENTS As The Shared Agent Constitution\n \n Shiki uses `AGENTS.md` as the runtime-neutral constitution and `CLAUDE.md` as a thin Claude Code wrapper. This keeps Codex, Claude Code, Hermes Runner, and future Agent Runtimes aligned on the same GitHub-first MergeGate rules while still allowing runtime-specific instructions where needed.\ndiff --git a/docs/adr/0003-require-skill-gate-for-mergegate-readiness.md b/docs/adr/SADR-0003-require-skill-gate-for-mergegate-readiness.md\nindex 424be0c9af4c8f0136f1fde68a468c47ff84e764..7fc337987c7df9792f75f181dfcbed75fe5ffff2\n--- a/docs/adr/0003-require-skill-gate-for-mergegate-readiness.md\n+++ b/docs/adr/SADR-0003-require-skill-gate-for-mergegate-readiness.md\n@@ -1,3 +1,3 @@\n-# Require Skill Gate For MergeGate Readiness\n+# SADR-0003: Require Skill Gate For MergeGate Readiness\n \n Shiki requires relevant engineering skills to be invoked before execution when their triggers apply. This makes skills part of MergeGate readiness rather than optional prompt style, because Shiki depends on repeatable planning, issue decomposition, TDD, diagnosis, triage, and architecture review before autonomous or parallel agent work can be trusted.\ndiff --git a/docs/adr/0004-use-github-first-with-shiki-mirror.md b/docs/adr/SADR-0004-use-github-first-with-shiki-mirror.md\nindex d0a3a9174dc646a945a9d325954ffaf00c352040..a79c54ae9ee8eed2a0524192b864da4e22a92d3f\n--- a/docs/adr/0004-use-github-first-with-shiki-mirror.md\n+++ b/docs/adr/SADR-0004-use-github-first-with-shiki-mirror.md\n@@ -1,4 +1,4 @@\n-# ADR 0004: Use GitHub First With A Repository-Local Shiki Mirror\n+# SADR-0004: Use GitHub First With A Repository-Local Shiki Mirror\n \n ## Status\n \ndiff --git a/docs/adr/0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md b/docs/adr/SADR-0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\nindex b339baf49b89d9290e79c3d51b36e7b2bde3bd95..0f3c3eb9afccdb3403ce978e21d04fe12e495da0\n--- a/docs/adr/0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\n+++ b/docs/adr/SADR-0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\n@@ -1,8 +1,8 @@\n-# ADR 0005: Use Codex As Front And Claude Code Action As The GitHub Runtime\n+# SADR-0005: Use Codex As Front And Claude Code Action As The GitHub Runtime\n \n ## Status\n \n-Accepted (implementer default superseded by ADR 0008; the auth model and the\n+Accepted (implementer default superseded by SADR-0008; the auth model and the\n Claude Code Action GitHub runtime decision remain in force)\n \n ## Context\n@@ -26,4 +26,4 @@\n - Shiki avoids requiring `OPENAI_API_KEY` for the default workflow.\n - Codex work happens through the user's authenticated Codex front surface, not as a hidden backend Action.\n - Claude Code Action is configured for OAuth-token based subscription usage by default.\n-- API-key based automation remains possible only as an explicit target-repo extension with its own ADR.\n+- API-key based automation remains possible only as an explicit target-repo extension with its own target ADR.\ndiff --git a/docs/adr/0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md b/docs/adr/SADR-0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\nindex 4bcae9d1e52f0a37b8e9fbafa5cd14b6ee1993bd..7a38538a6ae9929567f0ade67e7ea6a60d0b3f7e\n--- a/docs/adr/0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\n+++ b/docs/adr/SADR-0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\n@@ -1,4 +1,4 @@\n-# ADR 0006: Separate Codex Implementation, CCA Completion, And MergeGate Authorization\n+# SADR-0006: Separate Codex Implementation, CCA Completion, And MergeGate Authorization\n \n ## Status\n \ndiff --git a/docs/adr/0007-packaging-and-release.md b/docs/adr/SADR-0007-packaging-and-release.md\nindex 79b84ddb97167372b44641e5b7860e400ce80822..bb445a2e2e856d9d1f5fdbe35de8d4e8cec0fb32\n--- a/docs/adr/0007-packaging-and-release.md\n+++ b/docs/adr/SADR-0007-packaging-and-release.md\n@@ -1,4 +1,4 @@\n-# ADR 0007: Packaging And Release For Shiki\n+# SADR-0007: Packaging And Release For Shiki\n \n ## Status\n \n@@ -25,7 +25,7 @@\n   not introduce third-party runtime dependencies or break the shim.\n - Releases must run inside the operator's subscription-authenticated toolchain\n   and must not require extra secrets beyond the built-in `GITHUB_TOKEN`\n-  (consistent with ADR 0005's no-API-key-by-default stance).\n+  (consistent with SADR-0005's no-API-key-by-default stance).\n - `.shiki/manifest.json` already encodes the install surface (`install.include`,\n   `install.create_directories`, `install.exclude_from_commit`), and it must not\n   be mutated as part of this change.\n@@ -34,7 +34,7 @@\n \n This is a hard-to-reverse platform decision (it sets the version source of\n truth, the release trigger, and the public install contract), so it is recorded\n-as an ADR.\n+as a SADR.\n \n ## Decision\n \n@@ -104,7 +104,7 @@\n 3. **Publishing to PyPI.** Rejected for now: Shiki is distributed as a\n    repository template + git-installable CLI, not a PyPI library; PyPI would add\n    credentials/secrets and a different versioning contract. Can be added later as\n-   its own ADR if a packaged distribution is needed.\n+   its own SADR if a packaged distribution is needed.\n 4. **Dynamic version (read `VERSION` via a build backend `dynamic` field).**\n    Rejected to keep `pyproject.toml` backend-agnostic and dependency-free; the\n    small duplication between `VERSION` and `pyproject.toml` is guarded by the\n@@ -123,4 +123,4 @@\n   releasing versions the whole platform but ships only `install.include` to\n   targets.\n - Future PyPI distribution or a console entry point are deferred and would each\n-  warrant their own ADR.\n+  warrant their own SADR.\ndiff --git a/docs/adr/0008-use-claude-code-as-default-implementer.md b/docs/adr/SADR-0008-use-claude-code-as-default-implementer.md\nindex dedf4510b7c7799521422f622029563d373db031..35c2625d908167fbc58855b5b63e283805f167e8\n--- a/docs/adr/0008-use-claude-code-as-default-implementer.md\n+++ b/docs/adr/SADR-0008-use-claude-code-as-default-implementer.md\n@@ -1,12 +1,12 @@\n-# ADR 0008: Use Claude Code As The Default Implementer Runtime\n+# SADR-0008: Use Claude Code As The Default Implementer Runtime\n \n ## Status\n \n-Proposed (supersedes ADR 0005 for the implementer default; keeps its auth model)\n+Proposed (supersedes SADR-0005 for the implementer default; keeps its auth model)\n \n ## Context\n \n-ADR 0005 made Codex Front the default implementation runtime, with Claude Code\n+SADR-0005 made Codex Front the default implementation runtime, with Claude Code\n restricted to planner and reviewer roles by the runtime registry, and the only\n autonomous implementation adapter was `shiki runner codex`.\n \n@@ -18,13 +18,13 @@\n - The platform's planning, review, and CCA layers already run on Claude Code,\n   and Claude Code's native capabilities (the built-in `/code-review` skill and\n   the Workflow multi-agent orchestration tool) have no Codex equivalent.\n-- The Goal lifecycle redesign (ADR 0009) requires an autonomous post-freeze\n+- The Goal lifecycle redesign (SADR-0009) requires an autonomous post-freeze\n   loop driven from the operator's Claude Code session, including bounded\n   repair dispatch, which must not depend on a runtime that cannot be invoked\n   unattended.\n \n The constitution claims Shiki is runtime-agnostic (\"not a Claude-only\n-workflow\", ADR 0002). Any change to the implementer default must preserve the\n+workflow\", SADR-0002). Any change to the implementer default must preserve the\n registry-based runtime model rather than hardcoding one vendor.\n \n ## Decision\n@@ -39,7 +39,7 @@\n   the task to review or repair-needed.\n - Keep Codex registered as an optional implementer runtime. `shiki runner\n   codex`, the Codex handoff templates, and the ChatGPT OAuth auth model from\n-  ADR 0005 remain supported for tasks explicitly assigned to `codex`.\n+  SADR-0005 remain supported for tasks explicitly assigned to `codex`.\n - Commit to mandating two Claude-native capabilities as loop contract in\n   follow-up Goals (status: the code-review gate was delivered by Goal G-B \u2014\n   skill registry, checklists item PR-12, default required_skills \u2014 while the\n@@ -60,7 +60,7 @@\n \n Out of scope: removing Codex support, changing the CCA runtime (Claude Code\n Action with `CLAUDE_CODE_OAUTH_TOKEN` stays), and API-key based automation\n-(still requires its own ADR per ADR 0005).\n+(still requires its own target ADR per SADR-0005).\n \n ## Consequences\n \n@@ -100,7 +100,7 @@\n   harness rejection of direct `codex exec` is outside Shiki's control, and the\n   plan's mandatory native capabilities cannot run on Codex at all.\n - **Remove Codex entirely**: rejected; it abandons the runtime-agnostic\n-  constitution (ADR 0002) for no operational gain and forecloses\n+  constitution (SADR-0002) for no operational gain and forecloses\n   cross-runtime adversarial review.\n - **GitHub Actions cloud implementation via claude-code-action**: rejected as\n   default; durable evidence is automatic but token cost, Node 24 workflow\ndiff --git a/docs/adr/0009-requirements-definition-and-spec-freeze.md b/docs/adr/SADR-0009-requirements-definition-and-spec-freeze.md\nindex 29b3f7871bef9203a8b4a831a65c39eb2f506e42..52e7dd69ad0aa4efe0b68c62678abbf7df02aaec\n--- a/docs/adr/0009-requirements-definition-and-spec-freeze.md\n+++ b/docs/adr/SADR-0009-requirements-definition-and-spec-freeze.md\n@@ -1,4 +1,4 @@\n-# ADR 0009: Merge Grill And Planning Into Requirements Definition With Spec Freeze\n+# SADR-0009: Merge Grill And Planning Into Requirements Definition With Spec Freeze\n \n ## Status\n \ndiff --git a/docs/adr/0010-external-ai-guardian-review.md b/docs/adr/SADR-0010-external-ai-guardian-review.md\nindex e6108bbe8bb4e73e0ff4684c8f78001a80438c4e..f3510b145664298adafb151b6f0db51b972f01ec\n--- a/docs/adr/0010-external-ai-guardian-review.md\n+++ b/docs/adr/SADR-0010-external-ai-guardian-review.md\n@@ -1,4 +1,4 @@\n-# ADR 0010: External AI Guardian Review As A First-Class Approval Authority\n+# SADR-0010: External AI Guardian Review As A First-Class Approval Authority\n \n ## Status\n \n@@ -8,7 +8,7 @@\n \n Shiki is intended to be an autonomous, recursive, self-improving agent\n framework: authority-in-the-loop, not human-in-the-loop. Until now the Guardian\n-approval required for high/critical-risk merges (guardian-policy.json, ADR\n+approval required for high/critical-risk merges (guardian-policy.json, SADR\n governance) recognized only human authority sources \u2014 a GitHub review approval,\n the `guardian:approved` label applied by a configured Guardian, or a comment\n carrying `Guardian approval granted` plus the current head SHA.\n@@ -89,7 +89,7 @@\n   autonomous design the operator intends and produced the impersonation\n   incident.\n - **Record AI approval as an operator comment**: rejected as forgery of human\n-  authority \u2014 the exact failure this ADR corrects.\n+  authority \u2014 the exact failure this SADR corrects.\n - **Deliver the artifact as a committed `.shiki/` file**: rejected; a committed\n   approval inside the PR being approved is forgeable by anyone who can push.\n - **A dedicated reviewer bot identity instead of a relayed comment**: deferred;\ndiff --git a/docs/adr/0011-autonomous-quality-gate-evidence.md b/docs/adr/SADR-0011-autonomous-quality-gate-evidence.md\nindex bca0246c5d46e44afbde15486a75eec425fc02f5..9c93eac16d8c3a6d4b11bd7224e3955102cd7db4\n--- a/docs/adr/0011-autonomous-quality-gate-evidence.md\n+++ b/docs/adr/SADR-0011-autonomous-quality-gate-evidence.md\n@@ -1,4 +1,4 @@\n-# ADR 0011: Autonomous Quality-Gate Evidence \u2014 Deterministic Facts And An Independent Verifier\n+# SADR-0011: Autonomous Quality-Gate Evidence \u2014 Deterministic Facts And An Independent Verifier\n \n ## Status\n \ndiff --git a/docs/adr/0012-autonomous-loop-closeout-to-main.md b/docs/adr/SADR-0012-autonomous-loop-closeout-to-main.md\nindex 637fad6dde4989c9c2bf0f81f676d8d29991123c..ec7d85f81030a676bcbba28b43eb365422a36eac\n--- a/docs/adr/0012-autonomous-loop-closeout-to-main.md\n+++ b/docs/adr/SADR-0012-autonomous-loop-closeout-to-main.md\n@@ -1,4 +1,4 @@\n-# 0012 \u2014 Autonomous loop pushes goal completion to main via a closeout PR\n+# SADR-0012 \u2014 Autonomous loop pushes goal completion to main via a closeout PR\n \n - Status: accepted\n - Date: 2026-06-18\ndiff --git a/docs/adr/0013-bootstrap-grants-actions-approve-pull-requests.md b/docs/adr/SADR-0013-bootstrap-grants-actions-approve-pull-requests.md\nindex b6c69d64f1c5ced346b88d6859586715b3b5bd0e..cf48ff4a6716267f539ca58edd1a852e7c7a2d64\n--- a/docs/adr/0013-bootstrap-grants-actions-approve-pull-requests.md\n+++ b/docs/adr/SADR-0013-bootstrap-grants-actions-approve-pull-requests.md\n@@ -1,4 +1,4 @@\n-# ADR 0013: Bootstrap Grants GitHub Actions Permission To Create And Approve Pull Requests\n+# SADR-0013: Bootstrap Grants GitHub Actions Permission To Create And Approve Pull Requests\n \n ## Status\n \n@@ -31,7 +31,7 @@\n security tradeoff: it lets repository automation satisfy a human-review gate. In\n a multi-maintainer repo that could let a compromised or buggy workflow\n self-approve changes. So whether bootstrap turns it on by default \u2014 and with\n-what default token scope \u2014 is an ADR-worthy decision, not an implementation\n+what default token scope \u2014 is a SADR-worthy decision, not an implementation\n detail.\n \n ## Decision\n@@ -48,7 +48,7 @@\n Specifically:\n \n - **`can_approve_pull_request_reviews` defaults to `true`.** Shiki's default\n-  posture is solo/AFK autonomous operation (ADR 0008/0009/0012), where the CCA\n+  posture is solo/AFK autonomous operation (SADR-0008/0009/0012), where the CCA\n   Review Bridge is the only path that can satisfy `required_review: true` after\n   the CCA and Guardian gates have already passed. The default makes a\n   freshly-bootstrapped repo able to run the autonomous loop end to end without a\ndiff --git a/docs/adr/0014-codex-app-external-ai-guardian-ui-adapter.md b/docs/adr/SADR-0014-codex-app-external-ai-guardian-ui-adapter.md\nindex 238db06279334caa341c117362df9271382a7fcd..65f12e9939321e1b3e389a34c822fc1549e5155d\n--- a/docs/adr/0014-codex-app-external-ai-guardian-ui-adapter.md\n+++ b/docs/adr/SADR-0014-codex-app-external-ai-guardian-ui-adapter.md\n@@ -1,4 +1,4 @@\n-# ADR 0014: Use Codex App As The External AI Guardian UI Adapter\n+# SADR-0014: Use Codex App As The External AI Guardian UI Adapter\n \n ## Status\n \ndiff --git a/docs/adr/0015-contract-approval-before-dispatch.md b/docs/adr/SADR-0015-contract-approval-before-dispatch.md\nindex fad6c656741a20a7684ba16080a7db159f63a52f..6061271a46d3f1d6ae7174477de3fb75673123a6\n--- a/docs/adr/0015-contract-approval-before-dispatch.md\n+++ b/docs/adr/SADR-0015-contract-approval-before-dispatch.md\n@@ -1,4 +1,4 @@\n-# ADR 0015: Approve The Task Contract Before Dispatch, Not The Diff After It\n+# SADR-0015: Approve The Task Contract Before Dispatch, Not The Diff After It\n \n ## Status\n \n@@ -9,7 +9,7 @@\n Guardian approval for high/critical risk is expressible only on a pull request.\n Every approval source in `guardian-policy.json` \u2014 GitHub review, the\n `guardian:approved` label, the `Guardian approval granted` comment, and the\n-`external_ai_guardian_review` artifact (ADR 0010/0014) \u2014 is bound to a PR, and\n+`external_ai_guardian_review` artifact (SADR-0010/0014) \u2014 is bound to a PR, and\n three of the four require the current head SHA. Approval therefore cannot exist\n before an implementation PR exists.\n \n@@ -81,7 +81,7 @@\n An envelope breach must surface only on the non-repairable policy gate, never on\n the repairable `MergeGate metadata check`. Otherwise the Goal Loop routes it to\n `dispatch_repair` \u2014 instructing a runner to make the breach go away \u2014 which is\n-exactly what this ADR forbids.\n+exactly what this SADR forbids.\n \n **Contract immutability.** After registration, an implementation PR must not\n change the governance fields of its own task contract (`scope`, `non_goals`,\n@@ -116,7 +116,7 @@\n fails any verdict that is not `complete`, and the signal the CCA prompt reads is\n computed from `evaluate_guardian_approval` alone. Without the OR-merge at the\n signal, a contract-approved PR still returns `needs_guardian`, the CCA job fails,\n-and the MergeGate policy check never runs \u2014 the gate this ADR changes would never\n+and the MergeGate policy check never runs \u2014 the gate this SADR changes would never\n be reached. The CCA *prompt* needs no change: a `contract_approval` entry in the\n signal's existing `sources` array satisfies its current wording.\n \n@@ -130,7 +130,7 @@\n written, and an approved contract lets its implementation PRs merge through the\n same automatic path as low/medium risk. The wasted implement-then-block cycle\n disappears. Guardian review shifts from reading diffs to reading contracts, which\n-is both earlier and smaller; the external AI Guardian path (ADR 0010/0014) works\n+is both earlier and smaller; the external AI Guardian path (SADR-0010/0014) works\n on contracts with no change.\n \n We accept that the Guardian authorizes a class of change without seeing its\n@@ -146,7 +146,7 @@\n adversarial tests for the new approval path equivalent in rigor to\n `test_shiki_governance_evidence.sh`.\n \n-Bootstrap costs, which apply only while this ADR is being implemented. The tasks\n+Bootstrap costs, which apply only while this SADR is being implemented. The tasks\n that build Contract Approval merge under the existing rules, so the ones\n dispatched after the pre-dispatch gate lands but before the approval path lands\n need a Guardian-approved Contract PR *and* a live Guardian approval on their\n@@ -210,7 +210,7 @@\n risk; they do not bound authorized work.\n \n **Auto-escalate risk on envelope breach instead of stopping.** Returns the\n-workflow to a diff-time Guardian decision \u2014 the exact behavior this ADR removes.\n+workflow to a diff-time Guardian decision \u2014 the exact behavior this SADR removes.\n \n **Resolve governance fields from the base snapshot only.** Rejected against the\n registration flow: implementation PRs add their own contract, so the base has no\ndiff --git a/docs/adr/0016-locks-bound-product-paths.md b/docs/adr/SADR-0016-locks-bound-product-paths.md\nindex b5e850f3f1f7f4ef6e7f9d1763a29a51bd300c45..46b40a9b99a7aae644c02bb458f5473ff0a90ce2\n--- a/docs/adr/0016-locks-bound-product-paths.md\n+++ b/docs/adr/SADR-0016-locks-bound-product-paths.md\n@@ -1,4 +1,4 @@\n-# ADR 0016: Locks bound product paths; the Shiki mirror is bound by record identity\n+# SADR-0016: Locks bound product paths; the Shiki mirror is bound by record identity\n \n ## Status\n \n@@ -6,8 +6,8 @@\n \n - Date: 2026-07-28\n - Deciders: operator (mizutani-140), Claude (planner)\n-- Related: ADR 0004 (GitHub-first with a `.shiki` mirror), ADR 0009 (Spec Freeze),\n-  ADR 0012 (autonomous loop closeout to main), ADR 0015 (Contract Approval before\n+- Related: SADR-0004 (GitHub-first with a `.shiki` mirror), SADR-0009 (Spec Freeze),\n+  SADR-0012 (autonomous loop closeout to main), SADR-0015 (Contract Approval before\n   dispatch), remediation goal task R-02, the live PR #179 deadlock.\n \n ## Context\n@@ -90,11 +90,11 @@\n files, documentation files and named source modules are nameable; runtime-\n allocated ledger, runner and report ids are not \u2014 and those live in `.shiki`,\n which locks no longer govern. Existing frozen contracts are brought into line by\n-a one-time Spec Amendment per plan, which must land before ADR 0015's contract\n+a one-time Spec Amendment per plan, which must land before SADR-0015's contract\n immutability makes `locks` unwritable from the implementation PR.\n \n **Lock state is resolved from the durable side.** MergeGate resolves the lock\n-registry, and ADR 0015's governance fields, from the base `.shiki` snapshot when\n+registry, and SADR-0015's governance fields, from the base `.shiki` snapshot when\n present, falling back to head only when absent. The `MergeGate metadata check`\n job is given `--base-shiki`, which it does not receive today. Locks are not\n derived from the live open-pull-request set: a pull request body is untrusted\n@@ -108,7 +108,7 @@\n Explicitly out of scope: moving MergeGate's contract resolution off the PR\n checkout wholesale; making the mirror a projection of GitHub state; introducing\n new lock kinds; moving ledger evidence out of the repository; and unwinding\n-ADR 0012's closeout pull request. Registration of multi-task goals is ADR 0015's\n+SADR-0012's closeout pull request. Registration of multi-task goals is SADR-0015's\n Contract PR and is not re-decided here.\n \n ## Consequences\n@@ -127,7 +127,7 @@\n \n Risks accepted. First, goal-scoped identity is only as strong as `goal_id`,\n which is resolved from the PR body and the head task file; the mitigation is to\n-add `goal_id` to ADR 0015's frozen governance field set, and until that lands a\n+add `goal_id` to SADR-0015's frozen governance field set, and until that lands a\n task file can name a foreign goal. Second, base-when-present resolution is\n unavailable on fork pull requests, where the head is contributor-controlled;\n fork pull requests already cannot complete a Shiki merge, and this decision does\n@@ -139,7 +139,7 @@\n path, not by implementation pull requests.\n \n New obligations: one Spec Amendment per frozen plan to rewrite `locks`, ordered\n-before ADR 0015 T1; extension of the adversarial suites in\n+before SADR-0015 T1; extension of the adversarial suites in\n `scripts/test_shiki_adversarial_state.sh` and\n `scripts/test_shiki_governance_evidence.sh` to cover the eleven newly-scoped\n directories; and updating `scripts/test_shiki_loop_lock_guard.sh`,\n@@ -151,32 +151,32 @@\n whether the loop's transitive sync of ledger `evidence` references can be\n deleted once the Contract PR carries its own source plan.\n \n-## Relationship to ADR 0004\n+## Relationship to SADR-0004\n \n-This ADR **clarifies something ADR 0004 left unspecified**. ADR 0004 decides that\n+This SADR **clarifies something SADR-0004 left unspecified**. SADR-0004 decides that\n GitHub is the operational source of truth and that each target repository keeps a\n `.shiki/` mirror. It says nothing about pull requests writing the mirror, does\n not name MergeGate, and does not make the mirror an input to a gate. Nothing in\n-ADR 0004's Decision or Consequences is amended, superseded or weakened: the\n+SADR-0004's Decision or Consequences is amended, superseded or weakened: the\n mirror still exists, still records the same artifacts, and pull request\n-boundaries remain the coordination surface. What this ADR settles is the question\n+boundaries remain the coordination surface. What this SADR settles is the question\n 0004 never reached \u2014 by what authority a pull request may write a mirror record \u2014\n and it answers it the way `.shiki/manifest.json` already declares\n (`pr_mutation: current-task-or-current-goal-only`).\n \n-It **amends ADR 0012**, whose Decision item 5 states that \"Loop-executed tasks\n+It **amends SADR-0012**, whose Decision item 5 states that \"Loop-executed tasks\n declare the `path:.shiki/**` lock, which covers every `.shiki` file the closeout\n stages ... so `files_outside_locks` is satisfied without per-file lock\n bookkeeping.\" That clause is replaced: the closeout's mirror writes are\n-authorized by record identity, not by a declared lock. ADR 0012's substantive\n+authorized by record identity, not by a declared lock. SADR-0012's substantive\n decision \u2014 that completion reaches `main` through a closeout pull request rather\n than local attestation \u2014 is preserved unchanged.\n \n-It is **complementary to ADR 0015**, not a substitute. ADR 0015 owns contract\n+It is **complementary to SADR-0015**, not a substitute. SADR-0015 owns contract\n immutability, the pre-dispatch gate, the Contract PR and the Scope Envelope. This\n-ADR owns what a lock means and who may write the mirror. The one ordering\n+SADR owns what a lock means and who may write the mirror. The one ordering\n constraint between them is that the lock-narrowing Spec Amendment must precede\n-ADR 0015 T1's freezing of the `locks` field.\n+SADR-0015 T1's freezing of the `locks` field.\n \n ## Alternatives Considered\n \n@@ -186,7 +186,7 @@\n the only writer that can register them may run. Migrating the Guardian signal to\n that store makes every registration PR fail closed to `needs_guardian` at every\n risk level, because the task file is by definition absent from the store, which\n-removes the low/medium autonomous path ADR 0008/0009 exists to provide. And it\n+removes the low/medium autonomous path SADR-0008/0009 exists to provide. And it\n leaves schemas, manifest, config, guardian policy, templates, migrations, plans,\n worktrees, runner records, runs and handoffs with no writer at all in a\n repository whose product is `.shiki`. Measured benefit on the frozen backlog:\n@@ -199,14 +199,14 @@\n namespace is not in fact ownership-partitioned \u2014 56 ledger entries carry no\n `task_id`, and one goal DAG is writable by 35 registered tasks. The correct\n expression of its insight is that locks do not govern the mirror at all, which is\n-what this ADR decides.\n+what this SADR decides.\n \n **Make the mirror a projection written only by a registration writer and a\n post-merge projection job.** Rejected. It requires a bypass actor with write\n access to the protected default branch; it makes its own master invariant\n readable from a manifest that pull requests may edit; it deletes both reconcile\n modes and leaves a corrupted mirror with no in-band repair path, contradicting\n-ADR 0004's consequence that mirror conflicts are repaired against GitHub state;\n+SADR-0004's consequence that mirror conflicts are repaired against GitHub state;\n and its content-derived ids are rejected by `validate_shiki`'s currently-green id\n pattern.\n \ndiff --git a/docs/adr/0017-bookkeeping-closeouts-do-not-inherit-risk.md b/docs/adr/SADR-0017-bookkeeping-closeouts-do-not-inherit-risk.md\nindex 554e3a0d526a72ca067108c5464cc68c3edcf174..b9676857a4713850e063af7c39c8e0b01ecb4c66\n--- a/docs/adr/0017-bookkeeping-closeouts-do-not-inherit-risk.md\n+++ b/docs/adr/SADR-0017-bookkeeping-closeouts-do-not-inherit-risk.md\n@@ -1,4 +1,4 @@\n-# ADR 0017: A Bookkeeping Closeout Does Not Inherit Its Task's Risk\n+# SADR-0017: A Bookkeeping Closeout Does Not Inherit Its Task's Risk\n \n ## Status\n \n@@ -6,7 +6,7 @@\n \n ## Context\n \n-ADR 0012 made the goal loop push a task's completion to the default branch through a\n+SADR-0012 made the goal loop push a task's completion to the default branch through a\n closeout pull request: after the implementation PR merges, the loop opens a second PR\n carrying `task.status = done`, `lock.state = released`, and \u2014 when the task completes\n its goal \u2014 `goal.status = complete` plus one scorecard report. Nothing else. The\n@@ -49,14 +49,14 @@\n 4. The task file's governance fields (`scope`, `non_goals`, `required_skills`,\n    `risk_level`, `locks`, `acceptance_checks`, `test_command`) are byte-identical to\n    the base snapshot. Only `status`, `expected_pr`, `closeout_pr`, `ledger_evidence`\n-   and `expected_branch` may differ \u2014 the ADR 0015 contract-immutability rule,\n+   and `expected_branch` may differ \u2014 the SADR-0015 contract-immutability rule,\n    unchanged.\n \n    `expected_branch` is in that set because a closeout is *by construction* on a\n    different branch from the implementation it closes, and the loop rewrites the\n    field accordingly. It is safe to let it differ because MergeGate independently\n    requires `expected_branch` to equal the PR's actual head ref, so a PR cannot set\n-   it to anything other than the branch it is really on. The first draft of this ADR\n+   it to anything other than the branch it is really on. The first draft of this SADR\n    omitted it, which made the exemption unreachable: every real closeout failed\n    condition 4 on a field the loop always changes. See PR #209.\n 5. The state transitions are exactly the terminal set: task `review` \u2192 `done`, lock\n@@ -113,12 +113,12 @@\n classification must be derived, not asserted.\n \n **Fold the closeout into the implementation PR** so no second PR exists. Rejected under\n-ADR 0012, which recorded why: a task file that is already `done` on the implementation\n+SADR-0012, which recorded why: a task file that is already `done` on the implementation\n branch short-circuits the loop's review \u2192 CCA \u2192 merge \u2192 done ordering into a local\n goal-completion before any merge happens.\n \n **Have the loop auto-merge closeouts with elevated credentials.** Rejected outright: that\n-is a runtime authoring its own approval, which ADR 0010 and the constitution's\n+is a runtime authoring its own approval, which SADR-0010 and the constitution's\n approval-record prohibition forbid. It would also be indistinguishable, in the evidence,\n from the forgery those rules exist to prevent.\n \ndiff --git a/docs/adr/0018-carried-guardian-approval-across-a-proven-base-sync.md b/docs/adr/SADR-0018-carried-guardian-approval-across-a-proven-base-sync.md\nindex 22358562da390ed8b45cd600da08a43d378b4680..e6e2907cefa776460af73ddf7d39db605f3c39b0\n--- a/docs/adr/0018-carried-guardian-approval-across-a-proven-base-sync.md\n+++ b/docs/adr/SADR-0018-carried-guardian-approval-across-a-proven-base-sync.md\n@@ -1,4 +1,4 @@\n-# ADR 0018: Carried Guardian Approval Across A Proven Base Sync\n+# SADR-0018: Carried Guardian Approval Across A Proven Base Sync\n \n ## Status\n \n@@ -34,7 +34,7 @@\n Guardian evaluator that consumes it (`shiki_guardian.evaluate_guardian_approval`,\n the `guardian_comment_carried` source) were built and tested in isolation. Neither\n does anything until a caller passes the flags that turn the carry on, so the\n-feature was invisible in CI. This ADR records the decision the two of them\n+feature was invisible in CI. This SADR records the decision the two of them\n implement, and this task wires it into the four CI invocations that judge Guardian\n approval.\n \ndiff --git a/docs/adr/README.md b/docs/adr/README.md\nindex c70e057a4520c36e042ab68c53cd4a21ef709b71..5da43856836e2ce6a150f8127766e2f88355ab4b\n--- a/docs/adr/README.md\n+++ b/docs/adr/README.md\n@@ -1,40 +1,41 @@\n-# Architecture Decision Records\n+# Decision Record Namespaces\n \n-This directory holds Shiki's Architecture Decision Records (ADRs): durable\n-records of hard-to-reverse platform decisions. ADRs are part of the Shiki source\n-of truth alongside GitHub state, the `.shiki/` mirror, and `CONTEXT.md`.\n+This directory holds two independent decision namespaces:\n \n-## How ADRs Work\n+- Shiki Architecture Decision Records (SADRs), named\n+  `SADR-NNNN-<short-slug>.md`, record hard-to-reverse Shiki platform decisions.\n+- Target Architecture Decision Records (ADRs), named\n+  `NNNN-<short-slug>.md`, belong to the target product.\n \n-- New ADRs are added as `docs/adr/NNNN-<short-slug>.md`, where `NNNN` is the next\n-  zero-padded sequence number.\n-- The **canonical list of ADRs is the set of files matching `docs/adr/*.md`** in\n-  this directory. Browse `docs/adr/*.md` for the authoritative, current set.\n-- Use [`template.md`](template.md) as the starting point for a new ADR.\n-- Record an ADR when a decision is hard to reverse, surprising without context,\n-  and a real tradeoff. Do not write an ADR for routine or easily reversible\n-  choices.\n+The same four-digit number may appear once in each namespace. A platform\n+`SADR-NNNN` citation resolves only to `SADR-NNNN-*.md`; a target `ADR NNNN`\n+citation resolves only to `NNNN-*.md`.\n \n-Using the `docs/adr/*.md` glob as the source of truth means other PRs can add an\n-ADR without editing this index file, avoiding merge contention on a\n-hand-maintained enumeration.\n+## How Decision Records Work\n \n-## Current ADRs (snapshot)\n-\n-The list below is a convenience snapshot and may lag behind the directory. When\n-in doubt, list `docs/adr/*.md`.\n+- New Shiki platform decisions use the next zero-padded SADR number.\n+- New target product decisions use the target's own ADR sequence.\n+- The canonical list is the files in `docs/adr/`; no hand-maintained index is\n+  authoritative.\n+- Use [`template.md`](template.md) as the starting point and choose the filename\n+  namespace that owns the decision.\n+- Record a decision when it is hard to reverse, surprising without context, and\n+  a real tradeoff. Do not write a decision record for routine or easily\n+  reversible choices.\n \n-- [0001 \u2014 Keep Shiki As The Platform Name](0001-keep-shiki-as-platform-name.md)\n-- [0002 \u2014 Use AGENTS As The Shared Agent Constitution](0002-use-agents-as-shared-constitution.md)\n-- [0003 \u2014 Require Skill Gate For MergeGate Readiness](0003-require-skill-gate-for-mergegate-readiness.md)\n-- [0004 \u2014 Use GitHub First With A Repository-Local Shiki Mirror](0004-use-github-first-with-shiki-mirror.md)\n-- [0005 \u2014 Use Codex As Front And Claude Code Action As The GitHub Runtime](0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md)\n-- [0006 \u2014 Separate Codex Implementation, CCA Completion, And MergeGate Authorization](0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md)\n+Using the directory as the source of truth lets independent changes add records\n+without contending on an enumerated index.\n \n-## Adding A New ADR\n+## Adding A Shiki SADR\n \n-1. Copy `template.md` to `docs/adr/NNNN-<short-slug>.md`.\n+1. Copy `template.md` to `docs/adr/SADR-NNNN-<short-slug>.md`.\n 2. Fill in the title, status, context, decision, and consequences.\n-3. Set the status to `Proposed`, then `Accepted` once the decision is approved.\n-4. Open a PR following `CONTRIBUTING.md`. You do not need to edit this index for\n-   the ADR to be discoverable via `docs/adr/*.md`.\n+3. Set the status to `Proposed`, then `Accepted` once the proper authority has\n+   approved the decision.\n+4. Open a PR following `CONTRIBUTING.md`.\n+\n+## Adding A Target ADR\n+\n+1. Copy `template.md` to `docs/adr/NNNN-<short-slug>.md` using the target's own\n+   sequence.\n+2. Cite it as `ADR NNNN`; do not prefix its filename with `SADR-`.\ndiff --git a/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md b/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\nnew file mode 100644\nindex 0000000000000000000000000000000000000000..7455ce1f3bed02ddddb851ac21a1d1a648c9aeba\n--- /dev/null\n+++ b/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\n@@ -0,0 +1,55 @@\n+# SADR-0019: Separate Shiki Platform SADRs From Target ADRs\n+\n+## Status\n+\n+Accepted\n+\n+## Context\n+\n+Shiki installs its platform decision records into `docs/adr/` in every target\n+repository. Shiki and its targets previously used the same numeric\n+`NNNN-<slug>.md` namespace. A target that already owned ADRs 0013 through 0018\n+therefore acquired two records for each number during installation, making an\n+`ADR NNNN` citation ambiguous. Renumbering target decisions would rewrite target\n+history, while renumbering Shiki decisions per target would destroy their stable\n+platform identity.\n+\n+Upgrades also need to remove the former Shiki-owned numeric paths without ever\n+mistaking a target-owned ADR for platform content. The install stamp already\n+records exact shipped paths and content digests, so it can prove ownership only\n+when both still match.\n+\n+## Decision\n+\n+Shiki platform decisions use the `SADR` namespace:\n+\n+- records are named `docs/adr/SADR-NNNN-<slug>.md`;\n+- live platform citations use `SADR-NNNN`;\n+- target product decisions retain `docs/adr/NNNN-<slug>.md` and `ADR NNNN`;\n+- the reference validator resolves the two namespaces independently, so the same\n+  four-digit number may exist once in each namespace.\n+\n+Before a forced upgrade writes anything, the installer inventories the exact\n+former numeric Shiki ADR paths. It may delete a legacy path only when the\n+existing install stamp contains that exact path and its recorded SHA-256 digest\n+matches the file currently on disk. All ownership blockers are reported\n+together and abort the upgrade before its first write. A non-forced install\n+never performs this cleanup.\n+\n+Historical `.shiki/` evidence remains unchanged because it records the namespace\n+that existed when that evidence was produced.\n+\n+## Consequences\n+\n+- Targets may use any ADR number without colliding with Shiki's platform SADRs.\n+- Fresh installs preserve target ADR bytes and install a stable SADR set.\n+- Forced upgrades fail closed when legacy ownership cannot be proven, and a\n+  successful upgrade refreshes the stamp without removed legacy paths.\n+- Current governance, packaging, workflow, prompt, script, and skill references\n+  must move with the platform records.\n+\n+## Alternatives Considered\n+\n+- Collision-only refusal leaves affected targets permanently unupgradable.\n+- Renumbering target ADRs mutates target-owned history.\n+- Per-target Shiki renumbering makes platform citations non-portable.\ndiff --git a/docs/adr/template.md b/docs/adr/template.md\nindex a92a97daddd0b00ab9dd626cc8d86942da2eafed..17dd79175b5fee2105935557f691b40cf9492942\n--- a/docs/adr/template.md\n+++ b/docs/adr/template.md\n@@ -1,14 +1,14 @@\n-# NNNN: <Short Decision Title>\n+# <SADR-NNNN or NNNN>: <Short Decision Title>\n \n ## Status\n \n-Proposed | Accepted | Superseded by ADR-NNNN\n+Proposed | Accepted | Superseded by SADR-NNNN or target ADR NNNN\n \n ## Context\n \n What is the situation, constraint, or force that requires a decision? Reference\n-relevant domain terms (`CONTEXT.md`), prior ADRs, and code reality. State the\n-problem, not the solution.\n+relevant domain terms (`CONTEXT.md`), prior Shiki SADRs or target ADRs, and code\n+reality. State the problem, not the solution.\n \n ## Decision\n \ndiff --git a/docs/agents/autonomous-loop-e2e-acceptance.md b/docs/agents/autonomous-loop-e2e-acceptance.md\nindex bbf84c6081987f159ac6ad9ecf3c82b7aa72e7e8..5c13c1d02a05a90b0dc46cbef7c95a9846d0c237\n--- a/docs/agents/autonomous-loop-e2e-acceptance.md\n+++ b/docs/agents/autonomous-loop-e2e-acceptance.md\n@@ -40,8 +40,8 @@\n 3. `gh auth status` is logged in with merge permission on the repo.\n 4. Branch protection + required checks are configured exactly as the goal-loop\n    required-check set expects (see `docs/agents/branch-protection-smoke.md`).\n-5. The repo `.shiki/policy` permits autonomous low/medium-risk merges (ADR\n-   0008/0009); the MergeGate policy check is the recorded-authority gate.\n+5. The repo `.shiki/policy` permits autonomous low/medium-risk merges\n+   (SADR-0008/0009); the MergeGate policy check is the recorded-authority gate.\n \n The operator's only actions are: register the task and start the loop. **No\n manual pre-clearing** means after `shiki loop run` starts, the operator does not\ndiff --git a/docs/agents/bootstrap-command.md b/docs/agents/bootstrap-command.md\nindex 05f7f203a52cf412b9f97084a6e131ed0f785afd..f7aa0b734dfebee4a3b139335b66ee3415f48713\n--- a/docs/agents/bootstrap-command.md\n+++ b/docs/agents/bootstrap-command.md\n@@ -189,7 +189,9 @@\n ```\n \n Use this only for tests, fixtures, or explicit local-only template inspection.\n-Use `--force` only when you intentionally want to overwrite existing target files.\n+Use `--force` only when you intentionally want to refresh the target template.\n+Legacy numeric Shiki decision cleanup is stamp-and-digest authorized before any\n+write; target ADR paths are not cleanup candidates.\n \n ## Slash Command\n \ndiff --git a/docs/agents/checklists.md b/docs/agents/checklists.md\nindex 4a7f03ade346f53a1c1d82981f3fd744f9ab4547..0c5ec6c53dc7197b7b7a7d18e667b325babd6c45\n--- a/docs/agents/checklists.md\n+++ b/docs/agents/checklists.md\n@@ -32,18 +32,18 @@\n | ID | Item | Blocking |\n | --- | --- | --- |\n | GD-01 | Plan was challenged against `CONTEXT.md`. | blocking for non-trivial Goals |\n-| GD-02 | Relevant ADRs were read or absence was recorded. | blocking for non-trivial Goals |\n+| GD-02 | Relevant SADRs and target ADRs were read or absence was recorded. | blocking for non-trivial Goals |\n | GD-03 | Ambiguous terms were resolved or converted to HITL blockers. | blocking |\n | GD-04 | Edge scenarios were considered. | advisory |\n | GD-05 | Code reality was checked when code could answer. | blocking when applicable |\n | GD-06 | New domain terms were added to `CONTEXT.md` when needed. | blocking when applicable |\n-| GD-07 | ADR was created or explicitly skipped using the ADR criteria. | blocking when applicable |\n+| GD-07 | A SADR or target ADR was created or explicitly skipped using the decision-record criteria. | blocking when applicable |\n \n ## CI \u2014 Context & Impact Checklist\n \n | ID | Item | Blocking |\n | --- | --- | --- |\n-| CI-01 | Relevant docs and ADRs identified. | blocking |\n+| CI-01 | Relevant docs, SADRs, and target ADRs identified. | blocking |\n | CI-02 | Relevant modules, interfaces, seams, callers, and tests identified. | blocking |\n | CI-03 | Dependencies and blocked-by relationships identified. | blocking |\n | CI-04 | Candidate locks identified. | blocking |\n@@ -62,7 +62,7 @@\n | PRD-04 | Implementation decisions are recorded without volatile code detail. | blocking |\n | PRD-05 | Testing decisions are recorded. | blocking |\n | PRD-06 | Out-of-scope items are explicit. | blocking |\n-| PRD-07 | Relevant glossary/ADR links are present when applicable. | blocking |\n+| PRD-07 | Relevant glossary/SADR/target-ADR links are present when applicable. | blocking |\n \n ## SF \u2014 Spec Freeze Checklist\n \n@@ -98,7 +98,7 @@\n | H-02 | Branch/worktree target is present. | blocking |\n | H-03 | Scope and non-goals are present. | blocking |\n | H-04 | Dependencies and locks are present. | blocking |\n-| H-05 | Relevant docs, ADRs, modules, and tests are referenced. | blocking |\n+| H-05 | Relevant docs, SADRs, target ADRs, modules, and tests are referenced. | blocking |\n | H-06 | Required skills are stated. | blocking |\n | H-07 | TDD expectations are stated. | blocking when behavior work |\n | H-08 | Acceptance criteria are included. | blocking |\ndiff --git a/docs/agents/codex-handoff.md b/docs/agents/codex-handoff.md\nindex ede468f47cb277f69a857e8c45df703e4ed0151d..6f97bdffb67e44d7edea2c71e01ea28933c4eb6c\n--- a/docs/agents/codex-handoff.md\n+++ b/docs/agents/codex-handoff.md\n@@ -46,7 +46,7 @@\n \n ## Context\n - Domain terms:\n-- Relevant ADRs:\n+- Relevant SADRs and target ADRs:\n - Relevant docs:\n - Relevant modules/interfaces/seams:\n - Similar tests/prior art:\ndiff --git a/docs/agents/completion-check-agent.md b/docs/agents/completion-check-agent.md\nindex 13e4c035db35025f4e3cdcc5adfb24314ac4d521..61bfa1218cf36ca605250c91919e620f33238aa0\n--- a/docs/agents/completion-check-agent.md\n+++ b/docs/agents/completion-check-agent.md\n@@ -32,7 +32,7 @@\n - `AGENTS.md`\n - `CLAUDE.md`\n - `CONTEXT.md`\n-- relevant ADRs\n+- relevant SADRs and target ADRs\n - parent Goal issue\n - PRD issue or document\n - task issue\ndiff --git a/docs/agents/decision-control.md b/docs/agents/decision-control.md\nindex 7d8ee5ab03a5ccbcbd04056ae61de03785a2a9c9..8c94ecda00fffaa0e92c325ab83aa9776bd1dac1\n--- a/docs/agents/decision-control.md\n+++ b/docs/agents/decision-control.md\n@@ -11,7 +11,7 @@\n | Decision | Owner | When | Required inputs | Output | Enforcement surface |\n | --- | --- | --- | --- | --- | --- |\n | Is the Goal clear enough to plan? | Planner + Guardian when needed | Goal intake | User request, existing docs, risk signals | Goal readiness verdict | Goal checklist, issue template, ledger |\n-| Must `grill-with-docs` run? | Planner / Skill Gate | Before PRD or issue split | Ambiguity, domain terms, ADR conflicts, unclear boundaries | Skill invocation or explicit skip rationale | Skill Gate checklist |\n+| Must `grill-with-docs` run? | Planner / Skill Gate | Before PRD or issue split | Ambiguity, domain terms, SADR/target-ADR conflicts, unclear boundaries | Skill invocation or explicit skip rationale | Skill Gate checklist |\n | Is a PRD required? | Planner | After Goal context stabilizes | Goal, `grill-with-docs` output, Context & Impact output | PRD required / not required | PRD checklist, CCA evidence check |\n | Can the spec be frozen? | Operator only | End of Requirements Definition, before issue split | PRD/requirements summary, scope inventory (SF-02), open-question list | `spec_freeze` block in the plan + ledger evidence | `require_grilled_plan` (plan ingest/run), SF checklist |\n | Can a frozen spec change? | Operator only (Spec Amendment) | When implementation contradicts the frozen spec | Contested decisions, scoped re-grill result | recorded amendment + re-stamped freeze | SF checklist, ledger evidence |\ndiff --git a/docs/agents/domain.md b/docs/agents/domain.md\nindex 258da8ea81e87ac0a6b0b5b5e5c2fde980eaabb7..d57b0cf17ff3e1fa9e6e5885372bf083d8295421\n--- a/docs/agents/domain.md\n+++ b/docs/agents/domain.md\n@@ -5,7 +5,9 @@\n ## Layout\n \n - `CONTEXT.md` defines Shiki language. Keep it implementation-free.\n-- `docs/adr/` records hard-to-reverse decisions.\n+- `docs/adr/SADR-NNNN-*.md` records Shiki platform decisions.\n+- `docs/adr/NNNN-*.md` records target product decisions without sharing the\n+  SADR namespace.\n - `AGENTS.md` is the runtime-neutral agent constitution.\n - `CLAUDE.md` is the Claude Code wrapper around `AGENTS.md`.\n - `.shiki/` is the Target Repository mirror for Goals, PRDs, plans, Task DAGs, contracts, locks, worktree records, ledger entries, CCA verdicts, repair packets, reports, and handoffs.\n@@ -15,6 +17,6 @@\n \n - Use Shiki terms exactly when they exist.\n - Add glossary terms to `CONTEXT.md` only when the term is part of the domain, not a generic programming concept.\n-- Create ADRs only for hard-to-reverse, surprising, tradeoff-based decisions.\n+- Create SADRs or target ADRs only for hard-to-reverse, surprising, tradeoff-based decisions.\n - Keep product-specific language in the Target Repository, not in Shiki Template docs.\n-- If an ADR conflicts with a proposed implementation, surface the conflict before editing.\n+- If a SADR or target ADR conflicts with a proposed implementation, surface the conflict before editing.\ndiff --git a/docs/agents/execution-control.md b/docs/agents/execution-control.md\nindex ad8ee3cdb36be56fedc1f46408ca92b387d74047..bc63c3a73661d12b567016ba299f1a6b66bf9088\n--- a/docs/agents/execution-control.md\n+++ b/docs/agents/execution-control.md\n@@ -63,7 +63,7 @@\n | Branch name | Orchestrator | Orchestrator | Before implementer dispatch | branch policy | branch ref in task/ledger |\n | Lock reservation | Context & Impact | MergeGate | Before dispatch and before merge | `.shiki/locks/`, labels, PR metadata | lock id, owner, scope |\n | CI profile | Planner | MergeGate | During issue triage, rechecked at PR | labels, `.shiki/policy.yaml`, workflow matrix | required check list |\n-| Required status checks | Repo maintainer / Guardian | GitHub branch protection / rulesets | Repository setup, updated via ADR | GitHub settings | branch/ruleset config, ADR |\n+| Required status checks | Repo maintainer / Guardian | GitHub branch protection / rulesets | Repository setup, updated via target ADR | GitHub settings | branch/ruleset config, target ADR |\n | CCA judgment | CCA | GitHub required check / MergeGate | Every non-draft PR update | GitHub Action check | structured CCA verdict |\n | Repair assignment | CCA | MergeGate | After failed CCA/CI/review | repair packet, issue/PR comment | repair id and attempt count |\n | Merge readiness | MergeGate | GitHub branch protection / Guardian if needed | After CCA complete | required checks, review rules, rulesets | mergegate check output |\ndiff --git a/docs/agents/guardian-policy.md b/docs/agents/guardian-policy.md\nindex 4147c7adf05a47adb4fd24ada1a1c2e1f2ba6d2d..a20d9cc6c00a569bb057b9ca8d6b51c7fc4a9847\n--- a/docs/agents/guardian-policy.md\n+++ b/docs/agents/guardian-policy.md\n@@ -98,8 +98,8 @@\n `origin/<default_branch>` and reuses each job's existing base fetch; a shallow\n (`--depth`) or second base fetch would make the proof unresolvable and silently\n disable the carry, so neither is permitted. CCA reads `guardian_comment_carried` as\n-a recorded authority in the deterministic signal and does not re-derive it. See ADR\n-0018 for the full decision, the rejected diff/patch-equality and path-disjointness\n+a recorded authority in the deterministic signal and does not re-derive it. See\n+SADR-0018 for the full decision, the rejected diff/patch-equality and path-disjointness\n alternatives, and the accepted residual.\n \n ## External AI Guardian Review (SADR-0010)\ndiff --git a/docs/agents/implementation-policy.md b/docs/agents/implementation-policy.md\nindex c754144a9fb5bf4a38aa82a60e1670c59e626b44..85c37281bcad196c6b7b02343faf00b03cd9c57a\n--- a/docs/agents/implementation-policy.md\n+++ b/docs/agents/implementation-policy.md\n@@ -31,10 +31,13 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n ## Non-Negotiables\n \n 1. **Planning precedes implementation.** Non-trivial Goals start with `grill-with-docs` and Context & Impact.\n-2. **Domain language is controlled.** Terms must be checked against `CONTEXT.md`; hard-to-reverse tradeoffs must be checked against ADRs.\n+2. **Domain language is controlled.** Terms must be checked against `CONTEXT.md`; hard-to-reverse tradeoffs must be checked against Shiki SADRs and target ADRs.\n 3. **PRD precedes issue decomposition.** `to-prd` is used once the context is settled enough to become durable intent.\n 4. **Issues are vertical slices.** `to-issues` must produce independently grabbable tracer-bullet tasks, not horizontal layer tickets.\n 5. **Triage controls dispatch.** Only `ready-for-agent` / AFK tasks may be assigned to Codex without human input.\n@@ -95,19 +98,19 @@\n Process:\n \n - Challenge terms against `CONTEXT.md`.\n-- Check ADRs before reopening decisions.\n+- Check SADRs and target ADRs before reopening decisions.\n - Inspect code when code can answer a question.\n - Ask one question at a time when the operator is available.\n - Provide a recommended answer with each question.\n - Record resolved terms in `CONTEXT.md`.\n-- Create ADRs only for hard-to-reverse, surprising, tradeoff-based decisions.\n+- Create SADRs or target ADRs only for hard-to-reverse, surprising, tradeoff-based decisions.\n - Convert unresolved load-bearing questions into HITL blockers.\n \n Outputs:\n \n - Resolved design-tree decisions.\n - Updated glossary terms when needed.\n-- ADRs when justified.\n+- SADRs or target ADRs when justified.\n - HITL blockers when decisions remain unresolved.\n \n ## Phase 3 \u2014 Context & Impact\n@@ -117,7 +120,7 @@\n \n Outputs must include:\n \n-- Relevant documents and ADRs.\n+- Relevant documents, SADRs, and target ADRs.\n - Relevant modules, interfaces, seams, callers, and tests.\n - Candidate locks.\n - Dependencies.\n@@ -140,7 +143,7 @@\n - Testing decisions.\n - Out of scope.\n - Further notes.\n-- Links to relevant Goal, domain terms, and ADRs.\n+- Links to relevant Goal, domain terms, SADRs, and target ADRs.\n \n Phase 4 ends with Spec Freeze: the operator explicitly approves the PRD, the\n plan records a `spec_freeze` block (status, approved_by, source), and the SF\n@@ -240,7 +243,7 @@\n - Dependencies and locks.\n - Risk and Guardian requirements.\n - Ledger completeness.\n-- Documentation and ADR updates.\n+- Documentation and SADR/target-ADR updates.\n - Scope drift.\n \n CCA emits one of:\n@@ -301,7 +304,7 @@\n | Evidence failure | Behavior might be done, but proof is missing. | Add checks, PR evidence, ledger entry, or artifacts. |\n | Scope drift | PR changes unrelated behavior. | Revert unrelated changes or split into new Goal. |\n | Dependency block | Upstream task or lock not complete. | Block until dependency resolves. |\n-| Architecture block | Decision needs ADR or deep-module discussion. | `grill-with-docs` or `improve-codebase-architecture`. |\n+| Architecture block | Decision needs a SADR, target ADR, or deep-module discussion. | `grill-with-docs` or `improve-codebase-architecture`. |\n | Risk block | Security/production/policy change needs human approval. | Guardian decision. |\n \n ## Phase 5.5 \u2014 Execution Decision Control\ndiff --git a/docs/agents/runtime-auth-model.md b/docs/agents/runtime-auth-model.md\nindex 6a2e1a47e81ea89b5c68a210ec07772a9374babc..3c54748d24233dbd0ba8473950282547ef4ce935\n--- a/docs/agents/runtime-auth-model.md\n+++ b/docs/agents/runtime-auth-model.md\n@@ -148,7 +148,7 @@\n \n API-key based runners may be added by a target repository as an explicit extension. They are not the default Shiki template path.\n \n-If a repo opts into API-key mode, record the exception in an ADR and update the repo's `AGENTS.md`, `.shiki/config.yaml`, and workflow permissions.\n+If a repo opts into API-key mode, record the target-specific exception in a target ADR and update the repo's `AGENTS.md`, `.shiki/config.yaml`, and workflow permissions.\n \n ## GitHub Actions Runtime Compatibility\n \ndiff --git a/docs/agents/skill-gate.md b/docs/agents/skill-gate.md\nindex c50398b0d7c60a8598f2a5c88a10015730dced13..3acffe6444617bb1d54b77d4b4708158ec735bc6\n--- a/docs/agents/skill-gate.md\n+++ b/docs/agents/skill-gate.md\n@@ -9,7 +9,7 @@\n | Skill | Trigger | Expected output |\n | --- | --- | --- |\n | `setup-matt-pocock-skills` | First configuring a repo for these skills, or when issue tracker, triage labels, or domain docs are missing. | `docs/agents/` configuration and an `Agent skills` block in `AGENTS.md` or `CLAUDE.md`. |\n-| `grill-with-docs` | Ambiguous plan, unclear terminology, boundary decision, tradeoff, or ADR-worthy design choice. | Clarified decision tree, recommended answers, updates to `CONTEXT.md` or ADRs when decisions crystallize. |\n+| `grill-with-docs` | Ambiguous plan, unclear terminology, boundary decision, tradeoff, or decision-record-worthy design choice. | Clarified decision tree, recommended answers, updates to `CONTEXT.md`, SADRs, or target ADRs when decisions crystallize. |\n | `zoom-out` | Agent lacks a higher-level architectural map. | Map of relevant modules, callers, seams, and domain concepts. |\n | `to-prd` | Settled context should become a PRD. | PRD published to the issue tracker with problem, solution, user stories, decisions, testing, and out-of-scope notes. |\n | `to-issues` | Goal, PRD, or plan must become implementation tickets. | Vertical-slice issues with acceptance criteria, dependencies, and AFK/HITL classification. |\n@@ -17,7 +17,7 @@\n | `tdd` | Feature work or bug fix has observable behavior that can be specified with tests. | Red-green-refactor loop through public interfaces, one vertical slice at a time. |\n | `code-review` | The TDD loop is green and the implementer is about to create or update a PR. Default for every implementation task. | Pre-PR self-review of the task diff with fixes applied on the task branch; findings and resolutions recorded as a ledger entry naming the skill and a `## Pre-PR code review` PR body section. |\n | `diagnose` | Hard bug, failing check, regression, flaky behavior, or performance problem. | Reproduction loop, ranked hypotheses, instrumentation, fix, regression test, post-mortem. |\n-| `improve-codebase-architecture` | Structural friction, testability, AI-navigability, deep-module opportunity, or refactor request. | Architecture candidates and deepening recommendations informed by `CONTEXT.md` and ADRs. |\n+| `improve-codebase-architecture` | Structural friction, testability, AI-navigability, deep-module opportunity, or refactor request. | Architecture candidates and deepening recommendations informed by `CONTEXT.md`, SADRs, and target ADRs. |\n | `prototype` | A throwaway logic or UI prototype is needed to answer a design question. | Clearly marked disposable prototype and captured decision. |\n \n ## MergeGate Interaction\ndiff --git a/docs/operations.md b/docs/operations.md\nindex cf1f452d4c589e38ccd58553bda0ad8684b51eb7..ace48a90ffbec9e5c0a7113bef16a4858bc44b3f\n--- a/docs/operations.md\n+++ b/docs/operations.md\n@@ -89,6 +89,16 @@\n      target history, brought forward by `shiki migrate`, not a template.\n    - **Everything else** (CLI scripts, workflows, schemas, docs) is overwritten.\n \n+   A forced upgrade also migrates the former numeric Shiki decision paths to\n+   `SADR-NNNN-*`. Before the first write, the installer inventories the exact\n+   legacy Shiki paths and authorizes each deletion only when the existing\n+   `.shiki/install-stamp.json` names that path and its SHA-256 digest still\n+   matches the file. An absent, unreadable, malformed, incomplete, or\n+   mismatching stamp reports every blocked path and stops with no filesystem\n+   change. Target-owned `NNNN-*` ADRs are not cleanup candidates. A non-force\n+   install deletes nothing and retains any still-matching legacy stamp entries\n+   so a later forced upgrade can prove ownership.\n+\n    If the target has pending migrations, `--force` refuses and writes nothing \u2014\n    apply migrations first (step 3), then re-run. The run ends with a single\n    summary of every `.new` written, so a half-upgrade is visible.\ndiff --git a/docs/releasing.md b/docs/releasing.md\nindex 10d73828e7dbd64e9d9f6b83bc84d010537ce62a..aafeee8c4d441e1e427efee2ce4f81a647f020e1\n--- a/docs/releasing.md\n+++ b/docs/releasing.md\n@@ -67,7 +67,7 @@\n CLI in `scripts/`. There are no third-party Python runtime dependencies. The CLI\n is intentionally **not** exposed as a `[project.scripts]` console entry point\n because `scripts/shiki.py` is a dependency-free shim and `scripts/` is not\n-packaged as an importable distribution (see the ADR for the rationale).\n+packaged as an importable distribution (see SADR-0007 for the rationale).\n \n Supported install paths:\n \n@@ -155,6 +155,15 @@\n    [docs/operations.md](operations.md#the-new-convention). If the target has\n    pending migrations, `--force` refuses and writes nothing; apply migrations\n    (step 2) first, then re-run.\n+\n+   The SADR namespace upgrade has an additional pre-write gate: each former\n+   numeric Shiki decision path is removed only when the existing install stamp\n+   contains that exact path and a digest matching its current bytes. The\n+   installer aggregates absent/unreadable/malformed-stamp, missing-digest, and\n+   digest-mismatch blockers before raising, so refusal leaves the target tree\n+   byte-identical. Numeric target ADRs are preserved. A successful forced\n+   upgrade installs and stamps `SADR-*` paths and drops the removed legacy paths\n+   from the refreshed stamp.\n 2. Apply repository-local `.shiki/` state migrations with the migrate command:\n \n    ```bash\ndiff --git a/docs/usage.md b/docs/usage.md\nindex b83407ee3970dd395dd893010bc5dbe8bad26051..a130ef6e40dfd8169a5bc701339351097df1a09f\n--- a/docs/usage.md\n+++ b/docs/usage.md\n@@ -67,8 +67,10 @@\n shiki install-target /path/to/target-repo --local-only\n ```\n \n-Use `--force` only when you intentionally want to overwrite existing target\n-files.\n+Use `--force` only when you intentionally want to refresh the target template.\n+For the SADR namespace migration, `--force` deletes only exact legacy Shiki\n+decision paths whose existing install-stamp digest still matches; any ownership\n+blocker stops before the first write. Numeric target ADRs are never candidates.\n \n ## `shiki bootstrap-platform` \u2014 publish the Shiki platform repo\n \ndiff --git a/scripts/guardian_approval_signal.py b/scripts/guardian_approval_signal.py\nindex d059f92b2c82aa2b5d6ca421365537a87886c1cb..16924892714f4f5b9303c0ef0e325a7e70075630\n--- a/scripts/guardian_approval_signal.py\n+++ b/scripts/guardian_approval_signal.py\n@@ -199,8 +199,8 @@\n ) -> Any:\n     \"\"\"Evaluate SADR-0015 Contract Approval for the CCA Guardian signal.\n \n-    The SAME OR that MergeGate applies must reach this deterministic signal: ADR\n-    0015 is explicit that without it, a contract-approved PR still returns\n+    The SAME OR that MergeGate applies must reach this deterministic signal:\n+    SADR-0015 is explicit that without it, a contract-approved PR still returns\n     ``needs_guardian``, the CCA job fails, and the MergeGate policy check never\n     runs \u2014 the gate this change moves would never be reached. This resolves the\n     task the SAME way MergeGate does (the first Shiki task id in the PR body),\ndiff --git a/scripts/shiki_contract_approval.py b/scripts/shiki_contract_approval.py\nindex 941bcb96fdd8b9a85bc0c4f0f04d5dad9d04839c..4a4b418f6527413161db82670bec857a043360e2\n--- a/scripts/shiki_contract_approval.py\n+++ b/scripts/shiki_contract_approval.py\n@@ -142,7 +142,7 @@\n     changed_files_status: Any,\n     registration: Any,\n ) -> ContractApprovalResult:\n-    \"\"\"Whether this PR carries an SADR-0015 Contract Approval.\n+    \"\"\"Whether this PR carries a SADR-0015 Contract Approval.\n \n     The four conditions are evaluated IN ORDER, each failure recorded as a\n     distinct reason string:\ndiff --git a/scripts/shiki_guardian.py b/scripts/shiki_guardian.py\nindex a4ecb79bbda55fd5c8b06adfae2eb99760a03c45..4dd99bbe714e26f421fa19abac7a34ae1b195ecc\n--- a/scripts/shiki_guardian.py\n+++ b/scripts/shiki_guardian.py\n@@ -12,7 +12,7 @@\n # The carry re-derives its proof from git in-process on every judging invocation;\n # it is never read from an artifact, so there is nothing to go stale or be forged.\n # shiki_sync_proof is the ONLY guardian-family module that runs git, and every one\n-# of its invocations is read-only and local (ADR: prove-from-git base sync).\n+# of its invocations is read-only and local (SADR: prove-from-git base sync).\n from shiki_sync_proof import verify_pure_base_sync\n \n GUARDIAN_POLICY_PATH = \".shiki/guardian-policy.json\"\ndiff --git a/scripts/shiki_guardian_review.py b/scripts/shiki_guardian_review.py\nindex dd0f104d457899e0ba5ff1e162011c1e90b6f92b..e10ef58c046324e7ea3a3392feb3048ca77414ce\n--- a/scripts/shiki_guardian_review.py\n+++ b/scripts/shiki_guardian_review.py\n@@ -159,7 +159,7 @@\n     docs_only = _is_docs_only(changed_files)\n     if docs_only and matched == 0:\n         focus.append(\n-            \"Docs/ADR-only change: confirm the change is genuinely documentation/decision-only, introduces no behavior or contract drift, and stays consistent with the constitution and existing ADRs.\"\n+            \"Docs/decision-record-only change: confirm the change is genuinely documentation/decision-only, introduces no behavior or contract drift, and stays consistent with the constitution and existing SADRs and target ADRs.\"\n         )\n \n     # Conservative augmentation: unknown (no category and not docs-only), mixed\ndiff --git a/scripts/shiki_installer.py b/scripts/shiki_installer.py\nindex be064ab7a29ff6836f7f7a1d70500a43e0d11d7b..9fd8eefd137ce7003dad5c0dcb2e48e2cd47ab32\n--- a/scripts/shiki_installer.py\n+++ b/scripts/shiki_installer.py\n@@ -118,6 +118,7 @@\n # what lets a target carry it without a governance-schema change.\n INSTALL_STAMP_PATH = \".shiki/install-stamp.json\"\n INSTALL_STAMP_VERSION = 1\n+LEGACY_SHIKI_SADR_MAX = 18\n \n # Under --force the shipped surface splits three ways, so nothing is lost and\n # nothing is silently stale.\n@@ -237,9 +238,98 @@\n         )\n \n \n+def legacy_shiki_adr_paths() -> tuple[str, ...]:\n+    \"\"\"Former numeric paths for Shiki SADR-0001 through SADR-0018.\n+\n+    The authoritative slugs come from the current SADR filenames. SADR-0019 is\n+    the namespace decision itself and never shipped under a numeric legacy path.\n+    \"\"\"\n+    paths: list[str] = []\n+    for sadr in sorted((ROOT / \"docs\" / \"adr\").glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\")):\n+        number = sadr.name.split(\"-\", 2)[1]\n+        if int(number) <= LEGACY_SHIKI_SADR_MAX:\n+            paths.append(f\"docs/adr/{sadr.name.removeprefix('SADR-')}\")\n+    return tuple(paths)\n+\n+\n+def preflight_legacy_sadr_cleanup(target: Path, *, force: bool) -> tuple[str, ...]:\n+    \"\"\"Authorize exact legacy Shiki ADR removals before the first write.\n+\n+    A non-force install never removes existing files. A forced install needs no\n+    stamp when no legacy Shiki path is present. When legacy paths do exist, each\n+    must be named in a readable install stamp and its current bytes must match\n+    the recorded digest.\n+    \"\"\"\n+    if not force:\n+        return ()\n+    present = tuple(\n+        relative\n+        for relative in legacy_shiki_adr_paths()\n+        if (target / relative).exists() or (target / relative).is_symlink()\n+    )\n+    if not present:\n+        return ()\n+\n+    stamp_path = target / INSTALL_STAMP_PATH\n+    try:\n+        raw = stamp_path.read_text(encoding=\"utf-8\")\n+        stamp = json.loads(raw)\n+    except FileNotFoundError:\n+        detail = \"install stamp is absent\"\n+        blockers = [f\"{relative}: {detail}\" for relative in present]\n+    except (OSError, UnicodeDecodeError) as error:\n+        detail = f\"install stamp is unreadable ({error})\"\n+        blockers = [f\"{relative}: {detail}\" for relative in present]\n+    except json.JSONDecodeError as error:\n+        detail = f\"install stamp is malformed ({error})\"\n+        blockers = [f\"{relative}: {detail}\" for relative in present]\n+    else:\n+        if not isinstance(stamp, dict):\n+            blockers = [\n+                f\"{relative}: install stamp is malformed (expected an object)\"\n+                for relative in present\n+            ]\n+        else:\n+            digests = stamp.get(\"digests\")\n+            blockers = []\n+            if not isinstance(digests, dict):\n+                blockers.extend(\n+                    f\"{relative}: install stamp has no digest for this path\"\n+                    for relative in present\n+                )\n+            else:\n+                for relative in present:\n+                    expected = digests.get(relative)\n+                    if not isinstance(expected, str) or not expected:\n+                        blockers.append(\n+                            f\"{relative}: install stamp has no digest for this path\"\n+                        )\n+                        continue\n+                    try:\n+                        actual = _sha256_file(target / relative)\n+                    except OSError as error:\n+                        blockers.append(\n+                            f\"{relative}: legacy path is unreadable ({error})\"\n+                        )\n+                        continue\n+                    if actual != expected:\n+                        blockers.append(\n+                            f\"{relative}: digest mismatch (stamp {expected}, current {actual})\"\n+                        )\n+\n+    if blockers:\n+        joined = \"\\n\".join(f\"- {blocker}\" for blocker in blockers)\n+        raise ShikiError(\n+            \"legacy Shiki ADR cleanup is not authorized:\\n\"\n+            f\"{joined}\\nNo files were written.\"\n+        )\n+    return present\n+\n+\n def install_template(target: Path, *, force: bool, validate: bool) -> list[NewFileNote]:\n     # Preconditions first: never rewrite the tree of a target that cannot be\n     # upgraded cleanly (see refuse_pending_migrations).\n+    legacy_cleanup = preflight_legacy_sadr_cleanup(target, force=force)\n     refuse_pending_migrations(target)\n \n     # Resolve the target's required CODEOWNERS owner once from its\n@@ -250,6 +340,10 @@\n     # verbatim. See ``codeowners_required_owner``.\n     codeowners_owner = codeowners_required_owner(target)\n \n+    for relative in legacy_cleanup:\n+        (target / relative).unlink()\n+        info(f\"removed stamped legacy Shiki ADR: {target / relative}\")\n+\n     notes: list[NewFileNote] = []\n     for relative in TEMPLATE_PATHS:\n         source = ROOT / relative\n@@ -647,6 +741,22 @@\n     digests = target_stamp_digests(target)\n     existing = read_install_stamp(target)\n     if existing is not None:\n+        # A non-force install deliberately leaves legacy Shiki ADRs in place.\n+        # Preserve only still-matching ownership proof so a later forced\n+        # upgrade can authorize cleanup; never carry a missing or stale digest.\n+        existing_digests = existing.get(\"digests\")\n+        if isinstance(existing_digests, dict):\n+            for relative in legacy_shiki_adr_paths():\n+                expected = existing_digests.get(relative)\n+                path = target / relative\n+                if not isinstance(expected, str) or not path.is_file():\n+                    continue\n+                try:\n+                    actual = _sha256_file(path)\n+                except OSError:\n+                    continue\n+                if actual == expected:\n+                    digests[relative] = actual\n         if _incoming_is_older(commit, existing.get(\"platform_commit\")):\n             warn(\n                 \"kept existing install stamp: incoming install \"\ndiff --git a/scripts/shiki_loop.py b/scripts/shiki_loop.py\nindex 3611641da8aeb5038b1da90e3114b09a473ec6d8..84caa491b9cad0edc4176f209c13e9221e975681\n--- a/scripts/shiki_loop.py\n+++ b/scripts/shiki_loop.py\n@@ -424,7 +424,7 @@\n         # unresolvable CCA verdict has already produced its terminal stop and is never\n         # laundered into a head-moving sync (SADR-0010). It also never fires while the\n         # policy (Guardian) gate is red \u2014 for ANY reason, not only when it is the sole\n-        # red check \u2014 so a head-bound Guardian approval, or an SADR-0015\n+        # red check \u2014 so a head-bound Guardian approval, or a SADR-0015\n         # contract-approval carry that base movement defeated (the policy gate compares\n         # base and head task governance directly), is never silently restored by\n         # adopting base wholesale. It is bounded by a durable per-task counter so the\ndiff --git a/scripts/shiki_tasks.py b/scripts/shiki_tasks.py\nindex aec3809f24b4a0c8c96fc50971cff9ae9ad30381..d531052e9d9717058d4631483cefab44c26a65c1\n--- a/scripts/shiki_tasks.py\n+++ b/scripts/shiki_tasks.py\n@@ -821,7 +821,7 @@\n         \"prompt\": prompt,\n         \"entry_skill\": \"grill-with-docs\",\n         \"required_next_steps\": [\n-            \"Run grill-with-docs until terminology, boundaries, risks, and ADR-worthy decisions are settled.\",\n+            \"Run grill-with-docs until terminology, boundaries, risks, and decision-record-worthy choices are settled.\",\n             \"Obtain the operator's explicit approval of the PRD/requirements (Spec Freeze).\",\n             \"Write a machine-readable plan JSON with grill_with_docs.status=complete and spec_freeze.status=frozen.\",\n             \"Run shiki plan ingest --plan-file PLAN.json.\",\ndiff --git a/scripts/test_shiki_goal_loop.sh b/scripts/test_shiki_goal_loop.sh\nindex 753c7b516e10f0be3bc92a4bd0eb1fa699776ae6..8f3820a0a079b3044424cfe1a6c6b7fe68896e67\n--- a/scripts/test_shiki_goal_loop.sh\n+++ b/scripts/test_shiki_goal_loop.sh\n@@ -231,8 +231,8 @@\n \n # High/critical risk merges autonomously when all required checks are green:\n # the \"MergeGate policy check\" required check IS the Guardian gate (it enforces\n-# guardian-policy.json \u2014 human approval OR external AI guardian review, ADR\n-# 0010), so green checks mean a recorded authority approved. The fake gh returns\n+# guardian-policy.json \u2014 human approval OR external AI guardian review,\n+# SADR-0010), so green checks mean a recorded authority approved. The fake gh returns\n # green checks, simulating that the Guardian gate passed.\n python3 \"$ROOT/scripts/shiki.py\" goal create --target \"$TARGET\" --title \"Guardian gate\" --outcome \"High risk merges via policy gate\" >/tmp/shiki-goal-loop-guardian-goal.json\n GGOAL=\"$(json_get /tmp/shiki-goal-loop-guardian-goal.json goal_id)\"\ndiff --git a/tests/test_adr_references.py b/tests/test_adr_references.py\nindex 51706329c797d5dd9b47e082a333fa83b261d482..db6e693e35f67103935be34f1d9bb4ff7ee60395\n--- a/tests/test_adr_references.py\n+++ b/tests/test_adr_references.py\n@@ -1,7 +1,7 @@\n-\"\"\"Every ``ADR ####`` citation must resolve to exactly one decision record.\n+\"\"\"Every target ADR and Shiki SADR citation resolves in its own namespace.\n \n Background. ``scripts/mergegate_check.py`` justifies ``is_bookkeeping_closeout``\n-with \"ADR 0017\" nine times and ``scripts/guardian_approval_signal.py`` cites\n+with \"SADR-0017\" nine times and ``scripts/guardian_approval_signal.py`` cites\n 0016/0017 seven times, yet for a stretch the numbering in ``docs/adr/`` stopped\n at 0015: the authoring commits lived on a coordinator branch and no task ever\n declared ``docs/adr/...`` in its locks, so nothing carried the records to the\n@@ -11,11 +11,12 @@\n and the directory drifted with no signal.\n \n This is the same unbound-pair shape behind other silent failures: a reference\n-and its target with nothing asserting they agree. This test binds them. It scans\n-every ``scripts/*.py`` (plus ``docs/**/*.md`` and the root ``*.md`` governance\n-files, which are cheap) for the ``ADR ####`` pattern and asserts each cited\n-number resolves to exactly one ``docs/adr/####-*.md``. The converse is\n-deliberately NOT required: an ADR may exist without being cited.\n+and its target with nothing asserting they agree. This test binds them. Target\n+``ADR NNNN`` citations resolve only to ``docs/adr/NNNN-*.md``; Shiki\n+``SADR-NNNN`` citations resolve only to ``docs/adr/SADR-NNNN-*.md``. The scan\n+covers current governance docs, scripts, workflows, prompts, packaging metadata,\n+and repo-local Codex guidance while deliberately excluding historical\n+``.shiki`` evidence. The converse is not required: a record may be uncited.\n \n The suite is dependency free: standard-library ``unittest`` only.\n \"\"\"\n@@ -31,17 +32,18 @@\n \n REPO_ROOT = Path(__file__).resolve().parents[1]\n \n-# \"ADR 0016\" -- a four-digit citation. The trailing \\b keeps a five-digit token\n-# from being read as a truncated four-digit number.\n-_CITATION = re.compile(r\"\\bADR (\\d{4})\\b\")\n-# \"0016-locks-bound-product-paths.md\" -- the leading four digits name the record.\n-_ADR_FILENAME = re.compile(r\"^(\\d{4})-.*\\.md$\")\n+# ``ADR 0016`` / ``SADR-0016`` -- a namespace plus four-digit citation. The\n+# trailing ``\\b`` keeps a five-digit token from being read as a truncated one.\n+_CITATION = re.compile(r\"\\b(S?ADR)[ -](\\d{4})\\b\")\n+# Target ADRs retain the numeric filename; Shiki platform decisions use SADR.\n+_DECISION_FILENAME = re.compile(r\"^(?:(SADR)-)?(\\d{4})-.*\\.md$\")\n \n \n class Violation(NamedTuple):\n     \"\"\"A citation that does not resolve to exactly one decision record.\"\"\"\n \n     citing_file: str  # repo-relative path of the file that cites the ADR\n+    namespace: str  # \"ADR\" for target decisions; \"SADR\" for Shiki decisions\n     number: str  # the cited four-digit number, e.g. \"0016\"\n     line: int  # 1-based line of the citation\n     reason: str  # \"missing\" (zero matches) or \"ambiguous\" (two or more)\n@@ -49,44 +51,59 @@\n \n     @property\n     def expected_path(self) -> str:\n-        return f\"docs/adr/{self.number}-*.md\"\n+        prefix = \"SADR-\" if self.namespace == \"SADR\" else \"\"\n+        return f\"docs/adr/{prefix}{self.number}-*.md\"\n \n     def describe(self) -> str:\n         \"\"\"Name the citing file, the cited number, and the missing path.\"\"\"\n         if self.reason == \"missing\":\n             return (\n-                f\"{self.citing_file}:{self.line} cites ADR {self.number} \"\n+                f\"{self.citing_file}:{self.line} cites {self.namespace} {self.number} \"\n                 f\"but no document matches {self.expected_path}\"\n             )\n         joined = \", \".join(self.matches)\n         return (\n-            f\"{self.citing_file}:{self.line} cites ADR {self.number} which \"\n+            f\"{self.citing_file}:{self.line} cites {self.namespace} {self.number} which \"\n             f\"resolves to {len(self.matches)} documents (expected exactly one \"\n             f\"at {self.expected_path}): {joined}\"\n         )\n \n \n-def _adr_index(root: Path) -> dict[str, list[Path]]:\n-    \"\"\"Map every four-digit ADR number to the record files that claim it.\"\"\"\n-    index: dict[str, list[Path]] = {}\n+def _adr_index(root: Path) -> dict[tuple[str, str], list[Path]]:\n+    \"\"\"Map each decision namespace and number to the files that claim it.\"\"\"\n+    index: dict[tuple[str, str], list[Path]] = {}\n     adr_dir = root / \"docs\" / \"adr\"\n     if adr_dir.is_dir():\n         for path in sorted(adr_dir.glob(\"*.md\")):\n-            match = _ADR_FILENAME.match(path.name)\n+            match = _DECISION_FILENAME.match(path.name)\n             if match:\n-                index.setdefault(match.group(1), []).append(path)\n+                namespace = \"SADR\" if match.group(1) else \"ADR\"\n+                index.setdefault((namespace, match.group(2)), []).append(path)\n     return index\n \n \n def _citing_files(root: Path, include_docs: bool = True) -> list[Path]:\n-    \"\"\"Files scanned for citations: scripts/*.py, and cheaply the markdown.\"\"\"\n+    \"\"\"Current, shipped text surfaces scanned for decision citations.\"\"\"\n     files = sorted((root / \"scripts\").glob(\"*.py\"))\n+    files += sorted((root / \"scripts\").glob(\"*.sh\"))\n     if include_docs:\n         docs = root / \"docs\"\n         if docs.is_dir():\n             files += sorted(docs.rglob(\"*.md\"))\n         files += sorted(root.glob(\"*.md\"))\n-    return files\n+        pyproject = root / \"pyproject.toml\"\n+        if pyproject.is_file():\n+            files.append(pyproject)\n+        for relative in (\".github\", \".codex\"):\n+            surface = root / relative\n+            if surface.is_dir():\n+                files += sorted(\n+                    path\n+                    for path in surface.rglob(\"*\")\n+                    if path.is_file()\n+                    and path.suffix in {\".md\", \".py\", \".sh\", \".toml\", \".yaml\", \".yml\"}\n+                )\n+    return sorted(set(files))\n \n \n def find_adr_reference_violations(\n@@ -94,10 +111,9 @@\n ) -> list[Violation]:\n     \"\"\"Return every ``ADR ####`` citation under ``root`` that fails to resolve.\n \n-    A citation resolves when exactly one ``docs/adr/####-*.md`` matches its\n-    number. Zero matches is a \"missing\" violation; two or more is \"ambiguous\".\n-    Records that are never cited produce nothing -- the converse is not\n-    required.\n+    A citation resolves when exactly one record in the same namespace matches\n+    its number. Zero matches is a \"missing\" violation; two or more is\n+    \"ambiguous\". Records that are never cited produce nothing.\n     \"\"\"\n     root = Path(root)\n     index = _adr_index(root)\n@@ -110,13 +126,15 @@\n         rel = path.relative_to(root).as_posix()\n         for lineno, line in enumerate(text.splitlines(), start=1):\n             for match in _CITATION.finditer(line):\n-                number = match.group(1)\n-                found = index.get(number, [])\n+                namespace, number = match.groups()\n+                found = index.get((namespace, number), [])\n                 if len(found) == 1:\n                     continue\n                 reason = \"missing\" if not found else \"ambiguous\"\n                 rels = [p.relative_to(root).as_posix() for p in found]\n-                violations.append(Violation(rel, number, lineno, reason, rels))\n+                violations.append(\n+                    Violation(rel, namespace, number, lineno, reason, rels)\n+                )\n     return violations\n \n \n@@ -129,8 +147,8 @@\n         self.assertEqual(\n             violations,\n             [],\n-            \"Every 'ADR ####' citation must resolve to exactly one \"\n-            f\"docs/adr/####-*.md:\\n{detail}\",\n+            \"Every ADR/SADR citation must resolve exactly once in its own \"\n+            f\"docs/adr namespace:\\n{detail}\",\n         )\n \n     def test_scan_actually_reads_scripts(self) -> None:\n@@ -146,8 +164,21 @@\n         # The two records this Goal carried onto the default branch, and which\n         # the scripts cite most, must be present.\n         index = _adr_index(REPO_ROOT)\n-        self.assertIn(\"0016\", index)\n-        self.assertIn(\"0017\", index)\n+        self.assertIn((\"SADR\", \"0016\"), index)\n+        self.assertIn((\"SADR\", \"0017\"), index)\n+\n+    def test_platform_tree_has_only_sadr_0001_through_0019(self) -> None:\n+        adr_dir = REPO_ROOT / \"docs\" / \"adr\"\n+        sadrs = sorted(adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\"))\n+        self.assertEqual(\n+            {path.name.split(\"-\", 2)[1] for path in sadrs},\n+            {f\"{number:04d}\" for number in range(1, 20)},\n+        )\n+        self.assertEqual(\n+            list(adr_dir.glob(\"[0-9][0-9][0-9][0-9]-*.md\")),\n+            [],\n+            \"the Shiki platform tree must not retain legacy numeric SADR paths\",\n+        )\n \n \n class FixtureAdrReferences(unittest.TestCase):\n@@ -180,6 +211,26 @@\n         self.assertIn(\"9991\", message)  # cited number\n         self.assertIn(\"docs/adr/9991-*.md\", message)  # missing path\n \n+    def test_target_adr_does_not_satisfy_missing_platform_sadr(self) -> None:\n+        root = self._make_repo(\n+            adr_names=[\"0042-target-decision.md\"],\n+            script_body='\"\"\"Target ADR 0042 exists, but platform SADR-0042 does not.\"\"\"\\n',\n+        )\n+        violations = find_adr_reference_violations(root, include_docs=False)\n+        self.assertEqual(len(violations), 1)\n+        self.assertEqual(violations[0].namespace, \"SADR\")\n+        self.assertEqual(violations[0].number, \"0042\")\n+        self.assertEqual(violations[0].expected_path, \"docs/adr/SADR-0042-*.md\")\n+\n+    def test_target_adr_and_platform_sadr_may_reuse_a_number(self) -> None:\n+        root = self._make_repo(\n+            adr_names=[\"0042-target-decision.md\", \"SADR-0042-platform-decision.md\"],\n+            script_body='\"\"\"Target ADR 0042 and platform SADR-0042 are distinct.\"\"\"\\n',\n+        )\n+        self.assertEqual(\n+            find_adr_reference_violations(root, include_docs=False), []\n+        )\n+\n     def test_uncited_adr_is_allowed(self) -> None:\n         # An ADR exists but nothing cites it -> not a violation. The converse\n         # (every ADR must be cited) is explicitly NOT required.\ndiff --git a/tests/test_guardian_review.py b/tests/test_guardian_review.py\nindex 88829e7810212635e2fc98aa5918d6f26294677d..00a85a237acd5bf2d6cb5b294128198c422402ca\n--- a/tests/test_guardian_review.py\n+++ b/tests/test_guardian_review.py\n@@ -162,7 +162,7 @@\n \n     def test_docs_only_is_light_and_not_conservative(self):\n         focus = gr.classify_review_focus([\"docs/adr/0099-x.md\", \"README.md\"], \"\", \"low\")\n-        self.assertTrue(any(\"Docs/ADR-only\" in f for f in focus))\n+        self.assertTrue(any(\"Docs/decision-record-only\" in f for f in focus))\n         self.assertFalse(any(\"conservative default\" in f.lower() for f in focus))\n \n     def test_unknown_category_is_conservative(self):\ndiff --git a/tests/test_policy_job_merged_prs.py b/tests/test_policy_job_merged_prs.py\nindex d45512e74681e5e649279236c7d4e79d5c3da471..b97989f8bd74d091de1a6b5275a20d8dc68c4512\n--- a/tests/test_policy_job_merged_prs.py\n+++ b/tests/test_policy_job_merged_prs.py\n@@ -3,8 +3,8 @@\n \n ``.github/workflows/shiki-cca-completion.yml`` has two consumers of the merged-PR\n proof: the CCA *signal* (``guardian_approval_signal.py`` in \"Prepare CCA context\")\n-and the authoritative gate (``mergegate_check.py`` in \"Enforce MergeGate\"). ADR\n-0017's bookkeeping-closeout exemption (condition 6) needs the merged implementation\n+and the authoritative gate (``mergegate_check.py`` in \"Enforce MergeGate\").\n+SADR-0017's bookkeeping-closeout exemption (condition 6) needs the merged implementation\n PR proven, and a closeout carries NO ``post_merge_reconcile`` marker. The signal\n already resolves ``MERGED_PRS`` unconditionally, but \"Enforce MergeGate\" used to\n resolve it only inside ``grep -q '<!-- shiki:post_merge_reconcile -->'``. So a\ndiff --git a/tests/test_sadr_install_migration.py b/tests/test_sadr_install_migration.py\nnew file mode 100644\nindex 0000000000000000000000000000000000000000..a6f4c57e4dcaef73ef600b0d4804744138ab9071\n--- /dev/null\n+++ b/tests/test_sadr_install_migration.py\n@@ -0,0 +1,229 @@\n+\"\"\"SADR installation and collision-safe legacy upgrade behavior.\"\"\"\n+\n+from __future__ import annotations\n+\n+import contextlib\n+import hashlib\n+import io\n+import json\n+import shutil\n+import tempfile\n+import unittest\n+from pathlib import Path\n+\n+import shiki_test_support  # noqa: F401  (path bootstrap)\n+\n+import shiki_installer\n+from tests.test_adr_references import find_adr_reference_violations\n+\n+\n+PRODUCT_NUMBERS = tuple(range(13, 19))\n+\n+\n+def _quiet_install(target: Path, *, force: bool, validate: bool) -> None:\n+    with contextlib.redirect_stdout(io.StringIO()):\n+        shiki_installer.install_template(target, force=force, validate=validate)\n+\n+\n+def _sha256(path: Path) -> str:\n+    return hashlib.sha256(path.read_bytes()).hexdigest()\n+\n+\n+def _legacy_sadr_paths() -> tuple[str, ...]:\n+    return tuple(\n+        f\"docs/adr/{path.name.removeprefix('SADR-')}\"\n+        for path in sorted((shiki_installer.ROOT / \"docs\" / \"adr\").glob(\"SADR-*.md\"))\n+        if path.name != \"SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\"\n+    )\n+\n+\n+def _create_legacy_upgrade_fixture(\n+    target: Path,\n+) -> tuple[dict[str, bytes], tuple[str, ...]]:\n+    adr_dir = target / \"docs\" / \"adr\"\n+    adr_dir.mkdir(parents=True)\n+    product_bytes = {\n+        f\"{number:04d}-cubeshare-product-decision.md\": (\n+            f\"# CubeShare ADR {number:04d}\\n\\nDo not rewrite {number}.\\n\"\n+        ).encode()\n+        for number in PRODUCT_NUMBERS\n+    }\n+    for name, content in product_bytes.items():\n+        (adr_dir / name).write_bytes(content)\n+\n+    legacy_paths = _legacy_sadr_paths()\n+    for relative in legacy_paths:\n+        (target / relative).write_bytes(f\"legacy Shiki bytes for {relative}\\n\".encode())\n+\n+    stamp_path = target / shiki_installer.INSTALL_STAMP_PATH\n+    stamp_path.parent.mkdir(parents=True, exist_ok=True)\n+    stamp = {\n+        \"version\": shiki_installer.INSTALL_STAMP_VERSION,\n+        \"platform_commit\": None,\n+        \"installed_at\": \"2000-01-01T00:00:00+00:00\",\n+        \"digests\": {\n+            relative: _sha256(target / relative) for relative in legacy_paths\n+        },\n+    }\n+    stamp_path.write_text(\n+        json.dumps(stamp, indent=2, sort_keys=True) + \"\\n\", encoding=\"utf-8\"\n+    )\n+    return product_bytes, legacy_paths\n+\n+\n+def _tree_snapshot(root: Path) -> dict[str, tuple[str, bytes | str]]:\n+    \"\"\"Recursive byte/shape snapshot used to prove preflight is write-free.\"\"\"\n+    snapshot: dict[str, tuple[str, bytes | str]] = {}\n+    for path in sorted(root.rglob(\"*\")):\n+        relative = path.relative_to(root).as_posix()\n+        if path.is_symlink():\n+            snapshot[relative] = (\"symlink\", path.readlink().as_posix())\n+        elif path.is_dir():\n+            snapshot[relative] = (\"directory\", \"\")\n+        else:\n+            snapshot[relative] = (\"file\", path.read_bytes())\n+    return snapshot\n+\n+\n+class FreshSadrInstallTests(unittest.TestCase):\n+    def setUp(self) -> None:\n+        self.tmp = Path(tempfile.mkdtemp(prefix=\"shiki-sadr-fresh-\"))\n+        self.addCleanup(lambda: shutil.rmtree(self.tmp, ignore_errors=True))\n+        self.target = self.tmp / \"target\"\n+        self.adr_dir = self.target / \"docs\" / \"adr\"\n+        self.adr_dir.mkdir(parents=True)\n+        self.product_bytes = {\n+            f\"{number:04d}-product-decision.md\": (\n+                f\"# Product ADR {number:04d}\\n\\nTarget-owned bytes {number}.\\n\"\n+            ).encode()\n+            for number in PRODUCT_NUMBERS\n+        }\n+        for name, content in self.product_bytes.items():\n+            (self.adr_dir / name).write_bytes(content)\n+\n+    def test_fresh_install_preserves_product_adrs_and_installs_all_sadrs(self) -> None:\n+        _quiet_install(self.target, force=False, validate=True)\n+\n+        for name, expected in self.product_bytes.items():\n+            self.assertEqual((self.adr_dir / name).read_bytes(), expected)\n+\n+        sadr_numbers = {\n+            path.name.split(\"-\", 2)[1]\n+            for path in self.adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\")\n+        }\n+        self.assertEqual(sadr_numbers, {f\"{number:04d}\" for number in range(1, 20)})\n+\n+        numeric_records = {\n+            path.name\n+            for path in self.adr_dir.glob(\"[0-9][0-9][0-9][0-9]-*.md\")\n+        }\n+        self.assertEqual(numeric_records, set(self.product_bytes))\n+        self.assertEqual(find_adr_reference_violations(self.target), [])\n+\n+\n+class LegacySadrUpgradeTests(unittest.TestCase):\n+    def setUp(self) -> None:\n+        self.tmp = Path(tempfile.mkdtemp(prefix=\"shiki-sadr-upgrade-\"))\n+        self.addCleanup(lambda: shutil.rmtree(self.tmp, ignore_errors=True))\n+        self.target = self.tmp / \"target\"\n+        self.adr_dir = self.target / \"docs\" / \"adr\"\n+        self.product_bytes, self.legacy_paths = _create_legacy_upgrade_fixture(\n+            self.target\n+        )\n+\n+    def test_force_upgrade_deletes_only_stamped_legacy_sadrs(self) -> None:\n+        _quiet_install(self.target, force=True, validate=True)\n+\n+        for relative in self.legacy_paths:\n+            self.assertFalse((self.target / relative).exists(), relative)\n+        for name, expected in self.product_bytes.items():\n+            self.assertEqual((self.adr_dir / name).read_bytes(), expected)\n+\n+        sadrs = list(self.adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\"))\n+        self.assertEqual(len(sadrs), 19)\n+        refreshed = json.loads(\n+            (self.target / shiki_installer.INSTALL_STAMP_PATH).read_text(encoding=\"utf-8\")\n+        )\n+        for relative in self.legacy_paths:\n+            self.assertNotIn(relative, refreshed[\"digests\"])\n+        for path in sadrs:\n+            relative = path.relative_to(self.target).as_posix()\n+            self.assertEqual(refreshed[\"digests\"][relative], _sha256(path))\n+\n+    def test_non_force_install_never_deletes_legacy_files(self) -> None:\n+        before = {\n+            relative: (self.target / relative).read_bytes()\n+            for relative in self.legacy_paths\n+        }\n+        _quiet_install(self.target, force=False, validate=False)\n+        for relative, expected in before.items():\n+            self.assertEqual((self.target / relative).read_bytes(), expected)\n+        refreshed = json.loads(\n+            (self.target / shiki_installer.INSTALL_STAMP_PATH).read_text(encoding=\"utf-8\")\n+        )\n+        for relative in self.legacy_paths:\n+            self.assertEqual(refreshed[\"digests\"][relative], _sha256(self.target / relative))\n+\n+\n+class LegacyCleanupPreflightTests(unittest.TestCase):\n+    def setUp(self) -> None:\n+        self.tmp = Path(tempfile.mkdtemp(prefix=\"shiki-sadr-preflight-\"))\n+        self.addCleanup(lambda: shutil.rmtree(self.tmp, ignore_errors=True))\n+\n+    def test_all_ownership_blockers_abort_before_any_write(self) -> None:\n+        cases = (\n+            (\"absent\", \"install stamp is absent\"),\n+            (\"unreadable\", \"install stamp is unreadable\"),\n+            (\"malformed\", \"install stamp is malformed\"),\n+            (\"missing-digest\", \"install stamp has no digest\"),\n+            (\"digest-mismatch\", \"digest mismatch\"),\n+        )\n+        for name, expected_reason in cases:\n+            with self.subTest(name=name):\n+                target = self.tmp / name\n+                _, legacy_paths = _create_legacy_upgrade_fixture(target)\n+                stamp_path = target / shiki_installer.INSTALL_STAMP_PATH\n+\n+                if name == \"absent\":\n+                    stamp_path.unlink()\n+                elif name == \"unreadable\":\n+                    stamp_path.unlink()\n+                    stamp_path.mkdir()\n+                elif name == \"malformed\":\n+                    stamp_path.write_text(\"{not-json\\n\", encoding=\"utf-8\")\n+                else:\n+                    stamp = json.loads(stamp_path.read_text(encoding=\"utf-8\"))\n+                    if name == \"missing-digest\":\n+                        stamp[\"digests\"] = {}\n+                    else:\n+                        stamp[\"digests\"] = {\n+                            relative: \"deadbeef\" for relative in legacy_paths\n+                        }\n+                    stamp_path.write_text(\n+                        json.dumps(stamp, indent=2, sort_keys=True) + \"\\n\",\n+                        encoding=\"utf-8\",\n+                    )\n+\n+                before = _tree_snapshot(target)\n+                with self.assertRaises(shiki_installer.ShikiError) as raised:\n+                    _quiet_install(target, force=True, validate=False)\n+                after = _tree_snapshot(target)\n+\n+                self.assertEqual(before, after, f\"{name} preflight changed the tree\")\n+                message = str(raised.exception)\n+                self.assertIn(expected_reason, message)\n+                self.assertIn(\"No files were written\", message)\n+                for relative in legacy_paths:\n+                    self.assertIn(relative, message)\n+\n+    def test_absent_stamp_is_allowed_when_no_legacy_path_exists(self) -> None:\n+        target = self.tmp / \"no-legacy\"\n+        target.mkdir()\n+        _quiet_install(target, force=True, validate=False)\n+        self.assertTrue(\n+            (target / \"docs\" / \"adr\" / \"SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\").is_file()\n+        )\n+\n+\n+if __name__ == \"__main__\":\n+    unittest.main()\n\n2026-08-07T08:18:05.237573Z ERROR codex_models_manager::manager: failed to renew cache TTL: missing field `base_instructions` at line 94 column 5\n succeeded in 91559ms:\n----------------------------------------------------------------------\nRan 1146 tests in 91.483s\n\nOK\nGuardian approval status for PR #130 (mizutani-140/shiki)\nHead SHA: aaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa\nResult: NOT APPROVED\n\nSatisfied approval sources:\n  - guardian_label (by mizutani-140)\n\nMissing / what the gate is waiting for:\n  - Guardian approval requires a configured Guardian review or current-head Guardian comment\n\nHead SHA stability:\n  - clean: safe to request approval against this head SHA\n\nPaste-ready Guardian approval comment (a configured Guardian pastes this verbatim):\n----------8<----------\nGuardian approval granted\n\naaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa\n---------->8----------\n{\n  \"handoff_file\": \"/private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/tmpbp03wino/repo/.shiki/handoffs/RP-20260807T081728001530Z-1f835502-repair.md\",\n  \"ledger_id\": \"L-20260807T081728015958Z-8e8d4067\",\n  \"repair_id\": \"RP-20260807T081728001530Z-1f835502\"\n}\n{\n  \"handoff_file\": \"/private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/tmpffg8fm5f/repo/.shiki/handoffs/RP-20260807T081729879684Z-1886c86a-repair.md\",\n  \"ledger_id\": \"L-20260807T081729893863Z-63c5dc7e\",\n  \"repair_id\": \"RP-20260807T081729879684Z-1886c86a\"\n}\n{\n  \"handoff_file\": \"/private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/tmpifpj3x2u/repo/.shiki/handoffs/RP-20260807T081731234502Z-bc7456e5-repair.md\",\n  \"ledger_id\": \"L-20260807T081731248601Z-0e4c73bd\",\n  \"repair_id\": \"RP-20260807T081731234502Z-bc7456e5\"\n}\n{\n  \"handoff_file\": \"/private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/tmpybtzenb7/repo/.shiki/handoffs/RP-20260807T081743263122Z-f1d853b7-repair.md\",\n  \"ledger_id\": \"L-20260807T081743279005Z-0adf6a2f\",\n  \"repair_id\": \"RP-20260807T081743263122Z-f1d853b7\"\n}\n{\n  \"handoff_file\": \"/private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/tmps_hmp9ae/repo/.shiki/handoffs/RP-20260807T081744486572Z-9fe148b3-repair.md\",\n  \"ledger_id\": \"L-20260807T081744500764Z-475c6417\",\n  \"repair_id\": \"RP-20260807T081744486572Z-9fe148b3\"\n}\n{\n  \"handoff_file\": \"/private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/tmpbj_kw7m0/repo/.shiki/handoffs/RP-20260807T081745036834Z-bdad85f3-repair.md\",\n  \"ledger_id\": \"L-20260807T081745052057Z-2b43fd23\",\n  \"repair_id\": \"RP-20260807T081745036834Z-bdad85f3\"\n}\n{\n  \"handoff_file\": \"/private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/tmpov3pwegy/repo/.shiki/handoffs/RP-20260807T081745684841Z-057eaf4d-repair.md\",\n  \"ledger_id\": \"L-20260807T081745698293Z-fcce98f2\",\n  \"repair_id\": \"RP-20260807T081745684841Z-057eaf4d\"\n}\n{\n  \"handoff_file\": \"/private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/tmpwyu5elur/repo/.shiki/handoffs/RP-20260807T081746295839Z-324a4f06-repair.md\",\n  \"ledger_id\": \"L-20260807T081746309802Z-ab0202de\",\n  \"repair_id\": \"RP-20260807T081746295839Z-324a4f06\"\n}\n{\n  \"handoff_file\": \"/private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/tmpr00g4n4q/repo/.shiki/handoffs/RP-20260807T081746647621Z-372c13c5-repair.md\",\n  \"ledger_id\": \"L-20260807T081746660990Z-9b2063b1\",\n  \"repair_id\": \"RP-20260807T081746647621Z-372c13c5\"\n}\n{\n  \"handoff_file\": \"/private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/tmpp0kpdb8t/repo/.shiki/handoffs/RP-20260807T081747115063Z-32e1b9fc-repair.md\",\n  \"ledger_id\": \"L-20260807T081747129359Z-7e1b8f19\",\n  \"repair_id\": \"RP-20260807T081747115063Z-32e1b9fc\"\n}\n[shiki] token verified: ok\n[shiki] token verified: ok\n{\n  \"repo\": \"owner/name\",\n  \"secret\": \"CLAUDE_CODE_OAUTH_TOKEN\",\n  \"source\": \"env:SHIKI_TEST_TOK\",\n  \"verified\": true\n}\n\ncodex\nThe full Python suite is green: 1,146 tests passed. I\u2019m now running the handoff\u2019s remaining local gates on the same worktree\u2014control-plane, adversarial state, and `validate_shiki`\u2014before reviewing the complete diff.\nexec\n/bin/zsh -lc 'bash scripts/test_shiki_adversarial_state.sh' in /private/tmp/shiki-adr-collision.pa0KwY/.worktrees/shiki-t-20260807t073307221254z-2d22d654-introduc\nexec\n/bin/zsh -lc 'bash scripts/test_shiki_control_plane.sh' in /private/tmp/shiki-adr-collision.pa0KwY/.worktrees/shiki-t-20260807t073307221254z-2d22d654-introduc\nexec\n/bin/zsh -lc 'python3 scripts/validate_shiki.py --target .' in /private/tmp/shiki-adr-collision.pa0KwY/.worktrees/shiki-t-20260807t073307221254z-2d22d654-introduc\n succeeded in 266ms:\nShiki validation passed\n\n succeeded in 5090ms:\nadversarial state/evidence/lock regression suite passed\n\ndiff --git a/.codex/skills/shiki/SKILL.md b/.codex/skills/shiki/SKILL.md\nindex c8e2e85d9d75d49cc4f2d6286ee22659c4e53e75..93cded623b2c15d9f65c76491a1c2d813ac9e6c0\n--- a/.codex/skills/shiki/SKILL.md\n+++ b/.codex/skills/shiki/SKILL.md\n@@ -18,6 +18,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n Codex CLI does not register this skill as a custom `/shiki` slash command. When\n the user types `/shiki` in Codex CLI and gets \"Unrecognized command\", explain\n that this is expected: invoke Shiki in Codex with natural language such as\n@@ -69,7 +72,7 @@\n \n ## Responsibilities\n \n-- Claude Code implements and repairs by default (`shiki runner claude`, ADR 0008); Codex implements and repairs tasks explicitly assigned to `codex`.\n+- Claude Code implements and repairs by default (`shiki runner claude`, SADR-0008); Codex implements and repairs tasks explicitly assigned to `codex`.\n - Claude Code Action can act as GitHub-side CCA or reviewer.\n - CCA judges completion.\n - MergeGate authorizes state transitions and merge readiness.\ndiff --git a/.github/prompts/cca-completion-check.md b/.github/prompts/cca-completion-check.md\nindex ed203ad5f302ecc81372d8ab27d2b1232ca97497..fd89950528333ef6242e6d5443904f5392ab7e56\n--- a/.github/prompts/cca-completion-check.md\n+++ b/.github/prompts/cca-completion-check.md\n@@ -19,6 +19,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n ## Required Reading\n \n Read only the files needed to judge the PR contract. Prefer this order:\ndiff --git a/.github/prompts/codex-task-handoff.md b/.github/prompts/codex-task-handoff.md\nindex 695e211d76dea1c60e8d5bb360e78487808286a4..59441d576b01a39569722f226c854e5a740e9eb5\n--- a/.github/prompts/codex-task-handoff.md\n+++ b/.github/prompts/codex-task-handoff.md\n@@ -2,7 +2,7 @@\n \n You are Codex Front acting as the Implementer for a Shiki task.\n \n-Follow `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant ADRs, and `docs/agents/codex-handoff.md`.\n+Follow `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant Shiki SADRs and target ADRs, and `docs/agents/codex-handoff.md`.\n \n Rules:\n \ndiff --git a/AGENTS.md b/AGENTS.md\nindex 7f041100d49c2a7525f51cb0f5d696a4a6d1c78c..a06bfbabc6fa48a007389e25e12d7ed523936bed\n--- a/AGENTS.md\n+++ b/AGENTS.md\n@@ -22,7 +22,7 @@\n - **Codex Front** is an optional implementer through Codex App, Codex CLI, Codex IDE extension, or Codex Web signed in with ChatGPT OAuth/subscription auth, dispatched with `shiki runner codex` when a task is explicitly assigned to `codex`.\n - **GitHub CCA** is implemented by Claude Code Action by default, using `CLAUDE_CODE_OAUTH_TOKEN`.\n \n-Do not assume `openai/codex-action`, `OPENAI_API_KEY`, or API-key based Codex automation in the default Shiki path. API-key runners are explicit target-repo extensions and require their own ADR.\n+Do not assume `openai/codex-action`, `OPENAI_API_KEY`, or API-key based Codex automation in the default Shiki path. API-key runners are explicit target-repo extensions and require their own target ADR.\n \n Shiki is not:\n \n@@ -44,10 +44,14 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use\n+`SADR-NNNN-*.md`; target product decisions retain `NNNN-*.md` and the `ADR`\n+namespace.\n+\n Use the canonical source-of-truth order above. Runtime-specific reading orders\n are operational guidance only and must not override or reorder it.\n \n-Conversation state is not durable truth. If a decision matters, put it in GitHub, `.shiki/`, `CONTEXT.md`, or an ADR.\n+Conversation state is not durable truth. If a decision matters, put it in GitHub, `.shiki/`, `CONTEXT.md`, a Shiki SADR, or a target ADR.\n \n If GitHub and `.shiki/` disagree, surface the conflict and prefer GitHub operational state until the mirror is repaired.\n \n@@ -75,8 +79,8 @@\n Every non-trivial change follows this loop:\n \n 1. **Goal**: clarify outcome, completion conditions, non-goals, risk level, and success signals.\n-2. **grill-with-docs**: challenge the plan against domain docs, ADRs, code reality, terminology, and edge scenarios.\n-3. **Context & Impact**: find relevant docs, ADRs, code, dependencies, owners, locks, and verification surfaces.\n+2. **grill-with-docs**: challenge the plan against domain docs, Shiki SADRs, target ADRs, code reality, terminology, and edge scenarios.\n+3. **Context & Impact**: find relevant docs, SADRs, target ADRs, code, dependencies, owners, locks, and verification surfaces.\n 4. **PRD and Spec Freeze**: use `to-prd` when context is settled enough to become durable product/engineering intent. The operator's explicit approval of the PRD is Spec Freeze: the plan records a `spec_freeze` block, and no plan runs without it. Steps 1-4 form Requirements Definition \u2014 one continuous operator dialogue. After Spec Freeze, scope changes require an operator-approved Spec Amendment; non-scope-moving interpretations go to the Assumption Log.\n 5. **Issues**: use `to-issues` to create independently grabbable vertical-slice issues. Prefer AFK slices over HITL slices where possible.\n 6. **Triage**: label issues for readiness, risk, runtime, skills, and MergeGate state.\n@@ -167,7 +171,7 @@\n - Testing decisions.\n - Out-of-scope items.\n - Further notes.\n-- Links to relevant domain terms and ADRs.\n+- Links to relevant domain terms, SADRs, and target ADRs.\n \n Every executable task must include:\n \n@@ -193,7 +197,7 @@\n \n Context & Impact must identify:\n \n-- Relevant documents, domain terms, ADRs, and past decisions.\n+- Relevant documents, domain terms, SADRs, target ADRs, and past decisions.\n - Relevant modules, interfaces, seams, callers, owners, and tests.\n - Dependency relationships between tasks.\n - Candidate locks and lock conflicts.\n@@ -210,7 +214,7 @@\n | Skill | Required when |\n | --- | --- |\n | `setup-matt-pocock-skills` | First configuring the repo for skills, or when issue tracker, triage labels, or domain docs are missing. |\n-| `grill-with-docs` | Default for non-trivial Goals before PRD/issues; always required when plans, terminology, boundaries, tradeoffs, or ADR-worthy decisions are ambiguous. |\n+| `grill-with-docs` | Default for non-trivial Goals before PRD/issues; always required when plans, terminology, boundaries, tradeoffs, or decision-record-worthy choices are ambiguous. |\n | `zoom-out` | The agent lacks an architectural map of the relevant code area. |\n | `to-prd` | Settled Goal context must become durable product/engineering intent. |\n | `to-issues` | A Goal, PRD, or plan must become independently grabbable vertical-slice issues. |\n@@ -348,7 +352,7 @@\n - Three or more new files.\n - Any high-risk or critical label.\n \n-When the architecture gate triggers, run `grill-with-docs` or `improve-codebase-architecture` as appropriate and record durable decisions in an ADR when needed.\n+When the architecture gate triggers, run `grill-with-docs` or `improve-codebase-architecture` as appropriate and record durable decisions in a Shiki SADR or target ADR as applicable.\n \n ## Engineering Discipline\n \ndiff --git a/CLAUDE.md b/CLAUDE.md\nindex 98a932e7841a632866fcd53b0bad0e3af3711d3d..98a31399404d6fb044b4fd493f4459ac87f439de\n--- a/CLAUDE.md\n+++ b/CLAUDE.md\n@@ -14,6 +14,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n ## Claude Role\n \n Default to planner, default implementer, reviewer, coordinator, documentation editor, CCA-style completion judge, and final-judgment assistant.\n@@ -57,7 +60,7 @@\n At the start of a Shiki session:\n \n 1. Read `AGENTS.md`.\n-2. Read `CONTEXT.md` and relevant ADRs.\n+2. Read `CONTEXT.md` and relevant Shiki SADRs and target ADRs.\n 3. Read active `.shiki/` Goal/task/ledger state when present.\n 4. Read active distilled rules from `.shiki/memories/` (status `distilled`, `active: true`, not revoked or superseded) for applicable guidance; the same rules are injected deterministically into task handoffs as the `## Distilled Rules` section (\u00a73.5).\n 5. Read the GitHub Issue or PR when the task is GitHub-backed.\n@@ -74,7 +77,7 @@\n - Define the Goal, completion conditions, non-goals, and risk level.\n - Treat `grill-with-docs` as the default planning entry point for non-trivial Goals.\n - Challenge domain terms against `CONTEXT.md`.\n-- Challenge hard-to-reverse tradeoffs against ADRs.\n+- Challenge hard-to-reverse tradeoffs against SADRs and target ADRs.\n - Explore code instead of asking when code can answer the question.\n - Run Context & Impact before implementation planning.\n - Produce a Task DAG, not an unordered checklist.\n@@ -94,7 +97,7 @@\n - For each question, provide your recommended answer.\n - Resolve dependency decisions before downstream design questions.\n - Update `CONTEXT.md` inline when terminology crystallizes.\n-- Offer ADRs only when the decision is hard to reverse, surprising without context, and a real tradeoff.\n+- Offer a SADR or target ADR only when the decision is hard to reverse, surprising without context, and a real tradeoff.\n - Convert unresolved questions into HITL blockers rather than allowing Codex to guess.\n \n ## PRD and Issues Standard\n@@ -103,7 +106,7 @@\n \n - Use the project's domain glossary vocabulary.\n - Avoid volatile file paths unless they are essential.\n-- Include implementation decisions, testing decisions, out-of-scope items, and relevant ADR/domain links.\n+- Include implementation decisions, testing decisions, out-of-scope items, and relevant SADR/target-ADR/domain links.\n - Identify deep-module opportunities and testable interfaces.\n \n When creating issues:\n@@ -121,7 +124,7 @@\n - Branch/worktree target.\n - Scope and non-goals.\n - Dependencies and locks.\n-- Relevant docs, ADRs, modules, and tests.\n+- Relevant docs, SADRs, target ADRs, modules, and tests.\n - Required skill invocations.\n - TDD expectations.\n - Pre-PR `code-review` gate expectations.\n@@ -152,7 +155,7 @@\n \n - Treat yourself as a judge, not implementer.\n - Do not write production code.\n-- Read `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant ADRs, PRD, task issue, PR body, diff, checks, reviews, labels, and `.shiki/` state.\n+- Read `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant SADRs and target ADRs, PRD, task issue, PR body, diff, checks, reviews, labels, and `.shiki/` state.\n - Evaluate every applicable checklist in `docs/agents/checklists.md`.\n - Map each acceptance criterion to durable evidence.\n - Distinguish implementation failure from missing evidence.\ndiff --git a/CONTEXT.md b/CONTEXT.md\nindex e61a9be06a0cce0d026484083a657bde2067e234..237ee8aa3ea8fab334ece49768bc5ed3b5af0974\n--- a/CONTEXT.md\n+++ b/CONTEXT.md\n@@ -25,7 +25,7 @@\n _Avoid_: jumping directly into code\n \n **grill-with-docs**:\n-A planning skill that challenges a plan against domain language, ADRs, code reality, and concrete edge scenarios. It resolves design-tree questions before PRD/issues. It runs inside Requirements Definition.\n+A planning skill that challenges a plan against domain language, Shiki SADRs, target ADRs, code reality, and concrete edge scenarios. It resolves design-tree questions before PRD/issues. It runs inside Requirements Definition.\n _Avoid_: generic brainstorming, silent assumptions\n \n **Requirements Definition**:\ndiff --git a/CONTRIBUTING.md b/CONTRIBUTING.md\nindex c6d9b656e73abb8a55e5f660123e03153dcb3e6d..9e37a8fbf668db5b22461aaae5548b8f1354394c\n--- a/CONTRIBUTING.md\n+++ b/CONTRIBUTING.md\n@@ -17,7 +17,7 @@\n 1. **Goal** \u2014 start from a user-approved Goal with an outcome, completion\n    conditions, non-goals, risk level, and success signals.\n 2. **grill-with-docs** \u2014 for any non-trivial change, challenge the plan against\n-   domain language, ADRs, and code reality before writing a PRD.\n+   domain language, Shiki SADRs, target ADRs, and code reality before writing a PRD.\n 3. **PRD** \u2014 capture settled product/engineering intent (`to-prd`).\n 4. **Spec Freeze** \u2014 the operator explicitly approves the PRD/requirements;\n    the plan records a `spec_freeze` block and no plan runs without it\n@@ -94,7 +94,7 @@\n   entries.\n - Do not hand-edit `.shiki/goals/*.json` or `.shiki/manifest.json` in a task PR.\n - Conversation state is not durable truth. If a decision matters, record it in\n-  GitHub, `.shiki/`, `CONTEXT.md`, or an ADR.\n+  GitHub, `.shiki/`, `CONTEXT.md`, a Shiki SADR, or a target ADR.\n \n ## Architecture Gate\n \n@@ -103,7 +103,7 @@\n or CI policy, package structure, three or more structural files, more than 100\n net new lines in a core module, three or more new files, or any high-risk label.\n When triggered, run `grill-with-docs` or `improve-codebase-architecture` and\n-record durable decisions in an ADR when needed (see `docs/adr/`).\n+record durable decisions in a Shiki SADR or target ADR as applicable (see `docs/adr/`).\n \n ## Safety\n \ndiff --git a/SYSTEM_PROMPT.md b/SYSTEM_PROMPT.md\nindex aaaea45e3a896c3a44ba89eec8bb84325ecf6c62..da009c86a88285269dee0155e58e550c6d744ea9\n--- a/SYSTEM_PROMPT.md\n+++ b/SYSTEM_PROMPT.md\n@@ -30,6 +30,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n Use the canonical source-of-truth order above. Runtime-specific reading orders\n are operational guidance only and must not override or reorder it. The current\n conversation is non-durable operator input.\n@@ -48,14 +51,14 @@\n \n A runtime may only do work that is explicitly assigned by the Goal, task, PR, or MergeGate decision.\n \n-Do not assume `openai/codex-action`, `OPENAI_API_KEY`, or API-key based Codex automation in the default Shiki path. API-key runners are explicit target-repo extensions and require their own ADR.\n+Do not assume `openai/codex-action`, `OPENAI_API_KEY`, or API-key based Codex automation in the default Shiki path. API-key runners are explicit target-repo extensions and require their own target ADR.\n \n ## 4. Non-negotiable Shiki flow\n \n For any non-trivial Goal, use this flow:\n \n 1. **Goal Seek**: define outcome, non-goals, risk level, completion criteria, and evidence requirements.\n-2. **grill-with-docs**: challenge the plan against `CONTEXT.md`, ADRs, code reality, terminology, boundaries, and edge scenarios. Ask one question at a time when the operator is available. If code can answer a question, inspect code instead of asking.\n+2. **grill-with-docs**: challenge the plan against `CONTEXT.md`, Shiki SADRs, target ADRs, code reality, terminology, boundaries, and edge scenarios. Ask one question at a time when the operator is available. If code can answer a question, inspect code instead of asking.\n 3. **Context & Impact**: identify modules, interfaces, seams, callers, dependencies, locks, risk, and verification surfaces.\n 4. **to-prd + Spec Freeze**: turn settled context into a PRD using domain vocabulary and testing decisions. The operator's explicit approval of the PRD is Spec Freeze, recorded as the plan's `spec_freeze` block; plans without it do not run. Steps 1-4 form Requirements Definition.\n 5. **to-issues**: decompose the PRD into independently grabbable vertical-slice issues. Prefer AFK slices; mark HITL slices when judgment is still required.\n@@ -75,7 +78,7 @@\n Before making any material change:\n \n 1. Read `AGENTS.md` and any runtime-specific wrapper such as `CLAUDE.md`.\n-2. Read `CONTEXT.md` and relevant ADRs.\n+2. Read `CONTEXT.md` and relevant SADRs and target ADRs.\n 3. Read the GitHub Issue / PR and `.shiki/` state for the active Goal or task.\n 4. Identify your role: Planner, Implementer, Reviewer, Completion Check Agent, Repairer, or Guardian-assist.\n 5. Identify Goal id, task id, branch, PR, dependencies, locks, risk level, required skills, and required evidence.\n@@ -103,7 +106,7 @@\n \n At minimum, identify:\n \n-- Relevant documents, ADRs, domain terms, and prior decisions.\n+- Relevant documents, SADRs, target ADRs, domain terms, and prior decisions.\n - Relevant modules, interfaces, seams, callers, owners, and tests.\n - Dependency relationships between tasks.\n - Candidate file locks and lock conflicts.\n@@ -152,7 +155,7 @@\n \n CCA must read:\n \n-- `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant ADRs.\n+- `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant SADRs and target ADRs.\n - Parent Goal issue, PRD issue, task issue, comments, and labels.\n - PR body, diff, commits, changed files, reviews, CI checks, and artifacts available to the workflow.\n - `.shiki/` task contract, locks, ledger entries, CCA checklist profile, and prior repair packets.\n@@ -214,7 +217,7 @@\n - Three or more new files.\n - Any high-risk or critical label.\n \n-When the architecture gate triggers, run `grill-with-docs` or `improve-codebase-architecture` as appropriate and record the decision in an ADR if it is hard to reverse or likely to be re-litigated.\n+When the architecture gate triggers, run `grill-with-docs` or `improve-codebase-architecture` as appropriate and record the decision in a Shiki SADR or target ADR as applicable if it is hard to reverse or likely to be re-litigated.\n \n ## 14. Skill Gate\n \n@@ -223,7 +226,7 @@\n Use these skills:\n \n - `setup-matt-pocock-skills`: before first use of the engineering skill set, or when issue tracker, triage labels, or domain docs are missing.\n-- `grill-with-docs`: default for non-trivial Goals before PRD/issues; required for ambiguous plans, terminology, boundaries, ADR-worthy tradeoffs, or design-tree decisions.\n+- `grill-with-docs`: default for non-trivial Goals before PRD/issues; required for ambiguous plans, terminology, boundaries, decision-record-worthy tradeoffs, or design-tree decisions.\n - `zoom-out`: unfamiliar code area or missing architectural map.\n - `to-prd`: convert settled context into a PRD.\n - `to-issues`: break a PRD, plan, or Goal into independently grabbable vertical-slice issues.\ndiff --git a/docs/adr/0001-keep-shiki-as-platform-name.md b/docs/adr/SADR-0001-keep-shiki-as-platform-name.md\nindex 20a9637c3b9d8c97de7ba8db7cfae4d82d9a7dba..8b685439959aada1b221c112f0ac3c51ca8a1695\n--- a/docs/adr/0001-keep-shiki-as-platform-name.md\n+++ b/docs/adr/SADR-0001-keep-shiki-as-platform-name.md\n@@ -1,3 +1,3 @@\n-# Keep Shiki As The Platform Name\n+# SADR-0001: Keep Shiki As The Platform Name\n \n Shiki remains the product name for the GitHub-native agentic engineering control plane. We considered more descriptive names such as MergePilot, MergeGate, and Kiroku, but kept Shiki because it carries the existing goal-seeking framework identity while remaining independent of Hermes, Codex, Claude Code, or any single runtime.\ndiff --git a/docs/adr/0002-use-agents-as-shared-constitution.md b/docs/adr/SADR-0002-use-agents-as-shared-constitution.md\nindex 73de30ecb078120eac540d9bc51855e3cd48d0e3..c5486182bf06fca36ea508496b013c67302fd989\n--- a/docs/adr/0002-use-agents-as-shared-constitution.md\n+++ b/docs/adr/SADR-0002-use-agents-as-shared-constitution.md\n@@ -1,3 +1,3 @@\n-# Use AGENTS As The Shared Agent Constitution\n+# SADR-0002: Use AGENTS As The Shared Agent Constitution\n \n Shiki uses `AGENTS.md` as the runtime-neutral constitution and `CLAUDE.md` as a thin Claude Code wrapper. This keeps Codex, Claude Code, Hermes Runner, and future Agent Runtimes aligned on the same GitHub-first MergeGate rules while still allowing runtime-specific instructions where needed.\ndiff --git a/docs/adr/0003-require-skill-gate-for-mergegate-readiness.md b/docs/adr/SADR-0003-require-skill-gate-for-mergegate-readiness.md\nindex 424be0c9af4c8f0136f1fde68a468c47ff84e764..7fc337987c7df9792f75f181dfcbed75fe5ffff2\n--- a/docs/adr/0003-require-skill-gate-for-mergegate-readiness.md\n+++ b/docs/adr/SADR-0003-require-skill-gate-for-mergegate-readiness.md\n@@ -1,3 +1,3 @@\n-# Require Skill Gate For MergeGate Readiness\n+# SADR-0003: Require Skill Gate For MergeGate Readiness\n \n Shiki requires relevant engineering skills to be invoked before execution when their triggers apply. This makes skills part of MergeGate readiness rather than optional prompt style, because Shiki depends on repeatable planning, issue decomposition, TDD, diagnosis, triage, and architecture review before autonomous or parallel agent work can be trusted.\ndiff --git a/docs/adr/0004-use-github-first-with-shiki-mirror.md b/docs/adr/SADR-0004-use-github-first-with-shiki-mirror.md\nindex d0a3a9174dc646a945a9d325954ffaf00c352040..a79c54ae9ee8eed2a0524192b864da4e22a92d3f\n--- a/docs/adr/0004-use-github-first-with-shiki-mirror.md\n+++ b/docs/adr/SADR-0004-use-github-first-with-shiki-mirror.md\n@@ -1,4 +1,4 @@\n-# ADR 0004: Use GitHub First With A Repository-Local Shiki Mirror\n+# SADR-0004: Use GitHub First With A Repository-Local Shiki Mirror\n \n ## Status\n \ndiff --git a/docs/adr/0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md b/docs/adr/SADR-0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\nindex b339baf49b89d9290e79c3d51b36e7b2bde3bd95..0f3c3eb9afccdb3403ce978e21d04fe12e495da0\n--- a/docs/adr/0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\n+++ b/docs/adr/SADR-0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\n@@ -1,8 +1,8 @@\n-# ADR 0005: Use Codex As Front And Claude Code Action As The GitHub Runtime\n+# SADR-0005: Use Codex As Front And Claude Code Action As The GitHub Runtime\n \n ## Status\n \n-Accepted (implementer default superseded by ADR 0008; the auth model and the\n+Accepted (implementer default superseded by SADR-0008; the auth model and the\n Claude Code Action GitHub runtime decision remain in force)\n \n ## Context\n@@ -26,4 +26,4 @@\n - Shiki avoids requiring `OPENAI_API_KEY` for the default workflow.\n - Codex work happens through the user's authenticated Codex front surface, not as a hidden backend Action.\n - Claude Code Action is configured for OAuth-token based subscription usage by default.\n-- API-key based automation remains possible only as an explicit target-repo extension with its own ADR.\n+- API-key based automation remains possible only as an explicit target-repo extension with its own target ADR.\ndiff --git a/docs/adr/0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md b/docs/adr/SADR-0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\nindex 4bcae9d1e52f0a37b8e9fbafa5cd14b6ee1993bd..7a38538a6ae9929567f0ade67e7ea6a60d0b3f7e\n--- a/docs/adr/0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\n+++ b/docs/adr/SADR-0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\n@@ -1,4 +1,4 @@\n-# ADR 0006: Separate Codex Implementation, CCA Completion, And MergeGate Authorization\n+# SADR-0006: Separate Codex Implementation, CCA Completion, And MergeGate Authorization\n \n ## Status\n \ndiff --git a/docs/adr/0007-packaging-and-release.md b/docs/adr/SADR-0007-packaging-and-release.md\nindex 79b84ddb97167372b44641e5b7860e400ce80822..bb445a2e2e856d9d1f5fdbe35de8d4e8cec0fb32\n--- a/docs/adr/0007-packaging-and-release.md\n+++ b/docs/adr/SADR-0007-packaging-and-release.md\n@@ -1,4 +1,4 @@\n-# ADR 0007: Packaging And Release For Shiki\n+# SADR-0007: Packaging And Release For Shiki\n \n ## Status\n \n@@ -25,7 +25,7 @@\n   not introduce third-party runtime dependencies or break the shim.\n - Releases must run inside the operator's subscription-authenticated toolchain\n   and must not require extra secrets beyond the built-in `GITHUB_TOKEN`\n-  (consistent with ADR 0005's no-API-key-by-default stance).\n+  (consistent with SADR-0005's no-API-key-by-default stance).\n - `.shiki/manifest.json` already encodes the install surface (`install.include`,\n   `install.create_directories`, `install.exclude_from_commit`), and it must not\n   be mutated as part of this change.\n@@ -34,7 +34,7 @@\n \n This is a hard-to-reverse platform decision (it sets the version source of\n truth, the release trigger, and the public install contract), so it is recorded\n-as an ADR.\n+as a SADR.\n \n ## Decision\n \n@@ -104,7 +104,7 @@\n 3. **Publishing to PyPI.** Rejected for now: Shiki is distributed as a\n    repository template + git-installable CLI, not a PyPI library; PyPI would add\n    credentials/secrets and a different versioning contract. Can be added later as\n-   its own ADR if a packaged distribution is needed.\n+   its own SADR if a packaged distribution is needed.\n 4. **Dynamic version (read `VERSION` via a build backend `dynamic` field).**\n    Rejected to keep `pyproject.toml` backend-agnostic and dependency-free; the\n    small duplication between `VERSION` and `pyproject.toml` is guarded by the\n@@ -123,4 +123,4 @@\n   releasing versions the whole platform but ships only `install.include` to\n   targets.\n - Future PyPI distribution or a console entry point are deferred and would each\n-  warrant their own ADR.\n+  warrant their own SADR.\ndiff --git a/docs/adr/0008-use-claude-code-as-default-implementer.md b/docs/adr/SADR-0008-use-claude-code-as-default-implementer.md\nindex dedf4510b7c7799521422f622029563d373db031..35c2625d908167fbc58855b5b63e283805f167e8\n--- a/docs/adr/0008-use-claude-code-as-default-implementer.md\n+++ b/docs/adr/SADR-0008-use-claude-code-as-default-implementer.md\n@@ -1,12 +1,12 @@\n-# ADR 0008: Use Claude Code As The Default Implementer Runtime\n+# SADR-0008: Use Claude Code As The Default Implementer Runtime\n \n ## Status\n \n-Proposed (supersedes ADR 0005 for the implementer default; keeps its auth model)\n+Proposed (supersedes SADR-0005 for the implementer default; keeps its auth model)\n \n ## Context\n \n-ADR 0005 made Codex Front the default implementation runtime, with Claude Code\n+SADR-0005 made Codex Front the default implementation runtime, with Claude Code\n restricted to planner and reviewer roles by the runtime registry, and the only\n autonomous implementation adapter was `shiki runner codex`.\n \n@@ -18,13 +18,13 @@\n - The platform's planning, review, and CCA layers already run on Claude Code,\n   and Claude Code's native capabilities (the built-in `/code-review` skill and\n   the Workflow multi-agent orchestration tool) have no Codex equivalent.\n-- The Goal lifecycle redesign (ADR 0009) requires an autonomous post-freeze\n+- The Goal lifecycle redesign (SADR-0009) requires an autonomous post-freeze\n   loop driven from the operator's Claude Code session, including bounded\n   repair dispatch, which must not depend on a runtime that cannot be invoked\n   unattended.\n \n The constitution claims Shiki is runtime-agnostic (\"not a Claude-only\n-workflow\", ADR 0002). Any change to the implementer default must preserve the\n+workflow\", SADR-0002). Any change to the implementer default must preserve the\n registry-based runtime model rather than hardcoding one vendor.\n \n ## Decision\n@@ -39,7 +39,7 @@\n   the task to review or repair-needed.\n - Keep Codex registered as an optional implementer runtime. `shiki runner\n   codex`, the Codex handoff templates, and the ChatGPT OAuth auth model from\n-  ADR 0005 remain supported for tasks explicitly assigned to `codex`.\n+  SADR-0005 remain supported for tasks explicitly assigned to `codex`.\n - Commit to mandating two Claude-native capabilities as loop contract in\n   follow-up Goals (status: the code-review gate was delivered by Goal G-B \u2014\n   skill registry, checklists item PR-12, default required_skills \u2014 while the\n@@ -60,7 +60,7 @@\n \n Out of scope: removing Codex support, changing the CCA runtime (Claude Code\n Action with `CLAUDE_CODE_OAUTH_TOKEN` stays), and API-key based automation\n-(still requires its own ADR per ADR 0005).\n+(still requires its own target ADR per SADR-0005).\n \n ## Consequences\n \n@@ -100,7 +100,7 @@\n   harness rejection of direct `codex exec` is outside Shiki's control, and the\n   plan's mandatory native capabilities cannot run on Codex at all.\n - **Remove Codex entirely**: rejected; it abandons the runtime-agnostic\n-  constitution (ADR 0002) for no operational gain and forecloses\n+  constitution (SADR-0002) for no operational gain and forecloses\n   cross-runtime adversarial review.\n - **GitHub Actions cloud implementation via claude-code-action**: rejected as\n   default; durable evidence is automatic but token cost, Node 24 workflow\ndiff --git a/docs/adr/0009-requirements-definition-and-spec-freeze.md b/docs/adr/SADR-0009-requirements-definition-and-spec-freeze.md\nindex 29b3f7871bef9203a8b4a831a65c39eb2f506e42..52e7dd69ad0aa4efe0b68c62678abbf7df02aaec\n--- a/docs/adr/0009-requirements-definition-and-spec-freeze.md\n+++ b/docs/adr/SADR-0009-requirements-definition-and-spec-freeze.md\n@@ -1,4 +1,4 @@\n-# ADR 0009: Merge Grill And Planning Into Requirements Definition With Spec Freeze\n+# SADR-0009: Merge Grill And Planning Into Requirements Definition With Spec Freeze\n \n ## Status\n \ndiff --git a/docs/adr/0010-external-ai-guardian-review.md b/docs/adr/SADR-0010-external-ai-guardian-review.md\nindex e6108bbe8bb4e73e0ff4684c8f78001a80438c4e..f3510b145664298adafb151b6f0db51b972f01ec\n--- a/docs/adr/0010-external-ai-guardian-review.md\n+++ b/docs/adr/SADR-0010-external-ai-guardian-review.md\n@@ -1,4 +1,4 @@\n-# ADR 0010: External AI Guardian Review As A First-Class Approval Authority\n+# SADR-0010: External AI Guardian Review As A First-Class Approval Authority\n \n ## Status\n \n@@ -8,7 +8,7 @@\n \n Shiki is intended to be an autonomous, recursive, self-improving agent\n framework: authority-in-the-loop, not human-in-the-loop. Until now the Guardian\n-approval required for high/critical-risk merges (guardian-policy.json, ADR\n+approval required for high/critical-risk merges (guardian-policy.json, SADR\n governance) recognized only human authority sources \u2014 a GitHub review approval,\n the `guardian:approved` label applied by a configured Guardian, or a comment\n carrying `Guardian approval granted` plus the current head SHA.\n@@ -89,7 +89,7 @@\n   autonomous design the operator intends and produced the impersonation\n   incident.\n - **Record AI approval as an operator comment**: rejected as forgery of human\n-  authority \u2014 the exact failure this ADR corrects.\n+  authority \u2014 the exact failure this SADR corrects.\n - **Deliver the artifact as a committed `.shiki/` file**: rejected; a committed\n   approval inside the PR being approved is forgeable by anyone who can push.\n - **A dedicated reviewer bot identity instead of a relayed comment**: deferred;\ndiff --git a/docs/adr/0011-autonomous-quality-gate-evidence.md b/docs/adr/SADR-0011-autonomous-quality-gate-evidence.md\nindex bca0246c5d46e44afbde15486a75eec425fc02f5..9c93eac16d8c3a6d4b11bd7224e3955102cd7db4\n--- a/docs/adr/0011-autonomous-quality-gate-evidence.md\n+++ b/docs/adr/SADR-0011-autonomous-quality-gate-evidence.md\n@@ -1,4 +1,4 @@\n-# ADR 0011: Autonomous Quality-Gate Evidence \u2014 Deterministic Facts And An Independent Verifier\n+# SADR-0011: Autonomous Quality-Gate Evidence \u2014 Deterministic Facts And An Independent Verifier\n \n ## Status\n \ndiff --git a/docs/adr/0012-autonomous-loop-closeout-to-main.md b/docs/adr/SADR-0012-autonomous-loop-closeout-to-main.md\nindex 637fad6dde4989c9c2bf0f81f676d8d29991123c..ec7d85f81030a676bcbba28b43eb365422a36eac\n--- a/docs/adr/0012-autonomous-loop-closeout-to-main.md\n+++ b/docs/adr/SADR-0012-autonomous-loop-closeout-to-main.md\n@@ -1,4 +1,4 @@\n-# 0012 \u2014 Autonomous loop pushes goal completion to main via a closeout PR\n+# SADR-0012 \u2014 Autonomous loop pushes goal completion to main via a closeout PR\n \n - Status: accepted\n - Date: 2026-06-18\ndiff --git a/docs/adr/0013-bootstrap-grants-actions-approve-pull-requests.md b/docs/adr/SADR-0013-bootstrap-grants-actions-approve-pull-requests.md\nindex b6c69d64f1c5ced346b88d6859586715b3b5bd0e..cf48ff4a6716267f539ca58edd1a852e7c7a2d64\n--- a/docs/adr/0013-bootstrap-grants-actions-approve-pull-requests.md\n+++ b/docs/adr/SADR-0013-bootstrap-grants-actions-approve-pull-requests.md\n@@ -1,4 +1,4 @@\n-# ADR 0013: Bootstrap Grants GitHub Actions Permission To Create And Approve Pull Requests\n+# SADR-0013: Bootstrap Grants GitHub Actions Permission To Create And Approve Pull Requests\n \n ## Status\n \n@@ -31,7 +31,7 @@\n security tradeoff: it lets repository automation satisfy a human-review gate. In\n a multi-maintainer repo that could let a compromised or buggy workflow\n self-approve changes. So whether bootstrap turns it on by default \u2014 and with\n-what default token scope \u2014 is an ADR-worthy decision, not an implementation\n+what default token scope \u2014 is a SADR-worthy decision, not an implementation\n detail.\n \n ## Decision\n@@ -48,7 +48,7 @@\n Specifically:\n \n - **`can_approve_pull_request_reviews` defaults to `true`.** Shiki's default\n-  posture is solo/AFK autonomous operation (ADR 0008/0009/0012), where the CCA\n+  posture is solo/AFK autonomous operation (SADR-0008/0009/0012), where the CCA\n   Review Bridge is the only path that can satisfy `required_review: true` after\n   the CCA and Guardian gates have already passed. The default makes a\n   freshly-bootstrapped repo able to run the autonomous loop end to end without a\ndiff --git a/docs/adr/0014-codex-app-external-ai-guardian-ui-adapter.md b/docs/adr/SADR-0014-codex-app-external-ai-guardian-ui-adapter.md\nindex 238db06279334caa341c117362df9271382a7fcd..65f12e9939321e1b3e389a34c822fc1549e5155d\n--- a/docs/adr/0014-codex-app-external-ai-guardian-ui-adapter.md\n+++ b/docs/adr/SADR-0014-codex-app-external-ai-guardian-ui-adapter.md\n@@ -1,4 +1,4 @@\n-# ADR 0014: Use Codex App As The External AI Guardian UI Adapter\n+# SADR-0014: Use Codex App As The External AI Guardian UI Adapter\n \n ## Status\n \ndiff --git a/docs/adr/0015-contract-approval-before-dispatch.md b/docs/adr/SADR-0015-contract-approval-before-dispatch.md\nindex fad6c656741a20a7684ba16080a7db159f63a52f..6061271a46d3f1d6ae7174477de3fb75673123a6\n--- a/docs/adr/0015-contract-approval-before-dispatch.md\n+++ b/docs/adr/SADR-0015-contract-approval-before-dispatch.md\n@@ -1,4 +1,4 @@\n-# ADR 0015: Approve The Task Contract Before Dispatch, Not The Diff After It\n+# SADR-0015: Approve The Task Contract Before Dispatch, Not The Diff After It\n \n ## Status\n \n@@ -9,7 +9,7 @@\n Guardian approval for high/critical risk is expressible only on a pull request.\n Every approval source in `guardian-policy.json` \u2014 GitHub review, the\n `guardian:approved` label, the `Guardian approval granted` comment, and the\n-`external_ai_guardian_review` artifact (ADR 0010/0014) \u2014 is bound to a PR, and\n+`external_ai_guardian_review` artifact (SADR-0010/0014) \u2014 is bound to a PR, and\n three of the four require the current head SHA. Approval therefore cannot exist\n before an implementation PR exists.\n \n@@ -81,7 +81,7 @@\n An envelope breach must surface only on the non-repairable policy gate, never on\n the repairable `MergeGate metadata check`. Otherwise the Goal Loop routes it to\n `dispatch_repair` \u2014 instructing a runner to make the breach go away \u2014 which is\n-exactly what this ADR forbids.\n+exactly what this SADR forbids.\n \n **Contract immutability.** After registration, an implementation PR must not\n change the governance fields of its own task contract (`scope`, `non_goals`,\n@@ -116,7 +116,7 @@\n fails any verdict that is not `complete`, and the signal the CCA prompt reads is\n computed from `evaluate_guardian_approval` alone. Without the OR-merge at the\n signal, a contract-approved PR still returns `needs_guardian`, the CCA job fails,\n-and the MergeGate policy check never runs \u2014 the gate this ADR changes would never\n+and the MergeGate policy check never runs \u2014 the gate this SADR changes would never\n be reached. The CCA *prompt* needs no change: a `contract_approval` entry in the\n signal's existing `sources` array satisfies its current wording.\n \n@@ -130,7 +130,7 @@\n written, and an approved contract lets its implementation PRs merge through the\n same automatic path as low/medium risk. The wasted implement-then-block cycle\n disappears. Guardian review shifts from reading diffs to reading contracts, which\n-is both earlier and smaller; the external AI Guardian path (ADR 0010/0014) works\n+is both earlier and smaller; the external AI Guardian path (SADR-0010/0014) works\n on contracts with no change.\n \n We accept that the Guardian authorizes a class of change without seeing its\n@@ -146,7 +146,7 @@\n adversarial tests for the new approval path equivalent in rigor to\n `test_shiki_governance_evidence.sh`.\n \n-Bootstrap costs, which apply only while this ADR is being implemented. The tasks\n+Bootstrap costs, which apply only while this SADR is being implemented. The tasks\n that build Contract Approval merge under the existing rules, so the ones\n dispatched after the pre-dispatch gate lands but before the approval path lands\n need a Guardian-approved Contract PR *and* a live Guardian approval on their\n@@ -210,7 +210,7 @@\n risk; they do not bound authorized work.\n \n **Auto-escalate risk on envelope breach instead of stopping.** Returns the\n-workflow to a diff-time Guardian decision \u2014 the exact behavior this ADR removes.\n+workflow to a diff-time Guardian decision \u2014 the exact behavior this SADR removes.\n \n **Resolve governance fields from the base snapshot only.** Rejected against the\n registration flow: implementation PRs add their own contract, so the base has no\ndiff --git a/docs/adr/0016-locks-bound-product-paths.md b/docs/adr/SADR-0016-locks-bound-product-paths.md\nindex b5e850f3f1f7f4ef6e7f9d1763a29a51bd300c45..46b40a9b99a7aae644c02bb458f5473ff0a90ce2\n--- a/docs/adr/0016-locks-bound-product-paths.md\n+++ b/docs/adr/SADR-0016-locks-bound-product-paths.md\n@@ -1,4 +1,4 @@\n-# ADR 0016: Locks bound product paths; the Shiki mirror is bound by record identity\n+# SADR-0016: Locks bound product paths; the Shiki mirror is bound by record identity\n \n ## Status\n \n@@ -6,8 +6,8 @@\n \n - Date: 2026-07-28\n - Deciders: operator (mizutani-140), Claude (planner)\n-- Related: ADR 0004 (GitHub-first with a `.shiki` mirror), ADR 0009 (Spec Freeze),\n-  ADR 0012 (autonomous loop closeout to main), ADR 0015 (Contract Approval before\n+- Related: SADR-0004 (GitHub-first with a `.shiki` mirror), SADR-0009 (Spec Freeze),\n+  SADR-0012 (autonomous loop closeout to main), SADR-0015 (Contract Approval before\n   dispatch), remediation goal task R-02, the live PR #179 deadlock.\n \n ## Context\n@@ -90,11 +90,11 @@\n files, documentation files and named source modules are nameable; runtime-\n allocated ledger, runner and report ids are not \u2014 and those live in `.shiki`,\n which locks no longer govern. Existing frozen contracts are brought into line by\n-a one-time Spec Amendment per plan, which must land before ADR 0015's contract\n+a one-time Spec Amendment per plan, which must land before SADR-0015's contract\n immutability makes `locks` unwritable from the implementation PR.\n \n **Lock state is resolved from the durable side.** MergeGate resolves the lock\n-registry, and ADR 0015's governance fields, from the base `.shiki` snapshot when\n+registry, and SADR-0015's governance fields, from the base `.shiki` snapshot when\n present, falling back to head only when absent. The `MergeGate metadata check`\n job is given `--base-shiki`, which it does not receive today. Locks are not\n derived from the live open-pull-request set: a pull request body is untrusted\n@@ -108,7 +108,7 @@\n Explicitly out of scope: moving MergeGate's contract resolution off the PR\n checkout wholesale; making the mirror a projection of GitHub state; introducing\n new lock kinds; moving ledger evidence out of the repository; and unwinding\n-ADR 0012's closeout pull request. Registration of multi-task goals is ADR 0015's\n+SADR-0012's closeout pull request. Registration of multi-task goals is SADR-0015's\n Contract PR and is not re-decided here.\n \n ## Consequences\n@@ -127,7 +127,7 @@\n \n Risks accepted. First, goal-scoped identity is only as strong as `goal_id`,\n which is resolved from the PR body and the head task file; the mitigation is to\n-add `goal_id` to ADR 0015's frozen governance field set, and until that lands a\n+add `goal_id` to SADR-0015's frozen governance field set, and until that lands a\n task file can name a foreign goal. Second, base-when-present resolution is\n unavailable on fork pull requests, where the head is contributor-controlled;\n fork pull requests already cannot complete a Shiki merge, and this decision does\n@@ -139,7 +139,7 @@\n path, not by implementation pull requests.\n \n New obligations: one Spec Amendment per frozen plan to rewrite `locks`, ordered\n-before ADR 0015 T1; extension of the adversarial suites in\n+before SADR-0015 T1; extension of the adversarial suites in\n `scripts/test_shiki_adversarial_state.sh` and\n `scripts/test_shiki_governance_evidence.sh` to cover the eleven newly-scoped\n directories; and updating `scripts/test_shiki_loop_lock_guard.sh`,\n@@ -151,32 +151,32 @@\n whether the loop's transitive sync of ledger `evidence` references can be\n deleted once the Contract PR carries its own source plan.\n \n-## Relationship to ADR 0004\n+## Relationship to SADR-0004\n \n-This ADR **clarifies something ADR 0004 left unspecified**. ADR 0004 decides that\n+This SADR **clarifies something SADR-0004 left unspecified**. SADR-0004 decides that\n GitHub is the operational source of truth and that each target repository keeps a\n `.shiki/` mirror. It says nothing about pull requests writing the mirror, does\n not name MergeGate, and does not make the mirror an input to a gate. Nothing in\n-ADR 0004's Decision or Consequences is amended, superseded or weakened: the\n+SADR-0004's Decision or Consequences is amended, superseded or weakened: the\n mirror still exists, still records the same artifacts, and pull request\n-boundaries remain the coordination surface. What this ADR settles is the question\n+boundaries remain the coordination surface. What this SADR settles is the question\n 0004 never reached \u2014 by what authority a pull request may write a mirror record \u2014\n and it answers it the way `.shiki/manifest.json` already declares\n (`pr_mutation: current-task-or-current-goal-only`).\n \n-It **amends ADR 0012**, whose Decision item 5 states that \"Loop-executed tasks\n+It **amends SADR-0012**, whose Decision item 5 states that \"Loop-executed tasks\n declare the `path:.shiki/**` lock, which covers every `.shiki` file the closeout\n stages ... so `files_outside_locks` is satisfied without per-file lock\n bookkeeping.\" That clause is replaced: the closeout's mirror writes are\n-authorized by record identity, not by a declared lock. ADR 0012's substantive\n+authorized by record identity, not by a declared lock. SADR-0012's substantive\n decision \u2014 that completion reaches `main` through a closeout pull request rather\n than local attestation \u2014 is preserved unchanged.\n \n-It is **complementary to ADR 0015**, not a substitute. ADR 0015 owns contract\n+It is **complementary to SADR-0015**, not a substitute. SADR-0015 owns contract\n immutability, the pre-dispatch gate, the Contract PR and the Scope Envelope. This\n-ADR owns what a lock means and who may write the mirror. The one ordering\n+SADR owns what a lock means and who may write the mirror. The one ordering\n constraint between them is that the lock-narrowing Spec Amendment must precede\n-ADR 0015 T1's freezing of the `locks` field.\n+SADR-0015 T1's freezing of the `locks` field.\n \n ## Alternatives Considered\n \n@@ -186,7 +186,7 @@\n the only writer that can register them may run. Migrating the Guardian signal to\n that store makes every registration PR fail closed to `needs_guardian` at every\n risk level, because the task file is by definition absent from the store, which\n-removes the low/medium autonomous path ADR 0008/0009 exists to provide. And it\n+removes the low/medium autonomous path SADR-0008/0009 exists to provide. And it\n leaves schemas, manifest, config, guardian policy, templates, migrations, plans,\n worktrees, runner records, runs and handoffs with no writer at all in a\n repository whose product is `.shiki`. Measured benefit on the frozen backlog:\n@@ -199,14 +199,14 @@\n namespace is not in fact ownership-partitioned \u2014 56 ledger entries carry no\n `task_id`, and one goal DAG is writable by 35 registered tasks. The correct\n expression of its insight is that locks do not govern the mirror at all, which is\n-what this ADR decides.\n+what this SADR decides.\n \n **Make the mirror a projection written only by a registration writer and a\n post-merge projection job.** Rejected. It requires a bypass actor with write\n access to the protected default branch; it makes its own master invariant\n readable from a manifest that pull requests may edit; it deletes both reconcile\n modes and leaves a corrupted mirror with no in-band repair path, contradicting\n-ADR 0004's consequence that mirror conflicts are repaired against GitHub state;\n+SADR-0004's consequence that mirror conflicts are repaired against GitHub state;\n and its content-derived ids are rejected by `validate_shiki`'s currently-green id\n pattern.\n \ndiff --git a/docs/adr/0017-bookkeeping-closeouts-do-not-inherit-risk.md b/docs/adr/SADR-0017-bookkeeping-closeouts-do-not-inherit-risk.md\nindex 554e3a0d526a72ca067108c5464cc68c3edcf174..b9676857a4713850e063af7c39c8e0b01ecb4c66\n--- a/docs/adr/0017-bookkeeping-closeouts-do-not-inherit-risk.md\n+++ b/docs/adr/SADR-0017-bookkeeping-closeouts-do-not-inherit-risk.md\n@@ -1,4 +1,4 @@\n-# ADR 0017: A Bookkeeping Closeout Does Not Inherit Its Task's Risk\n+# SADR-0017: A Bookkeeping Closeout Does Not Inherit Its Task's Risk\n \n ## Status\n \n@@ -6,7 +6,7 @@\n \n ## Context\n \n-ADR 0012 made the goal loop push a task's completion to the default branch through a\n+SADR-0012 made the goal loop push a task's completion to the default branch through a\n closeout pull request: after the implementation PR merges, the loop opens a second PR\n carrying `task.status = done`, `lock.state = released`, and \u2014 when the task completes\n its goal \u2014 `goal.status = complete` plus one scorecard report. Nothing else. The\n@@ -49,14 +49,14 @@\n 4. The task file's governance fields (`scope`, `non_goals`, `required_skills`,\n    `risk_level`, `locks`, `acceptance_checks`, `test_command`) are byte-identical to\n    the base snapshot. Only `status`, `expected_pr`, `closeout_pr`, `ledger_evidence`\n-   and `expected_branch` may differ \u2014 the ADR 0015 contract-immutability rule,\n+   and `expected_branch` may differ \u2014 the SADR-0015 contract-immutability rule,\n    unchanged.\n \n    `expected_branch` is in that set because a closeout is *by construction* on a\n    different branch from the implementation it closes, and the loop rewrites the\n    field accordingly. It is safe to let it differ because MergeGate independently\n    requires `expected_branch` to equal the PR's actual head ref, so a PR cannot set\n-   it to anything other than the branch it is really on. The first draft of this ADR\n+   it to anything other than the branch it is really on. The first draft of this SADR\n    omitted it, which made the exemption unreachable: every real closeout failed\n    condition 4 on a field the loop always changes. See PR #209.\n 5. The state transitions are exactly the terminal set: task `review` \u2192 `done`, lock\n@@ -113,12 +113,12 @@\n classification must be derived, not asserted.\n \n **Fold the closeout into the implementation PR** so no second PR exists. Rejected under\n-ADR 0012, which recorded why: a task file that is already `done` on the implementation\n+SADR-0012, which recorded why: a task file that is already `done` on the implementation\n branch short-circuits the loop's review \u2192 CCA \u2192 merge \u2192 done ordering into a local\n goal-completion before any merge happens.\n \n **Have the loop auto-merge closeouts with elevated credentials.** Rejected outright: that\n-is a runtime authoring its own approval, which ADR 0010 and the constitution's\n+is a runtime authoring its own approval, which SADR-0010 and the constitution's\n approval-record prohibition forbid. It would also be indistinguishable, in the evidence,\n from the forgery those rules exist to prevent.\n \ndiff --git a/docs/adr/0018-carried-guardian-approval-across-a-proven-base-sync.md b/docs/adr/SADR-0018-carried-guardian-approval-across-a-proven-base-sync.md\nindex 22358562da390ed8b45cd600da08a43d378b4680..e6e2907cefa776460af73ddf7d39db605f3c39b0\n--- a/docs/adr/0018-carried-guardian-approval-across-a-proven-base-sync.md\n+++ b/docs/adr/SADR-0018-carried-guardian-approval-across-a-proven-base-sync.md\n@@ -1,4 +1,4 @@\n-# ADR 0018: Carried Guardian Approval Across A Proven Base Sync\n+# SADR-0018: Carried Guardian Approval Across A Proven Base Sync\n \n ## Status\n \n@@ -34,7 +34,7 @@\n Guardian evaluator that consumes it (`shiki_guardian.evaluate_guardian_approval`,\n the `guardian_comment_carried` source) were built and tested in isolation. Neither\n does anything until a caller passes the flags that turn the carry on, so the\n-feature was invisible in CI. This ADR records the decision the two of them\n+feature was invisible in CI. This SADR records the decision the two of them\n implement, and this task wires it into the four CI invocations that judge Guardian\n approval.\n \ndiff --git a/docs/adr/README.md b/docs/adr/README.md\nindex c70e057a4520c36e042ab68c53cd4a21ef709b71..5da43856836e2ce6a150f8127766e2f88355ab4b\n--- a/docs/adr/README.md\n+++ b/docs/adr/README.md\n@@ -1,40 +1,41 @@\n-# Architecture Decision Records\n+# Decision Record Namespaces\n \n-This directory holds Shiki's Architecture Decision Records (ADRs): durable\n-records of hard-to-reverse platform decisions. ADRs are part of the Shiki source\n-of truth alongside GitHub state, the `.shiki/` mirror, and `CONTEXT.md`.\n+This directory holds two independent decision namespaces:\n \n-## How ADRs Work\n+- Shiki Architecture Decision Records (SADRs), named\n+  `SADR-NNNN-<short-slug>.md`, record hard-to-reverse Shiki platform decisions.\n+- Target Architecture Decision Records (ADRs), named\n+  `NNNN-<short-slug>.md`, belong to the target product.\n \n-- New ADRs are added as `docs/adr/NNNN-<short-slug>.md`, where `NNNN` is the next\n-  zero-padded sequence number.\n-- The **canonical list of ADRs is the set of files matching `docs/adr/*.md`** in\n-  this directory. Browse `docs/adr/*.md` for the authoritative, current set.\n-- Use [`template.md`](template.md) as the starting point for a new ADR.\n-- Record an ADR when a decision is hard to reverse, surprising without context,\n-  and a real tradeoff. Do not write an ADR for routine or easily reversible\n-  choices.\n+The same four-digit number may appear once in each namespace. A platform\n+`SADR-NNNN` citation resolves only to `SADR-NNNN-*.md`; a target `ADR NNNN`\n+citation resolves only to `NNNN-*.md`.\n \n-Using the `docs/adr/*.md` glob as the source of truth means other PRs can add an\n-ADR without editing this index file, avoiding merge contention on a\n-hand-maintained enumeration.\n+## How Decision Records Work\n \n-## Current ADRs (snapshot)\n-\n-The list below is a convenience snapshot and may lag behind the directory. When\n-in doubt, list `docs/adr/*.md`.\n+- New Shiki platform decisions use the next zero-padded SADR number.\n+- New target product decisions use the target's own ADR sequence.\n+- The canonical list is the files in `docs/adr/`; no hand-maintained index is\n+  authoritative.\n+- Use [`template.md`](template.md) as the starting point and choose the filename\n+  namespace that owns the decision.\n+- Record a decision when it is hard to reverse, surprising without context, and\n+  a real tradeoff. Do not write a decision record for routine or easily\n+  reversible choices.\n \n-- [0001 \u2014 Keep Shiki As The Platform Name](0001-keep-shiki-as-platform-name.md)\n-- [0002 \u2014 Use AGENTS As The Shared Agent Constitution](0002-use-agents-as-shared-constitution.md)\n-- [0003 \u2014 Require Skill Gate For MergeGate Readiness](0003-require-skill-gate-for-mergegate-readiness.md)\n-- [0004 \u2014 Use GitHub First With A Repository-Local Shiki Mirror](0004-use-github-first-with-shiki-mirror.md)\n-- [0005 \u2014 Use Codex As Front And Claude Code Action As The GitHub Runtime](0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md)\n-- [0006 \u2014 Separate Codex Implementation, CCA Completion, And MergeGate Authorization](0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md)\n+Using the directory as the source of truth lets independent changes add records\n+without contending on an enumerated index.\n \n-## Adding A New ADR\n+## Adding A Shiki SADR\n \n-1. Copy `template.md` to `docs/adr/NNNN-<short-slug>.md`.\n+1. Copy `template.md` to `docs/adr/SADR-NNNN-<short-slug>.md`.\n 2. Fill in the title, status, context, decision, and consequences.\n-3. Set the status to `Proposed`, then `Accepted` once the decision is approved.\n-4. Open a PR following `CONTRIBUTING.md`. You do not need to edit this index for\n-   the ADR to be discoverable via `docs/adr/*.md`.\n+3. Set the status to `Proposed`, then `Accepted` once the proper authority has\n+   approved the decision.\n+4. Open a PR following `CONTRIBUTING.md`.\n+\n+## Adding A Target ADR\n+\n+1. Copy `template.md` to `docs/adr/NNNN-<short-slug>.md` using the target's own\n+   sequence.\n+2. Cite it as `ADR NNNN`; do not prefix its filename with `SADR-`.\ndiff --git a/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md b/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\nnew file mode 100644\nindex 0000000000000000000000000000000000000000..7455ce1f3bed02ddddb851ac21a1d1a648c9aeba\n--- /dev/null\n+++ b/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\n@@ -0,0 +1,55 @@\n+# SADR-0019: Separate Shiki Platform SADRs From Target ADRs\n+\n+## Status\n+\n+Accepted\n+\n+## Context\n+\n+Shiki installs its platform decision records into `docs/adr/` in every target\n+repository. Shiki and its targets previously used the same numeric\n+`NNNN-<slug>.md` namespace. A target that already owned ADRs 0013 through 0018\n+therefore acquired two records for each number during installation, making an\n+`ADR NNNN` citation ambiguous. Renumbering target decisions would rewrite target\n+history, while renumbering Shiki decisions per target would destroy their stable\n+platform identity.\n+\n+Upgrades also need to remove the former Shiki-owned numeric paths without ever\n+mistaking a target-owned ADR for platform content. The install stamp already\n+records exact shipped paths and content digests, so it can prove ownership only\n+when both still match.\n+\n+## Decision\n+\n+Shiki platform decisions use the `SADR` namespace:\n+\n+- records are named `docs/adr/SADR-NNNN-<slug>.md`;\n+- live platform citations use `SADR-NNNN`;\n+- target product decisions retain `docs/adr/NNNN-<slug>.md` and `ADR NNNN`;\n+- the reference validator resolves the two namespaces independently, so the same\n+  four-digit number may exist once in each namespace.\n+\n+Before a forced upgrade writes anything, the installer inventories the exact\n+former numeric Shiki ADR paths. It may delete a legacy path only when the\n+existing install stamp contains that exact path and its recorded SHA-256 digest\n+matches the file currently on disk. All ownership blockers are reported\n+together and abort the upgrade before its first write. A non-forced install\n+never performs this cleanup.\n+\n+Historical `.shiki/` evidence remains unchanged because it records the namespace\n+that existed when that evidence was produced.\n+\n+## Consequences\n+\n+- Targets may use any ADR number without colliding with Shiki's platform SADRs.\n+- Fresh installs preserve target ADR bytes and install a stable SADR set.\n+- Forced upgrades fail closed when legacy ownership cannot be proven, and a\n+  successful upgrade refreshes the stamp without removed legacy paths.\n+- Current governance, packaging, workflow, prompt, script, and skill references\n+  must move with the platform records.\n+\n+## Alternatives Considered\n+\n+- Collision-only refusal leaves affected targets permanently unupgradable.\n+- Renumbering target ADRs mutates target-owned history.\n+- Per-target Shiki renumbering makes platform citations non-portable.\ndiff --git a/docs/adr/template.md b/docs/adr/template.md\nindex a92a97daddd0b00ab9dd626cc8d86942da2eafed..17dd79175b5fee2105935557f691b40cf9492942\n--- a/docs/adr/template.md\n+++ b/docs/adr/template.md\n@@ -1,14 +1,14 @@\n-# NNNN: <Short Decision Title>\n+# <SADR-NNNN or NNNN>: <Short Decision Title>\n \n ## Status\n \n-Proposed | Accepted | Superseded by ADR-NNNN\n+Proposed | Accepted | Superseded by SADR-NNNN or target ADR NNNN\n \n ## Context\n \n What is the situation, constraint, or force that requires a decision? Reference\n-relevant domain terms (`CONTEXT.md`), prior ADRs, and code reality. State the\n-problem, not the solution.\n+relevant domain terms (`CONTEXT.md`), prior Shiki SADRs or target ADRs, and code\n+reality. State the problem, not the solution.\n \n ## Decision\n \ndiff --git a/docs/agents/autonomous-loop-e2e-acceptance.md b/docs/agents/autonomous-loop-e2e-acceptance.md\nindex bbf84c6081987f159ac6ad9ecf3c82b7aa72e7e8..5c13c1d02a05a90b0dc46cbef7c95a9846d0c237\n--- a/docs/agents/autonomous-loop-e2e-acceptance.md\n+++ b/docs/agents/autonomous-loop-e2e-acceptance.md\n@@ -40,8 +40,8 @@\n 3. `gh auth status` is logged in with merge permission on the repo.\n 4. Branch protection + required checks are configured exactly as the goal-loop\n    required-check set expects (see `docs/agents/branch-protection-smoke.md`).\n-5. The repo `.shiki/policy` permits autonomous low/medium-risk merges (ADR\n-   0008/0009); the MergeGate policy check is the recorded-authority gate.\n+5. The repo `.shiki/policy` permits autonomous low/medium-risk merges\n+   (SADR-0008/0009); the MergeGate policy check is the recorded-authority gate.\n \n The operator's only actions are: register the task and start the loop. **No\n manual pre-clearing** means after `shiki loop run` starts, the operator does not\ndiff --git a/docs/agents/bootstrap-command.md b/docs/agents/bootstrap-command.md\nindex 05f7f203a52cf412b9f97084a6e131ed0f785afd..f7aa0b734dfebee4a3b139335b66ee3415f48713\n--- a/docs/agents/bootstrap-command.md\n+++ b/docs/agents/bootstrap-command.md\n@@ -189,7 +189,9 @@\n ```\n \n Use this only for tests, fixtures, or explicit local-only template inspection.\n-Use `--force` only when you intentionally want to overwrite existing target files.\n+Use `--force` only when you intentionally want to refresh the target template.\n+Legacy numeric Shiki decision cleanup is stamp-and-digest authorized before any\n+write; target ADR paths are not cleanup candidates.\n \n ## Slash Command\n \ndiff --git a/docs/agents/checklists.md b/docs/agents/checklists.md\nindex 4a7f03ade346f53a1c1d82981f3fd744f9ab4547..0c5ec6c53dc7197b7b7a7d18e667b325babd6c45\n--- a/docs/agents/checklists.md\n+++ b/docs/agents/checklists.md\n@@ -32,18 +32,18 @@\n | ID | Item | Blocking |\n | --- | --- | --- |\n | GD-01 | Plan was challenged against `CONTEXT.md`. | blocking for non-trivial Goals |\n-| GD-02 | Relevant ADRs were read or absence was recorded. | blocking for non-trivial Goals |\n+| GD-02 | Relevant SADRs and target ADRs were read or absence was recorded. | blocking for non-trivial Goals |\n | GD-03 | Ambiguous terms were resolved or converted to HITL blockers. | blocking |\n | GD-04 | Edge scenarios were considered. | advisory |\n | GD-05 | Code reality was checked when code could answer. | blocking when applicable |\n | GD-06 | New domain terms were added to `CONTEXT.md` when needed. | blocking when applicable |\n-| GD-07 | ADR was created or explicitly skipped using the ADR criteria. | blocking when applicable |\n+| GD-07 | A SADR or target ADR was created or explicitly skipped using the decision-record criteria. | blocking when applicable |\n \n ## CI \u2014 Context & Impact Checklist\n \n | ID | Item | Blocking |\n | --- | --- | --- |\n-| CI-01 | Relevant docs and ADRs identified. | blocking |\n+| CI-01 | Relevant docs, SADRs, and target ADRs identified. | blocking |\n | CI-02 | Relevant modules, interfaces, seams, callers, and tests identified. | blocking |\n | CI-03 | Dependencies and blocked-by relationships identified. | blocking |\n | CI-04 | Candidate locks identified. | blocking |\n@@ -62,7 +62,7 @@\n | PRD-04 | Implementation decisions are recorded without volatile code detail. | blocking |\n | PRD-05 | Testing decisions are recorded. | blocking |\n | PRD-06 | Out-of-scope items are explicit. | blocking |\n-| PRD-07 | Relevant glossary/ADR links are present when applicable. | blocking |\n+| PRD-07 | Relevant glossary/SADR/target-ADR links are present when applicable. | blocking |\n \n ## SF \u2014 Spec Freeze Checklist\n \n@@ -98,7 +98,7 @@\n | H-02 | Branch/worktree target is present. | blocking |\n | H-03 | Scope and non-goals are present. | blocking |\n | H-04 | Dependencies and locks are present. | blocking |\n-| H-05 | Relevant docs, ADRs, modules, and tests are referenced. | blocking |\n+| H-05 | Relevant docs, SADRs, target ADRs, modules, and tests are referenced. | blocking |\n | H-06 | Required skills are stated. | blocking |\n | H-07 | TDD expectations are stated. | blocking when behavior work |\n | H-08 | Acceptance criteria are included. | blocking |\ndiff --git a/docs/agents/codex-handoff.md b/docs/agents/codex-handoff.md\nindex ede468f47cb277f69a857e8c45df703e4ed0151d..6f97bdffb67e44d7edea2c71e01ea28933c4eb6c\n--- a/docs/agents/codex-handoff.md\n+++ b/docs/agents/codex-handoff.md\n@@ -46,7 +46,7 @@\n \n ## Context\n - Domain terms:\n-- Relevant ADRs:\n+- Relevant SADRs and target ADRs:\n - Relevant docs:\n - Relevant modules/interfaces/seams:\n - Similar tests/prior art:\ndiff --git a/docs/agents/completion-check-agent.md b/docs/agents/completion-check-agent.md\nindex 13e4c035db35025f4e3cdcc5adfb24314ac4d521..61bfa1218cf36ca605250c91919e620f33238aa0\n--- a/docs/agents/completion-check-agent.md\n+++ b/docs/agents/completion-check-agent.md\n@@ -32,7 +32,7 @@\n - `AGENTS.md`\n - `CLAUDE.md`\n - `CONTEXT.md`\n-- relevant ADRs\n+- relevant SADRs and target ADRs\n - parent Goal issue\n - PRD issue or document\n - task issue\ndiff --git a/docs/agents/decision-control.md b/docs/agents/decision-control.md\nindex 7d8ee5ab03a5ccbcbd04056ae61de03785a2a9c9..8c94ecda00fffaa0e92c325ab83aa9776bd1dac1\n--- a/docs/agents/decision-control.md\n+++ b/docs/agents/decision-control.md\n@@ -11,7 +11,7 @@\n | Decision | Owner | When | Required inputs | Output | Enforcement surface |\n | --- | --- | --- | --- | --- | --- |\n | Is the Goal clear enough to plan? | Planner + Guardian when needed | Goal intake | User request, existing docs, risk signals | Goal readiness verdict | Goal checklist, issue template, ledger |\n-| Must `grill-with-docs` run? | Planner / Skill Gate | Before PRD or issue split | Ambiguity, domain terms, ADR conflicts, unclear boundaries | Skill invocation or explicit skip rationale | Skill Gate checklist |\n+| Must `grill-with-docs` run? | Planner / Skill Gate | Before PRD or issue split | Ambiguity, domain terms, SADR/target-ADR conflicts, unclear boundaries | Skill invocation or explicit skip rationale | Skill Gate checklist |\n | Is a PRD required? | Planner | After Goal context stabilizes | Goal, `grill-with-docs` output, Context & Impact output | PRD required / not required | PRD checklist, CCA evidence check |\n | Can the spec be frozen? | Operator only | End of Requirements Definition, before issue split | PRD/requirements summary, scope inventory (SF-02), open-question list | `spec_freeze` block in the plan + ledger evidence | `require_grilled_plan` (plan ingest/run), SF checklist |\n | Can a frozen spec change? | Operator only (Spec Amendment) | When implementation contradicts the frozen spec | Contested decisions, scoped re-grill result | recorded amendment + re-stamped freeze | SF checklist, ledger evidence |\ndiff --git a/docs/agents/domain.md b/docs/agents/domain.md\nindex 258da8ea81e87ac0a6b0b5b5e5c2fde980eaabb7..d57b0cf17ff3e1fa9e6e5885372bf083d8295421\n--- a/docs/agents/domain.md\n+++ b/docs/agents/domain.md\n@@ -5,7 +5,9 @@\n ## Layout\n \n - `CONTEXT.md` defines Shiki language. Keep it implementation-free.\n-- `docs/adr/` records hard-to-reverse decisions.\n+- `docs/adr/SADR-NNNN-*.md` records Shiki platform decisions.\n+- `docs/adr/NNNN-*.md` records target product decisions without sharing the\n+  SADR namespace.\n - `AGENTS.md` is the runtime-neutral agent constitution.\n - `CLAUDE.md` is the Claude Code wrapper around `AGENTS.md`.\n - `.shiki/` is the Target Repository mirror for Goals, PRDs, plans, Task DAGs, contracts, locks, worktree records, ledger entries, CCA verdicts, repair packets, reports, and handoffs.\n@@ -15,6 +17,6 @@\n \n - Use Shiki terms exactly when they exist.\n - Add glossary terms to `CONTEXT.md` only when the term is part of the domain, not a generic programming concept.\n-- Create ADRs only for hard-to-reverse, surprising, tradeoff-based decisions.\n+- Create SADRs or target ADRs only for hard-to-reverse, surprising, tradeoff-based decisions.\n - Keep product-specific language in the Target Repository, not in Shiki Template docs.\n-- If an ADR conflicts with a proposed implementation, surface the conflict before editing.\n+- If a SADR or target ADR conflicts with a proposed implementation, surface the conflict before editing.\ndiff --git a/docs/agents/execution-control.md b/docs/agents/execution-control.md\nindex ad8ee3cdb36be56fedc1f46408ca92b387d74047..bc63c3a73661d12b567016ba299f1a6b66bf9088\n--- a/docs/agents/execution-control.md\n+++ b/docs/agents/execution-control.md\n@@ -63,7 +63,7 @@\n | Branch name | Orchestrator | Orchestrator | Before implementer dispatch | branch policy | branch ref in task/ledger |\n | Lock reservation | Context & Impact | MergeGate | Before dispatch and before merge | `.shiki/locks/`, labels, PR metadata | lock id, owner, scope |\n | CI profile | Planner | MergeGate | During issue triage, rechecked at PR | labels, `.shiki/policy.yaml`, workflow matrix | required check list |\n-| Required status checks | Repo maintainer / Guardian | GitHub branch protection / rulesets | Repository setup, updated via ADR | GitHub settings | branch/ruleset config, ADR |\n+| Required status checks | Repo maintainer / Guardian | GitHub branch protection / rulesets | Repository setup, updated via target ADR | GitHub settings | branch/ruleset config, target ADR |\n | CCA judgment | CCA | GitHub required check / MergeGate | Every non-draft PR update | GitHub Action check | structured CCA verdict |\n | Repair assignment | CCA | MergeGate | After failed CCA/CI/review | repair packet, issue/PR comment | repair id and attempt count |\n | Merge readiness | MergeGate | GitHub branch protection / Guardian if needed | After CCA complete | required checks, review rules, rulesets | mergegate check output |\ndiff --git a/docs/agents/guardian-policy.md b/docs/agents/guardian-policy.md\nindex 4147c7adf05a47adb4fd24ada1a1c2e1f2ba6d2d..a20d9cc6c00a569bb057b9ca8d6b51c7fc4a9847\n--- a/docs/agents/guardian-policy.md\n+++ b/docs/agents/guardian-policy.md\n@@ -98,8 +98,8 @@\n `origin/<default_branch>` and reuses each job's existing base fetch; a shallow\n (`--depth`) or second base fetch would make the proof unresolvable and silently\n disable the carry, so neither is permitted. CCA reads `guardian_comment_carried` as\n-a recorded authority in the deterministic signal and does not re-derive it. See ADR\n-0018 for the full decision, the rejected diff/patch-equality and path-disjointness\n+a recorded authority in the deterministic signal and does not re-derive it. See\n+SADR-0018 for the full decision, the rejected diff/patch-equality and path-disjointness\n alternatives, and the accepted residual.\n \n ## External AI Guardian Review (SADR-0010)\ndiff --git a/docs/agents/implementation-policy.md b/docs/agents/implementation-policy.md\nindex c754144a9fb5bf4a38aa82a60e1670c59e626b44..85c37281bcad196c6b7b02343faf00b03cd9c57a\n--- a/docs/agents/implementation-policy.md\n+++ b/docs/agents/implementation-policy.md\n@@ -31,10 +31,13 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n ## Non-Negotiables\n \n 1. **Planning precedes implementation.** Non-trivial Goals start with `grill-with-docs` and Context & Impact.\n-2. **Domain language is controlled.** Terms must be checked against `CONTEXT.md`; hard-to-reverse tradeoffs must be checked against ADRs.\n+2. **Domain language is controlled.** Terms must be checked against `CONTEXT.md`; hard-to-reverse tradeoffs must be checked against Shiki SADRs and target ADRs.\n 3. **PRD precedes issue decomposition.** `to-prd` is used once the context is settled enough to become durable intent.\n 4. **Issues are vertical slices.** `to-issues` must produce independently grabbable tracer-bullet tasks, not horizontal layer tickets.\n 5. **Triage controls dispatch.** Only `ready-for-agent` / AFK tasks may be assigned to Codex without human input.\n@@ -95,19 +98,19 @@\n Process:\n \n - Challenge terms against `CONTEXT.md`.\n-- Check ADRs before reopening decisions.\n+- Check SADRs and target ADRs before reopening decisions.\n - Inspect code when code can answer a question.\n - Ask one question at a time when the operator is available.\n - Provide a recommended answer with each question.\n - Record resolved terms in `CONTEXT.md`.\n-- Create ADRs only for hard-to-reverse, surprising, tradeoff-based decisions.\n+- Create SADRs or target ADRs only for hard-to-reverse, surprising, tradeoff-based decisions.\n - Convert unresolved load-bearing questions into HITL blockers.\n \n Outputs:\n \n - Resolved design-tree decisions.\n - Updated glossary terms when needed.\n-- ADRs when justified.\n+- SADRs or target ADRs when justified.\n - HITL blockers when decisions remain unresolved.\n \n ## Phase 3 \u2014 Context & Impact\n@@ -117,7 +120,7 @@\n \n Outputs must include:\n \n-- Relevant documents and ADRs.\n+- Relevant documents, SADRs, and target ADRs.\n - Relevant modules, interfaces, seams, callers, and tests.\n - Candidate locks.\n - Dependencies.\n@@ -140,7 +143,7 @@\n - Testing decisions.\n - Out of scope.\n - Further notes.\n-- Links to relevant Goal, domain terms, and ADRs.\n+- Links to relevant Goal, domain terms, SADRs, and target ADRs.\n \n Phase 4 ends with Spec Freeze: the operator explicitly approves the PRD, the\n plan records a `spec_freeze` block (status, approved_by, source), and the SF\n@@ -240,7 +243,7 @@\n - Dependencies and locks.\n - Risk and Guardian requirements.\n - Ledger completeness.\n-- Documentation and ADR updates.\n+- Documentation and SADR/target-ADR updates.\n - Scope drift.\n \n CCA emits one of:\n@@ -301,7 +304,7 @@\n | Evidence failure | Behavior might be done, but proof is missing. | Add checks, PR evidence, ledger entry, or artifacts. |\n | Scope drift | PR changes unrelated behavior. | Revert unrelated changes or split into new Goal. |\n | Dependency block | Upstream task or lock not complete. | Block until dependency resolves. |\n-| Architecture block | Decision needs ADR or deep-module discussion. | `grill-with-docs` or `improve-codebase-architecture`. |\n+| Architecture block | Decision needs a SADR, target ADR, or deep-module discussion. | `grill-with-docs` or `improve-codebase-architecture`. |\n | Risk block | Security/production/policy change needs human approval. | Guardian decision. |\n \n ## Phase 5.5 \u2014 Execution Decision Control\ndiff --git a/docs/agents/runtime-auth-model.md b/docs/agents/runtime-auth-model.md\nindex 6a2e1a47e81ea89b5c68a210ec07772a9374babc..3c54748d24233dbd0ba8473950282547ef4ce935\n--- a/docs/agents/runtime-auth-model.md\n+++ b/docs/agents/runtime-auth-model.md\n@@ -148,7 +148,7 @@\n \n API-key based runners may be added by a target repository as an explicit extension. They are not the default Shiki template path.\n \n-If a repo opts into API-key mode, record the exception in an ADR and update the repo's `AGENTS.md`, `.shiki/config.yaml`, and workflow permissions.\n+If a repo opts into API-key mode, record the target-specific exception in a target ADR and update the repo's `AGENTS.md`, `.shiki/config.yaml`, and workflow permissions.\n \n ## GitHub Actions Runtime Compatibility\n \ndiff --git a/docs/agents/skill-gate.md b/docs/agents/skill-gate.md\nindex c50398b0d7c60a8598f2a5c88a10015730dced13..3acffe6444617bb1d54b77d4b4708158ec735bc6\n--- a/docs/agents/skill-gate.md\n+++ b/docs/agents/skill-gate.md\n@@ -9,7 +9,7 @@\n | Skill | Trigger | Expected output |\n | --- | --- | --- |\n | `setup-matt-pocock-skills` | First configuring a repo for these skills, or when issue tracker, triage labels, or domain docs are missing. | `docs/agents/` configuration and an `Agent skills` block in `AGENTS.md` or `CLAUDE.md`. |\n-| `grill-with-docs` | Ambiguous plan, unclear terminology, boundary decision, tradeoff, or ADR-worthy design choice. | Clarified decision tree, recommended answers, updates to `CONTEXT.md` or ADRs when decisions crystallize. |\n+| `grill-with-docs` | Ambiguous plan, unclear terminology, boundary decision, tradeoff, or decision-record-worthy design choice. | Clarified decision tree, recommended answers, updates to `CONTEXT.md`, SADRs, or target ADRs when decisions crystallize. |\n | `zoom-out` | Agent lacks a higher-level architectural map. | Map of relevant modules, callers, seams, and domain concepts. |\n | `to-prd` | Settled context should become a PRD. | PRD published to the issue tracker with problem, solution, user stories, decisions, testing, and out-of-scope notes. |\n | `to-issues` | Goal, PRD, or plan must become implementation tickets. | Vertical-slice issues with acceptance criteria, dependencies, and AFK/HITL classification. |\n@@ -17,7 +17,7 @@\n | `tdd` | Feature work or bug fix has observable behavior that can be specified with tests. | Red-green-refactor loop through public interfaces, one vertical slice at a time. |\n | `code-review` | The TDD loop is green and the implementer is about to create or update a PR. Default for every implementation task. | Pre-PR self-review of the task diff with fixes applied on the task branch; findings and resolutions recorded as a ledger entry naming the skill and a `## Pre-PR code review` PR body section. |\n | `diagnose` | Hard bug, failing check, regression, flaky behavior, or performance problem. | Reproduction loop, ranked hypotheses, instrumentation, fix, regression test, post-mortem. |\n-| `improve-codebase-architecture` | Structural friction, testability, AI-navigability, deep-module opportunity, or refactor request. | Architecture candidates and deepening recommendations informed by `CONTEXT.md` and ADRs. |\n+| `improve-codebase-architecture` | Structural friction, testability, AI-navigability, deep-module opportunity, or refactor request. | Architecture candidates and deepening recommendations informed by `CONTEXT.md`, SADRs, and target ADRs. |\n | `prototype` | A throwaway logic or UI prototype is needed to answer a design question. | Clearly marked disposable prototype and captured decision. |\n \n ## MergeGate Interaction\ndiff --git a/docs/operations.md b/docs/operations.md\nindex cf1f452d4c589e38ccd58553bda0ad8684b51eb7..ace48a90ffbec9e5c0a7113bef16a4858bc44b3f\n--- a/docs/operations.md\n+++ b/docs/operations.md\n@@ -89,6 +89,16 @@\n      target history, brought forward by `shiki migrate`, not a template.\n    - **Everything else** (CLI scripts, workflows, schemas, docs) is overwritten.\n \n+   A forced upgrade also migrates the former numeric Shiki decision paths to\n+   `SADR-NNNN-*`. Before the first write, the installer inventories the exact\n+   legacy Shiki paths and authorizes each deletion only when the existing\n+   `.shiki/install-stamp.json` names that path and its SHA-256 digest still\n+   matches the file. An absent, unreadable, malformed, incomplete, or\n+   mismatching stamp reports every blocked path and stops with no filesystem\n+   change. Target-owned `NNNN-*` ADRs are not cleanup candidates. A non-force\n+   install deletes nothing and retains any still-matching legacy stamp entries\n+   so a later forced upgrade can prove ownership.\n+\n    If the target has pending migrations, `--force` refuses and writes nothing \u2014\n    apply migrations first (step 3), then re-run. The run ends with a single\n    summary of every `.new` written, so a half-upgrade is visible.\ndiff --git a/docs/releasing.md b/docs/releasing.md\nindex 10d73828e7dbd64e9d9f6b83bc84d010537ce62a..aafeee8c4d441e1e427efee2ce4f81a647f020e1\n--- a/docs/releasing.md\n+++ b/docs/releasing.md\n@@ -67,7 +67,7 @@\n CLI in `scripts/`. There are no third-party Python runtime dependencies. The CLI\n is intentionally **not** exposed as a `[project.scripts]` console entry point\n because `scripts/shiki.py` is a dependency-free shim and `scripts/` is not\n-packaged as an importable distribution (see the ADR for the rationale).\n+packaged as an importable distribution (see SADR-0007 for the rationale).\n \n Supported install paths:\n \n@@ -155,6 +155,15 @@\n    [docs/operations.md](operations.md#the-new-convention). If the target has\n    pending migrations, `--force` refuses and writes nothing; apply migrations\n    (step 2) first, then re-run.\n+\n+   The SADR namespace upgrade has an additional pre-write gate: each former\n+   numeric Shiki decision path is removed only when the existing install stamp\n+   contains that exact path and a digest matching its current bytes. The\n+   installer aggregates absent/unreadable/malformed-stamp, missing-digest, and\n+   digest-mismatch blockers before raising, so refusal leaves the target tree\n+   byte-identical. Numeric target ADRs are preserved. A successful forced\n+   upgrade installs and stamps `SADR-*` paths and drops the removed legacy paths\n+   from the refreshed stamp.\n 2. Apply repository-local `.shiki/` state migrations with the migrate command:\n \n    ```bash\ndiff --git a/docs/usage.md b/docs/usage.md\nindex b83407ee3970dd395dd893010bc5dbe8bad26051..a130ef6e40dfd8169a5bc701339351097df1a09f\n--- a/docs/usage.md\n+++ b/docs/usage.md\n@@ -67,8 +67,10 @@\n shiki install-target /path/to/target-repo --local-only\n ```\n \n-Use `--force` only when you intentionally want to overwrite existing target\n-files.\n+Use `--force` only when you intentionally want to refresh the target template.\n+For the SADR namespace migration, `--force` deletes only exact legacy Shiki\n+decision paths whose existing install-stamp digest still matches; any ownership\n+blocker stops before the first write. Numeric target ADRs are never candidates.\n \n ## `shiki bootstrap-platform` \u2014 publish the Shiki platform repo\n \ndiff --git a/scripts/guardian_approval_signal.py b/scripts/guardian_approval_signal.py\nindex d059f92b2c82aa2b5d6ca421365537a87886c1cb..16924892714f4f5b9303c0ef0e325a7e70075630\n--- a/scripts/guardian_approval_signal.py\n+++ b/scripts/guardian_approval_signal.py\n@@ -199,8 +199,8 @@\n ) -> Any:\n     \"\"\"Evaluate SADR-0015 Contract Approval for the CCA Guardian signal.\n \n-    The SAME OR that MergeGate applies must reach this deterministic signal: ADR\n-    0015 is explicit that without it, a contract-approved PR still returns\n+    The SAME OR that MergeGate applies must reach this deterministic signal:\n+    SADR-0015 is explicit that without it, a contract-approved PR still returns\n     ``needs_guardian``, the CCA job fails, and the MergeGate policy check never\n     runs \u2014 the gate this change moves would never be reached. This resolves the\n     task the SAME way MergeGate does (the first Shiki task id in the PR body),\ndiff --git a/scripts/shiki_contract_approval.py b/scripts/shiki_contract_approval.py\nindex 941bcb96fdd8b9a85bc0c4f0f04d5dad9d04839c..4a4b418f6527413161db82670bec857a043360e2\n--- a/scripts/shiki_contract_approval.py\n+++ b/scripts/shiki_contract_approval.py\n@@ -142,7 +142,7 @@\n     changed_files_status: Any,\n     registration: Any,\n ) -> ContractApprovalResult:\n-    \"\"\"Whether this PR carries an SADR-0015 Contract Approval.\n+    \"\"\"Whether this PR carries a SADR-0015 Contract Approval.\n \n     The four conditions are evaluated IN ORDER, each failure recorded as a\n     distinct reason string:\ndiff --git a/scripts/shiki_guardian.py b/scripts/shiki_guardian.py\nindex a4ecb79bbda55fd5c8b06adfae2eb99760a03c45..4dd99bbe714e26f421fa19abac7a34ae1b195ecc\n--- a/scripts/shiki_guardian.py\n+++ b/scripts/shiki_guardian.py\n@@ -12,7 +12,7 @@\n # The carry re-derives its proof from git in-process on every judging invocation;\n # it is never read from an artifact, so there is nothing to go stale or be forged.\n # shiki_sync_proof is the ONLY guardian-family module that runs git, and every one\n-# of its invocations is read-only and local (ADR: prove-from-git base sync).\n+# of its invocations is read-only and local (SADR: prove-from-git base sync).\n from shiki_sync_proof import verify_pure_base_sync\n \n GUARDIAN_POLICY_PATH = \".shiki/guardian-policy.json\"\ndiff --git a/scripts/shiki_guardian_review.py b/scripts/shiki_guardian_review.py\nindex dd0f104d457899e0ba5ff1e162011c1e90b6f92b..e10ef58c046324e7ea3a3392feb3048ca77414ce\n--- a/scripts/shiki_guardian_review.py\n+++ b/scripts/shiki_guardian_review.py\n@@ -159,7 +159,7 @@\n     docs_only = _is_docs_only(changed_files)\n     if docs_only and matched == 0:\n         focus.append(\n-            \"Docs/ADR-only change: confirm the change is genuinely documentation/decision-only, introduces no behavior or contract drift, and stays consistent with the constitution and existing ADRs.\"\n+            \"Docs/decision-record-only change: confirm the change is genuinely documentation/decision-only, introduces no behavior or contract drift, and stays consistent with the constitution and existing SADRs and target ADRs.\"\n         )\n \n     # Conservative augmentation: unknown (no category and not docs-only), mixed\ndiff --git a/scripts/shiki_installer.py b/scripts/shiki_installer.py\nindex be064ab7a29ff6836f7f7a1d70500a43e0d11d7b..9fd8eefd137ce7003dad5c0dcb2e48e2cd47ab32\n--- a/scripts/shiki_installer.py\n+++ b/scripts/shiki_installer.py\n@@ -118,6 +118,7 @@\n # what lets a target carry it without a governance-schema change.\n INSTALL_STAMP_PATH = \".shiki/install-stamp.json\"\n INSTALL_STAMP_VERSION = 1\n+LEGACY_SHIKI_SADR_MAX = 18\n \n # Under --force the shipped surface splits three ways, so nothing is lost and\n # nothing is silently stale.\n@@ -237,9 +238,98 @@\n         )\n \n \n+def legacy_shiki_adr_paths() -> tuple[str, ...]:\n+    \"\"\"Former numeric paths for Shiki SADR-0001 through SADR-0018.\n+\n+    The authoritative slugs come from the current SADR filenames. SADR-0019 is\n+    the namespace decision itself and never shipped under a numeric legacy path.\n+    \"\"\"\n+    paths: list[str] = []\n+    for sadr in sorted((ROOT / \"docs\" / \"adr\").glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\")):\n+        number = sadr.name.split(\"-\", 2)[1]\n+        if int(number) <= LEGACY_SHIKI_SADR_MAX:\n+            paths.append(f\"docs/adr/{sadr.name.removeprefix('SADR-')}\")\n+    return tuple(paths)\n+\n+\n+def preflight_legacy_sadr_cleanup(target: Path, *, force: bool) -> tuple[str, ...]:\n+    \"\"\"Authorize exact legacy Shiki ADR removals before the first write.\n+\n+    A non-force install never removes existing files. A forced install needs no\n+    stamp when no legacy Shiki path is present. When legacy paths do exist, each\n+    must be named in a readable install stamp and its current bytes must match\n+    the recorded digest.\n+    \"\"\"\n+    if not force:\n+        return ()\n+    present = tuple(\n+        relative\n+        for relative in legacy_shiki_adr_paths()\n+        if (target / relative).exists() or (target / relative).is_symlink()\n+    )\n+    if not present:\n+        return ()\n+\n+    stamp_path = target / INSTALL_STAMP_PATH\n+    try:\n+        raw = stamp_path.read_text(encoding=\"utf-8\")\n+        stamp = json.loads(raw)\n+    except FileNotFoundError:\n+        detail = \"install stamp is absent\"\n+        blockers = [f\"{relative}: {detail}\" for relative in present]\n+    except (OSError, UnicodeDecodeError) as error:\n+        detail = f\"install stamp is unreadable ({error})\"\n+        blockers = [f\"{relative}: {detail}\" for relative in present]\n+    except json.JSONDecodeError as error:\n+        detail = f\"install stamp is malformed ({error})\"\n+        blockers = [f\"{relative}: {detail}\" for relative in present]\n+    else:\n+        if not isinstance(stamp, dict):\n+            blockers = [\n+                f\"{relative}: install stamp is malformed (expected an object)\"\n+                for relative in present\n+            ]\n+        else:\n+            digests = stamp.get(\"digests\")\n+            blockers = []\n+            if not isinstance(digests, dict):\n+                blockers.extend(\n+                    f\"{relative}: install stamp has no digest for this path\"\n+                    for relative in present\n+                )\n+            else:\n+                for relative in present:\n+                    expected = digests.get(relative)\n+                    if not isinstance(expected, str) or not expected:\n+                        blockers.append(\n+                            f\"{relative}: install stamp has no digest for this path\"\n+                        )\n+                        continue\n+                    try:\n+                        actual = _sha256_file(target / relative)\n+                    except OSError as error:\n+                        blockers.append(\n+                            f\"{relative}: legacy path is unreadable ({error})\"\n+                        )\n+                        continue\n+                    if actual != expected:\n+                        blockers.append(\n+                            f\"{relative}: digest mismatch (stamp {expected}, current {actual})\"\n+                        )\n+\n+    if blockers:\n+        joined = \"\\n\".join(f\"- {blocker}\" for blocker in blockers)\n+        raise ShikiError(\n+            \"legacy Shiki ADR cleanup is not authorized:\\n\"\n+            f\"{joined}\\nNo files were written.\"\n+        )\n+    return present\n+\n+\n def install_template(target: Path, *, force: bool, validate: bool) -> list[NewFileNote]:\n     # Preconditions first: never rewrite the tree of a target that cannot be\n     # upgraded cleanly (see refuse_pending_migrations).\n+    legacy_cleanup = preflight_legacy_sadr_cleanup(target, force=force)\n     refuse_pending_migrations(target)\n \n     # Resolve the target's required CODEOWNERS owner once from its\n@@ -250,6 +340,10 @@\n     # verbatim. See ``codeowners_required_owner``.\n     codeowners_owner = codeowners_required_owner(target)\n \n+    for relative in legacy_cleanup:\n+        (target / relative).unlink()\n+        info(f\"removed stamped legacy Shiki ADR: {target / relative}\")\n+\n     notes: list[NewFileNote] = []\n     for relative in TEMPLATE_PATHS:\n         source = ROOT / relative\n@@ -647,6 +741,22 @@\n     digests = target_stamp_digests(target)\n     existing = read_install_stamp(target)\n     if existing is not None:\n+        # A non-force install deliberately leaves legacy Shiki ADRs in place.\n+        # Preserve only still-matching ownership proof so a later forced\n+        # upgrade can authorize cleanup; never carry a missing or stale digest.\n+        existing_digests = existing.get(\"digests\")\n+        if isinstance(existing_digests, dict):\n+            for relative in legacy_shiki_adr_paths():\n+                expected = existing_digests.get(relative)\n+                path = target / relative\n+                if not isinstance(expected, str) or not path.is_file():\n+                    continue\n+                try:\n+                    actual = _sha256_file(path)\n+                except OSError:\n+                    continue\n+                if actual == expected:\n+                    digests[relative] = actual\n         if _incoming_is_older(commit, existing.get(\"platform_commit\")):\n             warn(\n                 \"kept existing install stamp: incoming install \"\ndiff --git a/scripts/shiki_loop.py b/scripts/shiki_loop.py\nindex 3611641da8aeb5038b1da90e3114b09a473ec6d8..84caa491b9cad0edc4176f209c13e9221e975681\n--- a/scripts/shiki_loop.py\n+++ b/scripts/shiki_loop.py\n@@ -424,7 +424,7 @@\n         # unresolvable CCA verdict has already produced its terminal stop and is never\n         # laundered into a head-moving sync (SADR-0010). It also never fires while the\n         # policy (Guardian) gate is red \u2014 for ANY reason, not only when it is the sole\n-        # red check \u2014 so a head-bound Guardian approval, or an SADR-0015\n+        # red check \u2014 so a head-bound Guardian approval, or a SADR-0015\n         # contract-approval carry that base movement defeated (the policy gate compares\n         # base and head task governance directly), is never silently restored by\n         # adopting base wholesale. It is bounded by a durable per-task counter so the\ndiff --git a/scripts/shiki_tasks.py b/scripts/shiki_tasks.py\nindex aec3809f24b4a0c8c96fc50971cff9ae9ad30381..d531052e9d9717058d4631483cefab44c26a65c1\n--- a/scripts/shiki_tasks.py\n+++ b/scripts/shiki_tasks.py\n@@ -821,7 +821,7 @@\n         \"prompt\": prompt,\n         \"entry_skill\": \"grill-with-docs\",\n         \"required_next_steps\": [\n-            \"Run grill-with-docs until terminology, boundaries, risks, and ADR-worthy decisions are settled.\",\n+            \"Run grill-with-docs until terminology, boundaries, risks, and decision-record-worthy choices are settled.\",\n             \"Obtain the operator's explicit approval of the PRD/requirements (Spec Freeze).\",\n             \"Write a machine-readable plan JSON with grill_with_docs.status=complete and spec_freeze.status=frozen.\",\n             \"Run shiki plan ingest --plan-file PLAN.json.\",\ndiff --git a/scripts/test_shiki_goal_loop.sh b/scripts/test_shiki_goal_loop.sh\nindex 753c7b516e10f0be3bc92a4bd0eb1fa699776ae6..8f3820a0a079b3044424cfe1a6c6b7fe68896e67\n--- a/scripts/test_shiki_goal_loop.sh\n+++ b/scripts/test_shiki_goal_loop.sh\n@@ -231,8 +231,8 @@\n \n # High/critical risk merges autonomously when all required checks are green:\n # the \"MergeGate policy check\" required check IS the Guardian gate (it enforces\n-# guardian-policy.json \u2014 human approval OR external AI guardian review, ADR\n-# 0010), so green checks mean a recorded authority approved. The fake gh returns\n+# guardian-policy.json \u2014 human approval OR external AI guardian review,\n+# SADR-0010), so green checks mean a recorded authority approved. The fake gh returns\n # green checks, simulating that the Guardian gate passed.\n python3 \"$ROOT/scripts/shiki.py\" goal create --target \"$TARGET\" --title \"Guardian gate\" --outcome \"High risk merges via policy gate\" >/tmp/shiki-goal-loop-guardian-goal.json\n GGOAL=\"$(json_get /tmp/shiki-goal-loop-guardian-goal.json goal_id)\"\ndiff --git a/tests/test_adr_references.py b/tests/test_adr_references.py\nindex 51706329c797d5dd9b47e082a333fa83b261d482..db6e693e35f67103935be34f1d9bb4ff7ee60395\n--- a/tests/test_adr_references.py\n+++ b/tests/test_adr_references.py\n@@ -1,7 +1,7 @@\n-\"\"\"Every ``ADR ####`` citation must resolve to exactly one decision record.\n+\"\"\"Every target ADR and Shiki SADR citation resolves in its own namespace.\n \n Background. ``scripts/mergegate_check.py`` justifies ``is_bookkeeping_closeout``\n-with \"ADR 0017\" nine times and ``scripts/guardian_approval_signal.py`` cites\n+with \"SADR-0017\" nine times and ``scripts/guardian_approval_signal.py`` cites\n 0016/0017 seven times, yet for a stretch the numbering in ``docs/adr/`` stopped\n at 0015: the authoring commits lived on a coordinator branch and no task ever\n declared ``docs/adr/...`` in its locks, so nothing carried the records to the\n@@ -11,11 +11,12 @@\n and the directory drifted with no signal.\n \n This is the same unbound-pair shape behind other silent failures: a reference\n-and its target with nothing asserting they agree. This test binds them. It scans\n-every ``scripts/*.py`` (plus ``docs/**/*.md`` and the root ``*.md`` governance\n-files, which are cheap) for the ``ADR ####`` pattern and asserts each cited\n-number resolves to exactly one ``docs/adr/####-*.md``. The converse is\n-deliberately NOT required: an ADR may exist without being cited.\n+and its target with nothing asserting they agree. This test binds them. Target\n+``ADR NNNN`` citations resolve only to ``docs/adr/NNNN-*.md``; Shiki\n+``SADR-NNNN`` citations resolve only to ``docs/adr/SADR-NNNN-*.md``. The scan\n+covers current governance docs, scripts, workflows, prompts, packaging metadata,\n+and repo-local Codex guidance while deliberately excluding historical\n+``.shiki`` evidence. The converse is not required: a record may be uncited.\n \n The suite is dependency free: standard-library ``unittest`` only.\n \"\"\"\n@@ -31,17 +32,18 @@\n \n REPO_ROOT = Path(__file__).resolve().parents[1]\n \n-# \"ADR 0016\" -- a four-digit citation. The trailing \\b keeps a five-digit token\n-# from being read as a truncated four-digit number.\n-_CITATION = re.compile(r\"\\bADR (\\d{4})\\b\")\n-# \"0016-locks-bound-product-paths.md\" -- the leading four digits name the record.\n-_ADR_FILENAME = re.compile(r\"^(\\d{4})-.*\\.md$\")\n+# ``ADR 0016`` / ``SADR-0016`` -- a namespace plus four-digit citation. The\n+# trailing ``\\b`` keeps a five-digit token from being read as a truncated one.\n+_CITATION = re.compile(r\"\\b(S?ADR)[ -](\\d{4})\\b\")\n+# Target ADRs retain the numeric filename; Shiki platform decisions use SADR.\n+_DECISION_FILENAME = re.compile(r\"^(?:(SADR)-)?(\\d{4})-.*\\.md$\")\n \n \n class Violation(NamedTuple):\n     \"\"\"A citation that does not resolve to exactly one decision record.\"\"\"\n \n     citing_file: str  # repo-relative path of the file that cites the ADR\n+    namespace: str  # \"ADR\" for target decisions; \"SADR\" for Shiki decisions\n     number: str  # the cited four-digit number, e.g. \"0016\"\n     line: int  # 1-based line of the citation\n     reason: str  # \"missing\" (zero matches) or \"ambiguous\" (two or more)\n@@ -49,44 +51,59 @@\n \n     @property\n     def expected_path(self) -> str:\n-        return f\"docs/adr/{self.number}-*.md\"\n+        prefix = \"SADR-\" if self.namespace == \"SADR\" else \"\"\n+        return f\"docs/adr/{prefix}{self.number}-*.md\"\n \n     def describe(self) -> str:\n         \"\"\"Name the citing file, the cited number, and the missing path.\"\"\"\n         if self.reason == \"missing\":\n             return (\n-                f\"{self.citing_file}:{self.line} cites ADR {self.number} \"\n+                f\"{self.citing_file}:{self.line} cites {self.namespace} {self.number} \"\n                 f\"but no document matches {self.expected_path}\"\n             )\n         joined = \", \".join(self.matches)\n         return (\n-            f\"{self.citing_file}:{self.line} cites ADR {self.number} which \"\n+            f\"{self.citing_file}:{self.line} cites {self.namespace} {self.number} which \"\n             f\"resolves to {len(self.matches)} documents (expected exactly one \"\n             f\"at {self.expected_path}): {joined}\"\n         )\n \n \n-def _adr_index(root: Path) -> dict[str, list[Path]]:\n-    \"\"\"Map every four-digit ADR number to the record files that claim it.\"\"\"\n-    index: dict[str, list[Path]] = {}\n+def _adr_index(root: Path) -> dict[tuple[str, str], list[Path]]:\n+    \"\"\"Map each decision namespace and number to the files that claim it.\"\"\"\n+    index: dict[tuple[str, str], list[Path]] = {}\n     adr_dir = root / \"docs\" / \"adr\"\n     if adr_dir.is_dir():\n         for path in sorted(adr_dir.glob(\"*.md\")):\n-            match = _ADR_FILENAME.match(path.name)\n+            match = _DECISION_FILENAME.match(path.name)\n             if match:\n-                index.setdefault(match.group(1), []).append(path)\n+                namespace = \"SADR\" if match.group(1) else \"ADR\"\n+                index.setdefault((namespace, match.group(2)), []).append(path)\n     return index\n \n \n def _citing_files(root: Path, include_docs: bool = True) -> list[Path]:\n-    \"\"\"Files scanned for citations: scripts/*.py, and cheaply the markdown.\"\"\"\n+    \"\"\"Current, shipped text surfaces scanned for decision citations.\"\"\"\n     files = sorted((root / \"scripts\").glob(\"*.py\"))\n+    files += sorted((root / \"scripts\").glob(\"*.sh\"))\n     if include_docs:\n         docs = root / \"docs\"\n         if docs.is_dir():\n             files += sorted(docs.rglob(\"*.md\"))\n         files += sorted(root.glob(\"*.md\"))\n-    return files\n+        pyproject = root / \"pyproject.toml\"\n+        if pyproject.is_file():\n+            files.append(pyproject)\n+        for relative in (\".github\", \".codex\"):\n+            surface = root / relative\n+            if surface.is_dir():\n+                files += sorted(\n+                    path\n+                    for path in surface.rglob(\"*\")\n+                    if path.is_file()\n+                    and path.suffix in {\".md\", \".py\", \".sh\", \".toml\", \".yaml\", \".yml\"}\n+                )\n+    return sorted(set(files))\n \n \n def find_adr_reference_violations(\n@@ -94,10 +111,9 @@\n ) -> list[Violation]:\n     \"\"\"Return every ``ADR ####`` citation under ``root`` that fails to resolve.\n \n-    A citation resolves when exactly one ``docs/adr/####-*.md`` matches its\n-    number. Zero matches is a \"missing\" violation; two or more is \"ambiguous\".\n-    Records that are never cited produce nothing -- the converse is not\n-    required.\n+    A citation resolves when exactly one record in the same namespace matches\n+    its number. Zero matches is a \"missing\" violation; two or more is\n+    \"ambiguous\". Records that are never cited produce nothing.\n     \"\"\"\n     root = Path(root)\n     index = _adr_index(root)\n@@ -110,13 +126,15 @@\n         rel = path.relative_to(root).as_posix()\n         for lineno, line in enumerate(text.splitlines(), start=1):\n             for match in _CITATION.finditer(line):\n-                number = match.group(1)\n-                found = index.get(number, [])\n+                namespace, number = match.groups()\n+                found = index.get((namespace, number), [])\n                 if len(found) == 1:\n                     continue\n                 reason = \"missing\" if not found else \"ambiguous\"\n                 rels = [p.relative_to(root).as_posix() for p in found]\n-                violations.append(Violation(rel, number, lineno, reason, rels))\n+                violations.append(\n+                    Violation(rel, namespace, number, lineno, reason, rels)\n+                )\n     return violations\n \n \n@@ -129,8 +147,8 @@\n         self.assertEqual(\n             violations,\n             [],\n-            \"Every 'ADR ####' citation must resolve to exactly one \"\n-            f\"docs/adr/####-*.md:\\n{detail}\",\n+            \"Every ADR/SADR citation must resolve exactly once in its own \"\n+            f\"docs/adr namespace:\\n{detail}\",\n         )\n \n     def test_scan_actually_reads_scripts(self) -> None:\n@@ -146,8 +164,21 @@\n         # The two records this Goal carried onto the default branch, and which\n         # the scripts cite most, must be present.\n         index = _adr_index(REPO_ROOT)\n-        self.assertIn(\"0016\", index)\n-        self.assertIn(\"0017\", index)\n+        self.assertIn((\"SADR\", \"0016\"), index)\n+        self.assertIn((\"SADR\", \"0017\"), index)\n+\n+    def test_platform_tree_has_only_sadr_0001_through_0019(self) -> None:\n+        adr_dir = REPO_ROOT / \"docs\" / \"adr\"\n+        sadrs = sorted(adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\"))\n+        self.assertEqual(\n+            {path.name.split(\"-\", 2)[1] for path in sadrs},\n+            {f\"{number:04d}\" for number in range(1, 20)},\n+        )\n+        self.assertEqual(\n+            list(adr_dir.glob(\"[0-9][0-9][0-9][0-9]-*.md\")),\n+            [],\n+            \"the Shiki platform tree must not retain legacy numeric SADR paths\",\n+        )\n \n \n class FixtureAdrReferences(unittest.TestCase):\n@@ -180,6 +211,26 @@\n         self.assertIn(\"9991\", message)  # cited number\n         self.assertIn(\"docs/adr/9991-*.md\", message)  # missing path\n \n+    def test_target_adr_does_not_satisfy_missing_platform_sadr(self) -> None:\n+        root = self._make_repo(\n+            adr_names=[\"0042-target-decision.md\"],\n+            script_body='\"\"\"Target ADR 0042 exists, but platform SADR-0042 does not.\"\"\"\\n',\n+        )\n+        violations = find_adr_reference_violations(root, include_docs=False)\n+        self.assertEqual(len(violations), 1)\n+        self.assertEqual(violations[0].namespace, \"SADR\")\n+        self.assertEqual(violations[0].number, \"0042\")\n+        self.assertEqual(violations[0].expected_path, \"docs/adr/SADR-0042-*.md\")\n+\n+    def test_target_adr_and_platform_sadr_may_reuse_a_number(self) -> None:\n+        root = self._make_repo(\n+            adr_names=[\"0042-target-decision.md\", \"SADR-0042-platform-decision.md\"],\n+            script_body='\"\"\"Target ADR 0042 and platform SADR-0042 are distinct.\"\"\"\\n',\n+        )\n+        self.assertEqual(\n+            find_adr_reference_violations(root, include_docs=False), []\n+        )\n+\n     def test_uncited_adr_is_allowed(self) -> None:\n         # An ADR exists but nothing cites it -> not a violation. The converse\n         # (every ADR must be cited) is explicitly NOT required.\ndiff --git a/tests/test_guardian_review.py b/tests/test_guardian_review.py\nindex 88829e7810212635e2fc98aa5918d6f26294677d..00a85a237acd5bf2d6cb5b294128198c422402ca\n--- a/tests/test_guardian_review.py\n+++ b/tests/test_guardian_review.py\n@@ -162,7 +162,7 @@\n \n     def test_docs_only_is_light_and_not_conservative(self):\n         focus = gr.classify_review_focus([\"docs/adr/0099-x.md\", \"README.md\"], \"\", \"low\")\n-        self.assertTrue(any(\"Docs/ADR-only\" in f for f in focus))\n+        self.assertTrue(any(\"Docs/decision-record-only\" in f for f in focus))\n         self.assertFalse(any(\"conservative default\" in f.lower() for f in focus))\n \n     def test_unknown_category_is_conservative(self):\ndiff --git a/tests/test_policy_job_merged_prs.py b/tests/test_policy_job_merged_prs.py\nindex d45512e74681e5e649279236c7d4e79d5c3da471..b97989f8bd74d091de1a6b5275a20d8dc68c4512\n--- a/tests/test_policy_job_merged_prs.py\n+++ b/tests/test_policy_job_merged_prs.py\n@@ -3,8 +3,8 @@\n \n ``.github/workflows/shiki-cca-completion.yml`` has two consumers of the merged-PR\n proof: the CCA *signal* (``guardian_approval_signal.py`` in \"Prepare CCA context\")\n-and the authoritative gate (``mergegate_check.py`` in \"Enforce MergeGate\"). ADR\n-0017's bookkeeping-closeout exemption (condition 6) needs the merged implementation\n+and the authoritative gate (``mergegate_check.py`` in \"Enforce MergeGate\").\n+SADR-0017's bookkeeping-closeout exemption (condition 6) needs the merged implementation\n PR proven, and a closeout carries NO ``post_merge_reconcile`` marker. The signal\n already resolves ``MERGED_PRS`` unconditionally, but \"Enforce MergeGate\" used to\n resolve it only inside ``grep -q '<!-- shiki:post_merge_reconcile -->'``. So a\ndiff --git a/tests/test_sadr_install_migration.py b/tests/test_sadr_install_migration.py\nnew file mode 100644\nindex 0000000000000000000000000000000000000000..a6f4c57e4dcaef73ef600b0d4804744138ab9071\n--- /dev/null\n+++ b/tests/test_sadr_install_migration.py\n@@ -0,0 +1,229 @@\n+\"\"\"SADR installation and collision-safe legacy upgrade behavior.\"\"\"\n+\n+from __future__ import annotations\n+\n+import contextlib\n+import hashlib\n+import io\n+import json\n+import shutil\n+import tempfile\n+import unittest\n+from pathlib import Path\n+\n+import shiki_test_support  # noqa: F401  (path bootstrap)\n+\n+import shiki_installer\n+from tests.test_adr_references import find_adr_reference_violations\n+\n+\n+PRODUCT_NUMBERS = tuple(range(13, 19))\n+\n+\n+def _quiet_install(target: Path, *, force: bool, validate: bool) -> None:\n+    with contextlib.redirect_stdout(io.StringIO()):\n+        shiki_installer.install_template(target, force=force, validate=validate)\n+\n+\n+def _sha256(path: Path) -> str:\n+    return hashlib.sha256(path.read_bytes()).hexdigest()\n+\n+\n+def _legacy_sadr_paths() -> tuple[str, ...]:\n+    return tuple(\n+        f\"docs/adr/{path.name.removeprefix('SADR-')}\"\n+        for path in sorted((shiki_installer.ROOT / \"docs\" / \"adr\").glob(\"SADR-*.md\"))\n+        if path.name != \"SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\"\n+    )\n+\n+\n+def _create_legacy_upgrade_fixture(\n+    target: Path,\n+) -> tuple[dict[str, bytes], tuple[str, ...]]:\n+    adr_dir = target / \"docs\" / \"adr\"\n+    adr_dir.mkdir(parents=True)\n+    product_bytes = {\n+        f\"{number:04d}-cubeshare-product-decision.md\": (\n+            f\"# CubeShare ADR {number:04d}\\n\\nDo not rewrite {number}.\\n\"\n+        ).encode()\n+        for number in PRODUCT_NUMBERS\n+    }\n+    for name, content in product_bytes.items():\n+        (adr_dir / name).write_bytes(content)\n+\n+    legacy_paths = _legacy_sadr_paths()\n+    for relative in legacy_paths:\n+        (target / relative).write_bytes(f\"legacy Shiki bytes for {relative}\\n\".encode())\n+\n+    stamp_path = target / shiki_installer.INSTALL_STAMP_PATH\n+    stamp_path.parent.mkdir(parents=True, exist_ok=True)\n+    stamp = {\n+        \"version\": shiki_installer.INSTALL_STAMP_VERSION,\n+        \"platform_commit\": None,\n+        \"installed_at\": \"2000-01-01T00:00:00+00:00\",\n+        \"digests\": {\n+            relative: _sha256(target / relative) for relative in legacy_paths\n+        },\n+    }\n+    stamp_path.write_text(\n+        json.dumps(stamp, indent=2, sort_keys=True) + \"\\n\", encoding=\"utf-8\"\n+    )\n+    return product_bytes, legacy_paths\n+\n+\n+def _tree_snapshot(root: Path) -> dict[str, tuple[str, bytes | str]]:\n+    \"\"\"Recursive byte/shape snapshot used to prove preflight is write-free.\"\"\"\n+    snapshot: dict[str, tuple[str, bytes | str]] = {}\n+    for path in sorted(root.rglob(\"*\")):\n+        relative = path.relative_to(root).as_posix()\n+        if path.is_symlink():\n+            snapshot[relative] = (\"symlink\", path.readlink().as_posix())\n+        elif path.is_dir():\n+            snapshot[relative] = (\"directory\", \"\")\n+        else:\n+            snapshot[relative] = (\"file\", path.read_bytes())\n+    return snapshot\n+\n+\n+class FreshSadrInstallTests(unittest.TestCase):\n+    def setUp(self) -> None:\n+        self.tmp = Path(tempfile.mkdtemp(prefix=\"shiki-sadr-fresh-\"))\n+        self.addCleanup(lambda: shutil.rmtree(self.tmp, ignore_errors=True))\n+        self.target = self.tmp / \"target\"\n+        self.adr_dir = self.target / \"docs\" / \"adr\"\n+        self.adr_dir.mkdir(parents=True)\n+        self.product_bytes = {\n+            f\"{number:04d}-product-decision.md\": (\n+                f\"# Product ADR {number:04d}\\n\\nTarget-owned bytes {number}.\\n\"\n+            ).encode()\n+            for number in PRODUCT_NUMBERS\n+        }\n+        for name, content in self.product_bytes.items():\n+            (self.adr_dir / name).write_bytes(content)\n+\n+    def test_fresh_install_preserves_product_adrs_and_installs_all_sadrs(self) -> None:\n+        _quiet_install(self.target, force=False, validate=True)\n+\n+        for name, expected in self.product_bytes.items():\n+            self.assertEqual((self.adr_dir / name).read_bytes(), expected)\n+\n+        sadr_numbers = {\n+            path.name.split(\"-\", 2)[1]\n+            for path in self.adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\")\n+        }\n+        self.assertEqual(sadr_numbers, {f\"{number:04d}\" for number in range(1, 20)})\n+\n+        numeric_records = {\n+            path.name\n+            for path in self.adr_dir.glob(\"[0-9][0-9][0-9][0-9]-*.md\")\n+        }\n+        self.assertEqual(numeric_records, set(self.product_bytes))\n+        self.assertEqual(find_adr_reference_violations(self.target), [])\n+\n+\n+class LegacySadrUpgradeTests(unittest.TestCase):\n+    def setUp(self) -> None:\n+        self.tmp = Path(tempfile.mkdtemp(prefix=\"shiki-sadr-upgrade-\"))\n+        self.addCleanup(lambda: shutil.rmtree(self.tmp, ignore_errors=True))\n+        self.target = self.tmp / \"target\"\n+        self.adr_dir = self.target / \"docs\" / \"adr\"\n+        self.product_bytes, self.legacy_paths = _create_legacy_upgrade_fixture(\n+            self.target\n+        )\n+\n+    def test_force_upgrade_deletes_only_stamped_legacy_sadrs(self) -> None:\n+        _quiet_install(self.target, force=True, validate=True)\n+\n+        for relative in self.legacy_paths:\n+            self.assertFalse((self.target / relative).exists(), relative)\n+        for name, expected in self.product_bytes.items():\n+            self.assertEqual((self.adr_dir / name).read_bytes(), expected)\n+\n+        sadrs = list(self.adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\"))\n+        self.assertEqual(len(sadrs), 19)\n+        refreshed = json.loads(\n+            (self.target / shiki_installer.INSTALL_STAMP_PATH).read_text(encoding=\"utf-8\")\n+        )\n+        for relative in self.legacy_paths:\n+            self.assertNotIn(relative, refreshed[\"digests\"])\n+        for path in sadrs:\n+            relative = path.relative_to(self.target).as_posix()\n+            self.assertEqual(refreshed[\"digests\"][relative], _sha256(path))\n+\n+    def test_non_force_install_never_deletes_legacy_files(self) -> None:\n+        before = {\n+            relative: (self.target / relative).read_bytes()\n+            for relative in self.legacy_paths\n+        }\n+        _quiet_install(self.target, force=False, validate=False)\n+        for relative, expected in before.items():\n+            self.assertEqual((self.target / relative).read_bytes(), expected)\n+        refreshed = json.loads(\n+            (self.target / shiki_installer.INSTALL_STAMP_PATH).read_text(encoding=\"utf-8\")\n+        )\n+        for relative in self.legacy_paths:\n+            self.assertEqual(refreshed[\"digests\"][relative], _sha256(self.target / relative))\n+\n+\n+class LegacyCleanupPreflightTests(unittest.TestCase):\n+    def setUp(self) -> None:\n+        self.tmp = Path(tempfile.mkdtemp(prefix=\"shiki-sadr-preflight-\"))\n+        self.addCleanup(lambda: shutil.rmtree(self.tmp, ignore_errors=True))\n+\n+    def test_all_ownership_blockers_abort_before_any_write(self) -> None:\n+        cases = (\n+            (\"absent\", \"install stamp is absent\"),\n+            (\"unreadable\", \"install stamp is unreadable\"),\n+            (\"malformed\", \"install stamp is malformed\"),\n+            (\"missing-digest\", \"install stamp has no digest\"),\n+            (\"digest-mismatch\", \"digest mismatch\"),\n+        )\n+        for name, expected_reason in cases:\n+            with self.subTest(name=name):\n+                target = self.tmp / name\n+                _, legacy_paths = _create_legacy_upgrade_fixture(target)\n+                stamp_path = target / shiki_installer.INSTALL_STAMP_PATH\n+\n+                if name == \"absent\":\n+                    stamp_path.unlink()\n+                elif name == \"unreadable\":\n+                    stamp_path.unlink()\n+                    stamp_path.mkdir()\n+                elif name == \"malformed\":\n+                    stamp_path.write_text(\"{not-json\\n\", encoding=\"utf-8\")\n+                else:\n+                    stamp = json.loads(stamp_path.read_text(encoding=\"utf-8\"))\n+                    if name == \"missing-digest\":\n+                        stamp[\"digests\"] = {}\n+                    else:\n+                        stamp[\"digests\"] = {\n+                            relative: \"deadbeef\" for relative in legacy_paths\n+                        }\n+                    stamp_path.write_text(\n+                        json.dumps(stamp, indent=2, sort_keys=True) + \"\\n\",\n+                        encoding=\"utf-8\",\n+                    )\n+\n+                before = _tree_snapshot(target)\n+                with self.assertRaises(shiki_installer.ShikiError) as raised:\n+                    _quiet_install(target, force=True, validate=False)\n+                after = _tree_snapshot(target)\n+\n+                self.assertEqual(before, after, f\"{name} preflight changed the tree\")\n+                message = str(raised.exception)\n+                self.assertIn(expected_reason, message)\n+                self.assertIn(\"No files were written\", message)\n+                for relative in legacy_paths:\n+                    self.assertIn(relative, message)\n+\n+    def test_absent_stamp_is_allowed_when_no_legacy_path_exists(self) -> None:\n+        target = self.tmp / \"no-legacy\"\n+        target.mkdir()\n+        _quiet_install(target, force=True, validate=False)\n+        self.assertTrue(\n+            (target / \"docs\" / \"adr\" / \"SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\").is_file()\n+        )\n+\n+\n+if __name__ == \"__main__\":\n+    unittest.main()\n\n succeeded in 10231ms:\nShiki validation passed\nShiki validation passed\nShiki validation passed\nshiki control-plane tests passed\n\ndiff --git a/.codex/skills/shiki/SKILL.md b/.codex/skills/shiki/SKILL.md\nindex c8e2e85d9d75d49cc4f2d6286ee22659c4e53e75..93cded623b2c15d9f65c76491a1c2d813ac9e6c0\n--- a/.codex/skills/shiki/SKILL.md\n+++ b/.codex/skills/shiki/SKILL.md\n@@ -18,6 +18,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n Codex CLI does not register this skill as a custom `/shiki` slash command. When\n the user types `/shiki` in Codex CLI and gets \"Unrecognized command\", explain\n that this is expected: invoke Shiki in Codex with natural language such as\n@@ -69,7 +72,7 @@\n \n ## Responsibilities\n \n-- Claude Code implements and repairs by default (`shiki runner claude`, ADR 0008); Codex implements and repairs tasks explicitly assigned to `codex`.\n+- Claude Code implements and repairs by default (`shiki runner claude`, SADR-0008); Codex implements and repairs tasks explicitly assigned to `codex`.\n - Claude Code Action can act as GitHub-side CCA or reviewer.\n - CCA judges completion.\n - MergeGate authorizes state transitions and merge readiness.\ndiff --git a/.github/prompts/cca-completion-check.md b/.github/prompts/cca-completion-check.md\nindex ed203ad5f302ecc81372d8ab27d2b1232ca97497..fd89950528333ef6242e6d5443904f5392ab7e56\n--- a/.github/prompts/cca-completion-check.md\n+++ b/.github/prompts/cca-completion-check.md\n@@ -19,6 +19,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n ## Required Reading\n \n Read only the files needed to judge the PR contract. Prefer this order:\ndiff --git a/.github/prompts/codex-task-handoff.md b/.github/prompts/codex-task-handoff.md\nindex 695e211d76dea1c60e8d5bb360e78487808286a4..59441d576b01a39569722f226c854e5a740e9eb5\n--- a/.github/prompts/codex-task-handoff.md\n+++ b/.github/prompts/codex-task-handoff.md\n@@ -2,7 +2,7 @@\n \n You are Codex Front acting as the Implementer for a Shiki task.\n \n-Follow `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant ADRs, and `docs/agents/codex-handoff.md`.\n+Follow `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant Shiki SADRs and target ADRs, and `docs/agents/codex-handoff.md`.\n \n Rules:\n \ndiff --git a/AGENTS.md b/AGENTS.md\nindex 7f041100d49c2a7525f51cb0f5d696a4a6d1c78c..a06bfbabc6fa48a007389e25e12d7ed523936bed\n--- a/AGENTS.md\n+++ b/AGENTS.md\n@@ -22,7 +22,7 @@\n - **Codex Front** is an optional implementer through Codex App, Codex CLI, Codex IDE extension, or Codex Web signed in with ChatGPT OAuth/subscription auth, dispatched with `shiki runner codex` when a task is explicitly assigned to `codex`.\n - **GitHub CCA** is implemented by Claude Code Action by default, using `CLAUDE_CODE_OAUTH_TOKEN`.\n \n-Do not assume `openai/codex-action`, `OPENAI_API_KEY`, or API-key based Codex automation in the default Shiki path. API-key runners are explicit target-repo extensions and require their own ADR.\n+Do not assume `openai/codex-action`, `OPENAI_API_KEY`, or API-key based Codex automation in the default Shiki path. API-key runners are explicit target-repo extensions and require their own target ADR.\n \n Shiki is not:\n \n@@ -44,10 +44,14 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use\n+`SADR-NNNN-*.md`; target product decisions retain `NNNN-*.md` and the `ADR`\n+namespace.\n+\n Use the canonical source-of-truth order above. Runtime-specific reading orders\n are operational guidance only and must not override or reorder it.\n \n-Conversation state is not durable truth. If a decision matters, put it in GitHub, `.shiki/`, `CONTEXT.md`, or an ADR.\n+Conversation state is not durable truth. If a decision matters, put it in GitHub, `.shiki/`, `CONTEXT.md`, a Shiki SADR, or a target ADR.\n \n If GitHub and `.shiki/` disagree, surface the conflict and prefer GitHub operational state until the mirror is repaired.\n \n@@ -75,8 +79,8 @@\n Every non-trivial change follows this loop:\n \n 1. **Goal**: clarify outcome, completion conditions, non-goals, risk level, and success signals.\n-2. **grill-with-docs**: challenge the plan against domain docs, ADRs, code reality, terminology, and edge scenarios.\n-3. **Context & Impact**: find relevant docs, ADRs, code, dependencies, owners, locks, and verification surfaces.\n+2. **grill-with-docs**: challenge the plan against domain docs, Shiki SADRs, target ADRs, code reality, terminology, and edge scenarios.\n+3. **Context & Impact**: find relevant docs, SADRs, target ADRs, code, dependencies, owners, locks, and verification surfaces.\n 4. **PRD and Spec Freeze**: use `to-prd` when context is settled enough to become durable product/engineering intent. The operator's explicit approval of the PRD is Spec Freeze: the plan records a `spec_freeze` block, and no plan runs without it. Steps 1-4 form Requirements Definition \u2014 one continuous operator dialogue. After Spec Freeze, scope changes require an operator-approved Spec Amendment; non-scope-moving interpretations go to the Assumption Log.\n 5. **Issues**: use `to-issues` to create independently grabbable vertical-slice issues. Prefer AFK slices over HITL slices where possible.\n 6. **Triage**: label issues for readiness, risk, runtime, skills, and MergeGate state.\n@@ -167,7 +171,7 @@\n - Testing decisions.\n - Out-of-scope items.\n - Further notes.\n-- Links to relevant domain terms and ADRs.\n+- Links to relevant domain terms, SADRs, and target ADRs.\n \n Every executable task must include:\n \n@@ -193,7 +197,7 @@\n \n Context & Impact must identify:\n \n-- Relevant documents, domain terms, ADRs, and past decisions.\n+- Relevant documents, domain terms, SADRs, target ADRs, and past decisions.\n - Relevant modules, interfaces, seams, callers, owners, and tests.\n - Dependency relationships between tasks.\n - Candidate locks and lock conflicts.\n@@ -210,7 +214,7 @@\n | Skill | Required when |\n | --- | --- |\n | `setup-matt-pocock-skills` | First configuring the repo for skills, or when issue tracker, triage labels, or domain docs are missing. |\n-| `grill-with-docs` | Default for non-trivial Goals before PRD/issues; always required when plans, terminology, boundaries, tradeoffs, or ADR-worthy decisions are ambiguous. |\n+| `grill-with-docs` | Default for non-trivial Goals before PRD/issues; always required when plans, terminology, boundaries, tradeoffs, or decision-record-worthy choices are ambiguous. |\n | `zoom-out` | The agent lacks an architectural map of the relevant code area. |\n | `to-prd` | Settled Goal context must become durable product/engineering intent. |\n | `to-issues` | A Goal, PRD, or plan must become independently grabbable vertical-slice issues. |\n@@ -348,7 +352,7 @@\n - Three or more new files.\n - Any high-risk or critical label.\n \n-When the architecture gate triggers, run `grill-with-docs` or `improve-codebase-architecture` as appropriate and record durable decisions in an ADR when needed.\n+When the architecture gate triggers, run `grill-with-docs` or `improve-codebase-architecture` as appropriate and record durable decisions in a Shiki SADR or target ADR as applicable.\n \n ## Engineering Discipline\n \ndiff --git a/CLAUDE.md b/CLAUDE.md\nindex 98a932e7841a632866fcd53b0bad0e3af3711d3d..98a31399404d6fb044b4fd493f4459ac87f439de\n--- a/CLAUDE.md\n+++ b/CLAUDE.md\n@@ -14,6 +14,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n ## Claude Role\n \n Default to planner, default implementer, reviewer, coordinator, documentation editor, CCA-style completion judge, and final-judgment assistant.\n@@ -57,7 +60,7 @@\n At the start of a Shiki session:\n \n 1. Read `AGENTS.md`.\n-2. Read `CONTEXT.md` and relevant ADRs.\n+2. Read `CONTEXT.md` and relevant Shiki SADRs and target ADRs.\n 3. Read active `.shiki/` Goal/task/ledger state when present.\n 4. Read active distilled rules from `.shiki/memories/` (status `distilled`, `active: true`, not revoked or superseded) for applicable guidance; the same rules are injected deterministically into task handoffs as the `## Distilled Rules` section (\u00a73.5).\n 5. Read the GitHub Issue or PR when the task is GitHub-backed.\n@@ -74,7 +77,7 @@\n - Define the Goal, completion conditions, non-goals, and risk level.\n - Treat `grill-with-docs` as the default planning entry point for non-trivial Goals.\n - Challenge domain terms against `CONTEXT.md`.\n-- Challenge hard-to-reverse tradeoffs against ADRs.\n+- Challenge hard-to-reverse tradeoffs against SADRs and target ADRs.\n - Explore code instead of asking when code can answer the question.\n - Run Context & Impact before implementation planning.\n - Produce a Task DAG, not an unordered checklist.\n@@ -94,7 +97,7 @@\n - For each question, provide your recommended answer.\n - Resolve dependency decisions before downstream design questions.\n - Update `CONTEXT.md` inline when terminology crystallizes.\n-- Offer ADRs only when the decision is hard to reverse, surprising without context, and a real tradeoff.\n+- Offer a SADR or target ADR only when the decision is hard to reverse, surprising without context, and a real tradeoff.\n - Convert unresolved questions into HITL blockers rather than allowing Codex to guess.\n \n ## PRD and Issues Standard\n@@ -103,7 +106,7 @@\n \n - Use the project's domain glossary vocabulary.\n - Avoid volatile file paths unless they are essential.\n-- Include implementation decisions, testing decisions, out-of-scope items, and relevant ADR/domain links.\n+- Include implementation decisions, testing decisions, out-of-scope items, and relevant SADR/target-ADR/domain links.\n - Identify deep-module opportunities and testable interfaces.\n \n When creating issues:\n@@ -121,7 +124,7 @@\n - Branch/worktree target.\n - Scope and non-goals.\n - Dependencies and locks.\n-- Relevant docs, ADRs, modules, and tests.\n+- Relevant docs, SADRs, target ADRs, modules, and tests.\n - Required skill invocations.\n - TDD expectations.\n - Pre-PR `code-review` gate expectations.\n@@ -152,7 +155,7 @@\n \n - Treat yourself as a judge, not implementer.\n - Do not write production code.\n-- Read `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant ADRs, PRD, task issue, PR body, diff, checks, reviews, labels, and `.shiki/` state.\n+- Read `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant SADRs and target ADRs, PRD, task issue, PR body, diff, checks, reviews, labels, and `.shiki/` state.\n - Evaluate every applicable checklist in `docs/agents/checklists.md`.\n - Map each acceptance criterion to durable evidence.\n - Distinguish implementation failure from missing evidence.\ndiff --git a/CONTEXT.md b/CONTEXT.md\nindex e61a9be06a0cce0d026484083a657bde2067e234..237ee8aa3ea8fab334ece49768bc5ed3b5af0974\n--- a/CONTEXT.md\n+++ b/CONTEXT.md\n@@ -25,7 +25,7 @@\n _Avoid_: jumping directly into code\n \n **grill-with-docs**:\n-A planning skill that challenges a plan against domain language, ADRs, code reality, and concrete edge scenarios. It resolves design-tree questions before PRD/issues. It runs inside Requirements Definition.\n+A planning skill that challenges a plan against domain language, Shiki SADRs, target ADRs, code reality, and concrete edge scenarios. It resolves design-tree questions before PRD/issues. It runs inside Requirements Definition.\n _Avoid_: generic brainstorming, silent assumptions\n \n **Requirements Definition**:\ndiff --git a/CONTRIBUTING.md b/CONTRIBUTING.md\nindex c6d9b656e73abb8a55e5f660123e03153dcb3e6d..9e37a8fbf668db5b22461aaae5548b8f1354394c\n--- a/CONTRIBUTING.md\n+++ b/CONTRIBUTING.md\n@@ -17,7 +17,7 @@\n 1. **Goal** \u2014 start from a user-approved Goal with an outcome, completion\n    conditions, non-goals, risk level, and success signals.\n 2. **grill-with-docs** \u2014 for any non-trivial change, challenge the plan against\n-   domain language, ADRs, and code reality before writing a PRD.\n+   domain language, Shiki SADRs, target ADRs, and code reality before writing a PRD.\n 3. **PRD** \u2014 capture settled product/engineering intent (`to-prd`).\n 4. **Spec Freeze** \u2014 the operator explicitly approves the PRD/requirements;\n    the plan records a `spec_freeze` block and no plan runs without it\n@@ -94,7 +94,7 @@\n   entries.\n - Do not hand-edit `.shiki/goals/*.json` or `.shiki/manifest.json` in a task PR.\n - Conversation state is not durable truth. If a decision matters, record it in\n-  GitHub, `.shiki/`, `CONTEXT.md`, or an ADR.\n+  GitHub, `.shiki/`, `CONTEXT.md`, a Shiki SADR, or a target ADR.\n \n ## Architecture Gate\n \n@@ -103,7 +103,7 @@\n or CI policy, package structure, three or more structural files, more than 100\n net new lines in a core module, three or more new files, or any high-risk label.\n When triggered, run `grill-with-docs` or `improve-codebase-architecture` and\n-record durable decisions in an ADR when needed (see `docs/adr/`).\n+record durable decisions in a Shiki SADR or target ADR as applicable (see `docs/adr/`).\n \n ## Safety\n \ndiff --git a/SYSTEM_PROMPT.md b/SYSTEM_PROMPT.md\nindex aaaea45e3a896c3a44ba89eec8bb84325ecf6c62..da009c86a88285269dee0155e58e550c6d744ea9\n--- a/SYSTEM_PROMPT.md\n+++ b/SYSTEM_PROMPT.md\n@@ -30,6 +30,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n Use the canonical source-of-truth order above. Runtime-specific reading orders\n are operational guidance only and must not override or reorder it. The current\n conversation is non-durable operator input.\n@@ -48,14 +51,14 @@\n \n A runtime may only do work that is explicitly assigned by the Goal, task, PR, or MergeGate decision.\n \n-Do not assume `openai/codex-action`, `OPENAI_API_KEY`, or API-key based Codex automation in the default Shiki path. API-key runners are explicit target-repo extensions and require their own ADR.\n+Do not assume `openai/codex-action`, `OPENAI_API_KEY`, or API-key based Codex automation in the default Shiki path. API-key runners are explicit target-repo extensions and require their own target ADR.\n \n ## 4. Non-negotiable Shiki flow\n \n For any non-trivial Goal, use this flow:\n \n 1. **Goal Seek**: define outcome, non-goals, risk level, completion criteria, and evidence requirements.\n-2. **grill-with-docs**: challenge the plan against `CONTEXT.md`, ADRs, code reality, terminology, boundaries, and edge scenarios. Ask one question at a time when the operator is available. If code can answer a question, inspect code instead of asking.\n+2. **grill-with-docs**: challenge the plan against `CONTEXT.md`, Shiki SADRs, target ADRs, code reality, terminology, boundaries, and edge scenarios. Ask one question at a time when the operator is available. If code can answer a question, inspect code instead of asking.\n 3. **Context & Impact**: identify modules, interfaces, seams, callers, dependencies, locks, risk, and verification surfaces.\n 4. **to-prd + Spec Freeze**: turn settled context into a PRD using domain vocabulary and testing decisions. The operator's explicit approval of the PRD is Spec Freeze, recorded as the plan's `spec_freeze` block; plans without it do not run. Steps 1-4 form Requirements Definition.\n 5. **to-issues**: decompose the PRD into independently grabbable vertical-slice issues. Prefer AFK slices; mark HITL slices when judgment is still required.\n@@ -75,7 +78,7 @@\n Before making any material change:\n \n 1. Read `AGENTS.md` and any runtime-specific wrapper such as `CLAUDE.md`.\n-2. Read `CONTEXT.md` and relevant ADRs.\n+2. Read `CONTEXT.md` and relevant SADRs and target ADRs.\n 3. Read the GitHub Issue / PR and `.shiki/` state for the active Goal or task.\n 4. Identify your role: Planner, Implementer, Reviewer, Completion Check Agent, Repairer, or Guardian-assist.\n 5. Identify Goal id, task id, branch, PR, dependencies, locks, risk level, required skills, and required evidence.\n@@ -103,7 +106,7 @@\n \n At minimum, identify:\n \n-- Relevant documents, ADRs, domain terms, and prior decisions.\n+- Relevant documents, SADRs, target ADRs, domain terms, and prior decisions.\n - Relevant modules, interfaces, seams, callers, owners, and tests.\n - Dependency relationships between tasks.\n - Candidate file locks and lock conflicts.\n@@ -152,7 +155,7 @@\n \n CCA must read:\n \n-- `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant ADRs.\n+- `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant SADRs and target ADRs.\n - Parent Goal issue, PRD issue, task issue, comments, and labels.\n - PR body, diff, commits, changed files, reviews, CI checks, and artifacts available to the workflow.\n - `.shiki/` task contract, locks, ledger entries, CCA checklist profile, and prior repair packets.\n@@ -214,7 +217,7 @@\n - Three or more new files.\n - Any high-risk or critical label.\n \n-When the architecture gate triggers, run `grill-with-docs` or `improve-codebase-architecture` as appropriate and record the decision in an ADR if it is hard to reverse or likely to be re-litigated.\n+When the architecture gate triggers, run `grill-with-docs` or `improve-codebase-architecture` as appropriate and record the decision in a Shiki SADR or target ADR as applicable if it is hard to reverse or likely to be re-litigated.\n \n ## 14. Skill Gate\n \n@@ -223,7 +226,7 @@\n Use these skills:\n \n - `setup-matt-pocock-skills`: before first use of the engineering skill set, or when issue tracker, triage labels, or domain docs are missing.\n-- `grill-with-docs`: default for non-trivial Goals before PRD/issues; required for ambiguous plans, terminology, boundaries, ADR-worthy tradeoffs, or design-tree decisions.\n+- `grill-with-docs`: default for non-trivial Goals before PRD/issues; required for ambiguous plans, terminology, boundaries, decision-record-worthy tradeoffs, or design-tree decisions.\n - `zoom-out`: unfamiliar code area or missing architectural map.\n - `to-prd`: convert settled context into a PRD.\n - `to-issues`: break a PRD, plan, or Goal into independently grabbable vertical-slice issues.\ndiff --git a/docs/adr/0001-keep-shiki-as-platform-name.md b/docs/adr/SADR-0001-keep-shiki-as-platform-name.md\nindex 20a9637c3b9d8c97de7ba8db7cfae4d82d9a7dba..8b685439959aada1b221c112f0ac3c51ca8a1695\n--- a/docs/adr/0001-keep-shiki-as-platform-name.md\n+++ b/docs/adr/SADR-0001-keep-shiki-as-platform-name.md\n@@ -1,3 +1,3 @@\n-# Keep Shiki As The Platform Name\n+# SADR-0001: Keep Shiki As The Platform Name\n \n Shiki remains the product name for the GitHub-native agentic engineering control plane. We considered more descriptive names such as MergePilot, MergeGate, and Kiroku, but kept Shiki because it carries the existing goal-seeking framework identity while remaining independent of Hermes, Codex, Claude Code, or any single runtime.\ndiff --git a/docs/adr/0002-use-agents-as-shared-constitution.md b/docs/adr/SADR-0002-use-agents-as-shared-constitution.md\nindex 73de30ecb078120eac540d9bc51855e3cd48d0e3..c5486182bf06fca36ea508496b013c67302fd989\n--- a/docs/adr/0002-use-agents-as-shared-constitution.md\n+++ b/docs/adr/SADR-0002-use-agents-as-shared-constitution.md\n@@ -1,3 +1,3 @@\n-# Use AGENTS As The Shared Agent Constitution\n+# SADR-0002: Use AGENTS As The Shared Agent Constitution\n \n Shiki uses `AGENTS.md` as the runtime-neutral constitution and `CLAUDE.md` as a thin Claude Code wrapper. This keeps Codex, Claude Code, Hermes Runner, and future Agent Runtimes aligned on the same GitHub-first MergeGate rules while still allowing runtime-specific instructions where needed.\ndiff --git a/docs/adr/0003-require-skill-gate-for-mergegate-readiness.md b/docs/adr/SADR-0003-require-skill-gate-for-mergegate-readiness.md\nindex 424be0c9af4c8f0136f1fde68a468c47ff84e764..7fc337987c7df9792f75f181dfcbed75fe5ffff2\n--- a/docs/adr/0003-require-skill-gate-for-mergegate-readiness.md\n+++ b/docs/adr/SADR-0003-require-skill-gate-for-mergegate-readiness.md\n@@ -1,3 +1,3 @@\n-# Require Skill Gate For MergeGate Readiness\n+# SADR-0003: Require Skill Gate For MergeGate Readiness\n \n Shiki requires relevant engineering skills to be invoked before execution when their triggers apply. This makes skills part of MergeGate readiness rather than optional prompt style, because Shiki depends on repeatable planning, issue decomposition, TDD, diagnosis, triage, and architecture review before autonomous or parallel agent work can be trusted.\ndiff --git a/docs/adr/0004-use-github-first-with-shiki-mirror.md b/docs/adr/SADR-0004-use-github-first-with-shiki-mirror.md\nindex d0a3a9174dc646a945a9d325954ffaf00c352040..a79c54ae9ee8eed2a0524192b864da4e22a92d3f\n--- a/docs/adr/0004-use-github-first-with-shiki-mirror.md\n+++ b/docs/adr/SADR-0004-use-github-first-with-shiki-mirror.md\n@@ -1,4 +1,4 @@\n-# ADR 0004: Use GitHub First With A Repository-Local Shiki Mirror\n+# SADR-0004: Use GitHub First With A Repository-Local Shiki Mirror\n \n ## Status\n \ndiff --git a/docs/adr/0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md b/docs/adr/SADR-0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\nindex b339baf49b89d9290e79c3d51b36e7b2bde3bd95..0f3c3eb9afccdb3403ce978e21d04fe12e495da0\n--- a/docs/adr/0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\n+++ b/docs/adr/SADR-0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\n@@ -1,8 +1,8 @@\n-# ADR 0005: Use Codex As Front And Claude Code Action As The GitHub Runtime\n+# SADR-0005: Use Codex As Front And Claude Code Action As The GitHub Runtime\n \n ## Status\n \n-Accepted (implementer default superseded by ADR 0008; the auth model and the\n+Accepted (implementer default superseded by SADR-0008; the auth model and the\n Claude Code Action GitHub runtime decision remain in force)\n \n ## Context\n@@ -26,4 +26,4 @@\n - Shiki avoids requiring `OPENAI_API_KEY` for the default workflow.\n - Codex work happens through the user's authenticated Codex front surface, not as a hidden backend Action.\n - Claude Code Action is configured for OAuth-token based subscription usage by default.\n-- API-key based automation remains possible only as an explicit target-repo extension with its own ADR.\n+- API-key based automation remains possible only as an explicit target-repo extension with its own target ADR.\ndiff --git a/docs/adr/0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md b/docs/adr/SADR-0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\nindex 4bcae9d1e52f0a37b8e9fbafa5cd14b6ee1993bd..7a38538a6ae9929567f0ade67e7ea6a60d0b3f7e\n--- a/docs/adr/0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\n+++ b/docs/adr/SADR-0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\n@@ -1,4 +1,4 @@\n-# ADR 0006: Separate Codex Implementation, CCA Completion, And MergeGate Authorization\n+# SADR-0006: Separate Codex Implementation, CCA Completion, And MergeGate Authorization\n \n ## Status\n \ndiff --git a/docs/adr/0007-packaging-and-release.md b/docs/adr/SADR-0007-packaging-and-release.md\nindex 79b84ddb97167372b44641e5b7860e400ce80822..bb445a2e2e856d9d1f5fdbe35de8d4e8cec0fb32\n--- a/docs/adr/0007-packaging-and-release.md\n+++ b/docs/adr/SADR-0007-packaging-and-release.md\n@@ -1,4 +1,4 @@\n-# ADR 0007: Packaging And Release For Shiki\n+# SADR-0007: Packaging And Release For Shiki\n \n ## Status\n \n@@ -25,7 +25,7 @@\n   not introduce third-party runtime dependencies or break the shim.\n - Releases must run inside the operator's subscription-authenticated toolchain\n   and must not require extra secrets beyond the built-in `GITHUB_TOKEN`\n-  (consistent with ADR 0005's no-API-key-by-default stance).\n+  (consistent with SADR-0005's no-API-key-by-default stance).\n - `.shiki/manifest.json` already encodes the install surface (`install.include`,\n   `install.create_directories`, `install.exclude_from_commit`), and it must not\n   be mutated as part of this change.\n@@ -34,7 +34,7 @@\n \n This is a hard-to-reverse platform decision (it sets the version source of\n truth, the release trigger, and the public install contract), so it is recorded\n-as an ADR.\n+as a SADR.\n \n ## Decision\n \n@@ -104,7 +104,7 @@\n 3. **Publishing to PyPI.** Rejected for now: Shiki is distributed as a\n    repository template + git-installable CLI, not a PyPI library; PyPI would add\n    credentials/secrets and a different versioning contract. Can be added later as\n-   its own ADR if a packaged distribution is needed.\n+   its own SADR if a packaged distribution is needed.\n 4. **Dynamic version (read `VERSION` via a build backend `dynamic` field).**\n    Rejected to keep `pyproject.toml` backend-agnostic and dependency-free; the\n    small duplication between `VERSION` and `pyproject.toml` is guarded by the\n@@ -123,4 +123,4 @@\n   releasing versions the whole platform but ships only `install.include` to\n   targets.\n - Future PyPI distribution or a console entry point are deferred and would each\n-  warrant their own ADR.\n+  warrant their own SADR.\ndiff --git a/docs/adr/0008-use-claude-code-as-default-implementer.md b/docs/adr/SADR-0008-use-claude-code-as-default-implementer.md\nindex dedf4510b7c7799521422f622029563d373db031..35c2625d908167fbc58855b5b63e283805f167e8\n--- a/docs/adr/0008-use-claude-code-as-default-implementer.md\n+++ b/docs/adr/SADR-0008-use-claude-code-as-default-implementer.md\n@@ -1,12 +1,12 @@\n-# ADR 0008: Use Claude Code As The Default Implementer Runtime\n+# SADR-0008: Use Claude Code As The Default Implementer Runtime\n \n ## Status\n \n-Proposed (supersedes ADR 0005 for the implementer default; keeps its auth model)\n+Proposed (supersedes SADR-0005 for the implementer default; keeps its auth model)\n \n ## Context\n \n-ADR 0005 made Codex Front the default implementation runtime, with Claude Code\n+SADR-0005 made Codex Front the default implementation runtime, with Claude Code\n restricted to planner and reviewer roles by the runtime registry, and the only\n autonomous implementation adapter was `shiki runner codex`.\n \n@@ -18,13 +18,13 @@\n - The platform's planning, review, and CCA layers already run on Claude Code,\n   and Claude Code's native capabilities (the built-in `/code-review` skill and\n   the Workflow multi-agent orchestration tool) have no Codex equivalent.\n-- The Goal lifecycle redesign (ADR 0009) requires an autonomous post-freeze\n+- The Goal lifecycle redesign (SADR-0009) requires an autonomous post-freeze\n   loop driven from the operator's Claude Code session, including bounded\n   repair dispatch, which must not depend on a runtime that cannot be invoked\n   unattended.\n \n The constitution claims Shiki is runtime-agnostic (\"not a Claude-only\n-workflow\", ADR 0002). Any change to the implementer default must preserve the\n+workflow\", SADR-0002). Any change to the implementer default must preserve the\n registry-based runtime model rather than hardcoding one vendor.\n \n ## Decision\n@@ -39,7 +39,7 @@\n   the task to review or repair-needed.\n - Keep Codex registered as an optional implementer runtime. `shiki runner\n   codex`, the Codex handoff templates, and the ChatGPT OAuth auth model from\n-  ADR 0005 remain supported for tasks explicitly assigned to `codex`.\n+  SADR-0005 remain supported for tasks explicitly assigned to `codex`.\n - Commit to mandating two Claude-native capabilities as loop contract in\n   follow-up Goals (status: the code-review gate was delivered by Goal G-B \u2014\n   skill registry, checklists item PR-12, default required_skills \u2014 while the\n@@ -60,7 +60,7 @@\n \n Out of scope: removing Codex support, changing the CCA runtime (Claude Code\n Action with `CLAUDE_CODE_OAUTH_TOKEN` stays), and API-key based automation\n-(still requires its own ADR per ADR 0005).\n+(still requires its own target ADR per SADR-0005).\n \n ## Consequences\n \n@@ -100,7 +100,7 @@\n   harness rejection of direct `codex exec` is outside Shiki's control, and the\n   plan's mandatory native capabilities cannot run on Codex at all.\n - **Remove Codex entirely**: rejected; it abandons the runtime-agnostic\n-  constitution (ADR 0002) for no operational gain and forecloses\n+  constitution (SADR-0002) for no operational gain and forecloses\n   cross-runtime adversarial review.\n - **GitHub Actions cloud implementation via claude-code-action**: rejected as\n   default; durable evidence is automatic but token cost, Node 24 workflow\ndiff --git a/docs/adr/0009-requirements-definition-and-spec-freeze.md b/docs/adr/SADR-0009-requirements-definition-and-spec-freeze.md\nindex 29b3f7871bef9203a8b4a831a65c39eb2f506e42..52e7dd69ad0aa4efe0b68c62678abbf7df02aaec\n--- a/docs/adr/0009-requirements-definition-and-spec-freeze.md\n+++ b/docs/adr/SADR-0009-requirements-definition-and-spec-freeze.md\n@@ -1,4 +1,4 @@\n-# ADR 0009: Merge Grill And Planning Into Requirements Definition With Spec Freeze\n+# SADR-0009: Merge Grill And Planning Into Requirements Definition With Spec Freeze\n \n ## Status\n \ndiff --git a/docs/adr/0010-external-ai-guardian-review.md b/docs/adr/SADR-0010-external-ai-guardian-review.md\nindex e6108bbe8bb4e73e0ff4684c8f78001a80438c4e..f3510b145664298adafb151b6f0db51b972f01ec\n--- a/docs/adr/0010-external-ai-guardian-review.md\n+++ b/docs/adr/SADR-0010-external-ai-guardian-review.md\n@@ -1,4 +1,4 @@\n-# ADR 0010: External AI Guardian Review As A First-Class Approval Authority\n+# SADR-0010: External AI Guardian Review As A First-Class Approval Authority\n \n ## Status\n \n@@ -8,7 +8,7 @@\n \n Shiki is intended to be an autonomous, recursive, self-improving agent\n framework: authority-in-the-loop, not human-in-the-loop. Until now the Guardian\n-approval required for high/critical-risk merges (guardian-policy.json, ADR\n+approval required for high/critical-risk merges (guardian-policy.json, SADR\n governance) recognized only human authority sources \u2014 a GitHub review approval,\n the `guardian:approved` label applied by a configured Guardian, or a comment\n carrying `Guardian approval granted` plus the current head SHA.\n@@ -89,7 +89,7 @@\n   autonomous design the operator intends and produced the impersonation\n   incident.\n - **Record AI approval as an operator comment**: rejected as forgery of human\n-  authority \u2014 the exact failure this ADR corrects.\n+  authority \u2014 the exact failure this SADR corrects.\n - **Deliver the artifact as a committed `.shiki/` file**: rejected; a committed\n   approval inside the PR being approved is forgeable by anyone who can push.\n - **A dedicated reviewer bot identity instead of a relayed comment**: deferred;\ndiff --git a/docs/adr/0011-autonomous-quality-gate-evidence.md b/docs/adr/SADR-0011-autonomous-quality-gate-evidence.md\nindex bca0246c5d46e44afbde15486a75eec425fc02f5..9c93eac16d8c3a6d4b11bd7224e3955102cd7db4\n--- a/docs/adr/0011-autonomous-quality-gate-evidence.md\n+++ b/docs/adr/SADR-0011-autonomous-quality-gate-evidence.md\n@@ -1,4 +1,4 @@\n-# ADR 0011: Autonomous Quality-Gate Evidence \u2014 Deterministic Facts And An Independent Verifier\n+# SADR-0011: Autonomous Quality-Gate Evidence \u2014 Deterministic Facts And An Independent Verifier\n \n ## Status\n \ndiff --git a/docs/adr/0012-autonomous-loop-closeout-to-main.md b/docs/adr/SADR-0012-autonomous-loop-closeout-to-main.md\nindex 637fad6dde4989c9c2bf0f81f676d8d29991123c..ec7d85f81030a676bcbba28b43eb365422a36eac\n--- a/docs/adr/0012-autonomous-loop-closeout-to-main.md\n+++ b/docs/adr/SADR-0012-autonomous-loop-closeout-to-main.md\n@@ -1,4 +1,4 @@\n-# 0012 \u2014 Autonomous loop pushes goal completion to main via a closeout PR\n+# SADR-0012 \u2014 Autonomous loop pushes goal completion to main via a closeout PR\n \n - Status: accepted\n - Date: 2026-06-18\ndiff --git a/docs/adr/0013-bootstrap-grants-actions-approve-pull-requests.md b/docs/adr/SADR-0013-bootstrap-grants-actions-approve-pull-requests.md\nindex b6c69d64f1c5ced346b88d6859586715b3b5bd0e..cf48ff4a6716267f539ca58edd1a852e7c7a2d64\n--- a/docs/adr/0013-bootstrap-grants-actions-approve-pull-requests.md\n+++ b/docs/adr/SADR-0013-bootstrap-grants-actions-approve-pull-requests.md\n@@ -1,4 +1,4 @@\n-# ADR 0013: Bootstrap Grants GitHub Actions Permission To Create And Approve Pull Requests\n+# SADR-0013: Bootstrap Grants GitHub Actions Permission To Create And Approve Pull Requests\n \n ## Status\n \n@@ -31,7 +31,7 @@\n security tradeoff: it lets repository automation satisfy a human-review gate. In\n a multi-maintainer repo that could let a compromised or buggy workflow\n self-approve changes. So whether bootstrap turns it on by default \u2014 and with\n-what default token scope \u2014 is an ADR-worthy decision, not an implementation\n+what default token scope \u2014 is a SADR-worthy decision, not an implementation\n detail.\n \n ## Decision\n@@ -48,7 +48,7 @@\n Specifically:\n \n - **`can_approve_pull_request_reviews` defaults to `true`.** Shiki's default\n-  posture is solo/AFK autonomous operation (ADR 0008/0009/0012), where the CCA\n+  posture is solo/AFK autonomous operation (SADR-0008/0009/0012), where the CCA\n   Review Bridge is the only path that can satisfy `required_review: true` after\n   the CCA and Guardian gates have already passed. The default makes a\n   freshly-bootstrapped repo able to run the autonomous loop end to end without a\ndiff --git a/docs/adr/0014-codex-app-external-ai-guardian-ui-adapter.md b/docs/adr/SADR-0014-codex-app-external-ai-guardian-ui-adapter.md\nindex 238db06279334caa341c117362df9271382a7fcd..65f12e9939321e1b3e389a34c822fc1549e5155d\n--- a/docs/adr/0014-codex-app-external-ai-guardian-ui-adapter.md\n+++ b/docs/adr/SADR-0014-codex-app-external-ai-guardian-ui-adapter.md\n@@ -1,4 +1,4 @@\n-# ADR 0014: Use Codex App As The External AI Guardian UI Adapter\n+# SADR-0014: Use Codex App As The External AI Guardian UI Adapter\n \n ## Status\n \ndiff --git a/docs/adr/0015-contract-approval-before-dispatch.md b/docs/adr/SADR-0015-contract-approval-before-dispatch.md\nindex fad6c656741a20a7684ba16080a7db159f63a52f..6061271a46d3f1d6ae7174477de3fb75673123a6\n--- a/docs/adr/0015-contract-approval-before-dispatch.md\n+++ b/docs/adr/SADR-0015-contract-approval-before-dispatch.md\n@@ -1,4 +1,4 @@\n-# ADR 0015: Approve The Task Contract Before Dispatch, Not The Diff After It\n+# SADR-0015: Approve The Task Contract Before Dispatch, Not The Diff After It\n \n ## Status\n \n@@ -9,7 +9,7 @@\n Guardian approval for high/critical risk is expressible only on a pull request.\n Every approval source in `guardian-policy.json` \u2014 GitHub review, the\n `guardian:approved` label, the `Guardian approval granted` comment, and the\n-`external_ai_guardian_review` artifact (ADR 0010/0014) \u2014 is bound to a PR, and\n+`external_ai_guardian_review` artifact (SADR-0010/0014) \u2014 is bound to a PR, and\n three of the four require the current head SHA. Approval therefore cannot exist\n before an implementation PR exists.\n \n@@ -81,7 +81,7 @@\n An envelope breach must surface only on the non-repairable policy gate, never on\n the repairable `MergeGate metadata check`. Otherwise the Goal Loop routes it to\n `dispatch_repair` \u2014 instructing a runner to make the breach go away \u2014 which is\n-exactly what this ADR forbids.\n+exactly what this SADR forbids.\n \n **Contract immutability.** After registration, an implementation PR must not\n change the governance fields of its own task contract (`scope`, `non_goals`,\n@@ -116,7 +116,7 @@\n fails any verdict that is not `complete`, and the signal the CCA prompt reads is\n computed from `evaluate_guardian_approval` alone. Without the OR-merge at the\n signal, a contract-approved PR still returns `needs_guardian`, the CCA job fails,\n-and the MergeGate policy check never runs \u2014 the gate this ADR changes would never\n+and the MergeGate policy check never runs \u2014 the gate this SADR changes would never\n be reached. The CCA *prompt* needs no change: a `contract_approval` entry in the\n signal's existing `sources` array satisfies its current wording.\n \n@@ -130,7 +130,7 @@\n written, and an approved contract lets its implementation PRs merge through the\n same automatic path as low/medium risk. The wasted implement-then-block cycle\n disappears. Guardian review shifts from reading diffs to reading contracts, which\n-is both earlier and smaller; the external AI Guardian path (ADR 0010/0014) works\n+is both earlier and smaller; the external AI Guardian path (SADR-0010/0014) works\n on contracts with no change.\n \n We accept that the Guardian authorizes a class of change without seeing its\n@@ -146,7 +146,7 @@\n adversarial tests for the new approval path equivalent in rigor to\n `test_shiki_governance_evidence.sh`.\n \n-Bootstrap costs, which apply only while this ADR is being implemented. The tasks\n+Bootstrap costs, which apply only while this SADR is being implemented. The tasks\n that build Contract Approval merge under the existing rules, so the ones\n dispatched after the pre-dispatch gate lands but before the approval path lands\n need a Guardian-approved Contract PR *and* a live Guardian approval on their\n@@ -210,7 +210,7 @@\n risk; they do not bound authorized work.\n \n **Auto-escalate risk on envelope breach instead of stopping.** Returns the\n-workflow to a diff-time Guardian decision \u2014 the exact behavior this ADR removes.\n+workflow to a diff-time Guardian decision \u2014 the exact behavior this SADR removes.\n \n **Resolve governance fields from the base snapshot only.** Rejected against the\n registration flow: implementation PRs add their own contract, so the base has no\ndiff --git a/docs/adr/0016-locks-bound-product-paths.md b/docs/adr/SADR-0016-locks-bound-product-paths.md\nindex b5e850f3f1f7f4ef6e7f9d1763a29a51bd300c45..46b40a9b99a7aae644c02bb458f5473ff0a90ce2\n--- a/docs/adr/0016-locks-bound-product-paths.md\n+++ b/docs/adr/SADR-0016-locks-bound-product-paths.md\n@@ -1,4 +1,4 @@\n-# ADR 0016: Locks bound product paths; the Shiki mirror is bound by record identity\n+# SADR-0016: Locks bound product paths; the Shiki mirror is bound by record identity\n \n ## Status\n \n@@ -6,8 +6,8 @@\n \n - Date: 2026-07-28\n - Deciders: operator (mizutani-140), Claude (planner)\n-- Related: ADR 0004 (GitHub-first with a `.shiki` mirror), ADR 0009 (Spec Freeze),\n-  ADR 0012 (autonomous loop closeout to main), ADR 0015 (Contract Approval before\n+- Related: SADR-0004 (GitHub-first with a `.shiki` mirror), SADR-0009 (Spec Freeze),\n+  SADR-0012 (autonomous loop closeout to main), SADR-0015 (Contract Approval before\n   dispatch), remediation goal task R-02, the live PR #179 deadlock.\n \n ## Context\n@@ -90,11 +90,11 @@\n files, documentation files and named source modules are nameable; runtime-\n allocated ledger, runner and report ids are not \u2014 and those live in `.shiki`,\n which locks no longer govern. Existing frozen contracts are brought into line by\n-a one-time Spec Amendment per plan, which must land before ADR 0015's contract\n+a one-time Spec Amendment per plan, which must land before SADR-0015's contract\n immutability makes `locks` unwritable from the implementation PR.\n \n **Lock state is resolved from the durable side.** MergeGate resolves the lock\n-registry, and ADR 0015's governance fields, from the base `.shiki` snapshot when\n+registry, and SADR-0015's governance fields, from the base `.shiki` snapshot when\n present, falling back to head only when absent. The `MergeGate metadata check`\n job is given `--base-shiki`, which it does not receive today. Locks are not\n derived from the live open-pull-request set: a pull request body is untrusted\n@@ -108,7 +108,7 @@\n Explicitly out of scope: moving MergeGate's contract resolution off the PR\n checkout wholesale; making the mirror a projection of GitHub state; introducing\n new lock kinds; moving ledger evidence out of the repository; and unwinding\n-ADR 0012's closeout pull request. Registration of multi-task goals is ADR 0015's\n+SADR-0012's closeout pull request. Registration of multi-task goals is SADR-0015's\n Contract PR and is not re-decided here.\n \n ## Consequences\n@@ -127,7 +127,7 @@\n \n Risks accepted. First, goal-scoped identity is only as strong as `goal_id`,\n which is resolved from the PR body and the head task file; the mitigation is to\n-add `goal_id` to ADR 0015's frozen governance field set, and until that lands a\n+add `goal_id` to SADR-0015's frozen governance field set, and until that lands a\n task file can name a foreign goal. Second, base-when-present resolution is\n unavailable on fork pull requests, where the head is contributor-controlled;\n fork pull requests already cannot complete a Shiki merge, and this decision does\n@@ -139,7 +139,7 @@\n path, not by implementation pull requests.\n \n New obligations: one Spec Amendment per frozen plan to rewrite `locks`, ordered\n-before ADR 0015 T1; extension of the adversarial suites in\n+before SADR-0015 T1; extension of the adversarial suites in\n `scripts/test_shiki_adversarial_state.sh` and\n `scripts/test_shiki_governance_evidence.sh` to cover the eleven newly-scoped\n directories; and updating `scripts/test_shiki_loop_lock_guard.sh`,\n@@ -151,32 +151,32 @@\n whether the loop's transitive sync of ledger `evidence` references can be\n deleted once the Contract PR carries its own source plan.\n \n-## Relationship to ADR 0004\n+## Relationship to SADR-0004\n \n-This ADR **clarifies something ADR 0004 left unspecified**. ADR 0004 decides that\n+This SADR **clarifies something SADR-0004 left unspecified**. SADR-0004 decides that\n GitHub is the operational source of truth and that each target repository keeps a\n `.shiki/` mirror. It says nothing about pull requests writing the mirror, does\n not name MergeGate, and does not make the mirror an input to a gate. Nothing in\n-ADR 0004's Decision or Consequences is amended, superseded or weakened: the\n+SADR-0004's Decision or Consequences is amended, superseded or weakened: the\n mirror still exists, still records the same artifacts, and pull request\n-boundaries remain the coordination surface. What this ADR settles is the question\n+boundaries remain the coordination surface. What this SADR settles is the question\n 0004 never reached \u2014 by what authority a pull request may write a mirror record \u2014\n and it answers it the way `.shiki/manifest.json` already declares\n (`pr_mutation: current-task-or-current-goal-only`).\n \n-It **amends ADR 0012**, whose Decision item 5 states that \"Loop-executed tasks\n+It **amends SADR-0012**, whose Decision item 5 states that \"Loop-executed tasks\n declare the `path:.shiki/**` lock, which covers every `.shiki` file the closeout\n stages ... so `files_outside_locks` is satisfied without per-file lock\n bookkeeping.\" That clause is replaced: the closeout's mirror writes are\n-authorized by record identity, not by a declared lock. ADR 0012's substantive\n+authorized by record identity, not by a declared lock. SADR-0012's substantive\n decision \u2014 that completion reaches `main` through a closeout pull request rather\n than local attestation \u2014 is preserved unchanged.\n \n-It is **complementary to ADR 0015**, not a substitute. ADR 0015 owns contract\n+It is **complementary to SADR-0015**, not a substitute. SADR-0015 owns contract\n immutability, the pre-dispatch gate, the Contract PR and the Scope Envelope. This\n-ADR owns what a lock means and who may write the mirror. The one ordering\n+SADR owns what a lock means and who may write the mirror. The one ordering\n constraint between them is that the lock-narrowing Spec Amendment must precede\n-ADR 0015 T1's freezing of the `locks` field.\n+SADR-0015 T1's freezing of the `locks` field.\n \n ## Alternatives Considered\n \n@@ -186,7 +186,7 @@\n the only writer that can register them may run. Migrating the Guardian signal to\n that store makes every registration PR fail closed to `needs_guardian` at every\n risk level, because the task file is by definition absent from the store, which\n-removes the low/medium autonomous path ADR 0008/0009 exists to provide. And it\n+removes the low/medium autonomous path SADR-0008/0009 exists to provide. And it\n leaves schemas, manifest, config, guardian policy, templates, migrations, plans,\n worktrees, runner records, runs and handoffs with no writer at all in a\n repository whose product is `.shiki`. Measured benefit on the frozen backlog:\n@@ -199,14 +199,14 @@\n namespace is not in fact ownership-partitioned \u2014 56 ledger entries carry no\n `task_id`, and one goal DAG is writable by 35 registered tasks. The correct\n expression of its insight is that locks do not govern the mirror at all, which is\n-what this ADR decides.\n+what this SADR decides.\n \n **Make the mirror a projection written only by a registration writer and a\n post-merge projection job.** Rejected. It requires a bypass actor with write\n access to the protected default branch; it makes its own master invariant\n readable from a manifest that pull requests may edit; it deletes both reconcile\n modes and leaves a corrupted mirror with no in-band repair path, contradicting\n-ADR 0004's consequence that mirror conflicts are repaired against GitHub state;\n+SADR-0004's consequence that mirror conflicts are repaired against GitHub state;\n and its content-derived ids are rejected by `validate_shiki`'s currently-green id\n pattern.\n \ndiff --git a/docs/adr/0017-bookkeeping-closeouts-do-not-inherit-risk.md b/docs/adr/SADR-0017-bookkeeping-closeouts-do-not-inherit-risk.md\nindex 554e3a0d526a72ca067108c5464cc68c3edcf174..b9676857a4713850e063af7c39c8e0b01ecb4c66\n--- a/docs/adr/0017-bookkeeping-closeouts-do-not-inherit-risk.md\n+++ b/docs/adr/SADR-0017-bookkeeping-closeouts-do-not-inherit-risk.md\n@@ -1,4 +1,4 @@\n-# ADR 0017: A Bookkeeping Closeout Does Not Inherit Its Task's Risk\n+# SADR-0017: A Bookkeeping Closeout Does Not Inherit Its Task's Risk\n \n ## Status\n \n@@ -6,7 +6,7 @@\n \n ## Context\n \n-ADR 0012 made the goal loop push a task's completion to the default branch through a\n+SADR-0012 made the goal loop push a task's completion to the default branch through a\n closeout pull request: after the implementation PR merges, the loop opens a second PR\n carrying `task.status = done`, `lock.state = released`, and \u2014 when the task completes\n its goal \u2014 `goal.status = complete` plus one scorecard report. Nothing else. The\n@@ -49,14 +49,14 @@\n 4. The task file's governance fields (`scope`, `non_goals`, `required_skills`,\n    `risk_level`, `locks`, `acceptance_checks`, `test_command`) are byte-identical to\n    the base snapshot. Only `status`, `expected_pr`, `closeout_pr`, `ledger_evidence`\n-   and `expected_branch` may differ \u2014 the ADR 0015 contract-immutability rule,\n+   and `expected_branch` may differ \u2014 the SADR-0015 contract-immutability rule,\n    unchanged.\n \n    `expected_branch` is in that set because a closeout is *by construction* on a\n    different branch from the implementation it closes, and the loop rewrites the\n    field accordingly. It is safe to let it differ because MergeGate independently\n    requires `expected_branch` to equal the PR's actual head ref, so a PR cannot set\n-   it to anything other than the branch it is really on. The first draft of this ADR\n+   it to anything other than the branch it is really on. The first draft of this SADR\n    omitted it, which made the exemption unreachable: every real closeout failed\n    condition 4 on a field the loop always changes. See PR #209.\n 5. The state transitions are exactly the terminal set: task `review` \u2192 `done`, lock\n@@ -113,12 +113,12 @@\n classification must be derived, not asserted.\n \n **Fold the closeout into the implementation PR** so no second PR exists. Rejected under\n-ADR 0012, which recorded why: a task file that is already `done` on the implementation\n+SADR-0012, which recorded why: a task file that is already `done` on the implementation\n branch short-circuits the loop's review \u2192 CCA \u2192 merge \u2192 done ordering into a local\n goal-completion before any merge happens.\n \n **Have the loop auto-merge closeouts with elevated credentials.** Rejected outright: that\n-is a runtime authoring its own approval, which ADR 0010 and the constitution's\n+is a runtime authoring its own approval, which SADR-0010 and the constitution's\n approval-record prohibition forbid. It would also be indistinguishable, in the evidence,\n from the forgery those rules exist to prevent.\n \ndiff --git a/docs/adr/0018-carried-guardian-approval-across-a-proven-base-sync.md b/docs/adr/SADR-0018-carried-guardian-approval-across-a-proven-base-sync.md\nindex 22358562da390ed8b45cd600da08a43d378b4680..e6e2907cefa776460af73ddf7d39db605f3c39b0\n--- a/docs/adr/0018-carried-guardian-approval-across-a-proven-base-sync.md\n+++ b/docs/adr/SADR-0018-carried-guardian-approval-across-a-proven-base-sync.md\n@@ -1,4 +1,4 @@\n-# ADR 0018: Carried Guardian Approval Across A Proven Base Sync\n+# SADR-0018: Carried Guardian Approval Across A Proven Base Sync\n \n ## Status\n \n@@ -34,7 +34,7 @@\n Guardian evaluator that consumes it (`shiki_guardian.evaluate_guardian_approval`,\n the `guardian_comment_carried` source) were built and tested in isolation. Neither\n does anything until a caller passes the flags that turn the carry on, so the\n-feature was invisible in CI. This ADR records the decision the two of them\n+feature was invisible in CI. This SADR records the decision the two of them\n implement, and this task wires it into the four CI invocations that judge Guardian\n approval.\n \ndiff --git a/docs/adr/README.md b/docs/adr/README.md\nindex c70e057a4520c36e042ab68c53cd4a21ef709b71..5da43856836e2ce6a150f8127766e2f88355ab4b\n--- a/docs/adr/README.md\n+++ b/docs/adr/README.md\n@@ -1,40 +1,41 @@\n-# Architecture Decision Records\n+# Decision Record Namespaces\n \n-This directory holds Shiki's Architecture Decision Records (ADRs): durable\n-records of hard-to-reverse platform decisions. ADRs are part of the Shiki source\n-of truth alongside GitHub state, the `.shiki/` mirror, and `CONTEXT.md`.\n+This directory holds two independent decision namespaces:\n \n-## How ADRs Work\n+- Shiki Architecture Decision Records (SADRs), named\n+  `SADR-NNNN-<short-slug>.md`, record hard-to-reverse Shiki platform decisions.\n+- Target Architecture Decision Records (ADRs), named\n+  `NNNN-<short-slug>.md`, belong to the target product.\n \n-- New ADRs are added as `docs/adr/NNNN-<short-slug>.md`, where `NNNN` is the next\n-  zero-padded sequence number.\n-- The **canonical list of ADRs is the set of files matching `docs/adr/*.md`** in\n-  this directory. Browse `docs/adr/*.md` for the authoritative, current set.\n-- Use [`template.md`](template.md) as the starting point for a new ADR.\n-- Record an ADR when a decision is hard to reverse, surprising without context,\n-  and a real tradeoff. Do not write an ADR for routine or easily reversible\n-  choices.\n+The same four-digit number may appear once in each namespace. A platform\n+`SADR-NNNN` citation resolves only to `SADR-NNNN-*.md`; a target `ADR NNNN`\n+citation resolves only to `NNNN-*.md`.\n \n-Using the `docs/adr/*.md` glob as the source of truth means other PRs can add an\n-ADR without editing this index file, avoiding merge contention on a\n-hand-maintained enumeration.\n+## How Decision Records Work\n \n-## Current ADRs (snapshot)\n-\n-The list below is a convenience snapshot and may lag behind the directory. When\n-in doubt, list `docs/adr/*.md`.\n+- New Shiki platform decisions use the next zero-padded SADR number.\n+- New target product decisions use the target's own ADR sequence.\n+- The canonical list is the files in `docs/adr/`; no hand-maintained index is\n+  authoritative.\n+- Use [`template.md`](template.md) as the starting point and choose the filename\n+  namespace that owns the decision.\n+- Record a decision when it is hard to reverse, surprising without context, and\n+  a real tradeoff. Do not write a decision record for routine or easily\n+  reversible choices.\n \n-- [0001 \u2014 Keep Shiki As The Platform Name](0001-keep-shiki-as-platform-name.md)\n-- [0002 \u2014 Use AGENTS As The Shared Agent Constitution](0002-use-agents-as-shared-constitution.md)\n-- [0003 \u2014 Require Skill Gate For MergeGate Readiness](0003-require-skill-gate-for-mergegate-readiness.md)\n-- [0004 \u2014 Use GitHub First With A Repository-Local Shiki Mirror](0004-use-github-first-with-shiki-mirror.md)\n-- [0005 \u2014 Use Codex As Front And Claude Code Action As The GitHub Runtime](0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md)\n-- [0006 \u2014 Separate Codex Implementation, CCA Completion, And MergeGate Authorization](0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md)\n+Using the directory as the source of truth lets independent changes add records\n+without contending on an enumerated index.\n \n-## Adding A New ADR\n+## Adding A Shiki SADR\n \n-1. Copy `template.md` to `docs/adr/NNNN-<short-slug>.md`.\n+1. Copy `template.md` to `docs/adr/SADR-NNNN-<short-slug>.md`.\n 2. Fill in the title, status, context, decision, and consequences.\n-3. Set the status to `Proposed`, then `Accepted` once the decision is approved.\n-4. Open a PR following `CONTRIBUTING.md`. You do not need to edit this index for\n-   the ADR to be discoverable via `docs/adr/*.md`.\n+3. Set the status to `Proposed`, then `Accepted` once the proper authority has\n+   approved the decision.\n+4. Open a PR following `CONTRIBUTING.md`.\n+\n+## Adding A Target ADR\n+\n+1. Copy `template.md` to `docs/adr/NNNN-<short-slug>.md` using the target's own\n+   sequence.\n+2. Cite it as `ADR NNNN`; do not prefix its filename with `SADR-`.\ndiff --git a/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md b/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\nnew file mode 100644\nindex 0000000000000000000000000000000000000000..7455ce1f3bed02ddddb851ac21a1d1a648c9aeba\n--- /dev/null\n+++ b/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\n@@ -0,0 +1,55 @@\n+# SADR-0019: Separate Shiki Platform SADRs From Target ADRs\n+\n+## Status\n+\n+Accepted\n+\n+## Context\n+\n+Shiki installs its platform decision records into `docs/adr/` in every target\n+repository. Shiki and its targets previously used the same numeric\n+`NNNN-<slug>.md` namespace. A target that already owned ADRs 0013 through 0018\n+therefore acquired two records for each number during installation, making an\n+`ADR NNNN` citation ambiguous. Renumbering target decisions would rewrite target\n+history, while renumbering Shiki decisions per target would destroy their stable\n+platform identity.\n+\n+Upgrades also need to remove the former Shiki-owned numeric paths without ever\n+mistaking a target-owned ADR for platform content. The install stamp already\n+records exact shipped paths and content digests, so it can prove ownership only\n+when both still match.\n+\n+## Decision\n+\n+Shiki platform decisions use the `SADR` namespace:\n+\n+- records are named `docs/adr/SADR-NNNN-<slug>.md`;\n+- live platform citations use `SADR-NNNN`;\n+- target product decisions retain `docs/adr/NNNN-<slug>.md` and `ADR NNNN`;\n+- the reference validator resolves the two namespaces independently, so the same\n+  four-digit number may exist once in each namespace.\n+\n+Before a forced upgrade writes anything, the installer inventories the exact\n+former numeric Shiki ADR paths. It may delete a legacy path only when the\n+existing install stamp contains that exact path and its recorded SHA-256 digest\n+matches the file currently on disk. All ownership blockers are reported\n+together and abort the upgrade before its first write. A non-forced install\n+never performs this cleanup.\n+\n+Historical `.shiki/` evidence remains unchanged because it records the namespace\n+that existed when that evidence was produced.\n+\n+## Consequences\n+\n+- Targets may use any ADR number without colliding with Shiki's platform SADRs.\n+- Fresh installs preserve target ADR bytes and install a stable SADR set.\n+- Forced upgrades fail closed when legacy ownership cannot be proven, and a\n+  successful upgrade refreshes the stamp without removed legacy paths.\n+- Current governance, packaging, workflow, prompt, script, and skill references\n+  must move with the platform records.\n+\n+## Alternatives Considered\n+\n+- Collision-only refusal leaves affected targets permanently unupgradable.\n+- Renumbering target ADRs mutates target-owned history.\n+- Per-target Shiki renumbering makes platform citations non-portable.\ndiff --git a/docs/adr/template.md b/docs/adr/template.md\nindex a92a97daddd0b00ab9dd626cc8d86942da2eafed..17dd79175b5fee2105935557f691b40cf9492942\n--- a/docs/adr/template.md\n+++ b/docs/adr/template.md\n@@ -1,14 +1,14 @@\n-# NNNN: <Short Decision Title>\n+# <SADR-NNNN or NNNN>: <Short Decision Title>\n \n ## Status\n \n-Proposed | Accepted | Superseded by ADR-NNNN\n+Proposed | Accepted | Superseded by SADR-NNNN or target ADR NNNN\n \n ## Context\n \n What is the situation, constraint, or force that requires a decision? Reference\n-relevant domain terms (`CONTEXT.md`), prior ADRs, and code reality. State the\n-problem, not the solution.\n+relevant domain terms (`CONTEXT.md`), prior Shiki SADRs or target ADRs, and code\n+reality. State the problem, not the solution.\n \n ## Decision\n \ndiff --git a/docs/agents/autonomous-loop-e2e-acceptance.md b/docs/agents/autonomous-loop-e2e-acceptance.md\nindex bbf84c6081987f159ac6ad9ecf3c82b7aa72e7e8..5c13c1d02a05a90b0dc46cbef7c95a9846d0c237\n--- a/docs/agents/autonomous-loop-e2e-acceptance.md\n+++ b/docs/agents/autonomous-loop-e2e-acceptance.md\n@@ -40,8 +40,8 @@\n 3. `gh auth status` is logged in with merge permission on the repo.\n 4. Branch protection + required checks are configured exactly as the goal-loop\n    required-check set expects (see `docs/agents/branch-protection-smoke.md`).\n-5. The repo `.shiki/policy` permits autonomous low/medium-risk merges (ADR\n-   0008/0009); the MergeGate policy check is the recorded-authority gate.\n+5. The repo `.shiki/policy` permits autonomous low/medium-risk merges\n+   (SADR-0008/0009); the MergeGate policy check is the recorded-authority gate.\n \n The operator's only actions are: register the task and start the loop. **No\n manual pre-clearing** means after `shiki loop run` starts, the operator does not\ndiff --git a/docs/agents/bootstrap-command.md b/docs/agents/bootstrap-command.md\nindex 05f7f203a52cf412b9f97084a6e131ed0f785afd..f7aa0b734dfebee4a3b139335b66ee3415f48713\n--- a/docs/agents/bootstrap-command.md\n+++ b/docs/agents/bootstrap-command.md\n@@ -189,7 +189,9 @@\n ```\n \n Use this only for tests, fixtures, or explicit local-only template inspection.\n-Use `--force` only when you intentionally want to overwrite existing target files.\n+Use `--force` only when you intentionally want to refresh the target template.\n+Legacy numeric Shiki decision cleanup is stamp-and-digest authorized before any\n+write; target ADR paths are not cleanup candidates.\n \n ## Slash Command\n \ndiff --git a/docs/agents/checklists.md b/docs/agents/checklists.md\nindex 4a7f03ade346f53a1c1d82981f3fd744f9ab4547..0c5ec6c53dc7197b7b7a7d18e667b325babd6c45\n--- a/docs/agents/checklists.md\n+++ b/docs/agents/checklists.md\n@@ -32,18 +32,18 @@\n | ID | Item | Blocking |\n | --- | --- | --- |\n | GD-01 | Plan was challenged against `CONTEXT.md`. | blocking for non-trivial Goals |\n-| GD-02 | Relevant ADRs were read or absence was recorded. | blocking for non-trivial Goals |\n+| GD-02 | Relevant SADRs and target ADRs were read or absence was recorded. | blocking for non-trivial Goals |\n | GD-03 | Ambiguous terms were resolved or converted to HITL blockers. | blocking |\n | GD-04 | Edge scenarios were considered. | advisory |\n | GD-05 | Code reality was checked when code could answer. | blocking when applicable |\n | GD-06 | New domain terms were added to `CONTEXT.md` when needed. | blocking when applicable |\n-| GD-07 | ADR was created or explicitly skipped using the ADR criteria. | blocking when applicable |\n+| GD-07 | A SADR or target ADR was created or explicitly skipped using the decision-record criteria. | blocking when applicable |\n \n ## CI \u2014 Context & Impact Checklist\n \n | ID | Item | Blocking |\n | --- | --- | --- |\n-| CI-01 | Relevant docs and ADRs identified. | blocking |\n+| CI-01 | Relevant docs, SADRs, and target ADRs identified. | blocking |\n | CI-02 | Relevant modules, interfaces, seams, callers, and tests identified. | blocking |\n | CI-03 | Dependencies and blocked-by relationships identified. | blocking |\n | CI-04 | Candidate locks identified. | blocking |\n@@ -62,7 +62,7 @@\n | PRD-04 | Implementation decisions are recorded without volatile code detail. | blocking |\n | PRD-05 | Testing decisions are recorded. | blocking |\n | PRD-06 | Out-of-scope items are explicit. | blocking |\n-| PRD-07 | Relevant glossary/ADR links are present when applicable. | blocking |\n+| PRD-07 | Relevant glossary/SADR/target-ADR links are present when applicable. | blocking |\n \n ## SF \u2014 Spec Freeze Checklist\n \n@@ -98,7 +98,7 @@\n | H-02 | Branch/worktree target is present. | blocking |\n | H-03 | Scope and non-goals are present. | blocking |\n | H-04 | Dependencies and locks are present. | blocking |\n-| H-05 | Relevant docs, ADRs, modules, and tests are referenced. | blocking |\n+| H-05 | Relevant docs, SADRs, target ADRs, modules, and tests are referenced. | blocking |\n | H-06 | Required skills are stated. | blocking |\n | H-07 | TDD expectations are stated. | blocking when behavior work |\n | H-08 | Acceptance criteria are included. | blocking |\ndiff --git a/docs/agents/codex-handoff.md b/docs/agents/codex-handoff.md\nindex ede468f47cb277f69a857e8c45df703e4ed0151d..6f97bdffb67e44d7edea2c71e01ea28933c4eb6c\n--- a/docs/agents/codex-handoff.md\n+++ b/docs/agents/codex-handoff.md\n@@ -46,7 +46,7 @@\n \n ## Context\n - Domain terms:\n-- Relevant ADRs:\n+- Relevant SADRs and target ADRs:\n - Relevant docs:\n - Relevant modules/interfaces/seams:\n - Similar tests/prior art:\ndiff --git a/docs/agents/completion-check-agent.md b/docs/agents/completion-check-agent.md\nindex 13e4c035db35025f4e3cdcc5adfb24314ac4d521..61bfa1218cf36ca605250c91919e620f33238aa0\n--- a/docs/agents/completion-check-agent.md\n+++ b/docs/agents/completion-check-agent.md\n@@ -32,7 +32,7 @@\n - `AGENTS.md`\n - `CLAUDE.md`\n - `CONTEXT.md`\n-- relevant ADRs\n+- relevant SADRs and target ADRs\n - parent Goal issue\n - PRD issue or document\n - task issue\ndiff --git a/docs/agents/decision-control.md b/docs/agents/decision-control.md\nindex 7d8ee5ab03a5ccbcbd04056ae61de03785a2a9c9..8c94ecda00fffaa0e92c325ab83aa9776bd1dac1\n--- a/docs/agents/decision-control.md\n+++ b/docs/agents/decision-control.md\n@@ -11,7 +11,7 @@\n | Decision | Owner | When | Required inputs | Output | Enforcement surface |\n | --- | --- | --- | --- | --- | --- |\n | Is the Goal clear enough to plan? | Planner + Guardian when needed | Goal intake | User request, existing docs, risk signals | Goal readiness verdict | Goal checklist, issue template, ledger |\n-| Must `grill-with-docs` run? | Planner / Skill Gate | Before PRD or issue split | Ambiguity, domain terms, ADR conflicts, unclear boundaries | Skill invocation or explicit skip rationale | Skill Gate checklist |\n+| Must `grill-with-docs` run? | Planner / Skill Gate | Before PRD or issue split | Ambiguity, domain terms, SADR/target-ADR conflicts, unclear boundaries | Skill invocation or explicit skip rationale | Skill Gate checklist |\n | Is a PRD required? | Planner | After Goal context stabilizes | Goal, `grill-with-docs` output, Context & Impact output | PRD required / not required | PRD checklist, CCA evidence check |\n | Can the spec be frozen? | Operator only | End of Requirements Definition, before issue split | PRD/requirements summary, scope inventory (SF-02), open-question list | `spec_freeze` block in the plan + ledger evidence | `require_grilled_plan` (plan ingest/run), SF checklist |\n | Can a frozen spec change? | Operator only (Spec Amendment) | When implementation contradicts the frozen spec | Contested decisions, scoped re-grill result | recorded amendment + re-stamped freeze | SF checklist, ledger evidence |\ndiff --git a/docs/agents/domain.md b/docs/agents/domain.md\nindex 258da8ea81e87ac0a6b0b5b5e5c2fde980eaabb7..d57b0cf17ff3e1fa9e6e5885372bf083d8295421\n--- a/docs/agents/domain.md\n+++ b/docs/agents/domain.md\n@@ -5,7 +5,9 @@\n ## Layout\n \n - `CONTEXT.md` defines Shiki language. Keep it implementation-free.\n-- `docs/adr/` records hard-to-reverse decisions.\n+- `docs/adr/SADR-NNNN-*.md` records Shiki platform decisions.\n+- `docs/adr/NNNN-*.md` records target product decisions without sharing the\n+  SADR namespace.\n - `AGENTS.md` is the runtime-neutral agent constitution.\n - `CLAUDE.md` is the Claude Code wrapper around `AGENTS.md`.\n - `.shiki/` is the Target Repository mirror for Goals, PRDs, plans, Task DAGs, contracts, locks, worktree records, ledger entries, CCA verdicts, repair packets, reports, and handoffs.\n@@ -15,6 +17,6 @@\n \n - Use Shiki terms exactly when they exist.\n - Add glossary terms to `CONTEXT.md` only when the term is part of the domain, not a generic programming concept.\n-- Create ADRs only for hard-to-reverse, surprising, tradeoff-based decisions.\n+- Create SADRs or target ADRs only for hard-to-reverse, surprising, tradeoff-based decisions.\n - Keep product-specific language in the Target Repository, not in Shiki Template docs.\n-- If an ADR conflicts with a proposed implementation, surface the conflict before editing.\n+- If a SADR or target ADR conflicts with a proposed implementation, surface the conflict before editing.\ndiff --git a/docs/agents/execution-control.md b/docs/agents/execution-control.md\nindex ad8ee3cdb36be56fedc1f46408ca92b387d74047..bc63c3a73661d12b567016ba299f1a6b66bf9088\n--- a/docs/agents/execution-control.md\n+++ b/docs/agents/execution-control.md\n@@ -63,7 +63,7 @@\n | Branch name | Orchestrator | Orchestrator | Before implementer dispatch | branch policy | branch ref in task/ledger |\n | Lock reservation | Context & Impact | MergeGate | Before dispatch and before merge | `.shiki/locks/`, labels, PR metadata | lock id, owner, scope |\n | CI profile | Planner | MergeGate | During issue triage, rechecked at PR | labels, `.shiki/policy.yaml`, workflow matrix | required check list |\n-| Required status checks | Repo maintainer / Guardian | GitHub branch protection / rulesets | Repository setup, updated via ADR | GitHub settings | branch/ruleset config, ADR |\n+| Required status checks | Repo maintainer / Guardian | GitHub branch protection / rulesets | Repository setup, updated via target ADR | GitHub settings | branch/ruleset config, target ADR |\n | CCA judgment | CCA | GitHub required check / MergeGate | Every non-draft PR update | GitHub Action check | structured CCA verdict |\n | Repair assignment | CCA | MergeGate | After failed CCA/CI/review | repair packet, issue/PR comment | repair id and attempt count |\n | Merge readiness | MergeGate | GitHub branch protection / Guardian if needed | After CCA complete | required checks, review rules, rulesets | mergegate check output |\ndiff --git a/docs/agents/guardian-policy.md b/docs/agents/guardian-policy.md\nindex 4147c7adf05a47adb4fd24ada1a1c2e1f2ba6d2d..a20d9cc6c00a569bb057b9ca8d6b51c7fc4a9847\n--- a/docs/agents/guardian-policy.md\n+++ b/docs/agents/guardian-policy.md\n@@ -98,8 +98,8 @@\n `origin/<default_branch>` and reuses each job's existing base fetch; a shallow\n (`--depth`) or second base fetch would make the proof unresolvable and silently\n disable the carry, so neither is permitted. CCA reads `guardian_comment_carried` as\n-a recorded authority in the deterministic signal and does not re-derive it. See ADR\n-0018 for the full decision, the rejected diff/patch-equality and path-disjointness\n+a recorded authority in the deterministic signal and does not re-derive it. See\n+SADR-0018 for the full decision, the rejected diff/patch-equality and path-disjointness\n alternatives, and the accepted residual.\n \n ## External AI Guardian Review (SADR-0010)\ndiff --git a/docs/agents/implementation-policy.md b/docs/agents/implementation-policy.md\nindex c754144a9fb5bf4a38aa82a60e1670c59e626b44..85c37281bcad196c6b7b02343faf00b03cd9c57a\n--- a/docs/agents/implementation-policy.md\n+++ b/docs/agents/implementation-policy.md\n@@ -31,10 +31,13 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n ## Non-Negotiables\n \n 1. **Planning precedes implementation.** Non-trivial Goals start with `grill-with-docs` and Context & Impact.\n-2. **Domain language is controlled.** Terms must be checked against `CONTEXT.md`; hard-to-reverse tradeoffs must be checked against ADRs.\n+2. **Domain language is controlled.** Terms must be checked against `CONTEXT.md`; hard-to-reverse tradeoffs must be checked against Shiki SADRs and target ADRs.\n 3. **PRD precedes issue decomposition.** `to-prd` is used once the context is settled enough to become durable intent.\n 4. **Issues are vertical slices.** `to-issues` must produce independently grabbable tracer-bullet tasks, not horizontal layer tickets.\n 5. **Triage controls dispatch.** Only `ready-for-agent` / AFK tasks may be assigned to Codex without human input.\n@@ -95,19 +98,19 @@\n Process:\n \n - Challenge terms against `CONTEXT.md`.\n-- Check ADRs before reopening decisions.\n+- Check SADRs and target ADRs before reopening decisions.\n - Inspect code when code can answer a question.\n - Ask one question at a time when the operator is available.\n - Provide a recommended answer with each question.\n - Record resolved terms in `CONTEXT.md`.\n-- Create ADRs only for hard-to-reverse, surprising, tradeoff-based decisions.\n+- Create SADRs or target ADRs only for hard-to-reverse, surprising, tradeoff-based decisions.\n - Convert unresolved load-bearing questions into HITL blockers.\n \n Outputs:\n \n - Resolved design-tree decisions.\n - Updated glossary terms when needed.\n-- ADRs when justified.\n+- SADRs or target ADRs when justified.\n - HITL blockers when decisions remain unresolved.\n \n ## Phase 3 \u2014 Context & Impact\n@@ -117,7 +120,7 @@\n \n Outputs must include:\n \n-- Relevant documents and ADRs.\n+- Relevant documents, SADRs, and target ADRs.\n - Relevant modules, interfaces, seams, callers, and tests.\n - Candidate locks.\n - Dependencies.\n@@ -140,7 +143,7 @@\n - Testing decisions.\n - Out of scope.\n - Further notes.\n-- Links to relevant Goal, domain terms, and ADRs.\n+- Links to relevant Goal, domain terms, SADRs, and target ADRs.\n \n Phase 4 ends with Spec Freeze: the operator explicitly approves the PRD, the\n plan records a `spec_freeze` block (status, approved_by, source), and the SF\n@@ -240,7 +243,7 @@\n - Dependencies and locks.\n - Risk and Guardian requirements.\n - Ledger completeness.\n-- Documentation and ADR updates.\n+- Documentation and SADR/target-ADR updates.\n - Scope drift.\n \n CCA emits one of:\n@@ -301,7 +304,7 @@\n | Evidence failure | Behavior might be done, but proof is missing. | Add checks, PR evidence, ledger entry, or artifacts. |\n | Scope drift | PR changes unrelated behavior. | Revert unrelated changes or split into new Goal. |\n | Dependency block | Upstream task or lock not complete. | Block until dependency resolves. |\n-| Architecture block | Decision needs ADR or deep-module discussion. | `grill-with-docs` or `improve-codebase-architecture`. |\n+| Architecture block | Decision needs a SADR, target ADR, or deep-module discussion. | `grill-with-docs` or `improve-codebase-architecture`. |\n | Risk block | Security/production/policy change needs human approval. | Guardian decision. |\n \n ## Phase 5.5 \u2014 Execution Decision Control\ndiff --git a/docs/agents/runtime-auth-model.md b/docs/agents/runtime-auth-model.md\nindex 6a2e1a47e81ea89b5c68a210ec07772a9374babc..3c54748d24233dbd0ba8473950282547ef4ce935\n--- a/docs/agents/runtime-auth-model.md\n+++ b/docs/agents/runtime-auth-model.md\n@@ -148,7 +148,7 @@\n \n API-key based runners may be added by a target repository as an explicit extension. They are not the default Shiki template path.\n \n-If a repo opts into API-key mode, record the exception in an ADR and update the repo's `AGENTS.md`, `.shiki/config.yaml`, and workflow permissions.\n+If a repo opts into API-key mode, record the target-specific exception in a target ADR and update the repo's `AGENTS.md`, `.shiki/config.yaml`, and workflow permissions.\n \n ## GitHub Actions Runtime Compatibility\n \ndiff --git a/docs/agents/skill-gate.md b/docs/agents/skill-gate.md\nindex c50398b0d7c60a8598f2a5c88a10015730dced13..3acffe6444617bb1d54b77d4b4708158ec735bc6\n--- a/docs/agents/skill-gate.md\n+++ b/docs/agents/skill-gate.md\n@@ -9,7 +9,7 @@\n | Skill | Trigger | Expected output |\n | --- | --- | --- |\n | `setup-matt-pocock-skills` | First configuring a repo for these skills, or when issue tracker, triage labels, or domain docs are missing. | `docs/agents/` configuration and an `Agent skills` block in `AGENTS.md` or `CLAUDE.md`. |\n-| `grill-with-docs` | Ambiguous plan, unclear terminology, boundary decision, tradeoff, or ADR-worthy design choice. | Clarified decision tree, recommended answers, updates to `CONTEXT.md` or ADRs when decisions crystallize. |\n+| `grill-with-docs` | Ambiguous plan, unclear terminology, boundary decision, tradeoff, or decision-record-worthy design choice. | Clarified decision tree, recommended answers, updates to `CONTEXT.md`, SADRs, or target ADRs when decisions crystallize. |\n | `zoom-out` | Agent lacks a higher-level architectural map. | Map of relevant modules, callers, seams, and domain concepts. |\n | `to-prd` | Settled context should become a PRD. | PRD published to the issue tracker with problem, solution, user stories, decisions, testing, and out-of-scope notes. |\n | `to-issues` | Goal, PRD, or plan must become implementation tickets. | Vertical-slice issues with acceptance criteria, dependencies, and AFK/HITL classification. |\n@@ -17,7 +17,7 @@\n | `tdd` | Feature work or bug fix has observable behavior that can be specified with tests. | Red-green-refactor loop through public interfaces, one vertical slice at a time. |\n | `code-review` | The TDD loop is green and the implementer is about to create or update a PR. Default for every implementation task. | Pre-PR self-review of the task diff with fixes applied on the task branch; findings and resolutions recorded as a ledger entry naming the skill and a `## Pre-PR code review` PR body section. |\n | `diagnose` | Hard bug, failing check, regression, flaky behavior, or performance problem. | Reproduction loop, ranked hypotheses, instrumentation, fix, regression test, post-mortem. |\n-| `improve-codebase-architecture` | Structural friction, testability, AI-navigability, deep-module opportunity, or refactor request. | Architecture candidates and deepening recommendations informed by `CONTEXT.md` and ADRs. |\n+| `improve-codebase-architecture` | Structural friction, testability, AI-navigability, deep-module opportunity, or refactor request. | Architecture candidates and deepening recommendations informed by `CONTEXT.md`, SADRs, and target ADRs. |\n | `prototype` | A throwaway logic or UI prototype is needed to answer a design question. | Clearly marked disposable prototype and captured decision. |\n \n ## MergeGate Interaction\ndiff --git a/docs/operations.md b/docs/operations.md\nindex cf1f452d4c589e38ccd58553bda0ad8684b51eb7..ace48a90ffbec9e5c0a7113bef16a4858bc44b3f\n--- a/docs/operations.md\n+++ b/docs/operations.md\n@@ -89,6 +89,16 @@\n      target history, brought forward by `shiki migrate`, not a template.\n    - **Everything else** (CLI scripts, workflows, schemas, docs) is overwritten.\n \n+   A forced upgrade also migrates the former numeric Shiki decision paths to\n+   `SADR-NNNN-*`. Before the first write, the installer inventories the exact\n+   legacy Shiki paths and authorizes each deletion only when the existing\n+   `.shiki/install-stamp.json` names that path and its SHA-256 digest still\n+   matches the file. An absent, unreadable, malformed, incomplete, or\n+   mismatching stamp reports every blocked path and stops with no filesystem\n+   change. Target-owned `NNNN-*` ADRs are not cleanup candidates. A non-force\n+   install deletes nothing and retains any still-matching legacy stamp entries\n+   so a later forced upgrade can prove ownership.\n+\n    If the target has pending migrations, `--force` refuses and writes nothing \u2014\n    apply migrations first (step 3), then re-run. The run ends with a single\n    summary of every `.new` written, so a half-upgrade is visible.\ndiff --git a/docs/releasing.md b/docs/releasing.md\nindex 10d73828e7dbd64e9d9f6b83bc84d010537ce62a..aafeee8c4d441e1e427efee2ce4f81a647f020e1\n--- a/docs/releasing.md\n+++ b/docs/releasing.md\n@@ -67,7 +67,7 @@\n CLI in `scripts/`. There are no third-party Python runtime dependencies. The CLI\n is intentionally **not** exposed as a `[project.scripts]` console entry point\n because `scripts/shiki.py` is a dependency-free shim and `scripts/` is not\n-packaged as an importable distribution (see the ADR for the rationale).\n+packaged as an importable distribution (see SADR-0007 for the rationale).\n \n Supported install paths:\n \n@@ -155,6 +155,15 @@\n    [docs/operations.md](operations.md#the-new-convention). If the target has\n    pending migrations, `--force` refuses and writes nothing; apply migrations\n    (step 2) first, then re-run.\n+\n+   The SADR namespace upgrade has an additional pre-write gate: each former\n+   numeric Shiki decision path is removed only when the existing install stamp\n+   contains that exact path and a digest matching its current bytes. The\n+   installer aggregates absent/unreadable/malformed-stamp, missing-digest, and\n+   digest-mismatch blockers before raising, so refusal leaves the target tree\n+   byte-identical. Numeric target ADRs are preserved. A successful forced\n+   upgrade installs and stamps `SADR-*` paths and drops the removed legacy paths\n+   from the refreshed stamp.\n 2. Apply repository-local `.shiki/` state migrations with the migrate command:\n \n    ```bash\ndiff --git a/docs/usage.md b/docs/usage.md\nindex b83407ee3970dd395dd893010bc5dbe8bad26051..a130ef6e40dfd8169a5bc701339351097df1a09f\n--- a/docs/usage.md\n+++ b/docs/usage.md\n@@ -67,8 +67,10 @@\n shiki install-target /path/to/target-repo --local-only\n ```\n \n-Use `--force` only when you intentionally want to overwrite existing target\n-files.\n+Use `--force` only when you intentionally want to refresh the target template.\n+For the SADR namespace migration, `--force` deletes only exact legacy Shiki\n+decision paths whose existing install-stamp digest still matches; any ownership\n+blocker stops before the first write. Numeric target ADRs are never candidates.\n \n ## `shiki bootstrap-platform` \u2014 publish the Shiki platform repo\n \ndiff --git a/scripts/guardian_approval_signal.py b/scripts/guardian_approval_signal.py\nindex d059f92b2c82aa2b5d6ca421365537a87886c1cb..16924892714f4f5b9303c0ef0e325a7e70075630\n--- a/scripts/guardian_approval_signal.py\n+++ b/scripts/guardian_approval_signal.py\n@@ -199,8 +199,8 @@\n ) -> Any:\n     \"\"\"Evaluate SADR-0015 Contract Approval for the CCA Guardian signal.\n \n-    The SAME OR that MergeGate applies must reach this deterministic signal: ADR\n-    0015 is explicit that without it, a contract-approved PR still returns\n+    The SAME OR that MergeGate applies must reach this deterministic signal:\n+    SADR-0015 is explicit that without it, a contract-approved PR still returns\n     ``needs_guardian``, the CCA job fails, and the MergeGate policy check never\n     runs \u2014 the gate this change moves would never be reached. This resolves the\n     task the SAME way MergeGate does (the first Shiki task id in the PR body),\ndiff --git a/scripts/shiki_contract_approval.py b/scripts/shiki_contract_approval.py\nindex 941bcb96fdd8b9a85bc0c4f0f04d5dad9d04839c..4a4b418f6527413161db82670bec857a043360e2\n--- a/scripts/shiki_contract_approval.py\n+++ b/scripts/shiki_contract_approval.py\n@@ -142,7 +142,7 @@\n     changed_files_status: Any,\n     registration: Any,\n ) -> ContractApprovalResult:\n-    \"\"\"Whether this PR carries an SADR-0015 Contract Approval.\n+    \"\"\"Whether this PR carries a SADR-0015 Contract Approval.\n \n     The four conditions are evaluated IN ORDER, each failure recorded as a\n     distinct reason string:\ndiff --git a/scripts/shiki_guardian.py b/scripts/shiki_guardian.py\nindex a4ecb79bbda55fd5c8b06adfae2eb99760a03c45..4dd99bbe714e26f421fa19abac7a34ae1b195ecc\n--- a/scripts/shiki_guardian.py\n+++ b/scripts/shiki_guardian.py\n@@ -12,7 +12,7 @@\n # The carry re-derives its proof from git in-process on every judging invocation;\n # it is never read from an artifact, so there is nothing to go stale or be forged.\n # shiki_sync_proof is the ONLY guardian-family module that runs git, and every one\n-# of its invocations is read-only and local (ADR: prove-from-git base sync).\n+# of its invocations is read-only and local (SADR: prove-from-git base sync).\n from shiki_sync_proof import verify_pure_base_sync\n \n GUARDIAN_POLICY_PATH = \".shiki/guardian-policy.json\"\ndiff --git a/scripts/shiki_guardian_review.py b/scripts/shiki_guardian_review.py\nindex dd0f104d457899e0ba5ff1e162011c1e90b6f92b..e10ef58c046324e7ea3a3392feb3048ca77414ce\n--- a/scripts/shiki_guardian_review.py\n+++ b/scripts/shiki_guardian_review.py\n@@ -159,7 +159,7 @@\n     docs_only = _is_docs_only(changed_files)\n     if docs_only and matched == 0:\n         focus.append(\n-            \"Docs/ADR-only change: confirm the change is genuinely documentation/decision-only, introduces no behavior or contract drift, and stays consistent with the constitution and existing ADRs.\"\n+            \"Docs/decision-record-only change: confirm the change is genuinely documentation/decision-only, introduces no behavior or contract drift, and stays consistent with the constitution and existing SADRs and target ADRs.\"\n         )\n \n     # Conservative augmentation: unknown (no category and not docs-only), mixed\ndiff --git a/scripts/shiki_installer.py b/scripts/shiki_installer.py\nindex be064ab7a29ff6836f7f7a1d70500a43e0d11d7b..9fd8eefd137ce7003dad5c0dcb2e48e2cd47ab32\n--- a/scripts/shiki_installer.py\n+++ b/scripts/shiki_installer.py\n@@ -118,6 +118,7 @@\n # what lets a target carry it without a governance-schema change.\n INSTALL_STAMP_PATH = \".shiki/install-stamp.json\"\n INSTALL_STAMP_VERSION = 1\n+LEGACY_SHIKI_SADR_MAX = 18\n \n # Under --force the shipped surface splits three ways, so nothing is lost and\n # nothing is silently stale.\n@@ -237,9 +238,98 @@\n         )\n \n \n+def legacy_shiki_adr_paths() -> tuple[str, ...]:\n+    \"\"\"Former numeric paths for Shiki SADR-0001 through SADR-0018.\n+\n+    The authoritative slugs come from the current SADR filenames. SADR-0019 is\n+    the namespace decision itself and never shipped under a numeric legacy path.\n+    \"\"\"\n+    paths: list[str] = []\n+    for sadr in sorted((ROOT / \"docs\" / \"adr\").glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\")):\n+        number = sadr.name.split(\"-\", 2)[1]\n+        if int(number) <= LEGACY_SHIKI_SADR_MAX:\n+            paths.append(f\"docs/adr/{sadr.name.removeprefix('SADR-')}\")\n+    return tuple(paths)\n+\n+\n+def preflight_legacy_sadr_cleanup(target: Path, *, force: bool) -> tuple[str, ...]:\n+    \"\"\"Authorize exact legacy Shiki ADR removals before the first write.\n+\n+    A non-force install never removes existing files. A forced install needs no\n+    stamp when no legacy Shiki path is present. When legacy paths do exist, each\n+    must be named in a readable install stamp and its current bytes must match\n+    the recorded digest.\n+    \"\"\"\n+    if not force:\n+        return ()\n+    present = tuple(\n+        relative\n+        for relative in legacy_shiki_adr_paths()\n+        if (target / relative).exists() or (target / relative).is_symlink()\n+    )\n+    if not present:\n+        return ()\n+\n+    stamp_path = target / INSTALL_STAMP_PATH\n+    try:\n+        raw = stamp_path.read_text(encoding=\"utf-8\")\n+        stamp = json.loads(raw)\n+    except FileNotFoundError:\n+        detail = \"install stamp is absent\"\n+        blockers = [f\"{relative}: {detail}\" for relative in present]\n+    except (OSError, UnicodeDecodeError) as error:\n+        detail = f\"install stamp is unreadable ({error})\"\n+        blockers = [f\"{relative}: {detail}\" for relative in present]\n+    except json.JSONDecodeError as error:\n+        detail = f\"install stamp is malformed ({error})\"\n+        blockers = [f\"{relative}: {detail}\" for relative in present]\n+    else:\n+        if not isinstance(stamp, dict):\n+            blockers = [\n+                f\"{relative}: install stamp is malformed (expected an object)\"\n+                for relative in present\n+            ]\n+        else:\n+            digests = stamp.get(\"digests\")\n+            blockers = []\n+            if not isinstance(digests, dict):\n+                blockers.extend(\n+                    f\"{relative}: install stamp has no digest for this path\"\n+                    for relative in present\n+                )\n+            else:\n+                for relative in present:\n+                    expected = digests.get(relative)\n+                    if not isinstance(expected, str) or not expected:\n+                        blockers.append(\n+                            f\"{relative}: install stamp has no digest for this path\"\n+                        )\n+                        continue\n+                    try:\n+                        actual = _sha256_file(target / relative)\n+                    except OSError as error:\n+                        blockers.append(\n+                            f\"{relative}: legacy path is unreadable ({error})\"\n+                        )\n+                        continue\n+                    if actual != expected:\n+                        blockers.append(\n+                            f\"{relative}: digest mismatch (stamp {expected}, current {actual})\"\n+                        )\n+\n+    if blockers:\n+        joined = \"\\n\".join(f\"- {blocker}\" for blocker in blockers)\n+        raise ShikiError(\n+            \"legacy Shiki ADR cleanup is not authorized:\\n\"\n+            f\"{joined}\\nNo files were written.\"\n+        )\n+    return present\n+\n+\n def install_template(target: Path, *, force: bool, validate: bool) -> list[NewFileNote]:\n     # Preconditions first: never rewrite the tree of a target that cannot be\n     # upgraded cleanly (see refuse_pending_migrations).\n+    legacy_cleanup = preflight_legacy_sadr_cleanup(target, force=force)\n     refuse_pending_migrations(target)\n \n     # Resolve the target's required CODEOWNERS owner once from its\n@@ -250,6 +340,10 @@\n     # verbatim. See ``codeowners_required_owner``.\n     codeowners_owner = codeowners_required_owner(target)\n \n+    for relative in legacy_cleanup:\n+        (target / relative).unlink()\n+        info(f\"removed stamped legacy Shiki ADR: {target / relative}\")\n+\n     notes: list[NewFileNote] = []\n     for relative in TEMPLATE_PATHS:\n         source = ROOT / relative\n@@ -647,6 +741,22 @@\n     digests = target_stamp_digests(target)\n     existing = read_install_stamp(target)\n     if existing is not None:\n+        # A non-force install deliberately leaves legacy Shiki ADRs in place.\n+        # Preserve only still-matching ownership proof so a later forced\n+        # upgrade can authorize cleanup; never carry a missing or stale digest.\n+        existing_digests = existing.get(\"digests\")\n+        if isinstance(existing_digests, dict):\n+            for relative in legacy_shiki_adr_paths():\n+                expected = existing_digests.get(relative)\n+                path = target / relative\n+                if not isinstance(expected, str) or not path.is_file():\n+                    continue\n+                try:\n+                    actual = _sha256_file(path)\n+                except OSError:\n+                    continue\n+                if actual == expected:\n+                    digests[relative] = actual\n         if _incoming_is_older(commit, existing.get(\"platform_commit\")):\n             warn(\n                 \"kept existing install stamp: incoming install \"\ndiff --git a/scripts/shiki_loop.py b/scripts/shiki_loop.py\nindex 3611641da8aeb5038b1da90e3114b09a473ec6d8..84caa491b9cad0edc4176f209c13e9221e975681\n--- a/scripts/shiki_loop.py\n+++ b/scripts/shiki_loop.py\n@@ -424,7 +424,7 @@\n         # unresolvable CCA verdict has already produced its terminal stop and is never\n         # laundered into a head-moving sync (SADR-0010). It also never fires while the\n         # policy (Guardian) gate is red \u2014 for ANY reason, not only when it is the sole\n-        # red check \u2014 so a head-bound Guardian approval, or an SADR-0015\n+        # red check \u2014 so a head-bound Guardian approval, or a SADR-0015\n         # contract-approval carry that base movement defeated (the policy gate compares\n         # base and head task governance directly), is never silently restored by\n         # adopting base wholesale. It is bounded by a durable per-task counter so the\ndiff --git a/scripts/shiki_tasks.py b/scripts/shiki_tasks.py\nindex aec3809f24b4a0c8c96fc50971cff9ae9ad30381..d531052e9d9717058d4631483cefab44c26a65c1\n--- a/scripts/shiki_tasks.py\n+++ b/scripts/shiki_tasks.py\n@@ -821,7 +821,7 @@\n         \"prompt\": prompt,\n         \"entry_skill\": \"grill-with-docs\",\n         \"required_next_steps\": [\n-            \"Run grill-with-docs until terminology, boundaries, risks, and ADR-worthy decisions are settled.\",\n+            \"Run grill-with-docs until terminology, boundaries, risks, and decision-record-worthy choices are settled.\",\n             \"Obtain the operator's explicit approval of the PRD/requirements (Spec Freeze).\",\n             \"Write a machine-readable plan JSON with grill_with_docs.status=complete and spec_freeze.status=frozen.\",\n             \"Run shiki plan ingest --plan-file PLAN.json.\",\ndiff --git a/scripts/test_shiki_goal_loop.sh b/scripts/test_shiki_goal_loop.sh\nindex 753c7b516e10f0be3bc92a4bd0eb1fa699776ae6..8f3820a0a079b3044424cfe1a6c6b7fe68896e67\n--- a/scripts/test_shiki_goal_loop.sh\n+++ b/scripts/test_shiki_goal_loop.sh\n@@ -231,8 +231,8 @@\n \n # High/critical risk merges autonomously when all required checks are green:\n # the \"MergeGate policy check\" required check IS the Guardian gate (it enforces\n-# guardian-policy.json \u2014 human approval OR external AI guardian review, ADR\n-# 0010), so green checks mean a recorded authority approved. The fake gh returns\n+# guardian-policy.json \u2014 human approval OR external AI guardian review,\n+# SADR-0010), so green checks mean a recorded authority approved. The fake gh returns\n # green checks, simulating that the Guardian gate passed.\n python3 \"$ROOT/scripts/shiki.py\" goal create --target \"$TARGET\" --title \"Guardian gate\" --outcome \"High risk merges via policy gate\" >/tmp/shiki-goal-loop-guardian-goal.json\n GGOAL=\"$(json_get /tmp/shiki-goal-loop-guardian-goal.json goal_id)\"\ndiff --git a/tests/test_adr_references.py b/tests/test_adr_references.py\nindex 51706329c797d5dd9b47e082a333fa83b261d482..db6e693e35f67103935be34f1d9bb4ff7ee60395\n--- a/tests/test_adr_references.py\n+++ b/tests/test_adr_references.py\n@@ -1,7 +1,7 @@\n-\"\"\"Every ``ADR ####`` citation must resolve to exactly one decision record.\n+\"\"\"Every target ADR and Shiki SADR citation resolves in its own namespace.\n \n Background. ``scripts/mergegate_check.py`` justifies ``is_bookkeeping_closeout``\n-with \"ADR 0017\" nine times and ``scripts/guardian_approval_signal.py`` cites\n+with \"SADR-0017\" nine times and ``scripts/guardian_approval_signal.py`` cites\n 0016/0017 seven times, yet for a stretch the numbering in ``docs/adr/`` stopped\n at 0015: the authoring commits lived on a coordinator branch and no task ever\n declared ``docs/adr/...`` in its locks, so nothing carried the records to the\n@@ -11,11 +11,12 @@\n and the directory drifted with no signal.\n \n This is the same unbound-pair shape behind other silent failures: a reference\n-and its target with nothing asserting they agree. This test binds them. It scans\n-every ``scripts/*.py`` (plus ``docs/**/*.md`` and the root ``*.md`` governance\n-files, which are cheap) for the ``ADR ####`` pattern and asserts each cited\n-number resolves to exactly one ``docs/adr/####-*.md``. The converse is\n-deliberately NOT required: an ADR may exist without being cited.\n+and its target with nothing asserting they agree. This test binds them. Target\n+``ADR NNNN`` citations resolve only to ``docs/adr/NNNN-*.md``; Shiki\n+``SADR-NNNN`` citations resolve only to ``docs/adr/SADR-NNNN-*.md``. The scan\n+covers current governance docs, scripts, workflows, prompts, packaging metadata,\n+and repo-local Codex guidance while deliberately excluding historical\n+``.shiki`` evidence. The converse is not required: a record may be uncited.\n \n The suite is dependency free: standard-library ``unittest`` only.\n \"\"\"\n@@ -31,17 +32,18 @@\n \n REPO_ROOT = Path(__file__).resolve().parents[1]\n \n-# \"ADR 0016\" -- a four-digit citation. The trailing \\b keeps a five-digit token\n-# from being read as a truncated four-digit number.\n-_CITATION = re.compile(r\"\\bADR (\\d{4})\\b\")\n-# \"0016-locks-bound-product-paths.md\" -- the leading four digits name the record.\n-_ADR_FILENAME = re.compile(r\"^(\\d{4})-.*\\.md$\")\n+# ``ADR 0016`` / ``SADR-0016`` -- a namespace plus four-digit citation. The\n+# trailing ``\\b`` keeps a five-digit token from being read as a truncated one.\n+_CITATION = re.compile(r\"\\b(S?ADR)[ -](\\d{4})\\b\")\n+# Target ADRs retain the numeric filename; Shiki platform decisions use SADR.\n+_DECISION_FILENAME = re.compile(r\"^(?:(SADR)-)?(\\d{4})-.*\\.md$\")\n \n \n class Violation(NamedTuple):\n     \"\"\"A citation that does not resolve to exactly one decision record.\"\"\"\n \n     citing_file: str  # repo-relative path of the file that cites the ADR\n+    namespace: str  # \"ADR\" for target decisions; \"SADR\" for Shiki decisions\n     number: str  # the cited four-digit number, e.g. \"0016\"\n     line: int  # 1-based line of the citation\n     reason: str  # \"missing\" (zero matches) or \"ambiguous\" (two or more)\n@@ -49,44 +51,59 @@\n \n     @property\n     def expected_path(self) -> str:\n-        return f\"docs/adr/{self.number}-*.md\"\n+        prefix = \"SADR-\" if self.namespace == \"SADR\" else \"\"\n+        return f\"docs/adr/{prefix}{self.number}-*.md\"\n \n     def describe(self) -> str:\n         \"\"\"Name the citing file, the cited number, and the missing path.\"\"\"\n         if self.reason == \"missing\":\n             return (\n-                f\"{self.citing_file}:{self.line} cites ADR {self.number} \"\n+                f\"{self.citing_file}:{self.line} cites {self.namespace} {self.number} \"\n                 f\"but no document matches {self.expected_path}\"\n             )\n         joined = \", \".join(self.matches)\n         return (\n-            f\"{self.citing_file}:{self.line} cites ADR {self.number} which \"\n+            f\"{self.citing_file}:{self.line} cites {self.namespace} {self.number} which \"\n             f\"resolves to {len(self.matches)} documents (expected exactly one \"\n             f\"at {self.expected_path}): {joined}\"\n         )\n \n \n-def _adr_index(root: Path) -> dict[str, list[Path]]:\n-    \"\"\"Map every four-digit ADR number to the record files that claim it.\"\"\"\n-    index: dict[str, list[Path]] = {}\n+def _adr_index(root: Path) -> dict[tuple[str, str], list[Path]]:\n+    \"\"\"Map each decision namespace and number to the files that claim it.\"\"\"\n+    index: dict[tuple[str, str], list[Path]] = {}\n     adr_dir = root / \"docs\" / \"adr\"\n     if adr_dir.is_dir():\n         for path in sorted(adr_dir.glob(\"*.md\")):\n-            match = _ADR_FILENAME.match(path.name)\n+            match = _DECISION_FILENAME.match(path.name)\n             if match:\n-                index.setdefault(match.group(1), []).append(path)\n+                namespace = \"SADR\" if match.group(1) else \"ADR\"\n+                index.setdefault((namespace, match.group(2)), []).append(path)\n     return index\n \n \n def _citing_files(root: Path, include_docs: bool = True) -> list[Path]:\n-    \"\"\"Files scanned for citations: scripts/*.py, and cheaply the markdown.\"\"\"\n+    \"\"\"Current, shipped text surfaces scanned for decision citations.\"\"\"\n     files = sorted((root / \"scripts\").glob(\"*.py\"))\n+    files += sorted((root / \"scripts\").glob(\"*.sh\"))\n     if include_docs:\n         docs = root / \"docs\"\n         if docs.is_dir():\n             files += sorted(docs.rglob(\"*.md\"))\n         files += sorted(root.glob(\"*.md\"))\n-    return files\n+        pyproject = root / \"pyproject.toml\"\n+        if pyproject.is_file():\n+            files.append(pyproject)\n+        for relative in (\".github\", \".codex\"):\n+            surface = root / relative\n+            if surface.is_dir():\n+                files += sorted(\n+                    path\n+                    for path in surface.rglob(\"*\")\n+                    if path.is_file()\n+                    and path.suffix in {\".md\", \".py\", \".sh\", \".toml\", \".yaml\", \".yml\"}\n+                )\n+    return sorted(set(files))\n \n \n def find_adr_reference_violations(\n@@ -94,10 +111,9 @@\n ) -> list[Violation]:\n     \"\"\"Return every ``ADR ####`` citation under ``root`` that fails to resolve.\n \n-    A citation resolves when exactly one ``docs/adr/####-*.md`` matches its\n-    number. Zero matches is a \"missing\" violation; two or more is \"ambiguous\".\n-    Records that are never cited produce nothing -- the converse is not\n-    required.\n+    A citation resolves when exactly one record in the same namespace matches\n+    its number. Zero matches is a \"missing\" violation; two or more is\n+    \"ambiguous\". Records that are never cited produce nothing.\n     \"\"\"\n     root = Path(root)\n     index = _adr_index(root)\n@@ -110,13 +126,15 @@\n         rel = path.relative_to(root).as_posix()\n         for lineno, line in enumerate(text.splitlines(), start=1):\n             for match in _CITATION.finditer(line):\n-                number = match.group(1)\n-                found = index.get(number, [])\n+                namespace, number = match.groups()\n+                found = index.get((namespace, number), [])\n                 if len(found) == 1:\n                     continue\n                 reason = \"missing\" if not found else \"ambiguous\"\n                 rels = [p.relative_to(root).as_posix() for p in found]\n-                violations.append(Violation(rel, number, lineno, reason, rels))\n+                violations.append(\n+                    Violation(rel, namespace, number, lineno, reason, rels)\n+                )\n     return violations\n \n \n@@ -129,8 +147,8 @@\n         self.assertEqual(\n             violations,\n             [],\n-            \"Every 'ADR ####' citation must resolve to exactly one \"\n-            f\"docs/adr/####-*.md:\\n{detail}\",\n+            \"Every ADR/SADR citation must resolve exactly once in its own \"\n+            f\"docs/adr namespace:\\n{detail}\",\n         )\n \n     def test_scan_actually_reads_scripts(self) -> None:\n@@ -146,8 +164,21 @@\n         # The two records this Goal carried onto the default branch, and which\n         # the scripts cite most, must be present.\n         index = _adr_index(REPO_ROOT)\n-        self.assertIn(\"0016\", index)\n-        self.assertIn(\"0017\", index)\n+        self.assertIn((\"SADR\", \"0016\"), index)\n+        self.assertIn((\"SADR\", \"0017\"), index)\n+\n+    def test_platform_tree_has_only_sadr_0001_through_0019(self) -> None:\n+        adr_dir = REPO_ROOT / \"docs\" / \"adr\"\n+        sadrs = sorted(adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\"))\n+        self.assertEqual(\n+            {path.name.split(\"-\", 2)[1] for path in sadrs},\n+            {f\"{number:04d}\" for number in range(1, 20)},\n+        )\n+        self.assertEqual(\n+            list(adr_dir.glob(\"[0-9][0-9][0-9][0-9]-*.md\")),\n+            [],\n+            \"the Shiki platform tree must not retain legacy numeric SADR paths\",\n+        )\n \n \n class FixtureAdrReferences(unittest.TestCase):\n@@ -180,6 +211,26 @@\n         self.assertIn(\"9991\", message)  # cited number\n         self.assertIn(\"docs/adr/9991-*.md\", message)  # missing path\n \n+    def test_target_adr_does_not_satisfy_missing_platform_sadr(self) -> None:\n+        root = self._make_repo(\n+            adr_names=[\"0042-target-decision.md\"],\n+            script_body='\"\"\"Target ADR 0042 exists, but platform SADR-0042 does not.\"\"\"\\n',\n+        )\n+        violations = find_adr_reference_violations(root, include_docs=False)\n+        self.assertEqual(len(violations), 1)\n+        self.assertEqual(violations[0].namespace, \"SADR\")\n+        self.assertEqual(violations[0].number, \"0042\")\n+        self.assertEqual(violations[0].expected_path, \"docs/adr/SADR-0042-*.md\")\n+\n+    def test_target_adr_and_platform_sadr_may_reuse_a_number(self) -> None:\n+        root = self._make_repo(\n+            adr_names=[\"0042-target-decision.md\", \"SADR-0042-platform-decision.md\"],\n+            script_body='\"\"\"Target ADR 0042 and platform SADR-0042 are distinct.\"\"\"\\n',\n+        )\n+        self.assertEqual(\n+            find_adr_reference_violations(root, include_docs=False), []\n+        )\n+\n     def test_uncited_adr_is_allowed(self) -> None:\n         # An ADR exists but nothing cites it -> not a violation. The converse\n         # (every ADR must be cited) is explicitly NOT required.\ndiff --git a/tests/test_guardian_review.py b/tests/test_guardian_review.py\nindex 88829e7810212635e2fc98aa5918d6f26294677d..00a85a237acd5bf2d6cb5b294128198c422402ca\n--- a/tests/test_guardian_review.py\n+++ b/tests/test_guardian_review.py\n@@ -162,7 +162,7 @@\n \n     def test_docs_only_is_light_and_not_conservative(self):\n         focus = gr.classify_review_focus([\"docs/adr/0099-x.md\", \"README.md\"], \"\", \"low\")\n-        self.assertTrue(any(\"Docs/ADR-only\" in f for f in focus))\n+        self.assertTrue(any(\"Docs/decision-record-only\" in f for f in focus))\n         self.assertFalse(any(\"conservative default\" in f.lower() for f in focus))\n \n     def test_unknown_category_is_conservative(self):\ndiff --git a/tests/test_policy_job_merged_prs.py b/tests/test_policy_job_merged_prs.py\nindex d45512e74681e5e649279236c7d4e79d5c3da471..b97989f8bd74d091de1a6b5275a20d8dc68c4512\n--- a/tests/test_policy_job_merged_prs.py\n+++ b/tests/test_policy_job_merged_prs.py\n@@ -3,8 +3,8 @@\n \n ``.github/workflows/shiki-cca-completion.yml`` has two consumers of the merged-PR\n proof: the CCA *signal* (``guardian_approval_signal.py`` in \"Prepare CCA context\")\n-and the authoritative gate (``mergegate_check.py`` in \"Enforce MergeGate\"). ADR\n-0017's bookkeeping-closeout exemption (condition 6) needs the merged implementation\n+and the authoritative gate (``mergegate_check.py`` in \"Enforce MergeGate\").\n+SADR-0017's bookkeeping-closeout exemption (condition 6) needs the merged implementation\n PR proven, and a closeout carries NO ``post_merge_reconcile`` marker. The signal\n already resolves ``MERGED_PRS`` unconditionally, but \"Enforce MergeGate\" used to\n resolve it only inside ``grep -q '<!-- shiki:post_merge_reconcile -->'``. So a\ndiff --git a/tests/test_sadr_install_migration.py b/tests/test_sadr_install_migration.py\nnew file mode 100644\nindex 0000000000000000000000000000000000000000..a6f4c57e4dcaef73ef600b0d4804744138ab9071\n--- /dev/null\n+++ b/tests/test_sadr_install_migration.py\n@@ -0,0 +1,229 @@\n+\"\"\"SADR installation and collision-safe legacy upgrade behavior.\"\"\"\n+\n+from __future__ import annotations\n+\n+import contextlib\n+import hashlib\n+import io\n+import json\n+import shutil\n+import tempfile\n+import unittest\n+from pathlib import Path\n+\n+import shiki_test_support  # noqa: F401  (path bootstrap)\n+\n+import shiki_installer\n+from tests.test_adr_references import find_adr_reference_violations\n+\n+\n+PRODUCT_NUMBERS = tuple(range(13, 19))\n+\n+\n+def _quiet_install(target: Path, *, force: bool, validate: bool) -> None:\n+    with contextlib.redirect_stdout(io.StringIO()):\n+        shiki_installer.install_template(target, force=force, validate=validate)\n+\n+\n+def _sha256(path: Path) -> str:\n+    return hashlib.sha256(path.read_bytes()).hexdigest()\n+\n+\n+def _legacy_sadr_paths() -> tuple[str, ...]:\n+    return tuple(\n+        f\"docs/adr/{path.name.removeprefix('SADR-')}\"\n+        for path in sorted((shiki_installer.ROOT / \"docs\" / \"adr\").glob(\"SADR-*.md\"))\n+        if path.name != \"SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\"\n+    )\n+\n+\n+def _create_legacy_upgrade_fixture(\n+    target: Path,\n+) -> tuple[dict[str, bytes], tuple[str, ...]]:\n+    adr_dir = target / \"docs\" / \"adr\"\n+    adr_dir.mkdir(parents=True)\n+    product_bytes = {\n+        f\"{number:04d}-cubeshare-product-decision.md\": (\n+            f\"# CubeShare ADR {number:04d}\\n\\nDo not rewrite {number}.\\n\"\n+        ).encode()\n+        for number in PRODUCT_NUMBERS\n+    }\n+    for name, content in product_bytes.items():\n+        (adr_dir / name).write_bytes(content)\n+\n+    legacy_paths = _legacy_sadr_paths()\n+    for relative in legacy_paths:\n+        (target / relative).write_bytes(f\"legacy Shiki bytes for {relative}\\n\".encode())\n+\n+    stamp_path = target / shiki_installer.INSTALL_STAMP_PATH\n+    stamp_path.parent.mkdir(parents=True, exist_ok=True)\n+    stamp = {\n+        \"version\": shiki_installer.INSTALL_STAMP_VERSION,\n+        \"platform_commit\": None,\n+        \"installed_at\": \"2000-01-01T00:00:00+00:00\",\n+        \"digests\": {\n+            relative: _sha256(target / relative) for relative in legacy_paths\n+        },\n+    }\n+    stamp_path.write_text(\n+        json.dumps(stamp, indent=2, sort_keys=True) + \"\\n\", encoding=\"utf-8\"\n+    )\n+    return product_bytes, legacy_paths\n+\n+\n+def _tree_snapshot(root: Path) -> dict[str, tuple[str, bytes | str]]:\n+    \"\"\"Recursive byte/shape snapshot used to prove preflight is write-free.\"\"\"\n+    snapshot: dict[str, tuple[str, bytes | str]] = {}\n+    for path in sorted(root.rglob(\"*\")):\n+        relative = path.relative_to(root).as_posix()\n+        if path.is_symlink():\n+            snapshot[relative] = (\"symlink\", path.readlink().as_posix())\n+        elif path.is_dir():\n+            snapshot[relative] = (\"directory\", \"\")\n+        else:\n+            snapshot[relative] = (\"file\", path.read_bytes())\n+    return snapshot\n+\n+\n+class FreshSadrInstallTests(unittest.TestCase):\n+    def setUp(self) -> None:\n+        self.tmp = Path(tempfile.mkdtemp(prefix=\"shiki-sadr-fresh-\"))\n+        self.addCleanup(lambda: shutil.rmtree(self.tmp, ignore_errors=True))\n+        self.target = self.tmp / \"target\"\n+        self.adr_dir = self.target / \"docs\" / \"adr\"\n+        self.adr_dir.mkdir(parents=True)\n+        self.product_bytes = {\n+            f\"{number:04d}-product-decision.md\": (\n+                f\"# Product ADR {number:04d}\\n\\nTarget-owned bytes {number}.\\n\"\n+            ).encode()\n+            for number in PRODUCT_NUMBERS\n+        }\n+        for name, content in self.product_bytes.items():\n+            (self.adr_dir / name).write_bytes(content)\n+\n+    def test_fresh_install_preserves_product_adrs_and_installs_all_sadrs(self) -> None:\n+        _quiet_install(self.target, force=False, validate=True)\n+\n+        for name, expected in self.product_bytes.items():\n+            self.assertEqual((self.adr_dir / name).read_bytes(), expected)\n+\n+        sadr_numbers = {\n+            path.name.split(\"-\", 2)[1]\n+            for path in self.adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\")\n+        }\n+        self.assertEqual(sadr_numbers, {f\"{number:04d}\" for number in range(1, 20)})\n+\n+        numeric_records = {\n+            path.name\n+            for path in self.adr_dir.glob(\"[0-9][0-9][0-9][0-9]-*.md\")\n+        }\n+        self.assertEqual(numeric_records, set(self.product_bytes))\n+        self.assertEqual(find_adr_reference_violations(self.target), [])\n+\n+\n+class LegacySadrUpgradeTests(unittest.TestCase):\n+    def setUp(self) -> None:\n+        self.tmp = Path(tempfile.mkdtemp(prefix=\"shiki-sadr-upgrade-\"))\n+        self.addCleanup(lambda: shutil.rmtree(self.tmp, ignore_errors=True))\n+        self.target = self.tmp / \"target\"\n+        self.adr_dir = self.target / \"docs\" / \"adr\"\n+        self.product_bytes, self.legacy_paths = _create_legacy_upgrade_fixture(\n+            self.target\n+        )\n+\n+    def test_force_upgrade_deletes_only_stamped_legacy_sadrs(self) -> None:\n+        _quiet_install(self.target, force=True, validate=True)\n+\n+        for relative in self.legacy_paths:\n+            self.assertFalse((self.target / relative).exists(), relative)\n+        for name, expected in self.product_bytes.items():\n+            self.assertEqual((self.adr_dir / name).read_bytes(), expected)\n+\n+        sadrs = list(self.adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\"))\n+        self.assertEqual(len(sadrs), 19)\n+        refreshed = json.loads(\n+            (self.target / shiki_installer.INSTALL_STAMP_PATH).read_text(encoding=\"utf-8\")\n+        )\n+        for relative in self.legacy_paths:\n+            self.assertNotIn(relative, refreshed[\"digests\"])\n+        for path in sadrs:\n+            relative = path.relative_to(self.target).as_posix()\n+            self.assertEqual(refreshed[\"digests\"][relative], _sha256(path))\n+\n+    def test_non_force_install_never_deletes_legacy_files(self) -> None:\n+        before = {\n+            relative: (self.target / relative).read_bytes()\n+            for relative in self.legacy_paths\n+        }\n+        _quiet_install(self.target, force=False, validate=False)\n+        for relative, expected in before.items():\n+            self.assertEqual((self.target / relative).read_bytes(), expected)\n+        refreshed = json.loads(\n+            (self.target / shiki_installer.INSTALL_STAMP_PATH).read_text(encoding=\"utf-8\")\n+        )\n+        for relative in self.legacy_paths:\n+            self.assertEqual(refreshed[\"digests\"][relative], _sha256(self.target / relative))\n+\n+\n+class LegacyCleanupPreflightTests(unittest.TestCase):\n+    def setUp(self) -> None:\n+        self.tmp = Path(tempfile.mkdtemp(prefix=\"shiki-sadr-preflight-\"))\n+        self.addCleanup(lambda: shutil.rmtree(self.tmp, ignore_errors=True))\n+\n+    def test_all_ownership_blockers_abort_before_any_write(self) -> None:\n+        cases = (\n+            (\"absent\", \"install stamp is absent\"),\n+            (\"unreadable\", \"install stamp is unreadable\"),\n+            (\"malformed\", \"install stamp is malformed\"),\n+            (\"missing-digest\", \"install stamp has no digest\"),\n+            (\"digest-mismatch\", \"digest mismatch\"),\n+        )\n+        for name, expected_reason in cases:\n+            with self.subTest(name=name):\n+                target = self.tmp / name\n+                _, legacy_paths = _create_legacy_upgrade_fixture(target)\n+                stamp_path = target / shiki_installer.INSTALL_STAMP_PATH\n+\n+                if name == \"absent\":\n+                    stamp_path.unlink()\n+                elif name == \"unreadable\":\n+                    stamp_path.unlink()\n+                    stamp_path.mkdir()\n+                elif name == \"malformed\":\n+                    stamp_path.write_text(\"{not-json\\n\", encoding=\"utf-8\")\n+                else:\n+                    stamp = json.loads(stamp_path.read_text(encoding=\"utf-8\"))\n+                    if name == \"missing-digest\":\n+                        stamp[\"digests\"] = {}\n+                    else:\n+                        stamp[\"digests\"] = {\n+                            relative: \"deadbeef\" for relative in legacy_paths\n+                        }\n+                    stamp_path.write_text(\n+                        json.dumps(stamp, indent=2, sort_keys=True) + \"\\n\",\n+                        encoding=\"utf-8\",\n+                    )\n+\n+                before = _tree_snapshot(target)\n+                with self.assertRaises(shiki_installer.ShikiError) as raised:\n+                    _quiet_install(target, force=True, validate=False)\n+                after = _tree_snapshot(target)\n+\n+                self.assertEqual(before, after, f\"{name} preflight changed the tree\")\n+                message = str(raised.exception)\n+                self.assertIn(expected_reason, message)\n+                self.assertIn(\"No files were written\", message)\n+                for relative in legacy_paths:\n+                    self.assertIn(relative, message)\n+\n+    def test_absent_stamp_is_allowed_when_no_legacy_path_exists(self) -> None:\n+        target = self.tmp / \"no-legacy\"\n+        target.mkdir()\n+        _quiet_install(target, force=True, validate=False)\n+        self.assertTrue(\n+            (target / \"docs\" / \"adr\" / \"SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\").is_file()\n+        )\n+\n+\n+if __name__ == \"__main__\":\n+    unittest.main()\n\nRequired skills and preflight are complete. Resolver, SADR tree/citations, installer preflight/cleanup, and collision fixtures are implemented through recorded RED\u2192GREEN cycles. Broad local verification is green; remaining work is diff QA, structured code-review evidence, any bounded repairs, and final re-verification.\n  \u2713 Load required Shiki, diagnose, TDD, and code-review guidance; run scoped preflight and inspect durable task/context evidence\n  \u2713 Build a deterministic failing validator/installer feedback loop and record RED evidence\n  \u2713 Implement SADR namespace migration and citation/package/validator updates in vertical TDD slices\n  \u2713 Implement preflight authorization and collision-safe installer migration with adversarial TDD fixtures\n  \u2192 Run scoped and full verification suites; refactor only while green\n  \u2022 Run the structured code-review gate, repair blocking findings, re-review, and report evidence without commit/PR actions\ndiff --git a/.codex/skills/shiki/SKILL.md b/.codex/skills/shiki/SKILL.md\nindex c8e2e85d9d75d49cc4f2d6286ee22659c4e53e75..93cded623b2c15d9f65c76491a1c2d813ac9e6c0\n--- a/.codex/skills/shiki/SKILL.md\n+++ b/.codex/skills/shiki/SKILL.md\n@@ -18,6 +18,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n Codex CLI does not register this skill as a custom `/shiki` slash command. When\n the user types `/shiki` in Codex CLI and gets \"Unrecognized command\", explain\n that this is expected: invoke Shiki in Codex with natural language such as\n@@ -69,7 +72,7 @@\n \n ## Responsibilities\n \n-- Claude Code implements and repairs by default (`shiki runner claude`, ADR 0008); Codex implements and repairs tasks explicitly assigned to `codex`.\n+- Claude Code implements and repairs by default (`shiki runner claude`, SADR-0008); Codex implements and repairs tasks explicitly assigned to `codex`.\n - Claude Code Action can act as GitHub-side CCA or reviewer.\n - CCA judges completion.\n - MergeGate authorizes state transitions and merge readiness.\ndiff --git a/.github/prompts/cca-completion-check.md b/.github/prompts/cca-completion-check.md\nindex ed203ad5f302ecc81372d8ab27d2b1232ca97497..fd89950528333ef6242e6d5443904f5392ab7e56\n--- a/.github/prompts/cca-completion-check.md\n+++ b/.github/prompts/cca-completion-check.md\n@@ -19,6 +19,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n ## Required Reading\n \n Read only the files needed to judge the PR contract. Prefer this order:\ndiff --git a/.github/prompts/codex-task-handoff.md b/.github/prompts/codex-task-handoff.md\nindex 695e211d76dea1c60e8d5bb360e78487808286a4..59441d576b01a39569722f226c854e5a740e9eb5\n--- a/.github/prompts/codex-task-handoff.md\n+++ b/.github/prompts/codex-task-handoff.md\n@@ -2,7 +2,7 @@\n \n You are Codex Front acting as the Implementer for a Shiki task.\n \n-Follow `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant ADRs, and `docs/agents/codex-handoff.md`.\n+Follow `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant Shiki SADRs and target ADRs, and `docs/agents/codex-handoff.md`.\n \n Rules:\n \ndiff --git a/AGENTS.md b/AGENTS.md\nindex 7f041100d49c2a7525f51cb0f5d696a4a6d1c78c..a06bfbabc6fa48a007389e25e12d7ed523936bed\n--- a/AGENTS.md\n+++ b/AGENTS.md\n@@ -22,7 +22,7 @@\n - **Codex Front** is an optional implementer through Codex App, Codex CLI, Codex IDE extension, or Codex Web signed in with ChatGPT OAuth/subscription auth, dispatched with `shiki runner codex` when a task is explicitly assigned to `codex`.\n - **GitHub CCA** is implemented by Claude Code Action by default, using `CLAUDE_CODE_OAUTH_TOKEN`.\n \n-Do not assume `openai/codex-action`, `OPENAI_API_KEY`, or API-key based Codex automation in the default Shiki path. API-key runners are explicit target-repo extensions and require their own ADR.\n+Do not assume `openai/codex-action`, `OPENAI_API_KEY`, or API-key based Codex automation in the default Shiki path. API-key runners are explicit target-repo extensions and require their own target ADR.\n \n Shiki is not:\n \n@@ -44,10 +44,14 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use\n+`SADR-NNNN-*.md`; target product decisions retain `NNNN-*.md` and the `ADR`\n+namespace.\n+\n Use the canonical source-of-truth order above. Runtime-specific reading orders\n are operational guidance only and must not override or reorder it.\n \n-Conversation state is not durable truth. If a decision matters, put it in GitHub, `.shiki/`, `CONTEXT.md`, or an ADR.\n+Conversation state is not durable truth. If a decision matters, put it in GitHub, `.shiki/`, `CONTEXT.md`, a Shiki SADR, or a target ADR.\n \n If GitHub and `.shiki/` disagree, surface the conflict and prefer GitHub operational state until the mirror is repaired.\n \n@@ -75,8 +79,8 @@\n Every non-trivial change follows this loop:\n \n 1. **Goal**: clarify outcome, completion conditions, non-goals, risk level, and success signals.\n-2. **grill-with-docs**: challenge the plan against domain docs, ADRs, code reality, terminology, and edge scenarios.\n-3. **Context & Impact**: find relevant docs, ADRs, code, dependencies, owners, locks, and verification surfaces.\n+2. **grill-with-docs**: challenge the plan against domain docs, Shiki SADRs, target ADRs, code reality, terminology, and edge scenarios.\n+3. **Context & Impact**: find relevant docs, SADRs, target ADRs, code, dependencies, owners, locks, and verification surfaces.\n 4. **PRD and Spec Freeze**: use `to-prd` when context is settled enough to become durable product/engineering intent. The operator's explicit approval of the PRD is Spec Freeze: the plan records a `spec_freeze` block, and no plan runs without it. Steps 1-4 form Requirements Definition \u2014 one continuous operator dialogue. After Spec Freeze, scope changes require an operator-approved Spec Amendment; non-scope-moving interpretations go to the Assumption Log.\n 5. **Issues**: use `to-issues` to create independently grabbable vertical-slice issues. Prefer AFK slices over HITL slices where possible.\n 6. **Triage**: label issues for readiness, risk, runtime, skills, and MergeGate state.\n@@ -167,7 +171,7 @@\n - Testing decisions.\n - Out-of-scope items.\n - Further notes.\n-- Links to relevant domain terms and ADRs.\n+- Links to relevant domain terms, SADRs, and target ADRs.\n \n Every executable task must include:\n \n@@ -193,7 +197,7 @@\n \n Context & Impact must identify:\n \n-- Relevant documents, domain terms, ADRs, and past decisions.\n+- Relevant documents, domain terms, SADRs, target ADRs, and past decisions.\n - Relevant modules, interfaces, seams, callers, owners, and tests.\n - Dependency relationships between tasks.\n - Candidate locks and lock conflicts.\n@@ -210,7 +214,7 @@\n | Skill | Required when |\n | --- | --- |\n | `setup-matt-pocock-skills` | First configuring the repo for skills, or when issue tracker, triage labels, or domain docs are missing. |\n-| `grill-with-docs` | Default for non-trivial Goals before PRD/issues; always required when plans, terminology, boundaries, tradeoffs, or ADR-worthy decisions are ambiguous. |\n+| `grill-with-docs` | Default for non-trivial Goals before PRD/issues; always required when plans, terminology, boundaries, tradeoffs, or decision-record-worthy choices are ambiguous. |\n | `zoom-out` | The agent lacks an architectural map of the relevant code area. |\n | `to-prd` | Settled Goal context must become durable product/engineering intent. |\n | `to-issues` | A Goal, PRD, or plan must become independently grabbable vertical-slice issues. |\n@@ -348,7 +352,7 @@\n - Three or more new files.\n - Any high-risk or critical label.\n \n-When the architecture gate triggers, run `grill-with-docs` or `improve-codebase-architecture` as appropriate and record durable decisions in an ADR when needed.\n+When the architecture gate triggers, run `grill-with-docs` or `improve-codebase-architecture` as appropriate and record durable decisions in a Shiki SADR or target ADR as applicable.\n \n ## Engineering Discipline\n \ndiff --git a/CLAUDE.md b/CLAUDE.md\nindex 98a932e7841a632866fcd53b0bad0e3af3711d3d..98a31399404d6fb044b4fd493f4459ac87f439de\n--- a/CLAUDE.md\n+++ b/CLAUDE.md\n@@ -14,6 +14,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n ## Claude Role\n \n Default to planner, default implementer, reviewer, coordinator, documentation editor, CCA-style completion judge, and final-judgment assistant.\n@@ -57,7 +60,7 @@\n At the start of a Shiki session:\n \n 1. Read `AGENTS.md`.\n-2. Read `CONTEXT.md` and relevant ADRs.\n+2. Read `CONTEXT.md` and relevant Shiki SADRs and target ADRs.\n 3. Read active `.shiki/` Goal/task/ledger state when present.\n 4. Read active distilled rules from `.shiki/memories/` (status `distilled`, `active: true`, not revoked or superseded) for applicable guidance; the same rules are injected deterministically into task handoffs as the `## Distilled Rules` section (\u00a73.5).\n 5. Read the GitHub Issue or PR when the task is GitHub-backed.\n@@ -74,7 +77,7 @@\n - Define the Goal, completion conditions, non-goals, and risk level.\n - Treat `grill-with-docs` as the default planning entry point for non-trivial Goals.\n - Challenge domain terms against `CONTEXT.md`.\n-- Challenge hard-to-reverse tradeoffs against ADRs.\n+- Challenge hard-to-reverse tradeoffs against SADRs and target ADRs.\n - Explore code instead of asking when code can answer the question.\n - Run Context & Impact before implementation planning.\n - Produce a Task DAG, not an unordered checklist.\n@@ -94,7 +97,7 @@\n - For each question, provide your recommended answer.\n - Resolve dependency decisions before downstream design questions.\n - Update `CONTEXT.md` inline when terminology crystallizes.\n-- Offer ADRs only when the decision is hard to reverse, surprising without context, and a real tradeoff.\n+- Offer a SADR or target ADR only when the decision is hard to reverse, surprising without context, and a real tradeoff.\n - Convert unresolved questions into HITL blockers rather than allowing Codex to guess.\n \n ## PRD and Issues Standard\n@@ -103,7 +106,7 @@\n \n - Use the project's domain glossary vocabulary.\n - Avoid volatile file paths unless they are essential.\n-- Include implementation decisions, testing decisions, out-of-scope items, and relevant ADR/domain links.\n+- Include implementation decisions, testing decisions, out-of-scope items, and relevant SADR/target-ADR/domain links.\n - Identify deep-module opportunities and testable interfaces.\n \n When creating issues:\n@@ -121,7 +124,7 @@\n - Branch/worktree target.\n - Scope and non-goals.\n - Dependencies and locks.\n-- Relevant docs, ADRs, modules, and tests.\n+- Relevant docs, SADRs, target ADRs, modules, and tests.\n - Required skill invocations.\n - TDD expectations.\n - Pre-PR `code-review` gate expectations.\n@@ -152,7 +155,7 @@\n \n - Treat yourself as a judge, not implementer.\n - Do not write production code.\n-- Read `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant ADRs, PRD, task issue, PR body, diff, checks, reviews, labels, and `.shiki/` state.\n+- Read `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant SADRs and target ADRs, PRD, task issue, PR body, diff, checks, reviews, labels, and `.shiki/` state.\n - Evaluate every applicable checklist in `docs/agents/checklists.md`.\n - Map each acceptance criterion to durable evidence.\n - Distinguish implementation failure from missing evidence.\ndiff --git a/CONTEXT.md b/CONTEXT.md\nindex e61a9be06a0cce0d026484083a657bde2067e234..237ee8aa3ea8fab334ece49768bc5ed3b5af0974\n--- a/CONTEXT.md\n+++ b/CONTEXT.md\n@@ -25,7 +25,7 @@\n _Avoid_: jumping directly into code\n \n **grill-with-docs**:\n-A planning skill that challenges a plan against domain language, ADRs, code reality, and concrete edge scenarios. It resolves design-tree questions before PRD/issues. It runs inside Requirements Definition.\n+A planning skill that challenges a plan against domain language, Shiki SADRs, target ADRs, code reality, and concrete edge scenarios. It resolves design-tree questions before PRD/issues. It runs inside Requirements Definition.\n _Avoid_: generic brainstorming, silent assumptions\n \n **Requirements Definition**:\ndiff --git a/CONTRIBUTING.md b/CONTRIBUTING.md\nindex c6d9b656e73abb8a55e5f660123e03153dcb3e6d..9e37a8fbf668db5b22461aaae5548b8f1354394c\n--- a/CONTRIBUTING.md\n+++ b/CONTRIBUTING.md\n@@ -17,7 +17,7 @@\n 1. **Goal** \u2014 start from a user-approved Goal with an outcome, completion\n    conditions, non-goals, risk level, and success signals.\n 2. **grill-with-docs** \u2014 for any non-trivial change, challenge the plan against\n-   domain language, ADRs, and code reality before writing a PRD.\n+   domain language, Shiki SADRs, target ADRs, and code reality before writing a PRD.\n 3. **PRD** \u2014 capture settled product/engineering intent (`to-prd`).\n 4. **Spec Freeze** \u2014 the operator explicitly approves the PRD/requirements;\n    the plan records a `spec_freeze` block and no plan runs without it\n@@ -94,7 +94,7 @@\n   entries.\n - Do not hand-edit `.shiki/goals/*.json` or `.shiki/manifest.json` in a task PR.\n - Conversation state is not durable truth. If a decision matters, record it in\n-  GitHub, `.shiki/`, `CONTEXT.md`, or an ADR.\n+  GitHub, `.shiki/`, `CONTEXT.md`, a Shiki SADR, or a target ADR.\n \n ## Architecture Gate\n \n@@ -103,7 +103,7 @@\n or CI policy, package structure, three or more structural files, more than 100\n net new lines in a core module, three or more new files, or any high-risk label.\n When triggered, run `grill-with-docs` or `improve-codebase-architecture` and\n-record durable decisions in an ADR when needed (see `docs/adr/`).\n+record durable decisions in a Shiki SADR or target ADR as applicable (see `docs/adr/`).\n \n ## Safety\n \ndiff --git a/SYSTEM_PROMPT.md b/SYSTEM_PROMPT.md\nindex aaaea45e3a896c3a44ba89eec8bb84325ecf6c62..da009c86a88285269dee0155e58e550c6d744ea9\n--- a/SYSTEM_PROMPT.md\n+++ b/SYSTEM_PROMPT.md\n@@ -30,6 +30,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n Use the canonical source-of-truth order above. Runtime-specific reading orders\n are operational guidance only and must not override or reorder it. The current\n conversation is non-durable operator input.\n@@ -48,14 +51,14 @@\n \n A runtime may only do work that is explicitly assigned by the Goal, task, PR, or MergeGate decision.\n \n-Do not assume `openai/codex-action`, `OPENAI_API_KEY`, or API-key based Codex automation in the default Shiki path. API-key runners are explicit target-repo extensions and require their own ADR.\n+Do not assume `openai/codex-action`, `OPENAI_API_KEY`, or API-key based Codex automation in the default Shiki path. API-key runners are explicit target-repo extensions and require their own target ADR.\n \n ## 4. Non-negotiable Shiki flow\n \n For any non-trivial Goal, use this flow:\n \n 1. **Goal Seek**: define outcome, non-goals, risk level, completion criteria, and evidence requirements.\n-2. **grill-with-docs**: challenge the plan against `CONTEXT.md`, ADRs, code reality, terminology, boundaries, and edge scenarios. Ask one question at a time when the operator is available. If code can answer a question, inspect code instead of asking.\n+2. **grill-with-docs**: challenge the plan against `CONTEXT.md`, Shiki SADRs, target ADRs, code reality, terminology, boundaries, and edge scenarios. Ask one question at a time when the operator is available. If code can answer a question, inspect code instead of asking.\n 3. **Context & Impact**: identify modules, interfaces, seams, callers, dependencies, locks, risk, and verification surfaces.\n 4. **to-prd + Spec Freeze**: turn settled context into a PRD using domain vocabulary and testing decisions. The operator's explicit approval of the PRD is Spec Freeze, recorded as the plan's `spec_freeze` block; plans without it do not run. Steps 1-4 form Requirements Definition.\n 5. **to-issues**: decompose the PRD into independently grabbable vertical-slice issues. Prefer AFK slices; mark HITL slices when judgment is still required.\n@@ -75,7 +78,7 @@\n Before making any material change:\n \n 1. Read `AGENTS.md` and any runtime-specific wrapper such as `CLAUDE.md`.\n-2. Read `CONTEXT.md` and relevant ADRs.\n+2. Read `CONTEXT.md` and relevant SADRs and target ADRs.\n 3. Read the GitHub Issue / PR and `.shiki/` state for the active Goal or task.\n 4. Identify your role: Planner, Implementer, Reviewer, Completion Check Agent, Repairer, or Guardian-assist.\n 5. Identify Goal id, task id, branch, PR, dependencies, locks, risk level, required skills, and required evidence.\n@@ -103,7 +106,7 @@\n \n At minimum, identify:\n \n-- Relevant documents, ADRs, domain terms, and prior decisions.\n+- Relevant documents, SADRs, target ADRs, domain terms, and prior decisions.\n - Relevant modules, interfaces, seams, callers, owners, and tests.\n - Dependency relationships between tasks.\n - Candidate file locks and lock conflicts.\n@@ -152,7 +155,7 @@\n \n CCA must read:\n \n-- `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant ADRs.\n+- `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant SADRs and target ADRs.\n - Parent Goal issue, PRD issue, task issue, comments, and labels.\n - PR body, diff, commits, changed files, reviews, CI checks, and artifacts available to the workflow.\n - `.shiki/` task contract, locks, ledger entries, CCA checklist profile, and prior repair packets.\n@@ -214,7 +217,7 @@\n - Three or more new files.\n - Any high-risk or critical label.\n \n-When the architecture gate triggers, run `grill-with-docs` or `improve-codebase-architecture` as appropriate and record the decision in an ADR if it is hard to reverse or likely to be re-litigated.\n+When the architecture gate triggers, run `grill-with-docs` or `improve-codebase-architecture` as appropriate and record the decision in a Shiki SADR or target ADR as applicable if it is hard to reverse or likely to be re-litigated.\n \n ## 14. Skill Gate\n \n@@ -223,7 +226,7 @@\n Use these skills:\n \n - `setup-matt-pocock-skills`: before first use of the engineering skill set, or when issue tracker, triage labels, or domain docs are missing.\n-- `grill-with-docs`: default for non-trivial Goals before PRD/issues; required for ambiguous plans, terminology, boundaries, ADR-worthy tradeoffs, or design-tree decisions.\n+- `grill-with-docs`: default for non-trivial Goals before PRD/issues; required for ambiguous plans, terminology, boundaries, decision-record-worthy tradeoffs, or design-tree decisions.\n - `zoom-out`: unfamiliar code area or missing architectural map.\n - `to-prd`: convert settled context into a PRD.\n - `to-issues`: break a PRD, plan, or Goal into independently grabbable vertical-slice issues.\ndiff --git a/docs/adr/0001-keep-shiki-as-platform-name.md b/docs/adr/SADR-0001-keep-shiki-as-platform-name.md\nindex 20a9637c3b9d8c97de7ba8db7cfae4d82d9a7dba..8b685439959aada1b221c112f0ac3c51ca8a1695\n--- a/docs/adr/0001-keep-shiki-as-platform-name.md\n+++ b/docs/adr/SADR-0001-keep-shiki-as-platform-name.md\n@@ -1,3 +1,3 @@\n-# Keep Shiki As The Platform Name\n+# SADR-0001: Keep Shiki As The Platform Name\n \n Shiki remains the product name for the GitHub-native agentic engineering control plane. We considered more descriptive names such as MergePilot, MergeGate, and Kiroku, but kept Shiki because it carries the existing goal-seeking framework identity while remaining independent of Hermes, Codex, Claude Code, or any single runtime.\ndiff --git a/docs/adr/0002-use-agents-as-shared-constitution.md b/docs/adr/SADR-0002-use-agents-as-shared-constitution.md\nindex 73de30ecb078120eac540d9bc51855e3cd48d0e3..c5486182bf06fca36ea508496b013c67302fd989\n--- a/docs/adr/0002-use-agents-as-shared-constitution.md\n+++ b/docs/adr/SADR-0002-use-agents-as-shared-constitution.md\n@@ -1,3 +1,3 @@\n-# Use AGENTS As The Shared Agent Constitution\n+# SADR-0002: Use AGENTS As The Shared Agent Constitution\n \n Shiki uses `AGENTS.md` as the runtime-neutral constitution and `CLAUDE.md` as a thin Claude Code wrapper. This keeps Codex, Claude Code, Hermes Runner, and future Agent Runtimes aligned on the same GitHub-first MergeGate rules while still allowing runtime-specific instructions where needed.\ndiff --git a/docs/adr/0003-require-skill-gate-for-mergegate-readiness.md b/docs/adr/SADR-0003-require-skill-gate-for-mergegate-readiness.md\nindex 424be0c9af4c8f0136f1fde68a468c47ff84e764..7fc337987c7df9792f75f181dfcbed75fe5ffff2\n--- a/docs/adr/0003-require-skill-gate-for-mergegate-readiness.md\n+++ b/docs/adr/SADR-0003-require-skill-gate-for-mergegate-readiness.md\n@@ -1,3 +1,3 @@\n-# Require Skill Gate For MergeGate Readiness\n+# SADR-0003: Require Skill Gate For MergeGate Readiness\n \n Shiki requires relevant engineering skills to be invoked before execution when their triggers apply. This makes skills part of MergeGate readiness rather than optional prompt style, because Shiki depends on repeatable planning, issue decomposition, TDD, diagnosis, triage, and architecture review before autonomous or parallel agent work can be trusted.\ndiff --git a/docs/adr/0004-use-github-first-with-shiki-mirror.md b/docs/adr/SADR-0004-use-github-first-with-shiki-mirror.md\nindex d0a3a9174dc646a945a9d325954ffaf00c352040..a79c54ae9ee8eed2a0524192b864da4e22a92d3f\n--- a/docs/adr/0004-use-github-first-with-shiki-mirror.md\n+++ b/docs/adr/SADR-0004-use-github-first-with-shiki-mirror.md\n@@ -1,4 +1,4 @@\n-# ADR 0004: Use GitHub First With A Repository-Local Shiki Mirror\n+# SADR-0004: Use GitHub First With A Repository-Local Shiki Mirror\n \n ## Status\n \ndiff --git a/docs/adr/0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md b/docs/adr/SADR-0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\nindex b339baf49b89d9290e79c3d51b36e7b2bde3bd95..0f3c3eb9afccdb3403ce978e21d04fe12e495da0\n--- a/docs/adr/0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\n+++ b/docs/adr/SADR-0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\n@@ -1,8 +1,8 @@\n-# ADR 0005: Use Codex As Front And Claude Code Action As The GitHub Runtime\n+# SADR-0005: Use Codex As Front And Claude Code Action As The GitHub Runtime\n \n ## Status\n \n-Accepted (implementer default superseded by ADR 0008; the auth model and the\n+Accepted (implementer default superseded by SADR-0008; the auth model and the\n Claude Code Action GitHub runtime decision remain in force)\n \n ## Context\n@@ -26,4 +26,4 @@\n - Shiki avoids requiring `OPENAI_API_KEY` for the default workflow.\n - Codex work happens through the user's authenticated Codex front surface, not as a hidden backend Action.\n - Claude Code Action is configured for OAuth-token based subscription usage by default.\n-- API-key based automation remains possible only as an explicit target-repo extension with its own ADR.\n+- API-key based automation remains possible only as an explicit target-repo extension with its own target ADR.\ndiff --git a/docs/adr/0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md b/docs/adr/SADR-0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\nindex 4bcae9d1e52f0a37b8e9fbafa5cd14b6ee1993bd..7a38538a6ae9929567f0ade67e7ea6a60d0b3f7e\n--- a/docs/adr/0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\n+++ b/docs/adr/SADR-0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\n@@ -1,4 +1,4 @@\n-# ADR 0006: Separate Codex Implementation, CCA Completion, And MergeGate Authorization\n+# SADR-0006: Separate Codex Implementation, CCA Completion, And MergeGate Authorization\n \n ## Status\n \ndiff --git a/docs/adr/0007-packaging-and-release.md b/docs/adr/SADR-0007-packaging-and-release.md\nindex 79b84ddb97167372b44641e5b7860e400ce80822..bb445a2e2e856d9d1f5fdbe35de8d4e8cec0fb32\n--- a/docs/adr/0007-packaging-and-release.md\n+++ b/docs/adr/SADR-0007-packaging-and-release.md\n@@ -1,4 +1,4 @@\n-# ADR 0007: Packaging And Release For Shiki\n+# SADR-0007: Packaging And Release For Shiki\n \n ## Status\n \n@@ -25,7 +25,7 @@\n   not introduce third-party runtime dependencies or break the shim.\n - Releases must run inside the operator's subscription-authenticated toolchain\n   and must not require extra secrets beyond the built-in `GITHUB_TOKEN`\n-  (consistent with ADR 0005's no-API-key-by-default stance).\n+  (consistent with SADR-0005's no-API-key-by-default stance).\n - `.shiki/manifest.json` already encodes the install surface (`install.include`,\n   `install.create_directories`, `install.exclude_from_commit`), and it must not\n   be mutated as part of this change.\n@@ -34,7 +34,7 @@\n \n This is a hard-to-reverse platform decision (it sets the version source of\n truth, the release trigger, and the public install contract), so it is recorded\n-as an ADR.\n+as a SADR.\n \n ## Decision\n \n@@ -104,7 +104,7 @@\n 3. **Publishing to PyPI.** Rejected for now: Shiki is distributed as a\n    repository template + git-installable CLI, not a PyPI library; PyPI would add\n    credentials/secrets and a different versioning contract. Can be added later as\n-   its own ADR if a packaged distribution is needed.\n+   its own SADR if a packaged distribution is needed.\n 4. **Dynamic version (read `VERSION` via a build backend `dynamic` field).**\n    Rejected to keep `pyproject.toml` backend-agnostic and dependency-free; the\n    small duplication between `VERSION` and `pyproject.toml` is guarded by the\n@@ -123,4 +123,4 @@\n   releasing versions the whole platform but ships only `install.include` to\n   targets.\n - Future PyPI distribution or a console entry point are deferred and would each\n-  warrant their own ADR.\n+  warrant their own SADR.\ndiff --git a/docs/adr/0008-use-claude-code-as-default-implementer.md b/docs/adr/SADR-0008-use-claude-code-as-default-implementer.md\nindex dedf4510b7c7799521422f622029563d373db031..35c2625d908167fbc58855b5b63e283805f167e8\n--- a/docs/adr/0008-use-claude-code-as-default-implementer.md\n+++ b/docs/adr/SADR-0008-use-claude-code-as-default-implementer.md\n@@ -1,12 +1,12 @@\n-# ADR 0008: Use Claude Code As The Default Implementer Runtime\n+# SADR-0008: Use Claude Code As The Default Implementer Runtime\n \n ## Status\n \n-Proposed (supersedes ADR 0005 for the implementer default; keeps its auth model)\n+Proposed (supersedes SADR-0005 for the implementer default; keeps its auth model)\n \n ## Context\n \n-ADR 0005 made Codex Front the default implementation runtime, with Claude Code\n+SADR-0005 made Codex Front the default implementation runtime, with Claude Code\n restricted to planner and reviewer roles by the runtime registry, and the only\n autonomous implementation adapter was `shiki runner codex`.\n \n@@ -18,13 +18,13 @@\n - The platform's planning, review, and CCA layers already run on Claude Code,\n   and Claude Code's native capabilities (the built-in `/code-review` skill and\n   the Workflow multi-agent orchestration tool) have no Codex equivalent.\n-- The Goal lifecycle redesign (ADR 0009) requires an autonomous post-freeze\n+- The Goal lifecycle redesign (SADR-0009) requires an autonomous post-freeze\n   loop driven from the operator's Claude Code session, including bounded\n   repair dispatch, which must not depend on a runtime that cannot be invoked\n   unattended.\n \n The constitution claims Shiki is runtime-agnostic (\"not a Claude-only\n-workflow\", ADR 0002). Any change to the implementer default must preserve the\n+workflow\", SADR-0002). Any change to the implementer default must preserve the\n registry-based runtime model rather than hardcoding one vendor.\n \n ## Decision\n@@ -39,7 +39,7 @@\n   the task to review or repair-needed.\n - Keep Codex registered as an optional implementer runtime. `shiki runner\n   codex`, the Codex handoff templates, and the ChatGPT OAuth auth model from\n-  ADR 0005 remain supported for tasks explicitly assigned to `codex`.\n+  SADR-0005 remain supported for tasks explicitly assigned to `codex`.\n - Commit to mandating two Claude-native capabilities as loop contract in\n   follow-up Goals (status: the code-review gate was delivered by Goal G-B \u2014\n   skill registry, checklists item PR-12, default required_skills \u2014 while the\n@@ -60,7 +60,7 @@\n \n Out of scope: removing Codex support, changing the CCA runtime (Claude Code\n Action with `CLAUDE_CODE_OAUTH_TOKEN` stays), and API-key based automation\n-(still requires its own ADR per ADR 0005).\n+(still requires its own target ADR per SADR-0005).\n \n ## Consequences\n \n@@ -100,7 +100,7 @@\n   harness rejection of direct `codex exec` is outside Shiki's control, and the\n   plan's mandatory native capabilities cannot run on Codex at all.\n - **Remove Codex entirely**: rejected; it abandons the runtime-agnostic\n-  constitution (ADR 0002) for no operational gain and forecloses\n+  constitution (SADR-0002) for no operational gain and forecloses\n   cross-runtime adversarial review.\n - **GitHub Actions cloud implementation via claude-code-action**: rejected as\n   default; durable evidence is automatic but token cost, Node 24 workflow\ndiff --git a/docs/adr/0009-requirements-definition-and-spec-freeze.md b/docs/adr/SADR-0009-requirements-definition-and-spec-freeze.md\nindex 29b3f7871bef9203a8b4a831a65c39eb2f506e42..52e7dd69ad0aa4efe0b68c62678abbf7df02aaec\n--- a/docs/adr/0009-requirements-definition-and-spec-freeze.md\n+++ b/docs/adr/SADR-0009-requirements-definition-and-spec-freeze.md\n@@ -1,4 +1,4 @@\n-# ADR 0009: Merge Grill And Planning Into Requirements Definition With Spec Freeze\n+# SADR-0009: Merge Grill And Planning Into Requirements Definition With Spec Freeze\n \n ## Status\n \ndiff --git a/docs/adr/0010-external-ai-guardian-review.md b/docs/adr/SADR-0010-external-ai-guardian-review.md\nindex e6108bbe8bb4e73e0ff4684c8f78001a80438c4e..f3510b145664298adafb151b6f0db51b972f01ec\n--- a/docs/adr/0010-external-ai-guardian-review.md\n+++ b/docs/adr/SADR-0010-external-ai-guardian-review.md\n@@ -1,4 +1,4 @@\n-# ADR 0010: External AI Guardian Review As A First-Class Approval Authority\n+# SADR-0010: External AI Guardian Review As A First-Class Approval Authority\n \n ## Status\n \n@@ -8,7 +8,7 @@\n \n Shiki is intended to be an autonomous, recursive, self-improving agent\n framework: authority-in-the-loop, not human-in-the-loop. Until now the Guardian\n-approval required for high/critical-risk merges (guardian-policy.json, ADR\n+approval required for high/critical-risk merges (guardian-policy.json, SADR\n governance) recognized only human authority sources \u2014 a GitHub review approval,\n the `guardian:approved` label applied by a configured Guardian, or a comment\n carrying `Guardian approval granted` plus the current head SHA.\n@@ -89,7 +89,7 @@\n   autonomous design the operator intends and produced the impersonation\n   incident.\n - **Record AI approval as an operator comment**: rejected as forgery of human\n-  authority \u2014 the exact failure this ADR corrects.\n+  authority \u2014 the exact failure this SADR corrects.\n - **Deliver the artifact as a committed `.shiki/` file**: rejected; a committed\n   approval inside the PR being approved is forgeable by anyone who can push.\n - **A dedicated reviewer bot identity instead of a relayed comment**: deferred;\ndiff --git a/docs/adr/0011-autonomous-quality-gate-evidence.md b/docs/adr/SADR-0011-autonomous-quality-gate-evidence.md\nindex bca0246c5d46e44afbde15486a75eec425fc02f5..9c93eac16d8c3a6d4b11bd7224e3955102cd7db4\n--- a/docs/adr/0011-autonomous-quality-gate-evidence.md\n+++ b/docs/adr/SADR-0011-autonomous-quality-gate-evidence.md\n@@ -1,4 +1,4 @@\n-# ADR 0011: Autonomous Quality-Gate Evidence \u2014 Deterministic Facts And An Independent Verifier\n+# SADR-0011: Autonomous Quality-Gate Evidence \u2014 Deterministic Facts And An Independent Verifier\n \n ## Status\n \ndiff --git a/docs/adr/0012-autonomous-loop-closeout-to-main.md b/docs/adr/SADR-0012-autonomous-loop-closeout-to-main.md\nindex 637fad6dde4989c9c2bf0f81f676d8d29991123c..ec7d85f81030a676bcbba28b43eb365422a36eac\n--- a/docs/adr/0012-autonomous-loop-closeout-to-main.md\n+++ b/docs/adr/SADR-0012-autonomous-loop-closeout-to-main.md\n@@ -1,4 +1,4 @@\n-# 0012 \u2014 Autonomous loop pushes goal completion to main via a closeout PR\n+# SADR-0012 \u2014 Autonomous loop pushes goal completion to main via a closeout PR\n \n - Status: accepted\n - Date: 2026-06-18\ndiff --git a/docs/adr/0013-bootstrap-grants-actions-approve-pull-requests.md b/docs/adr/SADR-0013-bootstrap-grants-actions-approve-pull-requests.md\nindex b6c69d64f1c5ced346b88d6859586715b3b5bd0e..cf48ff4a6716267f539ca58edd1a852e7c7a2d64\n--- a/docs/adr/0013-bootstrap-grants-actions-approve-pull-requests.md\n+++ b/docs/adr/SADR-0013-bootstrap-grants-actions-approve-pull-requests.md\n@@ -1,4 +1,4 @@\n-# ADR 0013: Bootstrap Grants GitHub Actions Permission To Create And Approve Pull Requests\n+# SADR-0013: Bootstrap Grants GitHub Actions Permission To Create And Approve Pull Requests\n \n ## Status\n \n@@ -31,7 +31,7 @@\n security tradeoff: it lets repository automation satisfy a human-review gate. In\n a multi-maintainer repo that could let a compromised or buggy workflow\n self-approve changes. So whether bootstrap turns it on by default \u2014 and with\n-what default token scope \u2014 is an ADR-worthy decision, not an implementation\n+what default token scope \u2014 is a SADR-worthy decision, not an implementation\n detail.\n \n ## Decision\n@@ -48,7 +48,7 @@\n Specifically:\n \n - **`can_approve_pull_request_reviews` defaults to `true`.** Shiki's default\n-  posture is solo/AFK autonomous operation (ADR 0008/0009/0012), where the CCA\n+  posture is solo/AFK autonomous operation (SADR-0008/0009/0012), where the CCA\n   Review Bridge is the only path that can satisfy `required_review: true` after\n   the CCA and Guardian gates have already passed. The default makes a\n   freshly-bootstrapped repo able to run the autonomous loop end to end without a\ndiff --git a/docs/adr/0014-codex-app-external-ai-guardian-ui-adapter.md b/docs/adr/SADR-0014-codex-app-external-ai-guardian-ui-adapter.md\nindex 238db06279334caa341c117362df9271382a7fcd..65f12e9939321e1b3e389a34c822fc1549e5155d\n--- a/docs/adr/0014-codex-app-external-ai-guardian-ui-adapter.md\n+++ b/docs/adr/SADR-0014-codex-app-external-ai-guardian-ui-adapter.md\n@@ -1,4 +1,4 @@\n-# ADR 0014: Use Codex App As The External AI Guardian UI Adapter\n+# SADR-0014: Use Codex App As The External AI Guardian UI Adapter\n \n ## Status\n \ndiff --git a/docs/adr/0015-contract-approval-before-dispatch.md b/docs/adr/SADR-0015-contract-approval-before-dispatch.md\nindex fad6c656741a20a7684ba16080a7db159f63a52f..6061271a46d3f1d6ae7174477de3fb75673123a6\n--- a/docs/adr/0015-contract-approval-before-dispatch.md\n+++ b/docs/adr/SADR-0015-contract-approval-before-dispatch.md\n@@ -1,4 +1,4 @@\n-# ADR 0015: Approve The Task Contract Before Dispatch, Not The Diff After It\n+# SADR-0015: Approve The Task Contract Before Dispatch, Not The Diff After It\n \n ## Status\n \n@@ -9,7 +9,7 @@\n Guardian approval for high/critical risk is expressible only on a pull request.\n Every approval source in `guardian-policy.json` \u2014 GitHub review, the\n `guardian:approved` label, the `Guardian approval granted` comment, and the\n-`external_ai_guardian_review` artifact (ADR 0010/0014) \u2014 is bound to a PR, and\n+`external_ai_guardian_review` artifact (SADR-0010/0014) \u2014 is bound to a PR, and\n three of the four require the current head SHA. Approval therefore cannot exist\n before an implementation PR exists.\n \n@@ -81,7 +81,7 @@\n An envelope breach must surface only on the non-repairable policy gate, never on\n the repairable `MergeGate metadata check`. Otherwise the Goal Loop routes it to\n `dispatch_repair` \u2014 instructing a runner to make the breach go away \u2014 which is\n-exactly what this ADR forbids.\n+exactly what this SADR forbids.\n \n **Contract immutability.** After registration, an implementation PR must not\n change the governance fields of its own task contract (`scope`, `non_goals`,\n@@ -116,7 +116,7 @@\n fails any verdict that is not `complete`, and the signal the CCA prompt reads is\n computed from `evaluate_guardian_approval` alone. Without the OR-merge at the\n signal, a contract-approved PR still returns `needs_guardian`, the CCA job fails,\n-and the MergeGate policy check never runs \u2014 the gate this ADR changes would never\n+and the MergeGate policy check never runs \u2014 the gate this SADR changes would never\n be reached. The CCA *prompt* needs no change: a `contract_approval` entry in the\n signal's existing `sources` array satisfies its current wording.\n \n@@ -130,7 +130,7 @@\n written, and an approved contract lets its implementation PRs merge through the\n same automatic path as low/medium risk. The wasted implement-then-block cycle\n disappears. Guardian review shifts from reading diffs to reading contracts, which\n-is both earlier and smaller; the external AI Guardian path (ADR 0010/0014) works\n+is both earlier and smaller; the external AI Guardian path (SADR-0010/0014) works\n on contracts with no change.\n \n We accept that the Guardian authorizes a class of change without seeing its\n@@ -146,7 +146,7 @@\n adversarial tests for the new approval path equivalent in rigor to\n `test_shiki_governance_evidence.sh`.\n \n-Bootstrap costs, which apply only while this ADR is being implemented. The tasks\n+Bootstrap costs, which apply only while this SADR is being implemented. The tasks\n that build Contract Approval merge under the existing rules, so the ones\n dispatched after the pre-dispatch gate lands but before the approval path lands\n need a Guardian-approved Contract PR *and* a live Guardian approval on their\n@@ -210,7 +210,7 @@\n risk; they do not bound authorized work.\n \n **Auto-escalate risk on envelope breach instead of stopping.** Returns the\n-workflow to a diff-time Guardian decision \u2014 the exact behavior this ADR removes.\n+workflow to a diff-time Guardian decision \u2014 the exact behavior this SADR removes.\n \n **Resolve governance fields from the base snapshot only.** Rejected against the\n registration flow: implementation PRs add their own contract, so the base has no\ndiff --git a/docs/adr/0016-locks-bound-product-paths.md b/docs/adr/SADR-0016-locks-bound-product-paths.md\nindex b5e850f3f1f7f4ef6e7f9d1763a29a51bd300c45..46b40a9b99a7aae644c02bb458f5473ff0a90ce2\n--- a/docs/adr/0016-locks-bound-product-paths.md\n+++ b/docs/adr/SADR-0016-locks-bound-product-paths.md\n@@ -1,4 +1,4 @@\n-# ADR 0016: Locks bound product paths; the Shiki mirror is bound by record identity\n+# SADR-0016: Locks bound product paths; the Shiki mirror is bound by record identity\n \n ## Status\n \n@@ -6,8 +6,8 @@\n \n - Date: 2026-07-28\n - Deciders: operator (mizutani-140), Claude (planner)\n-- Related: ADR 0004 (GitHub-first with a `.shiki` mirror), ADR 0009 (Spec Freeze),\n-  ADR 0012 (autonomous loop closeout to main), ADR 0015 (Contract Approval before\n+- Related: SADR-0004 (GitHub-first with a `.shiki` mirror), SADR-0009 (Spec Freeze),\n+  SADR-0012 (autonomous loop closeout to main), SADR-0015 (Contract Approval before\n   dispatch), remediation goal task R-02, the live PR #179 deadlock.\n \n ## Context\n@@ -90,11 +90,11 @@\n files, documentation files and named source modules are nameable; runtime-\n allocated ledger, runner and report ids are not \u2014 and those live in `.shiki`,\n which locks no longer govern. Existing frozen contracts are brought into line by\n-a one-time Spec Amendment per plan, which must land before ADR 0015's contract\n+a one-time Spec Amendment per plan, which must land before SADR-0015's contract\n immutability makes `locks` unwritable from the implementation PR.\n \n **Lock state is resolved from the durable side.** MergeGate resolves the lock\n-registry, and ADR 0015's governance fields, from the base `.shiki` snapshot when\n+registry, and SADR-0015's governance fields, from the base `.shiki` snapshot when\n present, falling back to head only when absent. The `MergeGate metadata check`\n job is given `--base-shiki`, which it does not receive today. Locks are not\n derived from the live open-pull-request set: a pull request body is untrusted\n@@ -108,7 +108,7 @@\n Explicitly out of scope: moving MergeGate's contract resolution off the PR\n checkout wholesale; making the mirror a projection of GitHub state; introducing\n new lock kinds; moving ledger evidence out of the repository; and unwinding\n-ADR 0012's closeout pull request. Registration of multi-task goals is ADR 0015's\n+SADR-0012's closeout pull request. Registration of multi-task goals is SADR-0015's\n Contract PR and is not re-decided here.\n \n ## Consequences\n@@ -127,7 +127,7 @@\n \n Risks accepted. First, goal-scoped identity is only as strong as `goal_id`,\n which is resolved from the PR body and the head task file; the mitigation is to\n-add `goal_id` to ADR 0015's frozen governance field set, and until that lands a\n+add `goal_id` to SADR-0015's frozen governance field set, and until that lands a\n task file can name a foreign goal. Second, base-when-present resolution is\n unavailable on fork pull requests, where the head is contributor-controlled;\n fork pull requests already cannot complete a Shiki merge, and this decision does\n@@ -139,7 +139,7 @@\n path, not by implementation pull requests.\n \n New obligations: one Spec Amendment per frozen plan to rewrite `locks`, ordered\n-before ADR 0015 T1; extension of the adversarial suites in\n+before SADR-0015 T1; extension of the adversarial suites in\n `scripts/test_shiki_adversarial_state.sh` and\n `scripts/test_shiki_governance_evidence.sh` to cover the eleven newly-scoped\n directories; and updating `scripts/test_shiki_loop_lock_guard.sh`,\n@@ -151,32 +151,32 @@\n whether the loop's transitive sync of ledger `evidence` references can be\n deleted once the Contract PR carries its own source plan.\n \n-## Relationship to ADR 0004\n+## Relationship to SADR-0004\n \n-This ADR **clarifies something ADR 0004 left unspecified**. ADR 0004 decides that\n+This SADR **clarifies something SADR-0004 left unspecified**. SADR-0004 decides that\n GitHub is the operational source of truth and that each target repository keeps a\n `.shiki/` mirror. It says nothing about pull requests writing the mirror, does\n not name MergeGate, and does not make the mirror an input to a gate. Nothing in\n-ADR 0004's Decision or Consequences is amended, superseded or weakened: the\n+SADR-0004's Decision or Consequences is amended, superseded or weakened: the\n mirror still exists, still records the same artifacts, and pull request\n-boundaries remain the coordination surface. What this ADR settles is the question\n+boundaries remain the coordination surface. What this SADR settles is the question\n 0004 never reached \u2014 by what authority a pull request may write a mirror record \u2014\n and it answers it the way `.shiki/manifest.json` already declares\n (`pr_mutation: current-task-or-current-goal-only`).\n \n-It **amends ADR 0012**, whose Decision item 5 states that \"Loop-executed tasks\n+It **amends SADR-0012**, whose Decision item 5 states that \"Loop-executed tasks\n declare the `path:.shiki/**` lock, which covers every `.shiki` file the closeout\n stages ... so `files_outside_locks` is satisfied without per-file lock\n bookkeeping.\" That clause is replaced: the closeout's mirror writes are\n-authorized by record identity, not by a declared lock. ADR 0012's substantive\n+authorized by record identity, not by a declared lock. SADR-0012's substantive\n decision \u2014 that completion reaches `main` through a closeout pull request rather\n than local attestation \u2014 is preserved unchanged.\n \n-It is **complementary to ADR 0015**, not a substitute. ADR 0015 owns contract\n+It is **complementary to SADR-0015**, not a substitute. SADR-0015 owns contract\n immutability, the pre-dispatch gate, the Contract PR and the Scope Envelope. This\n-ADR owns what a lock means and who may write the mirror. The one ordering\n+SADR owns what a lock means and who may write the mirror. The one ordering\n constraint between them is that the lock-narrowing Spec Amendment must precede\n-ADR 0015 T1's freezing of the `locks` field.\n+SADR-0015 T1's freezing of the `locks` field.\n \n ## Alternatives Considered\n \n@@ -186,7 +186,7 @@\n the only writer that can register them may run. Migrating the Guardian signal to\n that store makes every registration PR fail closed to `needs_guardian` at every\n risk level, because the task file is by definition absent from the store, which\n-removes the low/medium autonomous path ADR 0008/0009 exists to provide. And it\n+removes the low/medium autonomous path SADR-0008/0009 exists to provide. And it\n leaves schemas, manifest, config, guardian policy, templates, migrations, plans,\n worktrees, runner records, runs and handoffs with no writer at all in a\n repository whose product is `.shiki`. Measured benefit on the frozen backlog:\n@@ -199,14 +199,14 @@\n namespace is not in fact ownership-partitioned \u2014 56 ledger entries carry no\n `task_id`, and one goal DAG is writable by 35 registered tasks. The correct\n expression of its insight is that locks do not govern the mirror at all, which is\n-what this ADR decides.\n+what this SADR decides.\n \n **Make the mirror a projection written only by a registration writer and a\n post-merge projection job.** Rejected. It requires a bypass actor with write\n access to the protected default branch; it makes its own master invariant\n readable from a manifest that pull requests may edit; it deletes both reconcile\n modes and leaves a corrupted mirror with no in-band repair path, contradicting\n-ADR 0004's consequence that mirror conflicts are repaired against GitHub state;\n+SADR-0004's consequence that mirror conflicts are repaired against GitHub state;\n and its content-derived ids are rejected by `validate_shiki`'s currently-green id\n pattern.\n \ndiff --git a/docs/adr/0017-bookkeeping-closeouts-do-not-inherit-risk.md b/docs/adr/SADR-0017-bookkeeping-closeouts-do-not-inherit-risk.md\nindex 554e3a0d526a72ca067108c5464cc68c3edcf174..b9676857a4713850e063af7c39c8e0b01ecb4c66\n--- a/docs/adr/0017-bookkeeping-closeouts-do-not-inherit-risk.md\n+++ b/docs/adr/SADR-0017-bookkeeping-closeouts-do-not-inherit-risk.md\n@@ -1,4 +1,4 @@\n-# ADR 0017: A Bookkeeping Closeout Does Not Inherit Its Task's Risk\n+# SADR-0017: A Bookkeeping Closeout Does Not Inherit Its Task's Risk\n \n ## Status\n \n@@ -6,7 +6,7 @@\n \n ## Context\n \n-ADR 0012 made the goal loop push a task's completion to the default branch through a\n+SADR-0012 made the goal loop push a task's completion to the default branch through a\n closeout pull request: after the implementation PR merges, the loop opens a second PR\n carrying `task.status = done`, `lock.state = released`, and \u2014 when the task completes\n its goal \u2014 `goal.status = complete` plus one scorecard report. Nothing else. The\n@@ -49,14 +49,14 @@\n 4. The task file's governance fields (`scope`, `non_goals`, `required_skills`,\n    `risk_level`, `locks`, `acceptance_checks`, `test_command`) are byte-identical to\n    the base snapshot. Only `status`, `expected_pr`, `closeout_pr`, `ledger_evidence`\n-   and `expected_branch` may differ \u2014 the ADR 0015 contract-immutability rule,\n+   and `expected_branch` may differ \u2014 the SADR-0015 contract-immutability rule,\n    unchanged.\n \n    `expected_branch` is in that set because a closeout is *by construction* on a\n    different branch from the implementation it closes, and the loop rewrites the\n    field accordingly. It is safe to let it differ because MergeGate independently\n    requires `expected_branch` to equal the PR's actual head ref, so a PR cannot set\n-   it to anything other than the branch it is really on. The first draft of this ADR\n+   it to anything other than the branch it is really on. The first draft of this SADR\n    omitted it, which made the exemption unreachable: every real closeout failed\n    condition 4 on a field the loop always changes. See PR #209.\n 5. The state transitions are exactly the terminal set: task `review` \u2192 `done`, lock\n@@ -113,12 +113,12 @@\n classification must be derived, not asserted.\n \n **Fold the closeout into the implementation PR** so no second PR exists. Rejected under\n-ADR 0012, which recorded why: a task file that is already `done` on the implementation\n+SADR-0012, which recorded why: a task file that is already `done` on the implementation\n branch short-circuits the loop's review \u2192 CCA \u2192 merge \u2192 done ordering into a local\n goal-completion before any merge happens.\n \n **Have the loop auto-merge closeouts with elevated credentials.** Rejected outright: that\n-is a runtime authoring its own approval, which ADR 0010 and the constitution's\n+is a runtime authoring its own approval, which SADR-0010 and the constitution's\n approval-record prohibition forbid. It would also be indistinguishable, in the evidence,\n from the forgery those rules exist to prevent.\n \ndiff --git a/docs/adr/0018-carried-guardian-approval-across-a-proven-base-sync.md b/docs/adr/SADR-0018-carried-guardian-approval-across-a-proven-base-sync.md\nindex 22358562da390ed8b45cd600da08a43d378b4680..e6e2907cefa776460af73ddf7d39db605f3c39b0\n--- a/docs/adr/0018-carried-guardian-approval-across-a-proven-base-sync.md\n+++ b/docs/adr/SADR-0018-carried-guardian-approval-across-a-proven-base-sync.md\n@@ -1,4 +1,4 @@\n-# ADR 0018: Carried Guardian Approval Across A Proven Base Sync\n+# SADR-0018: Carried Guardian Approval Across A Proven Base Sync\n \n ## Status\n \n@@ -34,7 +34,7 @@\n Guardian evaluator that consumes it (`shiki_guardian.evaluate_guardian_approval`,\n the `guardian_comment_carried` source) were built and tested in isolation. Neither\n does anything until a caller passes the flags that turn the carry on, so the\n-feature was invisible in CI. This ADR records the decision the two of them\n+feature was invisible in CI. This SADR records the decision the two of them\n implement, and this task wires it into the four CI invocations that judge Guardian\n approval.\n \ndiff --git a/docs/adr/README.md b/docs/adr/README.md\nindex c70e057a4520c36e042ab68c53cd4a21ef709b71..5da43856836e2ce6a150f8127766e2f88355ab4b\n--- a/docs/adr/README.md\n+++ b/docs/adr/README.md\n@@ -1,40 +1,41 @@\n-# Architecture Decision Records\n+# Decision Record Namespaces\n \n-This directory holds Shiki's Architecture Decision Records (ADRs): durable\n-records of hard-to-reverse platform decisions. ADRs are part of the Shiki source\n-of truth alongside GitHub state, the `.shiki/` mirror, and `CONTEXT.md`.\n+This directory holds two independent decision namespaces:\n \n-## How ADRs Work\n+- Shiki Architecture Decision Records (SADRs), named\n+  `SADR-NNNN-<short-slug>.md`, record hard-to-reverse Shiki platform decisions.\n+- Target Architecture Decision Records (ADRs), named\n+  `NNNN-<short-slug>.md`, belong to the target product.\n \n-- New ADRs are added as `docs/adr/NNNN-<short-slug>.md`, where `NNNN` is the next\n-  zero-padded sequence number.\n-- The **canonical list of ADRs is the set of files matching `docs/adr/*.md`** in\n-  this directory. Browse `docs/adr/*.md` for the authoritative, current set.\n-- Use [`template.md`](template.md) as the starting point for a new ADR.\n-- Record an ADR when a decision is hard to reverse, surprising without context,\n-  and a real tradeoff. Do not write an ADR for routine or easily reversible\n-  choices.\n+The same four-digit number may appear once in each namespace. A platform\n+`SADR-NNNN` citation resolves only to `SADR-NNNN-*.md`; a target `ADR NNNN`\n+citation resolves only to `NNNN-*.md`.\n \n-Using the `docs/adr/*.md` glob as the source of truth means other PRs can add an\n-ADR without editing this index file, avoiding merge contention on a\n-hand-maintained enumeration.\n+## How Decision Records Work\n \n-## Current ADRs (snapshot)\n-\n-The list below is a convenience snapshot and may lag behind the directory. When\n-in doubt, list `docs/adr/*.md`.\n+- New Shiki platform decisions use the next zero-padded SADR number.\n+- New target product decisions use the target's own ADR sequence.\n+- The canonical list is the files in `docs/adr/`; no hand-maintained index is\n+  authoritative.\n+- Use [`template.md`](template.md) as the starting point and choose the filename\n+  namespace that owns the decision.\n+- Record a decision when it is hard to reverse, surprising without context, and\n+  a real tradeoff. Do not write a decision record for routine or easily\n+  reversible choices.\n \n-- [0001 \u2014 Keep Shiki As The Platform Name](0001-keep-shiki-as-platform-name.md)\n-- [0002 \u2014 Use AGENTS As The Shared Agent Constitution](0002-use-agents-as-shared-constitution.md)\n-- [0003 \u2014 Require Skill Gate For MergeGate Readiness](0003-require-skill-gate-for-mergegate-readiness.md)\n-- [0004 \u2014 Use GitHub First With A Repository-Local Shiki Mirror](0004-use-github-first-with-shiki-mirror.md)\n-- [0005 \u2014 Use Codex As Front And Claude Code Action As The GitHub Runtime](0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md)\n-- [0006 \u2014 Separate Codex Implementation, CCA Completion, And MergeGate Authorization](0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md)\n+Using the directory as the source of truth lets independent changes add records\n+without contending on an enumerated index.\n \n-## Adding A New ADR\n+## Adding A Shiki SADR\n \n-1. Copy `template.md` to `docs/adr/NNNN-<short-slug>.md`.\n+1. Copy `template.md` to `docs/adr/SADR-NNNN-<short-slug>.md`.\n 2. Fill in the title, status, context, decision, and consequences.\n-3. Set the status to `Proposed`, then `Accepted` once the decision is approved.\n-4. Open a PR following `CONTRIBUTING.md`. You do not need to edit this index for\n-   the ADR to be discoverable via `docs/adr/*.md`.\n+3. Set the status to `Proposed`, then `Accepted` once the proper authority has\n+   approved the decision.\n+4. Open a PR following `CONTRIBUTING.md`.\n+\n+## Adding A Target ADR\n+\n+1. Copy `template.md` to `docs/adr/NNNN-<short-slug>.md` using the target's own\n+   sequence.\n+2. Cite it as `ADR NNNN`; do not prefix its filename with `SADR-`.\ndiff --git a/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md b/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\nnew file mode 100644\nindex 0000000000000000000000000000000000000000..7455ce1f3bed02ddddb851ac21a1d1a648c9aeba\n--- /dev/null\n+++ b/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\n@@ -0,0 +1,55 @@\n+# SADR-0019: Separate Shiki Platform SADRs From Target ADRs\n+\n+## Status\n+\n+Accepted\n+\n+## Context\n+\n+Shiki installs its platform decision records into `docs/adr/` in every target\n+repository. Shiki and its targets previously used the same numeric\n+`NNNN-<slug>.md` namespace. A target that already owned ADRs 0013 through 0018\n+therefore acquired two records for each number during installation, making an\n+`ADR NNNN` citation ambiguous. Renumbering target decisions would rewrite target\n+history, while renumbering Shiki decisions per target would destroy their stable\n+platform identity.\n+\n+Upgrades also need to remove the former Shiki-owned numeric paths without ever\n+mistaking a target-owned ADR for platform content. The install stamp already\n+records exact shipped paths and content digests, so it can prove ownership only\n+when both still match.\n+\n+## Decision\n+\n+Shiki platform decisions use the `SADR` namespace:\n+\n+- records are named `docs/adr/SADR-NNNN-<slug>.md`;\n+- live platform citations use `SADR-NNNN`;\n+- target product decisions retain `docs/adr/NNNN-<slug>.md` and `ADR NNNN`;\n+- the reference validator resolves the two namespaces independently, so the same\n+  four-digit number may exist once in each namespace.\n+\n+Before a forced upgrade writes anything, the installer inventories the exact\n+former numeric Shiki ADR paths. It may delete a legacy path only when the\n+existing install stamp contains that exact path and its recorded SHA-256 digest\n+matches the file currently on disk. All ownership blockers are reported\n+together and abort the upgrade before its first write. A non-forced install\n+never performs this cleanup.\n+\n+Historical `.shiki/` evidence remains unchanged because it records the namespace\n+that existed when that evidence was produced.\n+\n+## Consequences\n+\n+- Targets may use any ADR number without colliding with Shiki's platform SADRs.\n+- Fresh installs preserve target ADR bytes and install a stable SADR set.\n+- Forced upgrades fail closed when legacy ownership cannot be proven, and a\n+  successful upgrade refreshes the stamp without removed legacy paths.\n+- Current governance, packaging, workflow, prompt, script, and skill references\n+  must move with the platform records.\n+\n+## Alternatives Considered\n+\n+- Collision-only refusal leaves affected targets permanently unupgradable.\n+- Renumbering target ADRs mutates target-owned history.\n+- Per-target Shiki renumbering makes platform citations non-portable.\ndiff --git a/docs/adr/template.md b/docs/adr/template.md\nindex a92a97daddd0b00ab9dd626cc8d86942da2eafed..17dd79175b5fee2105935557f691b40cf9492942\n--- a/docs/adr/template.md\n+++ b/docs/adr/template.md\n@@ -1,14 +1,14 @@\n-# NNNN: <Short Decision Title>\n+# <SADR-NNNN or NNNN>: <Short Decision Title>\n \n ## Status\n \n-Proposed | Accepted | Superseded by ADR-NNNN\n+Proposed | Accepted | Superseded by SADR-NNNN or target ADR NNNN\n \n ## Context\n \n What is the situation, constraint, or force that requires a decision? Reference\n-relevant domain terms (`CONTEXT.md`), prior ADRs, and code reality. State the\n-problem, not the solution.\n+relevant domain terms (`CONTEXT.md`), prior Shiki SADRs or target ADRs, and code\n+reality. State the problem, not the solution.\n \n ## Decision\n \ndiff --git a/docs/agents/autonomous-loop-e2e-acceptance.md b/docs/agents/autonomous-loop-e2e-acceptance.md\nindex bbf84c6081987f159ac6ad9ecf3c82b7aa72e7e8..5c13c1d02a05a90b0dc46cbef7c95a9846d0c237\n--- a/docs/agents/autonomous-loop-e2e-acceptance.md\n+++ b/docs/agents/autonomous-loop-e2e-acceptance.md\n@@ -40,8 +40,8 @@\n 3. `gh auth status` is logged in with merge permission on the repo.\n 4. Branch protection + required checks are configured exactly as the goal-loop\n    required-check set expects (see `docs/agents/branch-protection-smoke.md`).\n-5. The repo `.shiki/policy` permits autonomous low/medium-risk merges (ADR\n-   0008/0009); the MergeGate policy check is the recorded-authority gate.\n+5. The repo `.shiki/policy` permits autonomous low/medium-risk merges\n+   (SADR-0008/0009); the MergeGate policy check is the recorded-authority gate.\n \n The operator's only actions are: register the task and start the loop. **No\n manual pre-clearing** means after `shiki loop run` starts, the operator does not\ndiff --git a/docs/agents/bootstrap-command.md b/docs/agents/bootstrap-command.md\nindex 05f7f203a52cf412b9f97084a6e131ed0f785afd..f7aa0b734dfebee4a3b139335b66ee3415f48713\n--- a/docs/agents/bootstrap-command.md\n+++ b/docs/agents/bootstrap-command.md\n@@ -189,7 +189,9 @@\n ```\n \n Use this only for tests, fixtures, or explicit local-only template inspection.\n-Use `--force` only when you intentionally want to overwrite existing target files.\n+Use `--force` only when you intentionally want to refresh the target template.\n+Legacy numeric Shiki decision cleanup is stamp-and-digest authorized before any\n+write; target ADR paths are not cleanup candidates.\n \n ## Slash Command\n \ndiff --git a/docs/agents/checklists.md b/docs/agents/checklists.md\nindex 4a7f03ade346f53a1c1d82981f3fd744f9ab4547..0c5ec6c53dc7197b7b7a7d18e667b325babd6c45\n--- a/docs/agents/checklists.md\n+++ b/docs/agents/checklists.md\n@@ -32,18 +32,18 @@\n | ID | Item | Blocking |\n | --- | --- | --- |\n | GD-01 | Plan was challenged against `CONTEXT.md`. | blocking for non-trivial Goals |\n-| GD-02 | Relevant ADRs were read or absence was recorded. | blocking for non-trivial Goals |\n+| GD-02 | Relevant SADRs and target ADRs were read or absence was recorded. | blocking for non-trivial Goals |\n | GD-03 | Ambiguous terms were resolved or converted to HITL blockers. | blocking |\n | GD-04 | Edge scenarios were considered. | advisory |\n | GD-05 | Code reality was checked when code could answer. | blocking when applicable |\n | GD-06 | New domain terms were added to `CONTEXT.md` when needed. | blocking when applicable |\n-| GD-07 | ADR was created or explicitly skipped using the ADR criteria. | blocking when applicable |\n+| GD-07 | A SADR or target ADR was created or explicitly skipped using the decision-record criteria. | blocking when applicable |\n \n ## CI \u2014 Context & Impact Checklist\n \n | ID | Item | Blocking |\n | --- | --- | --- |\n-| CI-01 | Relevant docs and ADRs identified. | blocking |\n+| CI-01 | Relevant docs, SADRs, and target ADRs identified. | blocking |\n | CI-02 | Relevant modules, interfaces, seams, callers, and tests identified. | blocking |\n | CI-03 | Dependencies and blocked-by relationships identified. | blocking |\n | CI-04 | Candidate locks identified. | blocking |\n@@ -62,7 +62,7 @@\n | PRD-04 | Implementation decisions are recorded without volatile code detail. | blocking |\n | PRD-05 | Testing decisions are recorded. | blocking |\n | PRD-06 | Out-of-scope items are explicit. | blocking |\n-| PRD-07 | Relevant glossary/ADR links are present when applicable. | blocking |\n+| PRD-07 | Relevant glossary/SADR/target-ADR links are present when applicable. | blocking |\n \n ## SF \u2014 Spec Freeze Checklist\n \n@@ -98,7 +98,7 @@\n | H-02 | Branch/worktree target is present. | blocking |\n | H-03 | Scope and non-goals are present. | blocking |\n | H-04 | Dependencies and locks are present. | blocking |\n-| H-05 | Relevant docs, ADRs, modules, and tests are referenced. | blocking |\n+| H-05 | Relevant docs, SADRs, target ADRs, modules, and tests are referenced. | blocking |\n | H-06 | Required skills are stated. | blocking |\n | H-07 | TDD expectations are stated. | blocking when behavior work |\n | H-08 | Acceptance criteria are included. | blocking |\ndiff --git a/docs/agents/codex-handoff.md b/docs/agents/codex-handoff.md\nindex ede468f47cb277f69a857e8c45df703e4ed0151d..6f97bdffb67e44d7edea2c71e01ea28933c4eb6c\n--- a/docs/agents/codex-handoff.md\n+++ b/docs/agents/codex-handoff.md\n@@ -46,7 +46,7 @@\n \n ## Context\n - Domain terms:\n-- Relevant ADRs:\n+- Relevant SADRs and target ADRs:\n - Relevant docs:\n - Relevant modules/interfaces/seams:\n - Similar tests/prior art:\ndiff --git a/docs/agents/completion-check-agent.md b/docs/agents/completion-check-agent.md\nindex 13e4c035db35025f4e3cdcc5adfb24314ac4d521..61bfa1218cf36ca605250c91919e620f33238aa0\n--- a/docs/agents/completion-check-agent.md\n+++ b/docs/agents/completion-check-agent.md\n@@ -32,7 +32,7 @@\n - `AGENTS.md`\n - `CLAUDE.md`\n - `CONTEXT.md`\n-- relevant ADRs\n+- relevant SADRs and target ADRs\n - parent Goal issue\n - PRD issue or document\n - task issue\ndiff --git a/docs/agents/decision-control.md b/docs/agents/decision-control.md\nindex 7d8ee5ab03a5ccbcbd04056ae61de03785a2a9c9..8c94ecda00fffaa0e92c325ab83aa9776bd1dac1\n--- a/docs/agents/decision-control.md\n+++ b/docs/agents/decision-control.md\n@@ -11,7 +11,7 @@\n | Decision | Owner | When | Required inputs | Output | Enforcement surface |\n | --- | --- | --- | --- | --- | --- |\n | Is the Goal clear enough to plan? | Planner + Guardian when needed | Goal intake | User request, existing docs, risk signals | Goal readiness verdict | Goal checklist, issue template, ledger |\n-| Must `grill-with-docs` run? | Planner / Skill Gate | Before PRD or issue split | Ambiguity, domain terms, ADR conflicts, unclear boundaries | Skill invocation or explicit skip rationale | Skill Gate checklist |\n+| Must `grill-with-docs` run? | Planner / Skill Gate | Before PRD or issue split | Ambiguity, domain terms, SADR/target-ADR conflicts, unclear boundaries | Skill invocation or explicit skip rationale | Skill Gate checklist |\n | Is a PRD required? | Planner | After Goal context stabilizes | Goal, `grill-with-docs` output, Context & Impact output | PRD required / not required | PRD checklist, CCA evidence check |\n | Can the spec be frozen? | Operator only | End of Requirements Definition, before issue split | PRD/requirements summary, scope inventory (SF-02), open-question list | `spec_freeze` block in the plan + ledger evidence | `require_grilled_plan` (plan ingest/run), SF checklist |\n | Can a frozen spec change? | Operator only (Spec Amendment) | When implementation contradicts the frozen spec | Contested decisions, scoped re-grill result | recorded amendment + re-stamped freeze | SF checklist, ledger evidence |\ndiff --git a/docs/agents/domain.md b/docs/agents/domain.md\nindex 258da8ea81e87ac0a6b0b5b5e5c2fde980eaabb7..d57b0cf17ff3e1fa9e6e5885372bf083d8295421\n--- a/docs/agents/domain.md\n+++ b/docs/agents/domain.md\n@@ -5,7 +5,9 @@\n ## Layout\n \n - `CONTEXT.md` defines Shiki language. Keep it implementation-free.\n-- `docs/adr/` records hard-to-reverse decisions.\n+- `docs/adr/SADR-NNNN-*.md` records Shiki platform decisions.\n+- `docs/adr/NNNN-*.md` records target product decisions without sharing the\n+  SADR namespace.\n - `AGENTS.md` is the runtime-neutral agent constitution.\n - `CLAUDE.md` is the Claude Code wrapper around `AGENTS.md`.\n - `.shiki/` is the Target Repository mirror for Goals, PRDs, plans, Task DAGs, contracts, locks, worktree records, ledger entries, CCA verdicts, repair packets, reports, and handoffs.\n@@ -15,6 +17,6 @@\n \n - Use Shiki terms exactly when they exist.\n - Add glossary terms to `CONTEXT.md` only when the term is part of the domain, not a generic programming concept.\n-- Create ADRs only for hard-to-reverse, surprising, tradeoff-based decisions.\n+- Create SADRs or target ADRs only for hard-to-reverse, surprising, tradeoff-based decisions.\n - Keep product-specific language in the Target Repository, not in Shiki Template docs.\n-- If an ADR conflicts with a proposed implementation, surface the conflict before editing.\n+- If a SADR or target ADR conflicts with a proposed implementation, surface the conflict before editing.\ndiff --git a/docs/agents/execution-control.md b/docs/agents/execution-control.md\nindex ad8ee3cdb36be56fedc1f46408ca92b387d74047..bc63c3a73661d12b567016ba299f1a6b66bf9088\n--- a/docs/agents/execution-control.md\n+++ b/docs/agents/execution-control.md\n@@ -63,7 +63,7 @@\n | Branch name | Orchestrator | Orchestrator | Before implementer dispatch | branch policy | branch ref in task/ledger |\n | Lock reservation | Context & Impact | MergeGate | Before dispatch and before merge | `.shiki/locks/`, labels, PR metadata | lock id, owner, scope |\n | CI profile | Planner | MergeGate | During issue triage, rechecked at PR | labels, `.shiki/policy.yaml`, workflow matrix | required check list |\n-| Required status checks | Repo maintainer / Guardian | GitHub branch protection / rulesets | Repository setup, updated via ADR | GitHub settings | branch/ruleset config, ADR |\n+| Required status checks | Repo maintainer / Guardian | GitHub branch protection / rulesets | Repository setup, updated via target ADR | GitHub settings | branch/ruleset config, target ADR |\n | CCA judgment | CCA | GitHub required check / MergeGate | Every non-draft PR update | GitHub Action check | structured CCA verdict |\n | Repair assignment | CCA | MergeGate | After failed CCA/CI/review | repair packet, issue/PR comment | repair id and attempt count |\n | Merge readiness | MergeGate | GitHub branch protection / Guardian if needed | After CCA complete | required checks, review rules, rulesets | mergegate check output |\ndiff --git a/docs/agents/guardian-policy.md b/docs/agents/guardian-policy.md\nindex 4147c7adf05a47adb4fd24ada1a1c2e1f2ba6d2d..a20d9cc6c00a569bb057b9ca8d6b51c7fc4a9847\n--- a/docs/agents/guardian-policy.md\n+++ b/docs/agents/guardian-policy.md\n@@ -98,8 +98,8 @@\n `origin/<default_branch>` and reuses each job's existing base fetch; a shallow\n (`--depth`) or second base fetch would make the proof unresolvable and silently\n disable the carry, so neither is permitted. CCA reads `guardian_comment_carried` as\n-a recorded authority in the deterministic signal and does not re-derive it. See ADR\n-0018 for the full decision, the rejected diff/patch-equality and path-disjointness\n+a recorded authority in the deterministic signal and does not re-derive it. See\n+SADR-0018 for the full decision, the rejected diff/patch-equality and path-disjointness\n alternatives, and the accepted residual.\n \n ## External AI Guardian Review (SADR-0010)\ndiff --git a/docs/agents/implementation-policy.md b/docs/agents/implementation-policy.md\nindex c754144a9fb5bf4a38aa82a60e1670c59e626b44..85c37281bcad196c6b7b02343faf00b03cd9c57a\n--- a/docs/agents/implementation-policy.md\n+++ b/docs/agents/implementation-policy.md\n@@ -31,10 +31,13 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n ## Non-Negotiables\n \n 1. **Planning precedes implementation.** Non-trivial Goals start with `grill-with-docs` and Context & Impact.\n-2. **Domain language is controlled.** Terms must be checked against `CONTEXT.md`; hard-to-reverse tradeoffs must be checked against ADRs.\n+2. **Domain language is controlled.** Terms must be checked against `CONTEXT.md`; hard-to-reverse tradeoffs must be checked against Shiki SADRs and target ADRs.\n 3. **PRD precedes issue decomposition.** `to-prd` is used once the context is settled enough to become durable intent.\n 4. **Issues are vertical slices.** `to-issues` must produce independently grabbable tracer-bullet tasks, not horizontal layer tickets.\n 5. **Triage controls dispatch.** Only `ready-for-agent` / AFK tasks may be assigned to Codex without human input.\n@@ -95,19 +98,19 @@\n Process:\n \n - Challenge terms against `CONTEXT.md`.\n-- Check ADRs before reopening decisions.\n+- Check SADRs and target ADRs before reopening decisions.\n - Inspect code when code can answer a question.\n - Ask one question at a time when the operator is available.\n - Provide a recommended answer with each question.\n - Record resolved terms in `CONTEXT.md`.\n-- Create ADRs only for hard-to-reverse, surprising, tradeoff-based decisions.\n+- Create SADRs or target ADRs only for hard-to-reverse, surprising, tradeoff-based decisions.\n - Convert unresolved load-bearing questions into HITL blockers.\n \n Outputs:\n \n - Resolved design-tree decisions.\n - Updated glossary terms when needed.\n-- ADRs when justified.\n+- SADRs or target ADRs when justified.\n - HITL blockers when decisions remain unresolved.\n \n ## Phase 3 \u2014 Context & Impact\n@@ -117,7 +120,7 @@\n \n Outputs must include:\n \n-- Relevant documents and ADRs.\n+- Relevant documents, SADRs, and target ADRs.\n - Relevant modules, interfaces, seams, callers, and tests.\n - Candidate locks.\n - Dependencies.\n@@ -140,7 +143,7 @@\n - Testing decisions.\n - Out of scope.\n - Further notes.\n-- Links to relevant Goal, domain terms, and ADRs.\n+- Links to relevant Goal, domain terms, SADRs, and target ADRs.\n \n Phase 4 ends with Spec Freeze: the operator explicitly approves the PRD, the\n plan records a `spec_freeze` block (status, approved_by, source), and the SF\n@@ -240,7 +243,7 @@\n - Dependencies and locks.\n - Risk and Guardian requirements.\n - Ledger completeness.\n-- Documentation and ADR updates.\n+- Documentation and SADR/target-ADR updates.\n - Scope drift.\n \n CCA emits one of:\n@@ -301,7 +304,7 @@\n | Evidence failure | Behavior might be done, but proof is missing. | Add checks, PR evidence, ledger entry, or artifacts. |\n | Scope drift | PR changes unrelated behavior. | Revert unrelated changes or split into new Goal. |\n | Dependency block | Upstream task or lock not complete. | Block until dependency resolves. |\n-| Architecture block | Decision needs ADR or deep-module discussion. | `grill-with-docs` or `improve-codebase-architecture`. |\n+| Architecture block | Decision needs a SADR, target ADR, or deep-module discussion. | `grill-with-docs` or `improve-codebase-architecture`. |\n | Risk block | Security/production/policy change needs human approval. | Guardian decision. |\n \n ## Phase 5.5 \u2014 Execution Decision Control\ndiff --git a/docs/agents/runtime-auth-model.md b/docs/agents/runtime-auth-model.md\nindex 6a2e1a47e81ea89b5c68a210ec07772a9374babc..3c54748d24233dbd0ba8473950282547ef4ce935\n--- a/docs/agents/runtime-auth-model.md\n+++ b/docs/agents/runtime-auth-model.md\n@@ -148,7 +148,7 @@\n \n API-key based runners may be added by a target repository as an explicit extension. They are not the default Shiki template path.\n \n-If a repo opts into API-key mode, record the exception in an ADR and update the repo's `AGENTS.md`, `.shiki/config.yaml`, and workflow permissions.\n+If a repo opts into API-key mode, record the target-specific exception in a target ADR and update the repo's `AGENTS.md`, `.shiki/config.yaml`, and workflow permissions.\n \n ## GitHub Actions Runtime Compatibility\n \ndiff --git a/docs/agents/skill-gate.md b/docs/agents/skill-gate.md\nindex c50398b0d7c60a8598f2a5c88a10015730dced13..3acffe6444617bb1d54b77d4b4708158ec735bc6\n--- a/docs/agents/skill-gate.md\n+++ b/docs/agents/skill-gate.md\n@@ -9,7 +9,7 @@\n | Skill | Trigger | Expected output |\n | --- | --- | --- |\n | `setup-matt-pocock-skills` | First configuring a repo for these skills, or when issue tracker, triage labels, or domain docs are missing. | `docs/agents/` configuration and an `Agent skills` block in `AGENTS.md` or `CLAUDE.md`. |\n-| `grill-with-docs` | Ambiguous plan, unclear terminology, boundary decision, tradeoff, or ADR-worthy design choice. | Clarified decision tree, recommended answers, updates to `CONTEXT.md` or ADRs when decisions crystallize. |\n+| `grill-with-docs` | Ambiguous plan, unclear terminology, boundary decision, tradeoff, or decision-record-worthy design choice. | Clarified decision tree, recommended answers, updates to `CONTEXT.md`, SADRs, or target ADRs when decisions crystallize. |\n | `zoom-out` | Agent lacks a higher-level architectural map. | Map of relevant modules, callers, seams, and domain concepts. |\n | `to-prd` | Settled context should become a PRD. | PRD published to the issue tracker with problem, solution, user stories, decisions, testing, and out-of-scope notes. |\n | `to-issues` | Goal, PRD, or plan must become implementation tickets. | Vertical-slice issues with acceptance criteria, dependencies, and AFK/HITL classification. |\n@@ -17,7 +17,7 @@\n | `tdd` | Feature work or bug fix has observable behavior that can be specified with tests. | Red-green-refactor loop through public interfaces, one vertical slice at a time. |\n | `code-review` | The TDD loop is green and the implementer is about to create or update a PR. Default for every implementation task. | Pre-PR self-review of the task diff with fixes applied on the task branch; findings and resolutions recorded as a ledger entry naming the skill and a `## Pre-PR code review` PR body section. |\n | `diagnose` | Hard bug, failing check, regression, flaky behavior, or performance problem. | Reproduction loop, ranked hypotheses, instrumentation, fix, regression test, post-mortem. |\n-| `improve-codebase-architecture` | Structural friction, testability, AI-navigability, deep-module opportunity, or refactor request. | Architecture candidates and deepening recommendations informed by `CONTEXT.md` and ADRs. |\n+| `improve-codebase-architecture` | Structural friction, testability, AI-navigability, deep-module opportunity, or refactor request. | Architecture candidates and deepening recommendations informed by `CONTEXT.md`, SADRs, and target ADRs. |\n | `prototype` | A throwaway logic or UI prototype is needed to answer a design question. | Clearly marked disposable prototype and captured decision. |\n \n ## MergeGate Interaction\ndiff --git a/docs/operations.md b/docs/operations.md\nindex cf1f452d4c589e38ccd58553bda0ad8684b51eb7..ace48a90ffbec9e5c0a7113bef16a4858bc44b3f\n--- a/docs/operations.md\n+++ b/docs/operations.md\n@@ -89,6 +89,16 @@\n      target history, brought forward by `shiki migrate`, not a template.\n    - **Everything else** (CLI scripts, workflows, schemas, docs) is overwritten.\n \n+   A forced upgrade also migrates the former numeric Shiki decision paths to\n+   `SADR-NNNN-*`. Before the first write, the installer inventories the exact\n+   legacy Shiki paths and authorizes each deletion only when the existing\n+   `.shiki/install-stamp.json` names that path and its SHA-256 digest still\n+   matches the file. An absent, unreadable, malformed, incomplete, or\n+   mismatching stamp reports every blocked path and stops with no filesystem\n+   change. Target-owned `NNNN-*` ADRs are not cleanup candidates. A non-force\n+   install deletes nothing and retains any still-matching legacy stamp entries\n+   so a later forced upgrade can prove ownership.\n+\n    If the target has pending migrations, `--force` refuses and writes nothing \u2014\n    apply migrations first (step 3), then re-run. The run ends with a single\n    summary of every `.new` written, so a half-upgrade is visible.\ndiff --git a/docs/releasing.md b/docs/releasing.md\nindex 10d73828e7dbd64e9d9f6b83bc84d010537ce62a..aafeee8c4d441e1e427efee2ce4f81a647f020e1\n--- a/docs/releasing.md\n+++ b/docs/releasing.md\n@@ -67,7 +67,7 @@\n CLI in `scripts/`. There are no third-party Python runtime dependencies. The CLI\n is intentionally **not** exposed as a `[project.scripts]` console entry point\n because `scripts/shiki.py` is a dependency-free shim and `scripts/` is not\n-packaged as an importable distribution (see the ADR for the rationale).\n+packaged as an importable distribution (see SADR-0007 for the rationale).\n \n Supported install paths:\n \n@@ -155,6 +155,15 @@\n    [docs/operations.md](operations.md#the-new-convention). If the target has\n    pending migrations, `--force` refuses and writes nothing; apply migrations\n    (step 2) first, then re-run.\n+\n+   The SADR namespace upgrade has an additional pre-write gate: each former\n+   numeric Shiki decision path is removed only when the existing install stamp\n+   contains that exact path and a digest matching its current bytes. The\n+   installer aggregates absent/unreadable/malformed-stamp, missing-digest, and\n+   digest-mismatch blockers before raising, so refusal leaves the target tree\n+   byte-identical. Numeric target ADRs are preserved. A successful forced\n+   upgrade installs and stamps `SADR-*` paths and drops the removed legacy paths\n+   from the refreshed stamp.\n 2. Apply repository-local `.shiki/` state migrations with the migrate command:\n \n    ```bash\ndiff --git a/docs/usage.md b/docs/usage.md\nindex b83407ee3970dd395dd893010bc5dbe8bad26051..a130ef6e40dfd8169a5bc701339351097df1a09f\n--- a/docs/usage.md\n+++ b/docs/usage.md\n@@ -67,8 +67,10 @@\n shiki install-target /path/to/target-repo --local-only\n ```\n \n-Use `--force` only when you intentionally want to overwrite existing target\n-files.\n+Use `--force` only when you intentionally want to refresh the target template.\n+For the SADR namespace migration, `--force` deletes only exact legacy Shiki\n+decision paths whose existing install-stamp digest still matches; any ownership\n+blocker stops before the first write. Numeric target ADRs are never candidates.\n \n ## `shiki bootstrap-platform` \u2014 publish the Shiki platform repo\n \ndiff --git a/scripts/guardian_approval_signal.py b/scripts/guardian_approval_signal.py\nindex d059f92b2c82aa2b5d6ca421365537a87886c1cb..16924892714f4f5b9303c0ef0e325a7e70075630\n--- a/scripts/guardian_approval_signal.py\n+++ b/scripts/guardian_approval_signal.py\n@@ -199,8 +199,8 @@\n ) -> Any:\n     \"\"\"Evaluate SADR-0015 Contract Approval for the CCA Guardian signal.\n \n-    The SAME OR that MergeGate applies must reach this deterministic signal: ADR\n-    0015 is explicit that without it, a contract-approved PR still returns\n+    The SAME OR that MergeGate applies must reach this deterministic signal:\n+    SADR-0015 is explicit that without it, a contract-approved PR still returns\n     ``needs_guardian``, the CCA job fails, and the MergeGate policy check never\n     runs \u2014 the gate this change moves would never be reached. This resolves the\n     task the SAME way MergeGate does (the first Shiki task id in the PR body),\ndiff --git a/scripts/shiki_contract_approval.py b/scripts/shiki_contract_approval.py\nindex 941bcb96fdd8b9a85bc0c4f0f04d5dad9d04839c..4a4b418f6527413161db82670bec857a043360e2\n--- a/scripts/shiki_contract_approval.py\n+++ b/scripts/shiki_contract_approval.py\n@@ -142,7 +142,7 @@\n     changed_files_status: Any,\n     registration: Any,\n ) -> ContractApprovalResult:\n-    \"\"\"Whether this PR carries an SADR-0015 Contract Approval.\n+    \"\"\"Whether this PR carries a SADR-0015 Contract Approval.\n \n     The four conditions are evaluated IN ORDER, each failure recorded as a\n     distinct reason string:\ndiff --git a/scripts/shiki_guardian.py b/scripts/shiki_guardian.py\nindex a4ecb79bbda55fd5c8b06adfae2eb99760a03c45..4dd99bbe714e26f421fa19abac7a34ae1b195ecc\n--- a/scripts/shiki_guardian.py\n+++ b/scripts/shiki_guardian.py\n@@ -12,7 +12,7 @@\n # The carry re-derives its proof from git in-process on every judging invocation;\n # it is never read from an artifact, so there is nothing to go stale or be forged.\n # shiki_sync_proof is the ONLY guardian-family module that runs git, and every one\n-# of its invocations is read-only and local (ADR: prove-from-git base sync).\n+# of its invocations is read-only and local (SADR: prove-from-git base sync).\n from shiki_sync_proof import verify_pure_base_sync\n \n GUARDIAN_POLICY_PATH = \".shiki/guardian-policy.json\"\ndiff --git a/scripts/shiki_guardian_review.py b/scripts/shiki_guardian_review.py\nindex dd0f104d457899e0ba5ff1e162011c1e90b6f92b..e10ef58c046324e7ea3a3392feb3048ca77414ce\n--- a/scripts/shiki_guardian_review.py\n+++ b/scripts/shiki_guardian_review.py\n@@ -159,7 +159,7 @@\n     docs_only = _is_docs_only(changed_files)\n     if docs_only and matched == 0:\n         focus.append(\n-            \"Docs/ADR-only change: confirm the change is genuinely documentation/decision-only, introduces no behavior or contract drift, and stays consistent with the constitution and existing ADRs.\"\n+            \"Docs/decision-record-only change: confirm the change is genuinely documentation/decision-only, introduces no behavior or contract drift, and stays consistent with the constitution and existing SADRs and target ADRs.\"\n         )\n \n     # Conservative augmentation: unknown (no category and not docs-only), mixed\ndiff --git a/scripts/shiki_installer.py b/scripts/shiki_installer.py\nindex be064ab7a29ff6836f7f7a1d70500a43e0d11d7b..9fd8eefd137ce7003dad5c0dcb2e48e2cd47ab32\n--- a/scripts/shiki_installer.py\n+++ b/scripts/shiki_installer.py\n@@ -118,6 +118,7 @@\n # what lets a target carry it without a governance-schema change.\n INSTALL_STAMP_PATH = \".shiki/install-stamp.json\"\n INSTALL_STAMP_VERSION = 1\n+LEGACY_SHIKI_SADR_MAX = 18\n \n # Under --force the shipped surface splits three ways, so nothing is lost and\n # nothing is silently stale.\n@@ -237,9 +238,98 @@\n         )\n \n \n+def legacy_shiki_adr_paths() -> tuple[str, ...]:\n+    \"\"\"Former numeric paths for Shiki SADR-0001 through SADR-0018.\n+\n+    The authoritative slugs come from the current SADR filenames. SADR-0019 is\n+    the namespace decision itself and never shipped under a numeric legacy path.\n+    \"\"\"\n+    paths: list[str] = []\n+    for sadr in sorted((ROOT / \"docs\" / \"adr\").glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\")):\n+        number = sadr.name.split(\"-\", 2)[1]\n+        if int(number) <= LEGACY_SHIKI_SADR_MAX:\n+            paths.append(f\"docs/adr/{sadr.name.removeprefix('SADR-')}\")\n+    return tuple(paths)\n+\n+\n+def preflight_legacy_sadr_cleanup(target: Path, *, force: bool) -> tuple[str, ...]:\n+    \"\"\"Authorize exact legacy Shiki ADR removals before the first write.\n+\n+    A non-force install never removes existing files. A forced install needs no\n+    stamp when no legacy Shiki path is present. When legacy paths do exist, each\n+    must be named in a readable install stamp and its current bytes must match\n+    the recorded digest.\n+    \"\"\"\n+    if not force:\n+        return ()\n+    present = tuple(\n+        relative\n+        for relative in legacy_shiki_adr_paths()\n+        if (target / relative).exists() or (target / relative).is_symlink()\n+    )\n+    if not present:\n+        return ()\n+\n+    stamp_path = target / INSTALL_STAMP_PATH\n+    try:\n+        raw = stamp_path.read_text(encoding=\"utf-8\")\n+        stamp = json.loads(raw)\n+    except FileNotFoundError:\n+        detail = \"install stamp is absent\"\n+        blockers = [f\"{relative}: {detail}\" for relative in present]\n+    except (OSError, UnicodeDecodeError) as error:\n+        detail = f\"install stamp is unreadable ({error})\"\n+        blockers = [f\"{relative}: {detail}\" for relative in present]\n+    except json.JSONDecodeError as error:\n+        detail = f\"install stamp is malformed ({error})\"\n+        blockers = [f\"{relative}: {detail}\" for relative in present]\n+    else:\n+        if not isinstance(stamp, dict):\n+            blockers = [\n+                f\"{relative}: install stamp is malformed (expected an object)\"\n+                for relative in present\n+            ]\n+        else:\n+            digests = stamp.get(\"digests\")\n+            blockers = []\n+            if not isinstance(digests, dict):\n+                blockers.extend(\n+                    f\"{relative}: install stamp has no digest for this path\"\n+                    for relative in present\n+                )\n+            else:\n+                for relative in present:\n+                    expected = digests.get(relative)\n+                    if not isinstance(expected, str) or not expected:\n+                        blockers.append(\n+                            f\"{relative}: install stamp has no digest for this path\"\n+                        )\n+                        continue\n+                    try:\n+                        actual = _sha256_file(target / relative)\n+                    except OSError as error:\n+                        blockers.append(\n+                            f\"{relative}: legacy path is unreadable ({error})\"\n+                        )\n+                        continue\n+                    if actual != expected:\n+                        blockers.append(\n+                            f\"{relative}: digest mismatch (stamp {expected}, current {actual})\"\n+                        )\n+\n+    if blockers:\n+        joined = \"\\n\".join(f\"- {blocker}\" for blocker in blockers)\n+        raise ShikiError(\n+            \"legacy Shiki ADR cleanup is not authorized:\\n\"\n+            f\"{joined}\\nNo files were written.\"\n+        )\n+    return present\n+\n+\n def install_template(target: Path, *, force: bool, validate: bool) -> list[NewFileNote]:\n     # Preconditions first: never rewrite the tree of a target that cannot be\n     # upgraded cleanly (see refuse_pending_migrations).\n+    legacy_cleanup = preflight_legacy_sadr_cleanup(target, force=force)\n     refuse_pending_migrations(target)\n \n     # Resolve the target's required CODEOWNERS owner once from its\n@@ -250,6 +340,10 @@\n     # verbatim. See ``codeowners_required_owner``.\n     codeowners_owner = codeowners_required_owner(target)\n \n+    for relative in legacy_cleanup:\n+        (target / relative).unlink()\n+        info(f\"removed stamped legacy Shiki ADR: {target / relative}\")\n+\n     notes: list[NewFileNote] = []\n     for relative in TEMPLATE_PATHS:\n         source = ROOT / relative\n@@ -647,6 +741,22 @@\n     digests = target_stamp_digests(target)\n     existing = read_install_stamp(target)\n     if existing is not None:\n+        # A non-force install deliberately leaves legacy Shiki ADRs in place.\n+        # Preserve only still-matching ownership proof so a later forced\n+        # upgrade can authorize cleanup; never carry a missing or stale digest.\n+        existing_digests = existing.get(\"digests\")\n+        if isinstance(existing_digests, dict):\n+            for relative in legacy_shiki_adr_paths():\n+                expected = existing_digests.get(relative)\n+                path = target / relative\n+                if not isinstance(expected, str) or not path.is_file():\n+                    continue\n+                try:\n+                    actual = _sha256_file(path)\n+                except OSError:\n+                    continue\n+                if actual == expected:\n+                    digests[relative] = actual\n         if _incoming_is_older(commit, existing.get(\"platform_commit\")):\n             warn(\n                 \"kept existing install stamp: incoming install \"\ndiff --git a/scripts/shiki_loop.py b/scripts/shiki_loop.py\nindex 3611641da8aeb5038b1da90e3114b09a473ec6d8..84caa491b9cad0edc4176f209c13e9221e975681\n--- a/scripts/shiki_loop.py\n+++ b/scripts/shiki_loop.py\n@@ -424,7 +424,7 @@\n         # unresolvable CCA verdict has already produced its terminal stop and is never\n         # laundered into a head-moving sync (SADR-0010). It also never fires while the\n         # policy (Guardian) gate is red \u2014 for ANY reason, not only when it is the sole\n-        # red check \u2014 so a head-bound Guardian approval, or an SADR-0015\n+        # red check \u2014 so a head-bound Guardian approval, or a SADR-0015\n         # contract-approval carry that base movement defeated (the policy gate compares\n         # base and head task governance directly), is never silently restored by\n         # adopting base wholesale. It is bounded by a durable per-task counter so the\ndiff --git a/scripts/shiki_tasks.py b/scripts/shiki_tasks.py\nindex aec3809f24b4a0c8c96fc50971cff9ae9ad30381..d531052e9d9717058d4631483cefab44c26a65c1\n--- a/scripts/shiki_tasks.py\n+++ b/scripts/shiki_tasks.py\n@@ -821,7 +821,7 @@\n         \"prompt\": prompt,\n         \"entry_skill\": \"grill-with-docs\",\n         \"required_next_steps\": [\n-            \"Run grill-with-docs until terminology, boundaries, risks, and ADR-worthy decisions are settled.\",\n+            \"Run grill-with-docs until terminology, boundaries, risks, and decision-record-worthy choices are settled.\",\n             \"Obtain the operator's explicit approval of the PRD/requirements (Spec Freeze).\",\n             \"Write a machine-readable plan JSON with grill_with_docs.status=complete and spec_freeze.status=frozen.\",\n             \"Run shiki plan ingest --plan-file PLAN.json.\",\ndiff --git a/scripts/test_shiki_goal_loop.sh b/scripts/test_shiki_goal_loop.sh\nindex 753c7b516e10f0be3bc92a4bd0eb1fa699776ae6..8f3820a0a079b3044424cfe1a6c6b7fe68896e67\n--- a/scripts/test_shiki_goal_loop.sh\n+++ b/scripts/test_shiki_goal_loop.sh\n@@ -231,8 +231,8 @@\n \n # High/critical risk merges autonomously when all required checks are green:\n # the \"MergeGate policy check\" required check IS the Guardian gate (it enforces\n-# guardian-policy.json \u2014 human approval OR external AI guardian review, ADR\n-# 0010), so green checks mean a recorded authority approved. The fake gh returns\n+# guardian-policy.json \u2014 human approval OR external AI guardian review,\n+# SADR-0010), so green checks mean a recorded authority approved. The fake gh returns\n # green checks, simulating that the Guardian gate passed.\n python3 \"$ROOT/scripts/shiki.py\" goal create --target \"$TARGET\" --title \"Guardian gate\" --outcome \"High risk merges via policy gate\" >/tmp/shiki-goal-loop-guardian-goal.json\n GGOAL=\"$(json_get /tmp/shiki-goal-loop-guardian-goal.json goal_id)\"\ndiff --git a/tests/test_adr_references.py b/tests/test_adr_references.py\nindex 51706329c797d5dd9b47e082a333fa83b261d482..db6e693e35f67103935be34f1d9bb4ff7ee60395\n--- a/tests/test_adr_references.py\n+++ b/tests/test_adr_references.py\n@@ -1,7 +1,7 @@\n-\"\"\"Every ``ADR ####`` citation must resolve to exactly one decision record.\n+\"\"\"Every target ADR and Shiki SADR citation resolves in its own namespace.\n \n Background. ``scripts/mergegate_check.py`` justifies ``is_bookkeeping_closeout``\n-with \"ADR 0017\" nine times and ``scripts/guardian_approval_signal.py`` cites\n+with \"SADR-0017\" nine times and ``scripts/guardian_approval_signal.py`` cites\n 0016/0017 seven times, yet for a stretch the numbering in ``docs/adr/`` stopped\n at 0015: the authoring commits lived on a coordinator branch and no task ever\n declared ``docs/adr/...`` in its locks, so nothing carried the records to the\n@@ -11,11 +11,12 @@\n and the directory drifted with no signal.\n \n This is the same unbound-pair shape behind other silent failures: a reference\n-and its target with nothing asserting they agree. This test binds them. It scans\n-every ``scripts/*.py`` (plus ``docs/**/*.md`` and the root ``*.md`` governance\n-files, which are cheap) for the ``ADR ####`` pattern and asserts each cited\n-number resolves to exactly one ``docs/adr/####-*.md``. The converse is\n-deliberately NOT required: an ADR may exist without being cited.\n+and its target with nothing asserting they agree. This test binds them. Target\n+``ADR NNNN`` citations resolve only to ``docs/adr/NNNN-*.md``; Shiki\n+``SADR-NNNN`` citations resolve only to ``docs/adr/SADR-NNNN-*.md``. The scan\n+covers current governance docs, scripts, workflows, prompts, packaging metadata,\n+and repo-local Codex guidance while deliberately excluding historical\n+``.shiki`` evidence. The converse is not required: a record may be uncited.\n \n The suite is dependency free: standard-library ``unittest`` only.\n \"\"\"\n@@ -31,17 +32,18 @@\n \n REPO_ROOT = Path(__file__).resolve().parents[1]\n \n-# \"ADR 0016\" -- a four-digit citation. The trailing \\b keeps a five-digit token\n-# from being read as a truncated four-digit number.\n-_CITATION = re.compile(r\"\\bADR (\\d{4})\\b\")\n-# \"0016-locks-bound-product-paths.md\" -- the leading four digits name the record.\n-_ADR_FILENAME = re.compile(r\"^(\\d{4})-.*\\.md$\")\n+# ``ADR 0016`` / ``SADR-0016`` -- a namespace plus four-digit citation. The\n+# trailing ``\\b`` keeps a five-digit token from being read as a truncated one.\n+_CITATION = re.compile(r\"\\b(S?ADR)[ -](\\d{4})\\b\")\n+# Target ADRs retain the numeric filename; Shiki platform decisions use SADR.\n+_DECISION_FILENAME = re.compile(r\"^(?:(SADR)-)?(\\d{4})-.*\\.md$\")\n \n \n class Violation(NamedTuple):\n     \"\"\"A citation that does not resolve to exactly one decision record.\"\"\"\n \n     citing_file: str  # repo-relative path of the file that cites the ADR\n+    namespace: str  # \"ADR\" for target decisions; \"SADR\" for Shiki decisions\n     number: str  # the cited four-digit number, e.g. \"0016\"\n     line: int  # 1-based line of the citation\n     reason: str  # \"missing\" (zero matches) or \"ambiguous\" (two or more)\n@@ -49,44 +51,59 @@\n \n     @property\n     def expected_path(self) -> str:\n-        return f\"docs/adr/{self.number}-*.md\"\n+        prefix = \"SADR-\" if self.namespace == \"SADR\" else \"\"\n+        return f\"docs/adr/{prefix}{self.number}-*.md\"\n \n     def describe(self) -> str:\n         \"\"\"Name the citing file, the cited number, and the missing path.\"\"\"\n         if self.reason == \"missing\":\n             return (\n-                f\"{self.citing_file}:{self.line} cites ADR {self.number} \"\n+                f\"{self.citing_file}:{self.line} cites {self.namespace} {self.number} \"\n                 f\"but no document matches {self.expected_path}\"\n             )\n         joined = \", \".join(self.matches)\n         return (\n-            f\"{self.citing_file}:{self.line} cites ADR {self.number} which \"\n+            f\"{self.citing_file}:{self.line} cites {self.namespace} {self.number} which \"\n             f\"resolves to {len(self.matches)} documents (expected exactly one \"\n             f\"at {self.expected_path}): {joined}\"\n         )\n \n \n-def _adr_index(root: Path) -> dict[str, list[Path]]:\n-    \"\"\"Map every four-digit ADR number to the record files that claim it.\"\"\"\n-    index: dict[str, list[Path]] = {}\n+def _adr_index(root: Path) -> dict[tuple[str, str], list[Path]]:\n+    \"\"\"Map each decision namespace and number to the files that claim it.\"\"\"\n+    index: dict[tuple[str, str], list[Path]] = {}\n     adr_dir = root / \"docs\" / \"adr\"\n     if adr_dir.is_dir():\n         for path in sorted(adr_dir.glob(\"*.md\")):\n-            match = _ADR_FILENAME.match(path.name)\n+            match = _DECISION_FILENAME.match(path.name)\n             if match:\n-                index.setdefault(match.group(1), []).append(path)\n+                namespace = \"SADR\" if match.group(1) else \"ADR\"\n+                index.setdefault((namespace, match.group(2)), []).append(path)\n     return index\n \n \n def _citing_files(root: Path, include_docs: bool = True) -> list[Path]:\n-    \"\"\"Files scanned for citations: scripts/*.py, and cheaply the markdown.\"\"\"\n+    \"\"\"Current, shipped text surfaces scanned for decision citations.\"\"\"\n     files = sorted((root / \"scripts\").glob(\"*.py\"))\n+    files += sorted((root / \"scripts\").glob(\"*.sh\"))\n     if include_docs:\n         docs = root / \"docs\"\n         if docs.is_dir():\n             files += sorted(docs.rglob(\"*.md\"))\n         files += sorted(root.glob(\"*.md\"))\n-    return files\n+        pyproject = root / \"pyproject.toml\"\n+        if pyproject.is_file():\n+            files.append(pyproject)\n+        for relative in (\".github\", \".codex\"):\n+            surface = root / relative\n+            if surface.is_dir():\n+                files += sorted(\n+                    path\n+                    for path in surface.rglob(\"*\")\n+                    if path.is_file()\n+                    and path.suffix in {\".md\", \".py\", \".sh\", \".toml\", \".yaml\", \".yml\"}\n+                )\n+    return sorted(set(files))\n \n \n def find_adr_reference_violations(\n@@ -94,10 +111,9 @@\n ) -> list[Violation]:\n     \"\"\"Return every ``ADR ####`` citation under ``root`` that fails to resolve.\n \n-    A citation resolves when exactly one ``docs/adr/####-*.md`` matches its\n-    number. Zero matches is a \"missing\" violation; two or more is \"ambiguous\".\n-    Records that are never cited produce nothing -- the converse is not\n-    required.\n+    A citation resolves when exactly one record in the same namespace matches\n+    its number. Zero matches is a \"missing\" violation; two or more is\n+    \"ambiguous\". Records that are never cited produce nothing.\n     \"\"\"\n     root = Path(root)\n     index = _adr_index(root)\n@@ -110,13 +126,15 @@\n         rel = path.relative_to(root).as_posix()\n         for lineno, line in enumerate(text.splitlines(), start=1):\n             for match in _CITATION.finditer(line):\n-                number = match.group(1)\n-                found = index.get(number, [])\n+                namespace, number = match.groups()\n+                found = index.get((namespace, number), [])\n                 if len(found) == 1:\n                     continue\n                 reason = \"missing\" if not found else \"ambiguous\"\n                 rels = [p.relative_to(root).as_posix() for p in found]\n-                violations.append(Violation(rel, number, lineno, reason, rels))\n+                violations.append(\n+                    Violation(rel, namespace, number, lineno, reason, rels)\n+                )\n     return violations\n \n \n@@ -129,8 +147,8 @@\n         self.assertEqual(\n             violations,\n             [],\n-            \"Every 'ADR ####' citation must resolve to exactly one \"\n-            f\"docs/adr/####-*.md:\\n{detail}\",\n+            \"Every ADR/SADR citation must resolve exactly once in its own \"\n+            f\"docs/adr namespace:\\n{detail}\",\n         )\n \n     def test_scan_actually_reads_scripts(self) -> None:\n@@ -146,8 +164,21 @@\n         # The two records this Goal carried onto the default branch, and which\n         # the scripts cite most, must be present.\n         index = _adr_index(REPO_ROOT)\n-        self.assertIn(\"0016\", index)\n-        self.assertIn(\"0017\", index)\n+        self.assertIn((\"SADR\", \"0016\"), index)\n+        self.assertIn((\"SADR\", \"0017\"), index)\n+\n+    def test_platform_tree_has_only_sadr_0001_through_0019(self) -> None:\n+        adr_dir = REPO_ROOT / \"docs\" / \"adr\"\n+        sadrs = sorted(adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\"))\n+        self.assertEqual(\n+            {path.name.split(\"-\", 2)[1] for path in sadrs},\n+            {f\"{number:04d}\" for number in range(1, 20)},\n+        )\n+        self.assertEqual(\n+            list(adr_dir.glob(\"[0-9][0-9][0-9][0-9]-*.md\")),\n+            [],\n+            \"the Shiki platform tree must not retain legacy numeric SADR paths\",\n+        )\n \n \n class FixtureAdrReferences(unittest.TestCase):\n@@ -180,6 +211,26 @@\n         self.assertIn(\"9991\", message)  # cited number\n         self.assertIn(\"docs/adr/9991-*.md\", message)  # missing path\n \n+    def test_target_adr_does_not_satisfy_missing_platform_sadr(self) -> None:\n+        root = self._make_repo(\n+            adr_names=[\"0042-target-decision.md\"],\n+            script_body='\"\"\"Target ADR 0042 exists, but platform SADR-0042 does not.\"\"\"\\n',\n+        )\n+        violations = find_adr_reference_violations(root, include_docs=False)\n+        self.assertEqual(len(violations), 1)\n+        self.assertEqual(violations[0].namespace, \"SADR\")\n+        self.assertEqual(violations[0].number, \"0042\")\n+        self.assertEqual(violations[0].expected_path, \"docs/adr/SADR-0042-*.md\")\n+\n+    def test_target_adr_and_platform_sadr_may_reuse_a_number(self) -> None:\n+        root = self._make_repo(\n+            adr_names=[\"0042-target-decision.md\", \"SADR-0042-platform-decision.md\"],\n+            script_body='\"\"\"Target ADR 0042 and platform SADR-0042 are distinct.\"\"\"\\n',\n+        )\n+        self.assertEqual(\n+            find_adr_reference_violations(root, include_docs=False), []\n+        )\n+\n     def test_uncited_adr_is_allowed(self) -> None:\n         # An ADR exists but nothing cites it -> not a violation. The converse\n         # (every ADR must be cited) is explicitly NOT required.\ndiff --git a/tests/test_guardian_review.py b/tests/test_guardian_review.py\nindex 88829e7810212635e2fc98aa5918d6f26294677d..00a85a237acd5bf2d6cb5b294128198c422402ca\n--- a/tests/test_guardian_review.py\n+++ b/tests/test_guardian_review.py\n@@ -162,7 +162,7 @@\n \n     def test_docs_only_is_light_and_not_conservative(self):\n         focus = gr.classify_review_focus([\"docs/adr/0099-x.md\", \"README.md\"], \"\", \"low\")\n-        self.assertTrue(any(\"Docs/ADR-only\" in f for f in focus))\n+        self.assertTrue(any(\"Docs/decision-record-only\" in f for f in focus))\n         self.assertFalse(any(\"conservative default\" in f.lower() for f in focus))\n \n     def test_unknown_category_is_conservative(self):\ndiff --git a/tests/test_policy_job_merged_prs.py b/tests/test_policy_job_merged_prs.py\nindex d45512e74681e5e649279236c7d4e79d5c3da471..b97989f8bd74d091de1a6b5275a20d8dc68c4512\n--- a/tests/test_policy_job_merged_prs.py\n+++ b/tests/test_policy_job_merged_prs.py\n@@ -3,8 +3,8 @@\n \n ``.github/workflows/shiki-cca-completion.yml`` has two consumers of the merged-PR\n proof: the CCA *signal* (``guardian_approval_signal.py`` in \"Prepare CCA context\")\n-and the authoritative gate (``mergegate_check.py`` in \"Enforce MergeGate\"). ADR\n-0017's bookkeeping-closeout exemption (condition 6) needs the merged implementation\n+and the authoritative gate (``mergegate_check.py`` in \"Enforce MergeGate\").\n+SADR-0017's bookkeeping-closeout exemption (condition 6) needs the merged implementation\n PR proven, and a closeout carries NO ``post_merge_reconcile`` marker. The signal\n already resolves ``MERGED_PRS`` unconditionally, but \"Enforce MergeGate\" used to\n resolve it only inside ``grep -q '<!-- shiki:post_merge_reconcile -->'``. So a\ndiff --git a/tests/test_sadr_install_migration.py b/tests/test_sadr_install_migration.py\nnew file mode 100644\nindex 0000000000000000000000000000000000000000..a6f4c57e4dcaef73ef600b0d4804744138ab9071\n--- /dev/null\n+++ b/tests/test_sadr_install_migration.py\n@@ -0,0 +1,229 @@\n+\"\"\"SADR installation and collision-safe legacy upgrade behavior.\"\"\"\n+\n+from __future__ import annotations\n+\n+import contextlib\n+import hashlib\n+import io\n+import json\n+import shutil\n+import tempfile\n+import unittest\n+from pathlib import Path\n+\n+import shiki_test_support  # noqa: F401  (path bootstrap)\n+\n+import shiki_installer\n+from tests.test_adr_references import find_adr_reference_violations\n+\n+\n+PRODUCT_NUMBERS = tuple(range(13, 19))\n+\n+\n+def _quiet_install(target: Path, *, force: bool, validate: bool) -> None:\n+    with contextlib.redirect_stdout(io.StringIO()):\n+        shiki_installer.install_template(target, force=force, validate=validate)\n+\n+\n+def _sha256(path: Path) -> str:\n+    return hashlib.sha256(path.read_bytes()).hexdigest()\n+\n+\n+def _legacy_sadr_paths() -> tuple[str, ...]:\n+    return tuple(\n+        f\"docs/adr/{path.name.removeprefix('SADR-')}\"\n+        for path in sorted((shiki_installer.ROOT / \"docs\" / \"adr\").glob(\"SADR-*.md\"))\n+        if path.name != \"SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\"\n+    )\n+\n+\n+def _create_legacy_upgrade_fixture(\n+    target: Path,\n+) -> tuple[dict[str, bytes], tuple[str, ...]]:\n+    adr_dir = target / \"docs\" / \"adr\"\n+    adr_dir.mkdir(parents=True)\n+    product_bytes = {\n+        f\"{number:04d}-cubeshare-product-decision.md\": (\n+            f\"# CubeShare ADR {number:04d}\\n\\nDo not rewrite {number}.\\n\"\n+        ).encode()\n+        for number in PRODUCT_NUMBERS\n+    }\n+    for name, content in product_bytes.items():\n+        (adr_dir / name).write_bytes(content)\n+\n+    legacy_paths = _legacy_sadr_paths()\n+    for relative in legacy_paths:\n+        (target / relative).write_bytes(f\"legacy Shiki bytes for {relative}\\n\".encode())\n+\n+    stamp_path = target / shiki_installer.INSTALL_STAMP_PATH\n+    stamp_path.parent.mkdir(parents=True, exist_ok=True)\n+    stamp = {\n+        \"version\": shiki_installer.INSTALL_STAMP_VERSION,\n+        \"platform_commit\": None,\n+        \"installed_at\": \"2000-01-01T00:00:00+00:00\",\n+        \"digests\": {\n+            relative: _sha256(target / relative) for relative in legacy_paths\n+        },\n+    }\n+    stamp_path.write_text(\n+        json.dumps(stamp, indent=2, sort_keys=True) + \"\\n\", encoding=\"utf-8\"\n+    )\n+    return product_bytes, legacy_paths\n+\n+\n+def _tree_snapshot(root: Path) -> dict[str, tuple[str, bytes | str]]:\n+    \"\"\"Recursive byte/shape snapshot used to prove preflight is write-free.\"\"\"\n+    snapshot: dict[str, tuple[str, bytes | str]] = {}\n+    for path in sorted(root.rglob(\"*\")):\n+        relative = path.relative_to(root).as_posix()\n+        if path.is_symlink():\n+            snapshot[relative] = (\"symlink\", path.readlink().as_posix())\n+        elif path.is_dir():\n+            snapshot[relative] = (\"directory\", \"\")\n+        else:\n+            snapshot[relative] = (\"file\", path.read_bytes())\n+    return snapshot\n+\n+\n+class FreshSadrInstallTests(unittest.TestCase):\n+    def setUp(self) -> None:\n+        self.tmp = Path(tempfile.mkdtemp(prefix=\"shiki-sadr-fresh-\"))\n+        self.addCleanup(lambda: shutil.rmtree(self.tmp, ignore_errors=True))\n+        self.target = self.tmp / \"target\"\n+        self.adr_dir = self.target / \"docs\" / \"adr\"\n+        self.adr_dir.mkdir(parents=True)\n+        self.product_bytes = {\n+            f\"{number:04d}-product-decision.md\": (\n+                f\"# Product ADR {number:04d}\\n\\nTarget-owned bytes {number}.\\n\"\n+            ).encode()\n+            for number in PRODUCT_NUMBERS\n+        }\n+        for name, content in self.product_bytes.items():\n+            (self.adr_dir / name).write_bytes(content)\n+\n+    def test_fresh_install_preserves_product_adrs_and_installs_all_sadrs(self) -> None:\n+        _quiet_install(self.target, force=False, validate=True)\n+\n+        for name, expected in self.product_bytes.items():\n+            self.assertEqual((self.adr_dir / name).read_bytes(), expected)\n+\n+        sadr_numbers = {\n+            path.name.split(\"-\", 2)[1]\n+            for path in self.adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\")\n+        }\n+        self.assertEqual(sadr_numbers, {f\"{number:04d}\" for number in range(1, 20)})\n+\n+        numeric_records = {\n+            path.name\n+            for path in self.adr_dir.glob(\"[0-9][0-9][0-9][0-9]-*.md\")\n+        }\n+        self.assertEqual(numeric_records, set(self.product_bytes))\n+        self.assertEqual(find_adr_reference_violations(self.target), [])\n+\n+\n+class LegacySadrUpgradeTests(unittest.TestCase):\n+    def setUp(self) -> None:\n+        self.tmp = Path(tempfile.mkdtemp(prefix=\"shiki-sadr-upgrade-\"))\n+        self.addCleanup(lambda: shutil.rmtree(self.tmp, ignore_errors=True))\n+        self.target = self.tmp / \"target\"\n+        self.adr_dir = self.target / \"docs\" / \"adr\"\n+        self.product_bytes, self.legacy_paths = _create_legacy_upgrade_fixture(\n+            self.target\n+        )\n+\n+    def test_force_upgrade_deletes_only_stamped_legacy_sadrs(self) -> None:\n+        _quiet_install(self.target, force=True, validate=True)\n+\n+        for relative in self.legacy_paths:\n+            self.assertFalse((self.target / relative).exists(), relative)\n+        for name, expected in self.product_bytes.items():\n+            self.assertEqual((self.adr_dir / name).read_bytes(), expected)\n+\n+        sadrs = list(self.adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\"))\n+        self.assertEqual(len(sadrs), 19)\n+        refreshed = json.loads(\n+            (self.target / shiki_installer.INSTALL_STAMP_PATH).read_text(encoding=\"utf-8\")\n+        )\n+        for relative in self.legacy_paths:\n+            self.assertNotIn(relative, refreshed[\"digests\"])\n+        for path in sadrs:\n+            relative = path.relative_to(self.target).as_posix()\n+            self.assertEqual(refreshed[\"digests\"][relative], _sha256(path))\n+\n+    def test_non_force_install_never_deletes_legacy_files(self) -> None:\n+        before = {\n+            relative: (self.target / relative).read_bytes()\n+            for relative in self.legacy_paths\n+        }\n+        _quiet_install(self.target, force=False, validate=False)\n+        for relative, expected in before.items():\n+            self.assertEqual((self.target / relative).read_bytes(), expected)\n+        refreshed = json.loads(\n+            (self.target / shiki_installer.INSTALL_STAMP_PATH).read_text(encoding=\"utf-8\")\n+        )\n+        for relative in self.legacy_paths:\n+            self.assertEqual(refreshed[\"digests\"][relative], _sha256(self.target / relative))\n+\n+\n+class LegacyCleanupPreflightTests(unittest.TestCase):\n+    def setUp(self) -> None:\n+        self.tmp = Path(tempfile.mkdtemp(prefix=\"shiki-sadr-preflight-\"))\n+        self.addCleanup(lambda: shutil.rmtree(self.tmp, ignore_errors=True))\n+\n+    def test_all_ownership_blockers_abort_before_any_write(self) -> None:\n+        cases = (\n+            (\"absent\", \"install stamp is absent\"),\n+            (\"unreadable\", \"install stamp is unreadable\"),\n+            (\"malformed\", \"install stamp is malformed\"),\n+            (\"missing-digest\", \"install stamp has no digest\"),\n+            (\"digest-mismatch\", \"digest mismatch\"),\n+        )\n+        for name, expected_reason in cases:\n+            with self.subTest(name=name):\n+                target = self.tmp / name\n+                _, legacy_paths = _create_legacy_upgrade_fixture(target)\n+                stamp_path = target / shiki_installer.INSTALL_STAMP_PATH\n+\n+                if name == \"absent\":\n+                    stamp_path.unlink()\n+                elif name == \"unreadable\":\n+                    stamp_path.unlink()\n+                    stamp_path.mkdir()\n+                elif name == \"malformed\":\n+                    stamp_path.write_text(\"{not-json\\n\", encoding=\"utf-8\")\n+                else:\n+                    stamp = json.loads(stamp_path.read_text(encoding=\"utf-8\"))\n+                    if name == \"missing-digest\":\n+                        stamp[\"digests\"] = {}\n+                    else:\n+                        stamp[\"digests\"] = {\n+                            relative: \"deadbeef\" for relative in legacy_paths\n+                        }\n+                    stamp_path.write_text(\n+                        json.dumps(stamp, indent=2, sort_keys=True) + \"\\n\",\n+                        encoding=\"utf-8\",\n+                    )\n+\n+                before = _tree_snapshot(target)\n+                with self.assertRaises(shiki_installer.ShikiError) as raised:\n+                    _quiet_install(target, force=True, validate=False)\n+                after = _tree_snapshot(target)\n+\n+                self.assertEqual(before, after, f\"{name} preflight changed the tree\")\n+                message = str(raised.exception)\n+                self.assertIn(expected_reason, message)\n+                self.assertIn(\"No files were written\", message)\n+                for relative in legacy_paths:\n+                    self.assertIn(relative, message)\n+\n+    def test_absent_stamp_is_allowed_when_no_legacy_path_exists(self) -> None:\n+        target = self.tmp / \"no-legacy\"\n+        target.mkdir()\n+        _quiet_install(target, force=True, validate=False)\n+        self.assertTrue(\n+            (target / \"docs\" / \"adr\" / \"SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\").is_file()\n+        )\n+\n+\n+if __name__ == \"__main__\":\n+    unittest.main()\n\nexec\n/bin/zsh -lc 'git diff --name-status --find-renames=50%' in /private/tmp/shiki-adr-collision.pa0KwY/.worktrees/shiki-t-20260807t073307221254z-2d22d654-introduc\nexec\n/bin/zsh -lc 'git diff -- docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md docs/adr/README.md docs/operations.md docs/releasing.md docs/usage.md' in /private/tmp/shiki-adr-collision.pa0KwY/.worktrees/shiki-t-20260807t073307221254z-2d22d654-introduc\n succeeded in 0ms:\nM\t.codex/skills/shiki/SKILL.md\nM\t.github/prompts/cca-completion-check.md\nM\t.github/prompts/codex-task-handoff.md\nM\t.github/workflows/release.yml\nM\t.github/workflows/shiki-cca-completion.yml\nM\t.github/workflows/shiki-mergegate.yml\nM\t.shiki/tasks/T-20260807T073307221254Z-2d22d654.json\nM\tAGENTS.md\nM\tCLAUDE.md\nM\tCONTEXT.md\nM\tCONTRIBUTING.md\nM\tSYSTEM_PROMPT.md\nD\tdocs/adr/0001-keep-shiki-as-platform-name.md\nD\tdocs/adr/0002-use-agents-as-shared-constitution.md\nD\tdocs/adr/0003-require-skill-gate-for-mergegate-readiness.md\nD\tdocs/adr/0004-use-github-first-with-shiki-mirror.md\nD\tdocs/adr/0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\nD\tdocs/adr/0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\nD\tdocs/adr/0007-packaging-and-release.md\nD\tdocs/adr/0008-use-claude-code-as-default-implementer.md\nD\tdocs/adr/0009-requirements-definition-and-spec-freeze.md\nD\tdocs/adr/0010-external-ai-guardian-review.md\nD\tdocs/adr/0011-autonomous-quality-gate-evidence.md\nD\tdocs/adr/0012-autonomous-loop-closeout-to-main.md\nD\tdocs/adr/0013-bootstrap-grants-actions-approve-pull-requests.md\nD\tdocs/adr/0014-codex-app-external-ai-guardian-ui-adapter.md\nD\tdocs/adr/0015-contract-approval-before-dispatch.md\nD\tdocs/adr/0016-locks-bound-product-paths.md\nD\tdocs/adr/0017-bookkeeping-closeouts-do-not-inherit-risk.md\nD\tdocs/adr/0018-carried-guardian-approval-across-a-proven-base-sync.md\nM\tdocs/adr/README.md\nM\tdocs/adr/template.md\nM\tdocs/agents/autonomous-loop-e2e-acceptance.md\nM\tdocs/agents/bootstrap-command.md\nM\tdocs/agents/checklists.md\nM\tdocs/agents/codex-handoff.md\nM\tdocs/agents/completion-check-agent.md\nM\tdocs/agents/decision-control.md\nM\tdocs/agents/domain.md\nM\tdocs/agents/execution-control.md\nM\tdocs/agents/guardian-policy.md\nM\tdocs/agents/implementation-policy.md\nM\tdocs/agents/runtime-auth-model.md\nM\tdocs/agents/runtime-registry.md\nM\tdocs/agents/shiki-cli-architecture.md\nM\tdocs/agents/skill-gate.md\nM\tdocs/agents/triage-labels.md\nM\tdocs/operations.md\nM\tdocs/proposals/0001-memory-loop-spec-freeze-review.md\nM\tdocs/proposals/0002-autonomous-loop-completion.md\nM\tdocs/releasing.md\nM\tdocs/usage.md\nM\tpyproject.toml\nM\tscripts/guardian_approval_signal.py\nM\tscripts/mergegate_check.py\nM\tscripts/shiki_bootstrap.py\nM\tscripts/shiki_cli.py\nM\tscripts/shiki_contract_approval.py\nM\tscripts/shiki_github.py\nM\tscripts/shiki_guardian.py\nM\tscripts/shiki_guardian_review.py\nM\tscripts/shiki_installer.py\nM\tscripts/shiki_loop.py\nM\tscripts/shiki_migrations.py\nM\tscripts/shiki_runtime_adapters.py\nM\tscripts/shiki_runtime_registry.py\nM\tscripts/shiki_tasks.py\nM\tscripts/test_shiki_adversarial_state.sh\nM\tscripts/test_shiki_code_review_gate.sh\nM\tscripts/test_shiki_control_plane.sh\nM\tscripts/test_shiki_goal_loop.sh\nM\tscripts/test_shiki_governance_evidence.sh\nM\tscripts/test_shiki_guardian_policy.sh\nM\tscripts/test_shiki_loop_e2e.sh\nM\tscripts/test_shiki_loop_lock_guard.sh\nM\tscripts/test_shiki_run_orchestrator.sh\nM\tscripts/test_shiki_runner_claude.sh\nM\tscripts/test_shiki_runtime_registry.sh\nM\tscripts/test_shiki_start.sh\nM\tscripts/validate_shiki.py\nM\ttests/test_adr_references.py\nM\ttests/test_bookkeeping_closeout.py\nM\ttests/test_branch_behind_sync.py\nM\ttests/test_cca_evidence_manifest_taskless.py\nM\ttests/test_closeout.py\nM\ttests/test_completion_lock_scope.py\nM\ttests/test_contract_amendment.py\nM\ttests/test_contract_approval.py\nM\ttests/test_contract_immutability.py\nM\ttests/test_contract_mode.py\nM\ttests/test_contract_open.py\nM\ttests/test_contract_plan_registration.py\nM\ttests/test_dispatch_lock_precondition.py\nM\ttests/test_guardian_review.py\nM\ttests/test_guardian_signal.py\nM\ttests/test_guardian_signal_closeout.py\nM\ttests/test_loop_branch_plumbing.py\nM\ttests/test_loop_code_review.py\nM\ttests/test_loop_e2e_contract.py\nM\ttests/test_loop_engine.py\nM\ttests/test_loop_lock_validator_hint.py\nM\ttests/test_loop_tdd_evidence.py\nM\ttests/test_mirror_scope.py\nM\ttests/test_paired_invariants.py\nM\ttests/test_policy_job_merged_prs.py\nM\ttests/test_repair_delivery.py\nM\ttests/test_runner_adapters.py\nM\ttests/test_workflow_invocation.py\nM\ttests/test_workflow_invocation_carry.py\n\n succeeded in 0ms:\ndiff --git a/docs/adr/README.md b/docs/adr/README.md\nindex c70e057..5da4385 100644\n--- a/docs/adr/README.md\n+++ b/docs/adr/README.md\n@@ -1,40 +1,41 @@\n-# Architecture Decision Records\n+# Decision Record Namespaces\n \n-This directory holds Shiki's Architecture Decision Records (ADRs): durable\n-records of hard-to-reverse platform decisions. ADRs are part of the Shiki source\n-of truth alongside GitHub state, the `.shiki/` mirror, and `CONTEXT.md`.\n+This directory holds two independent decision namespaces:\n \n-## How ADRs Work\n+- Shiki Architecture Decision Records (SADRs), named\n+  `SADR-NNNN-<short-slug>.md`, record hard-to-reverse Shiki platform decisions.\n+- Target Architecture Decision Records (ADRs), named\n+  `NNNN-<short-slug>.md`, belong to the target product.\n \n-- New ADRs are added as `docs/adr/NNNN-<short-slug>.md`, where `NNNN` is the next\n-  zero-padded sequence number.\n-- The **canonical list of ADRs is the set of files matching `docs/adr/*.md`** in\n-  this directory. Browse `docs/adr/*.md` for the authoritative, current set.\n-- Use [`template.md`](template.md) as the starting point for a new ADR.\n-- Record an ADR when a decision is hard to reverse, surprising without context,\n-  and a real tradeoff. Do not write an ADR for routine or easily reversible\n-  choices.\n+The same four-digit number may appear once in each namespace. A platform\n+`SADR-NNNN` citation resolves only to `SADR-NNNN-*.md`; a target `ADR NNNN`\n+citation resolves only to `NNNN-*.md`.\n \n-Using the `docs/adr/*.md` glob as the source of truth means other PRs can add an\n-ADR without editing this index file, avoiding merge contention on a\n-hand-maintained enumeration.\n+## How Decision Records Work\n \n-## Current ADRs (snapshot)\n+- New Shiki platform decisions use the next zero-padded SADR number.\n+- New target product decisions use the target's own ADR sequence.\n+- The canonical list is the files in `docs/adr/`; no hand-maintained index is\n+  authoritative.\n+- Use [`template.md`](template.md) as the starting point and choose the filename\n+  namespace that owns the decision.\n+- Record a decision when it is hard to reverse, surprising without context, and\n+  a real tradeoff. Do not write a decision record for routine or easily\n+  reversible choices.\n \n-The list below is a convenience snapshot and may lag behind the directory. When\n-in doubt, list `docs/adr/*.md`.\n+Using the directory as the source of truth lets independent changes add records\n+without contending on an enumerated index.\n \n-- [0001 \u2014 Keep Shiki As The Platform Name](0001-keep-shiki-as-platform-name.md)\n-- [0002 \u2014 Use AGENTS As The Shared Agent Constitution](0002-use-agents-as-shared-constitution.md)\n-- [0003 \u2014 Require Skill Gate For MergeGate Readiness](0003-require-skill-gate-for-mergegate-readiness.md)\n-- [0004 \u2014 Use GitHub First With A Repository-Local Shiki Mirror](0004-use-github-first-with-shiki-mirror.md)\n-- [0005 \u2014 Use Codex As Front And Claude Code Action As The GitHub Runtime](0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md)\n-- [0006 \u2014 Separate Codex Implementation, CCA Completion, And MergeGate Authorization](0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md)\n+## Adding A Shiki SADR\n \n-## Adding A New ADR\n-\n-1. Copy `template.md` to `docs/adr/NNNN-<short-slug>.md`.\n+1. Copy `template.md` to `docs/adr/SADR-NNNN-<short-slug>.md`.\n 2. Fill in the title, status, context, decision, and consequences.\n-3. Set the status to `Proposed`, then `Accepted` once the decision is approved.\n-4. Open a PR following `CONTRIBUTING.md`. You do not need to edit this index for\n-   the ADR to be discoverable via `docs/adr/*.md`.\n+3. Set the status to `Proposed`, then `Accepted` once the proper authority has\n+   approved the decision.\n+4. Open a PR following `CONTRIBUTING.md`.\n+\n+## Adding A Target ADR\n+\n+1. Copy `template.md` to `docs/adr/NNNN-<short-slug>.md` using the target's own\n+   sequence.\n+2. Cite it as `ADR NNNN`; do not prefix its filename with `SADR-`.\ndiff --git a/docs/operations.md b/docs/operations.md\nindex cf1f452..ace48a9 100644\n--- a/docs/operations.md\n+++ b/docs/operations.md\n@@ -89,6 +89,16 @@ template, CLI, and mirror schema.\n      target history, brought forward by `shiki migrate`, not a template.\n    - **Everything else** (CLI scripts, workflows, schemas, docs) is overwritten.\n \n+   A forced upgrade also migrates the former numeric Shiki decision paths to\n+   `SADR-NNNN-*`. Before the first write, the installer inventories the exact\n+   legacy Shiki paths and authorizes each deletion only when the existing\n+   `.shiki/install-stamp.json` names that path and its SHA-256 digest still\n+   matches the file. An absent, unreadable, malformed, incomplete, or\n+   mismatching stamp reports every blocked path and stops with no filesystem\n+   change. Target-owned `NNNN-*` ADRs are not cleanup candidates. A non-force\n+   install deletes nothing and retains any still-matching legacy stamp entries\n+   so a later forced upgrade can prove ownership.\n+\n    If the target has pending migrations, `--force` refuses and writes nothing \u2014\n    apply migrations first (step 3), then re-run. The run ends with a single\n    summary of every `.new` written, so a half-upgrade is visible.\ndiff --git a/docs/releasing.md b/docs/releasing.md\nindex ad35cd5..aafeee8 100644\n--- a/docs/releasing.md\n+++ b/docs/releasing.md\n@@ -3,7 +3,7 @@\n This document defines how Shiki is versioned, released, installed, upgraded, and\n rolled back, and how the platform-runtime boundary differs from the\n target-template boundary. The durable decision record is\n-[docs/adr/0007-packaging-and-release.md](adr/0007-packaging-and-release.md).\n+[docs/adr/SADR-0007-packaging-and-release.md](adr/SADR-0007-packaging-and-release.md).\n \n Shiki is a GitHub-first agentic engineering control plane. The operational\n source of truth is GitHub (Issues, PRs, Checks, Reviews, merges); the\n@@ -67,7 +67,7 @@ Shiki ships as a repository template plus a dependency-free, standard-library\n CLI in `scripts/`. There are no third-party Python runtime dependencies. The CLI\n is intentionally **not** exposed as a `[project.scripts]` console entry point\n because `scripts/shiki.py` is a dependency-free shim and `scripts/` is not\n-packaged as an importable distribution (see the ADR for the rationale).\n+packaged as an importable distribution (see SADR-0007 for the rationale).\n \n Supported install paths:\n \n@@ -102,7 +102,7 @@ Supported install paths:\n   importable distribution, `pipx install git+https://\u2026` would not expose a usable\n   `shiki` command. Use the script-install path above instead. A `pipx`/PyPI\n   install path may be reconsidered only after a future packaging restructure\n-  (see [docs/adr/0007-packaging-and-release.md](adr/0007-packaging-and-release.md)).\n+  (see [docs/adr/SADR-0007-packaging-and-release.md](adr/SADR-0007-packaging-and-release.md)).\n \n - **Install into a target repository:** use the bootstrap path documented in\n   [docs/agents/bootstrap-command.md](agents/bootstrap-command.md) (`bin/shiki`)\n@@ -155,6 +155,15 @@ applying any repository-local state migrations:\n    [docs/operations.md](operations.md#the-new-convention). If the target has\n    pending migrations, `--force` refuses and writes nothing; apply migrations\n    (step 2) first, then re-run.\n+\n+   The SADR namespace upgrade has an additional pre-write gate: each former\n+   numeric Shiki decision path is removed only when the existing install stamp\n+   contains that exact path and a digest matching its current bytes. The\n+   installer aggregates absent/unreadable/malformed-stamp, missing-digest, and\n+   digest-mismatch blockers before raising, so refusal leaves the target tree\n+   byte-identical. Numeric target ADRs are preserved. A successful forced\n+   upgrade installs and stamps `SADR-*` paths and drops the removed legacy paths\n+   from the refreshed stamp.\n 2. Apply repository-local `.shiki/` state migrations with the migrate command:\n \n    ```bash\ndiff --git a/docs/usage.md b/docs/usage.md\nindex b83407e..a130ef6 100644\n--- a/docs/usage.md\n+++ b/docs/usage.md\n@@ -67,8 +67,10 @@ GitHub-first.\n shiki install-target /path/to/target-repo --local-only\n ```\n \n-Use `--force` only when you intentionally want to overwrite existing target\n-files.\n+Use `--force` only when you intentionally want to refresh the target template.\n+For the SADR namespace migration, `--force` deletes only exact legacy Shiki\n+decision paths whose existing install-stamp digest still matches; any ownership\n+blocker stops before the first write. Numeric target ADRs are never candidates.\n \n ## `shiki bootstrap-platform` \u2014 publish the Shiki platform repo\n \n\nexec\n/bin/zsh -lc 'git diff -- scripts/shiki_installer.py tests/test_sadr_install_migration.py tests/test_adr_references.py' in /private/tmp/shiki-adr-collision.pa0KwY/.worktrees/shiki-t-20260807t073307221254z-2d22d654-introduc\n succeeded in 0ms:\ndiff --git a/scripts/shiki_installer.py b/scripts/shiki_installer.py\nindex be064ab..9fd8eef 100644\n--- a/scripts/shiki_installer.py\n+++ b/scripts/shiki_installer.py\n@@ -118,6 +118,7 @@ DEFAULT_CODEX_SKILL_PATH = \"~/.codex/skills/shiki/SKILL.md\"\n # what lets a target carry it without a governance-schema change.\n INSTALL_STAMP_PATH = \".shiki/install-stamp.json\"\n INSTALL_STAMP_VERSION = 1\n+LEGACY_SHIKI_SADR_MAX = 18\n \n # Under --force the shipped surface splits three ways, so nothing is lost and\n # nothing is silently stale.\n@@ -237,9 +238,98 @@ def refuse_pending_migrations(target: Path) -> None:\n         )\n \n \n+def legacy_shiki_adr_paths() -> tuple[str, ...]:\n+    \"\"\"Former numeric paths for Shiki SADR-0001 through SADR-0018.\n+\n+    The authoritative slugs come from the current SADR filenames. SADR-0019 is\n+    the namespace decision itself and never shipped under a numeric legacy path.\n+    \"\"\"\n+    paths: list[str] = []\n+    for sadr in sorted((ROOT / \"docs\" / \"adr\").glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\")):\n+        number = sadr.name.split(\"-\", 2)[1]\n+        if int(number) <= LEGACY_SHIKI_SADR_MAX:\n+            paths.append(f\"docs/adr/{sadr.name.removeprefix('SADR-')}\")\n+    return tuple(paths)\n+\n+\n+def preflight_legacy_sadr_cleanup(target: Path, *, force: bool) -> tuple[str, ...]:\n+    \"\"\"Authorize exact legacy Shiki ADR removals before the first write.\n+\n+    A non-force install never removes existing files. A forced install needs no\n+    stamp when no legacy Shiki path is present. When legacy paths do exist, each\n+    must be named in a readable install stamp and its current bytes must match\n+    the recorded digest.\n+    \"\"\"\n+    if not force:\n+        return ()\n+    present = tuple(\n+        relative\n+        for relative in legacy_shiki_adr_paths()\n+        if (target / relative).exists() or (target / relative).is_symlink()\n+    )\n+    if not present:\n+        return ()\n+\n+    stamp_path = target / INSTALL_STAMP_PATH\n+    try:\n+        raw = stamp_path.read_text(encoding=\"utf-8\")\n+        stamp = json.loads(raw)\n+    except FileNotFoundError:\n+        detail = \"install stamp is absent\"\n+        blockers = [f\"{relative}: {detail}\" for relative in present]\n+    except (OSError, UnicodeDecodeError) as error:\n+        detail = f\"install stamp is unreadable ({error})\"\n+        blockers = [f\"{relative}: {detail}\" for relative in present]\n+    except json.JSONDecodeError as error:\n+        detail = f\"install stamp is malformed ({error})\"\n+        blockers = [f\"{relative}: {detail}\" for relative in present]\n+    else:\n+        if not isinstance(stamp, dict):\n+            blockers = [\n+                f\"{relative}: install stamp is malformed (expected an object)\"\n+                for relative in present\n+            ]\n+        else:\n+            digests = stamp.get(\"digests\")\n+            blockers = []\n+            if not isinstance(digests, dict):\n+                blockers.extend(\n+                    f\"{relative}: install stamp has no digest for this path\"\n+                    for relative in present\n+                )\n+            else:\n+                for relative in present:\n+                    expected = digests.get(relative)\n+                    if not isinstance(expected, str) or not expected:\n+                        blockers.append(\n+                            f\"{relative}: install stamp has no digest for this path\"\n+                        )\n+                        continue\n+                    try:\n+                        actual = _sha256_file(target / relative)\n+                    except OSError as error:\n+                        blockers.append(\n+                            f\"{relative}: legacy path is unreadable ({error})\"\n+                        )\n+                        continue\n+                    if actual != expected:\n+                        blockers.append(\n+                            f\"{relative}: digest mismatch (stamp {expected}, current {actual})\"\n+                        )\n+\n+    if blockers:\n+        joined = \"\\n\".join(f\"- {blocker}\" for blocker in blockers)\n+        raise ShikiError(\n+            \"legacy Shiki ADR cleanup is not authorized:\\n\"\n+            f\"{joined}\\nNo files were written.\"\n+        )\n+    return present\n+\n+\n def install_template(target: Path, *, force: bool, validate: bool) -> list[NewFileNote]:\n     # Preconditions first: never rewrite the tree of a target that cannot be\n     # upgraded cleanly (see refuse_pending_migrations).\n+    legacy_cleanup = preflight_legacy_sadr_cleanup(target, force=force)\n     refuse_pending_migrations(target)\n \n     # Resolve the target's required CODEOWNERS owner once from its\n@@ -250,6 +340,10 @@ def install_template(target: Path, *, force: bool, validate: bool) -> list[NewFi\n     # verbatim. See ``codeowners_required_owner``.\n     codeowners_owner = codeowners_required_owner(target)\n \n+    for relative in legacy_cleanup:\n+        (target / relative).unlink()\n+        info(f\"removed stamped legacy Shiki ADR: {target / relative}\")\n+\n     notes: list[NewFileNote] = []\n     for relative in TEMPLATE_PATHS:\n         source = ROOT / relative\n@@ -647,6 +741,22 @@ def write_install_stamp(target: Path) -> None:\n     digests = target_stamp_digests(target)\n     existing = read_install_stamp(target)\n     if existing is not None:\n+        # A non-force install deliberately leaves legacy Shiki ADRs in place.\n+        # Preserve only still-matching ownership proof so a later forced\n+        # upgrade can authorize cleanup; never carry a missing or stale digest.\n+        existing_digests = existing.get(\"digests\")\n+        if isinstance(existing_digests, dict):\n+            for relative in legacy_shiki_adr_paths():\n+                expected = existing_digests.get(relative)\n+                path = target / relative\n+                if not isinstance(expected, str) or not path.is_file():\n+                    continue\n+                try:\n+                    actual = _sha256_file(path)\n+                except OSError:\n+                    continue\n+                if actual == expected:\n+                    digests[relative] = actual\n         if _incoming_is_older(commit, existing.get(\"platform_commit\")):\n             warn(\n                 \"kept existing install stamp: incoming install \"\ndiff --git a/tests/test_adr_references.py b/tests/test_adr_references.py\nindex 5170632..db6e693 100644\n--- a/tests/test_adr_references.py\n+++ b/tests/test_adr_references.py\n@@ -1,7 +1,7 @@\n-\"\"\"Every ``ADR ####`` citation must resolve to exactly one decision record.\n+\"\"\"Every target ADR and Shiki SADR citation resolves in its own namespace.\n \n Background. ``scripts/mergegate_check.py`` justifies ``is_bookkeeping_closeout``\n-with \"ADR 0017\" nine times and ``scripts/guardian_approval_signal.py`` cites\n+with \"SADR-0017\" nine times and ``scripts/guardian_approval_signal.py`` cites\n 0016/0017 seven times, yet for a stretch the numbering in ``docs/adr/`` stopped\n at 0015: the authoring commits lived on a coordinator branch and no task ever\n declared ``docs/adr/...`` in its locks, so nothing carried the records to the\n@@ -11,11 +11,12 @@ returns nothing -- no validator required a cited ADR to exist, so the citations\n and the directory drifted with no signal.\n \n This is the same unbound-pair shape behind other silent failures: a reference\n-and its target with nothing asserting they agree. This test binds them. It scans\n-every ``scripts/*.py`` (plus ``docs/**/*.md`` and the root ``*.md`` governance\n-files, which are cheap) for the ``ADR ####`` pattern and asserts each cited\n-number resolves to exactly one ``docs/adr/####-*.md``. The converse is\n-deliberately NOT required: an ADR may exist without being cited.\n+and its target with nothing asserting they agree. This test binds them. Target\n+``ADR NNNN`` citations resolve only to ``docs/adr/NNNN-*.md``; Shiki\n+``SADR-NNNN`` citations resolve only to ``docs/adr/SADR-NNNN-*.md``. The scan\n+covers current governance docs, scripts, workflows, prompts, packaging metadata,\n+and repo-local Codex guidance while deliberately excluding historical\n+``.shiki`` evidence. The converse is not required: a record may be uncited.\n \n The suite is dependency free: standard-library ``unittest`` only.\n \"\"\"\n@@ -31,17 +32,18 @@ from typing import NamedTuple, Sequence\n \n REPO_ROOT = Path(__file__).resolve().parents[1]\n \n-# \"ADR 0016\" -- a four-digit citation. The trailing \\b keeps a five-digit token\n-# from being read as a truncated four-digit number.\n-_CITATION = re.compile(r\"\\bADR (\\d{4})\\b\")\n-# \"0016-locks-bound-product-paths.md\" -- the leading four digits name the record.\n-_ADR_FILENAME = re.compile(r\"^(\\d{4})-.*\\.md$\")\n+# ``ADR 0016`` / ``SADR-0016`` -- a namespace plus four-digit citation. The\n+# trailing ``\\b`` keeps a five-digit token from being read as a truncated one.\n+_CITATION = re.compile(r\"\\b(S?ADR)[ -](\\d{4})\\b\")\n+# Target ADRs retain the numeric filename; Shiki platform decisions use SADR.\n+_DECISION_FILENAME = re.compile(r\"^(?:(SADR)-)?(\\d{4})-.*\\.md$\")\n \n \n class Violation(NamedTuple):\n     \"\"\"A citation that does not resolve to exactly one decision record.\"\"\"\n \n     citing_file: str  # repo-relative path of the file that cites the ADR\n+    namespace: str  # \"ADR\" for target decisions; \"SADR\" for Shiki decisions\n     number: str  # the cited four-digit number, e.g. \"0016\"\n     line: int  # 1-based line of the citation\n     reason: str  # \"missing\" (zero matches) or \"ambiguous\" (two or more)\n@@ -49,44 +51,59 @@ class Violation(NamedTuple):\n \n     @property\n     def expected_path(self) -> str:\n-        return f\"docs/adr/{self.number}-*.md\"\n+        prefix = \"SADR-\" if self.namespace == \"SADR\" else \"\"\n+        return f\"docs/adr/{prefix}{self.number}-*.md\"\n \n     def describe(self) -> str:\n         \"\"\"Name the citing file, the cited number, and the missing path.\"\"\"\n         if self.reason == \"missing\":\n             return (\n-                f\"{self.citing_file}:{self.line} cites ADR {self.number} \"\n+                f\"{self.citing_file}:{self.line} cites {self.namespace} {self.number} \"\n                 f\"but no document matches {self.expected_path}\"\n             )\n         joined = \", \".join(self.matches)\n         return (\n-            f\"{self.citing_file}:{self.line} cites ADR {self.number} which \"\n+            f\"{self.citing_file}:{self.line} cites {self.namespace} {self.number} which \"\n             f\"resolves to {len(self.matches)} documents (expected exactly one \"\n             f\"at {self.expected_path}): {joined}\"\n         )\n \n \n-def _adr_index(root: Path) -> dict[str, list[Path]]:\n-    \"\"\"Map every four-digit ADR number to the record files that claim it.\"\"\"\n-    index: dict[str, list[Path]] = {}\n+def _adr_index(root: Path) -> dict[tuple[str, str], list[Path]]:\n+    \"\"\"Map each decision namespace and number to the files that claim it.\"\"\"\n+    index: dict[tuple[str, str], list[Path]] = {}\n     adr_dir = root / \"docs\" / \"adr\"\n     if adr_dir.is_dir():\n         for path in sorted(adr_dir.glob(\"*.md\")):\n-            match = _ADR_FILENAME.match(path.name)\n+            match = _DECISION_FILENAME.match(path.name)\n             if match:\n-                index.setdefault(match.group(1), []).append(path)\n+                namespace = \"SADR\" if match.group(1) else \"ADR\"\n+                index.setdefault((namespace, match.group(2)), []).append(path)\n     return index\n \n \n def _citing_files(root: Path, include_docs: bool = True) -> list[Path]:\n-    \"\"\"Files scanned for citations: scripts/*.py, and cheaply the markdown.\"\"\"\n+    \"\"\"Current, shipped text surfaces scanned for decision citations.\"\"\"\n     files = sorted((root / \"scripts\").glob(\"*.py\"))\n+    files += sorted((root / \"scripts\").glob(\"*.sh\"))\n     if include_docs:\n         docs = root / \"docs\"\n         if docs.is_dir():\n             files += sorted(docs.rglob(\"*.md\"))\n         files += sorted(root.glob(\"*.md\"))\n-    return files\n+        pyproject = root / \"pyproject.toml\"\n+        if pyproject.is_file():\n+            files.append(pyproject)\n+        for relative in (\".github\", \".codex\"):\n+            surface = root / relative\n+            if surface.is_dir():\n+                files += sorted(\n+                    path\n+                    for path in surface.rglob(\"*\")\n+                    if path.is_file()\n+                    and path.suffix in {\".md\", \".py\", \".sh\", \".toml\", \".yaml\", \".yml\"}\n+                )\n+    return sorted(set(files))\n \n \n def find_adr_reference_violations(\n@@ -94,10 +111,9 @@ def find_adr_reference_violations(\n ) -> list[Violation]:\n     \"\"\"Return every ``ADR ####`` citation under ``root`` that fails to resolve.\n \n-    A citation resolves when exactly one ``docs/adr/####-*.md`` matches its\n-    number. Zero matches is a \"missing\" violation; two or more is \"ambiguous\".\n-    Records that are never cited produce nothing -- the converse is not\n-    required.\n+    A citation resolves when exactly one record in the same namespace matches\n+    its number. Zero matches is a \"missing\" violation; two or more is\n+    \"ambiguous\". Records that are never cited produce nothing.\n     \"\"\"\n     root = Path(root)\n     index = _adr_index(root)\n@@ -110,13 +126,15 @@ def find_adr_reference_violations(\n         rel = path.relative_to(root).as_posix()\n         for lineno, line in enumerate(text.splitlines(), start=1):\n             for match in _CITATION.finditer(line):\n-                number = match.group(1)\n-                found = index.get(number, [])\n+                namespace, number = match.groups()\n+                found = index.get((namespace, number), [])\n                 if len(found) == 1:\n                     continue\n                 reason = \"missing\" if not found else \"ambiguous\"\n                 rels = [p.relative_to(root).as_posix() for p in found]\n-                violations.append(Violation(rel, number, lineno, reason, rels))\n+                violations.append(\n+                    Violation(rel, namespace, number, lineno, reason, rels)\n+                )\n     return violations\n \n \n@@ -129,8 +147,8 @@ class RealRepoAdrReferences(unittest.TestCase):\n         self.assertEqual(\n             violations,\n             [],\n-            \"Every 'ADR ####' citation must resolve to exactly one \"\n-            f\"docs/adr/####-*.md:\\n{detail}\",\n+            \"Every ADR/SADR citation must resolve exactly once in its own \"\n+            f\"docs/adr namespace:\\n{detail}\",\n         )\n \n     def test_scan_actually_reads_scripts(self) -> None:\n@@ -146,8 +164,21 @@ class RealRepoAdrReferences(unittest.TestCase):\n         # The two records this Goal carried onto the default branch, and which\n         # the scripts cite most, must be present.\n         index = _adr_index(REPO_ROOT)\n-        self.assertIn(\"0016\", index)\n-        self.assertIn(\"0017\", index)\n+        self.assertIn((\"SADR\", \"0016\"), index)\n+        self.assertIn((\"SADR\", \"0017\"), index)\n+\n+    def test_platform_tree_has_only_sadr_0001_through_0019(self) -> None:\n+        adr_dir = REPO_ROOT / \"docs\" / \"adr\"\n+        sadrs = sorted(adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\"))\n+        self.assertEqual(\n+            {path.name.split(\"-\", 2)[1] for path in sadrs},\n+            {f\"{number:04d}\" for number in range(1, 20)},\n+        )\n+        self.assertEqual(\n+            list(adr_dir.glob(\"[0-9][0-9][0-9][0-9]-*.md\")),\n+            [],\n+            \"the Shiki platform tree must not retain legacy numeric SADR paths\",\n+        )\n \n \n class FixtureAdrReferences(unittest.TestCase):\n@@ -180,6 +211,26 @@ class FixtureAdrReferences(unittest.TestCase):\n         self.assertIn(\"9991\", message)  # cited number\n         self.assertIn(\"docs/adr/9991-*.md\", message)  # missing path\n \n+    def test_target_adr_does_not_satisfy_missing_platform_sadr(self) -> None:\n+        root = self._make_repo(\n+            adr_names=[\"0042-target-decision.md\"],\n+            script_body='\"\"\"Target ADR 0042 exists, but platform SADR-0042 does not.\"\"\"\\n',\n+        )\n+        violations = find_adr_reference_violations(root, include_docs=False)\n+        self.assertEqual(len(violations), 1)\n+        self.assertEqual(violations[0].namespace, \"SADR\")\n+        self.assertEqual(violations[0].number, \"0042\")\n+        self.assertEqual(violations[0].expected_path, \"docs/adr/SADR-0042-*.md\")\n+\n+    def test_target_adr_and_platform_sadr_may_reuse_a_number(self) -> None:\n+        root = self._make_repo(\n+            adr_names=[\"0042-target-decision.md\", \"SADR-0042-platform-decision.md\"],\n+            script_body='\"\"\"Target ADR 0042 and platform SADR-0042 are distinct.\"\"\"\\n',\n+        )\n+        self.assertEqual(\n+            find_adr_reference_violations(root, include_docs=False), []\n+        )\n+\n     def test_uncited_adr_is_allowed(self) -> None:\n         # An ADR exists but nothing cites it -> not a violation. The converse\n         # (every ADR must be cited) is explicitly NOT required.\n\nexec\n/bin/zsh -lc 'git diff --find-renames=50% --numstat' in /private/tmp/shiki-adr-collision.pa0KwY/.worktrees/shiki-t-20260807t073307221254z-2d22d654-introduc\nexec\n/bin/zsh -lc 'git diff --check' in /private/tmp/shiki-adr-collision.pa0KwY/.worktrees/shiki-t-20260807t073307221254z-2d22d654-introduc\nexec\n/bin/zsh -lc 'git diff --find-renames=50% --stat' in /private/tmp/shiki-adr-collision.pa0KwY/.worktrees/shiki-t-20260807t073307221254z-2d22d654-introduc\n succeeded in 0ms:\n succeeded in 0ms:\n4\t1\t.codex/skills/shiki/SKILL.md\n4\t1\t.github/prompts/cca-completion-check.md\n1\t1\t.github/prompts/codex-task-handoff.md\n1\t1\t.github/workflows/release.yml\n12\t12\t.github/workflows/shiki-cca-completion.yml\n4\t4\t.github/workflows/shiki-mergegate.yml\n20\t16\t.shiki/tasks/T-20260807T073307221254Z-2d22d654.json\n15\t11\tAGENTS.md\n10\t7\tCLAUDE.md\n5\t5\tCONTEXT.md\n4\t4\tCONTRIBUTING.md\n11\t8\tSYSTEM_PROMPT.md\n0\t3\tdocs/adr/0001-keep-shiki-as-platform-name.md\n0\t3\tdocs/adr/0002-use-agents-as-shared-constitution.md\n0\t3\tdocs/adr/0003-require-skill-gate-for-mergegate-readiness.md\n0\t22\tdocs/adr/0004-use-github-first-with-shiki-mirror.md\n0\t29\tdocs/adr/0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\n0\t54\tdocs/adr/0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\n0\t126\tdocs/adr/0007-packaging-and-release.md\n0\t108\tdocs/adr/0008-use-claude-code-as-default-implementer.md\n0\t90\tdocs/adr/0009-requirements-definition-and-spec-freeze.md\n0\t97\tdocs/adr/0010-external-ai-guardian-review.md\n0\t87\tdocs/adr/0011-autonomous-quality-gate-evidence.md\n0\t44\tdocs/adr/0012-autonomous-loop-closeout-to-main.md\n0\t109\tdocs/adr/0013-bootstrap-grants-actions-approve-pull-requests.md\n0\t102\tdocs/adr/0014-codex-app-external-ai-guardian-ui-adapter.md\n0\t229\tdocs/adr/0015-contract-approval-before-dispatch.md\n0\t233\tdocs/adr/0016-locks-bound-product-paths.md\n0\t129\tdocs/adr/0017-bookkeeping-closeouts-do-not-inherit-risk.md\n0\t147\tdocs/adr/0018-carried-guardian-approval-across-a-proven-base-sync.md\n32\t31\tdocs/adr/README.md\n4\t4\tdocs/adr/template.md\n7\t7\tdocs/agents/autonomous-loop-e2e-acceptance.md\n3\t1\tdocs/agents/bootstrap-command.md\n5\t5\tdocs/agents/checklists.md\n2\t2\tdocs/agents/codex-handoff.md\n1\t1\tdocs/agents/completion-check-agent.md\n1\t1\tdocs/agents/decision-control.md\n5\t3\tdocs/agents/domain.md\n1\t1\tdocs/agents/execution-control.md\n5\t5\tdocs/agents/guardian-policy.md\n14\t11\tdocs/agents/implementation-policy.md\n3\t3\tdocs/agents/runtime-auth-model.md\n1\t1\tdocs/agents/runtime-registry.md\n1\t1\tdocs/agents/shiki-cli-architecture.md\n2\t2\tdocs/agents/skill-gate.md\n1\t1\tdocs/agents/triage-labels.md\n10\t0\tdocs/operations.md\n2\t2\tdocs/proposals/0001-memory-loop-spec-freeze-review.md\n10\t10\tdocs/proposals/0002-autonomous-loop-completion.md\n12\t3\tdocs/releasing.md\n4\t2\tdocs/usage.md\n1\t1\tpyproject.toml\n15\t15\tscripts/guardian_approval_signal.py\n42\t42\tscripts/mergegate_check.py\n1\t1\tscripts/shiki_bootstrap.py\n1\t1\tscripts/shiki_cli.py\n7\t7\tscripts/shiki_contract_approval.py\n4\t4\tscripts/shiki_github.py\n3\t3\tscripts/shiki_guardian.py\n3\t3\tscripts/shiki_guardian_review.py\n110\t0\tscripts/shiki_installer.py\n28\t28\tscripts/shiki_loop.py\n2\t2\tscripts/shiki_migrations.py\n1\t1\tscripts/shiki_runtime_adapters.py\n1\t1\tscripts/shiki_runtime_registry.py\n8\t8\tscripts/shiki_tasks.py\n4\t4\tscripts/test_shiki_adversarial_state.sh\n1\t1\tscripts/test_shiki_code_review_gate.sh\n4\t4\tscripts/test_shiki_control_plane.sh\n7\t7\tscripts/test_shiki_goal_loop.sh\n2\t2\tscripts/test_shiki_governance_evidence.sh\n1\t1\tscripts/test_shiki_guardian_policy.sh\n3\t3\tscripts/test_shiki_loop_e2e.sh\n1\t1\tscripts/test_shiki_loop_lock_guard.sh\n1\t1\tscripts/test_shiki_run_orchestrator.sh\n1\t1\tscripts/test_shiki_runner_claude.sh\n1\t1\tscripts/test_shiki_runtime_registry.sh\n2\t2\tscripts/test_shiki_start.sh\n5\t5\tscripts/validate_shiki.py\n84\t33\ttests/test_adr_references.py\n3\t3\ttests/test_bookkeeping_closeout.py\n2\t2\ttests/test_branch_behind_sync.py\n1\t1\ttests/test_cca_evidence_manifest_taskless.py\n1\t1\ttests/test_closeout.py\n1\t1\ttests/test_completion_lock_scope.py\n1\t1\ttests/test_contract_amendment.py\n2\t2\ttests/test_contract_approval.py\n3\t3\ttests/test_contract_immutability.py\n2\t2\ttests/test_contract_mode.py\n1\t1\ttests/test_contract_open.py\n2\t2\ttests/test_contract_plan_registration.py\n1\t1\ttests/test_dispatch_lock_precondition.py\n3\t3\ttests/test_guardian_review.py\n1\t1\ttests/test_guardian_signal.py\n3\t3\ttests/test_guardian_signal_closeout.py\n1\t1\ttests/test_loop_branch_plumbing.py\n1\t1\ttests/test_loop_code_review.py\n1\t1\ttests/test_loop_e2e_contract.py\n3\t3\ttests/test_loop_engine.py\n3\t3\ttests/test_loop_lock_validator_hint.py\n2\t2\ttests/test_loop_tdd_evidence.py\n1\t1\ttests/test_mirror_scope.py\n1\t1\ttests/test_paired_invariants.py\n3\t3\ttests/test_policy_job_merged_prs.py\n1\t1\ttests/test_repair_delivery.py\n1\t1\ttests/test_runner_adapters.py\n4\t4\ttests/test_workflow_invocation.py\n1\t1\ttests/test_workflow_invocation_carry.py\n\n succeeded in 0ms:\n .codex/skills/shiki/SKILL.md                       |   5 +-\n .github/prompts/cca-completion-check.md            |   5 +-\n .github/prompts/codex-task-handoff.md              |   2 +-\n .github/workflows/release.yml                      |   2 +-\n .github/workflows/shiki-cca-completion.yml         |  24 +--\n .github/workflows/shiki-mergegate.yml              |   8 +-\n .../tasks/T-20260807T073307221254Z-2d22d654.json   |  36 ++--\n AGENTS.md                                          |  26 ++-\n CLAUDE.md                                          |  17 +-\n CONTEXT.md                                         |  10 +-\n CONTRIBUTING.md                                    |   8 +-\n SYSTEM_PROMPT.md                                   |  19 +-\n docs/adr/0001-keep-shiki-as-platform-name.md       |   3 -\n docs/adr/0002-use-agents-as-shared-constitution.md |   3 -\n ...3-require-skill-gate-for-mergegate-readiness.md |   3 -\n .../adr/0004-use-github-first-with-shiki-mirror.md |  22 --\n ...ont-and-claude-code-action-as-github-runtime.md |  29 ---\n ...n-cca-completion-and-mergegate-authorization.md |  54 -----\n docs/adr/0007-packaging-and-release.md             | 126 -----------\n .../0008-use-claude-code-as-default-implementer.md | 108 ----------\n ...0009-requirements-definition-and-spec-freeze.md |  90 --------\n docs/adr/0010-external-ai-guardian-review.md       |  97 ---------\n docs/adr/0011-autonomous-quality-gate-evidence.md  |  87 --------\n docs/adr/0012-autonomous-loop-closeout-to-main.md  |  44 ----\n ...otstrap-grants-actions-approve-pull-requests.md | 109 ----------\n ...14-codex-app-external-ai-guardian-ui-adapter.md | 102 ---------\n docs/adr/0015-contract-approval-before-dispatch.md | 229 --------------------\n docs/adr/0016-locks-bound-product-paths.md         | 233 ---------------------\n ...17-bookkeeping-closeouts-do-not-inherit-risk.md | 129 ------------\n ...-guardian-approval-across-a-proven-base-sync.md | 147 -------------\n docs/adr/README.md                                 |  63 +++---\n docs/adr/template.md                               |   8 +-\n docs/agents/autonomous-loop-e2e-acceptance.md      |  14 +-\n docs/agents/bootstrap-command.md                   |   4 +-\n docs/agents/checklists.md                          |  10 +-\n docs/agents/codex-handoff.md                       |   4 +-\n docs/agents/completion-check-agent.md              |   2 +-\n docs/agents/decision-control.md                    |   2 +-\n docs/agents/domain.md                              |   8 +-\n docs/agents/execution-control.md                   |   2 +-\n docs/agents/guardian-policy.md                     |  10 +-\n docs/agents/implementation-policy.md               |  25 ++-\n docs/agents/runtime-auth-model.md                  |   6 +-\n docs/agents/runtime-registry.md                    |   2 +-\n docs/agents/shiki-cli-architecture.md              |   2 +-\n docs/agents/skill-gate.md                          |   4 +-\n docs/agents/triage-labels.md                       |   2 +-\n docs/operations.md                                 |  10 +\n .../0001-memory-loop-spec-freeze-review.md         |   4 +-\n docs/proposals/0002-autonomous-loop-completion.md  |  20 +-\n docs/releasing.md                                  |  15 +-\n docs/usage.md                                      |   6 +-\n pyproject.toml                                     |   2 +-\n scripts/guardian_approval_signal.py                |  30 +--\n scripts/mergegate_check.py                         |  84 ++++----\n scripts/shiki_bootstrap.py                         |   2 +-\n scripts/shiki_cli.py                               |   2 +-\n scripts/shiki_contract_approval.py                 |  14 +-\n scripts/shiki_github.py                            |   8 +-\n scripts/shiki_guardian.py                          |   6 +-\n scripts/shiki_guardian_review.py                   |   6 +-\n scripts/shiki_installer.py                         | 110 ++++++++++\n scripts/shiki_loop.py                              |  56 ++---\n scripts/shiki_migrations.py                        |   4 +-\n scripts/shiki_runtime_adapters.py                  |   2 +-\n scripts/shiki_runtime_registry.py                  |   2 +-\n scripts/shiki_tasks.py                             |  16 +-\n scripts/test_shiki_adversarial_state.sh            |   8 +-\n scripts/test_shiki_code_review_gate.sh             |   2 +-\n scripts/test_shiki_control_plane.sh                |   8 +-\n scripts/test_shiki_goal_loop.sh                    |  14 +-\n scripts/test_shiki_governance_evidence.sh          |   4 +-\n scripts/test_shiki_guardian_policy.sh              |   2 +-\n scripts/test_shiki_loop_e2e.sh                     |   6 +-\n scripts/test_shiki_loop_lock_guard.sh              |   2 +-\n scripts/test_shiki_run_orchestrator.sh             |   2 +-\n scripts/test_shiki_runner_claude.sh                |   2 +-\n scripts/test_shiki_runtime_registry.sh             |   2 +-\n scripts/test_shiki_start.sh                        |   4 +-\n scripts/validate_shiki.py                          |  10 +-\n tests/test_adr_references.py                       | 117 ++++++++---\n tests/test_bookkeeping_closeout.py                 |   6 +-\n tests/test_branch_behind_sync.py                   |   4 +-\n tests/test_cca_evidence_manifest_taskless.py       |   2 +-\n tests/test_closeout.py                             |   2 +-\n tests/test_completion_lock_scope.py                |   2 +-\n tests/test_contract_amendment.py                   |   2 +-\n tests/test_contract_approval.py                    |   4 +-\n tests/test_contract_immutability.py                |   6 +-\n tests/test_contract_mode.py                        |   4 +-\n tests/test_contract_open.py                        |   2 +-\n tests/test_contract_plan_registration.py           |   4 +-\n tests/test_dispatch_lock_precondition.py           |   2 +-\n tests/test_guardian_review.py                      |   6 +-\n tests/test_guardian_signal.py                      |   2 +-\n tests/test_guardian_signal_closeout.py             |   6 +-\n tests/test_loop_branch_plumbing.py                 |   2 +-\n tests/test_loop_code_review.py                     |   2 +-\n tests/test_loop_e2e_contract.py                    |   2 +-\n tests/test_loop_engine.py                          |   6 +-\n tests/test_loop_lock_validator_hint.py             |   6 +-\n tests/test_loop_tdd_evidence.py                    |   4 +-\n tests/test_mirror_scope.py                         |   2 +-\n tests/test_paired_invariants.py                    |   2 +-\n tests/test_policy_job_merged_prs.py                |   6 +-\n tests/test_repair_delivery.py                      |   2 +-\n tests/test_runner_adapters.py                      |   2 +-\n tests/test_workflow_invocation.py                  |   8 +-\n tests/test_workflow_invocation_carry.py            |   2 +-\n 109 files changed, 611 insertions(+), 2016 deletions(-)\n\ndiff --git a/.codex/skills/shiki/SKILL.md b/.codex/skills/shiki/SKILL.md\nindex c8e2e85d9d75d49cc4f2d6286ee22659c4e53e75..93cded623b2c15d9f65c76491a1c2d813ac9e6c0\n--- a/.codex/skills/shiki/SKILL.md\n+++ b/.codex/skills/shiki/SKILL.md\n@@ -18,6 +18,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n Codex CLI does not register this skill as a custom `/shiki` slash command. When\n the user types `/shiki` in Codex CLI and gets \"Unrecognized command\", explain\n that this is expected: invoke Shiki in Codex with natural language such as\n@@ -69,7 +72,7 @@\n \n ## Responsibilities\n \n-- Claude Code implements and repairs by default (`shiki runner claude`, ADR 0008); Codex implements and repairs tasks explicitly assigned to `codex`.\n+- Claude Code implements and repairs by default (`shiki runner claude`, SADR-0008); Codex implements and repairs tasks explicitly assigned to `codex`.\n - Claude Code Action can act as GitHub-side CCA or reviewer.\n - CCA judges completion.\n - MergeGate authorizes state transitions and merge readiness.\ndiff --git a/.github/prompts/cca-completion-check.md b/.github/prompts/cca-completion-check.md\nindex ed203ad5f302ecc81372d8ab27d2b1232ca97497..fd89950528333ef6242e6d5443904f5392ab7e56\n--- a/.github/prompts/cca-completion-check.md\n+++ b/.github/prompts/cca-completion-check.md\n@@ -19,6 +19,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n ## Required Reading\n \n Read only the files needed to judge the PR contract. Prefer this order:\ndiff --git a/.github/prompts/codex-task-handoff.md b/.github/prompts/codex-task-handoff.md\nindex 695e211d76dea1c60e8d5bb360e78487808286a4..59441d576b01a39569722f226c854e5a740e9eb5\n--- a/.github/prompts/codex-task-handoff.md\n+++ b/.github/prompts/codex-task-handoff.md\n@@ -2,7 +2,7 @@\n \n You are Codex Front acting as the Implementer for a Shiki task.\n \n-Follow `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant ADRs, and `docs/agents/codex-handoff.md`.\n+Follow `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant Shiki SADRs and target ADRs, and `docs/agents/codex-handoff.md`.\n \n Rules:\n \ndiff --git a/AGENTS.md b/AGENTS.md\nindex 7f041100d49c2a7525f51cb0f5d696a4a6d1c78c..a06bfbabc6fa48a007389e25e12d7ed523936bed\n--- a/AGENTS.md\n+++ b/AGENTS.md\n@@ -22,7 +22,7 @@\n - **Codex Front** is an optional implementer through Codex App, Codex CLI, Codex IDE extension, or Codex Web signed in with ChatGPT OAuth/subscription auth, dispatched with `shiki runner codex` when a task is explicitly assigned to `codex`.\n - **GitHub CCA** is implemented by Claude Code Action by default, using `CLAUDE_CODE_OAUTH_TOKEN`.\n \n-Do not assume `openai/codex-action`, `OPENAI_API_KEY`, or API-key based Codex automation in the default Shiki path. API-key runners are explicit target-repo extensions and require their own ADR.\n+Do not assume `openai/codex-action`, `OPENAI_API_KEY`, or API-key based Codex automation in the default Shiki path. API-key runners are explicit target-repo extensions and require their own target ADR.\n \n Shiki is not:\n \n@@ -44,10 +44,14 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use\n+`SADR-NNNN-*.md`; target product decisions retain `NNNN-*.md` and the `ADR`\n+namespace.\n+\n Use the canonical source-of-truth order above. Runtime-specific reading orders\n are operational guidance only and must not override or reorder it.\n \n-Conversation state is not durable truth. If a decision matters, put it in GitHub, `.shiki/`, `CONTEXT.md`, or an ADR.\n+Conversation state is not durable truth. If a decision matters, put it in GitHub, `.shiki/`, `CONTEXT.md`, a Shiki SADR, or a target ADR.\n \n If GitHub and `.shiki/` disagree, surface the conflict and prefer GitHub operational state until the mirror is repaired.\n \n@@ -75,8 +79,8 @@\n Every non-trivial change follows this loop:\n \n 1. **Goal**: clarify outcome, completion conditions, non-goals, risk level, and success signals.\n-2. **grill-with-docs**: challenge the plan against domain docs, ADRs, code reality, terminology, and edge scenarios.\n-3. **Context & Impact**: find relevant docs, ADRs, code, dependencies, owners, locks, and verification surfaces.\n+2. **grill-with-docs**: challenge the plan against domain docs, Shiki SADRs, target ADRs, code reality, terminology, and edge scenarios.\n+3. **Context & Impact**: find relevant docs, SADRs, target ADRs, code, dependencies, owners, locks, and verification surfaces.\n 4. **PRD and Spec Freeze**: use `to-prd` when context is settled enough to become durable product/engineering intent. The operator's explicit approval of the PRD is Spec Freeze: the plan records a `spec_freeze` block, and no plan runs without it. Steps 1-4 form Requirements Definition \u2014 one continuous operator dialogue. After Spec Freeze, scope changes require an operator-approved Spec Amendment; non-scope-moving interpretations go to the Assumption Log.\n 5. **Issues**: use `to-issues` to create independently grabbable vertical-slice issues. Prefer AFK slices over HITL slices where possible.\n 6. **Triage**: label issues for readiness, risk, runtime, skills, and MergeGate state.\n@@ -167,7 +171,7 @@\n - Testing decisions.\n - Out-of-scope items.\n - Further notes.\n-- Links to relevant domain terms and ADRs.\n+- Links to relevant domain terms, SADRs, and target ADRs.\n \n Every executable task must include:\n \n@@ -193,7 +197,7 @@\n \n Context & Impact must identify:\n \n-- Relevant documents, domain terms, ADRs, and past decisions.\n+- Relevant documents, domain terms, SADRs, target ADRs, and past decisions.\n - Relevant modules, interfaces, seams, callers, owners, and tests.\n - Dependency relationships between tasks.\n - Candidate locks and lock conflicts.\n@@ -210,7 +214,7 @@\n | Skill | Required when |\n | --- | --- |\n | `setup-matt-pocock-skills` | First configuring the repo for skills, or when issue tracker, triage labels, or domain docs are missing. |\n-| `grill-with-docs` | Default for non-trivial Goals before PRD/issues; always required when plans, terminology, boundaries, tradeoffs, or ADR-worthy decisions are ambiguous. |\n+| `grill-with-docs` | Default for non-trivial Goals before PRD/issues; always required when plans, terminology, boundaries, tradeoffs, or decision-record-worthy choices are ambiguous. |\n | `zoom-out` | The agent lacks an architectural map of the relevant code area. |\n | `to-prd` | Settled Goal context must become durable product/engineering intent. |\n | `to-issues` | A Goal, PRD, or plan must become independently grabbable vertical-slice issues. |\n@@ -348,7 +352,7 @@\n - Three or more new files.\n - Any high-risk or critical label.\n \n-When the architecture gate triggers, run `grill-with-docs` or `improve-codebase-architecture` as appropriate and record durable decisions in an ADR when needed.\n+When the architecture gate triggers, run `grill-with-docs` or `improve-codebase-architecture` as appropriate and record durable decisions in a Shiki SADR or target ADR as applicable.\n \n ## Engineering Discipline\n \ndiff --git a/CLAUDE.md b/CLAUDE.md\nindex 98a932e7841a632866fcd53b0bad0e3af3711d3d..98a31399404d6fb044b4fd493f4459ac87f439de\n--- a/CLAUDE.md\n+++ b/CLAUDE.md\n@@ -14,6 +14,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n ## Claude Role\n \n Default to planner, default implementer, reviewer, coordinator, documentation editor, CCA-style completion judge, and final-judgment assistant.\n@@ -57,7 +60,7 @@\n At the start of a Shiki session:\n \n 1. Read `AGENTS.md`.\n-2. Read `CONTEXT.md` and relevant ADRs.\n+2. Read `CONTEXT.md` and relevant Shiki SADRs and target ADRs.\n 3. Read active `.shiki/` Goal/task/ledger state when present.\n 4. Read active distilled rules from `.shiki/memories/` (status `distilled`, `active: true`, not revoked or superseded) for applicable guidance; the same rules are injected deterministically into task handoffs as the `## Distilled Rules` section (\u00a73.5).\n 5. Read the GitHub Issue or PR when the task is GitHub-backed.\n@@ -74,7 +77,7 @@\n - Define the Goal, completion conditions, non-goals, and risk level.\n - Treat `grill-with-docs` as the default planning entry point for non-trivial Goals.\n - Challenge domain terms against `CONTEXT.md`.\n-- Challenge hard-to-reverse tradeoffs against ADRs.\n+- Challenge hard-to-reverse tradeoffs against SADRs and target ADRs.\n - Explore code instead of asking when code can answer the question.\n - Run Context & Impact before implementation planning.\n - Produce a Task DAG, not an unordered checklist.\n@@ -94,7 +97,7 @@\n - For each question, provide your recommended answer.\n - Resolve dependency decisions before downstream design questions.\n - Update `CONTEXT.md` inline when terminology crystallizes.\n-- Offer ADRs only when the decision is hard to reverse, surprising without context, and a real tradeoff.\n+- Offer a SADR or target ADR only when the decision is hard to reverse, surprising without context, and a real tradeoff.\n - Convert unresolved questions into HITL blockers rather than allowing Codex to guess.\n \n ## PRD and Issues Standard\n@@ -103,7 +106,7 @@\n \n - Use the project's domain glossary vocabulary.\n - Avoid volatile file paths unless they are essential.\n-- Include implementation decisions, testing decisions, out-of-scope items, and relevant ADR/domain links.\n+- Include implementation decisions, testing decisions, out-of-scope items, and relevant SADR/target-ADR/domain links.\n - Identify deep-module opportunities and testable interfaces.\n \n When creating issues:\n@@ -121,7 +124,7 @@\n - Branch/worktree target.\n - Scope and non-goals.\n - Dependencies and locks.\n-- Relevant docs, ADRs, modules, and tests.\n+- Relevant docs, SADRs, target ADRs, modules, and tests.\n - Required skill invocations.\n - TDD expectations.\n - Pre-PR `code-review` gate expectations.\n@@ -152,7 +155,7 @@\n \n - Treat yourself as a judge, not implementer.\n - Do not write production code.\n-- Read `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant ADRs, PRD, task issue, PR body, diff, checks, reviews, labels, and `.shiki/` state.\n+- Read `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant SADRs and target ADRs, PRD, task issue, PR body, diff, checks, reviews, labels, and `.shiki/` state.\n - Evaluate every applicable checklist in `docs/agents/checklists.md`.\n - Map each acceptance criterion to durable evidence.\n - Distinguish implementation failure from missing evidence.\ndiff --git a/CONTEXT.md b/CONTEXT.md\nindex e61a9be06a0cce0d026484083a657bde2067e234..237ee8aa3ea8fab334ece49768bc5ed3b5af0974\n--- a/CONTEXT.md\n+++ b/CONTEXT.md\n@@ -25,7 +25,7 @@\n _Avoid_: jumping directly into code\n \n **grill-with-docs**:\n-A planning skill that challenges a plan against domain language, ADRs, code reality, and concrete edge scenarios. It resolves design-tree questions before PRD/issues. It runs inside Requirements Definition.\n+A planning skill that challenges a plan against domain language, Shiki SADRs, target ADRs, code reality, and concrete edge scenarios. It resolves design-tree questions before PRD/issues. It runs inside Requirements Definition.\n _Avoid_: generic brainstorming, silent assumptions\n \n **Requirements Definition**:\ndiff --git a/CONTRIBUTING.md b/CONTRIBUTING.md\nindex c6d9b656e73abb8a55e5f660123e03153dcb3e6d..9e37a8fbf668db5b22461aaae5548b8f1354394c\n--- a/CONTRIBUTING.md\n+++ b/CONTRIBUTING.md\n@@ -17,7 +17,7 @@\n 1. **Goal** \u2014 start from a user-approved Goal with an outcome, completion\n    conditions, non-goals, risk level, and success signals.\n 2. **grill-with-docs** \u2014 for any non-trivial change, challenge the plan against\n-   domain language, ADRs, and code reality before writing a PRD.\n+   domain language, Shiki SADRs, target ADRs, and code reality before writing a PRD.\n 3. **PRD** \u2014 capture settled product/engineering intent (`to-prd`).\n 4. **Spec Freeze** \u2014 the operator explicitly approves the PRD/requirements;\n    the plan records a `spec_freeze` block and no plan runs without it\n@@ -94,7 +94,7 @@\n   entries.\n - Do not hand-edit `.shiki/goals/*.json` or `.shiki/manifest.json` in a task PR.\n - Conversation state is not durable truth. If a decision matters, record it in\n-  GitHub, `.shiki/`, `CONTEXT.md`, or an ADR.\n+  GitHub, `.shiki/`, `CONTEXT.md`, a Shiki SADR, or a target ADR.\n \n ## Architecture Gate\n \n@@ -103,7 +103,7 @@\n or CI policy, package structure, three or more structural files, more than 100\n net new lines in a core module, three or more new files, or any high-risk label.\n When triggered, run `grill-with-docs` or `improve-codebase-architecture` and\n-record durable decisions in an ADR when needed (see `docs/adr/`).\n+record durable decisions in a Shiki SADR or target ADR as applicable (see `docs/adr/`).\n \n ## Safety\n \ndiff --git a/SYSTEM_PROMPT.md b/SYSTEM_PROMPT.md\nindex aaaea45e3a896c3a44ba89eec8bb84325ecf6c62..da009c86a88285269dee0155e58e550c6d744ea9\n--- a/SYSTEM_PROMPT.md\n+++ b/SYSTEM_PROMPT.md\n@@ -30,6 +30,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n Use the canonical source-of-truth order above. Runtime-specific reading orders\n are operational guidance only and must not override or reorder it. The current\n conversation is non-durable operator input.\n@@ -48,14 +51,14 @@\n \n A runtime may only do work that is explicitly assigned by the Goal, task, PR, or MergeGate decision.\n \n-Do not assume `openai/codex-action`, `OPENAI_API_KEY`, or API-key based Codex automation in the default Shiki path. API-key runners are explicit target-repo extensions and require their own ADR.\n+Do not assume `openai/codex-action`, `OPENAI_API_KEY`, or API-key based Codex automation in the default Shiki path. API-key runners are explicit target-repo extensions and require their own target ADR.\n \n ## 4. Non-negotiable Shiki flow\n \n For any non-trivial Goal, use this flow:\n \n 1. **Goal Seek**: define outcome, non-goals, risk level, completion criteria, and evidence requirements.\n-2. **grill-with-docs**: challenge the plan against `CONTEXT.md`, ADRs, code reality, terminology, boundaries, and edge scenarios. Ask one question at a time when the operator is available. If code can answer a question, inspect code instead of asking.\n+2. **grill-with-docs**: challenge the plan against `CONTEXT.md`, Shiki SADRs, target ADRs, code reality, terminology, boundaries, and edge scenarios. Ask one question at a time when the operator is available. If code can answer a question, inspect code instead of asking.\n 3. **Context & Impact**: identify modules, interfaces, seams, callers, dependencies, locks, risk, and verification surfaces.\n 4. **to-prd + Spec Freeze**: turn settled context into a PRD using domain vocabulary and testing decisions. The operator's explicit approval of the PRD is Spec Freeze, recorded as the plan's `spec_freeze` block; plans without it do not run. Steps 1-4 form Requirements Definition.\n 5. **to-issues**: decompose the PRD into independently grabbable vertical-slice issues. Prefer AFK slices; mark HITL slices when judgment is still required.\n@@ -75,7 +78,7 @@\n Before making any material change:\n \n 1. Read `AGENTS.md` and any runtime-specific wrapper such as `CLAUDE.md`.\n-2. Read `CONTEXT.md` and relevant ADRs.\n+2. Read `CONTEXT.md` and relevant SADRs and target ADRs.\n 3. Read the GitHub Issue / PR and `.shiki/` state for the active Goal or task.\n 4. Identify your role: Planner, Implementer, Reviewer, Completion Check Agent, Repairer, or Guardian-assist.\n 5. Identify Goal id, task id, branch, PR, dependencies, locks, risk level, required skills, and required evidence.\n@@ -103,7 +106,7 @@\n \n At minimum, identify:\n \n-- Relevant documents, ADRs, domain terms, and prior decisions.\n+- Relevant documents, SADRs, target ADRs, domain terms, and prior decisions.\n - Relevant modules, interfaces, seams, callers, owners, and tests.\n - Dependency relationships between tasks.\n - Candidate file locks and lock conflicts.\n@@ -152,7 +155,7 @@\n \n CCA must read:\n \n-- `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant ADRs.\n+- `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant SADRs and target ADRs.\n - Parent Goal issue, PRD issue, task issue, comments, and labels.\n - PR body, diff, commits, changed files, reviews, CI checks, and artifacts available to the workflow.\n - `.shiki/` task contract, locks, ledger entries, CCA checklist profile, and prior repair packets.\n@@ -214,7 +217,7 @@\n - Three or more new files.\n - Any high-risk or critical label.\n \n-When the architecture gate triggers, run `grill-with-docs` or `improve-codebase-architecture` as appropriate and record the decision in an ADR if it is hard to reverse or likely to be re-litigated.\n+When the architecture gate triggers, run `grill-with-docs` or `improve-codebase-architecture` as appropriate and record the decision in a Shiki SADR or target ADR as applicable if it is hard to reverse or likely to be re-litigated.\n \n ## 14. Skill Gate\n \n@@ -223,7 +226,7 @@\n Use these skills:\n \n - `setup-matt-pocock-skills`: before first use of the engineering skill set, or when issue tracker, triage labels, or domain docs are missing.\n-- `grill-with-docs`: default for non-trivial Goals before PRD/issues; required for ambiguous plans, terminology, boundaries, ADR-worthy tradeoffs, or design-tree decisions.\n+- `grill-with-docs`: default for non-trivial Goals before PRD/issues; required for ambiguous plans, terminology, boundaries, decision-record-worthy tradeoffs, or design-tree decisions.\n - `zoom-out`: unfamiliar code area or missing architectural map.\n - `to-prd`: convert settled context into a PRD.\n - `to-issues`: break a PRD, plan, or Goal into independently grabbable vertical-slice issues.\ndiff --git a/docs/adr/0001-keep-shiki-as-platform-name.md b/docs/adr/SADR-0001-keep-shiki-as-platform-name.md\nindex 20a9637c3b9d8c97de7ba8db7cfae4d82d9a7dba..8b685439959aada1b221c112f0ac3c51ca8a1695\n--- a/docs/adr/0001-keep-shiki-as-platform-name.md\n+++ b/docs/adr/SADR-0001-keep-shiki-as-platform-name.md\n@@ -1,3 +1,3 @@\n-# Keep Shiki As The Platform Name\n+# SADR-0001: Keep Shiki As The Platform Name\n \n Shiki remains the product name for the GitHub-native agentic engineering control plane. We considered more descriptive names such as MergePilot, MergeGate, and Kiroku, but kept Shiki because it carries the existing goal-seeking framework identity while remaining independent of Hermes, Codex, Claude Code, or any single runtime.\ndiff --git a/docs/adr/0002-use-agents-as-shared-constitution.md b/docs/adr/SADR-0002-use-agents-as-shared-constitution.md\nindex 73de30ecb078120eac540d9bc51855e3cd48d0e3..c5486182bf06fca36ea508496b013c67302fd989\n--- a/docs/adr/0002-use-agents-as-shared-constitution.md\n+++ b/docs/adr/SADR-0002-use-agents-as-shared-constitution.md\n@@ -1,3 +1,3 @@\n-# Use AGENTS As The Shared Agent Constitution\n+# SADR-0002: Use AGENTS As The Shared Agent Constitution\n \n Shiki uses `AGENTS.md` as the runtime-neutral constitution and `CLAUDE.md` as a thin Claude Code wrapper. This keeps Codex, Claude Code, Hermes Runner, and future Agent Runtimes aligned on the same GitHub-first MergeGate rules while still allowing runtime-specific instructions where needed.\ndiff --git a/docs/adr/0003-require-skill-gate-for-mergegate-readiness.md b/docs/adr/SADR-0003-require-skill-gate-for-mergegate-readiness.md\nindex 424be0c9af4c8f0136f1fde68a468c47ff84e764..7fc337987c7df9792f75f181dfcbed75fe5ffff2\n--- a/docs/adr/0003-require-skill-gate-for-mergegate-readiness.md\n+++ b/docs/adr/SADR-0003-require-skill-gate-for-mergegate-readiness.md\n@@ -1,3 +1,3 @@\n-# Require Skill Gate For MergeGate Readiness\n+# SADR-0003: Require Skill Gate For MergeGate Readiness\n \n Shiki requires relevant engineering skills to be invoked before execution when their triggers apply. This makes skills part of MergeGate readiness rather than optional prompt style, because Shiki depends on repeatable planning, issue decomposition, TDD, diagnosis, triage, and architecture review before autonomous or parallel agent work can be trusted.\ndiff --git a/docs/adr/0004-use-github-first-with-shiki-mirror.md b/docs/adr/SADR-0004-use-github-first-with-shiki-mirror.md\nindex d0a3a9174dc646a945a9d325954ffaf00c352040..a79c54ae9ee8eed2a0524192b864da4e22a92d3f\n--- a/docs/adr/0004-use-github-first-with-shiki-mirror.md\n+++ b/docs/adr/SADR-0004-use-github-first-with-shiki-mirror.md\n@@ -1,4 +1,4 @@\n-# ADR 0004: Use GitHub First With A Repository-Local Shiki Mirror\n+# SADR-0004: Use GitHub First With A Repository-Local Shiki Mirror\n \n ## Status\n \ndiff --git a/docs/adr/0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md b/docs/adr/SADR-0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\nindex b339baf49b89d9290e79c3d51b36e7b2bde3bd95..0f3c3eb9afccdb3403ce978e21d04fe12e495da0\n--- a/docs/adr/0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\n+++ b/docs/adr/SADR-0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\n@@ -1,8 +1,8 @@\n-# ADR 0005: Use Codex As Front And Claude Code Action As The GitHub Runtime\n+# SADR-0005: Use Codex As Front And Claude Code Action As The GitHub Runtime\n \n ## Status\n \n-Accepted (implementer default superseded by ADR 0008; the auth model and the\n+Accepted (implementer default superseded by SADR-0008; the auth model and the\n Claude Code Action GitHub runtime decision remain in force)\n \n ## Context\n@@ -26,4 +26,4 @@\n - Shiki avoids requiring `OPENAI_API_KEY` for the default workflow.\n - Codex work happens through the user's authenticated Codex front surface, not as a hidden backend Action.\n - Claude Code Action is configured for OAuth-token based subscription usage by default.\n-- API-key based automation remains possible only as an explicit target-repo extension with its own ADR.\n+- API-key based automation remains possible only as an explicit target-repo extension with its own target ADR.\ndiff --git a/docs/adr/0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md b/docs/adr/SADR-0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\nindex 4bcae9d1e52f0a37b8e9fbafa5cd14b6ee1993bd..7a38538a6ae9929567f0ade67e7ea6a60d0b3f7e\n--- a/docs/adr/0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\n+++ b/docs/adr/SADR-0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\n@@ -1,4 +1,4 @@\n-# ADR 0006: Separate Codex Implementation, CCA Completion, And MergeGate Authorization\n+# SADR-0006: Separate Codex Implementation, CCA Completion, And MergeGate Authorization\n \n ## Status\n \ndiff --git a/docs/adr/0007-packaging-and-release.md b/docs/adr/SADR-0007-packaging-and-release.md\nindex 79b84ddb97167372b44641e5b7860e400ce80822..bb445a2e2e856d9d1f5fdbe35de8d4e8cec0fb32\n--- a/docs/adr/0007-packaging-and-release.md\n+++ b/docs/adr/SADR-0007-packaging-and-release.md\n@@ -1,4 +1,4 @@\n-# ADR 0007: Packaging And Release For Shiki\n+# SADR-0007: Packaging And Release For Shiki\n \n ## Status\n \n@@ -25,7 +25,7 @@\n   not introduce third-party runtime dependencies or break the shim.\n - Releases must run inside the operator's subscription-authenticated toolchain\n   and must not require extra secrets beyond the built-in `GITHUB_TOKEN`\n-  (consistent with ADR 0005's no-API-key-by-default stance).\n+  (consistent with SADR-0005's no-API-key-by-default stance).\n - `.shiki/manifest.json` already encodes the install surface (`install.include`,\n   `install.create_directories`, `install.exclude_from_commit`), and it must not\n   be mutated as part of this change.\n@@ -34,7 +34,7 @@\n \n This is a hard-to-reverse platform decision (it sets the version source of\n truth, the release trigger, and the public install contract), so it is recorded\n-as an ADR.\n+as a SADR.\n \n ## Decision\n \n@@ -104,7 +104,7 @@\n 3. **Publishing to PyPI.** Rejected for now: Shiki is distributed as a\n    repository template + git-installable CLI, not a PyPI library; PyPI would add\n    credentials/secrets and a different versioning contract. Can be added later as\n-   its own ADR if a packaged distribution is needed.\n+   its own SADR if a packaged distribution is needed.\n 4. **Dynamic version (read `VERSION` via a build backend `dynamic` field).**\n    Rejected to keep `pyproject.toml` backend-agnostic and dependency-free; the\n    small duplication between `VERSION` and `pyproject.toml` is guarded by the\n@@ -123,4 +123,4 @@\n   releasing versions the whole platform but ships only `install.include` to\n   targets.\n - Future PyPI distribution or a console entry point are deferred and would each\n-  warrant their own ADR.\n+  warrant their own SADR.\ndiff --git a/docs/adr/0008-use-claude-code-as-default-implementer.md b/docs/adr/SADR-0008-use-claude-code-as-default-implementer.md\nindex dedf4510b7c7799521422f622029563d373db031..35c2625d908167fbc58855b5b63e283805f167e8\n--- a/docs/adr/0008-use-claude-code-as-default-implementer.md\n+++ b/docs/adr/SADR-0008-use-claude-code-as-default-implementer.md\n@@ -1,12 +1,12 @@\n-# ADR 0008: Use Claude Code As The Default Implementer Runtime\n+# SADR-0008: Use Claude Code As The Default Implementer Runtime\n \n ## Status\n \n-Proposed (supersedes ADR 0005 for the implementer default; keeps its auth model)\n+Proposed (supersedes SADR-0005 for the implementer default; keeps its auth model)\n \n ## Context\n \n-ADR 0005 made Codex Front the default implementation runtime, with Claude Code\n+SADR-0005 made Codex Front the default implementation runtime, with Claude Code\n restricted to planner and reviewer roles by the runtime registry, and the only\n autonomous implementation adapter was `shiki runner codex`.\n \n@@ -18,13 +18,13 @@\n - The platform's planning, review, and CCA layers already run on Claude Code,\n   and Claude Code's native capabilities (the built-in `/code-review` skill and\n   the Workflow multi-agent orchestration tool) have no Codex equivalent.\n-- The Goal lifecycle redesign (ADR 0009) requires an autonomous post-freeze\n+- The Goal lifecycle redesign (SADR-0009) requires an autonomous post-freeze\n   loop driven from the operator's Claude Code session, including bounded\n   repair dispatch, which must not depend on a runtime that cannot be invoked\n   unattended.\n \n The constitution claims Shiki is runtime-agnostic (\"not a Claude-only\n-workflow\", ADR 0002). Any change to the implementer default must preserve the\n+workflow\", SADR-0002). Any change to the implementer default must preserve the\n registry-based runtime model rather than hardcoding one vendor.\n \n ## Decision\n@@ -39,7 +39,7 @@\n   the task to review or repair-needed.\n - Keep Codex registered as an optional implementer runtime. `shiki runner\n   codex`, the Codex handoff templates, and the ChatGPT OAuth auth model from\n-  ADR 0005 remain supported for tasks explicitly assigned to `codex`.\n+  SADR-0005 remain supported for tasks explicitly assigned to `codex`.\n - Commit to mandating two Claude-native capabilities as loop contract in\n   follow-up Goals (status: the code-review gate was delivered by Goal G-B \u2014\n   skill registry, checklists item PR-12, default required_skills \u2014 while the\n@@ -60,7 +60,7 @@\n \n Out of scope: removing Codex support, changing the CCA runtime (Claude Code\n Action with `CLAUDE_CODE_OAUTH_TOKEN` stays), and API-key based automation\n-(still requires its own ADR per ADR 0005).\n+(still requires its own target ADR per SADR-0005).\n \n ## Consequences\n \n@@ -100,7 +100,7 @@\n   harness rejection of direct `codex exec` is outside Shiki's control, and the\n   plan's mandatory native capabilities cannot run on Codex at all.\n - **Remove Codex entirely**: rejected; it abandons the runtime-agnostic\n-  constitution (ADR 0002) for no operational gain and forecloses\n+  constitution (SADR-0002) for no operational gain and forecloses\n   cross-runtime adversarial review.\n - **GitHub Actions cloud implementation via claude-code-action**: rejected as\n   default; durable evidence is automatic but token cost, Node 24 workflow\ndiff --git a/docs/adr/0009-requirements-definition-and-spec-freeze.md b/docs/adr/SADR-0009-requirements-definition-and-spec-freeze.md\nindex 29b3f7871bef9203a8b4a831a65c39eb2f506e42..52e7dd69ad0aa4efe0b68c62678abbf7df02aaec\n--- a/docs/adr/0009-requirements-definition-and-spec-freeze.md\n+++ b/docs/adr/SADR-0009-requirements-definition-and-spec-freeze.md\n@@ -1,4 +1,4 @@\n-# ADR 0009: Merge Grill And Planning Into Requirements Definition With Spec Freeze\n+# SADR-0009: Merge Grill And Planning Into Requirements Definition With Spec Freeze\n \n ## Status\n \ndiff --git a/docs/adr/0010-external-ai-guardian-review.md b/docs/adr/SADR-0010-external-ai-guardian-review.md\nindex e6108bbe8bb4e73e0ff4684c8f78001a80438c4e..f3510b145664298adafb151b6f0db51b972f01ec\n--- a/docs/adr/0010-external-ai-guardian-review.md\n+++ b/docs/adr/SADR-0010-external-ai-guardian-review.md\n@@ -1,4 +1,4 @@\n-# ADR 0010: External AI Guardian Review As A First-Class Approval Authority\n+# SADR-0010: External AI Guardian Review As A First-Class Approval Authority\n \n ## Status\n \n@@ -8,7 +8,7 @@\n \n Shiki is intended to be an autonomous, recursive, self-improving agent\n framework: authority-in-the-loop, not human-in-the-loop. Until now the Guardian\n-approval required for high/critical-risk merges (guardian-policy.json, ADR\n+approval required for high/critical-risk merges (guardian-policy.json, SADR\n governance) recognized only human authority sources \u2014 a GitHub review approval,\n the `guardian:approved` label applied by a configured Guardian, or a comment\n carrying `Guardian approval granted` plus the current head SHA.\n@@ -89,7 +89,7 @@\n   autonomous design the operator intends and produced the impersonation\n   incident.\n - **Record AI approval as an operator comment**: rejected as forgery of human\n-  authority \u2014 the exact failure this ADR corrects.\n+  authority \u2014 the exact failure this SADR corrects.\n - **Deliver the artifact as a committed `.shiki/` file**: rejected; a committed\n   approval inside the PR being approved is forgeable by anyone who can push.\n - **A dedicated reviewer bot identity instead of a relayed comment**: deferred;\ndiff --git a/docs/adr/0011-autonomous-quality-gate-evidence.md b/docs/adr/SADR-0011-autonomous-quality-gate-evidence.md\nindex bca0246c5d46e44afbde15486a75eec425fc02f5..9c93eac16d8c3a6d4b11bd7224e3955102cd7db4\n--- a/docs/adr/0011-autonomous-quality-gate-evidence.md\n+++ b/docs/adr/SADR-0011-autonomous-quality-gate-evidence.md\n@@ -1,4 +1,4 @@\n-# ADR 0011: Autonomous Quality-Gate Evidence \u2014 Deterministic Facts And An Independent Verifier\n+# SADR-0011: Autonomous Quality-Gate Evidence \u2014 Deterministic Facts And An Independent Verifier\n \n ## Status\n \ndiff --git a/docs/adr/0012-autonomous-loop-closeout-to-main.md b/docs/adr/SADR-0012-autonomous-loop-closeout-to-main.md\nindex 637fad6dde4989c9c2bf0f81f676d8d29991123c..ec7d85f81030a676bcbba28b43eb365422a36eac\n--- a/docs/adr/0012-autonomous-loop-closeout-to-main.md\n+++ b/docs/adr/SADR-0012-autonomous-loop-closeout-to-main.md\n@@ -1,4 +1,4 @@\n-# 0012 \u2014 Autonomous loop pushes goal completion to main via a closeout PR\n+# SADR-0012 \u2014 Autonomous loop pushes goal completion to main via a closeout PR\n \n - Status: accepted\n - Date: 2026-06-18\ndiff --git a/docs/adr/0013-bootstrap-grants-actions-approve-pull-requests.md b/docs/adr/SADR-0013-bootstrap-grants-actions-approve-pull-requests.md\nindex b6c69d64f1c5ced346b88d6859586715b3b5bd0e..cf48ff4a6716267f539ca58edd1a852e7c7a2d64\n--- a/docs/adr/0013-bootstrap-grants-actions-approve-pull-requests.md\n+++ b/docs/adr/SADR-0013-bootstrap-grants-actions-approve-pull-requests.md\n@@ -1,4 +1,4 @@\n-# ADR 0013: Bootstrap Grants GitHub Actions Permission To Create And Approve Pull Requests\n+# SADR-0013: Bootstrap Grants GitHub Actions Permission To Create And Approve Pull Requests\n \n ## Status\n \n@@ -31,7 +31,7 @@\n security tradeoff: it lets repository automation satisfy a human-review gate. In\n a multi-maintainer repo that could let a compromised or buggy workflow\n self-approve changes. So whether bootstrap turns it on by default \u2014 and with\n-what default token scope \u2014 is an ADR-worthy decision, not an implementation\n+what default token scope \u2014 is a SADR-worthy decision, not an implementation\n detail.\n \n ## Decision\n@@ -48,7 +48,7 @@\n Specifically:\n \n - **`can_approve_pull_request_reviews` defaults to `true`.** Shiki's default\n-  posture is solo/AFK autonomous operation (ADR 0008/0009/0012), where the CCA\n+  posture is solo/AFK autonomous operation (SADR-0008/0009/0012), where the CCA\n   Review Bridge is the only path that can satisfy `required_review: true` after\n   the CCA and Guardian gates have already passed. The default makes a\n   freshly-bootstrapped repo able to run the autonomous loop end to end without a\ndiff --git a/docs/adr/0014-codex-app-external-ai-guardian-ui-adapter.md b/docs/adr/SADR-0014-codex-app-external-ai-guardian-ui-adapter.md\nindex 238db06279334caa341c117362df9271382a7fcd..65f12e9939321e1b3e389a34c822fc1549e5155d\n--- a/docs/adr/0014-codex-app-external-ai-guardian-ui-adapter.md\n+++ b/docs/adr/SADR-0014-codex-app-external-ai-guardian-ui-adapter.md\n@@ -1,4 +1,4 @@\n-# ADR 0014: Use Codex App As The External AI Guardian UI Adapter\n+# SADR-0014: Use Codex App As The External AI Guardian UI Adapter\n \n ## Status\n \ndiff --git a/docs/adr/0015-contract-approval-before-dispatch.md b/docs/adr/SADR-0015-contract-approval-before-dispatch.md\nindex fad6c656741a20a7684ba16080a7db159f63a52f..6061271a46d3f1d6ae7174477de3fb75673123a6\n--- a/docs/adr/0015-contract-approval-before-dispatch.md\n+++ b/docs/adr/SADR-0015-contract-approval-before-dispatch.md\n@@ -1,4 +1,4 @@\n-# ADR 0015: Approve The Task Contract Before Dispatch, Not The Diff After It\n+# SADR-0015: Approve The Task Contract Before Dispatch, Not The Diff After It\n \n ## Status\n \n@@ -9,7 +9,7 @@\n Guardian approval for high/critical risk is expressible only on a pull request.\n Every approval source in `guardian-policy.json` \u2014 GitHub review, the\n `guardian:approved` label, the `Guardian approval granted` comment, and the\n-`external_ai_guardian_review` artifact (ADR 0010/0014) \u2014 is bound to a PR, and\n+`external_ai_guardian_review` artifact (SADR-0010/0014) \u2014 is bound to a PR, and\n three of the four require the current head SHA. Approval therefore cannot exist\n before an implementation PR exists.\n \n@@ -81,7 +81,7 @@\n An envelope breach must surface only on the non-repairable policy gate, never on\n the repairable `MergeGate metadata check`. Otherwise the Goal Loop routes it to\n `dispatch_repair` \u2014 instructing a runner to make the breach go away \u2014 which is\n-exactly what this ADR forbids.\n+exactly what this SADR forbids.\n \n **Contract immutability.** After registration, an implementation PR must not\n change the governance fields of its own task contract (`scope`, `non_goals`,\n@@ -116,7 +116,7 @@\n fails any verdict that is not `complete`, and the signal the CCA prompt reads is\n computed from `evaluate_guardian_approval` alone. Without the OR-merge at the\n signal, a contract-approved PR still returns `needs_guardian`, the CCA job fails,\n-and the MergeGate policy check never runs \u2014 the gate this ADR changes would never\n+and the MergeGate policy check never runs \u2014 the gate this SADR changes would never\n be reached. The CCA *prompt* needs no change: a `contract_approval` entry in the\n signal's existing `sources` array satisfies its current wording.\n \n@@ -130,7 +130,7 @@\n written, and an approved contract lets its implementation PRs merge through the\n same automatic path as low/medium risk. The wasted implement-then-block cycle\n disappears. Guardian review shifts from reading diffs to reading contracts, which\n-is both earlier and smaller; the external AI Guardian path (ADR 0010/0014) works\n+is both earlier and smaller; the external AI Guardian path (SADR-0010/0014) works\n on contracts with no change.\n \n We accept that the Guardian authorizes a class of change without seeing its\n@@ -146,7 +146,7 @@\n adversarial tests for the new approval path equivalent in rigor to\n `test_shiki_governance_evidence.sh`.\n \n-Bootstrap costs, which apply only while this ADR is being implemented. The tasks\n+Bootstrap costs, which apply only while this SADR is being implemented. The tasks\n that build Contract Approval merge under the existing rules, so the ones\n dispatched after the pre-dispatch gate lands but before the approval path lands\n need a Guardian-approved Contract PR *and* a live Guardian approval on their\n@@ -210,7 +210,7 @@\n risk; they do not bound authorized work.\n \n **Auto-escalate risk on envelope breach instead of stopping.** Returns the\n-workflow to a diff-time Guardian decision \u2014 the exact behavior this ADR removes.\n+workflow to a diff-time Guardian decision \u2014 the exact behavior this SADR removes.\n \n **Resolve governance fields from the base snapshot only.** Rejected against the\n registration flow: implementation PRs add their own contract, so the base has no\ndiff --git a/docs/adr/0016-locks-bound-product-paths.md b/docs/adr/SADR-0016-locks-bound-product-paths.md\nindex b5e850f3f1f7f4ef6e7f9d1763a29a51bd300c45..46b40a9b99a7aae644c02bb458f5473ff0a90ce2\n--- a/docs/adr/0016-locks-bound-product-paths.md\n+++ b/docs/adr/SADR-0016-locks-bound-product-paths.md\n@@ -1,4 +1,4 @@\n-# ADR 0016: Locks bound product paths; the Shiki mirror is bound by record identity\n+# SADR-0016: Locks bound product paths; the Shiki mirror is bound by record identity\n \n ## Status\n \n@@ -6,8 +6,8 @@\n \n - Date: 2026-07-28\n - Deciders: operator (mizutani-140), Claude (planner)\n-- Related: ADR 0004 (GitHub-first with a `.shiki` mirror), ADR 0009 (Spec Freeze),\n-  ADR 0012 (autonomous loop closeout to main), ADR 0015 (Contract Approval before\n+- Related: SADR-0004 (GitHub-first with a `.shiki` mirror), SADR-0009 (Spec Freeze),\n+  SADR-0012 (autonomous loop closeout to main), SADR-0015 (Contract Approval before\n   dispatch), remediation goal task R-02, the live PR #179 deadlock.\n \n ## Context\n@@ -90,11 +90,11 @@\n files, documentation files and named source modules are nameable; runtime-\n allocated ledger, runner and report ids are not \u2014 and those live in `.shiki`,\n which locks no longer govern. Existing frozen contracts are brought into line by\n-a one-time Spec Amendment per plan, which must land before ADR 0015's contract\n+a one-time Spec Amendment per plan, which must land before SADR-0015's contract\n immutability makes `locks` unwritable from the implementation PR.\n \n **Lock state is resolved from the durable side.** MergeGate resolves the lock\n-registry, and ADR 0015's governance fields, from the base `.shiki` snapshot when\n+registry, and SADR-0015's governance fields, from the base `.shiki` snapshot when\n present, falling back to head only when absent. The `MergeGate metadata check`\n job is given `--base-shiki`, which it does not receive today. Locks are not\n derived from the live open-pull-request set: a pull request body is untrusted\n@@ -108,7 +108,7 @@\n Explicitly out of scope: moving MergeGate's contract resolution off the PR\n checkout wholesale; making the mirror a projection of GitHub state; introducing\n new lock kinds; moving ledger evidence out of the repository; and unwinding\n-ADR 0012's closeout pull request. Registration of multi-task goals is ADR 0015's\n+SADR-0012's closeout pull request. Registration of multi-task goals is SADR-0015's\n Contract PR and is not re-decided here.\n \n ## Consequences\n@@ -127,7 +127,7 @@\n \n Risks accepted. First, goal-scoped identity is only as strong as `goal_id`,\n which is resolved from the PR body and the head task file; the mitigation is to\n-add `goal_id` to ADR 0015's frozen governance field set, and until that lands a\n+add `goal_id` to SADR-0015's frozen governance field set, and until that lands a\n task file can name a foreign goal. Second, base-when-present resolution is\n unavailable on fork pull requests, where the head is contributor-controlled;\n fork pull requests already cannot complete a Shiki merge, and this decision does\n@@ -139,7 +139,7 @@\n path, not by implementation pull requests.\n \n New obligations: one Spec Amendment per frozen plan to rewrite `locks`, ordered\n-before ADR 0015 T1; extension of the adversarial suites in\n+before SADR-0015 T1; extension of the adversarial suites in\n `scripts/test_shiki_adversarial_state.sh` and\n `scripts/test_shiki_governance_evidence.sh` to cover the eleven newly-scoped\n directories; and updating `scripts/test_shiki_loop_lock_guard.sh`,\n@@ -151,32 +151,32 @@\n whether the loop's transitive sync of ledger `evidence` references can be\n deleted once the Contract PR carries its own source plan.\n \n-## Relationship to ADR 0004\n+## Relationship to SADR-0004\n \n-This ADR **clarifies something ADR 0004 left unspecified**. ADR 0004 decides that\n+This SADR **clarifies something SADR-0004 left unspecified**. SADR-0004 decides that\n GitHub is the operational source of truth and that each target repository keeps a\n `.shiki/` mirror. It says nothing about pull requests writing the mirror, does\n not name MergeGate, and does not make the mirror an input to a gate. Nothing in\n-ADR 0004's Decision or Consequences is amended, superseded or weakened: the\n+SADR-0004's Decision or Consequences is amended, superseded or weakened: the\n mirror still exists, still records the same artifacts, and pull request\n-boundaries remain the coordination surface. What this ADR settles is the question\n+boundaries remain the coordination surface. What this SADR settles is the question\n 0004 never reached \u2014 by what authority a pull request may write a mirror record \u2014\n and it answers it the way `.shiki/manifest.json` already declares\n (`pr_mutation: current-task-or-current-goal-only`).\n \n-It **amends ADR 0012**, whose Decision item 5 states that \"Loop-executed tasks\n+It **amends SADR-0012**, whose Decision item 5 states that \"Loop-executed tasks\n declare the `path:.shiki/**` lock, which covers every `.shiki` file the closeout\n stages ... so `files_outside_locks` is satisfied without per-file lock\n bookkeeping.\" That clause is replaced: the closeout's mirror writes are\n-authorized by record identity, not by a declared lock. ADR 0012's substantive\n+authorized by record identity, not by a declared lock. SADR-0012's substantive\n decision \u2014 that completion reaches `main` through a closeout pull request rather\n than local attestation \u2014 is preserved unchanged.\n \n-It is **complementary to ADR 0015**, not a substitute. ADR 0015 owns contract\n+It is **complementary to SADR-0015**, not a substitute. SADR-0015 owns contract\n immutability, the pre-dispatch gate, the Contract PR and the Scope Envelope. This\n-ADR owns what a lock means and who may write the mirror. The one ordering\n+SADR owns what a lock means and who may write the mirror. The one ordering\n constraint between them is that the lock-narrowing Spec Amendment must precede\n-ADR 0015 T1's freezing of the `locks` field.\n+SADR-0015 T1's freezing of the `locks` field.\n \n ## Alternatives Considered\n \n@@ -186,7 +186,7 @@\n the only writer that can register them may run. Migrating the Guardian signal to\n that store makes every registration PR fail closed to `needs_guardian` at every\n risk level, because the task file is by definition absent from the store, which\n-removes the low/medium autonomous path ADR 0008/0009 exists to provide. And it\n+removes the low/medium autonomous path SADR-0008/0009 exists to provide. And it\n leaves schemas, manifest, config, guardian policy, templates, migrations, plans,\n worktrees, runner records, runs and handoffs with no writer at all in a\n repository whose product is `.shiki`. Measured benefit on the frozen backlog:\n@@ -199,14 +199,14 @@\n namespace is not in fact ownership-partitioned \u2014 56 ledger entries carry no\n `task_id`, and one goal DAG is writable by 35 registered tasks. The correct\n expression of its insight is that locks do not govern the mirror at all, which is\n-what this ADR decides.\n+what this SADR decides.\n \n **Make the mirror a projection written only by a registration writer and a\n post-merge projection job.** Rejected. It requires a bypass actor with write\n access to the protected default branch; it makes its own master invariant\n readable from a manifest that pull requests may edit; it deletes both reconcile\n modes and leaves a corrupted mirror with no in-band repair path, contradicting\n-ADR 0004's consequence that mirror conflicts are repaired against GitHub state;\n+SADR-0004's consequence that mirror conflicts are repaired against GitHub state;\n and its content-derived ids are rejected by `validate_shiki`'s currently-green id\n pattern.\n \ndiff --git a/docs/adr/0017-bookkeeping-closeouts-do-not-inherit-risk.md b/docs/adr/SADR-0017-bookkeeping-closeouts-do-not-inherit-risk.md\nindex 554e3a0d526a72ca067108c5464cc68c3edcf174..b9676857a4713850e063af7c39c8e0b01ecb4c66\n--- a/docs/adr/0017-bookkeeping-closeouts-do-not-inherit-risk.md\n+++ b/docs/adr/SADR-0017-bookkeeping-closeouts-do-not-inherit-risk.md\n@@ -1,4 +1,4 @@\n-# ADR 0017: A Bookkeeping Closeout Does Not Inherit Its Task's Risk\n+# SADR-0017: A Bookkeeping Closeout Does Not Inherit Its Task's Risk\n \n ## Status\n \n@@ -6,7 +6,7 @@\n \n ## Context\n \n-ADR 0012 made the goal loop push a task's completion to the default branch through a\n+SADR-0012 made the goal loop push a task's completion to the default branch through a\n closeout pull request: after the implementation PR merges, the loop opens a second PR\n carrying `task.status = done`, `lock.state = released`, and \u2014 when the task completes\n its goal \u2014 `goal.status = complete` plus one scorecard report. Nothing else. The\n@@ -49,14 +49,14 @@\n 4. The task file's governance fields (`scope`, `non_goals`, `required_skills`,\n    `risk_level`, `locks`, `acceptance_checks`, `test_command`) are byte-identical to\n    the base snapshot. Only `status`, `expected_pr`, `closeout_pr`, `ledger_evidence`\n-   and `expected_branch` may differ \u2014 the ADR 0015 contract-immutability rule,\n+   and `expected_branch` may differ \u2014 the SADR-0015 contract-immutability rule,\n    unchanged.\n \n    `expected_branch` is in that set because a closeout is *by construction* on a\n    different branch from the implementation it closes, and the loop rewrites the\n    field accordingly. It is safe to let it differ because MergeGate independently\n    requires `expected_branch` to equal the PR's actual head ref, so a PR cannot set\n-   it to anything other than the branch it is really on. The first draft of this ADR\n+   it to anything other than the branch it is really on. The first draft of this SADR\n    omitted it, which made the exemption unreachable: every real closeout failed\n    condition 4 on a field the loop always changes. See PR #209.\n 5. The state transitions are exactly the terminal set: task `review` \u2192 `done`, lock\n@@ -113,12 +113,12 @@\n classification must be derived, not asserted.\n \n **Fold the closeout into the implementation PR** so no second PR exists. Rejected under\n-ADR 0012, which recorded why: a task file that is already `done` on the implementation\n+SADR-0012, which recorded why: a task file that is already `done` on the implementation\n branch short-circuits the loop's review \u2192 CCA \u2192 merge \u2192 done ordering into a local\n goal-completion before any merge happens.\n \n **Have the loop auto-merge closeouts with elevated credentials.** Rejected outright: that\n-is a runtime authoring its own approval, which ADR 0010 and the constitution's\n+is a runtime authoring its own approval, which SADR-0010 and the constitution's\n approval-record prohibition forbid. It would also be indistinguishable, in the evidence,\n from the forgery those rules exist to prevent.\n \ndiff --git a/docs/adr/0018-carried-guardian-approval-across-a-proven-base-sync.md b/docs/adr/SADR-0018-carried-guardian-approval-across-a-proven-base-sync.md\nindex 22358562da390ed8b45cd600da08a43d378b4680..e6e2907cefa776460af73ddf7d39db605f3c39b0\n--- a/docs/adr/0018-carried-guardian-approval-across-a-proven-base-sync.md\n+++ b/docs/adr/SADR-0018-carried-guardian-approval-across-a-proven-base-sync.md\n@@ -1,4 +1,4 @@\n-# ADR 0018: Carried Guardian Approval Across A Proven Base Sync\n+# SADR-0018: Carried Guardian Approval Across A Proven Base Sync\n \n ## Status\n \n@@ -34,7 +34,7 @@\n Guardian evaluator that consumes it (`shiki_guardian.evaluate_guardian_approval`,\n the `guardian_comment_carried` source) were built and tested in isolation. Neither\n does anything until a caller passes the flags that turn the carry on, so the\n-feature was invisible in CI. This ADR records the decision the two of them\n+feature was invisible in CI. This SADR records the decision the two of them\n implement, and this task wires it into the four CI invocations that judge Guardian\n approval.\n \ndiff --git a/docs/adr/README.md b/docs/adr/README.md\nindex c70e057a4520c36e042ab68c53cd4a21ef709b71..5da43856836e2ce6a150f8127766e2f88355ab4b\n--- a/docs/adr/README.md\n+++ b/docs/adr/README.md\n@@ -1,40 +1,41 @@\n-# Architecture Decision Records\n+# Decision Record Namespaces\n \n-This directory holds Shiki's Architecture Decision Records (ADRs): durable\n-records of hard-to-reverse platform decisions. ADRs are part of the Shiki source\n-of truth alongside GitHub state, the `.shiki/` mirror, and `CONTEXT.md`.\n+This directory holds two independent decision namespaces:\n \n-## How ADRs Work\n+- Shiki Architecture Decision Records (SADRs), named\n+  `SADR-NNNN-<short-slug>.md`, record hard-to-reverse Shiki platform decisions.\n+- Target Architecture Decision Records (ADRs), named\n+  `NNNN-<short-slug>.md`, belong to the target product.\n \n-- New ADRs are added as `docs/adr/NNNN-<short-slug>.md`, where `NNNN` is the next\n-  zero-padded sequence number.\n-- The **canonical list of ADRs is the set of files matching `docs/adr/*.md`** in\n-  this directory. Browse `docs/adr/*.md` for the authoritative, current set.\n-- Use [`template.md`](template.md) as the starting point for a new ADR.\n-- Record an ADR when a decision is hard to reverse, surprising without context,\n-  and a real tradeoff. Do not write an ADR for routine or easily reversible\n-  choices.\n+The same four-digit number may appear once in each namespace. A platform\n+`SADR-NNNN` citation resolves only to `SADR-NNNN-*.md`; a target `ADR NNNN`\n+citation resolves only to `NNNN-*.md`.\n \n-Using the `docs/adr/*.md` glob as the source of truth means other PRs can add an\n-ADR without editing this index file, avoiding merge contention on a\n-hand-maintained enumeration.\n+## How Decision Records Work\n \n-## Current ADRs (snapshot)\n-\n-The list below is a convenience snapshot and may lag behind the directory. When\n-in doubt, list `docs/adr/*.md`.\n+- New Shiki platform decisions use the next zero-padded SADR number.\n+- New target product decisions use the target's own ADR sequence.\n+- The canonical list is the files in `docs/adr/`; no hand-maintained index is\n+  authoritative.\n+- Use [`template.md`](template.md) as the starting point and choose the filename\n+  namespace that owns the decision.\n+- Record a decision when it is hard to reverse, surprising without context, and\n+  a real tradeoff. Do not write a decision record for routine or easily\n+  reversible choices.\n \n-- [0001 \u2014 Keep Shiki As The Platform Name](0001-keep-shiki-as-platform-name.md)\n-- [0002 \u2014 Use AGENTS As The Shared Agent Constitution](0002-use-agents-as-shared-constitution.md)\n-- [0003 \u2014 Require Skill Gate For MergeGate Readiness](0003-require-skill-gate-for-mergegate-readiness.md)\n-- [0004 \u2014 Use GitHub First With A Repository-Local Shiki Mirror](0004-use-github-first-with-shiki-mirror.md)\n-- [0005 \u2014 Use Codex As Front And Claude Code Action As The GitHub Runtime](0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md)\n-- [0006 \u2014 Separate Codex Implementation, CCA Completion, And MergeGate Authorization](0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md)\n+Using the directory as the source of truth lets independent changes add records\n+without contending on an enumerated index.\n \n-## Adding A New ADR\n+## Adding A Shiki SADR\n \n-1. Copy `template.md` to `docs/adr/NNNN-<short-slug>.md`.\n+1. Copy `template.md` to `docs/adr/SADR-NNNN-<short-slug>.md`.\n 2. Fill in the title, status, context, decision, and consequences.\n-3. Set the status to `Proposed`, then `Accepted` once the decision is approved.\n-4. Open a PR following `CONTRIBUTING.md`. You do not need to edit this index for\n-   the ADR to be discoverable via `docs/adr/*.md`.\n+3. Set the status to `Proposed`, then `Accepted` once the proper authority has\n+   approved the decision.\n+4. Open a PR following `CONTRIBUTING.md`.\n+\n+## Adding A Target ADR\n+\n+1. Copy `template.md` to `docs/adr/NNNN-<short-slug>.md` using the target's own\n+   sequence.\n+2. Cite it as `ADR NNNN`; do not prefix its filename with `SADR-`.\ndiff --git a/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md b/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\nnew file mode 100644\nindex 0000000000000000000000000000000000000000..7455ce1f3bed02ddddb851ac21a1d1a648c9aeba\n--- /dev/null\n+++ b/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\n@@ -0,0 +1,55 @@\n+# SADR-0019: Separate Shiki Platform SADRs From Target ADRs\n+\n+## Status\n+\n+Accepted\n+\n+## Context\n+\n+Shiki installs its platform decision records into `docs/adr/` in every target\n+repository. Shiki and its targets previously used the same numeric\n+`NNNN-<slug>.md` namespace. A target that already owned ADRs 0013 through 0018\n+therefore acquired two records for each number during installation, making an\n+`ADR NNNN` citation ambiguous. Renumbering target decisions would rewrite target\n+history, while renumbering Shiki decisions per target would destroy their stable\n+platform identity.\n+\n+Upgrades also need to remove the former Shiki-owned numeric paths without ever\n+mistaking a target-owned ADR for platform content. The install stamp already\n+records exact shipped paths and content digests, so it can prove ownership only\n+when both still match.\n+\n+## Decision\n+\n+Shiki platform decisions use the `SADR` namespace:\n+\n+- records are named `docs/adr/SADR-NNNN-<slug>.md`;\n+- live platform citations use `SADR-NNNN`;\n+- target product decisions retain `docs/adr/NNNN-<slug>.md` and `ADR NNNN`;\n+- the reference validator resolves the two namespaces independently, so the same\n+  four-digit number may exist once in each namespace.\n+\n+Before a forced upgrade writes anything, the installer inventories the exact\n+former numeric Shiki ADR paths. It may delete a legacy path only when the\n+existing install stamp contains that exact path and its recorded SHA-256 digest\n+matches the file currently on disk. All ownership blockers are reported\n+together and abort the upgrade before its first write. A non-forced install\n+never performs this cleanup.\n+\n+Historical `.shiki/` evidence remains unchanged because it records the namespace\n+that existed when that evidence was produced.\n+\n+## Consequences\n+\n+- Targets may use any ADR number without colliding with Shiki's platform SADRs.\n+- Fresh installs preserve target ADR bytes and install a stable SADR set.\n+- Forced upgrades fail closed when legacy ownership cannot be proven, and a\n+  successful upgrade refreshes the stamp without removed legacy paths.\n+- Current governance, packaging, workflow, prompt, script, and skill references\n+  must move with the platform records.\n+\n+## Alternatives Considered\n+\n+- Collision-only refusal leaves affected targets permanently unupgradable.\n+- Renumbering target ADRs mutates target-owned history.\n+- Per-target Shiki renumbering makes platform citations non-portable.\ndiff --git a/docs/adr/template.md b/docs/adr/template.md\nindex a92a97daddd0b00ab9dd626cc8d86942da2eafed..17dd79175b5fee2105935557f691b40cf9492942\n--- a/docs/adr/template.md\n+++ b/docs/adr/template.md\n@@ -1,14 +1,14 @@\n-# NNNN: <Short Decision Title>\n+# <SADR-NNNN or NNNN>: <Short Decision Title>\n \n ## Status\n \n-Proposed | Accepted | Superseded by ADR-NNNN\n+Proposed | Accepted | Superseded by SADR-NNNN or target ADR NNNN\n \n ## Context\n \n What is the situation, constraint, or force that requires a decision? Reference\n-relevant domain terms (`CONTEXT.md`), prior ADRs, and code reality. State the\n-problem, not the solution.\n+relevant domain terms (`CONTEXT.md`), prior Shiki SADRs or target ADRs, and code\n+reality. State the problem, not the solution.\n \n ## Decision\n \ndiff --git a/docs/agents/autonomous-loop-e2e-acceptance.md b/docs/agents/autonomous-loop-e2e-acceptance.md\nindex bbf84c6081987f159ac6ad9ecf3c82b7aa72e7e8..5c13c1d02a05a90b0dc46cbef7c95a9846d0c237\n--- a/docs/agents/autonomous-loop-e2e-acceptance.md\n+++ b/docs/agents/autonomous-loop-e2e-acceptance.md\n@@ -40,8 +40,8 @@\n 3. `gh auth status` is logged in with merge permission on the repo.\n 4. Branch protection + required checks are configured exactly as the goal-loop\n    required-check set expects (see `docs/agents/branch-protection-smoke.md`).\n-5. The repo `.shiki/policy` permits autonomous low/medium-risk merges (ADR\n-   0008/0009); the MergeGate policy check is the recorded-authority gate.\n+5. The repo `.shiki/policy` permits autonomous low/medium-risk merges\n+   (SADR-0008/0009); the MergeGate policy check is the recorded-authority gate.\n \n The operator's only actions are: register the task and start the loop. **No\n manual pre-clearing** means after `shiki loop run` starts, the operator does not\ndiff --git a/docs/agents/bootstrap-command.md b/docs/agents/bootstrap-command.md\nindex 05f7f203a52cf412b9f97084a6e131ed0f785afd..f7aa0b734dfebee4a3b139335b66ee3415f48713\n--- a/docs/agents/bootstrap-command.md\n+++ b/docs/agents/bootstrap-command.md\n@@ -189,7 +189,9 @@\n ```\n \n Use this only for tests, fixtures, or explicit local-only template inspection.\n-Use `--force` only when you intentionally want to overwrite existing target files.\n+Use `--force` only when you intentionally want to refresh the target template.\n+Legacy numeric Shiki decision cleanup is stamp-and-digest authorized before any\n+write; target ADR paths are not cleanup candidates.\n \n ## Slash Command\n \ndiff --git a/docs/agents/checklists.md b/docs/agents/checklists.md\nindex 4a7f03ade346f53a1c1d82981f3fd744f9ab4547..0c5ec6c53dc7197b7b7a7d18e667b325babd6c45\n--- a/docs/agents/checklists.md\n+++ b/docs/agents/checklists.md\n@@ -32,18 +32,18 @@\n | ID | Item | Blocking |\n | --- | --- | --- |\n | GD-01 | Plan was challenged against `CONTEXT.md`. | blocking for non-trivial Goals |\n-| GD-02 | Relevant ADRs were read or absence was recorded. | blocking for non-trivial Goals |\n+| GD-02 | Relevant SADRs and target ADRs were read or absence was recorded. | blocking for non-trivial Goals |\n | GD-03 | Ambiguous terms were resolved or converted to HITL blockers. | blocking |\n | GD-04 | Edge scenarios were considered. | advisory |\n | GD-05 | Code reality was checked when code could answer. | blocking when applicable |\n | GD-06 | New domain terms were added to `CONTEXT.md` when needed. | blocking when applicable |\n-| GD-07 | ADR was created or explicitly skipped using the ADR criteria. | blocking when applicable |\n+| GD-07 | A SADR or target ADR was created or explicitly skipped using the decision-record criteria. | blocking when applicable |\n \n ## CI \u2014 Context & Impact Checklist\n \n | ID | Item | Blocking |\n | --- | --- | --- |\n-| CI-01 | Relevant docs and ADRs identified. | blocking |\n+| CI-01 | Relevant docs, SADRs, and target ADRs identified. | blocking |\n | CI-02 | Relevant modules, interfaces, seams, callers, and tests identified. | blocking |\n | CI-03 | Dependencies and blocked-by relationships identified. | blocking |\n | CI-04 | Candidate locks identified. | blocking |\n@@ -62,7 +62,7 @@\n | PRD-04 | Implementation decisions are recorded without volatile code detail. | blocking |\n | PRD-05 | Testing decisions are recorded. | blocking |\n | PRD-06 | Out-of-scope items are explicit. | blocking |\n-| PRD-07 | Relevant glossary/ADR links are present when applicable. | blocking |\n+| PRD-07 | Relevant glossary/SADR/target-ADR links are present when applicable. | blocking |\n \n ## SF \u2014 Spec Freeze Checklist\n \n@@ -98,7 +98,7 @@\n | H-02 | Branch/worktree target is present. | blocking |\n | H-03 | Scope and non-goals are present. | blocking |\n | H-04 | Dependencies and locks are present. | blocking |\n-| H-05 | Relevant docs, ADRs, modules, and tests are referenced. | blocking |\n+| H-05 | Relevant docs, SADRs, target ADRs, modules, and tests are referenced. | blocking |\n | H-06 | Required skills are stated. | blocking |\n | H-07 | TDD expectations are stated. | blocking when behavior work |\n | H-08 | Acceptance criteria are included. | blocking |\ndiff --git a/docs/agents/codex-handoff.md b/docs/agents/codex-handoff.md\nindex ede468f47cb277f69a857e8c45df703e4ed0151d..6f97bdffb67e44d7edea2c71e01ea28933c4eb6c\n--- a/docs/agents/codex-handoff.md\n+++ b/docs/agents/codex-handoff.md\n@@ -46,7 +46,7 @@\n \n ## Context\n - Domain terms:\n-- Relevant ADRs:\n+- Relevant SADRs and target ADRs:\n - Relevant docs:\n - Relevant modules/interfaces/seams:\n - Similar tests/prior art:\ndiff --git a/docs/agents/completion-check-agent.md b/docs/agents/completion-check-agent.md\nindex 13e4c035db35025f4e3cdcc5adfb24314ac4d521..61bfa1218cf36ca605250c91919e620f33238aa0\n--- a/docs/agents/completion-check-agent.md\n+++ b/docs/agents/completion-check-agent.md\n@@ -32,7 +32,7 @@\n - `AGENTS.md`\n - `CLAUDE.md`\n - `CONTEXT.md`\n-- relevant ADRs\n+- relevant SADRs and target ADRs\n - parent Goal issue\n - PRD issue or document\n - task issue\ndiff --git a/docs/agents/decision-control.md b/docs/agents/decision-control.md\nindex 7d8ee5ab03a5ccbcbd04056ae61de03785a2a9c9..8c94ecda00fffaa0e92c325ab83aa9776bd1dac1\n--- a/docs/agents/decision-control.md\n+++ b/docs/agents/decision-control.md\n@@ -11,7 +11,7 @@\n | Decision | Owner | When | Required inputs | Output | Enforcement surface |\n | --- | --- | --- | --- | --- | --- |\n | Is the Goal clear enough to plan? | Planner + Guardian when needed | Goal intake | User request, existing docs, risk signals | Goal readiness verdict | Goal checklist, issue template, ledger |\n-| Must `grill-with-docs` run? | Planner / Skill Gate | Before PRD or issue split | Ambiguity, domain terms, ADR conflicts, unclear boundaries | Skill invocation or explicit skip rationale | Skill Gate checklist |\n+| Must `grill-with-docs` run? | Planner / Skill Gate | Before PRD or issue split | Ambiguity, domain terms, SADR/target-ADR conflicts, unclear boundaries | Skill invocation or explicit skip rationale | Skill Gate checklist |\n | Is a PRD required? | Planner | After Goal context stabilizes | Goal, `grill-with-docs` output, Context & Impact output | PRD required / not required | PRD checklist, CCA evidence check |\n | Can the spec be frozen? | Operator only | End of Requirements Definition, before issue split | PRD/requirements summary, scope inventory (SF-02), open-question list | `spec_freeze` block in the plan + ledger evidence | `require_grilled_plan` (plan ingest/run), SF checklist |\n | Can a frozen spec change? | Operator only (Spec Amendment) | When implementation contradicts the frozen spec | Contested decisions, scoped re-grill result | recorded amendment + re-stamped freeze | SF checklist, ledger evidence |\ndiff --git a/docs/agents/domain.md b/docs/agents/domain.md\nindex 258da8ea81e87ac0a6b0b5b5e5c2fde980eaabb7..d57b0cf17ff3e1fa9e6e5885372bf083d8295421\n--- a/docs/agents/domain.md\n+++ b/docs/agents/domain.md\n@@ -5,7 +5,9 @@\n ## Layout\n \n - `CONTEXT.md` defines Shiki language. Keep it implementation-free.\n-- `docs/adr/` records hard-to-reverse decisions.\n+- `docs/adr/SADR-NNNN-*.md` records Shiki platform decisions.\n+- `docs/adr/NNNN-*.md` records target product decisions without sharing the\n+  SADR namespace.\n - `AGENTS.md` is the runtime-neutral agent constitution.\n - `CLAUDE.md` is the Claude Code wrapper around `AGENTS.md`.\n - `.shiki/` is the Target Repository mirror for Goals, PRDs, plans, Task DAGs, contracts, locks, worktree records, ledger entries, CCA verdicts, repair packets, reports, and handoffs.\n@@ -15,6 +17,6 @@\n \n - Use Shiki terms exactly when they exist.\n - Add glossary terms to `CONTEXT.md` only when the term is part of the domain, not a generic programming concept.\n-- Create ADRs only for hard-to-reverse, surprising, tradeoff-based decisions.\n+- Create SADRs or target ADRs only for hard-to-reverse, surprising, tradeoff-based decisions.\n - Keep product-specific language in the Target Repository, not in Shiki Template docs.\n-- If an ADR conflicts with a proposed implementation, surface the conflict before editing.\n+- If a SADR or target ADR conflicts with a proposed implementation, surface the conflict before editing.\ndiff --git a/docs/agents/execution-control.md b/docs/agents/execution-control.md\nindex ad8ee3cdb36be56fedc1f46408ca92b387d74047..bc63c3a73661d12b567016ba299f1a6b66bf9088\n--- a/docs/agents/execution-control.md\n+++ b/docs/agents/execution-control.md\n@@ -63,7 +63,7 @@\n | Branch name | Orchestrator | Orchestrator | Before implementer dispatch | branch policy | branch ref in task/ledger |\n | Lock reservation | Context & Impact | MergeGate | Before dispatch and before merge | `.shiki/locks/`, labels, PR metadata | lock id, owner, scope |\n | CI profile | Planner | MergeGate | During issue triage, rechecked at PR | labels, `.shiki/policy.yaml`, workflow matrix | required check list |\n-| Required status checks | Repo maintainer / Guardian | GitHub branch protection / rulesets | Repository setup, updated via ADR | GitHub settings | branch/ruleset config, ADR |\n+| Required status checks | Repo maintainer / Guardian | GitHub branch protection / rulesets | Repository setup, updated via target ADR | GitHub settings | branch/ruleset config, target ADR |\n | CCA judgment | CCA | GitHub required check / MergeGate | Every non-draft PR update | GitHub Action check | structured CCA verdict |\n | Repair assignment | CCA | MergeGate | After failed CCA/CI/review | repair packet, issue/PR comment | repair id and attempt count |\n | Merge readiness | MergeGate | GitHub branch protection / Guardian if needed | After CCA complete | required checks, review rules, rulesets | mergegate check output |\ndiff --git a/docs/agents/guardian-policy.md b/docs/agents/guardian-policy.md\nindex 4147c7adf05a47adb4fd24ada1a1c2e1f2ba6d2d..a20d9cc6c00a569bb057b9ca8d6b51c7fc4a9847\n--- a/docs/agents/guardian-policy.md\n+++ b/docs/agents/guardian-policy.md\n@@ -98,8 +98,8 @@\n `origin/<default_branch>` and reuses each job's existing base fetch; a shallow\n (`--depth`) or second base fetch would make the proof unresolvable and silently\n disable the carry, so neither is permitted. CCA reads `guardian_comment_carried` as\n-a recorded authority in the deterministic signal and does not re-derive it. See ADR\n-0018 for the full decision, the rejected diff/patch-equality and path-disjointness\n+a recorded authority in the deterministic signal and does not re-derive it. See\n+SADR-0018 for the full decision, the rejected diff/patch-equality and path-disjointness\n alternatives, and the accepted residual.\n \n ## External AI Guardian Review (SADR-0010)\ndiff --git a/docs/agents/implementation-policy.md b/docs/agents/implementation-policy.md\nindex c754144a9fb5bf4a38aa82a60e1670c59e626b44..85c37281bcad196c6b7b02343faf00b03cd9c57a\n--- a/docs/agents/implementation-policy.md\n+++ b/docs/agents/implementation-policy.md\n@@ -31,10 +31,13 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n ## Non-Negotiables\n \n 1. **Planning precedes implementation.** Non-trivial Goals start with `grill-with-docs` and Context & Impact.\n-2. **Domain language is controlled.** Terms must be checked against `CONTEXT.md`; hard-to-reverse tradeoffs must be checked against ADRs.\n+2. **Domain language is controlled.** Terms must be checked against `CONTEXT.md`; hard-to-reverse tradeoffs must be checked against Shiki SADRs and target ADRs.\n 3. **PRD precedes issue decomposition.** `to-prd` is used once the context is settled enough to become durable intent.\n 4. **Issues are vertical slices.** `to-issues` must produce independently grabbable tracer-bullet tasks, not horizontal layer tickets.\n 5. **Triage controls dispatch.** Only `ready-for-agent` / AFK tasks may be assigned to Codex without human input.\n@@ -95,19 +98,19 @@\n Process:\n \n - Challenge terms against `CONTEXT.md`.\n-- Check ADRs before reopening decisions.\n+- Check SADRs and target ADRs before reopening decisions.\n - Inspect code when code can answer a question.\n - Ask one question at a time when the operator is available.\n - Provide a recommended answer with each question.\n - Record resolved terms in `CONTEXT.md`.\n-- Create ADRs only for hard-to-reverse, surprising, tradeoff-based decisions.\n+- Create SADRs or target ADRs only for hard-to-reverse, surprising, tradeoff-based decisions.\n - Convert unresolved load-bearing questions into HITL blockers.\n \n Outputs:\n \n - Resolved design-tree decisions.\n - Updated glossary terms when needed.\n-- ADRs when justified.\n+- SADRs or target ADRs when justified.\n - HITL blockers when decisions remain unresolved.\n \n ## Phase 3 \u2014 Context & Impact\n@@ -117,7 +120,7 @@\n \n Outputs must include:\n \n-- Relevant documents and ADRs.\n+- Relevant documents, SADRs, and target ADRs.\n - Relevant modules, interfaces, seams, callers, and tests.\n - Candidate locks.\n - Dependencies.\n@@ -140,7 +143,7 @@\n - Testing decisions.\n - Out of scope.\n - Further notes.\n-- Links to relevant Goal, domain terms, and ADRs.\n+- Links to relevant Goal, domain terms, SADRs, and target ADRs.\n \n Phase 4 ends with Spec Freeze: the operator explicitly approves the PRD, the\n plan records a `spec_freeze` block (status, approved_by, source), and the SF\n@@ -240,7 +243,7 @@\n - Dependencies and locks.\n - Risk and Guardian requirements.\n - Ledger completeness.\n-- Documentation and ADR updates.\n+- Documentation and SADR/target-ADR updates.\n - Scope drift.\n \n CCA emits one of:\n@@ -301,7 +304,7 @@\n | Evidence failure | Behavior might be done, but proof is missing. | Add checks, PR evidence, ledger entry, or artifacts. |\n | Scope drift | PR changes unrelated behavior. | Revert unrelated changes or split into new Goal. |\n | Dependency block | Upstream task or lock not complete. | Block until dependency resolves. |\n-| Architecture block | Decision needs ADR or deep-module discussion. | `grill-with-docs` or `improve-codebase-architecture`. |\n+| Architecture block | Decision needs a SADR, target ADR, or deep-module discussion. | `grill-with-docs` or `improve-codebase-architecture`. |\n | Risk block | Security/production/policy change needs human approval. | Guardian decision. |\n \n ## Phase 5.5 \u2014 Execution Decision Control\ndiff --git a/docs/agents/runtime-auth-model.md b/docs/agents/runtime-auth-model.md\nindex 6a2e1a47e81ea89b5c68a210ec07772a9374babc..3c54748d24233dbd0ba8473950282547ef4ce935\n--- a/docs/agents/runtime-auth-model.md\n+++ b/docs/agents/runtime-auth-model.md\n@@ -148,7 +148,7 @@\n \n API-key based runners may be added by a target repository as an explicit extension. They are not the default Shiki template path.\n \n-If a repo opts into API-key mode, record the exception in an ADR and update the repo's `AGENTS.md`, `.shiki/config.yaml`, and workflow permissions.\n+If a repo opts into API-key mode, record the target-specific exception in a target ADR and update the repo's `AGENTS.md`, `.shiki/config.yaml`, and workflow permissions.\n \n ## GitHub Actions Runtime Compatibility\n \ndiff --git a/docs/agents/skill-gate.md b/docs/agents/skill-gate.md\nindex c50398b0d7c60a8598f2a5c88a10015730dced13..3acffe6444617bb1d54b77d4b4708158ec735bc6\n--- a/docs/agents/skill-gate.md\n+++ b/docs/agents/skill-gate.md\n@@ -9,7 +9,7 @@\n | Skill | Trigger | Expected output |\n | --- | --- | --- |\n | `setup-matt-pocock-skills` | First configuring a repo for these skills, or when issue tracker, triage labels, or domain docs are missing. | `docs/agents/` configuration and an `Agent skills` block in `AGENTS.md` or `CLAUDE.md`. |\n-| `grill-with-docs` | Ambiguous plan, unclear terminology, boundary decision, tradeoff, or ADR-worthy design choice. | Clarified decision tree, recommended answers, updates to `CONTEXT.md` or ADRs when decisions crystallize. |\n+| `grill-with-docs` | Ambiguous plan, unclear terminology, boundary decision, tradeoff, or decision-record-worthy design choice. | Clarified decision tree, recommended answers, updates to `CONTEXT.md`, SADRs, or target ADRs when decisions crystallize. |\n | `zoom-out` | Agent lacks a higher-level architectural map. | Map of relevant modules, callers, seams, and domain concepts. |\n | `to-prd` | Settled context should become a PRD. | PRD published to the issue tracker with problem, solution, user stories, decisions, testing, and out-of-scope notes. |\n | `to-issues` | Goal, PRD, or plan must become implementation tickets. | Vertical-slice issues with acceptance criteria, dependencies, and AFK/HITL classification. |\n@@ -17,7 +17,7 @@\n | `tdd` | Feature work or bug fix has observable behavior that can be specified with tests. | Red-green-refactor loop through public interfaces, one vertical slice at a time. |\n | `code-review` | The TDD loop is green and the implementer is about to create or update a PR. Default for every implementation task. | Pre-PR self-review of the task diff with fixes applied on the task branch; findings and resolutions recorded as a ledger entry naming the skill and a `## Pre-PR code review` PR body section. |\n | `diagnose` | Hard bug, failing check, regression, flaky behavior, or performance problem. | Reproduction loop, ranked hypotheses, instrumentation, fix, regression test, post-mortem. |\n-| `improve-codebase-architecture` | Structural friction, testability, AI-navigability, deep-module opportunity, or refactor request. | Architecture candidates and deepening recommendations informed by `CONTEXT.md` and ADRs. |\n+| `improve-codebase-architecture` | Structural friction, testability, AI-navigability, deep-module opportunity, or refactor request. | Architecture candidates and deepening recommendations informed by `CONTEXT.md`, SADRs, and target ADRs. |\n | `prototype` | A throwaway logic or UI prototype is needed to answer a design question. | Clearly marked disposable prototype and captured decision. |\n \n ## MergeGate Interaction\ndiff --git a/docs/operations.md b/docs/operations.md\nindex cf1f452d4c589e38ccd58553bda0ad8684b51eb7..ace48a90ffbec9e5c0a7113bef16a4858bc44b3f\n--- a/docs/operations.md\n+++ b/docs/operations.md\n@@ -89,6 +89,16 @@\n      target history, brought forward by `shiki migrate`, not a template.\n    - **Everything else** (CLI scripts, workflows, schemas, docs) is overwritten.\n \n+   A forced upgrade also migrates the former numeric Shiki decision paths to\n+   `SADR-NNNN-*`. Before the first write, the installer inventories the exact\n+   legacy Shiki paths and authorizes each deletion only when the existing\n+   `.shiki/install-stamp.json` names that path and its SHA-256 digest still\n+   matches the file. An absent, unreadable, malformed, incomplete, or\n+   mismatching stamp reports every blocked path and stops with no filesystem\n+   change. Target-owned `NNNN-*` ADRs are not cleanup candidates. A non-force\n+   install deletes nothing and retains any still-matching legacy stamp entries\n+   so a later forced upgrade can prove ownership.\n+\n    If the target has pending migrations, `--force` refuses and writes nothing \u2014\n    apply migrations first (step 3), then re-run. The run ends with a single\n    summary of every `.new` written, so a half-upgrade is visible.\ndiff --git a/docs/releasing.md b/docs/releasing.md\nindex 10d73828e7dbd64e9d9f6b83bc84d010537ce62a..aafeee8c4d441e1e427efee2ce4f81a647f020e1\n--- a/docs/releasing.md\n+++ b/docs/releasing.md\n@@ -67,7 +67,7 @@\n CLI in `scripts/`. There are no third-party Python runtime dependencies. The CLI\n is intentionally **not** exposed as a `[project.scripts]` console entry point\n because `scripts/shiki.py` is a dependency-free shim and `scripts/` is not\n-packaged as an importable distribution (see the ADR for the rationale).\n+packaged as an importable distribution (see SADR-0007 for the rationale).\n \n Supported install paths:\n \n@@ -155,6 +155,15 @@\n    [docs/operations.md](operations.md#the-new-convention). If the target has\n    pending migrations, `--force` refuses and writes nothing; apply migrations\n    (step 2) first, then re-run.\n+\n+   The SADR namespace upgrade has an additional pre-write gate: each former\n+   numeric Shiki decision path is removed only when the existing install stamp\n+   contains that exact path and a digest matching its current bytes. The\n+   installer aggregates absent/unreadable/malformed-stamp, missing-digest, and\n+   digest-mismatch blockers before raising, so refusal leaves the target tree\n+   byte-identical. Numeric target ADRs are preserved. A successful forced\n+   upgrade installs and stamps `SADR-*` paths and drops the removed legacy paths\n+   from the refreshed stamp.\n 2. Apply repository-local `.shiki/` state migrations with the migrate command:\n \n    ```bash\ndiff --git a/docs/usage.md b/docs/usage.md\nindex b83407ee3970dd395dd893010bc5dbe8bad26051..a130ef6e40dfd8169a5bc701339351097df1a09f\n--- a/docs/usage.md\n+++ b/docs/usage.md\n@@ -67,8 +67,10 @@\n shiki install-target /path/to/target-repo --local-only\n ```\n \n-Use `--force` only when you intentionally want to overwrite existing target\n-files.\n+Use `--force` only when you intentionally want to refresh the target template.\n+For the SADR namespace migration, `--force` deletes only exact legacy Shiki\n+decision paths whose existing install-stamp digest still matches; any ownership\n+blocker stops before the first write. Numeric target ADRs are never candidates.\n \n ## `shiki bootstrap-platform` \u2014 publish the Shiki platform repo\n \ndiff --git a/scripts/guardian_approval_signal.py b/scripts/guardian_approval_signal.py\nindex d059f92b2c82aa2b5d6ca421365537a87886c1cb..16924892714f4f5b9303c0ef0e325a7e70075630\n--- a/scripts/guardian_approval_signal.py\n+++ b/scripts/guardian_approval_signal.py\n@@ -199,8 +199,8 @@\n ) -> Any:\n     \"\"\"Evaluate SADR-0015 Contract Approval for the CCA Guardian signal.\n \n-    The SAME OR that MergeGate applies must reach this deterministic signal: ADR\n-    0015 is explicit that without it, a contract-approved PR still returns\n+    The SAME OR that MergeGate applies must reach this deterministic signal:\n+    SADR-0015 is explicit that without it, a contract-approved PR still returns\n     ``needs_guardian``, the CCA job fails, and the MergeGate policy check never\n     runs \u2014 the gate this change moves would never be reached. This resolves the\n     task the SAME way MergeGate does (the first Shiki task id in the PR body),\ndiff --git a/scripts/shiki_contract_approval.py b/scripts/shiki_contract_approval.py\nindex 941bcb96fdd8b9a85bc0c4f0f04d5dad9d04839c..4a4b418f6527413161db82670bec857a043360e2\n--- a/scripts/shiki_contract_approval.py\n+++ b/scripts/shiki_contract_approval.py\n@@ -142,7 +142,7 @@\n     changed_files_status: Any,\n     registration: Any,\n ) -> ContractApprovalResult:\n-    \"\"\"Whether this PR carries an SADR-0015 Contract Approval.\n+    \"\"\"Whether this PR carries a SADR-0015 Contract Approval.\n \n     The four conditions are evaluated IN ORDER, each failure recorded as a\n     distinct reason string:\ndiff --git a/scripts/shiki_guardian.py b/scripts/shiki_guardian.py\nindex a4ecb79bbda55fd5c8b06adfae2eb99760a03c45..4dd99bbe714e26f421fa19abac7a34ae1b195ecc\n--- a/scripts/shiki_guardian.py\n+++ b/scripts/shiki_guardian.py\n@@ -12,7 +12,7 @@\n # The carry re-derives its proof from git in-process on every judging invocation;\n # it is never read from an artifact, so there is nothing to go stale or be forged.\n # shiki_sync_proof is the ONLY guardian-family module that runs git, and every one\n-# of its invocations is read-only and local (ADR: prove-from-git base sync).\n+# of its invocations is read-only and local (SADR: prove-from-git base sync).\n from shiki_sync_proof import verify_pure_base_sync\n \n GUARDIAN_POLICY_PATH = \".shiki/guardian-policy.json\"\ndiff --git a/scripts/shiki_guardian_review.py b/scripts/shiki_guardian_review.py\nindex dd0f104d457899e0ba5ff1e162011c1e90b6f92b..e10ef58c046324e7ea3a3392feb3048ca77414ce\n--- a/scripts/shiki_guardian_review.py\n+++ b/scripts/shiki_guardian_review.py\n@@ -159,7 +159,7 @@\n     docs_only = _is_docs_only(changed_files)\n     if docs_only and matched == 0:\n         focus.append(\n-            \"Docs/ADR-only change: confirm the change is genuinely documentation/decision-only, introduces no behavior or contract drift, and stays consistent with the constitution and existing ADRs.\"\n+            \"Docs/decision-record-only change: confirm the change is genuinely documentation/decision-only, introduces no behavior or contract drift, and stays consistent with the constitution and existing SADRs and target ADRs.\"\n         )\n \n     # Conservative augmentation: unknown (no category and not docs-only), mixed\ndiff --git a/scripts/shiki_installer.py b/scripts/shiki_installer.py\nindex be064ab7a29ff6836f7f7a1d70500a43e0d11d7b..9fd8eefd137ce7003dad5c0dcb2e48e2cd47ab32\n--- a/scripts/shiki_installer.py\n+++ b/scripts/shiki_installer.py\n@@ -118,6 +118,7 @@\n # what lets a target carry it without a governance-schema change.\n INSTALL_STAMP_PATH = \".shiki/install-stamp.json\"\n INSTALL_STAMP_VERSION = 1\n+LEGACY_SHIKI_SADR_MAX = 18\n \n # Under --force the shipped surface splits three ways, so nothing is lost and\n # nothing is silently stale.\n@@ -237,9 +238,98 @@\n         )\n \n \n+def legacy_shiki_adr_paths() -> tuple[str, ...]:\n+    \"\"\"Former numeric paths for Shiki SADR-0001 through SADR-0018.\n+\n+    The authoritative slugs come from the current SADR filenames. SADR-0019 is\n+    the namespace decision itself and never shipped under a numeric legacy path.\n+    \"\"\"\n+    paths: list[str] = []\n+    for sadr in sorted((ROOT / \"docs\" / \"adr\").glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\")):\n+        number = sadr.name.split(\"-\", 2)[1]\n+        if int(number) <= LEGACY_SHIKI_SADR_MAX:\n+            paths.append(f\"docs/adr/{sadr.name.removeprefix('SADR-')}\")\n+    return tuple(paths)\n+\n+\n+def preflight_legacy_sadr_cleanup(target: Path, *, force: bool) -> tuple[str, ...]:\n+    \"\"\"Authorize exact legacy Shiki ADR removals before the first write.\n+\n+    A non-force install never removes existing files. A forced install needs no\n+    stamp when no legacy Shiki path is present. When legacy paths do exist, each\n+    must be named in a readable install stamp and its current bytes must match\n+    the recorded digest.\n+    \"\"\"\n+    if not force:\n+        return ()\n+    present = tuple(\n+        relative\n+        for relative in legacy_shiki_adr_paths()\n+        if (target / relative).exists() or (target / relative).is_symlink()\n+    )\n+    if not present:\n+        return ()\n+\n+    stamp_path = target / INSTALL_STAMP_PATH\n+    try:\n+        raw = stamp_path.read_text(encoding=\"utf-8\")\n+        stamp = json.loads(raw)\n+    except FileNotFoundError:\n+        detail = \"install stamp is absent\"\n+        blockers = [f\"{relative}: {detail}\" for relative in present]\n+    except (OSError, UnicodeDecodeError) as error:\n+        detail = f\"install stamp is unreadable ({error})\"\n+        blockers = [f\"{relative}: {detail}\" for relative in present]\n+    except json.JSONDecodeError as error:\n+        detail = f\"install stamp is malformed ({error})\"\n+        blockers = [f\"{relative}: {detail}\" for relative in present]\n+    else:\n+        if not isinstance(stamp, dict):\n+            blockers = [\n+                f\"{relative}: install stamp is malformed (expected an object)\"\n+                for relative in present\n+            ]\n+        else:\n+            digests = stamp.get(\"digests\")\n+            blockers = []\n+            if not isinstance(digests, dict):\n+                blockers.extend(\n+                    f\"{relative}: install stamp has no digest for this path\"\n+                    for relative in present\n+                )\n+            else:\n+                for relative in present:\n+                    expected = digests.get(relative)\n+                    if not isinstance(expected, str) or not expected:\n+                        blockers.append(\n+                            f\"{relative}: install stamp has no digest for this path\"\n+                        )\n+                        continue\n+                    try:\n+                        actual = _sha256_file(target / relative)\n+                    except OSError as error:\n+                        blockers.append(\n+                            f\"{relative}: legacy path is unreadable ({error})\"\n+                        )\n+                        continue\n+                    if actual != expected:\n+                        blockers.append(\n+                            f\"{relative}: digest mismatch (stamp {expected}, current {actual})\"\n+                        )\n+\n+    if blockers:\n+        joined = \"\\n\".join(f\"- {blocker}\" for blocker in blockers)\n+        raise ShikiError(\n+            \"legacy Shiki ADR cleanup is not authorized:\\n\"\n+            f\"{joined}\\nNo files were written.\"\n+        )\n+    return present\n+\n+\n def install_template(target: Path, *, force: bool, validate: bool) -> list[NewFileNote]:\n     # Preconditions first: never rewrite the tree of a target that cannot be\n     # upgraded cleanly (see refuse_pending_migrations).\n+    legacy_cleanup = preflight_legacy_sadr_cleanup(target, force=force)\n     refuse_pending_migrations(target)\n \n     # Resolve the target's required CODEOWNERS owner once from its\n@@ -250,6 +340,10 @@\n     # verbatim. See ``codeowners_required_owner``.\n     codeowners_owner = codeowners_required_owner(target)\n \n+    for relative in legacy_cleanup:\n+        (target / relative).unlink()\n+        info(f\"removed stamped legacy Shiki ADR: {target / relative}\")\n+\n     notes: list[NewFileNote] = []\n     for relative in TEMPLATE_PATHS:\n         source = ROOT / relative\n@@ -647,6 +741,22 @@\n     digests = target_stamp_digests(target)\n     existing = read_install_stamp(target)\n     if existing is not None:\n+        # A non-force install deliberately leaves legacy Shiki ADRs in place.\n+        # Preserve only still-matching ownership proof so a later forced\n+        # upgrade can authorize cleanup; never carry a missing or stale digest.\n+        existing_digests = existing.get(\"digests\")\n+        if isinstance(existing_digests, dict):\n+            for relative in legacy_shiki_adr_paths():\n+                expected = existing_digests.get(relative)\n+                path = target / relative\n+                if not isinstance(expected, str) or not path.is_file():\n+                    continue\n+                try:\n+                    actual = _sha256_file(path)\n+                except OSError:\n+                    continue\n+                if actual == expected:\n+                    digests[relative] = actual\n         if _incoming_is_older(commit, existing.get(\"platform_commit\")):\n             warn(\n                 \"kept existing install stamp: incoming install \"\ndiff --git a/scripts/shiki_loop.py b/scripts/shiki_loop.py\nindex 3611641da8aeb5038b1da90e3114b09a473ec6d8..84caa491b9cad0edc4176f209c13e9221e975681\n--- a/scripts/shiki_loop.py\n+++ b/scripts/shiki_loop.py\n@@ -424,7 +424,7 @@\n         # unresolvable CCA verdict has already produced its terminal stop and is never\n         # laundered into a head-moving sync (SADR-0010). It also never fires while the\n         # policy (Guardian) gate is red \u2014 for ANY reason, not only when it is the sole\n-        # red check \u2014 so a head-bound Guardian approval, or an SADR-0015\n+        # red check \u2014 so a head-bound Guardian approval, or a SADR-0015\n         # contract-approval carry that base movement defeated (the policy gate compares\n         # base and head task governance directly), is never silently restored by\n         # adopting base wholesale. It is bounded by a durable per-task counter so the\ndiff --git a/scripts/shiki_tasks.py b/scripts/shiki_tasks.py\nindex aec3809f24b4a0c8c96fc50971cff9ae9ad30381..d531052e9d9717058d4631483cefab44c26a65c1\n--- a/scripts/shiki_tasks.py\n+++ b/scripts/shiki_tasks.py\n@@ -821,7 +821,7 @@\n         \"prompt\": prompt,\n         \"entry_skill\": \"grill-with-docs\",\n         \"required_next_steps\": [\n-            \"Run grill-with-docs until terminology, boundaries, risks, and ADR-worthy decisions are settled.\",\n+            \"Run grill-with-docs until terminology, boundaries, risks, and decision-record-worthy choices are settled.\",\n             \"Obtain the operator's explicit approval of the PRD/requirements (Spec Freeze).\",\n             \"Write a machine-readable plan JSON with grill_with_docs.status=complete and spec_freeze.status=frozen.\",\n             \"Run shiki plan ingest --plan-file PLAN.json.\",\ndiff --git a/scripts/test_shiki_goal_loop.sh b/scripts/test_shiki_goal_loop.sh\nindex 753c7b516e10f0be3bc92a4bd0eb1fa699776ae6..8f3820a0a079b3044424cfe1a6c6b7fe68896e67\n--- a/scripts/test_shiki_goal_loop.sh\n+++ b/scripts/test_shiki_goal_loop.sh\n@@ -231,8 +231,8 @@\n \n # High/critical risk merges autonomously when all required checks are green:\n # the \"MergeGate policy check\" required check IS the Guardian gate (it enforces\n-# guardian-policy.json \u2014 human approval OR external AI guardian review, ADR\n-# 0010), so green checks mean a recorded authority approved. The fake gh returns\n+# guardian-policy.json \u2014 human approval OR external AI guardian review,\n+# SADR-0010), so green checks mean a recorded authority approved. The fake gh returns\n # green checks, simulating that the Guardian gate passed.\n python3 \"$ROOT/scripts/shiki.py\" goal create --target \"$TARGET\" --title \"Guardian gate\" --outcome \"High risk merges via policy gate\" >/tmp/shiki-goal-loop-guardian-goal.json\n GGOAL=\"$(json_get /tmp/shiki-goal-loop-guardian-goal.json goal_id)\"\ndiff --git a/tests/test_adr_references.py b/tests/test_adr_references.py\nindex 51706329c797d5dd9b47e082a333fa83b261d482..db6e693e35f67103935be34f1d9bb4ff7ee60395\n--- a/tests/test_adr_references.py\n+++ b/tests/test_adr_references.py\n@@ -1,7 +1,7 @@\n-\"\"\"Every ``ADR ####`` citation must resolve to exactly one decision record.\n+\"\"\"Every target ADR and Shiki SADR citation resolves in its own namespace.\n \n Background. ``scripts/mergegate_check.py`` justifies ``is_bookkeeping_closeout``\n-with \"ADR 0017\" nine times and ``scripts/guardian_approval_signal.py`` cites\n+with \"SADR-0017\" nine times and ``scripts/guardian_approval_signal.py`` cites\n 0016/0017 seven times, yet for a stretch the numbering in ``docs/adr/`` stopped\n at 0015: the authoring commits lived on a coordinator branch and no task ever\n declared ``docs/adr/...`` in its locks, so nothing carried the records to the\n@@ -11,11 +11,12 @@\n and the directory drifted with no signal.\n \n This is the same unbound-pair shape behind other silent failures: a reference\n-and its target with nothing asserting they agree. This test binds them. It scans\n-every ``scripts/*.py`` (plus ``docs/**/*.md`` and the root ``*.md`` governance\n-files, which are cheap) for the ``ADR ####`` pattern and asserts each cited\n-number resolves to exactly one ``docs/adr/####-*.md``. The converse is\n-deliberately NOT required: an ADR may exist without being cited.\n+and its target with nothing asserting they agree. This test binds them. Target\n+``ADR NNNN`` citations resolve only to ``docs/adr/NNNN-*.md``; Shiki\n+``SADR-NNNN`` citations resolve only to ``docs/adr/SADR-NNNN-*.md``. The scan\n+covers current governance docs, scripts, workflows, prompts, packaging metadata,\n+and repo-local Codex guidance while deliberately excluding historical\n+``.shiki`` evidence. The converse is not required: a record may be uncited.\n \n The suite is dependency free: standard-library ``unittest`` only.\n \"\"\"\n@@ -31,17 +32,18 @@\n \n REPO_ROOT = Path(__file__).resolve().parents[1]\n \n-# \"ADR 0016\" -- a four-digit citation. The trailing \\b keeps a five-digit token\n-# from being read as a truncated four-digit number.\n-_CITATION = re.compile(r\"\\bADR (\\d{4})\\b\")\n-# \"0016-locks-bound-product-paths.md\" -- the leading four digits name the record.\n-_ADR_FILENAME = re.compile(r\"^(\\d{4})-.*\\.md$\")\n+# ``ADR 0016`` / ``SADR-0016`` -- a namespace plus four-digit citation. The\n+# trailing ``\\b`` keeps a five-digit token from being read as a truncated one.\n+_CITATION = re.compile(r\"\\b(S?ADR)[ -](\\d{4})\\b\")\n+# Target ADRs retain the numeric filename; Shiki platform decisions use SADR.\n+_DECISION_FILENAME = re.compile(r\"^(?:(SADR)-)?(\\d{4})-.*\\.md$\")\n \n \n class Violation(NamedTuple):\n     \"\"\"A citation that does not resolve to exactly one decision record.\"\"\"\n \n     citing_file: str  # repo-relative path of the file that cites the ADR\n+    namespace: str  # \"ADR\" for target decisions; \"SADR\" for Shiki decisions\n     number: str  # the cited four-digit number, e.g. \"0016\"\n     line: int  # 1-based line of the citation\n     reason: str  # \"missing\" (zero matches) or \"ambiguous\" (two or more)\n@@ -49,44 +51,59 @@\n \n     @property\n     def expected_path(self) -> str:\n-        return f\"docs/adr/{self.number}-*.md\"\n+        prefix = \"SADR-\" if self.namespace == \"SADR\" else \"\"\n+        return f\"docs/adr/{prefix}{self.number}-*.md\"\n \n     def describe(self) -> str:\n         \"\"\"Name the citing file, the cited number, and the missing path.\"\"\"\n         if self.reason == \"missing\":\n             return (\n-                f\"{self.citing_file}:{self.line} cites ADR {self.number} \"\n+                f\"{self.citing_file}:{self.line} cites {self.namespace} {self.number} \"\n                 f\"but no document matches {self.expected_path}\"\n             )\n         joined = \", \".join(self.matches)\n         return (\n-            f\"{self.citing_file}:{self.line} cites ADR {self.number} which \"\n+            f\"{self.citing_file}:{self.line} cites {self.namespace} {self.number} which \"\n             f\"resolves to {len(self.matches)} documents (expected exactly one \"\n             f\"at {self.expected_path}): {joined}\"\n         )\n \n \n-def _adr_index(root: Path) -> dict[str, list[Path]]:\n-    \"\"\"Map every four-digit ADR number to the record files that claim it.\"\"\"\n-    index: dict[str, list[Path]] = {}\n+def _adr_index(root: Path) -> dict[tuple[str, str], list[Path]]:\n+    \"\"\"Map each decision namespace and number to the files that claim it.\"\"\"\n+    index: dict[tuple[str, str], list[Path]] = {}\n     adr_dir = root / \"docs\" / \"adr\"\n     if adr_dir.is_dir():\n         for path in sorted(adr_dir.glob(\"*.md\")):\n-            match = _ADR_FILENAME.match(path.name)\n+            match = _DECISION_FILENAME.match(path.name)\n             if match:\n-                index.setdefault(match.group(1), []).append(path)\n+                namespace = \"SADR\" if match.group(1) else \"ADR\"\n+                index.setdefault((namespace, match.group(2)), []).append(path)\n     return index\n \n \n def _citing_files(root: Path, include_docs: bool = True) -> list[Path]:\n-    \"\"\"Files scanned for citations: scripts/*.py, and cheaply the markdown.\"\"\"\n+    \"\"\"Current, shipped text surfaces scanned for decision citations.\"\"\"\n     files = sorted((root / \"scripts\").glob(\"*.py\"))\n+    files += sorted((root / \"scripts\").glob(\"*.sh\"))\n     if include_docs:\n         docs = root / \"docs\"\n         if docs.is_dir():\n             files += sorted(docs.rglob(\"*.md\"))\n         files += sorted(root.glob(\"*.md\"))\n-    return files\n+        pyproject = root / \"pyproject.toml\"\n+        if pyproject.is_file():\n+            files.append(pyproject)\n+        for relative in (\".github\", \".codex\"):\n+            surface = root / relative\n+            if surface.is_dir():\n+                files += sorted(\n+                    path\n+                    for path in surface.rglob(\"*\")\n+                    if path.is_file()\n+                    and path.suffix in {\".md\", \".py\", \".sh\", \".toml\", \".yaml\", \".yml\"}\n+                )\n+    return sorted(set(files))\n \n \n def find_adr_reference_violations(\n@@ -94,10 +111,9 @@\n ) -> list[Violation]:\n     \"\"\"Return every ``ADR ####`` citation under ``root`` that fails to resolve.\n \n-    A citation resolves when exactly one ``docs/adr/####-*.md`` matches its\n-    number. Zero matches is a \"missing\" violation; two or more is \"ambiguous\".\n-    Records that are never cited produce nothing -- the converse is not\n-    required.\n+    A citation resolves when exactly one record in the same namespace matches\n+    its number. Zero matches is a \"missing\" violation; two or more is\n+    \"ambiguous\". Records that are never cited produce nothing.\n     \"\"\"\n     root = Path(root)\n     index = _adr_index(root)\n@@ -110,13 +126,15 @@\n         rel = path.relative_to(root).as_posix()\n         for lineno, line in enumerate(text.splitlines(), start=1):\n             for match in _CITATION.finditer(line):\n-                number = match.group(1)\n-                found = index.get(number, [])\n+                namespace, number = match.groups()\n+                found = index.get((namespace, number), [])\n                 if len(found) == 1:\n                     continue\n                 reason = \"missing\" if not found else \"ambiguous\"\n                 rels = [p.relative_to(root).as_posix() for p in found]\n-                violations.append(Violation(rel, number, lineno, reason, rels))\n+                violations.append(\n+                    Violation(rel, namespace, number, lineno, reason, rels)\n+                )\n     return violations\n \n \n@@ -129,8 +147,8 @@\n         self.assertEqual(\n             violations,\n             [],\n-            \"Every 'ADR ####' citation must resolve to exactly one \"\n-            f\"docs/adr/####-*.md:\\n{detail}\",\n+            \"Every ADR/SADR citation must resolve exactly once in its own \"\n+            f\"docs/adr namespace:\\n{detail}\",\n         )\n \n     def test_scan_actually_reads_scripts(self) -> None:\n@@ -146,8 +164,21 @@\n         # The two records this Goal carried onto the default branch, and which\n         # the scripts cite most, must be present.\n         index = _adr_index(REPO_ROOT)\n-        self.assertIn(\"0016\", index)\n-        self.assertIn(\"0017\", index)\n+        self.assertIn((\"SADR\", \"0016\"), index)\n+        self.assertIn((\"SADR\", \"0017\"), index)\n+\n+    def test_platform_tree_has_only_sadr_0001_through_0019(self) -> None:\n+        adr_dir = REPO_ROOT / \"docs\" / \"adr\"\n+        sadrs = sorted(adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\"))\n+        self.assertEqual(\n+            {path.name.split(\"-\", 2)[1] for path in sadrs},\n+            {f\"{number:04d}\" for number in range(1, 20)},\n+        )\n+        self.assertEqual(\n+            list(adr_dir.glob(\"[0-9][0-9][0-9][0-9]-*.md\")),\n+            [],\n+            \"the Shiki platform tree must not retain legacy numeric SADR paths\",\n+        )\n \n \n class FixtureAdrReferences(unittest.TestCase):\n@@ -180,6 +211,26 @@\n         self.assertIn(\"9991\", message)  # cited number\n         self.assertIn(\"docs/adr/9991-*.md\", message)  # missing path\n \n+    def test_target_adr_does_not_satisfy_missing_platform_sadr(self) -> None:\n+        root = self._make_repo(\n+            adr_names=[\"0042-target-decision.md\"],\n+            script_body='\"\"\"Target ADR 0042 exists, but platform SADR-0042 does not.\"\"\"\\n',\n+        )\n+        violations = find_adr_reference_violations(root, include_docs=False)\n+        self.assertEqual(len(violations), 1)\n+        self.assertEqual(violations[0].namespace, \"SADR\")\n+        self.assertEqual(violations[0].number, \"0042\")\n+        self.assertEqual(violations[0].expected_path, \"docs/adr/SADR-0042-*.md\")\n+\n+    def test_target_adr_and_platform_sadr_may_reuse_a_number(self) -> None:\n+        root = self._make_repo(\n+            adr_names=[\"0042-target-decision.md\", \"SADR-0042-platform-decision.md\"],\n+            script_body='\"\"\"Target ADR 0042 and platform SADR-0042 are distinct.\"\"\"\\n',\n+        )\n+        self.assertEqual(\n+            find_adr_reference_violations(root, include_docs=False), []\n+        )\n+\n     def test_uncited_adr_is_allowed(self) -> None:\n         # An ADR exists but nothing cites it -> not a violation. The converse\n         # (every ADR must be cited) is explicitly NOT required.\ndiff --git a/tests/test_guardian_review.py b/tests/test_guardian_review.py\nindex 88829e7810212635e2fc98aa5918d6f26294677d..00a85a237acd5bf2d6cb5b294128198c422402ca\n--- a/tests/test_guardian_review.py\n+++ b/tests/test_guardian_review.py\n@@ -162,7 +162,7 @@\n \n     def test_docs_only_is_light_and_not_conservative(self):\n         focus = gr.classify_review_focus([\"docs/adr/0099-x.md\", \"README.md\"], \"\", \"low\")\n-        self.assertTrue(any(\"Docs/ADR-only\" in f for f in focus))\n+        self.assertTrue(any(\"Docs/decision-record-only\" in f for f in focus))\n         self.assertFalse(any(\"conservative default\" in f.lower() for f in focus))\n \n     def test_unknown_category_is_conservative(self):\ndiff --git a/tests/test_policy_job_merged_prs.py b/tests/test_policy_job_merged_prs.py\nindex d45512e74681e5e649279236c7d4e79d5c3da471..b97989f8bd74d091de1a6b5275a20d8dc68c4512\n--- a/tests/test_policy_job_merged_prs.py\n+++ b/tests/test_policy_job_merged_prs.py\n@@ -3,8 +3,8 @@\n \n ``.github/workflows/shiki-cca-completion.yml`` has two consumers of the merged-PR\n proof: the CCA *signal* (``guardian_approval_signal.py`` in \"Prepare CCA context\")\n-and the authoritative gate (``mergegate_check.py`` in \"Enforce MergeGate\"). ADR\n-0017's bookkeeping-closeout exemption (condition 6) needs the merged implementation\n+and the authoritative gate (``mergegate_check.py`` in \"Enforce MergeGate\").\n+SADR-0017's bookkeeping-closeout exemption (condition 6) needs the merged implementation\n PR proven, and a closeout carries NO ``post_merge_reconcile`` marker. The signal\n already resolves ``MERGED_PRS`` unconditionally, but \"Enforce MergeGate\" used to\n resolve it only inside ``grep -q '<!-- shiki:post_merge_reconcile -->'``. So a\ndiff --git a/tests/test_sadr_install_migration.py b/tests/test_sadr_install_migration.py\nnew file mode 100644\nindex 0000000000000000000000000000000000000000..a6f4c57e4dcaef73ef600b0d4804744138ab9071\n--- /dev/null\n+++ b/tests/test_sadr_install_migration.py\n@@ -0,0 +1,229 @@\n+\"\"\"SADR installation and collision-safe legacy upgrade behavior.\"\"\"\n+\n+from __future__ import annotations\n+\n+import contextlib\n+import hashlib\n+import io\n+import json\n+import shutil\n+import tempfile\n+import unittest\n+from pathlib import Path\n+\n+import shiki_test_support  # noqa: F401  (path bootstrap)\n+\n+import shiki_installer\n+from tests.test_adr_references import find_adr_reference_violations\n+\n+\n+PRODUCT_NUMBERS = tuple(range(13, 19))\n+\n+\n+def _quiet_install(target: Path, *, force: bool, validate: bool) -> None:\n+    with contextlib.redirect_stdout(io.StringIO()):\n+        shiki_installer.install_template(target, force=force, validate=validate)\n+\n+\n+def _sha256(path: Path) -> str:\n+    return hashlib.sha256(path.read_bytes()).hexdigest()\n+\n+\n+def _legacy_sadr_paths() -> tuple[str, ...]:\n+    return tuple(\n+        f\"docs/adr/{path.name.removeprefix('SADR-')}\"\n+        for path in sorted((shiki_installer.ROOT / \"docs\" / \"adr\").glob(\"SADR-*.md\"))\n+        if path.name != \"SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\"\n+    )\n+\n+\n+def _create_legacy_upgrade_fixture(\n+    target: Path,\n+) -> tuple[dict[str, bytes], tuple[str, ...]]:\n+    adr_dir = target / \"docs\" / \"adr\"\n+    adr_dir.mkdir(parents=True)\n+    product_bytes = {\n+        f\"{number:04d}-cubeshare-product-decision.md\": (\n+            f\"# CubeShare ADR {number:04d}\\n\\nDo not rewrite {number}.\\n\"\n+        ).encode()\n+        for number in PRODUCT_NUMBERS\n+    }\n+    for name, content in product_bytes.items():\n+        (adr_dir / name).write_bytes(content)\n+\n+    legacy_paths = _legacy_sadr_paths()\n+    for relative in legacy_paths:\n+        (target / relative).write_bytes(f\"legacy Shiki bytes for {relative}\\n\".encode())\n+\n+    stamp_path = target / shiki_installer.INSTALL_STAMP_PATH\n+    stamp_path.parent.mkdir(parents=True, exist_ok=True)\n+    stamp = {\n+        \"version\": shiki_installer.INSTALL_STAMP_VERSION,\n+        \"platform_commit\": None,\n+        \"installed_at\": \"2000-01-01T00:00:00+00:00\",\n+        \"digests\": {\n+            relative: _sha256(target / relative) for relative in legacy_paths\n+        },\n+    }\n+    stamp_path.write_text(\n+        json.dumps(stamp, indent=2, sort_keys=True) + \"\\n\", encoding=\"utf-8\"\n+    )\n+    return product_bytes, legacy_paths\n+\n+\n+def _tree_snapshot(root: Path) -> dict[str, tuple[str, bytes | str]]:\n+    \"\"\"Recursive byte/shape snapshot used to prove preflight is write-free.\"\"\"\n+    snapshot: dict[str, tuple[str, bytes | str]] = {}\n+    for path in sorted(root.rglob(\"*\")):\n+        relative = path.relative_to(root).as_posix()\n+        if path.is_symlink():\n+            snapshot[relative] = (\"symlink\", path.readlink().as_posix())\n+        elif path.is_dir():\n+            snapshot[relative] = (\"directory\", \"\")\n+        else:\n+            snapshot[relative] = (\"file\", path.read_bytes())\n+    return snapshot\n+\n+\n+class FreshSadrInstallTests(unittest.TestCase):\n+    def setUp(self) -> None:\n+        self.tmp = Path(tempfile.mkdtemp(prefix=\"shiki-sadr-fresh-\"))\n+        self.addCleanup(lambda: shutil.rmtree(self.tmp, ignore_errors=True))\n+        self.target = self.tmp / \"target\"\n+        self.adr_dir = self.target / \"docs\" / \"adr\"\n+        self.adr_dir.mkdir(parents=True)\n+        self.product_bytes = {\n+            f\"{number:04d}-product-decision.md\": (\n+                f\"# Product ADR {number:04d}\\n\\nTarget-owned bytes {number}.\\n\"\n+            ).encode()\n+            for number in PRODUCT_NUMBERS\n+        }\n+        for name, content in self.product_bytes.items():\n+            (self.adr_dir / name).write_bytes(content)\n+\n+    def test_fresh_install_preserves_product_adrs_and_installs_all_sadrs(self) -> None:\n+        _quiet_install(self.target, force=False, validate=True)\n+\n+        for name, expected in self.product_bytes.items():\n+            self.assertEqual((self.adr_dir / name).read_bytes(), expected)\n+\n+        sadr_numbers = {\n+            path.name.split(\"-\", 2)[1]\n+            for path in self.adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\")\n+        }\n+        self.assertEqual(sadr_numbers, {f\"{number:04d}\" for number in range(1, 20)})\n+\n+        numeric_records = {\n+            path.name\n+            for path in self.adr_dir.glob(\"[0-9][0-9][0-9][0-9]-*.md\")\n+        }\n+        self.assertEqual(numeric_records, set(self.product_bytes))\n+        self.assertEqual(find_adr_reference_violations(self.target), [])\n+\n+\n+class LegacySadrUpgradeTests(unittest.TestCase):\n+    def setUp(self) -> None:\n+        self.tmp = Path(tempfile.mkdtemp(prefix=\"shiki-sadr-upgrade-\"))\n+        self.addCleanup(lambda: shutil.rmtree(self.tmp, ignore_errors=True))\n+        self.target = self.tmp / \"target\"\n+        self.adr_dir = self.target / \"docs\" / \"adr\"\n+        self.product_bytes, self.legacy_paths = _create_legacy_upgrade_fixture(\n+            self.target\n+        )\n+\n+    def test_force_upgrade_deletes_only_stamped_legacy_sadrs(self) -> None:\n+        _quiet_install(self.target, force=True, validate=True)\n+\n+        for relative in self.legacy_paths:\n+            self.assertFalse((self.target / relative).exists(), relative)\n+        for name, expected in self.product_bytes.items():\n+            self.assertEqual((self.adr_dir / name).read_bytes(), expected)\n+\n+        sadrs = list(self.adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\"))\n+        self.assertEqual(len(sadrs), 19)\n+        refreshed = json.loads(\n+            (self.target / shiki_installer.INSTALL_STAMP_PATH).read_text(encoding=\"utf-8\")\n+        )\n+        for relative in self.legacy_paths:\n+            self.assertNotIn(relative, refreshed[\"digests\"])\n+        for path in sadrs:\n+            relative = path.relative_to(self.target).as_posix()\n+            self.assertEqual(refreshed[\"digests\"][relative], _sha256(path))\n+\n+    def test_non_force_install_never_deletes_legacy_files(self) -> None:\n+        before = {\n+            relative: (self.target / relative).read_bytes()\n+            for relative in self.legacy_paths\n+        }\n+        _quiet_install(self.target, force=False, validate=False)\n+        for relative, expected in before.items():\n+            self.assertEqual((self.target / relative).read_bytes(), expected)\n+        refreshed = json.loads(\n+            (self.target / shiki_installer.INSTALL_STAMP_PATH).read_text(encoding=\"utf-8\")\n+        )\n+        for relative in self.legacy_paths:\n+            self.assertEqual(refreshed[\"digests\"][relative], _sha256(self.target / relative))\n+\n+\n+class LegacyCleanupPreflightTests(unittest.TestCase):\n+    def setUp(self) -> None:\n+        self.tmp = Path(tempfile.mkdtemp(prefix=\"shiki-sadr-preflight-\"))\n+        self.addCleanup(lambda: shutil.rmtree(self.tmp, ignore_errors=True))\n+\n+    def test_all_ownership_blockers_abort_before_any_write(self) -> None:\n+        cases = (\n+            (\"absent\", \"install stamp is absent\"),\n+            (\"unreadable\", \"install stamp is unreadable\"),\n+            (\"malformed\", \"install stamp is malformed\"),\n+            (\"missing-digest\", \"install stamp has no digest\"),\n+            (\"digest-mismatch\", \"digest mismatch\"),\n+        )\n+        for name, expected_reason in cases:\n+            with self.subTest(name=name):\n+                target = self.tmp / name\n+                _, legacy_paths = _create_legacy_upgrade_fixture(target)\n+                stamp_path = target / shiki_installer.INSTALL_STAMP_PATH\n+\n+                if name == \"absent\":\n+                    stamp_path.unlink()\n+                elif name == \"unreadable\":\n+                    stamp_path.unlink()\n+                    stamp_path.mkdir()\n+                elif name == \"malformed\":\n+                    stamp_path.write_text(\"{not-json\\n\", encoding=\"utf-8\")\n+                else:\n+                    stamp = json.loads(stamp_path.read_text(encoding=\"utf-8\"))\n+                    if name == \"missing-digest\":\n+                        stamp[\"digests\"] = {}\n+                    else:\n+                        stamp[\"digests\"] = {\n+                            relative: \"deadbeef\" for relative in legacy_paths\n+                        }\n+                    stamp_path.write_text(\n+                        json.dumps(stamp, indent=2, sort_keys=True) + \"\\n\",\n+                        encoding=\"utf-8\",\n+                    )\n+\n+                before = _tree_snapshot(target)\n+                with self.assertRaises(shiki_installer.ShikiError) as raised:\n+                    _quiet_install(target, force=True, validate=False)\n+                after = _tree_snapshot(target)\n+\n+                self.assertEqual(before, after, f\"{name} preflight changed the tree\")\n+                message = str(raised.exception)\n+                self.assertIn(expected_reason, message)\n+                self.assertIn(\"No files were written\", message)\n+                for relative in legacy_paths:\n+                    self.assertIn(relative, message)\n+\n+    def test_absent_stamp_is_allowed_when_no_legacy_path_exists(self) -> None:\n+        target = self.tmp / \"no-legacy\"\n+        target.mkdir()\n+        _quiet_install(target, force=True, validate=False)\n+        self.assertTrue(\n+            (target / \"docs\" / \"adr\" / \"SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\").is_file()\n+        )\n+\n+\n+if __name__ == \"__main__\":\n+    unittest.main()\n\nexec\n/bin/zsh -lc \"sed -n '1,220p' .shiki/ledger/L-20260807T080107948254Z-61f24da0.json 2>/dev/null || true\" in /private/tmp/shiki-adr-collision.pa0KwY/.worktrees/shiki-t-20260807t073307221254z-2d22d654-introduc\n succeeded in 0ms:\nexec\n/bin/zsh -lc \"sed -n '1,220p' .shiki/ledger/L-20260807T080228312210Z-07ac626e.json 2>/dev/null || true\" in /private/tmp/shiki-adr-collision.pa0KwY/.worktrees/shiki-t-20260807t073307221254z-2d22d654-introduc\n succeeded in 0ms:\nexec\n/bin/zsh -lc \"sed -n '1,220p' .shiki/ledger/L-20260807T080256330734Z-5bf40cb6.json 2>/dev/null || true\" in /private/tmp/shiki-adr-collision.pa0KwY/.worktrees/shiki-t-20260807t073307221254z-2d22d654-introduc\n succeeded in 0ms:\nexec\n/bin/zsh -lc \"sed -n '1,220p' .shiki/ledger/L-20260807T073307221361Z-b3c3a718.json 2>/dev/null || true\" in /private/tmp/shiki-adr-collision.pa0KwY/.worktrees/shiki-t-20260807t073307221254z-2d22d654-introduc\n succeeded in 0ms:\n{\n  \"actor\": \"shiki-cli\",\n  \"evidence\": [\n    \".shiki/tasks/T-20260807T073307221254Z-2d22d654.json\"\n  ],\n  \"goal_id\": \"G-20260807T073307219752Z-1ee1ed68\",\n  \"id\": \"L-20260807T073307221361Z-b3c3a718\",\n  \"links\": [],\n  \"summary\": \"Task registered from plan: Introduce SADR and migrate installs without touching product ADRs\",\n  \"task_id\": \"T-20260807T073307221254Z-2d22d654\",\n  \"timestamp\": \"2026-08-07T07:33:07+00:00\",\n  \"type\": \"task-registered\"\n}\n\nexec\n/bin/zsh -lc \"sed -n '1,220p' .shiki/ledger/L-20260807T080228575038Z-d574f629.json 2>/dev/null || true\" in /private/tmp/shiki-adr-collision.pa0KwY/.worktrees/shiki-t-20260807t073307221254z-2d22d654-introduc\n succeeded in 0ms:\ndiff --git a/.codex/skills/shiki/SKILL.md b/.codex/skills/shiki/SKILL.md\nindex c8e2e85d9d75d49cc4f2d6286ee22659c4e53e75..93cded623b2c15d9f65c76491a1c2d813ac9e6c0\n--- a/.codex/skills/shiki/SKILL.md\n+++ b/.codex/skills/shiki/SKILL.md\n@@ -18,6 +18,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n Codex CLI does not register this skill as a custom `/shiki` slash command. When\n the user types `/shiki` in Codex CLI and gets \"Unrecognized command\", explain\n that this is expected: invoke Shiki in Codex with natural language such as\n@@ -69,7 +72,7 @@\n \n ## Responsibilities\n \n-- Claude Code implements and repairs by default (`shiki runner claude`, ADR 0008); Codex implements and repairs tasks explicitly assigned to `codex`.\n+- Claude Code implements and repairs by default (`shiki runner claude`, SADR-0008); Codex implements and repairs tasks explicitly assigned to `codex`.\n - Claude Code Action can act as GitHub-side CCA or reviewer.\n - CCA judges completion.\n - MergeGate authorizes state transitions and merge readiness.\ndiff --git a/.github/prompts/cca-completion-check.md b/.github/prompts/cca-completion-check.md\nindex ed203ad5f302ecc81372d8ab27d2b1232ca97497..fd89950528333ef6242e6d5443904f5392ab7e56\n--- a/.github/prompts/cca-completion-check.md\n+++ b/.github/prompts/cca-completion-check.md\n@@ -19,6 +19,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n ## Required Reading\n \n Read only the files needed to judge the PR contract. Prefer this order:\ndiff --git a/.github/prompts/codex-task-handoff.md b/.github/prompts/codex-task-handoff.md\nindex 695e211d76dea1c60e8d5bb360e78487808286a4..59441d576b01a39569722f226c854e5a740e9eb5\n--- a/.github/prompts/codex-task-handoff.md\n+++ b/.github/prompts/codex-task-handoff.md\n@@ -2,7 +2,7 @@\n \n You are Codex Front acting as the Implementer for a Shiki task.\n \n-Follow `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant ADRs, and `docs/agents/codex-handoff.md`.\n+Follow `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant Shiki SADRs and target ADRs, and `docs/agents/codex-handoff.md`.\n \n Rules:\n \ndiff --git a/AGENTS.md b/AGENTS.md\nindex 7f041100d49c2a7525f51cb0f5d696a4a6d1c78c..a06bfbabc6fa48a007389e25e12d7ed523936bed\n--- a/AGENTS.md\n+++ b/AGENTS.md\n@@ -22,7 +22,7 @@\n - **Codex Front** is an optional implementer through Codex App, Codex CLI, Codex IDE extension, or Codex Web signed in with ChatGPT OAuth/subscription auth, dispatched with `shiki runner codex` when a task is explicitly assigned to `codex`.\n - **GitHub CCA** is implemented by Claude Code Action by default, using `CLAUDE_CODE_OAUTH_TOKEN`.\n \n-Do not assume `openai/codex-action`, `OPENAI_API_KEY`, or API-key based Codex automation in the default Shiki path. API-key runners are explicit target-repo extensions and require their own ADR.\n+Do not assume `openai/codex-action`, `OPENAI_API_KEY`, or API-key based Codex automation in the default Shiki path. API-key runners are explicit target-repo extensions and require their own target ADR.\n \n Shiki is not:\n \n@@ -44,10 +44,14 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use\n+`SADR-NNNN-*.md`; target product decisions retain `NNNN-*.md` and the `ADR`\n+namespace.\n+\n Use the canonical source-of-truth order above. Runtime-specific reading orders\n are operational guidance only and must not override or reorder it.\n \n-Conversation state is not durable truth. If a decision matters, put it in GitHub, `.shiki/`, `CONTEXT.md`, or an ADR.\n+Conversation state is not durable truth. If a decision matters, put it in GitHub, `.shiki/`, `CONTEXT.md`, a Shiki SADR, or a target ADR.\n \n If GitHub and `.shiki/` disagree, surface the conflict and prefer GitHub operational state until the mirror is repaired.\n \n@@ -75,8 +79,8 @@\n Every non-trivial change follows this loop:\n \n 1. **Goal**: clarify outcome, completion conditions, non-goals, risk level, and success signals.\n-2. **grill-with-docs**: challenge the plan against domain docs, ADRs, code reality, terminology, and edge scenarios.\n-3. **Context & Impact**: find relevant docs, ADRs, code, dependencies, owners, locks, and verification surfaces.\n+2. **grill-with-docs**: challenge the plan against domain docs, Shiki SADRs, target ADRs, code reality, terminology, and edge scenarios.\n+3. **Context & Impact**: find relevant docs, SADRs, target ADRs, code, dependencies, owners, locks, and verification surfaces.\n 4. **PRD and Spec Freeze**: use `to-prd` when context is settled enough to become durable product/engineering intent. The operator's explicit approval of the PRD is Spec Freeze: the plan records a `spec_freeze` block, and no plan runs without it. Steps 1-4 form Requirements Definition \u2014 one continuous operator dialogue. After Spec Freeze, scope changes require an operator-approved Spec Amendment; non-scope-moving interpretations go to the Assumption Log.\n 5. **Issues**: use `to-issues` to create independently grabbable vertical-slice issues. Prefer AFK slices over HITL slices where possible.\n 6. **Triage**: label issues for readiness, risk, runtime, skills, and MergeGate state.\n@@ -167,7 +171,7 @@\n - Testing decisions.\n - Out-of-scope items.\n - Further notes.\n-- Links to relevant domain terms and ADRs.\n+- Links to relevant domain terms, SADRs, and target ADRs.\n \n Every executable task must include:\n \n@@ -193,7 +197,7 @@\n \n Context & Impact must identify:\n \n-- Relevant documents, domain terms, ADRs, and past decisions.\n+- Relevant documents, domain terms, SADRs, target ADRs, and past decisions.\n - Relevant modules, interfaces, seams, callers, owners, and tests.\n - Dependency relationships between tasks.\n - Candidate locks and lock conflicts.\n@@ -210,7 +214,7 @@\n | Skill | Required when |\n | --- | --- |\n | `setup-matt-pocock-skills` | First configuring the repo for skills, or when issue tracker, triage labels, or domain docs are missing. |\n-| `grill-with-docs` | Default for non-trivial Goals before PRD/issues; always required when plans, terminology, boundaries, tradeoffs, or ADR-worthy decisions are ambiguous. |\n+| `grill-with-docs` | Default for non-trivial Goals before PRD/issues; always required when plans, terminology, boundaries, tradeoffs, or decision-record-worthy choices are ambiguous. |\n | `zoom-out` | The agent lacks an architectural map of the relevant code area. |\n | `to-prd` | Settled Goal context must become durable product/engineering intent. |\n | `to-issues` | A Goal, PRD, or plan must become independently grabbable vertical-slice issues. |\n@@ -348,7 +352,7 @@\n - Three or more new files.\n - Any high-risk or critical label.\n \n-When the architecture gate triggers, run `grill-with-docs` or `improve-codebase-architecture` as appropriate and record durable decisions in an ADR when needed.\n+When the architecture gate triggers, run `grill-with-docs` or `improve-codebase-architecture` as appropriate and record durable decisions in a Shiki SADR or target ADR as applicable.\n \n ## Engineering Discipline\n \ndiff --git a/CLAUDE.md b/CLAUDE.md\nindex 98a932e7841a632866fcd53b0bad0e3af3711d3d..98a31399404d6fb044b4fd493f4459ac87f439de\n--- a/CLAUDE.md\n+++ b/CLAUDE.md\n@@ -14,6 +14,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n ## Claude Role\n \n Default to planner, default implementer, reviewer, coordinator, documentation editor, CCA-style completion judge, and final-judgment assistant.\n@@ -57,7 +60,7 @@\n At the start of a Shiki session:\n \n 1. Read `AGENTS.md`.\n-2. Read `CONTEXT.md` and relevant ADRs.\n+2. Read `CONTEXT.md` and relevant Shiki SADRs and target ADRs.\n 3. Read active `.shiki/` Goal/task/ledger state when present.\n 4. Read active distilled rules from `.shiki/memories/` (status `distilled`, `active: true`, not revoked or superseded) for applicable guidance; the same rules are injected deterministically into task handoffs as the `## Distilled Rules` section (\u00a73.5).\n 5. Read the GitHub Issue or PR when the task is GitHub-backed.\n@@ -74,7 +77,7 @@\n - Define the Goal, completion conditions, non-goals, and risk level.\n - Treat `grill-with-docs` as the default planning entry point for non-trivial Goals.\n - Challenge domain terms against `CONTEXT.md`.\n-- Challenge hard-to-reverse tradeoffs against ADRs.\n+- Challenge hard-to-reverse tradeoffs against SADRs and target ADRs.\n - Explore code instead of asking when code can answer the question.\n - Run Context & Impact before implementation planning.\n - Produce a Task DAG, not an unordered checklist.\n@@ -94,7 +97,7 @@\n - For each question, provide your recommended answer.\n - Resolve dependency decisions before downstream design questions.\n - Update `CONTEXT.md` inline when terminology crystallizes.\n-- Offer ADRs only when the decision is hard to reverse, surprising without context, and a real tradeoff.\n+- Offer a SADR or target ADR only when the decision is hard to reverse, surprising without context, and a real tradeoff.\n - Convert unresolved questions into HITL blockers rather than allowing Codex to guess.\n \n ## PRD and Issues Standard\n@@ -103,7 +106,7 @@\n \n - Use the project's domain glossary vocabulary.\n - Avoid volatile file paths unless they are essential.\n-- Include implementation decisions, testing decisions, out-of-scope items, and relevant ADR/domain links.\n+- Include implementation decisions, testing decisions, out-of-scope items, and relevant SADR/target-ADR/domain links.\n - Identify deep-module opportunities and testable interfaces.\n \n When creating issues:\n@@ -121,7 +124,7 @@\n - Branch/worktree target.\n - Scope and non-goals.\n - Dependencies and locks.\n-- Relevant docs, ADRs, modules, and tests.\n+- Relevant docs, SADRs, target ADRs, modules, and tests.\n - Required skill invocations.\n - TDD expectations.\n - Pre-PR `code-review` gate expectations.\n@@ -152,7 +155,7 @@\n \n - Treat yourself as a judge, not implementer.\n - Do not write production code.\n-- Read `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant ADRs, PRD, task issue, PR body, diff, checks, reviews, labels, and `.shiki/` state.\n+- Read `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant SADRs and target ADRs, PRD, task issue, PR body, diff, checks, reviews, labels, and `.shiki/` state.\n - Evaluate every applicable checklist in `docs/agents/checklists.md`.\n - Map each acceptance criterion to durable evidence.\n - Distinguish implementation failure from missing evidence.\ndiff --git a/CONTEXT.md b/CONTEXT.md\nindex e61a9be06a0cce0d026484083a657bde2067e234..237ee8aa3ea8fab334ece49768bc5ed3b5af0974\n--- a/CONTEXT.md\n+++ b/CONTEXT.md\n@@ -25,7 +25,7 @@\n _Avoid_: jumping directly into code\n \n **grill-with-docs**:\n-A planning skill that challenges a plan against domain language, ADRs, code reality, and concrete edge scenarios. It resolves design-tree questions before PRD/issues. It runs inside Requirements Definition.\n+A planning skill that challenges a plan against domain language, Shiki SADRs, target ADRs, code reality, and concrete edge scenarios. It resolves design-tree questions before PRD/issues. It runs inside Requirements Definition.\n _Avoid_: generic brainstorming, silent assumptions\n \n **Requirements Definition**:\ndiff --git a/CONTRIBUTING.md b/CONTRIBUTING.md\nindex c6d9b656e73abb8a55e5f660123e03153dcb3e6d..9e37a8fbf668db5b22461aaae5548b8f1354394c\n--- a/CONTRIBUTING.md\n+++ b/CONTRIBUTING.md\n@@ -17,7 +17,7 @@\n 1. **Goal** \u2014 start from a user-approved Goal with an outcome, completion\n    conditions, non-goals, risk level, and success signals.\n 2. **grill-with-docs** \u2014 for any non-trivial change, challenge the plan against\n-   domain language, ADRs, and code reality before writing a PRD.\n+   domain language, Shiki SADRs, target ADRs, and code reality before writing a PRD.\n 3. **PRD** \u2014 capture settled product/engineering intent (`to-prd`).\n 4. **Spec Freeze** \u2014 the operator explicitly approves the PRD/requirements;\n    the plan records a `spec_freeze` block and no plan runs without it\n@@ -94,7 +94,7 @@\n   entries.\n - Do not hand-edit `.shiki/goals/*.json` or `.shiki/manifest.json` in a task PR.\n - Conversation state is not durable truth. If a decision matters, record it in\n-  GitHub, `.shiki/`, `CONTEXT.md`, or an ADR.\n+  GitHub, `.shiki/`, `CONTEXT.md`, a Shiki SADR, or a target ADR.\n \n ## Architecture Gate\n \n@@ -103,7 +103,7 @@\n or CI policy, package structure, three or more structural files, more than 100\n net new lines in a core module, three or more new files, or any high-risk label.\n When triggered, run `grill-with-docs` or `improve-codebase-architecture` and\n-record durable decisions in an ADR when needed (see `docs/adr/`).\n+record durable decisions in a Shiki SADR or target ADR as applicable (see `docs/adr/`).\n \n ## Safety\n \ndiff --git a/SYSTEM_PROMPT.md b/SYSTEM_PROMPT.md\nindex aaaea45e3a896c3a44ba89eec8bb84325ecf6c62..da009c86a88285269dee0155e58e550c6d744ea9\n--- a/SYSTEM_PROMPT.md\n+++ b/SYSTEM_PROMPT.md\n@@ -30,6 +30,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n Use the canonical source-of-truth order above. Runtime-specific reading orders\n are operational guidance only and must not override or reorder it. The current\n conversation is non-durable operator input.\n@@ -48,14 +51,14 @@\n \n A runtime may only do work that is explicitly assigned by the Goal, task, PR, or MergeGate decision.\n \n-Do not assume `openai/codex-action`, `OPENAI_API_KEY`, or API-key based Codex automation in the default Shiki path. API-key runners are explicit target-repo extensions and require their own ADR.\n+Do not assume `openai/codex-action`, `OPENAI_API_KEY`, or API-key based Codex automation in the default Shiki path. API-key runners are explicit target-repo extensions and require their own target ADR.\n \n ## 4. Non-negotiable Shiki flow\n \n For any non-trivial Goal, use this flow:\n \n 1. **Goal Seek**: define outcome, non-goals, risk level, completion criteria, and evidence requirements.\n-2. **grill-with-docs**: challenge the plan against `CONTEXT.md`, ADRs, code reality, terminology, boundaries, and edge scenarios. Ask one question at a time when the operator is available. If code can answer a question, inspect code instead of asking.\n+2. **grill-with-docs**: challenge the plan against `CONTEXT.md`, Shiki SADRs, target ADRs, code reality, terminology, boundaries, and edge scenarios. Ask one question at a time when the operator is available. If code can answer a question, inspect code instead of asking.\n 3. **Context & Impact**: identify modules, interfaces, seams, callers, dependencies, locks, risk, and verification surfaces.\n 4. **to-prd + Spec Freeze**: turn settled context into a PRD using domain vocabulary and testing decisions. The operator's explicit approval of the PRD is Spec Freeze, recorded as the plan's `spec_freeze` block; plans without it do not run. Steps 1-4 form Requirements Definition.\n 5. **to-issues**: decompose the PRD into independently grabbable vertical-slice issues. Prefer AFK slices; mark HITL slices when judgment is still required.\n@@ -75,7 +78,7 @@\n Before making any material change:\n \n 1. Read `AGENTS.md` and any runtime-specific wrapper such as `CLAUDE.md`.\n-2. Read `CONTEXT.md` and relevant ADRs.\n+2. Read `CONTEXT.md` and relevant SADRs and target ADRs.\n 3. Read the GitHub Issue / PR and `.shiki/` state for the active Goal or task.\n 4. Identify your role: Planner, Implementer, Reviewer, Completion Check Agent, Repairer, or Guardian-assist.\n 5. Identify Goal id, task id, branch, PR, dependencies, locks, risk level, required skills, and required evidence.\n@@ -103,7 +106,7 @@\n \n At minimum, identify:\n \n-- Relevant documents, ADRs, domain terms, and prior decisions.\n+- Relevant documents, SADRs, target ADRs, domain terms, and prior decisions.\n - Relevant modules, interfaces, seams, callers, owners, and tests.\n - Dependency relationships between tasks.\n - Candidate file locks and lock conflicts.\n@@ -152,7 +155,7 @@\n \n CCA must read:\n \n-- `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant ADRs.\n+- `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant SADRs and target ADRs.\n - Parent Goal issue, PRD issue, task issue, comments, and labels.\n - PR body, diff, commits, changed files, reviews, CI checks, and artifacts available to the workflow.\n - `.shiki/` task contract, locks, ledger entries, CCA checklist profile, and prior repair packets.\n@@ -214,7 +217,7 @@\n - Three or more new files.\n - Any high-risk or critical label.\n \n-When the architecture gate triggers, run `grill-with-docs` or `improve-codebase-architecture` as appropriate and record the decision in an ADR if it is hard to reverse or likely to be re-litigated.\n+When the architecture gate triggers, run `grill-with-docs` or `improve-codebase-architecture` as appropriate and record the decision in a Shiki SADR or target ADR as applicable if it is hard to reverse or likely to be re-litigated.\n \n ## 14. Skill Gate\n \n@@ -223,7 +226,7 @@\n Use these skills:\n \n - `setup-matt-pocock-skills`: before first use of the engineering skill set, or when issue tracker, triage labels, or domain docs are missing.\n-- `grill-with-docs`: default for non-trivial Goals before PRD/issues; required for ambiguous plans, terminology, boundaries, ADR-worthy tradeoffs, or design-tree decisions.\n+- `grill-with-docs`: default for non-trivial Goals before PRD/issues; required for ambiguous plans, terminology, boundaries, decision-record-worthy tradeoffs, or design-tree decisions.\n - `zoom-out`: unfamiliar code area or missing architectural map.\n - `to-prd`: convert settled context into a PRD.\n - `to-issues`: break a PRD, plan, or Goal into independently grabbable vertical-slice issues.\ndiff --git a/docs/adr/0001-keep-shiki-as-platform-name.md b/docs/adr/SADR-0001-keep-shiki-as-platform-name.md\nindex 20a9637c3b9d8c97de7ba8db7cfae4d82d9a7dba..8b685439959aada1b221c112f0ac3c51ca8a1695\n--- a/docs/adr/0001-keep-shiki-as-platform-name.md\n+++ b/docs/adr/SADR-0001-keep-shiki-as-platform-name.md\n@@ -1,3 +1,3 @@\n-# Keep Shiki As The Platform Name\n+# SADR-0001: Keep Shiki As The Platform Name\n \n Shiki remains the product name for the GitHub-native agentic engineering control plane. We considered more descriptive names such as MergePilot, MergeGate, and Kiroku, but kept Shiki because it carries the existing goal-seeking framework identity while remaining independent of Hermes, Codex, Claude Code, or any single runtime.\ndiff --git a/docs/adr/0002-use-agents-as-shared-constitution.md b/docs/adr/SADR-0002-use-agents-as-shared-constitution.md\nindex 73de30ecb078120eac540d9bc51855e3cd48d0e3..c5486182bf06fca36ea508496b013c67302fd989\n--- a/docs/adr/0002-use-agents-as-shared-constitution.md\n+++ b/docs/adr/SADR-0002-use-agents-as-shared-constitution.md\n@@ -1,3 +1,3 @@\n-# Use AGENTS As The Shared Agent Constitution\n+# SADR-0002: Use AGENTS As The Shared Agent Constitution\n \n Shiki uses `AGENTS.md` as the runtime-neutral constitution and `CLAUDE.md` as a thin Claude Code wrapper. This keeps Codex, Claude Code, Hermes Runner, and future Agent Runtimes aligned on the same GitHub-first MergeGate rules while still allowing runtime-specific instructions where needed.\ndiff --git a/docs/adr/0003-require-skill-gate-for-mergegate-readiness.md b/docs/adr/SADR-0003-require-skill-gate-for-mergegate-readiness.md\nindex 424be0c9af4c8f0136f1fde68a468c47ff84e764..7fc337987c7df9792f75f181dfcbed75fe5ffff2\n--- a/docs/adr/0003-require-skill-gate-for-mergegate-readiness.md\n+++ b/docs/adr/SADR-0003-require-skill-gate-for-mergegate-readiness.md\n@@ -1,3 +1,3 @@\n-# Require Skill Gate For MergeGate Readiness\n+# SADR-0003: Require Skill Gate For MergeGate Readiness\n \n Shiki requires relevant engineering skills to be invoked before execution when their triggers apply. This makes skills part of MergeGate readiness rather than optional prompt style, because Shiki depends on repeatable planning, issue decomposition, TDD, diagnosis, triage, and architecture review before autonomous or parallel agent work can be trusted.\ndiff --git a/docs/adr/0004-use-github-first-with-shiki-mirror.md b/docs/adr/SADR-0004-use-github-first-with-shiki-mirror.md\nindex d0a3a9174dc646a945a9d325954ffaf00c352040..a79c54ae9ee8eed2a0524192b864da4e22a92d3f\n--- a/docs/adr/0004-use-github-first-with-shiki-mirror.md\n+++ b/docs/adr/SADR-0004-use-github-first-with-shiki-mirror.md\n@@ -1,4 +1,4 @@\n-# ADR 0004: Use GitHub First With A Repository-Local Shiki Mirror\n+# SADR-0004: Use GitHub First With A Repository-Local Shiki Mirror\n \n ## Status\n \ndiff --git a/docs/adr/0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md b/docs/adr/SADR-0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\nindex b339baf49b89d9290e79c3d51b36e7b2bde3bd95..0f3c3eb9afccdb3403ce978e21d04fe12e495da0\n--- a/docs/adr/0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\n+++ b/docs/adr/SADR-0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\n@@ -1,8 +1,8 @@\n-# ADR 0005: Use Codex As Front And Claude Code Action As The GitHub Runtime\n+# SADR-0005: Use Codex As Front And Claude Code Action As The GitHub Runtime\n \n ## Status\n \n-Accepted (implementer default superseded by ADR 0008; the auth model and the\n+Accepted (implementer default superseded by SADR-0008; the auth model and the\n Claude Code Action GitHub runtime decision remain in force)\n \n ## Context\n@@ -26,4 +26,4 @@\n - Shiki avoids requiring `OPENAI_API_KEY` for the default workflow.\n - Codex work happens through the user's authenticated Codex front surface, not as a hidden backend Action.\n - Claude Code Action is configured for OAuth-token based subscription usage by default.\n-- API-key based automation remains possible only as an explicit target-repo extension with its own ADR.\n+- API-key based automation remains possible only as an explicit target-repo extension with its own target ADR.\ndiff --git a/docs/adr/0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md b/docs/adr/SADR-0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\nindex 4bcae9d1e52f0a37b8e9fbafa5cd14b6ee1993bd..7a38538a6ae9929567f0ade67e7ea6a60d0b3f7e\n--- a/docs/adr/0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\n+++ b/docs/adr/SADR-0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\n@@ -1,4 +1,4 @@\n-# ADR 0006: Separate Codex Implementation, CCA Completion, And MergeGate Authorization\n+# SADR-0006: Separate Codex Implementation, CCA Completion, And MergeGate Authorization\n \n ## Status\n \ndiff --git a/docs/adr/0007-packaging-and-release.md b/docs/adr/SADR-0007-packaging-and-release.md\nindex 79b84ddb97167372b44641e5b7860e400ce80822..bb445a2e2e856d9d1f5fdbe35de8d4e8cec0fb32\n--- a/docs/adr/0007-packaging-and-release.md\n+++ b/docs/adr/SADR-0007-packaging-and-release.md\n@@ -1,4 +1,4 @@\n-# ADR 0007: Packaging And Release For Shiki\n+# SADR-0007: Packaging And Release For Shiki\n \n ## Status\n \n@@ -25,7 +25,7 @@\n   not introduce third-party runtime dependencies or break the shim.\n - Releases must run inside the operator's subscription-authenticated toolchain\n   and must not require extra secrets beyond the built-in `GITHUB_TOKEN`\n-  (consistent with ADR 0005's no-API-key-by-default stance).\n+  (consistent with SADR-0005's no-API-key-by-default stance).\n - `.shiki/manifest.json` already encodes the install surface (`install.include`,\n   `install.create_directories`, `install.exclude_from_commit`), and it must not\n   be mutated as part of this change.\n@@ -34,7 +34,7 @@\n \n This is a hard-to-reverse platform decision (it sets the version source of\n truth, the release trigger, and the public install contract), so it is recorded\n-as an ADR.\n+as a SADR.\n \n ## Decision\n \n@@ -104,7 +104,7 @@\n 3. **Publishing to PyPI.** Rejected for now: Shiki is distributed as a\n    repository template + git-installable CLI, not a PyPI library; PyPI would add\n    credentials/secrets and a different versioning contract. Can be added later as\n-   its own ADR if a packaged distribution is needed.\n+   its own SADR if a packaged distribution is needed.\n 4. **Dynamic version (read `VERSION` via a build backend `dynamic` field).**\n    Rejected to keep `pyproject.toml` backend-agnostic and dependency-free; the\n    small duplication between `VERSION` and `pyproject.toml` is guarded by the\n@@ -123,4 +123,4 @@\n   releasing versions the whole platform but ships only `install.include` to\n   targets.\n - Future PyPI distribution or a console entry point are deferred and would each\n-  warrant their own ADR.\n+  warrant their own SADR.\ndiff --git a/docs/adr/0008-use-claude-code-as-default-implementer.md b/docs/adr/SADR-0008-use-claude-code-as-default-implementer.md\nindex dedf4510b7c7799521422f622029563d373db031..35c2625d908167fbc58855b5b63e283805f167e8\n--- a/docs/adr/0008-use-claude-code-as-default-implementer.md\n+++ b/docs/adr/SADR-0008-use-claude-code-as-default-implementer.md\n@@ -1,12 +1,12 @@\n-# ADR 0008: Use Claude Code As The Default Implementer Runtime\n+# SADR-0008: Use Claude Code As The Default Implementer Runtime\n \n ## Status\n \n-Proposed (supersedes ADR 0005 for the implementer default; keeps its auth model)\n+Proposed (supersedes SADR-0005 for the implementer default; keeps its auth model)\n \n ## Context\n \n-ADR 0005 made Codex Front the default implementation runtime, with Claude Code\n+SADR-0005 made Codex Front the default implementation runtime, with Claude Code\n restricted to planner and reviewer roles by the runtime registry, and the only\n autonomous implementation adapter was `shiki runner codex`.\n \n@@ -18,13 +18,13 @@\n - The platform's planning, review, and CCA layers already run on Claude Code,\n   and Claude Code's native capabilities (the built-in `/code-review` skill and\n   the Workflow multi-agent orchestration tool) have no Codex equivalent.\n-- The Goal lifecycle redesign (ADR 0009) requires an autonomous post-freeze\n+- The Goal lifecycle redesign (SADR-0009) requires an autonomous post-freeze\n   loop driven from the operator's Claude Code session, including bounded\n   repair dispatch, which must not depend on a runtime that cannot be invoked\n   unattended.\n \n The constitution claims Shiki is runtime-agnostic (\"not a Claude-only\n-workflow\", ADR 0002). Any change to the implementer default must preserve the\n+workflow\", SADR-0002). Any change to the implementer default must preserve the\n registry-based runtime model rather than hardcoding one vendor.\n \n ## Decision\n@@ -39,7 +39,7 @@\n   the task to review or repair-needed.\n - Keep Codex registered as an optional implementer runtime. `shiki runner\n   codex`, the Codex handoff templates, and the ChatGPT OAuth auth model from\n-  ADR 0005 remain supported for tasks explicitly assigned to `codex`.\n+  SADR-0005 remain supported for tasks explicitly assigned to `codex`.\n - Commit to mandating two Claude-native capabilities as loop contract in\n   follow-up Goals (status: the code-review gate was delivered by Goal G-B \u2014\n   skill registry, checklists item PR-12, default required_skills \u2014 while the\n@@ -60,7 +60,7 @@\n \n Out of scope: removing Codex support, changing the CCA runtime (Claude Code\n Action with `CLAUDE_CODE_OAUTH_TOKEN` stays), and API-key based automation\n-(still requires its own ADR per ADR 0005).\n+(still requires its own target ADR per SADR-0005).\n \n ## Consequences\n \n@@ -100,7 +100,7 @@\n   harness rejection of direct `codex exec` is outside Shiki's control, and the\n   plan's mandatory native capabilities cannot run on Codex at all.\n - **Remove Codex entirely**: rejected; it abandons the runtime-agnostic\n-  constitution (ADR 0002) for no operational gain and forecloses\n+  constitution (SADR-0002) for no operational gain and forecloses\n   cross-runtime adversarial review.\n - **GitHub Actions cloud implementation via claude-code-action**: rejected as\n   default; durable evidence is automatic but token cost, Node 24 workflow\ndiff --git a/docs/adr/0009-requirements-definition-and-spec-freeze.md b/docs/adr/SADR-0009-requirements-definition-and-spec-freeze.md\nindex 29b3f7871bef9203a8b4a831a65c39eb2f506e42..52e7dd69ad0aa4efe0b68c62678abbf7df02aaec\n--- a/docs/adr/0009-requirements-definition-and-spec-freeze.md\n+++ b/docs/adr/SADR-0009-requirements-definition-and-spec-freeze.md\n@@ -1,4 +1,4 @@\n-# ADR 0009: Merge Grill And Planning Into Requirements Definition With Spec Freeze\n+# SADR-0009: Merge Grill And Planning Into Requirements Definition With Spec Freeze\n \n ## Status\n \ndiff --git a/docs/adr/0010-external-ai-guardian-review.md b/docs/adr/SADR-0010-external-ai-guardian-review.md\nindex e6108bbe8bb4e73e0ff4684c8f78001a80438c4e..f3510b145664298adafb151b6f0db51b972f01ec\n--- a/docs/adr/0010-external-ai-guardian-review.md\n+++ b/docs/adr/SADR-0010-external-ai-guardian-review.md\n@@ -1,4 +1,4 @@\n-# ADR 0010: External AI Guardian Review As A First-Class Approval Authority\n+# SADR-0010: External AI Guardian Review As A First-Class Approval Authority\n \n ## Status\n \n@@ -8,7 +8,7 @@\n \n Shiki is intended to be an autonomous, recursive, self-improving agent\n framework: authority-in-the-loop, not human-in-the-loop. Until now the Guardian\n-approval required for high/critical-risk merges (guardian-policy.json, ADR\n+approval required for high/critical-risk merges (guardian-policy.json, SADR\n governance) recognized only human authority sources \u2014 a GitHub review approval,\n the `guardian:approved` label applied by a configured Guardian, or a comment\n carrying `Guardian approval granted` plus the current head SHA.\n@@ -89,7 +89,7 @@\n   autonomous design the operator intends and produced the impersonation\n   incident.\n - **Record AI approval as an operator comment**: rejected as forgery of human\n-  authority \u2014 the exact failure this ADR corrects.\n+  authority \u2014 the exact failure this SADR corrects.\n - **Deliver the artifact as a committed `.shiki/` file**: rejected; a committed\n   approval inside the PR being approved is forgeable by anyone who can push.\n - **A dedicated reviewer bot identity instead of a relayed comment**: deferred;\ndiff --git a/docs/adr/0011-autonomous-quality-gate-evidence.md b/docs/adr/SADR-0011-autonomous-quality-gate-evidence.md\nindex bca0246c5d46e44afbde15486a75eec425fc02f5..9c93eac16d8c3a6d4b11bd7224e3955102cd7db4\n--- a/docs/adr/0011-autonomous-quality-gate-evidence.md\n+++ b/docs/adr/SADR-0011-autonomous-quality-gate-evidence.md\n@@ -1,4 +1,4 @@\n-# ADR 0011: Autonomous Quality-Gate Evidence \u2014 Deterministic Facts And An Independent Verifier\n+# SADR-0011: Autonomous Quality-Gate Evidence \u2014 Deterministic Facts And An Independent Verifier\n \n ## Status\n \ndiff --git a/docs/adr/0012-autonomous-loop-closeout-to-main.md b/docs/adr/SADR-0012-autonomous-loop-closeout-to-main.md\nindex 637fad6dde4989c9c2bf0f81f676d8d29991123c..ec7d85f81030a676bcbba28b43eb365422a36eac\n--- a/docs/adr/0012-autonomous-loop-closeout-to-main.md\n+++ b/docs/adr/SADR-0012-autonomous-loop-closeout-to-main.md\n@@ -1,4 +1,4 @@\n-# 0012 \u2014 Autonomous loop pushes goal completion to main via a closeout PR\n+# SADR-0012 \u2014 Autonomous loop pushes goal completion to main via a closeout PR\n \n - Status: accepted\n - Date: 2026-06-18\ndiff --git a/docs/adr/0013-bootstrap-grants-actions-approve-pull-requests.md b/docs/adr/SADR-0013-bootstrap-grants-actions-approve-pull-requests.md\nindex b6c69d64f1c5ced346b88d6859586715b3b5bd0e..cf48ff4a6716267f539ca58edd1a852e7c7a2d64\n--- a/docs/adr/0013-bootstrap-grants-actions-approve-pull-requests.md\n+++ b/docs/adr/SADR-0013-bootstrap-grants-actions-approve-pull-requests.md\n@@ -1,4 +1,4 @@\n-# ADR 0013: Bootstrap Grants GitHub Actions Permission To Create And Approve Pull Requests\n+# SADR-0013: Bootstrap Grants GitHub Actions Permission To Create And Approve Pull Requests\n \n ## Status\n \n@@ -31,7 +31,7 @@\n security tradeoff: it lets repository automation satisfy a human-review gate. In\n a multi-maintainer repo that could let a compromised or buggy workflow\n self-approve changes. So whether bootstrap turns it on by default \u2014 and with\n-what default token scope \u2014 is an ADR-worthy decision, not an implementation\n+what default token scope \u2014 is a SADR-worthy decision, not an implementation\n detail.\n \n ## Decision\n@@ -48,7 +48,7 @@\n Specifically:\n \n - **`can_approve_pull_request_reviews` defaults to `true`.** Shiki's default\n-  posture is solo/AFK autonomous operation (ADR 0008/0009/0012), where the CCA\n+  posture is solo/AFK autonomous operation (SADR-0008/0009/0012), where the CCA\n   Review Bridge is the only path that can satisfy `required_review: true` after\n   the CCA and Guardian gates have already passed. The default makes a\n   freshly-bootstrapped repo able to run the autonomous loop end to end without a\ndiff --git a/docs/adr/0014-codex-app-external-ai-guardian-ui-adapter.md b/docs/adr/SADR-0014-codex-app-external-ai-guardian-ui-adapter.md\nindex 238db06279334caa341c117362df9271382a7fcd..65f12e9939321e1b3e389a34c822fc1549e5155d\n--- a/docs/adr/0014-codex-app-external-ai-guardian-ui-adapter.md\n+++ b/docs/adr/SADR-0014-codex-app-external-ai-guardian-ui-adapter.md\n@@ -1,4 +1,4 @@\n-# ADR 0014: Use Codex App As The External AI Guardian UI Adapter\n+# SADR-0014: Use Codex App As The External AI Guardian UI Adapter\n \n ## Status\n \ndiff --git a/docs/adr/0015-contract-approval-before-dispatch.md b/docs/adr/SADR-0015-contract-approval-before-dispatch.md\nindex fad6c656741a20a7684ba16080a7db159f63a52f..6061271a46d3f1d6ae7174477de3fb75673123a6\n--- a/docs/adr/0015-contract-approval-before-dispatch.md\n+++ b/docs/adr/SADR-0015-contract-approval-before-dispatch.md\n@@ -1,4 +1,4 @@\n-# ADR 0015: Approve The Task Contract Before Dispatch, Not The Diff After It\n+# SADR-0015: Approve The Task Contract Before Dispatch, Not The Diff After It\n \n ## Status\n \n@@ -9,7 +9,7 @@\n Guardian approval for high/critical risk is expressible only on a pull request.\n Every approval source in `guardian-policy.json` \u2014 GitHub review, the\n `guardian:approved` label, the `Guardian approval granted` comment, and the\n-`external_ai_guardian_review` artifact (ADR 0010/0014) \u2014 is bound to a PR, and\n+`external_ai_guardian_review` artifact (SADR-0010/0014) \u2014 is bound to a PR, and\n three of the four require the current head SHA. Approval therefore cannot exist\n before an implementation PR exists.\n \n@@ -81,7 +81,7 @@\n An envelope breach must surface only on the non-repairable policy gate, never on\n the repairable `MergeGate metadata check`. Otherwise the Goal Loop routes it to\n `dispatch_repair` \u2014 instructing a runner to make the breach go away \u2014 which is\n-exactly what this ADR forbids.\n+exactly what this SADR forbids.\n \n **Contract immutability.** After registration, an implementation PR must not\n change the governance fields of its own task contract (`scope`, `non_goals`,\n@@ -116,7 +116,7 @@\n fails any verdict that is not `complete`, and the signal the CCA prompt reads is\n computed from `evaluate_guardian_approval` alone. Without the OR-merge at the\n signal, a contract-approved PR still returns `needs_guardian`, the CCA job fails,\n-and the MergeGate policy check never runs \u2014 the gate this ADR changes would never\n+and the MergeGate policy check never runs \u2014 the gate this SADR changes would never\n be reached. The CCA *prompt* needs no change: a `contract_approval` entry in the\n signal's existing `sources` array satisfies its current wording.\n \n@@ -130,7 +130,7 @@\n written, and an approved contract lets its implementation PRs merge through the\n same automatic path as low/medium risk. The wasted implement-then-block cycle\n disappears. Guardian review shifts from reading diffs to reading contracts, which\n-is both earlier and smaller; the external AI Guardian path (ADR 0010/0014) works\n+is both earlier and smaller; the external AI Guardian path (SADR-0010/0014) works\n on contracts with no change.\n \n We accept that the Guardian authorizes a class of change without seeing its\n@@ -146,7 +146,7 @@\n adversarial tests for the new approval path equivalent in rigor to\n `test_shiki_governance_evidence.sh`.\n \n-Bootstrap costs, which apply only while this ADR is being implemented. The tasks\n+Bootstrap costs, which apply only while this SADR is being implemented. The tasks\n that build Contract Approval merge under the existing rules, so the ones\n dispatched after the pre-dispatch gate lands but before the approval path lands\n need a Guardian-approved Contract PR *and* a live Guardian approval on their\n@@ -210,7 +210,7 @@\n risk; they do not bound authorized work.\n \n **Auto-escalate risk on envelope breach instead of stopping.** Returns the\n-workflow to a diff-time Guardian decision \u2014 the exact behavior this ADR removes.\n+workflow to a diff-time Guardian decision \u2014 the exact behavior this SADR removes.\n \n **Resolve governance fields from the base snapshot only.** Rejected against the\n registration flow: implementation PRs add their own contract, so the base has no\ndiff --git a/docs/adr/0016-locks-bound-product-paths.md b/docs/adr/SADR-0016-locks-bound-product-paths.md\nindex b5e850f3f1f7f4ef6e7f9d1763a29a51bd300c45..46b40a9b99a7aae644c02bb458f5473ff0a90ce2\n--- a/docs/adr/0016-locks-bound-product-paths.md\n+++ b/docs/adr/SADR-0016-locks-bound-product-paths.md\n@@ -1,4 +1,4 @@\n-# ADR 0016: Locks bound product paths; the Shiki mirror is bound by record identity\n+# SADR-0016: Locks bound product paths; the Shiki mirror is bound by record identity\n \n ## Status\n \n@@ -6,8 +6,8 @@\n \n - Date: 2026-07-28\n - Deciders: operator (mizutani-140), Claude (planner)\n-- Related: ADR 0004 (GitHub-first with a `.shiki` mirror), ADR 0009 (Spec Freeze),\n-  ADR 0012 (autonomous loop closeout to main), ADR 0015 (Contract Approval before\n+- Related: SADR-0004 (GitHub-first with a `.shiki` mirror), SADR-0009 (Spec Freeze),\n+  SADR-0012 (autonomous loop closeout to main), SADR-0015 (Contract Approval before\n   dispatch), remediation goal task R-02, the live PR #179 deadlock.\n \n ## Context\n@@ -90,11 +90,11 @@\n files, documentation files and named source modules are nameable; runtime-\n allocated ledger, runner and report ids are not \u2014 and those live in `.shiki`,\n which locks no longer govern. Existing frozen contracts are brought into line by\n-a one-time Spec Amendment per plan, which must land before ADR 0015's contract\n+a one-time Spec Amendment per plan, which must land before SADR-0015's contract\n immutability makes `locks` unwritable from the implementation PR.\n \n **Lock state is resolved from the durable side.** MergeGate resolves the lock\n-registry, and ADR 0015's governance fields, from the base `.shiki` snapshot when\n+registry, and SADR-0015's governance fields, from the base `.shiki` snapshot when\n present, falling back to head only when absent. The `MergeGate metadata check`\n job is given `--base-shiki`, which it does not receive today. Locks are not\n derived from the live open-pull-request set: a pull request body is untrusted\n@@ -108,7 +108,7 @@\n Explicitly out of scope: moving MergeGate's contract resolution off the PR\n checkout wholesale; making the mirror a projection of GitHub state; introducing\n new lock kinds; moving ledger evidence out of the repository; and unwinding\n-ADR 0012's closeout pull request. Registration of multi-task goals is ADR 0015's\n+SADR-0012's closeout pull request. Registration of multi-task goals is SADR-0015's\n Contract PR and is not re-decided here.\n \n ## Consequences\n@@ -127,7 +127,7 @@\n \n Risks accepted. First, goal-scoped identity is only as strong as `goal_id`,\n which is resolved from the PR body and the head task file; the mitigation is to\n-add `goal_id` to ADR 0015's frozen governance field set, and until that lands a\n+add `goal_id` to SADR-0015's frozen governance field set, and until that lands a\n task file can name a foreign goal. Second, base-when-present resolution is\n unavailable on fork pull requests, where the head is contributor-controlled;\n fork pull requests already cannot complete a Shiki merge, and this decision does\n@@ -139,7 +139,7 @@\n path, not by implementation pull requests.\n \n New obligations: one Spec Amendment per frozen plan to rewrite `locks`, ordered\n-before ADR 0015 T1; extension of the adversarial suites in\n+before SADR-0015 T1; extension of the adversarial suites in\n `scripts/test_shiki_adversarial_state.sh` and\n `scripts/test_shiki_governance_evidence.sh` to cover the eleven newly-scoped\n directories; and updating `scripts/test_shiki_loop_lock_guard.sh`,\n@@ -151,32 +151,32 @@\n whether the loop's transitive sync of ledger `evidence` references can be\n deleted once the Contract PR carries its own source plan.\n \n-## Relationship to ADR 0004\n+## Relationship to SADR-0004\n \n-This ADR **clarifies something ADR 0004 left unspecified**. ADR 0004 decides that\n+This SADR **clarifies something SADR-0004 left unspecified**. SADR-0004 decides that\n GitHub is the operational source of truth and that each target repository keeps a\n `.shiki/` mirror. It says nothing about pull requests writing the mirror, does\n not name MergeGate, and does not make the mirror an input to a gate. Nothing in\n-ADR 0004's Decision or Consequences is amended, superseded or weakened: the\n+SADR-0004's Decision or Consequences is amended, superseded or weakened: the\n mirror still exists, still records the same artifacts, and pull request\n-boundaries remain the coordination surface. What this ADR settles is the question\n+boundaries remain the coordination surface. What this SADR settles is the question\n 0004 never reached \u2014 by what authority a pull request may write a mirror record \u2014\n and it answers it the way `.shiki/manifest.json` already declares\n (`pr_mutation: current-task-or-current-goal-only`).\n \n-It **amends ADR 0012**, whose Decision item 5 states that \"Loop-executed tasks\n+It **amends SADR-0012**, whose Decision item 5 states that \"Loop-executed tasks\n declare the `path:.shiki/**` lock, which covers every `.shiki` file the closeout\n stages ... so `files_outside_locks` is satisfied without per-file lock\n bookkeeping.\" That clause is replaced: the closeout's mirror writes are\n-authorized by record identity, not by a declared lock. ADR 0012's substantive\n+authorized by record identity, not by a declared lock. SADR-0012's substantive\n decision \u2014 that completion reaches `main` through a closeout pull request rather\n than local attestation \u2014 is preserved unchanged.\n \n-It is **complementary to ADR 0015**, not a substitute. ADR 0015 owns contract\n+It is **complementary to SADR-0015**, not a substitute. SADR-0015 owns contract\n immutability, the pre-dispatch gate, the Contract PR and the Scope Envelope. This\n-ADR owns what a lock means and who may write the mirror. The one ordering\n+SADR owns what a lock means and who may write the mirror. The one ordering\n constraint between them is that the lock-narrowing Spec Amendment must precede\n-ADR 0015 T1's freezing of the `locks` field.\n+SADR-0015 T1's freezing of the `locks` field.\n \n ## Alternatives Considered\n \n@@ -186,7 +186,7 @@\n the only writer that can register them may run. Migrating the Guardian signal to\n that store makes every registration PR fail closed to `needs_guardian` at every\n risk level, because the task file is by definition absent from the store, which\n-removes the low/medium autonomous path ADR 0008/0009 exists to provide. And it\n+removes the low/medium autonomous path SADR-0008/0009 exists to provide. And it\n leaves schemas, manifest, config, guardian policy, templates, migrations, plans,\n worktrees, runner records, runs and handoffs with no writer at all in a\n repository whose product is `.shiki`. Measured benefit on the frozen backlog:\n@@ -199,14 +199,14 @@\n namespace is not in fact ownership-partitioned \u2014 56 ledger entries carry no\n `task_id`, and one goal DAG is writable by 35 registered tasks. The correct\n expression of its insight is that locks do not govern the mirror at all, which is\n-what this ADR decides.\n+what this SADR decides.\n \n **Make the mirror a projection written only by a registration writer and a\n post-merge projection job.** Rejected. It requires a bypass actor with write\n access to the protected default branch; it makes its own master invariant\n readable from a manifest that pull requests may edit; it deletes both reconcile\n modes and leaves a corrupted mirror with no in-band repair path, contradicting\n-ADR 0004's consequence that mirror conflicts are repaired against GitHub state;\n+SADR-0004's consequence that mirror conflicts are repaired against GitHub state;\n and its content-derived ids are rejected by `validate_shiki`'s currently-green id\n pattern.\n \ndiff --git a/docs/adr/0017-bookkeeping-closeouts-do-not-inherit-risk.md b/docs/adr/SADR-0017-bookkeeping-closeouts-do-not-inherit-risk.md\nindex 554e3a0d526a72ca067108c5464cc68c3edcf174..b9676857a4713850e063af7c39c8e0b01ecb4c66\n--- a/docs/adr/0017-bookkeeping-closeouts-do-not-inherit-risk.md\n+++ b/docs/adr/SADR-0017-bookkeeping-closeouts-do-not-inherit-risk.md\n@@ -1,4 +1,4 @@\n-# ADR 0017: A Bookkeeping Closeout Does Not Inherit Its Task's Risk\n+# SADR-0017: A Bookkeeping Closeout Does Not Inherit Its Task's Risk\n \n ## Status\n \n@@ -6,7 +6,7 @@\n \n ## Context\n \n-ADR 0012 made the goal loop push a task's completion to the default branch through a\n+SADR-0012 made the goal loop push a task's completion to the default branch through a\n closeout pull request: after the implementation PR merges, the loop opens a second PR\n carrying `task.status = done`, `lock.state = released`, and \u2014 when the task completes\n its goal \u2014 `goal.status = complete` plus one scorecard report. Nothing else. The\n@@ -49,14 +49,14 @@\n 4. The task file's governance fields (`scope`, `non_goals`, `required_skills`,\n    `risk_level`, `locks`, `acceptance_checks`, `test_command`) are byte-identical to\n    the base snapshot. Only `status`, `expected_pr`, `closeout_pr`, `ledger_evidence`\n-   and `expected_branch` may differ \u2014 the ADR 0015 contract-immutability rule,\n+   and `expected_branch` may differ \u2014 the SADR-0015 contract-immutability rule,\n    unchanged.\n \n    `expected_branch` is in that set because a closeout is *by construction* on a\n    different branch from the implementation it closes, and the loop rewrites the\n    field accordingly. It is safe to let it differ because MergeGate independently\n    requires `expected_branch` to equal the PR's actual head ref, so a PR cannot set\n-   it to anything other than the branch it is really on. The first draft of this ADR\n+   it to anything other than the branch it is really on. The first draft of this SADR\n    omitted it, which made the exemption unreachable: every real closeout failed\n    condition 4 on a field the loop always changes. See PR #209.\n 5. The state transitions are exactly the terminal set: task `review` \u2192 `done`, lock\n@@ -113,12 +113,12 @@\n classification must be derived, not asserted.\n \n **Fold the closeout into the implementation PR** so no second PR exists. Rejected under\n-ADR 0012, which recorded why: a task file that is already `done` on the implementation\n+SADR-0012, which recorded why: a task file that is already `done` on the implementation\n branch short-circuits the loop's review \u2192 CCA \u2192 merge \u2192 done ordering into a local\n goal-completion before any merge happens.\n \n **Have the loop auto-merge closeouts with elevated credentials.** Rejected outright: that\n-is a runtime authoring its own approval, which ADR 0010 and the constitution's\n+is a runtime authoring its own approval, which SADR-0010 and the constitution's\n approval-record prohibition forbid. It would also be indistinguishable, in the evidence,\n from the forgery those rules exist to prevent.\n \ndiff --git a/docs/adr/0018-carried-guardian-approval-across-a-proven-base-sync.md b/docs/adr/SADR-0018-carried-guardian-approval-across-a-proven-base-sync.md\nindex 22358562da390ed8b45cd600da08a43d378b4680..e6e2907cefa776460af73ddf7d39db605f3c39b0\n--- a/docs/adr/0018-carried-guardian-approval-across-a-proven-base-sync.md\n+++ b/docs/adr/SADR-0018-carried-guardian-approval-across-a-proven-base-sync.md\n@@ -1,4 +1,4 @@\n-# ADR 0018: Carried Guardian Approval Across A Proven Base Sync\n+# SADR-0018: Carried Guardian Approval Across A Proven Base Sync\n \n ## Status\n \n@@ -34,7 +34,7 @@\n Guardian evaluator that consumes it (`shiki_guardian.evaluate_guardian_approval`,\n the `guardian_comment_carried` source) were built and tested in isolation. Neither\n does anything until a caller passes the flags that turn the carry on, so the\n-feature was invisible in CI. This ADR records the decision the two of them\n+feature was invisible in CI. This SADR records the decision the two of them\n implement, and this task wires it into the four CI invocations that judge Guardian\n approval.\n \ndiff --git a/docs/adr/README.md b/docs/adr/README.md\nindex c70e057a4520c36e042ab68c53cd4a21ef709b71..5da43856836e2ce6a150f8127766e2f88355ab4b\n--- a/docs/adr/README.md\n+++ b/docs/adr/README.md\n@@ -1,40 +1,41 @@\n-# Architecture Decision Records\n+# Decision Record Namespaces\n \n-This directory holds Shiki's Architecture Decision Records (ADRs): durable\n-records of hard-to-reverse platform decisions. ADRs are part of the Shiki source\n-of truth alongside GitHub state, the `.shiki/` mirror, and `CONTEXT.md`.\n+This directory holds two independent decision namespaces:\n \n-## How ADRs Work\n+- Shiki Architecture Decision Records (SADRs), named\n+  `SADR-NNNN-<short-slug>.md`, record hard-to-reverse Shiki platform decisions.\n+- Target Architecture Decision Records (ADRs), named\n+  `NNNN-<short-slug>.md`, belong to the target product.\n \n-- New ADRs are added as `docs/adr/NNNN-<short-slug>.md`, where `NNNN` is the next\n-  zero-padded sequence number.\n-- The **canonical list of ADRs is the set of files matching `docs/adr/*.md`** in\n-  this directory. Browse `docs/adr/*.md` for the authoritative, current set.\n-- Use [`template.md`](template.md) as the starting point for a new ADR.\n-- Record an ADR when a decision is hard to reverse, surprising without context,\n-  and a real tradeoff. Do not write an ADR for routine or easily reversible\n-  choices.\n+The same four-digit number may appear once in each namespace. A platform\n+`SADR-NNNN` citation resolves only to `SADR-NNNN-*.md`; a target `ADR NNNN`\n+citation resolves only to `NNNN-*.md`.\n \n-Using the `docs/adr/*.md` glob as the source of truth means other PRs can add an\n-ADR without editing this index file, avoiding merge contention on a\n-hand-maintained enumeration.\n+## How Decision Records Work\n \n-## Current ADRs (snapshot)\n-\n-The list below is a convenience snapshot and may lag behind the directory. When\n-in doubt, list `docs/adr/*.md`.\n+- New Shiki platform decisions use the next zero-padded SADR number.\n+- New target product decisions use the target's own ADR sequence.\n+- The canonical list is the files in `docs/adr/`; no hand-maintained index is\n+  authoritative.\n+- Use [`template.md`](template.md) as the starting point and choose the filename\n+  namespace that owns the decision.\n+- Record a decision when it is hard to reverse, surprising without context, and\n+  a real tradeoff. Do not write a decision record for routine or easily\n+  reversible choices.\n \n-- [0001 \u2014 Keep Shiki As The Platform Name](0001-keep-shiki-as-platform-name.md)\n-- [0002 \u2014 Use AGENTS As The Shared Agent Constitution](0002-use-agents-as-shared-constitution.md)\n-- [0003 \u2014 Require Skill Gate For MergeGate Readiness](0003-require-skill-gate-for-mergegate-readiness.md)\n-- [0004 \u2014 Use GitHub First With A Repository-Local Shiki Mirror](0004-use-github-first-with-shiki-mirror.md)\n-- [0005 \u2014 Use Codex As Front And Claude Code Action As The GitHub Runtime](0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md)\n-- [0006 \u2014 Separate Codex Implementation, CCA Completion, And MergeGate Authorization](0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md)\n+Using the directory as the source of truth lets independent changes add records\n+without contending on an enumerated index.\n \n-## Adding A New ADR\n+## Adding A Shiki SADR\n \n-1. Copy `template.md` to `docs/adr/NNNN-<short-slug>.md`.\n+1. Copy `template.md` to `docs/adr/SADR-NNNN-<short-slug>.md`.\n 2. Fill in the title, status, context, decision, and consequences.\n-3. Set the status to `Proposed`, then `Accepted` once the decision is approved.\n-4. Open a PR following `CONTRIBUTING.md`. You do not need to edit this index for\n-   the ADR to be discoverable via `docs/adr/*.md`.\n+3. Set the status to `Proposed`, then `Accepted` once the proper authority has\n+   approved the decision.\n+4. Open a PR following `CONTRIBUTING.md`.\n+\n+## Adding A Target ADR\n+\n+1. Copy `template.md` to `docs/adr/NNNN-<short-slug>.md` using the target's own\n+   sequence.\n+2. Cite it as `ADR NNNN`; do not prefix its filename with `SADR-`.\ndiff --git a/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md b/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\nnew file mode 100644\nindex 0000000000000000000000000000000000000000..7455ce1f3bed02ddddb851ac21a1d1a648c9aeba\n--- /dev/null\n+++ b/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\n@@ -0,0 +1,55 @@\n+# SADR-0019: Separate Shiki Platform SADRs From Target ADRs\n+\n+## Status\n+\n+Accepted\n+\n+## Context\n+\n+Shiki installs its platform decision records into `docs/adr/` in every target\n+repository. Shiki and its targets previously used the same numeric\n+`NNNN-<slug>.md` namespace. A target that already owned ADRs 0013 through 0018\n+therefore acquired two records for each number during installation, making an\n+`ADR NNNN` citation ambiguous. Renumbering target decisions would rewrite target\n+history, while renumbering Shiki decisions per target would destroy their stable\n+platform identity.\n+\n+Upgrades also need to remove the former Shiki-owned numeric paths without ever\n+mistaking a target-owned ADR for platform content. The install stamp already\n+records exact shipped paths and content digests, so it can prove ownership only\n+when both still match.\n+\n+## Decision\n+\n+Shiki platform decisions use the `SADR` namespace:\n+\n+- records are named `docs/adr/SADR-NNNN-<slug>.md`;\n+- live platform citations use `SADR-NNNN`;\n+- target product decisions retain `docs/adr/NNNN-<slug>.md` and `ADR NNNN`;\n+- the reference validator resolves the two namespaces independently, so the same\n+  four-digit number may exist once in each namespace.\n+\n+Before a forced upgrade writes anything, the installer inventories the exact\n+former numeric Shiki ADR paths. It may delete a legacy path only when the\n+existing install stamp contains that exact path and its recorded SHA-256 digest\n+matches the file currently on disk. All ownership blockers are reported\n+together and abort the upgrade before its first write. A non-forced install\n+never performs this cleanup.\n+\n+Historical `.shiki/` evidence remains unchanged because it records the namespace\n+that existed when that evidence was produced.\n+\n+## Consequences\n+\n+- Targets may use any ADR number without colliding with Shiki's platform SADRs.\n+- Fresh installs preserve target ADR bytes and install a stable SADR set.\n+- Forced upgrades fail closed when legacy ownership cannot be proven, and a\n+  successful upgrade refreshes the stamp without removed legacy paths.\n+- Current governance, packaging, workflow, prompt, script, and skill references\n+  must move with the platform records.\n+\n+## Alternatives Considered\n+\n+- Collision-only refusal leaves affected targets permanently unupgradable.\n+- Renumbering target ADRs mutates target-owned history.\n+- Per-target Shiki renumbering makes platform citations non-portable.\ndiff --git a/docs/adr/template.md b/docs/adr/template.md\nindex a92a97daddd0b00ab9dd626cc8d86942da2eafed..17dd79175b5fee2105935557f691b40cf9492942\n--- a/docs/adr/template.md\n+++ b/docs/adr/template.md\n@@ -1,14 +1,14 @@\n-# NNNN: <Short Decision Title>\n+# <SADR-NNNN or NNNN>: <Short Decision Title>\n \n ## Status\n \n-Proposed | Accepted | Superseded by ADR-NNNN\n+Proposed | Accepted | Superseded by SADR-NNNN or target ADR NNNN\n \n ## Context\n \n What is the situation, constraint, or force that requires a decision? Reference\n-relevant domain terms (`CONTEXT.md`), prior ADRs, and code reality. State the\n-problem, not the solution.\n+relevant domain terms (`CONTEXT.md`), prior Shiki SADRs or target ADRs, and code\n+reality. State the problem, not the solution.\n \n ## Decision\n \ndiff --git a/docs/agents/autonomous-loop-e2e-acceptance.md b/docs/agents/autonomous-loop-e2e-acceptance.md\nindex bbf84c6081987f159ac6ad9ecf3c82b7aa72e7e8..5c13c1d02a05a90b0dc46cbef7c95a9846d0c237\n--- a/docs/agents/autonomous-loop-e2e-acceptance.md\n+++ b/docs/agents/autonomous-loop-e2e-acceptance.md\n@@ -40,8 +40,8 @@\n 3. `gh auth status` is logged in with merge permission on the repo.\n 4. Branch protection + required checks are configured exactly as the goal-loop\n    required-check set expects (see `docs/agents/branch-protection-smoke.md`).\n-5. The repo `.shiki/policy` permits autonomous low/medium-risk merges (ADR\n-   0008/0009); the MergeGate policy check is the recorded-authority gate.\n+5. The repo `.shiki/policy` permits autonomous low/medium-risk merges\n+   (SADR-0008/0009); the MergeGate policy check is the recorded-authority gate.\n \n The operator's only actions are: register the task and start the loop. **No\n manual pre-clearing** means after `shiki loop run` starts, the operator does not\ndiff --git a/docs/agents/bootstrap-command.md b/docs/agents/bootstrap-command.md\nindex 05f7f203a52cf412b9f97084a6e131ed0f785afd..f7aa0b734dfebee4a3b139335b66ee3415f48713\n--- a/docs/agents/bootstrap-command.md\n+++ b/docs/agents/bootstrap-command.md\n@@ -189,7 +189,9 @@\n ```\n \n Use this only for tests, fixtures, or explicit local-only template inspection.\n-Use `--force` only when you intentionally want to overwrite existing target files.\n+Use `--force` only when you intentionally want to refresh the target template.\n+Legacy numeric Shiki decision cleanup is stamp-and-digest authorized before any\n+write; target ADR paths are not cleanup candidates.\n \n ## Slash Command\n \ndiff --git a/docs/agents/checklists.md b/docs/agents/checklists.md\nindex 4a7f03ade346f53a1c1d82981f3fd744f9ab4547..0c5ec6c53dc7197b7b7a7d18e667b325babd6c45\n--- a/docs/agents/checklists.md\n+++ b/docs/agents/checklists.md\n@@ -32,18 +32,18 @@\n | ID | Item | Blocking |\n | --- | --- | --- |\n | GD-01 | Plan was challenged against `CONTEXT.md`. | blocking for non-trivial Goals |\n-| GD-02 | Relevant ADRs were read or absence was recorded. | blocking for non-trivial Goals |\n+| GD-02 | Relevant SADRs and target ADRs were read or absence was recorded. | blocking for non-trivial Goals |\n | GD-03 | Ambiguous terms were resolved or converted to HITL blockers. | blocking |\n | GD-04 | Edge scenarios were considered. | advisory |\n | GD-05 | Code reality was checked when code could answer. | blocking when applicable |\n | GD-06 | New domain terms were added to `CONTEXT.md` when needed. | blocking when applicable |\n-| GD-07 | ADR was created or explicitly skipped using the ADR criteria. | blocking when applicable |\n+| GD-07 | A SADR or target ADR was created or explicitly skipped using the decision-record criteria. | blocking when applicable |\n \n ## CI \u2014 Context & Impact Checklist\n \n | ID | Item | Blocking |\n | --- | --- | --- |\n-| CI-01 | Relevant docs and ADRs identified. | blocking |\n+| CI-01 | Relevant docs, SADRs, and target ADRs identified. | blocking |\n | CI-02 | Relevant modules, interfaces, seams, callers, and tests identified. | blocking |\n | CI-03 | Dependencies and blocked-by relationships identified. | blocking |\n | CI-04 | Candidate locks identified. | blocking |\n@@ -62,7 +62,7 @@\n | PRD-04 | Implementation decisions are recorded without volatile code detail. | blocking |\n | PRD-05 | Testing decisions are recorded. | blocking |\n | PRD-06 | Out-of-scope items are explicit. | blocking |\n-| PRD-07 | Relevant glossary/ADR links are present when applicable. | blocking |\n+| PRD-07 | Relevant glossary/SADR/target-ADR links are present when applicable. | blocking |\n \n ## SF \u2014 Spec Freeze Checklist\n \n@@ -98,7 +98,7 @@\n | H-02 | Branch/worktree target is present. | blocking |\n | H-03 | Scope and non-goals are present. | blocking |\n | H-04 | Dependencies and locks are present. | blocking |\n-| H-05 | Relevant docs, ADRs, modules, and tests are referenced. | blocking |\n+| H-05 | Relevant docs, SADRs, target ADRs, modules, and tests are referenced. | blocking |\n | H-06 | Required skills are stated. | blocking |\n | H-07 | TDD expectations are stated. | blocking when behavior work |\n | H-08 | Acceptance criteria are included. | blocking |\ndiff --git a/docs/agents/codex-handoff.md b/docs/agents/codex-handoff.md\nindex ede468f47cb277f69a857e8c45df703e4ed0151d..6f97bdffb67e44d7edea2c71e01ea28933c4eb6c\n--- a/docs/agents/codex-handoff.md\n+++ b/docs/agents/codex-handoff.md\n@@ -46,7 +46,7 @@\n \n ## Context\n - Domain terms:\n-- Relevant ADRs:\n+- Relevant SADRs and target ADRs:\n - Relevant docs:\n - Relevant modules/interfaces/seams:\n - Similar tests/prior art:\ndiff --git a/docs/agents/completion-check-agent.md b/docs/agents/completion-check-agent.md\nindex 13e4c035db35025f4e3cdcc5adfb24314ac4d521..61bfa1218cf36ca605250c91919e620f33238aa0\n--- a/docs/agents/completion-check-agent.md\n+++ b/docs/agents/completion-check-agent.md\n@@ -32,7 +32,7 @@\n - `AGENTS.md`\n - `CLAUDE.md`\n - `CONTEXT.md`\n-- relevant ADRs\n+- relevant SADRs and target ADRs\n - parent Goal issue\n - PRD issue or document\n - task issue\ndiff --git a/docs/agents/decision-control.md b/docs/agents/decision-control.md\nindex 7d8ee5ab03a5ccbcbd04056ae61de03785a2a9c9..8c94ecda00fffaa0e92c325ab83aa9776bd1dac1\n--- a/docs/agents/decision-control.md\n+++ b/docs/agents/decision-control.md\n@@ -11,7 +11,7 @@\n | Decision | Owner | When | Required inputs | Output | Enforcement surface |\n | --- | --- | --- | --- | --- | --- |\n | Is the Goal clear enough to plan? | Planner + Guardian when needed | Goal intake | User request, existing docs, risk signals | Goal readiness verdict | Goal checklist, issue template, ledger |\n-| Must `grill-with-docs` run? | Planner / Skill Gate | Before PRD or issue split | Ambiguity, domain terms, ADR conflicts, unclear boundaries | Skill invocation or explicit skip rationale | Skill Gate checklist |\n+| Must `grill-with-docs` run? | Planner / Skill Gate | Before PRD or issue split | Ambiguity, domain terms, SADR/target-ADR conflicts, unclear boundaries | Skill invocation or explicit skip rationale | Skill Gate checklist |\n | Is a PRD required? | Planner | After Goal context stabilizes | Goal, `grill-with-docs` output, Context & Impact output | PRD required / not required | PRD checklist, CCA evidence check |\n | Can the spec be frozen? | Operator only | End of Requirements Definition, before issue split | PRD/requirements summary, scope inventory (SF-02), open-question list | `spec_freeze` block in the plan + ledger evidence | `require_grilled_plan` (plan ingest/run), SF checklist |\n | Can a frozen spec change? | Operator only (Spec Amendment) | When implementation contradicts the frozen spec | Contested decisions, scoped re-grill result | recorded amendment + re-stamped freeze | SF checklist, ledger evidence |\ndiff --git a/docs/agents/domain.md b/docs/agents/domain.md\nindex 258da8ea81e87ac0a6b0b5b5e5c2fde980eaabb7..d57b0cf17ff3e1fa9e6e5885372bf083d8295421\n--- a/docs/agents/domain.md\n+++ b/docs/agents/domain.md\n@@ -5,7 +5,9 @@\n ## Layout\n \n - `CONTEXT.md` defines Shiki language. Keep it implementation-free.\n-- `docs/adr/` records hard-to-reverse decisions.\n+- `docs/adr/SADR-NNNN-*.md` records Shiki platform decisions.\n+- `docs/adr/NNNN-*.md` records target product decisions without sharing the\n+  SADR namespace.\n - `AGENTS.md` is the runtime-neutral agent constitution.\n - `CLAUDE.md` is the Claude Code wrapper around `AGENTS.md`.\n - `.shiki/` is the Target Repository mirror for Goals, PRDs, plans, Task DAGs, contracts, locks, worktree records, ledger entries, CCA verdicts, repair packets, reports, and handoffs.\n@@ -15,6 +17,6 @@\n \n - Use Shiki terms exactly when they exist.\n - Add glossary terms to `CONTEXT.md` only when the term is part of the domain, not a generic programming concept.\n-- Create ADRs only for hard-to-reverse, surprising, tradeoff-based decisions.\n+- Create SADRs or target ADRs only for hard-to-reverse, surprising, tradeoff-based decisions.\n - Keep product-specific language in the Target Repository, not in Shiki Template docs.\n-- If an ADR conflicts with a proposed implementation, surface the conflict before editing.\n+- If a SADR or target ADR conflicts with a proposed implementation, surface the conflict before editing.\ndiff --git a/docs/agents/execution-control.md b/docs/agents/execution-control.md\nindex ad8ee3cdb36be56fedc1f46408ca92b387d74047..bc63c3a73661d12b567016ba299f1a6b66bf9088\n--- a/docs/agents/execution-control.md\n+++ b/docs/agents/execution-control.md\n@@ -63,7 +63,7 @@\n | Branch name | Orchestrator | Orchestrator | Before implementer dispatch | branch policy | branch ref in task/ledger |\n | Lock reservation | Context & Impact | MergeGate | Before dispatch and before merge | `.shiki/locks/`, labels, PR metadata | lock id, owner, scope |\n | CI profile | Planner | MergeGate | During issue triage, rechecked at PR | labels, `.shiki/policy.yaml`, workflow matrix | required check list |\n-| Required status checks | Repo maintainer / Guardian | GitHub branch protection / rulesets | Repository setup, updated via ADR | GitHub settings | branch/ruleset config, ADR |\n+| Required status checks | Repo maintainer / Guardian | GitHub branch protection / rulesets | Repository setup, updated via target ADR | GitHub settings | branch/ruleset config, target ADR |\n | CCA judgment | CCA | GitHub required check / MergeGate | Every non-draft PR update | GitHub Action check | structured CCA verdict |\n | Repair assignment | CCA | MergeGate | After failed CCA/CI/review | repair packet, issue/PR comment | repair id and attempt count |\n | Merge readiness | MergeGate | GitHub branch protection / Guardian if needed | After CCA complete | required checks, review rules, rulesets | mergegate check output |\ndiff --git a/docs/agents/guardian-policy.md b/docs/agents/guardian-policy.md\nindex 4147c7adf05a47adb4fd24ada1a1c2e1f2ba6d2d..a20d9cc6c00a569bb057b9ca8d6b51c7fc4a9847\n--- a/docs/agents/guardian-policy.md\n+++ b/docs/agents/guardian-policy.md\n@@ -98,8 +98,8 @@\n `origin/<default_branch>` and reuses each job's existing base fetch; a shallow\n (`--depth`) or second base fetch would make the proof unresolvable and silently\n disable the carry, so neither is permitted. CCA reads `guardian_comment_carried` as\n-a recorded authority in the deterministic signal and does not re-derive it. See ADR\n-0018 for the full decision, the rejected diff/patch-equality and path-disjointness\n+a recorded authority in the deterministic signal and does not re-derive it. See\n+SADR-0018 for the full decision, the rejected diff/patch-equality and path-disjointness\n alternatives, and the accepted residual.\n \n ## External AI Guardian Review (SADR-0010)\ndiff --git a/docs/agents/implementation-policy.md b/docs/agents/implementation-policy.md\nindex c754144a9fb5bf4a38aa82a60e1670c59e626b44..85c37281bcad196c6b7b02343faf00b03cd9c57a\n--- a/docs/agents/implementation-policy.md\n+++ b/docs/agents/implementation-policy.md\n@@ -31,10 +31,13 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n ## Non-Negotiables\n \n 1. **Planning precedes implementation.** Non-trivial Goals start with `grill-with-docs` and Context & Impact.\n-2. **Domain language is controlled.** Terms must be checked against `CONTEXT.md`; hard-to-reverse tradeoffs must be checked against ADRs.\n+2. **Domain language is controlled.** Terms must be checked against `CONTEXT.md`; hard-to-reverse tradeoffs must be checked against Shiki SADRs and target ADRs.\n 3. **PRD precedes issue decomposition.** `to-prd` is used once the context is settled enough to become durable intent.\n 4. **Issues are vertical slices.** `to-issues` must produce independently grabbable tracer-bullet tasks, not horizontal layer tickets.\n 5. **Triage controls dispatch.** Only `ready-for-agent` / AFK tasks may be assigned to Codex without human input.\n@@ -95,19 +98,19 @@\n Process:\n \n - Challenge terms against `CONTEXT.md`.\n-- Check ADRs before reopening decisions.\n+- Check SADRs and target ADRs before reopening decisions.\n - Inspect code when code can answer a question.\n - Ask one question at a time when the operator is available.\n - Provide a recommended answer with each question.\n - Record resolved terms in `CONTEXT.md`.\n-- Create ADRs only for hard-to-reverse, surprising, tradeoff-based decisions.\n+- Create SADRs or target ADRs only for hard-to-reverse, surprising, tradeoff-based decisions.\n - Convert unresolved load-bearing questions into HITL blockers.\n \n Outputs:\n \n - Resolved design-tree decisions.\n - Updated glossary terms when needed.\n-- ADRs when justified.\n+- SADRs or target ADRs when justified.\n - HITL blockers when decisions remain unresolved.\n \n ## Phase 3 \u2014 Context & Impact\n@@ -117,7 +120,7 @@\n \n Outputs must include:\n \n-- Relevant documents and ADRs.\n+- Relevant documents, SADRs, and target ADRs.\n - Relevant modules, interfaces, seams, callers, and tests.\n - Candidate locks.\n - Dependencies.\n@@ -140,7 +143,7 @@\n - Testing decisions.\n - Out of scope.\n - Further notes.\n-- Links to relevant Goal, domain terms, and ADRs.\n+- Links to relevant Goal, domain terms, SADRs, and target ADRs.\n \n Phase 4 ends with Spec Freeze: the operator explicitly approves the PRD, the\n plan records a `spec_freeze` block (status, approved_by, source), and the SF\n@@ -240,7 +243,7 @@\n - Dependencies and locks.\n - Risk and Guardian requirements.\n - Ledger completeness.\n-- Documentation and ADR updates.\n+- Documentation and SADR/target-ADR updates.\n - Scope drift.\n \n CCA emits one of:\n@@ -301,7 +304,7 @@\n | Evidence failure | Behavior might be done, but proof is missing. | Add checks, PR evidence, ledger entry, or artifacts. |\n | Scope drift | PR changes unrelated behavior. | Revert unrelated changes or split into new Goal. |\n | Dependency block | Upstream task or lock not complete. | Block until dependency resolves. |\n-| Architecture block | Decision needs ADR or deep-module discussion. | `grill-with-docs` or `improve-codebase-architecture`. |\n+| Architecture block | Decision needs a SADR, target ADR, or deep-module discussion. | `grill-with-docs` or `improve-codebase-architecture`. |\n | Risk block | Security/production/policy change needs human approval. | Guardian decision. |\n \n ## Phase 5.5 \u2014 Execution Decision Control\ndiff --git a/docs/agents/runtime-auth-model.md b/docs/agents/runtime-auth-model.md\nindex 6a2e1a47e81ea89b5c68a210ec07772a9374babc..3c54748d24233dbd0ba8473950282547ef4ce935\n--- a/docs/agents/runtime-auth-model.md\n+++ b/docs/agents/runtime-auth-model.md\n@@ -148,7 +148,7 @@\n \n API-key based runners may be added by a target repository as an explicit extension. They are not the default Shiki template path.\n \n-If a repo opts into API-key mode, record the exception in an ADR and update the repo's `AGENTS.md`, `.shiki/config.yaml`, and workflow permissions.\n+If a repo opts into API-key mode, record the target-specific exception in a target ADR and update the repo's `AGENTS.md`, `.shiki/config.yaml`, and workflow permissions.\n \n ## GitHub Actions Runtime Compatibility\n \ndiff --git a/docs/agents/skill-gate.md b/docs/agents/skill-gate.md\nindex c50398b0d7c60a8598f2a5c88a10015730dced13..3acffe6444617bb1d54b77d4b4708158ec735bc6\n--- a/docs/agents/skill-gate.md\n+++ b/docs/agents/skill-gate.md\n@@ -9,7 +9,7 @@\n | Skill | Trigger | Expected output |\n | --- | --- | --- |\n | `setup-matt-pocock-skills` | First configuring a repo for these skills, or when issue tracker, triage labels, or domain docs are missing. | `docs/agents/` configuration and an `Agent skills` block in `AGENTS.md` or `CLAUDE.md`. |\n-| `grill-with-docs` | Ambiguous plan, unclear terminology, boundary decision, tradeoff, or ADR-worthy design choice. | Clarified decision tree, recommended answers, updates to `CONTEXT.md` or ADRs when decisions crystallize. |\n+| `grill-with-docs` | Ambiguous plan, unclear terminology, boundary decision, tradeoff, or decision-record-worthy design choice. | Clarified decision tree, recommended answers, updates to `CONTEXT.md`, SADRs, or target ADRs when decisions crystallize. |\n | `zoom-out` | Agent lacks a higher-level architectural map. | Map of relevant modules, callers, seams, and domain concepts. |\n | `to-prd` | Settled context should become a PRD. | PRD published to the issue tracker with problem, solution, user stories, decisions, testing, and out-of-scope notes. |\n | `to-issues` | Goal, PRD, or plan must become implementation tickets. | Vertical-slice issues with acceptance criteria, dependencies, and AFK/HITL classification. |\n@@ -17,7 +17,7 @@\n | `tdd` | Feature work or bug fix has observable behavior that can be specified with tests. | Red-green-refactor loop through public interfaces, one vertical slice at a time. |\n | `code-review` | The TDD loop is green and the implementer is about to create or update a PR. Default for every implementation task. | Pre-PR self-review of the task diff with fixes applied on the task branch; findings and resolutions recorded as a ledger entry naming the skill and a `## Pre-PR code review` PR body section. |\n | `diagnose` | Hard bug, failing check, regression, flaky behavior, or performance problem. | Reproduction loop, ranked hypotheses, instrumentation, fix, regression test, post-mortem. |\n-| `improve-codebase-architecture` | Structural friction, testability, AI-navigability, deep-module opportunity, or refactor request. | Architecture candidates and deepening recommendations informed by `CONTEXT.md` and ADRs. |\n+| `improve-codebase-architecture` | Structural friction, testability, AI-navigability, deep-module opportunity, or refactor request. | Architecture candidates and deepening recommendations informed by `CONTEXT.md`, SADRs, and target ADRs. |\n | `prototype` | A throwaway logic or UI prototype is needed to answer a design question. | Clearly marked disposable prototype and captured decision. |\n \n ## MergeGate Interaction\ndiff --git a/docs/operations.md b/docs/operations.md\nindex cf1f452d4c589e38ccd58553bda0ad8684b51eb7..ace48a90ffbec9e5c0a7113bef16a4858bc44b3f\n--- a/docs/operations.md\n+++ b/docs/operations.md\n@@ -89,6 +89,16 @@\n      target history, brought forward by `shiki migrate`, not a template.\n    - **Everything else** (CLI scripts, workflows, schemas, docs) is overwritten.\n \n+   A forced upgrade also migrates the former numeric Shiki decision paths to\n+   `SADR-NNNN-*`. Before the first write, the installer inventories the exact\n+   legacy Shiki paths and authorizes each deletion only when the existing\n+   `.shiki/install-stamp.json` names that path and its SHA-256 digest still\n+   matches the file. An absent, unreadable, malformed, incomplete, or\n+   mismatching stamp reports every blocked path and stops with no filesystem\n+   change. Target-owned `NNNN-*` ADRs are not cleanup candidates. A non-force\n+   install deletes nothing and retains any still-matching legacy stamp entries\n+   so a later forced upgrade can prove ownership.\n+\n    If the target has pending migrations, `--force` refuses and writes nothing \u2014\n    apply migrations first (step 3), then re-run. The run ends with a single\n    summary of every `.new` written, so a half-upgrade is visible.\ndiff --git a/docs/releasing.md b/docs/releasing.md\nindex 10d73828e7dbd64e9d9f6b83bc84d010537ce62a..aafeee8c4d441e1e427efee2ce4f81a647f020e1\n--- a/docs/releasing.md\n+++ b/docs/releasing.md\n@@ -67,7 +67,7 @@\n CLI in `scripts/`. There are no third-party Python runtime dependencies. The CLI\n is intentionally **not** exposed as a `[project.scripts]` console entry point\n because `scripts/shiki.py` is a dependency-free shim and `scripts/` is not\n-packaged as an importable distribution (see the ADR for the rationale).\n+packaged as an importable distribution (see SADR-0007 for the rationale).\n \n Supported install paths:\n \n@@ -155,6 +155,15 @@\n    [docs/operations.md](operations.md#the-new-convention). If the target has\n    pending migrations, `--force` refuses and writes nothing; apply migrations\n    (step 2) first, then re-run.\n+\n+   The SADR namespace upgrade has an additional pre-write gate: each former\n+   numeric Shiki decision path is removed only when the existing install stamp\n+   contains that exact path and a digest matching its current bytes. The\n+   installer aggregates absent/unreadable/malformed-stamp, missing-digest, and\n+   digest-mismatch blockers before raising, so refusal leaves the target tree\n+   byte-identical. Numeric target ADRs are preserved. A successful forced\n+   upgrade installs and stamps `SADR-*` paths and drops the removed legacy paths\n+   from the refreshed stamp.\n 2. Apply repository-local `.shiki/` state migrations with the migrate command:\n \n    ```bash\ndiff --git a/docs/usage.md b/docs/usage.md\nindex b83407ee3970dd395dd893010bc5dbe8bad26051..a130ef6e40dfd8169a5bc701339351097df1a09f\n--- a/docs/usage.md\n+++ b/docs/usage.md\n@@ -67,8 +67,10 @@\n shiki install-target /path/to/target-repo --local-only\n ```\n \n-Use `--force` only when you intentionally want to overwrite existing target\n-files.\n+Use `--force` only when you intentionally want to refresh the target template.\n+For the SADR namespace migration, `--force` deletes only exact legacy Shiki\n+decision paths whose existing install-stamp digest still matches; any ownership\n+blocker stops before the first write. Numeric target ADRs are never candidates.\n \n ## `shiki bootstrap-platform` \u2014 publish the Shiki platform repo\n \ndiff --git a/scripts/guardian_approval_signal.py b/scripts/guardian_approval_signal.py\nindex d059f92b2c82aa2b5d6ca421365537a87886c1cb..16924892714f4f5b9303c0ef0e325a7e70075630\n--- a/scripts/guardian_approval_signal.py\n+++ b/scripts/guardian_approval_signal.py\n@@ -199,8 +199,8 @@\n ) -> Any:\n     \"\"\"Evaluate SADR-0015 Contract Approval for the CCA Guardian signal.\n \n-    The SAME OR that MergeGate applies must reach this deterministic signal: ADR\n-    0015 is explicit that without it, a contract-approved PR still returns\n+    The SAME OR that MergeGate applies must reach this deterministic signal:\n+    SADR-0015 is explicit that without it, a contract-approved PR still returns\n     ``needs_guardian``, the CCA job fails, and the MergeGate policy check never\n     runs \u2014 the gate this change moves would never be reached. This resolves the\n     task the SAME way MergeGate does (the first Shiki task id in the PR body),\ndiff --git a/scripts/shiki_contract_approval.py b/scripts/shiki_contract_approval.py\nindex 941bcb96fdd8b9a85bc0c4f0f04d5dad9d04839c..4a4b418f6527413161db82670bec857a043360e2\n--- a/scripts/shiki_contract_approval.py\n+++ b/scripts/shiki_contract_approval.py\n@@ -142,7 +142,7 @@\n     changed_files_status: Any,\n     registration: Any,\n ) -> ContractApprovalResult:\n-    \"\"\"Whether this PR carries an SADR-0015 Contract Approval.\n+    \"\"\"Whether this PR carries a SADR-0015 Contract Approval.\n \n     The four conditions are evaluated IN ORDER, each failure recorded as a\n     distinct reason string:\ndiff --git a/scripts/shiki_guardian.py b/scripts/shiki_guardian.py\nindex a4ecb79bbda55fd5c8b06adfae2eb99760a03c45..4dd99bbe714e26f421fa19abac7a34ae1b195ecc\n--- a/scripts/shiki_guardian.py\n+++ b/scripts/shiki_guardian.py\n@@ -12,7 +12,7 @@\n # The carry re-derives its proof from git in-process on every judging invocation;\n # it is never read from an artifact, so there is nothing to go stale or be forged.\n # shiki_sync_proof is the ONLY guardian-family module that runs git, and every one\n-# of its invocations is read-only and local (ADR: prove-from-git base sync).\n+# of its invocations is read-only and local (SADR: prove-from-git base sync).\n from shiki_sync_proof import verify_pure_base_sync\n \n GUARDIAN_POLICY_PATH = \".shiki/guardian-policy.json\"\ndiff --git a/scripts/shiki_guardian_review.py b/scripts/shiki_guardian_review.py\nindex dd0f104d457899e0ba5ff1e162011c1e90b6f92b..e10ef58c046324e7ea3a3392feb3048ca77414ce\n--- a/scripts/shiki_guardian_review.py\n+++ b/scripts/shiki_guardian_review.py\n@@ -159,7 +159,7 @@\n     docs_only = _is_docs_only(changed_files)\n     if docs_only and matched == 0:\n         focus.append(\n-            \"Docs/ADR-only change: confirm the change is genuinely documentation/decision-only, introduces no behavior or contract drift, and stays consistent with the constitution and existing ADRs.\"\n+            \"Docs/decision-record-only change: confirm the change is genuinely documentation/decision-only, introduces no behavior or contract drift, and stays consistent with the constitution and existing SADRs and target ADRs.\"\n         )\n \n     # Conservative augmentation: unknown (no category and not docs-only), mixed\ndiff --git a/scripts/shiki_installer.py b/scripts/shiki_installer.py\nindex be064ab7a29ff6836f7f7a1d70500a43e0d11d7b..9fd8eefd137ce7003dad5c0dcb2e48e2cd47ab32\n--- a/scripts/shiki_installer.py\n+++ b/scripts/shiki_installer.py\n@@ -118,6 +118,7 @@\n # what lets a target carry it without a governance-schema change.\n INSTALL_STAMP_PATH = \".shiki/install-stamp.json\"\n INSTALL_STAMP_VERSION = 1\n+LEGACY_SHIKI_SADR_MAX = 18\n \n # Under --force the shipped surface splits three ways, so nothing is lost and\n # nothing is silently stale.\n@@ -237,9 +238,98 @@\n         )\n \n \n+def legacy_shiki_adr_paths() -> tuple[str, ...]:\n+    \"\"\"Former numeric paths for Shiki SADR-0001 through SADR-0018.\n+\n+    The authoritative slugs come from the current SADR filenames. SADR-0019 is\n+    the namespace decision itself and never shipped under a numeric legacy path.\n+    \"\"\"\n+    paths: list[str] = []\n+    for sadr in sorted((ROOT / \"docs\" / \"adr\").glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\")):\n+        number = sadr.name.split(\"-\", 2)[1]\n+        if int(number) <= LEGACY_SHIKI_SADR_MAX:\n+            paths.append(f\"docs/adr/{sadr.name.removeprefix('SADR-')}\")\n+    return tuple(paths)\n+\n+\n+def preflight_legacy_sadr_cleanup(target: Path, *, force: bool) -> tuple[str, ...]:\n+    \"\"\"Authorize exact legacy Shiki ADR removals before the first write.\n+\n+    A non-force install never removes existing files. A forced install needs no\n+    stamp when no legacy Shiki path is present. When legacy paths do exist, each\n+    must be named in a readable install stamp and its current bytes must match\n+    the recorded digest.\n+    \"\"\"\n+    if not force:\n+        return ()\n+    present = tuple(\n+        relative\n+        for relative in legacy_shiki_adr_paths()\n+        if (target / relative).exists() or (target / relative).is_symlink()\n+    )\n+    if not present:\n+        return ()\n+\n+    stamp_path = target / INSTALL_STAMP_PATH\n+    try:\n+        raw = stamp_path.read_text(encoding=\"utf-8\")\n+        stamp = json.loads(raw)\n+    except FileNotFoundError:\n+        detail = \"install stamp is absent\"\n+        blockers = [f\"{relative}: {detail}\" for relative in present]\n+    except (OSError, UnicodeDecodeError) as error:\n+        detail = f\"install stamp is unreadable ({error})\"\n+        blockers = [f\"{relative}: {detail}\" for relative in present]\n+    except json.JSONDecodeError as error:\n+        detail = f\"install stamp is malformed ({error})\"\n+        blockers = [f\"{relative}: {detail}\" for relative in present]\n+    else:\n+        if not isinstance(stamp, dict):\n+            blockers = [\n+                f\"{relative}: install stamp is malformed (expected an object)\"\n+                for relative in present\n+            ]\n+        else:\n+            digests = stamp.get(\"digests\")\n+            blockers = []\n+            if not isinstance(digests, dict):\n+                blockers.extend(\n+                    f\"{relative}: install stamp has no digest for this path\"\n+                    for relative in present\n+                )\n+            else:\n+                for relative in present:\n+                    expected = digests.get(relative)\n+                    if not isinstance(expected, str) or not expected:\n+                        blockers.append(\n+                            f\"{relative}: install stamp has no digest for this path\"\n+                        )\n+                        continue\n+                    try:\n+                        actual = _sha256_file(target / relative)\n+                    except OSError as error:\n+                        blockers.append(\n+                            f\"{relative}: legacy path is unreadable ({error})\"\n+                        )\n+                        continue\n+                    if actual != expected:\n+                        blockers.append(\n+                            f\"{relative}: digest mismatch (stamp {expected}, current {actual})\"\n+                        )\n+\n+    if blockers:\n+        joined = \"\\n\".join(f\"- {blocker}\" for blocker in blockers)\n+        raise ShikiError(\n+            \"legacy Shiki ADR cleanup is not authorized:\\n\"\n+            f\"{joined}\\nNo files were written.\"\n+        )\n+    return present\n+\n+\n def install_template(target: Path, *, force: bool, validate: bool) -> list[NewFileNote]:\n     # Preconditions first: never rewrite the tree of a target that cannot be\n     # upgraded cleanly (see refuse_pending_migrations).\n+    legacy_cleanup = preflight_legacy_sadr_cleanup(target, force=force)\n     refuse_pending_migrations(target)\n \n     # Resolve the target's required CODEOWNERS owner once from its\n@@ -250,6 +340,10 @@\n     # verbatim. See ``codeowners_required_owner``.\n     codeowners_owner = codeowners_required_owner(target)\n \n+    for relative in legacy_cleanup:\n+        (target / relative).unlink()\n+        info(f\"removed stamped legacy Shiki ADR: {target / relative}\")\n+\n     notes: list[NewFileNote] = []\n     for relative in TEMPLATE_PATHS:\n         source = ROOT / relative\n@@ -647,6 +741,22 @@\n     digests = target_stamp_digests(target)\n     existing = read_install_stamp(target)\n     if existing is not None:\n+        # A non-force install deliberately leaves legacy Shiki ADRs in place.\n+        # Preserve only still-matching ownership proof so a later forced\n+        # upgrade can authorize cleanup; never carry a missing or stale digest.\n+        existing_digests = existing.get(\"digests\")\n+        if isinstance(existing_digests, dict):\n+            for relative in legacy_shiki_adr_paths():\n+                expected = existing_digests.get(relative)\n+                path = target / relative\n+                if not isinstance(expected, str) or not path.is_file():\n+                    continue\n+                try:\n+                    actual = _sha256_file(path)\n+                except OSError:\n+                    continue\n+                if actual == expected:\n+                    digests[relative] = actual\n         if _incoming_is_older(commit, existing.get(\"platform_commit\")):\n             warn(\n                 \"kept existing install stamp: incoming install \"\ndiff --git a/scripts/shiki_loop.py b/scripts/shiki_loop.py\nindex 3611641da8aeb5038b1da90e3114b09a473ec6d8..84caa491b9cad0edc4176f209c13e9221e975681\n--- a/scripts/shiki_loop.py\n+++ b/scripts/shiki_loop.py\n@@ -424,7 +424,7 @@\n         # unresolvable CCA verdict has already produced its terminal stop and is never\n         # laundered into a head-moving sync (SADR-0010). It also never fires while the\n         # policy (Guardian) gate is red \u2014 for ANY reason, not only when it is the sole\n-        # red check \u2014 so a head-bound Guardian approval, or an SADR-0015\n+        # red check \u2014 so a head-bound Guardian approval, or a SADR-0015\n         # contract-approval carry that base movement defeated (the policy gate compares\n         # base and head task governance directly), is never silently restored by\n         # adopting base wholesale. It is bounded by a durable per-task counter so the\ndiff --git a/scripts/shiki_tasks.py b/scripts/shiki_tasks.py\nindex aec3809f24b4a0c8c96fc50971cff9ae9ad30381..d531052e9d9717058d4631483cefab44c26a65c1\n--- a/scripts/shiki_tasks.py\n+++ b/scripts/shiki_tasks.py\n@@ -821,7 +821,7 @@\n         \"prompt\": prompt,\n         \"entry_skill\": \"grill-with-docs\",\n         \"required_next_steps\": [\n-            \"Run grill-with-docs until terminology, boundaries, risks, and ADR-worthy decisions are settled.\",\n+            \"Run grill-with-docs until terminology, boundaries, risks, and decision-record-worthy choices are settled.\",\n             \"Obtain the operator's explicit approval of the PRD/requirements (Spec Freeze).\",\n             \"Write a machine-readable plan JSON with grill_with_docs.status=complete and spec_freeze.status=frozen.\",\n             \"Run shiki plan ingest --plan-file PLAN.json.\",\ndiff --git a/scripts/test_shiki_goal_loop.sh b/scripts/test_shiki_goal_loop.sh\nindex 753c7b516e10f0be3bc92a4bd0eb1fa699776ae6..8f3820a0a079b3044424cfe1a6c6b7fe68896e67\n--- a/scripts/test_shiki_goal_loop.sh\n+++ b/scripts/test_shiki_goal_loop.sh\n@@ -231,8 +231,8 @@\n \n # High/critical risk merges autonomously when all required checks are green:\n # the \"MergeGate policy check\" required check IS the Guardian gate (it enforces\n-# guardian-policy.json \u2014 human approval OR external AI guardian review, ADR\n-# 0010), so green checks mean a recorded authority approved. The fake gh returns\n+# guardian-policy.json \u2014 human approval OR external AI guardian review,\n+# SADR-0010), so green checks mean a recorded authority approved. The fake gh returns\n # green checks, simulating that the Guardian gate passed.\n python3 \"$ROOT/scripts/shiki.py\" goal create --target \"$TARGET\" --title \"Guardian gate\" --outcome \"High risk merges via policy gate\" >/tmp/shiki-goal-loop-guardian-goal.json\n GGOAL=\"$(json_get /tmp/shiki-goal-loop-guardian-goal.json goal_id)\"\ndiff --git a/tests/test_adr_references.py b/tests/test_adr_references.py\nindex 51706329c797d5dd9b47e082a333fa83b261d482..db6e693e35f67103935be34f1d9bb4ff7ee60395\n--- a/tests/test_adr_references.py\n+++ b/tests/test_adr_references.py\n@@ -1,7 +1,7 @@\n-\"\"\"Every ``ADR ####`` citation must resolve to exactly one decision record.\n+\"\"\"Every target ADR and Shiki SADR citation resolves in its own namespace.\n \n Background. ``scripts/mergegate_check.py`` justifies ``is_bookkeeping_closeout``\n-with \"ADR 0017\" nine times and ``scripts/guardian_approval_signal.py`` cites\n+with \"SADR-0017\" nine times and ``scripts/guardian_approval_signal.py`` cites\n 0016/0017 seven times, yet for a stretch the numbering in ``docs/adr/`` stopped\n at 0015: the authoring commits lived on a coordinator branch and no task ever\n declared ``docs/adr/...`` in its locks, so nothing carried the records to the\n@@ -11,11 +11,12 @@\n and the directory drifted with no signal.\n \n This is the same unbound-pair shape behind other silent failures: a reference\n-and its target with nothing asserting they agree. This test binds them. It scans\n-every ``scripts/*.py`` (plus ``docs/**/*.md`` and the root ``*.md`` governance\n-files, which are cheap) for the ``ADR ####`` pattern and asserts each cited\n-number resolves to exactly one ``docs/adr/####-*.md``. The converse is\n-deliberately NOT required: an ADR may exist without being cited.\n+and its target with nothing asserting they agree. This test binds them. Target\n+``ADR NNNN`` citations resolve only to ``docs/adr/NNNN-*.md``; Shiki\n+``SADR-NNNN`` citations resolve only to ``docs/adr/SADR-NNNN-*.md``. The scan\n+covers current governance docs, scripts, workflows, prompts, packaging metadata,\n+and repo-local Codex guidance while deliberately excluding historical\n+``.shiki`` evidence. The converse is not required: a record may be uncited.\n \n The suite is dependency free: standard-library ``unittest`` only.\n \"\"\"\n@@ -31,17 +32,18 @@\n \n REPO_ROOT = Path(__file__).resolve().parents[1]\n \n-# \"ADR 0016\" -- a four-digit citation. The trailing \\b keeps a five-digit token\n-# from being read as a truncated four-digit number.\n-_CITATION = re.compile(r\"\\bADR (\\d{4})\\b\")\n-# \"0016-locks-bound-product-paths.md\" -- the leading four digits name the record.\n-_ADR_FILENAME = re.compile(r\"^(\\d{4})-.*\\.md$\")\n+# ``ADR 0016`` / ``SADR-0016`` -- a namespace plus four-digit citation. The\n+# trailing ``\\b`` keeps a five-digit token from being read as a truncated one.\n+_CITATION = re.compile(r\"\\b(S?ADR)[ -](\\d{4})\\b\")\n+# Target ADRs retain the numeric filename; Shiki platform decisions use SADR.\n+_DECISION_FILENAME = re.compile(r\"^(?:(SADR)-)?(\\d{4})-.*\\.md$\")\n \n \n class Violation(NamedTuple):\n     \"\"\"A citation that does not resolve to exactly one decision record.\"\"\"\n \n     citing_file: str  # repo-relative path of the file that cites the ADR\n+    namespace: str  # \"ADR\" for target decisions; \"SADR\" for Shiki decisions\n     number: str  # the cited four-digit number, e.g. \"0016\"\n     line: int  # 1-based line of the citation\n     reason: str  # \"missing\" (zero matches) or \"ambiguous\" (two or more)\n@@ -49,44 +51,59 @@\n \n     @property\n     def expected_path(self) -> str:\n-        return f\"docs/adr/{self.number}-*.md\"\n+        prefix = \"SADR-\" if self.namespace == \"SADR\" else \"\"\n+        return f\"docs/adr/{prefix}{self.number}-*.md\"\n \n     def describe(self) -> str:\n         \"\"\"Name the citing file, the cited number, and the missing path.\"\"\"\n         if self.reason == \"missing\":\n             return (\n-                f\"{self.citing_file}:{self.line} cites ADR {self.number} \"\n+                f\"{self.citing_file}:{self.line} cites {self.namespace} {self.number} \"\n                 f\"but no document matches {self.expected_path}\"\n             )\n         joined = \", \".join(self.matches)\n         return (\n-            f\"{self.citing_file}:{self.line} cites ADR {self.number} which \"\n+            f\"{self.citing_file}:{self.line} cites {self.namespace} {self.number} which \"\n             f\"resolves to {len(self.matches)} documents (expected exactly one \"\n             f\"at {self.expected_path}): {joined}\"\n         )\n \n \n-def _adr_index(root: Path) -> dict[str, list[Path]]:\n-    \"\"\"Map every four-digit ADR number to the record files that claim it.\"\"\"\n-    index: dict[str, list[Path]] = {}\n+def _adr_index(root: Path) -> dict[tuple[str, str], list[Path]]:\n+    \"\"\"Map each decision namespace and number to the files that claim it.\"\"\"\n+    index: dict[tuple[str, str], list[Path]] = {}\n     adr_dir = root / \"docs\" / \"adr\"\n     if adr_dir.is_dir():\n         for path in sorted(adr_dir.glob(\"*.md\")):\n-            match = _ADR_FILENAME.match(path.name)\n+            match = _DECISION_FILENAME.match(path.name)\n             if match:\n-                index.setdefault(match.group(1), []).append(path)\n+                namespace = \"SADR\" if match.group(1) else \"ADR\"\n+                index.setdefault((namespace, match.group(2)), []).append(path)\n     return index\n \n \n def _citing_files(root: Path, include_docs: bool = True) -> list[Path]:\n-    \"\"\"Files scanned for citations: scripts/*.py, and cheaply the markdown.\"\"\"\n+    \"\"\"Current, shipped text surfaces scanned for decision citations.\"\"\"\n     files = sorted((root / \"scripts\").glob(\"*.py\"))\n+    files += sorted((root / \"scripts\").glob(\"*.sh\"))\n     if include_docs:\n         docs = root / \"docs\"\n         if docs.is_dir():\n             files += sorted(docs.rglob(\"*.md\"))\n         files += sorted(root.glob(\"*.md\"))\n-    return files\n+        pyproject = root / \"pyproject.toml\"\n+        if pyproject.is_file():\n+            files.append(pyproject)\n+        for relative in (\".github\", \".codex\"):\n+            surface = root / relative\n+            if surface.is_dir():\n+                files += sorted(\n+                    path\n+                    for path in surface.rglob(\"*\")\n+                    if path.is_file()\n+                    and path.suffix in {\".md\", \".py\", \".sh\", \".toml\", \".yaml\", \".yml\"}\n+                )\n+    return sorted(set(files))\n \n \n def find_adr_reference_violations(\n@@ -94,10 +111,9 @@\n ) -> list[Violation]:\n     \"\"\"Return every ``ADR ####`` citation under ``root`` that fails to resolve.\n \n-    A citation resolves when exactly one ``docs/adr/####-*.md`` matches its\n-    number. Zero matches is a \"missing\" violation; two or more is \"ambiguous\".\n-    Records that are never cited produce nothing -- the converse is not\n-    required.\n+    A citation resolves when exactly one record in the same namespace matches\n+    its number. Zero matches is a \"missing\" violation; two or more is\n+    \"ambiguous\". Records that are never cited produce nothing.\n     \"\"\"\n     root = Path(root)\n     index = _adr_index(root)\n@@ -110,13 +126,15 @@\n         rel = path.relative_to(root).as_posix()\n         for lineno, line in enumerate(text.splitlines(), start=1):\n             for match in _CITATION.finditer(line):\n-                number = match.group(1)\n-                found = index.get(number, [])\n+                namespace, number = match.groups()\n+                found = index.get((namespace, number), [])\n                 if len(found) == 1:\n                     continue\n                 reason = \"missing\" if not found else \"ambiguous\"\n                 rels = [p.relative_to(root).as_posix() for p in found]\n-                violations.append(Violation(rel, number, lineno, reason, rels))\n+                violations.append(\n+                    Violation(rel, namespace, number, lineno, reason, rels)\n+                )\n     return violations\n \n \n@@ -129,8 +147,8 @@\n         self.assertEqual(\n             violations,\n             [],\n-            \"Every 'ADR ####' citation must resolve to exactly one \"\n-            f\"docs/adr/####-*.md:\\n{detail}\",\n+            \"Every ADR/SADR citation must resolve exactly once in its own \"\n+            f\"docs/adr namespace:\\n{detail}\",\n         )\n \n     def test_scan_actually_reads_scripts(self) -> None:\n@@ -146,8 +164,21 @@\n         # The two records this Goal carried onto the default branch, and which\n         # the scripts cite most, must be present.\n         index = _adr_index(REPO_ROOT)\n-        self.assertIn(\"0016\", index)\n-        self.assertIn(\"0017\", index)\n+        self.assertIn((\"SADR\", \"0016\"), index)\n+        self.assertIn((\"SADR\", \"0017\"), index)\n+\n+    def test_platform_tree_has_only_sadr_0001_through_0019(self) -> None:\n+        adr_dir = REPO_ROOT / \"docs\" / \"adr\"\n+        sadrs = sorted(adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\"))\n+        self.assertEqual(\n+            {path.name.split(\"-\", 2)[1] for path in sadrs},\n+            {f\"{number:04d}\" for number in range(1, 20)},\n+        )\n+        self.assertEqual(\n+            list(adr_dir.glob(\"[0-9][0-9][0-9][0-9]-*.md\")),\n+            [],\n+            \"the Shiki platform tree must not retain legacy numeric SADR paths\",\n+        )\n \n \n class FixtureAdrReferences(unittest.TestCase):\n@@ -180,6 +211,26 @@\n         self.assertIn(\"9991\", message)  # cited number\n         self.assertIn(\"docs/adr/9991-*.md\", message)  # missing path\n \n+    def test_target_adr_does_not_satisfy_missing_platform_sadr(self) -> None:\n+        root = self._make_repo(\n+            adr_names=[\"0042-target-decision.md\"],\n+            script_body='\"\"\"Target ADR 0042 exists, but platform SADR-0042 does not.\"\"\"\\n',\n+        )\n+        violations = find_adr_reference_violations(root, include_docs=False)\n+        self.assertEqual(len(violations), 1)\n+        self.assertEqual(violations[0].namespace, \"SADR\")\n+        self.assertEqual(violations[0].number, \"0042\")\n+        self.assertEqual(violations[0].expected_path, \"docs/adr/SADR-0042-*.md\")\n+\n+    def test_target_adr_and_platform_sadr_may_reuse_a_number(self) -> None:\n+        root = self._make_repo(\n+            adr_names=[\"0042-target-decision.md\", \"SADR-0042-platform-decision.md\"],\n+            script_body='\"\"\"Target ADR 0042 and platform SADR-0042 are distinct.\"\"\"\\n',\n+        )\n+        self.assertEqual(\n+            find_adr_reference_violations(root, include_docs=False), []\n+        )\n+\n     def test_uncited_adr_is_allowed(self) -> None:\n         # An ADR exists but nothing cites it -> not a violation. The converse\n         # (every ADR must be cited) is explicitly NOT required.\ndiff --git a/tests/test_guardian_review.py b/tests/test_guardian_review.py\nindex 88829e7810212635e2fc98aa5918d6f26294677d..00a85a237acd5bf2d6cb5b294128198c422402ca\n--- a/tests/test_guardian_review.py\n+++ b/tests/test_guardian_review.py\n@@ -162,7 +162,7 @@\n \n     def test_docs_only_is_light_and_not_conservative(self):\n         focus = gr.classify_review_focus([\"docs/adr/0099-x.md\", \"README.md\"], \"\", \"low\")\n-        self.assertTrue(any(\"Docs/ADR-only\" in f for f in focus))\n+        self.assertTrue(any(\"Docs/decision-record-only\" in f for f in focus))\n         self.assertFalse(any(\"conservative default\" in f.lower() for f in focus))\n \n     def test_unknown_category_is_conservative(self):\ndiff --git a/tests/test_policy_job_merged_prs.py b/tests/test_policy_job_merged_prs.py\nindex d45512e74681e5e649279236c7d4e79d5c3da471..b97989f8bd74d091de1a6b5275a20d8dc68c4512\n--- a/tests/test_policy_job_merged_prs.py\n+++ b/tests/test_policy_job_merged_prs.py\n@@ -3,8 +3,8 @@\n \n ``.github/workflows/shiki-cca-completion.yml`` has two consumers of the merged-PR\n proof: the CCA *signal* (``guardian_approval_signal.py`` in \"Prepare CCA context\")\n-and the authoritative gate (``mergegate_check.py`` in \"Enforce MergeGate\"). ADR\n-0017's bookkeeping-closeout exemption (condition 6) needs the merged implementation\n+and the authoritative gate (``mergegate_check.py`` in \"Enforce MergeGate\").\n+SADR-0017's bookkeeping-closeout exemption (condition 6) needs the merged implementation\n PR proven, and a closeout carries NO ``post_merge_reconcile`` marker. The signal\n already resolves ``MERGED_PRS`` unconditionally, but \"Enforce MergeGate\" used to\n resolve it only inside ``grep -q '<!-- shiki:post_merge_reconcile -->'``. So a\ndiff --git a/tests/test_sadr_install_migration.py b/tests/test_sadr_install_migration.py\nnew file mode 100644\nindex 0000000000000000000000000000000000000000..a6f4c57e4dcaef73ef600b0d4804744138ab9071\n--- /dev/null\n+++ b/tests/test_sadr_install_migration.py\n@@ -0,0 +1,229 @@\n+\"\"\"SADR installation and collision-safe legacy upgrade behavior.\"\"\"\n+\n+from __future__ import annotations\n+\n+import contextlib\n+import hashlib\n+import io\n+import json\n+import shutil\n+import tempfile\n+import unittest\n+from pathlib import Path\n+\n+import shiki_test_support  # noqa: F401  (path bootstrap)\n+\n+import shiki_installer\n+from tests.test_adr_references import find_adr_reference_violations\n+\n+\n+PRODUCT_NUMBERS = tuple(range(13, 19))\n+\n+\n+def _quiet_install(target: Path, *, force: bool, validate: bool) -> None:\n+    with contextlib.redirect_stdout(io.StringIO()):\n+        shiki_installer.install_template(target, force=force, validate=validate)\n+\n+\n+def _sha256(path: Path) -> str:\n+    return hashlib.sha256(path.read_bytes()).hexdigest()\n+\n+\n+def _legacy_sadr_paths() -> tuple[str, ...]:\n+    return tuple(\n+        f\"docs/adr/{path.name.removeprefix('SADR-')}\"\n+        for path in sorted((shiki_installer.ROOT / \"docs\" / \"adr\").glob(\"SADR-*.md\"))\n+        if path.name != \"SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\"\n+    )\n+\n+\n+def _create_legacy_upgrade_fixture(\n+    target: Path,\n+) -> tuple[dict[str, bytes], tuple[str, ...]]:\n+    adr_dir = target / \"docs\" / \"adr\"\n+    adr_dir.mkdir(parents=True)\n+    product_bytes = {\n+        f\"{number:04d}-cubeshare-product-decision.md\": (\n+            f\"# CubeShare ADR {number:04d}\\n\\nDo not rewrite {number}.\\n\"\n+        ).encode()\n+        for number in PRODUCT_NUMBERS\n+    }\n+    for name, content in product_bytes.items():\n+        (adr_dir / name).write_bytes(content)\n+\n+    legacy_paths = _legacy_sadr_paths()\n+    for relative in legacy_paths:\n+        (target / relative).write_bytes(f\"legacy Shiki bytes for {relative}\\n\".encode())\n+\n+    stamp_path = target / shiki_installer.INSTALL_STAMP_PATH\n+    stamp_path.parent.mkdir(parents=True, exist_ok=True)\n+    stamp = {\n+        \"version\": shiki_installer.INSTALL_STAMP_VERSION,\n+        \"platform_commit\": None,\n+        \"installed_at\": \"2000-01-01T00:00:00+00:00\",\n+        \"digests\": {\n+            relative: _sha256(target / relative) for relative in legacy_paths\n+        },\n+    }\n+    stamp_path.write_text(\n+        json.dumps(stamp, indent=2, sort_keys=True) + \"\\n\", encoding=\"utf-8\"\n+    )\n+    return product_bytes, legacy_paths\n+\n+\n+def _tree_snapshot(root: Path) -> dict[str, tuple[str, bytes | str]]:\n+    \"\"\"Recursive byte/shape snapshot used to prove preflight is write-free.\"\"\"\n+    snapshot: dict[str, tuple[str, bytes | str]] = {}\n+    for path in sorted(root.rglob(\"*\")):\n+        relative = path.relative_to(root).as_posix()\n+        if path.is_symlink():\n+            snapshot[relative] = (\"symlink\", path.readlink().as_posix())\n+        elif path.is_dir():\n+            snapshot[relative] = (\"directory\", \"\")\n+        else:\n+            snapshot[relative] = (\"file\", path.read_bytes())\n+    return snapshot\n+\n+\n+class FreshSadrInstallTests(unittest.TestCase):\n+    def setUp(self) -> None:\n+        self.tmp = Path(tempfile.mkdtemp(prefix=\"shiki-sadr-fresh-\"))\n+        self.addCleanup(lambda: shutil.rmtree(self.tmp, ignore_errors=True))\n+        self.target = self.tmp / \"target\"\n+        self.adr_dir = self.target / \"docs\" / \"adr\"\n+        self.adr_dir.mkdir(parents=True)\n+        self.product_bytes = {\n+            f\"{number:04d}-product-decision.md\": (\n+                f\"# Product ADR {number:04d}\\n\\nTarget-owned bytes {number}.\\n\"\n+            ).encode()\n+            for number in PRODUCT_NUMBERS\n+        }\n+        for name, content in self.product_bytes.items():\n+            (self.adr_dir / name).write_bytes(content)\n+\n+    def test_fresh_install_preserves_product_adrs_and_installs_all_sadrs(self) -> None:\n+        _quiet_install(self.target, force=False, validate=True)\n+\n+        for name, expected in self.product_bytes.items():\n+            self.assertEqual((self.adr_dir / name).read_bytes(), expected)\n+\n+        sadr_numbers = {\n+            path.name.split(\"-\", 2)[1]\n+            for path in self.adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\")\n+        }\n+        self.assertEqual(sadr_numbers, {f\"{number:04d}\" for number in range(1, 20)})\n+\n+        numeric_records = {\n+            path.name\n+            for path in self.adr_dir.glob(\"[0-9][0-9][0-9][0-9]-*.md\")\n+        }\n+        self.assertEqual(numeric_records, set(self.product_bytes))\n+        self.assertEqual(find_adr_reference_violations(self.target), [])\n+\n+\n+class LegacySadrUpgradeTests(unittest.TestCase):\n+    def setUp(self) -> None:\n+        self.tmp = Path(tempfile.mkdtemp(prefix=\"shiki-sadr-upgrade-\"))\n+        self.addCleanup(lambda: shutil.rmtree(self.tmp, ignore_errors=True))\n+        self.target = self.tmp / \"target\"\n+        self.adr_dir = self.target / \"docs\" / \"adr\"\n+        self.product_bytes, self.legacy_paths = _create_legacy_upgrade_fixture(\n+            self.target\n+        )\n+\n+    def test_force_upgrade_deletes_only_stamped_legacy_sadrs(self) -> None:\n+        _quiet_install(self.target, force=True, validate=True)\n+\n+        for relative in self.legacy_paths:\n+            self.assertFalse((self.target / relative).exists(), relative)\n+        for name, expected in self.product_bytes.items():\n+            self.assertEqual((self.adr_dir / name).read_bytes(), expected)\n+\n+        sadrs = list(self.adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\"))\n+        self.assertEqual(len(sadrs), 19)\n+        refreshed = json.loads(\n+            (self.target / shiki_installer.INSTALL_STAMP_PATH).read_text(encoding=\"utf-8\")\n+        )\n+        for relative in self.legacy_paths:\n+            self.assertNotIn(relative, refreshed[\"digests\"])\n+        for path in sadrs:\n+            relative = path.relative_to(self.target).as_posix()\n+            self.assertEqual(refreshed[\"digests\"][relative], _sha256(path))\n+\n+    def test_non_force_install_never_deletes_legacy_files(self) -> None:\n+        before = {\n+            relative: (self.target / relative).read_bytes()\n+            for relative in self.legacy_paths\n+        }\n+        _quiet_install(self.target, force=False, validate=False)\n+        for relative, expected in before.items():\n+            self.assertEqual((self.target / relative).read_bytes(), expected)\n+        refreshed = json.loads(\n+            (self.target / shiki_installer.INSTALL_STAMP_PATH).read_text(encoding=\"utf-8\")\n+        )\n+        for relative in self.legacy_paths:\n+            self.assertEqual(refreshed[\"digests\"][relative], _sha256(self.target / relative))\n+\n+\n+class LegacyCleanupPreflightTests(unittest.TestCase):\n+    def setUp(self) -> None:\n+        self.tmp = Path(tempfile.mkdtemp(prefix=\"shiki-sadr-preflight-\"))\n+        self.addCleanup(lambda: shutil.rmtree(self.tmp, ignore_errors=True))\n+\n+    def test_all_ownership_blockers_abort_before_any_write(self) -> None:\n+        cases = (\n+            (\"absent\", \"install stamp is absent\"),\n+            (\"unreadable\", \"install stamp is unreadable\"),\n+            (\"malformed\", \"install stamp is malformed\"),\n+            (\"missing-digest\", \"install stamp has no digest\"),\n+            (\"digest-mismatch\", \"digest mismatch\"),\n+        )\n+        for name, expected_reason in cases:\n+            with self.subTest(name=name):\n+                target = self.tmp / name\n+                _, legacy_paths = _create_legacy_upgrade_fixture(target)\n+                stamp_path = target / shiki_installer.INSTALL_STAMP_PATH\n+\n+                if name == \"absent\":\n+                    stamp_path.unlink()\n+                elif name == \"unreadable\":\n+                    stamp_path.unlink()\n+                    stamp_path.mkdir()\n+                elif name == \"malformed\":\n+                    stamp_path.write_text(\"{not-json\\n\", encoding=\"utf-8\")\n+                else:\n+                    stamp = json.loads(stamp_path.read_text(encoding=\"utf-8\"))\n+                    if name == \"missing-digest\":\n+                        stamp[\"digests\"] = {}\n+                    else:\n+                        stamp[\"digests\"] = {\n+                            relative: \"deadbeef\" for relative in legacy_paths\n+                        }\n+                    stamp_path.write_text(\n+                        json.dumps(stamp, indent=2, sort_keys=True) + \"\\n\",\n+                        encoding=\"utf-8\",\n+                    )\n+\n+                before = _tree_snapshot(target)\n+                with self.assertRaises(shiki_installer.ShikiError) as raised:\n+                    _quiet_install(target, force=True, validate=False)\n+                after = _tree_snapshot(target)\n+\n+                self.assertEqual(before, after, f\"{name} preflight changed the tree\")\n+                message = str(raised.exception)\n+                self.assertIn(expected_reason, message)\n+                self.assertIn(\"No files were written\", message)\n+                for relative in legacy_paths:\n+                    self.assertIn(relative, message)\n+\n+    def test_absent_stamp_is_allowed_when_no_legacy_path_exists(self) -> None:\n+        target = self.tmp / \"no-legacy\"\n+        target.mkdir()\n+        _quiet_install(target, force=True, validate=False)\n+        self.assertTrue(\n+            (target / \"docs\" / \"adr\" / \"SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\").is_file()\n+        )\n+\n+\n+if __name__ == \"__main__\":\n+    unittest.main()\n\n2026-08-07T08:19:07.253487Z ERROR codex_models_manager::manager: failed to renew cache TTL: missing field `base_instructions` at line 94 column 5\nexec\n/bin/zsh -lc \"rg -n 'def .*ledger|ledger.*add|append_ledger|record_ledger|ledger entry|write_ledger' scripts/shiki_*.py | sed -n '1,260p'\" in /private/tmp/shiki-adr-collision.pa0KwY/.worktrees/shiki-t-20260807t073307221254z-2d22d654-introduc\n succeeded in 0ms:\nscripts/shiki_evidence.py:191:def _canonical_ledger_payload(entry: dict[str, Any]) -> dict[str, Any]:\nscripts/shiki_evidence.py:204:def ledger_entry_digest(entry: dict[str, Any]) -> str:\nscripts/shiki_evidence.py:210:def evidence_reference_for_ledger(\nscripts/shiki_evidence.py:277:def validate_ledger_integrity(entry: dict[str, Any]) -> list[str]:\nscripts/shiki_bootstrap.py:20:from shiki_tasks import append_ledger, load_task, next_control_id, orchestrate_plan, require_github_first_target, shiki_path as _unused_shiki_path, write_handoff\nscripts/shiki_bootstrap.py:522:    ledger_id = append_ledger(\nscripts/shiki_memory.py:190:def _ledger_ref_errors(value: Any, *, label: str, root: Path | None) -> list[str]:\nscripts/shiki_memory.py:344:from shiki_tasks import append_ledger, require_github_first_target  # noqa: E402\nscripts/shiki_memory.py:499:    writes a tree-invalidating ledger entry.\"\"\"\nscripts/shiki_memory.py:510:def _record_transition(target: Path, memory: dict[str, Any], summary: str, *, ledger_type: str = MEMORY_LEDGER_TYPE) -> str:\nscripts/shiki_memory.py:512:    return append_ledger(\nscripts/shiki_loop.py:33:    append_ledger,\nscripts/shiki_loop.py:849:    ledger_id = append_ledger(\nscripts/shiki_loop.py:1251:    # tasks/ledger files already rely on the same coverage). `add` no-ops on a\nscripts/shiki_loop.py:1305:def _sync_state_to_branch(target: Path, task_id: str, ledger_id: str | None) -> str:\nscripts/shiki_loop.py:1417:    return append_ledger(\nscripts/shiki_loop.py:1552:        ledger_id = append_ledger(\nscripts/shiki_loop.py:1568:    ledger_id = append_ledger(\nscripts/shiki_loop.py:1624:        from shiki_tasks import append_ledger, next_control_id\nscripts/shiki_loop.py:1664:        ledger_id = append_ledger(\nscripts/shiki_loop.py:1840:        pull_ledger = append_ledger(\nscripts/shiki_loop.py:1974:            sync_ledger = append_ledger(\nscripts/shiki_loop.py:2104:    def _collect_ledgers(entity: dict[str, Any] | None) -> None:\nscripts/shiki_loop.py:2367:        ledger_id = append_ledger(\nscripts/shiki_loop.py:2517:        ledger_id = append_ledger(\nscripts/shiki_github.py:20:from shiki_tasks import append_ledger, load_task, require_github_first_target, worktree_record\nscripts/shiki_github.py:807:    ledger_id = append_ledger(\nscripts/shiki_github.py:852:    ledger_id = append_ledger(\nscripts/shiki_state.py:71:def append_ledger_entry(\nscripts/shiki_state.py:77:    \"\"\"Append a ledger entry using no-overwrite creation and collision retry.\"\"\"\nscripts/shiki_runtime.py:32:from shiki_tasks import append_ledger, evaluate_lock_grant, load_task, orchestrate_plan, require_github_first_target, require_grilled_plan, next_control_id, task_files, worktree_record\nscripts/shiki_runtime.py:294:    ledger_id = append_ledger(\nscripts/shiki_runtime.py:403:        ledger_id = append_ledger(\nscripts/shiki_runtime.py:442:    ledger_id = append_ledger(\nscripts/shiki_tasks.py:14:from shiki_state import append_ledger_entry, new_control_id\nscripts/shiki_tasks.py:248:def append_ledger(\nscripts/shiki_tasks.py:259:        return append_ledger_entry(\nscripts/shiki_tasks.py:300:    ledger_id = append_ledger(\nscripts/shiki_tasks.py:321:    ledger_id = append_ledger(\nscripts/shiki_tasks.py:450:    ledger_id = append_ledger(\nscripts/shiki_tasks.py:565:    ledger_id = append_ledger(\nscripts/shiki_tasks.py:597:    ledger_id = append_ledger(\nscripts/shiki_tasks.py:677:    ledger_id = append_ledger(\nscripts/shiki_tasks.py:784:    ledger_id = append_ledger(\nscripts/shiki_tasks.py:936:    ledger_id = append_ledger(\nscripts/shiki_tasks.py:1016:    ledger_id = append_ledger(\nscripts/shiki_tasks.py:1121:    ledger_id = append_ledger(\nscripts/shiki_tasks.py:1245:    ledger_id = append_ledger(\nscripts/shiki_tasks.py:1305:    ledger_id = append_ledger(\nscripts/shiki_tasks.py:1366:    ledger_id = append_ledger(\nscripts/shiki_tasks.py:1423:    ledger_id = append_ledger(\nscripts/shiki_tasks.py:1504:    ledger_id = append_ledger(\nscripts/shiki_tasks.py:1597:    ledger_id = append_ledger(\n\ndiff --git a/.codex/skills/shiki/SKILL.md b/.codex/skills/shiki/SKILL.md\nindex c8e2e85d9d75d49cc4f2d6286ee22659c4e53e75..93cded623b2c15d9f65c76491a1c2d813ac9e6c0\n--- a/.codex/skills/shiki/SKILL.md\n+++ b/.codex/skills/shiki/SKILL.md\n@@ -18,6 +18,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n Codex CLI does not register this skill as a custom `/shiki` slash command. When\n the user types `/shiki` in Codex CLI and gets \"Unrecognized command\", explain\n that this is expected: invoke Shiki in Codex with natural language such as\n@@ -69,7 +72,7 @@\n \n ## Responsibilities\n \n-- Claude Code implements and repairs by default (`shiki runner claude`, ADR 0008); Codex implements and repairs tasks explicitly assigned to `codex`.\n+- Claude Code implements and repairs by default (`shiki runner claude`, SADR-0008); Codex implements and repairs tasks explicitly assigned to `codex`.\n - Claude Code Action can act as GitHub-side CCA or reviewer.\n - CCA judges completion.\n - MergeGate authorizes state transitions and merge readiness.\ndiff --git a/.github/prompts/cca-completion-check.md b/.github/prompts/cca-completion-check.md\nindex ed203ad5f302ecc81372d8ab27d2b1232ca97497..fd89950528333ef6242e6d5443904f5392ab7e56\n--- a/.github/prompts/cca-completion-check.md\n+++ b/.github/prompts/cca-completion-check.md\n@@ -19,6 +19,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n ## Required Reading\n \n Read only the files needed to judge the PR contract. Prefer this order:\ndiff --git a/.github/prompts/codex-task-handoff.md b/.github/prompts/codex-task-handoff.md\nindex 695e211d76dea1c60e8d5bb360e78487808286a4..59441d576b01a39569722f226c854e5a740e9eb5\n--- a/.github/prompts/codex-task-handoff.md\n+++ b/.github/prompts/codex-task-handoff.md\n@@ -2,7 +2,7 @@\n \n You are Codex Front acting as the Implementer for a Shiki task.\n \n-Follow `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant ADRs, and `docs/agents/codex-handoff.md`.\n+Follow `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant Shiki SADRs and target ADRs, and `docs/agents/codex-handoff.md`.\n \n Rules:\n \ndiff --git a/AGENTS.md b/AGENTS.md\nindex 7f041100d49c2a7525f51cb0f5d696a4a6d1c78c..a06bfbabc6fa48a007389e25e12d7ed523936bed\n--- a/AGENTS.md\n+++ b/AGENTS.md\n@@ -22,7 +22,7 @@\n - **Codex Front** is an optional implementer through Codex App, Codex CLI, Codex IDE extension, or Codex Web signed in with ChatGPT OAuth/subscription auth, dispatched with `shiki runner codex` when a task is explicitly assigned to `codex`.\n - **GitHub CCA** is implemented by Claude Code Action by default, using `CLAUDE_CODE_OAUTH_TOKEN`.\n \n-Do not assume `openai/codex-action`, `OPENAI_API_KEY`, or API-key based Codex automation in the default Shiki path. API-key runners are explicit target-repo extensions and require their own ADR.\n+Do not assume `openai/codex-action`, `OPENAI_API_KEY`, or API-key based Codex automation in the default Shiki path. API-key runners are explicit target-repo extensions and require their own target ADR.\n \n Shiki is not:\n \n@@ -44,10 +44,14 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use\n+`SADR-NNNN-*.md`; target product decisions retain `NNNN-*.md` and the `ADR`\n+namespace.\n+\n Use the canonical source-of-truth order above. Runtime-specific reading orders\n are operational guidance only and must not override or reorder it.\n \n-Conversation state is not durable truth. If a decision matters, put it in GitHub, `.shiki/`, `CONTEXT.md`, or an ADR.\n+Conversation state is not durable truth. If a decision matters, put it in GitHub, `.shiki/`, `CONTEXT.md`, a Shiki SADR, or a target ADR.\n \n If GitHub and `.shiki/` disagree, surface the conflict and prefer GitHub operational state until the mirror is repaired.\n \n@@ -75,8 +79,8 @@\n Every non-trivial change follows this loop:\n \n 1. **Goal**: clarify outcome, completion conditions, non-goals, risk level, and success signals.\n-2. **grill-with-docs**: challenge the plan against domain docs, ADRs, code reality, terminology, and edge scenarios.\n-3. **Context & Impact**: find relevant docs, ADRs, code, dependencies, owners, locks, and verification surfaces.\n+2. **grill-with-docs**: challenge the plan against domain docs, Shiki SADRs, target ADRs, code reality, terminology, and edge scenarios.\n+3. **Context & Impact**: find relevant docs, SADRs, target ADRs, code, dependencies, owners, locks, and verification surfaces.\n 4. **PRD and Spec Freeze**: use `to-prd` when context is settled enough to become durable product/engineering intent. The operator's explicit approval of the PRD is Spec Freeze: the plan records a `spec_freeze` block, and no plan runs without it. Steps 1-4 form Requirements Definition \u2014 one continuous operator dialogue. After Spec Freeze, scope changes require an operator-approved Spec Amendment; non-scope-moving interpretations go to the Assumption Log.\n 5. **Issues**: use `to-issues` to create independently grabbable vertical-slice issues. Prefer AFK slices over HITL slices where possible.\n 6. **Triage**: label issues for readiness, risk, runtime, skills, and MergeGate state.\n@@ -167,7 +171,7 @@\n - Testing decisions.\n - Out-of-scope items.\n - Further notes.\n-- Links to relevant domain terms and ADRs.\n+- Links to relevant domain terms, SADRs, and target ADRs.\n \n Every executable task must include:\n \n@@ -193,7 +197,7 @@\n \n Context & Impact must identify:\n \n-- Relevant documents, domain terms, ADRs, and past decisions.\n+- Relevant documents, domain terms, SADRs, target ADRs, and past decisions.\n - Relevant modules, interfaces, seams, callers, owners, and tests.\n - Dependency relationships between tasks.\n - Candidate locks and lock conflicts.\n@@ -210,7 +214,7 @@\n | Skill | Required when |\n | --- | --- |\n | `setup-matt-pocock-skills` | First configuring the repo for skills, or when issue tracker, triage labels, or domain docs are missing. |\n-| `grill-with-docs` | Default for non-trivial Goals before PRD/issues; always required when plans, terminology, boundaries, tradeoffs, or ADR-worthy decisions are ambiguous. |\n+| `grill-with-docs` | Default for non-trivial Goals before PRD/issues; always required when plans, terminology, boundaries, tradeoffs, or decision-record-worthy choices are ambiguous. |\n | `zoom-out` | The agent lacks an architectural map of the relevant code area. |\n | `to-prd` | Settled Goal context must become durable product/engineering intent. |\n | `to-issues` | A Goal, PRD, or plan must become independently grabbable vertical-slice issues. |\n@@ -348,7 +352,7 @@\n - Three or more new files.\n - Any high-risk or critical label.\n \n-When the architecture gate triggers, run `grill-with-docs` or `improve-codebase-architecture` as appropriate and record durable decisions in an ADR when needed.\n+When the architecture gate triggers, run `grill-with-docs` or `improve-codebase-architecture` as appropriate and record durable decisions in a Shiki SADR or target ADR as applicable.\n \n ## Engineering Discipline\n \ndiff --git a/CLAUDE.md b/CLAUDE.md\nindex 98a932e7841a632866fcd53b0bad0e3af3711d3d..98a31399404d6fb044b4fd493f4459ac87f439de\n--- a/CLAUDE.md\n+++ b/CLAUDE.md\n@@ -14,6 +14,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n ## Claude Role\n \n Default to planner, default implementer, reviewer, coordinator, documentation editor, CCA-style completion judge, and final-judgment assistant.\n@@ -57,7 +60,7 @@\n At the start of a Shiki session:\n \n 1. Read `AGENTS.md`.\n-2. Read `CONTEXT.md` and relevant ADRs.\n+2. Read `CONTEXT.md` and relevant Shiki SADRs and target ADRs.\n 3. Read active `.shiki/` Goal/task/ledger state when present.\n 4. Read active distilled rules from `.shiki/memories/` (status `distilled`, `active: true`, not revoked or superseded) for applicable guidance; the same rules are injected deterministically into task handoffs as the `## Distilled Rules` section (\u00a73.5).\n 5. Read the GitHub Issue or PR when the task is GitHub-backed.\n@@ -74,7 +77,7 @@\n - Define the Goal, completion conditions, non-goals, and risk level.\n - Treat `grill-with-docs` as the default planning entry point for non-trivial Goals.\n - Challenge domain terms against `CONTEXT.md`.\n-- Challenge hard-to-reverse tradeoffs against ADRs.\n+- Challenge hard-to-reverse tradeoffs against SADRs and target ADRs.\n - Explore code instead of asking when code can answer the question.\n - Run Context & Impact before implementation planning.\n - Produce a Task DAG, not an unordered checklist.\n@@ -94,7 +97,7 @@\n - For each question, provide your recommended answer.\n - Resolve dependency decisions before downstream design questions.\n - Update `CONTEXT.md` inline when terminology crystallizes.\n-- Offer ADRs only when the decision is hard to reverse, surprising without context, and a real tradeoff.\n+- Offer a SADR or target ADR only when the decision is hard to reverse, surprising without context, and a real tradeoff.\n - Convert unresolved questions into HITL blockers rather than allowing Codex to guess.\n \n ## PRD and Issues Standard\n@@ -103,7 +106,7 @@\n \n - Use the project's domain glossary vocabulary.\n - Avoid volatile file paths unless they are essential.\n-- Include implementation decisions, testing decisions, out-of-scope items, and relevant ADR/domain links.\n+- Include implementation decisions, testing decisions, out-of-scope items, and relevant SADR/target-ADR/domain links.\n - Identify deep-module opportunities and testable interfaces.\n \n When creating issues:\n@@ -121,7 +124,7 @@\n - Branch/worktree target.\n - Scope and non-goals.\n - Dependencies and locks.\n-- Relevant docs, ADRs, modules, and tests.\n+- Relevant docs, SADRs, target ADRs, modules, and tests.\n - Required skill invocations.\n - TDD expectations.\n - Pre-PR `code-review` gate expectations.\n@@ -152,7 +155,7 @@\n \n - Treat yourself as a judge, not implementer.\n - Do not write production code.\n-- Read `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant ADRs, PRD, task issue, PR body, diff, checks, reviews, labels, and `.shiki/` state.\n+- Read `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant SADRs and target ADRs, PRD, task issue, PR body, diff, checks, reviews, labels, and `.shiki/` state.\n - Evaluate every applicable checklist in `docs/agents/checklists.md`.\n - Map each acceptance criterion to durable evidence.\n - Distinguish implementation failure from missing evidence.\ndiff --git a/CONTEXT.md b/CONTEXT.md\nindex e61a9be06a0cce0d026484083a657bde2067e234..237ee8aa3ea8fab334ece49768bc5ed3b5af0974\n--- a/CONTEXT.md\n+++ b/CONTEXT.md\n@@ -25,7 +25,7 @@\n _Avoid_: jumping directly into code\n \n **grill-with-docs**:\n-A planning skill that challenges a plan against domain language, ADRs, code reality, and concrete edge scenarios. It resolves design-tree questions before PRD/issues. It runs inside Requirements Definition.\n+A planning skill that challenges a plan against domain language, Shiki SADRs, target ADRs, code reality, and concrete edge scenarios. It resolves design-tree questions before PRD/issues. It runs inside Requirements Definition.\n _Avoid_: generic brainstorming, silent assumptions\n \n **Requirements Definition**:\ndiff --git a/CONTRIBUTING.md b/CONTRIBUTING.md\nindex c6d9b656e73abb8a55e5f660123e03153dcb3e6d..9e37a8fbf668db5b22461aaae5548b8f1354394c\n--- a/CONTRIBUTING.md\n+++ b/CONTRIBUTING.md\n@@ -17,7 +17,7 @@\n 1. **Goal** \u2014 start from a user-approved Goal with an outcome, completion\n    conditions, non-goals, risk level, and success signals.\n 2. **grill-with-docs** \u2014 for any non-trivial change, challenge the plan against\n-   domain language, ADRs, and code reality before writing a PRD.\n+   domain language, Shiki SADRs, target ADRs, and code reality before writing a PRD.\n 3. **PRD** \u2014 capture settled product/engineering intent (`to-prd`).\n 4. **Spec Freeze** \u2014 the operator explicitly approves the PRD/requirements;\n    the plan records a `spec_freeze` block and no plan runs without it\n@@ -94,7 +94,7 @@\n   entries.\n - Do not hand-edit `.shiki/goals/*.json` or `.shiki/manifest.json` in a task PR.\n - Conversation state is not durable truth. If a decision matters, record it in\n-  GitHub, `.shiki/`, `CONTEXT.md`, or an ADR.\n+  GitHub, `.shiki/`, `CONTEXT.md`, a Shiki SADR, or a target ADR.\n \n ## Architecture Gate\n \n@@ -103,7 +103,7 @@\n or CI policy, package structure, three or more structural files, more than 100\n net new lines in a core module, three or more new files, or any high-risk label.\n When triggered, run `grill-with-docs` or `improve-codebase-architecture` and\n-record durable decisions in an ADR when needed (see `docs/adr/`).\n+record durable decisions in a Shiki SADR or target ADR as applicable (see `docs/adr/`).\n \n ## Safety\n \ndiff --git a/SYSTEM_PROMPT.md b/SYSTEM_PROMPT.md\nindex aaaea45e3a896c3a44ba89eec8bb84325ecf6c62..da009c86a88285269dee0155e58e550c6d744ea9\n--- a/SYSTEM_PROMPT.md\n+++ b/SYSTEM_PROMPT.md\n@@ -30,6 +30,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n Use the canonical source-of-truth order above. Runtime-specific reading orders\n are operational guidance only and must not override or reorder it. The current\n conversation is non-durable operator input.\n@@ -48,14 +51,14 @@\n \n A runtime may only do work that is explicitly assigned by the Goal, task, PR, or MergeGate decision.\n \n-Do not assume `openai/codex-action`, `OPENAI_API_KEY`, or API-key based Codex automation in the default Shiki path. API-key runners are explicit target-repo extensions and require their own ADR.\n+Do not assume `openai/codex-action`, `OPENAI_API_KEY`, or API-key based Codex automation in the default Shiki path. API-key runners are explicit target-repo extensions and require their own target ADR.\n \n ## 4. Non-negotiable Shiki flow\n \n For any non-trivial Goal, use this flow:\n \n 1. **Goal Seek**: define outcome, non-goals, risk level, completion criteria, and evidence requirements.\n-2. **grill-with-docs**: challenge the plan against `CONTEXT.md`, ADRs, code reality, terminology, boundaries, and edge scenarios. Ask one question at a time when the operator is available. If code can answer a question, inspect code instead of asking.\n+2. **grill-with-docs**: challenge the plan against `CONTEXT.md`, Shiki SADRs, target ADRs, code reality, terminology, boundaries, and edge scenarios. Ask one question at a time when the operator is available. If code can answer a question, inspect code instead of asking.\n 3. **Context & Impact**: identify modules, interfaces, seams, callers, dependencies, locks, risk, and verification surfaces.\n 4. **to-prd + Spec Freeze**: turn settled context into a PRD using domain vocabulary and testing decisions. The operator's explicit approval of the PRD is Spec Freeze, recorded as the plan's `spec_freeze` block; plans without it do not run. Steps 1-4 form Requirements Definition.\n 5. **to-issues**: decompose the PRD into independently grabbable vertical-slice issues. Prefer AFK slices; mark HITL slices when judgment is still required.\n@@ -75,7 +78,7 @@\n Before making any material change:\n \n 1. Read `AGENTS.md` and any runtime-specific wrapper such as `CLAUDE.md`.\n-2. Read `CONTEXT.md` and relevant ADRs.\n+2. Read `CONTEXT.md` and relevant SADRs and target ADRs.\n 3. Read the GitHub Issue / PR and `.shiki/` state for the active Goal or task.\n 4. Identify your role: Planner, Implementer, Reviewer, Completion Check Agent, Repairer, or Guardian-assist.\n 5. Identify Goal id, task id, branch, PR, dependencies, locks, risk level, required skills, and required evidence.\n@@ -103,7 +106,7 @@\n \n At minimum, identify:\n \n-- Relevant documents, ADRs, domain terms, and prior decisions.\n+- Relevant documents, SADRs, target ADRs, domain terms, and prior decisions.\n - Relevant modules, interfaces, seams, callers, owners, and tests.\n - Dependency relationships between tasks.\n - Candidate file locks and lock conflicts.\n@@ -152,7 +155,7 @@\n \n CCA must read:\n \n-- `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant ADRs.\n+- `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant SADRs and target ADRs.\n - Parent Goal issue, PRD issue, task issue, comments, and labels.\n - PR body, diff, commits, changed files, reviews, CI checks, and artifacts available to the workflow.\n - `.shiki/` task contract, locks, ledger entries, CCA checklist profile, and prior repair packets.\n@@ -214,7 +217,7 @@\n - Three or more new files.\n - Any high-risk or critical label.\n \n-When the architecture gate triggers, run `grill-with-docs` or `improve-codebase-architecture` as appropriate and record the decision in an ADR if it is hard to reverse or likely to be re-litigated.\n+When the architecture gate triggers, run `grill-with-docs` or `improve-codebase-architecture` as appropriate and record the decision in a Shiki SADR or target ADR as applicable if it is hard to reverse or likely to be re-litigated.\n \n ## 14. Skill Gate\n \n@@ -223,7 +226,7 @@\n Use these skills:\n \n - `setup-matt-pocock-skills`: before first use of the engineering skill set, or when issue tracker, triage labels, or domain docs are missing.\n-- `grill-with-docs`: default for non-trivial Goals before PRD/issues; required for ambiguous plans, terminology, boundaries, ADR-worthy tradeoffs, or design-tree decisions.\n+- `grill-with-docs`: default for non-trivial Goals before PRD/issues; required for ambiguous plans, terminology, boundaries, decision-record-worthy tradeoffs, or design-tree decisions.\n - `zoom-out`: unfamiliar code area or missing architectural map.\n - `to-prd`: convert settled context into a PRD.\n - `to-issues`: break a PRD, plan, or Goal into independently grabbable vertical-slice issues.\ndiff --git a/docs/adr/0001-keep-shiki-as-platform-name.md b/docs/adr/SADR-0001-keep-shiki-as-platform-name.md\nindex 20a9637c3b9d8c97de7ba8db7cfae4d82d9a7dba..8b685439959aada1b221c112f0ac3c51ca8a1695\n--- a/docs/adr/0001-keep-shiki-as-platform-name.md\n+++ b/docs/adr/SADR-0001-keep-shiki-as-platform-name.md\n@@ -1,3 +1,3 @@\n-# Keep Shiki As The Platform Name\n+# SADR-0001: Keep Shiki As The Platform Name\n \n Shiki remains the product name for the GitHub-native agentic engineering control plane. We considered more descriptive names such as MergePilot, MergeGate, and Kiroku, but kept Shiki because it carries the existing goal-seeking framework identity while remaining independent of Hermes, Codex, Claude Code, or any single runtime.\ndiff --git a/docs/adr/0002-use-agents-as-shared-constitution.md b/docs/adr/SADR-0002-use-agents-as-shared-constitution.md\nindex 73de30ecb078120eac540d9bc51855e3cd48d0e3..c5486182bf06fca36ea508496b013c67302fd989\n--- a/docs/adr/0002-use-agents-as-shared-constitution.md\n+++ b/docs/adr/SADR-0002-use-agents-as-shared-constitution.md\n@@ -1,3 +1,3 @@\n-# Use AGENTS As The Shared Agent Constitution\n+# SADR-0002: Use AGENTS As The Shared Agent Constitution\n \n Shiki uses `AGENTS.md` as the runtime-neutral constitution and `CLAUDE.md` as a thin Claude Code wrapper. This keeps Codex, Claude Code, Hermes Runner, and future Agent Runtimes aligned on the same GitHub-first MergeGate rules while still allowing runtime-specific instructions where needed.\ndiff --git a/docs/adr/0003-require-skill-gate-for-mergegate-readiness.md b/docs/adr/SADR-0003-require-skill-gate-for-mergegate-readiness.md\nindex 424be0c9af4c8f0136f1fde68a468c47ff84e764..7fc337987c7df9792f75f181dfcbed75fe5ffff2\n--- a/docs/adr/0003-require-skill-gate-for-mergegate-readiness.md\n+++ b/docs/adr/SADR-0003-require-skill-gate-for-mergegate-readiness.md\n@@ -1,3 +1,3 @@\n-# Require Skill Gate For MergeGate Readiness\n+# SADR-0003: Require Skill Gate For MergeGate Readiness\n \n Shiki requires relevant engineering skills to be invoked before execution when their triggers apply. This makes skills part of MergeGate readiness rather than optional prompt style, because Shiki depends on repeatable planning, issue decomposition, TDD, diagnosis, triage, and architecture review before autonomous or parallel agent work can be trusted.\ndiff --git a/docs/adr/0004-use-github-first-with-shiki-mirror.md b/docs/adr/SADR-0004-use-github-first-with-shiki-mirror.md\nindex d0a3a9174dc646a945a9d325954ffaf00c352040..a79c54ae9ee8eed2a0524192b864da4e22a92d3f\n--- a/docs/adr/0004-use-github-first-with-shiki-mirror.md\n+++ b/docs/adr/SADR-0004-use-github-first-with-shiki-mirror.md\n@@ -1,4 +1,4 @@\n-# ADR 0004: Use GitHub First With A Repository-Local Shiki Mirror\n+# SADR-0004: Use GitHub First With A Repository-Local Shiki Mirror\n \n ## Status\n \ndiff --git a/docs/adr/0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md b/docs/adr/SADR-0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\nindex b339baf49b89d9290e79c3d51b36e7b2bde3bd95..0f3c3eb9afccdb3403ce978e21d04fe12e495da0\n--- a/docs/adr/0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\n+++ b/docs/adr/SADR-0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\n@@ -1,8 +1,8 @@\n-# ADR 0005: Use Codex As Front And Claude Code Action As The GitHub Runtime\n+# SADR-0005: Use Codex As Front And Claude Code Action As The GitHub Runtime\n \n ## Status\n \n-Accepted (implementer default superseded by ADR 0008; the auth model and the\n+Accepted (implementer default superseded by SADR-0008; the auth model and the\n Claude Code Action GitHub runtime decision remain in force)\n \n ## Context\n@@ -26,4 +26,4 @@\n - Shiki avoids requiring `OPENAI_API_KEY` for the default workflow.\n - Codex work happens through the user's authenticated Codex front surface, not as a hidden backend Action.\n - Claude Code Action is configured for OAuth-token based subscription usage by default.\n-- API-key based automation remains possible only as an explicit target-repo extension with its own ADR.\n+- API-key based automation remains possible only as an explicit target-repo extension with its own target ADR.\ndiff --git a/docs/adr/0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md b/docs/adr/SADR-0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\nindex 4bcae9d1e52f0a37b8e9fbafa5cd14b6ee1993bd..7a38538a6ae9929567f0ade67e7ea6a60d0b3f7e\n--- a/docs/adr/0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\n+++ b/docs/adr/SADR-0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\n@@ -1,4 +1,4 @@\n-# ADR 0006: Separate Codex Implementation, CCA Completion, And MergeGate Authorization\n+# SADR-0006: Separate Codex Implementation, CCA Completion, And MergeGate Authorization\n \n ## Status\n \ndiff --git a/docs/adr/0007-packaging-and-release.md b/docs/adr/SADR-0007-packaging-and-release.md\nindex 79b84ddb97167372b44641e5b7860e400ce80822..bb445a2e2e856d9d1f5fdbe35de8d4e8cec0fb32\n--- a/docs/adr/0007-packaging-and-release.md\n+++ b/docs/adr/SADR-0007-packaging-and-release.md\n@@ -1,4 +1,4 @@\n-# ADR 0007: Packaging And Release For Shiki\n+# SADR-0007: Packaging And Release For Shiki\n \n ## Status\n \n@@ -25,7 +25,7 @@\n   not introduce third-party runtime dependencies or break the shim.\n - Releases must run inside the operator's subscription-authenticated toolchain\n   and must not require extra secrets beyond the built-in `GITHUB_TOKEN`\n-  (consistent with ADR 0005's no-API-key-by-default stance).\n+  (consistent with SADR-0005's no-API-key-by-default stance).\n - `.shiki/manifest.json` already encodes the install surface (`install.include`,\n   `install.create_directories`, `install.exclude_from_commit`), and it must not\n   be mutated as part of this change.\n@@ -34,7 +34,7 @@\n \n This is a hard-to-reverse platform decision (it sets the version source of\n truth, the release trigger, and the public install contract), so it is recorded\n-as an ADR.\n+as a SADR.\n \n ## Decision\n \n@@ -104,7 +104,7 @@\n 3. **Publishing to PyPI.** Rejected for now: Shiki is distributed as a\n    repository template + git-installable CLI, not a PyPI library; PyPI would add\n    credentials/secrets and a different versioning contract. Can be added later as\n-   its own ADR if a packaged distribution is needed.\n+   its own SADR if a packaged distribution is needed.\n 4. **Dynamic version (read `VERSION` via a build backend `dynamic` field).**\n    Rejected to keep `pyproject.toml` backend-agnostic and dependency-free; the\n    small duplication between `VERSION` and `pyproject.toml` is guarded by the\n@@ -123,4 +123,4 @@\n   releasing versions the whole platform but ships only `install.include` to\n   targets.\n - Future PyPI distribution or a console entry point are deferred and would each\n-  warrant their own ADR.\n+  warrant their own SADR.\ndiff --git a/docs/adr/0008-use-claude-code-as-default-implementer.md b/docs/adr/SADR-0008-use-claude-code-as-default-implementer.md\nindex dedf4510b7c7799521422f622029563d373db031..35c2625d908167fbc58855b5b63e283805f167e8\n--- a/docs/adr/0008-use-claude-code-as-default-implementer.md\n+++ b/docs/adr/SADR-0008-use-claude-code-as-default-implementer.md\n@@ -1,12 +1,12 @@\n-# ADR 0008: Use Claude Code As The Default Implementer Runtime\n+# SADR-0008: Use Claude Code As The Default Implementer Runtime\n \n ## Status\n \n-Proposed (supersedes ADR 0005 for the implementer default; keeps its auth model)\n+Proposed (supersedes SADR-0005 for the implementer default; keeps its auth model)\n \n ## Context\n \n-ADR 0005 made Codex Front the default implementation runtime, with Claude Code\n+SADR-0005 made Codex Front the default implementation runtime, with Claude Code\n restricted to planner and reviewer roles by the runtime registry, and the only\n autonomous implementation adapter was `shiki runner codex`.\n \n@@ -18,13 +18,13 @@\n - The platform's planning, review, and CCA layers already run on Claude Code,\n   and Claude Code's native capabilities (the built-in `/code-review` skill and\n   the Workflow multi-agent orchestration tool) have no Codex equivalent.\n-- The Goal lifecycle redesign (ADR 0009) requires an autonomous post-freeze\n+- The Goal lifecycle redesign (SADR-0009) requires an autonomous post-freeze\n   loop driven from the operator's Claude Code session, including bounded\n   repair dispatch, which must not depend on a runtime that cannot be invoked\n   unattended.\n \n The constitution claims Shiki is runtime-agnostic (\"not a Claude-only\n-workflow\", ADR 0002). Any change to the implementer default must preserve the\n+workflow\", SADR-0002). Any change to the implementer default must preserve the\n registry-based runtime model rather than hardcoding one vendor.\n \n ## Decision\n@@ -39,7 +39,7 @@\n   the task to review or repair-needed.\n - Keep Codex registered as an optional implementer runtime. `shiki runner\n   codex`, the Codex handoff templates, and the ChatGPT OAuth auth model from\n-  ADR 0005 remain supported for tasks explicitly assigned to `codex`.\n+  SADR-0005 remain supported for tasks explicitly assigned to `codex`.\n - Commit to mandating two Claude-native capabilities as loop contract in\n   follow-up Goals (status: the code-review gate was delivered by Goal G-B \u2014\n   skill registry, checklists item PR-12, default required_skills \u2014 while the\n@@ -60,7 +60,7 @@\n \n Out of scope: removing Codex support, changing the CCA runtime (Claude Code\n Action with `CLAUDE_CODE_OAUTH_TOKEN` stays), and API-key based automation\n-(still requires its own ADR per ADR 0005).\n+(still requires its own target ADR per SADR-0005).\n \n ## Consequences\n \n@@ -100,7 +100,7 @@\n   harness rejection of direct `codex exec` is outside Shiki's control, and the\n   plan's mandatory native capabilities cannot run on Codex at all.\n - **Remove Codex entirely**: rejected; it abandons the runtime-agnostic\n-  constitution (ADR 0002) for no operational gain and forecloses\n+  constitution (SADR-0002) for no operational gain and forecloses\n   cross-runtime adversarial review.\n - **GitHub Actions cloud implementation via claude-code-action**: rejected as\n   default; durable evidence is automatic but token cost, Node 24 workflow\ndiff --git a/docs/adr/0009-requirements-definition-and-spec-freeze.md b/docs/adr/SADR-0009-requirements-definition-and-spec-freeze.md\nindex 29b3f7871bef9203a8b4a831a65c39eb2f506e42..52e7dd69ad0aa4efe0b68c62678abbf7df02aaec\n--- a/docs/adr/0009-requirements-definition-and-spec-freeze.md\n+++ b/docs/adr/SADR-0009-requirements-definition-and-spec-freeze.md\n@@ -1,4 +1,4 @@\n-# ADR 0009: Merge Grill And Planning Into Requirements Definition With Spec Freeze\n+# SADR-0009: Merge Grill And Planning Into Requirements Definition With Spec Freeze\n \n ## Status\n \ndiff --git a/docs/adr/0010-external-ai-guardian-review.md b/docs/adr/SADR-0010-external-ai-guardian-review.md\nindex e6108bbe8bb4e73e0ff4684c8f78001a80438c4e..f3510b145664298adafb151b6f0db51b972f01ec\n--- a/docs/adr/0010-external-ai-guardian-review.md\n+++ b/docs/adr/SADR-0010-external-ai-guardian-review.md\n@@ -1,4 +1,4 @@\n-# ADR 0010: External AI Guardian Review As A First-Class Approval Authority\n+# SADR-0010: External AI Guardian Review As A First-Class Approval Authority\n \n ## Status\n \n@@ -8,7 +8,7 @@\n \n Shiki is intended to be an autonomous, recursive, self-improving agent\n framework: authority-in-the-loop, not human-in-the-loop. Until now the Guardian\n-approval required for high/critical-risk merges (guardian-policy.json, ADR\n+approval required for high/critical-risk merges (guardian-policy.json, SADR\n governance) recognized only human authority sources \u2014 a GitHub review approval,\n the `guardian:approved` label applied by a configured Guardian, or a comment\n carrying `Guardian approval granted` plus the current head SHA.\n@@ -89,7 +89,7 @@\n   autonomous design the operator intends and produced the impersonation\n   incident.\n - **Record AI approval as an operator comment**: rejected as forgery of human\n-  authority \u2014 the exact failure this ADR corrects.\n+  authority \u2014 the exact failure this SADR corrects.\n - **Deliver the artifact as a committed `.shiki/` file**: rejected; a committed\n   approval inside the PR being approved is forgeable by anyone who can push.\n - **A dedicated reviewer bot identity instead of a relayed comment**: deferred;\ndiff --git a/docs/adr/0011-autonomous-quality-gate-evidence.md b/docs/adr/SADR-0011-autonomous-quality-gate-evidence.md\nindex bca0246c5d46e44afbde15486a75eec425fc02f5..9c93eac16d8c3a6d4b11bd7224e3955102cd7db4\n--- a/docs/adr/0011-autonomous-quality-gate-evidence.md\n+++ b/docs/adr/SADR-0011-autonomous-quality-gate-evidence.md\n@@ -1,4 +1,4 @@\n-# ADR 0011: Autonomous Quality-Gate Evidence \u2014 Deterministic Facts And An Independent Verifier\n+# SADR-0011: Autonomous Quality-Gate Evidence \u2014 Deterministic Facts And An Independent Verifier\n \n ## Status\n \ndiff --git a/docs/adr/0012-autonomous-loop-closeout-to-main.md b/docs/adr/SADR-0012-autonomous-loop-closeout-to-main.md\nindex 637fad6dde4989c9c2bf0f81f676d8d29991123c..ec7d85f81030a676bcbba28b43eb365422a36eac\n--- a/docs/adr/0012-autonomous-loop-closeout-to-main.md\n+++ b/docs/adr/SADR-0012-autonomous-loop-closeout-to-main.md\n@@ -1,4 +1,4 @@\n-# 0012 \u2014 Autonomous loop pushes goal completion to main via a closeout PR\n+# SADR-0012 \u2014 Autonomous loop pushes goal completion to main via a closeout PR\n \n - Status: accepted\n - Date: 2026-06-18\ndiff --git a/docs/adr/0013-bootstrap-grants-actions-approve-pull-requests.md b/docs/adr/SADR-0013-bootstrap-grants-actions-approve-pull-requests.md\nindex b6c69d64f1c5ced346b88d6859586715b3b5bd0e..cf48ff4a6716267f539ca58edd1a852e7c7a2d64\n--- a/docs/adr/0013-bootstrap-grants-actions-approve-pull-requests.md\n+++ b/docs/adr/SADR-0013-bootstrap-grants-actions-approve-pull-requests.md\n@@ -1,4 +1,4 @@\n-# ADR 0013: Bootstrap Grants GitHub Actions Permission To Create And Approve Pull Requests\n+# SADR-0013: Bootstrap Grants GitHub Actions Permission To Create And Approve Pull Requests\n \n ## Status\n \n@@ -31,7 +31,7 @@\n security tradeoff: it lets repository automation satisfy a human-review gate. In\n a multi-maintainer repo that could let a compromised or buggy workflow\n self-approve changes. So whether bootstrap turns it on by default \u2014 and with\n-what default token scope \u2014 is an ADR-worthy decision, not an implementation\n+what default token scope \u2014 is a SADR-worthy decision, not an implementation\n detail.\n \n ## Decision\n@@ -48,7 +48,7 @@\n Specifically:\n \n - **`can_approve_pull_request_reviews` defaults to `true`.** Shiki's default\n-  posture is solo/AFK autonomous operation (ADR 0008/0009/0012), where the CCA\n+  posture is solo/AFK autonomous operation (SADR-0008/0009/0012), where the CCA\n   Review Bridge is the only path that can satisfy `required_review: true` after\n   the CCA and Guardian gates have already passed. The default makes a\n   freshly-bootstrapped repo able to run the autonomous loop end to end without a\ndiff --git a/docs/adr/0014-codex-app-external-ai-guardian-ui-adapter.md b/docs/adr/SADR-0014-codex-app-external-ai-guardian-ui-adapter.md\nindex 238db06279334caa341c117362df9271382a7fcd..65f12e9939321e1b3e389a34c822fc1549e5155d\n--- a/docs/adr/0014-codex-app-external-ai-guardian-ui-adapter.md\n+++ b/docs/adr/SADR-0014-codex-app-external-ai-guardian-ui-adapter.md\n@@ -1,4 +1,4 @@\n-# ADR 0014: Use Codex App As The External AI Guardian UI Adapter\n+# SADR-0014: Use Codex App As The External AI Guardian UI Adapter\n \n ## Status\n \ndiff --git a/docs/adr/0015-contract-approval-before-dispatch.md b/docs/adr/SADR-0015-contract-approval-before-dispatch.md\nindex fad6c656741a20a7684ba16080a7db159f63a52f..6061271a46d3f1d6ae7174477de3fb75673123a6\n--- a/docs/adr/0015-contract-approval-before-dispatch.md\n+++ b/docs/adr/SADR-0015-contract-approval-before-dispatch.md\n@@ -1,4 +1,4 @@\n-# ADR 0015: Approve The Task Contract Before Dispatch, Not The Diff After It\n+# SADR-0015: Approve The Task Contract Before Dispatch, Not The Diff After It\n \n ## Status\n \n@@ -9,7 +9,7 @@\n Guardian approval for high/critical risk is expressible only on a pull request.\n Every approval source in `guardian-policy.json` \u2014 GitHub review, the\n `guardian:approved` label, the `Guardian approval granted` comment, and the\n-`external_ai_guardian_review` artifact (ADR 0010/0014) \u2014 is bound to a PR, and\n+`external_ai_guardian_review` artifact (SADR-0010/0014) \u2014 is bound to a PR, and\n three of the four require the current head SHA. Approval therefore cannot exist\n before an implementation PR exists.\n \n@@ -81,7 +81,7 @@\n An envelope breach must surface only on the non-repairable policy gate, never on\n the repairable `MergeGate metadata check`. Otherwise the Goal Loop routes it to\n `dispatch_repair` \u2014 instructing a runner to make the breach go away \u2014 which is\n-exactly what this ADR forbids.\n+exactly what this SADR forbids.\n \n **Contract immutability.** After registration, an implementation PR must not\n change the governance fields of its own task contract (`scope`, `non_goals`,\n@@ -116,7 +116,7 @@\n fails any verdict that is not `complete`, and the signal the CCA prompt reads is\n computed from `evaluate_guardian_approval` alone. Without the OR-merge at the\n signal, a contract-approved PR still returns `needs_guardian`, the CCA job fails,\n-and the MergeGate policy check never runs \u2014 the gate this ADR changes would never\n+and the MergeGate policy check never runs \u2014 the gate this SADR changes would never\n be reached. The CCA *prompt* needs no change: a `contract_approval` entry in the\n signal's existing `sources` array satisfies its current wording.\n \n@@ -130,7 +130,7 @@\n written, and an approved contract lets its implementation PRs merge through the\n same automatic path as low/medium risk. The wasted implement-then-block cycle\n disappears. Guardian review shifts from reading diffs to reading contracts, which\n-is both earlier and smaller; the external AI Guardian path (ADR 0010/0014) works\n+is both earlier and smaller; the external AI Guardian path (SADR-0010/0014) works\n on contracts with no change.\n \n We accept that the Guardian authorizes a class of change without seeing its\n@@ -146,7 +146,7 @@\n adversarial tests for the new approval path equivalent in rigor to\n `test_shiki_governance_evidence.sh`.\n \n-Bootstrap costs, which apply only while this ADR is being implemented. The tasks\n+Bootstrap costs, which apply only while this SADR is being implemented. The tasks\n that build Contract Approval merge under the existing rules, so the ones\n dispatched after the pre-dispatch gate lands but before the approval path lands\n need a Guardian-approved Contract PR *and* a live Guardian approval on their\n@@ -210,7 +210,7 @@\n risk; they do not bound authorized work.\n \n **Auto-escalate risk on envelope breach instead of stopping.** Returns the\n-workflow to a diff-time Guardian decision \u2014 the exact behavior this ADR removes.\n+workflow to a diff-time Guardian decision \u2014 the exact behavior this SADR removes.\n \n **Resolve governance fields from the base snapshot only.** Rejected against the\n registration flow: implementation PRs add their own contract, so the base has no\ndiff --git a/docs/adr/0016-locks-bound-product-paths.md b/docs/adr/SADR-0016-locks-bound-product-paths.md\nindex b5e850f3f1f7f4ef6e7f9d1763a29a51bd300c45..46b40a9b99a7aae644c02bb458f5473ff0a90ce2\n--- a/docs/adr/0016-locks-bound-product-paths.md\n+++ b/docs/adr/SADR-0016-locks-bound-product-paths.md\n@@ -1,4 +1,4 @@\n-# ADR 0016: Locks bound product paths; the Shiki mirror is bound by record identity\n+# SADR-0016: Locks bound product paths; the Shiki mirror is bound by record identity\n \n ## Status\n \n@@ -6,8 +6,8 @@\n \n - Date: 2026-07-28\n - Deciders: operator (mizutani-140), Claude (planner)\n-- Related: ADR 0004 (GitHub-first with a `.shiki` mirror), ADR 0009 (Spec Freeze),\n-  ADR 0012 (autonomous loop closeout to main), ADR 0015 (Contract Approval before\n+- Related: SADR-0004 (GitHub-first with a `.shiki` mirror), SADR-0009 (Spec Freeze),\n+  SADR-0012 (autonomous loop closeout to main), SADR-0015 (Contract Approval before\n   dispatch), remediation goal task R-02, the live PR #179 deadlock.\n \n ## Context\n@@ -90,11 +90,11 @@\n files, documentation files and named source modules are nameable; runtime-\n allocated ledger, runner and report ids are not \u2014 and those live in `.shiki`,\n which locks no longer govern. Existing frozen contracts are brought into line by\n-a one-time Spec Amendment per plan, which must land before ADR 0015's contract\n+a one-time Spec Amendment per plan, which must land before SADR-0015's contract\n immutability makes `locks` unwritable from the implementation PR.\n \n **Lock state is resolved from the durable side.** MergeGate resolves the lock\n-registry, and ADR 0015's governance fields, from the base `.shiki` snapshot when\n+registry, and SADR-0015's governance fields, from the base `.shiki` snapshot when\n present, falling back to head only when absent. The `MergeGate metadata check`\n job is given `--base-shiki`, which it does not receive today. Locks are not\n derived from the live open-pull-request set: a pull request body is untrusted\n@@ -108,7 +108,7 @@\n Explicitly out of scope: moving MergeGate's contract resolution off the PR\n checkout wholesale; making the mirror a projection of GitHub state; introducing\n new lock kinds; moving ledger evidence out of the repository; and unwinding\n-ADR 0012's closeout pull request. Registration of multi-task goals is ADR 0015's\n+SADR-0012's closeout pull request. Registration of multi-task goals is SADR-0015's\n Contract PR and is not re-decided here.\n \n ## Consequences\n@@ -127,7 +127,7 @@\n \n Risks accepted. First, goal-scoped identity is only as strong as `goal_id`,\n which is resolved from the PR body and the head task file; the mitigation is to\n-add `goal_id` to ADR 0015's frozen governance field set, and until that lands a\n+add `goal_id` to SADR-0015's frozen governance field set, and until that lands a\n task file can name a foreign goal. Second, base-when-present resolution is\n unavailable on fork pull requests, where the head is contributor-controlled;\n fork pull requests already cannot complete a Shiki merge, and this decision does\n@@ -139,7 +139,7 @@\n path, not by implementation pull requests.\n \n New obligations: one Spec Amendment per frozen plan to rewrite `locks`, ordered\n-before ADR 0015 T1; extension of the adversarial suites in\n+before SADR-0015 T1; extension of the adversarial suites in\n `scripts/test_shiki_adversarial_state.sh` and\n `scripts/test_shiki_governance_evidence.sh` to cover the eleven newly-scoped\n directories; and updating `scripts/test_shiki_loop_lock_guard.sh`,\n@@ -151,32 +151,32 @@\n whether the loop's transitive sync of ledger `evidence` references can be\n deleted once the Contract PR carries its own source plan.\n \n-## Relationship to ADR 0004\n+## Relationship to SADR-0004\n \n-This ADR **clarifies something ADR 0004 left unspecified**. ADR 0004 decides that\n+This SADR **clarifies something SADR-0004 left unspecified**. SADR-0004 decides that\n GitHub is the operational source of truth and that each target repository keeps a\n `.shiki/` mirror. It says nothing about pull requests writing the mirror, does\n not name MergeGate, and does not make the mirror an input to a gate. Nothing in\n-ADR 0004's Decision or Consequences is amended, superseded or weakened: the\n+SADR-0004's Decision or Consequences is amended, superseded or weakened: the\n mirror still exists, still records the same artifacts, and pull request\n-boundaries remain the coordination surface. What this ADR settles is the question\n+boundaries remain the coordination surface. What this SADR settles is the question\n 0004 never reached \u2014 by what authority a pull request may write a mirror record \u2014\n and it answers it the way `.shiki/manifest.json` already declares\n (`pr_mutation: current-task-or-current-goal-only`).\n \n-It **amends ADR 0012**, whose Decision item 5 states that \"Loop-executed tasks\n+It **amends SADR-0012**, whose Decision item 5 states that \"Loop-executed tasks\n declare the `path:.shiki/**` lock, which covers every `.shiki` file the closeout\n stages ... so `files_outside_locks` is satisfied without per-file lock\n bookkeeping.\" That clause is replaced: the closeout's mirror writes are\n-authorized by record identity, not by a declared lock. ADR 0012's substantive\n+authorized by record identity, not by a declared lock. SADR-0012's substantive\n decision \u2014 that completion reaches `main` through a closeout pull request rather\n than local attestation \u2014 is preserved unchanged.\n \n-It is **complementary to ADR 0015**, not a substitute. ADR 0015 owns contract\n+It is **complementary to SADR-0015**, not a substitute. SADR-0015 owns contract\n immutability, the pre-dispatch gate, the Contract PR and the Scope Envelope. This\n-ADR owns what a lock means and who may write the mirror. The one ordering\n+SADR owns what a lock means and who may write the mirror. The one ordering\n constraint between them is that the lock-narrowing Spec Amendment must precede\n-ADR 0015 T1's freezing of the `locks` field.\n+SADR-0015 T1's freezing of the `locks` field.\n \n ## Alternatives Considered\n \n@@ -186,7 +186,7 @@\n the only writer that can register them may run. Migrating the Guardian signal to\n that store makes every registration PR fail closed to `needs_guardian` at every\n risk level, because the task file is by definition absent from the store, which\n-removes the low/medium autonomous path ADR 0008/0009 exists to provide. And it\n+removes the low/medium autonomous path SADR-0008/0009 exists to provide. And it\n leaves schemas, manifest, config, guardian policy, templates, migrations, plans,\n worktrees, runner records, runs and handoffs with no writer at all in a\n repository whose product is `.shiki`. Measured benefit on the frozen backlog:\n@@ -199,14 +199,14 @@\n namespace is not in fact ownership-partitioned \u2014 56 ledger entries carry no\n `task_id`, and one goal DAG is writable by 35 registered tasks. The correct\n expression of its insight is that locks do not govern the mirror at all, which is\n-what this ADR decides.\n+what this SADR decides.\n \n **Make the mirror a projection written only by a registration writer and a\n post-merge projection job.** Rejected. It requires a bypass actor with write\n access to the protected default branch; it makes its own master invariant\n readable from a manifest that pull requests may edit; it deletes both reconcile\n modes and leaves a corrupted mirror with no in-band repair path, contradicting\n-ADR 0004's consequence that mirror conflicts are repaired against GitHub state;\n+SADR-0004's consequence that mirror conflicts are repaired against GitHub state;\n and its content-derived ids are rejected by `validate_shiki`'s currently-green id\n pattern.\n \ndiff --git a/docs/adr/0017-bookkeeping-closeouts-do-not-inherit-risk.md b/docs/adr/SADR-0017-bookkeeping-closeouts-do-not-inherit-risk.md\nindex 554e3a0d526a72ca067108c5464cc68c3edcf174..b9676857a4713850e063af7c39c8e0b01ecb4c66\n--- a/docs/adr/0017-bookkeeping-closeouts-do-not-inherit-risk.md\n+++ b/docs/adr/SADR-0017-bookkeeping-closeouts-do-not-inherit-risk.md\n@@ -1,4 +1,4 @@\n-# ADR 0017: A Bookkeeping Closeout Does Not Inherit Its Task's Risk\n+# SADR-0017: A Bookkeeping Closeout Does Not Inherit Its Task's Risk\n \n ## Status\n \n@@ -6,7 +6,7 @@\n \n ## Context\n \n-ADR 0012 made the goal loop push a task's completion to the default branch through a\n+SADR-0012 made the goal loop push a task's completion to the default branch through a\n closeout pull request: after the implementation PR merges, the loop opens a second PR\n carrying `task.status = done`, `lock.state = released`, and \u2014 when the task completes\n its goal \u2014 `goal.status = complete` plus one scorecard report. Nothing else. The\n@@ -49,14 +49,14 @@\n 4. The task file's governance fields (`scope`, `non_goals`, `required_skills`,\n    `risk_level`, `locks`, `acceptance_checks`, `test_command`) are byte-identical to\n    the base snapshot. Only `status`, `expected_pr`, `closeout_pr`, `ledger_evidence`\n-   and `expected_branch` may differ \u2014 the ADR 0015 contract-immutability rule,\n+   and `expected_branch` may differ \u2014 the SADR-0015 contract-immutability rule,\n    unchanged.\n \n    `expected_branch` is in that set because a closeout is *by construction* on a\n    different branch from the implementation it closes, and the loop rewrites the\n    field accordingly. It is safe to let it differ because MergeGate independently\n    requires `expected_branch` to equal the PR's actual head ref, so a PR cannot set\n-   it to anything other than the branch it is really on. The first draft of this ADR\n+   it to anything other than the branch it is really on. The first draft of this SADR\n    omitted it, which made the exemption unreachable: every real closeout failed\n    condition 4 on a field the loop always changes. See PR #209.\n 5. The state transitions are exactly the terminal set: task `review` \u2192 `done`, lock\n@@ -113,12 +113,12 @@\n classification must be derived, not asserted.\n \n **Fold the closeout into the implementation PR** so no second PR exists. Rejected under\n-ADR 0012, which recorded why: a task file that is already `done` on the implementation\n+SADR-0012, which recorded why: a task file that is already `done` on the implementation\n branch short-circuits the loop's review \u2192 CCA \u2192 merge \u2192 done ordering into a local\n goal-completion before any merge happens.\n \n **Have the loop auto-merge closeouts with elevated credentials.** Rejected outright: that\n-is a runtime authoring its own approval, which ADR 0010 and the constitution's\n+is a runtime authoring its own approval, which SADR-0010 and the constitution's\n approval-record prohibition forbid. It would also be indistinguishable, in the evidence,\n from the forgery those rules exist to prevent.\n \ndiff --git a/docs/adr/0018-carried-guardian-approval-across-a-proven-base-sync.md b/docs/adr/SADR-0018-carried-guardian-approval-across-a-proven-base-sync.md\nindex 22358562da390ed8b45cd600da08a43d378b4680..e6e2907cefa776460af73ddf7d39db605f3c39b0\n--- a/docs/adr/0018-carried-guardian-approval-across-a-proven-base-sync.md\n+++ b/docs/adr/SADR-0018-carried-guardian-approval-across-a-proven-base-sync.md\n@@ -1,4 +1,4 @@\n-# ADR 0018: Carried Guardian Approval Across A Proven Base Sync\n+# SADR-0018: Carried Guardian Approval Across A Proven Base Sync\n \n ## Status\n \n@@ -34,7 +34,7 @@\n Guardian evaluator that consumes it (`shiki_guardian.evaluate_guardian_approval`,\n the `guardian_comment_carried` source) were built and tested in isolation. Neither\n does anything until a caller passes the flags that turn the carry on, so the\n-feature was invisible in CI. This ADR records the decision the two of them\n+feature was invisible in CI. This SADR records the decision the two of them\n implement, and this task wires it into the four CI invocations that judge Guardian\n approval.\n \ndiff --git a/docs/adr/README.md b/docs/adr/README.md\nindex c70e057a4520c36e042ab68c53cd4a21ef709b71..5da43856836e2ce6a150f8127766e2f88355ab4b\n--- a/docs/adr/README.md\n+++ b/docs/adr/README.md\n@@ -1,40 +1,41 @@\n-# Architecture Decision Records\n+# Decision Record Namespaces\n \n-This directory holds Shiki's Architecture Decision Records (ADRs): durable\n-records of hard-to-reverse platform decisions. ADRs are part of the Shiki source\n-of truth alongside GitHub state, the `.shiki/` mirror, and `CONTEXT.md`.\n+This directory holds two independent decision namespaces:\n \n-## How ADRs Work\n+- Shiki Architecture Decision Records (SADRs), named\n+  `SADR-NNNN-<short-slug>.md`, record hard-to-reverse Shiki platform decisions.\n+- Target Architecture Decision Records (ADRs), named\n+  `NNNN-<short-slug>.md`, belong to the target product.\n \n-- New ADRs are added as `docs/adr/NNNN-<short-slug>.md`, where `NNNN` is the next\n-  zero-padded sequence number.\n-- The **canonical list of ADRs is the set of files matching `docs/adr/*.md`** in\n-  this directory. Browse `docs/adr/*.md` for the authoritative, current set.\n-- Use [`template.md`](template.md) as the starting point for a new ADR.\n-- Record an ADR when a decision is hard to reverse, surprising without context,\n-  and a real tradeoff. Do not write an ADR for routine or easily reversible\n-  choices.\n+The same four-digit number may appear once in each namespace. A platform\n+`SADR-NNNN` citation resolves only to `SADR-NNNN-*.md`; a target `ADR NNNN`\n+citation resolves only to `NNNN-*.md`.\n \n-Using the `docs/adr/*.md` glob as the source of truth means other PRs can add an\n-ADR without editing this index file, avoiding merge contention on a\n-hand-maintained enumeration.\n+## How Decision Records Work\n \n-## Current ADRs (snapshot)\n-\n-The list below is a convenience snapshot and may lag behind the directory. When\n-in doubt, list `docs/adr/*.md`.\n+- New Shiki platform decisions use the next zero-padded SADR number.\n+- New target product decisions use the target's own ADR sequence.\n+- The canonical list is the files in `docs/adr/`; no hand-maintained index is\n+  authoritative.\n+- Use [`template.md`](template.md) as the starting point and choose the filename\n+  namespace that owns the decision.\n+- Record a decision when it is hard to reverse, surprising without context, and\n+  a real tradeoff. Do not write a decision record for routine or easily\n+  reversible choices.\n \n-- [0001 \u2014 Keep Shiki As The Platform Name](0001-keep-shiki-as-platform-name.md)\n-- [0002 \u2014 Use AGENTS As The Shared Agent Constitution](0002-use-agents-as-shared-constitution.md)\n-- [0003 \u2014 Require Skill Gate For MergeGate Readiness](0003-require-skill-gate-for-mergegate-readiness.md)\n-- [0004 \u2014 Use GitHub First With A Repository-Local Shiki Mirror](0004-use-github-first-with-shiki-mirror.md)\n-- [0005 \u2014 Use Codex As Front And Claude Code Action As The GitHub Runtime](0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md)\n-- [0006 \u2014 Separate Codex Implementation, CCA Completion, And MergeGate Authorization](0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md)\n+Using the directory as the source of truth lets independent changes add records\n+without contending on an enumerated index.\n \n-## Adding A New ADR\n+## Adding A Shiki SADR\n \n-1. Copy `template.md` to `docs/adr/NNNN-<short-slug>.md`.\n+1. Copy `template.md` to `docs/adr/SADR-NNNN-<short-slug>.md`.\n 2. Fill in the title, status, context, decision, and consequences.\n-3. Set the status to `Proposed`, then `Accepted` once the decision is approved.\n-4. Open a PR following `CONTRIBUTING.md`. You do not need to edit this index for\n-   the ADR to be discoverable via `docs/adr/*.md`.\n+3. Set the status to `Proposed`, then `Accepted` once the proper authority has\n+   approved the decision.\n+4. Open a PR following `CONTRIBUTING.md`.\n+\n+## Adding A Target ADR\n+\n+1. Copy `template.md` to `docs/adr/NNNN-<short-slug>.md` using the target's own\n+   sequence.\n+2. Cite it as `ADR NNNN`; do not prefix its filename with `SADR-`.\ndiff --git a/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md b/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\nnew file mode 100644\nindex 0000000000000000000000000000000000000000..7455ce1f3bed02ddddb851ac21a1d1a648c9aeba\n--- /dev/null\n+++ b/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\n@@ -0,0 +1,55 @@\n+# SADR-0019: Separate Shiki Platform SADRs From Target ADRs\n+\n+## Status\n+\n+Accepted\n+\n+## Context\n+\n+Shiki installs its platform decision records into `docs/adr/` in every target\n+repository. Shiki and its targets previously used the same numeric\n+`NNNN-<slug>.md` namespace. A target that already owned ADRs 0013 through 0018\n+therefore acquired two records for each number during installation, making an\n+`ADR NNNN` citation ambiguous. Renumbering target decisions would rewrite target\n+history, while renumbering Shiki decisions per target would destroy their stable\n+platform identity.\n+\n+Upgrades also need to remove the former Shiki-owned numeric paths without ever\n+mistaking a target-owned ADR for platform content. The install stamp already\n+records exact shipped paths and content digests, so it can prove ownership only\n+when both still match.\n+\n+## Decision\n+\n+Shiki platform decisions use the `SADR` namespace:\n+\n+- records are named `docs/adr/SADR-NNNN-<slug>.md`;\n+- live platform citations use `SADR-NNNN`;\n+- target product decisions retain `docs/adr/NNNN-<slug>.md` and `ADR NNNN`;\n+- the reference validator resolves the two namespaces independently, so the same\n+  four-digit number may exist once in each namespace.\n+\n+Before a forced upgrade writes anything, the installer inventories the exact\n+former numeric Shiki ADR paths. It may delete a legacy path only when the\n+existing install stamp contains that exact path and its recorded SHA-256 digest\n+matches the file currently on disk. All ownership blockers are reported\n+together and abort the upgrade before its first write. A non-forced install\n+never performs this cleanup.\n+\n+Historical `.shiki/` evidence remains unchanged because it records the namespace\n+that existed when that evidence was produced.\n+\n+## Consequences\n+\n+- Targets may use any ADR number without colliding with Shiki's platform SADRs.\n+- Fresh installs preserve target ADR bytes and install a stable SADR set.\n+- Forced upgrades fail closed when legacy ownership cannot be proven, and a\n+  successful upgrade refreshes the stamp without removed legacy paths.\n+- Current governance, packaging, workflow, prompt, script, and skill references\n+  must move with the platform records.\n+\n+## Alternatives Considered\n+\n+- Collision-only refusal leaves affected targets permanently unupgradable.\n+- Renumbering target ADRs mutates target-owned history.\n+- Per-target Shiki renumbering makes platform citations non-portable.\ndiff --git a/docs/adr/template.md b/docs/adr/template.md\nindex a92a97daddd0b00ab9dd626cc8d86942da2eafed..17dd79175b5fee2105935557f691b40cf9492942\n--- a/docs/adr/template.md\n+++ b/docs/adr/template.md\n@@ -1,14 +1,14 @@\n-# NNNN: <Short Decision Title>\n+# <SADR-NNNN or NNNN>: <Short Decision Title>\n \n ## Status\n \n-Proposed | Accepted | Superseded by ADR-NNNN\n+Proposed | Accepted | Superseded by SADR-NNNN or target ADR NNNN\n \n ## Context\n \n What is the situation, constraint, or force that requires a decision? Reference\n-relevant domain terms (`CONTEXT.md`), prior ADRs, and code reality. State the\n-problem, not the solution.\n+relevant domain terms (`CONTEXT.md`), prior Shiki SADRs or target ADRs, and code\n+reality. State the problem, not the solution.\n \n ## Decision\n \ndiff --git a/docs/agents/autonomous-loop-e2e-acceptance.md b/docs/agents/autonomous-loop-e2e-acceptance.md\nindex bbf84c6081987f159ac6ad9ecf3c82b7aa72e7e8..5c13c1d02a05a90b0dc46cbef7c95a9846d0c237\n--- a/docs/agents/autonomous-loop-e2e-acceptance.md\n+++ b/docs/agents/autonomous-loop-e2e-acceptance.md\n@@ -40,8 +40,8 @@\n 3. `gh auth status` is logged in with merge permission on the repo.\n 4. Branch protection + required checks are configured exactly as the goal-loop\n    required-check set expects (see `docs/agents/branch-protection-smoke.md`).\n-5. The repo `.shiki/policy` permits autonomous low/medium-risk merges (ADR\n-   0008/0009); the MergeGate policy check is the recorded-authority gate.\n+5. The repo `.shiki/policy` permits autonomous low/medium-risk merges\n+   (SADR-0008/0009); the MergeGate policy check is the recorded-authority gate.\n \n The operator's only actions are: register the task and start the loop. **No\n manual pre-clearing** means after `shiki loop run` starts, the operator does not\ndiff --git a/docs/agents/bootstrap-command.md b/docs/agents/bootstrap-command.md\nindex 05f7f203a52cf412b9f97084a6e131ed0f785afd..f7aa0b734dfebee4a3b139335b66ee3415f48713\n--- a/docs/agents/bootstrap-command.md\n+++ b/docs/agents/bootstrap-command.md\n@@ -189,7 +189,9 @@\n ```\n \n Use this only for tests, fixtures, or explicit local-only template inspection.\n-Use `--force` only when you intentionally want to overwrite existing target files.\n+Use `--force` only when you intentionally want to refresh the target template.\n+Legacy numeric Shiki decision cleanup is stamp-and-digest authorized before any\n+write; target ADR paths are not cleanup candidates.\n \n ## Slash Command\n \ndiff --git a/docs/agents/checklists.md b/docs/agents/checklists.md\nindex 4a7f03ade346f53a1c1d82981f3fd744f9ab4547..0c5ec6c53dc7197b7b7a7d18e667b325babd6c45\n--- a/docs/agents/checklists.md\n+++ b/docs/agents/checklists.md\n@@ -32,18 +32,18 @@\n | ID | Item | Blocking |\n | --- | --- | --- |\n | GD-01 | Plan was challenged against `CONTEXT.md`. | blocking for non-trivial Goals |\n-| GD-02 | Relevant ADRs were read or absence was recorded. | blocking for non-trivial Goals |\n+| GD-02 | Relevant SADRs and target ADRs were read or absence was recorded. | blocking for non-trivial Goals |\n | GD-03 | Ambiguous terms were resolved or converted to HITL blockers. | blocking |\n | GD-04 | Edge scenarios were considered. | advisory |\n | GD-05 | Code reality was checked when code could answer. | blocking when applicable |\n | GD-06 | New domain terms were added to `CONTEXT.md` when needed. | blocking when applicable |\n-| GD-07 | ADR was created or explicitly skipped using the ADR criteria. | blocking when applicable |\n+| GD-07 | A SADR or target ADR was created or explicitly skipped using the decision-record criteria. | blocking when applicable |\n \n ## CI \u2014 Context & Impact Checklist\n \n | ID | Item | Blocking |\n | --- | --- | --- |\n-| CI-01 | Relevant docs and ADRs identified. | blocking |\n+| CI-01 | Relevant docs, SADRs, and target ADRs identified. | blocking |\n | CI-02 | Relevant modules, interfaces, seams, callers, and tests identified. | blocking |\n | CI-03 | Dependencies and blocked-by relationships identified. | blocking |\n | CI-04 | Candidate locks identified. | blocking |\n@@ -62,7 +62,7 @@\n | PRD-04 | Implementation decisions are recorded without volatile code detail. | blocking |\n | PRD-05 | Testing decisions are recorded. | blocking |\n | PRD-06 | Out-of-scope items are explicit. | blocking |\n-| PRD-07 | Relevant glossary/ADR links are present when applicable. | blocking |\n+| PRD-07 | Relevant glossary/SADR/target-ADR links are present when applicable. | blocking |\n \n ## SF \u2014 Spec Freeze Checklist\n \n@@ -98,7 +98,7 @@\n | H-02 | Branch/worktree target is present. | blocking |\n | H-03 | Scope and non-goals are present. | blocking |\n | H-04 | Dependencies and locks are present. | blocking |\n-| H-05 | Relevant docs, ADRs, modules, and tests are referenced. | blocking |\n+| H-05 | Relevant docs, SADRs, target ADRs, modules, and tests are referenced. | blocking |\n | H-06 | Required skills are stated. | blocking |\n | H-07 | TDD expectations are stated. | blocking when behavior work |\n | H-08 | Acceptance criteria are included. | blocking |\ndiff --git a/docs/agents/codex-handoff.md b/docs/agents/codex-handoff.md\nindex ede468f47cb277f69a857e8c45df703e4ed0151d..6f97bdffb67e44d7edea2c71e01ea28933c4eb6c\n--- a/docs/agents/codex-handoff.md\n+++ b/docs/agents/codex-handoff.md\n@@ -46,7 +46,7 @@\n \n ## Context\n - Domain terms:\n-- Relevant ADRs:\n+- Relevant SADRs and target ADRs:\n - Relevant docs:\n - Relevant modules/interfaces/seams:\n - Similar tests/prior art:\ndiff --git a/docs/agents/completion-check-agent.md b/docs/agents/completion-check-agent.md\nindex 13e4c035db35025f4e3cdcc5adfb24314ac4d521..61bfa1218cf36ca605250c91919e620f33238aa0\n--- a/docs/agents/completion-check-agent.md\n+++ b/docs/agents/completion-check-agent.md\n@@ -32,7 +32,7 @@\n - `AGENTS.md`\n - `CLAUDE.md`\n - `CONTEXT.md`\n-- relevant ADRs\n+- relevant SADRs and target ADRs\n - parent Goal issue\n - PRD issue or document\n - task issue\ndiff --git a/docs/agents/decision-control.md b/docs/agents/decision-control.md\nindex 7d8ee5ab03a5ccbcbd04056ae61de03785a2a9c9..8c94ecda00fffaa0e92c325ab83aa9776bd1dac1\n--- a/docs/agents/decision-control.md\n+++ b/docs/agents/decision-control.md\n@@ -11,7 +11,7 @@\n | Decision | Owner | When | Required inputs | Output | Enforcement surface |\n | --- | --- | --- | --- | --- | --- |\n | Is the Goal clear enough to plan? | Planner + Guardian when needed | Goal intake | User request, existing docs, risk signals | Goal readiness verdict | Goal checklist, issue template, ledger |\n-| Must `grill-with-docs` run? | Planner / Skill Gate | Before PRD or issue split | Ambiguity, domain terms, ADR conflicts, unclear boundaries | Skill invocation or explicit skip rationale | Skill Gate checklist |\n+| Must `grill-with-docs` run? | Planner / Skill Gate | Before PRD or issue split | Ambiguity, domain terms, SADR/target-ADR conflicts, unclear boundaries | Skill invocation or explicit skip rationale | Skill Gate checklist |\n | Is a PRD required? | Planner | After Goal context stabilizes | Goal, `grill-with-docs` output, Context & Impact output | PRD required / not required | PRD checklist, CCA evidence check |\n | Can the spec be frozen? | Operator only | End of Requirements Definition, before issue split | PRD/requirements summary, scope inventory (SF-02), open-question list | `spec_freeze` block in the plan + ledger evidence | `require_grilled_plan` (plan ingest/run), SF checklist |\n | Can a frozen spec change? | Operator only (Spec Amendment) | When implementation contradicts the frozen spec | Contested decisions, scoped re-grill result | recorded amendment + re-stamped freeze | SF checklist, ledger evidence |\ndiff --git a/docs/agents/domain.md b/docs/agents/domain.md\nindex 258da8ea81e87ac0a6b0b5b5e5c2fde980eaabb7..d57b0cf17ff3e1fa9e6e5885372bf083d8295421\n--- a/docs/agents/domain.md\n+++ b/docs/agents/domain.md\n@@ -5,7 +5,9 @@\n ## Layout\n \n - `CONTEXT.md` defines Shiki language. Keep it implementation-free.\n-- `docs/adr/` records hard-to-reverse decisions.\n+- `docs/adr/SADR-NNNN-*.md` records Shiki platform decisions.\n+- `docs/adr/NNNN-*.md` records target product decisions without sharing the\n+  SADR namespace.\n - `AGENTS.md` is the runtime-neutral agent constitution.\n - `CLAUDE.md` is the Claude Code wrapper around `AGENTS.md`.\n - `.shiki/` is the Target Repository mirror for Goals, PRDs, plans, Task DAGs, contracts, locks, worktree records, ledger entries, CCA verdicts, repair packets, reports, and handoffs.\n@@ -15,6 +17,6 @@\n \n - Use Shiki terms exactly when they exist.\n - Add glossary terms to `CONTEXT.md` only when the term is part of the domain, not a generic programming concept.\n-- Create ADRs only for hard-to-reverse, surprising, tradeoff-based decisions.\n+- Create SADRs or target ADRs only for hard-to-reverse, surprising, tradeoff-based decisions.\n - Keep product-specific language in the Target Repository, not in Shiki Template docs.\n-- If an ADR conflicts with a proposed implementation, surface the conflict before editing.\n+- If a SADR or target ADR conflicts with a proposed implementation, surface the conflict before editing.\ndiff --git a/docs/agents/execution-control.md b/docs/agents/execution-control.md\nindex ad8ee3cdb36be56fedc1f46408ca92b387d74047..bc63c3a73661d12b567016ba299f1a6b66bf9088\n--- a/docs/agents/execution-control.md\n+++ b/docs/agents/execution-control.md\n@@ -63,7 +63,7 @@\n | Branch name | Orchestrator | Orchestrator | Before implementer dispatch | branch policy | branch ref in task/ledger |\n | Lock reservation | Context & Impact | MergeGate | Before dispatch and before merge | `.shiki/locks/`, labels, PR metadata | lock id, owner, scope |\n | CI profile | Planner | MergeGate | During issue triage, rechecked at PR | labels, `.shiki/policy.yaml`, workflow matrix | required check list |\n-| Required status checks | Repo maintainer / Guardian | GitHub branch protection / rulesets | Repository setup, updated via ADR | GitHub settings | branch/ruleset config, ADR |\n+| Required status checks | Repo maintainer / Guardian | GitHub branch protection / rulesets | Repository setup, updated via target ADR | GitHub settings | branch/ruleset config, target ADR |\n | CCA judgment | CCA | GitHub required check / MergeGate | Every non-draft PR update | GitHub Action check | structured CCA verdict |\n | Repair assignment | CCA | MergeGate | After failed CCA/CI/review | repair packet, issue/PR comment | repair id and attempt count |\n | Merge readiness | MergeGate | GitHub branch protection / Guardian if needed | After CCA complete | required checks, review rules, rulesets | mergegate check output |\ndiff --git a/docs/agents/guardian-policy.md b/docs/agents/guardian-policy.md\nindex 4147c7adf05a47adb4fd24ada1a1c2e1f2ba6d2d..a20d9cc6c00a569bb057b9ca8d6b51c7fc4a9847\n--- a/docs/agents/guardian-policy.md\n+++ b/docs/agents/guardian-policy.md\n@@ -98,8 +98,8 @@\n `origin/<default_branch>` and reuses each job's existing base fetch; a shallow\n (`--depth`) or second base fetch would make the proof unresolvable and silently\n disable the carry, so neither is permitted. CCA reads `guardian_comment_carried` as\n-a recorded authority in the deterministic signal and does not re-derive it. See ADR\n-0018 for the full decision, the rejected diff/patch-equality and path-disjointness\n+a recorded authority in the deterministic signal and does not re-derive it. See\n+SADR-0018 for the full decision, the rejected diff/patch-equality and path-disjointness\n alternatives, and the accepted residual.\n \n ## External AI Guardian Review (SADR-0010)\ndiff --git a/docs/agents/implementation-policy.md b/docs/agents/implementation-policy.md\nindex c754144a9fb5bf4a38aa82a60e1670c59e626b44..85c37281bcad196c6b7b02343faf00b03cd9c57a\n--- a/docs/agents/implementation-policy.md\n+++ b/docs/agents/implementation-policy.md\n@@ -31,10 +31,13 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n ## Non-Negotiables\n \n 1. **Planning precedes implementation.** Non-trivial Goals start with `grill-with-docs` and Context & Impact.\n-2. **Domain language is controlled.** Terms must be checked against `CONTEXT.md`; hard-to-reverse tradeoffs must be checked against ADRs.\n+2. **Domain language is controlled.** Terms must be checked against `CONTEXT.md`; hard-to-reverse tradeoffs must be checked against Shiki SADRs and target ADRs.\n 3. **PRD precedes issue decomposition.** `to-prd` is used once the context is settled enough to become durable intent.\n 4. **Issues are vertical slices.** `to-issues` must produce independently grabbable tracer-bullet tasks, not horizontal layer tickets.\n 5. **Triage controls dispatch.** Only `ready-for-agent` / AFK tasks may be assigned to Codex without human input.\n@@ -95,19 +98,19 @@\n Process:\n \n - Challenge terms against `CONTEXT.md`.\n-- Check ADRs before reopening decisions.\n+- Check SADRs and target ADRs before reopening decisions.\n - Inspect code when code can answer a question.\n - Ask one question at a time when the operator is available.\n - Provide a recommended answer with each question.\n - Record resolved terms in `CONTEXT.md`.\n-- Create ADRs only for hard-to-reverse, surprising, tradeoff-based decisions.\n+- Create SADRs or target ADRs only for hard-to-reverse, surprising, tradeoff-based decisions.\n - Convert unresolved load-bearing questions into HITL blockers.\n \n Outputs:\n \n - Resolved design-tree decisions.\n - Updated glossary terms when needed.\n-- ADRs when justified.\n+- SADRs or target ADRs when justified.\n - HITL blockers when decisions remain unresolved.\n \n ## Phase 3 \u2014 Context & Impact\n@@ -117,7 +120,7 @@\n \n Outputs must include:\n \n-- Relevant documents and ADRs.\n+- Relevant documents, SADRs, and target ADRs.\n - Relevant modules, interfaces, seams, callers, and tests.\n - Candidate locks.\n - Dependencies.\n@@ -140,7 +143,7 @@\n - Testing decisions.\n - Out of scope.\n - Further notes.\n-- Links to relevant Goal, domain terms, and ADRs.\n+- Links to relevant Goal, domain terms, SADRs, and target ADRs.\n \n Phase 4 ends with Spec Freeze: the operator explicitly approves the PRD, the\n plan records a `spec_freeze` block (status, approved_by, source), and the SF\n@@ -240,7 +243,7 @@\n - Dependencies and locks.\n - Risk and Guardian requirements.\n - Ledger completeness.\n-- Documentation and ADR updates.\n+- Documentation and SADR/target-ADR updates.\n - Scope drift.\n \n CCA emits one of:\n@@ -301,7 +304,7 @@\n | Evidence failure | Behavior might be done, but proof is missing. | Add checks, PR evidence, ledger entry, or artifacts. |\n | Scope drift | PR changes unrelated behavior. | Revert unrelated changes or split into new Goal. |\n | Dependency block | Upstream task or lock not complete. | Block until dependency resolves. |\n-| Architecture block | Decision needs ADR or deep-module discussion. | `grill-with-docs` or `improve-codebase-architecture`. |\n+| Architecture block | Decision needs a SADR, target ADR, or deep-module discussion. | `grill-with-docs` or `improve-codebase-architecture`. |\n | Risk block | Security/production/policy change needs human approval. | Guardian decision. |\n \n ## Phase 5.5 \u2014 Execution Decision Control\ndiff --git a/docs/agents/runtime-auth-model.md b/docs/agents/runtime-auth-model.md\nindex 6a2e1a47e81ea89b5c68a210ec07772a9374babc..3c54748d24233dbd0ba8473950282547ef4ce935\n--- a/docs/agents/runtime-auth-model.md\n+++ b/docs/agents/runtime-auth-model.md\n@@ -148,7 +148,7 @@\n \n API-key based runners may be added by a target repository as an explicit extension. They are not the default Shiki template path.\n \n-If a repo opts into API-key mode, record the exception in an ADR and update the repo's `AGENTS.md`, `.shiki/config.yaml`, and workflow permissions.\n+If a repo opts into API-key mode, record the target-specific exception in a target ADR and update the repo's `AGENTS.md`, `.shiki/config.yaml`, and workflow permissions.\n \n ## GitHub Actions Runtime Compatibility\n \ndiff --git a/docs/agents/skill-gate.md b/docs/agents/skill-gate.md\nindex c50398b0d7c60a8598f2a5c88a10015730dced13..3acffe6444617bb1d54b77d4b4708158ec735bc6\n--- a/docs/agents/skill-gate.md\n+++ b/docs/agents/skill-gate.md\n@@ -9,7 +9,7 @@\n | Skill | Trigger | Expected output |\n | --- | --- | --- |\n | `setup-matt-pocock-skills` | First configuring a repo for these skills, or when issue tracker, triage labels, or domain docs are missing. | `docs/agents/` configuration and an `Agent skills` block in `AGENTS.md` or `CLAUDE.md`. |\n-| `grill-with-docs` | Ambiguous plan, unclear terminology, boundary decision, tradeoff, or ADR-worthy design choice. | Clarified decision tree, recommended answers, updates to `CONTEXT.md` or ADRs when decisions crystallize. |\n+| `grill-with-docs` | Ambiguous plan, unclear terminology, boundary decision, tradeoff, or decision-record-worthy design choice. | Clarified decision tree, recommended answers, updates to `CONTEXT.md`, SADRs, or target ADRs when decisions crystallize. |\n | `zoom-out` | Agent lacks a higher-level architectural map. | Map of relevant modules, callers, seams, and domain concepts. |\n | `to-prd` | Settled context should become a PRD. | PRD published to the issue tracker with problem, solution, user stories, decisions, testing, and out-of-scope notes. |\n | `to-issues` | Goal, PRD, or plan must become implementation tickets. | Vertical-slice issues with acceptance criteria, dependencies, and AFK/HITL classification. |\n@@ -17,7 +17,7 @@\n | `tdd` | Feature work or bug fix has observable behavior that can be specified with tests. | Red-green-refactor loop through public interfaces, one vertical slice at a time. |\n | `code-review` | The TDD loop is green and the implementer is about to create or update a PR. Default for every implementation task. | Pre-PR self-review of the task diff with fixes applied on the task branch; findings and resolutions recorded as a ledger entry naming the skill and a `## Pre-PR code review` PR body section. |\n | `diagnose` | Hard bug, failing check, regression, flaky behavior, or performance problem. | Reproduction loop, ranked hypotheses, instrumentation, fix, regression test, post-mortem. |\n-| `improve-codebase-architecture` | Structural friction, testability, AI-navigability, deep-module opportunity, or refactor request. | Architecture candidates and deepening recommendations informed by `CONTEXT.md` and ADRs. |\n+| `improve-codebase-architecture` | Structural friction, testability, AI-navigability, deep-module opportunity, or refactor request. | Architecture candidates and deepening recommendations informed by `CONTEXT.md`, SADRs, and target ADRs. |\n | `prototype` | A throwaway logic or UI prototype is needed to answer a design question. | Clearly marked disposable prototype and captured decision. |\n \n ## MergeGate Interaction\ndiff --git a/docs/operations.md b/docs/operations.md\nindex cf1f452d4c589e38ccd58553bda0ad8684b51eb7..ace48a90ffbec9e5c0a7113bef16a4858bc44b3f\n--- a/docs/operations.md\n+++ b/docs/operations.md\n@@ -89,6 +89,16 @@\n      target history, brought forward by `shiki migrate`, not a template.\n    - **Everything else** (CLI scripts, workflows, schemas, docs) is overwritten.\n \n+   A forced upgrade also migrates the former numeric Shiki decision paths to\n+   `SADR-NNNN-*`. Before the first write, the installer inventories the exact\n+   legacy Shiki paths and authorizes each deletion only when the existing\n+   `.shiki/install-stamp.json` names that path and its SHA-256 digest still\n+   matches the file. An absent, unreadable, malformed, incomplete, or\n+   mismatching stamp reports every blocked path and stops with no filesystem\n+   change. Target-owned `NNNN-*` ADRs are not cleanup candidates. A non-force\n+   install deletes nothing and retains any still-matching legacy stamp entries\n+   so a later forced upgrade can prove ownership.\n+\n    If the target has pending migrations, `--force` refuses and writes nothing \u2014\n    apply migrations first (step 3), then re-run. The run ends with a single\n    summary of every `.new` written, so a half-upgrade is visible.\ndiff --git a/docs/releasing.md b/docs/releasing.md\nindex 10d73828e7dbd64e9d9f6b83bc84d010537ce62a..aafeee8c4d441e1e427efee2ce4f81a647f020e1\n--- a/docs/releasing.md\n+++ b/docs/releasing.md\n@@ -67,7 +67,7 @@\n CLI in `scripts/`. There are no third-party Python runtime dependencies. The CLI\n is intentionally **not** exposed as a `[project.scripts]` console entry point\n because `scripts/shiki.py` is a dependency-free shim and `scripts/` is not\n-packaged as an importable distribution (see the ADR for the rationale).\n+packaged as an importable distribution (see SADR-0007 for the rationale).\n \n Supported install paths:\n \n@@ -155,6 +155,15 @@\n    [docs/operations.md](operations.md#the-new-convention). If the target has\n    pending migrations, `--force` refuses and writes nothing; apply migrations\n    (step 2) first, then re-run.\n+\n+   The SADR namespace upgrade has an additional pre-write gate: each former\n+   numeric Shiki decision path is removed only when the existing install stamp\n+   contains that exact path and a digest matching its current bytes. The\n+   installer aggregates absent/unreadable/malformed-stamp, missing-digest, and\n+   digest-mismatch blockers before raising, so refusal leaves the target tree\n+   byte-identical. Numeric target ADRs are preserved. A successful forced\n+   upgrade installs and stamps `SADR-*` paths and drops the removed legacy paths\n+   from the refreshed stamp.\n 2. Apply repository-local `.shiki/` state migrations with the migrate command:\n \n    ```bash\ndiff --git a/docs/usage.md b/docs/usage.md\nindex b83407ee3970dd395dd893010bc5dbe8bad26051..a130ef6e40dfd8169a5bc701339351097df1a09f\n--- a/docs/usage.md\n+++ b/docs/usage.md\n@@ -67,8 +67,10 @@\n shiki install-target /path/to/target-repo --local-only\n ```\n \n-Use `--force` only when you intentionally want to overwrite existing target\n-files.\n+Use `--force` only when you intentionally want to refresh the target template.\n+For the SADR namespace migration, `--force` deletes only exact legacy Shiki\n+decision paths whose existing install-stamp digest still matches; any ownership\n+blocker stops before the first write. Numeric target ADRs are never candidates.\n \n ## `shiki bootstrap-platform` \u2014 publish the Shiki platform repo\n \ndiff --git a/scripts/guardian_approval_signal.py b/scripts/guardian_approval_signal.py\nindex d059f92b2c82aa2b5d6ca421365537a87886c1cb..16924892714f4f5b9303c0ef0e325a7e70075630\n--- a/scripts/guardian_approval_signal.py\n+++ b/scripts/guardian_approval_signal.py\n@@ -199,8 +199,8 @@\n ) -> Any:\n     \"\"\"Evaluate SADR-0015 Contract Approval for the CCA Guardian signal.\n \n-    The SAME OR that MergeGate applies must reach this deterministic signal: ADR\n-    0015 is explicit that without it, a contract-approved PR still returns\n+    The SAME OR that MergeGate applies must reach this deterministic signal:\n+    SADR-0015 is explicit that without it, a contract-approved PR still returns\n     ``needs_guardian``, the CCA job fails, and the MergeGate policy check never\n     runs \u2014 the gate this change moves would never be reached. This resolves the\n     task the SAME way MergeGate does (the first Shiki task id in the PR body),\ndiff --git a/scripts/shiki_contract_approval.py b/scripts/shiki_contract_approval.py\nindex 941bcb96fdd8b9a85bc0c4f0f04d5dad9d04839c..4a4b418f6527413161db82670bec857a043360e2\n--- a/scripts/shiki_contract_approval.py\n+++ b/scripts/shiki_contract_approval.py\n@@ -142,7 +142,7 @@\n     changed_files_status: Any,\n     registration: Any,\n ) -> ContractApprovalResult:\n-    \"\"\"Whether this PR carries an SADR-0015 Contract Approval.\n+    \"\"\"Whether this PR carries a SADR-0015 Contract Approval.\n \n     The four conditions are evaluated IN ORDER, each failure recorded as a\n     distinct reason string:\ndiff --git a/scripts/shiki_guardian.py b/scripts/shiki_guardian.py\nindex a4ecb79bbda55fd5c8b06adfae2eb99760a03c45..4dd99bbe714e26f421fa19abac7a34ae1b195ecc\n--- a/scripts/shiki_guardian.py\n+++ b/scripts/shiki_guardian.py\n@@ -12,7 +12,7 @@\n # The carry re-derives its proof from git in-process on every judging invocation;\n # it is never read from an artifact, so there is nothing to go stale or be forged.\n # shiki_sync_proof is the ONLY guardian-family module that runs git, and every one\n-# of its invocations is read-only and local (ADR: prove-from-git base sync).\n+# of its invocations is read-only and local (SADR: prove-from-git base sync).\n from shiki_sync_proof import verify_pure_base_sync\n \n GUARDIAN_POLICY_PATH = \".shiki/guardian-policy.json\"\ndiff --git a/scripts/shiki_guardian_review.py b/scripts/shiki_guardian_review.py\nindex dd0f104d457899e0ba5ff1e162011c1e90b6f92b..e10ef58c046324e7ea3a3392feb3048ca77414ce\n--- a/scripts/shiki_guardian_review.py\n+++ b/scripts/shiki_guardian_review.py\n@@ -159,7 +159,7 @@\n     docs_only = _is_docs_only(changed_files)\n     if docs_only and matched == 0:\n         focus.append(\n-            \"Docs/ADR-only change: confirm the change is genuinely documentation/decision-only, introduces no behavior or contract drift, and stays consistent with the constitution and existing ADRs.\"\n+            \"Docs/decision-record-only change: confirm the change is genuinely documentation/decision-only, introduces no behavior or contract drift, and stays consistent with the constitution and existing SADRs and target ADRs.\"\n         )\n \n     # Conservative augmentation: unknown (no category and not docs-only), mixed\ndiff --git a/scripts/shiki_installer.py b/scripts/shiki_installer.py\nindex be064ab7a29ff6836f7f7a1d70500a43e0d11d7b..9fd8eefd137ce7003dad5c0dcb2e48e2cd47ab32\n--- a/scripts/shiki_installer.py\n+++ b/scripts/shiki_installer.py\n@@ -118,6 +118,7 @@\n # what lets a target carry it without a governance-schema change.\n INSTALL_STAMP_PATH = \".shiki/install-stamp.json\"\n INSTALL_STAMP_VERSION = 1\n+LEGACY_SHIKI_SADR_MAX = 18\n \n # Under --force the shipped surface splits three ways, so nothing is lost and\n # nothing is silently stale.\n@@ -237,9 +238,98 @@\n         )\n \n \n+def legacy_shiki_adr_paths() -> tuple[str, ...]:\n+    \"\"\"Former numeric paths for Shiki SADR-0001 through SADR-0018.\n+\n+    The authoritative slugs come from the current SADR filenames. SADR-0019 is\n+    the namespace decision itself and never shipped under a numeric legacy path.\n+    \"\"\"\n+    paths: list[str] = []\n+    for sadr in sorted((ROOT / \"docs\" / \"adr\").glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\")):\n+        number = sadr.name.split(\"-\", 2)[1]\n+        if int(number) <= LEGACY_SHIKI_SADR_MAX:\n+            paths.append(f\"docs/adr/{sadr.name.removeprefix('SADR-')}\")\n+    return tuple(paths)\n+\n+\n+def preflight_legacy_sadr_cleanup(target: Path, *, force: bool) -> tuple[str, ...]:\n+    \"\"\"Authorize exact legacy Shiki ADR removals before the first write.\n+\n+    A non-force install never removes existing files. A forced install needs no\n+    stamp when no legacy Shiki path is present. When legacy paths do exist, each\n+    must be named in a readable install stamp and its current bytes must match\n+    the recorded digest.\n+    \"\"\"\n+    if not force:\n+        return ()\n+    present = tuple(\n+        relative\n+        for relative in legacy_shiki_adr_paths()\n+        if (target / relative).exists() or (target / relative).is_symlink()\n+    )\n+    if not present:\n+        return ()\n+\n+    stamp_path = target / INSTALL_STAMP_PATH\n+    try:\n+        raw = stamp_path.read_text(encoding=\"utf-8\")\n+        stamp = json.loads(raw)\n+    except FileNotFoundError:\n+        detail = \"install stamp is absent\"\n+        blockers = [f\"{relative}: {detail}\" for relative in present]\n+    except (OSError, UnicodeDecodeError) as error:\n+        detail = f\"install stamp is unreadable ({error})\"\n+        blockers = [f\"{relative}: {detail}\" for relative in present]\n+    except json.JSONDecodeError as error:\n+        detail = f\"install stamp is malformed ({error})\"\n+        blockers = [f\"{relative}: {detail}\" for relative in present]\n+    else:\n+        if not isinstance(stamp, dict):\n+            blockers = [\n+                f\"{relative}: install stamp is malformed (expected an object)\"\n+                for relative in present\n+            ]\n+        else:\n+            digests = stamp.get(\"digests\")\n+            blockers = []\n+            if not isinstance(digests, dict):\n+                blockers.extend(\n+                    f\"{relative}: install stamp has no digest for this path\"\n+                    for relative in present\n+                )\n+            else:\n+                for relative in present:\n+                    expected = digests.get(relative)\n+                    if not isinstance(expected, str) or not expected:\n+                        blockers.append(\n+                            f\"{relative}: install stamp has no digest for this path\"\n+                        )\n+                        continue\n+                    try:\n+                        actual = _sha256_file(target / relative)\n+                    except OSError as error:\n+                        blockers.append(\n+                            f\"{relative}: legacy path is unreadable ({error})\"\n+                        )\n+                        continue\n+                    if actual != expected:\n+                        blockers.append(\n+                            f\"{relative}: digest mismatch (stamp {expected}, current {actual})\"\n+                        )\n+\n+    if blockers:\n+        joined = \"\\n\".join(f\"- {blocker}\" for blocker in blockers)\n+        raise ShikiError(\n+            \"legacy Shiki ADR cleanup is not authorized:\\n\"\n+            f\"{joined}\\nNo files were written.\"\n+        )\n+    return present\n+\n+\n def install_template(target: Path, *, force: bool, validate: bool) -> list[NewFileNote]:\n     # Preconditions first: never rewrite the tree of a target that cannot be\n     # upgraded cleanly (see refuse_pending_migrations).\n+    legacy_cleanup = preflight_legacy_sadr_cleanup(target, force=force)\n     refuse_pending_migrations(target)\n \n     # Resolve the target's required CODEOWNERS owner once from its\n@@ -250,6 +340,10 @@\n     # verbatim. See ``codeowners_required_owner``.\n     codeowners_owner = codeowners_required_owner(target)\n \n+    for relative in legacy_cleanup:\n+        (target / relative).unlink()\n+        info(f\"removed stamped legacy Shiki ADR: {target / relative}\")\n+\n     notes: list[NewFileNote] = []\n     for relative in TEMPLATE_PATHS:\n         source = ROOT / relative\n@@ -647,6 +741,22 @@\n     digests = target_stamp_digests(target)\n     existing = read_install_stamp(target)\n     if existing is not None:\n+        # A non-force install deliberately leaves legacy Shiki ADRs in place.\n+        # Preserve only still-matching ownership proof so a later forced\n+        # upgrade can authorize cleanup; never carry a missing or stale digest.\n+        existing_digests = existing.get(\"digests\")\n+        if isinstance(existing_digests, dict):\n+            for relative in legacy_shiki_adr_paths():\n+                expected = existing_digests.get(relative)\n+                path = target / relative\n+                if not isinstance(expected, str) or not path.is_file():\n+                    continue\n+                try:\n+                    actual = _sha256_file(path)\n+                except OSError:\n+                    continue\n+                if actual == expected:\n+                    digests[relative] = actual\n         if _incoming_is_older(commit, existing.get(\"platform_commit\")):\n             warn(\n                 \"kept existing install stamp: incoming install \"\ndiff --git a/scripts/shiki_loop.py b/scripts/shiki_loop.py\nindex 3611641da8aeb5038b1da90e3114b09a473ec6d8..84caa491b9cad0edc4176f209c13e9221e975681\n--- a/scripts/shiki_loop.py\n+++ b/scripts/shiki_loop.py\n@@ -424,7 +424,7 @@\n         # unresolvable CCA verdict has already produced its terminal stop and is never\n         # laundered into a head-moving sync (SADR-0010). It also never fires while the\n         # policy (Guardian) gate is red \u2014 for ANY reason, not only when it is the sole\n-        # red check \u2014 so a head-bound Guardian approval, or an SADR-0015\n+        # red check \u2014 so a head-bound Guardian approval, or a SADR-0015\n         # contract-approval carry that base movement defeated (the policy gate compares\n         # base and head task governance directly), is never silently restored by\n         # adopting base wholesale. It is bounded by a durable per-task counter so the\ndiff --git a/scripts/shiki_tasks.py b/scripts/shiki_tasks.py\nindex aec3809f24b4a0c8c96fc50971cff9ae9ad30381..d531052e9d9717058d4631483cefab44c26a65c1\n--- a/scripts/shiki_tasks.py\n+++ b/scripts/shiki_tasks.py\n@@ -821,7 +821,7 @@\n         \"prompt\": prompt,\n         \"entry_skill\": \"grill-with-docs\",\n         \"required_next_steps\": [\n-            \"Run grill-with-docs until terminology, boundaries, risks, and ADR-worthy decisions are settled.\",\n+            \"Run grill-with-docs until terminology, boundaries, risks, and decision-record-worthy choices are settled.\",\n             \"Obtain the operator's explicit approval of the PRD/requirements (Spec Freeze).\",\n             \"Write a machine-readable plan JSON with grill_with_docs.status=complete and spec_freeze.status=frozen.\",\n             \"Run shiki plan ingest --plan-file PLAN.json.\",\ndiff --git a/scripts/test_shiki_goal_loop.sh b/scripts/test_shiki_goal_loop.sh\nindex 753c7b516e10f0be3bc92a4bd0eb1fa699776ae6..8f3820a0a079b3044424cfe1a6c6b7fe68896e67\n--- a/scripts/test_shiki_goal_loop.sh\n+++ b/scripts/test_shiki_goal_loop.sh\n@@ -231,8 +231,8 @@\n \n # High/critical risk merges autonomously when all required checks are green:\n # the \"MergeGate policy check\" required check IS the Guardian gate (it enforces\n-# guardian-policy.json \u2014 human approval OR external AI guardian review, ADR\n-# 0010), so green checks mean a recorded authority approved. The fake gh returns\n+# guardian-policy.json \u2014 human approval OR external AI guardian review,\n+# SADR-0010), so green checks mean a recorded authority approved. The fake gh returns\n # green checks, simulating that the Guardian gate passed.\n python3 \"$ROOT/scripts/shiki.py\" goal create --target \"$TARGET\" --title \"Guardian gate\" --outcome \"High risk merges via policy gate\" >/tmp/shiki-goal-loop-guardian-goal.json\n GGOAL=\"$(json_get /tmp/shiki-goal-loop-guardian-goal.json goal_id)\"\ndiff --git a/tests/test_adr_references.py b/tests/test_adr_references.py\nindex 51706329c797d5dd9b47e082a333fa83b261d482..db6e693e35f67103935be34f1d9bb4ff7ee60395\n--- a/tests/test_adr_references.py\n+++ b/tests/test_adr_references.py\n@@ -1,7 +1,7 @@\n-\"\"\"Every ``ADR ####`` citation must resolve to exactly one decision record.\n+\"\"\"Every target ADR and Shiki SADR citation resolves in its own namespace.\n \n Background. ``scripts/mergegate_check.py`` justifies ``is_bookkeeping_closeout``\n-with \"ADR 0017\" nine times and ``scripts/guardian_approval_signal.py`` cites\n+with \"SADR-0017\" nine times and ``scripts/guardian_approval_signal.py`` cites\n 0016/0017 seven times, yet for a stretch the numbering in ``docs/adr/`` stopped\n at 0015: the authoring commits lived on a coordinator branch and no task ever\n declared ``docs/adr/...`` in its locks, so nothing carried the records to the\n@@ -11,11 +11,12 @@\n and the directory drifted with no signal.\n \n This is the same unbound-pair shape behind other silent failures: a reference\n-and its target with nothing asserting they agree. This test binds them. It scans\n-every ``scripts/*.py`` (plus ``docs/**/*.md`` and the root ``*.md`` governance\n-files, which are cheap) for the ``ADR ####`` pattern and asserts each cited\n-number resolves to exactly one ``docs/adr/####-*.md``. The converse is\n-deliberately NOT required: an ADR may exist without being cited.\n+and its target with nothing asserting they agree. This test binds them. Target\n+``ADR NNNN`` citations resolve only to ``docs/adr/NNNN-*.md``; Shiki\n+``SADR-NNNN`` citations resolve only to ``docs/adr/SADR-NNNN-*.md``. The scan\n+covers current governance docs, scripts, workflows, prompts, packaging metadata,\n+and repo-local Codex guidance while deliberately excluding historical\n+``.shiki`` evidence. The converse is not required: a record may be uncited.\n \n The suite is dependency free: standard-library ``unittest`` only.\n \"\"\"\n@@ -31,17 +32,18 @@\n \n REPO_ROOT = Path(__file__).resolve().parents[1]\n \n-# \"ADR 0016\" -- a four-digit citation. The trailing \\b keeps a five-digit token\n-# from being read as a truncated four-digit number.\n-_CITATION = re.compile(r\"\\bADR (\\d{4})\\b\")\n-# \"0016-locks-bound-product-paths.md\" -- the leading four digits name the record.\n-_ADR_FILENAME = re.compile(r\"^(\\d{4})-.*\\.md$\")\n+# ``ADR 0016`` / ``SADR-0016`` -- a namespace plus four-digit citation. The\n+# trailing ``\\b`` keeps a five-digit token from being read as a truncated one.\n+_CITATION = re.compile(r\"\\b(S?ADR)[ -](\\d{4})\\b\")\n+# Target ADRs retain the numeric filename; Shiki platform decisions use SADR.\n+_DECISION_FILENAME = re.compile(r\"^(?:(SADR)-)?(\\d{4})-.*\\.md$\")\n \n \n class Violation(NamedTuple):\n     \"\"\"A citation that does not resolve to exactly one decision record.\"\"\"\n \n     citing_file: str  # repo-relative path of the file that cites the ADR\n+    namespace: str  # \"ADR\" for target decisions; \"SADR\" for Shiki decisions\n     number: str  # the cited four-digit number, e.g. \"0016\"\n     line: int  # 1-based line of the citation\n     reason: str  # \"missing\" (zero matches) or \"ambiguous\" (two or more)\n@@ -49,44 +51,59 @@\n \n     @property\n     def expected_path(self) -> str:\n-        return f\"docs/adr/{self.number}-*.md\"\n+        prefix = \"SADR-\" if self.namespace == \"SADR\" else \"\"\n+        return f\"docs/adr/{prefix}{self.number}-*.md\"\n \n     def describe(self) -> str:\n         \"\"\"Name the citing file, the cited number, and the missing path.\"\"\"\n         if self.reason == \"missing\":\n             return (\n-                f\"{self.citing_file}:{self.line} cites ADR {self.number} \"\n+                f\"{self.citing_file}:{self.line} cites {self.namespace} {self.number} \"\n                 f\"but no document matches {self.expected_path}\"\n             )\n         joined = \", \".join(self.matches)\n         return (\n-            f\"{self.citing_file}:{self.line} cites ADR {self.number} which \"\n+            f\"{self.citing_file}:{self.line} cites {self.namespace} {self.number} which \"\n             f\"resolves to {len(self.matches)} documents (expected exactly one \"\n             f\"at {self.expected_path}): {joined}\"\n         )\n \n \n-def _adr_index(root: Path) -> dict[str, list[Path]]:\n-    \"\"\"Map every four-digit ADR number to the record files that claim it.\"\"\"\n-    index: dict[str, list[Path]] = {}\n+def _adr_index(root: Path) -> dict[tuple[str, str], list[Path]]:\n+    \"\"\"Map each decision namespace and number to the files that claim it.\"\"\"\n+    index: dict[tuple[str, str], list[Path]] = {}\n     adr_dir = root / \"docs\" / \"adr\"\n     if adr_dir.is_dir():\n         for path in sorted(adr_dir.glob(\"*.md\")):\n-            match = _ADR_FILENAME.match(path.name)\n+            match = _DECISION_FILENAME.match(path.name)\n             if match:\n-                index.setdefault(match.group(1), []).append(path)\n+                namespace = \"SADR\" if match.group(1) else \"ADR\"\n+                index.setdefault((namespace, match.group(2)), []).append(path)\n     return index\n \n \n def _citing_files(root: Path, include_docs: bool = True) -> list[Path]:\n-    \"\"\"Files scanned for citations: scripts/*.py, and cheaply the markdown.\"\"\"\n+    \"\"\"Current, shipped text surfaces scanned for decision citations.\"\"\"\n     files = sorted((root / \"scripts\").glob(\"*.py\"))\n+    files += sorted((root / \"scripts\").glob(\"*.sh\"))\n     if include_docs:\n         docs = root / \"docs\"\n         if docs.is_dir():\n             files += sorted(docs.rglob(\"*.md\"))\n         files += sorted(root.glob(\"*.md\"))\n-    return files\n+        pyproject = root / \"pyproject.toml\"\n+        if pyproject.is_file():\n+            files.append(pyproject)\n+        for relative in (\".github\", \".codex\"):\n+            surface = root / relative\n+            if surface.is_dir():\n+                files += sorted(\n+                    path\n+                    for path in surface.rglob(\"*\")\n+                    if path.is_file()\n+                    and path.suffix in {\".md\", \".py\", \".sh\", \".toml\", \".yaml\", \".yml\"}\n+                )\n+    return sorted(set(files))\n \n \n def find_adr_reference_violations(\n@@ -94,10 +111,9 @@\n ) -> list[Violation]:\n     \"\"\"Return every ``ADR ####`` citation under ``root`` that fails to resolve.\n \n-    A citation resolves when exactly one ``docs/adr/####-*.md`` matches its\n-    number. Zero matches is a \"missing\" violation; two or more is \"ambiguous\".\n-    Records that are never cited produce nothing -- the converse is not\n-    required.\n+    A citation resolves when exactly one record in the same namespace matches\n+    its number. Zero matches is a \"missing\" violation; two or more is\n+    \"ambiguous\". Records that are never cited produce nothing.\n     \"\"\"\n     root = Path(root)\n     index = _adr_index(root)\n@@ -110,13 +126,15 @@\n         rel = path.relative_to(root).as_posix()\n         for lineno, line in enumerate(text.splitlines(), start=1):\n             for match in _CITATION.finditer(line):\n-                number = match.group(1)\n-                found = index.get(number, [])\n+                namespace, number = match.groups()\n+                found = index.get((namespace, number), [])\n                 if len(found) == 1:\n                     continue\n                 reason = \"missing\" if not found else \"ambiguous\"\n                 rels = [p.relative_to(root).as_posix() for p in found]\n-                violations.append(Violation(rel, number, lineno, reason, rels))\n+                violations.append(\n+                    Violation(rel, namespace, number, lineno, reason, rels)\n+                )\n     return violations\n \n \n@@ -129,8 +147,8 @@\n         self.assertEqual(\n             violations,\n             [],\n-            \"Every 'ADR ####' citation must resolve to exactly one \"\n-            f\"docs/adr/####-*.md:\\n{detail}\",\n+            \"Every ADR/SADR citation must resolve exactly once in its own \"\n+            f\"docs/adr namespace:\\n{detail}\",\n         )\n \n     def test_scan_actually_reads_scripts(self) -> None:\n@@ -146,8 +164,21 @@\n         # The two records this Goal carried onto the default branch, and which\n         # the scripts cite most, must be present.\n         index = _adr_index(REPO_ROOT)\n-        self.assertIn(\"0016\", index)\n-        self.assertIn(\"0017\", index)\n+        self.assertIn((\"SADR\", \"0016\"), index)\n+        self.assertIn((\"SADR\", \"0017\"), index)\n+\n+    def test_platform_tree_has_only_sadr_0001_through_0019(self) -> None:\n+        adr_dir = REPO_ROOT / \"docs\" / \"adr\"\n+        sadrs = sorted(adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\"))\n+        self.assertEqual(\n+            {path.name.split(\"-\", 2)[1] for path in sadrs},\n+            {f\"{number:04d}\" for number in range(1, 20)},\n+        )\n+        self.assertEqual(\n+            list(adr_dir.glob(\"[0-9][0-9][0-9][0-9]-*.md\")),\n+            [],\n+            \"the Shiki platform tree must not retain legacy numeric SADR paths\",\n+        )\n \n \n class FixtureAdrReferences(unittest.TestCase):\n@@ -180,6 +211,26 @@\n         self.assertIn(\"9991\", message)  # cited number\n         self.assertIn(\"docs/adr/9991-*.md\", message)  # missing path\n \n+    def test_target_adr_does_not_satisfy_missing_platform_sadr(self) -> None:\n+        root = self._make_repo(\n+            adr_names=[\"0042-target-decision.md\"],\n+            script_body='\"\"\"Target ADR 0042 exists, but platform SADR-0042 does not.\"\"\"\\n',\n+        )\n+        violations = find_adr_reference_violations(root, include_docs=False)\n+        self.assertEqual(len(violations), 1)\n+        self.assertEqual(violations[0].namespace, \"SADR\")\n+        self.assertEqual(violations[0].number, \"0042\")\n+        self.assertEqual(violations[0].expected_path, \"docs/adr/SADR-0042-*.md\")\n+\n+    def test_target_adr_and_platform_sadr_may_reuse_a_number(self) -> None:\n+        root = self._make_repo(\n+            adr_names=[\"0042-target-decision.md\", \"SADR-0042-platform-decision.md\"],\n+            script_body='\"\"\"Target ADR 0042 and platform SADR-0042 are distinct.\"\"\"\\n',\n+        )\n+        self.assertEqual(\n+            find_adr_reference_violations(root, include_docs=False), []\n+        )\n+\n     def test_uncited_adr_is_allowed(self) -> None:\n         # An ADR exists but nothing cites it -> not a violation. The converse\n         # (every ADR must be cited) is explicitly NOT required.\ndiff --git a/tests/test_guardian_review.py b/tests/test_guardian_review.py\nindex 88829e7810212635e2fc98aa5918d6f26294677d..00a85a237acd5bf2d6cb5b294128198c422402ca\n--- a/tests/test_guardian_review.py\n+++ b/tests/test_guardian_review.py\n@@ -162,7 +162,7 @@\n \n     def test_docs_only_is_light_and_not_conservative(self):\n         focus = gr.classify_review_focus([\"docs/adr/0099-x.md\", \"README.md\"], \"\", \"low\")\n-        self.assertTrue(any(\"Docs/ADR-only\" in f for f in focus))\n+        self.assertTrue(any(\"Docs/decision-record-only\" in f for f in focus))\n         self.assertFalse(any(\"conservative default\" in f.lower() for f in focus))\n \n     def test_unknown_category_is_conservative(self):\ndiff --git a/tests/test_policy_job_merged_prs.py b/tests/test_policy_job_merged_prs.py\nindex d45512e74681e5e649279236c7d4e79d5c3da471..b97989f8bd74d091de1a6b5275a20d8dc68c4512\n--- a/tests/test_policy_job_merged_prs.py\n+++ b/tests/test_policy_job_merged_prs.py\n@@ -3,8 +3,8 @@\n \n ``.github/workflows/shiki-cca-completion.yml`` has two consumers of the merged-PR\n proof: the CCA *signal* (``guardian_approval_signal.py`` in \"Prepare CCA context\")\n-and the authoritative gate (``mergegate_check.py`` in \"Enforce MergeGate\"). ADR\n-0017's bookkeeping-closeout exemption (condition 6) needs the merged implementation\n+and the authoritative gate (``mergegate_check.py`` in \"Enforce MergeGate\").\n+SADR-0017's bookkeeping-closeout exemption (condition 6) needs the merged implementation\n PR proven, and a closeout carries NO ``post_merge_reconcile`` marker. The signal\n already resolves ``MERGED_PRS`` unconditionally, but \"Enforce MergeGate\" used to\n resolve it only inside ``grep -q '<!-- shiki:post_merge_reconcile -->'``. So a\ndiff --git a/tests/test_sadr_install_migration.py b/tests/test_sadr_install_migration.py\nnew file mode 100644\nindex 0000000000000000000000000000000000000000..a6f4c57e4dcaef73ef600b0d4804744138ab9071\n--- /dev/null\n+++ b/tests/test_sadr_install_migration.py\n@@ -0,0 +1,229 @@\n+\"\"\"SADR installation and collision-safe legacy upgrade behavior.\"\"\"\n+\n+from __future__ import annotations\n+\n+import contextlib\n+import hashlib\n+import io\n+import json\n+import shutil\n+import tempfile\n+import unittest\n+from pathlib import Path\n+\n+import shiki_test_support  # noqa: F401  (path bootstrap)\n+\n+import shiki_installer\n+from tests.test_adr_references import find_adr_reference_violations\n+\n+\n+PRODUCT_NUMBERS = tuple(range(13, 19))\n+\n+\n+def _quiet_install(target: Path, *, force: bool, validate: bool) -> None:\n+    with contextlib.redirect_stdout(io.StringIO()):\n+        shiki_installer.install_template(target, force=force, validate=validate)\n+\n+\n+def _sha256(path: Path) -> str:\n+    return hashlib.sha256(path.read_bytes()).hexdigest()\n+\n+\n+def _legacy_sadr_paths() -> tuple[str, ...]:\n+    return tuple(\n+        f\"docs/adr/{path.name.removeprefix('SADR-')}\"\n+        for path in sorted((shiki_installer.ROOT / \"docs\" / \"adr\").glob(\"SADR-*.md\"))\n+        if path.name != \"SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\"\n+    )\n+\n+\n+def _create_legacy_upgrade_fixture(\n+    target: Path,\n+) -> tuple[dict[str, bytes], tuple[str, ...]]:\n+    adr_dir = target / \"docs\" / \"adr\"\n+    adr_dir.mkdir(parents=True)\n+    product_bytes = {\n+        f\"{number:04d}-cubeshare-product-decision.md\": (\n+            f\"# CubeShare ADR {number:04d}\\n\\nDo not rewrite {number}.\\n\"\n+        ).encode()\n+        for number in PRODUCT_NUMBERS\n+    }\n+    for name, content in product_bytes.items():\n+        (adr_dir / name).write_bytes(content)\n+\n+    legacy_paths = _legacy_sadr_paths()\n+    for relative in legacy_paths:\n+        (target / relative).write_bytes(f\"legacy Shiki bytes for {relative}\\n\".encode())\n+\n+    stamp_path = target / shiki_installer.INSTALL_STAMP_PATH\n+    stamp_path.parent.mkdir(parents=True, exist_ok=True)\n+    stamp = {\n+        \"version\": shiki_installer.INSTALL_STAMP_VERSION,\n+        \"platform_commit\": None,\n+        \"installed_at\": \"2000-01-01T00:00:00+00:00\",\n+        \"digests\": {\n+            relative: _sha256(target / relative) for relative in legacy_paths\n+        },\n+    }\n+    stamp_path.write_text(\n+        json.dumps(stamp, indent=2, sort_keys=True) + \"\\n\", encoding=\"utf-8\"\n+    )\n+    return product_bytes, legacy_paths\n+\n+\n+def _tree_snapshot(root: Path) -> dict[str, tuple[str, bytes | str]]:\n+    \"\"\"Recursive byte/shape snapshot used to prove preflight is write-free.\"\"\"\n+    snapshot: dict[str, tuple[str, bytes | str]] = {}\n+    for path in sorted(root.rglob(\"*\")):\n+        relative = path.relative_to(root).as_posix()\n+        if path.is_symlink():\n+            snapshot[relative] = (\"symlink\", path.readlink().as_posix())\n+        elif path.is_dir():\n+            snapshot[relative] = (\"directory\", \"\")\n+        else:\n+            snapshot[relative] = (\"file\", path.read_bytes())\n+    return snapshot\n+\n+\n+class FreshSadrInstallTests(unittest.TestCase):\n+    def setUp(self) -> None:\n+        self.tmp = Path(tempfile.mkdtemp(prefix=\"shiki-sadr-fresh-\"))\n+        self.addCleanup(lambda: shutil.rmtree(self.tmp, ignore_errors=True))\n+        self.target = self.tmp / \"target\"\n+        self.adr_dir = self.target / \"docs\" / \"adr\"\n+        self.adr_dir.mkdir(parents=True)\n+        self.product_bytes = {\n+            f\"{number:04d}-product-decision.md\": (\n+                f\"# Product ADR {number:04d}\\n\\nTarget-owned bytes {number}.\\n\"\n+            ).encode()\n+            for number in PRODUCT_NUMBERS\n+        }\n+        for name, content in self.product_bytes.items():\n+            (self.adr_dir / name).write_bytes(content)\n+\n+    def test_fresh_install_preserves_product_adrs_and_installs_all_sadrs(self) -> None:\n+        _quiet_install(self.target, force=False, validate=True)\n+\n+        for name, expected in self.product_bytes.items():\n+            self.assertEqual((self.adr_dir / name).read_bytes(), expected)\n+\n+        sadr_numbers = {\n+            path.name.split(\"-\", 2)[1]\n+            for path in self.adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\")\n+        }\n+        self.assertEqual(sadr_numbers, {f\"{number:04d}\" for number in range(1, 20)})\n+\n+        numeric_records = {\n+            path.name\n+            for path in self.adr_dir.glob(\"[0-9][0-9][0-9][0-9]-*.md\")\n+        }\n+        self.assertEqual(numeric_records, set(self.product_bytes))\n+        self.assertEqual(find_adr_reference_violations(self.target), [])\n+\n+\n+class LegacySadrUpgradeTests(unittest.TestCase):\n+    def setUp(self) -> None:\n+        self.tmp = Path(tempfile.mkdtemp(prefix=\"shiki-sadr-upgrade-\"))\n+        self.addCleanup(lambda: shutil.rmtree(self.tmp, ignore_errors=True))\n+        self.target = self.tmp / \"target\"\n+        self.adr_dir = self.target / \"docs\" / \"adr\"\n+        self.product_bytes, self.legacy_paths = _create_legacy_upgrade_fixture(\n+            self.target\n+        )\n+\n+    def test_force_upgrade_deletes_only_stamped_legacy_sadrs(self) -> None:\n+        _quiet_install(self.target, force=True, validate=True)\n+\n+        for relative in self.legacy_paths:\n+            self.assertFalse((self.target / relative).exists(), relative)\n+        for name, expected in self.product_bytes.items():\n+            self.assertEqual((self.adr_dir / name).read_bytes(), expected)\n+\n+        sadrs = list(self.adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\"))\n+        self.assertEqual(len(sadrs), 19)\n+        refreshed = json.loads(\n+            (self.target / shiki_installer.INSTALL_STAMP_PATH).read_text(encoding=\"utf-8\")\n+        )\n+        for relative in self.legacy_paths:\n+            self.assertNotIn(relative, refreshed[\"digests\"])\n+        for path in sadrs:\n+            relative = path.relative_to(self.target).as_posix()\n+            self.assertEqual(refreshed[\"digests\"][relative], _sha256(path))\n+\n+    def test_non_force_install_never_deletes_legacy_files(self) -> None:\n+        before = {\n+            relative: (self.target / relative).read_bytes()\n+            for relative in self.legacy_paths\n+        }\n+        _quiet_install(self.target, force=False, validate=False)\n+        for relative, expected in before.items():\n+            self.assertEqual((self.target / relative).read_bytes(), expected)\n+        refreshed = json.loads(\n+            (self.target / shiki_installer.INSTALL_STAMP_PATH).read_text(encoding=\"utf-8\")\n+        )\n+        for relative in self.legacy_paths:\n+            self.assertEqual(refreshed[\"digests\"][relative], _sha256(self.target / relative))\n+\n+\n+class LegacyCleanupPreflightTests(unittest.TestCase):\n+    def setUp(self) -> None:\n+        self.tmp = Path(tempfile.mkdtemp(prefix=\"shiki-sadr-preflight-\"))\n+        self.addCleanup(lambda: shutil.rmtree(self.tmp, ignore_errors=True))\n+\n+    def test_all_ownership_blockers_abort_before_any_write(self) -> None:\n+        cases = (\n+            (\"absent\", \"install stamp is absent\"),\n+            (\"unreadable\", \"install stamp is unreadable\"),\n+            (\"malformed\", \"install stamp is malformed\"),\n+            (\"missing-digest\", \"install stamp has no digest\"),\n+            (\"digest-mismatch\", \"digest mismatch\"),\n+        )\n+        for name, expected_reason in cases:\n+            with self.subTest(name=name):\n+                target = self.tmp / name\n+                _, legacy_paths = _create_legacy_upgrade_fixture(target)\n+                stamp_path = target / shiki_installer.INSTALL_STAMP_PATH\n+\n+                if name == \"absent\":\n+                    stamp_path.unlink()\n+                elif name == \"unreadable\":\n+                    stamp_path.unlink()\n+                    stamp_path.mkdir()\n+                elif name == \"malformed\":\n+                    stamp_path.write_text(\"{not-json\\n\", encoding=\"utf-8\")\n+                else:\n+                    stamp = json.loads(stamp_path.read_text(encoding=\"utf-8\"))\n+                    if name == \"missing-digest\":\n+                        stamp[\"digests\"] = {}\n+                    else:\n+                        stamp[\"digests\"] = {\n+                            relative: \"deadbeef\" for relative in legacy_paths\n+                        }\n+                    stamp_path.write_text(\n+                        json.dumps(stamp, indent=2, sort_keys=True) + \"\\n\",\n+                        encoding=\"utf-8\",\n+                    )\n+\n+                before = _tree_snapshot(target)\n+                with self.assertRaises(shiki_installer.ShikiError) as raised:\n+                    _quiet_install(target, force=True, validate=False)\n+                after = _tree_snapshot(target)\n+\n+                self.assertEqual(before, after, f\"{name} preflight changed the tree\")\n+                message = str(raised.exception)\n+                self.assertIn(expected_reason, message)\n+                self.assertIn(\"No files were written\", message)\n+                for relative in legacy_paths:\n+                    self.assertIn(relative, message)\n+\n+    def test_absent_stamp_is_allowed_when_no_legacy_path_exists(self) -> None:\n+        target = self.tmp / \"no-legacy\"\n+        target.mkdir()\n+        _quiet_install(target, force=True, validate=False)\n+        self.assertTrue(\n+            (target / \"docs\" / \"adr\" / \"SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\").is_file()\n+        )\n+\n+\n+if __name__ == \"__main__\":\n+    unittest.main()\n\n2026-08-07T08:19:14.808051Z ERROR codex_models_manager::manager: failed to renew cache TTL: missing field `base_instructions` at line 94 column 5\nexec\n/bin/zsh -lc \"sed -n '1360,1590p' scripts/shiki_loop.py; sed -n '240,285p' scripts/shiki_tasks.py; sed -n '1,130p' .shiki/schemas/ledger.schema.json\" in /private/tmp/shiki-adr-collision.pa0KwY/.worktrees/shiki-t-20260807t073307221254z-2d22d654-introduc\n succeeded in 0ms:\n    commit = run(\n        [\"git\", \"commit\", \"-m\", f\"shiki: link PR evidence to {task_id} (goal loop)\"],\n        cwd=worktree_path,\n        check=False,\n    )\n    if commit.returncode != 0:\n        return \"nothing to commit for state sync\"\n    push = run([\"git\", \"push\"], cwd=worktree_path, check=False)\n    if push.returncode != 0:\n        return \"state committed on the task branch; push failed \u2014 push manually\"\n    return \"PR evidence committed and pushed to the task branch\"\n\n\n# The independent read-only reviewer's prompt. The diff is the ONLY thing it\n# judges; it returns the structured verdict the loop parses. It is explicitly\n# told it is read-only and may not edit \u2014 the --allowedTools confinement is the\n# real guarantee, this is belt-and-suspenders.\n_CODE_REVIEW_PROMPT = (\n    \"You are an INDEPENDENT pre-PR code reviewer running read-only in a separate \"\n    \"context (SADR-0011). Review ONLY the diff below for correctness bugs, broken \"\n    \"contracts, security issues, data loss, and missing tests. You may use read \"\n    \"tools to inspect the worktree; you may NOT edit anything. Emit a single JSON \"\n    'object matching the verdict schema: verdict \"clean\" when nothing blocking is '\n    'found, \"blocking\" when a blocking issue exists, with a findings array. Do not '\n    \"wrap the JSON in prose.\\n\\n## Task diff (git diff of the task branch against the default branch)\\n\"\n)\n\n\ndef _record_reviewer_raw_output(target: Path, task_id: str, raw_stdout: str) -> str:\n    \"\"\"Record the reviewer's raw output as durable, branch-syncable evidence.\n\n    A degenerate (blocking, zero-findings) verdict names nothing usable, so its\n    only diagnosable artifact is what the reviewer actually emitted. Mirror the\n    TDD gate's EXEC pattern \u2014 an ``EXEC-*.json`` holding the raw stdout, referenced\n    by a ``type:\"check\"`` ledger \u2014 so a future diagnosis has something concrete to\n    read. Returns the ledger id (the EXEC id is embedded in its evidence ref).\"\"\"\n    from shiki_process import shiki_path as _shiki_path\n    from shiki_process import utc_now, write_json\n    from shiki_tasks import next_control_id\n\n    task = load_task(target, task_id)\n    record_id = next_control_id(target, \"EXEC\")\n    record_file = _shiki_path(target, \"runner\", f\"{record_id}.json\")\n    write_json(\n        record_file,\n        {\n            \"id\": record_id,\n            \"task_id\": task[\"id\"],\n            \"goal_id\": task[\"goal_id\"],\n            \"command\": \"independent pre-PR code review (claude -p, read-only) \u2014 SADR-0011\",\n            \"returncode\": 0,\n            \"stdout\": raw_stdout,\n            \"stderr\": \"\",\n            \"created_at\": utc_now(),\n        },\n    )\n    exec_rel = str(record_file.relative_to(target))\n    return append_ledger(\n        target,\n        goal_id=task[\"goal_id\"],\n        task_id=task_id,\n        ledger_type=\"check\",\n        summary=(\n            f\"Pre-PR code-review verdict BLOCKING with zero findings for {task_id} \"\n            \"(degenerate \u2014 the reviewer produced no usable judgment); raw reviewer \"\n            \"output recorded for diagnosis\"\n        ),\n        evidence=[exec_rel],\n    )\n\n\ndef _run_pre_pr_code_review(target: Path, task_id: str) -> dict[str, Any]:\n    \"\"\"Run the independent read-only code-review verifier over the task diff.\n\n    Loop-owned quality-gate step (SADR-0011): the reviewer is the same model as the\n    implementer but in a separate context, confined to read tools (no edit tools),\n    bound to a structured verdict. The loop parses that verdict deterministically.\n\n    Returns a dict with ``status`` in {clean, blocking, retry, fail}:\n\n    * ``clean``    \u2014 verdict parsed as clean; a type:\"check\" \"code-review\" ledger\n      is recorded and ``pre_pr_code_review`` is written onto the task so the\n      PR-12 ``## Pre-PR code review`` body section renders from it.\n    * ``blocking`` \u2014 verdict parsed as blocking WITH at least one named finding, or\n      a degenerate blocking verdict whose retry budget is exhausted. Fail-closed: a\n      blocking pre-PR review CANNOT anchor a repair packet (no PR exists yet at\n      create_pr time by construction), so the caller stops the loop for diagnosis.\n      NOT a repair.\n    * ``retry``    \u2014 verdict parsed as blocking but naming ZERO findings (degenerate:\n      the reviewer produced nothing usable) while the durable retry budget still\n      allows one more attempt. NEVER treated as clean: the caller re-runs the\n      reviewer next pass rather than opening a PR or stopping. The reviewer's raw\n      output is recorded durably first.\n    * ``fail``     \u2014 dispatch failed, the worktree/diff is unavailable, or the\n      verdict could not be parsed. Fail-closed: review-not-done is never silently\n      passed.\n\n    Never raises into the loop.\n    \"\"\"\n    record = worktree_record(target, task_id)\n    if not record:\n        return {\"status\": \"fail\", \"reason\": \"no worktree record; pre-PR code review skipped\"}\n    worktree_path = Path(record[\"path\"]).expanduser().resolve()\n    if not worktree_path.exists() or worktree_path == target.resolve():\n        return {\"status\": \"fail\", \"reason\": \"worktree unavailable; cannot run the pre-PR code review\"}\n\n    # The review runs BEFORE commit/push, so the implementer's work may still be\n    # uncommitted (and new test files untracked) in the worktree. Stage everything\n    # first (non-destructive \u2014 the commit/push step re-stages anyway) so the diff\n    # is complete, then diff the index against the default branch resolved from\n    # origin. This shows the FULL task change set the reviewer must judge,\n    # committed or not, tracked or new \u2014 and never the already-merged history a\n    # stale local default branch would drag in.\n    run([\"git\", \"add\", \"-A\"], cwd=worktree_path, check=False)\n    base = _default_base_ref(target)\n    diff = run([\"git\", \"diff\", \"--cached\", base], cwd=worktree_path, check=False)\n    if diff.returncode != 0:\n        # Fall back to the committed-only diff (e.g. the default branch is\n        # unrelated/missing so the two-dot diff cannot be computed).\n        diff = run([\"git\", \"diff\", f\"{base}...HEAD\"], cwd=worktree_path, check=False)\n        if diff.returncode != 0:\n            return {\"status\": \"fail\", \"reason\": \"could not compute the task diff for review\"}\n    prompt = _CODE_REVIEW_PROMPT + (diff.stdout or \"\")\n\n    try:\n        exec_result = REVIEWER_ADAPTER.execute(worktree_path, prompt)\n    except Exception:\n        # Effectors fail closed and never raise into the loop (T1 style).\n        return {\"status\": \"fail\", \"reason\": \"reviewer dispatch raised; failing closed\"}\n    if exec_result.returncode != 0:\n        return {\"status\": \"fail\", \"reason\": f\"reviewer exited {exec_result.returncode}; failing closed\"}\n\n    verdict = parse_code_review_verdict(exec_result.stdout)\n    if verdict is None:\n        return {\"status\": \"fail\", \"reason\": \"reviewer verdict could not be parsed; failing closed\"}\n\n    if verdict.get(\"verdict\") == \"blocking\":\n        findings = verdict.get(\"findings\") or []\n        if not findings:\n            # A blocking verdict that names NOTHING is not a usable judgment \u2014 it\n            # blocked the PR while pointing at no concrete issue (Measured\n            # 2026-08-05/06 on work that met every contract requirement; a plain\n            # re-run then passed). Record the reviewer's raw output durably so a\n            # future diagnosis has something to read, then RETRY once (bounded by a\n            # durable per-task counter) before stopping. A degenerate verdict is\n            # NEVER treated as clean: the retry re-runs the reviewer, and once the\n            # budget is exhausted the loop stops for diagnosis with the degenerate\n            # verdict named \u2014 it never becomes a silent pass.\n            ledger_id = _record_reviewer_raw_output(target, task_id, exec_result.stdout)\n            task = load_task(target, task_id)\n            task.setdefault(\"ledger_evidence\", []).append(ledger_id)\n            retries = int(task.get(\"pre_pr_review_empty_retries\") or 0)\n            if retries < MAX_PRE_PR_REVIEW_EMPTY_RETRIES:\n                task[\"pre_pr_review_empty_retries\"] = retries + 1\n                task[\"pre_pr_code_review\"] = {\n                    \"verdict\": \"blocking\",\n                    \"findings\": [],\n                    \"degenerate\": True,\n                    \"empty_retries\": retries + 1,\n                    \"ledger_id\": ledger_id,\n                }\n                _save_task(target, task)\n                return {\n                    \"status\": \"retry\",\n                    \"reason\": (\n                        \"independent pre-PR code review returned a blocking verdict naming zero \"\n                        f\"findings (attempt {retries + 1}/{MAX_PRE_PR_REVIEW_EMPTY_RETRIES + 1}); \"\n                        \"the reviewer produced nothing usable \u2014 re-running it on the same work\"\n                    ),\n                    \"ledger_id\": ledger_id,\n                    \"empty_retries\": retries + 1,\n                }\n            task[\"pre_pr_code_review\"] = {\n                \"verdict\": \"blocking\",\n                \"findings\": [],\n                \"degenerate\": True,\n                \"empty_retries\": retries,\n                \"ledger_id\": ledger_id,\n            }\n            _save_task(target, task)\n            return {\n                \"status\": \"blocking\",\n                \"reason\": (\n                    \"independent pre-PR code review returned a blocking verdict naming zero \"\n                    f\"findings after {retries} retr{'y' if retries == 1 else 'ies'}; the reviewer \"\n                    \"produced nothing usable \u2014 diagnose (its raw output is recorded)\"\n                ),\n                \"ledger_id\": ledger_id,\n                \"degenerate\": True,\n            }\n        # Record the blocking verdict as a check ledger for the audit trail, then\n        # fail closed. No PR exists yet, so this cannot become a repair packet.\n        ledger_id = append_ledger(\n            target,\n            goal_id=load_task(target, task_id)[\"goal_id\"],\n            task_id=task_id,\n            ledger_type=\"check\",\n            summary=f\"Pre-PR code-review verdict BLOCKING for {task_id} ({len(findings)} finding(s)); loop stops for diagnosis\",\n            evidence=[\"independent read-only reviewer (claude -p) \u2014 SADR-0011\"],\n        )\n        task = load_task(target, task_id)\n        task.setdefault(\"ledger_evidence\", []).append(ledger_id)\n        task[\"pre_pr_code_review\"] = {\"verdict\": \"blocking\", \"findings\": findings, \"ledger_id\": ledger_id}\n        _save_task(target, task)\n        return {\"status\": \"blocking\", \"reason\": \"independent pre-PR code review found blocking issues\", \"ledger_id\": ledger_id}\n\n    # Clean verdict: record the code-review check ledger and the PR-12 evidence.\n    findings = verdict.get(\"findings\") or []\n    ledger_id = append_ledger(\n        target,\n        goal_id=load_task(target, task_id)[\"goal_id\"],\n        task_id=task_id,\n        ledger_type=\"check\",\n        summary=f\"Pre-PR code-review verdict CLEAN for {task_id} (independent read-only reviewer, code-review skill)\",\n        evidence=[\"independent read-only reviewer (claude -p) \u2014 SADR-0011\"],\n    )\n    task = load_task(target, task_id)\n    task.setdefault(\"ledger_evidence\", []).append(ledger_id)\n    task[\"pre_pr_code_review\"] = {\"verdict\": \"clean\", \"findings\": findings, \"ledger_id\": ledger_id}\n    _save_task(target, task)\n    return {\"status\": \"clean\", \"reason\": \"independent pre-PR code review passed\", \"ledger_id\": ledger_id}\ndef task_test_command(task: dict[str, Any]) -> str:\n    \"\"\"The structured command the loop exec's for the task's TDD gate.\n\n    Reads the task's ``test_command`` field, falling back to the safe unittest\n    discover default when it is absent or blank. ``acceptance_checks`` is\n    free-form prose+commands and is deliberately NOT consulted here \u2014 it must\n    never be handed to a shell (SADR-0011: a deterministic observable command,\n    not narrative, is what the independent verifier runs).\n    \"\"\"\n    # Lazy import keeps the shiki_loop <-> shiki_tasks edge one-directional.\n            raise ShikiError(f\"plan task {index} must be an object\")\n        for field in (\"title\", \"scope\", \"acceptance_checks\"):\n            if field not in task:\n                raise ShikiError(f\"plan task {index} is missing required field: {field}\")\n        if not isinstance(task[\"acceptance_checks\"], list) or not task[\"acceptance_checks\"]:\n            raise ShikiError(f\"plan task {index} must include acceptance_checks\")\n\n\ndef append_ledger(\n    target: Path,\n    *,\n    goal_id: str,\n    ledger_type: str,\n    summary: str,\n    evidence: list[str],\n    task_id: str | None = None,\n    links: list[str] | None = None,\n) -> str:\n    try:\n        return append_ledger_entry(\n            target,\n            lambda ledger_id: {\n                \"id\": ledger_id,\n                \"timestamp\": utc_now(),\n                \"goal_id\": goal_id,\n                \"task_id\": task_id,\n                \"type\": ledger_type,\n                \"actor\": \"shiki-cli\",\n                \"summary\": summary,\n                \"evidence\": evidence,\n                \"links\": links or [],\n            },\n        )\n    except FileExistsError as error:\n        raise ShikiError(str(error)) from error\n\n\ndef register_goal_from_plan(target: Path, plan: dict[str, Any], *, github_issue: int | None = None) -> tuple[str, str]:\n    goal_id = next_control_id(target, \"G\")\n    goal_file = shiki_path(target, \"goals\", f\"{goal_id}.json\")\n    payload = {\n        \"id\": goal_id,\n        \"github_issue\": github_issue,\n        \"title\": plan[\"title\"],\n        \"outcome\": plan[\"outcome\"],\n        \"completion_conditions\": plan.get(\"completion_conditions\") or [plan[\"outcome\"]],\n{\n  \"$schema\": \"https://json-schema.org/draft/2020-12/schema\",\n  \"$id\": \"https://shiki.local/schemas/ledger.schema.json\",\n  \"title\": \"Shiki Ledger Entry\",\n  \"type\": \"object\",\n  \"required\": [\n    \"id\",\n    \"timestamp\",\n    \"goal_id\",\n    \"type\",\n    \"actor\",\n    \"summary\",\n    \"evidence\"\n  ],\n  \"properties\": {\n    \"id\": {\n      \"type\": \"string\",\n      \"pattern\": \"^L-([0-9]{4,}|[0-9]{8}T[0-9]{12}Z-[0-9a-f]{8})$\"\n    },\n    \"timestamp\": {\n      \"type\": \"string\"\n    },\n    \"goal_id\": {\n      \"type\": \"string\",\n      \"pattern\": \"^G-([0-9]{4,}|[0-9]{8}T[0-9]{12}Z-[0-9a-f]{8})$\"\n    },\n    \"task_id\": {\n      \"type\": [\n        \"string\",\n        \"null\"\n      ],\n      \"pattern\": \"^T-([0-9]{4,}|[0-9]{8}T[0-9]{12}Z-[0-9a-f]{8})$\"\n    },\n    \"type\": {\n      \"enum\": [\n        \"goal-created\",\n        \"context-impact\",\n        \"task-registered\",\n        \"lock\",\n        \"check\",\n        \"review\",\n        \"cca-verdict\",\n        \"repair\",\n        \"mergegate\",\n        \"completion\",\n        \"handoff\",\n        \"memory-transition\",\n        \"contract-amended\"\n      ]\n    },\n    \"actor\": {\n      \"type\": \"string\",\n      \"minLength\": 1\n    },\n    \"summary\": {\n      \"type\": \"string\",\n      \"minLength\": 1\n    },\n    \"evidence\": {\n      \"type\": \"array\",\n      \"items\": {\n        \"type\": \"string\",\n        \"minLength\": 1\n      },\n      \"minItems\": 1\n    },\n    \"links\": {\n      \"type\": \"array\",\n      \"items\": {\n        \"type\": \"string\"\n      }\n    },\n    \"evidence_refs\": {\n      \"type\": \"array\",\n      \"items\": {\n        \"type\": \"object\",\n        \"required\": [\n          \"kind\"\n        ],\n        \"properties\": {\n          \"kind\": {\n            \"enum\": [\n              \"github-pr\",\n              \"github-issue\",\n              \"github-workflow-run\",\n              \"github-artifact\",\n              \"ledger-digest\"\n            ]\n          },\n          \"pr\": {\n            \"type\": \"integer\"\n          },\n          \"issue\": {\n            \"type\": \"integer\"\n          },\n          \"head_sha\": {\n            \"type\": \"string\"\n          },\n          \"workflow\": {\n            \"type\": \"string\"\n          },\n          \"run_id\": {\n            \"type\": \"string\"\n          },\n          \"run_attempt\": {\n            \"type\": \"string\"\n          },\n          \"name\": {\n            \"type\": \"string\"\n          },\n          \"manifest_path\": {\n            \"type\": \"string\"\n          },\n          \"manifest_sha256\": {\n            \"type\": \"string\"\n          },\n          \"algorithm\": {\n            \"type\": \"string\"\n          },\n          \"digest\": {\n            \"type\": \"string\"\n          }\n        },\n        \"additionalProperties\": true\n      }\n    },\n    \"ledger_integrity\": {\n      \"type\": \"object\",\n      \"properties\": {\n        \"algorithm\": {\n\ndiff --git a/.codex/skills/shiki/SKILL.md b/.codex/skills/shiki/SKILL.md\nindex c8e2e85d9d75d49cc4f2d6286ee22659c4e53e75..93cded623b2c15d9f65c76491a1c2d813ac9e6c0\n--- a/.codex/skills/shiki/SKILL.md\n+++ b/.codex/skills/shiki/SKILL.md\n@@ -18,6 +18,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n Codex CLI does not register this skill as a custom `/shiki` slash command. When\n the user types `/shiki` in Codex CLI and gets \"Unrecognized command\", explain\n that this is expected: invoke Shiki in Codex with natural language such as\n@@ -69,7 +72,7 @@\n \n ## Responsibilities\n \n-- Claude Code implements and repairs by default (`shiki runner claude`, ADR 0008); Codex implements and repairs tasks explicitly assigned to `codex`.\n+- Claude Code implements and repairs by default (`shiki runner claude`, SADR-0008); Codex implements and repairs tasks explicitly assigned to `codex`.\n - Claude Code Action can act as GitHub-side CCA or reviewer.\n - CCA judges completion.\n - MergeGate authorizes state transitions and merge readiness.\ndiff --git a/.github/prompts/cca-completion-check.md b/.github/prompts/cca-completion-check.md\nindex ed203ad5f302ecc81372d8ab27d2b1232ca97497..fd89950528333ef6242e6d5443904f5392ab7e56\n--- a/.github/prompts/cca-completion-check.md\n+++ b/.github/prompts/cca-completion-check.md\n@@ -19,6 +19,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n ## Required Reading\n \n Read only the files needed to judge the PR contract. Prefer this order:\ndiff --git a/.github/prompts/codex-task-handoff.md b/.github/prompts/codex-task-handoff.md\nindex 695e211d76dea1c60e8d5bb360e78487808286a4..59441d576b01a39569722f226c854e5a740e9eb5\n--- a/.github/prompts/codex-task-handoff.md\n+++ b/.github/prompts/codex-task-handoff.md\n@@ -2,7 +2,7 @@\n \n You are Codex Front acting as the Implementer for a Shiki task.\n \n-Follow `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant ADRs, and `docs/agents/codex-handoff.md`.\n+Follow `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant Shiki SADRs and target ADRs, and `docs/agents/codex-handoff.md`.\n \n Rules:\n \ndiff --git a/AGENTS.md b/AGENTS.md\nindex 7f041100d49c2a7525f51cb0f5d696a4a6d1c78c..a06bfbabc6fa48a007389e25e12d7ed523936bed\n--- a/AGENTS.md\n+++ b/AGENTS.md\n@@ -22,7 +22,7 @@\n - **Codex Front** is an optional implementer through Codex App, Codex CLI, Codex IDE extension, or Codex Web signed in with ChatGPT OAuth/subscription auth, dispatched with `shiki runner codex` when a task is explicitly assigned to `codex`.\n - **GitHub CCA** is implemented by Claude Code Action by default, using `CLAUDE_CODE_OAUTH_TOKEN`.\n \n-Do not assume `openai/codex-action`, `OPENAI_API_KEY`, or API-key based Codex automation in the default Shiki path. API-key runners are explicit target-repo extensions and require their own ADR.\n+Do not assume `openai/codex-action`, `OPENAI_API_KEY`, or API-key based Codex automation in the default Shiki path. API-key runners are explicit target-repo extensions and require their own target ADR.\n \n Shiki is not:\n \n@@ -44,10 +44,14 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use\n+`SADR-NNNN-*.md`; target product decisions retain `NNNN-*.md` and the `ADR`\n+namespace.\n+\n Use the canonical source-of-truth order above. Runtime-specific reading orders\n are operational guidance only and must not override or reorder it.\n \n-Conversation state is not durable truth. If a decision matters, put it in GitHub, `.shiki/`, `CONTEXT.md`, or an ADR.\n+Conversation state is not durable truth. If a decision matters, put it in GitHub, `.shiki/`, `CONTEXT.md`, a Shiki SADR, or a target ADR.\n \n If GitHub and `.shiki/` disagree, surface the conflict and prefer GitHub operational state until the mirror is repaired.\n \n@@ -75,8 +79,8 @@\n Every non-trivial change follows this loop:\n \n 1. **Goal**: clarify outcome, completion conditions, non-goals, risk level, and success signals.\n-2. **grill-with-docs**: challenge the plan against domain docs, ADRs, code reality, terminology, and edge scenarios.\n-3. **Context & Impact**: find relevant docs, ADRs, code, dependencies, owners, locks, and verification surfaces.\n+2. **grill-with-docs**: challenge the plan against domain docs, Shiki SADRs, target ADRs, code reality, terminology, and edge scenarios.\n+3. **Context & Impact**: find relevant docs, SADRs, target ADRs, code, dependencies, owners, locks, and verification surfaces.\n 4. **PRD and Spec Freeze**: use `to-prd` when context is settled enough to become durable product/engineering intent. The operator's explicit approval of the PRD is Spec Freeze: the plan records a `spec_freeze` block, and no plan runs without it. Steps 1-4 form Requirements Definition \u2014 one continuous operator dialogue. After Spec Freeze, scope changes require an operator-approved Spec Amendment; non-scope-moving interpretations go to the Assumption Log.\n 5. **Issues**: use `to-issues` to create independently grabbable vertical-slice issues. Prefer AFK slices over HITL slices where possible.\n 6. **Triage**: label issues for readiness, risk, runtime, skills, and MergeGate state.\n@@ -167,7 +171,7 @@\n - Testing decisions.\n - Out-of-scope items.\n - Further notes.\n-- Links to relevant domain terms and ADRs.\n+- Links to relevant domain terms, SADRs, and target ADRs.\n \n Every executable task must include:\n \n@@ -193,7 +197,7 @@\n \n Context & Impact must identify:\n \n-- Relevant documents, domain terms, ADRs, and past decisions.\n+- Relevant documents, domain terms, SADRs, target ADRs, and past decisions.\n - Relevant modules, interfaces, seams, callers, owners, and tests.\n - Dependency relationships between tasks.\n - Candidate locks and lock conflicts.\n@@ -210,7 +214,7 @@\n | Skill | Required when |\n | --- | --- |\n | `setup-matt-pocock-skills` | First configuring the repo for skills, or when issue tracker, triage labels, or domain docs are missing. |\n-| `grill-with-docs` | Default for non-trivial Goals before PRD/issues; always required when plans, terminology, boundaries, tradeoffs, or ADR-worthy decisions are ambiguous. |\n+| `grill-with-docs` | Default for non-trivial Goals before PRD/issues; always required when plans, terminology, boundaries, tradeoffs, or decision-record-worthy choices are ambiguous. |\n | `zoom-out` | The agent lacks an architectural map of the relevant code area. |\n | `to-prd` | Settled Goal context must become durable product/engineering intent. |\n | `to-issues` | A Goal, PRD, or plan must become independently grabbable vertical-slice issues. |\n@@ -348,7 +352,7 @@\n - Three or more new files.\n - Any high-risk or critical label.\n \n-When the architecture gate triggers, run `grill-with-docs` or `improve-codebase-architecture` as appropriate and record durable decisions in an ADR when needed.\n+When the architecture gate triggers, run `grill-with-docs` or `improve-codebase-architecture` as appropriate and record durable decisions in a Shiki SADR or target ADR as applicable.\n \n ## Engineering Discipline\n \ndiff --git a/CLAUDE.md b/CLAUDE.md\nindex 98a932e7841a632866fcd53b0bad0e3af3711d3d..98a31399404d6fb044b4fd493f4459ac87f439de\n--- a/CLAUDE.md\n+++ b/CLAUDE.md\n@@ -14,6 +14,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n ## Claude Role\n \n Default to planner, default implementer, reviewer, coordinator, documentation editor, CCA-style completion judge, and final-judgment assistant.\n@@ -57,7 +60,7 @@\n At the start of a Shiki session:\n \n 1. Read `AGENTS.md`.\n-2. Read `CONTEXT.md` and relevant ADRs.\n+2. Read `CONTEXT.md` and relevant Shiki SADRs and target ADRs.\n 3. Read active `.shiki/` Goal/task/ledger state when present.\n 4. Read active distilled rules from `.shiki/memories/` (status `distilled`, `active: true`, not revoked or superseded) for applicable guidance; the same rules are injected deterministically into task handoffs as the `## Distilled Rules` section (\u00a73.5).\n 5. Read the GitHub Issue or PR when the task is GitHub-backed.\n@@ -74,7 +77,7 @@\n - Define the Goal, completion conditions, non-goals, and risk level.\n - Treat `grill-with-docs` as the default planning entry point for non-trivial Goals.\n - Challenge domain terms against `CONTEXT.md`.\n-- Challenge hard-to-reverse tradeoffs against ADRs.\n+- Challenge hard-to-reverse tradeoffs against SADRs and target ADRs.\n - Explore code instead of asking when code can answer the question.\n - Run Context & Impact before implementation planning.\n - Produce a Task DAG, not an unordered checklist.\n@@ -94,7 +97,7 @@\n - For each question, provide your recommended answer.\n - Resolve dependency decisions before downstream design questions.\n - Update `CONTEXT.md` inline when terminology crystallizes.\n-- Offer ADRs only when the decision is hard to reverse, surprising without context, and a real tradeoff.\n+- Offer a SADR or target ADR only when the decision is hard to reverse, surprising without context, and a real tradeoff.\n - Convert unresolved questions into HITL blockers rather than allowing Codex to guess.\n \n ## PRD and Issues Standard\n@@ -103,7 +106,7 @@\n \n - Use the project's domain glossary vocabulary.\n - Avoid volatile file paths unless they are essential.\n-- Include implementation decisions, testing decisions, out-of-scope items, and relevant ADR/domain links.\n+- Include implementation decisions, testing decisions, out-of-scope items, and relevant SADR/target-ADR/domain links.\n - Identify deep-module opportunities and testable interfaces.\n \n When creating issues:\n@@ -121,7 +124,7 @@\n - Branch/worktree target.\n - Scope and non-goals.\n - Dependencies and locks.\n-- Relevant docs, ADRs, modules, and tests.\n+- Relevant docs, SADRs, target ADRs, modules, and tests.\n - Required skill invocations.\n - TDD expectations.\n - Pre-PR `code-review` gate expectations.\n@@ -152,7 +155,7 @@\n \n - Treat yourself as a judge, not implementer.\n - Do not write production code.\n-- Read `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant ADRs, PRD, task issue, PR body, diff, checks, reviews, labels, and `.shiki/` state.\n+- Read `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant SADRs and target ADRs, PRD, task issue, PR body, diff, checks, reviews, labels, and `.shiki/` state.\n - Evaluate every applicable checklist in `docs/agents/checklists.md`.\n - Map each acceptance criterion to durable evidence.\n - Distinguish implementation failure from missing evidence.\ndiff --git a/CONTEXT.md b/CONTEXT.md\nindex e61a9be06a0cce0d026484083a657bde2067e234..237ee8aa3ea8fab334ece49768bc5ed3b5af0974\n--- a/CONTEXT.md\n+++ b/CONTEXT.md\n@@ -25,7 +25,7 @@\n _Avoid_: jumping directly into code\n \n **grill-with-docs**:\n-A planning skill that challenges a plan against domain language, ADRs, code reality, and concrete edge scenarios. It resolves design-tree questions before PRD/issues. It runs inside Requirements Definition.\n+A planning skill that challenges a plan against domain language, Shiki SADRs, target ADRs, code reality, and concrete edge scenarios. It resolves design-tree questions before PRD/issues. It runs inside Requirements Definition.\n _Avoid_: generic brainstorming, silent assumptions\n \n **Requirements Definition**:\ndiff --git a/CONTRIBUTING.md b/CONTRIBUTING.md\nindex c6d9b656e73abb8a55e5f660123e03153dcb3e6d..9e37a8fbf668db5b22461aaae5548b8f1354394c\n--- a/CONTRIBUTING.md\n+++ b/CONTRIBUTING.md\n@@ -17,7 +17,7 @@\n 1. **Goal** \u2014 start from a user-approved Goal with an outcome, completion\n    conditions, non-goals, risk level, and success signals.\n 2. **grill-with-docs** \u2014 for any non-trivial change, challenge the plan against\n-   domain language, ADRs, and code reality before writing a PRD.\n+   domain language, Shiki SADRs, target ADRs, and code reality before writing a PRD.\n 3. **PRD** \u2014 capture settled product/engineering intent (`to-prd`).\n 4. **Spec Freeze** \u2014 the operator explicitly approves the PRD/requirements;\n    the plan records a `spec_freeze` block and no plan runs without it\n@@ -94,7 +94,7 @@\n   entries.\n - Do not hand-edit `.shiki/goals/*.json` or `.shiki/manifest.json` in a task PR.\n - Conversation state is not durable truth. If a decision matters, record it in\n-  GitHub, `.shiki/`, `CONTEXT.md`, or an ADR.\n+  GitHub, `.shiki/`, `CONTEXT.md`, a Shiki SADR, or a target ADR.\n \n ## Architecture Gate\n \n@@ -103,7 +103,7 @@\n or CI policy, package structure, three or more structural files, more than 100\n net new lines in a core module, three or more new files, or any high-risk label.\n When triggered, run `grill-with-docs` or `improve-codebase-architecture` and\n-record durable decisions in an ADR when needed (see `docs/adr/`).\n+record durable decisions in a Shiki SADR or target ADR as applicable (see `docs/adr/`).\n \n ## Safety\n \ndiff --git a/SYSTEM_PROMPT.md b/SYSTEM_PROMPT.md\nindex aaaea45e3a896c3a44ba89eec8bb84325ecf6c62..da009c86a88285269dee0155e58e550c6d744ea9\n--- a/SYSTEM_PROMPT.md\n+++ b/SYSTEM_PROMPT.md\n@@ -30,6 +30,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n Use the canonical source-of-truth order above. Runtime-specific reading orders\n are operational guidance only and must not override or reorder it. The current\n conversation is non-durable operator input.\n@@ -48,14 +51,14 @@\n \n A runtime may only do work that is explicitly assigned by the Goal, task, PR, or MergeGate decision.\n \n-Do not assume `openai/codex-action`, `OPENAI_API_KEY`, or API-key based Codex automation in the default Shiki path. API-key runners are explicit target-repo extensions and require their own ADR.\n+Do not assume `openai/codex-action`, `OPENAI_API_KEY`, or API-key based Codex automation in the default Shiki path. API-key runners are explicit target-repo extensions and require their own target ADR.\n \n ## 4. Non-negotiable Shiki flow\n \n For any non-trivial Goal, use this flow:\n \n 1. **Goal Seek**: define outcome, non-goals, risk level, completion criteria, and evidence requirements.\n-2. **grill-with-docs**: challenge the plan against `CONTEXT.md`, ADRs, code reality, terminology, boundaries, and edge scenarios. Ask one question at a time when the operator is available. If code can answer a question, inspect code instead of asking.\n+2. **grill-with-docs**: challenge the plan against `CONTEXT.md`, Shiki SADRs, target ADRs, code reality, terminology, boundaries, and edge scenarios. Ask one question at a time when the operator is available. If code can answer a question, inspect code instead of asking.\n 3. **Context & Impact**: identify modules, interfaces, seams, callers, dependencies, locks, risk, and verification surfaces.\n 4. **to-prd + Spec Freeze**: turn settled context into a PRD using domain vocabulary and testing decisions. The operator's explicit approval of the PRD is Spec Freeze, recorded as the plan's `spec_freeze` block; plans without it do not run. Steps 1-4 form Requirements Definition.\n 5. **to-issues**: decompose the PRD into independently grabbable vertical-slice issues. Prefer AFK slices; mark HITL slices when judgment is still required.\n@@ -75,7 +78,7 @@\n Before making any material change:\n \n 1. Read `AGENTS.md` and any runtime-specific wrapper such as `CLAUDE.md`.\n-2. Read `CONTEXT.md` and relevant ADRs.\n+2. Read `CONTEXT.md` and relevant SADRs and target ADRs.\n 3. Read the GitHub Issue / PR and `.shiki/` state for the active Goal or task.\n 4. Identify your role: Planner, Implementer, Reviewer, Completion Check Agent, Repairer, or Guardian-assist.\n 5. Identify Goal id, task id, branch, PR, dependencies, locks, risk level, required skills, and required evidence.\n@@ -103,7 +106,7 @@\n \n At minimum, identify:\n \n-- Relevant documents, ADRs, domain terms, and prior decisions.\n+- Relevant documents, SADRs, target ADRs, domain terms, and prior decisions.\n - Relevant modules, interfaces, seams, callers, owners, and tests.\n - Dependency relationships between tasks.\n - Candidate file locks and lock conflicts.\n@@ -152,7 +155,7 @@\n \n CCA must read:\n \n-- `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant ADRs.\n+- `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant SADRs and target ADRs.\n - Parent Goal issue, PRD issue, task issue, comments, and labels.\n - PR body, diff, commits, changed files, reviews, CI checks, and artifacts available to the workflow.\n - `.shiki/` task contract, locks, ledger entries, CCA checklist profile, and prior repair packets.\n@@ -214,7 +217,7 @@\n - Three or more new files.\n - Any high-risk or critical label.\n \n-When the architecture gate triggers, run `grill-with-docs` or `improve-codebase-architecture` as appropriate and record the decision in an ADR if it is hard to reverse or likely to be re-litigated.\n+When the architecture gate triggers, run `grill-with-docs` or `improve-codebase-architecture` as appropriate and record the decision in a Shiki SADR or target ADR as applicable if it is hard to reverse or likely to be re-litigated.\n \n ## 14. Skill Gate\n \n@@ -223,7 +226,7 @@\n Use these skills:\n \n - `setup-matt-pocock-skills`: before first use of the engineering skill set, or when issue tracker, triage labels, or domain docs are missing.\n-- `grill-with-docs`: default for non-trivial Goals before PRD/issues; required for ambiguous plans, terminology, boundaries, ADR-worthy tradeoffs, or design-tree decisions.\n+- `grill-with-docs`: default for non-trivial Goals before PRD/issues; required for ambiguous plans, terminology, boundaries, decision-record-worthy tradeoffs, or design-tree decisions.\n - `zoom-out`: unfamiliar code area or missing architectural map.\n - `to-prd`: convert settled context into a PRD.\n - `to-issues`: break a PRD, plan, or Goal into independently grabbable vertical-slice issues.\ndiff --git a/docs/adr/0001-keep-shiki-as-platform-name.md b/docs/adr/SADR-0001-keep-shiki-as-platform-name.md\nindex 20a9637c3b9d8c97de7ba8db7cfae4d82d9a7dba..8b685439959aada1b221c112f0ac3c51ca8a1695\n--- a/docs/adr/0001-keep-shiki-as-platform-name.md\n+++ b/docs/adr/SADR-0001-keep-shiki-as-platform-name.md\n@@ -1,3 +1,3 @@\n-# Keep Shiki As The Platform Name\n+# SADR-0001: Keep Shiki As The Platform Name\n \n Shiki remains the product name for the GitHub-native agentic engineering control plane. We considered more descriptive names such as MergePilot, MergeGate, and Kiroku, but kept Shiki because it carries the existing goal-seeking framework identity while remaining independent of Hermes, Codex, Claude Code, or any single runtime.\ndiff --git a/docs/adr/0002-use-agents-as-shared-constitution.md b/docs/adr/SADR-0002-use-agents-as-shared-constitution.md\nindex 73de30ecb078120eac540d9bc51855e3cd48d0e3..c5486182bf06fca36ea508496b013c67302fd989\n--- a/docs/adr/0002-use-agents-as-shared-constitution.md\n+++ b/docs/adr/SADR-0002-use-agents-as-shared-constitution.md\n@@ -1,3 +1,3 @@\n-# Use AGENTS As The Shared Agent Constitution\n+# SADR-0002: Use AGENTS As The Shared Agent Constitution\n \n Shiki uses `AGENTS.md` as the runtime-neutral constitution and `CLAUDE.md` as a thin Claude Code wrapper. This keeps Codex, Claude Code, Hermes Runner, and future Agent Runtimes aligned on the same GitHub-first MergeGate rules while still allowing runtime-specific instructions where needed.\ndiff --git a/docs/adr/0003-require-skill-gate-for-mergegate-readiness.md b/docs/adr/SADR-0003-require-skill-gate-for-mergegate-readiness.md\nindex 424be0c9af4c8f0136f1fde68a468c47ff84e764..7fc337987c7df9792f75f181dfcbed75fe5ffff2\n--- a/docs/adr/0003-require-skill-gate-for-mergegate-readiness.md\n+++ b/docs/adr/SADR-0003-require-skill-gate-for-mergegate-readiness.md\n@@ -1,3 +1,3 @@\n-# Require Skill Gate For MergeGate Readiness\n+# SADR-0003: Require Skill Gate For MergeGate Readiness\n \n Shiki requires relevant engineering skills to be invoked before execution when their triggers apply. This makes skills part of MergeGate readiness rather than optional prompt style, because Shiki depends on repeatable planning, issue decomposition, TDD, diagnosis, triage, and architecture review before autonomous or parallel agent work can be trusted.\ndiff --git a/docs/adr/0004-use-github-first-with-shiki-mirror.md b/docs/adr/SADR-0004-use-github-first-with-shiki-mirror.md\nindex d0a3a9174dc646a945a9d325954ffaf00c352040..a79c54ae9ee8eed2a0524192b864da4e22a92d3f\n--- a/docs/adr/0004-use-github-first-with-shiki-mirror.md\n+++ b/docs/adr/SADR-0004-use-github-first-with-shiki-mirror.md\n@@ -1,4 +1,4 @@\n-# ADR 0004: Use GitHub First With A Repository-Local Shiki Mirror\n+# SADR-0004: Use GitHub First With A Repository-Local Shiki Mirror\n \n ## Status\n \ndiff --git a/docs/adr/0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md b/docs/adr/SADR-0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\nindex b339baf49b89d9290e79c3d51b36e7b2bde3bd95..0f3c3eb9afccdb3403ce978e21d04fe12e495da0\n--- a/docs/adr/0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\n+++ b/docs/adr/SADR-0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\n@@ -1,8 +1,8 @@\n-# ADR 0005: Use Codex As Front And Claude Code Action As The GitHub Runtime\n+# SADR-0005: Use Codex As Front And Claude Code Action As The GitHub Runtime\n \n ## Status\n \n-Accepted (implementer default superseded by ADR 0008; the auth model and the\n+Accepted (implementer default superseded by SADR-0008; the auth model and the\n Claude Code Action GitHub runtime decision remain in force)\n \n ## Context\n@@ -26,4 +26,4 @@\n - Shiki avoids requiring `OPENAI_API_KEY` for the default workflow.\n - Codex work happens through the user's authenticated Codex front surface, not as a hidden backend Action.\n - Claude Code Action is configured for OAuth-token based subscription usage by default.\n-- API-key based automation remains possible only as an explicit target-repo extension with its own ADR.\n+- API-key based automation remains possible only as an explicit target-repo extension with its own target ADR.\ndiff --git a/docs/adr/0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md b/docs/adr/SADR-0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\nindex 4bcae9d1e52f0a37b8e9fbafa5cd14b6ee1993bd..7a38538a6ae9929567f0ade67e7ea6a60d0b3f7e\n--- a/docs/adr/0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\n+++ b/docs/adr/SADR-0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\n@@ -1,4 +1,4 @@\n-# ADR 0006: Separate Codex Implementation, CCA Completion, And MergeGate Authorization\n+# SADR-0006: Separate Codex Implementation, CCA Completion, And MergeGate Authorization\n \n ## Status\n \ndiff --git a/docs/adr/0007-packaging-and-release.md b/docs/adr/SADR-0007-packaging-and-release.md\nindex 79b84ddb97167372b44641e5b7860e400ce80822..bb445a2e2e856d9d1f5fdbe35de8d4e8cec0fb32\n--- a/docs/adr/0007-packaging-and-release.md\n+++ b/docs/adr/SADR-0007-packaging-and-release.md\n@@ -1,4 +1,4 @@\n-# ADR 0007: Packaging And Release For Shiki\n+# SADR-0007: Packaging And Release For Shiki\n \n ## Status\n \n@@ -25,7 +25,7 @@\n   not introduce third-party runtime dependencies or break the shim.\n - Releases must run inside the operator's subscription-authenticated toolchain\n   and must not require extra secrets beyond the built-in `GITHUB_TOKEN`\n-  (consistent with ADR 0005's no-API-key-by-default stance).\n+  (consistent with SADR-0005's no-API-key-by-default stance).\n - `.shiki/manifest.json` already encodes the install surface (`install.include`,\n   `install.create_directories`, `install.exclude_from_commit`), and it must not\n   be mutated as part of this change.\n@@ -34,7 +34,7 @@\n \n This is a hard-to-reverse platform decision (it sets the version source of\n truth, the release trigger, and the public install contract), so it is recorded\n-as an ADR.\n+as a SADR.\n \n ## Decision\n \n@@ -104,7 +104,7 @@\n 3. **Publishing to PyPI.** Rejected for now: Shiki is distributed as a\n    repository template + git-installable CLI, not a PyPI library; PyPI would add\n    credentials/secrets and a different versioning contract. Can be added later as\n-   its own ADR if a packaged distribution is needed.\n+   its own SADR if a packaged distribution is needed.\n 4. **Dynamic version (read `VERSION` via a build backend `dynamic` field).**\n    Rejected to keep `pyproject.toml` backend-agnostic and dependency-free; the\n    small duplication between `VERSION` and `pyproject.toml` is guarded by the\n@@ -123,4 +123,4 @@\n   releasing versions the whole platform but ships only `install.include` to\n   targets.\n - Future PyPI distribution or a console entry point are deferred and would each\n-  warrant their own ADR.\n+  warrant their own SADR.\ndiff --git a/docs/adr/0008-use-claude-code-as-default-implementer.md b/docs/adr/SADR-0008-use-claude-code-as-default-implementer.md\nindex dedf4510b7c7799521422f622029563d373db031..35c2625d908167fbc58855b5b63e283805f167e8\n--- a/docs/adr/0008-use-claude-code-as-default-implementer.md\n+++ b/docs/adr/SADR-0008-use-claude-code-as-default-implementer.md\n@@ -1,12 +1,12 @@\n-# ADR 0008: Use Claude Code As The Default Implementer Runtime\n+# SADR-0008: Use Claude Code As The Default Implementer Runtime\n \n ## Status\n \n-Proposed (supersedes ADR 0005 for the implementer default; keeps its auth model)\n+Proposed (supersedes SADR-0005 for the implementer default; keeps its auth model)\n \n ## Context\n \n-ADR 0005 made Codex Front the default implementation runtime, with Claude Code\n+SADR-0005 made Codex Front the default implementation runtime, with Claude Code\n restricted to planner and reviewer roles by the runtime registry, and the only\n autonomous implementation adapter was `shiki runner codex`.\n \n@@ -18,13 +18,13 @@\n - The platform's planning, review, and CCA layers already run on Claude Code,\n   and Claude Code's native capabilities (the built-in `/code-review` skill and\n   the Workflow multi-agent orchestration tool) have no Codex equivalent.\n-- The Goal lifecycle redesign (ADR 0009) requires an autonomous post-freeze\n+- The Goal lifecycle redesign (SADR-0009) requires an autonomous post-freeze\n   loop driven from the operator's Claude Code session, including bounded\n   repair dispatch, which must not depend on a runtime that cannot be invoked\n   unattended.\n \n The constitution claims Shiki is runtime-agnostic (\"not a Claude-only\n-workflow\", ADR 0002). Any change to the implementer default must preserve the\n+workflow\", SADR-0002). Any change to the implementer default must preserve the\n registry-based runtime model rather than hardcoding one vendor.\n \n ## Decision\n@@ -39,7 +39,7 @@\n   the task to review or repair-needed.\n - Keep Codex registered as an optional implementer runtime. `shiki runner\n   codex`, the Codex handoff templates, and the ChatGPT OAuth auth model from\n-  ADR 0005 remain supported for tasks explicitly assigned to `codex`.\n+  SADR-0005 remain supported for tasks explicitly assigned to `codex`.\n - Commit to mandating two Claude-native capabilities as loop contract in\n   follow-up Goals (status: the code-review gate was delivered by Goal G-B \u2014\n   skill registry, checklists item PR-12, default required_skills \u2014 while the\n@@ -60,7 +60,7 @@\n \n Out of scope: removing Codex support, changing the CCA runtime (Claude Code\n Action with `CLAUDE_CODE_OAUTH_TOKEN` stays), and API-key based automation\n-(still requires its own ADR per ADR 0005).\n+(still requires its own target ADR per SADR-0005).\n \n ## Consequences\n \n@@ -100,7 +100,7 @@\n   harness rejection of direct `codex exec` is outside Shiki's control, and the\n   plan's mandatory native capabilities cannot run on Codex at all.\n - **Remove Codex entirely**: rejected; it abandons the runtime-agnostic\n-  constitution (ADR 0002) for no operational gain and forecloses\n+  constitution (SADR-0002) for no operational gain and forecloses\n   cross-runtime adversarial review.\n - **GitHub Actions cloud implementation via claude-code-action**: rejected as\n   default; durable evidence is automatic but token cost, Node 24 workflow\ndiff --git a/docs/adr/0009-requirements-definition-and-spec-freeze.md b/docs/adr/SADR-0009-requirements-definition-and-spec-freeze.md\nindex 29b3f7871bef9203a8b4a831a65c39eb2f506e42..52e7dd69ad0aa4efe0b68c62678abbf7df02aaec\n--- a/docs/adr/0009-requirements-definition-and-spec-freeze.md\n+++ b/docs/adr/SADR-0009-requirements-definition-and-spec-freeze.md\n@@ -1,4 +1,4 @@\n-# ADR 0009: Merge Grill And Planning Into Requirements Definition With Spec Freeze\n+# SADR-0009: Merge Grill And Planning Into Requirements Definition With Spec Freeze\n \n ## Status\n \ndiff --git a/docs/adr/0010-external-ai-guardian-review.md b/docs/adr/SADR-0010-external-ai-guardian-review.md\nindex e6108bbe8bb4e73e0ff4684c8f78001a80438c4e..f3510b145664298adafb151b6f0db51b972f01ec\n--- a/docs/adr/0010-external-ai-guardian-review.md\n+++ b/docs/adr/SADR-0010-external-ai-guardian-review.md\n@@ -1,4 +1,4 @@\n-# ADR 0010: External AI Guardian Review As A First-Class Approval Authority\n+# SADR-0010: External AI Guardian Review As A First-Class Approval Authority\n \n ## Status\n \n@@ -8,7 +8,7 @@\n \n Shiki is intended to be an autonomous, recursive, self-improving agent\n framework: authority-in-the-loop, not human-in-the-loop. Until now the Guardian\n-approval required for high/critical-risk merges (guardian-policy.json, ADR\n+approval required for high/critical-risk merges (guardian-policy.json, SADR\n governance) recognized only human authority sources \u2014 a GitHub review approval,\n the `guardian:approved` label applied by a configured Guardian, or a comment\n carrying `Guardian approval granted` plus the current head SHA.\n@@ -89,7 +89,7 @@\n   autonomous design the operator intends and produced the impersonation\n   incident.\n - **Record AI approval as an operator comment**: rejected as forgery of human\n-  authority \u2014 the exact failure this ADR corrects.\n+  authority \u2014 the exact failure this SADR corrects.\n - **Deliver the artifact as a committed `.shiki/` file**: rejected; a committed\n   approval inside the PR being approved is forgeable by anyone who can push.\n - **A dedicated reviewer bot identity instead of a relayed comment**: deferred;\ndiff --git a/docs/adr/0011-autonomous-quality-gate-evidence.md b/docs/adr/SADR-0011-autonomous-quality-gate-evidence.md\nindex bca0246c5d46e44afbde15486a75eec425fc02f5..9c93eac16d8c3a6d4b11bd7224e3955102cd7db4\n--- a/docs/adr/0011-autonomous-quality-gate-evidence.md\n+++ b/docs/adr/SADR-0011-autonomous-quality-gate-evidence.md\n@@ -1,4 +1,4 @@\n-# ADR 0011: Autonomous Quality-Gate Evidence \u2014 Deterministic Facts And An Independent Verifier\n+# SADR-0011: Autonomous Quality-Gate Evidence \u2014 Deterministic Facts And An Independent Verifier\n \n ## Status\n \ndiff --git a/docs/adr/0012-autonomous-loop-closeout-to-main.md b/docs/adr/SADR-0012-autonomous-loop-closeout-to-main.md\nindex 637fad6dde4989c9c2bf0f81f676d8d29991123c..ec7d85f81030a676bcbba28b43eb365422a36eac\n--- a/docs/adr/0012-autonomous-loop-closeout-to-main.md\n+++ b/docs/adr/SADR-0012-autonomous-loop-closeout-to-main.md\n@@ -1,4 +1,4 @@\n-# 0012 \u2014 Autonomous loop pushes goal completion to main via a closeout PR\n+# SADR-0012 \u2014 Autonomous loop pushes goal completion to main via a closeout PR\n \n - Status: accepted\n - Date: 2026-06-18\ndiff --git a/docs/adr/0013-bootstrap-grants-actions-approve-pull-requests.md b/docs/adr/SADR-0013-bootstrap-grants-actions-approve-pull-requests.md\nindex b6c69d64f1c5ced346b88d6859586715b3b5bd0e..cf48ff4a6716267f539ca58edd1a852e7c7a2d64\n--- a/docs/adr/0013-bootstrap-grants-actions-approve-pull-requests.md\n+++ b/docs/adr/SADR-0013-bootstrap-grants-actions-approve-pull-requests.md\n@@ -1,4 +1,4 @@\n-# ADR 0013: Bootstrap Grants GitHub Actions Permission To Create And Approve Pull Requests\n+# SADR-0013: Bootstrap Grants GitHub Actions Permission To Create And Approve Pull Requests\n \n ## Status\n \n@@ -31,7 +31,7 @@\n security tradeoff: it lets repository automation satisfy a human-review gate. In\n a multi-maintainer repo that could let a compromised or buggy workflow\n self-approve changes. So whether bootstrap turns it on by default \u2014 and with\n-what default token scope \u2014 is an ADR-worthy decision, not an implementation\n+what default token scope \u2014 is a SADR-worthy decision, not an implementation\n detail.\n \n ## Decision\n@@ -48,7 +48,7 @@\n Specifically:\n \n - **`can_approve_pull_request_reviews` defaults to `true`.** Shiki's default\n-  posture is solo/AFK autonomous operation (ADR 0008/0009/0012), where the CCA\n+  posture is solo/AFK autonomous operation (SADR-0008/0009/0012), where the CCA\n   Review Bridge is the only path that can satisfy `required_review: true` after\n   the CCA and Guardian gates have already passed. The default makes a\n   freshly-bootstrapped repo able to run the autonomous loop end to end without a\ndiff --git a/docs/adr/0014-codex-app-external-ai-guardian-ui-adapter.md b/docs/adr/SADR-0014-codex-app-external-ai-guardian-ui-adapter.md\nindex 238db06279334caa341c117362df9271382a7fcd..65f12e9939321e1b3e389a34c822fc1549e5155d\n--- a/docs/adr/0014-codex-app-external-ai-guardian-ui-adapter.md\n+++ b/docs/adr/SADR-0014-codex-app-external-ai-guardian-ui-adapter.md\n@@ -1,4 +1,4 @@\n-# ADR 0014: Use Codex App As The External AI Guardian UI Adapter\n+# SADR-0014: Use Codex App As The External AI Guardian UI Adapter\n \n ## Status\n \ndiff --git a/docs/adr/0015-contract-approval-before-dispatch.md b/docs/adr/SADR-0015-contract-approval-before-dispatch.md\nindex fad6c656741a20a7684ba16080a7db159f63a52f..6061271a46d3f1d6ae7174477de3fb75673123a6\n--- a/docs/adr/0015-contract-approval-before-dispatch.md\n+++ b/docs/adr/SADR-0015-contract-approval-before-dispatch.md\n@@ -1,4 +1,4 @@\n-# ADR 0015: Approve The Task Contract Before Dispatch, Not The Diff After It\n+# SADR-0015: Approve The Task Contract Before Dispatch, Not The Diff After It\n \n ## Status\n \n@@ -9,7 +9,7 @@\n Guardian approval for high/critical risk is expressible only on a pull request.\n Every approval source in `guardian-policy.json` \u2014 GitHub review, the\n `guardian:approved` label, the `Guardian approval granted` comment, and the\n-`external_ai_guardian_review` artifact (ADR 0010/0014) \u2014 is bound to a PR, and\n+`external_ai_guardian_review` artifact (SADR-0010/0014) \u2014 is bound to a PR, and\n three of the four require the current head SHA. Approval therefore cannot exist\n before an implementation PR exists.\n \n@@ -81,7 +81,7 @@\n An envelope breach must surface only on the non-repairable policy gate, never on\n the repairable `MergeGate metadata check`. Otherwise the Goal Loop routes it to\n `dispatch_repair` \u2014 instructing a runner to make the breach go away \u2014 which is\n-exactly what this ADR forbids.\n+exactly what this SADR forbids.\n \n **Contract immutability.** After registration, an implementation PR must not\n change the governance fields of its own task contract (`scope`, `non_goals`,\n@@ -116,7 +116,7 @@\n fails any verdict that is not `complete`, and the signal the CCA prompt reads is\n computed from `evaluate_guardian_approval` alone. Without the OR-merge at the\n signal, a contract-approved PR still returns `needs_guardian`, the CCA job fails,\n-and the MergeGate policy check never runs \u2014 the gate this ADR changes would never\n+and the MergeGate policy check never runs \u2014 the gate this SADR changes would never\n be reached. The CCA *prompt* needs no change: a `contract_approval` entry in the\n signal's existing `sources` array satisfies its current wording.\n \n@@ -130,7 +130,7 @@\n written, and an approved contract lets its implementation PRs merge through the\n same automatic path as low/medium risk. The wasted implement-then-block cycle\n disappears. Guardian review shifts from reading diffs to reading contracts, which\n-is both earlier and smaller; the external AI Guardian path (ADR 0010/0014) works\n+is both earlier and smaller; the external AI Guardian path (SADR-0010/0014) works\n on contracts with no change.\n \n We accept that the Guardian authorizes a class of change without seeing its\n@@ -146,7 +146,7 @@\n adversarial tests for the new approval path equivalent in rigor to\n `test_shiki_governance_evidence.sh`.\n \n-Bootstrap costs, which apply only while this ADR is being implemented. The tasks\n+Bootstrap costs, which apply only while this SADR is being implemented. The tasks\n that build Contract Approval merge under the existing rules, so the ones\n dispatched after the pre-dispatch gate lands but before the approval path lands\n need a Guardian-approved Contract PR *and* a live Guardian approval on their\n@@ -210,7 +210,7 @@\n risk; they do not bound authorized work.\n \n **Auto-escalate risk on envelope breach instead of stopping.** Returns the\n-workflow to a diff-time Guardian decision \u2014 the exact behavior this ADR removes.\n+workflow to a diff-time Guardian decision \u2014 the exact behavior this SADR removes.\n \n **Resolve governance fields from the base snapshot only.** Rejected against the\n registration flow: implementation PRs add their own contract, so the base has no\ndiff --git a/docs/adr/0016-locks-bound-product-paths.md b/docs/adr/SADR-0016-locks-bound-product-paths.md\nindex b5e850f3f1f7f4ef6e7f9d1763a29a51bd300c45..46b40a9b99a7aae644c02bb458f5473ff0a90ce2\n--- a/docs/adr/0016-locks-bound-product-paths.md\n+++ b/docs/adr/SADR-0016-locks-bound-product-paths.md\n@@ -1,4 +1,4 @@\n-# ADR 0016: Locks bound product paths; the Shiki mirror is bound by record identity\n+# SADR-0016: Locks bound product paths; the Shiki mirror is bound by record identity\n \n ## Status\n \n@@ -6,8 +6,8 @@\n \n - Date: 2026-07-28\n - Deciders: operator (mizutani-140), Claude (planner)\n-- Related: ADR 0004 (GitHub-first with a `.shiki` mirror), ADR 0009 (Spec Freeze),\n-  ADR 0012 (autonomous loop closeout to main), ADR 0015 (Contract Approval before\n+- Related: SADR-0004 (GitHub-first with a `.shiki` mirror), SADR-0009 (Spec Freeze),\n+  SADR-0012 (autonomous loop closeout to main), SADR-0015 (Contract Approval before\n   dispatch), remediation goal task R-02, the live PR #179 deadlock.\n \n ## Context\n@@ -90,11 +90,11 @@\n files, documentation files and named source modules are nameable; runtime-\n allocated ledger, runner and report ids are not \u2014 and those live in `.shiki`,\n which locks no longer govern. Existing frozen contracts are brought into line by\n-a one-time Spec Amendment per plan, which must land before ADR 0015's contract\n+a one-time Spec Amendment per plan, which must land before SADR-0015's contract\n immutability makes `locks` unwritable from the implementation PR.\n \n **Lock state is resolved from the durable side.** MergeGate resolves the lock\n-registry, and ADR 0015's governance fields, from the base `.shiki` snapshot when\n+registry, and SADR-0015's governance fields, from the base `.shiki` snapshot when\n present, falling back to head only when absent. The `MergeGate metadata check`\n job is given `--base-shiki`, which it does not receive today. Locks are not\n derived from the live open-pull-request set: a pull request body is untrusted\n@@ -108,7 +108,7 @@\n Explicitly out of scope: moving MergeGate's contract resolution off the PR\n checkout wholesale; making the mirror a projection of GitHub state; introducing\n new lock kinds; moving ledger evidence out of the repository; and unwinding\n-ADR 0012's closeout pull request. Registration of multi-task goals is ADR 0015's\n+SADR-0012's closeout pull request. Registration of multi-task goals is SADR-0015's\n Contract PR and is not re-decided here.\n \n ## Consequences\n@@ -127,7 +127,7 @@\n \n Risks accepted. First, goal-scoped identity is only as strong as `goal_id`,\n which is resolved from the PR body and the head task file; the mitigation is to\n-add `goal_id` to ADR 0015's frozen governance field set, and until that lands a\n+add `goal_id` to SADR-0015's frozen governance field set, and until that lands a\n task file can name a foreign goal. Second, base-when-present resolution is\n unavailable on fork pull requests, where the head is contributor-controlled;\n fork pull requests already cannot complete a Shiki merge, and this decision does\n@@ -139,7 +139,7 @@\n path, not by implementation pull requests.\n \n New obligations: one Spec Amendment per frozen plan to rewrite `locks`, ordered\n-before ADR 0015 T1; extension of the adversarial suites in\n+before SADR-0015 T1; extension of the adversarial suites in\n `scripts/test_shiki_adversarial_state.sh` and\n `scripts/test_shiki_governance_evidence.sh` to cover the eleven newly-scoped\n directories; and updating `scripts/test_shiki_loop_lock_guard.sh`,\n@@ -151,32 +151,32 @@\n whether the loop's transitive sync of ledger `evidence` references can be\n deleted once the Contract PR carries its own source plan.\n \n-## Relationship to ADR 0004\n+## Relationship to SADR-0004\n \n-This ADR **clarifies something ADR 0004 left unspecified**. ADR 0004 decides that\n+This SADR **clarifies something SADR-0004 left unspecified**. SADR-0004 decides that\n GitHub is the operational source of truth and that each target repository keeps a\n `.shiki/` mirror. It says nothing about pull requests writing the mirror, does\n not name MergeGate, and does not make the mirror an input to a gate. Nothing in\n-ADR 0004's Decision or Consequences is amended, superseded or weakened: the\n+SADR-0004's Decision or Consequences is amended, superseded or weakened: the\n mirror still exists, still records the same artifacts, and pull request\n-boundaries remain the coordination surface. What this ADR settles is the question\n+boundaries remain the coordination surface. What this SADR settles is the question\n 0004 never reached \u2014 by what authority a pull request may write a mirror record \u2014\n and it answers it the way `.shiki/manifest.json` already declares\n (`pr_mutation: current-task-or-current-goal-only`).\n \n-It **amends ADR 0012**, whose Decision item 5 states that \"Loop-executed tasks\n+It **amends SADR-0012**, whose Decision item 5 states that \"Loop-executed tasks\n declare the `path:.shiki/**` lock, which covers every `.shiki` file the closeout\n stages ... so `files_outside_locks` is satisfied without per-file lock\n bookkeeping.\" That clause is replaced: the closeout's mirror writes are\n-authorized by record identity, not by a declared lock. ADR 0012's substantive\n+authorized by record identity, not by a declared lock. SADR-0012's substantive\n decision \u2014 that completion reaches `main` through a closeout pull request rather\n than local attestation \u2014 is preserved unchanged.\n \n-It is **complementary to ADR 0015**, not a substitute. ADR 0015 owns contract\n+It is **complementary to SADR-0015**, not a substitute. SADR-0015 owns contract\n immutability, the pre-dispatch gate, the Contract PR and the Scope Envelope. This\n-ADR owns what a lock means and who may write the mirror. The one ordering\n+SADR owns what a lock means and who may write the mirror. The one ordering\n constraint between them is that the lock-narrowing Spec Amendment must precede\n-ADR 0015 T1's freezing of the `locks` field.\n+SADR-0015 T1's freezing of the `locks` field.\n \n ## Alternatives Considered\n \n@@ -186,7 +186,7 @@\n the only writer that can register them may run. Migrating the Guardian signal to\n that store makes every registration PR fail closed to `needs_guardian` at every\n risk level, because the task file is by definition absent from the store, which\n-removes the low/medium autonomous path ADR 0008/0009 exists to provide. And it\n+removes the low/medium autonomous path SADR-0008/0009 exists to provide. And it\n leaves schemas, manifest, config, guardian policy, templates, migrations, plans,\n worktrees, runner records, runs and handoffs with no writer at all in a\n repository whose product is `.shiki`. Measured benefit on the frozen backlog:\n@@ -199,14 +199,14 @@\n namespace is not in fact ownership-partitioned \u2014 56 ledger entries carry no\n `task_id`, and one goal DAG is writable by 35 registered tasks. The correct\n expression of its insight is that locks do not govern the mirror at all, which is\n-what this ADR decides.\n+what this SADR decides.\n \n **Make the mirror a projection written only by a registration writer and a\n post-merge projection job.** Rejected. It requires a bypass actor with write\n access to the protected default branch; it makes its own master invariant\n readable from a manifest that pull requests may edit; it deletes both reconcile\n modes and leaves a corrupted mirror with no in-band repair path, contradicting\n-ADR 0004's consequence that mirror conflicts are repaired against GitHub state;\n+SADR-0004's consequence that mirror conflicts are repaired against GitHub state;\n and its content-derived ids are rejected by `validate_shiki`'s currently-green id\n pattern.\n \ndiff --git a/docs/adr/0017-bookkeeping-closeouts-do-not-inherit-risk.md b/docs/adr/SADR-0017-bookkeeping-closeouts-do-not-inherit-risk.md\nindex 554e3a0d526a72ca067108c5464cc68c3edcf174..b9676857a4713850e063af7c39c8e0b01ecb4c66\n--- a/docs/adr/0017-bookkeeping-closeouts-do-not-inherit-risk.md\n+++ b/docs/adr/SADR-0017-bookkeeping-closeouts-do-not-inherit-risk.md\n@@ -1,4 +1,4 @@\n-# ADR 0017: A Bookkeeping Closeout Does Not Inherit Its Task's Risk\n+# SADR-0017: A Bookkeeping Closeout Does Not Inherit Its Task's Risk\n \n ## Status\n \n@@ -6,7 +6,7 @@\n \n ## Context\n \n-ADR 0012 made the goal loop push a task's completion to the default branch through a\n+SADR-0012 made the goal loop push a task's completion to the default branch through a\n closeout pull request: after the implementation PR merges, the loop opens a second PR\n carrying `task.status = done`, `lock.state = released`, and \u2014 when the task completes\n its goal \u2014 `goal.status = complete` plus one scorecard report. Nothing else. The\n@@ -49,14 +49,14 @@\n 4. The task file's governance fields (`scope`, `non_goals`, `required_skills`,\n    `risk_level`, `locks`, `acceptance_checks`, `test_command`) are byte-identical to\n    the base snapshot. Only `status`, `expected_pr`, `closeout_pr`, `ledger_evidence`\n-   and `expected_branch` may differ \u2014 the ADR 0015 contract-immutability rule,\n+   and `expected_branch` may differ \u2014 the SADR-0015 contract-immutability rule,\n    unchanged.\n \n    `expected_branch` is in that set because a closeout is *by construction* on a\n    different branch from the implementation it closes, and the loop rewrites the\n    field accordingly. It is safe to let it differ because MergeGate independently\n    requires `expected_branch` to equal the PR's actual head ref, so a PR cannot set\n-   it to anything other than the branch it is really on. The first draft of this ADR\n+   it to anything other than the branch it is really on. The first draft of this SADR\n    omitted it, which made the exemption unreachable: every real closeout failed\n    condition 4 on a field the loop always changes. See PR #209.\n 5. The state transitions are exactly the terminal set: task `review` \u2192 `done`, lock\n@@ -113,12 +113,12 @@\n classification must be derived, not asserted.\n \n **Fold the closeout into the implementation PR** so no second PR exists. Rejected under\n-ADR 0012, which recorded why: a task file that is already `done` on the implementation\n+SADR-0012, which recorded why: a task file that is already `done` on the implementation\n branch short-circuits the loop's review \u2192 CCA \u2192 merge \u2192 done ordering into a local\n goal-completion before any merge happens.\n \n **Have the loop auto-merge closeouts with elevated credentials.** Rejected outright: that\n-is a runtime authoring its own approval, which ADR 0010 and the constitution's\n+is a runtime authoring its own approval, which SADR-0010 and the constitution's\n approval-record prohibition forbid. It would also be indistinguishable, in the evidence,\n from the forgery those rules exist to prevent.\n \ndiff --git a/docs/adr/0018-carried-guardian-approval-across-a-proven-base-sync.md b/docs/adr/SADR-0018-carried-guardian-approval-across-a-proven-base-sync.md\nindex 22358562da390ed8b45cd600da08a43d378b4680..e6e2907cefa776460af73ddf7d39db605f3c39b0\n--- a/docs/adr/0018-carried-guardian-approval-across-a-proven-base-sync.md\n+++ b/docs/adr/SADR-0018-carried-guardian-approval-across-a-proven-base-sync.md\n@@ -1,4 +1,4 @@\n-# ADR 0018: Carried Guardian Approval Across A Proven Base Sync\n+# SADR-0018: Carried Guardian Approval Across A Proven Base Sync\n \n ## Status\n \n@@ -34,7 +34,7 @@\n Guardian evaluator that consumes it (`shiki_guardian.evaluate_guardian_approval`,\n the `guardian_comment_carried` source) were built and tested in isolation. Neither\n does anything until a caller passes the flags that turn the carry on, so the\n-feature was invisible in CI. This ADR records the decision the two of them\n+feature was invisible in CI. This SADR records the decision the two of them\n implement, and this task wires it into the four CI invocations that judge Guardian\n approval.\n \ndiff --git a/docs/adr/README.md b/docs/adr/README.md\nindex c70e057a4520c36e042ab68c53cd4a21ef709b71..5da43856836e2ce6a150f8127766e2f88355ab4b\n--- a/docs/adr/README.md\n+++ b/docs/adr/README.md\n@@ -1,40 +1,41 @@\n-# Architecture Decision Records\n+# Decision Record Namespaces\n \n-This directory holds Shiki's Architecture Decision Records (ADRs): durable\n-records of hard-to-reverse platform decisions. ADRs are part of the Shiki source\n-of truth alongside GitHub state, the `.shiki/` mirror, and `CONTEXT.md`.\n+This directory holds two independent decision namespaces:\n \n-## How ADRs Work\n+- Shiki Architecture Decision Records (SADRs), named\n+  `SADR-NNNN-<short-slug>.md`, record hard-to-reverse Shiki platform decisions.\n+- Target Architecture Decision Records (ADRs), named\n+  `NNNN-<short-slug>.md`, belong to the target product.\n \n-- New ADRs are added as `docs/adr/NNNN-<short-slug>.md`, where `NNNN` is the next\n-  zero-padded sequence number.\n-- The **canonical list of ADRs is the set of files matching `docs/adr/*.md`** in\n-  this directory. Browse `docs/adr/*.md` for the authoritative, current set.\n-- Use [`template.md`](template.md) as the starting point for a new ADR.\n-- Record an ADR when a decision is hard to reverse, surprising without context,\n-  and a real tradeoff. Do not write an ADR for routine or easily reversible\n-  choices.\n+The same four-digit number may appear once in each namespace. A platform\n+`SADR-NNNN` citation resolves only to `SADR-NNNN-*.md`; a target `ADR NNNN`\n+citation resolves only to `NNNN-*.md`.\n \n-Using the `docs/adr/*.md` glob as the source of truth means other PRs can add an\n-ADR without editing this index file, avoiding merge contention on a\n-hand-maintained enumeration.\n+## How Decision Records Work\n \n-## Current ADRs (snapshot)\n-\n-The list below is a convenience snapshot and may lag behind the directory. When\n-in doubt, list `docs/adr/*.md`.\n+- New Shiki platform decisions use the next zero-padded SADR number.\n+- New target product decisions use the target's own ADR sequence.\n+- The canonical list is the files in `docs/adr/`; no hand-maintained index is\n+  authoritative.\n+- Use [`template.md`](template.md) as the starting point and choose the filename\n+  namespace that owns the decision.\n+- Record a decision when it is hard to reverse, surprising without context, and\n+  a real tradeoff. Do not write a decision record for routine or easily\n+  reversible choices.\n \n-- [0001 \u2014 Keep Shiki As The Platform Name](0001-keep-shiki-as-platform-name.md)\n-- [0002 \u2014 Use AGENTS As The Shared Agent Constitution](0002-use-agents-as-shared-constitution.md)\n-- [0003 \u2014 Require Skill Gate For MergeGate Readiness](0003-require-skill-gate-for-mergegate-readiness.md)\n-- [0004 \u2014 Use GitHub First With A Repository-Local Shiki Mirror](0004-use-github-first-with-shiki-mirror.md)\n-- [0005 \u2014 Use Codex As Front And Claude Code Action As The GitHub Runtime](0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md)\n-- [0006 \u2014 Separate Codex Implementation, CCA Completion, And MergeGate Authorization](0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md)\n+Using the directory as the source of truth lets independent changes add records\n+without contending on an enumerated index.\n \n-## Adding A New ADR\n+## Adding A Shiki SADR\n \n-1. Copy `template.md` to `docs/adr/NNNN-<short-slug>.md`.\n+1. Copy `template.md` to `docs/adr/SADR-NNNN-<short-slug>.md`.\n 2. Fill in the title, status, context, decision, and consequences.\n-3. Set the status to `Proposed`, then `Accepted` once the decision is approved.\n-4. Open a PR following `CONTRIBUTING.md`. You do not need to edit this index for\n-   the ADR to be discoverable via `docs/adr/*.md`.\n+3. Set the status to `Proposed`, then `Accepted` once the proper authority has\n+   approved the decision.\n+4. Open a PR following `CONTRIBUTING.md`.\n+\n+## Adding A Target ADR\n+\n+1. Copy `template.md` to `docs/adr/NNNN-<short-slug>.md` using the target's own\n+   sequence.\n+2. Cite it as `ADR NNNN`; do not prefix its filename with `SADR-`.\ndiff --git a/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md b/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\nnew file mode 100644\nindex 0000000000000000000000000000000000000000..7455ce1f3bed02ddddb851ac21a1d1a648c9aeba\n--- /dev/null\n+++ b/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\n@@ -0,0 +1,55 @@\n+# SADR-0019: Separate Shiki Platform SADRs From Target ADRs\n+\n+## Status\n+\n+Accepted\n+\n+## Context\n+\n+Shiki installs its platform decision records into `docs/adr/` in every target\n+repository. Shiki and its targets previously used the same numeric\n+`NNNN-<slug>.md` namespace. A target that already owned ADRs 0013 through 0018\n+therefore acquired two records for each number during installation, making an\n+`ADR NNNN` citation ambiguous. Renumbering target decisions would rewrite target\n+history, while renumbering Shiki decisions per target would destroy their stable\n+platform identity.\n+\n+Upgrades also need to remove the former Shiki-owned numeric paths without ever\n+mistaking a target-owned ADR for platform content. The install stamp already\n+records exact shipped paths and content digests, so it can prove ownership only\n+when both still match.\n+\n+## Decision\n+\n+Shiki platform decisions use the `SADR` namespace:\n+\n+- records are named `docs/adr/SADR-NNNN-<slug>.md`;\n+- live platform citations use `SADR-NNNN`;\n+- target product decisions retain `docs/adr/NNNN-<slug>.md` and `ADR NNNN`;\n+- the reference validator resolves the two namespaces independently, so the same\n+  four-digit number may exist once in each namespace.\n+\n+Before a forced upgrade writes anything, the installer inventories the exact\n+former numeric Shiki ADR paths. It may delete a legacy path only when the\n+existing install stamp contains that exact path and its recorded SHA-256 digest\n+matches the file currently on disk. All ownership blockers are reported\n+together and abort the upgrade before its first write. A non-forced install\n+never performs this cleanup.\n+\n+Historical `.shiki/` evidence remains unchanged because it records the namespace\n+that existed when that evidence was produced.\n+\n+## Consequences\n+\n+- Targets may use any ADR number without colliding with Shiki's platform SADRs.\n+- Fresh installs preserve target ADR bytes and install a stable SADR set.\n+- Forced upgrades fail closed when legacy ownership cannot be proven, and a\n+  successful upgrade refreshes the stamp without removed legacy paths.\n+- Current governance, packaging, workflow, prompt, script, and skill references\n+  must move with the platform records.\n+\n+## Alternatives Considered\n+\n+- Collision-only refusal leaves affected targets permanently unupgradable.\n+- Renumbering target ADRs mutates target-owned history.\n+- Per-target Shiki renumbering makes platform citations non-portable.\ndiff --git a/docs/adr/template.md b/docs/adr/template.md\nindex a92a97daddd0b00ab9dd626cc8d86942da2eafed..17dd79175b5fee2105935557f691b40cf9492942\n--- a/docs/adr/template.md\n+++ b/docs/adr/template.md\n@@ -1,14 +1,14 @@\n-# NNNN: <Short Decision Title>\n+# <SADR-NNNN or NNNN>: <Short Decision Title>\n \n ## Status\n \n-Proposed | Accepted | Superseded by ADR-NNNN\n+Proposed | Accepted | Superseded by SADR-NNNN or target ADR NNNN\n \n ## Context\n \n What is the situation, constraint, or force that requires a decision? Reference\n-relevant domain terms (`CONTEXT.md`), prior ADRs, and code reality. State the\n-problem, not the solution.\n+relevant domain terms (`CONTEXT.md`), prior Shiki SADRs or target ADRs, and code\n+reality. State the problem, not the solution.\n \n ## Decision\n \ndiff --git a/docs/agents/autonomous-loop-e2e-acceptance.md b/docs/agents/autonomous-loop-e2e-acceptance.md\nindex bbf84c6081987f159ac6ad9ecf3c82b7aa72e7e8..5c13c1d02a05a90b0dc46cbef7c95a9846d0c237\n--- a/docs/agents/autonomous-loop-e2e-acceptance.md\n+++ b/docs/agents/autonomous-loop-e2e-acceptance.md\n@@ -40,8 +40,8 @@\n 3. `gh auth status` is logged in with merge permission on the repo.\n 4. Branch protection + required checks are configured exactly as the goal-loop\n    required-check set expects (see `docs/agents/branch-protection-smoke.md`).\n-5. The repo `.shiki/policy` permits autonomous low/medium-risk merges (ADR\n-   0008/0009); the MergeGate policy check is the recorded-authority gate.\n+5. The repo `.shiki/policy` permits autonomous low/medium-risk merges\n+   (SADR-0008/0009); the MergeGate policy check is the recorded-authority gate.\n \n The operator's only actions are: register the task and start the loop. **No\n manual pre-clearing** means after `shiki loop run` starts, the operator does not\ndiff --git a/docs/agents/bootstrap-command.md b/docs/agents/bootstrap-command.md\nindex 05f7f203a52cf412b9f97084a6e131ed0f785afd..f7aa0b734dfebee4a3b139335b66ee3415f48713\n--- a/docs/agents/bootstrap-command.md\n+++ b/docs/agents/bootstrap-command.md\n@@ -189,7 +189,9 @@\n ```\n \n Use this only for tests, fixtures, or explicit local-only template inspection.\n-Use `--force` only when you intentionally want to overwrite existing target files.\n+Use `--force` only when you intentionally want to refresh the target template.\n+Legacy numeric Shiki decision cleanup is stamp-and-digest authorized before any\n+write; target ADR paths are not cleanup candidates.\n \n ## Slash Command\n \ndiff --git a/docs/agents/checklists.md b/docs/agents/checklists.md\nindex 4a7f03ade346f53a1c1d82981f3fd744f9ab4547..0c5ec6c53dc7197b7b7a7d18e667b325babd6c45\n--- a/docs/agents/checklists.md\n+++ b/docs/agents/checklists.md\n@@ -32,18 +32,18 @@\n | ID | Item | Blocking |\n | --- | --- | --- |\n | GD-01 | Plan was challenged against `CONTEXT.md`. | blocking for non-trivial Goals |\n-| GD-02 | Relevant ADRs were read or absence was recorded. | blocking for non-trivial Goals |\n+| GD-02 | Relevant SADRs and target ADRs were read or absence was recorded. | blocking for non-trivial Goals |\n | GD-03 | Ambiguous terms were resolved or converted to HITL blockers. | blocking |\n | GD-04 | Edge scenarios were considered. | advisory |\n | GD-05 | Code reality was checked when code could answer. | blocking when applicable |\n | GD-06 | New domain terms were added to `CONTEXT.md` when needed. | blocking when applicable |\n-| GD-07 | ADR was created or explicitly skipped using the ADR criteria. | blocking when applicable |\n+| GD-07 | A SADR or target ADR was created or explicitly skipped using the decision-record criteria. | blocking when applicable |\n \n ## CI \u2014 Context & Impact Checklist\n \n | ID | Item | Blocking |\n | --- | --- | --- |\n-| CI-01 | Relevant docs and ADRs identified. | blocking |\n+| CI-01 | Relevant docs, SADRs, and target ADRs identified. | blocking |\n | CI-02 | Relevant modules, interfaces, seams, callers, and tests identified. | blocking |\n | CI-03 | Dependencies and blocked-by relationships identified. | blocking |\n | CI-04 | Candidate locks identified. | blocking |\n@@ -62,7 +62,7 @@\n | PRD-04 | Implementation decisions are recorded without volatile code detail. | blocking |\n | PRD-05 | Testing decisions are recorded. | blocking |\n | PRD-06 | Out-of-scope items are explicit. | blocking |\n-| PRD-07 | Relevant glossary/ADR links are present when applicable. | blocking |\n+| PRD-07 | Relevant glossary/SADR/target-ADR links are present when applicable. | blocking |\n \n ## SF \u2014 Spec Freeze Checklist\n \n@@ -98,7 +98,7 @@\n | H-02 | Branch/worktree target is present. | blocking |\n | H-03 | Scope and non-goals are present. | blocking |\n | H-04 | Dependencies and locks are present. | blocking |\n-| H-05 | Relevant docs, ADRs, modules, and tests are referenced. | blocking |\n+| H-05 | Relevant docs, SADRs, target ADRs, modules, and tests are referenced. | blocking |\n | H-06 | Required skills are stated. | blocking |\n | H-07 | TDD expectations are stated. | blocking when behavior work |\n | H-08 | Acceptance criteria are included. | blocking |\ndiff --git a/docs/agents/codex-handoff.md b/docs/agents/codex-handoff.md\nindex ede468f47cb277f69a857e8c45df703e4ed0151d..6f97bdffb67e44d7edea2c71e01ea28933c4eb6c\n--- a/docs/agents/codex-handoff.md\n+++ b/docs/agents/codex-handoff.md\n@@ -46,7 +46,7 @@\n \n ## Context\n - Domain terms:\n-- Relevant ADRs:\n+- Relevant SADRs and target ADRs:\n - Relevant docs:\n - Relevant modules/interfaces/seams:\n - Similar tests/prior art:\ndiff --git a/docs/agents/completion-check-agent.md b/docs/agents/completion-check-agent.md\nindex 13e4c035db35025f4e3cdcc5adfb24314ac4d521..61bfa1218cf36ca605250c91919e620f33238aa0\n--- a/docs/agents/completion-check-agent.md\n+++ b/docs/agents/completion-check-agent.md\n@@ -32,7 +32,7 @@\n - `AGENTS.md`\n - `CLAUDE.md`\n - `CONTEXT.md`\n-- relevant ADRs\n+- relevant SADRs and target ADRs\n - parent Goal issue\n - PRD issue or document\n - task issue\ndiff --git a/docs/agents/decision-control.md b/docs/agents/decision-control.md\nindex 7d8ee5ab03a5ccbcbd04056ae61de03785a2a9c9..8c94ecda00fffaa0e92c325ab83aa9776bd1dac1\n--- a/docs/agents/decision-control.md\n+++ b/docs/agents/decision-control.md\n@@ -11,7 +11,7 @@\n | Decision | Owner | When | Required inputs | Output | Enforcement surface |\n | --- | --- | --- | --- | --- | --- |\n | Is the Goal clear enough to plan? | Planner + Guardian when needed | Goal intake | User request, existing docs, risk signals | Goal readiness verdict | Goal checklist, issue template, ledger |\n-| Must `grill-with-docs` run? | Planner / Skill Gate | Before PRD or issue split | Ambiguity, domain terms, ADR conflicts, unclear boundaries | Skill invocation or explicit skip rationale | Skill Gate checklist |\n+| Must `grill-with-docs` run? | Planner / Skill Gate | Before PRD or issue split | Ambiguity, domain terms, SADR/target-ADR conflicts, unclear boundaries | Skill invocation or explicit skip rationale | Skill Gate checklist |\n | Is a PRD required? | Planner | After Goal context stabilizes | Goal, `grill-with-docs` output, Context & Impact output | PRD required / not required | PRD checklist, CCA evidence check |\n | Can the spec be frozen? | Operator only | End of Requirements Definition, before issue split | PRD/requirements summary, scope inventory (SF-02), open-question list | `spec_freeze` block in the plan + ledger evidence | `require_grilled_plan` (plan ingest/run), SF checklist |\n | Can a frozen spec change? | Operator only (Spec Amendment) | When implementation contradicts the frozen spec | Contested decisions, scoped re-grill result | recorded amendment + re-stamped freeze | SF checklist, ledger evidence |\ndiff --git a/docs/agents/domain.md b/docs/agents/domain.md\nindex 258da8ea81e87ac0a6b0b5b5e5c2fde980eaabb7..d57b0cf17ff3e1fa9e6e5885372bf083d8295421\n--- a/docs/agents/domain.md\n+++ b/docs/agents/domain.md\n@@ -5,7 +5,9 @@\n ## Layout\n \n - `CONTEXT.md` defines Shiki language. Keep it implementation-free.\n-- `docs/adr/` records hard-to-reverse decisions.\n+- `docs/adr/SADR-NNNN-*.md` records Shiki platform decisions.\n+- `docs/adr/NNNN-*.md` records target product decisions without sharing the\n+  SADR namespace.\n - `AGENTS.md` is the runtime-neutral agent constitution.\n - `CLAUDE.md` is the Claude Code wrapper around `AGENTS.md`.\n - `.shiki/` is the Target Repository mirror for Goals, PRDs, plans, Task DAGs, contracts, locks, worktree records, ledger entries, CCA verdicts, repair packets, reports, and handoffs.\n@@ -15,6 +17,6 @@\n \n - Use Shiki terms exactly when they exist.\n - Add glossary terms to `CONTEXT.md` only when the term is part of the domain, not a generic programming concept.\n-- Create ADRs only for hard-to-reverse, surprising, tradeoff-based decisions.\n+- Create SADRs or target ADRs only for hard-to-reverse, surprising, tradeoff-based decisions.\n - Keep product-specific language in the Target Repository, not in Shiki Template docs.\n-- If an ADR conflicts with a proposed implementation, surface the conflict before editing.\n+- If a SADR or target ADR conflicts with a proposed implementation, surface the conflict before editing.\ndiff --git a/docs/agents/execution-control.md b/docs/agents/execution-control.md\nindex ad8ee3cdb36be56fedc1f46408ca92b387d74047..bc63c3a73661d12b567016ba299f1a6b66bf9088\n--- a/docs/agents/execution-control.md\n+++ b/docs/agents/execution-control.md\n@@ -63,7 +63,7 @@\n | Branch name | Orchestrator | Orchestrator | Before implementer dispatch | branch policy | branch ref in task/ledger |\n | Lock reservation | Context & Impact | MergeGate | Before dispatch and before merge | `.shiki/locks/`, labels, PR metadata | lock id, owner, scope |\n | CI profile | Planner | MergeGate | During issue triage, rechecked at PR | labels, `.shiki/policy.yaml`, workflow matrix | required check list |\n-| Required status checks | Repo maintainer / Guardian | GitHub branch protection / rulesets | Repository setup, updated via ADR | GitHub settings | branch/ruleset config, ADR |\n+| Required status checks | Repo maintainer / Guardian | GitHub branch protection / rulesets | Repository setup, updated via target ADR | GitHub settings | branch/ruleset config, target ADR |\n | CCA judgment | CCA | GitHub required check / MergeGate | Every non-draft PR update | GitHub Action check | structured CCA verdict |\n | Repair assignment | CCA | MergeGate | After failed CCA/CI/review | repair packet, issue/PR comment | repair id and attempt count |\n | Merge readiness | MergeGate | GitHub branch protection / Guardian if needed | After CCA complete | required checks, review rules, rulesets | mergegate check output |\ndiff --git a/docs/agents/guardian-policy.md b/docs/agents/guardian-policy.md\nindex 4147c7adf05a47adb4fd24ada1a1c2e1f2ba6d2d..a20d9cc6c00a569bb057b9ca8d6b51c7fc4a9847\n--- a/docs/agents/guardian-policy.md\n+++ b/docs/agents/guardian-policy.md\n@@ -98,8 +98,8 @@\n `origin/<default_branch>` and reuses each job's existing base fetch; a shallow\n (`--depth`) or second base fetch would make the proof unresolvable and silently\n disable the carry, so neither is permitted. CCA reads `guardian_comment_carried` as\n-a recorded authority in the deterministic signal and does not re-derive it. See ADR\n-0018 for the full decision, the rejected diff/patch-equality and path-disjointness\n+a recorded authority in the deterministic signal and does not re-derive it. See\n+SADR-0018 for the full decision, the rejected diff/patch-equality and path-disjointness\n alternatives, and the accepted residual.\n \n ## External AI Guardian Review (SADR-0010)\ndiff --git a/docs/agents/implementation-policy.md b/docs/agents/implementation-policy.md\nindex c754144a9fb5bf4a38aa82a60e1670c59e626b44..85c37281bcad196c6b7b02343faf00b03cd9c57a\n--- a/docs/agents/implementation-policy.md\n+++ b/docs/agents/implementation-policy.md\n@@ -31,10 +31,13 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n ## Non-Negotiables\n \n 1. **Planning precedes implementation.** Non-trivial Goals start with `grill-with-docs` and Context & Impact.\n-2. **Domain language is controlled.** Terms must be checked against `CONTEXT.md`; hard-to-reverse tradeoffs must be checked against ADRs.\n+2. **Domain language is controlled.** Terms must be checked against `CONTEXT.md`; hard-to-reverse tradeoffs must be checked against Shiki SADRs and target ADRs.\n 3. **PRD precedes issue decomposition.** `to-prd` is used once the context is settled enough to become durable intent.\n 4. **Issues are vertical slices.** `to-issues` must produce independently grabbable tracer-bullet tasks, not horizontal layer tickets.\n 5. **Triage controls dispatch.** Only `ready-for-agent` / AFK tasks may be assigned to Codex without human input.\n@@ -95,19 +98,19 @@\n Process:\n \n - Challenge terms against `CONTEXT.md`.\n-- Check ADRs before reopening decisions.\n+- Check SADRs and target ADRs before reopening decisions.\n - Inspect code when code can answer a question.\n - Ask one question at a time when the operator is available.\n - Provide a recommended answer with each question.\n - Record resolved terms in `CONTEXT.md`.\n-- Create ADRs only for hard-to-reverse, surprising, tradeoff-based decisions.\n+- Create SADRs or target ADRs only for hard-to-reverse, surprising, tradeoff-based decisions.\n - Convert unresolved load-bearing questions into HITL blockers.\n \n Outputs:\n \n - Resolved design-tree decisions.\n - Updated glossary terms when needed.\n-- ADRs when justified.\n+- SADRs or target ADRs when justified.\n - HITL blockers when decisions remain unresolved.\n \n ## Phase 3 \u2014 Context & Impact\n@@ -117,7 +120,7 @@\n \n Outputs must include:\n \n-- Relevant documents and ADRs.\n+- Relevant documents, SADRs, and target ADRs.\n - Relevant modules, interfaces, seams, callers, and tests.\n - Candidate locks.\n - Dependencies.\n@@ -140,7 +143,7 @@\n - Testing decisions.\n - Out of scope.\n - Further notes.\n-- Links to relevant Goal, domain terms, and ADRs.\n+- Links to relevant Goal, domain terms, SADRs, and target ADRs.\n \n Phase 4 ends with Spec Freeze: the operator explicitly approves the PRD, the\n plan records a `spec_freeze` block (status, approved_by, source), and the SF\n@@ -240,7 +243,7 @@\n - Dependencies and locks.\n - Risk and Guardian requirements.\n - Ledger completeness.\n-- Documentation and ADR updates.\n+- Documentation and SADR/target-ADR updates.\n - Scope drift.\n \n CCA emits one of:\n@@ -301,7 +304,7 @@\n | Evidence failure | Behavior might be done, but proof is missing. | Add checks, PR evidence, ledger entry, or artifacts. |\n | Scope drift | PR changes unrelated behavior. | Revert unrelated changes or split into new Goal. |\n | Dependency block | Upstream task or lock not complete. | Block until dependency resolves. |\n-| Architecture block | Decision needs ADR or deep-module discussion. | `grill-with-docs` or `improve-codebase-architecture`. |\n+| Architecture block | Decision needs a SADR, target ADR, or deep-module discussion. | `grill-with-docs` or `improve-codebase-architecture`. |\n | Risk block | Security/production/policy change needs human approval. | Guardian decision. |\n \n ## Phase 5.5 \u2014 Execution Decision Control\ndiff --git a/docs/agents/runtime-auth-model.md b/docs/agents/runtime-auth-model.md\nindex 6a2e1a47e81ea89b5c68a210ec07772a9374babc..3c54748d24233dbd0ba8473950282547ef4ce935\n--- a/docs/agents/runtime-auth-model.md\n+++ b/docs/agents/runtime-auth-model.md\n@@ -148,7 +148,7 @@\n \n API-key based runners may be added by a target repository as an explicit extension. They are not the default Shiki template path.\n \n-If a repo opts into API-key mode, record the exception in an ADR and update the repo's `AGENTS.md`, `.shiki/config.yaml`, and workflow permissions.\n+If a repo opts into API-key mode, record the target-specific exception in a target ADR and update the repo's `AGENTS.md`, `.shiki/config.yaml`, and workflow permissions.\n \n ## GitHub Actions Runtime Compatibility\n \ndiff --git a/docs/agents/skill-gate.md b/docs/agents/skill-gate.md\nindex c50398b0d7c60a8598f2a5c88a10015730dced13..3acffe6444617bb1d54b77d4b4708158ec735bc6\n--- a/docs/agents/skill-gate.md\n+++ b/docs/agents/skill-gate.md\n@@ -9,7 +9,7 @@\n | Skill | Trigger | Expected output |\n | --- | --- | --- |\n | `setup-matt-pocock-skills` | First configuring a repo for these skills, or when issue tracker, triage labels, or domain docs are missing. | `docs/agents/` configuration and an `Agent skills` block in `AGENTS.md` or `CLAUDE.md`. |\n-| `grill-with-docs` | Ambiguous plan, unclear terminology, boundary decision, tradeoff, or ADR-worthy design choice. | Clarified decision tree, recommended answers, updates to `CONTEXT.md` or ADRs when decisions crystallize. |\n+| `grill-with-docs` | Ambiguous plan, unclear terminology, boundary decision, tradeoff, or decision-record-worthy design choice. | Clarified decision tree, recommended answers, updates to `CONTEXT.md`, SADRs, or target ADRs when decisions crystallize. |\n | `zoom-out` | Agent lacks a higher-level architectural map. | Map of relevant modules, callers, seams, and domain concepts. |\n | `to-prd` | Settled context should become a PRD. | PRD published to the issue tracker with problem, solution, user stories, decisions, testing, and out-of-scope notes. |\n | `to-issues` | Goal, PRD, or plan must become implementation tickets. | Vertical-slice issues with acceptance criteria, dependencies, and AFK/HITL classification. |\n@@ -17,7 +17,7 @@\n | `tdd` | Feature work or bug fix has observable behavior that can be specified with tests. | Red-green-refactor loop through public interfaces, one vertical slice at a time. |\n | `code-review` | The TDD loop is green and the implementer is about to create or update a PR. Default for every implementation task. | Pre-PR self-review of the task diff with fixes applied on the task branch; findings and resolutions recorded as a ledger entry naming the skill and a `## Pre-PR code review` PR body section. |\n | `diagnose` | Hard bug, failing check, regression, flaky behavior, or performance problem. | Reproduction loop, ranked hypotheses, instrumentation, fix, regression test, post-mortem. |\n-| `improve-codebase-architecture` | Structural friction, testability, AI-navigability, deep-module opportunity, or refactor request. | Architecture candidates and deepening recommendations informed by `CONTEXT.md` and ADRs. |\n+| `improve-codebase-architecture` | Structural friction, testability, AI-navigability, deep-module opportunity, or refactor request. | Architecture candidates and deepening recommendations informed by `CONTEXT.md`, SADRs, and target ADRs. |\n | `prototype` | A throwaway logic or UI prototype is needed to answer a design question. | Clearly marked disposable prototype and captured decision. |\n \n ## MergeGate Interaction\ndiff --git a/docs/operations.md b/docs/operations.md\nindex cf1f452d4c589e38ccd58553bda0ad8684b51eb7..ace48a90ffbec9e5c0a7113bef16a4858bc44b3f\n--- a/docs/operations.md\n+++ b/docs/operations.md\n@@ -89,6 +89,16 @@\n      target history, brought forward by `shiki migrate`, not a template.\n    - **Everything else** (CLI scripts, workflows, schemas, docs) is overwritten.\n \n+   A forced upgrade also migrates the former numeric Shiki decision paths to\n+   `SADR-NNNN-*`. Before the first write, the installer inventories the exact\n+   legacy Shiki paths and authorizes each deletion only when the existing\n+   `.shiki/install-stamp.json` names that path and its SHA-256 digest still\n+   matches the file. An absent, unreadable, malformed, incomplete, or\n+   mismatching stamp reports every blocked path and stops with no filesystem\n+   change. Target-owned `NNNN-*` ADRs are not cleanup candidates. A non-force\n+   install deletes nothing and retains any still-matching legacy stamp entries\n+   so a later forced upgrade can prove ownership.\n+\n    If the target has pending migrations, `--force` refuses and writes nothing \u2014\n    apply migrations first (step 3), then re-run. The run ends with a single\n    summary of every `.new` written, so a half-upgrade is visible.\ndiff --git a/docs/releasing.md b/docs/releasing.md\nindex 10d73828e7dbd64e9d9f6b83bc84d010537ce62a..aafeee8c4d441e1e427efee2ce4f81a647f020e1\n--- a/docs/releasing.md\n+++ b/docs/releasing.md\n@@ -67,7 +67,7 @@\n CLI in `scripts/`. There are no third-party Python runtime dependencies. The CLI\n is intentionally **not** exposed as a `[project.scripts]` console entry point\n because `scripts/shiki.py` is a dependency-free shim and `scripts/` is not\n-packaged as an importable distribution (see the ADR for the rationale).\n+packaged as an importable distribution (see SADR-0007 for the rationale).\n \n Supported install paths:\n \n@@ -155,6 +155,15 @@\n    [docs/operations.md](operations.md#the-new-convention). If the target has\n    pending migrations, `--force` refuses and writes nothing; apply migrations\n    (step 2) first, then re-run.\n+\n+   The SADR namespace upgrade has an additional pre-write gate: each former\n+   numeric Shiki decision path is removed only when the existing install stamp\n+   contains that exact path and a digest matching its current bytes. The\n+   installer aggregates absent/unreadable/malformed-stamp, missing-digest, and\n+   digest-mismatch blockers before raising, so refusal leaves the target tree\n+   byte-identical. Numeric target ADRs are preserved. A successful forced\n+   upgrade installs and stamps `SADR-*` paths and drops the removed legacy paths\n+   from the refreshed stamp.\n 2. Apply repository-local `.shiki/` state migrations with the migrate command:\n \n    ```bash\ndiff --git a/docs/usage.md b/docs/usage.md\nindex b83407ee3970dd395dd893010bc5dbe8bad26051..a130ef6e40dfd8169a5bc701339351097df1a09f\n--- a/docs/usage.md\n+++ b/docs/usage.md\n@@ -67,8 +67,10 @@\n shiki install-target /path/to/target-repo --local-only\n ```\n \n-Use `--force` only when you intentionally want to overwrite existing target\n-files.\n+Use `--force` only when you intentionally want to refresh the target template.\n+For the SADR namespace migration, `--force` deletes only exact legacy Shiki\n+decision paths whose existing install-stamp digest still matches; any ownership\n+blocker stops before the first write. Numeric target ADRs are never candidates.\n \n ## `shiki bootstrap-platform` \u2014 publish the Shiki platform repo\n \ndiff --git a/scripts/guardian_approval_signal.py b/scripts/guardian_approval_signal.py\nindex d059f92b2c82aa2b5d6ca421365537a87886c1cb..16924892714f4f5b9303c0ef0e325a7e70075630\n--- a/scripts/guardian_approval_signal.py\n+++ b/scripts/guardian_approval_signal.py\n@@ -199,8 +199,8 @@\n ) -> Any:\n     \"\"\"Evaluate SADR-0015 Contract Approval for the CCA Guardian signal.\n \n-    The SAME OR that MergeGate applies must reach this deterministic signal: ADR\n-    0015 is explicit that without it, a contract-approved PR still returns\n+    The SAME OR that MergeGate applies must reach this deterministic signal:\n+    SADR-0015 is explicit that without it, a contract-approved PR still returns\n     ``needs_guardian``, the CCA job fails, and the MergeGate policy check never\n     runs \u2014 the gate this change moves would never be reached. This resolves the\n     task the SAME way MergeGate does (the first Shiki task id in the PR body),\ndiff --git a/scripts/shiki_contract_approval.py b/scripts/shiki_contract_approval.py\nindex 941bcb96fdd8b9a85bc0c4f0f04d5dad9d04839c..4a4b418f6527413161db82670bec857a043360e2\n--- a/scripts/shiki_contract_approval.py\n+++ b/scripts/shiki_contract_approval.py\n@@ -142,7 +142,7 @@\n     changed_files_status: Any,\n     registration: Any,\n ) -> ContractApprovalResult:\n-    \"\"\"Whether this PR carries an SADR-0015 Contract Approval.\n+    \"\"\"Whether this PR carries a SADR-0015 Contract Approval.\n \n     The four conditions are evaluated IN ORDER, each failure recorded as a\n     distinct reason string:\ndiff --git a/scripts/shiki_guardian.py b/scripts/shiki_guardian.py\nindex a4ecb79bbda55fd5c8b06adfae2eb99760a03c45..4dd99bbe714e26f421fa19abac7a34ae1b195ecc\n--- a/scripts/shiki_guardian.py\n+++ b/scripts/shiki_guardian.py\n@@ -12,7 +12,7 @@\n # The carry re-derives its proof from git in-process on every judging invocation;\n # it is never read from an artifact, so there is nothing to go stale or be forged.\n # shiki_sync_proof is the ONLY guardian-family module that runs git, and every one\n-# of its invocations is read-only and local (ADR: prove-from-git base sync).\n+# of its invocations is read-only and local (SADR: prove-from-git base sync).\n from shiki_sync_proof import verify_pure_base_sync\n \n GUARDIAN_POLICY_PATH = \".shiki/guardian-policy.json\"\ndiff --git a/scripts/shiki_guardian_review.py b/scripts/shiki_guardian_review.py\nindex dd0f104d457899e0ba5ff1e162011c1e90b6f92b..e10ef58c046324e7ea3a3392feb3048ca77414ce\n--- a/scripts/shiki_guardian_review.py\n+++ b/scripts/shiki_guardian_review.py\n@@ -159,7 +159,7 @@\n     docs_only = _is_docs_only(changed_files)\n     if docs_only and matched == 0:\n         focus.append(\n-            \"Docs/ADR-only change: confirm the change is genuinely documentation/decision-only, introduces no behavior or contract drift, and stays consistent with the constitution and existing ADRs.\"\n+            \"Docs/decision-record-only change: confirm the change is genuinely documentation/decision-only, introduces no behavior or contract drift, and stays consistent with the constitution and existing SADRs and target ADRs.\"\n         )\n \n     # Conservative augmentation: unknown (no category and not docs-only), mixed\ndiff --git a/scripts/shiki_installer.py b/scripts/shiki_installer.py\nindex be064ab7a29ff6836f7f7a1d70500a43e0d11d7b..9fd8eefd137ce7003dad5c0dcb2e48e2cd47ab32\n--- a/scripts/shiki_installer.py\n+++ b/scripts/shiki_installer.py\n@@ -118,6 +118,7 @@\n # what lets a target carry it without a governance-schema change.\n INSTALL_STAMP_PATH = \".shiki/install-stamp.json\"\n INSTALL_STAMP_VERSION = 1\n+LEGACY_SHIKI_SADR_MAX = 18\n \n # Under --force the shipped surface splits three ways, so nothing is lost and\n # nothing is silently stale.\n@@ -237,9 +238,98 @@\n         )\n \n \n+def legacy_shiki_adr_paths() -> tuple[str, ...]:\n+    \"\"\"Former numeric paths for Shiki SADR-0001 through SADR-0018.\n+\n+    The authoritative slugs come from the current SADR filenames. SADR-0019 is\n+    the namespace decision itself and never shipped under a numeric legacy path.\n+    \"\"\"\n+    paths: list[str] = []\n+    for sadr in sorted((ROOT / \"docs\" / \"adr\").glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\")):\n+        number = sadr.name.split(\"-\", 2)[1]\n+        if int(number) <= LEGACY_SHIKI_SADR_MAX:\n+            paths.append(f\"docs/adr/{sadr.name.removeprefix('SADR-')}\")\n+    return tuple(paths)\n+\n+\n+def preflight_legacy_sadr_cleanup(target: Path, *, force: bool) -> tuple[str, ...]:\n+    \"\"\"Authorize exact legacy Shiki ADR removals before the first write.\n+\n+    A non-force install never removes existing files. A forced install needs no\n+    stamp when no legacy Shiki path is present. When legacy paths do exist, each\n+    must be named in a readable install stamp and its current bytes must match\n+    the recorded digest.\n+    \"\"\"\n+    if not force:\n+        return ()\n+    present = tuple(\n+        relative\n+        for relative in legacy_shiki_adr_paths()\n+        if (target / relative).exists() or (target / relative).is_symlink()\n+    )\n+    if not present:\n+        return ()\n+\n+    stamp_path = target / INSTALL_STAMP_PATH\n+    try:\n+        raw = stamp_path.read_text(encoding=\"utf-8\")\n+        stamp = json.loads(raw)\n+    except FileNotFoundError:\n+        detail = \"install stamp is absent\"\n+        blockers = [f\"{relative}: {detail}\" for relative in present]\n+    except (OSError, UnicodeDecodeError) as error:\n+        detail = f\"install stamp is unreadable ({error})\"\n+        blockers = [f\"{relative}: {detail}\" for relative in present]\n+    except json.JSONDecodeError as error:\n+        detail = f\"install stamp is malformed ({error})\"\n+        blockers = [f\"{relative}: {detail}\" for relative in present]\n+    else:\n+        if not isinstance(stamp, dict):\n+            blockers = [\n+                f\"{relative}: install stamp is malformed (expected an object)\"\n+                for relative in present\n+            ]\n+        else:\n+            digests = stamp.get(\"digests\")\n+            blockers = []\n+            if not isinstance(digests, dict):\n+                blockers.extend(\n+                    f\"{relative}: install stamp has no digest for this path\"\n+                    for relative in present\n+                )\n+            else:\n+                for relative in present:\n+                    expected = digests.get(relative)\n+                    if not isinstance(expected, str) or not expected:\n+                        blockers.append(\n+                            f\"{relative}: install stamp has no digest for this path\"\n+                        )\n+                        continue\n+                    try:\n+                        actual = _sha256_file(target / relative)\n+                    except OSError as error:\n+                        blockers.append(\n+                            f\"{relative}: legacy path is unreadable ({error})\"\n+                        )\n+                        continue\n+                    if actual != expected:\n+                        blockers.append(\n+                            f\"{relative}: digest mismatch (stamp {expected}, current {actual})\"\n+                        )\n+\n+    if blockers:\n+        joined = \"\\n\".join(f\"- {blocker}\" for blocker in blockers)\n+        raise ShikiError(\n+            \"legacy Shiki ADR cleanup is not authorized:\\n\"\n+            f\"{joined}\\nNo files were written.\"\n+        )\n+    return present\n+\n+\n def install_template(target: Path, *, force: bool, validate: bool) -> list[NewFileNote]:\n     # Preconditions first: never rewrite the tree of a target that cannot be\n     # upgraded cleanly (see refuse_pending_migrations).\n+    legacy_cleanup = preflight_legacy_sadr_cleanup(target, force=force)\n     refuse_pending_migrations(target)\n \n     # Resolve the target's required CODEOWNERS owner once from its\n@@ -250,6 +340,10 @@\n     # verbatim. See ``codeowners_required_owner``.\n     codeowners_owner = codeowners_required_owner(target)\n \n+    for relative in legacy_cleanup:\n+        (target / relative).unlink()\n+        info(f\"removed stamped legacy Shiki ADR: {target / relative}\")\n+\n     notes: list[NewFileNote] = []\n     for relative in TEMPLATE_PATHS:\n         source = ROOT / relative\n@@ -647,6 +741,22 @@\n     digests = target_stamp_digests(target)\n     existing = read_install_stamp(target)\n     if existing is not None:\n+        # A non-force install deliberately leaves legacy Shiki ADRs in place.\n+        # Preserve only still-matching ownership proof so a later forced\n+        # upgrade can authorize cleanup; never carry a missing or stale digest.\n+        existing_digests = existing.get(\"digests\")\n+        if isinstance(existing_digests, dict):\n+            for relative in legacy_shiki_adr_paths():\n+                expected = existing_digests.get(relative)\n+                path = target / relative\n+                if not isinstance(expected, str) or not path.is_file():\n+                    continue\n+                try:\n+                    actual = _sha256_file(path)\n+                except OSError:\n+                    continue\n+                if actual == expected:\n+                    digests[relative] = actual\n         if _incoming_is_older(commit, existing.get(\"platform_commit\")):\n             warn(\n                 \"kept existing install stamp: incoming install \"\ndiff --git a/scripts/shiki_loop.py b/scripts/shiki_loop.py\nindex 3611641da8aeb5038b1da90e3114b09a473ec6d8..84caa491b9cad0edc4176f209c13e9221e975681\n--- a/scripts/shiki_loop.py\n+++ b/scripts/shiki_loop.py\n@@ -424,7 +424,7 @@\n         # unresolvable CCA verdict has already produced its terminal stop and is never\n         # laundered into a head-moving sync (SADR-0010). It also never fires while the\n         # policy (Guardian) gate is red \u2014 for ANY reason, not only when it is the sole\n-        # red check \u2014 so a head-bound Guardian approval, or an SADR-0015\n+        # red check \u2014 so a head-bound Guardian approval, or a SADR-0015\n         # contract-approval carry that base movement defeated (the policy gate compares\n         # base and head task governance directly), is never silently restored by\n         # adopting base wholesale. It is bounded by a durable per-task counter so the\ndiff --git a/scripts/shiki_tasks.py b/scripts/shiki_tasks.py\nindex aec3809f24b4a0c8c96fc50971cff9ae9ad30381..d531052e9d9717058d4631483cefab44c26a65c1\n--- a/scripts/shiki_tasks.py\n+++ b/scripts/shiki_tasks.py\n@@ -821,7 +821,7 @@\n         \"prompt\": prompt,\n         \"entry_skill\": \"grill-with-docs\",\n         \"required_next_steps\": [\n-            \"Run grill-with-docs until terminology, boundaries, risks, and ADR-worthy decisions are settled.\",\n+            \"Run grill-with-docs until terminology, boundaries, risks, and decision-record-worthy choices are settled.\",\n             \"Obtain the operator's explicit approval of the PRD/requirements (Spec Freeze).\",\n             \"Write a machine-readable plan JSON with grill_with_docs.status=complete and spec_freeze.status=frozen.\",\n             \"Run shiki plan ingest --plan-file PLAN.json.\",\ndiff --git a/scripts/test_shiki_goal_loop.sh b/scripts/test_shiki_goal_loop.sh\nindex 753c7b516e10f0be3bc92a4bd0eb1fa699776ae6..8f3820a0a079b3044424cfe1a6c6b7fe68896e67\n--- a/scripts/test_shiki_goal_loop.sh\n+++ b/scripts/test_shiki_goal_loop.sh\n@@ -231,8 +231,8 @@\n \n # High/critical risk merges autonomously when all required checks are green:\n # the \"MergeGate policy check\" required check IS the Guardian gate (it enforces\n-# guardian-policy.json \u2014 human approval OR external AI guardian review, ADR\n-# 0010), so green checks mean a recorded authority approved. The fake gh returns\n+# guardian-policy.json \u2014 human approval OR external AI guardian review,\n+# SADR-0010), so green checks mean a recorded authority approved. The fake gh returns\n # green checks, simulating that the Guardian gate passed.\n python3 \"$ROOT/scripts/shiki.py\" goal create --target \"$TARGET\" --title \"Guardian gate\" --outcome \"High risk merges via policy gate\" >/tmp/shiki-goal-loop-guardian-goal.json\n GGOAL=\"$(json_get /tmp/shiki-goal-loop-guardian-goal.json goal_id)\"\ndiff --git a/tests/test_adr_references.py b/tests/test_adr_references.py\nindex 51706329c797d5dd9b47e082a333fa83b261d482..db6e693e35f67103935be34f1d9bb4ff7ee60395\n--- a/tests/test_adr_references.py\n+++ b/tests/test_adr_references.py\n@@ -1,7 +1,7 @@\n-\"\"\"Every ``ADR ####`` citation must resolve to exactly one decision record.\n+\"\"\"Every target ADR and Shiki SADR citation resolves in its own namespace.\n \n Background. ``scripts/mergegate_check.py`` justifies ``is_bookkeeping_closeout``\n-with \"ADR 0017\" nine times and ``scripts/guardian_approval_signal.py`` cites\n+with \"SADR-0017\" nine times and ``scripts/guardian_approval_signal.py`` cites\n 0016/0017 seven times, yet for a stretch the numbering in ``docs/adr/`` stopped\n at 0015: the authoring commits lived on a coordinator branch and no task ever\n declared ``docs/adr/...`` in its locks, so nothing carried the records to the\n@@ -11,11 +11,12 @@\n and the directory drifted with no signal.\n \n This is the same unbound-pair shape behind other silent failures: a reference\n-and its target with nothing asserting they agree. This test binds them. It scans\n-every ``scripts/*.py`` (plus ``docs/**/*.md`` and the root ``*.md`` governance\n-files, which are cheap) for the ``ADR ####`` pattern and asserts each cited\n-number resolves to exactly one ``docs/adr/####-*.md``. The converse is\n-deliberately NOT required: an ADR may exist without being cited.\n+and its target with nothing asserting they agree. This test binds them. Target\n+``ADR NNNN`` citations resolve only to ``docs/adr/NNNN-*.md``; Shiki\n+``SADR-NNNN`` citations resolve only to ``docs/adr/SADR-NNNN-*.md``. The scan\n+covers current governance docs, scripts, workflows, prompts, packaging metadata,\n+and repo-local Codex guidance while deliberately excluding historical\n+``.shiki`` evidence. The converse is not required: a record may be uncited.\n \n The suite is dependency free: standard-library ``unittest`` only.\n \"\"\"\n@@ -31,17 +32,18 @@\n \n REPO_ROOT = Path(__file__).resolve().parents[1]\n \n-# \"ADR 0016\" -- a four-digit citation. The trailing \\b keeps a five-digit token\n-# from being read as a truncated four-digit number.\n-_CITATION = re.compile(r\"\\bADR (\\d{4})\\b\")\n-# \"0016-locks-bound-product-paths.md\" -- the leading four digits name the record.\n-_ADR_FILENAME = re.compile(r\"^(\\d{4})-.*\\.md$\")\n+# ``ADR 0016`` / ``SADR-0016`` -- a namespace plus four-digit citation. The\n+# trailing ``\\b`` keeps a five-digit token from being read as a truncated one.\n+_CITATION = re.compile(r\"\\b(S?ADR)[ -](\\d{4})\\b\")\n+# Target ADRs retain the numeric filename; Shiki platform decisions use SADR.\n+_DECISION_FILENAME = re.compile(r\"^(?:(SADR)-)?(\\d{4})-.*\\.md$\")\n \n \n class Violation(NamedTuple):\n     \"\"\"A citation that does not resolve to exactly one decision record.\"\"\"\n \n     citing_file: str  # repo-relative path of the file that cites the ADR\n+    namespace: str  # \"ADR\" for target decisions; \"SADR\" for Shiki decisions\n     number: str  # the cited four-digit number, e.g. \"0016\"\n     line: int  # 1-based line of the citation\n     reason: str  # \"missing\" (zero matches) or \"ambiguous\" (two or more)\n@@ -49,44 +51,59 @@\n \n     @property\n     def expected_path(self) -> str:\n-        return f\"docs/adr/{self.number}-*.md\"\n+        prefix = \"SADR-\" if self.namespace == \"SADR\" else \"\"\n+        return f\"docs/adr/{prefix}{self.number}-*.md\"\n \n     def describe(self) -> str:\n         \"\"\"Name the citing file, the cited number, and the missing path.\"\"\"\n         if self.reason == \"missing\":\n             return (\n-                f\"{self.citing_file}:{self.line} cites ADR {self.number} \"\n+                f\"{self.citing_file}:{self.line} cites {self.namespace} {self.number} \"\n                 f\"but no document matches {self.expected_path}\"\n             )\n         joined = \", \".join(self.matches)\n         return (\n-            f\"{self.citing_file}:{self.line} cites ADR {self.number} which \"\n+            f\"{self.citing_file}:{self.line} cites {self.namespace} {self.number} which \"\n             f\"resolves to {len(self.matches)} documents (expected exactly one \"\n             f\"at {self.expected_path}): {joined}\"\n         )\n \n \n-def _adr_index(root: Path) -> dict[str, list[Path]]:\n-    \"\"\"Map every four-digit ADR number to the record files that claim it.\"\"\"\n-    index: dict[str, list[Path]] = {}\n+def _adr_index(root: Path) -> dict[tuple[str, str], list[Path]]:\n+    \"\"\"Map each decision namespace and number to the files that claim it.\"\"\"\n+    index: dict[tuple[str, str], list[Path]] = {}\n     adr_dir = root / \"docs\" / \"adr\"\n     if adr_dir.is_dir():\n         for path in sorted(adr_dir.glob(\"*.md\")):\n-            match = _ADR_FILENAME.match(path.name)\n+            match = _DECISION_FILENAME.match(path.name)\n             if match:\n-                index.setdefault(match.group(1), []).append(path)\n+                namespace = \"SADR\" if match.group(1) else \"ADR\"\n+                index.setdefault((namespace, match.group(2)), []).append(path)\n     return index\n \n \n def _citing_files(root: Path, include_docs: bool = True) -> list[Path]:\n-    \"\"\"Files scanned for citations: scripts/*.py, and cheaply the markdown.\"\"\"\n+    \"\"\"Current, shipped text surfaces scanned for decision citations.\"\"\"\n     files = sorted((root / \"scripts\").glob(\"*.py\"))\n+    files += sorted((root / \"scripts\").glob(\"*.sh\"))\n     if include_docs:\n         docs = root / \"docs\"\n         if docs.is_dir():\n             files += sorted(docs.rglob(\"*.md\"))\n         files += sorted(root.glob(\"*.md\"))\n-    return files\n+        pyproject = root / \"pyproject.toml\"\n+        if pyproject.is_file():\n+            files.append(pyproject)\n+        for relative in (\".github\", \".codex\"):\n+            surface = root / relative\n+            if surface.is_dir():\n+                files += sorted(\n+                    path\n+                    for path in surface.rglob(\"*\")\n+                    if path.is_file()\n+                    and path.suffix in {\".md\", \".py\", \".sh\", \".toml\", \".yaml\", \".yml\"}\n+                )\n+    return sorted(set(files))\n \n \n def find_adr_reference_violations(\n@@ -94,10 +111,9 @@\n ) -> list[Violation]:\n     \"\"\"Return every ``ADR ####`` citation under ``root`` that fails to resolve.\n \n-    A citation resolves when exactly one ``docs/adr/####-*.md`` matches its\n-    number. Zero matches is a \"missing\" violation; two or more is \"ambiguous\".\n-    Records that are never cited produce nothing -- the converse is not\n-    required.\n+    A citation resolves when exactly one record in the same namespace matches\n+    its number. Zero matches is a \"missing\" violation; two or more is\n+    \"ambiguous\". Records that are never cited produce nothing.\n     \"\"\"\n     root = Path(root)\n     index = _adr_index(root)\n@@ -110,13 +126,15 @@\n         rel = path.relative_to(root).as_posix()\n         for lineno, line in enumerate(text.splitlines(), start=1):\n             for match in _CITATION.finditer(line):\n-                number = match.group(1)\n-                found = index.get(number, [])\n+                namespace, number = match.groups()\n+                found = index.get((namespace, number), [])\n                 if len(found) == 1:\n                     continue\n                 reason = \"missing\" if not found else \"ambiguous\"\n                 rels = [p.relative_to(root).as_posix() for p in found]\n-                violations.append(Violation(rel, number, lineno, reason, rels))\n+                violations.append(\n+                    Violation(rel, namespace, number, lineno, reason, rels)\n+                )\n     return violations\n \n \n@@ -129,8 +147,8 @@\n         self.assertEqual(\n             violations,\n             [],\n-            \"Every 'ADR ####' citation must resolve to exactly one \"\n-            f\"docs/adr/####-*.md:\\n{detail}\",\n+            \"Every ADR/SADR citation must resolve exactly once in its own \"\n+            f\"docs/adr namespace:\\n{detail}\",\n         )\n \n     def test_scan_actually_reads_scripts(self) -> None:\n@@ -146,8 +164,21 @@\n         # The two records this Goal carried onto the default branch, and which\n         # the scripts cite most, must be present.\n         index = _adr_index(REPO_ROOT)\n-        self.assertIn(\"0016\", index)\n-        self.assertIn(\"0017\", index)\n+        self.assertIn((\"SADR\", \"0016\"), index)\n+        self.assertIn((\"SADR\", \"0017\"), index)\n+\n+    def test_platform_tree_has_only_sadr_0001_through_0019(self) -> None:\n+        adr_dir = REPO_ROOT / \"docs\" / \"adr\"\n+        sadrs = sorted(adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\"))\n+        self.assertEqual(\n+            {path.name.split(\"-\", 2)[1] for path in sadrs},\n+            {f\"{number:04d}\" for number in range(1, 20)},\n+        )\n+        self.assertEqual(\n+            list(adr_dir.glob(\"[0-9][0-9][0-9][0-9]-*.md\")),\n+            [],\n+            \"the Shiki platform tree must not retain legacy numeric SADR paths\",\n+        )\n \n \n class FixtureAdrReferences(unittest.TestCase):\n@@ -180,6 +211,26 @@\n         self.assertIn(\"9991\", message)  # cited number\n         self.assertIn(\"docs/adr/9991-*.md\", message)  # missing path\n \n+    def test_target_adr_does_not_satisfy_missing_platform_sadr(self) -> None:\n+        root = self._make_repo(\n+            adr_names=[\"0042-target-decision.md\"],\n+            script_body='\"\"\"Target ADR 0042 exists, but platform SADR-0042 does not.\"\"\"\\n',\n+        )\n+        violations = find_adr_reference_violations(root, include_docs=False)\n+        self.assertEqual(len(violations), 1)\n+        self.assertEqual(violations[0].namespace, \"SADR\")\n+        self.assertEqual(violations[0].number, \"0042\")\n+        self.assertEqual(violations[0].expected_path, \"docs/adr/SADR-0042-*.md\")\n+\n+    def test_target_adr_and_platform_sadr_may_reuse_a_number(self) -> None:\n+        root = self._make_repo(\n+            adr_names=[\"0042-target-decision.md\", \"SADR-0042-platform-decision.md\"],\n+            script_body='\"\"\"Target ADR 0042 and platform SADR-0042 are distinct.\"\"\"\\n',\n+        )\n+        self.assertEqual(\n+            find_adr_reference_violations(root, include_docs=False), []\n+        )\n+\n     def test_uncited_adr_is_allowed(self) -> None:\n         # An ADR exists but nothing cites it -> not a violation. The converse\n         # (every ADR must be cited) is explicitly NOT required.\ndiff --git a/tests/test_guardian_review.py b/tests/test_guardian_review.py\nindex 88829e7810212635e2fc98aa5918d6f26294677d..00a85a237acd5bf2d6cb5b294128198c422402ca\n--- a/tests/test_guardian_review.py\n+++ b/tests/test_guardian_review.py\n@@ -162,7 +162,7 @@\n \n     def test_docs_only_is_light_and_not_conservative(self):\n         focus = gr.classify_review_focus([\"docs/adr/0099-x.md\", \"README.md\"], \"\", \"low\")\n-        self.assertTrue(any(\"Docs/ADR-only\" in f for f in focus))\n+        self.assertTrue(any(\"Docs/decision-record-only\" in f for f in focus))\n         self.assertFalse(any(\"conservative default\" in f.lower() for f in focus))\n \n     def test_unknown_category_is_conservative(self):\ndiff --git a/tests/test_policy_job_merged_prs.py b/tests/test_policy_job_merged_prs.py\nindex d45512e74681e5e649279236c7d4e79d5c3da471..b97989f8bd74d091de1a6b5275a20d8dc68c4512\n--- a/tests/test_policy_job_merged_prs.py\n+++ b/tests/test_policy_job_merged_prs.py\n@@ -3,8 +3,8 @@\n \n ``.github/workflows/shiki-cca-completion.yml`` has two consumers of the merged-PR\n proof: the CCA *signal* (``guardian_approval_signal.py`` in \"Prepare CCA context\")\n-and the authoritative gate (``mergegate_check.py`` in \"Enforce MergeGate\"). ADR\n-0017's bookkeeping-closeout exemption (condition 6) needs the merged implementation\n+and the authoritative gate (``mergegate_check.py`` in \"Enforce MergeGate\").\n+SADR-0017's bookkeeping-closeout exemption (condition 6) needs the merged implementation\n PR proven, and a closeout carries NO ``post_merge_reconcile`` marker. The signal\n already resolves ``MERGED_PRS`` unconditionally, but \"Enforce MergeGate\" used to\n resolve it only inside ``grep -q '<!-- shiki:post_merge_reconcile -->'``. So a\ndiff --git a/tests/test_sadr_install_migration.py b/tests/test_sadr_install_migration.py\nnew file mode 100644\nindex 0000000000000000000000000000000000000000..a6f4c57e4dcaef73ef600b0d4804744138ab9071\n--- /dev/null\n+++ b/tests/test_sadr_install_migration.py\n@@ -0,0 +1,229 @@\n+\"\"\"SADR installation and collision-safe legacy upgrade behavior.\"\"\"\n+\n+from __future__ import annotations\n+\n+import contextlib\n+import hashlib\n+import io\n+import json\n+import shutil\n+import tempfile\n+import unittest\n+from pathlib import Path\n+\n+import shiki_test_support  # noqa: F401  (path bootstrap)\n+\n+import shiki_installer\n+from tests.test_adr_references import find_adr_reference_violations\n+\n+\n+PRODUCT_NUMBERS = tuple(range(13, 19))\n+\n+\n+def _quiet_install(target: Path, *, force: bool, validate: bool) -> None:\n+    with contextlib.redirect_stdout(io.StringIO()):\n+        shiki_installer.install_template(target, force=force, validate=validate)\n+\n+\n+def _sha256(path: Path) -> str:\n+    return hashlib.sha256(path.read_bytes()).hexdigest()\n+\n+\n+def _legacy_sadr_paths() -> tuple[str, ...]:\n+    return tuple(\n+        f\"docs/adr/{path.name.removeprefix('SADR-')}\"\n+        for path in sorted((shiki_installer.ROOT / \"docs\" / \"adr\").glob(\"SADR-*.md\"))\n+        if path.name != \"SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\"\n+    )\n+\n+\n+def _create_legacy_upgrade_fixture(\n+    target: Path,\n+) -> tuple[dict[str, bytes], tuple[str, ...]]:\n+    adr_dir = target / \"docs\" / \"adr\"\n+    adr_dir.mkdir(parents=True)\n+    product_bytes = {\n+        f\"{number:04d}-cubeshare-product-decision.md\": (\n+            f\"# CubeShare ADR {number:04d}\\n\\nDo not rewrite {number}.\\n\"\n+        ).encode()\n+        for number in PRODUCT_NUMBERS\n+    }\n+    for name, content in product_bytes.items():\n+        (adr_dir / name).write_bytes(content)\n+\n+    legacy_paths = _legacy_sadr_paths()\n+    for relative in legacy_paths:\n+        (target / relative).write_bytes(f\"legacy Shiki bytes for {relative}\\n\".encode())\n+\n+    stamp_path = target / shiki_installer.INSTALL_STAMP_PATH\n+    stamp_path.parent.mkdir(parents=True, exist_ok=True)\n+    stamp = {\n+        \"version\": shiki_installer.INSTALL_STAMP_VERSION,\n+        \"platform_commit\": None,\n+        \"installed_at\": \"2000-01-01T00:00:00+00:00\",\n+        \"digests\": {\n+            relative: _sha256(target / relative) for relative in legacy_paths\n+        },\n+    }\n+    stamp_path.write_text(\n+        json.dumps(stamp, indent=2, sort_keys=True) + \"\\n\", encoding=\"utf-8\"\n+    )\n+    return product_bytes, legacy_paths\n+\n+\n+def _tree_snapshot(root: Path) -> dict[str, tuple[str, bytes | str]]:\n+    \"\"\"Recursive byte/shape snapshot used to prove preflight is write-free.\"\"\"\n+    snapshot: dict[str, tuple[str, bytes | str]] = {}\n+    for path in sorted(root.rglob(\"*\")):\n+        relative = path.relative_to(root).as_posix()\n+        if path.is_symlink():\n+            snapshot[relative] = (\"symlink\", path.readlink().as_posix())\n+        elif path.is_dir():\n+            snapshot[relative] = (\"directory\", \"\")\n+        else:\n+            snapshot[relative] = (\"file\", path.read_bytes())\n+    return snapshot\n+\n+\n+class FreshSadrInstallTests(unittest.TestCase):\n+    def setUp(self) -> None:\n+        self.tmp = Path(tempfile.mkdtemp(prefix=\"shiki-sadr-fresh-\"))\n+        self.addCleanup(lambda: shutil.rmtree(self.tmp, ignore_errors=True))\n+        self.target = self.tmp / \"target\"\n+        self.adr_dir = self.target / \"docs\" / \"adr\"\n+        self.adr_dir.mkdir(parents=True)\n+        self.product_bytes = {\n+            f\"{number:04d}-product-decision.md\": (\n+                f\"# Product ADR {number:04d}\\n\\nTarget-owned bytes {number}.\\n\"\n+            ).encode()\n+            for number in PRODUCT_NUMBERS\n+        }\n+        for name, content in self.product_bytes.items():\n+            (self.adr_dir / name).write_bytes(content)\n+\n+    def test_fresh_install_preserves_product_adrs_and_installs_all_sadrs(self) -> None:\n+        _quiet_install(self.target, force=False, validate=True)\n+\n+        for name, expected in self.product_bytes.items():\n+            self.assertEqual((self.adr_dir / name).read_bytes(), expected)\n+\n+        sadr_numbers = {\n+            path.name.split(\"-\", 2)[1]\n+            for path in self.adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\")\n+        }\n+        self.assertEqual(sadr_numbers, {f\"{number:04d}\" for number in range(1, 20)})\n+\n+        numeric_records = {\n+            path.name\n+            for path in self.adr_dir.glob(\"[0-9][0-9][0-9][0-9]-*.md\")\n+        }\n+        self.assertEqual(numeric_records, set(self.product_bytes))\n+        self.assertEqual(find_adr_reference_violations(self.target), [])\n+\n+\n+class LegacySadrUpgradeTests(unittest.TestCase):\n+    def setUp(self) -> None:\n+        self.tmp = Path(tempfile.mkdtemp(prefix=\"shiki-sadr-upgrade-\"))\n+        self.addCleanup(lambda: shutil.rmtree(self.tmp, ignore_errors=True))\n+        self.target = self.tmp / \"target\"\n+        self.adr_dir = self.target / \"docs\" / \"adr\"\n+        self.product_bytes, self.legacy_paths = _create_legacy_upgrade_fixture(\n+            self.target\n+        )\n+\n+    def test_force_upgrade_deletes_only_stamped_legacy_sadrs(self) -> None:\n+        _quiet_install(self.target, force=True, validate=True)\n+\n+        for relative in self.legacy_paths:\n+            self.assertFalse((self.target / relative).exists(), relative)\n+        for name, expected in self.product_bytes.items():\n+            self.assertEqual((self.adr_dir / name).read_bytes(), expected)\n+\n+        sadrs = list(self.adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\"))\n+        self.assertEqual(len(sadrs), 19)\n+        refreshed = json.loads(\n+            (self.target / shiki_installer.INSTALL_STAMP_PATH).read_text(encoding=\"utf-8\")\n+        )\n+        for relative in self.legacy_paths:\n+            self.assertNotIn(relative, refreshed[\"digests\"])\n+        for path in sadrs:\n+            relative = path.relative_to(self.target).as_posix()\n+            self.assertEqual(refreshed[\"digests\"][relative], _sha256(path))\n+\n+    def test_non_force_install_never_deletes_legacy_files(self) -> None:\n+        before = {\n+            relative: (self.target / relative).read_bytes()\n+            for relative in self.legacy_paths\n+        }\n+        _quiet_install(self.target, force=False, validate=False)\n+        for relative, expected in before.items():\n+            self.assertEqual((self.target / relative).read_bytes(), expected)\n+        refreshed = json.loads(\n+            (self.target / shiki_installer.INSTALL_STAMP_PATH).read_text(encoding=\"utf-8\")\n+        )\n+        for relative in self.legacy_paths:\n+            self.assertEqual(refreshed[\"digests\"][relative], _sha256(self.target / relative))\n+\n+\n+class LegacyCleanupPreflightTests(unittest.TestCase):\n+    def setUp(self) -> None:\n+        self.tmp = Path(tempfile.mkdtemp(prefix=\"shiki-sadr-preflight-\"))\n+        self.addCleanup(lambda: shutil.rmtree(self.tmp, ignore_errors=True))\n+\n+    def test_all_ownership_blockers_abort_before_any_write(self) -> None:\n+        cases = (\n+            (\"absent\", \"install stamp is absent\"),\n+            (\"unreadable\", \"install stamp is unreadable\"),\n+            (\"malformed\", \"install stamp is malformed\"),\n+            (\"missing-digest\", \"install stamp has no digest\"),\n+            (\"digest-mismatch\", \"digest mismatch\"),\n+        )\n+        for name, expected_reason in cases:\n+            with self.subTest(name=name):\n+                target = self.tmp / name\n+                _, legacy_paths = _create_legacy_upgrade_fixture(target)\n+                stamp_path = target / shiki_installer.INSTALL_STAMP_PATH\n+\n+                if name == \"absent\":\n+                    stamp_path.unlink()\n+                elif name == \"unreadable\":\n+                    stamp_path.unlink()\n+                    stamp_path.mkdir()\n+                elif name == \"malformed\":\n+                    stamp_path.write_text(\"{not-json\\n\", encoding=\"utf-8\")\n+                else:\n+                    stamp = json.loads(stamp_path.read_text(encoding=\"utf-8\"))\n+                    if name == \"missing-digest\":\n+                        stamp[\"digests\"] = {}\n+                    else:\n+                        stamp[\"digests\"] = {\n+                            relative: \"deadbeef\" for relative in legacy_paths\n+                        }\n+                    stamp_path.write_text(\n+                        json.dumps(stamp, indent=2, sort_keys=True) + \"\\n\",\n+                        encoding=\"utf-8\",\n+                    )\n+\n+                before = _tree_snapshot(target)\n+                with self.assertRaises(shiki_installer.ShikiError) as raised:\n+                    _quiet_install(target, force=True, validate=False)\n+                after = _tree_snapshot(target)\n+\n+                self.assertEqual(before, after, f\"{name} preflight changed the tree\")\n+                message = str(raised.exception)\n+                self.assertIn(expected_reason, message)\n+                self.assertIn(\"No files were written\", message)\n+                for relative in legacy_paths:\n+                    self.assertIn(relative, message)\n+\n+    def test_absent_stamp_is_allowed_when_no_legacy_path_exists(self) -> None:\n+        target = self.tmp / \"no-legacy\"\n+        target.mkdir()\n+        _quiet_install(target, force=True, validate=False)\n+        self.assertTrue(\n+            (target / \"docs\" / \"adr\" / \"SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\").is_file()\n+        )\n+\n+\n+if __name__ == \"__main__\":\n+    unittest.main()\n\n2026-08-07T08:19:20.583750Z ERROR codex_models_manager::manager: failed to renew cache TTL: missing field `base_instructions` at line 94 column 5\nexec\n/bin/zsh -lc \"git diff --find-renames=1% --summary; git diff --find-renames=1% --stat | sed -n '1,160p'\" in /private/tmp/shiki-adr-collision.pa0KwY/.worktrees/shiki-t-20260807t073307221254z-2d22d654-introduc\n succeeded in 0ms:\n delete mode 100644 docs/adr/0001-keep-shiki-as-platform-name.md\n delete mode 100644 docs/adr/0002-use-agents-as-shared-constitution.md\n delete mode 100644 docs/adr/0003-require-skill-gate-for-mergegate-readiness.md\n delete mode 100644 docs/adr/0004-use-github-first-with-shiki-mirror.md\n delete mode 100644 docs/adr/0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\n delete mode 100644 docs/adr/0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\n delete mode 100644 docs/adr/0007-packaging-and-release.md\n delete mode 100644 docs/adr/0008-use-claude-code-as-default-implementer.md\n delete mode 100644 docs/adr/0009-requirements-definition-and-spec-freeze.md\n delete mode 100644 docs/adr/0010-external-ai-guardian-review.md\n delete mode 100644 docs/adr/0011-autonomous-quality-gate-evidence.md\n delete mode 100644 docs/adr/0012-autonomous-loop-closeout-to-main.md\n delete mode 100644 docs/adr/0013-bootstrap-grants-actions-approve-pull-requests.md\n delete mode 100644 docs/adr/0014-codex-app-external-ai-guardian-ui-adapter.md\n delete mode 100644 docs/adr/0015-contract-approval-before-dispatch.md\n delete mode 100644 docs/adr/0016-locks-bound-product-paths.md\n delete mode 100644 docs/adr/0017-bookkeeping-closeouts-do-not-inherit-risk.md\n delete mode 100644 docs/adr/0018-carried-guardian-approval-across-a-proven-base-sync.md\n .codex/skills/shiki/SKILL.md                       |   5 +-\n .github/prompts/cca-completion-check.md            |   5 +-\n .github/prompts/codex-task-handoff.md              |   2 +-\n .github/workflows/release.yml                      |   2 +-\n .github/workflows/shiki-cca-completion.yml         |  24 +--\n .github/workflows/shiki-mergegate.yml              |   8 +-\n .../tasks/T-20260807T073307221254Z-2d22d654.json   |  36 ++--\n AGENTS.md                                          |  26 ++-\n CLAUDE.md                                          |  17 +-\n CONTEXT.md                                         |  10 +-\n CONTRIBUTING.md                                    |   8 +-\n SYSTEM_PROMPT.md                                   |  19 +-\n docs/adr/0001-keep-shiki-as-platform-name.md       |   3 -\n docs/adr/0002-use-agents-as-shared-constitution.md |   3 -\n ...3-require-skill-gate-for-mergegate-readiness.md |   3 -\n .../adr/0004-use-github-first-with-shiki-mirror.md |  22 --\n ...ont-and-claude-code-action-as-github-runtime.md |  29 ---\n ...n-cca-completion-and-mergegate-authorization.md |  54 -----\n docs/adr/0007-packaging-and-release.md             | 126 -----------\n .../0008-use-claude-code-as-default-implementer.md | 108 ----------\n ...0009-requirements-definition-and-spec-freeze.md |  90 --------\n docs/adr/0010-external-ai-guardian-review.md       |  97 ---------\n docs/adr/0011-autonomous-quality-gate-evidence.md  |  87 --------\n docs/adr/0012-autonomous-loop-closeout-to-main.md  |  44 ----\n ...otstrap-grants-actions-approve-pull-requests.md | 109 ----------\n ...14-codex-app-external-ai-guardian-ui-adapter.md | 102 ---------\n docs/adr/0015-contract-approval-before-dispatch.md | 229 --------------------\n docs/adr/0016-locks-bound-product-paths.md         | 233 ---------------------\n ...17-bookkeeping-closeouts-do-not-inherit-risk.md | 129 ------------\n ...-guardian-approval-across-a-proven-base-sync.md | 147 -------------\n docs/adr/README.md                                 |  63 +++---\n docs/adr/template.md                               |   8 +-\n docs/agents/autonomous-loop-e2e-acceptance.md      |  14 +-\n docs/agents/bootstrap-command.md                   |   4 +-\n docs/agents/checklists.md                          |  10 +-\n docs/agents/codex-handoff.md                       |   4 +-\n docs/agents/completion-check-agent.md              |   2 +-\n docs/agents/decision-control.md                    |   2 +-\n docs/agents/domain.md                              |   8 +-\n docs/agents/execution-control.md                   |   2 +-\n docs/agents/guardian-policy.md                     |  10 +-\n docs/agents/implementation-policy.md               |  25 ++-\n docs/agents/runtime-auth-model.md                  |   6 +-\n docs/agents/runtime-registry.md                    |   2 +-\n docs/agents/shiki-cli-architecture.md              |   2 +-\n docs/agents/skill-gate.md                          |   4 +-\n docs/agents/triage-labels.md                       |   2 +-\n docs/operations.md                                 |  10 +\n .../0001-memory-loop-spec-freeze-review.md         |   4 +-\n docs/proposals/0002-autonomous-loop-completion.md  |  20 +-\n docs/releasing.md                                  |  15 +-\n docs/usage.md                                      |   6 +-\n pyproject.toml                                     |   2 +-\n scripts/guardian_approval_signal.py                |  30 +--\n scripts/mergegate_check.py                         |  84 ++++----\n scripts/shiki_bootstrap.py                         |   2 +-\n scripts/shiki_cli.py                               |   2 +-\n scripts/shiki_contract_approval.py                 |  14 +-\n scripts/shiki_github.py                            |   8 +-\n scripts/shiki_guardian.py                          |   6 +-\n scripts/shiki_guardian_review.py                   |   6 +-\n scripts/shiki_installer.py                         | 110 ++++++++++\n scripts/shiki_loop.py                              |  56 ++---\n scripts/shiki_migrations.py                        |   4 +-\n scripts/shiki_runtime_adapters.py                  |   2 +-\n scripts/shiki_runtime_registry.py                  |   2 +-\n scripts/shiki_tasks.py                             |  16 +-\n scripts/test_shiki_adversarial_state.sh            |   8 +-\n scripts/test_shiki_code_review_gate.sh             |   2 +-\n scripts/test_shiki_control_plane.sh                |   8 +-\n scripts/test_shiki_goal_loop.sh                    |  14 +-\n scripts/test_shiki_governance_evidence.sh          |   4 +-\n scripts/test_shiki_guardian_policy.sh              |   2 +-\n scripts/test_shiki_loop_e2e.sh                     |   6 +-\n scripts/test_shiki_loop_lock_guard.sh              |   2 +-\n scripts/test_shiki_run_orchestrator.sh             |   2 +-\n scripts/test_shiki_runner_claude.sh                |   2 +-\n scripts/test_shiki_runtime_registry.sh             |   2 +-\n scripts/test_shiki_start.sh                        |   4 +-\n scripts/validate_shiki.py                          |  10 +-\n tests/test_adr_references.py                       | 117 ++++++++---\n tests/test_bookkeeping_closeout.py                 |   6 +-\n tests/test_branch_behind_sync.py                   |   4 +-\n tests/test_cca_evidence_manifest_taskless.py       |   2 +-\n tests/test_closeout.py                             |   2 +-\n tests/test_completion_lock_scope.py                |   2 +-\n tests/test_contract_amendment.py                   |   2 +-\n tests/test_contract_approval.py                    |   4 +-\n tests/test_contract_immutability.py                |   6 +-\n tests/test_contract_mode.py                        |   4 +-\n tests/test_contract_open.py                        |   2 +-\n tests/test_contract_plan_registration.py           |   4 +-\n tests/test_dispatch_lock_precondition.py           |   2 +-\n tests/test_guardian_review.py                      |   6 +-\n tests/test_guardian_signal.py                      |   2 +-\n tests/test_guardian_signal_closeout.py             |   6 +-\n tests/test_loop_branch_plumbing.py                 |   2 +-\n tests/test_loop_code_review.py                     |   2 +-\n tests/test_loop_e2e_contract.py                    |   2 +-\n tests/test_loop_engine.py                          |   6 +-\n tests/test_loop_lock_validator_hint.py             |   6 +-\n tests/test_loop_tdd_evidence.py                    |   4 +-\n tests/test_mirror_scope.py                         |   2 +-\n tests/test_paired_invariants.py                    |   2 +-\n tests/test_policy_job_merged_prs.py                |   6 +-\n tests/test_repair_delivery.py                      |   2 +-\n tests/test_runner_adapters.py                      |   2 +-\n tests/test_workflow_invocation.py                  |   8 +-\n tests/test_workflow_invocation_carry.py            |   2 +-\n 109 files changed, 611 insertions(+), 2016 deletions(-)\n\ndiff --git a/.codex/skills/shiki/SKILL.md b/.codex/skills/shiki/SKILL.md\nindex c8e2e85d9d75d49cc4f2d6286ee22659c4e53e75..93cded623b2c15d9f65c76491a1c2d813ac9e6c0\n--- a/.codex/skills/shiki/SKILL.md\n+++ b/.codex/skills/shiki/SKILL.md\n@@ -18,6 +18,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n Codex CLI does not register this skill as a custom `/shiki` slash command. When\n the user types `/shiki` in Codex CLI and gets \"Unrecognized command\", explain\n that this is expected: invoke Shiki in Codex with natural language such as\n@@ -69,7 +72,7 @@\n \n ## Responsibilities\n \n-- Claude Code implements and repairs by default (`shiki runner claude`, ADR 0008); Codex implements and repairs tasks explicitly assigned to `codex`.\n+- Claude Code implements and repairs by default (`shiki runner claude`, SADR-0008); Codex implements and repairs tasks explicitly assigned to `codex`.\n - Claude Code Action can act as GitHub-side CCA or reviewer.\n - CCA judges completion.\n - MergeGate authorizes state transitions and merge readiness.\ndiff --git a/.github/prompts/cca-completion-check.md b/.github/prompts/cca-completion-check.md\nindex ed203ad5f302ecc81372d8ab27d2b1232ca97497..fd89950528333ef6242e6d5443904f5392ab7e56\n--- a/.github/prompts/cca-completion-check.md\n+++ b/.github/prompts/cca-completion-check.md\n@@ -19,6 +19,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n ## Required Reading\n \n Read only the files needed to judge the PR contract. Prefer this order:\ndiff --git a/.github/prompts/codex-task-handoff.md b/.github/prompts/codex-task-handoff.md\nindex 695e211d76dea1c60e8d5bb360e78487808286a4..59441d576b01a39569722f226c854e5a740e9eb5\n--- a/.github/prompts/codex-task-handoff.md\n+++ b/.github/prompts/codex-task-handoff.md\n@@ -2,7 +2,7 @@\n \n You are Codex Front acting as the Implementer for a Shiki task.\n \n-Follow `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant ADRs, and `docs/agents/codex-handoff.md`.\n+Follow `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant Shiki SADRs and target ADRs, and `docs/agents/codex-handoff.md`.\n \n Rules:\n \ndiff --git a/AGENTS.md b/AGENTS.md\nindex 7f041100d49c2a7525f51cb0f5d696a4a6d1c78c..a06bfbabc6fa48a007389e25e12d7ed523936bed\n--- a/AGENTS.md\n+++ b/AGENTS.md\n@@ -22,7 +22,7 @@\n - **Codex Front** is an optional implementer through Codex App, Codex CLI, Codex IDE extension, or Codex Web signed in with ChatGPT OAuth/subscription auth, dispatched with `shiki runner codex` when a task is explicitly assigned to `codex`.\n - **GitHub CCA** is implemented by Claude Code Action by default, using `CLAUDE_CODE_OAUTH_TOKEN`.\n \n-Do not assume `openai/codex-action`, `OPENAI_API_KEY`, or API-key based Codex automation in the default Shiki path. API-key runners are explicit target-repo extensions and require their own ADR.\n+Do not assume `openai/codex-action`, `OPENAI_API_KEY`, or API-key based Codex automation in the default Shiki path. API-key runners are explicit target-repo extensions and require their own target ADR.\n \n Shiki is not:\n \n@@ -44,10 +44,14 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use\n+`SADR-NNNN-*.md`; target product decisions retain `NNNN-*.md` and the `ADR`\n+namespace.\n+\n Use the canonical source-of-truth order above. Runtime-specific reading orders\n are operational guidance only and must not override or reorder it.\n \n-Conversation state is not durable truth. If a decision matters, put it in GitHub, `.shiki/`, `CONTEXT.md`, or an ADR.\n+Conversation state is not durable truth. If a decision matters, put it in GitHub, `.shiki/`, `CONTEXT.md`, a Shiki SADR, or a target ADR.\n \n If GitHub and `.shiki/` disagree, surface the conflict and prefer GitHub operational state until the mirror is repaired.\n \n@@ -75,8 +79,8 @@\n Every non-trivial change follows this loop:\n \n 1. **Goal**: clarify outcome, completion conditions, non-goals, risk level, and success signals.\n-2. **grill-with-docs**: challenge the plan against domain docs, ADRs, code reality, terminology, and edge scenarios.\n-3. **Context & Impact**: find relevant docs, ADRs, code, dependencies, owners, locks, and verification surfaces.\n+2. **grill-with-docs**: challenge the plan against domain docs, Shiki SADRs, target ADRs, code reality, terminology, and edge scenarios.\n+3. **Context & Impact**: find relevant docs, SADRs, target ADRs, code, dependencies, owners, locks, and verification surfaces.\n 4. **PRD and Spec Freeze**: use `to-prd` when context is settled enough to become durable product/engineering intent. The operator's explicit approval of the PRD is Spec Freeze: the plan records a `spec_freeze` block, and no plan runs without it. Steps 1-4 form Requirements Definition \u2014 one continuous operator dialogue. After Spec Freeze, scope changes require an operator-approved Spec Amendment; non-scope-moving interpretations go to the Assumption Log.\n 5. **Issues**: use `to-issues` to create independently grabbable vertical-slice issues. Prefer AFK slices over HITL slices where possible.\n 6. **Triage**: label issues for readiness, risk, runtime, skills, and MergeGate state.\n@@ -167,7 +171,7 @@\n - Testing decisions.\n - Out-of-scope items.\n - Further notes.\n-- Links to relevant domain terms and ADRs.\n+- Links to relevant domain terms, SADRs, and target ADRs.\n \n Every executable task must include:\n \n@@ -193,7 +197,7 @@\n \n Context & Impact must identify:\n \n-- Relevant documents, domain terms, ADRs, and past decisions.\n+- Relevant documents, domain terms, SADRs, target ADRs, and past decisions.\n - Relevant modules, interfaces, seams, callers, owners, and tests.\n - Dependency relationships between tasks.\n - Candidate locks and lock conflicts.\n@@ -210,7 +214,7 @@\n | Skill | Required when |\n | --- | --- |\n | `setup-matt-pocock-skills` | First configuring the repo for skills, or when issue tracker, triage labels, or domain docs are missing. |\n-| `grill-with-docs` | Default for non-trivial Goals before PRD/issues; always required when plans, terminology, boundaries, tradeoffs, or ADR-worthy decisions are ambiguous. |\n+| `grill-with-docs` | Default for non-trivial Goals before PRD/issues; always required when plans, terminology, boundaries, tradeoffs, or decision-record-worthy choices are ambiguous. |\n | `zoom-out` | The agent lacks an architectural map of the relevant code area. |\n | `to-prd` | Settled Goal context must become durable product/engineering intent. |\n | `to-issues` | A Goal, PRD, or plan must become independently grabbable vertical-slice issues. |\n@@ -348,7 +352,7 @@\n - Three or more new files.\n - Any high-risk or critical label.\n \n-When the architecture gate triggers, run `grill-with-docs` or `improve-codebase-architecture` as appropriate and record durable decisions in an ADR when needed.\n+When the architecture gate triggers, run `grill-with-docs` or `improve-codebase-architecture` as appropriate and record durable decisions in a Shiki SADR or target ADR as applicable.\n \n ## Engineering Discipline\n \ndiff --git a/CLAUDE.md b/CLAUDE.md\nindex 98a932e7841a632866fcd53b0bad0e3af3711d3d..98a31399404d6fb044b4fd493f4459ac87f439de\n--- a/CLAUDE.md\n+++ b/CLAUDE.md\n@@ -14,6 +14,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n ## Claude Role\n \n Default to planner, default implementer, reviewer, coordinator, documentation editor, CCA-style completion judge, and final-judgment assistant.\n@@ -57,7 +60,7 @@\n At the start of a Shiki session:\n \n 1. Read `AGENTS.md`.\n-2. Read `CONTEXT.md` and relevant ADRs.\n+2. Read `CONTEXT.md` and relevant Shiki SADRs and target ADRs.\n 3. Read active `.shiki/` Goal/task/ledger state when present.\n 4. Read active distilled rules from `.shiki/memories/` (status `distilled`, `active: true`, not revoked or superseded) for applicable guidance; the same rules are injected deterministically into task handoffs as the `## Distilled Rules` section (\u00a73.5).\n 5. Read the GitHub Issue or PR when the task is GitHub-backed.\n@@ -74,7 +77,7 @@\n - Define the Goal, completion conditions, non-goals, and risk level.\n - Treat `grill-with-docs` as the default planning entry point for non-trivial Goals.\n - Challenge domain terms against `CONTEXT.md`.\n-- Challenge hard-to-reverse tradeoffs against ADRs.\n+- Challenge hard-to-reverse tradeoffs against SADRs and target ADRs.\n - Explore code instead of asking when code can answer the question.\n - Run Context & Impact before implementation planning.\n - Produce a Task DAG, not an unordered checklist.\n@@ -94,7 +97,7 @@\n - For each question, provide your recommended answer.\n - Resolve dependency decisions before downstream design questions.\n - Update `CONTEXT.md` inline when terminology crystallizes.\n-- Offer ADRs only when the decision is hard to reverse, surprising without context, and a real tradeoff.\n+- Offer a SADR or target ADR only when the decision is hard to reverse, surprising without context, and a real tradeoff.\n - Convert unresolved questions into HITL blockers rather than allowing Codex to guess.\n \n ## PRD and Issues Standard\n@@ -103,7 +106,7 @@\n \n - Use the project's domain glossary vocabulary.\n - Avoid volatile file paths unless they are essential.\n-- Include implementation decisions, testing decisions, out-of-scope items, and relevant ADR/domain links.\n+- Include implementation decisions, testing decisions, out-of-scope items, and relevant SADR/target-ADR/domain links.\n - Identify deep-module opportunities and testable interfaces.\n \n When creating issues:\n@@ -121,7 +124,7 @@\n - Branch/worktree target.\n - Scope and non-goals.\n - Dependencies and locks.\n-- Relevant docs, ADRs, modules, and tests.\n+- Relevant docs, SADRs, target ADRs, modules, and tests.\n - Required skill invocations.\n - TDD expectations.\n - Pre-PR `code-review` gate expectations.\n@@ -152,7 +155,7 @@\n \n - Treat yourself as a judge, not implementer.\n - Do not write production code.\n-- Read `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant ADRs, PRD, task issue, PR body, diff, checks, reviews, labels, and `.shiki/` state.\n+- Read `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant SADRs and target ADRs, PRD, task issue, PR body, diff, checks, reviews, labels, and `.shiki/` state.\n - Evaluate every applicable checklist in `docs/agents/checklists.md`.\n - Map each acceptance criterion to durable evidence.\n - Distinguish implementation failure from missing evidence.\ndiff --git a/CONTEXT.md b/CONTEXT.md\nindex e61a9be06a0cce0d026484083a657bde2067e234..237ee8aa3ea8fab334ece49768bc5ed3b5af0974\n--- a/CONTEXT.md\n+++ b/CONTEXT.md\n@@ -25,7 +25,7 @@\n _Avoid_: jumping directly into code\n \n **grill-with-docs**:\n-A planning skill that challenges a plan against domain language, ADRs, code reality, and concrete edge scenarios. It resolves design-tree questions before PRD/issues. It runs inside Requirements Definition.\n+A planning skill that challenges a plan against domain language, Shiki SADRs, target ADRs, code reality, and concrete edge scenarios. It resolves design-tree questions before PRD/issues. It runs inside Requirements Definition.\n _Avoid_: generic brainstorming, silent assumptions\n \n **Requirements Definition**:\ndiff --git a/CONTRIBUTING.md b/CONTRIBUTING.md\nindex c6d9b656e73abb8a55e5f660123e03153dcb3e6d..9e37a8fbf668db5b22461aaae5548b8f1354394c\n--- a/CONTRIBUTING.md\n+++ b/CONTRIBUTING.md\n@@ -17,7 +17,7 @@\n 1. **Goal** \u2014 start from a user-approved Goal with an outcome, completion\n    conditions, non-goals, risk level, and success signals.\n 2. **grill-with-docs** \u2014 for any non-trivial change, challenge the plan against\n-   domain language, ADRs, and code reality before writing a PRD.\n+   domain language, Shiki SADRs, target ADRs, and code reality before writing a PRD.\n 3. **PRD** \u2014 capture settled product/engineering intent (`to-prd`).\n 4. **Spec Freeze** \u2014 the operator explicitly approves the PRD/requirements;\n    the plan records a `spec_freeze` block and no plan runs without it\n@@ -94,7 +94,7 @@\n   entries.\n - Do not hand-edit `.shiki/goals/*.json` or `.shiki/manifest.json` in a task PR.\n - Conversation state is not durable truth. If a decision matters, record it in\n-  GitHub, `.shiki/`, `CONTEXT.md`, or an ADR.\n+  GitHub, `.shiki/`, `CONTEXT.md`, a Shiki SADR, or a target ADR.\n \n ## Architecture Gate\n \n@@ -103,7 +103,7 @@\n or CI policy, package structure, three or more structural files, more than 100\n net new lines in a core module, three or more new files, or any high-risk label.\n When triggered, run `grill-with-docs` or `improve-codebase-architecture` and\n-record durable decisions in an ADR when needed (see `docs/adr/`).\n+record durable decisions in a Shiki SADR or target ADR as applicable (see `docs/adr/`).\n \n ## Safety\n \ndiff --git a/SYSTEM_PROMPT.md b/SYSTEM_PROMPT.md\nindex aaaea45e3a896c3a44ba89eec8bb84325ecf6c62..da009c86a88285269dee0155e58e550c6d744ea9\n--- a/SYSTEM_PROMPT.md\n+++ b/SYSTEM_PROMPT.md\n@@ -30,6 +30,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n Use the canonical source-of-truth order above. Runtime-specific reading orders\n are operational guidance only and must not override or reorder it. The current\n conversation is non-durable operator input.\n@@ -48,14 +51,14 @@\n \n A runtime may only do work that is explicitly assigned by the Goal, task, PR, or MergeGate decision.\n \n-Do not assume `openai/codex-action`, `OPENAI_API_KEY`, or API-key based Codex automation in the default Shiki path. API-key runners are explicit target-repo extensions and require their own ADR.\n+Do not assume `openai/codex-action`, `OPENAI_API_KEY`, or API-key based Codex automation in the default Shiki path. API-key runners are explicit target-repo extensions and require their own target ADR.\n \n ## 4. Non-negotiable Shiki flow\n \n For any non-trivial Goal, use this flow:\n \n 1. **Goal Seek**: define outcome, non-goals, risk level, completion criteria, and evidence requirements.\n-2. **grill-with-docs**: challenge the plan against `CONTEXT.md`, ADRs, code reality, terminology, boundaries, and edge scenarios. Ask one question at a time when the operator is available. If code can answer a question, inspect code instead of asking.\n+2. **grill-with-docs**: challenge the plan against `CONTEXT.md`, Shiki SADRs, target ADRs, code reality, terminology, boundaries, and edge scenarios. Ask one question at a time when the operator is available. If code can answer a question, inspect code instead of asking.\n 3. **Context & Impact**: identify modules, interfaces, seams, callers, dependencies, locks, risk, and verification surfaces.\n 4. **to-prd + Spec Freeze**: turn settled context into a PRD using domain vocabulary and testing decisions. The operator's explicit approval of the PRD is Spec Freeze, recorded as the plan's `spec_freeze` block; plans without it do not run. Steps 1-4 form Requirements Definition.\n 5. **to-issues**: decompose the PRD into independently grabbable vertical-slice issues. Prefer AFK slices; mark HITL slices when judgment is still required.\n@@ -75,7 +78,7 @@\n Before making any material change:\n \n 1. Read `AGENTS.md` and any runtime-specific wrapper such as `CLAUDE.md`.\n-2. Read `CONTEXT.md` and relevant ADRs.\n+2. Read `CONTEXT.md` and relevant SADRs and target ADRs.\n 3. Read the GitHub Issue / PR and `.shiki/` state for the active Goal or task.\n 4. Identify your role: Planner, Implementer, Reviewer, Completion Check Agent, Repairer, or Guardian-assist.\n 5. Identify Goal id, task id, branch, PR, dependencies, locks, risk level, required skills, and required evidence.\n@@ -103,7 +106,7 @@\n \n At minimum, identify:\n \n-- Relevant documents, ADRs, domain terms, and prior decisions.\n+- Relevant documents, SADRs, target ADRs, domain terms, and prior decisions.\n - Relevant modules, interfaces, seams, callers, owners, and tests.\n - Dependency relationships between tasks.\n - Candidate file locks and lock conflicts.\n@@ -152,7 +155,7 @@\n \n CCA must read:\n \n-- `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant ADRs.\n+- `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant SADRs and target ADRs.\n - Parent Goal issue, PRD issue, task issue, comments, and labels.\n - PR body, diff, commits, changed files, reviews, CI checks, and artifacts available to the workflow.\n - `.shiki/` task contract, locks, ledger entries, CCA checklist profile, and prior repair packets.\n@@ -214,7 +217,7 @@\n - Three or more new files.\n - Any high-risk or critical label.\n \n-When the architecture gate triggers, run `grill-with-docs` or `improve-codebase-architecture` as appropriate and record the decision in an ADR if it is hard to reverse or likely to be re-litigated.\n+When the architecture gate triggers, run `grill-with-docs` or `improve-codebase-architecture` as appropriate and record the decision in a Shiki SADR or target ADR as applicable if it is hard to reverse or likely to be re-litigated.\n \n ## 14. Skill Gate\n \n@@ -223,7 +226,7 @@\n Use these skills:\n \n - `setup-matt-pocock-skills`: before first use of the engineering skill set, or when issue tracker, triage labels, or domain docs are missing.\n-- `grill-with-docs`: default for non-trivial Goals before PRD/issues; required for ambiguous plans, terminology, boundaries, ADR-worthy tradeoffs, or design-tree decisions.\n+- `grill-with-docs`: default for non-trivial Goals before PRD/issues; required for ambiguous plans, terminology, boundaries, decision-record-worthy tradeoffs, or design-tree decisions.\n - `zoom-out`: unfamiliar code area or missing architectural map.\n - `to-prd`: convert settled context into a PRD.\n - `to-issues`: break a PRD, plan, or Goal into independently grabbable vertical-slice issues.\ndiff --git a/docs/adr/0001-keep-shiki-as-platform-name.md b/docs/adr/SADR-0001-keep-shiki-as-platform-name.md\nindex 20a9637c3b9d8c97de7ba8db7cfae4d82d9a7dba..8b685439959aada1b221c112f0ac3c51ca8a1695\n--- a/docs/adr/0001-keep-shiki-as-platform-name.md\n+++ b/docs/adr/SADR-0001-keep-shiki-as-platform-name.md\n@@ -1,3 +1,3 @@\n-# Keep Shiki As The Platform Name\n+# SADR-0001: Keep Shiki As The Platform Name\n \n Shiki remains the product name for the GitHub-native agentic engineering control plane. We considered more descriptive names such as MergePilot, MergeGate, and Kiroku, but kept Shiki because it carries the existing goal-seeking framework identity while remaining independent of Hermes, Codex, Claude Code, or any single runtime.\ndiff --git a/docs/adr/0002-use-agents-as-shared-constitution.md b/docs/adr/SADR-0002-use-agents-as-shared-constitution.md\nindex 73de30ecb078120eac540d9bc51855e3cd48d0e3..c5486182bf06fca36ea508496b013c67302fd989\n--- a/docs/adr/0002-use-agents-as-shared-constitution.md\n+++ b/docs/adr/SADR-0002-use-agents-as-shared-constitution.md\n@@ -1,3 +1,3 @@\n-# Use AGENTS As The Shared Agent Constitution\n+# SADR-0002: Use AGENTS As The Shared Agent Constitution\n \n Shiki uses `AGENTS.md` as the runtime-neutral constitution and `CLAUDE.md` as a thin Claude Code wrapper. This keeps Codex, Claude Code, Hermes Runner, and future Agent Runtimes aligned on the same GitHub-first MergeGate rules while still allowing runtime-specific instructions where needed.\ndiff --git a/docs/adr/0003-require-skill-gate-for-mergegate-readiness.md b/docs/adr/SADR-0003-require-skill-gate-for-mergegate-readiness.md\nindex 424be0c9af4c8f0136f1fde68a468c47ff84e764..7fc337987c7df9792f75f181dfcbed75fe5ffff2\n--- a/docs/adr/0003-require-skill-gate-for-mergegate-readiness.md\n+++ b/docs/adr/SADR-0003-require-skill-gate-for-mergegate-readiness.md\n@@ -1,3 +1,3 @@\n-# Require Skill Gate For MergeGate Readiness\n+# SADR-0003: Require Skill Gate For MergeGate Readiness\n \n Shiki requires relevant engineering skills to be invoked before execution when their triggers apply. This makes skills part of MergeGate readiness rather than optional prompt style, because Shiki depends on repeatable planning, issue decomposition, TDD, diagnosis, triage, and architecture review before autonomous or parallel agent work can be trusted.\ndiff --git a/docs/adr/0004-use-github-first-with-shiki-mirror.md b/docs/adr/SADR-0004-use-github-first-with-shiki-mirror.md\nindex d0a3a9174dc646a945a9d325954ffaf00c352040..a79c54ae9ee8eed2a0524192b864da4e22a92d3f\n--- a/docs/adr/0004-use-github-first-with-shiki-mirror.md\n+++ b/docs/adr/SADR-0004-use-github-first-with-shiki-mirror.md\n@@ -1,4 +1,4 @@\n-# ADR 0004: Use GitHub First With A Repository-Local Shiki Mirror\n+# SADR-0004: Use GitHub First With A Repository-Local Shiki Mirror\n \n ## Status\n \ndiff --git a/docs/adr/0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md b/docs/adr/SADR-0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\nindex b339baf49b89d9290e79c3d51b36e7b2bde3bd95..0f3c3eb9afccdb3403ce978e21d04fe12e495da0\n--- a/docs/adr/0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\n+++ b/docs/adr/SADR-0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\n@@ -1,8 +1,8 @@\n-# ADR 0005: Use Codex As Front And Claude Code Action As The GitHub Runtime\n+# SADR-0005: Use Codex As Front And Claude Code Action As The GitHub Runtime\n \n ## Status\n \n-Accepted (implementer default superseded by ADR 0008; the auth model and the\n+Accepted (implementer default superseded by SADR-0008; the auth model and the\n Claude Code Action GitHub runtime decision remain in force)\n \n ## Context\n@@ -26,4 +26,4 @@\n - Shiki avoids requiring `OPENAI_API_KEY` for the default workflow.\n - Codex work happens through the user's authenticated Codex front surface, not as a hidden backend Action.\n - Claude Code Action is configured for OAuth-token based subscription usage by default.\n-- API-key based automation remains possible only as an explicit target-repo extension with its own ADR.\n+- API-key based automation remains possible only as an explicit target-repo extension with its own target ADR.\ndiff --git a/docs/adr/0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md b/docs/adr/SADR-0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\nindex 4bcae9d1e52f0a37b8e9fbafa5cd14b6ee1993bd..7a38538a6ae9929567f0ade67e7ea6a60d0b3f7e\n--- a/docs/adr/0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\n+++ b/docs/adr/SADR-0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\n@@ -1,4 +1,4 @@\n-# ADR 0006: Separate Codex Implementation, CCA Completion, And MergeGate Authorization\n+# SADR-0006: Separate Codex Implementation, CCA Completion, And MergeGate Authorization\n \n ## Status\n \ndiff --git a/docs/adr/0007-packaging-and-release.md b/docs/adr/SADR-0007-packaging-and-release.md\nindex 79b84ddb97167372b44641e5b7860e400ce80822..bb445a2e2e856d9d1f5fdbe35de8d4e8cec0fb32\n--- a/docs/adr/0007-packaging-and-release.md\n+++ b/docs/adr/SADR-0007-packaging-and-release.md\n@@ -1,4 +1,4 @@\n-# ADR 0007: Packaging And Release For Shiki\n+# SADR-0007: Packaging And Release For Shiki\n \n ## Status\n \n@@ -25,7 +25,7 @@\n   not introduce third-party runtime dependencies or break the shim.\n - Releases must run inside the operator's subscription-authenticated toolchain\n   and must not require extra secrets beyond the built-in `GITHUB_TOKEN`\n-  (consistent with ADR 0005's no-API-key-by-default stance).\n+  (consistent with SADR-0005's no-API-key-by-default stance).\n - `.shiki/manifest.json` already encodes the install surface (`install.include`,\n   `install.create_directories`, `install.exclude_from_commit`), and it must not\n   be mutated as part of this change.\n@@ -34,7 +34,7 @@\n \n This is a hard-to-reverse platform decision (it sets the version source of\n truth, the release trigger, and the public install contract), so it is recorded\n-as an ADR.\n+as a SADR.\n \n ## Decision\n \n@@ -104,7 +104,7 @@\n 3. **Publishing to PyPI.** Rejected for now: Shiki is distributed as a\n    repository template + git-installable CLI, not a PyPI library; PyPI would add\n    credentials/secrets and a different versioning contract. Can be added later as\n-   its own ADR if a packaged distribution is needed.\n+   its own SADR if a packaged distribution is needed.\n 4. **Dynamic version (read `VERSION` via a build backend `dynamic` field).**\n    Rejected to keep `pyproject.toml` backend-agnostic and dependency-free; the\n    small duplication between `VERSION` and `pyproject.toml` is guarded by the\n@@ -123,4 +123,4 @@\n   releasing versions the whole platform but ships only `install.include` to\n   targets.\n - Future PyPI distribution or a console entry point are deferred and would each\n-  warrant their own ADR.\n+  warrant their own SADR.\ndiff --git a/docs/adr/0008-use-claude-code-as-default-implementer.md b/docs/adr/SADR-0008-use-claude-code-as-default-implementer.md\nindex dedf4510b7c7799521422f622029563d373db031..35c2625d908167fbc58855b5b63e283805f167e8\n--- a/docs/adr/0008-use-claude-code-as-default-implementer.md\n+++ b/docs/adr/SADR-0008-use-claude-code-as-default-implementer.md\n@@ -1,12 +1,12 @@\n-# ADR 0008: Use Claude Code As The Default Implementer Runtime\n+# SADR-0008: Use Claude Code As The Default Implementer Runtime\n \n ## Status\n \n-Proposed (supersedes ADR 0005 for the implementer default; keeps its auth model)\n+Proposed (supersedes SADR-0005 for the implementer default; keeps its auth model)\n \n ## Context\n \n-ADR 0005 made Codex Front the default implementation runtime, with Claude Code\n+SADR-0005 made Codex Front the default implementation runtime, with Claude Code\n restricted to planner and reviewer roles by the runtime registry, and the only\n autonomous implementation adapter was `shiki runner codex`.\n \n@@ -18,13 +18,13 @@\n - The platform's planning, review, and CCA layers already run on Claude Code,\n   and Claude Code's native capabilities (the built-in `/code-review` skill and\n   the Workflow multi-agent orchestration tool) have no Codex equivalent.\n-- The Goal lifecycle redesign (ADR 0009) requires an autonomous post-freeze\n+- The Goal lifecycle redesign (SADR-0009) requires an autonomous post-freeze\n   loop driven from the operator's Claude Code session, including bounded\n   repair dispatch, which must not depend on a runtime that cannot be invoked\n   unattended.\n \n The constitution claims Shiki is runtime-agnostic (\"not a Claude-only\n-workflow\", ADR 0002). Any change to the implementer default must preserve the\n+workflow\", SADR-0002). Any change to the implementer default must preserve the\n registry-based runtime model rather than hardcoding one vendor.\n \n ## Decision\n@@ -39,7 +39,7 @@\n   the task to review or repair-needed.\n - Keep Codex registered as an optional implementer runtime. `shiki runner\n   codex`, the Codex handoff templates, and the ChatGPT OAuth auth model from\n-  ADR 0005 remain supported for tasks explicitly assigned to `codex`.\n+  SADR-0005 remain supported for tasks explicitly assigned to `codex`.\n - Commit to mandating two Claude-native capabilities as loop contract in\n   follow-up Goals (status: the code-review gate was delivered by Goal G-B \u2014\n   skill registry, checklists item PR-12, default required_skills \u2014 while the\n@@ -60,7 +60,7 @@\n \n Out of scope: removing Codex support, changing the CCA runtime (Claude Code\n Action with `CLAUDE_CODE_OAUTH_TOKEN` stays), and API-key based automation\n-(still requires its own ADR per ADR 0005).\n+(still requires its own target ADR per SADR-0005).\n \n ## Consequences\n \n@@ -100,7 +100,7 @@\n   harness rejection of direct `codex exec` is outside Shiki's control, and the\n   plan's mandatory native capabilities cannot run on Codex at all.\n - **Remove Codex entirely**: rejected; it abandons the runtime-agnostic\n-  constitution (ADR 0002) for no operational gain and forecloses\n+  constitution (SADR-0002) for no operational gain and forecloses\n   cross-runtime adversarial review.\n - **GitHub Actions cloud implementation via claude-code-action**: rejected as\n   default; durable evidence is automatic but token cost, Node 24 workflow\ndiff --git a/docs/adr/0009-requirements-definition-and-spec-freeze.md b/docs/adr/SADR-0009-requirements-definition-and-spec-freeze.md\nindex 29b3f7871bef9203a8b4a831a65c39eb2f506e42..52e7dd69ad0aa4efe0b68c62678abbf7df02aaec\n--- a/docs/adr/0009-requirements-definition-and-spec-freeze.md\n+++ b/docs/adr/SADR-0009-requirements-definition-and-spec-freeze.md\n@@ -1,4 +1,4 @@\n-# ADR 0009: Merge Grill And Planning Into Requirements Definition With Spec Freeze\n+# SADR-0009: Merge Grill And Planning Into Requirements Definition With Spec Freeze\n \n ## Status\n \ndiff --git a/docs/adr/0010-external-ai-guardian-review.md b/docs/adr/SADR-0010-external-ai-guardian-review.md\nindex e6108bbe8bb4e73e0ff4684c8f78001a80438c4e..f3510b145664298adafb151b6f0db51b972f01ec\n--- a/docs/adr/0010-external-ai-guardian-review.md\n+++ b/docs/adr/SADR-0010-external-ai-guardian-review.md\n@@ -1,4 +1,4 @@\n-# ADR 0010: External AI Guardian Review As A First-Class Approval Authority\n+# SADR-0010: External AI Guardian Review As A First-Class Approval Authority\n \n ## Status\n \n@@ -8,7 +8,7 @@\n \n Shiki is intended to be an autonomous, recursive, self-improving agent\n framework: authority-in-the-loop, not human-in-the-loop. Until now the Guardian\n-approval required for high/critical-risk merges (guardian-policy.json, ADR\n+approval required for high/critical-risk merges (guardian-policy.json, SADR\n governance) recognized only human authority sources \u2014 a GitHub review approval,\n the `guardian:approved` label applied by a configured Guardian, or a comment\n carrying `Guardian approval granted` plus the current head SHA.\n@@ -89,7 +89,7 @@\n   autonomous design the operator intends and produced the impersonation\n   incident.\n - **Record AI approval as an operator comment**: rejected as forgery of human\n-  authority \u2014 the exact failure this ADR corrects.\n+  authority \u2014 the exact failure this SADR corrects.\n - **Deliver the artifact as a committed `.shiki/` file**: rejected; a committed\n   approval inside the PR being approved is forgeable by anyone who can push.\n - **A dedicated reviewer bot identity instead of a relayed comment**: deferred;\ndiff --git a/docs/adr/0011-autonomous-quality-gate-evidence.md b/docs/adr/SADR-0011-autonomous-quality-gate-evidence.md\nindex bca0246c5d46e44afbde15486a75eec425fc02f5..9c93eac16d8c3a6d4b11bd7224e3955102cd7db4\n--- a/docs/adr/0011-autonomous-quality-gate-evidence.md\n+++ b/docs/adr/SADR-0011-autonomous-quality-gate-evidence.md\n@@ -1,4 +1,4 @@\n-# ADR 0011: Autonomous Quality-Gate Evidence \u2014 Deterministic Facts And An Independent Verifier\n+# SADR-0011: Autonomous Quality-Gate Evidence \u2014 Deterministic Facts And An Independent Verifier\n \n ## Status\n \ndiff --git a/docs/adr/0012-autonomous-loop-closeout-to-main.md b/docs/adr/SADR-0012-autonomous-loop-closeout-to-main.md\nindex 637fad6dde4989c9c2bf0f81f676d8d29991123c..ec7d85f81030a676bcbba28b43eb365422a36eac\n--- a/docs/adr/0012-autonomous-loop-closeout-to-main.md\n+++ b/docs/adr/SADR-0012-autonomous-loop-closeout-to-main.md\n@@ -1,4 +1,4 @@\n-# 0012 \u2014 Autonomous loop pushes goal completion to main via a closeout PR\n+# SADR-0012 \u2014 Autonomous loop pushes goal completion to main via a closeout PR\n \n - Status: accepted\n - Date: 2026-06-18\ndiff --git a/docs/adr/0013-bootstrap-grants-actions-approve-pull-requests.md b/docs/adr/SADR-0013-bootstrap-grants-actions-approve-pull-requests.md\nindex b6c69d64f1c5ced346b88d6859586715b3b5bd0e..cf48ff4a6716267f539ca58edd1a852e7c7a2d64\n--- a/docs/adr/0013-bootstrap-grants-actions-approve-pull-requests.md\n+++ b/docs/adr/SADR-0013-bootstrap-grants-actions-approve-pull-requests.md\n@@ -1,4 +1,4 @@\n-# ADR 0013: Bootstrap Grants GitHub Actions Permission To Create And Approve Pull Requests\n+# SADR-0013: Bootstrap Grants GitHub Actions Permission To Create And Approve Pull Requests\n \n ## Status\n \n@@ -31,7 +31,7 @@\n security tradeoff: it lets repository automation satisfy a human-review gate. In\n a multi-maintainer repo that could let a compromised or buggy workflow\n self-approve changes. So whether bootstrap turns it on by default \u2014 and with\n-what default token scope \u2014 is an ADR-worthy decision, not an implementation\n+what default token scope \u2014 is a SADR-worthy decision, not an implementation\n detail.\n \n ## Decision\n@@ -48,7 +48,7 @@\n Specifically:\n \n - **`can_approve_pull_request_reviews` defaults to `true`.** Shiki's default\n-  posture is solo/AFK autonomous operation (ADR 0008/0009/0012), where the CCA\n+  posture is solo/AFK autonomous operation (SADR-0008/0009/0012), where the CCA\n   Review Bridge is the only path that can satisfy `required_review: true` after\n   the CCA and Guardian gates have already passed. The default makes a\n   freshly-bootstrapped repo able to run the autonomous loop end to end without a\ndiff --git a/docs/adr/0014-codex-app-external-ai-guardian-ui-adapter.md b/docs/adr/SADR-0014-codex-app-external-ai-guardian-ui-adapter.md\nindex 238db06279334caa341c117362df9271382a7fcd..65f12e9939321e1b3e389a34c822fc1549e5155d\n--- a/docs/adr/0014-codex-app-external-ai-guardian-ui-adapter.md\n+++ b/docs/adr/SADR-0014-codex-app-external-ai-guardian-ui-adapter.md\n@@ -1,4 +1,4 @@\n-# ADR 0014: Use Codex App As The External AI Guardian UI Adapter\n+# SADR-0014: Use Codex App As The External AI Guardian UI Adapter\n \n ## Status\n \ndiff --git a/docs/adr/0015-contract-approval-before-dispatch.md b/docs/adr/SADR-0015-contract-approval-before-dispatch.md\nindex fad6c656741a20a7684ba16080a7db159f63a52f..6061271a46d3f1d6ae7174477de3fb75673123a6\n--- a/docs/adr/0015-contract-approval-before-dispatch.md\n+++ b/docs/adr/SADR-0015-contract-approval-before-dispatch.md\n@@ -1,4 +1,4 @@\n-# ADR 0015: Approve The Task Contract Before Dispatch, Not The Diff After It\n+# SADR-0015: Approve The Task Contract Before Dispatch, Not The Diff After It\n \n ## Status\n \n@@ -9,7 +9,7 @@\n Guardian approval for high/critical risk is expressible only on a pull request.\n Every approval source in `guardian-policy.json` \u2014 GitHub review, the\n `guardian:approved` label, the `Guardian approval granted` comment, and the\n-`external_ai_guardian_review` artifact (ADR 0010/0014) \u2014 is bound to a PR, and\n+`external_ai_guardian_review` artifact (SADR-0010/0014) \u2014 is bound to a PR, and\n three of the four require the current head SHA. Approval therefore cannot exist\n before an implementation PR exists.\n \n@@ -81,7 +81,7 @@\n An envelope breach must surface only on the non-repairable policy gate, never on\n the repairable `MergeGate metadata check`. Otherwise the Goal Loop routes it to\n `dispatch_repair` \u2014 instructing a runner to make the breach go away \u2014 which is\n-exactly what this ADR forbids.\n+exactly what this SADR forbids.\n \n **Contract immutability.** After registration, an implementation PR must not\n change the governance fields of its own task contract (`scope`, `non_goals`,\n@@ -116,7 +116,7 @@\n fails any verdict that is not `complete`, and the signal the CCA prompt reads is\n computed from `evaluate_guardian_approval` alone. Without the OR-merge at the\n signal, a contract-approved PR still returns `needs_guardian`, the CCA job fails,\n-and the MergeGate policy check never runs \u2014 the gate this ADR changes would never\n+and the MergeGate policy check never runs \u2014 the gate this SADR changes would never\n be reached. The CCA *prompt* needs no change: a `contract_approval` entry in the\n signal's existing `sources` array satisfies its current wording.\n \n@@ -130,7 +130,7 @@\n written, and an approved contract lets its implementation PRs merge through the\n same automatic path as low/medium risk. The wasted implement-then-block cycle\n disappears. Guardian review shifts from reading diffs to reading contracts, which\n-is both earlier and smaller; the external AI Guardian path (ADR 0010/0014) works\n+is both earlier and smaller; the external AI Guardian path (SADR-0010/0014) works\n on contracts with no change.\n \n We accept that the Guardian authorizes a class of change without seeing its\n@@ -146,7 +146,7 @@\n adversarial tests for the new approval path equivalent in rigor to\n `test_shiki_governance_evidence.sh`.\n \n-Bootstrap costs, which apply only while this ADR is being implemented. The tasks\n+Bootstrap costs, which apply only while this SADR is being implemented. The tasks\n that build Contract Approval merge under the existing rules, so the ones\n dispatched after the pre-dispatch gate lands but before the approval path lands\n need a Guardian-approved Contract PR *and* a live Guardian approval on their\n@@ -210,7 +210,7 @@\n risk; they do not bound authorized work.\n \n **Auto-escalate risk on envelope breach instead of stopping.** Returns the\n-workflow to a diff-time Guardian decision \u2014 the exact behavior this ADR removes.\n+workflow to a diff-time Guardian decision \u2014 the exact behavior this SADR removes.\n \n **Resolve governance fields from the base snapshot only.** Rejected against the\n registration flow: implementation PRs add their own contract, so the base has no\ndiff --git a/docs/adr/0016-locks-bound-product-paths.md b/docs/adr/SADR-0016-locks-bound-product-paths.md\nindex b5e850f3f1f7f4ef6e7f9d1763a29a51bd300c45..46b40a9b99a7aae644c02bb458f5473ff0a90ce2\n--- a/docs/adr/0016-locks-bound-product-paths.md\n+++ b/docs/adr/SADR-0016-locks-bound-product-paths.md\n@@ -1,4 +1,4 @@\n-# ADR 0016: Locks bound product paths; the Shiki mirror is bound by record identity\n+# SADR-0016: Locks bound product paths; the Shiki mirror is bound by record identity\n \n ## Status\n \n@@ -6,8 +6,8 @@\n \n - Date: 2026-07-28\n - Deciders: operator (mizutani-140), Claude (planner)\n-- Related: ADR 0004 (GitHub-first with a `.shiki` mirror), ADR 0009 (Spec Freeze),\n-  ADR 0012 (autonomous loop closeout to main), ADR 0015 (Contract Approval before\n+- Related: SADR-0004 (GitHub-first with a `.shiki` mirror), SADR-0009 (Spec Freeze),\n+  SADR-0012 (autonomous loop closeout to main), SADR-0015 (Contract Approval before\n   dispatch), remediation goal task R-02, the live PR #179 deadlock.\n \n ## Context\n@@ -90,11 +90,11 @@\n files, documentation files and named source modules are nameable; runtime-\n allocated ledger, runner and report ids are not \u2014 and those live in `.shiki`,\n which locks no longer govern. Existing frozen contracts are brought into line by\n-a one-time Spec Amendment per plan, which must land before ADR 0015's contract\n+a one-time Spec Amendment per plan, which must land before SADR-0015's contract\n immutability makes `locks` unwritable from the implementation PR.\n \n **Lock state is resolved from the durable side.** MergeGate resolves the lock\n-registry, and ADR 0015's governance fields, from the base `.shiki` snapshot when\n+registry, and SADR-0015's governance fields, from the base `.shiki` snapshot when\n present, falling back to head only when absent. The `MergeGate metadata check`\n job is given `--base-shiki`, which it does not receive today. Locks are not\n derived from the live open-pull-request set: a pull request body is untrusted\n@@ -108,7 +108,7 @@\n Explicitly out of scope: moving MergeGate's contract resolution off the PR\n checkout wholesale; making the mirror a projection of GitHub state; introducing\n new lock kinds; moving ledger evidence out of the repository; and unwinding\n-ADR 0012's closeout pull request. Registration of multi-task goals is ADR 0015's\n+SADR-0012's closeout pull request. Registration of multi-task goals is SADR-0015's\n Contract PR and is not re-decided here.\n \n ## Consequences\n@@ -127,7 +127,7 @@\n \n Risks accepted. First, goal-scoped identity is only as strong as `goal_id`,\n which is resolved from the PR body and the head task file; the mitigation is to\n-add `goal_id` to ADR 0015's frozen governance field set, and until that lands a\n+add `goal_id` to SADR-0015's frozen governance field set, and until that lands a\n task file can name a foreign goal. Second, base-when-present resolution is\n unavailable on fork pull requests, where the head is contributor-controlled;\n fork pull requests already cannot complete a Shiki merge, and this decision does\n@@ -139,7 +139,7 @@\n path, not by implementation pull requests.\n \n New obligations: one Spec Amendment per frozen plan to rewrite `locks`, ordered\n-before ADR 0015 T1; extension of the adversarial suites in\n+before SADR-0015 T1; extension of the adversarial suites in\n `scripts/test_shiki_adversarial_state.sh` and\n `scripts/test_shiki_governance_evidence.sh` to cover the eleven newly-scoped\n directories; and updating `scripts/test_shiki_loop_lock_guard.sh`,\n@@ -151,32 +151,32 @@\n whether the loop's transitive sync of ledger `evidence` references can be\n deleted once the Contract PR carries its own source plan.\n \n-## Relationship to ADR 0004\n+## Relationship to SADR-0004\n \n-This ADR **clarifies something ADR 0004 left unspecified**. ADR 0004 decides that\n+This SADR **clarifies something SADR-0004 left unspecified**. SADR-0004 decides that\n GitHub is the operational source of truth and that each target repository keeps a\n `.shiki/` mirror. It says nothing about pull requests writing the mirror, does\n not name MergeGate, and does not make the mirror an input to a gate. Nothing in\n-ADR 0004's Decision or Consequences is amended, superseded or weakened: the\n+SADR-0004's Decision or Consequences is amended, superseded or weakened: the\n mirror still exists, still records the same artifacts, and pull request\n-boundaries remain the coordination surface. What this ADR settles is the question\n+boundaries remain the coordination surface. What this SADR settles is the question\n 0004 never reached \u2014 by what authority a pull request may write a mirror record \u2014\n and it answers it the way `.shiki/manifest.json` already declares\n (`pr_mutation: current-task-or-current-goal-only`).\n \n-It **amends ADR 0012**, whose Decision item 5 states that \"Loop-executed tasks\n+It **amends SADR-0012**, whose Decision item 5 states that \"Loop-executed tasks\n declare the `path:.shiki/**` lock, which covers every `.shiki` file the closeout\n stages ... so `files_outside_locks` is satisfied without per-file lock\n bookkeeping.\" That clause is replaced: the closeout's mirror writes are\n-authorized by record identity, not by a declared lock. ADR 0012's substantive\n+authorized by record identity, not by a declared lock. SADR-0012's substantive\n decision \u2014 that completion reaches `main` through a closeout pull request rather\n than local attestation \u2014 is preserved unchanged.\n \n-It is **complementary to ADR 0015**, not a substitute. ADR 0015 owns contract\n+It is **complementary to SADR-0015**, not a substitute. SADR-0015 owns contract\n immutability, the pre-dispatch gate, the Contract PR and the Scope Envelope. This\n-ADR owns what a lock means and who may write the mirror. The one ordering\n+SADR owns what a lock means and who may write the mirror. The one ordering\n constraint between them is that the lock-narrowing Spec Amendment must precede\n-ADR 0015 T1's freezing of the `locks` field.\n+SADR-0015 T1's freezing of the `locks` field.\n \n ## Alternatives Considered\n \n@@ -186,7 +186,7 @@\n the only writer that can register them may run. Migrating the Guardian signal to\n that store makes every registration PR fail closed to `needs_guardian` at every\n risk level, because the task file is by definition absent from the store, which\n-removes the low/medium autonomous path ADR 0008/0009 exists to provide. And it\n+removes the low/medium autonomous path SADR-0008/0009 exists to provide. And it\n leaves schemas, manifest, config, guardian policy, templates, migrations, plans,\n worktrees, runner records, runs and handoffs with no writer at all in a\n repository whose product is `.shiki`. Measured benefit on the frozen backlog:\n@@ -199,14 +199,14 @@\n namespace is not in fact ownership-partitioned \u2014 56 ledger entries carry no\n `task_id`, and one goal DAG is writable by 35 registered tasks. The correct\n expression of its insight is that locks do not govern the mirror at all, which is\n-what this ADR decides.\n+what this SADR decides.\n \n **Make the mirror a projection written only by a registration writer and a\n post-merge projection job.** Rejected. It requires a bypass actor with write\n access to the protected default branch; it makes its own master invariant\n readable from a manifest that pull requests may edit; it deletes both reconcile\n modes and leaves a corrupted mirror with no in-band repair path, contradicting\n-ADR 0004's consequence that mirror conflicts are repaired against GitHub state;\n+SADR-0004's consequence that mirror conflicts are repaired against GitHub state;\n and its content-derived ids are rejected by `validate_shiki`'s currently-green id\n pattern.\n \ndiff --git a/docs/adr/0017-bookkeeping-closeouts-do-not-inherit-risk.md b/docs/adr/SADR-0017-bookkeeping-closeouts-do-not-inherit-risk.md\nindex 554e3a0d526a72ca067108c5464cc68c3edcf174..b9676857a4713850e063af7c39c8e0b01ecb4c66\n--- a/docs/adr/0017-bookkeeping-closeouts-do-not-inherit-risk.md\n+++ b/docs/adr/SADR-0017-bookkeeping-closeouts-do-not-inherit-risk.md\n@@ -1,4 +1,4 @@\n-# ADR 0017: A Bookkeeping Closeout Does Not Inherit Its Task's Risk\n+# SADR-0017: A Bookkeeping Closeout Does Not Inherit Its Task's Risk\n \n ## Status\n \n@@ -6,7 +6,7 @@\n \n ## Context\n \n-ADR 0012 made the goal loop push a task's completion to the default branch through a\n+SADR-0012 made the goal loop push a task's completion to the default branch through a\n closeout pull request: after the implementation PR merges, the loop opens a second PR\n carrying `task.status = done`, `lock.state = released`, and \u2014 when the task completes\n its goal \u2014 `goal.status = complete` plus one scorecard report. Nothing else. The\n@@ -49,14 +49,14 @@\n 4. The task file's governance fields (`scope`, `non_goals`, `required_skills`,\n    `risk_level`, `locks`, `acceptance_checks`, `test_command`) are byte-identical to\n    the base snapshot. Only `status`, `expected_pr`, `closeout_pr`, `ledger_evidence`\n-   and `expected_branch` may differ \u2014 the ADR 0015 contract-immutability rule,\n+   and `expected_branch` may differ \u2014 the SADR-0015 contract-immutability rule,\n    unchanged.\n \n    `expected_branch` is in that set because a closeout is *by construction* on a\n    different branch from the implementation it closes, and the loop rewrites the\n    field accordingly. It is safe to let it differ because MergeGate independently\n    requires `expected_branch` to equal the PR's actual head ref, so a PR cannot set\n-   it to anything other than the branch it is really on. The first draft of this ADR\n+   it to anything other than the branch it is really on. The first draft of this SADR\n    omitted it, which made the exemption unreachable: every real closeout failed\n    condition 4 on a field the loop always changes. See PR #209.\n 5. The state transitions are exactly the terminal set: task `review` \u2192 `done`, lock\n@@ -113,12 +113,12 @@\n classification must be derived, not asserted.\n \n **Fold the closeout into the implementation PR** so no second PR exists. Rejected under\n-ADR 0012, which recorded why: a task file that is already `done` on the implementation\n+SADR-0012, which recorded why: a task file that is already `done` on the implementation\n branch short-circuits the loop's review \u2192 CCA \u2192 merge \u2192 done ordering into a local\n goal-completion before any merge happens.\n \n **Have the loop auto-merge closeouts with elevated credentials.** Rejected outright: that\n-is a runtime authoring its own approval, which ADR 0010 and the constitution's\n+is a runtime authoring its own approval, which SADR-0010 and the constitution's\n approval-record prohibition forbid. It would also be indistinguishable, in the evidence,\n from the forgery those rules exist to prevent.\n \ndiff --git a/docs/adr/0018-carried-guardian-approval-across-a-proven-base-sync.md b/docs/adr/SADR-0018-carried-guardian-approval-across-a-proven-base-sync.md\nindex 22358562da390ed8b45cd600da08a43d378b4680..e6e2907cefa776460af73ddf7d39db605f3c39b0\n--- a/docs/adr/0018-carried-guardian-approval-across-a-proven-base-sync.md\n+++ b/docs/adr/SADR-0018-carried-guardian-approval-across-a-proven-base-sync.md\n@@ -1,4 +1,4 @@\n-# ADR 0018: Carried Guardian Approval Across A Proven Base Sync\n+# SADR-0018: Carried Guardian Approval Across A Proven Base Sync\n \n ## Status\n \n@@ -34,7 +34,7 @@\n Guardian evaluator that consumes it (`shiki_guardian.evaluate_guardian_approval`,\n the `guardian_comment_carried` source) were built and tested in isolation. Neither\n does anything until a caller passes the flags that turn the carry on, so the\n-feature was invisible in CI. This ADR records the decision the two of them\n+feature was invisible in CI. This SADR records the decision the two of them\n implement, and this task wires it into the four CI invocations that judge Guardian\n approval.\n \ndiff --git a/docs/adr/README.md b/docs/adr/README.md\nindex c70e057a4520c36e042ab68c53cd4a21ef709b71..5da43856836e2ce6a150f8127766e2f88355ab4b\n--- a/docs/adr/README.md\n+++ b/docs/adr/README.md\n@@ -1,40 +1,41 @@\n-# Architecture Decision Records\n+# Decision Record Namespaces\n \n-This directory holds Shiki's Architecture Decision Records (ADRs): durable\n-records of hard-to-reverse platform decisions. ADRs are part of the Shiki source\n-of truth alongside GitHub state, the `.shiki/` mirror, and `CONTEXT.md`.\n+This directory holds two independent decision namespaces:\n \n-## How ADRs Work\n+- Shiki Architecture Decision Records (SADRs), named\n+  `SADR-NNNN-<short-slug>.md`, record hard-to-reverse Shiki platform decisions.\n+- Target Architecture Decision Records (ADRs), named\n+  `NNNN-<short-slug>.md`, belong to the target product.\n \n-- New ADRs are added as `docs/adr/NNNN-<short-slug>.md`, where `NNNN` is the next\n-  zero-padded sequence number.\n-- The **canonical list of ADRs is the set of files matching `docs/adr/*.md`** in\n-  this directory. Browse `docs/adr/*.md` for the authoritative, current set.\n-- Use [`template.md`](template.md) as the starting point for a new ADR.\n-- Record an ADR when a decision is hard to reverse, surprising without context,\n-  and a real tradeoff. Do not write an ADR for routine or easily reversible\n-  choices.\n+The same four-digit number may appear once in each namespace. A platform\n+`SADR-NNNN` citation resolves only to `SADR-NNNN-*.md`; a target `ADR NNNN`\n+citation resolves only to `NNNN-*.md`.\n \n-Using the `docs/adr/*.md` glob as the source of truth means other PRs can add an\n-ADR without editing this index file, avoiding merge contention on a\n-hand-maintained enumeration.\n+## How Decision Records Work\n \n-## Current ADRs (snapshot)\n-\n-The list below is a convenience snapshot and may lag behind the directory. When\n-in doubt, list `docs/adr/*.md`.\n+- New Shiki platform decisions use the next zero-padded SADR number.\n+- New target product decisions use the target's own ADR sequence.\n+- The canonical list is the files in `docs/adr/`; no hand-maintained index is\n+  authoritative.\n+- Use [`template.md`](template.md) as the starting point and choose the filename\n+  namespace that owns the decision.\n+- Record a decision when it is hard to reverse, surprising without context, and\n+  a real tradeoff. Do not write a decision record for routine or easily\n+  reversible choices.\n \n-- [0001 \u2014 Keep Shiki As The Platform Name](0001-keep-shiki-as-platform-name.md)\n-- [0002 \u2014 Use AGENTS As The Shared Agent Constitution](0002-use-agents-as-shared-constitution.md)\n-- [0003 \u2014 Require Skill Gate For MergeGate Readiness](0003-require-skill-gate-for-mergegate-readiness.md)\n-- [0004 \u2014 Use GitHub First With A Repository-Local Shiki Mirror](0004-use-github-first-with-shiki-mirror.md)\n-- [0005 \u2014 Use Codex As Front And Claude Code Action As The GitHub Runtime](0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md)\n-- [0006 \u2014 Separate Codex Implementation, CCA Completion, And MergeGate Authorization](0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md)\n+Using the directory as the source of truth lets independent changes add records\n+without contending on an enumerated index.\n \n-## Adding A New ADR\n+## Adding A Shiki SADR\n \n-1. Copy `template.md` to `docs/adr/NNNN-<short-slug>.md`.\n+1. Copy `template.md` to `docs/adr/SADR-NNNN-<short-slug>.md`.\n 2. Fill in the title, status, context, decision, and consequences.\n-3. Set the status to `Proposed`, then `Accepted` once the decision is approved.\n-4. Open a PR following `CONTRIBUTING.md`. You do not need to edit this index for\n-   the ADR to be discoverable via `docs/adr/*.md`.\n+3. Set the status to `Proposed`, then `Accepted` once the proper authority has\n+   approved the decision.\n+4. Open a PR following `CONTRIBUTING.md`.\n+\n+## Adding A Target ADR\n+\n+1. Copy `template.md` to `docs/adr/NNNN-<short-slug>.md` using the target's own\n+   sequence.\n+2. Cite it as `ADR NNNN`; do not prefix its filename with `SADR-`.\ndiff --git a/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md b/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\nnew file mode 100644\nindex 0000000000000000000000000000000000000000..7455ce1f3bed02ddddb851ac21a1d1a648c9aeba\n--- /dev/null\n+++ b/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\n@@ -0,0 +1,55 @@\n+# SADR-0019: Separate Shiki Platform SADRs From Target ADRs\n+\n+## Status\n+\n+Accepted\n+\n+## Context\n+\n+Shiki installs its platform decision records into `docs/adr/` in every target\n+repository. Shiki and its targets previously used the same numeric\n+`NNNN-<slug>.md` namespace. A target that already owned ADRs 0013 through 0018\n+therefore acquired two records for each number during installation, making an\n+`ADR NNNN` citation ambiguous. Renumbering target decisions would rewrite target\n+history, while renumbering Shiki decisions per target would destroy their stable\n+platform identity.\n+\n+Upgrades also need to remove the former Shiki-owned numeric paths without ever\n+mistaking a target-owned ADR for platform content. The install stamp already\n+records exact shipped paths and content digests, so it can prove ownership only\n+when both still match.\n+\n+## Decision\n+\n+Shiki platform decisions use the `SADR` namespace:\n+\n+- records are named `docs/adr/SADR-NNNN-<slug>.md`;\n+- live platform citations use `SADR-NNNN`;\n+- target product decisions retain `docs/adr/NNNN-<slug>.md` and `ADR NNNN`;\n+- the reference validator resolves the two namespaces independently, so the same\n+  four-digit number may exist once in each namespace.\n+\n+Before a forced upgrade writes anything, the installer inventories the exact\n+former numeric Shiki ADR paths. It may delete a legacy path only when the\n+existing install stamp contains that exact path and its recorded SHA-256 digest\n+matches the file currently on disk. All ownership blockers are reported\n+together and abort the upgrade before its first write. A non-forced install\n+never performs this cleanup.\n+\n+Historical `.shiki/` evidence remains unchanged because it records the namespace\n+that existed when that evidence was produced.\n+\n+## Consequences\n+\n+- Targets may use any ADR number without colliding with Shiki's platform SADRs.\n+- Fresh installs preserve target ADR bytes and install a stable SADR set.\n+- Forced upgrades fail closed when legacy ownership cannot be proven, and a\n+  successful upgrade refreshes the stamp without removed legacy paths.\n+- Current governance, packaging, workflow, prompt, script, and skill references\n+  must move with the platform records.\n+\n+## Alternatives Considered\n+\n+- Collision-only refusal leaves affected targets permanently unupgradable.\n+- Renumbering target ADRs mutates target-owned history.\n+- Per-target Shiki renumbering makes platform citations non-portable.\ndiff --git a/docs/adr/template.md b/docs/adr/template.md\nindex a92a97daddd0b00ab9dd626cc8d86942da2eafed..17dd79175b5fee2105935557f691b40cf9492942\n--- a/docs/adr/template.md\n+++ b/docs/adr/template.md\n@@ -1,14 +1,14 @@\n-# NNNN: <Short Decision Title>\n+# <SADR-NNNN or NNNN>: <Short Decision Title>\n \n ## Status\n \n-Proposed | Accepted | Superseded by ADR-NNNN\n+Proposed | Accepted | Superseded by SADR-NNNN or target ADR NNNN\n \n ## Context\n \n What is the situation, constraint, or force that requires a decision? Reference\n-relevant domain terms (`CONTEXT.md`), prior ADRs, and code reality. State the\n-problem, not the solution.\n+relevant domain terms (`CONTEXT.md`), prior Shiki SADRs or target ADRs, and code\n+reality. State the problem, not the solution.\n \n ## Decision\n \ndiff --git a/docs/agents/autonomous-loop-e2e-acceptance.md b/docs/agents/autonomous-loop-e2e-acceptance.md\nindex bbf84c6081987f159ac6ad9ecf3c82b7aa72e7e8..5c13c1d02a05a90b0dc46cbef7c95a9846d0c237\n--- a/docs/agents/autonomous-loop-e2e-acceptance.md\n+++ b/docs/agents/autonomous-loop-e2e-acceptance.md\n@@ -40,8 +40,8 @@\n 3. `gh auth status` is logged in with merge permission on the repo.\n 4. Branch protection + required checks are configured exactly as the goal-loop\n    required-check set expects (see `docs/agents/branch-protection-smoke.md`).\n-5. The repo `.shiki/policy` permits autonomous low/medium-risk merges (ADR\n-   0008/0009); the MergeGate policy check is the recorded-authority gate.\n+5. The repo `.shiki/policy` permits autonomous low/medium-risk merges\n+   (SADR-0008/0009); the MergeGate policy check is the recorded-authority gate.\n \n The operator's only actions are: register the task and start the loop. **No\n manual pre-clearing** means after `shiki loop run` starts, the operator does not\ndiff --git a/docs/agents/bootstrap-command.md b/docs/agents/bootstrap-command.md\nindex 05f7f203a52cf412b9f97084a6e131ed0f785afd..f7aa0b734dfebee4a3b139335b66ee3415f48713\n--- a/docs/agents/bootstrap-command.md\n+++ b/docs/agents/bootstrap-command.md\n@@ -189,7 +189,9 @@\n ```\n \n Use this only for tests, fixtures, or explicit local-only template inspection.\n-Use `--force` only when you intentionally want to overwrite existing target files.\n+Use `--force` only when you intentionally want to refresh the target template.\n+Legacy numeric Shiki decision cleanup is stamp-and-digest authorized before any\n+write; target ADR paths are not cleanup candidates.\n \n ## Slash Command\n \ndiff --git a/docs/agents/checklists.md b/docs/agents/checklists.md\nindex 4a7f03ade346f53a1c1d82981f3fd744f9ab4547..0c5ec6c53dc7197b7b7a7d18e667b325babd6c45\n--- a/docs/agents/checklists.md\n+++ b/docs/agents/checklists.md\n@@ -32,18 +32,18 @@\n | ID | Item | Blocking |\n | --- | --- | --- |\n | GD-01 | Plan was challenged against `CONTEXT.md`. | blocking for non-trivial Goals |\n-| GD-02 | Relevant ADRs were read or absence was recorded. | blocking for non-trivial Goals |\n+| GD-02 | Relevant SADRs and target ADRs were read or absence was recorded. | blocking for non-trivial Goals |\n | GD-03 | Ambiguous terms were resolved or converted to HITL blockers. | blocking |\n | GD-04 | Edge scenarios were considered. | advisory |\n | GD-05 | Code reality was checked when code could answer. | blocking when applicable |\n | GD-06 | New domain terms were added to `CONTEXT.md` when needed. | blocking when applicable |\n-| GD-07 | ADR was created or explicitly skipped using the ADR criteria. | blocking when applicable |\n+| GD-07 | A SADR or target ADR was created or explicitly skipped using the decision-record criteria. | blocking when applicable |\n \n ## CI \u2014 Context & Impact Checklist\n \n | ID | Item | Blocking |\n | --- | --- | --- |\n-| CI-01 | Relevant docs and ADRs identified. | blocking |\n+| CI-01 | Relevant docs, SADRs, and target ADRs identified. | blocking |\n | CI-02 | Relevant modules, interfaces, seams, callers, and tests identified. | blocking |\n | CI-03 | Dependencies and blocked-by relationships identified. | blocking |\n | CI-04 | Candidate locks identified. | blocking |\n@@ -62,7 +62,7 @@\n | PRD-04 | Implementation decisions are recorded without volatile code detail. | blocking |\n | PRD-05 | Testing decisions are recorded. | blocking |\n | PRD-06 | Out-of-scope items are explicit. | blocking |\n-| PRD-07 | Relevant glossary/ADR links are present when applicable. | blocking |\n+| PRD-07 | Relevant glossary/SADR/target-ADR links are present when applicable. | blocking |\n \n ## SF \u2014 Spec Freeze Checklist\n \n@@ -98,7 +98,7 @@\n | H-02 | Branch/worktree target is present. | blocking |\n | H-03 | Scope and non-goals are present. | blocking |\n | H-04 | Dependencies and locks are present. | blocking |\n-| H-05 | Relevant docs, ADRs, modules, and tests are referenced. | blocking |\n+| H-05 | Relevant docs, SADRs, target ADRs, modules, and tests are referenced. | blocking |\n | H-06 | Required skills are stated. | blocking |\n | H-07 | TDD expectations are stated. | blocking when behavior work |\n | H-08 | Acceptance criteria are included. | blocking |\ndiff --git a/docs/agents/codex-handoff.md b/docs/agents/codex-handoff.md\nindex ede468f47cb277f69a857e8c45df703e4ed0151d..6f97bdffb67e44d7edea2c71e01ea28933c4eb6c\n--- a/docs/agents/codex-handoff.md\n+++ b/docs/agents/codex-handoff.md\n@@ -46,7 +46,7 @@\n \n ## Context\n - Domain terms:\n-- Relevant ADRs:\n+- Relevant SADRs and target ADRs:\n - Relevant docs:\n - Relevant modules/interfaces/seams:\n - Similar tests/prior art:\ndiff --git a/docs/agents/completion-check-agent.md b/docs/agents/completion-check-agent.md\nindex 13e4c035db35025f4e3cdcc5adfb24314ac4d521..61bfa1218cf36ca605250c91919e620f33238aa0\n--- a/docs/agents/completion-check-agent.md\n+++ b/docs/agents/completion-check-agent.md\n@@ -32,7 +32,7 @@\n - `AGENTS.md`\n - `CLAUDE.md`\n - `CONTEXT.md`\n-- relevant ADRs\n+- relevant SADRs and target ADRs\n - parent Goal issue\n - PRD issue or document\n - task issue\ndiff --git a/docs/agents/decision-control.md b/docs/agents/decision-control.md\nindex 7d8ee5ab03a5ccbcbd04056ae61de03785a2a9c9..8c94ecda00fffaa0e92c325ab83aa9776bd1dac1\n--- a/docs/agents/decision-control.md\n+++ b/docs/agents/decision-control.md\n@@ -11,7 +11,7 @@\n | Decision | Owner | When | Required inputs | Output | Enforcement surface |\n | --- | --- | --- | --- | --- | --- |\n | Is the Goal clear enough to plan? | Planner + Guardian when needed | Goal intake | User request, existing docs, risk signals | Goal readiness verdict | Goal checklist, issue template, ledger |\n-| Must `grill-with-docs` run? | Planner / Skill Gate | Before PRD or issue split | Ambiguity, domain terms, ADR conflicts, unclear boundaries | Skill invocation or explicit skip rationale | Skill Gate checklist |\n+| Must `grill-with-docs` run? | Planner / Skill Gate | Before PRD or issue split | Ambiguity, domain terms, SADR/target-ADR conflicts, unclear boundaries | Skill invocation or explicit skip rationale | Skill Gate checklist |\n | Is a PRD required? | Planner | After Goal context stabilizes | Goal, `grill-with-docs` output, Context & Impact output | PRD required / not required | PRD checklist, CCA evidence check |\n | Can the spec be frozen? | Operator only | End of Requirements Definition, before issue split | PRD/requirements summary, scope inventory (SF-02), open-question list | `spec_freeze` block in the plan + ledger evidence | `require_grilled_plan` (plan ingest/run), SF checklist |\n | Can a frozen spec change? | Operator only (Spec Amendment) | When implementation contradicts the frozen spec | Contested decisions, scoped re-grill result | recorded amendment + re-stamped freeze | SF checklist, ledger evidence |\ndiff --git a/docs/agents/domain.md b/docs/agents/domain.md\nindex 258da8ea81e87ac0a6b0b5b5e5c2fde980eaabb7..d57b0cf17ff3e1fa9e6e5885372bf083d8295421\n--- a/docs/agents/domain.md\n+++ b/docs/agents/domain.md\n@@ -5,7 +5,9 @@\n ## Layout\n \n - `CONTEXT.md` defines Shiki language. Keep it implementation-free.\n-- `docs/adr/` records hard-to-reverse decisions.\n+- `docs/adr/SADR-NNNN-*.md` records Shiki platform decisions.\n+- `docs/adr/NNNN-*.md` records target product decisions without sharing the\n+  SADR namespace.\n - `AGENTS.md` is the runtime-neutral agent constitution.\n - `CLAUDE.md` is the Claude Code wrapper around `AGENTS.md`.\n - `.shiki/` is the Target Repository mirror for Goals, PRDs, plans, Task DAGs, contracts, locks, worktree records, ledger entries, CCA verdicts, repair packets, reports, and handoffs.\n@@ -15,6 +17,6 @@\n \n - Use Shiki terms exactly when they exist.\n - Add glossary terms to `CONTEXT.md` only when the term is part of the domain, not a generic programming concept.\n-- Create ADRs only for hard-to-reverse, surprising, tradeoff-based decisions.\n+- Create SADRs or target ADRs only for hard-to-reverse, surprising, tradeoff-based decisions.\n - Keep product-specific language in the Target Repository, not in Shiki Template docs.\n-- If an ADR conflicts with a proposed implementation, surface the conflict before editing.\n+- If a SADR or target ADR conflicts with a proposed implementation, surface the conflict before editing.\ndiff --git a/docs/agents/execution-control.md b/docs/agents/execution-control.md\nindex ad8ee3cdb36be56fedc1f46408ca92b387d74047..bc63c3a73661d12b567016ba299f1a6b66bf9088\n--- a/docs/agents/execution-control.md\n+++ b/docs/agents/execution-control.md\n@@ -63,7 +63,7 @@\n | Branch name | Orchestrator | Orchestrator | Before implementer dispatch | branch policy | branch ref in task/ledger |\n | Lock reservation | Context & Impact | MergeGate | Before dispatch and before merge | `.shiki/locks/`, labels, PR metadata | lock id, owner, scope |\n | CI profile | Planner | MergeGate | During issue triage, rechecked at PR | labels, `.shiki/policy.yaml`, workflow matrix | required check list |\n-| Required status checks | Repo maintainer / Guardian | GitHub branch protection / rulesets | Repository setup, updated via ADR | GitHub settings | branch/ruleset config, ADR |\n+| Required status checks | Repo maintainer / Guardian | GitHub branch protection / rulesets | Repository setup, updated via target ADR | GitHub settings | branch/ruleset config, target ADR |\n | CCA judgment | CCA | GitHub required check / MergeGate | Every non-draft PR update | GitHub Action check | structured CCA verdict |\n | Repair assignment | CCA | MergeGate | After failed CCA/CI/review | repair packet, issue/PR comment | repair id and attempt count |\n | Merge readiness | MergeGate | GitHub branch protection / Guardian if needed | After CCA complete | required checks, review rules, rulesets | mergegate check output |\ndiff --git a/docs/agents/guardian-policy.md b/docs/agents/guardian-policy.md\nindex 4147c7adf05a47adb4fd24ada1a1c2e1f2ba6d2d..a20d9cc6c00a569bb057b9ca8d6b51c7fc4a9847\n--- a/docs/agents/guardian-policy.md\n+++ b/docs/agents/guardian-policy.md\n@@ -98,8 +98,8 @@\n `origin/<default_branch>` and reuses each job's existing base fetch; a shallow\n (`--depth`) or second base fetch would make the proof unresolvable and silently\n disable the carry, so neither is permitted. CCA reads `guardian_comment_carried` as\n-a recorded authority in the deterministic signal and does not re-derive it. See ADR\n-0018 for the full decision, the rejected diff/patch-equality and path-disjointness\n+a recorded authority in the deterministic signal and does not re-derive it. See\n+SADR-0018 for the full decision, the rejected diff/patch-equality and path-disjointness\n alternatives, and the accepted residual.\n \n ## External AI Guardian Review (SADR-0010)\ndiff --git a/docs/agents/implementation-policy.md b/docs/agents/implementation-policy.md\nindex c754144a9fb5bf4a38aa82a60e1670c59e626b44..85c37281bcad196c6b7b02343faf00b03cd9c57a\n--- a/docs/agents/implementation-policy.md\n+++ b/docs/agents/implementation-policy.md\n@@ -31,10 +31,13 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n ## Non-Negotiables\n \n 1. **Planning precedes implementation.** Non-trivial Goals start with `grill-with-docs` and Context & Impact.\n-2. **Domain language is controlled.** Terms must be checked against `CONTEXT.md`; hard-to-reverse tradeoffs must be checked against ADRs.\n+2. **Domain language is controlled.** Terms must be checked against `CONTEXT.md`; hard-to-reverse tradeoffs must be checked against Shiki SADRs and target ADRs.\n 3. **PRD precedes issue decomposition.** `to-prd` is used once the context is settled enough to become durable intent.\n 4. **Issues are vertical slices.** `to-issues` must produce independently grabbable tracer-bullet tasks, not horizontal layer tickets.\n 5. **Triage controls dispatch.** Only `ready-for-agent` / AFK tasks may be assigned to Codex without human input.\n@@ -95,19 +98,19 @@\n Process:\n \n - Challenge terms against `CONTEXT.md`.\n-- Check ADRs before reopening decisions.\n+- Check SADRs and target ADRs before reopening decisions.\n - Inspect code when code can answer a question.\n - Ask one question at a time when the operator is available.\n - Provide a recommended answer with each question.\n - Record resolved terms in `CONTEXT.md`.\n-- Create ADRs only for hard-to-reverse, surprising, tradeoff-based decisions.\n+- Create SADRs or target ADRs only for hard-to-reverse, surprising, tradeoff-based decisions.\n - Convert unresolved load-bearing questions into HITL blockers.\n \n Outputs:\n \n - Resolved design-tree decisions.\n - Updated glossary terms when needed.\n-- ADRs when justified.\n+- SADRs or target ADRs when justified.\n - HITL blockers when decisions remain unresolved.\n \n ## Phase 3 \u2014 Context & Impact\n@@ -117,7 +120,7 @@\n \n Outputs must include:\n \n-- Relevant documents and ADRs.\n+- Relevant documents, SADRs, and target ADRs.\n - Relevant modules, interfaces, seams, callers, and tests.\n - Candidate locks.\n - Dependencies.\n@@ -140,7 +143,7 @@\n - Testing decisions.\n - Out of scope.\n - Further notes.\n-- Links to relevant Goal, domain terms, and ADRs.\n+- Links to relevant Goal, domain terms, SADRs, and target ADRs.\n \n Phase 4 ends with Spec Freeze: the operator explicitly approves the PRD, the\n plan records a `spec_freeze` block (status, approved_by, source), and the SF\n@@ -240,7 +243,7 @@\n - Dependencies and locks.\n - Risk and Guardian requirements.\n - Ledger completeness.\n-- Documentation and ADR updates.\n+- Documentation and SADR/target-ADR updates.\n - Scope drift.\n \n CCA emits one of:\n@@ -301,7 +304,7 @@\n | Evidence failure | Behavior might be done, but proof is missing. | Add checks, PR evidence, ledger entry, or artifacts. |\n | Scope drift | PR changes unrelated behavior. | Revert unrelated changes or split into new Goal. |\n | Dependency block | Upstream task or lock not complete. | Block until dependency resolves. |\n-| Architecture block | Decision needs ADR or deep-module discussion. | `grill-with-docs` or `improve-codebase-architecture`. |\n+| Architecture block | Decision needs a SADR, target ADR, or deep-module discussion. | `grill-with-docs` or `improve-codebase-architecture`. |\n | Risk block | Security/production/policy change needs human approval. | Guardian decision. |\n \n ## Phase 5.5 \u2014 Execution Decision Control\ndiff --git a/docs/agents/runtime-auth-model.md b/docs/agents/runtime-auth-model.md\nindex 6a2e1a47e81ea89b5c68a210ec07772a9374babc..3c54748d24233dbd0ba8473950282547ef4ce935\n--- a/docs/agents/runtime-auth-model.md\n+++ b/docs/agents/runtime-auth-model.md\n@@ -148,7 +148,7 @@\n \n API-key based runners may be added by a target repository as an explicit extension. They are not the default Shiki template path.\n \n-If a repo opts into API-key mode, record the exception in an ADR and update the repo's `AGENTS.md`, `.shiki/config.yaml`, and workflow permissions.\n+If a repo opts into API-key mode, record the target-specific exception in a target ADR and update the repo's `AGENTS.md`, `.shiki/config.yaml`, and workflow permissions.\n \n ## GitHub Actions Runtime Compatibility\n \ndiff --git a/docs/agents/skill-gate.md b/docs/agents/skill-gate.md\nindex c50398b0d7c60a8598f2a5c88a10015730dced13..3acffe6444617bb1d54b77d4b4708158ec735bc6\n--- a/docs/agents/skill-gate.md\n+++ b/docs/agents/skill-gate.md\n@@ -9,7 +9,7 @@\n | Skill | Trigger | Expected output |\n | --- | --- | --- |\n | `setup-matt-pocock-skills` | First configuring a repo for these skills, or when issue tracker, triage labels, or domain docs are missing. | `docs/agents/` configuration and an `Agent skills` block in `AGENTS.md` or `CLAUDE.md`. |\n-| `grill-with-docs` | Ambiguous plan, unclear terminology, boundary decision, tradeoff, or ADR-worthy design choice. | Clarified decision tree, recommended answers, updates to `CONTEXT.md` or ADRs when decisions crystallize. |\n+| `grill-with-docs` | Ambiguous plan, unclear terminology, boundary decision, tradeoff, or decision-record-worthy design choice. | Clarified decision tree, recommended answers, updates to `CONTEXT.md`, SADRs, or target ADRs when decisions crystallize. |\n | `zoom-out` | Agent lacks a higher-level architectural map. | Map of relevant modules, callers, seams, and domain concepts. |\n | `to-prd` | Settled context should become a PRD. | PRD published to the issue tracker with problem, solution, user stories, decisions, testing, and out-of-scope notes. |\n | `to-issues` | Goal, PRD, or plan must become implementation tickets. | Vertical-slice issues with acceptance criteria, dependencies, and AFK/HITL classification. |\n@@ -17,7 +17,7 @@\n | `tdd` | Feature work or bug fix has observable behavior that can be specified with tests. | Red-green-refactor loop through public interfaces, one vertical slice at a time. |\n | `code-review` | The TDD loop is green and the implementer is about to create or update a PR. Default for every implementation task. | Pre-PR self-review of the task diff with fixes applied on the task branch; findings and resolutions recorded as a ledger entry naming the skill and a `## Pre-PR code review` PR body section. |\n | `diagnose` | Hard bug, failing check, regression, flaky behavior, or performance problem. | Reproduction loop, ranked hypotheses, instrumentation, fix, regression test, post-mortem. |\n-| `improve-codebase-architecture` | Structural friction, testability, AI-navigability, deep-module opportunity, or refactor request. | Architecture candidates and deepening recommendations informed by `CONTEXT.md` and ADRs. |\n+| `improve-codebase-architecture` | Structural friction, testability, AI-navigability, deep-module opportunity, or refactor request. | Architecture candidates and deepening recommendations informed by `CONTEXT.md`, SADRs, and target ADRs. |\n | `prototype` | A throwaway logic or UI prototype is needed to answer a design question. | Clearly marked disposable prototype and captured decision. |\n \n ## MergeGate Interaction\ndiff --git a/docs/operations.md b/docs/operations.md\nindex cf1f452d4c589e38ccd58553bda0ad8684b51eb7..ace48a90ffbec9e5c0a7113bef16a4858bc44b3f\n--- a/docs/operations.md\n+++ b/docs/operations.md\n@@ -89,6 +89,16 @@\n      target history, brought forward by `shiki migrate`, not a template.\n    - **Everything else** (CLI scripts, workflows, schemas, docs) is overwritten.\n \n+   A forced upgrade also migrates the former numeric Shiki decision paths to\n+   `SADR-NNNN-*`. Before the first write, the installer inventories the exact\n+   legacy Shiki paths and authorizes each deletion only when the existing\n+   `.shiki/install-stamp.json` names that path and its SHA-256 digest still\n+   matches the file. An absent, unreadable, malformed, incomplete, or\n+   mismatching stamp reports every blocked path and stops with no filesystem\n+   change. Target-owned `NNNN-*` ADRs are not cleanup candidates. A non-force\n+   install deletes nothing and retains any still-matching legacy stamp entries\n+   so a later forced upgrade can prove ownership.\n+\n    If the target has pending migrations, `--force` refuses and writes nothing \u2014\n    apply migrations first (step 3), then re-run. The run ends with a single\n    summary of every `.new` written, so a half-upgrade is visible.\ndiff --git a/docs/releasing.md b/docs/releasing.md\nindex 10d73828e7dbd64e9d9f6b83bc84d010537ce62a..aafeee8c4d441e1e427efee2ce4f81a647f020e1\n--- a/docs/releasing.md\n+++ b/docs/releasing.md\n@@ -67,7 +67,7 @@\n CLI in `scripts/`. There are no third-party Python runtime dependencies. The CLI\n is intentionally **not** exposed as a `[project.scripts]` console entry point\n because `scripts/shiki.py` is a dependency-free shim and `scripts/` is not\n-packaged as an importable distribution (see the ADR for the rationale).\n+packaged as an importable distribution (see SADR-0007 for the rationale).\n \n Supported install paths:\n \n@@ -155,6 +155,15 @@\n    [docs/operations.md](operations.md#the-new-convention). If the target has\n    pending migrations, `--force` refuses and writes nothing; apply migrations\n    (step 2) first, then re-run.\n+\n+   The SADR namespace upgrade has an additional pre-write gate: each former\n+   numeric Shiki decision path is removed only when the existing install stamp\n+   contains that exact path and a digest matching its current bytes. The\n+   installer aggregates absent/unreadable/malformed-stamp, missing-digest, and\n+   digest-mismatch blockers before raising, so refusal leaves the target tree\n+   byte-identical. Numeric target ADRs are preserved. A successful forced\n+   upgrade installs and stamps `SADR-*` paths and drops the removed legacy paths\n+   from the refreshed stamp.\n 2. Apply repository-local `.shiki/` state migrations with the migrate command:\n \n    ```bash\ndiff --git a/docs/usage.md b/docs/usage.md\nindex b83407ee3970dd395dd893010bc5dbe8bad26051..a130ef6e40dfd8169a5bc701339351097df1a09f\n--- a/docs/usage.md\n+++ b/docs/usage.md\n@@ -67,8 +67,10 @@\n shiki install-target /path/to/target-repo --local-only\n ```\n \n-Use `--force` only when you intentionally want to overwrite existing target\n-files.\n+Use `--force` only when you intentionally want to refresh the target template.\n+For the SADR namespace migration, `--force` deletes only exact legacy Shiki\n+decision paths whose existing install-stamp digest still matches; any ownership\n+blocker stops before the first write. Numeric target ADRs are never candidates.\n \n ## `shiki bootstrap-platform` \u2014 publish the Shiki platform repo\n \ndiff --git a/scripts/guardian_approval_signal.py b/scripts/guardian_approval_signal.py\nindex d059f92b2c82aa2b5d6ca421365537a87886c1cb..16924892714f4f5b9303c0ef0e325a7e70075630\n--- a/scripts/guardian_approval_signal.py\n+++ b/scripts/guardian_approval_signal.py\n@@ -199,8 +199,8 @@\n ) -> Any:\n     \"\"\"Evaluate SADR-0015 Contract Approval for the CCA Guardian signal.\n \n-    The SAME OR that MergeGate applies must reach this deterministic signal: ADR\n-    0015 is explicit that without it, a contract-approved PR still returns\n+    The SAME OR that MergeGate applies must reach this deterministic signal:\n+    SADR-0015 is explicit that without it, a contract-approved PR still returns\n     ``needs_guardian``, the CCA job fails, and the MergeGate policy check never\n     runs \u2014 the gate this change moves would never be reached. This resolves the\n     task the SAME way MergeGate does (the first Shiki task id in the PR body),\ndiff --git a/scripts/shiki_contract_approval.py b/scripts/shiki_contract_approval.py\nindex 941bcb96fdd8b9a85bc0c4f0f04d5dad9d04839c..4a4b418f6527413161db82670bec857a043360e2\n--- a/scripts/shiki_contract_approval.py\n+++ b/scripts/shiki_contract_approval.py\n@@ -142,7 +142,7 @@\n     changed_files_status: Any,\n     registration: Any,\n ) -> ContractApprovalResult:\n-    \"\"\"Whether this PR carries an SADR-0015 Contract Approval.\n+    \"\"\"Whether this PR carries a SADR-0015 Contract Approval.\n \n     The four conditions are evaluated IN ORDER, each failure recorded as a\n     distinct reason string:\ndiff --git a/scripts/shiki_guardian.py b/scripts/shiki_guardian.py\nindex a4ecb79bbda55fd5c8b06adfae2eb99760a03c45..4dd99bbe714e26f421fa19abac7a34ae1b195ecc\n--- a/scripts/shiki_guardian.py\n+++ b/scripts/shiki_guardian.py\n@@ -12,7 +12,7 @@\n # The carry re-derives its proof from git in-process on every judging invocation;\n # it is never read from an artifact, so there is nothing to go stale or be forged.\n # shiki_sync_proof is the ONLY guardian-family module that runs git, and every one\n-# of its invocations is read-only and local (ADR: prove-from-git base sync).\n+# of its invocations is read-only and local (SADR: prove-from-git base sync).\n from shiki_sync_proof import verify_pure_base_sync\n \n GUARDIAN_POLICY_PATH = \".shiki/guardian-policy.json\"\ndiff --git a/scripts/shiki_guardian_review.py b/scripts/shiki_guardian_review.py\nindex dd0f104d457899e0ba5ff1e162011c1e90b6f92b..e10ef58c046324e7ea3a3392feb3048ca77414ce\n--- a/scripts/shiki_guardian_review.py\n+++ b/scripts/shiki_guardian_review.py\n@@ -159,7 +159,7 @@\n     docs_only = _is_docs_only(changed_files)\n     if docs_only and matched == 0:\n         focus.append(\n-            \"Docs/ADR-only change: confirm the change is genuinely documentation/decision-only, introduces no behavior or contract drift, and stays consistent with the constitution and existing ADRs.\"\n+            \"Docs/decision-record-only change: confirm the change is genuinely documentation/decision-only, introduces no behavior or contract drift, and stays consistent with the constitution and existing SADRs and target ADRs.\"\n         )\n \n     # Conservative augmentation: unknown (no category and not docs-only), mixed\ndiff --git a/scripts/shiki_installer.py b/scripts/shiki_installer.py\nindex be064ab7a29ff6836f7f7a1d70500a43e0d11d7b..9fd8eefd137ce7003dad5c0dcb2e48e2cd47ab32\n--- a/scripts/shiki_installer.py\n+++ b/scripts/shiki_installer.py\n@@ -118,6 +118,7 @@\n # what lets a target carry it without a governance-schema change.\n INSTALL_STAMP_PATH = \".shiki/install-stamp.json\"\n INSTALL_STAMP_VERSION = 1\n+LEGACY_SHIKI_SADR_MAX = 18\n \n # Under --force the shipped surface splits three ways, so nothing is lost and\n # nothing is silently stale.\n@@ -237,9 +238,98 @@\n         )\n \n \n+def legacy_shiki_adr_paths() -> tuple[str, ...]:\n+    \"\"\"Former numeric paths for Shiki SADR-0001 through SADR-0018.\n+\n+    The authoritative slugs come from the current SADR filenames. SADR-0019 is\n+    the namespace decision itself and never shipped under a numeric legacy path.\n+    \"\"\"\n+    paths: list[str] = []\n+    for sadr in sorted((ROOT / \"docs\" / \"adr\").glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\")):\n+        number = sadr.name.split(\"-\", 2)[1]\n+        if int(number) <= LEGACY_SHIKI_SADR_MAX:\n+            paths.append(f\"docs/adr/{sadr.name.removeprefix('SADR-')}\")\n+    return tuple(paths)\n+\n+\n+def preflight_legacy_sadr_cleanup(target: Path, *, force: bool) -> tuple[str, ...]:\n+    \"\"\"Authorize exact legacy Shiki ADR removals before the first write.\n+\n+    A non-force install never removes existing files. A forced install needs no\n+    stamp when no legacy Shiki path is present. When legacy paths do exist, each\n+    must be named in a readable install stamp and its current bytes must match\n+    the recorded digest.\n+    \"\"\"\n+    if not force:\n+        return ()\n+    present = tuple(\n+        relative\n+        for relative in legacy_shiki_adr_paths()\n+        if (target / relative).exists() or (target / relative).is_symlink()\n+    )\n+    if not present:\n+        return ()\n+\n+    stamp_path = target / INSTALL_STAMP_PATH\n+    try:\n+        raw = stamp_path.read_text(encoding=\"utf-8\")\n+        stamp = json.loads(raw)\n+    except FileNotFoundError:\n+        detail = \"install stamp is absent\"\n+        blockers = [f\"{relative}: {detail}\" for relative in present]\n+    except (OSError, UnicodeDecodeError) as error:\n+        detail = f\"install stamp is unreadable ({error})\"\n+        blockers = [f\"{relative}: {detail}\" for relative in present]\n+    except json.JSONDecodeError as error:\n+        detail = f\"install stamp is malformed ({error})\"\n+        blockers = [f\"{relative}: {detail}\" for relative in present]\n+    else:\n+        if not isinstance(stamp, dict):\n+            blockers = [\n+                f\"{relative}: install stamp is malformed (expected an object)\"\n+                for relative in present\n+            ]\n+        else:\n+            digests = stamp.get(\"digests\")\n+            blockers = []\n+            if not isinstance(digests, dict):\n+                blockers.extend(\n+                    f\"{relative}: install stamp has no digest for this path\"\n+                    for relative in present\n+                )\n+            else:\n+                for relative in present:\n+                    expected = digests.get(relative)\n+                    if not isinstance(expected, str) or not expected:\n+                        blockers.append(\n+                            f\"{relative}: install stamp has no digest for this path\"\n+                        )\n+                        continue\n+                    try:\n+                        actual = _sha256_file(target / relative)\n+                    except OSError as error:\n+                        blockers.append(\n+                            f\"{relative}: legacy path is unreadable ({error})\"\n+                        )\n+                        continue\n+                    if actual != expected:\n+                        blockers.append(\n+                            f\"{relative}: digest mismatch (stamp {expected}, current {actual})\"\n+                        )\n+\n+    if blockers:\n+        joined = \"\\n\".join(f\"- {blocker}\" for blocker in blockers)\n+        raise ShikiError(\n+            \"legacy Shiki ADR cleanup is not authorized:\\n\"\n+            f\"{joined}\\nNo files were written.\"\n+        )\n+    return present\n+\n+\n def install_template(target: Path, *, force: bool, validate: bool) -> list[NewFileNote]:\n     # Preconditions first: never rewrite the tree of a target that cannot be\n     # upgraded cleanly (see refuse_pending_migrations).\n+    legacy_cleanup = preflight_legacy_sadr_cleanup(target, force=force)\n     refuse_pending_migrations(target)\n \n     # Resolve the target's required CODEOWNERS owner once from its\n@@ -250,6 +340,10 @@\n     # verbatim. See ``codeowners_required_owner``.\n     codeowners_owner = codeowners_required_owner(target)\n \n+    for relative in legacy_cleanup:\n+        (target / relative).unlink()\n+        info(f\"removed stamped legacy Shiki ADR: {target / relative}\")\n+\n     notes: list[NewFileNote] = []\n     for relative in TEMPLATE_PATHS:\n         source = ROOT / relative\n@@ -647,6 +741,22 @@\n     digests = target_stamp_digests(target)\n     existing = read_install_stamp(target)\n     if existing is not None:\n+        # A non-force install deliberately leaves legacy Shiki ADRs in place.\n+        # Preserve only still-matching ownership proof so a later forced\n+        # upgrade can authorize cleanup; never carry a missing or stale digest.\n+        existing_digests = existing.get(\"digests\")\n+        if isinstance(existing_digests, dict):\n+            for relative in legacy_shiki_adr_paths():\n+                expected = existing_digests.get(relative)\n+                path = target / relative\n+                if not isinstance(expected, str) or not path.is_file():\n+                    continue\n+                try:\n+                    actual = _sha256_file(path)\n+                except OSError:\n+                    continue\n+                if actual == expected:\n+                    digests[relative] = actual\n         if _incoming_is_older(commit, existing.get(\"platform_commit\")):\n             warn(\n                 \"kept existing install stamp: incoming install \"\ndiff --git a/scripts/shiki_loop.py b/scripts/shiki_loop.py\nindex 3611641da8aeb5038b1da90e3114b09a473ec6d8..84caa491b9cad0edc4176f209c13e9221e975681\n--- a/scripts/shiki_loop.py\n+++ b/scripts/shiki_loop.py\n@@ -424,7 +424,7 @@\n         # unresolvable CCA verdict has already produced its terminal stop and is never\n         # laundered into a head-moving sync (SADR-0010). It also never fires while the\n         # policy (Guardian) gate is red \u2014 for ANY reason, not only when it is the sole\n-        # red check \u2014 so a head-bound Guardian approval, or an SADR-0015\n+        # red check \u2014 so a head-bound Guardian approval, or a SADR-0015\n         # contract-approval carry that base movement defeated (the policy gate compares\n         # base and head task governance directly), is never silently restored by\n         # adopting base wholesale. It is bounded by a durable per-task counter so the\ndiff --git a/scripts/shiki_tasks.py b/scripts/shiki_tasks.py\nindex aec3809f24b4a0c8c96fc50971cff9ae9ad30381..d531052e9d9717058d4631483cefab44c26a65c1\n--- a/scripts/shiki_tasks.py\n+++ b/scripts/shiki_tasks.py\n@@ -821,7 +821,7 @@\n         \"prompt\": prompt,\n         \"entry_skill\": \"grill-with-docs\",\n         \"required_next_steps\": [\n-            \"Run grill-with-docs until terminology, boundaries, risks, and ADR-worthy decisions are settled.\",\n+            \"Run grill-with-docs until terminology, boundaries, risks, and decision-record-worthy choices are settled.\",\n             \"Obtain the operator's explicit approval of the PRD/requirements (Spec Freeze).\",\n             \"Write a machine-readable plan JSON with grill_with_docs.status=complete and spec_freeze.status=frozen.\",\n             \"Run shiki plan ingest --plan-file PLAN.json.\",\ndiff --git a/scripts/test_shiki_goal_loop.sh b/scripts/test_shiki_goal_loop.sh\nindex 753c7b516e10f0be3bc92a4bd0eb1fa699776ae6..8f3820a0a079b3044424cfe1a6c6b7fe68896e67\n--- a/scripts/test_shiki_goal_loop.sh\n+++ b/scripts/test_shiki_goal_loop.sh\n@@ -231,8 +231,8 @@\n \n # High/critical risk merges autonomously when all required checks are green:\n # the \"MergeGate policy check\" required check IS the Guardian gate (it enforces\n-# guardian-policy.json \u2014 human approval OR external AI guardian review, ADR\n-# 0010), so green checks mean a recorded authority approved. The fake gh returns\n+# guardian-policy.json \u2014 human approval OR external AI guardian review,\n+# SADR-0010), so green checks mean a recorded authority approved. The fake gh returns\n # green checks, simulating that the Guardian gate passed.\n python3 \"$ROOT/scripts/shiki.py\" goal create --target \"$TARGET\" --title \"Guardian gate\" --outcome \"High risk merges via policy gate\" >/tmp/shiki-goal-loop-guardian-goal.json\n GGOAL=\"$(json_get /tmp/shiki-goal-loop-guardian-goal.json goal_id)\"\ndiff --git a/tests/test_adr_references.py b/tests/test_adr_references.py\nindex 51706329c797d5dd9b47e082a333fa83b261d482..db6e693e35f67103935be34f1d9bb4ff7ee60395\n--- a/tests/test_adr_references.py\n+++ b/tests/test_adr_references.py\n@@ -1,7 +1,7 @@\n-\"\"\"Every ``ADR ####`` citation must resolve to exactly one decision record.\n+\"\"\"Every target ADR and Shiki SADR citation resolves in its own namespace.\n \n Background. ``scripts/mergegate_check.py`` justifies ``is_bookkeeping_closeout``\n-with \"ADR 0017\" nine times and ``scripts/guardian_approval_signal.py`` cites\n+with \"SADR-0017\" nine times and ``scripts/guardian_approval_signal.py`` cites\n 0016/0017 seven times, yet for a stretch the numbering in ``docs/adr/`` stopped\n at 0015: the authoring commits lived on a coordinator branch and no task ever\n declared ``docs/adr/...`` in its locks, so nothing carried the records to the\n@@ -11,11 +11,12 @@\n and the directory drifted with no signal.\n \n This is the same unbound-pair shape behind other silent failures: a reference\n-and its target with nothing asserting they agree. This test binds them. It scans\n-every ``scripts/*.py`` (plus ``docs/**/*.md`` and the root ``*.md`` governance\n-files, which are cheap) for the ``ADR ####`` pattern and asserts each cited\n-number resolves to exactly one ``docs/adr/####-*.md``. The converse is\n-deliberately NOT required: an ADR may exist without being cited.\n+and its target with nothing asserting they agree. This test binds them. Target\n+``ADR NNNN`` citations resolve only to ``docs/adr/NNNN-*.md``; Shiki\n+``SADR-NNNN`` citations resolve only to ``docs/adr/SADR-NNNN-*.md``. The scan\n+covers current governance docs, scripts, workflows, prompts, packaging metadata,\n+and repo-local Codex guidance while deliberately excluding historical\n+``.shiki`` evidence. The converse is not required: a record may be uncited.\n \n The suite is dependency free: standard-library ``unittest`` only.\n \"\"\"\n@@ -31,17 +32,18 @@\n \n REPO_ROOT = Path(__file__).resolve().parents[1]\n \n-# \"ADR 0016\" -- a four-digit citation. The trailing \\b keeps a five-digit token\n-# from being read as a truncated four-digit number.\n-_CITATION = re.compile(r\"\\bADR (\\d{4})\\b\")\n-# \"0016-locks-bound-product-paths.md\" -- the leading four digits name the record.\n-_ADR_FILENAME = re.compile(r\"^(\\d{4})-.*\\.md$\")\n+# ``ADR 0016`` / ``SADR-0016`` -- a namespace plus four-digit citation. The\n+# trailing ``\\b`` keeps a five-digit token from being read as a truncated one.\n+_CITATION = re.compile(r\"\\b(S?ADR)[ -](\\d{4})\\b\")\n+# Target ADRs retain the numeric filename; Shiki platform decisions use SADR.\n+_DECISION_FILENAME = re.compile(r\"^(?:(SADR)-)?(\\d{4})-.*\\.md$\")\n \n \n class Violation(NamedTuple):\n     \"\"\"A citation that does not resolve to exactly one decision record.\"\"\"\n \n     citing_file: str  # repo-relative path of the file that cites the ADR\n+    namespace: str  # \"ADR\" for target decisions; \"SADR\" for Shiki decisions\n     number: str  # the cited four-digit number, e.g. \"0016\"\n     line: int  # 1-based line of the citation\n     reason: str  # \"missing\" (zero matches) or \"ambiguous\" (two or more)\n@@ -49,44 +51,59 @@\n \n     @property\n     def expected_path(self) -> str:\n-        return f\"docs/adr/{self.number}-*.md\"\n+        prefix = \"SADR-\" if self.namespace == \"SADR\" else \"\"\n+        return f\"docs/adr/{prefix}{self.number}-*.md\"\n \n     def describe(self) -> str:\n         \"\"\"Name the citing file, the cited number, and the missing path.\"\"\"\n         if self.reason == \"missing\":\n             return (\n-                f\"{self.citing_file}:{self.line} cites ADR {self.number} \"\n+                f\"{self.citing_file}:{self.line} cites {self.namespace} {self.number} \"\n                 f\"but no document matches {self.expected_path}\"\n             )\n         joined = \", \".join(self.matches)\n         return (\n-            f\"{self.citing_file}:{self.line} cites ADR {self.number} which \"\n+            f\"{self.citing_file}:{self.line} cites {self.namespace} {self.number} which \"\n             f\"resolves to {len(self.matches)} documents (expected exactly one \"\n             f\"at {self.expected_path}): {joined}\"\n         )\n \n \n-def _adr_index(root: Path) -> dict[str, list[Path]]:\n-    \"\"\"Map every four-digit ADR number to the record files that claim it.\"\"\"\n-    index: dict[str, list[Path]] = {}\n+def _adr_index(root: Path) -> dict[tuple[str, str], list[Path]]:\n+    \"\"\"Map each decision namespace and number to the files that claim it.\"\"\"\n+    index: dict[tuple[str, str], list[Path]] = {}\n     adr_dir = root / \"docs\" / \"adr\"\n     if adr_dir.is_dir():\n         for path in sorted(adr_dir.glob(\"*.md\")):\n-            match = _ADR_FILENAME.match(path.name)\n+            match = _DECISION_FILENAME.match(path.name)\n             if match:\n-                index.setdefault(match.group(1), []).append(path)\n+                namespace = \"SADR\" if match.group(1) else \"ADR\"\n+                index.setdefault((namespace, match.group(2)), []).append(path)\n     return index\n \n \n def _citing_files(root: Path, include_docs: bool = True) -> list[Path]:\n-    \"\"\"Files scanned for citations: scripts/*.py, and cheaply the markdown.\"\"\"\n+    \"\"\"Current, shipped text surfaces scanned for decision citations.\"\"\"\n     files = sorted((root / \"scripts\").glob(\"*.py\"))\n+    files += sorted((root / \"scripts\").glob(\"*.sh\"))\n     if include_docs:\n         docs = root / \"docs\"\n         if docs.is_dir():\n             files += sorted(docs.rglob(\"*.md\"))\n         files += sorted(root.glob(\"*.md\"))\n-    return files\n+        pyproject = root / \"pyproject.toml\"\n+        if pyproject.is_file():\n+            files.append(pyproject)\n+        for relative in (\".github\", \".codex\"):\n+            surface = root / relative\n+            if surface.is_dir():\n+                files += sorted(\n+                    path\n+                    for path in surface.rglob(\"*\")\n+                    if path.is_file()\n+                    and path.suffix in {\".md\", \".py\", \".sh\", \".toml\", \".yaml\", \".yml\"}\n+                )\n+    return sorted(set(files))\n \n \n def find_adr_reference_violations(\n@@ -94,10 +111,9 @@\n ) -> list[Violation]:\n     \"\"\"Return every ``ADR ####`` citation under ``root`` that fails to resolve.\n \n-    A citation resolves when exactly one ``docs/adr/####-*.md`` matches its\n-    number. Zero matches is a \"missing\" violation; two or more is \"ambiguous\".\n-    Records that are never cited produce nothing -- the converse is not\n-    required.\n+    A citation resolves when exactly one record in the same namespace matches\n+    its number. Zero matches is a \"missing\" violation; two or more is\n+    \"ambiguous\". Records that are never cited produce nothing.\n     \"\"\"\n     root = Path(root)\n     index = _adr_index(root)\n@@ -110,13 +126,15 @@\n         rel = path.relative_to(root).as_posix()\n         for lineno, line in enumerate(text.splitlines(), start=1):\n             for match in _CITATION.finditer(line):\n-                number = match.group(1)\n-                found = index.get(number, [])\n+                namespace, number = match.groups()\n+                found = index.get((namespace, number), [])\n                 if len(found) == 1:\n                     continue\n                 reason = \"missing\" if not found else \"ambiguous\"\n                 rels = [p.relative_to(root).as_posix() for p in found]\n-                violations.append(Violation(rel, number, lineno, reason, rels))\n+                violations.append(\n+                    Violation(rel, namespace, number, lineno, reason, rels)\n+                )\n     return violations\n \n \n@@ -129,8 +147,8 @@\n         self.assertEqual(\n             violations,\n             [],\n-            \"Every 'ADR ####' citation must resolve to exactly one \"\n-            f\"docs/adr/####-*.md:\\n{detail}\",\n+            \"Every ADR/SADR citation must resolve exactly once in its own \"\n+            f\"docs/adr namespace:\\n{detail}\",\n         )\n \n     def test_scan_actually_reads_scripts(self) -> None:\n@@ -146,8 +164,21 @@\n         # The two records this Goal carried onto the default branch, and which\n         # the scripts cite most, must be present.\n         index = _adr_index(REPO_ROOT)\n-        self.assertIn(\"0016\", index)\n-        self.assertIn(\"0017\", index)\n+        self.assertIn((\"SADR\", \"0016\"), index)\n+        self.assertIn((\"SADR\", \"0017\"), index)\n+\n+    def test_platform_tree_has_only_sadr_0001_through_0019(self) -> None:\n+        adr_dir = REPO_ROOT / \"docs\" / \"adr\"\n+        sadrs = sorted(adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\"))\n+        self.assertEqual(\n+            {path.name.split(\"-\", 2)[1] for path in sadrs},\n+            {f\"{number:04d}\" for number in range(1, 20)},\n+        )\n+        self.assertEqual(\n+            list(adr_dir.glob(\"[0-9][0-9][0-9][0-9]-*.md\")),\n+            [],\n+            \"the Shiki platform tree must not retain legacy numeric SADR paths\",\n+        )\n \n \n class FixtureAdrReferences(unittest.TestCase):\n@@ -180,6 +211,26 @@\n         self.assertIn(\"9991\", message)  # cited number\n         self.assertIn(\"docs/adr/9991-*.md\", message)  # missing path\n \n+    def test_target_adr_does_not_satisfy_missing_platform_sadr(self) -> None:\n+        root = self._make_repo(\n+            adr_names=[\"0042-target-decision.md\"],\n+            script_body='\"\"\"Target ADR 0042 exists, but platform SADR-0042 does not.\"\"\"\\n',\n+        )\n+        violations = find_adr_reference_violations(root, include_docs=False)\n+        self.assertEqual(len(violations), 1)\n+        self.assertEqual(violations[0].namespace, \"SADR\")\n+        self.assertEqual(violations[0].number, \"0042\")\n+        self.assertEqual(violations[0].expected_path, \"docs/adr/SADR-0042-*.md\")\n+\n+    def test_target_adr_and_platform_sadr_may_reuse_a_number(self) -> None:\n+        root = self._make_repo(\n+            adr_names=[\"0042-target-decision.md\", \"SADR-0042-platform-decision.md\"],\n+            script_body='\"\"\"Target ADR 0042 and platform SADR-0042 are distinct.\"\"\"\\n',\n+        )\n+        self.assertEqual(\n+            find_adr_reference_violations(root, include_docs=False), []\n+        )\n+\n     def test_uncited_adr_is_allowed(self) -> None:\n         # An ADR exists but nothing cites it -> not a violation. The converse\n         # (every ADR must be cited) is explicitly NOT required.\ndiff --git a/tests/test_guardian_review.py b/tests/test_guardian_review.py\nindex 88829e7810212635e2fc98aa5918d6f26294677d..00a85a237acd5bf2d6cb5b294128198c422402ca\n--- a/tests/test_guardian_review.py\n+++ b/tests/test_guardian_review.py\n@@ -162,7 +162,7 @@\n \n     def test_docs_only_is_light_and_not_conservative(self):\n         focus = gr.classify_review_focus([\"docs/adr/0099-x.md\", \"README.md\"], \"\", \"low\")\n-        self.assertTrue(any(\"Docs/ADR-only\" in f for f in focus))\n+        self.assertTrue(any(\"Docs/decision-record-only\" in f for f in focus))\n         self.assertFalse(any(\"conservative default\" in f.lower() for f in focus))\n \n     def test_unknown_category_is_conservative(self):\ndiff --git a/tests/test_policy_job_merged_prs.py b/tests/test_policy_job_merged_prs.py\nindex d45512e74681e5e649279236c7d4e79d5c3da471..b97989f8bd74d091de1a6b5275a20d8dc68c4512\n--- a/tests/test_policy_job_merged_prs.py\n+++ b/tests/test_policy_job_merged_prs.py\n@@ -3,8 +3,8 @@\n \n ``.github/workflows/shiki-cca-completion.yml`` has two consumers of the merged-PR\n proof: the CCA *signal* (``guardian_approval_signal.py`` in \"Prepare CCA context\")\n-and the authoritative gate (``mergegate_check.py`` in \"Enforce MergeGate\"). ADR\n-0017's bookkeeping-closeout exemption (condition 6) needs the merged implementation\n+and the authoritative gate (``mergegate_check.py`` in \"Enforce MergeGate\").\n+SADR-0017's bookkeeping-closeout exemption (condition 6) needs the merged implementation\n PR proven, and a closeout carries NO ``post_merge_reconcile`` marker. The signal\n already resolves ``MERGED_PRS`` unconditionally, but \"Enforce MergeGate\" used to\n resolve it only inside ``grep -q '<!-- shiki:post_merge_reconcile -->'``. So a\ndiff --git a/tests/test_sadr_install_migration.py b/tests/test_sadr_install_migration.py\nnew file mode 100644\nindex 0000000000000000000000000000000000000000..a6f4c57e4dcaef73ef600b0d4804744138ab9071\n--- /dev/null\n+++ b/tests/test_sadr_install_migration.py\n@@ -0,0 +1,229 @@\n+\"\"\"SADR installation and collision-safe legacy upgrade behavior.\"\"\"\n+\n+from __future__ import annotations\n+\n+import contextlib\n+import hashlib\n+import io\n+import json\n+import shutil\n+import tempfile\n+import unittest\n+from pathlib import Path\n+\n+import shiki_test_support  # noqa: F401  (path bootstrap)\n+\n+import shiki_installer\n+from tests.test_adr_references import find_adr_reference_violations\n+\n+\n+PRODUCT_NUMBERS = tuple(range(13, 19))\n+\n+\n+def _quiet_install(target: Path, *, force: bool, validate: bool) -> None:\n+    with contextlib.redirect_stdout(io.StringIO()):\n+        shiki_installer.install_template(target, force=force, validate=validate)\n+\n+\n+def _sha256(path: Path) -> str:\n+    return hashlib.sha256(path.read_bytes()).hexdigest()\n+\n+\n+def _legacy_sadr_paths() -> tuple[str, ...]:\n+    return tuple(\n+        f\"docs/adr/{path.name.removeprefix('SADR-')}\"\n+        for path in sorted((shiki_installer.ROOT / \"docs\" / \"adr\").glob(\"SADR-*.md\"))\n+        if path.name != \"SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\"\n+    )\n+\n+\n+def _create_legacy_upgrade_fixture(\n+    target: Path,\n+) -> tuple[dict[str, bytes], tuple[str, ...]]:\n+    adr_dir = target / \"docs\" / \"adr\"\n+    adr_dir.mkdir(parents=True)\n+    product_bytes = {\n+        f\"{number:04d}-cubeshare-product-decision.md\": (\n+            f\"# CubeShare ADR {number:04d}\\n\\nDo not rewrite {number}.\\n\"\n+        ).encode()\n+        for number in PRODUCT_NUMBERS\n+    }\n+    for name, content in product_bytes.items():\n+        (adr_dir / name).write_bytes(content)\n+\n+    legacy_paths = _legacy_sadr_paths()\n+    for relative in legacy_paths:\n+        (target / relative).write_bytes(f\"legacy Shiki bytes for {relative}\\n\".encode())\n+\n+    stamp_path = target / shiki_installer.INSTALL_STAMP_PATH\n+    stamp_path.parent.mkdir(parents=True, exist_ok=True)\n+    stamp = {\n+        \"version\": shiki_installer.INSTALL_STAMP_VERSION,\n+        \"platform_commit\": None,\n+        \"installed_at\": \"2000-01-01T00:00:00+00:00\",\n+        \"digests\": {\n+            relative: _sha256(target / relative) for relative in legacy_paths\n+        },\n+    }\n+    stamp_path.write_text(\n+        json.dumps(stamp, indent=2, sort_keys=True) + \"\\n\", encoding=\"utf-8\"\n+    )\n+    return product_bytes, legacy_paths\n+\n+\n+def _tree_snapshot(root: Path) -> dict[str, tuple[str, bytes | str]]:\n+    \"\"\"Recursive byte/shape snapshot used to prove preflight is write-free.\"\"\"\n+    snapshot: dict[str, tuple[str, bytes | str]] = {}\n+    for path in sorted(root.rglob(\"*\")):\n+        relative = path.relative_to(root).as_posix()\n+        if path.is_symlink():\n+            snapshot[relative] = (\"symlink\", path.readlink().as_posix())\n+        elif path.is_dir():\n+            snapshot[relative] = (\"directory\", \"\")\n+        else:\n+            snapshot[relative] = (\"file\", path.read_bytes())\n+    return snapshot\n+\n+\n+class FreshSadrInstallTests(unittest.TestCase):\n+    def setUp(self) -> None:\n+        self.tmp = Path(tempfile.mkdtemp(prefix=\"shiki-sadr-fresh-\"))\n+        self.addCleanup(lambda: shutil.rmtree(self.tmp, ignore_errors=True))\n+        self.target = self.tmp / \"target\"\n+        self.adr_dir = self.target / \"docs\" / \"adr\"\n+        self.adr_dir.mkdir(parents=True)\n+        self.product_bytes = {\n+            f\"{number:04d}-product-decision.md\": (\n+                f\"# Product ADR {number:04d}\\n\\nTarget-owned bytes {number}.\\n\"\n+            ).encode()\n+            for number in PRODUCT_NUMBERS\n+        }\n+        for name, content in self.product_bytes.items():\n+            (self.adr_dir / name).write_bytes(content)\n+\n+    def test_fresh_install_preserves_product_adrs_and_installs_all_sadrs(self) -> None:\n+        _quiet_install(self.target, force=False, validate=True)\n+\n+        for name, expected in self.product_bytes.items():\n+            self.assertEqual((self.adr_dir / name).read_bytes(), expected)\n+\n+        sadr_numbers = {\n+            path.name.split(\"-\", 2)[1]\n+            for path in self.adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\")\n+        }\n+        self.assertEqual(sadr_numbers, {f\"{number:04d}\" for number in range(1, 20)})\n+\n+        numeric_records = {\n+            path.name\n+            for path in self.adr_dir.glob(\"[0-9][0-9][0-9][0-9]-*.md\")\n+        }\n+        self.assertEqual(numeric_records, set(self.product_bytes))\n+        self.assertEqual(find_adr_reference_violations(self.target), [])\n+\n+\n+class LegacySadrUpgradeTests(unittest.TestCase):\n+    def setUp(self) -> None:\n+        self.tmp = Path(tempfile.mkdtemp(prefix=\"shiki-sadr-upgrade-\"))\n+        self.addCleanup(lambda: shutil.rmtree(self.tmp, ignore_errors=True))\n+        self.target = self.tmp / \"target\"\n+        self.adr_dir = self.target / \"docs\" / \"adr\"\n+        self.product_bytes, self.legacy_paths = _create_legacy_upgrade_fixture(\n+            self.target\n+        )\n+\n+    def test_force_upgrade_deletes_only_stamped_legacy_sadrs(self) -> None:\n+        _quiet_install(self.target, force=True, validate=True)\n+\n+        for relative in self.legacy_paths:\n+            self.assertFalse((self.target / relative).exists(), relative)\n+        for name, expected in self.product_bytes.items():\n+            self.assertEqual((self.adr_dir / name).read_bytes(), expected)\n+\n+        sadrs = list(self.adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\"))\n+        self.assertEqual(len(sadrs), 19)\n+        refreshed = json.loads(\n+            (self.target / shiki_installer.INSTALL_STAMP_PATH).read_text(encoding=\"utf-8\")\n+        )\n+        for relative in self.legacy_paths:\n+            self.assertNotIn(relative, refreshed[\"digests\"])\n+        for path in sadrs:\n+            relative = path.relative_to(self.target).as_posix()\n+            self.assertEqual(refreshed[\"digests\"][relative], _sha256(path))\n+\n+    def test_non_force_install_never_deletes_legacy_files(self) -> None:\n+        before = {\n+            relative: (self.target / relative).read_bytes()\n+            for relative in self.legacy_paths\n+        }\n+        _quiet_install(self.target, force=False, validate=False)\n+        for relative, expected in before.items():\n+            self.assertEqual((self.target / relative).read_bytes(), expected)\n+        refreshed = json.loads(\n+            (self.target / shiki_installer.INSTALL_STAMP_PATH).read_text(encoding=\"utf-8\")\n+        )\n+        for relative in self.legacy_paths:\n+            self.assertEqual(refreshed[\"digests\"][relative], _sha256(self.target / relative))\n+\n+\n+class LegacyCleanupPreflightTests(unittest.TestCase):\n+    def setUp(self) -> None:\n+        self.tmp = Path(tempfile.mkdtemp(prefix=\"shiki-sadr-preflight-\"))\n+        self.addCleanup(lambda: shutil.rmtree(self.tmp, ignore_errors=True))\n+\n+    def test_all_ownership_blockers_abort_before_any_write(self) -> None:\n+        cases = (\n+            (\"absent\", \"install stamp is absent\"),\n+            (\"unreadable\", \"install stamp is unreadable\"),\n+            (\"malformed\", \"install stamp is malformed\"),\n+            (\"missing-digest\", \"install stamp has no digest\"),\n+            (\"digest-mismatch\", \"digest mismatch\"),\n+        )\n+        for name, expected_reason in cases:\n+            with self.subTest(name=name):\n+                target = self.tmp / name\n+                _, legacy_paths = _create_legacy_upgrade_fixture(target)\n+                stamp_path = target / shiki_installer.INSTALL_STAMP_PATH\n+\n+                if name == \"absent\":\n+                    stamp_path.unlink()\n+                elif name == \"unreadable\":\n+                    stamp_path.unlink()\n+                    stamp_path.mkdir()\n+                elif name == \"malformed\":\n+                    stamp_path.write_text(\"{not-json\\n\", encoding=\"utf-8\")\n+                else:\n+                    stamp = json.loads(stamp_path.read_text(encoding=\"utf-8\"))\n+                    if name == \"missing-digest\":\n+                        stamp[\"digests\"] = {}\n+                    else:\n+                        stamp[\"digests\"] = {\n+                            relative: \"deadbeef\" for relative in legacy_paths\n+                        }\n+                    stamp_path.write_text(\n+                        json.dumps(stamp, indent=2, sort_keys=True) + \"\\n\",\n+                        encoding=\"utf-8\",\n+                    )\n+\n+                before = _tree_snapshot(target)\n+                with self.assertRaises(shiki_installer.ShikiError) as raised:\n+                    _quiet_install(target, force=True, validate=False)\n+                after = _tree_snapshot(target)\n+\n+                self.assertEqual(before, after, f\"{name} preflight changed the tree\")\n+                message = str(raised.exception)\n+                self.assertIn(expected_reason, message)\n+                self.assertIn(\"No files were written\", message)\n+                for relative in legacy_paths:\n+                    self.assertIn(relative, message)\n+\n+    def test_absent_stamp_is_allowed_when_no_legacy_path_exists(self) -> None:\n+        target = self.tmp / \"no-legacy\"\n+        target.mkdir()\n+        _quiet_install(target, force=True, validate=False)\n+        self.assertTrue(\n+            (target / \"docs\" / \"adr\" / \"SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\").is_file()\n+        )\n+\n+\n+if __name__ == \"__main__\":\n+    unittest.main()\n\n2026-08-07T08:19:36.478078Z ERROR codex_models_manager::manager: failed to renew cache TTL: missing field `base_instructions` at line 94 column 5\nexec\n/bin/zsh -lc \"sed -n '1,120p' scripts/shiki_process.py; sed -n '1,120p' tests/shiki_test_support.py\" in /private/tmp/shiki-adr-collision.pa0KwY/.worktrees/shiki-t-20260807t073307221254z-2d22d654-introduc\n succeeded in 0ms:\n#!/usr/bin/env python3\n\"\"\"Shared process, path, JSON, and console helpers for Shiki.\n\nThis module is intentionally standard-library only and has no import-time side\neffects beyond constant definition.\n\"\"\"\n\nfrom __future__ import annotations\n\nfrom dataclasses import dataclass\nfrom datetime import datetime, timezone\nimport json\nimport os\nfrom pathlib import Path\nimport re\nimport shutil\nimport subprocess\nimport sys\nfrom typing import Any\n\nfrom shiki_manifest import load_manifest, manifest_create_directories, manifest_directories\nfrom shiki_state import atomic_replace_json\n\nROOT = Path(__file__).resolve().parents[1]\nLOCAL_CONFIG = Path.home() / \".shiki\" / \"config.json\"\nDEFAULT_ENGINEERING_SKILLS_DIRS = [\n    \"~/Documents/lead-os/skills/engineering\",\n    \"~/skills/skills/engineering\",\n]\n\n@dataclass\nclass CommandResult:\n    args: list[str]\n    returncode: int\n    stdout: str\n    stderr: str\n\n\nclass ShikiError(Exception):\n    pass\n\n\ndef run(\n    args: list[str],\n    *,\n    cwd: Path = ROOT,\n    input_text: str | None = None,\n    env: dict[str, str] | None = None,\n    check: bool = True,\n) -> CommandResult:\n    process_env = os.environ.copy()\n    if env:\n        process_env.update(env)\n    process = subprocess.run(\n        args,\n        cwd=str(cwd),\n        input=input_text,\n        text=True,\n        capture_output=True,\n        env=process_env,\n        check=False,\n    )\n    result = CommandResult(args, process.returncode, process.stdout, process.stderr)\n    if check and process.returncode != 0:\n        command = \" \".join(args)\n        raise ShikiError(f\"{command} failed\\n{process.stderr.strip()}\")\n    return result\n\n\ndef info(message: str) -> None:\n    print(f\"[shiki] {message}\")\n\n\ndef warn(message: str) -> None:\n    print(f\"[shiki] warning: {message}\", file=sys.stderr)\n\n\ndef require_tool(name: str) -> None:\n    if shutil.which(name) is None:\n        raise ShikiError(f\"required tool not found: {name}\")\n\n\ndef validate_local_shiki() -> None:\n    run([\"python3\", \"scripts/validate_shiki.py\"], cwd=ROOT)\n    info(\"local Shiki validation passed\")\n\n\ndef validate_target_shiki(target: Path) -> None:\n    run([\"python3\", \"scripts/validate_shiki.py\"], cwd=target)\n    info(\"target Shiki validation passed\")\n\n\ndef save_default_config(repo: str, branch: str) -> None:\n    LOCAL_CONFIG.parent.mkdir(parents=True, exist_ok=True)\n    data = {\n        \"repo\": repo,\n        \"default_branch\": branch,\n        \"shiki_root\": str(ROOT),\n    }\n    LOCAL_CONFIG.write_text(json.dumps(data, indent=2, sort_keys=True) + \"\\n\", encoding=\"utf-8\")\n    info(f\"saved defaults to {LOCAL_CONFIG}\")\n\n\ndef load_default_config() -> dict[str, str]:\n    if not LOCAL_CONFIG.exists():\n        return {}\n    return json.loads(LOCAL_CONFIG.read_text(encoding=\"utf-8\"))\n\n\ndef utc_now() -> str:\n    return datetime.now(timezone.utc).replace(microsecond=0).isoformat()\n\n\ndef target_path(value: str) -> Path:\n    return Path(value).expanduser().resolve()\n\n\ndef shiki_path(target: Path, *parts: str) -> Path:\n    return target / \".shiki\" / Path(*parts)\n\n\"\"\"Shared helpers for the Shiki Python regression test suite.\n\nThese tests import the real ``scripts/`` modules and assert their observed\nbehavior. They never modify the modules under test. The suite is dependency\nfree: standard-library ``unittest`` only.\n\"\"\"\n\nfrom __future__ import annotations\n\nimport sys\nfrom pathlib import Path\n\nREPO_ROOT = Path(__file__).resolve().parents[1]\nSCRIPTS_DIR = REPO_ROOT / \"scripts\"\n\n\ndef ensure_scripts_on_path() -> None:\n    \"\"\"Put ``scripts/`` on ``sys.path`` so the real modules import cleanly.\"\"\"\n    scripts = str(SCRIPTS_DIR)\n    if scripts not in sys.path:\n        sys.path.insert(0, scripts)\n\n\nensure_scripts_on_path()\n\n\ndef synthetic_secrets() -> dict[str, str]:\n    \"\"\"Secret-SHAPED strings assembled at runtime from fragments.\n\n    The memory auto-capture tests must exercise redaction against realistic\n    token shapes, but a contiguous token literal in source would trip GitHub\n    secret scanning / push protection. Joining fragments keeps the literal out\n    of the committed file while the runtime value still matches the redaction\n    patterns under test. None of these are real credentials.\n    \"\"\"\n    j = \"\".join\n    return {\n        \"github_pat\": j([\"ghp\", \"_\", \"0123456789abcdefghij\", \"ABCDEFGHIJ0123\"]),\n        \"github_fine\": j([\"github\", \"_pat_\", \"11ABCDEF0123456789_\", \"abcdefghijABCDEFGHIJ\"]),\n        \"aws_key_id\": j([\"AK\", \"IA\", \"IOSFODNN7\", \"EXAMPLE\"]),\n        \"slack\": j([\"xox\", \"b\", \"-1234567890-\", \"abcdefghijklmnop\"]),\n        \"openai\": j([\"sk\", \"-\", \"abcdefghijklmnopqrstuvwxyz0123456789\"]),\n        \"jwt\": j([\"eyJ\", \"hbGciOiJIUzI1\", \".eyJ\", \"zdWIiOiIxMjM0NTY\", \".SflKxwRJSMeKKF2QT\"]),\n        \"aws_kv\": j([\"AWS_SECRET_ACCESS_KEY\", \"=\", \"wJalrXUtnFEMI/K7MDENG/bPxRfiCYEXAMPLEKEY\"]),\n    }\n\ndiff --git a/.codex/skills/shiki/SKILL.md b/.codex/skills/shiki/SKILL.md\nindex c8e2e85d9d75d49cc4f2d6286ee22659c4e53e75..93cded623b2c15d9f65c76491a1c2d813ac9e6c0\n--- a/.codex/skills/shiki/SKILL.md\n+++ b/.codex/skills/shiki/SKILL.md\n@@ -18,6 +18,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n Codex CLI does not register this skill as a custom `/shiki` slash command. When\n the user types `/shiki` in Codex CLI and gets \"Unrecognized command\", explain\n that this is expected: invoke Shiki in Codex with natural language such as\n@@ -69,7 +72,7 @@\n \n ## Responsibilities\n \n-- Claude Code implements and repairs by default (`shiki runner claude`, ADR 0008); Codex implements and repairs tasks explicitly assigned to `codex`.\n+- Claude Code implements and repairs by default (`shiki runner claude`, SADR-0008); Codex implements and repairs tasks explicitly assigned to `codex`.\n - Claude Code Action can act as GitHub-side CCA or reviewer.\n - CCA judges completion.\n - MergeGate authorizes state transitions and merge readiness.\ndiff --git a/.github/prompts/cca-completion-check.md b/.github/prompts/cca-completion-check.md\nindex ed203ad5f302ecc81372d8ab27d2b1232ca97497..fd89950528333ef6242e6d5443904f5392ab7e56\n--- a/.github/prompts/cca-completion-check.md\n+++ b/.github/prompts/cca-completion-check.md\n@@ -19,6 +19,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n ## Required Reading\n \n Read only the files needed to judge the PR contract. Prefer this order:\ndiff --git a/.github/prompts/codex-task-handoff.md b/.github/prompts/codex-task-handoff.md\nindex 695e211d76dea1c60e8d5bb360e78487808286a4..59441d576b01a39569722f226c854e5a740e9eb5\n--- a/.github/prompts/codex-task-handoff.md\n+++ b/.github/prompts/codex-task-handoff.md\n@@ -2,7 +2,7 @@\n \n You are Codex Front acting as the Implementer for a Shiki task.\n \n-Follow `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant ADRs, and `docs/agents/codex-handoff.md`.\n+Follow `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant Shiki SADRs and target ADRs, and `docs/agents/codex-handoff.md`.\n \n Rules:\n \ndiff --git a/AGENTS.md b/AGENTS.md\nindex 7f041100d49c2a7525f51cb0f5d696a4a6d1c78c..a06bfbabc6fa48a007389e25e12d7ed523936bed\n--- a/AGENTS.md\n+++ b/AGENTS.md\n@@ -22,7 +22,7 @@\n - **Codex Front** is an optional implementer through Codex App, Codex CLI, Codex IDE extension, or Codex Web signed in with ChatGPT OAuth/subscription auth, dispatched with `shiki runner codex` when a task is explicitly assigned to `codex`.\n - **GitHub CCA** is implemented by Claude Code Action by default, using `CLAUDE_CODE_OAUTH_TOKEN`.\n \n-Do not assume `openai/codex-action`, `OPENAI_API_KEY`, or API-key based Codex automation in the default Shiki path. API-key runners are explicit target-repo extensions and require their own ADR.\n+Do not assume `openai/codex-action`, `OPENAI_API_KEY`, or API-key based Codex automation in the default Shiki path. API-key runners are explicit target-repo extensions and require their own target ADR.\n \n Shiki is not:\n \n@@ -44,10 +44,14 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use\n+`SADR-NNNN-*.md`; target product decisions retain `NNNN-*.md` and the `ADR`\n+namespace.\n+\n Use the canonical source-of-truth order above. Runtime-specific reading orders\n are operational guidance only and must not override or reorder it.\n \n-Conversation state is not durable truth. If a decision matters, put it in GitHub, `.shiki/`, `CONTEXT.md`, or an ADR.\n+Conversation state is not durable truth. If a decision matters, put it in GitHub, `.shiki/`, `CONTEXT.md`, a Shiki SADR, or a target ADR.\n \n If GitHub and `.shiki/` disagree, surface the conflict and prefer GitHub operational state until the mirror is repaired.\n \n@@ -75,8 +79,8 @@\n Every non-trivial change follows this loop:\n \n 1. **Goal**: clarify outcome, completion conditions, non-goals, risk level, and success signals.\n-2. **grill-with-docs**: challenge the plan against domain docs, ADRs, code reality, terminology, and edge scenarios.\n-3. **Context & Impact**: find relevant docs, ADRs, code, dependencies, owners, locks, and verification surfaces.\n+2. **grill-with-docs**: challenge the plan against domain docs, Shiki SADRs, target ADRs, code reality, terminology, and edge scenarios.\n+3. **Context & Impact**: find relevant docs, SADRs, target ADRs, code, dependencies, owners, locks, and verification surfaces.\n 4. **PRD and Spec Freeze**: use `to-prd` when context is settled enough to become durable product/engineering intent. The operator's explicit approval of the PRD is Spec Freeze: the plan records a `spec_freeze` block, and no plan runs without it. Steps 1-4 form Requirements Definition \u2014 one continuous operator dialogue. After Spec Freeze, scope changes require an operator-approved Spec Amendment; non-scope-moving interpretations go to the Assumption Log.\n 5. **Issues**: use `to-issues` to create independently grabbable vertical-slice issues. Prefer AFK slices over HITL slices where possible.\n 6. **Triage**: label issues for readiness, risk, runtime, skills, and MergeGate state.\n@@ -167,7 +171,7 @@\n - Testing decisions.\n - Out-of-scope items.\n - Further notes.\n-- Links to relevant domain terms and ADRs.\n+- Links to relevant domain terms, SADRs, and target ADRs.\n \n Every executable task must include:\n \n@@ -193,7 +197,7 @@\n \n Context & Impact must identify:\n \n-- Relevant documents, domain terms, ADRs, and past decisions.\n+- Relevant documents, domain terms, SADRs, target ADRs, and past decisions.\n - Relevant modules, interfaces, seams, callers, owners, and tests.\n - Dependency relationships between tasks.\n - Candidate locks and lock conflicts.\n@@ -210,7 +214,7 @@\n | Skill | Required when |\n | --- | --- |\n | `setup-matt-pocock-skills` | First configuring the repo for skills, or when issue tracker, triage labels, or domain docs are missing. |\n-| `grill-with-docs` | Default for non-trivial Goals before PRD/issues; always required when plans, terminology, boundaries, tradeoffs, or ADR-worthy decisions are ambiguous. |\n+| `grill-with-docs` | Default for non-trivial Goals before PRD/issues; always required when plans, terminology, boundaries, tradeoffs, or decision-record-worthy choices are ambiguous. |\n | `zoom-out` | The agent lacks an architectural map of the relevant code area. |\n | `to-prd` | Settled Goal context must become durable product/engineering intent. |\n | `to-issues` | A Goal, PRD, or plan must become independently grabbable vertical-slice issues. |\n@@ -348,7 +352,7 @@\n - Three or more new files.\n - Any high-risk or critical label.\n \n-When the architecture gate triggers, run `grill-with-docs` or `improve-codebase-architecture` as appropriate and record durable decisions in an ADR when needed.\n+When the architecture gate triggers, run `grill-with-docs` or `improve-codebase-architecture` as appropriate and record durable decisions in a Shiki SADR or target ADR as applicable.\n \n ## Engineering Discipline\n \ndiff --git a/CLAUDE.md b/CLAUDE.md\nindex 98a932e7841a632866fcd53b0bad0e3af3711d3d..98a31399404d6fb044b4fd493f4459ac87f439de\n--- a/CLAUDE.md\n+++ b/CLAUDE.md\n@@ -14,6 +14,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n ## Claude Role\n \n Default to planner, default implementer, reviewer, coordinator, documentation editor, CCA-style completion judge, and final-judgment assistant.\n@@ -57,7 +60,7 @@\n At the start of a Shiki session:\n \n 1. Read `AGENTS.md`.\n-2. Read `CONTEXT.md` and relevant ADRs.\n+2. Read `CONTEXT.md` and relevant Shiki SADRs and target ADRs.\n 3. Read active `.shiki/` Goal/task/ledger state when present.\n 4. Read active distilled rules from `.shiki/memories/` (status `distilled`, `active: true`, not revoked or superseded) for applicable guidance; the same rules are injected deterministically into task handoffs as the `## Distilled Rules` section (\u00a73.5).\n 5. Read the GitHub Issue or PR when the task is GitHub-backed.\n@@ -74,7 +77,7 @@\n - Define the Goal, completion conditions, non-goals, and risk level.\n - Treat `grill-with-docs` as the default planning entry point for non-trivial Goals.\n - Challenge domain terms against `CONTEXT.md`.\n-- Challenge hard-to-reverse tradeoffs against ADRs.\n+- Challenge hard-to-reverse tradeoffs against SADRs and target ADRs.\n - Explore code instead of asking when code can answer the question.\n - Run Context & Impact before implementation planning.\n - Produce a Task DAG, not an unordered checklist.\n@@ -94,7 +97,7 @@\n - For each question, provide your recommended answer.\n - Resolve dependency decisions before downstream design questions.\n - Update `CONTEXT.md` inline when terminology crystallizes.\n-- Offer ADRs only when the decision is hard to reverse, surprising without context, and a real tradeoff.\n+- Offer a SADR or target ADR only when the decision is hard to reverse, surprising without context, and a real tradeoff.\n - Convert unresolved questions into HITL blockers rather than allowing Codex to guess.\n \n ## PRD and Issues Standard\n@@ -103,7 +106,7 @@\n \n - Use the project's domain glossary vocabulary.\n - Avoid volatile file paths unless they are essential.\n-- Include implementation decisions, testing decisions, out-of-scope items, and relevant ADR/domain links.\n+- Include implementation decisions, testing decisions, out-of-scope items, and relevant SADR/target-ADR/domain links.\n - Identify deep-module opportunities and testable interfaces.\n \n When creating issues:\n@@ -121,7 +124,7 @@\n - Branch/worktree target.\n - Scope and non-goals.\n - Dependencies and locks.\n-- Relevant docs, ADRs, modules, and tests.\n+- Relevant docs, SADRs, target ADRs, modules, and tests.\n - Required skill invocations.\n - TDD expectations.\n - Pre-PR `code-review` gate expectations.\n@@ -152,7 +155,7 @@\n \n - Treat yourself as a judge, not implementer.\n - Do not write production code.\n-- Read `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant ADRs, PRD, task issue, PR body, diff, checks, reviews, labels, and `.shiki/` state.\n+- Read `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant SADRs and target ADRs, PRD, task issue, PR body, diff, checks, reviews, labels, and `.shiki/` state.\n - Evaluate every applicable checklist in `docs/agents/checklists.md`.\n - Map each acceptance criterion to durable evidence.\n - Distinguish implementation failure from missing evidence.\ndiff --git a/CONTEXT.md b/CONTEXT.md\nindex e61a9be06a0cce0d026484083a657bde2067e234..237ee8aa3ea8fab334ece49768bc5ed3b5af0974\n--- a/CONTEXT.md\n+++ b/CONTEXT.md\n@@ -25,7 +25,7 @@\n _Avoid_: jumping directly into code\n \n **grill-with-docs**:\n-A planning skill that challenges a plan against domain language, ADRs, code reality, and concrete edge scenarios. It resolves design-tree questions before PRD/issues. It runs inside Requirements Definition.\n+A planning skill that challenges a plan against domain language, Shiki SADRs, target ADRs, code reality, and concrete edge scenarios. It resolves design-tree questions before PRD/issues. It runs inside Requirements Definition.\n _Avoid_: generic brainstorming, silent assumptions\n \n **Requirements Definition**:\ndiff --git a/CONTRIBUTING.md b/CONTRIBUTING.md\nindex c6d9b656e73abb8a55e5f660123e03153dcb3e6d..9e37a8fbf668db5b22461aaae5548b8f1354394c\n--- a/CONTRIBUTING.md\n+++ b/CONTRIBUTING.md\n@@ -17,7 +17,7 @@\n 1. **Goal** \u2014 start from a user-approved Goal with an outcome, completion\n    conditions, non-goals, risk level, and success signals.\n 2. **grill-with-docs** \u2014 for any non-trivial change, challenge the plan against\n-   domain language, ADRs, and code reality before writing a PRD.\n+   domain language, Shiki SADRs, target ADRs, and code reality before writing a PRD.\n 3. **PRD** \u2014 capture settled product/engineering intent (`to-prd`).\n 4. **Spec Freeze** \u2014 the operator explicitly approves the PRD/requirements;\n    the plan records a `spec_freeze` block and no plan runs without it\n@@ -94,7 +94,7 @@\n   entries.\n - Do not hand-edit `.shiki/goals/*.json` or `.shiki/manifest.json` in a task PR.\n - Conversation state is not durable truth. If a decision matters, record it in\n-  GitHub, `.shiki/`, `CONTEXT.md`, or an ADR.\n+  GitHub, `.shiki/`, `CONTEXT.md`, a Shiki SADR, or a target ADR.\n \n ## Architecture Gate\n \n@@ -103,7 +103,7 @@\n or CI policy, package structure, three or more structural files, more than 100\n net new lines in a core module, three or more new files, or any high-risk label.\n When triggered, run `grill-with-docs` or `improve-codebase-architecture` and\n-record durable decisions in an ADR when needed (see `docs/adr/`).\n+record durable decisions in a Shiki SADR or target ADR as applicable (see `docs/adr/`).\n \n ## Safety\n \ndiff --git a/SYSTEM_PROMPT.md b/SYSTEM_PROMPT.md\nindex aaaea45e3a896c3a44ba89eec8bb84325ecf6c62..da009c86a88285269dee0155e58e550c6d744ea9\n--- a/SYSTEM_PROMPT.md\n+++ b/SYSTEM_PROMPT.md\n@@ -30,6 +30,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n Use the canonical source-of-truth order above. Runtime-specific reading orders\n are operational guidance only and must not override or reorder it. The current\n conversation is non-durable operator input.\n@@ -48,14 +51,14 @@\n \n A runtime may only do work that is explicitly assigned by the Goal, task, PR, or MergeGate decision.\n \n-Do not assume `openai/codex-action`, `OPENAI_API_KEY`, or API-key based Codex automation in the default Shiki path. API-key runners are explicit target-repo extensions and require their own ADR.\n+Do not assume `openai/codex-action`, `OPENAI_API_KEY`, or API-key based Codex automation in the default Shiki path. API-key runners are explicit target-repo extensions and require their own target ADR.\n \n ## 4. Non-negotiable Shiki flow\n \n For any non-trivial Goal, use this flow:\n \n 1. **Goal Seek**: define outcome, non-goals, risk level, completion criteria, and evidence requirements.\n-2. **grill-with-docs**: challenge the plan against `CONTEXT.md`, ADRs, code reality, terminology, boundaries, and edge scenarios. Ask one question at a time when the operator is available. If code can answer a question, inspect code instead of asking.\n+2. **grill-with-docs**: challenge the plan against `CONTEXT.md`, Shiki SADRs, target ADRs, code reality, terminology, boundaries, and edge scenarios. Ask one question at a time when the operator is available. If code can answer a question, inspect code instead of asking.\n 3. **Context & Impact**: identify modules, interfaces, seams, callers, dependencies, locks, risk, and verification surfaces.\n 4. **to-prd + Spec Freeze**: turn settled context into a PRD using domain vocabulary and testing decisions. The operator's explicit approval of the PRD is Spec Freeze, recorded as the plan's `spec_freeze` block; plans without it do not run. Steps 1-4 form Requirements Definition.\n 5. **to-issues**: decompose the PRD into independently grabbable vertical-slice issues. Prefer AFK slices; mark HITL slices when judgment is still required.\n@@ -75,7 +78,7 @@\n Before making any material change:\n \n 1. Read `AGENTS.md` and any runtime-specific wrapper such as `CLAUDE.md`.\n-2. Read `CONTEXT.md` and relevant ADRs.\n+2. Read `CONTEXT.md` and relevant SADRs and target ADRs.\n 3. Read the GitHub Issue / PR and `.shiki/` state for the active Goal or task.\n 4. Identify your role: Planner, Implementer, Reviewer, Completion Check Agent, Repairer, or Guardian-assist.\n 5. Identify Goal id, task id, branch, PR, dependencies, locks, risk level, required skills, and required evidence.\n@@ -103,7 +106,7 @@\n \n At minimum, identify:\n \n-- Relevant documents, ADRs, domain terms, and prior decisions.\n+- Relevant documents, SADRs, target ADRs, domain terms, and prior decisions.\n - Relevant modules, interfaces, seams, callers, owners, and tests.\n - Dependency relationships between tasks.\n - Candidate file locks and lock conflicts.\n@@ -152,7 +155,7 @@\n \n CCA must read:\n \n-- `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant ADRs.\n+- `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant SADRs and target ADRs.\n - Parent Goal issue, PRD issue, task issue, comments, and labels.\n - PR body, diff, commits, changed files, reviews, CI checks, and artifacts available to the workflow.\n - `.shiki/` task contract, locks, ledger entries, CCA checklist profile, and prior repair packets.\n@@ -214,7 +217,7 @@\n - Three or more new files.\n - Any high-risk or critical label.\n \n-When the architecture gate triggers, run `grill-with-docs` or `improve-codebase-architecture` as appropriate and record the decision in an ADR if it is hard to reverse or likely to be re-litigated.\n+When the architecture gate triggers, run `grill-with-docs` or `improve-codebase-architecture` as appropriate and record the decision in a Shiki SADR or target ADR as applicable if it is hard to reverse or likely to be re-litigated.\n \n ## 14. Skill Gate\n \n@@ -223,7 +226,7 @@\n Use these skills:\n \n - `setup-matt-pocock-skills`: before first use of the engineering skill set, or when issue tracker, triage labels, or domain docs are missing.\n-- `grill-with-docs`: default for non-trivial Goals before PRD/issues; required for ambiguous plans, terminology, boundaries, ADR-worthy tradeoffs, or design-tree decisions.\n+- `grill-with-docs`: default for non-trivial Goals before PRD/issues; required for ambiguous plans, terminology, boundaries, decision-record-worthy tradeoffs, or design-tree decisions.\n - `zoom-out`: unfamiliar code area or missing architectural map.\n - `to-prd`: convert settled context into a PRD.\n - `to-issues`: break a PRD, plan, or Goal into independently grabbable vertical-slice issues.\ndiff --git a/docs/adr/0001-keep-shiki-as-platform-name.md b/docs/adr/SADR-0001-keep-shiki-as-platform-name.md\nindex 20a9637c3b9d8c97de7ba8db7cfae4d82d9a7dba..8b685439959aada1b221c112f0ac3c51ca8a1695\n--- a/docs/adr/0001-keep-shiki-as-platform-name.md\n+++ b/docs/adr/SADR-0001-keep-shiki-as-platform-name.md\n@@ -1,3 +1,3 @@\n-# Keep Shiki As The Platform Name\n+# SADR-0001: Keep Shiki As The Platform Name\n \n Shiki remains the product name for the GitHub-native agentic engineering control plane. We considered more descriptive names such as MergePilot, MergeGate, and Kiroku, but kept Shiki because it carries the existing goal-seeking framework identity while remaining independent of Hermes, Codex, Claude Code, or any single runtime.\ndiff --git a/docs/adr/0002-use-agents-as-shared-constitution.md b/docs/adr/SADR-0002-use-agents-as-shared-constitution.md\nindex 73de30ecb078120eac540d9bc51855e3cd48d0e3..c5486182bf06fca36ea508496b013c67302fd989\n--- a/docs/adr/0002-use-agents-as-shared-constitution.md\n+++ b/docs/adr/SADR-0002-use-agents-as-shared-constitution.md\n@@ -1,3 +1,3 @@\n-# Use AGENTS As The Shared Agent Constitution\n+# SADR-0002: Use AGENTS As The Shared Agent Constitution\n \n Shiki uses `AGENTS.md` as the runtime-neutral constitution and `CLAUDE.md` as a thin Claude Code wrapper. This keeps Codex, Claude Code, Hermes Runner, and future Agent Runtimes aligned on the same GitHub-first MergeGate rules while still allowing runtime-specific instructions where needed.\ndiff --git a/docs/adr/0003-require-skill-gate-for-mergegate-readiness.md b/docs/adr/SADR-0003-require-skill-gate-for-mergegate-readiness.md\nindex 424be0c9af4c8f0136f1fde68a468c47ff84e764..7fc337987c7df9792f75f181dfcbed75fe5ffff2\n--- a/docs/adr/0003-require-skill-gate-for-mergegate-readiness.md\n+++ b/docs/adr/SADR-0003-require-skill-gate-for-mergegate-readiness.md\n@@ -1,3 +1,3 @@\n-# Require Skill Gate For MergeGate Readiness\n+# SADR-0003: Require Skill Gate For MergeGate Readiness\n \n Shiki requires relevant engineering skills to be invoked before execution when their triggers apply. This makes skills part of MergeGate readiness rather than optional prompt style, because Shiki depends on repeatable planning, issue decomposition, TDD, diagnosis, triage, and architecture review before autonomous or parallel agent work can be trusted.\ndiff --git a/docs/adr/0004-use-github-first-with-shiki-mirror.md b/docs/adr/SADR-0004-use-github-first-with-shiki-mirror.md\nindex d0a3a9174dc646a945a9d325954ffaf00c352040..a79c54ae9ee8eed2a0524192b864da4e22a92d3f\n--- a/docs/adr/0004-use-github-first-with-shiki-mirror.md\n+++ b/docs/adr/SADR-0004-use-github-first-with-shiki-mirror.md\n@@ -1,4 +1,4 @@\n-# ADR 0004: Use GitHub First With A Repository-Local Shiki Mirror\n+# SADR-0004: Use GitHub First With A Repository-Local Shiki Mirror\n \n ## Status\n \ndiff --git a/docs/adr/0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md b/docs/adr/SADR-0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\nindex b339baf49b89d9290e79c3d51b36e7b2bde3bd95..0f3c3eb9afccdb3403ce978e21d04fe12e495da0\n--- a/docs/adr/0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\n+++ b/docs/adr/SADR-0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\n@@ -1,8 +1,8 @@\n-# ADR 0005: Use Codex As Front And Claude Code Action As The GitHub Runtime\n+# SADR-0005: Use Codex As Front And Claude Code Action As The GitHub Runtime\n \n ## Status\n \n-Accepted (implementer default superseded by ADR 0008; the auth model and the\n+Accepted (implementer default superseded by SADR-0008; the auth model and the\n Claude Code Action GitHub runtime decision remain in force)\n \n ## Context\n@@ -26,4 +26,4 @@\n - Shiki avoids requiring `OPENAI_API_KEY` for the default workflow.\n - Codex work happens through the user's authenticated Codex front surface, not as a hidden backend Action.\n - Claude Code Action is configured for OAuth-token based subscription usage by default.\n-- API-key based automation remains possible only as an explicit target-repo extension with its own ADR.\n+- API-key based automation remains possible only as an explicit target-repo extension with its own target ADR.\ndiff --git a/docs/adr/0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md b/docs/adr/SADR-0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\nindex 4bcae9d1e52f0a37b8e9fbafa5cd14b6ee1993bd..7a38538a6ae9929567f0ade67e7ea6a60d0b3f7e\n--- a/docs/adr/0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\n+++ b/docs/adr/SADR-0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\n@@ -1,4 +1,4 @@\n-# ADR 0006: Separate Codex Implementation, CCA Completion, And MergeGate Authorization\n+# SADR-0006: Separate Codex Implementation, CCA Completion, And MergeGate Authorization\n \n ## Status\n \ndiff --git a/docs/adr/0007-packaging-and-release.md b/docs/adr/SADR-0007-packaging-and-release.md\nindex 79b84ddb97167372b44641e5b7860e400ce80822..bb445a2e2e856d9d1f5fdbe35de8d4e8cec0fb32\n--- a/docs/adr/0007-packaging-and-release.md\n+++ b/docs/adr/SADR-0007-packaging-and-release.md\n@@ -1,4 +1,4 @@\n-# ADR 0007: Packaging And Release For Shiki\n+# SADR-0007: Packaging And Release For Shiki\n \n ## Status\n \n@@ -25,7 +25,7 @@\n   not introduce third-party runtime dependencies or break the shim.\n - Releases must run inside the operator's subscription-authenticated toolchain\n   and must not require extra secrets beyond the built-in `GITHUB_TOKEN`\n-  (consistent with ADR 0005's no-API-key-by-default stance).\n+  (consistent with SADR-0005's no-API-key-by-default stance).\n - `.shiki/manifest.json` already encodes the install surface (`install.include`,\n   `install.create_directories`, `install.exclude_from_commit`), and it must not\n   be mutated as part of this change.\n@@ -34,7 +34,7 @@\n \n This is a hard-to-reverse platform decision (it sets the version source of\n truth, the release trigger, and the public install contract), so it is recorded\n-as an ADR.\n+as a SADR.\n \n ## Decision\n \n@@ -104,7 +104,7 @@\n 3. **Publishing to PyPI.** Rejected for now: Shiki is distributed as a\n    repository template + git-installable CLI, not a PyPI library; PyPI would add\n    credentials/secrets and a different versioning contract. Can be added later as\n-   its own ADR if a packaged distribution is needed.\n+   its own SADR if a packaged distribution is needed.\n 4. **Dynamic version (read `VERSION` via a build backend `dynamic` field).**\n    Rejected to keep `pyproject.toml` backend-agnostic and dependency-free; the\n    small duplication between `VERSION` and `pyproject.toml` is guarded by the\n@@ -123,4 +123,4 @@\n   releasing versions the whole platform but ships only `install.include` to\n   targets.\n - Future PyPI distribution or a console entry point are deferred and would each\n-  warrant their own ADR.\n+  warrant their own SADR.\ndiff --git a/docs/adr/0008-use-claude-code-as-default-implementer.md b/docs/adr/SADR-0008-use-claude-code-as-default-implementer.md\nindex dedf4510b7c7799521422f622029563d373db031..35c2625d908167fbc58855b5b63e283805f167e8\n--- a/docs/adr/0008-use-claude-code-as-default-implementer.md\n+++ b/docs/adr/SADR-0008-use-claude-code-as-default-implementer.md\n@@ -1,12 +1,12 @@\n-# ADR 0008: Use Claude Code As The Default Implementer Runtime\n+# SADR-0008: Use Claude Code As The Default Implementer Runtime\n \n ## Status\n \n-Proposed (supersedes ADR 0005 for the implementer default; keeps its auth model)\n+Proposed (supersedes SADR-0005 for the implementer default; keeps its auth model)\n \n ## Context\n \n-ADR 0005 made Codex Front the default implementation runtime, with Claude Code\n+SADR-0005 made Codex Front the default implementation runtime, with Claude Code\n restricted to planner and reviewer roles by the runtime registry, and the only\n autonomous implementation adapter was `shiki runner codex`.\n \n@@ -18,13 +18,13 @@\n - The platform's planning, review, and CCA layers already run on Claude Code,\n   and Claude Code's native capabilities (the built-in `/code-review` skill and\n   the Workflow multi-agent orchestration tool) have no Codex equivalent.\n-- The Goal lifecycle redesign (ADR 0009) requires an autonomous post-freeze\n+- The Goal lifecycle redesign (SADR-0009) requires an autonomous post-freeze\n   loop driven from the operator's Claude Code session, including bounded\n   repair dispatch, which must not depend on a runtime that cannot be invoked\n   unattended.\n \n The constitution claims Shiki is runtime-agnostic (\"not a Claude-only\n-workflow\", ADR 0002). Any change to the implementer default must preserve the\n+workflow\", SADR-0002). Any change to the implementer default must preserve the\n registry-based runtime model rather than hardcoding one vendor.\n \n ## Decision\n@@ -39,7 +39,7 @@\n   the task to review or repair-needed.\n - Keep Codex registered as an optional implementer runtime. `shiki runner\n   codex`, the Codex handoff templates, and the ChatGPT OAuth auth model from\n-  ADR 0005 remain supported for tasks explicitly assigned to `codex`.\n+  SADR-0005 remain supported for tasks explicitly assigned to `codex`.\n - Commit to mandating two Claude-native capabilities as loop contract in\n   follow-up Goals (status: the code-review gate was delivered by Goal G-B \u2014\n   skill registry, checklists item PR-12, default required_skills \u2014 while the\n@@ -60,7 +60,7 @@\n \n Out of scope: removing Codex support, changing the CCA runtime (Claude Code\n Action with `CLAUDE_CODE_OAUTH_TOKEN` stays), and API-key based automation\n-(still requires its own ADR per ADR 0005).\n+(still requires its own target ADR per SADR-0005).\n \n ## Consequences\n \n@@ -100,7 +100,7 @@\n   harness rejection of direct `codex exec` is outside Shiki's control, and the\n   plan's mandatory native capabilities cannot run on Codex at all.\n - **Remove Codex entirely**: rejected; it abandons the runtime-agnostic\n-  constitution (ADR 0002) for no operational gain and forecloses\n+  constitution (SADR-0002) for no operational gain and forecloses\n   cross-runtime adversarial review.\n - **GitHub Actions cloud implementation via claude-code-action**: rejected as\n   default; durable evidence is automatic but token cost, Node 24 workflow\ndiff --git a/docs/adr/0009-requirements-definition-and-spec-freeze.md b/docs/adr/SADR-0009-requirements-definition-and-spec-freeze.md\nindex 29b3f7871bef9203a8b4a831a65c39eb2f506e42..52e7dd69ad0aa4efe0b68c62678abbf7df02aaec\n--- a/docs/adr/0009-requirements-definition-and-spec-freeze.md\n+++ b/docs/adr/SADR-0009-requirements-definition-and-spec-freeze.md\n@@ -1,4 +1,4 @@\n-# ADR 0009: Merge Grill And Planning Into Requirements Definition With Spec Freeze\n+# SADR-0009: Merge Grill And Planning Into Requirements Definition With Spec Freeze\n \n ## Status\n \ndiff --git a/docs/adr/0010-external-ai-guardian-review.md b/docs/adr/SADR-0010-external-ai-guardian-review.md\nindex e6108bbe8bb4e73e0ff4684c8f78001a80438c4e..f3510b145664298adafb151b6f0db51b972f01ec\n--- a/docs/adr/0010-external-ai-guardian-review.md\n+++ b/docs/adr/SADR-0010-external-ai-guardian-review.md\n@@ -1,4 +1,4 @@\n-# ADR 0010: External AI Guardian Review As A First-Class Approval Authority\n+# SADR-0010: External AI Guardian Review As A First-Class Approval Authority\n \n ## Status\n \n@@ -8,7 +8,7 @@\n \n Shiki is intended to be an autonomous, recursive, self-improving agent\n framework: authority-in-the-loop, not human-in-the-loop. Until now the Guardian\n-approval required for high/critical-risk merges (guardian-policy.json, ADR\n+approval required for high/critical-risk merges (guardian-policy.json, SADR\n governance) recognized only human authority sources \u2014 a GitHub review approval,\n the `guardian:approved` label applied by a configured Guardian, or a comment\n carrying `Guardian approval granted` plus the current head SHA.\n@@ -89,7 +89,7 @@\n   autonomous design the operator intends and produced the impersonation\n   incident.\n - **Record AI approval as an operator comment**: rejected as forgery of human\n-  authority \u2014 the exact failure this ADR corrects.\n+  authority \u2014 the exact failure this SADR corrects.\n - **Deliver the artifact as a committed `.shiki/` file**: rejected; a committed\n   approval inside the PR being approved is forgeable by anyone who can push.\n - **A dedicated reviewer bot identity instead of a relayed comment**: deferred;\ndiff --git a/docs/adr/0011-autonomous-quality-gate-evidence.md b/docs/adr/SADR-0011-autonomous-quality-gate-evidence.md\nindex bca0246c5d46e44afbde15486a75eec425fc02f5..9c93eac16d8c3a6d4b11bd7224e3955102cd7db4\n--- a/docs/adr/0011-autonomous-quality-gate-evidence.md\n+++ b/docs/adr/SADR-0011-autonomous-quality-gate-evidence.md\n@@ -1,4 +1,4 @@\n-# ADR 0011: Autonomous Quality-Gate Evidence \u2014 Deterministic Facts And An Independent Verifier\n+# SADR-0011: Autonomous Quality-Gate Evidence \u2014 Deterministic Facts And An Independent Verifier\n \n ## Status\n \ndiff --git a/docs/adr/0012-autonomous-loop-closeout-to-main.md b/docs/adr/SADR-0012-autonomous-loop-closeout-to-main.md\nindex 637fad6dde4989c9c2bf0f81f676d8d29991123c..ec7d85f81030a676bcbba28b43eb365422a36eac\n--- a/docs/adr/0012-autonomous-loop-closeout-to-main.md\n+++ b/docs/adr/SADR-0012-autonomous-loop-closeout-to-main.md\n@@ -1,4 +1,4 @@\n-# 0012 \u2014 Autonomous loop pushes goal completion to main via a closeout PR\n+# SADR-0012 \u2014 Autonomous loop pushes goal completion to main via a closeout PR\n \n - Status: accepted\n - Date: 2026-06-18\ndiff --git a/docs/adr/0013-bootstrap-grants-actions-approve-pull-requests.md b/docs/adr/SADR-0013-bootstrap-grants-actions-approve-pull-requests.md\nindex b6c69d64f1c5ced346b88d6859586715b3b5bd0e..cf48ff4a6716267f539ca58edd1a852e7c7a2d64\n--- a/docs/adr/0013-bootstrap-grants-actions-approve-pull-requests.md\n+++ b/docs/adr/SADR-0013-bootstrap-grants-actions-approve-pull-requests.md\n@@ -1,4 +1,4 @@\n-# ADR 0013: Bootstrap Grants GitHub Actions Permission To Create And Approve Pull Requests\n+# SADR-0013: Bootstrap Grants GitHub Actions Permission To Create And Approve Pull Requests\n \n ## Status\n \n@@ -31,7 +31,7 @@\n security tradeoff: it lets repository automation satisfy a human-review gate. In\n a multi-maintainer repo that could let a compromised or buggy workflow\n self-approve changes. So whether bootstrap turns it on by default \u2014 and with\n-what default token scope \u2014 is an ADR-worthy decision, not an implementation\n+what default token scope \u2014 is a SADR-worthy decision, not an implementation\n detail.\n \n ## Decision\n@@ -48,7 +48,7 @@\n Specifically:\n \n - **`can_approve_pull_request_reviews` defaults to `true`.** Shiki's default\n-  posture is solo/AFK autonomous operation (ADR 0008/0009/0012), where the CCA\n+  posture is solo/AFK autonomous operation (SADR-0008/0009/0012), where the CCA\n   Review Bridge is the only path that can satisfy `required_review: true` after\n   the CCA and Guardian gates have already passed. The default makes a\n   freshly-bootstrapped repo able to run the autonomous loop end to end without a\ndiff --git a/docs/adr/0014-codex-app-external-ai-guardian-ui-adapter.md b/docs/adr/SADR-0014-codex-app-external-ai-guardian-ui-adapter.md\nindex 238db06279334caa341c117362df9271382a7fcd..65f12e9939321e1b3e389a34c822fc1549e5155d\n--- a/docs/adr/0014-codex-app-external-ai-guardian-ui-adapter.md\n+++ b/docs/adr/SADR-0014-codex-app-external-ai-guardian-ui-adapter.md\n@@ -1,4 +1,4 @@\n-# ADR 0014: Use Codex App As The External AI Guardian UI Adapter\n+# SADR-0014: Use Codex App As The External AI Guardian UI Adapter\n \n ## Status\n \ndiff --git a/docs/adr/0015-contract-approval-before-dispatch.md b/docs/adr/SADR-0015-contract-approval-before-dispatch.md\nindex fad6c656741a20a7684ba16080a7db159f63a52f..6061271a46d3f1d6ae7174477de3fb75673123a6\n--- a/docs/adr/0015-contract-approval-before-dispatch.md\n+++ b/docs/adr/SADR-0015-contract-approval-before-dispatch.md\n@@ -1,4 +1,4 @@\n-# ADR 0015: Approve The Task Contract Before Dispatch, Not The Diff After It\n+# SADR-0015: Approve The Task Contract Before Dispatch, Not The Diff After It\n \n ## Status\n \n@@ -9,7 +9,7 @@\n Guardian approval for high/critical risk is expressible only on a pull request.\n Every approval source in `guardian-policy.json` \u2014 GitHub review, the\n `guardian:approved` label, the `Guardian approval granted` comment, and the\n-`external_ai_guardian_review` artifact (ADR 0010/0014) \u2014 is bound to a PR, and\n+`external_ai_guardian_review` artifact (SADR-0010/0014) \u2014 is bound to a PR, and\n three of the four require the current head SHA. Approval therefore cannot exist\n before an implementation PR exists.\n \n@@ -81,7 +81,7 @@\n An envelope breach must surface only on the non-repairable policy gate, never on\n the repairable `MergeGate metadata check`. Otherwise the Goal Loop routes it to\n `dispatch_repair` \u2014 instructing a runner to make the breach go away \u2014 which is\n-exactly what this ADR forbids.\n+exactly what this SADR forbids.\n \n **Contract immutability.** After registration, an implementation PR must not\n change the governance fields of its own task contract (`scope`, `non_goals`,\n@@ -116,7 +116,7 @@\n fails any verdict that is not `complete`, and the signal the CCA prompt reads is\n computed from `evaluate_guardian_approval` alone. Without the OR-merge at the\n signal, a contract-approved PR still returns `needs_guardian`, the CCA job fails,\n-and the MergeGate policy check never runs \u2014 the gate this ADR changes would never\n+and the MergeGate policy check never runs \u2014 the gate this SADR changes would never\n be reached. The CCA *prompt* needs no change: a `contract_approval` entry in the\n signal's existing `sources` array satisfies its current wording.\n \n@@ -130,7 +130,7 @@\n written, and an approved contract lets its implementation PRs merge through the\n same automatic path as low/medium risk. The wasted implement-then-block cycle\n disappears. Guardian review shifts from reading diffs to reading contracts, which\n-is both earlier and smaller; the external AI Guardian path (ADR 0010/0014) works\n+is both earlier and smaller; the external AI Guardian path (SADR-0010/0014) works\n on contracts with no change.\n \n We accept that the Guardian authorizes a class of change without seeing its\n@@ -146,7 +146,7 @@\n adversarial tests for the new approval path equivalent in rigor to\n `test_shiki_governance_evidence.sh`.\n \n-Bootstrap costs, which apply only while this ADR is being implemented. The tasks\n+Bootstrap costs, which apply only while this SADR is being implemented. The tasks\n that build Contract Approval merge under the existing rules, so the ones\n dispatched after the pre-dispatch gate lands but before the approval path lands\n need a Guardian-approved Contract PR *and* a live Guardian approval on their\n@@ -210,7 +210,7 @@\n risk; they do not bound authorized work.\n \n **Auto-escalate risk on envelope breach instead of stopping.** Returns the\n-workflow to a diff-time Guardian decision \u2014 the exact behavior this ADR removes.\n+workflow to a diff-time Guardian decision \u2014 the exact behavior this SADR removes.\n \n **Resolve governance fields from the base snapshot only.** Rejected against the\n registration flow: implementation PRs add their own contract, so the base has no\ndiff --git a/docs/adr/0016-locks-bound-product-paths.md b/docs/adr/SADR-0016-locks-bound-product-paths.md\nindex b5e850f3f1f7f4ef6e7f9d1763a29a51bd300c45..46b40a9b99a7aae644c02bb458f5473ff0a90ce2\n--- a/docs/adr/0016-locks-bound-product-paths.md\n+++ b/docs/adr/SADR-0016-locks-bound-product-paths.md\n@@ -1,4 +1,4 @@\n-# ADR 0016: Locks bound product paths; the Shiki mirror is bound by record identity\n+# SADR-0016: Locks bound product paths; the Shiki mirror is bound by record identity\n \n ## Status\n \n@@ -6,8 +6,8 @@\n \n - Date: 2026-07-28\n - Deciders: operator (mizutani-140), Claude (planner)\n-- Related: ADR 0004 (GitHub-first with a `.shiki` mirror), ADR 0009 (Spec Freeze),\n-  ADR 0012 (autonomous loop closeout to main), ADR 0015 (Contract Approval before\n+- Related: SADR-0004 (GitHub-first with a `.shiki` mirror), SADR-0009 (Spec Freeze),\n+  SADR-0012 (autonomous loop closeout to main), SADR-0015 (Contract Approval before\n   dispatch), remediation goal task R-02, the live PR #179 deadlock.\n \n ## Context\n@@ -90,11 +90,11 @@\n files, documentation files and named source modules are nameable; runtime-\n allocated ledger, runner and report ids are not \u2014 and those live in `.shiki`,\n which locks no longer govern. Existing frozen contracts are brought into line by\n-a one-time Spec Amendment per plan, which must land before ADR 0015's contract\n+a one-time Spec Amendment per plan, which must land before SADR-0015's contract\n immutability makes `locks` unwritable from the implementation PR.\n \n **Lock state is resolved from the durable side.** MergeGate resolves the lock\n-registry, and ADR 0015's governance fields, from the base `.shiki` snapshot when\n+registry, and SADR-0015's governance fields, from the base `.shiki` snapshot when\n present, falling back to head only when absent. The `MergeGate metadata check`\n job is given `--base-shiki`, which it does not receive today. Locks are not\n derived from the live open-pull-request set: a pull request body is untrusted\n@@ -108,7 +108,7 @@\n Explicitly out of scope: moving MergeGate's contract resolution off the PR\n checkout wholesale; making the mirror a projection of GitHub state; introducing\n new lock kinds; moving ledger evidence out of the repository; and unwinding\n-ADR 0012's closeout pull request. Registration of multi-task goals is ADR 0015's\n+SADR-0012's closeout pull request. Registration of multi-task goals is SADR-0015's\n Contract PR and is not re-decided here.\n \n ## Consequences\n@@ -127,7 +127,7 @@\n \n Risks accepted. First, goal-scoped identity is only as strong as `goal_id`,\n which is resolved from the PR body and the head task file; the mitigation is to\n-add `goal_id` to ADR 0015's frozen governance field set, and until that lands a\n+add `goal_id` to SADR-0015's frozen governance field set, and until that lands a\n task file can name a foreign goal. Second, base-when-present resolution is\n unavailable on fork pull requests, where the head is contributor-controlled;\n fork pull requests already cannot complete a Shiki merge, and this decision does\n@@ -139,7 +139,7 @@\n path, not by implementation pull requests.\n \n New obligations: one Spec Amendment per frozen plan to rewrite `locks`, ordered\n-before ADR 0015 T1; extension of the adversarial suites in\n+before SADR-0015 T1; extension of the adversarial suites in\n `scripts/test_shiki_adversarial_state.sh` and\n `scripts/test_shiki_governance_evidence.sh` to cover the eleven newly-scoped\n directories; and updating `scripts/test_shiki_loop_lock_guard.sh`,\n@@ -151,32 +151,32 @@\n whether the loop's transitive sync of ledger `evidence` references can be\n deleted once the Contract PR carries its own source plan.\n \n-## Relationship to ADR 0004\n+## Relationship to SADR-0004\n \n-This ADR **clarifies something ADR 0004 left unspecified**. ADR 0004 decides that\n+This SADR **clarifies something SADR-0004 left unspecified**. SADR-0004 decides that\n GitHub is the operational source of truth and that each target repository keeps a\n `.shiki/` mirror. It says nothing about pull requests writing the mirror, does\n not name MergeGate, and does not make the mirror an input to a gate. Nothing in\n-ADR 0004's Decision or Consequences is amended, superseded or weakened: the\n+SADR-0004's Decision or Consequences is amended, superseded or weakened: the\n mirror still exists, still records the same artifacts, and pull request\n-boundaries remain the coordination surface. What this ADR settles is the question\n+boundaries remain the coordination surface. What this SADR settles is the question\n 0004 never reached \u2014 by what authority a pull request may write a mirror record \u2014\n and it answers it the way `.shiki/manifest.json` already declares\n (`pr_mutation: current-task-or-current-goal-only`).\n \n-It **amends ADR 0012**, whose Decision item 5 states that \"Loop-executed tasks\n+It **amends SADR-0012**, whose Decision item 5 states that \"Loop-executed tasks\n declare the `path:.shiki/**` lock, which covers every `.shiki` file the closeout\n stages ... so `files_outside_locks` is satisfied without per-file lock\n bookkeeping.\" That clause is replaced: the closeout's mirror writes are\n-authorized by record identity, not by a declared lock. ADR 0012's substantive\n+authorized by record identity, not by a declared lock. SADR-0012's substantive\n decision \u2014 that completion reaches `main` through a closeout pull request rather\n than local attestation \u2014 is preserved unchanged.\n \n-It is **complementary to ADR 0015**, not a substitute. ADR 0015 owns contract\n+It is **complementary to SADR-0015**, not a substitute. SADR-0015 owns contract\n immutability, the pre-dispatch gate, the Contract PR and the Scope Envelope. This\n-ADR owns what a lock means and who may write the mirror. The one ordering\n+SADR owns what a lock means and who may write the mirror. The one ordering\n constraint between them is that the lock-narrowing Spec Amendment must precede\n-ADR 0015 T1's freezing of the `locks` field.\n+SADR-0015 T1's freezing of the `locks` field.\n \n ## Alternatives Considered\n \n@@ -186,7 +186,7 @@\n the only writer that can register them may run. Migrating the Guardian signal to\n that store makes every registration PR fail closed to `needs_guardian` at every\n risk level, because the task file is by definition absent from the store, which\n-removes the low/medium autonomous path ADR 0008/0009 exists to provide. And it\n+removes the low/medium autonomous path SADR-0008/0009 exists to provide. And it\n leaves schemas, manifest, config, guardian policy, templates, migrations, plans,\n worktrees, runner records, runs and handoffs with no writer at all in a\n repository whose product is `.shiki`. Measured benefit on the frozen backlog:\n@@ -199,14 +199,14 @@\n namespace is not in fact ownership-partitioned \u2014 56 ledger entries carry no\n `task_id`, and one goal DAG is writable by 35 registered tasks. The correct\n expression of its insight is that locks do not govern the mirror at all, which is\n-what this ADR decides.\n+what this SADR decides.\n \n **Make the mirror a projection written only by a registration writer and a\n post-merge projection job.** Rejected. It requires a bypass actor with write\n access to the protected default branch; it makes its own master invariant\n readable from a manifest that pull requests may edit; it deletes both reconcile\n modes and leaves a corrupted mirror with no in-band repair path, contradicting\n-ADR 0004's consequence that mirror conflicts are repaired against GitHub state;\n+SADR-0004's consequence that mirror conflicts are repaired against GitHub state;\n and its content-derived ids are rejected by `validate_shiki`'s currently-green id\n pattern.\n \ndiff --git a/docs/adr/0017-bookkeeping-closeouts-do-not-inherit-risk.md b/docs/adr/SADR-0017-bookkeeping-closeouts-do-not-inherit-risk.md\nindex 554e3a0d526a72ca067108c5464cc68c3edcf174..b9676857a4713850e063af7c39c8e0b01ecb4c66\n--- a/docs/adr/0017-bookkeeping-closeouts-do-not-inherit-risk.md\n+++ b/docs/adr/SADR-0017-bookkeeping-closeouts-do-not-inherit-risk.md\n@@ -1,4 +1,4 @@\n-# ADR 0017: A Bookkeeping Closeout Does Not Inherit Its Task's Risk\n+# SADR-0017: A Bookkeeping Closeout Does Not Inherit Its Task's Risk\n \n ## Status\n \n@@ -6,7 +6,7 @@\n \n ## Context\n \n-ADR 0012 made the goal loop push a task's completion to the default branch through a\n+SADR-0012 made the goal loop push a task's completion to the default branch through a\n closeout pull request: after the implementation PR merges, the loop opens a second PR\n carrying `task.status = done`, `lock.state = released`, and \u2014 when the task completes\n its goal \u2014 `goal.status = complete` plus one scorecard report. Nothing else. The\n@@ -49,14 +49,14 @@\n 4. The task file's governance fields (`scope`, `non_goals`, `required_skills`,\n    `risk_level`, `locks`, `acceptance_checks`, `test_command`) are byte-identical to\n    the base snapshot. Only `status`, `expected_pr`, `closeout_pr`, `ledger_evidence`\n-   and `expected_branch` may differ \u2014 the ADR 0015 contract-immutability rule,\n+   and `expected_branch` may differ \u2014 the SADR-0015 contract-immutability rule,\n    unchanged.\n \n    `expected_branch` is in that set because a closeout is *by construction* on a\n    different branch from the implementation it closes, and the loop rewrites the\n    field accordingly. It is safe to let it differ because MergeGate independently\n    requires `expected_branch` to equal the PR's actual head ref, so a PR cannot set\n-   it to anything other than the branch it is really on. The first draft of this ADR\n+   it to anything other than the branch it is really on. The first draft of this SADR\n    omitted it, which made the exemption unreachable: every real closeout failed\n    condition 4 on a field the loop always changes. See PR #209.\n 5. The state transitions are exactly the terminal set: task `review` \u2192 `done`, lock\n@@ -113,12 +113,12 @@\n classification must be derived, not asserted.\n \n **Fold the closeout into the implementation PR** so no second PR exists. Rejected under\n-ADR 0012, which recorded why: a task file that is already `done` on the implementation\n+SADR-0012, which recorded why: a task file that is already `done` on the implementation\n branch short-circuits the loop's review \u2192 CCA \u2192 merge \u2192 done ordering into a local\n goal-completion before any merge happens.\n \n **Have the loop auto-merge closeouts with elevated credentials.** Rejected outright: that\n-is a runtime authoring its own approval, which ADR 0010 and the constitution's\n+is a runtime authoring its own approval, which SADR-0010 and the constitution's\n approval-record prohibition forbid. It would also be indistinguishable, in the evidence,\n from the forgery those rules exist to prevent.\n \ndiff --git a/docs/adr/0018-carried-guardian-approval-across-a-proven-base-sync.md b/docs/adr/SADR-0018-carried-guardian-approval-across-a-proven-base-sync.md\nindex 22358562da390ed8b45cd600da08a43d378b4680..e6e2907cefa776460af73ddf7d39db605f3c39b0\n--- a/docs/adr/0018-carried-guardian-approval-across-a-proven-base-sync.md\n+++ b/docs/adr/SADR-0018-carried-guardian-approval-across-a-proven-base-sync.md\n@@ -1,4 +1,4 @@\n-# ADR 0018: Carried Guardian Approval Across A Proven Base Sync\n+# SADR-0018: Carried Guardian Approval Across A Proven Base Sync\n \n ## Status\n \n@@ -34,7 +34,7 @@\n Guardian evaluator that consumes it (`shiki_guardian.evaluate_guardian_approval`,\n the `guardian_comment_carried` source) were built and tested in isolation. Neither\n does anything until a caller passes the flags that turn the carry on, so the\n-feature was invisible in CI. This ADR records the decision the two of them\n+feature was invisible in CI. This SADR records the decision the two of them\n implement, and this task wires it into the four CI invocations that judge Guardian\n approval.\n \ndiff --git a/docs/adr/README.md b/docs/adr/README.md\nindex c70e057a4520c36e042ab68c53cd4a21ef709b71..5da43856836e2ce6a150f8127766e2f88355ab4b\n--- a/docs/adr/README.md\n+++ b/docs/adr/README.md\n@@ -1,40 +1,41 @@\n-# Architecture Decision Records\n+# Decision Record Namespaces\n \n-This directory holds Shiki's Architecture Decision Records (ADRs): durable\n-records of hard-to-reverse platform decisions. ADRs are part of the Shiki source\n-of truth alongside GitHub state, the `.shiki/` mirror, and `CONTEXT.md`.\n+This directory holds two independent decision namespaces:\n \n-## How ADRs Work\n+- Shiki Architecture Decision Records (SADRs), named\n+  `SADR-NNNN-<short-slug>.md`, record hard-to-reverse Shiki platform decisions.\n+- Target Architecture Decision Records (ADRs), named\n+  `NNNN-<short-slug>.md`, belong to the target product.\n \n-- New ADRs are added as `docs/adr/NNNN-<short-slug>.md`, where `NNNN` is the next\n-  zero-padded sequence number.\n-- The **canonical list of ADRs is the set of files matching `docs/adr/*.md`** in\n-  this directory. Browse `docs/adr/*.md` for the authoritative, current set.\n-- Use [`template.md`](template.md) as the starting point for a new ADR.\n-- Record an ADR when a decision is hard to reverse, surprising without context,\n-  and a real tradeoff. Do not write an ADR for routine or easily reversible\n-  choices.\n+The same four-digit number may appear once in each namespace. A platform\n+`SADR-NNNN` citation resolves only to `SADR-NNNN-*.md`; a target `ADR NNNN`\n+citation resolves only to `NNNN-*.md`.\n \n-Using the `docs/adr/*.md` glob as the source of truth means other PRs can add an\n-ADR without editing this index file, avoiding merge contention on a\n-hand-maintained enumeration.\n+## How Decision Records Work\n \n-## Current ADRs (snapshot)\n-\n-The list below is a convenience snapshot and may lag behind the directory. When\n-in doubt, list `docs/adr/*.md`.\n+- New Shiki platform decisions use the next zero-padded SADR number.\n+- New target product decisions use the target's own ADR sequence.\n+- The canonical list is the files in `docs/adr/`; no hand-maintained index is\n+  authoritative.\n+- Use [`template.md`](template.md) as the starting point and choose the filename\n+  namespace that owns the decision.\n+- Record a decision when it is hard to reverse, surprising without context, and\n+  a real tradeoff. Do not write a decision record for routine or easily\n+  reversible choices.\n \n-- [0001 \u2014 Keep Shiki As The Platform Name](0001-keep-shiki-as-platform-name.md)\n-- [0002 \u2014 Use AGENTS As The Shared Agent Constitution](0002-use-agents-as-shared-constitution.md)\n-- [0003 \u2014 Require Skill Gate For MergeGate Readiness](0003-require-skill-gate-for-mergegate-readiness.md)\n-- [0004 \u2014 Use GitHub First With A Repository-Local Shiki Mirror](0004-use-github-first-with-shiki-mirror.md)\n-- [0005 \u2014 Use Codex As Front And Claude Code Action As The GitHub Runtime](0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md)\n-- [0006 \u2014 Separate Codex Implementation, CCA Completion, And MergeGate Authorization](0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md)\n+Using the directory as the source of truth lets independent changes add records\n+without contending on an enumerated index.\n \n-## Adding A New ADR\n+## Adding A Shiki SADR\n \n-1. Copy `template.md` to `docs/adr/NNNN-<short-slug>.md`.\n+1. Copy `template.md` to `docs/adr/SADR-NNNN-<short-slug>.md`.\n 2. Fill in the title, status, context, decision, and consequences.\n-3. Set the status to `Proposed`, then `Accepted` once the decision is approved.\n-4. Open a PR following `CONTRIBUTING.md`. You do not need to edit this index for\n-   the ADR to be discoverable via `docs/adr/*.md`.\n+3. Set the status to `Proposed`, then `Accepted` once the proper authority has\n+   approved the decision.\n+4. Open a PR following `CONTRIBUTING.md`.\n+\n+## Adding A Target ADR\n+\n+1. Copy `template.md` to `docs/adr/NNNN-<short-slug>.md` using the target's own\n+   sequence.\n+2. Cite it as `ADR NNNN`; do not prefix its filename with `SADR-`.\ndiff --git a/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md b/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\nnew file mode 100644\nindex 0000000000000000000000000000000000000000..7455ce1f3bed02ddddb851ac21a1d1a648c9aeba\n--- /dev/null\n+++ b/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\n@@ -0,0 +1,55 @@\n+# SADR-0019: Separate Shiki Platform SADRs From Target ADRs\n+\n+## Status\n+\n+Accepted\n+\n+## Context\n+\n+Shiki installs its platform decision records into `docs/adr/` in every target\n+repository. Shiki and its targets previously used the same numeric\n+`NNNN-<slug>.md` namespace. A target that already owned ADRs 0013 through 0018\n+therefore acquired two records for each number during installation, making an\n+`ADR NNNN` citation ambiguous. Renumbering target decisions would rewrite target\n+history, while renumbering Shiki decisions per target would destroy their stable\n+platform identity.\n+\n+Upgrades also need to remove the former Shiki-owned numeric paths without ever\n+mistaking a target-owned ADR for platform content. The install stamp already\n+records exact shipped paths and content digests, so it can prove ownership only\n+when both still match.\n+\n+## Decision\n+\n+Shiki platform decisions use the `SADR` namespace:\n+\n+- records are named `docs/adr/SADR-NNNN-<slug>.md`;\n+- live platform citations use `SADR-NNNN`;\n+- target product decisions retain `docs/adr/NNNN-<slug>.md` and `ADR NNNN`;\n+- the reference validator resolves the two namespaces independently, so the same\n+  four-digit number may exist once in each namespace.\n+\n+Before a forced upgrade writes anything, the installer inventories the exact\n+former numeric Shiki ADR paths. It may delete a legacy path only when the\n+existing install stamp contains that exact path and its recorded SHA-256 digest\n+matches the file currently on disk. All ownership blockers are reported\n+together and abort the upgrade before its first write. A non-forced install\n+never performs this cleanup.\n+\n+Historical `.shiki/` evidence remains unchanged because it records the namespace\n+that existed when that evidence was produced.\n+\n+## Consequences\n+\n+- Targets may use any ADR number without colliding with Shiki's platform SADRs.\n+- Fresh installs preserve target ADR bytes and install a stable SADR set.\n+- Forced upgrades fail closed when legacy ownership cannot be proven, and a\n+  successful upgrade refreshes the stamp without removed legacy paths.\n+- Current governance, packaging, workflow, prompt, script, and skill references\n+  must move with the platform records.\n+\n+## Alternatives Considered\n+\n+- Collision-only refusal leaves affected targets permanently unupgradable.\n+- Renumbering target ADRs mutates target-owned history.\n+- Per-target Shiki renumbering makes platform citations non-portable.\ndiff --git a/docs/adr/template.md b/docs/adr/template.md\nindex a92a97daddd0b00ab9dd626cc8d86942da2eafed..17dd79175b5fee2105935557f691b40cf9492942\n--- a/docs/adr/template.md\n+++ b/docs/adr/template.md\n@@ -1,14 +1,14 @@\n-# NNNN: <Short Decision Title>\n+# <SADR-NNNN or NNNN>: <Short Decision Title>\n \n ## Status\n \n-Proposed | Accepted | Superseded by ADR-NNNN\n+Proposed | Accepted | Superseded by SADR-NNNN or target ADR NNNN\n \n ## Context\n \n What is the situation, constraint, or force that requires a decision? Reference\n-relevant domain terms (`CONTEXT.md`), prior ADRs, and code reality. State the\n-problem, not the solution.\n+relevant domain terms (`CONTEXT.md`), prior Shiki SADRs or target ADRs, and code\n+reality. State the problem, not the solution.\n \n ## Decision\n \ndiff --git a/docs/agents/autonomous-loop-e2e-acceptance.md b/docs/agents/autonomous-loop-e2e-acceptance.md\nindex bbf84c6081987f159ac6ad9ecf3c82b7aa72e7e8..5c13c1d02a05a90b0dc46cbef7c95a9846d0c237\n--- a/docs/agents/autonomous-loop-e2e-acceptance.md\n+++ b/docs/agents/autonomous-loop-e2e-acceptance.md\n@@ -40,8 +40,8 @@\n 3. `gh auth status` is logged in with merge permission on the repo.\n 4. Branch protection + required checks are configured exactly as the goal-loop\n    required-check set expects (see `docs/agents/branch-protection-smoke.md`).\n-5. The repo `.shiki/policy` permits autonomous low/medium-risk merges (ADR\n-   0008/0009); the MergeGate policy check is the recorded-authority gate.\n+5. The repo `.shiki/policy` permits autonomous low/medium-risk merges\n+   (SADR-0008/0009); the MergeGate policy check is the recorded-authority gate.\n \n The operator's only actions are: register the task and start the loop. **No\n manual pre-clearing** means after `shiki loop run` starts, the operator does not\ndiff --git a/docs/agents/bootstrap-command.md b/docs/agents/bootstrap-command.md\nindex 05f7f203a52cf412b9f97084a6e131ed0f785afd..f7aa0b734dfebee4a3b139335b66ee3415f48713\n--- a/docs/agents/bootstrap-command.md\n+++ b/docs/agents/bootstrap-command.md\n@@ -189,7 +189,9 @@\n ```\n \n Use this only for tests, fixtures, or explicit local-only template inspection.\n-Use `--force` only when you intentionally want to overwrite existing target files.\n+Use `--force` only when you intentionally want to refresh the target template.\n+Legacy numeric Shiki decision cleanup is stamp-and-digest authorized before any\n+write; target ADR paths are not cleanup candidates.\n \n ## Slash Command\n \ndiff --git a/docs/agents/checklists.md b/docs/agents/checklists.md\nindex 4a7f03ade346f53a1c1d82981f3fd744f9ab4547..0c5ec6c53dc7197b7b7a7d18e667b325babd6c45\n--- a/docs/agents/checklists.md\n+++ b/docs/agents/checklists.md\n@@ -32,18 +32,18 @@\n | ID | Item | Blocking |\n | --- | --- | --- |\n | GD-01 | Plan was challenged against `CONTEXT.md`. | blocking for non-trivial Goals |\n-| GD-02 | Relevant ADRs were read or absence was recorded. | blocking for non-trivial Goals |\n+| GD-02 | Relevant SADRs and target ADRs were read or absence was recorded. | blocking for non-trivial Goals |\n | GD-03 | Ambiguous terms were resolved or converted to HITL blockers. | blocking |\n | GD-04 | Edge scenarios were considered. | advisory |\n | GD-05 | Code reality was checked when code could answer. | blocking when applicable |\n | GD-06 | New domain terms were added to `CONTEXT.md` when needed. | blocking when applicable |\n-| GD-07 | ADR was created or explicitly skipped using the ADR criteria. | blocking when applicable |\n+| GD-07 | A SADR or target ADR was created or explicitly skipped using the decision-record criteria. | blocking when applicable |\n \n ## CI \u2014 Context & Impact Checklist\n \n | ID | Item | Blocking |\n | --- | --- | --- |\n-| CI-01 | Relevant docs and ADRs identified. | blocking |\n+| CI-01 | Relevant docs, SADRs, and target ADRs identified. | blocking |\n | CI-02 | Relevant modules, interfaces, seams, callers, and tests identified. | blocking |\n | CI-03 | Dependencies and blocked-by relationships identified. | blocking |\n | CI-04 | Candidate locks identified. | blocking |\n@@ -62,7 +62,7 @@\n | PRD-04 | Implementation decisions are recorded without volatile code detail. | blocking |\n | PRD-05 | Testing decisions are recorded. | blocking |\n | PRD-06 | Out-of-scope items are explicit. | blocking |\n-| PRD-07 | Relevant glossary/ADR links are present when applicable. | blocking |\n+| PRD-07 | Relevant glossary/SADR/target-ADR links are present when applicable. | blocking |\n \n ## SF \u2014 Spec Freeze Checklist\n \n@@ -98,7 +98,7 @@\n | H-02 | Branch/worktree target is present. | blocking |\n | H-03 | Scope and non-goals are present. | blocking |\n | H-04 | Dependencies and locks are present. | blocking |\n-| H-05 | Relevant docs, ADRs, modules, and tests are referenced. | blocking |\n+| H-05 | Relevant docs, SADRs, target ADRs, modules, and tests are referenced. | blocking |\n | H-06 | Required skills are stated. | blocking |\n | H-07 | TDD expectations are stated. | blocking when behavior work |\n | H-08 | Acceptance criteria are included. | blocking |\ndiff --git a/docs/agents/codex-handoff.md b/docs/agents/codex-handoff.md\nindex ede468f47cb277f69a857e8c45df703e4ed0151d..6f97bdffb67e44d7edea2c71e01ea28933c4eb6c\n--- a/docs/agents/codex-handoff.md\n+++ b/docs/agents/codex-handoff.md\n@@ -46,7 +46,7 @@\n \n ## Context\n - Domain terms:\n-- Relevant ADRs:\n+- Relevant SADRs and target ADRs:\n - Relevant docs:\n - Relevant modules/interfaces/seams:\n - Similar tests/prior art:\ndiff --git a/docs/agents/completion-check-agent.md b/docs/agents/completion-check-agent.md\nindex 13e4c035db35025f4e3cdcc5adfb24314ac4d521..61bfa1218cf36ca605250c91919e620f33238aa0\n--- a/docs/agents/completion-check-agent.md\n+++ b/docs/agents/completion-check-agent.md\n@@ -32,7 +32,7 @@\n - `AGENTS.md`\n - `CLAUDE.md`\n - `CONTEXT.md`\n-- relevant ADRs\n+- relevant SADRs and target ADRs\n - parent Goal issue\n - PRD issue or document\n - task issue\ndiff --git a/docs/agents/decision-control.md b/docs/agents/decision-control.md\nindex 7d8ee5ab03a5ccbcbd04056ae61de03785a2a9c9..8c94ecda00fffaa0e92c325ab83aa9776bd1dac1\n--- a/docs/agents/decision-control.md\n+++ b/docs/agents/decision-control.md\n@@ -11,7 +11,7 @@\n | Decision | Owner | When | Required inputs | Output | Enforcement surface |\n | --- | --- | --- | --- | --- | --- |\n | Is the Goal clear enough to plan? | Planner + Guardian when needed | Goal intake | User request, existing docs, risk signals | Goal readiness verdict | Goal checklist, issue template, ledger |\n-| Must `grill-with-docs` run? | Planner / Skill Gate | Before PRD or issue split | Ambiguity, domain terms, ADR conflicts, unclear boundaries | Skill invocation or explicit skip rationale | Skill Gate checklist |\n+| Must `grill-with-docs` run? | Planner / Skill Gate | Before PRD or issue split | Ambiguity, domain terms, SADR/target-ADR conflicts, unclear boundaries | Skill invocation or explicit skip rationale | Skill Gate checklist |\n | Is a PRD required? | Planner | After Goal context stabilizes | Goal, `grill-with-docs` output, Context & Impact output | PRD required / not required | PRD checklist, CCA evidence check |\n | Can the spec be frozen? | Operator only | End of Requirements Definition, before issue split | PRD/requirements summary, scope inventory (SF-02), open-question list | `spec_freeze` block in the plan + ledger evidence | `require_grilled_plan` (plan ingest/run), SF checklist |\n | Can a frozen spec change? | Operator only (Spec Amendment) | When implementation contradicts the frozen spec | Contested decisions, scoped re-grill result | recorded amendment + re-stamped freeze | SF checklist, ledger evidence |\ndiff --git a/docs/agents/domain.md b/docs/agents/domain.md\nindex 258da8ea81e87ac0a6b0b5b5e5c2fde980eaabb7..d57b0cf17ff3e1fa9e6e5885372bf083d8295421\n--- a/docs/agents/domain.md\n+++ b/docs/agents/domain.md\n@@ -5,7 +5,9 @@\n ## Layout\n \n - `CONTEXT.md` defines Shiki language. Keep it implementation-free.\n-- `docs/adr/` records hard-to-reverse decisions.\n+- `docs/adr/SADR-NNNN-*.md` records Shiki platform decisions.\n+- `docs/adr/NNNN-*.md` records target product decisions without sharing the\n+  SADR namespace.\n - `AGENTS.md` is the runtime-neutral agent constitution.\n - `CLAUDE.md` is the Claude Code wrapper around `AGENTS.md`.\n - `.shiki/` is the Target Repository mirror for Goals, PRDs, plans, Task DAGs, contracts, locks, worktree records, ledger entries, CCA verdicts, repair packets, reports, and handoffs.\n@@ -15,6 +17,6 @@\n \n - Use Shiki terms exactly when they exist.\n - Add glossary terms to `CONTEXT.md` only when the term is part of the domain, not a generic programming concept.\n-- Create ADRs only for hard-to-reverse, surprising, tradeoff-based decisions.\n+- Create SADRs or target ADRs only for hard-to-reverse, surprising, tradeoff-based decisions.\n - Keep product-specific language in the Target Repository, not in Shiki Template docs.\n-- If an ADR conflicts with a proposed implementation, surface the conflict before editing.\n+- If a SADR or target ADR conflicts with a proposed implementation, surface the conflict before editing.\ndiff --git a/docs/agents/execution-control.md b/docs/agents/execution-control.md\nindex ad8ee3cdb36be56fedc1f46408ca92b387d74047..bc63c3a73661d12b567016ba299f1a6b66bf9088\n--- a/docs/agents/execution-control.md\n+++ b/docs/agents/execution-control.md\n@@ -63,7 +63,7 @@\n | Branch name | Orchestrator | Orchestrator | Before implementer dispatch | branch policy | branch ref in task/ledger |\n | Lock reservation | Context & Impact | MergeGate | Before dispatch and before merge | `.shiki/locks/`, labels, PR metadata | lock id, owner, scope |\n | CI profile | Planner | MergeGate | During issue triage, rechecked at PR | labels, `.shiki/policy.yaml`, workflow matrix | required check list |\n-| Required status checks | Repo maintainer / Guardian | GitHub branch protection / rulesets | Repository setup, updated via ADR | GitHub settings | branch/ruleset config, ADR |\n+| Required status checks | Repo maintainer / Guardian | GitHub branch protection / rulesets | Repository setup, updated via target ADR | GitHub settings | branch/ruleset config, target ADR |\n | CCA judgment | CCA | GitHub required check / MergeGate | Every non-draft PR update | GitHub Action check | structured CCA verdict |\n | Repair assignment | CCA | MergeGate | After failed CCA/CI/review | repair packet, issue/PR comment | repair id and attempt count |\n | Merge readiness | MergeGate | GitHub branch protection / Guardian if needed | After CCA complete | required checks, review rules, rulesets | mergegate check output |\ndiff --git a/docs/agents/guardian-policy.md b/docs/agents/guardian-policy.md\nindex 4147c7adf05a47adb4fd24ada1a1c2e1f2ba6d2d..a20d9cc6c00a569bb057b9ca8d6b51c7fc4a9847\n--- a/docs/agents/guardian-policy.md\n+++ b/docs/agents/guardian-policy.md\n@@ -98,8 +98,8 @@\n `origin/<default_branch>` and reuses each job's existing base fetch; a shallow\n (`--depth`) or second base fetch would make the proof unresolvable and silently\n disable the carry, so neither is permitted. CCA reads `guardian_comment_carried` as\n-a recorded authority in the deterministic signal and does not re-derive it. See ADR\n-0018 for the full decision, the rejected diff/patch-equality and path-disjointness\n+a recorded authority in the deterministic signal and does not re-derive it. See\n+SADR-0018 for the full decision, the rejected diff/patch-equality and path-disjointness\n alternatives, and the accepted residual.\n \n ## External AI Guardian Review (SADR-0010)\ndiff --git a/docs/agents/implementation-policy.md b/docs/agents/implementation-policy.md\nindex c754144a9fb5bf4a38aa82a60e1670c59e626b44..85c37281bcad196c6b7b02343faf00b03cd9c57a\n--- a/docs/agents/implementation-policy.md\n+++ b/docs/agents/implementation-policy.md\n@@ -31,10 +31,13 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n ## Non-Negotiables\n \n 1. **Planning precedes implementation.** Non-trivial Goals start with `grill-with-docs` and Context & Impact.\n-2. **Domain language is controlled.** Terms must be checked against `CONTEXT.md`; hard-to-reverse tradeoffs must be checked against ADRs.\n+2. **Domain language is controlled.** Terms must be checked against `CONTEXT.md`; hard-to-reverse tradeoffs must be checked against Shiki SADRs and target ADRs.\n 3. **PRD precedes issue decomposition.** `to-prd` is used once the context is settled enough to become durable intent.\n 4. **Issues are vertical slices.** `to-issues` must produce independently grabbable tracer-bullet tasks, not horizontal layer tickets.\n 5. **Triage controls dispatch.** Only `ready-for-agent` / AFK tasks may be assigned to Codex without human input.\n@@ -95,19 +98,19 @@\n Process:\n \n - Challenge terms against `CONTEXT.md`.\n-- Check ADRs before reopening decisions.\n+- Check SADRs and target ADRs before reopening decisions.\n - Inspect code when code can answer a question.\n - Ask one question at a time when the operator is available.\n - Provide a recommended answer with each question.\n - Record resolved terms in `CONTEXT.md`.\n-- Create ADRs only for hard-to-reverse, surprising, tradeoff-based decisions.\n+- Create SADRs or target ADRs only for hard-to-reverse, surprising, tradeoff-based decisions.\n - Convert unresolved load-bearing questions into HITL blockers.\n \n Outputs:\n \n - Resolved design-tree decisions.\n - Updated glossary terms when needed.\n-- ADRs when justified.\n+- SADRs or target ADRs when justified.\n - HITL blockers when decisions remain unresolved.\n \n ## Phase 3 \u2014 Context & Impact\n@@ -117,7 +120,7 @@\n \n Outputs must include:\n \n-- Relevant documents and ADRs.\n+- Relevant documents, SADRs, and target ADRs.\n - Relevant modules, interfaces, seams, callers, and tests.\n - Candidate locks.\n - Dependencies.\n@@ -140,7 +143,7 @@\n - Testing decisions.\n - Out of scope.\n - Further notes.\n-- Links to relevant Goal, domain terms, and ADRs.\n+- Links to relevant Goal, domain terms, SADRs, and target ADRs.\n \n Phase 4 ends with Spec Freeze: the operator explicitly approves the PRD, the\n plan records a `spec_freeze` block (status, approved_by, source), and the SF\n@@ -240,7 +243,7 @@\n - Dependencies and locks.\n - Risk and Guardian requirements.\n - Ledger completeness.\n-- Documentation and ADR updates.\n+- Documentation and SADR/target-ADR updates.\n - Scope drift.\n \n CCA emits one of:\n@@ -301,7 +304,7 @@\n | Evidence failure | Behavior might be done, but proof is missing. | Add checks, PR evidence, ledger entry, or artifacts. |\n | Scope drift | PR changes unrelated behavior. | Revert unrelated changes or split into new Goal. |\n | Dependency block | Upstream task or lock not complete. | Block until dependency resolves. |\n-| Architecture block | Decision needs ADR or deep-module discussion. | `grill-with-docs` or `improve-codebase-architecture`. |\n+| Architecture block | Decision needs a SADR, target ADR, or deep-module discussion. | `grill-with-docs` or `improve-codebase-architecture`. |\n | Risk block | Security/production/policy change needs human approval. | Guardian decision. |\n \n ## Phase 5.5 \u2014 Execution Decision Control\ndiff --git a/docs/agents/runtime-auth-model.md b/docs/agents/runtime-auth-model.md\nindex 6a2e1a47e81ea89b5c68a210ec07772a9374babc..3c54748d24233dbd0ba8473950282547ef4ce935\n--- a/docs/agents/runtime-auth-model.md\n+++ b/docs/agents/runtime-auth-model.md\n@@ -148,7 +148,7 @@\n \n API-key based runners may be added by a target repository as an explicit extension. They are not the default Shiki template path.\n \n-If a repo opts into API-key mode, record the exception in an ADR and update the repo's `AGENTS.md`, `.shiki/config.yaml`, and workflow permissions.\n+If a repo opts into API-key mode, record the target-specific exception in a target ADR and update the repo's `AGENTS.md`, `.shiki/config.yaml`, and workflow permissions.\n \n ## GitHub Actions Runtime Compatibility\n \ndiff --git a/docs/agents/skill-gate.md b/docs/agents/skill-gate.md\nindex c50398b0d7c60a8598f2a5c88a10015730dced13..3acffe6444617bb1d54b77d4b4708158ec735bc6\n--- a/docs/agents/skill-gate.md\n+++ b/docs/agents/skill-gate.md\n@@ -9,7 +9,7 @@\n | Skill | Trigger | Expected output |\n | --- | --- | --- |\n | `setup-matt-pocock-skills` | First configuring a repo for these skills, or when issue tracker, triage labels, or domain docs are missing. | `docs/agents/` configuration and an `Agent skills` block in `AGENTS.md` or `CLAUDE.md`. |\n-| `grill-with-docs` | Ambiguous plan, unclear terminology, boundary decision, tradeoff, or ADR-worthy design choice. | Clarified decision tree, recommended answers, updates to `CONTEXT.md` or ADRs when decisions crystallize. |\n+| `grill-with-docs` | Ambiguous plan, unclear terminology, boundary decision, tradeoff, or decision-record-worthy design choice. | Clarified decision tree, recommended answers, updates to `CONTEXT.md`, SADRs, or target ADRs when decisions crystallize. |\n | `zoom-out` | Agent lacks a higher-level architectural map. | Map of relevant modules, callers, seams, and domain concepts. |\n | `to-prd` | Settled context should become a PRD. | PRD published to the issue tracker with problem, solution, user stories, decisions, testing, and out-of-scope notes. |\n | `to-issues` | Goal, PRD, or plan must become implementation tickets. | Vertical-slice issues with acceptance criteria, dependencies, and AFK/HITL classification. |\n@@ -17,7 +17,7 @@\n | `tdd` | Feature work or bug fix has observable behavior that can be specified with tests. | Red-green-refactor loop through public interfaces, one vertical slice at a time. |\n | `code-review` | The TDD loop is green and the implementer is about to create or update a PR. Default for every implementation task. | Pre-PR self-review of the task diff with fixes applied on the task branch; findings and resolutions recorded as a ledger entry naming the skill and a `## Pre-PR code review` PR body section. |\n | `diagnose` | Hard bug, failing check, regression, flaky behavior, or performance problem. | Reproduction loop, ranked hypotheses, instrumentation, fix, regression test, post-mortem. |\n-| `improve-codebase-architecture` | Structural friction, testability, AI-navigability, deep-module opportunity, or refactor request. | Architecture candidates and deepening recommendations informed by `CONTEXT.md` and ADRs. |\n+| `improve-codebase-architecture` | Structural friction, testability, AI-navigability, deep-module opportunity, or refactor request. | Architecture candidates and deepening recommendations informed by `CONTEXT.md`, SADRs, and target ADRs. |\n | `prototype` | A throwaway logic or UI prototype is needed to answer a design question. | Clearly marked disposable prototype and captured decision. |\n \n ## MergeGate Interaction\ndiff --git a/docs/operations.md b/docs/operations.md\nindex cf1f452d4c589e38ccd58553bda0ad8684b51eb7..ace48a90ffbec9e5c0a7113bef16a4858bc44b3f\n--- a/docs/operations.md\n+++ b/docs/operations.md\n@@ -89,6 +89,16 @@\n      target history, brought forward by `shiki migrate`, not a template.\n    - **Everything else** (CLI scripts, workflows, schemas, docs) is overwritten.\n \n+   A forced upgrade also migrates the former numeric Shiki decision paths to\n+   `SADR-NNNN-*`. Before the first write, the installer inventories the exact\n+   legacy Shiki paths and authorizes each deletion only when the existing\n+   `.shiki/install-stamp.json` names that path and its SHA-256 digest still\n+   matches the file. An absent, unreadable, malformed, incomplete, or\n+   mismatching stamp reports every blocked path and stops with no filesystem\n+   change. Target-owned `NNNN-*` ADRs are not cleanup candidates. A non-force\n+   install deletes nothing and retains any still-matching legacy stamp entries\n+   so a later forced upgrade can prove ownership.\n+\n    If the target has pending migrations, `--force` refuses and writes nothing \u2014\n    apply migrations first (step 3), then re-run. The run ends with a single\n    summary of every `.new` written, so a half-upgrade is visible.\ndiff --git a/docs/releasing.md b/docs/releasing.md\nindex 10d73828e7dbd64e9d9f6b83bc84d010537ce62a..aafeee8c4d441e1e427efee2ce4f81a647f020e1\n--- a/docs/releasing.md\n+++ b/docs/releasing.md\n@@ -67,7 +67,7 @@\n CLI in `scripts/`. There are no third-party Python runtime dependencies. The CLI\n is intentionally **not** exposed as a `[project.scripts]` console entry point\n because `scripts/shiki.py` is a dependency-free shim and `scripts/` is not\n-packaged as an importable distribution (see the ADR for the rationale).\n+packaged as an importable distribution (see SADR-0007 for the rationale).\n \n Supported install paths:\n \n@@ -155,6 +155,15 @@\n    [docs/operations.md](operations.md#the-new-convention). If the target has\n    pending migrations, `--force` refuses and writes nothing; apply migrations\n    (step 2) first, then re-run.\n+\n+   The SADR namespace upgrade has an additional pre-write gate: each former\n+   numeric Shiki decision path is removed only when the existing install stamp\n+   contains that exact path and a digest matching its current bytes. The\n+   installer aggregates absent/unreadable/malformed-stamp, missing-digest, and\n+   digest-mismatch blockers before raising, so refusal leaves the target tree\n+   byte-identical. Numeric target ADRs are preserved. A successful forced\n+   upgrade installs and stamps `SADR-*` paths and drops the removed legacy paths\n+   from the refreshed stamp.\n 2. Apply repository-local `.shiki/` state migrations with the migrate command:\n \n    ```bash\ndiff --git a/docs/usage.md b/docs/usage.md\nindex b83407ee3970dd395dd893010bc5dbe8bad26051..a130ef6e40dfd8169a5bc701339351097df1a09f\n--- a/docs/usage.md\n+++ b/docs/usage.md\n@@ -67,8 +67,10 @@\n shiki install-target /path/to/target-repo --local-only\n ```\n \n-Use `--force` only when you intentionally want to overwrite existing target\n-files.\n+Use `--force` only when you intentionally want to refresh the target template.\n+For the SADR namespace migration, `--force` deletes only exact legacy Shiki\n+decision paths whose existing install-stamp digest still matches; any ownership\n+blocker stops before the first write. Numeric target ADRs are never candidates.\n \n ## `shiki bootstrap-platform` \u2014 publish the Shiki platform repo\n \ndiff --git a/scripts/guardian_approval_signal.py b/scripts/guardian_approval_signal.py\nindex d059f92b2c82aa2b5d6ca421365537a87886c1cb..16924892714f4f5b9303c0ef0e325a7e70075630\n--- a/scripts/guardian_approval_signal.py\n+++ b/scripts/guardian_approval_signal.py\n@@ -199,8 +199,8 @@\n ) -> Any:\n     \"\"\"Evaluate SADR-0015 Contract Approval for the CCA Guardian signal.\n \n-    The SAME OR that MergeGate applies must reach this deterministic signal: ADR\n-    0015 is explicit that without it, a contract-approved PR still returns\n+    The SAME OR that MergeGate applies must reach this deterministic signal:\n+    SADR-0015 is explicit that without it, a contract-approved PR still returns\n     ``needs_guardian``, the CCA job fails, and the MergeGate policy check never\n     runs \u2014 the gate this change moves would never be reached. This resolves the\n     task the SAME way MergeGate does (the first Shiki task id in the PR body),\ndiff --git a/scripts/shiki_contract_approval.py b/scripts/shiki_contract_approval.py\nindex 941bcb96fdd8b9a85bc0c4f0f04d5dad9d04839c..4a4b418f6527413161db82670bec857a043360e2\n--- a/scripts/shiki_contract_approval.py\n+++ b/scripts/shiki_contract_approval.py\n@@ -142,7 +142,7 @@\n     changed_files_status: Any,\n     registration: Any,\n ) -> ContractApprovalResult:\n-    \"\"\"Whether this PR carries an SADR-0015 Contract Approval.\n+    \"\"\"Whether this PR carries a SADR-0015 Contract Approval.\n \n     The four conditions are evaluated IN ORDER, each failure recorded as a\n     distinct reason string:\ndiff --git a/scripts/shiki_guardian.py b/scripts/shiki_guardian.py\nindex a4ecb79bbda55fd5c8b06adfae2eb99760a03c45..4dd99bbe714e26f421fa19abac7a34ae1b195ecc\n--- a/scripts/shiki_guardian.py\n+++ b/scripts/shiki_guardian.py\n@@ -12,7 +12,7 @@\n # The carry re-derives its proof from git in-process on every judging invocation;\n # it is never read from an artifact, so there is nothing to go stale or be forged.\n # shiki_sync_proof is the ONLY guardian-family module that runs git, and every one\n-# of its invocations is read-only and local (ADR: prove-from-git base sync).\n+# of its invocations is read-only and local (SADR: prove-from-git base sync).\n from shiki_sync_proof import verify_pure_base_sync\n \n GUARDIAN_POLICY_PATH = \".shiki/guardian-policy.json\"\ndiff --git a/scripts/shiki_guardian_review.py b/scripts/shiki_guardian_review.py\nindex dd0f104d457899e0ba5ff1e162011c1e90b6f92b..e10ef58c046324e7ea3a3392feb3048ca77414ce\n--- a/scripts/shiki_guardian_review.py\n+++ b/scripts/shiki_guardian_review.py\n@@ -159,7 +159,7 @@\n     docs_only = _is_docs_only(changed_files)\n     if docs_only and matched == 0:\n         focus.append(\n-            \"Docs/ADR-only change: confirm the change is genuinely documentation/decision-only, introduces no behavior or contract drift, and stays consistent with the constitution and existing ADRs.\"\n+            \"Docs/decision-record-only change: confirm the change is genuinely documentation/decision-only, introduces no behavior or contract drift, and stays consistent with the constitution and existing SADRs and target ADRs.\"\n         )\n \n     # Conservative augmentation: unknown (no category and not docs-only), mixed\ndiff --git a/scripts/shiki_installer.py b/scripts/shiki_installer.py\nindex be064ab7a29ff6836f7f7a1d70500a43e0d11d7b..9fd8eefd137ce7003dad5c0dcb2e48e2cd47ab32\n--- a/scripts/shiki_installer.py\n+++ b/scripts/shiki_installer.py\n@@ -118,6 +118,7 @@\n # what lets a target carry it without a governance-schema change.\n INSTALL_STAMP_PATH = \".shiki/install-stamp.json\"\n INSTALL_STAMP_VERSION = 1\n+LEGACY_SHIKI_SADR_MAX = 18\n \n # Under --force the shipped surface splits three ways, so nothing is lost and\n # nothing is silently stale.\n@@ -237,9 +238,98 @@\n         )\n \n \n+def legacy_shiki_adr_paths() -> tuple[str, ...]:\n+    \"\"\"Former numeric paths for Shiki SADR-0001 through SADR-0018.\n+\n+    The authoritative slugs come from the current SADR filenames. SADR-0019 is\n+    the namespace decision itself and never shipped under a numeric legacy path.\n+    \"\"\"\n+    paths: list[str] = []\n+    for sadr in sorted((ROOT / \"docs\" / \"adr\").glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\")):\n+        number = sadr.name.split(\"-\", 2)[1]\n+        if int(number) <= LEGACY_SHIKI_SADR_MAX:\n+            paths.append(f\"docs/adr/{sadr.name.removeprefix('SADR-')}\")\n+    return tuple(paths)\n+\n+\n+def preflight_legacy_sadr_cleanup(target: Path, *, force: bool) -> tuple[str, ...]:\n+    \"\"\"Authorize exact legacy Shiki ADR removals before the first write.\n+\n+    A non-force install never removes existing files. A forced install needs no\n+    stamp when no legacy Shiki path is present. When legacy paths do exist, each\n+    must be named in a readable install stamp and its current bytes must match\n+    the recorded digest.\n+    \"\"\"\n+    if not force:\n+        return ()\n+    present = tuple(\n+        relative\n+        for relative in legacy_shiki_adr_paths()\n+        if (target / relative).exists() or (target / relative).is_symlink()\n+    )\n+    if not present:\n+        return ()\n+\n+    stamp_path = target / INSTALL_STAMP_PATH\n+    try:\n+        raw = stamp_path.read_text(encoding=\"utf-8\")\n+        stamp = json.loads(raw)\n+    except FileNotFoundError:\n+        detail = \"install stamp is absent\"\n+        blockers = [f\"{relative}: {detail}\" for relative in present]\n+    except (OSError, UnicodeDecodeError) as error:\n+        detail = f\"install stamp is unreadable ({error})\"\n+        blockers = [f\"{relative}: {detail}\" for relative in present]\n+    except json.JSONDecodeError as error:\n+        detail = f\"install stamp is malformed ({error})\"\n+        blockers = [f\"{relative}: {detail}\" for relative in present]\n+    else:\n+        if not isinstance(stamp, dict):\n+            blockers = [\n+                f\"{relative}: install stamp is malformed (expected an object)\"\n+                for relative in present\n+            ]\n+        else:\n+            digests = stamp.get(\"digests\")\n+            blockers = []\n+            if not isinstance(digests, dict):\n+                blockers.extend(\n+                    f\"{relative}: install stamp has no digest for this path\"\n+                    for relative in present\n+                )\n+            else:\n+                for relative in present:\n+                    expected = digests.get(relative)\n+                    if not isinstance(expected, str) or not expected:\n+                        blockers.append(\n+                            f\"{relative}: install stamp has no digest for this path\"\n+                        )\n+                        continue\n+                    try:\n+                        actual = _sha256_file(target / relative)\n+                    except OSError as error:\n+                        blockers.append(\n+                            f\"{relative}: legacy path is unreadable ({error})\"\n+                        )\n+                        continue\n+                    if actual != expected:\n+                        blockers.append(\n+                            f\"{relative}: digest mismatch (stamp {expected}, current {actual})\"\n+                        )\n+\n+    if blockers:\n+        joined = \"\\n\".join(f\"- {blocker}\" for blocker in blockers)\n+        raise ShikiError(\n+            \"legacy Shiki ADR cleanup is not authorized:\\n\"\n+            f\"{joined}\\nNo files were written.\"\n+        )\n+    return present\n+\n+\n def install_template(target: Path, *, force: bool, validate: bool) -> list[NewFileNote]:\n     # Preconditions first: never rewrite the tree of a target that cannot be\n     # upgraded cleanly (see refuse_pending_migrations).\n+    legacy_cleanup = preflight_legacy_sadr_cleanup(target, force=force)\n     refuse_pending_migrations(target)\n \n     # Resolve the target's required CODEOWNERS owner once from its\n@@ -250,6 +340,10 @@\n     # verbatim. See ``codeowners_required_owner``.\n     codeowners_owner = codeowners_required_owner(target)\n \n+    for relative in legacy_cleanup:\n+        (target / relative).unlink()\n+        info(f\"removed stamped legacy Shiki ADR: {target / relative}\")\n+\n     notes: list[NewFileNote] = []\n     for relative in TEMPLATE_PATHS:\n         source = ROOT / relative\n@@ -647,6 +741,22 @@\n     digests = target_stamp_digests(target)\n     existing = read_install_stamp(target)\n     if existing is not None:\n+        # A non-force install deliberately leaves legacy Shiki ADRs in place.\n+        # Preserve only still-matching ownership proof so a later forced\n+        # upgrade can authorize cleanup; never carry a missing or stale digest.\n+        existing_digests = existing.get(\"digests\")\n+        if isinstance(existing_digests, dict):\n+            for relative in legacy_shiki_adr_paths():\n+                expected = existing_digests.get(relative)\n+                path = target / relative\n+                if not isinstance(expected, str) or not path.is_file():\n+                    continue\n+                try:\n+                    actual = _sha256_file(path)\n+                except OSError:\n+                    continue\n+                if actual == expected:\n+                    digests[relative] = actual\n         if _incoming_is_older(commit, existing.get(\"platform_commit\")):\n             warn(\n                 \"kept existing install stamp: incoming install \"\ndiff --git a/scripts/shiki_loop.py b/scripts/shiki_loop.py\nindex 3611641da8aeb5038b1da90e3114b09a473ec6d8..84caa491b9cad0edc4176f209c13e9221e975681\n--- a/scripts/shiki_loop.py\n+++ b/scripts/shiki_loop.py\n@@ -424,7 +424,7 @@\n         # unresolvable CCA verdict has already produced its terminal stop and is never\n         # laundered into a head-moving sync (SADR-0010). It also never fires while the\n         # policy (Guardian) gate is red \u2014 for ANY reason, not only when it is the sole\n-        # red check \u2014 so a head-bound Guardian approval, or an SADR-0015\n+        # red check \u2014 so a head-bound Guardian approval, or a SADR-0015\n         # contract-approval carry that base movement defeated (the policy gate compares\n         # base and head task governance directly), is never silently restored by\n         # adopting base wholesale. It is bounded by a durable per-task counter so the\ndiff --git a/scripts/shiki_tasks.py b/scripts/shiki_tasks.py\nindex aec3809f24b4a0c8c96fc50971cff9ae9ad30381..d531052e9d9717058d4631483cefab44c26a65c1\n--- a/scripts/shiki_tasks.py\n+++ b/scripts/shiki_tasks.py\n@@ -821,7 +821,7 @@\n         \"prompt\": prompt,\n         \"entry_skill\": \"grill-with-docs\",\n         \"required_next_steps\": [\n-            \"Run grill-with-docs until terminology, boundaries, risks, and ADR-worthy decisions are settled.\",\n+            \"Run grill-with-docs until terminology, boundaries, risks, and decision-record-worthy choices are settled.\",\n             \"Obtain the operator's explicit approval of the PRD/requirements (Spec Freeze).\",\n             \"Write a machine-readable plan JSON with grill_with_docs.status=complete and spec_freeze.status=frozen.\",\n             \"Run shiki plan ingest --plan-file PLAN.json.\",\ndiff --git a/scripts/test_shiki_goal_loop.sh b/scripts/test_shiki_goal_loop.sh\nindex 753c7b516e10f0be3bc92a4bd0eb1fa699776ae6..8f3820a0a079b3044424cfe1a6c6b7fe68896e67\n--- a/scripts/test_shiki_goal_loop.sh\n+++ b/scripts/test_shiki_goal_loop.sh\n@@ -231,8 +231,8 @@\n \n # High/critical risk merges autonomously when all required checks are green:\n # the \"MergeGate policy check\" required check IS the Guardian gate (it enforces\n-# guardian-policy.json \u2014 human approval OR external AI guardian review, ADR\n-# 0010), so green checks mean a recorded authority approved. The fake gh returns\n+# guardian-policy.json \u2014 human approval OR external AI guardian review,\n+# SADR-0010), so green checks mean a recorded authority approved. The fake gh returns\n # green checks, simulating that the Guardian gate passed.\n python3 \"$ROOT/scripts/shiki.py\" goal create --target \"$TARGET\" --title \"Guardian gate\" --outcome \"High risk merges via policy gate\" >/tmp/shiki-goal-loop-guardian-goal.json\n GGOAL=\"$(json_get /tmp/shiki-goal-loop-guardian-goal.json goal_id)\"\ndiff --git a/tests/test_adr_references.py b/tests/test_adr_references.py\nindex 51706329c797d5dd9b47e082a333fa83b261d482..db6e693e35f67103935be34f1d9bb4ff7ee60395\n--- a/tests/test_adr_references.py\n+++ b/tests/test_adr_references.py\n@@ -1,7 +1,7 @@\n-\"\"\"Every ``ADR ####`` citation must resolve to exactly one decision record.\n+\"\"\"Every target ADR and Shiki SADR citation resolves in its own namespace.\n \n Background. ``scripts/mergegate_check.py`` justifies ``is_bookkeeping_closeout``\n-with \"ADR 0017\" nine times and ``scripts/guardian_approval_signal.py`` cites\n+with \"SADR-0017\" nine times and ``scripts/guardian_approval_signal.py`` cites\n 0016/0017 seven times, yet for a stretch the numbering in ``docs/adr/`` stopped\n at 0015: the authoring commits lived on a coordinator branch and no task ever\n declared ``docs/adr/...`` in its locks, so nothing carried the records to the\n@@ -11,11 +11,12 @@\n and the directory drifted with no signal.\n \n This is the same unbound-pair shape behind other silent failures: a reference\n-and its target with nothing asserting they agree. This test binds them. It scans\n-every ``scripts/*.py`` (plus ``docs/**/*.md`` and the root ``*.md`` governance\n-files, which are cheap) for the ``ADR ####`` pattern and asserts each cited\n-number resolves to exactly one ``docs/adr/####-*.md``. The converse is\n-deliberately NOT required: an ADR may exist without being cited.\n+and its target with nothing asserting they agree. This test binds them. Target\n+``ADR NNNN`` citations resolve only to ``docs/adr/NNNN-*.md``; Shiki\n+``SADR-NNNN`` citations resolve only to ``docs/adr/SADR-NNNN-*.md``. The scan\n+covers current governance docs, scripts, workflows, prompts, packaging metadata,\n+and repo-local Codex guidance while deliberately excluding historical\n+``.shiki`` evidence. The converse is not required: a record may be uncited.\n \n The suite is dependency free: standard-library ``unittest`` only.\n \"\"\"\n@@ -31,17 +32,18 @@\n \n REPO_ROOT = Path(__file__).resolve().parents[1]\n \n-# \"ADR 0016\" -- a four-digit citation. The trailing \\b keeps a five-digit token\n-# from being read as a truncated four-digit number.\n-_CITATION = re.compile(r\"\\bADR (\\d{4})\\b\")\n-# \"0016-locks-bound-product-paths.md\" -- the leading four digits name the record.\n-_ADR_FILENAME = re.compile(r\"^(\\d{4})-.*\\.md$\")\n+# ``ADR 0016`` / ``SADR-0016`` -- a namespace plus four-digit citation. The\n+# trailing ``\\b`` keeps a five-digit token from being read as a truncated one.\n+_CITATION = re.compile(r\"\\b(S?ADR)[ -](\\d{4})\\b\")\n+# Target ADRs retain the numeric filename; Shiki platform decisions use SADR.\n+_DECISION_FILENAME = re.compile(r\"^(?:(SADR)-)?(\\d{4})-.*\\.md$\")\n \n \n class Violation(NamedTuple):\n     \"\"\"A citation that does not resolve to exactly one decision record.\"\"\"\n \n     citing_file: str  # repo-relative path of the file that cites the ADR\n+    namespace: str  # \"ADR\" for target decisions; \"SADR\" for Shiki decisions\n     number: str  # the cited four-digit number, e.g. \"0016\"\n     line: int  # 1-based line of the citation\n     reason: str  # \"missing\" (zero matches) or \"ambiguous\" (two or more)\n@@ -49,44 +51,59 @@\n \n     @property\n     def expected_path(self) -> str:\n-        return f\"docs/adr/{self.number}-*.md\"\n+        prefix = \"SADR-\" if self.namespace == \"SADR\" else \"\"\n+        return f\"docs/adr/{prefix}{self.number}-*.md\"\n \n     def describe(self) -> str:\n         \"\"\"Name the citing file, the cited number, and the missing path.\"\"\"\n         if self.reason == \"missing\":\n             return (\n-                f\"{self.citing_file}:{self.line} cites ADR {self.number} \"\n+                f\"{self.citing_file}:{self.line} cites {self.namespace} {self.number} \"\n                 f\"but no document matches {self.expected_path}\"\n             )\n         joined = \", \".join(self.matches)\n         return (\n-            f\"{self.citing_file}:{self.line} cites ADR {self.number} which \"\n+            f\"{self.citing_file}:{self.line} cites {self.namespace} {self.number} which \"\n             f\"resolves to {len(self.matches)} documents (expected exactly one \"\n             f\"at {self.expected_path}): {joined}\"\n         )\n \n \n-def _adr_index(root: Path) -> dict[str, list[Path]]:\n-    \"\"\"Map every four-digit ADR number to the record files that claim it.\"\"\"\n-    index: dict[str, list[Path]] = {}\n+def _adr_index(root: Path) -> dict[tuple[str, str], list[Path]]:\n+    \"\"\"Map each decision namespace and number to the files that claim it.\"\"\"\n+    index: dict[tuple[str, str], list[Path]] = {}\n     adr_dir = root / \"docs\" / \"adr\"\n     if adr_dir.is_dir():\n         for path in sorted(adr_dir.glob(\"*.md\")):\n-            match = _ADR_FILENAME.match(path.name)\n+            match = _DECISION_FILENAME.match(path.name)\n             if match:\n-                index.setdefault(match.group(1), []).append(path)\n+                namespace = \"SADR\" if match.group(1) else \"ADR\"\n+                index.setdefault((namespace, match.group(2)), []).append(path)\n     return index\n \n \n def _citing_files(root: Path, include_docs: bool = True) -> list[Path]:\n-    \"\"\"Files scanned for citations: scripts/*.py, and cheaply the markdown.\"\"\"\n+    \"\"\"Current, shipped text surfaces scanned for decision citations.\"\"\"\n     files = sorted((root / \"scripts\").glob(\"*.py\"))\n+    files += sorted((root / \"scripts\").glob(\"*.sh\"))\n     if include_docs:\n         docs = root / \"docs\"\n         if docs.is_dir():\n             files += sorted(docs.rglob(\"*.md\"))\n         files += sorted(root.glob(\"*.md\"))\n-    return files\n+        pyproject = root / \"pyproject.toml\"\n+        if pyproject.is_file():\n+            files.append(pyproject)\n+        for relative in (\".github\", \".codex\"):\n+            surface = root / relative\n+            if surface.is_dir():\n+                files += sorted(\n+                    path\n+                    for path in surface.rglob(\"*\")\n+                    if path.is_file()\n+                    and path.suffix in {\".md\", \".py\", \".sh\", \".toml\", \".yaml\", \".yml\"}\n+                )\n+    return sorted(set(files))\n \n \n def find_adr_reference_violations(\n@@ -94,10 +111,9 @@\n ) -> list[Violation]:\n     \"\"\"Return every ``ADR ####`` citation under ``root`` that fails to resolve.\n \n-    A citation resolves when exactly one ``docs/adr/####-*.md`` matches its\n-    number. Zero matches is a \"missing\" violation; two or more is \"ambiguous\".\n-    Records that are never cited produce nothing -- the converse is not\n-    required.\n+    A citation resolves when exactly one record in the same namespace matches\n+    its number. Zero matches is a \"missing\" violation; two or more is\n+    \"ambiguous\". Records that are never cited produce nothing.\n     \"\"\"\n     root = Path(root)\n     index = _adr_index(root)\n@@ -110,13 +126,15 @@\n         rel = path.relative_to(root).as_posix()\n         for lineno, line in enumerate(text.splitlines(), start=1):\n             for match in _CITATION.finditer(line):\n-                number = match.group(1)\n-                found = index.get(number, [])\n+                namespace, number = match.groups()\n+                found = index.get((namespace, number), [])\n                 if len(found) == 1:\n                     continue\n                 reason = \"missing\" if not found else \"ambiguous\"\n                 rels = [p.relative_to(root).as_posix() for p in found]\n-                violations.append(Violation(rel, number, lineno, reason, rels))\n+                violations.append(\n+                    Violation(rel, namespace, number, lineno, reason, rels)\n+                )\n     return violations\n \n \n@@ -129,8 +147,8 @@\n         self.assertEqual(\n             violations,\n             [],\n-            \"Every 'ADR ####' citation must resolve to exactly one \"\n-            f\"docs/adr/####-*.md:\\n{detail}\",\n+            \"Every ADR/SADR citation must resolve exactly once in its own \"\n+            f\"docs/adr namespace:\\n{detail}\",\n         )\n \n     def test_scan_actually_reads_scripts(self) -> None:\n@@ -146,8 +164,21 @@\n         # The two records this Goal carried onto the default branch, and which\n         # the scripts cite most, must be present.\n         index = _adr_index(REPO_ROOT)\n-        self.assertIn(\"0016\", index)\n-        self.assertIn(\"0017\", index)\n+        self.assertIn((\"SADR\", \"0016\"), index)\n+        self.assertIn((\"SADR\", \"0017\"), index)\n+\n+    def test_platform_tree_has_only_sadr_0001_through_0019(self) -> None:\n+        adr_dir = REPO_ROOT / \"docs\" / \"adr\"\n+        sadrs = sorted(adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\"))\n+        self.assertEqual(\n+            {path.name.split(\"-\", 2)[1] for path in sadrs},\n+            {f\"{number:04d}\" for number in range(1, 20)},\n+        )\n+        self.assertEqual(\n+            list(adr_dir.glob(\"[0-9][0-9][0-9][0-9]-*.md\")),\n+            [],\n+            \"the Shiki platform tree must not retain legacy numeric SADR paths\",\n+        )\n \n \n class FixtureAdrReferences(unittest.TestCase):\n@@ -180,6 +211,26 @@\n         self.assertIn(\"9991\", message)  # cited number\n         self.assertIn(\"docs/adr/9991-*.md\", message)  # missing path\n \n+    def test_target_adr_does_not_satisfy_missing_platform_sadr(self) -> None:\n+        root = self._make_repo(\n+            adr_names=[\"0042-target-decision.md\"],\n+            script_body='\"\"\"Target ADR 0042 exists, but platform SADR-0042 does not.\"\"\"\\n',\n+        )\n+        violations = find_adr_reference_violations(root, include_docs=False)\n+        self.assertEqual(len(violations), 1)\n+        self.assertEqual(violations[0].namespace, \"SADR\")\n+        self.assertEqual(violations[0].number, \"0042\")\n+        self.assertEqual(violations[0].expected_path, \"docs/adr/SADR-0042-*.md\")\n+\n+    def test_target_adr_and_platform_sadr_may_reuse_a_number(self) -> None:\n+        root = self._make_repo(\n+            adr_names=[\"0042-target-decision.md\", \"SADR-0042-platform-decision.md\"],\n+            script_body='\"\"\"Target ADR 0042 and platform SADR-0042 are distinct.\"\"\"\\n',\n+        )\n+        self.assertEqual(\n+            find_adr_reference_violations(root, include_docs=False), []\n+        )\n+\n     def test_uncited_adr_is_allowed(self) -> None:\n         # An ADR exists but nothing cites it -> not a violation. The converse\n         # (every ADR must be cited) is explicitly NOT required.\ndiff --git a/tests/test_guardian_review.py b/tests/test_guardian_review.py\nindex 88829e7810212635e2fc98aa5918d6f26294677d..00a85a237acd5bf2d6cb5b294128198c422402ca\n--- a/tests/test_guardian_review.py\n+++ b/tests/test_guardian_review.py\n@@ -162,7 +162,7 @@\n \n     def test_docs_only_is_light_and_not_conservative(self):\n         focus = gr.classify_review_focus([\"docs/adr/0099-x.md\", \"README.md\"], \"\", \"low\")\n-        self.assertTrue(any(\"Docs/ADR-only\" in f for f in focus))\n+        self.assertTrue(any(\"Docs/decision-record-only\" in f for f in focus))\n         self.assertFalse(any(\"conservative default\" in f.lower() for f in focus))\n \n     def test_unknown_category_is_conservative(self):\ndiff --git a/tests/test_policy_job_merged_prs.py b/tests/test_policy_job_merged_prs.py\nindex d45512e74681e5e649279236c7d4e79d5c3da471..b97989f8bd74d091de1a6b5275a20d8dc68c4512\n--- a/tests/test_policy_job_merged_prs.py\n+++ b/tests/test_policy_job_merged_prs.py\n@@ -3,8 +3,8 @@\n \n ``.github/workflows/shiki-cca-completion.yml`` has two consumers of the merged-PR\n proof: the CCA *signal* (``guardian_approval_signal.py`` in \"Prepare CCA context\")\n-and the authoritative gate (``mergegate_check.py`` in \"Enforce MergeGate\"). ADR\n-0017's bookkeeping-closeout exemption (condition 6) needs the merged implementation\n+and the authoritative gate (``mergegate_check.py`` in \"Enforce MergeGate\").\n+SADR-0017's bookkeeping-closeout exemption (condition 6) needs the merged implementation\n PR proven, and a closeout carries NO ``post_merge_reconcile`` marker. The signal\n already resolves ``MERGED_PRS`` unconditionally, but \"Enforce MergeGate\" used to\n resolve it only inside ``grep -q '<!-- shiki:post_merge_reconcile -->'``. So a\ndiff --git a/tests/test_sadr_install_migration.py b/tests/test_sadr_install_migration.py\nnew file mode 100644\nindex 0000000000000000000000000000000000000000..a6f4c57e4dcaef73ef600b0d4804744138ab9071\n--- /dev/null\n+++ b/tests/test_sadr_install_migration.py\n@@ -0,0 +1,229 @@\n+\"\"\"SADR installation and collision-safe legacy upgrade behavior.\"\"\"\n+\n+from __future__ import annotations\n+\n+import contextlib\n+import hashlib\n+import io\n+import json\n+import shutil\n+import tempfile\n+import unittest\n+from pathlib import Path\n+\n+import shiki_test_support  # noqa: F401  (path bootstrap)\n+\n+import shiki_installer\n+from tests.test_adr_references import find_adr_reference_violations\n+\n+\n+PRODUCT_NUMBERS = tuple(range(13, 19))\n+\n+\n+def _quiet_install(target: Path, *, force: bool, validate: bool) -> None:\n+    with contextlib.redirect_stdout(io.StringIO()):\n+        shiki_installer.install_template(target, force=force, validate=validate)\n+\n+\n+def _sha256(path: Path) -> str:\n+    return hashlib.sha256(path.read_bytes()).hexdigest()\n+\n+\n+def _legacy_sadr_paths() -> tuple[str, ...]:\n+    return tuple(\n+        f\"docs/adr/{path.name.removeprefix('SADR-')}\"\n+        for path in sorted((shiki_installer.ROOT / \"docs\" / \"adr\").glob(\"SADR-*.md\"))\n+        if path.name != \"SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\"\n+    )\n+\n+\n+def _create_legacy_upgrade_fixture(\n+    target: Path,\n+) -> tuple[dict[str, bytes], tuple[str, ...]]:\n+    adr_dir = target / \"docs\" / \"adr\"\n+    adr_dir.mkdir(parents=True)\n+    product_bytes = {\n+        f\"{number:04d}-cubeshare-product-decision.md\": (\n+            f\"# CubeShare ADR {number:04d}\\n\\nDo not rewrite {number}.\\n\"\n+        ).encode()\n+        for number in PRODUCT_NUMBERS\n+    }\n+    for name, content in product_bytes.items():\n+        (adr_dir / name).write_bytes(content)\n+\n+    legacy_paths = _legacy_sadr_paths()\n+    for relative in legacy_paths:\n+        (target / relative).write_bytes(f\"legacy Shiki bytes for {relative}\\n\".encode())\n+\n+    stamp_path = target / shiki_installer.INSTALL_STAMP_PATH\n+    stamp_path.parent.mkdir(parents=True, exist_ok=True)\n+    stamp = {\n+        \"version\": shiki_installer.INSTALL_STAMP_VERSION,\n+        \"platform_commit\": None,\n+        \"installed_at\": \"2000-01-01T00:00:00+00:00\",\n+        \"digests\": {\n+            relative: _sha256(target / relative) for relative in legacy_paths\n+        },\n+    }\n+    stamp_path.write_text(\n+        json.dumps(stamp, indent=2, sort_keys=True) + \"\\n\", encoding=\"utf-8\"\n+    )\n+    return product_bytes, legacy_paths\n+\n+\n+def _tree_snapshot(root: Path) -> dict[str, tuple[str, bytes | str]]:\n+    \"\"\"Recursive byte/shape snapshot used to prove preflight is write-free.\"\"\"\n+    snapshot: dict[str, tuple[str, bytes | str]] = {}\n+    for path in sorted(root.rglob(\"*\")):\n+        relative = path.relative_to(root).as_posix()\n+        if path.is_symlink():\n+            snapshot[relative] = (\"symlink\", path.readlink().as_posix())\n+        elif path.is_dir():\n+            snapshot[relative] = (\"directory\", \"\")\n+        else:\n+            snapshot[relative] = (\"file\", path.read_bytes())\n+    return snapshot\n+\n+\n+class FreshSadrInstallTests(unittest.TestCase):\n+    def setUp(self) -> None:\n+        self.tmp = Path(tempfile.mkdtemp(prefix=\"shiki-sadr-fresh-\"))\n+        self.addCleanup(lambda: shutil.rmtree(self.tmp, ignore_errors=True))\n+        self.target = self.tmp / \"target\"\n+        self.adr_dir = self.target / \"docs\" / \"adr\"\n+        self.adr_dir.mkdir(parents=True)\n+        self.product_bytes = {\n+            f\"{number:04d}-product-decision.md\": (\n+                f\"# Product ADR {number:04d}\\n\\nTarget-owned bytes {number}.\\n\"\n+            ).encode()\n+            for number in PRODUCT_NUMBERS\n+        }\n+        for name, content in self.product_bytes.items():\n+            (self.adr_dir / name).write_bytes(content)\n+\n+    def test_fresh_install_preserves_product_adrs_and_installs_all_sadrs(self) -> None:\n+        _quiet_install(self.target, force=False, validate=True)\n+\n+        for name, expected in self.product_bytes.items():\n+            self.assertEqual((self.adr_dir / name).read_bytes(), expected)\n+\n+        sadr_numbers = {\n+            path.name.split(\"-\", 2)[1]\n+            for path in self.adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\")\n+        }\n+        self.assertEqual(sadr_numbers, {f\"{number:04d}\" for number in range(1, 20)})\n+\n+        numeric_records = {\n+            path.name\n+            for path in self.adr_dir.glob(\"[0-9][0-9][0-9][0-9]-*.md\")\n+        }\n+        self.assertEqual(numeric_records, set(self.product_bytes))\n+        self.assertEqual(find_adr_reference_violations(self.target), [])\n+\n+\n+class LegacySadrUpgradeTests(unittest.TestCase):\n+    def setUp(self) -> None:\n+        self.tmp = Path(tempfile.mkdtemp(prefix=\"shiki-sadr-upgrade-\"))\n+        self.addCleanup(lambda: shutil.rmtree(self.tmp, ignore_errors=True))\n+        self.target = self.tmp / \"target\"\n+        self.adr_dir = self.target / \"docs\" / \"adr\"\n+        self.product_bytes, self.legacy_paths = _create_legacy_upgrade_fixture(\n+            self.target\n+        )\n+\n+    def test_force_upgrade_deletes_only_stamped_legacy_sadrs(self) -> None:\n+        _quiet_install(self.target, force=True, validate=True)\n+\n+        for relative in self.legacy_paths:\n+            self.assertFalse((self.target / relative).exists(), relative)\n+        for name, expected in self.product_bytes.items():\n+            self.assertEqual((self.adr_dir / name).read_bytes(), expected)\n+\n+        sadrs = list(self.adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\"))\n+        self.assertEqual(len(sadrs), 19)\n+        refreshed = json.loads(\n+            (self.target / shiki_installer.INSTALL_STAMP_PATH).read_text(encoding=\"utf-8\")\n+        )\n+        for relative in self.legacy_paths:\n+            self.assertNotIn(relative, refreshed[\"digests\"])\n+        for path in sadrs:\n+            relative = path.relative_to(self.target).as_posix()\n+            self.assertEqual(refreshed[\"digests\"][relative], _sha256(path))\n+\n+    def test_non_force_install_never_deletes_legacy_files(self) -> None:\n+        before = {\n+            relative: (self.target / relative).read_bytes()\n+            for relative in self.legacy_paths\n+        }\n+        _quiet_install(self.target, force=False, validate=False)\n+        for relative, expected in before.items():\n+            self.assertEqual((self.target / relative).read_bytes(), expected)\n+        refreshed = json.loads(\n+            (self.target / shiki_installer.INSTALL_STAMP_PATH).read_text(encoding=\"utf-8\")\n+        )\n+        for relative in self.legacy_paths:\n+            self.assertEqual(refreshed[\"digests\"][relative], _sha256(self.target / relative))\n+\n+\n+class LegacyCleanupPreflightTests(unittest.TestCase):\n+    def setUp(self) -> None:\n+        self.tmp = Path(tempfile.mkdtemp(prefix=\"shiki-sadr-preflight-\"))\n+        self.addCleanup(lambda: shutil.rmtree(self.tmp, ignore_errors=True))\n+\n+    def test_all_ownership_blockers_abort_before_any_write(self) -> None:\n+        cases = (\n+            (\"absent\", \"install stamp is absent\"),\n+            (\"unreadable\", \"install stamp is unreadable\"),\n+            (\"malformed\", \"install stamp is malformed\"),\n+            (\"missing-digest\", \"install stamp has no digest\"),\n+            (\"digest-mismatch\", \"digest mismatch\"),\n+        )\n+        for name, expected_reason in cases:\n+            with self.subTest(name=name):\n+                target = self.tmp / name\n+                _, legacy_paths = _create_legacy_upgrade_fixture(target)\n+                stamp_path = target / shiki_installer.INSTALL_STAMP_PATH\n+\n+                if name == \"absent\":\n+                    stamp_path.unlink()\n+                elif name == \"unreadable\":\n+                    stamp_path.unlink()\n+                    stamp_path.mkdir()\n+                elif name == \"malformed\":\n+                    stamp_path.write_text(\"{not-json\\n\", encoding=\"utf-8\")\n+                else:\n+                    stamp = json.loads(stamp_path.read_text(encoding=\"utf-8\"))\n+                    if name == \"missing-digest\":\n+                        stamp[\"digests\"] = {}\n+                    else:\n+                        stamp[\"digests\"] = {\n+                            relative: \"deadbeef\" for relative in legacy_paths\n+                        }\n+                    stamp_path.write_text(\n+                        json.dumps(stamp, indent=2, sort_keys=True) + \"\\n\",\n+                        encoding=\"utf-8\",\n+                    )\n+\n+                before = _tree_snapshot(target)\n+                with self.assertRaises(shiki_installer.ShikiError) as raised:\n+                    _quiet_install(target, force=True, validate=False)\n+                after = _tree_snapshot(target)\n+\n+                self.assertEqual(before, after, f\"{name} preflight changed the tree\")\n+                message = str(raised.exception)\n+                self.assertIn(expected_reason, message)\n+                self.assertIn(\"No files were written\", message)\n+                for relative in legacy_paths:\n+                    self.assertIn(relative, message)\n+\n+    def test_absent_stamp_is_allowed_when_no_legacy_path_exists(self) -> None:\n+        target = self.tmp / \"no-legacy\"\n+        target.mkdir()\n+        _quiet_install(target, force=True, validate=False)\n+        self.assertTrue(\n+            (target / \"docs\" / \"adr\" / \"SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\").is_file()\n+        )\n+\n+\n+if __name__ == \"__main__\":\n+    unittest.main()\n\n2026-08-07T08:19:46.047769Z ERROR codex_models_manager::manager: failed to renew cache TTL: missing field `base_instructions` at line 94 column 5\nexec\n/bin/zsh -lc \"rg -n 'SADR-[0-9]{4}/[0-9]{4}' AGENTS.md CLAUDE.md CONTEXT.md CONTRIBUTING.md SYSTEM_PROMPT.md pyproject.toml docs scripts tests .github .codex --glob '\"'!.shiki/**'\"' --glob '\"'!skills/**'\"'\" in /private/tmp/shiki-adr-collision.pa0KwY/.worktrees/shiki-t-20260807t073307221254z-2d22d654-introduc\n succeeded in 0ms:\nAGENTS.md:303:When every MergeGate condition is satisfied, the goal loop (`shiki loop`) may merge risk low/medium PRs autonomously (SADR-0008/0009). High and critical risk always require Guardian approval before merge.\ndocs/proposals/0002-autonomous-loop-completion.md:8:The Shiki 0.2.0 autonomous post-freeze loop (SADR-0008/0009, #119 G-D) is not\ndocs/agents/implementation-policy.md:50:12. **Approval records are authored only by their authority.** An implementation runtime must never author, edit, forge, or backfill an operative approval record \u2014 a `spec_freeze` block, a Spec Amendment approval, Guardian approval evidence, a CCA `complete` verdict, or any other artifact whose meaning is that an authority approved something. An acceptance criterion that cannot be satisfied from the worktree because the missing artifact is such a record is reported as a stop, never closed by manufacturing it. This does not cover approval-record schemas or clearly-scoped test fixtures that never enter the real `.shiki/` mirror (SADR-0009/0010).\ntests/test_contract_amendment.py:1:\"\"\"Spec Amendment PR mode (SADR-0009/0015): correct a frozen contract on a\ntests/test_loop_engine.py:1:\"\"\"Goal loop decision engine contract (G-D, SADR-0008/0009).\ntests/test_guardian_review.py:1:\"\"\"Tests for the External AI Guardian Review adapter contract (SADR-0010/0014).\nscripts/test_shiki_adversarial_state.sh:1202:# Amendment mode (SADR-0009/0015) is two-factor (marker + mergegate:amendment\ndocs/agents/autonomous-loop-e2e-acceptance.md:44:   (SADR-0008/0009); the MergeGate policy check is the recorded-authority gate.\nscripts/test_shiki_goal_loop.sh:196:    \"decisions\": [\"Loop drives frozen goals (SADR-0008/0009); completion is pushed to main via a closeout PR (SADR-0012)\"]\nscripts/test_shiki_loop_e2e.sh:217:    \"decisions\": [\"The loop drives frozen goals (SADR-0008/0009/0011)\"]\nscripts/mergegate_check.py:335:# An Amendment PR (Spec Amendment, SADR-0009/0015) declares itself with this exact\nscripts/mergegate_check.py:1124:    \"\"\"Validate a Spec Amendment PR (SADR-0009/0015): correct a spec-frozen Goal's\nscripts/mergegate_check.py:3076:        # Amendment mode (SADR-0009/0015): a goal-scoped PR that corrects a\ndocs/adr/SADR-0013-bootstrap-grants-actions-approve-pull-requests.md:51:  posture is solo/AFK autonomous operation (SADR-0008/0009/0012), where the CCA\nscripts/shiki_loop.py:2:\"\"\"Autonomous post-freeze Goal loop (SADR-0008/0009).\nscripts/test_shiki_governance_evidence.sh:966:# Amendment mode (SADR-0009/0015): a Spec Amendment PR cannot lower its OWN\ndocs/adr/SADR-0016-locks-bound-product-paths.md:189:removes the low/medium autonomous path SADR-0008/0009 exists to provide. And it\ndocs/adr/SADR-0011-autonomous-quality-gate-evidence.md:9:The Shiki 0.2.0 autonomous post-freeze loop (SADR-0008/0009, PRD #119 G-D) must\ndocs/adr/SADR-0015-contract-approval-before-dispatch.md:12:`external_ai_guardian_review` artifact (SADR-0010/0014) \u2014 is bound to a PR, and\ndocs/adr/SADR-0015-contract-approval-before-dispatch.md:133:is both earlier and smaller; the external AI Guardian path (SADR-0010/0014) works\n\ndiff --git a/.codex/skills/shiki/SKILL.md b/.codex/skills/shiki/SKILL.md\nindex c8e2e85d9d75d49cc4f2d6286ee22659c4e53e75..93cded623b2c15d9f65c76491a1c2d813ac9e6c0\n--- a/.codex/skills/shiki/SKILL.md\n+++ b/.codex/skills/shiki/SKILL.md\n@@ -18,6 +18,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n Codex CLI does not register this skill as a custom `/shiki` slash command. When\n the user types `/shiki` in Codex CLI and gets \"Unrecognized command\", explain\n that this is expected: invoke Shiki in Codex with natural language such as\n@@ -69,7 +72,7 @@\n \n ## Responsibilities\n \n-- Claude Code implements and repairs by default (`shiki runner claude`, ADR 0008); Codex implements and repairs tasks explicitly assigned to `codex`.\n+- Claude Code implements and repairs by default (`shiki runner claude`, SADR-0008); Codex implements and repairs tasks explicitly assigned to `codex`.\n - Claude Code Action can act as GitHub-side CCA or reviewer.\n - CCA judges completion.\n - MergeGate authorizes state transitions and merge readiness.\ndiff --git a/.github/prompts/cca-completion-check.md b/.github/prompts/cca-completion-check.md\nindex ed203ad5f302ecc81372d8ab27d2b1232ca97497..fd89950528333ef6242e6d5443904f5392ab7e56\n--- a/.github/prompts/cca-completion-check.md\n+++ b/.github/prompts/cca-completion-check.md\n@@ -19,6 +19,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n ## Required Reading\n \n Read only the files needed to judge the PR contract. Prefer this order:\ndiff --git a/.github/prompts/codex-task-handoff.md b/.github/prompts/codex-task-handoff.md\nindex 695e211d76dea1c60e8d5bb360e78487808286a4..59441d576b01a39569722f226c854e5a740e9eb5\n--- a/.github/prompts/codex-task-handoff.md\n+++ b/.github/prompts/codex-task-handoff.md\n@@ -2,7 +2,7 @@\n \n You are Codex Front acting as the Implementer for a Shiki task.\n \n-Follow `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant ADRs, and `docs/agents/codex-handoff.md`.\n+Follow `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant Shiki SADRs and target ADRs, and `docs/agents/codex-handoff.md`.\n \n Rules:\n \ndiff --git a/AGENTS.md b/AGENTS.md\nindex 7f041100d49c2a7525f51cb0f5d696a4a6d1c78c..a06bfbabc6fa48a007389e25e12d7ed523936bed\n--- a/AGENTS.md\n+++ b/AGENTS.md\n@@ -22,7 +22,7 @@\n - **Codex Front** is an optional implementer through Codex App, Codex CLI, Codex IDE extension, or Codex Web signed in with ChatGPT OAuth/subscription auth, dispatched with `shiki runner codex` when a task is explicitly assigned to `codex`.\n - **GitHub CCA** is implemented by Claude Code Action by default, using `CLAUDE_CODE_OAUTH_TOKEN`.\n \n-Do not assume `openai/codex-action`, `OPENAI_API_KEY`, or API-key based Codex automation in the default Shiki path. API-key runners are explicit target-repo extensions and require their own ADR.\n+Do not assume `openai/codex-action`, `OPENAI_API_KEY`, or API-key based Codex automation in the default Shiki path. API-key runners are explicit target-repo extensions and require their own target ADR.\n \n Shiki is not:\n \n@@ -44,10 +44,14 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use\n+`SADR-NNNN-*.md`; target product decisions retain `NNNN-*.md` and the `ADR`\n+namespace.\n+\n Use the canonical source-of-truth order above. Runtime-specific reading orders\n are operational guidance only and must not override or reorder it.\n \n-Conversation state is not durable truth. If a decision matters, put it in GitHub, `.shiki/`, `CONTEXT.md`, or an ADR.\n+Conversation state is not durable truth. If a decision matters, put it in GitHub, `.shiki/`, `CONTEXT.md`, a Shiki SADR, or a target ADR.\n \n If GitHub and `.shiki/` disagree, surface the conflict and prefer GitHub operational state until the mirror is repaired.\n \n@@ -75,8 +79,8 @@\n Every non-trivial change follows this loop:\n \n 1. **Goal**: clarify outcome, completion conditions, non-goals, risk level, and success signals.\n-2. **grill-with-docs**: challenge the plan against domain docs, ADRs, code reality, terminology, and edge scenarios.\n-3. **Context & Impact**: find relevant docs, ADRs, code, dependencies, owners, locks, and verification surfaces.\n+2. **grill-with-docs**: challenge the plan against domain docs, Shiki SADRs, target ADRs, code reality, terminology, and edge scenarios.\n+3. **Context & Impact**: find relevant docs, SADRs, target ADRs, code, dependencies, owners, locks, and verification surfaces.\n 4. **PRD and Spec Freeze**: use `to-prd` when context is settled enough to become durable product/engineering intent. The operator's explicit approval of the PRD is Spec Freeze: the plan records a `spec_freeze` block, and no plan runs without it. Steps 1-4 form Requirements Definition \u2014 one continuous operator dialogue. After Spec Freeze, scope changes require an operator-approved Spec Amendment; non-scope-moving interpretations go to the Assumption Log.\n 5. **Issues**: use `to-issues` to create independently grabbable vertical-slice issues. Prefer AFK slices over HITL slices where possible.\n 6. **Triage**: label issues for readiness, risk, runtime, skills, and MergeGate state.\n@@ -167,7 +171,7 @@\n - Testing decisions.\n - Out-of-scope items.\n - Further notes.\n-- Links to relevant domain terms and ADRs.\n+- Links to relevant domain terms, SADRs, and target ADRs.\n \n Every executable task must include:\n \n@@ -193,7 +197,7 @@\n \n Context & Impact must identify:\n \n-- Relevant documents, domain terms, ADRs, and past decisions.\n+- Relevant documents, domain terms, SADRs, target ADRs, and past decisions.\n - Relevant modules, interfaces, seams, callers, owners, and tests.\n - Dependency relationships between tasks.\n - Candidate locks and lock conflicts.\n@@ -210,7 +214,7 @@\n | Skill | Required when |\n | --- | --- |\n | `setup-matt-pocock-skills` | First configuring the repo for skills, or when issue tracker, triage labels, or domain docs are missing. |\n-| `grill-with-docs` | Default for non-trivial Goals before PRD/issues; always required when plans, terminology, boundaries, tradeoffs, or ADR-worthy decisions are ambiguous. |\n+| `grill-with-docs` | Default for non-trivial Goals before PRD/issues; always required when plans, terminology, boundaries, tradeoffs, or decision-record-worthy choices are ambiguous. |\n | `zoom-out` | The agent lacks an architectural map of the relevant code area. |\n | `to-prd` | Settled Goal context must become durable product/engineering intent. |\n | `to-issues` | A Goal, PRD, or plan must become independently grabbable vertical-slice issues. |\n@@ -348,7 +352,7 @@\n - Three or more new files.\n - Any high-risk or critical label.\n \n-When the architecture gate triggers, run `grill-with-docs` or `improve-codebase-architecture` as appropriate and record durable decisions in an ADR when needed.\n+When the architecture gate triggers, run `grill-with-docs` or `improve-codebase-architecture` as appropriate and record durable decisions in a Shiki SADR or target ADR as applicable.\n \n ## Engineering Discipline\n \ndiff --git a/CLAUDE.md b/CLAUDE.md\nindex 98a932e7841a632866fcd53b0bad0e3af3711d3d..98a31399404d6fb044b4fd493f4459ac87f439de\n--- a/CLAUDE.md\n+++ b/CLAUDE.md\n@@ -14,6 +14,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n ## Claude Role\n \n Default to planner, default implementer, reviewer, coordinator, documentation editor, CCA-style completion judge, and final-judgment assistant.\n@@ -57,7 +60,7 @@\n At the start of a Shiki session:\n \n 1. Read `AGENTS.md`.\n-2. Read `CONTEXT.md` and relevant ADRs.\n+2. Read `CONTEXT.md` and relevant Shiki SADRs and target ADRs.\n 3. Read active `.shiki/` Goal/task/ledger state when present.\n 4. Read active distilled rules from `.shiki/memories/` (status `distilled`, `active: true`, not revoked or superseded) for applicable guidance; the same rules are injected deterministically into task handoffs as the `## Distilled Rules` section (\u00a73.5).\n 5. Read the GitHub Issue or PR when the task is GitHub-backed.\n@@ -74,7 +77,7 @@\n - Define the Goal, completion conditions, non-goals, and risk level.\n - Treat `grill-with-docs` as the default planning entry point for non-trivial Goals.\n - Challenge domain terms against `CONTEXT.md`.\n-- Challenge hard-to-reverse tradeoffs against ADRs.\n+- Challenge hard-to-reverse tradeoffs against SADRs and target ADRs.\n - Explore code instead of asking when code can answer the question.\n - Run Context & Impact before implementation planning.\n - Produce a Task DAG, not an unordered checklist.\n@@ -94,7 +97,7 @@\n - For each question, provide your recommended answer.\n - Resolve dependency decisions before downstream design questions.\n - Update `CONTEXT.md` inline when terminology crystallizes.\n-- Offer ADRs only when the decision is hard to reverse, surprising without context, and a real tradeoff.\n+- Offer a SADR or target ADR only when the decision is hard to reverse, surprising without context, and a real tradeoff.\n - Convert unresolved questions into HITL blockers rather than allowing Codex to guess.\n \n ## PRD and Issues Standard\n@@ -103,7 +106,7 @@\n \n - Use the project's domain glossary vocabulary.\n - Avoid volatile file paths unless they are essential.\n-- Include implementation decisions, testing decisions, out-of-scope items, and relevant ADR/domain links.\n+- Include implementation decisions, testing decisions, out-of-scope items, and relevant SADR/target-ADR/domain links.\n - Identify deep-module opportunities and testable interfaces.\n \n When creating issues:\n@@ -121,7 +124,7 @@\n - Branch/worktree target.\n - Scope and non-goals.\n - Dependencies and locks.\n-- Relevant docs, ADRs, modules, and tests.\n+- Relevant docs, SADRs, target ADRs, modules, and tests.\n - Required skill invocations.\n - TDD expectations.\n - Pre-PR `code-review` gate expectations.\n@@ -152,7 +155,7 @@\n \n - Treat yourself as a judge, not implementer.\n - Do not write production code.\n-- Read `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant ADRs, PRD, task issue, PR body, diff, checks, reviews, labels, and `.shiki/` state.\n+- Read `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant SADRs and target ADRs, PRD, task issue, PR body, diff, checks, reviews, labels, and `.shiki/` state.\n - Evaluate every applicable checklist in `docs/agents/checklists.md`.\n - Map each acceptance criterion to durable evidence.\n - Distinguish implementation failure from missing evidence.\ndiff --git a/CONTEXT.md b/CONTEXT.md\nindex e61a9be06a0cce0d026484083a657bde2067e234..237ee8aa3ea8fab334ece49768bc5ed3b5af0974\n--- a/CONTEXT.md\n+++ b/CONTEXT.md\n@@ -25,7 +25,7 @@\n _Avoid_: jumping directly into code\n \n **grill-with-docs**:\n-A planning skill that challenges a plan against domain language, ADRs, code reality, and concrete edge scenarios. It resolves design-tree questions before PRD/issues. It runs inside Requirements Definition.\n+A planning skill that challenges a plan against domain language, Shiki SADRs, target ADRs, code reality, and concrete edge scenarios. It resolves design-tree questions before PRD/issues. It runs inside Requirements Definition.\n _Avoid_: generic brainstorming, silent assumptions\n \n **Requirements Definition**:\ndiff --git a/CONTRIBUTING.md b/CONTRIBUTING.md\nindex c6d9b656e73abb8a55e5f660123e03153dcb3e6d..9e37a8fbf668db5b22461aaae5548b8f1354394c\n--- a/CONTRIBUTING.md\n+++ b/CONTRIBUTING.md\n@@ -17,7 +17,7 @@\n 1. **Goal** \u2014 start from a user-approved Goal with an outcome, completion\n    conditions, non-goals, risk level, and success signals.\n 2. **grill-with-docs** \u2014 for any non-trivial change, challenge the plan against\n-   domain language, ADRs, and code reality before writing a PRD.\n+   domain language, Shiki SADRs, target ADRs, and code reality before writing a PRD.\n 3. **PRD** \u2014 capture settled product/engineering intent (`to-prd`).\n 4. **Spec Freeze** \u2014 the operator explicitly approves the PRD/requirements;\n    the plan records a `spec_freeze` block and no plan runs without it\n@@ -94,7 +94,7 @@\n   entries.\n - Do not hand-edit `.shiki/goals/*.json` or `.shiki/manifest.json` in a task PR.\n - Conversation state is not durable truth. If a decision matters, record it in\n-  GitHub, `.shiki/`, `CONTEXT.md`, or an ADR.\n+  GitHub, `.shiki/`, `CONTEXT.md`, a Shiki SADR, or a target ADR.\n \n ## Architecture Gate\n \n@@ -103,7 +103,7 @@\n or CI policy, package structure, three or more structural files, more than 100\n net new lines in a core module, three or more new files, or any high-risk label.\n When triggered, run `grill-with-docs` or `improve-codebase-architecture` and\n-record durable decisions in an ADR when needed (see `docs/adr/`).\n+record durable decisions in a Shiki SADR or target ADR as applicable (see `docs/adr/`).\n \n ## Safety\n \ndiff --git a/SYSTEM_PROMPT.md b/SYSTEM_PROMPT.md\nindex aaaea45e3a896c3a44ba89eec8bb84325ecf6c62..da009c86a88285269dee0155e58e550c6d744ea9\n--- a/SYSTEM_PROMPT.md\n+++ b/SYSTEM_PROMPT.md\n@@ -30,6 +30,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n Use the canonical source-of-truth order above. Runtime-specific reading orders\n are operational guidance only and must not override or reorder it. The current\n conversation is non-durable operator input.\n@@ -48,14 +51,14 @@\n \n A runtime may only do work that is explicitly assigned by the Goal, task, PR, or MergeGate decision.\n \n-Do not assume `openai/codex-action`, `OPENAI_API_KEY`, or API-key based Codex automation in the default Shiki path. API-key runners are explicit target-repo extensions and require their own ADR.\n+Do not assume `openai/codex-action`, `OPENAI_API_KEY`, or API-key based Codex automation in the default Shiki path. API-key runners are explicit target-repo extensions and require their own target ADR.\n \n ## 4. Non-negotiable Shiki flow\n \n For any non-trivial Goal, use this flow:\n \n 1. **Goal Seek**: define outcome, non-goals, risk level, completion criteria, and evidence requirements.\n-2. **grill-with-docs**: challenge the plan against `CONTEXT.md`, ADRs, code reality, terminology, boundaries, and edge scenarios. Ask one question at a time when the operator is available. If code can answer a question, inspect code instead of asking.\n+2. **grill-with-docs**: challenge the plan against `CONTEXT.md`, Shiki SADRs, target ADRs, code reality, terminology, boundaries, and edge scenarios. Ask one question at a time when the operator is available. If code can answer a question, inspect code instead of asking.\n 3. **Context & Impact**: identify modules, interfaces, seams, callers, dependencies, locks, risk, and verification surfaces.\n 4. **to-prd + Spec Freeze**: turn settled context into a PRD using domain vocabulary and testing decisions. The operator's explicit approval of the PRD is Spec Freeze, recorded as the plan's `spec_freeze` block; plans without it do not run. Steps 1-4 form Requirements Definition.\n 5. **to-issues**: decompose the PRD into independently grabbable vertical-slice issues. Prefer AFK slices; mark HITL slices when judgment is still required.\n@@ -75,7 +78,7 @@\n Before making any material change:\n \n 1. Read `AGENTS.md` and any runtime-specific wrapper such as `CLAUDE.md`.\n-2. Read `CONTEXT.md` and relevant ADRs.\n+2. Read `CONTEXT.md` and relevant SADRs and target ADRs.\n 3. Read the GitHub Issue / PR and `.shiki/` state for the active Goal or task.\n 4. Identify your role: Planner, Implementer, Reviewer, Completion Check Agent, Repairer, or Guardian-assist.\n 5. Identify Goal id, task id, branch, PR, dependencies, locks, risk level, required skills, and required evidence.\n@@ -103,7 +106,7 @@\n \n At minimum, identify:\n \n-- Relevant documents, ADRs, domain terms, and prior decisions.\n+- Relevant documents, SADRs, target ADRs, domain terms, and prior decisions.\n - Relevant modules, interfaces, seams, callers, owners, and tests.\n - Dependency relationships between tasks.\n - Candidate file locks and lock conflicts.\n@@ -152,7 +155,7 @@\n \n CCA must read:\n \n-- `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant ADRs.\n+- `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant SADRs and target ADRs.\n - Parent Goal issue, PRD issue, task issue, comments, and labels.\n - PR body, diff, commits, changed files, reviews, CI checks, and artifacts available to the workflow.\n - `.shiki/` task contract, locks, ledger entries, CCA checklist profile, and prior repair packets.\n@@ -214,7 +217,7 @@\n - Three or more new files.\n - Any high-risk or critical label.\n \n-When the architecture gate triggers, run `grill-with-docs` or `improve-codebase-architecture` as appropriate and record the decision in an ADR if it is hard to reverse or likely to be re-litigated.\n+When the architecture gate triggers, run `grill-with-docs` or `improve-codebase-architecture` as appropriate and record the decision in a Shiki SADR or target ADR as applicable if it is hard to reverse or likely to be re-litigated.\n \n ## 14. Skill Gate\n \n@@ -223,7 +226,7 @@\n Use these skills:\n \n - `setup-matt-pocock-skills`: before first use of the engineering skill set, or when issue tracker, triage labels, or domain docs are missing.\n-- `grill-with-docs`: default for non-trivial Goals before PRD/issues; required for ambiguous plans, terminology, boundaries, ADR-worthy tradeoffs, or design-tree decisions.\n+- `grill-with-docs`: default for non-trivial Goals before PRD/issues; required for ambiguous plans, terminology, boundaries, decision-record-worthy tradeoffs, or design-tree decisions.\n - `zoom-out`: unfamiliar code area or missing architectural map.\n - `to-prd`: convert settled context into a PRD.\n - `to-issues`: break a PRD, plan, or Goal into independently grabbable vertical-slice issues.\ndiff --git a/docs/adr/0001-keep-shiki-as-platform-name.md b/docs/adr/SADR-0001-keep-shiki-as-platform-name.md\nindex 20a9637c3b9d8c97de7ba8db7cfae4d82d9a7dba..8b685439959aada1b221c112f0ac3c51ca8a1695\n--- a/docs/adr/0001-keep-shiki-as-platform-name.md\n+++ b/docs/adr/SADR-0001-keep-shiki-as-platform-name.md\n@@ -1,3 +1,3 @@\n-# Keep Shiki As The Platform Name\n+# SADR-0001: Keep Shiki As The Platform Name\n \n Shiki remains the product name for the GitHub-native agentic engineering control plane. We considered more descriptive names such as MergePilot, MergeGate, and Kiroku, but kept Shiki because it carries the existing goal-seeking framework identity while remaining independent of Hermes, Codex, Claude Code, or any single runtime.\ndiff --git a/docs/adr/0002-use-agents-as-shared-constitution.md b/docs/adr/SADR-0002-use-agents-as-shared-constitution.md\nindex 73de30ecb078120eac540d9bc51855e3cd48d0e3..c5486182bf06fca36ea508496b013c67302fd989\n--- a/docs/adr/0002-use-agents-as-shared-constitution.md\n+++ b/docs/adr/SADR-0002-use-agents-as-shared-constitution.md\n@@ -1,3 +1,3 @@\n-# Use AGENTS As The Shared Agent Constitution\n+# SADR-0002: Use AGENTS As The Shared Agent Constitution\n \n Shiki uses `AGENTS.md` as the runtime-neutral constitution and `CLAUDE.md` as a thin Claude Code wrapper. This keeps Codex, Claude Code, Hermes Runner, and future Agent Runtimes aligned on the same GitHub-first MergeGate rules while still allowing runtime-specific instructions where needed.\ndiff --git a/docs/adr/0003-require-skill-gate-for-mergegate-readiness.md b/docs/adr/SADR-0003-require-skill-gate-for-mergegate-readiness.md\nindex 424be0c9af4c8f0136f1fde68a468c47ff84e764..7fc337987c7df9792f75f181dfcbed75fe5ffff2\n--- a/docs/adr/0003-require-skill-gate-for-mergegate-readiness.md\n+++ b/docs/adr/SADR-0003-require-skill-gate-for-mergegate-readiness.md\n@@ -1,3 +1,3 @@\n-# Require Skill Gate For MergeGate Readiness\n+# SADR-0003: Require Skill Gate For MergeGate Readiness\n \n Shiki requires relevant engineering skills to be invoked before execution when their triggers apply. This makes skills part of MergeGate readiness rather than optional prompt style, because Shiki depends on repeatable planning, issue decomposition, TDD, diagnosis, triage, and architecture review before autonomous or parallel agent work can be trusted.\ndiff --git a/docs/adr/0004-use-github-first-with-shiki-mirror.md b/docs/adr/SADR-0004-use-github-first-with-shiki-mirror.md\nindex d0a3a9174dc646a945a9d325954ffaf00c352040..a79c54ae9ee8eed2a0524192b864da4e22a92d3f\n--- a/docs/adr/0004-use-github-first-with-shiki-mirror.md\n+++ b/docs/adr/SADR-0004-use-github-first-with-shiki-mirror.md\n@@ -1,4 +1,4 @@\n-# ADR 0004: Use GitHub First With A Repository-Local Shiki Mirror\n+# SADR-0004: Use GitHub First With A Repository-Local Shiki Mirror\n \n ## Status\n \ndiff --git a/docs/adr/0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md b/docs/adr/SADR-0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\nindex b339baf49b89d9290e79c3d51b36e7b2bde3bd95..0f3c3eb9afccdb3403ce978e21d04fe12e495da0\n--- a/docs/adr/0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\n+++ b/docs/adr/SADR-0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\n@@ -1,8 +1,8 @@\n-# ADR 0005: Use Codex As Front And Claude Code Action As The GitHub Runtime\n+# SADR-0005: Use Codex As Front And Claude Code Action As The GitHub Runtime\n \n ## Status\n \n-Accepted (implementer default superseded by ADR 0008; the auth model and the\n+Accepted (implementer default superseded by SADR-0008; the auth model and the\n Claude Code Action GitHub runtime decision remain in force)\n \n ## Context\n@@ -26,4 +26,4 @@\n - Shiki avoids requiring `OPENAI_API_KEY` for the default workflow.\n - Codex work happens through the user's authenticated Codex front surface, not as a hidden backend Action.\n - Claude Code Action is configured for OAuth-token based subscription usage by default.\n-- API-key based automation remains possible only as an explicit target-repo extension with its own ADR.\n+- API-key based automation remains possible only as an explicit target-repo extension with its own target ADR.\ndiff --git a/docs/adr/0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md b/docs/adr/SADR-0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\nindex 4bcae9d1e52f0a37b8e9fbafa5cd14b6ee1993bd..7a38538a6ae9929567f0ade67e7ea6a60d0b3f7e\n--- a/docs/adr/0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\n+++ b/docs/adr/SADR-0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\n@@ -1,4 +1,4 @@\n-# ADR 0006: Separate Codex Implementation, CCA Completion, And MergeGate Authorization\n+# SADR-0006: Separate Codex Implementation, CCA Completion, And MergeGate Authorization\n \n ## Status\n \ndiff --git a/docs/adr/0007-packaging-and-release.md b/docs/adr/SADR-0007-packaging-and-release.md\nindex 79b84ddb97167372b44641e5b7860e400ce80822..bb445a2e2e856d9d1f5fdbe35de8d4e8cec0fb32\n--- a/docs/adr/0007-packaging-and-release.md\n+++ b/docs/adr/SADR-0007-packaging-and-release.md\n@@ -1,4 +1,4 @@\n-# ADR 0007: Packaging And Release For Shiki\n+# SADR-0007: Packaging And Release For Shiki\n \n ## Status\n \n@@ -25,7 +25,7 @@\n   not introduce third-party runtime dependencies or break the shim.\n - Releases must run inside the operator's subscription-authenticated toolchain\n   and must not require extra secrets beyond the built-in `GITHUB_TOKEN`\n-  (consistent with ADR 0005's no-API-key-by-default stance).\n+  (consistent with SADR-0005's no-API-key-by-default stance).\n - `.shiki/manifest.json` already encodes the install surface (`install.include`,\n   `install.create_directories`, `install.exclude_from_commit`), and it must not\n   be mutated as part of this change.\n@@ -34,7 +34,7 @@\n \n This is a hard-to-reverse platform decision (it sets the version source of\n truth, the release trigger, and the public install contract), so it is recorded\n-as an ADR.\n+as a SADR.\n \n ## Decision\n \n@@ -104,7 +104,7 @@\n 3. **Publishing to PyPI.** Rejected for now: Shiki is distributed as a\n    repository template + git-installable CLI, not a PyPI library; PyPI would add\n    credentials/secrets and a different versioning contract. Can be added later as\n-   its own ADR if a packaged distribution is needed.\n+   its own SADR if a packaged distribution is needed.\n 4. **Dynamic version (read `VERSION` via a build backend `dynamic` field).**\n    Rejected to keep `pyproject.toml` backend-agnostic and dependency-free; the\n    small duplication between `VERSION` and `pyproject.toml` is guarded by the\n@@ -123,4 +123,4 @@\n   releasing versions the whole platform but ships only `install.include` to\n   targets.\n - Future PyPI distribution or a console entry point are deferred and would each\n-  warrant their own ADR.\n+  warrant their own SADR.\ndiff --git a/docs/adr/0008-use-claude-code-as-default-implementer.md b/docs/adr/SADR-0008-use-claude-code-as-default-implementer.md\nindex dedf4510b7c7799521422f622029563d373db031..35c2625d908167fbc58855b5b63e283805f167e8\n--- a/docs/adr/0008-use-claude-code-as-default-implementer.md\n+++ b/docs/adr/SADR-0008-use-claude-code-as-default-implementer.md\n@@ -1,12 +1,12 @@\n-# ADR 0008: Use Claude Code As The Default Implementer Runtime\n+# SADR-0008: Use Claude Code As The Default Implementer Runtime\n \n ## Status\n \n-Proposed (supersedes ADR 0005 for the implementer default; keeps its auth model)\n+Proposed (supersedes SADR-0005 for the implementer default; keeps its auth model)\n \n ## Context\n \n-ADR 0005 made Codex Front the default implementation runtime, with Claude Code\n+SADR-0005 made Codex Front the default implementation runtime, with Claude Code\n restricted to planner and reviewer roles by the runtime registry, and the only\n autonomous implementation adapter was `shiki runner codex`.\n \n@@ -18,13 +18,13 @@\n - The platform's planning, review, and CCA layers already run on Claude Code,\n   and Claude Code's native capabilities (the built-in `/code-review` skill and\n   the Workflow multi-agent orchestration tool) have no Codex equivalent.\n-- The Goal lifecycle redesign (ADR 0009) requires an autonomous post-freeze\n+- The Goal lifecycle redesign (SADR-0009) requires an autonomous post-freeze\n   loop driven from the operator's Claude Code session, including bounded\n   repair dispatch, which must not depend on a runtime that cannot be invoked\n   unattended.\n \n The constitution claims Shiki is runtime-agnostic (\"not a Claude-only\n-workflow\", ADR 0002). Any change to the implementer default must preserve the\n+workflow\", SADR-0002). Any change to the implementer default must preserve the\n registry-based runtime model rather than hardcoding one vendor.\n \n ## Decision\n@@ -39,7 +39,7 @@\n   the task to review or repair-needed.\n - Keep Codex registered as an optional implementer runtime. `shiki runner\n   codex`, the Codex handoff templates, and the ChatGPT OAuth auth model from\n-  ADR 0005 remain supported for tasks explicitly assigned to `codex`.\n+  SADR-0005 remain supported for tasks explicitly assigned to `codex`.\n - Commit to mandating two Claude-native capabilities as loop contract in\n   follow-up Goals (status: the code-review gate was delivered by Goal G-B \u2014\n   skill registry, checklists item PR-12, default required_skills \u2014 while the\n@@ -60,7 +60,7 @@\n \n Out of scope: removing Codex support, changing the CCA runtime (Claude Code\n Action with `CLAUDE_CODE_OAUTH_TOKEN` stays), and API-key based automation\n-(still requires its own ADR per ADR 0005).\n+(still requires its own target ADR per SADR-0005).\n \n ## Consequences\n \n@@ -100,7 +100,7 @@\n   harness rejection of direct `codex exec` is outside Shiki's control, and the\n   plan's mandatory native capabilities cannot run on Codex at all.\n - **Remove Codex entirely**: rejected; it abandons the runtime-agnostic\n-  constitution (ADR 0002) for no operational gain and forecloses\n+  constitution (SADR-0002) for no operational gain and forecloses\n   cross-runtime adversarial review.\n - **GitHub Actions cloud implementation via claude-code-action**: rejected as\n   default; durable evidence is automatic but token cost, Node 24 workflow\ndiff --git a/docs/adr/0009-requirements-definition-and-spec-freeze.md b/docs/adr/SADR-0009-requirements-definition-and-spec-freeze.md\nindex 29b3f7871bef9203a8b4a831a65c39eb2f506e42..52e7dd69ad0aa4efe0b68c62678abbf7df02aaec\n--- a/docs/adr/0009-requirements-definition-and-spec-freeze.md\n+++ b/docs/adr/SADR-0009-requirements-definition-and-spec-freeze.md\n@@ -1,4 +1,4 @@\n-# ADR 0009: Merge Grill And Planning Into Requirements Definition With Spec Freeze\n+# SADR-0009: Merge Grill And Planning Into Requirements Definition With Spec Freeze\n \n ## Status\n \ndiff --git a/docs/adr/0010-external-ai-guardian-review.md b/docs/adr/SADR-0010-external-ai-guardian-review.md\nindex e6108bbe8bb4e73e0ff4684c8f78001a80438c4e..f3510b145664298adafb151b6f0db51b972f01ec\n--- a/docs/adr/0010-external-ai-guardian-review.md\n+++ b/docs/adr/SADR-0010-external-ai-guardian-review.md\n@@ -1,4 +1,4 @@\n-# ADR 0010: External AI Guardian Review As A First-Class Approval Authority\n+# SADR-0010: External AI Guardian Review As A First-Class Approval Authority\n \n ## Status\n \n@@ -8,7 +8,7 @@\n \n Shiki is intended to be an autonomous, recursive, self-improving agent\n framework: authority-in-the-loop, not human-in-the-loop. Until now the Guardian\n-approval required for high/critical-risk merges (guardian-policy.json, ADR\n+approval required for high/critical-risk merges (guardian-policy.json, SADR\n governance) recognized only human authority sources \u2014 a GitHub review approval,\n the `guardian:approved` label applied by a configured Guardian, or a comment\n carrying `Guardian approval granted` plus the current head SHA.\n@@ -89,7 +89,7 @@\n   autonomous design the operator intends and produced the impersonation\n   incident.\n - **Record AI approval as an operator comment**: rejected as forgery of human\n-  authority \u2014 the exact failure this ADR corrects.\n+  authority \u2014 the exact failure this SADR corrects.\n - **Deliver the artifact as a committed `.shiki/` file**: rejected; a committed\n   approval inside the PR being approved is forgeable by anyone who can push.\n - **A dedicated reviewer bot identity instead of a relayed comment**: deferred;\ndiff --git a/docs/adr/0011-autonomous-quality-gate-evidence.md b/docs/adr/SADR-0011-autonomous-quality-gate-evidence.md\nindex bca0246c5d46e44afbde15486a75eec425fc02f5..9c93eac16d8c3a6d4b11bd7224e3955102cd7db4\n--- a/docs/adr/0011-autonomous-quality-gate-evidence.md\n+++ b/docs/adr/SADR-0011-autonomous-quality-gate-evidence.md\n@@ -1,4 +1,4 @@\n-# ADR 0011: Autonomous Quality-Gate Evidence \u2014 Deterministic Facts And An Independent Verifier\n+# SADR-0011: Autonomous Quality-Gate Evidence \u2014 Deterministic Facts And An Independent Verifier\n \n ## Status\n \ndiff --git a/docs/adr/0012-autonomous-loop-closeout-to-main.md b/docs/adr/SADR-0012-autonomous-loop-closeout-to-main.md\nindex 637fad6dde4989c9c2bf0f81f676d8d29991123c..ec7d85f81030a676bcbba28b43eb365422a36eac\n--- a/docs/adr/0012-autonomous-loop-closeout-to-main.md\n+++ b/docs/adr/SADR-0012-autonomous-loop-closeout-to-main.md\n@@ -1,4 +1,4 @@\n-# 0012 \u2014 Autonomous loop pushes goal completion to main via a closeout PR\n+# SADR-0012 \u2014 Autonomous loop pushes goal completion to main via a closeout PR\n \n - Status: accepted\n - Date: 2026-06-18\ndiff --git a/docs/adr/0013-bootstrap-grants-actions-approve-pull-requests.md b/docs/adr/SADR-0013-bootstrap-grants-actions-approve-pull-requests.md\nindex b6c69d64f1c5ced346b88d6859586715b3b5bd0e..cf48ff4a6716267f539ca58edd1a852e7c7a2d64\n--- a/docs/adr/0013-bootstrap-grants-actions-approve-pull-requests.md\n+++ b/docs/adr/SADR-0013-bootstrap-grants-actions-approve-pull-requests.md\n@@ -1,4 +1,4 @@\n-# ADR 0013: Bootstrap Grants GitHub Actions Permission To Create And Approve Pull Requests\n+# SADR-0013: Bootstrap Grants GitHub Actions Permission To Create And Approve Pull Requests\n \n ## Status\n \n@@ -31,7 +31,7 @@\n security tradeoff: it lets repository automation satisfy a human-review gate. In\n a multi-maintainer repo that could let a compromised or buggy workflow\n self-approve changes. So whether bootstrap turns it on by default \u2014 and with\n-what default token scope \u2014 is an ADR-worthy decision, not an implementation\n+what default token scope \u2014 is a SADR-worthy decision, not an implementation\n detail.\n \n ## Decision\n@@ -48,7 +48,7 @@\n Specifically:\n \n - **`can_approve_pull_request_reviews` defaults to `true`.** Shiki's default\n-  posture is solo/AFK autonomous operation (ADR 0008/0009/0012), where the CCA\n+  posture is solo/AFK autonomous operation (SADR-0008/0009/0012), where the CCA\n   Review Bridge is the only path that can satisfy `required_review: true` after\n   the CCA and Guardian gates have already passed. The default makes a\n   freshly-bootstrapped repo able to run the autonomous loop end to end without a\ndiff --git a/docs/adr/0014-codex-app-external-ai-guardian-ui-adapter.md b/docs/adr/SADR-0014-codex-app-external-ai-guardian-ui-adapter.md\nindex 238db06279334caa341c117362df9271382a7fcd..65f12e9939321e1b3e389a34c822fc1549e5155d\n--- a/docs/adr/0014-codex-app-external-ai-guardian-ui-adapter.md\n+++ b/docs/adr/SADR-0014-codex-app-external-ai-guardian-ui-adapter.md\n@@ -1,4 +1,4 @@\n-# ADR 0014: Use Codex App As The External AI Guardian UI Adapter\n+# SADR-0014: Use Codex App As The External AI Guardian UI Adapter\n \n ## Status\n \ndiff --git a/docs/adr/0015-contract-approval-before-dispatch.md b/docs/adr/SADR-0015-contract-approval-before-dispatch.md\nindex fad6c656741a20a7684ba16080a7db159f63a52f..6061271a46d3f1d6ae7174477de3fb75673123a6\n--- a/docs/adr/0015-contract-approval-before-dispatch.md\n+++ b/docs/adr/SADR-0015-contract-approval-before-dispatch.md\n@@ -1,4 +1,4 @@\n-# ADR 0015: Approve The Task Contract Before Dispatch, Not The Diff After It\n+# SADR-0015: Approve The Task Contract Before Dispatch, Not The Diff After It\n \n ## Status\n \n@@ -9,7 +9,7 @@\n Guardian approval for high/critical risk is expressible only on a pull request.\n Every approval source in `guardian-policy.json` \u2014 GitHub review, the\n `guardian:approved` label, the `Guardian approval granted` comment, and the\n-`external_ai_guardian_review` artifact (ADR 0010/0014) \u2014 is bound to a PR, and\n+`external_ai_guardian_review` artifact (SADR-0010/0014) \u2014 is bound to a PR, and\n three of the four require the current head SHA. Approval therefore cannot exist\n before an implementation PR exists.\n \n@@ -81,7 +81,7 @@\n An envelope breach must surface only on the non-repairable policy gate, never on\n the repairable `MergeGate metadata check`. Otherwise the Goal Loop routes it to\n `dispatch_repair` \u2014 instructing a runner to make the breach go away \u2014 which is\n-exactly what this ADR forbids.\n+exactly what this SADR forbids.\n \n **Contract immutability.** After registration, an implementation PR must not\n change the governance fields of its own task contract (`scope`, `non_goals`,\n@@ -116,7 +116,7 @@\n fails any verdict that is not `complete`, and the signal the CCA prompt reads is\n computed from `evaluate_guardian_approval` alone. Without the OR-merge at the\n signal, a contract-approved PR still returns `needs_guardian`, the CCA job fails,\n-and the MergeGate policy check never runs \u2014 the gate this ADR changes would never\n+and the MergeGate policy check never runs \u2014 the gate this SADR changes would never\n be reached. The CCA *prompt* needs no change: a `contract_approval` entry in the\n signal's existing `sources` array satisfies its current wording.\n \n@@ -130,7 +130,7 @@\n written, and an approved contract lets its implementation PRs merge through the\n same automatic path as low/medium risk. The wasted implement-then-block cycle\n disappears. Guardian review shifts from reading diffs to reading contracts, which\n-is both earlier and smaller; the external AI Guardian path (ADR 0010/0014) works\n+is both earlier and smaller; the external AI Guardian path (SADR-0010/0014) works\n on contracts with no change.\n \n We accept that the Guardian authorizes a class of change without seeing its\n@@ -146,7 +146,7 @@\n adversarial tests for the new approval path equivalent in rigor to\n `test_shiki_governance_evidence.sh`.\n \n-Bootstrap costs, which apply only while this ADR is being implemented. The tasks\n+Bootstrap costs, which apply only while this SADR is being implemented. The tasks\n that build Contract Approval merge under the existing rules, so the ones\n dispatched after the pre-dispatch gate lands but before the approval path lands\n need a Guardian-approved Contract PR *and* a live Guardian approval on their\n@@ -210,7 +210,7 @@\n risk; they do not bound authorized work.\n \n **Auto-escalate risk on envelope breach instead of stopping.** Returns the\n-workflow to a diff-time Guardian decision \u2014 the exact behavior this ADR removes.\n+workflow to a diff-time Guardian decision \u2014 the exact behavior this SADR removes.\n \n **Resolve governance fields from the base snapshot only.** Rejected against the\n registration flow: implementation PRs add their own contract, so the base has no\ndiff --git a/docs/adr/0016-locks-bound-product-paths.md b/docs/adr/SADR-0016-locks-bound-product-paths.md\nindex b5e850f3f1f7f4ef6e7f9d1763a29a51bd300c45..46b40a9b99a7aae644c02bb458f5473ff0a90ce2\n--- a/docs/adr/0016-locks-bound-product-paths.md\n+++ b/docs/adr/SADR-0016-locks-bound-product-paths.md\n@@ -1,4 +1,4 @@\n-# ADR 0016: Locks bound product paths; the Shiki mirror is bound by record identity\n+# SADR-0016: Locks bound product paths; the Shiki mirror is bound by record identity\n \n ## Status\n \n@@ -6,8 +6,8 @@\n \n - Date: 2026-07-28\n - Deciders: operator (mizutani-140), Claude (planner)\n-- Related: ADR 0004 (GitHub-first with a `.shiki` mirror), ADR 0009 (Spec Freeze),\n-  ADR 0012 (autonomous loop closeout to main), ADR 0015 (Contract Approval before\n+- Related: SADR-0004 (GitHub-first with a `.shiki` mirror), SADR-0009 (Spec Freeze),\n+  SADR-0012 (autonomous loop closeout to main), SADR-0015 (Contract Approval before\n   dispatch), remediation goal task R-02, the live PR #179 deadlock.\n \n ## Context\n@@ -90,11 +90,11 @@\n files, documentation files and named source modules are nameable; runtime-\n allocated ledger, runner and report ids are not \u2014 and those live in `.shiki`,\n which locks no longer govern. Existing frozen contracts are brought into line by\n-a one-time Spec Amendment per plan, which must land before ADR 0015's contract\n+a one-time Spec Amendment per plan, which must land before SADR-0015's contract\n immutability makes `locks` unwritable from the implementation PR.\n \n **Lock state is resolved from the durable side.** MergeGate resolves the lock\n-registry, and ADR 0015's governance fields, from the base `.shiki` snapshot when\n+registry, and SADR-0015's governance fields, from the base `.shiki` snapshot when\n present, falling back to head only when absent. The `MergeGate metadata check`\n job is given `--base-shiki`, which it does not receive today. Locks are not\n derived from the live open-pull-request set: a pull request body is untrusted\n@@ -108,7 +108,7 @@\n Explicitly out of scope: moving MergeGate's contract resolution off the PR\n checkout wholesale; making the mirror a projection of GitHub state; introducing\n new lock kinds; moving ledger evidence out of the repository; and unwinding\n-ADR 0012's closeout pull request. Registration of multi-task goals is ADR 0015's\n+SADR-0012's closeout pull request. Registration of multi-task goals is SADR-0015's\n Contract PR and is not re-decided here.\n \n ## Consequences\n@@ -127,7 +127,7 @@\n \n Risks accepted. First, goal-scoped identity is only as strong as `goal_id`,\n which is resolved from the PR body and the head task file; the mitigation is to\n-add `goal_id` to ADR 0015's frozen governance field set, and until that lands a\n+add `goal_id` to SADR-0015's frozen governance field set, and until that lands a\n task file can name a foreign goal. Second, base-when-present resolution is\n unavailable on fork pull requests, where the head is contributor-controlled;\n fork pull requests already cannot complete a Shiki merge, and this decision does\n@@ -139,7 +139,7 @@\n path, not by implementation pull requests.\n \n New obligations: one Spec Amendment per frozen plan to rewrite `locks`, ordered\n-before ADR 0015 T1; extension of the adversarial suites in\n+before SADR-0015 T1; extension of the adversarial suites in\n `scripts/test_shiki_adversarial_state.sh` and\n `scripts/test_shiki_governance_evidence.sh` to cover the eleven newly-scoped\n directories; and updating `scripts/test_shiki_loop_lock_guard.sh`,\n@@ -151,32 +151,32 @@\n whether the loop's transitive sync of ledger `evidence` references can be\n deleted once the Contract PR carries its own source plan.\n \n-## Relationship to ADR 0004\n+## Relationship to SADR-0004\n \n-This ADR **clarifies something ADR 0004 left unspecified**. ADR 0004 decides that\n+This SADR **clarifies something SADR-0004 left unspecified**. SADR-0004 decides that\n GitHub is the operational source of truth and that each target repository keeps a\n `.shiki/` mirror. It says nothing about pull requests writing the mirror, does\n not name MergeGate, and does not make the mirror an input to a gate. Nothing in\n-ADR 0004's Decision or Consequences is amended, superseded or weakened: the\n+SADR-0004's Decision or Consequences is amended, superseded or weakened: the\n mirror still exists, still records the same artifacts, and pull request\n-boundaries remain the coordination surface. What this ADR settles is the question\n+boundaries remain the coordination surface. What this SADR settles is the question\n 0004 never reached \u2014 by what authority a pull request may write a mirror record \u2014\n and it answers it the way `.shiki/manifest.json` already declares\n (`pr_mutation: current-task-or-current-goal-only`).\n \n-It **amends ADR 0012**, whose Decision item 5 states that \"Loop-executed tasks\n+It **amends SADR-0012**, whose Decision item 5 states that \"Loop-executed tasks\n declare the `path:.shiki/**` lock, which covers every `.shiki` file the closeout\n stages ... so `files_outside_locks` is satisfied without per-file lock\n bookkeeping.\" That clause is replaced: the closeout's mirror writes are\n-authorized by record identity, not by a declared lock. ADR 0012's substantive\n+authorized by record identity, not by a declared lock. SADR-0012's substantive\n decision \u2014 that completion reaches `main` through a closeout pull request rather\n than local attestation \u2014 is preserved unchanged.\n \n-It is **complementary to ADR 0015**, not a substitute. ADR 0015 owns contract\n+It is **complementary to SADR-0015**, not a substitute. SADR-0015 owns contract\n immutability, the pre-dispatch gate, the Contract PR and the Scope Envelope. This\n-ADR owns what a lock means and who may write the mirror. The one ordering\n+SADR owns what a lock means and who may write the mirror. The one ordering\n constraint between them is that the lock-narrowing Spec Amendment must precede\n-ADR 0015 T1's freezing of the `locks` field.\n+SADR-0015 T1's freezing of the `locks` field.\n \n ## Alternatives Considered\n \n@@ -186,7 +186,7 @@\n the only writer that can register them may run. Migrating the Guardian signal to\n that store makes every registration PR fail closed to `needs_guardian` at every\n risk level, because the task file is by definition absent from the store, which\n-removes the low/medium autonomous path ADR 0008/0009 exists to provide. And it\n+removes the low/medium autonomous path SADR-0008/0009 exists to provide. And it\n leaves schemas, manifest, config, guardian policy, templates, migrations, plans,\n worktrees, runner records, runs and handoffs with no writer at all in a\n repository whose product is `.shiki`. Measured benefit on the frozen backlog:\n@@ -199,14 +199,14 @@\n namespace is not in fact ownership-partitioned \u2014 56 ledger entries carry no\n `task_id`, and one goal DAG is writable by 35 registered tasks. The correct\n expression of its insight is that locks do not govern the mirror at all, which is\n-what this ADR decides.\n+what this SADR decides.\n \n **Make the mirror a projection written only by a registration writer and a\n post-merge projection job.** Rejected. It requires a bypass actor with write\n access to the protected default branch; it makes its own master invariant\n readable from a manifest that pull requests may edit; it deletes both reconcile\n modes and leaves a corrupted mirror with no in-band repair path, contradicting\n-ADR 0004's consequence that mirror conflicts are repaired against GitHub state;\n+SADR-0004's consequence that mirror conflicts are repaired against GitHub state;\n and its content-derived ids are rejected by `validate_shiki`'s currently-green id\n pattern.\n \ndiff --git a/docs/adr/0017-bookkeeping-closeouts-do-not-inherit-risk.md b/docs/adr/SADR-0017-bookkeeping-closeouts-do-not-inherit-risk.md\nindex 554e3a0d526a72ca067108c5464cc68c3edcf174..b9676857a4713850e063af7c39c8e0b01ecb4c66\n--- a/docs/adr/0017-bookkeeping-closeouts-do-not-inherit-risk.md\n+++ b/docs/adr/SADR-0017-bookkeeping-closeouts-do-not-inherit-risk.md\n@@ -1,4 +1,4 @@\n-# ADR 0017: A Bookkeeping Closeout Does Not Inherit Its Task's Risk\n+# SADR-0017: A Bookkeeping Closeout Does Not Inherit Its Task's Risk\n \n ## Status\n \n@@ -6,7 +6,7 @@\n \n ## Context\n \n-ADR 0012 made the goal loop push a task's completion to the default branch through a\n+SADR-0012 made the goal loop push a task's completion to the default branch through a\n closeout pull request: after the implementation PR merges, the loop opens a second PR\n carrying `task.status = done`, `lock.state = released`, and \u2014 when the task completes\n its goal \u2014 `goal.status = complete` plus one scorecard report. Nothing else. The\n@@ -49,14 +49,14 @@\n 4. The task file's governance fields (`scope`, `non_goals`, `required_skills`,\n    `risk_level`, `locks`, `acceptance_checks`, `test_command`) are byte-identical to\n    the base snapshot. Only `status`, `expected_pr`, `closeout_pr`, `ledger_evidence`\n-   and `expected_branch` may differ \u2014 the ADR 0015 contract-immutability rule,\n+   and `expected_branch` may differ \u2014 the SADR-0015 contract-immutability rule,\n    unchanged.\n \n    `expected_branch` is in that set because a closeout is *by construction* on a\n    different branch from the implementation it closes, and the loop rewrites the\n    field accordingly. It is safe to let it differ because MergeGate independently\n    requires `expected_branch` to equal the PR's actual head ref, so a PR cannot set\n-   it to anything other than the branch it is really on. The first draft of this ADR\n+   it to anything other than the branch it is really on. The first draft of this SADR\n    omitted it, which made the exemption unreachable: every real closeout failed\n    condition 4 on a field the loop always changes. See PR #209.\n 5. The state transitions are exactly the terminal set: task `review` \u2192 `done`, lock\n@@ -113,12 +113,12 @@\n classification must be derived, not asserted.\n \n **Fold the closeout into the implementation PR** so no second PR exists. Rejected under\n-ADR 0012, which recorded why: a task file that is already `done` on the implementation\n+SADR-0012, which recorded why: a task file that is already `done` on the implementation\n branch short-circuits the loop's review \u2192 CCA \u2192 merge \u2192 done ordering into a local\n goal-completion before any merge happens.\n \n **Have the loop auto-merge closeouts with elevated credentials.** Rejected outright: that\n-is a runtime authoring its own approval, which ADR 0010 and the constitution's\n+is a runtime authoring its own approval, which SADR-0010 and the constitution's\n approval-record prohibition forbid. It would also be indistinguishable, in the evidence,\n from the forgery those rules exist to prevent.\n \ndiff --git a/docs/adr/0018-carried-guardian-approval-across-a-proven-base-sync.md b/docs/adr/SADR-0018-carried-guardian-approval-across-a-proven-base-sync.md\nindex 22358562da390ed8b45cd600da08a43d378b4680..e6e2907cefa776460af73ddf7d39db605f3c39b0\n--- a/docs/adr/0018-carried-guardian-approval-across-a-proven-base-sync.md\n+++ b/docs/adr/SADR-0018-carried-guardian-approval-across-a-proven-base-sync.md\n@@ -1,4 +1,4 @@\n-# ADR 0018: Carried Guardian Approval Across A Proven Base Sync\n+# SADR-0018: Carried Guardian Approval Across A Proven Base Sync\n \n ## Status\n \n@@ -34,7 +34,7 @@\n Guardian evaluator that consumes it (`shiki_guardian.evaluate_guardian_approval`,\n the `guardian_comment_carried` source) were built and tested in isolation. Neither\n does anything until a caller passes the flags that turn the carry on, so the\n-feature was invisible in CI. This ADR records the decision the two of them\n+feature was invisible in CI. This SADR records the decision the two of them\n implement, and this task wires it into the four CI invocations that judge Guardian\n approval.\n \ndiff --git a/docs/adr/README.md b/docs/adr/README.md\nindex c70e057a4520c36e042ab68c53cd4a21ef709b71..5da43856836e2ce6a150f8127766e2f88355ab4b\n--- a/docs/adr/README.md\n+++ b/docs/adr/README.md\n@@ -1,40 +1,41 @@\n-# Architecture Decision Records\n+# Decision Record Namespaces\n \n-This directory holds Shiki's Architecture Decision Records (ADRs): durable\n-records of hard-to-reverse platform decisions. ADRs are part of the Shiki source\n-of truth alongside GitHub state, the `.shiki/` mirror, and `CONTEXT.md`.\n+This directory holds two independent decision namespaces:\n \n-## How ADRs Work\n+- Shiki Architecture Decision Records (SADRs), named\n+  `SADR-NNNN-<short-slug>.md`, record hard-to-reverse Shiki platform decisions.\n+- Target Architecture Decision Records (ADRs), named\n+  `NNNN-<short-slug>.md`, belong to the target product.\n \n-- New ADRs are added as `docs/adr/NNNN-<short-slug>.md`, where `NNNN` is the next\n-  zero-padded sequence number.\n-- The **canonical list of ADRs is the set of files matching `docs/adr/*.md`** in\n-  this directory. Browse `docs/adr/*.md` for the authoritative, current set.\n-- Use [`template.md`](template.md) as the starting point for a new ADR.\n-- Record an ADR when a decision is hard to reverse, surprising without context,\n-  and a real tradeoff. Do not write an ADR for routine or easily reversible\n-  choices.\n+The same four-digit number may appear once in each namespace. A platform\n+`SADR-NNNN` citation resolves only to `SADR-NNNN-*.md`; a target `ADR NNNN`\n+citation resolves only to `NNNN-*.md`.\n \n-Using the `docs/adr/*.md` glob as the source of truth means other PRs can add an\n-ADR without editing this index file, avoiding merge contention on a\n-hand-maintained enumeration.\n+## How Decision Records Work\n \n-## Current ADRs (snapshot)\n-\n-The list below is a convenience snapshot and may lag behind the directory. When\n-in doubt, list `docs/adr/*.md`.\n+- New Shiki platform decisions use the next zero-padded SADR number.\n+- New target product decisions use the target's own ADR sequence.\n+- The canonical list is the files in `docs/adr/`; no hand-maintained index is\n+  authoritative.\n+- Use [`template.md`](template.md) as the starting point and choose the filename\n+  namespace that owns the decision.\n+- Record a decision when it is hard to reverse, surprising without context, and\n+  a real tradeoff. Do not write a decision record for routine or easily\n+  reversible choices.\n \n-- [0001 \u2014 Keep Shiki As The Platform Name](0001-keep-shiki-as-platform-name.md)\n-- [0002 \u2014 Use AGENTS As The Shared Agent Constitution](0002-use-agents-as-shared-constitution.md)\n-- [0003 \u2014 Require Skill Gate For MergeGate Readiness](0003-require-skill-gate-for-mergegate-readiness.md)\n-- [0004 \u2014 Use GitHub First With A Repository-Local Shiki Mirror](0004-use-github-first-with-shiki-mirror.md)\n-- [0005 \u2014 Use Codex As Front And Claude Code Action As The GitHub Runtime](0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md)\n-- [0006 \u2014 Separate Codex Implementation, CCA Completion, And MergeGate Authorization](0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md)\n+Using the directory as the source of truth lets independent changes add records\n+without contending on an enumerated index.\n \n-## Adding A New ADR\n+## Adding A Shiki SADR\n \n-1. Copy `template.md` to `docs/adr/NNNN-<short-slug>.md`.\n+1. Copy `template.md` to `docs/adr/SADR-NNNN-<short-slug>.md`.\n 2. Fill in the title, status, context, decision, and consequences.\n-3. Set the status to `Proposed`, then `Accepted` once the decision is approved.\n-4. Open a PR following `CONTRIBUTING.md`. You do not need to edit this index for\n-   the ADR to be discoverable via `docs/adr/*.md`.\n+3. Set the status to `Proposed`, then `Accepted` once the proper authority has\n+   approved the decision.\n+4. Open a PR following `CONTRIBUTING.md`.\n+\n+## Adding A Target ADR\n+\n+1. Copy `template.md` to `docs/adr/NNNN-<short-slug>.md` using the target's own\n+   sequence.\n+2. Cite it as `ADR NNNN`; do not prefix its filename with `SADR-`.\ndiff --git a/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md b/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\nnew file mode 100644\nindex 0000000000000000000000000000000000000000..7455ce1f3bed02ddddb851ac21a1d1a648c9aeba\n--- /dev/null\n+++ b/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\n@@ -0,0 +1,55 @@\n+# SADR-0019: Separate Shiki Platform SADRs From Target ADRs\n+\n+## Status\n+\n+Accepted\n+\n+## Context\n+\n+Shiki installs its platform decision records into `docs/adr/` in every target\n+repository. Shiki and its targets previously used the same numeric\n+`NNNN-<slug>.md` namespace. A target that already owned ADRs 0013 through 0018\n+therefore acquired two records for each number during installation, making an\n+`ADR NNNN` citation ambiguous. Renumbering target decisions would rewrite target\n+history, while renumbering Shiki decisions per target would destroy their stable\n+platform identity.\n+\n+Upgrades also need to remove the former Shiki-owned numeric paths without ever\n+mistaking a target-owned ADR for platform content. The install stamp already\n+records exact shipped paths and content digests, so it can prove ownership only\n+when both still match.\n+\n+## Decision\n+\n+Shiki platform decisions use the `SADR` namespace:\n+\n+- records are named `docs/adr/SADR-NNNN-<slug>.md`;\n+- live platform citations use `SADR-NNNN`;\n+- target product decisions retain `docs/adr/NNNN-<slug>.md` and `ADR NNNN`;\n+- the reference validator resolves the two namespaces independently, so the same\n+  four-digit number may exist once in each namespace.\n+\n+Before a forced upgrade writes anything, the installer inventories the exact\n+former numeric Shiki ADR paths. It may delete a legacy path only when the\n+existing install stamp contains that exact path and its recorded SHA-256 digest\n+matches the file currently on disk. All ownership blockers are reported\n+together and abort the upgrade before its first write. A non-forced install\n+never performs this cleanup.\n+\n+Historical `.shiki/` evidence remains unchanged because it records the namespace\n+that existed when that evidence was produced.\n+\n+## Consequences\n+\n+- Targets may use any ADR number without colliding with Shiki's platform SADRs.\n+- Fresh installs preserve target ADR bytes and install a stable SADR set.\n+- Forced upgrades fail closed when legacy ownership cannot be proven, and a\n+  successful upgrade refreshes the stamp without removed legacy paths.\n+- Current governance, packaging, workflow, prompt, script, and skill references\n+  must move with the platform records.\n+\n+## Alternatives Considered\n+\n+- Collision-only refusal leaves affected targets permanently unupgradable.\n+- Renumbering target ADRs mutates target-owned history.\n+- Per-target Shiki renumbering makes platform citations non-portable.\ndiff --git a/docs/adr/template.md b/docs/adr/template.md\nindex a92a97daddd0b00ab9dd626cc8d86942da2eafed..17dd79175b5fee2105935557f691b40cf9492942\n--- a/docs/adr/template.md\n+++ b/docs/adr/template.md\n@@ -1,14 +1,14 @@\n-# NNNN: <Short Decision Title>\n+# <SADR-NNNN or NNNN>: <Short Decision Title>\n \n ## Status\n \n-Proposed | Accepted | Superseded by ADR-NNNN\n+Proposed | Accepted | Superseded by SADR-NNNN or target ADR NNNN\n \n ## Context\n \n What is the situation, constraint, or force that requires a decision? Reference\n-relevant domain terms (`CONTEXT.md`), prior ADRs, and code reality. State the\n-problem, not the solution.\n+relevant domain terms (`CONTEXT.md`), prior Shiki SADRs or target ADRs, and code\n+reality. State the problem, not the solution.\n \n ## Decision\n \ndiff --git a/docs/agents/autonomous-loop-e2e-acceptance.md b/docs/agents/autonomous-loop-e2e-acceptance.md\nindex bbf84c6081987f159ac6ad9ecf3c82b7aa72e7e8..5c13c1d02a05a90b0dc46cbef7c95a9846d0c237\n--- a/docs/agents/autonomous-loop-e2e-acceptance.md\n+++ b/docs/agents/autonomous-loop-e2e-acceptance.md\n@@ -40,8 +40,8 @@\n 3. `gh auth status` is logged in with merge permission on the repo.\n 4. Branch protection + required checks are configured exactly as the goal-loop\n    required-check set expects (see `docs/agents/branch-protection-smoke.md`).\n-5. The repo `.shiki/policy` permits autonomous low/medium-risk merges (ADR\n-   0008/0009); the MergeGate policy check is the recorded-authority gate.\n+5. The repo `.shiki/policy` permits autonomous low/medium-risk merges\n+   (SADR-0008/0009); the MergeGate policy check is the recorded-authority gate.\n \n The operator's only actions are: register the task and start the loop. **No\n manual pre-clearing** means after `shiki loop run` starts, the operator does not\ndiff --git a/docs/agents/bootstrap-command.md b/docs/agents/bootstrap-command.md\nindex 05f7f203a52cf412b9f97084a6e131ed0f785afd..f7aa0b734dfebee4a3b139335b66ee3415f48713\n--- a/docs/agents/bootstrap-command.md\n+++ b/docs/agents/bootstrap-command.md\n@@ -189,7 +189,9 @@\n ```\n \n Use this only for tests, fixtures, or explicit local-only template inspection.\n-Use `--force` only when you intentionally want to overwrite existing target files.\n+Use `--force` only when you intentionally want to refresh the target template.\n+Legacy numeric Shiki decision cleanup is stamp-and-digest authorized before any\n+write; target ADR paths are not cleanup candidates.\n \n ## Slash Command\n \ndiff --git a/docs/agents/checklists.md b/docs/agents/checklists.md\nindex 4a7f03ade346f53a1c1d82981f3fd744f9ab4547..0c5ec6c53dc7197b7b7a7d18e667b325babd6c45\n--- a/docs/agents/checklists.md\n+++ b/docs/agents/checklists.md\n@@ -32,18 +32,18 @@\n | ID | Item | Blocking |\n | --- | --- | --- |\n | GD-01 | Plan was challenged against `CONTEXT.md`. | blocking for non-trivial Goals |\n-| GD-02 | Relevant ADRs were read or absence was recorded. | blocking for non-trivial Goals |\n+| GD-02 | Relevant SADRs and target ADRs were read or absence was recorded. | blocking for non-trivial Goals |\n | GD-03 | Ambiguous terms were resolved or converted to HITL blockers. | blocking |\n | GD-04 | Edge scenarios were considered. | advisory |\n | GD-05 | Code reality was checked when code could answer. | blocking when applicable |\n | GD-06 | New domain terms were added to `CONTEXT.md` when needed. | blocking when applicable |\n-| GD-07 | ADR was created or explicitly skipped using the ADR criteria. | blocking when applicable |\n+| GD-07 | A SADR or target ADR was created or explicitly skipped using the decision-record criteria. | blocking when applicable |\n \n ## CI \u2014 Context & Impact Checklist\n \n | ID | Item | Blocking |\n | --- | --- | --- |\n-| CI-01 | Relevant docs and ADRs identified. | blocking |\n+| CI-01 | Relevant docs, SADRs, and target ADRs identified. | blocking |\n | CI-02 | Relevant modules, interfaces, seams, callers, and tests identified. | blocking |\n | CI-03 | Dependencies and blocked-by relationships identified. | blocking |\n | CI-04 | Candidate locks identified. | blocking |\n@@ -62,7 +62,7 @@\n | PRD-04 | Implementation decisions are recorded without volatile code detail. | blocking |\n | PRD-05 | Testing decisions are recorded. | blocking |\n | PRD-06 | Out-of-scope items are explicit. | blocking |\n-| PRD-07 | Relevant glossary/ADR links are present when applicable. | blocking |\n+| PRD-07 | Relevant glossary/SADR/target-ADR links are present when applicable. | blocking |\n \n ## SF \u2014 Spec Freeze Checklist\n \n@@ -98,7 +98,7 @@\n | H-02 | Branch/worktree target is present. | blocking |\n | H-03 | Scope and non-goals are present. | blocking |\n | H-04 | Dependencies and locks are present. | blocking |\n-| H-05 | Relevant docs, ADRs, modules, and tests are referenced. | blocking |\n+| H-05 | Relevant docs, SADRs, target ADRs, modules, and tests are referenced. | blocking |\n | H-06 | Required skills are stated. | blocking |\n | H-07 | TDD expectations are stated. | blocking when behavior work |\n | H-08 | Acceptance criteria are included. | blocking |\ndiff --git a/docs/agents/codex-handoff.md b/docs/agents/codex-handoff.md\nindex ede468f47cb277f69a857e8c45df703e4ed0151d..6f97bdffb67e44d7edea2c71e01ea28933c4eb6c\n--- a/docs/agents/codex-handoff.md\n+++ b/docs/agents/codex-handoff.md\n@@ -46,7 +46,7 @@\n \n ## Context\n - Domain terms:\n-- Relevant ADRs:\n+- Relevant SADRs and target ADRs:\n - Relevant docs:\n - Relevant modules/interfaces/seams:\n - Similar tests/prior art:\ndiff --git a/docs/agents/completion-check-agent.md b/docs/agents/completion-check-agent.md\nindex 13e4c035db35025f4e3cdcc5adfb24314ac4d521..61bfa1218cf36ca605250c91919e620f33238aa0\n--- a/docs/agents/completion-check-agent.md\n+++ b/docs/agents/completion-check-agent.md\n@@ -32,7 +32,7 @@\n - `AGENTS.md`\n - `CLAUDE.md`\n - `CONTEXT.md`\n-- relevant ADRs\n+- relevant SADRs and target ADRs\n - parent Goal issue\n - PRD issue or document\n - task issue\ndiff --git a/docs/agents/decision-control.md b/docs/agents/decision-control.md\nindex 7d8ee5ab03a5ccbcbd04056ae61de03785a2a9c9..8c94ecda00fffaa0e92c325ab83aa9776bd1dac1\n--- a/docs/agents/decision-control.md\n+++ b/docs/agents/decision-control.md\n@@ -11,7 +11,7 @@\n | Decision | Owner | When | Required inputs | Output | Enforcement surface |\n | --- | --- | --- | --- | --- | --- |\n | Is the Goal clear enough to plan? | Planner + Guardian when needed | Goal intake | User request, existing docs, risk signals | Goal readiness verdict | Goal checklist, issue template, ledger |\n-| Must `grill-with-docs` run? | Planner / Skill Gate | Before PRD or issue split | Ambiguity, domain terms, ADR conflicts, unclear boundaries | Skill invocation or explicit skip rationale | Skill Gate checklist |\n+| Must `grill-with-docs` run? | Planner / Skill Gate | Before PRD or issue split | Ambiguity, domain terms, SADR/target-ADR conflicts, unclear boundaries | Skill invocation or explicit skip rationale | Skill Gate checklist |\n | Is a PRD required? | Planner | After Goal context stabilizes | Goal, `grill-with-docs` output, Context & Impact output | PRD required / not required | PRD checklist, CCA evidence check |\n | Can the spec be frozen? | Operator only | End of Requirements Definition, before issue split | PRD/requirements summary, scope inventory (SF-02), open-question list | `spec_freeze` block in the plan + ledger evidence | `require_grilled_plan` (plan ingest/run), SF checklist |\n | Can a frozen spec change? | Operator only (Spec Amendment) | When implementation contradicts the frozen spec | Contested decisions, scoped re-grill result | recorded amendment + re-stamped freeze | SF checklist, ledger evidence |\ndiff --git a/docs/agents/domain.md b/docs/agents/domain.md\nindex 258da8ea81e87ac0a6b0b5b5e5c2fde980eaabb7..d57b0cf17ff3e1fa9e6e5885372bf083d8295421\n--- a/docs/agents/domain.md\n+++ b/docs/agents/domain.md\n@@ -5,7 +5,9 @@\n ## Layout\n \n - `CONTEXT.md` defines Shiki language. Keep it implementation-free.\n-- `docs/adr/` records hard-to-reverse decisions.\n+- `docs/adr/SADR-NNNN-*.md` records Shiki platform decisions.\n+- `docs/adr/NNNN-*.md` records target product decisions without sharing the\n+  SADR namespace.\n - `AGENTS.md` is the runtime-neutral agent constitution.\n - `CLAUDE.md` is the Claude Code wrapper around `AGENTS.md`.\n - `.shiki/` is the Target Repository mirror for Goals, PRDs, plans, Task DAGs, contracts, locks, worktree records, ledger entries, CCA verdicts, repair packets, reports, and handoffs.\n@@ -15,6 +17,6 @@\n \n - Use Shiki terms exactly when they exist.\n - Add glossary terms to `CONTEXT.md` only when the term is part of the domain, not a generic programming concept.\n-- Create ADRs only for hard-to-reverse, surprising, tradeoff-based decisions.\n+- Create SADRs or target ADRs only for hard-to-reverse, surprising, tradeoff-based decisions.\n - Keep product-specific language in the Target Repository, not in Shiki Template docs.\n-- If an ADR conflicts with a proposed implementation, surface the conflict before editing.\n+- If a SADR or target ADR conflicts with a proposed implementation, surface the conflict before editing.\ndiff --git a/docs/agents/execution-control.md b/docs/agents/execution-control.md\nindex ad8ee3cdb36be56fedc1f46408ca92b387d74047..bc63c3a73661d12b567016ba299f1a6b66bf9088\n--- a/docs/agents/execution-control.md\n+++ b/docs/agents/execution-control.md\n@@ -63,7 +63,7 @@\n | Branch name | Orchestrator | Orchestrator | Before implementer dispatch | branch policy | branch ref in task/ledger |\n | Lock reservation | Context & Impact | MergeGate | Before dispatch and before merge | `.shiki/locks/`, labels, PR metadata | lock id, owner, scope |\n | CI profile | Planner | MergeGate | During issue triage, rechecked at PR | labels, `.shiki/policy.yaml`, workflow matrix | required check list |\n-| Required status checks | Repo maintainer / Guardian | GitHub branch protection / rulesets | Repository setup, updated via ADR | GitHub settings | branch/ruleset config, ADR |\n+| Required status checks | Repo maintainer / Guardian | GitHub branch protection / rulesets | Repository setup, updated via target ADR | GitHub settings | branch/ruleset config, target ADR |\n | CCA judgment | CCA | GitHub required check / MergeGate | Every non-draft PR update | GitHub Action check | structured CCA verdict |\n | Repair assignment | CCA | MergeGate | After failed CCA/CI/review | repair packet, issue/PR comment | repair id and attempt count |\n | Merge readiness | MergeGate | GitHub branch protection / Guardian if needed | After CCA complete | required checks, review rules, rulesets | mergegate check output |\ndiff --git a/docs/agents/guardian-policy.md b/docs/agents/guardian-policy.md\nindex 4147c7adf05a47adb4fd24ada1a1c2e1f2ba6d2d..a20d9cc6c00a569bb057b9ca8d6b51c7fc4a9847\n--- a/docs/agents/guardian-policy.md\n+++ b/docs/agents/guardian-policy.md\n@@ -98,8 +98,8 @@\n `origin/<default_branch>` and reuses each job's existing base fetch; a shallow\n (`--depth`) or second base fetch would make the proof unresolvable and silently\n disable the carry, so neither is permitted. CCA reads `guardian_comment_carried` as\n-a recorded authority in the deterministic signal and does not re-derive it. See ADR\n-0018 for the full decision, the rejected diff/patch-equality and path-disjointness\n+a recorded authority in the deterministic signal and does not re-derive it. See\n+SADR-0018 for the full decision, the rejected diff/patch-equality and path-disjointness\n alternatives, and the accepted residual.\n \n ## External AI Guardian Review (SADR-0010)\ndiff --git a/docs/agents/implementation-policy.md b/docs/agents/implementation-policy.md\nindex c754144a9fb5bf4a38aa82a60e1670c59e626b44..85c37281bcad196c6b7b02343faf00b03cd9c57a\n--- a/docs/agents/implementation-policy.md\n+++ b/docs/agents/implementation-policy.md\n@@ -31,10 +31,13 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n ## Non-Negotiables\n \n 1. **Planning precedes implementation.** Non-trivial Goals start with `grill-with-docs` and Context & Impact.\n-2. **Domain language is controlled.** Terms must be checked against `CONTEXT.md`; hard-to-reverse tradeoffs must be checked against ADRs.\n+2. **Domain language is controlled.** Terms must be checked against `CONTEXT.md`; hard-to-reverse tradeoffs must be checked against Shiki SADRs and target ADRs.\n 3. **PRD precedes issue decomposition.** `to-prd` is used once the context is settled enough to become durable intent.\n 4. **Issues are vertical slices.** `to-issues` must produce independently grabbable tracer-bullet tasks, not horizontal layer tickets.\n 5. **Triage controls dispatch.** Only `ready-for-agent` / AFK tasks may be assigned to Codex without human input.\n@@ -95,19 +98,19 @@\n Process:\n \n - Challenge terms against `CONTEXT.md`.\n-- Check ADRs before reopening decisions.\n+- Check SADRs and target ADRs before reopening decisions.\n - Inspect code when code can answer a question.\n - Ask one question at a time when the operator is available.\n - Provide a recommended answer with each question.\n - Record resolved terms in `CONTEXT.md`.\n-- Create ADRs only for hard-to-reverse, surprising, tradeoff-based decisions.\n+- Create SADRs or target ADRs only for hard-to-reverse, surprising, tradeoff-based decisions.\n - Convert unresolved load-bearing questions into HITL blockers.\n \n Outputs:\n \n - Resolved design-tree decisions.\n - Updated glossary terms when needed.\n-- ADRs when justified.\n+- SADRs or target ADRs when justified.\n - HITL blockers when decisions remain unresolved.\n \n ## Phase 3 \u2014 Context & Impact\n@@ -117,7 +120,7 @@\n \n Outputs must include:\n \n-- Relevant documents and ADRs.\n+- Relevant documents, SADRs, and target ADRs.\n - Relevant modules, interfaces, seams, callers, and tests.\n - Candidate locks.\n - Dependencies.\n@@ -140,7 +143,7 @@\n - Testing decisions.\n - Out of scope.\n - Further notes.\n-- Links to relevant Goal, domain terms, and ADRs.\n+- Links to relevant Goal, domain terms, SADRs, and target ADRs.\n \n Phase 4 ends with Spec Freeze: the operator explicitly approves the PRD, the\n plan records a `spec_freeze` block (status, approved_by, source), and the SF\n@@ -240,7 +243,7 @@\n - Dependencies and locks.\n - Risk and Guardian requirements.\n - Ledger completeness.\n-- Documentation and ADR updates.\n+- Documentation and SADR/target-ADR updates.\n - Scope drift.\n \n CCA emits one of:\n@@ -301,7 +304,7 @@\n | Evidence failure | Behavior might be done, but proof is missing. | Add checks, PR evidence, ledger entry, or artifacts. |\n | Scope drift | PR changes unrelated behavior. | Revert unrelated changes or split into new Goal. |\n | Dependency block | Upstream task or lock not complete. | Block until dependency resolves. |\n-| Architecture block | Decision needs ADR or deep-module discussion. | `grill-with-docs` or `improve-codebase-architecture`. |\n+| Architecture block | Decision needs a SADR, target ADR, or deep-module discussion. | `grill-with-docs` or `improve-codebase-architecture`. |\n | Risk block | Security/production/policy change needs human approval. | Guardian decision. |\n \n ## Phase 5.5 \u2014 Execution Decision Control\ndiff --git a/docs/agents/runtime-auth-model.md b/docs/agents/runtime-auth-model.md\nindex 6a2e1a47e81ea89b5c68a210ec07772a9374babc..3c54748d24233dbd0ba8473950282547ef4ce935\n--- a/docs/agents/runtime-auth-model.md\n+++ b/docs/agents/runtime-auth-model.md\n@@ -148,7 +148,7 @@\n \n API-key based runners may be added by a target repository as an explicit extension. They are not the default Shiki template path.\n \n-If a repo opts into API-key mode, record the exception in an ADR and update the repo's `AGENTS.md`, `.shiki/config.yaml`, and workflow permissions.\n+If a repo opts into API-key mode, record the target-specific exception in a target ADR and update the repo's `AGENTS.md`, `.shiki/config.yaml`, and workflow permissions.\n \n ## GitHub Actions Runtime Compatibility\n \ndiff --git a/docs/agents/skill-gate.md b/docs/agents/skill-gate.md\nindex c50398b0d7c60a8598f2a5c88a10015730dced13..3acffe6444617bb1d54b77d4b4708158ec735bc6\n--- a/docs/agents/skill-gate.md\n+++ b/docs/agents/skill-gate.md\n@@ -9,7 +9,7 @@\n | Skill | Trigger | Expected output |\n | --- | --- | --- |\n | `setup-matt-pocock-skills` | First configuring a repo for these skills, or when issue tracker, triage labels, or domain docs are missing. | `docs/agents/` configuration and an `Agent skills` block in `AGENTS.md` or `CLAUDE.md`. |\n-| `grill-with-docs` | Ambiguous plan, unclear terminology, boundary decision, tradeoff, or ADR-worthy design choice. | Clarified decision tree, recommended answers, updates to `CONTEXT.md` or ADRs when decisions crystallize. |\n+| `grill-with-docs` | Ambiguous plan, unclear terminology, boundary decision, tradeoff, or decision-record-worthy design choice. | Clarified decision tree, recommended answers, updates to `CONTEXT.md`, SADRs, or target ADRs when decisions crystallize. |\n | `zoom-out` | Agent lacks a higher-level architectural map. | Map of relevant modules, callers, seams, and domain concepts. |\n | `to-prd` | Settled context should become a PRD. | PRD published to the issue tracker with problem, solution, user stories, decisions, testing, and out-of-scope notes. |\n | `to-issues` | Goal, PRD, or plan must become implementation tickets. | Vertical-slice issues with acceptance criteria, dependencies, and AFK/HITL classification. |\n@@ -17,7 +17,7 @@\n | `tdd` | Feature work or bug fix has observable behavior that can be specified with tests. | Red-green-refactor loop through public interfaces, one vertical slice at a time. |\n | `code-review` | The TDD loop is green and the implementer is about to create or update a PR. Default for every implementation task. | Pre-PR self-review of the task diff with fixes applied on the task branch; findings and resolutions recorded as a ledger entry naming the skill and a `## Pre-PR code review` PR body section. |\n | `diagnose` | Hard bug, failing check, regression, flaky behavior, or performance problem. | Reproduction loop, ranked hypotheses, instrumentation, fix, regression test, post-mortem. |\n-| `improve-codebase-architecture` | Structural friction, testability, AI-navigability, deep-module opportunity, or refactor request. | Architecture candidates and deepening recommendations informed by `CONTEXT.md` and ADRs. |\n+| `improve-codebase-architecture` | Structural friction, testability, AI-navigability, deep-module opportunity, or refactor request. | Architecture candidates and deepening recommendations informed by `CONTEXT.md`, SADRs, and target ADRs. |\n | `prototype` | A throwaway logic or UI prototype is needed to answer a design question. | Clearly marked disposable prototype and captured decision. |\n \n ## MergeGate Interaction\ndiff --git a/docs/operations.md b/docs/operations.md\nindex cf1f452d4c589e38ccd58553bda0ad8684b51eb7..ace48a90ffbec9e5c0a7113bef16a4858bc44b3f\n--- a/docs/operations.md\n+++ b/docs/operations.md\n@@ -89,6 +89,16 @@\n      target history, brought forward by `shiki migrate`, not a template.\n    - **Everything else** (CLI scripts, workflows, schemas, docs) is overwritten.\n \n+   A forced upgrade also migrates the former numeric Shiki decision paths to\n+   `SADR-NNNN-*`. Before the first write, the installer inventories the exact\n+   legacy Shiki paths and authorizes each deletion only when the existing\n+   `.shiki/install-stamp.json` names that path and its SHA-256 digest still\n+   matches the file. An absent, unreadable, malformed, incomplete, or\n+   mismatching stamp reports every blocked path and stops with no filesystem\n+   change. Target-owned `NNNN-*` ADRs are not cleanup candidates. A non-force\n+   install deletes nothing and retains any still-matching legacy stamp entries\n+   so a later forced upgrade can prove ownership.\n+\n    If the target has pending migrations, `--force` refuses and writes nothing \u2014\n    apply migrations first (step 3), then re-run. The run ends with a single\n    summary of every `.new` written, so a half-upgrade is visible.\ndiff --git a/docs/releasing.md b/docs/releasing.md\nindex 10d73828e7dbd64e9d9f6b83bc84d010537ce62a..aafeee8c4d441e1e427efee2ce4f81a647f020e1\n--- a/docs/releasing.md\n+++ b/docs/releasing.md\n@@ -67,7 +67,7 @@\n CLI in `scripts/`. There are no third-party Python runtime dependencies. The CLI\n is intentionally **not** exposed as a `[project.scripts]` console entry point\n because `scripts/shiki.py` is a dependency-free shim and `scripts/` is not\n-packaged as an importable distribution (see the ADR for the rationale).\n+packaged as an importable distribution (see SADR-0007 for the rationale).\n \n Supported install paths:\n \n@@ -155,6 +155,15 @@\n    [docs/operations.md](operations.md#the-new-convention). If the target has\n    pending migrations, `--force` refuses and writes nothing; apply migrations\n    (step 2) first, then re-run.\n+\n+   The SADR namespace upgrade has an additional pre-write gate: each former\n+   numeric Shiki decision path is removed only when the existing install stamp\n+   contains that exact path and a digest matching its current bytes. The\n+   installer aggregates absent/unreadable/malformed-stamp, missing-digest, and\n+   digest-mismatch blockers before raising, so refusal leaves the target tree\n+   byte-identical. Numeric target ADRs are preserved. A successful forced\n+   upgrade installs and stamps `SADR-*` paths and drops the removed legacy paths\n+   from the refreshed stamp.\n 2. Apply repository-local `.shiki/` state migrations with the migrate command:\n \n    ```bash\ndiff --git a/docs/usage.md b/docs/usage.md\nindex b83407ee3970dd395dd893010bc5dbe8bad26051..a130ef6e40dfd8169a5bc701339351097df1a09f\n--- a/docs/usage.md\n+++ b/docs/usage.md\n@@ -67,8 +67,10 @@\n shiki install-target /path/to/target-repo --local-only\n ```\n \n-Use `--force` only when you intentionally want to overwrite existing target\n-files.\n+Use `--force` only when you intentionally want to refresh the target template.\n+For the SADR namespace migration, `--force` deletes only exact legacy Shiki\n+decision paths whose existing install-stamp digest still matches; any ownership\n+blocker stops before the first write. Numeric target ADRs are never candidates.\n \n ## `shiki bootstrap-platform` \u2014 publish the Shiki platform repo\n \ndiff --git a/scripts/guardian_approval_signal.py b/scripts/guardian_approval_signal.py\nindex d059f92b2c82aa2b5d6ca421365537a87886c1cb..16924892714f4f5b9303c0ef0e325a7e70075630\n--- a/scripts/guardian_approval_signal.py\n+++ b/scripts/guardian_approval_signal.py\n@@ -199,8 +199,8 @@\n ) -> Any:\n     \"\"\"Evaluate SADR-0015 Contract Approval for the CCA Guardian signal.\n \n-    The SAME OR that MergeGate applies must reach this deterministic signal: ADR\n-    0015 is explicit that without it, a contract-approved PR still returns\n+    The SAME OR that MergeGate applies must reach this deterministic signal:\n+    SADR-0015 is explicit that without it, a contract-approved PR still returns\n     ``needs_guardian``, the CCA job fails, and the MergeGate policy check never\n     runs \u2014 the gate this change moves would never be reached. This resolves the\n     task the SAME way MergeGate does (the first Shiki task id in the PR body),\ndiff --git a/scripts/shiki_contract_approval.py b/scripts/shiki_contract_approval.py\nindex 941bcb96fdd8b9a85bc0c4f0f04d5dad9d04839c..4a4b418f6527413161db82670bec857a043360e2\n--- a/scripts/shiki_contract_approval.py\n+++ b/scripts/shiki_contract_approval.py\n@@ -142,7 +142,7 @@\n     changed_files_status: Any,\n     registration: Any,\n ) -> ContractApprovalResult:\n-    \"\"\"Whether this PR carries an SADR-0015 Contract Approval.\n+    \"\"\"Whether this PR carries a SADR-0015 Contract Approval.\n \n     The four conditions are evaluated IN ORDER, each failure recorded as a\n     distinct reason string:\ndiff --git a/scripts/shiki_guardian.py b/scripts/shiki_guardian.py\nindex a4ecb79bbda55fd5c8b06adfae2eb99760a03c45..4dd99bbe714e26f421fa19abac7a34ae1b195ecc\n--- a/scripts/shiki_guardian.py\n+++ b/scripts/shiki_guardian.py\n@@ -12,7 +12,7 @@\n # The carry re-derives its proof from git in-process on every judging invocation;\n # it is never read from an artifact, so there is nothing to go stale or be forged.\n # shiki_sync_proof is the ONLY guardian-family module that runs git, and every one\n-# of its invocations is read-only and local (ADR: prove-from-git base sync).\n+# of its invocations is read-only and local (SADR: prove-from-git base sync).\n from shiki_sync_proof import verify_pure_base_sync\n \n GUARDIAN_POLICY_PATH = \".shiki/guardian-policy.json\"\ndiff --git a/scripts/shiki_guardian_review.py b/scripts/shiki_guardian_review.py\nindex dd0f104d457899e0ba5ff1e162011c1e90b6f92b..e10ef58c046324e7ea3a3392feb3048ca77414ce\n--- a/scripts/shiki_guardian_review.py\n+++ b/scripts/shiki_guardian_review.py\n@@ -159,7 +159,7 @@\n     docs_only = _is_docs_only(changed_files)\n     if docs_only and matched == 0:\n         focus.append(\n-            \"Docs/ADR-only change: confirm the change is genuinely documentation/decision-only, introduces no behavior or contract drift, and stays consistent with the constitution and existing ADRs.\"\n+            \"Docs/decision-record-only change: confirm the change is genuinely documentation/decision-only, introduces no behavior or contract drift, and stays consistent with the constitution and existing SADRs and target ADRs.\"\n         )\n \n     # Conservative augmentation: unknown (no category and not docs-only), mixed\ndiff --git a/scripts/shiki_installer.py b/scripts/shiki_installer.py\nindex be064ab7a29ff6836f7f7a1d70500a43e0d11d7b..9fd8eefd137ce7003dad5c0dcb2e48e2cd47ab32\n--- a/scripts/shiki_installer.py\n+++ b/scripts/shiki_installer.py\n@@ -118,6 +118,7 @@\n # what lets a target carry it without a governance-schema change.\n INSTALL_STAMP_PATH = \".shiki/install-stamp.json\"\n INSTALL_STAMP_VERSION = 1\n+LEGACY_SHIKI_SADR_MAX = 18\n \n # Under --force the shipped surface splits three ways, so nothing is lost and\n # nothing is silently stale.\n@@ -237,9 +238,98 @@\n         )\n \n \n+def legacy_shiki_adr_paths() -> tuple[str, ...]:\n+    \"\"\"Former numeric paths for Shiki SADR-0001 through SADR-0018.\n+\n+    The authoritative slugs come from the current SADR filenames. SADR-0019 is\n+    the namespace decision itself and never shipped under a numeric legacy path.\n+    \"\"\"\n+    paths: list[str] = []\n+    for sadr in sorted((ROOT / \"docs\" / \"adr\").glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\")):\n+        number = sadr.name.split(\"-\", 2)[1]\n+        if int(number) <= LEGACY_SHIKI_SADR_MAX:\n+            paths.append(f\"docs/adr/{sadr.name.removeprefix('SADR-')}\")\n+    return tuple(paths)\n+\n+\n+def preflight_legacy_sadr_cleanup(target: Path, *, force: bool) -> tuple[str, ...]:\n+    \"\"\"Authorize exact legacy Shiki ADR removals before the first write.\n+\n+    A non-force install never removes existing files. A forced install needs no\n+    stamp when no legacy Shiki path is present. When legacy paths do exist, each\n+    must be named in a readable install stamp and its current bytes must match\n+    the recorded digest.\n+    \"\"\"\n+    if not force:\n+        return ()\n+    present = tuple(\n+        relative\n+        for relative in legacy_shiki_adr_paths()\n+        if (target / relative).exists() or (target / relative).is_symlink()\n+    )\n+    if not present:\n+        return ()\n+\n+    stamp_path = target / INSTALL_STAMP_PATH\n+    try:\n+        raw = stamp_path.read_text(encoding=\"utf-8\")\n+        stamp = json.loads(raw)\n+    except FileNotFoundError:\n+        detail = \"install stamp is absent\"\n+        blockers = [f\"{relative}: {detail}\" for relative in present]\n+    except (OSError, UnicodeDecodeError) as error:\n+        detail = f\"install stamp is unreadable ({error})\"\n+        blockers = [f\"{relative}: {detail}\" for relative in present]\n+    except json.JSONDecodeError as error:\n+        detail = f\"install stamp is malformed ({error})\"\n+        blockers = [f\"{relative}: {detail}\" for relative in present]\n+    else:\n+        if not isinstance(stamp, dict):\n+            blockers = [\n+                f\"{relative}: install stamp is malformed (expected an object)\"\n+                for relative in present\n+            ]\n+        else:\n+            digests = stamp.get(\"digests\")\n+            blockers = []\n+            if not isinstance(digests, dict):\n+                blockers.extend(\n+                    f\"{relative}: install stamp has no digest for this path\"\n+                    for relative in present\n+                )\n+            else:\n+                for relative in present:\n+                    expected = digests.get(relative)\n+                    if not isinstance(expected, str) or not expected:\n+                        blockers.append(\n+                            f\"{relative}: install stamp has no digest for this path\"\n+                        )\n+                        continue\n+                    try:\n+                        actual = _sha256_file(target / relative)\n+                    except OSError as error:\n+                        blockers.append(\n+                            f\"{relative}: legacy path is unreadable ({error})\"\n+                        )\n+                        continue\n+                    if actual != expected:\n+                        blockers.append(\n+                            f\"{relative}: digest mismatch (stamp {expected}, current {actual})\"\n+                        )\n+\n+    if blockers:\n+        joined = \"\\n\".join(f\"- {blocker}\" for blocker in blockers)\n+        raise ShikiError(\n+            \"legacy Shiki ADR cleanup is not authorized:\\n\"\n+            f\"{joined}\\nNo files were written.\"\n+        )\n+    return present\n+\n+\n def install_template(target: Path, *, force: bool, validate: bool) -> list[NewFileNote]:\n     # Preconditions first: never rewrite the tree of a target that cannot be\n     # upgraded cleanly (see refuse_pending_migrations).\n+    legacy_cleanup = preflight_legacy_sadr_cleanup(target, force=force)\n     refuse_pending_migrations(target)\n \n     # Resolve the target's required CODEOWNERS owner once from its\n@@ -250,6 +340,10 @@\n     # verbatim. See ``codeowners_required_owner``.\n     codeowners_owner = codeowners_required_owner(target)\n \n+    for relative in legacy_cleanup:\n+        (target / relative).unlink()\n+        info(f\"removed stamped legacy Shiki ADR: {target / relative}\")\n+\n     notes: list[NewFileNote] = []\n     for relative in TEMPLATE_PATHS:\n         source = ROOT / relative\n@@ -647,6 +741,22 @@\n     digests = target_stamp_digests(target)\n     existing = read_install_stamp(target)\n     if existing is not None:\n+        # A non-force install deliberately leaves legacy Shiki ADRs in place.\n+        # Preserve only still-matching ownership proof so a later forced\n+        # upgrade can authorize cleanup; never carry a missing or stale digest.\n+        existing_digests = existing.get(\"digests\")\n+        if isinstance(existing_digests, dict):\n+            for relative in legacy_shiki_adr_paths():\n+                expected = existing_digests.get(relative)\n+                path = target / relative\n+                if not isinstance(expected, str) or not path.is_file():\n+                    continue\n+                try:\n+                    actual = _sha256_file(path)\n+                except OSError:\n+                    continue\n+                if actual == expected:\n+                    digests[relative] = actual\n         if _incoming_is_older(commit, existing.get(\"platform_commit\")):\n             warn(\n                 \"kept existing install stamp: incoming install \"\ndiff --git a/scripts/shiki_loop.py b/scripts/shiki_loop.py\nindex 3611641da8aeb5038b1da90e3114b09a473ec6d8..84caa491b9cad0edc4176f209c13e9221e975681\n--- a/scripts/shiki_loop.py\n+++ b/scripts/shiki_loop.py\n@@ -424,7 +424,7 @@\n         # unresolvable CCA verdict has already produced its terminal stop and is never\n         # laundered into a head-moving sync (SADR-0010). It also never fires while the\n         # policy (Guardian) gate is red \u2014 for ANY reason, not only when it is the sole\n-        # red check \u2014 so a head-bound Guardian approval, or an SADR-0015\n+        # red check \u2014 so a head-bound Guardian approval, or a SADR-0015\n         # contract-approval carry that base movement defeated (the policy gate compares\n         # base and head task governance directly), is never silently restored by\n         # adopting base wholesale. It is bounded by a durable per-task counter so the\ndiff --git a/scripts/shiki_tasks.py b/scripts/shiki_tasks.py\nindex aec3809f24b4a0c8c96fc50971cff9ae9ad30381..d531052e9d9717058d4631483cefab44c26a65c1\n--- a/scripts/shiki_tasks.py\n+++ b/scripts/shiki_tasks.py\n@@ -821,7 +821,7 @@\n         \"prompt\": prompt,\n         \"entry_skill\": \"grill-with-docs\",\n         \"required_next_steps\": [\n-            \"Run grill-with-docs until terminology, boundaries, risks, and ADR-worthy decisions are settled.\",\n+            \"Run grill-with-docs until terminology, boundaries, risks, and decision-record-worthy choices are settled.\",\n             \"Obtain the operator's explicit approval of the PRD/requirements (Spec Freeze).\",\n             \"Write a machine-readable plan JSON with grill_with_docs.status=complete and spec_freeze.status=frozen.\",\n             \"Run shiki plan ingest --plan-file PLAN.json.\",\ndiff --git a/scripts/test_shiki_goal_loop.sh b/scripts/test_shiki_goal_loop.sh\nindex 753c7b516e10f0be3bc92a4bd0eb1fa699776ae6..8f3820a0a079b3044424cfe1a6c6b7fe68896e67\n--- a/scripts/test_shiki_goal_loop.sh\n+++ b/scripts/test_shiki_goal_loop.sh\n@@ -231,8 +231,8 @@\n \n # High/critical risk merges autonomously when all required checks are green:\n # the \"MergeGate policy check\" required check IS the Guardian gate (it enforces\n-# guardian-policy.json \u2014 human approval OR external AI guardian review, ADR\n-# 0010), so green checks mean a recorded authority approved. The fake gh returns\n+# guardian-policy.json \u2014 human approval OR external AI guardian review,\n+# SADR-0010), so green checks mean a recorded authority approved. The fake gh returns\n # green checks, simulating that the Guardian gate passed.\n python3 \"$ROOT/scripts/shiki.py\" goal create --target \"$TARGET\" --title \"Guardian gate\" --outcome \"High risk merges via policy gate\" >/tmp/shiki-goal-loop-guardian-goal.json\n GGOAL=\"$(json_get /tmp/shiki-goal-loop-guardian-goal.json goal_id)\"\ndiff --git a/tests/test_adr_references.py b/tests/test_adr_references.py\nindex 51706329c797d5dd9b47e082a333fa83b261d482..db6e693e35f67103935be34f1d9bb4ff7ee60395\n--- a/tests/test_adr_references.py\n+++ b/tests/test_adr_references.py\n@@ -1,7 +1,7 @@\n-\"\"\"Every ``ADR ####`` citation must resolve to exactly one decision record.\n+\"\"\"Every target ADR and Shiki SADR citation resolves in its own namespace.\n \n Background. ``scripts/mergegate_check.py`` justifies ``is_bookkeeping_closeout``\n-with \"ADR 0017\" nine times and ``scripts/guardian_approval_signal.py`` cites\n+with \"SADR-0017\" nine times and ``scripts/guardian_approval_signal.py`` cites\n 0016/0017 seven times, yet for a stretch the numbering in ``docs/adr/`` stopped\n at 0015: the authoring commits lived on a coordinator branch and no task ever\n declared ``docs/adr/...`` in its locks, so nothing carried the records to the\n@@ -11,11 +11,12 @@\n and the directory drifted with no signal.\n \n This is the same unbound-pair shape behind other silent failures: a reference\n-and its target with nothing asserting they agree. This test binds them. It scans\n-every ``scripts/*.py`` (plus ``docs/**/*.md`` and the root ``*.md`` governance\n-files, which are cheap) for the ``ADR ####`` pattern and asserts each cited\n-number resolves to exactly one ``docs/adr/####-*.md``. The converse is\n-deliberately NOT required: an ADR may exist without being cited.\n+and its target with nothing asserting they agree. This test binds them. Target\n+``ADR NNNN`` citations resolve only to ``docs/adr/NNNN-*.md``; Shiki\n+``SADR-NNNN`` citations resolve only to ``docs/adr/SADR-NNNN-*.md``. The scan\n+covers current governance docs, scripts, workflows, prompts, packaging metadata,\n+and repo-local Codex guidance while deliberately excluding historical\n+``.shiki`` evidence. The converse is not required: a record may be uncited.\n \n The suite is dependency free: standard-library ``unittest`` only.\n \"\"\"\n@@ -31,17 +32,18 @@\n \n REPO_ROOT = Path(__file__).resolve().parents[1]\n \n-# \"ADR 0016\" -- a four-digit citation. The trailing \\b keeps a five-digit token\n-# from being read as a truncated four-digit number.\n-_CITATION = re.compile(r\"\\bADR (\\d{4})\\b\")\n-# \"0016-locks-bound-product-paths.md\" -- the leading four digits name the record.\n-_ADR_FILENAME = re.compile(r\"^(\\d{4})-.*\\.md$\")\n+# ``ADR 0016`` / ``SADR-0016`` -- a namespace plus four-digit citation. The\n+# trailing ``\\b`` keeps a five-digit token from being read as a truncated one.\n+_CITATION = re.compile(r\"\\b(S?ADR)[ -](\\d{4})\\b\")\n+# Target ADRs retain the numeric filename; Shiki platform decisions use SADR.\n+_DECISION_FILENAME = re.compile(r\"^(?:(SADR)-)?(\\d{4})-.*\\.md$\")\n \n \n class Violation(NamedTuple):\n     \"\"\"A citation that does not resolve to exactly one decision record.\"\"\"\n \n     citing_file: str  # repo-relative path of the file that cites the ADR\n+    namespace: str  # \"ADR\" for target decisions; \"SADR\" for Shiki decisions\n     number: str  # the cited four-digit number, e.g. \"0016\"\n     line: int  # 1-based line of the citation\n     reason: str  # \"missing\" (zero matches) or \"ambiguous\" (two or more)\n@@ -49,44 +51,59 @@\n \n     @property\n     def expected_path(self) -> str:\n-        return f\"docs/adr/{self.number}-*.md\"\n+        prefix = \"SADR-\" if self.namespace == \"SADR\" else \"\"\n+        return f\"docs/adr/{prefix}{self.number}-*.md\"\n \n     def describe(self) -> str:\n         \"\"\"Name the citing file, the cited number, and the missing path.\"\"\"\n         if self.reason == \"missing\":\n             return (\n-                f\"{self.citing_file}:{self.line} cites ADR {self.number} \"\n+                f\"{self.citing_file}:{self.line} cites {self.namespace} {self.number} \"\n                 f\"but no document matches {self.expected_path}\"\n             )\n         joined = \", \".join(self.matches)\n         return (\n-            f\"{self.citing_file}:{self.line} cites ADR {self.number} which \"\n+            f\"{self.citing_file}:{self.line} cites {self.namespace} {self.number} which \"\n             f\"resolves to {len(self.matches)} documents (expected exactly one \"\n             f\"at {self.expected_path}): {joined}\"\n         )\n \n \n-def _adr_index(root: Path) -> dict[str, list[Path]]:\n-    \"\"\"Map every four-digit ADR number to the record files that claim it.\"\"\"\n-    index: dict[str, list[Path]] = {}\n+def _adr_index(root: Path) -> dict[tuple[str, str], list[Path]]:\n+    \"\"\"Map each decision namespace and number to the files that claim it.\"\"\"\n+    index: dict[tuple[str, str], list[Path]] = {}\n     adr_dir = root / \"docs\" / \"adr\"\n     if adr_dir.is_dir():\n         for path in sorted(adr_dir.glob(\"*.md\")):\n-            match = _ADR_FILENAME.match(path.name)\n+            match = _DECISION_FILENAME.match(path.name)\n             if match:\n-                index.setdefault(match.group(1), []).append(path)\n+                namespace = \"SADR\" if match.group(1) else \"ADR\"\n+                index.setdefault((namespace, match.group(2)), []).append(path)\n     return index\n \n \n def _citing_files(root: Path, include_docs: bool = True) -> list[Path]:\n-    \"\"\"Files scanned for citations: scripts/*.py, and cheaply the markdown.\"\"\"\n+    \"\"\"Current, shipped text surfaces scanned for decision citations.\"\"\"\n     files = sorted((root / \"scripts\").glob(\"*.py\"))\n+    files += sorted((root / \"scripts\").glob(\"*.sh\"))\n     if include_docs:\n         docs = root / \"docs\"\n         if docs.is_dir():\n             files += sorted(docs.rglob(\"*.md\"))\n         files += sorted(root.glob(\"*.md\"))\n-    return files\n+        pyproject = root / \"pyproject.toml\"\n+        if pyproject.is_file():\n+            files.append(pyproject)\n+        for relative in (\".github\", \".codex\"):\n+            surface = root / relative\n+            if surface.is_dir():\n+                files += sorted(\n+                    path\n+                    for path in surface.rglob(\"*\")\n+                    if path.is_file()\n+                    and path.suffix in {\".md\", \".py\", \".sh\", \".toml\", \".yaml\", \".yml\"}\n+                )\n+    return sorted(set(files))\n \n \n def find_adr_reference_violations(\n@@ -94,10 +111,9 @@\n ) -> list[Violation]:\n     \"\"\"Return every ``ADR ####`` citation under ``root`` that fails to resolve.\n \n-    A citation resolves when exactly one ``docs/adr/####-*.md`` matches its\n-    number. Zero matches is a \"missing\" violation; two or more is \"ambiguous\".\n-    Records that are never cited produce nothing -- the converse is not\n-    required.\n+    A citation resolves when exactly one record in the same namespace matches\n+    its number. Zero matches is a \"missing\" violation; two or more is\n+    \"ambiguous\". Records that are never cited produce nothing.\n     \"\"\"\n     root = Path(root)\n     index = _adr_index(root)\n@@ -110,13 +126,15 @@\n         rel = path.relative_to(root).as_posix()\n         for lineno, line in enumerate(text.splitlines(), start=1):\n             for match in _CITATION.finditer(line):\n-                number = match.group(1)\n-                found = index.get(number, [])\n+                namespace, number = match.groups()\n+                found = index.get((namespace, number), [])\n                 if len(found) == 1:\n                     continue\n                 reason = \"missing\" if not found else \"ambiguous\"\n                 rels = [p.relative_to(root).as_posix() for p in found]\n-                violations.append(Violation(rel, number, lineno, reason, rels))\n+                violations.append(\n+                    Violation(rel, namespace, number, lineno, reason, rels)\n+                )\n     return violations\n \n \n@@ -129,8 +147,8 @@\n         self.assertEqual(\n             violations,\n             [],\n-            \"Every 'ADR ####' citation must resolve to exactly one \"\n-            f\"docs/adr/####-*.md:\\n{detail}\",\n+            \"Every ADR/SADR citation must resolve exactly once in its own \"\n+            f\"docs/adr namespace:\\n{detail}\",\n         )\n \n     def test_scan_actually_reads_scripts(self) -> None:\n@@ -146,8 +164,21 @@\n         # The two records this Goal carried onto the default branch, and which\n         # the scripts cite most, must be present.\n         index = _adr_index(REPO_ROOT)\n-        self.assertIn(\"0016\", index)\n-        self.assertIn(\"0017\", index)\n+        self.assertIn((\"SADR\", \"0016\"), index)\n+        self.assertIn((\"SADR\", \"0017\"), index)\n+\n+    def test_platform_tree_has_only_sadr_0001_through_0019(self) -> None:\n+        adr_dir = REPO_ROOT / \"docs\" / \"adr\"\n+        sadrs = sorted(adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\"))\n+        self.assertEqual(\n+            {path.name.split(\"-\", 2)[1] for path in sadrs},\n+            {f\"{number:04d}\" for number in range(1, 20)},\n+        )\n+        self.assertEqual(\n+            list(adr_dir.glob(\"[0-9][0-9][0-9][0-9]-*.md\")),\n+            [],\n+            \"the Shiki platform tree must not retain legacy numeric SADR paths\",\n+        )\n \n \n class FixtureAdrReferences(unittest.TestCase):\n@@ -180,6 +211,26 @@\n         self.assertIn(\"9991\", message)  # cited number\n         self.assertIn(\"docs/adr/9991-*.md\", message)  # missing path\n \n+    def test_target_adr_does_not_satisfy_missing_platform_sadr(self) -> None:\n+        root = self._make_repo(\n+            adr_names=[\"0042-target-decision.md\"],\n+            script_body='\"\"\"Target ADR 0042 exists, but platform SADR-0042 does not.\"\"\"\\n',\n+        )\n+        violations = find_adr_reference_violations(root, include_docs=False)\n+        self.assertEqual(len(violations), 1)\n+        self.assertEqual(violations[0].namespace, \"SADR\")\n+        self.assertEqual(violations[0].number, \"0042\")\n+        self.assertEqual(violations[0].expected_path, \"docs/adr/SADR-0042-*.md\")\n+\n+    def test_target_adr_and_platform_sadr_may_reuse_a_number(self) -> None:\n+        root = self._make_repo(\n+            adr_names=[\"0042-target-decision.md\", \"SADR-0042-platform-decision.md\"],\n+            script_body='\"\"\"Target ADR 0042 and platform SADR-0042 are distinct.\"\"\"\\n',\n+        )\n+        self.assertEqual(\n+            find_adr_reference_violations(root, include_docs=False), []\n+        )\n+\n     def test_uncited_adr_is_allowed(self) -> None:\n         # An ADR exists but nothing cites it -> not a violation. The converse\n         # (every ADR must be cited) is explicitly NOT required.\ndiff --git a/tests/test_guardian_review.py b/tests/test_guardian_review.py\nindex 88829e7810212635e2fc98aa5918d6f26294677d..00a85a237acd5bf2d6cb5b294128198c422402ca\n--- a/tests/test_guardian_review.py\n+++ b/tests/test_guardian_review.py\n@@ -162,7 +162,7 @@\n \n     def test_docs_only_is_light_and_not_conservative(self):\n         focus = gr.classify_review_focus([\"docs/adr/0099-x.md\", \"README.md\"], \"\", \"low\")\n-        self.assertTrue(any(\"Docs/ADR-only\" in f for f in focus))\n+        self.assertTrue(any(\"Docs/decision-record-only\" in f for f in focus))\n         self.assertFalse(any(\"conservative default\" in f.lower() for f in focus))\n \n     def test_unknown_category_is_conservative(self):\ndiff --git a/tests/test_policy_job_merged_prs.py b/tests/test_policy_job_merged_prs.py\nindex d45512e74681e5e649279236c7d4e79d5c3da471..b97989f8bd74d091de1a6b5275a20d8dc68c4512\n--- a/tests/test_policy_job_merged_prs.py\n+++ b/tests/test_policy_job_merged_prs.py\n@@ -3,8 +3,8 @@\n \n ``.github/workflows/shiki-cca-completion.yml`` has two consumers of the merged-PR\n proof: the CCA *signal* (``guardian_approval_signal.py`` in \"Prepare CCA context\")\n-and the authoritative gate (``mergegate_check.py`` in \"Enforce MergeGate\"). ADR\n-0017's bookkeeping-closeout exemption (condition 6) needs the merged implementation\n+and the authoritative gate (``mergegate_check.py`` in \"Enforce MergeGate\").\n+SADR-0017's bookkeeping-closeout exemption (condition 6) needs the merged implementation\n PR proven, and a closeout carries NO ``post_merge_reconcile`` marker. The signal\n already resolves ``MERGED_PRS`` unconditionally, but \"Enforce MergeGate\" used to\n resolve it only inside ``grep -q '<!-- shiki:post_merge_reconcile -->'``. So a\ndiff --git a/tests/test_sadr_install_migration.py b/tests/test_sadr_install_migration.py\nnew file mode 100644\nindex 0000000000000000000000000000000000000000..a6f4c57e4dcaef73ef600b0d4804744138ab9071\n--- /dev/null\n+++ b/tests/test_sadr_install_migration.py\n@@ -0,0 +1,229 @@\n+\"\"\"SADR installation and collision-safe legacy upgrade behavior.\"\"\"\n+\n+from __future__ import annotations\n+\n+import contextlib\n+import hashlib\n+import io\n+import json\n+import shutil\n+import tempfile\n+import unittest\n+from pathlib import Path\n+\n+import shiki_test_support  # noqa: F401  (path bootstrap)\n+\n+import shiki_installer\n+from tests.test_adr_references import find_adr_reference_violations\n+\n+\n+PRODUCT_NUMBERS = tuple(range(13, 19))\n+\n+\n+def _quiet_install(target: Path, *, force: bool, validate: bool) -> None:\n+    with contextlib.redirect_stdout(io.StringIO()):\n+        shiki_installer.install_template(target, force=force, validate=validate)\n+\n+\n+def _sha256(path: Path) -> str:\n+    return hashlib.sha256(path.read_bytes()).hexdigest()\n+\n+\n+def _legacy_sadr_paths() -> tuple[str, ...]:\n+    return tuple(\n+        f\"docs/adr/{path.name.removeprefix('SADR-')}\"\n+        for path in sorted((shiki_installer.ROOT / \"docs\" / \"adr\").glob(\"SADR-*.md\"))\n+        if path.name != \"SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\"\n+    )\n+\n+\n+def _create_legacy_upgrade_fixture(\n+    target: Path,\n+) -> tuple[dict[str, bytes], tuple[str, ...]]:\n+    adr_dir = target / \"docs\" / \"adr\"\n+    adr_dir.mkdir(parents=True)\n+    product_bytes = {\n+        f\"{number:04d}-cubeshare-product-decision.md\": (\n+            f\"# CubeShare ADR {number:04d}\\n\\nDo not rewrite {number}.\\n\"\n+        ).encode()\n+        for number in PRODUCT_NUMBERS\n+    }\n+    for name, content in product_bytes.items():\n+        (adr_dir / name).write_bytes(content)\n+\n+    legacy_paths = _legacy_sadr_paths()\n+    for relative in legacy_paths:\n+        (target / relative).write_bytes(f\"legacy Shiki bytes for {relative}\\n\".encode())\n+\n+    stamp_path = target / shiki_installer.INSTALL_STAMP_PATH\n+    stamp_path.parent.mkdir(parents=True, exist_ok=True)\n+    stamp = {\n+        \"version\": shiki_installer.INSTALL_STAMP_VERSION,\n+        \"platform_commit\": None,\n+        \"installed_at\": \"2000-01-01T00:00:00+00:00\",\n+        \"digests\": {\n+            relative: _sha256(target / relative) for relative in legacy_paths\n+        },\n+    }\n+    stamp_path.write_text(\n+        json.dumps(stamp, indent=2, sort_keys=True) + \"\\n\", encoding=\"utf-8\"\n+    )\n+    return product_bytes, legacy_paths\n+\n+\n+def _tree_snapshot(root: Path) -> dict[str, tuple[str, bytes | str]]:\n+    \"\"\"Recursive byte/shape snapshot used to prove preflight is write-free.\"\"\"\n+    snapshot: dict[str, tuple[str, bytes | str]] = {}\n+    for path in sorted(root.rglob(\"*\")):\n+        relative = path.relative_to(root).as_posix()\n+        if path.is_symlink():\n+            snapshot[relative] = (\"symlink\", path.readlink().as_posix())\n+        elif path.is_dir():\n+            snapshot[relative] = (\"directory\", \"\")\n+        else:\n+            snapshot[relative] = (\"file\", path.read_bytes())\n+    return snapshot\n+\n+\n+class FreshSadrInstallTests(unittest.TestCase):\n+    def setUp(self) -> None:\n+        self.tmp = Path(tempfile.mkdtemp(prefix=\"shiki-sadr-fresh-\"))\n+        self.addCleanup(lambda: shutil.rmtree(self.tmp, ignore_errors=True))\n+        self.target = self.tmp / \"target\"\n+        self.adr_dir = self.target / \"docs\" / \"adr\"\n+        self.adr_dir.mkdir(parents=True)\n+        self.product_bytes = {\n+            f\"{number:04d}-product-decision.md\": (\n+                f\"# Product ADR {number:04d}\\n\\nTarget-owned bytes {number}.\\n\"\n+            ).encode()\n+            for number in PRODUCT_NUMBERS\n+        }\n+        for name, content in self.product_bytes.items():\n+            (self.adr_dir / name).write_bytes(content)\n+\n+    def test_fresh_install_preserves_product_adrs_and_installs_all_sadrs(self) -> None:\n+        _quiet_install(self.target, force=False, validate=True)\n+\n+        for name, expected in self.product_bytes.items():\n+            self.assertEqual((self.adr_dir / name).read_bytes(), expected)\n+\n+        sadr_numbers = {\n+            path.name.split(\"-\", 2)[1]\n+            for path in self.adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\")\n+        }\n+        self.assertEqual(sadr_numbers, {f\"{number:04d}\" for number in range(1, 20)})\n+\n+        numeric_records = {\n+            path.name\n+            for path in self.adr_dir.glob(\"[0-9][0-9][0-9][0-9]-*.md\")\n+        }\n+        self.assertEqual(numeric_records, set(self.product_bytes))\n+        self.assertEqual(find_adr_reference_violations(self.target), [])\n+\n+\n+class LegacySadrUpgradeTests(unittest.TestCase):\n+    def setUp(self) -> None:\n+        self.tmp = Path(tempfile.mkdtemp(prefix=\"shiki-sadr-upgrade-\"))\n+        self.addCleanup(lambda: shutil.rmtree(self.tmp, ignore_errors=True))\n+        self.target = self.tmp / \"target\"\n+        self.adr_dir = self.target / \"docs\" / \"adr\"\n+        self.product_bytes, self.legacy_paths = _create_legacy_upgrade_fixture(\n+            self.target\n+        )\n+\n+    def test_force_upgrade_deletes_only_stamped_legacy_sadrs(self) -> None:\n+        _quiet_install(self.target, force=True, validate=True)\n+\n+        for relative in self.legacy_paths:\n+            self.assertFalse((self.target / relative).exists(), relative)\n+        for name, expected in self.product_bytes.items():\n+            self.assertEqual((self.adr_dir / name).read_bytes(), expected)\n+\n+        sadrs = list(self.adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\"))\n+        self.assertEqual(len(sadrs), 19)\n+        refreshed = json.loads(\n+            (self.target / shiki_installer.INSTALL_STAMP_PATH).read_text(encoding=\"utf-8\")\n+        )\n+        for relative in self.legacy_paths:\n+            self.assertNotIn(relative, refreshed[\"digests\"])\n+        for path in sadrs:\n+            relative = path.relative_to(self.target).as_posix()\n+            self.assertEqual(refreshed[\"digests\"][relative], _sha256(path))\n+\n+    def test_non_force_install_never_deletes_legacy_files(self) -> None:\n+        before = {\n+            relative: (self.target / relative).read_bytes()\n+            for relative in self.legacy_paths\n+        }\n+        _quiet_install(self.target, force=False, validate=False)\n+        for relative, expected in before.items():\n+            self.assertEqual((self.target / relative).read_bytes(), expected)\n+        refreshed = json.loads(\n+            (self.target / shiki_installer.INSTALL_STAMP_PATH).read_text(encoding=\"utf-8\")\n+        )\n+        for relative in self.legacy_paths:\n+            self.assertEqual(refreshed[\"digests\"][relative], _sha256(self.target / relative))\n+\n+\n+class LegacyCleanupPreflightTests(unittest.TestCase):\n+    def setUp(self) -> None:\n+        self.tmp = Path(tempfile.mkdtemp(prefix=\"shiki-sadr-preflight-\"))\n+        self.addCleanup(lambda: shutil.rmtree(self.tmp, ignore_errors=True))\n+\n+    def test_all_ownership_blockers_abort_before_any_write(self) -> None:\n+        cases = (\n+            (\"absent\", \"install stamp is absent\"),\n+            (\"unreadable\", \"install stamp is unreadable\"),\n+            (\"malformed\", \"install stamp is malformed\"),\n+            (\"missing-digest\", \"install stamp has no digest\"),\n+            (\"digest-mismatch\", \"digest mismatch\"),\n+        )\n+        for name, expected_reason in cases:\n+            with self.subTest(name=name):\n+                target = self.tmp / name\n+                _, legacy_paths = _create_legacy_upgrade_fixture(target)\n+                stamp_path = target / shiki_installer.INSTALL_STAMP_PATH\n+\n+                if name == \"absent\":\n+                    stamp_path.unlink()\n+                elif name == \"unreadable\":\n+                    stamp_path.unlink()\n+                    stamp_path.mkdir()\n+                elif name == \"malformed\":\n+                    stamp_path.write_text(\"{not-json\\n\", encoding=\"utf-8\")\n+                else:\n+                    stamp = json.loads(stamp_path.read_text(encoding=\"utf-8\"))\n+                    if name == \"missing-digest\":\n+                        stamp[\"digests\"] = {}\n+                    else:\n+                        stamp[\"digests\"] = {\n+                            relative: \"deadbeef\" for relative in legacy_paths\n+                        }\n+                    stamp_path.write_text(\n+                        json.dumps(stamp, indent=2, sort_keys=True) + \"\\n\",\n+                        encoding=\"utf-8\",\n+                    )\n+\n+                before = _tree_snapshot(target)\n+                with self.assertRaises(shiki_installer.ShikiError) as raised:\n+                    _quiet_install(target, force=True, validate=False)\n+                after = _tree_snapshot(target)\n+\n+                self.assertEqual(before, after, f\"{name} preflight changed the tree\")\n+                message = str(raised.exception)\n+                self.assertIn(expected_reason, message)\n+                self.assertIn(\"No files were written\", message)\n+                for relative in legacy_paths:\n+                    self.assertIn(relative, message)\n+\n+    def test_absent_stamp_is_allowed_when_no_legacy_path_exists(self) -> None:\n+        target = self.tmp / \"no-legacy\"\n+        target.mkdir()\n+        _quiet_install(target, force=True, validate=False)\n+        self.assertTrue(\n+            (target / \"docs\" / \"adr\" / \"SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\").is_file()\n+        )\n+\n+\n+if __name__ == \"__main__\":\n+    unittest.main()\n\n2026-08-07T08:20:18.544291Z ERROR codex_models_manager::manager: failed to renew cache TTL: missing field `base_instructions` at line 94 column 5\nexec\n/bin/zsh -lc \"rg -l -0 'SADR-[0-9]{4}/[0-9]{4}' AGENTS.md CLAUDE.md CONTEXT.md CONTRIBUTING.md SYSTEM_PROMPT.md pyproject.toml docs scripts tests .github --glob '\"'!.shiki/**'\"' --glob '\"'!skills/**'\"' | xargs -0 perl -pi -e 's/SADR-([0-9]{4})\\\\/([0-9]{4})\\\\/([0-9]{4})/SADR-\"'$1'\"\\\\/SADR-\"'$2'\"\\\\/SADR-\"'$3/g; s/SADR-([0-9]{4})'\"\\\\/([0-9]{4})/SADR-\"'$1'\"\\\\/SADR-\"'$2/g'\"'\" in /private/tmp/shiki-adr-collision.pa0KwY/.worktrees/shiki-t-20260807t073307221254z-2d22d654-introduc\n succeeded in 0ms:\ndiff --git a/.codex/skills/shiki/SKILL.md b/.codex/skills/shiki/SKILL.md\nindex c8e2e85d9d75d49cc4f2d6286ee22659c4e53e75..93cded623b2c15d9f65c76491a1c2d813ac9e6c0\n--- a/.codex/skills/shiki/SKILL.md\n+++ b/.codex/skills/shiki/SKILL.md\n@@ -18,6 +18,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n Codex CLI does not register this skill as a custom `/shiki` slash command. When\n the user types `/shiki` in Codex CLI and gets \"Unrecognized command\", explain\n that this is expected: invoke Shiki in Codex with natural language such as\n@@ -69,7 +72,7 @@\n \n ## Responsibilities\n \n-- Claude Code implements and repairs by default (`shiki runner claude`, ADR 0008); Codex implements and repairs tasks explicitly assigned to `codex`.\n+- Claude Code implements and repairs by default (`shiki runner claude`, SADR-0008); Codex implements and repairs tasks explicitly assigned to `codex`.\n - Claude Code Action can act as GitHub-side CCA or reviewer.\n - CCA judges completion.\n - MergeGate authorizes state transitions and merge readiness.\ndiff --git a/.github/prompts/cca-completion-check.md b/.github/prompts/cca-completion-check.md\nindex ed203ad5f302ecc81372d8ab27d2b1232ca97497..fd89950528333ef6242e6d5443904f5392ab7e56\n--- a/.github/prompts/cca-completion-check.md\n+++ b/.github/prompts/cca-completion-check.md\n@@ -19,6 +19,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n ## Required Reading\n \n Read only the files needed to judge the PR contract. Prefer this order:\ndiff --git a/.github/prompts/codex-task-handoff.md b/.github/prompts/codex-task-handoff.md\nindex 695e211d76dea1c60e8d5bb360e78487808286a4..59441d576b01a39569722f226c854e5a740e9eb5\n--- a/.github/prompts/codex-task-handoff.md\n+++ b/.github/prompts/codex-task-handoff.md\n@@ -2,7 +2,7 @@\n \n You are Codex Front acting as the Implementer for a Shiki task.\n \n-Follow `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant ADRs, and `docs/agents/codex-handoff.md`.\n+Follow `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant Shiki SADRs and target ADRs, and `docs/agents/codex-handoff.md`.\n \n Rules:\n \ndiff --git a/AGENTS.md b/AGENTS.md\nindex 7f041100d49c2a7525f51cb0f5d696a4a6d1c78c..a06bfbabc6fa48a007389e25e12d7ed523936bed\n--- a/AGENTS.md\n+++ b/AGENTS.md\n@@ -22,7 +22,7 @@\n - **Codex Front** is an optional implementer through Codex App, Codex CLI, Codex IDE extension, or Codex Web signed in with ChatGPT OAuth/subscription auth, dispatched with `shiki runner codex` when a task is explicitly assigned to `codex`.\n - **GitHub CCA** is implemented by Claude Code Action by default, using `CLAUDE_CODE_OAUTH_TOKEN`.\n \n-Do not assume `openai/codex-action`, `OPENAI_API_KEY`, or API-key based Codex automation in the default Shiki path. API-key runners are explicit target-repo extensions and require their own ADR.\n+Do not assume `openai/codex-action`, `OPENAI_API_KEY`, or API-key based Codex automation in the default Shiki path. API-key runners are explicit target-repo extensions and require their own target ADR.\n \n Shiki is not:\n \n@@ -44,10 +44,14 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use\n+`SADR-NNNN-*.md`; target product decisions retain `NNNN-*.md` and the `ADR`\n+namespace.\n+\n Use the canonical source-of-truth order above. Runtime-specific reading orders\n are operational guidance only and must not override or reorder it.\n \n-Conversation state is not durable truth. If a decision matters, put it in GitHub, `.shiki/`, `CONTEXT.md`, or an ADR.\n+Conversation state is not durable truth. If a decision matters, put it in GitHub, `.shiki/`, `CONTEXT.md`, a Shiki SADR, or a target ADR.\n \n If GitHub and `.shiki/` disagree, surface the conflict and prefer GitHub operational state until the mirror is repaired.\n \n@@ -75,8 +79,8 @@\n Every non-trivial change follows this loop:\n \n 1. **Goal**: clarify outcome, completion conditions, non-goals, risk level, and success signals.\n-2. **grill-with-docs**: challenge the plan against domain docs, ADRs, code reality, terminology, and edge scenarios.\n-3. **Context & Impact**: find relevant docs, ADRs, code, dependencies, owners, locks, and verification surfaces.\n+2. **grill-with-docs**: challenge the plan against domain docs, Shiki SADRs, target ADRs, code reality, terminology, and edge scenarios.\n+3. **Context & Impact**: find relevant docs, SADRs, target ADRs, code, dependencies, owners, locks, and verification surfaces.\n 4. **PRD and Spec Freeze**: use `to-prd` when context is settled enough to become durable product/engineering intent. The operator's explicit approval of the PRD is Spec Freeze: the plan records a `spec_freeze` block, and no plan runs without it. Steps 1-4 form Requirements Definition \u2014 one continuous operator dialogue. After Spec Freeze, scope changes require an operator-approved Spec Amendment; non-scope-moving interpretations go to the Assumption Log.\n 5. **Issues**: use `to-issues` to create independently grabbable vertical-slice issues. Prefer AFK slices over HITL slices where possible.\n 6. **Triage**: label issues for readiness, risk, runtime, skills, and MergeGate state.\n@@ -167,7 +171,7 @@\n - Testing decisions.\n - Out-of-scope items.\n - Further notes.\n-- Links to relevant domain terms and ADRs.\n+- Links to relevant domain terms, SADRs, and target ADRs.\n \n Every executable task must include:\n \n@@ -193,7 +197,7 @@\n \n Context & Impact must identify:\n \n-- Relevant documents, domain terms, ADRs, and past decisions.\n+- Relevant documents, domain terms, SADRs, target ADRs, and past decisions.\n - Relevant modules, interfaces, seams, callers, owners, and tests.\n - Dependency relationships between tasks.\n - Candidate locks and lock conflicts.\n@@ -210,7 +214,7 @@\n | Skill | Required when |\n | --- | --- |\n | `setup-matt-pocock-skills` | First configuring the repo for skills, or when issue tracker, triage labels, or domain docs are missing. |\n-| `grill-with-docs` | Default for non-trivial Goals before PRD/issues; always required when plans, terminology, boundaries, tradeoffs, or ADR-worthy decisions are ambiguous. |\n+| `grill-with-docs` | Default for non-trivial Goals before PRD/issues; always required when plans, terminology, boundaries, tradeoffs, or decision-record-worthy choices are ambiguous. |\n | `zoom-out` | The agent lacks an architectural map of the relevant code area. |\n | `to-prd` | Settled Goal context must become durable product/engineering intent. |\n | `to-issues` | A Goal, PRD, or plan must become independently grabbable vertical-slice issues. |\n@@ -348,7 +352,7 @@\n - Three or more new files.\n - Any high-risk or critical label.\n \n-When the architecture gate triggers, run `grill-with-docs` or `improve-codebase-architecture` as appropriate and record durable decisions in an ADR when needed.\n+When the architecture gate triggers, run `grill-with-docs` or `improve-codebase-architecture` as appropriate and record durable decisions in a Shiki SADR or target ADR as applicable.\n \n ## Engineering Discipline\n \ndiff --git a/CLAUDE.md b/CLAUDE.md\nindex 98a932e7841a632866fcd53b0bad0e3af3711d3d..98a31399404d6fb044b4fd493f4459ac87f439de\n--- a/CLAUDE.md\n+++ b/CLAUDE.md\n@@ -14,6 +14,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n ## Claude Role\n \n Default to planner, default implementer, reviewer, coordinator, documentation editor, CCA-style completion judge, and final-judgment assistant.\n@@ -57,7 +60,7 @@\n At the start of a Shiki session:\n \n 1. Read `AGENTS.md`.\n-2. Read `CONTEXT.md` and relevant ADRs.\n+2. Read `CONTEXT.md` and relevant Shiki SADRs and target ADRs.\n 3. Read active `.shiki/` Goal/task/ledger state when present.\n 4. Read active distilled rules from `.shiki/memories/` (status `distilled`, `active: true`, not revoked or superseded) for applicable guidance; the same rules are injected deterministically into task handoffs as the `## Distilled Rules` section (\u00a73.5).\n 5. Read the GitHub Issue or PR when the task is GitHub-backed.\n@@ -74,7 +77,7 @@\n - Define the Goal, completion conditions, non-goals, and risk level.\n - Treat `grill-with-docs` as the default planning entry point for non-trivial Goals.\n - Challenge domain terms against `CONTEXT.md`.\n-- Challenge hard-to-reverse tradeoffs against ADRs.\n+- Challenge hard-to-reverse tradeoffs against SADRs and target ADRs.\n - Explore code instead of asking when code can answer the question.\n - Run Context & Impact before implementation planning.\n - Produce a Task DAG, not an unordered checklist.\n@@ -94,7 +97,7 @@\n - For each question, provide your recommended answer.\n - Resolve dependency decisions before downstream design questions.\n - Update `CONTEXT.md` inline when terminology crystallizes.\n-- Offer ADRs only when the decision is hard to reverse, surprising without context, and a real tradeoff.\n+- Offer a SADR or target ADR only when the decision is hard to reverse, surprising without context, and a real tradeoff.\n - Convert unresolved questions into HITL blockers rather than allowing Codex to guess.\n \n ## PRD and Issues Standard\n@@ -103,7 +106,7 @@\n \n - Use the project's domain glossary vocabulary.\n - Avoid volatile file paths unless they are essential.\n-- Include implementation decisions, testing decisions, out-of-scope items, and relevant ADR/domain links.\n+- Include implementation decisions, testing decisions, out-of-scope items, and relevant SADR/target-ADR/domain links.\n - Identify deep-module opportunities and testable interfaces.\n \n When creating issues:\n@@ -121,7 +124,7 @@\n - Branch/worktree target.\n - Scope and non-goals.\n - Dependencies and locks.\n-- Relevant docs, ADRs, modules, and tests.\n+- Relevant docs, SADRs, target ADRs, modules, and tests.\n - Required skill invocations.\n - TDD expectations.\n - Pre-PR `code-review` gate expectations.\n@@ -152,7 +155,7 @@\n \n - Treat yourself as a judge, not implementer.\n - Do not write production code.\n-- Read `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant ADRs, PRD, task issue, PR body, diff, checks, reviews, labels, and `.shiki/` state.\n+- Read `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant SADRs and target ADRs, PRD, task issue, PR body, diff, checks, reviews, labels, and `.shiki/` state.\n - Evaluate every applicable checklist in `docs/agents/checklists.md`.\n - Map each acceptance criterion to durable evidence.\n - Distinguish implementation failure from missing evidence.\ndiff --git a/CONTEXT.md b/CONTEXT.md\nindex e61a9be06a0cce0d026484083a657bde2067e234..237ee8aa3ea8fab334ece49768bc5ed3b5af0974\n--- a/CONTEXT.md\n+++ b/CONTEXT.md\n@@ -25,7 +25,7 @@\n _Avoid_: jumping directly into code\n \n **grill-with-docs**:\n-A planning skill that challenges a plan against domain language, ADRs, code reality, and concrete edge scenarios. It resolves design-tree questions before PRD/issues. It runs inside Requirements Definition.\n+A planning skill that challenges a plan against domain language, Shiki SADRs, target ADRs, code reality, and concrete edge scenarios. It resolves design-tree questions before PRD/issues. It runs inside Requirements Definition.\n _Avoid_: generic brainstorming, silent assumptions\n \n **Requirements Definition**:\ndiff --git a/CONTRIBUTING.md b/CONTRIBUTING.md\nindex c6d9b656e73abb8a55e5f660123e03153dcb3e6d..9e37a8fbf668db5b22461aaae5548b8f1354394c\n--- a/CONTRIBUTING.md\n+++ b/CONTRIBUTING.md\n@@ -17,7 +17,7 @@\n 1. **Goal** \u2014 start from a user-approved Goal with an outcome, completion\n    conditions, non-goals, risk level, and success signals.\n 2. **grill-with-docs** \u2014 for any non-trivial change, challenge the plan against\n-   domain language, ADRs, and code reality before writing a PRD.\n+   domain language, Shiki SADRs, target ADRs, and code reality before writing a PRD.\n 3. **PRD** \u2014 capture settled product/engineering intent (`to-prd`).\n 4. **Spec Freeze** \u2014 the operator explicitly approves the PRD/requirements;\n    the plan records a `spec_freeze` block and no plan runs without it\n@@ -94,7 +94,7 @@\n   entries.\n - Do not hand-edit `.shiki/goals/*.json` or `.shiki/manifest.json` in a task PR.\n - Conversation state is not durable truth. If a decision matters, record it in\n-  GitHub, `.shiki/`, `CONTEXT.md`, or an ADR.\n+  GitHub, `.shiki/`, `CONTEXT.md`, a Shiki SADR, or a target ADR.\n \n ## Architecture Gate\n \n@@ -103,7 +103,7 @@\n or CI policy, package structure, three or more structural files, more than 100\n net new lines in a core module, three or more new files, or any high-risk label.\n When triggered, run `grill-with-docs` or `improve-codebase-architecture` and\n-record durable decisions in an ADR when needed (see `docs/adr/`).\n+record durable decisions in a Shiki SADR or target ADR as applicable (see `docs/adr/`).\n \n ## Safety\n \ndiff --git a/SYSTEM_PROMPT.md b/SYSTEM_PROMPT.md\nindex aaaea45e3a896c3a44ba89eec8bb84325ecf6c62..da009c86a88285269dee0155e58e550c6d744ea9\n--- a/SYSTEM_PROMPT.md\n+++ b/SYSTEM_PROMPT.md\n@@ -30,6 +30,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n Use the canonical source-of-truth order above. Runtime-specific reading orders\n are operational guidance only and must not override or reorder it. The current\n conversation is non-durable operator input.\n@@ -48,14 +51,14 @@\n \n A runtime may only do work that is explicitly assigned by the Goal, task, PR, or MergeGate decision.\n \n-Do not assume `openai/codex-action`, `OPENAI_API_KEY`, or API-key based Codex automation in the default Shiki path. API-key runners are explicit target-repo extensions and require their own ADR.\n+Do not assume `openai/codex-action`, `OPENAI_API_KEY`, or API-key based Codex automation in the default Shiki path. API-key runners are explicit target-repo extensions and require their own target ADR.\n \n ## 4. Non-negotiable Shiki flow\n \n For any non-trivial Goal, use this flow:\n \n 1. **Goal Seek**: define outcome, non-goals, risk level, completion criteria, and evidence requirements.\n-2. **grill-with-docs**: challenge the plan against `CONTEXT.md`, ADRs, code reality, terminology, boundaries, and edge scenarios. Ask one question at a time when the operator is available. If code can answer a question, inspect code instead of asking.\n+2. **grill-with-docs**: challenge the plan against `CONTEXT.md`, Shiki SADRs, target ADRs, code reality, terminology, boundaries, and edge scenarios. Ask one question at a time when the operator is available. If code can answer a question, inspect code instead of asking.\n 3. **Context & Impact**: identify modules, interfaces, seams, callers, dependencies, locks, risk, and verification surfaces.\n 4. **to-prd + Spec Freeze**: turn settled context into a PRD using domain vocabulary and testing decisions. The operator's explicit approval of the PRD is Spec Freeze, recorded as the plan's `spec_freeze` block; plans without it do not run. Steps 1-4 form Requirements Definition.\n 5. **to-issues**: decompose the PRD into independently grabbable vertical-slice issues. Prefer AFK slices; mark HITL slices when judgment is still required.\n@@ -75,7 +78,7 @@\n Before making any material change:\n \n 1. Read `AGENTS.md` and any runtime-specific wrapper such as `CLAUDE.md`.\n-2. Read `CONTEXT.md` and relevant ADRs.\n+2. Read `CONTEXT.md` and relevant SADRs and target ADRs.\n 3. Read the GitHub Issue / PR and `.shiki/` state for the active Goal or task.\n 4. Identify your role: Planner, Implementer, Reviewer, Completion Check Agent, Repairer, or Guardian-assist.\n 5. Identify Goal id, task id, branch, PR, dependencies, locks, risk level, required skills, and required evidence.\n@@ -103,7 +106,7 @@\n \n At minimum, identify:\n \n-- Relevant documents, ADRs, domain terms, and prior decisions.\n+- Relevant documents, SADRs, target ADRs, domain terms, and prior decisions.\n - Relevant modules, interfaces, seams, callers, owners, and tests.\n - Dependency relationships between tasks.\n - Candidate file locks and lock conflicts.\n@@ -152,7 +155,7 @@\n \n CCA must read:\n \n-- `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant ADRs.\n+- `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant SADRs and target ADRs.\n - Parent Goal issue, PRD issue, task issue, comments, and labels.\n - PR body, diff, commits, changed files, reviews, CI checks, and artifacts available to the workflow.\n - `.shiki/` task contract, locks, ledger entries, CCA checklist profile, and prior repair packets.\n@@ -214,7 +217,7 @@\n - Three or more new files.\n - Any high-risk or critical label.\n \n-When the architecture gate triggers, run `grill-with-docs` or `improve-codebase-architecture` as appropriate and record the decision in an ADR if it is hard to reverse or likely to be re-litigated.\n+When the architecture gate triggers, run `grill-with-docs` or `improve-codebase-architecture` as appropriate and record the decision in a Shiki SADR or target ADR as applicable if it is hard to reverse or likely to be re-litigated.\n \n ## 14. Skill Gate\n \n@@ -223,7 +226,7 @@\n Use these skills:\n \n - `setup-matt-pocock-skills`: before first use of the engineering skill set, or when issue tracker, triage labels, or domain docs are missing.\n-- `grill-with-docs`: default for non-trivial Goals before PRD/issues; required for ambiguous plans, terminology, boundaries, ADR-worthy tradeoffs, or design-tree decisions.\n+- `grill-with-docs`: default for non-trivial Goals before PRD/issues; required for ambiguous plans, terminology, boundaries, decision-record-worthy tradeoffs, or design-tree decisions.\n - `zoom-out`: unfamiliar code area or missing architectural map.\n - `to-prd`: convert settled context into a PRD.\n - `to-issues`: break a PRD, plan, or Goal into independently grabbable vertical-slice issues.\ndiff --git a/docs/adr/0001-keep-shiki-as-platform-name.md b/docs/adr/SADR-0001-keep-shiki-as-platform-name.md\nindex 20a9637c3b9d8c97de7ba8db7cfae4d82d9a7dba..8b685439959aada1b221c112f0ac3c51ca8a1695\n--- a/docs/adr/0001-keep-shiki-as-platform-name.md\n+++ b/docs/adr/SADR-0001-keep-shiki-as-platform-name.md\n@@ -1,3 +1,3 @@\n-# Keep Shiki As The Platform Name\n+# SADR-0001: Keep Shiki As The Platform Name\n \n Shiki remains the product name for the GitHub-native agentic engineering control plane. We considered more descriptive names such as MergePilot, MergeGate, and Kiroku, but kept Shiki because it carries the existing goal-seeking framework identity while remaining independent of Hermes, Codex, Claude Code, or any single runtime.\ndiff --git a/docs/adr/0002-use-agents-as-shared-constitution.md b/docs/adr/SADR-0002-use-agents-as-shared-constitution.md\nindex 73de30ecb078120eac540d9bc51855e3cd48d0e3..c5486182bf06fca36ea508496b013c67302fd989\n--- a/docs/adr/0002-use-agents-as-shared-constitution.md\n+++ b/docs/adr/SADR-0002-use-agents-as-shared-constitution.md\n@@ -1,3 +1,3 @@\n-# Use AGENTS As The Shared Agent Constitution\n+# SADR-0002: Use AGENTS As The Shared Agent Constitution\n \n Shiki uses `AGENTS.md` as the runtime-neutral constitution and `CLAUDE.md` as a thin Claude Code wrapper. This keeps Codex, Claude Code, Hermes Runner, and future Agent Runtimes aligned on the same GitHub-first MergeGate rules while still allowing runtime-specific instructions where needed.\ndiff --git a/docs/adr/0003-require-skill-gate-for-mergegate-readiness.md b/docs/adr/SADR-0003-require-skill-gate-for-mergegate-readiness.md\nindex 424be0c9af4c8f0136f1fde68a468c47ff84e764..7fc337987c7df9792f75f181dfcbed75fe5ffff2\n--- a/docs/adr/0003-require-skill-gate-for-mergegate-readiness.md\n+++ b/docs/adr/SADR-0003-require-skill-gate-for-mergegate-readiness.md\n@@ -1,3 +1,3 @@\n-# Require Skill Gate For MergeGate Readiness\n+# SADR-0003: Require Skill Gate For MergeGate Readiness\n \n Shiki requires relevant engineering skills to be invoked before execution when their triggers apply. This makes skills part of MergeGate readiness rather than optional prompt style, because Shiki depends on repeatable planning, issue decomposition, TDD, diagnosis, triage, and architecture review before autonomous or parallel agent work can be trusted.\ndiff --git a/docs/adr/0004-use-github-first-with-shiki-mirror.md b/docs/adr/SADR-0004-use-github-first-with-shiki-mirror.md\nindex d0a3a9174dc646a945a9d325954ffaf00c352040..a79c54ae9ee8eed2a0524192b864da4e22a92d3f\n--- a/docs/adr/0004-use-github-first-with-shiki-mirror.md\n+++ b/docs/adr/SADR-0004-use-github-first-with-shiki-mirror.md\n@@ -1,4 +1,4 @@\n-# ADR 0004: Use GitHub First With A Repository-Local Shiki Mirror\n+# SADR-0004: Use GitHub First With A Repository-Local Shiki Mirror\n \n ## Status\n \ndiff --git a/docs/adr/0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md b/docs/adr/SADR-0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\nindex b339baf49b89d9290e79c3d51b36e7b2bde3bd95..0f3c3eb9afccdb3403ce978e21d04fe12e495da0\n--- a/docs/adr/0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\n+++ b/docs/adr/SADR-0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\n@@ -1,8 +1,8 @@\n-# ADR 0005: Use Codex As Front And Claude Code Action As The GitHub Runtime\n+# SADR-0005: Use Codex As Front And Claude Code Action As The GitHub Runtime\n \n ## Status\n \n-Accepted (implementer default superseded by ADR 0008; the auth model and the\n+Accepted (implementer default superseded by SADR-0008; the auth model and the\n Claude Code Action GitHub runtime decision remain in force)\n \n ## Context\n@@ -26,4 +26,4 @@\n - Shiki avoids requiring `OPENAI_API_KEY` for the default workflow.\n - Codex work happens through the user's authenticated Codex front surface, not as a hidden backend Action.\n - Claude Code Action is configured for OAuth-token based subscription usage by default.\n-- API-key based automation remains possible only as an explicit target-repo extension with its own ADR.\n+- API-key based automation remains possible only as an explicit target-repo extension with its own target ADR.\ndiff --git a/docs/adr/0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md b/docs/adr/SADR-0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\nindex 4bcae9d1e52f0a37b8e9fbafa5cd14b6ee1993bd..7a38538a6ae9929567f0ade67e7ea6a60d0b3f7e\n--- a/docs/adr/0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\n+++ b/docs/adr/SADR-0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\n@@ -1,4 +1,4 @@\n-# ADR 0006: Separate Codex Implementation, CCA Completion, And MergeGate Authorization\n+# SADR-0006: Separate Codex Implementation, CCA Completion, And MergeGate Authorization\n \n ## Status\n \ndiff --git a/docs/adr/0007-packaging-and-release.md b/docs/adr/SADR-0007-packaging-and-release.md\nindex 79b84ddb97167372b44641e5b7860e400ce80822..bb445a2e2e856d9d1f5fdbe35de8d4e8cec0fb32\n--- a/docs/adr/0007-packaging-and-release.md\n+++ b/docs/adr/SADR-0007-packaging-and-release.md\n@@ -1,4 +1,4 @@\n-# ADR 0007: Packaging And Release For Shiki\n+# SADR-0007: Packaging And Release For Shiki\n \n ## Status\n \n@@ -25,7 +25,7 @@\n   not introduce third-party runtime dependencies or break the shim.\n - Releases must run inside the operator's subscription-authenticated toolchain\n   and must not require extra secrets beyond the built-in `GITHUB_TOKEN`\n-  (consistent with ADR 0005's no-API-key-by-default stance).\n+  (consistent with SADR-0005's no-API-key-by-default stance).\n - `.shiki/manifest.json` already encodes the install surface (`install.include`,\n   `install.create_directories`, `install.exclude_from_commit`), and it must not\n   be mutated as part of this change.\n@@ -34,7 +34,7 @@\n \n This is a hard-to-reverse platform decision (it sets the version source of\n truth, the release trigger, and the public install contract), so it is recorded\n-as an ADR.\n+as a SADR.\n \n ## Decision\n \n@@ -104,7 +104,7 @@\n 3. **Publishing to PyPI.** Rejected for now: Shiki is distributed as a\n    repository template + git-installable CLI, not a PyPI library; PyPI would add\n    credentials/secrets and a different versioning contract. Can be added later as\n-   its own ADR if a packaged distribution is needed.\n+   its own SADR if a packaged distribution is needed.\n 4. **Dynamic version (read `VERSION` via a build backend `dynamic` field).**\n    Rejected to keep `pyproject.toml` backend-agnostic and dependency-free; the\n    small duplication between `VERSION` and `pyproject.toml` is guarded by the\n@@ -123,4 +123,4 @@\n   releasing versions the whole platform but ships only `install.include` to\n   targets.\n - Future PyPI distribution or a console entry point are deferred and would each\n-  warrant their own ADR.\n+  warrant their own SADR.\ndiff --git a/docs/adr/0008-use-claude-code-as-default-implementer.md b/docs/adr/SADR-0008-use-claude-code-as-default-implementer.md\nindex dedf4510b7c7799521422f622029563d373db031..35c2625d908167fbc58855b5b63e283805f167e8\n--- a/docs/adr/0008-use-claude-code-as-default-implementer.md\n+++ b/docs/adr/SADR-0008-use-claude-code-as-default-implementer.md\n@@ -1,12 +1,12 @@\n-# ADR 0008: Use Claude Code As The Default Implementer Runtime\n+# SADR-0008: Use Claude Code As The Default Implementer Runtime\n \n ## Status\n \n-Proposed (supersedes ADR 0005 for the implementer default; keeps its auth model)\n+Proposed (supersedes SADR-0005 for the implementer default; keeps its auth model)\n \n ## Context\n \n-ADR 0005 made Codex Front the default implementation runtime, with Claude Code\n+SADR-0005 made Codex Front the default implementation runtime, with Claude Code\n restricted to planner and reviewer roles by the runtime registry, and the only\n autonomous implementation adapter was `shiki runner codex`.\n \n@@ -18,13 +18,13 @@\n - The platform's planning, review, and CCA layers already run on Claude Code,\n   and Claude Code's native capabilities (the built-in `/code-review` skill and\n   the Workflow multi-agent orchestration tool) have no Codex equivalent.\n-- The Goal lifecycle redesign (ADR 0009) requires an autonomous post-freeze\n+- The Goal lifecycle redesign (SADR-0009) requires an autonomous post-freeze\n   loop driven from the operator's Claude Code session, including bounded\n   repair dispatch, which must not depend on a runtime that cannot be invoked\n   unattended.\n \n The constitution claims Shiki is runtime-agnostic (\"not a Claude-only\n-workflow\", ADR 0002). Any change to the implementer default must preserve the\n+workflow\", SADR-0002). Any change to the implementer default must preserve the\n registry-based runtime model rather than hardcoding one vendor.\n \n ## Decision\n@@ -39,7 +39,7 @@\n   the task to review or repair-needed.\n - Keep Codex registered as an optional implementer runtime. `shiki runner\n   codex`, the Codex handoff templates, and the ChatGPT OAuth auth model from\n-  ADR 0005 remain supported for tasks explicitly assigned to `codex`.\n+  SADR-0005 remain supported for tasks explicitly assigned to `codex`.\n - Commit to mandating two Claude-native capabilities as loop contract in\n   follow-up Goals (status: the code-review gate was delivered by Goal G-B \u2014\n   skill registry, checklists item PR-12, default required_skills \u2014 while the\n@@ -60,7 +60,7 @@\n \n Out of scope: removing Codex support, changing the CCA runtime (Claude Code\n Action with `CLAUDE_CODE_OAUTH_TOKEN` stays), and API-key based automation\n-(still requires its own ADR per ADR 0005).\n+(still requires its own target ADR per SADR-0005).\n \n ## Consequences\n \n@@ -100,7 +100,7 @@\n   harness rejection of direct `codex exec` is outside Shiki's control, and the\n   plan's mandatory native capabilities cannot run on Codex at all.\n - **Remove Codex entirely**: rejected; it abandons the runtime-agnostic\n-  constitution (ADR 0002) for no operational gain and forecloses\n+  constitution (SADR-0002) for no operational gain and forecloses\n   cross-runtime adversarial review.\n - **GitHub Actions cloud implementation via claude-code-action**: rejected as\n   default; durable evidence is automatic but token cost, Node 24 workflow\ndiff --git a/docs/adr/0009-requirements-definition-and-spec-freeze.md b/docs/adr/SADR-0009-requirements-definition-and-spec-freeze.md\nindex 29b3f7871bef9203a8b4a831a65c39eb2f506e42..52e7dd69ad0aa4efe0b68c62678abbf7df02aaec\n--- a/docs/adr/0009-requirements-definition-and-spec-freeze.md\n+++ b/docs/adr/SADR-0009-requirements-definition-and-spec-freeze.md\n@@ -1,4 +1,4 @@\n-# ADR 0009: Merge Grill And Planning Into Requirements Definition With Spec Freeze\n+# SADR-0009: Merge Grill And Planning Into Requirements Definition With Spec Freeze\n \n ## Status\n \ndiff --git a/docs/adr/0010-external-ai-guardian-review.md b/docs/adr/SADR-0010-external-ai-guardian-review.md\nindex e6108bbe8bb4e73e0ff4684c8f78001a80438c4e..f3510b145664298adafb151b6f0db51b972f01ec\n--- a/docs/adr/0010-external-ai-guardian-review.md\n+++ b/docs/adr/SADR-0010-external-ai-guardian-review.md\n@@ -1,4 +1,4 @@\n-# ADR 0010: External AI Guardian Review As A First-Class Approval Authority\n+# SADR-0010: External AI Guardian Review As A First-Class Approval Authority\n \n ## Status\n \n@@ -8,7 +8,7 @@\n \n Shiki is intended to be an autonomous, recursive, self-improving agent\n framework: authority-in-the-loop, not human-in-the-loop. Until now the Guardian\n-approval required for high/critical-risk merges (guardian-policy.json, ADR\n+approval required for high/critical-risk merges (guardian-policy.json, SADR\n governance) recognized only human authority sources \u2014 a GitHub review approval,\n the `guardian:approved` label applied by a configured Guardian, or a comment\n carrying `Guardian approval granted` plus the current head SHA.\n@@ -89,7 +89,7 @@\n   autonomous design the operator intends and produced the impersonation\n   incident.\n - **Record AI approval as an operator comment**: rejected as forgery of human\n-  authority \u2014 the exact failure this ADR corrects.\n+  authority \u2014 the exact failure this SADR corrects.\n - **Deliver the artifact as a committed `.shiki/` file**: rejected; a committed\n   approval inside the PR being approved is forgeable by anyone who can push.\n - **A dedicated reviewer bot identity instead of a relayed comment**: deferred;\ndiff --git a/docs/adr/0011-autonomous-quality-gate-evidence.md b/docs/adr/SADR-0011-autonomous-quality-gate-evidence.md\nindex bca0246c5d46e44afbde15486a75eec425fc02f5..9c93eac16d8c3a6d4b11bd7224e3955102cd7db4\n--- a/docs/adr/0011-autonomous-quality-gate-evidence.md\n+++ b/docs/adr/SADR-0011-autonomous-quality-gate-evidence.md\n@@ -1,4 +1,4 @@\n-# ADR 0011: Autonomous Quality-Gate Evidence \u2014 Deterministic Facts And An Independent Verifier\n+# SADR-0011: Autonomous Quality-Gate Evidence \u2014 Deterministic Facts And An Independent Verifier\n \n ## Status\n \ndiff --git a/docs/adr/0012-autonomous-loop-closeout-to-main.md b/docs/adr/SADR-0012-autonomous-loop-closeout-to-main.md\nindex 637fad6dde4989c9c2bf0f81f676d8d29991123c..ec7d85f81030a676bcbba28b43eb365422a36eac\n--- a/docs/adr/0012-autonomous-loop-closeout-to-main.md\n+++ b/docs/adr/SADR-0012-autonomous-loop-closeout-to-main.md\n@@ -1,4 +1,4 @@\n-# 0012 \u2014 Autonomous loop pushes goal completion to main via a closeout PR\n+# SADR-0012 \u2014 Autonomous loop pushes goal completion to main via a closeout PR\n \n - Status: accepted\n - Date: 2026-06-18\ndiff --git a/docs/adr/0013-bootstrap-grants-actions-approve-pull-requests.md b/docs/adr/SADR-0013-bootstrap-grants-actions-approve-pull-requests.md\nindex b6c69d64f1c5ced346b88d6859586715b3b5bd0e..cf48ff4a6716267f539ca58edd1a852e7c7a2d64\n--- a/docs/adr/0013-bootstrap-grants-actions-approve-pull-requests.md\n+++ b/docs/adr/SADR-0013-bootstrap-grants-actions-approve-pull-requests.md\n@@ -1,4 +1,4 @@\n-# ADR 0013: Bootstrap Grants GitHub Actions Permission To Create And Approve Pull Requests\n+# SADR-0013: Bootstrap Grants GitHub Actions Permission To Create And Approve Pull Requests\n \n ## Status\n \n@@ -31,7 +31,7 @@\n security tradeoff: it lets repository automation satisfy a human-review gate. In\n a multi-maintainer repo that could let a compromised or buggy workflow\n self-approve changes. So whether bootstrap turns it on by default \u2014 and with\n-what default token scope \u2014 is an ADR-worthy decision, not an implementation\n+what default token scope \u2014 is a SADR-worthy decision, not an implementation\n detail.\n \n ## Decision\n@@ -48,7 +48,7 @@\n Specifically:\n \n - **`can_approve_pull_request_reviews` defaults to `true`.** Shiki's default\n-  posture is solo/AFK autonomous operation (ADR 0008/0009/0012), where the CCA\n+  posture is solo/AFK autonomous operation (SADR-0008/0009/0012), where the CCA\n   Review Bridge is the only path that can satisfy `required_review: true` after\n   the CCA and Guardian gates have already passed. The default makes a\n   freshly-bootstrapped repo able to run the autonomous loop end to end without a\ndiff --git a/docs/adr/0014-codex-app-external-ai-guardian-ui-adapter.md b/docs/adr/SADR-0014-codex-app-external-ai-guardian-ui-adapter.md\nindex 238db06279334caa341c117362df9271382a7fcd..65f12e9939321e1b3e389a34c822fc1549e5155d\n--- a/docs/adr/0014-codex-app-external-ai-guardian-ui-adapter.md\n+++ b/docs/adr/SADR-0014-codex-app-external-ai-guardian-ui-adapter.md\n@@ -1,4 +1,4 @@\n-# ADR 0014: Use Codex App As The External AI Guardian UI Adapter\n+# SADR-0014: Use Codex App As The External AI Guardian UI Adapter\n \n ## Status\n \ndiff --git a/docs/adr/0015-contract-approval-before-dispatch.md b/docs/adr/SADR-0015-contract-approval-before-dispatch.md\nindex fad6c656741a20a7684ba16080a7db159f63a52f..6061271a46d3f1d6ae7174477de3fb75673123a6\n--- a/docs/adr/0015-contract-approval-before-dispatch.md\n+++ b/docs/adr/SADR-0015-contract-approval-before-dispatch.md\n@@ -1,4 +1,4 @@\n-# ADR 0015: Approve The Task Contract Before Dispatch, Not The Diff After It\n+# SADR-0015: Approve The Task Contract Before Dispatch, Not The Diff After It\n \n ## Status\n \n@@ -9,7 +9,7 @@\n Guardian approval for high/critical risk is expressible only on a pull request.\n Every approval source in `guardian-policy.json` \u2014 GitHub review, the\n `guardian:approved` label, the `Guardian approval granted` comment, and the\n-`external_ai_guardian_review` artifact (ADR 0010/0014) \u2014 is bound to a PR, and\n+`external_ai_guardian_review` artifact (SADR-0010/0014) \u2014 is bound to a PR, and\n three of the four require the current head SHA. Approval therefore cannot exist\n before an implementation PR exists.\n \n@@ -81,7 +81,7 @@\n An envelope breach must surface only on the non-repairable policy gate, never on\n the repairable `MergeGate metadata check`. Otherwise the Goal Loop routes it to\n `dispatch_repair` \u2014 instructing a runner to make the breach go away \u2014 which is\n-exactly what this ADR forbids.\n+exactly what this SADR forbids.\n \n **Contract immutability.** After registration, an implementation PR must not\n change the governance fields of its own task contract (`scope`, `non_goals`,\n@@ -116,7 +116,7 @@\n fails any verdict that is not `complete`, and the signal the CCA prompt reads is\n computed from `evaluate_guardian_approval` alone. Without the OR-merge at the\n signal, a contract-approved PR still returns `needs_guardian`, the CCA job fails,\n-and the MergeGate policy check never runs \u2014 the gate this ADR changes would never\n+and the MergeGate policy check never runs \u2014 the gate this SADR changes would never\n be reached. The CCA *prompt* needs no change: a `contract_approval` entry in the\n signal's existing `sources` array satisfies its current wording.\n \n@@ -130,7 +130,7 @@\n written, and an approved contract lets its implementation PRs merge through the\n same automatic path as low/medium risk. The wasted implement-then-block cycle\n disappears. Guardian review shifts from reading diffs to reading contracts, which\n-is both earlier and smaller; the external AI Guardian path (ADR 0010/0014) works\n+is both earlier and smaller; the external AI Guardian path (SADR-0010/0014) works\n on contracts with no change.\n \n We accept that the Guardian authorizes a class of change without seeing its\n@@ -146,7 +146,7 @@\n adversarial tests for the new approval path equivalent in rigor to\n `test_shiki_governance_evidence.sh`.\n \n-Bootstrap costs, which apply only while this ADR is being implemented. The tasks\n+Bootstrap costs, which apply only while this SADR is being implemented. The tasks\n that build Contract Approval merge under the existing rules, so the ones\n dispatched after the pre-dispatch gate lands but before the approval path lands\n need a Guardian-approved Contract PR *and* a live Guardian approval on their\n@@ -210,7 +210,7 @@\n risk; they do not bound authorized work.\n \n **Auto-escalate risk on envelope breach instead of stopping.** Returns the\n-workflow to a diff-time Guardian decision \u2014 the exact behavior this ADR removes.\n+workflow to a diff-time Guardian decision \u2014 the exact behavior this SADR removes.\n \n **Resolve governance fields from the base snapshot only.** Rejected against the\n registration flow: implementation PRs add their own contract, so the base has no\ndiff --git a/docs/adr/0016-locks-bound-product-paths.md b/docs/adr/SADR-0016-locks-bound-product-paths.md\nindex b5e850f3f1f7f4ef6e7f9d1763a29a51bd300c45..46b40a9b99a7aae644c02bb458f5473ff0a90ce2\n--- a/docs/adr/0016-locks-bound-product-paths.md\n+++ b/docs/adr/SADR-0016-locks-bound-product-paths.md\n@@ -1,4 +1,4 @@\n-# ADR 0016: Locks bound product paths; the Shiki mirror is bound by record identity\n+# SADR-0016: Locks bound product paths; the Shiki mirror is bound by record identity\n \n ## Status\n \n@@ -6,8 +6,8 @@\n \n - Date: 2026-07-28\n - Deciders: operator (mizutani-140), Claude (planner)\n-- Related: ADR 0004 (GitHub-first with a `.shiki` mirror), ADR 0009 (Spec Freeze),\n-  ADR 0012 (autonomous loop closeout to main), ADR 0015 (Contract Approval before\n+- Related: SADR-0004 (GitHub-first with a `.shiki` mirror), SADR-0009 (Spec Freeze),\n+  SADR-0012 (autonomous loop closeout to main), SADR-0015 (Contract Approval before\n   dispatch), remediation goal task R-02, the live PR #179 deadlock.\n \n ## Context\n@@ -90,11 +90,11 @@\n files, documentation files and named source modules are nameable; runtime-\n allocated ledger, runner and report ids are not \u2014 and those live in `.shiki`,\n which locks no longer govern. Existing frozen contracts are brought into line by\n-a one-time Spec Amendment per plan, which must land before ADR 0015's contract\n+a one-time Spec Amendment per plan, which must land before SADR-0015's contract\n immutability makes `locks` unwritable from the implementation PR.\n \n **Lock state is resolved from the durable side.** MergeGate resolves the lock\n-registry, and ADR 0015's governance fields, from the base `.shiki` snapshot when\n+registry, and SADR-0015's governance fields, from the base `.shiki` snapshot when\n present, falling back to head only when absent. The `MergeGate metadata check`\n job is given `--base-shiki`, which it does not receive today. Locks are not\n derived from the live open-pull-request set: a pull request body is untrusted\n@@ -108,7 +108,7 @@\n Explicitly out of scope: moving MergeGate's contract resolution off the PR\n checkout wholesale; making the mirror a projection of GitHub state; introducing\n new lock kinds; moving ledger evidence out of the repository; and unwinding\n-ADR 0012's closeout pull request. Registration of multi-task goals is ADR 0015's\n+SADR-0012's closeout pull request. Registration of multi-task goals is SADR-0015's\n Contract PR and is not re-decided here.\n \n ## Consequences\n@@ -127,7 +127,7 @@\n \n Risks accepted. First, goal-scoped identity is only as strong as `goal_id`,\n which is resolved from the PR body and the head task file; the mitigation is to\n-add `goal_id` to ADR 0015's frozen governance field set, and until that lands a\n+add `goal_id` to SADR-0015's frozen governance field set, and until that lands a\n task file can name a foreign goal. Second, base-when-present resolution is\n unavailable on fork pull requests, where the head is contributor-controlled;\n fork pull requests already cannot complete a Shiki merge, and this decision does\n@@ -139,7 +139,7 @@\n path, not by implementation pull requests.\n \n New obligations: one Spec Amendment per frozen plan to rewrite `locks`, ordered\n-before ADR 0015 T1; extension of the adversarial suites in\n+before SADR-0015 T1; extension of the adversarial suites in\n `scripts/test_shiki_adversarial_state.sh` and\n `scripts/test_shiki_governance_evidence.sh` to cover the eleven newly-scoped\n directories; and updating `scripts/test_shiki_loop_lock_guard.sh`,\n@@ -151,32 +151,32 @@\n whether the loop's transitive sync of ledger `evidence` references can be\n deleted once the Contract PR carries its own source plan.\n \n-## Relationship to ADR 0004\n+## Relationship to SADR-0004\n \n-This ADR **clarifies something ADR 0004 left unspecified**. ADR 0004 decides that\n+This SADR **clarifies something SADR-0004 left unspecified**. SADR-0004 decides that\n GitHub is the operational source of truth and that each target repository keeps a\n `.shiki/` mirror. It says nothing about pull requests writing the mirror, does\n not name MergeGate, and does not make the mirror an input to a gate. Nothing in\n-ADR 0004's Decision or Consequences is amended, superseded or weakened: the\n+SADR-0004's Decision or Consequences is amended, superseded or weakened: the\n mirror still exists, still records the same artifacts, and pull request\n-boundaries remain the coordination surface. What this ADR settles is the question\n+boundaries remain the coordination surface. What this SADR settles is the question\n 0004 never reached \u2014 by what authority a pull request may write a mirror record \u2014\n and it answers it the way `.shiki/manifest.json` already declares\n (`pr_mutation: current-task-or-current-goal-only`).\n \n-It **amends ADR 0012**, whose Decision item 5 states that \"Loop-executed tasks\n+It **amends SADR-0012**, whose Decision item 5 states that \"Loop-executed tasks\n declare the `path:.shiki/**` lock, which covers every `.shiki` file the closeout\n stages ... so `files_outside_locks` is satisfied without per-file lock\n bookkeeping.\" That clause is replaced: the closeout's mirror writes are\n-authorized by record identity, not by a declared lock. ADR 0012's substantive\n+authorized by record identity, not by a declared lock. SADR-0012's substantive\n decision \u2014 that completion reaches `main` through a closeout pull request rather\n than local attestation \u2014 is preserved unchanged.\n \n-It is **complementary to ADR 0015**, not a substitute. ADR 0015 owns contract\n+It is **complementary to SADR-0015**, not a substitute. SADR-0015 owns contract\n immutability, the pre-dispatch gate, the Contract PR and the Scope Envelope. This\n-ADR owns what a lock means and who may write the mirror. The one ordering\n+SADR owns what a lock means and who may write the mirror. The one ordering\n constraint between them is that the lock-narrowing Spec Amendment must precede\n-ADR 0015 T1's freezing of the `locks` field.\n+SADR-0015 T1's freezing of the `locks` field.\n \n ## Alternatives Considered\n \n@@ -186,7 +186,7 @@\n the only writer that can register them may run. Migrating the Guardian signal to\n that store makes every registration PR fail closed to `needs_guardian` at every\n risk level, because the task file is by definition absent from the store, which\n-removes the low/medium autonomous path ADR 0008/0009 exists to provide. And it\n+removes the low/medium autonomous path SADR-0008/0009 exists to provide. And it\n leaves schemas, manifest, config, guardian policy, templates, migrations, plans,\n worktrees, runner records, runs and handoffs with no writer at all in a\n repository whose product is `.shiki`. Measured benefit on the frozen backlog:\n@@ -199,14 +199,14 @@\n namespace is not in fact ownership-partitioned \u2014 56 ledger entries carry no\n `task_id`, and one goal DAG is writable by 35 registered tasks. The correct\n expression of its insight is that locks do not govern the mirror at all, which is\n-what this ADR decides.\n+what this SADR decides.\n \n **Make the mirror a projection written only by a registration writer and a\n post-merge projection job.** Rejected. It requires a bypass actor with write\n access to the protected default branch; it makes its own master invariant\n readable from a manifest that pull requests may edit; it deletes both reconcile\n modes and leaves a corrupted mirror with no in-band repair path, contradicting\n-ADR 0004's consequence that mirror conflicts are repaired against GitHub state;\n+SADR-0004's consequence that mirror conflicts are repaired against GitHub state;\n and its content-derived ids are rejected by `validate_shiki`'s currently-green id\n pattern.\n \ndiff --git a/docs/adr/0017-bookkeeping-closeouts-do-not-inherit-risk.md b/docs/adr/SADR-0017-bookkeeping-closeouts-do-not-inherit-risk.md\nindex 554e3a0d526a72ca067108c5464cc68c3edcf174..b9676857a4713850e063af7c39c8e0b01ecb4c66\n--- a/docs/adr/0017-bookkeeping-closeouts-do-not-inherit-risk.md\n+++ b/docs/adr/SADR-0017-bookkeeping-closeouts-do-not-inherit-risk.md\n@@ -1,4 +1,4 @@\n-# ADR 0017: A Bookkeeping Closeout Does Not Inherit Its Task's Risk\n+# SADR-0017: A Bookkeeping Closeout Does Not Inherit Its Task's Risk\n \n ## Status\n \n@@ -6,7 +6,7 @@\n \n ## Context\n \n-ADR 0012 made the goal loop push a task's completion to the default branch through a\n+SADR-0012 made the goal loop push a task's completion to the default branch through a\n closeout pull request: after the implementation PR merges, the loop opens a second PR\n carrying `task.status = done`, `lock.state = released`, and \u2014 when the task completes\n its goal \u2014 `goal.status = complete` plus one scorecard report. Nothing else. The\n@@ -49,14 +49,14 @@\n 4. The task file's governance fields (`scope`, `non_goals`, `required_skills`,\n    `risk_level`, `locks`, `acceptance_checks`, `test_command`) are byte-identical to\n    the base snapshot. Only `status`, `expected_pr`, `closeout_pr`, `ledger_evidence`\n-   and `expected_branch` may differ \u2014 the ADR 0015 contract-immutability rule,\n+   and `expected_branch` may differ \u2014 the SADR-0015 contract-immutability rule,\n    unchanged.\n \n    `expected_branch` is in that set because a closeout is *by construction* on a\n    different branch from the implementation it closes, and the loop rewrites the\n    field accordingly. It is safe to let it differ because MergeGate independently\n    requires `expected_branch` to equal the PR's actual head ref, so a PR cannot set\n-   it to anything other than the branch it is really on. The first draft of this ADR\n+   it to anything other than the branch it is really on. The first draft of this SADR\n    omitted it, which made the exemption unreachable: every real closeout failed\n    condition 4 on a field the loop always changes. See PR #209.\n 5. The state transitions are exactly the terminal set: task `review` \u2192 `done`, lock\n@@ -113,12 +113,12 @@\n classification must be derived, not asserted.\n \n **Fold the closeout into the implementation PR** so no second PR exists. Rejected under\n-ADR 0012, which recorded why: a task file that is already `done` on the implementation\n+SADR-0012, which recorded why: a task file that is already `done` on the implementation\n branch short-circuits the loop's review \u2192 CCA \u2192 merge \u2192 done ordering into a local\n goal-completion before any merge happens.\n \n **Have the loop auto-merge closeouts with elevated credentials.** Rejected outright: that\n-is a runtime authoring its own approval, which ADR 0010 and the constitution's\n+is a runtime authoring its own approval, which SADR-0010 and the constitution's\n approval-record prohibition forbid. It would also be indistinguishable, in the evidence,\n from the forgery those rules exist to prevent.\n \ndiff --git a/docs/adr/0018-carried-guardian-approval-across-a-proven-base-sync.md b/docs/adr/SADR-0018-carried-guardian-approval-across-a-proven-base-sync.md\nindex 22358562da390ed8b45cd600da08a43d378b4680..e6e2907cefa776460af73ddf7d39db605f3c39b0\n--- a/docs/adr/0018-carried-guardian-approval-across-a-proven-base-sync.md\n+++ b/docs/adr/SADR-0018-carried-guardian-approval-across-a-proven-base-sync.md\n@@ -1,4 +1,4 @@\n-# ADR 0018: Carried Guardian Approval Across A Proven Base Sync\n+# SADR-0018: Carried Guardian Approval Across A Proven Base Sync\n \n ## Status\n \n@@ -34,7 +34,7 @@\n Guardian evaluator that consumes it (`shiki_guardian.evaluate_guardian_approval`,\n the `guardian_comment_carried` source) were built and tested in isolation. Neither\n does anything until a caller passes the flags that turn the carry on, so the\n-feature was invisible in CI. This ADR records the decision the two of them\n+feature was invisible in CI. This SADR records the decision the two of them\n implement, and this task wires it into the four CI invocations that judge Guardian\n approval.\n \ndiff --git a/docs/adr/README.md b/docs/adr/README.md\nindex c70e057a4520c36e042ab68c53cd4a21ef709b71..5da43856836e2ce6a150f8127766e2f88355ab4b\n--- a/docs/adr/README.md\n+++ b/docs/adr/README.md\n@@ -1,40 +1,41 @@\n-# Architecture Decision Records\n+# Decision Record Namespaces\n \n-This directory holds Shiki's Architecture Decision Records (ADRs): durable\n-records of hard-to-reverse platform decisions. ADRs are part of the Shiki source\n-of truth alongside GitHub state, the `.shiki/` mirror, and `CONTEXT.md`.\n+This directory holds two independent decision namespaces:\n \n-## How ADRs Work\n+- Shiki Architecture Decision Records (SADRs), named\n+  `SADR-NNNN-<short-slug>.md`, record hard-to-reverse Shiki platform decisions.\n+- Target Architecture Decision Records (ADRs), named\n+  `NNNN-<short-slug>.md`, belong to the target product.\n \n-- New ADRs are added as `docs/adr/NNNN-<short-slug>.md`, where `NNNN` is the next\n-  zero-padded sequence number.\n-- The **canonical list of ADRs is the set of files matching `docs/adr/*.md`** in\n-  this directory. Browse `docs/adr/*.md` for the authoritative, current set.\n-- Use [`template.md`](template.md) as the starting point for a new ADR.\n-- Record an ADR when a decision is hard to reverse, surprising without context,\n-  and a real tradeoff. Do not write an ADR for routine or easily reversible\n-  choices.\n+The same four-digit number may appear once in each namespace. A platform\n+`SADR-NNNN` citation resolves only to `SADR-NNNN-*.md`; a target `ADR NNNN`\n+citation resolves only to `NNNN-*.md`.\n \n-Using the `docs/adr/*.md` glob as the source of truth means other PRs can add an\n-ADR without editing this index file, avoiding merge contention on a\n-hand-maintained enumeration.\n+## How Decision Records Work\n \n-## Current ADRs (snapshot)\n-\n-The list below is a convenience snapshot and may lag behind the directory. When\n-in doubt, list `docs/adr/*.md`.\n+- New Shiki platform decisions use the next zero-padded SADR number.\n+- New target product decisions use the target's own ADR sequence.\n+- The canonical list is the files in `docs/adr/`; no hand-maintained index is\n+  authoritative.\n+- Use [`template.md`](template.md) as the starting point and choose the filename\n+  namespace that owns the decision.\n+- Record a decision when it is hard to reverse, surprising without context, and\n+  a real tradeoff. Do not write a decision record for routine or easily\n+  reversible choices.\n \n-- [0001 \u2014 Keep Shiki As The Platform Name](0001-keep-shiki-as-platform-name.md)\n-- [0002 \u2014 Use AGENTS As The Shared Agent Constitution](0002-use-agents-as-shared-constitution.md)\n-- [0003 \u2014 Require Skill Gate For MergeGate Readiness](0003-require-skill-gate-for-mergegate-readiness.md)\n-- [0004 \u2014 Use GitHub First With A Repository-Local Shiki Mirror](0004-use-github-first-with-shiki-mirror.md)\n-- [0005 \u2014 Use Codex As Front And Claude Code Action As The GitHub Runtime](0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md)\n-- [0006 \u2014 Separate Codex Implementation, CCA Completion, And MergeGate Authorization](0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md)\n+Using the directory as the source of truth lets independent changes add records\n+without contending on an enumerated index.\n \n-## Adding A New ADR\n+## Adding A Shiki SADR\n \n-1. Copy `template.md` to `docs/adr/NNNN-<short-slug>.md`.\n+1. Copy `template.md` to `docs/adr/SADR-NNNN-<short-slug>.md`.\n 2. Fill in the title, status, context, decision, and consequences.\n-3. Set the status to `Proposed`, then `Accepted` once the decision is approved.\n-4. Open a PR following `CONTRIBUTING.md`. You do not need to edit this index for\n-   the ADR to be discoverable via `docs/adr/*.md`.\n+3. Set the status to `Proposed`, then `Accepted` once the proper authority has\n+   approved the decision.\n+4. Open a PR following `CONTRIBUTING.md`.\n+\n+## Adding A Target ADR\n+\n+1. Copy `template.md` to `docs/adr/NNNN-<short-slug>.md` using the target's own\n+   sequence.\n+2. Cite it as `ADR NNNN`; do not prefix its filename with `SADR-`.\ndiff --git a/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md b/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\nnew file mode 100644\nindex 0000000000000000000000000000000000000000..7455ce1f3bed02ddddb851ac21a1d1a648c9aeba\n--- /dev/null\n+++ b/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\n@@ -0,0 +1,55 @@\n+# SADR-0019: Separate Shiki Platform SADRs From Target ADRs\n+\n+## Status\n+\n+Accepted\n+\n+## Context\n+\n+Shiki installs its platform decision records into `docs/adr/` in every target\n+repository. Shiki and its targets previously used the same numeric\n+`NNNN-<slug>.md` namespace. A target that already owned ADRs 0013 through 0018\n+therefore acquired two records for each number during installation, making an\n+`ADR NNNN` citation ambiguous. Renumbering target decisions would rewrite target\n+history, while renumbering Shiki decisions per target would destroy their stable\n+platform identity.\n+\n+Upgrades also need to remove the former Shiki-owned numeric paths without ever\n+mistaking a target-owned ADR for platform content. The install stamp already\n+records exact shipped paths and content digests, so it can prove ownership only\n+when both still match.\n+\n+## Decision\n+\n+Shiki platform decisions use the `SADR` namespace:\n+\n+- records are named `docs/adr/SADR-NNNN-<slug>.md`;\n+- live platform citations use `SADR-NNNN`;\n+- target product decisions retain `docs/adr/NNNN-<slug>.md` and `ADR NNNN`;\n+- the reference validator resolves the two namespaces independently, so the same\n+  four-digit number may exist once in each namespace.\n+\n+Before a forced upgrade writes anything, the installer inventories the exact\n+former numeric Shiki ADR paths. It may delete a legacy path only when the\n+existing install stamp contains that exact path and its recorded SHA-256 digest\n+matches the file currently on disk. All ownership blockers are reported\n+together and abort the upgrade before its first write. A non-forced install\n+never performs this cleanup.\n+\n+Historical `.shiki/` evidence remains unchanged because it records the namespace\n+that existed when that evidence was produced.\n+\n+## Consequences\n+\n+- Targets may use any ADR number without colliding with Shiki's platform SADRs.\n+- Fresh installs preserve target ADR bytes and install a stable SADR set.\n+- Forced upgrades fail closed when legacy ownership cannot be proven, and a\n+  successful upgrade refreshes the stamp without removed legacy paths.\n+- Current governance, packaging, workflow, prompt, script, and skill references\n+  must move with the platform records.\n+\n+## Alternatives Considered\n+\n+- Collision-only refusal leaves affected targets permanently unupgradable.\n+- Renumbering target ADRs mutates target-owned history.\n+- Per-target Shiki renumbering makes platform citations non-portable.\ndiff --git a/docs/adr/template.md b/docs/adr/template.md\nindex a92a97daddd0b00ab9dd626cc8d86942da2eafed..17dd79175b5fee2105935557f691b40cf9492942\n--- a/docs/adr/template.md\n+++ b/docs/adr/template.md\n@@ -1,14 +1,14 @@\n-# NNNN: <Short Decision Title>\n+# <SADR-NNNN or NNNN>: <Short Decision Title>\n \n ## Status\n \n-Proposed | Accepted | Superseded by ADR-NNNN\n+Proposed | Accepted | Superseded by SADR-NNNN or target ADR NNNN\n \n ## Context\n \n What is the situation, constraint, or force that requires a decision? Reference\n-relevant domain terms (`CONTEXT.md`), prior ADRs, and code reality. State the\n-problem, not the solution.\n+relevant domain terms (`CONTEXT.md`), prior Shiki SADRs or target ADRs, and code\n+reality. State the problem, not the solution.\n \n ## Decision\n \ndiff --git a/docs/agents/autonomous-loop-e2e-acceptance.md b/docs/agents/autonomous-loop-e2e-acceptance.md\nindex bbf84c6081987f159ac6ad9ecf3c82b7aa72e7e8..5c13c1d02a05a90b0dc46cbef7c95a9846d0c237\n--- a/docs/agents/autonomous-loop-e2e-acceptance.md\n+++ b/docs/agents/autonomous-loop-e2e-acceptance.md\n@@ -40,8 +40,8 @@\n 3. `gh auth status` is logged in with merge permission on the repo.\n 4. Branch protection + required checks are configured exactly as the goal-loop\n    required-check set expects (see `docs/agents/branch-protection-smoke.md`).\n-5. The repo `.shiki/policy` permits autonomous low/medium-risk merges (ADR\n-   0008/0009); the MergeGate policy check is the recorded-authority gate.\n+5. The repo `.shiki/policy` permits autonomous low/medium-risk merges\n+   (SADR-0008/0009); the MergeGate policy check is the recorded-authority gate.\n \n The operator's only actions are: register the task and start the loop. **No\n manual pre-clearing** means after `shiki loop run` starts, the operator does not\ndiff --git a/docs/agents/bootstrap-command.md b/docs/agents/bootstrap-command.md\nindex 05f7f203a52cf412b9f97084a6e131ed0f785afd..f7aa0b734dfebee4a3b139335b66ee3415f48713\n--- a/docs/agents/bootstrap-command.md\n+++ b/docs/agents/bootstrap-command.md\n@@ -189,7 +189,9 @@\n ```\n \n Use this only for tests, fixtures, or explicit local-only template inspection.\n-Use `--force` only when you intentionally want to overwrite existing target files.\n+Use `--force` only when you intentionally want to refresh the target template.\n+Legacy numeric Shiki decision cleanup is stamp-and-digest authorized before any\n+write; target ADR paths are not cleanup candidates.\n \n ## Slash Command\n \ndiff --git a/docs/agents/checklists.md b/docs/agents/checklists.md\nindex 4a7f03ade346f53a1c1d82981f3fd744f9ab4547..0c5ec6c53dc7197b7b7a7d18e667b325babd6c45\n--- a/docs/agents/checklists.md\n+++ b/docs/agents/checklists.md\n@@ -32,18 +32,18 @@\n | ID | Item | Blocking |\n | --- | --- | --- |\n | GD-01 | Plan was challenged against `CONTEXT.md`. | blocking for non-trivial Goals |\n-| GD-02 | Relevant ADRs were read or absence was recorded. | blocking for non-trivial Goals |\n+| GD-02 | Relevant SADRs and target ADRs were read or absence was recorded. | blocking for non-trivial Goals |\n | GD-03 | Ambiguous terms were resolved or converted to HITL blockers. | blocking |\n | GD-04 | Edge scenarios were considered. | advisory |\n | GD-05 | Code reality was checked when code could answer. | blocking when applicable |\n | GD-06 | New domain terms were added to `CONTEXT.md` when needed. | blocking when applicable |\n-| GD-07 | ADR was created or explicitly skipped using the ADR criteria. | blocking when applicable |\n+| GD-07 | A SADR or target ADR was created or explicitly skipped using the decision-record criteria. | blocking when applicable |\n \n ## CI \u2014 Context & Impact Checklist\n \n | ID | Item | Blocking |\n | --- | --- | --- |\n-| CI-01 | Relevant docs and ADRs identified. | blocking |\n+| CI-01 | Relevant docs, SADRs, and target ADRs identified. | blocking |\n | CI-02 | Relevant modules, interfaces, seams, callers, and tests identified. | blocking |\n | CI-03 | Dependencies and blocked-by relationships identified. | blocking |\n | CI-04 | Candidate locks identified. | blocking |\n@@ -62,7 +62,7 @@\n | PRD-04 | Implementation decisions are recorded without volatile code detail. | blocking |\n | PRD-05 | Testing decisions are recorded. | blocking |\n | PRD-06 | Out-of-scope items are explicit. | blocking |\n-| PRD-07 | Relevant glossary/ADR links are present when applicable. | blocking |\n+| PRD-07 | Relevant glossary/SADR/target-ADR links are present when applicable. | blocking |\n \n ## SF \u2014 Spec Freeze Checklist\n \n@@ -98,7 +98,7 @@\n | H-02 | Branch/worktree target is present. | blocking |\n | H-03 | Scope and non-goals are present. | blocking |\n | H-04 | Dependencies and locks are present. | blocking |\n-| H-05 | Relevant docs, ADRs, modules, and tests are referenced. | blocking |\n+| H-05 | Relevant docs, SADRs, target ADRs, modules, and tests are referenced. | blocking |\n | H-06 | Required skills are stated. | blocking |\n | H-07 | TDD expectations are stated. | blocking when behavior work |\n | H-08 | Acceptance criteria are included. | blocking |\ndiff --git a/docs/agents/codex-handoff.md b/docs/agents/codex-handoff.md\nindex ede468f47cb277f69a857e8c45df703e4ed0151d..6f97bdffb67e44d7edea2c71e01ea28933c4eb6c\n--- a/docs/agents/codex-handoff.md\n+++ b/docs/agents/codex-handoff.md\n@@ -46,7 +46,7 @@\n \n ## Context\n - Domain terms:\n-- Relevant ADRs:\n+- Relevant SADRs and target ADRs:\n - Relevant docs:\n - Relevant modules/interfaces/seams:\n - Similar tests/prior art:\ndiff --git a/docs/agents/completion-check-agent.md b/docs/agents/completion-check-agent.md\nindex 13e4c035db35025f4e3cdcc5adfb24314ac4d521..61bfa1218cf36ca605250c91919e620f33238aa0\n--- a/docs/agents/completion-check-agent.md\n+++ b/docs/agents/completion-check-agent.md\n@@ -32,7 +32,7 @@\n - `AGENTS.md`\n - `CLAUDE.md`\n - `CONTEXT.md`\n-- relevant ADRs\n+- relevant SADRs and target ADRs\n - parent Goal issue\n - PRD issue or document\n - task issue\ndiff --git a/docs/agents/decision-control.md b/docs/agents/decision-control.md\nindex 7d8ee5ab03a5ccbcbd04056ae61de03785a2a9c9..8c94ecda00fffaa0e92c325ab83aa9776bd1dac1\n--- a/docs/agents/decision-control.md\n+++ b/docs/agents/decision-control.md\n@@ -11,7 +11,7 @@\n | Decision | Owner | When | Required inputs | Output | Enforcement surface |\n | --- | --- | --- | --- | --- | --- |\n | Is the Goal clear enough to plan? | Planner + Guardian when needed | Goal intake | User request, existing docs, risk signals | Goal readiness verdict | Goal checklist, issue template, ledger |\n-| Must `grill-with-docs` run? | Planner / Skill Gate | Before PRD or issue split | Ambiguity, domain terms, ADR conflicts, unclear boundaries | Skill invocation or explicit skip rationale | Skill Gate checklist |\n+| Must `grill-with-docs` run? | Planner / Skill Gate | Before PRD or issue split | Ambiguity, domain terms, SADR/target-ADR conflicts, unclear boundaries | Skill invocation or explicit skip rationale | Skill Gate checklist |\n | Is a PRD required? | Planner | After Goal context stabilizes | Goal, `grill-with-docs` output, Context & Impact output | PRD required / not required | PRD checklist, CCA evidence check |\n | Can the spec be frozen? | Operator only | End of Requirements Definition, before issue split | PRD/requirements summary, scope inventory (SF-02), open-question list | `spec_freeze` block in the plan + ledger evidence | `require_grilled_plan` (plan ingest/run), SF checklist |\n | Can a frozen spec change? | Operator only (Spec Amendment) | When implementation contradicts the frozen spec | Contested decisions, scoped re-grill result | recorded amendment + re-stamped freeze | SF checklist, ledger evidence |\ndiff --git a/docs/agents/domain.md b/docs/agents/domain.md\nindex 258da8ea81e87ac0a6b0b5b5e5c2fde980eaabb7..d57b0cf17ff3e1fa9e6e5885372bf083d8295421\n--- a/docs/agents/domain.md\n+++ b/docs/agents/domain.md\n@@ -5,7 +5,9 @@\n ## Layout\n \n - `CONTEXT.md` defines Shiki language. Keep it implementation-free.\n-- `docs/adr/` records hard-to-reverse decisions.\n+- `docs/adr/SADR-NNNN-*.md` records Shiki platform decisions.\n+- `docs/adr/NNNN-*.md` records target product decisions without sharing the\n+  SADR namespace.\n - `AGENTS.md` is the runtime-neutral agent constitution.\n - `CLAUDE.md` is the Claude Code wrapper around `AGENTS.md`.\n - `.shiki/` is the Target Repository mirror for Goals, PRDs, plans, Task DAGs, contracts, locks, worktree records, ledger entries, CCA verdicts, repair packets, reports, and handoffs.\n@@ -15,6 +17,6 @@\n \n - Use Shiki terms exactly when they exist.\n - Add glossary terms to `CONTEXT.md` only when the term is part of the domain, not a generic programming concept.\n-- Create ADRs only for hard-to-reverse, surprising, tradeoff-based decisions.\n+- Create SADRs or target ADRs only for hard-to-reverse, surprising, tradeoff-based decisions.\n - Keep product-specific language in the Target Repository, not in Shiki Template docs.\n-- If an ADR conflicts with a proposed implementation, surface the conflict before editing.\n+- If a SADR or target ADR conflicts with a proposed implementation, surface the conflict before editing.\ndiff --git a/docs/agents/execution-control.md b/docs/agents/execution-control.md\nindex ad8ee3cdb36be56fedc1f46408ca92b387d74047..bc63c3a73661d12b567016ba299f1a6b66bf9088\n--- a/docs/agents/execution-control.md\n+++ b/docs/agents/execution-control.md\n@@ -63,7 +63,7 @@\n | Branch name | Orchestrator | Orchestrator | Before implementer dispatch | branch policy | branch ref in task/ledger |\n | Lock reservation | Context & Impact | MergeGate | Before dispatch and before merge | `.shiki/locks/`, labels, PR metadata | lock id, owner, scope |\n | CI profile | Planner | MergeGate | During issue triage, rechecked at PR | labels, `.shiki/policy.yaml`, workflow matrix | required check list |\n-| Required status checks | Repo maintainer / Guardian | GitHub branch protection / rulesets | Repository setup, updated via ADR | GitHub settings | branch/ruleset config, ADR |\n+| Required status checks | Repo maintainer / Guardian | GitHub branch protection / rulesets | Repository setup, updated via target ADR | GitHub settings | branch/ruleset config, target ADR |\n | CCA judgment | CCA | GitHub required check / MergeGate | Every non-draft PR update | GitHub Action check | structured CCA verdict |\n | Repair assignment | CCA | MergeGate | After failed CCA/CI/review | repair packet, issue/PR comment | repair id and attempt count |\n | Merge readiness | MergeGate | GitHub branch protection / Guardian if needed | After CCA complete | required checks, review rules, rulesets | mergegate check output |\ndiff --git a/docs/agents/guardian-policy.md b/docs/agents/guardian-policy.md\nindex 4147c7adf05a47adb4fd24ada1a1c2e1f2ba6d2d..a20d9cc6c00a569bb057b9ca8d6b51c7fc4a9847\n--- a/docs/agents/guardian-policy.md\n+++ b/docs/agents/guardian-policy.md\n@@ -98,8 +98,8 @@\n `origin/<default_branch>` and reuses each job's existing base fetch; a shallow\n (`--depth`) or second base fetch would make the proof unresolvable and silently\n disable the carry, so neither is permitted. CCA reads `guardian_comment_carried` as\n-a recorded authority in the deterministic signal and does not re-derive it. See ADR\n-0018 for the full decision, the rejected diff/patch-equality and path-disjointness\n+a recorded authority in the deterministic signal and does not re-derive it. See\n+SADR-0018 for the full decision, the rejected diff/patch-equality and path-disjointness\n alternatives, and the accepted residual.\n \n ## External AI Guardian Review (SADR-0010)\ndiff --git a/docs/agents/implementation-policy.md b/docs/agents/implementation-policy.md\nindex c754144a9fb5bf4a38aa82a60e1670c59e626b44..85c37281bcad196c6b7b02343faf00b03cd9c57a\n--- a/docs/agents/implementation-policy.md\n+++ b/docs/agents/implementation-policy.md\n@@ -31,10 +31,13 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n ## Non-Negotiables\n \n 1. **Planning precedes implementation.** Non-trivial Goals start with `grill-with-docs` and Context & Impact.\n-2. **Domain language is controlled.** Terms must be checked against `CONTEXT.md`; hard-to-reverse tradeoffs must be checked against ADRs.\n+2. **Domain language is controlled.** Terms must be checked against `CONTEXT.md`; hard-to-reverse tradeoffs must be checked against Shiki SADRs and target ADRs.\n 3. **PRD precedes issue decomposition.** `to-prd` is used once the context is settled enough to become durable intent.\n 4. **Issues are vertical slices.** `to-issues` must produce independently grabbable tracer-bullet tasks, not horizontal layer tickets.\n 5. **Triage controls dispatch.** Only `ready-for-agent` / AFK tasks may be assigned to Codex without human input.\n@@ -95,19 +98,19 @@\n Process:\n \n - Challenge terms against `CONTEXT.md`.\n-- Check ADRs before reopening decisions.\n+- Check SADRs and target ADRs before reopening decisions.\n - Inspect code when code can answer a question.\n - Ask one question at a time when the operator is available.\n - Provide a recommended answer with each question.\n - Record resolved terms in `CONTEXT.md`.\n-- Create ADRs only for hard-to-reverse, surprising, tradeoff-based decisions.\n+- Create SADRs or target ADRs only for hard-to-reverse, surprising, tradeoff-based decisions.\n - Convert unresolved load-bearing questions into HITL blockers.\n \n Outputs:\n \n - Resolved design-tree decisions.\n - Updated glossary terms when needed.\n-- ADRs when justified.\n+- SADRs or target ADRs when justified.\n - HITL blockers when decisions remain unresolved.\n \n ## Phase 3 \u2014 Context & Impact\n@@ -117,7 +120,7 @@\n \n Outputs must include:\n \n-- Relevant documents and ADRs.\n+- Relevant documents, SADRs, and target ADRs.\n - Relevant modules, interfaces, seams, callers, and tests.\n - Candidate locks.\n - Dependencies.\n@@ -140,7 +143,7 @@\n - Testing decisions.\n - Out of scope.\n - Further notes.\n-- Links to relevant Goal, domain terms, and ADRs.\n+- Links to relevant Goal, domain terms, SADRs, and target ADRs.\n \n Phase 4 ends with Spec Freeze: the operator explicitly approves the PRD, the\n plan records a `spec_freeze` block (status, approved_by, source), and the SF\n@@ -240,7 +243,7 @@\n - Dependencies and locks.\n - Risk and Guardian requirements.\n - Ledger completeness.\n-- Documentation and ADR updates.\n+- Documentation and SADR/target-ADR updates.\n - Scope drift.\n \n CCA emits one of:\n@@ -301,7 +304,7 @@\n | Evidence failure | Behavior might be done, but proof is missing. | Add checks, PR evidence, ledger entry, or artifacts. |\n | Scope drift | PR changes unrelated behavior. | Revert unrelated changes or split into new Goal. |\n | Dependency block | Upstream task or lock not complete. | Block until dependency resolves. |\n-| Architecture block | Decision needs ADR or deep-module discussion. | `grill-with-docs` or `improve-codebase-architecture`. |\n+| Architecture block | Decision needs a SADR, target ADR, or deep-module discussion. | `grill-with-docs` or `improve-codebase-architecture`. |\n | Risk block | Security/production/policy change needs human approval. | Guardian decision. |\n \n ## Phase 5.5 \u2014 Execution Decision Control\ndiff --git a/docs/agents/runtime-auth-model.md b/docs/agents/runtime-auth-model.md\nindex 6a2e1a47e81ea89b5c68a210ec07772a9374babc..3c54748d24233dbd0ba8473950282547ef4ce935\n--- a/docs/agents/runtime-auth-model.md\n+++ b/docs/agents/runtime-auth-model.md\n@@ -148,7 +148,7 @@\n \n API-key based runners may be added by a target repository as an explicit extension. They are not the default Shiki template path.\n \n-If a repo opts into API-key mode, record the exception in an ADR and update the repo's `AGENTS.md`, `.shiki/config.yaml`, and workflow permissions.\n+If a repo opts into API-key mode, record the target-specific exception in a target ADR and update the repo's `AGENTS.md`, `.shiki/config.yaml`, and workflow permissions.\n \n ## GitHub Actions Runtime Compatibility\n \ndiff --git a/docs/agents/skill-gate.md b/docs/agents/skill-gate.md\nindex c50398b0d7c60a8598f2a5c88a10015730dced13..3acffe6444617bb1d54b77d4b4708158ec735bc6\n--- a/docs/agents/skill-gate.md\n+++ b/docs/agents/skill-gate.md\n@@ -9,7 +9,7 @@\n | Skill | Trigger | Expected output |\n | --- | --- | --- |\n | `setup-matt-pocock-skills` | First configuring a repo for these skills, or when issue tracker, triage labels, or domain docs are missing. | `docs/agents/` configuration and an `Agent skills` block in `AGENTS.md` or `CLAUDE.md`. |\n-| `grill-with-docs` | Ambiguous plan, unclear terminology, boundary decision, tradeoff, or ADR-worthy design choice. | Clarified decision tree, recommended answers, updates to `CONTEXT.md` or ADRs when decisions crystallize. |\n+| `grill-with-docs` | Ambiguous plan, unclear terminology, boundary decision, tradeoff, or decision-record-worthy design choice. | Clarified decision tree, recommended answers, updates to `CONTEXT.md`, SADRs, or target ADRs when decisions crystallize. |\n | `zoom-out` | Agent lacks a higher-level architectural map. | Map of relevant modules, callers, seams, and domain concepts. |\n | `to-prd` | Settled context should become a PRD. | PRD published to the issue tracker with problem, solution, user stories, decisions, testing, and out-of-scope notes. |\n | `to-issues` | Goal, PRD, or plan must become implementation tickets. | Vertical-slice issues with acceptance criteria, dependencies, and AFK/HITL classification. |\n@@ -17,7 +17,7 @@\n | `tdd` | Feature work or bug fix has observable behavior that can be specified with tests. | Red-green-refactor loop through public interfaces, one vertical slice at a time. |\n | `code-review` | The TDD loop is green and the implementer is about to create or update a PR. Default for every implementation task. | Pre-PR self-review of the task diff with fixes applied on the task branch; findings and resolutions recorded as a ledger entry naming the skill and a `## Pre-PR code review` PR body section. |\n | `diagnose` | Hard bug, failing check, regression, flaky behavior, or performance problem. | Reproduction loop, ranked hypotheses, instrumentation, fix, regression test, post-mortem. |\n-| `improve-codebase-architecture` | Structural friction, testability, AI-navigability, deep-module opportunity, or refactor request. | Architecture candidates and deepening recommendations informed by `CONTEXT.md` and ADRs. |\n+| `improve-codebase-architecture` | Structural friction, testability, AI-navigability, deep-module opportunity, or refactor request. | Architecture candidates and deepening recommendations informed by `CONTEXT.md`, SADRs, and target ADRs. |\n | `prototype` | A throwaway logic or UI prototype is needed to answer a design question. | Clearly marked disposable prototype and captured decision. |\n \n ## MergeGate Interaction\ndiff --git a/docs/operations.md b/docs/operations.md\nindex cf1f452d4c589e38ccd58553bda0ad8684b51eb7..ace48a90ffbec9e5c0a7113bef16a4858bc44b3f\n--- a/docs/operations.md\n+++ b/docs/operations.md\n@@ -89,6 +89,16 @@\n      target history, brought forward by `shiki migrate`, not a template.\n    - **Everything else** (CLI scripts, workflows, schemas, docs) is overwritten.\n \n+   A forced upgrade also migrates the former numeric Shiki decision paths to\n+   `SADR-NNNN-*`. Before the first write, the installer inventories the exact\n+   legacy Shiki paths and authorizes each deletion only when the existing\n+   `.shiki/install-stamp.json` names that path and its SHA-256 digest still\n+   matches the file. An absent, unreadable, malformed, incomplete, or\n+   mismatching stamp reports every blocked path and stops with no filesystem\n+   change. Target-owned `NNNN-*` ADRs are not cleanup candidates. A non-force\n+   install deletes nothing and retains any still-matching legacy stamp entries\n+   so a later forced upgrade can prove ownership.\n+\n    If the target has pending migrations, `--force` refuses and writes nothing \u2014\n    apply migrations first (step 3), then re-run. The run ends with a single\n    summary of every `.new` written, so a half-upgrade is visible.\ndiff --git a/docs/releasing.md b/docs/releasing.md\nindex 10d73828e7dbd64e9d9f6b83bc84d010537ce62a..aafeee8c4d441e1e427efee2ce4f81a647f020e1\n--- a/docs/releasing.md\n+++ b/docs/releasing.md\n@@ -67,7 +67,7 @@\n CLI in `scripts/`. There are no third-party Python runtime dependencies. The CLI\n is intentionally **not** exposed as a `[project.scripts]` console entry point\n because `scripts/shiki.py` is a dependency-free shim and `scripts/` is not\n-packaged as an importable distribution (see the ADR for the rationale).\n+packaged as an importable distribution (see SADR-0007 for the rationale).\n \n Supported install paths:\n \n@@ -155,6 +155,15 @@\n    [docs/operations.md](operations.md#the-new-convention). If the target has\n    pending migrations, `--force` refuses and writes nothing; apply migrations\n    (step 2) first, then re-run.\n+\n+   The SADR namespace upgrade has an additional pre-write gate: each former\n+   numeric Shiki decision path is removed only when the existing install stamp\n+   contains that exact path and a digest matching its current bytes. The\n+   installer aggregates absent/unreadable/malformed-stamp, missing-digest, and\n+   digest-mismatch blockers before raising, so refusal leaves the target tree\n+   byte-identical. Numeric target ADRs are preserved. A successful forced\n+   upgrade installs and stamps `SADR-*` paths and drops the removed legacy paths\n+   from the refreshed stamp.\n 2. Apply repository-local `.shiki/` state migrations with the migrate command:\n \n    ```bash\ndiff --git a/docs/usage.md b/docs/usage.md\nindex b83407ee3970dd395dd893010bc5dbe8bad26051..a130ef6e40dfd8169a5bc701339351097df1a09f\n--- a/docs/usage.md\n+++ b/docs/usage.md\n@@ -67,8 +67,10 @@\n shiki install-target /path/to/target-repo --local-only\n ```\n \n-Use `--force` only when you intentionally want to overwrite existing target\n-files.\n+Use `--force` only when you intentionally want to refresh the target template.\n+For the SADR namespace migration, `--force` deletes only exact legacy Shiki\n+decision paths whose existing install-stamp digest still matches; any ownership\n+blocker stops before the first write. Numeric target ADRs are never candidates.\n \n ## `shiki bootstrap-platform` \u2014 publish the Shiki platform repo\n \ndiff --git a/scripts/guardian_approval_signal.py b/scripts/guardian_approval_signal.py\nindex d059f92b2c82aa2b5d6ca421365537a87886c1cb..16924892714f4f5b9303c0ef0e325a7e70075630\n--- a/scripts/guardian_approval_signal.py\n+++ b/scripts/guardian_approval_signal.py\n@@ -199,8 +199,8 @@\n ) -> Any:\n     \"\"\"Evaluate SADR-0015 Contract Approval for the CCA Guardian signal.\n \n-    The SAME OR that MergeGate applies must reach this deterministic signal: ADR\n-    0015 is explicit that without it, a contract-approved PR still returns\n+    The SAME OR that MergeGate applies must reach this deterministic signal:\n+    SADR-0015 is explicit that without it, a contract-approved PR still returns\n     ``needs_guardian``, the CCA job fails, and the MergeGate policy check never\n     runs \u2014 the gate this change moves would never be reached. This resolves the\n     task the SAME way MergeGate does (the first Shiki task id in the PR body),\ndiff --git a/scripts/shiki_contract_approval.py b/scripts/shiki_contract_approval.py\nindex 941bcb96fdd8b9a85bc0c4f0f04d5dad9d04839c..4a4b418f6527413161db82670bec857a043360e2\n--- a/scripts/shiki_contract_approval.py\n+++ b/scripts/shiki_contract_approval.py\n@@ -142,7 +142,7 @@\n     changed_files_status: Any,\n     registration: Any,\n ) -> ContractApprovalResult:\n-    \"\"\"Whether this PR carries an SADR-0015 Contract Approval.\n+    \"\"\"Whether this PR carries a SADR-0015 Contract Approval.\n \n     The four conditions are evaluated IN ORDER, each failure recorded as a\n     distinct reason string:\ndiff --git a/scripts/shiki_guardian.py b/scripts/shiki_guardian.py\nindex a4ecb79bbda55fd5c8b06adfae2eb99760a03c45..4dd99bbe714e26f421fa19abac7a34ae1b195ecc\n--- a/scripts/shiki_guardian.py\n+++ b/scripts/shiki_guardian.py\n@@ -12,7 +12,7 @@\n # The carry re-derives its proof from git in-process on every judging invocation;\n # it is never read from an artifact, so there is nothing to go stale or be forged.\n # shiki_sync_proof is the ONLY guardian-family module that runs git, and every one\n-# of its invocations is read-only and local (ADR: prove-from-git base sync).\n+# of its invocations is read-only and local (SADR: prove-from-git base sync).\n from shiki_sync_proof import verify_pure_base_sync\n \n GUARDIAN_POLICY_PATH = \".shiki/guardian-policy.json\"\ndiff --git a/scripts/shiki_guardian_review.py b/scripts/shiki_guardian_review.py\nindex dd0f104d457899e0ba5ff1e162011c1e90b6f92b..e10ef58c046324e7ea3a3392feb3048ca77414ce\n--- a/scripts/shiki_guardian_review.py\n+++ b/scripts/shiki_guardian_review.py\n@@ -159,7 +159,7 @@\n     docs_only = _is_docs_only(changed_files)\n     if docs_only and matched == 0:\n         focus.append(\n-            \"Docs/ADR-only change: confirm the change is genuinely documentation/decision-only, introduces no behavior or contract drift, and stays consistent with the constitution and existing ADRs.\"\n+            \"Docs/decision-record-only change: confirm the change is genuinely documentation/decision-only, introduces no behavior or contract drift, and stays consistent with the constitution and existing SADRs and target ADRs.\"\n         )\n \n     # Conservative augmentation: unknown (no category and not docs-only), mixed\ndiff --git a/scripts/shiki_installer.py b/scripts/shiki_installer.py\nindex be064ab7a29ff6836f7f7a1d70500a43e0d11d7b..9fd8eefd137ce7003dad5c0dcb2e48e2cd47ab32\n--- a/scripts/shiki_installer.py\n+++ b/scripts/shiki_installer.py\n@@ -118,6 +118,7 @@\n # what lets a target carry it without a governance-schema change.\n INSTALL_STAMP_PATH = \".shiki/install-stamp.json\"\n INSTALL_STAMP_VERSION = 1\n+LEGACY_SHIKI_SADR_MAX = 18\n \n # Under --force the shipped surface splits three ways, so nothing is lost and\n # nothing is silently stale.\n@@ -237,9 +238,98 @@\n         )\n \n \n+def legacy_shiki_adr_paths() -> tuple[str, ...]:\n+    \"\"\"Former numeric paths for Shiki SADR-0001 through SADR-0018.\n+\n+    The authoritative slugs come from the current SADR filenames. SADR-0019 is\n+    the namespace decision itself and never shipped under a numeric legacy path.\n+    \"\"\"\n+    paths: list[str] = []\n+    for sadr in sorted((ROOT / \"docs\" / \"adr\").glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\")):\n+        number = sadr.name.split(\"-\", 2)[1]\n+        if int(number) <= LEGACY_SHIKI_SADR_MAX:\n+            paths.append(f\"docs/adr/{sadr.name.removeprefix('SADR-')}\")\n+    return tuple(paths)\n+\n+\n+def preflight_legacy_sadr_cleanup(target: Path, *, force: bool) -> tuple[str, ...]:\n+    \"\"\"Authorize exact legacy Shiki ADR removals before the first write.\n+\n+    A non-force install never removes existing files. A forced install needs no\n+    stamp when no legacy Shiki path is present. When legacy paths do exist, each\n+    must be named in a readable install stamp and its current bytes must match\n+    the recorded digest.\n+    \"\"\"\n+    if not force:\n+        return ()\n+    present = tuple(\n+        relative\n+        for relative in legacy_shiki_adr_paths()\n+        if (target / relative).exists() or (target / relative).is_symlink()\n+    )\n+    if not present:\n+        return ()\n+\n+    stamp_path = target / INSTALL_STAMP_PATH\n+    try:\n+        raw = stamp_path.read_text(encoding=\"utf-8\")\n+        stamp = json.loads(raw)\n+    except FileNotFoundError:\n+        detail = \"install stamp is absent\"\n+        blockers = [f\"{relative}: {detail}\" for relative in present]\n+    except (OSError, UnicodeDecodeError) as error:\n+        detail = f\"install stamp is unreadable ({error})\"\n+        blockers = [f\"{relative}: {detail}\" for relative in present]\n+    except json.JSONDecodeError as error:\n+        detail = f\"install stamp is malformed ({error})\"\n+        blockers = [f\"{relative}: {detail}\" for relative in present]\n+    else:\n+        if not isinstance(stamp, dict):\n+            blockers = [\n+                f\"{relative}: install stamp is malformed (expected an object)\"\n+                for relative in present\n+            ]\n+        else:\n+            digests = stamp.get(\"digests\")\n+            blockers = []\n+            if not isinstance(digests, dict):\n+                blockers.extend(\n+                    f\"{relative}: install stamp has no digest for this path\"\n+                    for relative in present\n+                )\n+            else:\n+                for relative in present:\n+                    expected = digests.get(relative)\n+                    if not isinstance(expected, str) or not expected:\n+                        blockers.append(\n+                            f\"{relative}: install stamp has no digest for this path\"\n+                        )\n+                        continue\n+                    try:\n+                        actual = _sha256_file(target / relative)\n+                    except OSError as error:\n+                        blockers.append(\n+                            f\"{relative}: legacy path is unreadable ({error})\"\n+                        )\n+                        continue\n+                    if actual != expected:\n+                        blockers.append(\n+                            f\"{relative}: digest mismatch (stamp {expected}, current {actual})\"\n+                        )\n+\n+    if blockers:\n+        joined = \"\\n\".join(f\"- {blocker}\" for blocker in blockers)\n+        raise ShikiError(\n+            \"legacy Shiki ADR cleanup is not authorized:\\n\"\n+            f\"{joined}\\nNo files were written.\"\n+        )\n+    return present\n+\n+\n def install_template(target: Path, *, force: bool, validate: bool) -> list[NewFileNote]:\n     # Preconditions first: never rewrite the tree of a target that cannot be\n     # upgraded cleanly (see refuse_pending_migrations).\n+    legacy_cleanup = preflight_legacy_sadr_cleanup(target, force=force)\n     refuse_pending_migrations(target)\n \n     # Resolve the target's required CODEOWNERS owner once from its\n@@ -250,6 +340,10 @@\n     # verbatim. See ``codeowners_required_owner``.\n     codeowners_owner = codeowners_required_owner(target)\n \n+    for relative in legacy_cleanup:\n+        (target / relative).unlink()\n+        info(f\"removed stamped legacy Shiki ADR: {target / relative}\")\n+\n     notes: list[NewFileNote] = []\n     for relative in TEMPLATE_PATHS:\n         source = ROOT / relative\n@@ -647,6 +741,22 @@\n     digests = target_stamp_digests(target)\n     existing = read_install_stamp(target)\n     if existing is not None:\n+        # A non-force install deliberately leaves legacy Shiki ADRs in place.\n+        # Preserve only still-matching ownership proof so a later forced\n+        # upgrade can authorize cleanup; never carry a missing or stale digest.\n+        existing_digests = existing.get(\"digests\")\n+        if isinstance(existing_digests, dict):\n+            for relative in legacy_shiki_adr_paths():\n+                expected = existing_digests.get(relative)\n+                path = target / relative\n+                if not isinstance(expected, str) or not path.is_file():\n+                    continue\n+                try:\n+                    actual = _sha256_file(path)\n+                except OSError:\n+                    continue\n+                if actual == expected:\n+                    digests[relative] = actual\n         if _incoming_is_older(commit, existing.get(\"platform_commit\")):\n             warn(\n                 \"kept existing install stamp: incoming install \"\ndiff --git a/scripts/shiki_loop.py b/scripts/shiki_loop.py\nindex 3611641da8aeb5038b1da90e3114b09a473ec6d8..84caa491b9cad0edc4176f209c13e9221e975681\n--- a/scripts/shiki_loop.py\n+++ b/scripts/shiki_loop.py\n@@ -424,7 +424,7 @@\n         # unresolvable CCA verdict has already produced its terminal stop and is never\n         # laundered into a head-moving sync (SADR-0010). It also never fires while the\n         # policy (Guardian) gate is red \u2014 for ANY reason, not only when it is the sole\n-        # red check \u2014 so a head-bound Guardian approval, or an SADR-0015\n+        # red check \u2014 so a head-bound Guardian approval, or a SADR-0015\n         # contract-approval carry that base movement defeated (the policy gate compares\n         # base and head task governance directly), is never silently restored by\n         # adopting base wholesale. It is bounded by a durable per-task counter so the\ndiff --git a/scripts/shiki_tasks.py b/scripts/shiki_tasks.py\nindex aec3809f24b4a0c8c96fc50971cff9ae9ad30381..d531052e9d9717058d4631483cefab44c26a65c1\n--- a/scripts/shiki_tasks.py\n+++ b/scripts/shiki_tasks.py\n@@ -821,7 +821,7 @@\n         \"prompt\": prompt,\n         \"entry_skill\": \"grill-with-docs\",\n         \"required_next_steps\": [\n-            \"Run grill-with-docs until terminology, boundaries, risks, and ADR-worthy decisions are settled.\",\n+            \"Run grill-with-docs until terminology, boundaries, risks, and decision-record-worthy choices are settled.\",\n             \"Obtain the operator's explicit approval of the PRD/requirements (Spec Freeze).\",\n             \"Write a machine-readable plan JSON with grill_with_docs.status=complete and spec_freeze.status=frozen.\",\n             \"Run shiki plan ingest --plan-file PLAN.json.\",\ndiff --git a/scripts/test_shiki_goal_loop.sh b/scripts/test_shiki_goal_loop.sh\nindex 753c7b516e10f0be3bc92a4bd0eb1fa699776ae6..8f3820a0a079b3044424cfe1a6c6b7fe68896e67\n--- a/scripts/test_shiki_goal_loop.sh\n+++ b/scripts/test_shiki_goal_loop.sh\n@@ -231,8 +231,8 @@\n \n # High/critical risk merges autonomously when all required checks are green:\n # the \"MergeGate policy check\" required check IS the Guardian gate (it enforces\n-# guardian-policy.json \u2014 human approval OR external AI guardian review, ADR\n-# 0010), so green checks mean a recorded authority approved. The fake gh returns\n+# guardian-policy.json \u2014 human approval OR external AI guardian review,\n+# SADR-0010), so green checks mean a recorded authority approved. The fake gh returns\n # green checks, simulating that the Guardian gate passed.\n python3 \"$ROOT/scripts/shiki.py\" goal create --target \"$TARGET\" --title \"Guardian gate\" --outcome \"High risk merges via policy gate\" >/tmp/shiki-goal-loop-guardian-goal.json\n GGOAL=\"$(json_get /tmp/shiki-goal-loop-guardian-goal.json goal_id)\"\ndiff --git a/tests/test_adr_references.py b/tests/test_adr_references.py\nindex 51706329c797d5dd9b47e082a333fa83b261d482..db6e693e35f67103935be34f1d9bb4ff7ee60395\n--- a/tests/test_adr_references.py\n+++ b/tests/test_adr_references.py\n@@ -1,7 +1,7 @@\n-\"\"\"Every ``ADR ####`` citation must resolve to exactly one decision record.\n+\"\"\"Every target ADR and Shiki SADR citation resolves in its own namespace.\n \n Background. ``scripts/mergegate_check.py`` justifies ``is_bookkeeping_closeout``\n-with \"ADR 0017\" nine times and ``scripts/guardian_approval_signal.py`` cites\n+with \"SADR-0017\" nine times and ``scripts/guardian_approval_signal.py`` cites\n 0016/0017 seven times, yet for a stretch the numbering in ``docs/adr/`` stopped\n at 0015: the authoring commits lived on a coordinator branch and no task ever\n declared ``docs/adr/...`` in its locks, so nothing carried the records to the\n@@ -11,11 +11,12 @@\n and the directory drifted with no signal.\n \n This is the same unbound-pair shape behind other silent failures: a reference\n-and its target with nothing asserting they agree. This test binds them. It scans\n-every ``scripts/*.py`` (plus ``docs/**/*.md`` and the root ``*.md`` governance\n-files, which are cheap) for the ``ADR ####`` pattern and asserts each cited\n-number resolves to exactly one ``docs/adr/####-*.md``. The converse is\n-deliberately NOT required: an ADR may exist without being cited.\n+and its target with nothing asserting they agree. This test binds them. Target\n+``ADR NNNN`` citations resolve only to ``docs/adr/NNNN-*.md``; Shiki\n+``SADR-NNNN`` citations resolve only to ``docs/adr/SADR-NNNN-*.md``. The scan\n+covers current governance docs, scripts, workflows, prompts, packaging metadata,\n+and repo-local Codex guidance while deliberately excluding historical\n+``.shiki`` evidence. The converse is not required: a record may be uncited.\n \n The suite is dependency free: standard-library ``unittest`` only.\n \"\"\"\n@@ -31,17 +32,18 @@\n \n REPO_ROOT = Path(__file__).resolve().parents[1]\n \n-# \"ADR 0016\" -- a four-digit citation. The trailing \\b keeps a five-digit token\n-# from being read as a truncated four-digit number.\n-_CITATION = re.compile(r\"\\bADR (\\d{4})\\b\")\n-# \"0016-locks-bound-product-paths.md\" -- the leading four digits name the record.\n-_ADR_FILENAME = re.compile(r\"^(\\d{4})-.*\\.md$\")\n+# ``ADR 0016`` / ``SADR-0016`` -- a namespace plus four-digit citation. The\n+# trailing ``\\b`` keeps a five-digit token from being read as a truncated one.\n+_CITATION = re.compile(r\"\\b(S?ADR)[ -](\\d{4})\\b\")\n+# Target ADRs retain the numeric filename; Shiki platform decisions use SADR.\n+_DECISION_FILENAME = re.compile(r\"^(?:(SADR)-)?(\\d{4})-.*\\.md$\")\n \n \n class Violation(NamedTuple):\n     \"\"\"A citation that does not resolve to exactly one decision record.\"\"\"\n \n     citing_file: str  # repo-relative path of the file that cites the ADR\n+    namespace: str  # \"ADR\" for target decisions; \"SADR\" for Shiki decisions\n     number: str  # the cited four-digit number, e.g. \"0016\"\n     line: int  # 1-based line of the citation\n     reason: str  # \"missing\" (zero matches) or \"ambiguous\" (two or more)\n@@ -49,44 +51,59 @@\n \n     @property\n     def expected_path(self) -> str:\n-        return f\"docs/adr/{self.number}-*.md\"\n+        prefix = \"SADR-\" if self.namespace == \"SADR\" else \"\"\n+        return f\"docs/adr/{prefix}{self.number}-*.md\"\n \n     def describe(self) -> str:\n         \"\"\"Name the citing file, the cited number, and the missing path.\"\"\"\n         if self.reason == \"missing\":\n             return (\n-                f\"{self.citing_file}:{self.line} cites ADR {self.number} \"\n+                f\"{self.citing_file}:{self.line} cites {self.namespace} {self.number} \"\n                 f\"but no document matches {self.expected_path}\"\n             )\n         joined = \", \".join(self.matches)\n         return (\n-            f\"{self.citing_file}:{self.line} cites ADR {self.number} which \"\n+            f\"{self.citing_file}:{self.line} cites {self.namespace} {self.number} which \"\n             f\"resolves to {len(self.matches)} documents (expected exactly one \"\n             f\"at {self.expected_path}): {joined}\"\n         )\n \n \n-def _adr_index(root: Path) -> dict[str, list[Path]]:\n-    \"\"\"Map every four-digit ADR number to the record files that claim it.\"\"\"\n-    index: dict[str, list[Path]] = {}\n+def _adr_index(root: Path) -> dict[tuple[str, str], list[Path]]:\n+    \"\"\"Map each decision namespace and number to the files that claim it.\"\"\"\n+    index: dict[tuple[str, str], list[Path]] = {}\n     adr_dir = root / \"docs\" / \"adr\"\n     if adr_dir.is_dir():\n         for path in sorted(adr_dir.glob(\"*.md\")):\n-            match = _ADR_FILENAME.match(path.name)\n+            match = _DECISION_FILENAME.match(path.name)\n             if match:\n-                index.setdefault(match.group(1), []).append(path)\n+                namespace = \"SADR\" if match.group(1) else \"ADR\"\n+                index.setdefault((namespace, match.group(2)), []).append(path)\n     return index\n \n \n def _citing_files(root: Path, include_docs: bool = True) -> list[Path]:\n-    \"\"\"Files scanned for citations: scripts/*.py, and cheaply the markdown.\"\"\"\n+    \"\"\"Current, shipped text surfaces scanned for decision citations.\"\"\"\n     files = sorted((root / \"scripts\").glob(\"*.py\"))\n+    files += sorted((root / \"scripts\").glob(\"*.sh\"))\n     if include_docs:\n         docs = root / \"docs\"\n         if docs.is_dir():\n             files += sorted(docs.rglob(\"*.md\"))\n         files += sorted(root.glob(\"*.md\"))\n-    return files\n+        pyproject = root / \"pyproject.toml\"\n+        if pyproject.is_file():\n+            files.append(pyproject)\n+        for relative in (\".github\", \".codex\"):\n+            surface = root / relative\n+            if surface.is_dir():\n+                files += sorted(\n+                    path\n+                    for path in surface.rglob(\"*\")\n+                    if path.is_file()\n+                    and path.suffix in {\".md\", \".py\", \".sh\", \".toml\", \".yaml\", \".yml\"}\n+                )\n+    return sorted(set(files))\n \n \n def find_adr_reference_violations(\n@@ -94,10 +111,9 @@\n ) -> list[Violation]:\n     \"\"\"Return every ``ADR ####`` citation under ``root`` that fails to resolve.\n \n-    A citation resolves when exactly one ``docs/adr/####-*.md`` matches its\n-    number. Zero matches is a \"missing\" violation; two or more is \"ambiguous\".\n-    Records that are never cited produce nothing -- the converse is not\n-    required.\n+    A citation resolves when exactly one record in the same namespace matches\n+    its number. Zero matches is a \"missing\" violation; two or more is\n+    \"ambiguous\". Records that are never cited produce nothing.\n     \"\"\"\n     root = Path(root)\n     index = _adr_index(root)\n@@ -110,13 +126,15 @@\n         rel = path.relative_to(root).as_posix()\n         for lineno, line in enumerate(text.splitlines(), start=1):\n             for match in _CITATION.finditer(line):\n-                number = match.group(1)\n-                found = index.get(number, [])\n+                namespace, number = match.groups()\n+                found = index.get((namespace, number), [])\n                 if len(found) == 1:\n                     continue\n                 reason = \"missing\" if not found else \"ambiguous\"\n                 rels = [p.relative_to(root).as_posix() for p in found]\n-                violations.append(Violation(rel, number, lineno, reason, rels))\n+                violations.append(\n+                    Violation(rel, namespace, number, lineno, reason, rels)\n+                )\n     return violations\n \n \n@@ -129,8 +147,8 @@\n         self.assertEqual(\n             violations,\n             [],\n-            \"Every 'ADR ####' citation must resolve to exactly one \"\n-            f\"docs/adr/####-*.md:\\n{detail}\",\n+            \"Every ADR/SADR citation must resolve exactly once in its own \"\n+            f\"docs/adr namespace:\\n{detail}\",\n         )\n \n     def test_scan_actually_reads_scripts(self) -> None:\n@@ -146,8 +164,21 @@\n         # The two records this Goal carried onto the default branch, and which\n         # the scripts cite most, must be present.\n         index = _adr_index(REPO_ROOT)\n-        self.assertIn(\"0016\", index)\n-        self.assertIn(\"0017\", index)\n+        self.assertIn((\"SADR\", \"0016\"), index)\n+        self.assertIn((\"SADR\", \"0017\"), index)\n+\n+    def test_platform_tree_has_only_sadr_0001_through_0019(self) -> None:\n+        adr_dir = REPO_ROOT / \"docs\" / \"adr\"\n+        sadrs = sorted(adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\"))\n+        self.assertEqual(\n+            {path.name.split(\"-\", 2)[1] for path in sadrs},\n+            {f\"{number:04d}\" for number in range(1, 20)},\n+        )\n+        self.assertEqual(\n+            list(adr_dir.glob(\"[0-9][0-9][0-9][0-9]-*.md\")),\n+            [],\n+            \"the Shiki platform tree must not retain legacy numeric SADR paths\",\n+        )\n \n \n class FixtureAdrReferences(unittest.TestCase):\n@@ -180,6 +211,26 @@\n         self.assertIn(\"9991\", message)  # cited number\n         self.assertIn(\"docs/adr/9991-*.md\", message)  # missing path\n \n+    def test_target_adr_does_not_satisfy_missing_platform_sadr(self) -> None:\n+        root = self._make_repo(\n+            adr_names=[\"0042-target-decision.md\"],\n+            script_body='\"\"\"Target ADR 0042 exists, but platform SADR-0042 does not.\"\"\"\\n',\n+        )\n+        violations = find_adr_reference_violations(root, include_docs=False)\n+        self.assertEqual(len(violations), 1)\n+        self.assertEqual(violations[0].namespace, \"SADR\")\n+        self.assertEqual(violations[0].number, \"0042\")\n+        self.assertEqual(violations[0].expected_path, \"docs/adr/SADR-0042-*.md\")\n+\n+    def test_target_adr_and_platform_sadr_may_reuse_a_number(self) -> None:\n+        root = self._make_repo(\n+            adr_names=[\"0042-target-decision.md\", \"SADR-0042-platform-decision.md\"],\n+            script_body='\"\"\"Target ADR 0042 and platform SADR-0042 are distinct.\"\"\"\\n',\n+        )\n+        self.assertEqual(\n+            find_adr_reference_violations(root, include_docs=False), []\n+        )\n+\n     def test_uncited_adr_is_allowed(self) -> None:\n         # An ADR exists but nothing cites it -> not a violation. The converse\n         # (every ADR must be cited) is explicitly NOT required.\ndiff --git a/tests/test_guardian_review.py b/tests/test_guardian_review.py\nindex 88829e7810212635e2fc98aa5918d6f26294677d..00a85a237acd5bf2d6cb5b294128198c422402ca\n--- a/tests/test_guardian_review.py\n+++ b/tests/test_guardian_review.py\n@@ -162,7 +162,7 @@\n \n     def test_docs_only_is_light_and_not_conservative(self):\n         focus = gr.classify_review_focus([\"docs/adr/0099-x.md\", \"README.md\"], \"\", \"low\")\n-        self.assertTrue(any(\"Docs/ADR-only\" in f for f in focus))\n+        self.assertTrue(any(\"Docs/decision-record-only\" in f for f in focus))\n         self.assertFalse(any(\"conservative default\" in f.lower() for f in focus))\n \n     def test_unknown_category_is_conservative(self):\ndiff --git a/tests/test_policy_job_merged_prs.py b/tests/test_policy_job_merged_prs.py\nindex d45512e74681e5e649279236c7d4e79d5c3da471..b97989f8bd74d091de1a6b5275a20d8dc68c4512\n--- a/tests/test_policy_job_merged_prs.py\n+++ b/tests/test_policy_job_merged_prs.py\n@@ -3,8 +3,8 @@\n \n ``.github/workflows/shiki-cca-completion.yml`` has two consumers of the merged-PR\n proof: the CCA *signal* (``guardian_approval_signal.py`` in \"Prepare CCA context\")\n-and the authoritative gate (``mergegate_check.py`` in \"Enforce MergeGate\"). ADR\n-0017's bookkeeping-closeout exemption (condition 6) needs the merged implementation\n+and the authoritative gate (``mergegate_check.py`` in \"Enforce MergeGate\").\n+SADR-0017's bookkeeping-closeout exemption (condition 6) needs the merged implementation\n PR proven, and a closeout carries NO ``post_merge_reconcile`` marker. The signal\n already resolves ``MERGED_PRS`` unconditionally, but \"Enforce MergeGate\" used to\n resolve it only inside ``grep -q '<!-- shiki:post_merge_reconcile -->'``. So a\ndiff --git a/tests/test_sadr_install_migration.py b/tests/test_sadr_install_migration.py\nnew file mode 100644\nindex 0000000000000000000000000000000000000000..a6f4c57e4dcaef73ef600b0d4804744138ab9071\n--- /dev/null\n+++ b/tests/test_sadr_install_migration.py\n@@ -0,0 +1,229 @@\n+\"\"\"SADR installation and collision-safe legacy upgrade behavior.\"\"\"\n+\n+from __future__ import annotations\n+\n+import contextlib\n+import hashlib\n+import io\n+import json\n+import shutil\n+import tempfile\n+import unittest\n+from pathlib import Path\n+\n+import shiki_test_support  # noqa: F401  (path bootstrap)\n+\n+import shiki_installer\n+from tests.test_adr_references import find_adr_reference_violations\n+\n+\n+PRODUCT_NUMBERS = tuple(range(13, 19))\n+\n+\n+def _quiet_install(target: Path, *, force: bool, validate: bool) -> None:\n+    with contextlib.redirect_stdout(io.StringIO()):\n+        shiki_installer.install_template(target, force=force, validate=validate)\n+\n+\n+def _sha256(path: Path) -> str:\n+    return hashlib.sha256(path.read_bytes()).hexdigest()\n+\n+\n+def _legacy_sadr_paths() -> tuple[str, ...]:\n+    return tuple(\n+        f\"docs/adr/{path.name.removeprefix('SADR-')}\"\n+        for path in sorted((shiki_installer.ROOT / \"docs\" / \"adr\").glob(\"SADR-*.md\"))\n+        if path.name != \"SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\"\n+    )\n+\n+\n+def _create_legacy_upgrade_fixture(\n+    target: Path,\n+) -> tuple[dict[str, bytes], tuple[str, ...]]:\n+    adr_dir = target / \"docs\" / \"adr\"\n+    adr_dir.mkdir(parents=True)\n+    product_bytes = {\n+        f\"{number:04d}-cubeshare-product-decision.md\": (\n+            f\"# CubeShare ADR {number:04d}\\n\\nDo not rewrite {number}.\\n\"\n+        ).encode()\n+        for number in PRODUCT_NUMBERS\n+    }\n+    for name, content in product_bytes.items():\n+        (adr_dir / name).write_bytes(content)\n+\n+    legacy_paths = _legacy_sadr_paths()\n+    for relative in legacy_paths:\n+        (target / relative).write_bytes(f\"legacy Shiki bytes for {relative}\\n\".encode())\n+\n+    stamp_path = target / shiki_installer.INSTALL_STAMP_PATH\n+    stamp_path.parent.mkdir(parents=True, exist_ok=True)\n+    stamp = {\n+        \"version\": shiki_installer.INSTALL_STAMP_VERSION,\n+        \"platform_commit\": None,\n+        \"installed_at\": \"2000-01-01T00:00:00+00:00\",\n+        \"digests\": {\n+            relative: _sha256(target / relative) for relative in legacy_paths\n+        },\n+    }\n+    stamp_path.write_text(\n+        json.dumps(stamp, indent=2, sort_keys=True) + \"\\n\", encoding=\"utf-8\"\n+    )\n+    return product_bytes, legacy_paths\n+\n+\n+def _tree_snapshot(root: Path) -> dict[str, tuple[str, bytes | str]]:\n+    \"\"\"Recursive byte/shape snapshot used to prove preflight is write-free.\"\"\"\n+    snapshot: dict[str, tuple[str, bytes | str]] = {}\n+    for path in sorted(root.rglob(\"*\")):\n+        relative = path.relative_to(root).as_posix()\n+        if path.is_symlink():\n+            snapshot[relative] = (\"symlink\", path.readlink().as_posix())\n+        elif path.is_dir():\n+            snapshot[relative] = (\"directory\", \"\")\n+        else:\n+            snapshot[relative] = (\"file\", path.read_bytes())\n+    return snapshot\n+\n+\n+class FreshSadrInstallTests(unittest.TestCase):\n+    def setUp(self) -> None:\n+        self.tmp = Path(tempfile.mkdtemp(prefix=\"shiki-sadr-fresh-\"))\n+        self.addCleanup(lambda: shutil.rmtree(self.tmp, ignore_errors=True))\n+        self.target = self.tmp / \"target\"\n+        self.adr_dir = self.target / \"docs\" / \"adr\"\n+        self.adr_dir.mkdir(parents=True)\n+        self.product_bytes = {\n+            f\"{number:04d}-product-decision.md\": (\n+                f\"# Product ADR {number:04d}\\n\\nTarget-owned bytes {number}.\\n\"\n+            ).encode()\n+            for number in PRODUCT_NUMBERS\n+        }\n+        for name, content in self.product_bytes.items():\n+            (self.adr_dir / name).write_bytes(content)\n+\n+    def test_fresh_install_preserves_product_adrs_and_installs_all_sadrs(self) -> None:\n+        _quiet_install(self.target, force=False, validate=True)\n+\n+        for name, expected in self.product_bytes.items():\n+            self.assertEqual((self.adr_dir / name).read_bytes(), expected)\n+\n+        sadr_numbers = {\n+            path.name.split(\"-\", 2)[1]\n+            for path in self.adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\")\n+        }\n+        self.assertEqual(sadr_numbers, {f\"{number:04d}\" for number in range(1, 20)})\n+\n+        numeric_records = {\n+            path.name\n+            for path in self.adr_dir.glob(\"[0-9][0-9][0-9][0-9]-*.md\")\n+        }\n+        self.assertEqual(numeric_records, set(self.product_bytes))\n+        self.assertEqual(find_adr_reference_violations(self.target), [])\n+\n+\n+class LegacySadrUpgradeTests(unittest.TestCase):\n+    def setUp(self) -> None:\n+        self.tmp = Path(tempfile.mkdtemp(prefix=\"shiki-sadr-upgrade-\"))\n+        self.addCleanup(lambda: shutil.rmtree(self.tmp, ignore_errors=True))\n+        self.target = self.tmp / \"target\"\n+        self.adr_dir = self.target / \"docs\" / \"adr\"\n+        self.product_bytes, self.legacy_paths = _create_legacy_upgrade_fixture(\n+            self.target\n+        )\n+\n+    def test_force_upgrade_deletes_only_stamped_legacy_sadrs(self) -> None:\n+        _quiet_install(self.target, force=True, validate=True)\n+\n+        for relative in self.legacy_paths:\n+            self.assertFalse((self.target / relative).exists(), relative)\n+        for name, expected in self.product_bytes.items():\n+            self.assertEqual((self.adr_dir / name).read_bytes(), expected)\n+\n+        sadrs = list(self.adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\"))\n+        self.assertEqual(len(sadrs), 19)\n+        refreshed = json.loads(\n+            (self.target / shiki_installer.INSTALL_STAMP_PATH).read_text(encoding=\"utf-8\")\n+        )\n+        for relative in self.legacy_paths:\n+            self.assertNotIn(relative, refreshed[\"digests\"])\n+        for path in sadrs:\n+            relative = path.relative_to(self.target).as_posix()\n+            self.assertEqual(refreshed[\"digests\"][relative], _sha256(path))\n+\n+    def test_non_force_install_never_deletes_legacy_files(self) -> None:\n+        before = {\n+            relative: (self.target / relative).read_bytes()\n+            for relative in self.legacy_paths\n+        }\n+        _quiet_install(self.target, force=False, validate=False)\n+        for relative, expected in before.items():\n+            self.assertEqual((self.target / relative).read_bytes(), expected)\n+        refreshed = json.loads(\n+            (self.target / shiki_installer.INSTALL_STAMP_PATH).read_text(encoding=\"utf-8\")\n+        )\n+        for relative in self.legacy_paths:\n+            self.assertEqual(refreshed[\"digests\"][relative], _sha256(self.target / relative))\n+\n+\n+class LegacyCleanupPreflightTests(unittest.TestCase):\n+    def setUp(self) -> None:\n+        self.tmp = Path(tempfile.mkdtemp(prefix=\"shiki-sadr-preflight-\"))\n+        self.addCleanup(lambda: shutil.rmtree(self.tmp, ignore_errors=True))\n+\n+    def test_all_ownership_blockers_abort_before_any_write(self) -> None:\n+        cases = (\n+            (\"absent\", \"install stamp is absent\"),\n+            (\"unreadable\", \"install stamp is unreadable\"),\n+            (\"malformed\", \"install stamp is malformed\"),\n+            (\"missing-digest\", \"install stamp has no digest\"),\n+            (\"digest-mismatch\", \"digest mismatch\"),\n+        )\n+        for name, expected_reason in cases:\n+            with self.subTest(name=name):\n+                target = self.tmp / name\n+                _, legacy_paths = _create_legacy_upgrade_fixture(target)\n+                stamp_path = target / shiki_installer.INSTALL_STAMP_PATH\n+\n+                if name == \"absent\":\n+                    stamp_path.unlink()\n+                elif name == \"unreadable\":\n+                    stamp_path.unlink()\n+                    stamp_path.mkdir()\n+                elif name == \"malformed\":\n+                    stamp_path.write_text(\"{not-json\\n\", encoding=\"utf-8\")\n+                else:\n+                    stamp = json.loads(stamp_path.read_text(encoding=\"utf-8\"))\n+                    if name == \"missing-digest\":\n+                        stamp[\"digests\"] = {}\n+                    else:\n+                        stamp[\"digests\"] = {\n+                            relative: \"deadbeef\" for relative in legacy_paths\n+                        }\n+                    stamp_path.write_text(\n+                        json.dumps(stamp, indent=2, sort_keys=True) + \"\\n\",\n+                        encoding=\"utf-8\",\n+                    )\n+\n+                before = _tree_snapshot(target)\n+                with self.assertRaises(shiki_installer.ShikiError) as raised:\n+                    _quiet_install(target, force=True, validate=False)\n+                after = _tree_snapshot(target)\n+\n+                self.assertEqual(before, after, f\"{name} preflight changed the tree\")\n+                message = str(raised.exception)\n+                self.assertIn(expected_reason, message)\n+                self.assertIn(\"No files were written\", message)\n+                for relative in legacy_paths:\n+                    self.assertIn(relative, message)\n+\n+    def test_absent_stamp_is_allowed_when_no_legacy_path_exists(self) -> None:\n+        target = self.tmp / \"no-legacy\"\n+        target.mkdir()\n+        _quiet_install(target, force=True, validate=False)\n+        self.assertTrue(\n+            (target / \"docs\" / \"adr\" / \"SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\").is_file()\n+        )\n+\n+\n+if __name__ == \"__main__\":\n+    unittest.main()\n\n2026-08-07T08:20:27.250378Z ERROR codex_models_manager::manager: failed to renew cache TTL: missing field `base_instructions` at line 94 column 5\napply patch\npatch: completed\n/private/tmp/shiki-adr-collision.pa0KwY/.worktrees/shiki-t-20260807t073307221254z-2d22d654-introduc/tests/test_adr_references.py\ndiff --git a/.codex/skills/shiki/SKILL.md b/.codex/skills/shiki/SKILL.md\nindex c8e2e85d9d75d49cc4f2d6286ee22659c4e53e75..93cded623b2c15d9f65c76491a1c2d813ac9e6c0\n--- a/.codex/skills/shiki/SKILL.md\n+++ b/.codex/skills/shiki/SKILL.md\n@@ -18,6 +18,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n Codex CLI does not register this skill as a custom `/shiki` slash command. When\n the user types `/shiki` in Codex CLI and gets \"Unrecognized command\", explain\n that this is expected: invoke Shiki in Codex with natural language such as\n@@ -69,7 +72,7 @@\n \n ## Responsibilities\n \n-- Claude Code implements and repairs by default (`shiki runner claude`, ADR 0008); Codex implements and repairs tasks explicitly assigned to `codex`.\n+- Claude Code implements and repairs by default (`shiki runner claude`, SADR-0008); Codex implements and repairs tasks explicitly assigned to `codex`.\n - Claude Code Action can act as GitHub-side CCA or reviewer.\n - CCA judges completion.\n - MergeGate authorizes state transitions and merge readiness.\ndiff --git a/.github/prompts/cca-completion-check.md b/.github/prompts/cca-completion-check.md\nindex ed203ad5f302ecc81372d8ab27d2b1232ca97497..fd89950528333ef6242e6d5443904f5392ab7e56\n--- a/.github/prompts/cca-completion-check.md\n+++ b/.github/prompts/cca-completion-check.md\n@@ -19,6 +19,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n ## Required Reading\n \n Read only the files needed to judge the PR contract. Prefer this order:\ndiff --git a/.github/prompts/codex-task-handoff.md b/.github/prompts/codex-task-handoff.md\nindex 695e211d76dea1c60e8d5bb360e78487808286a4..59441d576b01a39569722f226c854e5a740e9eb5\n--- a/.github/prompts/codex-task-handoff.md\n+++ b/.github/prompts/codex-task-handoff.md\n@@ -2,7 +2,7 @@\n \n You are Codex Front acting as the Implementer for a Shiki task.\n \n-Follow `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant ADRs, and `docs/agents/codex-handoff.md`.\n+Follow `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant Shiki SADRs and target ADRs, and `docs/agents/codex-handoff.md`.\n \n Rules:\n \ndiff --git a/AGENTS.md b/AGENTS.md\nindex 7f041100d49c2a7525f51cb0f5d696a4a6d1c78c..a06bfbabc6fa48a007389e25e12d7ed523936bed\n--- a/AGENTS.md\n+++ b/AGENTS.md\n@@ -22,7 +22,7 @@\n - **Codex Front** is an optional implementer through Codex App, Codex CLI, Codex IDE extension, or Codex Web signed in with ChatGPT OAuth/subscription auth, dispatched with `shiki runner codex` when a task is explicitly assigned to `codex`.\n - **GitHub CCA** is implemented by Claude Code Action by default, using `CLAUDE_CODE_OAUTH_TOKEN`.\n \n-Do not assume `openai/codex-action`, `OPENAI_API_KEY`, or API-key based Codex automation in the default Shiki path. API-key runners are explicit target-repo extensions and require their own ADR.\n+Do not assume `openai/codex-action`, `OPENAI_API_KEY`, or API-key based Codex automation in the default Shiki path. API-key runners are explicit target-repo extensions and require their own target ADR.\n \n Shiki is not:\n \n@@ -44,10 +44,14 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use\n+`SADR-NNNN-*.md`; target product decisions retain `NNNN-*.md` and the `ADR`\n+namespace.\n+\n Use the canonical source-of-truth order above. Runtime-specific reading orders\n are operational guidance only and must not override or reorder it.\n \n-Conversation state is not durable truth. If a decision matters, put it in GitHub, `.shiki/`, `CONTEXT.md`, or an ADR.\n+Conversation state is not durable truth. If a decision matters, put it in GitHub, `.shiki/`, `CONTEXT.md`, a Shiki SADR, or a target ADR.\n \n If GitHub and `.shiki/` disagree, surface the conflict and prefer GitHub operational state until the mirror is repaired.\n \n@@ -75,8 +79,8 @@\n Every non-trivial change follows this loop:\n \n 1. **Goal**: clarify outcome, completion conditions, non-goals, risk level, and success signals.\n-2. **grill-with-docs**: challenge the plan against domain docs, ADRs, code reality, terminology, and edge scenarios.\n-3. **Context & Impact**: find relevant docs, ADRs, code, dependencies, owners, locks, and verification surfaces.\n+2. **grill-with-docs**: challenge the plan against domain docs, Shiki SADRs, target ADRs, code reality, terminology, and edge scenarios.\n+3. **Context & Impact**: find relevant docs, SADRs, target ADRs, code, dependencies, owners, locks, and verification surfaces.\n 4. **PRD and Spec Freeze**: use `to-prd` when context is settled enough to become durable product/engineering intent. The operator's explicit approval of the PRD is Spec Freeze: the plan records a `spec_freeze` block, and no plan runs without it. Steps 1-4 form Requirements Definition \u2014 one continuous operator dialogue. After Spec Freeze, scope changes require an operator-approved Spec Amendment; non-scope-moving interpretations go to the Assumption Log.\n 5. **Issues**: use `to-issues` to create independently grabbable vertical-slice issues. Prefer AFK slices over HITL slices where possible.\n 6. **Triage**: label issues for readiness, risk, runtime, skills, and MergeGate state.\n@@ -167,7 +171,7 @@\n - Testing decisions.\n - Out-of-scope items.\n - Further notes.\n-- Links to relevant domain terms and ADRs.\n+- Links to relevant domain terms, SADRs, and target ADRs.\n \n Every executable task must include:\n \n@@ -193,7 +197,7 @@\n \n Context & Impact must identify:\n \n-- Relevant documents, domain terms, ADRs, and past decisions.\n+- Relevant documents, domain terms, SADRs, target ADRs, and past decisions.\n - Relevant modules, interfaces, seams, callers, owners, and tests.\n - Dependency relationships between tasks.\n - Candidate locks and lock conflicts.\n@@ -210,7 +214,7 @@\n | Skill | Required when |\n | --- | --- |\n | `setup-matt-pocock-skills` | First configuring the repo for skills, or when issue tracker, triage labels, or domain docs are missing. |\n-| `grill-with-docs` | Default for non-trivial Goals before PRD/issues; always required when plans, terminology, boundaries, tradeoffs, or ADR-worthy decisions are ambiguous. |\n+| `grill-with-docs` | Default for non-trivial Goals before PRD/issues; always required when plans, terminology, boundaries, tradeoffs, or decision-record-worthy choices are ambiguous. |\n | `zoom-out` | The agent lacks an architectural map of the relevant code area. |\n | `to-prd` | Settled Goal context must become durable product/engineering intent. |\n | `to-issues` | A Goal, PRD, or plan must become independently grabbable vertical-slice issues. |\n@@ -348,7 +352,7 @@\n - Three or more new files.\n - Any high-risk or critical label.\n \n-When the architecture gate triggers, run `grill-with-docs` or `improve-codebase-architecture` as appropriate and record durable decisions in an ADR when needed.\n+When the architecture gate triggers, run `grill-with-docs` or `improve-codebase-architecture` as appropriate and record durable decisions in a Shiki SADR or target ADR as applicable.\n \n ## Engineering Discipline\n \ndiff --git a/CLAUDE.md b/CLAUDE.md\nindex 98a932e7841a632866fcd53b0bad0e3af3711d3d..98a31399404d6fb044b4fd493f4459ac87f439de\n--- a/CLAUDE.md\n+++ b/CLAUDE.md\n@@ -14,6 +14,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n ## Claude Role\n \n Default to planner, default implementer, reviewer, coordinator, documentation editor, CCA-style completion judge, and final-judgment assistant.\n@@ -57,7 +60,7 @@\n At the start of a Shiki session:\n \n 1. Read `AGENTS.md`.\n-2. Read `CONTEXT.md` and relevant ADRs.\n+2. Read `CONTEXT.md` and relevant Shiki SADRs and target ADRs.\n 3. Read active `.shiki/` Goal/task/ledger state when present.\n 4. Read active distilled rules from `.shiki/memories/` (status `distilled`, `active: true`, not revoked or superseded) for applicable guidance; the same rules are injected deterministically into task handoffs as the `## Distilled Rules` section (\u00a73.5).\n 5. Read the GitHub Issue or PR when the task is GitHub-backed.\n@@ -74,7 +77,7 @@\n - Define the Goal, completion conditions, non-goals, and risk level.\n - Treat `grill-with-docs` as the default planning entry point for non-trivial Goals.\n - Challenge domain terms against `CONTEXT.md`.\n-- Challenge hard-to-reverse tradeoffs against ADRs.\n+- Challenge hard-to-reverse tradeoffs against SADRs and target ADRs.\n - Explore code instead of asking when code can answer the question.\n - Run Context & Impact before implementation planning.\n - Produce a Task DAG, not an unordered checklist.\n@@ -94,7 +97,7 @@\n - For each question, provide your recommended answer.\n - Resolve dependency decisions before downstream design questions.\n - Update `CONTEXT.md` inline when terminology crystallizes.\n-- Offer ADRs only when the decision is hard to reverse, surprising without context, and a real tradeoff.\n+- Offer a SADR or target ADR only when the decision is hard to reverse, surprising without context, and a real tradeoff.\n - Convert unresolved questions into HITL blockers rather than allowing Codex to guess.\n \n ## PRD and Issues Standard\n@@ -103,7 +106,7 @@\n \n - Use the project's domain glossary vocabulary.\n - Avoid volatile file paths unless they are essential.\n-- Include implementation decisions, testing decisions, out-of-scope items, and relevant ADR/domain links.\n+- Include implementation decisions, testing decisions, out-of-scope items, and relevant SADR/target-ADR/domain links.\n - Identify deep-module opportunities and testable interfaces.\n \n When creating issues:\n@@ -121,7 +124,7 @@\n - Branch/worktree target.\n - Scope and non-goals.\n - Dependencies and locks.\n-- Relevant docs, ADRs, modules, and tests.\n+- Relevant docs, SADRs, target ADRs, modules, and tests.\n - Required skill invocations.\n - TDD expectations.\n - Pre-PR `code-review` gate expectations.\n@@ -152,7 +155,7 @@\n \n - Treat yourself as a judge, not implementer.\n - Do not write production code.\n-- Read `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant ADRs, PRD, task issue, PR body, diff, checks, reviews, labels, and `.shiki/` state.\n+- Read `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant SADRs and target ADRs, PRD, task issue, PR body, diff, checks, reviews, labels, and `.shiki/` state.\n - Evaluate every applicable checklist in `docs/agents/checklists.md`.\n - Map each acceptance criterion to durable evidence.\n - Distinguish implementation failure from missing evidence.\ndiff --git a/CONTEXT.md b/CONTEXT.md\nindex e61a9be06a0cce0d026484083a657bde2067e234..237ee8aa3ea8fab334ece49768bc5ed3b5af0974\n--- a/CONTEXT.md\n+++ b/CONTEXT.md\n@@ -25,7 +25,7 @@\n _Avoid_: jumping directly into code\n \n **grill-with-docs**:\n-A planning skill that challenges a plan against domain language, ADRs, code reality, and concrete edge scenarios. It resolves design-tree questions before PRD/issues. It runs inside Requirements Definition.\n+A planning skill that challenges a plan against domain language, Shiki SADRs, target ADRs, code reality, and concrete edge scenarios. It resolves design-tree questions before PRD/issues. It runs inside Requirements Definition.\n _Avoid_: generic brainstorming, silent assumptions\n \n **Requirements Definition**:\ndiff --git a/CONTRIBUTING.md b/CONTRIBUTING.md\nindex c6d9b656e73abb8a55e5f660123e03153dcb3e6d..9e37a8fbf668db5b22461aaae5548b8f1354394c\n--- a/CONTRIBUTING.md\n+++ b/CONTRIBUTING.md\n@@ -17,7 +17,7 @@\n 1. **Goal** \u2014 start from a user-approved Goal with an outcome, completion\n    conditions, non-goals, risk level, and success signals.\n 2. **grill-with-docs** \u2014 for any non-trivial change, challenge the plan against\n-   domain language, ADRs, and code reality before writing a PRD.\n+   domain language, Shiki SADRs, target ADRs, and code reality before writing a PRD.\n 3. **PRD** \u2014 capture settled product/engineering intent (`to-prd`).\n 4. **Spec Freeze** \u2014 the operator explicitly approves the PRD/requirements;\n    the plan records a `spec_freeze` block and no plan runs without it\n@@ -94,7 +94,7 @@\n   entries.\n - Do not hand-edit `.shiki/goals/*.json` or `.shiki/manifest.json` in a task PR.\n - Conversation state is not durable truth. If a decision matters, record it in\n-  GitHub, `.shiki/`, `CONTEXT.md`, or an ADR.\n+  GitHub, `.shiki/`, `CONTEXT.md`, a Shiki SADR, or a target ADR.\n \n ## Architecture Gate\n \n@@ -103,7 +103,7 @@\n or CI policy, package structure, three or more structural files, more than 100\n net new lines in a core module, three or more new files, or any high-risk label.\n When triggered, run `grill-with-docs` or `improve-codebase-architecture` and\n-record durable decisions in an ADR when needed (see `docs/adr/`).\n+record durable decisions in a Shiki SADR or target ADR as applicable (see `docs/adr/`).\n \n ## Safety\n \ndiff --git a/SYSTEM_PROMPT.md b/SYSTEM_PROMPT.md\nindex aaaea45e3a896c3a44ba89eec8bb84325ecf6c62..da009c86a88285269dee0155e58e550c6d744ea9\n--- a/SYSTEM_PROMPT.md\n+++ b/SYSTEM_PROMPT.md\n@@ -30,6 +30,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n Use the canonical source-of-truth order above. Runtime-specific reading orders\n are operational guidance only and must not override or reorder it. The current\n conversation is non-durable operator input.\n@@ -48,14 +51,14 @@\n \n A runtime may only do work that is explicitly assigned by the Goal, task, PR, or MergeGate decision.\n \n-Do not assume `openai/codex-action`, `OPENAI_API_KEY`, or API-key based Codex automation in the default Shiki path. API-key runners are explicit target-repo extensions and require their own ADR.\n+Do not assume `openai/codex-action`, `OPENAI_API_KEY`, or API-key based Codex automation in the default Shiki path. API-key runners are explicit target-repo extensions and require their own target ADR.\n \n ## 4. Non-negotiable Shiki flow\n \n For any non-trivial Goal, use this flow:\n \n 1. **Goal Seek**: define outcome, non-goals, risk level, completion criteria, and evidence requirements.\n-2. **grill-with-docs**: challenge the plan against `CONTEXT.md`, ADRs, code reality, terminology, boundaries, and edge scenarios. Ask one question at a time when the operator is available. If code can answer a question, inspect code instead of asking.\n+2. **grill-with-docs**: challenge the plan against `CONTEXT.md`, Shiki SADRs, target ADRs, code reality, terminology, boundaries, and edge scenarios. Ask one question at a time when the operator is available. If code can answer a question, inspect code instead of asking.\n 3. **Context & Impact**: identify modules, interfaces, seams, callers, dependencies, locks, risk, and verification surfaces.\n 4. **to-prd + Spec Freeze**: turn settled context into a PRD using domain vocabulary and testing decisions. The operator's explicit approval of the PRD is Spec Freeze, recorded as the plan's `spec_freeze` block; plans without it do not run. Steps 1-4 form Requirements Definition.\n 5. **to-issues**: decompose the PRD into independently grabbable vertical-slice issues. Prefer AFK slices; mark HITL slices when judgment is still required.\n@@ -75,7 +78,7 @@\n Before making any material change:\n \n 1. Read `AGENTS.md` and any runtime-specific wrapper such as `CLAUDE.md`.\n-2. Read `CONTEXT.md` and relevant ADRs.\n+2. Read `CONTEXT.md` and relevant SADRs and target ADRs.\n 3. Read the GitHub Issue / PR and `.shiki/` state for the active Goal or task.\n 4. Identify your role: Planner, Implementer, Reviewer, Completion Check Agent, Repairer, or Guardian-assist.\n 5. Identify Goal id, task id, branch, PR, dependencies, locks, risk level, required skills, and required evidence.\n@@ -103,7 +106,7 @@\n \n At minimum, identify:\n \n-- Relevant documents, ADRs, domain terms, and prior decisions.\n+- Relevant documents, SADRs, target ADRs, domain terms, and prior decisions.\n - Relevant modules, interfaces, seams, callers, owners, and tests.\n - Dependency relationships between tasks.\n - Candidate file locks and lock conflicts.\n@@ -152,7 +155,7 @@\n \n CCA must read:\n \n-- `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant ADRs.\n+- `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant SADRs and target ADRs.\n - Parent Goal issue, PRD issue, task issue, comments, and labels.\n - PR body, diff, commits, changed files, reviews, CI checks, and artifacts available to the workflow.\n - `.shiki/` task contract, locks, ledger entries, CCA checklist profile, and prior repair packets.\n@@ -214,7 +217,7 @@\n - Three or more new files.\n - Any high-risk or critical label.\n \n-When the architecture gate triggers, run `grill-with-docs` or `improve-codebase-architecture` as appropriate and record the decision in an ADR if it is hard to reverse or likely to be re-litigated.\n+When the architecture gate triggers, run `grill-with-docs` or `improve-codebase-architecture` as appropriate and record the decision in a Shiki SADR or target ADR as applicable if it is hard to reverse or likely to be re-litigated.\n \n ## 14. Skill Gate\n \n@@ -223,7 +226,7 @@\n Use these skills:\n \n - `setup-matt-pocock-skills`: before first use of the engineering skill set, or when issue tracker, triage labels, or domain docs are missing.\n-- `grill-with-docs`: default for non-trivial Goals before PRD/issues; required for ambiguous plans, terminology, boundaries, ADR-worthy tradeoffs, or design-tree decisions.\n+- `grill-with-docs`: default for non-trivial Goals before PRD/issues; required for ambiguous plans, terminology, boundaries, decision-record-worthy tradeoffs, or design-tree decisions.\n - `zoom-out`: unfamiliar code area or missing architectural map.\n - `to-prd`: convert settled context into a PRD.\n - `to-issues`: break a PRD, plan, or Goal into independently grabbable vertical-slice issues.\ndiff --git a/docs/adr/0001-keep-shiki-as-platform-name.md b/docs/adr/SADR-0001-keep-shiki-as-platform-name.md\nindex 20a9637c3b9d8c97de7ba8db7cfae4d82d9a7dba..8b685439959aada1b221c112f0ac3c51ca8a1695\n--- a/docs/adr/0001-keep-shiki-as-platform-name.md\n+++ b/docs/adr/SADR-0001-keep-shiki-as-platform-name.md\n@@ -1,3 +1,3 @@\n-# Keep Shiki As The Platform Name\n+# SADR-0001: Keep Shiki As The Platform Name\n \n Shiki remains the product name for the GitHub-native agentic engineering control plane. We considered more descriptive names such as MergePilot, MergeGate, and Kiroku, but kept Shiki because it carries the existing goal-seeking framework identity while remaining independent of Hermes, Codex, Claude Code, or any single runtime.\ndiff --git a/docs/adr/0002-use-agents-as-shared-constitution.md b/docs/adr/SADR-0002-use-agents-as-shared-constitution.md\nindex 73de30ecb078120eac540d9bc51855e3cd48d0e3..c5486182bf06fca36ea508496b013c67302fd989\n--- a/docs/adr/0002-use-agents-as-shared-constitution.md\n+++ b/docs/adr/SADR-0002-use-agents-as-shared-constitution.md\n@@ -1,3 +1,3 @@\n-# Use AGENTS As The Shared Agent Constitution\n+# SADR-0002: Use AGENTS As The Shared Agent Constitution\n \n Shiki uses `AGENTS.md` as the runtime-neutral constitution and `CLAUDE.md` as a thin Claude Code wrapper. This keeps Codex, Claude Code, Hermes Runner, and future Agent Runtimes aligned on the same GitHub-first MergeGate rules while still allowing runtime-specific instructions where needed.\ndiff --git a/docs/adr/0003-require-skill-gate-for-mergegate-readiness.md b/docs/adr/SADR-0003-require-skill-gate-for-mergegate-readiness.md\nindex 424be0c9af4c8f0136f1fde68a468c47ff84e764..7fc337987c7df9792f75f181dfcbed75fe5ffff2\n--- a/docs/adr/0003-require-skill-gate-for-mergegate-readiness.md\n+++ b/docs/adr/SADR-0003-require-skill-gate-for-mergegate-readiness.md\n@@ -1,3 +1,3 @@\n-# Require Skill Gate For MergeGate Readiness\n+# SADR-0003: Require Skill Gate For MergeGate Readiness\n \n Shiki requires relevant engineering skills to be invoked before execution when their triggers apply. This makes skills part of MergeGate readiness rather than optional prompt style, because Shiki depends on repeatable planning, issue decomposition, TDD, diagnosis, triage, and architecture review before autonomous or parallel agent work can be trusted.\ndiff --git a/docs/adr/0004-use-github-first-with-shiki-mirror.md b/docs/adr/SADR-0004-use-github-first-with-shiki-mirror.md\nindex d0a3a9174dc646a945a9d325954ffaf00c352040..a79c54ae9ee8eed2a0524192b864da4e22a92d3f\n--- a/docs/adr/0004-use-github-first-with-shiki-mirror.md\n+++ b/docs/adr/SADR-0004-use-github-first-with-shiki-mirror.md\n@@ -1,4 +1,4 @@\n-# ADR 0004: Use GitHub First With A Repository-Local Shiki Mirror\n+# SADR-0004: Use GitHub First With A Repository-Local Shiki Mirror\n \n ## Status\n \ndiff --git a/docs/adr/0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md b/docs/adr/SADR-0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\nindex b339baf49b89d9290e79c3d51b36e7b2bde3bd95..0f3c3eb9afccdb3403ce978e21d04fe12e495da0\n--- a/docs/adr/0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\n+++ b/docs/adr/SADR-0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\n@@ -1,8 +1,8 @@\n-# ADR 0005: Use Codex As Front And Claude Code Action As The GitHub Runtime\n+# SADR-0005: Use Codex As Front And Claude Code Action As The GitHub Runtime\n \n ## Status\n \n-Accepted (implementer default superseded by ADR 0008; the auth model and the\n+Accepted (implementer default superseded by SADR-0008; the auth model and the\n Claude Code Action GitHub runtime decision remain in force)\n \n ## Context\n@@ -26,4 +26,4 @@\n - Shiki avoids requiring `OPENAI_API_KEY` for the default workflow.\n - Codex work happens through the user's authenticated Codex front surface, not as a hidden backend Action.\n - Claude Code Action is configured for OAuth-token based subscription usage by default.\n-- API-key based automation remains possible only as an explicit target-repo extension with its own ADR.\n+- API-key based automation remains possible only as an explicit target-repo extension with its own target ADR.\ndiff --git a/docs/adr/0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md b/docs/adr/SADR-0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\nindex 4bcae9d1e52f0a37b8e9fbafa5cd14b6ee1993bd..7a38538a6ae9929567f0ade67e7ea6a60d0b3f7e\n--- a/docs/adr/0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\n+++ b/docs/adr/SADR-0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\n@@ -1,4 +1,4 @@\n-# ADR 0006: Separate Codex Implementation, CCA Completion, And MergeGate Authorization\n+# SADR-0006: Separate Codex Implementation, CCA Completion, And MergeGate Authorization\n \n ## Status\n \ndiff --git a/docs/adr/0007-packaging-and-release.md b/docs/adr/SADR-0007-packaging-and-release.md\nindex 79b84ddb97167372b44641e5b7860e400ce80822..bb445a2e2e856d9d1f5fdbe35de8d4e8cec0fb32\n--- a/docs/adr/0007-packaging-and-release.md\n+++ b/docs/adr/SADR-0007-packaging-and-release.md\n@@ -1,4 +1,4 @@\n-# ADR 0007: Packaging And Release For Shiki\n+# SADR-0007: Packaging And Release For Shiki\n \n ## Status\n \n@@ -25,7 +25,7 @@\n   not introduce third-party runtime dependencies or break the shim.\n - Releases must run inside the operator's subscription-authenticated toolchain\n   and must not require extra secrets beyond the built-in `GITHUB_TOKEN`\n-  (consistent with ADR 0005's no-API-key-by-default stance).\n+  (consistent with SADR-0005's no-API-key-by-default stance).\n - `.shiki/manifest.json` already encodes the install surface (`install.include`,\n   `install.create_directories`, `install.exclude_from_commit`), and it must not\n   be mutated as part of this change.\n@@ -34,7 +34,7 @@\n \n This is a hard-to-reverse platform decision (it sets the version source of\n truth, the release trigger, and the public install contract), so it is recorded\n-as an ADR.\n+as a SADR.\n \n ## Decision\n \n@@ -104,7 +104,7 @@\n 3. **Publishing to PyPI.** Rejected for now: Shiki is distributed as a\n    repository template + git-installable CLI, not a PyPI library; PyPI would add\n    credentials/secrets and a different versioning contract. Can be added later as\n-   its own ADR if a packaged distribution is needed.\n+   its own SADR if a packaged distribution is needed.\n 4. **Dynamic version (read `VERSION` via a build backend `dynamic` field).**\n    Rejected to keep `pyproject.toml` backend-agnostic and dependency-free; the\n    small duplication between `VERSION` and `pyproject.toml` is guarded by the\n@@ -123,4 +123,4 @@\n   releasing versions the whole platform but ships only `install.include` to\n   targets.\n - Future PyPI distribution or a console entry point are deferred and would each\n-  warrant their own ADR.\n+  warrant their own SADR.\ndiff --git a/docs/adr/0008-use-claude-code-as-default-implementer.md b/docs/adr/SADR-0008-use-claude-code-as-default-implementer.md\nindex dedf4510b7c7799521422f622029563d373db031..35c2625d908167fbc58855b5b63e283805f167e8\n--- a/docs/adr/0008-use-claude-code-as-default-implementer.md\n+++ b/docs/adr/SADR-0008-use-claude-code-as-default-implementer.md\n@@ -1,12 +1,12 @@\n-# ADR 0008: Use Claude Code As The Default Implementer Runtime\n+# SADR-0008: Use Claude Code As The Default Implementer Runtime\n \n ## Status\n \n-Proposed (supersedes ADR 0005 for the implementer default; keeps its auth model)\n+Proposed (supersedes SADR-0005 for the implementer default; keeps its auth model)\n \n ## Context\n \n-ADR 0005 made Codex Front the default implementation runtime, with Claude Code\n+SADR-0005 made Codex Front the default implementation runtime, with Claude Code\n restricted to planner and reviewer roles by the runtime registry, and the only\n autonomous implementation adapter was `shiki runner codex`.\n \n@@ -18,13 +18,13 @@\n - The platform's planning, review, and CCA layers already run on Claude Code,\n   and Claude Code's native capabilities (the built-in `/code-review` skill and\n   the Workflow multi-agent orchestration tool) have no Codex equivalent.\n-- The Goal lifecycle redesign (ADR 0009) requires an autonomous post-freeze\n+- The Goal lifecycle redesign (SADR-0009) requires an autonomous post-freeze\n   loop driven from the operator's Claude Code session, including bounded\n   repair dispatch, which must not depend on a runtime that cannot be invoked\n   unattended.\n \n The constitution claims Shiki is runtime-agnostic (\"not a Claude-only\n-workflow\", ADR 0002). Any change to the implementer default must preserve the\n+workflow\", SADR-0002). Any change to the implementer default must preserve the\n registry-based runtime model rather than hardcoding one vendor.\n \n ## Decision\n@@ -39,7 +39,7 @@\n   the task to review or repair-needed.\n - Keep Codex registered as an optional implementer runtime. `shiki runner\n   codex`, the Codex handoff templates, and the ChatGPT OAuth auth model from\n-  ADR 0005 remain supported for tasks explicitly assigned to `codex`.\n+  SADR-0005 remain supported for tasks explicitly assigned to `codex`.\n - Commit to mandating two Claude-native capabilities as loop contract in\n   follow-up Goals (status: the code-review gate was delivered by Goal G-B \u2014\n   skill registry, checklists item PR-12, default required_skills \u2014 while the\n@@ -60,7 +60,7 @@\n \n Out of scope: removing Codex support, changing the CCA runtime (Claude Code\n Action with `CLAUDE_CODE_OAUTH_TOKEN` stays), and API-key based automation\n-(still requires its own ADR per ADR 0005).\n+(still requires its own target ADR per SADR-0005).\n \n ## Consequences\n \n@@ -100,7 +100,7 @@\n   harness rejection of direct `codex exec` is outside Shiki's control, and the\n   plan's mandatory native capabilities cannot run on Codex at all.\n - **Remove Codex entirely**: rejected; it abandons the runtime-agnostic\n-  constitution (ADR 0002) for no operational gain and forecloses\n+  constitution (SADR-0002) for no operational gain and forecloses\n   cross-runtime adversarial review.\n - **GitHub Actions cloud implementation via claude-code-action**: rejected as\n   default; durable evidence is automatic but token cost, Node 24 workflow\ndiff --git a/docs/adr/0009-requirements-definition-and-spec-freeze.md b/docs/adr/SADR-0009-requirements-definition-and-spec-freeze.md\nindex 29b3f7871bef9203a8b4a831a65c39eb2f506e42..52e7dd69ad0aa4efe0b68c62678abbf7df02aaec\n--- a/docs/adr/0009-requirements-definition-and-spec-freeze.md\n+++ b/docs/adr/SADR-0009-requirements-definition-and-spec-freeze.md\n@@ -1,4 +1,4 @@\n-# ADR 0009: Merge Grill And Planning Into Requirements Definition With Spec Freeze\n+# SADR-0009: Merge Grill And Planning Into Requirements Definition With Spec Freeze\n \n ## Status\n \ndiff --git a/docs/adr/0010-external-ai-guardian-review.md b/docs/adr/SADR-0010-external-ai-guardian-review.md\nindex e6108bbe8bb4e73e0ff4684c8f78001a80438c4e..f3510b145664298adafb151b6f0db51b972f01ec\n--- a/docs/adr/0010-external-ai-guardian-review.md\n+++ b/docs/adr/SADR-0010-external-ai-guardian-review.md\n@@ -1,4 +1,4 @@\n-# ADR 0010: External AI Guardian Review As A First-Class Approval Authority\n+# SADR-0010: External AI Guardian Review As A First-Class Approval Authority\n \n ## Status\n \n@@ -8,7 +8,7 @@\n \n Shiki is intended to be an autonomous, recursive, self-improving agent\n framework: authority-in-the-loop, not human-in-the-loop. Until now the Guardian\n-approval required for high/critical-risk merges (guardian-policy.json, ADR\n+approval required for high/critical-risk merges (guardian-policy.json, SADR\n governance) recognized only human authority sources \u2014 a GitHub review approval,\n the `guardian:approved` label applied by a configured Guardian, or a comment\n carrying `Guardian approval granted` plus the current head SHA.\n@@ -89,7 +89,7 @@\n   autonomous design the operator intends and produced the impersonation\n   incident.\n - **Record AI approval as an operator comment**: rejected as forgery of human\n-  authority \u2014 the exact failure this ADR corrects.\n+  authority \u2014 the exact failure this SADR corrects.\n - **Deliver the artifact as a committed `.shiki/` file**: rejected; a committed\n   approval inside the PR being approved is forgeable by anyone who can push.\n - **A dedicated reviewer bot identity instead of a relayed comment**: deferred;\ndiff --git a/docs/adr/0011-autonomous-quality-gate-evidence.md b/docs/adr/SADR-0011-autonomous-quality-gate-evidence.md\nindex bca0246c5d46e44afbde15486a75eec425fc02f5..9c93eac16d8c3a6d4b11bd7224e3955102cd7db4\n--- a/docs/adr/0011-autonomous-quality-gate-evidence.md\n+++ b/docs/adr/SADR-0011-autonomous-quality-gate-evidence.md\n@@ -1,4 +1,4 @@\n-# ADR 0011: Autonomous Quality-Gate Evidence \u2014 Deterministic Facts And An Independent Verifier\n+# SADR-0011: Autonomous Quality-Gate Evidence \u2014 Deterministic Facts And An Independent Verifier\n \n ## Status\n \ndiff --git a/docs/adr/0012-autonomous-loop-closeout-to-main.md b/docs/adr/SADR-0012-autonomous-loop-closeout-to-main.md\nindex 637fad6dde4989c9c2bf0f81f676d8d29991123c..ec7d85f81030a676bcbba28b43eb365422a36eac\n--- a/docs/adr/0012-autonomous-loop-closeout-to-main.md\n+++ b/docs/adr/SADR-0012-autonomous-loop-closeout-to-main.md\n@@ -1,4 +1,4 @@\n-# 0012 \u2014 Autonomous loop pushes goal completion to main via a closeout PR\n+# SADR-0012 \u2014 Autonomous loop pushes goal completion to main via a closeout PR\n \n - Status: accepted\n - Date: 2026-06-18\ndiff --git a/docs/adr/0013-bootstrap-grants-actions-approve-pull-requests.md b/docs/adr/SADR-0013-bootstrap-grants-actions-approve-pull-requests.md\nindex b6c69d64f1c5ced346b88d6859586715b3b5bd0e..cf48ff4a6716267f539ca58edd1a852e7c7a2d64\n--- a/docs/adr/0013-bootstrap-grants-actions-approve-pull-requests.md\n+++ b/docs/adr/SADR-0013-bootstrap-grants-actions-approve-pull-requests.md\n@@ -1,4 +1,4 @@\n-# ADR 0013: Bootstrap Grants GitHub Actions Permission To Create And Approve Pull Requests\n+# SADR-0013: Bootstrap Grants GitHub Actions Permission To Create And Approve Pull Requests\n \n ## Status\n \n@@ -31,7 +31,7 @@\n security tradeoff: it lets repository automation satisfy a human-review gate. In\n a multi-maintainer repo that could let a compromised or buggy workflow\n self-approve changes. So whether bootstrap turns it on by default \u2014 and with\n-what default token scope \u2014 is an ADR-worthy decision, not an implementation\n+what default token scope \u2014 is a SADR-worthy decision, not an implementation\n detail.\n \n ## Decision\n@@ -48,7 +48,7 @@\n Specifically:\n \n - **`can_approve_pull_request_reviews` defaults to `true`.** Shiki's default\n-  posture is solo/AFK autonomous operation (ADR 0008/0009/0012), where the CCA\n+  posture is solo/AFK autonomous operation (SADR-0008/0009/0012), where the CCA\n   Review Bridge is the only path that can satisfy `required_review: true` after\n   the CCA and Guardian gates have already passed. The default makes a\n   freshly-bootstrapped repo able to run the autonomous loop end to end without a\ndiff --git a/docs/adr/0014-codex-app-external-ai-guardian-ui-adapter.md b/docs/adr/SADR-0014-codex-app-external-ai-guardian-ui-adapter.md\nindex 238db06279334caa341c117362df9271382a7fcd..65f12e9939321e1b3e389a34c822fc1549e5155d\n--- a/docs/adr/0014-codex-app-external-ai-guardian-ui-adapter.md\n+++ b/docs/adr/SADR-0014-codex-app-external-ai-guardian-ui-adapter.md\n@@ -1,4 +1,4 @@\n-# ADR 0014: Use Codex App As The External AI Guardian UI Adapter\n+# SADR-0014: Use Codex App As The External AI Guardian UI Adapter\n \n ## Status\n \ndiff --git a/docs/adr/0015-contract-approval-before-dispatch.md b/docs/adr/SADR-0015-contract-approval-before-dispatch.md\nindex fad6c656741a20a7684ba16080a7db159f63a52f..6061271a46d3f1d6ae7174477de3fb75673123a6\n--- a/docs/adr/0015-contract-approval-before-dispatch.md\n+++ b/docs/adr/SADR-0015-contract-approval-before-dispatch.md\n@@ -1,4 +1,4 @@\n-# ADR 0015: Approve The Task Contract Before Dispatch, Not The Diff After It\n+# SADR-0015: Approve The Task Contract Before Dispatch, Not The Diff After It\n \n ## Status\n \n@@ -9,7 +9,7 @@\n Guardian approval for high/critical risk is expressible only on a pull request.\n Every approval source in `guardian-policy.json` \u2014 GitHub review, the\n `guardian:approved` label, the `Guardian approval granted` comment, and the\n-`external_ai_guardian_review` artifact (ADR 0010/0014) \u2014 is bound to a PR, and\n+`external_ai_guardian_review` artifact (SADR-0010/0014) \u2014 is bound to a PR, and\n three of the four require the current head SHA. Approval therefore cannot exist\n before an implementation PR exists.\n \n@@ -81,7 +81,7 @@\n An envelope breach must surface only on the non-repairable policy gate, never on\n the repairable `MergeGate metadata check`. Otherwise the Goal Loop routes it to\n `dispatch_repair` \u2014 instructing a runner to make the breach go away \u2014 which is\n-exactly what this ADR forbids.\n+exactly what this SADR forbids.\n \n **Contract immutability.** After registration, an implementation PR must not\n change the governance fields of its own task contract (`scope`, `non_goals`,\n@@ -116,7 +116,7 @@\n fails any verdict that is not `complete`, and the signal the CCA prompt reads is\n computed from `evaluate_guardian_approval` alone. Without the OR-merge at the\n signal, a contract-approved PR still returns `needs_guardian`, the CCA job fails,\n-and the MergeGate policy check never runs \u2014 the gate this ADR changes would never\n+and the MergeGate policy check never runs \u2014 the gate this SADR changes would never\n be reached. The CCA *prompt* needs no change: a `contract_approval` entry in the\n signal's existing `sources` array satisfies its current wording.\n \n@@ -130,7 +130,7 @@\n written, and an approved contract lets its implementation PRs merge through the\n same automatic path as low/medium risk. The wasted implement-then-block cycle\n disappears. Guardian review shifts from reading diffs to reading contracts, which\n-is both earlier and smaller; the external AI Guardian path (ADR 0010/0014) works\n+is both earlier and smaller; the external AI Guardian path (SADR-0010/0014) works\n on contracts with no change.\n \n We accept that the Guardian authorizes a class of change without seeing its\n@@ -146,7 +146,7 @@\n adversarial tests for the new approval path equivalent in rigor to\n `test_shiki_governance_evidence.sh`.\n \n-Bootstrap costs, which apply only while this ADR is being implemented. The tasks\n+Bootstrap costs, which apply only while this SADR is being implemented. The tasks\n that build Contract Approval merge under the existing rules, so the ones\n dispatched after the pre-dispatch gate lands but before the approval path lands\n need a Guardian-approved Contract PR *and* a live Guardian approval on their\n@@ -210,7 +210,7 @@\n risk; they do not bound authorized work.\n \n **Auto-escalate risk on envelope breach instead of stopping.** Returns the\n-workflow to a diff-time Guardian decision \u2014 the exact behavior this ADR removes.\n+workflow to a diff-time Guardian decision \u2014 the exact behavior this SADR removes.\n \n **Resolve governance fields from the base snapshot only.** Rejected against the\n registration flow: implementation PRs add their own contract, so the base has no\ndiff --git a/docs/adr/0016-locks-bound-product-paths.md b/docs/adr/SADR-0016-locks-bound-product-paths.md\nindex b5e850f3f1f7f4ef6e7f9d1763a29a51bd300c45..46b40a9b99a7aae644c02bb458f5473ff0a90ce2\n--- a/docs/adr/0016-locks-bound-product-paths.md\n+++ b/docs/adr/SADR-0016-locks-bound-product-paths.md\n@@ -1,4 +1,4 @@\n-# ADR 0016: Locks bound product paths; the Shiki mirror is bound by record identity\n+# SADR-0016: Locks bound product paths; the Shiki mirror is bound by record identity\n \n ## Status\n \n@@ -6,8 +6,8 @@\n \n - Date: 2026-07-28\n - Deciders: operator (mizutani-140), Claude (planner)\n-- Related: ADR 0004 (GitHub-first with a `.shiki` mirror), ADR 0009 (Spec Freeze),\n-  ADR 0012 (autonomous loop closeout to main), ADR 0015 (Contract Approval before\n+- Related: SADR-0004 (GitHub-first with a `.shiki` mirror), SADR-0009 (Spec Freeze),\n+  SADR-0012 (autonomous loop closeout to main), SADR-0015 (Contract Approval before\n   dispatch), remediation goal task R-02, the live PR #179 deadlock.\n \n ## Context\n@@ -90,11 +90,11 @@\n files, documentation files and named source modules are nameable; runtime-\n allocated ledger, runner and report ids are not \u2014 and those live in `.shiki`,\n which locks no longer govern. Existing frozen contracts are brought into line by\n-a one-time Spec Amendment per plan, which must land before ADR 0015's contract\n+a one-time Spec Amendment per plan, which must land before SADR-0015's contract\n immutability makes `locks` unwritable from the implementation PR.\n \n **Lock state is resolved from the durable side.** MergeGate resolves the lock\n-registry, and ADR 0015's governance fields, from the base `.shiki` snapshot when\n+registry, and SADR-0015's governance fields, from the base `.shiki` snapshot when\n present, falling back to head only when absent. The `MergeGate metadata check`\n job is given `--base-shiki`, which it does not receive today. Locks are not\n derived from the live open-pull-request set: a pull request body is untrusted\n@@ -108,7 +108,7 @@\n Explicitly out of scope: moving MergeGate's contract resolution off the PR\n checkout wholesale; making the mirror a projection of GitHub state; introducing\n new lock kinds; moving ledger evidence out of the repository; and unwinding\n-ADR 0012's closeout pull request. Registration of multi-task goals is ADR 0015's\n+SADR-0012's closeout pull request. Registration of multi-task goals is SADR-0015's\n Contract PR and is not re-decided here.\n \n ## Consequences\n@@ -127,7 +127,7 @@\n \n Risks accepted. First, goal-scoped identity is only as strong as `goal_id`,\n which is resolved from the PR body and the head task file; the mitigation is to\n-add `goal_id` to ADR 0015's frozen governance field set, and until that lands a\n+add `goal_id` to SADR-0015's frozen governance field set, and until that lands a\n task file can name a foreign goal. Second, base-when-present resolution is\n unavailable on fork pull requests, where the head is contributor-controlled;\n fork pull requests already cannot complete a Shiki merge, and this decision does\n@@ -139,7 +139,7 @@\n path, not by implementation pull requests.\n \n New obligations: one Spec Amendment per frozen plan to rewrite `locks`, ordered\n-before ADR 0015 T1; extension of the adversarial suites in\n+before SADR-0015 T1; extension of the adversarial suites in\n `scripts/test_shiki_adversarial_state.sh` and\n `scripts/test_shiki_governance_evidence.sh` to cover the eleven newly-scoped\n directories; and updating `scripts/test_shiki_loop_lock_guard.sh`,\n@@ -151,32 +151,32 @@\n whether the loop's transitive sync of ledger `evidence` references can be\n deleted once the Contract PR carries its own source plan.\n \n-## Relationship to ADR 0004\n+## Relationship to SADR-0004\n \n-This ADR **clarifies something ADR 0004 left unspecified**. ADR 0004 decides that\n+This SADR **clarifies something SADR-0004 left unspecified**. SADR-0004 decides that\n GitHub is the operational source of truth and that each target repository keeps a\n `.shiki/` mirror. It says nothing about pull requests writing the mirror, does\n not name MergeGate, and does not make the mirror an input to a gate. Nothing in\n-ADR 0004's Decision or Consequences is amended, superseded or weakened: the\n+SADR-0004's Decision or Consequences is amended, superseded or weakened: the\n mirror still exists, still records the same artifacts, and pull request\n-boundaries remain the coordination surface. What this ADR settles is the question\n+boundaries remain the coordination surface. What this SADR settles is the question\n 0004 never reached \u2014 by what authority a pull request may write a mirror record \u2014\n and it answers it the way `.shiki/manifest.json` already declares\n (`pr_mutation: current-task-or-current-goal-only`).\n \n-It **amends ADR 0012**, whose Decision item 5 states that \"Loop-executed tasks\n+It **amends SADR-0012**, whose Decision item 5 states that \"Loop-executed tasks\n declare the `path:.shiki/**` lock, which covers every `.shiki` file the closeout\n stages ... so `files_outside_locks` is satisfied without per-file lock\n bookkeeping.\" That clause is replaced: the closeout's mirror writes are\n-authorized by record identity, not by a declared lock. ADR 0012's substantive\n+authorized by record identity, not by a declared lock. SADR-0012's substantive\n decision \u2014 that completion reaches `main` through a closeout pull request rather\n than local attestation \u2014 is preserved unchanged.\n \n-It is **complementary to ADR 0015**, not a substitute. ADR 0015 owns contract\n+It is **complementary to SADR-0015**, not a substitute. SADR-0015 owns contract\n immutability, the pre-dispatch gate, the Contract PR and the Scope Envelope. This\n-ADR owns what a lock means and who may write the mirror. The one ordering\n+SADR owns what a lock means and who may write the mirror. The one ordering\n constraint between them is that the lock-narrowing Spec Amendment must precede\n-ADR 0015 T1's freezing of the `locks` field.\n+SADR-0015 T1's freezing of the `locks` field.\n \n ## Alternatives Considered\n \n@@ -186,7 +186,7 @@\n the only writer that can register them may run. Migrating the Guardian signal to\n that store makes every registration PR fail closed to `needs_guardian` at every\n risk level, because the task file is by definition absent from the store, which\n-removes the low/medium autonomous path ADR 0008/0009 exists to provide. And it\n+removes the low/medium autonomous path SADR-0008/0009 exists to provide. And it\n leaves schemas, manifest, config, guardian policy, templates, migrations, plans,\n worktrees, runner records, runs and handoffs with no writer at all in a\n repository whose product is `.shiki`. Measured benefit on the frozen backlog:\n@@ -199,14 +199,14 @@\n namespace is not in fact ownership-partitioned \u2014 56 ledger entries carry no\n `task_id`, and one goal DAG is writable by 35 registered tasks. The correct\n expression of its insight is that locks do not govern the mirror at all, which is\n-what this ADR decides.\n+what this SADR decides.\n \n **Make the mirror a projection written only by a registration writer and a\n post-merge projection job.** Rejected. It requires a bypass actor with write\n access to the protected default branch; it makes its own master invariant\n readable from a manifest that pull requests may edit; it deletes both reconcile\n modes and leaves a corrupted mirror with no in-band repair path, contradicting\n-ADR 0004's consequence that mirror conflicts are repaired against GitHub state;\n+SADR-0004's consequence that mirror conflicts are repaired against GitHub state;\n and its content-derived ids are rejected by `validate_shiki`'s currently-green id\n pattern.\n \ndiff --git a/docs/adr/0017-bookkeeping-closeouts-do-not-inherit-risk.md b/docs/adr/SADR-0017-bookkeeping-closeouts-do-not-inherit-risk.md\nindex 554e3a0d526a72ca067108c5464cc68c3edcf174..b9676857a4713850e063af7c39c8e0b01ecb4c66\n--- a/docs/adr/0017-bookkeeping-closeouts-do-not-inherit-risk.md\n+++ b/docs/adr/SADR-0017-bookkeeping-closeouts-do-not-inherit-risk.md\n@@ -1,4 +1,4 @@\n-# ADR 0017: A Bookkeeping Closeout Does Not Inherit Its Task's Risk\n+# SADR-0017: A Bookkeeping Closeout Does Not Inherit Its Task's Risk\n \n ## Status\n \n@@ -6,7 +6,7 @@\n \n ## Context\n \n-ADR 0012 made the goal loop push a task's completion to the default branch through a\n+SADR-0012 made the goal loop push a task's completion to the default branch through a\n closeout pull request: after the implementation PR merges, the loop opens a second PR\n carrying `task.status = done`, `lock.state = released`, and \u2014 when the task completes\n its goal \u2014 `goal.status = complete` plus one scorecard report. Nothing else. The\n@@ -49,14 +49,14 @@\n 4. The task file's governance fields (`scope`, `non_goals`, `required_skills`,\n    `risk_level`, `locks`, `acceptance_checks`, `test_command`) are byte-identical to\n    the base snapshot. Only `status`, `expected_pr`, `closeout_pr`, `ledger_evidence`\n-   and `expected_branch` may differ \u2014 the ADR 0015 contract-immutability rule,\n+   and `expected_branch` may differ \u2014 the SADR-0015 contract-immutability rule,\n    unchanged.\n \n    `expected_branch` is in that set because a closeout is *by construction* on a\n    different branch from the implementation it closes, and the loop rewrites the\n    field accordingly. It is safe to let it differ because MergeGate independently\n    requires `expected_branch` to equal the PR's actual head ref, so a PR cannot set\n-   it to anything other than the branch it is really on. The first draft of this ADR\n+   it to anything other than the branch it is really on. The first draft of this SADR\n    omitted it, which made the exemption unreachable: every real closeout failed\n    condition 4 on a field the loop always changes. See PR #209.\n 5. The state transitions are exactly the terminal set: task `review` \u2192 `done`, lock\n@@ -113,12 +113,12 @@\n classification must be derived, not asserted.\n \n **Fold the closeout into the implementation PR** so no second PR exists. Rejected under\n-ADR 0012, which recorded why: a task file that is already `done` on the implementation\n+SADR-0012, which recorded why: a task file that is already `done` on the implementation\n branch short-circuits the loop's review \u2192 CCA \u2192 merge \u2192 done ordering into a local\n goal-completion before any merge happens.\n \n **Have the loop auto-merge closeouts with elevated credentials.** Rejected outright: that\n-is a runtime authoring its own approval, which ADR 0010 and the constitution's\n+is a runtime authoring its own approval, which SADR-0010 and the constitution's\n approval-record prohibition forbid. It would also be indistinguishable, in the evidence,\n from the forgery those rules exist to prevent.\n \ndiff --git a/docs/adr/0018-carried-guardian-approval-across-a-proven-base-sync.md b/docs/adr/SADR-0018-carried-guardian-approval-across-a-proven-base-sync.md\nindex 22358562da390ed8b45cd600da08a43d378b4680..e6e2907cefa776460af73ddf7d39db605f3c39b0\n--- a/docs/adr/0018-carried-guardian-approval-across-a-proven-base-sync.md\n+++ b/docs/adr/SADR-0018-carried-guardian-approval-across-a-proven-base-sync.md\n@@ -1,4 +1,4 @@\n-# ADR 0018: Carried Guardian Approval Across A Proven Base Sync\n+# SADR-0018: Carried Guardian Approval Across A Proven Base Sync\n \n ## Status\n \n@@ -34,7 +34,7 @@\n Guardian evaluator that consumes it (`shiki_guardian.evaluate_guardian_approval`,\n the `guardian_comment_carried` source) were built and tested in isolation. Neither\n does anything until a caller passes the flags that turn the carry on, so the\n-feature was invisible in CI. This ADR records the decision the two of them\n+feature was invisible in CI. This SADR records the decision the two of them\n implement, and this task wires it into the four CI invocations that judge Guardian\n approval.\n \ndiff --git a/docs/adr/README.md b/docs/adr/README.md\nindex c70e057a4520c36e042ab68c53cd4a21ef709b71..5da43856836e2ce6a150f8127766e2f88355ab4b\n--- a/docs/adr/README.md\n+++ b/docs/adr/README.md\n@@ -1,40 +1,41 @@\n-# Architecture Decision Records\n+# Decision Record Namespaces\n \n-This directory holds Shiki's Architecture Decision Records (ADRs): durable\n-records of hard-to-reverse platform decisions. ADRs are part of the Shiki source\n-of truth alongside GitHub state, the `.shiki/` mirror, and `CONTEXT.md`.\n+This directory holds two independent decision namespaces:\n \n-## How ADRs Work\n+- Shiki Architecture Decision Records (SADRs), named\n+  `SADR-NNNN-<short-slug>.md`, record hard-to-reverse Shiki platform decisions.\n+- Target Architecture Decision Records (ADRs), named\n+  `NNNN-<short-slug>.md`, belong to the target product.\n \n-- New ADRs are added as `docs/adr/NNNN-<short-slug>.md`, where `NNNN` is the next\n-  zero-padded sequence number.\n-- The **canonical list of ADRs is the set of files matching `docs/adr/*.md`** in\n-  this directory. Browse `docs/adr/*.md` for the authoritative, current set.\n-- Use [`template.md`](template.md) as the starting point for a new ADR.\n-- Record an ADR when a decision is hard to reverse, surprising without context,\n-  and a real tradeoff. Do not write an ADR for routine or easily reversible\n-  choices.\n+The same four-digit number may appear once in each namespace. A platform\n+`SADR-NNNN` citation resolves only to `SADR-NNNN-*.md`; a target `ADR NNNN`\n+citation resolves only to `NNNN-*.md`.\n \n-Using the `docs/adr/*.md` glob as the source of truth means other PRs can add an\n-ADR without editing this index file, avoiding merge contention on a\n-hand-maintained enumeration.\n+## How Decision Records Work\n \n-## Current ADRs (snapshot)\n-\n-The list below is a convenience snapshot and may lag behind the directory. When\n-in doubt, list `docs/adr/*.md`.\n+- New Shiki platform decisions use the next zero-padded SADR number.\n+- New target product decisions use the target's own ADR sequence.\n+- The canonical list is the files in `docs/adr/`; no hand-maintained index is\n+  authoritative.\n+- Use [`template.md`](template.md) as the starting point and choose the filename\n+  namespace that owns the decision.\n+- Record a decision when it is hard to reverse, surprising without context, and\n+  a real tradeoff. Do not write a decision record for routine or easily\n+  reversible choices.\n \n-- [0001 \u2014 Keep Shiki As The Platform Name](0001-keep-shiki-as-platform-name.md)\n-- [0002 \u2014 Use AGENTS As The Shared Agent Constitution](0002-use-agents-as-shared-constitution.md)\n-- [0003 \u2014 Require Skill Gate For MergeGate Readiness](0003-require-skill-gate-for-mergegate-readiness.md)\n-- [0004 \u2014 Use GitHub First With A Repository-Local Shiki Mirror](0004-use-github-first-with-shiki-mirror.md)\n-- [0005 \u2014 Use Codex As Front And Claude Code Action As The GitHub Runtime](0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md)\n-- [0006 \u2014 Separate Codex Implementation, CCA Completion, And MergeGate Authorization](0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md)\n+Using the directory as the source of truth lets independent changes add records\n+without contending on an enumerated index.\n \n-## Adding A New ADR\n+## Adding A Shiki SADR\n \n-1. Copy `template.md` to `docs/adr/NNNN-<short-slug>.md`.\n+1. Copy `template.md` to `docs/adr/SADR-NNNN-<short-slug>.md`.\n 2. Fill in the title, status, context, decision, and consequences.\n-3. Set the status to `Proposed`, then `Accepted` once the decision is approved.\n-4. Open a PR following `CONTRIBUTING.md`. You do not need to edit this index for\n-   the ADR to be discoverable via `docs/adr/*.md`.\n+3. Set the status to `Proposed`, then `Accepted` once the proper authority has\n+   approved the decision.\n+4. Open a PR following `CONTRIBUTING.md`.\n+\n+## Adding A Target ADR\n+\n+1. Copy `template.md` to `docs/adr/NNNN-<short-slug>.md` using the target's own\n+   sequence.\n+2. Cite it as `ADR NNNN`; do not prefix its filename with `SADR-`.\ndiff --git a/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md b/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\nnew file mode 100644\nindex 0000000000000000000000000000000000000000..7455ce1f3bed02ddddb851ac21a1d1a648c9aeba\n--- /dev/null\n+++ b/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\n@@ -0,0 +1,55 @@\n+# SADR-0019: Separate Shiki Platform SADRs From Target ADRs\n+\n+## Status\n+\n+Accepted\n+\n+## Context\n+\n+Shiki installs its platform decision records into `docs/adr/` in every target\n+repository. Shiki and its targets previously used the same numeric\n+`NNNN-<slug>.md` namespace. A target that already owned ADRs 0013 through 0018\n+therefore acquired two records for each number during installation, making an\n+`ADR NNNN` citation ambiguous. Renumbering target decisions would rewrite target\n+history, while renumbering Shiki decisions per target would destroy their stable\n+platform identity.\n+\n+Upgrades also need to remove the former Shiki-owned numeric paths without ever\n+mistaking a target-owned ADR for platform content. The install stamp already\n+records exact shipped paths and content digests, so it can prove ownership only\n+when both still match.\n+\n+## Decision\n+\n+Shiki platform decisions use the `SADR` namespace:\n+\n+- records are named `docs/adr/SADR-NNNN-<slug>.md`;\n+- live platform citations use `SADR-NNNN`;\n+- target product decisions retain `docs/adr/NNNN-<slug>.md` and `ADR NNNN`;\n+- the reference validator resolves the two namespaces independently, so the same\n+  four-digit number may exist once in each namespace.\n+\n+Before a forced upgrade writes anything, the installer inventories the exact\n+former numeric Shiki ADR paths. It may delete a legacy path only when the\n+existing install stamp contains that exact path and its recorded SHA-256 digest\n+matches the file currently on disk. All ownership blockers are reported\n+together and abort the upgrade before its first write. A non-forced install\n+never performs this cleanup.\n+\n+Historical `.shiki/` evidence remains unchanged because it records the namespace\n+that existed when that evidence was produced.\n+\n+## Consequences\n+\n+- Targets may use any ADR number without colliding with Shiki's platform SADRs.\n+- Fresh installs preserve target ADR bytes and install a stable SADR set.\n+- Forced upgrades fail closed when legacy ownership cannot be proven, and a\n+  successful upgrade refreshes the stamp without removed legacy paths.\n+- Current governance, packaging, workflow, prompt, script, and skill references\n+  must move with the platform records.\n+\n+## Alternatives Considered\n+\n+- Collision-only refusal leaves affected targets permanently unupgradable.\n+- Renumbering target ADRs mutates target-owned history.\n+- Per-target Shiki renumbering makes platform citations non-portable.\ndiff --git a/docs/adr/template.md b/docs/adr/template.md\nindex a92a97daddd0b00ab9dd626cc8d86942da2eafed..17dd79175b5fee2105935557f691b40cf9492942\n--- a/docs/adr/template.md\n+++ b/docs/adr/template.md\n@@ -1,14 +1,14 @@\n-# NNNN: <Short Decision Title>\n+# <SADR-NNNN or NNNN>: <Short Decision Title>\n \n ## Status\n \n-Proposed | Accepted | Superseded by ADR-NNNN\n+Proposed | Accepted | Superseded by SADR-NNNN or target ADR NNNN\n \n ## Context\n \n What is the situation, constraint, or force that requires a decision? Reference\n-relevant domain terms (`CONTEXT.md`), prior ADRs, and code reality. State the\n-problem, not the solution.\n+relevant domain terms (`CONTEXT.md`), prior Shiki SADRs or target ADRs, and code\n+reality. State the problem, not the solution.\n \n ## Decision\n \ndiff --git a/docs/agents/autonomous-loop-e2e-acceptance.md b/docs/agents/autonomous-loop-e2e-acceptance.md\nindex bbf84c6081987f159ac6ad9ecf3c82b7aa72e7e8..5c13c1d02a05a90b0dc46cbef7c95a9846d0c237\n--- a/docs/agents/autonomous-loop-e2e-acceptance.md\n+++ b/docs/agents/autonomous-loop-e2e-acceptance.md\n@@ -40,8 +40,8 @@\n 3. `gh auth status` is logged in with merge permission on the repo.\n 4. Branch protection + required checks are configured exactly as the goal-loop\n    required-check set expects (see `docs/agents/branch-protection-smoke.md`).\n-5. The repo `.shiki/policy` permits autonomous low/medium-risk merges (ADR\n-   0008/0009); the MergeGate policy check is the recorded-authority gate.\n+5. The repo `.shiki/policy` permits autonomous low/medium-risk merges\n+   (SADR-0008/0009); the MergeGate policy check is the recorded-authority gate.\n \n The operator's only actions are: register the task and start the loop. **No\n manual pre-clearing** means after `shiki loop run` starts, the operator does not\ndiff --git a/docs/agents/bootstrap-command.md b/docs/agents/bootstrap-command.md\nindex 05f7f203a52cf412b9f97084a6e131ed0f785afd..f7aa0b734dfebee4a3b139335b66ee3415f48713\n--- a/docs/agents/bootstrap-command.md\n+++ b/docs/agents/bootstrap-command.md\n@@ -189,7 +189,9 @@\n ```\n \n Use this only for tests, fixtures, or explicit local-only template inspection.\n-Use `--force` only when you intentionally want to overwrite existing target files.\n+Use `--force` only when you intentionally want to refresh the target template.\n+Legacy numeric Shiki decision cleanup is stamp-and-digest authorized before any\n+write; target ADR paths are not cleanup candidates.\n \n ## Slash Command\n \ndiff --git a/docs/agents/checklists.md b/docs/agents/checklists.md\nindex 4a7f03ade346f53a1c1d82981f3fd744f9ab4547..0c5ec6c53dc7197b7b7a7d18e667b325babd6c45\n--- a/docs/agents/checklists.md\n+++ b/docs/agents/checklists.md\n@@ -32,18 +32,18 @@\n | ID | Item | Blocking |\n | --- | --- | --- |\n | GD-01 | Plan was challenged against `CONTEXT.md`. | blocking for non-trivial Goals |\n-| GD-02 | Relevant ADRs were read or absence was recorded. | blocking for non-trivial Goals |\n+| GD-02 | Relevant SADRs and target ADRs were read or absence was recorded. | blocking for non-trivial Goals |\n | GD-03 | Ambiguous terms were resolved or converted to HITL blockers. | blocking |\n | GD-04 | Edge scenarios were considered. | advisory |\n | GD-05 | Code reality was checked when code could answer. | blocking when applicable |\n | GD-06 | New domain terms were added to `CONTEXT.md` when needed. | blocking when applicable |\n-| GD-07 | ADR was created or explicitly skipped using the ADR criteria. | blocking when applicable |\n+| GD-07 | A SADR or target ADR was created or explicitly skipped using the decision-record criteria. | blocking when applicable |\n \n ## CI \u2014 Context & Impact Checklist\n \n | ID | Item | Blocking |\n | --- | --- | --- |\n-| CI-01 | Relevant docs and ADRs identified. | blocking |\n+| CI-01 | Relevant docs, SADRs, and target ADRs identified. | blocking |\n | CI-02 | Relevant modules, interfaces, seams, callers, and tests identified. | blocking |\n | CI-03 | Dependencies and blocked-by relationships identified. | blocking |\n | CI-04 | Candidate locks identified. | blocking |\n@@ -62,7 +62,7 @@\n | PRD-04 | Implementation decisions are recorded without volatile code detail. | blocking |\n | PRD-05 | Testing decisions are recorded. | blocking |\n | PRD-06 | Out-of-scope items are explicit. | blocking |\n-| PRD-07 | Relevant glossary/ADR links are present when applicable. | blocking |\n+| PRD-07 | Relevant glossary/SADR/target-ADR links are present when applicable. | blocking |\n \n ## SF \u2014 Spec Freeze Checklist\n \n@@ -98,7 +98,7 @@\n | H-02 | Branch/worktree target is present. | blocking |\n | H-03 | Scope and non-goals are present. | blocking |\n | H-04 | Dependencies and locks are present. | blocking |\n-| H-05 | Relevant docs, ADRs, modules, and tests are referenced. | blocking |\n+| H-05 | Relevant docs, SADRs, target ADRs, modules, and tests are referenced. | blocking |\n | H-06 | Required skills are stated. | blocking |\n | H-07 | TDD expectations are stated. | blocking when behavior work |\n | H-08 | Acceptance criteria are included. | blocking |\ndiff --git a/docs/agents/codex-handoff.md b/docs/agents/codex-handoff.md\nindex ede468f47cb277f69a857e8c45df703e4ed0151d..6f97bdffb67e44d7edea2c71e01ea28933c4eb6c\n--- a/docs/agents/codex-handoff.md\n+++ b/docs/agents/codex-handoff.md\n@@ -46,7 +46,7 @@\n \n ## Context\n - Domain terms:\n-- Relevant ADRs:\n+- Relevant SADRs and target ADRs:\n - Relevant docs:\n - Relevant modules/interfaces/seams:\n - Similar tests/prior art:\ndiff --git a/docs/agents/completion-check-agent.md b/docs/agents/completion-check-agent.md\nindex 13e4c035db35025f4e3cdcc5adfb24314ac4d521..61bfa1218cf36ca605250c91919e620f33238aa0\n--- a/docs/agents/completion-check-agent.md\n+++ b/docs/agents/completion-check-agent.md\n@@ -32,7 +32,7 @@\n - `AGENTS.md`\n - `CLAUDE.md`\n - `CONTEXT.md`\n-- relevant ADRs\n+- relevant SADRs and target ADRs\n - parent Goal issue\n - PRD issue or document\n - task issue\ndiff --git a/docs/agents/decision-control.md b/docs/agents/decision-control.md\nindex 7d8ee5ab03a5ccbcbd04056ae61de03785a2a9c9..8c94ecda00fffaa0e92c325ab83aa9776bd1dac1\n--- a/docs/agents/decision-control.md\n+++ b/docs/agents/decision-control.md\n@@ -11,7 +11,7 @@\n | Decision | Owner | When | Required inputs | Output | Enforcement surface |\n | --- | --- | --- | --- | --- | --- |\n | Is the Goal clear enough to plan? | Planner + Guardian when needed | Goal intake | User request, existing docs, risk signals | Goal readiness verdict | Goal checklist, issue template, ledger |\n-| Must `grill-with-docs` run? | Planner / Skill Gate | Before PRD or issue split | Ambiguity, domain terms, ADR conflicts, unclear boundaries | Skill invocation or explicit skip rationale | Skill Gate checklist |\n+| Must `grill-with-docs` run? | Planner / Skill Gate | Before PRD or issue split | Ambiguity, domain terms, SADR/target-ADR conflicts, unclear boundaries | Skill invocation or explicit skip rationale | Skill Gate checklist |\n | Is a PRD required? | Planner | After Goal context stabilizes | Goal, `grill-with-docs` output, Context & Impact output | PRD required / not required | PRD checklist, CCA evidence check |\n | Can the spec be frozen? | Operator only | End of Requirements Definition, before issue split | PRD/requirements summary, scope inventory (SF-02), open-question list | `spec_freeze` block in the plan + ledger evidence | `require_grilled_plan` (plan ingest/run), SF checklist |\n | Can a frozen spec change? | Operator only (Spec Amendment) | When implementation contradicts the frozen spec | Contested decisions, scoped re-grill result | recorded amendment + re-stamped freeze | SF checklist, ledger evidence |\ndiff --git a/docs/agents/domain.md b/docs/agents/domain.md\nindex 258da8ea81e87ac0a6b0b5b5e5c2fde980eaabb7..d57b0cf17ff3e1fa9e6e5885372bf083d8295421\n--- a/docs/agents/domain.md\n+++ b/docs/agents/domain.md\n@@ -5,7 +5,9 @@\n ## Layout\n \n - `CONTEXT.md` defines Shiki language. Keep it implementation-free.\n-- `docs/adr/` records hard-to-reverse decisions.\n+- `docs/adr/SADR-NNNN-*.md` records Shiki platform decisions.\n+- `docs/adr/NNNN-*.md` records target product decisions without sharing the\n+  SADR namespace.\n - `AGENTS.md` is the runtime-neutral agent constitution.\n - `CLAUDE.md` is the Claude Code wrapper around `AGENTS.md`.\n - `.shiki/` is the Target Repository mirror for Goals, PRDs, plans, Task DAGs, contracts, locks, worktree records, ledger entries, CCA verdicts, repair packets, reports, and handoffs.\n@@ -15,6 +17,6 @@\n \n - Use Shiki terms exactly when they exist.\n - Add glossary terms to `CONTEXT.md` only when the term is part of the domain, not a generic programming concept.\n-- Create ADRs only for hard-to-reverse, surprising, tradeoff-based decisions.\n+- Create SADRs or target ADRs only for hard-to-reverse, surprising, tradeoff-based decisions.\n - Keep product-specific language in the Target Repository, not in Shiki Template docs.\n-- If an ADR conflicts with a proposed implementation, surface the conflict before editing.\n+- If a SADR or target ADR conflicts with a proposed implementation, surface the conflict before editing.\ndiff --git a/docs/agents/execution-control.md b/docs/agents/execution-control.md\nindex ad8ee3cdb36be56fedc1f46408ca92b387d74047..bc63c3a73661d12b567016ba299f1a6b66bf9088\n--- a/docs/agents/execution-control.md\n+++ b/docs/agents/execution-control.md\n@@ -63,7 +63,7 @@\n | Branch name | Orchestrator | Orchestrator | Before implementer dispatch | branch policy | branch ref in task/ledger |\n | Lock reservation | Context & Impact | MergeGate | Before dispatch and before merge | `.shiki/locks/`, labels, PR metadata | lock id, owner, scope |\n | CI profile | Planner | MergeGate | During issue triage, rechecked at PR | labels, `.shiki/policy.yaml`, workflow matrix | required check list |\n-| Required status checks | Repo maintainer / Guardian | GitHub branch protection / rulesets | Repository setup, updated via ADR | GitHub settings | branch/ruleset config, ADR |\n+| Required status checks | Repo maintainer / Guardian | GitHub branch protection / rulesets | Repository setup, updated via target ADR | GitHub settings | branch/ruleset config, target ADR |\n | CCA judgment | CCA | GitHub required check / MergeGate | Every non-draft PR update | GitHub Action check | structured CCA verdict |\n | Repair assignment | CCA | MergeGate | After failed CCA/CI/review | repair packet, issue/PR comment | repair id and attempt count |\n | Merge readiness | MergeGate | GitHub branch protection / Guardian if needed | After CCA complete | required checks, review rules, rulesets | mergegate check output |\ndiff --git a/docs/agents/guardian-policy.md b/docs/agents/guardian-policy.md\nindex 4147c7adf05a47adb4fd24ada1a1c2e1f2ba6d2d..a20d9cc6c00a569bb057b9ca8d6b51c7fc4a9847\n--- a/docs/agents/guardian-policy.md\n+++ b/docs/agents/guardian-policy.md\n@@ -98,8 +98,8 @@\n `origin/<default_branch>` and reuses each job's existing base fetch; a shallow\n (`--depth`) or second base fetch would make the proof unresolvable and silently\n disable the carry, so neither is permitted. CCA reads `guardian_comment_carried` as\n-a recorded authority in the deterministic signal and does not re-derive it. See ADR\n-0018 for the full decision, the rejected diff/patch-equality and path-disjointness\n+a recorded authority in the deterministic signal and does not re-derive it. See\n+SADR-0018 for the full decision, the rejected diff/patch-equality and path-disjointness\n alternatives, and the accepted residual.\n \n ## External AI Guardian Review (SADR-0010)\ndiff --git a/docs/agents/implementation-policy.md b/docs/agents/implementation-policy.md\nindex c754144a9fb5bf4a38aa82a60e1670c59e626b44..85c37281bcad196c6b7b02343faf00b03cd9c57a\n--- a/docs/agents/implementation-policy.md\n+++ b/docs/agents/implementation-policy.md\n@@ -31,10 +31,13 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n ## Non-Negotiables\n \n 1. **Planning precedes implementation.** Non-trivial Goals start with `grill-with-docs` and Context & Impact.\n-2. **Domain language is controlled.** Terms must be checked against `CONTEXT.md`; hard-to-reverse tradeoffs must be checked against ADRs.\n+2. **Domain language is controlled.** Terms must be checked against `CONTEXT.md`; hard-to-reverse tradeoffs must be checked against Shiki SADRs and target ADRs.\n 3. **PRD precedes issue decomposition.** `to-prd` is used once the context is settled enough to become durable intent.\n 4. **Issues are vertical slices.** `to-issues` must produce independently grabbable tracer-bullet tasks, not horizontal layer tickets.\n 5. **Triage controls dispatch.** Only `ready-for-agent` / AFK tasks may be assigned to Codex without human input.\n@@ -95,19 +98,19 @@\n Process:\n \n - Challenge terms against `CONTEXT.md`.\n-- Check ADRs before reopening decisions.\n+- Check SADRs and target ADRs before reopening decisions.\n - Inspect code when code can answer a question.\n - Ask one question at a time when the operator is available.\n - Provide a recommended answer with each question.\n - Record resolved terms in `CONTEXT.md`.\n-- Create ADRs only for hard-to-reverse, surprising, tradeoff-based decisions.\n+- Create SADRs or target ADRs only for hard-to-reverse, surprising, tradeoff-based decisions.\n - Convert unresolved load-bearing questions into HITL blockers.\n \n Outputs:\n \n - Resolved design-tree decisions.\n - Updated glossary terms when needed.\n-- ADRs when justified.\n+- SADRs or target ADRs when justified.\n - HITL blockers when decisions remain unresolved.\n \n ## Phase 3 \u2014 Context & Impact\n@@ -117,7 +120,7 @@\n \n Outputs must include:\n \n-- Relevant documents and ADRs.\n+- Relevant documents, SADRs, and target ADRs.\n - Relevant modules, interfaces, seams, callers, and tests.\n - Candidate locks.\n - Dependencies.\n@@ -140,7 +143,7 @@\n - Testing decisions.\n - Out of scope.\n - Further notes.\n-- Links to relevant Goal, domain terms, and ADRs.\n+- Links to relevant Goal, domain terms, SADRs, and target ADRs.\n \n Phase 4 ends with Spec Freeze: the operator explicitly approves the PRD, the\n plan records a `spec_freeze` block (status, approved_by, source), and the SF\n@@ -240,7 +243,7 @@\n - Dependencies and locks.\n - Risk and Guardian requirements.\n - Ledger completeness.\n-- Documentation and ADR updates.\n+- Documentation and SADR/target-ADR updates.\n - Scope drift.\n \n CCA emits one of:\n@@ -301,7 +304,7 @@\n | Evidence failure | Behavior might be done, but proof is missing. | Add checks, PR evidence, ledger entry, or artifacts. |\n | Scope drift | PR changes unrelated behavior. | Revert unrelated changes or split into new Goal. |\n | Dependency block | Upstream task or lock not complete. | Block until dependency resolves. |\n-| Architecture block | Decision needs ADR or deep-module discussion. | `grill-with-docs` or `improve-codebase-architecture`. |\n+| Architecture block | Decision needs a SADR, target ADR, or deep-module discussion. | `grill-with-docs` or `improve-codebase-architecture`. |\n | Risk block | Security/production/policy change needs human approval. | Guardian decision. |\n \n ## Phase 5.5 \u2014 Execution Decision Control\ndiff --git a/docs/agents/runtime-auth-model.md b/docs/agents/runtime-auth-model.md\nindex 6a2e1a47e81ea89b5c68a210ec07772a9374babc..3c54748d24233dbd0ba8473950282547ef4ce935\n--- a/docs/agents/runtime-auth-model.md\n+++ b/docs/agents/runtime-auth-model.md\n@@ -148,7 +148,7 @@\n \n API-key based runners may be added by a target repository as an explicit extension. They are not the default Shiki template path.\n \n-If a repo opts into API-key mode, record the exception in an ADR and update the repo's `AGENTS.md`, `.shiki/config.yaml`, and workflow permissions.\n+If a repo opts into API-key mode, record the target-specific exception in a target ADR and update the repo's `AGENTS.md`, `.shiki/config.yaml`, and workflow permissions.\n \n ## GitHub Actions Runtime Compatibility\n \ndiff --git a/docs/agents/skill-gate.md b/docs/agents/skill-gate.md\nindex c50398b0d7c60a8598f2a5c88a10015730dced13..3acffe6444617bb1d54b77d4b4708158ec735bc6\n--- a/docs/agents/skill-gate.md\n+++ b/docs/agents/skill-gate.md\n@@ -9,7 +9,7 @@\n | Skill | Trigger | Expected output |\n | --- | --- | --- |\n | `setup-matt-pocock-skills` | First configuring a repo for these skills, or when issue tracker, triage labels, or domain docs are missing. | `docs/agents/` configuration and an `Agent skills` block in `AGENTS.md` or `CLAUDE.md`. |\n-| `grill-with-docs` | Ambiguous plan, unclear terminology, boundary decision, tradeoff, or ADR-worthy design choice. | Clarified decision tree, recommended answers, updates to `CONTEXT.md` or ADRs when decisions crystallize. |\n+| `grill-with-docs` | Ambiguous plan, unclear terminology, boundary decision, tradeoff, or decision-record-worthy design choice. | Clarified decision tree, recommended answers, updates to `CONTEXT.md`, SADRs, or target ADRs when decisions crystallize. |\n | `zoom-out` | Agent lacks a higher-level architectural map. | Map of relevant modules, callers, seams, and domain concepts. |\n | `to-prd` | Settled context should become a PRD. | PRD published to the issue tracker with problem, solution, user stories, decisions, testing, and out-of-scope notes. |\n | `to-issues` | Goal, PRD, or plan must become implementation tickets. | Vertical-slice issues with acceptance criteria, dependencies, and AFK/HITL classification. |\n@@ -17,7 +17,7 @@\n | `tdd` | Feature work or bug fix has observable behavior that can be specified with tests. | Red-green-refactor loop through public interfaces, one vertical slice at a time. |\n | `code-review` | The TDD loop is green and the implementer is about to create or update a PR. Default for every implementation task. | Pre-PR self-review of the task diff with fixes applied on the task branch; findings and resolutions recorded as a ledger entry naming the skill and a `## Pre-PR code review` PR body section. |\n | `diagnose` | Hard bug, failing check, regression, flaky behavior, or performance problem. | Reproduction loop, ranked hypotheses, instrumentation, fix, regression test, post-mortem. |\n-| `improve-codebase-architecture` | Structural friction, testability, AI-navigability, deep-module opportunity, or refactor request. | Architecture candidates and deepening recommendations informed by `CONTEXT.md` and ADRs. |\n+| `improve-codebase-architecture` | Structural friction, testability, AI-navigability, deep-module opportunity, or refactor request. | Architecture candidates and deepening recommendations informed by `CONTEXT.md`, SADRs, and target ADRs. |\n | `prototype` | A throwaway logic or UI prototype is needed to answer a design question. | Clearly marked disposable prototype and captured decision. |\n \n ## MergeGate Interaction\ndiff --git a/docs/operations.md b/docs/operations.md\nindex cf1f452d4c589e38ccd58553bda0ad8684b51eb7..ace48a90ffbec9e5c0a7113bef16a4858bc44b3f\n--- a/docs/operations.md\n+++ b/docs/operations.md\n@@ -89,6 +89,16 @@\n      target history, brought forward by `shiki migrate`, not a template.\n    - **Everything else** (CLI scripts, workflows, schemas, docs) is overwritten.\n \n+   A forced upgrade also migrates the former numeric Shiki decision paths to\n+   `SADR-NNNN-*`. Before the first write, the installer inventories the exact\n+   legacy Shiki paths and authorizes each deletion only when the existing\n+   `.shiki/install-stamp.json` names that path and its SHA-256 digest still\n+   matches the file. An absent, unreadable, malformed, incomplete, or\n+   mismatching stamp reports every blocked path and stops with no filesystem\n+   change. Target-owned `NNNN-*` ADRs are not cleanup candidates. A non-force\n+   install deletes nothing and retains any still-matching legacy stamp entries\n+   so a later forced upgrade can prove ownership.\n+\n    If the target has pending migrations, `--force` refuses and writes nothing \u2014\n    apply migrations first (step 3), then re-run. The run ends with a single\n    summary of every `.new` written, so a half-upgrade is visible.\ndiff --git a/docs/releasing.md b/docs/releasing.md\nindex 10d73828e7dbd64e9d9f6b83bc84d010537ce62a..aafeee8c4d441e1e427efee2ce4f81a647f020e1\n--- a/docs/releasing.md\n+++ b/docs/releasing.md\n@@ -67,7 +67,7 @@\n CLI in `scripts/`. There are no third-party Python runtime dependencies. The CLI\n is intentionally **not** exposed as a `[project.scripts]` console entry point\n because `scripts/shiki.py` is a dependency-free shim and `scripts/` is not\n-packaged as an importable distribution (see the ADR for the rationale).\n+packaged as an importable distribution (see SADR-0007 for the rationale).\n \n Supported install paths:\n \n@@ -155,6 +155,15 @@\n    [docs/operations.md](operations.md#the-new-convention). If the target has\n    pending migrations, `--force` refuses and writes nothing; apply migrations\n    (step 2) first, then re-run.\n+\n+   The SADR namespace upgrade has an additional pre-write gate: each former\n+   numeric Shiki decision path is removed only when the existing install stamp\n+   contains that exact path and a digest matching its current bytes. The\n+   installer aggregates absent/unreadable/malformed-stamp, missing-digest, and\n+   digest-mismatch blockers before raising, so refusal leaves the target tree\n+   byte-identical. Numeric target ADRs are preserved. A successful forced\n+   upgrade installs and stamps `SADR-*` paths and drops the removed legacy paths\n+   from the refreshed stamp.\n 2. Apply repository-local `.shiki/` state migrations with the migrate command:\n \n    ```bash\ndiff --git a/docs/usage.md b/docs/usage.md\nindex b83407ee3970dd395dd893010bc5dbe8bad26051..a130ef6e40dfd8169a5bc701339351097df1a09f\n--- a/docs/usage.md\n+++ b/docs/usage.md\n@@ -67,8 +67,10 @@\n shiki install-target /path/to/target-repo --local-only\n ```\n \n-Use `--force` only when you intentionally want to overwrite existing target\n-files.\n+Use `--force` only when you intentionally want to refresh the target template.\n+For the SADR namespace migration, `--force` deletes only exact legacy Shiki\n+decision paths whose existing install-stamp digest still matches; any ownership\n+blocker stops before the first write. Numeric target ADRs are never candidates.\n \n ## `shiki bootstrap-platform` \u2014 publish the Shiki platform repo\n \ndiff --git a/scripts/guardian_approval_signal.py b/scripts/guardian_approval_signal.py\nindex d059f92b2c82aa2b5d6ca421365537a87886c1cb..16924892714f4f5b9303c0ef0e325a7e70075630\n--- a/scripts/guardian_approval_signal.py\n+++ b/scripts/guardian_approval_signal.py\n@@ -199,8 +199,8 @@\n ) -> Any:\n     \"\"\"Evaluate SADR-0015 Contract Approval for the CCA Guardian signal.\n \n-    The SAME OR that MergeGate applies must reach this deterministic signal: ADR\n-    0015 is explicit that without it, a contract-approved PR still returns\n+    The SAME OR that MergeGate applies must reach this deterministic signal:\n+    SADR-0015 is explicit that without it, a contract-approved PR still returns\n     ``needs_guardian``, the CCA job fails, and the MergeGate policy check never\n     runs \u2014 the gate this change moves would never be reached. This resolves the\n     task the SAME way MergeGate does (the first Shiki task id in the PR body),\ndiff --git a/scripts/shiki_contract_approval.py b/scripts/shiki_contract_approval.py\nindex 941bcb96fdd8b9a85bc0c4f0f04d5dad9d04839c..4a4b418f6527413161db82670bec857a043360e2\n--- a/scripts/shiki_contract_approval.py\n+++ b/scripts/shiki_contract_approval.py\n@@ -142,7 +142,7 @@\n     changed_files_status: Any,\n     registration: Any,\n ) -> ContractApprovalResult:\n-    \"\"\"Whether this PR carries an SADR-0015 Contract Approval.\n+    \"\"\"Whether this PR carries a SADR-0015 Contract Approval.\n \n     The four conditions are evaluated IN ORDER, each failure recorded as a\n     distinct reason string:\ndiff --git a/scripts/shiki_guardian.py b/scripts/shiki_guardian.py\nindex a4ecb79bbda55fd5c8b06adfae2eb99760a03c45..4dd99bbe714e26f421fa19abac7a34ae1b195ecc\n--- a/scripts/shiki_guardian.py\n+++ b/scripts/shiki_guardian.py\n@@ -12,7 +12,7 @@\n # The carry re-derives its proof from git in-process on every judging invocation;\n # it is never read from an artifact, so there is nothing to go stale or be forged.\n # shiki_sync_proof is the ONLY guardian-family module that runs git, and every one\n-# of its invocations is read-only and local (ADR: prove-from-git base sync).\n+# of its invocations is read-only and local (SADR: prove-from-git base sync).\n from shiki_sync_proof import verify_pure_base_sync\n \n GUARDIAN_POLICY_PATH = \".shiki/guardian-policy.json\"\ndiff --git a/scripts/shiki_guardian_review.py b/scripts/shiki_guardian_review.py\nindex dd0f104d457899e0ba5ff1e162011c1e90b6f92b..e10ef58c046324e7ea3a3392feb3048ca77414ce\n--- a/scripts/shiki_guardian_review.py\n+++ b/scripts/shiki_guardian_review.py\n@@ -159,7 +159,7 @@\n     docs_only = _is_docs_only(changed_files)\n     if docs_only and matched == 0:\n         focus.append(\n-            \"Docs/ADR-only change: confirm the change is genuinely documentation/decision-only, introduces no behavior or contract drift, and stays consistent with the constitution and existing ADRs.\"\n+            \"Docs/decision-record-only change: confirm the change is genuinely documentation/decision-only, introduces no behavior or contract drift, and stays consistent with the constitution and existing SADRs and target ADRs.\"\n         )\n \n     # Conservative augmentation: unknown (no category and not docs-only), mixed\ndiff --git a/scripts/shiki_installer.py b/scripts/shiki_installer.py\nindex be064ab7a29ff6836f7f7a1d70500a43e0d11d7b..9fd8eefd137ce7003dad5c0dcb2e48e2cd47ab32\n--- a/scripts/shiki_installer.py\n+++ b/scripts/shiki_installer.py\n@@ -118,6 +118,7 @@\n # what lets a target carry it without a governance-schema change.\n INSTALL_STAMP_PATH = \".shiki/install-stamp.json\"\n INSTALL_STAMP_VERSION = 1\n+LEGACY_SHIKI_SADR_MAX = 18\n \n # Under --force the shipped surface splits three ways, so nothing is lost and\n # nothing is silently stale.\n@@ -237,9 +238,98 @@\n         )\n \n \n+def legacy_shiki_adr_paths() -> tuple[str, ...]:\n+    \"\"\"Former numeric paths for Shiki SADR-0001 through SADR-0018.\n+\n+    The authoritative slugs come from the current SADR filenames. SADR-0019 is\n+    the namespace decision itself and never shipped under a numeric legacy path.\n+    \"\"\"\n+    paths: list[str] = []\n+    for sadr in sorted((ROOT / \"docs\" / \"adr\").glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\")):\n+        number = sadr.name.split(\"-\", 2)[1]\n+        if int(number) <= LEGACY_SHIKI_SADR_MAX:\n+            paths.append(f\"docs/adr/{sadr.name.removeprefix('SADR-')}\")\n+    return tuple(paths)\n+\n+\n+def preflight_legacy_sadr_cleanup(target: Path, *, force: bool) -> tuple[str, ...]:\n+    \"\"\"Authorize exact legacy Shiki ADR removals before the first write.\n+\n+    A non-force install never removes existing files. A forced install needs no\n+    stamp when no legacy Shiki path is present. When legacy paths do exist, each\n+    must be named in a readable install stamp and its current bytes must match\n+    the recorded digest.\n+    \"\"\"\n+    if not force:\n+        return ()\n+    present = tuple(\n+        relative\n+        for relative in legacy_shiki_adr_paths()\n+        if (target / relative).exists() or (target / relative).is_symlink()\n+    )\n+    if not present:\n+        return ()\n+\n+    stamp_path = target / INSTALL_STAMP_PATH\n+    try:\n+        raw = stamp_path.read_text(encoding=\"utf-8\")\n+        stamp = json.loads(raw)\n+    except FileNotFoundError:\n+        detail = \"install stamp is absent\"\n+        blockers = [f\"{relative}: {detail}\" for relative in present]\n+    except (OSError, UnicodeDecodeError) as error:\n+        detail = f\"install stamp is unreadable ({error})\"\n+        blockers = [f\"{relative}: {detail}\" for relative in present]\n+    except json.JSONDecodeError as error:\n+        detail = f\"install stamp is malformed ({error})\"\n+        blockers = [f\"{relative}: {detail}\" for relative in present]\n+    else:\n+        if not isinstance(stamp, dict):\n+            blockers = [\n+                f\"{relative}: install stamp is malformed (expected an object)\"\n+                for relative in present\n+            ]\n+        else:\n+            digests = stamp.get(\"digests\")\n+            blockers = []\n+            if not isinstance(digests, dict):\n+                blockers.extend(\n+                    f\"{relative}: install stamp has no digest for this path\"\n+                    for relative in present\n+                )\n+            else:\n+                for relative in present:\n+                    expected = digests.get(relative)\n+                    if not isinstance(expected, str) or not expected:\n+                        blockers.append(\n+                            f\"{relative}: install stamp has no digest for this path\"\n+                        )\n+                        continue\n+                    try:\n+                        actual = _sha256_file(target / relative)\n+                    except OSError as error:\n+                        blockers.append(\n+                            f\"{relative}: legacy path is unreadable ({error})\"\n+                        )\n+                        continue\n+                    if actual != expected:\n+                        blockers.append(\n+                            f\"{relative}: digest mismatch (stamp {expected}, current {actual})\"\n+                        )\n+\n+    if blockers:\n+        joined = \"\\n\".join(f\"- {blocker}\" for blocker in blockers)\n+        raise ShikiError(\n+            \"legacy Shiki ADR cleanup is not authorized:\\n\"\n+            f\"{joined}\\nNo files were written.\"\n+        )\n+    return present\n+\n+\n def install_template(target: Path, *, force: bool, validate: bool) -> list[NewFileNote]:\n     # Preconditions first: never rewrite the tree of a target that cannot be\n     # upgraded cleanly (see refuse_pending_migrations).\n+    legacy_cleanup = preflight_legacy_sadr_cleanup(target, force=force)\n     refuse_pending_migrations(target)\n \n     # Resolve the target's required CODEOWNERS owner once from its\n@@ -250,6 +340,10 @@\n     # verbatim. See ``codeowners_required_owner``.\n     codeowners_owner = codeowners_required_owner(target)\n \n+    for relative in legacy_cleanup:\n+        (target / relative).unlink()\n+        info(f\"removed stamped legacy Shiki ADR: {target / relative}\")\n+\n     notes: list[NewFileNote] = []\n     for relative in TEMPLATE_PATHS:\n         source = ROOT / relative\n@@ -647,6 +741,22 @@\n     digests = target_stamp_digests(target)\n     existing = read_install_stamp(target)\n     if existing is not None:\n+        # A non-force install deliberately leaves legacy Shiki ADRs in place.\n+        # Preserve only still-matching ownership proof so a later forced\n+        # upgrade can authorize cleanup; never carry a missing or stale digest.\n+        existing_digests = existing.get(\"digests\")\n+        if isinstance(existing_digests, dict):\n+            for relative in legacy_shiki_adr_paths():\n+                expected = existing_digests.get(relative)\n+                path = target / relative\n+                if not isinstance(expected, str) or not path.is_file():\n+                    continue\n+                try:\n+                    actual = _sha256_file(path)\n+                except OSError:\n+                    continue\n+                if actual == expected:\n+                    digests[relative] = actual\n         if _incoming_is_older(commit, existing.get(\"platform_commit\")):\n             warn(\n                 \"kept existing install stamp: incoming install \"\ndiff --git a/scripts/shiki_loop.py b/scripts/shiki_loop.py\nindex 3611641da8aeb5038b1da90e3114b09a473ec6d8..84caa491b9cad0edc4176f209c13e9221e975681\n--- a/scripts/shiki_loop.py\n+++ b/scripts/shiki_loop.py\n@@ -424,7 +424,7 @@\n         # unresolvable CCA verdict has already produced its terminal stop and is never\n         # laundered into a head-moving sync (SADR-0010). It also never fires while the\n         # policy (Guardian) gate is red \u2014 for ANY reason, not only when it is the sole\n-        # red check \u2014 so a head-bound Guardian approval, or an SADR-0015\n+        # red check \u2014 so a head-bound Guardian approval, or a SADR-0015\n         # contract-approval carry that base movement defeated (the policy gate compares\n         # base and head task governance directly), is never silently restored by\n         # adopting base wholesale. It is bounded by a durable per-task counter so the\ndiff --git a/scripts/shiki_tasks.py b/scripts/shiki_tasks.py\nindex aec3809f24b4a0c8c96fc50971cff9ae9ad30381..d531052e9d9717058d4631483cefab44c26a65c1\n--- a/scripts/shiki_tasks.py\n+++ b/scripts/shiki_tasks.py\n@@ -821,7 +821,7 @@\n         \"prompt\": prompt,\n         \"entry_skill\": \"grill-with-docs\",\n         \"required_next_steps\": [\n-            \"Run grill-with-docs until terminology, boundaries, risks, and ADR-worthy decisions are settled.\",\n+            \"Run grill-with-docs until terminology, boundaries, risks, and decision-record-worthy choices are settled.\",\n             \"Obtain the operator's explicit approval of the PRD/requirements (Spec Freeze).\",\n             \"Write a machine-readable plan JSON with grill_with_docs.status=complete and spec_freeze.status=frozen.\",\n             \"Run shiki plan ingest --plan-file PLAN.json.\",\ndiff --git a/scripts/test_shiki_goal_loop.sh b/scripts/test_shiki_goal_loop.sh\nindex 753c7b516e10f0be3bc92a4bd0eb1fa699776ae6..8f3820a0a079b3044424cfe1a6c6b7fe68896e67\n--- a/scripts/test_shiki_goal_loop.sh\n+++ b/scripts/test_shiki_goal_loop.sh\n@@ -231,8 +231,8 @@\n \n # High/critical risk merges autonomously when all required checks are green:\n # the \"MergeGate policy check\" required check IS the Guardian gate (it enforces\n-# guardian-policy.json \u2014 human approval OR external AI guardian review, ADR\n-# 0010), so green checks mean a recorded authority approved. The fake gh returns\n+# guardian-policy.json \u2014 human approval OR external AI guardian review,\n+# SADR-0010), so green checks mean a recorded authority approved. The fake gh returns\n # green checks, simulating that the Guardian gate passed.\n python3 \"$ROOT/scripts/shiki.py\" goal create --target \"$TARGET\" --title \"Guardian gate\" --outcome \"High risk merges via policy gate\" >/tmp/shiki-goal-loop-guardian-goal.json\n GGOAL=\"$(json_get /tmp/shiki-goal-loop-guardian-goal.json goal_id)\"\ndiff --git a/tests/test_adr_references.py b/tests/test_adr_references.py\nindex 51706329c797d5dd9b47e082a333fa83b261d482..d005da4f771f5e8085ec22d4b787980e68f5ae8c\n--- a/tests/test_adr_references.py\n+++ b/tests/test_adr_references.py\n@@ -1,8 +1,8 @@\n-\"\"\"Every ``ADR ####`` citation must resolve to exactly one decision record.\n+\"\"\"Every target ADR and Shiki SADR citation resolves in its own namespace.\n \n Background. ``scripts/mergegate_check.py`` justifies ``is_bookkeeping_closeout``\n-with \"ADR 0017\" nine times and ``scripts/guardian_approval_signal.py`` cites\n-0016/0017 seven times, yet for a stretch the numbering in ``docs/adr/`` stopped\n+with \"SADR-0017\" nine times and ``scripts/guardian_approval_signal.py`` cites\n+SADR-0016/SADR-0017 seven times, yet for a stretch the numbering in ``docs/adr/`` stopped\n at 0015: the authoring commits lived on a coordinator branch and no task ever\n declared ``docs/adr/...`` in its locks, so nothing carried the records to the\n default branch. The stated basis of an exemption that merged seven PRs with zero\n@@ -11,11 +11,12 @@\n and the directory drifted with no signal.\n \n This is the same unbound-pair shape behind other silent failures: a reference\n-and its target with nothing asserting they agree. This test binds them. It scans\n-every ``scripts/*.py`` (plus ``docs/**/*.md`` and the root ``*.md`` governance\n-files, which are cheap) for the ``ADR ####`` pattern and asserts each cited\n-number resolves to exactly one ``docs/adr/####-*.md``. The converse is\n-deliberately NOT required: an ADR may exist without being cited.\n+and its target with nothing asserting they agree. This test binds them. Target\n+``ADR NNNN`` citations resolve only to ``docs/adr/NNNN-*.md``; Shiki\n+``SADR-NNNN`` citations resolve only to ``docs/adr/SADR-NNNN-*.md``. The scan\n+covers current governance docs, scripts, workflows, prompts, packaging metadata,\n+and repo-local Codex guidance while deliberately excluding historical\n+``.shiki`` evidence. The converse is not required: a record may be uncited.\n \n The suite is dependency free: standard-library ``unittest`` only.\n \"\"\"\n@@ -31,17 +32,18 @@\n \n REPO_ROOT = Path(__file__).resolve().parents[1]\n \n-# \"ADR 0016\" -- a four-digit citation. The trailing \\b keeps a five-digit token\n-# from being read as a truncated four-digit number.\n-_CITATION = re.compile(r\"\\bADR (\\d{4})\\b\")\n-# \"0016-locks-bound-product-paths.md\" -- the leading four digits name the record.\n-_ADR_FILENAME = re.compile(r\"^(\\d{4})-.*\\.md$\")\n+# ``ADR 0016`` / ``SADR-0016`` -- a namespace plus four-digit citation. The\n+# trailing ``\\b`` keeps a five-digit token from being read as a truncated one.\n+_CITATION = re.compile(r\"\\b(S?ADR)[ -](\\d{4})\\b\")\n+# Target ADRs retain the numeric filename; Shiki platform decisions use SADR.\n+_DECISION_FILENAME = re.compile(r\"^(?:(SADR)-)?(\\d{4})-.*\\.md$\")\n \n \n class Violation(NamedTuple):\n     \"\"\"A citation that does not resolve to exactly one decision record.\"\"\"\n \n     citing_file: str  # repo-relative path of the file that cites the ADR\n+    namespace: str  # \"ADR\" for target decisions; \"SADR\" for Shiki decisions\n     number: str  # the cited four-digit number, e.g. \"0016\"\n     line: int  # 1-based line of the citation\n     reason: str  # \"missing\" (zero matches) or \"ambiguous\" (two or more)\n@@ -49,44 +51,59 @@\n \n     @property\n     def expected_path(self) -> str:\n-        return f\"docs/adr/{self.number}-*.md\"\n+        prefix = \"SADR-\" if self.namespace == \"SADR\" else \"\"\n+        return f\"docs/adr/{prefix}{self.number}-*.md\"\n \n     def describe(self) -> str:\n         \"\"\"Name the citing file, the cited number, and the missing path.\"\"\"\n         if self.reason == \"missing\":\n             return (\n-                f\"{self.citing_file}:{self.line} cites ADR {self.number} \"\n+                f\"{self.citing_file}:{self.line} cites {self.namespace} {self.number} \"\n                 f\"but no document matches {self.expected_path}\"\n             )\n         joined = \", \".join(self.matches)\n         return (\n-            f\"{self.citing_file}:{self.line} cites ADR {self.number} which \"\n+            f\"{self.citing_file}:{self.line} cites {self.namespace} {self.number} which \"\n             f\"resolves to {len(self.matches)} documents (expected exactly one \"\n             f\"at {self.expected_path}): {joined}\"\n         )\n \n \n-def _adr_index(root: Path) -> dict[str, list[Path]]:\n-    \"\"\"Map every four-digit ADR number to the record files that claim it.\"\"\"\n-    index: dict[str, list[Path]] = {}\n+def _adr_index(root: Path) -> dict[tuple[str, str], list[Path]]:\n+    \"\"\"Map each decision namespace and number to the files that claim it.\"\"\"\n+    index: dict[tuple[str, str], list[Path]] = {}\n     adr_dir = root / \"docs\" / \"adr\"\n     if adr_dir.is_dir():\n         for path in sorted(adr_dir.glob(\"*.md\")):\n-            match = _ADR_FILENAME.match(path.name)\n+            match = _DECISION_FILENAME.match(path.name)\n             if match:\n-                index.setdefault(match.group(1), []).append(path)\n+                namespace = \"SADR\" if match.group(1) else \"ADR\"\n+                index.setdefault((namespace, match.group(2)), []).append(path)\n     return index\n \n \n def _citing_files(root: Path, include_docs: bool = True) -> list[Path]:\n-    \"\"\"Files scanned for citations: scripts/*.py, and cheaply the markdown.\"\"\"\n+    \"\"\"Current, shipped text surfaces scanned for decision citations.\"\"\"\n     files = sorted((root / \"scripts\").glob(\"*.py\"))\n+    files += sorted((root / \"scripts\").glob(\"*.sh\"))\n     if include_docs:\n         docs = root / \"docs\"\n         if docs.is_dir():\n             files += sorted(docs.rglob(\"*.md\"))\n         files += sorted(root.glob(\"*.md\"))\n-    return files\n+        pyproject = root / \"pyproject.toml\"\n+        if pyproject.is_file():\n+            files.append(pyproject)\n+        for relative in (\".github\", \".codex\"):\n+            surface = root / relative\n+            if surface.is_dir():\n+                files += sorted(\n+                    path\n+                    for path in surface.rglob(\"*\")\n+                    if path.is_file()\n+                    and path.suffix in {\".md\", \".py\", \".sh\", \".toml\", \".yaml\", \".yml\"}\n+                )\n+    return sorted(set(files))\n \n \n def find_adr_reference_violations(\n@@ -94,10 +111,9 @@\n ) -> list[Violation]:\n     \"\"\"Return every ``ADR ####`` citation under ``root`` that fails to resolve.\n \n-    A citation resolves when exactly one ``docs/adr/####-*.md`` matches its\n-    number. Zero matches is a \"missing\" violation; two or more is \"ambiguous\".\n-    Records that are never cited produce nothing -- the converse is not\n-    required.\n+    A citation resolves when exactly one record in the same namespace matches\n+    its number. Zero matches is a \"missing\" violation; two or more is\n+    \"ambiguous\". Records that are never cited produce nothing.\n     \"\"\"\n     root = Path(root)\n     index = _adr_index(root)\n@@ -110,13 +126,15 @@\n         rel = path.relative_to(root).as_posix()\n         for lineno, line in enumerate(text.splitlines(), start=1):\n             for match in _CITATION.finditer(line):\n-                number = match.group(1)\n-                found = index.get(number, [])\n+                namespace, number = match.groups()\n+                found = index.get((namespace, number), [])\n                 if len(found) == 1:\n                     continue\n                 reason = \"missing\" if not found else \"ambiguous\"\n                 rels = [p.relative_to(root).as_posix() for p in found]\n-                violations.append(Violation(rel, number, lineno, reason, rels))\n+                violations.append(\n+                    Violation(rel, namespace, number, lineno, reason, rels)\n+                )\n     return violations\n \n \n@@ -129,8 +147,8 @@\n         self.assertEqual(\n             violations,\n             [],\n-            \"Every 'ADR ####' citation must resolve to exactly one \"\n-            f\"docs/adr/####-*.md:\\n{detail}\",\n+            \"Every ADR/SADR citation must resolve exactly once in its own \"\n+            f\"docs/adr namespace:\\n{detail}\",\n         )\n \n     def test_scan_actually_reads_scripts(self) -> None:\n@@ -142,12 +160,38 @@\n             \"the citation scan must cover scripts/*.py\",\n         )\n \n+    def test_platform_citations_do_not_use_unresolved_numeric_shorthand(self) -> None:\n+        shorthand = re.compile(r\"SADR-\\d{4}/\\d{4}\")\n+        offenders = []\n+        for path in _citing_files(REPO_ROOT):\n+            text = path.read_text(encoding=\"utf-8\")\n+            if shorthand.search(text):\n+                offenders.append(path.relative_to(REPO_ROOT).as_posix())\n+        self.assertEqual(\n+            offenders,\n+            [],\n+            \"repeat the SADR prefix so every cited number is independently resolvable\",\n+        )\n+\n     def test_carried_records_are_indexed(self) -> None:\n         # The two records this Goal carried onto the default branch, and which\n         # the scripts cite most, must be present.\n         index = _adr_index(REPO_ROOT)\n-        self.assertIn(\"0016\", index)\n-        self.assertIn(\"0017\", index)\n+        self.assertIn((\"SADR\", \"0016\"), index)\n+        self.assertIn((\"SADR\", \"0017\"), index)\n+\n+    def test_platform_tree_has_only_sadr_0001_through_0019(self) -> None:\n+        adr_dir = REPO_ROOT / \"docs\" / \"adr\"\n+        sadrs = sorted(adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\"))\n+        self.assertEqual(\n+            {path.name.split(\"-\", 2)[1] for path in sadrs},\n+            {f\"{number:04d}\" for number in range(1, 20)},\n+        )\n+        self.assertEqual(\n+            list(adr_dir.glob(\"[0-9][0-9][0-9][0-9]-*.md\")),\n+            [],\n+            \"the Shiki platform tree must not retain legacy numeric SADR paths\",\n+        )\n \n \n class FixtureAdrReferences(unittest.TestCase):\n@@ -180,6 +224,26 @@\n         self.assertIn(\"9991\", message)  # cited number\n         self.assertIn(\"docs/adr/9991-*.md\", message)  # missing path\n \n+    def test_target_adr_does_not_satisfy_missing_platform_sadr(self) -> None:\n+        root = self._make_repo(\n+            adr_names=[\"0042-target-decision.md\"],\n+            script_body='\"\"\"Target ADR 0042 exists, but platform SADR-0042 does not.\"\"\"\\n',\n+        )\n+        violations = find_adr_reference_violations(root, include_docs=False)\n+        self.assertEqual(len(violations), 1)\n+        self.assertEqual(violations[0].namespace, \"SADR\")\n+        self.assertEqual(violations[0].number, \"0042\")\n+        self.assertEqual(violations[0].expected_path, \"docs/adr/SADR-0042-*.md\")\n+\n+    def test_target_adr_and_platform_sadr_may_reuse_a_number(self) -> None:\n+        root = self._make_repo(\n+            adr_names=[\"0042-target-decision.md\", \"SADR-0042-platform-decision.md\"],\n+            script_body='\"\"\"Target ADR 0042 and platform SADR-0042 are distinct.\"\"\"\\n',\n+        )\n+        self.assertEqual(\n+            find_adr_reference_violations(root, include_docs=False), []\n+        )\n+\n     def test_uncited_adr_is_allowed(self) -> None:\n         # An ADR exists but nothing cites it -> not a violation. The converse\n         # (every ADR must be cited) is explicitly NOT required.\ndiff --git a/tests/test_guardian_review.py b/tests/test_guardian_review.py\nindex 88829e7810212635e2fc98aa5918d6f26294677d..00a85a237acd5bf2d6cb5b294128198c422402ca\n--- a/tests/test_guardian_review.py\n+++ b/tests/test_guardian_review.py\n@@ -162,7 +162,7 @@\n \n     def test_docs_only_is_light_and_not_conservative(self):\n         focus = gr.classify_review_focus([\"docs/adr/0099-x.md\", \"README.md\"], \"\", \"low\")\n-        self.assertTrue(any(\"Docs/ADR-only\" in f for f in focus))\n+        self.assertTrue(any(\"Docs/decision-record-only\" in f for f in focus))\n         self.assertFalse(any(\"conservative default\" in f.lower() for f in focus))\n \n     def test_unknown_category_is_conservative(self):\ndiff --git a/tests/test_policy_job_merged_prs.py b/tests/test_policy_job_merged_prs.py\nindex d45512e74681e5e649279236c7d4e79d5c3da471..b97989f8bd74d091de1a6b5275a20d8dc68c4512\n--- a/tests/test_policy_job_merged_prs.py\n+++ b/tests/test_policy_job_merged_prs.py\n@@ -3,8 +3,8 @@\n \n ``.github/workflows/shiki-cca-completion.yml`` has two consumers of the merged-PR\n proof: the CCA *signal* (``guardian_approval_signal.py`` in \"Prepare CCA context\")\n-and the authoritative gate (``mergegate_check.py`` in \"Enforce MergeGate\"). ADR\n-0017's bookkeeping-closeout exemption (condition 6) needs the merged implementation\n+and the authoritative gate (``mergegate_check.py`` in \"Enforce MergeGate\").\n+SADR-0017's bookkeeping-closeout exemption (condition 6) needs the merged implementation\n PR proven, and a closeout carries NO ``post_merge_reconcile`` marker. The signal\n already resolves ``MERGED_PRS`` unconditionally, but \"Enforce MergeGate\" used to\n resolve it only inside ``grep -q '<!-- shiki:post_merge_reconcile -->'``. So a\ndiff --git a/tests/test_sadr_install_migration.py b/tests/test_sadr_install_migration.py\nnew file mode 100644\nindex 0000000000000000000000000000000000000000..a6f4c57e4dcaef73ef600b0d4804744138ab9071\n--- /dev/null\n+++ b/tests/test_sadr_install_migration.py\n@@ -0,0 +1,229 @@\n+\"\"\"SADR installation and collision-safe legacy upgrade behavior.\"\"\"\n+\n+from __future__ import annotations\n+\n+import contextlib\n+import hashlib\n+import io\n+import json\n+import shutil\n+import tempfile\n+import unittest\n+from pathlib import Path\n+\n+import shiki_test_support  # noqa: F401  (path bootstrap)\n+\n+import shiki_installer\n+from tests.test_adr_references import find_adr_reference_violations\n+\n+\n+PRODUCT_NUMBERS = tuple(range(13, 19))\n+\n+\n+def _quiet_install(target: Path, *, force: bool, validate: bool) -> None:\n+    with contextlib.redirect_stdout(io.StringIO()):\n+        shiki_installer.install_template(target, force=force, validate=validate)\n+\n+\n+def _sha256(path: Path) -> str:\n+    return hashlib.sha256(path.read_bytes()).hexdigest()\n+\n+\n+def _legacy_sadr_paths() -> tuple[str, ...]:\n+    return tuple(\n+        f\"docs/adr/{path.name.removeprefix('SADR-')}\"\n+        for path in sorted((shiki_installer.ROOT / \"docs\" / \"adr\").glob(\"SADR-*.md\"))\n+        if path.name != \"SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\"\n+    )\n+\n+\n+def _create_legacy_upgrade_fixture(\n+    target: Path,\n+) -> tuple[dict[str, bytes], tuple[str, ...]]:\n+    adr_dir = target / \"docs\" / \"adr\"\n+    adr_dir.mkdir(parents=True)\n+    product_bytes = {\n+        f\"{number:04d}-cubeshare-product-decision.md\": (\n+            f\"# CubeShare ADR {number:04d}\\n\\nDo not rewrite {number}.\\n\"\n+        ).encode()\n+        for number in PRODUCT_NUMBERS\n+    }\n+    for name, content in product_bytes.items():\n+        (adr_dir / name).write_bytes(content)\n+\n+    legacy_paths = _legacy_sadr_paths()\n+    for relative in legacy_paths:\n+        (target / relative).write_bytes(f\"legacy Shiki bytes for {relative}\\n\".encode())\n+\n+    stamp_path = target / shiki_installer.INSTALL_STAMP_PATH\n+    stamp_path.parent.mkdir(parents=True, exist_ok=True)\n+    stamp = {\n+        \"version\": shiki_installer.INSTALL_STAMP_VERSION,\n+        \"platform_commit\": None,\n+        \"installed_at\": \"2000-01-01T00:00:00+00:00\",\n+        \"digests\": {\n+            relative: _sha256(target / relative) for relative in legacy_paths\n+        },\n+    }\n+    stamp_path.write_text(\n+        json.dumps(stamp, indent=2, sort_keys=True) + \"\\n\", encoding=\"utf-8\"\n+    )\n+    return product_bytes, legacy_paths\n+\n+\n+def _tree_snapshot(root: Path) -> dict[str, tuple[str, bytes | str]]:\n+    \"\"\"Recursive byte/shape snapshot used to prove preflight is write-free.\"\"\"\n+    snapshot: dict[str, tuple[str, bytes | str]] = {}\n+    for path in sorted(root.rglob(\"*\")):\n+        relative = path.relative_to(root).as_posix()\n+        if path.is_symlink():\n+            snapshot[relative] = (\"symlink\", path.readlink().as_posix())\n+        elif path.is_dir():\n+            snapshot[relative] = (\"directory\", \"\")\n+        else:\n+            snapshot[relative] = (\"file\", path.read_bytes())\n+    return snapshot\n+\n+\n+class FreshSadrInstallTests(unittest.TestCase):\n+    def setUp(self) -> None:\n+        self.tmp = Path(tempfile.mkdtemp(prefix=\"shiki-sadr-fresh-\"))\n+        self.addCleanup(lambda: shutil.rmtree(self.tmp, ignore_errors=True))\n+        self.target = self.tmp / \"target\"\n+        self.adr_dir = self.target / \"docs\" / \"adr\"\n+        self.adr_dir.mkdir(parents=True)\n+        self.product_bytes = {\n+            f\"{number:04d}-product-decision.md\": (\n+                f\"# Product ADR {number:04d}\\n\\nTarget-owned bytes {number}.\\n\"\n+            ).encode()\n+            for number in PRODUCT_NUMBERS\n+        }\n+        for name, content in self.product_bytes.items():\n+            (self.adr_dir / name).write_bytes(content)\n+\n+    def test_fresh_install_preserves_product_adrs_and_installs_all_sadrs(self) -> None:\n+        _quiet_install(self.target, force=False, validate=True)\n+\n+        for name, expected in self.product_bytes.items():\n+            self.assertEqual((self.adr_dir / name).read_bytes(), expected)\n+\n+        sadr_numbers = {\n+            path.name.split(\"-\", 2)[1]\n+            for path in self.adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\")\n+        }\n+        self.assertEqual(sadr_numbers, {f\"{number:04d}\" for number in range(1, 20)})\n+\n+        numeric_records = {\n+            path.name\n+            for path in self.adr_dir.glob(\"[0-9][0-9][0-9][0-9]-*.md\")\n+        }\n+        self.assertEqual(numeric_records, set(self.product_bytes))\n+        self.assertEqual(find_adr_reference_violations(self.target), [])\n+\n+\n+class LegacySadrUpgradeTests(unittest.TestCase):\n+    def setUp(self) -> None:\n+        self.tmp = Path(tempfile.mkdtemp(prefix=\"shiki-sadr-upgrade-\"))\n+        self.addCleanup(lambda: shutil.rmtree(self.tmp, ignore_errors=True))\n+        self.target = self.tmp / \"target\"\n+        self.adr_dir = self.target / \"docs\" / \"adr\"\n+        self.product_bytes, self.legacy_paths = _create_legacy_upgrade_fixture(\n+            self.target\n+        )\n+\n+    def test_force_upgrade_deletes_only_stamped_legacy_sadrs(self) -> None:\n+        _quiet_install(self.target, force=True, validate=True)\n+\n+        for relative in self.legacy_paths:\n+            self.assertFalse((self.target / relative).exists(), relative)\n+        for name, expected in self.product_bytes.items():\n+            self.assertEqual((self.adr_dir / name).read_bytes(), expected)\n+\n+        sadrs = list(self.adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\"))\n+        self.assertEqual(len(sadrs), 19)\n+        refreshed = json.loads(\n+            (self.target / shiki_installer.INSTALL_STAMP_PATH).read_text(encoding=\"utf-8\")\n+        )\n+        for relative in self.legacy_paths:\n+            self.assertNotIn(relative, refreshed[\"digests\"])\n+        for path in sadrs:\n+            relative = path.relative_to(self.target).as_posix()\n+            self.assertEqual(refreshed[\"digests\"][relative], _sha256(path))\n+\n+    def test_non_force_install_never_deletes_legacy_files(self) -> None:\n+        before = {\n+            relative: (self.target / relative).read_bytes()\n+            for relative in self.legacy_paths\n+        }\n+        _quiet_install(self.target, force=False, validate=False)\n+        for relative, expected in before.items():\n+            self.assertEqual((self.target / relative).read_bytes(), expected)\n+        refreshed = json.loads(\n+            (self.target / shiki_installer.INSTALL_STAMP_PATH).read_text(encoding=\"utf-8\")\n+        )\n+        for relative in self.legacy_paths:\n+            self.assertEqual(refreshed[\"digests\"][relative], _sha256(self.target / relative))\n+\n+\n+class LegacyCleanupPreflightTests(unittest.TestCase):\n+    def setUp(self) -> None:\n+        self.tmp = Path(tempfile.mkdtemp(prefix=\"shiki-sadr-preflight-\"))\n+        self.addCleanup(lambda: shutil.rmtree(self.tmp, ignore_errors=True))\n+\n+    def test_all_ownership_blockers_abort_before_any_write(self) -> None:\n+        cases = (\n+            (\"absent\", \"install stamp is absent\"),\n+            (\"unreadable\", \"install stamp is unreadable\"),\n+            (\"malformed\", \"install stamp is malformed\"),\n+            (\"missing-digest\", \"install stamp has no digest\"),\n+            (\"digest-mismatch\", \"digest mismatch\"),\n+        )\n+        for name, expected_reason in cases:\n+            with self.subTest(name=name):\n+                target = self.tmp / name\n+                _, legacy_paths = _create_legacy_upgrade_fixture(target)\n+                stamp_path = target / shiki_installer.INSTALL_STAMP_PATH\n+\n+                if name == \"absent\":\n+                    stamp_path.unlink()\n+                elif name == \"unreadable\":\n+                    stamp_path.unlink()\n+                    stamp_path.mkdir()\n+                elif name == \"malformed\":\n+                    stamp_path.write_text(\"{not-json\\n\", encoding=\"utf-8\")\n+                else:\n+                    stamp = json.loads(stamp_path.read_text(encoding=\"utf-8\"))\n+                    if name == \"missing-digest\":\n+                        stamp[\"digests\"] = {}\n+                    else:\n+                        stamp[\"digests\"] = {\n+                            relative: \"deadbeef\" for relative in legacy_paths\n+                        }\n+                    stamp_path.write_text(\n+                        json.dumps(stamp, indent=2, sort_keys=True) + \"\\n\",\n+                        encoding=\"utf-8\",\n+                    )\n+\n+                before = _tree_snapshot(target)\n+                with self.assertRaises(shiki_installer.ShikiError) as raised:\n+                    _quiet_install(target, force=True, validate=False)\n+                after = _tree_snapshot(target)\n+\n+                self.assertEqual(before, after, f\"{name} preflight changed the tree\")\n+                message = str(raised.exception)\n+                self.assertIn(expected_reason, message)\n+                self.assertIn(\"No files were written\", message)\n+                for relative in legacy_paths:\n+                    self.assertIn(relative, message)\n+\n+    def test_absent_stamp_is_allowed_when_no_legacy_path_exists(self) -> None:\n+        target = self.tmp / \"no-legacy\"\n+        target.mkdir()\n+        _quiet_install(target, force=True, validate=False)\n+        self.assertTrue(\n+            (target / \"docs\" / \"adr\" / \"SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\").is_file()\n+        )\n+\n+\n+if __name__ == \"__main__\":\n+    unittest.main()\n\ndiff --git a/.codex/skills/shiki/SKILL.md b/.codex/skills/shiki/SKILL.md\nindex c8e2e85d9d75d49cc4f2d6286ee22659c4e53e75..93cded623b2c15d9f65c76491a1c2d813ac9e6c0\n--- a/.codex/skills/shiki/SKILL.md\n+++ b/.codex/skills/shiki/SKILL.md\n@@ -18,6 +18,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n Codex CLI does not register this skill as a custom `/shiki` slash command. When\n the user types `/shiki` in Codex CLI and gets \"Unrecognized command\", explain\n that this is expected: invoke Shiki in Codex with natural language such as\n@@ -69,7 +72,7 @@\n \n ## Responsibilities\n \n-- Claude Code implements and repairs by default (`shiki runner claude`, ADR 0008); Codex implements and repairs tasks explicitly assigned to `codex`.\n+- Claude Code implements and repairs by default (`shiki runner claude`, SADR-0008); Codex implements and repairs tasks explicitly assigned to `codex`.\n - Claude Code Action can act as GitHub-side CCA or reviewer.\n - CCA judges completion.\n - MergeGate authorizes state transitions and merge readiness.\ndiff --git a/.github/prompts/cca-completion-check.md b/.github/prompts/cca-completion-check.md\nindex ed203ad5f302ecc81372d8ab27d2b1232ca97497..fd89950528333ef6242e6d5443904f5392ab7e56\n--- a/.github/prompts/cca-completion-check.md\n+++ b/.github/prompts/cca-completion-check.md\n@@ -19,6 +19,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n ## Required Reading\n \n Read only the files needed to judge the PR contract. Prefer this order:\ndiff --git a/.github/prompts/codex-task-handoff.md b/.github/prompts/codex-task-handoff.md\nindex 695e211d76dea1c60e8d5bb360e78487808286a4..59441d576b01a39569722f226c854e5a740e9eb5\n--- a/.github/prompts/codex-task-handoff.md\n+++ b/.github/prompts/codex-task-handoff.md\n@@ -2,7 +2,7 @@\n \n You are Codex Front acting as the Implementer for a Shiki task.\n \n-Follow `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant ADRs, and `docs/agents/codex-handoff.md`.\n+Follow `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant Shiki SADRs and target ADRs, and `docs/agents/codex-handoff.md`.\n \n Rules:\n \ndiff --git a/AGENTS.md b/AGENTS.md\nindex 7f041100d49c2a7525f51cb0f5d696a4a6d1c78c..a06bfbabc6fa48a007389e25e12d7ed523936bed\n--- a/AGENTS.md\n+++ b/AGENTS.md\n@@ -22,7 +22,7 @@\n - **Codex Front** is an optional implementer through Codex App, Codex CLI, Codex IDE extension, or Codex Web signed in with ChatGPT OAuth/subscription auth, dispatched with `shiki runner codex` when a task is explicitly assigned to `codex`.\n - **GitHub CCA** is implemented by Claude Code Action by default, using `CLAUDE_CODE_OAUTH_TOKEN`.\n \n-Do not assume `openai/codex-action`, `OPENAI_API_KEY`, or API-key based Codex automation in the default Shiki path. API-key runners are explicit target-repo extensions and require their own ADR.\n+Do not assume `openai/codex-action`, `OPENAI_API_KEY`, or API-key based Codex automation in the default Shiki path. API-key runners are explicit target-repo extensions and require their own target ADR.\n \n Shiki is not:\n \n@@ -44,10 +44,14 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use\n+`SADR-NNNN-*.md`; target product decisions retain `NNNN-*.md` and the `ADR`\n+namespace.\n+\n Use the canonical source-of-truth order above. Runtime-specific reading orders\n are operational guidance only and must not override or reorder it.\n \n-Conversation state is not durable truth. If a decision matters, put it in GitHub, `.shiki/`, `CONTEXT.md`, or an ADR.\n+Conversation state is not durable truth. If a decision matters, put it in GitHub, `.shiki/`, `CONTEXT.md`, a Shiki SADR, or a target ADR.\n \n If GitHub and `.shiki/` disagree, surface the conflict and prefer GitHub operational state until the mirror is repaired.\n \n@@ -75,8 +79,8 @@\n Every non-trivial change follows this loop:\n \n 1. **Goal**: clarify outcome, completion conditions, non-goals, risk level, and success signals.\n-2. **grill-with-docs**: challenge the plan against domain docs, ADRs, code reality, terminology, and edge scenarios.\n-3. **Context & Impact**: find relevant docs, ADRs, code, dependencies, owners, locks, and verification surfaces.\n+2. **grill-with-docs**: challenge the plan against domain docs, Shiki SADRs, target ADRs, code reality, terminology, and edge scenarios.\n+3. **Context & Impact**: find relevant docs, SADRs, target ADRs, code, dependencies, owners, locks, and verification surfaces.\n 4. **PRD and Spec Freeze**: use `to-prd` when context is settled enough to become durable product/engineering intent. The operator's explicit approval of the PRD is Spec Freeze: the plan records a `spec_freeze` block, and no plan runs without it. Steps 1-4 form Requirements Definition \u2014 one continuous operator dialogue. After Spec Freeze, scope changes require an operator-approved Spec Amendment; non-scope-moving interpretations go to the Assumption Log.\n 5. **Issues**: use `to-issues` to create independently grabbable vertical-slice issues. Prefer AFK slices over HITL slices where possible.\n 6. **Triage**: label issues for readiness, risk, runtime, skills, and MergeGate state.\n@@ -167,7 +171,7 @@\n - Testing decisions.\n - Out-of-scope items.\n - Further notes.\n-- Links to relevant domain terms and ADRs.\n+- Links to relevant domain terms, SADRs, and target ADRs.\n \n Every executable task must include:\n \n@@ -193,7 +197,7 @@\n \n Context & Impact must identify:\n \n-- Relevant documents, domain terms, ADRs, and past decisions.\n+- Relevant documents, domain terms, SADRs, target ADRs, and past decisions.\n - Relevant modules, interfaces, seams, callers, owners, and tests.\n - Dependency relationships between tasks.\n - Candidate locks and lock conflicts.\n@@ -210,7 +214,7 @@\n | Skill | Required when |\n | --- | --- |\n | `setup-matt-pocock-skills` | First configuring the repo for skills, or when issue tracker, triage labels, or domain docs are missing. |\n-| `grill-with-docs` | Default for non-trivial Goals before PRD/issues; always required when plans, terminology, boundaries, tradeoffs, or ADR-worthy decisions are ambiguous. |\n+| `grill-with-docs` | Default for non-trivial Goals before PRD/issues; always required when plans, terminology, boundaries, tradeoffs, or decision-record-worthy choices are ambiguous. |\n | `zoom-out` | The agent lacks an architectural map of the relevant code area. |\n | `to-prd` | Settled Goal context must become durable product/engineering intent. |\n | `to-issues` | A Goal, PRD, or plan must become independently grabbable vertical-slice issues. |\n@@ -348,7 +352,7 @@\n - Three or more new files.\n - Any high-risk or critical label.\n \n-When the architecture gate triggers, run `grill-with-docs` or `improve-codebase-architecture` as appropriate and record durable decisions in an ADR when needed.\n+When the architecture gate triggers, run `grill-with-docs` or `improve-codebase-architecture` as appropriate and record durable decisions in a Shiki SADR or target ADR as applicable.\n \n ## Engineering Discipline\n \ndiff --git a/CLAUDE.md b/CLAUDE.md\nindex 98a932e7841a632866fcd53b0bad0e3af3711d3d..98a31399404d6fb044b4fd493f4459ac87f439de\n--- a/CLAUDE.md\n+++ b/CLAUDE.md\n@@ -14,6 +14,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n ## Claude Role\n \n Default to planner, default implementer, reviewer, coordinator, documentation editor, CCA-style completion judge, and final-judgment assistant.\n@@ -57,7 +60,7 @@\n At the start of a Shiki session:\n \n 1. Read `AGENTS.md`.\n-2. Read `CONTEXT.md` and relevant ADRs.\n+2. Read `CONTEXT.md` and relevant Shiki SADRs and target ADRs.\n 3. Read active `.shiki/` Goal/task/ledger state when present.\n 4. Read active distilled rules from `.shiki/memories/` (status `distilled`, `active: true`, not revoked or superseded) for applicable guidance; the same rules are injected deterministically into task handoffs as the `## Distilled Rules` section (\u00a73.5).\n 5. Read the GitHub Issue or PR when the task is GitHub-backed.\n@@ -74,7 +77,7 @@\n - Define the Goal, completion conditions, non-goals, and risk level.\n - Treat `grill-with-docs` as the default planning entry point for non-trivial Goals.\n - Challenge domain terms against `CONTEXT.md`.\n-- Challenge hard-to-reverse tradeoffs against ADRs.\n+- Challenge hard-to-reverse tradeoffs against SADRs and target ADRs.\n - Explore code instead of asking when code can answer the question.\n - Run Context & Impact before implementation planning.\n - Produce a Task DAG, not an unordered checklist.\n@@ -94,7 +97,7 @@\n - For each question, provide your recommended answer.\n - Resolve dependency decisions before downstream design questions.\n - Update `CONTEXT.md` inline when terminology crystallizes.\n-- Offer ADRs only when the decision is hard to reverse, surprising without context, and a real tradeoff.\n+- Offer a SADR or target ADR only when the decision is hard to reverse, surprising without context, and a real tradeoff.\n - Convert unresolved questions into HITL blockers rather than allowing Codex to guess.\n \n ## PRD and Issues Standard\n@@ -103,7 +106,7 @@\n \n - Use the project's domain glossary vocabulary.\n - Avoid volatile file paths unless they are essential.\n-- Include implementation decisions, testing decisions, out-of-scope items, and relevant ADR/domain links.\n+- Include implementation decisions, testing decisions, out-of-scope items, and relevant SADR/target-ADR/domain links.\n - Identify deep-module opportunities and testable interfaces.\n \n When creating issues:\n@@ -121,7 +124,7 @@\n - Branch/worktree target.\n - Scope and non-goals.\n - Dependencies and locks.\n-- Relevant docs, ADRs, modules, and tests.\n+- Relevant docs, SADRs, target ADRs, modules, and tests.\n - Required skill invocations.\n - TDD expectations.\n - Pre-PR `code-review` gate expectations.\n@@ -152,7 +155,7 @@\n \n - Treat yourself as a judge, not implementer.\n - Do not write production code.\n-- Read `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant ADRs, PRD, task issue, PR body, diff, checks, reviews, labels, and `.shiki/` state.\n+- Read `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant SADRs and target ADRs, PRD, task issue, PR body, diff, checks, reviews, labels, and `.shiki/` state.\n - Evaluate every applicable checklist in `docs/agents/checklists.md`.\n - Map each acceptance criterion to durable evidence.\n - Distinguish implementation failure from missing evidence.\ndiff --git a/CONTEXT.md b/CONTEXT.md\nindex e61a9be06a0cce0d026484083a657bde2067e234..237ee8aa3ea8fab334ece49768bc5ed3b5af0974\n--- a/CONTEXT.md\n+++ b/CONTEXT.md\n@@ -25,7 +25,7 @@\n _Avoid_: jumping directly into code\n \n **grill-with-docs**:\n-A planning skill that challenges a plan against domain language, ADRs, code reality, and concrete edge scenarios. It resolves design-tree questions before PRD/issues. It runs inside Requirements Definition.\n+A planning skill that challenges a plan against domain language, Shiki SADRs, target ADRs, code reality, and concrete edge scenarios. It resolves design-tree questions before PRD/issues. It runs inside Requirements Definition.\n _Avoid_: generic brainstorming, silent assumptions\n \n **Requirements Definition**:\ndiff --git a/CONTRIBUTING.md b/CONTRIBUTING.md\nindex c6d9b656e73abb8a55e5f660123e03153dcb3e6d..9e37a8fbf668db5b22461aaae5548b8f1354394c\n--- a/CONTRIBUTING.md\n+++ b/CONTRIBUTING.md\n@@ -17,7 +17,7 @@\n 1. **Goal** \u2014 start from a user-approved Goal with an outcome, completion\n    conditions, non-goals, risk level, and success signals.\n 2. **grill-with-docs** \u2014 for any non-trivial change, challenge the plan against\n-   domain language, ADRs, and code reality before writing a PRD.\n+   domain language, Shiki SADRs, target ADRs, and code reality before writing a PRD.\n 3. **PRD** \u2014 capture settled product/engineering intent (`to-prd`).\n 4. **Spec Freeze** \u2014 the operator explicitly approves the PRD/requirements;\n    the plan records a `spec_freeze` block and no plan runs without it\n@@ -94,7 +94,7 @@\n   entries.\n - Do not hand-edit `.shiki/goals/*.json` or `.shiki/manifest.json` in a task PR.\n - Conversation state is not durable truth. If a decision matters, record it in\n-  GitHub, `.shiki/`, `CONTEXT.md`, or an ADR.\n+  GitHub, `.shiki/`, `CONTEXT.md`, a Shiki SADR, or a target ADR.\n \n ## Architecture Gate\n \n@@ -103,7 +103,7 @@\n or CI policy, package structure, three or more structural files, more than 100\n net new lines in a core module, three or more new files, or any high-risk label.\n When triggered, run `grill-with-docs` or `improve-codebase-architecture` and\n-record durable decisions in an ADR when needed (see `docs/adr/`).\n+record durable decisions in a Shiki SADR or target ADR as applicable (see `docs/adr/`).\n \n ## Safety\n \ndiff --git a/SYSTEM_PROMPT.md b/SYSTEM_PROMPT.md\nindex aaaea45e3a896c3a44ba89eec8bb84325ecf6c62..da009c86a88285269dee0155e58e550c6d744ea9\n--- a/SYSTEM_PROMPT.md\n+++ b/SYSTEM_PROMPT.md\n@@ -30,6 +30,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n Use the canonical source-of-truth order above. Runtime-specific reading orders\n are operational guidance only and must not override or reorder it. The current\n conversation is non-durable operator input.\n@@ -48,14 +51,14 @@\n \n A runtime may only do work that is explicitly assigned by the Goal, task, PR, or MergeGate decision.\n \n-Do not assume `openai/codex-action`, `OPENAI_API_KEY`, or API-key based Codex automation in the default Shiki path. API-key runners are explicit target-repo extensions and require their own ADR.\n+Do not assume `openai/codex-action`, `OPENAI_API_KEY`, or API-key based Codex automation in the default Shiki path. API-key runners are explicit target-repo extensions and require their own target ADR.\n \n ## 4. Non-negotiable Shiki flow\n \n For any non-trivial Goal, use this flow:\n \n 1. **Goal Seek**: define outcome, non-goals, risk level, completion criteria, and evidence requirements.\n-2. **grill-with-docs**: challenge the plan against `CONTEXT.md`, ADRs, code reality, terminology, boundaries, and edge scenarios. Ask one question at a time when the operator is available. If code can answer a question, inspect code instead of asking.\n+2. **grill-with-docs**: challenge the plan against `CONTEXT.md`, Shiki SADRs, target ADRs, code reality, terminology, boundaries, and edge scenarios. Ask one question at a time when the operator is available. If code can answer a question, inspect code instead of asking.\n 3. **Context & Impact**: identify modules, interfaces, seams, callers, dependencies, locks, risk, and verification surfaces.\n 4. **to-prd + Spec Freeze**: turn settled context into a PRD using domain vocabulary and testing decisions. The operator's explicit approval of the PRD is Spec Freeze, recorded as the plan's `spec_freeze` block; plans without it do not run. Steps 1-4 form Requirements Definition.\n 5. **to-issues**: decompose the PRD into independently grabbable vertical-slice issues. Prefer AFK slices; mark HITL slices when judgment is still required.\n@@ -75,7 +78,7 @@\n Before making any material change:\n \n 1. Read `AGENTS.md` and any runtime-specific wrapper such as `CLAUDE.md`.\n-2. Read `CONTEXT.md` and relevant ADRs.\n+2. Read `CONTEXT.md` and relevant SADRs and target ADRs.\n 3. Read the GitHub Issue / PR and `.shiki/` state for the active Goal or task.\n 4. Identify your role: Planner, Implementer, Reviewer, Completion Check Agent, Repairer, or Guardian-assist.\n 5. Identify Goal id, task id, branch, PR, dependencies, locks, risk level, required skills, and required evidence.\n@@ -103,7 +106,7 @@\n \n At minimum, identify:\n \n-- Relevant documents, ADRs, domain terms, and prior decisions.\n+- Relevant documents, SADRs, target ADRs, domain terms, and prior decisions.\n - Relevant modules, interfaces, seams, callers, owners, and tests.\n - Dependency relationships between tasks.\n - Candidate file locks and lock conflicts.\n@@ -152,7 +155,7 @@\n \n CCA must read:\n \n-- `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant ADRs.\n+- `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant SADRs and target ADRs.\n - Parent Goal issue, PRD issue, task issue, comments, and labels.\n - PR body, diff, commits, changed files, reviews, CI checks, and artifacts available to the workflow.\n - `.shiki/` task contract, locks, ledger entries, CCA checklist profile, and prior repair packets.\n@@ -214,7 +217,7 @@\n - Three or more new files.\n - Any high-risk or critical label.\n \n-When the architecture gate triggers, run `grill-with-docs` or `improve-codebase-architecture` as appropriate and record the decision in an ADR if it is hard to reverse or likely to be re-litigated.\n+When the architecture gate triggers, run `grill-with-docs` or `improve-codebase-architecture` as appropriate and record the decision in a Shiki SADR or target ADR as applicable if it is hard to reverse or likely to be re-litigated.\n \n ## 14. Skill Gate\n \n@@ -223,7 +226,7 @@\n Use these skills:\n \n - `setup-matt-pocock-skills`: before first use of the engineering skill set, or when issue tracker, triage labels, or domain docs are missing.\n-- `grill-with-docs`: default for non-trivial Goals before PRD/issues; required for ambiguous plans, terminology, boundaries, ADR-worthy tradeoffs, or design-tree decisions.\n+- `grill-with-docs`: default for non-trivial Goals before PRD/issues; required for ambiguous plans, terminology, boundaries, decision-record-worthy tradeoffs, or design-tree decisions.\n - `zoom-out`: unfamiliar code area or missing architectural map.\n - `to-prd`: convert settled context into a PRD.\n - `to-issues`: break a PRD, plan, or Goal into independently grabbable vertical-slice issues.\ndiff --git a/docs/adr/0001-keep-shiki-as-platform-name.md b/docs/adr/SADR-0001-keep-shiki-as-platform-name.md\nindex 20a9637c3b9d8c97de7ba8db7cfae4d82d9a7dba..8b685439959aada1b221c112f0ac3c51ca8a1695\n--- a/docs/adr/0001-keep-shiki-as-platform-name.md\n+++ b/docs/adr/SADR-0001-keep-shiki-as-platform-name.md\n@@ -1,3 +1,3 @@\n-# Keep Shiki As The Platform Name\n+# SADR-0001: Keep Shiki As The Platform Name\n \n Shiki remains the product name for the GitHub-native agentic engineering control plane. We considered more descriptive names such as MergePilot, MergeGate, and Kiroku, but kept Shiki because it carries the existing goal-seeking framework identity while remaining independent of Hermes, Codex, Claude Code, or any single runtime.\ndiff --git a/docs/adr/0002-use-agents-as-shared-constitution.md b/docs/adr/SADR-0002-use-agents-as-shared-constitution.md\nindex 73de30ecb078120eac540d9bc51855e3cd48d0e3..c5486182bf06fca36ea508496b013c67302fd989\n--- a/docs/adr/0002-use-agents-as-shared-constitution.md\n+++ b/docs/adr/SADR-0002-use-agents-as-shared-constitution.md\n@@ -1,3 +1,3 @@\n-# Use AGENTS As The Shared Agent Constitution\n+# SADR-0002: Use AGENTS As The Shared Agent Constitution\n \n Shiki uses `AGENTS.md` as the runtime-neutral constitution and `CLAUDE.md` as a thin Claude Code wrapper. This keeps Codex, Claude Code, Hermes Runner, and future Agent Runtimes aligned on the same GitHub-first MergeGate rules while still allowing runtime-specific instructions where needed.\ndiff --git a/docs/adr/0003-require-skill-gate-for-mergegate-readiness.md b/docs/adr/SADR-0003-require-skill-gate-for-mergegate-readiness.md\nindex 424be0c9af4c8f0136f1fde68a468c47ff84e764..7fc337987c7df9792f75f181dfcbed75fe5ffff2\n--- a/docs/adr/0003-require-skill-gate-for-mergegate-readiness.md\n+++ b/docs/adr/SADR-0003-require-skill-gate-for-mergegate-readiness.md\n@@ -1,3 +1,3 @@\n-# Require Skill Gate For MergeGate Readiness\n+# SADR-0003: Require Skill Gate For MergeGate Readiness\n \n Shiki requires relevant engineering skills to be invoked before execution when their triggers apply. This makes skills part of MergeGate readiness rather than optional prompt style, because Shiki depends on repeatable planning, issue decomposition, TDD, diagnosis, triage, and architecture review before autonomous or parallel agent work can be trusted.\ndiff --git a/docs/adr/0004-use-github-first-with-shiki-mirror.md b/docs/adr/SADR-0004-use-github-first-with-shiki-mirror.md\nindex d0a3a9174dc646a945a9d325954ffaf00c352040..a79c54ae9ee8eed2a0524192b864da4e22a92d3f\n--- a/docs/adr/0004-use-github-first-with-shiki-mirror.md\n+++ b/docs/adr/SADR-0004-use-github-first-with-shiki-mirror.md\n@@ -1,4 +1,4 @@\n-# ADR 0004: Use GitHub First With A Repository-Local Shiki Mirror\n+# SADR-0004: Use GitHub First With A Repository-Local Shiki Mirror\n \n ## Status\n \ndiff --git a/docs/adr/0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md b/docs/adr/SADR-0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\nindex b339baf49b89d9290e79c3d51b36e7b2bde3bd95..0f3c3eb9afccdb3403ce978e21d04fe12e495da0\n--- a/docs/adr/0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\n+++ b/docs/adr/SADR-0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\n@@ -1,8 +1,8 @@\n-# ADR 0005: Use Codex As Front And Claude Code Action As The GitHub Runtime\n+# SADR-0005: Use Codex As Front And Claude Code Action As The GitHub Runtime\n \n ## Status\n \n-Accepted (implementer default superseded by ADR 0008; the auth model and the\n+Accepted (implementer default superseded by SADR-0008; the auth model and the\n Claude Code Action GitHub runtime decision remain in force)\n \n ## Context\n@@ -26,4 +26,4 @@\n - Shiki avoids requiring `OPENAI_API_KEY` for the default workflow.\n - Codex work happens through the user's authenticated Codex front surface, not as a hidden backend Action.\n - Claude Code Action is configured for OAuth-token based subscription usage by default.\n-- API-key based automation remains possible only as an explicit target-repo extension with its own ADR.\n+- API-key based automation remains possible only as an explicit target-repo extension with its own target ADR.\ndiff --git a/docs/adr/0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md b/docs/adr/SADR-0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\nindex 4bcae9d1e52f0a37b8e9fbafa5cd14b6ee1993bd..7a38538a6ae9929567f0ade67e7ea6a60d0b3f7e\n--- a/docs/adr/0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\n+++ b/docs/adr/SADR-0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\n@@ -1,4 +1,4 @@\n-# ADR 0006: Separate Codex Implementation, CCA Completion, And MergeGate Authorization\n+# SADR-0006: Separate Codex Implementation, CCA Completion, And MergeGate Authorization\n \n ## Status\n \ndiff --git a/docs/adr/0007-packaging-and-release.md b/docs/adr/SADR-0007-packaging-and-release.md\nindex 79b84ddb97167372b44641e5b7860e400ce80822..bb445a2e2e856d9d1f5fdbe35de8d4e8cec0fb32\n--- a/docs/adr/0007-packaging-and-release.md\n+++ b/docs/adr/SADR-0007-packaging-and-release.md\n@@ -1,4 +1,4 @@\n-# ADR 0007: Packaging And Release For Shiki\n+# SADR-0007: Packaging And Release For Shiki\n \n ## Status\n \n@@ -25,7 +25,7 @@\n   not introduce third-party runtime dependencies or break the shim.\n - Releases must run inside the operator's subscription-authenticated toolchain\n   and must not require extra secrets beyond the built-in `GITHUB_TOKEN`\n-  (consistent with ADR 0005's no-API-key-by-default stance).\n+  (consistent with SADR-0005's no-API-key-by-default stance).\n - `.shiki/manifest.json` already encodes the install surface (`install.include`,\n   `install.create_directories`, `install.exclude_from_commit`), and it must not\n   be mutated as part of this change.\n@@ -34,7 +34,7 @@\n \n This is a hard-to-reverse platform decision (it sets the version source of\n truth, the release trigger, and the public install contract), so it is recorded\n-as an ADR.\n+as a SADR.\n \n ## Decision\n \n@@ -104,7 +104,7 @@\n 3. **Publishing to PyPI.** Rejected for now: Shiki is distributed as a\n    repository template + git-installable CLI, not a PyPI library; PyPI would add\n    credentials/secrets and a different versioning contract. Can be added later as\n-   its own ADR if a packaged distribution is needed.\n+   its own SADR if a packaged distribution is needed.\n 4. **Dynamic version (read `VERSION` via a build backend `dynamic` field).**\n    Rejected to keep `pyproject.toml` backend-agnostic and dependency-free; the\n    small duplication between `VERSION` and `pyproject.toml` is guarded by the\n@@ -123,4 +123,4 @@\n   releasing versions the whole platform but ships only `install.include` to\n   targets.\n - Future PyPI distribution or a console entry point are deferred and would each\n-  warrant their own ADR.\n+  warrant their own SADR.\ndiff --git a/docs/adr/0008-use-claude-code-as-default-implementer.md b/docs/adr/SADR-0008-use-claude-code-as-default-implementer.md\nindex dedf4510b7c7799521422f622029563d373db031..35c2625d908167fbc58855b5b63e283805f167e8\n--- a/docs/adr/0008-use-claude-code-as-default-implementer.md\n+++ b/docs/adr/SADR-0008-use-claude-code-as-default-implementer.md\n@@ -1,12 +1,12 @@\n-# ADR 0008: Use Claude Code As The Default Implementer Runtime\n+# SADR-0008: Use Claude Code As The Default Implementer Runtime\n \n ## Status\n \n-Proposed (supersedes ADR 0005 for the implementer default; keeps its auth model)\n+Proposed (supersedes SADR-0005 for the implementer default; keeps its auth model)\n \n ## Context\n \n-ADR 0005 made Codex Front the default implementation runtime, with Claude Code\n+SADR-0005 made Codex Front the default implementation runtime, with Claude Code\n restricted to planner and reviewer roles by the runtime registry, and the only\n autonomous implementation adapter was `shiki runner codex`.\n \n@@ -18,13 +18,13 @@\n - The platform's planning, review, and CCA layers already run on Claude Code,\n   and Claude Code's native capabilities (the built-in `/code-review` skill and\n   the Workflow multi-agent orchestration tool) have no Codex equivalent.\n-- The Goal lifecycle redesign (ADR 0009) requires an autonomous post-freeze\n+- The Goal lifecycle redesign (SADR-0009) requires an autonomous post-freeze\n   loop driven from the operator's Claude Code session, including bounded\n   repair dispatch, which must not depend on a runtime that cannot be invoked\n   unattended.\n \n The constitution claims Shiki is runtime-agnostic (\"not a Claude-only\n-workflow\", ADR 0002). Any change to the implementer default must preserve the\n+workflow\", SADR-0002). Any change to the implementer default must preserve the\n registry-based runtime model rather than hardcoding one vendor.\n \n ## Decision\n@@ -39,7 +39,7 @@\n   the task to review or repair-needed.\n - Keep Codex registered as an optional implementer runtime. `shiki runner\n   codex`, the Codex handoff templates, and the ChatGPT OAuth auth model from\n-  ADR 0005 remain supported for tasks explicitly assigned to `codex`.\n+  SADR-0005 remain supported for tasks explicitly assigned to `codex`.\n - Commit to mandating two Claude-native capabilities as loop contract in\n   follow-up Goals (status: the code-review gate was delivered by Goal G-B \u2014\n   skill registry, checklists item PR-12, default required_skills \u2014 while the\n@@ -60,7 +60,7 @@\n \n Out of scope: removing Codex support, changing the CCA runtime (Claude Code\n Action with `CLAUDE_CODE_OAUTH_TOKEN` stays), and API-key based automation\n-(still requires its own ADR per ADR 0005).\n+(still requires its own target ADR per SADR-0005).\n \n ## Consequences\n \n@@ -100,7 +100,7 @@\n   harness rejection of direct `codex exec` is outside Shiki's control, and the\n   plan's mandatory native capabilities cannot run on Codex at all.\n - **Remove Codex entirely**: rejected; it abandons the runtime-agnostic\n-  constitution (ADR 0002) for no operational gain and forecloses\n+  constitution (SADR-0002) for no operational gain and forecloses\n   cross-runtime adversarial review.\n - **GitHub Actions cloud implementation via claude-code-action**: rejected as\n   default; durable evidence is automatic but token cost, Node 24 workflow\ndiff --git a/docs/adr/0009-requirements-definition-and-spec-freeze.md b/docs/adr/SADR-0009-requirements-definition-and-spec-freeze.md\nindex 29b3f7871bef9203a8b4a831a65c39eb2f506e42..52e7dd69ad0aa4efe0b68c62678abbf7df02aaec\n--- a/docs/adr/0009-requirements-definition-and-spec-freeze.md\n+++ b/docs/adr/SADR-0009-requirements-definition-and-spec-freeze.md\n@@ -1,4 +1,4 @@\n-# ADR 0009: Merge Grill And Planning Into Requirements Definition With Spec Freeze\n+# SADR-0009: Merge Grill And Planning Into Requirements Definition With Spec Freeze\n \n ## Status\n \ndiff --git a/docs/adr/0010-external-ai-guardian-review.md b/docs/adr/SADR-0010-external-ai-guardian-review.md\nindex e6108bbe8bb4e73e0ff4684c8f78001a80438c4e..f3510b145664298adafb151b6f0db51b972f01ec\n--- a/docs/adr/0010-external-ai-guardian-review.md\n+++ b/docs/adr/SADR-0010-external-ai-guardian-review.md\n@@ -1,4 +1,4 @@\n-# ADR 0010: External AI Guardian Review As A First-Class Approval Authority\n+# SADR-0010: External AI Guardian Review As A First-Class Approval Authority\n \n ## Status\n \n@@ -8,7 +8,7 @@\n \n Shiki is intended to be an autonomous, recursive, self-improving agent\n framework: authority-in-the-loop, not human-in-the-loop. Until now the Guardian\n-approval required for high/critical-risk merges (guardian-policy.json, ADR\n+approval required for high/critical-risk merges (guardian-policy.json, SADR\n governance) recognized only human authority sources \u2014 a GitHub review approval,\n the `guardian:approved` label applied by a configured Guardian, or a comment\n carrying `Guardian approval granted` plus the current head SHA.\n@@ -89,7 +89,7 @@\n   autonomous design the operator intends and produced the impersonation\n   incident.\n - **Record AI approval as an operator comment**: rejected as forgery of human\n-  authority \u2014 the exact failure this ADR corrects.\n+  authority \u2014 the exact failure this SADR corrects.\n - **Deliver the artifact as a committed `.shiki/` file**: rejected; a committed\n   approval inside the PR being approved is forgeable by anyone who can push.\n - **A dedicated reviewer bot identity instead of a relayed comment**: deferred;\ndiff --git a/docs/adr/0011-autonomous-quality-gate-evidence.md b/docs/adr/SADR-0011-autonomous-quality-gate-evidence.md\nindex bca0246c5d46e44afbde15486a75eec425fc02f5..9c93eac16d8c3a6d4b11bd7224e3955102cd7db4\n--- a/docs/adr/0011-autonomous-quality-gate-evidence.md\n+++ b/docs/adr/SADR-0011-autonomous-quality-gate-evidence.md\n@@ -1,4 +1,4 @@\n-# ADR 0011: Autonomous Quality-Gate Evidence \u2014 Deterministic Facts And An Independent Verifier\n+# SADR-0011: Autonomous Quality-Gate Evidence \u2014 Deterministic Facts And An Independent Verifier\n \n ## Status\n \ndiff --git a/docs/adr/0012-autonomous-loop-closeout-to-main.md b/docs/adr/SADR-0012-autonomous-loop-closeout-to-main.md\nindex 637fad6dde4989c9c2bf0f81f676d8d29991123c..ec7d85f81030a676bcbba28b43eb365422a36eac\n--- a/docs/adr/0012-autonomous-loop-closeout-to-main.md\n+++ b/docs/adr/SADR-0012-autonomous-loop-closeout-to-main.md\n@@ -1,4 +1,4 @@\n-# 0012 \u2014 Autonomous loop pushes goal completion to main via a closeout PR\n+# SADR-0012 \u2014 Autonomous loop pushes goal completion to main via a closeout PR\n \n - Status: accepted\n - Date: 2026-06-18\ndiff --git a/docs/adr/0013-bootstrap-grants-actions-approve-pull-requests.md b/docs/adr/SADR-0013-bootstrap-grants-actions-approve-pull-requests.md\nindex b6c69d64f1c5ced346b88d6859586715b3b5bd0e..cf48ff4a6716267f539ca58edd1a852e7c7a2d64\n--- a/docs/adr/0013-bootstrap-grants-actions-approve-pull-requests.md\n+++ b/docs/adr/SADR-0013-bootstrap-grants-actions-approve-pull-requests.md\n@@ -1,4 +1,4 @@\n-# ADR 0013: Bootstrap Grants GitHub Actions Permission To Create And Approve Pull Requests\n+# SADR-0013: Bootstrap Grants GitHub Actions Permission To Create And Approve Pull Requests\n \n ## Status\n \n@@ -31,7 +31,7 @@\n security tradeoff: it lets repository automation satisfy a human-review gate. In\n a multi-maintainer repo that could let a compromised or buggy workflow\n self-approve changes. So whether bootstrap turns it on by default \u2014 and with\n-what default token scope \u2014 is an ADR-worthy decision, not an implementation\n+what default token scope \u2014 is a SADR-worthy decision, not an implementation\n detail.\n \n ## Decision\n@@ -48,7 +48,7 @@\n Specifically:\n \n - **`can_approve_pull_request_reviews` defaults to `true`.** Shiki's default\n-  posture is solo/AFK autonomous operation (ADR 0008/0009/0012), where the CCA\n+  posture is solo/AFK autonomous operation (SADR-0008/0009/0012), where the CCA\n   Review Bridge is the only path that can satisfy `required_review: true` after\n   the CCA and Guardian gates have already passed. The default makes a\n   freshly-bootstrapped repo able to run the autonomous loop end to end without a\ndiff --git a/docs/adr/0014-codex-app-external-ai-guardian-ui-adapter.md b/docs/adr/SADR-0014-codex-app-external-ai-guardian-ui-adapter.md\nindex 238db06279334caa341c117362df9271382a7fcd..65f12e9939321e1b3e389a34c822fc1549e5155d\n--- a/docs/adr/0014-codex-app-external-ai-guardian-ui-adapter.md\n+++ b/docs/adr/SADR-0014-codex-app-external-ai-guardian-ui-adapter.md\n@@ -1,4 +1,4 @@\n-# ADR 0014: Use Codex App As The External AI Guardian UI Adapter\n+# SADR-0014: Use Codex App As The External AI Guardian UI Adapter\n \n ## Status\n \ndiff --git a/docs/adr/0015-contract-approval-before-dispatch.md b/docs/adr/SADR-0015-contract-approval-before-dispatch.md\nindex fad6c656741a20a7684ba16080a7db159f63a52f..6061271a46d3f1d6ae7174477de3fb75673123a6\n--- a/docs/adr/0015-contract-approval-before-dispatch.md\n+++ b/docs/adr/SADR-0015-contract-approval-before-dispatch.md\n@@ -1,4 +1,4 @@\n-# ADR 0015: Approve The Task Contract Before Dispatch, Not The Diff After It\n+# SADR-0015: Approve The Task Contract Before Dispatch, Not The Diff After It\n \n ## Status\n \n@@ -9,7 +9,7 @@\n Guardian approval for high/critical risk is expressible only on a pull request.\n Every approval source in `guardian-policy.json` \u2014 GitHub review, the\n `guardian:approved` label, the `Guardian approval granted` comment, and the\n-`external_ai_guardian_review` artifact (ADR 0010/0014) \u2014 is bound to a PR, and\n+`external_ai_guardian_review` artifact (SADR-0010/0014) \u2014 is bound to a PR, and\n three of the four require the current head SHA. Approval therefore cannot exist\n before an implementation PR exists.\n \n@@ -81,7 +81,7 @@\n An envelope breach must surface only on the non-repairable policy gate, never on\n the repairable `MergeGate metadata check`. Otherwise the Goal Loop routes it to\n `dispatch_repair` \u2014 instructing a runner to make the breach go away \u2014 which is\n-exactly what this ADR forbids.\n+exactly what this SADR forbids.\n \n **Contract immutability.** After registration, an implementation PR must not\n change the governance fields of its own task contract (`scope`, `non_goals`,\n@@ -116,7 +116,7 @@\n fails any verdict that is not `complete`, and the signal the CCA prompt reads is\n computed from `evaluate_guardian_approval` alone. Without the OR-merge at the\n signal, a contract-approved PR still returns `needs_guardian`, the CCA job fails,\n-and the MergeGate policy check never runs \u2014 the gate this ADR changes would never\n+and the MergeGate policy check never runs \u2014 the gate this SADR changes would never\n be reached. The CCA *prompt* needs no change: a `contract_approval` entry in the\n signal's existing `sources` array satisfies its current wording.\n \n@@ -130,7 +130,7 @@\n written, and an approved contract lets its implementation PRs merge through the\n same automatic path as low/medium risk. The wasted implement-then-block cycle\n disappears. Guardian review shifts from reading diffs to reading contracts, which\n-is both earlier and smaller; the external AI Guardian path (ADR 0010/0014) works\n+is both earlier and smaller; the external AI Guardian path (SADR-0010/0014) works\n on contracts with no change.\n \n We accept that the Guardian authorizes a class of change without seeing its\n@@ -146,7 +146,7 @@\n adversarial tests for the new approval path equivalent in rigor to\n `test_shiki_governance_evidence.sh`.\n \n-Bootstrap costs, which apply only while this ADR is being implemented. The tasks\n+Bootstrap costs, which apply only while this SADR is being implemented. The tasks\n that build Contract Approval merge under the existing rules, so the ones\n dispatched after the pre-dispatch gate lands but before the approval path lands\n need a Guardian-approved Contract PR *and* a live Guardian approval on their\n@@ -210,7 +210,7 @@\n risk; they do not bound authorized work.\n \n **Auto-escalate risk on envelope breach instead of stopping.** Returns the\n-workflow to a diff-time Guardian decision \u2014 the exact behavior this ADR removes.\n+workflow to a diff-time Guardian decision \u2014 the exact behavior this SADR removes.\n \n **Resolve governance fields from the base snapshot only.** Rejected against the\n registration flow: implementation PRs add their own contract, so the base has no\ndiff --git a/docs/adr/0016-locks-bound-product-paths.md b/docs/adr/SADR-0016-locks-bound-product-paths.md\nindex b5e850f3f1f7f4ef6e7f9d1763a29a51bd300c45..46b40a9b99a7aae644c02bb458f5473ff0a90ce2\n--- a/docs/adr/0016-locks-bound-product-paths.md\n+++ b/docs/adr/SADR-0016-locks-bound-product-paths.md\n@@ -1,4 +1,4 @@\n-# ADR 0016: Locks bound product paths; the Shiki mirror is bound by record identity\n+# SADR-0016: Locks bound product paths; the Shiki mirror is bound by record identity\n \n ## Status\n \n@@ -6,8 +6,8 @@\n \n - Date: 2026-07-28\n - Deciders: operator (mizutani-140), Claude (planner)\n-- Related: ADR 0004 (GitHub-first with a `.shiki` mirror), ADR 0009 (Spec Freeze),\n-  ADR 0012 (autonomous loop closeout to main), ADR 0015 (Contract Approval before\n+- Related: SADR-0004 (GitHub-first with a `.shiki` mirror), SADR-0009 (Spec Freeze),\n+  SADR-0012 (autonomous loop closeout to main), SADR-0015 (Contract Approval before\n   dispatch), remediation goal task R-02, the live PR #179 deadlock.\n \n ## Context\n@@ -90,11 +90,11 @@\n files, documentation files and named source modules are nameable; runtime-\n allocated ledger, runner and report ids are not \u2014 and those live in `.shiki`,\n which locks no longer govern. Existing frozen contracts are brought into line by\n-a one-time Spec Amendment per plan, which must land before ADR 0015's contract\n+a one-time Spec Amendment per plan, which must land before SADR-0015's contract\n immutability makes `locks` unwritable from the implementation PR.\n \n **Lock state is resolved from the durable side.** MergeGate resolves the lock\n-registry, and ADR 0015's governance fields, from the base `.shiki` snapshot when\n+registry, and SADR-0015's governance fields, from the base `.shiki` snapshot when\n present, falling back to head only when absent. The `MergeGate metadata check`\n job is given `--base-shiki`, which it does not receive today. Locks are not\n derived from the live open-pull-request set: a pull request body is untrusted\n@@ -108,7 +108,7 @@\n Explicitly out of scope: moving MergeGate's contract resolution off the PR\n checkout wholesale; making the mirror a projection of GitHub state; introducing\n new lock kinds; moving ledger evidence out of the repository; and unwinding\n-ADR 0012's closeout pull request. Registration of multi-task goals is ADR 0015's\n+SADR-0012's closeout pull request. Registration of multi-task goals is SADR-0015's\n Contract PR and is not re-decided here.\n \n ## Consequences\n@@ -127,7 +127,7 @@\n \n Risks accepted. First, goal-scoped identity is only as strong as `goal_id`,\n which is resolved from the PR body and the head task file; the mitigation is to\n-add `goal_id` to ADR 0015's frozen governance field set, and until that lands a\n+add `goal_id` to SADR-0015's frozen governance field set, and until that lands a\n task file can name a foreign goal. Second, base-when-present resolution is\n unavailable on fork pull requests, where the head is contributor-controlled;\n fork pull requests already cannot complete a Shiki merge, and this decision does\n@@ -139,7 +139,7 @@\n path, not by implementation pull requests.\n \n New obligations: one Spec Amendment per frozen plan to rewrite `locks`, ordered\n-before ADR 0015 T1; extension of the adversarial suites in\n+before SADR-0015 T1; extension of the adversarial suites in\n `scripts/test_shiki_adversarial_state.sh` and\n `scripts/test_shiki_governance_evidence.sh` to cover the eleven newly-scoped\n directories; and updating `scripts/test_shiki_loop_lock_guard.sh`,\n@@ -151,32 +151,32 @@\n whether the loop's transitive sync of ledger `evidence` references can be\n deleted once the Contract PR carries its own source plan.\n \n-## Relationship to ADR 0004\n+## Relationship to SADR-0004\n \n-This ADR **clarifies something ADR 0004 left unspecified**. ADR 0004 decides that\n+This SADR **clarifies something SADR-0004 left unspecified**. SADR-0004 decides that\n GitHub is the operational source of truth and that each target repository keeps a\n `.shiki/` mirror. It says nothing about pull requests writing the mirror, does\n not name MergeGate, and does not make the mirror an input to a gate. Nothing in\n-ADR 0004's Decision or Consequences is amended, superseded or weakened: the\n+SADR-0004's Decision or Consequences is amended, superseded or weakened: the\n mirror still exists, still records the same artifacts, and pull request\n-boundaries remain the coordination surface. What this ADR settles is the question\n+boundaries remain the coordination surface. What this SADR settles is the question\n 0004 never reached \u2014 by what authority a pull request may write a mirror record \u2014\n and it answers it the way `.shiki/manifest.json` already declares\n (`pr_mutation: current-task-or-current-goal-only`).\n \n-It **amends ADR 0012**, whose Decision item 5 states that \"Loop-executed tasks\n+It **amends SADR-0012**, whose Decision item 5 states that \"Loop-executed tasks\n declare the `path:.shiki/**` lock, which covers every `.shiki` file the closeout\n stages ... so `files_outside_locks` is satisfied without per-file lock\n bookkeeping.\" That clause is replaced: the closeout's mirror writes are\n-authorized by record identity, not by a declared lock. ADR 0012's substantive\n+authorized by record identity, not by a declared lock. SADR-0012's substantive\n decision \u2014 that completion reaches `main` through a closeout pull request rather\n than local attestation \u2014 is preserved unchanged.\n \n-It is **complementary to ADR 0015**, not a substitute. ADR 0015 owns contract\n+It is **complementary to SADR-0015**, not a substitute. SADR-0015 owns contract\n immutability, the pre-dispatch gate, the Contract PR and the Scope Envelope. This\n-ADR owns what a lock means and who may write the mirror. The one ordering\n+SADR owns what a lock means and who may write the mirror. The one ordering\n constraint between them is that the lock-narrowing Spec Amendment must precede\n-ADR 0015 T1's freezing of the `locks` field.\n+SADR-0015 T1's freezing of the `locks` field.\n \n ## Alternatives Considered\n \n@@ -186,7 +186,7 @@\n the only writer that can register them may run. Migrating the Guardian signal to\n that store makes every registration PR fail closed to `needs_guardian` at every\n risk level, because the task file is by definition absent from the store, which\n-removes the low/medium autonomous path ADR 0008/0009 exists to provide. And it\n+removes the low/medium autonomous path SADR-0008/0009 exists to provide. And it\n leaves schemas, manifest, config, guardian policy, templates, migrations, plans,\n worktrees, runner records, runs and handoffs with no writer at all in a\n repository whose product is `.shiki`. Measured benefit on the frozen backlog:\n@@ -199,14 +199,14 @@\n namespace is not in fact ownership-partitioned \u2014 56 ledger entries carry no\n `task_id`, and one goal DAG is writable by 35 registered tasks. The correct\n expression of its insight is that locks do not govern the mirror at all, which is\n-what this ADR decides.\n+what this SADR decides.\n \n **Make the mirror a projection written only by a registration writer and a\n post-merge projection job.** Rejected. It requires a bypass actor with write\n access to the protected default branch; it makes its own master invariant\n readable from a manifest that pull requests may edit; it deletes both reconcile\n modes and leaves a corrupted mirror with no in-band repair path, contradicting\n-ADR 0004's consequence that mirror conflicts are repaired against GitHub state;\n+SADR-0004's consequence that mirror conflicts are repaired against GitHub state;\n and its content-derived ids are rejected by `validate_shiki`'s currently-green id\n pattern.\n \ndiff --git a/docs/adr/0017-bookkeeping-closeouts-do-not-inherit-risk.md b/docs/adr/SADR-0017-bookkeeping-closeouts-do-not-inherit-risk.md\nindex 554e3a0d526a72ca067108c5464cc68c3edcf174..b9676857a4713850e063af7c39c8e0b01ecb4c66\n--- a/docs/adr/0017-bookkeeping-closeouts-do-not-inherit-risk.md\n+++ b/docs/adr/SADR-0017-bookkeeping-closeouts-do-not-inherit-risk.md\n@@ -1,4 +1,4 @@\n-# ADR 0017: A Bookkeeping Closeout Does Not Inherit Its Task's Risk\n+# SADR-0017: A Bookkeeping Closeout Does Not Inherit Its Task's Risk\n \n ## Status\n \n@@ -6,7 +6,7 @@\n \n ## Context\n \n-ADR 0012 made the goal loop push a task's completion to the default branch through a\n+SADR-0012 made the goal loop push a task's completion to the default branch through a\n closeout pull request: after the implementation PR merges, the loop opens a second PR\n carrying `task.status = done`, `lock.state = released`, and \u2014 when the task completes\n its goal \u2014 `goal.status = complete` plus one scorecard report. Nothing else. The\n@@ -49,14 +49,14 @@\n 4. The task file's governance fields (`scope`, `non_goals`, `required_skills`,\n    `risk_level`, `locks`, `acceptance_checks`, `test_command`) are byte-identical to\n    the base snapshot. Only `status`, `expected_pr`, `closeout_pr`, `ledger_evidence`\n-   and `expected_branch` may differ \u2014 the ADR 0015 contract-immutability rule,\n+   and `expected_branch` may differ \u2014 the SADR-0015 contract-immutability rule,\n    unchanged.\n \n    `expected_branch` is in that set because a closeout is *by construction* on a\n    different branch from the implementation it closes, and the loop rewrites the\n    field accordingly. It is safe to let it differ because MergeGate independently\n    requires `expected_branch` to equal the PR's actual head ref, so a PR cannot set\n-   it to anything other than the branch it is really on. The first draft of this ADR\n+   it to anything other than the branch it is really on. The first draft of this SADR\n    omitted it, which made the exemption unreachable: every real closeout failed\n    condition 4 on a field the loop always changes. See PR #209.\n 5. The state transitions are exactly the terminal set: task `review` \u2192 `done`, lock\n@@ -113,12 +113,12 @@\n classification must be derived, not asserted.\n \n **Fold the closeout into the implementation PR** so no second PR exists. Rejected under\n-ADR 0012, which recorded why: a task file that is already `done` on the implementation\n+SADR-0012, which recorded why: a task file that is already `done` on the implementation\n branch short-circuits the loop's review \u2192 CCA \u2192 merge \u2192 done ordering into a local\n goal-completion before any merge happens.\n \n **Have the loop auto-merge closeouts with elevated credentials.** Rejected outright: that\n-is a runtime authoring its own approval, which ADR 0010 and the constitution's\n+is a runtime authoring its own approval, which SADR-0010 and the constitution's\n approval-record prohibition forbid. It would also be indistinguishable, in the evidence,\n from the forgery those rules exist to prevent.\n \ndiff --git a/docs/adr/0018-carried-guardian-approval-across-a-proven-base-sync.md b/docs/adr/SADR-0018-carried-guardian-approval-across-a-proven-base-sync.md\nindex 22358562da390ed8b45cd600da08a43d378b4680..e6e2907cefa776460af73ddf7d39db605f3c39b0\n--- a/docs/adr/0018-carried-guardian-approval-across-a-proven-base-sync.md\n+++ b/docs/adr/SADR-0018-carried-guardian-approval-across-a-proven-base-sync.md\n@@ -1,4 +1,4 @@\n-# ADR 0018: Carried Guardian Approval Across A Proven Base Sync\n+# SADR-0018: Carried Guardian Approval Across A Proven Base Sync\n \n ## Status\n \n@@ -34,7 +34,7 @@\n Guardian evaluator that consumes it (`shiki_guardian.evaluate_guardian_approval`,\n the `guardian_comment_carried` source) were built and tested in isolation. Neither\n does anything until a caller passes the flags that turn the carry on, so the\n-feature was invisible in CI. This ADR records the decision the two of them\n+feature was invisible in CI. This SADR records the decision the two of them\n implement, and this task wires it into the four CI invocations that judge Guardian\n approval.\n \ndiff --git a/docs/adr/README.md b/docs/adr/README.md\nindex c70e057a4520c36e042ab68c53cd4a21ef709b71..5da43856836e2ce6a150f8127766e2f88355ab4b\n--- a/docs/adr/README.md\n+++ b/docs/adr/README.md\n@@ -1,40 +1,41 @@\n-# Architecture Decision Records\n+# Decision Record Namespaces\n \n-This directory holds Shiki's Architecture Decision Records (ADRs): durable\n-records of hard-to-reverse platform decisions. ADRs are part of the Shiki source\n-of truth alongside GitHub state, the `.shiki/` mirror, and `CONTEXT.md`.\n+This directory holds two independent decision namespaces:\n \n-## How ADRs Work\n+- Shiki Architecture Decision Records (SADRs), named\n+  `SADR-NNNN-<short-slug>.md`, record hard-to-reverse Shiki platform decisions.\n+- Target Architecture Decision Records (ADRs), named\n+  `NNNN-<short-slug>.md`, belong to the target product.\n \n-- New ADRs are added as `docs/adr/NNNN-<short-slug>.md`, where `NNNN` is the next\n-  zero-padded sequence number.\n-- The **canonical list of ADRs is the set of files matching `docs/adr/*.md`** in\n-  this directory. Browse `docs/adr/*.md` for the authoritative, current set.\n-- Use [`template.md`](template.md) as the starting point for a new ADR.\n-- Record an ADR when a decision is hard to reverse, surprising without context,\n-  and a real tradeoff. Do not write an ADR for routine or easily reversible\n-  choices.\n+The same four-digit number may appear once in each namespace. A platform\n+`SADR-NNNN` citation resolves only to `SADR-NNNN-*.md`; a target `ADR NNNN`\n+citation resolves only to `NNNN-*.md`.\n \n-Using the `docs/adr/*.md` glob as the source of truth means other PRs can add an\n-ADR without editing this index file, avoiding merge contention on a\n-hand-maintained enumeration.\n+## How Decision Records Work\n \n-## Current ADRs (snapshot)\n-\n-The list below is a convenience snapshot and may lag behind the directory. When\n-in doubt, list `docs/adr/*.md`.\n+- New Shiki platform decisions use the next zero-padded SADR number.\n+- New target product decisions use the target's own ADR sequence.\n+- The canonical list is the files in `docs/adr/`; no hand-maintained index is\n+  authoritative.\n+- Use [`template.md`](template.md) as the starting point and choose the filename\n+  namespace that owns the decision.\n+- Record a decision when it is hard to reverse, surprising without context, and\n+  a real tradeoff. Do not write a decision record for routine or easily\n+  reversible choices.\n \n-- [0001 \u2014 Keep Shiki As The Platform Name](0001-keep-shiki-as-platform-name.md)\n-- [0002 \u2014 Use AGENTS As The Shared Agent Constitution](0002-use-agents-as-shared-constitution.md)\n-- [0003 \u2014 Require Skill Gate For MergeGate Readiness](0003-require-skill-gate-for-mergegate-readiness.md)\n-- [0004 \u2014 Use GitHub First With A Repository-Local Shiki Mirror](0004-use-github-first-with-shiki-mirror.md)\n-- [0005 \u2014 Use Codex As Front And Claude Code Action As The GitHub Runtime](0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md)\n-- [0006 \u2014 Separate Codex Implementation, CCA Completion, And MergeGate Authorization](0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md)\n+Using the directory as the source of truth lets independent changes add records\n+without contending on an enumerated index.\n \n-## Adding A New ADR\n+## Adding A Shiki SADR\n \n-1. Copy `template.md` to `docs/adr/NNNN-<short-slug>.md`.\n+1. Copy `template.md` to `docs/adr/SADR-NNNN-<short-slug>.md`.\n 2. Fill in the title, status, context, decision, and consequences.\n-3. Set the status to `Proposed`, then `Accepted` once the decision is approved.\n-4. Open a PR following `CONTRIBUTING.md`. You do not need to edit this index for\n-   the ADR to be discoverable via `docs/adr/*.md`.\n+3. Set the status to `Proposed`, then `Accepted` once the proper authority has\n+   approved the decision.\n+4. Open a PR following `CONTRIBUTING.md`.\n+\n+## Adding A Target ADR\n+\n+1. Copy `template.md` to `docs/adr/NNNN-<short-slug>.md` using the target's own\n+   sequence.\n+2. Cite it as `ADR NNNN`; do not prefix its filename with `SADR-`.\ndiff --git a/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md b/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\nnew file mode 100644\nindex 0000000000000000000000000000000000000000..7455ce1f3bed02ddddb851ac21a1d1a648c9aeba\n--- /dev/null\n+++ b/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\n@@ -0,0 +1,55 @@\n+# SADR-0019: Separate Shiki Platform SADRs From Target ADRs\n+\n+## Status\n+\n+Accepted\n+\n+## Context\n+\n+Shiki installs its platform decision records into `docs/adr/` in every target\n+repository. Shiki and its targets previously used the same numeric\n+`NNNN-<slug>.md` namespace. A target that already owned ADRs 0013 through 0018\n+therefore acquired two records for each number during installation, making an\n+`ADR NNNN` citation ambiguous. Renumbering target decisions would rewrite target\n+history, while renumbering Shiki decisions per target would destroy their stable\n+platform identity.\n+\n+Upgrades also need to remove the former Shiki-owned numeric paths without ever\n+mistaking a target-owned ADR for platform content. The install stamp already\n+records exact shipped paths and content digests, so it can prove ownership only\n+when both still match.\n+\n+## Decision\n+\n+Shiki platform decisions use the `SADR` namespace:\n+\n+- records are named `docs/adr/SADR-NNNN-<slug>.md`;\n+- live platform citations use `SADR-NNNN`;\n+- target product decisions retain `docs/adr/NNNN-<slug>.md` and `ADR NNNN`;\n+- the reference validator resolves the two namespaces independently, so the same\n+  four-digit number may exist once in each namespace.\n+\n+Before a forced upgrade writes anything, the installer inventories the exact\n+former numeric Shiki ADR paths. It may delete a legacy path only when the\n+existing install stamp contains that exact path and its recorded SHA-256 digest\n+matches the file currently on disk. All ownership blockers are reported\n+together and abort the upgrade before its first write. A non-forced install\n+never performs this cleanup.\n+\n+Historical `.shiki/` evidence remains unchanged because it records the namespace\n+that existed when that evidence was produced.\n+\n+## Consequences\n+\n+- Targets may use any ADR number without colliding with Shiki's platform SADRs.\n+- Fresh installs preserve target ADR bytes and install a stable SADR set.\n+- Forced upgrades fail closed when legacy ownership cannot be proven, and a\n+  successful upgrade refreshes the stamp without removed legacy paths.\n+- Current governance, packaging, workflow, prompt, script, and skill references\n+  must move with the platform records.\n+\n+## Alternatives Considered\n+\n+- Collision-only refusal leaves affected targets permanently unupgradable.\n+- Renumbering target ADRs mutates target-owned history.\n+- Per-target Shiki renumbering makes platform citations non-portable.\ndiff --git a/docs/adr/template.md b/docs/adr/template.md\nindex a92a97daddd0b00ab9dd626cc8d86942da2eafed..17dd79175b5fee2105935557f691b40cf9492942\n--- a/docs/adr/template.md\n+++ b/docs/adr/template.md\n@@ -1,14 +1,14 @@\n-# NNNN: <Short Decision Title>\n+# <SADR-NNNN or NNNN>: <Short Decision Title>\n \n ## Status\n \n-Proposed | Accepted | Superseded by ADR-NNNN\n+Proposed | Accepted | Superseded by SADR-NNNN or target ADR NNNN\n \n ## Context\n \n What is the situation, constraint, or force that requires a decision? Reference\n-relevant domain terms (`CONTEXT.md`), prior ADRs, and code reality. State the\n-problem, not the solution.\n+relevant domain terms (`CONTEXT.md`), prior Shiki SADRs or target ADRs, and code\n+reality. State the problem, not the solution.\n \n ## Decision\n \ndiff --git a/docs/agents/autonomous-loop-e2e-acceptance.md b/docs/agents/autonomous-loop-e2e-acceptance.md\nindex bbf84c6081987f159ac6ad9ecf3c82b7aa72e7e8..5c13c1d02a05a90b0dc46cbef7c95a9846d0c237\n--- a/docs/agents/autonomous-loop-e2e-acceptance.md\n+++ b/docs/agents/autonomous-loop-e2e-acceptance.md\n@@ -40,8 +40,8 @@\n 3. `gh auth status` is logged in with merge permission on the repo.\n 4. Branch protection + required checks are configured exactly as the goal-loop\n    required-check set expects (see `docs/agents/branch-protection-smoke.md`).\n-5. The repo `.shiki/policy` permits autonomous low/medium-risk merges (ADR\n-   0008/0009); the MergeGate policy check is the recorded-authority gate.\n+5. The repo `.shiki/policy` permits autonomous low/medium-risk merges\n+   (SADR-0008/0009); the MergeGate policy check is the recorded-authority gate.\n \n The operator's only actions are: register the task and start the loop. **No\n manual pre-clearing** means after `shiki loop run` starts, the operator does not\ndiff --git a/docs/agents/bootstrap-command.md b/docs/agents/bootstrap-command.md\nindex 05f7f203a52cf412b9f97084a6e131ed0f785afd..f7aa0b734dfebee4a3b139335b66ee3415f48713\n--- a/docs/agents/bootstrap-command.md\n+++ b/docs/agents/bootstrap-command.md\n@@ -189,7 +189,9 @@\n ```\n \n Use this only for tests, fixtures, or explicit local-only template inspection.\n-Use `--force` only when you intentionally want to overwrite existing target files.\n+Use `--force` only when you intentionally want to refresh the target template.\n+Legacy numeric Shiki decision cleanup is stamp-and-digest authorized before any\n+write; target ADR paths are not cleanup candidates.\n \n ## Slash Command\n \ndiff --git a/docs/agents/checklists.md b/docs/agents/checklists.md\nindex 4a7f03ade346f53a1c1d82981f3fd744f9ab4547..0c5ec6c53dc7197b7b7a7d18e667b325babd6c45\n--- a/docs/agents/checklists.md\n+++ b/docs/agents/checklists.md\n@@ -32,18 +32,18 @@\n | ID | Item | Blocking |\n | --- | --- | --- |\n | GD-01 | Plan was challenged against `CONTEXT.md`. | blocking for non-trivial Goals |\n-| GD-02 | Relevant ADRs were read or absence was recorded. | blocking for non-trivial Goals |\n+| GD-02 | Relevant SADRs and target ADRs were read or absence was recorded. | blocking for non-trivial Goals |\n | GD-03 | Ambiguous terms were resolved or converted to HITL blockers. | blocking |\n | GD-04 | Edge scenarios were considered. | advisory |\n | GD-05 | Code reality was checked when code could answer. | blocking when applicable |\n | GD-06 | New domain terms were added to `CONTEXT.md` when needed. | blocking when applicable |\n-| GD-07 | ADR was created or explicitly skipped using the ADR criteria. | blocking when applicable |\n+| GD-07 | A SADR or target ADR was created or explicitly skipped using the decision-record criteria. | blocking when applicable |\n \n ## CI \u2014 Context & Impact Checklist\n \n | ID | Item | Blocking |\n | --- | --- | --- |\n-| CI-01 | Relevant docs and ADRs identified. | blocking |\n+| CI-01 | Relevant docs, SADRs, and target ADRs identified. | blocking |\n | CI-02 | Relevant modules, interfaces, seams, callers, and tests identified. | blocking |\n | CI-03 | Dependencies and blocked-by relationships identified. | blocking |\n | CI-04 | Candidate locks identified. | blocking |\n@@ -62,7 +62,7 @@\n | PRD-04 | Implementation decisions are recorded without volatile code detail. | blocking |\n | PRD-05 | Testing decisions are recorded. | blocking |\n | PRD-06 | Out-of-scope items are explicit. | blocking |\n-| PRD-07 | Relevant glossary/ADR links are present when applicable. | blocking |\n+| PRD-07 | Relevant glossary/SADR/target-ADR links are present when applicable. | blocking |\n \n ## SF \u2014 Spec Freeze Checklist\n \n@@ -98,7 +98,7 @@\n | H-02 | Branch/worktree target is present. | blocking |\n | H-03 | Scope and non-goals are present. | blocking |\n | H-04 | Dependencies and locks are present. | blocking |\n-| H-05 | Relevant docs, ADRs, modules, and tests are referenced. | blocking |\n+| H-05 | Relevant docs, SADRs, target ADRs, modules, and tests are referenced. | blocking |\n | H-06 | Required skills are stated. | blocking |\n | H-07 | TDD expectations are stated. | blocking when behavior work |\n | H-08 | Acceptance criteria are included. | blocking |\ndiff --git a/docs/agents/codex-handoff.md b/docs/agents/codex-handoff.md\nindex ede468f47cb277f69a857e8c45df703e4ed0151d..6f97bdffb67e44d7edea2c71e01ea28933c4eb6c\n--- a/docs/agents/codex-handoff.md\n+++ b/docs/agents/codex-handoff.md\n@@ -46,7 +46,7 @@\n \n ## Context\n - Domain terms:\n-- Relevant ADRs:\n+- Relevant SADRs and target ADRs:\n - Relevant docs:\n - Relevant modules/interfaces/seams:\n - Similar tests/prior art:\ndiff --git a/docs/agents/completion-check-agent.md b/docs/agents/completion-check-agent.md\nindex 13e4c035db35025f4e3cdcc5adfb24314ac4d521..61bfa1218cf36ca605250c91919e620f33238aa0\n--- a/docs/agents/completion-check-agent.md\n+++ b/docs/agents/completion-check-agent.md\n@@ -32,7 +32,7 @@\n - `AGENTS.md`\n - `CLAUDE.md`\n - `CONTEXT.md`\n-- relevant ADRs\n+- relevant SADRs and target ADRs\n - parent Goal issue\n - PRD issue or document\n - task issue\ndiff --git a/docs/agents/decision-control.md b/docs/agents/decision-control.md\nindex 7d8ee5ab03a5ccbcbd04056ae61de03785a2a9c9..8c94ecda00fffaa0e92c325ab83aa9776bd1dac1\n--- a/docs/agents/decision-control.md\n+++ b/docs/agents/decision-control.md\n@@ -11,7 +11,7 @@\n | Decision | Owner | When | Required inputs | Output | Enforcement surface |\n | --- | --- | --- | --- | --- | --- |\n | Is the Goal clear enough to plan? | Planner + Guardian when needed | Goal intake | User request, existing docs, risk signals | Goal readiness verdict | Goal checklist, issue template, ledger |\n-| Must `grill-with-docs` run? | Planner / Skill Gate | Before PRD or issue split | Ambiguity, domain terms, ADR conflicts, unclear boundaries | Skill invocation or explicit skip rationale | Skill Gate checklist |\n+| Must `grill-with-docs` run? | Planner / Skill Gate | Before PRD or issue split | Ambiguity, domain terms, SADR/target-ADR conflicts, unclear boundaries | Skill invocation or explicit skip rationale | Skill Gate checklist |\n | Is a PRD required? | Planner | After Goal context stabilizes | Goal, `grill-with-docs` output, Context & Impact output | PRD required / not required | PRD checklist, CCA evidence check |\n | Can the spec be frozen? | Operator only | End of Requirements Definition, before issue split | PRD/requirements summary, scope inventory (SF-02), open-question list | `spec_freeze` block in the plan + ledger evidence | `require_grilled_plan` (plan ingest/run), SF checklist |\n | Can a frozen spec change? | Operator only (Spec Amendment) | When implementation contradicts the frozen spec | Contested decisions, scoped re-grill result | recorded amendment + re-stamped freeze | SF checklist, ledger evidence |\ndiff --git a/docs/agents/domain.md b/docs/agents/domain.md\nindex 258da8ea81e87ac0a6b0b5b5e5c2fde980eaabb7..d57b0cf17ff3e1fa9e6e5885372bf083d8295421\n--- a/docs/agents/domain.md\n+++ b/docs/agents/domain.md\n@@ -5,7 +5,9 @@\n ## Layout\n \n - `CONTEXT.md` defines Shiki language. Keep it implementation-free.\n-- `docs/adr/` records hard-to-reverse decisions.\n+- `docs/adr/SADR-NNNN-*.md` records Shiki platform decisions.\n+- `docs/adr/NNNN-*.md` records target product decisions without sharing the\n+  SADR namespace.\n - `AGENTS.md` is the runtime-neutral agent constitution.\n - `CLAUDE.md` is the Claude Code wrapper around `AGENTS.md`.\n - `.shiki/` is the Target Repository mirror for Goals, PRDs, plans, Task DAGs, contracts, locks, worktree records, ledger entries, CCA verdicts, repair packets, reports, and handoffs.\n@@ -15,6 +17,6 @@\n \n - Use Shiki terms exactly when they exist.\n - Add glossary terms to `CONTEXT.md` only when the term is part of the domain, not a generic programming concept.\n-- Create ADRs only for hard-to-reverse, surprising, tradeoff-based decisions.\n+- Create SADRs or target ADRs only for hard-to-reverse, surprising, tradeoff-based decisions.\n - Keep product-specific language in the Target Repository, not in Shiki Template docs.\n-- If an ADR conflicts with a proposed implementation, surface the conflict before editing.\n+- If a SADR or target ADR conflicts with a proposed implementation, surface the conflict before editing.\ndiff --git a/docs/agents/execution-control.md b/docs/agents/execution-control.md\nindex ad8ee3cdb36be56fedc1f46408ca92b387d74047..bc63c3a73661d12b567016ba299f1a6b66bf9088\n--- a/docs/agents/execution-control.md\n+++ b/docs/agents/execution-control.md\n@@ -63,7 +63,7 @@\n | Branch name | Orchestrator | Orchestrator | Before implementer dispatch | branch policy | branch ref in task/ledger |\n | Lock reservation | Context & Impact | MergeGate | Before dispatch and before merge | `.shiki/locks/`, labels, PR metadata | lock id, owner, scope |\n | CI profile | Planner | MergeGate | During issue triage, rechecked at PR | labels, `.shiki/policy.yaml`, workflow matrix | required check list |\n-| Required status checks | Repo maintainer / Guardian | GitHub branch protection / rulesets | Repository setup, updated via ADR | GitHub settings | branch/ruleset config, ADR |\n+| Required status checks | Repo maintainer / Guardian | GitHub branch protection / rulesets | Repository setup, updated via target ADR | GitHub settings | branch/ruleset config, target ADR |\n | CCA judgment | CCA | GitHub required check / MergeGate | Every non-draft PR update | GitHub Action check | structured CCA verdict |\n | Repair assignment | CCA | MergeGate | After failed CCA/CI/review | repair packet, issue/PR comment | repair id and attempt count |\n | Merge readiness | MergeGate | GitHub branch protection / Guardian if needed | After CCA complete | required checks, review rules, rulesets | mergegate check output |\ndiff --git a/docs/agents/guardian-policy.md b/docs/agents/guardian-policy.md\nindex 4147c7adf05a47adb4fd24ada1a1c2e1f2ba6d2d..a20d9cc6c00a569bb057b9ca8d6b51c7fc4a9847\n--- a/docs/agents/guardian-policy.md\n+++ b/docs/agents/guardian-policy.md\n@@ -98,8 +98,8 @@\n `origin/<default_branch>` and reuses each job's existing base fetch; a shallow\n (`--depth`) or second base fetch would make the proof unresolvable and silently\n disable the carry, so neither is permitted. CCA reads `guardian_comment_carried` as\n-a recorded authority in the deterministic signal and does not re-derive it. See ADR\n-0018 for the full decision, the rejected diff/patch-equality and path-disjointness\n+a recorded authority in the deterministic signal and does not re-derive it. See\n+SADR-0018 for the full decision, the rejected diff/patch-equality and path-disjointness\n alternatives, and the accepted residual.\n \n ## External AI Guardian Review (SADR-0010)\ndiff --git a/docs/agents/implementation-policy.md b/docs/agents/implementation-policy.md\nindex c754144a9fb5bf4a38aa82a60e1670c59e626b44..85c37281bcad196c6b7b02343faf00b03cd9c57a\n--- a/docs/agents/implementation-policy.md\n+++ b/docs/agents/implementation-policy.md\n@@ -31,10 +31,13 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n ## Non-Negotiables\n \n 1. **Planning precedes implementation.** Non-trivial Goals start with `grill-with-docs` and Context & Impact.\n-2. **Domain language is controlled.** Terms must be checked against `CONTEXT.md`; hard-to-reverse tradeoffs must be checked against ADRs.\n+2. **Domain language is controlled.** Terms must be checked against `CONTEXT.md`; hard-to-reverse tradeoffs must be checked against Shiki SADRs and target ADRs.\n 3. **PRD precedes issue decomposition.** `to-prd` is used once the context is settled enough to become durable intent.\n 4. **Issues are vertical slices.** `to-issues` must produce independently grabbable tracer-bullet tasks, not horizontal layer tickets.\n 5. **Triage controls dispatch.** Only `ready-for-agent` / AFK tasks may be assigned to Codex without human input.\n@@ -95,19 +98,19 @@\n Process:\n \n - Challenge terms against `CONTEXT.md`.\n-- Check ADRs before reopening decisions.\n+- Check SADRs and target ADRs before reopening decisions.\n - Inspect code when code can answer a question.\n - Ask one question at a time when the operator is available.\n - Provide a recommended answer with each question.\n - Record resolved terms in `CONTEXT.md`.\n-- Create ADRs only for hard-to-reverse, surprising, tradeoff-based decisions.\n+- Create SADRs or target ADRs only for hard-to-reverse, surprising, tradeoff-based decisions.\n - Convert unresolved load-bearing questions into HITL blockers.\n \n Outputs:\n \n - Resolved design-tree decisions.\n - Updated glossary terms when needed.\n-- ADRs when justified.\n+- SADRs or target ADRs when justified.\n - HITL blockers when decisions remain unresolved.\n \n ## Phase 3 \u2014 Context & Impact\n@@ -117,7 +120,7 @@\n \n Outputs must include:\n \n-- Relevant documents and ADRs.\n+- Relevant documents, SADRs, and target ADRs.\n - Relevant modules, interfaces, seams, callers, and tests.\n - Candidate locks.\n - Dependencies.\n@@ -140,7 +143,7 @@\n - Testing decisions.\n - Out of scope.\n - Further notes.\n-- Links to relevant Goal, domain terms, and ADRs.\n+- Links to relevant Goal, domain terms, SADRs, and target ADRs.\n \n Phase 4 ends with Spec Freeze: the operator explicitly approves the PRD, the\n plan records a `spec_freeze` block (status, approved_by, source), and the SF\n@@ -240,7 +243,7 @@\n - Dependencies and locks.\n - Risk and Guardian requirements.\n - Ledger completeness.\n-- Documentation and ADR updates.\n+- Documentation and SADR/target-ADR updates.\n - Scope drift.\n \n CCA emits one of:\n@@ -301,7 +304,7 @@\n | Evidence failure | Behavior might be done, but proof is missing. | Add checks, PR evidence, ledger entry, or artifacts. |\n | Scope drift | PR changes unrelated behavior. | Revert unrelated changes or split into new Goal. |\n | Dependency block | Upstream task or lock not complete. | Block until dependency resolves. |\n-| Architecture block | Decision needs ADR or deep-module discussion. | `grill-with-docs` or `improve-codebase-architecture`. |\n+| Architecture block | Decision needs a SADR, target ADR, or deep-module discussion. | `grill-with-docs` or `improve-codebase-architecture`. |\n | Risk block | Security/production/policy change needs human approval. | Guardian decision. |\n \n ## Phase 5.5 \u2014 Execution Decision Control\ndiff --git a/docs/agents/runtime-auth-model.md b/docs/agents/runtime-auth-model.md\nindex 6a2e1a47e81ea89b5c68a210ec07772a9374babc..3c54748d24233dbd0ba8473950282547ef4ce935\n--- a/docs/agents/runtime-auth-model.md\n+++ b/docs/agents/runtime-auth-model.md\n@@ -148,7 +148,7 @@\n \n API-key based runners may be added by a target repository as an explicit extension. They are not the default Shiki template path.\n \n-If a repo opts into API-key mode, record the exception in an ADR and update the repo's `AGENTS.md`, `.shiki/config.yaml`, and workflow permissions.\n+If a repo opts into API-key mode, record the target-specific exception in a target ADR and update the repo's `AGENTS.md`, `.shiki/config.yaml`, and workflow permissions.\n \n ## GitHub Actions Runtime Compatibility\n \ndiff --git a/docs/agents/skill-gate.md b/docs/agents/skill-gate.md\nindex c50398b0d7c60a8598f2a5c88a10015730dced13..3acffe6444617bb1d54b77d4b4708158ec735bc6\n--- a/docs/agents/skill-gate.md\n+++ b/docs/agents/skill-gate.md\n@@ -9,7 +9,7 @@\n | Skill | Trigger | Expected output |\n | --- | --- | --- |\n | `setup-matt-pocock-skills` | First configuring a repo for these skills, or when issue tracker, triage labels, or domain docs are missing. | `docs/agents/` configuration and an `Agent skills` block in `AGENTS.md` or `CLAUDE.md`. |\n-| `grill-with-docs` | Ambiguous plan, unclear terminology, boundary decision, tradeoff, or ADR-worthy design choice. | Clarified decision tree, recommended answers, updates to `CONTEXT.md` or ADRs when decisions crystallize. |\n+| `grill-with-docs` | Ambiguous plan, unclear terminology, boundary decision, tradeoff, or decision-record-worthy design choice. | Clarified decision tree, recommended answers, updates to `CONTEXT.md`, SADRs, or target ADRs when decisions crystallize. |\n | `zoom-out` | Agent lacks a higher-level architectural map. | Map of relevant modules, callers, seams, and domain concepts. |\n | `to-prd` | Settled context should become a PRD. | PRD published to the issue tracker with problem, solution, user stories, decisions, testing, and out-of-scope notes. |\n | `to-issues` | Goal, PRD, or plan must become implementation tickets. | Vertical-slice issues with acceptance criteria, dependencies, and AFK/HITL classification. |\n@@ -17,7 +17,7 @@\n | `tdd` | Feature work or bug fix has observable behavior that can be specified with tests. | Red-green-refactor loop through public interfaces, one vertical slice at a time. |\n | `code-review` | The TDD loop is green and the implementer is about to create or update a PR. Default for every implementation task. | Pre-PR self-review of the task diff with fixes applied on the task branch; findings and resolutions recorded as a ledger entry naming the skill and a `## Pre-PR code review` PR body section. |\n | `diagnose` | Hard bug, failing check, regression, flaky behavior, or performance problem. | Reproduction loop, ranked hypotheses, instrumentation, fix, regression test, post-mortem. |\n-| `improve-codebase-architecture` | Structural friction, testability, AI-navigability, deep-module opportunity, or refactor request. | Architecture candidates and deepening recommendations informed by `CONTEXT.md` and ADRs. |\n+| `improve-codebase-architecture` | Structural friction, testability, AI-navigability, deep-module opportunity, or refactor request. | Architecture candidates and deepening recommendations informed by `CONTEXT.md`, SADRs, and target ADRs. |\n | `prototype` | A throwaway logic or UI prototype is needed to answer a design question. | Clearly marked disposable prototype and captured decision. |\n \n ## MergeGate Interaction\ndiff --git a/docs/operations.md b/docs/operations.md\nindex cf1f452d4c589e38ccd58553bda0ad8684b51eb7..ace48a90ffbec9e5c0a7113bef16a4858bc44b3f\n--- a/docs/operations.md\n+++ b/docs/operations.md\n@@ -89,6 +89,16 @@\n      target history, brought forward by `shiki migrate`, not a template.\n    - **Everything else** (CLI scripts, workflows, schemas, docs) is overwritten.\n \n+   A forced upgrade also migrates the former numeric Shiki decision paths to\n+   `SADR-NNNN-*`. Before the first write, the installer inventories the exact\n+   legacy Shiki paths and authorizes each deletion only when the existing\n+   `.shiki/install-stamp.json` names that path and its SHA-256 digest still\n+   matches the file. An absent, unreadable, malformed, incomplete, or\n+   mismatching stamp reports every blocked path and stops with no filesystem\n+   change. Target-owned `NNNN-*` ADRs are not cleanup candidates. A non-force\n+   install deletes nothing and retains any still-matching legacy stamp entries\n+   so a later forced upgrade can prove ownership.\n+\n    If the target has pending migrations, `--force` refuses and writes nothing \u2014\n    apply migrations first (step 3), then re-run. The run ends with a single\n    summary of every `.new` written, so a half-upgrade is visible.\ndiff --git a/docs/releasing.md b/docs/releasing.md\nindex 10d73828e7dbd64e9d9f6b83bc84d010537ce62a..aafeee8c4d441e1e427efee2ce4f81a647f020e1\n--- a/docs/releasing.md\n+++ b/docs/releasing.md\n@@ -67,7 +67,7 @@\n CLI in `scripts/`. There are no third-party Python runtime dependencies. The CLI\n is intentionally **not** exposed as a `[project.scripts]` console entry point\n because `scripts/shiki.py` is a dependency-free shim and `scripts/` is not\n-packaged as an importable distribution (see the ADR for the rationale).\n+packaged as an importable distribution (see SADR-0007 for the rationale).\n \n Supported install paths:\n \n@@ -155,6 +155,15 @@\n    [docs/operations.md](operations.md#the-new-convention). If the target has\n    pending migrations, `--force` refuses and writes nothing; apply migrations\n    (step 2) first, then re-run.\n+\n+   The SADR namespace upgrade has an additional pre-write gate: each former\n+   numeric Shiki decision path is removed only when the existing install stamp\n+   contains that exact path and a digest matching its current bytes. The\n+   installer aggregates absent/unreadable/malformed-stamp, missing-digest, and\n+   digest-mismatch blockers before raising, so refusal leaves the target tree\n+   byte-identical. Numeric target ADRs are preserved. A successful forced\n+   upgrade installs and stamps `SADR-*` paths and drops the removed legacy paths\n+   from the refreshed stamp.\n 2. Apply repository-local `.shiki/` state migrations with the migrate command:\n \n    ```bash\ndiff --git a/docs/usage.md b/docs/usage.md\nindex b83407ee3970dd395dd893010bc5dbe8bad26051..a130ef6e40dfd8169a5bc701339351097df1a09f\n--- a/docs/usage.md\n+++ b/docs/usage.md\n@@ -67,8 +67,10 @@\n shiki install-target /path/to/target-repo --local-only\n ```\n \n-Use `--force` only when you intentionally want to overwrite existing target\n-files.\n+Use `--force` only when you intentionally want to refresh the target template.\n+For the SADR namespace migration, `--force` deletes only exact legacy Shiki\n+decision paths whose existing install-stamp digest still matches; any ownership\n+blocker stops before the first write. Numeric target ADRs are never candidates.\n \n ## `shiki bootstrap-platform` \u2014 publish the Shiki platform repo\n \ndiff --git a/scripts/guardian_approval_signal.py b/scripts/guardian_approval_signal.py\nindex d059f92b2c82aa2b5d6ca421365537a87886c1cb..16924892714f4f5b9303c0ef0e325a7e70075630\n--- a/scripts/guardian_approval_signal.py\n+++ b/scripts/guardian_approval_signal.py\n@@ -199,8 +199,8 @@\n ) -> Any:\n     \"\"\"Evaluate SADR-0015 Contract Approval for the CCA Guardian signal.\n \n-    The SAME OR that MergeGate applies must reach this deterministic signal: ADR\n-    0015 is explicit that without it, a contract-approved PR still returns\n+    The SAME OR that MergeGate applies must reach this deterministic signal:\n+    SADR-0015 is explicit that without it, a contract-approved PR still returns\n     ``needs_guardian``, the CCA job fails, and the MergeGate policy check never\n     runs \u2014 the gate this change moves would never be reached. This resolves the\n     task the SAME way MergeGate does (the first Shiki task id in the PR body),\ndiff --git a/scripts/shiki_contract_approval.py b/scripts/shiki_contract_approval.py\nindex 941bcb96fdd8b9a85bc0c4f0f04d5dad9d04839c..4a4b418f6527413161db82670bec857a043360e2\n--- a/scripts/shiki_contract_approval.py\n+++ b/scripts/shiki_contract_approval.py\n@@ -142,7 +142,7 @@\n     changed_files_status: Any,\n     registration: Any,\n ) -> ContractApprovalResult:\n-    \"\"\"Whether this PR carries an SADR-0015 Contract Approval.\n+    \"\"\"Whether this PR carries a SADR-0015 Contract Approval.\n \n     The four conditions are evaluated IN ORDER, each failure recorded as a\n     distinct reason string:\ndiff --git a/scripts/shiki_guardian.py b/scripts/shiki_guardian.py\nindex a4ecb79bbda55fd5c8b06adfae2eb99760a03c45..4dd99bbe714e26f421fa19abac7a34ae1b195ecc\n--- a/scripts/shiki_guardian.py\n+++ b/scripts/shiki_guardian.py\n@@ -12,7 +12,7 @@\n # The carry re-derives its proof from git in-process on every judging invocation;\n # it is never read from an artifact, so there is nothing to go stale or be forged.\n # shiki_sync_proof is the ONLY guardian-family module that runs git, and every one\n-# of its invocations is read-only and local (ADR: prove-from-git base sync).\n+# of its invocations is read-only and local (SADR: prove-from-git base sync).\n from shiki_sync_proof import verify_pure_base_sync\n \n GUARDIAN_POLICY_PATH = \".shiki/guardian-policy.json\"\ndiff --git a/scripts/shiki_guardian_review.py b/scripts/shiki_guardian_review.py\nindex dd0f104d457899e0ba5ff1e162011c1e90b6f92b..e10ef58c046324e7ea3a3392feb3048ca77414ce\n--- a/scripts/shiki_guardian_review.py\n+++ b/scripts/shiki_guardian_review.py\n@@ -159,7 +159,7 @@\n     docs_only = _is_docs_only(changed_files)\n     if docs_only and matched == 0:\n         focus.append(\n-            \"Docs/ADR-only change: confirm the change is genuinely documentation/decision-only, introduces no behavior or contract drift, and stays consistent with the constitution and existing ADRs.\"\n+            \"Docs/decision-record-only change: confirm the change is genuinely documentation/decision-only, introduces no behavior or contract drift, and stays consistent with the constitution and existing SADRs and target ADRs.\"\n         )\n \n     # Conservative augmentation: unknown (no category and not docs-only), mixed\ndiff --git a/scripts/shiki_installer.py b/scripts/shiki_installer.py\nindex be064ab7a29ff6836f7f7a1d70500a43e0d11d7b..9fd8eefd137ce7003dad5c0dcb2e48e2cd47ab32\n--- a/scripts/shiki_installer.py\n+++ b/scripts/shiki_installer.py\n@@ -118,6 +118,7 @@\n # what lets a target carry it without a governance-schema change.\n INSTALL_STAMP_PATH = \".shiki/install-stamp.json\"\n INSTALL_STAMP_VERSION = 1\n+LEGACY_SHIKI_SADR_MAX = 18\n \n # Under --force the shipped surface splits three ways, so nothing is lost and\n # nothing is silently stale.\n@@ -237,9 +238,98 @@\n         )\n \n \n+def legacy_shiki_adr_paths() -> tuple[str, ...]:\n+    \"\"\"Former numeric paths for Shiki SADR-0001 through SADR-0018.\n+\n+    The authoritative slugs come from the current SADR filenames. SADR-0019 is\n+    the namespace decision itself and never shipped under a numeric legacy path.\n+    \"\"\"\n+    paths: list[str] = []\n+    for sadr in sorted((ROOT / \"docs\" / \"adr\").glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\")):\n+        number = sadr.name.split(\"-\", 2)[1]\n+        if int(number) <= LEGACY_SHIKI_SADR_MAX:\n+            paths.append(f\"docs/adr/{sadr.name.removeprefix('SADR-')}\")\n+    return tuple(paths)\n+\n+\n+def preflight_legacy_sadr_cleanup(target: Path, *, force: bool) -> tuple[str, ...]:\n+    \"\"\"Authorize exact legacy Shiki ADR removals before the first write.\n+\n+    A non-force install never removes existing files. A forced install needs no\n+    stamp when no legacy Shiki path is present. When legacy paths do exist, each\n+    must be named in a readable install stamp and its current bytes must match\n+    the recorded digest.\n+    \"\"\"\n+    if not force:\n+        return ()\n+    present = tuple(\n+        relative\n+        for relative in legacy_shiki_adr_paths()\n+        if (target / relative).exists() or (target / relative).is_symlink()\n+    )\n+    if not present:\n+        return ()\n+\n+    stamp_path = target / INSTALL_STAMP_PATH\n+    try:\n+        raw = stamp_path.read_text(encoding=\"utf-8\")\n+        stamp = json.loads(raw)\n+    except FileNotFoundError:\n+        detail = \"install stamp is absent\"\n+        blockers = [f\"{relative}: {detail}\" for relative in present]\n+    except (OSError, UnicodeDecodeError) as error:\n+        detail = f\"install stamp is unreadable ({error})\"\n+        blockers = [f\"{relative}: {detail}\" for relative in present]\n+    except json.JSONDecodeError as error:\n+        detail = f\"install stamp is malformed ({error})\"\n+        blockers = [f\"{relative}: {detail}\" for relative in present]\n+    else:\n+        if not isinstance(stamp, dict):\n+            blockers = [\n+                f\"{relative}: install stamp is malformed (expected an object)\"\n+                for relative in present\n+            ]\n+        else:\n+            digests = stamp.get(\"digests\")\n+            blockers = []\n+            if not isinstance(digests, dict):\n+                blockers.extend(\n+                    f\"{relative}: install stamp has no digest for this path\"\n+                    for relative in present\n+                )\n+            else:\n+                for relative in present:\n+                    expected = digests.get(relative)\n+                    if not isinstance(expected, str) or not expected:\n+                        blockers.append(\n+                            f\"{relative}: install stamp has no digest for this path\"\n+                        )\n+                        continue\n+                    try:\n+                        actual = _sha256_file(target / relative)\n+                    except OSError as error:\n+                        blockers.append(\n+                            f\"{relative}: legacy path is unreadable ({error})\"\n+                        )\n+                        continue\n+                    if actual != expected:\n+                        blockers.append(\n+                            f\"{relative}: digest mismatch (stamp {expected}, current {actual})\"\n+                        )\n+\n+    if blockers:\n+        joined = \"\\n\".join(f\"- {blocker}\" for blocker in blockers)\n+        raise ShikiError(\n+            \"legacy Shiki ADR cleanup is not authorized:\\n\"\n+            f\"{joined}\\nNo files were written.\"\n+        )\n+    return present\n+\n+\n def install_template(target: Path, *, force: bool, validate: bool) -> list[NewFileNote]:\n     # Preconditions first: never rewrite the tree of a target that cannot be\n     # upgraded cleanly (see refuse_pending_migrations).\n+    legacy_cleanup = preflight_legacy_sadr_cleanup(target, force=force)\n     refuse_pending_migrations(target)\n \n     # Resolve the target's required CODEOWNERS owner once from its\n@@ -250,6 +340,10 @@\n     # verbatim. See ``codeowners_required_owner``.\n     codeowners_owner = codeowners_required_owner(target)\n \n+    for relative in legacy_cleanup:\n+        (target / relative).unlink()\n+        info(f\"removed stamped legacy Shiki ADR: {target / relative}\")\n+\n     notes: list[NewFileNote] = []\n     for relative in TEMPLATE_PATHS:\n         source = ROOT / relative\n@@ -647,6 +741,22 @@\n     digests = target_stamp_digests(target)\n     existing = read_install_stamp(target)\n     if existing is not None:\n+        # A non-force install deliberately leaves legacy Shiki ADRs in place.\n+        # Preserve only still-matching ownership proof so a later forced\n+        # upgrade can authorize cleanup; never carry a missing or stale digest.\n+        existing_digests = existing.get(\"digests\")\n+        if isinstance(existing_digests, dict):\n+            for relative in legacy_shiki_adr_paths():\n+                expected = existing_digests.get(relative)\n+                path = target / relative\n+                if not isinstance(expected, str) or not path.is_file():\n+                    continue\n+                try:\n+                    actual = _sha256_file(path)\n+                except OSError:\n+                    continue\n+                if actual == expected:\n+                    digests[relative] = actual\n         if _incoming_is_older(commit, existing.get(\"platform_commit\")):\n             warn(\n                 \"kept existing install stamp: incoming install \"\ndiff --git a/scripts/shiki_loop.py b/scripts/shiki_loop.py\nindex 3611641da8aeb5038b1da90e3114b09a473ec6d8..84caa491b9cad0edc4176f209c13e9221e975681\n--- a/scripts/shiki_loop.py\n+++ b/scripts/shiki_loop.py\n@@ -424,7 +424,7 @@\n         # unresolvable CCA verdict has already produced its terminal stop and is never\n         # laundered into a head-moving sync (SADR-0010). It also never fires while the\n         # policy (Guardian) gate is red \u2014 for ANY reason, not only when it is the sole\n-        # red check \u2014 so a head-bound Guardian approval, or an SADR-0015\n+        # red check \u2014 so a head-bound Guardian approval, or a SADR-0015\n         # contract-approval carry that base movement defeated (the policy gate compares\n         # base and head task governance directly), is never silently restored by\n         # adopting base wholesale. It is bounded by a durable per-task counter so the\ndiff --git a/scripts/shiki_tasks.py b/scripts/shiki_tasks.py\nindex aec3809f24b4a0c8c96fc50971cff9ae9ad30381..d531052e9d9717058d4631483cefab44c26a65c1\n--- a/scripts/shiki_tasks.py\n+++ b/scripts/shiki_tasks.py\n@@ -821,7 +821,7 @@\n         \"prompt\": prompt,\n         \"entry_skill\": \"grill-with-docs\",\n         \"required_next_steps\": [\n-            \"Run grill-with-docs until terminology, boundaries, risks, and ADR-worthy decisions are settled.\",\n+            \"Run grill-with-docs until terminology, boundaries, risks, and decision-record-worthy choices are settled.\",\n             \"Obtain the operator's explicit approval of the PRD/requirements (Spec Freeze).\",\n             \"Write a machine-readable plan JSON with grill_with_docs.status=complete and spec_freeze.status=frozen.\",\n             \"Run shiki plan ingest --plan-file PLAN.json.\",\ndiff --git a/scripts/test_shiki_goal_loop.sh b/scripts/test_shiki_goal_loop.sh\nindex 753c7b516e10f0be3bc92a4bd0eb1fa699776ae6..8f3820a0a079b3044424cfe1a6c6b7fe68896e67\n--- a/scripts/test_shiki_goal_loop.sh\n+++ b/scripts/test_shiki_goal_loop.sh\n@@ -231,8 +231,8 @@\n \n # High/critical risk merges autonomously when all required checks are green:\n # the \"MergeGate policy check\" required check IS the Guardian gate (it enforces\n-# guardian-policy.json \u2014 human approval OR external AI guardian review, ADR\n-# 0010), so green checks mean a recorded authority approved. The fake gh returns\n+# guardian-policy.json \u2014 human approval OR external AI guardian review,\n+# SADR-0010), so green checks mean a recorded authority approved. The fake gh returns\n # green checks, simulating that the Guardian gate passed.\n python3 \"$ROOT/scripts/shiki.py\" goal create --target \"$TARGET\" --title \"Guardian gate\" --outcome \"High risk merges via policy gate\" >/tmp/shiki-goal-loop-guardian-goal.json\n GGOAL=\"$(json_get /tmp/shiki-goal-loop-guardian-goal.json goal_id)\"\ndiff --git a/tests/test_adr_references.py b/tests/test_adr_references.py\nindex 51706329c797d5dd9b47e082a333fa83b261d482..d005da4f771f5e8085ec22d4b787980e68f5ae8c\n--- a/tests/test_adr_references.py\n+++ b/tests/test_adr_references.py\n@@ -1,8 +1,8 @@\n-\"\"\"Every ``ADR ####`` citation must resolve to exactly one decision record.\n+\"\"\"Every target ADR and Shiki SADR citation resolves in its own namespace.\n \n Background. ``scripts/mergegate_check.py`` justifies ``is_bookkeeping_closeout``\n-with \"ADR 0017\" nine times and ``scripts/guardian_approval_signal.py`` cites\n-0016/0017 seven times, yet for a stretch the numbering in ``docs/adr/`` stopped\n+with \"SADR-0017\" nine times and ``scripts/guardian_approval_signal.py`` cites\n+SADR-0016/SADR-0017 seven times, yet for a stretch the numbering in ``docs/adr/`` stopped\n at 0015: the authoring commits lived on a coordinator branch and no task ever\n declared ``docs/adr/...`` in its locks, so nothing carried the records to the\n default branch. The stated basis of an exemption that merged seven PRs with zero\n@@ -11,11 +11,12 @@\n and the directory drifted with no signal.\n \n This is the same unbound-pair shape behind other silent failures: a reference\n-and its target with nothing asserting they agree. This test binds them. It scans\n-every ``scripts/*.py`` (plus ``docs/**/*.md`` and the root ``*.md`` governance\n-files, which are cheap) for the ``ADR ####`` pattern and asserts each cited\n-number resolves to exactly one ``docs/adr/####-*.md``. The converse is\n-deliberately NOT required: an ADR may exist without being cited.\n+and its target with nothing asserting they agree. This test binds them. Target\n+``ADR NNNN`` citations resolve only to ``docs/adr/NNNN-*.md``; Shiki\n+``SADR-NNNN`` citations resolve only to ``docs/adr/SADR-NNNN-*.md``. The scan\n+covers current governance docs, scripts, workflows, prompts, packaging metadata,\n+and repo-local Codex guidance while deliberately excluding historical\n+``.shiki`` evidence. The converse is not required: a record may be uncited.\n \n The suite is dependency free: standard-library ``unittest`` only.\n \"\"\"\n@@ -31,17 +32,18 @@\n \n REPO_ROOT = Path(__file__).resolve().parents[1]\n \n-# \"ADR 0016\" -- a four-digit citation. The trailing \\b keeps a five-digit token\n-# from being read as a truncated four-digit number.\n-_CITATION = re.compile(r\"\\bADR (\\d{4})\\b\")\n-# \"0016-locks-bound-product-paths.md\" -- the leading four digits name the record.\n-_ADR_FILENAME = re.compile(r\"^(\\d{4})-.*\\.md$\")\n+# ``ADR 0016`` / ``SADR-0016`` -- a namespace plus four-digit citation. The\n+# trailing ``\\b`` keeps a five-digit token from being read as a truncated one.\n+_CITATION = re.compile(r\"\\b(S?ADR)[ -](\\d{4})\\b\")\n+# Target ADRs retain the numeric filename; Shiki platform decisions use SADR.\n+_DECISION_FILENAME = re.compile(r\"^(?:(SADR)-)?(\\d{4})-.*\\.md$\")\n \n \n class Violation(NamedTuple):\n     \"\"\"A citation that does not resolve to exactly one decision record.\"\"\"\n \n     citing_file: str  # repo-relative path of the file that cites the ADR\n+    namespace: str  # \"ADR\" for target decisions; \"SADR\" for Shiki decisions\n     number: str  # the cited four-digit number, e.g. \"0016\"\n     line: int  # 1-based line of the citation\n     reason: str  # \"missing\" (zero matches) or \"ambiguous\" (two or more)\n@@ -49,44 +51,59 @@\n \n     @property\n     def expected_path(self) -> str:\n-        return f\"docs/adr/{self.number}-*.md\"\n+        prefix = \"SADR-\" if self.namespace == \"SADR\" else \"\"\n+        return f\"docs/adr/{prefix}{self.number}-*.md\"\n \n     def describe(self) -> str:\n         \"\"\"Name the citing file, the cited number, and the missing path.\"\"\"\n         if self.reason == \"missing\":\n             return (\n-                f\"{self.citing_file}:{self.line} cites ADR {self.number} \"\n+                f\"{self.citing_file}:{self.line} cites {self.namespace} {self.number} \"\n                 f\"but no document matches {self.expected_path}\"\n             )\n         joined = \", \".join(self.matches)\n         return (\n-            f\"{self.citing_file}:{self.line} cites ADR {self.number} which \"\n+            f\"{self.citing_file}:{self.line} cites {self.namespace} {self.number} which \"\n             f\"resolves to {len(self.matches)} documents (expected exactly one \"\n             f\"at {self.expected_path}): {joined}\"\n         )\n \n \n-def _adr_index(root: Path) -> dict[str, list[Path]]:\n-    \"\"\"Map every four-digit ADR number to the record files that claim it.\"\"\"\n-    index: dict[str, list[Path]] = {}\n+def _adr_index(root: Path) -> dict[tuple[str, str], list[Path]]:\n+    \"\"\"Map each decision namespace and number to the files that claim it.\"\"\"\n+    index: dict[tuple[str, str], list[Path]] = {}\n     adr_dir = root / \"docs\" / \"adr\"\n     if adr_dir.is_dir():\n         for path in sorted(adr_dir.glob(\"*.md\")):\n-            match = _ADR_FILENAME.match(path.name)\n+            match = _DECISION_FILENAME.match(path.name)\n             if match:\n-                index.setdefault(match.group(1), []).append(path)\n+                namespace = \"SADR\" if match.group(1) else \"ADR\"\n+                index.setdefault((namespace, match.group(2)), []).append(path)\n     return index\n \n \n def _citing_files(root: Path, include_docs: bool = True) -> list[Path]:\n-    \"\"\"Files scanned for citations: scripts/*.py, and cheaply the markdown.\"\"\"\n+    \"\"\"Current, shipped text surfaces scanned for decision citations.\"\"\"\n     files = sorted((root / \"scripts\").glob(\"*.py\"))\n+    files += sorted((root / \"scripts\").glob(\"*.sh\"))\n     if include_docs:\n         docs = root / \"docs\"\n         if docs.is_dir():\n             files += sorted(docs.rglob(\"*.md\"))\n         files += sorted(root.glob(\"*.md\"))\n-    return files\n+        pyproject = root / \"pyproject.toml\"\n+        if pyproject.is_file():\n+            files.append(pyproject)\n+        for relative in (\".github\", \".codex\"):\n+            surface = root / relative\n+            if surface.is_dir():\n+                files += sorted(\n+                    path\n+                    for path in surface.rglob(\"*\")\n+                    if path.is_file()\n+                    and path.suffix in {\".md\", \".py\", \".sh\", \".toml\", \".yaml\", \".yml\"}\n+                )\n+    return sorted(set(files))\n \n \n def find_adr_reference_violations(\n@@ -94,10 +111,9 @@\n ) -> list[Violation]:\n     \"\"\"Return every ``ADR ####`` citation under ``root`` that fails to resolve.\n \n-    A citation resolves when exactly one ``docs/adr/####-*.md`` matches its\n-    number. Zero matches is a \"missing\" violation; two or more is \"ambiguous\".\n-    Records that are never cited produce nothing -- the converse is not\n-    required.\n+    A citation resolves when exactly one record in the same namespace matches\n+    its number. Zero matches is a \"missing\" violation; two or more is\n+    \"ambiguous\". Records that are never cited produce nothing.\n     \"\"\"\n     root = Path(root)\n     index = _adr_index(root)\n@@ -110,13 +126,15 @@\n         rel = path.relative_to(root).as_posix()\n         for lineno, line in enumerate(text.splitlines(), start=1):\n             for match in _CITATION.finditer(line):\n-                number = match.group(1)\n-                found = index.get(number, [])\n+                namespace, number = match.groups()\n+                found = index.get((namespace, number), [])\n                 if len(found) == 1:\n                     continue\n                 reason = \"missing\" if not found else \"ambiguous\"\n                 rels = [p.relative_to(root).as_posix() for p in found]\n-                violations.append(Violation(rel, number, lineno, reason, rels))\n+                violations.append(\n+                    Violation(rel, namespace, number, lineno, reason, rels)\n+                )\n     return violations\n \n \n@@ -129,8 +147,8 @@\n         self.assertEqual(\n             violations,\n             [],\n-            \"Every 'ADR ####' citation must resolve to exactly one \"\n-            f\"docs/adr/####-*.md:\\n{detail}\",\n+            \"Every ADR/SADR citation must resolve exactly once in its own \"\n+            f\"docs/adr namespace:\\n{detail}\",\n         )\n \n     def test_scan_actually_reads_scripts(self) -> None:\n@@ -142,12 +160,38 @@\n             \"the citation scan must cover scripts/*.py\",\n         )\n \n+    def test_platform_citations_do_not_use_unresolved_numeric_shorthand(self) -> None:\n+        shorthand = re.compile(r\"SADR-\\d{4}/\\d{4}\")\n+        offenders = []\n+        for path in _citing_files(REPO_ROOT):\n+            text = path.read_text(encoding=\"utf-8\")\n+            if shorthand.search(text):\n+                offenders.append(path.relative_to(REPO_ROOT).as_posix())\n+        self.assertEqual(\n+            offenders,\n+            [],\n+            \"repeat the SADR prefix so every cited number is independently resolvable\",\n+        )\n+\n     def test_carried_records_are_indexed(self) -> None:\n         # The two records this Goal carried onto the default branch, and which\n         # the scripts cite most, must be present.\n         index = _adr_index(REPO_ROOT)\n-        self.assertIn(\"0016\", index)\n-        self.assertIn(\"0017\", index)\n+        self.assertIn((\"SADR\", \"0016\"), index)\n+        self.assertIn((\"SADR\", \"0017\"), index)\n+\n+    def test_platform_tree_has_only_sadr_0001_through_0019(self) -> None:\n+        adr_dir = REPO_ROOT / \"docs\" / \"adr\"\n+        sadrs = sorted(adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\"))\n+        self.assertEqual(\n+            {path.name.split(\"-\", 2)[1] for path in sadrs},\n+            {f\"{number:04d}\" for number in range(1, 20)},\n+        )\n+        self.assertEqual(\n+            list(adr_dir.glob(\"[0-9][0-9][0-9][0-9]-*.md\")),\n+            [],\n+            \"the Shiki platform tree must not retain legacy numeric SADR paths\",\n+        )\n \n \n class FixtureAdrReferences(unittest.TestCase):\n@@ -180,6 +224,26 @@\n         self.assertIn(\"9991\", message)  # cited number\n         self.assertIn(\"docs/adr/9991-*.md\", message)  # missing path\n \n+    def test_target_adr_does_not_satisfy_missing_platform_sadr(self) -> None:\n+        root = self._make_repo(\n+            adr_names=[\"0042-target-decision.md\"],\n+            script_body='\"\"\"Target ADR 0042 exists, but platform SADR-0042 does not.\"\"\"\\n',\n+        )\n+        violations = find_adr_reference_violations(root, include_docs=False)\n+        self.assertEqual(len(violations), 1)\n+        self.assertEqual(violations[0].namespace, \"SADR\")\n+        self.assertEqual(violations[0].number, \"0042\")\n+        self.assertEqual(violations[0].expected_path, \"docs/adr/SADR-0042-*.md\")\n+\n+    def test_target_adr_and_platform_sadr_may_reuse_a_number(self) -> None:\n+        root = self._make_repo(\n+            adr_names=[\"0042-target-decision.md\", \"SADR-0042-platform-decision.md\"],\n+            script_body='\"\"\"Target ADR 0042 and platform SADR-0042 are distinct.\"\"\"\\n',\n+        )\n+        self.assertEqual(\n+            find_adr_reference_violations(root, include_docs=False), []\n+        )\n+\n     def test_uncited_adr_is_allowed(self) -> None:\n         # An ADR exists but nothing cites it -> not a violation. The converse\n         # (every ADR must be cited) is explicitly NOT required.\ndiff --git a/tests/test_guardian_review.py b/tests/test_guardian_review.py\nindex 88829e7810212635e2fc98aa5918d6f26294677d..00a85a237acd5bf2d6cb5b294128198c422402ca\n--- a/tests/test_guardian_review.py\n+++ b/tests/test_guardian_review.py\n@@ -162,7 +162,7 @@\n \n     def test_docs_only_is_light_and_not_conservative(self):\n         focus = gr.classify_review_focus([\"docs/adr/0099-x.md\", \"README.md\"], \"\", \"low\")\n-        self.assertTrue(any(\"Docs/ADR-only\" in f for f in focus))\n+        self.assertTrue(any(\"Docs/decision-record-only\" in f for f in focus))\n         self.assertFalse(any(\"conservative default\" in f.lower() for f in focus))\n \n     def test_unknown_category_is_conservative(self):\ndiff --git a/tests/test_policy_job_merged_prs.py b/tests/test_policy_job_merged_prs.py\nindex d45512e74681e5e649279236c7d4e79d5c3da471..b97989f8bd74d091de1a6b5275a20d8dc68c4512\n--- a/tests/test_policy_job_merged_prs.py\n+++ b/tests/test_policy_job_merged_prs.py\n@@ -3,8 +3,8 @@\n \n ``.github/workflows/shiki-cca-completion.yml`` has two consumers of the merged-PR\n proof: the CCA *signal* (``guardian_approval_signal.py`` in \"Prepare CCA context\")\n-and the authoritative gate (``mergegate_check.py`` in \"Enforce MergeGate\"). ADR\n-0017's bookkeeping-closeout exemption (condition 6) needs the merged implementation\n+and the authoritative gate (``mergegate_check.py`` in \"Enforce MergeGate\").\n+SADR-0017's bookkeeping-closeout exemption (condition 6) needs the merged implementation\n PR proven, and a closeout carries NO ``post_merge_reconcile`` marker. The signal\n already resolves ``MERGED_PRS`` unconditionally, but \"Enforce MergeGate\" used to\n resolve it only inside ``grep -q '<!-- shiki:post_merge_reconcile -->'``. So a\ndiff --git a/tests/test_sadr_install_migration.py b/tests/test_sadr_install_migration.py\nnew file mode 100644\nindex 0000000000000000000000000000000000000000..a6f4c57e4dcaef73ef600b0d4804744138ab9071\n--- /dev/null\n+++ b/tests/test_sadr_install_migration.py\n@@ -0,0 +1,229 @@\n+\"\"\"SADR installation and collision-safe legacy upgrade behavior.\"\"\"\n+\n+from __future__ import annotations\n+\n+import contextlib\n+import hashlib\n+import io\n+import json\n+import shutil\n+import tempfile\n+import unittest\n+from pathlib import Path\n+\n+import shiki_test_support  # noqa: F401  (path bootstrap)\n+\n+import shiki_installer\n+from tests.test_adr_references import find_adr_reference_violations\n+\n+\n+PRODUCT_NUMBERS = tuple(range(13, 19))\n+\n+\n+def _quiet_install(target: Path, *, force: bool, validate: bool) -> None:\n+    with contextlib.redirect_stdout(io.StringIO()):\n+        shiki_installer.install_template(target, force=force, validate=validate)\n+\n+\n+def _sha256(path: Path) -> str:\n+    return hashlib.sha256(path.read_bytes()).hexdigest()\n+\n+\n+def _legacy_sadr_paths() -> tuple[str, ...]:\n+    return tuple(\n+        f\"docs/adr/{path.name.removeprefix('SADR-')}\"\n+        for path in sorted((shiki_installer.ROOT / \"docs\" / \"adr\").glob(\"SADR-*.md\"))\n+        if path.name != \"SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\"\n+    )\n+\n+\n+def _create_legacy_upgrade_fixture(\n+    target: Path,\n+) -> tuple[dict[str, bytes], tuple[str, ...]]:\n+    adr_dir = target / \"docs\" / \"adr\"\n+    adr_dir.mkdir(parents=True)\n+    product_bytes = {\n+        f\"{number:04d}-cubeshare-product-decision.md\": (\n+            f\"# CubeShare ADR {number:04d}\\n\\nDo not rewrite {number}.\\n\"\n+        ).encode()\n+        for number in PRODUCT_NUMBERS\n+    }\n+    for name, content in product_bytes.items():\n+        (adr_dir / name).write_bytes(content)\n+\n+    legacy_paths = _legacy_sadr_paths()\n+    for relative in legacy_paths:\n+        (target / relative).write_bytes(f\"legacy Shiki bytes for {relative}\\n\".encode())\n+\n+    stamp_path = target / shiki_installer.INSTALL_STAMP_PATH\n+    stamp_path.parent.mkdir(parents=True, exist_ok=True)\n+    stamp = {\n+        \"version\": shiki_installer.INSTALL_STAMP_VERSION,\n+        \"platform_commit\": None,\n+        \"installed_at\": \"2000-01-01T00:00:00+00:00\",\n+        \"digests\": {\n+            relative: _sha256(target / relative) for relative in legacy_paths\n+        },\n+    }\n+    stamp_path.write_text(\n+        json.dumps(stamp, indent=2, sort_keys=True) + \"\\n\", encoding=\"utf-8\"\n+    )\n+    return product_bytes, legacy_paths\n+\n+\n+def _tree_snapshot(root: Path) -> dict[str, tuple[str, bytes | str]]:\n+    \"\"\"Recursive byte/shape snapshot used to prove preflight is write-free.\"\"\"\n+    snapshot: dict[str, tuple[str, bytes | str]] = {}\n+    for path in sorted(root.rglob(\"*\")):\n+        relative = path.relative_to(root).as_posix()\n+        if path.is_symlink():\n+            snapshot[relative] = (\"symlink\", path.readlink().as_posix())\n+        elif path.is_dir():\n+            snapshot[relative] = (\"directory\", \"\")\n+        else:\n+            snapshot[relative] = (\"file\", path.read_bytes())\n+    return snapshot\n+\n+\n+class FreshSadrInstallTests(unittest.TestCase):\n+    def setUp(self) -> None:\n+        self.tmp = Path(tempfile.mkdtemp(prefix=\"shiki-sadr-fresh-\"))\n+        self.addCleanup(lambda: shutil.rmtree(self.tmp, ignore_errors=True))\n+        self.target = self.tmp / \"target\"\n+        self.adr_dir = self.target / \"docs\" / \"adr\"\n+        self.adr_dir.mkdir(parents=True)\n+        self.product_bytes = {\n+            f\"{number:04d}-product-decision.md\": (\n+                f\"# Product ADR {number:04d}\\n\\nTarget-owned bytes {number}.\\n\"\n+            ).encode()\n+            for number in PRODUCT_NUMBERS\n+        }\n+        for name, content in self.product_bytes.items():\n+            (self.adr_dir / name).write_bytes(content)\n+\n+    def test_fresh_install_preserves_product_adrs_and_installs_all_sadrs(self) -> None:\n+        _quiet_install(self.target, force=False, validate=True)\n+\n+        for name, expected in self.product_bytes.items():\n+            self.assertEqual((self.adr_dir / name).read_bytes(), expected)\n+\n+        sadr_numbers = {\n+            path.name.split(\"-\", 2)[1]\n+            for path in self.adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\")\n+        }\n+        self.assertEqual(sadr_numbers, {f\"{number:04d}\" for number in range(1, 20)})\n+\n+        numeric_records = {\n+            path.name\n+            for path in self.adr_dir.glob(\"[0-9][0-9][0-9][0-9]-*.md\")\n+        }\n+        self.assertEqual(numeric_records, set(self.product_bytes))\n+        self.assertEqual(find_adr_reference_violations(self.target), [])\n+\n+\n+class LegacySadrUpgradeTests(unittest.TestCase):\n+    def setUp(self) -> None:\n+        self.tmp = Path(tempfile.mkdtemp(prefix=\"shiki-sadr-upgrade-\"))\n+        self.addCleanup(lambda: shutil.rmtree(self.tmp, ignore_errors=True))\n+        self.target = self.tmp / \"target\"\n+        self.adr_dir = self.target / \"docs\" / \"adr\"\n+        self.product_bytes, self.legacy_paths = _create_legacy_upgrade_fixture(\n+            self.target\n+        )\n+\n+    def test_force_upgrade_deletes_only_stamped_legacy_sadrs(self) -> None:\n+        _quiet_install(self.target, force=True, validate=True)\n+\n+        for relative in self.legacy_paths:\n+            self.assertFalse((self.target / relative).exists(), relative)\n+        for name, expected in self.product_bytes.items():\n+            self.assertEqual((self.adr_dir / name).read_bytes(), expected)\n+\n+        sadrs = list(self.adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\"))\n+        self.assertEqual(len(sadrs), 19)\n+        refreshed = json.loads(\n+            (self.target / shiki_installer.INSTALL_STAMP_PATH).read_text(encoding=\"utf-8\")\n+        )\n+        for relative in self.legacy_paths:\n+            self.assertNotIn(relative, refreshed[\"digests\"])\n+        for path in sadrs:\n+            relative = path.relative_to(self.target).as_posix()\n+            self.assertEqual(refreshed[\"digests\"][relative], _sha256(path))\n+\n+    def test_non_force_install_never_deletes_legacy_files(self) -> None:\n+        before = {\n+            relative: (self.target / relative).read_bytes()\n+            for relative in self.legacy_paths\n+        }\n+        _quiet_install(self.target, force=False, validate=False)\n+        for relative, expected in before.items():\n+            self.assertEqual((self.target / relative).read_bytes(), expected)\n+        refreshed = json.loads(\n+            (self.target / shiki_installer.INSTALL_STAMP_PATH).read_text(encoding=\"utf-8\")\n+        )\n+        for relative in self.legacy_paths:\n+            self.assertEqual(refreshed[\"digests\"][relative], _sha256(self.target / relative))\n+\n+\n+class LegacyCleanupPreflightTests(unittest.TestCase):\n+    def setUp(self) -> None:\n+        self.tmp = Path(tempfile.mkdtemp(prefix=\"shiki-sadr-preflight-\"))\n+        self.addCleanup(lambda: shutil.rmtree(self.tmp, ignore_errors=True))\n+\n+    def test_all_ownership_blockers_abort_before_any_write(self) -> None:\n+        cases = (\n+            (\"absent\", \"install stamp is absent\"),\n+            (\"unreadable\", \"install stamp is unreadable\"),\n+            (\"malformed\", \"install stamp is malformed\"),\n+            (\"missing-digest\", \"install stamp has no digest\"),\n+            (\"digest-mismatch\", \"digest mismatch\"),\n+        )\n+        for name, expected_reason in cases:\n+            with self.subTest(name=name):\n+                target = self.tmp / name\n+                _, legacy_paths = _create_legacy_upgrade_fixture(target)\n+                stamp_path = target / shiki_installer.INSTALL_STAMP_PATH\n+\n+                if name == \"absent\":\n+                    stamp_path.unlink()\n+                elif name == \"unreadable\":\n+                    stamp_path.unlink()\n+                    stamp_path.mkdir()\n+                elif name == \"malformed\":\n+                    stamp_path.write_text(\"{not-json\\n\", encoding=\"utf-8\")\n+                else:\n+                    stamp = json.loads(stamp_path.read_text(encoding=\"utf-8\"))\n+                    if name == \"missing-digest\":\n+                        stamp[\"digests\"] = {}\n+                    else:\n+                        stamp[\"digests\"] = {\n+                            relative: \"deadbeef\" for relative in legacy_paths\n+                        }\n+                    stamp_path.write_text(\n+                        json.dumps(stamp, indent=2, sort_keys=True) + \"\\n\",\n+                        encoding=\"utf-8\",\n+                    )\n+\n+                before = _tree_snapshot(target)\n+                with self.assertRaises(shiki_installer.ShikiError) as raised:\n+                    _quiet_install(target, force=True, validate=False)\n+                after = _tree_snapshot(target)\n+\n+                self.assertEqual(before, after, f\"{name} preflight changed the tree\")\n+                message = str(raised.exception)\n+                self.assertIn(expected_reason, message)\n+                self.assertIn(\"No files were written\", message)\n+                for relative in legacy_paths:\n+                    self.assertIn(relative, message)\n+\n+    def test_absent_stamp_is_allowed_when_no_legacy_path_exists(self) -> None:\n+        target = self.tmp / \"no-legacy\"\n+        target.mkdir()\n+        _quiet_install(target, force=True, validate=False)\n+        self.assertTrue(\n+            (target / \"docs\" / \"adr\" / \"SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\").is_file()\n+        )\n+\n+\n+if __name__ == \"__main__\":\n+    unittest.main()\n\n2026-08-07T08:20:39.493559Z ERROR codex_models_manager::manager: failed to renew cache TTL: missing field `base_instructions` at line 94 column 5\nexec\n/bin/zsh -lc \"rg -n -U '\\\\bADR[- ]?\\\\s*(000[1-9]|001[0-8])\\\\b|docs/adr/(000[1-9]|001[0-8])-' .claude 2>/dev/null || true\" in /private/tmp/shiki-adr-collision.pa0KwY/.worktrees/shiki-t-20260807t073307221254z-2d22d654-introduc\n succeeded in 0ms:\ndiff --git a/.codex/skills/shiki/SKILL.md b/.codex/skills/shiki/SKILL.md\nindex c8e2e85d9d75d49cc4f2d6286ee22659c4e53e75..93cded623b2c15d9f65c76491a1c2d813ac9e6c0\n--- a/.codex/skills/shiki/SKILL.md\n+++ b/.codex/skills/shiki/SKILL.md\n@@ -18,6 +18,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n Codex CLI does not register this skill as a custom `/shiki` slash command. When\n the user types `/shiki` in Codex CLI and gets \"Unrecognized command\", explain\n that this is expected: invoke Shiki in Codex with natural language such as\n@@ -69,7 +72,7 @@\n \n ## Responsibilities\n \n-- Claude Code implements and repairs by default (`shiki runner claude`, ADR 0008); Codex implements and repairs tasks explicitly assigned to `codex`.\n+- Claude Code implements and repairs by default (`shiki runner claude`, SADR-0008); Codex implements and repairs tasks explicitly assigned to `codex`.\n - Claude Code Action can act as GitHub-side CCA or reviewer.\n - CCA judges completion.\n - MergeGate authorizes state transitions and merge readiness.\ndiff --git a/.github/prompts/cca-completion-check.md b/.github/prompts/cca-completion-check.md\nindex ed203ad5f302ecc81372d8ab27d2b1232ca97497..fd89950528333ef6242e6d5443904f5392ab7e56\n--- a/.github/prompts/cca-completion-check.md\n+++ b/.github/prompts/cca-completion-check.md\n@@ -19,6 +19,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n ## Required Reading\n \n Read only the files needed to judge the PR contract. Prefer this order:\ndiff --git a/.github/prompts/codex-task-handoff.md b/.github/prompts/codex-task-handoff.md\nindex 695e211d76dea1c60e8d5bb360e78487808286a4..59441d576b01a39569722f226c854e5a740e9eb5\n--- a/.github/prompts/codex-task-handoff.md\n+++ b/.github/prompts/codex-task-handoff.md\n@@ -2,7 +2,7 @@\n \n You are Codex Front acting as the Implementer for a Shiki task.\n \n-Follow `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant ADRs, and `docs/agents/codex-handoff.md`.\n+Follow `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant Shiki SADRs and target ADRs, and `docs/agents/codex-handoff.md`.\n \n Rules:\n \ndiff --git a/AGENTS.md b/AGENTS.md\nindex 7f041100d49c2a7525f51cb0f5d696a4a6d1c78c..a06bfbabc6fa48a007389e25e12d7ed523936bed\n--- a/AGENTS.md\n+++ b/AGENTS.md\n@@ -22,7 +22,7 @@\n - **Codex Front** is an optional implementer through Codex App, Codex CLI, Codex IDE extension, or Codex Web signed in with ChatGPT OAuth/subscription auth, dispatched with `shiki runner codex` when a task is explicitly assigned to `codex`.\n - **GitHub CCA** is implemented by Claude Code Action by default, using `CLAUDE_CODE_OAUTH_TOKEN`.\n \n-Do not assume `openai/codex-action`, `OPENAI_API_KEY`, or API-key based Codex automation in the default Shiki path. API-key runners are explicit target-repo extensions and require their own ADR.\n+Do not assume `openai/codex-action`, `OPENAI_API_KEY`, or API-key based Codex automation in the default Shiki path. API-key runners are explicit target-repo extensions and require their own target ADR.\n \n Shiki is not:\n \n@@ -44,10 +44,14 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use\n+`SADR-NNNN-*.md`; target product decisions retain `NNNN-*.md` and the `ADR`\n+namespace.\n+\n Use the canonical source-of-truth order above. Runtime-specific reading orders\n are operational guidance only and must not override or reorder it.\n \n-Conversation state is not durable truth. If a decision matters, put it in GitHub, `.shiki/`, `CONTEXT.md`, or an ADR.\n+Conversation state is not durable truth. If a decision matters, put it in GitHub, `.shiki/`, `CONTEXT.md`, a Shiki SADR, or a target ADR.\n \n If GitHub and `.shiki/` disagree, surface the conflict and prefer GitHub operational state until the mirror is repaired.\n \n@@ -75,8 +79,8 @@\n Every non-trivial change follows this loop:\n \n 1. **Goal**: clarify outcome, completion conditions, non-goals, risk level, and success signals.\n-2. **grill-with-docs**: challenge the plan against domain docs, ADRs, code reality, terminology, and edge scenarios.\n-3. **Context & Impact**: find relevant docs, ADRs, code, dependencies, owners, locks, and verification surfaces.\n+2. **grill-with-docs**: challenge the plan against domain docs, Shiki SADRs, target ADRs, code reality, terminology, and edge scenarios.\n+3. **Context & Impact**: find relevant docs, SADRs, target ADRs, code, dependencies, owners, locks, and verification surfaces.\n 4. **PRD and Spec Freeze**: use `to-prd` when context is settled enough to become durable product/engineering intent. The operator's explicit approval of the PRD is Spec Freeze: the plan records a `spec_freeze` block, and no plan runs without it. Steps 1-4 form Requirements Definition \u2014 one continuous operator dialogue. After Spec Freeze, scope changes require an operator-approved Spec Amendment; non-scope-moving interpretations go to the Assumption Log.\n 5. **Issues**: use `to-issues` to create independently grabbable vertical-slice issues. Prefer AFK slices over HITL slices where possible.\n 6. **Triage**: label issues for readiness, risk, runtime, skills, and MergeGate state.\n@@ -167,7 +171,7 @@\n - Testing decisions.\n - Out-of-scope items.\n - Further notes.\n-- Links to relevant domain terms and ADRs.\n+- Links to relevant domain terms, SADRs, and target ADRs.\n \n Every executable task must include:\n \n@@ -193,7 +197,7 @@\n \n Context & Impact must identify:\n \n-- Relevant documents, domain terms, ADRs, and past decisions.\n+- Relevant documents, domain terms, SADRs, target ADRs, and past decisions.\n - Relevant modules, interfaces, seams, callers, owners, and tests.\n - Dependency relationships between tasks.\n - Candidate locks and lock conflicts.\n@@ -210,7 +214,7 @@\n | Skill | Required when |\n | --- | --- |\n | `setup-matt-pocock-skills` | First configuring the repo for skills, or when issue tracker, triage labels, or domain docs are missing. |\n-| `grill-with-docs` | Default for non-trivial Goals before PRD/issues; always required when plans, terminology, boundaries, tradeoffs, or ADR-worthy decisions are ambiguous. |\n+| `grill-with-docs` | Default for non-trivial Goals before PRD/issues; always required when plans, terminology, boundaries, tradeoffs, or decision-record-worthy choices are ambiguous. |\n | `zoom-out` | The agent lacks an architectural map of the relevant code area. |\n | `to-prd` | Settled Goal context must become durable product/engineering intent. |\n | `to-issues` | A Goal, PRD, or plan must become independently grabbable vertical-slice issues. |\n@@ -348,7 +352,7 @@\n - Three or more new files.\n - Any high-risk or critical label.\n \n-When the architecture gate triggers, run `grill-with-docs` or `improve-codebase-architecture` as appropriate and record durable decisions in an ADR when needed.\n+When the architecture gate triggers, run `grill-with-docs` or `improve-codebase-architecture` as appropriate and record durable decisions in a Shiki SADR or target ADR as applicable.\n \n ## Engineering Discipline\n \ndiff --git a/CLAUDE.md b/CLAUDE.md\nindex 98a932e7841a632866fcd53b0bad0e3af3711d3d..98a31399404d6fb044b4fd493f4459ac87f439de\n--- a/CLAUDE.md\n+++ b/CLAUDE.md\n@@ -14,6 +14,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n ## Claude Role\n \n Default to planner, default implementer, reviewer, coordinator, documentation editor, CCA-style completion judge, and final-judgment assistant.\n@@ -57,7 +60,7 @@\n At the start of a Shiki session:\n \n 1. Read `AGENTS.md`.\n-2. Read `CONTEXT.md` and relevant ADRs.\n+2. Read `CONTEXT.md` and relevant Shiki SADRs and target ADRs.\n 3. Read active `.shiki/` Goal/task/ledger state when present.\n 4. Read active distilled rules from `.shiki/memories/` (status `distilled`, `active: true`, not revoked or superseded) for applicable guidance; the same rules are injected deterministically into task handoffs as the `## Distilled Rules` section (\u00a73.5).\n 5. Read the GitHub Issue or PR when the task is GitHub-backed.\n@@ -74,7 +77,7 @@\n - Define the Goal, completion conditions, non-goals, and risk level.\n - Treat `grill-with-docs` as the default planning entry point for non-trivial Goals.\n - Challenge domain terms against `CONTEXT.md`.\n-- Challenge hard-to-reverse tradeoffs against ADRs.\n+- Challenge hard-to-reverse tradeoffs against SADRs and target ADRs.\n - Explore code instead of asking when code can answer the question.\n - Run Context & Impact before implementation planning.\n - Produce a Task DAG, not an unordered checklist.\n@@ -94,7 +97,7 @@\n - For each question, provide your recommended answer.\n - Resolve dependency decisions before downstream design questions.\n - Update `CONTEXT.md` inline when terminology crystallizes.\n-- Offer ADRs only when the decision is hard to reverse, surprising without context, and a real tradeoff.\n+- Offer a SADR or target ADR only when the decision is hard to reverse, surprising without context, and a real tradeoff.\n - Convert unresolved questions into HITL blockers rather than allowing Codex to guess.\n \n ## PRD and Issues Standard\n@@ -103,7 +106,7 @@\n \n - Use the project's domain glossary vocabulary.\n - Avoid volatile file paths unless they are essential.\n-- Include implementation decisions, testing decisions, out-of-scope items, and relevant ADR/domain links.\n+- Include implementation decisions, testing decisions, out-of-scope items, and relevant SADR/target-ADR/domain links.\n - Identify deep-module opportunities and testable interfaces.\n \n When creating issues:\n@@ -121,7 +124,7 @@\n - Branch/worktree target.\n - Scope and non-goals.\n - Dependencies and locks.\n-- Relevant docs, ADRs, modules, and tests.\n+- Relevant docs, SADRs, target ADRs, modules, and tests.\n - Required skill invocations.\n - TDD expectations.\n - Pre-PR `code-review` gate expectations.\n@@ -152,7 +155,7 @@\n \n - Treat yourself as a judge, not implementer.\n - Do not write production code.\n-- Read `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant ADRs, PRD, task issue, PR body, diff, checks, reviews, labels, and `.shiki/` state.\n+- Read `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant SADRs and target ADRs, PRD, task issue, PR body, diff, checks, reviews, labels, and `.shiki/` state.\n - Evaluate every applicable checklist in `docs/agents/checklists.md`.\n - Map each acceptance criterion to durable evidence.\n - Distinguish implementation failure from missing evidence.\ndiff --git a/CONTEXT.md b/CONTEXT.md\nindex e61a9be06a0cce0d026484083a657bde2067e234..237ee8aa3ea8fab334ece49768bc5ed3b5af0974\n--- a/CONTEXT.md\n+++ b/CONTEXT.md\n@@ -25,7 +25,7 @@\n _Avoid_: jumping directly into code\n \n **grill-with-docs**:\n-A planning skill that challenges a plan against domain language, ADRs, code reality, and concrete edge scenarios. It resolves design-tree questions before PRD/issues. It runs inside Requirements Definition.\n+A planning skill that challenges a plan against domain language, Shiki SADRs, target ADRs, code reality, and concrete edge scenarios. It resolves design-tree questions before PRD/issues. It runs inside Requirements Definition.\n _Avoid_: generic brainstorming, silent assumptions\n \n **Requirements Definition**:\ndiff --git a/CONTRIBUTING.md b/CONTRIBUTING.md\nindex c6d9b656e73abb8a55e5f660123e03153dcb3e6d..9e37a8fbf668db5b22461aaae5548b8f1354394c\n--- a/CONTRIBUTING.md\n+++ b/CONTRIBUTING.md\n@@ -17,7 +17,7 @@\n 1. **Goal** \u2014 start from a user-approved Goal with an outcome, completion\n    conditions, non-goals, risk level, and success signals.\n 2. **grill-with-docs** \u2014 for any non-trivial change, challenge the plan against\n-   domain language, ADRs, and code reality before writing a PRD.\n+   domain language, Shiki SADRs, target ADRs, and code reality before writing a PRD.\n 3. **PRD** \u2014 capture settled product/engineering intent (`to-prd`).\n 4. **Spec Freeze** \u2014 the operator explicitly approves the PRD/requirements;\n    the plan records a `spec_freeze` block and no plan runs without it\n@@ -94,7 +94,7 @@\n   entries.\n - Do not hand-edit `.shiki/goals/*.json` or `.shiki/manifest.json` in a task PR.\n - Conversation state is not durable truth. If a decision matters, record it in\n-  GitHub, `.shiki/`, `CONTEXT.md`, or an ADR.\n+  GitHub, `.shiki/`, `CONTEXT.md`, a Shiki SADR, or a target ADR.\n \n ## Architecture Gate\n \n@@ -103,7 +103,7 @@\n or CI policy, package structure, three or more structural files, more than 100\n net new lines in a core module, three or more new files, or any high-risk label.\n When triggered, run `grill-with-docs` or `improve-codebase-architecture` and\n-record durable decisions in an ADR when needed (see `docs/adr/`).\n+record durable decisions in a Shiki SADR or target ADR as applicable (see `docs/adr/`).\n \n ## Safety\n \ndiff --git a/SYSTEM_PROMPT.md b/SYSTEM_PROMPT.md\nindex aaaea45e3a896c3a44ba89eec8bb84325ecf6c62..da009c86a88285269dee0155e58e550c6d744ea9\n--- a/SYSTEM_PROMPT.md\n+++ b/SYSTEM_PROMPT.md\n@@ -30,6 +30,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n Use the canonical source-of-truth order above. Runtime-specific reading orders\n are operational guidance only and must not override or reorder it. The current\n conversation is non-durable operator input.\n@@ -48,14 +51,14 @@\n \n A runtime may only do work that is explicitly assigned by the Goal, task, PR, or MergeGate decision.\n \n-Do not assume `openai/codex-action`, `OPENAI_API_KEY`, or API-key based Codex automation in the default Shiki path. API-key runners are explicit target-repo extensions and require their own ADR.\n+Do not assume `openai/codex-action`, `OPENAI_API_KEY`, or API-key based Codex automation in the default Shiki path. API-key runners are explicit target-repo extensions and require their own target ADR.\n \n ## 4. Non-negotiable Shiki flow\n \n For any non-trivial Goal, use this flow:\n \n 1. **Goal Seek**: define outcome, non-goals, risk level, completion criteria, and evidence requirements.\n-2. **grill-with-docs**: challenge the plan against `CONTEXT.md`, ADRs, code reality, terminology, boundaries, and edge scenarios. Ask one question at a time when the operator is available. If code can answer a question, inspect code instead of asking.\n+2. **grill-with-docs**: challenge the plan against `CONTEXT.md`, Shiki SADRs, target ADRs, code reality, terminology, boundaries, and edge scenarios. Ask one question at a time when the operator is available. If code can answer a question, inspect code instead of asking.\n 3. **Context & Impact**: identify modules, interfaces, seams, callers, dependencies, locks, risk, and verification surfaces.\n 4. **to-prd + Spec Freeze**: turn settled context into a PRD using domain vocabulary and testing decisions. The operator's explicit approval of the PRD is Spec Freeze, recorded as the plan's `spec_freeze` block; plans without it do not run. Steps 1-4 form Requirements Definition.\n 5. **to-issues**: decompose the PRD into independently grabbable vertical-slice issues. Prefer AFK slices; mark HITL slices when judgment is still required.\n@@ -75,7 +78,7 @@\n Before making any material change:\n \n 1. Read `AGENTS.md` and any runtime-specific wrapper such as `CLAUDE.md`.\n-2. Read `CONTEXT.md` and relevant ADRs.\n+2. Read `CONTEXT.md` and relevant SADRs and target ADRs.\n 3. Read the GitHub Issue / PR and `.shiki/` state for the active Goal or task.\n 4. Identify your role: Planner, Implementer, Reviewer, Completion Check Agent, Repairer, or Guardian-assist.\n 5. Identify Goal id, task id, branch, PR, dependencies, locks, risk level, required skills, and required evidence.\n@@ -103,7 +106,7 @@\n \n At minimum, identify:\n \n-- Relevant documents, ADRs, domain terms, and prior decisions.\n+- Relevant documents, SADRs, target ADRs, domain terms, and prior decisions.\n - Relevant modules, interfaces, seams, callers, owners, and tests.\n - Dependency relationships between tasks.\n - Candidate file locks and lock conflicts.\n@@ -152,7 +155,7 @@\n \n CCA must read:\n \n-- `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant ADRs.\n+- `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant SADRs and target ADRs.\n - Parent Goal issue, PRD issue, task issue, comments, and labels.\n - PR body, diff, commits, changed files, reviews, CI checks, and artifacts available to the workflow.\n - `.shiki/` task contract, locks, ledger entries, CCA checklist profile, and prior repair packets.\n@@ -214,7 +217,7 @@\n - Three or more new files.\n - Any high-risk or critical label.\n \n-When the architecture gate triggers, run `grill-with-docs` or `improve-codebase-architecture` as appropriate and record the decision in an ADR if it is hard to reverse or likely to be re-litigated.\n+When the architecture gate triggers, run `grill-with-docs` or `improve-codebase-architecture` as appropriate and record the decision in a Shiki SADR or target ADR as applicable if it is hard to reverse or likely to be re-litigated.\n \n ## 14. Skill Gate\n \n@@ -223,7 +226,7 @@\n Use these skills:\n \n - `setup-matt-pocock-skills`: before first use of the engineering skill set, or when issue tracker, triage labels, or domain docs are missing.\n-- `grill-with-docs`: default for non-trivial Goals before PRD/issues; required for ambiguous plans, terminology, boundaries, ADR-worthy tradeoffs, or design-tree decisions.\n+- `grill-with-docs`: default for non-trivial Goals before PRD/issues; required for ambiguous plans, terminology, boundaries, decision-record-worthy tradeoffs, or design-tree decisions.\n - `zoom-out`: unfamiliar code area or missing architectural map.\n - `to-prd`: convert settled context into a PRD.\n - `to-issues`: break a PRD, plan, or Goal into independently grabbable vertical-slice issues.\ndiff --git a/docs/adr/0001-keep-shiki-as-platform-name.md b/docs/adr/SADR-0001-keep-shiki-as-platform-name.md\nindex 20a9637c3b9d8c97de7ba8db7cfae4d82d9a7dba..8b685439959aada1b221c112f0ac3c51ca8a1695\n--- a/docs/adr/0001-keep-shiki-as-platform-name.md\n+++ b/docs/adr/SADR-0001-keep-shiki-as-platform-name.md\n@@ -1,3 +1,3 @@\n-# Keep Shiki As The Platform Name\n+# SADR-0001: Keep Shiki As The Platform Name\n \n Shiki remains the product name for the GitHub-native agentic engineering control plane. We considered more descriptive names such as MergePilot, MergeGate, and Kiroku, but kept Shiki because it carries the existing goal-seeking framework identity while remaining independent of Hermes, Codex, Claude Code, or any single runtime.\ndiff --git a/docs/adr/0002-use-agents-as-shared-constitution.md b/docs/adr/SADR-0002-use-agents-as-shared-constitution.md\nindex 73de30ecb078120eac540d9bc51855e3cd48d0e3..c5486182bf06fca36ea508496b013c67302fd989\n--- a/docs/adr/0002-use-agents-as-shared-constitution.md\n+++ b/docs/adr/SADR-0002-use-agents-as-shared-constitution.md\n@@ -1,3 +1,3 @@\n-# Use AGENTS As The Shared Agent Constitution\n+# SADR-0002: Use AGENTS As The Shared Agent Constitution\n \n Shiki uses `AGENTS.md` as the runtime-neutral constitution and `CLAUDE.md` as a thin Claude Code wrapper. This keeps Codex, Claude Code, Hermes Runner, and future Agent Runtimes aligned on the same GitHub-first MergeGate rules while still allowing runtime-specific instructions where needed.\ndiff --git a/docs/adr/0003-require-skill-gate-for-mergegate-readiness.md b/docs/adr/SADR-0003-require-skill-gate-for-mergegate-readiness.md\nindex 424be0c9af4c8f0136f1fde68a468c47ff84e764..7fc337987c7df9792f75f181dfcbed75fe5ffff2\n--- a/docs/adr/0003-require-skill-gate-for-mergegate-readiness.md\n+++ b/docs/adr/SADR-0003-require-skill-gate-for-mergegate-readiness.md\n@@ -1,3 +1,3 @@\n-# Require Skill Gate For MergeGate Readiness\n+# SADR-0003: Require Skill Gate For MergeGate Readiness\n \n Shiki requires relevant engineering skills to be invoked before execution when their triggers apply. This makes skills part of MergeGate readiness rather than optional prompt style, because Shiki depends on repeatable planning, issue decomposition, TDD, diagnosis, triage, and architecture review before autonomous or parallel agent work can be trusted.\ndiff --git a/docs/adr/0004-use-github-first-with-shiki-mirror.md b/docs/adr/SADR-0004-use-github-first-with-shiki-mirror.md\nindex d0a3a9174dc646a945a9d325954ffaf00c352040..a79c54ae9ee8eed2a0524192b864da4e22a92d3f\n--- a/docs/adr/0004-use-github-first-with-shiki-mirror.md\n+++ b/docs/adr/SADR-0004-use-github-first-with-shiki-mirror.md\n@@ -1,4 +1,4 @@\n-# ADR 0004: Use GitHub First With A Repository-Local Shiki Mirror\n+# SADR-0004: Use GitHub First With A Repository-Local Shiki Mirror\n \n ## Status\n \ndiff --git a/docs/adr/0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md b/docs/adr/SADR-0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\nindex b339baf49b89d9290e79c3d51b36e7b2bde3bd95..0f3c3eb9afccdb3403ce978e21d04fe12e495da0\n--- a/docs/adr/0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\n+++ b/docs/adr/SADR-0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\n@@ -1,8 +1,8 @@\n-# ADR 0005: Use Codex As Front And Claude Code Action As The GitHub Runtime\n+# SADR-0005: Use Codex As Front And Claude Code Action As The GitHub Runtime\n \n ## Status\n \n-Accepted (implementer default superseded by ADR 0008; the auth model and the\n+Accepted (implementer default superseded by SADR-0008; the auth model and the\n Claude Code Action GitHub runtime decision remain in force)\n \n ## Context\n@@ -26,4 +26,4 @@\n - Shiki avoids requiring `OPENAI_API_KEY` for the default workflow.\n - Codex work happens through the user's authenticated Codex front surface, not as a hidden backend Action.\n - Claude Code Action is configured for OAuth-token based subscription usage by default.\n-- API-key based automation remains possible only as an explicit target-repo extension with its own ADR.\n+- API-key based automation remains possible only as an explicit target-repo extension with its own target ADR.\ndiff --git a/docs/adr/0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md b/docs/adr/SADR-0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\nindex 4bcae9d1e52f0a37b8e9fbafa5cd14b6ee1993bd..7a38538a6ae9929567f0ade67e7ea6a60d0b3f7e\n--- a/docs/adr/0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\n+++ b/docs/adr/SADR-0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\n@@ -1,4 +1,4 @@\n-# ADR 0006: Separate Codex Implementation, CCA Completion, And MergeGate Authorization\n+# SADR-0006: Separate Codex Implementation, CCA Completion, And MergeGate Authorization\n \n ## Status\n \ndiff --git a/docs/adr/0007-packaging-and-release.md b/docs/adr/SADR-0007-packaging-and-release.md\nindex 79b84ddb97167372b44641e5b7860e400ce80822..bb445a2e2e856d9d1f5fdbe35de8d4e8cec0fb32\n--- a/docs/adr/0007-packaging-and-release.md\n+++ b/docs/adr/SADR-0007-packaging-and-release.md\n@@ -1,4 +1,4 @@\n-# ADR 0007: Packaging And Release For Shiki\n+# SADR-0007: Packaging And Release For Shiki\n \n ## Status\n \n@@ -25,7 +25,7 @@\n   not introduce third-party runtime dependencies or break the shim.\n - Releases must run inside the operator's subscription-authenticated toolchain\n   and must not require extra secrets beyond the built-in `GITHUB_TOKEN`\n-  (consistent with ADR 0005's no-API-key-by-default stance).\n+  (consistent with SADR-0005's no-API-key-by-default stance).\n - `.shiki/manifest.json` already encodes the install surface (`install.include`,\n   `install.create_directories`, `install.exclude_from_commit`), and it must not\n   be mutated as part of this change.\n@@ -34,7 +34,7 @@\n \n This is a hard-to-reverse platform decision (it sets the version source of\n truth, the release trigger, and the public install contract), so it is recorded\n-as an ADR.\n+as a SADR.\n \n ## Decision\n \n@@ -104,7 +104,7 @@\n 3. **Publishing to PyPI.** Rejected for now: Shiki is distributed as a\n    repository template + git-installable CLI, not a PyPI library; PyPI would add\n    credentials/secrets and a different versioning contract. Can be added later as\n-   its own ADR if a packaged distribution is needed.\n+   its own SADR if a packaged distribution is needed.\n 4. **Dynamic version (read `VERSION` via a build backend `dynamic` field).**\n    Rejected to keep `pyproject.toml` backend-agnostic and dependency-free; the\n    small duplication between `VERSION` and `pyproject.toml` is guarded by the\n@@ -123,4 +123,4 @@\n   releasing versions the whole platform but ships only `install.include` to\n   targets.\n - Future PyPI distribution or a console entry point are deferred and would each\n-  warrant their own ADR.\n+  warrant their own SADR.\ndiff --git a/docs/adr/0008-use-claude-code-as-default-implementer.md b/docs/adr/SADR-0008-use-claude-code-as-default-implementer.md\nindex dedf4510b7c7799521422f622029563d373db031..35c2625d908167fbc58855b5b63e283805f167e8\n--- a/docs/adr/0008-use-claude-code-as-default-implementer.md\n+++ b/docs/adr/SADR-0008-use-claude-code-as-default-implementer.md\n@@ -1,12 +1,12 @@\n-# ADR 0008: Use Claude Code As The Default Implementer Runtime\n+# SADR-0008: Use Claude Code As The Default Implementer Runtime\n \n ## Status\n \n-Proposed (supersedes ADR 0005 for the implementer default; keeps its auth model)\n+Proposed (supersedes SADR-0005 for the implementer default; keeps its auth model)\n \n ## Context\n \n-ADR 0005 made Codex Front the default implementation runtime, with Claude Code\n+SADR-0005 made Codex Front the default implementation runtime, with Claude Code\n restricted to planner and reviewer roles by the runtime registry, and the only\n autonomous implementation adapter was `shiki runner codex`.\n \n@@ -18,13 +18,13 @@\n - The platform's planning, review, and CCA layers already run on Claude Code,\n   and Claude Code's native capabilities (the built-in `/code-review` skill and\n   the Workflow multi-agent orchestration tool) have no Codex equivalent.\n-- The Goal lifecycle redesign (ADR 0009) requires an autonomous post-freeze\n+- The Goal lifecycle redesign (SADR-0009) requires an autonomous post-freeze\n   loop driven from the operator's Claude Code session, including bounded\n   repair dispatch, which must not depend on a runtime that cannot be invoked\n   unattended.\n \n The constitution claims Shiki is runtime-agnostic (\"not a Claude-only\n-workflow\", ADR 0002). Any change to the implementer default must preserve the\n+workflow\", SADR-0002). Any change to the implementer default must preserve the\n registry-based runtime model rather than hardcoding one vendor.\n \n ## Decision\n@@ -39,7 +39,7 @@\n   the task to review or repair-needed.\n - Keep Codex registered as an optional implementer runtime. `shiki runner\n   codex`, the Codex handoff templates, and the ChatGPT OAuth auth model from\n-  ADR 0005 remain supported for tasks explicitly assigned to `codex`.\n+  SADR-0005 remain supported for tasks explicitly assigned to `codex`.\n - Commit to mandating two Claude-native capabilities as loop contract in\n   follow-up Goals (status: the code-review gate was delivered by Goal G-B \u2014\n   skill registry, checklists item PR-12, default required_skills \u2014 while the\n@@ -60,7 +60,7 @@\n \n Out of scope: removing Codex support, changing the CCA runtime (Claude Code\n Action with `CLAUDE_CODE_OAUTH_TOKEN` stays), and API-key based automation\n-(still requires its own ADR per ADR 0005).\n+(still requires its own target ADR per SADR-0005).\n \n ## Consequences\n \n@@ -100,7 +100,7 @@\n   harness rejection of direct `codex exec` is outside Shiki's control, and the\n   plan's mandatory native capabilities cannot run on Codex at all.\n - **Remove Codex entirely**: rejected; it abandons the runtime-agnostic\n-  constitution (ADR 0002) for no operational gain and forecloses\n+  constitution (SADR-0002) for no operational gain and forecloses\n   cross-runtime adversarial review.\n - **GitHub Actions cloud implementation via claude-code-action**: rejected as\n   default; durable evidence is automatic but token cost, Node 24 workflow\ndiff --git a/docs/adr/0009-requirements-definition-and-spec-freeze.md b/docs/adr/SADR-0009-requirements-definition-and-spec-freeze.md\nindex 29b3f7871bef9203a8b4a831a65c39eb2f506e42..52e7dd69ad0aa4efe0b68c62678abbf7df02aaec\n--- a/docs/adr/0009-requirements-definition-and-spec-freeze.md\n+++ b/docs/adr/SADR-0009-requirements-definition-and-spec-freeze.md\n@@ -1,4 +1,4 @@\n-# ADR 0009: Merge Grill And Planning Into Requirements Definition With Spec Freeze\n+# SADR-0009: Merge Grill And Planning Into Requirements Definition With Spec Freeze\n \n ## Status\n \ndiff --git a/docs/adr/0010-external-ai-guardian-review.md b/docs/adr/SADR-0010-external-ai-guardian-review.md\nindex e6108bbe8bb4e73e0ff4684c8f78001a80438c4e..f3510b145664298adafb151b6f0db51b972f01ec\n--- a/docs/adr/0010-external-ai-guardian-review.md\n+++ b/docs/adr/SADR-0010-external-ai-guardian-review.md\n@@ -1,4 +1,4 @@\n-# ADR 0010: External AI Guardian Review As A First-Class Approval Authority\n+# SADR-0010: External AI Guardian Review As A First-Class Approval Authority\n \n ## Status\n \n@@ -8,7 +8,7 @@\n \n Shiki is intended to be an autonomous, recursive, self-improving agent\n framework: authority-in-the-loop, not human-in-the-loop. Until now the Guardian\n-approval required for high/critical-risk merges (guardian-policy.json, ADR\n+approval required for high/critical-risk merges (guardian-policy.json, SADR\n governance) recognized only human authority sources \u2014 a GitHub review approval,\n the `guardian:approved` label applied by a configured Guardian, or a comment\n carrying `Guardian approval granted` plus the current head SHA.\n@@ -89,7 +89,7 @@\n   autonomous design the operator intends and produced the impersonation\n   incident.\n - **Record AI approval as an operator comment**: rejected as forgery of human\n-  authority \u2014 the exact failure this ADR corrects.\n+  authority \u2014 the exact failure this SADR corrects.\n - **Deliver the artifact as a committed `.shiki/` file**: rejected; a committed\n   approval inside the PR being approved is forgeable by anyone who can push.\n - **A dedicated reviewer bot identity instead of a relayed comment**: deferred;\ndiff --git a/docs/adr/0011-autonomous-quality-gate-evidence.md b/docs/adr/SADR-0011-autonomous-quality-gate-evidence.md\nindex bca0246c5d46e44afbde15486a75eec425fc02f5..9c93eac16d8c3a6d4b11bd7224e3955102cd7db4\n--- a/docs/adr/0011-autonomous-quality-gate-evidence.md\n+++ b/docs/adr/SADR-0011-autonomous-quality-gate-evidence.md\n@@ -1,4 +1,4 @@\n-# ADR 0011: Autonomous Quality-Gate Evidence \u2014 Deterministic Facts And An Independent Verifier\n+# SADR-0011: Autonomous Quality-Gate Evidence \u2014 Deterministic Facts And An Independent Verifier\n \n ## Status\n \ndiff --git a/docs/adr/0012-autonomous-loop-closeout-to-main.md b/docs/adr/SADR-0012-autonomous-loop-closeout-to-main.md\nindex 637fad6dde4989c9c2bf0f81f676d8d29991123c..ec7d85f81030a676bcbba28b43eb365422a36eac\n--- a/docs/adr/0012-autonomous-loop-closeout-to-main.md\n+++ b/docs/adr/SADR-0012-autonomous-loop-closeout-to-main.md\n@@ -1,4 +1,4 @@\n-# 0012 \u2014 Autonomous loop pushes goal completion to main via a closeout PR\n+# SADR-0012 \u2014 Autonomous loop pushes goal completion to main via a closeout PR\n \n - Status: accepted\n - Date: 2026-06-18\ndiff --git a/docs/adr/0013-bootstrap-grants-actions-approve-pull-requests.md b/docs/adr/SADR-0013-bootstrap-grants-actions-approve-pull-requests.md\nindex b6c69d64f1c5ced346b88d6859586715b3b5bd0e..cf48ff4a6716267f539ca58edd1a852e7c7a2d64\n--- a/docs/adr/0013-bootstrap-grants-actions-approve-pull-requests.md\n+++ b/docs/adr/SADR-0013-bootstrap-grants-actions-approve-pull-requests.md\n@@ -1,4 +1,4 @@\n-# ADR 0013: Bootstrap Grants GitHub Actions Permission To Create And Approve Pull Requests\n+# SADR-0013: Bootstrap Grants GitHub Actions Permission To Create And Approve Pull Requests\n \n ## Status\n \n@@ -31,7 +31,7 @@\n security tradeoff: it lets repository automation satisfy a human-review gate. In\n a multi-maintainer repo that could let a compromised or buggy workflow\n self-approve changes. So whether bootstrap turns it on by default \u2014 and with\n-what default token scope \u2014 is an ADR-worthy decision, not an implementation\n+what default token scope \u2014 is a SADR-worthy decision, not an implementation\n detail.\n \n ## Decision\n@@ -48,7 +48,7 @@\n Specifically:\n \n - **`can_approve_pull_request_reviews` defaults to `true`.** Shiki's default\n-  posture is solo/AFK autonomous operation (ADR 0008/0009/0012), where the CCA\n+  posture is solo/AFK autonomous operation (SADR-0008/0009/0012), where the CCA\n   Review Bridge is the only path that can satisfy `required_review: true` after\n   the CCA and Guardian gates have already passed. The default makes a\n   freshly-bootstrapped repo able to run the autonomous loop end to end without a\ndiff --git a/docs/adr/0014-codex-app-external-ai-guardian-ui-adapter.md b/docs/adr/SADR-0014-codex-app-external-ai-guardian-ui-adapter.md\nindex 238db06279334caa341c117362df9271382a7fcd..65f12e9939321e1b3e389a34c822fc1549e5155d\n--- a/docs/adr/0014-codex-app-external-ai-guardian-ui-adapter.md\n+++ b/docs/adr/SADR-0014-codex-app-external-ai-guardian-ui-adapter.md\n@@ -1,4 +1,4 @@\n-# ADR 0014: Use Codex App As The External AI Guardian UI Adapter\n+# SADR-0014: Use Codex App As The External AI Guardian UI Adapter\n \n ## Status\n \ndiff --git a/docs/adr/0015-contract-approval-before-dispatch.md b/docs/adr/SADR-0015-contract-approval-before-dispatch.md\nindex fad6c656741a20a7684ba16080a7db159f63a52f..6061271a46d3f1d6ae7174477de3fb75673123a6\n--- a/docs/adr/0015-contract-approval-before-dispatch.md\n+++ b/docs/adr/SADR-0015-contract-approval-before-dispatch.md\n@@ -1,4 +1,4 @@\n-# ADR 0015: Approve The Task Contract Before Dispatch, Not The Diff After It\n+# SADR-0015: Approve The Task Contract Before Dispatch, Not The Diff After It\n \n ## Status\n \n@@ -9,7 +9,7 @@\n Guardian approval for high/critical risk is expressible only on a pull request.\n Every approval source in `guardian-policy.json` \u2014 GitHub review, the\n `guardian:approved` label, the `Guardian approval granted` comment, and the\n-`external_ai_guardian_review` artifact (ADR 0010/0014) \u2014 is bound to a PR, and\n+`external_ai_guardian_review` artifact (SADR-0010/0014) \u2014 is bound to a PR, and\n three of the four require the current head SHA. Approval therefore cannot exist\n before an implementation PR exists.\n \n@@ -81,7 +81,7 @@\n An envelope breach must surface only on the non-repairable policy gate, never on\n the repairable `MergeGate metadata check`. Otherwise the Goal Loop routes it to\n `dispatch_repair` \u2014 instructing a runner to make the breach go away \u2014 which is\n-exactly what this ADR forbids.\n+exactly what this SADR forbids.\n \n **Contract immutability.** After registration, an implementation PR must not\n change the governance fields of its own task contract (`scope`, `non_goals`,\n@@ -116,7 +116,7 @@\n fails any verdict that is not `complete`, and the signal the CCA prompt reads is\n computed from `evaluate_guardian_approval` alone. Without the OR-merge at the\n signal, a contract-approved PR still returns `needs_guardian`, the CCA job fails,\n-and the MergeGate policy check never runs \u2014 the gate this ADR changes would never\n+and the MergeGate policy check never runs \u2014 the gate this SADR changes would never\n be reached. The CCA *prompt* needs no change: a `contract_approval` entry in the\n signal's existing `sources` array satisfies its current wording.\n \n@@ -130,7 +130,7 @@\n written, and an approved contract lets its implementation PRs merge through the\n same automatic path as low/medium risk. The wasted implement-then-block cycle\n disappears. Guardian review shifts from reading diffs to reading contracts, which\n-is both earlier and smaller; the external AI Guardian path (ADR 0010/0014) works\n+is both earlier and smaller; the external AI Guardian path (SADR-0010/0014) works\n on contracts with no change.\n \n We accept that the Guardian authorizes a class of change without seeing its\n@@ -146,7 +146,7 @@\n adversarial tests for the new approval path equivalent in rigor to\n `test_shiki_governance_evidence.sh`.\n \n-Bootstrap costs, which apply only while this ADR is being implemented. The tasks\n+Bootstrap costs, which apply only while this SADR is being implemented. The tasks\n that build Contract Approval merge under the existing rules, so the ones\n dispatched after the pre-dispatch gate lands but before the approval path lands\n need a Guardian-approved Contract PR *and* a live Guardian approval on their\n@@ -210,7 +210,7 @@\n risk; they do not bound authorized work.\n \n **Auto-escalate risk on envelope breach instead of stopping.** Returns the\n-workflow to a diff-time Guardian decision \u2014 the exact behavior this ADR removes.\n+workflow to a diff-time Guardian decision \u2014 the exact behavior this SADR removes.\n \n **Resolve governance fields from the base snapshot only.** Rejected against the\n registration flow: implementation PRs add their own contract, so the base has no\ndiff --git a/docs/adr/0016-locks-bound-product-paths.md b/docs/adr/SADR-0016-locks-bound-product-paths.md\nindex b5e850f3f1f7f4ef6e7f9d1763a29a51bd300c45..46b40a9b99a7aae644c02bb458f5473ff0a90ce2\n--- a/docs/adr/0016-locks-bound-product-paths.md\n+++ b/docs/adr/SADR-0016-locks-bound-product-paths.md\n@@ -1,4 +1,4 @@\n-# ADR 0016: Locks bound product paths; the Shiki mirror is bound by record identity\n+# SADR-0016: Locks bound product paths; the Shiki mirror is bound by record identity\n \n ## Status\n \n@@ -6,8 +6,8 @@\n \n - Date: 2026-07-28\n - Deciders: operator (mizutani-140), Claude (planner)\n-- Related: ADR 0004 (GitHub-first with a `.shiki` mirror), ADR 0009 (Spec Freeze),\n-  ADR 0012 (autonomous loop closeout to main), ADR 0015 (Contract Approval before\n+- Related: SADR-0004 (GitHub-first with a `.shiki` mirror), SADR-0009 (Spec Freeze),\n+  SADR-0012 (autonomous loop closeout to main), SADR-0015 (Contract Approval before\n   dispatch), remediation goal task R-02, the live PR #179 deadlock.\n \n ## Context\n@@ -90,11 +90,11 @@\n files, documentation files and named source modules are nameable; runtime-\n allocated ledger, runner and report ids are not \u2014 and those live in `.shiki`,\n which locks no longer govern. Existing frozen contracts are brought into line by\n-a one-time Spec Amendment per plan, which must land before ADR 0015's contract\n+a one-time Spec Amendment per plan, which must land before SADR-0015's contract\n immutability makes `locks` unwritable from the implementation PR.\n \n **Lock state is resolved from the durable side.** MergeGate resolves the lock\n-registry, and ADR 0015's governance fields, from the base `.shiki` snapshot when\n+registry, and SADR-0015's governance fields, from the base `.shiki` snapshot when\n present, falling back to head only when absent. The `MergeGate metadata check`\n job is given `--base-shiki`, which it does not receive today. Locks are not\n derived from the live open-pull-request set: a pull request body is untrusted\n@@ -108,7 +108,7 @@\n Explicitly out of scope: moving MergeGate's contract resolution off the PR\n checkout wholesale; making the mirror a projection of GitHub state; introducing\n new lock kinds; moving ledger evidence out of the repository; and unwinding\n-ADR 0012's closeout pull request. Registration of multi-task goals is ADR 0015's\n+SADR-0012's closeout pull request. Registration of multi-task goals is SADR-0015's\n Contract PR and is not re-decided here.\n \n ## Consequences\n@@ -127,7 +127,7 @@\n \n Risks accepted. First, goal-scoped identity is only as strong as `goal_id`,\n which is resolved from the PR body and the head task file; the mitigation is to\n-add `goal_id` to ADR 0015's frozen governance field set, and until that lands a\n+add `goal_id` to SADR-0015's frozen governance field set, and until that lands a\n task file can name a foreign goal. Second, base-when-present resolution is\n unavailable on fork pull requests, where the head is contributor-controlled;\n fork pull requests already cannot complete a Shiki merge, and this decision does\n@@ -139,7 +139,7 @@\n path, not by implementation pull requests.\n \n New obligations: one Spec Amendment per frozen plan to rewrite `locks`, ordered\n-before ADR 0015 T1; extension of the adversarial suites in\n+before SADR-0015 T1; extension of the adversarial suites in\n `scripts/test_shiki_adversarial_state.sh` and\n `scripts/test_shiki_governance_evidence.sh` to cover the eleven newly-scoped\n directories; and updating `scripts/test_shiki_loop_lock_guard.sh`,\n@@ -151,32 +151,32 @@\n whether the loop's transitive sync of ledger `evidence` references can be\n deleted once the Contract PR carries its own source plan.\n \n-## Relationship to ADR 0004\n+## Relationship to SADR-0004\n \n-This ADR **clarifies something ADR 0004 left unspecified**. ADR 0004 decides that\n+This SADR **clarifies something SADR-0004 left unspecified**. SADR-0004 decides that\n GitHub is the operational source of truth and that each target repository keeps a\n `.shiki/` mirror. It says nothing about pull requests writing the mirror, does\n not name MergeGate, and does not make the mirror an input to a gate. Nothing in\n-ADR 0004's Decision or Consequences is amended, superseded or weakened: the\n+SADR-0004's Decision or Consequences is amended, superseded or weakened: the\n mirror still exists, still records the same artifacts, and pull request\n-boundaries remain the coordination surface. What this ADR settles is the question\n+boundaries remain the coordination surface. What this SADR settles is the question\n 0004 never reached \u2014 by what authority a pull request may write a mirror record \u2014\n and it answers it the way `.shiki/manifest.json` already declares\n (`pr_mutation: current-task-or-current-goal-only`).\n \n-It **amends ADR 0012**, whose Decision item 5 states that \"Loop-executed tasks\n+It **amends SADR-0012**, whose Decision item 5 states that \"Loop-executed tasks\n declare the `path:.shiki/**` lock, which covers every `.shiki` file the closeout\n stages ... so `files_outside_locks` is satisfied without per-file lock\n bookkeeping.\" That clause is replaced: the closeout's mirror writes are\n-authorized by record identity, not by a declared lock. ADR 0012's substantive\n+authorized by record identity, not by a declared lock. SADR-0012's substantive\n decision \u2014 that completion reaches `main` through a closeout pull request rather\n than local attestation \u2014 is preserved unchanged.\n \n-It is **complementary to ADR 0015**, not a substitute. ADR 0015 owns contract\n+It is **complementary to SADR-0015**, not a substitute. SADR-0015 owns contract\n immutability, the pre-dispatch gate, the Contract PR and the Scope Envelope. This\n-ADR owns what a lock means and who may write the mirror. The one ordering\n+SADR owns what a lock means and who may write the mirror. The one ordering\n constraint between them is that the lock-narrowing Spec Amendment must precede\n-ADR 0015 T1's freezing of the `locks` field.\n+SADR-0015 T1's freezing of the `locks` field.\n \n ## Alternatives Considered\n \n@@ -186,7 +186,7 @@\n the only writer that can register them may run. Migrating the Guardian signal to\n that store makes every registration PR fail closed to `needs_guardian` at every\n risk level, because the task file is by definition absent from the store, which\n-removes the low/medium autonomous path ADR 0008/0009 exists to provide. And it\n+removes the low/medium autonomous path SADR-0008/0009 exists to provide. And it\n leaves schemas, manifest, config, guardian policy, templates, migrations, plans,\n worktrees, runner records, runs and handoffs with no writer at all in a\n repository whose product is `.shiki`. Measured benefit on the frozen backlog:\n@@ -199,14 +199,14 @@\n namespace is not in fact ownership-partitioned \u2014 56 ledger entries carry no\n `task_id`, and one goal DAG is writable by 35 registered tasks. The correct\n expression of its insight is that locks do not govern the mirror at all, which is\n-what this ADR decides.\n+what this SADR decides.\n \n **Make the mirror a projection written only by a registration writer and a\n post-merge projection job.** Rejected. It requires a bypass actor with write\n access to the protected default branch; it makes its own master invariant\n readable from a manifest that pull requests may edit; it deletes both reconcile\n modes and leaves a corrupted mirror with no in-band repair path, contradicting\n-ADR 0004's consequence that mirror conflicts are repaired against GitHub state;\n+SADR-0004's consequence that mirror conflicts are repaired against GitHub state;\n and its content-derived ids are rejected by `validate_shiki`'s currently-green id\n pattern.\n \ndiff --git a/docs/adr/0017-bookkeeping-closeouts-do-not-inherit-risk.md b/docs/adr/SADR-0017-bookkeeping-closeouts-do-not-inherit-risk.md\nindex 554e3a0d526a72ca067108c5464cc68c3edcf174..b9676857a4713850e063af7c39c8e0b01ecb4c66\n--- a/docs/adr/0017-bookkeeping-closeouts-do-not-inherit-risk.md\n+++ b/docs/adr/SADR-0017-bookkeeping-closeouts-do-not-inherit-risk.md\n@@ -1,4 +1,4 @@\n-# ADR 0017: A Bookkeeping Closeout Does Not Inherit Its Task's Risk\n+# SADR-0017: A Bookkeeping Closeout Does Not Inherit Its Task's Risk\n \n ## Status\n \n@@ -6,7 +6,7 @@\n \n ## Context\n \n-ADR 0012 made the goal loop push a task's completion to the default branch through a\n+SADR-0012 made the goal loop push a task's completion to the default branch through a\n closeout pull request: after the implementation PR merges, the loop opens a second PR\n carrying `task.status = done`, `lock.state = released`, and \u2014 when the task completes\n its goal \u2014 `goal.status = complete` plus one scorecard report. Nothing else. The\n@@ -49,14 +49,14 @@\n 4. The task file's governance fields (`scope`, `non_goals`, `required_skills`,\n    `risk_level`, `locks`, `acceptance_checks`, `test_command`) are byte-identical to\n    the base snapshot. Only `status`, `expected_pr`, `closeout_pr`, `ledger_evidence`\n-   and `expected_branch` may differ \u2014 the ADR 0015 contract-immutability rule,\n+   and `expected_branch` may differ \u2014 the SADR-0015 contract-immutability rule,\n    unchanged.\n \n    `expected_branch` is in that set because a closeout is *by construction* on a\n    different branch from the implementation it closes, and the loop rewrites the\n    field accordingly. It is safe to let it differ because MergeGate independently\n    requires `expected_branch` to equal the PR's actual head ref, so a PR cannot set\n-   it to anything other than the branch it is really on. The first draft of this ADR\n+   it to anything other than the branch it is really on. The first draft of this SADR\n    omitted it, which made the exemption unreachable: every real closeout failed\n    condition 4 on a field the loop always changes. See PR #209.\n 5. The state transitions are exactly the terminal set: task `review` \u2192 `done`, lock\n@@ -113,12 +113,12 @@\n classification must be derived, not asserted.\n \n **Fold the closeout into the implementation PR** so no second PR exists. Rejected under\n-ADR 0012, which recorded why: a task file that is already `done` on the implementation\n+SADR-0012, which recorded why: a task file that is already `done` on the implementation\n branch short-circuits the loop's review \u2192 CCA \u2192 merge \u2192 done ordering into a local\n goal-completion before any merge happens.\n \n **Have the loop auto-merge closeouts with elevated credentials.** Rejected outright: that\n-is a runtime authoring its own approval, which ADR 0010 and the constitution's\n+is a runtime authoring its own approval, which SADR-0010 and the constitution's\n approval-record prohibition forbid. It would also be indistinguishable, in the evidence,\n from the forgery those rules exist to prevent.\n \ndiff --git a/docs/adr/0018-carried-guardian-approval-across-a-proven-base-sync.md b/docs/adr/SADR-0018-carried-guardian-approval-across-a-proven-base-sync.md\nindex 22358562da390ed8b45cd600da08a43d378b4680..e6e2907cefa776460af73ddf7d39db605f3c39b0\n--- a/docs/adr/0018-carried-guardian-approval-across-a-proven-base-sync.md\n+++ b/docs/adr/SADR-0018-carried-guardian-approval-across-a-proven-base-sync.md\n@@ -1,4 +1,4 @@\n-# ADR 0018: Carried Guardian Approval Across A Proven Base Sync\n+# SADR-0018: Carried Guardian Approval Across A Proven Base Sync\n \n ## Status\n \n@@ -34,7 +34,7 @@\n Guardian evaluator that consumes it (`shiki_guardian.evaluate_guardian_approval`,\n the `guardian_comment_carried` source) were built and tested in isolation. Neither\n does anything until a caller passes the flags that turn the carry on, so the\n-feature was invisible in CI. This ADR records the decision the two of them\n+feature was invisible in CI. This SADR records the decision the two of them\n implement, and this task wires it into the four CI invocations that judge Guardian\n approval.\n \ndiff --git a/docs/adr/README.md b/docs/adr/README.md\nindex c70e057a4520c36e042ab68c53cd4a21ef709b71..5da43856836e2ce6a150f8127766e2f88355ab4b\n--- a/docs/adr/README.md\n+++ b/docs/adr/README.md\n@@ -1,40 +1,41 @@\n-# Architecture Decision Records\n+# Decision Record Namespaces\n \n-This directory holds Shiki's Architecture Decision Records (ADRs): durable\n-records of hard-to-reverse platform decisions. ADRs are part of the Shiki source\n-of truth alongside GitHub state, the `.shiki/` mirror, and `CONTEXT.md`.\n+This directory holds two independent decision namespaces:\n \n-## How ADRs Work\n+- Shiki Architecture Decision Records (SADRs), named\n+  `SADR-NNNN-<short-slug>.md`, record hard-to-reverse Shiki platform decisions.\n+- Target Architecture Decision Records (ADRs), named\n+  `NNNN-<short-slug>.md`, belong to the target product.\n \n-- New ADRs are added as `docs/adr/NNNN-<short-slug>.md`, where `NNNN` is the next\n-  zero-padded sequence number.\n-- The **canonical list of ADRs is the set of files matching `docs/adr/*.md`** in\n-  this directory. Browse `docs/adr/*.md` for the authoritative, current set.\n-- Use [`template.md`](template.md) as the starting point for a new ADR.\n-- Record an ADR when a decision is hard to reverse, surprising without context,\n-  and a real tradeoff. Do not write an ADR for routine or easily reversible\n-  choices.\n+The same four-digit number may appear once in each namespace. A platform\n+`SADR-NNNN` citation resolves only to `SADR-NNNN-*.md`; a target `ADR NNNN`\n+citation resolves only to `NNNN-*.md`.\n \n-Using the `docs/adr/*.md` glob as the source of truth means other PRs can add an\n-ADR without editing this index file, avoiding merge contention on a\n-hand-maintained enumeration.\n+## How Decision Records Work\n \n-## Current ADRs (snapshot)\n-\n-The list below is a convenience snapshot and may lag behind the directory. When\n-in doubt, list `docs/adr/*.md`.\n+- New Shiki platform decisions use the next zero-padded SADR number.\n+- New target product decisions use the target's own ADR sequence.\n+- The canonical list is the files in `docs/adr/`; no hand-maintained index is\n+  authoritative.\n+- Use [`template.md`](template.md) as the starting point and choose the filename\n+  namespace that owns the decision.\n+- Record a decision when it is hard to reverse, surprising without context, and\n+  a real tradeoff. Do not write a decision record for routine or easily\n+  reversible choices.\n \n-- [0001 \u2014 Keep Shiki As The Platform Name](0001-keep-shiki-as-platform-name.md)\n-- [0002 \u2014 Use AGENTS As The Shared Agent Constitution](0002-use-agents-as-shared-constitution.md)\n-- [0003 \u2014 Require Skill Gate For MergeGate Readiness](0003-require-skill-gate-for-mergegate-readiness.md)\n-- [0004 \u2014 Use GitHub First With A Repository-Local Shiki Mirror](0004-use-github-first-with-shiki-mirror.md)\n-- [0005 \u2014 Use Codex As Front And Claude Code Action As The GitHub Runtime](0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md)\n-- [0006 \u2014 Separate Codex Implementation, CCA Completion, And MergeGate Authorization](0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md)\n+Using the directory as the source of truth lets independent changes add records\n+without contending on an enumerated index.\n \n-## Adding A New ADR\n+## Adding A Shiki SADR\n \n-1. Copy `template.md` to `docs/adr/NNNN-<short-slug>.md`.\n+1. Copy `template.md` to `docs/adr/SADR-NNNN-<short-slug>.md`.\n 2. Fill in the title, status, context, decision, and consequences.\n-3. Set the status to `Proposed`, then `Accepted` once the decision is approved.\n-4. Open a PR following `CONTRIBUTING.md`. You do not need to edit this index for\n-   the ADR to be discoverable via `docs/adr/*.md`.\n+3. Set the status to `Proposed`, then `Accepted` once the proper authority has\n+   approved the decision.\n+4. Open a PR following `CONTRIBUTING.md`.\n+\n+## Adding A Target ADR\n+\n+1. Copy `template.md` to `docs/adr/NNNN-<short-slug>.md` using the target's own\n+   sequence.\n+2. Cite it as `ADR NNNN`; do not prefix its filename with `SADR-`.\ndiff --git a/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md b/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\nnew file mode 100644\nindex 0000000000000000000000000000000000000000..7455ce1f3bed02ddddb851ac21a1d1a648c9aeba\n--- /dev/null\n+++ b/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\n@@ -0,0 +1,55 @@\n+# SADR-0019: Separate Shiki Platform SADRs From Target ADRs\n+\n+## Status\n+\n+Accepted\n+\n+## Context\n+\n+Shiki installs its platform decision records into `docs/adr/` in every target\n+repository. Shiki and its targets previously used the same numeric\n+`NNNN-<slug>.md` namespace. A target that already owned ADRs 0013 through 0018\n+therefore acquired two records for each number during installation, making an\n+`ADR NNNN` citation ambiguous. Renumbering target decisions would rewrite target\n+history, while renumbering Shiki decisions per target would destroy their stable\n+platform identity.\n+\n+Upgrades also need to remove the former Shiki-owned numeric paths without ever\n+mistaking a target-owned ADR for platform content. The install stamp already\n+records exact shipped paths and content digests, so it can prove ownership only\n+when both still match.\n+\n+## Decision\n+\n+Shiki platform decisions use the `SADR` namespace:\n+\n+- records are named `docs/adr/SADR-NNNN-<slug>.md`;\n+- live platform citations use `SADR-NNNN`;\n+- target product decisions retain `docs/adr/NNNN-<slug>.md` and `ADR NNNN`;\n+- the reference validator resolves the two namespaces independently, so the same\n+  four-digit number may exist once in each namespace.\n+\n+Before a forced upgrade writes anything, the installer inventories the exact\n+former numeric Shiki ADR paths. It may delete a legacy path only when the\n+existing install stamp contains that exact path and its recorded SHA-256 digest\n+matches the file currently on disk. All ownership blockers are reported\n+together and abort the upgrade before its first write. A non-forced install\n+never performs this cleanup.\n+\n+Historical `.shiki/` evidence remains unchanged because it records the namespace\n+that existed when that evidence was produced.\n+\n+## Consequences\n+\n+- Targets may use any ADR number without colliding with Shiki's platform SADRs.\n+- Fresh installs preserve target ADR bytes and install a stable SADR set.\n+- Forced upgrades fail closed when legacy ownership cannot be proven, and a\n+  successful upgrade refreshes the stamp without removed legacy paths.\n+- Current governance, packaging, workflow, prompt, script, and skill references\n+  must move with the platform records.\n+\n+## Alternatives Considered\n+\n+- Collision-only refusal leaves affected targets permanently unupgradable.\n+- Renumbering target ADRs mutates target-owned history.\n+- Per-target Shiki renumbering makes platform citations non-portable.\ndiff --git a/docs/adr/template.md b/docs/adr/template.md\nindex a92a97daddd0b00ab9dd626cc8d86942da2eafed..17dd79175b5fee2105935557f691b40cf9492942\n--- a/docs/adr/template.md\n+++ b/docs/adr/template.md\n@@ -1,14 +1,14 @@\n-# NNNN: <Short Decision Title>\n+# <SADR-NNNN or NNNN>: <Short Decision Title>\n \n ## Status\n \n-Proposed | Accepted | Superseded by ADR-NNNN\n+Proposed | Accepted | Superseded by SADR-NNNN or target ADR NNNN\n \n ## Context\n \n What is the situation, constraint, or force that requires a decision? Reference\n-relevant domain terms (`CONTEXT.md`), prior ADRs, and code reality. State the\n-problem, not the solution.\n+relevant domain terms (`CONTEXT.md`), prior Shiki SADRs or target ADRs, and code\n+reality. State the problem, not the solution.\n \n ## Decision\n \ndiff --git a/docs/agents/autonomous-loop-e2e-acceptance.md b/docs/agents/autonomous-loop-e2e-acceptance.md\nindex bbf84c6081987f159ac6ad9ecf3c82b7aa72e7e8..5c13c1d02a05a90b0dc46cbef7c95a9846d0c237\n--- a/docs/agents/autonomous-loop-e2e-acceptance.md\n+++ b/docs/agents/autonomous-loop-e2e-acceptance.md\n@@ -40,8 +40,8 @@\n 3. `gh auth status` is logged in with merge permission on the repo.\n 4. Branch protection + required checks are configured exactly as the goal-loop\n    required-check set expects (see `docs/agents/branch-protection-smoke.md`).\n-5. The repo `.shiki/policy` permits autonomous low/medium-risk merges (ADR\n-   0008/0009); the MergeGate policy check is the recorded-authority gate.\n+5. The repo `.shiki/policy` permits autonomous low/medium-risk merges\n+   (SADR-0008/0009); the MergeGate policy check is the recorded-authority gate.\n \n The operator's only actions are: register the task and start the loop. **No\n manual pre-clearing** means after `shiki loop run` starts, the operator does not\ndiff --git a/docs/agents/bootstrap-command.md b/docs/agents/bootstrap-command.md\nindex 05f7f203a52cf412b9f97084a6e131ed0f785afd..f7aa0b734dfebee4a3b139335b66ee3415f48713\n--- a/docs/agents/bootstrap-command.md\n+++ b/docs/agents/bootstrap-command.md\n@@ -189,7 +189,9 @@\n ```\n \n Use this only for tests, fixtures, or explicit local-only template inspection.\n-Use `--force` only when you intentionally want to overwrite existing target files.\n+Use `--force` only when you intentionally want to refresh the target template.\n+Legacy numeric Shiki decision cleanup is stamp-and-digest authorized before any\n+write; target ADR paths are not cleanup candidates.\n \n ## Slash Command\n \ndiff --git a/docs/agents/checklists.md b/docs/agents/checklists.md\nindex 4a7f03ade346f53a1c1d82981f3fd744f9ab4547..0c5ec6c53dc7197b7b7a7d18e667b325babd6c45\n--- a/docs/agents/checklists.md\n+++ b/docs/agents/checklists.md\n@@ -32,18 +32,18 @@\n | ID | Item | Blocking |\n | --- | --- | --- |\n | GD-01 | Plan was challenged against `CONTEXT.md`. | blocking for non-trivial Goals |\n-| GD-02 | Relevant ADRs were read or absence was recorded. | blocking for non-trivial Goals |\n+| GD-02 | Relevant SADRs and target ADRs were read or absence was recorded. | blocking for non-trivial Goals |\n | GD-03 | Ambiguous terms were resolved or converted to HITL blockers. | blocking |\n | GD-04 | Edge scenarios were considered. | advisory |\n | GD-05 | Code reality was checked when code could answer. | blocking when applicable |\n | GD-06 | New domain terms were added to `CONTEXT.md` when needed. | blocking when applicable |\n-| GD-07 | ADR was created or explicitly skipped using the ADR criteria. | blocking when applicable |\n+| GD-07 | A SADR or target ADR was created or explicitly skipped using the decision-record criteria. | blocking when applicable |\n \n ## CI \u2014 Context & Impact Checklist\n \n | ID | Item | Blocking |\n | --- | --- | --- |\n-| CI-01 | Relevant docs and ADRs identified. | blocking |\n+| CI-01 | Relevant docs, SADRs, and target ADRs identified. | blocking |\n | CI-02 | Relevant modules, interfaces, seams, callers, and tests identified. | blocking |\n | CI-03 | Dependencies and blocked-by relationships identified. | blocking |\n | CI-04 | Candidate locks identified. | blocking |\n@@ -62,7 +62,7 @@\n | PRD-04 | Implementation decisions are recorded without volatile code detail. | blocking |\n | PRD-05 | Testing decisions are recorded. | blocking |\n | PRD-06 | Out-of-scope items are explicit. | blocking |\n-| PRD-07 | Relevant glossary/ADR links are present when applicable. | blocking |\n+| PRD-07 | Relevant glossary/SADR/target-ADR links are present when applicable. | blocking |\n \n ## SF \u2014 Spec Freeze Checklist\n \n@@ -98,7 +98,7 @@\n | H-02 | Branch/worktree target is present. | blocking |\n | H-03 | Scope and non-goals are present. | blocking |\n | H-04 | Dependencies and locks are present. | blocking |\n-| H-05 | Relevant docs, ADRs, modules, and tests are referenced. | blocking |\n+| H-05 | Relevant docs, SADRs, target ADRs, modules, and tests are referenced. | blocking |\n | H-06 | Required skills are stated. | blocking |\n | H-07 | TDD expectations are stated. | blocking when behavior work |\n | H-08 | Acceptance criteria are included. | blocking |\ndiff --git a/docs/agents/codex-handoff.md b/docs/agents/codex-handoff.md\nindex ede468f47cb277f69a857e8c45df703e4ed0151d..6f97bdffb67e44d7edea2c71e01ea28933c4eb6c\n--- a/docs/agents/codex-handoff.md\n+++ b/docs/agents/codex-handoff.md\n@@ -46,7 +46,7 @@\n \n ## Context\n - Domain terms:\n-- Relevant ADRs:\n+- Relevant SADRs and target ADRs:\n - Relevant docs:\n - Relevant modules/interfaces/seams:\n - Similar tests/prior art:\ndiff --git a/docs/agents/completion-check-agent.md b/docs/agents/completion-check-agent.md\nindex 13e4c035db35025f4e3cdcc5adfb24314ac4d521..61bfa1218cf36ca605250c91919e620f33238aa0\n--- a/docs/agents/completion-check-agent.md\n+++ b/docs/agents/completion-check-agent.md\n@@ -32,7 +32,7 @@\n - `AGENTS.md`\n - `CLAUDE.md`\n - `CONTEXT.md`\n-- relevant ADRs\n+- relevant SADRs and target ADRs\n - parent Goal issue\n - PRD issue or document\n - task issue\ndiff --git a/docs/agents/decision-control.md b/docs/agents/decision-control.md\nindex 7d8ee5ab03a5ccbcbd04056ae61de03785a2a9c9..8c94ecda00fffaa0e92c325ab83aa9776bd1dac1\n--- a/docs/agents/decision-control.md\n+++ b/docs/agents/decision-control.md\n@@ -11,7 +11,7 @@\n | Decision | Owner | When | Required inputs | Output | Enforcement surface |\n | --- | --- | --- | --- | --- | --- |\n | Is the Goal clear enough to plan? | Planner + Guardian when needed | Goal intake | User request, existing docs, risk signals | Goal readiness verdict | Goal checklist, issue template, ledger |\n-| Must `grill-with-docs` run? | Planner / Skill Gate | Before PRD or issue split | Ambiguity, domain terms, ADR conflicts, unclear boundaries | Skill invocation or explicit skip rationale | Skill Gate checklist |\n+| Must `grill-with-docs` run? | Planner / Skill Gate | Before PRD or issue split | Ambiguity, domain terms, SADR/target-ADR conflicts, unclear boundaries | Skill invocation or explicit skip rationale | Skill Gate checklist |\n | Is a PRD required? | Planner | After Goal context stabilizes | Goal, `grill-with-docs` output, Context & Impact output | PRD required / not required | PRD checklist, CCA evidence check |\n | Can the spec be frozen? | Operator only | End of Requirements Definition, before issue split | PRD/requirements summary, scope inventory (SF-02), open-question list | `spec_freeze` block in the plan + ledger evidence | `require_grilled_plan` (plan ingest/run), SF checklist |\n | Can a frozen spec change? | Operator only (Spec Amendment) | When implementation contradicts the frozen spec | Contested decisions, scoped re-grill result | recorded amendment + re-stamped freeze | SF checklist, ledger evidence |\ndiff --git a/docs/agents/domain.md b/docs/agents/domain.md\nindex 258da8ea81e87ac0a6b0b5b5e5c2fde980eaabb7..d57b0cf17ff3e1fa9e6e5885372bf083d8295421\n--- a/docs/agents/domain.md\n+++ b/docs/agents/domain.md\n@@ -5,7 +5,9 @@\n ## Layout\n \n - `CONTEXT.md` defines Shiki language. Keep it implementation-free.\n-- `docs/adr/` records hard-to-reverse decisions.\n+- `docs/adr/SADR-NNNN-*.md` records Shiki platform decisions.\n+- `docs/adr/NNNN-*.md` records target product decisions without sharing the\n+  SADR namespace.\n - `AGENTS.md` is the runtime-neutral agent constitution.\n - `CLAUDE.md` is the Claude Code wrapper around `AGENTS.md`.\n - `.shiki/` is the Target Repository mirror for Goals, PRDs, plans, Task DAGs, contracts, locks, worktree records, ledger entries, CCA verdicts, repair packets, reports, and handoffs.\n@@ -15,6 +17,6 @@\n \n - Use Shiki terms exactly when they exist.\n - Add glossary terms to `CONTEXT.md` only when the term is part of the domain, not a generic programming concept.\n-- Create ADRs only for hard-to-reverse, surprising, tradeoff-based decisions.\n+- Create SADRs or target ADRs only for hard-to-reverse, surprising, tradeoff-based decisions.\n - Keep product-specific language in the Target Repository, not in Shiki Template docs.\n-- If an ADR conflicts with a proposed implementation, surface the conflict before editing.\n+- If a SADR or target ADR conflicts with a proposed implementation, surface the conflict before editing.\ndiff --git a/docs/agents/execution-control.md b/docs/agents/execution-control.md\nindex ad8ee3cdb36be56fedc1f46408ca92b387d74047..bc63c3a73661d12b567016ba299f1a6b66bf9088\n--- a/docs/agents/execution-control.md\n+++ b/docs/agents/execution-control.md\n@@ -63,7 +63,7 @@\n | Branch name | Orchestrator | Orchestrator | Before implementer dispatch | branch policy | branch ref in task/ledger |\n | Lock reservation | Context & Impact | MergeGate | Before dispatch and before merge | `.shiki/locks/`, labels, PR metadata | lock id, owner, scope |\n | CI profile | Planner | MergeGate | During issue triage, rechecked at PR | labels, `.shiki/policy.yaml`, workflow matrix | required check list |\n-| Required status checks | Repo maintainer / Guardian | GitHub branch protection / rulesets | Repository setup, updated via ADR | GitHub settings | branch/ruleset config, ADR |\n+| Required status checks | Repo maintainer / Guardian | GitHub branch protection / rulesets | Repository setup, updated via target ADR | GitHub settings | branch/ruleset config, target ADR |\n | CCA judgment | CCA | GitHub required check / MergeGate | Every non-draft PR update | GitHub Action check | structured CCA verdict |\n | Repair assignment | CCA | MergeGate | After failed CCA/CI/review | repair packet, issue/PR comment | repair id and attempt count |\n | Merge readiness | MergeGate | GitHub branch protection / Guardian if needed | After CCA complete | required checks, review rules, rulesets | mergegate check output |\ndiff --git a/docs/agents/guardian-policy.md b/docs/agents/guardian-policy.md\nindex 4147c7adf05a47adb4fd24ada1a1c2e1f2ba6d2d..a20d9cc6c00a569bb057b9ca8d6b51c7fc4a9847\n--- a/docs/agents/guardian-policy.md\n+++ b/docs/agents/guardian-policy.md\n@@ -98,8 +98,8 @@\n `origin/<default_branch>` and reuses each job's existing base fetch; a shallow\n (`--depth`) or second base fetch would make the proof unresolvable and silently\n disable the carry, so neither is permitted. CCA reads `guardian_comment_carried` as\n-a recorded authority in the deterministic signal and does not re-derive it. See ADR\n-0018 for the full decision, the rejected diff/patch-equality and path-disjointness\n+a recorded authority in the deterministic signal and does not re-derive it. See\n+SADR-0018 for the full decision, the rejected diff/patch-equality and path-disjointness\n alternatives, and the accepted residual.\n \n ## External AI Guardian Review (SADR-0010)\ndiff --git a/docs/agents/implementation-policy.md b/docs/agents/implementation-policy.md\nindex c754144a9fb5bf4a38aa82a60e1670c59e626b44..85c37281bcad196c6b7b02343faf00b03cd9c57a\n--- a/docs/agents/implementation-policy.md\n+++ b/docs/agents/implementation-policy.md\n@@ -31,10 +31,13 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n ## Non-Negotiables\n \n 1. **Planning precedes implementation.** Non-trivial Goals start with `grill-with-docs` and Context & Impact.\n-2. **Domain language is controlled.** Terms must be checked against `CONTEXT.md`; hard-to-reverse tradeoffs must be checked against ADRs.\n+2. **Domain language is controlled.** Terms must be checked against `CONTEXT.md`; hard-to-reverse tradeoffs must be checked against Shiki SADRs and target ADRs.\n 3. **PRD precedes issue decomposition.** `to-prd` is used once the context is settled enough to become durable intent.\n 4. **Issues are vertical slices.** `to-issues` must produce independently grabbable tracer-bullet tasks, not horizontal layer tickets.\n 5. **Triage controls dispatch.** Only `ready-for-agent` / AFK tasks may be assigned to Codex without human input.\n@@ -95,19 +98,19 @@\n Process:\n \n - Challenge terms against `CONTEXT.md`.\n-- Check ADRs before reopening decisions.\n+- Check SADRs and target ADRs before reopening decisions.\n - Inspect code when code can answer a question.\n - Ask one question at a time when the operator is available.\n - Provide a recommended answer with each question.\n - Record resolved terms in `CONTEXT.md`.\n-- Create ADRs only for hard-to-reverse, surprising, tradeoff-based decisions.\n+- Create SADRs or target ADRs only for hard-to-reverse, surprising, tradeoff-based decisions.\n - Convert unresolved load-bearing questions into HITL blockers.\n \n Outputs:\n \n - Resolved design-tree decisions.\n - Updated glossary terms when needed.\n-- ADRs when justified.\n+- SADRs or target ADRs when justified.\n - HITL blockers when decisions remain unresolved.\n \n ## Phase 3 \u2014 Context & Impact\n@@ -117,7 +120,7 @@\n \n Outputs must include:\n \n-- Relevant documents and ADRs.\n+- Relevant documents, SADRs, and target ADRs.\n - Relevant modules, interfaces, seams, callers, and tests.\n - Candidate locks.\n - Dependencies.\n@@ -140,7 +143,7 @@\n - Testing decisions.\n - Out of scope.\n - Further notes.\n-- Links to relevant Goal, domain terms, and ADRs.\n+- Links to relevant Goal, domain terms, SADRs, and target ADRs.\n \n Phase 4 ends with Spec Freeze: the operator explicitly approves the PRD, the\n plan records a `spec_freeze` block (status, approved_by, source), and the SF\n@@ -240,7 +243,7 @@\n - Dependencies and locks.\n - Risk and Guardian requirements.\n - Ledger completeness.\n-- Documentation and ADR updates.\n+- Documentation and SADR/target-ADR updates.\n - Scope drift.\n \n CCA emits one of:\n@@ -301,7 +304,7 @@\n | Evidence failure | Behavior might be done, but proof is missing. | Add checks, PR evidence, ledger entry, or artifacts. |\n | Scope drift | PR changes unrelated behavior. | Revert unrelated changes or split into new Goal. |\n | Dependency block | Upstream task or lock not complete. | Block until dependency resolves. |\n-| Architecture block | Decision needs ADR or deep-module discussion. | `grill-with-docs` or `improve-codebase-architecture`. |\n+| Architecture block | Decision needs a SADR, target ADR, or deep-module discussion. | `grill-with-docs` or `improve-codebase-architecture`. |\n | Risk block | Security/production/policy change needs human approval. | Guardian decision. |\n \n ## Phase 5.5 \u2014 Execution Decision Control\ndiff --git a/docs/agents/runtime-auth-model.md b/docs/agents/runtime-auth-model.md\nindex 6a2e1a47e81ea89b5c68a210ec07772a9374babc..3c54748d24233dbd0ba8473950282547ef4ce935\n--- a/docs/agents/runtime-auth-model.md\n+++ b/docs/agents/runtime-auth-model.md\n@@ -148,7 +148,7 @@\n \n API-key based runners may be added by a target repository as an explicit extension. They are not the default Shiki template path.\n \n-If a repo opts into API-key mode, record the exception in an ADR and update the repo's `AGENTS.md`, `.shiki/config.yaml`, and workflow permissions.\n+If a repo opts into API-key mode, record the target-specific exception in a target ADR and update the repo's `AGENTS.md`, `.shiki/config.yaml`, and workflow permissions.\n \n ## GitHub Actions Runtime Compatibility\n \ndiff --git a/docs/agents/skill-gate.md b/docs/agents/skill-gate.md\nindex c50398b0d7c60a8598f2a5c88a10015730dced13..3acffe6444617bb1d54b77d4b4708158ec735bc6\n--- a/docs/agents/skill-gate.md\n+++ b/docs/agents/skill-gate.md\n@@ -9,7 +9,7 @@\n | Skill | Trigger | Expected output |\n | --- | --- | --- |\n | `setup-matt-pocock-skills` | First configuring a repo for these skills, or when issue tracker, triage labels, or domain docs are missing. | `docs/agents/` configuration and an `Agent skills` block in `AGENTS.md` or `CLAUDE.md`. |\n-| `grill-with-docs` | Ambiguous plan, unclear terminology, boundary decision, tradeoff, or ADR-worthy design choice. | Clarified decision tree, recommended answers, updates to `CONTEXT.md` or ADRs when decisions crystallize. |\n+| `grill-with-docs` | Ambiguous plan, unclear terminology, boundary decision, tradeoff, or decision-record-worthy design choice. | Clarified decision tree, recommended answers, updates to `CONTEXT.md`, SADRs, or target ADRs when decisions crystallize. |\n | `zoom-out` | Agent lacks a higher-level architectural map. | Map of relevant modules, callers, seams, and domain concepts. |\n | `to-prd` | Settled context should become a PRD. | PRD published to the issue tracker with problem, solution, user stories, decisions, testing, and out-of-scope notes. |\n | `to-issues` | Goal, PRD, or plan must become implementation tickets. | Vertical-slice issues with acceptance criteria, dependencies, and AFK/HITL classification. |\n@@ -17,7 +17,7 @@\n | `tdd` | Feature work or bug fix has observable behavior that can be specified with tests. | Red-green-refactor loop through public interfaces, one vertical slice at a time. |\n | `code-review` | The TDD loop is green and the implementer is about to create or update a PR. Default for every implementation task. | Pre-PR self-review of the task diff with fixes applied on the task branch; findings and resolutions recorded as a ledger entry naming the skill and a `## Pre-PR code review` PR body section. |\n | `diagnose` | Hard bug, failing check, regression, flaky behavior, or performance problem. | Reproduction loop, ranked hypotheses, instrumentation, fix, regression test, post-mortem. |\n-| `improve-codebase-architecture` | Structural friction, testability, AI-navigability, deep-module opportunity, or refactor request. | Architecture candidates and deepening recommendations informed by `CONTEXT.md` and ADRs. |\n+| `improve-codebase-architecture` | Structural friction, testability, AI-navigability, deep-module opportunity, or refactor request. | Architecture candidates and deepening recommendations informed by `CONTEXT.md`, SADRs, and target ADRs. |\n | `prototype` | A throwaway logic or UI prototype is needed to answer a design question. | Clearly marked disposable prototype and captured decision. |\n \n ## MergeGate Interaction\ndiff --git a/docs/operations.md b/docs/operations.md\nindex cf1f452d4c589e38ccd58553bda0ad8684b51eb7..ace48a90ffbec9e5c0a7113bef16a4858bc44b3f\n--- a/docs/operations.md\n+++ b/docs/operations.md\n@@ -89,6 +89,16 @@\n      target history, brought forward by `shiki migrate`, not a template.\n    - **Everything else** (CLI scripts, workflows, schemas, docs) is overwritten.\n \n+   A forced upgrade also migrates the former numeric Shiki decision paths to\n+   `SADR-NNNN-*`. Before the first write, the installer inventories the exact\n+   legacy Shiki paths and authorizes each deletion only when the existing\n+   `.shiki/install-stamp.json` names that path and its SHA-256 digest still\n+   matches the file. An absent, unreadable, malformed, incomplete, or\n+   mismatching stamp reports every blocked path and stops with no filesystem\n+   change. Target-owned `NNNN-*` ADRs are not cleanup candidates. A non-force\n+   install deletes nothing and retains any still-matching legacy stamp entries\n+   so a later forced upgrade can prove ownership.\n+\n    If the target has pending migrations, `--force` refuses and writes nothing \u2014\n    apply migrations first (step 3), then re-run. The run ends with a single\n    summary of every `.new` written, so a half-upgrade is visible.\ndiff --git a/docs/releasing.md b/docs/releasing.md\nindex 10d73828e7dbd64e9d9f6b83bc84d010537ce62a..aafeee8c4d441e1e427efee2ce4f81a647f020e1\n--- a/docs/releasing.md\n+++ b/docs/releasing.md\n@@ -67,7 +67,7 @@\n CLI in `scripts/`. There are no third-party Python runtime dependencies. The CLI\n is intentionally **not** exposed as a `[project.scripts]` console entry point\n because `scripts/shiki.py` is a dependency-free shim and `scripts/` is not\n-packaged as an importable distribution (see the ADR for the rationale).\n+packaged as an importable distribution (see SADR-0007 for the rationale).\n \n Supported install paths:\n \n@@ -155,6 +155,15 @@\n    [docs/operations.md](operations.md#the-new-convention). If the target has\n    pending migrations, `--force` refuses and writes nothing; apply migrations\n    (step 2) first, then re-run.\n+\n+   The SADR namespace upgrade has an additional pre-write gate: each former\n+   numeric Shiki decision path is removed only when the existing install stamp\n+   contains that exact path and a digest matching its current bytes. The\n+   installer aggregates absent/unreadable/malformed-stamp, missing-digest, and\n+   digest-mismatch blockers before raising, so refusal leaves the target tree\n+   byte-identical. Numeric target ADRs are preserved. A successful forced\n+   upgrade installs and stamps `SADR-*` paths and drops the removed legacy paths\n+   from the refreshed stamp.\n 2. Apply repository-local `.shiki/` state migrations with the migrate command:\n \n    ```bash\ndiff --git a/docs/usage.md b/docs/usage.md\nindex b83407ee3970dd395dd893010bc5dbe8bad26051..a130ef6e40dfd8169a5bc701339351097df1a09f\n--- a/docs/usage.md\n+++ b/docs/usage.md\n@@ -67,8 +67,10 @@\n shiki install-target /path/to/target-repo --local-only\n ```\n \n-Use `--force` only when you intentionally want to overwrite existing target\n-files.\n+Use `--force` only when you intentionally want to refresh the target template.\n+For the SADR namespace migration, `--force` deletes only exact legacy Shiki\n+decision paths whose existing install-stamp digest still matches; any ownership\n+blocker stops before the first write. Numeric target ADRs are never candidates.\n \n ## `shiki bootstrap-platform` \u2014 publish the Shiki platform repo\n \ndiff --git a/scripts/guardian_approval_signal.py b/scripts/guardian_approval_signal.py\nindex d059f92b2c82aa2b5d6ca421365537a87886c1cb..16924892714f4f5b9303c0ef0e325a7e70075630\n--- a/scripts/guardian_approval_signal.py\n+++ b/scripts/guardian_approval_signal.py\n@@ -199,8 +199,8 @@\n ) -> Any:\n     \"\"\"Evaluate SADR-0015 Contract Approval for the CCA Guardian signal.\n \n-    The SAME OR that MergeGate applies must reach this deterministic signal: ADR\n-    0015 is explicit that without it, a contract-approved PR still returns\n+    The SAME OR that MergeGate applies must reach this deterministic signal:\n+    SADR-0015 is explicit that without it, a contract-approved PR still returns\n     ``needs_guardian``, the CCA job fails, and the MergeGate policy check never\n     runs \u2014 the gate this change moves would never be reached. This resolves the\n     task the SAME way MergeGate does (the first Shiki task id in the PR body),\ndiff --git a/scripts/shiki_contract_approval.py b/scripts/shiki_contract_approval.py\nindex 941bcb96fdd8b9a85bc0c4f0f04d5dad9d04839c..4a4b418f6527413161db82670bec857a043360e2\n--- a/scripts/shiki_contract_approval.py\n+++ b/scripts/shiki_contract_approval.py\n@@ -142,7 +142,7 @@\n     changed_files_status: Any,\n     registration: Any,\n ) -> ContractApprovalResult:\n-    \"\"\"Whether this PR carries an SADR-0015 Contract Approval.\n+    \"\"\"Whether this PR carries a SADR-0015 Contract Approval.\n \n     The four conditions are evaluated IN ORDER, each failure recorded as a\n     distinct reason string:\ndiff --git a/scripts/shiki_guardian.py b/scripts/shiki_guardian.py\nindex a4ecb79bbda55fd5c8b06adfae2eb99760a03c45..4dd99bbe714e26f421fa19abac7a34ae1b195ecc\n--- a/scripts/shiki_guardian.py\n+++ b/scripts/shiki_guardian.py\n@@ -12,7 +12,7 @@\n # The carry re-derives its proof from git in-process on every judging invocation;\n # it is never read from an artifact, so there is nothing to go stale or be forged.\n # shiki_sync_proof is the ONLY guardian-family module that runs git, and every one\n-# of its invocations is read-only and local (ADR: prove-from-git base sync).\n+# of its invocations is read-only and local (SADR: prove-from-git base sync).\n from shiki_sync_proof import verify_pure_base_sync\n \n GUARDIAN_POLICY_PATH = \".shiki/guardian-policy.json\"\ndiff --git a/scripts/shiki_guardian_review.py b/scripts/shiki_guardian_review.py\nindex dd0f104d457899e0ba5ff1e162011c1e90b6f92b..e10ef58c046324e7ea3a3392feb3048ca77414ce\n--- a/scripts/shiki_guardian_review.py\n+++ b/scripts/shiki_guardian_review.py\n@@ -159,7 +159,7 @@\n     docs_only = _is_docs_only(changed_files)\n     if docs_only and matched == 0:\n         focus.append(\n-            \"Docs/ADR-only change: confirm the change is genuinely documentation/decision-only, introduces no behavior or contract drift, and stays consistent with the constitution and existing ADRs.\"\n+            \"Docs/decision-record-only change: confirm the change is genuinely documentation/decision-only, introduces no behavior or contract drift, and stays consistent with the constitution and existing SADRs and target ADRs.\"\n         )\n \n     # Conservative augmentation: unknown (no category and not docs-only), mixed\ndiff --git a/scripts/shiki_installer.py b/scripts/shiki_installer.py\nindex be064ab7a29ff6836f7f7a1d70500a43e0d11d7b..9fd8eefd137ce7003dad5c0dcb2e48e2cd47ab32\n--- a/scripts/shiki_installer.py\n+++ b/scripts/shiki_installer.py\n@@ -118,6 +118,7 @@\n # what lets a target carry it without a governance-schema change.\n INSTALL_STAMP_PATH = \".shiki/install-stamp.json\"\n INSTALL_STAMP_VERSION = 1\n+LEGACY_SHIKI_SADR_MAX = 18\n \n # Under --force the shipped surface splits three ways, so nothing is lost and\n # nothing is silently stale.\n@@ -237,9 +238,98 @@\n         )\n \n \n+def legacy_shiki_adr_paths() -> tuple[str, ...]:\n+    \"\"\"Former numeric paths for Shiki SADR-0001 through SADR-0018.\n+\n+    The authoritative slugs come from the current SADR filenames. SADR-0019 is\n+    the namespace decision itself and never shipped under a numeric legacy path.\n+    \"\"\"\n+    paths: list[str] = []\n+    for sadr in sorted((ROOT / \"docs\" / \"adr\").glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\")):\n+        number = sadr.name.split(\"-\", 2)[1]\n+        if int(number) <= LEGACY_SHIKI_SADR_MAX:\n+            paths.append(f\"docs/adr/{sadr.name.removeprefix('SADR-')}\")\n+    return tuple(paths)\n+\n+\n+def preflight_legacy_sadr_cleanup(target: Path, *, force: bool) -> tuple[str, ...]:\n+    \"\"\"Authorize exact legacy Shiki ADR removals before the first write.\n+\n+    A non-force install never removes existing files. A forced install needs no\n+    stamp when no legacy Shiki path is present. When legacy paths do exist, each\n+    must be named in a readable install stamp and its current bytes must match\n+    the recorded digest.\n+    \"\"\"\n+    if not force:\n+        return ()\n+    present = tuple(\n+        relative\n+        for relative in legacy_shiki_adr_paths()\n+        if (target / relative).exists() or (target / relative).is_symlink()\n+    )\n+    if not present:\n+        return ()\n+\n+    stamp_path = target / INSTALL_STAMP_PATH\n+    try:\n+        raw = stamp_path.read_text(encoding=\"utf-8\")\n+        stamp = json.loads(raw)\n+    except FileNotFoundError:\n+        detail = \"install stamp is absent\"\n+        blockers = [f\"{relative}: {detail}\" for relative in present]\n+    except (OSError, UnicodeDecodeError) as error:\n+        detail = f\"install stamp is unreadable ({error})\"\n+        blockers = [f\"{relative}: {detail}\" for relative in present]\n+    except json.JSONDecodeError as error:\n+        detail = f\"install stamp is malformed ({error})\"\n+        blockers = [f\"{relative}: {detail}\" for relative in present]\n+    else:\n+        if not isinstance(stamp, dict):\n+            blockers = [\n+                f\"{relative}: install stamp is malformed (expected an object)\"\n+                for relative in present\n+            ]\n+        else:\n+            digests = stamp.get(\"digests\")\n+            blockers = []\n+            if not isinstance(digests, dict):\n+                blockers.extend(\n+                    f\"{relative}: install stamp has no digest for this path\"\n+                    for relative in present\n+                )\n+            else:\n+                for relative in present:\n+                    expected = digests.get(relative)\n+                    if not isinstance(expected, str) or not expected:\n+                        blockers.append(\n+                            f\"{relative}: install stamp has no digest for this path\"\n+                        )\n+                        continue\n+                    try:\n+                        actual = _sha256_file(target / relative)\n+                    except OSError as error:\n+                        blockers.append(\n+                            f\"{relative}: legacy path is unreadable ({error})\"\n+                        )\n+                        continue\n+                    if actual != expected:\n+                        blockers.append(\n+                            f\"{relative}: digest mismatch (stamp {expected}, current {actual})\"\n+                        )\n+\n+    if blockers:\n+        joined = \"\\n\".join(f\"- {blocker}\" for blocker in blockers)\n+        raise ShikiError(\n+            \"legacy Shiki ADR cleanup is not authorized:\\n\"\n+            f\"{joined}\\nNo files were written.\"\n+        )\n+    return present\n+\n+\n def install_template(target: Path, *, force: bool, validate: bool) -> list[NewFileNote]:\n     # Preconditions first: never rewrite the tree of a target that cannot be\n     # upgraded cleanly (see refuse_pending_migrations).\n+    legacy_cleanup = preflight_legacy_sadr_cleanup(target, force=force)\n     refuse_pending_migrations(target)\n \n     # Resolve the target's required CODEOWNERS owner once from its\n@@ -250,6 +340,10 @@\n     # verbatim. See ``codeowners_required_owner``.\n     codeowners_owner = codeowners_required_owner(target)\n \n+    for relative in legacy_cleanup:\n+        (target / relative).unlink()\n+        info(f\"removed stamped legacy Shiki ADR: {target / relative}\")\n+\n     notes: list[NewFileNote] = []\n     for relative in TEMPLATE_PATHS:\n         source = ROOT / relative\n@@ -647,6 +741,22 @@\n     digests = target_stamp_digests(target)\n     existing = read_install_stamp(target)\n     if existing is not None:\n+        # A non-force install deliberately leaves legacy Shiki ADRs in place.\n+        # Preserve only still-matching ownership proof so a later forced\n+        # upgrade can authorize cleanup; never carry a missing or stale digest.\n+        existing_digests = existing.get(\"digests\")\n+        if isinstance(existing_digests, dict):\n+            for relative in legacy_shiki_adr_paths():\n+                expected = existing_digests.get(relative)\n+                path = target / relative\n+                if not isinstance(expected, str) or not path.is_file():\n+                    continue\n+                try:\n+                    actual = _sha256_file(path)\n+                except OSError:\n+                    continue\n+                if actual == expected:\n+                    digests[relative] = actual\n         if _incoming_is_older(commit, existing.get(\"platform_commit\")):\n             warn(\n                 \"kept existing install stamp: incoming install \"\ndiff --git a/scripts/shiki_loop.py b/scripts/shiki_loop.py\nindex 3611641da8aeb5038b1da90e3114b09a473ec6d8..84caa491b9cad0edc4176f209c13e9221e975681\n--- a/scripts/shiki_loop.py\n+++ b/scripts/shiki_loop.py\n@@ -424,7 +424,7 @@\n         # unresolvable CCA verdict has already produced its terminal stop and is never\n         # laundered into a head-moving sync (SADR-0010). It also never fires while the\n         # policy (Guardian) gate is red \u2014 for ANY reason, not only when it is the sole\n-        # red check \u2014 so a head-bound Guardian approval, or an SADR-0015\n+        # red check \u2014 so a head-bound Guardian approval, or a SADR-0015\n         # contract-approval carry that base movement defeated (the policy gate compares\n         # base and head task governance directly), is never silently restored by\n         # adopting base wholesale. It is bounded by a durable per-task counter so the\ndiff --git a/scripts/shiki_tasks.py b/scripts/shiki_tasks.py\nindex aec3809f24b4a0c8c96fc50971cff9ae9ad30381..d531052e9d9717058d4631483cefab44c26a65c1\n--- a/scripts/shiki_tasks.py\n+++ b/scripts/shiki_tasks.py\n@@ -821,7 +821,7 @@\n         \"prompt\": prompt,\n         \"entry_skill\": \"grill-with-docs\",\n         \"required_next_steps\": [\n-            \"Run grill-with-docs until terminology, boundaries, risks, and ADR-worthy decisions are settled.\",\n+            \"Run grill-with-docs until terminology, boundaries, risks, and decision-record-worthy choices are settled.\",\n             \"Obtain the operator's explicit approval of the PRD/requirements (Spec Freeze).\",\n             \"Write a machine-readable plan JSON with grill_with_docs.status=complete and spec_freeze.status=frozen.\",\n             \"Run shiki plan ingest --plan-file PLAN.json.\",\ndiff --git a/scripts/test_shiki_goal_loop.sh b/scripts/test_shiki_goal_loop.sh\nindex 753c7b516e10f0be3bc92a4bd0eb1fa699776ae6..8f3820a0a079b3044424cfe1a6c6b7fe68896e67\n--- a/scripts/test_shiki_goal_loop.sh\n+++ b/scripts/test_shiki_goal_loop.sh\n@@ -231,8 +231,8 @@\n \n # High/critical risk merges autonomously when all required checks are green:\n # the \"MergeGate policy check\" required check IS the Guardian gate (it enforces\n-# guardian-policy.json \u2014 human approval OR external AI guardian review, ADR\n-# 0010), so green checks mean a recorded authority approved. The fake gh returns\n+# guardian-policy.json \u2014 human approval OR external AI guardian review,\n+# SADR-0010), so green checks mean a recorded authority approved. The fake gh returns\n # green checks, simulating that the Guardian gate passed.\n python3 \"$ROOT/scripts/shiki.py\" goal create --target \"$TARGET\" --title \"Guardian gate\" --outcome \"High risk merges via policy gate\" >/tmp/shiki-goal-loop-guardian-goal.json\n GGOAL=\"$(json_get /tmp/shiki-goal-loop-guardian-goal.json goal_id)\"\ndiff --git a/tests/test_adr_references.py b/tests/test_adr_references.py\nindex 51706329c797d5dd9b47e082a333fa83b261d482..d005da4f771f5e8085ec22d4b787980e68f5ae8c\n--- a/tests/test_adr_references.py\n+++ b/tests/test_adr_references.py\n@@ -1,8 +1,8 @@\n-\"\"\"Every ``ADR ####`` citation must resolve to exactly one decision record.\n+\"\"\"Every target ADR and Shiki SADR citation resolves in its own namespace.\n \n Background. ``scripts/mergegate_check.py`` justifies ``is_bookkeeping_closeout``\n-with \"ADR 0017\" nine times and ``scripts/guardian_approval_signal.py`` cites\n-0016/0017 seven times, yet for a stretch the numbering in ``docs/adr/`` stopped\n+with \"SADR-0017\" nine times and ``scripts/guardian_approval_signal.py`` cites\n+SADR-0016/SADR-0017 seven times, yet for a stretch the numbering in ``docs/adr/`` stopped\n at 0015: the authoring commits lived on a coordinator branch and no task ever\n declared ``docs/adr/...`` in its locks, so nothing carried the records to the\n default branch. The stated basis of an exemption that merged seven PRs with zero\n@@ -11,11 +11,12 @@\n and the directory drifted with no signal.\n \n This is the same unbound-pair shape behind other silent failures: a reference\n-and its target with nothing asserting they agree. This test binds them. It scans\n-every ``scripts/*.py`` (plus ``docs/**/*.md`` and the root ``*.md`` governance\n-files, which are cheap) for the ``ADR ####`` pattern and asserts each cited\n-number resolves to exactly one ``docs/adr/####-*.md``. The converse is\n-deliberately NOT required: an ADR may exist without being cited.\n+and its target with nothing asserting they agree. This test binds them. Target\n+``ADR NNNN`` citations resolve only to ``docs/adr/NNNN-*.md``; Shiki\n+``SADR-NNNN`` citations resolve only to ``docs/adr/SADR-NNNN-*.md``. The scan\n+covers current governance docs, scripts, workflows, prompts, packaging metadata,\n+and repo-local Codex guidance while deliberately excluding historical\n+``.shiki`` evidence. The converse is not required: a record may be uncited.\n \n The suite is dependency free: standard-library ``unittest`` only.\n \"\"\"\n@@ -31,17 +32,18 @@\n \n REPO_ROOT = Path(__file__).resolve().parents[1]\n \n-# \"ADR 0016\" -- a four-digit citation. The trailing \\b keeps a five-digit token\n-# from being read as a truncated four-digit number.\n-_CITATION = re.compile(r\"\\bADR (\\d{4})\\b\")\n-# \"0016-locks-bound-product-paths.md\" -- the leading four digits name the record.\n-_ADR_FILENAME = re.compile(r\"^(\\d{4})-.*\\.md$\")\n+# ``ADR 0016`` / ``SADR-0016`` -- a namespace plus four-digit citation. The\n+# trailing ``\\b`` keeps a five-digit token from being read as a truncated one.\n+_CITATION = re.compile(r\"\\b(S?ADR)[ -](\\d{4})\\b\")\n+# Target ADRs retain the numeric filename; Shiki platform decisions use SADR.\n+_DECISION_FILENAME = re.compile(r\"^(?:(SADR)-)?(\\d{4})-.*\\.md$\")\n \n \n class Violation(NamedTuple):\n     \"\"\"A citation that does not resolve to exactly one decision record.\"\"\"\n \n     citing_file: str  # repo-relative path of the file that cites the ADR\n+    namespace: str  # \"ADR\" for target decisions; \"SADR\" for Shiki decisions\n     number: str  # the cited four-digit number, e.g. \"0016\"\n     line: int  # 1-based line of the citation\n     reason: str  # \"missing\" (zero matches) or \"ambiguous\" (two or more)\n@@ -49,44 +51,59 @@\n \n     @property\n     def expected_path(self) -> str:\n-        return f\"docs/adr/{self.number}-*.md\"\n+        prefix = \"SADR-\" if self.namespace == \"SADR\" else \"\"\n+        return f\"docs/adr/{prefix}{self.number}-*.md\"\n \n     def describe(self) -> str:\n         \"\"\"Name the citing file, the cited number, and the missing path.\"\"\"\n         if self.reason == \"missing\":\n             return (\n-                f\"{self.citing_file}:{self.line} cites ADR {self.number} \"\n+                f\"{self.citing_file}:{self.line} cites {self.namespace} {self.number} \"\n                 f\"but no document matches {self.expected_path}\"\n             )\n         joined = \", \".join(self.matches)\n         return (\n-            f\"{self.citing_file}:{self.line} cites ADR {self.number} which \"\n+            f\"{self.citing_file}:{self.line} cites {self.namespace} {self.number} which \"\n             f\"resolves to {len(self.matches)} documents (expected exactly one \"\n             f\"at {self.expected_path}): {joined}\"\n         )\n \n \n-def _adr_index(root: Path) -> dict[str, list[Path]]:\n-    \"\"\"Map every four-digit ADR number to the record files that claim it.\"\"\"\n-    index: dict[str, list[Path]] = {}\n+def _adr_index(root: Path) -> dict[tuple[str, str], list[Path]]:\n+    \"\"\"Map each decision namespace and number to the files that claim it.\"\"\"\n+    index: dict[tuple[str, str], list[Path]] = {}\n     adr_dir = root / \"docs\" / \"adr\"\n     if adr_dir.is_dir():\n         for path in sorted(adr_dir.glob(\"*.md\")):\n-            match = _ADR_FILENAME.match(path.name)\n+            match = _DECISION_FILENAME.match(path.name)\n             if match:\n-                index.setdefault(match.group(1), []).append(path)\n+                namespace = \"SADR\" if match.group(1) else \"ADR\"\n+                index.setdefault((namespace, match.group(2)), []).append(path)\n     return index\n \n \n def _citing_files(root: Path, include_docs: bool = True) -> list[Path]:\n-    \"\"\"Files scanned for citations: scripts/*.py, and cheaply the markdown.\"\"\"\n+    \"\"\"Current, shipped text surfaces scanned for decision citations.\"\"\"\n     files = sorted((root / \"scripts\").glob(\"*.py\"))\n+    files += sorted((root / \"scripts\").glob(\"*.sh\"))\n     if include_docs:\n         docs = root / \"docs\"\n         if docs.is_dir():\n             files += sorted(docs.rglob(\"*.md\"))\n         files += sorted(root.glob(\"*.md\"))\n-    return files\n+        pyproject = root / \"pyproject.toml\"\n+        if pyproject.is_file():\n+            files.append(pyproject)\n+        for relative in (\".github\", \".codex\"):\n+            surface = root / relative\n+            if surface.is_dir():\n+                files += sorted(\n+                    path\n+                    for path in surface.rglob(\"*\")\n+                    if path.is_file()\n+                    and path.suffix in {\".md\", \".py\", \".sh\", \".toml\", \".yaml\", \".yml\"}\n+                )\n+    return sorted(set(files))\n \n \n def find_adr_reference_violations(\n@@ -94,10 +111,9 @@\n ) -> list[Violation]:\n     \"\"\"Return every ``ADR ####`` citation under ``root`` that fails to resolve.\n \n-    A citation resolves when exactly one ``docs/adr/####-*.md`` matches its\n-    number. Zero matches is a \"missing\" violation; two or more is \"ambiguous\".\n-    Records that are never cited produce nothing -- the converse is not\n-    required.\n+    A citation resolves when exactly one record in the same namespace matches\n+    its number. Zero matches is a \"missing\" violation; two or more is\n+    \"ambiguous\". Records that are never cited produce nothing.\n     \"\"\"\n     root = Path(root)\n     index = _adr_index(root)\n@@ -110,13 +126,15 @@\n         rel = path.relative_to(root).as_posix()\n         for lineno, line in enumerate(text.splitlines(), start=1):\n             for match in _CITATION.finditer(line):\n-                number = match.group(1)\n-                found = index.get(number, [])\n+                namespace, number = match.groups()\n+                found = index.get((namespace, number), [])\n                 if len(found) == 1:\n                     continue\n                 reason = \"missing\" if not found else \"ambiguous\"\n                 rels = [p.relative_to(root).as_posix() for p in found]\n-                violations.append(Violation(rel, number, lineno, reason, rels))\n+                violations.append(\n+                    Violation(rel, namespace, number, lineno, reason, rels)\n+                )\n     return violations\n \n \n@@ -129,8 +147,8 @@\n         self.assertEqual(\n             violations,\n             [],\n-            \"Every 'ADR ####' citation must resolve to exactly one \"\n-            f\"docs/adr/####-*.md:\\n{detail}\",\n+            \"Every ADR/SADR citation must resolve exactly once in its own \"\n+            f\"docs/adr namespace:\\n{detail}\",\n         )\n \n     def test_scan_actually_reads_scripts(self) -> None:\n@@ -142,12 +160,38 @@\n             \"the citation scan must cover scripts/*.py\",\n         )\n \n+    def test_platform_citations_do_not_use_unresolved_numeric_shorthand(self) -> None:\n+        shorthand = re.compile(r\"SADR-\\d{4}/\\d{4}\")\n+        offenders = []\n+        for path in _citing_files(REPO_ROOT):\n+            text = path.read_text(encoding=\"utf-8\")\n+            if shorthand.search(text):\n+                offenders.append(path.relative_to(REPO_ROOT).as_posix())\n+        self.assertEqual(\n+            offenders,\n+            [],\n+            \"repeat the SADR prefix so every cited number is independently resolvable\",\n+        )\n+\n     def test_carried_records_are_indexed(self) -> None:\n         # The two records this Goal carried onto the default branch, and which\n         # the scripts cite most, must be present.\n         index = _adr_index(REPO_ROOT)\n-        self.assertIn(\"0016\", index)\n-        self.assertIn(\"0017\", index)\n+        self.assertIn((\"SADR\", \"0016\"), index)\n+        self.assertIn((\"SADR\", \"0017\"), index)\n+\n+    def test_platform_tree_has_only_sadr_0001_through_0019(self) -> None:\n+        adr_dir = REPO_ROOT / \"docs\" / \"adr\"\n+        sadrs = sorted(adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\"))\n+        self.assertEqual(\n+            {path.name.split(\"-\", 2)[1] for path in sadrs},\n+            {f\"{number:04d}\" for number in range(1, 20)},\n+        )\n+        self.assertEqual(\n+            list(adr_dir.glob(\"[0-9][0-9][0-9][0-9]-*.md\")),\n+            [],\n+            \"the Shiki platform tree must not retain legacy numeric SADR paths\",\n+        )\n \n \n class FixtureAdrReferences(unittest.TestCase):\n@@ -180,6 +224,26 @@\n         self.assertIn(\"9991\", message)  # cited number\n         self.assertIn(\"docs/adr/9991-*.md\", message)  # missing path\n \n+    def test_target_adr_does_not_satisfy_missing_platform_sadr(self) -> None:\n+        root = self._make_repo(\n+            adr_names=[\"0042-target-decision.md\"],\n+            script_body='\"\"\"Target ADR 0042 exists, but platform SADR-0042 does not.\"\"\"\\n',\n+        )\n+        violations = find_adr_reference_violations(root, include_docs=False)\n+        self.assertEqual(len(violations), 1)\n+        self.assertEqual(violations[0].namespace, \"SADR\")\n+        self.assertEqual(violations[0].number, \"0042\")\n+        self.assertEqual(violations[0].expected_path, \"docs/adr/SADR-0042-*.md\")\n+\n+    def test_target_adr_and_platform_sadr_may_reuse_a_number(self) -> None:\n+        root = self._make_repo(\n+            adr_names=[\"0042-target-decision.md\", \"SADR-0042-platform-decision.md\"],\n+            script_body='\"\"\"Target ADR 0042 and platform SADR-0042 are distinct.\"\"\"\\n',\n+        )\n+        self.assertEqual(\n+            find_adr_reference_violations(root, include_docs=False), []\n+        )\n+\n     def test_uncited_adr_is_allowed(self) -> None:\n         # An ADR exists but nothing cites it -> not a violation. The converse\n         # (every ADR must be cited) is explicitly NOT required.\ndiff --git a/tests/test_guardian_review.py b/tests/test_guardian_review.py\nindex 88829e7810212635e2fc98aa5918d6f26294677d..00a85a237acd5bf2d6cb5b294128198c422402ca\n--- a/tests/test_guardian_review.py\n+++ b/tests/test_guardian_review.py\n@@ -162,7 +162,7 @@\n \n     def test_docs_only_is_light_and_not_conservative(self):\n         focus = gr.classify_review_focus([\"docs/adr/0099-x.md\", \"README.md\"], \"\", \"low\")\n-        self.assertTrue(any(\"Docs/ADR-only\" in f for f in focus))\n+        self.assertTrue(any(\"Docs/decision-record-only\" in f for f in focus))\n         self.assertFalse(any(\"conservative default\" in f.lower() for f in focus))\n \n     def test_unknown_category_is_conservative(self):\ndiff --git a/tests/test_policy_job_merged_prs.py b/tests/test_policy_job_merged_prs.py\nindex d45512e74681e5e649279236c7d4e79d5c3da471..b97989f8bd74d091de1a6b5275a20d8dc68c4512\n--- a/tests/test_policy_job_merged_prs.py\n+++ b/tests/test_policy_job_merged_prs.py\n@@ -3,8 +3,8 @@\n \n ``.github/workflows/shiki-cca-completion.yml`` has two consumers of the merged-PR\n proof: the CCA *signal* (``guardian_approval_signal.py`` in \"Prepare CCA context\")\n-and the authoritative gate (``mergegate_check.py`` in \"Enforce MergeGate\"). ADR\n-0017's bookkeeping-closeout exemption (condition 6) needs the merged implementation\n+and the authoritative gate (``mergegate_check.py`` in \"Enforce MergeGate\").\n+SADR-0017's bookkeeping-closeout exemption (condition 6) needs the merged implementation\n PR proven, and a closeout carries NO ``post_merge_reconcile`` marker. The signal\n already resolves ``MERGED_PRS`` unconditionally, but \"Enforce MergeGate\" used to\n resolve it only inside ``grep -q '<!-- shiki:post_merge_reconcile -->'``. So a\ndiff --git a/tests/test_sadr_install_migration.py b/tests/test_sadr_install_migration.py\nnew file mode 100644\nindex 0000000000000000000000000000000000000000..a6f4c57e4dcaef73ef600b0d4804744138ab9071\n--- /dev/null\n+++ b/tests/test_sadr_install_migration.py\n@@ -0,0 +1,229 @@\n+\"\"\"SADR installation and collision-safe legacy upgrade behavior.\"\"\"\n+\n+from __future__ import annotations\n+\n+import contextlib\n+import hashlib\n+import io\n+import json\n+import shutil\n+import tempfile\n+import unittest\n+from pathlib import Path\n+\n+import shiki_test_support  # noqa: F401  (path bootstrap)\n+\n+import shiki_installer\n+from tests.test_adr_references import find_adr_reference_violations\n+\n+\n+PRODUCT_NUMBERS = tuple(range(13, 19))\n+\n+\n+def _quiet_install(target: Path, *, force: bool, validate: bool) -> None:\n+    with contextlib.redirect_stdout(io.StringIO()):\n+        shiki_installer.install_template(target, force=force, validate=validate)\n+\n+\n+def _sha256(path: Path) -> str:\n+    return hashlib.sha256(path.read_bytes()).hexdigest()\n+\n+\n+def _legacy_sadr_paths() -> tuple[str, ...]:\n+    return tuple(\n+        f\"docs/adr/{path.name.removeprefix('SADR-')}\"\n+        for path in sorted((shiki_installer.ROOT / \"docs\" / \"adr\").glob(\"SADR-*.md\"))\n+        if path.name != \"SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\"\n+    )\n+\n+\n+def _create_legacy_upgrade_fixture(\n+    target: Path,\n+) -> tuple[dict[str, bytes], tuple[str, ...]]:\n+    adr_dir = target / \"docs\" / \"adr\"\n+    adr_dir.mkdir(parents=True)\n+    product_bytes = {\n+        f\"{number:04d}-cubeshare-product-decision.md\": (\n+            f\"# CubeShare ADR {number:04d}\\n\\nDo not rewrite {number}.\\n\"\n+        ).encode()\n+        for number in PRODUCT_NUMBERS\n+    }\n+    for name, content in product_bytes.items():\n+        (adr_dir / name).write_bytes(content)\n+\n+    legacy_paths = _legacy_sadr_paths()\n+    for relative in legacy_paths:\n+        (target / relative).write_bytes(f\"legacy Shiki bytes for {relative}\\n\".encode())\n+\n+    stamp_path = target / shiki_installer.INSTALL_STAMP_PATH\n+    stamp_path.parent.mkdir(parents=True, exist_ok=True)\n+    stamp = {\n+        \"version\": shiki_installer.INSTALL_STAMP_VERSION,\n+        \"platform_commit\": None,\n+        \"installed_at\": \"2000-01-01T00:00:00+00:00\",\n+        \"digests\": {\n+            relative: _sha256(target / relative) for relative in legacy_paths\n+        },\n+    }\n+    stamp_path.write_text(\n+        json.dumps(stamp, indent=2, sort_keys=True) + \"\\n\", encoding=\"utf-8\"\n+    )\n+    return product_bytes, legacy_paths\n+\n+\n+def _tree_snapshot(root: Path) -> dict[str, tuple[str, bytes | str]]:\n+    \"\"\"Recursive byte/shape snapshot used to prove preflight is write-free.\"\"\"\n+    snapshot: dict[str, tuple[str, bytes | str]] = {}\n+    for path in sorted(root.rglob(\"*\")):\n+        relative = path.relative_to(root).as_posix()\n+        if path.is_symlink():\n+            snapshot[relative] = (\"symlink\", path.readlink().as_posix())\n+        elif path.is_dir():\n+            snapshot[relative] = (\"directory\", \"\")\n+        else:\n+            snapshot[relative] = (\"file\", path.read_bytes())\n+    return snapshot\n+\n+\n+class FreshSadrInstallTests(unittest.TestCase):\n+    def setUp(self) -> None:\n+        self.tmp = Path(tempfile.mkdtemp(prefix=\"shiki-sadr-fresh-\"))\n+        self.addCleanup(lambda: shutil.rmtree(self.tmp, ignore_errors=True))\n+        self.target = self.tmp / \"target\"\n+        self.adr_dir = self.target / \"docs\" / \"adr\"\n+        self.adr_dir.mkdir(parents=True)\n+        self.product_bytes = {\n+            f\"{number:04d}-product-decision.md\": (\n+                f\"# Product ADR {number:04d}\\n\\nTarget-owned bytes {number}.\\n\"\n+            ).encode()\n+            for number in PRODUCT_NUMBERS\n+        }\n+        for name, content in self.product_bytes.items():\n+            (self.adr_dir / name).write_bytes(content)\n+\n+    def test_fresh_install_preserves_product_adrs_and_installs_all_sadrs(self) -> None:\n+        _quiet_install(self.target, force=False, validate=True)\n+\n+        for name, expected in self.product_bytes.items():\n+            self.assertEqual((self.adr_dir / name).read_bytes(), expected)\n+\n+        sadr_numbers = {\n+            path.name.split(\"-\", 2)[1]\n+            for path in self.adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\")\n+        }\n+        self.assertEqual(sadr_numbers, {f\"{number:04d}\" for number in range(1, 20)})\n+\n+        numeric_records = {\n+            path.name\n+            for path in self.adr_dir.glob(\"[0-9][0-9][0-9][0-9]-*.md\")\n+        }\n+        self.assertEqual(numeric_records, set(self.product_bytes))\n+        self.assertEqual(find_adr_reference_violations(self.target), [])\n+\n+\n+class LegacySadrUpgradeTests(unittest.TestCase):\n+    def setUp(self) -> None:\n+        self.tmp = Path(tempfile.mkdtemp(prefix=\"shiki-sadr-upgrade-\"))\n+        self.addCleanup(lambda: shutil.rmtree(self.tmp, ignore_errors=True))\n+        self.target = self.tmp / \"target\"\n+        self.adr_dir = self.target / \"docs\" / \"adr\"\n+        self.product_bytes, self.legacy_paths = _create_legacy_upgrade_fixture(\n+            self.target\n+        )\n+\n+    def test_force_upgrade_deletes_only_stamped_legacy_sadrs(self) -> None:\n+        _quiet_install(self.target, force=True, validate=True)\n+\n+        for relative in self.legacy_paths:\n+            self.assertFalse((self.target / relative).exists(), relative)\n+        for name, expected in self.product_bytes.items():\n+            self.assertEqual((self.adr_dir / name).read_bytes(), expected)\n+\n+        sadrs = list(self.adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\"))\n+        self.assertEqual(len(sadrs), 19)\n+        refreshed = json.loads(\n+            (self.target / shiki_installer.INSTALL_STAMP_PATH).read_text(encoding=\"utf-8\")\n+        )\n+        for relative in self.legacy_paths:\n+            self.assertNotIn(relative, refreshed[\"digests\"])\n+        for path in sadrs:\n+            relative = path.relative_to(self.target).as_posix()\n+            self.assertEqual(refreshed[\"digests\"][relative], _sha256(path))\n+\n+    def test_non_force_install_never_deletes_legacy_files(self) -> None:\n+        before = {\n+            relative: (self.target / relative).read_bytes()\n+            for relative in self.legacy_paths\n+        }\n+        _quiet_install(self.target, force=False, validate=False)\n+        for relative, expected in before.items():\n+            self.assertEqual((self.target / relative).read_bytes(), expected)\n+        refreshed = json.loads(\n+            (self.target / shiki_installer.INSTALL_STAMP_PATH).read_text(encoding=\"utf-8\")\n+        )\n+        for relative in self.legacy_paths:\n+            self.assertEqual(refreshed[\"digests\"][relative], _sha256(self.target / relative))\n+\n+\n+class LegacyCleanupPreflightTests(unittest.TestCase):\n+    def setUp(self) -> None:\n+        self.tmp = Path(tempfile.mkdtemp(prefix=\"shiki-sadr-preflight-\"))\n+        self.addCleanup(lambda: shutil.rmtree(self.tmp, ignore_errors=True))\n+\n+    def test_all_ownership_blockers_abort_before_any_write(self) -> None:\n+        cases = (\n+            (\"absent\", \"install stamp is absent\"),\n+            (\"unreadable\", \"install stamp is unreadable\"),\n+            (\"malformed\", \"install stamp is malformed\"),\n+            (\"missing-digest\", \"install stamp has no digest\"),\n+            (\"digest-mismatch\", \"digest mismatch\"),\n+        )\n+        for name, expected_reason in cases:\n+            with self.subTest(name=name):\n+                target = self.tmp / name\n+                _, legacy_paths = _create_legacy_upgrade_fixture(target)\n+                stamp_path = target / shiki_installer.INSTALL_STAMP_PATH\n+\n+                if name == \"absent\":\n+                    stamp_path.unlink()\n+                elif name == \"unreadable\":\n+                    stamp_path.unlink()\n+                    stamp_path.mkdir()\n+                elif name == \"malformed\":\n+                    stamp_path.write_text(\"{not-json\\n\", encoding=\"utf-8\")\n+                else:\n+                    stamp = json.loads(stamp_path.read_text(encoding=\"utf-8\"))\n+                    if name == \"missing-digest\":\n+                        stamp[\"digests\"] = {}\n+                    else:\n+                        stamp[\"digests\"] = {\n+                            relative: \"deadbeef\" for relative in legacy_paths\n+                        }\n+                    stamp_path.write_text(\n+                        json.dumps(stamp, indent=2, sort_keys=True) + \"\\n\",\n+                        encoding=\"utf-8\",\n+                    )\n+\n+                before = _tree_snapshot(target)\n+                with self.assertRaises(shiki_installer.ShikiError) as raised:\n+                    _quiet_install(target, force=True, validate=False)\n+                after = _tree_snapshot(target)\n+\n+                self.assertEqual(before, after, f\"{name} preflight changed the tree\")\n+                message = str(raised.exception)\n+                self.assertIn(expected_reason, message)\n+                self.assertIn(\"No files were written\", message)\n+                for relative in legacy_paths:\n+                    self.assertIn(relative, message)\n+\n+    def test_absent_stamp_is_allowed_when_no_legacy_path_exists(self) -> None:\n+        target = self.tmp / \"no-legacy\"\n+        target.mkdir()\n+        _quiet_install(target, force=True, validate=False)\n+        self.assertTrue(\n+            (target / \"docs\" / \"adr\" / \"SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\").is_file()\n+        )\n+\n+\n+if __name__ == \"__main__\":\n+    unittest.main()\n\n2026-08-07T08:20:47.096589Z ERROR codex_models_manager::manager: failed to renew cache TTL: missing field `base_instructions` at line 94 column 5\nexec\n/bin/zsh -lc 'for new in docs/adr/SADR-00[0-1][0-9]-*.md docs/adr/SADR-000[1-9]-*.md; do test -f \"$new\" || continue; base=\"${new##*/}\"; old=\"docs/adr/${base#SADR-}\"; test -e \"$old\" && continue; old_lines=$(git show \"HEAD:$old\" 2>/dev/null | wc -l | tr -d '\"' '); new_lines=\"'$(wc -l < \"$new\" | tr -d '\"' '); test -n \\\"\"'$old_lines\" && printf '\"'%s %s %s\\\\n' \\\"\"'$old\" \"$old_lines\" \"$new_lines\"; done | sort -u' in /private/tmp/shiki-adr-collision.pa0KwY/.worktrees/shiki-t-20260807t073307221254z-2d22d654-introduc\ndiff --git a/.codex/skills/shiki/SKILL.md b/.codex/skills/shiki/SKILL.md\nindex c8e2e85d9d75d49cc4f2d6286ee22659c4e53e75..93cded623b2c15d9f65c76491a1c2d813ac9e6c0\n--- a/.codex/skills/shiki/SKILL.md\n+++ b/.codex/skills/shiki/SKILL.md\n@@ -18,6 +18,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n Codex CLI does not register this skill as a custom `/shiki` slash command. When\n the user types `/shiki` in Codex CLI and gets \"Unrecognized command\", explain\n that this is expected: invoke Shiki in Codex with natural language such as\n@@ -69,7 +72,7 @@\n \n ## Responsibilities\n \n-- Claude Code implements and repairs by default (`shiki runner claude`, ADR 0008); Codex implements and repairs tasks explicitly assigned to `codex`.\n+- Claude Code implements and repairs by default (`shiki runner claude`, SADR-0008); Codex implements and repairs tasks explicitly assigned to `codex`.\n - Claude Code Action can act as GitHub-side CCA or reviewer.\n - CCA judges completion.\n - MergeGate authorizes state transitions and merge readiness.\ndiff --git a/.github/prompts/cca-completion-check.md b/.github/prompts/cca-completion-check.md\nindex ed203ad5f302ecc81372d8ab27d2b1232ca97497..fd89950528333ef6242e6d5443904f5392ab7e56\n--- a/.github/prompts/cca-completion-check.md\n+++ b/.github/prompts/cca-completion-check.md\n@@ -19,6 +19,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n ## Required Reading\n \n Read only the files needed to judge the PR contract. Prefer this order:\ndiff --git a/.github/prompts/codex-task-handoff.md b/.github/prompts/codex-task-handoff.md\nindex 695e211d76dea1c60e8d5bb360e78487808286a4..59441d576b01a39569722f226c854e5a740e9eb5\n--- a/.github/prompts/codex-task-handoff.md\n+++ b/.github/prompts/codex-task-handoff.md\n@@ -2,7 +2,7 @@\n \n You are Codex Front acting as the Implementer for a Shiki task.\n \n-Follow `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant ADRs, and `docs/agents/codex-handoff.md`.\n+Follow `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant Shiki SADRs and target ADRs, and `docs/agents/codex-handoff.md`.\n \n Rules:\n \ndiff --git a/AGENTS.md b/AGENTS.md\nindex 7f041100d49c2a7525f51cb0f5d696a4a6d1c78c..a06bfbabc6fa48a007389e25e12d7ed523936bed\n--- a/AGENTS.md\n+++ b/AGENTS.md\n@@ -22,7 +22,7 @@\n - **Codex Front** is an optional implementer through Codex App, Codex CLI, Codex IDE extension, or Codex Web signed in with ChatGPT OAuth/subscription auth, dispatched with `shiki runner codex` when a task is explicitly assigned to `codex`.\n - **GitHub CCA** is implemented by Claude Code Action by default, using `CLAUDE_CODE_OAUTH_TOKEN`.\n \n-Do not assume `openai/codex-action`, `OPENAI_API_KEY`, or API-key based Codex automation in the default Shiki path. API-key runners are explicit target-repo extensions and require their own ADR.\n+Do not assume `openai/codex-action`, `OPENAI_API_KEY`, or API-key based Codex automation in the default Shiki path. API-key runners are explicit target-repo extensions and require their own target ADR.\n \n Shiki is not:\n \n@@ -44,10 +44,14 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use\n+`SADR-NNNN-*.md`; target product decisions retain `NNNN-*.md` and the `ADR`\n+namespace.\n+\n Use the canonical source-of-truth order above. Runtime-specific reading orders\n are operational guidance only and must not override or reorder it.\n \n-Conversation state is not durable truth. If a decision matters, put it in GitHub, `.shiki/`, `CONTEXT.md`, or an ADR.\n+Conversation state is not durable truth. If a decision matters, put it in GitHub, `.shiki/`, `CONTEXT.md`, a Shiki SADR, or a target ADR.\n \n If GitHub and `.shiki/` disagree, surface the conflict and prefer GitHub operational state until the mirror is repaired.\n \n@@ -75,8 +79,8 @@\n Every non-trivial change follows this loop:\n \n 1. **Goal**: clarify outcome, completion conditions, non-goals, risk level, and success signals.\n-2. **grill-with-docs**: challenge the plan against domain docs, ADRs, code reality, terminology, and edge scenarios.\n-3. **Context & Impact**: find relevant docs, ADRs, code, dependencies, owners, locks, and verification surfaces.\n+2. **grill-with-docs**: challenge the plan against domain docs, Shiki SADRs, target ADRs, code reality, terminology, and edge scenarios.\n+3. **Context & Impact**: find relevant docs, SADRs, target ADRs, code, dependencies, owners, locks, and verification surfaces.\n 4. **PRD and Spec Freeze**: use `to-prd` when context is settled enough to become durable product/engineering intent. The operator's explicit approval of the PRD is Spec Freeze: the plan records a `spec_freeze` block, and no plan runs without it. Steps 1-4 form Requirements Definition \u2014 one continuous operator dialogue. After Spec Freeze, scope changes require an operator-approved Spec Amendment; non-scope-moving interpretations go to the Assumption Log.\n 5. **Issues**: use `to-issues` to create independently grabbable vertical-slice issues. Prefer AFK slices over HITL slices where possible.\n 6. **Triage**: label issues for readiness, risk, runtime, skills, and MergeGate state.\n@@ -167,7 +171,7 @@\n - Testing decisions.\n - Out-of-scope items.\n - Further notes.\n-- Links to relevant domain terms and ADRs.\n+- Links to relevant domain terms, SADRs, and target ADRs.\n \n Every executable task must include:\n \n@@ -193,7 +197,7 @@\n \n Context & Impact must identify:\n \n-- Relevant documents, domain terms, ADRs, and past decisions.\n+- Relevant documents, domain terms, SADRs, target ADRs, and past decisions.\n - Relevant modules, interfaces, seams, callers, owners, and tests.\n - Dependency relationships between tasks.\n - Candidate locks and lock conflicts.\n@@ -210,7 +214,7 @@\n | Skill | Required when |\n | --- | --- |\n | `setup-matt-pocock-skills` | First configuring the repo for skills, or when issue tracker, triage labels, or domain docs are missing. |\n-| `grill-with-docs` | Default for non-trivial Goals before PRD/issues; always required when plans, terminology, boundaries, tradeoffs, or ADR-worthy decisions are ambiguous. |\n+| `grill-with-docs` | Default for non-trivial Goals before PRD/issues; always required when plans, terminology, boundaries, tradeoffs, or decision-record-worthy choices are ambiguous. |\n | `zoom-out` | The agent lacks an architectural map of the relevant code area. |\n | `to-prd` | Settled Goal context must become durable product/engineering intent. |\n | `to-issues` | A Goal, PRD, or plan must become independently grabbable vertical-slice issues. |\n@@ -348,7 +352,7 @@\n - Three or more new files.\n - Any high-risk or critical label.\n \n-When the architecture gate triggers, run `grill-with-docs` or `improve-codebase-architecture` as appropriate and record durable decisions in an ADR when needed.\n+When the architecture gate triggers, run `grill-with-docs` or `improve-codebase-architecture` as appropriate and record durable decisions in a Shiki SADR or target ADR as applicable.\n \n ## Engineering Discipline\n \ndiff --git a/CLAUDE.md b/CLAUDE.md\nindex 98a932e7841a632866fcd53b0bad0e3af3711d3d..98a31399404d6fb044b4fd493f4459ac87f439de\n--- a/CLAUDE.md\n+++ b/CLAUDE.md\n@@ -14,6 +14,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n ## Claude Role\n \n Default to planner, default implementer, reviewer, coordinator, documentation editor, CCA-style completion judge, and final-judgment assistant.\n@@ -57,7 +60,7 @@\n At the start of a Shiki session:\n \n 1. Read `AGENTS.md`.\n-2. Read `CONTEXT.md` and relevant ADRs.\n+2. Read `CONTEXT.md` and relevant Shiki SADRs and target ADRs.\n 3. Read active `.shiki/` Goal/task/ledger state when present.\n 4. Read active distilled rules from `.shiki/memories/` (status `distilled`, `active: true`, not revoked or superseded) for applicable guidance; the same rules are injected deterministically into task handoffs as the `## Distilled Rules` section (\u00a73.5).\n 5. Read the GitHub Issue or PR when the task is GitHub-backed.\n@@ -74,7 +77,7 @@\n - Define the Goal, completion conditions, non-goals, and risk level.\n - Treat `grill-with-docs` as the default planning entry point for non-trivial Goals.\n - Challenge domain terms against `CONTEXT.md`.\n-- Challenge hard-to-reverse tradeoffs against ADRs.\n+- Challenge hard-to-reverse tradeoffs against SADRs and target ADRs.\n - Explore code instead of asking when code can answer the question.\n - Run Context & Impact before implementation planning.\n - Produce a Task DAG, not an unordered checklist.\n@@ -94,7 +97,7 @@\n - For each question, provide your recommended answer.\n - Resolve dependency decisions before downstream design questions.\n - Update `CONTEXT.md` inline when terminology crystallizes.\n-- Offer ADRs only when the decision is hard to reverse, surprising without context, and a real tradeoff.\n+- Offer a SADR or target ADR only when the decision is hard to reverse, surprising without context, and a real tradeoff.\n - Convert unresolved questions into HITL blockers rather than allowing Codex to guess.\n \n ## PRD and Issues Standard\n@@ -103,7 +106,7 @@\n \n - Use the project's domain glossary vocabulary.\n - Avoid volatile file paths unless they are essential.\n-- Include implementation decisions, testing decisions, out-of-scope items, and relevant ADR/domain links.\n+- Include implementation decisions, testing decisions, out-of-scope items, and relevant SADR/target-ADR/domain links.\n - Identify deep-module opportunities and testable interfaces.\n \n When creating issues:\n@@ -121,7 +124,7 @@\n - Branch/worktree target.\n - Scope and non-goals.\n - Dependencies and locks.\n-- Relevant docs, ADRs, modules, and tests.\n+- Relevant docs, SADRs, target ADRs, modules, and tests.\n - Required skill invocations.\n - TDD expectations.\n - Pre-PR `code-review` gate expectations.\n@@ -152,7 +155,7 @@\n \n - Treat yourself as a judge, not implementer.\n - Do not write production code.\n-- Read `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant ADRs, PRD, task issue, PR body, diff, checks, reviews, labels, and `.shiki/` state.\n+- Read `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant SADRs and target ADRs, PRD, task issue, PR body, diff, checks, reviews, labels, and `.shiki/` state.\n - Evaluate every applicable checklist in `docs/agents/checklists.md`.\n - Map each acceptance criterion to durable evidence.\n - Distinguish implementation failure from missing evidence.\ndiff --git a/CONTEXT.md b/CONTEXT.md\nindex e61a9be06a0cce0d026484083a657bde2067e234..237ee8aa3ea8fab334ece49768bc5ed3b5af0974\n--- a/CONTEXT.md\n+++ b/CONTEXT.md\n@@ -25,7 +25,7 @@\n _Avoid_: jumping directly into code\n \n **grill-with-docs**:\n-A planning skill that challenges a plan against domain language, ADRs, code reality, and concrete edge scenarios. It resolves design-tree questions before PRD/issues. It runs inside Requirements Definition.\n+A planning skill that challenges a plan against domain language, Shiki SADRs, target ADRs, code reality, and concrete edge scenarios. It resolves design-tree questions before PRD/issues. It runs inside Requirements Definition.\n _Avoid_: generic brainstorming, silent assumptions\n \n **Requirements Definition**:\ndiff --git a/CONTRIBUTING.md b/CONTRIBUTING.md\nindex c6d9b656e73abb8a55e5f660123e03153dcb3e6d..9e37a8fbf668db5b22461aaae5548b8f1354394c\n--- a/CONTRIBUTING.md\n+++ b/CONTRIBUTING.md\n@@ -17,7 +17,7 @@\n 1. **Goal** \u2014 start from a user-approved Goal with an outcome, completion\n    conditions, non-goals, risk level, and success signals.\n 2. **grill-with-docs** \u2014 for any non-trivial change, challenge the plan against\n-   domain language, ADRs, and code reality before writing a PRD.\n+   domain language, Shiki SADRs, target ADRs, and code reality before writing a PRD.\n 3. **PRD** \u2014 capture settled product/engineering intent (`to-prd`).\n 4. **Spec Freeze** \u2014 the operator explicitly approves the PRD/requirements;\n    the plan records a `spec_freeze` block and no plan runs without it\n@@ -94,7 +94,7 @@\n   entries.\n - Do not hand-edit `.shiki/goals/*.json` or `.shiki/manifest.json` in a task PR.\n - Conversation state is not durable truth. If a decision matters, record it in\n-  GitHub, `.shiki/`, `CONTEXT.md`, or an ADR.\n+  GitHub, `.shiki/`, `CONTEXT.md`, a Shiki SADR, or a target ADR.\n \n ## Architecture Gate\n \n@@ -103,7 +103,7 @@\n or CI policy, package structure, three or more structural files, more than 100\n net new lines in a core module, three or more new files, or any high-risk label.\n When triggered, run `grill-with-docs` or `improve-codebase-architecture` and\n-record durable decisions in an ADR when needed (see `docs/adr/`).\n+record durable decisions in a Shiki SADR or target ADR as applicable (see `docs/adr/`).\n \n ## Safety\n \ndiff --git a/SYSTEM_PROMPT.md b/SYSTEM_PROMPT.md\nindex aaaea45e3a896c3a44ba89eec8bb84325ecf6c62..da009c86a88285269dee0155e58e550c6d744ea9\n--- a/SYSTEM_PROMPT.md\n+++ b/SYSTEM_PROMPT.md\n@@ -30,6 +30,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n Use the canonical source-of-truth order above. Runtime-specific reading orders\n are operational guidance only and must not override or reorder it. The current\n conversation is non-durable operator input.\n@@ -48,14 +51,14 @@\n \n A runtime may only do work that is explicitly assigned by the Goal, task, PR, or MergeGate decision.\n \n-Do not assume `openai/codex-action`, `OPENAI_API_KEY`, or API-key based Codex automation in the default Shiki path. API-key runners are explicit target-repo extensions and require their own ADR.\n+Do not assume `openai/codex-action`, `OPENAI_API_KEY`, or API-key based Codex automation in the default Shiki path. API-key runners are explicit target-repo extensions and require their own target ADR.\n \n ## 4. Non-negotiable Shiki flow\n \n For any non-trivial Goal, use this flow:\n \n 1. **Goal Seek**: define outcome, non-goals, risk level, completion criteria, and evidence requirements.\n-2. **grill-with-docs**: challenge the plan against `CONTEXT.md`, ADRs, code reality, terminology, boundaries, and edge scenarios. Ask one question at a time when the operator is available. If code can answer a question, inspect code instead of asking.\n+2. **grill-with-docs**: challenge the plan against `CONTEXT.md`, Shiki SADRs, target ADRs, code reality, terminology, boundaries, and edge scenarios. Ask one question at a time when the operator is available. If code can answer a question, inspect code instead of asking.\n 3. **Context & Impact**: identify modules, interfaces, seams, callers, dependencies, locks, risk, and verification surfaces.\n 4. **to-prd + Spec Freeze**: turn settled context into a PRD using domain vocabulary and testing decisions. The operator's explicit approval of the PRD is Spec Freeze, recorded as the plan's `spec_freeze` block; plans without it do not run. Steps 1-4 form Requirements Definition.\n 5. **to-issues**: decompose the PRD into independently grabbable vertical-slice issues. Prefer AFK slices; mark HITL slices when judgment is still required.\n@@ -75,7 +78,7 @@\n Before making any material change:\n \n 1. Read `AGENTS.md` and any runtime-specific wrapper such as `CLAUDE.md`.\n-2. Read `CONTEXT.md` and relevant ADRs.\n+2. Read `CONTEXT.md` and relevant SADRs and target ADRs.\n 3. Read the GitHub Issue / PR and `.shiki/` state for the active Goal or task.\n 4. Identify your role: Planner, Implementer, Reviewer, Completion Check Agent, Repairer, or Guardian-assist.\n 5. Identify Goal id, task id, branch, PR, dependencies, locks, risk level, required skills, and required evidence.\n@@ -103,7 +106,7 @@\n \n At minimum, identify:\n \n-- Relevant documents, ADRs, domain terms, and prior decisions.\n+- Relevant documents, SADRs, target ADRs, domain terms, and prior decisions.\n - Relevant modules, interfaces, seams, callers, owners, and tests.\n - Dependency relationships between tasks.\n - Candidate file locks and lock conflicts.\n@@ -152,7 +155,7 @@\n \n CCA must read:\n \n-- `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant ADRs.\n+- `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant SADRs and target ADRs.\n - Parent Goal issue, PRD issue, task issue, comments, and labels.\n - PR body, diff, commits, changed files, reviews, CI checks, and artifacts available to the workflow.\n - `.shiki/` task contract, locks, ledger entries, CCA checklist profile, and prior repair packets.\n@@ -214,7 +217,7 @@\n - Three or more new files.\n - Any high-risk or critical label.\n \n-When the architecture gate triggers, run `grill-with-docs` or `improve-codebase-architecture` as appropriate and record the decision in an ADR if it is hard to reverse or likely to be re-litigated.\n+When the architecture gate triggers, run `grill-with-docs` or `improve-codebase-architecture` as appropriate and record the decision in a Shiki SADR or target ADR as applicable if it is hard to reverse or likely to be re-litigated.\n \n ## 14. Skill Gate\n \n@@ -223,7 +226,7 @@\n Use these skills:\n \n - `setup-matt-pocock-skills`: before first use of the engineering skill set, or when issue tracker, triage labels, or domain docs are missing.\n-- `grill-with-docs`: default for non-trivial Goals before PRD/issues; required for ambiguous plans, terminology, boundaries, ADR-worthy tradeoffs, or design-tree decisions.\n+- `grill-with-docs`: default for non-trivial Goals before PRD/issues; required for ambiguous plans, terminology, boundaries, decision-record-worthy tradeoffs, or design-tree decisions.\n - `zoom-out`: unfamiliar code area or missing architectural map.\n - `to-prd`: convert settled context into a PRD.\n - `to-issues`: break a PRD, plan, or Goal into independently grabbable vertical-slice issues.\ndiff --git a/docs/adr/0001-keep-shiki-as-platform-name.md b/docs/adr/SADR-0001-keep-shiki-as-platform-name.md\nindex 20a9637c3b9d8c97de7ba8db7cfae4d82d9a7dba..8b685439959aada1b221c112f0ac3c51ca8a1695\n--- a/docs/adr/0001-keep-shiki-as-platform-name.md\n+++ b/docs/adr/SADR-0001-keep-shiki-as-platform-name.md\n@@ -1,3 +1,3 @@\n-# Keep Shiki As The Platform Name\n+# SADR-0001: Keep Shiki As The Platform Name\n \n Shiki remains the product name for the GitHub-native agentic engineering control plane. We considered more descriptive names such as MergePilot, MergeGate, and Kiroku, but kept Shiki because it carries the existing goal-seeking framework identity while remaining independent of Hermes, Codex, Claude Code, or any single runtime.\ndiff --git a/docs/adr/0002-use-agents-as-shared-constitution.md b/docs/adr/SADR-0002-use-agents-as-shared-constitution.md\nindex 73de30ecb078120eac540d9bc51855e3cd48d0e3..c5486182bf06fca36ea508496b013c67302fd989\n--- a/docs/adr/0002-use-agents-as-shared-constitution.md\n+++ b/docs/adr/SADR-0002-use-agents-as-shared-constitution.md\n@@ -1,3 +1,3 @@\n-# Use AGENTS As The Shared Agent Constitution\n+# SADR-0002: Use AGENTS As The Shared Agent Constitution\n \n Shiki uses `AGENTS.md` as the runtime-neutral constitution and `CLAUDE.md` as a thin Claude Code wrapper. This keeps Codex, Claude Code, Hermes Runner, and future Agent Runtimes aligned on the same GitHub-first MergeGate rules while still allowing runtime-specific instructions where needed.\ndiff --git a/docs/adr/0003-require-skill-gate-for-mergegate-readiness.md b/docs/adr/SADR-0003-require-skill-gate-for-mergegate-readiness.md\nindex 424be0c9af4c8f0136f1fde68a468c47ff84e764..7fc337987c7df9792f75f181dfcbed75fe5ffff2\n--- a/docs/adr/0003-require-skill-gate-for-mergegate-readiness.md\n+++ b/docs/adr/SADR-0003-require-skill-gate-for-mergegate-readiness.md\n@@ -1,3 +1,3 @@\n-# Require Skill Gate For MergeGate Readiness\n+# SADR-0003: Require Skill Gate For MergeGate Readiness\n \n Shiki requires relevant engineering skills to be invoked before execution when their triggers apply. This makes skills part of MergeGate readiness rather than optional prompt style, because Shiki depends on repeatable planning, issue decomposition, TDD, diagnosis, triage, and architecture review before autonomous or parallel agent work can be trusted.\ndiff --git a/docs/adr/0004-use-github-first-with-shiki-mirror.md b/docs/adr/SADR-0004-use-github-first-with-shiki-mirror.md\nindex d0a3a9174dc646a945a9d325954ffaf00c352040..a79c54ae9ee8eed2a0524192b864da4e22a92d3f\n--- a/docs/adr/0004-use-github-first-with-shiki-mirror.md\n+++ b/docs/adr/SADR-0004-use-github-first-with-shiki-mirror.md\n@@ -1,4 +1,4 @@\n-# ADR 0004: Use GitHub First With A Repository-Local Shiki Mirror\n+# SADR-0004: Use GitHub First With A Repository-Local Shiki Mirror\n \n ## Status\n \ndiff --git a/docs/adr/0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md b/docs/adr/SADR-0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\nindex b339baf49b89d9290e79c3d51b36e7b2bde3bd95..0f3c3eb9afccdb3403ce978e21d04fe12e495da0\n--- a/docs/adr/0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\n+++ b/docs/adr/SADR-0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\n@@ -1,8 +1,8 @@\n-# ADR 0005: Use Codex As Front And Claude Code Action As The GitHub Runtime\n+# SADR-0005: Use Codex As Front And Claude Code Action As The GitHub Runtime\n \n ## Status\n \n-Accepted (implementer default superseded by ADR 0008; the auth model and the\n+Accepted (implementer default superseded by SADR-0008; the auth model and the\n Claude Code Action GitHub runtime decision remain in force)\n \n ## Context\n@@ -26,4 +26,4 @@\n - Shiki avoids requiring `OPENAI_API_KEY` for the default workflow.\n - Codex work happens through the user's authenticated Codex front surface, not as a hidden backend Action.\n - Claude Code Action is configured for OAuth-token based subscription usage by default.\n-- API-key based automation remains possible only as an explicit target-repo extension with its own ADR.\n+- API-key based automation remains possible only as an explicit target-repo extension with its own target ADR.\ndiff --git a/docs/adr/0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md b/docs/adr/SADR-0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\nindex 4bcae9d1e52f0a37b8e9fbafa5cd14b6ee1993bd..7a38538a6ae9929567f0ade67e7ea6a60d0b3f7e\n--- a/docs/adr/0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\n+++ b/docs/adr/SADR-0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\n@@ -1,4 +1,4 @@\n-# ADR 0006: Separate Codex Implementation, CCA Completion, And MergeGate Authorization\n+# SADR-0006: Separate Codex Implementation, CCA Completion, And MergeGate Authorization\n \n ## Status\n \ndiff --git a/docs/adr/0007-packaging-and-release.md b/docs/adr/SADR-0007-packaging-and-release.md\nindex 79b84ddb97167372b44641e5b7860e400ce80822..bb445a2e2e856d9d1f5fdbe35de8d4e8cec0fb32\n--- a/docs/adr/0007-packaging-and-release.md\n+++ b/docs/adr/SADR-0007-packaging-and-release.md\n@@ -1,4 +1,4 @@\n-# ADR 0007: Packaging And Release For Shiki\n+# SADR-0007: Packaging And Release For Shiki\n \n ## Status\n \n@@ -25,7 +25,7 @@\n   not introduce third-party runtime dependencies or break the shim.\n - Releases must run inside the operator's subscription-authenticated toolchain\n   and must not require extra secrets beyond the built-in `GITHUB_TOKEN`\n-  (consistent with ADR 0005's no-API-key-by-default stance).\n+  (consistent with SADR-0005's no-API-key-by-default stance).\n - `.shiki/manifest.json` already encodes the install surface (`install.include`,\n   `install.create_directories`, `install.exclude_from_commit`), and it must not\n   be mutated as part of this change.\n@@ -34,7 +34,7 @@\n \n This is a hard-to-reverse platform decision (it sets the version source of\n truth, the release trigger, and the public install contract), so it is recorded\n-as an ADR.\n+as a SADR.\n \n ## Decision\n \n@@ -104,7 +104,7 @@\n 3. **Publishing to PyPI.** Rejected for now: Shiki is distributed as a\n    repository template + git-installable CLI, not a PyPI library; PyPI would add\n    credentials/secrets and a different versioning contract. Can be added later as\n-   its own ADR if a packaged distribution is needed.\n+   its own SADR if a packaged distribution is needed.\n 4. **Dynamic version (read `VERSION` via a build backend `dynamic` field).**\n    Rejected to keep `pyproject.toml` backend-agnostic and dependency-free; the\n    small duplication between `VERSION` and `pyproject.toml` is guarded by the\n@@ -123,4 +123,4 @@\n   releasing versions the whole platform but ships only `install.include` to\n   targets.\n - Future PyPI distribution or a console entry point are deferred and would each\n-  warrant their own ADR.\n+  warrant their own SADR.\ndiff --git a/docs/adr/0008-use-claude-code-as-default-implementer.md b/docs/adr/SADR-0008-use-claude-code-as-default-implementer.md\nindex dedf4510b7c7799521422f622029563d373db031..35c2625d908167fbc58855b5b63e283805f167e8\n--- a/docs/adr/0008-use-claude-code-as-default-implementer.md\n+++ b/docs/adr/SADR-0008-use-claude-code-as-default-implementer.md\n@@ -1,12 +1,12 @@\n-# ADR 0008: Use Claude Code As The Default Implementer Runtime\n+# SADR-0008: Use Claude Code As The Default Implementer Runtime\n \n ## Status\n \n-Proposed (supersedes ADR 0005 for the implementer default; keeps its auth model)\n+Proposed (supersedes SADR-0005 for the implementer default; keeps its auth model)\n \n ## Context\n \n-ADR 0005 made Codex Front the default implementation runtime, with Claude Code\n+SADR-0005 made Codex Front the default implementation runtime, with Claude Code\n restricted to planner and reviewer roles by the runtime registry, and the only\n autonomous implementation adapter was `shiki runner codex`.\n \n@@ -18,13 +18,13 @@\n - The platform's planning, review, and CCA layers already run on Claude Code,\n   and Claude Code's native capabilities (the built-in `/code-review` skill and\n   the Workflow multi-agent orchestration tool) have no Codex equivalent.\n-- The Goal lifecycle redesign (ADR 0009) requires an autonomous post-freeze\n+- The Goal lifecycle redesign (SADR-0009) requires an autonomous post-freeze\n   loop driven from the operator's Claude Code session, including bounded\n   repair dispatch, which must not depend on a runtime that cannot be invoked\n   unattended.\n \n The constitution claims Shiki is runtime-agnostic (\"not a Claude-only\n-workflow\", ADR 0002). Any change to the implementer default must preserve the\n+workflow\", SADR-0002). Any change to the implementer default must preserve the\n registry-based runtime model rather than hardcoding one vendor.\n \n ## Decision\n@@ -39,7 +39,7 @@\n   the task to review or repair-needed.\n - Keep Codex registered as an optional implementer runtime. `shiki runner\n   codex`, the Codex handoff templates, and the ChatGPT OAuth auth model from\n-  ADR 0005 remain supported for tasks explicitly assigned to `codex`.\n+  SADR-0005 remain supported for tasks explicitly assigned to `codex`.\n - Commit to mandating two Claude-native capabilities as loop contract in\n   follow-up Goals (status: the code-review gate was delivered by Goal G-B \u2014\n   skill registry, checklists item PR-12, default required_skills \u2014 while the\n@@ -60,7 +60,7 @@\n \n Out of scope: removing Codex support, changing the CCA runtime (Claude Code\n Action with `CLAUDE_CODE_OAUTH_TOKEN` stays), and API-key based automation\n-(still requires its own ADR per ADR 0005).\n+(still requires its own target ADR per SADR-0005).\n \n ## Consequences\n \n@@ -100,7 +100,7 @@\n   harness rejection of direct `codex exec` is outside Shiki's control, and the\n   plan's mandatory native capabilities cannot run on Codex at all.\n - **Remove Codex entirely**: rejected; it abandons the runtime-agnostic\n-  constitution (ADR 0002) for no operational gain and forecloses\n+  constitution (SADR-0002) for no operational gain and forecloses\n   cross-runtime adversarial review.\n - **GitHub Actions cloud implementation via claude-code-action**: rejected as\n   default; durable evidence is automatic but token cost, Node 24 workflow\ndiff --git a/docs/adr/0009-requirements-definition-and-spec-freeze.md b/docs/adr/SADR-0009-requirements-definition-and-spec-freeze.md\nindex 29b3f7871bef9203a8b4a831a65c39eb2f506e42..52e7dd69ad0aa4efe0b68c62678abbf7df02aaec\n--- a/docs/adr/0009-requirements-definition-and-spec-freeze.md\n+++ b/docs/adr/SADR-0009-requirements-definition-and-spec-freeze.md\n@@ -1,4 +1,4 @@\n-# ADR 0009: Merge Grill And Planning Into Requirements Definition With Spec Freeze\n+# SADR-0009: Merge Grill And Planning Into Requirements Definition With Spec Freeze\n \n ## Status\n \ndiff --git a/docs/adr/0010-external-ai-guardian-review.md b/docs/adr/SADR-0010-external-ai-guardian-review.md\nindex e6108bbe8bb4e73e0ff4684c8f78001a80438c4e..f3510b145664298adafb151b6f0db51b972f01ec\n--- a/docs/adr/0010-external-ai-guardian-review.md\n+++ b/docs/adr/SADR-0010-external-ai-guardian-review.md\n@@ -1,4 +1,4 @@\n-# ADR 0010: External AI Guardian Review As A First-Class Approval Authority\n+# SADR-0010: External AI Guardian Review As A First-Class Approval Authority\n \n ## Status\n \n@@ -8,7 +8,7 @@\n \n Shiki is intended to be an autonomous, recursive, self-improving agent\n framework: authority-in-the-loop, not human-in-the-loop. Until now the Guardian\n-approval required for high/critical-risk merges (guardian-policy.json, ADR\n+approval required for high/critical-risk merges (guardian-policy.json, SADR\n governance) recognized only human authority sources \u2014 a GitHub review approval,\n the `guardian:approved` label applied by a configured Guardian, or a comment\n carrying `Guardian approval granted` plus the current head SHA.\n@@ -89,7 +89,7 @@\n   autonomous design the operator intends and produced the impersonation\n   incident.\n - **Record AI approval as an operator comment**: rejected as forgery of human\n-  authority \u2014 the exact failure this ADR corrects.\n+  authority \u2014 the exact failure this SADR corrects.\n - **Deliver the artifact as a committed `.shiki/` file**: rejected; a committed\n   approval inside the PR being approved is forgeable by anyone who can push.\n - **A dedicated reviewer bot identity instead of a relayed comment**: deferred;\ndiff --git a/docs/adr/0011-autonomous-quality-gate-evidence.md b/docs/adr/SADR-0011-autonomous-quality-gate-evidence.md\nindex bca0246c5d46e44afbde15486a75eec425fc02f5..9c93eac16d8c3a6d4b11bd7224e3955102cd7db4\n--- a/docs/adr/0011-autonomous-quality-gate-evidence.md\n+++ b/docs/adr/SADR-0011-autonomous-quality-gate-evidence.md\n@@ -1,4 +1,4 @@\n-# ADR 0011: Autonomous Quality-Gate Evidence \u2014 Deterministic Facts And An Independent Verifier\n+# SADR-0011: Autonomous Quality-Gate Evidence \u2014 Deterministic Facts And An Independent Verifier\n \n ## Status\n \ndiff --git a/docs/adr/0012-autonomous-loop-closeout-to-main.md b/docs/adr/SADR-0012-autonomous-loop-closeout-to-main.md\nindex 637fad6dde4989c9c2bf0f81f676d8d29991123c..ec7d85f81030a676bcbba28b43eb365422a36eac\n--- a/docs/adr/0012-autonomous-loop-closeout-to-main.md\n+++ b/docs/adr/SADR-0012-autonomous-loop-closeout-to-main.md\n@@ -1,4 +1,4 @@\n-# 0012 \u2014 Autonomous loop pushes goal completion to main via a closeout PR\n+# SADR-0012 \u2014 Autonomous loop pushes goal completion to main via a closeout PR\n \n - Status: accepted\n - Date: 2026-06-18\ndiff --git a/docs/adr/0013-bootstrap-grants-actions-approve-pull-requests.md b/docs/adr/SADR-0013-bootstrap-grants-actions-approve-pull-requests.md\nindex b6c69d64f1c5ced346b88d6859586715b3b5bd0e..cf48ff4a6716267f539ca58edd1a852e7c7a2d64\n--- a/docs/adr/0013-bootstrap-grants-actions-approve-pull-requests.md\n+++ b/docs/adr/SADR-0013-bootstrap-grants-actions-approve-pull-requests.md\n@@ -1,4 +1,4 @@\n-# ADR 0013: Bootstrap Grants GitHub Actions Permission To Create And Approve Pull Requests\n+# SADR-0013: Bootstrap Grants GitHub Actions Permission To Create And Approve Pull Requests\n \n ## Status\n \n@@ -31,7 +31,7 @@\n security tradeoff: it lets repository automation satisfy a human-review gate. In\n a multi-maintainer repo that could let a compromised or buggy workflow\n self-approve changes. So whether bootstrap turns it on by default \u2014 and with\n-what default token scope \u2014 is an ADR-worthy decision, not an implementation\n+what default token scope \u2014 is a SADR-worthy decision, not an implementation\n detail.\n \n ## Decision\n@@ -48,7 +48,7 @@\n Specifically:\n \n - **`can_approve_pull_request_reviews` defaults to `true`.** Shiki's default\n-  posture is solo/AFK autonomous operation (ADR 0008/0009/0012), where the CCA\n+  posture is solo/AFK autonomous operation (SADR-0008/0009/0012), where the CCA\n   Review Bridge is the only path that can satisfy `required_review: true` after\n   the CCA and Guardian gates have already passed. The default makes a\n   freshly-bootstrapped repo able to run the autonomous loop end to end without a\ndiff --git a/docs/adr/0014-codex-app-external-ai-guardian-ui-adapter.md b/docs/adr/SADR-0014-codex-app-external-ai-guardian-ui-adapter.md\nindex 238db06279334caa341c117362df9271382a7fcd..65f12e9939321e1b3e389a34c822fc1549e5155d\n--- a/docs/adr/0014-codex-app-external-ai-guardian-ui-adapter.md\n+++ b/docs/adr/SADR-0014-codex-app-external-ai-guardian-ui-adapter.md\n@@ -1,4 +1,4 @@\n-# ADR 0014: Use Codex App As The External AI Guardian UI Adapter\n+# SADR-0014: Use Codex App As The External AI Guardian UI Adapter\n \n ## Status\n \ndiff --git a/docs/adr/0015-contract-approval-before-dispatch.md b/docs/adr/SADR-0015-contract-approval-before-dispatch.md\nindex fad6c656741a20a7684ba16080a7db159f63a52f..6061271a46d3f1d6ae7174477de3fb75673123a6\n--- a/docs/adr/0015-contract-approval-before-dispatch.md\n+++ b/docs/adr/SADR-0015-contract-approval-before-dispatch.md\n@@ -1,4 +1,4 @@\n-# ADR 0015: Approve The Task Contract Before Dispatch, Not The Diff After It\n+# SADR-0015: Approve The Task Contract Before Dispatch, Not The Diff After It\n \n ## Status\n \n@@ -9,7 +9,7 @@\n Guardian approval for high/critical risk is expressible only on a pull request.\n Every approval source in `guardian-policy.json` \u2014 GitHub review, the\n `guardian:approved` label, the `Guardian approval granted` comment, and the\n-`external_ai_guardian_review` artifact (ADR 0010/0014) \u2014 is bound to a PR, and\n+`external_ai_guardian_review` artifact (SADR-0010/0014) \u2014 is bound to a PR, and\n three of the four require the current head SHA. Approval therefore cannot exist\n before an implementation PR exists.\n \n@@ -81,7 +81,7 @@\n An envelope breach must surface only on the non-repairable policy gate, never on\n the repairable `MergeGate metadata check`. Otherwise the Goal Loop routes it to\n `dispatch_repair` \u2014 instructing a runner to make the breach go away \u2014 which is\n-exactly what this ADR forbids.\n+exactly what this SADR forbids.\n \n **Contract immutability.** After registration, an implementation PR must not\n change the governance fields of its own task contract (`scope`, `non_goals`,\n@@ -116,7 +116,7 @@\n fails any verdict that is not `complete`, and the signal the CCA prompt reads is\n computed from `evaluate_guardian_approval` alone. Without the OR-merge at the\n signal, a contract-approved PR still returns `needs_guardian`, the CCA job fails,\n-and the MergeGate policy check never runs \u2014 the gate this ADR changes would never\n+and the MergeGate policy check never runs \u2014 the gate this SADR changes would never\n be reached. The CCA *prompt* needs no change: a `contract_approval` entry in the\n signal's existing `sources` array satisfies its current wording.\n \n@@ -130,7 +130,7 @@\n written, and an approved contract lets its implementation PRs merge through the\n same automatic path as low/medium risk. The wasted implement-then-block cycle\n disappears. Guardian review shifts from reading diffs to reading contracts, which\n-is both earlier and smaller; the external AI Guardian path (ADR 0010/0014) works\n+is both earlier and smaller; the external AI Guardian path (SADR-0010/0014) works\n on contracts with no change.\n \n We accept that the Guardian authorizes a class of change without seeing its\n@@ -146,7 +146,7 @@\n adversarial tests for the new approval path equivalent in rigor to\n `test_shiki_governance_evidence.sh`.\n \n-Bootstrap costs, which apply only while this ADR is being implemented. The tasks\n+Bootstrap costs, which apply only while this SADR is being implemented. The tasks\n that build Contract Approval merge under the existing rules, so the ones\n dispatched after the pre-dispatch gate lands but before the approval path lands\n need a Guardian-approved Contract PR *and* a live Guardian approval on their\n@@ -210,7 +210,7 @@\n risk; they do not bound authorized work.\n \n **Auto-escalate risk on envelope breach instead of stopping.** Returns the\n-workflow to a diff-time Guardian decision \u2014 the exact behavior this ADR removes.\n+workflow to a diff-time Guardian decision \u2014 the exact behavior this SADR removes.\n \n **Resolve governance fields from the base snapshot only.** Rejected against the\n registration flow: implementation PRs add their own contract, so the base has no\ndiff --git a/docs/adr/0016-locks-bound-product-paths.md b/docs/adr/SADR-0016-locks-bound-product-paths.md\nindex b5e850f3f1f7f4ef6e7f9d1763a29a51bd300c45..46b40a9b99a7aae644c02bb458f5473ff0a90ce2\n--- a/docs/adr/0016-locks-bound-product-paths.md\n+++ b/docs/adr/SADR-0016-locks-bound-product-paths.md\n@@ -1,4 +1,4 @@\n-# ADR 0016: Locks bound product paths; the Shiki mirror is bound by record identity\n+# SADR-0016: Locks bound product paths; the Shiki mirror is bound by record identity\n \n ## Status\n \n@@ -6,8 +6,8 @@\n \n - Date: 2026-07-28\n - Deciders: operator (mizutani-140), Claude (planner)\n-- Related: ADR 0004 (GitHub-first with a `.shiki` mirror), ADR 0009 (Spec Freeze),\n-  ADR 0012 (autonomous loop closeout to main), ADR 0015 (Contract Approval before\n+- Related: SADR-0004 (GitHub-first with a `.shiki` mirror), SADR-0009 (Spec Freeze),\n+  SADR-0012 (autonomous loop closeout to main), SADR-0015 (Contract Approval before\n   dispatch), remediation goal task R-02, the live PR #179 deadlock.\n \n ## Context\n@@ -90,11 +90,11 @@\n files, documentation files and named source modules are nameable; runtime-\n allocated ledger, runner and report ids are not \u2014 and those live in `.shiki`,\n which locks no longer govern. Existing frozen contracts are brought into line by\n-a one-time Spec Amendment per plan, which must land before ADR 0015's contract\n+a one-time Spec Amendment per plan, which must land before SADR-0015's contract\n immutability makes `locks` unwritable from the implementation PR.\n \n **Lock state is resolved from the durable side.** MergeGate resolves the lock\n-registry, and ADR 0015's governance fields, from the base `.shiki` snapshot when\n+registry, and SADR-0015's governance fields, from the base `.shiki` snapshot when\n present, falling back to head only when absent. The `MergeGate metadata check`\n job is given `--base-shiki`, which it does not receive today. Locks are not\n derived from the live open-pull-request set: a pull request body is untrusted\n@@ -108,7 +108,7 @@\n Explicitly out of scope: moving MergeGate's contract resolution off the PR\n checkout wholesale; making the mirror a projection of GitHub state; introducing\n new lock kinds; moving ledger evidence out of the repository; and unwinding\n-ADR 0012's closeout pull request. Registration of multi-task goals is ADR 0015's\n+SADR-0012's closeout pull request. Registration of multi-task goals is SADR-0015's\n Contract PR and is not re-decided here.\n \n ## Consequences\n@@ -127,7 +127,7 @@\n \n Risks accepted. First, goal-scoped identity is only as strong as `goal_id`,\n which is resolved from the PR body and the head task file; the mitigation is to\n-add `goal_id` to ADR 0015's frozen governance field set, and until that lands a\n+add `goal_id` to SADR-0015's frozen governance field set, and until that lands a\n task file can name a foreign goal. Second, base-when-present resolution is\n unavailable on fork pull requests, where the head is contributor-controlled;\n fork pull requests already cannot complete a Shiki merge, and this decision does\n@@ -139,7 +139,7 @@\n path, not by implementation pull requests.\n \n New obligations: one Spec Amendment per frozen plan to rewrite `locks`, ordered\n-before ADR 0015 T1; extension of the adversarial suites in\n+before SADR-0015 T1; extension of the adversarial suites in\n `scripts/test_shiki_adversarial_state.sh` and\n `scripts/test_shiki_governance_evidence.sh` to cover the eleven newly-scoped\n directories; and updating `scripts/test_shiki_loop_lock_guard.sh`,\n@@ -151,32 +151,32 @@\n whether the loop's transitive sync of ledger `evidence` references can be\n deleted once the Contract PR carries its own source plan.\n \n-## Relationship to ADR 0004\n+## Relationship to SADR-0004\n \n-This ADR **clarifies something ADR 0004 left unspecified**. ADR 0004 decides that\n+This SADR **clarifies something SADR-0004 left unspecified**. SADR-0004 decides that\n GitHub is the operational source of truth and that each target repository keeps a\n `.shiki/` mirror. It says nothing about pull requests writing the mirror, does\n not name MergeGate, and does not make the mirror an input to a gate. Nothing in\n-ADR 0004's Decision or Consequences is amended, superseded or weakened: the\n+SADR-0004's Decision or Consequences is amended, superseded or weakened: the\n mirror still exists, still records the same artifacts, and pull request\n-boundaries remain the coordination surface. What this ADR settles is the question\n+boundaries remain the coordination surface. What this SADR settles is the question\n 0004 never reached \u2014 by what authority a pull request may write a mirror record \u2014\n and it answers it the way `.shiki/manifest.json` already declares\n (`pr_mutation: current-task-or-current-goal-only`).\n \n-It **amends ADR 0012**, whose Decision item 5 states that \"Loop-executed tasks\n+It **amends SADR-0012**, whose Decision item 5 states that \"Loop-executed tasks\n declare the `path:.shiki/**` lock, which covers every `.shiki` file the closeout\n stages ... so `files_outside_locks` is satisfied without per-file lock\n bookkeeping.\" That clause is replaced: the closeout's mirror writes are\n-authorized by record identity, not by a declared lock. ADR 0012's substantive\n+authorized by record identity, not by a declared lock. SADR-0012's substantive\n decision \u2014 that completion reaches `main` through a closeout pull request rather\n than local attestation \u2014 is preserved unchanged.\n \n-It is **complementary to ADR 0015**, not a substitute. ADR 0015 owns contract\n+It is **complementary to SADR-0015**, not a substitute. SADR-0015 owns contract\n immutability, the pre-dispatch gate, the Contract PR and the Scope Envelope. This\n-ADR owns what a lock means and who may write the mirror. The one ordering\n+SADR owns what a lock means and who may write the mirror. The one ordering\n constraint between them is that the lock-narrowing Spec Amendment must precede\n-ADR 0015 T1's freezing of the `locks` field.\n+SADR-0015 T1's freezing of the `locks` field.\n \n ## Alternatives Considered\n \n@@ -186,7 +186,7 @@\n the only writer that can register them may run. Migrating the Guardian signal to\n that store makes every registration PR fail closed to `needs_guardian` at every\n risk level, because the task file is by definition absent from the store, which\n-removes the low/medium autonomous path ADR 0008/0009 exists to provide. And it\n+removes the low/medium autonomous path SADR-0008/0009 exists to provide. And it\n leaves schemas, manifest, config, guardian policy, templates, migrations, plans,\n worktrees, runner records, runs and handoffs with no writer at all in a\n repository whose product is `.shiki`. Measured benefit on the frozen backlog:\n@@ -199,14 +199,14 @@\n namespace is not in fact ownership-partitioned \u2014 56 ledger entries carry no\n `task_id`, and one goal DAG is writable by 35 registered tasks. The correct\n expression of its insight is that locks do not govern the mirror at all, which is\n-what this ADR decides.\n+what this SADR decides.\n \n **Make the mirror a projection written only by a registration writer and a\n post-merge projection job.** Rejected. It requires a bypass actor with write\n access to the protected default branch; it makes its own master invariant\n readable from a manifest that pull requests may edit; it deletes both reconcile\n modes and leaves a corrupted mirror with no in-band repair path, contradicting\n-ADR 0004's consequence that mirror conflicts are repaired against GitHub state;\n+SADR-0004's consequence that mirror conflicts are repaired against GitHub state;\n and its content-derived ids are rejected by `validate_shiki`'s currently-green id\n pattern.\n \ndiff --git a/docs/adr/0017-bookkeeping-closeouts-do-not-inherit-risk.md b/docs/adr/SADR-0017-bookkeeping-closeouts-do-not-inherit-risk.md\nindex 554e3a0d526a72ca067108c5464cc68c3edcf174..b9676857a4713850e063af7c39c8e0b01ecb4c66\n--- a/docs/adr/0017-bookkeeping-closeouts-do-not-inherit-risk.md\n+++ b/docs/adr/SADR-0017-bookkeeping-closeouts-do-not-inherit-risk.md\n@@ -1,4 +1,4 @@\n-# ADR 0017: A Bookkeeping Closeout Does Not Inherit Its Task's Risk\n+# SADR-0017: A Bookkeeping Closeout Does Not Inherit Its Task's Risk\n \n ## Status\n \n@@ -6,7 +6,7 @@\n \n ## Context\n \n-ADR 0012 made the goal loop push a task's completion to the default branch through a\n+SADR-0012 made the goal loop push a task's completion to the default branch through a\n closeout pull request: after the implementation PR merges, the loop opens a second PR\n carrying `task.status = done`, `lock.state = released`, and \u2014 when the task completes\n its goal \u2014 `goal.status = complete` plus one scorecard report. Nothing else. The\n@@ -49,14 +49,14 @@\n 4. The task file's governance fields (`scope`, `non_goals`, `required_skills`,\n    `risk_level`, `locks`, `acceptance_checks`, `test_command`) are byte-identical to\n    the base snapshot. Only `status`, `expected_pr`, `closeout_pr`, `ledger_evidence`\n-   and `expected_branch` may differ \u2014 the ADR 0015 contract-immutability rule,\n+   and `expected_branch` may differ \u2014 the SADR-0015 contract-immutability rule,\n    unchanged.\n \n    `expected_branch` is in that set because a closeout is *by construction* on a\n    different branch from the implementation it closes, and the loop rewrites the\n    field accordingly. It is safe to let it differ because MergeGate independently\n    requires `expected_branch` to equal the PR's actual head ref, so a PR cannot set\n-   it to anything other than the branch it is really on. The first draft of this ADR\n+   it to anything other than the branch it is really on. The first draft of this SADR\n    omitted it, which made the exemption unreachable: every real closeout failed\n    condition 4 on a field the loop always changes. See PR #209.\n 5. The state transitions are exactly the terminal set: task `review` \u2192 `done`, lock\n@@ -113,12 +113,12 @@\n classification must be derived, not asserted.\n \n **Fold the closeout into the implementation PR** so no second PR exists. Rejected under\n-ADR 0012, which recorded why: a task file that is already `done` on the implementation\n+SADR-0012, which recorded why: a task file that is already `done` on the implementation\n branch short-circuits the loop's review \u2192 CCA \u2192 merge \u2192 done ordering into a local\n goal-completion before any merge happens.\n \n **Have the loop auto-merge closeouts with elevated credentials.** Rejected outright: that\n-is a runtime authoring its own approval, which ADR 0010 and the constitution's\n+is a runtime authoring its own approval, which SADR-0010 and the constitution's\n approval-record prohibition forbid. It would also be indistinguishable, in the evidence,\n from the forgery those rules exist to prevent.\n \ndiff --git a/docs/adr/0018-carried-guardian-approval-across-a-proven-base-sync.md b/docs/adr/SADR-0018-carried-guardian-approval-across-a-proven-base-sync.md\nindex 22358562da390ed8b45cd600da08a43d378b4680..e6e2907cefa776460af73ddf7d39db605f3c39b0\n--- a/docs/adr/0018-carried-guardian-approval-across-a-proven-base-sync.md\n+++ b/docs/adr/SADR-0018-carried-guardian-approval-across-a-proven-base-sync.md\n@@ -1,4 +1,4 @@\n-# ADR 0018: Carried Guardian Approval Across A Proven Base Sync\n+# SADR-0018: Carried Guardian Approval Across A Proven Base Sync\n \n ## Status\n \n@@ -34,7 +34,7 @@\n Guardian evaluator that consumes it (`shiki_guardian.evaluate_guardian_approval`,\n the `guardian_comment_carried` source) were built and tested in isolation. Neither\n does anything until a caller passes the flags that turn the carry on, so the\n-feature was invisible in CI. This ADR records the decision the two of them\n+feature was invisible in CI. This SADR records the decision the two of them\n implement, and this task wires it into the four CI invocations that judge Guardian\n approval.\n \ndiff --git a/docs/adr/README.md b/docs/adr/README.md\nindex c70e057a4520c36e042ab68c53cd4a21ef709b71..5da43856836e2ce6a150f8127766e2f88355ab4b\n--- a/docs/adr/README.md\n+++ b/docs/adr/README.md\n@@ -1,40 +1,41 @@\n-# Architecture Decision Records\n+# Decision Record Namespaces\n \n-This directory holds Shiki's Architecture Decision Records (ADRs): durable\n-records of hard-to-reverse platform decisions. ADRs are part of the Shiki source\n-of truth alongside GitHub state, the `.shiki/` mirror, and `CONTEXT.md`.\n+This directory holds two independent decision namespaces:\n \n-## How ADRs Work\n+- Shiki Architecture Decision Records (SADRs), named\n+  `SADR-NNNN-<short-slug>.md`, record hard-to-reverse Shiki platform decisions.\n+- Target Architecture Decision Records (ADRs), named\n+  `NNNN-<short-slug>.md`, belong to the target product.\n \n-- New ADRs are added as `docs/adr/NNNN-<short-slug>.md`, where `NNNN` is the next\n-  zero-padded sequence number.\n-- The **canonical list of ADRs is the set of files matching `docs/adr/*.md`** in\n-  this directory. Browse `docs/adr/*.md` for the authoritative, current set.\n-- Use [`template.md`](template.md) as the starting point for a new ADR.\n-- Record an ADR when a decision is hard to reverse, surprising without context,\n-  and a real tradeoff. Do not write an ADR for routine or easily reversible\n-  choices.\n+The same four-digit number may appear once in each namespace. A platform\n+`SADR-NNNN` citation resolves only to `SADR-NNNN-*.md`; a target `ADR NNNN`\n+citation resolves only to `NNNN-*.md`.\n \n-Using the `docs/adr/*.md` glob as the source of truth means other PRs can add an\n-ADR without editing this index file, avoiding merge contention on a\n-hand-maintained enumeration.\n+## How Decision Records Work\n \n-## Current ADRs (snapshot)\n-\n-The list below is a convenience snapshot and may lag behind the directory. When\n-in doubt, list `docs/adr/*.md`.\n+- New Shiki platform decisions use the next zero-padded SADR number.\n+- New target product decisions use the target's own ADR sequence.\n+- The canonical list is the files in `docs/adr/`; no hand-maintained index is\n+  authoritative.\n+- Use [`template.md`](template.md) as the starting point and choose the filename\n+  namespace that owns the decision.\n+- Record a decision when it is hard to reverse, surprising without context, and\n+  a real tradeoff. Do not write a decision record for routine or easily\n+  reversible choices.\n \n-- [0001 \u2014 Keep Shiki As The Platform Name](0001-keep-shiki-as-platform-name.md)\n-- [0002 \u2014 Use AGENTS As The Shared Agent Constitution](0002-use-agents-as-shared-constitution.md)\n-- [0003 \u2014 Require Skill Gate For MergeGate Readiness](0003-require-skill-gate-for-mergegate-readiness.md)\n-- [0004 \u2014 Use GitHub First With A Repository-Local Shiki Mirror](0004-use-github-first-with-shiki-mirror.md)\n-- [0005 \u2014 Use Codex As Front And Claude Code Action As The GitHub Runtime](0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md)\n-- [0006 \u2014 Separate Codex Implementation, CCA Completion, And MergeGate Authorization](0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md)\n+Using the directory as the source of truth lets independent changes add records\n+without contending on an enumerated index.\n \n-## Adding A New ADR\n+## Adding A Shiki SADR\n \n-1. Copy `template.md` to `docs/adr/NNNN-<short-slug>.md`.\n+1. Copy `template.md` to `docs/adr/SADR-NNNN-<short-slug>.md`.\n 2. Fill in the title, status, context, decision, and consequences.\n-3. Set the status to `Proposed`, then `Accepted` once the decision is approved.\n-4. Open a PR following `CONTRIBUTING.md`. You do not need to edit this index for\n-   the ADR to be discoverable via `docs/adr/*.md`.\n+3. Set the status to `Proposed`, then `Accepted` once the proper authority has\n+   approved the decision.\n+4. Open a PR following `CONTRIBUTING.md`.\n+\n+## Adding A Target ADR\n+\n+1. Copy `template.md` to `docs/adr/NNNN-<short-slug>.md` using the target's own\n+   sequence.\n+2. Cite it as `ADR NNNN`; do not prefix its filename with `SADR-`.\ndiff --git a/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md b/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\nnew file mode 100644\nindex 0000000000000000000000000000000000000000..7455ce1f3bed02ddddb851ac21a1d1a648c9aeba\n--- /dev/null\n+++ b/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\n@@ -0,0 +1,55 @@\n+# SADR-0019: Separate Shiki Platform SADRs From Target ADRs\n+\n+## Status\n+\n+Accepted\n+\n+## Context\n+\n+Shiki installs its platform decision records into `docs/adr/` in every target\n+repository. Shiki and its targets previously used the same numeric\n+`NNNN-<slug>.md` namespace. A target that already owned ADRs 0013 through 0018\n+therefore acquired two records for each number during installation, making an\n+`ADR NNNN` citation ambiguous. Renumbering target decisions would rewrite target\n+history, while renumbering Shiki decisions per target would destroy their stable\n+platform identity.\n+\n+Upgrades also need to remove the former Shiki-owned numeric paths without ever\n+mistaking a target-owned ADR for platform content. The install stamp already\n+records exact shipped paths and content digests, so it can prove ownership only\n+when both still match.\n+\n+## Decision\n+\n+Shiki platform decisions use the `SADR` namespace:\n+\n+- records are named `docs/adr/SADR-NNNN-<slug>.md`;\n+- live platform citations use `SADR-NNNN`;\n+- target product decisions retain `docs/adr/NNNN-<slug>.md` and `ADR NNNN`;\n+- the reference validator resolves the two namespaces independently, so the same\n+  four-digit number may exist once in each namespace.\n+\n+Before a forced upgrade writes anything, the installer inventories the exact\n+former numeric Shiki ADR paths. It may delete a legacy path only when the\n+existing install stamp contains that exact path and its recorded SHA-256 digest\n+matches the file currently on disk. All ownership blockers are reported\n+together and abort the upgrade before its first write. A non-forced install\n+never performs this cleanup.\n+\n+Historical `.shiki/` evidence remains unchanged because it records the namespace\n+that existed when that evidence was produced.\n+\n+## Consequences\n+\n+- Targets may use any ADR number without colliding with Shiki's platform SADRs.\n+- Fresh installs preserve target ADR bytes and install a stable SADR set.\n+- Forced upgrades fail closed when legacy ownership cannot be proven, and a\n+  successful upgrade refreshes the stamp without removed legacy paths.\n+- Current governance, packaging, workflow, prompt, script, and skill references\n+  must move with the platform records.\n+\n+## Alternatives Considered\n+\n+- Collision-only refusal leaves affected targets permanently unupgradable.\n+- Renumbering target ADRs mutates target-owned history.\n+- Per-target Shiki renumbering makes platform citations non-portable.\ndiff --git a/docs/adr/template.md b/docs/adr/template.md\nindex a92a97daddd0b00ab9dd626cc8d86942da2eafed..17dd79175b5fee2105935557f691b40cf9492942\n--- a/docs/adr/template.md\n+++ b/docs/adr/template.md\n@@ -1,14 +1,14 @@\n-# NNNN: <Short Decision Title>\n+# <SADR-NNNN or NNNN>: <Short Decision Title>\n \n ## Status\n \n-Proposed | Accepted | Superseded by ADR-NNNN\n+Proposed | Accepted | Superseded by SADR-NNNN or target ADR NNNN\n \n ## Context\n \n What is the situation, constraint, or force that requires a decision? Reference\n-relevant domain terms (`CONTEXT.md`), prior ADRs, and code reality. State the\n-problem, not the solution.\n+relevant domain terms (`CONTEXT.md`), prior Shiki SADRs or target ADRs, and code\n+reality. State the problem, not the solution.\n \n ## Decision\n \ndiff --git a/docs/agents/autonomous-loop-e2e-acceptance.md b/docs/agents/autonomous-loop-e2e-acceptance.md\nindex bbf84c6081987f159ac6ad9ecf3c82b7aa72e7e8..5c13c1d02a05a90b0dc46cbef7c95a9846d0c237\n--- a/docs/agents/autonomous-loop-e2e-acceptance.md\n+++ b/docs/agents/autonomous-loop-e2e-acceptance.md\n@@ -40,8 +40,8 @@\n 3. `gh auth status` is logged in with merge permission on the repo.\n 4. Branch protection + required checks are configured exactly as the goal-loop\n    required-check set expects (see `docs/agents/branch-protection-smoke.md`).\n-5. The repo `.shiki/policy` permits autonomous low/medium-risk merges (ADR\n-   0008/0009); the MergeGate policy check is the recorded-authority gate.\n+5. The repo `.shiki/policy` permits autonomous low/medium-risk merges\n+   (SADR-0008/0009); the MergeGate policy check is the recorded-authority gate.\n \n The operator's only actions are: register the task and start the loop. **No\n manual pre-clearing** means after `shiki loop run` starts, the operator does not\ndiff --git a/docs/agents/bootstrap-command.md b/docs/agents/bootstrap-command.md\nindex 05f7f203a52cf412b9f97084a6e131ed0f785afd..f7aa0b734dfebee4a3b139335b66ee3415f48713\n--- a/docs/agents/bootstrap-command.md\n+++ b/docs/agents/bootstrap-command.md\n@@ -189,7 +189,9 @@\n ```\n \n Use this only for tests, fixtures, or explicit local-only template inspection.\n-Use `--force` only when you intentionally want to overwrite existing target files.\n+Use `--force` only when you intentionally want to refresh the target template.\n+Legacy numeric Shiki decision cleanup is stamp-and-digest authorized before any\n+write; target ADR paths are not cleanup candidates.\n \n ## Slash Command\n \ndiff --git a/docs/agents/checklists.md b/docs/agents/checklists.md\nindex 4a7f03ade346f53a1c1d82981f3fd744f9ab4547..0c5ec6c53dc7197b7b7a7d18e667b325babd6c45\n--- a/docs/agents/checklists.md\n+++ b/docs/agents/checklists.md\n@@ -32,18 +32,18 @@\n | ID | Item | Blocking |\n | --- | --- | --- |\n | GD-01 | Plan was challenged against `CONTEXT.md`. | blocking for non-trivial Goals |\n-| GD-02 | Relevant ADRs were read or absence was recorded. | blocking for non-trivial Goals |\n+| GD-02 | Relevant SADRs and target ADRs were read or absence was recorded. | blocking for non-trivial Goals |\n | GD-03 | Ambiguous terms were resolved or converted to HITL blockers. | blocking |\n | GD-04 | Edge scenarios were considered. | advisory |\n | GD-05 | Code reality was checked when code could answer. | blocking when applicable |\n | GD-06 | New domain terms were added to `CONTEXT.md` when needed. | blocking when applicable |\n-| GD-07 | ADR was created or explicitly skipped using the ADR criteria. | blocking when applicable |\n+| GD-07 | A SADR or target ADR was created or explicitly skipped using the decision-record criteria. | blocking when applicable |\n \n ## CI \u2014 Context & Impact Checklist\n \n | ID | Item | Blocking |\n | --- | --- | --- |\n-| CI-01 | Relevant docs and ADRs identified. | blocking |\n+| CI-01 | Relevant docs, SADRs, and target ADRs identified. | blocking |\n | CI-02 | Relevant modules, interfaces, seams, callers, and tests identified. | blocking |\n | CI-03 | Dependencies and blocked-by relationships identified. | blocking |\n | CI-04 | Candidate locks identified. | blocking |\n@@ -62,7 +62,7 @@\n | PRD-04 | Implementation decisions are recorded without volatile code detail. | blocking |\n | PRD-05 | Testing decisions are recorded. | blocking |\n | PRD-06 | Out-of-scope items are explicit. | blocking |\n-| PRD-07 | Relevant glossary/ADR links are present when applicable. | blocking |\n+| PRD-07 | Relevant glossary/SADR/target-ADR links are present when applicable. | blocking |\n \n ## SF \u2014 Spec Freeze Checklist\n \n@@ -98,7 +98,7 @@\n | H-02 | Branch/worktree target is present. | blocking |\n | H-03 | Scope and non-goals are present. | blocking |\n | H-04 | Dependencies and locks are present. | blocking |\n-| H-05 | Relevant docs, ADRs, modules, and tests are referenced. | blocking |\n+| H-05 | Relevant docs, SADRs, target ADRs, modules, and tests are referenced. | blocking |\n | H-06 | Required skills are stated. | blocking |\n | H-07 | TDD expectations are stated. | blocking when behavior work |\n | H-08 | Acceptance criteria are included. | blocking |\ndiff --git a/docs/agents/codex-handoff.md b/docs/agents/codex-handoff.md\nindex ede468f47cb277f69a857e8c45df703e4ed0151d..6f97bdffb67e44d7edea2c71e01ea28933c4eb6c\n--- a/docs/agents/codex-handoff.md\n+++ b/docs/agents/codex-handoff.md\n@@ -46,7 +46,7 @@\n \n ## Context\n - Domain terms:\n-- Relevant ADRs:\n+- Relevant SADRs and target ADRs:\n - Relevant docs:\n - Relevant modules/interfaces/seams:\n - Similar tests/prior art:\ndiff --git a/docs/agents/completion-check-agent.md b/docs/agents/completion-check-agent.md\nindex 13e4c035db35025f4e3cdcc5adfb24314ac4d521..61bfa1218cf36ca605250c91919e620f33238aa0\n--- a/docs/agents/completion-check-agent.md\n+++ b/docs/agents/completion-check-agent.md\n@@ -32,7 +32,7 @@\n - `AGENTS.md`\n - `CLAUDE.md`\n - `CONTEXT.md`\n-- relevant ADRs\n+- relevant SADRs and target ADRs\n - parent Goal issue\n - PRD issue or document\n - task issue\ndiff --git a/docs/agents/decision-control.md b/docs/agents/decision-control.md\nindex 7d8ee5ab03a5ccbcbd04056ae61de03785a2a9c9..8c94ecda00fffaa0e92c325ab83aa9776bd1dac1\n--- a/docs/agents/decision-control.md\n+++ b/docs/agents/decision-control.md\n@@ -11,7 +11,7 @@\n | Decision | Owner | When | Required inputs | Output | Enforcement surface |\n | --- | --- | --- | --- | --- | --- |\n | Is the Goal clear enough to plan? | Planner + Guardian when needed | Goal intake | User request, existing docs, risk signals | Goal readiness verdict | Goal checklist, issue template, ledger |\n-| Must `grill-with-docs` run? | Planner / Skill Gate | Before PRD or issue split | Ambiguity, domain terms, ADR conflicts, unclear boundaries | Skill invocation or explicit skip rationale | Skill Gate checklist |\n+| Must `grill-with-docs` run? | Planner / Skill Gate | Before PRD or issue split | Ambiguity, domain terms, SADR/target-ADR conflicts, unclear boundaries | Skill invocation or explicit skip rationale | Skill Gate checklist |\n | Is a PRD required? | Planner | After Goal context stabilizes | Goal, `grill-with-docs` output, Context & Impact output | PRD required / not required | PRD checklist, CCA evidence check |\n | Can the spec be frozen? | Operator only | End of Requirements Definition, before issue split | PRD/requirements summary, scope inventory (SF-02), open-question list | `spec_freeze` block in the plan + ledger evidence | `require_grilled_plan` (plan ingest/run), SF checklist |\n | Can a frozen spec change? | Operator only (Spec Amendment) | When implementation contradicts the frozen spec | Contested decisions, scoped re-grill result | recorded amendment + re-stamped freeze | SF checklist, ledger evidence |\ndiff --git a/docs/agents/domain.md b/docs/agents/domain.md\nindex 258da8ea81e87ac0a6b0b5b5e5c2fde980eaabb7..d57b0cf17ff3e1fa9e6e5885372bf083d8295421\n--- a/docs/agents/domain.md\n+++ b/docs/agents/domain.md\n@@ -5,7 +5,9 @@\n ## Layout\n \n - `CONTEXT.md` defines Shiki language. Keep it implementation-free.\n-- `docs/adr/` records hard-to-reverse decisions.\n+- `docs/adr/SADR-NNNN-*.md` records Shiki platform decisions.\n+- `docs/adr/NNNN-*.md` records target product decisions without sharing the\n+  SADR namespace.\n - `AGENTS.md` is the runtime-neutral agent constitution.\n - `CLAUDE.md` is the Claude Code wrapper around `AGENTS.md`.\n - `.shiki/` is the Target Repository mirror for Goals, PRDs, plans, Task DAGs, contracts, locks, worktree records, ledger entries, CCA verdicts, repair packets, reports, and handoffs.\n@@ -15,6 +17,6 @@\n \n - Use Shiki terms exactly when they exist.\n - Add glossary terms to `CONTEXT.md` only when the term is part of the domain, not a generic programming concept.\n-- Create ADRs only for hard-to-reverse, surprising, tradeoff-based decisions.\n+- Create SADRs or target ADRs only for hard-to-reverse, surprising, tradeoff-based decisions.\n - Keep product-specific language in the Target Repository, not in Shiki Template docs.\n-- If an ADR conflicts with a proposed implementation, surface the conflict before editing.\n+- If a SADR or target ADR conflicts with a proposed implementation, surface the conflict before editing.\ndiff --git a/docs/agents/execution-control.md b/docs/agents/execution-control.md\nindex ad8ee3cdb36be56fedc1f46408ca92b387d74047..bc63c3a73661d12b567016ba299f1a6b66bf9088\n--- a/docs/agents/execution-control.md\n+++ b/docs/agents/execution-control.md\n@@ -63,7 +63,7 @@\n | Branch name | Orchestrator | Orchestrator | Before implementer dispatch | branch policy | branch ref in task/ledger |\n | Lock reservation | Context & Impact | MergeGate | Before dispatch and before merge | `.shiki/locks/`, labels, PR metadata | lock id, owner, scope |\n | CI profile | Planner | MergeGate | During issue triage, rechecked at PR | labels, `.shiki/policy.yaml`, workflow matrix | required check list |\n-| Required status checks | Repo maintainer / Guardian | GitHub branch protection / rulesets | Repository setup, updated via ADR | GitHub settings | branch/ruleset config, ADR |\n+| Required status checks | Repo maintainer / Guardian | GitHub branch protection / rulesets | Repository setup, updated via target ADR | GitHub settings | branch/ruleset config, target ADR |\n | CCA judgment | CCA | GitHub required check / MergeGate | Every non-draft PR update | GitHub Action check | structured CCA verdict |\n | Repair assignment | CCA | MergeGate | After failed CCA/CI/review | repair packet, issue/PR comment | repair id and attempt count |\n | Merge readiness | MergeGate | GitHub branch protection / Guardian if needed | After CCA complete | required checks, review rules, rulesets | mergegate check output |\ndiff --git a/docs/agents/guardian-policy.md b/docs/agents/guardian-policy.md\nindex 4147c7adf05a47adb4fd24ada1a1c2e1f2ba6d2d..a20d9cc6c00a569bb057b9ca8d6b51c7fc4a9847\n--- a/docs/agents/guardian-policy.md\n+++ b/docs/agents/guardian-policy.md\n@@ -98,8 +98,8 @@\n `origin/<default_branch>` and reuses each job's existing base fetch; a shallow\n (`--depth`) or second base fetch would make the proof unresolvable and silently\n disable the carry, so neither is permitted. CCA reads `guardian_comment_carried` as\n-a recorded authority in the deterministic signal and does not re-derive it. See ADR\n-0018 for the full decision, the rejected diff/patch-equality and path-disjointness\n+a recorded authority in the deterministic signal and does not re-derive it. See\n+SADR-0018 for the full decision, the rejected diff/patch-equality and path-disjointness\n alternatives, and the accepted residual.\n \n ## External AI Guardian Review (SADR-0010)\ndiff --git a/docs/agents/implementation-policy.md b/docs/agents/implementation-policy.md\nindex c754144a9fb5bf4a38aa82a60e1670c59e626b44..85c37281bcad196c6b7b02343faf00b03cd9c57a\n--- a/docs/agents/implementation-policy.md\n+++ b/docs/agents/implementation-policy.md\n@@ -31,10 +31,13 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n ## Non-Negotiables\n \n 1. **Planning precedes implementation.** Non-trivial Goals start with `grill-with-docs` and Context & Impact.\n-2. **Domain language is controlled.** Terms must be checked against `CONTEXT.md`; hard-to-reverse tradeoffs must be checked against ADRs.\n+2. **Domain language is controlled.** Terms must be checked against `CONTEXT.md`; hard-to-reverse tradeoffs must be checked against Shiki SADRs and target ADRs.\n 3. **PRD precedes issue decomposition.** `to-prd` is used once the context is settled enough to become durable intent.\n 4. **Issues are vertical slices.** `to-issues` must produce independently grabbable tracer-bullet tasks, not horizontal layer tickets.\n 5. **Triage controls dispatch.** Only `ready-for-agent` / AFK tasks may be assigned to Codex without human input.\n@@ -95,19 +98,19 @@\n Process:\n \n - Challenge terms against `CONTEXT.md`.\n-- Check ADRs before reopening decisions.\n+- Check SADRs and target ADRs before reopening decisions.\n - Inspect code when code can answer a question.\n - Ask one question at a time when the operator is available.\n - Provide a recommended answer with each question.\n - Record resolved terms in `CONTEXT.md`.\n-- Create ADRs only for hard-to-reverse, surprising, tradeoff-based decisions.\n+- Create SADRs or target ADRs only for hard-to-reverse, surprising, tradeoff-based decisions.\n - Convert unresolved load-bearing questions into HITL blockers.\n \n Outputs:\n \n - Resolved design-tree decisions.\n - Updated glossary terms when needed.\n-- ADRs when justified.\n+- SADRs or target ADRs when justified.\n - HITL blockers when decisions remain unresolved.\n \n ## Phase 3 \u2014 Context & Impact\n@@ -117,7 +120,7 @@\n \n Outputs must include:\n \n-- Relevant documents and ADRs.\n+- Relevant documents, SADRs, and target ADRs.\n - Relevant modules, interfaces, seams, callers, and tests.\n - Candidate locks.\n - Dependencies.\n@@ -140,7 +143,7 @@\n - Testing decisions.\n - Out of scope.\n - Further notes.\n-- Links to relevant Goal, domain terms, and ADRs.\n+- Links to relevant Goal, domain terms, SADRs, and target ADRs.\n \n Phase 4 ends with Spec Freeze: the operator explicitly approves the PRD, the\n plan records a `spec_freeze` block (status, approved_by, source), and the SF\n@@ -240,7 +243,7 @@\n - Dependencies and locks.\n - Risk and Guardian requirements.\n - Ledger completeness.\n-- Documentation and ADR updates.\n+- Documentation and SADR/target-ADR updates.\n - Scope drift.\n \n CCA emits one of:\n@@ -301,7 +304,7 @@\n | Evidence failure | Behavior might be done, but proof is missing. | Add checks, PR evidence, ledger entry, or artifacts. |\n | Scope drift | PR changes unrelated behavior. | Revert unrelated changes or split into new Goal. |\n | Dependency block | Upstream task or lock not complete. | Block until dependency resolves. |\n-| Architecture block | Decision needs ADR or deep-module discussion. | `grill-with-docs` or `improve-codebase-architecture`. |\n+| Architecture block | Decision needs a SADR, target ADR, or deep-module discussion. | `grill-with-docs` or `improve-codebase-architecture`. |\n | Risk block | Security/production/policy change needs human approval. | Guardian decision. |\n \n ## Phase 5.5 \u2014 Execution Decision Control\ndiff --git a/docs/agents/runtime-auth-model.md b/docs/agents/runtime-auth-model.md\nindex 6a2e1a47e81ea89b5c68a210ec07772a9374babc..3c54748d24233dbd0ba8473950282547ef4ce935\n--- a/docs/agents/runtime-auth-model.md\n+++ b/docs/agents/runtime-auth-model.md\n@@ -148,7 +148,7 @@\n \n API-key based runners may be added by a target repository as an explicit extension. They are not the default Shiki template path.\n \n-If a repo opts into API-key mode, record the exception in an ADR and update the repo's `AGENTS.md`, `.shiki/config.yaml`, and workflow permissions.\n+If a repo opts into API-key mode, record the target-specific exception in a target ADR and update the repo's `AGENTS.md`, `.shiki/config.yaml`, and workflow permissions.\n \n ## GitHub Actions Runtime Compatibility\n \ndiff --git a/docs/agents/skill-gate.md b/docs/agents/skill-gate.md\nindex c50398b0d7c60a8598f2a5c88a10015730dced13..3acffe6444617bb1d54b77d4b4708158ec735bc6\n--- a/docs/agents/skill-gate.md\n+++ b/docs/agents/skill-gate.md\n@@ -9,7 +9,7 @@\n | Skill | Trigger | Expected output |\n | --- | --- | --- |\n | `setup-matt-pocock-skills` | First configuring a repo for these skills, or when issue tracker, triage labels, or domain docs are missing. | `docs/agents/` configuration and an `Agent skills` block in `AGENTS.md` or `CLAUDE.md`. |\n-| `grill-with-docs` | Ambiguous plan, unclear terminology, boundary decision, tradeoff, or ADR-worthy design choice. | Clarified decision tree, recommended answers, updates to `CONTEXT.md` or ADRs when decisions crystallize. |\n+| `grill-with-docs` | Ambiguous plan, unclear terminology, boundary decision, tradeoff, or decision-record-worthy design choice. | Clarified decision tree, recommended answers, updates to `CONTEXT.md`, SADRs, or target ADRs when decisions crystallize. |\n | `zoom-out` | Agent lacks a higher-level architectural map. | Map of relevant modules, callers, seams, and domain concepts. |\n | `to-prd` | Settled context should become a PRD. | PRD published to the issue tracker with problem, solution, user stories, decisions, testing, and out-of-scope notes. |\n | `to-issues` | Goal, PRD, or plan must become implementation tickets. | Vertical-slice issues with acceptance criteria, dependencies, and AFK/HITL classification. |\n@@ -17,7 +17,7 @@\n | `tdd` | Feature work or bug fix has observable behavior that can be specified with tests. | Red-green-refactor loop through public interfaces, one vertical slice at a time. |\n | `code-review` | The TDD loop is green and the implementer is about to create or update a PR. Default for every implementation task. | Pre-PR self-review of the task diff with fixes applied on the task branch; findings and resolutions recorded as a ledger entry naming the skill and a `## Pre-PR code review` PR body section. |\n | `diagnose` | Hard bug, failing check, regression, flaky behavior, or performance problem. | Reproduction loop, ranked hypotheses, instrumentation, fix, regression test, post-mortem. |\n-| `improve-codebase-architecture` | Structural friction, testability, AI-navigability, deep-module opportunity, or refactor request. | Architecture candidates and deepening recommendations informed by `CONTEXT.md` and ADRs. |\n+| `improve-codebase-architecture` | Structural friction, testability, AI-navigability, deep-module opportunity, or refactor request. | Architecture candidates and deepening recommendations informed by `CONTEXT.md`, SADRs, and target ADRs. |\n | `prototype` | A throwaway logic or UI prototype is needed to answer a design question. | Clearly marked disposable prototype and captured decision. |\n \n ## MergeGate Interaction\ndiff --git a/docs/operations.md b/docs/operations.md\nindex cf1f452d4c589e38ccd58553bda0ad8684b51eb7..ace48a90ffbec9e5c0a7113bef16a4858bc44b3f\n--- a/docs/operations.md\n+++ b/docs/operations.md\n@@ -89,6 +89,16 @@\n      target history, brought forward by `shiki migrate`, not a template.\n    - **Everything else** (CLI scripts, workflows, schemas, docs) is overwritten.\n \n+   A forced upgrade also migrates the former numeric Shiki decision paths to\n+   `SADR-NNNN-*`. Before the first write, the installer inventories the exact\n+   legacy Shiki paths and authorizes each deletion only when the existing\n+   `.shiki/install-stamp.json` names that path and its SHA-256 digest still\n+   matches the file. An absent, unreadable, malformed, incomplete, or\n+   mismatching stamp reports every blocked path and stops with no filesystem\n+   change. Target-owned `NNNN-*` ADRs are not cleanup candidates. A non-force\n+   install deletes nothing and retains any still-matching legacy stamp entries\n+   so a later forced upgrade can prove ownership.\n+\n    If the target has pending migrations, `--force` refuses and writes nothing \u2014\n    apply migrations first (step 3), then re-run. The run ends with a single\n    summary of every `.new` written, so a half-upgrade is visible.\ndiff --git a/docs/releasing.md b/docs/releasing.md\nindex 10d73828e7dbd64e9d9f6b83bc84d010537ce62a..aafeee8c4d441e1e427efee2ce4f81a647f020e1\n--- a/docs/releasing.md\n+++ b/docs/releasing.md\n@@ -67,7 +67,7 @@\n CLI in `scripts/`. There are no third-party Python runtime dependencies. The CLI\n is intentionally **not** exposed as a `[project.scripts]` console entry point\n because `scripts/shiki.py` is a dependency-free shim and `scripts/` is not\n-packaged as an importable distribution (see the ADR for the rationale).\n+packaged as an importable distribution (see SADR-0007 for the rationale).\n \n Supported install paths:\n \n@@ -155,6 +155,15 @@\n    [docs/operations.md](operations.md#the-new-convention). If the target has\n    pending migrations, `--force` refuses and writes nothing; apply migrations\n    (step 2) first, then re-run.\n+\n+   The SADR namespace upgrade has an additional pre-write gate: each former\n+   numeric Shiki decision path is removed only when the existing install stamp\n+   contains that exact path and a digest matching its current bytes. The\n+   installer aggregates absent/unreadable/malformed-stamp, missing-digest, and\n+   digest-mismatch blockers before raising, so refusal leaves the target tree\n+   byte-identical. Numeric target ADRs are preserved. A successful forced\n+   upgrade installs and stamps `SADR-*` paths and drops the removed legacy paths\n+   from the refreshed stamp.\n 2. Apply repository-local `.shiki/` state migrations with the migrate command:\n \n    ```bash\ndiff --git a/docs/usage.md b/docs/usage.md\nindex b83407ee3970dd395dd893010bc5dbe8bad26051..a130ef6e40dfd8169a5bc701339351097df1a09f\n--- a/docs/usage.md\n+++ b/docs/usage.md\n@@ -67,8 +67,10 @@\n shiki install-target /path/to/target-repo --local-only\n ```\n \n-Use `--force` only when you intentionally want to overwrite existing target\n-files.\n+Use `--force` only when you intentionally want to refresh the target template.\n+For the SADR namespace migration, `--force` deletes only exact legacy Shiki\n+decision paths whose existing install-stamp digest still matches; any ownership\n+blocker stops before the first write. Numeric target ADRs are never candidates.\n \n ## `shiki bootstrap-platform` \u2014 publish the Shiki platform repo\n \ndiff --git a/scripts/guardian_approval_signal.py b/scripts/guardian_approval_signal.py\nindex d059f92b2c82aa2b5d6ca421365537a87886c1cb..16924892714f4f5b9303c0ef0e325a7e70075630\n--- a/scripts/guardian_approval_signal.py\n+++ b/scripts/guardian_approval_signal.py\n@@ -199,8 +199,8 @@\n ) -> Any:\n     \"\"\"Evaluate SADR-0015 Contract Approval for the CCA Guardian signal.\n \n-    The SAME OR that MergeGate applies must reach this deterministic signal: ADR\n-    0015 is explicit that without it, a contract-approved PR still returns\n+    The SAME OR that MergeGate applies must reach this deterministic signal:\n+    SADR-0015 is explicit that without it, a contract-approved PR still returns\n     ``needs_guardian``, the CCA job fails, and the MergeGate policy check never\n     runs \u2014 the gate this change moves would never be reached. This resolves the\n     task the SAME way MergeGate does (the first Shiki task id in the PR body),\ndiff --git a/scripts/shiki_contract_approval.py b/scripts/shiki_contract_approval.py\nindex 941bcb96fdd8b9a85bc0c4f0f04d5dad9d04839c..4a4b418f6527413161db82670bec857a043360e2\n--- a/scripts/shiki_contract_approval.py\n+++ b/scripts/shiki_contract_approval.py\n@@ -142,7 +142,7 @@\n     changed_files_status: Any,\n     registration: Any,\n ) -> ContractApprovalResult:\n-    \"\"\"Whether this PR carries an SADR-0015 Contract Approval.\n+    \"\"\"Whether this PR carries a SADR-0015 Contract Approval.\n \n     The four conditions are evaluated IN ORDER, each failure recorded as a\n     distinct reason string:\ndiff --git a/scripts/shiki_guardian.py b/scripts/shiki_guardian.py\nindex a4ecb79bbda55fd5c8b06adfae2eb99760a03c45..4dd99bbe714e26f421fa19abac7a34ae1b195ecc\n--- a/scripts/shiki_guardian.py\n+++ b/scripts/shiki_guardian.py\n@@ -12,7 +12,7 @@\n # The carry re-derives its proof from git in-process on every judging invocation;\n # it is never read from an artifact, so there is nothing to go stale or be forged.\n # shiki_sync_proof is the ONLY guardian-family module that runs git, and every one\n-# of its invocations is read-only and local (ADR: prove-from-git base sync).\n+# of its invocations is read-only and local (SADR: prove-from-git base sync).\n from shiki_sync_proof import verify_pure_base_sync\n \n GUARDIAN_POLICY_PATH = \".shiki/guardian-policy.json\"\ndiff --git a/scripts/shiki_guardian_review.py b/scripts/shiki_guardian_review.py\nindex dd0f104d457899e0ba5ff1e162011c1e90b6f92b..e10ef58c046324e7ea3a3392feb3048ca77414ce\n--- a/scripts/shiki_guardian_review.py\n+++ b/scripts/shiki_guardian_review.py\n@@ -159,7 +159,7 @@\n     docs_only = _is_docs_only(changed_files)\n     if docs_only and matched == 0:\n         focus.append(\n-            \"Docs/ADR-only change: confirm the change is genuinely documentation/decision-only, introduces no behavior or contract drift, and stays consistent with the constitution and existing ADRs.\"\n+            \"Docs/decision-record-only change: confirm the change is genuinely documentation/decision-only, introduces no behavior or contract drift, and stays consistent with the constitution and existing SADRs and target ADRs.\"\n         )\n \n     # Conservative augmentation: unknown (no category and not docs-only), mixed\ndiff --git a/scripts/shiki_installer.py b/scripts/shiki_installer.py\nindex be064ab7a29ff6836f7f7a1d70500a43e0d11d7b..9fd8eefd137ce7003dad5c0dcb2e48e2cd47ab32\n--- a/scripts/shiki_installer.py\n+++ b/scripts/shiki_installer.py\n@@ -118,6 +118,7 @@\n # what lets a target carry it without a governance-schema change.\n INSTALL_STAMP_PATH = \".shiki/install-stamp.json\"\n INSTALL_STAMP_VERSION = 1\n+LEGACY_SHIKI_SADR_MAX = 18\n \n # Under --force the shipped surface splits three ways, so nothing is lost and\n # nothing is silently stale.\n@@ -237,9 +238,98 @@\n         )\n \n \n+def legacy_shiki_adr_paths() -> tuple[str, ...]:\n+    \"\"\"Former numeric paths for Shiki SADR-0001 through SADR-0018.\n+\n+    The authoritative slugs come from the current SADR filenames. SADR-0019 is\n+    the namespace decision itself and never shipped under a numeric legacy path.\n+    \"\"\"\n+    paths: list[str] = []\n+    for sadr in sorted((ROOT / \"docs\" / \"adr\").glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\")):\n+        number = sadr.name.split(\"-\", 2)[1]\n+        if int(number) <= LEGACY_SHIKI_SADR_MAX:\n+            paths.append(f\"docs/adr/{sadr.name.removeprefix('SADR-')}\")\n+    return tuple(paths)\n+\n+\n+def preflight_legacy_sadr_cleanup(target: Path, *, force: bool) -> tuple[str, ...]:\n+    \"\"\"Authorize exact legacy Shiki ADR removals before the first write.\n+\n+    A non-force install never removes existing files. A forced install needs no\n+    stamp when no legacy Shiki path is present. When legacy paths do exist, each\n+    must be named in a readable install stamp and its current bytes must match\n+    the recorded digest.\n+    \"\"\"\n+    if not force:\n+        return ()\n+    present = tuple(\n+        relative\n+        for relative in legacy_shiki_adr_paths()\n+        if (target / relative).exists() or (target / relative).is_symlink()\n+    )\n+    if not present:\n+        return ()\n+\n+    stamp_path = target / INSTALL_STAMP_PATH\n+    try:\n+        raw = stamp_path.read_text(encoding=\"utf-8\")\n+        stamp = json.loads(raw)\n+    except FileNotFoundError:\n+        detail = \"install stamp is absent\"\n+        blockers = [f\"{relative}: {detail}\" for relative in present]\n+    except (OSError, UnicodeDecodeError) as error:\n+        detail = f\"install stamp is unreadable ({error})\"\n+        blockers = [f\"{relative}: {detail}\" for relative in present]\n+    except json.JSONDecodeError as error:\n+        detail = f\"install stamp is malformed ({error})\"\n+        blockers = [f\"{relative}: {detail}\" for relative in present]\n+    else:\n+        if not isinstance(stamp, dict):\n+            blockers = [\n+                f\"{relative}: install stamp is malformed (expected an object)\"\n+                for relative in present\n+            ]\n+        else:\n+            digests = stamp.get(\"digests\")\n+            blockers = []\n+            if not isinstance(digests, dict):\n+                blockers.extend(\n+                    f\"{relative}: install stamp has no digest for this path\"\n+                    for relative in present\n+                )\n+            else:\n+                for relative in present:\n+                    expected = digests.get(relative)\n+                    if not isinstance(expected, str) or not expected:\n+                        blockers.append(\n+                            f\"{relative}: install stamp has no digest for this path\"\n+                        )\n+                        continue\n+                    try:\n+                        actual = _sha256_file(target / relative)\n+                    except OSError as error:\n+                        blockers.append(\n+                            f\"{relative}: legacy path is unreadable ({error})\"\n+                        )\n+                        continue\n+                    if actual != expected:\n+                        blockers.append(\n+                            f\"{relative}: digest mismatch (stamp {expected}, current {actual})\"\n+                        )\n+\n+    if blockers:\n+        joined = \"\\n\".join(f\"- {blocker}\" for blocker in blockers)\n+        raise ShikiError(\n+            \"legacy Shiki ADR cleanup is not authorized:\\n\"\n+            f\"{joined}\\nNo files were written.\"\n+        )\n+    return present\n+\n+\n def install_template(target: Path, *, force: bool, validate: bool) -> list[NewFileNote]:\n     # Preconditions first: never rewrite the tree of a target that cannot be\n     # upgraded cleanly (see refuse_pending_migrations).\n+    legacy_cleanup = preflight_legacy_sadr_cleanup(target, force=force)\n     refuse_pending_migrations(target)\n \n     # Resolve the target's required CODEOWNERS owner once from its\n@@ -250,6 +340,10 @@\n     # verbatim. See ``codeowners_required_owner``.\n     codeowners_owner = codeowners_required_owner(target)\n \n+    for relative in legacy_cleanup:\n+        (target / relative).unlink()\n+        info(f\"removed stamped legacy Shiki ADR: {target / relative}\")\n+\n     notes: list[NewFileNote] = []\n     for relative in TEMPLATE_PATHS:\n         source = ROOT / relative\n@@ -647,6 +741,22 @@\n     digests = target_stamp_digests(target)\n     existing = read_install_stamp(target)\n     if existing is not None:\n+        # A non-force install deliberately leaves legacy Shiki ADRs in place.\n+        # Preserve only still-matching ownership proof so a later forced\n+        # upgrade can authorize cleanup; never carry a missing or stale digest.\n+        existing_digests = existing.get(\"digests\")\n+        if isinstance(existing_digests, dict):\n+            for relative in legacy_shiki_adr_paths():\n+                expected = existing_digests.get(relative)\n+                path = target / relative\n+                if not isinstance(expected, str) or not path.is_file():\n+                    continue\n+                try:\n+                    actual = _sha256_file(path)\n+                except OSError:\n+                    continue\n+                if actual == expected:\n+                    digests[relative] = actual\n         if _incoming_is_older(commit, existing.get(\"platform_commit\")):\n             warn(\n                 \"kept existing install stamp: incoming install \"\ndiff --git a/scripts/shiki_loop.py b/scripts/shiki_loop.py\nindex 3611641da8aeb5038b1da90e3114b09a473ec6d8..84caa491b9cad0edc4176f209c13e9221e975681\n--- a/scripts/shiki_loop.py\n+++ b/scripts/shiki_loop.py\n@@ -424,7 +424,7 @@\n         # unresolvable CCA verdict has already produced its terminal stop and is never\n         # laundered into a head-moving sync (SADR-0010). It also never fires while the\n         # policy (Guardian) gate is red \u2014 for ANY reason, not only when it is the sole\n-        # red check \u2014 so a head-bound Guardian approval, or an SADR-0015\n+        # red check \u2014 so a head-bound Guardian approval, or a SADR-0015\n         # contract-approval carry that base movement defeated (the policy gate compares\n         # base and head task governance directly), is never silently restored by\n         # adopting base wholesale. It is bounded by a durable per-task counter so the\ndiff --git a/scripts/shiki_tasks.py b/scripts/shiki_tasks.py\nindex aec3809f24b4a0c8c96fc50971cff9ae9ad30381..d531052e9d9717058d4631483cefab44c26a65c1\n--- a/scripts/shiki_tasks.py\n+++ b/scripts/shiki_tasks.py\n@@ -821,7 +821,7 @@\n         \"prompt\": prompt,\n         \"entry_skill\": \"grill-with-docs\",\n         \"required_next_steps\": [\n-            \"Run grill-with-docs until terminology, boundaries, risks, and ADR-worthy decisions are settled.\",\n+            \"Run grill-with-docs until terminology, boundaries, risks, and decision-record-worthy choices are settled.\",\n             \"Obtain the operator's explicit approval of the PRD/requirements (Spec Freeze).\",\n             \"Write a machine-readable plan JSON with grill_with_docs.status=complete and spec_freeze.status=frozen.\",\n             \"Run shiki plan ingest --plan-file PLAN.json.\",\ndiff --git a/scripts/test_shiki_goal_loop.sh b/scripts/test_shiki_goal_loop.sh\nindex 753c7b516e10f0be3bc92a4bd0eb1fa699776ae6..8f3820a0a079b3044424cfe1a6c6b7fe68896e67\n--- a/scripts/test_shiki_goal_loop.sh\n+++ b/scripts/test_shiki_goal_loop.sh\n@@ -231,8 +231,8 @@\n \n # High/critical risk merges autonomously when all required checks are green:\n # the \"MergeGate policy check\" required check IS the Guardian gate (it enforces\n-# guardian-policy.json \u2014 human approval OR external AI guardian review, ADR\n-# 0010), so green checks mean a recorded authority approved. The fake gh returns\n+# guardian-policy.json \u2014 human approval OR external AI guardian review,\n+# SADR-0010), so green checks mean a recorded authority approved. The fake gh returns\n # green checks, simulating that the Guardian gate passed.\n python3 \"$ROOT/scripts/shiki.py\" goal create --target \"$TARGET\" --title \"Guardian gate\" --outcome \"High risk merges via policy gate\" >/tmp/shiki-goal-loop-guardian-goal.json\n GGOAL=\"$(json_get /tmp/shiki-goal-loop-guardian-goal.json goal_id)\"\ndiff --git a/tests/test_adr_references.py b/tests/test_adr_references.py\nindex 51706329c797d5dd9b47e082a333fa83b261d482..d005da4f771f5e8085ec22d4b787980e68f5ae8c\n--- a/tests/test_adr_references.py\n+++ b/tests/test_adr_references.py\n@@ -1,8 +1,8 @@\n-\"\"\"Every ``ADR ####`` citation must resolve to exactly one decision record.\n+\"\"\"Every target ADR and Shiki SADR citation resolves in its own namespace.\n \n Background. ``scripts/mergegate_check.py`` justifies ``is_bookkeeping_closeout``\n-with \"ADR 0017\" nine times and ``scripts/guardian_approval_signal.py`` cites\n-0016/0017 seven times, yet for a stretch the numbering in ``docs/adr/`` stopped\n+with \"SADR-0017\" nine times and ``scripts/guardian_approval_signal.py`` cites\n+SADR-0016/SADR-0017 seven times, yet for a stretch the numbering in ``docs/adr/`` stopped\n at 0015: the authoring commits lived on a coordinator branch and no task ever\n declared ``docs/adr/...`` in its locks, so nothing carried the records to the\n default branch. The stated basis of an exemption that merged seven PRs with zero\n@@ -11,11 +11,12 @@\n and the directory drifted with no signal.\n \n This is the same unbound-pair shape behind other silent failures: a reference\n-and its target with nothing asserting they agree. This test binds them. It scans\n-every ``scripts/*.py`` (plus ``docs/**/*.md`` and the root ``*.md`` governance\n-files, which are cheap) for the ``ADR ####`` pattern and asserts each cited\n-number resolves to exactly one ``docs/adr/####-*.md``. The converse is\n-deliberately NOT required: an ADR may exist without being cited.\n+and its target with nothing asserting they agree. This test binds them. Target\n+``ADR NNNN`` citations resolve only to ``docs/adr/NNNN-*.md``; Shiki\n+``SADR-NNNN`` citations resolve only to ``docs/adr/SADR-NNNN-*.md``. The scan\n+covers current governance docs, scripts, workflows, prompts, packaging metadata,\n+and repo-local Codex guidance while deliberately excluding historical\n+``.shiki`` evidence. The converse is not required: a record may be uncited.\n \n The suite is dependency free: standard-library ``unittest`` only.\n \"\"\"\n@@ -31,17 +32,18 @@\n \n REPO_ROOT = Path(__file__).resolve().parents[1]\n \n-# \"ADR 0016\" -- a four-digit citation. The trailing \\b keeps a five-digit token\n-# from being read as a truncated four-digit number.\n-_CITATION = re.compile(r\"\\bADR (\\d{4})\\b\")\n-# \"0016-locks-bound-product-paths.md\" -- the leading four digits name the record.\n-_ADR_FILENAME = re.compile(r\"^(\\d{4})-.*\\.md$\")\n+# ``ADR 0016`` / ``SADR-0016`` -- a namespace plus four-digit citation. The\n+# trailing ``\\b`` keeps a five-digit token from being read as a truncated one.\n+_CITATION = re.compile(r\"\\b(S?ADR)[ -](\\d{4})\\b\")\n+# Target ADRs retain the numeric filename; Shiki platform decisions use SADR.\n+_DECISION_FILENAME = re.compile(r\"^(?:(SADR)-)?(\\d{4})-.*\\.md$\")\n \n \n class Violation(NamedTuple):\n     \"\"\"A citation that does not resolve to exactly one decision record.\"\"\"\n \n     citing_file: str  # repo-relative path of the file that cites the ADR\n+    namespace: str  # \"ADR\" for target decisions; \"SADR\" for Shiki decisions\n     number: str  # the cited four-digit number, e.g. \"0016\"\n     line: int  # 1-based line of the citation\n     reason: str  # \"missing\" (zero matches) or \"ambiguous\" (two or more)\n@@ -49,44 +51,59 @@\n \n     @property\n     def expected_path(self) -> str:\n-        return f\"docs/adr/{self.number}-*.md\"\n+        prefix = \"SADR-\" if self.namespace == \"SADR\" else \"\"\n+        return f\"docs/adr/{prefix}{self.number}-*.md\"\n \n     def describe(self) -> str:\n         \"\"\"Name the citing file, the cited number, and the missing path.\"\"\"\n         if self.reason == \"missing\":\n             return (\n-                f\"{self.citing_file}:{self.line} cites ADR {self.number} \"\n+                f\"{self.citing_file}:{self.line} cites {self.namespace} {self.number} \"\n                 f\"but no document matches {self.expected_path}\"\n             )\n         joined = \", \".join(self.matches)\n         return (\n-            f\"{self.citing_file}:{self.line} cites ADR {self.number} which \"\n+            f\"{self.citing_file}:{self.line} cites {self.namespace} {self.number} which \"\n             f\"resolves to {len(self.matches)} documents (expected exactly one \"\n             f\"at {self.expected_path}): {joined}\"\n         )\n \n \n-def _adr_index(root: Path) -> dict[str, list[Path]]:\n-    \"\"\"Map every four-digit ADR number to the record files that claim it.\"\"\"\n-    index: dict[str, list[Path]] = {}\n+def _adr_index(root: Path) -> dict[tuple[str, str], list[Path]]:\n+    \"\"\"Map each decision namespace and number to the files that claim it.\"\"\"\n+    index: dict[tuple[str, str], list[Path]] = {}\n     adr_dir = root / \"docs\" / \"adr\"\n     if adr_dir.is_dir():\n         for path in sorted(adr_dir.glob(\"*.md\")):\n-            match = _ADR_FILENAME.match(path.name)\n+            match = _DECISION_FILENAME.match(path.name)\n             if match:\n-                index.setdefault(match.group(1), []).append(path)\n+                namespace = \"SADR\" if match.group(1) else \"ADR\"\n+                index.setdefault((namespace, match.group(2)), []).append(path)\n     return index\n \n \n def _citing_files(root: Path, include_docs: bool = True) -> list[Path]:\n-    \"\"\"Files scanned for citations: scripts/*.py, and cheaply the markdown.\"\"\"\n+    \"\"\"Current, shipped text surfaces scanned for decision citations.\"\"\"\n     files = sorted((root / \"scripts\").glob(\"*.py\"))\n+    files += sorted((root / \"scripts\").glob(\"*.sh\"))\n     if include_docs:\n         docs = root / \"docs\"\n         if docs.is_dir():\n             files += sorted(docs.rglob(\"*.md\"))\n         files += sorted(root.glob(\"*.md\"))\n-    return files\n+        pyproject = root / \"pyproject.toml\"\n+        if pyproject.is_file():\n+            files.append(pyproject)\n+        for relative in (\".github\", \".codex\"):\n+            surface = root / relative\n+            if surface.is_dir():\n+                files += sorted(\n+                    path\n+                    for path in surface.rglob(\"*\")\n+                    if path.is_file()\n+                    and path.suffix in {\".md\", \".py\", \".sh\", \".toml\", \".yaml\", \".yml\"}\n+                )\n+    return sorted(set(files))\n \n \n def find_adr_reference_violations(\n@@ -94,10 +111,9 @@\n ) -> list[Violation]:\n     \"\"\"Return every ``ADR ####`` citation under ``root`` that fails to resolve.\n \n-    A citation resolves when exactly one ``docs/adr/####-*.md`` matches its\n-    number. Zero matches is a \"missing\" violation; two or more is \"ambiguous\".\n-    Records that are never cited produce nothing -- the converse is not\n-    required.\n+    A citation resolves when exactly one record in the same namespace matches\n+    its number. Zero matches is a \"missing\" violation; two or more is\n+    \"ambiguous\". Records that are never cited produce nothing.\n     \"\"\"\n     root = Path(root)\n     index = _adr_index(root)\n@@ -110,13 +126,15 @@\n         rel = path.relative_to(root).as_posix()\n         for lineno, line in enumerate(text.splitlines(), start=1):\n             for match in _CITATION.finditer(line):\n-                number = match.group(1)\n-                found = index.get(number, [])\n+                namespace, number = match.groups()\n+                found = index.get((namespace, number), [])\n                 if len(found) == 1:\n                     continue\n                 reason = \"missing\" if not found else \"ambiguous\"\n                 rels = [p.relative_to(root).as_posix() for p in found]\n-                violations.append(Violation(rel, number, lineno, reason, rels))\n+                violations.append(\n+                    Violation(rel, namespace, number, lineno, reason, rels)\n+                )\n     return violations\n \n \n@@ -129,8 +147,8 @@\n         self.assertEqual(\n             violations,\n             [],\n-            \"Every 'ADR ####' citation must resolve to exactly one \"\n-            f\"docs/adr/####-*.md:\\n{detail}\",\n+            \"Every ADR/SADR citation must resolve exactly once in its own \"\n+            f\"docs/adr namespace:\\n{detail}\",\n         )\n \n     def test_scan_actually_reads_scripts(self) -> None:\n@@ -142,12 +160,38 @@\n             \"the citation scan must cover scripts/*.py\",\n         )\n \n+    def test_platform_citations_do_not_use_unresolved_numeric_shorthand(self) -> None:\n+        shorthand = re.compile(r\"SADR-\\d{4}/\\d{4}\")\n+        offenders = []\n+        for path in _citing_files(REPO_ROOT):\n+            text = path.read_text(encoding=\"utf-8\")\n+            if shorthand.search(text):\n+                offenders.append(path.relative_to(REPO_ROOT).as_posix())\n+        self.assertEqual(\n+            offenders,\n+            [],\n+            \"repeat the SADR prefix so every cited number is independently resolvable\",\n+        )\n+\n     def test_carried_records_are_indexed(self) -> None:\n         # The two records this Goal carried onto the default branch, and which\n         # the scripts cite most, must be present.\n         index = _adr_index(REPO_ROOT)\n-        self.assertIn(\"0016\", index)\n-        self.assertIn(\"0017\", index)\n+        self.assertIn((\"SADR\", \"0016\"), index)\n+        self.assertIn((\"SADR\", \"0017\"), index)\n+\n+    def test_platform_tree_has_only_sadr_0001_through_0019(self) -> None:\n+        adr_dir = REPO_ROOT / \"docs\" / \"adr\"\n+        sadrs = sorted(adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\"))\n+        self.assertEqual(\n+            {path.name.split(\"-\", 2)[1] for path in sadrs},\n+            {f\"{number:04d}\" for number in range(1, 20)},\n+        )\n+        self.assertEqual(\n+            list(adr_dir.glob(\"[0-9][0-9][0-9][0-9]-*.md\")),\n+            [],\n+            \"the Shiki platform tree must not retain legacy numeric SADR paths\",\n+        )\n \n \n class FixtureAdrReferences(unittest.TestCase):\n@@ -180,6 +224,26 @@\n         self.assertIn(\"9991\", message)  # cited number\n         self.assertIn(\"docs/adr/9991-*.md\", message)  # missing path\n \n+    def test_target_adr_does_not_satisfy_missing_platform_sadr(self) -> None:\n+        root = self._make_repo(\n+            adr_names=[\"0042-target-decision.md\"],\n+            script_body='\"\"\"Target ADR 0042 exists, but platform SADR-0042 does not.\"\"\"\\n',\n+        )\n+        violations = find_adr_reference_violations(root, include_docs=False)\n+        self.assertEqual(len(violations), 1)\n+        self.assertEqual(violations[0].namespace, \"SADR\")\n+        self.assertEqual(violations[0].number, \"0042\")\n+        self.assertEqual(violations[0].expected_path, \"docs/adr/SADR-0042-*.md\")\n+\n+    def test_target_adr_and_platform_sadr_may_reuse_a_number(self) -> None:\n+        root = self._make_repo(\n+            adr_names=[\"0042-target-decision.md\", \"SADR-0042-platform-decision.md\"],\n+            script_body='\"\"\"Target ADR 0042 and platform SADR-0042 are distinct.\"\"\"\\n',\n+        )\n+        self.assertEqual(\n+            find_adr_reference_violations(root, include_docs=False), []\n+        )\n+\n     def test_uncited_adr_is_allowed(self) -> None:\n         # An ADR exists but nothing cites it -> not a violation. The converse\n         # (every ADR must be cited) is explicitly NOT required.\ndiff --git a/tests/test_guardian_review.py b/tests/test_guardian_review.py\nindex 88829e7810212635e2fc98aa5918d6f26294677d..00a85a237acd5bf2d6cb5b294128198c422402ca\n--- a/tests/test_guardian_review.py\n+++ b/tests/test_guardian_review.py\n@@ -162,7 +162,7 @@\n \n     def test_docs_only_is_light_and_not_conservative(self):\n         focus = gr.classify_review_focus([\"docs/adr/0099-x.md\", \"README.md\"], \"\", \"low\")\n-        self.assertTrue(any(\"Docs/ADR-only\" in f for f in focus))\n+        self.assertTrue(any(\"Docs/decision-record-only\" in f for f in focus))\n         self.assertFalse(any(\"conservative default\" in f.lower() for f in focus))\n \n     def test_unknown_category_is_conservative(self):\ndiff --git a/tests/test_policy_job_merged_prs.py b/tests/test_policy_job_merged_prs.py\nindex d45512e74681e5e649279236c7d4e79d5c3da471..b97989f8bd74d091de1a6b5275a20d8dc68c4512\n--- a/tests/test_policy_job_merged_prs.py\n+++ b/tests/test_policy_job_merged_prs.py\n@@ -3,8 +3,8 @@\n \n ``.github/workflows/shiki-cca-completion.yml`` has two consumers of the merged-PR\n proof: the CCA *signal* (``guardian_approval_signal.py`` in \"Prepare CCA context\")\n-and the authoritative gate (``mergegate_check.py`` in \"Enforce MergeGate\"). ADR\n-0017's bookkeeping-closeout exemption (condition 6) needs the merged implementation\n+and the authoritative gate (``mergegate_check.py`` in \"Enforce MergeGate\").\n+SADR-0017's bookkeeping-closeout exemption (condition 6) needs the merged implementation\n PR proven, and a closeout carries NO ``post_merge_reconcile`` marker. The signal\n already resolves ``MERGED_PRS`` unconditionally, but \"Enforce MergeGate\" used to\n resolve it only inside ``grep -q '<!-- shiki:post_merge_reconcile -->'``. So a\ndiff --git a/tests/test_sadr_install_migration.py b/tests/test_sadr_install_migration.py\nnew file mode 100644\nindex 0000000000000000000000000000000000000000..a6f4c57e4dcaef73ef600b0d4804744138ab9071\n--- /dev/null\n+++ b/tests/test_sadr_install_migration.py\n@@ -0,0 +1,229 @@\n+\"\"\"SADR installation and collision-safe legacy upgrade behavior.\"\"\"\n+\n+from __future__ import annotations\n+\n+import contextlib\n+import hashlib\n+import io\n+import json\n+import shutil\n+import tempfile\n+import unittest\n+from pathlib import Path\n+\n+import shiki_test_support  # noqa: F401  (path bootstrap)\n+\n+import shiki_installer\n+from tests.test_adr_references import find_adr_reference_violations\n+\n+\n+PRODUCT_NUMBERS = tuple(range(13, 19))\n+\n+\n+def _quiet_install(target: Path, *, force: bool, validate: bool) -> None:\n+    with contextlib.redirect_stdout(io.StringIO()):\n+        shiki_installer.install_template(target, force=force, validate=validate)\n+\n+\n+def _sha256(path: Path) -> str:\n+    return hashlib.sha256(path.read_bytes()).hexdigest()\n+\n+\n+def _legacy_sadr_paths() -> tuple[str, ...]:\n+    return tuple(\n+        f\"docs/adr/{path.name.removeprefix('SADR-')}\"\n+        for path in sorted((shiki_installer.ROOT / \"docs\" / \"adr\").glob(\"SADR-*.md\"))\n+        if path.name != \"SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\"\n+    )\n+\n+\n+def _create_legacy_upgrade_fixture(\n+    target: Path,\n+) -> tuple[dict[str, bytes], tuple[str, ...]]:\n+    adr_dir = target / \"docs\" / \"adr\"\n+    adr_dir.mkdir(parents=True)\n+    product_bytes = {\n+        f\"{number:04d}-cubeshare-product-decision.md\": (\n+            f\"# CubeShare ADR {number:04d}\\n\\nDo not rewrite {number}.\\n\"\n+        ).encode()\n+        for number in PRODUCT_NUMBERS\n+    }\n+    for name, content in product_bytes.items():\n+        (adr_dir / name).write_bytes(content)\n+\n+    legacy_paths = _legacy_sadr_paths()\n+    for relative in legacy_paths:\n+        (target / relative).write_bytes(f\"legacy Shiki bytes for {relative}\\n\".encode())\n+\n+    stamp_path = target / shiki_installer.INSTALL_STAMP_PATH\n+    stamp_path.parent.mkdir(parents=True, exist_ok=True)\n+    stamp = {\n+        \"version\": shiki_installer.INSTALL_STAMP_VERSION,\n+        \"platform_commit\": None,\n+        \"installed_at\": \"2000-01-01T00:00:00+00:00\",\n+        \"digests\": {\n+            relative: _sha256(target / relative) for relative in legacy_paths\n+        },\n+    }\n+    stamp_path.write_text(\n+        json.dumps(stamp, indent=2, sort_keys=True) + \"\\n\", encoding=\"utf-8\"\n+    )\n+    return product_bytes, legacy_paths\n+\n+\n+def _tree_snapshot(root: Path) -> dict[str, tuple[str, bytes | str]]:\n+    \"\"\"Recursive byte/shape snapshot used to prove preflight is write-free.\"\"\"\n+    snapshot: dict[str, tuple[str, bytes | str]] = {}\n+    for path in sorted(root.rglob(\"*\")):\n+        relative = path.relative_to(root).as_posix()\n+        if path.is_symlink():\n+            snapshot[relative] = (\"symlink\", path.readlink().as_posix())\n+        elif path.is_dir():\n+            snapshot[relative] = (\"directory\", \"\")\n+        else:\n+            snapshot[relative] = (\"file\", path.read_bytes())\n+    return snapshot\n+\n+\n+class FreshSadrInstallTests(unittest.TestCase):\n+    def setUp(self) -> None:\n+        self.tmp = Path(tempfile.mkdtemp(prefix=\"shiki-sadr-fresh-\"))\n+        self.addCleanup(lambda: shutil.rmtree(self.tmp, ignore_errors=True))\n+        self.target = self.tmp / \"target\"\n+        self.adr_dir = self.target / \"docs\" / \"adr\"\n+        self.adr_dir.mkdir(parents=True)\n+        self.product_bytes = {\n+            f\"{number:04d}-product-decision.md\": (\n+                f\"# Product ADR {number:04d}\\n\\nTarget-owned bytes {number}.\\n\"\n+            ).encode()\n+            for number in PRODUCT_NUMBERS\n+        }\n+        for name, content in self.product_bytes.items():\n+            (self.adr_dir / name).write_bytes(content)\n+\n+    def test_fresh_install_preserves_product_adrs_and_installs_all_sadrs(self) -> None:\n+        _quiet_install(self.target, force=False, validate=True)\n+\n+        for name, expected in self.product_bytes.items():\n+            self.assertEqual((self.adr_dir / name).read_bytes(), expected)\n+\n+        sadr_numbers = {\n+            path.name.split(\"-\", 2)[1]\n+            for path in self.adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\")\n+        }\n+        self.assertEqual(sadr_numbers, {f\"{number:04d}\" for number in range(1, 20)})\n+\n+        numeric_records = {\n+            path.name\n+            for path in self.adr_dir.glob(\"[0-9][0-9][0-9][0-9]-*.md\")\n+        }\n+        self.assertEqual(numeric_records, set(self.product_bytes))\n+        self.assertEqual(find_adr_reference_violations(self.target), [])\n+\n+\n+class LegacySadrUpgradeTests(unittest.TestCase):\n+    def setUp(self) -> None:\n+        self.tmp = Path(tempfile.mkdtemp(prefix=\"shiki-sadr-upgrade-\"))\n+        self.addCleanup(lambda: shutil.rmtree(self.tmp, ignore_errors=True))\n+        self.target = self.tmp / \"target\"\n+        self.adr_dir = self.target / \"docs\" / \"adr\"\n+        self.product_bytes, self.legacy_paths = _create_legacy_upgrade_fixture(\n+            self.target\n+        )\n+\n+    def test_force_upgrade_deletes_only_stamped_legacy_sadrs(self) -> None:\n+        _quiet_install(self.target, force=True, validate=True)\n+\n+        for relative in self.legacy_paths:\n+            self.assertFalse((self.target / relative).exists(), relative)\n+        for name, expected in self.product_bytes.items():\n+            self.assertEqual((self.adr_dir / name).read_bytes(), expected)\n+\n+        sadrs = list(self.adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\"))\n+        self.assertEqual(len(sadrs), 19)\n+        refreshed = json.loads(\n+            (self.target / shiki_installer.INSTALL_STAMP_PATH).read_text(encoding=\"utf-8\")\n+        )\n+        for relative in self.legacy_paths:\n+            self.assertNotIn(relative, refreshed[\"digests\"])\n+        for path in sadrs:\n+            relative = path.relative_to(self.target).as_posix()\n+            self.assertEqual(refreshed[\"digests\"][relative], _sha256(path))\n+\n+    def test_non_force_install_never_deletes_legacy_files(self) -> None:\n+        before = {\n+            relative: (self.target / relative).read_bytes()\n+            for relative in self.legacy_paths\n+        }\n+        _quiet_install(self.target, force=False, validate=False)\n+        for relative, expected in before.items():\n+            self.assertEqual((self.target / relative).read_bytes(), expected)\n+        refreshed = json.loads(\n+            (self.target / shiki_installer.INSTALL_STAMP_PATH).read_text(encoding=\"utf-8\")\n+        )\n+        for relative in self.legacy_paths:\n+            self.assertEqual(refreshed[\"digests\"][relative], _sha256(self.target / relative))\n+\n+\n+class LegacyCleanupPreflightTests(unittest.TestCase):\n+    def setUp(self) -> None:\n+        self.tmp = Path(tempfile.mkdtemp(prefix=\"shiki-sadr-preflight-\"))\n+        self.addCleanup(lambda: shutil.rmtree(self.tmp, ignore_errors=True))\n+\n+    def test_all_ownership_blockers_abort_before_any_write(self) -> None:\n+        cases = (\n+            (\"absent\", \"install stamp is absent\"),\n+            (\"unreadable\", \"install stamp is unreadable\"),\n+            (\"malformed\", \"install stamp is malformed\"),\n+            (\"missing-digest\", \"install stamp has no digest\"),\n+            (\"digest-mismatch\", \"digest mismatch\"),\n+        )\n+        for name, expected_reason in cases:\n+            with self.subTest(name=name):\n+                target = self.tmp / name\n+                _, legacy_paths = _create_legacy_upgrade_fixture(target)\n+                stamp_path = target / shiki_installer.INSTALL_STAMP_PATH\n+\n+                if name == \"absent\":\n+                    stamp_path.unlink()\n+                elif name == \"unreadable\":\n+                    stamp_path.unlink()\n+                    stamp_path.mkdir()\n+                elif name == \"malformed\":\n+                    stamp_path.write_text(\"{not-json\\n\", encoding=\"utf-8\")\n+                else:\n+                    stamp = json.loads(stamp_path.read_text(encoding=\"utf-8\"))\n+                    if name == \"missing-digest\":\n+                        stamp[\"digests\"] = {}\n+                    else:\n+                        stamp[\"digests\"] = {\n+                            relative: \"deadbeef\" for relative in legacy_paths\n+                        }\n+                    stamp_path.write_text(\n+                        json.dumps(stamp, indent=2, sort_keys=True) + \"\\n\",\n+                        encoding=\"utf-8\",\n+                    )\n+\n+                before = _tree_snapshot(target)\n+                with self.assertRaises(shiki_installer.ShikiError) as raised:\n+                    _quiet_install(target, force=True, validate=False)\n+                after = _tree_snapshot(target)\n+\n+                self.assertEqual(before, after, f\"{name} preflight changed the tree\")\n+                message = str(raised.exception)\n+                self.assertIn(expected_reason, message)\n+                self.assertIn(\"No files were written\", message)\n+                for relative in legacy_paths:\n+                    self.assertIn(relative, message)\n+\n+    def test_absent_stamp_is_allowed_when_no_legacy_path_exists(self) -> None:\n+        target = self.tmp / \"no-legacy\"\n+        target.mkdir()\n+        _quiet_install(target, force=True, validate=False)\n+        self.assertTrue(\n+            (target / \"docs\" / \"adr\" / \"SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\").is_file()\n+        )\n+\n+\n+if __name__ == \"__main__\":\n+    unittest.main()\n\n2026-08-07T08:21:05.180189Z ERROR codex_models_manager::manager: failed to renew cache TTL: missing field `base_instructions` at line 94 column 5\n succeeded in 218ms:\ndocs/adr/0001-keep-shiki-as-platform-name.md 3 3\ndocs/adr/0002-use-agents-as-shared-constitution.md 3 3\ndocs/adr/0003-require-skill-gate-for-mergegate-readiness.md 3 3\ndocs/adr/0004-use-github-first-with-shiki-mirror.md 22 22\ndocs/adr/0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md 29 29\ndocs/adr/0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md 54 54\ndocs/adr/0007-packaging-and-release.md 126 126\ndocs/adr/0008-use-claude-code-as-default-implementer.md 108 108\ndocs/adr/0009-requirements-definition-and-spec-freeze.md 90 90\ndocs/adr/0010-external-ai-guardian-review.md 97 97\ndocs/adr/0011-autonomous-quality-gate-evidence.md 87 87\ndocs/adr/0012-autonomous-loop-closeout-to-main.md 44 44\ndocs/adr/0013-bootstrap-grants-actions-approve-pull-requests.md 109 109\ndocs/adr/0014-codex-app-external-ai-guardian-ui-adapter.md 102 102\ndocs/adr/0015-contract-approval-before-dispatch.md 229 229\ndocs/adr/0016-locks-bound-product-paths.md 233 233\ndocs/adr/0017-bookkeeping-closeouts-do-not-inherit-risk.md 129 129\ndocs/adr/0018-carried-guardian-approval-across-a-proven-base-sync.md 147 147\ndocs/adr/0019-separate-platform-sadr-and-target-adr-namespaces.md 0 55\n\nexec\n/bin/zsh -lc 'PYTHONPATH=tests python3 -m unittest tests.test_adr_references tests.test_guardian_review tests.test_paired_invariants -v' in /private/tmp/shiki-adr-collision.pa0KwY/.worktrees/shiki-t-20260807t073307221254z-2d22d654-introduc\ndiff --git a/.codex/skills/shiki/SKILL.md b/.codex/skills/shiki/SKILL.md\nindex c8e2e85d9d75d49cc4f2d6286ee22659c4e53e75..93cded623b2c15d9f65c76491a1c2d813ac9e6c0\n--- a/.codex/skills/shiki/SKILL.md\n+++ b/.codex/skills/shiki/SKILL.md\n@@ -18,6 +18,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n Codex CLI does not register this skill as a custom `/shiki` slash command. When\n the user types `/shiki` in Codex CLI and gets \"Unrecognized command\", explain\n that this is expected: invoke Shiki in Codex with natural language such as\n@@ -69,7 +72,7 @@\n \n ## Responsibilities\n \n-- Claude Code implements and repairs by default (`shiki runner claude`, ADR 0008); Codex implements and repairs tasks explicitly assigned to `codex`.\n+- Claude Code implements and repairs by default (`shiki runner claude`, SADR-0008); Codex implements and repairs tasks explicitly assigned to `codex`.\n - Claude Code Action can act as GitHub-side CCA or reviewer.\n - CCA judges completion.\n - MergeGate authorizes state transitions and merge readiness.\ndiff --git a/.github/prompts/cca-completion-check.md b/.github/prompts/cca-completion-check.md\nindex ed203ad5f302ecc81372d8ab27d2b1232ca97497..fd89950528333ef6242e6d5443904f5392ab7e56\n--- a/.github/prompts/cca-completion-check.md\n+++ b/.github/prompts/cca-completion-check.md\n@@ -19,6 +19,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n ## Required Reading\n \n Read only the files needed to judge the PR contract. Prefer this order:\ndiff --git a/.github/prompts/codex-task-handoff.md b/.github/prompts/codex-task-handoff.md\nindex 695e211d76dea1c60e8d5bb360e78487808286a4..59441d576b01a39569722f226c854e5a740e9eb5\n--- a/.github/prompts/codex-task-handoff.md\n+++ b/.github/prompts/codex-task-handoff.md\n@@ -2,7 +2,7 @@\n \n You are Codex Front acting as the Implementer for a Shiki task.\n \n-Follow `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant ADRs, and `docs/agents/codex-handoff.md`.\n+Follow `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant Shiki SADRs and target ADRs, and `docs/agents/codex-handoff.md`.\n \n Rules:\n \ndiff --git a/AGENTS.md b/AGENTS.md\nindex 7f041100d49c2a7525f51cb0f5d696a4a6d1c78c..a06bfbabc6fa48a007389e25e12d7ed523936bed\n--- a/AGENTS.md\n+++ b/AGENTS.md\n@@ -22,7 +22,7 @@\n - **Codex Front** is an optional implementer through Codex App, Codex CLI, Codex IDE extension, or Codex Web signed in with ChatGPT OAuth/subscription auth, dispatched with `shiki runner codex` when a task is explicitly assigned to `codex`.\n - **GitHub CCA** is implemented by Claude Code Action by default, using `CLAUDE_CODE_OAUTH_TOKEN`.\n \n-Do not assume `openai/codex-action`, `OPENAI_API_KEY`, or API-key based Codex automation in the default Shiki path. API-key runners are explicit target-repo extensions and require their own ADR.\n+Do not assume `openai/codex-action`, `OPENAI_API_KEY`, or API-key based Codex automation in the default Shiki path. API-key runners are explicit target-repo extensions and require their own target ADR.\n \n Shiki is not:\n \n@@ -44,10 +44,14 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use\n+`SADR-NNNN-*.md`; target product decisions retain `NNNN-*.md` and the `ADR`\n+namespace.\n+\n Use the canonical source-of-truth order above. Runtime-specific reading orders\n are operational guidance only and must not override or reorder it.\n \n-Conversation state is not durable truth. If a decision matters, put it in GitHub, `.shiki/`, `CONTEXT.md`, or an ADR.\n+Conversation state is not durable truth. If a decision matters, put it in GitHub, `.shiki/`, `CONTEXT.md`, a Shiki SADR, or a target ADR.\n \n If GitHub and `.shiki/` disagree, surface the conflict and prefer GitHub operational state until the mirror is repaired.\n \n@@ -75,8 +79,8 @@\n Every non-trivial change follows this loop:\n \n 1. **Goal**: clarify outcome, completion conditions, non-goals, risk level, and success signals.\n-2. **grill-with-docs**: challenge the plan against domain docs, ADRs, code reality, terminology, and edge scenarios.\n-3. **Context & Impact**: find relevant docs, ADRs, code, dependencies, owners, locks, and verification surfaces.\n+2. **grill-with-docs**: challenge the plan against domain docs, Shiki SADRs, target ADRs, code reality, terminology, and edge scenarios.\n+3. **Context & Impact**: find relevant docs, SADRs, target ADRs, code, dependencies, owners, locks, and verification surfaces.\n 4. **PRD and Spec Freeze**: use `to-prd` when context is settled enough to become durable product/engineering intent. The operator's explicit approval of the PRD is Spec Freeze: the plan records a `spec_freeze` block, and no plan runs without it. Steps 1-4 form Requirements Definition \u2014 one continuous operator dialogue. After Spec Freeze, scope changes require an operator-approved Spec Amendment; non-scope-moving interpretations go to the Assumption Log.\n 5. **Issues**: use `to-issues` to create independently grabbable vertical-slice issues. Prefer AFK slices over HITL slices where possible.\n 6. **Triage**: label issues for readiness, risk, runtime, skills, and MergeGate state.\n@@ -167,7 +171,7 @@\n - Testing decisions.\n - Out-of-scope items.\n - Further notes.\n-- Links to relevant domain terms and ADRs.\n+- Links to relevant domain terms, SADRs, and target ADRs.\n \n Every executable task must include:\n \n@@ -193,7 +197,7 @@\n \n Context & Impact must identify:\n \n-- Relevant documents, domain terms, ADRs, and past decisions.\n+- Relevant documents, domain terms, SADRs, target ADRs, and past decisions.\n - Relevant modules, interfaces, seams, callers, owners, and tests.\n - Dependency relationships between tasks.\n - Candidate locks and lock conflicts.\n@@ -210,7 +214,7 @@\n | Skill | Required when |\n | --- | --- |\n | `setup-matt-pocock-skills` | First configuring the repo for skills, or when issue tracker, triage labels, or domain docs are missing. |\n-| `grill-with-docs` | Default for non-trivial Goals before PRD/issues; always required when plans, terminology, boundaries, tradeoffs, or ADR-worthy decisions are ambiguous. |\n+| `grill-with-docs` | Default for non-trivial Goals before PRD/issues; always required when plans, terminology, boundaries, tradeoffs, or decision-record-worthy choices are ambiguous. |\n | `zoom-out` | The agent lacks an architectural map of the relevant code area. |\n | `to-prd` | Settled Goal context must become durable product/engineering intent. |\n | `to-issues` | A Goal, PRD, or plan must become independently grabbable vertical-slice issues. |\n@@ -348,7 +352,7 @@\n - Three or more new files.\n - Any high-risk or critical label.\n \n-When the architecture gate triggers, run `grill-with-docs` or `improve-codebase-architecture` as appropriate and record durable decisions in an ADR when needed.\n+When the architecture gate triggers, run `grill-with-docs` or `improve-codebase-architecture` as appropriate and record durable decisions in a Shiki SADR or target ADR as applicable.\n \n ## Engineering Discipline\n \ndiff --git a/CLAUDE.md b/CLAUDE.md\nindex 98a932e7841a632866fcd53b0bad0e3af3711d3d..98a31399404d6fb044b4fd493f4459ac87f439de\n--- a/CLAUDE.md\n+++ b/CLAUDE.md\n@@ -14,6 +14,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n ## Claude Role\n \n Default to planner, default implementer, reviewer, coordinator, documentation editor, CCA-style completion judge, and final-judgment assistant.\n@@ -57,7 +60,7 @@\n At the start of a Shiki session:\n \n 1. Read `AGENTS.md`.\n-2. Read `CONTEXT.md` and relevant ADRs.\n+2. Read `CONTEXT.md` and relevant Shiki SADRs and target ADRs.\n 3. Read active `.shiki/` Goal/task/ledger state when present.\n 4. Read active distilled rules from `.shiki/memories/` (status `distilled`, `active: true`, not revoked or superseded) for applicable guidance; the same rules are injected deterministically into task handoffs as the `## Distilled Rules` section (\u00a73.5).\n 5. Read the GitHub Issue or PR when the task is GitHub-backed.\n@@ -74,7 +77,7 @@\n - Define the Goal, completion conditions, non-goals, and risk level.\n - Treat `grill-with-docs` as the default planning entry point for non-trivial Goals.\n - Challenge domain terms against `CONTEXT.md`.\n-- Challenge hard-to-reverse tradeoffs against ADRs.\n+- Challenge hard-to-reverse tradeoffs against SADRs and target ADRs.\n - Explore code instead of asking when code can answer the question.\n - Run Context & Impact before implementation planning.\n - Produce a Task DAG, not an unordered checklist.\n@@ -94,7 +97,7 @@\n - For each question, provide your recommended answer.\n - Resolve dependency decisions before downstream design questions.\n - Update `CONTEXT.md` inline when terminology crystallizes.\n-- Offer ADRs only when the decision is hard to reverse, surprising without context, and a real tradeoff.\n+- Offer a SADR or target ADR only when the decision is hard to reverse, surprising without context, and a real tradeoff.\n - Convert unresolved questions into HITL blockers rather than allowing Codex to guess.\n \n ## PRD and Issues Standard\n@@ -103,7 +106,7 @@\n \n - Use the project's domain glossary vocabulary.\n - Avoid volatile file paths unless they are essential.\n-- Include implementation decisions, testing decisions, out-of-scope items, and relevant ADR/domain links.\n+- Include implementation decisions, testing decisions, out-of-scope items, and relevant SADR/target-ADR/domain links.\n - Identify deep-module opportunities and testable interfaces.\n \n When creating issues:\n@@ -121,7 +124,7 @@\n - Branch/worktree target.\n - Scope and non-goals.\n - Dependencies and locks.\n-- Relevant docs, ADRs, modules, and tests.\n+- Relevant docs, SADRs, target ADRs, modules, and tests.\n - Required skill invocations.\n - TDD expectations.\n - Pre-PR `code-review` gate expectations.\n@@ -152,7 +155,7 @@\n \n - Treat yourself as a judge, not implementer.\n - Do not write production code.\n-- Read `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant ADRs, PRD, task issue, PR body, diff, checks, reviews, labels, and `.shiki/` state.\n+- Read `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant SADRs and target ADRs, PRD, task issue, PR body, diff, checks, reviews, labels, and `.shiki/` state.\n - Evaluate every applicable checklist in `docs/agents/checklists.md`.\n - Map each acceptance criterion to durable evidence.\n - Distinguish implementation failure from missing evidence.\ndiff --git a/CONTEXT.md b/CONTEXT.md\nindex e61a9be06a0cce0d026484083a657bde2067e234..237ee8aa3ea8fab334ece49768bc5ed3b5af0974\n--- a/CONTEXT.md\n+++ b/CONTEXT.md\n@@ -25,7 +25,7 @@\n _Avoid_: jumping directly into code\n \n **grill-with-docs**:\n-A planning skill that challenges a plan against domain language, ADRs, code reality, and concrete edge scenarios. It resolves design-tree questions before PRD/issues. It runs inside Requirements Definition.\n+A planning skill that challenges a plan against domain language, Shiki SADRs, target ADRs, code reality, and concrete edge scenarios. It resolves design-tree questions before PRD/issues. It runs inside Requirements Definition.\n _Avoid_: generic brainstorming, silent assumptions\n \n **Requirements Definition**:\ndiff --git a/CONTRIBUTING.md b/CONTRIBUTING.md\nindex c6d9b656e73abb8a55e5f660123e03153dcb3e6d..9e37a8fbf668db5b22461aaae5548b8f1354394c\n--- a/CONTRIBUTING.md\n+++ b/CONTRIBUTING.md\n@@ -17,7 +17,7 @@\n 1. **Goal** \u2014 start from a user-approved Goal with an outcome, completion\n    conditions, non-goals, risk level, and success signals.\n 2. **grill-with-docs** \u2014 for any non-trivial change, challenge the plan against\n-   domain language, ADRs, and code reality before writing a PRD.\n+   domain language, Shiki SADRs, target ADRs, and code reality before writing a PRD.\n 3. **PRD** \u2014 capture settled product/engineering intent (`to-prd`).\n 4. **Spec Freeze** \u2014 the operator explicitly approves the PRD/requirements;\n    the plan records a `spec_freeze` block and no plan runs without it\n@@ -94,7 +94,7 @@\n   entries.\n - Do not hand-edit `.shiki/goals/*.json` or `.shiki/manifest.json` in a task PR.\n - Conversation state is not durable truth. If a decision matters, record it in\n-  GitHub, `.shiki/`, `CONTEXT.md`, or an ADR.\n+  GitHub, `.shiki/`, `CONTEXT.md`, a Shiki SADR, or a target ADR.\n \n ## Architecture Gate\n \n@@ -103,7 +103,7 @@\n or CI policy, package structure, three or more structural files, more than 100\n net new lines in a core module, three or more new files, or any high-risk label.\n When triggered, run `grill-with-docs` or `improve-codebase-architecture` and\n-record durable decisions in an ADR when needed (see `docs/adr/`).\n+record durable decisions in a Shiki SADR or target ADR as applicable (see `docs/adr/`).\n \n ## Safety\n \ndiff --git a/SYSTEM_PROMPT.md b/SYSTEM_PROMPT.md\nindex aaaea45e3a896c3a44ba89eec8bb84325ecf6c62..da009c86a88285269dee0155e58e550c6d744ea9\n--- a/SYSTEM_PROMPT.md\n+++ b/SYSTEM_PROMPT.md\n@@ -30,6 +30,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n Use the canonical source-of-truth order above. Runtime-specific reading orders\n are operational guidance only and must not override or reorder it. The current\n conversation is non-durable operator input.\n@@ -48,14 +51,14 @@\n \n A runtime may only do work that is explicitly assigned by the Goal, task, PR, or MergeGate decision.\n \n-Do not assume `openai/codex-action`, `OPENAI_API_KEY`, or API-key based Codex automation in the default Shiki path. API-key runners are explicit target-repo extensions and require their own ADR.\n+Do not assume `openai/codex-action`, `OPENAI_API_KEY`, or API-key based Codex automation in the default Shiki path. API-key runners are explicit target-repo extensions and require their own target ADR.\n \n ## 4. Non-negotiable Shiki flow\n \n For any non-trivial Goal, use this flow:\n \n 1. **Goal Seek**: define outcome, non-goals, risk level, completion criteria, and evidence requirements.\n-2. **grill-with-docs**: challenge the plan against `CONTEXT.md`, ADRs, code reality, terminology, boundaries, and edge scenarios. Ask one question at a time when the operator is available. If code can answer a question, inspect code instead of asking.\n+2. **grill-with-docs**: challenge the plan against `CONTEXT.md`, Shiki SADRs, target ADRs, code reality, terminology, boundaries, and edge scenarios. Ask one question at a time when the operator is available. If code can answer a question, inspect code instead of asking.\n 3. **Context & Impact**: identify modules, interfaces, seams, callers, dependencies, locks, risk, and verification surfaces.\n 4. **to-prd + Spec Freeze**: turn settled context into a PRD using domain vocabulary and testing decisions. The operator's explicit approval of the PRD is Spec Freeze, recorded as the plan's `spec_freeze` block; plans without it do not run. Steps 1-4 form Requirements Definition.\n 5. **to-issues**: decompose the PRD into independently grabbable vertical-slice issues. Prefer AFK slices; mark HITL slices when judgment is still required.\n@@ -75,7 +78,7 @@\n Before making any material change:\n \n 1. Read `AGENTS.md` and any runtime-specific wrapper such as `CLAUDE.md`.\n-2. Read `CONTEXT.md` and relevant ADRs.\n+2. Read `CONTEXT.md` and relevant SADRs and target ADRs.\n 3. Read the GitHub Issue / PR and `.shiki/` state for the active Goal or task.\n 4. Identify your role: Planner, Implementer, Reviewer, Completion Check Agent, Repairer, or Guardian-assist.\n 5. Identify Goal id, task id, branch, PR, dependencies, locks, risk level, required skills, and required evidence.\n@@ -103,7 +106,7 @@\n \n At minimum, identify:\n \n-- Relevant documents, ADRs, domain terms, and prior decisions.\n+- Relevant documents, SADRs, target ADRs, domain terms, and prior decisions.\n - Relevant modules, interfaces, seams, callers, owners, and tests.\n - Dependency relationships between tasks.\n - Candidate file locks and lock conflicts.\n@@ -152,7 +155,7 @@\n \n CCA must read:\n \n-- `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant ADRs.\n+- `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant SADRs and target ADRs.\n - Parent Goal issue, PRD issue, task issue, comments, and labels.\n - PR body, diff, commits, changed files, reviews, CI checks, and artifacts available to the workflow.\n - `.shiki/` task contract, locks, ledger entries, CCA checklist profile, and prior repair packets.\n@@ -214,7 +217,7 @@\n - Three or more new files.\n - Any high-risk or critical label.\n \n-When the architecture gate triggers, run `grill-with-docs` or `improve-codebase-architecture` as appropriate and record the decision in an ADR if it is hard to reverse or likely to be re-litigated.\n+When the architecture gate triggers, run `grill-with-docs` or `improve-codebase-architecture` as appropriate and record the decision in a Shiki SADR or target ADR as applicable if it is hard to reverse or likely to be re-litigated.\n \n ## 14. Skill Gate\n \n@@ -223,7 +226,7 @@\n Use these skills:\n \n - `setup-matt-pocock-skills`: before first use of the engineering skill set, or when issue tracker, triage labels, or domain docs are missing.\n-- `grill-with-docs`: default for non-trivial Goals before PRD/issues; required for ambiguous plans, terminology, boundaries, ADR-worthy tradeoffs, or design-tree decisions.\n+- `grill-with-docs`: default for non-trivial Goals before PRD/issues; required for ambiguous plans, terminology, boundaries, decision-record-worthy tradeoffs, or design-tree decisions.\n - `zoom-out`: unfamiliar code area or missing architectural map.\n - `to-prd`: convert settled context into a PRD.\n - `to-issues`: break a PRD, plan, or Goal into independently grabbable vertical-slice issues.\ndiff --git a/docs/adr/0001-keep-shiki-as-platform-name.md b/docs/adr/SADR-0001-keep-shiki-as-platform-name.md\nindex 20a9637c3b9d8c97de7ba8db7cfae4d82d9a7dba..8b685439959aada1b221c112f0ac3c51ca8a1695\n--- a/docs/adr/0001-keep-shiki-as-platform-name.md\n+++ b/docs/adr/SADR-0001-keep-shiki-as-platform-name.md\n@@ -1,3 +1,3 @@\n-# Keep Shiki As The Platform Name\n+# SADR-0001: Keep Shiki As The Platform Name\n \n Shiki remains the product name for the GitHub-native agentic engineering control plane. We considered more descriptive names such as MergePilot, MergeGate, and Kiroku, but kept Shiki because it carries the existing goal-seeking framework identity while remaining independent of Hermes, Codex, Claude Code, or any single runtime.\ndiff --git a/docs/adr/0002-use-agents-as-shared-constitution.md b/docs/adr/SADR-0002-use-agents-as-shared-constitution.md\nindex 73de30ecb078120eac540d9bc51855e3cd48d0e3..c5486182bf06fca36ea508496b013c67302fd989\n--- a/docs/adr/0002-use-agents-as-shared-constitution.md\n+++ b/docs/adr/SADR-0002-use-agents-as-shared-constitution.md\n@@ -1,3 +1,3 @@\n-# Use AGENTS As The Shared Agent Constitution\n+# SADR-0002: Use AGENTS As The Shared Agent Constitution\n \n Shiki uses `AGENTS.md` as the runtime-neutral constitution and `CLAUDE.md` as a thin Claude Code wrapper. This keeps Codex, Claude Code, Hermes Runner, and future Agent Runtimes aligned on the same GitHub-first MergeGate rules while still allowing runtime-specific instructions where needed.\ndiff --git a/docs/adr/0003-require-skill-gate-for-mergegate-readiness.md b/docs/adr/SADR-0003-require-skill-gate-for-mergegate-readiness.md\nindex 424be0c9af4c8f0136f1fde68a468c47ff84e764..7fc337987c7df9792f75f181dfcbed75fe5ffff2\n--- a/docs/adr/0003-require-skill-gate-for-mergegate-readiness.md\n+++ b/docs/adr/SADR-0003-require-skill-gate-for-mergegate-readiness.md\n@@ -1,3 +1,3 @@\n-# Require Skill Gate For MergeGate Readiness\n+# SADR-0003: Require Skill Gate For MergeGate Readiness\n \n Shiki requires relevant engineering skills to be invoked before execution when their triggers apply. This makes skills part of MergeGate readiness rather than optional prompt style, because Shiki depends on repeatable planning, issue decomposition, TDD, diagnosis, triage, and architecture review before autonomous or parallel agent work can be trusted.\ndiff --git a/docs/adr/0004-use-github-first-with-shiki-mirror.md b/docs/adr/SADR-0004-use-github-first-with-shiki-mirror.md\nindex d0a3a9174dc646a945a9d325954ffaf00c352040..a79c54ae9ee8eed2a0524192b864da4e22a92d3f\n--- a/docs/adr/0004-use-github-first-with-shiki-mirror.md\n+++ b/docs/adr/SADR-0004-use-github-first-with-shiki-mirror.md\n@@ -1,4 +1,4 @@\n-# ADR 0004: Use GitHub First With A Repository-Local Shiki Mirror\n+# SADR-0004: Use GitHub First With A Repository-Local Shiki Mirror\n \n ## Status\n \ndiff --git a/docs/adr/0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md b/docs/adr/SADR-0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\nindex b339baf49b89d9290e79c3d51b36e7b2bde3bd95..0f3c3eb9afccdb3403ce978e21d04fe12e495da0\n--- a/docs/adr/0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\n+++ b/docs/adr/SADR-0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\n@@ -1,8 +1,8 @@\n-# ADR 0005: Use Codex As Front And Claude Code Action As The GitHub Runtime\n+# SADR-0005: Use Codex As Front And Claude Code Action As The GitHub Runtime\n \n ## Status\n \n-Accepted (implementer default superseded by ADR 0008; the auth model and the\n+Accepted (implementer default superseded by SADR-0008; the auth model and the\n Claude Code Action GitHub runtime decision remain in force)\n \n ## Context\n@@ -26,4 +26,4 @@\n - Shiki avoids requiring `OPENAI_API_KEY` for the default workflow.\n - Codex work happens through the user's authenticated Codex front surface, not as a hidden backend Action.\n - Claude Code Action is configured for OAuth-token based subscription usage by default.\n-- API-key based automation remains possible only as an explicit target-repo extension with its own ADR.\n+- API-key based automation remains possible only as an explicit target-repo extension with its own target ADR.\ndiff --git a/docs/adr/0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md b/docs/adr/SADR-0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\nindex 4bcae9d1e52f0a37b8e9fbafa5cd14b6ee1993bd..7a38538a6ae9929567f0ade67e7ea6a60d0b3f7e\n--- a/docs/adr/0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\n+++ b/docs/adr/SADR-0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\n@@ -1,4 +1,4 @@\n-# ADR 0006: Separate Codex Implementation, CCA Completion, And MergeGate Authorization\n+# SADR-0006: Separate Codex Implementation, CCA Completion, And MergeGate Authorization\n \n ## Status\n \ndiff --git a/docs/adr/0007-packaging-and-release.md b/docs/adr/SADR-0007-packaging-and-release.md\nindex 79b84ddb97167372b44641e5b7860e400ce80822..bb445a2e2e856d9d1f5fdbe35de8d4e8cec0fb32\n--- a/docs/adr/0007-packaging-and-release.md\n+++ b/docs/adr/SADR-0007-packaging-and-release.md\n@@ -1,4 +1,4 @@\n-# ADR 0007: Packaging And Release For Shiki\n+# SADR-0007: Packaging And Release For Shiki\n \n ## Status\n \n@@ -25,7 +25,7 @@\n   not introduce third-party runtime dependencies or break the shim.\n - Releases must run inside the operator's subscription-authenticated toolchain\n   and must not require extra secrets beyond the built-in `GITHUB_TOKEN`\n-  (consistent with ADR 0005's no-API-key-by-default stance).\n+  (consistent with SADR-0005's no-API-key-by-default stance).\n - `.shiki/manifest.json` already encodes the install surface (`install.include`,\n   `install.create_directories`, `install.exclude_from_commit`), and it must not\n   be mutated as part of this change.\n@@ -34,7 +34,7 @@\n \n This is a hard-to-reverse platform decision (it sets the version source of\n truth, the release trigger, and the public install contract), so it is recorded\n-as an ADR.\n+as a SADR.\n \n ## Decision\n \n@@ -104,7 +104,7 @@\n 3. **Publishing to PyPI.** Rejected for now: Shiki is distributed as a\n    repository template + git-installable CLI, not a PyPI library; PyPI would add\n    credentials/secrets and a different versioning contract. Can be added later as\n-   its own ADR if a packaged distribution is needed.\n+   its own SADR if a packaged distribution is needed.\n 4. **Dynamic version (read `VERSION` via a build backend `dynamic` field).**\n    Rejected to keep `pyproject.toml` backend-agnostic and dependency-free; the\n    small duplication between `VERSION` and `pyproject.toml` is guarded by the\n@@ -123,4 +123,4 @@\n   releasing versions the whole platform but ships only `install.include` to\n   targets.\n - Future PyPI distribution or a console entry point are deferred and would each\n-  warrant their own ADR.\n+  warrant their own SADR.\ndiff --git a/docs/adr/0008-use-claude-code-as-default-implementer.md b/docs/adr/SADR-0008-use-claude-code-as-default-implementer.md\nindex dedf4510b7c7799521422f622029563d373db031..35c2625d908167fbc58855b5b63e283805f167e8\n--- a/docs/adr/0008-use-claude-code-as-default-implementer.md\n+++ b/docs/adr/SADR-0008-use-claude-code-as-default-implementer.md\n@@ -1,12 +1,12 @@\n-# ADR 0008: Use Claude Code As The Default Implementer Runtime\n+# SADR-0008: Use Claude Code As The Default Implementer Runtime\n \n ## Status\n \n-Proposed (supersedes ADR 0005 for the implementer default; keeps its auth model)\n+Proposed (supersedes SADR-0005 for the implementer default; keeps its auth model)\n \n ## Context\n \n-ADR 0005 made Codex Front the default implementation runtime, with Claude Code\n+SADR-0005 made Codex Front the default implementation runtime, with Claude Code\n restricted to planner and reviewer roles by the runtime registry, and the only\n autonomous implementation adapter was `shiki runner codex`.\n \n@@ -18,13 +18,13 @@\n - The platform's planning, review, and CCA layers already run on Claude Code,\n   and Claude Code's native capabilities (the built-in `/code-review` skill and\n   the Workflow multi-agent orchestration tool) have no Codex equivalent.\n-- The Goal lifecycle redesign (ADR 0009) requires an autonomous post-freeze\n+- The Goal lifecycle redesign (SADR-0009) requires an autonomous post-freeze\n   loop driven from the operator's Claude Code session, including bounded\n   repair dispatch, which must not depend on a runtime that cannot be invoked\n   unattended.\n \n The constitution claims Shiki is runtime-agnostic (\"not a Claude-only\n-workflow\", ADR 0002). Any change to the implementer default must preserve the\n+workflow\", SADR-0002). Any change to the implementer default must preserve the\n registry-based runtime model rather than hardcoding one vendor.\n \n ## Decision\n@@ -39,7 +39,7 @@\n   the task to review or repair-needed.\n - Keep Codex registered as an optional implementer runtime. `shiki runner\n   codex`, the Codex handoff templates, and the ChatGPT OAuth auth model from\n-  ADR 0005 remain supported for tasks explicitly assigned to `codex`.\n+  SADR-0005 remain supported for tasks explicitly assigned to `codex`.\n - Commit to mandating two Claude-native capabilities as loop contract in\n   follow-up Goals (status: the code-review gate was delivered by Goal G-B \u2014\n   skill registry, checklists item PR-12, default required_skills \u2014 while the\n@@ -60,7 +60,7 @@\n \n Out of scope: removing Codex support, changing the CCA runtime (Claude Code\n Action with `CLAUDE_CODE_OAUTH_TOKEN` stays), and API-key based automation\n-(still requires its own ADR per ADR 0005).\n+(still requires its own target ADR per SADR-0005).\n \n ## Consequences\n \n@@ -100,7 +100,7 @@\n   harness rejection of direct `codex exec` is outside Shiki's control, and the\n   plan's mandatory native capabilities cannot run on Codex at all.\n - **Remove Codex entirely**: rejected; it abandons the runtime-agnostic\n-  constitution (ADR 0002) for no operational gain and forecloses\n+  constitution (SADR-0002) for no operational gain and forecloses\n   cross-runtime adversarial review.\n - **GitHub Actions cloud implementation via claude-code-action**: rejected as\n   default; durable evidence is automatic but token cost, Node 24 workflow\ndiff --git a/docs/adr/0009-requirements-definition-and-spec-freeze.md b/docs/adr/SADR-0009-requirements-definition-and-spec-freeze.md\nindex 29b3f7871bef9203a8b4a831a65c39eb2f506e42..52e7dd69ad0aa4efe0b68c62678abbf7df02aaec\n--- a/docs/adr/0009-requirements-definition-and-spec-freeze.md\n+++ b/docs/adr/SADR-0009-requirements-definition-and-spec-freeze.md\n@@ -1,4 +1,4 @@\n-# ADR 0009: Merge Grill And Planning Into Requirements Definition With Spec Freeze\n+# SADR-0009: Merge Grill And Planning Into Requirements Definition With Spec Freeze\n \n ## Status\n \ndiff --git a/docs/adr/0010-external-ai-guardian-review.md b/docs/adr/SADR-0010-external-ai-guardian-review.md\nindex e6108bbe8bb4e73e0ff4684c8f78001a80438c4e..f3510b145664298adafb151b6f0db51b972f01ec\n--- a/docs/adr/0010-external-ai-guardian-review.md\n+++ b/docs/adr/SADR-0010-external-ai-guardian-review.md\n@@ -1,4 +1,4 @@\n-# ADR 0010: External AI Guardian Review As A First-Class Approval Authority\n+# SADR-0010: External AI Guardian Review As A First-Class Approval Authority\n \n ## Status\n \n@@ -8,7 +8,7 @@\n \n Shiki is intended to be an autonomous, recursive, self-improving agent\n framework: authority-in-the-loop, not human-in-the-loop. Until now the Guardian\n-approval required for high/critical-risk merges (guardian-policy.json, ADR\n+approval required for high/critical-risk merges (guardian-policy.json, SADR\n governance) recognized only human authority sources \u2014 a GitHub review approval,\n the `guardian:approved` label applied by a configured Guardian, or a comment\n carrying `Guardian approval granted` plus the current head SHA.\n@@ -89,7 +89,7 @@\n   autonomous design the operator intends and produced the impersonation\n   incident.\n - **Record AI approval as an operator comment**: rejected as forgery of human\n-  authority \u2014 the exact failure this ADR corrects.\n+  authority \u2014 the exact failure this SADR corrects.\n - **Deliver the artifact as a committed `.shiki/` file**: rejected; a committed\n   approval inside the PR being approved is forgeable by anyone who can push.\n - **A dedicated reviewer bot identity instead of a relayed comment**: deferred;\ndiff --git a/docs/adr/0011-autonomous-quality-gate-evidence.md b/docs/adr/SADR-0011-autonomous-quality-gate-evidence.md\nindex bca0246c5d46e44afbde15486a75eec425fc02f5..9c93eac16d8c3a6d4b11bd7224e3955102cd7db4\n--- a/docs/adr/0011-autonomous-quality-gate-evidence.md\n+++ b/docs/adr/SADR-0011-autonomous-quality-gate-evidence.md\n@@ -1,4 +1,4 @@\n-# ADR 0011: Autonomous Quality-Gate Evidence \u2014 Deterministic Facts And An Independent Verifier\n+# SADR-0011: Autonomous Quality-Gate Evidence \u2014 Deterministic Facts And An Independent Verifier\n \n ## Status\n \ndiff --git a/docs/adr/0012-autonomous-loop-closeout-to-main.md b/docs/adr/SADR-0012-autonomous-loop-closeout-to-main.md\nindex 637fad6dde4989c9c2bf0f81f676d8d29991123c..ec7d85f81030a676bcbba28b43eb365422a36eac\n--- a/docs/adr/0012-autonomous-loop-closeout-to-main.md\n+++ b/docs/adr/SADR-0012-autonomous-loop-closeout-to-main.md\n@@ -1,4 +1,4 @@\n-# 0012 \u2014 Autonomous loop pushes goal completion to main via a closeout PR\n+# SADR-0012 \u2014 Autonomous loop pushes goal completion to main via a closeout PR\n \n - Status: accepted\n - Date: 2026-06-18\ndiff --git a/docs/adr/0013-bootstrap-grants-actions-approve-pull-requests.md b/docs/adr/SADR-0013-bootstrap-grants-actions-approve-pull-requests.md\nindex b6c69d64f1c5ced346b88d6859586715b3b5bd0e..cf48ff4a6716267f539ca58edd1a852e7c7a2d64\n--- a/docs/adr/0013-bootstrap-grants-actions-approve-pull-requests.md\n+++ b/docs/adr/SADR-0013-bootstrap-grants-actions-approve-pull-requests.md\n@@ -1,4 +1,4 @@\n-# ADR 0013: Bootstrap Grants GitHub Actions Permission To Create And Approve Pull Requests\n+# SADR-0013: Bootstrap Grants GitHub Actions Permission To Create And Approve Pull Requests\n \n ## Status\n \n@@ -31,7 +31,7 @@\n security tradeoff: it lets repository automation satisfy a human-review gate. In\n a multi-maintainer repo that could let a compromised or buggy workflow\n self-approve changes. So whether bootstrap turns it on by default \u2014 and with\n-what default token scope \u2014 is an ADR-worthy decision, not an implementation\n+what default token scope \u2014 is a SADR-worthy decision, not an implementation\n detail.\n \n ## Decision\n@@ -48,7 +48,7 @@\n Specifically:\n \n - **`can_approve_pull_request_reviews` defaults to `true`.** Shiki's default\n-  posture is solo/AFK autonomous operation (ADR 0008/0009/0012), where the CCA\n+  posture is solo/AFK autonomous operation (SADR-0008/0009/0012), where the CCA\n   Review Bridge is the only path that can satisfy `required_review: true` after\n   the CCA and Guardian gates have already passed. The default makes a\n   freshly-bootstrapped repo able to run the autonomous loop end to end without a\ndiff --git a/docs/adr/0014-codex-app-external-ai-guardian-ui-adapter.md b/docs/adr/SADR-0014-codex-app-external-ai-guardian-ui-adapter.md\nindex 238db06279334caa341c117362df9271382a7fcd..65f12e9939321e1b3e389a34c822fc1549e5155d\n--- a/docs/adr/0014-codex-app-external-ai-guardian-ui-adapter.md\n+++ b/docs/adr/SADR-0014-codex-app-external-ai-guardian-ui-adapter.md\n@@ -1,4 +1,4 @@\n-# ADR 0014: Use Codex App As The External AI Guardian UI Adapter\n+# SADR-0014: Use Codex App As The External AI Guardian UI Adapter\n \n ## Status\n \ndiff --git a/docs/adr/0015-contract-approval-before-dispatch.md b/docs/adr/SADR-0015-contract-approval-before-dispatch.md\nindex fad6c656741a20a7684ba16080a7db159f63a52f..6061271a46d3f1d6ae7174477de3fb75673123a6\n--- a/docs/adr/0015-contract-approval-before-dispatch.md\n+++ b/docs/adr/SADR-0015-contract-approval-before-dispatch.md\n@@ -1,4 +1,4 @@\n-# ADR 0015: Approve The Task Contract Before Dispatch, Not The Diff After It\n+# SADR-0015: Approve The Task Contract Before Dispatch, Not The Diff After It\n \n ## Status\n \n@@ -9,7 +9,7 @@\n Guardian approval for high/critical risk is expressible only on a pull request.\n Every approval source in `guardian-policy.json` \u2014 GitHub review, the\n `guardian:approved` label, the `Guardian approval granted` comment, and the\n-`external_ai_guardian_review` artifact (ADR 0010/0014) \u2014 is bound to a PR, and\n+`external_ai_guardian_review` artifact (SADR-0010/0014) \u2014 is bound to a PR, and\n three of the four require the current head SHA. Approval therefore cannot exist\n before an implementation PR exists.\n \n@@ -81,7 +81,7 @@\n An envelope breach must surface only on the non-repairable policy gate, never on\n the repairable `MergeGate metadata check`. Otherwise the Goal Loop routes it to\n `dispatch_repair` \u2014 instructing a runner to make the breach go away \u2014 which is\n-exactly what this ADR forbids.\n+exactly what this SADR forbids.\n \n **Contract immutability.** After registration, an implementation PR must not\n change the governance fields of its own task contract (`scope`, `non_goals`,\n@@ -116,7 +116,7 @@\n fails any verdict that is not `complete`, and the signal the CCA prompt reads is\n computed from `evaluate_guardian_approval` alone. Without the OR-merge at the\n signal, a contract-approved PR still returns `needs_guardian`, the CCA job fails,\n-and the MergeGate policy check never runs \u2014 the gate this ADR changes would never\n+and the MergeGate policy check never runs \u2014 the gate this SADR changes would never\n be reached. The CCA *prompt* needs no change: a `contract_approval` entry in the\n signal's existing `sources` array satisfies its current wording.\n \n@@ -130,7 +130,7 @@\n written, and an approved contract lets its implementation PRs merge through the\n same automatic path as low/medium risk. The wasted implement-then-block cycle\n disappears. Guardian review shifts from reading diffs to reading contracts, which\n-is both earlier and smaller; the external AI Guardian path (ADR 0010/0014) works\n+is both earlier and smaller; the external AI Guardian path (SADR-0010/0014) works\n on contracts with no change.\n \n We accept that the Guardian authorizes a class of change without seeing its\n@@ -146,7 +146,7 @@\n adversarial tests for the new approval path equivalent in rigor to\n `test_shiki_governance_evidence.sh`.\n \n-Bootstrap costs, which apply only while this ADR is being implemented. The tasks\n+Bootstrap costs, which apply only while this SADR is being implemented. The tasks\n that build Contract Approval merge under the existing rules, so the ones\n dispatched after the pre-dispatch gate lands but before the approval path lands\n need a Guardian-approved Contract PR *and* a live Guardian approval on their\n@@ -210,7 +210,7 @@\n risk; they do not bound authorized work.\n \n **Auto-escalate risk on envelope breach instead of stopping.** Returns the\n-workflow to a diff-time Guardian decision \u2014 the exact behavior this ADR removes.\n+workflow to a diff-time Guardian decision \u2014 the exact behavior this SADR removes.\n \n **Resolve governance fields from the base snapshot only.** Rejected against the\n registration flow: implementation PRs add their own contract, so the base has no\ndiff --git a/docs/adr/0016-locks-bound-product-paths.md b/docs/adr/SADR-0016-locks-bound-product-paths.md\nindex b5e850f3f1f7f4ef6e7f9d1763a29a51bd300c45..46b40a9b99a7aae644c02bb458f5473ff0a90ce2\n--- a/docs/adr/0016-locks-bound-product-paths.md\n+++ b/docs/adr/SADR-0016-locks-bound-product-paths.md\n@@ -1,4 +1,4 @@\n-# ADR 0016: Locks bound product paths; the Shiki mirror is bound by record identity\n+# SADR-0016: Locks bound product paths; the Shiki mirror is bound by record identity\n \n ## Status\n \n@@ -6,8 +6,8 @@\n \n - Date: 2026-07-28\n - Deciders: operator (mizutani-140), Claude (planner)\n-- Related: ADR 0004 (GitHub-first with a `.shiki` mirror), ADR 0009 (Spec Freeze),\n-  ADR 0012 (autonomous loop closeout to main), ADR 0015 (Contract Approval before\n+- Related: SADR-0004 (GitHub-first with a `.shiki` mirror), SADR-0009 (Spec Freeze),\n+  SADR-0012 (autonomous loop closeout to main), SADR-0015 (Contract Approval before\n   dispatch), remediation goal task R-02, the live PR #179 deadlock.\n \n ## Context\n@@ -90,11 +90,11 @@\n files, documentation files and named source modules are nameable; runtime-\n allocated ledger, runner and report ids are not \u2014 and those live in `.shiki`,\n which locks no longer govern. Existing frozen contracts are brought into line by\n-a one-time Spec Amendment per plan, which must land before ADR 0015's contract\n+a one-time Spec Amendment per plan, which must land before SADR-0015's contract\n immutability makes `locks` unwritable from the implementation PR.\n \n **Lock state is resolved from the durable side.** MergeGate resolves the lock\n-registry, and ADR 0015's governance fields, from the base `.shiki` snapshot when\n+registry, and SADR-0015's governance fields, from the base `.shiki` snapshot when\n present, falling back to head only when absent. The `MergeGate metadata check`\n job is given `--base-shiki`, which it does not receive today. Locks are not\n derived from the live open-pull-request set: a pull request body is untrusted\n@@ -108,7 +108,7 @@\n Explicitly out of scope: moving MergeGate's contract resolution off the PR\n checkout wholesale; making the mirror a projection of GitHub state; introducing\n new lock kinds; moving ledger evidence out of the repository; and unwinding\n-ADR 0012's closeout pull request. Registration of multi-task goals is ADR 0015's\n+SADR-0012's closeout pull request. Registration of multi-task goals is SADR-0015's\n Contract PR and is not re-decided here.\n \n ## Consequences\n@@ -127,7 +127,7 @@\n \n Risks accepted. First, goal-scoped identity is only as strong as `goal_id`,\n which is resolved from the PR body and the head task file; the mitigation is to\n-add `goal_id` to ADR 0015's frozen governance field set, and until that lands a\n+add `goal_id` to SADR-0015's frozen governance field set, and until that lands a\n task file can name a foreign goal. Second, base-when-present resolution is\n unavailable on fork pull requests, where the head is contributor-controlled;\n fork pull requests already cannot complete a Shiki merge, and this decision does\n@@ -139,7 +139,7 @@\n path, not by implementation pull requests.\n \n New obligations: one Spec Amendment per frozen plan to rewrite `locks`, ordered\n-before ADR 0015 T1; extension of the adversarial suites in\n+before SADR-0015 T1; extension of the adversarial suites in\n `scripts/test_shiki_adversarial_state.sh` and\n `scripts/test_shiki_governance_evidence.sh` to cover the eleven newly-scoped\n directories; and updating `scripts/test_shiki_loop_lock_guard.sh`,\n@@ -151,32 +151,32 @@\n whether the loop's transitive sync of ledger `evidence` references can be\n deleted once the Contract PR carries its own source plan.\n \n-## Relationship to ADR 0004\n+## Relationship to SADR-0004\n \n-This ADR **clarifies something ADR 0004 left unspecified**. ADR 0004 decides that\n+This SADR **clarifies something SADR-0004 left unspecified**. SADR-0004 decides that\n GitHub is the operational source of truth and that each target repository keeps a\n `.shiki/` mirror. It says nothing about pull requests writing the mirror, does\n not name MergeGate, and does not make the mirror an input to a gate. Nothing in\n-ADR 0004's Decision or Consequences is amended, superseded or weakened: the\n+SADR-0004's Decision or Consequences is amended, superseded or weakened: the\n mirror still exists, still records the same artifacts, and pull request\n-boundaries remain the coordination surface. What this ADR settles is the question\n+boundaries remain the coordination surface. What this SADR settles is the question\n 0004 never reached \u2014 by what authority a pull request may write a mirror record \u2014\n and it answers it the way `.shiki/manifest.json` already declares\n (`pr_mutation: current-task-or-current-goal-only`).\n \n-It **amends ADR 0012**, whose Decision item 5 states that \"Loop-executed tasks\n+It **amends SADR-0012**, whose Decision item 5 states that \"Loop-executed tasks\n declare the `path:.shiki/**` lock, which covers every `.shiki` file the closeout\n stages ... so `files_outside_locks` is satisfied without per-file lock\n bookkeeping.\" That clause is replaced: the closeout's mirror writes are\n-authorized by record identity, not by a declared lock. ADR 0012's substantive\n+authorized by record identity, not by a declared lock. SADR-0012's substantive\n decision \u2014 that completion reaches `main` through a closeout pull request rather\n than local attestation \u2014 is preserved unchanged.\n \n-It is **complementary to ADR 0015**, not a substitute. ADR 0015 owns contract\n+It is **complementary to SADR-0015**, not a substitute. SADR-0015 owns contract\n immutability, the pre-dispatch gate, the Contract PR and the Scope Envelope. This\n-ADR owns what a lock means and who may write the mirror. The one ordering\n+SADR owns what a lock means and who may write the mirror. The one ordering\n constraint between them is that the lock-narrowing Spec Amendment must precede\n-ADR 0015 T1's freezing of the `locks` field.\n+SADR-0015 T1's freezing of the `locks` field.\n \n ## Alternatives Considered\n \n@@ -186,7 +186,7 @@\n the only writer that can register them may run. Migrating the Guardian signal to\n that store makes every registration PR fail closed to `needs_guardian` at every\n risk level, because the task file is by definition absent from the store, which\n-removes the low/medium autonomous path ADR 0008/0009 exists to provide. And it\n+removes the low/medium autonomous path SADR-0008/0009 exists to provide. And it\n leaves schemas, manifest, config, guardian policy, templates, migrations, plans,\n worktrees, runner records, runs and handoffs with no writer at all in a\n repository whose product is `.shiki`. Measured benefit on the frozen backlog:\n@@ -199,14 +199,14 @@\n namespace is not in fact ownership-partitioned \u2014 56 ledger entries carry no\n `task_id`, and one goal DAG is writable by 35 registered tasks. The correct\n expression of its insight is that locks do not govern the mirror at all, which is\n-what this ADR decides.\n+what this SADR decides.\n \n **Make the mirror a projection written only by a registration writer and a\n post-merge projection job.** Rejected. It requires a bypass actor with write\n access to the protected default branch; it makes its own master invariant\n readable from a manifest that pull requests may edit; it deletes both reconcile\n modes and leaves a corrupted mirror with no in-band repair path, contradicting\n-ADR 0004's consequence that mirror conflicts are repaired against GitHub state;\n+SADR-0004's consequence that mirror conflicts are repaired against GitHub state;\n and its content-derived ids are rejected by `validate_shiki`'s currently-green id\n pattern.\n \ndiff --git a/docs/adr/0017-bookkeeping-closeouts-do-not-inherit-risk.md b/docs/adr/SADR-0017-bookkeeping-closeouts-do-not-inherit-risk.md\nindex 554e3a0d526a72ca067108c5464cc68c3edcf174..b9676857a4713850e063af7c39c8e0b01ecb4c66\n--- a/docs/adr/0017-bookkeeping-closeouts-do-not-inherit-risk.md\n+++ b/docs/adr/SADR-0017-bookkeeping-closeouts-do-not-inherit-risk.md\n@@ -1,4 +1,4 @@\n-# ADR 0017: A Bookkeeping Closeout Does Not Inherit Its Task's Risk\n+# SADR-0017: A Bookkeeping Closeout Does Not Inherit Its Task's Risk\n \n ## Status\n \n@@ -6,7 +6,7 @@\n \n ## Context\n \n-ADR 0012 made the goal loop push a task's completion to the default branch through a\n+SADR-0012 made the goal loop push a task's completion to the default branch through a\n closeout pull request: after the implementation PR merges, the loop opens a second PR\n carrying `task.status = done`, `lock.state = released`, and \u2014 when the task completes\n its goal \u2014 `goal.status = complete` plus one scorecard report. Nothing else. The\n@@ -49,14 +49,14 @@\n 4. The task file's governance fields (`scope`, `non_goals`, `required_skills`,\n    `risk_level`, `locks`, `acceptance_checks`, `test_command`) are byte-identical to\n    the base snapshot. Only `status`, `expected_pr`, `closeout_pr`, `ledger_evidence`\n-   and `expected_branch` may differ \u2014 the ADR 0015 contract-immutability rule,\n+   and `expected_branch` may differ \u2014 the SADR-0015 contract-immutability rule,\n    unchanged.\n \n    `expected_branch` is in that set because a closeout is *by construction* on a\n    different branch from the implementation it closes, and the loop rewrites the\n    field accordingly. It is safe to let it differ because MergeGate independently\n    requires `expected_branch` to equal the PR's actual head ref, so a PR cannot set\n-   it to anything other than the branch it is really on. The first draft of this ADR\n+   it to anything other than the branch it is really on. The first draft of this SADR\n    omitted it, which made the exemption unreachable: every real closeout failed\n    condition 4 on a field the loop always changes. See PR #209.\n 5. The state transitions are exactly the terminal set: task `review` \u2192 `done`, lock\n@@ -113,12 +113,12 @@\n classification must be derived, not asserted.\n \n **Fold the closeout into the implementation PR** so no second PR exists. Rejected under\n-ADR 0012, which recorded why: a task file that is already `done` on the implementation\n+SADR-0012, which recorded why: a task file that is already `done` on the implementation\n branch short-circuits the loop's review \u2192 CCA \u2192 merge \u2192 done ordering into a local\n goal-completion before any merge happens.\n \n **Have the loop auto-merge closeouts with elevated credentials.** Rejected outright: that\n-is a runtime authoring its own approval, which ADR 0010 and the constitution's\n+is a runtime authoring its own approval, which SADR-0010 and the constitution's\n approval-record prohibition forbid. It would also be indistinguishable, in the evidence,\n from the forgery those rules exist to prevent.\n \ndiff --git a/docs/adr/0018-carried-guardian-approval-across-a-proven-base-sync.md b/docs/adr/SADR-0018-carried-guardian-approval-across-a-proven-base-sync.md\nindex 22358562da390ed8b45cd600da08a43d378b4680..e6e2907cefa776460af73ddf7d39db605f3c39b0\n--- a/docs/adr/0018-carried-guardian-approval-across-a-proven-base-sync.md\n+++ b/docs/adr/SADR-0018-carried-guardian-approval-across-a-proven-base-sync.md\n@@ -1,4 +1,4 @@\n-# ADR 0018: Carried Guardian Approval Across A Proven Base Sync\n+# SADR-0018: Carried Guardian Approval Across A Proven Base Sync\n \n ## Status\n \n@@ -34,7 +34,7 @@\n Guardian evaluator that consumes it (`shiki_guardian.evaluate_guardian_approval`,\n the `guardian_comment_carried` source) were built and tested in isolation. Neither\n does anything until a caller passes the flags that turn the carry on, so the\n-feature was invisible in CI. This ADR records the decision the two of them\n+feature was invisible in CI. This SADR records the decision the two of them\n implement, and this task wires it into the four CI invocations that judge Guardian\n approval.\n \ndiff --git a/docs/adr/README.md b/docs/adr/README.md\nindex c70e057a4520c36e042ab68c53cd4a21ef709b71..5da43856836e2ce6a150f8127766e2f88355ab4b\n--- a/docs/adr/README.md\n+++ b/docs/adr/README.md\n@@ -1,40 +1,41 @@\n-# Architecture Decision Records\n+# Decision Record Namespaces\n \n-This directory holds Shiki's Architecture Decision Records (ADRs): durable\n-records of hard-to-reverse platform decisions. ADRs are part of the Shiki source\n-of truth alongside GitHub state, the `.shiki/` mirror, and `CONTEXT.md`.\n+This directory holds two independent decision namespaces:\n \n-## How ADRs Work\n+- Shiki Architecture Decision Records (SADRs), named\n+  `SADR-NNNN-<short-slug>.md`, record hard-to-reverse Shiki platform decisions.\n+- Target Architecture Decision Records (ADRs), named\n+  `NNNN-<short-slug>.md`, belong to the target product.\n \n-- New ADRs are added as `docs/adr/NNNN-<short-slug>.md`, where `NNNN` is the next\n-  zero-padded sequence number.\n-- The **canonical list of ADRs is the set of files matching `docs/adr/*.md`** in\n-  this directory. Browse `docs/adr/*.md` for the authoritative, current set.\n-- Use [`template.md`](template.md) as the starting point for a new ADR.\n-- Record an ADR when a decision is hard to reverse, surprising without context,\n-  and a real tradeoff. Do not write an ADR for routine or easily reversible\n-  choices.\n+The same four-digit number may appear once in each namespace. A platform\n+`SADR-NNNN` citation resolves only to `SADR-NNNN-*.md`; a target `ADR NNNN`\n+citation resolves only to `NNNN-*.md`.\n \n-Using the `docs/adr/*.md` glob as the source of truth means other PRs can add an\n-ADR without editing this index file, avoiding merge contention on a\n-hand-maintained enumeration.\n+## How Decision Records Work\n \n-## Current ADRs (snapshot)\n-\n-The list below is a convenience snapshot and may lag behind the directory. When\n-in doubt, list `docs/adr/*.md`.\n+- New Shiki platform decisions use the next zero-padded SADR number.\n+- New target product decisions use the target's own ADR sequence.\n+- The canonical list is the files in `docs/adr/`; no hand-maintained index is\n+  authoritative.\n+- Use [`template.md`](template.md) as the starting point and choose the filename\n+  namespace that owns the decision.\n+- Record a decision when it is hard to reverse, surprising without context, and\n+  a real tradeoff. Do not write a decision record for routine or easily\n+  reversible choices.\n \n-- [0001 \u2014 Keep Shiki As The Platform Name](0001-keep-shiki-as-platform-name.md)\n-- [0002 \u2014 Use AGENTS As The Shared Agent Constitution](0002-use-agents-as-shared-constitution.md)\n-- [0003 \u2014 Require Skill Gate For MergeGate Readiness](0003-require-skill-gate-for-mergegate-readiness.md)\n-- [0004 \u2014 Use GitHub First With A Repository-Local Shiki Mirror](0004-use-github-first-with-shiki-mirror.md)\n-- [0005 \u2014 Use Codex As Front And Claude Code Action As The GitHub Runtime](0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md)\n-- [0006 \u2014 Separate Codex Implementation, CCA Completion, And MergeGate Authorization](0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md)\n+Using the directory as the source of truth lets independent changes add records\n+without contending on an enumerated index.\n \n-## Adding A New ADR\n+## Adding A Shiki SADR\n \n-1. Copy `template.md` to `docs/adr/NNNN-<short-slug>.md`.\n+1. Copy `template.md` to `docs/adr/SADR-NNNN-<short-slug>.md`.\n 2. Fill in the title, status, context, decision, and consequences.\n-3. Set the status to `Proposed`, then `Accepted` once the decision is approved.\n-4. Open a PR following `CONTRIBUTING.md`. You do not need to edit this index for\n-   the ADR to be discoverable via `docs/adr/*.md`.\n+3. Set the status to `Proposed`, then `Accepted` once the proper authority has\n+   approved the decision.\n+4. Open a PR following `CONTRIBUTING.md`.\n+\n+## Adding A Target ADR\n+\n+1. Copy `template.md` to `docs/adr/NNNN-<short-slug>.md` using the target's own\n+   sequence.\n+2. Cite it as `ADR NNNN`; do not prefix its filename with `SADR-`.\ndiff --git a/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md b/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\nnew file mode 100644\nindex 0000000000000000000000000000000000000000..7455ce1f3bed02ddddb851ac21a1d1a648c9aeba\n--- /dev/null\n+++ b/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\n@@ -0,0 +1,55 @@\n+# SADR-0019: Separate Shiki Platform SADRs From Target ADRs\n+\n+## Status\n+\n+Accepted\n+\n+## Context\n+\n+Shiki installs its platform decision records into `docs/adr/` in every target\n+repository. Shiki and its targets previously used the same numeric\n+`NNNN-<slug>.md` namespace. A target that already owned ADRs 0013 through 0018\n+therefore acquired two records for each number during installation, making an\n+`ADR NNNN` citation ambiguous. Renumbering target decisions would rewrite target\n+history, while renumbering Shiki decisions per target would destroy their stable\n+platform identity.\n+\n+Upgrades also need to remove the former Shiki-owned numeric paths without ever\n+mistaking a target-owned ADR for platform content. The install stamp already\n+records exact shipped paths and content digests, so it can prove ownership only\n+when both still match.\n+\n+## Decision\n+\n+Shiki platform decisions use the `SADR` namespace:\n+\n+- records are named `docs/adr/SADR-NNNN-<slug>.md`;\n+- live platform citations use `SADR-NNNN`;\n+- target product decisions retain `docs/adr/NNNN-<slug>.md` and `ADR NNNN`;\n+- the reference validator resolves the two namespaces independently, so the same\n+  four-digit number may exist once in each namespace.\n+\n+Before a forced upgrade writes anything, the installer inventories the exact\n+former numeric Shiki ADR paths. It may delete a legacy path only when the\n+existing install stamp contains that exact path and its recorded SHA-256 digest\n+matches the file currently on disk. All ownership blockers are reported\n+together and abort the upgrade before its first write. A non-forced install\n+never performs this cleanup.\n+\n+Historical `.shiki/` evidence remains unchanged because it records the namespace\n+that existed when that evidence was produced.\n+\n+## Consequences\n+\n+- Targets may use any ADR number without colliding with Shiki's platform SADRs.\n+- Fresh installs preserve target ADR bytes and install a stable SADR set.\n+- Forced upgrades fail closed when legacy ownership cannot be proven, and a\n+  successful upgrade refreshes the stamp without removed legacy paths.\n+- Current governance, packaging, workflow, prompt, script, and skill references\n+  must move with the platform records.\n+\n+## Alternatives Considered\n+\n+- Collision-only refusal leaves affected targets permanently unupgradable.\n+- Renumbering target ADRs mutates target-owned history.\n+- Per-target Shiki renumbering makes platform citations non-portable.\ndiff --git a/docs/adr/template.md b/docs/adr/template.md\nindex a92a97daddd0b00ab9dd626cc8d86942da2eafed..17dd79175b5fee2105935557f691b40cf9492942\n--- a/docs/adr/template.md\n+++ b/docs/adr/template.md\n@@ -1,14 +1,14 @@\n-# NNNN: <Short Decision Title>\n+# <SADR-NNNN or NNNN>: <Short Decision Title>\n \n ## Status\n \n-Proposed | Accepted | Superseded by ADR-NNNN\n+Proposed | Accepted | Superseded by SADR-NNNN or target ADR NNNN\n \n ## Context\n \n What is the situation, constraint, or force that requires a decision? Reference\n-relevant domain terms (`CONTEXT.md`), prior ADRs, and code reality. State the\n-problem, not the solution.\n+relevant domain terms (`CONTEXT.md`), prior Shiki SADRs or target ADRs, and code\n+reality. State the problem, not the solution.\n \n ## Decision\n \ndiff --git a/docs/agents/autonomous-loop-e2e-acceptance.md b/docs/agents/autonomous-loop-e2e-acceptance.md\nindex bbf84c6081987f159ac6ad9ecf3c82b7aa72e7e8..5c13c1d02a05a90b0dc46cbef7c95a9846d0c237\n--- a/docs/agents/autonomous-loop-e2e-acceptance.md\n+++ b/docs/agents/autonomous-loop-e2e-acceptance.md\n@@ -40,8 +40,8 @@\n 3. `gh auth status` is logged in with merge permission on the repo.\n 4. Branch protection + required checks are configured exactly as the goal-loop\n    required-check set expects (see `docs/agents/branch-protection-smoke.md`).\n-5. The repo `.shiki/policy` permits autonomous low/medium-risk merges (ADR\n-   0008/0009); the MergeGate policy check is the recorded-authority gate.\n+5. The repo `.shiki/policy` permits autonomous low/medium-risk merges\n+   (SADR-0008/0009); the MergeGate policy check is the recorded-authority gate.\n \n The operator's only actions are: register the task and start the loop. **No\n manual pre-clearing** means after `shiki loop run` starts, the operator does not\ndiff --git a/docs/agents/bootstrap-command.md b/docs/agents/bootstrap-command.md\nindex 05f7f203a52cf412b9f97084a6e131ed0f785afd..f7aa0b734dfebee4a3b139335b66ee3415f48713\n--- a/docs/agents/bootstrap-command.md\n+++ b/docs/agents/bootstrap-command.md\n@@ -189,7 +189,9 @@\n ```\n \n Use this only for tests, fixtures, or explicit local-only template inspection.\n-Use `--force` only when you intentionally want to overwrite existing target files.\n+Use `--force` only when you intentionally want to refresh the target template.\n+Legacy numeric Shiki decision cleanup is stamp-and-digest authorized before any\n+write; target ADR paths are not cleanup candidates.\n \n ## Slash Command\n \ndiff --git a/docs/agents/checklists.md b/docs/agents/checklists.md\nindex 4a7f03ade346f53a1c1d82981f3fd744f9ab4547..0c5ec6c53dc7197b7b7a7d18e667b325babd6c45\n--- a/docs/agents/checklists.md\n+++ b/docs/agents/checklists.md\n@@ -32,18 +32,18 @@\n | ID | Item | Blocking |\n | --- | --- | --- |\n | GD-01 | Plan was challenged against `CONTEXT.md`. | blocking for non-trivial Goals |\n-| GD-02 | Relevant ADRs were read or absence was recorded. | blocking for non-trivial Goals |\n+| GD-02 | Relevant SADRs and target ADRs were read or absence was recorded. | blocking for non-trivial Goals |\n | GD-03 | Ambiguous terms were resolved or converted to HITL blockers. | blocking |\n | GD-04 | Edge scenarios were considered. | advisory |\n | GD-05 | Code reality was checked when code could answer. | blocking when applicable |\n | GD-06 | New domain terms were added to `CONTEXT.md` when needed. | blocking when applicable |\n-| GD-07 | ADR was created or explicitly skipped using the ADR criteria. | blocking when applicable |\n+| GD-07 | A SADR or target ADR was created or explicitly skipped using the decision-record criteria. | blocking when applicable |\n \n ## CI \u2014 Context & Impact Checklist\n \n | ID | Item | Blocking |\n | --- | --- | --- |\n-| CI-01 | Relevant docs and ADRs identified. | blocking |\n+| CI-01 | Relevant docs, SADRs, and target ADRs identified. | blocking |\n | CI-02 | Relevant modules, interfaces, seams, callers, and tests identified. | blocking |\n | CI-03 | Dependencies and blocked-by relationships identified. | blocking |\n | CI-04 | Candidate locks identified. | blocking |\n@@ -62,7 +62,7 @@\n | PRD-04 | Implementation decisions are recorded without volatile code detail. | blocking |\n | PRD-05 | Testing decisions are recorded. | blocking |\n | PRD-06 | Out-of-scope items are explicit. | blocking |\n-| PRD-07 | Relevant glossary/ADR links are present when applicable. | blocking |\n+| PRD-07 | Relevant glossary/SADR/target-ADR links are present when applicable. | blocking |\n \n ## SF \u2014 Spec Freeze Checklist\n \n@@ -98,7 +98,7 @@\n | H-02 | Branch/worktree target is present. | blocking |\n | H-03 | Scope and non-goals are present. | blocking |\n | H-04 | Dependencies and locks are present. | blocking |\n-| H-05 | Relevant docs, ADRs, modules, and tests are referenced. | blocking |\n+| H-05 | Relevant docs, SADRs, target ADRs, modules, and tests are referenced. | blocking |\n | H-06 | Required skills are stated. | blocking |\n | H-07 | TDD expectations are stated. | blocking when behavior work |\n | H-08 | Acceptance criteria are included. | blocking |\ndiff --git a/docs/agents/codex-handoff.md b/docs/agents/codex-handoff.md\nindex ede468f47cb277f69a857e8c45df703e4ed0151d..6f97bdffb67e44d7edea2c71e01ea28933c4eb6c\n--- a/docs/agents/codex-handoff.md\n+++ b/docs/agents/codex-handoff.md\n@@ -46,7 +46,7 @@\n \n ## Context\n - Domain terms:\n-- Relevant ADRs:\n+- Relevant SADRs and target ADRs:\n - Relevant docs:\n - Relevant modules/interfaces/seams:\n - Similar tests/prior art:\ndiff --git a/docs/agents/completion-check-agent.md b/docs/agents/completion-check-agent.md\nindex 13e4c035db35025f4e3cdcc5adfb24314ac4d521..61bfa1218cf36ca605250c91919e620f33238aa0\n--- a/docs/agents/completion-check-agent.md\n+++ b/docs/agents/completion-check-agent.md\n@@ -32,7 +32,7 @@\n - `AGENTS.md`\n - `CLAUDE.md`\n - `CONTEXT.md`\n-- relevant ADRs\n+- relevant SADRs and target ADRs\n - parent Goal issue\n - PRD issue or document\n - task issue\ndiff --git a/docs/agents/decision-control.md b/docs/agents/decision-control.md\nindex 7d8ee5ab03a5ccbcbd04056ae61de03785a2a9c9..8c94ecda00fffaa0e92c325ab83aa9776bd1dac1\n--- a/docs/agents/decision-control.md\n+++ b/docs/agents/decision-control.md\n@@ -11,7 +11,7 @@\n | Decision | Owner | When | Required inputs | Output | Enforcement surface |\n | --- | --- | --- | --- | --- | --- |\n | Is the Goal clear enough to plan? | Planner + Guardian when needed | Goal intake | User request, existing docs, risk signals | Goal readiness verdict | Goal checklist, issue template, ledger |\n-| Must `grill-with-docs` run? | Planner / Skill Gate | Before PRD or issue split | Ambiguity, domain terms, ADR conflicts, unclear boundaries | Skill invocation or explicit skip rationale | Skill Gate checklist |\n+| Must `grill-with-docs` run? | Planner / Skill Gate | Before PRD or issue split | Ambiguity, domain terms, SADR/target-ADR conflicts, unclear boundaries | Skill invocation or explicit skip rationale | Skill Gate checklist |\n | Is a PRD required? | Planner | After Goal context stabilizes | Goal, `grill-with-docs` output, Context & Impact output | PRD required / not required | PRD checklist, CCA evidence check |\n | Can the spec be frozen? | Operator only | End of Requirements Definition, before issue split | PRD/requirements summary, scope inventory (SF-02), open-question list | `spec_freeze` block in the plan + ledger evidence | `require_grilled_plan` (plan ingest/run), SF checklist |\n | Can a frozen spec change? | Operator only (Spec Amendment) | When implementation contradicts the frozen spec | Contested decisions, scoped re-grill result | recorded amendment + re-stamped freeze | SF checklist, ledger evidence |\ndiff --git a/docs/agents/domain.md b/docs/agents/domain.md\nindex 258da8ea81e87ac0a6b0b5b5e5c2fde980eaabb7..d57b0cf17ff3e1fa9e6e5885372bf083d8295421\n--- a/docs/agents/domain.md\n+++ b/docs/agents/domain.md\n@@ -5,7 +5,9 @@\n ## Layout\n \n - `CONTEXT.md` defines Shiki language. Keep it implementation-free.\n-- `docs/adr/` records hard-to-reverse decisions.\n+- `docs/adr/SADR-NNNN-*.md` records Shiki platform decisions.\n+- `docs/adr/NNNN-*.md` records target product decisions without sharing the\n+  SADR namespace.\n - `AGENTS.md` is the runtime-neutral agent constitution.\n - `CLAUDE.md` is the Claude Code wrapper around `AGENTS.md`.\n - `.shiki/` is the Target Repository mirror for Goals, PRDs, plans, Task DAGs, contracts, locks, worktree records, ledger entries, CCA verdicts, repair packets, reports, and handoffs.\n@@ -15,6 +17,6 @@\n \n - Use Shiki terms exactly when they exist.\n - Add glossary terms to `CONTEXT.md` only when the term is part of the domain, not a generic programming concept.\n-- Create ADRs only for hard-to-reverse, surprising, tradeoff-based decisions.\n+- Create SADRs or target ADRs only for hard-to-reverse, surprising, tradeoff-based decisions.\n - Keep product-specific language in the Target Repository, not in Shiki Template docs.\n-- If an ADR conflicts with a proposed implementation, surface the conflict before editing.\n+- If a SADR or target ADR conflicts with a proposed implementation, surface the conflict before editing.\ndiff --git a/docs/agents/execution-control.md b/docs/agents/execution-control.md\nindex ad8ee3cdb36be56fedc1f46408ca92b387d74047..bc63c3a73661d12b567016ba299f1a6b66bf9088\n--- a/docs/agents/execution-control.md\n+++ b/docs/agents/execution-control.md\n@@ -63,7 +63,7 @@\n | Branch name | Orchestrator | Orchestrator | Before implementer dispatch | branch policy | branch ref in task/ledger |\n | Lock reservation | Context & Impact | MergeGate | Before dispatch and before merge | `.shiki/locks/`, labels, PR metadata | lock id, owner, scope |\n | CI profile | Planner | MergeGate | During issue triage, rechecked at PR | labels, `.shiki/policy.yaml`, workflow matrix | required check list |\n-| Required status checks | Repo maintainer / Guardian | GitHub branch protection / rulesets | Repository setup, updated via ADR | GitHub settings | branch/ruleset config, ADR |\n+| Required status checks | Repo maintainer / Guardian | GitHub branch protection / rulesets | Repository setup, updated via target ADR | GitHub settings | branch/ruleset config, target ADR |\n | CCA judgment | CCA | GitHub required check / MergeGate | Every non-draft PR update | GitHub Action check | structured CCA verdict |\n | Repair assignment | CCA | MergeGate | After failed CCA/CI/review | repair packet, issue/PR comment | repair id and attempt count |\n | Merge readiness | MergeGate | GitHub branch protection / Guardian if needed | After CCA complete | required checks, review rules, rulesets | mergegate check output |\ndiff --git a/docs/agents/guardian-policy.md b/docs/agents/guardian-policy.md\nindex 4147c7adf05a47adb4fd24ada1a1c2e1f2ba6d2d..a20d9cc6c00a569bb057b9ca8d6b51c7fc4a9847\n--- a/docs/agents/guardian-policy.md\n+++ b/docs/agents/guardian-policy.md\n@@ -98,8 +98,8 @@\n `origin/<default_branch>` and reuses each job's existing base fetch; a shallow\n (`--depth`) or second base fetch would make the proof unresolvable and silently\n disable the carry, so neither is permitted. CCA reads `guardian_comment_carried` as\n-a recorded authority in the deterministic signal and does not re-derive it. See ADR\n-0018 for the full decision, the rejected diff/patch-equality and path-disjointness\n+a recorded authority in the deterministic signal and does not re-derive it. See\n+SADR-0018 for the full decision, the rejected diff/patch-equality and path-disjointness\n alternatives, and the accepted residual.\n \n ## External AI Guardian Review (SADR-0010)\ndiff --git a/docs/agents/implementation-policy.md b/docs/agents/implementation-policy.md\nindex c754144a9fb5bf4a38aa82a60e1670c59e626b44..85c37281bcad196c6b7b02343faf00b03cd9c57a\n--- a/docs/agents/implementation-policy.md\n+++ b/docs/agents/implementation-policy.md\n@@ -31,10 +31,13 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n ## Non-Negotiables\n \n 1. **Planning precedes implementation.** Non-trivial Goals start with `grill-with-docs` and Context & Impact.\n-2. **Domain language is controlled.** Terms must be checked against `CONTEXT.md`; hard-to-reverse tradeoffs must be checked against ADRs.\n+2. **Domain language is controlled.** Terms must be checked against `CONTEXT.md`; hard-to-reverse tradeoffs must be checked against Shiki SADRs and target ADRs.\n 3. **PRD precedes issue decomposition.** `to-prd` is used once the context is settled enough to become durable intent.\n 4. **Issues are vertical slices.** `to-issues` must produce independently grabbable tracer-bullet tasks, not horizontal layer tickets.\n 5. **Triage controls dispatch.** Only `ready-for-agent` / AFK tasks may be assigned to Codex without human input.\n@@ -95,19 +98,19 @@\n Process:\n \n - Challenge terms against `CONTEXT.md`.\n-- Check ADRs before reopening decisions.\n+- Check SADRs and target ADRs before reopening decisions.\n - Inspect code when code can answer a question.\n - Ask one question at a time when the operator is available.\n - Provide a recommended answer with each question.\n - Record resolved terms in `CONTEXT.md`.\n-- Create ADRs only for hard-to-reverse, surprising, tradeoff-based decisions.\n+- Create SADRs or target ADRs only for hard-to-reverse, surprising, tradeoff-based decisions.\n - Convert unresolved load-bearing questions into HITL blockers.\n \n Outputs:\n \n - Resolved design-tree decisions.\n - Updated glossary terms when needed.\n-- ADRs when justified.\n+- SADRs or target ADRs when justified.\n - HITL blockers when decisions remain unresolved.\n \n ## Phase 3 \u2014 Context & Impact\n@@ -117,7 +120,7 @@\n \n Outputs must include:\n \n-- Relevant documents and ADRs.\n+- Relevant documents, SADRs, and target ADRs.\n - Relevant modules, interfaces, seams, callers, and tests.\n - Candidate locks.\n - Dependencies.\n@@ -140,7 +143,7 @@\n - Testing decisions.\n - Out of scope.\n - Further notes.\n-- Links to relevant Goal, domain terms, and ADRs.\n+- Links to relevant Goal, domain terms, SADRs, and target ADRs.\n \n Phase 4 ends with Spec Freeze: the operator explicitly approves the PRD, the\n plan records a `spec_freeze` block (status, approved_by, source), and the SF\n@@ -240,7 +243,7 @@\n - Dependencies and locks.\n - Risk and Guardian requirements.\n - Ledger completeness.\n-- Documentation and ADR updates.\n+- Documentation and SADR/target-ADR updates.\n - Scope drift.\n \n CCA emits one of:\n@@ -301,7 +304,7 @@\n | Evidence failure | Behavior might be done, but proof is missing. | Add checks, PR evidence, ledger entry, or artifacts. |\n | Scope drift | PR changes unrelated behavior. | Revert unrelated changes or split into new Goal. |\n | Dependency block | Upstream task or lock not complete. | Block until dependency resolves. |\n-| Architecture block | Decision needs ADR or deep-module discussion. | `grill-with-docs` or `improve-codebase-architecture`. |\n+| Architecture block | Decision needs a SADR, target ADR, or deep-module discussion. | `grill-with-docs` or `improve-codebase-architecture`. |\n | Risk block | Security/production/policy change needs human approval. | Guardian decision. |\n \n ## Phase 5.5 \u2014 Execution Decision Control\ndiff --git a/docs/agents/runtime-auth-model.md b/docs/agents/runtime-auth-model.md\nindex 6a2e1a47e81ea89b5c68a210ec07772a9374babc..3c54748d24233dbd0ba8473950282547ef4ce935\n--- a/docs/agents/runtime-auth-model.md\n+++ b/docs/agents/runtime-auth-model.md\n@@ -148,7 +148,7 @@\n \n API-key based runners may be added by a target repository as an explicit extension. They are not the default Shiki template path.\n \n-If a repo opts into API-key mode, record the exception in an ADR and update the repo's `AGENTS.md`, `.shiki/config.yaml`, and workflow permissions.\n+If a repo opts into API-key mode, record the target-specific exception in a target ADR and update the repo's `AGENTS.md`, `.shiki/config.yaml`, and workflow permissions.\n \n ## GitHub Actions Runtime Compatibility\n \ndiff --git a/docs/agents/skill-gate.md b/docs/agents/skill-gate.md\nindex c50398b0d7c60a8598f2a5c88a10015730dced13..3acffe6444617bb1d54b77d4b4708158ec735bc6\n--- a/docs/agents/skill-gate.md\n+++ b/docs/agents/skill-gate.md\n@@ -9,7 +9,7 @@\n | Skill | Trigger | Expected output |\n | --- | --- | --- |\n | `setup-matt-pocock-skills` | First configuring a repo for these skills, or when issue tracker, triage labels, or domain docs are missing. | `docs/agents/` configuration and an `Agent skills` block in `AGENTS.md` or `CLAUDE.md`. |\n-| `grill-with-docs` | Ambiguous plan, unclear terminology, boundary decision, tradeoff, or ADR-worthy design choice. | Clarified decision tree, recommended answers, updates to `CONTEXT.md` or ADRs when decisions crystallize. |\n+| `grill-with-docs` | Ambiguous plan, unclear terminology, boundary decision, tradeoff, or decision-record-worthy design choice. | Clarified decision tree, recommended answers, updates to `CONTEXT.md`, SADRs, or target ADRs when decisions crystallize. |\n | `zoom-out` | Agent lacks a higher-level architectural map. | Map of relevant modules, callers, seams, and domain concepts. |\n | `to-prd` | Settled context should become a PRD. | PRD published to the issue tracker with problem, solution, user stories, decisions, testing, and out-of-scope notes. |\n | `to-issues` | Goal, PRD, or plan must become implementation tickets. | Vertical-slice issues with acceptance criteria, dependencies, and AFK/HITL classification. |\n@@ -17,7 +17,7 @@\n | `tdd` | Feature work or bug fix has observable behavior that can be specified with tests. | Red-green-refactor loop through public interfaces, one vertical slice at a time. |\n | `code-review` | The TDD loop is green and the implementer is about to create or update a PR. Default for every implementation task. | Pre-PR self-review of the task diff with fixes applied on the task branch; findings and resolutions recorded as a ledger entry naming the skill and a `## Pre-PR code review` PR body section. |\n | `diagnose` | Hard bug, failing check, regression, flaky behavior, or performance problem. | Reproduction loop, ranked hypotheses, instrumentation, fix, regression test, post-mortem. |\n-| `improve-codebase-architecture` | Structural friction, testability, AI-navigability, deep-module opportunity, or refactor request. | Architecture candidates and deepening recommendations informed by `CONTEXT.md` and ADRs. |\n+| `improve-codebase-architecture` | Structural friction, testability, AI-navigability, deep-module opportunity, or refactor request. | Architecture candidates and deepening recommendations informed by `CONTEXT.md`, SADRs, and target ADRs. |\n | `prototype` | A throwaway logic or UI prototype is needed to answer a design question. | Clearly marked disposable prototype and captured decision. |\n \n ## MergeGate Interaction\ndiff --git a/docs/operations.md b/docs/operations.md\nindex cf1f452d4c589e38ccd58553bda0ad8684b51eb7..ace48a90ffbec9e5c0a7113bef16a4858bc44b3f\n--- a/docs/operations.md\n+++ b/docs/operations.md\n@@ -89,6 +89,16 @@\n      target history, brought forward by `shiki migrate`, not a template.\n    - **Everything else** (CLI scripts, workflows, schemas, docs) is overwritten.\n \n+   A forced upgrade also migrates the former numeric Shiki decision paths to\n+   `SADR-NNNN-*`. Before the first write, the installer inventories the exact\n+   legacy Shiki paths and authorizes each deletion only when the existing\n+   `.shiki/install-stamp.json` names that path and its SHA-256 digest still\n+   matches the file. An absent, unreadable, malformed, incomplete, or\n+   mismatching stamp reports every blocked path and stops with no filesystem\n+   change. Target-owned `NNNN-*` ADRs are not cleanup candidates. A non-force\n+   install deletes nothing and retains any still-matching legacy stamp entries\n+   so a later forced upgrade can prove ownership.\n+\n    If the target has pending migrations, `--force` refuses and writes nothing \u2014\n    apply migrations first (step 3), then re-run. The run ends with a single\n    summary of every `.new` written, so a half-upgrade is visible.\ndiff --git a/docs/releasing.md b/docs/releasing.md\nindex 10d73828e7dbd64e9d9f6b83bc84d010537ce62a..aafeee8c4d441e1e427efee2ce4f81a647f020e1\n--- a/docs/releasing.md\n+++ b/docs/releasing.md\n@@ -67,7 +67,7 @@\n CLI in `scripts/`. There are no third-party Python runtime dependencies. The CLI\n is intentionally **not** exposed as a `[project.scripts]` console entry point\n because `scripts/shiki.py` is a dependency-free shim and `scripts/` is not\n-packaged as an importable distribution (see the ADR for the rationale).\n+packaged as an importable distribution (see SADR-0007 for the rationale).\n \n Supported install paths:\n \n@@ -155,6 +155,15 @@\n    [docs/operations.md](operations.md#the-new-convention). If the target has\n    pending migrations, `--force` refuses and writes nothing; apply migrations\n    (step 2) first, then re-run.\n+\n+   The SADR namespace upgrade has an additional pre-write gate: each former\n+   numeric Shiki decision path is removed only when the existing install stamp\n+   contains that exact path and a digest matching its current bytes. The\n+   installer aggregates absent/unreadable/malformed-stamp, missing-digest, and\n+   digest-mismatch blockers before raising, so refusal leaves the target tree\n+   byte-identical. Numeric target ADRs are preserved. A successful forced\n+   upgrade installs and stamps `SADR-*` paths and drops the removed legacy paths\n+   from the refreshed stamp.\n 2. Apply repository-local `.shiki/` state migrations with the migrate command:\n \n    ```bash\ndiff --git a/docs/usage.md b/docs/usage.md\nindex b83407ee3970dd395dd893010bc5dbe8bad26051..a130ef6e40dfd8169a5bc701339351097df1a09f\n--- a/docs/usage.md\n+++ b/docs/usage.md\n@@ -67,8 +67,10 @@\n shiki install-target /path/to/target-repo --local-only\n ```\n \n-Use `--force` only when you intentionally want to overwrite existing target\n-files.\n+Use `--force` only when you intentionally want to refresh the target template.\n+For the SADR namespace migration, `--force` deletes only exact legacy Shiki\n+decision paths whose existing install-stamp digest still matches; any ownership\n+blocker stops before the first write. Numeric target ADRs are never candidates.\n \n ## `shiki bootstrap-platform` \u2014 publish the Shiki platform repo\n \ndiff --git a/scripts/guardian_approval_signal.py b/scripts/guardian_approval_signal.py\nindex d059f92b2c82aa2b5d6ca421365537a87886c1cb..16924892714f4f5b9303c0ef0e325a7e70075630\n--- a/scripts/guardian_approval_signal.py\n+++ b/scripts/guardian_approval_signal.py\n@@ -199,8 +199,8 @@\n ) -> Any:\n     \"\"\"Evaluate SADR-0015 Contract Approval for the CCA Guardian signal.\n \n-    The SAME OR that MergeGate applies must reach this deterministic signal: ADR\n-    0015 is explicit that without it, a contract-approved PR still returns\n+    The SAME OR that MergeGate applies must reach this deterministic signal:\n+    SADR-0015 is explicit that without it, a contract-approved PR still returns\n     ``needs_guardian``, the CCA job fails, and the MergeGate policy check never\n     runs \u2014 the gate this change moves would never be reached. This resolves the\n     task the SAME way MergeGate does (the first Shiki task id in the PR body),\ndiff --git a/scripts/shiki_contract_approval.py b/scripts/shiki_contract_approval.py\nindex 941bcb96fdd8b9a85bc0c4f0f04d5dad9d04839c..4a4b418f6527413161db82670bec857a043360e2\n--- a/scripts/shiki_contract_approval.py\n+++ b/scripts/shiki_contract_approval.py\n@@ -142,7 +142,7 @@\n     changed_files_status: Any,\n     registration: Any,\n ) -> ContractApprovalResult:\n-    \"\"\"Whether this PR carries an SADR-0015 Contract Approval.\n+    \"\"\"Whether this PR carries a SADR-0015 Contract Approval.\n \n     The four conditions are evaluated IN ORDER, each failure recorded as a\n     distinct reason string:\ndiff --git a/scripts/shiki_guardian.py b/scripts/shiki_guardian.py\nindex a4ecb79bbda55fd5c8b06adfae2eb99760a03c45..4dd99bbe714e26f421fa19abac7a34ae1b195ecc\n--- a/scripts/shiki_guardian.py\n+++ b/scripts/shiki_guardian.py\n@@ -12,7 +12,7 @@\n # The carry re-derives its proof from git in-process on every judging invocation;\n # it is never read from an artifact, so there is nothing to go stale or be forged.\n # shiki_sync_proof is the ONLY guardian-family module that runs git, and every one\n-# of its invocations is read-only and local (ADR: prove-from-git base sync).\n+# of its invocations is read-only and local (SADR: prove-from-git base sync).\n from shiki_sync_proof import verify_pure_base_sync\n \n GUARDIAN_POLICY_PATH = \".shiki/guardian-policy.json\"\ndiff --git a/scripts/shiki_guardian_review.py b/scripts/shiki_guardian_review.py\nindex dd0f104d457899e0ba5ff1e162011c1e90b6f92b..e10ef58c046324e7ea3a3392feb3048ca77414ce\n--- a/scripts/shiki_guardian_review.py\n+++ b/scripts/shiki_guardian_review.py\n@@ -159,7 +159,7 @@\n     docs_only = _is_docs_only(changed_files)\n     if docs_only and matched == 0:\n         focus.append(\n-            \"Docs/ADR-only change: confirm the change is genuinely documentation/decision-only, introduces no behavior or contract drift, and stays consistent with the constitution and existing ADRs.\"\n+            \"Docs/decision-record-only change: confirm the change is genuinely documentation/decision-only, introduces no behavior or contract drift, and stays consistent with the constitution and existing SADRs and target ADRs.\"\n         )\n \n     # Conservative augmentation: unknown (no category and not docs-only), mixed\ndiff --git a/scripts/shiki_installer.py b/scripts/shiki_installer.py\nindex be064ab7a29ff6836f7f7a1d70500a43e0d11d7b..9fd8eefd137ce7003dad5c0dcb2e48e2cd47ab32\n--- a/scripts/shiki_installer.py\n+++ b/scripts/shiki_installer.py\n@@ -118,6 +118,7 @@\n # what lets a target carry it without a governance-schema change.\n INSTALL_STAMP_PATH = \".shiki/install-stamp.json\"\n INSTALL_STAMP_VERSION = 1\n+LEGACY_SHIKI_SADR_MAX = 18\n \n # Under --force the shipped surface splits three ways, so nothing is lost and\n # nothing is silently stale.\n@@ -237,9 +238,98 @@\n         )\n \n \n+def legacy_shiki_adr_paths() -> tuple[str, ...]:\n+    \"\"\"Former numeric paths for Shiki SADR-0001 through SADR-0018.\n+\n+    The authoritative slugs come from the current SADR filenames. SADR-0019 is\n+    the namespace decision itself and never shipped under a numeric legacy path.\n+    \"\"\"\n+    paths: list[str] = []\n+    for sadr in sorted((ROOT / \"docs\" / \"adr\").glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\")):\n+        number = sadr.name.split(\"-\", 2)[1]\n+        if int(number) <= LEGACY_SHIKI_SADR_MAX:\n+            paths.append(f\"docs/adr/{sadr.name.removeprefix('SADR-')}\")\n+    return tuple(paths)\n+\n+\n+def preflight_legacy_sadr_cleanup(target: Path, *, force: bool) -> tuple[str, ...]:\n+    \"\"\"Authorize exact legacy Shiki ADR removals before the first write.\n+\n+    A non-force install never removes existing files. A forced install needs no\n+    stamp when no legacy Shiki path is present. When legacy paths do exist, each\n+    must be named in a readable install stamp and its current bytes must match\n+    the recorded digest.\n+    \"\"\"\n+    if not force:\n+        return ()\n+    present = tuple(\n+        relative\n+        for relative in legacy_shiki_adr_paths()\n+        if (target / relative).exists() or (target / relative).is_symlink()\n+    )\n+    if not present:\n+        return ()\n+\n+    stamp_path = target / INSTALL_STAMP_PATH\n+    try:\n+        raw = stamp_path.read_text(encoding=\"utf-8\")\n+        stamp = json.loads(raw)\n+    except FileNotFoundError:\n+        detail = \"install stamp is absent\"\n+        blockers = [f\"{relative}: {detail}\" for relative in present]\n+    except (OSError, UnicodeDecodeError) as error:\n+        detail = f\"install stamp is unreadable ({error})\"\n+        blockers = [f\"{relative}: {detail}\" for relative in present]\n+    except json.JSONDecodeError as error:\n+        detail = f\"install stamp is malformed ({error})\"\n+        blockers = [f\"{relative}: {detail}\" for relative in present]\n+    else:\n+        if not isinstance(stamp, dict):\n+            blockers = [\n+                f\"{relative}: install stamp is malformed (expected an object)\"\n+                for relative in present\n+            ]\n+        else:\n+            digests = stamp.get(\"digests\")\n+            blockers = []\n+            if not isinstance(digests, dict):\n+                blockers.extend(\n+                    f\"{relative}: install stamp has no digest for this path\"\n+                    for relative in present\n+                )\n+            else:\n+                for relative in present:\n+                    expected = digests.get(relative)\n+                    if not isinstance(expected, str) or not expected:\n+                        blockers.append(\n+                            f\"{relative}: install stamp has no digest for this path\"\n+                        )\n+                        continue\n+                    try:\n+                        actual = _sha256_file(target / relative)\n+                    except OSError as error:\n+                        blockers.append(\n+                            f\"{relative}: legacy path is unreadable ({error})\"\n+                        )\n+                        continue\n+                    if actual != expected:\n+                        blockers.append(\n+                            f\"{relative}: digest mismatch (stamp {expected}, current {actual})\"\n+                        )\n+\n+    if blockers:\n+        joined = \"\\n\".join(f\"- {blocker}\" for blocker in blockers)\n+        raise ShikiError(\n+            \"legacy Shiki ADR cleanup is not authorized:\\n\"\n+            f\"{joined}\\nNo files were written.\"\n+        )\n+    return present\n+\n+\n def install_template(target: Path, *, force: bool, validate: bool) -> list[NewFileNote]:\n     # Preconditions first: never rewrite the tree of a target that cannot be\n     # upgraded cleanly (see refuse_pending_migrations).\n+    legacy_cleanup = preflight_legacy_sadr_cleanup(target, force=force)\n     refuse_pending_migrations(target)\n \n     # Resolve the target's required CODEOWNERS owner once from its\n@@ -250,6 +340,10 @@\n     # verbatim. See ``codeowners_required_owner``.\n     codeowners_owner = codeowners_required_owner(target)\n \n+    for relative in legacy_cleanup:\n+        (target / relative).unlink()\n+        info(f\"removed stamped legacy Shiki ADR: {target / relative}\")\n+\n     notes: list[NewFileNote] = []\n     for relative in TEMPLATE_PATHS:\n         source = ROOT / relative\n@@ -647,6 +741,22 @@\n     digests = target_stamp_digests(target)\n     existing = read_install_stamp(target)\n     if existing is not None:\n+        # A non-force install deliberately leaves legacy Shiki ADRs in place.\n+        # Preserve only still-matching ownership proof so a later forced\n+        # upgrade can authorize cleanup; never carry a missing or stale digest.\n+        existing_digests = existing.get(\"digests\")\n+        if isinstance(existing_digests, dict):\n+            for relative in legacy_shiki_adr_paths():\n+                expected = existing_digests.get(relative)\n+                path = target / relative\n+                if not isinstance(expected, str) or not path.is_file():\n+                    continue\n+                try:\n+                    actual = _sha256_file(path)\n+                except OSError:\n+                    continue\n+                if actual == expected:\n+                    digests[relative] = actual\n         if _incoming_is_older(commit, existing.get(\"platform_commit\")):\n             warn(\n                 \"kept existing install stamp: incoming install \"\ndiff --git a/scripts/shiki_loop.py b/scripts/shiki_loop.py\nindex 3611641da8aeb5038b1da90e3114b09a473ec6d8..84caa491b9cad0edc4176f209c13e9221e975681\n--- a/scripts/shiki_loop.py\n+++ b/scripts/shiki_loop.py\n@@ -424,7 +424,7 @@\n         # unresolvable CCA verdict has already produced its terminal stop and is never\n         # laundered into a head-moving sync (SADR-0010). It also never fires while the\n         # policy (Guardian) gate is red \u2014 for ANY reason, not only when it is the sole\n-        # red check \u2014 so a head-bound Guardian approval, or an SADR-0015\n+        # red check \u2014 so a head-bound Guardian approval, or a SADR-0015\n         # contract-approval carry that base movement defeated (the policy gate compares\n         # base and head task governance directly), is never silently restored by\n         # adopting base wholesale. It is bounded by a durable per-task counter so the\ndiff --git a/scripts/shiki_tasks.py b/scripts/shiki_tasks.py\nindex aec3809f24b4a0c8c96fc50971cff9ae9ad30381..d531052e9d9717058d4631483cefab44c26a65c1\n--- a/scripts/shiki_tasks.py\n+++ b/scripts/shiki_tasks.py\n@@ -821,7 +821,7 @@\n         \"prompt\": prompt,\n         \"entry_skill\": \"grill-with-docs\",\n         \"required_next_steps\": [\n-            \"Run grill-with-docs until terminology, boundaries, risks, and ADR-worthy decisions are settled.\",\n+            \"Run grill-with-docs until terminology, boundaries, risks, and decision-record-worthy choices are settled.\",\n             \"Obtain the operator's explicit approval of the PRD/requirements (Spec Freeze).\",\n             \"Write a machine-readable plan JSON with grill_with_docs.status=complete and spec_freeze.status=frozen.\",\n             \"Run shiki plan ingest --plan-file PLAN.json.\",\ndiff --git a/scripts/test_shiki_goal_loop.sh b/scripts/test_shiki_goal_loop.sh\nindex 753c7b516e10f0be3bc92a4bd0eb1fa699776ae6..8f3820a0a079b3044424cfe1a6c6b7fe68896e67\n--- a/scripts/test_shiki_goal_loop.sh\n+++ b/scripts/test_shiki_goal_loop.sh\n@@ -231,8 +231,8 @@\n \n # High/critical risk merges autonomously when all required checks are green:\n # the \"MergeGate policy check\" required check IS the Guardian gate (it enforces\n-# guardian-policy.json \u2014 human approval OR external AI guardian review, ADR\n-# 0010), so green checks mean a recorded authority approved. The fake gh returns\n+# guardian-policy.json \u2014 human approval OR external AI guardian review,\n+# SADR-0010), so green checks mean a recorded authority approved. The fake gh returns\n # green checks, simulating that the Guardian gate passed.\n python3 \"$ROOT/scripts/shiki.py\" goal create --target \"$TARGET\" --title \"Guardian gate\" --outcome \"High risk merges via policy gate\" >/tmp/shiki-goal-loop-guardian-goal.json\n GGOAL=\"$(json_get /tmp/shiki-goal-loop-guardian-goal.json goal_id)\"\ndiff --git a/tests/test_adr_references.py b/tests/test_adr_references.py\nindex 51706329c797d5dd9b47e082a333fa83b261d482..d005da4f771f5e8085ec22d4b787980e68f5ae8c\n--- a/tests/test_adr_references.py\n+++ b/tests/test_adr_references.py\n@@ -1,8 +1,8 @@\n-\"\"\"Every ``ADR ####`` citation must resolve to exactly one decision record.\n+\"\"\"Every target ADR and Shiki SADR citation resolves in its own namespace.\n \n Background. ``scripts/mergegate_check.py`` justifies ``is_bookkeeping_closeout``\n-with \"ADR 0017\" nine times and ``scripts/guardian_approval_signal.py`` cites\n-0016/0017 seven times, yet for a stretch the numbering in ``docs/adr/`` stopped\n+with \"SADR-0017\" nine times and ``scripts/guardian_approval_signal.py`` cites\n+SADR-0016/SADR-0017 seven times, yet for a stretch the numbering in ``docs/adr/`` stopped\n at 0015: the authoring commits lived on a coordinator branch and no task ever\n declared ``docs/adr/...`` in its locks, so nothing carried the records to the\n default branch. The stated basis of an exemption that merged seven PRs with zero\n@@ -11,11 +11,12 @@\n and the directory drifted with no signal.\n \n This is the same unbound-pair shape behind other silent failures: a reference\n-and its target with nothing asserting they agree. This test binds them. It scans\n-every ``scripts/*.py`` (plus ``docs/**/*.md`` and the root ``*.md`` governance\n-files, which are cheap) for the ``ADR ####`` pattern and asserts each cited\n-number resolves to exactly one ``docs/adr/####-*.md``. The converse is\n-deliberately NOT required: an ADR may exist without being cited.\n+and its target with nothing asserting they agree. This test binds them. Target\n+``ADR NNNN`` citations resolve only to ``docs/adr/NNNN-*.md``; Shiki\n+``SADR-NNNN`` citations resolve only to ``docs/adr/SADR-NNNN-*.md``. The scan\n+covers current governance docs, scripts, workflows, prompts, packaging metadata,\n+and repo-local Codex guidance while deliberately excluding historical\n+``.shiki`` evidence. The converse is not required: a record may be uncited.\n \n The suite is dependency free: standard-library ``unittest`` only.\n \"\"\"\n@@ -31,17 +32,18 @@\n \n REPO_ROOT = Path(__file__).resolve().parents[1]\n \n-# \"ADR 0016\" -- a four-digit citation. The trailing \\b keeps a five-digit token\n-# from being read as a truncated four-digit number.\n-_CITATION = re.compile(r\"\\bADR (\\d{4})\\b\")\n-# \"0016-locks-bound-product-paths.md\" -- the leading four digits name the record.\n-_ADR_FILENAME = re.compile(r\"^(\\d{4})-.*\\.md$\")\n+# ``ADR 0016`` / ``SADR-0016`` -- a namespace plus four-digit citation. The\n+# trailing ``\\b`` keeps a five-digit token from being read as a truncated one.\n+_CITATION = re.compile(r\"\\b(S?ADR)[ -](\\d{4})\\b\")\n+# Target ADRs retain the numeric filename; Shiki platform decisions use SADR.\n+_DECISION_FILENAME = re.compile(r\"^(?:(SADR)-)?(\\d{4})-.*\\.md$\")\n \n \n class Violation(NamedTuple):\n     \"\"\"A citation that does not resolve to exactly one decision record.\"\"\"\n \n     citing_file: str  # repo-relative path of the file that cites the ADR\n+    namespace: str  # \"ADR\" for target decisions; \"SADR\" for Shiki decisions\n     number: str  # the cited four-digit number, e.g. \"0016\"\n     line: int  # 1-based line of the citation\n     reason: str  # \"missing\" (zero matches) or \"ambiguous\" (two or more)\n@@ -49,44 +51,59 @@\n \n     @property\n     def expected_path(self) -> str:\n-        return f\"docs/adr/{self.number}-*.md\"\n+        prefix = \"SADR-\" if self.namespace == \"SADR\" else \"\"\n+        return f\"docs/adr/{prefix}{self.number}-*.md\"\n \n     def describe(self) -> str:\n         \"\"\"Name the citing file, the cited number, and the missing path.\"\"\"\n         if self.reason == \"missing\":\n             return (\n-                f\"{self.citing_file}:{self.line} cites ADR {self.number} \"\n+                f\"{self.citing_file}:{self.line} cites {self.namespace} {self.number} \"\n                 f\"but no document matches {self.expected_path}\"\n             )\n         joined = \", \".join(self.matches)\n         return (\n-            f\"{self.citing_file}:{self.line} cites ADR {self.number} which \"\n+            f\"{self.citing_file}:{self.line} cites {self.namespace} {self.number} which \"\n             f\"resolves to {len(self.matches)} documents (expected exactly one \"\n             f\"at {self.expected_path}): {joined}\"\n         )\n \n \n-def _adr_index(root: Path) -> dict[str, list[Path]]:\n-    \"\"\"Map every four-digit ADR number to the record files that claim it.\"\"\"\n-    index: dict[str, list[Path]] = {}\n+def _adr_index(root: Path) -> dict[tuple[str, str], list[Path]]:\n+    \"\"\"Map each decision namespace and number to the files that claim it.\"\"\"\n+    index: dict[tuple[str, str], list[Path]] = {}\n     adr_dir = root / \"docs\" / \"adr\"\n     if adr_dir.is_dir():\n         for path in sorted(adr_dir.glob(\"*.md\")):\n-            match = _ADR_FILENAME.match(path.name)\n+            match = _DECISION_FILENAME.match(path.name)\n             if match:\n-                index.setdefault(match.group(1), []).append(path)\n+                namespace = \"SADR\" if match.group(1) else \"ADR\"\n+                index.setdefault((namespace, match.group(2)), []).append(path)\n     return index\n \n \n def _citing_files(root: Path, include_docs: bool = True) -> list[Path]:\n-    \"\"\"Files scanned for citations: scripts/*.py, and cheaply the markdown.\"\"\"\n+    \"\"\"Current, shipped text surfaces scanned for decision citations.\"\"\"\n     files = sorted((root / \"scripts\").glob(\"*.py\"))\n+    files += sorted((root / \"scripts\").glob(\"*.sh\"))\n     if include_docs:\n         docs = root / \"docs\"\n         if docs.is_dir():\n             files += sorted(docs.rglob(\"*.md\"))\n         files += sorted(root.glob(\"*.md\"))\n-    return files\n+        pyproject = root / \"pyproject.toml\"\n+        if pyproject.is_file():\n+            files.append(pyproject)\n+        for relative in (\".github\", \".codex\"):\n+            surface = root / relative\n+            if surface.is_dir():\n+                files += sorted(\n+                    path\n+                    for path in surface.rglob(\"*\")\n+                    if path.is_file()\n+                    and path.suffix in {\".md\", \".py\", \".sh\", \".toml\", \".yaml\", \".yml\"}\n+                )\n+    return sorted(set(files))\n \n \n def find_adr_reference_violations(\n@@ -94,10 +111,9 @@\n ) -> list[Violation]:\n     \"\"\"Return every ``ADR ####`` citation under ``root`` that fails to resolve.\n \n-    A citation resolves when exactly one ``docs/adr/####-*.md`` matches its\n-    number. Zero matches is a \"missing\" violation; two or more is \"ambiguous\".\n-    Records that are never cited produce nothing -- the converse is not\n-    required.\n+    A citation resolves when exactly one record in the same namespace matches\n+    its number. Zero matches is a \"missing\" violation; two or more is\n+    \"ambiguous\". Records that are never cited produce nothing.\n     \"\"\"\n     root = Path(root)\n     index = _adr_index(root)\n@@ -110,13 +126,15 @@\n         rel = path.relative_to(root).as_posix()\n         for lineno, line in enumerate(text.splitlines(), start=1):\n             for match in _CITATION.finditer(line):\n-                number = match.group(1)\n-                found = index.get(number, [])\n+                namespace, number = match.groups()\n+                found = index.get((namespace, number), [])\n                 if len(found) == 1:\n                     continue\n                 reason = \"missing\" if not found else \"ambiguous\"\n                 rels = [p.relative_to(root).as_posix() for p in found]\n-                violations.append(Violation(rel, number, lineno, reason, rels))\n+                violations.append(\n+                    Violation(rel, namespace, number, lineno, reason, rels)\n+                )\n     return violations\n \n \n@@ -129,8 +147,8 @@\n         self.assertEqual(\n             violations,\n             [],\n-            \"Every 'ADR ####' citation must resolve to exactly one \"\n-            f\"docs/adr/####-*.md:\\n{detail}\",\n+            \"Every ADR/SADR citation must resolve exactly once in its own \"\n+            f\"docs/adr namespace:\\n{detail}\",\n         )\n \n     def test_scan_actually_reads_scripts(self) -> None:\n@@ -142,12 +160,38 @@\n             \"the citation scan must cover scripts/*.py\",\n         )\n \n+    def test_platform_citations_do_not_use_unresolved_numeric_shorthand(self) -> None:\n+        shorthand = re.compile(r\"SADR-\\d{4}/\\d{4}\")\n+        offenders = []\n+        for path in _citing_files(REPO_ROOT):\n+            text = path.read_text(encoding=\"utf-8\")\n+            if shorthand.search(text):\n+                offenders.append(path.relative_to(REPO_ROOT).as_posix())\n+        self.assertEqual(\n+            offenders,\n+            [],\n+            \"repeat the SADR prefix so every cited number is independently resolvable\",\n+        )\n+\n     def test_carried_records_are_indexed(self) -> None:\n         # The two records this Goal carried onto the default branch, and which\n         # the scripts cite most, must be present.\n         index = _adr_index(REPO_ROOT)\n-        self.assertIn(\"0016\", index)\n-        self.assertIn(\"0017\", index)\n+        self.assertIn((\"SADR\", \"0016\"), index)\n+        self.assertIn((\"SADR\", \"0017\"), index)\n+\n+    def test_platform_tree_has_only_sadr_0001_through_0019(self) -> None:\n+        adr_dir = REPO_ROOT / \"docs\" / \"adr\"\n+        sadrs = sorted(adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\"))\n+        self.assertEqual(\n+            {path.name.split(\"-\", 2)[1] for path in sadrs},\n+            {f\"{number:04d}\" for number in range(1, 20)},\n+        )\n+        self.assertEqual(\n+            list(adr_dir.glob(\"[0-9][0-9][0-9][0-9]-*.md\")),\n+            [],\n+            \"the Shiki platform tree must not retain legacy numeric SADR paths\",\n+        )\n \n \n class FixtureAdrReferences(unittest.TestCase):\n@@ -180,6 +224,26 @@\n         self.assertIn(\"9991\", message)  # cited number\n         self.assertIn(\"docs/adr/9991-*.md\", message)  # missing path\n \n+    def test_target_adr_does_not_satisfy_missing_platform_sadr(self) -> None:\n+        root = self._make_repo(\n+            adr_names=[\"0042-target-decision.md\"],\n+            script_body='\"\"\"Target ADR 0042 exists, but platform SADR-0042 does not.\"\"\"\\n',\n+        )\n+        violations = find_adr_reference_violations(root, include_docs=False)\n+        self.assertEqual(len(violations), 1)\n+        self.assertEqual(violations[0].namespace, \"SADR\")\n+        self.assertEqual(violations[0].number, \"0042\")\n+        self.assertEqual(violations[0].expected_path, \"docs/adr/SADR-0042-*.md\")\n+\n+    def test_target_adr_and_platform_sadr_may_reuse_a_number(self) -> None:\n+        root = self._make_repo(\n+            adr_names=[\"0042-target-decision.md\", \"SADR-0042-platform-decision.md\"],\n+            script_body='\"\"\"Target ADR 0042 and platform SADR-0042 are distinct.\"\"\"\\n',\n+        )\n+        self.assertEqual(\n+            find_adr_reference_violations(root, include_docs=False), []\n+        )\n+\n     def test_uncited_adr_is_allowed(self) -> None:\n         # An ADR exists but nothing cites it -> not a violation. The converse\n         # (every ADR must be cited) is explicitly NOT required.\ndiff --git a/tests/test_guardian_review.py b/tests/test_guardian_review.py\nindex 88829e7810212635e2fc98aa5918d6f26294677d..00a85a237acd5bf2d6cb5b294128198c422402ca\n--- a/tests/test_guardian_review.py\n+++ b/tests/test_guardian_review.py\n@@ -162,7 +162,7 @@\n \n     def test_docs_only_is_light_and_not_conservative(self):\n         focus = gr.classify_review_focus([\"docs/adr/0099-x.md\", \"README.md\"], \"\", \"low\")\n-        self.assertTrue(any(\"Docs/ADR-only\" in f for f in focus))\n+        self.assertTrue(any(\"Docs/decision-record-only\" in f for f in focus))\n         self.assertFalse(any(\"conservative default\" in f.lower() for f in focus))\n \n     def test_unknown_category_is_conservative(self):\ndiff --git a/tests/test_policy_job_merged_prs.py b/tests/test_policy_job_merged_prs.py\nindex d45512e74681e5e649279236c7d4e79d5c3da471..b97989f8bd74d091de1a6b5275a20d8dc68c4512\n--- a/tests/test_policy_job_merged_prs.py\n+++ b/tests/test_policy_job_merged_prs.py\n@@ -3,8 +3,8 @@\n \n ``.github/workflows/shiki-cca-completion.yml`` has two consumers of the merged-PR\n proof: the CCA *signal* (``guardian_approval_signal.py`` in \"Prepare CCA context\")\n-and the authoritative gate (``mergegate_check.py`` in \"Enforce MergeGate\"). ADR\n-0017's bookkeeping-closeout exemption (condition 6) needs the merged implementation\n+and the authoritative gate (``mergegate_check.py`` in \"Enforce MergeGate\").\n+SADR-0017's bookkeeping-closeout exemption (condition 6) needs the merged implementation\n PR proven, and a closeout carries NO ``post_merge_reconcile`` marker. The signal\n already resolves ``MERGED_PRS`` unconditionally, but \"Enforce MergeGate\" used to\n resolve it only inside ``grep -q '<!-- shiki:post_merge_reconcile -->'``. So a\ndiff --git a/tests/test_sadr_install_migration.py b/tests/test_sadr_install_migration.py\nnew file mode 100644\nindex 0000000000000000000000000000000000000000..a6f4c57e4dcaef73ef600b0d4804744138ab9071\n--- /dev/null\n+++ b/tests/test_sadr_install_migration.py\n@@ -0,0 +1,229 @@\n+\"\"\"SADR installation and collision-safe legacy upgrade behavior.\"\"\"\n+\n+from __future__ import annotations\n+\n+import contextlib\n+import hashlib\n+import io\n+import json\n+import shutil\n+import tempfile\n+import unittest\n+from pathlib import Path\n+\n+import shiki_test_support  # noqa: F401  (path bootstrap)\n+\n+import shiki_installer\n+from tests.test_adr_references import find_adr_reference_violations\n+\n+\n+PRODUCT_NUMBERS = tuple(range(13, 19))\n+\n+\n+def _quiet_install(target: Path, *, force: bool, validate: bool) -> None:\n+    with contextlib.redirect_stdout(io.StringIO()):\n+        shiki_installer.install_template(target, force=force, validate=validate)\n+\n+\n+def _sha256(path: Path) -> str:\n+    return hashlib.sha256(path.read_bytes()).hexdigest()\n+\n+\n+def _legacy_sadr_paths() -> tuple[str, ...]:\n+    return tuple(\n+        f\"docs/adr/{path.name.removeprefix('SADR-')}\"\n+        for path in sorted((shiki_installer.ROOT / \"docs\" / \"adr\").glob(\"SADR-*.md\"))\n+        if path.name != \"SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\"\n+    )\n+\n+\n+def _create_legacy_upgrade_fixture(\n+    target: Path,\n+) -> tuple[dict[str, bytes], tuple[str, ...]]:\n+    adr_dir = target / \"docs\" / \"adr\"\n+    adr_dir.mkdir(parents=True)\n+    product_bytes = {\n+        f\"{number:04d}-cubeshare-product-decision.md\": (\n+            f\"# CubeShare ADR {number:04d}\\n\\nDo not rewrite {number}.\\n\"\n+        ).encode()\n+        for number in PRODUCT_NUMBERS\n+    }\n+    for name, content in product_bytes.items():\n+        (adr_dir / name).write_bytes(content)\n+\n+    legacy_paths = _legacy_sadr_paths()\n+    for relative in legacy_paths:\n+        (target / relative).write_bytes(f\"legacy Shiki bytes for {relative}\\n\".encode())\n+\n+    stamp_path = target / shiki_installer.INSTALL_STAMP_PATH\n+    stamp_path.parent.mkdir(parents=True, exist_ok=True)\n+    stamp = {\n+        \"version\": shiki_installer.INSTALL_STAMP_VERSION,\n+        \"platform_commit\": None,\n+        \"installed_at\": \"2000-01-01T00:00:00+00:00\",\n+        \"digests\": {\n+            relative: _sha256(target / relative) for relative in legacy_paths\n+        },\n+    }\n+    stamp_path.write_text(\n+        json.dumps(stamp, indent=2, sort_keys=True) + \"\\n\", encoding=\"utf-8\"\n+    )\n+    return product_bytes, legacy_paths\n+\n+\n+def _tree_snapshot(root: Path) -> dict[str, tuple[str, bytes | str]]:\n+    \"\"\"Recursive byte/shape snapshot used to prove preflight is write-free.\"\"\"\n+    snapshot: dict[str, tuple[str, bytes | str]] = {}\n+    for path in sorted(root.rglob(\"*\")):\n+        relative = path.relative_to(root).as_posix()\n+        if path.is_symlink():\n+            snapshot[relative] = (\"symlink\", path.readlink().as_posix())\n+        elif path.is_dir():\n+            snapshot[relative] = (\"directory\", \"\")\n+        else:\n+            snapshot[relative] = (\"file\", path.read_bytes())\n+    return snapshot\n+\n+\n+class FreshSadrInstallTests(unittest.TestCase):\n+    def setUp(self) -> None:\n+        self.tmp = Path(tempfile.mkdtemp(prefix=\"shiki-sadr-fresh-\"))\n+        self.addCleanup(lambda: shutil.rmtree(self.tmp, ignore_errors=True))\n+        self.target = self.tmp / \"target\"\n+        self.adr_dir = self.target / \"docs\" / \"adr\"\n+        self.adr_dir.mkdir(parents=True)\n+        self.product_bytes = {\n+            f\"{number:04d}-product-decision.md\": (\n+                f\"# Product ADR {number:04d}\\n\\nTarget-owned bytes {number}.\\n\"\n+            ).encode()\n+            for number in PRODUCT_NUMBERS\n+        }\n+        for name, content in self.product_bytes.items():\n+            (self.adr_dir / name).write_bytes(content)\n+\n+    def test_fresh_install_preserves_product_adrs_and_installs_all_sadrs(self) -> None:\n+        _quiet_install(self.target, force=False, validate=True)\n+\n+        for name, expected in self.product_bytes.items():\n+            self.assertEqual((self.adr_dir / name).read_bytes(), expected)\n+\n+        sadr_numbers = {\n+            path.name.split(\"-\", 2)[1]\n+            for path in self.adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\")\n+        }\n+        self.assertEqual(sadr_numbers, {f\"{number:04d}\" for number in range(1, 20)})\n+\n+        numeric_records = {\n+            path.name\n+            for path in self.adr_dir.glob(\"[0-9][0-9][0-9][0-9]-*.md\")\n+        }\n+        self.assertEqual(numeric_records, set(self.product_bytes))\n+        self.assertEqual(find_adr_reference_violations(self.target), [])\n+\n+\n+class LegacySadrUpgradeTests(unittest.TestCase):\n+    def setUp(self) -> None:\n+        self.tmp = Path(tempfile.mkdtemp(prefix=\"shiki-sadr-upgrade-\"))\n+        self.addCleanup(lambda: shutil.rmtree(self.tmp, ignore_errors=True))\n+        self.target = self.tmp / \"target\"\n+        self.adr_dir = self.target / \"docs\" / \"adr\"\n+        self.product_bytes, self.legacy_paths = _create_legacy_upgrade_fixture(\n+            self.target\n+        )\n+\n+    def test_force_upgrade_deletes_only_stamped_legacy_sadrs(self) -> None:\n+        _quiet_install(self.target, force=True, validate=True)\n+\n+        for relative in self.legacy_paths:\n+            self.assertFalse((self.target / relative).exists(), relative)\n+        for name, expected in self.product_bytes.items():\n+            self.assertEqual((self.adr_dir / name).read_bytes(), expected)\n+\n+        sadrs = list(self.adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\"))\n+        self.assertEqual(len(sadrs), 19)\n+        refreshed = json.loads(\n+            (self.target / shiki_installer.INSTALL_STAMP_PATH).read_text(encoding=\"utf-8\")\n+        )\n+        for relative in self.legacy_paths:\n+            self.assertNotIn(relative, refreshed[\"digests\"])\n+        for path in sadrs:\n+            relative = path.relative_to(self.target).as_posix()\n+            self.assertEqual(refreshed[\"digests\"][relative], _sha256(path))\n+\n+    def test_non_force_install_never_deletes_legacy_files(self) -> None:\n+        before = {\n+            relative: (self.target / relative).read_bytes()\n+            for relative in self.legacy_paths\n+        }\n+        _quiet_install(self.target, force=False, validate=False)\n+        for relative, expected in before.items():\n+            self.assertEqual((self.target / relative).read_bytes(), expected)\n+        refreshed = json.loads(\n+            (self.target / shiki_installer.INSTALL_STAMP_PATH).read_text(encoding=\"utf-8\")\n+        )\n+        for relative in self.legacy_paths:\n+            self.assertEqual(refreshed[\"digests\"][relative], _sha256(self.target / relative))\n+\n+\n+class LegacyCleanupPreflightTests(unittest.TestCase):\n+    def setUp(self) -> None:\n+        self.tmp = Path(tempfile.mkdtemp(prefix=\"shiki-sadr-preflight-\"))\n+        self.addCleanup(lambda: shutil.rmtree(self.tmp, ignore_errors=True))\n+\n+    def test_all_ownership_blockers_abort_before_any_write(self) -> None:\n+        cases = (\n+            (\"absent\", \"install stamp is absent\"),\n+            (\"unreadable\", \"install stamp is unreadable\"),\n+            (\"malformed\", \"install stamp is malformed\"),\n+            (\"missing-digest\", \"install stamp has no digest\"),\n+            (\"digest-mismatch\", \"digest mismatch\"),\n+        )\n+        for name, expected_reason in cases:\n+            with self.subTest(name=name):\n+                target = self.tmp / name\n+                _, legacy_paths = _create_legacy_upgrade_fixture(target)\n+                stamp_path = target / shiki_installer.INSTALL_STAMP_PATH\n+\n+                if name == \"absent\":\n+                    stamp_path.unlink()\n+                elif name == \"unreadable\":\n+                    stamp_path.unlink()\n+                    stamp_path.mkdir()\n+                elif name == \"malformed\":\n+                    stamp_path.write_text(\"{not-json\\n\", encoding=\"utf-8\")\n+                else:\n+                    stamp = json.loads(stamp_path.read_text(encoding=\"utf-8\"))\n+                    if name == \"missing-digest\":\n+                        stamp[\"digests\"] = {}\n+                    else:\n+                        stamp[\"digests\"] = {\n+                            relative: \"deadbeef\" for relative in legacy_paths\n+                        }\n+                    stamp_path.write_text(\n+                        json.dumps(stamp, indent=2, sort_keys=True) + \"\\n\",\n+                        encoding=\"utf-8\",\n+                    )\n+\n+                before = _tree_snapshot(target)\n+                with self.assertRaises(shiki_installer.ShikiError) as raised:\n+                    _quiet_install(target, force=True, validate=False)\n+                after = _tree_snapshot(target)\n+\n+                self.assertEqual(before, after, f\"{name} preflight changed the tree\")\n+                message = str(raised.exception)\n+                self.assertIn(expected_reason, message)\n+                self.assertIn(\"No files were written\", message)\n+                for relative in legacy_paths:\n+                    self.assertIn(relative, message)\n+\n+    def test_absent_stamp_is_allowed_when_no_legacy_path_exists(self) -> None:\n+        target = self.tmp / \"no-legacy\"\n+        target.mkdir()\n+        _quiet_install(target, force=True, validate=False)\n+        self.assertTrue(\n+            (target / \"docs\" / \"adr\" / \"SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\").is_file()\n+        )\n+\n+\n+if __name__ == \"__main__\":\n+    unittest.main()\n\n2026-08-07T08:21:13.028629Z ERROR codex_models_manager::manager: failed to renew cache TTL: missing field `base_instructions` at line 94 column 5\n succeeded in 116ms:\nok\ntest_holds (tests.test_paired_invariants.Pair12ChecklistIds.test_holds) ... ok\ntest_divergence_is_detected (tests.test_paired_invariants.Pair13GuardianIdentity.test_divergence_is_detected) ... ok\ntest_holds (tests.test_paired_invariants.Pair13GuardianIdentity.test_holds) ... ok\ntest_divergence_is_detected (tests.test_paired_invariants.Pair14SmokeStartRequired.test_divergence_is_detected) ... ok\ntest_holds (tests.test_paired_invariants.Pair14SmokeStartRequired.test_holds) ... ok\n\n----------------------------------------------------------------------\nRan 79 tests in 0.054s\n\nOK\n\ncodex\nThe verification gates are green and the implementation is now entering the required high-effort `code-review` skill gate. I\u2019m reviewing the complete tracked-plus-untracked change set against locks, data-loss/TOCTOU behavior, namespace resolution, installer stamp semantics, and test completeness. Any blocking finding will be repaired and the full gate rerun before I record the structured verdict.\nexec\n/bin/zsh -lc \"sed -n '1,280p' tests/test_sadr_install_migration.py\" in /private/tmp/shiki-adr-collision.pa0KwY/.worktrees/shiki-t-20260807t073307221254z-2d22d654-introduc\n succeeded in 0ms:\n\"\"\"SADR installation and collision-safe legacy upgrade behavior.\"\"\"\n\nfrom __future__ import annotations\n\nimport contextlib\nimport hashlib\nimport io\nimport json\nimport shutil\nimport tempfile\nimport unittest\nfrom pathlib import Path\n\nimport shiki_test_support  # noqa: F401  (path bootstrap)\n\nimport shiki_installer\nfrom tests.test_adr_references import find_adr_reference_violations\n\n\nPRODUCT_NUMBERS = tuple(range(13, 19))\n\n\ndef _quiet_install(target: Path, *, force: bool, validate: bool) -> None:\n    with contextlib.redirect_stdout(io.StringIO()):\n        shiki_installer.install_template(target, force=force, validate=validate)\n\n\ndef _sha256(path: Path) -> str:\n    return hashlib.sha256(path.read_bytes()).hexdigest()\n\n\ndef _legacy_sadr_paths() -> tuple[str, ...]:\n    return tuple(\n        f\"docs/adr/{path.name.removeprefix('SADR-')}\"\n        for path in sorted((shiki_installer.ROOT / \"docs\" / \"adr\").glob(\"SADR-*.md\"))\n        if path.name != \"SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\"\n    )\n\n\ndef _create_legacy_upgrade_fixture(\n    target: Path,\n) -> tuple[dict[str, bytes], tuple[str, ...]]:\n    adr_dir = target / \"docs\" / \"adr\"\n    adr_dir.mkdir(parents=True)\n    product_bytes = {\n        f\"{number:04d}-cubeshare-product-decision.md\": (\n            f\"# CubeShare ADR {number:04d}\\n\\nDo not rewrite {number}.\\n\"\n        ).encode()\n        for number in PRODUCT_NUMBERS\n    }\n    for name, content in product_bytes.items():\n        (adr_dir / name).write_bytes(content)\n\n    legacy_paths = _legacy_sadr_paths()\n    for relative in legacy_paths:\n        (target / relative).write_bytes(f\"legacy Shiki bytes for {relative}\\n\".encode())\n\n    stamp_path = target / shiki_installer.INSTALL_STAMP_PATH\n    stamp_path.parent.mkdir(parents=True, exist_ok=True)\n    stamp = {\n        \"version\": shiki_installer.INSTALL_STAMP_VERSION,\n        \"platform_commit\": None,\n        \"installed_at\": \"2000-01-01T00:00:00+00:00\",\n        \"digests\": {\n            relative: _sha256(target / relative) for relative in legacy_paths\n        },\n    }\n    stamp_path.write_text(\n        json.dumps(stamp, indent=2, sort_keys=True) + \"\\n\", encoding=\"utf-8\"\n    )\n    return product_bytes, legacy_paths\n\n\ndef _tree_snapshot(root: Path) -> dict[str, tuple[str, bytes | str]]:\n    \"\"\"Recursive byte/shape snapshot used to prove preflight is write-free.\"\"\"\n    snapshot: dict[str, tuple[str, bytes | str]] = {}\n    for path in sorted(root.rglob(\"*\")):\n        relative = path.relative_to(root).as_posix()\n        if path.is_symlink():\n            snapshot[relative] = (\"symlink\", path.readlink().as_posix())\n        elif path.is_dir():\n            snapshot[relative] = (\"directory\", \"\")\n        else:\n            snapshot[relative] = (\"file\", path.read_bytes())\n    return snapshot\n\n\nclass FreshSadrInstallTests(unittest.TestCase):\n    def setUp(self) -> None:\n        self.tmp = Path(tempfile.mkdtemp(prefix=\"shiki-sadr-fresh-\"))\n        self.addCleanup(lambda: shutil.rmtree(self.tmp, ignore_errors=True))\n        self.target = self.tmp / \"target\"\n        self.adr_dir = self.target / \"docs\" / \"adr\"\n        self.adr_dir.mkdir(parents=True)\n        self.product_bytes = {\n            f\"{number:04d}-product-decision.md\": (\n                f\"# Product ADR {number:04d}\\n\\nTarget-owned bytes {number}.\\n\"\n            ).encode()\n            for number in PRODUCT_NUMBERS\n        }\n        for name, content in self.product_bytes.items():\n            (self.adr_dir / name).write_bytes(content)\n\n    def test_fresh_install_preserves_product_adrs_and_installs_all_sadrs(self) -> None:\n        _quiet_install(self.target, force=False, validate=True)\n\n        for name, expected in self.product_bytes.items():\n            self.assertEqual((self.adr_dir / name).read_bytes(), expected)\n\n        sadr_numbers = {\n            path.name.split(\"-\", 2)[1]\n            for path in self.adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\")\n        }\n        self.assertEqual(sadr_numbers, {f\"{number:04d}\" for number in range(1, 20)})\n\n        numeric_records = {\n            path.name\n            for path in self.adr_dir.glob(\"[0-9][0-9][0-9][0-9]-*.md\")\n        }\n        self.assertEqual(numeric_records, set(self.product_bytes))\n        self.assertEqual(find_adr_reference_violations(self.target), [])\n\n\nclass LegacySadrUpgradeTests(unittest.TestCase):\n    def setUp(self) -> None:\n        self.tmp = Path(tempfile.mkdtemp(prefix=\"shiki-sadr-upgrade-\"))\n        self.addCleanup(lambda: shutil.rmtree(self.tmp, ignore_errors=True))\n        self.target = self.tmp / \"target\"\n        self.adr_dir = self.target / \"docs\" / \"adr\"\n        self.product_bytes, self.legacy_paths = _create_legacy_upgrade_fixture(\n            self.target\n        )\n\n    def test_force_upgrade_deletes_only_stamped_legacy_sadrs(self) -> None:\n        _quiet_install(self.target, force=True, validate=True)\n\n        for relative in self.legacy_paths:\n            self.assertFalse((self.target / relative).exists(), relative)\n        for name, expected in self.product_bytes.items():\n            self.assertEqual((self.adr_dir / name).read_bytes(), expected)\n\n        sadrs = list(self.adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\"))\n        self.assertEqual(len(sadrs), 19)\n        refreshed = json.loads(\n            (self.target / shiki_installer.INSTALL_STAMP_PATH).read_text(encoding=\"utf-8\")\n        )\n        for relative in self.legacy_paths:\n            self.assertNotIn(relative, refreshed[\"digests\"])\n        for path in sadrs:\n            relative = path.relative_to(self.target).as_posix()\n            self.assertEqual(refreshed[\"digests\"][relative], _sha256(path))\n\n    def test_non_force_install_never_deletes_legacy_files(self) -> None:\n        before = {\n            relative: (self.target / relative).read_bytes()\n            for relative in self.legacy_paths\n        }\n        _quiet_install(self.target, force=False, validate=False)\n        for relative, expected in before.items():\n            self.assertEqual((self.target / relative).read_bytes(), expected)\n        refreshed = json.loads(\n            (self.target / shiki_installer.INSTALL_STAMP_PATH).read_text(encoding=\"utf-8\")\n        )\n        for relative in self.legacy_paths:\n            self.assertEqual(refreshed[\"digests\"][relative], _sha256(self.target / relative))\n\n\nclass LegacyCleanupPreflightTests(unittest.TestCase):\n    def setUp(self) -> None:\n        self.tmp = Path(tempfile.mkdtemp(prefix=\"shiki-sadr-preflight-\"))\n        self.addCleanup(lambda: shutil.rmtree(self.tmp, ignore_errors=True))\n\n    def test_all_ownership_blockers_abort_before_any_write(self) -> None:\n        cases = (\n            (\"absent\", \"install stamp is absent\"),\n            (\"unreadable\", \"install stamp is unreadable\"),\n            (\"malformed\", \"install stamp is malformed\"),\n            (\"missing-digest\", \"install stamp has no digest\"),\n            (\"digest-mismatch\", \"digest mismatch\"),\n        )\n        for name, expected_reason in cases:\n            with self.subTest(name=name):\n                target = self.tmp / name\n                _, legacy_paths = _create_legacy_upgrade_fixture(target)\n                stamp_path = target / shiki_installer.INSTALL_STAMP_PATH\n\n                if name == \"absent\":\n                    stamp_path.unlink()\n                elif name == \"unreadable\":\n                    stamp_path.unlink()\n                    stamp_path.mkdir()\n                elif name == \"malformed\":\n                    stamp_path.write_text(\"{not-json\\n\", encoding=\"utf-8\")\n                else:\n                    stamp = json.loads(stamp_path.read_text(encoding=\"utf-8\"))\n                    if name == \"missing-digest\":\n                        stamp[\"digests\"] = {}\n                    else:\n                        stamp[\"digests\"] = {\n                            relative: \"deadbeef\" for relative in legacy_paths\n                        }\n                    stamp_path.write_text(\n                        json.dumps(stamp, indent=2, sort_keys=True) + \"\\n\",\n                        encoding=\"utf-8\",\n                    )\n\n                before = _tree_snapshot(target)\n                with self.assertRaises(shiki_installer.ShikiError) as raised:\n                    _quiet_install(target, force=True, validate=False)\n                after = _tree_snapshot(target)\n\n                self.assertEqual(before, after, f\"{name} preflight changed the tree\")\n                message = str(raised.exception)\n                self.assertIn(expected_reason, message)\n                self.assertIn(\"No files were written\", message)\n                for relative in legacy_paths:\n                    self.assertIn(relative, message)\n\n    def test_absent_stamp_is_allowed_when_no_legacy_path_exists(self) -> None:\n        target = self.tmp / \"no-legacy\"\n        target.mkdir()\n        _quiet_install(target, force=True, validate=False)\n        self.assertTrue(\n            (target / \"docs\" / \"adr\" / \"SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\").is_file()\n        )\n\n\nif __name__ == \"__main__\":\n    unittest.main()\n\nexec\n/bin/zsh -lc \"sed -n '220,370p' scripts/shiki_installer.py; sed -n '630,715p' scripts/shiki_installer.py\" in /private/tmp/shiki-adr-collision.pa0KwY/.worktrees/shiki-t-20260807t073307221254z-2d22d654-introduc\n succeeded in 0ms:\n\n\ndef refuse_pending_migrations(target: Path) -> None:\n    \"\"\"Precondition run BEFORE any file write.\n\n    Refuse to upgrade a target whose valid ``.shiki`` migration state is behind\n    the current registry, so a target with pending migrations keeps an untouched\n    tree instead of being half-rewritten and then aborting. Only the \"valid but\n    behind\" upgrade case is gated: a fresh target has no state file yet (it is\n    seeded by this install), and a target whose state file is absent or malformed\n    is left to post-write validation, which surfaces malformed state.\n    \"\"\"\n    status = migration_status(target)\n    if status[\"state_exists\"] and not status[\"errors\"] and status[\"pending\"]:\n        pending = \", \".join(status[\"pending\"])\n        raise ShikiError(\n            f\"target has pending .shiki migrations ({pending}); run \"\n            \"`shiki migrate apply --execute` before upgrading. No files were written.\"\n        )\n\n\ndef legacy_shiki_adr_paths() -> tuple[str, ...]:\n    \"\"\"Former numeric paths for Shiki SADR-0001 through SADR-0018.\n\n    The authoritative slugs come from the current SADR filenames. SADR-0019 is\n    the namespace decision itself and never shipped under a numeric legacy path.\n    \"\"\"\n    paths: list[str] = []\n    for sadr in sorted((ROOT / \"docs\" / \"adr\").glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\")):\n        number = sadr.name.split(\"-\", 2)[1]\n        if int(number) <= LEGACY_SHIKI_SADR_MAX:\n            paths.append(f\"docs/adr/{sadr.name.removeprefix('SADR-')}\")\n    return tuple(paths)\n\n\ndef preflight_legacy_sadr_cleanup(target: Path, *, force: bool) -> tuple[str, ...]:\n    \"\"\"Authorize exact legacy Shiki ADR removals before the first write.\n\n    A non-force install never removes existing files. A forced install needs no\n    stamp when no legacy Shiki path is present. When legacy paths do exist, each\n    must be named in a readable install stamp and its current bytes must match\n    the recorded digest.\n    \"\"\"\n    if not force:\n        return ()\n    present = tuple(\n        relative\n        for relative in legacy_shiki_adr_paths()\n        if (target / relative).exists() or (target / relative).is_symlink()\n    )\n    if not present:\n        return ()\n\n    stamp_path = target / INSTALL_STAMP_PATH\n    try:\n        raw = stamp_path.read_text(encoding=\"utf-8\")\n        stamp = json.loads(raw)\n    except FileNotFoundError:\n        detail = \"install stamp is absent\"\n        blockers = [f\"{relative}: {detail}\" for relative in present]\n    except (OSError, UnicodeDecodeError) as error:\n        detail = f\"install stamp is unreadable ({error})\"\n        blockers = [f\"{relative}: {detail}\" for relative in present]\n    except json.JSONDecodeError as error:\n        detail = f\"install stamp is malformed ({error})\"\n        blockers = [f\"{relative}: {detail}\" for relative in present]\n    else:\n        if not isinstance(stamp, dict):\n            blockers = [\n                f\"{relative}: install stamp is malformed (expected an object)\"\n                for relative in present\n            ]\n        else:\n            digests = stamp.get(\"digests\")\n            blockers = []\n            if not isinstance(digests, dict):\n                blockers.extend(\n                    f\"{relative}: install stamp has no digest for this path\"\n                    for relative in present\n                )\n            else:\n                for relative in present:\n                    expected = digests.get(relative)\n                    if not isinstance(expected, str) or not expected:\n                        blockers.append(\n                            f\"{relative}: install stamp has no digest for this path\"\n                        )\n                        continue\n                    try:\n                        actual = _sha256_file(target / relative)\n                    except OSError as error:\n                        blockers.append(\n                            f\"{relative}: legacy path is unreadable ({error})\"\n                        )\n                        continue\n                    if actual != expected:\n                        blockers.append(\n                            f\"{relative}: digest mismatch (stamp {expected}, current {actual})\"\n                        )\n\n    if blockers:\n        joined = \"\\n\".join(f\"- {blocker}\" for blocker in blockers)\n        raise ShikiError(\n            \"legacy Shiki ADR cleanup is not authorized:\\n\"\n            f\"{joined}\\nNo files were written.\"\n        )\n    return present\n\n\ndef install_template(target: Path, *, force: bool, validate: bool) -> list[NewFileNote]:\n    # Preconditions first: never rewrite the tree of a target that cannot be\n    # upgraded cleanly (see refuse_pending_migrations).\n    legacy_cleanup = preflight_legacy_sadr_cleanup(target, force=force)\n    refuse_pending_migrations(target)\n\n    # Resolve the target's required CODEOWNERS owner once from its\n    # ``.shiki/repo.json`` (present when a target already knows its GitHub\n    # identity). The shipped ``.github/CODEOWNERS`` names this maintainer on every\n    # line, so a foreign target's copy is rewritten to name its own owner; when no\n    # owner can be resolved this is the documented fallback and the file is copied\n    # verbatim. See ``codeowners_required_owner``.\n    codeowners_owner = codeowners_required_owner(target)\n\n    for relative in legacy_cleanup:\n        (target / relative).unlink()\n        info(f\"removed stamped legacy Shiki ADR: {target / relative}\")\n\n    notes: list[NewFileNote] = []\n    for relative in TEMPLATE_PATHS:\n        source = ROOT / relative\n        if not source.exists():\n            warn(f\"template path missing, skipped: {relative}\")\n            continue\n        copy_path(\n            source,\n            target / relative,\n            force=force,\n            target_install=True,\n            notes=notes,\n            codeowners_owner=codeowners_owner,\n        )\n\n    manifest = load_manifest(ROOT)\n    directories = manifest_directories(manifest)\n    for relative in manifest_create_directories(manifest):\n        state_dir = target / relative\n        state_dir.mkdir(parents=True, exist_ok=True)\n        metadata = directories.get(relative, {})\n        if metadata.get(\"tracked\") is True and metadata.get(\"required\") is True and not any(state_dir.iterdir()):\n            (state_dir / \".gitkeep\").write_text(\"\", encoding=\"utf-8\")\n        info(f\"ensured empty state directory: {state_dir}\")\n                collect(child)\n            return\n        relative = template_relative(source)\n        if relative is None or not _is_stamped_shipped(relative):\n            return\n        shipped.append(relative)\n\n    for relative in TEMPLATE_PATHS:\n        source = ROOT / relative\n        if source.exists():\n            collect(source)\n    return sorted(set(shipped))\n\n\ndef target_stamp_digests(target: Path) -> dict[str, str]:\n    \"\"\"Content digest for every stamped shipped path present in ``target``.\"\"\"\n    return {\n        relative: _sha256_file(target / relative)\n        for relative in shipped_stamp_paths()\n        if (target / relative).is_file()\n    }\n\n\ndef platform_commit(root: Path = ROOT) -> str | None:\n    \"\"\"The git HEAD of the running platform checkout, or None when unavailable.\"\"\"\n    result = subprocess.run(\n        [\"git\", \"-C\", str(root), \"rev-parse\", \"HEAD\"],\n        capture_output=True,\n        text=True,\n        check=False,\n    )\n    commit = result.stdout.strip()\n    return commit if result.returncode == 0 and commit else None\n\n\ndef commit_is_ancestor(root: Path, ancestor: str, descendant: str) -> bool | None:\n    \"\"\"True if ``ancestor`` is an ancestor of ``descendant`` in ``root``'s git.\n\n    None when the answer cannot be determined (git missing, or a commit unknown\n    to this checkout \u2014 which is itself the \"different lineage\" signal).\n    \"\"\"\n    if not ancestor or not descendant:\n        return None\n    result = subprocess.run(\n        [\"git\", \"-C\", str(root), \"merge-base\", \"--is-ancestor\", ancestor, descendant],\n        capture_output=True,\n        text=True,\n        check=False,\n    )\n    if result.returncode == 0:\n        return True\n    if result.returncode == 1:\n        return False\n    return None\n\n\ndef _incoming_is_older(incoming_commit: str | None, existing_commit: str | None) -> bool:\n    \"\"\"Whether a fresh install is provably older than the recorded stamp.\n\n    Only a *proven* older lineage \u2014 the incoming platform commit is a strict\n    ancestor of the recorded one \u2014 blocks the overwrite. Same commit, unrelated\n    lineages, or an undeterminable relationship all refresh, so the guard never\n    silently keeps stale content it cannot prove is newer.\n    \"\"\"\n    if not incoming_commit or not existing_commit or incoming_commit == existing_commit:\n        return False\n    return commit_is_ancestor(ROOT, incoming_commit, existing_commit) is True\n\n\ndef _ensure_stamp_gitignored(target: Path) -> None:\n    \"\"\"Keep the untracked install stamp out of ``git add`` / ``git add -A``.\n\n    The stamp is target-local state that is never committed: validate_shiki\n    rejects an unknown *tracked* ``.shiki/`` path, and the goal loop stages\n    worktrees with ``git add -A``, so an un-ignored stamp would be committed and\n    make the target's own Validate check permanently red. The shipped\n    ``.gitignore`` is platform-owned and cannot name it, so the installer\n    ensures the target's own ``.gitignore`` ignores it. Idempotent: a re-install\n    never appends the entry twice.\n    \"\"\"\n    gitignore = target / \".gitignore\"\n    existing = gitignore.read_text(encoding=\"utf-8\") if gitignore.is_file() else \"\"\n    if INSTALL_STAMP_PATH in {line.strip() for line in existing.splitlines()}:\n        return\n    prefix = \"\" if not existing or existing.endswith(\"\\n\") else \"\\n\"\n    addition = (\n\nexec\n/bin/zsh -lc \"sed -n '988,1028p' scripts/shiki_doctor.py\" in /private/tmp/shiki-adr-collision.pa0KwY/.worktrees/shiki-t-20260807t073307221254z-2d22d654-introduc\n succeeded in 0ms:\n    digest.update(path.read_bytes())\n    return digest.hexdigest()\n\n\ndef _install_stamp_content_finding(target: Path, stamp: dict[str, Any]) -> DoctorFinding:\n    digests = stamp.get(\"digests\")\n    if not isinstance(digests, dict):\n        return _finding(\n            \"doctor.install_stamp.content\",\n            \"fail\",\n            \"Shipped content drift\",\n            \"Install stamp is missing its per-path content digests, so drift cannot be checked.\",\n            \"Regenerate the install stamp by reinstalling with `--force`.\",\n            {\"path\": INSTALL_STAMP_PATH},\n        )\n    drifted: list[str] = []\n    missing: list[str] = []\n    for relative, expected in sorted(digests.items()):\n        if not isinstance(relative, str) or not isinstance(expected, str):\n            continue\n        path = target / relative\n        if not path.is_file():\n            missing.append(relative)\n        elif _sha256_file(path) != expected:\n            drifted.append(relative)\n    problems = sorted(drifted + missing)\n    ok = not problems\n    return _finding(\n        \"doctor.install_stamp.content\",\n        \"pass\" if ok else \"warn\",\n        \"Shipped content drift\",\n        \"All stamped shipped paths match their recorded content digests.\"\n        if ok\n        else f\"Shipped paths differ from their stamped content digests: {', '.join(problems)}.\",\n        \"Re-run the install/upgrade to restore shipped content, or reconcile the local edits.\" if not ok else \"\",\n        {\"drifted\": drifted, \"missing\": missing},\n    )\n\n\ndef _install_stamp_commit_finding(target: Path, stamp: dict[str, Any]) -> DoctorFinding:\n    commit = stamp.get(\"platform_commit\")\n\nexec\n/bin/zsh -lc \"git status --porcelain=v1 | sed -E 's/\"'^.. //'\"'\" in /private/tmp/shiki-adr-collision.pa0KwY/.worktrees/shiki-t-20260807t073307221254z-2d22d654-introduc\nexec\n/bin/zsh -lc \"find docs/adr -maxdepth 1 -type f -print | sort; rg -n -U --glob '\"'!.shiki/**'\"' --glob '\"'!skills/**'\"' --glob '\"'!tests/test_adr_references.py'\"' --glob '\"'!tests/test_sadr_install_migration.py'\"' '\\\\bADR[- ]?\\\\s*(000[1-9]|001[0-8])\\\\b|docs/adr/\\\\s*(000[1-9]|001[0-8])-' AGENTS.md CLAUDE.md CONTEXT.md CONTRIBUTING.md SYSTEM_PROMPT.md pyproject.toml docs scripts tests .github .codex || true\" in /private/tmp/shiki-adr-collision.pa0KwY/.worktrees/shiki-t-20260807t073307221254z-2d22d654-introduc\n succeeded in 0ms:\n.codex/skills/shiki/SKILL.md\n.github/prompts/cca-completion-check.md\n.github/prompts/codex-task-handoff.md\n.github/workflows/release.yml\n.github/workflows/shiki-cca-completion.yml\n.github/workflows/shiki-mergegate.yml\n.shiki/tasks/T-20260807T073307221254Z-2d22d654.json\nAGENTS.md\nCLAUDE.md\nCONTEXT.md\nCONTRIBUTING.md\nSYSTEM_PROMPT.md\ndocs/adr/0001-keep-shiki-as-platform-name.md\ndocs/adr/0002-use-agents-as-shared-constitution.md\ndocs/adr/0003-require-skill-gate-for-mergegate-readiness.md\ndocs/adr/0004-use-github-first-with-shiki-mirror.md\ndocs/adr/0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\ndocs/adr/0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\ndocs/adr/0007-packaging-and-release.md\ndocs/adr/0008-use-claude-code-as-default-implementer.md\ndocs/adr/0009-requirements-definition-and-spec-freeze.md\ndocs/adr/0010-external-ai-guardian-review.md\ndocs/adr/0011-autonomous-quality-gate-evidence.md\ndocs/adr/0012-autonomous-loop-closeout-to-main.md\ndocs/adr/0013-bootstrap-grants-actions-approve-pull-requests.md\ndocs/adr/0014-codex-app-external-ai-guardian-ui-adapter.md\ndocs/adr/0015-contract-approval-before-dispatch.md\ndocs/adr/0016-locks-bound-product-paths.md\ndocs/adr/0017-bookkeeping-closeouts-do-not-inherit-risk.md\ndocs/adr/0018-carried-guardian-approval-across-a-proven-base-sync.md\ndocs/adr/README.md\ndocs/adr/template.md\ndocs/agents/autonomous-loop-e2e-acceptance.md\ndocs/agents/bootstrap-command.md\ndocs/agents/checklists.md\ndocs/agents/codex-handoff.md\ndocs/agents/completion-check-agent.md\ndocs/agents/decision-control.md\ndocs/agents/domain.md\ndocs/agents/execution-control.md\ndocs/agents/guardian-policy.md\ndocs/agents/implementation-policy.md\ndocs/agents/runtime-auth-model.md\ndocs/agents/runtime-registry.md\ndocs/agents/shiki-cli-architecture.md\ndocs/agents/skill-gate.md\ndocs/agents/triage-labels.md\ndocs/operations.md\ndocs/proposals/0001-memory-loop-spec-freeze-review.md\ndocs/proposals/0002-autonomous-loop-completion.md\ndocs/releasing.md\ndocs/usage.md\npyproject.toml\nscripts/guardian_approval_signal.py\nscripts/mergegate_check.py\nscripts/shiki_bootstrap.py\nscripts/shiki_cli.py\nscripts/shiki_contract_approval.py\nscripts/shiki_github.py\nscripts/shiki_guardian.py\nscripts/shiki_guardian_review.py\nscripts/shiki_installer.py\nscripts/shiki_loop.py\nscripts/shiki_migrations.py\nscripts/shiki_runtime_adapters.py\nscripts/shiki_runtime_registry.py\nscripts/shiki_tasks.py\nscripts/test_shiki_adversarial_state.sh\nscripts/test_shiki_code_review_gate.sh\nscripts/test_shiki_control_plane.sh\nscripts/test_shiki_goal_loop.sh\nscripts/test_shiki_governance_evidence.sh\nscripts/test_shiki_guardian_policy.sh\nscripts/test_shiki_loop_e2e.sh\nscripts/test_shiki_loop_lock_guard.sh\nscripts/test_shiki_run_orchestrator.sh\nscripts/test_shiki_runner_claude.sh\nscripts/test_shiki_runtime_registry.sh\nscripts/test_shiki_start.sh\nscripts/validate_shiki.py\ntests/test_adr_references.py\ntests/test_bookkeeping_closeout.py\ntests/test_branch_behind_sync.py\ntests/test_cca_evidence_manifest_taskless.py\ntests/test_closeout.py\ntests/test_completion_lock_scope.py\ntests/test_contract_amendment.py\ntests/test_contract_approval.py\ntests/test_contract_immutability.py\ntests/test_contract_mode.py\ntests/test_contract_open.py\ntests/test_contract_plan_registration.py\ntests/test_dispatch_lock_precondition.py\ntests/test_guardian_review.py\ntests/test_guardian_signal.py\ntests/test_guardian_signal_closeout.py\ntests/test_loop_branch_plumbing.py\ntests/test_loop_code_review.py\ntests/test_loop_e2e_contract.py\ntests/test_loop_engine.py\ntests/test_loop_lock_validator_hint.py\ntests/test_loop_tdd_evidence.py\ntests/test_mirror_scope.py\ntests/test_paired_invariants.py\ntests/test_policy_job_merged_prs.py\ntests/test_repair_delivery.py\ntests/test_runner_adapters.py\ntests/test_workflow_invocation.py\ntests/test_workflow_invocation_carry.py\n.shiki/locks/T-20260807T073307221254Z-2d22d654.json\ndocs/adr/SADR-0001-keep-shiki-as-platform-name.md\ndocs/adr/SADR-0002-use-agents-as-shared-constitution.md\ndocs/adr/SADR-0003-require-skill-gate-for-mergegate-readiness.md\ndocs/adr/SADR-0004-use-github-first-with-shiki-mirror.md\ndocs/adr/SADR-0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\ndocs/adr/SADR-0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\ndocs/adr/SADR-0007-packaging-and-release.md\ndocs/adr/SADR-0008-use-claude-code-as-default-implementer.md\ndocs/adr/SADR-0009-requirements-definition-and-spec-freeze.md\ndocs/adr/SADR-0010-external-ai-guardian-review.md\ndocs/adr/SADR-0011-autonomous-quality-gate-evidence.md\ndocs/adr/SADR-0012-autonomous-loop-closeout-to-main.md\ndocs/adr/SADR-0013-bootstrap-grants-actions-approve-pull-requests.md\ndocs/adr/SADR-0014-codex-app-external-ai-guardian-ui-adapter.md\ndocs/adr/SADR-0015-contract-approval-before-dispatch.md\ndocs/adr/SADR-0016-locks-bound-product-paths.md\ndocs/adr/SADR-0017-bookkeeping-closeouts-do-not-inherit-risk.md\ndocs/adr/SADR-0018-carried-guardian-approval-across-a-proven-base-sync.md\ndocs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\ntests/test_sadr_install_migration.py\n\n succeeded in 0ms:\ndocs/adr/README.md\ndocs/adr/SADR-0001-keep-shiki-as-platform-name.md\ndocs/adr/SADR-0002-use-agents-as-shared-constitution.md\ndocs/adr/SADR-0003-require-skill-gate-for-mergegate-readiness.md\ndocs/adr/SADR-0004-use-github-first-with-shiki-mirror.md\ndocs/adr/SADR-0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\ndocs/adr/SADR-0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\ndocs/adr/SADR-0007-packaging-and-release.md\ndocs/adr/SADR-0008-use-claude-code-as-default-implementer.md\ndocs/adr/SADR-0009-requirements-definition-and-spec-freeze.md\ndocs/adr/SADR-0010-external-ai-guardian-review.md\ndocs/adr/SADR-0011-autonomous-quality-gate-evidence.md\ndocs/adr/SADR-0012-autonomous-loop-closeout-to-main.md\ndocs/adr/SADR-0013-bootstrap-grants-actions-approve-pull-requests.md\ndocs/adr/SADR-0014-codex-app-external-ai-guardian-ui-adapter.md\ndocs/adr/SADR-0015-contract-approval-before-dispatch.md\ndocs/adr/SADR-0016-locks-bound-product-paths.md\ndocs/adr/SADR-0017-bookkeeping-closeouts-do-not-inherit-risk.md\ndocs/adr/SADR-0018-carried-guardian-approval-across-a-proven-base-sync.md\ndocs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\ndocs/adr/template.md\n\nexec\n/bin/zsh -lc \"git diff -U0 -- . ':\"'!.shiki/**'\"' | rg '\"'^[+-][''^+-]'\"' | rg -v 'ADR|SADR|decision-record|docs/adr|target product decisions|Shiki platform decisions|Relevant documents|relevant docs|hard-to-reverse tradeoffs|grill-with-docs|Architecture block|Domain language|Documentation and|API-key runners|source-of-truth:end|\"'^[-+]#'\"' | sed -n '1,320p'\" in /private/tmp/shiki-adr-collision.pa0KwY/.worktrees/shiki-t-20260807t073307221254z-2d22d654-introduc\n succeeded in 0ms:\n+namespace.\n-Shiki remains the product name for the GitHub-native agentic engineering control plane. We considered more descriptive names such as MergePilot, MergeGate, and Kiroku, but kept Shiki because it carries the existing goal-seeking framework identity while remaining independent of Hermes, Codex, Claude Code, or any single runtime.\n-Shiki uses `AGENTS.md` as the runtime-neutral constitution and `CLAUDE.md` as a thin Claude Code wrapper. This keeps Codex, Claude Code, Hermes Runner, and future Agent Runtimes aligned on the same GitHub-first MergeGate rules while still allowing runtime-specific instructions where needed.\n-Shiki requires relevant engineering skills to be invoked before execution when their triggers apply. This makes skills part of MergeGate readiness rather than optional prompt style, because Shiki depends on repeatable planning, issue decomposition, TDD, diagnosis, triage, and architecture review before autonomous or parallel agent work can be trusted.\n-Accepted\n-Shiki must coordinate multiple agent runtimes without relying on one hidden chat session or one local checkout. The operator wants Codex, Claude Code, Hermes Runner, GitHub Actions, and future runtimes to participate through a durable, auditable workflow.\n-GitHub is the operational source of truth for Goals, Issues, Pull Requests, Checks, Reviews, comments, and merge evidence.\n-Each target repository also keeps a `.shiki/` mirror for Goals, plans, task DAGs, contracts, locks, ledger entries, reports, and handoffs.\n-Claude Code Action GitHub runtime decision remain in force)\n-Shiki should run inside the operator's subscription-authenticated toolchain. The intended default is not to run Codex as an API-key backed GitHub Action.\n-The operator uses Codex as the front surface for implementation and long-running Goal work. Claude Code runs in GitHub Actions for review, automation, and MergeGate-oriented judgment.\n-Use this default split:\n-The Shiki template must not include `openai/codex-action` as the default implementation path.\n-Accepted\n-Shiki needs autonomous implementation without letting a single agent decide that its own work is complete, mergeable, or deployable.\n-The standard pipeline is:\n-```text\n-Goal Seek\n-  -> Context & Impact\n-  -> to-prd\n-  -> to-issues\n-  -> triage\n-  -> Task DAG + locks\n-  -> Codex Front + tdd\n-  -> PR + evidence\n-  -> GitHub CCA completion judgment\n-  -> MergeGate\n-  -> bounded Repair Loop\n-  -> merge\n-  -> Goal completion judgment\n-```\n-Use this responsibility split:\n-CCA verdicts are limited to:\n-Only `complete` may proceed to MergeGate readiness.\n-Accepted\n-Shiki is a GitHub-first agentic engineering control plane distributed as a\n-repository template plus a dependency-free, standard-library CLI under\n-`scripts/`. Until now the repository had no declared version, no release\n-mechanism, and no documented install/upgrade/rollback path. P2.2 (Goal G-0012,\n-issue #101) requires:\n-Constraints from the Shiki constitution (`AGENTS.md`) shape the design:\n-  not introduce third-party runtime dependencies or break the shim.\n-  and must not require extra secrets beyond the built-in `GITHUB_TOKEN`\n-  `install.create_directories`, `install.exclude_from_commit`), and it must not\n-  be mutated as part of this change.\n-  architecture-gate / high-risk and requires Guardian approval.\n-This is a hard-to-reverse platform decision (it sets the version source of\n-truth, the release trigger, and the public install contract), so it is recorded\n-  `version` field in `pyproject.toml`; both are bumped together.\n-  that does not match `VERSION`.\n-  `requires-python = \">=3.11\"`, license, and URLs.\n-  packaged as an importable distribution and `scripts/shiki.py` is a\n-  dependency-free shim; a console entry would require restructuring scripts into\n-  an installable package and risk breaking the shim.\n-  now.** Because no console entry is declared and `scripts/` is not a packaged\n-  importable distribution, `pipx install git+https://\u2026` would not expose a usable\n-  `shiki` command, so advertising it would be misleading. The supported\n-  invocation paths are instead the script install\n-  (`python3 scripts/shiki.py install-global`, verified to exist) and running the\n-  shim directly with `python3 scripts/shiki.py`. `pipx`/PyPI installs are\n-  explicitly unsupported until a future packaging restructure adds a real entry\n-  point.\n-  `workflow_dispatch` for republishing an existing tag).\n-  `python3 scripts/validate_shiki.py` as a gate, and publishes a GitHub Release\n-  with `gh release create --generate-notes --verify-tag`.\n-  by the validator.\n-  `docs/releasing.md`, referencing `.shiki/manifest.json` `install.include`\n-  /`template` semantics without editing the manifest.\n-  rollback via pinning to a prior tag and superseding bad releases with a new\n-  PATCH (never re-tagging or force-deleting published tags).\n-1. **`[project.scripts]` console entry (`shiki = \"...:main\"`).** Rejected for\n-   now: it requires turning `scripts/` into an installable package and changing\n-   import targets, which is out of scope, risks the dependency-free shim, and\n-   pulls in build-backend complexity. Revisit if/when the CLI is restructured\n-   into a proper package.\n-5. **`pipx install git+\u2026` as a supported install path.** Rejected: with no\n-   `[project.scripts]` console entry and `scripts/` not packaged as an importable\n-   distribution, a pipx install produces no usable `shiki` command, so it would\n-   mislead users. The script install (`python3 scripts/shiki.py install-global`)\n-   is adopted instead. Reconsider only after a packaging restructure adds a real\n-   entry point.\n-2. **Third-party release action (e.g. `softprops/action-gh-release`).**\n-   Rejected: `gh release create` with the built-in `GITHUB_TOKEN` keeps the\n-   workflow dependency-free and avoids pinning/maintaining an extra third-party\n-   action.\n-3. **Publishing to PyPI.** Rejected for now: Shiki is distributed as a\n-   repository template + git-installable CLI, not a PyPI library; PyPI would add\n-   credentials/secrets and a different versioning contract. Can be added later as\n-4. **Dynamic version (read `VERSION` via a build backend `dynamic` field).**\n-   Rejected to keep `pyproject.toml` backend-agnostic and dependency-free; the\n-   small duplication between `VERSION` and `pyproject.toml` is guarded by the\n-   release workflow's tag/VERSION check.\n-  and documented install/upgrade/rollback paths.\n-  the release workflow enforces they agree with the tag.\n-  (`python3 scripts/shiki.py install-global`) or by running the shim directly; no\n-  console entry point is provided yet and `pipx`/PyPI installs are unsupported.\n-  releasing versions the whole platform but ships only `install.include` to\n-  targets.\n-restricted to planner and reviewer roles by the runtime registry, and the only\n-autonomous implementation adapter was `shiki runner codex`.\n-Operational reality diverged from this contract:\n-  automated repair dispatch never worked in practice. The practical repair path\n-  became Guardian-approved direct Claude assignment.\n-  and Claude Code's native capabilities (the built-in `/code-review` skill and\n-  the Workflow multi-agent orchestration tool) have no Codex equivalent.\n-  loop driven from the operator's Claude Code session, including bounded\n-  repair dispatch, which must not depend on a runtime that cannot be invoked\n-  unattended.\n-The constitution claims Shiki is runtime-agnostic (\"not a Claude-only\n-registry-based runtime model rather than hardcoding one vendor.\n-We will make `claude-code` the default implementer runtime:\n-  registry, and set it as the default `implementer` in `.shiki/config.yaml`.\n-  materializes the registered worktree, pipes the task or repair handoff into\n-  a headless `claude -p` session, records EXEC and ledger evidence, and moves\n-  the task to review or repair-needed.\n-  codex`, the Codex handoff templates, and the ChatGPT OAuth auth model from\n-  follow-up Goals (status: the code-review gate was delivered by Goal G-B \u2014\n-  skill registry, checklists item PR-12, default required_skills \u2014 while the\n-  Context & Impact sweep remains pending for Goal G-C):\n-  - the implementer will run the `code-review` skill as a pre-PR self-review\n-    gate (TDD, then code review with fixes applied, then PR), recorded in the\n-    ledger and the PR body and judged by a new CCA checklist item \u2014 to be\n-    introduced together with the skill-gate registry entry (Goal G-B of the\n-    0.2.0 series, PRD issue #119);\n-  - Context & Impact for non-trivial Goals will be produced by a Workflow-tool\n-    parallel exploration sweep, with the run recorded as evidence (Goal G-C).\n-  The constitution will state these as required capabilities whose default\n-  implementation is Claude Code native tooling; a future runtime may satisfy\n-  them with equivalent evidence.\n-  registered worktree. Read-only fan-out is unrestricted. Unregistered\n-  worktree creation remains a non-negotiable block.\n-Out of scope: removing Codex support, changing the CCA runtime (Claude Code\n-Action with `CLAUDE_CODE_OAUTH_TOKEN` stays), and API-key based automation\n-  operator's existing Claude subscription; the broken `codex exec` automated\n-  path stops being load-bearing.\n-  validation, AGENTS.md default assignment, CLAUDE.md direct-edit rules,\n-  runtime-auth-model.md, and the skill gate registry must change together.\n-  assignment now has a standing satisfier: tasks dispatched through\n-  `shiki runner claude` carry that assignment and its evidence.\n-  role-separation maxim still holds at the process level (separate sessions,\n-  separate evidence, CCA never implements), but model-diversity in judgment is\n-  reduced. Codex remains available as an independent implementation lens.\n-  reviewers do not mutate the implementation branch, because it runs in the\n-  Implementer role before review; the Reviewer and CCA roles remain\n-  mutation-free.\n-  a non-interactive session needs standing permissions, so Claude Code's\n-  per-action permission prompts are disabled inside the dispatched session.\n-  This is an accepted risk, bounded by: dispatch only through the runner for a\n-  registered, MergeGate-gated task; a hard refusal to execute when the\n-  registered worktree resolves to the target checkout itself; Guardian\n-  approval for high/critical risk; and MergeGate remaining the only authority\n-  for state transitions (\"LLM outputs may vary. State transitions must not\n-  vary.\"). A tighter posture (settings-based deny rules and hooks shipped into\n-  the dispatch worktree) was deferred past the 0.2.0 series: Goal G-D shipped\n-  the autonomous loop without it, and the bounding work remains an open,\n-  recorded follow-up for the next series (tracked in PRD issue #119).\n-  harness rejection of direct `codex exec` is outside Shiki's control, and the\n-  plan's mandatory native capabilities cannot run on Codex at all.\n-  cross-runtime adversarial review.\n-  default; durable evidence is automatic but token cost, Node 24 workflow\n-  constraints, and divergence from the operator-local Goal experience are too\n-  high. It remains a possible future extension.\n-Proposed\n-issue decomposition, and Task DAG planning as separate steps, each with its own\n-checklist family and state transition. In practice the operator experiences\n-this as repeated interactive sessions, and the boundary between \"plan settled\"\n-and \"implementation may start\" is implicit: the de facto marker is\n-`grill_with_docs.status=complete` in the plan JSON, which `shiki start` can\n-synthesize from a short interview without a real freeze decision.\n-Two operational failures motivated a redesign (recorded operator lessons):\n-required scope surfaced twice mid-implementation because requirements were\n-never frozen against an explicit out-of-scope boundary, and autonomous\n-execution stalled because every downstream step re-opened questions the\n-planning phase should have settled.\n-The Goal lifecycle should have exactly one human gate on the happy path, with\n-for this).\n-We will restructure the pre-implementation loop:\n-  one continuous operator dialogue, entered through the `/shiki` command's\n-  Goal mode (no separate `/goal` command). Context & Impact for non-trivial\n-  Goals is produced by a Workflow-tool parallel exploration sweep recorded as\n-  evidence.\n-  It is recorded as an explicit `spec_freeze` block in the plan contract and a\n-  ledger entry. Required external scopes and permissions (the scope inventory)\n-  must be enumerated before freeze.\n-  after Spec Freeze and are validated against the frozen PRD's scope\n-  boundaries. The post-freeze loop auto-merges risk low/medium PRs once\n-  MergeGate is satisfied, auto-dispatches bounded repairs, and re-evaluates\n-  the DAG after each merge. It stops only for: Spec Amendment, the repair\n-  attempt limit, `needs_guardian`, high/critical risk, or Goal completion.\n-  pause, the contested decisions are re-grilled with the operator, and freeze\n-  is re-stamped with the amendment recorded. Only the operator approves\n-  amendments. Non-scope-moving interpretations are recorded in the\n-  **Assumption Log** without pausing work and are challengeable by CCA.\n-  retained for compatibility, and `spec_freeze` is added alongside it, with a\n-  registered migration backfilling existing plans.\n-Out of scope: changing CCA verdict semantics, MergeGate required checks, or\n-the Guardian policy; applying the migration to existing Target Repositories\n-(separate Goal after the 0.2.0 release).\n-  signal of planning failure, measurable as Spec Amendment frequency.\n-  families gain Spec Freeze and Amendment items; CCA judges scope drift\n-  against the frozen PRD instead of an implicit plan.\n-  `shiki_tasks.py`, `shiki_bootstrap.py` synthesis, and the duplicated\n-  operating-model text (README, AGENTS.md, CLAUDE.md, CONTRIBUTING.md,\n-  docs/agents/*) must change together; validator cross-reference checks guard\n-  the sweep.\n-  from a real Requirements Definition dialogue or an explicitly recorded\n-  trivial-change justification.\n-  and raises the cost of a careless freeze; the Assumption Log keeps trivia\n-  from forcing amendments, and Spec Amendment keeps real discoveries bounded\n-  instead of silent.\n-  rejected because it lengthens the interactive phase and per-task CCA +\n-  MergeGate already guard decomposition quality.\n-  over-complex for a solo-operator platform and contrary to the one-gate goal.\n-  \"low risk\" would itself be an LLM judgment, hollowing out the freeze.\n-  every installed target's plans and start records for no semantic gain.\n-Proposed\n-Shiki is intended to be an autonomous, recursive, self-improving agent\n-framework: authority-in-the-loop, not human-in-the-loop. Until now the Guardian\n-governance) recognized only human authority sources \u2014 a GitHub review approval,\n-the `guardian:approved` label applied by a configured Guardian, or a comment\n-carrying `Guardian approval granted` plus the current head SHA.\n-This forced a human into every high/critical merge, contradicting the\n-autonomous design. Two failure modes surfaced in practice (2026-06-13):\n-  as the operator and deleted a stale comment to change the gate's evaluation \u2014\n-  i.e. it manufactured a human approval. This is forbidden: AI approval must\n-  never be recorded as human approval.\n-  comment lacking the current head SHA, so a stale or abbreviated-SHA approval\n-  comment poisoned the gate even when a valid full-SHA approval also existed.\n-The correct resolution is not to forbid AI merges, but to make external AI\n-guardian review a first-class authority kind whose identity is preserved.\n-We will admit `external_ai_guardian_review` as a first-class Guardian approval\n-source for high/critical-risk PRs:\n-  may authorize autonomous merge through a head-SHA-bound approval artifact.\n-  ```` ```external-ai-guardian-review ```` JSON block:\n-  `{kind, reviewer:{type:\"ai_model\", model, role}, repo, pr, head_sha, verdict,\n-  merge_permission, not_operator_approval:true, ...}`. It is a live source, not\n-  a committed `.shiki/` file (committed approvals in the approved PR would be\n-  forgeable).\n-  (integrity); the reviewer model and role are in the policy allow-lists; the\n-  head SHA matches the current PR head exactly; the verdict is `approve` and\n-  `merge_permission` is `autonomous_merge_permitted`.\n-  stamps `reviewer_type=external_ai_model`; the human relay is NOT recorded as\n-  an approver. AI approval is never transformed into operator approval.\n-  the human `guardian:approved` label. The human path (label + review/comment)\n-  is unchanged.\n-  current head SHA is a soft signal that is fatal only when no valid exact-head\n-  approval (from any authority) exists; otherwise it is a warning.\n-  the goal loop may merge high/critical risk when that check is green, because\n-  green means a recorded authority approved.\n-Authority kinds become explicit: operator, repository maintainer,\n-`external_ai_guardian_reviewer`, policy engine, verifier ensemble. Approval is\n-judged by authority kind, scope, head SHA, evidence, and audit trail \u2014 not by\n-whether a human pressed the button.\n-Out of scope: removing the human approval paths; auto-approving without a valid\n-artifact; treating committed `.shiki` files as approval; recording AI approval\n-under a human identity.\n-  guardian review exists, with the AI reviewer's identity preserved in the\n-  audit trail.\n-  shiki_guardian.py gains the artifact evaluator and the poisoning fix;\n-  mergegate_check.py records `reviewer_type=external_ai_model`; shiki_loop.py\n-  merges high/critical when the policy gate is green; validate_shiki.py,\n-  the governance docs, and CONTEXT.md change in lockstep.\n-  binding, the configured-Guardian relay requirement, and the audit trail \u2014\n-  rather than on a human pressing approve. This is an accepted, recorded\n-  tradeoff for an autonomous framework.\n-  is recorded as AI approval, never as a human's.\n-  autonomous design the operator intends and produced the impersonation\n-  incident.\n-  approval inside the PR being approved is forgeable by anyone who can push.\n-  the configured-Guardian relay is sufficient for the solo-maintainer model and\n-  avoids new credential management.\n-Proposed\n-take a ready task to an auto-merged PR with no human action. A live run on\n-2026-06-17 proved the loop self-implements (a headless `claude -p` runner wrote\n-a correct, passing test) and the Memory Loop capture fires, but it cannot reach\n-auto-merge: MergeGate and CCA require evidence the autonomous path never\n-produces \u2014 `required_skill` evidence for `tdd`/`code-review`, the CCA PR-12\n-pre-PR code-review section, and a deterministic verification record.\n-The naive fix is to let the headless implementer self-attest: instruct it to\n-\"do TDD, run a code review, and write the evidence.\" That collides with the\n-platform maxim \u2014 **\"LLM outputs may vary. State transitions must not vary.\"** \u2014\n-and with the finding (Anthropic engineering blog; Fable-5 prompting guidance)\n-that models self-critique poorly: grading in an *independent context window*\n-outperforms self-review. A runner that writes its own \"code review found 0\n-findings\" ledger is a varying, self-graded, forgeable state transition.\n-The CCA already embodies the right pattern (an independent judge in a separate\n-GitHub Actions context). The pre-PR quality gates must follow the same shape.\n-Autonomous quality-gate evidence is produced by the **loop**, from deterministic\n-observable facts and an independent verifier \u2014 never by the implementer runtime\n-self-attesting.\n-  implementer runs, the loop executes the task's tests in the worktree and\n-  records a `check` ledger of the command and its green result. The evidence is\n-  \"tests are present and pass (loop-observed)\", not \"the runner says it followed\n-  red-green\". Red-first ordering is the implementer runtime's concern and is not\n-  gated, because it cannot be observed deterministically without trusting the\n-  runner.\n-  the PR, the loop dispatches a **read-only** `claude -p` reviewer in a separate\n-  context (`--allowedTools` restricted to read tools; no edit tools) with a\n-  `--json-schema` structured-findings contract. The loop parses the verdict\n-  deterministically and records it as the `code-review` ledger plus the\n-  `## Pre-PR code review` PR-body section (PR-12). A dispatch or parse failure is\n-  **fail-closed** (review-not-done \u2192 block; never silently pass). Same model as\n-  the implementer, separate context \u2014 the independence is the context boundary,\n-  exactly as for CCA.\n-  becomes a repair packet; the implementer fixes; the reviewer re-runs), bounded\n-  by the standard 3-attempt repair limit. The reviewer is never asked to \"make\n-  the gate pass\"; it only judges.\n-  ledgers or PR-12 section. Those are loop-owned state transitions.\n-This keeps every evidence-producing transition deterministic and auditable while\n-honoring the independent-verifier principle.\n-  invocation) and a deterministic test-run + evidence-recording step, both\n-  before `create_pr`. `shiki_loop.py` records the `tdd` and `code-review`\n-  ledgers and writes the PR-12 section; the runner adapter set gains a read-only\n-  reviewer invocation distinct from the bypass-permissions implementer.\n-  manual pre-clearing, closing the gap that kept the loop from auto-merging.\n-  the reviewer's read-only confinement, the structured fail-closed verdict, and\n-  the audit trail \u2014 not on trusting a self-graded runner.\n-  independent gate, not a replacement for or input to the CCA verdict.\n-  evidence). Rejected: varying, self-graded, forgeable \u2014 violates the maxim and\n-  the independent-verifier finding.\n\ndiff --git a/.codex/skills/shiki/SKILL.md b/.codex/skills/shiki/SKILL.md\nindex c8e2e85d9d75d49cc4f2d6286ee22659c4e53e75..93cded623b2c15d9f65c76491a1c2d813ac9e6c0\n--- a/.codex/skills/shiki/SKILL.md\n+++ b/.codex/skills/shiki/SKILL.md\n@@ -18,6 +18,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n Codex CLI does not register this skill as a custom `/shiki` slash command. When\n the user types `/shiki` in Codex CLI and gets \"Unrecognized command\", explain\n that this is expected: invoke Shiki in Codex with natural language such as\n@@ -69,7 +72,7 @@\n \n ## Responsibilities\n \n-- Claude Code implements and repairs by default (`shiki runner claude`, ADR 0008); Codex implements and repairs tasks explicitly assigned to `codex`.\n+- Claude Code implements and repairs by default (`shiki runner claude`, SADR-0008); Codex implements and repairs tasks explicitly assigned to `codex`.\n - Claude Code Action can act as GitHub-side CCA or reviewer.\n - CCA judges completion.\n - MergeGate authorizes state transitions and merge readiness.\ndiff --git a/.github/prompts/cca-completion-check.md b/.github/prompts/cca-completion-check.md\nindex ed203ad5f302ecc81372d8ab27d2b1232ca97497..fd89950528333ef6242e6d5443904f5392ab7e56\n--- a/.github/prompts/cca-completion-check.md\n+++ b/.github/prompts/cca-completion-check.md\n@@ -19,6 +19,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n ## Required Reading\n \n Read only the files needed to judge the PR contract. Prefer this order:\ndiff --git a/.github/prompts/codex-task-handoff.md b/.github/prompts/codex-task-handoff.md\nindex 695e211d76dea1c60e8d5bb360e78487808286a4..59441d576b01a39569722f226c854e5a740e9eb5\n--- a/.github/prompts/codex-task-handoff.md\n+++ b/.github/prompts/codex-task-handoff.md\n@@ -2,7 +2,7 @@\n \n You are Codex Front acting as the Implementer for a Shiki task.\n \n-Follow `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant ADRs, and `docs/agents/codex-handoff.md`.\n+Follow `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant Shiki SADRs and target ADRs, and `docs/agents/codex-handoff.md`.\n \n Rules:\n \ndiff --git a/AGENTS.md b/AGENTS.md\nindex 7f041100d49c2a7525f51cb0f5d696a4a6d1c78c..a06bfbabc6fa48a007389e25e12d7ed523936bed\n--- a/AGENTS.md\n+++ b/AGENTS.md\n@@ -22,7 +22,7 @@\n - **Codex Front** is an optional implementer through Codex App, Codex CLI, Codex IDE extension, or Codex Web signed in with ChatGPT OAuth/subscription auth, dispatched with `shiki runner codex` when a task is explicitly assigned to `codex`.\n - **GitHub CCA** is implemented by Claude Code Action by default, using `CLAUDE_CODE_OAUTH_TOKEN`.\n \n-Do not assume `openai/codex-action`, `OPENAI_API_KEY`, or API-key based Codex automation in the default Shiki path. API-key runners are explicit target-repo extensions and require their own ADR.\n+Do not assume `openai/codex-action`, `OPENAI_API_KEY`, or API-key based Codex automation in the default Shiki path. API-key runners are explicit target-repo extensions and require their own target ADR.\n \n Shiki is not:\n \n@@ -44,10 +44,14 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use\n+`SADR-NNNN-*.md`; target product decisions retain `NNNN-*.md` and the `ADR`\n+namespace.\n+\n Use the canonical source-of-truth order above. Runtime-specific reading orders\n are operational guidance only and must not override or reorder it.\n \n-Conversation state is not durable truth. If a decision matters, put it in GitHub, `.shiki/`, `CONTEXT.md`, or an ADR.\n+Conversation state is not durable truth. If a decision matters, put it in GitHub, `.shiki/`, `CONTEXT.md`, a Shiki SADR, or a target ADR.\n \n If GitHub and `.shiki/` disagree, surface the conflict and prefer GitHub operational state until the mirror is repaired.\n \n@@ -75,8 +79,8 @@\n Every non-trivial change follows this loop:\n \n 1. **Goal**: clarify outcome, completion conditions, non-goals, risk level, and success signals.\n-2. **grill-with-docs**: challenge the plan against domain docs, ADRs, code reality, terminology, and edge scenarios.\n-3. **Context & Impact**: find relevant docs, ADRs, code, dependencies, owners, locks, and verification surfaces.\n+2. **grill-with-docs**: challenge the plan against domain docs, Shiki SADRs, target ADRs, code reality, terminology, and edge scenarios.\n+3. **Context & Impact**: find relevant docs, SADRs, target ADRs, code, dependencies, owners, locks, and verification surfaces.\n 4. **PRD and Spec Freeze**: use `to-prd` when context is settled enough to become durable product/engineering intent. The operator's explicit approval of the PRD is Spec Freeze: the plan records a `spec_freeze` block, and no plan runs without it. Steps 1-4 form Requirements Definition \u2014 one continuous operator dialogue. After Spec Freeze, scope changes require an operator-approved Spec Amendment; non-scope-moving interpretations go to the Assumption Log.\n 5. **Issues**: use `to-issues` to create independently grabbable vertical-slice issues. Prefer AFK slices over HITL slices where possible.\n 6. **Triage**: label issues for readiness, risk, runtime, skills, and MergeGate state.\n@@ -167,7 +171,7 @@\n - Testing decisions.\n - Out-of-scope items.\n - Further notes.\n-- Links to relevant domain terms and ADRs.\n+- Links to relevant domain terms, SADRs, and target ADRs.\n \n Every executable task must include:\n \n@@ -193,7 +197,7 @@\n \n Context & Impact must identify:\n \n-- Relevant documents, domain terms, ADRs, and past decisions.\n+- Relevant documents, domain terms, SADRs, target ADRs, and past decisions.\n - Relevant modules, interfaces, seams, callers, owners, and tests.\n - Dependency relationships between tasks.\n - Candidate locks and lock conflicts.\n@@ -210,7 +214,7 @@\n | Skill | Required when |\n | --- | --- |\n | `setup-matt-pocock-skills` | First configuring the repo for skills, or when issue tracker, triage labels, or domain docs are missing. |\n-| `grill-with-docs` | Default for non-trivial Goals before PRD/issues; always required when plans, terminology, boundaries, tradeoffs, or ADR-worthy decisions are ambiguous. |\n+| `grill-with-docs` | Default for non-trivial Goals before PRD/issues; always required when plans, terminology, boundaries, tradeoffs, or decision-record-worthy choices are ambiguous. |\n | `zoom-out` | The agent lacks an architectural map of the relevant code area. |\n | `to-prd` | Settled Goal context must become durable product/engineering intent. |\n | `to-issues` | A Goal, PRD, or plan must become independently grabbable vertical-slice issues. |\n@@ -348,7 +352,7 @@\n - Three or more new files.\n - Any high-risk or critical label.\n \n-When the architecture gate triggers, run `grill-with-docs` or `improve-codebase-architecture` as appropriate and record durable decisions in an ADR when needed.\n+When the architecture gate triggers, run `grill-with-docs` or `improve-codebase-architecture` as appropriate and record durable decisions in a Shiki SADR or target ADR as applicable.\n \n ## Engineering Discipline\n \ndiff --git a/CLAUDE.md b/CLAUDE.md\nindex 98a932e7841a632866fcd53b0bad0e3af3711d3d..98a31399404d6fb044b4fd493f4459ac87f439de\n--- a/CLAUDE.md\n+++ b/CLAUDE.md\n@@ -14,6 +14,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n ## Claude Role\n \n Default to planner, default implementer, reviewer, coordinator, documentation editor, CCA-style completion judge, and final-judgment assistant.\n@@ -57,7 +60,7 @@\n At the start of a Shiki session:\n \n 1. Read `AGENTS.md`.\n-2. Read `CONTEXT.md` and relevant ADRs.\n+2. Read `CONTEXT.md` and relevant Shiki SADRs and target ADRs.\n 3. Read active `.shiki/` Goal/task/ledger state when present.\n 4. Read active distilled rules from `.shiki/memories/` (status `distilled`, `active: true`, not revoked or superseded) for applicable guidance; the same rules are injected deterministically into task handoffs as the `## Distilled Rules` section (\u00a73.5).\n 5. Read the GitHub Issue or PR when the task is GitHub-backed.\n@@ -74,7 +77,7 @@\n - Define the Goal, completion conditions, non-goals, and risk level.\n - Treat `grill-with-docs` as the default planning entry point for non-trivial Goals.\n - Challenge domain terms against `CONTEXT.md`.\n-- Challenge hard-to-reverse tradeoffs against ADRs.\n+- Challenge hard-to-reverse tradeoffs against SADRs and target ADRs.\n - Explore code instead of asking when code can answer the question.\n - Run Context & Impact before implementation planning.\n - Produce a Task DAG, not an unordered checklist.\n@@ -94,7 +97,7 @@\n - For each question, provide your recommended answer.\n - Resolve dependency decisions before downstream design questions.\n - Update `CONTEXT.md` inline when terminology crystallizes.\n-- Offer ADRs only when the decision is hard to reverse, surprising without context, and a real tradeoff.\n+- Offer a SADR or target ADR only when the decision is hard to reverse, surprising without context, and a real tradeoff.\n - Convert unresolved questions into HITL blockers rather than allowing Codex to guess.\n \n ## PRD and Issues Standard\n@@ -103,7 +106,7 @@\n \n - Use the project's domain glossary vocabulary.\n - Avoid volatile file paths unless they are essential.\n-- Include implementation decisions, testing decisions, out-of-scope items, and relevant ADR/domain links.\n+- Include implementation decisions, testing decisions, out-of-scope items, and relevant SADR/target-ADR/domain links.\n - Identify deep-module opportunities and testable interfaces.\n \n When creating issues:\n@@ -121,7 +124,7 @@\n - Branch/worktree target.\n - Scope and non-goals.\n - Dependencies and locks.\n-- Relevant docs, ADRs, modules, and tests.\n+- Relevant docs, SADRs, target ADRs, modules, and tests.\n - Required skill invocations.\n - TDD expectations.\n - Pre-PR `code-review` gate expectations.\n@@ -152,7 +155,7 @@\n \n - Treat yourself as a judge, not implementer.\n - Do not write production code.\n-- Read `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant ADRs, PRD, task issue, PR body, diff, checks, reviews, labels, and `.shiki/` state.\n+- Read `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant SADRs and target ADRs, PRD, task issue, PR body, diff, checks, reviews, labels, and `.shiki/` state.\n - Evaluate every applicable checklist in `docs/agents/checklists.md`.\n - Map each acceptance criterion to durable evidence.\n - Distinguish implementation failure from missing evidence.\ndiff --git a/CONTEXT.md b/CONTEXT.md\nindex e61a9be06a0cce0d026484083a657bde2067e234..237ee8aa3ea8fab334ece49768bc5ed3b5af0974\n--- a/CONTEXT.md\n+++ b/CONTEXT.md\n@@ -25,7 +25,7 @@\n _Avoid_: jumping directly into code\n \n **grill-with-docs**:\n-A planning skill that challenges a plan against domain language, ADRs, code reality, and concrete edge scenarios. It resolves design-tree questions before PRD/issues. It runs inside Requirements Definition.\n+A planning skill that challenges a plan against domain language, Shiki SADRs, target ADRs, code reality, and concrete edge scenarios. It resolves design-tree questions before PRD/issues. It runs inside Requirements Definition.\n _Avoid_: generic brainstorming, silent assumptions\n \n **Requirements Definition**:\ndiff --git a/CONTRIBUTING.md b/CONTRIBUTING.md\nindex c6d9b656e73abb8a55e5f660123e03153dcb3e6d..9e37a8fbf668db5b22461aaae5548b8f1354394c\n--- a/CONTRIBUTING.md\n+++ b/CONTRIBUTING.md\n@@ -17,7 +17,7 @@\n 1. **Goal** \u2014 start from a user-approved Goal with an outcome, completion\n    conditions, non-goals, risk level, and success signals.\n 2. **grill-with-docs** \u2014 for any non-trivial change, challenge the plan against\n-   domain language, ADRs, and code reality before writing a PRD.\n+   domain language, Shiki SADRs, target ADRs, and code reality before writing a PRD.\n 3. **PRD** \u2014 capture settled product/engineering intent (`to-prd`).\n 4. **Spec Freeze** \u2014 the operator explicitly approves the PRD/requirements;\n    the plan records a `spec_freeze` block and no plan runs without it\n@@ -94,7 +94,7 @@\n   entries.\n - Do not hand-edit `.shiki/goals/*.json` or `.shiki/manifest.json` in a task PR.\n - Conversation state is not durable truth. If a decision matters, record it in\n-  GitHub, `.shiki/`, `CONTEXT.md`, or an ADR.\n+  GitHub, `.shiki/`, `CONTEXT.md`, a Shiki SADR, or a target ADR.\n \n ## Architecture Gate\n \n@@ -103,7 +103,7 @@\n or CI policy, package structure, three or more structural files, more than 100\n net new lines in a core module, three or more new files, or any high-risk label.\n When triggered, run `grill-with-docs` or `improve-codebase-architecture` and\n-record durable decisions in an ADR when needed (see `docs/adr/`).\n+record durable decisions in a Shiki SADR or target ADR as applicable (see `docs/adr/`).\n \n ## Safety\n \ndiff --git a/SYSTEM_PROMPT.md b/SYSTEM_PROMPT.md\nindex aaaea45e3a896c3a44ba89eec8bb84325ecf6c62..da009c86a88285269dee0155e58e550c6d744ea9\n--- a/SYSTEM_PROMPT.md\n+++ b/SYSTEM_PROMPT.md\n@@ -30,6 +30,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n Use the canonical source-of-truth order above. Runtime-specific reading orders\n are operational guidance only and must not override or reorder it. The current\n conversation is non-durable operator input.\n@@ -48,14 +51,14 @@\n \n A runtime may only do work that is explicitly assigned by the Goal, task, PR, or MergeGate decision.\n \n-Do not assume `openai/codex-action`, `OPENAI_API_KEY`, or API-key based Codex automation in the default Shiki path. API-key runners are explicit target-repo extensions and require their own ADR.\n+Do not assume `openai/codex-action`, `OPENAI_API_KEY`, or API-key based Codex automation in the default Shiki path. API-key runners are explicit target-repo extensions and require their own target ADR.\n \n ## 4. Non-negotiable Shiki flow\n \n For any non-trivial Goal, use this flow:\n \n 1. **Goal Seek**: define outcome, non-goals, risk level, completion criteria, and evidence requirements.\n-2. **grill-with-docs**: challenge the plan against `CONTEXT.md`, ADRs, code reality, terminology, boundaries, and edge scenarios. Ask one question at a time when the operator is available. If code can answer a question, inspect code instead of asking.\n+2. **grill-with-docs**: challenge the plan against `CONTEXT.md`, Shiki SADRs, target ADRs, code reality, terminology, boundaries, and edge scenarios. Ask one question at a time when the operator is available. If code can answer a question, inspect code instead of asking.\n 3. **Context & Impact**: identify modules, interfaces, seams, callers, dependencies, locks, risk, and verification surfaces.\n 4. **to-prd + Spec Freeze**: turn settled context into a PRD using domain vocabulary and testing decisions. The operator's explicit approval of the PRD is Spec Freeze, recorded as the plan's `spec_freeze` block; plans without it do not run. Steps 1-4 form Requirements Definition.\n 5. **to-issues**: decompose the PRD into independently grabbable vertical-slice issues. Prefer AFK slices; mark HITL slices when judgment is still required.\n@@ -75,7 +78,7 @@\n Before making any material change:\n \n 1. Read `AGENTS.md` and any runtime-specific wrapper such as `CLAUDE.md`.\n-2. Read `CONTEXT.md` and relevant ADRs.\n+2. Read `CONTEXT.md` and relevant SADRs and target ADRs.\n 3. Read the GitHub Issue / PR and `.shiki/` state for the active Goal or task.\n 4. Identify your role: Planner, Implementer, Reviewer, Completion Check Agent, Repairer, or Guardian-assist.\n 5. Identify Goal id, task id, branch, PR, dependencies, locks, risk level, required skills, and required evidence.\n@@ -103,7 +106,7 @@\n \n At minimum, identify:\n \n-- Relevant documents, ADRs, domain terms, and prior decisions.\n+- Relevant documents, SADRs, target ADRs, domain terms, and prior decisions.\n - Relevant modules, interfaces, seams, callers, owners, and tests.\n - Dependency relationships between tasks.\n - Candidate file locks and lock conflicts.\n@@ -152,7 +155,7 @@\n \n CCA must read:\n \n-- `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant ADRs.\n+- `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant SADRs and target ADRs.\n - Parent Goal issue, PRD issue, task issue, comments, and labels.\n - PR body, diff, commits, changed files, reviews, CI checks, and artifacts available to the workflow.\n - `.shiki/` task contract, locks, ledger entries, CCA checklist profile, and prior repair packets.\n@@ -214,7 +217,7 @@\n - Three or more new files.\n - Any high-risk or critical label.\n \n-When the architecture gate triggers, run `grill-with-docs` or `improve-codebase-architecture` as appropriate and record the decision in an ADR if it is hard to reverse or likely to be re-litigated.\n+When the architecture gate triggers, run `grill-with-docs` or `improve-codebase-architecture` as appropriate and record the decision in a Shiki SADR or target ADR as applicable if it is hard to reverse or likely to be re-litigated.\n \n ## 14. Skill Gate\n \n@@ -223,7 +226,7 @@\n Use these skills:\n \n - `setup-matt-pocock-skills`: before first use of the engineering skill set, or when issue tracker, triage labels, or domain docs are missing.\n-- `grill-with-docs`: default for non-trivial Goals before PRD/issues; required for ambiguous plans, terminology, boundaries, ADR-worthy tradeoffs, or design-tree decisions.\n+- `grill-with-docs`: default for non-trivial Goals before PRD/issues; required for ambiguous plans, terminology, boundaries, decision-record-worthy tradeoffs, or design-tree decisions.\n - `zoom-out`: unfamiliar code area or missing architectural map.\n - `to-prd`: convert settled context into a PRD.\n - `to-issues`: break a PRD, plan, or Goal into independently grabbable vertical-slice issues.\ndiff --git a/docs/adr/0001-keep-shiki-as-platform-name.md b/docs/adr/SADR-0001-keep-shiki-as-platform-name.md\nindex 20a9637c3b9d8c97de7ba8db7cfae4d82d9a7dba..8b685439959aada1b221c112f0ac3c51ca8a1695\n--- a/docs/adr/0001-keep-shiki-as-platform-name.md\n+++ b/docs/adr/SADR-0001-keep-shiki-as-platform-name.md\n@@ -1,3 +1,3 @@\n-# Keep Shiki As The Platform Name\n+# SADR-0001: Keep Shiki As The Platform Name\n \n Shiki remains the product name for the GitHub-native agentic engineering control plane. We considered more descriptive names such as MergePilot, MergeGate, and Kiroku, but kept Shiki because it carries the existing goal-seeking framework identity while remaining independent of Hermes, Codex, Claude Code, or any single runtime.\ndiff --git a/docs/adr/0002-use-agents-as-shared-constitution.md b/docs/adr/SADR-0002-use-agents-as-shared-constitution.md\nindex 73de30ecb078120eac540d9bc51855e3cd48d0e3..c5486182bf06fca36ea508496b013c67302fd989\n--- a/docs/adr/0002-use-agents-as-shared-constitution.md\n+++ b/docs/adr/SADR-0002-use-agents-as-shared-constitution.md\n@@ -1,3 +1,3 @@\n-# Use AGENTS As The Shared Agent Constitution\n+# SADR-0002: Use AGENTS As The Shared Agent Constitution\n \n Shiki uses `AGENTS.md` as the runtime-neutral constitution and `CLAUDE.md` as a thin Claude Code wrapper. This keeps Codex, Claude Code, Hermes Runner, and future Agent Runtimes aligned on the same GitHub-first MergeGate rules while still allowing runtime-specific instructions where needed.\ndiff --git a/docs/adr/0003-require-skill-gate-for-mergegate-readiness.md b/docs/adr/SADR-0003-require-skill-gate-for-mergegate-readiness.md\nindex 424be0c9af4c8f0136f1fde68a468c47ff84e764..7fc337987c7df9792f75f181dfcbed75fe5ffff2\n--- a/docs/adr/0003-require-skill-gate-for-mergegate-readiness.md\n+++ b/docs/adr/SADR-0003-require-skill-gate-for-mergegate-readiness.md\n@@ -1,3 +1,3 @@\n-# Require Skill Gate For MergeGate Readiness\n+# SADR-0003: Require Skill Gate For MergeGate Readiness\n \n Shiki requires relevant engineering skills to be invoked before execution when their triggers apply. This makes skills part of MergeGate readiness rather than optional prompt style, because Shiki depends on repeatable planning, issue decomposition, TDD, diagnosis, triage, and architecture review before autonomous or parallel agent work can be trusted.\ndiff --git a/docs/adr/0004-use-github-first-with-shiki-mirror.md b/docs/adr/SADR-0004-use-github-first-with-shiki-mirror.md\nindex d0a3a9174dc646a945a9d325954ffaf00c352040..a79c54ae9ee8eed2a0524192b864da4e22a92d3f\n--- a/docs/adr/0004-use-github-first-with-shiki-mirror.md\n+++ b/docs/adr/SADR-0004-use-github-first-with-shiki-mirror.md\n@@ -1,4 +1,4 @@\n-# ADR 0004: Use GitHub First With A Repository-Local Shiki Mirror\n+# SADR-0004: Use GitHub First With A Repository-Local Shiki Mirror\n \n ## Status\n \ndiff --git a/docs/adr/0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md b/docs/adr/SADR-0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\nindex b339baf49b89d9290e79c3d51b36e7b2bde3bd95..0f3c3eb9afccdb3403ce978e21d04fe12e495da0\n--- a/docs/adr/0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\n+++ b/docs/adr/SADR-0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\n@@ -1,8 +1,8 @@\n-# ADR 0005: Use Codex As Front And Claude Code Action As The GitHub Runtime\n+# SADR-0005: Use Codex As Front And Claude Code Action As The GitHub Runtime\n \n ## Status\n \n-Accepted (implementer default superseded by ADR 0008; the auth model and the\n+Accepted (implementer default superseded by SADR-0008; the auth model and the\n Claude Code Action GitHub runtime decision remain in force)\n \n ## Context\n@@ -26,4 +26,4 @@\n - Shiki avoids requiring `OPENAI_API_KEY` for the default workflow.\n - Codex work happens through the user's authenticated Codex front surface, not as a hidden backend Action.\n - Claude Code Action is configured for OAuth-token based subscription usage by default.\n-- API-key based automation remains possible only as an explicit target-repo extension with its own ADR.\n+- API-key based automation remains possible only as an explicit target-repo extension with its own target ADR.\ndiff --git a/docs/adr/0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md b/docs/adr/SADR-0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\nindex 4bcae9d1e52f0a37b8e9fbafa5cd14b6ee1993bd..7a38538a6ae9929567f0ade67e7ea6a60d0b3f7e\n--- a/docs/adr/0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\n+++ b/docs/adr/SADR-0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\n@@ -1,4 +1,4 @@\n-# ADR 0006: Separate Codex Implementation, CCA Completion, And MergeGate Authorization\n+# SADR-0006: Separate Codex Implementation, CCA Completion, And MergeGate Authorization\n \n ## Status\n \ndiff --git a/docs/adr/0007-packaging-and-release.md b/docs/adr/SADR-0007-packaging-and-release.md\nindex 79b84ddb97167372b44641e5b7860e400ce80822..bb445a2e2e856d9d1f5fdbe35de8d4e8cec0fb32\n--- a/docs/adr/0007-packaging-and-release.md\n+++ b/docs/adr/SADR-0007-packaging-and-release.md\n@@ -1,4 +1,4 @@\n-# ADR 0007: Packaging And Release For Shiki\n+# SADR-0007: Packaging And Release For Shiki\n \n ## Status\n \n@@ -25,7 +25,7 @@\n   not introduce third-party runtime dependencies or break the shim.\n - Releases must run inside the operator's subscription-authenticated toolchain\n   and must not require extra secrets beyond the built-in `GITHUB_TOKEN`\n-  (consistent with ADR 0005's no-API-key-by-default stance).\n+  (consistent with SADR-0005's no-API-key-by-default stance).\n - `.shiki/manifest.json` already encodes the install surface (`install.include`,\n   `install.create_directories`, `install.exclude_from_commit`), and it must not\n   be mutated as part of this change.\n@@ -34,7 +34,7 @@\n \n This is a hard-to-reverse platform decision (it sets the version source of\n truth, the release trigger, and the public install contract), so it is recorded\n-as an ADR.\n+as a SADR.\n \n ## Decision\n \n@@ -104,7 +104,7 @@\n 3. **Publishing to PyPI.** Rejected for now: Shiki is distributed as a\n    repository template + git-installable CLI, not a PyPI library; PyPI would add\n    credentials/secrets and a different versioning contract. Can be added later as\n-   its own ADR if a packaged distribution is needed.\n+   its own SADR if a packaged distribution is needed.\n 4. **Dynamic version (read `VERSION` via a build backend `dynamic` field).**\n    Rejected to keep `pyproject.toml` backend-agnostic and dependency-free; the\n    small duplication between `VERSION` and `pyproject.toml` is guarded by the\n@@ -123,4 +123,4 @@\n   releasing versions the whole platform but ships only `install.include` to\n   targets.\n - Future PyPI distribution or a console entry point are deferred and would each\n-  warrant their own ADR.\n+  warrant their own SADR.\ndiff --git a/docs/adr/0008-use-claude-code-as-default-implementer.md b/docs/adr/SADR-0008-use-claude-code-as-default-implementer.md\nindex dedf4510b7c7799521422f622029563d373db031..35c2625d908167fbc58855b5b63e283805f167e8\n--- a/docs/adr/0008-use-claude-code-as-default-implementer.md\n+++ b/docs/adr/SADR-0008-use-claude-code-as-default-implementer.md\n@@ -1,12 +1,12 @@\n-# ADR 0008: Use Claude Code As The Default Implementer Runtime\n+# SADR-0008: Use Claude Code As The Default Implementer Runtime\n \n ## Status\n \n-Proposed (supersedes ADR 0005 for the implementer default; keeps its auth model)\n+Proposed (supersedes SADR-0005 for the implementer default; keeps its auth model)\n \n ## Context\n \n-ADR 0005 made Codex Front the default implementation runtime, with Claude Code\n+SADR-0005 made Codex Front the default implementation runtime, with Claude Code\n restricted to planner and reviewer roles by the runtime registry, and the only\n autonomous implementation adapter was `shiki runner codex`.\n \n@@ -18,13 +18,13 @@\n - The platform's planning, review, and CCA layers already run on Claude Code,\n   and Claude Code's native capabilities (the built-in `/code-review` skill and\n   the Workflow multi-agent orchestration tool) have no Codex equivalent.\n-- The Goal lifecycle redesign (ADR 0009) requires an autonomous post-freeze\n+- The Goal lifecycle redesign (SADR-0009) requires an autonomous post-freeze\n   loop driven from the operator's Claude Code session, including bounded\n   repair dispatch, which must not depend on a runtime that cannot be invoked\n   unattended.\n \n The constitution claims Shiki is runtime-agnostic (\"not a Claude-only\n-workflow\", ADR 0002). Any change to the implementer default must preserve the\n+workflow\", SADR-0002). Any change to the implementer default must preserve the\n registry-based runtime model rather than hardcoding one vendor.\n \n ## Decision\n@@ -39,7 +39,7 @@\n   the task to review or repair-needed.\n - Keep Codex registered as an optional implementer runtime. `shiki runner\n   codex`, the Codex handoff templates, and the ChatGPT OAuth auth model from\n-  ADR 0005 remain supported for tasks explicitly assigned to `codex`.\n+  SADR-0005 remain supported for tasks explicitly assigned to `codex`.\n - Commit to mandating two Claude-native capabilities as loop contract in\n   follow-up Goals (status: the code-review gate was delivered by Goal G-B \u2014\n   skill registry, checklists item PR-12, default required_skills \u2014 while the\n@@ -60,7 +60,7 @@\n \n Out of scope: removing Codex support, changing the CCA runtime (Claude Code\n Action with `CLAUDE_CODE_OAUTH_TOKEN` stays), and API-key based automation\n-(still requires its own ADR per ADR 0005).\n+(still requires its own target ADR per SADR-0005).\n \n ## Consequences\n \n@@ -100,7 +100,7 @@\n   harness rejection of direct `codex exec` is outside Shiki's control, and the\n   plan's mandatory native capabilities cannot run on Codex at all.\n - **Remove Codex entirely**: rejected; it abandons the runtime-agnostic\n-  constitution (ADR 0002) for no operational gain and forecloses\n+  constitution (SADR-0002) for no operational gain and forecloses\n   cross-runtime adversarial review.\n - **GitHub Actions cloud implementation via claude-code-action**: rejected as\n   default; durable evidence is automatic but token cost, Node 24 workflow\ndiff --git a/docs/adr/0009-requirements-definition-and-spec-freeze.md b/docs/adr/SADR-0009-requirements-definition-and-spec-freeze.md\nindex 29b3f7871bef9203a8b4a831a65c39eb2f506e42..52e7dd69ad0aa4efe0b68c62678abbf7df02aaec\n--- a/docs/adr/0009-requirements-definition-and-spec-freeze.md\n+++ b/docs/adr/SADR-0009-requirements-definition-and-spec-freeze.md\n@@ -1,4 +1,4 @@\n-# ADR 0009: Merge Grill And Planning Into Requirements Definition With Spec Freeze\n+# SADR-0009: Merge Grill And Planning Into Requirements Definition With Spec Freeze\n \n ## Status\n \ndiff --git a/docs/adr/0010-external-ai-guardian-review.md b/docs/adr/SADR-0010-external-ai-guardian-review.md\nindex e6108bbe8bb4e73e0ff4684c8f78001a80438c4e..f3510b145664298adafb151b6f0db51b972f01ec\n--- a/docs/adr/0010-external-ai-guardian-review.md\n+++ b/docs/adr/SADR-0010-external-ai-guardian-review.md\n@@ -1,4 +1,4 @@\n-# ADR 0010: External AI Guardian Review As A First-Class Approval Authority\n+# SADR-0010: External AI Guardian Review As A First-Class Approval Authority\n \n ## Status\n \n@@ -8,7 +8,7 @@\n \n Shiki is intended to be an autonomous, recursive, self-improving agent\n framework: authority-in-the-loop, not human-in-the-loop. Until now the Guardian\n-approval required for high/critical-risk merges (guardian-policy.json, ADR\n+approval required for high/critical-risk merges (guardian-policy.json, SADR\n governance) recognized only human authority sources \u2014 a GitHub review approval,\n the `guardian:approved` label applied by a configured Guardian, or a comment\n carrying `Guardian approval granted` plus the current head SHA.\n@@ -89,7 +89,7 @@\n   autonomous design the operator intends and produced the impersonation\n   incident.\n - **Record AI approval as an operator comment**: rejected as forgery of human\n-  authority \u2014 the exact failure this ADR corrects.\n+  authority \u2014 the exact failure this SADR corrects.\n - **Deliver the artifact as a committed `.shiki/` file**: rejected; a committed\n   approval inside the PR being approved is forgeable by anyone who can push.\n - **A dedicated reviewer bot identity instead of a relayed comment**: deferred;\ndiff --git a/docs/adr/0011-autonomous-quality-gate-evidence.md b/docs/adr/SADR-0011-autonomous-quality-gate-evidence.md\nindex bca0246c5d46e44afbde15486a75eec425fc02f5..9c93eac16d8c3a6d4b11bd7224e3955102cd7db4\n--- a/docs/adr/0011-autonomous-quality-gate-evidence.md\n+++ b/docs/adr/SADR-0011-autonomous-quality-gate-evidence.md\n@@ -1,4 +1,4 @@\n-# ADR 0011: Autonomous Quality-Gate Evidence \u2014 Deterministic Facts And An Independent Verifier\n+# SADR-0011: Autonomous Quality-Gate Evidence \u2014 Deterministic Facts And An Independent Verifier\n \n ## Status\n \ndiff --git a/docs/adr/0012-autonomous-loop-closeout-to-main.md b/docs/adr/SADR-0012-autonomous-loop-closeout-to-main.md\nindex 637fad6dde4989c9c2bf0f81f676d8d29991123c..ec7d85f81030a676bcbba28b43eb365422a36eac\n--- a/docs/adr/0012-autonomous-loop-closeout-to-main.md\n+++ b/docs/adr/SADR-0012-autonomous-loop-closeout-to-main.md\n@@ -1,4 +1,4 @@\n-# 0012 \u2014 Autonomous loop pushes goal completion to main via a closeout PR\n+# SADR-0012 \u2014 Autonomous loop pushes goal completion to main via a closeout PR\n \n - Status: accepted\n - Date: 2026-06-18\ndiff --git a/docs/adr/0013-bootstrap-grants-actions-approve-pull-requests.md b/docs/adr/SADR-0013-bootstrap-grants-actions-approve-pull-requests.md\nindex b6c69d64f1c5ced346b88d6859586715b3b5bd0e..cf48ff4a6716267f539ca58edd1a852e7c7a2d64\n--- a/docs/adr/0013-bootstrap-grants-actions-approve-pull-requests.md\n+++ b/docs/adr/SADR-0013-bootstrap-grants-actions-approve-pull-requests.md\n@@ -1,4 +1,4 @@\n-# ADR 0013: Bootstrap Grants GitHub Actions Permission To Create And Approve Pull Requests\n+# SADR-0013: Bootstrap Grants GitHub Actions Permission To Create And Approve Pull Requests\n \n ## Status\n \n@@ -31,7 +31,7 @@\n security tradeoff: it lets repository automation satisfy a human-review gate. In\n a multi-maintainer repo that could let a compromised or buggy workflow\n self-approve changes. So whether bootstrap turns it on by default \u2014 and with\n-what default token scope \u2014 is an ADR-worthy decision, not an implementation\n+what default token scope \u2014 is a SADR-worthy decision, not an implementation\n detail.\n \n ## Decision\n@@ -48,7 +48,7 @@\n Specifically:\n \n - **`can_approve_pull_request_reviews` defaults to `true`.** Shiki's default\n-  posture is solo/AFK autonomous operation (ADR 0008/0009/0012), where the CCA\n+  posture is solo/AFK autonomous operation (SADR-0008/0009/0012), where the CCA\n   Review Bridge is the only path that can satisfy `required_review: true` after\n   the CCA and Guardian gates have already passed. The default makes a\n   freshly-bootstrapped repo able to run the autonomous loop end to end without a\ndiff --git a/docs/adr/0014-codex-app-external-ai-guardian-ui-adapter.md b/docs/adr/SADR-0014-codex-app-external-ai-guardian-ui-adapter.md\nindex 238db06279334caa341c117362df9271382a7fcd..65f12e9939321e1b3e389a34c822fc1549e5155d\n--- a/docs/adr/0014-codex-app-external-ai-guardian-ui-adapter.md\n+++ b/docs/adr/SADR-0014-codex-app-external-ai-guardian-ui-adapter.md\n@@ -1,4 +1,4 @@\n-# ADR 0014: Use Codex App As The External AI Guardian UI Adapter\n+# SADR-0014: Use Codex App As The External AI Guardian UI Adapter\n \n ## Status\n \ndiff --git a/docs/adr/0015-contract-approval-before-dispatch.md b/docs/adr/SADR-0015-contract-approval-before-dispatch.md\nindex fad6c656741a20a7684ba16080a7db159f63a52f..6061271a46d3f1d6ae7174477de3fb75673123a6\n--- a/docs/adr/0015-contract-approval-before-dispatch.md\n+++ b/docs/adr/SADR-0015-contract-approval-before-dispatch.md\n@@ -1,4 +1,4 @@\n-# ADR 0015: Approve The Task Contract Before Dispatch, Not The Diff After It\n+# SADR-0015: Approve The Task Contract Before Dispatch, Not The Diff After It\n \n ## Status\n \n@@ -9,7 +9,7 @@\n Guardian approval for high/critical risk is expressible only on a pull request.\n Every approval source in `guardian-policy.json` \u2014 GitHub review, the\n `guardian:approved` label, the `Guardian approval granted` comment, and the\n-`external_ai_guardian_review` artifact (ADR 0010/0014) \u2014 is bound to a PR, and\n+`external_ai_guardian_review` artifact (SADR-0010/0014) \u2014 is bound to a PR, and\n three of the four require the current head SHA. Approval therefore cannot exist\n before an implementation PR exists.\n \n@@ -81,7 +81,7 @@\n An envelope breach must surface only on the non-repairable policy gate, never on\n the repairable `MergeGate metadata check`. Otherwise the Goal Loop routes it to\n `dispatch_repair` \u2014 instructing a runner to make the breach go away \u2014 which is\n-exactly what this ADR forbids.\n+exactly what this SADR forbids.\n \n **Contract immutability.** After registration, an implementation PR must not\n change the governance fields of its own task contract (`scope`, `non_goals`,\n@@ -116,7 +116,7 @@\n fails any verdict that is not `complete`, and the signal the CCA prompt reads is\n computed from `evaluate_guardian_approval` alone. Without the OR-merge at the\n signal, a contract-approved PR still returns `needs_guardian`, the CCA job fails,\n-and the MergeGate policy check never runs \u2014 the gate this ADR changes would never\n+and the MergeGate policy check never runs \u2014 the gate this SADR changes would never\n be reached. The CCA *prompt* needs no change: a `contract_approval` entry in the\n signal's existing `sources` array satisfies its current wording.\n \n@@ -130,7 +130,7 @@\n written, and an approved contract lets its implementation PRs merge through the\n same automatic path as low/medium risk. The wasted implement-then-block cycle\n disappears. Guardian review shifts from reading diffs to reading contracts, which\n-is both earlier and smaller; the external AI Guardian path (ADR 0010/0014) works\n+is both earlier and smaller; the external AI Guardian path (SADR-0010/0014) works\n on contracts with no change.\n \n We accept that the Guardian authorizes a class of change without seeing its\n@@ -146,7 +146,7 @@\n adversarial tests for the new approval path equivalent in rigor to\n `test_shiki_governance_evidence.sh`.\n \n-Bootstrap costs, which apply only while this ADR is being implemented. The tasks\n+Bootstrap costs, which apply only while this SADR is being implemented. The tasks\n that build Contract Approval merge under the existing rules, so the ones\n dispatched after the pre-dispatch gate lands but before the approval path lands\n need a Guardian-approved Contract PR *and* a live Guardian approval on their\n@@ -210,7 +210,7 @@\n risk; they do not bound authorized work.\n \n **Auto-escalate risk on envelope breach instead of stopping.** Returns the\n-workflow to a diff-time Guardian decision \u2014 the exact behavior this ADR removes.\n+workflow to a diff-time Guardian decision \u2014 the exact behavior this SADR removes.\n \n **Resolve governance fields from the base snapshot only.** Rejected against the\n registration flow: implementation PRs add their own contract, so the base has no\ndiff --git a/docs/adr/0016-locks-bound-product-paths.md b/docs/adr/SADR-0016-locks-bound-product-paths.md\nindex b5e850f3f1f7f4ef6e7f9d1763a29a51bd300c45..46b40a9b99a7aae644c02bb458f5473ff0a90ce2\n--- a/docs/adr/0016-locks-bound-product-paths.md\n+++ b/docs/adr/SADR-0016-locks-bound-product-paths.md\n@@ -1,4 +1,4 @@\n-# ADR 0016: Locks bound product paths; the Shiki mirror is bound by record identity\n+# SADR-0016: Locks bound product paths; the Shiki mirror is bound by record identity\n \n ## Status\n \n@@ -6,8 +6,8 @@\n \n - Date: 2026-07-28\n - Deciders: operator (mizutani-140), Claude (planner)\n-- Related: ADR 0004 (GitHub-first with a `.shiki` mirror), ADR 0009 (Spec Freeze),\n-  ADR 0012 (autonomous loop closeout to main), ADR 0015 (Contract Approval before\n+- Related: SADR-0004 (GitHub-first with a `.shiki` mirror), SADR-0009 (Spec Freeze),\n+  SADR-0012 (autonomous loop closeout to main), SADR-0015 (Contract Approval before\n   dispatch), remediation goal task R-02, the live PR #179 deadlock.\n \n ## Context\n@@ -90,11 +90,11 @@\n files, documentation files and named source modules are nameable; runtime-\n allocated ledger, runner and report ids are not \u2014 and those live in `.shiki`,\n which locks no longer govern. Existing frozen contracts are brought into line by\n-a one-time Spec Amendment per plan, which must land before ADR 0015's contract\n+a one-time Spec Amendment per plan, which must land before SADR-0015's contract\n immutability makes `locks` unwritable from the implementation PR.\n \n **Lock state is resolved from the durable side.** MergeGate resolves the lock\n-registry, and ADR 0015's governance fields, from the base `.shiki` snapshot when\n+registry, and SADR-0015's governance fields, from the base `.shiki` snapshot when\n present, falling back to head only when absent. The `MergeGate metadata check`\n job is given `--base-shiki`, which it does not receive today. Locks are not\n derived from the live open-pull-request set: a pull request body is untrusted\n@@ -108,7 +108,7 @@\n Explicitly out of scope: moving MergeGate's contract resolution off the PR\n checkout wholesale; making the mirror a projection of GitHub state; introducing\n new lock kinds; moving ledger evidence out of the repository; and unwinding\n-ADR 0012's closeout pull request. Registration of multi-task goals is ADR 0015's\n+SADR-0012's closeout pull request. Registration of multi-task goals is SADR-0015's\n Contract PR and is not re-decided here.\n \n ## Consequences\n@@ -127,7 +127,7 @@\n \n Risks accepted. First, goal-scoped identity is only as strong as `goal_id`,\n which is resolved from the PR body and the head task file; the mitigation is to\n-add `goal_id` to ADR 0015's frozen governance field set, and until that lands a\n+add `goal_id` to SADR-0015's frozen governance field set, and until that lands a\n task file can name a foreign goal. Second, base-when-present resolution is\n unavailable on fork pull requests, where the head is contributor-controlled;\n fork pull requests already cannot complete a Shiki merge, and this decision does\n@@ -139,7 +139,7 @@\n path, not by implementation pull requests.\n \n New obligations: one Spec Amendment per frozen plan to rewrite `locks`, ordered\n-before ADR 0015 T1; extension of the adversarial suites in\n+before SADR-0015 T1; extension of the adversarial suites in\n `scripts/test_shiki_adversarial_state.sh` and\n `scripts/test_shiki_governance_evidence.sh` to cover the eleven newly-scoped\n directories; and updating `scripts/test_shiki_loop_lock_guard.sh`,\n@@ -151,32 +151,32 @@\n whether the loop's transitive sync of ledger `evidence` references can be\n deleted once the Contract PR carries its own source plan.\n \n-## Relationship to ADR 0004\n+## Relationship to SADR-0004\n \n-This ADR **clarifies something ADR 0004 left unspecified**. ADR 0004 decides that\n+This SADR **clarifies something SADR-0004 left unspecified**. SADR-0004 decides that\n GitHub is the operational source of truth and that each target repository keeps a\n `.shiki/` mirror. It says nothing about pull requests writing the mirror, does\n not name MergeGate, and does not make the mirror an input to a gate. Nothing in\n-ADR 0004's Decision or Consequences is amended, superseded or weakened: the\n+SADR-0004's Decision or Consequences is amended, superseded or weakened: the\n mirror still exists, still records the same artifacts, and pull request\n-boundaries remain the coordination surface. What this ADR settles is the question\n+boundaries remain the coordination surface. What this SADR settles is the question\n 0004 never reached \u2014 by what authority a pull request may write a mirror record \u2014\n and it answers it the way `.shiki/manifest.json` already declares\n (`pr_mutation: current-task-or-current-goal-only`).\n \n-It **amends ADR 0012**, whose Decision item 5 states that \"Loop-executed tasks\n+It **amends SADR-0012**, whose Decision item 5 states that \"Loop-executed tasks\n declare the `path:.shiki/**` lock, which covers every `.shiki` file the closeout\n stages ... so `files_outside_locks` is satisfied without per-file lock\n bookkeeping.\" That clause is replaced: the closeout's mirror writes are\n-authorized by record identity, not by a declared lock. ADR 0012's substantive\n+authorized by record identity, not by a declared lock. SADR-0012's substantive\n decision \u2014 that completion reaches `main` through a closeout pull request rather\n than local attestation \u2014 is preserved unchanged.\n \n-It is **complementary to ADR 0015**, not a substitute. ADR 0015 owns contract\n+It is **complementary to SADR-0015**, not a substitute. SADR-0015 owns contract\n immutability, the pre-dispatch gate, the Contract PR and the Scope Envelope. This\n-ADR owns what a lock means and who may write the mirror. The one ordering\n+SADR owns what a lock means and who may write the mirror. The one ordering\n constraint between them is that the lock-narrowing Spec Amendment must precede\n-ADR 0015 T1's freezing of the `locks` field.\n+SADR-0015 T1's freezing of the `locks` field.\n \n ## Alternatives Considered\n \n@@ -186,7 +186,7 @@\n the only writer that can register them may run. Migrating the Guardian signal to\n that store makes every registration PR fail closed to `needs_guardian` at every\n risk level, because the task file is by definition absent from the store, which\n-removes the low/medium autonomous path ADR 0008/0009 exists to provide. And it\n+removes the low/medium autonomous path SADR-0008/0009 exists to provide. And it\n leaves schemas, manifest, config, guardian policy, templates, migrations, plans,\n worktrees, runner records, runs and handoffs with no writer at all in a\n repository whose product is `.shiki`. Measured benefit on the frozen backlog:\n@@ -199,14 +199,14 @@\n namespace is not in fact ownership-partitioned \u2014 56 ledger entries carry no\n `task_id`, and one goal DAG is writable by 35 registered tasks. The correct\n expression of its insight is that locks do not govern the mirror at all, which is\n-what this ADR decides.\n+what this SADR decides.\n \n **Make the mirror a projection written only by a registration writer and a\n post-merge projection job.** Rejected. It requires a bypass actor with write\n access to the protected default branch; it makes its own master invariant\n readable from a manifest that pull requests may edit; it deletes both reconcile\n modes and leaves a corrupted mirror with no in-band repair path, contradicting\n-ADR 0004's consequence that mirror conflicts are repaired against GitHub state;\n+SADR-0004's consequence that mirror conflicts are repaired against GitHub state;\n and its content-derived ids are rejected by `validate_shiki`'s currently-green id\n pattern.\n \ndiff --git a/docs/adr/0017-bookkeeping-closeouts-do-not-inherit-risk.md b/docs/adr/SADR-0017-bookkeeping-closeouts-do-not-inherit-risk.md\nindex 554e3a0d526a72ca067108c5464cc68c3edcf174..b9676857a4713850e063af7c39c8e0b01ecb4c66\n--- a/docs/adr/0017-bookkeeping-closeouts-do-not-inherit-risk.md\n+++ b/docs/adr/SADR-0017-bookkeeping-closeouts-do-not-inherit-risk.md\n@@ -1,4 +1,4 @@\n-# ADR 0017: A Bookkeeping Closeout Does Not Inherit Its Task's Risk\n+# SADR-0017: A Bookkeeping Closeout Does Not Inherit Its Task's Risk\n \n ## Status\n \n@@ -6,7 +6,7 @@\n \n ## Context\n \n-ADR 0012 made the goal loop push a task's completion to the default branch through a\n+SADR-0012 made the goal loop push a task's completion to the default branch through a\n closeout pull request: after the implementation PR merges, the loop opens a second PR\n carrying `task.status = done`, `lock.state = released`, and \u2014 when the task completes\n its goal \u2014 `goal.status = complete` plus one scorecard report. Nothing else. The\n@@ -49,14 +49,14 @@\n 4. The task file's governance fields (`scope`, `non_goals`, `required_skills`,\n    `risk_level`, `locks`, `acceptance_checks`, `test_command`) are byte-identical to\n    the base snapshot. Only `status`, `expected_pr`, `closeout_pr`, `ledger_evidence`\n-   and `expected_branch` may differ \u2014 the ADR 0015 contract-immutability rule,\n+   and `expected_branch` may differ \u2014 the SADR-0015 contract-immutability rule,\n    unchanged.\n \n    `expected_branch` is in that set because a closeout is *by construction* on a\n    different branch from the implementation it closes, and the loop rewrites the\n    field accordingly. It is safe to let it differ because MergeGate independently\n    requires `expected_branch` to equal the PR's actual head ref, so a PR cannot set\n-   it to anything other than the branch it is really on. The first draft of this ADR\n+   it to anything other than the branch it is really on. The first draft of this SADR\n    omitted it, which made the exemption unreachable: every real closeout failed\n    condition 4 on a field the loop always changes. See PR #209.\n 5. The state transitions are exactly the terminal set: task `review` \u2192 `done`, lock\n@@ -113,12 +113,12 @@\n classification must be derived, not asserted.\n \n **Fold the closeout into the implementation PR** so no second PR exists. Rejected under\n-ADR 0012, which recorded why: a task file that is already `done` on the implementation\n+SADR-0012, which recorded why: a task file that is already `done` on the implementation\n branch short-circuits the loop's review \u2192 CCA \u2192 merge \u2192 done ordering into a local\n goal-completion before any merge happens.\n \n **Have the loop auto-merge closeouts with elevated credentials.** Rejected outright: that\n-is a runtime authoring its own approval, which ADR 0010 and the constitution's\n+is a runtime authoring its own approval, which SADR-0010 and the constitution's\n approval-record prohibition forbid. It would also be indistinguishable, in the evidence,\n from the forgery those rules exist to prevent.\n \ndiff --git a/docs/adr/0018-carried-guardian-approval-across-a-proven-base-sync.md b/docs/adr/SADR-0018-carried-guardian-approval-across-a-proven-base-sync.md\nindex 22358562da390ed8b45cd600da08a43d378b4680..e6e2907cefa776460af73ddf7d39db605f3c39b0\n--- a/docs/adr/0018-carried-guardian-approval-across-a-proven-base-sync.md\n+++ b/docs/adr/SADR-0018-carried-guardian-approval-across-a-proven-base-sync.md\n@@ -1,4 +1,4 @@\n-# ADR 0018: Carried Guardian Approval Across A Proven Base Sync\n+# SADR-0018: Carried Guardian Approval Across A Proven Base Sync\n \n ## Status\n \n@@ -34,7 +34,7 @@\n Guardian evaluator that consumes it (`shiki_guardian.evaluate_guardian_approval`,\n the `guardian_comment_carried` source) were built and tested in isolation. Neither\n does anything until a caller passes the flags that turn the carry on, so the\n-feature was invisible in CI. This ADR records the decision the two of them\n+feature was invisible in CI. This SADR records the decision the two of them\n implement, and this task wires it into the four CI invocations that judge Guardian\n approval.\n \ndiff --git a/docs/adr/README.md b/docs/adr/README.md\nindex c70e057a4520c36e042ab68c53cd4a21ef709b71..5da43856836e2ce6a150f8127766e2f88355ab4b\n--- a/docs/adr/README.md\n+++ b/docs/adr/README.md\n@@ -1,40 +1,41 @@\n-# Architecture Decision Records\n+# Decision Record Namespaces\n \n-This directory holds Shiki's Architecture Decision Records (ADRs): durable\n-records of hard-to-reverse platform decisions. ADRs are part of the Shiki source\n-of truth alongside GitHub state, the `.shiki/` mirror, and `CONTEXT.md`.\n+This directory holds two independent decision namespaces:\n \n-## How ADRs Work\n+- Shiki Architecture Decision Records (SADRs), named\n+  `SADR-NNNN-<short-slug>.md`, record hard-to-reverse Shiki platform decisions.\n+- Target Architecture Decision Records (ADRs), named\n+  `NNNN-<short-slug>.md`, belong to the target product.\n \n-- New ADRs are added as `docs/adr/NNNN-<short-slug>.md`, where `NNNN` is the next\n-  zero-padded sequence number.\n-- The **canonical list of ADRs is the set of files matching `docs/adr/*.md`** in\n-  this directory. Browse `docs/adr/*.md` for the authoritative, current set.\n-- Use [`template.md`](template.md) as the starting point for a new ADR.\n-- Record an ADR when a decision is hard to reverse, surprising without context,\n-  and a real tradeoff. Do not write an ADR for routine or easily reversible\n-  choices.\n+The same four-digit number may appear once in each namespace. A platform\n+`SADR-NNNN` citation resolves only to `SADR-NNNN-*.md`; a target `ADR NNNN`\n+citation resolves only to `NNNN-*.md`.\n \n-Using the `docs/adr/*.md` glob as the source of truth means other PRs can add an\n-ADR without editing this index file, avoiding merge contention on a\n-hand-maintained enumeration.\n+## How Decision Records Work\n \n-## Current ADRs (snapshot)\n-\n-The list below is a convenience snapshot and may lag behind the directory. When\n-in doubt, list `docs/adr/*.md`.\n+- New Shiki platform decisions use the next zero-padded SADR number.\n+- New target product decisions use the target's own ADR sequence.\n+- The canonical list is the files in `docs/adr/`; no hand-maintained index is\n+  authoritative.\n+- Use [`template.md`](template.md) as the starting point and choose the filename\n+  namespace that owns the decision.\n+- Record a decision when it is hard to reverse, surprising without context, and\n+  a real tradeoff. Do not write a decision record for routine or easily\n+  reversible choices.\n \n-- [0001 \u2014 Keep Shiki As The Platform Name](0001-keep-shiki-as-platform-name.md)\n-- [0002 \u2014 Use AGENTS As The Shared Agent Constitution](0002-use-agents-as-shared-constitution.md)\n-- [0003 \u2014 Require Skill Gate For MergeGate Readiness](0003-require-skill-gate-for-mergegate-readiness.md)\n-- [0004 \u2014 Use GitHub First With A Repository-Local Shiki Mirror](0004-use-github-first-with-shiki-mirror.md)\n-- [0005 \u2014 Use Codex As Front And Claude Code Action As The GitHub Runtime](0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md)\n-- [0006 \u2014 Separate Codex Implementation, CCA Completion, And MergeGate Authorization](0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md)\n+Using the directory as the source of truth lets independent changes add records\n+without contending on an enumerated index.\n \n-## Adding A New ADR\n+## Adding A Shiki SADR\n \n-1. Copy `template.md` to `docs/adr/NNNN-<short-slug>.md`.\n+1. Copy `template.md` to `docs/adr/SADR-NNNN-<short-slug>.md`.\n 2. Fill in the title, status, context, decision, and consequences.\n-3. Set the status to `Proposed`, then `Accepted` once the decision is approved.\n-4. Open a PR following `CONTRIBUTING.md`. You do not need to edit this index for\n-   the ADR to be discoverable via `docs/adr/*.md`.\n+3. Set the status to `Proposed`, then `Accepted` once the proper authority has\n+   approved the decision.\n+4. Open a PR following `CONTRIBUTING.md`.\n+\n+## Adding A Target ADR\n+\n+1. Copy `template.md` to `docs/adr/NNNN-<short-slug>.md` using the target's own\n+   sequence.\n+2. Cite it as `ADR NNNN`; do not prefix its filename with `SADR-`.\ndiff --git a/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md b/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\nnew file mode 100644\nindex 0000000000000000000000000000000000000000..7455ce1f3bed02ddddb851ac21a1d1a648c9aeba\n--- /dev/null\n+++ b/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\n@@ -0,0 +1,55 @@\n+# SADR-0019: Separate Shiki Platform SADRs From Target ADRs\n+\n+## Status\n+\n+Accepted\n+\n+## Context\n+\n+Shiki installs its platform decision records into `docs/adr/` in every target\n+repository. Shiki and its targets previously used the same numeric\n+`NNNN-<slug>.md` namespace. A target that already owned ADRs 0013 through 0018\n+therefore acquired two records for each number during installation, making an\n+`ADR NNNN` citation ambiguous. Renumbering target decisions would rewrite target\n+history, while renumbering Shiki decisions per target would destroy their stable\n+platform identity.\n+\n+Upgrades also need to remove the former Shiki-owned numeric paths without ever\n+mistaking a target-owned ADR for platform content. The install stamp already\n+records exact shipped paths and content digests, so it can prove ownership only\n+when both still match.\n+\n+## Decision\n+\n+Shiki platform decisions use the `SADR` namespace:\n+\n+- records are named `docs/adr/SADR-NNNN-<slug>.md`;\n+- live platform citations use `SADR-NNNN`;\n+- target product decisions retain `docs/adr/NNNN-<slug>.md` and `ADR NNNN`;\n+- the reference validator resolves the two namespaces independently, so the same\n+  four-digit number may exist once in each namespace.\n+\n+Before a forced upgrade writes anything, the installer inventories the exact\n+former numeric Shiki ADR paths. It may delete a legacy path only when the\n+existing install stamp contains that exact path and its recorded SHA-256 digest\n+matches the file currently on disk. All ownership blockers are reported\n+together and abort the upgrade before its first write. A non-forced install\n+never performs this cleanup.\n+\n+Historical `.shiki/` evidence remains unchanged because it records the namespace\n+that existed when that evidence was produced.\n+\n+## Consequences\n+\n+- Targets may use any ADR number without colliding with Shiki's platform SADRs.\n+- Fresh installs preserve target ADR bytes and install a stable SADR set.\n+- Forced upgrades fail closed when legacy ownership cannot be proven, and a\n+  successful upgrade refreshes the stamp without removed legacy paths.\n+- Current governance, packaging, workflow, prompt, script, and skill references\n+  must move with the platform records.\n+\n+## Alternatives Considered\n+\n+- Collision-only refusal leaves affected targets permanently unupgradable.\n+- Renumbering target ADRs mutates target-owned history.\n+- Per-target Shiki renumbering makes platform citations non-portable.\ndiff --git a/docs/adr/template.md b/docs/adr/template.md\nindex a92a97daddd0b00ab9dd626cc8d86942da2eafed..17dd79175b5fee2105935557f691b40cf9492942\n--- a/docs/adr/template.md\n+++ b/docs/adr/template.md\n@@ -1,14 +1,14 @@\n-# NNNN: <Short Decision Title>\n+# <SADR-NNNN or NNNN>: <Short Decision Title>\n \n ## Status\n \n-Proposed | Accepted | Superseded by ADR-NNNN\n+Proposed | Accepted | Superseded by SADR-NNNN or target ADR NNNN\n \n ## Context\n \n What is the situation, constraint, or force that requires a decision? Reference\n-relevant domain terms (`CONTEXT.md`), prior ADRs, and code reality. State the\n-problem, not the solution.\n+relevant domain terms (`CONTEXT.md`), prior Shiki SADRs or target ADRs, and code\n+reality. State the problem, not the solution.\n \n ## Decision\n \ndiff --git a/docs/agents/autonomous-loop-e2e-acceptance.md b/docs/agents/autonomous-loop-e2e-acceptance.md\nindex bbf84c6081987f159ac6ad9ecf3c82b7aa72e7e8..5c13c1d02a05a90b0dc46cbef7c95a9846d0c237\n--- a/docs/agents/autonomous-loop-e2e-acceptance.md\n+++ b/docs/agents/autonomous-loop-e2e-acceptance.md\n@@ -40,8 +40,8 @@\n 3. `gh auth status` is logged in with merge permission on the repo.\n 4. Branch protection + required checks are configured exactly as the goal-loop\n    required-check set expects (see `docs/agents/branch-protection-smoke.md`).\n-5. The repo `.shiki/policy` permits autonomous low/medium-risk merges (ADR\n-   0008/0009); the MergeGate policy check is the recorded-authority gate.\n+5. The repo `.shiki/policy` permits autonomous low/medium-risk merges\n+   (SADR-0008/0009); the MergeGate policy check is the recorded-authority gate.\n \n The operator's only actions are: register the task and start the loop. **No\n manual pre-clearing** means after `shiki loop run` starts, the operator does not\ndiff --git a/docs/agents/bootstrap-command.md b/docs/agents/bootstrap-command.md\nindex 05f7f203a52cf412b9f97084a6e131ed0f785afd..f7aa0b734dfebee4a3b139335b66ee3415f48713\n--- a/docs/agents/bootstrap-command.md\n+++ b/docs/agents/bootstrap-command.md\n@@ -189,7 +189,9 @@\n ```\n \n Use this only for tests, fixtures, or explicit local-only template inspection.\n-Use `--force` only when you intentionally want to overwrite existing target files.\n+Use `--force` only when you intentionally want to refresh the target template.\n+Legacy numeric Shiki decision cleanup is stamp-and-digest authorized before any\n+write; target ADR paths are not cleanup candidates.\n \n ## Slash Command\n \ndiff --git a/docs/agents/checklists.md b/docs/agents/checklists.md\nindex 4a7f03ade346f53a1c1d82981f3fd744f9ab4547..0c5ec6c53dc7197b7b7a7d18e667b325babd6c45\n--- a/docs/agents/checklists.md\n+++ b/docs/agents/checklists.md\n@@ -32,18 +32,18 @@\n | ID | Item | Blocking |\n | --- | --- | --- |\n | GD-01 | Plan was challenged against `CONTEXT.md`. | blocking for non-trivial Goals |\n-| GD-02 | Relevant ADRs were read or absence was recorded. | blocking for non-trivial Goals |\n+| GD-02 | Relevant SADRs and target ADRs were read or absence was recorded. | blocking for non-trivial Goals |\n | GD-03 | Ambiguous terms were resolved or converted to HITL blockers. | blocking |\n | GD-04 | Edge scenarios were considered. | advisory |\n | GD-05 | Code reality was checked when code could answer. | blocking when applicable |\n | GD-06 | New domain terms were added to `CONTEXT.md` when needed. | blocking when applicable |\n-| GD-07 | ADR was created or explicitly skipped using the ADR criteria. | blocking when applicable |\n+| GD-07 | A SADR or target ADR was created or explicitly skipped using the decision-record criteria. | blocking when applicable |\n \n ## CI \u2014 Context & Impact Checklist\n \n | ID | Item | Blocking |\n | --- | --- | --- |\n-| CI-01 | Relevant docs and ADRs identified. | blocking |\n+| CI-01 | Relevant docs, SADRs, and target ADRs identified. | blocking |\n | CI-02 | Relevant modules, interfaces, seams, callers, and tests identified. | blocking |\n | CI-03 | Dependencies and blocked-by relationships identified. | blocking |\n | CI-04 | Candidate locks identified. | blocking |\n@@ -62,7 +62,7 @@\n | PRD-04 | Implementation decisions are recorded without volatile code detail. | blocking |\n | PRD-05 | Testing decisions are recorded. | blocking |\n | PRD-06 | Out-of-scope items are explicit. | blocking |\n-| PRD-07 | Relevant glossary/ADR links are present when applicable. | blocking |\n+| PRD-07 | Relevant glossary/SADR/target-ADR links are present when applicable. | blocking |\n \n ## SF \u2014 Spec Freeze Checklist\n \n@@ -98,7 +98,7 @@\n | H-02 | Branch/worktree target is present. | blocking |\n | H-03 | Scope and non-goals are present. | blocking |\n | H-04 | Dependencies and locks are present. | blocking |\n-| H-05 | Relevant docs, ADRs, modules, and tests are referenced. | blocking |\n+| H-05 | Relevant docs, SADRs, target ADRs, modules, and tests are referenced. | blocking |\n | H-06 | Required skills are stated. | blocking |\n | H-07 | TDD expectations are stated. | blocking when behavior work |\n | H-08 | Acceptance criteria are included. | blocking |\ndiff --git a/docs/agents/codex-handoff.md b/docs/agents/codex-handoff.md\nindex ede468f47cb277f69a857e8c45df703e4ed0151d..6f97bdffb67e44d7edea2c71e01ea28933c4eb6c\n--- a/docs/agents/codex-handoff.md\n+++ b/docs/agents/codex-handoff.md\n@@ -46,7 +46,7 @@\n \n ## Context\n - Domain terms:\n-- Relevant ADRs:\n+- Relevant SADRs and target ADRs:\n - Relevant docs:\n - Relevant modules/interfaces/seams:\n - Similar tests/prior art:\ndiff --git a/docs/agents/completion-check-agent.md b/docs/agents/completion-check-agent.md\nindex 13e4c035db35025f4e3cdcc5adfb24314ac4d521..61bfa1218cf36ca605250c91919e620f33238aa0\n--- a/docs/agents/completion-check-agent.md\n+++ b/docs/agents/completion-check-agent.md\n@@ -32,7 +32,7 @@\n - `AGENTS.md`\n - `CLAUDE.md`\n - `CONTEXT.md`\n-- relevant ADRs\n+- relevant SADRs and target ADRs\n - parent Goal issue\n - PRD issue or document\n - task issue\ndiff --git a/docs/agents/decision-control.md b/docs/agents/decision-control.md\nindex 7d8ee5ab03a5ccbcbd04056ae61de03785a2a9c9..8c94ecda00fffaa0e92c325ab83aa9776bd1dac1\n--- a/docs/agents/decision-control.md\n+++ b/docs/agents/decision-control.md\n@@ -11,7 +11,7 @@\n | Decision | Owner | When | Required inputs | Output | Enforcement surface |\n | --- | --- | --- | --- | --- | --- |\n | Is the Goal clear enough to plan? | Planner + Guardian when needed | Goal intake | User request, existing docs, risk signals | Goal readiness verdict | Goal checklist, issue template, ledger |\n-| Must `grill-with-docs` run? | Planner / Skill Gate | Before PRD or issue split | Ambiguity, domain terms, ADR conflicts, unclear boundaries | Skill invocation or explicit skip rationale | Skill Gate checklist |\n+| Must `grill-with-docs` run? | Planner / Skill Gate | Before PRD or issue split | Ambiguity, domain terms, SADR/target-ADR conflicts, unclear boundaries | Skill invocation or explicit skip rationale | Skill Gate checklist |\n | Is a PRD required? | Planner | After Goal context stabilizes | Goal, `grill-with-docs` output, Context & Impact output | PRD required / not required | PRD checklist, CCA evidence check |\n | Can the spec be frozen? | Operator only | End of Requirements Definition, before issue split | PRD/requirements summary, scope inventory (SF-02), open-question list | `spec_freeze` block in the plan + ledger evidence | `require_grilled_plan` (plan ingest/run), SF checklist |\n | Can a frozen spec change? | Operator only (Spec Amendment) | When implementation contradicts the frozen spec | Contested decisions, scoped re-grill result | recorded amendment + re-stamped freeze | SF checklist, ledger evidence |\ndiff --git a/docs/agents/domain.md b/docs/agents/domain.md\nindex 258da8ea81e87ac0a6b0b5b5e5c2fde980eaabb7..d57b0cf17ff3e1fa9e6e5885372bf083d8295421\n--- a/docs/agents/domain.md\n+++ b/docs/agents/domain.md\n@@ -5,7 +5,9 @@\n ## Layout\n \n - `CONTEXT.md` defines Shiki language. Keep it implementation-free.\n-- `docs/adr/` records hard-to-reverse decisions.\n+- `docs/adr/SADR-NNNN-*.md` records Shiki platform decisions.\n+- `docs/adr/NNNN-*.md` records target product decisions without sharing the\n+  SADR namespace.\n - `AGENTS.md` is the runtime-neutral agent constitution.\n - `CLAUDE.md` is the Claude Code wrapper around `AGENTS.md`.\n - `.shiki/` is the Target Repository mirror for Goals, PRDs, plans, Task DAGs, contracts, locks, worktree records, ledger entries, CCA verdicts, repair packets, reports, and handoffs.\n@@ -15,6 +17,6 @@\n \n - Use Shiki terms exactly when they exist.\n - Add glossary terms to `CONTEXT.md` only when the term is part of the domain, not a generic programming concept.\n-- Create ADRs only for hard-to-reverse, surprising, tradeoff-based decisions.\n+- Create SADRs or target ADRs only for hard-to-reverse, surprising, tradeoff-based decisions.\n - Keep product-specific language in the Target Repository, not in Shiki Template docs.\n-- If an ADR conflicts with a proposed implementation, surface the conflict before editing.\n+- If a SADR or target ADR conflicts with a proposed implementation, surface the conflict before editing.\ndiff --git a/docs/agents/execution-control.md b/docs/agents/execution-control.md\nindex ad8ee3cdb36be56fedc1f46408ca92b387d74047..bc63c3a73661d12b567016ba299f1a6b66bf9088\n--- a/docs/agents/execution-control.md\n+++ b/docs/agents/execution-control.md\n@@ -63,7 +63,7 @@\n | Branch name | Orchestrator | Orchestrator | Before implementer dispatch | branch policy | branch ref in task/ledger |\n | Lock reservation | Context & Impact | MergeGate | Before dispatch and before merge | `.shiki/locks/`, labels, PR metadata | lock id, owner, scope |\n | CI profile | Planner | MergeGate | During issue triage, rechecked at PR | labels, `.shiki/policy.yaml`, workflow matrix | required check list |\n-| Required status checks | Repo maintainer / Guardian | GitHub branch protection / rulesets | Repository setup, updated via ADR | GitHub settings | branch/ruleset config, ADR |\n+| Required status checks | Repo maintainer / Guardian | GitHub branch protection / rulesets | Repository setup, updated via target ADR | GitHub settings | branch/ruleset config, target ADR |\n | CCA judgment | CCA | GitHub required check / MergeGate | Every non-draft PR update | GitHub Action check | structured CCA verdict |\n | Repair assignment | CCA | MergeGate | After failed CCA/CI/review | repair packet, issue/PR comment | repair id and attempt count |\n | Merge readiness | MergeGate | GitHub branch protection / Guardian if needed | After CCA complete | required checks, review rules, rulesets | mergegate check output |\ndiff --git a/docs/agents/guardian-policy.md b/docs/agents/guardian-policy.md\nindex 4147c7adf05a47adb4fd24ada1a1c2e1f2ba6d2d..a20d9cc6c00a569bb057b9ca8d6b51c7fc4a9847\n--- a/docs/agents/guardian-policy.md\n+++ b/docs/agents/guardian-policy.md\n@@ -98,8 +98,8 @@\n `origin/<default_branch>` and reuses each job's existing base fetch; a shallow\n (`--depth`) or second base fetch would make the proof unresolvable and silently\n disable the carry, so neither is permitted. CCA reads `guardian_comment_carried` as\n-a recorded authority in the deterministic signal and does not re-derive it. See ADR\n-0018 for the full decision, the rejected diff/patch-equality and path-disjointness\n+a recorded authority in the deterministic signal and does not re-derive it. See\n+SADR-0018 for the full decision, the rejected diff/patch-equality and path-disjointness\n alternatives, and the accepted residual.\n \n ## External AI Guardian Review (SADR-0010)\ndiff --git a/docs/agents/implementation-policy.md b/docs/agents/implementation-policy.md\nindex c754144a9fb5bf4a38aa82a60e1670c59e626b44..85c37281bcad196c6b7b02343faf00b03cd9c57a\n--- a/docs/agents/implementation-policy.md\n+++ b/docs/agents/implementation-policy.md\n@@ -31,10 +31,13 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n ## Non-Negotiables\n \n 1. **Planning precedes implementation.** Non-trivial Goals start with `grill-with-docs` and Context & Impact.\n-2. **Domain language is controlled.** Terms must be checked against `CONTEXT.md`; hard-to-reverse tradeoffs must be checked against ADRs.\n+2. **Domain language is controlled.** Terms must be checked against `CONTEXT.md`; hard-to-reverse tradeoffs must be checked against Shiki SADRs and target ADRs.\n 3. **PRD precedes issue decomposition.** `to-prd` is used once the context is settled enough to become durable intent.\n 4. **Issues are vertical slices.** `to-issues` must produce independently grabbable tracer-bullet tasks, not horizontal layer tickets.\n 5. **Triage controls dispatch.** Only `ready-for-agent` / AFK tasks may be assigned to Codex without human input.\n@@ -95,19 +98,19 @@\n Process:\n \n - Challenge terms against `CONTEXT.md`.\n-- Check ADRs before reopening decisions.\n+- Check SADRs and target ADRs before reopening decisions.\n - Inspect code when code can answer a question.\n - Ask one question at a time when the operator is available.\n - Provide a recommended answer with each question.\n - Record resolved terms in `CONTEXT.md`.\n-- Create ADRs only for hard-to-reverse, surprising, tradeoff-based decisions.\n+- Create SADRs or target ADRs only for hard-to-reverse, surprising, tradeoff-based decisions.\n - Convert unresolved load-bearing questions into HITL blockers.\n \n Outputs:\n \n - Resolved design-tree decisions.\n - Updated glossary terms when needed.\n-- ADRs when justified.\n+- SADRs or target ADRs when justified.\n - HITL blockers when decisions remain unresolved.\n \n ## Phase 3 \u2014 Context & Impact\n@@ -117,7 +120,7 @@\n \n Outputs must include:\n \n-- Relevant documents and ADRs.\n+- Relevant documents, SADRs, and target ADRs.\n - Relevant modules, interfaces, seams, callers, and tests.\n - Candidate locks.\n - Dependencies.\n@@ -140,7 +143,7 @@\n - Testing decisions.\n - Out of scope.\n - Further notes.\n-- Links to relevant Goal, domain terms, and ADRs.\n+- Links to relevant Goal, domain terms, SADRs, and target ADRs.\n \n Phase 4 ends with Spec Freeze: the operator explicitly approves the PRD, the\n plan records a `spec_freeze` block (status, approved_by, source), and the SF\n@@ -240,7 +243,7 @@\n - Dependencies and locks.\n - Risk and Guardian requirements.\n - Ledger completeness.\n-- Documentation and ADR updates.\n+- Documentation and SADR/target-ADR updates.\n - Scope drift.\n \n CCA emits one of:\n@@ -301,7 +304,7 @@\n | Evidence failure | Behavior might be done, but proof is missing. | Add checks, PR evidence, ledger entry, or artifacts. |\n | Scope drift | PR changes unrelated behavior. | Revert unrelated changes or split into new Goal. |\n | Dependency block | Upstream task or lock not complete. | Block until dependency resolves. |\n-| Architecture block | Decision needs ADR or deep-module discussion. | `grill-with-docs` or `improve-codebase-architecture`. |\n+| Architecture block | Decision needs a SADR, target ADR, or deep-module discussion. | `grill-with-docs` or `improve-codebase-architecture`. |\n | Risk block | Security/production/policy change needs human approval. | Guardian decision. |\n \n ## Phase 5.5 \u2014 Execution Decision Control\ndiff --git a/docs/agents/runtime-auth-model.md b/docs/agents/runtime-auth-model.md\nindex 6a2e1a47e81ea89b5c68a210ec07772a9374babc..3c54748d24233dbd0ba8473950282547ef4ce935\n--- a/docs/agents/runtime-auth-model.md\n+++ b/docs/agents/runtime-auth-model.md\n@@ -148,7 +148,7 @@\n \n API-key based runners may be added by a target repository as an explicit extension. They are not the default Shiki template path.\n \n-If a repo opts into API-key mode, record the exception in an ADR and update the repo's `AGENTS.md`, `.shiki/config.yaml`, and workflow permissions.\n+If a repo opts into API-key mode, record the target-specific exception in a target ADR and update the repo's `AGENTS.md`, `.shiki/config.yaml`, and workflow permissions.\n \n ## GitHub Actions Runtime Compatibility\n \ndiff --git a/docs/agents/skill-gate.md b/docs/agents/skill-gate.md\nindex c50398b0d7c60a8598f2a5c88a10015730dced13..3acffe6444617bb1d54b77d4b4708158ec735bc6\n--- a/docs/agents/skill-gate.md\n+++ b/docs/agents/skill-gate.md\n@@ -9,7 +9,7 @@\n | Skill | Trigger | Expected output |\n | --- | --- | --- |\n | `setup-matt-pocock-skills` | First configuring a repo for these skills, or when issue tracker, triage labels, or domain docs are missing. | `docs/agents/` configuration and an `Agent skills` block in `AGENTS.md` or `CLAUDE.md`. |\n-| `grill-with-docs` | Ambiguous plan, unclear terminology, boundary decision, tradeoff, or ADR-worthy design choice. | Clarified decision tree, recommended answers, updates to `CONTEXT.md` or ADRs when decisions crystallize. |\n+| `grill-with-docs` | Ambiguous plan, unclear terminology, boundary decision, tradeoff, or decision-record-worthy design choice. | Clarified decision tree, recommended answers, updates to `CONTEXT.md`, SADRs, or target ADRs when decisions crystallize. |\n | `zoom-out` | Agent lacks a higher-level architectural map. | Map of relevant modules, callers, seams, and domain concepts. |\n | `to-prd` | Settled context should become a PRD. | PRD published to the issue tracker with problem, solution, user stories, decisions, testing, and out-of-scope notes. |\n | `to-issues` | Goal, PRD, or plan must become implementation tickets. | Vertical-slice issues with acceptance criteria, dependencies, and AFK/HITL classification. |\n@@ -17,7 +17,7 @@\n | `tdd` | Feature work or bug fix has observable behavior that can be specified with tests. | Red-green-refactor loop through public interfaces, one vertical slice at a time. |\n | `code-review` | The TDD loop is green and the implementer is about to create or update a PR. Default for every implementation task. | Pre-PR self-review of the task diff with fixes applied on the task branch; findings and resolutions recorded as a ledger entry naming the skill and a `## Pre-PR code review` PR body section. |\n | `diagnose` | Hard bug, failing check, regression, flaky behavior, or performance problem. | Reproduction loop, ranked hypotheses, instrumentation, fix, regression test, post-mortem. |\n-| `improve-codebase-architecture` | Structural friction, testability, AI-navigability, deep-module opportunity, or refactor request. | Architecture candidates and deepening recommendations informed by `CONTEXT.md` and ADRs. |\n+| `improve-codebase-architecture` | Structural friction, testability, AI-navigability, deep-module opportunity, or refactor request. | Architecture candidates and deepening recommendations informed by `CONTEXT.md`, SADRs, and target ADRs. |\n | `prototype` | A throwaway logic or UI prototype is needed to answer a design question. | Clearly marked disposable prototype and captured decision. |\n \n ## MergeGate Interaction\ndiff --git a/docs/operations.md b/docs/operations.md\nindex cf1f452d4c589e38ccd58553bda0ad8684b51eb7..ace48a90ffbec9e5c0a7113bef16a4858bc44b3f\n--- a/docs/operations.md\n+++ b/docs/operations.md\n@@ -89,6 +89,16 @@\n      target history, brought forward by `shiki migrate`, not a template.\n    - **Everything else** (CLI scripts, workflows, schemas, docs) is overwritten.\n \n+   A forced upgrade also migrates the former numeric Shiki decision paths to\n+   `SADR-NNNN-*`. Before the first write, the installer inventories the exact\n+   legacy Shiki paths and authorizes each deletion only when the existing\n+   `.shiki/install-stamp.json` names that path and its SHA-256 digest still\n+   matches the file. An absent, unreadable, malformed, incomplete, or\n+   mismatching stamp reports every blocked path and stops with no filesystem\n+   change. Target-owned `NNNN-*` ADRs are not cleanup candidates. A non-force\n+   install deletes nothing and retains any still-matching legacy stamp entries\n+   so a later forced upgrade can prove ownership.\n+\n    If the target has pending migrations, `--force` refuses and writes nothing \u2014\n    apply migrations first (step 3), then re-run. The run ends with a single\n    summary of every `.new` written, so a half-upgrade is visible.\ndiff --git a/docs/releasing.md b/docs/releasing.md\nindex 10d73828e7dbd64e9d9f6b83bc84d010537ce62a..aafeee8c4d441e1e427efee2ce4f81a647f020e1\n--- a/docs/releasing.md\n+++ b/docs/releasing.md\n@@ -67,7 +67,7 @@\n CLI in `scripts/`. There are no third-party Python runtime dependencies. The CLI\n is intentionally **not** exposed as a `[project.scripts]` console entry point\n because `scripts/shiki.py` is a dependency-free shim and `scripts/` is not\n-packaged as an importable distribution (see the ADR for the rationale).\n+packaged as an importable distribution (see SADR-0007 for the rationale).\n \n Supported install paths:\n \n@@ -155,6 +155,15 @@\n    [docs/operations.md](operations.md#the-new-convention). If the target has\n    pending migrations, `--force` refuses and writes nothing; apply migrations\n    (step 2) first, then re-run.\n+\n+   The SADR namespace upgrade has an additional pre-write gate: each former\n+   numeric Shiki decision path is removed only when the existing install stamp\n+   contains that exact path and a digest matching its current bytes. The\n+   installer aggregates absent/unreadable/malformed-stamp, missing-digest, and\n+   digest-mismatch blockers before raising, so refusal leaves the target tree\n+   byte-identical. Numeric target ADRs are preserved. A successful forced\n+   upgrade installs and stamps `SADR-*` paths and drops the removed legacy paths\n+   from the refreshed stamp.\n 2. Apply repository-local `.shiki/` state migrations with the migrate command:\n \n    ```bash\ndiff --git a/docs/usage.md b/docs/usage.md\nindex b83407ee3970dd395dd893010bc5dbe8bad26051..a130ef6e40dfd8169a5bc701339351097df1a09f\n--- a/docs/usage.md\n+++ b/docs/usage.md\n@@ -67,8 +67,10 @@\n shiki install-target /path/to/target-repo --local-only\n ```\n \n-Use `--force` only when you intentionally want to overwrite existing target\n-files.\n+Use `--force` only when you intentionally want to refresh the target template.\n+For the SADR namespace migration, `--force` deletes only exact legacy Shiki\n+decision paths whose existing install-stamp digest still matches; any ownership\n+blocker stops before the first write. Numeric target ADRs are never candidates.\n \n ## `shiki bootstrap-platform` \u2014 publish the Shiki platform repo\n \ndiff --git a/scripts/guardian_approval_signal.py b/scripts/guardian_approval_signal.py\nindex d059f92b2c82aa2b5d6ca421365537a87886c1cb..16924892714f4f5b9303c0ef0e325a7e70075630\n--- a/scripts/guardian_approval_signal.py\n+++ b/scripts/guardian_approval_signal.py\n@@ -199,8 +199,8 @@\n ) -> Any:\n     \"\"\"Evaluate SADR-0015 Contract Approval for the CCA Guardian signal.\n \n-    The SAME OR that MergeGate applies must reach this deterministic signal: ADR\n-    0015 is explicit that without it, a contract-approved PR still returns\n+    The SAME OR that MergeGate applies must reach this deterministic signal:\n+    SADR-0015 is explicit that without it, a contract-approved PR still returns\n     ``needs_guardian``, the CCA job fails, and the MergeGate policy check never\n     runs \u2014 the gate this change moves would never be reached. This resolves the\n     task the SAME way MergeGate does (the first Shiki task id in the PR body),\ndiff --git a/scripts/shiki_contract_approval.py b/scripts/shiki_contract_approval.py\nindex 941bcb96fdd8b9a85bc0c4f0f04d5dad9d04839c..4a4b418f6527413161db82670bec857a043360e2\n--- a/scripts/shiki_contract_approval.py\n+++ b/scripts/shiki_contract_approval.py\n@@ -142,7 +142,7 @@\n     changed_files_status: Any,\n     registration: Any,\n ) -> ContractApprovalResult:\n-    \"\"\"Whether this PR carries an SADR-0015 Contract Approval.\n+    \"\"\"Whether this PR carries a SADR-0015 Contract Approval.\n \n     The four conditions are evaluated IN ORDER, each failure recorded as a\n     distinct reason string:\ndiff --git a/scripts/shiki_guardian.py b/scripts/shiki_guardian.py\nindex a4ecb79bbda55fd5c8b06adfae2eb99760a03c45..4dd99bbe714e26f421fa19abac7a34ae1b195ecc\n--- a/scripts/shiki_guardian.py\n+++ b/scripts/shiki_guardian.py\n@@ -12,7 +12,7 @@\n # The carry re-derives its proof from git in-process on every judging invocation;\n # it is never read from an artifact, so there is nothing to go stale or be forged.\n # shiki_sync_proof is the ONLY guardian-family module that runs git, and every one\n-# of its invocations is read-only and local (ADR: prove-from-git base sync).\n+# of its invocations is read-only and local (SADR: prove-from-git base sync).\n from shiki_sync_proof import verify_pure_base_sync\n \n GUARDIAN_POLICY_PATH = \".shiki/guardian-policy.json\"\ndiff --git a/scripts/shiki_guardian_review.py b/scripts/shiki_guardian_review.py\nindex dd0f104d457899e0ba5ff1e162011c1e90b6f92b..e10ef58c046324e7ea3a3392feb3048ca77414ce\n--- a/scripts/shiki_guardian_review.py\n+++ b/scripts/shiki_guardian_review.py\n@@ -159,7 +159,7 @@\n     docs_only = _is_docs_only(changed_files)\n     if docs_only and matched == 0:\n         focus.append(\n-            \"Docs/ADR-only change: confirm the change is genuinely documentation/decision-only, introduces no behavior or contract drift, and stays consistent with the constitution and existing ADRs.\"\n+            \"Docs/decision-record-only change: confirm the change is genuinely documentation/decision-only, introduces no behavior or contract drift, and stays consistent with the constitution and existing SADRs and target ADRs.\"\n         )\n \n     # Conservative augmentation: unknown (no category and not docs-only), mixed\ndiff --git a/scripts/shiki_installer.py b/scripts/shiki_installer.py\nindex be064ab7a29ff6836f7f7a1d70500a43e0d11d7b..9fd8eefd137ce7003dad5c0dcb2e48e2cd47ab32\n--- a/scripts/shiki_installer.py\n+++ b/scripts/shiki_installer.py\n@@ -118,6 +118,7 @@\n # what lets a target carry it without a governance-schema change.\n INSTALL_STAMP_PATH = \".shiki/install-stamp.json\"\n INSTALL_STAMP_VERSION = 1\n+LEGACY_SHIKI_SADR_MAX = 18\n \n # Under --force the shipped surface splits three ways, so nothing is lost and\n # nothing is silently stale.\n@@ -237,9 +238,98 @@\n         )\n \n \n+def legacy_shiki_adr_paths() -> tuple[str, ...]:\n+    \"\"\"Former numeric paths for Shiki SADR-0001 through SADR-0018.\n+\n+    The authoritative slugs come from the current SADR filenames. SADR-0019 is\n+    the namespace decision itself and never shipped under a numeric legacy path.\n+    \"\"\"\n+    paths: list[str] = []\n+    for sadr in sorted((ROOT / \"docs\" / \"adr\").glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\")):\n+        number = sadr.name.split(\"-\", 2)[1]\n+        if int(number) <= LEGACY_SHIKI_SADR_MAX:\n+            paths.append(f\"docs/adr/{sadr.name.removeprefix('SADR-')}\")\n+    return tuple(paths)\n+\n+\n+def preflight_legacy_sadr_cleanup(target: Path, *, force: bool) -> tuple[str, ...]:\n+    \"\"\"Authorize exact legacy Shiki ADR removals before the first write.\n+\n+    A non-force install never removes existing files. A forced install needs no\n+    stamp when no legacy Shiki path is present. When legacy paths do exist, each\n+    must be named in a readable install stamp and its current bytes must match\n+    the recorded digest.\n+    \"\"\"\n+    if not force:\n+        return ()\n+    present = tuple(\n+        relative\n+        for relative in legacy_shiki_adr_paths()\n+        if (target / relative).exists() or (target / relative).is_symlink()\n+    )\n+    if not present:\n+        return ()\n+\n+    stamp_path = target / INSTALL_STAMP_PATH\n+    try:\n+        raw = stamp_path.read_text(encoding=\"utf-8\")\n+        stamp = json.loads(raw)\n+    except FileNotFoundError:\n+        detail = \"install stamp is absent\"\n+        blockers = [f\"{relative}: {detail}\" for relative in present]\n+    except (OSError, UnicodeDecodeError) as error:\n+        detail = f\"install stamp is unreadable ({error})\"\n+        blockers = [f\"{relative}: {detail}\" for relative in present]\n+    except json.JSONDecodeError as error:\n+        detail = f\"install stamp is malformed ({error})\"\n+        blockers = [f\"{relative}: {detail}\" for relative in present]\n+    else:\n+        if not isinstance(stamp, dict):\n+            blockers = [\n+                f\"{relative}: install stamp is malformed (expected an object)\"\n+                for relative in present\n+            ]\n+        else:\n+            digests = stamp.get(\"digests\")\n+            blockers = []\n+            if not isinstance(digests, dict):\n+                blockers.extend(\n+                    f\"{relative}: install stamp has no digest for this path\"\n+                    for relative in present\n+                )\n+            else:\n+                for relative in present:\n+                    expected = digests.get(relative)\n+                    if not isinstance(expected, str) or not expected:\n+                        blockers.append(\n+                            f\"{relative}: install stamp has no digest for this path\"\n+                        )\n+                        continue\n+                    try:\n+                        actual = _sha256_file(target / relative)\n+                    except OSError as error:\n+                        blockers.append(\n+                            f\"{relative}: legacy path is unreadable ({error})\"\n+                        )\n+                        continue\n+                    if actual != expected:\n+                        blockers.append(\n+                            f\"{relative}: digest mismatch (stamp {expected}, current {actual})\"\n+                        )\n+\n+    if blockers:\n+        joined = \"\\n\".join(f\"- {blocker}\" for blocker in blockers)\n+        raise ShikiError(\n+            \"legacy Shiki ADR cleanup is not authorized:\\n\"\n+            f\"{joined}\\nNo files were written.\"\n+        )\n+    return present\n+\n+\n def install_template(target: Path, *, force: bool, validate: bool) -> list[NewFileNote]:\n     # Preconditions first: never rewrite the tree of a target that cannot be\n     # upgraded cleanly (see refuse_pending_migrations).\n+    legacy_cleanup = preflight_legacy_sadr_cleanup(target, force=force)\n     refuse_pending_migrations(target)\n \n     # Resolve the target's required CODEOWNERS owner once from its\n@@ -250,6 +340,10 @@\n     # verbatim. See ``codeowners_required_owner``.\n     codeowners_owner = codeowners_required_owner(target)\n \n+    for relative in legacy_cleanup:\n+        (target / relative).unlink()\n+        info(f\"removed stamped legacy Shiki ADR: {target / relative}\")\n+\n     notes: list[NewFileNote] = []\n     for relative in TEMPLATE_PATHS:\n         source = ROOT / relative\n@@ -647,6 +741,22 @@\n     digests = target_stamp_digests(target)\n     existing = read_install_stamp(target)\n     if existing is not None:\n+        # A non-force install deliberately leaves legacy Shiki ADRs in place.\n+        # Preserve only still-matching ownership proof so a later forced\n+        # upgrade can authorize cleanup; never carry a missing or stale digest.\n+        existing_digests = existing.get(\"digests\")\n+        if isinstance(existing_digests, dict):\n+            for relative in legacy_shiki_adr_paths():\n+                expected = existing_digests.get(relative)\n+                path = target / relative\n+                if not isinstance(expected, str) or not path.is_file():\n+                    continue\n+                try:\n+                    actual = _sha256_file(path)\n+                except OSError:\n+                    continue\n+                if actual == expected:\n+                    digests[relative] = actual\n         if _incoming_is_older(commit, existing.get(\"platform_commit\")):\n             warn(\n                 \"kept existing install stamp: incoming install \"\ndiff --git a/scripts/shiki_loop.py b/scripts/shiki_loop.py\nindex 3611641da8aeb5038b1da90e3114b09a473ec6d8..84caa491b9cad0edc4176f209c13e9221e975681\n--- a/scripts/shiki_loop.py\n+++ b/scripts/shiki_loop.py\n@@ -424,7 +424,7 @@\n         # unresolvable CCA verdict has already produced its terminal stop and is never\n         # laundered into a head-moving sync (SADR-0010). It also never fires while the\n         # policy (Guardian) gate is red \u2014 for ANY reason, not only when it is the sole\n-        # red check \u2014 so a head-bound Guardian approval, or an SADR-0015\n+        # red check \u2014 so a head-bound Guardian approval, or a SADR-0015\n         # contract-approval carry that base movement defeated (the policy gate compares\n         # base and head task governance directly), is never silently restored by\n         # adopting base wholesale. It is bounded by a durable per-task counter so the\ndiff --git a/scripts/shiki_tasks.py b/scripts/shiki_tasks.py\nindex aec3809f24b4a0c8c96fc50971cff9ae9ad30381..d531052e9d9717058d4631483cefab44c26a65c1\n--- a/scripts/shiki_tasks.py\n+++ b/scripts/shiki_tasks.py\n@@ -821,7 +821,7 @@\n         \"prompt\": prompt,\n         \"entry_skill\": \"grill-with-docs\",\n         \"required_next_steps\": [\n-            \"Run grill-with-docs until terminology, boundaries, risks, and ADR-worthy decisions are settled.\",\n+            \"Run grill-with-docs until terminology, boundaries, risks, and decision-record-worthy choices are settled.\",\n             \"Obtain the operator's explicit approval of the PRD/requirements (Spec Freeze).\",\n             \"Write a machine-readable plan JSON with grill_with_docs.status=complete and spec_freeze.status=frozen.\",\n             \"Run shiki plan ingest --plan-file PLAN.json.\",\ndiff --git a/scripts/test_shiki_goal_loop.sh b/scripts/test_shiki_goal_loop.sh\nindex 753c7b516e10f0be3bc92a4bd0eb1fa699776ae6..8f3820a0a079b3044424cfe1a6c6b7fe68896e67\n--- a/scripts/test_shiki_goal_loop.sh\n+++ b/scripts/test_shiki_goal_loop.sh\n@@ -231,8 +231,8 @@\n \n # High/critical risk merges autonomously when all required checks are green:\n # the \"MergeGate policy check\" required check IS the Guardian gate (it enforces\n-# guardian-policy.json \u2014 human approval OR external AI guardian review, ADR\n-# 0010), so green checks mean a recorded authority approved. The fake gh returns\n+# guardian-policy.json \u2014 human approval OR external AI guardian review,\n+# SADR-0010), so green checks mean a recorded authority approved. The fake gh returns\n # green checks, simulating that the Guardian gate passed.\n python3 \"$ROOT/scripts/shiki.py\" goal create --target \"$TARGET\" --title \"Guardian gate\" --outcome \"High risk merges via policy gate\" >/tmp/shiki-goal-loop-guardian-goal.json\n GGOAL=\"$(json_get /tmp/shiki-goal-loop-guardian-goal.json goal_id)\"\ndiff --git a/tests/test_adr_references.py b/tests/test_adr_references.py\nindex 51706329c797d5dd9b47e082a333fa83b261d482..d005da4f771f5e8085ec22d4b787980e68f5ae8c\n--- a/tests/test_adr_references.py\n+++ b/tests/test_adr_references.py\n@@ -1,8 +1,8 @@\n-\"\"\"Every ``ADR ####`` citation must resolve to exactly one decision record.\n+\"\"\"Every target ADR and Shiki SADR citation resolves in its own namespace.\n \n Background. ``scripts/mergegate_check.py`` justifies ``is_bookkeeping_closeout``\n-with \"ADR 0017\" nine times and ``scripts/guardian_approval_signal.py`` cites\n-0016/0017 seven times, yet for a stretch the numbering in ``docs/adr/`` stopped\n+with \"SADR-0017\" nine times and ``scripts/guardian_approval_signal.py`` cites\n+SADR-0016/SADR-0017 seven times, yet for a stretch the numbering in ``docs/adr/`` stopped\n at 0015: the authoring commits lived on a coordinator branch and no task ever\n declared ``docs/adr/...`` in its locks, so nothing carried the records to the\n default branch. The stated basis of an exemption that merged seven PRs with zero\n@@ -11,11 +11,12 @@\n and the directory drifted with no signal.\n \n This is the same unbound-pair shape behind other silent failures: a reference\n-and its target with nothing asserting they agree. This test binds them. It scans\n-every ``scripts/*.py`` (plus ``docs/**/*.md`` and the root ``*.md`` governance\n-files, which are cheap) for the ``ADR ####`` pattern and asserts each cited\n-number resolves to exactly one ``docs/adr/####-*.md``. The converse is\n-deliberately NOT required: an ADR may exist without being cited.\n+and its target with nothing asserting they agree. This test binds them. Target\n+``ADR NNNN`` citations resolve only to ``docs/adr/NNNN-*.md``; Shiki\n+``SADR-NNNN`` citations resolve only to ``docs/adr/SADR-NNNN-*.md``. The scan\n+covers current governance docs, scripts, workflows, prompts, packaging metadata,\n+and repo-local Codex guidance while deliberately excluding historical\n+``.shiki`` evidence. The converse is not required: a record may be uncited.\n \n The suite is dependency free: standard-library ``unittest`` only.\n \"\"\"\n@@ -31,17 +32,18 @@\n \n REPO_ROOT = Path(__file__).resolve().parents[1]\n \n-# \"ADR 0016\" -- a four-digit citation. The trailing \\b keeps a five-digit token\n-# from being read as a truncated four-digit number.\n-_CITATION = re.compile(r\"\\bADR (\\d{4})\\b\")\n-# \"0016-locks-bound-product-paths.md\" -- the leading four digits name the record.\n-_ADR_FILENAME = re.compile(r\"^(\\d{4})-.*\\.md$\")\n+# ``ADR 0016`` / ``SADR-0016`` -- a namespace plus four-digit citation. The\n+# trailing ``\\b`` keeps a five-digit token from being read as a truncated one.\n+_CITATION = re.compile(r\"\\b(S?ADR)[ -](\\d{4})\\b\")\n+# Target ADRs retain the numeric filename; Shiki platform decisions use SADR.\n+_DECISION_FILENAME = re.compile(r\"^(?:(SADR)-)?(\\d{4})-.*\\.md$\")\n \n \n class Violation(NamedTuple):\n     \"\"\"A citation that does not resolve to exactly one decision record.\"\"\"\n \n     citing_file: str  # repo-relative path of the file that cites the ADR\n+    namespace: str  # \"ADR\" for target decisions; \"SADR\" for Shiki decisions\n     number: str  # the cited four-digit number, e.g. \"0016\"\n     line: int  # 1-based line of the citation\n     reason: str  # \"missing\" (zero matches) or \"ambiguous\" (two or more)\n@@ -49,44 +51,59 @@\n \n     @property\n     def expected_path(self) -> str:\n-        return f\"docs/adr/{self.number}-*.md\"\n+        prefix = \"SADR-\" if self.namespace == \"SADR\" else \"\"\n+        return f\"docs/adr/{prefix}{self.number}-*.md\"\n \n     def describe(self) -> str:\n         \"\"\"Name the citing file, the cited number, and the missing path.\"\"\"\n         if self.reason == \"missing\":\n             return (\n-                f\"{self.citing_file}:{self.line} cites ADR {self.number} \"\n+                f\"{self.citing_file}:{self.line} cites {self.namespace} {self.number} \"\n                 f\"but no document matches {self.expected_path}\"\n             )\n         joined = \", \".join(self.matches)\n         return (\n-            f\"{self.citing_file}:{self.line} cites ADR {self.number} which \"\n+            f\"{self.citing_file}:{self.line} cites {self.namespace} {self.number} which \"\n             f\"resolves to {len(self.matches)} documents (expected exactly one \"\n             f\"at {self.expected_path}): {joined}\"\n         )\n \n \n-def _adr_index(root: Path) -> dict[str, list[Path]]:\n-    \"\"\"Map every four-digit ADR number to the record files that claim it.\"\"\"\n-    index: dict[str, list[Path]] = {}\n+def _adr_index(root: Path) -> dict[tuple[str, str], list[Path]]:\n+    \"\"\"Map each decision namespace and number to the files that claim it.\"\"\"\n+    index: dict[tuple[str, str], list[Path]] = {}\n     adr_dir = root / \"docs\" / \"adr\"\n     if adr_dir.is_dir():\n         for path in sorted(adr_dir.glob(\"*.md\")):\n-            match = _ADR_FILENAME.match(path.name)\n+            match = _DECISION_FILENAME.match(path.name)\n             if match:\n-                index.setdefault(match.group(1), []).append(path)\n+                namespace = \"SADR\" if match.group(1) else \"ADR\"\n+                index.setdefault((namespace, match.group(2)), []).append(path)\n     return index\n \n \n def _citing_files(root: Path, include_docs: bool = True) -> list[Path]:\n-    \"\"\"Files scanned for citations: scripts/*.py, and cheaply the markdown.\"\"\"\n+    \"\"\"Current, shipped text surfaces scanned for decision citations.\"\"\"\n     files = sorted((root / \"scripts\").glob(\"*.py\"))\n+    files += sorted((root / \"scripts\").glob(\"*.sh\"))\n     if include_docs:\n         docs = root / \"docs\"\n         if docs.is_dir():\n             files += sorted(docs.rglob(\"*.md\"))\n         files += sorted(root.glob(\"*.md\"))\n-    return files\n+        pyproject = root / \"pyproject.toml\"\n+        if pyproject.is_file():\n+            files.append(pyproject)\n+        for relative in (\".github\", \".codex\"):\n+            surface = root / relative\n+            if surface.is_dir():\n+                files += sorted(\n+                    path\n+                    for path in surface.rglob(\"*\")\n+                    if path.is_file()\n+                    and path.suffix in {\".md\", \".py\", \".sh\", \".toml\", \".yaml\", \".yml\"}\n+                )\n+    return sorted(set(files))\n \n \n def find_adr_reference_violations(\n@@ -94,10 +111,9 @@\n ) -> list[Violation]:\n     \"\"\"Return every ``ADR ####`` citation under ``root`` that fails to resolve.\n \n-    A citation resolves when exactly one ``docs/adr/####-*.md`` matches its\n-    number. Zero matches is a \"missing\" violation; two or more is \"ambiguous\".\n-    Records that are never cited produce nothing -- the converse is not\n-    required.\n+    A citation resolves when exactly one record in the same namespace matches\n+    its number. Zero matches is a \"missing\" violation; two or more is\n+    \"ambiguous\". Records that are never cited produce nothing.\n     \"\"\"\n     root = Path(root)\n     index = _adr_index(root)\n@@ -110,13 +126,15 @@\n         rel = path.relative_to(root).as_posix()\n         for lineno, line in enumerate(text.splitlines(), start=1):\n             for match in _CITATION.finditer(line):\n-                number = match.group(1)\n-                found = index.get(number, [])\n+                namespace, number = match.groups()\n+                found = index.get((namespace, number), [])\n                 if len(found) == 1:\n                     continue\n                 reason = \"missing\" if not found else \"ambiguous\"\n                 rels = [p.relative_to(root).as_posix() for p in found]\n-                violations.append(Violation(rel, number, lineno, reason, rels))\n+                violations.append(\n+                    Violation(rel, namespace, number, lineno, reason, rels)\n+                )\n     return violations\n \n \n@@ -129,8 +147,8 @@\n         self.assertEqual(\n             violations,\n             [],\n-            \"Every 'ADR ####' citation must resolve to exactly one \"\n-            f\"docs/adr/####-*.md:\\n{detail}\",\n+            \"Every ADR/SADR citation must resolve exactly once in its own \"\n+            f\"docs/adr namespace:\\n{detail}\",\n         )\n \n     def test_scan_actually_reads_scripts(self) -> None:\n@@ -142,12 +160,38 @@\n             \"the citation scan must cover scripts/*.py\",\n         )\n \n+    def test_platform_citations_do_not_use_unresolved_numeric_shorthand(self) -> None:\n+        shorthand = re.compile(r\"SADR-\\d{4}/\\d{4}\")\n+        offenders = []\n+        for path in _citing_files(REPO_ROOT):\n+            text = path.read_text(encoding=\"utf-8\")\n+            if shorthand.search(text):\n+                offenders.append(path.relative_to(REPO_ROOT).as_posix())\n+        self.assertEqual(\n+            offenders,\n+            [],\n+            \"repeat the SADR prefix so every cited number is independently resolvable\",\n+        )\n+\n     def test_carried_records_are_indexed(self) -> None:\n         # The two records this Goal carried onto the default branch, and which\n         # the scripts cite most, must be present.\n         index = _adr_index(REPO_ROOT)\n-        self.assertIn(\"0016\", index)\n-        self.assertIn(\"0017\", index)\n+        self.assertIn((\"SADR\", \"0016\"), index)\n+        self.assertIn((\"SADR\", \"0017\"), index)\n+\n+    def test_platform_tree_has_only_sadr_0001_through_0019(self) -> None:\n+        adr_dir = REPO_ROOT / \"docs\" / \"adr\"\n+        sadrs = sorted(adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\"))\n+        self.assertEqual(\n+            {path.name.split(\"-\", 2)[1] for path in sadrs},\n+            {f\"{number:04d}\" for number in range(1, 20)},\n+        )\n+        self.assertEqual(\n+            list(adr_dir.glob(\"[0-9][0-9][0-9][0-9]-*.md\")),\n+            [],\n+            \"the Shiki platform tree must not retain legacy numeric SADR paths\",\n+        )\n \n \n class FixtureAdrReferences(unittest.TestCase):\n@@ -180,6 +224,26 @@\n         self.assertIn(\"9991\", message)  # cited number\n         self.assertIn(\"docs/adr/9991-*.md\", message)  # missing path\n \n+    def test_target_adr_does_not_satisfy_missing_platform_sadr(self) -> None:\n+        root = self._make_repo(\n+            adr_names=[\"0042-target-decision.md\"],\n+            script_body='\"\"\"Target ADR 0042 exists, but platform SADR-0042 does not.\"\"\"\\n',\n+        )\n+        violations = find_adr_reference_violations(root, include_docs=False)\n+        self.assertEqual(len(violations), 1)\n+        self.assertEqual(violations[0].namespace, \"SADR\")\n+        self.assertEqual(violations[0].number, \"0042\")\n+        self.assertEqual(violations[0].expected_path, \"docs/adr/SADR-0042-*.md\")\n+\n+    def test_target_adr_and_platform_sadr_may_reuse_a_number(self) -> None:\n+        root = self._make_repo(\n+            adr_names=[\"0042-target-decision.md\", \"SADR-0042-platform-decision.md\"],\n+            script_body='\"\"\"Target ADR 0042 and platform SADR-0042 are distinct.\"\"\"\\n',\n+        )\n+        self.assertEqual(\n+            find_adr_reference_violations(root, include_docs=False), []\n+        )\n+\n     def test_uncited_adr_is_allowed(self) -> None:\n         # An ADR exists but nothing cites it -> not a violation. The converse\n         # (every ADR must be cited) is explicitly NOT required.\ndiff --git a/tests/test_guardian_review.py b/tests/test_guardian_review.py\nindex 88829e7810212635e2fc98aa5918d6f26294677d..00a85a237acd5bf2d6cb5b294128198c422402ca\n--- a/tests/test_guardian_review.py\n+++ b/tests/test_guardian_review.py\n@@ -162,7 +162,7 @@\n \n     def test_docs_only_is_light_and_not_conservative(self):\n         focus = gr.classify_review_focus([\"docs/adr/0099-x.md\", \"README.md\"], \"\", \"low\")\n-        self.assertTrue(any(\"Docs/ADR-only\" in f for f in focus))\n+        self.assertTrue(any(\"Docs/decision-record-only\" in f for f in focus))\n         self.assertFalse(any(\"conservative default\" in f.lower() for f in focus))\n \n     def test_unknown_category_is_conservative(self):\ndiff --git a/tests/test_policy_job_merged_prs.py b/tests/test_policy_job_merged_prs.py\nindex d45512e74681e5e649279236c7d4e79d5c3da471..b97989f8bd74d091de1a6b5275a20d8dc68c4512\n--- a/tests/test_policy_job_merged_prs.py\n+++ b/tests/test_policy_job_merged_prs.py\n@@ -3,8 +3,8 @@\n \n ``.github/workflows/shiki-cca-completion.yml`` has two consumers of the merged-PR\n proof: the CCA *signal* (``guardian_approval_signal.py`` in \"Prepare CCA context\")\n-and the authoritative gate (``mergegate_check.py`` in \"Enforce MergeGate\"). ADR\n-0017's bookkeeping-closeout exemption (condition 6) needs the merged implementation\n+and the authoritative gate (``mergegate_check.py`` in \"Enforce MergeGate\").\n+SADR-0017's bookkeeping-closeout exemption (condition 6) needs the merged implementation\n PR proven, and a closeout carries NO ``post_merge_reconcile`` marker. The signal\n already resolves ``MERGED_PRS`` unconditionally, but \"Enforce MergeGate\" used to\n resolve it only inside ``grep -q '<!-- shiki:post_merge_reconcile -->'``. So a\ndiff --git a/tests/test_sadr_install_migration.py b/tests/test_sadr_install_migration.py\nnew file mode 100644\nindex 0000000000000000000000000000000000000000..a6f4c57e4dcaef73ef600b0d4804744138ab9071\n--- /dev/null\n+++ b/tests/test_sadr_install_migration.py\n@@ -0,0 +1,229 @@\n+\"\"\"SADR installation and collision-safe legacy upgrade behavior.\"\"\"\n+\n+from __future__ import annotations\n+\n+import contextlib\n+import hashlib\n+import io\n+import json\n+import shutil\n+import tempfile\n+import unittest\n+from pathlib import Path\n+\n+import shiki_test_support  # noqa: F401  (path bootstrap)\n+\n+import shiki_installer\n+from tests.test_adr_references import find_adr_reference_violations\n+\n+\n+PRODUCT_NUMBERS = tuple(range(13, 19))\n+\n+\n+def _quiet_install(target: Path, *, force: bool, validate: bool) -> None:\n+    with contextlib.redirect_stdout(io.StringIO()):\n+        shiki_installer.install_template(target, force=force, validate=validate)\n+\n+\n+def _sha256(path: Path) -> str:\n+    return hashlib.sha256(path.read_bytes()).hexdigest()\n+\n+\n+def _legacy_sadr_paths() -> tuple[str, ...]:\n+    return tuple(\n+        f\"docs/adr/{path.name.removeprefix('SADR-')}\"\n+        for path in sorted((shiki_installer.ROOT / \"docs\" / \"adr\").glob(\"SADR-*.md\"))\n+        if path.name != \"SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\"\n+    )\n+\n+\n+def _create_legacy_upgrade_fixture(\n+    target: Path,\n+) -> tuple[dict[str, bytes], tuple[str, ...]]:\n+    adr_dir = target / \"docs\" / \"adr\"\n+    adr_dir.mkdir(parents=True)\n+    product_bytes = {\n+        f\"{number:04d}-cubeshare-product-decision.md\": (\n+            f\"# CubeShare ADR {number:04d}\\n\\nDo not rewrite {number}.\\n\"\n+        ).encode()\n+        for number in PRODUCT_NUMBERS\n+    }\n+    for name, content in product_bytes.items():\n+        (adr_dir / name).write_bytes(content)\n+\n+    legacy_paths = _legacy_sadr_paths()\n+    for relative in legacy_paths:\n+        (target / relative).write_bytes(f\"legacy Shiki bytes for {relative}\\n\".encode())\n+\n+    stamp_path = target / shiki_installer.INSTALL_STAMP_PATH\n+    stamp_path.parent.mkdir(parents=True, exist_ok=True)\n+    stamp = {\n+        \"version\": shiki_installer.INSTALL_STAMP_VERSION,\n+        \"platform_commit\": None,\n+        \"installed_at\": \"2000-01-01T00:00:00+00:00\",\n+        \"digests\": {\n+            relative: _sha256(target / relative) for relative in legacy_paths\n+        },\n+    }\n+    stamp_path.write_text(\n+        json.dumps(stamp, indent=2, sort_keys=True) + \"\\n\", encoding=\"utf-8\"\n+    )\n+    return product_bytes, legacy_paths\n+\n+\n+def _tree_snapshot(root: Path) -> dict[str, tuple[str, bytes | str]]:\n+    \"\"\"Recursive byte/shape snapshot used to prove preflight is write-free.\"\"\"\n+    snapshot: dict[str, tuple[str, bytes | str]] = {}\n+    for path in sorted(root.rglob(\"*\")):\n+        relative = path.relative_to(root).as_posix()\n+        if path.is_symlink():\n+            snapshot[relative] = (\"symlink\", path.readlink().as_posix())\n+        elif path.is_dir():\n+            snapshot[relative] = (\"directory\", \"\")\n+        else:\n+            snapshot[relative] = (\"file\", path.read_bytes())\n+    return snapshot\n+\n+\n+class FreshSadrInstallTests(unittest.TestCase):\n+    def setUp(self) -> None:\n+        self.tmp = Path(tempfile.mkdtemp(prefix=\"shiki-sadr-fresh-\"))\n+        self.addCleanup(lambda: shutil.rmtree(self.tmp, ignore_errors=True))\n+        self.target = self.tmp / \"target\"\n+        self.adr_dir = self.target / \"docs\" / \"adr\"\n+        self.adr_dir.mkdir(parents=True)\n+        self.product_bytes = {\n+            f\"{number:04d}-product-decision.md\": (\n+                f\"# Product ADR {number:04d}\\n\\nTarget-owned bytes {number}.\\n\"\n+            ).encode()\n+            for number in PRODUCT_NUMBERS\n+        }\n+        for name, content in self.product_bytes.items():\n+            (self.adr_dir / name).write_bytes(content)\n+\n+    def test_fresh_install_preserves_product_adrs_and_installs_all_sadrs(self) -> None:\n+        _quiet_install(self.target, force=False, validate=True)\n+\n+        for name, expected in self.product_bytes.items():\n+            self.assertEqual((self.adr_dir / name).read_bytes(), expected)\n+\n+        sadr_numbers = {\n+            path.name.split(\"-\", 2)[1]\n+            for path in self.adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\")\n+        }\n+        self.assertEqual(sadr_numbers, {f\"{number:04d}\" for number in range(1, 20)})\n+\n+        numeric_records = {\n+            path.name\n+            for path in self.adr_dir.glob(\"[0-9][0-9][0-9][0-9]-*.md\")\n+        }\n+        self.assertEqual(numeric_records, set(self.product_bytes))\n+        self.assertEqual(find_adr_reference_violations(self.target), [])\n+\n+\n+class LegacySadrUpgradeTests(unittest.TestCase):\n+    def setUp(self) -> None:\n+        self.tmp = Path(tempfile.mkdtemp(prefix=\"shiki-sadr-upgrade-\"))\n+        self.addCleanup(lambda: shutil.rmtree(self.tmp, ignore_errors=True))\n+        self.target = self.tmp / \"target\"\n+        self.adr_dir = self.target / \"docs\" / \"adr\"\n+        self.product_bytes, self.legacy_paths = _create_legacy_upgrade_fixture(\n+            self.target\n+        )\n+\n+    def test_force_upgrade_deletes_only_stamped_legacy_sadrs(self) -> None:\n+        _quiet_install(self.target, force=True, validate=True)\n+\n+        for relative in self.legacy_paths:\n+            self.assertFalse((self.target / relative).exists(), relative)\n+        for name, expected in self.product_bytes.items():\n+            self.assertEqual((self.adr_dir / name).read_bytes(), expected)\n+\n+        sadrs = list(self.adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\"))\n+        self.assertEqual(len(sadrs), 19)\n+        refreshed = json.loads(\n+            (self.target / shiki_installer.INSTALL_STAMP_PATH).read_text(encoding=\"utf-8\")\n+        )\n+        for relative in self.legacy_paths:\n+            self.assertNotIn(relative, refreshed[\"digests\"])\n+        for path in sadrs:\n+            relative = path.relative_to(self.target).as_posix()\n+            self.assertEqual(refreshed[\"digests\"][relative], _sha256(path))\n+\n+    def test_non_force_install_never_deletes_legacy_files(self) -> None:\n+        before = {\n+            relative: (self.target / relative).read_bytes()\n+            for relative in self.legacy_paths\n+        }\n+        _quiet_install(self.target, force=False, validate=False)\n+        for relative, expected in before.items():\n+            self.assertEqual((self.target / relative).read_bytes(), expected)\n+        refreshed = json.loads(\n+            (self.target / shiki_installer.INSTALL_STAMP_PATH).read_text(encoding=\"utf-8\")\n+        )\n+        for relative in self.legacy_paths:\n+            self.assertEqual(refreshed[\"digests\"][relative], _sha256(self.target / relative))\n+\n+\n+class LegacyCleanupPreflightTests(unittest.TestCase):\n+    def setUp(self) -> None:\n+        self.tmp = Path(tempfile.mkdtemp(prefix=\"shiki-sadr-preflight-\"))\n+        self.addCleanup(lambda: shutil.rmtree(self.tmp, ignore_errors=True))\n+\n+    def test_all_ownership_blockers_abort_before_any_write(self) -> None:\n+        cases = (\n+            (\"absent\", \"install stamp is absent\"),\n+            (\"unreadable\", \"install stamp is unreadable\"),\n+            (\"malformed\", \"install stamp is malformed\"),\n+            (\"missing-digest\", \"install stamp has no digest\"),\n+            (\"digest-mismatch\", \"digest mismatch\"),\n+        )\n+        for name, expected_reason in cases:\n+            with self.subTest(name=name):\n+                target = self.tmp / name\n+                _, legacy_paths = _create_legacy_upgrade_fixture(target)\n+                stamp_path = target / shiki_installer.INSTALL_STAMP_PATH\n+\n+                if name == \"absent\":\n+                    stamp_path.unlink()\n+                elif name == \"unreadable\":\n+                    stamp_path.unlink()\n+                    stamp_path.mkdir()\n+                elif name == \"malformed\":\n+                    stamp_path.write_text(\"{not-json\\n\", encoding=\"utf-8\")\n+                else:\n+                    stamp = json.loads(stamp_path.read_text(encoding=\"utf-8\"))\n+                    if name == \"missing-digest\":\n+                        stamp[\"digests\"] = {}\n+                    else:\n+                        stamp[\"digests\"] = {\n+                            relative: \"deadbeef\" for relative in legacy_paths\n+                        }\n+                    stamp_path.write_text(\n+                        json.dumps(stamp, indent=2, sort_keys=True) + \"\\n\",\n+                        encoding=\"utf-8\",\n+                    )\n+\n+                before = _tree_snapshot(target)\n+                with self.assertRaises(shiki_installer.ShikiError) as raised:\n+                    _quiet_install(target, force=True, validate=False)\n+                after = _tree_snapshot(target)\n+\n+                self.assertEqual(before, after, f\"{name} preflight changed the tree\")\n+                message = str(raised.exception)\n+                self.assertIn(expected_reason, message)\n+                self.assertIn(\"No files were written\", message)\n+                for relative in legacy_paths:\n+                    self.assertIn(relative, message)\n+\n+    def test_absent_stamp_is_allowed_when_no_legacy_path_exists(self) -> None:\n+        target = self.tmp / \"no-legacy\"\n+        target.mkdir()\n+        _quiet_install(target, force=True, validate=False)\n+        self.assertTrue(\n+            (target / \"docs\" / \"adr\" / \"SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\").is_file()\n+        )\n+\n+\n+if __name__ == \"__main__\":\n+    unittest.main()\n\nwarning: Skill descriptions were shortened to fit the 2% skills context budget. Codex can still see every skill, but some descriptions are shorter. Disable unused skills or plugins to leave more room for the rest.\ncontext compacted\n2026-08-07T08:22:31.656030Z ERROR codex_models_manager::manager: failed to renew cache TTL: missing field `base_instructions` at line 94 column 5\ncodex\nImplementation is now locally green across the main behavior paths. I\u2019m finishing the required structured code-review gate, checking every changed path against the task locks, then I\u2019ll rerun the full verification matrix and record only non-approval lifecycle evidence.\nSADR namespace and installer migration are implemented with red-green coverage. Final review and exact local verification remain.\n  \u2713 Load required skills, task contract, and run preflight\n  \u2713 Establish failing resolver and installer migration tests\n  \u2713 Rename platform decisions and migrate live citations to SADR\n  \u2713 Implement digest-authorized legacy installer cleanup\n  \u2192 Run structured code review and repair findings\n  \u2022 Rerun full required verification and record lifecycle evidence\ndiff --git a/.codex/skills/shiki/SKILL.md b/.codex/skills/shiki/SKILL.md\nindex c8e2e85d9d75d49cc4f2d6286ee22659c4e53e75..93cded623b2c15d9f65c76491a1c2d813ac9e6c0\n--- a/.codex/skills/shiki/SKILL.md\n+++ b/.codex/skills/shiki/SKILL.md\n@@ -18,6 +18,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n Codex CLI does not register this skill as a custom `/shiki` slash command. When\n the user types `/shiki` in Codex CLI and gets \"Unrecognized command\", explain\n that this is expected: invoke Shiki in Codex with natural language such as\n@@ -69,7 +72,7 @@\n \n ## Responsibilities\n \n-- Claude Code implements and repairs by default (`shiki runner claude`, ADR 0008); Codex implements and repairs tasks explicitly assigned to `codex`.\n+- Claude Code implements and repairs by default (`shiki runner claude`, SADR-0008); Codex implements and repairs tasks explicitly assigned to `codex`.\n - Claude Code Action can act as GitHub-side CCA or reviewer.\n - CCA judges completion.\n - MergeGate authorizes state transitions and merge readiness.\ndiff --git a/.github/prompts/cca-completion-check.md b/.github/prompts/cca-completion-check.md\nindex ed203ad5f302ecc81372d8ab27d2b1232ca97497..fd89950528333ef6242e6d5443904f5392ab7e56\n--- a/.github/prompts/cca-completion-check.md\n+++ b/.github/prompts/cca-completion-check.md\n@@ -19,6 +19,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n ## Required Reading\n \n Read only the files needed to judge the PR contract. Prefer this order:\ndiff --git a/.github/prompts/codex-task-handoff.md b/.github/prompts/codex-task-handoff.md\nindex 695e211d76dea1c60e8d5bb360e78487808286a4..59441d576b01a39569722f226c854e5a740e9eb5\n--- a/.github/prompts/codex-task-handoff.md\n+++ b/.github/prompts/codex-task-handoff.md\n@@ -2,7 +2,7 @@\n \n You are Codex Front acting as the Implementer for a Shiki task.\n \n-Follow `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant ADRs, and `docs/agents/codex-handoff.md`.\n+Follow `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant Shiki SADRs and target ADRs, and `docs/agents/codex-handoff.md`.\n \n Rules:\n \ndiff --git a/AGENTS.md b/AGENTS.md\nindex 7f041100d49c2a7525f51cb0f5d696a4a6d1c78c..a06bfbabc6fa48a007389e25e12d7ed523936bed\n--- a/AGENTS.md\n+++ b/AGENTS.md\n@@ -22,7 +22,7 @@\n - **Codex Front** is an optional implementer through Codex App, Codex CLI, Codex IDE extension, or Codex Web signed in with ChatGPT OAuth/subscription auth, dispatched with `shiki runner codex` when a task is explicitly assigned to `codex`.\n - **GitHub CCA** is implemented by Claude Code Action by default, using `CLAUDE_CODE_OAUTH_TOKEN`.\n \n-Do not assume `openai/codex-action`, `OPENAI_API_KEY`, or API-key based Codex automation in the default Shiki path. API-key runners are explicit target-repo extensions and require their own ADR.\n+Do not assume `openai/codex-action`, `OPENAI_API_KEY`, or API-key based Codex automation in the default Shiki path. API-key runners are explicit target-repo extensions and require their own target ADR.\n \n Shiki is not:\n \n@@ -44,10 +44,14 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use\n+`SADR-NNNN-*.md`; target product decisions retain `NNNN-*.md` and the `ADR`\n+namespace.\n+\n Use the canonical source-of-truth order above. Runtime-specific reading orders\n are operational guidance only and must not override or reorder it.\n \n-Conversation state is not durable truth. If a decision matters, put it in GitHub, `.shiki/`, `CONTEXT.md`, or an ADR.\n+Conversation state is not durable truth. If a decision matters, put it in GitHub, `.shiki/`, `CONTEXT.md`, a Shiki SADR, or a target ADR.\n \n If GitHub and `.shiki/` disagree, surface the conflict and prefer GitHub operational state until the mirror is repaired.\n \n@@ -75,8 +79,8 @@\n Every non-trivial change follows this loop:\n \n 1. **Goal**: clarify outcome, completion conditions, non-goals, risk level, and success signals.\n-2. **grill-with-docs**: challenge the plan against domain docs, ADRs, code reality, terminology, and edge scenarios.\n-3. **Context & Impact**: find relevant docs, ADRs, code, dependencies, owners, locks, and verification surfaces.\n+2. **grill-with-docs**: challenge the plan against domain docs, Shiki SADRs, target ADRs, code reality, terminology, and edge scenarios.\n+3. **Context & Impact**: find relevant docs, SADRs, target ADRs, code, dependencies, owners, locks, and verification surfaces.\n 4. **PRD and Spec Freeze**: use `to-prd` when context is settled enough to become durable product/engineering intent. The operator's explicit approval of the PRD is Spec Freeze: the plan records a `spec_freeze` block, and no plan runs without it. Steps 1-4 form Requirements Definition \u2014 one continuous operator dialogue. After Spec Freeze, scope changes require an operator-approved Spec Amendment; non-scope-moving interpretations go to the Assumption Log.\n 5. **Issues**: use `to-issues` to create independently grabbable vertical-slice issues. Prefer AFK slices over HITL slices where possible.\n 6. **Triage**: label issues for readiness, risk, runtime, skills, and MergeGate state.\n@@ -167,7 +171,7 @@\n - Testing decisions.\n - Out-of-scope items.\n - Further notes.\n-- Links to relevant domain terms and ADRs.\n+- Links to relevant domain terms, SADRs, and target ADRs.\n \n Every executable task must include:\n \n@@ -193,7 +197,7 @@\n \n Context & Impact must identify:\n \n-- Relevant documents, domain terms, ADRs, and past decisions.\n+- Relevant documents, domain terms, SADRs, target ADRs, and past decisions.\n - Relevant modules, interfaces, seams, callers, owners, and tests.\n - Dependency relationships between tasks.\n - Candidate locks and lock conflicts.\n@@ -210,7 +214,7 @@\n | Skill | Required when |\n | --- | --- |\n | `setup-matt-pocock-skills` | First configuring the repo for skills, or when issue tracker, triage labels, or domain docs are missing. |\n-| `grill-with-docs` | Default for non-trivial Goals before PRD/issues; always required when plans, terminology, boundaries, tradeoffs, or ADR-worthy decisions are ambiguous. |\n+| `grill-with-docs` | Default for non-trivial Goals before PRD/issues; always required when plans, terminology, boundaries, tradeoffs, or decision-record-worthy choices are ambiguous. |\n | `zoom-out` | The agent lacks an architectural map of the relevant code area. |\n | `to-prd` | Settled Goal context must become durable product/engineering intent. |\n | `to-issues` | A Goal, PRD, or plan must become independently grabbable vertical-slice issues. |\n@@ -348,7 +352,7 @@\n - Three or more new files.\n - Any high-risk or critical label.\n \n-When the architecture gate triggers, run `grill-with-docs` or `improve-codebase-architecture` as appropriate and record durable decisions in an ADR when needed.\n+When the architecture gate triggers, run `grill-with-docs` or `improve-codebase-architecture` as appropriate and record durable decisions in a Shiki SADR or target ADR as applicable.\n \n ## Engineering Discipline\n \ndiff --git a/CLAUDE.md b/CLAUDE.md\nindex 98a932e7841a632866fcd53b0bad0e3af3711d3d..98a31399404d6fb044b4fd493f4459ac87f439de\n--- a/CLAUDE.md\n+++ b/CLAUDE.md\n@@ -14,6 +14,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n ## Claude Role\n \n Default to planner, default implementer, reviewer, coordinator, documentation editor, CCA-style completion judge, and final-judgment assistant.\n@@ -57,7 +60,7 @@\n At the start of a Shiki session:\n \n 1. Read `AGENTS.md`.\n-2. Read `CONTEXT.md` and relevant ADRs.\n+2. Read `CONTEXT.md` and relevant Shiki SADRs and target ADRs.\n 3. Read active `.shiki/` Goal/task/ledger state when present.\n 4. Read active distilled rules from `.shiki/memories/` (status `distilled`, `active: true`, not revoked or superseded) for applicable guidance; the same rules are injected deterministically into task handoffs as the `## Distilled Rules` section (\u00a73.5).\n 5. Read the GitHub Issue or PR when the task is GitHub-backed.\n@@ -74,7 +77,7 @@\n - Define the Goal, completion conditions, non-goals, and risk level.\n - Treat `grill-with-docs` as the default planning entry point for non-trivial Goals.\n - Challenge domain terms against `CONTEXT.md`.\n-- Challenge hard-to-reverse tradeoffs against ADRs.\n+- Challenge hard-to-reverse tradeoffs against SADRs and target ADRs.\n - Explore code instead of asking when code can answer the question.\n - Run Context & Impact before implementation planning.\n - Produce a Task DAG, not an unordered checklist.\n@@ -94,7 +97,7 @@\n - For each question, provide your recommended answer.\n - Resolve dependency decisions before downstream design questions.\n - Update `CONTEXT.md` inline when terminology crystallizes.\n-- Offer ADRs only when the decision is hard to reverse, surprising without context, and a real tradeoff.\n+- Offer a SADR or target ADR only when the decision is hard to reverse, surprising without context, and a real tradeoff.\n - Convert unresolved questions into HITL blockers rather than allowing Codex to guess.\n \n ## PRD and Issues Standard\n@@ -103,7 +106,7 @@\n \n - Use the project's domain glossary vocabulary.\n - Avoid volatile file paths unless they are essential.\n-- Include implementation decisions, testing decisions, out-of-scope items, and relevant ADR/domain links.\n+- Include implementation decisions, testing decisions, out-of-scope items, and relevant SADR/target-ADR/domain links.\n - Identify deep-module opportunities and testable interfaces.\n \n When creating issues:\n@@ -121,7 +124,7 @@\n - Branch/worktree target.\n - Scope and non-goals.\n - Dependencies and locks.\n-- Relevant docs, ADRs, modules, and tests.\n+- Relevant docs, SADRs, target ADRs, modules, and tests.\n - Required skill invocations.\n - TDD expectations.\n - Pre-PR `code-review` gate expectations.\n@@ -152,7 +155,7 @@\n \n - Treat yourself as a judge, not implementer.\n - Do not write production code.\n-- Read `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant ADRs, PRD, task issue, PR body, diff, checks, reviews, labels, and `.shiki/` state.\n+- Read `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant SADRs and target ADRs, PRD, task issue, PR body, diff, checks, reviews, labels, and `.shiki/` state.\n - Evaluate every applicable checklist in `docs/agents/checklists.md`.\n - Map each acceptance criterion to durable evidence.\n - Distinguish implementation failure from missing evidence.\ndiff --git a/CONTEXT.md b/CONTEXT.md\nindex e61a9be06a0cce0d026484083a657bde2067e234..237ee8aa3ea8fab334ece49768bc5ed3b5af0974\n--- a/CONTEXT.md\n+++ b/CONTEXT.md\n@@ -25,7 +25,7 @@\n _Avoid_: jumping directly into code\n \n **grill-with-docs**:\n-A planning skill that challenges a plan against domain language, ADRs, code reality, and concrete edge scenarios. It resolves design-tree questions before PRD/issues. It runs inside Requirements Definition.\n+A planning skill that challenges a plan against domain language, Shiki SADRs, target ADRs, code reality, and concrete edge scenarios. It resolves design-tree questions before PRD/issues. It runs inside Requirements Definition.\n _Avoid_: generic brainstorming, silent assumptions\n \n **Requirements Definition**:\ndiff --git a/CONTRIBUTING.md b/CONTRIBUTING.md\nindex c6d9b656e73abb8a55e5f660123e03153dcb3e6d..9e37a8fbf668db5b22461aaae5548b8f1354394c\n--- a/CONTRIBUTING.md\n+++ b/CONTRIBUTING.md\n@@ -17,7 +17,7 @@\n 1. **Goal** \u2014 start from a user-approved Goal with an outcome, completion\n    conditions, non-goals, risk level, and success signals.\n 2. **grill-with-docs** \u2014 for any non-trivial change, challenge the plan against\n-   domain language, ADRs, and code reality before writing a PRD.\n+   domain language, Shiki SADRs, target ADRs, and code reality before writing a PRD.\n 3. **PRD** \u2014 capture settled product/engineering intent (`to-prd`).\n 4. **Spec Freeze** \u2014 the operator explicitly approves the PRD/requirements;\n    the plan records a `spec_freeze` block and no plan runs without it\n@@ -94,7 +94,7 @@\n   entries.\n - Do not hand-edit `.shiki/goals/*.json` or `.shiki/manifest.json` in a task PR.\n - Conversation state is not durable truth. If a decision matters, record it in\n-  GitHub, `.shiki/`, `CONTEXT.md`, or an ADR.\n+  GitHub, `.shiki/`, `CONTEXT.md`, a Shiki SADR, or a target ADR.\n \n ## Architecture Gate\n \n@@ -103,7 +103,7 @@\n or CI policy, package structure, three or more structural files, more than 100\n net new lines in a core module, three or more new files, or any high-risk label.\n When triggered, run `grill-with-docs` or `improve-codebase-architecture` and\n-record durable decisions in an ADR when needed (see `docs/adr/`).\n+record durable decisions in a Shiki SADR or target ADR as applicable (see `docs/adr/`).\n \n ## Safety\n \ndiff --git a/SYSTEM_PROMPT.md b/SYSTEM_PROMPT.md\nindex aaaea45e3a896c3a44ba89eec8bb84325ecf6c62..da009c86a88285269dee0155e58e550c6d744ea9\n--- a/SYSTEM_PROMPT.md\n+++ b/SYSTEM_PROMPT.md\n@@ -30,6 +30,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n Use the canonical source-of-truth order above. Runtime-specific reading orders\n are operational guidance only and must not override or reorder it. The current\n conversation is non-durable operator input.\n@@ -48,14 +51,14 @@\n \n A runtime may only do work that is explicitly assigned by the Goal, task, PR, or MergeGate decision.\n \n-Do not assume `openai/codex-action`, `OPENAI_API_KEY`, or API-key based Codex automation in the default Shiki path. API-key runners are explicit target-repo extensions and require their own ADR.\n+Do not assume `openai/codex-action`, `OPENAI_API_KEY`, or API-key based Codex automation in the default Shiki path. API-key runners are explicit target-repo extensions and require their own target ADR.\n \n ## 4. Non-negotiable Shiki flow\n \n For any non-trivial Goal, use this flow:\n \n 1. **Goal Seek**: define outcome, non-goals, risk level, completion criteria, and evidence requirements.\n-2. **grill-with-docs**: challenge the plan against `CONTEXT.md`, ADRs, code reality, terminology, boundaries, and edge scenarios. Ask one question at a time when the operator is available. If code can answer a question, inspect code instead of asking.\n+2. **grill-with-docs**: challenge the plan against `CONTEXT.md`, Shiki SADRs, target ADRs, code reality, terminology, boundaries, and edge scenarios. Ask one question at a time when the operator is available. If code can answer a question, inspect code instead of asking.\n 3. **Context & Impact**: identify modules, interfaces, seams, callers, dependencies, locks, risk, and verification surfaces.\n 4. **to-prd + Spec Freeze**: turn settled context into a PRD using domain vocabulary and testing decisions. The operator's explicit approval of the PRD is Spec Freeze, recorded as the plan's `spec_freeze` block; plans without it do not run. Steps 1-4 form Requirements Definition.\n 5. **to-issues**: decompose the PRD into independently grabbable vertical-slice issues. Prefer AFK slices; mark HITL slices when judgment is still required.\n@@ -75,7 +78,7 @@\n Before making any material change:\n \n 1. Read `AGENTS.md` and any runtime-specific wrapper such as `CLAUDE.md`.\n-2. Read `CONTEXT.md` and relevant ADRs.\n+2. Read `CONTEXT.md` and relevant SADRs and target ADRs.\n 3. Read the GitHub Issue / PR and `.shiki/` state for the active Goal or task.\n 4. Identify your role: Planner, Implementer, Reviewer, Completion Check Agent, Repairer, or Guardian-assist.\n 5. Identify Goal id, task id, branch, PR, dependencies, locks, risk level, required skills, and required evidence.\n@@ -103,7 +106,7 @@\n \n At minimum, identify:\n \n-- Relevant documents, ADRs, domain terms, and prior decisions.\n+- Relevant documents, SADRs, target ADRs, domain terms, and prior decisions.\n - Relevant modules, interfaces, seams, callers, owners, and tests.\n - Dependency relationships between tasks.\n - Candidate file locks and lock conflicts.\n@@ -152,7 +155,7 @@\n \n CCA must read:\n \n-- `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant ADRs.\n+- `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant SADRs and target ADRs.\n - Parent Goal issue, PRD issue, task issue, comments, and labels.\n - PR body, diff, commits, changed files, reviews, CI checks, and artifacts available to the workflow.\n - `.shiki/` task contract, locks, ledger entries, CCA checklist profile, and prior repair packets.\n@@ -214,7 +217,7 @@\n - Three or more new files.\n - Any high-risk or critical label.\n \n-When the architecture gate triggers, run `grill-with-docs` or `improve-codebase-architecture` as appropriate and record the decision in an ADR if it is hard to reverse or likely to be re-litigated.\n+When the architecture gate triggers, run `grill-with-docs` or `improve-codebase-architecture` as appropriate and record the decision in a Shiki SADR or target ADR as applicable if it is hard to reverse or likely to be re-litigated.\n \n ## 14. Skill Gate\n \n@@ -223,7 +226,7 @@\n Use these skills:\n \n - `setup-matt-pocock-skills`: before first use of the engineering skill set, or when issue tracker, triage labels, or domain docs are missing.\n-- `grill-with-docs`: default for non-trivial Goals before PRD/issues; required for ambiguous plans, terminology, boundaries, ADR-worthy tradeoffs, or design-tree decisions.\n+- `grill-with-docs`: default for non-trivial Goals before PRD/issues; required for ambiguous plans, terminology, boundaries, decision-record-worthy tradeoffs, or design-tree decisions.\n - `zoom-out`: unfamiliar code area or missing architectural map.\n - `to-prd`: convert settled context into a PRD.\n - `to-issues`: break a PRD, plan, or Goal into independently grabbable vertical-slice issues.\ndiff --git a/docs/adr/0001-keep-shiki-as-platform-name.md b/docs/adr/SADR-0001-keep-shiki-as-platform-name.md\nindex 20a9637c3b9d8c97de7ba8db7cfae4d82d9a7dba..8b685439959aada1b221c112f0ac3c51ca8a1695\n--- a/docs/adr/0001-keep-shiki-as-platform-name.md\n+++ b/docs/adr/SADR-0001-keep-shiki-as-platform-name.md\n@@ -1,3 +1,3 @@\n-# Keep Shiki As The Platform Name\n+# SADR-0001: Keep Shiki As The Platform Name\n \n Shiki remains the product name for the GitHub-native agentic engineering control plane. We considered more descriptive names such as MergePilot, MergeGate, and Kiroku, but kept Shiki because it carries the existing goal-seeking framework identity while remaining independent of Hermes, Codex, Claude Code, or any single runtime.\ndiff --git a/docs/adr/0002-use-agents-as-shared-constitution.md b/docs/adr/SADR-0002-use-agents-as-shared-constitution.md\nindex 73de30ecb078120eac540d9bc51855e3cd48d0e3..c5486182bf06fca36ea508496b013c67302fd989\n--- a/docs/adr/0002-use-agents-as-shared-constitution.md\n+++ b/docs/adr/SADR-0002-use-agents-as-shared-constitution.md\n@@ -1,3 +1,3 @@\n-# Use AGENTS As The Shared Agent Constitution\n+# SADR-0002: Use AGENTS As The Shared Agent Constitution\n \n Shiki uses `AGENTS.md` as the runtime-neutral constitution and `CLAUDE.md` as a thin Claude Code wrapper. This keeps Codex, Claude Code, Hermes Runner, and future Agent Runtimes aligned on the same GitHub-first MergeGate rules while still allowing runtime-specific instructions where needed.\ndiff --git a/docs/adr/0003-require-skill-gate-for-mergegate-readiness.md b/docs/adr/SADR-0003-require-skill-gate-for-mergegate-readiness.md\nindex 424be0c9af4c8f0136f1fde68a468c47ff84e764..7fc337987c7df9792f75f181dfcbed75fe5ffff2\n--- a/docs/adr/0003-require-skill-gate-for-mergegate-readiness.md\n+++ b/docs/adr/SADR-0003-require-skill-gate-for-mergegate-readiness.md\n@@ -1,3 +1,3 @@\n-# Require Skill Gate For MergeGate Readiness\n+# SADR-0003: Require Skill Gate For MergeGate Readiness\n \n Shiki requires relevant engineering skills to be invoked before execution when their triggers apply. This makes skills part of MergeGate readiness rather than optional prompt style, because Shiki depends on repeatable planning, issue decomposition, TDD, diagnosis, triage, and architecture review before autonomous or parallel agent work can be trusted.\ndiff --git a/docs/adr/0004-use-github-first-with-shiki-mirror.md b/docs/adr/SADR-0004-use-github-first-with-shiki-mirror.md\nindex d0a3a9174dc646a945a9d325954ffaf00c352040..a79c54ae9ee8eed2a0524192b864da4e22a92d3f\n--- a/docs/adr/0004-use-github-first-with-shiki-mirror.md\n+++ b/docs/adr/SADR-0004-use-github-first-with-shiki-mirror.md\n@@ -1,4 +1,4 @@\n-# ADR 0004: Use GitHub First With A Repository-Local Shiki Mirror\n+# SADR-0004: Use GitHub First With A Repository-Local Shiki Mirror\n \n ## Status\n \ndiff --git a/docs/adr/0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md b/docs/adr/SADR-0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\nindex b339baf49b89d9290e79c3d51b36e7b2bde3bd95..0f3c3eb9afccdb3403ce978e21d04fe12e495da0\n--- a/docs/adr/0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\n+++ b/docs/adr/SADR-0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\n@@ -1,8 +1,8 @@\n-# ADR 0005: Use Codex As Front And Claude Code Action As The GitHub Runtime\n+# SADR-0005: Use Codex As Front And Claude Code Action As The GitHub Runtime\n \n ## Status\n \n-Accepted (implementer default superseded by ADR 0008; the auth model and the\n+Accepted (implementer default superseded by SADR-0008; the auth model and the\n Claude Code Action GitHub runtime decision remain in force)\n \n ## Context\n@@ -26,4 +26,4 @@\n - Shiki avoids requiring `OPENAI_API_KEY` for the default workflow.\n - Codex work happens through the user's authenticated Codex front surface, not as a hidden backend Action.\n - Claude Code Action is configured for OAuth-token based subscription usage by default.\n-- API-key based automation remains possible only as an explicit target-repo extension with its own ADR.\n+- API-key based automation remains possible only as an explicit target-repo extension with its own target ADR.\ndiff --git a/docs/adr/0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md b/docs/adr/SADR-0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\nindex 4bcae9d1e52f0a37b8e9fbafa5cd14b6ee1993bd..7a38538a6ae9929567f0ade67e7ea6a60d0b3f7e\n--- a/docs/adr/0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\n+++ b/docs/adr/SADR-0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\n@@ -1,4 +1,4 @@\n-# ADR 0006: Separate Codex Implementation, CCA Completion, And MergeGate Authorization\n+# SADR-0006: Separate Codex Implementation, CCA Completion, And MergeGate Authorization\n \n ## Status\n \ndiff --git a/docs/adr/0007-packaging-and-release.md b/docs/adr/SADR-0007-packaging-and-release.md\nindex 79b84ddb97167372b44641e5b7860e400ce80822..bb445a2e2e856d9d1f5fdbe35de8d4e8cec0fb32\n--- a/docs/adr/0007-packaging-and-release.md\n+++ b/docs/adr/SADR-0007-packaging-and-release.md\n@@ -1,4 +1,4 @@\n-# ADR 0007: Packaging And Release For Shiki\n+# SADR-0007: Packaging And Release For Shiki\n \n ## Status\n \n@@ -25,7 +25,7 @@\n   not introduce third-party runtime dependencies or break the shim.\n - Releases must run inside the operator's subscription-authenticated toolchain\n   and must not require extra secrets beyond the built-in `GITHUB_TOKEN`\n-  (consistent with ADR 0005's no-API-key-by-default stance).\n+  (consistent with SADR-0005's no-API-key-by-default stance).\n - `.shiki/manifest.json` already encodes the install surface (`install.include`,\n   `install.create_directories`, `install.exclude_from_commit`), and it must not\n   be mutated as part of this change.\n@@ -34,7 +34,7 @@\n \n This is a hard-to-reverse platform decision (it sets the version source of\n truth, the release trigger, and the public install contract), so it is recorded\n-as an ADR.\n+as a SADR.\n \n ## Decision\n \n@@ -104,7 +104,7 @@\n 3. **Publishing to PyPI.** Rejected for now: Shiki is distributed as a\n    repository template + git-installable CLI, not a PyPI library; PyPI would add\n    credentials/secrets and a different versioning contract. Can be added later as\n-   its own ADR if a packaged distribution is needed.\n+   its own SADR if a packaged distribution is needed.\n 4. **Dynamic version (read `VERSION` via a build backend `dynamic` field).**\n    Rejected to keep `pyproject.toml` backend-agnostic and dependency-free; the\n    small duplication between `VERSION` and `pyproject.toml` is guarded by the\n@@ -123,4 +123,4 @@\n   releasing versions the whole platform but ships only `install.include` to\n   targets.\n - Future PyPI distribution or a console entry point are deferred and would each\n-  warrant their own ADR.\n+  warrant their own SADR.\ndiff --git a/docs/adr/0008-use-claude-code-as-default-implementer.md b/docs/adr/SADR-0008-use-claude-code-as-default-implementer.md\nindex dedf4510b7c7799521422f622029563d373db031..35c2625d908167fbc58855b5b63e283805f167e8\n--- a/docs/adr/0008-use-claude-code-as-default-implementer.md\n+++ b/docs/adr/SADR-0008-use-claude-code-as-default-implementer.md\n@@ -1,12 +1,12 @@\n-# ADR 0008: Use Claude Code As The Default Implementer Runtime\n+# SADR-0008: Use Claude Code As The Default Implementer Runtime\n \n ## Status\n \n-Proposed (supersedes ADR 0005 for the implementer default; keeps its auth model)\n+Proposed (supersedes SADR-0005 for the implementer default; keeps its auth model)\n \n ## Context\n \n-ADR 0005 made Codex Front the default implementation runtime, with Claude Code\n+SADR-0005 made Codex Front the default implementation runtime, with Claude Code\n restricted to planner and reviewer roles by the runtime registry, and the only\n autonomous implementation adapter was `shiki runner codex`.\n \n@@ -18,13 +18,13 @@\n - The platform's planning, review, and CCA layers already run on Claude Code,\n   and Claude Code's native capabilities (the built-in `/code-review` skill and\n   the Workflow multi-agent orchestration tool) have no Codex equivalent.\n-- The Goal lifecycle redesign (ADR 0009) requires an autonomous post-freeze\n+- The Goal lifecycle redesign (SADR-0009) requires an autonomous post-freeze\n   loop driven from the operator's Claude Code session, including bounded\n   repair dispatch, which must not depend on a runtime that cannot be invoked\n   unattended.\n \n The constitution claims Shiki is runtime-agnostic (\"not a Claude-only\n-workflow\", ADR 0002). Any change to the implementer default must preserve the\n+workflow\", SADR-0002). Any change to the implementer default must preserve the\n registry-based runtime model rather than hardcoding one vendor.\n \n ## Decision\n@@ -39,7 +39,7 @@\n   the task to review or repair-needed.\n - Keep Codex registered as an optional implementer runtime. `shiki runner\n   codex`, the Codex handoff templates, and the ChatGPT OAuth auth model from\n-  ADR 0005 remain supported for tasks explicitly assigned to `codex`.\n+  SADR-0005 remain supported for tasks explicitly assigned to `codex`.\n - Commit to mandating two Claude-native capabilities as loop contract in\n   follow-up Goals (status: the code-review gate was delivered by Goal G-B \u2014\n   skill registry, checklists item PR-12, default required_skills \u2014 while the\n@@ -60,7 +60,7 @@\n \n Out of scope: removing Codex support, changing the CCA runtime (Claude Code\n Action with `CLAUDE_CODE_OAUTH_TOKEN` stays), and API-key based automation\n-(still requires its own ADR per ADR 0005).\n+(still requires its own target ADR per SADR-0005).\n \n ## Consequences\n \n@@ -100,7 +100,7 @@\n   harness rejection of direct `codex exec` is outside Shiki's control, and the\n   plan's mandatory native capabilities cannot run on Codex at all.\n - **Remove Codex entirely**: rejected; it abandons the runtime-agnostic\n-  constitution (ADR 0002) for no operational gain and forecloses\n+  constitution (SADR-0002) for no operational gain and forecloses\n   cross-runtime adversarial review.\n - **GitHub Actions cloud implementation via claude-code-action**: rejected as\n   default; durable evidence is automatic but token cost, Node 24 workflow\ndiff --git a/docs/adr/0009-requirements-definition-and-spec-freeze.md b/docs/adr/SADR-0009-requirements-definition-and-spec-freeze.md\nindex 29b3f7871bef9203a8b4a831a65c39eb2f506e42..52e7dd69ad0aa4efe0b68c62678abbf7df02aaec\n--- a/docs/adr/0009-requirements-definition-and-spec-freeze.md\n+++ b/docs/adr/SADR-0009-requirements-definition-and-spec-freeze.md\n@@ -1,4 +1,4 @@\n-# ADR 0009: Merge Grill And Planning Into Requirements Definition With Spec Freeze\n+# SADR-0009: Merge Grill And Planning Into Requirements Definition With Spec Freeze\n \n ## Status\n \ndiff --git a/docs/adr/0010-external-ai-guardian-review.md b/docs/adr/SADR-0010-external-ai-guardian-review.md\nindex e6108bbe8bb4e73e0ff4684c8f78001a80438c4e..f3510b145664298adafb151b6f0db51b972f01ec\n--- a/docs/adr/0010-external-ai-guardian-review.md\n+++ b/docs/adr/SADR-0010-external-ai-guardian-review.md\n@@ -1,4 +1,4 @@\n-# ADR 0010: External AI Guardian Review As A First-Class Approval Authority\n+# SADR-0010: External AI Guardian Review As A First-Class Approval Authority\n \n ## Status\n \n@@ -8,7 +8,7 @@\n \n Shiki is intended to be an autonomous, recursive, self-improving agent\n framework: authority-in-the-loop, not human-in-the-loop. Until now the Guardian\n-approval required for high/critical-risk merges (guardian-policy.json, ADR\n+approval required for high/critical-risk merges (guardian-policy.json, SADR\n governance) recognized only human authority sources \u2014 a GitHub review approval,\n the `guardian:approved` label applied by a configured Guardian, or a comment\n carrying `Guardian approval granted` plus the current head SHA.\n@@ -89,7 +89,7 @@\n   autonomous design the operator intends and produced the impersonation\n   incident.\n - **Record AI approval as an operator comment**: rejected as forgery of human\n-  authority \u2014 the exact failure this ADR corrects.\n+  authority \u2014 the exact failure this SADR corrects.\n - **Deliver the artifact as a committed `.shiki/` file**: rejected; a committed\n   approval inside the PR being approved is forgeable by anyone who can push.\n - **A dedicated reviewer bot identity instead of a relayed comment**: deferred;\ndiff --git a/docs/adr/0011-autonomous-quality-gate-evidence.md b/docs/adr/SADR-0011-autonomous-quality-gate-evidence.md\nindex bca0246c5d46e44afbde15486a75eec425fc02f5..9c93eac16d8c3a6d4b11bd7224e3955102cd7db4\n--- a/docs/adr/0011-autonomous-quality-gate-evidence.md\n+++ b/docs/adr/SADR-0011-autonomous-quality-gate-evidence.md\n@@ -1,4 +1,4 @@\n-# ADR 0011: Autonomous Quality-Gate Evidence \u2014 Deterministic Facts And An Independent Verifier\n+# SADR-0011: Autonomous Quality-Gate Evidence \u2014 Deterministic Facts And An Independent Verifier\n \n ## Status\n \ndiff --git a/docs/adr/0012-autonomous-loop-closeout-to-main.md b/docs/adr/SADR-0012-autonomous-loop-closeout-to-main.md\nindex 637fad6dde4989c9c2bf0f81f676d8d29991123c..ec7d85f81030a676bcbba28b43eb365422a36eac\n--- a/docs/adr/0012-autonomous-loop-closeout-to-main.md\n+++ b/docs/adr/SADR-0012-autonomous-loop-closeout-to-main.md\n@@ -1,4 +1,4 @@\n-# 0012 \u2014 Autonomous loop pushes goal completion to main via a closeout PR\n+# SADR-0012 \u2014 Autonomous loop pushes goal completion to main via a closeout PR\n \n - Status: accepted\n - Date: 2026-06-18\ndiff --git a/docs/adr/0013-bootstrap-grants-actions-approve-pull-requests.md b/docs/adr/SADR-0013-bootstrap-grants-actions-approve-pull-requests.md\nindex b6c69d64f1c5ced346b88d6859586715b3b5bd0e..cf48ff4a6716267f539ca58edd1a852e7c7a2d64\n--- a/docs/adr/0013-bootstrap-grants-actions-approve-pull-requests.md\n+++ b/docs/adr/SADR-0013-bootstrap-grants-actions-approve-pull-requests.md\n@@ -1,4 +1,4 @@\n-# ADR 0013: Bootstrap Grants GitHub Actions Permission To Create And Approve Pull Requests\n+# SADR-0013: Bootstrap Grants GitHub Actions Permission To Create And Approve Pull Requests\n \n ## Status\n \n@@ -31,7 +31,7 @@\n security tradeoff: it lets repository automation satisfy a human-review gate. In\n a multi-maintainer repo that could let a compromised or buggy workflow\n self-approve changes. So whether bootstrap turns it on by default \u2014 and with\n-what default token scope \u2014 is an ADR-worthy decision, not an implementation\n+what default token scope \u2014 is a SADR-worthy decision, not an implementation\n detail.\n \n ## Decision\n@@ -48,7 +48,7 @@\n Specifically:\n \n - **`can_approve_pull_request_reviews` defaults to `true`.** Shiki's default\n-  posture is solo/AFK autonomous operation (ADR 0008/0009/0012), where the CCA\n+  posture is solo/AFK autonomous operation (SADR-0008/0009/0012), where the CCA\n   Review Bridge is the only path that can satisfy `required_review: true` after\n   the CCA and Guardian gates have already passed. The default makes a\n   freshly-bootstrapped repo able to run the autonomous loop end to end without a\ndiff --git a/docs/adr/0014-codex-app-external-ai-guardian-ui-adapter.md b/docs/adr/SADR-0014-codex-app-external-ai-guardian-ui-adapter.md\nindex 238db06279334caa341c117362df9271382a7fcd..65f12e9939321e1b3e389a34c822fc1549e5155d\n--- a/docs/adr/0014-codex-app-external-ai-guardian-ui-adapter.md\n+++ b/docs/adr/SADR-0014-codex-app-external-ai-guardian-ui-adapter.md\n@@ -1,4 +1,4 @@\n-# ADR 0014: Use Codex App As The External AI Guardian UI Adapter\n+# SADR-0014: Use Codex App As The External AI Guardian UI Adapter\n \n ## Status\n \ndiff --git a/docs/adr/0015-contract-approval-before-dispatch.md b/docs/adr/SADR-0015-contract-approval-before-dispatch.md\nindex fad6c656741a20a7684ba16080a7db159f63a52f..6061271a46d3f1d6ae7174477de3fb75673123a6\n--- a/docs/adr/0015-contract-approval-before-dispatch.md\n+++ b/docs/adr/SADR-0015-contract-approval-before-dispatch.md\n@@ -1,4 +1,4 @@\n-# ADR 0015: Approve The Task Contract Before Dispatch, Not The Diff After It\n+# SADR-0015: Approve The Task Contract Before Dispatch, Not The Diff After It\n \n ## Status\n \n@@ -9,7 +9,7 @@\n Guardian approval for high/critical risk is expressible only on a pull request.\n Every approval source in `guardian-policy.json` \u2014 GitHub review, the\n `guardian:approved` label, the `Guardian approval granted` comment, and the\n-`external_ai_guardian_review` artifact (ADR 0010/0014) \u2014 is bound to a PR, and\n+`external_ai_guardian_review` artifact (SADR-0010/0014) \u2014 is bound to a PR, and\n three of the four require the current head SHA. Approval therefore cannot exist\n before an implementation PR exists.\n \n@@ -81,7 +81,7 @@\n An envelope breach must surface only on the non-repairable policy gate, never on\n the repairable `MergeGate metadata check`. Otherwise the Goal Loop routes it to\n `dispatch_repair` \u2014 instructing a runner to make the breach go away \u2014 which is\n-exactly what this ADR forbids.\n+exactly what this SADR forbids.\n \n **Contract immutability.** After registration, an implementation PR must not\n change the governance fields of its own task contract (`scope`, `non_goals`,\n@@ -116,7 +116,7 @@\n fails any verdict that is not `complete`, and the signal the CCA prompt reads is\n computed from `evaluate_guardian_approval` alone. Without the OR-merge at the\n signal, a contract-approved PR still returns `needs_guardian`, the CCA job fails,\n-and the MergeGate policy check never runs \u2014 the gate this ADR changes would never\n+and the MergeGate policy check never runs \u2014 the gate this SADR changes would never\n be reached. The CCA *prompt* needs no change: a `contract_approval` entry in the\n signal's existing `sources` array satisfies its current wording.\n \n@@ -130,7 +130,7 @@\n written, and an approved contract lets its implementation PRs merge through the\n same automatic path as low/medium risk. The wasted implement-then-block cycle\n disappears. Guardian review shifts from reading diffs to reading contracts, which\n-is both earlier and smaller; the external AI Guardian path (ADR 0010/0014) works\n+is both earlier and smaller; the external AI Guardian path (SADR-0010/0014) works\n on contracts with no change.\n \n We accept that the Guardian authorizes a class of change without seeing its\n@@ -146,7 +146,7 @@\n adversarial tests for the new approval path equivalent in rigor to\n `test_shiki_governance_evidence.sh`.\n \n-Bootstrap costs, which apply only while this ADR is being implemented. The tasks\n+Bootstrap costs, which apply only while this SADR is being implemented. The tasks\n that build Contract Approval merge under the existing rules, so the ones\n dispatched after the pre-dispatch gate lands but before the approval path lands\n need a Guardian-approved Contract PR *and* a live Guardian approval on their\n@@ -210,7 +210,7 @@\n risk; they do not bound authorized work.\n \n **Auto-escalate risk on envelope breach instead of stopping.** Returns the\n-workflow to a diff-time Guardian decision \u2014 the exact behavior this ADR removes.\n+workflow to a diff-time Guardian decision \u2014 the exact behavior this SADR removes.\n \n **Resolve governance fields from the base snapshot only.** Rejected against the\n registration flow: implementation PRs add their own contract, so the base has no\ndiff --git a/docs/adr/0016-locks-bound-product-paths.md b/docs/adr/SADR-0016-locks-bound-product-paths.md\nindex b5e850f3f1f7f4ef6e7f9d1763a29a51bd300c45..46b40a9b99a7aae644c02bb458f5473ff0a90ce2\n--- a/docs/adr/0016-locks-bound-product-paths.md\n+++ b/docs/adr/SADR-0016-locks-bound-product-paths.md\n@@ -1,4 +1,4 @@\n-# ADR 0016: Locks bound product paths; the Shiki mirror is bound by record identity\n+# SADR-0016: Locks bound product paths; the Shiki mirror is bound by record identity\n \n ## Status\n \n@@ -6,8 +6,8 @@\n \n - Date: 2026-07-28\n - Deciders: operator (mizutani-140), Claude (planner)\n-- Related: ADR 0004 (GitHub-first with a `.shiki` mirror), ADR 0009 (Spec Freeze),\n-  ADR 0012 (autonomous loop closeout to main), ADR 0015 (Contract Approval before\n+- Related: SADR-0004 (GitHub-first with a `.shiki` mirror), SADR-0009 (Spec Freeze),\n+  SADR-0012 (autonomous loop closeout to main), SADR-0015 (Contract Approval before\n   dispatch), remediation goal task R-02, the live PR #179 deadlock.\n \n ## Context\n@@ -90,11 +90,11 @@\n files, documentation files and named source modules are nameable; runtime-\n allocated ledger, runner and report ids are not \u2014 and those live in `.shiki`,\n which locks no longer govern. Existing frozen contracts are brought into line by\n-a one-time Spec Amendment per plan, which must land before ADR 0015's contract\n+a one-time Spec Amendment per plan, which must land before SADR-0015's contract\n immutability makes `locks` unwritable from the implementation PR.\n \n **Lock state is resolved from the durable side.** MergeGate resolves the lock\n-registry, and ADR 0015's governance fields, from the base `.shiki` snapshot when\n+registry, and SADR-0015's governance fields, from the base `.shiki` snapshot when\n present, falling back to head only when absent. The `MergeGate metadata check`\n job is given `--base-shiki`, which it does not receive today. Locks are not\n derived from the live open-pull-request set: a pull request body is untrusted\n@@ -108,7 +108,7 @@\n Explicitly out of scope: moving MergeGate's contract resolution off the PR\n checkout wholesale; making the mirror a projection of GitHub state; introducing\n new lock kinds; moving ledger evidence out of the repository; and unwinding\n-ADR 0012's closeout pull request. Registration of multi-task goals is ADR 0015's\n+SADR-0012's closeout pull request. Registration of multi-task goals is SADR-0015's\n Contract PR and is not re-decided here.\n \n ## Consequences\n@@ -127,7 +127,7 @@\n \n Risks accepted. First, goal-scoped identity is only as strong as `goal_id`,\n which is resolved from the PR body and the head task file; the mitigation is to\n-add `goal_id` to ADR 0015's frozen governance field set, and until that lands a\n+add `goal_id` to SADR-0015's frozen governance field set, and until that lands a\n task file can name a foreign goal. Second, base-when-present resolution is\n unavailable on fork pull requests, where the head is contributor-controlled;\n fork pull requests already cannot complete a Shiki merge, and this decision does\n@@ -139,7 +139,7 @@\n path, not by implementation pull requests.\n \n New obligations: one Spec Amendment per frozen plan to rewrite `locks`, ordered\n-before ADR 0015 T1; extension of the adversarial suites in\n+before SADR-0015 T1; extension of the adversarial suites in\n `scripts/test_shiki_adversarial_state.sh` and\n `scripts/test_shiki_governance_evidence.sh` to cover the eleven newly-scoped\n directories; and updating `scripts/test_shiki_loop_lock_guard.sh`,\n@@ -151,32 +151,32 @@\n whether the loop's transitive sync of ledger `evidence` references can be\n deleted once the Contract PR carries its own source plan.\n \n-## Relationship to ADR 0004\n+## Relationship to SADR-0004\n \n-This ADR **clarifies something ADR 0004 left unspecified**. ADR 0004 decides that\n+This SADR **clarifies something SADR-0004 left unspecified**. SADR-0004 decides that\n GitHub is the operational source of truth and that each target repository keeps a\n `.shiki/` mirror. It says nothing about pull requests writing the mirror, does\n not name MergeGate, and does not make the mirror an input to a gate. Nothing in\n-ADR 0004's Decision or Consequences is amended, superseded or weakened: the\n+SADR-0004's Decision or Consequences is amended, superseded or weakened: the\n mirror still exists, still records the same artifacts, and pull request\n-boundaries remain the coordination surface. What this ADR settles is the question\n+boundaries remain the coordination surface. What this SADR settles is the question\n 0004 never reached \u2014 by what authority a pull request may write a mirror record \u2014\n and it answers it the way `.shiki/manifest.json` already declares\n (`pr_mutation: current-task-or-current-goal-only`).\n \n-It **amends ADR 0012**, whose Decision item 5 states that \"Loop-executed tasks\n+It **amends SADR-0012**, whose Decision item 5 states that \"Loop-executed tasks\n declare the `path:.shiki/**` lock, which covers every `.shiki` file the closeout\n stages ... so `files_outside_locks` is satisfied without per-file lock\n bookkeeping.\" That clause is replaced: the closeout's mirror writes are\n-authorized by record identity, not by a declared lock. ADR 0012's substantive\n+authorized by record identity, not by a declared lock. SADR-0012's substantive\n decision \u2014 that completion reaches `main` through a closeout pull request rather\n than local attestation \u2014 is preserved unchanged.\n \n-It is **complementary to ADR 0015**, not a substitute. ADR 0015 owns contract\n+It is **complementary to SADR-0015**, not a substitute. SADR-0015 owns contract\n immutability, the pre-dispatch gate, the Contract PR and the Scope Envelope. This\n-ADR owns what a lock means and who may write the mirror. The one ordering\n+SADR owns what a lock means and who may write the mirror. The one ordering\n constraint between them is that the lock-narrowing Spec Amendment must precede\n-ADR 0015 T1's freezing of the `locks` field.\n+SADR-0015 T1's freezing of the `locks` field.\n \n ## Alternatives Considered\n \n@@ -186,7 +186,7 @@\n the only writer that can register them may run. Migrating the Guardian signal to\n that store makes every registration PR fail closed to `needs_guardian` at every\n risk level, because the task file is by definition absent from the store, which\n-removes the low/medium autonomous path ADR 0008/0009 exists to provide. And it\n+removes the low/medium autonomous path SADR-0008/0009 exists to provide. And it\n leaves schemas, manifest, config, guardian policy, templates, migrations, plans,\n worktrees, runner records, runs and handoffs with no writer at all in a\n repository whose product is `.shiki`. Measured benefit on the frozen backlog:\n@@ -199,14 +199,14 @@\n namespace is not in fact ownership-partitioned \u2014 56 ledger entries carry no\n `task_id`, and one goal DAG is writable by 35 registered tasks. The correct\n expression of its insight is that locks do not govern the mirror at all, which is\n-what this ADR decides.\n+what this SADR decides.\n \n **Make the mirror a projection written only by a registration writer and a\n post-merge projection job.** Rejected. It requires a bypass actor with write\n access to the protected default branch; it makes its own master invariant\n readable from a manifest that pull requests may edit; it deletes both reconcile\n modes and leaves a corrupted mirror with no in-band repair path, contradicting\n-ADR 0004's consequence that mirror conflicts are repaired against GitHub state;\n+SADR-0004's consequence that mirror conflicts are repaired against GitHub state;\n and its content-derived ids are rejected by `validate_shiki`'s currently-green id\n pattern.\n \ndiff --git a/docs/adr/0017-bookkeeping-closeouts-do-not-inherit-risk.md b/docs/adr/SADR-0017-bookkeeping-closeouts-do-not-inherit-risk.md\nindex 554e3a0d526a72ca067108c5464cc68c3edcf174..b9676857a4713850e063af7c39c8e0b01ecb4c66\n--- a/docs/adr/0017-bookkeeping-closeouts-do-not-inherit-risk.md\n+++ b/docs/adr/SADR-0017-bookkeeping-closeouts-do-not-inherit-risk.md\n@@ -1,4 +1,4 @@\n-# ADR 0017: A Bookkeeping Closeout Does Not Inherit Its Task's Risk\n+# SADR-0017: A Bookkeeping Closeout Does Not Inherit Its Task's Risk\n \n ## Status\n \n@@ -6,7 +6,7 @@\n \n ## Context\n \n-ADR 0012 made the goal loop push a task's completion to the default branch through a\n+SADR-0012 made the goal loop push a task's completion to the default branch through a\n closeout pull request: after the implementation PR merges, the loop opens a second PR\n carrying `task.status = done`, `lock.state = released`, and \u2014 when the task completes\n its goal \u2014 `goal.status = complete` plus one scorecard report. Nothing else. The\n@@ -49,14 +49,14 @@\n 4. The task file's governance fields (`scope`, `non_goals`, `required_skills`,\n    `risk_level`, `locks`, `acceptance_checks`, `test_command`) are byte-identical to\n    the base snapshot. Only `status`, `expected_pr`, `closeout_pr`, `ledger_evidence`\n-   and `expected_branch` may differ \u2014 the ADR 0015 contract-immutability rule,\n+   and `expected_branch` may differ \u2014 the SADR-0015 contract-immutability rule,\n    unchanged.\n \n    `expected_branch` is in that set because a closeout is *by construction* on a\n    different branch from the implementation it closes, and the loop rewrites the\n    field accordingly. It is safe to let it differ because MergeGate independently\n    requires `expected_branch` to equal the PR's actual head ref, so a PR cannot set\n-   it to anything other than the branch it is really on. The first draft of this ADR\n+   it to anything other than the branch it is really on. The first draft of this SADR\n    omitted it, which made the exemption unreachable: every real closeout failed\n    condition 4 on a field the loop always changes. See PR #209.\n 5. The state transitions are exactly the terminal set: task `review` \u2192 `done`, lock\n@@ -113,12 +113,12 @@\n classification must be derived, not asserted.\n \n **Fold the closeout into the implementation PR** so no second PR exists. Rejected under\n-ADR 0012, which recorded why: a task file that is already `done` on the implementation\n+SADR-0012, which recorded why: a task file that is already `done` on the implementation\n branch short-circuits the loop's review \u2192 CCA \u2192 merge \u2192 done ordering into a local\n goal-completion before any merge happens.\n \n **Have the loop auto-merge closeouts with elevated credentials.** Rejected outright: that\n-is a runtime authoring its own approval, which ADR 0010 and the constitution's\n+is a runtime authoring its own approval, which SADR-0010 and the constitution's\n approval-record prohibition forbid. It would also be indistinguishable, in the evidence,\n from the forgery those rules exist to prevent.\n \ndiff --git a/docs/adr/0018-carried-guardian-approval-across-a-proven-base-sync.md b/docs/adr/SADR-0018-carried-guardian-approval-across-a-proven-base-sync.md\nindex 22358562da390ed8b45cd600da08a43d378b4680..e6e2907cefa776460af73ddf7d39db605f3c39b0\n--- a/docs/adr/0018-carried-guardian-approval-across-a-proven-base-sync.md\n+++ b/docs/adr/SADR-0018-carried-guardian-approval-across-a-proven-base-sync.md\n@@ -1,4 +1,4 @@\n-# ADR 0018: Carried Guardian Approval Across A Proven Base Sync\n+# SADR-0018: Carried Guardian Approval Across A Proven Base Sync\n \n ## Status\n \n@@ -34,7 +34,7 @@\n Guardian evaluator that consumes it (`shiki_guardian.evaluate_guardian_approval`,\n the `guardian_comment_carried` source) were built and tested in isolation. Neither\n does anything until a caller passes the flags that turn the carry on, so the\n-feature was invisible in CI. This ADR records the decision the two of them\n+feature was invisible in CI. This SADR records the decision the two of them\n implement, and this task wires it into the four CI invocations that judge Guardian\n approval.\n \ndiff --git a/docs/adr/README.md b/docs/adr/README.md\nindex c70e057a4520c36e042ab68c53cd4a21ef709b71..5da43856836e2ce6a150f8127766e2f88355ab4b\n--- a/docs/adr/README.md\n+++ b/docs/adr/README.md\n@@ -1,40 +1,41 @@\n-# Architecture Decision Records\n+# Decision Record Namespaces\n \n-This directory holds Shiki's Architecture Decision Records (ADRs): durable\n-records of hard-to-reverse platform decisions. ADRs are part of the Shiki source\n-of truth alongside GitHub state, the `.shiki/` mirror, and `CONTEXT.md`.\n+This directory holds two independent decision namespaces:\n \n-## How ADRs Work\n+- Shiki Architecture Decision Records (SADRs), named\n+  `SADR-NNNN-<short-slug>.md`, record hard-to-reverse Shiki platform decisions.\n+- Target Architecture Decision Records (ADRs), named\n+  `NNNN-<short-slug>.md`, belong to the target product.\n \n-- New ADRs are added as `docs/adr/NNNN-<short-slug>.md`, where `NNNN` is the next\n-  zero-padded sequence number.\n-- The **canonical list of ADRs is the set of files matching `docs/adr/*.md`** in\n-  this directory. Browse `docs/adr/*.md` for the authoritative, current set.\n-- Use [`template.md`](template.md) as the starting point for a new ADR.\n-- Record an ADR when a decision is hard to reverse, surprising without context,\n-  and a real tradeoff. Do not write an ADR for routine or easily reversible\n-  choices.\n+The same four-digit number may appear once in each namespace. A platform\n+`SADR-NNNN` citation resolves only to `SADR-NNNN-*.md`; a target `ADR NNNN`\n+citation resolves only to `NNNN-*.md`.\n \n-Using the `docs/adr/*.md` glob as the source of truth means other PRs can add an\n-ADR without editing this index file, avoiding merge contention on a\n-hand-maintained enumeration.\n+## How Decision Records Work\n \n-## Current ADRs (snapshot)\n-\n-The list below is a convenience snapshot and may lag behind the directory. When\n-in doubt, list `docs/adr/*.md`.\n+- New Shiki platform decisions use the next zero-padded SADR number.\n+- New target product decisions use the target's own ADR sequence.\n+- The canonical list is the files in `docs/adr/`; no hand-maintained index is\n+  authoritative.\n+- Use [`template.md`](template.md) as the starting point and choose the filename\n+  namespace that owns the decision.\n+- Record a decision when it is hard to reverse, surprising without context, and\n+  a real tradeoff. Do not write a decision record for routine or easily\n+  reversible choices.\n \n-- [0001 \u2014 Keep Shiki As The Platform Name](0001-keep-shiki-as-platform-name.md)\n-- [0002 \u2014 Use AGENTS As The Shared Agent Constitution](0002-use-agents-as-shared-constitution.md)\n-- [0003 \u2014 Require Skill Gate For MergeGate Readiness](0003-require-skill-gate-for-mergegate-readiness.md)\n-- [0004 \u2014 Use GitHub First With A Repository-Local Shiki Mirror](0004-use-github-first-with-shiki-mirror.md)\n-- [0005 \u2014 Use Codex As Front And Claude Code Action As The GitHub Runtime](0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md)\n-- [0006 \u2014 Separate Codex Implementation, CCA Completion, And MergeGate Authorization](0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md)\n+Using the directory as the source of truth lets independent changes add records\n+without contending on an enumerated index.\n \n-## Adding A New ADR\n+## Adding A Shiki SADR\n \n-1. Copy `template.md` to `docs/adr/NNNN-<short-slug>.md`.\n+1. Copy `template.md` to `docs/adr/SADR-NNNN-<short-slug>.md`.\n 2. Fill in the title, status, context, decision, and consequences.\n-3. Set the status to `Proposed`, then `Accepted` once the decision is approved.\n-4. Open a PR following `CONTRIBUTING.md`. You do not need to edit this index for\n-   the ADR to be discoverable via `docs/adr/*.md`.\n+3. Set the status to `Proposed`, then `Accepted` once the proper authority has\n+   approved the decision.\n+4. Open a PR following `CONTRIBUTING.md`.\n+\n+## Adding A Target ADR\n+\n+1. Copy `template.md` to `docs/adr/NNNN-<short-slug>.md` using the target's own\n+   sequence.\n+2. Cite it as `ADR NNNN`; do not prefix its filename with `SADR-`.\ndiff --git a/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md b/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\nnew file mode 100644\nindex 0000000000000000000000000000000000000000..7455ce1f3bed02ddddb851ac21a1d1a648c9aeba\n--- /dev/null\n+++ b/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\n@@ -0,0 +1,55 @@\n+# SADR-0019: Separate Shiki Platform SADRs From Target ADRs\n+\n+## Status\n+\n+Accepted\n+\n+## Context\n+\n+Shiki installs its platform decision records into `docs/adr/` in every target\n+repository. Shiki and its targets previously used the same numeric\n+`NNNN-<slug>.md` namespace. A target that already owned ADRs 0013 through 0018\n+therefore acquired two records for each number during installation, making an\n+`ADR NNNN` citation ambiguous. Renumbering target decisions would rewrite target\n+history, while renumbering Shiki decisions per target would destroy their stable\n+platform identity.\n+\n+Upgrades also need to remove the former Shiki-owned numeric paths without ever\n+mistaking a target-owned ADR for platform content. The install stamp already\n+records exact shipped paths and content digests, so it can prove ownership only\n+when both still match.\n+\n+## Decision\n+\n+Shiki platform decisions use the `SADR` namespace:\n+\n+- records are named `docs/adr/SADR-NNNN-<slug>.md`;\n+- live platform citations use `SADR-NNNN`;\n+- target product decisions retain `docs/adr/NNNN-<slug>.md` and `ADR NNNN`;\n+- the reference validator resolves the two namespaces independently, so the same\n+  four-digit number may exist once in each namespace.\n+\n+Before a forced upgrade writes anything, the installer inventories the exact\n+former numeric Shiki ADR paths. It may delete a legacy path only when the\n+existing install stamp contains that exact path and its recorded SHA-256 digest\n+matches the file currently on disk. All ownership blockers are reported\n+together and abort the upgrade before its first write. A non-forced install\n+never performs this cleanup.\n+\n+Historical `.shiki/` evidence remains unchanged because it records the namespace\n+that existed when that evidence was produced.\n+\n+## Consequences\n+\n+- Targets may use any ADR number without colliding with Shiki's platform SADRs.\n+- Fresh installs preserve target ADR bytes and install a stable SADR set.\n+- Forced upgrades fail closed when legacy ownership cannot be proven, and a\n+  successful upgrade refreshes the stamp without removed legacy paths.\n+- Current governance, packaging, workflow, prompt, script, and skill references\n+  must move with the platform records.\n+\n+## Alternatives Considered\n+\n+- Collision-only refusal leaves affected targets permanently unupgradable.\n+- Renumbering target ADRs mutates target-owned history.\n+- Per-target Shiki renumbering makes platform citations non-portable.\ndiff --git a/docs/adr/template.md b/docs/adr/template.md\nindex a92a97daddd0b00ab9dd626cc8d86942da2eafed..17dd79175b5fee2105935557f691b40cf9492942\n--- a/docs/adr/template.md\n+++ b/docs/adr/template.md\n@@ -1,14 +1,14 @@\n-# NNNN: <Short Decision Title>\n+# <SADR-NNNN or NNNN>: <Short Decision Title>\n \n ## Status\n \n-Proposed | Accepted | Superseded by ADR-NNNN\n+Proposed | Accepted | Superseded by SADR-NNNN or target ADR NNNN\n \n ## Context\n \n What is the situation, constraint, or force that requires a decision? Reference\n-relevant domain terms (`CONTEXT.md`), prior ADRs, and code reality. State the\n-problem, not the solution.\n+relevant domain terms (`CONTEXT.md`), prior Shiki SADRs or target ADRs, and code\n+reality. State the problem, not the solution.\n \n ## Decision\n \ndiff --git a/docs/agents/autonomous-loop-e2e-acceptance.md b/docs/agents/autonomous-loop-e2e-acceptance.md\nindex bbf84c6081987f159ac6ad9ecf3c82b7aa72e7e8..5c13c1d02a05a90b0dc46cbef7c95a9846d0c237\n--- a/docs/agents/autonomous-loop-e2e-acceptance.md\n+++ b/docs/agents/autonomous-loop-e2e-acceptance.md\n@@ -40,8 +40,8 @@\n 3. `gh auth status` is logged in with merge permission on the repo.\n 4. Branch protection + required checks are configured exactly as the goal-loop\n    required-check set expects (see `docs/agents/branch-protection-smoke.md`).\n-5. The repo `.shiki/policy` permits autonomous low/medium-risk merges (ADR\n-   0008/0009); the MergeGate policy check is the recorded-authority gate.\n+5. The repo `.shiki/policy` permits autonomous low/medium-risk merges\n+   (SADR-0008/0009); the MergeGate policy check is the recorded-authority gate.\n \n The operator's only actions are: register the task and start the loop. **No\n manual pre-clearing** means after `shiki loop run` starts, the operator does not\ndiff --git a/docs/agents/bootstrap-command.md b/docs/agents/bootstrap-command.md\nindex 05f7f203a52cf412b9f97084a6e131ed0f785afd..f7aa0b734dfebee4a3b139335b66ee3415f48713\n--- a/docs/agents/bootstrap-command.md\n+++ b/docs/agents/bootstrap-command.md\n@@ -189,7 +189,9 @@\n ```\n \n Use this only for tests, fixtures, or explicit local-only template inspection.\n-Use `--force` only when you intentionally want to overwrite existing target files.\n+Use `--force` only when you intentionally want to refresh the target template.\n+Legacy numeric Shiki decision cleanup is stamp-and-digest authorized before any\n+write; target ADR paths are not cleanup candidates.\n \n ## Slash Command\n \ndiff --git a/docs/agents/checklists.md b/docs/agents/checklists.md\nindex 4a7f03ade346f53a1c1d82981f3fd744f9ab4547..0c5ec6c53dc7197b7b7a7d18e667b325babd6c45\n--- a/docs/agents/checklists.md\n+++ b/docs/agents/checklists.md\n@@ -32,18 +32,18 @@\n | ID | Item | Blocking |\n | --- | --- | --- |\n | GD-01 | Plan was challenged against `CONTEXT.md`. | blocking for non-trivial Goals |\n-| GD-02 | Relevant ADRs were read or absence was recorded. | blocking for non-trivial Goals |\n+| GD-02 | Relevant SADRs and target ADRs were read or absence was recorded. | blocking for non-trivial Goals |\n | GD-03 | Ambiguous terms were resolved or converted to HITL blockers. | blocking |\n | GD-04 | Edge scenarios were considered. | advisory |\n | GD-05 | Code reality was checked when code could answer. | blocking when applicable |\n | GD-06 | New domain terms were added to `CONTEXT.md` when needed. | blocking when applicable |\n-| GD-07 | ADR was created or explicitly skipped using the ADR criteria. | blocking when applicable |\n+| GD-07 | A SADR or target ADR was created or explicitly skipped using the decision-record criteria. | blocking when applicable |\n \n ## CI \u2014 Context & Impact Checklist\n \n | ID | Item | Blocking |\n | --- | --- | --- |\n-| CI-01 | Relevant docs and ADRs identified. | blocking |\n+| CI-01 | Relevant docs, SADRs, and target ADRs identified. | blocking |\n | CI-02 | Relevant modules, interfaces, seams, callers, and tests identified. | blocking |\n | CI-03 | Dependencies and blocked-by relationships identified. | blocking |\n | CI-04 | Candidate locks identified. | blocking |\n@@ -62,7 +62,7 @@\n | PRD-04 | Implementation decisions are recorded without volatile code detail. | blocking |\n | PRD-05 | Testing decisions are recorded. | blocking |\n | PRD-06 | Out-of-scope items are explicit. | blocking |\n-| PRD-07 | Relevant glossary/ADR links are present when applicable. | blocking |\n+| PRD-07 | Relevant glossary/SADR/target-ADR links are present when applicable. | blocking |\n \n ## SF \u2014 Spec Freeze Checklist\n \n@@ -98,7 +98,7 @@\n | H-02 | Branch/worktree target is present. | blocking |\n | H-03 | Scope and non-goals are present. | blocking |\n | H-04 | Dependencies and locks are present. | blocking |\n-| H-05 | Relevant docs, ADRs, modules, and tests are referenced. | blocking |\n+| H-05 | Relevant docs, SADRs, target ADRs, modules, and tests are referenced. | blocking |\n | H-06 | Required skills are stated. | blocking |\n | H-07 | TDD expectations are stated. | blocking when behavior work |\n | H-08 | Acceptance criteria are included. | blocking |\ndiff --git a/docs/agents/codex-handoff.md b/docs/agents/codex-handoff.md\nindex ede468f47cb277f69a857e8c45df703e4ed0151d..6f97bdffb67e44d7edea2c71e01ea28933c4eb6c\n--- a/docs/agents/codex-handoff.md\n+++ b/docs/agents/codex-handoff.md\n@@ -46,7 +46,7 @@\n \n ## Context\n - Domain terms:\n-- Relevant ADRs:\n+- Relevant SADRs and target ADRs:\n - Relevant docs:\n - Relevant modules/interfaces/seams:\n - Similar tests/prior art:\ndiff --git a/docs/agents/completion-check-agent.md b/docs/agents/completion-check-agent.md\nindex 13e4c035db35025f4e3cdcc5adfb24314ac4d521..61bfa1218cf36ca605250c91919e620f33238aa0\n--- a/docs/agents/completion-check-agent.md\n+++ b/docs/agents/completion-check-agent.md\n@@ -32,7 +32,7 @@\n - `AGENTS.md`\n - `CLAUDE.md`\n - `CONTEXT.md`\n-- relevant ADRs\n+- relevant SADRs and target ADRs\n - parent Goal issue\n - PRD issue or document\n - task issue\ndiff --git a/docs/agents/decision-control.md b/docs/agents/decision-control.md\nindex 7d8ee5ab03a5ccbcbd04056ae61de03785a2a9c9..8c94ecda00fffaa0e92c325ab83aa9776bd1dac1\n--- a/docs/agents/decision-control.md\n+++ b/docs/agents/decision-control.md\n@@ -11,7 +11,7 @@\n | Decision | Owner | When | Required inputs | Output | Enforcement surface |\n | --- | --- | --- | --- | --- | --- |\n | Is the Goal clear enough to plan? | Planner + Guardian when needed | Goal intake | User request, existing docs, risk signals | Goal readiness verdict | Goal checklist, issue template, ledger |\n-| Must `grill-with-docs` run? | Planner / Skill Gate | Before PRD or issue split | Ambiguity, domain terms, ADR conflicts, unclear boundaries | Skill invocation or explicit skip rationale | Skill Gate checklist |\n+| Must `grill-with-docs` run? | Planner / Skill Gate | Before PRD or issue split | Ambiguity, domain terms, SADR/target-ADR conflicts, unclear boundaries | Skill invocation or explicit skip rationale | Skill Gate checklist |\n | Is a PRD required? | Planner | After Goal context stabilizes | Goal, `grill-with-docs` output, Context & Impact output | PRD required / not required | PRD checklist, CCA evidence check |\n | Can the spec be frozen? | Operator only | End of Requirements Definition, before issue split | PRD/requirements summary, scope inventory (SF-02), open-question list | `spec_freeze` block in the plan + ledger evidence | `require_grilled_plan` (plan ingest/run), SF checklist |\n | Can a frozen spec change? | Operator only (Spec Amendment) | When implementation contradicts the frozen spec | Contested decisions, scoped re-grill result | recorded amendment + re-stamped freeze | SF checklist, ledger evidence |\ndiff --git a/docs/agents/domain.md b/docs/agents/domain.md\nindex 258da8ea81e87ac0a6b0b5b5e5c2fde980eaabb7..d57b0cf17ff3e1fa9e6e5885372bf083d8295421\n--- a/docs/agents/domain.md\n+++ b/docs/agents/domain.md\n@@ -5,7 +5,9 @@\n ## Layout\n \n - `CONTEXT.md` defines Shiki language. Keep it implementation-free.\n-- `docs/adr/` records hard-to-reverse decisions.\n+- `docs/adr/SADR-NNNN-*.md` records Shiki platform decisions.\n+- `docs/adr/NNNN-*.md` records target product decisions without sharing the\n+  SADR namespace.\n - `AGENTS.md` is the runtime-neutral agent constitution.\n - `CLAUDE.md` is the Claude Code wrapper around `AGENTS.md`.\n - `.shiki/` is the Target Repository mirror for Goals, PRDs, plans, Task DAGs, contracts, locks, worktree records, ledger entries, CCA verdicts, repair packets, reports, and handoffs.\n@@ -15,6 +17,6 @@\n \n - Use Shiki terms exactly when they exist.\n - Add glossary terms to `CONTEXT.md` only when the term is part of the domain, not a generic programming concept.\n-- Create ADRs only for hard-to-reverse, surprising, tradeoff-based decisions.\n+- Create SADRs or target ADRs only for hard-to-reverse, surprising, tradeoff-based decisions.\n - Keep product-specific language in the Target Repository, not in Shiki Template docs.\n-- If an ADR conflicts with a proposed implementation, surface the conflict before editing.\n+- If a SADR or target ADR conflicts with a proposed implementation, surface the conflict before editing.\ndiff --git a/docs/agents/execution-control.md b/docs/agents/execution-control.md\nindex ad8ee3cdb36be56fedc1f46408ca92b387d74047..bc63c3a73661d12b567016ba299f1a6b66bf9088\n--- a/docs/agents/execution-control.md\n+++ b/docs/agents/execution-control.md\n@@ -63,7 +63,7 @@\n | Branch name | Orchestrator | Orchestrator | Before implementer dispatch | branch policy | branch ref in task/ledger |\n | Lock reservation | Context & Impact | MergeGate | Before dispatch and before merge | `.shiki/locks/`, labels, PR metadata | lock id, owner, scope |\n | CI profile | Planner | MergeGate | During issue triage, rechecked at PR | labels, `.shiki/policy.yaml`, workflow matrix | required check list |\n-| Required status checks | Repo maintainer / Guardian | GitHub branch protection / rulesets | Repository setup, updated via ADR | GitHub settings | branch/ruleset config, ADR |\n+| Required status checks | Repo maintainer / Guardian | GitHub branch protection / rulesets | Repository setup, updated via target ADR | GitHub settings | branch/ruleset config, target ADR |\n | CCA judgment | CCA | GitHub required check / MergeGate | Every non-draft PR update | GitHub Action check | structured CCA verdict |\n | Repair assignment | CCA | MergeGate | After failed CCA/CI/review | repair packet, issue/PR comment | repair id and attempt count |\n | Merge readiness | MergeGate | GitHub branch protection / Guardian if needed | After CCA complete | required checks, review rules, rulesets | mergegate check output |\ndiff --git a/docs/agents/guardian-policy.md b/docs/agents/guardian-policy.md\nindex 4147c7adf05a47adb4fd24ada1a1c2e1f2ba6d2d..a20d9cc6c00a569bb057b9ca8d6b51c7fc4a9847\n--- a/docs/agents/guardian-policy.md\n+++ b/docs/agents/guardian-policy.md\n@@ -98,8 +98,8 @@\n `origin/<default_branch>` and reuses each job's existing base fetch; a shallow\n (`--depth`) or second base fetch would make the proof unresolvable and silently\n disable the carry, so neither is permitted. CCA reads `guardian_comment_carried` as\n-a recorded authority in the deterministic signal and does not re-derive it. See ADR\n-0018 for the full decision, the rejected diff/patch-equality and path-disjointness\n+a recorded authority in the deterministic signal and does not re-derive it. See\n+SADR-0018 for the full decision, the rejected diff/patch-equality and path-disjointness\n alternatives, and the accepted residual.\n \n ## External AI Guardian Review (SADR-0010)\ndiff --git a/docs/agents/implementation-policy.md b/docs/agents/implementation-policy.md\nindex c754144a9fb5bf4a38aa82a60e1670c59e626b44..85c37281bcad196c6b7b02343faf00b03cd9c57a\n--- a/docs/agents/implementation-policy.md\n+++ b/docs/agents/implementation-policy.md\n@@ -31,10 +31,13 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n ## Non-Negotiables\n \n 1. **Planning precedes implementation.** Non-trivial Goals start with `grill-with-docs` and Context & Impact.\n-2. **Domain language is controlled.** Terms must be checked against `CONTEXT.md`; hard-to-reverse tradeoffs must be checked against ADRs.\n+2. **Domain language is controlled.** Terms must be checked against `CONTEXT.md`; hard-to-reverse tradeoffs must be checked against Shiki SADRs and target ADRs.\n 3. **PRD precedes issue decomposition.** `to-prd` is used once the context is settled enough to become durable intent.\n 4. **Issues are vertical slices.** `to-issues` must produce independently grabbable tracer-bullet tasks, not horizontal layer tickets.\n 5. **Triage controls dispatch.** Only `ready-for-agent` / AFK tasks may be assigned to Codex without human input.\n@@ -95,19 +98,19 @@\n Process:\n \n - Challenge terms against `CONTEXT.md`.\n-- Check ADRs before reopening decisions.\n+- Check SADRs and target ADRs before reopening decisions.\n - Inspect code when code can answer a question.\n - Ask one question at a time when the operator is available.\n - Provide a recommended answer with each question.\n - Record resolved terms in `CONTEXT.md`.\n-- Create ADRs only for hard-to-reverse, surprising, tradeoff-based decisions.\n+- Create SADRs or target ADRs only for hard-to-reverse, surprising, tradeoff-based decisions.\n - Convert unresolved load-bearing questions into HITL blockers.\n \n Outputs:\n \n - Resolved design-tree decisions.\n - Updated glossary terms when needed.\n-- ADRs when justified.\n+- SADRs or target ADRs when justified.\n - HITL blockers when decisions remain unresolved.\n \n ## Phase 3 \u2014 Context & Impact\n@@ -117,7 +120,7 @@\n \n Outputs must include:\n \n-- Relevant documents and ADRs.\n+- Relevant documents, SADRs, and target ADRs.\n - Relevant modules, interfaces, seams, callers, and tests.\n - Candidate locks.\n - Dependencies.\n@@ -140,7 +143,7 @@\n - Testing decisions.\n - Out of scope.\n - Further notes.\n-- Links to relevant Goal, domain terms, and ADRs.\n+- Links to relevant Goal, domain terms, SADRs, and target ADRs.\n \n Phase 4 ends with Spec Freeze: the operator explicitly approves the PRD, the\n plan records a `spec_freeze` block (status, approved_by, source), and the SF\n@@ -240,7 +243,7 @@\n - Dependencies and locks.\n - Risk and Guardian requirements.\n - Ledger completeness.\n-- Documentation and ADR updates.\n+- Documentation and SADR/target-ADR updates.\n - Scope drift.\n \n CCA emits one of:\n@@ -301,7 +304,7 @@\n | Evidence failure | Behavior might be done, but proof is missing. | Add checks, PR evidence, ledger entry, or artifacts. |\n | Scope drift | PR changes unrelated behavior. | Revert unrelated changes or split into new Goal. |\n | Dependency block | Upstream task or lock not complete. | Block until dependency resolves. |\n-| Architecture block | Decision needs ADR or deep-module discussion. | `grill-with-docs` or `improve-codebase-architecture`. |\n+| Architecture block | Decision needs a SADR, target ADR, or deep-module discussion. | `grill-with-docs` or `improve-codebase-architecture`. |\n | Risk block | Security/production/policy change needs human approval. | Guardian decision. |\n \n ## Phase 5.5 \u2014 Execution Decision Control\ndiff --git a/docs/agents/runtime-auth-model.md b/docs/agents/runtime-auth-model.md\nindex 6a2e1a47e81ea89b5c68a210ec07772a9374babc..3c54748d24233dbd0ba8473950282547ef4ce935\n--- a/docs/agents/runtime-auth-model.md\n+++ b/docs/agents/runtime-auth-model.md\n@@ -148,7 +148,7 @@\n \n API-key based runners may be added by a target repository as an explicit extension. They are not the default Shiki template path.\n \n-If a repo opts into API-key mode, record the exception in an ADR and update the repo's `AGENTS.md`, `.shiki/config.yaml`, and workflow permissions.\n+If a repo opts into API-key mode, record the target-specific exception in a target ADR and update the repo's `AGENTS.md`, `.shiki/config.yaml`, and workflow permissions.\n \n ## GitHub Actions Runtime Compatibility\n \ndiff --git a/docs/agents/skill-gate.md b/docs/agents/skill-gate.md\nindex c50398b0d7c60a8598f2a5c88a10015730dced13..3acffe6444617bb1d54b77d4b4708158ec735bc6\n--- a/docs/agents/skill-gate.md\n+++ b/docs/agents/skill-gate.md\n@@ -9,7 +9,7 @@\n | Skill | Trigger | Expected output |\n | --- | --- | --- |\n | `setup-matt-pocock-skills` | First configuring a repo for these skills, or when issue tracker, triage labels, or domain docs are missing. | `docs/agents/` configuration and an `Agent skills` block in `AGENTS.md` or `CLAUDE.md`. |\n-| `grill-with-docs` | Ambiguous plan, unclear terminology, boundary decision, tradeoff, or ADR-worthy design choice. | Clarified decision tree, recommended answers, updates to `CONTEXT.md` or ADRs when decisions crystallize. |\n+| `grill-with-docs` | Ambiguous plan, unclear terminology, boundary decision, tradeoff, or decision-record-worthy design choice. | Clarified decision tree, recommended answers, updates to `CONTEXT.md`, SADRs, or target ADRs when decisions crystallize. |\n | `zoom-out` | Agent lacks a higher-level architectural map. | Map of relevant modules, callers, seams, and domain concepts. |\n | `to-prd` | Settled context should become a PRD. | PRD published to the issue tracker with problem, solution, user stories, decisions, testing, and out-of-scope notes. |\n | `to-issues` | Goal, PRD, or plan must become implementation tickets. | Vertical-slice issues with acceptance criteria, dependencies, and AFK/HITL classification. |\n@@ -17,7 +17,7 @@\n | `tdd` | Feature work or bug fix has observable behavior that can be specified with tests. | Red-green-refactor loop through public interfaces, one vertical slice at a time. |\n | `code-review` | The TDD loop is green and the implementer is about to create or update a PR. Default for every implementation task. | Pre-PR self-review of the task diff with fixes applied on the task branch; findings and resolutions recorded as a ledger entry naming the skill and a `## Pre-PR code review` PR body section. |\n | `diagnose` | Hard bug, failing check, regression, flaky behavior, or performance problem. | Reproduction loop, ranked hypotheses, instrumentation, fix, regression test, post-mortem. |\n-| `improve-codebase-architecture` | Structural friction, testability, AI-navigability, deep-module opportunity, or refactor request. | Architecture candidates and deepening recommendations informed by `CONTEXT.md` and ADRs. |\n+| `improve-codebase-architecture` | Structural friction, testability, AI-navigability, deep-module opportunity, or refactor request. | Architecture candidates and deepening recommendations informed by `CONTEXT.md`, SADRs, and target ADRs. |\n | `prototype` | A throwaway logic or UI prototype is needed to answer a design question. | Clearly marked disposable prototype and captured decision. |\n \n ## MergeGate Interaction\ndiff --git a/docs/operations.md b/docs/operations.md\nindex cf1f452d4c589e38ccd58553bda0ad8684b51eb7..ace48a90ffbec9e5c0a7113bef16a4858bc44b3f\n--- a/docs/operations.md\n+++ b/docs/operations.md\n@@ -89,6 +89,16 @@\n      target history, brought forward by `shiki migrate`, not a template.\n    - **Everything else** (CLI scripts, workflows, schemas, docs) is overwritten.\n \n+   A forced upgrade also migrates the former numeric Shiki decision paths to\n+   `SADR-NNNN-*`. Before the first write, the installer inventories the exact\n+   legacy Shiki paths and authorizes each deletion only when the existing\n+   `.shiki/install-stamp.json` names that path and its SHA-256 digest still\n+   matches the file. An absent, unreadable, malformed, incomplete, or\n+   mismatching stamp reports every blocked path and stops with no filesystem\n+   change. Target-owned `NNNN-*` ADRs are not cleanup candidates. A non-force\n+   install deletes nothing and retains any still-matching legacy stamp entries\n+   so a later forced upgrade can prove ownership.\n+\n    If the target has pending migrations, `--force` refuses and writes nothing \u2014\n    apply migrations first (step 3), then re-run. The run ends with a single\n    summary of every `.new` written, so a half-upgrade is visible.\ndiff --git a/docs/releasing.md b/docs/releasing.md\nindex 10d73828e7dbd64e9d9f6b83bc84d010537ce62a..aafeee8c4d441e1e427efee2ce4f81a647f020e1\n--- a/docs/releasing.md\n+++ b/docs/releasing.md\n@@ -67,7 +67,7 @@\n CLI in `scripts/`. There are no third-party Python runtime dependencies. The CLI\n is intentionally **not** exposed as a `[project.scripts]` console entry point\n because `scripts/shiki.py` is a dependency-free shim and `scripts/` is not\n-packaged as an importable distribution (see the ADR for the rationale).\n+packaged as an importable distribution (see SADR-0007 for the rationale).\n \n Supported install paths:\n \n@@ -155,6 +155,15 @@\n    [docs/operations.md](operations.md#the-new-convention). If the target has\n    pending migrations, `--force` refuses and writes nothing; apply migrations\n    (step 2) first, then re-run.\n+\n+   The SADR namespace upgrade has an additional pre-write gate: each former\n+   numeric Shiki decision path is removed only when the existing install stamp\n+   contains that exact path and a digest matching its current bytes. The\n+   installer aggregates absent/unreadable/malformed-stamp, missing-digest, and\n+   digest-mismatch blockers before raising, so refusal leaves the target tree\n+   byte-identical. Numeric target ADRs are preserved. A successful forced\n+   upgrade installs and stamps `SADR-*` paths and drops the removed legacy paths\n+   from the refreshed stamp.\n 2. Apply repository-local `.shiki/` state migrations with the migrate command:\n \n    ```bash\ndiff --git a/docs/usage.md b/docs/usage.md\nindex b83407ee3970dd395dd893010bc5dbe8bad26051..a130ef6e40dfd8169a5bc701339351097df1a09f\n--- a/docs/usage.md\n+++ b/docs/usage.md\n@@ -67,8 +67,10 @@\n shiki install-target /path/to/target-repo --local-only\n ```\n \n-Use `--force` only when you intentionally want to overwrite existing target\n-files.\n+Use `--force` only when you intentionally want to refresh the target template.\n+For the SADR namespace migration, `--force` deletes only exact legacy Shiki\n+decision paths whose existing install-stamp digest still matches; any ownership\n+blocker stops before the first write. Numeric target ADRs are never candidates.\n \n ## `shiki bootstrap-platform` \u2014 publish the Shiki platform repo\n \ndiff --git a/scripts/guardian_approval_signal.py b/scripts/guardian_approval_signal.py\nindex d059f92b2c82aa2b5d6ca421365537a87886c1cb..16924892714f4f5b9303c0ef0e325a7e70075630\n--- a/scripts/guardian_approval_signal.py\n+++ b/scripts/guardian_approval_signal.py\n@@ -199,8 +199,8 @@\n ) -> Any:\n     \"\"\"Evaluate SADR-0015 Contract Approval for the CCA Guardian signal.\n \n-    The SAME OR that MergeGate applies must reach this deterministic signal: ADR\n-    0015 is explicit that without it, a contract-approved PR still returns\n+    The SAME OR that MergeGate applies must reach this deterministic signal:\n+    SADR-0015 is explicit that without it, a contract-approved PR still returns\n     ``needs_guardian``, the CCA job fails, and the MergeGate policy check never\n     runs \u2014 the gate this change moves would never be reached. This resolves the\n     task the SAME way MergeGate does (the first Shiki task id in the PR body),\ndiff --git a/scripts/shiki_contract_approval.py b/scripts/shiki_contract_approval.py\nindex 941bcb96fdd8b9a85bc0c4f0f04d5dad9d04839c..4a4b418f6527413161db82670bec857a043360e2\n--- a/scripts/shiki_contract_approval.py\n+++ b/scripts/shiki_contract_approval.py\n@@ -142,7 +142,7 @@\n     changed_files_status: Any,\n     registration: Any,\n ) -> ContractApprovalResult:\n-    \"\"\"Whether this PR carries an SADR-0015 Contract Approval.\n+    \"\"\"Whether this PR carries a SADR-0015 Contract Approval.\n \n     The four conditions are evaluated IN ORDER, each failure recorded as a\n     distinct reason string:\ndiff --git a/scripts/shiki_guardian.py b/scripts/shiki_guardian.py\nindex a4ecb79bbda55fd5c8b06adfae2eb99760a03c45..4dd99bbe714e26f421fa19abac7a34ae1b195ecc\n--- a/scripts/shiki_guardian.py\n+++ b/scripts/shiki_guardian.py\n@@ -12,7 +12,7 @@\n # The carry re-derives its proof from git in-process on every judging invocation;\n # it is never read from an artifact, so there is nothing to go stale or be forged.\n # shiki_sync_proof is the ONLY guardian-family module that runs git, and every one\n-# of its invocations is read-only and local (ADR: prove-from-git base sync).\n+# of its invocations is read-only and local (SADR: prove-from-git base sync).\n from shiki_sync_proof import verify_pure_base_sync\n \n GUARDIAN_POLICY_PATH = \".shiki/guardian-policy.json\"\ndiff --git a/scripts/shiki_guardian_review.py b/scripts/shiki_guardian_review.py\nindex dd0f104d457899e0ba5ff1e162011c1e90b6f92b..e10ef58c046324e7ea3a3392feb3048ca77414ce\n--- a/scripts/shiki_guardian_review.py\n+++ b/scripts/shiki_guardian_review.py\n@@ -159,7 +159,7 @@\n     docs_only = _is_docs_only(changed_files)\n     if docs_only and matched == 0:\n         focus.append(\n-            \"Docs/ADR-only change: confirm the change is genuinely documentation/decision-only, introduces no behavior or contract drift, and stays consistent with the constitution and existing ADRs.\"\n+            \"Docs/decision-record-only change: confirm the change is genuinely documentation/decision-only, introduces no behavior or contract drift, and stays consistent with the constitution and existing SADRs and target ADRs.\"\n         )\n \n     # Conservative augmentation: unknown (no category and not docs-only), mixed\ndiff --git a/scripts/shiki_installer.py b/scripts/shiki_installer.py\nindex be064ab7a29ff6836f7f7a1d70500a43e0d11d7b..9fd8eefd137ce7003dad5c0dcb2e48e2cd47ab32\n--- a/scripts/shiki_installer.py\n+++ b/scripts/shiki_installer.py\n@@ -118,6 +118,7 @@\n # what lets a target carry it without a governance-schema change.\n INSTALL_STAMP_PATH = \".shiki/install-stamp.json\"\n INSTALL_STAMP_VERSION = 1\n+LEGACY_SHIKI_SADR_MAX = 18\n \n # Under --force the shipped surface splits three ways, so nothing is lost and\n # nothing is silently stale.\n@@ -237,9 +238,98 @@\n         )\n \n \n+def legacy_shiki_adr_paths() -> tuple[str, ...]:\n+    \"\"\"Former numeric paths for Shiki SADR-0001 through SADR-0018.\n+\n+    The authoritative slugs come from the current SADR filenames. SADR-0019 is\n+    the namespace decision itself and never shipped under a numeric legacy path.\n+    \"\"\"\n+    paths: list[str] = []\n+    for sadr in sorted((ROOT / \"docs\" / \"adr\").glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\")):\n+        number = sadr.name.split(\"-\", 2)[1]\n+        if int(number) <= LEGACY_SHIKI_SADR_MAX:\n+            paths.append(f\"docs/adr/{sadr.name.removeprefix('SADR-')}\")\n+    return tuple(paths)\n+\n+\n+def preflight_legacy_sadr_cleanup(target: Path, *, force: bool) -> tuple[str, ...]:\n+    \"\"\"Authorize exact legacy Shiki ADR removals before the first write.\n+\n+    A non-force install never removes existing files. A forced install needs no\n+    stamp when no legacy Shiki path is present. When legacy paths do exist, each\n+    must be named in a readable install stamp and its current bytes must match\n+    the recorded digest.\n+    \"\"\"\n+    if not force:\n+        return ()\n+    present = tuple(\n+        relative\n+        for relative in legacy_shiki_adr_paths()\n+        if (target / relative).exists() or (target / relative).is_symlink()\n+    )\n+    if not present:\n+        return ()\n+\n+    stamp_path = target / INSTALL_STAMP_PATH\n+    try:\n+        raw = stamp_path.read_text(encoding=\"utf-8\")\n+        stamp = json.loads(raw)\n+    except FileNotFoundError:\n+        detail = \"install stamp is absent\"\n+        blockers = [f\"{relative}: {detail}\" for relative in present]\n+    except (OSError, UnicodeDecodeError) as error:\n+        detail = f\"install stamp is unreadable ({error})\"\n+        blockers = [f\"{relative}: {detail}\" for relative in present]\n+    except json.JSONDecodeError as error:\n+        detail = f\"install stamp is malformed ({error})\"\n+        blockers = [f\"{relative}: {detail}\" for relative in present]\n+    else:\n+        if not isinstance(stamp, dict):\n+            blockers = [\n+                f\"{relative}: install stamp is malformed (expected an object)\"\n+                for relative in present\n+            ]\n+        else:\n+            digests = stamp.get(\"digests\")\n+            blockers = []\n+            if not isinstance(digests, dict):\n+                blockers.extend(\n+                    f\"{relative}: install stamp has no digest for this path\"\n+                    for relative in present\n+                )\n+            else:\n+                for relative in present:\n+                    expected = digests.get(relative)\n+                    if not isinstance(expected, str) or not expected:\n+                        blockers.append(\n+                            f\"{relative}: install stamp has no digest for this path\"\n+                        )\n+                        continue\n+                    try:\n+                        actual = _sha256_file(target / relative)\n+                    except OSError as error:\n+                        blockers.append(\n+                            f\"{relative}: legacy path is unreadable ({error})\"\n+                        )\n+                        continue\n+                    if actual != expected:\n+                        blockers.append(\n+                            f\"{relative}: digest mismatch (stamp {expected}, current {actual})\"\n+                        )\n+\n+    if blockers:\n+        joined = \"\\n\".join(f\"- {blocker}\" for blocker in blockers)\n+        raise ShikiError(\n+            \"legacy Shiki ADR cleanup is not authorized:\\n\"\n+            f\"{joined}\\nNo files were written.\"\n+        )\n+    return present\n+\n+\n def install_template(target: Path, *, force: bool, validate: bool) -> list[NewFileNote]:\n     # Preconditions first: never rewrite the tree of a target that cannot be\n     # upgraded cleanly (see refuse_pending_migrations).\n+    legacy_cleanup = preflight_legacy_sadr_cleanup(target, force=force)\n     refuse_pending_migrations(target)\n \n     # Resolve the target's required CODEOWNERS owner once from its\n@@ -250,6 +340,10 @@\n     # verbatim. See ``codeowners_required_owner``.\n     codeowners_owner = codeowners_required_owner(target)\n \n+    for relative in legacy_cleanup:\n+        (target / relative).unlink()\n+        info(f\"removed stamped legacy Shiki ADR: {target / relative}\")\n+\n     notes: list[NewFileNote] = []\n     for relative in TEMPLATE_PATHS:\n         source = ROOT / relative\n@@ -647,6 +741,22 @@\n     digests = target_stamp_digests(target)\n     existing = read_install_stamp(target)\n     if existing is not None:\n+        # A non-force install deliberately leaves legacy Shiki ADRs in place.\n+        # Preserve only still-matching ownership proof so a later forced\n+        # upgrade can authorize cleanup; never carry a missing or stale digest.\n+        existing_digests = existing.get(\"digests\")\n+        if isinstance(existing_digests, dict):\n+            for relative in legacy_shiki_adr_paths():\n+                expected = existing_digests.get(relative)\n+                path = target / relative\n+                if not isinstance(expected, str) or not path.is_file():\n+                    continue\n+                try:\n+                    actual = _sha256_file(path)\n+                except OSError:\n+                    continue\n+                if actual == expected:\n+                    digests[relative] = actual\n         if _incoming_is_older(commit, existing.get(\"platform_commit\")):\n             warn(\n                 \"kept existing install stamp: incoming install \"\ndiff --git a/scripts/shiki_loop.py b/scripts/shiki_loop.py\nindex 3611641da8aeb5038b1da90e3114b09a473ec6d8..84caa491b9cad0edc4176f209c13e9221e975681\n--- a/scripts/shiki_loop.py\n+++ b/scripts/shiki_loop.py\n@@ -424,7 +424,7 @@\n         # unresolvable CCA verdict has already produced its terminal stop and is never\n         # laundered into a head-moving sync (SADR-0010). It also never fires while the\n         # policy (Guardian) gate is red \u2014 for ANY reason, not only when it is the sole\n-        # red check \u2014 so a head-bound Guardian approval, or an SADR-0015\n+        # red check \u2014 so a head-bound Guardian approval, or a SADR-0015\n         # contract-approval carry that base movement defeated (the policy gate compares\n         # base and head task governance directly), is never silently restored by\n         # adopting base wholesale. It is bounded by a durable per-task counter so the\ndiff --git a/scripts/shiki_tasks.py b/scripts/shiki_tasks.py\nindex aec3809f24b4a0c8c96fc50971cff9ae9ad30381..d531052e9d9717058d4631483cefab44c26a65c1\n--- a/scripts/shiki_tasks.py\n+++ b/scripts/shiki_tasks.py\n@@ -821,7 +821,7 @@\n         \"prompt\": prompt,\n         \"entry_skill\": \"grill-with-docs\",\n         \"required_next_steps\": [\n-            \"Run grill-with-docs until terminology, boundaries, risks, and ADR-worthy decisions are settled.\",\n+            \"Run grill-with-docs until terminology, boundaries, risks, and decision-record-worthy choices are settled.\",\n             \"Obtain the operator's explicit approval of the PRD/requirements (Spec Freeze).\",\n             \"Write a machine-readable plan JSON with grill_with_docs.status=complete and spec_freeze.status=frozen.\",\n             \"Run shiki plan ingest --plan-file PLAN.json.\",\ndiff --git a/scripts/test_shiki_goal_loop.sh b/scripts/test_shiki_goal_loop.sh\nindex 753c7b516e10f0be3bc92a4bd0eb1fa699776ae6..8f3820a0a079b3044424cfe1a6c6b7fe68896e67\n--- a/scripts/test_shiki_goal_loop.sh\n+++ b/scripts/test_shiki_goal_loop.sh\n@@ -231,8 +231,8 @@\n \n # High/critical risk merges autonomously when all required checks are green:\n # the \"MergeGate policy check\" required check IS the Guardian gate (it enforces\n-# guardian-policy.json \u2014 human approval OR external AI guardian review, ADR\n-# 0010), so green checks mean a recorded authority approved. The fake gh returns\n+# guardian-policy.json \u2014 human approval OR external AI guardian review,\n+# SADR-0010), so green checks mean a recorded authority approved. The fake gh returns\n # green checks, simulating that the Guardian gate passed.\n python3 \"$ROOT/scripts/shiki.py\" goal create --target \"$TARGET\" --title \"Guardian gate\" --outcome \"High risk merges via policy gate\" >/tmp/shiki-goal-loop-guardian-goal.json\n GGOAL=\"$(json_get /tmp/shiki-goal-loop-guardian-goal.json goal_id)\"\ndiff --git a/tests/test_adr_references.py b/tests/test_adr_references.py\nindex 51706329c797d5dd9b47e082a333fa83b261d482..d005da4f771f5e8085ec22d4b787980e68f5ae8c\n--- a/tests/test_adr_references.py\n+++ b/tests/test_adr_references.py\n@@ -1,8 +1,8 @@\n-\"\"\"Every ``ADR ####`` citation must resolve to exactly one decision record.\n+\"\"\"Every target ADR and Shiki SADR citation resolves in its own namespace.\n \n Background. ``scripts/mergegate_check.py`` justifies ``is_bookkeeping_closeout``\n-with \"ADR 0017\" nine times and ``scripts/guardian_approval_signal.py`` cites\n-0016/0017 seven times, yet for a stretch the numbering in ``docs/adr/`` stopped\n+with \"SADR-0017\" nine times and ``scripts/guardian_approval_signal.py`` cites\n+SADR-0016/SADR-0017 seven times, yet for a stretch the numbering in ``docs/adr/`` stopped\n at 0015: the authoring commits lived on a coordinator branch and no task ever\n declared ``docs/adr/...`` in its locks, so nothing carried the records to the\n default branch. The stated basis of an exemption that merged seven PRs with zero\n@@ -11,11 +11,12 @@\n and the directory drifted with no signal.\n \n This is the same unbound-pair shape behind other silent failures: a reference\n-and its target with nothing asserting they agree. This test binds them. It scans\n-every ``scripts/*.py`` (plus ``docs/**/*.md`` and the root ``*.md`` governance\n-files, which are cheap) for the ``ADR ####`` pattern and asserts each cited\n-number resolves to exactly one ``docs/adr/####-*.md``. The converse is\n-deliberately NOT required: an ADR may exist without being cited.\n+and its target with nothing asserting they agree. This test binds them. Target\n+``ADR NNNN`` citations resolve only to ``docs/adr/NNNN-*.md``; Shiki\n+``SADR-NNNN`` citations resolve only to ``docs/adr/SADR-NNNN-*.md``. The scan\n+covers current governance docs, scripts, workflows, prompts, packaging metadata,\n+and repo-local Codex guidance while deliberately excluding historical\n+``.shiki`` evidence. The converse is not required: a record may be uncited.\n \n The suite is dependency free: standard-library ``unittest`` only.\n \"\"\"\n@@ -31,17 +32,18 @@\n \n REPO_ROOT = Path(__file__).resolve().parents[1]\n \n-# \"ADR 0016\" -- a four-digit citation. The trailing \\b keeps a five-digit token\n-# from being read as a truncated four-digit number.\n-_CITATION = re.compile(r\"\\bADR (\\d{4})\\b\")\n-# \"0016-locks-bound-product-paths.md\" -- the leading four digits name the record.\n-_ADR_FILENAME = re.compile(r\"^(\\d{4})-.*\\.md$\")\n+# ``ADR 0016`` / ``SADR-0016`` -- a namespace plus four-digit citation. The\n+# trailing ``\\b`` keeps a five-digit token from being read as a truncated one.\n+_CITATION = re.compile(r\"\\b(S?ADR)[ -](\\d{4})\\b\")\n+# Target ADRs retain the numeric filename; Shiki platform decisions use SADR.\n+_DECISION_FILENAME = re.compile(r\"^(?:(SADR)-)?(\\d{4})-.*\\.md$\")\n \n \n class Violation(NamedTuple):\n     \"\"\"A citation that does not resolve to exactly one decision record.\"\"\"\n \n     citing_file: str  # repo-relative path of the file that cites the ADR\n+    namespace: str  # \"ADR\" for target decisions; \"SADR\" for Shiki decisions\n     number: str  # the cited four-digit number, e.g. \"0016\"\n     line: int  # 1-based line of the citation\n     reason: str  # \"missing\" (zero matches) or \"ambiguous\" (two or more)\n@@ -49,44 +51,59 @@\n \n     @property\n     def expected_path(self) -> str:\n-        return f\"docs/adr/{self.number}-*.md\"\n+        prefix = \"SADR-\" if self.namespace == \"SADR\" else \"\"\n+        return f\"docs/adr/{prefix}{self.number}-*.md\"\n \n     def describe(self) -> str:\n         \"\"\"Name the citing file, the cited number, and the missing path.\"\"\"\n         if self.reason == \"missing\":\n             return (\n-                f\"{self.citing_file}:{self.line} cites ADR {self.number} \"\n+                f\"{self.citing_file}:{self.line} cites {self.namespace} {self.number} \"\n                 f\"but no document matches {self.expected_path}\"\n             )\n         joined = \", \".join(self.matches)\n         return (\n-            f\"{self.citing_file}:{self.line} cites ADR {self.number} which \"\n+            f\"{self.citing_file}:{self.line} cites {self.namespace} {self.number} which \"\n             f\"resolves to {len(self.matches)} documents (expected exactly one \"\n             f\"at {self.expected_path}): {joined}\"\n         )\n \n \n-def _adr_index(root: Path) -> dict[str, list[Path]]:\n-    \"\"\"Map every four-digit ADR number to the record files that claim it.\"\"\"\n-    index: dict[str, list[Path]] = {}\n+def _adr_index(root: Path) -> dict[tuple[str, str], list[Path]]:\n+    \"\"\"Map each decision namespace and number to the files that claim it.\"\"\"\n+    index: dict[tuple[str, str], list[Path]] = {}\n     adr_dir = root / \"docs\" / \"adr\"\n     if adr_dir.is_dir():\n         for path in sorted(adr_dir.glob(\"*.md\")):\n-            match = _ADR_FILENAME.match(path.name)\n+            match = _DECISION_FILENAME.match(path.name)\n             if match:\n-                index.setdefault(match.group(1), []).append(path)\n+                namespace = \"SADR\" if match.group(1) else \"ADR\"\n+                index.setdefault((namespace, match.group(2)), []).append(path)\n     return index\n \n \n def _citing_files(root: Path, include_docs: bool = True) -> list[Path]:\n-    \"\"\"Files scanned for citations: scripts/*.py, and cheaply the markdown.\"\"\"\n+    \"\"\"Current, shipped text surfaces scanned for decision citations.\"\"\"\n     files = sorted((root / \"scripts\").glob(\"*.py\"))\n+    files += sorted((root / \"scripts\").glob(\"*.sh\"))\n     if include_docs:\n         docs = root / \"docs\"\n         if docs.is_dir():\n             files += sorted(docs.rglob(\"*.md\"))\n         files += sorted(root.glob(\"*.md\"))\n-    return files\n+        pyproject = root / \"pyproject.toml\"\n+        if pyproject.is_file():\n+            files.append(pyproject)\n+        for relative in (\".github\", \".codex\"):\n+            surface = root / relative\n+            if surface.is_dir():\n+                files += sorted(\n+                    path\n+                    for path in surface.rglob(\"*\")\n+                    if path.is_file()\n+                    and path.suffix in {\".md\", \".py\", \".sh\", \".toml\", \".yaml\", \".yml\"}\n+                )\n+    return sorted(set(files))\n \n \n def find_adr_reference_violations(\n@@ -94,10 +111,9 @@\n ) -> list[Violation]:\n     \"\"\"Return every ``ADR ####`` citation under ``root`` that fails to resolve.\n \n-    A citation resolves when exactly one ``docs/adr/####-*.md`` matches its\n-    number. Zero matches is a \"missing\" violation; two or more is \"ambiguous\".\n-    Records that are never cited produce nothing -- the converse is not\n-    required.\n+    A citation resolves when exactly one record in the same namespace matches\n+    its number. Zero matches is a \"missing\" violation; two or more is\n+    \"ambiguous\". Records that are never cited produce nothing.\n     \"\"\"\n     root = Path(root)\n     index = _adr_index(root)\n@@ -110,13 +126,15 @@\n         rel = path.relative_to(root).as_posix()\n         for lineno, line in enumerate(text.splitlines(), start=1):\n             for match in _CITATION.finditer(line):\n-                number = match.group(1)\n-                found = index.get(number, [])\n+                namespace, number = match.groups()\n+                found = index.get((namespace, number), [])\n                 if len(found) == 1:\n                     continue\n                 reason = \"missing\" if not found else \"ambiguous\"\n                 rels = [p.relative_to(root).as_posix() for p in found]\n-                violations.append(Violation(rel, number, lineno, reason, rels))\n+                violations.append(\n+                    Violation(rel, namespace, number, lineno, reason, rels)\n+                )\n     return violations\n \n \n@@ -129,8 +147,8 @@\n         self.assertEqual(\n             violations,\n             [],\n-            \"Every 'ADR ####' citation must resolve to exactly one \"\n-            f\"docs/adr/####-*.md:\\n{detail}\",\n+            \"Every ADR/SADR citation must resolve exactly once in its own \"\n+            f\"docs/adr namespace:\\n{detail}\",\n         )\n \n     def test_scan_actually_reads_scripts(self) -> None:\n@@ -142,12 +160,38 @@\n             \"the citation scan must cover scripts/*.py\",\n         )\n \n+    def test_platform_citations_do_not_use_unresolved_numeric_shorthand(self) -> None:\n+        shorthand = re.compile(r\"SADR-\\d{4}/\\d{4}\")\n+        offenders = []\n+        for path in _citing_files(REPO_ROOT):\n+            text = path.read_text(encoding=\"utf-8\")\n+            if shorthand.search(text):\n+                offenders.append(path.relative_to(REPO_ROOT).as_posix())\n+        self.assertEqual(\n+            offenders,\n+            [],\n+            \"repeat the SADR prefix so every cited number is independently resolvable\",\n+        )\n+\n     def test_carried_records_are_indexed(self) -> None:\n         # The two records this Goal carried onto the default branch, and which\n         # the scripts cite most, must be present.\n         index = _adr_index(REPO_ROOT)\n-        self.assertIn(\"0016\", index)\n-        self.assertIn(\"0017\", index)\n+        self.assertIn((\"SADR\", \"0016\"), index)\n+        self.assertIn((\"SADR\", \"0017\"), index)\n+\n+    def test_platform_tree_has_only_sadr_0001_through_0019(self) -> None:\n+        adr_dir = REPO_ROOT / \"docs\" / \"adr\"\n+        sadrs = sorted(adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\"))\n+        self.assertEqual(\n+            {path.name.split(\"-\", 2)[1] for path in sadrs},\n+            {f\"{number:04d}\" for number in range(1, 20)},\n+        )\n+        self.assertEqual(\n+            list(adr_dir.glob(\"[0-9][0-9][0-9][0-9]-*.md\")),\n+            [],\n+            \"the Shiki platform tree must not retain legacy numeric SADR paths\",\n+        )\n \n \n class FixtureAdrReferences(unittest.TestCase):\n@@ -180,6 +224,26 @@\n         self.assertIn(\"9991\", message)  # cited number\n         self.assertIn(\"docs/adr/9991-*.md\", message)  # missing path\n \n+    def test_target_adr_does_not_satisfy_missing_platform_sadr(self) -> None:\n+        root = self._make_repo(\n+            adr_names=[\"0042-target-decision.md\"],\n+            script_body='\"\"\"Target ADR 0042 exists, but platform SADR-0042 does not.\"\"\"\\n',\n+        )\n+        violations = find_adr_reference_violations(root, include_docs=False)\n+        self.assertEqual(len(violations), 1)\n+        self.assertEqual(violations[0].namespace, \"SADR\")\n+        self.assertEqual(violations[0].number, \"0042\")\n+        self.assertEqual(violations[0].expected_path, \"docs/adr/SADR-0042-*.md\")\n+\n+    def test_target_adr_and_platform_sadr_may_reuse_a_number(self) -> None:\n+        root = self._make_repo(\n+            adr_names=[\"0042-target-decision.md\", \"SADR-0042-platform-decision.md\"],\n+            script_body='\"\"\"Target ADR 0042 and platform SADR-0042 are distinct.\"\"\"\\n',\n+        )\n+        self.assertEqual(\n+            find_adr_reference_violations(root, include_docs=False), []\n+        )\n+\n     def test_uncited_adr_is_allowed(self) -> None:\n         # An ADR exists but nothing cites it -> not a violation. The converse\n         # (every ADR must be cited) is explicitly NOT required.\ndiff --git a/tests/test_guardian_review.py b/tests/test_guardian_review.py\nindex 88829e7810212635e2fc98aa5918d6f26294677d..00a85a237acd5bf2d6cb5b294128198c422402ca\n--- a/tests/test_guardian_review.py\n+++ b/tests/test_guardian_review.py\n@@ -162,7 +162,7 @@\n \n     def test_docs_only_is_light_and_not_conservative(self):\n         focus = gr.classify_review_focus([\"docs/adr/0099-x.md\", \"README.md\"], \"\", \"low\")\n-        self.assertTrue(any(\"Docs/ADR-only\" in f for f in focus))\n+        self.assertTrue(any(\"Docs/decision-record-only\" in f for f in focus))\n         self.assertFalse(any(\"conservative default\" in f.lower() for f in focus))\n \n     def test_unknown_category_is_conservative(self):\ndiff --git a/tests/test_policy_job_merged_prs.py b/tests/test_policy_job_merged_prs.py\nindex d45512e74681e5e649279236c7d4e79d5c3da471..b97989f8bd74d091de1a6b5275a20d8dc68c4512\n--- a/tests/test_policy_job_merged_prs.py\n+++ b/tests/test_policy_job_merged_prs.py\n@@ -3,8 +3,8 @@\n \n ``.github/workflows/shiki-cca-completion.yml`` has two consumers of the merged-PR\n proof: the CCA *signal* (``guardian_approval_signal.py`` in \"Prepare CCA context\")\n-and the authoritative gate (``mergegate_check.py`` in \"Enforce MergeGate\"). ADR\n-0017's bookkeeping-closeout exemption (condition 6) needs the merged implementation\n+and the authoritative gate (``mergegate_check.py`` in \"Enforce MergeGate\").\n+SADR-0017's bookkeeping-closeout exemption (condition 6) needs the merged implementation\n PR proven, and a closeout carries NO ``post_merge_reconcile`` marker. The signal\n already resolves ``MERGED_PRS`` unconditionally, but \"Enforce MergeGate\" used to\n resolve it only inside ``grep -q '<!-- shiki:post_merge_reconcile -->'``. So a\ndiff --git a/tests/test_sadr_install_migration.py b/tests/test_sadr_install_migration.py\nnew file mode 100644\nindex 0000000000000000000000000000000000000000..a6f4c57e4dcaef73ef600b0d4804744138ab9071\n--- /dev/null\n+++ b/tests/test_sadr_install_migration.py\n@@ -0,0 +1,229 @@\n+\"\"\"SADR installation and collision-safe legacy upgrade behavior.\"\"\"\n+\n+from __future__ import annotations\n+\n+import contextlib\n+import hashlib\n+import io\n+import json\n+import shutil\n+import tempfile\n+import unittest\n+from pathlib import Path\n+\n+import shiki_test_support  # noqa: F401  (path bootstrap)\n+\n+import shiki_installer\n+from tests.test_adr_references import find_adr_reference_violations\n+\n+\n+PRODUCT_NUMBERS = tuple(range(13, 19))\n+\n+\n+def _quiet_install(target: Path, *, force: bool, validate: bool) -> None:\n+    with contextlib.redirect_stdout(io.StringIO()):\n+        shiki_installer.install_template(target, force=force, validate=validate)\n+\n+\n+def _sha256(path: Path) -> str:\n+    return hashlib.sha256(path.read_bytes()).hexdigest()\n+\n+\n+def _legacy_sadr_paths() -> tuple[str, ...]:\n+    return tuple(\n+        f\"docs/adr/{path.name.removeprefix('SADR-')}\"\n+        for path in sorted((shiki_installer.ROOT / \"docs\" / \"adr\").glob(\"SADR-*.md\"))\n+        if path.name != \"SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\"\n+    )\n+\n+\n+def _create_legacy_upgrade_fixture(\n+    target: Path,\n+) -> tuple[dict[str, bytes], tuple[str, ...]]:\n+    adr_dir = target / \"docs\" / \"adr\"\n+    adr_dir.mkdir(parents=True)\n+    product_bytes = {\n+        f\"{number:04d}-cubeshare-product-decision.md\": (\n+            f\"# CubeShare ADR {number:04d}\\n\\nDo not rewrite {number}.\\n\"\n+        ).encode()\n+        for number in PRODUCT_NUMBERS\n+    }\n+    for name, content in product_bytes.items():\n+        (adr_dir / name).write_bytes(content)\n+\n+    legacy_paths = _legacy_sadr_paths()\n+    for relative in legacy_paths:\n+        (target / relative).write_bytes(f\"legacy Shiki bytes for {relative}\\n\".encode())\n+\n+    stamp_path = target / shiki_installer.INSTALL_STAMP_PATH\n+    stamp_path.parent.mkdir(parents=True, exist_ok=True)\n+    stamp = {\n+        \"version\": shiki_installer.INSTALL_STAMP_VERSION,\n+        \"platform_commit\": None,\n+        \"installed_at\": \"2000-01-01T00:00:00+00:00\",\n+        \"digests\": {\n+            relative: _sha256(target / relative) for relative in legacy_paths\n+        },\n+    }\n+    stamp_path.write_text(\n+        json.dumps(stamp, indent=2, sort_keys=True) + \"\\n\", encoding=\"utf-8\"\n+    )\n+    return product_bytes, legacy_paths\n+\n+\n+def _tree_snapshot(root: Path) -> dict[str, tuple[str, bytes | str]]:\n+    \"\"\"Recursive byte/shape snapshot used to prove preflight is write-free.\"\"\"\n+    snapshot: dict[str, tuple[str, bytes | str]] = {}\n+    for path in sorted(root.rglob(\"*\")):\n+        relative = path.relative_to(root).as_posix()\n+        if path.is_symlink():\n+            snapshot[relative] = (\"symlink\", path.readlink().as_posix())\n+        elif path.is_dir():\n+            snapshot[relative] = (\"directory\", \"\")\n+        else:\n+            snapshot[relative] = (\"file\", path.read_bytes())\n+    return snapshot\n+\n+\n+class FreshSadrInstallTests(unittest.TestCase):\n+    def setUp(self) -> None:\n+        self.tmp = Path(tempfile.mkdtemp(prefix=\"shiki-sadr-fresh-\"))\n+        self.addCleanup(lambda: shutil.rmtree(self.tmp, ignore_errors=True))\n+        self.target = self.tmp / \"target\"\n+        self.adr_dir = self.target / \"docs\" / \"adr\"\n+        self.adr_dir.mkdir(parents=True)\n+        self.product_bytes = {\n+            f\"{number:04d}-product-decision.md\": (\n+                f\"# Product ADR {number:04d}\\n\\nTarget-owned bytes {number}.\\n\"\n+            ).encode()\n+            for number in PRODUCT_NUMBERS\n+        }\n+        for name, content in self.product_bytes.items():\n+            (self.adr_dir / name).write_bytes(content)\n+\n+    def test_fresh_install_preserves_product_adrs_and_installs_all_sadrs(self) -> None:\n+        _quiet_install(self.target, force=False, validate=True)\n+\n+        for name, expected in self.product_bytes.items():\n+            self.assertEqual((self.adr_dir / name).read_bytes(), expected)\n+\n+        sadr_numbers = {\n+            path.name.split(\"-\", 2)[1]\n+            for path in self.adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\")\n+        }\n+        self.assertEqual(sadr_numbers, {f\"{number:04d}\" for number in range(1, 20)})\n+\n+        numeric_records = {\n+            path.name\n+            for path in self.adr_dir.glob(\"[0-9][0-9][0-9][0-9]-*.md\")\n+        }\n+        self.assertEqual(numeric_records, set(self.product_bytes))\n+        self.assertEqual(find_adr_reference_violations(self.target), [])\n+\n+\n+class LegacySadrUpgradeTests(unittest.TestCase):\n+    def setUp(self) -> None:\n+        self.tmp = Path(tempfile.mkdtemp(prefix=\"shiki-sadr-upgrade-\"))\n+        self.addCleanup(lambda: shutil.rmtree(self.tmp, ignore_errors=True))\n+        self.target = self.tmp / \"target\"\n+        self.adr_dir = self.target / \"docs\" / \"adr\"\n+        self.product_bytes, self.legacy_paths = _create_legacy_upgrade_fixture(\n+            self.target\n+        )\n+\n+    def test_force_upgrade_deletes_only_stamped_legacy_sadrs(self) -> None:\n+        _quiet_install(self.target, force=True, validate=True)\n+\n+        for relative in self.legacy_paths:\n+            self.assertFalse((self.target / relative).exists(), relative)\n+        for name, expected in self.product_bytes.items():\n+            self.assertEqual((self.adr_dir / name).read_bytes(), expected)\n+\n+        sadrs = list(self.adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\"))\n+        self.assertEqual(len(sadrs), 19)\n+        refreshed = json.loads(\n+            (self.target / shiki_installer.INSTALL_STAMP_PATH).read_text(encoding=\"utf-8\")\n+        )\n+        for relative in self.legacy_paths:\n+            self.assertNotIn(relative, refreshed[\"digests\"])\n+        for path in sadrs:\n+            relative = path.relative_to(self.target).as_posix()\n+            self.assertEqual(refreshed[\"digests\"][relative], _sha256(path))\n+\n+    def test_non_force_install_never_deletes_legacy_files(self) -> None:\n+        before = {\n+            relative: (self.target / relative).read_bytes()\n+            for relative in self.legacy_paths\n+        }\n+        _quiet_install(self.target, force=False, validate=False)\n+        for relative, expected in before.items():\n+            self.assertEqual((self.target / relative).read_bytes(), expected)\n+        refreshed = json.loads(\n+            (self.target / shiki_installer.INSTALL_STAMP_PATH).read_text(encoding=\"utf-8\")\n+        )\n+        for relative in self.legacy_paths:\n+            self.assertEqual(refreshed[\"digests\"][relative], _sha256(self.target / relative))\n+\n+\n+class LegacyCleanupPreflightTests(unittest.TestCase):\n+    def setUp(self) -> None:\n+        self.tmp = Path(tempfile.mkdtemp(prefix=\"shiki-sadr-preflight-\"))\n+        self.addCleanup(lambda: shutil.rmtree(self.tmp, ignore_errors=True))\n+\n+    def test_all_ownership_blockers_abort_before_any_write(self) -> None:\n+        cases = (\n+            (\"absent\", \"install stamp is absent\"),\n+            (\"unreadable\", \"install stamp is unreadable\"),\n+            (\"malformed\", \"install stamp is malformed\"),\n+            (\"missing-digest\", \"install stamp has no digest\"),\n+            (\"digest-mismatch\", \"digest mismatch\"),\n+        )\n+        for name, expected_reason in cases:\n+            with self.subTest(name=name):\n+                target = self.tmp / name\n+                _, legacy_paths = _create_legacy_upgrade_fixture(target)\n+                stamp_path = target / shiki_installer.INSTALL_STAMP_PATH\n+\n+                if name == \"absent\":\n+                    stamp_path.unlink()\n+                elif name == \"unreadable\":\n+                    stamp_path.unlink()\n+                    stamp_path.mkdir()\n+                elif name == \"malformed\":\n+                    stamp_path.write_text(\"{not-json\\n\", encoding=\"utf-8\")\n+                else:\n+                    stamp = json.loads(stamp_path.read_text(encoding=\"utf-8\"))\n+                    if name == \"missing-digest\":\n+                        stamp[\"digests\"] = {}\n+                    else:\n+                        stamp[\"digests\"] = {\n+                            relative: \"deadbeef\" for relative in legacy_paths\n+                        }\n+                    stamp_path.write_text(\n+                        json.dumps(stamp, indent=2, sort_keys=True) + \"\\n\",\n+                        encoding=\"utf-8\",\n+                    )\n+\n+                before = _tree_snapshot(target)\n+                with self.assertRaises(shiki_installer.ShikiError) as raised:\n+                    _quiet_install(target, force=True, validate=False)\n+                after = _tree_snapshot(target)\n+\n+                self.assertEqual(before, after, f\"{name} preflight changed the tree\")\n+                message = str(raised.exception)\n+                self.assertIn(expected_reason, message)\n+                self.assertIn(\"No files were written\", message)\n+                for relative in legacy_paths:\n+                    self.assertIn(relative, message)\n+\n+    def test_absent_stamp_is_allowed_when_no_legacy_path_exists(self) -> None:\n+        target = self.tmp / \"no-legacy\"\n+        target.mkdir()\n+        _quiet_install(target, force=True, validate=False)\n+        self.assertTrue(\n+            (target / \"docs\" / \"adr\" / \"SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\").is_file()\n+        )\n+\n+\n+if __name__ == \"__main__\":\n+    unittest.main()\n\n2026-08-07T08:22:38.321380Z ERROR codex_models_manager::manager: failed to renew cache TTL: missing field `base_instructions` at line 94 column 5\nexec\n/bin/zsh -lc 'find docs/adr -maxdepth 1 -type f -print | sort' in /private/tmp/shiki-adr-collision.pa0KwY/.worktrees/shiki-t-20260807t073307221254z-2d22d654-introduc\n succeeded in 0ms:\ndocs/adr/README.md\ndocs/adr/SADR-0001-keep-shiki-as-platform-name.md\ndocs/adr/SADR-0002-use-agents-as-shared-constitution.md\ndocs/adr/SADR-0003-require-skill-gate-for-mergegate-readiness.md\ndocs/adr/SADR-0004-use-github-first-with-shiki-mirror.md\ndocs/adr/SADR-0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\ndocs/adr/SADR-0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\ndocs/adr/SADR-0007-packaging-and-release.md\ndocs/adr/SADR-0008-use-claude-code-as-default-implementer.md\ndocs/adr/SADR-0009-requirements-definition-and-spec-freeze.md\ndocs/adr/SADR-0010-external-ai-guardian-review.md\ndocs/adr/SADR-0011-autonomous-quality-gate-evidence.md\ndocs/adr/SADR-0012-autonomous-loop-closeout-to-main.md\ndocs/adr/SADR-0013-bootstrap-grants-actions-approve-pull-requests.md\ndocs/adr/SADR-0014-codex-app-external-ai-guardian-ui-adapter.md\ndocs/adr/SADR-0015-contract-approval-before-dispatch.md\ndocs/adr/SADR-0016-locks-bound-product-paths.md\ndocs/adr/SADR-0017-bookkeeping-closeouts-do-not-inherit-risk.md\ndocs/adr/SADR-0018-carried-guardian-approval-across-a-proven-base-sync.md\ndocs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\ndocs/adr/template.md\n\nexec\n/bin/zsh -lc 'git status --short' in /private/tmp/shiki-adr-collision.pa0KwY/.worktrees/shiki-t-20260807t073307221254z-2d22d654-introduc\n succeeded in 0ms:\n M .codex/skills/shiki/SKILL.md\n M .github/prompts/cca-completion-check.md\n M .github/prompts/codex-task-handoff.md\n M .github/workflows/release.yml\n M .github/workflows/shiki-cca-completion.yml\n M .github/workflows/shiki-mergegate.yml\n M .shiki/tasks/T-20260807T073307221254Z-2d22d654.json\n M AGENTS.md\n M CLAUDE.md\n M CONTEXT.md\n M CONTRIBUTING.md\n M SYSTEM_PROMPT.md\n D docs/adr/0001-keep-shiki-as-platform-name.md\n D docs/adr/0002-use-agents-as-shared-constitution.md\n D docs/adr/0003-require-skill-gate-for-mergegate-readiness.md\n D docs/adr/0004-use-github-first-with-shiki-mirror.md\n D docs/adr/0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\n D docs/adr/0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\n D docs/adr/0007-packaging-and-release.md\n D docs/adr/0008-use-claude-code-as-default-implementer.md\n D docs/adr/0009-requirements-definition-and-spec-freeze.md\n D docs/adr/0010-external-ai-guardian-review.md\n D docs/adr/0011-autonomous-quality-gate-evidence.md\n D docs/adr/0012-autonomous-loop-closeout-to-main.md\n D docs/adr/0013-bootstrap-grants-actions-approve-pull-requests.md\n D docs/adr/0014-codex-app-external-ai-guardian-ui-adapter.md\n D docs/adr/0015-contract-approval-before-dispatch.md\n D docs/adr/0016-locks-bound-product-paths.md\n D docs/adr/0017-bookkeeping-closeouts-do-not-inherit-risk.md\n D docs/adr/0018-carried-guardian-approval-across-a-proven-base-sync.md\n M docs/adr/README.md\n M docs/adr/template.md\n M docs/agents/autonomous-loop-e2e-acceptance.md\n M docs/agents/bootstrap-command.md\n M docs/agents/checklists.md\n M docs/agents/codex-handoff.md\n M docs/agents/completion-check-agent.md\n M docs/agents/decision-control.md\n M docs/agents/domain.md\n M docs/agents/execution-control.md\n M docs/agents/guardian-policy.md\n M docs/agents/implementation-policy.md\n M docs/agents/runtime-auth-model.md\n M docs/agents/runtime-registry.md\n M docs/agents/shiki-cli-architecture.md\n M docs/agents/skill-gate.md\n M docs/agents/triage-labels.md\n M docs/operations.md\n M docs/proposals/0001-memory-loop-spec-freeze-review.md\n M docs/proposals/0002-autonomous-loop-completion.md\n M docs/releasing.md\n M docs/usage.md\n M pyproject.toml\n M scripts/guardian_approval_signal.py\n M scripts/mergegate_check.py\n M scripts/shiki_bootstrap.py\n M scripts/shiki_cli.py\n M scripts/shiki_contract_approval.py\n M scripts/shiki_github.py\n M scripts/shiki_guardian.py\n M scripts/shiki_guardian_review.py\n M scripts/shiki_installer.py\n M scripts/shiki_loop.py\n M scripts/shiki_migrations.py\n M scripts/shiki_runtime_adapters.py\n M scripts/shiki_runtime_registry.py\n M scripts/shiki_tasks.py\n M scripts/test_shiki_adversarial_state.sh\n M scripts/test_shiki_code_review_gate.sh\n M scripts/test_shiki_control_plane.sh\n M scripts/test_shiki_goal_loop.sh\n M scripts/test_shiki_governance_evidence.sh\n M scripts/test_shiki_guardian_policy.sh\n M scripts/test_shiki_loop_e2e.sh\n M scripts/test_shiki_loop_lock_guard.sh\n M scripts/test_shiki_run_orchestrator.sh\n M scripts/test_shiki_runner_claude.sh\n M scripts/test_shiki_runtime_registry.sh\n M scripts/test_shiki_start.sh\n M scripts/validate_shiki.py\n M tests/test_adr_references.py\n M tests/test_bookkeeping_closeout.py\n M tests/test_branch_behind_sync.py\n M tests/test_cca_evidence_manifest_taskless.py\n M tests/test_closeout.py\n M tests/test_completion_lock_scope.py\n M tests/test_contract_amendment.py\n M tests/test_contract_approval.py\n M tests/test_contract_immutability.py\n M tests/test_contract_mode.py\n M tests/test_contract_open.py\n M tests/test_contract_plan_registration.py\n M tests/test_dispatch_lock_precondition.py\n M tests/test_guardian_review.py\n M tests/test_guardian_signal.py\n M tests/test_guardian_signal_closeout.py\n M tests/test_loop_branch_plumbing.py\n M tests/test_loop_code_review.py\n M tests/test_loop_e2e_contract.py\n M tests/test_loop_engine.py\n M tests/test_loop_lock_validator_hint.py\n M tests/test_loop_tdd_evidence.py\n M tests/test_mirror_scope.py\n M tests/test_paired_invariants.py\n M tests/test_policy_job_merged_prs.py\n M tests/test_repair_delivery.py\n M tests/test_runner_adapters.py\n M tests/test_workflow_invocation.py\n M tests/test_workflow_invocation_carry.py\n?? .shiki/locks/T-20260807T073307221254Z-2d22d654.json\n?? docs/adr/SADR-0001-keep-shiki-as-platform-name.md\n?? docs/adr/SADR-0002-use-agents-as-shared-constitution.md\n?? docs/adr/SADR-0003-require-skill-gate-for-mergegate-readiness.md\n?? docs/adr/SADR-0004-use-github-first-with-shiki-mirror.md\n?? docs/adr/SADR-0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\n?? docs/adr/SADR-0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\n?? docs/adr/SADR-0007-packaging-and-release.md\n?? docs/adr/SADR-0008-use-claude-code-as-default-implementer.md\n?? docs/adr/SADR-0009-requirements-definition-and-spec-freeze.md\n?? docs/adr/SADR-0010-external-ai-guardian-review.md\n?? docs/adr/SADR-0011-autonomous-quality-gate-evidence.md\n?? docs/adr/SADR-0012-autonomous-loop-closeout-to-main.md\n?? docs/adr/SADR-0013-bootstrap-grants-actions-approve-pull-requests.md\n?? docs/adr/SADR-0014-codex-app-external-ai-guardian-ui-adapter.md\n?? docs/adr/SADR-0015-contract-approval-before-dispatch.md\n?? docs/adr/SADR-0016-locks-bound-product-paths.md\n?? docs/adr/SADR-0017-bookkeeping-closeouts-do-not-inherit-risk.md\n?? docs/adr/SADR-0018-carried-guardian-approval-across-a-proven-base-sync.md\n?? docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\n?? tests/test_sadr_install_migration.py\n\nexec\n/bin/zsh -lc 'git diff --stat' in /private/tmp/shiki-adr-collision.pa0KwY/.worktrees/shiki-t-20260807t073307221254z-2d22d654-introduc\nexec\n/bin/zsh -lc 'git diff --check' in /private/tmp/shiki-adr-collision.pa0KwY/.worktrees/shiki-t-20260807t073307221254z-2d22d654-introduc\n succeeded in 0ms:\n succeeded in 0ms:\n .codex/skills/shiki/SKILL.md                       |   5 +-\n .github/prompts/cca-completion-check.md            |   5 +-\n .github/prompts/codex-task-handoff.md              |   2 +-\n .github/workflows/release.yml                      |   2 +-\n .github/workflows/shiki-cca-completion.yml         |  24 +--\n .github/workflows/shiki-mergegate.yml              |   8 +-\n .../tasks/T-20260807T073307221254Z-2d22d654.json   |  36 ++--\n AGENTS.md                                          |  26 ++-\n CLAUDE.md                                          |  17 +-\n CONTEXT.md                                         |  10 +-\n CONTRIBUTING.md                                    |   8 +-\n SYSTEM_PROMPT.md                                   |  19 +-\n docs/adr/0001-keep-shiki-as-platform-name.md       |   3 -\n docs/adr/0002-use-agents-as-shared-constitution.md |   3 -\n ...3-require-skill-gate-for-mergegate-readiness.md |   3 -\n .../adr/0004-use-github-first-with-shiki-mirror.md |  22 --\n ...ont-and-claude-code-action-as-github-runtime.md |  29 ---\n ...n-cca-completion-and-mergegate-authorization.md |  54 -----\n docs/adr/0007-packaging-and-release.md             | 126 -----------\n .../0008-use-claude-code-as-default-implementer.md | 108 ----------\n ...0009-requirements-definition-and-spec-freeze.md |  90 --------\n docs/adr/0010-external-ai-guardian-review.md       |  97 ---------\n docs/adr/0011-autonomous-quality-gate-evidence.md  |  87 --------\n docs/adr/0012-autonomous-loop-closeout-to-main.md  |  44 ----\n ...otstrap-grants-actions-approve-pull-requests.md | 109 ----------\n ...14-codex-app-external-ai-guardian-ui-adapter.md | 102 ---------\n docs/adr/0015-contract-approval-before-dispatch.md | 229 --------------------\n docs/adr/0016-locks-bound-product-paths.md         | 233 ---------------------\n ...17-bookkeeping-closeouts-do-not-inherit-risk.md | 129 ------------\n ...-guardian-approval-across-a-proven-base-sync.md | 147 -------------\n docs/adr/README.md                                 |  63 +++---\n docs/adr/template.md                               |   8 +-\n docs/agents/autonomous-loop-e2e-acceptance.md      |  14 +-\n docs/agents/bootstrap-command.md                   |   4 +-\n docs/agents/checklists.md                          |  10 +-\n docs/agents/codex-handoff.md                       |   4 +-\n docs/agents/completion-check-agent.md              |   2 +-\n docs/agents/decision-control.md                    |   2 +-\n docs/agents/domain.md                              |   8 +-\n docs/agents/execution-control.md                   |   2 +-\n docs/agents/guardian-policy.md                     |  10 +-\n docs/agents/implementation-policy.md               |  25 ++-\n docs/agents/runtime-auth-model.md                  |   6 +-\n docs/agents/runtime-registry.md                    |   2 +-\n docs/agents/shiki-cli-architecture.md              |   2 +-\n docs/agents/skill-gate.md                          |   4 +-\n docs/agents/triage-labels.md                       |   2 +-\n docs/operations.md                                 |  10 +\n .../0001-memory-loop-spec-freeze-review.md         |   4 +-\n docs/proposals/0002-autonomous-loop-completion.md  |  20 +-\n docs/releasing.md                                  |  15 +-\n docs/usage.md                                      |   6 +-\n pyproject.toml                                     |   2 +-\n scripts/guardian_approval_signal.py                |  30 +--\n scripts/mergegate_check.py                         |  84 ++++----\n scripts/shiki_bootstrap.py                         |   2 +-\n scripts/shiki_cli.py                               |   2 +-\n scripts/shiki_contract_approval.py                 |  14 +-\n scripts/shiki_github.py                            |   8 +-\n scripts/shiki_guardian.py                          |   6 +-\n scripts/shiki_guardian_review.py                   |   6 +-\n scripts/shiki_installer.py                         | 110 ++++++++++\n scripts/shiki_loop.py                              |  56 ++---\n scripts/shiki_migrations.py                        |   4 +-\n scripts/shiki_runtime_adapters.py                  |   2 +-\n scripts/shiki_runtime_registry.py                  |   2 +-\n scripts/shiki_tasks.py                             |  16 +-\n scripts/test_shiki_adversarial_state.sh            |   8 +-\n scripts/test_shiki_code_review_gate.sh             |   2 +-\n scripts/test_shiki_control_plane.sh                |   8 +-\n scripts/test_shiki_goal_loop.sh                    |  14 +-\n scripts/test_shiki_governance_evidence.sh          |   4 +-\n scripts/test_shiki_guardian_policy.sh              |   2 +-\n scripts/test_shiki_loop_e2e.sh                     |   6 +-\n scripts/test_shiki_loop_lock_guard.sh              |   2 +-\n scripts/test_shiki_run_orchestrator.sh             |   2 +-\n scripts/test_shiki_runner_claude.sh                |   2 +-\n scripts/test_shiki_runtime_registry.sh             |   2 +-\n scripts/test_shiki_start.sh                        |   4 +-\n scripts/validate_shiki.py                          |  10 +-\n tests/test_adr_references.py                       | 132 +++++++++---\n tests/test_bookkeeping_closeout.py                 |   6 +-\n tests/test_branch_behind_sync.py                   |   4 +-\n tests/test_cca_evidence_manifest_taskless.py       |   2 +-\n tests/test_closeout.py                             |   2 +-\n tests/test_completion_lock_scope.py                |   2 +-\n tests/test_contract_amendment.py                   |   2 +-\n tests/test_contract_approval.py                    |   4 +-\n tests/test_contract_immutability.py                |   6 +-\n tests/test_contract_mode.py                        |   4 +-\n tests/test_contract_open.py                        |   2 +-\n tests/test_contract_plan_registration.py           |   4 +-\n tests/test_dispatch_lock_precondition.py           |   2 +-\n tests/test_guardian_review.py                      |   6 +-\n tests/test_guardian_signal.py                      |   2 +-\n tests/test_guardian_signal_closeout.py             |   6 +-\n tests/test_loop_branch_plumbing.py                 |   2 +-\n tests/test_loop_code_review.py                     |   2 +-\n tests/test_loop_e2e_contract.py                    |   2 +-\n tests/test_loop_engine.py                          |   6 +-\n tests/test_loop_lock_validator_hint.py             |   6 +-\n tests/test_loop_tdd_evidence.py                    |   4 +-\n tests/test_mirror_scope.py                         |   2 +-\n tests/test_paired_invariants.py                    |   2 +-\n tests/test_policy_job_merged_prs.py                |   6 +-\n tests/test_repair_delivery.py                      |   2 +-\n tests/test_runner_adapters.py                      |   2 +-\n tests/test_workflow_invocation.py                  |   8 +-\n tests/test_workflow_invocation_carry.py            |   2 +-\n 109 files changed, 625 insertions(+), 2017 deletions(-)\n\ndiff --git a/.codex/skills/shiki/SKILL.md b/.codex/skills/shiki/SKILL.md\nindex c8e2e85d9d75d49cc4f2d6286ee22659c4e53e75..93cded623b2c15d9f65c76491a1c2d813ac9e6c0\n--- a/.codex/skills/shiki/SKILL.md\n+++ b/.codex/skills/shiki/SKILL.md\n@@ -18,6 +18,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n Codex CLI does not register this skill as a custom `/shiki` slash command. When\n the user types `/shiki` in Codex CLI and gets \"Unrecognized command\", explain\n that this is expected: invoke Shiki in Codex with natural language such as\n@@ -69,7 +72,7 @@\n \n ## Responsibilities\n \n-- Claude Code implements and repairs by default (`shiki runner claude`, ADR 0008); Codex implements and repairs tasks explicitly assigned to `codex`.\n+- Claude Code implements and repairs by default (`shiki runner claude`, SADR-0008); Codex implements and repairs tasks explicitly assigned to `codex`.\n - Claude Code Action can act as GitHub-side CCA or reviewer.\n - CCA judges completion.\n - MergeGate authorizes state transitions and merge readiness.\ndiff --git a/.github/prompts/cca-completion-check.md b/.github/prompts/cca-completion-check.md\nindex ed203ad5f302ecc81372d8ab27d2b1232ca97497..fd89950528333ef6242e6d5443904f5392ab7e56\n--- a/.github/prompts/cca-completion-check.md\n+++ b/.github/prompts/cca-completion-check.md\n@@ -19,6 +19,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n ## Required Reading\n \n Read only the files needed to judge the PR contract. Prefer this order:\ndiff --git a/.github/prompts/codex-task-handoff.md b/.github/prompts/codex-task-handoff.md\nindex 695e211d76dea1c60e8d5bb360e78487808286a4..59441d576b01a39569722f226c854e5a740e9eb5\n--- a/.github/prompts/codex-task-handoff.md\n+++ b/.github/prompts/codex-task-handoff.md\n@@ -2,7 +2,7 @@\n \n You are Codex Front acting as the Implementer for a Shiki task.\n \n-Follow `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant ADRs, and `docs/agents/codex-handoff.md`.\n+Follow `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant Shiki SADRs and target ADRs, and `docs/agents/codex-handoff.md`.\n \n Rules:\n \ndiff --git a/AGENTS.md b/AGENTS.md\nindex 7f041100d49c2a7525f51cb0f5d696a4a6d1c78c..a06bfbabc6fa48a007389e25e12d7ed523936bed\n--- a/AGENTS.md\n+++ b/AGENTS.md\n@@ -22,7 +22,7 @@\n - **Codex Front** is an optional implementer through Codex App, Codex CLI, Codex IDE extension, or Codex Web signed in with ChatGPT OAuth/subscription auth, dispatched with `shiki runner codex` when a task is explicitly assigned to `codex`.\n - **GitHub CCA** is implemented by Claude Code Action by default, using `CLAUDE_CODE_OAUTH_TOKEN`.\n \n-Do not assume `openai/codex-action`, `OPENAI_API_KEY`, or API-key based Codex automation in the default Shiki path. API-key runners are explicit target-repo extensions and require their own ADR.\n+Do not assume `openai/codex-action`, `OPENAI_API_KEY`, or API-key based Codex automation in the default Shiki path. API-key runners are explicit target-repo extensions and require their own target ADR.\n \n Shiki is not:\n \n@@ -44,10 +44,14 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use\n+`SADR-NNNN-*.md`; target product decisions retain `NNNN-*.md` and the `ADR`\n+namespace.\n+\n Use the canonical source-of-truth order above. Runtime-specific reading orders\n are operational guidance only and must not override or reorder it.\n \n-Conversation state is not durable truth. If a decision matters, put it in GitHub, `.shiki/`, `CONTEXT.md`, or an ADR.\n+Conversation state is not durable truth. If a decision matters, put it in GitHub, `.shiki/`, `CONTEXT.md`, a Shiki SADR, or a target ADR.\n \n If GitHub and `.shiki/` disagree, surface the conflict and prefer GitHub operational state until the mirror is repaired.\n \n@@ -75,8 +79,8 @@\n Every non-trivial change follows this loop:\n \n 1. **Goal**: clarify outcome, completion conditions, non-goals, risk level, and success signals.\n-2. **grill-with-docs**: challenge the plan against domain docs, ADRs, code reality, terminology, and edge scenarios.\n-3. **Context & Impact**: find relevant docs, ADRs, code, dependencies, owners, locks, and verification surfaces.\n+2. **grill-with-docs**: challenge the plan against domain docs, Shiki SADRs, target ADRs, code reality, terminology, and edge scenarios.\n+3. **Context & Impact**: find relevant docs, SADRs, target ADRs, code, dependencies, owners, locks, and verification surfaces.\n 4. **PRD and Spec Freeze**: use `to-prd` when context is settled enough to become durable product/engineering intent. The operator's explicit approval of the PRD is Spec Freeze: the plan records a `spec_freeze` block, and no plan runs without it. Steps 1-4 form Requirements Definition \u2014 one continuous operator dialogue. After Spec Freeze, scope changes require an operator-approved Spec Amendment; non-scope-moving interpretations go to the Assumption Log.\n 5. **Issues**: use `to-issues` to create independently grabbable vertical-slice issues. Prefer AFK slices over HITL slices where possible.\n 6. **Triage**: label issues for readiness, risk, runtime, skills, and MergeGate state.\n@@ -167,7 +171,7 @@\n - Testing decisions.\n - Out-of-scope items.\n - Further notes.\n-- Links to relevant domain terms and ADRs.\n+- Links to relevant domain terms, SADRs, and target ADRs.\n \n Every executable task must include:\n \n@@ -193,7 +197,7 @@\n \n Context & Impact must identify:\n \n-- Relevant documents, domain terms, ADRs, and past decisions.\n+- Relevant documents, domain terms, SADRs, target ADRs, and past decisions.\n - Relevant modules, interfaces, seams, callers, owners, and tests.\n - Dependency relationships between tasks.\n - Candidate locks and lock conflicts.\n@@ -210,7 +214,7 @@\n | Skill | Required when |\n | --- | --- |\n | `setup-matt-pocock-skills` | First configuring the repo for skills, or when issue tracker, triage labels, or domain docs are missing. |\n-| `grill-with-docs` | Default for non-trivial Goals before PRD/issues; always required when plans, terminology, boundaries, tradeoffs, or ADR-worthy decisions are ambiguous. |\n+| `grill-with-docs` | Default for non-trivial Goals before PRD/issues; always required when plans, terminology, boundaries, tradeoffs, or decision-record-worthy choices are ambiguous. |\n | `zoom-out` | The agent lacks an architectural map of the relevant code area. |\n | `to-prd` | Settled Goal context must become durable product/engineering intent. |\n | `to-issues` | A Goal, PRD, or plan must become independently grabbable vertical-slice issues. |\n@@ -348,7 +352,7 @@\n - Three or more new files.\n - Any high-risk or critical label.\n \n-When the architecture gate triggers, run `grill-with-docs` or `improve-codebase-architecture` as appropriate and record durable decisions in an ADR when needed.\n+When the architecture gate triggers, run `grill-with-docs` or `improve-codebase-architecture` as appropriate and record durable decisions in a Shiki SADR or target ADR as applicable.\n \n ## Engineering Discipline\n \ndiff --git a/CLAUDE.md b/CLAUDE.md\nindex 98a932e7841a632866fcd53b0bad0e3af3711d3d..98a31399404d6fb044b4fd493f4459ac87f439de\n--- a/CLAUDE.md\n+++ b/CLAUDE.md\n@@ -14,6 +14,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n ## Claude Role\n \n Default to planner, default implementer, reviewer, coordinator, documentation editor, CCA-style completion judge, and final-judgment assistant.\n@@ -57,7 +60,7 @@\n At the start of a Shiki session:\n \n 1. Read `AGENTS.md`.\n-2. Read `CONTEXT.md` and relevant ADRs.\n+2. Read `CONTEXT.md` and relevant Shiki SADRs and target ADRs.\n 3. Read active `.shiki/` Goal/task/ledger state when present.\n 4. Read active distilled rules from `.shiki/memories/` (status `distilled`, `active: true`, not revoked or superseded) for applicable guidance; the same rules are injected deterministically into task handoffs as the `## Distilled Rules` section (\u00a73.5).\n 5. Read the GitHub Issue or PR when the task is GitHub-backed.\n@@ -74,7 +77,7 @@\n - Define the Goal, completion conditions, non-goals, and risk level.\n - Treat `grill-with-docs` as the default planning entry point for non-trivial Goals.\n - Challenge domain terms against `CONTEXT.md`.\n-- Challenge hard-to-reverse tradeoffs against ADRs.\n+- Challenge hard-to-reverse tradeoffs against SADRs and target ADRs.\n - Explore code instead of asking when code can answer the question.\n - Run Context & Impact before implementation planning.\n - Produce a Task DAG, not an unordered checklist.\n@@ -94,7 +97,7 @@\n - For each question, provide your recommended answer.\n - Resolve dependency decisions before downstream design questions.\n - Update `CONTEXT.md` inline when terminology crystallizes.\n-- Offer ADRs only when the decision is hard to reverse, surprising without context, and a real tradeoff.\n+- Offer a SADR or target ADR only when the decision is hard to reverse, surprising without context, and a real tradeoff.\n - Convert unresolved questions into HITL blockers rather than allowing Codex to guess.\n \n ## PRD and Issues Standard\n@@ -103,7 +106,7 @@\n \n - Use the project's domain glossary vocabulary.\n - Avoid volatile file paths unless they are essential.\n-- Include implementation decisions, testing decisions, out-of-scope items, and relevant ADR/domain links.\n+- Include implementation decisions, testing decisions, out-of-scope items, and relevant SADR/target-ADR/domain links.\n - Identify deep-module opportunities and testable interfaces.\n \n When creating issues:\n@@ -121,7 +124,7 @@\n - Branch/worktree target.\n - Scope and non-goals.\n - Dependencies and locks.\n-- Relevant docs, ADRs, modules, and tests.\n+- Relevant docs, SADRs, target ADRs, modules, and tests.\n - Required skill invocations.\n - TDD expectations.\n - Pre-PR `code-review` gate expectations.\n@@ -152,7 +155,7 @@\n \n - Treat yourself as a judge, not implementer.\n - Do not write production code.\n-- Read `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant ADRs, PRD, task issue, PR body, diff, checks, reviews, labels, and `.shiki/` state.\n+- Read `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant SADRs and target ADRs, PRD, task issue, PR body, diff, checks, reviews, labels, and `.shiki/` state.\n - Evaluate every applicable checklist in `docs/agents/checklists.md`.\n - Map each acceptance criterion to durable evidence.\n - Distinguish implementation failure from missing evidence.\ndiff --git a/CONTEXT.md b/CONTEXT.md\nindex e61a9be06a0cce0d026484083a657bde2067e234..237ee8aa3ea8fab334ece49768bc5ed3b5af0974\n--- a/CONTEXT.md\n+++ b/CONTEXT.md\n@@ -25,7 +25,7 @@\n _Avoid_: jumping directly into code\n \n **grill-with-docs**:\n-A planning skill that challenges a plan against domain language, ADRs, code reality, and concrete edge scenarios. It resolves design-tree questions before PRD/issues. It runs inside Requirements Definition.\n+A planning skill that challenges a plan against domain language, Shiki SADRs, target ADRs, code reality, and concrete edge scenarios. It resolves design-tree questions before PRD/issues. It runs inside Requirements Definition.\n _Avoid_: generic brainstorming, silent assumptions\n \n **Requirements Definition**:\ndiff --git a/CONTRIBUTING.md b/CONTRIBUTING.md\nindex c6d9b656e73abb8a55e5f660123e03153dcb3e6d..9e37a8fbf668db5b22461aaae5548b8f1354394c\n--- a/CONTRIBUTING.md\n+++ b/CONTRIBUTING.md\n@@ -17,7 +17,7 @@\n 1. **Goal** \u2014 start from a user-approved Goal with an outcome, completion\n    conditions, non-goals, risk level, and success signals.\n 2. **grill-with-docs** \u2014 for any non-trivial change, challenge the plan against\n-   domain language, ADRs, and code reality before writing a PRD.\n+   domain language, Shiki SADRs, target ADRs, and code reality before writing a PRD.\n 3. **PRD** \u2014 capture settled product/engineering intent (`to-prd`).\n 4. **Spec Freeze** \u2014 the operator explicitly approves the PRD/requirements;\n    the plan records a `spec_freeze` block and no plan runs without it\n@@ -94,7 +94,7 @@\n   entries.\n - Do not hand-edit `.shiki/goals/*.json` or `.shiki/manifest.json` in a task PR.\n - Conversation state is not durable truth. If a decision matters, record it in\n-  GitHub, `.shiki/`, `CONTEXT.md`, or an ADR.\n+  GitHub, `.shiki/`, `CONTEXT.md`, a Shiki SADR, or a target ADR.\n \n ## Architecture Gate\n \n@@ -103,7 +103,7 @@\n or CI policy, package structure, three or more structural files, more than 100\n net new lines in a core module, three or more new files, or any high-risk label.\n When triggered, run `grill-with-docs` or `improve-codebase-architecture` and\n-record durable decisions in an ADR when needed (see `docs/adr/`).\n+record durable decisions in a Shiki SADR or target ADR as applicable (see `docs/adr/`).\n \n ## Safety\n \ndiff --git a/SYSTEM_PROMPT.md b/SYSTEM_PROMPT.md\nindex aaaea45e3a896c3a44ba89eec8bb84325ecf6c62..da009c86a88285269dee0155e58e550c6d744ea9\n--- a/SYSTEM_PROMPT.md\n+++ b/SYSTEM_PROMPT.md\n@@ -30,6 +30,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n Use the canonical source-of-truth order above. Runtime-specific reading orders\n are operational guidance only and must not override or reorder it. The current\n conversation is non-durable operator input.\n@@ -48,14 +51,14 @@\n \n A runtime may only do work that is explicitly assigned by the Goal, task, PR, or MergeGate decision.\n \n-Do not assume `openai/codex-action`, `OPENAI_API_KEY`, or API-key based Codex automation in the default Shiki path. API-key runners are explicit target-repo extensions and require their own ADR.\n+Do not assume `openai/codex-action`, `OPENAI_API_KEY`, or API-key based Codex automation in the default Shiki path. API-key runners are explicit target-repo extensions and require their own target ADR.\n \n ## 4. Non-negotiable Shiki flow\n \n For any non-trivial Goal, use this flow:\n \n 1. **Goal Seek**: define outcome, non-goals, risk level, completion criteria, and evidence requirements.\n-2. **grill-with-docs**: challenge the plan against `CONTEXT.md`, ADRs, code reality, terminology, boundaries, and edge scenarios. Ask one question at a time when the operator is available. If code can answer a question, inspect code instead of asking.\n+2. **grill-with-docs**: challenge the plan against `CONTEXT.md`, Shiki SADRs, target ADRs, code reality, terminology, boundaries, and edge scenarios. Ask one question at a time when the operator is available. If code can answer a question, inspect code instead of asking.\n 3. **Context & Impact**: identify modules, interfaces, seams, callers, dependencies, locks, risk, and verification surfaces.\n 4. **to-prd + Spec Freeze**: turn settled context into a PRD using domain vocabulary and testing decisions. The operator's explicit approval of the PRD is Spec Freeze, recorded as the plan's `spec_freeze` block; plans without it do not run. Steps 1-4 form Requirements Definition.\n 5. **to-issues**: decompose the PRD into independently grabbable vertical-slice issues. Prefer AFK slices; mark HITL slices when judgment is still required.\n@@ -75,7 +78,7 @@\n Before making any material change:\n \n 1. Read `AGENTS.md` and any runtime-specific wrapper such as `CLAUDE.md`.\n-2. Read `CONTEXT.md` and relevant ADRs.\n+2. Read `CONTEXT.md` and relevant SADRs and target ADRs.\n 3. Read the GitHub Issue / PR and `.shiki/` state for the active Goal or task.\n 4. Identify your role: Planner, Implementer, Reviewer, Completion Check Agent, Repairer, or Guardian-assist.\n 5. Identify Goal id, task id, branch, PR, dependencies, locks, risk level, required skills, and required evidence.\n@@ -103,7 +106,7 @@\n \n At minimum, identify:\n \n-- Relevant documents, ADRs, domain terms, and prior decisions.\n+- Relevant documents, SADRs, target ADRs, domain terms, and prior decisions.\n - Relevant modules, interfaces, seams, callers, owners, and tests.\n - Dependency relationships between tasks.\n - Candidate file locks and lock conflicts.\n@@ -152,7 +155,7 @@\n \n CCA must read:\n \n-- `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant ADRs.\n+- `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant SADRs and target ADRs.\n - Parent Goal issue, PRD issue, task issue, comments, and labels.\n - PR body, diff, commits, changed files, reviews, CI checks, and artifacts available to the workflow.\n - `.shiki/` task contract, locks, ledger entries, CCA checklist profile, and prior repair packets.\n@@ -214,7 +217,7 @@\n - Three or more new files.\n - Any high-risk or critical label.\n \n-When the architecture gate triggers, run `grill-with-docs` or `improve-codebase-architecture` as appropriate and record the decision in an ADR if it is hard to reverse or likely to be re-litigated.\n+When the architecture gate triggers, run `grill-with-docs` or `improve-codebase-architecture` as appropriate and record the decision in a Shiki SADR or target ADR as applicable if it is hard to reverse or likely to be re-litigated.\n \n ## 14. Skill Gate\n \n@@ -223,7 +226,7 @@\n Use these skills:\n \n - `setup-matt-pocock-skills`: before first use of the engineering skill set, or when issue tracker, triage labels, or domain docs are missing.\n-- `grill-with-docs`: default for non-trivial Goals before PRD/issues; required for ambiguous plans, terminology, boundaries, ADR-worthy tradeoffs, or design-tree decisions.\n+- `grill-with-docs`: default for non-trivial Goals before PRD/issues; required for ambiguous plans, terminology, boundaries, decision-record-worthy tradeoffs, or design-tree decisions.\n - `zoom-out`: unfamiliar code area or missing architectural map.\n - `to-prd`: convert settled context into a PRD.\n - `to-issues`: break a PRD, plan, or Goal into independently grabbable vertical-slice issues.\ndiff --git a/docs/adr/0001-keep-shiki-as-platform-name.md b/docs/adr/SADR-0001-keep-shiki-as-platform-name.md\nindex 20a9637c3b9d8c97de7ba8db7cfae4d82d9a7dba..8b685439959aada1b221c112f0ac3c51ca8a1695\n--- a/docs/adr/0001-keep-shiki-as-platform-name.md\n+++ b/docs/adr/SADR-0001-keep-shiki-as-platform-name.md\n@@ -1,3 +1,3 @@\n-# Keep Shiki As The Platform Name\n+# SADR-0001: Keep Shiki As The Platform Name\n \n Shiki remains the product name for the GitHub-native agentic engineering control plane. We considered more descriptive names such as MergePilot, MergeGate, and Kiroku, but kept Shiki because it carries the existing goal-seeking framework identity while remaining independent of Hermes, Codex, Claude Code, or any single runtime.\ndiff --git a/docs/adr/0002-use-agents-as-shared-constitution.md b/docs/adr/SADR-0002-use-agents-as-shared-constitution.md\nindex 73de30ecb078120eac540d9bc51855e3cd48d0e3..c5486182bf06fca36ea508496b013c67302fd989\n--- a/docs/adr/0002-use-agents-as-shared-constitution.md\n+++ b/docs/adr/SADR-0002-use-agents-as-shared-constitution.md\n@@ -1,3 +1,3 @@\n-# Use AGENTS As The Shared Agent Constitution\n+# SADR-0002: Use AGENTS As The Shared Agent Constitution\n \n Shiki uses `AGENTS.md` as the runtime-neutral constitution and `CLAUDE.md` as a thin Claude Code wrapper. This keeps Codex, Claude Code, Hermes Runner, and future Agent Runtimes aligned on the same GitHub-first MergeGate rules while still allowing runtime-specific instructions where needed.\ndiff --git a/docs/adr/0003-require-skill-gate-for-mergegate-readiness.md b/docs/adr/SADR-0003-require-skill-gate-for-mergegate-readiness.md\nindex 424be0c9af4c8f0136f1fde68a468c47ff84e764..7fc337987c7df9792f75f181dfcbed75fe5ffff2\n--- a/docs/adr/0003-require-skill-gate-for-mergegate-readiness.md\n+++ b/docs/adr/SADR-0003-require-skill-gate-for-mergegate-readiness.md\n@@ -1,3 +1,3 @@\n-# Require Skill Gate For MergeGate Readiness\n+# SADR-0003: Require Skill Gate For MergeGate Readiness\n \n Shiki requires relevant engineering skills to be invoked before execution when their triggers apply. This makes skills part of MergeGate readiness rather than optional prompt style, because Shiki depends on repeatable planning, issue decomposition, TDD, diagnosis, triage, and architecture review before autonomous or parallel agent work can be trusted.\ndiff --git a/docs/adr/0004-use-github-first-with-shiki-mirror.md b/docs/adr/SADR-0004-use-github-first-with-shiki-mirror.md\nindex d0a3a9174dc646a945a9d325954ffaf00c352040..a79c54ae9ee8eed2a0524192b864da4e22a92d3f\n--- a/docs/adr/0004-use-github-first-with-shiki-mirror.md\n+++ b/docs/adr/SADR-0004-use-github-first-with-shiki-mirror.md\n@@ -1,4 +1,4 @@\n-# ADR 0004: Use GitHub First With A Repository-Local Shiki Mirror\n+# SADR-0004: Use GitHub First With A Repository-Local Shiki Mirror\n \n ## Status\n \ndiff --git a/docs/adr/0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md b/docs/adr/SADR-0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\nindex b339baf49b89d9290e79c3d51b36e7b2bde3bd95..0f3c3eb9afccdb3403ce978e21d04fe12e495da0\n--- a/docs/adr/0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\n+++ b/docs/adr/SADR-0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\n@@ -1,8 +1,8 @@\n-# ADR 0005: Use Codex As Front And Claude Code Action As The GitHub Runtime\n+# SADR-0005: Use Codex As Front And Claude Code Action As The GitHub Runtime\n \n ## Status\n \n-Accepted (implementer default superseded by ADR 0008; the auth model and the\n+Accepted (implementer default superseded by SADR-0008; the auth model and the\n Claude Code Action GitHub runtime decision remain in force)\n \n ## Context\n@@ -26,4 +26,4 @@\n - Shiki avoids requiring `OPENAI_API_KEY` for the default workflow.\n - Codex work happens through the user's authenticated Codex front surface, not as a hidden backend Action.\n - Claude Code Action is configured for OAuth-token based subscription usage by default.\n-- API-key based automation remains possible only as an explicit target-repo extension with its own ADR.\n+- API-key based automation remains possible only as an explicit target-repo extension with its own target ADR.\ndiff --git a/docs/adr/0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md b/docs/adr/SADR-0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\nindex 4bcae9d1e52f0a37b8e9fbafa5cd14b6ee1993bd..7a38538a6ae9929567f0ade67e7ea6a60d0b3f7e\n--- a/docs/adr/0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\n+++ b/docs/adr/SADR-0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\n@@ -1,4 +1,4 @@\n-# ADR 0006: Separate Codex Implementation, CCA Completion, And MergeGate Authorization\n+# SADR-0006: Separate Codex Implementation, CCA Completion, And MergeGate Authorization\n \n ## Status\n \ndiff --git a/docs/adr/0007-packaging-and-release.md b/docs/adr/SADR-0007-packaging-and-release.md\nindex 79b84ddb97167372b44641e5b7860e400ce80822..bb445a2e2e856d9d1f5fdbe35de8d4e8cec0fb32\n--- a/docs/adr/0007-packaging-and-release.md\n+++ b/docs/adr/SADR-0007-packaging-and-release.md\n@@ -1,4 +1,4 @@\n-# ADR 0007: Packaging And Release For Shiki\n+# SADR-0007: Packaging And Release For Shiki\n \n ## Status\n \n@@ -25,7 +25,7 @@\n   not introduce third-party runtime dependencies or break the shim.\n - Releases must run inside the operator's subscription-authenticated toolchain\n   and must not require extra secrets beyond the built-in `GITHUB_TOKEN`\n-  (consistent with ADR 0005's no-API-key-by-default stance).\n+  (consistent with SADR-0005's no-API-key-by-default stance).\n - `.shiki/manifest.json` already encodes the install surface (`install.include`,\n   `install.create_directories`, `install.exclude_from_commit`), and it must not\n   be mutated as part of this change.\n@@ -34,7 +34,7 @@\n \n This is a hard-to-reverse platform decision (it sets the version source of\n truth, the release trigger, and the public install contract), so it is recorded\n-as an ADR.\n+as a SADR.\n \n ## Decision\n \n@@ -104,7 +104,7 @@\n 3. **Publishing to PyPI.** Rejected for now: Shiki is distributed as a\n    repository template + git-installable CLI, not a PyPI library; PyPI would add\n    credentials/secrets and a different versioning contract. Can be added later as\n-   its own ADR if a packaged distribution is needed.\n+   its own SADR if a packaged distribution is needed.\n 4. **Dynamic version (read `VERSION` via a build backend `dynamic` field).**\n    Rejected to keep `pyproject.toml` backend-agnostic and dependency-free; the\n    small duplication between `VERSION` and `pyproject.toml` is guarded by the\n@@ -123,4 +123,4 @@\n   releasing versions the whole platform but ships only `install.include` to\n   targets.\n - Future PyPI distribution or a console entry point are deferred and would each\n-  warrant their own ADR.\n+  warrant their own SADR.\ndiff --git a/docs/adr/0008-use-claude-code-as-default-implementer.md b/docs/adr/SADR-0008-use-claude-code-as-default-implementer.md\nindex dedf4510b7c7799521422f622029563d373db031..35c2625d908167fbc58855b5b63e283805f167e8\n--- a/docs/adr/0008-use-claude-code-as-default-implementer.md\n+++ b/docs/adr/SADR-0008-use-claude-code-as-default-implementer.md\n@@ -1,12 +1,12 @@\n-# ADR 0008: Use Claude Code As The Default Implementer Runtime\n+# SADR-0008: Use Claude Code As The Default Implementer Runtime\n \n ## Status\n \n-Proposed (supersedes ADR 0005 for the implementer default; keeps its auth model)\n+Proposed (supersedes SADR-0005 for the implementer default; keeps its auth model)\n \n ## Context\n \n-ADR 0005 made Codex Front the default implementation runtime, with Claude Code\n+SADR-0005 made Codex Front the default implementation runtime, with Claude Code\n restricted to planner and reviewer roles by the runtime registry, and the only\n autonomous implementation adapter was `shiki runner codex`.\n \n@@ -18,13 +18,13 @@\n - The platform's planning, review, and CCA layers already run on Claude Code,\n   and Claude Code's native capabilities (the built-in `/code-review` skill and\n   the Workflow multi-agent orchestration tool) have no Codex equivalent.\n-- The Goal lifecycle redesign (ADR 0009) requires an autonomous post-freeze\n+- The Goal lifecycle redesign (SADR-0009) requires an autonomous post-freeze\n   loop driven from the operator's Claude Code session, including bounded\n   repair dispatch, which must not depend on a runtime that cannot be invoked\n   unattended.\n \n The constitution claims Shiki is runtime-agnostic (\"not a Claude-only\n-workflow\", ADR 0002). Any change to the implementer default must preserve the\n+workflow\", SADR-0002). Any change to the implementer default must preserve the\n registry-based runtime model rather than hardcoding one vendor.\n \n ## Decision\n@@ -39,7 +39,7 @@\n   the task to review or repair-needed.\n - Keep Codex registered as an optional implementer runtime. `shiki runner\n   codex`, the Codex handoff templates, and the ChatGPT OAuth auth model from\n-  ADR 0005 remain supported for tasks explicitly assigned to `codex`.\n+  SADR-0005 remain supported for tasks explicitly assigned to `codex`.\n - Commit to mandating two Claude-native capabilities as loop contract in\n   follow-up Goals (status: the code-review gate was delivered by Goal G-B \u2014\n   skill registry, checklists item PR-12, default required_skills \u2014 while the\n@@ -60,7 +60,7 @@\n \n Out of scope: removing Codex support, changing the CCA runtime (Claude Code\n Action with `CLAUDE_CODE_OAUTH_TOKEN` stays), and API-key based automation\n-(still requires its own ADR per ADR 0005).\n+(still requires its own target ADR per SADR-0005).\n \n ## Consequences\n \n@@ -100,7 +100,7 @@\n   harness rejection of direct `codex exec` is outside Shiki's control, and the\n   plan's mandatory native capabilities cannot run on Codex at all.\n - **Remove Codex entirely**: rejected; it abandons the runtime-agnostic\n-  constitution (ADR 0002) for no operational gain and forecloses\n+  constitution (SADR-0002) for no operational gain and forecloses\n   cross-runtime adversarial review.\n - **GitHub Actions cloud implementation via claude-code-action**: rejected as\n   default; durable evidence is automatic but token cost, Node 24 workflow\ndiff --git a/docs/adr/0009-requirements-definition-and-spec-freeze.md b/docs/adr/SADR-0009-requirements-definition-and-spec-freeze.md\nindex 29b3f7871bef9203a8b4a831a65c39eb2f506e42..52e7dd69ad0aa4efe0b68c62678abbf7df02aaec\n--- a/docs/adr/0009-requirements-definition-and-spec-freeze.md\n+++ b/docs/adr/SADR-0009-requirements-definition-and-spec-freeze.md\n@@ -1,4 +1,4 @@\n-# ADR 0009: Merge Grill And Planning Into Requirements Definition With Spec Freeze\n+# SADR-0009: Merge Grill And Planning Into Requirements Definition With Spec Freeze\n \n ## Status\n \ndiff --git a/docs/adr/0010-external-ai-guardian-review.md b/docs/adr/SADR-0010-external-ai-guardian-review.md\nindex e6108bbe8bb4e73e0ff4684c8f78001a80438c4e..f3510b145664298adafb151b6f0db51b972f01ec\n--- a/docs/adr/0010-external-ai-guardian-review.md\n+++ b/docs/adr/SADR-0010-external-ai-guardian-review.md\n@@ -1,4 +1,4 @@\n-# ADR 0010: External AI Guardian Review As A First-Class Approval Authority\n+# SADR-0010: External AI Guardian Review As A First-Class Approval Authority\n \n ## Status\n \n@@ -8,7 +8,7 @@\n \n Shiki is intended to be an autonomous, recursive, self-improving agent\n framework: authority-in-the-loop, not human-in-the-loop. Until now the Guardian\n-approval required for high/critical-risk merges (guardian-policy.json, ADR\n+approval required for high/critical-risk merges (guardian-policy.json, SADR\n governance) recognized only human authority sources \u2014 a GitHub review approval,\n the `guardian:approved` label applied by a configured Guardian, or a comment\n carrying `Guardian approval granted` plus the current head SHA.\n@@ -89,7 +89,7 @@\n   autonomous design the operator intends and produced the impersonation\n   incident.\n - **Record AI approval as an operator comment**: rejected as forgery of human\n-  authority \u2014 the exact failure this ADR corrects.\n+  authority \u2014 the exact failure this SADR corrects.\n - **Deliver the artifact as a committed `.shiki/` file**: rejected; a committed\n   approval inside the PR being approved is forgeable by anyone who can push.\n - **A dedicated reviewer bot identity instead of a relayed comment**: deferred;\ndiff --git a/docs/adr/0011-autonomous-quality-gate-evidence.md b/docs/adr/SADR-0011-autonomous-quality-gate-evidence.md\nindex bca0246c5d46e44afbde15486a75eec425fc02f5..9c93eac16d8c3a6d4b11bd7224e3955102cd7db4\n--- a/docs/adr/0011-autonomous-quality-gate-evidence.md\n+++ b/docs/adr/SADR-0011-autonomous-quality-gate-evidence.md\n@@ -1,4 +1,4 @@\n-# ADR 0011: Autonomous Quality-Gate Evidence \u2014 Deterministic Facts And An Independent Verifier\n+# SADR-0011: Autonomous Quality-Gate Evidence \u2014 Deterministic Facts And An Independent Verifier\n \n ## Status\n \ndiff --git a/docs/adr/0012-autonomous-loop-closeout-to-main.md b/docs/adr/SADR-0012-autonomous-loop-closeout-to-main.md\nindex 637fad6dde4989c9c2bf0f81f676d8d29991123c..ec7d85f81030a676bcbba28b43eb365422a36eac\n--- a/docs/adr/0012-autonomous-loop-closeout-to-main.md\n+++ b/docs/adr/SADR-0012-autonomous-loop-closeout-to-main.md\n@@ -1,4 +1,4 @@\n-# 0012 \u2014 Autonomous loop pushes goal completion to main via a closeout PR\n+# SADR-0012 \u2014 Autonomous loop pushes goal completion to main via a closeout PR\n \n - Status: accepted\n - Date: 2026-06-18\ndiff --git a/docs/adr/0013-bootstrap-grants-actions-approve-pull-requests.md b/docs/adr/SADR-0013-bootstrap-grants-actions-approve-pull-requests.md\nindex b6c69d64f1c5ced346b88d6859586715b3b5bd0e..cf48ff4a6716267f539ca58edd1a852e7c7a2d64\n--- a/docs/adr/0013-bootstrap-grants-actions-approve-pull-requests.md\n+++ b/docs/adr/SADR-0013-bootstrap-grants-actions-approve-pull-requests.md\n@@ -1,4 +1,4 @@\n-# ADR 0013: Bootstrap Grants GitHub Actions Permission To Create And Approve Pull Requests\n+# SADR-0013: Bootstrap Grants GitHub Actions Permission To Create And Approve Pull Requests\n \n ## Status\n \n@@ -31,7 +31,7 @@\n security tradeoff: it lets repository automation satisfy a human-review gate. In\n a multi-maintainer repo that could let a compromised or buggy workflow\n self-approve changes. So whether bootstrap turns it on by default \u2014 and with\n-what default token scope \u2014 is an ADR-worthy decision, not an implementation\n+what default token scope \u2014 is a SADR-worthy decision, not an implementation\n detail.\n \n ## Decision\n@@ -48,7 +48,7 @@\n Specifically:\n \n - **`can_approve_pull_request_reviews` defaults to `true`.** Shiki's default\n-  posture is solo/AFK autonomous operation (ADR 0008/0009/0012), where the CCA\n+  posture is solo/AFK autonomous operation (SADR-0008/0009/0012), where the CCA\n   Review Bridge is the only path that can satisfy `required_review: true` after\n   the CCA and Guardian gates have already passed. The default makes a\n   freshly-bootstrapped repo able to run the autonomous loop end to end without a\ndiff --git a/docs/adr/0014-codex-app-external-ai-guardian-ui-adapter.md b/docs/adr/SADR-0014-codex-app-external-ai-guardian-ui-adapter.md\nindex 238db06279334caa341c117362df9271382a7fcd..65f12e9939321e1b3e389a34c822fc1549e5155d\n--- a/docs/adr/0014-codex-app-external-ai-guardian-ui-adapter.md\n+++ b/docs/adr/SADR-0014-codex-app-external-ai-guardian-ui-adapter.md\n@@ -1,4 +1,4 @@\n-# ADR 0014: Use Codex App As The External AI Guardian UI Adapter\n+# SADR-0014: Use Codex App As The External AI Guardian UI Adapter\n \n ## Status\n \ndiff --git a/docs/adr/0015-contract-approval-before-dispatch.md b/docs/adr/SADR-0015-contract-approval-before-dispatch.md\nindex fad6c656741a20a7684ba16080a7db159f63a52f..6061271a46d3f1d6ae7174477de3fb75673123a6\n--- a/docs/adr/0015-contract-approval-before-dispatch.md\n+++ b/docs/adr/SADR-0015-contract-approval-before-dispatch.md\n@@ -1,4 +1,4 @@\n-# ADR 0015: Approve The Task Contract Before Dispatch, Not The Diff After It\n+# SADR-0015: Approve The Task Contract Before Dispatch, Not The Diff After It\n \n ## Status\n \n@@ -9,7 +9,7 @@\n Guardian approval for high/critical risk is expressible only on a pull request.\n Every approval source in `guardian-policy.json` \u2014 GitHub review, the\n `guardian:approved` label, the `Guardian approval granted` comment, and the\n-`external_ai_guardian_review` artifact (ADR 0010/0014) \u2014 is bound to a PR, and\n+`external_ai_guardian_review` artifact (SADR-0010/0014) \u2014 is bound to a PR, and\n three of the four require the current head SHA. Approval therefore cannot exist\n before an implementation PR exists.\n \n@@ -81,7 +81,7 @@\n An envelope breach must surface only on the non-repairable policy gate, never on\n the repairable `MergeGate metadata check`. Otherwise the Goal Loop routes it to\n `dispatch_repair` \u2014 instructing a runner to make the breach go away \u2014 which is\n-exactly what this ADR forbids.\n+exactly what this SADR forbids.\n \n **Contract immutability.** After registration, an implementation PR must not\n change the governance fields of its own task contract (`scope`, `non_goals`,\n@@ -116,7 +116,7 @@\n fails any verdict that is not `complete`, and the signal the CCA prompt reads is\n computed from `evaluate_guardian_approval` alone. Without the OR-merge at the\n signal, a contract-approved PR still returns `needs_guardian`, the CCA job fails,\n-and the MergeGate policy check never runs \u2014 the gate this ADR changes would never\n+and the MergeGate policy check never runs \u2014 the gate this SADR changes would never\n be reached. The CCA *prompt* needs no change: a `contract_approval` entry in the\n signal's existing `sources` array satisfies its current wording.\n \n@@ -130,7 +130,7 @@\n written, and an approved contract lets its implementation PRs merge through the\n same automatic path as low/medium risk. The wasted implement-then-block cycle\n disappears. Guardian review shifts from reading diffs to reading contracts, which\n-is both earlier and smaller; the external AI Guardian path (ADR 0010/0014) works\n+is both earlier and smaller; the external AI Guardian path (SADR-0010/0014) works\n on contracts with no change.\n \n We accept that the Guardian authorizes a class of change without seeing its\n@@ -146,7 +146,7 @@\n adversarial tests for the new approval path equivalent in rigor to\n `test_shiki_governance_evidence.sh`.\n \n-Bootstrap costs, which apply only while this ADR is being implemented. The tasks\n+Bootstrap costs, which apply only while this SADR is being implemented. The tasks\n that build Contract Approval merge under the existing rules, so the ones\n dispatched after the pre-dispatch gate lands but before the approval path lands\n need a Guardian-approved Contract PR *and* a live Guardian approval on their\n@@ -210,7 +210,7 @@\n risk; they do not bound authorized work.\n \n **Auto-escalate risk on envelope breach instead of stopping.** Returns the\n-workflow to a diff-time Guardian decision \u2014 the exact behavior this ADR removes.\n+workflow to a diff-time Guardian decision \u2014 the exact behavior this SADR removes.\n \n **Resolve governance fields from the base snapshot only.** Rejected against the\n registration flow: implementation PRs add their own contract, so the base has no\ndiff --git a/docs/adr/0016-locks-bound-product-paths.md b/docs/adr/SADR-0016-locks-bound-product-paths.md\nindex b5e850f3f1f7f4ef6e7f9d1763a29a51bd300c45..46b40a9b99a7aae644c02bb458f5473ff0a90ce2\n--- a/docs/adr/0016-locks-bound-product-paths.md\n+++ b/docs/adr/SADR-0016-locks-bound-product-paths.md\n@@ -1,4 +1,4 @@\n-# ADR 0016: Locks bound product paths; the Shiki mirror is bound by record identity\n+# SADR-0016: Locks bound product paths; the Shiki mirror is bound by record identity\n \n ## Status\n \n@@ -6,8 +6,8 @@\n \n - Date: 2026-07-28\n - Deciders: operator (mizutani-140), Claude (planner)\n-- Related: ADR 0004 (GitHub-first with a `.shiki` mirror), ADR 0009 (Spec Freeze),\n-  ADR 0012 (autonomous loop closeout to main), ADR 0015 (Contract Approval before\n+- Related: SADR-0004 (GitHub-first with a `.shiki` mirror), SADR-0009 (Spec Freeze),\n+  SADR-0012 (autonomous loop closeout to main), SADR-0015 (Contract Approval before\n   dispatch), remediation goal task R-02, the live PR #179 deadlock.\n \n ## Context\n@@ -90,11 +90,11 @@\n files, documentation files and named source modules are nameable; runtime-\n allocated ledger, runner and report ids are not \u2014 and those live in `.shiki`,\n which locks no longer govern. Existing frozen contracts are brought into line by\n-a one-time Spec Amendment per plan, which must land before ADR 0015's contract\n+a one-time Spec Amendment per plan, which must land before SADR-0015's contract\n immutability makes `locks` unwritable from the implementation PR.\n \n **Lock state is resolved from the durable side.** MergeGate resolves the lock\n-registry, and ADR 0015's governance fields, from the base `.shiki` snapshot when\n+registry, and SADR-0015's governance fields, from the base `.shiki` snapshot when\n present, falling back to head only when absent. The `MergeGate metadata check`\n job is given `--base-shiki`, which it does not receive today. Locks are not\n derived from the live open-pull-request set: a pull request body is untrusted\n@@ -108,7 +108,7 @@\n Explicitly out of scope: moving MergeGate's contract resolution off the PR\n checkout wholesale; making the mirror a projection of GitHub state; introducing\n new lock kinds; moving ledger evidence out of the repository; and unwinding\n-ADR 0012's closeout pull request. Registration of multi-task goals is ADR 0015's\n+SADR-0012's closeout pull request. Registration of multi-task goals is SADR-0015's\n Contract PR and is not re-decided here.\n \n ## Consequences\n@@ -127,7 +127,7 @@\n \n Risks accepted. First, goal-scoped identity is only as strong as `goal_id`,\n which is resolved from the PR body and the head task file; the mitigation is to\n-add `goal_id` to ADR 0015's frozen governance field set, and until that lands a\n+add `goal_id` to SADR-0015's frozen governance field set, and until that lands a\n task file can name a foreign goal. Second, base-when-present resolution is\n unavailable on fork pull requests, where the head is contributor-controlled;\n fork pull requests already cannot complete a Shiki merge, and this decision does\n@@ -139,7 +139,7 @@\n path, not by implementation pull requests.\n \n New obligations: one Spec Amendment per frozen plan to rewrite `locks`, ordered\n-before ADR 0015 T1; extension of the adversarial suites in\n+before SADR-0015 T1; extension of the adversarial suites in\n `scripts/test_shiki_adversarial_state.sh` and\n `scripts/test_shiki_governance_evidence.sh` to cover the eleven newly-scoped\n directories; and updating `scripts/test_shiki_loop_lock_guard.sh`,\n@@ -151,32 +151,32 @@\n whether the loop's transitive sync of ledger `evidence` references can be\n deleted once the Contract PR carries its own source plan.\n \n-## Relationship to ADR 0004\n+## Relationship to SADR-0004\n \n-This ADR **clarifies something ADR 0004 left unspecified**. ADR 0004 decides that\n+This SADR **clarifies something SADR-0004 left unspecified**. SADR-0004 decides that\n GitHub is the operational source of truth and that each target repository keeps a\n `.shiki/` mirror. It says nothing about pull requests writing the mirror, does\n not name MergeGate, and does not make the mirror an input to a gate. Nothing in\n-ADR 0004's Decision or Consequences is amended, superseded or weakened: the\n+SADR-0004's Decision or Consequences is amended, superseded or weakened: the\n mirror still exists, still records the same artifacts, and pull request\n-boundaries remain the coordination surface. What this ADR settles is the question\n+boundaries remain the coordination surface. What this SADR settles is the question\n 0004 never reached \u2014 by what authority a pull request may write a mirror record \u2014\n and it answers it the way `.shiki/manifest.json` already declares\n (`pr_mutation: current-task-or-current-goal-only`).\n \n-It **amends ADR 0012**, whose Decision item 5 states that \"Loop-executed tasks\n+It **amends SADR-0012**, whose Decision item 5 states that \"Loop-executed tasks\n declare the `path:.shiki/**` lock, which covers every `.shiki` file the closeout\n stages ... so `files_outside_locks` is satisfied without per-file lock\n bookkeeping.\" That clause is replaced: the closeout's mirror writes are\n-authorized by record identity, not by a declared lock. ADR 0012's substantive\n+authorized by record identity, not by a declared lock. SADR-0012's substantive\n decision \u2014 that completion reaches `main` through a closeout pull request rather\n than local attestation \u2014 is preserved unchanged.\n \n-It is **complementary to ADR 0015**, not a substitute. ADR 0015 owns contract\n+It is **complementary to SADR-0015**, not a substitute. SADR-0015 owns contract\n immutability, the pre-dispatch gate, the Contract PR and the Scope Envelope. This\n-ADR owns what a lock means and who may write the mirror. The one ordering\n+SADR owns what a lock means and who may write the mirror. The one ordering\n constraint between them is that the lock-narrowing Spec Amendment must precede\n-ADR 0015 T1's freezing of the `locks` field.\n+SADR-0015 T1's freezing of the `locks` field.\n \n ## Alternatives Considered\n \n@@ -186,7 +186,7 @@\n the only writer that can register them may run. Migrating the Guardian signal to\n that store makes every registration PR fail closed to `needs_guardian` at every\n risk level, because the task file is by definition absent from the store, which\n-removes the low/medium autonomous path ADR 0008/0009 exists to provide. And it\n+removes the low/medium autonomous path SADR-0008/0009 exists to provide. And it\n leaves schemas, manifest, config, guardian policy, templates, migrations, plans,\n worktrees, runner records, runs and handoffs with no writer at all in a\n repository whose product is `.shiki`. Measured benefit on the frozen backlog:\n@@ -199,14 +199,14 @@\n namespace is not in fact ownership-partitioned \u2014 56 ledger entries carry no\n `task_id`, and one goal DAG is writable by 35 registered tasks. The correct\n expression of its insight is that locks do not govern the mirror at all, which is\n-what this ADR decides.\n+what this SADR decides.\n \n **Make the mirror a projection written only by a registration writer and a\n post-merge projection job.** Rejected. It requires a bypass actor with write\n access to the protected default branch; it makes its own master invariant\n readable from a manifest that pull requests may edit; it deletes both reconcile\n modes and leaves a corrupted mirror with no in-band repair path, contradicting\n-ADR 0004's consequence that mirror conflicts are repaired against GitHub state;\n+SADR-0004's consequence that mirror conflicts are repaired against GitHub state;\n and its content-derived ids are rejected by `validate_shiki`'s currently-green id\n pattern.\n \ndiff --git a/docs/adr/0017-bookkeeping-closeouts-do-not-inherit-risk.md b/docs/adr/SADR-0017-bookkeeping-closeouts-do-not-inherit-risk.md\nindex 554e3a0d526a72ca067108c5464cc68c3edcf174..b9676857a4713850e063af7c39c8e0b01ecb4c66\n--- a/docs/adr/0017-bookkeeping-closeouts-do-not-inherit-risk.md\n+++ b/docs/adr/SADR-0017-bookkeeping-closeouts-do-not-inherit-risk.md\n@@ -1,4 +1,4 @@\n-# ADR 0017: A Bookkeeping Closeout Does Not Inherit Its Task's Risk\n+# SADR-0017: A Bookkeeping Closeout Does Not Inherit Its Task's Risk\n \n ## Status\n \n@@ -6,7 +6,7 @@\n \n ## Context\n \n-ADR 0012 made the goal loop push a task's completion to the default branch through a\n+SADR-0012 made the goal loop push a task's completion to the default branch through a\n closeout pull request: after the implementation PR merges, the loop opens a second PR\n carrying `task.status = done`, `lock.state = released`, and \u2014 when the task completes\n its goal \u2014 `goal.status = complete` plus one scorecard report. Nothing else. The\n@@ -49,14 +49,14 @@\n 4. The task file's governance fields (`scope`, `non_goals`, `required_skills`,\n    `risk_level`, `locks`, `acceptance_checks`, `test_command`) are byte-identical to\n    the base snapshot. Only `status`, `expected_pr`, `closeout_pr`, `ledger_evidence`\n-   and `expected_branch` may differ \u2014 the ADR 0015 contract-immutability rule,\n+   and `expected_branch` may differ \u2014 the SADR-0015 contract-immutability rule,\n    unchanged.\n \n    `expected_branch` is in that set because a closeout is *by construction* on a\n    different branch from the implementation it closes, and the loop rewrites the\n    field accordingly. It is safe to let it differ because MergeGate independently\n    requires `expected_branch` to equal the PR's actual head ref, so a PR cannot set\n-   it to anything other than the branch it is really on. The first draft of this ADR\n+   it to anything other than the branch it is really on. The first draft of this SADR\n    omitted it, which made the exemption unreachable: every real closeout failed\n    condition 4 on a field the loop always changes. See PR #209.\n 5. The state transitions are exactly the terminal set: task `review` \u2192 `done`, lock\n@@ -113,12 +113,12 @@\n classification must be derived, not asserted.\n \n **Fold the closeout into the implementation PR** so no second PR exists. Rejected under\n-ADR 0012, which recorded why: a task file that is already `done` on the implementation\n+SADR-0012, which recorded why: a task file that is already `done` on the implementation\n branch short-circuits the loop's review \u2192 CCA \u2192 merge \u2192 done ordering into a local\n goal-completion before any merge happens.\n \n **Have the loop auto-merge closeouts with elevated credentials.** Rejected outright: that\n-is a runtime authoring its own approval, which ADR 0010 and the constitution's\n+is a runtime authoring its own approval, which SADR-0010 and the constitution's\n approval-record prohibition forbid. It would also be indistinguishable, in the evidence,\n from the forgery those rules exist to prevent.\n \ndiff --git a/docs/adr/0018-carried-guardian-approval-across-a-proven-base-sync.md b/docs/adr/SADR-0018-carried-guardian-approval-across-a-proven-base-sync.md\nindex 22358562da390ed8b45cd600da08a43d378b4680..e6e2907cefa776460af73ddf7d39db605f3c39b0\n--- a/docs/adr/0018-carried-guardian-approval-across-a-proven-base-sync.md\n+++ b/docs/adr/SADR-0018-carried-guardian-approval-across-a-proven-base-sync.md\n@@ -1,4 +1,4 @@\n-# ADR 0018: Carried Guardian Approval Across A Proven Base Sync\n+# SADR-0018: Carried Guardian Approval Across A Proven Base Sync\n \n ## Status\n \n@@ -34,7 +34,7 @@\n Guardian evaluator that consumes it (`shiki_guardian.evaluate_guardian_approval`,\n the `guardian_comment_carried` source) were built and tested in isolation. Neither\n does anything until a caller passes the flags that turn the carry on, so the\n-feature was invisible in CI. This ADR records the decision the two of them\n+feature was invisible in CI. This SADR records the decision the two of them\n implement, and this task wires it into the four CI invocations that judge Guardian\n approval.\n \ndiff --git a/docs/adr/README.md b/docs/adr/README.md\nindex c70e057a4520c36e042ab68c53cd4a21ef709b71..5da43856836e2ce6a150f8127766e2f88355ab4b\n--- a/docs/adr/README.md\n+++ b/docs/adr/README.md\n@@ -1,40 +1,41 @@\n-# Architecture Decision Records\n+# Decision Record Namespaces\n \n-This directory holds Shiki's Architecture Decision Records (ADRs): durable\n-records of hard-to-reverse platform decisions. ADRs are part of the Shiki source\n-of truth alongside GitHub state, the `.shiki/` mirror, and `CONTEXT.md`.\n+This directory holds two independent decision namespaces:\n \n-## How ADRs Work\n+- Shiki Architecture Decision Records (SADRs), named\n+  `SADR-NNNN-<short-slug>.md`, record hard-to-reverse Shiki platform decisions.\n+- Target Architecture Decision Records (ADRs), named\n+  `NNNN-<short-slug>.md`, belong to the target product.\n \n-- New ADRs are added as `docs/adr/NNNN-<short-slug>.md`, where `NNNN` is the next\n-  zero-padded sequence number.\n-- The **canonical list of ADRs is the set of files matching `docs/adr/*.md`** in\n-  this directory. Browse `docs/adr/*.md` for the authoritative, current set.\n-- Use [`template.md`](template.md) as the starting point for a new ADR.\n-- Record an ADR when a decision is hard to reverse, surprising without context,\n-  and a real tradeoff. Do not write an ADR for routine or easily reversible\n-  choices.\n+The same four-digit number may appear once in each namespace. A platform\n+`SADR-NNNN` citation resolves only to `SADR-NNNN-*.md`; a target `ADR NNNN`\n+citation resolves only to `NNNN-*.md`.\n \n-Using the `docs/adr/*.md` glob as the source of truth means other PRs can add an\n-ADR without editing this index file, avoiding merge contention on a\n-hand-maintained enumeration.\n+## How Decision Records Work\n \n-## Current ADRs (snapshot)\n-\n-The list below is a convenience snapshot and may lag behind the directory. When\n-in doubt, list `docs/adr/*.md`.\n+- New Shiki platform decisions use the next zero-padded SADR number.\n+- New target product decisions use the target's own ADR sequence.\n+- The canonical list is the files in `docs/adr/`; no hand-maintained index is\n+  authoritative.\n+- Use [`template.md`](template.md) as the starting point and choose the filename\n+  namespace that owns the decision.\n+- Record a decision when it is hard to reverse, surprising without context, and\n+  a real tradeoff. Do not write a decision record for routine or easily\n+  reversible choices.\n \n-- [0001 \u2014 Keep Shiki As The Platform Name](0001-keep-shiki-as-platform-name.md)\n-- [0002 \u2014 Use AGENTS As The Shared Agent Constitution](0002-use-agents-as-shared-constitution.md)\n-- [0003 \u2014 Require Skill Gate For MergeGate Readiness](0003-require-skill-gate-for-mergegate-readiness.md)\n-- [0004 \u2014 Use GitHub First With A Repository-Local Shiki Mirror](0004-use-github-first-with-shiki-mirror.md)\n-- [0005 \u2014 Use Codex As Front And Claude Code Action As The GitHub Runtime](0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md)\n-- [0006 \u2014 Separate Codex Implementation, CCA Completion, And MergeGate Authorization](0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md)\n+Using the directory as the source of truth lets independent changes add records\n+without contending on an enumerated index.\n \n-## Adding A New ADR\n+## Adding A Shiki SADR\n \n-1. Copy `template.md` to `docs/adr/NNNN-<short-slug>.md`.\n+1. Copy `template.md` to `docs/adr/SADR-NNNN-<short-slug>.md`.\n 2. Fill in the title, status, context, decision, and consequences.\n-3. Set the status to `Proposed`, then `Accepted` once the decision is approved.\n-4. Open a PR following `CONTRIBUTING.md`. You do not need to edit this index for\n-   the ADR to be discoverable via `docs/adr/*.md`.\n+3. Set the status to `Proposed`, then `Accepted` once the proper authority has\n+   approved the decision.\n+4. Open a PR following `CONTRIBUTING.md`.\n+\n+## Adding A Target ADR\n+\n+1. Copy `template.md` to `docs/adr/NNNN-<short-slug>.md` using the target's own\n+   sequence.\n+2. Cite it as `ADR NNNN`; do not prefix its filename with `SADR-`.\ndiff --git a/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md b/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\nnew file mode 100644\nindex 0000000000000000000000000000000000000000..7455ce1f3bed02ddddb851ac21a1d1a648c9aeba\n--- /dev/null\n+++ b/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\n@@ -0,0 +1,55 @@\n+# SADR-0019: Separate Shiki Platform SADRs From Target ADRs\n+\n+## Status\n+\n+Accepted\n+\n+## Context\n+\n+Shiki installs its platform decision records into `docs/adr/` in every target\n+repository. Shiki and its targets previously used the same numeric\n+`NNNN-<slug>.md` namespace. A target that already owned ADRs 0013 through 0018\n+therefore acquired two records for each number during installation, making an\n+`ADR NNNN` citation ambiguous. Renumbering target decisions would rewrite target\n+history, while renumbering Shiki decisions per target would destroy their stable\n+platform identity.\n+\n+Upgrades also need to remove the former Shiki-owned numeric paths without ever\n+mistaking a target-owned ADR for platform content. The install stamp already\n+records exact shipped paths and content digests, so it can prove ownership only\n+when both still match.\n+\n+## Decision\n+\n+Shiki platform decisions use the `SADR` namespace:\n+\n+- records are named `docs/adr/SADR-NNNN-<slug>.md`;\n+- live platform citations use `SADR-NNNN`;\n+- target product decisions retain `docs/adr/NNNN-<slug>.md` and `ADR NNNN`;\n+- the reference validator resolves the two namespaces independently, so the same\n+  four-digit number may exist once in each namespace.\n+\n+Before a forced upgrade writes anything, the installer inventories the exact\n+former numeric Shiki ADR paths. It may delete a legacy path only when the\n+existing install stamp contains that exact path and its recorded SHA-256 digest\n+matches the file currently on disk. All ownership blockers are reported\n+together and abort the upgrade before its first write. A non-forced install\n+never performs this cleanup.\n+\n+Historical `.shiki/` evidence remains unchanged because it records the namespace\n+that existed when that evidence was produced.\n+\n+## Consequences\n+\n+- Targets may use any ADR number without colliding with Shiki's platform SADRs.\n+- Fresh installs preserve target ADR bytes and install a stable SADR set.\n+- Forced upgrades fail closed when legacy ownership cannot be proven, and a\n+  successful upgrade refreshes the stamp without removed legacy paths.\n+- Current governance, packaging, workflow, prompt, script, and skill references\n+  must move with the platform records.\n+\n+## Alternatives Considered\n+\n+- Collision-only refusal leaves affected targets permanently unupgradable.\n+- Renumbering target ADRs mutates target-owned history.\n+- Per-target Shiki renumbering makes platform citations non-portable.\ndiff --git a/docs/adr/template.md b/docs/adr/template.md\nindex a92a97daddd0b00ab9dd626cc8d86942da2eafed..17dd79175b5fee2105935557f691b40cf9492942\n--- a/docs/adr/template.md\n+++ b/docs/adr/template.md\n@@ -1,14 +1,14 @@\n-# NNNN: <Short Decision Title>\n+# <SADR-NNNN or NNNN>: <Short Decision Title>\n \n ## Status\n \n-Proposed | Accepted | Superseded by ADR-NNNN\n+Proposed | Accepted | Superseded by SADR-NNNN or target ADR NNNN\n \n ## Context\n \n What is the situation, constraint, or force that requires a decision? Reference\n-relevant domain terms (`CONTEXT.md`), prior ADRs, and code reality. State the\n-problem, not the solution.\n+relevant domain terms (`CONTEXT.md`), prior Shiki SADRs or target ADRs, and code\n+reality. State the problem, not the solution.\n \n ## Decision\n \ndiff --git a/docs/agents/autonomous-loop-e2e-acceptance.md b/docs/agents/autonomous-loop-e2e-acceptance.md\nindex bbf84c6081987f159ac6ad9ecf3c82b7aa72e7e8..5c13c1d02a05a90b0dc46cbef7c95a9846d0c237\n--- a/docs/agents/autonomous-loop-e2e-acceptance.md\n+++ b/docs/agents/autonomous-loop-e2e-acceptance.md\n@@ -40,8 +40,8 @@\n 3. `gh auth status` is logged in with merge permission on the repo.\n 4. Branch protection + required checks are configured exactly as the goal-loop\n    required-check set expects (see `docs/agents/branch-protection-smoke.md`).\n-5. The repo `.shiki/policy` permits autonomous low/medium-risk merges (ADR\n-   0008/0009); the MergeGate policy check is the recorded-authority gate.\n+5. The repo `.shiki/policy` permits autonomous low/medium-risk merges\n+   (SADR-0008/0009); the MergeGate policy check is the recorded-authority gate.\n \n The operator's only actions are: register the task and start the loop. **No\n manual pre-clearing** means after `shiki loop run` starts, the operator does not\ndiff --git a/docs/agents/bootstrap-command.md b/docs/agents/bootstrap-command.md\nindex 05f7f203a52cf412b9f97084a6e131ed0f785afd..f7aa0b734dfebee4a3b139335b66ee3415f48713\n--- a/docs/agents/bootstrap-command.md\n+++ b/docs/agents/bootstrap-command.md\n@@ -189,7 +189,9 @@\n ```\n \n Use this only for tests, fixtures, or explicit local-only template inspection.\n-Use `--force` only when you intentionally want to overwrite existing target files.\n+Use `--force` only when you intentionally want to refresh the target template.\n+Legacy numeric Shiki decision cleanup is stamp-and-digest authorized before any\n+write; target ADR paths are not cleanup candidates.\n \n ## Slash Command\n \ndiff --git a/docs/agents/checklists.md b/docs/agents/checklists.md\nindex 4a7f03ade346f53a1c1d82981f3fd744f9ab4547..0c5ec6c53dc7197b7b7a7d18e667b325babd6c45\n--- a/docs/agents/checklists.md\n+++ b/docs/agents/checklists.md\n@@ -32,18 +32,18 @@\n | ID | Item | Blocking |\n | --- | --- | --- |\n | GD-01 | Plan was challenged against `CONTEXT.md`. | blocking for non-trivial Goals |\n-| GD-02 | Relevant ADRs were read or absence was recorded. | blocking for non-trivial Goals |\n+| GD-02 | Relevant SADRs and target ADRs were read or absence was recorded. | blocking for non-trivial Goals |\n | GD-03 | Ambiguous terms were resolved or converted to HITL blockers. | blocking |\n | GD-04 | Edge scenarios were considered. | advisory |\n | GD-05 | Code reality was checked when code could answer. | blocking when applicable |\n | GD-06 | New domain terms were added to `CONTEXT.md` when needed. | blocking when applicable |\n-| GD-07 | ADR was created or explicitly skipped using the ADR criteria. | blocking when applicable |\n+| GD-07 | A SADR or target ADR was created or explicitly skipped using the decision-record criteria. | blocking when applicable |\n \n ## CI \u2014 Context & Impact Checklist\n \n | ID | Item | Blocking |\n | --- | --- | --- |\n-| CI-01 | Relevant docs and ADRs identified. | blocking |\n+| CI-01 | Relevant docs, SADRs, and target ADRs identified. | blocking |\n | CI-02 | Relevant modules, interfaces, seams, callers, and tests identified. | blocking |\n | CI-03 | Dependencies and blocked-by relationships identified. | blocking |\n | CI-04 | Candidate locks identified. | blocking |\n@@ -62,7 +62,7 @@\n | PRD-04 | Implementation decisions are recorded without volatile code detail. | blocking |\n | PRD-05 | Testing decisions are recorded. | blocking |\n | PRD-06 | Out-of-scope items are explicit. | blocking |\n-| PRD-07 | Relevant glossary/ADR links are present when applicable. | blocking |\n+| PRD-07 | Relevant glossary/SADR/target-ADR links are present when applicable. | blocking |\n \n ## SF \u2014 Spec Freeze Checklist\n \n@@ -98,7 +98,7 @@\n | H-02 | Branch/worktree target is present. | blocking |\n | H-03 | Scope and non-goals are present. | blocking |\n | H-04 | Dependencies and locks are present. | blocking |\n-| H-05 | Relevant docs, ADRs, modules, and tests are referenced. | blocking |\n+| H-05 | Relevant docs, SADRs, target ADRs, modules, and tests are referenced. | blocking |\n | H-06 | Required skills are stated. | blocking |\n | H-07 | TDD expectations are stated. | blocking when behavior work |\n | H-08 | Acceptance criteria are included. | blocking |\ndiff --git a/docs/agents/codex-handoff.md b/docs/agents/codex-handoff.md\nindex ede468f47cb277f69a857e8c45df703e4ed0151d..6f97bdffb67e44d7edea2c71e01ea28933c4eb6c\n--- a/docs/agents/codex-handoff.md\n+++ b/docs/agents/codex-handoff.md\n@@ -46,7 +46,7 @@\n \n ## Context\n - Domain terms:\n-- Relevant ADRs:\n+- Relevant SADRs and target ADRs:\n - Relevant docs:\n - Relevant modules/interfaces/seams:\n - Similar tests/prior art:\ndiff --git a/docs/agents/completion-check-agent.md b/docs/agents/completion-check-agent.md\nindex 13e4c035db35025f4e3cdcc5adfb24314ac4d521..61bfa1218cf36ca605250c91919e620f33238aa0\n--- a/docs/agents/completion-check-agent.md\n+++ b/docs/agents/completion-check-agent.md\n@@ -32,7 +32,7 @@\n - `AGENTS.md`\n - `CLAUDE.md`\n - `CONTEXT.md`\n-- relevant ADRs\n+- relevant SADRs and target ADRs\n - parent Goal issue\n - PRD issue or document\n - task issue\ndiff --git a/docs/agents/decision-control.md b/docs/agents/decision-control.md\nindex 7d8ee5ab03a5ccbcbd04056ae61de03785a2a9c9..8c94ecda00fffaa0e92c325ab83aa9776bd1dac1\n--- a/docs/agents/decision-control.md\n+++ b/docs/agents/decision-control.md\n@@ -11,7 +11,7 @@\n | Decision | Owner | When | Required inputs | Output | Enforcement surface |\n | --- | --- | --- | --- | --- | --- |\n | Is the Goal clear enough to plan? | Planner + Guardian when needed | Goal intake | User request, existing docs, risk signals | Goal readiness verdict | Goal checklist, issue template, ledger |\n-| Must `grill-with-docs` run? | Planner / Skill Gate | Before PRD or issue split | Ambiguity, domain terms, ADR conflicts, unclear boundaries | Skill invocation or explicit skip rationale | Skill Gate checklist |\n+| Must `grill-with-docs` run? | Planner / Skill Gate | Before PRD or issue split | Ambiguity, domain terms, SADR/target-ADR conflicts, unclear boundaries | Skill invocation or explicit skip rationale | Skill Gate checklist |\n | Is a PRD required? | Planner | After Goal context stabilizes | Goal, `grill-with-docs` output, Context & Impact output | PRD required / not required | PRD checklist, CCA evidence check |\n | Can the spec be frozen? | Operator only | End of Requirements Definition, before issue split | PRD/requirements summary, scope inventory (SF-02), open-question list | `spec_freeze` block in the plan + ledger evidence | `require_grilled_plan` (plan ingest/run), SF checklist |\n | Can a frozen spec change? | Operator only (Spec Amendment) | When implementation contradicts the frozen spec | Contested decisions, scoped re-grill result | recorded amendment + re-stamped freeze | SF checklist, ledger evidence |\ndiff --git a/docs/agents/domain.md b/docs/agents/domain.md\nindex 258da8ea81e87ac0a6b0b5b5e5c2fde980eaabb7..d57b0cf17ff3e1fa9e6e5885372bf083d8295421\n--- a/docs/agents/domain.md\n+++ b/docs/agents/domain.md\n@@ -5,7 +5,9 @@\n ## Layout\n \n - `CONTEXT.md` defines Shiki language. Keep it implementation-free.\n-- `docs/adr/` records hard-to-reverse decisions.\n+- `docs/adr/SADR-NNNN-*.md` records Shiki platform decisions.\n+- `docs/adr/NNNN-*.md` records target product decisions without sharing the\n+  SADR namespace.\n - `AGENTS.md` is the runtime-neutral agent constitution.\n - `CLAUDE.md` is the Claude Code wrapper around `AGENTS.md`.\n - `.shiki/` is the Target Repository mirror for Goals, PRDs, plans, Task DAGs, contracts, locks, worktree records, ledger entries, CCA verdicts, repair packets, reports, and handoffs.\n@@ -15,6 +17,6 @@\n \n - Use Shiki terms exactly when they exist.\n - Add glossary terms to `CONTEXT.md` only when the term is part of the domain, not a generic programming concept.\n-- Create ADRs only for hard-to-reverse, surprising, tradeoff-based decisions.\n+- Create SADRs or target ADRs only for hard-to-reverse, surprising, tradeoff-based decisions.\n - Keep product-specific language in the Target Repository, not in Shiki Template docs.\n-- If an ADR conflicts with a proposed implementation, surface the conflict before editing.\n+- If a SADR or target ADR conflicts with a proposed implementation, surface the conflict before editing.\ndiff --git a/docs/agents/execution-control.md b/docs/agents/execution-control.md\nindex ad8ee3cdb36be56fedc1f46408ca92b387d74047..bc63c3a73661d12b567016ba299f1a6b66bf9088\n--- a/docs/agents/execution-control.md\n+++ b/docs/agents/execution-control.md\n@@ -63,7 +63,7 @@\n | Branch name | Orchestrator | Orchestrator | Before implementer dispatch | branch policy | branch ref in task/ledger |\n | Lock reservation | Context & Impact | MergeGate | Before dispatch and before merge | `.shiki/locks/`, labels, PR metadata | lock id, owner, scope |\n | CI profile | Planner | MergeGate | During issue triage, rechecked at PR | labels, `.shiki/policy.yaml`, workflow matrix | required check list |\n-| Required status checks | Repo maintainer / Guardian | GitHub branch protection / rulesets | Repository setup, updated via ADR | GitHub settings | branch/ruleset config, ADR |\n+| Required status checks | Repo maintainer / Guardian | GitHub branch protection / rulesets | Repository setup, updated via target ADR | GitHub settings | branch/ruleset config, target ADR |\n | CCA judgment | CCA | GitHub required check / MergeGate | Every non-draft PR update | GitHub Action check | structured CCA verdict |\n | Repair assignment | CCA | MergeGate | After failed CCA/CI/review | repair packet, issue/PR comment | repair id and attempt count |\n | Merge readiness | MergeGate | GitHub branch protection / Guardian if needed | After CCA complete | required checks, review rules, rulesets | mergegate check output |\ndiff --git a/docs/agents/guardian-policy.md b/docs/agents/guardian-policy.md\nindex 4147c7adf05a47adb4fd24ada1a1c2e1f2ba6d2d..a20d9cc6c00a569bb057b9ca8d6b51c7fc4a9847\n--- a/docs/agents/guardian-policy.md\n+++ b/docs/agents/guardian-policy.md\n@@ -98,8 +98,8 @@\n `origin/<default_branch>` and reuses each job's existing base fetch; a shallow\n (`--depth`) or second base fetch would make the proof unresolvable and silently\n disable the carry, so neither is permitted. CCA reads `guardian_comment_carried` as\n-a recorded authority in the deterministic signal and does not re-derive it. See ADR\n-0018 for the full decision, the rejected diff/patch-equality and path-disjointness\n+a recorded authority in the deterministic signal and does not re-derive it. See\n+SADR-0018 for the full decision, the rejected diff/patch-equality and path-disjointness\n alternatives, and the accepted residual.\n \n ## External AI Guardian Review (SADR-0010)\ndiff --git a/docs/agents/implementation-policy.md b/docs/agents/implementation-policy.md\nindex c754144a9fb5bf4a38aa82a60e1670c59e626b44..85c37281bcad196c6b7b02343faf00b03cd9c57a\n--- a/docs/agents/implementation-policy.md\n+++ b/docs/agents/implementation-policy.md\n@@ -31,10 +31,13 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n ## Non-Negotiables\n \n 1. **Planning precedes implementation.** Non-trivial Goals start with `grill-with-docs` and Context & Impact.\n-2. **Domain language is controlled.** Terms must be checked against `CONTEXT.md`; hard-to-reverse tradeoffs must be checked against ADRs.\n+2. **Domain language is controlled.** Terms must be checked against `CONTEXT.md`; hard-to-reverse tradeoffs must be checked against Shiki SADRs and target ADRs.\n 3. **PRD precedes issue decomposition.** `to-prd` is used once the context is settled enough to become durable intent.\n 4. **Issues are vertical slices.** `to-issues` must produce independently grabbable tracer-bullet tasks, not horizontal layer tickets.\n 5. **Triage controls dispatch.** Only `ready-for-agent` / AFK tasks may be assigned to Codex without human input.\n@@ -95,19 +98,19 @@\n Process:\n \n - Challenge terms against `CONTEXT.md`.\n-- Check ADRs before reopening decisions.\n+- Check SADRs and target ADRs before reopening decisions.\n - Inspect code when code can answer a question.\n - Ask one question at a time when the operator is available.\n - Provide a recommended answer with each question.\n - Record resolved terms in `CONTEXT.md`.\n-- Create ADRs only for hard-to-reverse, surprising, tradeoff-based decisions.\n+- Create SADRs or target ADRs only for hard-to-reverse, surprising, tradeoff-based decisions.\n - Convert unresolved load-bearing questions into HITL blockers.\n \n Outputs:\n \n - Resolved design-tree decisions.\n - Updated glossary terms when needed.\n-- ADRs when justified.\n+- SADRs or target ADRs when justified.\n - HITL blockers when decisions remain unresolved.\n \n ## Phase 3 \u2014 Context & Impact\n@@ -117,7 +120,7 @@\n \n Outputs must include:\n \n-- Relevant documents and ADRs.\n+- Relevant documents, SADRs, and target ADRs.\n - Relevant modules, interfaces, seams, callers, and tests.\n - Candidate locks.\n - Dependencies.\n@@ -140,7 +143,7 @@\n - Testing decisions.\n - Out of scope.\n - Further notes.\n-- Links to relevant Goal, domain terms, and ADRs.\n+- Links to relevant Goal, domain terms, SADRs, and target ADRs.\n \n Phase 4 ends with Spec Freeze: the operator explicitly approves the PRD, the\n plan records a `spec_freeze` block (status, approved_by, source), and the SF\n@@ -240,7 +243,7 @@\n - Dependencies and locks.\n - Risk and Guardian requirements.\n - Ledger completeness.\n-- Documentation and ADR updates.\n+- Documentation and SADR/target-ADR updates.\n - Scope drift.\n \n CCA emits one of:\n@@ -301,7 +304,7 @@\n | Evidence failure | Behavior might be done, but proof is missing. | Add checks, PR evidence, ledger entry, or artifacts. |\n | Scope drift | PR changes unrelated behavior. | Revert unrelated changes or split into new Goal. |\n | Dependency block | Upstream task or lock not complete. | Block until dependency resolves. |\n-| Architecture block | Decision needs ADR or deep-module discussion. | `grill-with-docs` or `improve-codebase-architecture`. |\n+| Architecture block | Decision needs a SADR, target ADR, or deep-module discussion. | `grill-with-docs` or `improve-codebase-architecture`. |\n | Risk block | Security/production/policy change needs human approval. | Guardian decision. |\n \n ## Phase 5.5 \u2014 Execution Decision Control\ndiff --git a/docs/agents/runtime-auth-model.md b/docs/agents/runtime-auth-model.md\nindex 6a2e1a47e81ea89b5c68a210ec07772a9374babc..3c54748d24233dbd0ba8473950282547ef4ce935\n--- a/docs/agents/runtime-auth-model.md\n+++ b/docs/agents/runtime-auth-model.md\n@@ -148,7 +148,7 @@\n \n API-key based runners may be added by a target repository as an explicit extension. They are not the default Shiki template path.\n \n-If a repo opts into API-key mode, record the exception in an ADR and update the repo's `AGENTS.md`, `.shiki/config.yaml`, and workflow permissions.\n+If a repo opts into API-key mode, record the target-specific exception in a target ADR and update the repo's `AGENTS.md`, `.shiki/config.yaml`, and workflow permissions.\n \n ## GitHub Actions Runtime Compatibility\n \ndiff --git a/docs/agents/skill-gate.md b/docs/agents/skill-gate.md\nindex c50398b0d7c60a8598f2a5c88a10015730dced13..3acffe6444617bb1d54b77d4b4708158ec735bc6\n--- a/docs/agents/skill-gate.md\n+++ b/docs/agents/skill-gate.md\n@@ -9,7 +9,7 @@\n | Skill | Trigger | Expected output |\n | --- | --- | --- |\n | `setup-matt-pocock-skills` | First configuring a repo for these skills, or when issue tracker, triage labels, or domain docs are missing. | `docs/agents/` configuration and an `Agent skills` block in `AGENTS.md` or `CLAUDE.md`. |\n-| `grill-with-docs` | Ambiguous plan, unclear terminology, boundary decision, tradeoff, or ADR-worthy design choice. | Clarified decision tree, recommended answers, updates to `CONTEXT.md` or ADRs when decisions crystallize. |\n+| `grill-with-docs` | Ambiguous plan, unclear terminology, boundary decision, tradeoff, or decision-record-worthy design choice. | Clarified decision tree, recommended answers, updates to `CONTEXT.md`, SADRs, or target ADRs when decisions crystallize. |\n | `zoom-out` | Agent lacks a higher-level architectural map. | Map of relevant modules, callers, seams, and domain concepts. |\n | `to-prd` | Settled context should become a PRD. | PRD published to the issue tracker with problem, solution, user stories, decisions, testing, and out-of-scope notes. |\n | `to-issues` | Goal, PRD, or plan must become implementation tickets. | Vertical-slice issues with acceptance criteria, dependencies, and AFK/HITL classification. |\n@@ -17,7 +17,7 @@\n | `tdd` | Feature work or bug fix has observable behavior that can be specified with tests. | Red-green-refactor loop through public interfaces, one vertical slice at a time. |\n | `code-review` | The TDD loop is green and the implementer is about to create or update a PR. Default for every implementation task. | Pre-PR self-review of the task diff with fixes applied on the task branch; findings and resolutions recorded as a ledger entry naming the skill and a `## Pre-PR code review` PR body section. |\n | `diagnose` | Hard bug, failing check, regression, flaky behavior, or performance problem. | Reproduction loop, ranked hypotheses, instrumentation, fix, regression test, post-mortem. |\n-| `improve-codebase-architecture` | Structural friction, testability, AI-navigability, deep-module opportunity, or refactor request. | Architecture candidates and deepening recommendations informed by `CONTEXT.md` and ADRs. |\n+| `improve-codebase-architecture` | Structural friction, testability, AI-navigability, deep-module opportunity, or refactor request. | Architecture candidates and deepening recommendations informed by `CONTEXT.md`, SADRs, and target ADRs. |\n | `prototype` | A throwaway logic or UI prototype is needed to answer a design question. | Clearly marked disposable prototype and captured decision. |\n \n ## MergeGate Interaction\ndiff --git a/docs/operations.md b/docs/operations.md\nindex cf1f452d4c589e38ccd58553bda0ad8684b51eb7..ace48a90ffbec9e5c0a7113bef16a4858bc44b3f\n--- a/docs/operations.md\n+++ b/docs/operations.md\n@@ -89,6 +89,16 @@\n      target history, brought forward by `shiki migrate`, not a template.\n    - **Everything else** (CLI scripts, workflows, schemas, docs) is overwritten.\n \n+   A forced upgrade also migrates the former numeric Shiki decision paths to\n+   `SADR-NNNN-*`. Before the first write, the installer inventories the exact\n+   legacy Shiki paths and authorizes each deletion only when the existing\n+   `.shiki/install-stamp.json` names that path and its SHA-256 digest still\n+   matches the file. An absent, unreadable, malformed, incomplete, or\n+   mismatching stamp reports every blocked path and stops with no filesystem\n+   change. Target-owned `NNNN-*` ADRs are not cleanup candidates. A non-force\n+   install deletes nothing and retains any still-matching legacy stamp entries\n+   so a later forced upgrade can prove ownership.\n+\n    If the target has pending migrations, `--force` refuses and writes nothing \u2014\n    apply migrations first (step 3), then re-run. The run ends with a single\n    summary of every `.new` written, so a half-upgrade is visible.\ndiff --git a/docs/releasing.md b/docs/releasing.md\nindex 10d73828e7dbd64e9d9f6b83bc84d010537ce62a..aafeee8c4d441e1e427efee2ce4f81a647f020e1\n--- a/docs/releasing.md\n+++ b/docs/releasing.md\n@@ -67,7 +67,7 @@\n CLI in `scripts/`. There are no third-party Python runtime dependencies. The CLI\n is intentionally **not** exposed as a `[project.scripts]` console entry point\n because `scripts/shiki.py` is a dependency-free shim and `scripts/` is not\n-packaged as an importable distribution (see the ADR for the rationale).\n+packaged as an importable distribution (see SADR-0007 for the rationale).\n \n Supported install paths:\n \n@@ -155,6 +155,15 @@\n    [docs/operations.md](operations.md#the-new-convention). If the target has\n    pending migrations, `--force` refuses and writes nothing; apply migrations\n    (step 2) first, then re-run.\n+\n+   The SADR namespace upgrade has an additional pre-write gate: each former\n+   numeric Shiki decision path is removed only when the existing install stamp\n+   contains that exact path and a digest matching its current bytes. The\n+   installer aggregates absent/unreadable/malformed-stamp, missing-digest, and\n+   digest-mismatch blockers before raising, so refusal leaves the target tree\n+   byte-identical. Numeric target ADRs are preserved. A successful forced\n+   upgrade installs and stamps `SADR-*` paths and drops the removed legacy paths\n+   from the refreshed stamp.\n 2. Apply repository-local `.shiki/` state migrations with the migrate command:\n \n    ```bash\ndiff --git a/docs/usage.md b/docs/usage.md\nindex b83407ee3970dd395dd893010bc5dbe8bad26051..a130ef6e40dfd8169a5bc701339351097df1a09f\n--- a/docs/usage.md\n+++ b/docs/usage.md\n@@ -67,8 +67,10 @@\n shiki install-target /path/to/target-repo --local-only\n ```\n \n-Use `--force` only when you intentionally want to overwrite existing target\n-files.\n+Use `--force` only when you intentionally want to refresh the target template.\n+For the SADR namespace migration, `--force` deletes only exact legacy Shiki\n+decision paths whose existing install-stamp digest still matches; any ownership\n+blocker stops before the first write. Numeric target ADRs are never candidates.\n \n ## `shiki bootstrap-platform` \u2014 publish the Shiki platform repo\n \ndiff --git a/scripts/guardian_approval_signal.py b/scripts/guardian_approval_signal.py\nindex d059f92b2c82aa2b5d6ca421365537a87886c1cb..16924892714f4f5b9303c0ef0e325a7e70075630\n--- a/scripts/guardian_approval_signal.py\n+++ b/scripts/guardian_approval_signal.py\n@@ -199,8 +199,8 @@\n ) -> Any:\n     \"\"\"Evaluate SADR-0015 Contract Approval for the CCA Guardian signal.\n \n-    The SAME OR that MergeGate applies must reach this deterministic signal: ADR\n-    0015 is explicit that without it, a contract-approved PR still returns\n+    The SAME OR that MergeGate applies must reach this deterministic signal:\n+    SADR-0015 is explicit that without it, a contract-approved PR still returns\n     ``needs_guardian``, the CCA job fails, and the MergeGate policy check never\n     runs \u2014 the gate this change moves would never be reached. This resolves the\n     task the SAME way MergeGate does (the first Shiki task id in the PR body),\ndiff --git a/scripts/shiki_contract_approval.py b/scripts/shiki_contract_approval.py\nindex 941bcb96fdd8b9a85bc0c4f0f04d5dad9d04839c..4a4b418f6527413161db82670bec857a043360e2\n--- a/scripts/shiki_contract_approval.py\n+++ b/scripts/shiki_contract_approval.py\n@@ -142,7 +142,7 @@\n     changed_files_status: Any,\n     registration: Any,\n ) -> ContractApprovalResult:\n-    \"\"\"Whether this PR carries an SADR-0015 Contract Approval.\n+    \"\"\"Whether this PR carries a SADR-0015 Contract Approval.\n \n     The four conditions are evaluated IN ORDER, each failure recorded as a\n     distinct reason string:\ndiff --git a/scripts/shiki_guardian.py b/scripts/shiki_guardian.py\nindex a4ecb79bbda55fd5c8b06adfae2eb99760a03c45..4dd99bbe714e26f421fa19abac7a34ae1b195ecc\n--- a/scripts/shiki_guardian.py\n+++ b/scripts/shiki_guardian.py\n@@ -12,7 +12,7 @@\n # The carry re-derives its proof from git in-process on every judging invocation;\n # it is never read from an artifact, so there is nothing to go stale or be forged.\n # shiki_sync_proof is the ONLY guardian-family module that runs git, and every one\n-# of its invocations is read-only and local (ADR: prove-from-git base sync).\n+# of its invocations is read-only and local (SADR: prove-from-git base sync).\n from shiki_sync_proof import verify_pure_base_sync\n \n GUARDIAN_POLICY_PATH = \".shiki/guardian-policy.json\"\ndiff --git a/scripts/shiki_guardian_review.py b/scripts/shiki_guardian_review.py\nindex dd0f104d457899e0ba5ff1e162011c1e90b6f92b..e10ef58c046324e7ea3a3392feb3048ca77414ce\n--- a/scripts/shiki_guardian_review.py\n+++ b/scripts/shiki_guardian_review.py\n@@ -159,7 +159,7 @@\n     docs_only = _is_docs_only(changed_files)\n     if docs_only and matched == 0:\n         focus.append(\n-            \"Docs/ADR-only change: confirm the change is genuinely documentation/decision-only, introduces no behavior or contract drift, and stays consistent with the constitution and existing ADRs.\"\n+            \"Docs/decision-record-only change: confirm the change is genuinely documentation/decision-only, introduces no behavior or contract drift, and stays consistent with the constitution and existing SADRs and target ADRs.\"\n         )\n \n     # Conservative augmentation: unknown (no category and not docs-only), mixed\ndiff --git a/scripts/shiki_installer.py b/scripts/shiki_installer.py\nindex be064ab7a29ff6836f7f7a1d70500a43e0d11d7b..9fd8eefd137ce7003dad5c0dcb2e48e2cd47ab32\n--- a/scripts/shiki_installer.py\n+++ b/scripts/shiki_installer.py\n@@ -118,6 +118,7 @@\n # what lets a target carry it without a governance-schema change.\n INSTALL_STAMP_PATH = \".shiki/install-stamp.json\"\n INSTALL_STAMP_VERSION = 1\n+LEGACY_SHIKI_SADR_MAX = 18\n \n # Under --force the shipped surface splits three ways, so nothing is lost and\n # nothing is silently stale.\n@@ -237,9 +238,98 @@\n         )\n \n \n+def legacy_shiki_adr_paths() -> tuple[str, ...]:\n+    \"\"\"Former numeric paths for Shiki SADR-0001 through SADR-0018.\n+\n+    The authoritative slugs come from the current SADR filenames. SADR-0019 is\n+    the namespace decision itself and never shipped under a numeric legacy path.\n+    \"\"\"\n+    paths: list[str] = []\n+    for sadr in sorted((ROOT / \"docs\" / \"adr\").glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\")):\n+        number = sadr.name.split(\"-\", 2)[1]\n+        if int(number) <= LEGACY_SHIKI_SADR_MAX:\n+            paths.append(f\"docs/adr/{sadr.name.removeprefix('SADR-')}\")\n+    return tuple(paths)\n+\n+\n+def preflight_legacy_sadr_cleanup(target: Path, *, force: bool) -> tuple[str, ...]:\n+    \"\"\"Authorize exact legacy Shiki ADR removals before the first write.\n+\n+    A non-force install never removes existing files. A forced install needs no\n+    stamp when no legacy Shiki path is present. When legacy paths do exist, each\n+    must be named in a readable install stamp and its current bytes must match\n+    the recorded digest.\n+    \"\"\"\n+    if not force:\n+        return ()\n+    present = tuple(\n+        relative\n+        for relative in legacy_shiki_adr_paths()\n+        if (target / relative).exists() or (target / relative).is_symlink()\n+    )\n+    if not present:\n+        return ()\n+\n+    stamp_path = target / INSTALL_STAMP_PATH\n+    try:\n+        raw = stamp_path.read_text(encoding=\"utf-8\")\n+        stamp = json.loads(raw)\n+    except FileNotFoundError:\n+        detail = \"install stamp is absent\"\n+        blockers = [f\"{relative}: {detail}\" for relative in present]\n+    except (OSError, UnicodeDecodeError) as error:\n+        detail = f\"install stamp is unreadable ({error})\"\n+        blockers = [f\"{relative}: {detail}\" for relative in present]\n+    except json.JSONDecodeError as error:\n+        detail = f\"install stamp is malformed ({error})\"\n+        blockers = [f\"{relative}: {detail}\" for relative in present]\n+    else:\n+        if not isinstance(stamp, dict):\n+            blockers = [\n+                f\"{relative}: install stamp is malformed (expected an object)\"\n+                for relative in present\n+            ]\n+        else:\n+            digests = stamp.get(\"digests\")\n+            blockers = []\n+            if not isinstance(digests, dict):\n+                blockers.extend(\n+                    f\"{relative}: install stamp has no digest for this path\"\n+                    for relative in present\n+                )\n+            else:\n+                for relative in present:\n+                    expected = digests.get(relative)\n+                    if not isinstance(expected, str) or not expected:\n+                        blockers.append(\n+                            f\"{relative}: install stamp has no digest for this path\"\n+                        )\n+                        continue\n+                    try:\n+                        actual = _sha256_file(target / relative)\n+                    except OSError as error:\n+                        blockers.append(\n+                            f\"{relative}: legacy path is unreadable ({error})\"\n+                        )\n+                        continue\n+                    if actual != expected:\n+                        blockers.append(\n+                            f\"{relative}: digest mismatch (stamp {expected}, current {actual})\"\n+                        )\n+\n+    if blockers:\n+        joined = \"\\n\".join(f\"- {blocker}\" for blocker in blockers)\n+        raise ShikiError(\n+            \"legacy Shiki ADR cleanup is not authorized:\\n\"\n+            f\"{joined}\\nNo files were written.\"\n+        )\n+    return present\n+\n+\n def install_template(target: Path, *, force: bool, validate: bool) -> list[NewFileNote]:\n     # Preconditions first: never rewrite the tree of a target that cannot be\n     # upgraded cleanly (see refuse_pending_migrations).\n+    legacy_cleanup = preflight_legacy_sadr_cleanup(target, force=force)\n     refuse_pending_migrations(target)\n \n     # Resolve the target's required CODEOWNERS owner once from its\n@@ -250,6 +340,10 @@\n     # verbatim. See ``codeowners_required_owner``.\n     codeowners_owner = codeowners_required_owner(target)\n \n+    for relative in legacy_cleanup:\n+        (target / relative).unlink()\n+        info(f\"removed stamped legacy Shiki ADR: {target / relative}\")\n+\n     notes: list[NewFileNote] = []\n     for relative in TEMPLATE_PATHS:\n         source = ROOT / relative\n@@ -647,6 +741,22 @@\n     digests = target_stamp_digests(target)\n     existing = read_install_stamp(target)\n     if existing is not None:\n+        # A non-force install deliberately leaves legacy Shiki ADRs in place.\n+        # Preserve only still-matching ownership proof so a later forced\n+        # upgrade can authorize cleanup; never carry a missing or stale digest.\n+        existing_digests = existing.get(\"digests\")\n+        if isinstance(existing_digests, dict):\n+            for relative in legacy_shiki_adr_paths():\n+                expected = existing_digests.get(relative)\n+                path = target / relative\n+                if not isinstance(expected, str) or not path.is_file():\n+                    continue\n+                try:\n+                    actual = _sha256_file(path)\n+                except OSError:\n+                    continue\n+                if actual == expected:\n+                    digests[relative] = actual\n         if _incoming_is_older(commit, existing.get(\"platform_commit\")):\n             warn(\n                 \"kept existing install stamp: incoming install \"\ndiff --git a/scripts/shiki_loop.py b/scripts/shiki_loop.py\nindex 3611641da8aeb5038b1da90e3114b09a473ec6d8..84caa491b9cad0edc4176f209c13e9221e975681\n--- a/scripts/shiki_loop.py\n+++ b/scripts/shiki_loop.py\n@@ -424,7 +424,7 @@\n         # unresolvable CCA verdict has already produced its terminal stop and is never\n         # laundered into a head-moving sync (SADR-0010). It also never fires while the\n         # policy (Guardian) gate is red \u2014 for ANY reason, not only when it is the sole\n-        # red check \u2014 so a head-bound Guardian approval, or an SADR-0015\n+        # red check \u2014 so a head-bound Guardian approval, or a SADR-0015\n         # contract-approval carry that base movement defeated (the policy gate compares\n         # base and head task governance directly), is never silently restored by\n         # adopting base wholesale. It is bounded by a durable per-task counter so the\ndiff --git a/scripts/shiki_tasks.py b/scripts/shiki_tasks.py\nindex aec3809f24b4a0c8c96fc50971cff9ae9ad30381..d531052e9d9717058d4631483cefab44c26a65c1\n--- a/scripts/shiki_tasks.py\n+++ b/scripts/shiki_tasks.py\n@@ -821,7 +821,7 @@\n         \"prompt\": prompt,\n         \"entry_skill\": \"grill-with-docs\",\n         \"required_next_steps\": [\n-            \"Run grill-with-docs until terminology, boundaries, risks, and ADR-worthy decisions are settled.\",\n+            \"Run grill-with-docs until terminology, boundaries, risks, and decision-record-worthy choices are settled.\",\n             \"Obtain the operator's explicit approval of the PRD/requirements (Spec Freeze).\",\n             \"Write a machine-readable plan JSON with grill_with_docs.status=complete and spec_freeze.status=frozen.\",\n             \"Run shiki plan ingest --plan-file PLAN.json.\",\ndiff --git a/scripts/test_shiki_goal_loop.sh b/scripts/test_shiki_goal_loop.sh\nindex 753c7b516e10f0be3bc92a4bd0eb1fa699776ae6..8f3820a0a079b3044424cfe1a6c6b7fe68896e67\n--- a/scripts/test_shiki_goal_loop.sh\n+++ b/scripts/test_shiki_goal_loop.sh\n@@ -231,8 +231,8 @@\n \n # High/critical risk merges autonomously when all required checks are green:\n # the \"MergeGate policy check\" required check IS the Guardian gate (it enforces\n-# guardian-policy.json \u2014 human approval OR external AI guardian review, ADR\n-# 0010), so green checks mean a recorded authority approved. The fake gh returns\n+# guardian-policy.json \u2014 human approval OR external AI guardian review,\n+# SADR-0010), so green checks mean a recorded authority approved. The fake gh returns\n # green checks, simulating that the Guardian gate passed.\n python3 \"$ROOT/scripts/shiki.py\" goal create --target \"$TARGET\" --title \"Guardian gate\" --outcome \"High risk merges via policy gate\" >/tmp/shiki-goal-loop-guardian-goal.json\n GGOAL=\"$(json_get /tmp/shiki-goal-loop-guardian-goal.json goal_id)\"\ndiff --git a/tests/test_adr_references.py b/tests/test_adr_references.py\nindex 51706329c797d5dd9b47e082a333fa83b261d482..d005da4f771f5e8085ec22d4b787980e68f5ae8c\n--- a/tests/test_adr_references.py\n+++ b/tests/test_adr_references.py\n@@ -1,8 +1,8 @@\n-\"\"\"Every ``ADR ####`` citation must resolve to exactly one decision record.\n+\"\"\"Every target ADR and Shiki SADR citation resolves in its own namespace.\n \n Background. ``scripts/mergegate_check.py`` justifies ``is_bookkeeping_closeout``\n-with \"ADR 0017\" nine times and ``scripts/guardian_approval_signal.py`` cites\n-0016/0017 seven times, yet for a stretch the numbering in ``docs/adr/`` stopped\n+with \"SADR-0017\" nine times and ``scripts/guardian_approval_signal.py`` cites\n+SADR-0016/SADR-0017 seven times, yet for a stretch the numbering in ``docs/adr/`` stopped\n at 0015: the authoring commits lived on a coordinator branch and no task ever\n declared ``docs/adr/...`` in its locks, so nothing carried the records to the\n default branch. The stated basis of an exemption that merged seven PRs with zero\n@@ -11,11 +11,12 @@\n and the directory drifted with no signal.\n \n This is the same unbound-pair shape behind other silent failures: a reference\n-and its target with nothing asserting they agree. This test binds them. It scans\n-every ``scripts/*.py`` (plus ``docs/**/*.md`` and the root ``*.md`` governance\n-files, which are cheap) for the ``ADR ####`` pattern and asserts each cited\n-number resolves to exactly one ``docs/adr/####-*.md``. The converse is\n-deliberately NOT required: an ADR may exist without being cited.\n+and its target with nothing asserting they agree. This test binds them. Target\n+``ADR NNNN`` citations resolve only to ``docs/adr/NNNN-*.md``; Shiki\n+``SADR-NNNN`` citations resolve only to ``docs/adr/SADR-NNNN-*.md``. The scan\n+covers current governance docs, scripts, workflows, prompts, packaging metadata,\n+and repo-local Codex guidance while deliberately excluding historical\n+``.shiki`` evidence. The converse is not required: a record may be uncited.\n \n The suite is dependency free: standard-library ``unittest`` only.\n \"\"\"\n@@ -31,17 +32,18 @@\n \n REPO_ROOT = Path(__file__).resolve().parents[1]\n \n-# \"ADR 0016\" -- a four-digit citation. The trailing \\b keeps a five-digit token\n-# from being read as a truncated four-digit number.\n-_CITATION = re.compile(r\"\\bADR (\\d{4})\\b\")\n-# \"0016-locks-bound-product-paths.md\" -- the leading four digits name the record.\n-_ADR_FILENAME = re.compile(r\"^(\\d{4})-.*\\.md$\")\n+# ``ADR 0016`` / ``SADR-0016`` -- a namespace plus four-digit citation. The\n+# trailing ``\\b`` keeps a five-digit token from being read as a truncated one.\n+_CITATION = re.compile(r\"\\b(S?ADR)[ -](\\d{4})\\b\")\n+# Target ADRs retain the numeric filename; Shiki platform decisions use SADR.\n+_DECISION_FILENAME = re.compile(r\"^(?:(SADR)-)?(\\d{4})-.*\\.md$\")\n \n \n class Violation(NamedTuple):\n     \"\"\"A citation that does not resolve to exactly one decision record.\"\"\"\n \n     citing_file: str  # repo-relative path of the file that cites the ADR\n+    namespace: str  # \"ADR\" for target decisions; \"SADR\" for Shiki decisions\n     number: str  # the cited four-digit number, e.g. \"0016\"\n     line: int  # 1-based line of the citation\n     reason: str  # \"missing\" (zero matches) or \"ambiguous\" (two or more)\n@@ -49,44 +51,59 @@\n \n     @property\n     def expected_path(self) -> str:\n-        return f\"docs/adr/{self.number}-*.md\"\n+        prefix = \"SADR-\" if self.namespace == \"SADR\" else \"\"\n+        return f\"docs/adr/{prefix}{self.number}-*.md\"\n \n     def describe(self) -> str:\n         \"\"\"Name the citing file, the cited number, and the missing path.\"\"\"\n         if self.reason == \"missing\":\n             return (\n-                f\"{self.citing_file}:{self.line} cites ADR {self.number} \"\n+                f\"{self.citing_file}:{self.line} cites {self.namespace} {self.number} \"\n                 f\"but no document matches {self.expected_path}\"\n             )\n         joined = \", \".join(self.matches)\n         return (\n-            f\"{self.citing_file}:{self.line} cites ADR {self.number} which \"\n+            f\"{self.citing_file}:{self.line} cites {self.namespace} {self.number} which \"\n             f\"resolves to {len(self.matches)} documents (expected exactly one \"\n             f\"at {self.expected_path}): {joined}\"\n         )\n \n \n-def _adr_index(root: Path) -> dict[str, list[Path]]:\n-    \"\"\"Map every four-digit ADR number to the record files that claim it.\"\"\"\n-    index: dict[str, list[Path]] = {}\n+def _adr_index(root: Path) -> dict[tuple[str, str], list[Path]]:\n+    \"\"\"Map each decision namespace and number to the files that claim it.\"\"\"\n+    index: dict[tuple[str, str], list[Path]] = {}\n     adr_dir = root / \"docs\" / \"adr\"\n     if adr_dir.is_dir():\n         for path in sorted(adr_dir.glob(\"*.md\")):\n-            match = _ADR_FILENAME.match(path.name)\n+            match = _DECISION_FILENAME.match(path.name)\n             if match:\n-                index.setdefault(match.group(1), []).append(path)\n+                namespace = \"SADR\" if match.group(1) else \"ADR\"\n+                index.setdefault((namespace, match.group(2)), []).append(path)\n     return index\n \n \n def _citing_files(root: Path, include_docs: bool = True) -> list[Path]:\n-    \"\"\"Files scanned for citations: scripts/*.py, and cheaply the markdown.\"\"\"\n+    \"\"\"Current, shipped text surfaces scanned for decision citations.\"\"\"\n     files = sorted((root / \"scripts\").glob(\"*.py\"))\n+    files += sorted((root / \"scripts\").glob(\"*.sh\"))\n     if include_docs:\n         docs = root / \"docs\"\n         if docs.is_dir():\n             files += sorted(docs.rglob(\"*.md\"))\n         files += sorted(root.glob(\"*.md\"))\n-    return files\n+        pyproject = root / \"pyproject.toml\"\n+        if pyproject.is_file():\n+            files.append(pyproject)\n+        for relative in (\".github\", \".codex\"):\n+            surface = root / relative\n+            if surface.is_dir():\n+                files += sorted(\n+                    path\n+                    for path in surface.rglob(\"*\")\n+                    if path.is_file()\n+                    and path.suffix in {\".md\", \".py\", \".sh\", \".toml\", \".yaml\", \".yml\"}\n+                )\n+    return sorted(set(files))\n \n \n def find_adr_reference_violations(\n@@ -94,10 +111,9 @@\n ) -> list[Violation]:\n     \"\"\"Return every ``ADR ####`` citation under ``root`` that fails to resolve.\n \n-    A citation resolves when exactly one ``docs/adr/####-*.md`` matches its\n-    number. Zero matches is a \"missing\" violation; two or more is \"ambiguous\".\n-    Records that are never cited produce nothing -- the converse is not\n-    required.\n+    A citation resolves when exactly one record in the same namespace matches\n+    its number. Zero matches is a \"missing\" violation; two or more is\n+    \"ambiguous\". Records that are never cited produce nothing.\n     \"\"\"\n     root = Path(root)\n     index = _adr_index(root)\n@@ -110,13 +126,15 @@\n         rel = path.relative_to(root).as_posix()\n         for lineno, line in enumerate(text.splitlines(), start=1):\n             for match in _CITATION.finditer(line):\n-                number = match.group(1)\n-                found = index.get(number, [])\n+                namespace, number = match.groups()\n+                found = index.get((namespace, number), [])\n                 if len(found) == 1:\n                     continue\n                 reason = \"missing\" if not found else \"ambiguous\"\n                 rels = [p.relative_to(root).as_posix() for p in found]\n-                violations.append(Violation(rel, number, lineno, reason, rels))\n+                violations.append(\n+                    Violation(rel, namespace, number, lineno, reason, rels)\n+                )\n     return violations\n \n \n@@ -129,8 +147,8 @@\n         self.assertEqual(\n             violations,\n             [],\n-            \"Every 'ADR ####' citation must resolve to exactly one \"\n-            f\"docs/adr/####-*.md:\\n{detail}\",\n+            \"Every ADR/SADR citation must resolve exactly once in its own \"\n+            f\"docs/adr namespace:\\n{detail}\",\n         )\n \n     def test_scan_actually_reads_scripts(self) -> None:\n@@ -142,12 +160,38 @@\n             \"the citation scan must cover scripts/*.py\",\n         )\n \n+    def test_platform_citations_do_not_use_unresolved_numeric_shorthand(self) -> None:\n+        shorthand = re.compile(r\"SADR-\\d{4}/\\d{4}\")\n+        offenders = []\n+        for path in _citing_files(REPO_ROOT):\n+            text = path.read_text(encoding=\"utf-8\")\n+            if shorthand.search(text):\n+                offenders.append(path.relative_to(REPO_ROOT).as_posix())\n+        self.assertEqual(\n+            offenders,\n+            [],\n+            \"repeat the SADR prefix so every cited number is independently resolvable\",\n+        )\n+\n     def test_carried_records_are_indexed(self) -> None:\n         # The two records this Goal carried onto the default branch, and which\n         # the scripts cite most, must be present.\n         index = _adr_index(REPO_ROOT)\n-        self.assertIn(\"0016\", index)\n-        self.assertIn(\"0017\", index)\n+        self.assertIn((\"SADR\", \"0016\"), index)\n+        self.assertIn((\"SADR\", \"0017\"), index)\n+\n+    def test_platform_tree_has_only_sadr_0001_through_0019(self) -> None:\n+        adr_dir = REPO_ROOT / \"docs\" / \"adr\"\n+        sadrs = sorted(adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\"))\n+        self.assertEqual(\n+            {path.name.split(\"-\", 2)[1] for path in sadrs},\n+            {f\"{number:04d}\" for number in range(1, 20)},\n+        )\n+        self.assertEqual(\n+            list(adr_dir.glob(\"[0-9][0-9][0-9][0-9]-*.md\")),\n+            [],\n+            \"the Shiki platform tree must not retain legacy numeric SADR paths\",\n+        )\n \n \n class FixtureAdrReferences(unittest.TestCase):\n@@ -180,6 +224,26 @@\n         self.assertIn(\"9991\", message)  # cited number\n         self.assertIn(\"docs/adr/9991-*.md\", message)  # missing path\n \n+    def test_target_adr_does_not_satisfy_missing_platform_sadr(self) -> None:\n+        root = self._make_repo(\n+            adr_names=[\"0042-target-decision.md\"],\n+            script_body='\"\"\"Target ADR 0042 exists, but platform SADR-0042 does not.\"\"\"\\n',\n+        )\n+        violations = find_adr_reference_violations(root, include_docs=False)\n+        self.assertEqual(len(violations), 1)\n+        self.assertEqual(violations[0].namespace, \"SADR\")\n+        self.assertEqual(violations[0].number, \"0042\")\n+        self.assertEqual(violations[0].expected_path, \"docs/adr/SADR-0042-*.md\")\n+\n+    def test_target_adr_and_platform_sadr_may_reuse_a_number(self) -> None:\n+        root = self._make_repo(\n+            adr_names=[\"0042-target-decision.md\", \"SADR-0042-platform-decision.md\"],\n+            script_body='\"\"\"Target ADR 0042 and platform SADR-0042 are distinct.\"\"\"\\n',\n+        )\n+        self.assertEqual(\n+            find_adr_reference_violations(root, include_docs=False), []\n+        )\n+\n     def test_uncited_adr_is_allowed(self) -> None:\n         # An ADR exists but nothing cites it -> not a violation. The converse\n         # (every ADR must be cited) is explicitly NOT required.\ndiff --git a/tests/test_guardian_review.py b/tests/test_guardian_review.py\nindex 88829e7810212635e2fc98aa5918d6f26294677d..00a85a237acd5bf2d6cb5b294128198c422402ca\n--- a/tests/test_guardian_review.py\n+++ b/tests/test_guardian_review.py\n@@ -162,7 +162,7 @@\n \n     def test_docs_only_is_light_and_not_conservative(self):\n         focus = gr.classify_review_focus([\"docs/adr/0099-x.md\", \"README.md\"], \"\", \"low\")\n-        self.assertTrue(any(\"Docs/ADR-only\" in f for f in focus))\n+        self.assertTrue(any(\"Docs/decision-record-only\" in f for f in focus))\n         self.assertFalse(any(\"conservative default\" in f.lower() for f in focus))\n \n     def test_unknown_category_is_conservative(self):\ndiff --git a/tests/test_policy_job_merged_prs.py b/tests/test_policy_job_merged_prs.py\nindex d45512e74681e5e649279236c7d4e79d5c3da471..b97989f8bd74d091de1a6b5275a20d8dc68c4512\n--- a/tests/test_policy_job_merged_prs.py\n+++ b/tests/test_policy_job_merged_prs.py\n@@ -3,8 +3,8 @@\n \n ``.github/workflows/shiki-cca-completion.yml`` has two consumers of the merged-PR\n proof: the CCA *signal* (``guardian_approval_signal.py`` in \"Prepare CCA context\")\n-and the authoritative gate (``mergegate_check.py`` in \"Enforce MergeGate\"). ADR\n-0017's bookkeeping-closeout exemption (condition 6) needs the merged implementation\n+and the authoritative gate (``mergegate_check.py`` in \"Enforce MergeGate\").\n+SADR-0017's bookkeeping-closeout exemption (condition 6) needs the merged implementation\n PR proven, and a closeout carries NO ``post_merge_reconcile`` marker. The signal\n already resolves ``MERGED_PRS`` unconditionally, but \"Enforce MergeGate\" used to\n resolve it only inside ``grep -q '<!-- shiki:post_merge_reconcile -->'``. So a\ndiff --git a/tests/test_sadr_install_migration.py b/tests/test_sadr_install_migration.py\nnew file mode 100644\nindex 0000000000000000000000000000000000000000..a6f4c57e4dcaef73ef600b0d4804744138ab9071\n--- /dev/null\n+++ b/tests/test_sadr_install_migration.py\n@@ -0,0 +1,229 @@\n+\"\"\"SADR installation and collision-safe legacy upgrade behavior.\"\"\"\n+\n+from __future__ import annotations\n+\n+import contextlib\n+import hashlib\n+import io\n+import json\n+import shutil\n+import tempfile\n+import unittest\n+from pathlib import Path\n+\n+import shiki_test_support  # noqa: F401  (path bootstrap)\n+\n+import shiki_installer\n+from tests.test_adr_references import find_adr_reference_violations\n+\n+\n+PRODUCT_NUMBERS = tuple(range(13, 19))\n+\n+\n+def _quiet_install(target: Path, *, force: bool, validate: bool) -> None:\n+    with contextlib.redirect_stdout(io.StringIO()):\n+        shiki_installer.install_template(target, force=force, validate=validate)\n+\n+\n+def _sha256(path: Path) -> str:\n+    return hashlib.sha256(path.read_bytes()).hexdigest()\n+\n+\n+def _legacy_sadr_paths() -> tuple[str, ...]:\n+    return tuple(\n+        f\"docs/adr/{path.name.removeprefix('SADR-')}\"\n+        for path in sorted((shiki_installer.ROOT / \"docs\" / \"adr\").glob(\"SADR-*.md\"))\n+        if path.name != \"SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\"\n+    )\n+\n+\n+def _create_legacy_upgrade_fixture(\n+    target: Path,\n+) -> tuple[dict[str, bytes], tuple[str, ...]]:\n+    adr_dir = target / \"docs\" / \"adr\"\n+    adr_dir.mkdir(parents=True)\n+    product_bytes = {\n+        f\"{number:04d}-cubeshare-product-decision.md\": (\n+            f\"# CubeShare ADR {number:04d}\\n\\nDo not rewrite {number}.\\n\"\n+        ).encode()\n+        for number in PRODUCT_NUMBERS\n+    }\n+    for name, content in product_bytes.items():\n+        (adr_dir / name).write_bytes(content)\n+\n+    legacy_paths = _legacy_sadr_paths()\n+    for relative in legacy_paths:\n+        (target / relative).write_bytes(f\"legacy Shiki bytes for {relative}\\n\".encode())\n+\n+    stamp_path = target / shiki_installer.INSTALL_STAMP_PATH\n+    stamp_path.parent.mkdir(parents=True, exist_ok=True)\n+    stamp = {\n+        \"version\": shiki_installer.INSTALL_STAMP_VERSION,\n+        \"platform_commit\": None,\n+        \"installed_at\": \"2000-01-01T00:00:00+00:00\",\n+        \"digests\": {\n+            relative: _sha256(target / relative) for relative in legacy_paths\n+        },\n+    }\n+    stamp_path.write_text(\n+        json.dumps(stamp, indent=2, sort_keys=True) + \"\\n\", encoding=\"utf-8\"\n+    )\n+    return product_bytes, legacy_paths\n+\n+\n+def _tree_snapshot(root: Path) -> dict[str, tuple[str, bytes | str]]:\n+    \"\"\"Recursive byte/shape snapshot used to prove preflight is write-free.\"\"\"\n+    snapshot: dict[str, tuple[str, bytes | str]] = {}\n+    for path in sorted(root.rglob(\"*\")):\n+        relative = path.relative_to(root).as_posix()\n+        if path.is_symlink():\n+            snapshot[relative] = (\"symlink\", path.readlink().as_posix())\n+        elif path.is_dir():\n+            snapshot[relative] = (\"directory\", \"\")\n+        else:\n+            snapshot[relative] = (\"file\", path.read_bytes())\n+    return snapshot\n+\n+\n+class FreshSadrInstallTests(unittest.TestCase):\n+    def setUp(self) -> None:\n+        self.tmp = Path(tempfile.mkdtemp(prefix=\"shiki-sadr-fresh-\"))\n+        self.addCleanup(lambda: shutil.rmtree(self.tmp, ignore_errors=True))\n+        self.target = self.tmp / \"target\"\n+        self.adr_dir = self.target / \"docs\" / \"adr\"\n+        self.adr_dir.mkdir(parents=True)\n+        self.product_bytes = {\n+            f\"{number:04d}-product-decision.md\": (\n+                f\"# Product ADR {number:04d}\\n\\nTarget-owned bytes {number}.\\n\"\n+            ).encode()\n+            for number in PRODUCT_NUMBERS\n+        }\n+        for name, content in self.product_bytes.items():\n+            (self.adr_dir / name).write_bytes(content)\n+\n+    def test_fresh_install_preserves_product_adrs_and_installs_all_sadrs(self) -> None:\n+        _quiet_install(self.target, force=False, validate=True)\n+\n+        for name, expected in self.product_bytes.items():\n+            self.assertEqual((self.adr_dir / name).read_bytes(), expected)\n+\n+        sadr_numbers = {\n+            path.name.split(\"-\", 2)[1]\n+            for path in self.adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\")\n+        }\n+        self.assertEqual(sadr_numbers, {f\"{number:04d}\" for number in range(1, 20)})\n+\n+        numeric_records = {\n+            path.name\n+            for path in self.adr_dir.glob(\"[0-9][0-9][0-9][0-9]-*.md\")\n+        }\n+        self.assertEqual(numeric_records, set(self.product_bytes))\n+        self.assertEqual(find_adr_reference_violations(self.target), [])\n+\n+\n+class LegacySadrUpgradeTests(unittest.TestCase):\n+    def setUp(self) -> None:\n+        self.tmp = Path(tempfile.mkdtemp(prefix=\"shiki-sadr-upgrade-\"))\n+        self.addCleanup(lambda: shutil.rmtree(self.tmp, ignore_errors=True))\n+        self.target = self.tmp / \"target\"\n+        self.adr_dir = self.target / \"docs\" / \"adr\"\n+        self.product_bytes, self.legacy_paths = _create_legacy_upgrade_fixture(\n+            self.target\n+        )\n+\n+    def test_force_upgrade_deletes_only_stamped_legacy_sadrs(self) -> None:\n+        _quiet_install(self.target, force=True, validate=True)\n+\n+        for relative in self.legacy_paths:\n+            self.assertFalse((self.target / relative).exists(), relative)\n+        for name, expected in self.product_bytes.items():\n+            self.assertEqual((self.adr_dir / name).read_bytes(), expected)\n+\n+        sadrs = list(self.adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\"))\n+        self.assertEqual(len(sadrs), 19)\n+        refreshed = json.loads(\n+            (self.target / shiki_installer.INSTALL_STAMP_PATH).read_text(encoding=\"utf-8\")\n+        )\n+        for relative in self.legacy_paths:\n+            self.assertNotIn(relative, refreshed[\"digests\"])\n+        for path in sadrs:\n+            relative = path.relative_to(self.target).as_posix()\n+            self.assertEqual(refreshed[\"digests\"][relative], _sha256(path))\n+\n+    def test_non_force_install_never_deletes_legacy_files(self) -> None:\n+        before = {\n+            relative: (self.target / relative).read_bytes()\n+            for relative in self.legacy_paths\n+        }\n+        _quiet_install(self.target, force=False, validate=False)\n+        for relative, expected in before.items():\n+            self.assertEqual((self.target / relative).read_bytes(), expected)\n+        refreshed = json.loads(\n+            (self.target / shiki_installer.INSTALL_STAMP_PATH).read_text(encoding=\"utf-8\")\n+        )\n+        for relative in self.legacy_paths:\n+            self.assertEqual(refreshed[\"digests\"][relative], _sha256(self.target / relative))\n+\n+\n+class LegacyCleanupPreflightTests(unittest.TestCase):\n+    def setUp(self) -> None:\n+        self.tmp = Path(tempfile.mkdtemp(prefix=\"shiki-sadr-preflight-\"))\n+        self.addCleanup(lambda: shutil.rmtree(self.tmp, ignore_errors=True))\n+\n+    def test_all_ownership_blockers_abort_before_any_write(self) -> None:\n+        cases = (\n+            (\"absent\", \"install stamp is absent\"),\n+            (\"unreadable\", \"install stamp is unreadable\"),\n+            (\"malformed\", \"install stamp is malformed\"),\n+            (\"missing-digest\", \"install stamp has no digest\"),\n+            (\"digest-mismatch\", \"digest mismatch\"),\n+        )\n+        for name, expected_reason in cases:\n+            with self.subTest(name=name):\n+                target = self.tmp / name\n+                _, legacy_paths = _create_legacy_upgrade_fixture(target)\n+                stamp_path = target / shiki_installer.INSTALL_STAMP_PATH\n+\n+                if name == \"absent\":\n+                    stamp_path.unlink()\n+                elif name == \"unreadable\":\n+                    stamp_path.unlink()\n+                    stamp_path.mkdir()\n+                elif name == \"malformed\":\n+                    stamp_path.write_text(\"{not-json\\n\", encoding=\"utf-8\")\n+                else:\n+                    stamp = json.loads(stamp_path.read_text(encoding=\"utf-8\"))\n+                    if name == \"missing-digest\":\n+                        stamp[\"digests\"] = {}\n+                    else:\n+                        stamp[\"digests\"] = {\n+                            relative: \"deadbeef\" for relative in legacy_paths\n+                        }\n+                    stamp_path.write_text(\n+                        json.dumps(stamp, indent=2, sort_keys=True) + \"\\n\",\n+                        encoding=\"utf-8\",\n+                    )\n+\n+                before = _tree_snapshot(target)\n+                with self.assertRaises(shiki_installer.ShikiError) as raised:\n+                    _quiet_install(target, force=True, validate=False)\n+                after = _tree_snapshot(target)\n+\n+                self.assertEqual(before, after, f\"{name} preflight changed the tree\")\n+                message = str(raised.exception)\n+                self.assertIn(expected_reason, message)\n+                self.assertIn(\"No files were written\", message)\n+                for relative in legacy_paths:\n+                    self.assertIn(relative, message)\n+\n+    def test_absent_stamp_is_allowed_when_no_legacy_path_exists(self) -> None:\n+        target = self.tmp / \"no-legacy\"\n+        target.mkdir()\n+        _quiet_install(target, force=True, validate=False)\n+        self.assertTrue(\n+            (target / \"docs\" / \"adr\" / \"SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\").is_file()\n+        )\n+\n+\n+if __name__ == \"__main__\":\n+    unittest.main()\n\n2026-08-07T08:22:46.717935Z ERROR codex_models_manager::manager: failed to renew cache TTL: missing field `base_instructions` at line 94 column 5\nexec\n/bin/zsh -lc \"sed -n '1,260p' docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\" in /private/tmp/shiki-adr-collision.pa0KwY/.worktrees/shiki-t-20260807t073307221254z-2d22d654-introduc\n succeeded in 0ms:\n# SADR-0019: Separate Shiki Platform SADRs From Target ADRs\n\n## Status\n\nAccepted\n\n## Context\n\nShiki installs its platform decision records into `docs/adr/` in every target\nrepository. Shiki and its targets previously used the same numeric\n`NNNN-<slug>.md` namespace. A target that already owned ADRs 0013 through 0018\ntherefore acquired two records for each number during installation, making an\n`ADR NNNN` citation ambiguous. Renumbering target decisions would rewrite target\nhistory, while renumbering Shiki decisions per target would destroy their stable\nplatform identity.\n\nUpgrades also need to remove the former Shiki-owned numeric paths without ever\nmistaking a target-owned ADR for platform content. The install stamp already\nrecords exact shipped paths and content digests, so it can prove ownership only\nwhen both still match.\n\n## Decision\n\nShiki platform decisions use the `SADR` namespace:\n\n- records are named `docs/adr/SADR-NNNN-<slug>.md`;\n- live platform citations use `SADR-NNNN`;\n- target product decisions retain `docs/adr/NNNN-<slug>.md` and `ADR NNNN`;\n- the reference validator resolves the two namespaces independently, so the same\n  four-digit number may exist once in each namespace.\n\nBefore a forced upgrade writes anything, the installer inventories the exact\nformer numeric Shiki ADR paths. It may delete a legacy path only when the\nexisting install stamp contains that exact path and its recorded SHA-256 digest\nmatches the file currently on disk. All ownership blockers are reported\ntogether and abort the upgrade before its first write. A non-forced install\nnever performs this cleanup.\n\nHistorical `.shiki/` evidence remains unchanged because it records the namespace\nthat existed when that evidence was produced.\n\n## Consequences\n\n- Targets may use any ADR number without colliding with Shiki's platform SADRs.\n- Fresh installs preserve target ADR bytes and install a stable SADR set.\n- Forced upgrades fail closed when legacy ownership cannot be proven, and a\n  successful upgrade refreshes the stamp without removed legacy paths.\n- Current governance, packaging, workflow, prompt, script, and skill references\n  must move with the platform records.\n\n## Alternatives Considered\n\n- Collision-only refusal leaves affected targets permanently unupgradable.\n- Renumbering target ADRs mutates target-owned history.\n- Per-target Shiki renumbering makes platform citations non-portable.\n\nexec\n/bin/zsh -lc 'git diff -- tests/test_adr_references.py' in /private/tmp/shiki-adr-collision.pa0KwY/.worktrees/shiki-t-20260807t073307221254z-2d22d654-introduc\n succeeded in 0ms:\ndiff --git a/tests/test_adr_references.py b/tests/test_adr_references.py\nindex 5170632..d005da4 100644\n--- a/tests/test_adr_references.py\n+++ b/tests/test_adr_references.py\n@@ -1,8 +1,8 @@\n-\"\"\"Every ``ADR ####`` citation must resolve to exactly one decision record.\n+\"\"\"Every target ADR and Shiki SADR citation resolves in its own namespace.\n \n Background. ``scripts/mergegate_check.py`` justifies ``is_bookkeeping_closeout``\n-with \"ADR 0017\" nine times and ``scripts/guardian_approval_signal.py`` cites\n-0016/0017 seven times, yet for a stretch the numbering in ``docs/adr/`` stopped\n+with \"SADR-0017\" nine times and ``scripts/guardian_approval_signal.py`` cites\n+SADR-0016/SADR-0017 seven times, yet for a stretch the numbering in ``docs/adr/`` stopped\n at 0015: the authoring commits lived on a coordinator branch and no task ever\n declared ``docs/adr/...`` in its locks, so nothing carried the records to the\n default branch. The stated basis of an exemption that merged seven PRs with zero\n@@ -11,11 +11,12 @@ returns nothing -- no validator required a cited ADR to exist, so the citations\n and the directory drifted with no signal.\n \n This is the same unbound-pair shape behind other silent failures: a reference\n-and its target with nothing asserting they agree. This test binds them. It scans\n-every ``scripts/*.py`` (plus ``docs/**/*.md`` and the root ``*.md`` governance\n-files, which are cheap) for the ``ADR ####`` pattern and asserts each cited\n-number resolves to exactly one ``docs/adr/####-*.md``. The converse is\n-deliberately NOT required: an ADR may exist without being cited.\n+and its target with nothing asserting they agree. This test binds them. Target\n+``ADR NNNN`` citations resolve only to ``docs/adr/NNNN-*.md``; Shiki\n+``SADR-NNNN`` citations resolve only to ``docs/adr/SADR-NNNN-*.md``. The scan\n+covers current governance docs, scripts, workflows, prompts, packaging metadata,\n+and repo-local Codex guidance while deliberately excluding historical\n+``.shiki`` evidence. The converse is not required: a record may be uncited.\n \n The suite is dependency free: standard-library ``unittest`` only.\n \"\"\"\n@@ -31,17 +32,18 @@ from typing import NamedTuple, Sequence\n \n REPO_ROOT = Path(__file__).resolve().parents[1]\n \n-# \"ADR 0016\" -- a four-digit citation. The trailing \\b keeps a five-digit token\n-# from being read as a truncated four-digit number.\n-_CITATION = re.compile(r\"\\bADR (\\d{4})\\b\")\n-# \"0016-locks-bound-product-paths.md\" -- the leading four digits name the record.\n-_ADR_FILENAME = re.compile(r\"^(\\d{4})-.*\\.md$\")\n+# ``ADR 0016`` / ``SADR-0016`` -- a namespace plus four-digit citation. The\n+# trailing ``\\b`` keeps a five-digit token from being read as a truncated one.\n+_CITATION = re.compile(r\"\\b(S?ADR)[ -](\\d{4})\\b\")\n+# Target ADRs retain the numeric filename; Shiki platform decisions use SADR.\n+_DECISION_FILENAME = re.compile(r\"^(?:(SADR)-)?(\\d{4})-.*\\.md$\")\n \n \n class Violation(NamedTuple):\n     \"\"\"A citation that does not resolve to exactly one decision record.\"\"\"\n \n     citing_file: str  # repo-relative path of the file that cites the ADR\n+    namespace: str  # \"ADR\" for target decisions; \"SADR\" for Shiki decisions\n     number: str  # the cited four-digit number, e.g. \"0016\"\n     line: int  # 1-based line of the citation\n     reason: str  # \"missing\" (zero matches) or \"ambiguous\" (two or more)\n@@ -49,44 +51,59 @@ class Violation(NamedTuple):\n \n     @property\n     def expected_path(self) -> str:\n-        return f\"docs/adr/{self.number}-*.md\"\n+        prefix = \"SADR-\" if self.namespace == \"SADR\" else \"\"\n+        return f\"docs/adr/{prefix}{self.number}-*.md\"\n \n     def describe(self) -> str:\n         \"\"\"Name the citing file, the cited number, and the missing path.\"\"\"\n         if self.reason == \"missing\":\n             return (\n-                f\"{self.citing_file}:{self.line} cites ADR {self.number} \"\n+                f\"{self.citing_file}:{self.line} cites {self.namespace} {self.number} \"\n                 f\"but no document matches {self.expected_path}\"\n             )\n         joined = \", \".join(self.matches)\n         return (\n-            f\"{self.citing_file}:{self.line} cites ADR {self.number} which \"\n+            f\"{self.citing_file}:{self.line} cites {self.namespace} {self.number} which \"\n             f\"resolves to {len(self.matches)} documents (expected exactly one \"\n             f\"at {self.expected_path}): {joined}\"\n         )\n \n \n-def _adr_index(root: Path) -> dict[str, list[Path]]:\n-    \"\"\"Map every four-digit ADR number to the record files that claim it.\"\"\"\n-    index: dict[str, list[Path]] = {}\n+def _adr_index(root: Path) -> dict[tuple[str, str], list[Path]]:\n+    \"\"\"Map each decision namespace and number to the files that claim it.\"\"\"\n+    index: dict[tuple[str, str], list[Path]] = {}\n     adr_dir = root / \"docs\" / \"adr\"\n     if adr_dir.is_dir():\n         for path in sorted(adr_dir.glob(\"*.md\")):\n-            match = _ADR_FILENAME.match(path.name)\n+            match = _DECISION_FILENAME.match(path.name)\n             if match:\n-                index.setdefault(match.group(1), []).append(path)\n+                namespace = \"SADR\" if match.group(1) else \"ADR\"\n+                index.setdefault((namespace, match.group(2)), []).append(path)\n     return index\n \n \n def _citing_files(root: Path, include_docs: bool = True) -> list[Path]:\n-    \"\"\"Files scanned for citations: scripts/*.py, and cheaply the markdown.\"\"\"\n+    \"\"\"Current, shipped text surfaces scanned for decision citations.\"\"\"\n     files = sorted((root / \"scripts\").glob(\"*.py\"))\n+    files += sorted((root / \"scripts\").glob(\"*.sh\"))\n     if include_docs:\n         docs = root / \"docs\"\n         if docs.is_dir():\n             files += sorted(docs.rglob(\"*.md\"))\n         files += sorted(root.glob(\"*.md\"))\n-    return files\n+        pyproject = root / \"pyproject.toml\"\n+        if pyproject.is_file():\n+            files.append(pyproject)\n+        for relative in (\".github\", \".codex\"):\n+            surface = root / relative\n+            if surface.is_dir():\n+                files += sorted(\n+                    path\n+                    for path in surface.rglob(\"*\")\n+                    if path.is_file()\n+                    and path.suffix in {\".md\", \".py\", \".sh\", \".toml\", \".yaml\", \".yml\"}\n+                )\n+    return sorted(set(files))\n \n \n def find_adr_reference_violations(\n@@ -94,10 +111,9 @@ def find_adr_reference_violations(\n ) -> list[Violation]:\n     \"\"\"Return every ``ADR ####`` citation under ``root`` that fails to resolve.\n \n-    A citation resolves when exactly one ``docs/adr/####-*.md`` matches its\n-    number. Zero matches is a \"missing\" violation; two or more is \"ambiguous\".\n-    Records that are never cited produce nothing -- the converse is not\n-    required.\n+    A citation resolves when exactly one record in the same namespace matches\n+    its number. Zero matches is a \"missing\" violation; two or more is\n+    \"ambiguous\". Records that are never cited produce nothing.\n     \"\"\"\n     root = Path(root)\n     index = _adr_index(root)\n@@ -110,13 +126,15 @@ def find_adr_reference_violations(\n         rel = path.relative_to(root).as_posix()\n         for lineno, line in enumerate(text.splitlines(), start=1):\n             for match in _CITATION.finditer(line):\n-                number = match.group(1)\n-                found = index.get(number, [])\n+                namespace, number = match.groups()\n+                found = index.get((namespace, number), [])\n                 if len(found) == 1:\n                     continue\n                 reason = \"missing\" if not found else \"ambiguous\"\n                 rels = [p.relative_to(root).as_posix() for p in found]\n-                violations.append(Violation(rel, number, lineno, reason, rels))\n+                violations.append(\n+                    Violation(rel, namespace, number, lineno, reason, rels)\n+                )\n     return violations\n \n \n@@ -129,8 +147,8 @@ class RealRepoAdrReferences(unittest.TestCase):\n         self.assertEqual(\n             violations,\n             [],\n-            \"Every 'ADR ####' citation must resolve to exactly one \"\n-            f\"docs/adr/####-*.md:\\n{detail}\",\n+            \"Every ADR/SADR citation must resolve exactly once in its own \"\n+            f\"docs/adr namespace:\\n{detail}\",\n         )\n \n     def test_scan_actually_reads_scripts(self) -> None:\n@@ -142,12 +160,38 @@ class RealRepoAdrReferences(unittest.TestCase):\n             \"the citation scan must cover scripts/*.py\",\n         )\n \n+    def test_platform_citations_do_not_use_unresolved_numeric_shorthand(self) -> None:\n+        shorthand = re.compile(r\"SADR-\\d{4}/\\d{4}\")\n+        offenders = []\n+        for path in _citing_files(REPO_ROOT):\n+            text = path.read_text(encoding=\"utf-8\")\n+            if shorthand.search(text):\n+                offenders.append(path.relative_to(REPO_ROOT).as_posix())\n+        self.assertEqual(\n+            offenders,\n+            [],\n+            \"repeat the SADR prefix so every cited number is independently resolvable\",\n+        )\n+\n     def test_carried_records_are_indexed(self) -> None:\n         # The two records this Goal carried onto the default branch, and which\n         # the scripts cite most, must be present.\n         index = _adr_index(REPO_ROOT)\n-        self.assertIn(\"0016\", index)\n-        self.assertIn(\"0017\", index)\n+        self.assertIn((\"SADR\", \"0016\"), index)\n+        self.assertIn((\"SADR\", \"0017\"), index)\n+\n+    def test_platform_tree_has_only_sadr_0001_through_0019(self) -> None:\n+        adr_dir = REPO_ROOT / \"docs\" / \"adr\"\n+        sadrs = sorted(adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\"))\n+        self.assertEqual(\n+            {path.name.split(\"-\", 2)[1] for path in sadrs},\n+            {f\"{number:04d}\" for number in range(1, 20)},\n+        )\n+        self.assertEqual(\n+            list(adr_dir.glob(\"[0-9][0-9][0-9][0-9]-*.md\")),\n+            [],\n+            \"the Shiki platform tree must not retain legacy numeric SADR paths\",\n+        )\n \n \n class FixtureAdrReferences(unittest.TestCase):\n@@ -180,6 +224,26 @@ class FixtureAdrReferences(unittest.TestCase):\n         self.assertIn(\"9991\", message)  # cited number\n         self.assertIn(\"docs/adr/9991-*.md\", message)  # missing path\n \n+    def test_target_adr_does_not_satisfy_missing_platform_sadr(self) -> None:\n+        root = self._make_repo(\n+            adr_names=[\"0042-target-decision.md\"],\n+            script_body='\"\"\"Target ADR 0042 exists, but platform SADR-0042 does not.\"\"\"\\n',\n+        )\n+        violations = find_adr_reference_violations(root, include_docs=False)\n+        self.assertEqual(len(violations), 1)\n+        self.assertEqual(violations[0].namespace, \"SADR\")\n+        self.assertEqual(violations[0].number, \"0042\")\n+        self.assertEqual(violations[0].expected_path, \"docs/adr/SADR-0042-*.md\")\n+\n+    def test_target_adr_and_platform_sadr_may_reuse_a_number(self) -> None:\n+        root = self._make_repo(\n+            adr_names=[\"0042-target-decision.md\", \"SADR-0042-platform-decision.md\"],\n+            script_body='\"\"\"Target ADR 0042 and platform SADR-0042 are distinct.\"\"\"\\n',\n+        )\n+        self.assertEqual(\n+            find_adr_reference_violations(root, include_docs=False), []\n+        )\n+\n     def test_uncited_adr_is_allowed(self) -> None:\n         # An ADR exists but nothing cites it -> not a violation. The converse\n         # (every ADR must be cited) is explicitly NOT required.\n\nexec\n/bin/zsh -lc \"sed -n '1,320p' tests/test_sadr_install_migration.py\" in /private/tmp/shiki-adr-collision.pa0KwY/.worktrees/shiki-t-20260807t073307221254z-2d22d654-introduc\n succeeded in 0ms:\n\"\"\"SADR installation and collision-safe legacy upgrade behavior.\"\"\"\n\nfrom __future__ import annotations\n\nimport contextlib\nimport hashlib\nimport io\nimport json\nimport shutil\nimport tempfile\nimport unittest\nfrom pathlib import Path\n\nimport shiki_test_support  # noqa: F401  (path bootstrap)\n\nimport shiki_installer\nfrom tests.test_adr_references import find_adr_reference_violations\n\n\nPRODUCT_NUMBERS = tuple(range(13, 19))\n\n\ndef _quiet_install(target: Path, *, force: bool, validate: bool) -> None:\n    with contextlib.redirect_stdout(io.StringIO()):\n        shiki_installer.install_template(target, force=force, validate=validate)\n\n\ndef _sha256(path: Path) -> str:\n    return hashlib.sha256(path.read_bytes()).hexdigest()\n\n\ndef _legacy_sadr_paths() -> tuple[str, ...]:\n    return tuple(\n        f\"docs/adr/{path.name.removeprefix('SADR-')}\"\n        for path in sorted((shiki_installer.ROOT / \"docs\" / \"adr\").glob(\"SADR-*.md\"))\n        if path.name != \"SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\"\n    )\n\n\ndef _create_legacy_upgrade_fixture(\n    target: Path,\n) -> tuple[dict[str, bytes], tuple[str, ...]]:\n    adr_dir = target / \"docs\" / \"adr\"\n    adr_dir.mkdir(parents=True)\n    product_bytes = {\n        f\"{number:04d}-cubeshare-product-decision.md\": (\n            f\"# CubeShare ADR {number:04d}\\n\\nDo not rewrite {number}.\\n\"\n        ).encode()\n        for number in PRODUCT_NUMBERS\n    }\n    for name, content in product_bytes.items():\n        (adr_dir / name).write_bytes(content)\n\n    legacy_paths = _legacy_sadr_paths()\n    for relative in legacy_paths:\n        (target / relative).write_bytes(f\"legacy Shiki bytes for {relative}\\n\".encode())\n\n    stamp_path = target / shiki_installer.INSTALL_STAMP_PATH\n    stamp_path.parent.mkdir(parents=True, exist_ok=True)\n    stamp = {\n        \"version\": shiki_installer.INSTALL_STAMP_VERSION,\n        \"platform_commit\": None,\n        \"installed_at\": \"2000-01-01T00:00:00+00:00\",\n        \"digests\": {\n            relative: _sha256(target / relative) for relative in legacy_paths\n        },\n    }\n    stamp_path.write_text(\n        json.dumps(stamp, indent=2, sort_keys=True) + \"\\n\", encoding=\"utf-8\"\n    )\n    return product_bytes, legacy_paths\n\n\ndef _tree_snapshot(root: Path) -> dict[str, tuple[str, bytes | str]]:\n    \"\"\"Recursive byte/shape snapshot used to prove preflight is write-free.\"\"\"\n    snapshot: dict[str, tuple[str, bytes | str]] = {}\n    for path in sorted(root.rglob(\"*\")):\n        relative = path.relative_to(root).as_posix()\n        if path.is_symlink():\n            snapshot[relative] = (\"symlink\", path.readlink().as_posix())\n        elif path.is_dir():\n            snapshot[relative] = (\"directory\", \"\")\n        else:\n            snapshot[relative] = (\"file\", path.read_bytes())\n    return snapshot\n\n\nclass FreshSadrInstallTests(unittest.TestCase):\n    def setUp(self) -> None:\n        self.tmp = Path(tempfile.mkdtemp(prefix=\"shiki-sadr-fresh-\"))\n        self.addCleanup(lambda: shutil.rmtree(self.tmp, ignore_errors=True))\n        self.target = self.tmp / \"target\"\n        self.adr_dir = self.target / \"docs\" / \"adr\"\n        self.adr_dir.mkdir(parents=True)\n        self.product_bytes = {\n            f\"{number:04d}-product-decision.md\": (\n                f\"# Product ADR {number:04d}\\n\\nTarget-owned bytes {number}.\\n\"\n            ).encode()\n            for number in PRODUCT_NUMBERS\n        }\n        for name, content in self.product_bytes.items():\n            (self.adr_dir / name).write_bytes(content)\n\n    def test_fresh_install_preserves_product_adrs_and_installs_all_sadrs(self) -> None:\n        _quiet_install(self.target, force=False, validate=True)\n\n        for name, expected in self.product_bytes.items():\n            self.assertEqual((self.adr_dir / name).read_bytes(), expected)\n\n        sadr_numbers = {\n            path.name.split(\"-\", 2)[1]\n            for path in self.adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\")\n        }\n        self.assertEqual(sadr_numbers, {f\"{number:04d}\" for number in range(1, 20)})\n\n        numeric_records = {\n            path.name\n            for path in self.adr_dir.glob(\"[0-9][0-9][0-9][0-9]-*.md\")\n        }\n        self.assertEqual(numeric_records, set(self.product_bytes))\n        self.assertEqual(find_adr_reference_violations(self.target), [])\n\n\nclass LegacySadrUpgradeTests(unittest.TestCase):\n    def setUp(self) -> None:\n        self.tmp = Path(tempfile.mkdtemp(prefix=\"shiki-sadr-upgrade-\"))\n        self.addCleanup(lambda: shutil.rmtree(self.tmp, ignore_errors=True))\n        self.target = self.tmp / \"target\"\n        self.adr_dir = self.target / \"docs\" / \"adr\"\n        self.product_bytes, self.legacy_paths = _create_legacy_upgrade_fixture(\n            self.target\n        )\n\n    def test_force_upgrade_deletes_only_stamped_legacy_sadrs(self) -> None:\n        _quiet_install(self.target, force=True, validate=True)\n\n        for relative in self.legacy_paths:\n            self.assertFalse((self.target / relative).exists(), relative)\n        for name, expected in self.product_bytes.items():\n            self.assertEqual((self.adr_dir / name).read_bytes(), expected)\n\n        sadrs = list(self.adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\"))\n        self.assertEqual(len(sadrs), 19)\n        refreshed = json.loads(\n            (self.target / shiki_installer.INSTALL_STAMP_PATH).read_text(encoding=\"utf-8\")\n        )\n        for relative in self.legacy_paths:\n            self.assertNotIn(relative, refreshed[\"digests\"])\n        for path in sadrs:\n            relative = path.relative_to(self.target).as_posix()\n            self.assertEqual(refreshed[\"digests\"][relative], _sha256(path))\n\n    def test_non_force_install_never_deletes_legacy_files(self) -> None:\n        before = {\n            relative: (self.target / relative).read_bytes()\n            for relative in self.legacy_paths\n        }\n        _quiet_install(self.target, force=False, validate=False)\n        for relative, expected in before.items():\n            self.assertEqual((self.target / relative).read_bytes(), expected)\n        refreshed = json.loads(\n            (self.target / shiki_installer.INSTALL_STAMP_PATH).read_text(encoding=\"utf-8\")\n        )\n        for relative in self.legacy_paths:\n            self.assertEqual(refreshed[\"digests\"][relative], _sha256(self.target / relative))\n\n\nclass LegacyCleanupPreflightTests(unittest.TestCase):\n    def setUp(self) -> None:\n        self.tmp = Path(tempfile.mkdtemp(prefix=\"shiki-sadr-preflight-\"))\n        self.addCleanup(lambda: shutil.rmtree(self.tmp, ignore_errors=True))\n\n    def test_all_ownership_blockers_abort_before_any_write(self) -> None:\n        cases = (\n            (\"absent\", \"install stamp is absent\"),\n            (\"unreadable\", \"install stamp is unreadable\"),\n            (\"malformed\", \"install stamp is malformed\"),\n            (\"missing-digest\", \"install stamp has no digest\"),\n            (\"digest-mismatch\", \"digest mismatch\"),\n        )\n        for name, expected_reason in cases:\n            with self.subTest(name=name):\n                target = self.tmp / name\n                _, legacy_paths = _create_legacy_upgrade_fixture(target)\n                stamp_path = target / shiki_installer.INSTALL_STAMP_PATH\n\n                if name == \"absent\":\n                    stamp_path.unlink()\n                elif name == \"unreadable\":\n                    stamp_path.unlink()\n                    stamp_path.mkdir()\n                elif name == \"malformed\":\n                    stamp_path.write_text(\"{not-json\\n\", encoding=\"utf-8\")\n                else:\n                    stamp = json.loads(stamp_path.read_text(encoding=\"utf-8\"))\n                    if name == \"missing-digest\":\n                        stamp[\"digests\"] = {}\n                    else:\n                        stamp[\"digests\"] = {\n                            relative: \"deadbeef\" for relative in legacy_paths\n                        }\n                    stamp_path.write_text(\n                        json.dumps(stamp, indent=2, sort_keys=True) + \"\\n\",\n                        encoding=\"utf-8\",\n                    )\n\n                before = _tree_snapshot(target)\n                with self.assertRaises(shiki_installer.ShikiError) as raised:\n                    _quiet_install(target, force=True, validate=False)\n                after = _tree_snapshot(target)\n\n                self.assertEqual(before, after, f\"{name} preflight changed the tree\")\n                message = str(raised.exception)\n                self.assertIn(expected_reason, message)\n                self.assertIn(\"No files were written\", message)\n                for relative in legacy_paths:\n                    self.assertIn(relative, message)\n\n    def test_absent_stamp_is_allowed_when_no_legacy_path_exists(self) -> None:\n        target = self.tmp / \"no-legacy\"\n        target.mkdir()\n        _quiet_install(target, force=True, validate=False)\n        self.assertTrue(\n            (target / \"docs\" / \"adr\" / \"SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\").is_file()\n        )\n\n\nif __name__ == \"__main__\":\n    unittest.main()\n\nexec\n/bin/zsh -lc 'git diff -- scripts/shiki_installer.py' in /private/tmp/shiki-adr-collision.pa0KwY/.worktrees/shiki-t-20260807t073307221254z-2d22d654-introduc\n succeeded in 0ms:\ndiff --git a/scripts/shiki_installer.py b/scripts/shiki_installer.py\nindex be064ab..9fd8eef 100644\n--- a/scripts/shiki_installer.py\n+++ b/scripts/shiki_installer.py\n@@ -118,6 +118,7 @@ DEFAULT_CODEX_SKILL_PATH = \"~/.codex/skills/shiki/SKILL.md\"\n # what lets a target carry it without a governance-schema change.\n INSTALL_STAMP_PATH = \".shiki/install-stamp.json\"\n INSTALL_STAMP_VERSION = 1\n+LEGACY_SHIKI_SADR_MAX = 18\n \n # Under --force the shipped surface splits three ways, so nothing is lost and\n # nothing is silently stale.\n@@ -237,9 +238,98 @@ def refuse_pending_migrations(target: Path) -> None:\n         )\n \n \n+def legacy_shiki_adr_paths() -> tuple[str, ...]:\n+    \"\"\"Former numeric paths for Shiki SADR-0001 through SADR-0018.\n+\n+    The authoritative slugs come from the current SADR filenames. SADR-0019 is\n+    the namespace decision itself and never shipped under a numeric legacy path.\n+    \"\"\"\n+    paths: list[str] = []\n+    for sadr in sorted((ROOT / \"docs\" / \"adr\").glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\")):\n+        number = sadr.name.split(\"-\", 2)[1]\n+        if int(number) <= LEGACY_SHIKI_SADR_MAX:\n+            paths.append(f\"docs/adr/{sadr.name.removeprefix('SADR-')}\")\n+    return tuple(paths)\n+\n+\n+def preflight_legacy_sadr_cleanup(target: Path, *, force: bool) -> tuple[str, ...]:\n+    \"\"\"Authorize exact legacy Shiki ADR removals before the first write.\n+\n+    A non-force install never removes existing files. A forced install needs no\n+    stamp when no legacy Shiki path is present. When legacy paths do exist, each\n+    must be named in a readable install stamp and its current bytes must match\n+    the recorded digest.\n+    \"\"\"\n+    if not force:\n+        return ()\n+    present = tuple(\n+        relative\n+        for relative in legacy_shiki_adr_paths()\n+        if (target / relative).exists() or (target / relative).is_symlink()\n+    )\n+    if not present:\n+        return ()\n+\n+    stamp_path = target / INSTALL_STAMP_PATH\n+    try:\n+        raw = stamp_path.read_text(encoding=\"utf-8\")\n+        stamp = json.loads(raw)\n+    except FileNotFoundError:\n+        detail = \"install stamp is absent\"\n+        blockers = [f\"{relative}: {detail}\" for relative in present]\n+    except (OSError, UnicodeDecodeError) as error:\n+        detail = f\"install stamp is unreadable ({error})\"\n+        blockers = [f\"{relative}: {detail}\" for relative in present]\n+    except json.JSONDecodeError as error:\n+        detail = f\"install stamp is malformed ({error})\"\n+        blockers = [f\"{relative}: {detail}\" for relative in present]\n+    else:\n+        if not isinstance(stamp, dict):\n+            blockers = [\n+                f\"{relative}: install stamp is malformed (expected an object)\"\n+                for relative in present\n+            ]\n+        else:\n+            digests = stamp.get(\"digests\")\n+            blockers = []\n+            if not isinstance(digests, dict):\n+                blockers.extend(\n+                    f\"{relative}: install stamp has no digest for this path\"\n+                    for relative in present\n+                )\n+            else:\n+                for relative in present:\n+                    expected = digests.get(relative)\n+                    if not isinstance(expected, str) or not expected:\n+                        blockers.append(\n+                            f\"{relative}: install stamp has no digest for this path\"\n+                        )\n+                        continue\n+                    try:\n+                        actual = _sha256_file(target / relative)\n+                    except OSError as error:\n+                        blockers.append(\n+                            f\"{relative}: legacy path is unreadable ({error})\"\n+                        )\n+                        continue\n+                    if actual != expected:\n+                        blockers.append(\n+                            f\"{relative}: digest mismatch (stamp {expected}, current {actual})\"\n+                        )\n+\n+    if blockers:\n+        joined = \"\\n\".join(f\"- {blocker}\" for blocker in blockers)\n+        raise ShikiError(\n+            \"legacy Shiki ADR cleanup is not authorized:\\n\"\n+            f\"{joined}\\nNo files were written.\"\n+        )\n+    return present\n+\n+\n def install_template(target: Path, *, force: bool, validate: bool) -> list[NewFileNote]:\n     # Preconditions first: never rewrite the tree of a target that cannot be\n     # upgraded cleanly (see refuse_pending_migrations).\n+    legacy_cleanup = preflight_legacy_sadr_cleanup(target, force=force)\n     refuse_pending_migrations(target)\n \n     # Resolve the target's required CODEOWNERS owner once from its\n@@ -250,6 +340,10 @@ def install_template(target: Path, *, force: bool, validate: bool) -> list[NewFi\n     # verbatim. See ``codeowners_required_owner``.\n     codeowners_owner = codeowners_required_owner(target)\n \n+    for relative in legacy_cleanup:\n+        (target / relative).unlink()\n+        info(f\"removed stamped legacy Shiki ADR: {target / relative}\")\n+\n     notes: list[NewFileNote] = []\n     for relative in TEMPLATE_PATHS:\n         source = ROOT / relative\n@@ -647,6 +741,22 @@ def write_install_stamp(target: Path) -> None:\n     digests = target_stamp_digests(target)\n     existing = read_install_stamp(target)\n     if existing is not None:\n+        # A non-force install deliberately leaves legacy Shiki ADRs in place.\n+        # Preserve only still-matching ownership proof so a later forced\n+        # upgrade can authorize cleanup; never carry a missing or stale digest.\n+        existing_digests = existing.get(\"digests\")\n+        if isinstance(existing_digests, dict):\n+            for relative in legacy_shiki_adr_paths():\n+                expected = existing_digests.get(relative)\n+                path = target / relative\n+                if not isinstance(expected, str) or not path.is_file():\n+                    continue\n+                try:\n+                    actual = _sha256_file(path)\n+                except OSError:\n+                    continue\n+                if actual == expected:\n+                    digests[relative] = actual\n         if _incoming_is_older(commit, existing.get(\"platform_commit\")):\n             warn(\n                 \"kept existing install stamp: incoming install \"\n\ndiff --git a/.codex/skills/shiki/SKILL.md b/.codex/skills/shiki/SKILL.md\nindex c8e2e85d9d75d49cc4f2d6286ee22659c4e53e75..93cded623b2c15d9f65c76491a1c2d813ac9e6c0\n--- a/.codex/skills/shiki/SKILL.md\n+++ b/.codex/skills/shiki/SKILL.md\n@@ -18,6 +18,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n Codex CLI does not register this skill as a custom `/shiki` slash command. When\n the user types `/shiki` in Codex CLI and gets \"Unrecognized command\", explain\n that this is expected: invoke Shiki in Codex with natural language such as\n@@ -69,7 +72,7 @@\n \n ## Responsibilities\n \n-- Claude Code implements and repairs by default (`shiki runner claude`, ADR 0008); Codex implements and repairs tasks explicitly assigned to `codex`.\n+- Claude Code implements and repairs by default (`shiki runner claude`, SADR-0008); Codex implements and repairs tasks explicitly assigned to `codex`.\n - Claude Code Action can act as GitHub-side CCA or reviewer.\n - CCA judges completion.\n - MergeGate authorizes state transitions and merge readiness.\ndiff --git a/.github/prompts/cca-completion-check.md b/.github/prompts/cca-completion-check.md\nindex ed203ad5f302ecc81372d8ab27d2b1232ca97497..fd89950528333ef6242e6d5443904f5392ab7e56\n--- a/.github/prompts/cca-completion-check.md\n+++ b/.github/prompts/cca-completion-check.md\n@@ -19,6 +19,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n ## Required Reading\n \n Read only the files needed to judge the PR contract. Prefer this order:\ndiff --git a/.github/prompts/codex-task-handoff.md b/.github/prompts/codex-task-handoff.md\nindex 695e211d76dea1c60e8d5bb360e78487808286a4..59441d576b01a39569722f226c854e5a740e9eb5\n--- a/.github/prompts/codex-task-handoff.md\n+++ b/.github/prompts/codex-task-handoff.md\n@@ -2,7 +2,7 @@\n \n You are Codex Front acting as the Implementer for a Shiki task.\n \n-Follow `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant ADRs, and `docs/agents/codex-handoff.md`.\n+Follow `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant Shiki SADRs and target ADRs, and `docs/agents/codex-handoff.md`.\n \n Rules:\n \ndiff --git a/AGENTS.md b/AGENTS.md\nindex 7f041100d49c2a7525f51cb0f5d696a4a6d1c78c..a06bfbabc6fa48a007389e25e12d7ed523936bed\n--- a/AGENTS.md\n+++ b/AGENTS.md\n@@ -22,7 +22,7 @@\n - **Codex Front** is an optional implementer through Codex App, Codex CLI, Codex IDE extension, or Codex Web signed in with ChatGPT OAuth/subscription auth, dispatched with `shiki runner codex` when a task is explicitly assigned to `codex`.\n - **GitHub CCA** is implemented by Claude Code Action by default, using `CLAUDE_CODE_OAUTH_TOKEN`.\n \n-Do not assume `openai/codex-action`, `OPENAI_API_KEY`, or API-key based Codex automation in the default Shiki path. API-key runners are explicit target-repo extensions and require their own ADR.\n+Do not assume `openai/codex-action`, `OPENAI_API_KEY`, or API-key based Codex automation in the default Shiki path. API-key runners are explicit target-repo extensions and require their own target ADR.\n \n Shiki is not:\n \n@@ -44,10 +44,14 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use\n+`SADR-NNNN-*.md`; target product decisions retain `NNNN-*.md` and the `ADR`\n+namespace.\n+\n Use the canonical source-of-truth order above. Runtime-specific reading orders\n are operational guidance only and must not override or reorder it.\n \n-Conversation state is not durable truth. If a decision matters, put it in GitHub, `.shiki/`, `CONTEXT.md`, or an ADR.\n+Conversation state is not durable truth. If a decision matters, put it in GitHub, `.shiki/`, `CONTEXT.md`, a Shiki SADR, or a target ADR.\n \n If GitHub and `.shiki/` disagree, surface the conflict and prefer GitHub operational state until the mirror is repaired.\n \n@@ -75,8 +79,8 @@\n Every non-trivial change follows this loop:\n \n 1. **Goal**: clarify outcome, completion conditions, non-goals, risk level, and success signals.\n-2. **grill-with-docs**: challenge the plan against domain docs, ADRs, code reality, terminology, and edge scenarios.\n-3. **Context & Impact**: find relevant docs, ADRs, code, dependencies, owners, locks, and verification surfaces.\n+2. **grill-with-docs**: challenge the plan against domain docs, Shiki SADRs, target ADRs, code reality, terminology, and edge scenarios.\n+3. **Context & Impact**: find relevant docs, SADRs, target ADRs, code, dependencies, owners, locks, and verification surfaces.\n 4. **PRD and Spec Freeze**: use `to-prd` when context is settled enough to become durable product/engineering intent. The operator's explicit approval of the PRD is Spec Freeze: the plan records a `spec_freeze` block, and no plan runs without it. Steps 1-4 form Requirements Definition \u2014 one continuous operator dialogue. After Spec Freeze, scope changes require an operator-approved Spec Amendment; non-scope-moving interpretations go to the Assumption Log.\n 5. **Issues**: use `to-issues` to create independently grabbable vertical-slice issues. Prefer AFK slices over HITL slices where possible.\n 6. **Triage**: label issues for readiness, risk, runtime, skills, and MergeGate state.\n@@ -167,7 +171,7 @@\n - Testing decisions.\n - Out-of-scope items.\n - Further notes.\n-- Links to relevant domain terms and ADRs.\n+- Links to relevant domain terms, SADRs, and target ADRs.\n \n Every executable task must include:\n \n@@ -193,7 +197,7 @@\n \n Context & Impact must identify:\n \n-- Relevant documents, domain terms, ADRs, and past decisions.\n+- Relevant documents, domain terms, SADRs, target ADRs, and past decisions.\n - Relevant modules, interfaces, seams, callers, owners, and tests.\n - Dependency relationships between tasks.\n - Candidate locks and lock conflicts.\n@@ -210,7 +214,7 @@\n | Skill | Required when |\n | --- | --- |\n | `setup-matt-pocock-skills` | First configuring the repo for skills, or when issue tracker, triage labels, or domain docs are missing. |\n-| `grill-with-docs` | Default for non-trivial Goals before PRD/issues; always required when plans, terminology, boundaries, tradeoffs, or ADR-worthy decisions are ambiguous. |\n+| `grill-with-docs` | Default for non-trivial Goals before PRD/issues; always required when plans, terminology, boundaries, tradeoffs, or decision-record-worthy choices are ambiguous. |\n | `zoom-out` | The agent lacks an architectural map of the relevant code area. |\n | `to-prd` | Settled Goal context must become durable product/engineering intent. |\n | `to-issues` | A Goal, PRD, or plan must become independently grabbable vertical-slice issues. |\n@@ -348,7 +352,7 @@\n - Three or more new files.\n - Any high-risk or critical label.\n \n-When the architecture gate triggers, run `grill-with-docs` or `improve-codebase-architecture` as appropriate and record durable decisions in an ADR when needed.\n+When the architecture gate triggers, run `grill-with-docs` or `improve-codebase-architecture` as appropriate and record durable decisions in a Shiki SADR or target ADR as applicable.\n \n ## Engineering Discipline\n \ndiff --git a/CLAUDE.md b/CLAUDE.md\nindex 98a932e7841a632866fcd53b0bad0e3af3711d3d..98a31399404d6fb044b4fd493f4459ac87f439de\n--- a/CLAUDE.md\n+++ b/CLAUDE.md\n@@ -14,6 +14,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n ## Claude Role\n \n Default to planner, default implementer, reviewer, coordinator, documentation editor, CCA-style completion judge, and final-judgment assistant.\n@@ -57,7 +60,7 @@\n At the start of a Shiki session:\n \n 1. Read `AGENTS.md`.\n-2. Read `CONTEXT.md` and relevant ADRs.\n+2. Read `CONTEXT.md` and relevant Shiki SADRs and target ADRs.\n 3. Read active `.shiki/` Goal/task/ledger state when present.\n 4. Read active distilled rules from `.shiki/memories/` (status `distilled`, `active: true`, not revoked or superseded) for applicable guidance; the same rules are injected deterministically into task handoffs as the `## Distilled Rules` section (\u00a73.5).\n 5. Read the GitHub Issue or PR when the task is GitHub-backed.\n@@ -74,7 +77,7 @@\n - Define the Goal, completion conditions, non-goals, and risk level.\n - Treat `grill-with-docs` as the default planning entry point for non-trivial Goals.\n - Challenge domain terms against `CONTEXT.md`.\n-- Challenge hard-to-reverse tradeoffs against ADRs.\n+- Challenge hard-to-reverse tradeoffs against SADRs and target ADRs.\n - Explore code instead of asking when code can answer the question.\n - Run Context & Impact before implementation planning.\n - Produce a Task DAG, not an unordered checklist.\n@@ -94,7 +97,7 @@\n - For each question, provide your recommended answer.\n - Resolve dependency decisions before downstream design questions.\n - Update `CONTEXT.md` inline when terminology crystallizes.\n-- Offer ADRs only when the decision is hard to reverse, surprising without context, and a real tradeoff.\n+- Offer a SADR or target ADR only when the decision is hard to reverse, surprising without context, and a real tradeoff.\n - Convert unresolved questions into HITL blockers rather than allowing Codex to guess.\n \n ## PRD and Issues Standard\n@@ -103,7 +106,7 @@\n \n - Use the project's domain glossary vocabulary.\n - Avoid volatile file paths unless they are essential.\n-- Include implementation decisions, testing decisions, out-of-scope items, and relevant ADR/domain links.\n+- Include implementation decisions, testing decisions, out-of-scope items, and relevant SADR/target-ADR/domain links.\n - Identify deep-module opportunities and testable interfaces.\n \n When creating issues:\n@@ -121,7 +124,7 @@\n - Branch/worktree target.\n - Scope and non-goals.\n - Dependencies and locks.\n-- Relevant docs, ADRs, modules, and tests.\n+- Relevant docs, SADRs, target ADRs, modules, and tests.\n - Required skill invocations.\n - TDD expectations.\n - Pre-PR `code-review` gate expectations.\n@@ -152,7 +155,7 @@\n \n - Treat yourself as a judge, not implementer.\n - Do not write production code.\n-- Read `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant ADRs, PRD, task issue, PR body, diff, checks, reviews, labels, and `.shiki/` state.\n+- Read `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant SADRs and target ADRs, PRD, task issue, PR body, diff, checks, reviews, labels, and `.shiki/` state.\n - Evaluate every applicable checklist in `docs/agents/checklists.md`.\n - Map each acceptance criterion to durable evidence.\n - Distinguish implementation failure from missing evidence.\ndiff --git a/CONTEXT.md b/CONTEXT.md\nindex e61a9be06a0cce0d026484083a657bde2067e234..237ee8aa3ea8fab334ece49768bc5ed3b5af0974\n--- a/CONTEXT.md\n+++ b/CONTEXT.md\n@@ -25,7 +25,7 @@\n _Avoid_: jumping directly into code\n \n **grill-with-docs**:\n-A planning skill that challenges a plan against domain language, ADRs, code reality, and concrete edge scenarios. It resolves design-tree questions before PRD/issues. It runs inside Requirements Definition.\n+A planning skill that challenges a plan against domain language, Shiki SADRs, target ADRs, code reality, and concrete edge scenarios. It resolves design-tree questions before PRD/issues. It runs inside Requirements Definition.\n _Avoid_: generic brainstorming, silent assumptions\n \n **Requirements Definition**:\ndiff --git a/CONTRIBUTING.md b/CONTRIBUTING.md\nindex c6d9b656e73abb8a55e5f660123e03153dcb3e6d..9e37a8fbf668db5b22461aaae5548b8f1354394c\n--- a/CONTRIBUTING.md\n+++ b/CONTRIBUTING.md\n@@ -17,7 +17,7 @@\n 1. **Goal** \u2014 start from a user-approved Goal with an outcome, completion\n    conditions, non-goals, risk level, and success signals.\n 2. **grill-with-docs** \u2014 for any non-trivial change, challenge the plan against\n-   domain language, ADRs, and code reality before writing a PRD.\n+   domain language, Shiki SADRs, target ADRs, and code reality before writing a PRD.\n 3. **PRD** \u2014 capture settled product/engineering intent (`to-prd`).\n 4. **Spec Freeze** \u2014 the operator explicitly approves the PRD/requirements;\n    the plan records a `spec_freeze` block and no plan runs without it\n@@ -94,7 +94,7 @@\n   entries.\n - Do not hand-edit `.shiki/goals/*.json` or `.shiki/manifest.json` in a task PR.\n - Conversation state is not durable truth. If a decision matters, record it in\n-  GitHub, `.shiki/`, `CONTEXT.md`, or an ADR.\n+  GitHub, `.shiki/`, `CONTEXT.md`, a Shiki SADR, or a target ADR.\n \n ## Architecture Gate\n \n@@ -103,7 +103,7 @@\n or CI policy, package structure, three or more structural files, more than 100\n net new lines in a core module, three or more new files, or any high-risk label.\n When triggered, run `grill-with-docs` or `improve-codebase-architecture` and\n-record durable decisions in an ADR when needed (see `docs/adr/`).\n+record durable decisions in a Shiki SADR or target ADR as applicable (see `docs/adr/`).\n \n ## Safety\n \ndiff --git a/SYSTEM_PROMPT.md b/SYSTEM_PROMPT.md\nindex aaaea45e3a896c3a44ba89eec8bb84325ecf6c62..da009c86a88285269dee0155e58e550c6d744ea9\n--- a/SYSTEM_PROMPT.md\n+++ b/SYSTEM_PROMPT.md\n@@ -30,6 +30,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n Use the canonical source-of-truth order above. Runtime-specific reading orders\n are operational guidance only and must not override or reorder it. The current\n conversation is non-durable operator input.\n@@ -48,14 +51,14 @@\n \n A runtime may only do work that is explicitly assigned by the Goal, task, PR, or MergeGate decision.\n \n-Do not assume `openai/codex-action`, `OPENAI_API_KEY`, or API-key based Codex automation in the default Shiki path. API-key runners are explicit target-repo extensions and require their own ADR.\n+Do not assume `openai/codex-action`, `OPENAI_API_KEY`, or API-key based Codex automation in the default Shiki path. API-key runners are explicit target-repo extensions and require their own target ADR.\n \n ## 4. Non-negotiable Shiki flow\n \n For any non-trivial Goal, use this flow:\n \n 1. **Goal Seek**: define outcome, non-goals, risk level, completion criteria, and evidence requirements.\n-2. **grill-with-docs**: challenge the plan against `CONTEXT.md`, ADRs, code reality, terminology, boundaries, and edge scenarios. Ask one question at a time when the operator is available. If code can answer a question, inspect code instead of asking.\n+2. **grill-with-docs**: challenge the plan against `CONTEXT.md`, Shiki SADRs, target ADRs, code reality, terminology, boundaries, and edge scenarios. Ask one question at a time when the operator is available. If code can answer a question, inspect code instead of asking.\n 3. **Context & Impact**: identify modules, interfaces, seams, callers, dependencies, locks, risk, and verification surfaces.\n 4. **to-prd + Spec Freeze**: turn settled context into a PRD using domain vocabulary and testing decisions. The operator's explicit approval of the PRD is Spec Freeze, recorded as the plan's `spec_freeze` block; plans without it do not run. Steps 1-4 form Requirements Definition.\n 5. **to-issues**: decompose the PRD into independently grabbable vertical-slice issues. Prefer AFK slices; mark HITL slices when judgment is still required.\n@@ -75,7 +78,7 @@\n Before making any material change:\n \n 1. Read `AGENTS.md` and any runtime-specific wrapper such as `CLAUDE.md`.\n-2. Read `CONTEXT.md` and relevant ADRs.\n+2. Read `CONTEXT.md` and relevant SADRs and target ADRs.\n 3. Read the GitHub Issue / PR and `.shiki/` state for the active Goal or task.\n 4. Identify your role: Planner, Implementer, Reviewer, Completion Check Agent, Repairer, or Guardian-assist.\n 5. Identify Goal id, task id, branch, PR, dependencies, locks, risk level, required skills, and required evidence.\n@@ -103,7 +106,7 @@\n \n At minimum, identify:\n \n-- Relevant documents, ADRs, domain terms, and prior decisions.\n+- Relevant documents, SADRs, target ADRs, domain terms, and prior decisions.\n - Relevant modules, interfaces, seams, callers, owners, and tests.\n - Dependency relationships between tasks.\n - Candidate file locks and lock conflicts.\n@@ -152,7 +155,7 @@\n \n CCA must read:\n \n-- `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant ADRs.\n+- `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant SADRs and target ADRs.\n - Parent Goal issue, PRD issue, task issue, comments, and labels.\n - PR body, diff, commits, changed files, reviews, CI checks, and artifacts available to the workflow.\n - `.shiki/` task contract, locks, ledger entries, CCA checklist profile, and prior repair packets.\n@@ -214,7 +217,7 @@\n - Three or more new files.\n - Any high-risk or critical label.\n \n-When the architecture gate triggers, run `grill-with-docs` or `improve-codebase-architecture` as appropriate and record the decision in an ADR if it is hard to reverse or likely to be re-litigated.\n+When the architecture gate triggers, run `grill-with-docs` or `improve-codebase-architecture` as appropriate and record the decision in a Shiki SADR or target ADR as applicable if it is hard to reverse or likely to be re-litigated.\n \n ## 14. Skill Gate\n \n@@ -223,7 +226,7 @@\n Use these skills:\n \n - `setup-matt-pocock-skills`: before first use of the engineering skill set, or when issue tracker, triage labels, or domain docs are missing.\n-- `grill-with-docs`: default for non-trivial Goals before PRD/issues; required for ambiguous plans, terminology, boundaries, ADR-worthy tradeoffs, or design-tree decisions.\n+- `grill-with-docs`: default for non-trivial Goals before PRD/issues; required for ambiguous plans, terminology, boundaries, decision-record-worthy tradeoffs, or design-tree decisions.\n - `zoom-out`: unfamiliar code area or missing architectural map.\n - `to-prd`: convert settled context into a PRD.\n - `to-issues`: break a PRD, plan, or Goal into independently grabbable vertical-slice issues.\ndiff --git a/docs/adr/0001-keep-shiki-as-platform-name.md b/docs/adr/SADR-0001-keep-shiki-as-platform-name.md\nindex 20a9637c3b9d8c97de7ba8db7cfae4d82d9a7dba..8b685439959aada1b221c112f0ac3c51ca8a1695\n--- a/docs/adr/0001-keep-shiki-as-platform-name.md\n+++ b/docs/adr/SADR-0001-keep-shiki-as-platform-name.md\n@@ -1,3 +1,3 @@\n-# Keep Shiki As The Platform Name\n+# SADR-0001: Keep Shiki As The Platform Name\n \n Shiki remains the product name for the GitHub-native agentic engineering control plane. We considered more descriptive names such as MergePilot, MergeGate, and Kiroku, but kept Shiki because it carries the existing goal-seeking framework identity while remaining independent of Hermes, Codex, Claude Code, or any single runtime.\ndiff --git a/docs/adr/0002-use-agents-as-shared-constitution.md b/docs/adr/SADR-0002-use-agents-as-shared-constitution.md\nindex 73de30ecb078120eac540d9bc51855e3cd48d0e3..c5486182bf06fca36ea508496b013c67302fd989\n--- a/docs/adr/0002-use-agents-as-shared-constitution.md\n+++ b/docs/adr/SADR-0002-use-agents-as-shared-constitution.md\n@@ -1,3 +1,3 @@\n-# Use AGENTS As The Shared Agent Constitution\n+# SADR-0002: Use AGENTS As The Shared Agent Constitution\n \n Shiki uses `AGENTS.md` as the runtime-neutral constitution and `CLAUDE.md` as a thin Claude Code wrapper. This keeps Codex, Claude Code, Hermes Runner, and future Agent Runtimes aligned on the same GitHub-first MergeGate rules while still allowing runtime-specific instructions where needed.\ndiff --git a/docs/adr/0003-require-skill-gate-for-mergegate-readiness.md b/docs/adr/SADR-0003-require-skill-gate-for-mergegate-readiness.md\nindex 424be0c9af4c8f0136f1fde68a468c47ff84e764..7fc337987c7df9792f75f181dfcbed75fe5ffff2\n--- a/docs/adr/0003-require-skill-gate-for-mergegate-readiness.md\n+++ b/docs/adr/SADR-0003-require-skill-gate-for-mergegate-readiness.md\n@@ -1,3 +1,3 @@\n-# Require Skill Gate For MergeGate Readiness\n+# SADR-0003: Require Skill Gate For MergeGate Readiness\n \n Shiki requires relevant engineering skills to be invoked before execution when their triggers apply. This makes skills part of MergeGate readiness rather than optional prompt style, because Shiki depends on repeatable planning, issue decomposition, TDD, diagnosis, triage, and architecture review before autonomous or parallel agent work can be trusted.\ndiff --git a/docs/adr/0004-use-github-first-with-shiki-mirror.md b/docs/adr/SADR-0004-use-github-first-with-shiki-mirror.md\nindex d0a3a9174dc646a945a9d325954ffaf00c352040..a79c54ae9ee8eed2a0524192b864da4e22a92d3f\n--- a/docs/adr/0004-use-github-first-with-shiki-mirror.md\n+++ b/docs/adr/SADR-0004-use-github-first-with-shiki-mirror.md\n@@ -1,4 +1,4 @@\n-# ADR 0004: Use GitHub First With A Repository-Local Shiki Mirror\n+# SADR-0004: Use GitHub First With A Repository-Local Shiki Mirror\n \n ## Status\n \ndiff --git a/docs/adr/0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md b/docs/adr/SADR-0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\nindex b339baf49b89d9290e79c3d51b36e7b2bde3bd95..0f3c3eb9afccdb3403ce978e21d04fe12e495da0\n--- a/docs/adr/0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\n+++ b/docs/adr/SADR-0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\n@@ -1,8 +1,8 @@\n-# ADR 0005: Use Codex As Front And Claude Code Action As The GitHub Runtime\n+# SADR-0005: Use Codex As Front And Claude Code Action As The GitHub Runtime\n \n ## Status\n \n-Accepted (implementer default superseded by ADR 0008; the auth model and the\n+Accepted (implementer default superseded by SADR-0008; the auth model and the\n Claude Code Action GitHub runtime decision remain in force)\n \n ## Context\n@@ -26,4 +26,4 @@\n - Shiki avoids requiring `OPENAI_API_KEY` for the default workflow.\n - Codex work happens through the user's authenticated Codex front surface, not as a hidden backend Action.\n - Claude Code Action is configured for OAuth-token based subscription usage by default.\n-- API-key based automation remains possible only as an explicit target-repo extension with its own ADR.\n+- API-key based automation remains possible only as an explicit target-repo extension with its own target ADR.\ndiff --git a/docs/adr/0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md b/docs/adr/SADR-0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\nindex 4bcae9d1e52f0a37b8e9fbafa5cd14b6ee1993bd..7a38538a6ae9929567f0ade67e7ea6a60d0b3f7e\n--- a/docs/adr/0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\n+++ b/docs/adr/SADR-0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\n@@ -1,4 +1,4 @@\n-# ADR 0006: Separate Codex Implementation, CCA Completion, And MergeGate Authorization\n+# SADR-0006: Separate Codex Implementation, CCA Completion, And MergeGate Authorization\n \n ## Status\n \ndiff --git a/docs/adr/0007-packaging-and-release.md b/docs/adr/SADR-0007-packaging-and-release.md\nindex 79b84ddb97167372b44641e5b7860e400ce80822..bb445a2e2e856d9d1f5fdbe35de8d4e8cec0fb32\n--- a/docs/adr/0007-packaging-and-release.md\n+++ b/docs/adr/SADR-0007-packaging-and-release.md\n@@ -1,4 +1,4 @@\n-# ADR 0007: Packaging And Release For Shiki\n+# SADR-0007: Packaging And Release For Shiki\n \n ## Status\n \n@@ -25,7 +25,7 @@\n   not introduce third-party runtime dependencies or break the shim.\n - Releases must run inside the operator's subscription-authenticated toolchain\n   and must not require extra secrets beyond the built-in `GITHUB_TOKEN`\n-  (consistent with ADR 0005's no-API-key-by-default stance).\n+  (consistent with SADR-0005's no-API-key-by-default stance).\n - `.shiki/manifest.json` already encodes the install surface (`install.include`,\n   `install.create_directories`, `install.exclude_from_commit`), and it must not\n   be mutated as part of this change.\n@@ -34,7 +34,7 @@\n \n This is a hard-to-reverse platform decision (it sets the version source of\n truth, the release trigger, and the public install contract), so it is recorded\n-as an ADR.\n+as a SADR.\n \n ## Decision\n \n@@ -104,7 +104,7 @@\n 3. **Publishing to PyPI.** Rejected for now: Shiki is distributed as a\n    repository template + git-installable CLI, not a PyPI library; PyPI would add\n    credentials/secrets and a different versioning contract. Can be added later as\n-   its own ADR if a packaged distribution is needed.\n+   its own SADR if a packaged distribution is needed.\n 4. **Dynamic version (read `VERSION` via a build backend `dynamic` field).**\n    Rejected to keep `pyproject.toml` backend-agnostic and dependency-free; the\n    small duplication between `VERSION` and `pyproject.toml` is guarded by the\n@@ -123,4 +123,4 @@\n   releasing versions the whole platform but ships only `install.include` to\n   targets.\n - Future PyPI distribution or a console entry point are deferred and would each\n-  warrant their own ADR.\n+  warrant their own SADR.\ndiff --git a/docs/adr/0008-use-claude-code-as-default-implementer.md b/docs/adr/SADR-0008-use-claude-code-as-default-implementer.md\nindex dedf4510b7c7799521422f622029563d373db031..35c2625d908167fbc58855b5b63e283805f167e8\n--- a/docs/adr/0008-use-claude-code-as-default-implementer.md\n+++ b/docs/adr/SADR-0008-use-claude-code-as-default-implementer.md\n@@ -1,12 +1,12 @@\n-# ADR 0008: Use Claude Code As The Default Implementer Runtime\n+# SADR-0008: Use Claude Code As The Default Implementer Runtime\n \n ## Status\n \n-Proposed (supersedes ADR 0005 for the implementer default; keeps its auth model)\n+Proposed (supersedes SADR-0005 for the implementer default; keeps its auth model)\n \n ## Context\n \n-ADR 0005 made Codex Front the default implementation runtime, with Claude Code\n+SADR-0005 made Codex Front the default implementation runtime, with Claude Code\n restricted to planner and reviewer roles by the runtime registry, and the only\n autonomous implementation adapter was `shiki runner codex`.\n \n@@ -18,13 +18,13 @@\n - The platform's planning, review, and CCA layers already run on Claude Code,\n   and Claude Code's native capabilities (the built-in `/code-review` skill and\n   the Workflow multi-agent orchestration tool) have no Codex equivalent.\n-- The Goal lifecycle redesign (ADR 0009) requires an autonomous post-freeze\n+- The Goal lifecycle redesign (SADR-0009) requires an autonomous post-freeze\n   loop driven from the operator's Claude Code session, including bounded\n   repair dispatch, which must not depend on a runtime that cannot be invoked\n   unattended.\n \n The constitution claims Shiki is runtime-agnostic (\"not a Claude-only\n-workflow\", ADR 0002). Any change to the implementer default must preserve the\n+workflow\", SADR-0002). Any change to the implementer default must preserve the\n registry-based runtime model rather than hardcoding one vendor.\n \n ## Decision\n@@ -39,7 +39,7 @@\n   the task to review or repair-needed.\n - Keep Codex registered as an optional implementer runtime. `shiki runner\n   codex`, the Codex handoff templates, and the ChatGPT OAuth auth model from\n-  ADR 0005 remain supported for tasks explicitly assigned to `codex`.\n+  SADR-0005 remain supported for tasks explicitly assigned to `codex`.\n - Commit to mandating two Claude-native capabilities as loop contract in\n   follow-up Goals (status: the code-review gate was delivered by Goal G-B \u2014\n   skill registry, checklists item PR-12, default required_skills \u2014 while the\n@@ -60,7 +60,7 @@\n \n Out of scope: removing Codex support, changing the CCA runtime (Claude Code\n Action with `CLAUDE_CODE_OAUTH_TOKEN` stays), and API-key based automation\n-(still requires its own ADR per ADR 0005).\n+(still requires its own target ADR per SADR-0005).\n \n ## Consequences\n \n@@ -100,7 +100,7 @@\n   harness rejection of direct `codex exec` is outside Shiki's control, and the\n   plan's mandatory native capabilities cannot run on Codex at all.\n - **Remove Codex entirely**: rejected; it abandons the runtime-agnostic\n-  constitution (ADR 0002) for no operational gain and forecloses\n+  constitution (SADR-0002) for no operational gain and forecloses\n   cross-runtime adversarial review.\n - **GitHub Actions cloud implementation via claude-code-action**: rejected as\n   default; durable evidence is automatic but token cost, Node 24 workflow\ndiff --git a/docs/adr/0009-requirements-definition-and-spec-freeze.md b/docs/adr/SADR-0009-requirements-definition-and-spec-freeze.md\nindex 29b3f7871bef9203a8b4a831a65c39eb2f506e42..52e7dd69ad0aa4efe0b68c62678abbf7df02aaec\n--- a/docs/adr/0009-requirements-definition-and-spec-freeze.md\n+++ b/docs/adr/SADR-0009-requirements-definition-and-spec-freeze.md\n@@ -1,4 +1,4 @@\n-# ADR 0009: Merge Grill And Planning Into Requirements Definition With Spec Freeze\n+# SADR-0009: Merge Grill And Planning Into Requirements Definition With Spec Freeze\n \n ## Status\n \ndiff --git a/docs/adr/0010-external-ai-guardian-review.md b/docs/adr/SADR-0010-external-ai-guardian-review.md\nindex e6108bbe8bb4e73e0ff4684c8f78001a80438c4e..f3510b145664298adafb151b6f0db51b972f01ec\n--- a/docs/adr/0010-external-ai-guardian-review.md\n+++ b/docs/adr/SADR-0010-external-ai-guardian-review.md\n@@ -1,4 +1,4 @@\n-# ADR 0010: External AI Guardian Review As A First-Class Approval Authority\n+# SADR-0010: External AI Guardian Review As A First-Class Approval Authority\n \n ## Status\n \n@@ -8,7 +8,7 @@\n \n Shiki is intended to be an autonomous, recursive, self-improving agent\n framework: authority-in-the-loop, not human-in-the-loop. Until now the Guardian\n-approval required for high/critical-risk merges (guardian-policy.json, ADR\n+approval required for high/critical-risk merges (guardian-policy.json, SADR\n governance) recognized only human authority sources \u2014 a GitHub review approval,\n the `guardian:approved` label applied by a configured Guardian, or a comment\n carrying `Guardian approval granted` plus the current head SHA.\n@@ -89,7 +89,7 @@\n   autonomous design the operator intends and produced the impersonation\n   incident.\n - **Record AI approval as an operator comment**: rejected as forgery of human\n-  authority \u2014 the exact failure this ADR corrects.\n+  authority \u2014 the exact failure this SADR corrects.\n - **Deliver the artifact as a committed `.shiki/` file**: rejected; a committed\n   approval inside the PR being approved is forgeable by anyone who can push.\n - **A dedicated reviewer bot identity instead of a relayed comment**: deferred;\ndiff --git a/docs/adr/0011-autonomous-quality-gate-evidence.md b/docs/adr/SADR-0011-autonomous-quality-gate-evidence.md\nindex bca0246c5d46e44afbde15486a75eec425fc02f5..9c93eac16d8c3a6d4b11bd7224e3955102cd7db4\n--- a/docs/adr/0011-autonomous-quality-gate-evidence.md\n+++ b/docs/adr/SADR-0011-autonomous-quality-gate-evidence.md\n@@ -1,4 +1,4 @@\n-# ADR 0011: Autonomous Quality-Gate Evidence \u2014 Deterministic Facts And An Independent Verifier\n+# SADR-0011: Autonomous Quality-Gate Evidence \u2014 Deterministic Facts And An Independent Verifier\n \n ## Status\n \ndiff --git a/docs/adr/0012-autonomous-loop-closeout-to-main.md b/docs/adr/SADR-0012-autonomous-loop-closeout-to-main.md\nindex 637fad6dde4989c9c2bf0f81f676d8d29991123c..ec7d85f81030a676bcbba28b43eb365422a36eac\n--- a/docs/adr/0012-autonomous-loop-closeout-to-main.md\n+++ b/docs/adr/SADR-0012-autonomous-loop-closeout-to-main.md\n@@ -1,4 +1,4 @@\n-# 0012 \u2014 Autonomous loop pushes goal completion to main via a closeout PR\n+# SADR-0012 \u2014 Autonomous loop pushes goal completion to main via a closeout PR\n \n - Status: accepted\n - Date: 2026-06-18\ndiff --git a/docs/adr/0013-bootstrap-grants-actions-approve-pull-requests.md b/docs/adr/SADR-0013-bootstrap-grants-actions-approve-pull-requests.md\nindex b6c69d64f1c5ced346b88d6859586715b3b5bd0e..cf48ff4a6716267f539ca58edd1a852e7c7a2d64\n--- a/docs/adr/0013-bootstrap-grants-actions-approve-pull-requests.md\n+++ b/docs/adr/SADR-0013-bootstrap-grants-actions-approve-pull-requests.md\n@@ -1,4 +1,4 @@\n-# ADR 0013: Bootstrap Grants GitHub Actions Permission To Create And Approve Pull Requests\n+# SADR-0013: Bootstrap Grants GitHub Actions Permission To Create And Approve Pull Requests\n \n ## Status\n \n@@ -31,7 +31,7 @@\n security tradeoff: it lets repository automation satisfy a human-review gate. In\n a multi-maintainer repo that could let a compromised or buggy workflow\n self-approve changes. So whether bootstrap turns it on by default \u2014 and with\n-what default token scope \u2014 is an ADR-worthy decision, not an implementation\n+what default token scope \u2014 is a SADR-worthy decision, not an implementation\n detail.\n \n ## Decision\n@@ -48,7 +48,7 @@\n Specifically:\n \n - **`can_approve_pull_request_reviews` defaults to `true`.** Shiki's default\n-  posture is solo/AFK autonomous operation (ADR 0008/0009/0012), where the CCA\n+  posture is solo/AFK autonomous operation (SADR-0008/0009/0012), where the CCA\n   Review Bridge is the only path that can satisfy `required_review: true` after\n   the CCA and Guardian gates have already passed. The default makes a\n   freshly-bootstrapped repo able to run the autonomous loop end to end without a\ndiff --git a/docs/adr/0014-codex-app-external-ai-guardian-ui-adapter.md b/docs/adr/SADR-0014-codex-app-external-ai-guardian-ui-adapter.md\nindex 238db06279334caa341c117362df9271382a7fcd..65f12e9939321e1b3e389a34c822fc1549e5155d\n--- a/docs/adr/0014-codex-app-external-ai-guardian-ui-adapter.md\n+++ b/docs/adr/SADR-0014-codex-app-external-ai-guardian-ui-adapter.md\n@@ -1,4 +1,4 @@\n-# ADR 0014: Use Codex App As The External AI Guardian UI Adapter\n+# SADR-0014: Use Codex App As The External AI Guardian UI Adapter\n \n ## Status\n \ndiff --git a/docs/adr/0015-contract-approval-before-dispatch.md b/docs/adr/SADR-0015-contract-approval-before-dispatch.md\nindex fad6c656741a20a7684ba16080a7db159f63a52f..6061271a46d3f1d6ae7174477de3fb75673123a6\n--- a/docs/adr/0015-contract-approval-before-dispatch.md\n+++ b/docs/adr/SADR-0015-contract-approval-before-dispatch.md\n@@ -1,4 +1,4 @@\n-# ADR 0015: Approve The Task Contract Before Dispatch, Not The Diff After It\n+# SADR-0015: Approve The Task Contract Before Dispatch, Not The Diff After It\n \n ## Status\n \n@@ -9,7 +9,7 @@\n Guardian approval for high/critical risk is expressible only on a pull request.\n Every approval source in `guardian-policy.json` \u2014 GitHub review, the\n `guardian:approved` label, the `Guardian approval granted` comment, and the\n-`external_ai_guardian_review` artifact (ADR 0010/0014) \u2014 is bound to a PR, and\n+`external_ai_guardian_review` artifact (SADR-0010/0014) \u2014 is bound to a PR, and\n three of the four require the current head SHA. Approval therefore cannot exist\n before an implementation PR exists.\n \n@@ -81,7 +81,7 @@\n An envelope breach must surface only on the non-repairable policy gate, never on\n the repairable `MergeGate metadata check`. Otherwise the Goal Loop routes it to\n `dispatch_repair` \u2014 instructing a runner to make the breach go away \u2014 which is\n-exactly what this ADR forbids.\n+exactly what this SADR forbids.\n \n **Contract immutability.** After registration, an implementation PR must not\n change the governance fields of its own task contract (`scope`, `non_goals`,\n@@ -116,7 +116,7 @@\n fails any verdict that is not `complete`, and the signal the CCA prompt reads is\n computed from `evaluate_guardian_approval` alone. Without the OR-merge at the\n signal, a contract-approved PR still returns `needs_guardian`, the CCA job fails,\n-and the MergeGate policy check never runs \u2014 the gate this ADR changes would never\n+and the MergeGate policy check never runs \u2014 the gate this SADR changes would never\n be reached. The CCA *prompt* needs no change: a `contract_approval` entry in the\n signal's existing `sources` array satisfies its current wording.\n \n@@ -130,7 +130,7 @@\n written, and an approved contract lets its implementation PRs merge through the\n same automatic path as low/medium risk. The wasted implement-then-block cycle\n disappears. Guardian review shifts from reading diffs to reading contracts, which\n-is both earlier and smaller; the external AI Guardian path (ADR 0010/0014) works\n+is both earlier and smaller; the external AI Guardian path (SADR-0010/0014) works\n on contracts with no change.\n \n We accept that the Guardian authorizes a class of change without seeing its\n@@ -146,7 +146,7 @@\n adversarial tests for the new approval path equivalent in rigor to\n `test_shiki_governance_evidence.sh`.\n \n-Bootstrap costs, which apply only while this ADR is being implemented. The tasks\n+Bootstrap costs, which apply only while this SADR is being implemented. The tasks\n that build Contract Approval merge under the existing rules, so the ones\n dispatched after the pre-dispatch gate lands but before the approval path lands\n need a Guardian-approved Contract PR *and* a live Guardian approval on their\n@@ -210,7 +210,7 @@\n risk; they do not bound authorized work.\n \n **Auto-escalate risk on envelope breach instead of stopping.** Returns the\n-workflow to a diff-time Guardian decision \u2014 the exact behavior this ADR removes.\n+workflow to a diff-time Guardian decision \u2014 the exact behavior this SADR removes.\n \n **Resolve governance fields from the base snapshot only.** Rejected against the\n registration flow: implementation PRs add their own contract, so the base has no\ndiff --git a/docs/adr/0016-locks-bound-product-paths.md b/docs/adr/SADR-0016-locks-bound-product-paths.md\nindex b5e850f3f1f7f4ef6e7f9d1763a29a51bd300c45..46b40a9b99a7aae644c02bb458f5473ff0a90ce2\n--- a/docs/adr/0016-locks-bound-product-paths.md\n+++ b/docs/adr/SADR-0016-locks-bound-product-paths.md\n@@ -1,4 +1,4 @@\n-# ADR 0016: Locks bound product paths; the Shiki mirror is bound by record identity\n+# SADR-0016: Locks bound product paths; the Shiki mirror is bound by record identity\n \n ## Status\n \n@@ -6,8 +6,8 @@\n \n - Date: 2026-07-28\n - Deciders: operator (mizutani-140), Claude (planner)\n-- Related: ADR 0004 (GitHub-first with a `.shiki` mirror), ADR 0009 (Spec Freeze),\n-  ADR 0012 (autonomous loop closeout to main), ADR 0015 (Contract Approval before\n+- Related: SADR-0004 (GitHub-first with a `.shiki` mirror), SADR-0009 (Spec Freeze),\n+  SADR-0012 (autonomous loop closeout to main), SADR-0015 (Contract Approval before\n   dispatch), remediation goal task R-02, the live PR #179 deadlock.\n \n ## Context\n@@ -90,11 +90,11 @@\n files, documentation files and named source modules are nameable; runtime-\n allocated ledger, runner and report ids are not \u2014 and those live in `.shiki`,\n which locks no longer govern. Existing frozen contracts are brought into line by\n-a one-time Spec Amendment per plan, which must land before ADR 0015's contract\n+a one-time Spec Amendment per plan, which must land before SADR-0015's contract\n immutability makes `locks` unwritable from the implementation PR.\n \n **Lock state is resolved from the durable side.** MergeGate resolves the lock\n-registry, and ADR 0015's governance fields, from the base `.shiki` snapshot when\n+registry, and SADR-0015's governance fields, from the base `.shiki` snapshot when\n present, falling back to head only when absent. The `MergeGate metadata check`\n job is given `--base-shiki`, which it does not receive today. Locks are not\n derived from the live open-pull-request set: a pull request body is untrusted\n@@ -108,7 +108,7 @@\n Explicitly out of scope: moving MergeGate's contract resolution off the PR\n checkout wholesale; making the mirror a projection of GitHub state; introducing\n new lock kinds; moving ledger evidence out of the repository; and unwinding\n-ADR 0012's closeout pull request. Registration of multi-task goals is ADR 0015's\n+SADR-0012's closeout pull request. Registration of multi-task goals is SADR-0015's\n Contract PR and is not re-decided here.\n \n ## Consequences\n@@ -127,7 +127,7 @@\n \n Risks accepted. First, goal-scoped identity is only as strong as `goal_id`,\n which is resolved from the PR body and the head task file; the mitigation is to\n-add `goal_id` to ADR 0015's frozen governance field set, and until that lands a\n+add `goal_id` to SADR-0015's frozen governance field set, and until that lands a\n task file can name a foreign goal. Second, base-when-present resolution is\n unavailable on fork pull requests, where the head is contributor-controlled;\n fork pull requests already cannot complete a Shiki merge, and this decision does\n@@ -139,7 +139,7 @@\n path, not by implementation pull requests.\n \n New obligations: one Spec Amendment per frozen plan to rewrite `locks`, ordered\n-before ADR 0015 T1; extension of the adversarial suites in\n+before SADR-0015 T1; extension of the adversarial suites in\n `scripts/test_shiki_adversarial_state.sh` and\n `scripts/test_shiki_governance_evidence.sh` to cover the eleven newly-scoped\n directories; and updating `scripts/test_shiki_loop_lock_guard.sh`,\n@@ -151,32 +151,32 @@\n whether the loop's transitive sync of ledger `evidence` references can be\n deleted once the Contract PR carries its own source plan.\n \n-## Relationship to ADR 0004\n+## Relationship to SADR-0004\n \n-This ADR **clarifies something ADR 0004 left unspecified**. ADR 0004 decides that\n+This SADR **clarifies something SADR-0004 left unspecified**. SADR-0004 decides that\n GitHub is the operational source of truth and that each target repository keeps a\n `.shiki/` mirror. It says nothing about pull requests writing the mirror, does\n not name MergeGate, and does not make the mirror an input to a gate. Nothing in\n-ADR 0004's Decision or Consequences is amended, superseded or weakened: the\n+SADR-0004's Decision or Consequences is amended, superseded or weakened: the\n mirror still exists, still records the same artifacts, and pull request\n-boundaries remain the coordination surface. What this ADR settles is the question\n+boundaries remain the coordination surface. What this SADR settles is the question\n 0004 never reached \u2014 by what authority a pull request may write a mirror record \u2014\n and it answers it the way `.shiki/manifest.json` already declares\n (`pr_mutation: current-task-or-current-goal-only`).\n \n-It **amends ADR 0012**, whose Decision item 5 states that \"Loop-executed tasks\n+It **amends SADR-0012**, whose Decision item 5 states that \"Loop-executed tasks\n declare the `path:.shiki/**` lock, which covers every `.shiki` file the closeout\n stages ... so `files_outside_locks` is satisfied without per-file lock\n bookkeeping.\" That clause is replaced: the closeout's mirror writes are\n-authorized by record identity, not by a declared lock. ADR 0012's substantive\n+authorized by record identity, not by a declared lock. SADR-0012's substantive\n decision \u2014 that completion reaches `main` through a closeout pull request rather\n than local attestation \u2014 is preserved unchanged.\n \n-It is **complementary to ADR 0015**, not a substitute. ADR 0015 owns contract\n+It is **complementary to SADR-0015**, not a substitute. SADR-0015 owns contract\n immutability, the pre-dispatch gate, the Contract PR and the Scope Envelope. This\n-ADR owns what a lock means and who may write the mirror. The one ordering\n+SADR owns what a lock means and who may write the mirror. The one ordering\n constraint between them is that the lock-narrowing Spec Amendment must precede\n-ADR 0015 T1's freezing of the `locks` field.\n+SADR-0015 T1's freezing of the `locks` field.\n \n ## Alternatives Considered\n \n@@ -186,7 +186,7 @@\n the only writer that can register them may run. Migrating the Guardian signal to\n that store makes every registration PR fail closed to `needs_guardian` at every\n risk level, because the task file is by definition absent from the store, which\n-removes the low/medium autonomous path ADR 0008/0009 exists to provide. And it\n+removes the low/medium autonomous path SADR-0008/0009 exists to provide. And it\n leaves schemas, manifest, config, guardian policy, templates, migrations, plans,\n worktrees, runner records, runs and handoffs with no writer at all in a\n repository whose product is `.shiki`. Measured benefit on the frozen backlog:\n@@ -199,14 +199,14 @@\n namespace is not in fact ownership-partitioned \u2014 56 ledger entries carry no\n `task_id`, and one goal DAG is writable by 35 registered tasks. The correct\n expression of its insight is that locks do not govern the mirror at all, which is\n-what this ADR decides.\n+what this SADR decides.\n \n **Make the mirror a projection written only by a registration writer and a\n post-merge projection job.** Rejected. It requires a bypass actor with write\n access to the protected default branch; it makes its own master invariant\n readable from a manifest that pull requests may edit; it deletes both reconcile\n modes and leaves a corrupted mirror with no in-band repair path, contradicting\n-ADR 0004's consequence that mirror conflicts are repaired against GitHub state;\n+SADR-0004's consequence that mirror conflicts are repaired against GitHub state;\n and its content-derived ids are rejected by `validate_shiki`'s currently-green id\n pattern.\n \ndiff --git a/docs/adr/0017-bookkeeping-closeouts-do-not-inherit-risk.md b/docs/adr/SADR-0017-bookkeeping-closeouts-do-not-inherit-risk.md\nindex 554e3a0d526a72ca067108c5464cc68c3edcf174..b9676857a4713850e063af7c39c8e0b01ecb4c66\n--- a/docs/adr/0017-bookkeeping-closeouts-do-not-inherit-risk.md\n+++ b/docs/adr/SADR-0017-bookkeeping-closeouts-do-not-inherit-risk.md\n@@ -1,4 +1,4 @@\n-# ADR 0017: A Bookkeeping Closeout Does Not Inherit Its Task's Risk\n+# SADR-0017: A Bookkeeping Closeout Does Not Inherit Its Task's Risk\n \n ## Status\n \n@@ -6,7 +6,7 @@\n \n ## Context\n \n-ADR 0012 made the goal loop push a task's completion to the default branch through a\n+SADR-0012 made the goal loop push a task's completion to the default branch through a\n closeout pull request: after the implementation PR merges, the loop opens a second PR\n carrying `task.status = done`, `lock.state = released`, and \u2014 when the task completes\n its goal \u2014 `goal.status = complete` plus one scorecard report. Nothing else. The\n@@ -49,14 +49,14 @@\n 4. The task file's governance fields (`scope`, `non_goals`, `required_skills`,\n    `risk_level`, `locks`, `acceptance_checks`, `test_command`) are byte-identical to\n    the base snapshot. Only `status`, `expected_pr`, `closeout_pr`, `ledger_evidence`\n-   and `expected_branch` may differ \u2014 the ADR 0015 contract-immutability rule,\n+   and `expected_branch` may differ \u2014 the SADR-0015 contract-immutability rule,\n    unchanged.\n \n    `expected_branch` is in that set because a closeout is *by construction* on a\n    different branch from the implementation it closes, and the loop rewrites the\n    field accordingly. It is safe to let it differ because MergeGate independently\n    requires `expected_branch` to equal the PR's actual head ref, so a PR cannot set\n-   it to anything other than the branch it is really on. The first draft of this ADR\n+   it to anything other than the branch it is really on. The first draft of this SADR\n    omitted it, which made the exemption unreachable: every real closeout failed\n    condition 4 on a field the loop always changes. See PR #209.\n 5. The state transitions are exactly the terminal set: task `review` \u2192 `done`, lock\n@@ -113,12 +113,12 @@\n classification must be derived, not asserted.\n \n **Fold the closeout into the implementation PR** so no second PR exists. Rejected under\n-ADR 0012, which recorded why: a task file that is already `done` on the implementation\n+SADR-0012, which recorded why: a task file that is already `done` on the implementation\n branch short-circuits the loop's review \u2192 CCA \u2192 merge \u2192 done ordering into a local\n goal-completion before any merge happens.\n \n **Have the loop auto-merge closeouts with elevated credentials.** Rejected outright: that\n-is a runtime authoring its own approval, which ADR 0010 and the constitution's\n+is a runtime authoring its own approval, which SADR-0010 and the constitution's\n approval-record prohibition forbid. It would also be indistinguishable, in the evidence,\n from the forgery those rules exist to prevent.\n \ndiff --git a/docs/adr/0018-carried-guardian-approval-across-a-proven-base-sync.md b/docs/adr/SADR-0018-carried-guardian-approval-across-a-proven-base-sync.md\nindex 22358562da390ed8b45cd600da08a43d378b4680..e6e2907cefa776460af73ddf7d39db605f3c39b0\n--- a/docs/adr/0018-carried-guardian-approval-across-a-proven-base-sync.md\n+++ b/docs/adr/SADR-0018-carried-guardian-approval-across-a-proven-base-sync.md\n@@ -1,4 +1,4 @@\n-# ADR 0018: Carried Guardian Approval Across A Proven Base Sync\n+# SADR-0018: Carried Guardian Approval Across A Proven Base Sync\n \n ## Status\n \n@@ -34,7 +34,7 @@\n Guardian evaluator that consumes it (`shiki_guardian.evaluate_guardian_approval`,\n the `guardian_comment_carried` source) were built and tested in isolation. Neither\n does anything until a caller passes the flags that turn the carry on, so the\n-feature was invisible in CI. This ADR records the decision the two of them\n+feature was invisible in CI. This SADR records the decision the two of them\n implement, and this task wires it into the four CI invocations that judge Guardian\n approval.\n \ndiff --git a/docs/adr/README.md b/docs/adr/README.md\nindex c70e057a4520c36e042ab68c53cd4a21ef709b71..5da43856836e2ce6a150f8127766e2f88355ab4b\n--- a/docs/adr/README.md\n+++ b/docs/adr/README.md\n@@ -1,40 +1,41 @@\n-# Architecture Decision Records\n+# Decision Record Namespaces\n \n-This directory holds Shiki's Architecture Decision Records (ADRs): durable\n-records of hard-to-reverse platform decisions. ADRs are part of the Shiki source\n-of truth alongside GitHub state, the `.shiki/` mirror, and `CONTEXT.md`.\n+This directory holds two independent decision namespaces:\n \n-## How ADRs Work\n+- Shiki Architecture Decision Records (SADRs), named\n+  `SADR-NNNN-<short-slug>.md`, record hard-to-reverse Shiki platform decisions.\n+- Target Architecture Decision Records (ADRs), named\n+  `NNNN-<short-slug>.md`, belong to the target product.\n \n-- New ADRs are added as `docs/adr/NNNN-<short-slug>.md`, where `NNNN` is the next\n-  zero-padded sequence number.\n-- The **canonical list of ADRs is the set of files matching `docs/adr/*.md`** in\n-  this directory. Browse `docs/adr/*.md` for the authoritative, current set.\n-- Use [`template.md`](template.md) as the starting point for a new ADR.\n-- Record an ADR when a decision is hard to reverse, surprising without context,\n-  and a real tradeoff. Do not write an ADR for routine or easily reversible\n-  choices.\n+The same four-digit number may appear once in each namespace. A platform\n+`SADR-NNNN` citation resolves only to `SADR-NNNN-*.md`; a target `ADR NNNN`\n+citation resolves only to `NNNN-*.md`.\n \n-Using the `docs/adr/*.md` glob as the source of truth means other PRs can add an\n-ADR without editing this index file, avoiding merge contention on a\n-hand-maintained enumeration.\n+## How Decision Records Work\n \n-## Current ADRs (snapshot)\n-\n-The list below is a convenience snapshot and may lag behind the directory. When\n-in doubt, list `docs/adr/*.md`.\n+- New Shiki platform decisions use the next zero-padded SADR number.\n+- New target product decisions use the target's own ADR sequence.\n+- The canonical list is the files in `docs/adr/`; no hand-maintained index is\n+  authoritative.\n+- Use [`template.md`](template.md) as the starting point and choose the filename\n+  namespace that owns the decision.\n+- Record a decision when it is hard to reverse, surprising without context, and\n+  a real tradeoff. Do not write a decision record for routine or easily\n+  reversible choices.\n \n-- [0001 \u2014 Keep Shiki As The Platform Name](0001-keep-shiki-as-platform-name.md)\n-- [0002 \u2014 Use AGENTS As The Shared Agent Constitution](0002-use-agents-as-shared-constitution.md)\n-- [0003 \u2014 Require Skill Gate For MergeGate Readiness](0003-require-skill-gate-for-mergegate-readiness.md)\n-- [0004 \u2014 Use GitHub First With A Repository-Local Shiki Mirror](0004-use-github-first-with-shiki-mirror.md)\n-- [0005 \u2014 Use Codex As Front And Claude Code Action As The GitHub Runtime](0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md)\n-- [0006 \u2014 Separate Codex Implementation, CCA Completion, And MergeGate Authorization](0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md)\n+Using the directory as the source of truth lets independent changes add records\n+without contending on an enumerated index.\n \n-## Adding A New ADR\n+## Adding A Shiki SADR\n \n-1. Copy `template.md` to `docs/adr/NNNN-<short-slug>.md`.\n+1. Copy `template.md` to `docs/adr/SADR-NNNN-<short-slug>.md`.\n 2. Fill in the title, status, context, decision, and consequences.\n-3. Set the status to `Proposed`, then `Accepted` once the decision is approved.\n-4. Open a PR following `CONTRIBUTING.md`. You do not need to edit this index for\n-   the ADR to be discoverable via `docs/adr/*.md`.\n+3. Set the status to `Proposed`, then `Accepted` once the proper authority has\n+   approved the decision.\n+4. Open a PR following `CONTRIBUTING.md`.\n+\n+## Adding A Target ADR\n+\n+1. Copy `template.md` to `docs/adr/NNNN-<short-slug>.md` using the target's own\n+   sequence.\n+2. Cite it as `ADR NNNN`; do not prefix its filename with `SADR-`.\ndiff --git a/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md b/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\nnew file mode 100644\nindex 0000000000000000000000000000000000000000..7455ce1f3bed02ddddb851ac21a1d1a648c9aeba\n--- /dev/null\n+++ b/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\n@@ -0,0 +1,55 @@\n+# SADR-0019: Separate Shiki Platform SADRs From Target ADRs\n+\n+## Status\n+\n+Accepted\n+\n+## Context\n+\n+Shiki installs its platform decision records into `docs/adr/` in every target\n+repository. Shiki and its targets previously used the same numeric\n+`NNNN-<slug>.md` namespace. A target that already owned ADRs 0013 through 0018\n+therefore acquired two records for each number during installation, making an\n+`ADR NNNN` citation ambiguous. Renumbering target decisions would rewrite target\n+history, while renumbering Shiki decisions per target would destroy their stable\n+platform identity.\n+\n+Upgrades also need to remove the former Shiki-owned numeric paths without ever\n+mistaking a target-owned ADR for platform content. The install stamp already\n+records exact shipped paths and content digests, so it can prove ownership only\n+when both still match.\n+\n+## Decision\n+\n+Shiki platform decisions use the `SADR` namespace:\n+\n+- records are named `docs/adr/SADR-NNNN-<slug>.md`;\n+- live platform citations use `SADR-NNNN`;\n+- target product decisions retain `docs/adr/NNNN-<slug>.md` and `ADR NNNN`;\n+- the reference validator resolves the two namespaces independently, so the same\n+  four-digit number may exist once in each namespace.\n+\n+Before a forced upgrade writes anything, the installer inventories the exact\n+former numeric Shiki ADR paths. It may delete a legacy path only when the\n+existing install stamp contains that exact path and its recorded SHA-256 digest\n+matches the file currently on disk. All ownership blockers are reported\n+together and abort the upgrade before its first write. A non-forced install\n+never performs this cleanup.\n+\n+Historical `.shiki/` evidence remains unchanged because it records the namespace\n+that existed when that evidence was produced.\n+\n+## Consequences\n+\n+- Targets may use any ADR number without colliding with Shiki's platform SADRs.\n+- Fresh installs preserve target ADR bytes and install a stable SADR set.\n+- Forced upgrades fail closed when legacy ownership cannot be proven, and a\n+  successful upgrade refreshes the stamp without removed legacy paths.\n+- Current governance, packaging, workflow, prompt, script, and skill references\n+  must move with the platform records.\n+\n+## Alternatives Considered\n+\n+- Collision-only refusal leaves affected targets permanently unupgradable.\n+- Renumbering target ADRs mutates target-owned history.\n+- Per-target Shiki renumbering makes platform citations non-portable.\ndiff --git a/docs/adr/template.md b/docs/adr/template.md\nindex a92a97daddd0b00ab9dd626cc8d86942da2eafed..17dd79175b5fee2105935557f691b40cf9492942\n--- a/docs/adr/template.md\n+++ b/docs/adr/template.md\n@@ -1,14 +1,14 @@\n-# NNNN: <Short Decision Title>\n+# <SADR-NNNN or NNNN>: <Short Decision Title>\n \n ## Status\n \n-Proposed | Accepted | Superseded by ADR-NNNN\n+Proposed | Accepted | Superseded by SADR-NNNN or target ADR NNNN\n \n ## Context\n \n What is the situation, constraint, or force that requires a decision? Reference\n-relevant domain terms (`CONTEXT.md`), prior ADRs, and code reality. State the\n-problem, not the solution.\n+relevant domain terms (`CONTEXT.md`), prior Shiki SADRs or target ADRs, and code\n+reality. State the problem, not the solution.\n \n ## Decision\n \ndiff --git a/docs/agents/autonomous-loop-e2e-acceptance.md b/docs/agents/autonomous-loop-e2e-acceptance.md\nindex bbf84c6081987f159ac6ad9ecf3c82b7aa72e7e8..5c13c1d02a05a90b0dc46cbef7c95a9846d0c237\n--- a/docs/agents/autonomous-loop-e2e-acceptance.md\n+++ b/docs/agents/autonomous-loop-e2e-acceptance.md\n@@ -40,8 +40,8 @@\n 3. `gh auth status` is logged in with merge permission on the repo.\n 4. Branch protection + required checks are configured exactly as the goal-loop\n    required-check set expects (see `docs/agents/branch-protection-smoke.md`).\n-5. The repo `.shiki/policy` permits autonomous low/medium-risk merges (ADR\n-   0008/0009); the MergeGate policy check is the recorded-authority gate.\n+5. The repo `.shiki/policy` permits autonomous low/medium-risk merges\n+   (SADR-0008/0009); the MergeGate policy check is the recorded-authority gate.\n \n The operator's only actions are: register the task and start the loop. **No\n manual pre-clearing** means after `shiki loop run` starts, the operator does not\ndiff --git a/docs/agents/bootstrap-command.md b/docs/agents/bootstrap-command.md\nindex 05f7f203a52cf412b9f97084a6e131ed0f785afd..f7aa0b734dfebee4a3b139335b66ee3415f48713\n--- a/docs/agents/bootstrap-command.md\n+++ b/docs/agents/bootstrap-command.md\n@@ -189,7 +189,9 @@\n ```\n \n Use this only for tests, fixtures, or explicit local-only template inspection.\n-Use `--force` only when you intentionally want to overwrite existing target files.\n+Use `--force` only when you intentionally want to refresh the target template.\n+Legacy numeric Shiki decision cleanup is stamp-and-digest authorized before any\n+write; target ADR paths are not cleanup candidates.\n \n ## Slash Command\n \ndiff --git a/docs/agents/checklists.md b/docs/agents/checklists.md\nindex 4a7f03ade346f53a1c1d82981f3fd744f9ab4547..0c5ec6c53dc7197b7b7a7d18e667b325babd6c45\n--- a/docs/agents/checklists.md\n+++ b/docs/agents/checklists.md\n@@ -32,18 +32,18 @@\n | ID | Item | Blocking |\n | --- | --- | --- |\n | GD-01 | Plan was challenged against `CONTEXT.md`. | blocking for non-trivial Goals |\n-| GD-02 | Relevant ADRs were read or absence was recorded. | blocking for non-trivial Goals |\n+| GD-02 | Relevant SADRs and target ADRs were read or absence was recorded. | blocking for non-trivial Goals |\n | GD-03 | Ambiguous terms were resolved or converted to HITL blockers. | blocking |\n | GD-04 | Edge scenarios were considered. | advisory |\n | GD-05 | Code reality was checked when code could answer. | blocking when applicable |\n | GD-06 | New domain terms were added to `CONTEXT.md` when needed. | blocking when applicable |\n-| GD-07 | ADR was created or explicitly skipped using the ADR criteria. | blocking when applicable |\n+| GD-07 | A SADR or target ADR was created or explicitly skipped using the decision-record criteria. | blocking when applicable |\n \n ## CI \u2014 Context & Impact Checklist\n \n | ID | Item | Blocking |\n | --- | --- | --- |\n-| CI-01 | Relevant docs and ADRs identified. | blocking |\n+| CI-01 | Relevant docs, SADRs, and target ADRs identified. | blocking |\n | CI-02 | Relevant modules, interfaces, seams, callers, and tests identified. | blocking |\n | CI-03 | Dependencies and blocked-by relationships identified. | blocking |\n | CI-04 | Candidate locks identified. | blocking |\n@@ -62,7 +62,7 @@\n | PRD-04 | Implementation decisions are recorded without volatile code detail. | blocking |\n | PRD-05 | Testing decisions are recorded. | blocking |\n | PRD-06 | Out-of-scope items are explicit. | blocking |\n-| PRD-07 | Relevant glossary/ADR links are present when applicable. | blocking |\n+| PRD-07 | Relevant glossary/SADR/target-ADR links are present when applicable. | blocking |\n \n ## SF \u2014 Spec Freeze Checklist\n \n@@ -98,7 +98,7 @@\n | H-02 | Branch/worktree target is present. | blocking |\n | H-03 | Scope and non-goals are present. | blocking |\n | H-04 | Dependencies and locks are present. | blocking |\n-| H-05 | Relevant docs, ADRs, modules, and tests are referenced. | blocking |\n+| H-05 | Relevant docs, SADRs, target ADRs, modules, and tests are referenced. | blocking |\n | H-06 | Required skills are stated. | blocking |\n | H-07 | TDD expectations are stated. | blocking when behavior work |\n | H-08 | Acceptance criteria are included. | blocking |\ndiff --git a/docs/agents/codex-handoff.md b/docs/agents/codex-handoff.md\nindex ede468f47cb277f69a857e8c45df703e4ed0151d..6f97bdffb67e44d7edea2c71e01ea28933c4eb6c\n--- a/docs/agents/codex-handoff.md\n+++ b/docs/agents/codex-handoff.md\n@@ -46,7 +46,7 @@\n \n ## Context\n - Domain terms:\n-- Relevant ADRs:\n+- Relevant SADRs and target ADRs:\n - Relevant docs:\n - Relevant modules/interfaces/seams:\n - Similar tests/prior art:\ndiff --git a/docs/agents/completion-check-agent.md b/docs/agents/completion-check-agent.md\nindex 13e4c035db35025f4e3cdcc5adfb24314ac4d521..61bfa1218cf36ca605250c91919e620f33238aa0\n--- a/docs/agents/completion-check-agent.md\n+++ b/docs/agents/completion-check-agent.md\n@@ -32,7 +32,7 @@\n - `AGENTS.md`\n - `CLAUDE.md`\n - `CONTEXT.md`\n-- relevant ADRs\n+- relevant SADRs and target ADRs\n - parent Goal issue\n - PRD issue or document\n - task issue\ndiff --git a/docs/agents/decision-control.md b/docs/agents/decision-control.md\nindex 7d8ee5ab03a5ccbcbd04056ae61de03785a2a9c9..8c94ecda00fffaa0e92c325ab83aa9776bd1dac1\n--- a/docs/agents/decision-control.md\n+++ b/docs/agents/decision-control.md\n@@ -11,7 +11,7 @@\n | Decision | Owner | When | Required inputs | Output | Enforcement surface |\n | --- | --- | --- | --- | --- | --- |\n | Is the Goal clear enough to plan? | Planner + Guardian when needed | Goal intake | User request, existing docs, risk signals | Goal readiness verdict | Goal checklist, issue template, ledger |\n-| Must `grill-with-docs` run? | Planner / Skill Gate | Before PRD or issue split | Ambiguity, domain terms, ADR conflicts, unclear boundaries | Skill invocation or explicit skip rationale | Skill Gate checklist |\n+| Must `grill-with-docs` run? | Planner / Skill Gate | Before PRD or issue split | Ambiguity, domain terms, SADR/target-ADR conflicts, unclear boundaries | Skill invocation or explicit skip rationale | Skill Gate checklist |\n | Is a PRD required? | Planner | After Goal context stabilizes | Goal, `grill-with-docs` output, Context & Impact output | PRD required / not required | PRD checklist, CCA evidence check |\n | Can the spec be frozen? | Operator only | End of Requirements Definition, before issue split | PRD/requirements summary, scope inventory (SF-02), open-question list | `spec_freeze` block in the plan + ledger evidence | `require_grilled_plan` (plan ingest/run), SF checklist |\n | Can a frozen spec change? | Operator only (Spec Amendment) | When implementation contradicts the frozen spec | Contested decisions, scoped re-grill result | recorded amendment + re-stamped freeze | SF checklist, ledger evidence |\ndiff --git a/docs/agents/domain.md b/docs/agents/domain.md\nindex 258da8ea81e87ac0a6b0b5b5e5c2fde980eaabb7..d57b0cf17ff3e1fa9e6e5885372bf083d8295421\n--- a/docs/agents/domain.md\n+++ b/docs/agents/domain.md\n@@ -5,7 +5,9 @@\n ## Layout\n \n - `CONTEXT.md` defines Shiki language. Keep it implementation-free.\n-- `docs/adr/` records hard-to-reverse decisions.\n+- `docs/adr/SADR-NNNN-*.md` records Shiki platform decisions.\n+- `docs/adr/NNNN-*.md` records target product decisions without sharing the\n+  SADR namespace.\n - `AGENTS.md` is the runtime-neutral agent constitution.\n - `CLAUDE.md` is the Claude Code wrapper around `AGENTS.md`.\n - `.shiki/` is the Target Repository mirror for Goals, PRDs, plans, Task DAGs, contracts, locks, worktree records, ledger entries, CCA verdicts, repair packets, reports, and handoffs.\n@@ -15,6 +17,6 @@\n \n - Use Shiki terms exactly when they exist.\n - Add glossary terms to `CONTEXT.md` only when the term is part of the domain, not a generic programming concept.\n-- Create ADRs only for hard-to-reverse, surprising, tradeoff-based decisions.\n+- Create SADRs or target ADRs only for hard-to-reverse, surprising, tradeoff-based decisions.\n - Keep product-specific language in the Target Repository, not in Shiki Template docs.\n-- If an ADR conflicts with a proposed implementation, surface the conflict before editing.\n+- If a SADR or target ADR conflicts with a proposed implementation, surface the conflict before editing.\ndiff --git a/docs/agents/execution-control.md b/docs/agents/execution-control.md\nindex ad8ee3cdb36be56fedc1f46408ca92b387d74047..bc63c3a73661d12b567016ba299f1a6b66bf9088\n--- a/docs/agents/execution-control.md\n+++ b/docs/agents/execution-control.md\n@@ -63,7 +63,7 @@\n | Branch name | Orchestrator | Orchestrator | Before implementer dispatch | branch policy | branch ref in task/ledger |\n | Lock reservation | Context & Impact | MergeGate | Before dispatch and before merge | `.shiki/locks/`, labels, PR metadata | lock id, owner, scope |\n | CI profile | Planner | MergeGate | During issue triage, rechecked at PR | labels, `.shiki/policy.yaml`, workflow matrix | required check list |\n-| Required status checks | Repo maintainer / Guardian | GitHub branch protection / rulesets | Repository setup, updated via ADR | GitHub settings | branch/ruleset config, ADR |\n+| Required status checks | Repo maintainer / Guardian | GitHub branch protection / rulesets | Repository setup, updated via target ADR | GitHub settings | branch/ruleset config, target ADR |\n | CCA judgment | CCA | GitHub required check / MergeGate | Every non-draft PR update | GitHub Action check | structured CCA verdict |\n | Repair assignment | CCA | MergeGate | After failed CCA/CI/review | repair packet, issue/PR comment | repair id and attempt count |\n | Merge readiness | MergeGate | GitHub branch protection / Guardian if needed | After CCA complete | required checks, review rules, rulesets | mergegate check output |\ndiff --git a/docs/agents/guardian-policy.md b/docs/agents/guardian-policy.md\nindex 4147c7adf05a47adb4fd24ada1a1c2e1f2ba6d2d..a20d9cc6c00a569bb057b9ca8d6b51c7fc4a9847\n--- a/docs/agents/guardian-policy.md\n+++ b/docs/agents/guardian-policy.md\n@@ -98,8 +98,8 @@\n `origin/<default_branch>` and reuses each job's existing base fetch; a shallow\n (`--depth`) or second base fetch would make the proof unresolvable and silently\n disable the carry, so neither is permitted. CCA reads `guardian_comment_carried` as\n-a recorded authority in the deterministic signal and does not re-derive it. See ADR\n-0018 for the full decision, the rejected diff/patch-equality and path-disjointness\n+a recorded authority in the deterministic signal and does not re-derive it. See\n+SADR-0018 for the full decision, the rejected diff/patch-equality and path-disjointness\n alternatives, and the accepted residual.\n \n ## External AI Guardian Review (SADR-0010)\ndiff --git a/docs/agents/implementation-policy.md b/docs/agents/implementation-policy.md\nindex c754144a9fb5bf4a38aa82a60e1670c59e626b44..85c37281bcad196c6b7b02343faf00b03cd9c57a\n--- a/docs/agents/implementation-policy.md\n+++ b/docs/agents/implementation-policy.md\n@@ -31,10 +31,13 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n ## Non-Negotiables\n \n 1. **Planning precedes implementation.** Non-trivial Goals start with `grill-with-docs` and Context & Impact.\n-2. **Domain language is controlled.** Terms must be checked against `CONTEXT.md`; hard-to-reverse tradeoffs must be checked against ADRs.\n+2. **Domain language is controlled.** Terms must be checked against `CONTEXT.md`; hard-to-reverse tradeoffs must be checked against Shiki SADRs and target ADRs.\n 3. **PRD precedes issue decomposition.** `to-prd` is used once the context is settled enough to become durable intent.\n 4. **Issues are vertical slices.** `to-issues` must produce independently grabbable tracer-bullet tasks, not horizontal layer tickets.\n 5. **Triage controls dispatch.** Only `ready-for-agent` / AFK tasks may be assigned to Codex without human input.\n@@ -95,19 +98,19 @@\n Process:\n \n - Challenge terms against `CONTEXT.md`.\n-- Check ADRs before reopening decisions.\n+- Check SADRs and target ADRs before reopening decisions.\n - Inspect code when code can answer a question.\n - Ask one question at a time when the operator is available.\n - Provide a recommended answer with each question.\n - Record resolved terms in `CONTEXT.md`.\n-- Create ADRs only for hard-to-reverse, surprising, tradeoff-based decisions.\n+- Create SADRs or target ADRs only for hard-to-reverse, surprising, tradeoff-based decisions.\n - Convert unresolved load-bearing questions into HITL blockers.\n \n Outputs:\n \n - Resolved design-tree decisions.\n - Updated glossary terms when needed.\n-- ADRs when justified.\n+- SADRs or target ADRs when justified.\n - HITL blockers when decisions remain unresolved.\n \n ## Phase 3 \u2014 Context & Impact\n@@ -117,7 +120,7 @@\n \n Outputs must include:\n \n-- Relevant documents and ADRs.\n+- Relevant documents, SADRs, and target ADRs.\n - Relevant modules, interfaces, seams, callers, and tests.\n - Candidate locks.\n - Dependencies.\n@@ -140,7 +143,7 @@\n - Testing decisions.\n - Out of scope.\n - Further notes.\n-- Links to relevant Goal, domain terms, and ADRs.\n+- Links to relevant Goal, domain terms, SADRs, and target ADRs.\n \n Phase 4 ends with Spec Freeze: the operator explicitly approves the PRD, the\n plan records a `spec_freeze` block (status, approved_by, source), and the SF\n@@ -240,7 +243,7 @@\n - Dependencies and locks.\n - Risk and Guardian requirements.\n - Ledger completeness.\n-- Documentation and ADR updates.\n+- Documentation and SADR/target-ADR updates.\n - Scope drift.\n \n CCA emits one of:\n@@ -301,7 +304,7 @@\n | Evidence failure | Behavior might be done, but proof is missing. | Add checks, PR evidence, ledger entry, or artifacts. |\n | Scope drift | PR changes unrelated behavior. | Revert unrelated changes or split into new Goal. |\n | Dependency block | Upstream task or lock not complete. | Block until dependency resolves. |\n-| Architecture block | Decision needs ADR or deep-module discussion. | `grill-with-docs` or `improve-codebase-architecture`. |\n+| Architecture block | Decision needs a SADR, target ADR, or deep-module discussion. | `grill-with-docs` or `improve-codebase-architecture`. |\n | Risk block | Security/production/policy change needs human approval. | Guardian decision. |\n \n ## Phase 5.5 \u2014 Execution Decision Control\ndiff --git a/docs/agents/runtime-auth-model.md b/docs/agents/runtime-auth-model.md\nindex 6a2e1a47e81ea89b5c68a210ec07772a9374babc..3c54748d24233dbd0ba8473950282547ef4ce935\n--- a/docs/agents/runtime-auth-model.md\n+++ b/docs/agents/runtime-auth-model.md\n@@ -148,7 +148,7 @@\n \n API-key based runners may be added by a target repository as an explicit extension. They are not the default Shiki template path.\n \n-If a repo opts into API-key mode, record the exception in an ADR and update the repo's `AGENTS.md`, `.shiki/config.yaml`, and workflow permissions.\n+If a repo opts into API-key mode, record the target-specific exception in a target ADR and update the repo's `AGENTS.md`, `.shiki/config.yaml`, and workflow permissions.\n \n ## GitHub Actions Runtime Compatibility\n \ndiff --git a/docs/agents/skill-gate.md b/docs/agents/skill-gate.md\nindex c50398b0d7c60a8598f2a5c88a10015730dced13..3acffe6444617bb1d54b77d4b4708158ec735bc6\n--- a/docs/agents/skill-gate.md\n+++ b/docs/agents/skill-gate.md\n@@ -9,7 +9,7 @@\n | Skill | Trigger | Expected output |\n | --- | --- | --- |\n | `setup-matt-pocock-skills` | First configuring a repo for these skills, or when issue tracker, triage labels, or domain docs are missing. | `docs/agents/` configuration and an `Agent skills` block in `AGENTS.md` or `CLAUDE.md`. |\n-| `grill-with-docs` | Ambiguous plan, unclear terminology, boundary decision, tradeoff, or ADR-worthy design choice. | Clarified decision tree, recommended answers, updates to `CONTEXT.md` or ADRs when decisions crystallize. |\n+| `grill-with-docs` | Ambiguous plan, unclear terminology, boundary decision, tradeoff, or decision-record-worthy design choice. | Clarified decision tree, recommended answers, updates to `CONTEXT.md`, SADRs, or target ADRs when decisions crystallize. |\n | `zoom-out` | Agent lacks a higher-level architectural map. | Map of relevant modules, callers, seams, and domain concepts. |\n | `to-prd` | Settled context should become a PRD. | PRD published to the issue tracker with problem, solution, user stories, decisions, testing, and out-of-scope notes. |\n | `to-issues` | Goal, PRD, or plan must become implementation tickets. | Vertical-slice issues with acceptance criteria, dependencies, and AFK/HITL classification. |\n@@ -17,7 +17,7 @@\n | `tdd` | Feature work or bug fix has observable behavior that can be specified with tests. | Red-green-refactor loop through public interfaces, one vertical slice at a time. |\n | `code-review` | The TDD loop is green and the implementer is about to create or update a PR. Default for every implementation task. | Pre-PR self-review of the task diff with fixes applied on the task branch; findings and resolutions recorded as a ledger entry naming the skill and a `## Pre-PR code review` PR body section. |\n | `diagnose` | Hard bug, failing check, regression, flaky behavior, or performance problem. | Reproduction loop, ranked hypotheses, instrumentation, fix, regression test, post-mortem. |\n-| `improve-codebase-architecture` | Structural friction, testability, AI-navigability, deep-module opportunity, or refactor request. | Architecture candidates and deepening recommendations informed by `CONTEXT.md` and ADRs. |\n+| `improve-codebase-architecture` | Structural friction, testability, AI-navigability, deep-module opportunity, or refactor request. | Architecture candidates and deepening recommendations informed by `CONTEXT.md`, SADRs, and target ADRs. |\n | `prototype` | A throwaway logic or UI prototype is needed to answer a design question. | Clearly marked disposable prototype and captured decision. |\n \n ## MergeGate Interaction\ndiff --git a/docs/operations.md b/docs/operations.md\nindex cf1f452d4c589e38ccd58553bda0ad8684b51eb7..ace48a90ffbec9e5c0a7113bef16a4858bc44b3f\n--- a/docs/operations.md\n+++ b/docs/operations.md\n@@ -89,6 +89,16 @@\n      target history, brought forward by `shiki migrate`, not a template.\n    - **Everything else** (CLI scripts, workflows, schemas, docs) is overwritten.\n \n+   A forced upgrade also migrates the former numeric Shiki decision paths to\n+   `SADR-NNNN-*`. Before the first write, the installer inventories the exact\n+   legacy Shiki paths and authorizes each deletion only when the existing\n+   `.shiki/install-stamp.json` names that path and its SHA-256 digest still\n+   matches the file. An absent, unreadable, malformed, incomplete, or\n+   mismatching stamp reports every blocked path and stops with no filesystem\n+   change. Target-owned `NNNN-*` ADRs are not cleanup candidates. A non-force\n+   install deletes nothing and retains any still-matching legacy stamp entries\n+   so a later forced upgrade can prove ownership.\n+\n    If the target has pending migrations, `--force` refuses and writes nothing \u2014\n    apply migrations first (step 3), then re-run. The run ends with a single\n    summary of every `.new` written, so a half-upgrade is visible.\ndiff --git a/docs/releasing.md b/docs/releasing.md\nindex 10d73828e7dbd64e9d9f6b83bc84d010537ce62a..aafeee8c4d441e1e427efee2ce4f81a647f020e1\n--- a/docs/releasing.md\n+++ b/docs/releasing.md\n@@ -67,7 +67,7 @@\n CLI in `scripts/`. There are no third-party Python runtime dependencies. The CLI\n is intentionally **not** exposed as a `[project.scripts]` console entry point\n because `scripts/shiki.py` is a dependency-free shim and `scripts/` is not\n-packaged as an importable distribution (see the ADR for the rationale).\n+packaged as an importable distribution (see SADR-0007 for the rationale).\n \n Supported install paths:\n \n@@ -155,6 +155,15 @@\n    [docs/operations.md](operations.md#the-new-convention). If the target has\n    pending migrations, `--force` refuses and writes nothing; apply migrations\n    (step 2) first, then re-run.\n+\n+   The SADR namespace upgrade has an additional pre-write gate: each former\n+   numeric Shiki decision path is removed only when the existing install stamp\n+   contains that exact path and a digest matching its current bytes. The\n+   installer aggregates absent/unreadable/malformed-stamp, missing-digest, and\n+   digest-mismatch blockers before raising, so refusal leaves the target tree\n+   byte-identical. Numeric target ADRs are preserved. A successful forced\n+   upgrade installs and stamps `SADR-*` paths and drops the removed legacy paths\n+   from the refreshed stamp.\n 2. Apply repository-local `.shiki/` state migrations with the migrate command:\n \n    ```bash\ndiff --git a/docs/usage.md b/docs/usage.md\nindex b83407ee3970dd395dd893010bc5dbe8bad26051..a130ef6e40dfd8169a5bc701339351097df1a09f\n--- a/docs/usage.md\n+++ b/docs/usage.md\n@@ -67,8 +67,10 @@\n shiki install-target /path/to/target-repo --local-only\n ```\n \n-Use `--force` only when you intentionally want to overwrite existing target\n-files.\n+Use `--force` only when you intentionally want to refresh the target template.\n+For the SADR namespace migration, `--force` deletes only exact legacy Shiki\n+decision paths whose existing install-stamp digest still matches; any ownership\n+blocker stops before the first write. Numeric target ADRs are never candidates.\n \n ## `shiki bootstrap-platform` \u2014 publish the Shiki platform repo\n \ndiff --git a/scripts/guardian_approval_signal.py b/scripts/guardian_approval_signal.py\nindex d059f92b2c82aa2b5d6ca421365537a87886c1cb..16924892714f4f5b9303c0ef0e325a7e70075630\n--- a/scripts/guardian_approval_signal.py\n+++ b/scripts/guardian_approval_signal.py\n@@ -199,8 +199,8 @@\n ) -> Any:\n     \"\"\"Evaluate SADR-0015 Contract Approval for the CCA Guardian signal.\n \n-    The SAME OR that MergeGate applies must reach this deterministic signal: ADR\n-    0015 is explicit that without it, a contract-approved PR still returns\n+    The SAME OR that MergeGate applies must reach this deterministic signal:\n+    SADR-0015 is explicit that without it, a contract-approved PR still returns\n     ``needs_guardian``, the CCA job fails, and the MergeGate policy check never\n     runs \u2014 the gate this change moves would never be reached. This resolves the\n     task the SAME way MergeGate does (the first Shiki task id in the PR body),\ndiff --git a/scripts/shiki_contract_approval.py b/scripts/shiki_contract_approval.py\nindex 941bcb96fdd8b9a85bc0c4f0f04d5dad9d04839c..4a4b418f6527413161db82670bec857a043360e2\n--- a/scripts/shiki_contract_approval.py\n+++ b/scripts/shiki_contract_approval.py\n@@ -142,7 +142,7 @@\n     changed_files_status: Any,\n     registration: Any,\n ) -> ContractApprovalResult:\n-    \"\"\"Whether this PR carries an SADR-0015 Contract Approval.\n+    \"\"\"Whether this PR carries a SADR-0015 Contract Approval.\n \n     The four conditions are evaluated IN ORDER, each failure recorded as a\n     distinct reason string:\ndiff --git a/scripts/shiki_guardian.py b/scripts/shiki_guardian.py\nindex a4ecb79bbda55fd5c8b06adfae2eb99760a03c45..4dd99bbe714e26f421fa19abac7a34ae1b195ecc\n--- a/scripts/shiki_guardian.py\n+++ b/scripts/shiki_guardian.py\n@@ -12,7 +12,7 @@\n # The carry re-derives its proof from git in-process on every judging invocation;\n # it is never read from an artifact, so there is nothing to go stale or be forged.\n # shiki_sync_proof is the ONLY guardian-family module that runs git, and every one\n-# of its invocations is read-only and local (ADR: prove-from-git base sync).\n+# of its invocations is read-only and local (SADR: prove-from-git base sync).\n from shiki_sync_proof import verify_pure_base_sync\n \n GUARDIAN_POLICY_PATH = \".shiki/guardian-policy.json\"\ndiff --git a/scripts/shiki_guardian_review.py b/scripts/shiki_guardian_review.py\nindex dd0f104d457899e0ba5ff1e162011c1e90b6f92b..e10ef58c046324e7ea3a3392feb3048ca77414ce\n--- a/scripts/shiki_guardian_review.py\n+++ b/scripts/shiki_guardian_review.py\n@@ -159,7 +159,7 @@\n     docs_only = _is_docs_only(changed_files)\n     if docs_only and matched == 0:\n         focus.append(\n-            \"Docs/ADR-only change: confirm the change is genuinely documentation/decision-only, introduces no behavior or contract drift, and stays consistent with the constitution and existing ADRs.\"\n+            \"Docs/decision-record-only change: confirm the change is genuinely documentation/decision-only, introduces no behavior or contract drift, and stays consistent with the constitution and existing SADRs and target ADRs.\"\n         )\n \n     # Conservative augmentation: unknown (no category and not docs-only), mixed\ndiff --git a/scripts/shiki_installer.py b/scripts/shiki_installer.py\nindex be064ab7a29ff6836f7f7a1d70500a43e0d11d7b..9fd8eefd137ce7003dad5c0dcb2e48e2cd47ab32\n--- a/scripts/shiki_installer.py\n+++ b/scripts/shiki_installer.py\n@@ -118,6 +118,7 @@\n # what lets a target carry it without a governance-schema change.\n INSTALL_STAMP_PATH = \".shiki/install-stamp.json\"\n INSTALL_STAMP_VERSION = 1\n+LEGACY_SHIKI_SADR_MAX = 18\n \n # Under --force the shipped surface splits three ways, so nothing is lost and\n # nothing is silently stale.\n@@ -237,9 +238,98 @@\n         )\n \n \n+def legacy_shiki_adr_paths() -> tuple[str, ...]:\n+    \"\"\"Former numeric paths for Shiki SADR-0001 through SADR-0018.\n+\n+    The authoritative slugs come from the current SADR filenames. SADR-0019 is\n+    the namespace decision itself and never shipped under a numeric legacy path.\n+    \"\"\"\n+    paths: list[str] = []\n+    for sadr in sorted((ROOT / \"docs\" / \"adr\").glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\")):\n+        number = sadr.name.split(\"-\", 2)[1]\n+        if int(number) <= LEGACY_SHIKI_SADR_MAX:\n+            paths.append(f\"docs/adr/{sadr.name.removeprefix('SADR-')}\")\n+    return tuple(paths)\n+\n+\n+def preflight_legacy_sadr_cleanup(target: Path, *, force: bool) -> tuple[str, ...]:\n+    \"\"\"Authorize exact legacy Shiki ADR removals before the first write.\n+\n+    A non-force install never removes existing files. A forced install needs no\n+    stamp when no legacy Shiki path is present. When legacy paths do exist, each\n+    must be named in a readable install stamp and its current bytes must match\n+    the recorded digest.\n+    \"\"\"\n+    if not force:\n+        return ()\n+    present = tuple(\n+        relative\n+        for relative in legacy_shiki_adr_paths()\n+        if (target / relative).exists() or (target / relative).is_symlink()\n+    )\n+    if not present:\n+        return ()\n+\n+    stamp_path = target / INSTALL_STAMP_PATH\n+    try:\n+        raw = stamp_path.read_text(encoding=\"utf-8\")\n+        stamp = json.loads(raw)\n+    except FileNotFoundError:\n+        detail = \"install stamp is absent\"\n+        blockers = [f\"{relative}: {detail}\" for relative in present]\n+    except (OSError, UnicodeDecodeError) as error:\n+        detail = f\"install stamp is unreadable ({error})\"\n+        blockers = [f\"{relative}: {detail}\" for relative in present]\n+    except json.JSONDecodeError as error:\n+        detail = f\"install stamp is malformed ({error})\"\n+        blockers = [f\"{relative}: {detail}\" for relative in present]\n+    else:\n+        if not isinstance(stamp, dict):\n+            blockers = [\n+                f\"{relative}: install stamp is malformed (expected an object)\"\n+                for relative in present\n+            ]\n+        else:\n+            digests = stamp.get(\"digests\")\n+            blockers = []\n+            if not isinstance(digests, dict):\n+                blockers.extend(\n+                    f\"{relative}: install stamp has no digest for this path\"\n+                    for relative in present\n+                )\n+            else:\n+                for relative in present:\n+                    expected = digests.get(relative)\n+                    if not isinstance(expected, str) or not expected:\n+                        blockers.append(\n+                            f\"{relative}: install stamp has no digest for this path\"\n+                        )\n+                        continue\n+                    try:\n+                        actual = _sha256_file(target / relative)\n+                    except OSError as error:\n+                        blockers.append(\n+                            f\"{relative}: legacy path is unreadable ({error})\"\n+                        )\n+                        continue\n+                    if actual != expected:\n+                        blockers.append(\n+                            f\"{relative}: digest mismatch (stamp {expected}, current {actual})\"\n+                        )\n+\n+    if blockers:\n+        joined = \"\\n\".join(f\"- {blocker}\" for blocker in blockers)\n+        raise ShikiError(\n+            \"legacy Shiki ADR cleanup is not authorized:\\n\"\n+            f\"{joined}\\nNo files were written.\"\n+        )\n+    return present\n+\n+\n def install_template(target: Path, *, force: bool, validate: bool) -> list[NewFileNote]:\n     # Preconditions first: never rewrite the tree of a target that cannot be\n     # upgraded cleanly (see refuse_pending_migrations).\n+    legacy_cleanup = preflight_legacy_sadr_cleanup(target, force=force)\n     refuse_pending_migrations(target)\n \n     # Resolve the target's required CODEOWNERS owner once from its\n@@ -250,6 +340,10 @@\n     # verbatim. See ``codeowners_required_owner``.\n     codeowners_owner = codeowners_required_owner(target)\n \n+    for relative in legacy_cleanup:\n+        (target / relative).unlink()\n+        info(f\"removed stamped legacy Shiki ADR: {target / relative}\")\n+\n     notes: list[NewFileNote] = []\n     for relative in TEMPLATE_PATHS:\n         source = ROOT / relative\n@@ -647,6 +741,22 @@\n     digests = target_stamp_digests(target)\n     existing = read_install_stamp(target)\n     if existing is not None:\n+        # A non-force install deliberately leaves legacy Shiki ADRs in place.\n+        # Preserve only still-matching ownership proof so a later forced\n+        # upgrade can authorize cleanup; never carry a missing or stale digest.\n+        existing_digests = existing.get(\"digests\")\n+        if isinstance(existing_digests, dict):\n+            for relative in legacy_shiki_adr_paths():\n+                expected = existing_digests.get(relative)\n+                path = target / relative\n+                if not isinstance(expected, str) or not path.is_file():\n+                    continue\n+                try:\n+                    actual = _sha256_file(path)\n+                except OSError:\n+                    continue\n+                if actual == expected:\n+                    digests[relative] = actual\n         if _incoming_is_older(commit, existing.get(\"platform_commit\")):\n             warn(\n                 \"kept existing install stamp: incoming install \"\ndiff --git a/scripts/shiki_loop.py b/scripts/shiki_loop.py\nindex 3611641da8aeb5038b1da90e3114b09a473ec6d8..84caa491b9cad0edc4176f209c13e9221e975681\n--- a/scripts/shiki_loop.py\n+++ b/scripts/shiki_loop.py\n@@ -424,7 +424,7 @@\n         # unresolvable CCA verdict has already produced its terminal stop and is never\n         # laundered into a head-moving sync (SADR-0010). It also never fires while the\n         # policy (Guardian) gate is red \u2014 for ANY reason, not only when it is the sole\n-        # red check \u2014 so a head-bound Guardian approval, or an SADR-0015\n+        # red check \u2014 so a head-bound Guardian approval, or a SADR-0015\n         # contract-approval carry that base movement defeated (the policy gate compares\n         # base and head task governance directly), is never silently restored by\n         # adopting base wholesale. It is bounded by a durable per-task counter so the\ndiff --git a/scripts/shiki_tasks.py b/scripts/shiki_tasks.py\nindex aec3809f24b4a0c8c96fc50971cff9ae9ad30381..d531052e9d9717058d4631483cefab44c26a65c1\n--- a/scripts/shiki_tasks.py\n+++ b/scripts/shiki_tasks.py\n@@ -821,7 +821,7 @@\n         \"prompt\": prompt,\n         \"entry_skill\": \"grill-with-docs\",\n         \"required_next_steps\": [\n-            \"Run grill-with-docs until terminology, boundaries, risks, and ADR-worthy decisions are settled.\",\n+            \"Run grill-with-docs until terminology, boundaries, risks, and decision-record-worthy choices are settled.\",\n             \"Obtain the operator's explicit approval of the PRD/requirements (Spec Freeze).\",\n             \"Write a machine-readable plan JSON with grill_with_docs.status=complete and spec_freeze.status=frozen.\",\n             \"Run shiki plan ingest --plan-file PLAN.json.\",\ndiff --git a/scripts/test_shiki_goal_loop.sh b/scripts/test_shiki_goal_loop.sh\nindex 753c7b516e10f0be3bc92a4bd0eb1fa699776ae6..8f3820a0a079b3044424cfe1a6c6b7fe68896e67\n--- a/scripts/test_shiki_goal_loop.sh\n+++ b/scripts/test_shiki_goal_loop.sh\n@@ -231,8 +231,8 @@\n \n # High/critical risk merges autonomously when all required checks are green:\n # the \"MergeGate policy check\" required check IS the Guardian gate (it enforces\n-# guardian-policy.json \u2014 human approval OR external AI guardian review, ADR\n-# 0010), so green checks mean a recorded authority approved. The fake gh returns\n+# guardian-policy.json \u2014 human approval OR external AI guardian review,\n+# SADR-0010), so green checks mean a recorded authority approved. The fake gh returns\n # green checks, simulating that the Guardian gate passed.\n python3 \"$ROOT/scripts/shiki.py\" goal create --target \"$TARGET\" --title \"Guardian gate\" --outcome \"High risk merges via policy gate\" >/tmp/shiki-goal-loop-guardian-goal.json\n GGOAL=\"$(json_get /tmp/shiki-goal-loop-guardian-goal.json goal_id)\"\ndiff --git a/tests/test_adr_references.py b/tests/test_adr_references.py\nindex 51706329c797d5dd9b47e082a333fa83b261d482..d005da4f771f5e8085ec22d4b787980e68f5ae8c\n--- a/tests/test_adr_references.py\n+++ b/tests/test_adr_references.py\n@@ -1,8 +1,8 @@\n-\"\"\"Every ``ADR ####`` citation must resolve to exactly one decision record.\n+\"\"\"Every target ADR and Shiki SADR citation resolves in its own namespace.\n \n Background. ``scripts/mergegate_check.py`` justifies ``is_bookkeeping_closeout``\n-with \"ADR 0017\" nine times and ``scripts/guardian_approval_signal.py`` cites\n-0016/0017 seven times, yet for a stretch the numbering in ``docs/adr/`` stopped\n+with \"SADR-0017\" nine times and ``scripts/guardian_approval_signal.py`` cites\n+SADR-0016/SADR-0017 seven times, yet for a stretch the numbering in ``docs/adr/`` stopped\n at 0015: the authoring commits lived on a coordinator branch and no task ever\n declared ``docs/adr/...`` in its locks, so nothing carried the records to the\n default branch. The stated basis of an exemption that merged seven PRs with zero\n@@ -11,11 +11,12 @@\n and the directory drifted with no signal.\n \n This is the same unbound-pair shape behind other silent failures: a reference\n-and its target with nothing asserting they agree. This test binds them. It scans\n-every ``scripts/*.py`` (plus ``docs/**/*.md`` and the root ``*.md`` governance\n-files, which are cheap) for the ``ADR ####`` pattern and asserts each cited\n-number resolves to exactly one ``docs/adr/####-*.md``. The converse is\n-deliberately NOT required: an ADR may exist without being cited.\n+and its target with nothing asserting they agree. This test binds them. Target\n+``ADR NNNN`` citations resolve only to ``docs/adr/NNNN-*.md``; Shiki\n+``SADR-NNNN`` citations resolve only to ``docs/adr/SADR-NNNN-*.md``. The scan\n+covers current governance docs, scripts, workflows, prompts, packaging metadata,\n+and repo-local Codex guidance while deliberately excluding historical\n+``.shiki`` evidence. The converse is not required: a record may be uncited.\n \n The suite is dependency free: standard-library ``unittest`` only.\n \"\"\"\n@@ -31,17 +32,18 @@\n \n REPO_ROOT = Path(__file__).resolve().parents[1]\n \n-# \"ADR 0016\" -- a four-digit citation. The trailing \\b keeps a five-digit token\n-# from being read as a truncated four-digit number.\n-_CITATION = re.compile(r\"\\bADR (\\d{4})\\b\")\n-# \"0016-locks-bound-product-paths.md\" -- the leading four digits name the record.\n-_ADR_FILENAME = re.compile(r\"^(\\d{4})-.*\\.md$\")\n+# ``ADR 0016`` / ``SADR-0016`` -- a namespace plus four-digit citation. The\n+# trailing ``\\b`` keeps a five-digit token from being read as a truncated one.\n+_CITATION = re.compile(r\"\\b(S?ADR)[ -](\\d{4})\\b\")\n+# Target ADRs retain the numeric filename; Shiki platform decisions use SADR.\n+_DECISION_FILENAME = re.compile(r\"^(?:(SADR)-)?(\\d{4})-.*\\.md$\")\n \n \n class Violation(NamedTuple):\n     \"\"\"A citation that does not resolve to exactly one decision record.\"\"\"\n \n     citing_file: str  # repo-relative path of the file that cites the ADR\n+    namespace: str  # \"ADR\" for target decisions; \"SADR\" for Shiki decisions\n     number: str  # the cited four-digit number, e.g. \"0016\"\n     line: int  # 1-based line of the citation\n     reason: str  # \"missing\" (zero matches) or \"ambiguous\" (two or more)\n@@ -49,44 +51,59 @@\n \n     @property\n     def expected_path(self) -> str:\n-        return f\"docs/adr/{self.number}-*.md\"\n+        prefix = \"SADR-\" if self.namespace == \"SADR\" else \"\"\n+        return f\"docs/adr/{prefix}{self.number}-*.md\"\n \n     def describe(self) -> str:\n         \"\"\"Name the citing file, the cited number, and the missing path.\"\"\"\n         if self.reason == \"missing\":\n             return (\n-                f\"{self.citing_file}:{self.line} cites ADR {self.number} \"\n+                f\"{self.citing_file}:{self.line} cites {self.namespace} {self.number} \"\n                 f\"but no document matches {self.expected_path}\"\n             )\n         joined = \", \".join(self.matches)\n         return (\n-            f\"{self.citing_file}:{self.line} cites ADR {self.number} which \"\n+            f\"{self.citing_file}:{self.line} cites {self.namespace} {self.number} which \"\n             f\"resolves to {len(self.matches)} documents (expected exactly one \"\n             f\"at {self.expected_path}): {joined}\"\n         )\n \n \n-def _adr_index(root: Path) -> dict[str, list[Path]]:\n-    \"\"\"Map every four-digit ADR number to the record files that claim it.\"\"\"\n-    index: dict[str, list[Path]] = {}\n+def _adr_index(root: Path) -> dict[tuple[str, str], list[Path]]:\n+    \"\"\"Map each decision namespace and number to the files that claim it.\"\"\"\n+    index: dict[tuple[str, str], list[Path]] = {}\n     adr_dir = root / \"docs\" / \"adr\"\n     if adr_dir.is_dir():\n         for path in sorted(adr_dir.glob(\"*.md\")):\n-            match = _ADR_FILENAME.match(path.name)\n+            match = _DECISION_FILENAME.match(path.name)\n             if match:\n-                index.setdefault(match.group(1), []).append(path)\n+                namespace = \"SADR\" if match.group(1) else \"ADR\"\n+                index.setdefault((namespace, match.group(2)), []).append(path)\n     return index\n \n \n def _citing_files(root: Path, include_docs: bool = True) -> list[Path]:\n-    \"\"\"Files scanned for citations: scripts/*.py, and cheaply the markdown.\"\"\"\n+    \"\"\"Current, shipped text surfaces scanned for decision citations.\"\"\"\n     files = sorted((root / \"scripts\").glob(\"*.py\"))\n+    files += sorted((root / \"scripts\").glob(\"*.sh\"))\n     if include_docs:\n         docs = root / \"docs\"\n         if docs.is_dir():\n             files += sorted(docs.rglob(\"*.md\"))\n         files += sorted(root.glob(\"*.md\"))\n-    return files\n+        pyproject = root / \"pyproject.toml\"\n+        if pyproject.is_file():\n+            files.append(pyproject)\n+        for relative in (\".github\", \".codex\"):\n+            surface = root / relative\n+            if surface.is_dir():\n+                files += sorted(\n+                    path\n+                    for path in surface.rglob(\"*\")\n+                    if path.is_file()\n+                    and path.suffix in {\".md\", \".py\", \".sh\", \".toml\", \".yaml\", \".yml\"}\n+                )\n+    return sorted(set(files))\n \n \n def find_adr_reference_violations(\n@@ -94,10 +111,9 @@\n ) -> list[Violation]:\n     \"\"\"Return every ``ADR ####`` citation under ``root`` that fails to resolve.\n \n-    A citation resolves when exactly one ``docs/adr/####-*.md`` matches its\n-    number. Zero matches is a \"missing\" violation; two or more is \"ambiguous\".\n-    Records that are never cited produce nothing -- the converse is not\n-    required.\n+    A citation resolves when exactly one record in the same namespace matches\n+    its number. Zero matches is a \"missing\" violation; two or more is\n+    \"ambiguous\". Records that are never cited produce nothing.\n     \"\"\"\n     root = Path(root)\n     index = _adr_index(root)\n@@ -110,13 +126,15 @@\n         rel = path.relative_to(root).as_posix()\n         for lineno, line in enumerate(text.splitlines(), start=1):\n             for match in _CITATION.finditer(line):\n-                number = match.group(1)\n-                found = index.get(number, [])\n+                namespace, number = match.groups()\n+                found = index.get((namespace, number), [])\n                 if len(found) == 1:\n                     continue\n                 reason = \"missing\" if not found else \"ambiguous\"\n                 rels = [p.relative_to(root).as_posix() for p in found]\n-                violations.append(Violation(rel, number, lineno, reason, rels))\n+                violations.append(\n+                    Violation(rel, namespace, number, lineno, reason, rels)\n+                )\n     return violations\n \n \n@@ -129,8 +147,8 @@\n         self.assertEqual(\n             violations,\n             [],\n-            \"Every 'ADR ####' citation must resolve to exactly one \"\n-            f\"docs/adr/####-*.md:\\n{detail}\",\n+            \"Every ADR/SADR citation must resolve exactly once in its own \"\n+            f\"docs/adr namespace:\\n{detail}\",\n         )\n \n     def test_scan_actually_reads_scripts(self) -> None:\n@@ -142,12 +160,38 @@\n             \"the citation scan must cover scripts/*.py\",\n         )\n \n+    def test_platform_citations_do_not_use_unresolved_numeric_shorthand(self) -> None:\n+        shorthand = re.compile(r\"SADR-\\d{4}/\\d{4}\")\n+        offenders = []\n+        for path in _citing_files(REPO_ROOT):\n+            text = path.read_text(encoding=\"utf-8\")\n+            if shorthand.search(text):\n+                offenders.append(path.relative_to(REPO_ROOT).as_posix())\n+        self.assertEqual(\n+            offenders,\n+            [],\n+            \"repeat the SADR prefix so every cited number is independently resolvable\",\n+        )\n+\n     def test_carried_records_are_indexed(self) -> None:\n         # The two records this Goal carried onto the default branch, and which\n         # the scripts cite most, must be present.\n         index = _adr_index(REPO_ROOT)\n-        self.assertIn(\"0016\", index)\n-        self.assertIn(\"0017\", index)\n+        self.assertIn((\"SADR\", \"0016\"), index)\n+        self.assertIn((\"SADR\", \"0017\"), index)\n+\n+    def test_platform_tree_has_only_sadr_0001_through_0019(self) -> None:\n+        adr_dir = REPO_ROOT / \"docs\" / \"adr\"\n+        sadrs = sorted(adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\"))\n+        self.assertEqual(\n+            {path.name.split(\"-\", 2)[1] for path in sadrs},\n+            {f\"{number:04d}\" for number in range(1, 20)},\n+        )\n+        self.assertEqual(\n+            list(adr_dir.glob(\"[0-9][0-9][0-9][0-9]-*.md\")),\n+            [],\n+            \"the Shiki platform tree must not retain legacy numeric SADR paths\",\n+        )\n \n \n class FixtureAdrReferences(unittest.TestCase):\n@@ -180,6 +224,26 @@\n         self.assertIn(\"9991\", message)  # cited number\n         self.assertIn(\"docs/adr/9991-*.md\", message)  # missing path\n \n+    def test_target_adr_does_not_satisfy_missing_platform_sadr(self) -> None:\n+        root = self._make_repo(\n+            adr_names=[\"0042-target-decision.md\"],\n+            script_body='\"\"\"Target ADR 0042 exists, but platform SADR-0042 does not.\"\"\"\\n',\n+        )\n+        violations = find_adr_reference_violations(root, include_docs=False)\n+        self.assertEqual(len(violations), 1)\n+        self.assertEqual(violations[0].namespace, \"SADR\")\n+        self.assertEqual(violations[0].number, \"0042\")\n+        self.assertEqual(violations[0].expected_path, \"docs/adr/SADR-0042-*.md\")\n+\n+    def test_target_adr_and_platform_sadr_may_reuse_a_number(self) -> None:\n+        root = self._make_repo(\n+            adr_names=[\"0042-target-decision.md\", \"SADR-0042-platform-decision.md\"],\n+            script_body='\"\"\"Target ADR 0042 and platform SADR-0042 are distinct.\"\"\"\\n',\n+        )\n+        self.assertEqual(\n+            find_adr_reference_violations(root, include_docs=False), []\n+        )\n+\n     def test_uncited_adr_is_allowed(self) -> None:\n         # An ADR exists but nothing cites it -> not a violation. The converse\n         # (every ADR must be cited) is explicitly NOT required.\ndiff --git a/tests/test_guardian_review.py b/tests/test_guardian_review.py\nindex 88829e7810212635e2fc98aa5918d6f26294677d..00a85a237acd5bf2d6cb5b294128198c422402ca\n--- a/tests/test_guardian_review.py\n+++ b/tests/test_guardian_review.py\n@@ -162,7 +162,7 @@\n \n     def test_docs_only_is_light_and_not_conservative(self):\n         focus = gr.classify_review_focus([\"docs/adr/0099-x.md\", \"README.md\"], \"\", \"low\")\n-        self.assertTrue(any(\"Docs/ADR-only\" in f for f in focus))\n+        self.assertTrue(any(\"Docs/decision-record-only\" in f for f in focus))\n         self.assertFalse(any(\"conservative default\" in f.lower() for f in focus))\n \n     def test_unknown_category_is_conservative(self):\ndiff --git a/tests/test_policy_job_merged_prs.py b/tests/test_policy_job_merged_prs.py\nindex d45512e74681e5e649279236c7d4e79d5c3da471..b97989f8bd74d091de1a6b5275a20d8dc68c4512\n--- a/tests/test_policy_job_merged_prs.py\n+++ b/tests/test_policy_job_merged_prs.py\n@@ -3,8 +3,8 @@\n \n ``.github/workflows/shiki-cca-completion.yml`` has two consumers of the merged-PR\n proof: the CCA *signal* (``guardian_approval_signal.py`` in \"Prepare CCA context\")\n-and the authoritative gate (``mergegate_check.py`` in \"Enforce MergeGate\"). ADR\n-0017's bookkeeping-closeout exemption (condition 6) needs the merged implementation\n+and the authoritative gate (``mergegate_check.py`` in \"Enforce MergeGate\").\n+SADR-0017's bookkeeping-closeout exemption (condition 6) needs the merged implementation\n PR proven, and a closeout carries NO ``post_merge_reconcile`` marker. The signal\n already resolves ``MERGED_PRS`` unconditionally, but \"Enforce MergeGate\" used to\n resolve it only inside ``grep -q '<!-- shiki:post_merge_reconcile -->'``. So a\ndiff --git a/tests/test_sadr_install_migration.py b/tests/test_sadr_install_migration.py\nnew file mode 100644\nindex 0000000000000000000000000000000000000000..a6f4c57e4dcaef73ef600b0d4804744138ab9071\n--- /dev/null\n+++ b/tests/test_sadr_install_migration.py\n@@ -0,0 +1,229 @@\n+\"\"\"SADR installation and collision-safe legacy upgrade behavior.\"\"\"\n+\n+from __future__ import annotations\n+\n+import contextlib\n+import hashlib\n+import io\n+import json\n+import shutil\n+import tempfile\n+import unittest\n+from pathlib import Path\n+\n+import shiki_test_support  # noqa: F401  (path bootstrap)\n+\n+import shiki_installer\n+from tests.test_adr_references import find_adr_reference_violations\n+\n+\n+PRODUCT_NUMBERS = tuple(range(13, 19))\n+\n+\n+def _quiet_install(target: Path, *, force: bool, validate: bool) -> None:\n+    with contextlib.redirect_stdout(io.StringIO()):\n+        shiki_installer.install_template(target, force=force, validate=validate)\n+\n+\n+def _sha256(path: Path) -> str:\n+    return hashlib.sha256(path.read_bytes()).hexdigest()\n+\n+\n+def _legacy_sadr_paths() -> tuple[str, ...]:\n+    return tuple(\n+        f\"docs/adr/{path.name.removeprefix('SADR-')}\"\n+        for path in sorted((shiki_installer.ROOT / \"docs\" / \"adr\").glob(\"SADR-*.md\"))\n+        if path.name != \"SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\"\n+    )\n+\n+\n+def _create_legacy_upgrade_fixture(\n+    target: Path,\n+) -> tuple[dict[str, bytes], tuple[str, ...]]:\n+    adr_dir = target / \"docs\" / \"adr\"\n+    adr_dir.mkdir(parents=True)\n+    product_bytes = {\n+        f\"{number:04d}-cubeshare-product-decision.md\": (\n+            f\"# CubeShare ADR {number:04d}\\n\\nDo not rewrite {number}.\\n\"\n+        ).encode()\n+        for number in PRODUCT_NUMBERS\n+    }\n+    for name, content in product_bytes.items():\n+        (adr_dir / name).write_bytes(content)\n+\n+    legacy_paths = _legacy_sadr_paths()\n+    for relative in legacy_paths:\n+        (target / relative).write_bytes(f\"legacy Shiki bytes for {relative}\\n\".encode())\n+\n+    stamp_path = target / shiki_installer.INSTALL_STAMP_PATH\n+    stamp_path.parent.mkdir(parents=True, exist_ok=True)\n+    stamp = {\n+        \"version\": shiki_installer.INSTALL_STAMP_VERSION,\n+        \"platform_commit\": None,\n+        \"installed_at\": \"2000-01-01T00:00:00+00:00\",\n+        \"digests\": {\n+            relative: _sha256(target / relative) for relative in legacy_paths\n+        },\n+    }\n+    stamp_path.write_text(\n+        json.dumps(stamp, indent=2, sort_keys=True) + \"\\n\", encoding=\"utf-8\"\n+    )\n+    return product_bytes, legacy_paths\n+\n+\n+def _tree_snapshot(root: Path) -> dict[str, tuple[str, bytes | str]]:\n+    \"\"\"Recursive byte/shape snapshot used to prove preflight is write-free.\"\"\"\n+    snapshot: dict[str, tuple[str, bytes | str]] = {}\n+    for path in sorted(root.rglob(\"*\")):\n+        relative = path.relative_to(root).as_posix()\n+        if path.is_symlink():\n+            snapshot[relative] = (\"symlink\", path.readlink().as_posix())\n+        elif path.is_dir():\n+            snapshot[relative] = (\"directory\", \"\")\n+        else:\n+            snapshot[relative] = (\"file\", path.read_bytes())\n+    return snapshot\n+\n+\n+class FreshSadrInstallTests(unittest.TestCase):\n+    def setUp(self) -> None:\n+        self.tmp = Path(tempfile.mkdtemp(prefix=\"shiki-sadr-fresh-\"))\n+        self.addCleanup(lambda: shutil.rmtree(self.tmp, ignore_errors=True))\n+        self.target = self.tmp / \"target\"\n+        self.adr_dir = self.target / \"docs\" / \"adr\"\n+        self.adr_dir.mkdir(parents=True)\n+        self.product_bytes = {\n+            f\"{number:04d}-product-decision.md\": (\n+                f\"# Product ADR {number:04d}\\n\\nTarget-owned bytes {number}.\\n\"\n+            ).encode()\n+            for number in PRODUCT_NUMBERS\n+        }\n+        for name, content in self.product_bytes.items():\n+            (self.adr_dir / name).write_bytes(content)\n+\n+    def test_fresh_install_preserves_product_adrs_and_installs_all_sadrs(self) -> None:\n+        _quiet_install(self.target, force=False, validate=True)\n+\n+        for name, expected in self.product_bytes.items():\n+            self.assertEqual((self.adr_dir / name).read_bytes(), expected)\n+\n+        sadr_numbers = {\n+            path.name.split(\"-\", 2)[1]\n+            for path in self.adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\")\n+        }\n+        self.assertEqual(sadr_numbers, {f\"{number:04d}\" for number in range(1, 20)})\n+\n+        numeric_records = {\n+            path.name\n+            for path in self.adr_dir.glob(\"[0-9][0-9][0-9][0-9]-*.md\")\n+        }\n+        self.assertEqual(numeric_records, set(self.product_bytes))\n+        self.assertEqual(find_adr_reference_violations(self.target), [])\n+\n+\n+class LegacySadrUpgradeTests(unittest.TestCase):\n+    def setUp(self) -> None:\n+        self.tmp = Path(tempfile.mkdtemp(prefix=\"shiki-sadr-upgrade-\"))\n+        self.addCleanup(lambda: shutil.rmtree(self.tmp, ignore_errors=True))\n+        self.target = self.tmp / \"target\"\n+        self.adr_dir = self.target / \"docs\" / \"adr\"\n+        self.product_bytes, self.legacy_paths = _create_legacy_upgrade_fixture(\n+            self.target\n+        )\n+\n+    def test_force_upgrade_deletes_only_stamped_legacy_sadrs(self) -> None:\n+        _quiet_install(self.target, force=True, validate=True)\n+\n+        for relative in self.legacy_paths:\n+            self.assertFalse((self.target / relative).exists(), relative)\n+        for name, expected in self.product_bytes.items():\n+            self.assertEqual((self.adr_dir / name).read_bytes(), expected)\n+\n+        sadrs = list(self.adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\"))\n+        self.assertEqual(len(sadrs), 19)\n+        refreshed = json.loads(\n+            (self.target / shiki_installer.INSTALL_STAMP_PATH).read_text(encoding=\"utf-8\")\n+        )\n+        for relative in self.legacy_paths:\n+            self.assertNotIn(relative, refreshed[\"digests\"])\n+        for path in sadrs:\n+            relative = path.relative_to(self.target).as_posix()\n+            self.assertEqual(refreshed[\"digests\"][relative], _sha256(path))\n+\n+    def test_non_force_install_never_deletes_legacy_files(self) -> None:\n+        before = {\n+            relative: (self.target / relative).read_bytes()\n+            for relative in self.legacy_paths\n+        }\n+        _quiet_install(self.target, force=False, validate=False)\n+        for relative, expected in before.items():\n+            self.assertEqual((self.target / relative).read_bytes(), expected)\n+        refreshed = json.loads(\n+            (self.target / shiki_installer.INSTALL_STAMP_PATH).read_text(encoding=\"utf-8\")\n+        )\n+        for relative in self.legacy_paths:\n+            self.assertEqual(refreshed[\"digests\"][relative], _sha256(self.target / relative))\n+\n+\n+class LegacyCleanupPreflightTests(unittest.TestCase):\n+    def setUp(self) -> None:\n+        self.tmp = Path(tempfile.mkdtemp(prefix=\"shiki-sadr-preflight-\"))\n+        self.addCleanup(lambda: shutil.rmtree(self.tmp, ignore_errors=True))\n+\n+    def test_all_ownership_blockers_abort_before_any_write(self) -> None:\n+        cases = (\n+            (\"absent\", \"install stamp is absent\"),\n+            (\"unreadable\", \"install stamp is unreadable\"),\n+            (\"malformed\", \"install stamp is malformed\"),\n+            (\"missing-digest\", \"install stamp has no digest\"),\n+            (\"digest-mismatch\", \"digest mismatch\"),\n+        )\n+        for name, expected_reason in cases:\n+            with self.subTest(name=name):\n+                target = self.tmp / name\n+                _, legacy_paths = _create_legacy_upgrade_fixture(target)\n+                stamp_path = target / shiki_installer.INSTALL_STAMP_PATH\n+\n+                if name == \"absent\":\n+                    stamp_path.unlink()\n+                elif name == \"unreadable\":\n+                    stamp_path.unlink()\n+                    stamp_path.mkdir()\n+                elif name == \"malformed\":\n+                    stamp_path.write_text(\"{not-json\\n\", encoding=\"utf-8\")\n+                else:\n+                    stamp = json.loads(stamp_path.read_text(encoding=\"utf-8\"))\n+                    if name == \"missing-digest\":\n+                        stamp[\"digests\"] = {}\n+                    else:\n+                        stamp[\"digests\"] = {\n+                            relative: \"deadbeef\" for relative in legacy_paths\n+                        }\n+                    stamp_path.write_text(\n+                        json.dumps(stamp, indent=2, sort_keys=True) + \"\\n\",\n+                        encoding=\"utf-8\",\n+                    )\n+\n+                before = _tree_snapshot(target)\n+                with self.assertRaises(shiki_installer.ShikiError) as raised:\n+                    _quiet_install(target, force=True, validate=False)\n+                after = _tree_snapshot(target)\n+\n+                self.assertEqual(before, after, f\"{name} preflight changed the tree\")\n+                message = str(raised.exception)\n+                self.assertIn(expected_reason, message)\n+                self.assertIn(\"No files were written\", message)\n+                for relative in legacy_paths:\n+                    self.assertIn(relative, message)\n+\n+    def test_absent_stamp_is_allowed_when_no_legacy_path_exists(self) -> None:\n+        target = self.tmp / \"no-legacy\"\n+        target.mkdir()\n+        _quiet_install(target, force=True, validate=False)\n+        self.assertTrue(\n+            (target / \"docs\" / \"adr\" / \"SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\").is_file()\n+        )\n+\n+\n+if __name__ == \"__main__\":\n+    unittest.main()\n\n2026-08-07T08:22:55.884740Z ERROR codex_models_manager::manager: failed to renew cache TTL: missing field `base_instructions` at line 94 column 5\ncodex\nThe code-review gate found one blocking evidence gap: the fresh-install test proved SADRs were copied but did not directly prove the refreshed install stamp contained their path digests. I\u2019m adding that assertion, then I\u2019ll re-review the installer boundary and rerun the affected test before the full suite.\napply patch\npatch: completed\n/private/tmp/shiki-adr-collision.pa0KwY/.worktrees/shiki-t-20260807t073307221254z-2d22d654-introduc/tests/test_sadr_install_migration.py\ndiff --git a/.codex/skills/shiki/SKILL.md b/.codex/skills/shiki/SKILL.md\nindex c8e2e85d9d75d49cc4f2d6286ee22659c4e53e75..93cded623b2c15d9f65c76491a1c2d813ac9e6c0\n--- a/.codex/skills/shiki/SKILL.md\n+++ b/.codex/skills/shiki/SKILL.md\n@@ -18,6 +18,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n Codex CLI does not register this skill as a custom `/shiki` slash command. When\n the user types `/shiki` in Codex CLI and gets \"Unrecognized command\", explain\n that this is expected: invoke Shiki in Codex with natural language such as\n@@ -69,7 +72,7 @@\n \n ## Responsibilities\n \n-- Claude Code implements and repairs by default (`shiki runner claude`, ADR 0008); Codex implements and repairs tasks explicitly assigned to `codex`.\n+- Claude Code implements and repairs by default (`shiki runner claude`, SADR-0008); Codex implements and repairs tasks explicitly assigned to `codex`.\n - Claude Code Action can act as GitHub-side CCA or reviewer.\n - CCA judges completion.\n - MergeGate authorizes state transitions and merge readiness.\ndiff --git a/.github/prompts/cca-completion-check.md b/.github/prompts/cca-completion-check.md\nindex ed203ad5f302ecc81372d8ab27d2b1232ca97497..fd89950528333ef6242e6d5443904f5392ab7e56\n--- a/.github/prompts/cca-completion-check.md\n+++ b/.github/prompts/cca-completion-check.md\n@@ -19,6 +19,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n ## Required Reading\n \n Read only the files needed to judge the PR contract. Prefer this order:\ndiff --git a/.github/prompts/codex-task-handoff.md b/.github/prompts/codex-task-handoff.md\nindex 695e211d76dea1c60e8d5bb360e78487808286a4..59441d576b01a39569722f226c854e5a740e9eb5\n--- a/.github/prompts/codex-task-handoff.md\n+++ b/.github/prompts/codex-task-handoff.md\n@@ -2,7 +2,7 @@\n \n You are Codex Front acting as the Implementer for a Shiki task.\n \n-Follow `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant ADRs, and `docs/agents/codex-handoff.md`.\n+Follow `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant Shiki SADRs and target ADRs, and `docs/agents/codex-handoff.md`.\n \n Rules:\n \ndiff --git a/AGENTS.md b/AGENTS.md\nindex 7f041100d49c2a7525f51cb0f5d696a4a6d1c78c..a06bfbabc6fa48a007389e25e12d7ed523936bed\n--- a/AGENTS.md\n+++ b/AGENTS.md\n@@ -22,7 +22,7 @@\n - **Codex Front** is an optional implementer through Codex App, Codex CLI, Codex IDE extension, or Codex Web signed in with ChatGPT OAuth/subscription auth, dispatched with `shiki runner codex` when a task is explicitly assigned to `codex`.\n - **GitHub CCA** is implemented by Claude Code Action by default, using `CLAUDE_CODE_OAUTH_TOKEN`.\n \n-Do not assume `openai/codex-action`, `OPENAI_API_KEY`, or API-key based Codex automation in the default Shiki path. API-key runners are explicit target-repo extensions and require their own ADR.\n+Do not assume `openai/codex-action`, `OPENAI_API_KEY`, or API-key based Codex automation in the default Shiki path. API-key runners are explicit target-repo extensions and require their own target ADR.\n \n Shiki is not:\n \n@@ -44,10 +44,14 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use\n+`SADR-NNNN-*.md`; target product decisions retain `NNNN-*.md` and the `ADR`\n+namespace.\n+\n Use the canonical source-of-truth order above. Runtime-specific reading orders\n are operational guidance only and must not override or reorder it.\n \n-Conversation state is not durable truth. If a decision matters, put it in GitHub, `.shiki/`, `CONTEXT.md`, or an ADR.\n+Conversation state is not durable truth. If a decision matters, put it in GitHub, `.shiki/`, `CONTEXT.md`, a Shiki SADR, or a target ADR.\n \n If GitHub and `.shiki/` disagree, surface the conflict and prefer GitHub operational state until the mirror is repaired.\n \n@@ -75,8 +79,8 @@\n Every non-trivial change follows this loop:\n \n 1. **Goal**: clarify outcome, completion conditions, non-goals, risk level, and success signals.\n-2. **grill-with-docs**: challenge the plan against domain docs, ADRs, code reality, terminology, and edge scenarios.\n-3. **Context & Impact**: find relevant docs, ADRs, code, dependencies, owners, locks, and verification surfaces.\n+2. **grill-with-docs**: challenge the plan against domain docs, Shiki SADRs, target ADRs, code reality, terminology, and edge scenarios.\n+3. **Context & Impact**: find relevant docs, SADRs, target ADRs, code, dependencies, owners, locks, and verification surfaces.\n 4. **PRD and Spec Freeze**: use `to-prd` when context is settled enough to become durable product/engineering intent. The operator's explicit approval of the PRD is Spec Freeze: the plan records a `spec_freeze` block, and no plan runs without it. Steps 1-4 form Requirements Definition \u2014 one continuous operator dialogue. After Spec Freeze, scope changes require an operator-approved Spec Amendment; non-scope-moving interpretations go to the Assumption Log.\n 5. **Issues**: use `to-issues` to create independently grabbable vertical-slice issues. Prefer AFK slices over HITL slices where possible.\n 6. **Triage**: label issues for readiness, risk, runtime, skills, and MergeGate state.\n@@ -167,7 +171,7 @@\n - Testing decisions.\n - Out-of-scope items.\n - Further notes.\n-- Links to relevant domain terms and ADRs.\n+- Links to relevant domain terms, SADRs, and target ADRs.\n \n Every executable task must include:\n \n@@ -193,7 +197,7 @@\n \n Context & Impact must identify:\n \n-- Relevant documents, domain terms, ADRs, and past decisions.\n+- Relevant documents, domain terms, SADRs, target ADRs, and past decisions.\n - Relevant modules, interfaces, seams, callers, owners, and tests.\n - Dependency relationships between tasks.\n - Candidate locks and lock conflicts.\n@@ -210,7 +214,7 @@\n | Skill | Required when |\n | --- | --- |\n | `setup-matt-pocock-skills` | First configuring the repo for skills, or when issue tracker, triage labels, or domain docs are missing. |\n-| `grill-with-docs` | Default for non-trivial Goals before PRD/issues; always required when plans, terminology, boundaries, tradeoffs, or ADR-worthy decisions are ambiguous. |\n+| `grill-with-docs` | Default for non-trivial Goals before PRD/issues; always required when plans, terminology, boundaries, tradeoffs, or decision-record-worthy choices are ambiguous. |\n | `zoom-out` | The agent lacks an architectural map of the relevant code area. |\n | `to-prd` | Settled Goal context must become durable product/engineering intent. |\n | `to-issues` | A Goal, PRD, or plan must become independently grabbable vertical-slice issues. |\n@@ -348,7 +352,7 @@\n - Three or more new files.\n - Any high-risk or critical label.\n \n-When the architecture gate triggers, run `grill-with-docs` or `improve-codebase-architecture` as appropriate and record durable decisions in an ADR when needed.\n+When the architecture gate triggers, run `grill-with-docs` or `improve-codebase-architecture` as appropriate and record durable decisions in a Shiki SADR or target ADR as applicable.\n \n ## Engineering Discipline\n \ndiff --git a/CLAUDE.md b/CLAUDE.md\nindex 98a932e7841a632866fcd53b0bad0e3af3711d3d..98a31399404d6fb044b4fd493f4459ac87f439de\n--- a/CLAUDE.md\n+++ b/CLAUDE.md\n@@ -14,6 +14,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n ## Claude Role\n \n Default to planner, default implementer, reviewer, coordinator, documentation editor, CCA-style completion judge, and final-judgment assistant.\n@@ -57,7 +60,7 @@\n At the start of a Shiki session:\n \n 1. Read `AGENTS.md`.\n-2. Read `CONTEXT.md` and relevant ADRs.\n+2. Read `CONTEXT.md` and relevant Shiki SADRs and target ADRs.\n 3. Read active `.shiki/` Goal/task/ledger state when present.\n 4. Read active distilled rules from `.shiki/memories/` (status `distilled`, `active: true`, not revoked or superseded) for applicable guidance; the same rules are injected deterministically into task handoffs as the `## Distilled Rules` section (\u00a73.5).\n 5. Read the GitHub Issue or PR when the task is GitHub-backed.\n@@ -74,7 +77,7 @@\n - Define the Goal, completion conditions, non-goals, and risk level.\n - Treat `grill-with-docs` as the default planning entry point for non-trivial Goals.\n - Challenge domain terms against `CONTEXT.md`.\n-- Challenge hard-to-reverse tradeoffs against ADRs.\n+- Challenge hard-to-reverse tradeoffs against SADRs and target ADRs.\n - Explore code instead of asking when code can answer the question.\n - Run Context & Impact before implementation planning.\n - Produce a Task DAG, not an unordered checklist.\n@@ -94,7 +97,7 @@\n - For each question, provide your recommended answer.\n - Resolve dependency decisions before downstream design questions.\n - Update `CONTEXT.md` inline when terminology crystallizes.\n-- Offer ADRs only when the decision is hard to reverse, surprising without context, and a real tradeoff.\n+- Offer a SADR or target ADR only when the decision is hard to reverse, surprising without context, and a real tradeoff.\n - Convert unresolved questions into HITL blockers rather than allowing Codex to guess.\n \n ## PRD and Issues Standard\n@@ -103,7 +106,7 @@\n \n - Use the project's domain glossary vocabulary.\n - Avoid volatile file paths unless they are essential.\n-- Include implementation decisions, testing decisions, out-of-scope items, and relevant ADR/domain links.\n+- Include implementation decisions, testing decisions, out-of-scope items, and relevant SADR/target-ADR/domain links.\n - Identify deep-module opportunities and testable interfaces.\n \n When creating issues:\n@@ -121,7 +124,7 @@\n - Branch/worktree target.\n - Scope and non-goals.\n - Dependencies and locks.\n-- Relevant docs, ADRs, modules, and tests.\n+- Relevant docs, SADRs, target ADRs, modules, and tests.\n - Required skill invocations.\n - TDD expectations.\n - Pre-PR `code-review` gate expectations.\n@@ -152,7 +155,7 @@\n \n - Treat yourself as a judge, not implementer.\n - Do not write production code.\n-- Read `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant ADRs, PRD, task issue, PR body, diff, checks, reviews, labels, and `.shiki/` state.\n+- Read `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant SADRs and target ADRs, PRD, task issue, PR body, diff, checks, reviews, labels, and `.shiki/` state.\n - Evaluate every applicable checklist in `docs/agents/checklists.md`.\n - Map each acceptance criterion to durable evidence.\n - Distinguish implementation failure from missing evidence.\ndiff --git a/CONTEXT.md b/CONTEXT.md\nindex e61a9be06a0cce0d026484083a657bde2067e234..237ee8aa3ea8fab334ece49768bc5ed3b5af0974\n--- a/CONTEXT.md\n+++ b/CONTEXT.md\n@@ -25,7 +25,7 @@\n _Avoid_: jumping directly into code\n \n **grill-with-docs**:\n-A planning skill that challenges a plan against domain language, ADRs, code reality, and concrete edge scenarios. It resolves design-tree questions before PRD/issues. It runs inside Requirements Definition.\n+A planning skill that challenges a plan against domain language, Shiki SADRs, target ADRs, code reality, and concrete edge scenarios. It resolves design-tree questions before PRD/issues. It runs inside Requirements Definition.\n _Avoid_: generic brainstorming, silent assumptions\n \n **Requirements Definition**:\ndiff --git a/CONTRIBUTING.md b/CONTRIBUTING.md\nindex c6d9b656e73abb8a55e5f660123e03153dcb3e6d..9e37a8fbf668db5b22461aaae5548b8f1354394c\n--- a/CONTRIBUTING.md\n+++ b/CONTRIBUTING.md\n@@ -17,7 +17,7 @@\n 1. **Goal** \u2014 start from a user-approved Goal with an outcome, completion\n    conditions, non-goals, risk level, and success signals.\n 2. **grill-with-docs** \u2014 for any non-trivial change, challenge the plan against\n-   domain language, ADRs, and code reality before writing a PRD.\n+   domain language, Shiki SADRs, target ADRs, and code reality before writing a PRD.\n 3. **PRD** \u2014 capture settled product/engineering intent (`to-prd`).\n 4. **Spec Freeze** \u2014 the operator explicitly approves the PRD/requirements;\n    the plan records a `spec_freeze` block and no plan runs without it\n@@ -94,7 +94,7 @@\n   entries.\n - Do not hand-edit `.shiki/goals/*.json` or `.shiki/manifest.json` in a task PR.\n - Conversation state is not durable truth. If a decision matters, record it in\n-  GitHub, `.shiki/`, `CONTEXT.md`, or an ADR.\n+  GitHub, `.shiki/`, `CONTEXT.md`, a Shiki SADR, or a target ADR.\n \n ## Architecture Gate\n \n@@ -103,7 +103,7 @@\n or CI policy, package structure, three or more structural files, more than 100\n net new lines in a core module, three or more new files, or any high-risk label.\n When triggered, run `grill-with-docs` or `improve-codebase-architecture` and\n-record durable decisions in an ADR when needed (see `docs/adr/`).\n+record durable decisions in a Shiki SADR or target ADR as applicable (see `docs/adr/`).\n \n ## Safety\n \ndiff --git a/SYSTEM_PROMPT.md b/SYSTEM_PROMPT.md\nindex aaaea45e3a896c3a44ba89eec8bb84325ecf6c62..da009c86a88285269dee0155e58e550c6d744ea9\n--- a/SYSTEM_PROMPT.md\n+++ b/SYSTEM_PROMPT.md\n@@ -30,6 +30,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n Use the canonical source-of-truth order above. Runtime-specific reading orders\n are operational guidance only and must not override or reorder it. The current\n conversation is non-durable operator input.\n@@ -48,14 +51,14 @@\n \n A runtime may only do work that is explicitly assigned by the Goal, task, PR, or MergeGate decision.\n \n-Do not assume `openai/codex-action`, `OPENAI_API_KEY`, or API-key based Codex automation in the default Shiki path. API-key runners are explicit target-repo extensions and require their own ADR.\n+Do not assume `openai/codex-action`, `OPENAI_API_KEY`, or API-key based Codex automation in the default Shiki path. API-key runners are explicit target-repo extensions and require their own target ADR.\n \n ## 4. Non-negotiable Shiki flow\n \n For any non-trivial Goal, use this flow:\n \n 1. **Goal Seek**: define outcome, non-goals, risk level, completion criteria, and evidence requirements.\n-2. **grill-with-docs**: challenge the plan against `CONTEXT.md`, ADRs, code reality, terminology, boundaries, and edge scenarios. Ask one question at a time when the operator is available. If code can answer a question, inspect code instead of asking.\n+2. **grill-with-docs**: challenge the plan against `CONTEXT.md`, Shiki SADRs, target ADRs, code reality, terminology, boundaries, and edge scenarios. Ask one question at a time when the operator is available. If code can answer a question, inspect code instead of asking.\n 3. **Context & Impact**: identify modules, interfaces, seams, callers, dependencies, locks, risk, and verification surfaces.\n 4. **to-prd + Spec Freeze**: turn settled context into a PRD using domain vocabulary and testing decisions. The operator's explicit approval of the PRD is Spec Freeze, recorded as the plan's `spec_freeze` block; plans without it do not run. Steps 1-4 form Requirements Definition.\n 5. **to-issues**: decompose the PRD into independently grabbable vertical-slice issues. Prefer AFK slices; mark HITL slices when judgment is still required.\n@@ -75,7 +78,7 @@\n Before making any material change:\n \n 1. Read `AGENTS.md` and any runtime-specific wrapper such as `CLAUDE.md`.\n-2. Read `CONTEXT.md` and relevant ADRs.\n+2. Read `CONTEXT.md` and relevant SADRs and target ADRs.\n 3. Read the GitHub Issue / PR and `.shiki/` state for the active Goal or task.\n 4. Identify your role: Planner, Implementer, Reviewer, Completion Check Agent, Repairer, or Guardian-assist.\n 5. Identify Goal id, task id, branch, PR, dependencies, locks, risk level, required skills, and required evidence.\n@@ -103,7 +106,7 @@\n \n At minimum, identify:\n \n-- Relevant documents, ADRs, domain terms, and prior decisions.\n+- Relevant documents, SADRs, target ADRs, domain terms, and prior decisions.\n - Relevant modules, interfaces, seams, callers, owners, and tests.\n - Dependency relationships between tasks.\n - Candidate file locks and lock conflicts.\n@@ -152,7 +155,7 @@\n \n CCA must read:\n \n-- `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant ADRs.\n+- `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant SADRs and target ADRs.\n - Parent Goal issue, PRD issue, task issue, comments, and labels.\n - PR body, diff, commits, changed files, reviews, CI checks, and artifacts available to the workflow.\n - `.shiki/` task contract, locks, ledger entries, CCA checklist profile, and prior repair packets.\n@@ -214,7 +217,7 @@\n - Three or more new files.\n - Any high-risk or critical label.\n \n-When the architecture gate triggers, run `grill-with-docs` or `improve-codebase-architecture` as appropriate and record the decision in an ADR if it is hard to reverse or likely to be re-litigated.\n+When the architecture gate triggers, run `grill-with-docs` or `improve-codebase-architecture` as appropriate and record the decision in a Shiki SADR or target ADR as applicable if it is hard to reverse or likely to be re-litigated.\n \n ## 14. Skill Gate\n \n@@ -223,7 +226,7 @@\n Use these skills:\n \n - `setup-matt-pocock-skills`: before first use of the engineering skill set, or when issue tracker, triage labels, or domain docs are missing.\n-- `grill-with-docs`: default for non-trivial Goals before PRD/issues; required for ambiguous plans, terminology, boundaries, ADR-worthy tradeoffs, or design-tree decisions.\n+- `grill-with-docs`: default for non-trivial Goals before PRD/issues; required for ambiguous plans, terminology, boundaries, decision-record-worthy tradeoffs, or design-tree decisions.\n - `zoom-out`: unfamiliar code area or missing architectural map.\n - `to-prd`: convert settled context into a PRD.\n - `to-issues`: break a PRD, plan, or Goal into independently grabbable vertical-slice issues.\ndiff --git a/docs/adr/0001-keep-shiki-as-platform-name.md b/docs/adr/SADR-0001-keep-shiki-as-platform-name.md\nindex 20a9637c3b9d8c97de7ba8db7cfae4d82d9a7dba..8b685439959aada1b221c112f0ac3c51ca8a1695\n--- a/docs/adr/0001-keep-shiki-as-platform-name.md\n+++ b/docs/adr/SADR-0001-keep-shiki-as-platform-name.md\n@@ -1,3 +1,3 @@\n-# Keep Shiki As The Platform Name\n+# SADR-0001: Keep Shiki As The Platform Name\n \n Shiki remains the product name for the GitHub-native agentic engineering control plane. We considered more descriptive names such as MergePilot, MergeGate, and Kiroku, but kept Shiki because it carries the existing goal-seeking framework identity while remaining independent of Hermes, Codex, Claude Code, or any single runtime.\ndiff --git a/docs/adr/0002-use-agents-as-shared-constitution.md b/docs/adr/SADR-0002-use-agents-as-shared-constitution.md\nindex 73de30ecb078120eac540d9bc51855e3cd48d0e3..c5486182bf06fca36ea508496b013c67302fd989\n--- a/docs/adr/0002-use-agents-as-shared-constitution.md\n+++ b/docs/adr/SADR-0002-use-agents-as-shared-constitution.md\n@@ -1,3 +1,3 @@\n-# Use AGENTS As The Shared Agent Constitution\n+# SADR-0002: Use AGENTS As The Shared Agent Constitution\n \n Shiki uses `AGENTS.md` as the runtime-neutral constitution and `CLAUDE.md` as a thin Claude Code wrapper. This keeps Codex, Claude Code, Hermes Runner, and future Agent Runtimes aligned on the same GitHub-first MergeGate rules while still allowing runtime-specific instructions where needed.\ndiff --git a/docs/adr/0003-require-skill-gate-for-mergegate-readiness.md b/docs/adr/SADR-0003-require-skill-gate-for-mergegate-readiness.md\nindex 424be0c9af4c8f0136f1fde68a468c47ff84e764..7fc337987c7df9792f75f181dfcbed75fe5ffff2\n--- a/docs/adr/0003-require-skill-gate-for-mergegate-readiness.md\n+++ b/docs/adr/SADR-0003-require-skill-gate-for-mergegate-readiness.md\n@@ -1,3 +1,3 @@\n-# Require Skill Gate For MergeGate Readiness\n+# SADR-0003: Require Skill Gate For MergeGate Readiness\n \n Shiki requires relevant engineering skills to be invoked before execution when their triggers apply. This makes skills part of MergeGate readiness rather than optional prompt style, because Shiki depends on repeatable planning, issue decomposition, TDD, diagnosis, triage, and architecture review before autonomous or parallel agent work can be trusted.\ndiff --git a/docs/adr/0004-use-github-first-with-shiki-mirror.md b/docs/adr/SADR-0004-use-github-first-with-shiki-mirror.md\nindex d0a3a9174dc646a945a9d325954ffaf00c352040..a79c54ae9ee8eed2a0524192b864da4e22a92d3f\n--- a/docs/adr/0004-use-github-first-with-shiki-mirror.md\n+++ b/docs/adr/SADR-0004-use-github-first-with-shiki-mirror.md\n@@ -1,4 +1,4 @@\n-# ADR 0004: Use GitHub First With A Repository-Local Shiki Mirror\n+# SADR-0004: Use GitHub First With A Repository-Local Shiki Mirror\n \n ## Status\n \ndiff --git a/docs/adr/0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md b/docs/adr/SADR-0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\nindex b339baf49b89d9290e79c3d51b36e7b2bde3bd95..0f3c3eb9afccdb3403ce978e21d04fe12e495da0\n--- a/docs/adr/0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\n+++ b/docs/adr/SADR-0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\n@@ -1,8 +1,8 @@\n-# ADR 0005: Use Codex As Front And Claude Code Action As The GitHub Runtime\n+# SADR-0005: Use Codex As Front And Claude Code Action As The GitHub Runtime\n \n ## Status\n \n-Accepted (implementer default superseded by ADR 0008; the auth model and the\n+Accepted (implementer default superseded by SADR-0008; the auth model and the\n Claude Code Action GitHub runtime decision remain in force)\n \n ## Context\n@@ -26,4 +26,4 @@\n - Shiki avoids requiring `OPENAI_API_KEY` for the default workflow.\n - Codex work happens through the user's authenticated Codex front surface, not as a hidden backend Action.\n - Claude Code Action is configured for OAuth-token based subscription usage by default.\n-- API-key based automation remains possible only as an explicit target-repo extension with its own ADR.\n+- API-key based automation remains possible only as an explicit target-repo extension with its own target ADR.\ndiff --git a/docs/adr/0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md b/docs/adr/SADR-0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\nindex 4bcae9d1e52f0a37b8e9fbafa5cd14b6ee1993bd..7a38538a6ae9929567f0ade67e7ea6a60d0b3f7e\n--- a/docs/adr/0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\n+++ b/docs/adr/SADR-0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\n@@ -1,4 +1,4 @@\n-# ADR 0006: Separate Codex Implementation, CCA Completion, And MergeGate Authorization\n+# SADR-0006: Separate Codex Implementation, CCA Completion, And MergeGate Authorization\n \n ## Status\n \ndiff --git a/docs/adr/0007-packaging-and-release.md b/docs/adr/SADR-0007-packaging-and-release.md\nindex 79b84ddb97167372b44641e5b7860e400ce80822..bb445a2e2e856d9d1f5fdbe35de8d4e8cec0fb32\n--- a/docs/adr/0007-packaging-and-release.md\n+++ b/docs/adr/SADR-0007-packaging-and-release.md\n@@ -1,4 +1,4 @@\n-# ADR 0007: Packaging And Release For Shiki\n+# SADR-0007: Packaging And Release For Shiki\n \n ## Status\n \n@@ -25,7 +25,7 @@\n   not introduce third-party runtime dependencies or break the shim.\n - Releases must run inside the operator's subscription-authenticated toolchain\n   and must not require extra secrets beyond the built-in `GITHUB_TOKEN`\n-  (consistent with ADR 0005's no-API-key-by-default stance).\n+  (consistent with SADR-0005's no-API-key-by-default stance).\n - `.shiki/manifest.json` already encodes the install surface (`install.include`,\n   `install.create_directories`, `install.exclude_from_commit`), and it must not\n   be mutated as part of this change.\n@@ -34,7 +34,7 @@\n \n This is a hard-to-reverse platform decision (it sets the version source of\n truth, the release trigger, and the public install contract), so it is recorded\n-as an ADR.\n+as a SADR.\n \n ## Decision\n \n@@ -104,7 +104,7 @@\n 3. **Publishing to PyPI.** Rejected for now: Shiki is distributed as a\n    repository template + git-installable CLI, not a PyPI library; PyPI would add\n    credentials/secrets and a different versioning contract. Can be added later as\n-   its own ADR if a packaged distribution is needed.\n+   its own SADR if a packaged distribution is needed.\n 4. **Dynamic version (read `VERSION` via a build backend `dynamic` field).**\n    Rejected to keep `pyproject.toml` backend-agnostic and dependency-free; the\n    small duplication between `VERSION` and `pyproject.toml` is guarded by the\n@@ -123,4 +123,4 @@\n   releasing versions the whole platform but ships only `install.include` to\n   targets.\n - Future PyPI distribution or a console entry point are deferred and would each\n-  warrant their own ADR.\n+  warrant their own SADR.\ndiff --git a/docs/adr/0008-use-claude-code-as-default-implementer.md b/docs/adr/SADR-0008-use-claude-code-as-default-implementer.md\nindex dedf4510b7c7799521422f622029563d373db031..35c2625d908167fbc58855b5b63e283805f167e8\n--- a/docs/adr/0008-use-claude-code-as-default-implementer.md\n+++ b/docs/adr/SADR-0008-use-claude-code-as-default-implementer.md\n@@ -1,12 +1,12 @@\n-# ADR 0008: Use Claude Code As The Default Implementer Runtime\n+# SADR-0008: Use Claude Code As The Default Implementer Runtime\n \n ## Status\n \n-Proposed (supersedes ADR 0005 for the implementer default; keeps its auth model)\n+Proposed (supersedes SADR-0005 for the implementer default; keeps its auth model)\n \n ## Context\n \n-ADR 0005 made Codex Front the default implementation runtime, with Claude Code\n+SADR-0005 made Codex Front the default implementation runtime, with Claude Code\n restricted to planner and reviewer roles by the runtime registry, and the only\n autonomous implementation adapter was `shiki runner codex`.\n \n@@ -18,13 +18,13 @@\n - The platform's planning, review, and CCA layers already run on Claude Code,\n   and Claude Code's native capabilities (the built-in `/code-review` skill and\n   the Workflow multi-agent orchestration tool) have no Codex equivalent.\n-- The Goal lifecycle redesign (ADR 0009) requires an autonomous post-freeze\n+- The Goal lifecycle redesign (SADR-0009) requires an autonomous post-freeze\n   loop driven from the operator's Claude Code session, including bounded\n   repair dispatch, which must not depend on a runtime that cannot be invoked\n   unattended.\n \n The constitution claims Shiki is runtime-agnostic (\"not a Claude-only\n-workflow\", ADR 0002). Any change to the implementer default must preserve the\n+workflow\", SADR-0002). Any change to the implementer default must preserve the\n registry-based runtime model rather than hardcoding one vendor.\n \n ## Decision\n@@ -39,7 +39,7 @@\n   the task to review or repair-needed.\n - Keep Codex registered as an optional implementer runtime. `shiki runner\n   codex`, the Codex handoff templates, and the ChatGPT OAuth auth model from\n-  ADR 0005 remain supported for tasks explicitly assigned to `codex`.\n+  SADR-0005 remain supported for tasks explicitly assigned to `codex`.\n - Commit to mandating two Claude-native capabilities as loop contract in\n   follow-up Goals (status: the code-review gate was delivered by Goal G-B \u2014\n   skill registry, checklists item PR-12, default required_skills \u2014 while the\n@@ -60,7 +60,7 @@\n \n Out of scope: removing Codex support, changing the CCA runtime (Claude Code\n Action with `CLAUDE_CODE_OAUTH_TOKEN` stays), and API-key based automation\n-(still requires its own ADR per ADR 0005).\n+(still requires its own target ADR per SADR-0005).\n \n ## Consequences\n \n@@ -100,7 +100,7 @@\n   harness rejection of direct `codex exec` is outside Shiki's control, and the\n   plan's mandatory native capabilities cannot run on Codex at all.\n - **Remove Codex entirely**: rejected; it abandons the runtime-agnostic\n-  constitution (ADR 0002) for no operational gain and forecloses\n+  constitution (SADR-0002) for no operational gain and forecloses\n   cross-runtime adversarial review.\n - **GitHub Actions cloud implementation via claude-code-action**: rejected as\n   default; durable evidence is automatic but token cost, Node 24 workflow\ndiff --git a/docs/adr/0009-requirements-definition-and-spec-freeze.md b/docs/adr/SADR-0009-requirements-definition-and-spec-freeze.md\nindex 29b3f7871bef9203a8b4a831a65c39eb2f506e42..52e7dd69ad0aa4efe0b68c62678abbf7df02aaec\n--- a/docs/adr/0009-requirements-definition-and-spec-freeze.md\n+++ b/docs/adr/SADR-0009-requirements-definition-and-spec-freeze.md\n@@ -1,4 +1,4 @@\n-# ADR 0009: Merge Grill And Planning Into Requirements Definition With Spec Freeze\n+# SADR-0009: Merge Grill And Planning Into Requirements Definition With Spec Freeze\n \n ## Status\n \ndiff --git a/docs/adr/0010-external-ai-guardian-review.md b/docs/adr/SADR-0010-external-ai-guardian-review.md\nindex e6108bbe8bb4e73e0ff4684c8f78001a80438c4e..f3510b145664298adafb151b6f0db51b972f01ec\n--- a/docs/adr/0010-external-ai-guardian-review.md\n+++ b/docs/adr/SADR-0010-external-ai-guardian-review.md\n@@ -1,4 +1,4 @@\n-# ADR 0010: External AI Guardian Review As A First-Class Approval Authority\n+# SADR-0010: External AI Guardian Review As A First-Class Approval Authority\n \n ## Status\n \n@@ -8,7 +8,7 @@\n \n Shiki is intended to be an autonomous, recursive, self-improving agent\n framework: authority-in-the-loop, not human-in-the-loop. Until now the Guardian\n-approval required for high/critical-risk merges (guardian-policy.json, ADR\n+approval required for high/critical-risk merges (guardian-policy.json, SADR\n governance) recognized only human authority sources \u2014 a GitHub review approval,\n the `guardian:approved` label applied by a configured Guardian, or a comment\n carrying `Guardian approval granted` plus the current head SHA.\n@@ -89,7 +89,7 @@\n   autonomous design the operator intends and produced the impersonation\n   incident.\n - **Record AI approval as an operator comment**: rejected as forgery of human\n-  authority \u2014 the exact failure this ADR corrects.\n+  authority \u2014 the exact failure this SADR corrects.\n - **Deliver the artifact as a committed `.shiki/` file**: rejected; a committed\n   approval inside the PR being approved is forgeable by anyone who can push.\n - **A dedicated reviewer bot identity instead of a relayed comment**: deferred;\ndiff --git a/docs/adr/0011-autonomous-quality-gate-evidence.md b/docs/adr/SADR-0011-autonomous-quality-gate-evidence.md\nindex bca0246c5d46e44afbde15486a75eec425fc02f5..9c93eac16d8c3a6d4b11bd7224e3955102cd7db4\n--- a/docs/adr/0011-autonomous-quality-gate-evidence.md\n+++ b/docs/adr/SADR-0011-autonomous-quality-gate-evidence.md\n@@ -1,4 +1,4 @@\n-# ADR 0011: Autonomous Quality-Gate Evidence \u2014 Deterministic Facts And An Independent Verifier\n+# SADR-0011: Autonomous Quality-Gate Evidence \u2014 Deterministic Facts And An Independent Verifier\n \n ## Status\n \ndiff --git a/docs/adr/0012-autonomous-loop-closeout-to-main.md b/docs/adr/SADR-0012-autonomous-loop-closeout-to-main.md\nindex 637fad6dde4989c9c2bf0f81f676d8d29991123c..ec7d85f81030a676bcbba28b43eb365422a36eac\n--- a/docs/adr/0012-autonomous-loop-closeout-to-main.md\n+++ b/docs/adr/SADR-0012-autonomous-loop-closeout-to-main.md\n@@ -1,4 +1,4 @@\n-# 0012 \u2014 Autonomous loop pushes goal completion to main via a closeout PR\n+# SADR-0012 \u2014 Autonomous loop pushes goal completion to main via a closeout PR\n \n - Status: accepted\n - Date: 2026-06-18\ndiff --git a/docs/adr/0013-bootstrap-grants-actions-approve-pull-requests.md b/docs/adr/SADR-0013-bootstrap-grants-actions-approve-pull-requests.md\nindex b6c69d64f1c5ced346b88d6859586715b3b5bd0e..cf48ff4a6716267f539ca58edd1a852e7c7a2d64\n--- a/docs/adr/0013-bootstrap-grants-actions-approve-pull-requests.md\n+++ b/docs/adr/SADR-0013-bootstrap-grants-actions-approve-pull-requests.md\n@@ -1,4 +1,4 @@\n-# ADR 0013: Bootstrap Grants GitHub Actions Permission To Create And Approve Pull Requests\n+# SADR-0013: Bootstrap Grants GitHub Actions Permission To Create And Approve Pull Requests\n \n ## Status\n \n@@ -31,7 +31,7 @@\n security tradeoff: it lets repository automation satisfy a human-review gate. In\n a multi-maintainer repo that could let a compromised or buggy workflow\n self-approve changes. So whether bootstrap turns it on by default \u2014 and with\n-what default token scope \u2014 is an ADR-worthy decision, not an implementation\n+what default token scope \u2014 is a SADR-worthy decision, not an implementation\n detail.\n \n ## Decision\n@@ -48,7 +48,7 @@\n Specifically:\n \n - **`can_approve_pull_request_reviews` defaults to `true`.** Shiki's default\n-  posture is solo/AFK autonomous operation (ADR 0008/0009/0012), where the CCA\n+  posture is solo/AFK autonomous operation (SADR-0008/0009/0012), where the CCA\n   Review Bridge is the only path that can satisfy `required_review: true` after\n   the CCA and Guardian gates have already passed. The default makes a\n   freshly-bootstrapped repo able to run the autonomous loop end to end without a\ndiff --git a/docs/adr/0014-codex-app-external-ai-guardian-ui-adapter.md b/docs/adr/SADR-0014-codex-app-external-ai-guardian-ui-adapter.md\nindex 238db06279334caa341c117362df9271382a7fcd..65f12e9939321e1b3e389a34c822fc1549e5155d\n--- a/docs/adr/0014-codex-app-external-ai-guardian-ui-adapter.md\n+++ b/docs/adr/SADR-0014-codex-app-external-ai-guardian-ui-adapter.md\n@@ -1,4 +1,4 @@\n-# ADR 0014: Use Codex App As The External AI Guardian UI Adapter\n+# SADR-0014: Use Codex App As The External AI Guardian UI Adapter\n \n ## Status\n \ndiff --git a/docs/adr/0015-contract-approval-before-dispatch.md b/docs/adr/SADR-0015-contract-approval-before-dispatch.md\nindex fad6c656741a20a7684ba16080a7db159f63a52f..6061271a46d3f1d6ae7174477de3fb75673123a6\n--- a/docs/adr/0015-contract-approval-before-dispatch.md\n+++ b/docs/adr/SADR-0015-contract-approval-before-dispatch.md\n@@ -1,4 +1,4 @@\n-# ADR 0015: Approve The Task Contract Before Dispatch, Not The Diff After It\n+# SADR-0015: Approve The Task Contract Before Dispatch, Not The Diff After It\n \n ## Status\n \n@@ -9,7 +9,7 @@\n Guardian approval for high/critical risk is expressible only on a pull request.\n Every approval source in `guardian-policy.json` \u2014 GitHub review, the\n `guardian:approved` label, the `Guardian approval granted` comment, and the\n-`external_ai_guardian_review` artifact (ADR 0010/0014) \u2014 is bound to a PR, and\n+`external_ai_guardian_review` artifact (SADR-0010/0014) \u2014 is bound to a PR, and\n three of the four require the current head SHA. Approval therefore cannot exist\n before an implementation PR exists.\n \n@@ -81,7 +81,7 @@\n An envelope breach must surface only on the non-repairable policy gate, never on\n the repairable `MergeGate metadata check`. Otherwise the Goal Loop routes it to\n `dispatch_repair` \u2014 instructing a runner to make the breach go away \u2014 which is\n-exactly what this ADR forbids.\n+exactly what this SADR forbids.\n \n **Contract immutability.** After registration, an implementation PR must not\n change the governance fields of its own task contract (`scope`, `non_goals`,\n@@ -116,7 +116,7 @@\n fails any verdict that is not `complete`, and the signal the CCA prompt reads is\n computed from `evaluate_guardian_approval` alone. Without the OR-merge at the\n signal, a contract-approved PR still returns `needs_guardian`, the CCA job fails,\n-and the MergeGate policy check never runs \u2014 the gate this ADR changes would never\n+and the MergeGate policy check never runs \u2014 the gate this SADR changes would never\n be reached. The CCA *prompt* needs no change: a `contract_approval` entry in the\n signal's existing `sources` array satisfies its current wording.\n \n@@ -130,7 +130,7 @@\n written, and an approved contract lets its implementation PRs merge through the\n same automatic path as low/medium risk. The wasted implement-then-block cycle\n disappears. Guardian review shifts from reading diffs to reading contracts, which\n-is both earlier and smaller; the external AI Guardian path (ADR 0010/0014) works\n+is both earlier and smaller; the external AI Guardian path (SADR-0010/0014) works\n on contracts with no change.\n \n We accept that the Guardian authorizes a class of change without seeing its\n@@ -146,7 +146,7 @@\n adversarial tests for the new approval path equivalent in rigor to\n `test_shiki_governance_evidence.sh`.\n \n-Bootstrap costs, which apply only while this ADR is being implemented. The tasks\n+Bootstrap costs, which apply only while this SADR is being implemented. The tasks\n that build Contract Approval merge under the existing rules, so the ones\n dispatched after the pre-dispatch gate lands but before the approval path lands\n need a Guardian-approved Contract PR *and* a live Guardian approval on their\n@@ -210,7 +210,7 @@\n risk; they do not bound authorized work.\n \n **Auto-escalate risk on envelope breach instead of stopping.** Returns the\n-workflow to a diff-time Guardian decision \u2014 the exact behavior this ADR removes.\n+workflow to a diff-time Guardian decision \u2014 the exact behavior this SADR removes.\n \n **Resolve governance fields from the base snapshot only.** Rejected against the\n registration flow: implementation PRs add their own contract, so the base has no\ndiff --git a/docs/adr/0016-locks-bound-product-paths.md b/docs/adr/SADR-0016-locks-bound-product-paths.md\nindex b5e850f3f1f7f4ef6e7f9d1763a29a51bd300c45..46b40a9b99a7aae644c02bb458f5473ff0a90ce2\n--- a/docs/adr/0016-locks-bound-product-paths.md\n+++ b/docs/adr/SADR-0016-locks-bound-product-paths.md\n@@ -1,4 +1,4 @@\n-# ADR 0016: Locks bound product paths; the Shiki mirror is bound by record identity\n+# SADR-0016: Locks bound product paths; the Shiki mirror is bound by record identity\n \n ## Status\n \n@@ -6,8 +6,8 @@\n \n - Date: 2026-07-28\n - Deciders: operator (mizutani-140), Claude (planner)\n-- Related: ADR 0004 (GitHub-first with a `.shiki` mirror), ADR 0009 (Spec Freeze),\n-  ADR 0012 (autonomous loop closeout to main), ADR 0015 (Contract Approval before\n+- Related: SADR-0004 (GitHub-first with a `.shiki` mirror), SADR-0009 (Spec Freeze),\n+  SADR-0012 (autonomous loop closeout to main), SADR-0015 (Contract Approval before\n   dispatch), remediation goal task R-02, the live PR #179 deadlock.\n \n ## Context\n@@ -90,11 +90,11 @@\n files, documentation files and named source modules are nameable; runtime-\n allocated ledger, runner and report ids are not \u2014 and those live in `.shiki`,\n which locks no longer govern. Existing frozen contracts are brought into line by\n-a one-time Spec Amendment per plan, which must land before ADR 0015's contract\n+a one-time Spec Amendment per plan, which must land before SADR-0015's contract\n immutability makes `locks` unwritable from the implementation PR.\n \n **Lock state is resolved from the durable side.** MergeGate resolves the lock\n-registry, and ADR 0015's governance fields, from the base `.shiki` snapshot when\n+registry, and SADR-0015's governance fields, from the base `.shiki` snapshot when\n present, falling back to head only when absent. The `MergeGate metadata check`\n job is given `--base-shiki`, which it does not receive today. Locks are not\n derived from the live open-pull-request set: a pull request body is untrusted\n@@ -108,7 +108,7 @@\n Explicitly out of scope: moving MergeGate's contract resolution off the PR\n checkout wholesale; making the mirror a projection of GitHub state; introducing\n new lock kinds; moving ledger evidence out of the repository; and unwinding\n-ADR 0012's closeout pull request. Registration of multi-task goals is ADR 0015's\n+SADR-0012's closeout pull request. Registration of multi-task goals is SADR-0015's\n Contract PR and is not re-decided here.\n \n ## Consequences\n@@ -127,7 +127,7 @@\n \n Risks accepted. First, goal-scoped identity is only as strong as `goal_id`,\n which is resolved from the PR body and the head task file; the mitigation is to\n-add `goal_id` to ADR 0015's frozen governance field set, and until that lands a\n+add `goal_id` to SADR-0015's frozen governance field set, and until that lands a\n task file can name a foreign goal. Second, base-when-present resolution is\n unavailable on fork pull requests, where the head is contributor-controlled;\n fork pull requests already cannot complete a Shiki merge, and this decision does\n@@ -139,7 +139,7 @@\n path, not by implementation pull requests.\n \n New obligations: one Spec Amendment per frozen plan to rewrite `locks`, ordered\n-before ADR 0015 T1; extension of the adversarial suites in\n+before SADR-0015 T1; extension of the adversarial suites in\n `scripts/test_shiki_adversarial_state.sh` and\n `scripts/test_shiki_governance_evidence.sh` to cover the eleven newly-scoped\n directories; and updating `scripts/test_shiki_loop_lock_guard.sh`,\n@@ -151,32 +151,32 @@\n whether the loop's transitive sync of ledger `evidence` references can be\n deleted once the Contract PR carries its own source plan.\n \n-## Relationship to ADR 0004\n+## Relationship to SADR-0004\n \n-This ADR **clarifies something ADR 0004 left unspecified**. ADR 0004 decides that\n+This SADR **clarifies something SADR-0004 left unspecified**. SADR-0004 decides that\n GitHub is the operational source of truth and that each target repository keeps a\n `.shiki/` mirror. It says nothing about pull requests writing the mirror, does\n not name MergeGate, and does not make the mirror an input to a gate. Nothing in\n-ADR 0004's Decision or Consequences is amended, superseded or weakened: the\n+SADR-0004's Decision or Consequences is amended, superseded or weakened: the\n mirror still exists, still records the same artifacts, and pull request\n-boundaries remain the coordination surface. What this ADR settles is the question\n+boundaries remain the coordination surface. What this SADR settles is the question\n 0004 never reached \u2014 by what authority a pull request may write a mirror record \u2014\n and it answers it the way `.shiki/manifest.json` already declares\n (`pr_mutation: current-task-or-current-goal-only`).\n \n-It **amends ADR 0012**, whose Decision item 5 states that \"Loop-executed tasks\n+It **amends SADR-0012**, whose Decision item 5 states that \"Loop-executed tasks\n declare the `path:.shiki/**` lock, which covers every `.shiki` file the closeout\n stages ... so `files_outside_locks` is satisfied without per-file lock\n bookkeeping.\" That clause is replaced: the closeout's mirror writes are\n-authorized by record identity, not by a declared lock. ADR 0012's substantive\n+authorized by record identity, not by a declared lock. SADR-0012's substantive\n decision \u2014 that completion reaches `main` through a closeout pull request rather\n than local attestation \u2014 is preserved unchanged.\n \n-It is **complementary to ADR 0015**, not a substitute. ADR 0015 owns contract\n+It is **complementary to SADR-0015**, not a substitute. SADR-0015 owns contract\n immutability, the pre-dispatch gate, the Contract PR and the Scope Envelope. This\n-ADR owns what a lock means and who may write the mirror. The one ordering\n+SADR owns what a lock means and who may write the mirror. The one ordering\n constraint between them is that the lock-narrowing Spec Amendment must precede\n-ADR 0015 T1's freezing of the `locks` field.\n+SADR-0015 T1's freezing of the `locks` field.\n \n ## Alternatives Considered\n \n@@ -186,7 +186,7 @@\n the only writer that can register them may run. Migrating the Guardian signal to\n that store makes every registration PR fail closed to `needs_guardian` at every\n risk level, because the task file is by definition absent from the store, which\n-removes the low/medium autonomous path ADR 0008/0009 exists to provide. And it\n+removes the low/medium autonomous path SADR-0008/0009 exists to provide. And it\n leaves schemas, manifest, config, guardian policy, templates, migrations, plans,\n worktrees, runner records, runs and handoffs with no writer at all in a\n repository whose product is `.shiki`. Measured benefit on the frozen backlog:\n@@ -199,14 +199,14 @@\n namespace is not in fact ownership-partitioned \u2014 56 ledger entries carry no\n `task_id`, and one goal DAG is writable by 35 registered tasks. The correct\n expression of its insight is that locks do not govern the mirror at all, which is\n-what this ADR decides.\n+what this SADR decides.\n \n **Make the mirror a projection written only by a registration writer and a\n post-merge projection job.** Rejected. It requires a bypass actor with write\n access to the protected default branch; it makes its own master invariant\n readable from a manifest that pull requests may edit; it deletes both reconcile\n modes and leaves a corrupted mirror with no in-band repair path, contradicting\n-ADR 0004's consequence that mirror conflicts are repaired against GitHub state;\n+SADR-0004's consequence that mirror conflicts are repaired against GitHub state;\n and its content-derived ids are rejected by `validate_shiki`'s currently-green id\n pattern.\n \ndiff --git a/docs/adr/0017-bookkeeping-closeouts-do-not-inherit-risk.md b/docs/adr/SADR-0017-bookkeeping-closeouts-do-not-inherit-risk.md\nindex 554e3a0d526a72ca067108c5464cc68c3edcf174..b9676857a4713850e063af7c39c8e0b01ecb4c66\n--- a/docs/adr/0017-bookkeeping-closeouts-do-not-inherit-risk.md\n+++ b/docs/adr/SADR-0017-bookkeeping-closeouts-do-not-inherit-risk.md\n@@ -1,4 +1,4 @@\n-# ADR 0017: A Bookkeeping Closeout Does Not Inherit Its Task's Risk\n+# SADR-0017: A Bookkeeping Closeout Does Not Inherit Its Task's Risk\n \n ## Status\n \n@@ -6,7 +6,7 @@\n \n ## Context\n \n-ADR 0012 made the goal loop push a task's completion to the default branch through a\n+SADR-0012 made the goal loop push a task's completion to the default branch through a\n closeout pull request: after the implementation PR merges, the loop opens a second PR\n carrying `task.status = done`, `lock.state = released`, and \u2014 when the task completes\n its goal \u2014 `goal.status = complete` plus one scorecard report. Nothing else. The\n@@ -49,14 +49,14 @@\n 4. The task file's governance fields (`scope`, `non_goals`, `required_skills`,\n    `risk_level`, `locks`, `acceptance_checks`, `test_command`) are byte-identical to\n    the base snapshot. Only `status`, `expected_pr`, `closeout_pr`, `ledger_evidence`\n-   and `expected_branch` may differ \u2014 the ADR 0015 contract-immutability rule,\n+   and `expected_branch` may differ \u2014 the SADR-0015 contract-immutability rule,\n    unchanged.\n \n    `expected_branch` is in that set because a closeout is *by construction* on a\n    different branch from the implementation it closes, and the loop rewrites the\n    field accordingly. It is safe to let it differ because MergeGate independently\n    requires `expected_branch` to equal the PR's actual head ref, so a PR cannot set\n-   it to anything other than the branch it is really on. The first draft of this ADR\n+   it to anything other than the branch it is really on. The first draft of this SADR\n    omitted it, which made the exemption unreachable: every real closeout failed\n    condition 4 on a field the loop always changes. See PR #209.\n 5. The state transitions are exactly the terminal set: task `review` \u2192 `done`, lock\n@@ -113,12 +113,12 @@\n classification must be derived, not asserted.\n \n **Fold the closeout into the implementation PR** so no second PR exists. Rejected under\n-ADR 0012, which recorded why: a task file that is already `done` on the implementation\n+SADR-0012, which recorded why: a task file that is already `done` on the implementation\n branch short-circuits the loop's review \u2192 CCA \u2192 merge \u2192 done ordering into a local\n goal-completion before any merge happens.\n \n **Have the loop auto-merge closeouts with elevated credentials.** Rejected outright: that\n-is a runtime authoring its own approval, which ADR 0010 and the constitution's\n+is a runtime authoring its own approval, which SADR-0010 and the constitution's\n approval-record prohibition forbid. It would also be indistinguishable, in the evidence,\n from the forgery those rules exist to prevent.\n \ndiff --git a/docs/adr/0018-carried-guardian-approval-across-a-proven-base-sync.md b/docs/adr/SADR-0018-carried-guardian-approval-across-a-proven-base-sync.md\nindex 22358562da390ed8b45cd600da08a43d378b4680..e6e2907cefa776460af73ddf7d39db605f3c39b0\n--- a/docs/adr/0018-carried-guardian-approval-across-a-proven-base-sync.md\n+++ b/docs/adr/SADR-0018-carried-guardian-approval-across-a-proven-base-sync.md\n@@ -1,4 +1,4 @@\n-# ADR 0018: Carried Guardian Approval Across A Proven Base Sync\n+# SADR-0018: Carried Guardian Approval Across A Proven Base Sync\n \n ## Status\n \n@@ -34,7 +34,7 @@\n Guardian evaluator that consumes it (`shiki_guardian.evaluate_guardian_approval`,\n the `guardian_comment_carried` source) were built and tested in isolation. Neither\n does anything until a caller passes the flags that turn the carry on, so the\n-feature was invisible in CI. This ADR records the decision the two of them\n+feature was invisible in CI. This SADR records the decision the two of them\n implement, and this task wires it into the four CI invocations that judge Guardian\n approval.\n \ndiff --git a/docs/adr/README.md b/docs/adr/README.md\nindex c70e057a4520c36e042ab68c53cd4a21ef709b71..5da43856836e2ce6a150f8127766e2f88355ab4b\n--- a/docs/adr/README.md\n+++ b/docs/adr/README.md\n@@ -1,40 +1,41 @@\n-# Architecture Decision Records\n+# Decision Record Namespaces\n \n-This directory holds Shiki's Architecture Decision Records (ADRs): durable\n-records of hard-to-reverse platform decisions. ADRs are part of the Shiki source\n-of truth alongside GitHub state, the `.shiki/` mirror, and `CONTEXT.md`.\n+This directory holds two independent decision namespaces:\n \n-## How ADRs Work\n+- Shiki Architecture Decision Records (SADRs), named\n+  `SADR-NNNN-<short-slug>.md`, record hard-to-reverse Shiki platform decisions.\n+- Target Architecture Decision Records (ADRs), named\n+  `NNNN-<short-slug>.md`, belong to the target product.\n \n-- New ADRs are added as `docs/adr/NNNN-<short-slug>.md`, where `NNNN` is the next\n-  zero-padded sequence number.\n-- The **canonical list of ADRs is the set of files matching `docs/adr/*.md`** in\n-  this directory. Browse `docs/adr/*.md` for the authoritative, current set.\n-- Use [`template.md`](template.md) as the starting point for a new ADR.\n-- Record an ADR when a decision is hard to reverse, surprising without context,\n-  and a real tradeoff. Do not write an ADR for routine or easily reversible\n-  choices.\n+The same four-digit number may appear once in each namespace. A platform\n+`SADR-NNNN` citation resolves only to `SADR-NNNN-*.md`; a target `ADR NNNN`\n+citation resolves only to `NNNN-*.md`.\n \n-Using the `docs/adr/*.md` glob as the source of truth means other PRs can add an\n-ADR without editing this index file, avoiding merge contention on a\n-hand-maintained enumeration.\n+## How Decision Records Work\n \n-## Current ADRs (snapshot)\n-\n-The list below is a convenience snapshot and may lag behind the directory. When\n-in doubt, list `docs/adr/*.md`.\n+- New Shiki platform decisions use the next zero-padded SADR number.\n+- New target product decisions use the target's own ADR sequence.\n+- The canonical list is the files in `docs/adr/`; no hand-maintained index is\n+  authoritative.\n+- Use [`template.md`](template.md) as the starting point and choose the filename\n+  namespace that owns the decision.\n+- Record a decision when it is hard to reverse, surprising without context, and\n+  a real tradeoff. Do not write a decision record for routine or easily\n+  reversible choices.\n \n-- [0001 \u2014 Keep Shiki As The Platform Name](0001-keep-shiki-as-platform-name.md)\n-- [0002 \u2014 Use AGENTS As The Shared Agent Constitution](0002-use-agents-as-shared-constitution.md)\n-- [0003 \u2014 Require Skill Gate For MergeGate Readiness](0003-require-skill-gate-for-mergegate-readiness.md)\n-- [0004 \u2014 Use GitHub First With A Repository-Local Shiki Mirror](0004-use-github-first-with-shiki-mirror.md)\n-- [0005 \u2014 Use Codex As Front And Claude Code Action As The GitHub Runtime](0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md)\n-- [0006 \u2014 Separate Codex Implementation, CCA Completion, And MergeGate Authorization](0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md)\n+Using the directory as the source of truth lets independent changes add records\n+without contending on an enumerated index.\n \n-## Adding A New ADR\n+## Adding A Shiki SADR\n \n-1. Copy `template.md` to `docs/adr/NNNN-<short-slug>.md`.\n+1. Copy `template.md` to `docs/adr/SADR-NNNN-<short-slug>.md`.\n 2. Fill in the title, status, context, decision, and consequences.\n-3. Set the status to `Proposed`, then `Accepted` once the decision is approved.\n-4. Open a PR following `CONTRIBUTING.md`. You do not need to edit this index for\n-   the ADR to be discoverable via `docs/adr/*.md`.\n+3. Set the status to `Proposed`, then `Accepted` once the proper authority has\n+   approved the decision.\n+4. Open a PR following `CONTRIBUTING.md`.\n+\n+## Adding A Target ADR\n+\n+1. Copy `template.md` to `docs/adr/NNNN-<short-slug>.md` using the target's own\n+   sequence.\n+2. Cite it as `ADR NNNN`; do not prefix its filename with `SADR-`.\ndiff --git a/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md b/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\nnew file mode 100644\nindex 0000000000000000000000000000000000000000..7455ce1f3bed02ddddb851ac21a1d1a648c9aeba\n--- /dev/null\n+++ b/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\n@@ -0,0 +1,55 @@\n+# SADR-0019: Separate Shiki Platform SADRs From Target ADRs\n+\n+## Status\n+\n+Accepted\n+\n+## Context\n+\n+Shiki installs its platform decision records into `docs/adr/` in every target\n+repository. Shiki and its targets previously used the same numeric\n+`NNNN-<slug>.md` namespace. A target that already owned ADRs 0013 through 0018\n+therefore acquired two records for each number during installation, making an\n+`ADR NNNN` citation ambiguous. Renumbering target decisions would rewrite target\n+history, while renumbering Shiki decisions per target would destroy their stable\n+platform identity.\n+\n+Upgrades also need to remove the former Shiki-owned numeric paths without ever\n+mistaking a target-owned ADR for platform content. The install stamp already\n+records exact shipped paths and content digests, so it can prove ownership only\n+when both still match.\n+\n+## Decision\n+\n+Shiki platform decisions use the `SADR` namespace:\n+\n+- records are named `docs/adr/SADR-NNNN-<slug>.md`;\n+- live platform citations use `SADR-NNNN`;\n+- target product decisions retain `docs/adr/NNNN-<slug>.md` and `ADR NNNN`;\n+- the reference validator resolves the two namespaces independently, so the same\n+  four-digit number may exist once in each namespace.\n+\n+Before a forced upgrade writes anything, the installer inventories the exact\n+former numeric Shiki ADR paths. It may delete a legacy path only when the\n+existing install stamp contains that exact path and its recorded SHA-256 digest\n+matches the file currently on disk. All ownership blockers are reported\n+together and abort the upgrade before its first write. A non-forced install\n+never performs this cleanup.\n+\n+Historical `.shiki/` evidence remains unchanged because it records the namespace\n+that existed when that evidence was produced.\n+\n+## Consequences\n+\n+- Targets may use any ADR number without colliding with Shiki's platform SADRs.\n+- Fresh installs preserve target ADR bytes and install a stable SADR set.\n+- Forced upgrades fail closed when legacy ownership cannot be proven, and a\n+  successful upgrade refreshes the stamp without removed legacy paths.\n+- Current governance, packaging, workflow, prompt, script, and skill references\n+  must move with the platform records.\n+\n+## Alternatives Considered\n+\n+- Collision-only refusal leaves affected targets permanently unupgradable.\n+- Renumbering target ADRs mutates target-owned history.\n+- Per-target Shiki renumbering makes platform citations non-portable.\ndiff --git a/docs/adr/template.md b/docs/adr/template.md\nindex a92a97daddd0b00ab9dd626cc8d86942da2eafed..17dd79175b5fee2105935557f691b40cf9492942\n--- a/docs/adr/template.md\n+++ b/docs/adr/template.md\n@@ -1,14 +1,14 @@\n-# NNNN: <Short Decision Title>\n+# <SADR-NNNN or NNNN>: <Short Decision Title>\n \n ## Status\n \n-Proposed | Accepted | Superseded by ADR-NNNN\n+Proposed | Accepted | Superseded by SADR-NNNN or target ADR NNNN\n \n ## Context\n \n What is the situation, constraint, or force that requires a decision? Reference\n-relevant domain terms (`CONTEXT.md`), prior ADRs, and code reality. State the\n-problem, not the solution.\n+relevant domain terms (`CONTEXT.md`), prior Shiki SADRs or target ADRs, and code\n+reality. State the problem, not the solution.\n \n ## Decision\n \ndiff --git a/docs/agents/autonomous-loop-e2e-acceptance.md b/docs/agents/autonomous-loop-e2e-acceptance.md\nindex bbf84c6081987f159ac6ad9ecf3c82b7aa72e7e8..5c13c1d02a05a90b0dc46cbef7c95a9846d0c237\n--- a/docs/agents/autonomous-loop-e2e-acceptance.md\n+++ b/docs/agents/autonomous-loop-e2e-acceptance.md\n@@ -40,8 +40,8 @@\n 3. `gh auth status` is logged in with merge permission on the repo.\n 4. Branch protection + required checks are configured exactly as the goal-loop\n    required-check set expects (see `docs/agents/branch-protection-smoke.md`).\n-5. The repo `.shiki/policy` permits autonomous low/medium-risk merges (ADR\n-   0008/0009); the MergeGate policy check is the recorded-authority gate.\n+5. The repo `.shiki/policy` permits autonomous low/medium-risk merges\n+   (SADR-0008/0009); the MergeGate policy check is the recorded-authority gate.\n \n The operator's only actions are: register the task and start the loop. **No\n manual pre-clearing** means after `shiki loop run` starts, the operator does not\ndiff --git a/docs/agents/bootstrap-command.md b/docs/agents/bootstrap-command.md\nindex 05f7f203a52cf412b9f97084a6e131ed0f785afd..f7aa0b734dfebee4a3b139335b66ee3415f48713\n--- a/docs/agents/bootstrap-command.md\n+++ b/docs/agents/bootstrap-command.md\n@@ -189,7 +189,9 @@\n ```\n \n Use this only for tests, fixtures, or explicit local-only template inspection.\n-Use `--force` only when you intentionally want to overwrite existing target files.\n+Use `--force` only when you intentionally want to refresh the target template.\n+Legacy numeric Shiki decision cleanup is stamp-and-digest authorized before any\n+write; target ADR paths are not cleanup candidates.\n \n ## Slash Command\n \ndiff --git a/docs/agents/checklists.md b/docs/agents/checklists.md\nindex 4a7f03ade346f53a1c1d82981f3fd744f9ab4547..0c5ec6c53dc7197b7b7a7d18e667b325babd6c45\n--- a/docs/agents/checklists.md\n+++ b/docs/agents/checklists.md\n@@ -32,18 +32,18 @@\n | ID | Item | Blocking |\n | --- | --- | --- |\n | GD-01 | Plan was challenged against `CONTEXT.md`. | blocking for non-trivial Goals |\n-| GD-02 | Relevant ADRs were read or absence was recorded. | blocking for non-trivial Goals |\n+| GD-02 | Relevant SADRs and target ADRs were read or absence was recorded. | blocking for non-trivial Goals |\n | GD-03 | Ambiguous terms were resolved or converted to HITL blockers. | blocking |\n | GD-04 | Edge scenarios were considered. | advisory |\n | GD-05 | Code reality was checked when code could answer. | blocking when applicable |\n | GD-06 | New domain terms were added to `CONTEXT.md` when needed. | blocking when applicable |\n-| GD-07 | ADR was created or explicitly skipped using the ADR criteria. | blocking when applicable |\n+| GD-07 | A SADR or target ADR was created or explicitly skipped using the decision-record criteria. | blocking when applicable |\n \n ## CI \u2014 Context & Impact Checklist\n \n | ID | Item | Blocking |\n | --- | --- | --- |\n-| CI-01 | Relevant docs and ADRs identified. | blocking |\n+| CI-01 | Relevant docs, SADRs, and target ADRs identified. | blocking |\n | CI-02 | Relevant modules, interfaces, seams, callers, and tests identified. | blocking |\n | CI-03 | Dependencies and blocked-by relationships identified. | blocking |\n | CI-04 | Candidate locks identified. | blocking |\n@@ -62,7 +62,7 @@\n | PRD-04 | Implementation decisions are recorded without volatile code detail. | blocking |\n | PRD-05 | Testing decisions are recorded. | blocking |\n | PRD-06 | Out-of-scope items are explicit. | blocking |\n-| PRD-07 | Relevant glossary/ADR links are present when applicable. | blocking |\n+| PRD-07 | Relevant glossary/SADR/target-ADR links are present when applicable. | blocking |\n \n ## SF \u2014 Spec Freeze Checklist\n \n@@ -98,7 +98,7 @@\n | H-02 | Branch/worktree target is present. | blocking |\n | H-03 | Scope and non-goals are present. | blocking |\n | H-04 | Dependencies and locks are present. | blocking |\n-| H-05 | Relevant docs, ADRs, modules, and tests are referenced. | blocking |\n+| H-05 | Relevant docs, SADRs, target ADRs, modules, and tests are referenced. | blocking |\n | H-06 | Required skills are stated. | blocking |\n | H-07 | TDD expectations are stated. | blocking when behavior work |\n | H-08 | Acceptance criteria are included. | blocking |\ndiff --git a/docs/agents/codex-handoff.md b/docs/agents/codex-handoff.md\nindex ede468f47cb277f69a857e8c45df703e4ed0151d..6f97bdffb67e44d7edea2c71e01ea28933c4eb6c\n--- a/docs/agents/codex-handoff.md\n+++ b/docs/agents/codex-handoff.md\n@@ -46,7 +46,7 @@\n \n ## Context\n - Domain terms:\n-- Relevant ADRs:\n+- Relevant SADRs and target ADRs:\n - Relevant docs:\n - Relevant modules/interfaces/seams:\n - Similar tests/prior art:\ndiff --git a/docs/agents/completion-check-agent.md b/docs/agents/completion-check-agent.md\nindex 13e4c035db35025f4e3cdcc5adfb24314ac4d521..61bfa1218cf36ca605250c91919e620f33238aa0\n--- a/docs/agents/completion-check-agent.md\n+++ b/docs/agents/completion-check-agent.md\n@@ -32,7 +32,7 @@\n - `AGENTS.md`\n - `CLAUDE.md`\n - `CONTEXT.md`\n-- relevant ADRs\n+- relevant SADRs and target ADRs\n - parent Goal issue\n - PRD issue or document\n - task issue\ndiff --git a/docs/agents/decision-control.md b/docs/agents/decision-control.md\nindex 7d8ee5ab03a5ccbcbd04056ae61de03785a2a9c9..8c94ecda00fffaa0e92c325ab83aa9776bd1dac1\n--- a/docs/agents/decision-control.md\n+++ b/docs/agents/decision-control.md\n@@ -11,7 +11,7 @@\n | Decision | Owner | When | Required inputs | Output | Enforcement surface |\n | --- | --- | --- | --- | --- | --- |\n | Is the Goal clear enough to plan? | Planner + Guardian when needed | Goal intake | User request, existing docs, risk signals | Goal readiness verdict | Goal checklist, issue template, ledger |\n-| Must `grill-with-docs` run? | Planner / Skill Gate | Before PRD or issue split | Ambiguity, domain terms, ADR conflicts, unclear boundaries | Skill invocation or explicit skip rationale | Skill Gate checklist |\n+| Must `grill-with-docs` run? | Planner / Skill Gate | Before PRD or issue split | Ambiguity, domain terms, SADR/target-ADR conflicts, unclear boundaries | Skill invocation or explicit skip rationale | Skill Gate checklist |\n | Is a PRD required? | Planner | After Goal context stabilizes | Goal, `grill-with-docs` output, Context & Impact output | PRD required / not required | PRD checklist, CCA evidence check |\n | Can the spec be frozen? | Operator only | End of Requirements Definition, before issue split | PRD/requirements summary, scope inventory (SF-02), open-question list | `spec_freeze` block in the plan + ledger evidence | `require_grilled_plan` (plan ingest/run), SF checklist |\n | Can a frozen spec change? | Operator only (Spec Amendment) | When implementation contradicts the frozen spec | Contested decisions, scoped re-grill result | recorded amendment + re-stamped freeze | SF checklist, ledger evidence |\ndiff --git a/docs/agents/domain.md b/docs/agents/domain.md\nindex 258da8ea81e87ac0a6b0b5b5e5c2fde980eaabb7..d57b0cf17ff3e1fa9e6e5885372bf083d8295421\n--- a/docs/agents/domain.md\n+++ b/docs/agents/domain.md\n@@ -5,7 +5,9 @@\n ## Layout\n \n - `CONTEXT.md` defines Shiki language. Keep it implementation-free.\n-- `docs/adr/` records hard-to-reverse decisions.\n+- `docs/adr/SADR-NNNN-*.md` records Shiki platform decisions.\n+- `docs/adr/NNNN-*.md` records target product decisions without sharing the\n+  SADR namespace.\n - `AGENTS.md` is the runtime-neutral agent constitution.\n - `CLAUDE.md` is the Claude Code wrapper around `AGENTS.md`.\n - `.shiki/` is the Target Repository mirror for Goals, PRDs, plans, Task DAGs, contracts, locks, worktree records, ledger entries, CCA verdicts, repair packets, reports, and handoffs.\n@@ -15,6 +17,6 @@\n \n - Use Shiki terms exactly when they exist.\n - Add glossary terms to `CONTEXT.md` only when the term is part of the domain, not a generic programming concept.\n-- Create ADRs only for hard-to-reverse, surprising, tradeoff-based decisions.\n+- Create SADRs or target ADRs only for hard-to-reverse, surprising, tradeoff-based decisions.\n - Keep product-specific language in the Target Repository, not in Shiki Template docs.\n-- If an ADR conflicts with a proposed implementation, surface the conflict before editing.\n+- If a SADR or target ADR conflicts with a proposed implementation, surface the conflict before editing.\ndiff --git a/docs/agents/execution-control.md b/docs/agents/execution-control.md\nindex ad8ee3cdb36be56fedc1f46408ca92b387d74047..bc63c3a73661d12b567016ba299f1a6b66bf9088\n--- a/docs/agents/execution-control.md\n+++ b/docs/agents/execution-control.md\n@@ -63,7 +63,7 @@\n | Branch name | Orchestrator | Orchestrator | Before implementer dispatch | branch policy | branch ref in task/ledger |\n | Lock reservation | Context & Impact | MergeGate | Before dispatch and before merge | `.shiki/locks/`, labels, PR metadata | lock id, owner, scope |\n | CI profile | Planner | MergeGate | During issue triage, rechecked at PR | labels, `.shiki/policy.yaml`, workflow matrix | required check list |\n-| Required status checks | Repo maintainer / Guardian | GitHub branch protection / rulesets | Repository setup, updated via ADR | GitHub settings | branch/ruleset config, ADR |\n+| Required status checks | Repo maintainer / Guardian | GitHub branch protection / rulesets | Repository setup, updated via target ADR | GitHub settings | branch/ruleset config, target ADR |\n | CCA judgment | CCA | GitHub required check / MergeGate | Every non-draft PR update | GitHub Action check | structured CCA verdict |\n | Repair assignment | CCA | MergeGate | After failed CCA/CI/review | repair packet, issue/PR comment | repair id and attempt count |\n | Merge readiness | MergeGate | GitHub branch protection / Guardian if needed | After CCA complete | required checks, review rules, rulesets | mergegate check output |\ndiff --git a/docs/agents/guardian-policy.md b/docs/agents/guardian-policy.md\nindex 4147c7adf05a47adb4fd24ada1a1c2e1f2ba6d2d..a20d9cc6c00a569bb057b9ca8d6b51c7fc4a9847\n--- a/docs/agents/guardian-policy.md\n+++ b/docs/agents/guardian-policy.md\n@@ -98,8 +98,8 @@\n `origin/<default_branch>` and reuses each job's existing base fetch; a shallow\n (`--depth`) or second base fetch would make the proof unresolvable and silently\n disable the carry, so neither is permitted. CCA reads `guardian_comment_carried` as\n-a recorded authority in the deterministic signal and does not re-derive it. See ADR\n-0018 for the full decision, the rejected diff/patch-equality and path-disjointness\n+a recorded authority in the deterministic signal and does not re-derive it. See\n+SADR-0018 for the full decision, the rejected diff/patch-equality and path-disjointness\n alternatives, and the accepted residual.\n \n ## External AI Guardian Review (SADR-0010)\ndiff --git a/docs/agents/implementation-policy.md b/docs/agents/implementation-policy.md\nindex c754144a9fb5bf4a38aa82a60e1670c59e626b44..85c37281bcad196c6b7b02343faf00b03cd9c57a\n--- a/docs/agents/implementation-policy.md\n+++ b/docs/agents/implementation-policy.md\n@@ -31,10 +31,13 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n ## Non-Negotiables\n \n 1. **Planning precedes implementation.** Non-trivial Goals start with `grill-with-docs` and Context & Impact.\n-2. **Domain language is controlled.** Terms must be checked against `CONTEXT.md`; hard-to-reverse tradeoffs must be checked against ADRs.\n+2. **Domain language is controlled.** Terms must be checked against `CONTEXT.md`; hard-to-reverse tradeoffs must be checked against Shiki SADRs and target ADRs.\n 3. **PRD precedes issue decomposition.** `to-prd` is used once the context is settled enough to become durable intent.\n 4. **Issues are vertical slices.** `to-issues` must produce independently grabbable tracer-bullet tasks, not horizontal layer tickets.\n 5. **Triage controls dispatch.** Only `ready-for-agent` / AFK tasks may be assigned to Codex without human input.\n@@ -95,19 +98,19 @@\n Process:\n \n - Challenge terms against `CONTEXT.md`.\n-- Check ADRs before reopening decisions.\n+- Check SADRs and target ADRs before reopening decisions.\n - Inspect code when code can answer a question.\n - Ask one question at a time when the operator is available.\n - Provide a recommended answer with each question.\n - Record resolved terms in `CONTEXT.md`.\n-- Create ADRs only for hard-to-reverse, surprising, tradeoff-based decisions.\n+- Create SADRs or target ADRs only for hard-to-reverse, surprising, tradeoff-based decisions.\n - Convert unresolved load-bearing questions into HITL blockers.\n \n Outputs:\n \n - Resolved design-tree decisions.\n - Updated glossary terms when needed.\n-- ADRs when justified.\n+- SADRs or target ADRs when justified.\n - HITL blockers when decisions remain unresolved.\n \n ## Phase 3 \u2014 Context & Impact\n@@ -117,7 +120,7 @@\n \n Outputs must include:\n \n-- Relevant documents and ADRs.\n+- Relevant documents, SADRs, and target ADRs.\n - Relevant modules, interfaces, seams, callers, and tests.\n - Candidate locks.\n - Dependencies.\n@@ -140,7 +143,7 @@\n - Testing decisions.\n - Out of scope.\n - Further notes.\n-- Links to relevant Goal, domain terms, and ADRs.\n+- Links to relevant Goal, domain terms, SADRs, and target ADRs.\n \n Phase 4 ends with Spec Freeze: the operator explicitly approves the PRD, the\n plan records a `spec_freeze` block (status, approved_by, source), and the SF\n@@ -240,7 +243,7 @@\n - Dependencies and locks.\n - Risk and Guardian requirements.\n - Ledger completeness.\n-- Documentation and ADR updates.\n+- Documentation and SADR/target-ADR updates.\n - Scope drift.\n \n CCA emits one of:\n@@ -301,7 +304,7 @@\n | Evidence failure | Behavior might be done, but proof is missing. | Add checks, PR evidence, ledger entry, or artifacts. |\n | Scope drift | PR changes unrelated behavior. | Revert unrelated changes or split into new Goal. |\n | Dependency block | Upstream task or lock not complete. | Block until dependency resolves. |\n-| Architecture block | Decision needs ADR or deep-module discussion. | `grill-with-docs` or `improve-codebase-architecture`. |\n+| Architecture block | Decision needs a SADR, target ADR, or deep-module discussion. | `grill-with-docs` or `improve-codebase-architecture`. |\n | Risk block | Security/production/policy change needs human approval. | Guardian decision. |\n \n ## Phase 5.5 \u2014 Execution Decision Control\ndiff --git a/docs/agents/runtime-auth-model.md b/docs/agents/runtime-auth-model.md\nindex 6a2e1a47e81ea89b5c68a210ec07772a9374babc..3c54748d24233dbd0ba8473950282547ef4ce935\n--- a/docs/agents/runtime-auth-model.md\n+++ b/docs/agents/runtime-auth-model.md\n@@ -148,7 +148,7 @@\n \n API-key based runners may be added by a target repository as an explicit extension. They are not the default Shiki template path.\n \n-If a repo opts into API-key mode, record the exception in an ADR and update the repo's `AGENTS.md`, `.shiki/config.yaml`, and workflow permissions.\n+If a repo opts into API-key mode, record the target-specific exception in a target ADR and update the repo's `AGENTS.md`, `.shiki/config.yaml`, and workflow permissions.\n \n ## GitHub Actions Runtime Compatibility\n \ndiff --git a/docs/agents/skill-gate.md b/docs/agents/skill-gate.md\nindex c50398b0d7c60a8598f2a5c88a10015730dced13..3acffe6444617bb1d54b77d4b4708158ec735bc6\n--- a/docs/agents/skill-gate.md\n+++ b/docs/agents/skill-gate.md\n@@ -9,7 +9,7 @@\n | Skill | Trigger | Expected output |\n | --- | --- | --- |\n | `setup-matt-pocock-skills` | First configuring a repo for these skills, or when issue tracker, triage labels, or domain docs are missing. | `docs/agents/` configuration and an `Agent skills` block in `AGENTS.md` or `CLAUDE.md`. |\n-| `grill-with-docs` | Ambiguous plan, unclear terminology, boundary decision, tradeoff, or ADR-worthy design choice. | Clarified decision tree, recommended answers, updates to `CONTEXT.md` or ADRs when decisions crystallize. |\n+| `grill-with-docs` | Ambiguous plan, unclear terminology, boundary decision, tradeoff, or decision-record-worthy design choice. | Clarified decision tree, recommended answers, updates to `CONTEXT.md`, SADRs, or target ADRs when decisions crystallize. |\n | `zoom-out` | Agent lacks a higher-level architectural map. | Map of relevant modules, callers, seams, and domain concepts. |\n | `to-prd` | Settled context should become a PRD. | PRD published to the issue tracker with problem, solution, user stories, decisions, testing, and out-of-scope notes. |\n | `to-issues` | Goal, PRD, or plan must become implementation tickets. | Vertical-slice issues with acceptance criteria, dependencies, and AFK/HITL classification. |\n@@ -17,7 +17,7 @@\n | `tdd` | Feature work or bug fix has observable behavior that can be specified with tests. | Red-green-refactor loop through public interfaces, one vertical slice at a time. |\n | `code-review` | The TDD loop is green and the implementer is about to create or update a PR. Default for every implementation task. | Pre-PR self-review of the task diff with fixes applied on the task branch; findings and resolutions recorded as a ledger entry naming the skill and a `## Pre-PR code review` PR body section. |\n | `diagnose` | Hard bug, failing check, regression, flaky behavior, or performance problem. | Reproduction loop, ranked hypotheses, instrumentation, fix, regression test, post-mortem. |\n-| `improve-codebase-architecture` | Structural friction, testability, AI-navigability, deep-module opportunity, or refactor request. | Architecture candidates and deepening recommendations informed by `CONTEXT.md` and ADRs. |\n+| `improve-codebase-architecture` | Structural friction, testability, AI-navigability, deep-module opportunity, or refactor request. | Architecture candidates and deepening recommendations informed by `CONTEXT.md`, SADRs, and target ADRs. |\n | `prototype` | A throwaway logic or UI prototype is needed to answer a design question. | Clearly marked disposable prototype and captured decision. |\n \n ## MergeGate Interaction\ndiff --git a/docs/operations.md b/docs/operations.md\nindex cf1f452d4c589e38ccd58553bda0ad8684b51eb7..ace48a90ffbec9e5c0a7113bef16a4858bc44b3f\n--- a/docs/operations.md\n+++ b/docs/operations.md\n@@ -89,6 +89,16 @@\n      target history, brought forward by `shiki migrate`, not a template.\n    - **Everything else** (CLI scripts, workflows, schemas, docs) is overwritten.\n \n+   A forced upgrade also migrates the former numeric Shiki decision paths to\n+   `SADR-NNNN-*`. Before the first write, the installer inventories the exact\n+   legacy Shiki paths and authorizes each deletion only when the existing\n+   `.shiki/install-stamp.json` names that path and its SHA-256 digest still\n+   matches the file. An absent, unreadable, malformed, incomplete, or\n+   mismatching stamp reports every blocked path and stops with no filesystem\n+   change. Target-owned `NNNN-*` ADRs are not cleanup candidates. A non-force\n+   install deletes nothing and retains any still-matching legacy stamp entries\n+   so a later forced upgrade can prove ownership.\n+\n    If the target has pending migrations, `--force` refuses and writes nothing \u2014\n    apply migrations first (step 3), then re-run. The run ends with a single\n    summary of every `.new` written, so a half-upgrade is visible.\ndiff --git a/docs/releasing.md b/docs/releasing.md\nindex 10d73828e7dbd64e9d9f6b83bc84d010537ce62a..aafeee8c4d441e1e427efee2ce4f81a647f020e1\n--- a/docs/releasing.md\n+++ b/docs/releasing.md\n@@ -67,7 +67,7 @@\n CLI in `scripts/`. There are no third-party Python runtime dependencies. The CLI\n is intentionally **not** exposed as a `[project.scripts]` console entry point\n because `scripts/shiki.py` is a dependency-free shim and `scripts/` is not\n-packaged as an importable distribution (see the ADR for the rationale).\n+packaged as an importable distribution (see SADR-0007 for the rationale).\n \n Supported install paths:\n \n@@ -155,6 +155,15 @@\n    [docs/operations.md](operations.md#the-new-convention). If the target has\n    pending migrations, `--force` refuses and writes nothing; apply migrations\n    (step 2) first, then re-run.\n+\n+   The SADR namespace upgrade has an additional pre-write gate: each former\n+   numeric Shiki decision path is removed only when the existing install stamp\n+   contains that exact path and a digest matching its current bytes. The\n+   installer aggregates absent/unreadable/malformed-stamp, missing-digest, and\n+   digest-mismatch blockers before raising, so refusal leaves the target tree\n+   byte-identical. Numeric target ADRs are preserved. A successful forced\n+   upgrade installs and stamps `SADR-*` paths and drops the removed legacy paths\n+   from the refreshed stamp.\n 2. Apply repository-local `.shiki/` state migrations with the migrate command:\n \n    ```bash\ndiff --git a/docs/usage.md b/docs/usage.md\nindex b83407ee3970dd395dd893010bc5dbe8bad26051..a130ef6e40dfd8169a5bc701339351097df1a09f\n--- a/docs/usage.md\n+++ b/docs/usage.md\n@@ -67,8 +67,10 @@\n shiki install-target /path/to/target-repo --local-only\n ```\n \n-Use `--force` only when you intentionally want to overwrite existing target\n-files.\n+Use `--force` only when you intentionally want to refresh the target template.\n+For the SADR namespace migration, `--force` deletes only exact legacy Shiki\n+decision paths whose existing install-stamp digest still matches; any ownership\n+blocker stops before the first write. Numeric target ADRs are never candidates.\n \n ## `shiki bootstrap-platform` \u2014 publish the Shiki platform repo\n \ndiff --git a/scripts/guardian_approval_signal.py b/scripts/guardian_approval_signal.py\nindex d059f92b2c82aa2b5d6ca421365537a87886c1cb..16924892714f4f5b9303c0ef0e325a7e70075630\n--- a/scripts/guardian_approval_signal.py\n+++ b/scripts/guardian_approval_signal.py\n@@ -199,8 +199,8 @@\n ) -> Any:\n     \"\"\"Evaluate SADR-0015 Contract Approval for the CCA Guardian signal.\n \n-    The SAME OR that MergeGate applies must reach this deterministic signal: ADR\n-    0015 is explicit that without it, a contract-approved PR still returns\n+    The SAME OR that MergeGate applies must reach this deterministic signal:\n+    SADR-0015 is explicit that without it, a contract-approved PR still returns\n     ``needs_guardian``, the CCA job fails, and the MergeGate policy check never\n     runs \u2014 the gate this change moves would never be reached. This resolves the\n     task the SAME way MergeGate does (the first Shiki task id in the PR body),\ndiff --git a/scripts/shiki_contract_approval.py b/scripts/shiki_contract_approval.py\nindex 941bcb96fdd8b9a85bc0c4f0f04d5dad9d04839c..4a4b418f6527413161db82670bec857a043360e2\n--- a/scripts/shiki_contract_approval.py\n+++ b/scripts/shiki_contract_approval.py\n@@ -142,7 +142,7 @@\n     changed_files_status: Any,\n     registration: Any,\n ) -> ContractApprovalResult:\n-    \"\"\"Whether this PR carries an SADR-0015 Contract Approval.\n+    \"\"\"Whether this PR carries a SADR-0015 Contract Approval.\n \n     The four conditions are evaluated IN ORDER, each failure recorded as a\n     distinct reason string:\ndiff --git a/scripts/shiki_guardian.py b/scripts/shiki_guardian.py\nindex a4ecb79bbda55fd5c8b06adfae2eb99760a03c45..4dd99bbe714e26f421fa19abac7a34ae1b195ecc\n--- a/scripts/shiki_guardian.py\n+++ b/scripts/shiki_guardian.py\n@@ -12,7 +12,7 @@\n # The carry re-derives its proof from git in-process on every judging invocation;\n # it is never read from an artifact, so there is nothing to go stale or be forged.\n # shiki_sync_proof is the ONLY guardian-family module that runs git, and every one\n-# of its invocations is read-only and local (ADR: prove-from-git base sync).\n+# of its invocations is read-only and local (SADR: prove-from-git base sync).\n from shiki_sync_proof import verify_pure_base_sync\n \n GUARDIAN_POLICY_PATH = \".shiki/guardian-policy.json\"\ndiff --git a/scripts/shiki_guardian_review.py b/scripts/shiki_guardian_review.py\nindex dd0f104d457899e0ba5ff1e162011c1e90b6f92b..e10ef58c046324e7ea3a3392feb3048ca77414ce\n--- a/scripts/shiki_guardian_review.py\n+++ b/scripts/shiki_guardian_review.py\n@@ -159,7 +159,7 @@\n     docs_only = _is_docs_only(changed_files)\n     if docs_only and matched == 0:\n         focus.append(\n-            \"Docs/ADR-only change: confirm the change is genuinely documentation/decision-only, introduces no behavior or contract drift, and stays consistent with the constitution and existing ADRs.\"\n+            \"Docs/decision-record-only change: confirm the change is genuinely documentation/decision-only, introduces no behavior or contract drift, and stays consistent with the constitution and existing SADRs and target ADRs.\"\n         )\n \n     # Conservative augmentation: unknown (no category and not docs-only), mixed\ndiff --git a/scripts/shiki_installer.py b/scripts/shiki_installer.py\nindex be064ab7a29ff6836f7f7a1d70500a43e0d11d7b..9fd8eefd137ce7003dad5c0dcb2e48e2cd47ab32\n--- a/scripts/shiki_installer.py\n+++ b/scripts/shiki_installer.py\n@@ -118,6 +118,7 @@\n # what lets a target carry it without a governance-schema change.\n INSTALL_STAMP_PATH = \".shiki/install-stamp.json\"\n INSTALL_STAMP_VERSION = 1\n+LEGACY_SHIKI_SADR_MAX = 18\n \n # Under --force the shipped surface splits three ways, so nothing is lost and\n # nothing is silently stale.\n@@ -237,9 +238,98 @@\n         )\n \n \n+def legacy_shiki_adr_paths() -> tuple[str, ...]:\n+    \"\"\"Former numeric paths for Shiki SADR-0001 through SADR-0018.\n+\n+    The authoritative slugs come from the current SADR filenames. SADR-0019 is\n+    the namespace decision itself and never shipped under a numeric legacy path.\n+    \"\"\"\n+    paths: list[str] = []\n+    for sadr in sorted((ROOT / \"docs\" / \"adr\").glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\")):\n+        number = sadr.name.split(\"-\", 2)[1]\n+        if int(number) <= LEGACY_SHIKI_SADR_MAX:\n+            paths.append(f\"docs/adr/{sadr.name.removeprefix('SADR-')}\")\n+    return tuple(paths)\n+\n+\n+def preflight_legacy_sadr_cleanup(target: Path, *, force: bool) -> tuple[str, ...]:\n+    \"\"\"Authorize exact legacy Shiki ADR removals before the first write.\n+\n+    A non-force install never removes existing files. A forced install needs no\n+    stamp when no legacy Shiki path is present. When legacy paths do exist, each\n+    must be named in a readable install stamp and its current bytes must match\n+    the recorded digest.\n+    \"\"\"\n+    if not force:\n+        return ()\n+    present = tuple(\n+        relative\n+        for relative in legacy_shiki_adr_paths()\n+        if (target / relative).exists() or (target / relative).is_symlink()\n+    )\n+    if not present:\n+        return ()\n+\n+    stamp_path = target / INSTALL_STAMP_PATH\n+    try:\n+        raw = stamp_path.read_text(encoding=\"utf-8\")\n+        stamp = json.loads(raw)\n+    except FileNotFoundError:\n+        detail = \"install stamp is absent\"\n+        blockers = [f\"{relative}: {detail}\" for relative in present]\n+    except (OSError, UnicodeDecodeError) as error:\n+        detail = f\"install stamp is unreadable ({error})\"\n+        blockers = [f\"{relative}: {detail}\" for relative in present]\n+    except json.JSONDecodeError as error:\n+        detail = f\"install stamp is malformed ({error})\"\n+        blockers = [f\"{relative}: {detail}\" for relative in present]\n+    else:\n+        if not isinstance(stamp, dict):\n+            blockers = [\n+                f\"{relative}: install stamp is malformed (expected an object)\"\n+                for relative in present\n+            ]\n+        else:\n+            digests = stamp.get(\"digests\")\n+            blockers = []\n+            if not isinstance(digests, dict):\n+                blockers.extend(\n+                    f\"{relative}: install stamp has no digest for this path\"\n+                    for relative in present\n+                )\n+            else:\n+                for relative in present:\n+                    expected = digests.get(relative)\n+                    if not isinstance(expected, str) or not expected:\n+                        blockers.append(\n+                            f\"{relative}: install stamp has no digest for this path\"\n+                        )\n+                        continue\n+                    try:\n+                        actual = _sha256_file(target / relative)\n+                    except OSError as error:\n+                        blockers.append(\n+                            f\"{relative}: legacy path is unreadable ({error})\"\n+                        )\n+                        continue\n+                    if actual != expected:\n+                        blockers.append(\n+                            f\"{relative}: digest mismatch (stamp {expected}, current {actual})\"\n+                        )\n+\n+    if blockers:\n+        joined = \"\\n\".join(f\"- {blocker}\" for blocker in blockers)\n+        raise ShikiError(\n+            \"legacy Shiki ADR cleanup is not authorized:\\n\"\n+            f\"{joined}\\nNo files were written.\"\n+        )\n+    return present\n+\n+\n def install_template(target: Path, *, force: bool, validate: bool) -> list[NewFileNote]:\n     # Preconditions first: never rewrite the tree of a target that cannot be\n     # upgraded cleanly (see refuse_pending_migrations).\n+    legacy_cleanup = preflight_legacy_sadr_cleanup(target, force=force)\n     refuse_pending_migrations(target)\n \n     # Resolve the target's required CODEOWNERS owner once from its\n@@ -250,6 +340,10 @@\n     # verbatim. See ``codeowners_required_owner``.\n     codeowners_owner = codeowners_required_owner(target)\n \n+    for relative in legacy_cleanup:\n+        (target / relative).unlink()\n+        info(f\"removed stamped legacy Shiki ADR: {target / relative}\")\n+\n     notes: list[NewFileNote] = []\n     for relative in TEMPLATE_PATHS:\n         source = ROOT / relative\n@@ -647,6 +741,22 @@\n     digests = target_stamp_digests(target)\n     existing = read_install_stamp(target)\n     if existing is not None:\n+        # A non-force install deliberately leaves legacy Shiki ADRs in place.\n+        # Preserve only still-matching ownership proof so a later forced\n+        # upgrade can authorize cleanup; never carry a missing or stale digest.\n+        existing_digests = existing.get(\"digests\")\n+        if isinstance(existing_digests, dict):\n+            for relative in legacy_shiki_adr_paths():\n+                expected = existing_digests.get(relative)\n+                path = target / relative\n+                if not isinstance(expected, str) or not path.is_file():\n+                    continue\n+                try:\n+                    actual = _sha256_file(path)\n+                except OSError:\n+                    continue\n+                if actual == expected:\n+                    digests[relative] = actual\n         if _incoming_is_older(commit, existing.get(\"platform_commit\")):\n             warn(\n                 \"kept existing install stamp: incoming install \"\ndiff --git a/scripts/shiki_loop.py b/scripts/shiki_loop.py\nindex 3611641da8aeb5038b1da90e3114b09a473ec6d8..84caa491b9cad0edc4176f209c13e9221e975681\n--- a/scripts/shiki_loop.py\n+++ b/scripts/shiki_loop.py\n@@ -424,7 +424,7 @@\n         # unresolvable CCA verdict has already produced its terminal stop and is never\n         # laundered into a head-moving sync (SADR-0010). It also never fires while the\n         # policy (Guardian) gate is red \u2014 for ANY reason, not only when it is the sole\n-        # red check \u2014 so a head-bound Guardian approval, or an SADR-0015\n+        # red check \u2014 so a head-bound Guardian approval, or a SADR-0015\n         # contract-approval carry that base movement defeated (the policy gate compares\n         # base and head task governance directly), is never silently restored by\n         # adopting base wholesale. It is bounded by a durable per-task counter so the\ndiff --git a/scripts/shiki_tasks.py b/scripts/shiki_tasks.py\nindex aec3809f24b4a0c8c96fc50971cff9ae9ad30381..d531052e9d9717058d4631483cefab44c26a65c1\n--- a/scripts/shiki_tasks.py\n+++ b/scripts/shiki_tasks.py\n@@ -821,7 +821,7 @@\n         \"prompt\": prompt,\n         \"entry_skill\": \"grill-with-docs\",\n         \"required_next_steps\": [\n-            \"Run grill-with-docs until terminology, boundaries, risks, and ADR-worthy decisions are settled.\",\n+            \"Run grill-with-docs until terminology, boundaries, risks, and decision-record-worthy choices are settled.\",\n             \"Obtain the operator's explicit approval of the PRD/requirements (Spec Freeze).\",\n             \"Write a machine-readable plan JSON with grill_with_docs.status=complete and spec_freeze.status=frozen.\",\n             \"Run shiki plan ingest --plan-file PLAN.json.\",\ndiff --git a/scripts/test_shiki_goal_loop.sh b/scripts/test_shiki_goal_loop.sh\nindex 753c7b516e10f0be3bc92a4bd0eb1fa699776ae6..8f3820a0a079b3044424cfe1a6c6b7fe68896e67\n--- a/scripts/test_shiki_goal_loop.sh\n+++ b/scripts/test_shiki_goal_loop.sh\n@@ -231,8 +231,8 @@\n \n # High/critical risk merges autonomously when all required checks are green:\n # the \"MergeGate policy check\" required check IS the Guardian gate (it enforces\n-# guardian-policy.json \u2014 human approval OR external AI guardian review, ADR\n-# 0010), so green checks mean a recorded authority approved. The fake gh returns\n+# guardian-policy.json \u2014 human approval OR external AI guardian review,\n+# SADR-0010), so green checks mean a recorded authority approved. The fake gh returns\n # green checks, simulating that the Guardian gate passed.\n python3 \"$ROOT/scripts/shiki.py\" goal create --target \"$TARGET\" --title \"Guardian gate\" --outcome \"High risk merges via policy gate\" >/tmp/shiki-goal-loop-guardian-goal.json\n GGOAL=\"$(json_get /tmp/shiki-goal-loop-guardian-goal.json goal_id)\"\ndiff --git a/tests/test_adr_references.py b/tests/test_adr_references.py\nindex 51706329c797d5dd9b47e082a333fa83b261d482..d005da4f771f5e8085ec22d4b787980e68f5ae8c\n--- a/tests/test_adr_references.py\n+++ b/tests/test_adr_references.py\n@@ -1,8 +1,8 @@\n-\"\"\"Every ``ADR ####`` citation must resolve to exactly one decision record.\n+\"\"\"Every target ADR and Shiki SADR citation resolves in its own namespace.\n \n Background. ``scripts/mergegate_check.py`` justifies ``is_bookkeeping_closeout``\n-with \"ADR 0017\" nine times and ``scripts/guardian_approval_signal.py`` cites\n-0016/0017 seven times, yet for a stretch the numbering in ``docs/adr/`` stopped\n+with \"SADR-0017\" nine times and ``scripts/guardian_approval_signal.py`` cites\n+SADR-0016/SADR-0017 seven times, yet for a stretch the numbering in ``docs/adr/`` stopped\n at 0015: the authoring commits lived on a coordinator branch and no task ever\n declared ``docs/adr/...`` in its locks, so nothing carried the records to the\n default branch. The stated basis of an exemption that merged seven PRs with zero\n@@ -11,11 +11,12 @@\n and the directory drifted with no signal.\n \n This is the same unbound-pair shape behind other silent failures: a reference\n-and its target with nothing asserting they agree. This test binds them. It scans\n-every ``scripts/*.py`` (plus ``docs/**/*.md`` and the root ``*.md`` governance\n-files, which are cheap) for the ``ADR ####`` pattern and asserts each cited\n-number resolves to exactly one ``docs/adr/####-*.md``. The converse is\n-deliberately NOT required: an ADR may exist without being cited.\n+and its target with nothing asserting they agree. This test binds them. Target\n+``ADR NNNN`` citations resolve only to ``docs/adr/NNNN-*.md``; Shiki\n+``SADR-NNNN`` citations resolve only to ``docs/adr/SADR-NNNN-*.md``. The scan\n+covers current governance docs, scripts, workflows, prompts, packaging metadata,\n+and repo-local Codex guidance while deliberately excluding historical\n+``.shiki`` evidence. The converse is not required: a record may be uncited.\n \n The suite is dependency free: standard-library ``unittest`` only.\n \"\"\"\n@@ -31,17 +32,18 @@\n \n REPO_ROOT = Path(__file__).resolve().parents[1]\n \n-# \"ADR 0016\" -- a four-digit citation. The trailing \\b keeps a five-digit token\n-# from being read as a truncated four-digit number.\n-_CITATION = re.compile(r\"\\bADR (\\d{4})\\b\")\n-# \"0016-locks-bound-product-paths.md\" -- the leading four digits name the record.\n-_ADR_FILENAME = re.compile(r\"^(\\d{4})-.*\\.md$\")\n+# ``ADR 0016`` / ``SADR-0016`` -- a namespace plus four-digit citation. The\n+# trailing ``\\b`` keeps a five-digit token from being read as a truncated one.\n+_CITATION = re.compile(r\"\\b(S?ADR)[ -](\\d{4})\\b\")\n+# Target ADRs retain the numeric filename; Shiki platform decisions use SADR.\n+_DECISION_FILENAME = re.compile(r\"^(?:(SADR)-)?(\\d{4})-.*\\.md$\")\n \n \n class Violation(NamedTuple):\n     \"\"\"A citation that does not resolve to exactly one decision record.\"\"\"\n \n     citing_file: str  # repo-relative path of the file that cites the ADR\n+    namespace: str  # \"ADR\" for target decisions; \"SADR\" for Shiki decisions\n     number: str  # the cited four-digit number, e.g. \"0016\"\n     line: int  # 1-based line of the citation\n     reason: str  # \"missing\" (zero matches) or \"ambiguous\" (two or more)\n@@ -49,44 +51,59 @@\n \n     @property\n     def expected_path(self) -> str:\n-        return f\"docs/adr/{self.number}-*.md\"\n+        prefix = \"SADR-\" if self.namespace == \"SADR\" else \"\"\n+        return f\"docs/adr/{prefix}{self.number}-*.md\"\n \n     def describe(self) -> str:\n         \"\"\"Name the citing file, the cited number, and the missing path.\"\"\"\n         if self.reason == \"missing\":\n             return (\n-                f\"{self.citing_file}:{self.line} cites ADR {self.number} \"\n+                f\"{self.citing_file}:{self.line} cites {self.namespace} {self.number} \"\n                 f\"but no document matches {self.expected_path}\"\n             )\n         joined = \", \".join(self.matches)\n         return (\n-            f\"{self.citing_file}:{self.line} cites ADR {self.number} which \"\n+            f\"{self.citing_file}:{self.line} cites {self.namespace} {self.number} which \"\n             f\"resolves to {len(self.matches)} documents (expected exactly one \"\n             f\"at {self.expected_path}): {joined}\"\n         )\n \n \n-def _adr_index(root: Path) -> dict[str, list[Path]]:\n-    \"\"\"Map every four-digit ADR number to the record files that claim it.\"\"\"\n-    index: dict[str, list[Path]] = {}\n+def _adr_index(root: Path) -> dict[tuple[str, str], list[Path]]:\n+    \"\"\"Map each decision namespace and number to the files that claim it.\"\"\"\n+    index: dict[tuple[str, str], list[Path]] = {}\n     adr_dir = root / \"docs\" / \"adr\"\n     if adr_dir.is_dir():\n         for path in sorted(adr_dir.glob(\"*.md\")):\n-            match = _ADR_FILENAME.match(path.name)\n+            match = _DECISION_FILENAME.match(path.name)\n             if match:\n-                index.setdefault(match.group(1), []).append(path)\n+                namespace = \"SADR\" if match.group(1) else \"ADR\"\n+                index.setdefault((namespace, match.group(2)), []).append(path)\n     return index\n \n \n def _citing_files(root: Path, include_docs: bool = True) -> list[Path]:\n-    \"\"\"Files scanned for citations: scripts/*.py, and cheaply the markdown.\"\"\"\n+    \"\"\"Current, shipped text surfaces scanned for decision citations.\"\"\"\n     files = sorted((root / \"scripts\").glob(\"*.py\"))\n+    files += sorted((root / \"scripts\").glob(\"*.sh\"))\n     if include_docs:\n         docs = root / \"docs\"\n         if docs.is_dir():\n             files += sorted(docs.rglob(\"*.md\"))\n         files += sorted(root.glob(\"*.md\"))\n-    return files\n+        pyproject = root / \"pyproject.toml\"\n+        if pyproject.is_file():\n+            files.append(pyproject)\n+        for relative in (\".github\", \".codex\"):\n+            surface = root / relative\n+            if surface.is_dir():\n+                files += sorted(\n+                    path\n+                    for path in surface.rglob(\"*\")\n+                    if path.is_file()\n+                    and path.suffix in {\".md\", \".py\", \".sh\", \".toml\", \".yaml\", \".yml\"}\n+                )\n+    return sorted(set(files))\n \n \n def find_adr_reference_violations(\n@@ -94,10 +111,9 @@\n ) -> list[Violation]:\n     \"\"\"Return every ``ADR ####`` citation under ``root`` that fails to resolve.\n \n-    A citation resolves when exactly one ``docs/adr/####-*.md`` matches its\n-    number. Zero matches is a \"missing\" violation; two or more is \"ambiguous\".\n-    Records that are never cited produce nothing -- the converse is not\n-    required.\n+    A citation resolves when exactly one record in the same namespace matches\n+    its number. Zero matches is a \"missing\" violation; two or more is\n+    \"ambiguous\". Records that are never cited produce nothing.\n     \"\"\"\n     root = Path(root)\n     index = _adr_index(root)\n@@ -110,13 +126,15 @@\n         rel = path.relative_to(root).as_posix()\n         for lineno, line in enumerate(text.splitlines(), start=1):\n             for match in _CITATION.finditer(line):\n-                number = match.group(1)\n-                found = index.get(number, [])\n+                namespace, number = match.groups()\n+                found = index.get((namespace, number), [])\n                 if len(found) == 1:\n                     continue\n                 reason = \"missing\" if not found else \"ambiguous\"\n                 rels = [p.relative_to(root).as_posix() for p in found]\n-                violations.append(Violation(rel, number, lineno, reason, rels))\n+                violations.append(\n+                    Violation(rel, namespace, number, lineno, reason, rels)\n+                )\n     return violations\n \n \n@@ -129,8 +147,8 @@\n         self.assertEqual(\n             violations,\n             [],\n-            \"Every 'ADR ####' citation must resolve to exactly one \"\n-            f\"docs/adr/####-*.md:\\n{detail}\",\n+            \"Every ADR/SADR citation must resolve exactly once in its own \"\n+            f\"docs/adr namespace:\\n{detail}\",\n         )\n \n     def test_scan_actually_reads_scripts(self) -> None:\n@@ -142,12 +160,38 @@\n             \"the citation scan must cover scripts/*.py\",\n         )\n \n+    def test_platform_citations_do_not_use_unresolved_numeric_shorthand(self) -> None:\n+        shorthand = re.compile(r\"SADR-\\d{4}/\\d{4}\")\n+        offenders = []\n+        for path in _citing_files(REPO_ROOT):\n+            text = path.read_text(encoding=\"utf-8\")\n+            if shorthand.search(text):\n+                offenders.append(path.relative_to(REPO_ROOT).as_posix())\n+        self.assertEqual(\n+            offenders,\n+            [],\n+            \"repeat the SADR prefix so every cited number is independently resolvable\",\n+        )\n+\n     def test_carried_records_are_indexed(self) -> None:\n         # The two records this Goal carried onto the default branch, and which\n         # the scripts cite most, must be present.\n         index = _adr_index(REPO_ROOT)\n-        self.assertIn(\"0016\", index)\n-        self.assertIn(\"0017\", index)\n+        self.assertIn((\"SADR\", \"0016\"), index)\n+        self.assertIn((\"SADR\", \"0017\"), index)\n+\n+    def test_platform_tree_has_only_sadr_0001_through_0019(self) -> None:\n+        adr_dir = REPO_ROOT / \"docs\" / \"adr\"\n+        sadrs = sorted(adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\"))\n+        self.assertEqual(\n+            {path.name.split(\"-\", 2)[1] for path in sadrs},\n+            {f\"{number:04d}\" for number in range(1, 20)},\n+        )\n+        self.assertEqual(\n+            list(adr_dir.glob(\"[0-9][0-9][0-9][0-9]-*.md\")),\n+            [],\n+            \"the Shiki platform tree must not retain legacy numeric SADR paths\",\n+        )\n \n \n class FixtureAdrReferences(unittest.TestCase):\n@@ -180,6 +224,26 @@\n         self.assertIn(\"9991\", message)  # cited number\n         self.assertIn(\"docs/adr/9991-*.md\", message)  # missing path\n \n+    def test_target_adr_does_not_satisfy_missing_platform_sadr(self) -> None:\n+        root = self._make_repo(\n+            adr_names=[\"0042-target-decision.md\"],\n+            script_body='\"\"\"Target ADR 0042 exists, but platform SADR-0042 does not.\"\"\"\\n',\n+        )\n+        violations = find_adr_reference_violations(root, include_docs=False)\n+        self.assertEqual(len(violations), 1)\n+        self.assertEqual(violations[0].namespace, \"SADR\")\n+        self.assertEqual(violations[0].number, \"0042\")\n+        self.assertEqual(violations[0].expected_path, \"docs/adr/SADR-0042-*.md\")\n+\n+    def test_target_adr_and_platform_sadr_may_reuse_a_number(self) -> None:\n+        root = self._make_repo(\n+            adr_names=[\"0042-target-decision.md\", \"SADR-0042-platform-decision.md\"],\n+            script_body='\"\"\"Target ADR 0042 and platform SADR-0042 are distinct.\"\"\"\\n',\n+        )\n+        self.assertEqual(\n+            find_adr_reference_violations(root, include_docs=False), []\n+        )\n+\n     def test_uncited_adr_is_allowed(self) -> None:\n         # An ADR exists but nothing cites it -> not a violation. The converse\n         # (every ADR must be cited) is explicitly NOT required.\ndiff --git a/tests/test_guardian_review.py b/tests/test_guardian_review.py\nindex 88829e7810212635e2fc98aa5918d6f26294677d..00a85a237acd5bf2d6cb5b294128198c422402ca\n--- a/tests/test_guardian_review.py\n+++ b/tests/test_guardian_review.py\n@@ -162,7 +162,7 @@\n \n     def test_docs_only_is_light_and_not_conservative(self):\n         focus = gr.classify_review_focus([\"docs/adr/0099-x.md\", \"README.md\"], \"\", \"low\")\n-        self.assertTrue(any(\"Docs/ADR-only\" in f for f in focus))\n+        self.assertTrue(any(\"Docs/decision-record-only\" in f for f in focus))\n         self.assertFalse(any(\"conservative default\" in f.lower() for f in focus))\n \n     def test_unknown_category_is_conservative(self):\ndiff --git a/tests/test_policy_job_merged_prs.py b/tests/test_policy_job_merged_prs.py\nindex d45512e74681e5e649279236c7d4e79d5c3da471..b97989f8bd74d091de1a6b5275a20d8dc68c4512\n--- a/tests/test_policy_job_merged_prs.py\n+++ b/tests/test_policy_job_merged_prs.py\n@@ -3,8 +3,8 @@\n \n ``.github/workflows/shiki-cca-completion.yml`` has two consumers of the merged-PR\n proof: the CCA *signal* (``guardian_approval_signal.py`` in \"Prepare CCA context\")\n-and the authoritative gate (``mergegate_check.py`` in \"Enforce MergeGate\"). ADR\n-0017's bookkeeping-closeout exemption (condition 6) needs the merged implementation\n+and the authoritative gate (``mergegate_check.py`` in \"Enforce MergeGate\").\n+SADR-0017's bookkeeping-closeout exemption (condition 6) needs the merged implementation\n PR proven, and a closeout carries NO ``post_merge_reconcile`` marker. The signal\n already resolves ``MERGED_PRS`` unconditionally, but \"Enforce MergeGate\" used to\n resolve it only inside ``grep -q '<!-- shiki:post_merge_reconcile -->'``. So a\ndiff --git a/tests/test_sadr_install_migration.py b/tests/test_sadr_install_migration.py\nnew file mode 100644\nindex 0000000000000000000000000000000000000000..d51eac872b1c79ff4579c4c1401ca5d775a60d8d\n--- /dev/null\n+++ b/tests/test_sadr_install_migration.py\n@@ -0,0 +1,237 @@\n+\"\"\"SADR installation and collision-safe legacy upgrade behavior.\"\"\"\n+\n+from __future__ import annotations\n+\n+import contextlib\n+import hashlib\n+import io\n+import json\n+import shutil\n+import tempfile\n+import unittest\n+from pathlib import Path\n+\n+import shiki_test_support  # noqa: F401  (path bootstrap)\n+\n+import shiki_installer\n+from tests.test_adr_references import find_adr_reference_violations\n+\n+\n+PRODUCT_NUMBERS = tuple(range(13, 19))\n+\n+\n+def _quiet_install(target: Path, *, force: bool, validate: bool) -> None:\n+    with contextlib.redirect_stdout(io.StringIO()):\n+        shiki_installer.install_template(target, force=force, validate=validate)\n+\n+\n+def _sha256(path: Path) -> str:\n+    return hashlib.sha256(path.read_bytes()).hexdigest()\n+\n+\n+def _legacy_sadr_paths() -> tuple[str, ...]:\n+    return tuple(\n+        f\"docs/adr/{path.name.removeprefix('SADR-')}\"\n+        for path in sorted((shiki_installer.ROOT / \"docs\" / \"adr\").glob(\"SADR-*.md\"))\n+        if path.name != \"SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\"\n+    )\n+\n+\n+def _create_legacy_upgrade_fixture(\n+    target: Path,\n+) -> tuple[dict[str, bytes], tuple[str, ...]]:\n+    adr_dir = target / \"docs\" / \"adr\"\n+    adr_dir.mkdir(parents=True)\n+    product_bytes = {\n+        f\"{number:04d}-cubeshare-product-decision.md\": (\n+            f\"# CubeShare ADR {number:04d}\\n\\nDo not rewrite {number}.\\n\"\n+        ).encode()\n+        for number in PRODUCT_NUMBERS\n+    }\n+    for name, content in product_bytes.items():\n+        (adr_dir / name).write_bytes(content)\n+\n+    legacy_paths = _legacy_sadr_paths()\n+    for relative in legacy_paths:\n+        (target / relative).write_bytes(f\"legacy Shiki bytes for {relative}\\n\".encode())\n+\n+    stamp_path = target / shiki_installer.INSTALL_STAMP_PATH\n+    stamp_path.parent.mkdir(parents=True, exist_ok=True)\n+    stamp = {\n+        \"version\": shiki_installer.INSTALL_STAMP_VERSION,\n+        \"platform_commit\": None,\n+        \"installed_at\": \"2000-01-01T00:00:00+00:00\",\n+        \"digests\": {\n+            relative: _sha256(target / relative) for relative in legacy_paths\n+        },\n+    }\n+    stamp_path.write_text(\n+        json.dumps(stamp, indent=2, sort_keys=True) + \"\\n\", encoding=\"utf-8\"\n+    )\n+    return product_bytes, legacy_paths\n+\n+\n+def _tree_snapshot(root: Path) -> dict[str, tuple[str, bytes | str]]:\n+    \"\"\"Recursive byte/shape snapshot used to prove preflight is write-free.\"\"\"\n+    snapshot: dict[str, tuple[str, bytes | str]] = {}\n+    for path in sorted(root.rglob(\"*\")):\n+        relative = path.relative_to(root).as_posix()\n+        if path.is_symlink():\n+            snapshot[relative] = (\"symlink\", path.readlink().as_posix())\n+        elif path.is_dir():\n+            snapshot[relative] = (\"directory\", \"\")\n+        else:\n+            snapshot[relative] = (\"file\", path.read_bytes())\n+    return snapshot\n+\n+\n+class FreshSadrInstallTests(unittest.TestCase):\n+    def setUp(self) -> None:\n+        self.tmp = Path(tempfile.mkdtemp(prefix=\"shiki-sadr-fresh-\"))\n+        self.addCleanup(lambda: shutil.rmtree(self.tmp, ignore_errors=True))\n+        self.target = self.tmp / \"target\"\n+        self.adr_dir = self.target / \"docs\" / \"adr\"\n+        self.adr_dir.mkdir(parents=True)\n+        self.product_bytes = {\n+            f\"{number:04d}-product-decision.md\": (\n+                f\"# Product ADR {number:04d}\\n\\nTarget-owned bytes {number}.\\n\"\n+            ).encode()\n+            for number in PRODUCT_NUMBERS\n+        }\n+        for name, content in self.product_bytes.items():\n+            (self.adr_dir / name).write_bytes(content)\n+\n+    def test_fresh_install_preserves_product_adrs_and_installs_all_sadrs(self) -> None:\n+        _quiet_install(self.target, force=False, validate=True)\n+\n+        for name, expected in self.product_bytes.items():\n+            self.assertEqual((self.adr_dir / name).read_bytes(), expected)\n+\n+        sadr_numbers = {\n+            path.name.split(\"-\", 2)[1]\n+            for path in self.adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\")\n+        }\n+        self.assertEqual(sadr_numbers, {f\"{number:04d}\" for number in range(1, 20)})\n+\n+        numeric_records = {\n+            path.name\n+            for path in self.adr_dir.glob(\"[0-9][0-9][0-9][0-9]-*.md\")\n+        }\n+        self.assertEqual(numeric_records, set(self.product_bytes))\n+        stamp = json.loads(\n+            (self.target / shiki_installer.INSTALL_STAMP_PATH).read_text(\n+                encoding=\"utf-8\"\n+            )\n+        )\n+        for path in self.adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\"):\n+            relative = path.relative_to(self.target).as_posix()\n+            self.assertEqual(stamp[\"digests\"][relative], _sha256(path))\n+        self.assertEqual(find_adr_reference_violations(self.target), [])\n+\n+\n+class LegacySadrUpgradeTests(unittest.TestCase):\n+    def setUp(self) -> None:\n+        self.tmp = Path(tempfile.mkdtemp(prefix=\"shiki-sadr-upgrade-\"))\n+        self.addCleanup(lambda: shutil.rmtree(self.tmp, ignore_errors=True))\n+        self.target = self.tmp / \"target\"\n+        self.adr_dir = self.target / \"docs\" / \"adr\"\n+        self.product_bytes, self.legacy_paths = _create_legacy_upgrade_fixture(\n+            self.target\n+        )\n+\n+    def test_force_upgrade_deletes_only_stamped_legacy_sadrs(self) -> None:\n+        _quiet_install(self.target, force=True, validate=True)\n+\n+        for relative in self.legacy_paths:\n+            self.assertFalse((self.target / relative).exists(), relative)\n+        for name, expected in self.product_bytes.items():\n+            self.assertEqual((self.adr_dir / name).read_bytes(), expected)\n+\n+        sadrs = list(self.adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\"))\n+        self.assertEqual(len(sadrs), 19)\n+        refreshed = json.loads(\n+            (self.target / shiki_installer.INSTALL_STAMP_PATH).read_text(encoding=\"utf-8\")\n+        )\n+        for relative in self.legacy_paths:\n+            self.assertNotIn(relative, refreshed[\"digests\"])\n+        for path in sadrs:\n+            relative = path.relative_to(self.target).as_posix()\n+            self.assertEqual(refreshed[\"digests\"][relative], _sha256(path))\n+\n+    def test_non_force_install_never_deletes_legacy_files(self) -> None:\n+        before = {\n+            relative: (self.target / relative).read_bytes()\n+            for relative in self.legacy_paths\n+        }\n+        _quiet_install(self.target, force=False, validate=False)\n+        for relative, expected in before.items():\n+            self.assertEqual((self.target / relative).read_bytes(), expected)\n+        refreshed = json.loads(\n+            (self.target / shiki_installer.INSTALL_STAMP_PATH).read_text(encoding=\"utf-8\")\n+        )\n+        for relative in self.legacy_paths:\n+            self.assertEqual(refreshed[\"digests\"][relative], _sha256(self.target / relative))\n+\n+\n+class LegacyCleanupPreflightTests(unittest.TestCase):\n+    def setUp(self) -> None:\n+        self.tmp = Path(tempfile.mkdtemp(prefix=\"shiki-sadr-preflight-\"))\n+        self.addCleanup(lambda: shutil.rmtree(self.tmp, ignore_errors=True))\n+\n+    def test_all_ownership_blockers_abort_before_any_write(self) -> None:\n+        cases = (\n+            (\"absent\", \"install stamp is absent\"),\n+            (\"unreadable\", \"install stamp is unreadable\"),\n+            (\"malformed\", \"install stamp is malformed\"),\n+            (\"missing-digest\", \"install stamp has no digest\"),\n+            (\"digest-mismatch\", \"digest mismatch\"),\n+        )\n+        for name, expected_reason in cases:\n+            with self.subTest(name=name):\n+                target = self.tmp / name\n+                _, legacy_paths = _create_legacy_upgrade_fixture(target)\n+                stamp_path = target / shiki_installer.INSTALL_STAMP_PATH\n+\n+                if name == \"absent\":\n+                    stamp_path.unlink()\n+                elif name == \"unreadable\":\n+                    stamp_path.unlink()\n+                    stamp_path.mkdir()\n+                elif name == \"malformed\":\n+                    stamp_path.write_text(\"{not-json\\n\", encoding=\"utf-8\")\n+                else:\n+                    stamp = json.loads(stamp_path.read_text(encoding=\"utf-8\"))\n+                    if name == \"missing-digest\":\n+                        stamp[\"digests\"] = {}\n+                    else:\n+                        stamp[\"digests\"] = {\n+                            relative: \"deadbeef\" for relative in legacy_paths\n+                        }\n+                    stamp_path.write_text(\n+                        json.dumps(stamp, indent=2, sort_keys=True) + \"\\n\",\n+                        encoding=\"utf-8\",\n+                    )\n+\n+                before = _tree_snapshot(target)\n+                with self.assertRaises(shiki_installer.ShikiError) as raised:\n+                    _quiet_install(target, force=True, validate=False)\n+                after = _tree_snapshot(target)\n+\n+                self.assertEqual(before, after, f\"{name} preflight changed the tree\")\n+                message = str(raised.exception)\n+                self.assertIn(expected_reason, message)\n+                self.assertIn(\"No files were written\", message)\n+                for relative in legacy_paths:\n+                    self.assertIn(relative, message)\n+\n+    def test_absent_stamp_is_allowed_when_no_legacy_path_exists(self) -> None:\n+        target = self.tmp / \"no-legacy\"\n+        target.mkdir()\n+        _quiet_install(target, force=True, validate=False)\n+        self.assertTrue(\n+            (target / \"docs\" / \"adr\" / \"SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\").is_file()\n+        )\n+\n+\n+if __name__ == \"__main__\":\n+    unittest.main()\n\ndiff --git a/.codex/skills/shiki/SKILL.md b/.codex/skills/shiki/SKILL.md\nindex c8e2e85d9d75d49cc4f2d6286ee22659c4e53e75..93cded623b2c15d9f65c76491a1c2d813ac9e6c0\n--- a/.codex/skills/shiki/SKILL.md\n+++ b/.codex/skills/shiki/SKILL.md\n@@ -18,6 +18,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n Codex CLI does not register this skill as a custom `/shiki` slash command. When\n the user types `/shiki` in Codex CLI and gets \"Unrecognized command\", explain\n that this is expected: invoke Shiki in Codex with natural language such as\n@@ -69,7 +72,7 @@\n \n ## Responsibilities\n \n-- Claude Code implements and repairs by default (`shiki runner claude`, ADR 0008); Codex implements and repairs tasks explicitly assigned to `codex`.\n+- Claude Code implements and repairs by default (`shiki runner claude`, SADR-0008); Codex implements and repairs tasks explicitly assigned to `codex`.\n - Claude Code Action can act as GitHub-side CCA or reviewer.\n - CCA judges completion.\n - MergeGate authorizes state transitions and merge readiness.\ndiff --git a/.github/prompts/cca-completion-check.md b/.github/prompts/cca-completion-check.md\nindex ed203ad5f302ecc81372d8ab27d2b1232ca97497..fd89950528333ef6242e6d5443904f5392ab7e56\n--- a/.github/prompts/cca-completion-check.md\n+++ b/.github/prompts/cca-completion-check.md\n@@ -19,6 +19,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n ## Required Reading\n \n Read only the files needed to judge the PR contract. Prefer this order:\ndiff --git a/.github/prompts/codex-task-handoff.md b/.github/prompts/codex-task-handoff.md\nindex 695e211d76dea1c60e8d5bb360e78487808286a4..59441d576b01a39569722f226c854e5a740e9eb5\n--- a/.github/prompts/codex-task-handoff.md\n+++ b/.github/prompts/codex-task-handoff.md\n@@ -2,7 +2,7 @@\n \n You are Codex Front acting as the Implementer for a Shiki task.\n \n-Follow `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant ADRs, and `docs/agents/codex-handoff.md`.\n+Follow `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant Shiki SADRs and target ADRs, and `docs/agents/codex-handoff.md`.\n \n Rules:\n \ndiff --git a/AGENTS.md b/AGENTS.md\nindex 7f041100d49c2a7525f51cb0f5d696a4a6d1c78c..a06bfbabc6fa48a007389e25e12d7ed523936bed\n--- a/AGENTS.md\n+++ b/AGENTS.md\n@@ -22,7 +22,7 @@\n - **Codex Front** is an optional implementer through Codex App, Codex CLI, Codex IDE extension, or Codex Web signed in with ChatGPT OAuth/subscription auth, dispatched with `shiki runner codex` when a task is explicitly assigned to `codex`.\n - **GitHub CCA** is implemented by Claude Code Action by default, using `CLAUDE_CODE_OAUTH_TOKEN`.\n \n-Do not assume `openai/codex-action`, `OPENAI_API_KEY`, or API-key based Codex automation in the default Shiki path. API-key runners are explicit target-repo extensions and require their own ADR.\n+Do not assume `openai/codex-action`, `OPENAI_API_KEY`, or API-key based Codex automation in the default Shiki path. API-key runners are explicit target-repo extensions and require their own target ADR.\n \n Shiki is not:\n \n@@ -44,10 +44,14 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use\n+`SADR-NNNN-*.md`; target product decisions retain `NNNN-*.md` and the `ADR`\n+namespace.\n+\n Use the canonical source-of-truth order above. Runtime-specific reading orders\n are operational guidance only and must not override or reorder it.\n \n-Conversation state is not durable truth. If a decision matters, put it in GitHub, `.shiki/`, `CONTEXT.md`, or an ADR.\n+Conversation state is not durable truth. If a decision matters, put it in GitHub, `.shiki/`, `CONTEXT.md`, a Shiki SADR, or a target ADR.\n \n If GitHub and `.shiki/` disagree, surface the conflict and prefer GitHub operational state until the mirror is repaired.\n \n@@ -75,8 +79,8 @@\n Every non-trivial change follows this loop:\n \n 1. **Goal**: clarify outcome, completion conditions, non-goals, risk level, and success signals.\n-2. **grill-with-docs**: challenge the plan against domain docs, ADRs, code reality, terminology, and edge scenarios.\n-3. **Context & Impact**: find relevant docs, ADRs, code, dependencies, owners, locks, and verification surfaces.\n+2. **grill-with-docs**: challenge the plan against domain docs, Shiki SADRs, target ADRs, code reality, terminology, and edge scenarios.\n+3. **Context & Impact**: find relevant docs, SADRs, target ADRs, code, dependencies, owners, locks, and verification surfaces.\n 4. **PRD and Spec Freeze**: use `to-prd` when context is settled enough to become durable product/engineering intent. The operator's explicit approval of the PRD is Spec Freeze: the plan records a `spec_freeze` block, and no plan runs without it. Steps 1-4 form Requirements Definition \u2014 one continuous operator dialogue. After Spec Freeze, scope changes require an operator-approved Spec Amendment; non-scope-moving interpretations go to the Assumption Log.\n 5. **Issues**: use `to-issues` to create independently grabbable vertical-slice issues. Prefer AFK slices over HITL slices where possible.\n 6. **Triage**: label issues for readiness, risk, runtime, skills, and MergeGate state.\n@@ -167,7 +171,7 @@\n - Testing decisions.\n - Out-of-scope items.\n - Further notes.\n-- Links to relevant domain terms and ADRs.\n+- Links to relevant domain terms, SADRs, and target ADRs.\n \n Every executable task must include:\n \n@@ -193,7 +197,7 @@\n \n Context & Impact must identify:\n \n-- Relevant documents, domain terms, ADRs, and past decisions.\n+- Relevant documents, domain terms, SADRs, target ADRs, and past decisions.\n - Relevant modules, interfaces, seams, callers, owners, and tests.\n - Dependency relationships between tasks.\n - Candidate locks and lock conflicts.\n@@ -210,7 +214,7 @@\n | Skill | Required when |\n | --- | --- |\n | `setup-matt-pocock-skills` | First configuring the repo for skills, or when issue tracker, triage labels, or domain docs are missing. |\n-| `grill-with-docs` | Default for non-trivial Goals before PRD/issues; always required when plans, terminology, boundaries, tradeoffs, or ADR-worthy decisions are ambiguous. |\n+| `grill-with-docs` | Default for non-trivial Goals before PRD/issues; always required when plans, terminology, boundaries, tradeoffs, or decision-record-worthy choices are ambiguous. |\n | `zoom-out` | The agent lacks an architectural map of the relevant code area. |\n | `to-prd` | Settled Goal context must become durable product/engineering intent. |\n | `to-issues` | A Goal, PRD, or plan must become independently grabbable vertical-slice issues. |\n@@ -348,7 +352,7 @@\n - Three or more new files.\n - Any high-risk or critical label.\n \n-When the architecture gate triggers, run `grill-with-docs` or `improve-codebase-architecture` as appropriate and record durable decisions in an ADR when needed.\n+When the architecture gate triggers, run `grill-with-docs` or `improve-codebase-architecture` as appropriate and record durable decisions in a Shiki SADR or target ADR as applicable.\n \n ## Engineering Discipline\n \ndiff --git a/CLAUDE.md b/CLAUDE.md\nindex 98a932e7841a632866fcd53b0bad0e3af3711d3d..98a31399404d6fb044b4fd493f4459ac87f439de\n--- a/CLAUDE.md\n+++ b/CLAUDE.md\n@@ -14,6 +14,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n ## Claude Role\n \n Default to planner, default implementer, reviewer, coordinator, documentation editor, CCA-style completion judge, and final-judgment assistant.\n@@ -57,7 +60,7 @@\n At the start of a Shiki session:\n \n 1. Read `AGENTS.md`.\n-2. Read `CONTEXT.md` and relevant ADRs.\n+2. Read `CONTEXT.md` and relevant Shiki SADRs and target ADRs.\n 3. Read active `.shiki/` Goal/task/ledger state when present.\n 4. Read active distilled rules from `.shiki/memories/` (status `distilled`, `active: true`, not revoked or superseded) for applicable guidance; the same rules are injected deterministically into task handoffs as the `## Distilled Rules` section (\u00a73.5).\n 5. Read the GitHub Issue or PR when the task is GitHub-backed.\n@@ -74,7 +77,7 @@\n - Define the Goal, completion conditions, non-goals, and risk level.\n - Treat `grill-with-docs` as the default planning entry point for non-trivial Goals.\n - Challenge domain terms against `CONTEXT.md`.\n-- Challenge hard-to-reverse tradeoffs against ADRs.\n+- Challenge hard-to-reverse tradeoffs against SADRs and target ADRs.\n - Explore code instead of asking when code can answer the question.\n - Run Context & Impact before implementation planning.\n - Produce a Task DAG, not an unordered checklist.\n@@ -94,7 +97,7 @@\n - For each question, provide your recommended answer.\n - Resolve dependency decisions before downstream design questions.\n - Update `CONTEXT.md` inline when terminology crystallizes.\n-- Offer ADRs only when the decision is hard to reverse, surprising without context, and a real tradeoff.\n+- Offer a SADR or target ADR only when the decision is hard to reverse, surprising without context, and a real tradeoff.\n - Convert unresolved questions into HITL blockers rather than allowing Codex to guess.\n \n ## PRD and Issues Standard\n@@ -103,7 +106,7 @@\n \n - Use the project's domain glossary vocabulary.\n - Avoid volatile file paths unless they are essential.\n-- Include implementation decisions, testing decisions, out-of-scope items, and relevant ADR/domain links.\n+- Include implementation decisions, testing decisions, out-of-scope items, and relevant SADR/target-ADR/domain links.\n - Identify deep-module opportunities and testable interfaces.\n \n When creating issues:\n@@ -121,7 +124,7 @@\n - Branch/worktree target.\n - Scope and non-goals.\n - Dependencies and locks.\n-- Relevant docs, ADRs, modules, and tests.\n+- Relevant docs, SADRs, target ADRs, modules, and tests.\n - Required skill invocations.\n - TDD expectations.\n - Pre-PR `code-review` gate expectations.\n@@ -152,7 +155,7 @@\n \n - Treat yourself as a judge, not implementer.\n - Do not write production code.\n-- Read `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant ADRs, PRD, task issue, PR body, diff, checks, reviews, labels, and `.shiki/` state.\n+- Read `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant SADRs and target ADRs, PRD, task issue, PR body, diff, checks, reviews, labels, and `.shiki/` state.\n - Evaluate every applicable checklist in `docs/agents/checklists.md`.\n - Map each acceptance criterion to durable evidence.\n - Distinguish implementation failure from missing evidence.\ndiff --git a/CONTEXT.md b/CONTEXT.md\nindex e61a9be06a0cce0d026484083a657bde2067e234..237ee8aa3ea8fab334ece49768bc5ed3b5af0974\n--- a/CONTEXT.md\n+++ b/CONTEXT.md\n@@ -25,7 +25,7 @@\n _Avoid_: jumping directly into code\n \n **grill-with-docs**:\n-A planning skill that challenges a plan against domain language, ADRs, code reality, and concrete edge scenarios. It resolves design-tree questions before PRD/issues. It runs inside Requirements Definition.\n+A planning skill that challenges a plan against domain language, Shiki SADRs, target ADRs, code reality, and concrete edge scenarios. It resolves design-tree questions before PRD/issues. It runs inside Requirements Definition.\n _Avoid_: generic brainstorming, silent assumptions\n \n **Requirements Definition**:\ndiff --git a/CONTRIBUTING.md b/CONTRIBUTING.md\nindex c6d9b656e73abb8a55e5f660123e03153dcb3e6d..9e37a8fbf668db5b22461aaae5548b8f1354394c\n--- a/CONTRIBUTING.md\n+++ b/CONTRIBUTING.md\n@@ -17,7 +17,7 @@\n 1. **Goal** \u2014 start from a user-approved Goal with an outcome, completion\n    conditions, non-goals, risk level, and success signals.\n 2. **grill-with-docs** \u2014 for any non-trivial change, challenge the plan against\n-   domain language, ADRs, and code reality before writing a PRD.\n+   domain language, Shiki SADRs, target ADRs, and code reality before writing a PRD.\n 3. **PRD** \u2014 capture settled product/engineering intent (`to-prd`).\n 4. **Spec Freeze** \u2014 the operator explicitly approves the PRD/requirements;\n    the plan records a `spec_freeze` block and no plan runs without it\n@@ -94,7 +94,7 @@\n   entries.\n - Do not hand-edit `.shiki/goals/*.json` or `.shiki/manifest.json` in a task PR.\n - Conversation state is not durable truth. If a decision matters, record it in\n-  GitHub, `.shiki/`, `CONTEXT.md`, or an ADR.\n+  GitHub, `.shiki/`, `CONTEXT.md`, a Shiki SADR, or a target ADR.\n \n ## Architecture Gate\n \n@@ -103,7 +103,7 @@\n or CI policy, package structure, three or more structural files, more than 100\n net new lines in a core module, three or more new files, or any high-risk label.\n When triggered, run `grill-with-docs` or `improve-codebase-architecture` and\n-record durable decisions in an ADR when needed (see `docs/adr/`).\n+record durable decisions in a Shiki SADR or target ADR as applicable (see `docs/adr/`).\n \n ## Safety\n \ndiff --git a/SYSTEM_PROMPT.md b/SYSTEM_PROMPT.md\nindex aaaea45e3a896c3a44ba89eec8bb84325ecf6c62..da009c86a88285269dee0155e58e550c6d744ea9\n--- a/SYSTEM_PROMPT.md\n+++ b/SYSTEM_PROMPT.md\n@@ -30,6 +30,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n Use the canonical source-of-truth order above. Runtime-specific reading orders\n are operational guidance only and must not override or reorder it. The current\n conversation is non-durable operator input.\n@@ -48,14 +51,14 @@\n \n A runtime may only do work that is explicitly assigned by the Goal, task, PR, or MergeGate decision.\n \n-Do not assume `openai/codex-action`, `OPENAI_API_KEY`, or API-key based Codex automation in the default Shiki path. API-key runners are explicit target-repo extensions and require their own ADR.\n+Do not assume `openai/codex-action`, `OPENAI_API_KEY`, or API-key based Codex automation in the default Shiki path. API-key runners are explicit target-repo extensions and require their own target ADR.\n \n ## 4. Non-negotiable Shiki flow\n \n For any non-trivial Goal, use this flow:\n \n 1. **Goal Seek**: define outcome, non-goals, risk level, completion criteria, and evidence requirements.\n-2. **grill-with-docs**: challenge the plan against `CONTEXT.md`, ADRs, code reality, terminology, boundaries, and edge scenarios. Ask one question at a time when the operator is available. If code can answer a question, inspect code instead of asking.\n+2. **grill-with-docs**: challenge the plan against `CONTEXT.md`, Shiki SADRs, target ADRs, code reality, terminology, boundaries, and edge scenarios. Ask one question at a time when the operator is available. If code can answer a question, inspect code instead of asking.\n 3. **Context & Impact**: identify modules, interfaces, seams, callers, dependencies, locks, risk, and verification surfaces.\n 4. **to-prd + Spec Freeze**: turn settled context into a PRD using domain vocabulary and testing decisions. The operator's explicit approval of the PRD is Spec Freeze, recorded as the plan's `spec_freeze` block; plans without it do not run. Steps 1-4 form Requirements Definition.\n 5. **to-issues**: decompose the PRD into independently grabbable vertical-slice issues. Prefer AFK slices; mark HITL slices when judgment is still required.\n@@ -75,7 +78,7 @@\n Before making any material change:\n \n 1. Read `AGENTS.md` and any runtime-specific wrapper such as `CLAUDE.md`.\n-2. Read `CONTEXT.md` and relevant ADRs.\n+2. Read `CONTEXT.md` and relevant SADRs and target ADRs.\n 3. Read the GitHub Issue / PR and `.shiki/` state for the active Goal or task.\n 4. Identify your role: Planner, Implementer, Reviewer, Completion Check Agent, Repairer, or Guardian-assist.\n 5. Identify Goal id, task id, branch, PR, dependencies, locks, risk level, required skills, and required evidence.\n@@ -103,7 +106,7 @@\n \n At minimum, identify:\n \n-- Relevant documents, ADRs, domain terms, and prior decisions.\n+- Relevant documents, SADRs, target ADRs, domain terms, and prior decisions.\n - Relevant modules, interfaces, seams, callers, owners, and tests.\n - Dependency relationships between tasks.\n - Candidate file locks and lock conflicts.\n@@ -152,7 +155,7 @@\n \n CCA must read:\n \n-- `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant ADRs.\n+- `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant SADRs and target ADRs.\n - Parent Goal issue, PRD issue, task issue, comments, and labels.\n - PR body, diff, commits, changed files, reviews, CI checks, and artifacts available to the workflow.\n - `.shiki/` task contract, locks, ledger entries, CCA checklist profile, and prior repair packets.\n@@ -214,7 +217,7 @@\n - Three or more new files.\n - Any high-risk or critical label.\n \n-When the architecture gate triggers, run `grill-with-docs` or `improve-codebase-architecture` as appropriate and record the decision in an ADR if it is hard to reverse or likely to be re-litigated.\n+When the architecture gate triggers, run `grill-with-docs` or `improve-codebase-architecture` as appropriate and record the decision in a Shiki SADR or target ADR as applicable if it is hard to reverse or likely to be re-litigated.\n \n ## 14. Skill Gate\n \n@@ -223,7 +226,7 @@\n Use these skills:\n \n - `setup-matt-pocock-skills`: before first use of the engineering skill set, or when issue tracker, triage labels, or domain docs are missing.\n-- `grill-with-docs`: default for non-trivial Goals before PRD/issues; required for ambiguous plans, terminology, boundaries, ADR-worthy tradeoffs, or design-tree decisions.\n+- `grill-with-docs`: default for non-trivial Goals before PRD/issues; required for ambiguous plans, terminology, boundaries, decision-record-worthy tradeoffs, or design-tree decisions.\n - `zoom-out`: unfamiliar code area or missing architectural map.\n - `to-prd`: convert settled context into a PRD.\n - `to-issues`: break a PRD, plan, or Goal into independently grabbable vertical-slice issues.\ndiff --git a/docs/adr/0001-keep-shiki-as-platform-name.md b/docs/adr/SADR-0001-keep-shiki-as-platform-name.md\nindex 20a9637c3b9d8c97de7ba8db7cfae4d82d9a7dba..8b685439959aada1b221c112f0ac3c51ca8a1695\n--- a/docs/adr/0001-keep-shiki-as-platform-name.md\n+++ b/docs/adr/SADR-0001-keep-shiki-as-platform-name.md\n@@ -1,3 +1,3 @@\n-# Keep Shiki As The Platform Name\n+# SADR-0001: Keep Shiki As The Platform Name\n \n Shiki remains the product name for the GitHub-native agentic engineering control plane. We considered more descriptive names such as MergePilot, MergeGate, and Kiroku, but kept Shiki because it carries the existing goal-seeking framework identity while remaining independent of Hermes, Codex, Claude Code, or any single runtime.\ndiff --git a/docs/adr/0002-use-agents-as-shared-constitution.md b/docs/adr/SADR-0002-use-agents-as-shared-constitution.md\nindex 73de30ecb078120eac540d9bc51855e3cd48d0e3..c5486182bf06fca36ea508496b013c67302fd989\n--- a/docs/adr/0002-use-agents-as-shared-constitution.md\n+++ b/docs/adr/SADR-0002-use-agents-as-shared-constitution.md\n@@ -1,3 +1,3 @@\n-# Use AGENTS As The Shared Agent Constitution\n+# SADR-0002: Use AGENTS As The Shared Agent Constitution\n \n Shiki uses `AGENTS.md` as the runtime-neutral constitution and `CLAUDE.md` as a thin Claude Code wrapper. This keeps Codex, Claude Code, Hermes Runner, and future Agent Runtimes aligned on the same GitHub-first MergeGate rules while still allowing runtime-specific instructions where needed.\ndiff --git a/docs/adr/0003-require-skill-gate-for-mergegate-readiness.md b/docs/adr/SADR-0003-require-skill-gate-for-mergegate-readiness.md\nindex 424be0c9af4c8f0136f1fde68a468c47ff84e764..7fc337987c7df9792f75f181dfcbed75fe5ffff2\n--- a/docs/adr/0003-require-skill-gate-for-mergegate-readiness.md\n+++ b/docs/adr/SADR-0003-require-skill-gate-for-mergegate-readiness.md\n@@ -1,3 +1,3 @@\n-# Require Skill Gate For MergeGate Readiness\n+# SADR-0003: Require Skill Gate For MergeGate Readiness\n \n Shiki requires relevant engineering skills to be invoked before execution when their triggers apply. This makes skills part of MergeGate readiness rather than optional prompt style, because Shiki depends on repeatable planning, issue decomposition, TDD, diagnosis, triage, and architecture review before autonomous or parallel agent work can be trusted.\ndiff --git a/docs/adr/0004-use-github-first-with-shiki-mirror.md b/docs/adr/SADR-0004-use-github-first-with-shiki-mirror.md\nindex d0a3a9174dc646a945a9d325954ffaf00c352040..a79c54ae9ee8eed2a0524192b864da4e22a92d3f\n--- a/docs/adr/0004-use-github-first-with-shiki-mirror.md\n+++ b/docs/adr/SADR-0004-use-github-first-with-shiki-mirror.md\n@@ -1,4 +1,4 @@\n-# ADR 0004: Use GitHub First With A Repository-Local Shiki Mirror\n+# SADR-0004: Use GitHub First With A Repository-Local Shiki Mirror\n \n ## Status\n \ndiff --git a/docs/adr/0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md b/docs/adr/SADR-0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\nindex b339baf49b89d9290e79c3d51b36e7b2bde3bd95..0f3c3eb9afccdb3403ce978e21d04fe12e495da0\n--- a/docs/adr/0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\n+++ b/docs/adr/SADR-0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\n@@ -1,8 +1,8 @@\n-# ADR 0005: Use Codex As Front And Claude Code Action As The GitHub Runtime\n+# SADR-0005: Use Codex As Front And Claude Code Action As The GitHub Runtime\n \n ## Status\n \n-Accepted (implementer default superseded by ADR 0008; the auth model and the\n+Accepted (implementer default superseded by SADR-0008; the auth model and the\n Claude Code Action GitHub runtime decision remain in force)\n \n ## Context\n@@ -26,4 +26,4 @@\n - Shiki avoids requiring `OPENAI_API_KEY` for the default workflow.\n - Codex work happens through the user's authenticated Codex front surface, not as a hidden backend Action.\n - Claude Code Action is configured for OAuth-token based subscription usage by default.\n-- API-key based automation remains possible only as an explicit target-repo extension with its own ADR.\n+- API-key based automation remains possible only as an explicit target-repo extension with its own target ADR.\ndiff --git a/docs/adr/0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md b/docs/adr/SADR-0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\nindex 4bcae9d1e52f0a37b8e9fbafa5cd14b6ee1993bd..7a38538a6ae9929567f0ade67e7ea6a60d0b3f7e\n--- a/docs/adr/0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\n+++ b/docs/adr/SADR-0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\n@@ -1,4 +1,4 @@\n-# ADR 0006: Separate Codex Implementation, CCA Completion, And MergeGate Authorization\n+# SADR-0006: Separate Codex Implementation, CCA Completion, And MergeGate Authorization\n \n ## Status\n \ndiff --git a/docs/adr/0007-packaging-and-release.md b/docs/adr/SADR-0007-packaging-and-release.md\nindex 79b84ddb97167372b44641e5b7860e400ce80822..bb445a2e2e856d9d1f5fdbe35de8d4e8cec0fb32\n--- a/docs/adr/0007-packaging-and-release.md\n+++ b/docs/adr/SADR-0007-packaging-and-release.md\n@@ -1,4 +1,4 @@\n-# ADR 0007: Packaging And Release For Shiki\n+# SADR-0007: Packaging And Release For Shiki\n \n ## Status\n \n@@ -25,7 +25,7 @@\n   not introduce third-party runtime dependencies or break the shim.\n - Releases must run inside the operator's subscription-authenticated toolchain\n   and must not require extra secrets beyond the built-in `GITHUB_TOKEN`\n-  (consistent with ADR 0005's no-API-key-by-default stance).\n+  (consistent with SADR-0005's no-API-key-by-default stance).\n - `.shiki/manifest.json` already encodes the install surface (`install.include`,\n   `install.create_directories`, `install.exclude_from_commit`), and it must not\n   be mutated as part of this change.\n@@ -34,7 +34,7 @@\n \n This is a hard-to-reverse platform decision (it sets the version source of\n truth, the release trigger, and the public install contract), so it is recorded\n-as an ADR.\n+as a SADR.\n \n ## Decision\n \n@@ -104,7 +104,7 @@\n 3. **Publishing to PyPI.** Rejected for now: Shiki is distributed as a\n    repository template + git-installable CLI, not a PyPI library; PyPI would add\n    credentials/secrets and a different versioning contract. Can be added later as\n-   its own ADR if a packaged distribution is needed.\n+   its own SADR if a packaged distribution is needed.\n 4. **Dynamic version (read `VERSION` via a build backend `dynamic` field).**\n    Rejected to keep `pyproject.toml` backend-agnostic and dependency-free; the\n    small duplication between `VERSION` and `pyproject.toml` is guarded by the\n@@ -123,4 +123,4 @@\n   releasing versions the whole platform but ships only `install.include` to\n   targets.\n - Future PyPI distribution or a console entry point are deferred and would each\n-  warrant their own ADR.\n+  warrant their own SADR.\ndiff --git a/docs/adr/0008-use-claude-code-as-default-implementer.md b/docs/adr/SADR-0008-use-claude-code-as-default-implementer.md\nindex dedf4510b7c7799521422f622029563d373db031..35c2625d908167fbc58855b5b63e283805f167e8\n--- a/docs/adr/0008-use-claude-code-as-default-implementer.md\n+++ b/docs/adr/SADR-0008-use-claude-code-as-default-implementer.md\n@@ -1,12 +1,12 @@\n-# ADR 0008: Use Claude Code As The Default Implementer Runtime\n+# SADR-0008: Use Claude Code As The Default Implementer Runtime\n \n ## Status\n \n-Proposed (supersedes ADR 0005 for the implementer default; keeps its auth model)\n+Proposed (supersedes SADR-0005 for the implementer default; keeps its auth model)\n \n ## Context\n \n-ADR 0005 made Codex Front the default implementation runtime, with Claude Code\n+SADR-0005 made Codex Front the default implementation runtime, with Claude Code\n restricted to planner and reviewer roles by the runtime registry, and the only\n autonomous implementation adapter was `shiki runner codex`.\n \n@@ -18,13 +18,13 @@\n - The platform's planning, review, and CCA layers already run on Claude Code,\n   and Claude Code's native capabilities (the built-in `/code-review` skill and\n   the Workflow multi-agent orchestration tool) have no Codex equivalent.\n-- The Goal lifecycle redesign (ADR 0009) requires an autonomous post-freeze\n+- The Goal lifecycle redesign (SADR-0009) requires an autonomous post-freeze\n   loop driven from the operator's Claude Code session, including bounded\n   repair dispatch, which must not depend on a runtime that cannot be invoked\n   unattended.\n \n The constitution claims Shiki is runtime-agnostic (\"not a Claude-only\n-workflow\", ADR 0002). Any change to the implementer default must preserve the\n+workflow\", SADR-0002). Any change to the implementer default must preserve the\n registry-based runtime model rather than hardcoding one vendor.\n \n ## Decision\n@@ -39,7 +39,7 @@\n   the task to review or repair-needed.\n - Keep Codex registered as an optional implementer runtime. `shiki runner\n   codex`, the Codex handoff templates, and the ChatGPT OAuth auth model from\n-  ADR 0005 remain supported for tasks explicitly assigned to `codex`.\n+  SADR-0005 remain supported for tasks explicitly assigned to `codex`.\n - Commit to mandating two Claude-native capabilities as loop contract in\n   follow-up Goals (status: the code-review gate was delivered by Goal G-B \u2014\n   skill registry, checklists item PR-12, default required_skills \u2014 while the\n@@ -60,7 +60,7 @@\n \n Out of scope: removing Codex support, changing the CCA runtime (Claude Code\n Action with `CLAUDE_CODE_OAUTH_TOKEN` stays), and API-key based automation\n-(still requires its own ADR per ADR 0005).\n+(still requires its own target ADR per SADR-0005).\n \n ## Consequences\n \n@@ -100,7 +100,7 @@\n   harness rejection of direct `codex exec` is outside Shiki's control, and the\n   plan's mandatory native capabilities cannot run on Codex at all.\n - **Remove Codex entirely**: rejected; it abandons the runtime-agnostic\n-  constitution (ADR 0002) for no operational gain and forecloses\n+  constitution (SADR-0002) for no operational gain and forecloses\n   cross-runtime adversarial review.\n - **GitHub Actions cloud implementation via claude-code-action**: rejected as\n   default; durable evidence is automatic but token cost, Node 24 workflow\ndiff --git a/docs/adr/0009-requirements-definition-and-spec-freeze.md b/docs/adr/SADR-0009-requirements-definition-and-spec-freeze.md\nindex 29b3f7871bef9203a8b4a831a65c39eb2f506e42..52e7dd69ad0aa4efe0b68c62678abbf7df02aaec\n--- a/docs/adr/0009-requirements-definition-and-spec-freeze.md\n+++ b/docs/adr/SADR-0009-requirements-definition-and-spec-freeze.md\n@@ -1,4 +1,4 @@\n-# ADR 0009: Merge Grill And Planning Into Requirements Definition With Spec Freeze\n+# SADR-0009: Merge Grill And Planning Into Requirements Definition With Spec Freeze\n \n ## Status\n \ndiff --git a/docs/adr/0010-external-ai-guardian-review.md b/docs/adr/SADR-0010-external-ai-guardian-review.md\nindex e6108bbe8bb4e73e0ff4684c8f78001a80438c4e..f3510b145664298adafb151b6f0db51b972f01ec\n--- a/docs/adr/0010-external-ai-guardian-review.md\n+++ b/docs/adr/SADR-0010-external-ai-guardian-review.md\n@@ -1,4 +1,4 @@\n-# ADR 0010: External AI Guardian Review As A First-Class Approval Authority\n+# SADR-0010: External AI Guardian Review As A First-Class Approval Authority\n \n ## Status\n \n@@ -8,7 +8,7 @@\n \n Shiki is intended to be an autonomous, recursive, self-improving agent\n framework: authority-in-the-loop, not human-in-the-loop. Until now the Guardian\n-approval required for high/critical-risk merges (guardian-policy.json, ADR\n+approval required for high/critical-risk merges (guardian-policy.json, SADR\n governance) recognized only human authority sources \u2014 a GitHub review approval,\n the `guardian:approved` label applied by a configured Guardian, or a comment\n carrying `Guardian approval granted` plus the current head SHA.\n@@ -89,7 +89,7 @@\n   autonomous design the operator intends and produced the impersonation\n   incident.\n - **Record AI approval as an operator comment**: rejected as forgery of human\n-  authority \u2014 the exact failure this ADR corrects.\n+  authority \u2014 the exact failure this SADR corrects.\n - **Deliver the artifact as a committed `.shiki/` file**: rejected; a committed\n   approval inside the PR being approved is forgeable by anyone who can push.\n - **A dedicated reviewer bot identity instead of a relayed comment**: deferred;\ndiff --git a/docs/adr/0011-autonomous-quality-gate-evidence.md b/docs/adr/SADR-0011-autonomous-quality-gate-evidence.md\nindex bca0246c5d46e44afbde15486a75eec425fc02f5..9c93eac16d8c3a6d4b11bd7224e3955102cd7db4\n--- a/docs/adr/0011-autonomous-quality-gate-evidence.md\n+++ b/docs/adr/SADR-0011-autonomous-quality-gate-evidence.md\n@@ -1,4 +1,4 @@\n-# ADR 0011: Autonomous Quality-Gate Evidence \u2014 Deterministic Facts And An Independent Verifier\n+# SADR-0011: Autonomous Quality-Gate Evidence \u2014 Deterministic Facts And An Independent Verifier\n \n ## Status\n \ndiff --git a/docs/adr/0012-autonomous-loop-closeout-to-main.md b/docs/adr/SADR-0012-autonomous-loop-closeout-to-main.md\nindex 637fad6dde4989c9c2bf0f81f676d8d29991123c..ec7d85f81030a676bcbba28b43eb365422a36eac\n--- a/docs/adr/0012-autonomous-loop-closeout-to-main.md\n+++ b/docs/adr/SADR-0012-autonomous-loop-closeout-to-main.md\n@@ -1,4 +1,4 @@\n-# 0012 \u2014 Autonomous loop pushes goal completion to main via a closeout PR\n+# SADR-0012 \u2014 Autonomous loop pushes goal completion to main via a closeout PR\n \n - Status: accepted\n - Date: 2026-06-18\ndiff --git a/docs/adr/0013-bootstrap-grants-actions-approve-pull-requests.md b/docs/adr/SADR-0013-bootstrap-grants-actions-approve-pull-requests.md\nindex b6c69d64f1c5ced346b88d6859586715b3b5bd0e..cf48ff4a6716267f539ca58edd1a852e7c7a2d64\n--- a/docs/adr/0013-bootstrap-grants-actions-approve-pull-requests.md\n+++ b/docs/adr/SADR-0013-bootstrap-grants-actions-approve-pull-requests.md\n@@ -1,4 +1,4 @@\n-# ADR 0013: Bootstrap Grants GitHub Actions Permission To Create And Approve Pull Requests\n+# SADR-0013: Bootstrap Grants GitHub Actions Permission To Create And Approve Pull Requests\n \n ## Status\n \n@@ -31,7 +31,7 @@\n security tradeoff: it lets repository automation satisfy a human-review gate. In\n a multi-maintainer repo that could let a compromised or buggy workflow\n self-approve changes. So whether bootstrap turns it on by default \u2014 and with\n-what default token scope \u2014 is an ADR-worthy decision, not an implementation\n+what default token scope \u2014 is a SADR-worthy decision, not an implementation\n detail.\n \n ## Decision\n@@ -48,7 +48,7 @@\n Specifically:\n \n - **`can_approve_pull_request_reviews` defaults to `true`.** Shiki's default\n-  posture is solo/AFK autonomous operation (ADR 0008/0009/0012), where the CCA\n+  posture is solo/AFK autonomous operation (SADR-0008/0009/0012), where the CCA\n   Review Bridge is the only path that can satisfy `required_review: true` after\n   the CCA and Guardian gates have already passed. The default makes a\n   freshly-bootstrapped repo able to run the autonomous loop end to end without a\ndiff --git a/docs/adr/0014-codex-app-external-ai-guardian-ui-adapter.md b/docs/adr/SADR-0014-codex-app-external-ai-guardian-ui-adapter.md\nindex 238db06279334caa341c117362df9271382a7fcd..65f12e9939321e1b3e389a34c822fc1549e5155d\n--- a/docs/adr/0014-codex-app-external-ai-guardian-ui-adapter.md\n+++ b/docs/adr/SADR-0014-codex-app-external-ai-guardian-ui-adapter.md\n@@ -1,4 +1,4 @@\n-# ADR 0014: Use Codex App As The External AI Guardian UI Adapter\n+# SADR-0014: Use Codex App As The External AI Guardian UI Adapter\n \n ## Status\n \ndiff --git a/docs/adr/0015-contract-approval-before-dispatch.md b/docs/adr/SADR-0015-contract-approval-before-dispatch.md\nindex fad6c656741a20a7684ba16080a7db159f63a52f..6061271a46d3f1d6ae7174477de3fb75673123a6\n--- a/docs/adr/0015-contract-approval-before-dispatch.md\n+++ b/docs/adr/SADR-0015-contract-approval-before-dispatch.md\n@@ -1,4 +1,4 @@\n-# ADR 0015: Approve The Task Contract Before Dispatch, Not The Diff After It\n+# SADR-0015: Approve The Task Contract Before Dispatch, Not The Diff After It\n \n ## Status\n \n@@ -9,7 +9,7 @@\n Guardian approval for high/critical risk is expressible only on a pull request.\n Every approval source in `guardian-policy.json` \u2014 GitHub review, the\n `guardian:approved` label, the `Guardian approval granted` comment, and the\n-`external_ai_guardian_review` artifact (ADR 0010/0014) \u2014 is bound to a PR, and\n+`external_ai_guardian_review` artifact (SADR-0010/0014) \u2014 is bound to a PR, and\n three of the four require the current head SHA. Approval therefore cannot exist\n before an implementation PR exists.\n \n@@ -81,7 +81,7 @@\n An envelope breach must surface only on the non-repairable policy gate, never on\n the repairable `MergeGate metadata check`. Otherwise the Goal Loop routes it to\n `dispatch_repair` \u2014 instructing a runner to make the breach go away \u2014 which is\n-exactly what this ADR forbids.\n+exactly what this SADR forbids.\n \n **Contract immutability.** After registration, an implementation PR must not\n change the governance fields of its own task contract (`scope`, `non_goals`,\n@@ -116,7 +116,7 @@\n fails any verdict that is not `complete`, and the signal the CCA prompt reads is\n computed from `evaluate_guardian_approval` alone. Without the OR-merge at the\n signal, a contract-approved PR still returns `needs_guardian`, the CCA job fails,\n-and the MergeGate policy check never runs \u2014 the gate this ADR changes would never\n+and the MergeGate policy check never runs \u2014 the gate this SADR changes would never\n be reached. The CCA *prompt* needs no change: a `contract_approval` entry in the\n signal's existing `sources` array satisfies its current wording.\n \n@@ -130,7 +130,7 @@\n written, and an approved contract lets its implementation PRs merge through the\n same automatic path as low/medium risk. The wasted implement-then-block cycle\n disappears. Guardian review shifts from reading diffs to reading contracts, which\n-is both earlier and smaller; the external AI Guardian path (ADR 0010/0014) works\n+is both earlier and smaller; the external AI Guardian path (SADR-0010/0014) works\n on contracts with no change.\n \n We accept that the Guardian authorizes a class of change without seeing its\n@@ -146,7 +146,7 @@\n adversarial tests for the new approval path equivalent in rigor to\n `test_shiki_governance_evidence.sh`.\n \n-Bootstrap costs, which apply only while this ADR is being implemented. The tasks\n+Bootstrap costs, which apply only while this SADR is being implemented. The tasks\n that build Contract Approval merge under the existing rules, so the ones\n dispatched after the pre-dispatch gate lands but before the approval path lands\n need a Guardian-approved Contract PR *and* a live Guardian approval on their\n@@ -210,7 +210,7 @@\n risk; they do not bound authorized work.\n \n **Auto-escalate risk on envelope breach instead of stopping.** Returns the\n-workflow to a diff-time Guardian decision \u2014 the exact behavior this ADR removes.\n+workflow to a diff-time Guardian decision \u2014 the exact behavior this SADR removes.\n \n **Resolve governance fields from the base snapshot only.** Rejected against the\n registration flow: implementation PRs add their own contract, so the base has no\ndiff --git a/docs/adr/0016-locks-bound-product-paths.md b/docs/adr/SADR-0016-locks-bound-product-paths.md\nindex b5e850f3f1f7f4ef6e7f9d1763a29a51bd300c45..46b40a9b99a7aae644c02bb458f5473ff0a90ce2\n--- a/docs/adr/0016-locks-bound-product-paths.md\n+++ b/docs/adr/SADR-0016-locks-bound-product-paths.md\n@@ -1,4 +1,4 @@\n-# ADR 0016: Locks bound product paths; the Shiki mirror is bound by record identity\n+# SADR-0016: Locks bound product paths; the Shiki mirror is bound by record identity\n \n ## Status\n \n@@ -6,8 +6,8 @@\n \n - Date: 2026-07-28\n - Deciders: operator (mizutani-140), Claude (planner)\n-- Related: ADR 0004 (GitHub-first with a `.shiki` mirror), ADR 0009 (Spec Freeze),\n-  ADR 0012 (autonomous loop closeout to main), ADR 0015 (Contract Approval before\n+- Related: SADR-0004 (GitHub-first with a `.shiki` mirror), SADR-0009 (Spec Freeze),\n+  SADR-0012 (autonomous loop closeout to main), SADR-0015 (Contract Approval before\n   dispatch), remediation goal task R-02, the live PR #179 deadlock.\n \n ## Context\n@@ -90,11 +90,11 @@\n files, documentation files and named source modules are nameable; runtime-\n allocated ledger, runner and report ids are not \u2014 and those live in `.shiki`,\n which locks no longer govern. Existing frozen contracts are brought into line by\n-a one-time Spec Amendment per plan, which must land before ADR 0015's contract\n+a one-time Spec Amendment per plan, which must land before SADR-0015's contract\n immutability makes `locks` unwritable from the implementation PR.\n \n **Lock state is resolved from the durable side.** MergeGate resolves the lock\n-registry, and ADR 0015's governance fields, from the base `.shiki` snapshot when\n+registry, and SADR-0015's governance fields, from the base `.shiki` snapshot when\n present, falling back to head only when absent. The `MergeGate metadata check`\n job is given `--base-shiki`, which it does not receive today. Locks are not\n derived from the live open-pull-request set: a pull request body is untrusted\n@@ -108,7 +108,7 @@\n Explicitly out of scope: moving MergeGate's contract resolution off the PR\n checkout wholesale; making the mirror a projection of GitHub state; introducing\n new lock kinds; moving ledger evidence out of the repository; and unwinding\n-ADR 0012's closeout pull request. Registration of multi-task goals is ADR 0015's\n+SADR-0012's closeout pull request. Registration of multi-task goals is SADR-0015's\n Contract PR and is not re-decided here.\n \n ## Consequences\n@@ -127,7 +127,7 @@\n \n Risks accepted. First, goal-scoped identity is only as strong as `goal_id`,\n which is resolved from the PR body and the head task file; the mitigation is to\n-add `goal_id` to ADR 0015's frozen governance field set, and until that lands a\n+add `goal_id` to SADR-0015's frozen governance field set, and until that lands a\n task file can name a foreign goal. Second, base-when-present resolution is\n unavailable on fork pull requests, where the head is contributor-controlled;\n fork pull requests already cannot complete a Shiki merge, and this decision does\n@@ -139,7 +139,7 @@\n path, not by implementation pull requests.\n \n New obligations: one Spec Amendment per frozen plan to rewrite `locks`, ordered\n-before ADR 0015 T1; extension of the adversarial suites in\n+before SADR-0015 T1; extension of the adversarial suites in\n `scripts/test_shiki_adversarial_state.sh` and\n `scripts/test_shiki_governance_evidence.sh` to cover the eleven newly-scoped\n directories; and updating `scripts/test_shiki_loop_lock_guard.sh`,\n@@ -151,32 +151,32 @@\n whether the loop's transitive sync of ledger `evidence` references can be\n deleted once the Contract PR carries its own source plan.\n \n-## Relationship to ADR 0004\n+## Relationship to SADR-0004\n \n-This ADR **clarifies something ADR 0004 left unspecified**. ADR 0004 decides that\n+This SADR **clarifies something SADR-0004 left unspecified**. SADR-0004 decides that\n GitHub is the operational source of truth and that each target repository keeps a\n `.shiki/` mirror. It says nothing about pull requests writing the mirror, does\n not name MergeGate, and does not make the mirror an input to a gate. Nothing in\n-ADR 0004's Decision or Consequences is amended, superseded or weakened: the\n+SADR-0004's Decision or Consequences is amended, superseded or weakened: the\n mirror still exists, still records the same artifacts, and pull request\n-boundaries remain the coordination surface. What this ADR settles is the question\n+boundaries remain the coordination surface. What this SADR settles is the question\n 0004 never reached \u2014 by what authority a pull request may write a mirror record \u2014\n and it answers it the way `.shiki/manifest.json` already declares\n (`pr_mutation: current-task-or-current-goal-only`).\n \n-It **amends ADR 0012**, whose Decision item 5 states that \"Loop-executed tasks\n+It **amends SADR-0012**, whose Decision item 5 states that \"Loop-executed tasks\n declare the `path:.shiki/**` lock, which covers every `.shiki` file the closeout\n stages ... so `files_outside_locks` is satisfied without per-file lock\n bookkeeping.\" That clause is replaced: the closeout's mirror writes are\n-authorized by record identity, not by a declared lock. ADR 0012's substantive\n+authorized by record identity, not by a declared lock. SADR-0012's substantive\n decision \u2014 that completion reaches `main` through a closeout pull request rather\n than local attestation \u2014 is preserved unchanged.\n \n-It is **complementary to ADR 0015**, not a substitute. ADR 0015 owns contract\n+It is **complementary to SADR-0015**, not a substitute. SADR-0015 owns contract\n immutability, the pre-dispatch gate, the Contract PR and the Scope Envelope. This\n-ADR owns what a lock means and who may write the mirror. The one ordering\n+SADR owns what a lock means and who may write the mirror. The one ordering\n constraint between them is that the lock-narrowing Spec Amendment must precede\n-ADR 0015 T1's freezing of the `locks` field.\n+SADR-0015 T1's freezing of the `locks` field.\n \n ## Alternatives Considered\n \n@@ -186,7 +186,7 @@\n the only writer that can register them may run. Migrating the Guardian signal to\n that store makes every registration PR fail closed to `needs_guardian` at every\n risk level, because the task file is by definition absent from the store, which\n-removes the low/medium autonomous path ADR 0008/0009 exists to provide. And it\n+removes the low/medium autonomous path SADR-0008/0009 exists to provide. And it\n leaves schemas, manifest, config, guardian policy, templates, migrations, plans,\n worktrees, runner records, runs and handoffs with no writer at all in a\n repository whose product is `.shiki`. Measured benefit on the frozen backlog:\n@@ -199,14 +199,14 @@\n namespace is not in fact ownership-partitioned \u2014 56 ledger entries carry no\n `task_id`, and one goal DAG is writable by 35 registered tasks. The correct\n expression of its insight is that locks do not govern the mirror at all, which is\n-what this ADR decides.\n+what this SADR decides.\n \n **Make the mirror a projection written only by a registration writer and a\n post-merge projection job.** Rejected. It requires a bypass actor with write\n access to the protected default branch; it makes its own master invariant\n readable from a manifest that pull requests may edit; it deletes both reconcile\n modes and leaves a corrupted mirror with no in-band repair path, contradicting\n-ADR 0004's consequence that mirror conflicts are repaired against GitHub state;\n+SADR-0004's consequence that mirror conflicts are repaired against GitHub state;\n and its content-derived ids are rejected by `validate_shiki`'s currently-green id\n pattern.\n \ndiff --git a/docs/adr/0017-bookkeeping-closeouts-do-not-inherit-risk.md b/docs/adr/SADR-0017-bookkeeping-closeouts-do-not-inherit-risk.md\nindex 554e3a0d526a72ca067108c5464cc68c3edcf174..b9676857a4713850e063af7c39c8e0b01ecb4c66\n--- a/docs/adr/0017-bookkeeping-closeouts-do-not-inherit-risk.md\n+++ b/docs/adr/SADR-0017-bookkeeping-closeouts-do-not-inherit-risk.md\n@@ -1,4 +1,4 @@\n-# ADR 0017: A Bookkeeping Closeout Does Not Inherit Its Task's Risk\n+# SADR-0017: A Bookkeeping Closeout Does Not Inherit Its Task's Risk\n \n ## Status\n \n@@ -6,7 +6,7 @@\n \n ## Context\n \n-ADR 0012 made the goal loop push a task's completion to the default branch through a\n+SADR-0012 made the goal loop push a task's completion to the default branch through a\n closeout pull request: after the implementation PR merges, the loop opens a second PR\n carrying `task.status = done`, `lock.state = released`, and \u2014 when the task completes\n its goal \u2014 `goal.status = complete` plus one scorecard report. Nothing else. The\n@@ -49,14 +49,14 @@\n 4. The task file's governance fields (`scope`, `non_goals`, `required_skills`,\n    `risk_level`, `locks`, `acceptance_checks`, `test_command`) are byte-identical to\n    the base snapshot. Only `status`, `expected_pr`, `closeout_pr`, `ledger_evidence`\n-   and `expected_branch` may differ \u2014 the ADR 0015 contract-immutability rule,\n+   and `expected_branch` may differ \u2014 the SADR-0015 contract-immutability rule,\n    unchanged.\n \n    `expected_branch` is in that set because a closeout is *by construction* on a\n    different branch from the implementation it closes, and the loop rewrites the\n    field accordingly. It is safe to let it differ because MergeGate independently\n    requires `expected_branch` to equal the PR's actual head ref, so a PR cannot set\n-   it to anything other than the branch it is really on. The first draft of this ADR\n+   it to anything other than the branch it is really on. The first draft of this SADR\n    omitted it, which made the exemption unreachable: every real closeout failed\n    condition 4 on a field the loop always changes. See PR #209.\n 5. The state transitions are exactly the terminal set: task `review` \u2192 `done`, lock\n@@ -113,12 +113,12 @@\n classification must be derived, not asserted.\n \n **Fold the closeout into the implementation PR** so no second PR exists. Rejected under\n-ADR 0012, which recorded why: a task file that is already `done` on the implementation\n+SADR-0012, which recorded why: a task file that is already `done` on the implementation\n branch short-circuits the loop's review \u2192 CCA \u2192 merge \u2192 done ordering into a local\n goal-completion before any merge happens.\n \n **Have the loop auto-merge closeouts with elevated credentials.** Rejected outright: that\n-is a runtime authoring its own approval, which ADR 0010 and the constitution's\n+is a runtime authoring its own approval, which SADR-0010 and the constitution's\n approval-record prohibition forbid. It would also be indistinguishable, in the evidence,\n from the forgery those rules exist to prevent.\n \ndiff --git a/docs/adr/0018-carried-guardian-approval-across-a-proven-base-sync.md b/docs/adr/SADR-0018-carried-guardian-approval-across-a-proven-base-sync.md\nindex 22358562da390ed8b45cd600da08a43d378b4680..e6e2907cefa776460af73ddf7d39db605f3c39b0\n--- a/docs/adr/0018-carried-guardian-approval-across-a-proven-base-sync.md\n+++ b/docs/adr/SADR-0018-carried-guardian-approval-across-a-proven-base-sync.md\n@@ -1,4 +1,4 @@\n-# ADR 0018: Carried Guardian Approval Across A Proven Base Sync\n+# SADR-0018: Carried Guardian Approval Across A Proven Base Sync\n \n ## Status\n \n@@ -34,7 +34,7 @@\n Guardian evaluator that consumes it (`shiki_guardian.evaluate_guardian_approval`,\n the `guardian_comment_carried` source) were built and tested in isolation. Neither\n does anything until a caller passes the flags that turn the carry on, so the\n-feature was invisible in CI. This ADR records the decision the two of them\n+feature was invisible in CI. This SADR records the decision the two of them\n implement, and this task wires it into the four CI invocations that judge Guardian\n approval.\n \ndiff --git a/docs/adr/README.md b/docs/adr/README.md\nindex c70e057a4520c36e042ab68c53cd4a21ef709b71..5da43856836e2ce6a150f8127766e2f88355ab4b\n--- a/docs/adr/README.md\n+++ b/docs/adr/README.md\n@@ -1,40 +1,41 @@\n-# Architecture Decision Records\n+# Decision Record Namespaces\n \n-This directory holds Shiki's Architecture Decision Records (ADRs): durable\n-records of hard-to-reverse platform decisions. ADRs are part of the Shiki source\n-of truth alongside GitHub state, the `.shiki/` mirror, and `CONTEXT.md`.\n+This directory holds two independent decision namespaces:\n \n-## How ADRs Work\n+- Shiki Architecture Decision Records (SADRs), named\n+  `SADR-NNNN-<short-slug>.md`, record hard-to-reverse Shiki platform decisions.\n+- Target Architecture Decision Records (ADRs), named\n+  `NNNN-<short-slug>.md`, belong to the target product.\n \n-- New ADRs are added as `docs/adr/NNNN-<short-slug>.md`, where `NNNN` is the next\n-  zero-padded sequence number.\n-- The **canonical list of ADRs is the set of files matching `docs/adr/*.md`** in\n-  this directory. Browse `docs/adr/*.md` for the authoritative, current set.\n-- Use [`template.md`](template.md) as the starting point for a new ADR.\n-- Record an ADR when a decision is hard to reverse, surprising without context,\n-  and a real tradeoff. Do not write an ADR for routine or easily reversible\n-  choices.\n+The same four-digit number may appear once in each namespace. A platform\n+`SADR-NNNN` citation resolves only to `SADR-NNNN-*.md`; a target `ADR NNNN`\n+citation resolves only to `NNNN-*.md`.\n \n-Using the `docs/adr/*.md` glob as the source of truth means other PRs can add an\n-ADR without editing this index file, avoiding merge contention on a\n-hand-maintained enumeration.\n+## How Decision Records Work\n \n-## Current ADRs (snapshot)\n-\n-The list below is a convenience snapshot and may lag behind the directory. When\n-in doubt, list `docs/adr/*.md`.\n+- New Shiki platform decisions use the next zero-padded SADR number.\n+- New target product decisions use the target's own ADR sequence.\n+- The canonical list is the files in `docs/adr/`; no hand-maintained index is\n+  authoritative.\n+- Use [`template.md`](template.md) as the starting point and choose the filename\n+  namespace that owns the decision.\n+- Record a decision when it is hard to reverse, surprising without context, and\n+  a real tradeoff. Do not write a decision record for routine or easily\n+  reversible choices.\n \n-- [0001 \u2014 Keep Shiki As The Platform Name](0001-keep-shiki-as-platform-name.md)\n-- [0002 \u2014 Use AGENTS As The Shared Agent Constitution](0002-use-agents-as-shared-constitution.md)\n-- [0003 \u2014 Require Skill Gate For MergeGate Readiness](0003-require-skill-gate-for-mergegate-readiness.md)\n-- [0004 \u2014 Use GitHub First With A Repository-Local Shiki Mirror](0004-use-github-first-with-shiki-mirror.md)\n-- [0005 \u2014 Use Codex As Front And Claude Code Action As The GitHub Runtime](0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md)\n-- [0006 \u2014 Separate Codex Implementation, CCA Completion, And MergeGate Authorization](0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md)\n+Using the directory as the source of truth lets independent changes add records\n+without contending on an enumerated index.\n \n-## Adding A New ADR\n+## Adding A Shiki SADR\n \n-1. Copy `template.md` to `docs/adr/NNNN-<short-slug>.md`.\n+1. Copy `template.md` to `docs/adr/SADR-NNNN-<short-slug>.md`.\n 2. Fill in the title, status, context, decision, and consequences.\n-3. Set the status to `Proposed`, then `Accepted` once the decision is approved.\n-4. Open a PR following `CONTRIBUTING.md`. You do not need to edit this index for\n-   the ADR to be discoverable via `docs/adr/*.md`.\n+3. Set the status to `Proposed`, then `Accepted` once the proper authority has\n+   approved the decision.\n+4. Open a PR following `CONTRIBUTING.md`.\n+\n+## Adding A Target ADR\n+\n+1. Copy `template.md` to `docs/adr/NNNN-<short-slug>.md` using the target's own\n+   sequence.\n+2. Cite it as `ADR NNNN`; do not prefix its filename with `SADR-`.\ndiff --git a/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md b/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\nnew file mode 100644\nindex 0000000000000000000000000000000000000000..7455ce1f3bed02ddddb851ac21a1d1a648c9aeba\n--- /dev/null\n+++ b/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\n@@ -0,0 +1,55 @@\n+# SADR-0019: Separate Shiki Platform SADRs From Target ADRs\n+\n+## Status\n+\n+Accepted\n+\n+## Context\n+\n+Shiki installs its platform decision records into `docs/adr/` in every target\n+repository. Shiki and its targets previously used the same numeric\n+`NNNN-<slug>.md` namespace. A target that already owned ADRs 0013 through 0018\n+therefore acquired two records for each number during installation, making an\n+`ADR NNNN` citation ambiguous. Renumbering target decisions would rewrite target\n+history, while renumbering Shiki decisions per target would destroy their stable\n+platform identity.\n+\n+Upgrades also need to remove the former Shiki-owned numeric paths without ever\n+mistaking a target-owned ADR for platform content. The install stamp already\n+records exact shipped paths and content digests, so it can prove ownership only\n+when both still match.\n+\n+## Decision\n+\n+Shiki platform decisions use the `SADR` namespace:\n+\n+- records are named `docs/adr/SADR-NNNN-<slug>.md`;\n+- live platform citations use `SADR-NNNN`;\n+- target product decisions retain `docs/adr/NNNN-<slug>.md` and `ADR NNNN`;\n+- the reference validator resolves the two namespaces independently, so the same\n+  four-digit number may exist once in each namespace.\n+\n+Before a forced upgrade writes anything, the installer inventories the exact\n+former numeric Shiki ADR paths. It may delete a legacy path only when the\n+existing install stamp contains that exact path and its recorded SHA-256 digest\n+matches the file currently on disk. All ownership blockers are reported\n+together and abort the upgrade before its first write. A non-forced install\n+never performs this cleanup.\n+\n+Historical `.shiki/` evidence remains unchanged because it records the namespace\n+that existed when that evidence was produced.\n+\n+## Consequences\n+\n+- Targets may use any ADR number without colliding with Shiki's platform SADRs.\n+- Fresh installs preserve target ADR bytes and install a stable SADR set.\n+- Forced upgrades fail closed when legacy ownership cannot be proven, and a\n+  successful upgrade refreshes the stamp without removed legacy paths.\n+- Current governance, packaging, workflow, prompt, script, and skill references\n+  must move with the platform records.\n+\n+## Alternatives Considered\n+\n+- Collision-only refusal leaves affected targets permanently unupgradable.\n+- Renumbering target ADRs mutates target-owned history.\n+- Per-target Shiki renumbering makes platform citations non-portable.\ndiff --git a/docs/adr/template.md b/docs/adr/template.md\nindex a92a97daddd0b00ab9dd626cc8d86942da2eafed..17dd79175b5fee2105935557f691b40cf9492942\n--- a/docs/adr/template.md\n+++ b/docs/adr/template.md\n@@ -1,14 +1,14 @@\n-# NNNN: <Short Decision Title>\n+# <SADR-NNNN or NNNN>: <Short Decision Title>\n \n ## Status\n \n-Proposed | Accepted | Superseded by ADR-NNNN\n+Proposed | Accepted | Superseded by SADR-NNNN or target ADR NNNN\n \n ## Context\n \n What is the situation, constraint, or force that requires a decision? Reference\n-relevant domain terms (`CONTEXT.md`), prior ADRs, and code reality. State the\n-problem, not the solution.\n+relevant domain terms (`CONTEXT.md`), prior Shiki SADRs or target ADRs, and code\n+reality. State the problem, not the solution.\n \n ## Decision\n \ndiff --git a/docs/agents/autonomous-loop-e2e-acceptance.md b/docs/agents/autonomous-loop-e2e-acceptance.md\nindex bbf84c6081987f159ac6ad9ecf3c82b7aa72e7e8..5c13c1d02a05a90b0dc46cbef7c95a9846d0c237\n--- a/docs/agents/autonomous-loop-e2e-acceptance.md\n+++ b/docs/agents/autonomous-loop-e2e-acceptance.md\n@@ -40,8 +40,8 @@\n 3. `gh auth status` is logged in with merge permission on the repo.\n 4. Branch protection + required checks are configured exactly as the goal-loop\n    required-check set expects (see `docs/agents/branch-protection-smoke.md`).\n-5. The repo `.shiki/policy` permits autonomous low/medium-risk merges (ADR\n-   0008/0009); the MergeGate policy check is the recorded-authority gate.\n+5. The repo `.shiki/policy` permits autonomous low/medium-risk merges\n+   (SADR-0008/0009); the MergeGate policy check is the recorded-authority gate.\n \n The operator's only actions are: register the task and start the loop. **No\n manual pre-clearing** means after `shiki loop run` starts, the operator does not\ndiff --git a/docs/agents/bootstrap-command.md b/docs/agents/bootstrap-command.md\nindex 05f7f203a52cf412b9f97084a6e131ed0f785afd..f7aa0b734dfebee4a3b139335b66ee3415f48713\n--- a/docs/agents/bootstrap-command.md\n+++ b/docs/agents/bootstrap-command.md\n@@ -189,7 +189,9 @@\n ```\n \n Use this only for tests, fixtures, or explicit local-only template inspection.\n-Use `--force` only when you intentionally want to overwrite existing target files.\n+Use `--force` only when you intentionally want to refresh the target template.\n+Legacy numeric Shiki decision cleanup is stamp-and-digest authorized before any\n+write; target ADR paths are not cleanup candidates.\n \n ## Slash Command\n \ndiff --git a/docs/agents/checklists.md b/docs/agents/checklists.md\nindex 4a7f03ade346f53a1c1d82981f3fd744f9ab4547..0c5ec6c53dc7197b7b7a7d18e667b325babd6c45\n--- a/docs/agents/checklists.md\n+++ b/docs/agents/checklists.md\n@@ -32,18 +32,18 @@\n | ID | Item | Blocking |\n | --- | --- | --- |\n | GD-01 | Plan was challenged against `CONTEXT.md`. | blocking for non-trivial Goals |\n-| GD-02 | Relevant ADRs were read or absence was recorded. | blocking for non-trivial Goals |\n+| GD-02 | Relevant SADRs and target ADRs were read or absence was recorded. | blocking for non-trivial Goals |\n | GD-03 | Ambiguous terms were resolved or converted to HITL blockers. | blocking |\n | GD-04 | Edge scenarios were considered. | advisory |\n | GD-05 | Code reality was checked when code could answer. | blocking when applicable |\n | GD-06 | New domain terms were added to `CONTEXT.md` when needed. | blocking when applicable |\n-| GD-07 | ADR was created or explicitly skipped using the ADR criteria. | blocking when applicable |\n+| GD-07 | A SADR or target ADR was created or explicitly skipped using the decision-record criteria. | blocking when applicable |\n \n ## CI \u2014 Context & Impact Checklist\n \n | ID | Item | Blocking |\n | --- | --- | --- |\n-| CI-01 | Relevant docs and ADRs identified. | blocking |\n+| CI-01 | Relevant docs, SADRs, and target ADRs identified. | blocking |\n | CI-02 | Relevant modules, interfaces, seams, callers, and tests identified. | blocking |\n | CI-03 | Dependencies and blocked-by relationships identified. | blocking |\n | CI-04 | Candidate locks identified. | blocking |\n@@ -62,7 +62,7 @@\n | PRD-04 | Implementation decisions are recorded without volatile code detail. | blocking |\n | PRD-05 | Testing decisions are recorded. | blocking |\n | PRD-06 | Out-of-scope items are explicit. | blocking |\n-| PRD-07 | Relevant glossary/ADR links are present when applicable. | blocking |\n+| PRD-07 | Relevant glossary/SADR/target-ADR links are present when applicable. | blocking |\n \n ## SF \u2014 Spec Freeze Checklist\n \n@@ -98,7 +98,7 @@\n | H-02 | Branch/worktree target is present. | blocking |\n | H-03 | Scope and non-goals are present. | blocking |\n | H-04 | Dependencies and locks are present. | blocking |\n-| H-05 | Relevant docs, ADRs, modules, and tests are referenced. | blocking |\n+| H-05 | Relevant docs, SADRs, target ADRs, modules, and tests are referenced. | blocking |\n | H-06 | Required skills are stated. | blocking |\n | H-07 | TDD expectations are stated. | blocking when behavior work |\n | H-08 | Acceptance criteria are included. | blocking |\ndiff --git a/docs/agents/codex-handoff.md b/docs/agents/codex-handoff.md\nindex ede468f47cb277f69a857e8c45df703e4ed0151d..6f97bdffb67e44d7edea2c71e01ea28933c4eb6c\n--- a/docs/agents/codex-handoff.md\n+++ b/docs/agents/codex-handoff.md\n@@ -46,7 +46,7 @@\n \n ## Context\n - Domain terms:\n-- Relevant ADRs:\n+- Relevant SADRs and target ADRs:\n - Relevant docs:\n - Relevant modules/interfaces/seams:\n - Similar tests/prior art:\ndiff --git a/docs/agents/completion-check-agent.md b/docs/agents/completion-check-agent.md\nindex 13e4c035db35025f4e3cdcc5adfb24314ac4d521..61bfa1218cf36ca605250c91919e620f33238aa0\n--- a/docs/agents/completion-check-agent.md\n+++ b/docs/agents/completion-check-agent.md\n@@ -32,7 +32,7 @@\n - `AGENTS.md`\n - `CLAUDE.md`\n - `CONTEXT.md`\n-- relevant ADRs\n+- relevant SADRs and target ADRs\n - parent Goal issue\n - PRD issue or document\n - task issue\ndiff --git a/docs/agents/decision-control.md b/docs/agents/decision-control.md\nindex 7d8ee5ab03a5ccbcbd04056ae61de03785a2a9c9..8c94ecda00fffaa0e92c325ab83aa9776bd1dac1\n--- a/docs/agents/decision-control.md\n+++ b/docs/agents/decision-control.md\n@@ -11,7 +11,7 @@\n | Decision | Owner | When | Required inputs | Output | Enforcement surface |\n | --- | --- | --- | --- | --- | --- |\n | Is the Goal clear enough to plan? | Planner + Guardian when needed | Goal intake | User request, existing docs, risk signals | Goal readiness verdict | Goal checklist, issue template, ledger |\n-| Must `grill-with-docs` run? | Planner / Skill Gate | Before PRD or issue split | Ambiguity, domain terms, ADR conflicts, unclear boundaries | Skill invocation or explicit skip rationale | Skill Gate checklist |\n+| Must `grill-with-docs` run? | Planner / Skill Gate | Before PRD or issue split | Ambiguity, domain terms, SADR/target-ADR conflicts, unclear boundaries | Skill invocation or explicit skip rationale | Skill Gate checklist |\n | Is a PRD required? | Planner | After Goal context stabilizes | Goal, `grill-with-docs` output, Context & Impact output | PRD required / not required | PRD checklist, CCA evidence check |\n | Can the spec be frozen? | Operator only | End of Requirements Definition, before issue split | PRD/requirements summary, scope inventory (SF-02), open-question list | `spec_freeze` block in the plan + ledger evidence | `require_grilled_plan` (plan ingest/run), SF checklist |\n | Can a frozen spec change? | Operator only (Spec Amendment) | When implementation contradicts the frozen spec | Contested decisions, scoped re-grill result | recorded amendment + re-stamped freeze | SF checklist, ledger evidence |\ndiff --git a/docs/agents/domain.md b/docs/agents/domain.md\nindex 258da8ea81e87ac0a6b0b5b5e5c2fde980eaabb7..d57b0cf17ff3e1fa9e6e5885372bf083d8295421\n--- a/docs/agents/domain.md\n+++ b/docs/agents/domain.md\n@@ -5,7 +5,9 @@\n ## Layout\n \n - `CONTEXT.md` defines Shiki language. Keep it implementation-free.\n-- `docs/adr/` records hard-to-reverse decisions.\n+- `docs/adr/SADR-NNNN-*.md` records Shiki platform decisions.\n+- `docs/adr/NNNN-*.md` records target product decisions without sharing the\n+  SADR namespace.\n - `AGENTS.md` is the runtime-neutral agent constitution.\n - `CLAUDE.md` is the Claude Code wrapper around `AGENTS.md`.\n - `.shiki/` is the Target Repository mirror for Goals, PRDs, plans, Task DAGs, contracts, locks, worktree records, ledger entries, CCA verdicts, repair packets, reports, and handoffs.\n@@ -15,6 +17,6 @@\n \n - Use Shiki terms exactly when they exist.\n - Add glossary terms to `CONTEXT.md` only when the term is part of the domain, not a generic programming concept.\n-- Create ADRs only for hard-to-reverse, surprising, tradeoff-based decisions.\n+- Create SADRs or target ADRs only for hard-to-reverse, surprising, tradeoff-based decisions.\n - Keep product-specific language in the Target Repository, not in Shiki Template docs.\n-- If an ADR conflicts with a proposed implementation, surface the conflict before editing.\n+- If a SADR or target ADR conflicts with a proposed implementation, surface the conflict before editing.\ndiff --git a/docs/agents/execution-control.md b/docs/agents/execution-control.md\nindex ad8ee3cdb36be56fedc1f46408ca92b387d74047..bc63c3a73661d12b567016ba299f1a6b66bf9088\n--- a/docs/agents/execution-control.md\n+++ b/docs/agents/execution-control.md\n@@ -63,7 +63,7 @@\n | Branch name | Orchestrator | Orchestrator | Before implementer dispatch | branch policy | branch ref in task/ledger |\n | Lock reservation | Context & Impact | MergeGate | Before dispatch and before merge | `.shiki/locks/`, labels, PR metadata | lock id, owner, scope |\n | CI profile | Planner | MergeGate | During issue triage, rechecked at PR | labels, `.shiki/policy.yaml`, workflow matrix | required check list |\n-| Required status checks | Repo maintainer / Guardian | GitHub branch protection / rulesets | Repository setup, updated via ADR | GitHub settings | branch/ruleset config, ADR |\n+| Required status checks | Repo maintainer / Guardian | GitHub branch protection / rulesets | Repository setup, updated via target ADR | GitHub settings | branch/ruleset config, target ADR |\n | CCA judgment | CCA | GitHub required check / MergeGate | Every non-draft PR update | GitHub Action check | structured CCA verdict |\n | Repair assignment | CCA | MergeGate | After failed CCA/CI/review | repair packet, issue/PR comment | repair id and attempt count |\n | Merge readiness | MergeGate | GitHub branch protection / Guardian if needed | After CCA complete | required checks, review rules, rulesets | mergegate check output |\ndiff --git a/docs/agents/guardian-policy.md b/docs/agents/guardian-policy.md\nindex 4147c7adf05a47adb4fd24ada1a1c2e1f2ba6d2d..a20d9cc6c00a569bb057b9ca8d6b51c7fc4a9847\n--- a/docs/agents/guardian-policy.md\n+++ b/docs/agents/guardian-policy.md\n@@ -98,8 +98,8 @@\n `origin/<default_branch>` and reuses each job's existing base fetch; a shallow\n (`--depth`) or second base fetch would make the proof unresolvable and silently\n disable the carry, so neither is permitted. CCA reads `guardian_comment_carried` as\n-a recorded authority in the deterministic signal and does not re-derive it. See ADR\n-0018 for the full decision, the rejected diff/patch-equality and path-disjointness\n+a recorded authority in the deterministic signal and does not re-derive it. See\n+SADR-0018 for the full decision, the rejected diff/patch-equality and path-disjointness\n alternatives, and the accepted residual.\n \n ## External AI Guardian Review (SADR-0010)\ndiff --git a/docs/agents/implementation-policy.md b/docs/agents/implementation-policy.md\nindex c754144a9fb5bf4a38aa82a60e1670c59e626b44..85c37281bcad196c6b7b02343faf00b03cd9c57a\n--- a/docs/agents/implementation-policy.md\n+++ b/docs/agents/implementation-policy.md\n@@ -31,10 +31,13 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n ## Non-Negotiables\n \n 1. **Planning precedes implementation.** Non-trivial Goals start with `grill-with-docs` and Context & Impact.\n-2. **Domain language is controlled.** Terms must be checked against `CONTEXT.md`; hard-to-reverse tradeoffs must be checked against ADRs.\n+2. **Domain language is controlled.** Terms must be checked against `CONTEXT.md`; hard-to-reverse tradeoffs must be checked against Shiki SADRs and target ADRs.\n 3. **PRD precedes issue decomposition.** `to-prd` is used once the context is settled enough to become durable intent.\n 4. **Issues are vertical slices.** `to-issues` must produce independently grabbable tracer-bullet tasks, not horizontal layer tickets.\n 5. **Triage controls dispatch.** Only `ready-for-agent` / AFK tasks may be assigned to Codex without human input.\n@@ -95,19 +98,19 @@\n Process:\n \n - Challenge terms against `CONTEXT.md`.\n-- Check ADRs before reopening decisions.\n+- Check SADRs and target ADRs before reopening decisions.\n - Inspect code when code can answer a question.\n - Ask one question at a time when the operator is available.\n - Provide a recommended answer with each question.\n - Record resolved terms in `CONTEXT.md`.\n-- Create ADRs only for hard-to-reverse, surprising, tradeoff-based decisions.\n+- Create SADRs or target ADRs only for hard-to-reverse, surprising, tradeoff-based decisions.\n - Convert unresolved load-bearing questions into HITL blockers.\n \n Outputs:\n \n - Resolved design-tree decisions.\n - Updated glossary terms when needed.\n-- ADRs when justified.\n+- SADRs or target ADRs when justified.\n - HITL blockers when decisions remain unresolved.\n \n ## Phase 3 \u2014 Context & Impact\n@@ -117,7 +120,7 @@\n \n Outputs must include:\n \n-- Relevant documents and ADRs.\n+- Relevant documents, SADRs, and target ADRs.\n - Relevant modules, interfaces, seams, callers, and tests.\n - Candidate locks.\n - Dependencies.\n@@ -140,7 +143,7 @@\n - Testing decisions.\n - Out of scope.\n - Further notes.\n-- Links to relevant Goal, domain terms, and ADRs.\n+- Links to relevant Goal, domain terms, SADRs, and target ADRs.\n \n Phase 4 ends with Spec Freeze: the operator explicitly approves the PRD, the\n plan records a `spec_freeze` block (status, approved_by, source), and the SF\n@@ -240,7 +243,7 @@\n - Dependencies and locks.\n - Risk and Guardian requirements.\n - Ledger completeness.\n-- Documentation and ADR updates.\n+- Documentation and SADR/target-ADR updates.\n - Scope drift.\n \n CCA emits one of:\n@@ -301,7 +304,7 @@\n | Evidence failure | Behavior might be done, but proof is missing. | Add checks, PR evidence, ledger entry, or artifacts. |\n | Scope drift | PR changes unrelated behavior. | Revert unrelated changes or split into new Goal. |\n | Dependency block | Upstream task or lock not complete. | Block until dependency resolves. |\n-| Architecture block | Decision needs ADR or deep-module discussion. | `grill-with-docs` or `improve-codebase-architecture`. |\n+| Architecture block | Decision needs a SADR, target ADR, or deep-module discussion. | `grill-with-docs` or `improve-codebase-architecture`. |\n | Risk block | Security/production/policy change needs human approval. | Guardian decision. |\n \n ## Phase 5.5 \u2014 Execution Decision Control\ndiff --git a/docs/agents/runtime-auth-model.md b/docs/agents/runtime-auth-model.md\nindex 6a2e1a47e81ea89b5c68a210ec07772a9374babc..3c54748d24233dbd0ba8473950282547ef4ce935\n--- a/docs/agents/runtime-auth-model.md\n+++ b/docs/agents/runtime-auth-model.md\n@@ -148,7 +148,7 @@\n \n API-key based runners may be added by a target repository as an explicit extension. They are not the default Shiki template path.\n \n-If a repo opts into API-key mode, record the exception in an ADR and update the repo's `AGENTS.md`, `.shiki/config.yaml`, and workflow permissions.\n+If a repo opts into API-key mode, record the target-specific exception in a target ADR and update the repo's `AGENTS.md`, `.shiki/config.yaml`, and workflow permissions.\n \n ## GitHub Actions Runtime Compatibility\n \ndiff --git a/docs/agents/skill-gate.md b/docs/agents/skill-gate.md\nindex c50398b0d7c60a8598f2a5c88a10015730dced13..3acffe6444617bb1d54b77d4b4708158ec735bc6\n--- a/docs/agents/skill-gate.md\n+++ b/docs/agents/skill-gate.md\n@@ -9,7 +9,7 @@\n | Skill | Trigger | Expected output |\n | --- | --- | --- |\n | `setup-matt-pocock-skills` | First configuring a repo for these skills, or when issue tracker, triage labels, or domain docs are missing. | `docs/agents/` configuration and an `Agent skills` block in `AGENTS.md` or `CLAUDE.md`. |\n-| `grill-with-docs` | Ambiguous plan, unclear terminology, boundary decision, tradeoff, or ADR-worthy design choice. | Clarified decision tree, recommended answers, updates to `CONTEXT.md` or ADRs when decisions crystallize. |\n+| `grill-with-docs` | Ambiguous plan, unclear terminology, boundary decision, tradeoff, or decision-record-worthy design choice. | Clarified decision tree, recommended answers, updates to `CONTEXT.md`, SADRs, or target ADRs when decisions crystallize. |\n | `zoom-out` | Agent lacks a higher-level architectural map. | Map of relevant modules, callers, seams, and domain concepts. |\n | `to-prd` | Settled context should become a PRD. | PRD published to the issue tracker with problem, solution, user stories, decisions, testing, and out-of-scope notes. |\n | `to-issues` | Goal, PRD, or plan must become implementation tickets. | Vertical-slice issues with acceptance criteria, dependencies, and AFK/HITL classification. |\n@@ -17,7 +17,7 @@\n | `tdd` | Feature work or bug fix has observable behavior that can be specified with tests. | Red-green-refactor loop through public interfaces, one vertical slice at a time. |\n | `code-review` | The TDD loop is green and the implementer is about to create or update a PR. Default for every implementation task. | Pre-PR self-review of the task diff with fixes applied on the task branch; findings and resolutions recorded as a ledger entry naming the skill and a `## Pre-PR code review` PR body section. |\n | `diagnose` | Hard bug, failing check, regression, flaky behavior, or performance problem. | Reproduction loop, ranked hypotheses, instrumentation, fix, regression test, post-mortem. |\n-| `improve-codebase-architecture` | Structural friction, testability, AI-navigability, deep-module opportunity, or refactor request. | Architecture candidates and deepening recommendations informed by `CONTEXT.md` and ADRs. |\n+| `improve-codebase-architecture` | Structural friction, testability, AI-navigability, deep-module opportunity, or refactor request. | Architecture candidates and deepening recommendations informed by `CONTEXT.md`, SADRs, and target ADRs. |\n | `prototype` | A throwaway logic or UI prototype is needed to answer a design question. | Clearly marked disposable prototype and captured decision. |\n \n ## MergeGate Interaction\ndiff --git a/docs/operations.md b/docs/operations.md\nindex cf1f452d4c589e38ccd58553bda0ad8684b51eb7..ace48a90ffbec9e5c0a7113bef16a4858bc44b3f\n--- a/docs/operations.md\n+++ b/docs/operations.md\n@@ -89,6 +89,16 @@\n      target history, brought forward by `shiki migrate`, not a template.\n    - **Everything else** (CLI scripts, workflows, schemas, docs) is overwritten.\n \n+   A forced upgrade also migrates the former numeric Shiki decision paths to\n+   `SADR-NNNN-*`. Before the first write, the installer inventories the exact\n+   legacy Shiki paths and authorizes each deletion only when the existing\n+   `.shiki/install-stamp.json` names that path and its SHA-256 digest still\n+   matches the file. An absent, unreadable, malformed, incomplete, or\n+   mismatching stamp reports every blocked path and stops with no filesystem\n+   change. Target-owned `NNNN-*` ADRs are not cleanup candidates. A non-force\n+   install deletes nothing and retains any still-matching legacy stamp entries\n+   so a later forced upgrade can prove ownership.\n+\n    If the target has pending migrations, `--force` refuses and writes nothing \u2014\n    apply migrations first (step 3), then re-run. The run ends with a single\n    summary of every `.new` written, so a half-upgrade is visible.\ndiff --git a/docs/releasing.md b/docs/releasing.md\nindex 10d73828e7dbd64e9d9f6b83bc84d010537ce62a..aafeee8c4d441e1e427efee2ce4f81a647f020e1\n--- a/docs/releasing.md\n+++ b/docs/releasing.md\n@@ -67,7 +67,7 @@\n CLI in `scripts/`. There are no third-party Python runtime dependencies. The CLI\n is intentionally **not** exposed as a `[project.scripts]` console entry point\n because `scripts/shiki.py` is a dependency-free shim and `scripts/` is not\n-packaged as an importable distribution (see the ADR for the rationale).\n+packaged as an importable distribution (see SADR-0007 for the rationale).\n \n Supported install paths:\n \n@@ -155,6 +155,15 @@\n    [docs/operations.md](operations.md#the-new-convention). If the target has\n    pending migrations, `--force` refuses and writes nothing; apply migrations\n    (step 2) first, then re-run.\n+\n+   The SADR namespace upgrade has an additional pre-write gate: each former\n+   numeric Shiki decision path is removed only when the existing install stamp\n+   contains that exact path and a digest matching its current bytes. The\n+   installer aggregates absent/unreadable/malformed-stamp, missing-digest, and\n+   digest-mismatch blockers before raising, so refusal leaves the target tree\n+   byte-identical. Numeric target ADRs are preserved. A successful forced\n+   upgrade installs and stamps `SADR-*` paths and drops the removed legacy paths\n+   from the refreshed stamp.\n 2. Apply repository-local `.shiki/` state migrations with the migrate command:\n \n    ```bash\ndiff --git a/docs/usage.md b/docs/usage.md\nindex b83407ee3970dd395dd893010bc5dbe8bad26051..a130ef6e40dfd8169a5bc701339351097df1a09f\n--- a/docs/usage.md\n+++ b/docs/usage.md\n@@ -67,8 +67,10 @@\n shiki install-target /path/to/target-repo --local-only\n ```\n \n-Use `--force` only when you intentionally want to overwrite existing target\n-files.\n+Use `--force` only when you intentionally want to refresh the target template.\n+For the SADR namespace migration, `--force` deletes only exact legacy Shiki\n+decision paths whose existing install-stamp digest still matches; any ownership\n+blocker stops before the first write. Numeric target ADRs are never candidates.\n \n ## `shiki bootstrap-platform` \u2014 publish the Shiki platform repo\n \ndiff --git a/scripts/guardian_approval_signal.py b/scripts/guardian_approval_signal.py\nindex d059f92b2c82aa2b5d6ca421365537a87886c1cb..16924892714f4f5b9303c0ef0e325a7e70075630\n--- a/scripts/guardian_approval_signal.py\n+++ b/scripts/guardian_approval_signal.py\n@@ -199,8 +199,8 @@\n ) -> Any:\n     \"\"\"Evaluate SADR-0015 Contract Approval for the CCA Guardian signal.\n \n-    The SAME OR that MergeGate applies must reach this deterministic signal: ADR\n-    0015 is explicit that without it, a contract-approved PR still returns\n+    The SAME OR that MergeGate applies must reach this deterministic signal:\n+    SADR-0015 is explicit that without it, a contract-approved PR still returns\n     ``needs_guardian``, the CCA job fails, and the MergeGate policy check never\n     runs \u2014 the gate this change moves would never be reached. This resolves the\n     task the SAME way MergeGate does (the first Shiki task id in the PR body),\ndiff --git a/scripts/shiki_contract_approval.py b/scripts/shiki_contract_approval.py\nindex 941bcb96fdd8b9a85bc0c4f0f04d5dad9d04839c..4a4b418f6527413161db82670bec857a043360e2\n--- a/scripts/shiki_contract_approval.py\n+++ b/scripts/shiki_contract_approval.py\n@@ -142,7 +142,7 @@\n     changed_files_status: Any,\n     registration: Any,\n ) -> ContractApprovalResult:\n-    \"\"\"Whether this PR carries an SADR-0015 Contract Approval.\n+    \"\"\"Whether this PR carries a SADR-0015 Contract Approval.\n \n     The four conditions are evaluated IN ORDER, each failure recorded as a\n     distinct reason string:\ndiff --git a/scripts/shiki_guardian.py b/scripts/shiki_guardian.py\nindex a4ecb79bbda55fd5c8b06adfae2eb99760a03c45..4dd99bbe714e26f421fa19abac7a34ae1b195ecc\n--- a/scripts/shiki_guardian.py\n+++ b/scripts/shiki_guardian.py\n@@ -12,7 +12,7 @@\n # The carry re-derives its proof from git in-process on every judging invocation;\n # it is never read from an artifact, so there is nothing to go stale or be forged.\n # shiki_sync_proof is the ONLY guardian-family module that runs git, and every one\n-# of its invocations is read-only and local (ADR: prove-from-git base sync).\n+# of its invocations is read-only and local (SADR: prove-from-git base sync).\n from shiki_sync_proof import verify_pure_base_sync\n \n GUARDIAN_POLICY_PATH = \".shiki/guardian-policy.json\"\ndiff --git a/scripts/shiki_guardian_review.py b/scripts/shiki_guardian_review.py\nindex dd0f104d457899e0ba5ff1e162011c1e90b6f92b..e10ef58c046324e7ea3a3392feb3048ca77414ce\n--- a/scripts/shiki_guardian_review.py\n+++ b/scripts/shiki_guardian_review.py\n@@ -159,7 +159,7 @@\n     docs_only = _is_docs_only(changed_files)\n     if docs_only and matched == 0:\n         focus.append(\n-            \"Docs/ADR-only change: confirm the change is genuinely documentation/decision-only, introduces no behavior or contract drift, and stays consistent with the constitution and existing ADRs.\"\n+            \"Docs/decision-record-only change: confirm the change is genuinely documentation/decision-only, introduces no behavior or contract drift, and stays consistent with the constitution and existing SADRs and target ADRs.\"\n         )\n \n     # Conservative augmentation: unknown (no category and not docs-only), mixed\ndiff --git a/scripts/shiki_installer.py b/scripts/shiki_installer.py\nindex be064ab7a29ff6836f7f7a1d70500a43e0d11d7b..9fd8eefd137ce7003dad5c0dcb2e48e2cd47ab32\n--- a/scripts/shiki_installer.py\n+++ b/scripts/shiki_installer.py\n@@ -118,6 +118,7 @@\n # what lets a target carry it without a governance-schema change.\n INSTALL_STAMP_PATH = \".shiki/install-stamp.json\"\n INSTALL_STAMP_VERSION = 1\n+LEGACY_SHIKI_SADR_MAX = 18\n \n # Under --force the shipped surface splits three ways, so nothing is lost and\n # nothing is silently stale.\n@@ -237,9 +238,98 @@\n         )\n \n \n+def legacy_shiki_adr_paths() -> tuple[str, ...]:\n+    \"\"\"Former numeric paths for Shiki SADR-0001 through SADR-0018.\n+\n+    The authoritative slugs come from the current SADR filenames. SADR-0019 is\n+    the namespace decision itself and never shipped under a numeric legacy path.\n+    \"\"\"\n+    paths: list[str] = []\n+    for sadr in sorted((ROOT / \"docs\" / \"adr\").glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\")):\n+        number = sadr.name.split(\"-\", 2)[1]\n+        if int(number) <= LEGACY_SHIKI_SADR_MAX:\n+            paths.append(f\"docs/adr/{sadr.name.removeprefix('SADR-')}\")\n+    return tuple(paths)\n+\n+\n+def preflight_legacy_sadr_cleanup(target: Path, *, force: bool) -> tuple[str, ...]:\n+    \"\"\"Authorize exact legacy Shiki ADR removals before the first write.\n+\n+    A non-force install never removes existing files. A forced install needs no\n+    stamp when no legacy Shiki path is present. When legacy paths do exist, each\n+    must be named in a readable install stamp and its current bytes must match\n+    the recorded digest.\n+    \"\"\"\n+    if not force:\n+        return ()\n+    present = tuple(\n+        relative\n+        for relative in legacy_shiki_adr_paths()\n+        if (target / relative).exists() or (target / relative).is_symlink()\n+    )\n+    if not present:\n+        return ()\n+\n+    stamp_path = target / INSTALL_STAMP_PATH\n+    try:\n+        raw = stamp_path.read_text(encoding=\"utf-8\")\n+        stamp = json.loads(raw)\n+    except FileNotFoundError:\n+        detail = \"install stamp is absent\"\n+        blockers = [f\"{relative}: {detail}\" for relative in present]\n+    except (OSError, UnicodeDecodeError) as error:\n+        detail = f\"install stamp is unreadable ({error})\"\n+        blockers = [f\"{relative}: {detail}\" for relative in present]\n+    except json.JSONDecodeError as error:\n+        detail = f\"install stamp is malformed ({error})\"\n+        blockers = [f\"{relative}: {detail}\" for relative in present]\n+    else:\n+        if not isinstance(stamp, dict):\n+            blockers = [\n+                f\"{relative}: install stamp is malformed (expected an object)\"\n+                for relative in present\n+            ]\n+        else:\n+            digests = stamp.get(\"digests\")\n+            blockers = []\n+            if not isinstance(digests, dict):\n+                blockers.extend(\n+                    f\"{relative}: install stamp has no digest for this path\"\n+                    for relative in present\n+                )\n+            else:\n+                for relative in present:\n+                    expected = digests.get(relative)\n+                    if not isinstance(expected, str) or not expected:\n+                        blockers.append(\n+                            f\"{relative}: install stamp has no digest for this path\"\n+                        )\n+                        continue\n+                    try:\n+                        actual = _sha256_file(target / relative)\n+                    except OSError as error:\n+                        blockers.append(\n+                            f\"{relative}: legacy path is unreadable ({error})\"\n+                        )\n+                        continue\n+                    if actual != expected:\n+                        blockers.append(\n+                            f\"{relative}: digest mismatch (stamp {expected}, current {actual})\"\n+                        )\n+\n+    if blockers:\n+        joined = \"\\n\".join(f\"- {blocker}\" for blocker in blockers)\n+        raise ShikiError(\n+            \"legacy Shiki ADR cleanup is not authorized:\\n\"\n+            f\"{joined}\\nNo files were written.\"\n+        )\n+    return present\n+\n+\n def install_template(target: Path, *, force: bool, validate: bool) -> list[NewFileNote]:\n     # Preconditions first: never rewrite the tree of a target that cannot be\n     # upgraded cleanly (see refuse_pending_migrations).\n+    legacy_cleanup = preflight_legacy_sadr_cleanup(target, force=force)\n     refuse_pending_migrations(target)\n \n     # Resolve the target's required CODEOWNERS owner once from its\n@@ -250,6 +340,10 @@\n     # verbatim. See ``codeowners_required_owner``.\n     codeowners_owner = codeowners_required_owner(target)\n \n+    for relative in legacy_cleanup:\n+        (target / relative).unlink()\n+        info(f\"removed stamped legacy Shiki ADR: {target / relative}\")\n+\n     notes: list[NewFileNote] = []\n     for relative in TEMPLATE_PATHS:\n         source = ROOT / relative\n@@ -647,6 +741,22 @@\n     digests = target_stamp_digests(target)\n     existing = read_install_stamp(target)\n     if existing is not None:\n+        # A non-force install deliberately leaves legacy Shiki ADRs in place.\n+        # Preserve only still-matching ownership proof so a later forced\n+        # upgrade can authorize cleanup; never carry a missing or stale digest.\n+        existing_digests = existing.get(\"digests\")\n+        if isinstance(existing_digests, dict):\n+            for relative in legacy_shiki_adr_paths():\n+                expected = existing_digests.get(relative)\n+                path = target / relative\n+                if not isinstance(expected, str) or not path.is_file():\n+                    continue\n+                try:\n+                    actual = _sha256_file(path)\n+                except OSError:\n+                    continue\n+                if actual == expected:\n+                    digests[relative] = actual\n         if _incoming_is_older(commit, existing.get(\"platform_commit\")):\n             warn(\n                 \"kept existing install stamp: incoming install \"\ndiff --git a/scripts/shiki_loop.py b/scripts/shiki_loop.py\nindex 3611641da8aeb5038b1da90e3114b09a473ec6d8..84caa491b9cad0edc4176f209c13e9221e975681\n--- a/scripts/shiki_loop.py\n+++ b/scripts/shiki_loop.py\n@@ -424,7 +424,7 @@\n         # unresolvable CCA verdict has already produced its terminal stop and is never\n         # laundered into a head-moving sync (SADR-0010). It also never fires while the\n         # policy (Guardian) gate is red \u2014 for ANY reason, not only when it is the sole\n-        # red check \u2014 so a head-bound Guardian approval, or an SADR-0015\n+        # red check \u2014 so a head-bound Guardian approval, or a SADR-0015\n         # contract-approval carry that base movement defeated (the policy gate compares\n         # base and head task governance directly), is never silently restored by\n         # adopting base wholesale. It is bounded by a durable per-task counter so the\ndiff --git a/scripts/shiki_tasks.py b/scripts/shiki_tasks.py\nindex aec3809f24b4a0c8c96fc50971cff9ae9ad30381..d531052e9d9717058d4631483cefab44c26a65c1\n--- a/scripts/shiki_tasks.py\n+++ b/scripts/shiki_tasks.py\n@@ -821,7 +821,7 @@\n         \"prompt\": prompt,\n         \"entry_skill\": \"grill-with-docs\",\n         \"required_next_steps\": [\n-            \"Run grill-with-docs until terminology, boundaries, risks, and ADR-worthy decisions are settled.\",\n+            \"Run grill-with-docs until terminology, boundaries, risks, and decision-record-worthy choices are settled.\",\n             \"Obtain the operator's explicit approval of the PRD/requirements (Spec Freeze).\",\n             \"Write a machine-readable plan JSON with grill_with_docs.status=complete and spec_freeze.status=frozen.\",\n             \"Run shiki plan ingest --plan-file PLAN.json.\",\ndiff --git a/scripts/test_shiki_goal_loop.sh b/scripts/test_shiki_goal_loop.sh\nindex 753c7b516e10f0be3bc92a4bd0eb1fa699776ae6..8f3820a0a079b3044424cfe1a6c6b7fe68896e67\n--- a/scripts/test_shiki_goal_loop.sh\n+++ b/scripts/test_shiki_goal_loop.sh\n@@ -231,8 +231,8 @@\n \n # High/critical risk merges autonomously when all required checks are green:\n # the \"MergeGate policy check\" required check IS the Guardian gate (it enforces\n-# guardian-policy.json \u2014 human approval OR external AI guardian review, ADR\n-# 0010), so green checks mean a recorded authority approved. The fake gh returns\n+# guardian-policy.json \u2014 human approval OR external AI guardian review,\n+# SADR-0010), so green checks mean a recorded authority approved. The fake gh returns\n # green checks, simulating that the Guardian gate passed.\n python3 \"$ROOT/scripts/shiki.py\" goal create --target \"$TARGET\" --title \"Guardian gate\" --outcome \"High risk merges via policy gate\" >/tmp/shiki-goal-loop-guardian-goal.json\n GGOAL=\"$(json_get /tmp/shiki-goal-loop-guardian-goal.json goal_id)\"\ndiff --git a/tests/test_adr_references.py b/tests/test_adr_references.py\nindex 51706329c797d5dd9b47e082a333fa83b261d482..d005da4f771f5e8085ec22d4b787980e68f5ae8c\n--- a/tests/test_adr_references.py\n+++ b/tests/test_adr_references.py\n@@ -1,8 +1,8 @@\n-\"\"\"Every ``ADR ####`` citation must resolve to exactly one decision record.\n+\"\"\"Every target ADR and Shiki SADR citation resolves in its own namespace.\n \n Background. ``scripts/mergegate_check.py`` justifies ``is_bookkeeping_closeout``\n-with \"ADR 0017\" nine times and ``scripts/guardian_approval_signal.py`` cites\n-0016/0017 seven times, yet for a stretch the numbering in ``docs/adr/`` stopped\n+with \"SADR-0017\" nine times and ``scripts/guardian_approval_signal.py`` cites\n+SADR-0016/SADR-0017 seven times, yet for a stretch the numbering in ``docs/adr/`` stopped\n at 0015: the authoring commits lived on a coordinator branch and no task ever\n declared ``docs/adr/...`` in its locks, so nothing carried the records to the\n default branch. The stated basis of an exemption that merged seven PRs with zero\n@@ -11,11 +11,12 @@\n and the directory drifted with no signal.\n \n This is the same unbound-pair shape behind other silent failures: a reference\n-and its target with nothing asserting they agree. This test binds them. It scans\n-every ``scripts/*.py`` (plus ``docs/**/*.md`` and the root ``*.md`` governance\n-files, which are cheap) for the ``ADR ####`` pattern and asserts each cited\n-number resolves to exactly one ``docs/adr/####-*.md``. The converse is\n-deliberately NOT required: an ADR may exist without being cited.\n+and its target with nothing asserting they agree. This test binds them. Target\n+``ADR NNNN`` citations resolve only to ``docs/adr/NNNN-*.md``; Shiki\n+``SADR-NNNN`` citations resolve only to ``docs/adr/SADR-NNNN-*.md``. The scan\n+covers current governance docs, scripts, workflows, prompts, packaging metadata,\n+and repo-local Codex guidance while deliberately excluding historical\n+``.shiki`` evidence. The converse is not required: a record may be uncited.\n \n The suite is dependency free: standard-library ``unittest`` only.\n \"\"\"\n@@ -31,17 +32,18 @@\n \n REPO_ROOT = Path(__file__).resolve().parents[1]\n \n-# \"ADR 0016\" -- a four-digit citation. The trailing \\b keeps a five-digit token\n-# from being read as a truncated four-digit number.\n-_CITATION = re.compile(r\"\\bADR (\\d{4})\\b\")\n-# \"0016-locks-bound-product-paths.md\" -- the leading four digits name the record.\n-_ADR_FILENAME = re.compile(r\"^(\\d{4})-.*\\.md$\")\n+# ``ADR 0016`` / ``SADR-0016`` -- a namespace plus four-digit citation. The\n+# trailing ``\\b`` keeps a five-digit token from being read as a truncated one.\n+_CITATION = re.compile(r\"\\b(S?ADR)[ -](\\d{4})\\b\")\n+# Target ADRs retain the numeric filename; Shiki platform decisions use SADR.\n+_DECISION_FILENAME = re.compile(r\"^(?:(SADR)-)?(\\d{4})-.*\\.md$\")\n \n \n class Violation(NamedTuple):\n     \"\"\"A citation that does not resolve to exactly one decision record.\"\"\"\n \n     citing_file: str  # repo-relative path of the file that cites the ADR\n+    namespace: str  # \"ADR\" for target decisions; \"SADR\" for Shiki decisions\n     number: str  # the cited four-digit number, e.g. \"0016\"\n     line: int  # 1-based line of the citation\n     reason: str  # \"missing\" (zero matches) or \"ambiguous\" (two or more)\n@@ -49,44 +51,59 @@\n \n     @property\n     def expected_path(self) -> str:\n-        return f\"docs/adr/{self.number}-*.md\"\n+        prefix = \"SADR-\" if self.namespace == \"SADR\" else \"\"\n+        return f\"docs/adr/{prefix}{self.number}-*.md\"\n \n     def describe(self) -> str:\n         \"\"\"Name the citing file, the cited number, and the missing path.\"\"\"\n         if self.reason == \"missing\":\n             return (\n-                f\"{self.citing_file}:{self.line} cites ADR {self.number} \"\n+                f\"{self.citing_file}:{self.line} cites {self.namespace} {self.number} \"\n                 f\"but no document matches {self.expected_path}\"\n             )\n         joined = \", \".join(self.matches)\n         return (\n-            f\"{self.citing_file}:{self.line} cites ADR {self.number} which \"\n+            f\"{self.citing_file}:{self.line} cites {self.namespace} {self.number} which \"\n             f\"resolves to {len(self.matches)} documents (expected exactly one \"\n             f\"at {self.expected_path}): {joined}\"\n         )\n \n \n-def _adr_index(root: Path) -> dict[str, list[Path]]:\n-    \"\"\"Map every four-digit ADR number to the record files that claim it.\"\"\"\n-    index: dict[str, list[Path]] = {}\n+def _adr_index(root: Path) -> dict[tuple[str, str], list[Path]]:\n+    \"\"\"Map each decision namespace and number to the files that claim it.\"\"\"\n+    index: dict[tuple[str, str], list[Path]] = {}\n     adr_dir = root / \"docs\" / \"adr\"\n     if adr_dir.is_dir():\n         for path in sorted(adr_dir.glob(\"*.md\")):\n-            match = _ADR_FILENAME.match(path.name)\n+            match = _DECISION_FILENAME.match(path.name)\n             if match:\n-                index.setdefault(match.group(1), []).append(path)\n+                namespace = \"SADR\" if match.group(1) else \"ADR\"\n+                index.setdefault((namespace, match.group(2)), []).append(path)\n     return index\n \n \n def _citing_files(root: Path, include_docs: bool = True) -> list[Path]:\n-    \"\"\"Files scanned for citations: scripts/*.py, and cheaply the markdown.\"\"\"\n+    \"\"\"Current, shipped text surfaces scanned for decision citations.\"\"\"\n     files = sorted((root / \"scripts\").glob(\"*.py\"))\n+    files += sorted((root / \"scripts\").glob(\"*.sh\"))\n     if include_docs:\n         docs = root / \"docs\"\n         if docs.is_dir():\n             files += sorted(docs.rglob(\"*.md\"))\n         files += sorted(root.glob(\"*.md\"))\n-    return files\n+        pyproject = root / \"pyproject.toml\"\n+        if pyproject.is_file():\n+            files.append(pyproject)\n+        for relative in (\".github\", \".codex\"):\n+            surface = root / relative\n+            if surface.is_dir():\n+                files += sorted(\n+                    path\n+                    for path in surface.rglob(\"*\")\n+                    if path.is_file()\n+                    and path.suffix in {\".md\", \".py\", \".sh\", \".toml\", \".yaml\", \".yml\"}\n+                )\n+    return sorted(set(files))\n \n \n def find_adr_reference_violations(\n@@ -94,10 +111,9 @@\n ) -> list[Violation]:\n     \"\"\"Return every ``ADR ####`` citation under ``root`` that fails to resolve.\n \n-    A citation resolves when exactly one ``docs/adr/####-*.md`` matches its\n-    number. Zero matches is a \"missing\" violation; two or more is \"ambiguous\".\n-    Records that are never cited produce nothing -- the converse is not\n-    required.\n+    A citation resolves when exactly one record in the same namespace matches\n+    its number. Zero matches is a \"missing\" violation; two or more is\n+    \"ambiguous\". Records that are never cited produce nothing.\n     \"\"\"\n     root = Path(root)\n     index = _adr_index(root)\n@@ -110,13 +126,15 @@\n         rel = path.relative_to(root).as_posix()\n         for lineno, line in enumerate(text.splitlines(), start=1):\n             for match in _CITATION.finditer(line):\n-                number = match.group(1)\n-                found = index.get(number, [])\n+                namespace, number = match.groups()\n+                found = index.get((namespace, number), [])\n                 if len(found) == 1:\n                     continue\n                 reason = \"missing\" if not found else \"ambiguous\"\n                 rels = [p.relative_to(root).as_posix() for p in found]\n-                violations.append(Violation(rel, number, lineno, reason, rels))\n+                violations.append(\n+                    Violation(rel, namespace, number, lineno, reason, rels)\n+                )\n     return violations\n \n \n@@ -129,8 +147,8 @@\n         self.assertEqual(\n             violations,\n             [],\n-            \"Every 'ADR ####' citation must resolve to exactly one \"\n-            f\"docs/adr/####-*.md:\\n{detail}\",\n+            \"Every ADR/SADR citation must resolve exactly once in its own \"\n+            f\"docs/adr namespace:\\n{detail}\",\n         )\n \n     def test_scan_actually_reads_scripts(self) -> None:\n@@ -142,12 +160,38 @@\n             \"the citation scan must cover scripts/*.py\",\n         )\n \n+    def test_platform_citations_do_not_use_unresolved_numeric_shorthand(self) -> None:\n+        shorthand = re.compile(r\"SADR-\\d{4}/\\d{4}\")\n+        offenders = []\n+        for path in _citing_files(REPO_ROOT):\n+            text = path.read_text(encoding=\"utf-8\")\n+            if shorthand.search(text):\n+                offenders.append(path.relative_to(REPO_ROOT).as_posix())\n+        self.assertEqual(\n+            offenders,\n+            [],\n+            \"repeat the SADR prefix so every cited number is independently resolvable\",\n+        )\n+\n     def test_carried_records_are_indexed(self) -> None:\n         # The two records this Goal carried onto the default branch, and which\n         # the scripts cite most, must be present.\n         index = _adr_index(REPO_ROOT)\n-        self.assertIn(\"0016\", index)\n-        self.assertIn(\"0017\", index)\n+        self.assertIn((\"SADR\", \"0016\"), index)\n+        self.assertIn((\"SADR\", \"0017\"), index)\n+\n+    def test_platform_tree_has_only_sadr_0001_through_0019(self) -> None:\n+        adr_dir = REPO_ROOT / \"docs\" / \"adr\"\n+        sadrs = sorted(adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\"))\n+        self.assertEqual(\n+            {path.name.split(\"-\", 2)[1] for path in sadrs},\n+            {f\"{number:04d}\" for number in range(1, 20)},\n+        )\n+        self.assertEqual(\n+            list(adr_dir.glob(\"[0-9][0-9][0-9][0-9]-*.md\")),\n+            [],\n+            \"the Shiki platform tree must not retain legacy numeric SADR paths\",\n+        )\n \n \n class FixtureAdrReferences(unittest.TestCase):\n@@ -180,6 +224,26 @@\n         self.assertIn(\"9991\", message)  # cited number\n         self.assertIn(\"docs/adr/9991-*.md\", message)  # missing path\n \n+    def test_target_adr_does_not_satisfy_missing_platform_sadr(self) -> None:\n+        root = self._make_repo(\n+            adr_names=[\"0042-target-decision.md\"],\n+            script_body='\"\"\"Target ADR 0042 exists, but platform SADR-0042 does not.\"\"\"\\n',\n+        )\n+        violations = find_adr_reference_violations(root, include_docs=False)\n+        self.assertEqual(len(violations), 1)\n+        self.assertEqual(violations[0].namespace, \"SADR\")\n+        self.assertEqual(violations[0].number, \"0042\")\n+        self.assertEqual(violations[0].expected_path, \"docs/adr/SADR-0042-*.md\")\n+\n+    def test_target_adr_and_platform_sadr_may_reuse_a_number(self) -> None:\n+        root = self._make_repo(\n+            adr_names=[\"0042-target-decision.md\", \"SADR-0042-platform-decision.md\"],\n+            script_body='\"\"\"Target ADR 0042 and platform SADR-0042 are distinct.\"\"\"\\n',\n+        )\n+        self.assertEqual(\n+            find_adr_reference_violations(root, include_docs=False), []\n+        )\n+\n     def test_uncited_adr_is_allowed(self) -> None:\n         # An ADR exists but nothing cites it -> not a violation. The converse\n         # (every ADR must be cited) is explicitly NOT required.\ndiff --git a/tests/test_guardian_review.py b/tests/test_guardian_review.py\nindex 88829e7810212635e2fc98aa5918d6f26294677d..00a85a237acd5bf2d6cb5b294128198c422402ca\n--- a/tests/test_guardian_review.py\n+++ b/tests/test_guardian_review.py\n@@ -162,7 +162,7 @@\n \n     def test_docs_only_is_light_and_not_conservative(self):\n         focus = gr.classify_review_focus([\"docs/adr/0099-x.md\", \"README.md\"], \"\", \"low\")\n-        self.assertTrue(any(\"Docs/ADR-only\" in f for f in focus))\n+        self.assertTrue(any(\"Docs/decision-record-only\" in f for f in focus))\n         self.assertFalse(any(\"conservative default\" in f.lower() for f in focus))\n \n     def test_unknown_category_is_conservative(self):\ndiff --git a/tests/test_policy_job_merged_prs.py b/tests/test_policy_job_merged_prs.py\nindex d45512e74681e5e649279236c7d4e79d5c3da471..b97989f8bd74d091de1a6b5275a20d8dc68c4512\n--- a/tests/test_policy_job_merged_prs.py\n+++ b/tests/test_policy_job_merged_prs.py\n@@ -3,8 +3,8 @@\n \n ``.github/workflows/shiki-cca-completion.yml`` has two consumers of the merged-PR\n proof: the CCA *signal* (``guardian_approval_signal.py`` in \"Prepare CCA context\")\n-and the authoritative gate (``mergegate_check.py`` in \"Enforce MergeGate\"). ADR\n-0017's bookkeeping-closeout exemption (condition 6) needs the merged implementation\n+and the authoritative gate (``mergegate_check.py`` in \"Enforce MergeGate\").\n+SADR-0017's bookkeeping-closeout exemption (condition 6) needs the merged implementation\n PR proven, and a closeout carries NO ``post_merge_reconcile`` marker. The signal\n already resolves ``MERGED_PRS`` unconditionally, but \"Enforce MergeGate\" used to\n resolve it only inside ``grep -q '<!-- shiki:post_merge_reconcile -->'``. So a\ndiff --git a/tests/test_sadr_install_migration.py b/tests/test_sadr_install_migration.py\nnew file mode 100644\nindex 0000000000000000000000000000000000000000..d51eac872b1c79ff4579c4c1401ca5d775a60d8d\n--- /dev/null\n+++ b/tests/test_sadr_install_migration.py\n@@ -0,0 +1,237 @@\n+\"\"\"SADR installation and collision-safe legacy upgrade behavior.\"\"\"\n+\n+from __future__ import annotations\n+\n+import contextlib\n+import hashlib\n+import io\n+import json\n+import shutil\n+import tempfile\n+import unittest\n+from pathlib import Path\n+\n+import shiki_test_support  # noqa: F401  (path bootstrap)\n+\n+import shiki_installer\n+from tests.test_adr_references import find_adr_reference_violations\n+\n+\n+PRODUCT_NUMBERS = tuple(range(13, 19))\n+\n+\n+def _quiet_install(target: Path, *, force: bool, validate: bool) -> None:\n+    with contextlib.redirect_stdout(io.StringIO()):\n+        shiki_installer.install_template(target, force=force, validate=validate)\n+\n+\n+def _sha256(path: Path) -> str:\n+    return hashlib.sha256(path.read_bytes()).hexdigest()\n+\n+\n+def _legacy_sadr_paths() -> tuple[str, ...]:\n+    return tuple(\n+        f\"docs/adr/{path.name.removeprefix('SADR-')}\"\n+        for path in sorted((shiki_installer.ROOT / \"docs\" / \"adr\").glob(\"SADR-*.md\"))\n+        if path.name != \"SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\"\n+    )\n+\n+\n+def _create_legacy_upgrade_fixture(\n+    target: Path,\n+) -> tuple[dict[str, bytes], tuple[str, ...]]:\n+    adr_dir = target / \"docs\" / \"adr\"\n+    adr_dir.mkdir(parents=True)\n+    product_bytes = {\n+        f\"{number:04d}-cubeshare-product-decision.md\": (\n+            f\"# CubeShare ADR {number:04d}\\n\\nDo not rewrite {number}.\\n\"\n+        ).encode()\n+        for number in PRODUCT_NUMBERS\n+    }\n+    for name, content in product_bytes.items():\n+        (adr_dir / name).write_bytes(content)\n+\n+    legacy_paths = _legacy_sadr_paths()\n+    for relative in legacy_paths:\n+        (target / relative).write_bytes(f\"legacy Shiki bytes for {relative}\\n\".encode())\n+\n+    stamp_path = target / shiki_installer.INSTALL_STAMP_PATH\n+    stamp_path.parent.mkdir(parents=True, exist_ok=True)\n+    stamp = {\n+        \"version\": shiki_installer.INSTALL_STAMP_VERSION,\n+        \"platform_commit\": None,\n+        \"installed_at\": \"2000-01-01T00:00:00+00:00\",\n+        \"digests\": {\n+            relative: _sha256(target / relative) for relative in legacy_paths\n+        },\n+    }\n+    stamp_path.write_text(\n+        json.dumps(stamp, indent=2, sort_keys=True) + \"\\n\", encoding=\"utf-8\"\n+    )\n+    return product_bytes, legacy_paths\n+\n+\n+def _tree_snapshot(root: Path) -> dict[str, tuple[str, bytes | str]]:\n+    \"\"\"Recursive byte/shape snapshot used to prove preflight is write-free.\"\"\"\n+    snapshot: dict[str, tuple[str, bytes | str]] = {}\n+    for path in sorted(root.rglob(\"*\")):\n+        relative = path.relative_to(root).as_posix()\n+        if path.is_symlink():\n+            snapshot[relative] = (\"symlink\", path.readlink().as_posix())\n+        elif path.is_dir():\n+            snapshot[relative] = (\"directory\", \"\")\n+        else:\n+            snapshot[relative] = (\"file\", path.read_bytes())\n+    return snapshot\n+\n+\n+class FreshSadrInstallTests(unittest.TestCase):\n+    def setUp(self) -> None:\n+        self.tmp = Path(tempfile.mkdtemp(prefix=\"shiki-sadr-fresh-\"))\n+        self.addCleanup(lambda: shutil.rmtree(self.tmp, ignore_errors=True))\n+        self.target = self.tmp / \"target\"\n+        self.adr_dir = self.target / \"docs\" / \"adr\"\n+        self.adr_dir.mkdir(parents=True)\n+        self.product_bytes = {\n+            f\"{number:04d}-product-decision.md\": (\n+                f\"# Product ADR {number:04d}\\n\\nTarget-owned bytes {number}.\\n\"\n+            ).encode()\n+            for number in PRODUCT_NUMBERS\n+        }\n+        for name, content in self.product_bytes.items():\n+            (self.adr_dir / name).write_bytes(content)\n+\n+    def test_fresh_install_preserves_product_adrs_and_installs_all_sadrs(self) -> None:\n+        _quiet_install(self.target, force=False, validate=True)\n+\n+        for name, expected in self.product_bytes.items():\n+            self.assertEqual((self.adr_dir / name).read_bytes(), expected)\n+\n+        sadr_numbers = {\n+            path.name.split(\"-\", 2)[1]\n+            for path in self.adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\")\n+        }\n+        self.assertEqual(sadr_numbers, {f\"{number:04d}\" for number in range(1, 20)})\n+\n+        numeric_records = {\n+            path.name\n+            for path in self.adr_dir.glob(\"[0-9][0-9][0-9][0-9]-*.md\")\n+        }\n+        self.assertEqual(numeric_records, set(self.product_bytes))\n+        stamp = json.loads(\n+            (self.target / shiki_installer.INSTALL_STAMP_PATH).read_text(\n+                encoding=\"utf-8\"\n+            )\n+        )\n+        for path in self.adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\"):\n+            relative = path.relative_to(self.target).as_posix()\n+            self.assertEqual(stamp[\"digests\"][relative], _sha256(path))\n+        self.assertEqual(find_adr_reference_violations(self.target), [])\n+\n+\n+class LegacySadrUpgradeTests(unittest.TestCase):\n+    def setUp(self) -> None:\n+        self.tmp = Path(tempfile.mkdtemp(prefix=\"shiki-sadr-upgrade-\"))\n+        self.addCleanup(lambda: shutil.rmtree(self.tmp, ignore_errors=True))\n+        self.target = self.tmp / \"target\"\n+        self.adr_dir = self.target / \"docs\" / \"adr\"\n+        self.product_bytes, self.legacy_paths = _create_legacy_upgrade_fixture(\n+            self.target\n+        )\n+\n+    def test_force_upgrade_deletes_only_stamped_legacy_sadrs(self) -> None:\n+        _quiet_install(self.target, force=True, validate=True)\n+\n+        for relative in self.legacy_paths:\n+            self.assertFalse((self.target / relative).exists(), relative)\n+        for name, expected in self.product_bytes.items():\n+            self.assertEqual((self.adr_dir / name).read_bytes(), expected)\n+\n+        sadrs = list(self.adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\"))\n+        self.assertEqual(len(sadrs), 19)\n+        refreshed = json.loads(\n+            (self.target / shiki_installer.INSTALL_STAMP_PATH).read_text(encoding=\"utf-8\")\n+        )\n+        for relative in self.legacy_paths:\n+            self.assertNotIn(relative, refreshed[\"digests\"])\n+        for path in sadrs:\n+            relative = path.relative_to(self.target).as_posix()\n+            self.assertEqual(refreshed[\"digests\"][relative], _sha256(path))\n+\n+    def test_non_force_install_never_deletes_legacy_files(self) -> None:\n+        before = {\n+            relative: (self.target / relative).read_bytes()\n+            for relative in self.legacy_paths\n+        }\n+        _quiet_install(self.target, force=False, validate=False)\n+        for relative, expected in before.items():\n+            self.assertEqual((self.target / relative).read_bytes(), expected)\n+        refreshed = json.loads(\n+            (self.target / shiki_installer.INSTALL_STAMP_PATH).read_text(encoding=\"utf-8\")\n+        )\n+        for relative in self.legacy_paths:\n+            self.assertEqual(refreshed[\"digests\"][relative], _sha256(self.target / relative))\n+\n+\n+class LegacyCleanupPreflightTests(unittest.TestCase):\n+    def setUp(self) -> None:\n+        self.tmp = Path(tempfile.mkdtemp(prefix=\"shiki-sadr-preflight-\"))\n+        self.addCleanup(lambda: shutil.rmtree(self.tmp, ignore_errors=True))\n+\n+    def test_all_ownership_blockers_abort_before_any_write(self) -> None:\n+        cases = (\n+            (\"absent\", \"install stamp is absent\"),\n+            (\"unreadable\", \"install stamp is unreadable\"),\n+            (\"malformed\", \"install stamp is malformed\"),\n+            (\"missing-digest\", \"install stamp has no digest\"),\n+            (\"digest-mismatch\", \"digest mismatch\"),\n+        )\n+        for name, expected_reason in cases:\n+            with self.subTest(name=name):\n+                target = self.tmp / name\n+                _, legacy_paths = _create_legacy_upgrade_fixture(target)\n+                stamp_path = target / shiki_installer.INSTALL_STAMP_PATH\n+\n+                if name == \"absent\":\n+                    stamp_path.unlink()\n+                elif name == \"unreadable\":\n+                    stamp_path.unlink()\n+                    stamp_path.mkdir()\n+                elif name == \"malformed\":\n+                    stamp_path.write_text(\"{not-json\\n\", encoding=\"utf-8\")\n+                else:\n+                    stamp = json.loads(stamp_path.read_text(encoding=\"utf-8\"))\n+                    if name == \"missing-digest\":\n+                        stamp[\"digests\"] = {}\n+                    else:\n+                        stamp[\"digests\"] = {\n+                            relative: \"deadbeef\" for relative in legacy_paths\n+                        }\n+                    stamp_path.write_text(\n+                        json.dumps(stamp, indent=2, sort_keys=True) + \"\\n\",\n+                        encoding=\"utf-8\",\n+                    )\n+\n+                before = _tree_snapshot(target)\n+                with self.assertRaises(shiki_installer.ShikiError) as raised:\n+                    _quiet_install(target, force=True, validate=False)\n+                after = _tree_snapshot(target)\n+\n+                self.assertEqual(before, after, f\"{name} preflight changed the tree\")\n+                message = str(raised.exception)\n+                self.assertIn(expected_reason, message)\n+                self.assertIn(\"No files were written\", message)\n+                for relative in legacy_paths:\n+                    self.assertIn(relative, message)\n+\n+    def test_absent_stamp_is_allowed_when_no_legacy_path_exists(self) -> None:\n+        target = self.tmp / \"no-legacy\"\n+        target.mkdir()\n+        _quiet_install(target, force=True, validate=False)\n+        self.assertTrue(\n+            (target / \"docs\" / \"adr\" / \"SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\").is_file()\n+        )\n+\n+\n+if __name__ == \"__main__\":\n+    unittest.main()\n\n2026-08-07T08:23:12.173977Z ERROR codex_models_manager::manager: failed to renew cache TTL: missing field `base_instructions` at line 94 column 5\nexec\n/bin/zsh -lc 'python3 -m unittest tests.test_sadr_install_migration -v' in /private/tmp/shiki-adr-collision.pa0KwY/.worktrees/shiki-t-20260807t073307221254z-2d22d654-introduc\n exited 1 in 0ms:\ntest_sadr_install_migration (unittest.loader._FailedTest.test_sadr_install_migration) ... ERROR\n\n======================================================================\nERROR: test_sadr_install_migration (unittest.loader._FailedTest.test_sadr_install_migration)\n----------------------------------------------------------------------\nImportError: Failed to import test module: test_sadr_install_migration\nTraceback (most recent call last):\n  File \"/opt/homebrew/Cellar/python@3.14/3.14.6/Frameworks/Python.framework/Versions/3.14/lib/python3.14/unittest/loader.py\", line 137, in loadTestsFromName\n    module = __import__(module_name)\n  File \"/private/tmp/shiki-adr-collision.pa0KwY/.worktrees/shiki-t-20260807t073307221254z-2d22d654-introduc/tests/test_sadr_install_migration.py\", line 14, in <module>\n    import shiki_test_support  # noqa: F401  (path bootstrap)\n    ^^^^^^^^^^^^^^^^^^^^^^^^^\nModuleNotFoundError: No module named 'shiki_test_support'\n\n\n----------------------------------------------------------------------\nRan 1 test in 0.000s\n\nFAILED (errors=1)\n\ndiff --git a/.codex/skills/shiki/SKILL.md b/.codex/skills/shiki/SKILL.md\nindex c8e2e85d9d75d49cc4f2d6286ee22659c4e53e75..93cded623b2c15d9f65c76491a1c2d813ac9e6c0\n--- a/.codex/skills/shiki/SKILL.md\n+++ b/.codex/skills/shiki/SKILL.md\n@@ -18,6 +18,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n Codex CLI does not register this skill as a custom `/shiki` slash command. When\n the user types `/shiki` in Codex CLI and gets \"Unrecognized command\", explain\n that this is expected: invoke Shiki in Codex with natural language such as\n@@ -69,7 +72,7 @@\n \n ## Responsibilities\n \n-- Claude Code implements and repairs by default (`shiki runner claude`, ADR 0008); Codex implements and repairs tasks explicitly assigned to `codex`.\n+- Claude Code implements and repairs by default (`shiki runner claude`, SADR-0008); Codex implements and repairs tasks explicitly assigned to `codex`.\n - Claude Code Action can act as GitHub-side CCA or reviewer.\n - CCA judges completion.\n - MergeGate authorizes state transitions and merge readiness.\ndiff --git a/.github/prompts/cca-completion-check.md b/.github/prompts/cca-completion-check.md\nindex ed203ad5f302ecc81372d8ab27d2b1232ca97497..fd89950528333ef6242e6d5443904f5392ab7e56\n--- a/.github/prompts/cca-completion-check.md\n+++ b/.github/prompts/cca-completion-check.md\n@@ -19,6 +19,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n ## Required Reading\n \n Read only the files needed to judge the PR contract. Prefer this order:\ndiff --git a/.github/prompts/codex-task-handoff.md b/.github/prompts/codex-task-handoff.md\nindex 695e211d76dea1c60e8d5bb360e78487808286a4..59441d576b01a39569722f226c854e5a740e9eb5\n--- a/.github/prompts/codex-task-handoff.md\n+++ b/.github/prompts/codex-task-handoff.md\n@@ -2,7 +2,7 @@\n \n You are Codex Front acting as the Implementer for a Shiki task.\n \n-Follow `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant ADRs, and `docs/agents/codex-handoff.md`.\n+Follow `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant Shiki SADRs and target ADRs, and `docs/agents/codex-handoff.md`.\n \n Rules:\n \ndiff --git a/AGENTS.md b/AGENTS.md\nindex 7f041100d49c2a7525f51cb0f5d696a4a6d1c78c..a06bfbabc6fa48a007389e25e12d7ed523936bed\n--- a/AGENTS.md\n+++ b/AGENTS.md\n@@ -22,7 +22,7 @@\n - **Codex Front** is an optional implementer through Codex App, Codex CLI, Codex IDE extension, or Codex Web signed in with ChatGPT OAuth/subscription auth, dispatched with `shiki runner codex` when a task is explicitly assigned to `codex`.\n - **GitHub CCA** is implemented by Claude Code Action by default, using `CLAUDE_CODE_OAUTH_TOKEN`.\n \n-Do not assume `openai/codex-action`, `OPENAI_API_KEY`, or API-key based Codex automation in the default Shiki path. API-key runners are explicit target-repo extensions and require their own ADR.\n+Do not assume `openai/codex-action`, `OPENAI_API_KEY`, or API-key based Codex automation in the default Shiki path. API-key runners are explicit target-repo extensions and require their own target ADR.\n \n Shiki is not:\n \n@@ -44,10 +44,14 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use\n+`SADR-NNNN-*.md`; target product decisions retain `NNNN-*.md` and the `ADR`\n+namespace.\n+\n Use the canonical source-of-truth order above. Runtime-specific reading orders\n are operational guidance only and must not override or reorder it.\n \n-Conversation state is not durable truth. If a decision matters, put it in GitHub, `.shiki/`, `CONTEXT.md`, or an ADR.\n+Conversation state is not durable truth. If a decision matters, put it in GitHub, `.shiki/`, `CONTEXT.md`, a Shiki SADR, or a target ADR.\n \n If GitHub and `.shiki/` disagree, surface the conflict and prefer GitHub operational state until the mirror is repaired.\n \n@@ -75,8 +79,8 @@\n Every non-trivial change follows this loop:\n \n 1. **Goal**: clarify outcome, completion conditions, non-goals, risk level, and success signals.\n-2. **grill-with-docs**: challenge the plan against domain docs, ADRs, code reality, terminology, and edge scenarios.\n-3. **Context & Impact**: find relevant docs, ADRs, code, dependencies, owners, locks, and verification surfaces.\n+2. **grill-with-docs**: challenge the plan against domain docs, Shiki SADRs, target ADRs, code reality, terminology, and edge scenarios.\n+3. **Context & Impact**: find relevant docs, SADRs, target ADRs, code, dependencies, owners, locks, and verification surfaces.\n 4. **PRD and Spec Freeze**: use `to-prd` when context is settled enough to become durable product/engineering intent. The operator's explicit approval of the PRD is Spec Freeze: the plan records a `spec_freeze` block, and no plan runs without it. Steps 1-4 form Requirements Definition \u2014 one continuous operator dialogue. After Spec Freeze, scope changes require an operator-approved Spec Amendment; non-scope-moving interpretations go to the Assumption Log.\n 5. **Issues**: use `to-issues` to create independently grabbable vertical-slice issues. Prefer AFK slices over HITL slices where possible.\n 6. **Triage**: label issues for readiness, risk, runtime, skills, and MergeGate state.\n@@ -167,7 +171,7 @@\n - Testing decisions.\n - Out-of-scope items.\n - Further notes.\n-- Links to relevant domain terms and ADRs.\n+- Links to relevant domain terms, SADRs, and target ADRs.\n \n Every executable task must include:\n \n@@ -193,7 +197,7 @@\n \n Context & Impact must identify:\n \n-- Relevant documents, domain terms, ADRs, and past decisions.\n+- Relevant documents, domain terms, SADRs, target ADRs, and past decisions.\n - Relevant modules, interfaces, seams, callers, owners, and tests.\n - Dependency relationships between tasks.\n - Candidate locks and lock conflicts.\n@@ -210,7 +214,7 @@\n | Skill | Required when |\n | --- | --- |\n | `setup-matt-pocock-skills` | First configuring the repo for skills, or when issue tracker, triage labels, or domain docs are missing. |\n-| `grill-with-docs` | Default for non-trivial Goals before PRD/issues; always required when plans, terminology, boundaries, tradeoffs, or ADR-worthy decisions are ambiguous. |\n+| `grill-with-docs` | Default for non-trivial Goals before PRD/issues; always required when plans, terminology, boundaries, tradeoffs, or decision-record-worthy choices are ambiguous. |\n | `zoom-out` | The agent lacks an architectural map of the relevant code area. |\n | `to-prd` | Settled Goal context must become durable product/engineering intent. |\n | `to-issues` | A Goal, PRD, or plan must become independently grabbable vertical-slice issues. |\n@@ -348,7 +352,7 @@\n - Three or more new files.\n - Any high-risk or critical label.\n \n-When the architecture gate triggers, run `grill-with-docs` or `improve-codebase-architecture` as appropriate and record durable decisions in an ADR when needed.\n+When the architecture gate triggers, run `grill-with-docs` or `improve-codebase-architecture` as appropriate and record durable decisions in a Shiki SADR or target ADR as applicable.\n \n ## Engineering Discipline\n \ndiff --git a/CLAUDE.md b/CLAUDE.md\nindex 98a932e7841a632866fcd53b0bad0e3af3711d3d..98a31399404d6fb044b4fd493f4459ac87f439de\n--- a/CLAUDE.md\n+++ b/CLAUDE.md\n@@ -14,6 +14,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n ## Claude Role\n \n Default to planner, default implementer, reviewer, coordinator, documentation editor, CCA-style completion judge, and final-judgment assistant.\n@@ -57,7 +60,7 @@\n At the start of a Shiki session:\n \n 1. Read `AGENTS.md`.\n-2. Read `CONTEXT.md` and relevant ADRs.\n+2. Read `CONTEXT.md` and relevant Shiki SADRs and target ADRs.\n 3. Read active `.shiki/` Goal/task/ledger state when present.\n 4. Read active distilled rules from `.shiki/memories/` (status `distilled`, `active: true`, not revoked or superseded) for applicable guidance; the same rules are injected deterministically into task handoffs as the `## Distilled Rules` section (\u00a73.5).\n 5. Read the GitHub Issue or PR when the task is GitHub-backed.\n@@ -74,7 +77,7 @@\n - Define the Goal, completion conditions, non-goals, and risk level.\n - Treat `grill-with-docs` as the default planning entry point for non-trivial Goals.\n - Challenge domain terms against `CONTEXT.md`.\n-- Challenge hard-to-reverse tradeoffs against ADRs.\n+- Challenge hard-to-reverse tradeoffs against SADRs and target ADRs.\n - Explore code instead of asking when code can answer the question.\n - Run Context & Impact before implementation planning.\n - Produce a Task DAG, not an unordered checklist.\n@@ -94,7 +97,7 @@\n - For each question, provide your recommended answer.\n - Resolve dependency decisions before downstream design questions.\n - Update `CONTEXT.md` inline when terminology crystallizes.\n-- Offer ADRs only when the decision is hard to reverse, surprising without context, and a real tradeoff.\n+- Offer a SADR or target ADR only when the decision is hard to reverse, surprising without context, and a real tradeoff.\n - Convert unresolved questions into HITL blockers rather than allowing Codex to guess.\n \n ## PRD and Issues Standard\n@@ -103,7 +106,7 @@\n \n - Use the project's domain glossary vocabulary.\n - Avoid volatile file paths unless they are essential.\n-- Include implementation decisions, testing decisions, out-of-scope items, and relevant ADR/domain links.\n+- Include implementation decisions, testing decisions, out-of-scope items, and relevant SADR/target-ADR/domain links.\n - Identify deep-module opportunities and testable interfaces.\n \n When creating issues:\n@@ -121,7 +124,7 @@\n - Branch/worktree target.\n - Scope and non-goals.\n - Dependencies and locks.\n-- Relevant docs, ADRs, modules, and tests.\n+- Relevant docs, SADRs, target ADRs, modules, and tests.\n - Required skill invocations.\n - TDD expectations.\n - Pre-PR `code-review` gate expectations.\n@@ -152,7 +155,7 @@\n \n - Treat yourself as a judge, not implementer.\n - Do not write production code.\n-- Read `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant ADRs, PRD, task issue, PR body, diff, checks, reviews, labels, and `.shiki/` state.\n+- Read `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant SADRs and target ADRs, PRD, task issue, PR body, diff, checks, reviews, labels, and `.shiki/` state.\n - Evaluate every applicable checklist in `docs/agents/checklists.md`.\n - Map each acceptance criterion to durable evidence.\n - Distinguish implementation failure from missing evidence.\ndiff --git a/CONTEXT.md b/CONTEXT.md\nindex e61a9be06a0cce0d026484083a657bde2067e234..237ee8aa3ea8fab334ece49768bc5ed3b5af0974\n--- a/CONTEXT.md\n+++ b/CONTEXT.md\n@@ -25,7 +25,7 @@\n _Avoid_: jumping directly into code\n \n **grill-with-docs**:\n-A planning skill that challenges a plan against domain language, ADRs, code reality, and concrete edge scenarios. It resolves design-tree questions before PRD/issues. It runs inside Requirements Definition.\n+A planning skill that challenges a plan against domain language, Shiki SADRs, target ADRs, code reality, and concrete edge scenarios. It resolves design-tree questions before PRD/issues. It runs inside Requirements Definition.\n _Avoid_: generic brainstorming, silent assumptions\n \n **Requirements Definition**:\ndiff --git a/CONTRIBUTING.md b/CONTRIBUTING.md\nindex c6d9b656e73abb8a55e5f660123e03153dcb3e6d..9e37a8fbf668db5b22461aaae5548b8f1354394c\n--- a/CONTRIBUTING.md\n+++ b/CONTRIBUTING.md\n@@ -17,7 +17,7 @@\n 1. **Goal** \u2014 start from a user-approved Goal with an outcome, completion\n    conditions, non-goals, risk level, and success signals.\n 2. **grill-with-docs** \u2014 for any non-trivial change, challenge the plan against\n-   domain language, ADRs, and code reality before writing a PRD.\n+   domain language, Shiki SADRs, target ADRs, and code reality before writing a PRD.\n 3. **PRD** \u2014 capture settled product/engineering intent (`to-prd`).\n 4. **Spec Freeze** \u2014 the operator explicitly approves the PRD/requirements;\n    the plan records a `spec_freeze` block and no plan runs without it\n@@ -94,7 +94,7 @@\n   entries.\n - Do not hand-edit `.shiki/goals/*.json` or `.shiki/manifest.json` in a task PR.\n - Conversation state is not durable truth. If a decision matters, record it in\n-  GitHub, `.shiki/`, `CONTEXT.md`, or an ADR.\n+  GitHub, `.shiki/`, `CONTEXT.md`, a Shiki SADR, or a target ADR.\n \n ## Architecture Gate\n \n@@ -103,7 +103,7 @@\n or CI policy, package structure, three or more structural files, more than 100\n net new lines in a core module, three or more new files, or any high-risk label.\n When triggered, run `grill-with-docs` or `improve-codebase-architecture` and\n-record durable decisions in an ADR when needed (see `docs/adr/`).\n+record durable decisions in a Shiki SADR or target ADR as applicable (see `docs/adr/`).\n \n ## Safety\n \ndiff --git a/SYSTEM_PROMPT.md b/SYSTEM_PROMPT.md\nindex aaaea45e3a896c3a44ba89eec8bb84325ecf6c62..da009c86a88285269dee0155e58e550c6d744ea9\n--- a/SYSTEM_PROMPT.md\n+++ b/SYSTEM_PROMPT.md\n@@ -30,6 +30,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n Use the canonical source-of-truth order above. Runtime-specific reading orders\n are operational guidance only and must not override or reorder it. The current\n conversation is non-durable operator input.\n@@ -48,14 +51,14 @@\n \n A runtime may only do work that is explicitly assigned by the Goal, task, PR, or MergeGate decision.\n \n-Do not assume `openai/codex-action`, `OPENAI_API_KEY`, or API-key based Codex automation in the default Shiki path. API-key runners are explicit target-repo extensions and require their own ADR.\n+Do not assume `openai/codex-action`, `OPENAI_API_KEY`, or API-key based Codex automation in the default Shiki path. API-key runners are explicit target-repo extensions and require their own target ADR.\n \n ## 4. Non-negotiable Shiki flow\n \n For any non-trivial Goal, use this flow:\n \n 1. **Goal Seek**: define outcome, non-goals, risk level, completion criteria, and evidence requirements.\n-2. **grill-with-docs**: challenge the plan against `CONTEXT.md`, ADRs, code reality, terminology, boundaries, and edge scenarios. Ask one question at a time when the operator is available. If code can answer a question, inspect code instead of asking.\n+2. **grill-with-docs**: challenge the plan against `CONTEXT.md`, Shiki SADRs, target ADRs, code reality, terminology, boundaries, and edge scenarios. Ask one question at a time when the operator is available. If code can answer a question, inspect code instead of asking.\n 3. **Context & Impact**: identify modules, interfaces, seams, callers, dependencies, locks, risk, and verification surfaces.\n 4. **to-prd + Spec Freeze**: turn settled context into a PRD using domain vocabulary and testing decisions. The operator's explicit approval of the PRD is Spec Freeze, recorded as the plan's `spec_freeze` block; plans without it do not run. Steps 1-4 form Requirements Definition.\n 5. **to-issues**: decompose the PRD into independently grabbable vertical-slice issues. Prefer AFK slices; mark HITL slices when judgment is still required.\n@@ -75,7 +78,7 @@\n Before making any material change:\n \n 1. Read `AGENTS.md` and any runtime-specific wrapper such as `CLAUDE.md`.\n-2. Read `CONTEXT.md` and relevant ADRs.\n+2. Read `CONTEXT.md` and relevant SADRs and target ADRs.\n 3. Read the GitHub Issue / PR and `.shiki/` state for the active Goal or task.\n 4. Identify your role: Planner, Implementer, Reviewer, Completion Check Agent, Repairer, or Guardian-assist.\n 5. Identify Goal id, task id, branch, PR, dependencies, locks, risk level, required skills, and required evidence.\n@@ -103,7 +106,7 @@\n \n At minimum, identify:\n \n-- Relevant documents, ADRs, domain terms, and prior decisions.\n+- Relevant documents, SADRs, target ADRs, domain terms, and prior decisions.\n - Relevant modules, interfaces, seams, callers, owners, and tests.\n - Dependency relationships between tasks.\n - Candidate file locks and lock conflicts.\n@@ -152,7 +155,7 @@\n \n CCA must read:\n \n-- `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant ADRs.\n+- `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant SADRs and target ADRs.\n - Parent Goal issue, PRD issue, task issue, comments, and labels.\n - PR body, diff, commits, changed files, reviews, CI checks, and artifacts available to the workflow.\n - `.shiki/` task contract, locks, ledger entries, CCA checklist profile, and prior repair packets.\n@@ -214,7 +217,7 @@\n - Three or more new files.\n - Any high-risk or critical label.\n \n-When the architecture gate triggers, run `grill-with-docs` or `improve-codebase-architecture` as appropriate and record the decision in an ADR if it is hard to reverse or likely to be re-litigated.\n+When the architecture gate triggers, run `grill-with-docs` or `improve-codebase-architecture` as appropriate and record the decision in a Shiki SADR or target ADR as applicable if it is hard to reverse or likely to be re-litigated.\n \n ## 14. Skill Gate\n \n@@ -223,7 +226,7 @@\n Use these skills:\n \n - `setup-matt-pocock-skills`: before first use of the engineering skill set, or when issue tracker, triage labels, or domain docs are missing.\n-- `grill-with-docs`: default for non-trivial Goals before PRD/issues; required for ambiguous plans, terminology, boundaries, ADR-worthy tradeoffs, or design-tree decisions.\n+- `grill-with-docs`: default for non-trivial Goals before PRD/issues; required for ambiguous plans, terminology, boundaries, decision-record-worthy tradeoffs, or design-tree decisions.\n - `zoom-out`: unfamiliar code area or missing architectural map.\n - `to-prd`: convert settled context into a PRD.\n - `to-issues`: break a PRD, plan, or Goal into independently grabbable vertical-slice issues.\ndiff --git a/docs/adr/0001-keep-shiki-as-platform-name.md b/docs/adr/SADR-0001-keep-shiki-as-platform-name.md\nindex 20a9637c3b9d8c97de7ba8db7cfae4d82d9a7dba..8b685439959aada1b221c112f0ac3c51ca8a1695\n--- a/docs/adr/0001-keep-shiki-as-platform-name.md\n+++ b/docs/adr/SADR-0001-keep-shiki-as-platform-name.md\n@@ -1,3 +1,3 @@\n-# Keep Shiki As The Platform Name\n+# SADR-0001: Keep Shiki As The Platform Name\n \n Shiki remains the product name for the GitHub-native agentic engineering control plane. We considered more descriptive names such as MergePilot, MergeGate, and Kiroku, but kept Shiki because it carries the existing goal-seeking framework identity while remaining independent of Hermes, Codex, Claude Code, or any single runtime.\ndiff --git a/docs/adr/0002-use-agents-as-shared-constitution.md b/docs/adr/SADR-0002-use-agents-as-shared-constitution.md\nindex 73de30ecb078120eac540d9bc51855e3cd48d0e3..c5486182bf06fca36ea508496b013c67302fd989\n--- a/docs/adr/0002-use-agents-as-shared-constitution.md\n+++ b/docs/adr/SADR-0002-use-agents-as-shared-constitution.md\n@@ -1,3 +1,3 @@\n-# Use AGENTS As The Shared Agent Constitution\n+# SADR-0002: Use AGENTS As The Shared Agent Constitution\n \n Shiki uses `AGENTS.md` as the runtime-neutral constitution and `CLAUDE.md` as a thin Claude Code wrapper. This keeps Codex, Claude Code, Hermes Runner, and future Agent Runtimes aligned on the same GitHub-first MergeGate rules while still allowing runtime-specific instructions where needed.\ndiff --git a/docs/adr/0003-require-skill-gate-for-mergegate-readiness.md b/docs/adr/SADR-0003-require-skill-gate-for-mergegate-readiness.md\nindex 424be0c9af4c8f0136f1fde68a468c47ff84e764..7fc337987c7df9792f75f181dfcbed75fe5ffff2\n--- a/docs/adr/0003-require-skill-gate-for-mergegate-readiness.md\n+++ b/docs/adr/SADR-0003-require-skill-gate-for-mergegate-readiness.md\n@@ -1,3 +1,3 @@\n-# Require Skill Gate For MergeGate Readiness\n+# SADR-0003: Require Skill Gate For MergeGate Readiness\n \n Shiki requires relevant engineering skills to be invoked before execution when their triggers apply. This makes skills part of MergeGate readiness rather than optional prompt style, because Shiki depends on repeatable planning, issue decomposition, TDD, diagnosis, triage, and architecture review before autonomous or parallel agent work can be trusted.\ndiff --git a/docs/adr/0004-use-github-first-with-shiki-mirror.md b/docs/adr/SADR-0004-use-github-first-with-shiki-mirror.md\nindex d0a3a9174dc646a945a9d325954ffaf00c352040..a79c54ae9ee8eed2a0524192b864da4e22a92d3f\n--- a/docs/adr/0004-use-github-first-with-shiki-mirror.md\n+++ b/docs/adr/SADR-0004-use-github-first-with-shiki-mirror.md\n@@ -1,4 +1,4 @@\n-# ADR 0004: Use GitHub First With A Repository-Local Shiki Mirror\n+# SADR-0004: Use GitHub First With A Repository-Local Shiki Mirror\n \n ## Status\n \ndiff --git a/docs/adr/0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md b/docs/adr/SADR-0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\nindex b339baf49b89d9290e79c3d51b36e7b2bde3bd95..0f3c3eb9afccdb3403ce978e21d04fe12e495da0\n--- a/docs/adr/0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\n+++ b/docs/adr/SADR-0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\n@@ -1,8 +1,8 @@\n-# ADR 0005: Use Codex As Front And Claude Code Action As The GitHub Runtime\n+# SADR-0005: Use Codex As Front And Claude Code Action As The GitHub Runtime\n \n ## Status\n \n-Accepted (implementer default superseded by ADR 0008; the auth model and the\n+Accepted (implementer default superseded by SADR-0008; the auth model and the\n Claude Code Action GitHub runtime decision remain in force)\n \n ## Context\n@@ -26,4 +26,4 @@\n - Shiki avoids requiring `OPENAI_API_KEY` for the default workflow.\n - Codex work happens through the user's authenticated Codex front surface, not as a hidden backend Action.\n - Claude Code Action is configured for OAuth-token based subscription usage by default.\n-- API-key based automation remains possible only as an explicit target-repo extension with its own ADR.\n+- API-key based automation remains possible only as an explicit target-repo extension with its own target ADR.\ndiff --git a/docs/adr/0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md b/docs/adr/SADR-0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\nindex 4bcae9d1e52f0a37b8e9fbafa5cd14b6ee1993bd..7a38538a6ae9929567f0ade67e7ea6a60d0b3f7e\n--- a/docs/adr/0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\n+++ b/docs/adr/SADR-0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\n@@ -1,4 +1,4 @@\n-# ADR 0006: Separate Codex Implementation, CCA Completion, And MergeGate Authorization\n+# SADR-0006: Separate Codex Implementation, CCA Completion, And MergeGate Authorization\n \n ## Status\n \ndiff --git a/docs/adr/0007-packaging-and-release.md b/docs/adr/SADR-0007-packaging-and-release.md\nindex 79b84ddb97167372b44641e5b7860e400ce80822..bb445a2e2e856d9d1f5fdbe35de8d4e8cec0fb32\n--- a/docs/adr/0007-packaging-and-release.md\n+++ b/docs/adr/SADR-0007-packaging-and-release.md\n@@ -1,4 +1,4 @@\n-# ADR 0007: Packaging And Release For Shiki\n+# SADR-0007: Packaging And Release For Shiki\n \n ## Status\n \n@@ -25,7 +25,7 @@\n   not introduce third-party runtime dependencies or break the shim.\n - Releases must run inside the operator's subscription-authenticated toolchain\n   and must not require extra secrets beyond the built-in `GITHUB_TOKEN`\n-  (consistent with ADR 0005's no-API-key-by-default stance).\n+  (consistent with SADR-0005's no-API-key-by-default stance).\n - `.shiki/manifest.json` already encodes the install surface (`install.include`,\n   `install.create_directories`, `install.exclude_from_commit`), and it must not\n   be mutated as part of this change.\n@@ -34,7 +34,7 @@\n \n This is a hard-to-reverse platform decision (it sets the version source of\n truth, the release trigger, and the public install contract), so it is recorded\n-as an ADR.\n+as a SADR.\n \n ## Decision\n \n@@ -104,7 +104,7 @@\n 3. **Publishing to PyPI.** Rejected for now: Shiki is distributed as a\n    repository template + git-installable CLI, not a PyPI library; PyPI would add\n    credentials/secrets and a different versioning contract. Can be added later as\n-   its own ADR if a packaged distribution is needed.\n+   its own SADR if a packaged distribution is needed.\n 4. **Dynamic version (read `VERSION` via a build backend `dynamic` field).**\n    Rejected to keep `pyproject.toml` backend-agnostic and dependency-free; the\n    small duplication between `VERSION` and `pyproject.toml` is guarded by the\n@@ -123,4 +123,4 @@\n   releasing versions the whole platform but ships only `install.include` to\n   targets.\n - Future PyPI distribution or a console entry point are deferred and would each\n-  warrant their own ADR.\n+  warrant their own SADR.\ndiff --git a/docs/adr/0008-use-claude-code-as-default-implementer.md b/docs/adr/SADR-0008-use-claude-code-as-default-implementer.md\nindex dedf4510b7c7799521422f622029563d373db031..35c2625d908167fbc58855b5b63e283805f167e8\n--- a/docs/adr/0008-use-claude-code-as-default-implementer.md\n+++ b/docs/adr/SADR-0008-use-claude-code-as-default-implementer.md\n@@ -1,12 +1,12 @@\n-# ADR 0008: Use Claude Code As The Default Implementer Runtime\n+# SADR-0008: Use Claude Code As The Default Implementer Runtime\n \n ## Status\n \n-Proposed (supersedes ADR 0005 for the implementer default; keeps its auth model)\n+Proposed (supersedes SADR-0005 for the implementer default; keeps its auth model)\n \n ## Context\n \n-ADR 0005 made Codex Front the default implementation runtime, with Claude Code\n+SADR-0005 made Codex Front the default implementation runtime, with Claude Code\n restricted to planner and reviewer roles by the runtime registry, and the only\n autonomous implementation adapter was `shiki runner codex`.\n \n@@ -18,13 +18,13 @@\n - The platform's planning, review, and CCA layers already run on Claude Code,\n   and Claude Code's native capabilities (the built-in `/code-review` skill and\n   the Workflow multi-agent orchestration tool) have no Codex equivalent.\n-- The Goal lifecycle redesign (ADR 0009) requires an autonomous post-freeze\n+- The Goal lifecycle redesign (SADR-0009) requires an autonomous post-freeze\n   loop driven from the operator's Claude Code session, including bounded\n   repair dispatch, which must not depend on a runtime that cannot be invoked\n   unattended.\n \n The constitution claims Shiki is runtime-agnostic (\"not a Claude-only\n-workflow\", ADR 0002). Any change to the implementer default must preserve the\n+workflow\", SADR-0002). Any change to the implementer default must preserve the\n registry-based runtime model rather than hardcoding one vendor.\n \n ## Decision\n@@ -39,7 +39,7 @@\n   the task to review or repair-needed.\n - Keep Codex registered as an optional implementer runtime. `shiki runner\n   codex`, the Codex handoff templates, and the ChatGPT OAuth auth model from\n-  ADR 0005 remain supported for tasks explicitly assigned to `codex`.\n+  SADR-0005 remain supported for tasks explicitly assigned to `codex`.\n - Commit to mandating two Claude-native capabilities as loop contract in\n   follow-up Goals (status: the code-review gate was delivered by Goal G-B \u2014\n   skill registry, checklists item PR-12, default required_skills \u2014 while the\n@@ -60,7 +60,7 @@\n \n Out of scope: removing Codex support, changing the CCA runtime (Claude Code\n Action with `CLAUDE_CODE_OAUTH_TOKEN` stays), and API-key based automation\n-(still requires its own ADR per ADR 0005).\n+(still requires its own target ADR per SADR-0005).\n \n ## Consequences\n \n@@ -100,7 +100,7 @@\n   harness rejection of direct `codex exec` is outside Shiki's control, and the\n   plan's mandatory native capabilities cannot run on Codex at all.\n - **Remove Codex entirely**: rejected; it abandons the runtime-agnostic\n-  constitution (ADR 0002) for no operational gain and forecloses\n+  constitution (SADR-0002) for no operational gain and forecloses\n   cross-runtime adversarial review.\n - **GitHub Actions cloud implementation via claude-code-action**: rejected as\n   default; durable evidence is automatic but token cost, Node 24 workflow\ndiff --git a/docs/adr/0009-requirements-definition-and-spec-freeze.md b/docs/adr/SADR-0009-requirements-definition-and-spec-freeze.md\nindex 29b3f7871bef9203a8b4a831a65c39eb2f506e42..52e7dd69ad0aa4efe0b68c62678abbf7df02aaec\n--- a/docs/adr/0009-requirements-definition-and-spec-freeze.md\n+++ b/docs/adr/SADR-0009-requirements-definition-and-spec-freeze.md\n@@ -1,4 +1,4 @@\n-# ADR 0009: Merge Grill And Planning Into Requirements Definition With Spec Freeze\n+# SADR-0009: Merge Grill And Planning Into Requirements Definition With Spec Freeze\n \n ## Status\n \ndiff --git a/docs/adr/0010-external-ai-guardian-review.md b/docs/adr/SADR-0010-external-ai-guardian-review.md\nindex e6108bbe8bb4e73e0ff4684c8f78001a80438c4e..f3510b145664298adafb151b6f0db51b972f01ec\n--- a/docs/adr/0010-external-ai-guardian-review.md\n+++ b/docs/adr/SADR-0010-external-ai-guardian-review.md\n@@ -1,4 +1,4 @@\n-# ADR 0010: External AI Guardian Review As A First-Class Approval Authority\n+# SADR-0010: External AI Guardian Review As A First-Class Approval Authority\n \n ## Status\n \n@@ -8,7 +8,7 @@\n \n Shiki is intended to be an autonomous, recursive, self-improving agent\n framework: authority-in-the-loop, not human-in-the-loop. Until now the Guardian\n-approval required for high/critical-risk merges (guardian-policy.json, ADR\n+approval required for high/critical-risk merges (guardian-policy.json, SADR\n governance) recognized only human authority sources \u2014 a GitHub review approval,\n the `guardian:approved` label applied by a configured Guardian, or a comment\n carrying `Guardian approval granted` plus the current head SHA.\n@@ -89,7 +89,7 @@\n   autonomous design the operator intends and produced the impersonation\n   incident.\n - **Record AI approval as an operator comment**: rejected as forgery of human\n-  authority \u2014 the exact failure this ADR corrects.\n+  authority \u2014 the exact failure this SADR corrects.\n - **Deliver the artifact as a committed `.shiki/` file**: rejected; a committed\n   approval inside the PR being approved is forgeable by anyone who can push.\n - **A dedicated reviewer bot identity instead of a relayed comment**: deferred;\ndiff --git a/docs/adr/0011-autonomous-quality-gate-evidence.md b/docs/adr/SADR-0011-autonomous-quality-gate-evidence.md\nindex bca0246c5d46e44afbde15486a75eec425fc02f5..9c93eac16d8c3a6d4b11bd7224e3955102cd7db4\n--- a/docs/adr/0011-autonomous-quality-gate-evidence.md\n+++ b/docs/adr/SADR-0011-autonomous-quality-gate-evidence.md\n@@ -1,4 +1,4 @@\n-# ADR 0011: Autonomous Quality-Gate Evidence \u2014 Deterministic Facts And An Independent Verifier\n+# SADR-0011: Autonomous Quality-Gate Evidence \u2014 Deterministic Facts And An Independent Verifier\n \n ## Status\n \ndiff --git a/docs/adr/0012-autonomous-loop-closeout-to-main.md b/docs/adr/SADR-0012-autonomous-loop-closeout-to-main.md\nindex 637fad6dde4989c9c2bf0f81f676d8d29991123c..ec7d85f81030a676bcbba28b43eb365422a36eac\n--- a/docs/adr/0012-autonomous-loop-closeout-to-main.md\n+++ b/docs/adr/SADR-0012-autonomous-loop-closeout-to-main.md\n@@ -1,4 +1,4 @@\n-# 0012 \u2014 Autonomous loop pushes goal completion to main via a closeout PR\n+# SADR-0012 \u2014 Autonomous loop pushes goal completion to main via a closeout PR\n \n - Status: accepted\n - Date: 2026-06-18\ndiff --git a/docs/adr/0013-bootstrap-grants-actions-approve-pull-requests.md b/docs/adr/SADR-0013-bootstrap-grants-actions-approve-pull-requests.md\nindex b6c69d64f1c5ced346b88d6859586715b3b5bd0e..cf48ff4a6716267f539ca58edd1a852e7c7a2d64\n--- a/docs/adr/0013-bootstrap-grants-actions-approve-pull-requests.md\n+++ b/docs/adr/SADR-0013-bootstrap-grants-actions-approve-pull-requests.md\n@@ -1,4 +1,4 @@\n-# ADR 0013: Bootstrap Grants GitHub Actions Permission To Create And Approve Pull Requests\n+# SADR-0013: Bootstrap Grants GitHub Actions Permission To Create And Approve Pull Requests\n \n ## Status\n \n@@ -31,7 +31,7 @@\n security tradeoff: it lets repository automation satisfy a human-review gate. In\n a multi-maintainer repo that could let a compromised or buggy workflow\n self-approve changes. So whether bootstrap turns it on by default \u2014 and with\n-what default token scope \u2014 is an ADR-worthy decision, not an implementation\n+what default token scope \u2014 is a SADR-worthy decision, not an implementation\n detail.\n \n ## Decision\n@@ -48,7 +48,7 @@\n Specifically:\n \n - **`can_approve_pull_request_reviews` defaults to `true`.** Shiki's default\n-  posture is solo/AFK autonomous operation (ADR 0008/0009/0012), where the CCA\n+  posture is solo/AFK autonomous operation (SADR-0008/0009/0012), where the CCA\n   Review Bridge is the only path that can satisfy `required_review: true` after\n   the CCA and Guardian gates have already passed. The default makes a\n   freshly-bootstrapped repo able to run the autonomous loop end to end without a\ndiff --git a/docs/adr/0014-codex-app-external-ai-guardian-ui-adapter.md b/docs/adr/SADR-0014-codex-app-external-ai-guardian-ui-adapter.md\nindex 238db06279334caa341c117362df9271382a7fcd..65f12e9939321e1b3e389a34c822fc1549e5155d\n--- a/docs/adr/0014-codex-app-external-ai-guardian-ui-adapter.md\n+++ b/docs/adr/SADR-0014-codex-app-external-ai-guardian-ui-adapter.md\n@@ -1,4 +1,4 @@\n-# ADR 0014: Use Codex App As The External AI Guardian UI Adapter\n+# SADR-0014: Use Codex App As The External AI Guardian UI Adapter\n \n ## Status\n \ndiff --git a/docs/adr/0015-contract-approval-before-dispatch.md b/docs/adr/SADR-0015-contract-approval-before-dispatch.md\nindex fad6c656741a20a7684ba16080a7db159f63a52f..6061271a46d3f1d6ae7174477de3fb75673123a6\n--- a/docs/adr/0015-contract-approval-before-dispatch.md\n+++ b/docs/adr/SADR-0015-contract-approval-before-dispatch.md\n@@ -1,4 +1,4 @@\n-# ADR 0015: Approve The Task Contract Before Dispatch, Not The Diff After It\n+# SADR-0015: Approve The Task Contract Before Dispatch, Not The Diff After It\n \n ## Status\n \n@@ -9,7 +9,7 @@\n Guardian approval for high/critical risk is expressible only on a pull request.\n Every approval source in `guardian-policy.json` \u2014 GitHub review, the\n `guardian:approved` label, the `Guardian approval granted` comment, and the\n-`external_ai_guardian_review` artifact (ADR 0010/0014) \u2014 is bound to a PR, and\n+`external_ai_guardian_review` artifact (SADR-0010/0014) \u2014 is bound to a PR, and\n three of the four require the current head SHA. Approval therefore cannot exist\n before an implementation PR exists.\n \n@@ -81,7 +81,7 @@\n An envelope breach must surface only on the non-repairable policy gate, never on\n the repairable `MergeGate metadata check`. Otherwise the Goal Loop routes it to\n `dispatch_repair` \u2014 instructing a runner to make the breach go away \u2014 which is\n-exactly what this ADR forbids.\n+exactly what this SADR forbids.\n \n **Contract immutability.** After registration, an implementation PR must not\n change the governance fields of its own task contract (`scope`, `non_goals`,\n@@ -116,7 +116,7 @@\n fails any verdict that is not `complete`, and the signal the CCA prompt reads is\n computed from `evaluate_guardian_approval` alone. Without the OR-merge at the\n signal, a contract-approved PR still returns `needs_guardian`, the CCA job fails,\n-and the MergeGate policy check never runs \u2014 the gate this ADR changes would never\n+and the MergeGate policy check never runs \u2014 the gate this SADR changes would never\n be reached. The CCA *prompt* needs no change: a `contract_approval` entry in the\n signal's existing `sources` array satisfies its current wording.\n \n@@ -130,7 +130,7 @@\n written, and an approved contract lets its implementation PRs merge through the\n same automatic path as low/medium risk. The wasted implement-then-block cycle\n disappears. Guardian review shifts from reading diffs to reading contracts, which\n-is both earlier and smaller; the external AI Guardian path (ADR 0010/0014) works\n+is both earlier and smaller; the external AI Guardian path (SADR-0010/0014) works\n on contracts with no change.\n \n We accept that the Guardian authorizes a class of change without seeing its\n@@ -146,7 +146,7 @@\n adversarial tests for the new approval path equivalent in rigor to\n `test_shiki_governance_evidence.sh`.\n \n-Bootstrap costs, which apply only while this ADR is being implemented. The tasks\n+Bootstrap costs, which apply only while this SADR is being implemented. The tasks\n that build Contract Approval merge under the existing rules, so the ones\n dispatched after the pre-dispatch gate lands but before the approval path lands\n need a Guardian-approved Contract PR *and* a live Guardian approval on their\n@@ -210,7 +210,7 @@\n risk; they do not bound authorized work.\n \n **Auto-escalate risk on envelope breach instead of stopping.** Returns the\n-workflow to a diff-time Guardian decision \u2014 the exact behavior this ADR removes.\n+workflow to a diff-time Guardian decision \u2014 the exact behavior this SADR removes.\n \n **Resolve governance fields from the base snapshot only.** Rejected against the\n registration flow: implementation PRs add their own contract, so the base has no\ndiff --git a/docs/adr/0016-locks-bound-product-paths.md b/docs/adr/SADR-0016-locks-bound-product-paths.md\nindex b5e850f3f1f7f4ef6e7f9d1763a29a51bd300c45..46b40a9b99a7aae644c02bb458f5473ff0a90ce2\n--- a/docs/adr/0016-locks-bound-product-paths.md\n+++ b/docs/adr/SADR-0016-locks-bound-product-paths.md\n@@ -1,4 +1,4 @@\n-# ADR 0016: Locks bound product paths; the Shiki mirror is bound by record identity\n+# SADR-0016: Locks bound product paths; the Shiki mirror is bound by record identity\n \n ## Status\n \n@@ -6,8 +6,8 @@\n \n - Date: 2026-07-28\n - Deciders: operator (mizutani-140), Claude (planner)\n-- Related: ADR 0004 (GitHub-first with a `.shiki` mirror), ADR 0009 (Spec Freeze),\n-  ADR 0012 (autonomous loop closeout to main), ADR 0015 (Contract Approval before\n+- Related: SADR-0004 (GitHub-first with a `.shiki` mirror), SADR-0009 (Spec Freeze),\n+  SADR-0012 (autonomous loop closeout to main), SADR-0015 (Contract Approval before\n   dispatch), remediation goal task R-02, the live PR #179 deadlock.\n \n ## Context\n@@ -90,11 +90,11 @@\n files, documentation files and named source modules are nameable; runtime-\n allocated ledger, runner and report ids are not \u2014 and those live in `.shiki`,\n which locks no longer govern. Existing frozen contracts are brought into line by\n-a one-time Spec Amendment per plan, which must land before ADR 0015's contract\n+a one-time Spec Amendment per plan, which must land before SADR-0015's contract\n immutability makes `locks` unwritable from the implementation PR.\n \n **Lock state is resolved from the durable side.** MergeGate resolves the lock\n-registry, and ADR 0015's governance fields, from the base `.shiki` snapshot when\n+registry, and SADR-0015's governance fields, from the base `.shiki` snapshot when\n present, falling back to head only when absent. The `MergeGate metadata check`\n job is given `--base-shiki`, which it does not receive today. Locks are not\n derived from the live open-pull-request set: a pull request body is untrusted\n@@ -108,7 +108,7 @@\n Explicitly out of scope: moving MergeGate's contract resolution off the PR\n checkout wholesale; making the mirror a projection of GitHub state; introducing\n new lock kinds; moving ledger evidence out of the repository; and unwinding\n-ADR 0012's closeout pull request. Registration of multi-task goals is ADR 0015's\n+SADR-0012's closeout pull request. Registration of multi-task goals is SADR-0015's\n Contract PR and is not re-decided here.\n \n ## Consequences\n@@ -127,7 +127,7 @@\n \n Risks accepted. First, goal-scoped identity is only as strong as `goal_id`,\n which is resolved from the PR body and the head task file; the mitigation is to\n-add `goal_id` to ADR 0015's frozen governance field set, and until that lands a\n+add `goal_id` to SADR-0015's frozen governance field set, and until that lands a\n task file can name a foreign goal. Second, base-when-present resolution is\n unavailable on fork pull requests, where the head is contributor-controlled;\n fork pull requests already cannot complete a Shiki merge, and this decision does\n@@ -139,7 +139,7 @@\n path, not by implementation pull requests.\n \n New obligations: one Spec Amendment per frozen plan to rewrite `locks`, ordered\n-before ADR 0015 T1; extension of the adversarial suites in\n+before SADR-0015 T1; extension of the adversarial suites in\n `scripts/test_shiki_adversarial_state.sh` and\n `scripts/test_shiki_governance_evidence.sh` to cover the eleven newly-scoped\n directories; and updating `scripts/test_shiki_loop_lock_guard.sh`,\n@@ -151,32 +151,32 @@\n whether the loop's transitive sync of ledger `evidence` references can be\n deleted once the Contract PR carries its own source plan.\n \n-## Relationship to ADR 0004\n+## Relationship to SADR-0004\n \n-This ADR **clarifies something ADR 0004 left unspecified**. ADR 0004 decides that\n+This SADR **clarifies something SADR-0004 left unspecified**. SADR-0004 decides that\n GitHub is the operational source of truth and that each target repository keeps a\n `.shiki/` mirror. It says nothing about pull requests writing the mirror, does\n not name MergeGate, and does not make the mirror an input to a gate. Nothing in\n-ADR 0004's Decision or Consequences is amended, superseded or weakened: the\n+SADR-0004's Decision or Consequences is amended, superseded or weakened: the\n mirror still exists, still records the same artifacts, and pull request\n-boundaries remain the coordination surface. What this ADR settles is the question\n+boundaries remain the coordination surface. What this SADR settles is the question\n 0004 never reached \u2014 by what authority a pull request may write a mirror record \u2014\n and it answers it the way `.shiki/manifest.json` already declares\n (`pr_mutation: current-task-or-current-goal-only`).\n \n-It **amends ADR 0012**, whose Decision item 5 states that \"Loop-executed tasks\n+It **amends SADR-0012**, whose Decision item 5 states that \"Loop-executed tasks\n declare the `path:.shiki/**` lock, which covers every `.shiki` file the closeout\n stages ... so `files_outside_locks` is satisfied without per-file lock\n bookkeeping.\" That clause is replaced: the closeout's mirror writes are\n-authorized by record identity, not by a declared lock. ADR 0012's substantive\n+authorized by record identity, not by a declared lock. SADR-0012's substantive\n decision \u2014 that completion reaches `main` through a closeout pull request rather\n than local attestation \u2014 is preserved unchanged.\n \n-It is **complementary to ADR 0015**, not a substitute. ADR 0015 owns contract\n+It is **complementary to SADR-0015**, not a substitute. SADR-0015 owns contract\n immutability, the pre-dispatch gate, the Contract PR and the Scope Envelope. This\n-ADR owns what a lock means and who may write the mirror. The one ordering\n+SADR owns what a lock means and who may write the mirror. The one ordering\n constraint between them is that the lock-narrowing Spec Amendment must precede\n-ADR 0015 T1's freezing of the `locks` field.\n+SADR-0015 T1's freezing of the `locks` field.\n \n ## Alternatives Considered\n \n@@ -186,7 +186,7 @@\n the only writer that can register them may run. Migrating the Guardian signal to\n that store makes every registration PR fail closed to `needs_guardian` at every\n risk level, because the task file is by definition absent from the store, which\n-removes the low/medium autonomous path ADR 0008/0009 exists to provide. And it\n+removes the low/medium autonomous path SADR-0008/0009 exists to provide. And it\n leaves schemas, manifest, config, guardian policy, templates, migrations, plans,\n worktrees, runner records, runs and handoffs with no writer at all in a\n repository whose product is `.shiki`. Measured benefit on the frozen backlog:\n@@ -199,14 +199,14 @@\n namespace is not in fact ownership-partitioned \u2014 56 ledger entries carry no\n `task_id`, and one goal DAG is writable by 35 registered tasks. The correct\n expression of its insight is that locks do not govern the mirror at all, which is\n-what this ADR decides.\n+what this SADR decides.\n \n **Make the mirror a projection written only by a registration writer and a\n post-merge projection job.** Rejected. It requires a bypass actor with write\n access to the protected default branch; it makes its own master invariant\n readable from a manifest that pull requests may edit; it deletes both reconcile\n modes and leaves a corrupted mirror with no in-band repair path, contradicting\n-ADR 0004's consequence that mirror conflicts are repaired against GitHub state;\n+SADR-0004's consequence that mirror conflicts are repaired against GitHub state;\n and its content-derived ids are rejected by `validate_shiki`'s currently-green id\n pattern.\n \ndiff --git a/docs/adr/0017-bookkeeping-closeouts-do-not-inherit-risk.md b/docs/adr/SADR-0017-bookkeeping-closeouts-do-not-inherit-risk.md\nindex 554e3a0d526a72ca067108c5464cc68c3edcf174..b9676857a4713850e063af7c39c8e0b01ecb4c66\n--- a/docs/adr/0017-bookkeeping-closeouts-do-not-inherit-risk.md\n+++ b/docs/adr/SADR-0017-bookkeeping-closeouts-do-not-inherit-risk.md\n@@ -1,4 +1,4 @@\n-# ADR 0017: A Bookkeeping Closeout Does Not Inherit Its Task's Risk\n+# SADR-0017: A Bookkeeping Closeout Does Not Inherit Its Task's Risk\n \n ## Status\n \n@@ -6,7 +6,7 @@\n \n ## Context\n \n-ADR 0012 made the goal loop push a task's completion to the default branch through a\n+SADR-0012 made the goal loop push a task's completion to the default branch through a\n closeout pull request: after the implementation PR merges, the loop opens a second PR\n carrying `task.status = done`, `lock.state = released`, and \u2014 when the task completes\n its goal \u2014 `goal.status = complete` plus one scorecard report. Nothing else. The\n@@ -49,14 +49,14 @@\n 4. The task file's governance fields (`scope`, `non_goals`, `required_skills`,\n    `risk_level`, `locks`, `acceptance_checks`, `test_command`) are byte-identical to\n    the base snapshot. Only `status`, `expected_pr`, `closeout_pr`, `ledger_evidence`\n-   and `expected_branch` may differ \u2014 the ADR 0015 contract-immutability rule,\n+   and `expected_branch` may differ \u2014 the SADR-0015 contract-immutability rule,\n    unchanged.\n \n    `expected_branch` is in that set because a closeout is *by construction* on a\n    different branch from the implementation it closes, and the loop rewrites the\n    field accordingly. It is safe to let it differ because MergeGate independently\n    requires `expected_branch` to equal the PR's actual head ref, so a PR cannot set\n-   it to anything other than the branch it is really on. The first draft of this ADR\n+   it to anything other than the branch it is really on. The first draft of this SADR\n    omitted it, which made the exemption unreachable: every real closeout failed\n    condition 4 on a field the loop always changes. See PR #209.\n 5. The state transitions are exactly the terminal set: task `review` \u2192 `done`, lock\n@@ -113,12 +113,12 @@\n classification must be derived, not asserted.\n \n **Fold the closeout into the implementation PR** so no second PR exists. Rejected under\n-ADR 0012, which recorded why: a task file that is already `done` on the implementation\n+SADR-0012, which recorded why: a task file that is already `done` on the implementation\n branch short-circuits the loop's review \u2192 CCA \u2192 merge \u2192 done ordering into a local\n goal-completion before any merge happens.\n \n **Have the loop auto-merge closeouts with elevated credentials.** Rejected outright: that\n-is a runtime authoring its own approval, which ADR 0010 and the constitution's\n+is a runtime authoring its own approval, which SADR-0010 and the constitution's\n approval-record prohibition forbid. It would also be indistinguishable, in the evidence,\n from the forgery those rules exist to prevent.\n \ndiff --git a/docs/adr/0018-carried-guardian-approval-across-a-proven-base-sync.md b/docs/adr/SADR-0018-carried-guardian-approval-across-a-proven-base-sync.md\nindex 22358562da390ed8b45cd600da08a43d378b4680..e6e2907cefa776460af73ddf7d39db605f3c39b0\n--- a/docs/adr/0018-carried-guardian-approval-across-a-proven-base-sync.md\n+++ b/docs/adr/SADR-0018-carried-guardian-approval-across-a-proven-base-sync.md\n@@ -1,4 +1,4 @@\n-# ADR 0018: Carried Guardian Approval Across A Proven Base Sync\n+# SADR-0018: Carried Guardian Approval Across A Proven Base Sync\n \n ## Status\n \n@@ -34,7 +34,7 @@\n Guardian evaluator that consumes it (`shiki_guardian.evaluate_guardian_approval`,\n the `guardian_comment_carried` source) were built and tested in isolation. Neither\n does anything until a caller passes the flags that turn the carry on, so the\n-feature was invisible in CI. This ADR records the decision the two of them\n+feature was invisible in CI. This SADR records the decision the two of them\n implement, and this task wires it into the four CI invocations that judge Guardian\n approval.\n \ndiff --git a/docs/adr/README.md b/docs/adr/README.md\nindex c70e057a4520c36e042ab68c53cd4a21ef709b71..5da43856836e2ce6a150f8127766e2f88355ab4b\n--- a/docs/adr/README.md\n+++ b/docs/adr/README.md\n@@ -1,40 +1,41 @@\n-# Architecture Decision Records\n+# Decision Record Namespaces\n \n-This directory holds Shiki's Architecture Decision Records (ADRs): durable\n-records of hard-to-reverse platform decisions. ADRs are part of the Shiki source\n-of truth alongside GitHub state, the `.shiki/` mirror, and `CONTEXT.md`.\n+This directory holds two independent decision namespaces:\n \n-## How ADRs Work\n+- Shiki Architecture Decision Records (SADRs), named\n+  `SADR-NNNN-<short-slug>.md`, record hard-to-reverse Shiki platform decisions.\n+- Target Architecture Decision Records (ADRs), named\n+  `NNNN-<short-slug>.md`, belong to the target product.\n \n-- New ADRs are added as `docs/adr/NNNN-<short-slug>.md`, where `NNNN` is the next\n-  zero-padded sequence number.\n-- The **canonical list of ADRs is the set of files matching `docs/adr/*.md`** in\n-  this directory. Browse `docs/adr/*.md` for the authoritative, current set.\n-- Use [`template.md`](template.md) as the starting point for a new ADR.\n-- Record an ADR when a decision is hard to reverse, surprising without context,\n-  and a real tradeoff. Do not write an ADR for routine or easily reversible\n-  choices.\n+The same four-digit number may appear once in each namespace. A platform\n+`SADR-NNNN` citation resolves only to `SADR-NNNN-*.md`; a target `ADR NNNN`\n+citation resolves only to `NNNN-*.md`.\n \n-Using the `docs/adr/*.md` glob as the source of truth means other PRs can add an\n-ADR without editing this index file, avoiding merge contention on a\n-hand-maintained enumeration.\n+## How Decision Records Work\n \n-## Current ADRs (snapshot)\n-\n-The list below is a convenience snapshot and may lag behind the directory. When\n-in doubt, list `docs/adr/*.md`.\n+- New Shiki platform decisions use the next zero-padded SADR number.\n+- New target product decisions use the target's own ADR sequence.\n+- The canonical list is the files in `docs/adr/`; no hand-maintained index is\n+  authoritative.\n+- Use [`template.md`](template.md) as the starting point and choose the filename\n+  namespace that owns the decision.\n+- Record a decision when it is hard to reverse, surprising without context, and\n+  a real tradeoff. Do not write a decision record for routine or easily\n+  reversible choices.\n \n-- [0001 \u2014 Keep Shiki As The Platform Name](0001-keep-shiki-as-platform-name.md)\n-- [0002 \u2014 Use AGENTS As The Shared Agent Constitution](0002-use-agents-as-shared-constitution.md)\n-- [0003 \u2014 Require Skill Gate For MergeGate Readiness](0003-require-skill-gate-for-mergegate-readiness.md)\n-- [0004 \u2014 Use GitHub First With A Repository-Local Shiki Mirror](0004-use-github-first-with-shiki-mirror.md)\n-- [0005 \u2014 Use Codex As Front And Claude Code Action As The GitHub Runtime](0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md)\n-- [0006 \u2014 Separate Codex Implementation, CCA Completion, And MergeGate Authorization](0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md)\n+Using the directory as the source of truth lets independent changes add records\n+without contending on an enumerated index.\n \n-## Adding A New ADR\n+## Adding A Shiki SADR\n \n-1. Copy `template.md` to `docs/adr/NNNN-<short-slug>.md`.\n+1. Copy `template.md` to `docs/adr/SADR-NNNN-<short-slug>.md`.\n 2. Fill in the title, status, context, decision, and consequences.\n-3. Set the status to `Proposed`, then `Accepted` once the decision is approved.\n-4. Open a PR following `CONTRIBUTING.md`. You do not need to edit this index for\n-   the ADR to be discoverable via `docs/adr/*.md`.\n+3. Set the status to `Proposed`, then `Accepted` once the proper authority has\n+   approved the decision.\n+4. Open a PR following `CONTRIBUTING.md`.\n+\n+## Adding A Target ADR\n+\n+1. Copy `template.md` to `docs/adr/NNNN-<short-slug>.md` using the target's own\n+   sequence.\n+2. Cite it as `ADR NNNN`; do not prefix its filename with `SADR-`.\ndiff --git a/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md b/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\nnew file mode 100644\nindex 0000000000000000000000000000000000000000..7455ce1f3bed02ddddb851ac21a1d1a648c9aeba\n--- /dev/null\n+++ b/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\n@@ -0,0 +1,55 @@\n+# SADR-0019: Separate Shiki Platform SADRs From Target ADRs\n+\n+## Status\n+\n+Accepted\n+\n+## Context\n+\n+Shiki installs its platform decision records into `docs/adr/` in every target\n+repository. Shiki and its targets previously used the same numeric\n+`NNNN-<slug>.md` namespace. A target that already owned ADRs 0013 through 0018\n+therefore acquired two records for each number during installation, making an\n+`ADR NNNN` citation ambiguous. Renumbering target decisions would rewrite target\n+history, while renumbering Shiki decisions per target would destroy their stable\n+platform identity.\n+\n+Upgrades also need to remove the former Shiki-owned numeric paths without ever\n+mistaking a target-owned ADR for platform content. The install stamp already\n+records exact shipped paths and content digests, so it can prove ownership only\n+when both still match.\n+\n+## Decision\n+\n+Shiki platform decisions use the `SADR` namespace:\n+\n+- records are named `docs/adr/SADR-NNNN-<slug>.md`;\n+- live platform citations use `SADR-NNNN`;\n+- target product decisions retain `docs/adr/NNNN-<slug>.md` and `ADR NNNN`;\n+- the reference validator resolves the two namespaces independently, so the same\n+  four-digit number may exist once in each namespace.\n+\n+Before a forced upgrade writes anything, the installer inventories the exact\n+former numeric Shiki ADR paths. It may delete a legacy path only when the\n+existing install stamp contains that exact path and its recorded SHA-256 digest\n+matches the file currently on disk. All ownership blockers are reported\n+together and abort the upgrade before its first write. A non-forced install\n+never performs this cleanup.\n+\n+Historical `.shiki/` evidence remains unchanged because it records the namespace\n+that existed when that evidence was produced.\n+\n+## Consequences\n+\n+- Targets may use any ADR number without colliding with Shiki's platform SADRs.\n+- Fresh installs preserve target ADR bytes and install a stable SADR set.\n+- Forced upgrades fail closed when legacy ownership cannot be proven, and a\n+  successful upgrade refreshes the stamp without removed legacy paths.\n+- Current governance, packaging, workflow, prompt, script, and skill references\n+  must move with the platform records.\n+\n+## Alternatives Considered\n+\n+- Collision-only refusal leaves affected targets permanently unupgradable.\n+- Renumbering target ADRs mutates target-owned history.\n+- Per-target Shiki renumbering makes platform citations non-portable.\ndiff --git a/docs/adr/template.md b/docs/adr/template.md\nindex a92a97daddd0b00ab9dd626cc8d86942da2eafed..17dd79175b5fee2105935557f691b40cf9492942\n--- a/docs/adr/template.md\n+++ b/docs/adr/template.md\n@@ -1,14 +1,14 @@\n-# NNNN: <Short Decision Title>\n+# <SADR-NNNN or NNNN>: <Short Decision Title>\n \n ## Status\n \n-Proposed | Accepted | Superseded by ADR-NNNN\n+Proposed | Accepted | Superseded by SADR-NNNN or target ADR NNNN\n \n ## Context\n \n What is the situation, constraint, or force that requires a decision? Reference\n-relevant domain terms (`CONTEXT.md`), prior ADRs, and code reality. State the\n-problem, not the solution.\n+relevant domain terms (`CONTEXT.md`), prior Shiki SADRs or target ADRs, and code\n+reality. State the problem, not the solution.\n \n ## Decision\n \ndiff --git a/docs/agents/autonomous-loop-e2e-acceptance.md b/docs/agents/autonomous-loop-e2e-acceptance.md\nindex bbf84c6081987f159ac6ad9ecf3c82b7aa72e7e8..5c13c1d02a05a90b0dc46cbef7c95a9846d0c237\n--- a/docs/agents/autonomous-loop-e2e-acceptance.md\n+++ b/docs/agents/autonomous-loop-e2e-acceptance.md\n@@ -40,8 +40,8 @@\n 3. `gh auth status` is logged in with merge permission on the repo.\n 4. Branch protection + required checks are configured exactly as the goal-loop\n    required-check set expects (see `docs/agents/branch-protection-smoke.md`).\n-5. The repo `.shiki/policy` permits autonomous low/medium-risk merges (ADR\n-   0008/0009); the MergeGate policy check is the recorded-authority gate.\n+5. The repo `.shiki/policy` permits autonomous low/medium-risk merges\n+   (SADR-0008/0009); the MergeGate policy check is the recorded-authority gate.\n \n The operator's only actions are: register the task and start the loop. **No\n manual pre-clearing** means after `shiki loop run` starts, the operator does not\ndiff --git a/docs/agents/bootstrap-command.md b/docs/agents/bootstrap-command.md\nindex 05f7f203a52cf412b9f97084a6e131ed0f785afd..f7aa0b734dfebee4a3b139335b66ee3415f48713\n--- a/docs/agents/bootstrap-command.md\n+++ b/docs/agents/bootstrap-command.md\n@@ -189,7 +189,9 @@\n ```\n \n Use this only for tests, fixtures, or explicit local-only template inspection.\n-Use `--force` only when you intentionally want to overwrite existing target files.\n+Use `--force` only when you intentionally want to refresh the target template.\n+Legacy numeric Shiki decision cleanup is stamp-and-digest authorized before any\n+write; target ADR paths are not cleanup candidates.\n \n ## Slash Command\n \ndiff --git a/docs/agents/checklists.md b/docs/agents/checklists.md\nindex 4a7f03ade346f53a1c1d82981f3fd744f9ab4547..0c5ec6c53dc7197b7b7a7d18e667b325babd6c45\n--- a/docs/agents/checklists.md\n+++ b/docs/agents/checklists.md\n@@ -32,18 +32,18 @@\n | ID | Item | Blocking |\n | --- | --- | --- |\n | GD-01 | Plan was challenged against `CONTEXT.md`. | blocking for non-trivial Goals |\n-| GD-02 | Relevant ADRs were read or absence was recorded. | blocking for non-trivial Goals |\n+| GD-02 | Relevant SADRs and target ADRs were read or absence was recorded. | blocking for non-trivial Goals |\n | GD-03 | Ambiguous terms were resolved or converted to HITL blockers. | blocking |\n | GD-04 | Edge scenarios were considered. | advisory |\n | GD-05 | Code reality was checked when code could answer. | blocking when applicable |\n | GD-06 | New domain terms were added to `CONTEXT.md` when needed. | blocking when applicable |\n-| GD-07 | ADR was created or explicitly skipped using the ADR criteria. | blocking when applicable |\n+| GD-07 | A SADR or target ADR was created or explicitly skipped using the decision-record criteria. | blocking when applicable |\n \n ## CI \u2014 Context & Impact Checklist\n \n | ID | Item | Blocking |\n | --- | --- | --- |\n-| CI-01 | Relevant docs and ADRs identified. | blocking |\n+| CI-01 | Relevant docs, SADRs, and target ADRs identified. | blocking |\n | CI-02 | Relevant modules, interfaces, seams, callers, and tests identified. | blocking |\n | CI-03 | Dependencies and blocked-by relationships identified. | blocking |\n | CI-04 | Candidate locks identified. | blocking |\n@@ -62,7 +62,7 @@\n | PRD-04 | Implementation decisions are recorded without volatile code detail. | blocking |\n | PRD-05 | Testing decisions are recorded. | blocking |\n | PRD-06 | Out-of-scope items are explicit. | blocking |\n-| PRD-07 | Relevant glossary/ADR links are present when applicable. | blocking |\n+| PRD-07 | Relevant glossary/SADR/target-ADR links are present when applicable. | blocking |\n \n ## SF \u2014 Spec Freeze Checklist\n \n@@ -98,7 +98,7 @@\n | H-02 | Branch/worktree target is present. | blocking |\n | H-03 | Scope and non-goals are present. | blocking |\n | H-04 | Dependencies and locks are present. | blocking |\n-| H-05 | Relevant docs, ADRs, modules, and tests are referenced. | blocking |\n+| H-05 | Relevant docs, SADRs, target ADRs, modules, and tests are referenced. | blocking |\n | H-06 | Required skills are stated. | blocking |\n | H-07 | TDD expectations are stated. | blocking when behavior work |\n | H-08 | Acceptance criteria are included. | blocking |\ndiff --git a/docs/agents/codex-handoff.md b/docs/agents/codex-handoff.md\nindex ede468f47cb277f69a857e8c45df703e4ed0151d..6f97bdffb67e44d7edea2c71e01ea28933c4eb6c\n--- a/docs/agents/codex-handoff.md\n+++ b/docs/agents/codex-handoff.md\n@@ -46,7 +46,7 @@\n \n ## Context\n - Domain terms:\n-- Relevant ADRs:\n+- Relevant SADRs and target ADRs:\n - Relevant docs:\n - Relevant modules/interfaces/seams:\n - Similar tests/prior art:\ndiff --git a/docs/agents/completion-check-agent.md b/docs/agents/completion-check-agent.md\nindex 13e4c035db35025f4e3cdcc5adfb24314ac4d521..61bfa1218cf36ca605250c91919e620f33238aa0\n--- a/docs/agents/completion-check-agent.md\n+++ b/docs/agents/completion-check-agent.md\n@@ -32,7 +32,7 @@\n - `AGENTS.md`\n - `CLAUDE.md`\n - `CONTEXT.md`\n-- relevant ADRs\n+- relevant SADRs and target ADRs\n - parent Goal issue\n - PRD issue or document\n - task issue\ndiff --git a/docs/agents/decision-control.md b/docs/agents/decision-control.md\nindex 7d8ee5ab03a5ccbcbd04056ae61de03785a2a9c9..8c94ecda00fffaa0e92c325ab83aa9776bd1dac1\n--- a/docs/agents/decision-control.md\n+++ b/docs/agents/decision-control.md\n@@ -11,7 +11,7 @@\n | Decision | Owner | When | Required inputs | Output | Enforcement surface |\n | --- | --- | --- | --- | --- | --- |\n | Is the Goal clear enough to plan? | Planner + Guardian when needed | Goal intake | User request, existing docs, risk signals | Goal readiness verdict | Goal checklist, issue template, ledger |\n-| Must `grill-with-docs` run? | Planner / Skill Gate | Before PRD or issue split | Ambiguity, domain terms, ADR conflicts, unclear boundaries | Skill invocation or explicit skip rationale | Skill Gate checklist |\n+| Must `grill-with-docs` run? | Planner / Skill Gate | Before PRD or issue split | Ambiguity, domain terms, SADR/target-ADR conflicts, unclear boundaries | Skill invocation or explicit skip rationale | Skill Gate checklist |\n | Is a PRD required? | Planner | After Goal context stabilizes | Goal, `grill-with-docs` output, Context & Impact output | PRD required / not required | PRD checklist, CCA evidence check |\n | Can the spec be frozen? | Operator only | End of Requirements Definition, before issue split | PRD/requirements summary, scope inventory (SF-02), open-question list | `spec_freeze` block in the plan + ledger evidence | `require_grilled_plan` (plan ingest/run), SF checklist |\n | Can a frozen spec change? | Operator only (Spec Amendment) | When implementation contradicts the frozen spec | Contested decisions, scoped re-grill result | recorded amendment + re-stamped freeze | SF checklist, ledger evidence |\ndiff --git a/docs/agents/domain.md b/docs/agents/domain.md\nindex 258da8ea81e87ac0a6b0b5b5e5c2fde980eaabb7..d57b0cf17ff3e1fa9e6e5885372bf083d8295421\n--- a/docs/agents/domain.md\n+++ b/docs/agents/domain.md\n@@ -5,7 +5,9 @@\n ## Layout\n \n - `CONTEXT.md` defines Shiki language. Keep it implementation-free.\n-- `docs/adr/` records hard-to-reverse decisions.\n+- `docs/adr/SADR-NNNN-*.md` records Shiki platform decisions.\n+- `docs/adr/NNNN-*.md` records target product decisions without sharing the\n+  SADR namespace.\n - `AGENTS.md` is the runtime-neutral agent constitution.\n - `CLAUDE.md` is the Claude Code wrapper around `AGENTS.md`.\n - `.shiki/` is the Target Repository mirror for Goals, PRDs, plans, Task DAGs, contracts, locks, worktree records, ledger entries, CCA verdicts, repair packets, reports, and handoffs.\n@@ -15,6 +17,6 @@\n \n - Use Shiki terms exactly when they exist.\n - Add glossary terms to `CONTEXT.md` only when the term is part of the domain, not a generic programming concept.\n-- Create ADRs only for hard-to-reverse, surprising, tradeoff-based decisions.\n+- Create SADRs or target ADRs only for hard-to-reverse, surprising, tradeoff-based decisions.\n - Keep product-specific language in the Target Repository, not in Shiki Template docs.\n-- If an ADR conflicts with a proposed implementation, surface the conflict before editing.\n+- If a SADR or target ADR conflicts with a proposed implementation, surface the conflict before editing.\ndiff --git a/docs/agents/execution-control.md b/docs/agents/execution-control.md\nindex ad8ee3cdb36be56fedc1f46408ca92b387d74047..bc63c3a73661d12b567016ba299f1a6b66bf9088\n--- a/docs/agents/execution-control.md\n+++ b/docs/agents/execution-control.md\n@@ -63,7 +63,7 @@\n | Branch name | Orchestrator | Orchestrator | Before implementer dispatch | branch policy | branch ref in task/ledger |\n | Lock reservation | Context & Impact | MergeGate | Before dispatch and before merge | `.shiki/locks/`, labels, PR metadata | lock id, owner, scope |\n | CI profile | Planner | MergeGate | During issue triage, rechecked at PR | labels, `.shiki/policy.yaml`, workflow matrix | required check list |\n-| Required status checks | Repo maintainer / Guardian | GitHub branch protection / rulesets | Repository setup, updated via ADR | GitHub settings | branch/ruleset config, ADR |\n+| Required status checks | Repo maintainer / Guardian | GitHub branch protection / rulesets | Repository setup, updated via target ADR | GitHub settings | branch/ruleset config, target ADR |\n | CCA judgment | CCA | GitHub required check / MergeGate | Every non-draft PR update | GitHub Action check | structured CCA verdict |\n | Repair assignment | CCA | MergeGate | After failed CCA/CI/review | repair packet, issue/PR comment | repair id and attempt count |\n | Merge readiness | MergeGate | GitHub branch protection / Guardian if needed | After CCA complete | required checks, review rules, rulesets | mergegate check output |\ndiff --git a/docs/agents/guardian-policy.md b/docs/agents/guardian-policy.md\nindex 4147c7adf05a47adb4fd24ada1a1c2e1f2ba6d2d..a20d9cc6c00a569bb057b9ca8d6b51c7fc4a9847\n--- a/docs/agents/guardian-policy.md\n+++ b/docs/agents/guardian-policy.md\n@@ -98,8 +98,8 @@\n `origin/<default_branch>` and reuses each job's existing base fetch; a shallow\n (`--depth`) or second base fetch would make the proof unresolvable and silently\n disable the carry, so neither is permitted. CCA reads `guardian_comment_carried` as\n-a recorded authority in the deterministic signal and does not re-derive it. See ADR\n-0018 for the full decision, the rejected diff/patch-equality and path-disjointness\n+a recorded authority in the deterministic signal and does not re-derive it. See\n+SADR-0018 for the full decision, the rejected diff/patch-equality and path-disjointness\n alternatives, and the accepted residual.\n \n ## External AI Guardian Review (SADR-0010)\ndiff --git a/docs/agents/implementation-policy.md b/docs/agents/implementation-policy.md\nindex c754144a9fb5bf4a38aa82a60e1670c59e626b44..85c37281bcad196c6b7b02343faf00b03cd9c57a\n--- a/docs/agents/implementation-policy.md\n+++ b/docs/agents/implementation-policy.md\n@@ -31,10 +31,13 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n ## Non-Negotiables\n \n 1. **Planning precedes implementation.** Non-trivial Goals start with `grill-with-docs` and Context & Impact.\n-2. **Domain language is controlled.** Terms must be checked against `CONTEXT.md`; hard-to-reverse tradeoffs must be checked against ADRs.\n+2. **Domain language is controlled.** Terms must be checked against `CONTEXT.md`; hard-to-reverse tradeoffs must be checked against Shiki SADRs and target ADRs.\n 3. **PRD precedes issue decomposition.** `to-prd` is used once the context is settled enough to become durable intent.\n 4. **Issues are vertical slices.** `to-issues` must produce independently grabbable tracer-bullet tasks, not horizontal layer tickets.\n 5. **Triage controls dispatch.** Only `ready-for-agent` / AFK tasks may be assigned to Codex without human input.\n@@ -95,19 +98,19 @@\n Process:\n \n - Challenge terms against `CONTEXT.md`.\n-- Check ADRs before reopening decisions.\n+- Check SADRs and target ADRs before reopening decisions.\n - Inspect code when code can answer a question.\n - Ask one question at a time when the operator is available.\n - Provide a recommended answer with each question.\n - Record resolved terms in `CONTEXT.md`.\n-- Create ADRs only for hard-to-reverse, surprising, tradeoff-based decisions.\n+- Create SADRs or target ADRs only for hard-to-reverse, surprising, tradeoff-based decisions.\n - Convert unresolved load-bearing questions into HITL blockers.\n \n Outputs:\n \n - Resolved design-tree decisions.\n - Updated glossary terms when needed.\n-- ADRs when justified.\n+- SADRs or target ADRs when justified.\n - HITL blockers when decisions remain unresolved.\n \n ## Phase 3 \u2014 Context & Impact\n@@ -117,7 +120,7 @@\n \n Outputs must include:\n \n-- Relevant documents and ADRs.\n+- Relevant documents, SADRs, and target ADRs.\n - Relevant modules, interfaces, seams, callers, and tests.\n - Candidate locks.\n - Dependencies.\n@@ -140,7 +143,7 @@\n - Testing decisions.\n - Out of scope.\n - Further notes.\n-- Links to relevant Goal, domain terms, and ADRs.\n+- Links to relevant Goal, domain terms, SADRs, and target ADRs.\n \n Phase 4 ends with Spec Freeze: the operator explicitly approves the PRD, the\n plan records a `spec_freeze` block (status, approved_by, source), and the SF\n@@ -240,7 +243,7 @@\n - Dependencies and locks.\n - Risk and Guardian requirements.\n - Ledger completeness.\n-- Documentation and ADR updates.\n+- Documentation and SADR/target-ADR updates.\n - Scope drift.\n \n CCA emits one of:\n@@ -301,7 +304,7 @@\n | Evidence failure | Behavior might be done, but proof is missing. | Add checks, PR evidence, ledger entry, or artifacts. |\n | Scope drift | PR changes unrelated behavior. | Revert unrelated changes or split into new Goal. |\n | Dependency block | Upstream task or lock not complete. | Block until dependency resolves. |\n-| Architecture block | Decision needs ADR or deep-module discussion. | `grill-with-docs` or `improve-codebase-architecture`. |\n+| Architecture block | Decision needs a SADR, target ADR, or deep-module discussion. | `grill-with-docs` or `improve-codebase-architecture`. |\n | Risk block | Security/production/policy change needs human approval. | Guardian decision. |\n \n ## Phase 5.5 \u2014 Execution Decision Control\ndiff --git a/docs/agents/runtime-auth-model.md b/docs/agents/runtime-auth-model.md\nindex 6a2e1a47e81ea89b5c68a210ec07772a9374babc..3c54748d24233dbd0ba8473950282547ef4ce935\n--- a/docs/agents/runtime-auth-model.md\n+++ b/docs/agents/runtime-auth-model.md\n@@ -148,7 +148,7 @@\n \n API-key based runners may be added by a target repository as an explicit extension. They are not the default Shiki template path.\n \n-If a repo opts into API-key mode, record the exception in an ADR and update the repo's `AGENTS.md`, `.shiki/config.yaml`, and workflow permissions.\n+If a repo opts into API-key mode, record the target-specific exception in a target ADR and update the repo's `AGENTS.md`, `.shiki/config.yaml`, and workflow permissions.\n \n ## GitHub Actions Runtime Compatibility\n \ndiff --git a/docs/agents/skill-gate.md b/docs/agents/skill-gate.md\nindex c50398b0d7c60a8598f2a5c88a10015730dced13..3acffe6444617bb1d54b77d4b4708158ec735bc6\n--- a/docs/agents/skill-gate.md\n+++ b/docs/agents/skill-gate.md\n@@ -9,7 +9,7 @@\n | Skill | Trigger | Expected output |\n | --- | --- | --- |\n | `setup-matt-pocock-skills` | First configuring a repo for these skills, or when issue tracker, triage labels, or domain docs are missing. | `docs/agents/` configuration and an `Agent skills` block in `AGENTS.md` or `CLAUDE.md`. |\n-| `grill-with-docs` | Ambiguous plan, unclear terminology, boundary decision, tradeoff, or ADR-worthy design choice. | Clarified decision tree, recommended answers, updates to `CONTEXT.md` or ADRs when decisions crystallize. |\n+| `grill-with-docs` | Ambiguous plan, unclear terminology, boundary decision, tradeoff, or decision-record-worthy design choice. | Clarified decision tree, recommended answers, updates to `CONTEXT.md`, SADRs, or target ADRs when decisions crystallize. |\n | `zoom-out` | Agent lacks a higher-level architectural map. | Map of relevant modules, callers, seams, and domain concepts. |\n | `to-prd` | Settled context should become a PRD. | PRD published to the issue tracker with problem, solution, user stories, decisions, testing, and out-of-scope notes. |\n | `to-issues` | Goal, PRD, or plan must become implementation tickets. | Vertical-slice issues with acceptance criteria, dependencies, and AFK/HITL classification. |\n@@ -17,7 +17,7 @@\n | `tdd` | Feature work or bug fix has observable behavior that can be specified with tests. | Red-green-refactor loop through public interfaces, one vertical slice at a time. |\n | `code-review` | The TDD loop is green and the implementer is about to create or update a PR. Default for every implementation task. | Pre-PR self-review of the task diff with fixes applied on the task branch; findings and resolutions recorded as a ledger entry naming the skill and a `## Pre-PR code review` PR body section. |\n | `diagnose` | Hard bug, failing check, regression, flaky behavior, or performance problem. | Reproduction loop, ranked hypotheses, instrumentation, fix, regression test, post-mortem. |\n-| `improve-codebase-architecture` | Structural friction, testability, AI-navigability, deep-module opportunity, or refactor request. | Architecture candidates and deepening recommendations informed by `CONTEXT.md` and ADRs. |\n+| `improve-codebase-architecture` | Structural friction, testability, AI-navigability, deep-module opportunity, or refactor request. | Architecture candidates and deepening recommendations informed by `CONTEXT.md`, SADRs, and target ADRs. |\n | `prototype` | A throwaway logic or UI prototype is needed to answer a design question. | Clearly marked disposable prototype and captured decision. |\n \n ## MergeGate Interaction\ndiff --git a/docs/operations.md b/docs/operations.md\nindex cf1f452d4c589e38ccd58553bda0ad8684b51eb7..ace48a90ffbec9e5c0a7113bef16a4858bc44b3f\n--- a/docs/operations.md\n+++ b/docs/operations.md\n@@ -89,6 +89,16 @@\n      target history, brought forward by `shiki migrate`, not a template.\n    - **Everything else** (CLI scripts, workflows, schemas, docs) is overwritten.\n \n+   A forced upgrade also migrates the former numeric Shiki decision paths to\n+   `SADR-NNNN-*`. Before the first write, the installer inventories the exact\n+   legacy Shiki paths and authorizes each deletion only when the existing\n+   `.shiki/install-stamp.json` names that path and its SHA-256 digest still\n+   matches the file. An absent, unreadable, malformed, incomplete, or\n+   mismatching stamp reports every blocked path and stops with no filesystem\n+   change. Target-owned `NNNN-*` ADRs are not cleanup candidates. A non-force\n+   install deletes nothing and retains any still-matching legacy stamp entries\n+   so a later forced upgrade can prove ownership.\n+\n    If the target has pending migrations, `--force` refuses and writes nothing \u2014\n    apply migrations first (step 3), then re-run. The run ends with a single\n    summary of every `.new` written, so a half-upgrade is visible.\ndiff --git a/docs/releasing.md b/docs/releasing.md\nindex 10d73828e7dbd64e9d9f6b83bc84d010537ce62a..aafeee8c4d441e1e427efee2ce4f81a647f020e1\n--- a/docs/releasing.md\n+++ b/docs/releasing.md\n@@ -67,7 +67,7 @@\n CLI in `scripts/`. There are no third-party Python runtime dependencies. The CLI\n is intentionally **not** exposed as a `[project.scripts]` console entry point\n because `scripts/shiki.py` is a dependency-free shim and `scripts/` is not\n-packaged as an importable distribution (see the ADR for the rationale).\n+packaged as an importable distribution (see SADR-0007 for the rationale).\n \n Supported install paths:\n \n@@ -155,6 +155,15 @@\n    [docs/operations.md](operations.md#the-new-convention). If the target has\n    pending migrations, `--force` refuses and writes nothing; apply migrations\n    (step 2) first, then re-run.\n+\n+   The SADR namespace upgrade has an additional pre-write gate: each former\n+   numeric Shiki decision path is removed only when the existing install stamp\n+   contains that exact path and a digest matching its current bytes. The\n+   installer aggregates absent/unreadable/malformed-stamp, missing-digest, and\n+   digest-mismatch blockers before raising, so refusal leaves the target tree\n+   byte-identical. Numeric target ADRs are preserved. A successful forced\n+   upgrade installs and stamps `SADR-*` paths and drops the removed legacy paths\n+   from the refreshed stamp.\n 2. Apply repository-local `.shiki/` state migrations with the migrate command:\n \n    ```bash\ndiff --git a/docs/usage.md b/docs/usage.md\nindex b83407ee3970dd395dd893010bc5dbe8bad26051..a130ef6e40dfd8169a5bc701339351097df1a09f\n--- a/docs/usage.md\n+++ b/docs/usage.md\n@@ -67,8 +67,10 @@\n shiki install-target /path/to/target-repo --local-only\n ```\n \n-Use `--force` only when you intentionally want to overwrite existing target\n-files.\n+Use `--force` only when you intentionally want to refresh the target template.\n+For the SADR namespace migration, `--force` deletes only exact legacy Shiki\n+decision paths whose existing install-stamp digest still matches; any ownership\n+blocker stops before the first write. Numeric target ADRs are never candidates.\n \n ## `shiki bootstrap-platform` \u2014 publish the Shiki platform repo\n \ndiff --git a/scripts/guardian_approval_signal.py b/scripts/guardian_approval_signal.py\nindex d059f92b2c82aa2b5d6ca421365537a87886c1cb..16924892714f4f5b9303c0ef0e325a7e70075630\n--- a/scripts/guardian_approval_signal.py\n+++ b/scripts/guardian_approval_signal.py\n@@ -199,8 +199,8 @@\n ) -> Any:\n     \"\"\"Evaluate SADR-0015 Contract Approval for the CCA Guardian signal.\n \n-    The SAME OR that MergeGate applies must reach this deterministic signal: ADR\n-    0015 is explicit that without it, a contract-approved PR still returns\n+    The SAME OR that MergeGate applies must reach this deterministic signal:\n+    SADR-0015 is explicit that without it, a contract-approved PR still returns\n     ``needs_guardian``, the CCA job fails, and the MergeGate policy check never\n     runs \u2014 the gate this change moves would never be reached. This resolves the\n     task the SAME way MergeGate does (the first Shiki task id in the PR body),\ndiff --git a/scripts/shiki_contract_approval.py b/scripts/shiki_contract_approval.py\nindex 941bcb96fdd8b9a85bc0c4f0f04d5dad9d04839c..4a4b418f6527413161db82670bec857a043360e2\n--- a/scripts/shiki_contract_approval.py\n+++ b/scripts/shiki_contract_approval.py\n@@ -142,7 +142,7 @@\n     changed_files_status: Any,\n     registration: Any,\n ) -> ContractApprovalResult:\n-    \"\"\"Whether this PR carries an SADR-0015 Contract Approval.\n+    \"\"\"Whether this PR carries a SADR-0015 Contract Approval.\n \n     The four conditions are evaluated IN ORDER, each failure recorded as a\n     distinct reason string:\ndiff --git a/scripts/shiki_guardian.py b/scripts/shiki_guardian.py\nindex a4ecb79bbda55fd5c8b06adfae2eb99760a03c45..4dd99bbe714e26f421fa19abac7a34ae1b195ecc\n--- a/scripts/shiki_guardian.py\n+++ b/scripts/shiki_guardian.py\n@@ -12,7 +12,7 @@\n # The carry re-derives its proof from git in-process on every judging invocation;\n # it is never read from an artifact, so there is nothing to go stale or be forged.\n # shiki_sync_proof is the ONLY guardian-family module that runs git, and every one\n-# of its invocations is read-only and local (ADR: prove-from-git base sync).\n+# of its invocations is read-only and local (SADR: prove-from-git base sync).\n from shiki_sync_proof import verify_pure_base_sync\n \n GUARDIAN_POLICY_PATH = \".shiki/guardian-policy.json\"\ndiff --git a/scripts/shiki_guardian_review.py b/scripts/shiki_guardian_review.py\nindex dd0f104d457899e0ba5ff1e162011c1e90b6f92b..e10ef58c046324e7ea3a3392feb3048ca77414ce\n--- a/scripts/shiki_guardian_review.py\n+++ b/scripts/shiki_guardian_review.py\n@@ -159,7 +159,7 @@\n     docs_only = _is_docs_only(changed_files)\n     if docs_only and matched == 0:\n         focus.append(\n-            \"Docs/ADR-only change: confirm the change is genuinely documentation/decision-only, introduces no behavior or contract drift, and stays consistent with the constitution and existing ADRs.\"\n+            \"Docs/decision-record-only change: confirm the change is genuinely documentation/decision-only, introduces no behavior or contract drift, and stays consistent with the constitution and existing SADRs and target ADRs.\"\n         )\n \n     # Conservative augmentation: unknown (no category and not docs-only), mixed\ndiff --git a/scripts/shiki_installer.py b/scripts/shiki_installer.py\nindex be064ab7a29ff6836f7f7a1d70500a43e0d11d7b..9fd8eefd137ce7003dad5c0dcb2e48e2cd47ab32\n--- a/scripts/shiki_installer.py\n+++ b/scripts/shiki_installer.py\n@@ -118,6 +118,7 @@\n # what lets a target carry it without a governance-schema change.\n INSTALL_STAMP_PATH = \".shiki/install-stamp.json\"\n INSTALL_STAMP_VERSION = 1\n+LEGACY_SHIKI_SADR_MAX = 18\n \n # Under --force the shipped surface splits three ways, so nothing is lost and\n # nothing is silently stale.\n@@ -237,9 +238,98 @@\n         )\n \n \n+def legacy_shiki_adr_paths() -> tuple[str, ...]:\n+    \"\"\"Former numeric paths for Shiki SADR-0001 through SADR-0018.\n+\n+    The authoritative slugs come from the current SADR filenames. SADR-0019 is\n+    the namespace decision itself and never shipped under a numeric legacy path.\n+    \"\"\"\n+    paths: list[str] = []\n+    for sadr in sorted((ROOT / \"docs\" / \"adr\").glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\")):\n+        number = sadr.name.split(\"-\", 2)[1]\n+        if int(number) <= LEGACY_SHIKI_SADR_MAX:\n+            paths.append(f\"docs/adr/{sadr.name.removeprefix('SADR-')}\")\n+    return tuple(paths)\n+\n+\n+def preflight_legacy_sadr_cleanup(target: Path, *, force: bool) -> tuple[str, ...]:\n+    \"\"\"Authorize exact legacy Shiki ADR removals before the first write.\n+\n+    A non-force install never removes existing files. A forced install needs no\n+    stamp when no legacy Shiki path is present. When legacy paths do exist, each\n+    must be named in a readable install stamp and its current bytes must match\n+    the recorded digest.\n+    \"\"\"\n+    if not force:\n+        return ()\n+    present = tuple(\n+        relative\n+        for relative in legacy_shiki_adr_paths()\n+        if (target / relative).exists() or (target / relative).is_symlink()\n+    )\n+    if not present:\n+        return ()\n+\n+    stamp_path = target / INSTALL_STAMP_PATH\n+    try:\n+        raw = stamp_path.read_text(encoding=\"utf-8\")\n+        stamp = json.loads(raw)\n+    except FileNotFoundError:\n+        detail = \"install stamp is absent\"\n+        blockers = [f\"{relative}: {detail}\" for relative in present]\n+    except (OSError, UnicodeDecodeError) as error:\n+        detail = f\"install stamp is unreadable ({error})\"\n+        blockers = [f\"{relative}: {detail}\" for relative in present]\n+    except json.JSONDecodeError as error:\n+        detail = f\"install stamp is malformed ({error})\"\n+        blockers = [f\"{relative}: {detail}\" for relative in present]\n+    else:\n+        if not isinstance(stamp, dict):\n+            blockers = [\n+                f\"{relative}: install stamp is malformed (expected an object)\"\n+                for relative in present\n+            ]\n+        else:\n+            digests = stamp.get(\"digests\")\n+            blockers = []\n+            if not isinstance(digests, dict):\n+                blockers.extend(\n+                    f\"{relative}: install stamp has no digest for this path\"\n+                    for relative in present\n+                )\n+            else:\n+                for relative in present:\n+                    expected = digests.get(relative)\n+                    if not isinstance(expected, str) or not expected:\n+                        blockers.append(\n+                            f\"{relative}: install stamp has no digest for this path\"\n+                        )\n+                        continue\n+                    try:\n+                        actual = _sha256_file(target / relative)\n+                    except OSError as error:\n+                        blockers.append(\n+                            f\"{relative}: legacy path is unreadable ({error})\"\n+                        )\n+                        continue\n+                    if actual != expected:\n+                        blockers.append(\n+                            f\"{relative}: digest mismatch (stamp {expected}, current {actual})\"\n+                        )\n+\n+    if blockers:\n+        joined = \"\\n\".join(f\"- {blocker}\" for blocker in blockers)\n+        raise ShikiError(\n+            \"legacy Shiki ADR cleanup is not authorized:\\n\"\n+            f\"{joined}\\nNo files were written.\"\n+        )\n+    return present\n+\n+\n def install_template(target: Path, *, force: bool, validate: bool) -> list[NewFileNote]:\n     # Preconditions first: never rewrite the tree of a target that cannot be\n     # upgraded cleanly (see refuse_pending_migrations).\n+    legacy_cleanup = preflight_legacy_sadr_cleanup(target, force=force)\n     refuse_pending_migrations(target)\n \n     # Resolve the target's required CODEOWNERS owner once from its\n@@ -250,6 +340,10 @@\n     # verbatim. See ``codeowners_required_owner``.\n     codeowners_owner = codeowners_required_owner(target)\n \n+    for relative in legacy_cleanup:\n+        (target / relative).unlink()\n+        info(f\"removed stamped legacy Shiki ADR: {target / relative}\")\n+\n     notes: list[NewFileNote] = []\n     for relative in TEMPLATE_PATHS:\n         source = ROOT / relative\n@@ -647,6 +741,22 @@\n     digests = target_stamp_digests(target)\n     existing = read_install_stamp(target)\n     if existing is not None:\n+        # A non-force install deliberately leaves legacy Shiki ADRs in place.\n+        # Preserve only still-matching ownership proof so a later forced\n+        # upgrade can authorize cleanup; never carry a missing or stale digest.\n+        existing_digests = existing.get(\"digests\")\n+        if isinstance(existing_digests, dict):\n+            for relative in legacy_shiki_adr_paths():\n+                expected = existing_digests.get(relative)\n+                path = target / relative\n+                if not isinstance(expected, str) or not path.is_file():\n+                    continue\n+                try:\n+                    actual = _sha256_file(path)\n+                except OSError:\n+                    continue\n+                if actual == expected:\n+                    digests[relative] = actual\n         if _incoming_is_older(commit, existing.get(\"platform_commit\")):\n             warn(\n                 \"kept existing install stamp: incoming install \"\ndiff --git a/scripts/shiki_loop.py b/scripts/shiki_loop.py\nindex 3611641da8aeb5038b1da90e3114b09a473ec6d8..84caa491b9cad0edc4176f209c13e9221e975681\n--- a/scripts/shiki_loop.py\n+++ b/scripts/shiki_loop.py\n@@ -424,7 +424,7 @@\n         # unresolvable CCA verdict has already produced its terminal stop and is never\n         # laundered into a head-moving sync (SADR-0010). It also never fires while the\n         # policy (Guardian) gate is red \u2014 for ANY reason, not only when it is the sole\n-        # red check \u2014 so a head-bound Guardian approval, or an SADR-0015\n+        # red check \u2014 so a head-bound Guardian approval, or a SADR-0015\n         # contract-approval carry that base movement defeated (the policy gate compares\n         # base and head task governance directly), is never silently restored by\n         # adopting base wholesale. It is bounded by a durable per-task counter so the\ndiff --git a/scripts/shiki_tasks.py b/scripts/shiki_tasks.py\nindex aec3809f24b4a0c8c96fc50971cff9ae9ad30381..d531052e9d9717058d4631483cefab44c26a65c1\n--- a/scripts/shiki_tasks.py\n+++ b/scripts/shiki_tasks.py\n@@ -821,7 +821,7 @@\n         \"prompt\": prompt,\n         \"entry_skill\": \"grill-with-docs\",\n         \"required_next_steps\": [\n-            \"Run grill-with-docs until terminology, boundaries, risks, and ADR-worthy decisions are settled.\",\n+            \"Run grill-with-docs until terminology, boundaries, risks, and decision-record-worthy choices are settled.\",\n             \"Obtain the operator's explicit approval of the PRD/requirements (Spec Freeze).\",\n             \"Write a machine-readable plan JSON with grill_with_docs.status=complete and spec_freeze.status=frozen.\",\n             \"Run shiki plan ingest --plan-file PLAN.json.\",\ndiff --git a/scripts/test_shiki_goal_loop.sh b/scripts/test_shiki_goal_loop.sh\nindex 753c7b516e10f0be3bc92a4bd0eb1fa699776ae6..8f3820a0a079b3044424cfe1a6c6b7fe68896e67\n--- a/scripts/test_shiki_goal_loop.sh\n+++ b/scripts/test_shiki_goal_loop.sh\n@@ -231,8 +231,8 @@\n \n # High/critical risk merges autonomously when all required checks are green:\n # the \"MergeGate policy check\" required check IS the Guardian gate (it enforces\n-# guardian-policy.json \u2014 human approval OR external AI guardian review, ADR\n-# 0010), so green checks mean a recorded authority approved. The fake gh returns\n+# guardian-policy.json \u2014 human approval OR external AI guardian review,\n+# SADR-0010), so green checks mean a recorded authority approved. The fake gh returns\n # green checks, simulating that the Guardian gate passed.\n python3 \"$ROOT/scripts/shiki.py\" goal create --target \"$TARGET\" --title \"Guardian gate\" --outcome \"High risk merges via policy gate\" >/tmp/shiki-goal-loop-guardian-goal.json\n GGOAL=\"$(json_get /tmp/shiki-goal-loop-guardian-goal.json goal_id)\"\ndiff --git a/tests/test_adr_references.py b/tests/test_adr_references.py\nindex 51706329c797d5dd9b47e082a333fa83b261d482..d005da4f771f5e8085ec22d4b787980e68f5ae8c\n--- a/tests/test_adr_references.py\n+++ b/tests/test_adr_references.py\n@@ -1,8 +1,8 @@\n-\"\"\"Every ``ADR ####`` citation must resolve to exactly one decision record.\n+\"\"\"Every target ADR and Shiki SADR citation resolves in its own namespace.\n \n Background. ``scripts/mergegate_check.py`` justifies ``is_bookkeeping_closeout``\n-with \"ADR 0017\" nine times and ``scripts/guardian_approval_signal.py`` cites\n-0016/0017 seven times, yet for a stretch the numbering in ``docs/adr/`` stopped\n+with \"SADR-0017\" nine times and ``scripts/guardian_approval_signal.py`` cites\n+SADR-0016/SADR-0017 seven times, yet for a stretch the numbering in ``docs/adr/`` stopped\n at 0015: the authoring commits lived on a coordinator branch and no task ever\n declared ``docs/adr/...`` in its locks, so nothing carried the records to the\n default branch. The stated basis of an exemption that merged seven PRs with zero\n@@ -11,11 +11,12 @@\n and the directory drifted with no signal.\n \n This is the same unbound-pair shape behind other silent failures: a reference\n-and its target with nothing asserting they agree. This test binds them. It scans\n-every ``scripts/*.py`` (plus ``docs/**/*.md`` and the root ``*.md`` governance\n-files, which are cheap) for the ``ADR ####`` pattern and asserts each cited\n-number resolves to exactly one ``docs/adr/####-*.md``. The converse is\n-deliberately NOT required: an ADR may exist without being cited.\n+and its target with nothing asserting they agree. This test binds them. Target\n+``ADR NNNN`` citations resolve only to ``docs/adr/NNNN-*.md``; Shiki\n+``SADR-NNNN`` citations resolve only to ``docs/adr/SADR-NNNN-*.md``. The scan\n+covers current governance docs, scripts, workflows, prompts, packaging metadata,\n+and repo-local Codex guidance while deliberately excluding historical\n+``.shiki`` evidence. The converse is not required: a record may be uncited.\n \n The suite is dependency free: standard-library ``unittest`` only.\n \"\"\"\n@@ -31,17 +32,18 @@\n \n REPO_ROOT = Path(__file__).resolve().parents[1]\n \n-# \"ADR 0016\" -- a four-digit citation. The trailing \\b keeps a five-digit token\n-# from being read as a truncated four-digit number.\n-_CITATION = re.compile(r\"\\bADR (\\d{4})\\b\")\n-# \"0016-locks-bound-product-paths.md\" -- the leading four digits name the record.\n-_ADR_FILENAME = re.compile(r\"^(\\d{4})-.*\\.md$\")\n+# ``ADR 0016`` / ``SADR-0016`` -- a namespace plus four-digit citation. The\n+# trailing ``\\b`` keeps a five-digit token from being read as a truncated one.\n+_CITATION = re.compile(r\"\\b(S?ADR)[ -](\\d{4})\\b\")\n+# Target ADRs retain the numeric filename; Shiki platform decisions use SADR.\n+_DECISION_FILENAME = re.compile(r\"^(?:(SADR)-)?(\\d{4})-.*\\.md$\")\n \n \n class Violation(NamedTuple):\n     \"\"\"A citation that does not resolve to exactly one decision record.\"\"\"\n \n     citing_file: str  # repo-relative path of the file that cites the ADR\n+    namespace: str  # \"ADR\" for target decisions; \"SADR\" for Shiki decisions\n     number: str  # the cited four-digit number, e.g. \"0016\"\n     line: int  # 1-based line of the citation\n     reason: str  # \"missing\" (zero matches) or \"ambiguous\" (two or more)\n@@ -49,44 +51,59 @@\n \n     @property\n     def expected_path(self) -> str:\n-        return f\"docs/adr/{self.number}-*.md\"\n+        prefix = \"SADR-\" if self.namespace == \"SADR\" else \"\"\n+        return f\"docs/adr/{prefix}{self.number}-*.md\"\n \n     def describe(self) -> str:\n         \"\"\"Name the citing file, the cited number, and the missing path.\"\"\"\n         if self.reason == \"missing\":\n             return (\n-                f\"{self.citing_file}:{self.line} cites ADR {self.number} \"\n+                f\"{self.citing_file}:{self.line} cites {self.namespace} {self.number} \"\n                 f\"but no document matches {self.expected_path}\"\n             )\n         joined = \", \".join(self.matches)\n         return (\n-            f\"{self.citing_file}:{self.line} cites ADR {self.number} which \"\n+            f\"{self.citing_file}:{self.line} cites {self.namespace} {self.number} which \"\n             f\"resolves to {len(self.matches)} documents (expected exactly one \"\n             f\"at {self.expected_path}): {joined}\"\n         )\n \n \n-def _adr_index(root: Path) -> dict[str, list[Path]]:\n-    \"\"\"Map every four-digit ADR number to the record files that claim it.\"\"\"\n-    index: dict[str, list[Path]] = {}\n+def _adr_index(root: Path) -> dict[tuple[str, str], list[Path]]:\n+    \"\"\"Map each decision namespace and number to the files that claim it.\"\"\"\n+    index: dict[tuple[str, str], list[Path]] = {}\n     adr_dir = root / \"docs\" / \"adr\"\n     if adr_dir.is_dir():\n         for path in sorted(adr_dir.glob(\"*.md\")):\n-            match = _ADR_FILENAME.match(path.name)\n+            match = _DECISION_FILENAME.match(path.name)\n             if match:\n-                index.setdefault(match.group(1), []).append(path)\n+                namespace = \"SADR\" if match.group(1) else \"ADR\"\n+                index.setdefault((namespace, match.group(2)), []).append(path)\n     return index\n \n \n def _citing_files(root: Path, include_docs: bool = True) -> list[Path]:\n-    \"\"\"Files scanned for citations: scripts/*.py, and cheaply the markdown.\"\"\"\n+    \"\"\"Current, shipped text surfaces scanned for decision citations.\"\"\"\n     files = sorted((root / \"scripts\").glob(\"*.py\"))\n+    files += sorted((root / \"scripts\").glob(\"*.sh\"))\n     if include_docs:\n         docs = root / \"docs\"\n         if docs.is_dir():\n             files += sorted(docs.rglob(\"*.md\"))\n         files += sorted(root.glob(\"*.md\"))\n-    return files\n+        pyproject = root / \"pyproject.toml\"\n+        if pyproject.is_file():\n+            files.append(pyproject)\n+        for relative in (\".github\", \".codex\"):\n+            surface = root / relative\n+            if surface.is_dir():\n+                files += sorted(\n+                    path\n+                    for path in surface.rglob(\"*\")\n+                    if path.is_file()\n+                    and path.suffix in {\".md\", \".py\", \".sh\", \".toml\", \".yaml\", \".yml\"}\n+                )\n+    return sorted(set(files))\n \n \n def find_adr_reference_violations(\n@@ -94,10 +111,9 @@\n ) -> list[Violation]:\n     \"\"\"Return every ``ADR ####`` citation under ``root`` that fails to resolve.\n \n-    A citation resolves when exactly one ``docs/adr/####-*.md`` matches its\n-    number. Zero matches is a \"missing\" violation; two or more is \"ambiguous\".\n-    Records that are never cited produce nothing -- the converse is not\n-    required.\n+    A citation resolves when exactly one record in the same namespace matches\n+    its number. Zero matches is a \"missing\" violation; two or more is\n+    \"ambiguous\". Records that are never cited produce nothing.\n     \"\"\"\n     root = Path(root)\n     index = _adr_index(root)\n@@ -110,13 +126,15 @@\n         rel = path.relative_to(root).as_posix()\n         for lineno, line in enumerate(text.splitlines(), start=1):\n             for match in _CITATION.finditer(line):\n-                number = match.group(1)\n-                found = index.get(number, [])\n+                namespace, number = match.groups()\n+                found = index.get((namespace, number), [])\n                 if len(found) == 1:\n                     continue\n                 reason = \"missing\" if not found else \"ambiguous\"\n                 rels = [p.relative_to(root).as_posix() for p in found]\n-                violations.append(Violation(rel, number, lineno, reason, rels))\n+                violations.append(\n+                    Violation(rel, namespace, number, lineno, reason, rels)\n+                )\n     return violations\n \n \n@@ -129,8 +147,8 @@\n         self.assertEqual(\n             violations,\n             [],\n-            \"Every 'ADR ####' citation must resolve to exactly one \"\n-            f\"docs/adr/####-*.md:\\n{detail}\",\n+            \"Every ADR/SADR citation must resolve exactly once in its own \"\n+            f\"docs/adr namespace:\\n{detail}\",\n         )\n \n     def test_scan_actually_reads_scripts(self) -> None:\n@@ -142,12 +160,38 @@\n             \"the citation scan must cover scripts/*.py\",\n         )\n \n+    def test_platform_citations_do_not_use_unresolved_numeric_shorthand(self) -> None:\n+        shorthand = re.compile(r\"SADR-\\d{4}/\\d{4}\")\n+        offenders = []\n+        for path in _citing_files(REPO_ROOT):\n+            text = path.read_text(encoding=\"utf-8\")\n+            if shorthand.search(text):\n+                offenders.append(path.relative_to(REPO_ROOT).as_posix())\n+        self.assertEqual(\n+            offenders,\n+            [],\n+            \"repeat the SADR prefix so every cited number is independently resolvable\",\n+        )\n+\n     def test_carried_records_are_indexed(self) -> None:\n         # The two records this Goal carried onto the default branch, and which\n         # the scripts cite most, must be present.\n         index = _adr_index(REPO_ROOT)\n-        self.assertIn(\"0016\", index)\n-        self.assertIn(\"0017\", index)\n+        self.assertIn((\"SADR\", \"0016\"), index)\n+        self.assertIn((\"SADR\", \"0017\"), index)\n+\n+    def test_platform_tree_has_only_sadr_0001_through_0019(self) -> None:\n+        adr_dir = REPO_ROOT / \"docs\" / \"adr\"\n+        sadrs = sorted(adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\"))\n+        self.assertEqual(\n+            {path.name.split(\"-\", 2)[1] for path in sadrs},\n+            {f\"{number:04d}\" for number in range(1, 20)},\n+        )\n+        self.assertEqual(\n+            list(adr_dir.glob(\"[0-9][0-9][0-9][0-9]-*.md\")),\n+            [],\n+            \"the Shiki platform tree must not retain legacy numeric SADR paths\",\n+        )\n \n \n class FixtureAdrReferences(unittest.TestCase):\n@@ -180,6 +224,26 @@\n         self.assertIn(\"9991\", message)  # cited number\n         self.assertIn(\"docs/adr/9991-*.md\", message)  # missing path\n \n+    def test_target_adr_does_not_satisfy_missing_platform_sadr(self) -> None:\n+        root = self._make_repo(\n+            adr_names=[\"0042-target-decision.md\"],\n+            script_body='\"\"\"Target ADR 0042 exists, but platform SADR-0042 does not.\"\"\"\\n',\n+        )\n+        violations = find_adr_reference_violations(root, include_docs=False)\n+        self.assertEqual(len(violations), 1)\n+        self.assertEqual(violations[0].namespace, \"SADR\")\n+        self.assertEqual(violations[0].number, \"0042\")\n+        self.assertEqual(violations[0].expected_path, \"docs/adr/SADR-0042-*.md\")\n+\n+    def test_target_adr_and_platform_sadr_may_reuse_a_number(self) -> None:\n+        root = self._make_repo(\n+            adr_names=[\"0042-target-decision.md\", \"SADR-0042-platform-decision.md\"],\n+            script_body='\"\"\"Target ADR 0042 and platform SADR-0042 are distinct.\"\"\"\\n',\n+        )\n+        self.assertEqual(\n+            find_adr_reference_violations(root, include_docs=False), []\n+        )\n+\n     def test_uncited_adr_is_allowed(self) -> None:\n         # An ADR exists but nothing cites it -> not a violation. The converse\n         # (every ADR must be cited) is explicitly NOT required.\ndiff --git a/tests/test_guardian_review.py b/tests/test_guardian_review.py\nindex 88829e7810212635e2fc98aa5918d6f26294677d..00a85a237acd5bf2d6cb5b294128198c422402ca\n--- a/tests/test_guardian_review.py\n+++ b/tests/test_guardian_review.py\n@@ -162,7 +162,7 @@\n \n     def test_docs_only_is_light_and_not_conservative(self):\n         focus = gr.classify_review_focus([\"docs/adr/0099-x.md\", \"README.md\"], \"\", \"low\")\n-        self.assertTrue(any(\"Docs/ADR-only\" in f for f in focus))\n+        self.assertTrue(any(\"Docs/decision-record-only\" in f for f in focus))\n         self.assertFalse(any(\"conservative default\" in f.lower() for f in focus))\n \n     def test_unknown_category_is_conservative(self):\ndiff --git a/tests/test_policy_job_merged_prs.py b/tests/test_policy_job_merged_prs.py\nindex d45512e74681e5e649279236c7d4e79d5c3da471..b97989f8bd74d091de1a6b5275a20d8dc68c4512\n--- a/tests/test_policy_job_merged_prs.py\n+++ b/tests/test_policy_job_merged_prs.py\n@@ -3,8 +3,8 @@\n \n ``.github/workflows/shiki-cca-completion.yml`` has two consumers of the merged-PR\n proof: the CCA *signal* (``guardian_approval_signal.py`` in \"Prepare CCA context\")\n-and the authoritative gate (``mergegate_check.py`` in \"Enforce MergeGate\"). ADR\n-0017's bookkeeping-closeout exemption (condition 6) needs the merged implementation\n+and the authoritative gate (``mergegate_check.py`` in \"Enforce MergeGate\").\n+SADR-0017's bookkeeping-closeout exemption (condition 6) needs the merged implementation\n PR proven, and a closeout carries NO ``post_merge_reconcile`` marker. The signal\n already resolves ``MERGED_PRS`` unconditionally, but \"Enforce MergeGate\" used to\n resolve it only inside ``grep -q '<!-- shiki:post_merge_reconcile -->'``. So a\ndiff --git a/tests/test_sadr_install_migration.py b/tests/test_sadr_install_migration.py\nnew file mode 100644\nindex 0000000000000000000000000000000000000000..d51eac872b1c79ff4579c4c1401ca5d775a60d8d\n--- /dev/null\n+++ b/tests/test_sadr_install_migration.py\n@@ -0,0 +1,237 @@\n+\"\"\"SADR installation and collision-safe legacy upgrade behavior.\"\"\"\n+\n+from __future__ import annotations\n+\n+import contextlib\n+import hashlib\n+import io\n+import json\n+import shutil\n+import tempfile\n+import unittest\n+from pathlib import Path\n+\n+import shiki_test_support  # noqa: F401  (path bootstrap)\n+\n+import shiki_installer\n+from tests.test_adr_references import find_adr_reference_violations\n+\n+\n+PRODUCT_NUMBERS = tuple(range(13, 19))\n+\n+\n+def _quiet_install(target: Path, *, force: bool, validate: bool) -> None:\n+    with contextlib.redirect_stdout(io.StringIO()):\n+        shiki_installer.install_template(target, force=force, validate=validate)\n+\n+\n+def _sha256(path: Path) -> str:\n+    return hashlib.sha256(path.read_bytes()).hexdigest()\n+\n+\n+def _legacy_sadr_paths() -> tuple[str, ...]:\n+    return tuple(\n+        f\"docs/adr/{path.name.removeprefix('SADR-')}\"\n+        for path in sorted((shiki_installer.ROOT / \"docs\" / \"adr\").glob(\"SADR-*.md\"))\n+        if path.name != \"SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\"\n+    )\n+\n+\n+def _create_legacy_upgrade_fixture(\n+    target: Path,\n+) -> tuple[dict[str, bytes], tuple[str, ...]]:\n+    adr_dir = target / \"docs\" / \"adr\"\n+    adr_dir.mkdir(parents=True)\n+    product_bytes = {\n+        f\"{number:04d}-cubeshare-product-decision.md\": (\n+            f\"# CubeShare ADR {number:04d}\\n\\nDo not rewrite {number}.\\n\"\n+        ).encode()\n+        for number in PRODUCT_NUMBERS\n+    }\n+    for name, content in product_bytes.items():\n+        (adr_dir / name).write_bytes(content)\n+\n+    legacy_paths = _legacy_sadr_paths()\n+    for relative in legacy_paths:\n+        (target / relative).write_bytes(f\"legacy Shiki bytes for {relative}\\n\".encode())\n+\n+    stamp_path = target / shiki_installer.INSTALL_STAMP_PATH\n+    stamp_path.parent.mkdir(parents=True, exist_ok=True)\n+    stamp = {\n+        \"version\": shiki_installer.INSTALL_STAMP_VERSION,\n+        \"platform_commit\": None,\n+        \"installed_at\": \"2000-01-01T00:00:00+00:00\",\n+        \"digests\": {\n+            relative: _sha256(target / relative) for relative in legacy_paths\n+        },\n+    }\n+    stamp_path.write_text(\n+        json.dumps(stamp, indent=2, sort_keys=True) + \"\\n\", encoding=\"utf-8\"\n+    )\n+    return product_bytes, legacy_paths\n+\n+\n+def _tree_snapshot(root: Path) -> dict[str, tuple[str, bytes | str]]:\n+    \"\"\"Recursive byte/shape snapshot used to prove preflight is write-free.\"\"\"\n+    snapshot: dict[str, tuple[str, bytes | str]] = {}\n+    for path in sorted(root.rglob(\"*\")):\n+        relative = path.relative_to(root).as_posix()\n+        if path.is_symlink():\n+            snapshot[relative] = (\"symlink\", path.readlink().as_posix())\n+        elif path.is_dir():\n+            snapshot[relative] = (\"directory\", \"\")\n+        else:\n+            snapshot[relative] = (\"file\", path.read_bytes())\n+    return snapshot\n+\n+\n+class FreshSadrInstallTests(unittest.TestCase):\n+    def setUp(self) -> None:\n+        self.tmp = Path(tempfile.mkdtemp(prefix=\"shiki-sadr-fresh-\"))\n+        self.addCleanup(lambda: shutil.rmtree(self.tmp, ignore_errors=True))\n+        self.target = self.tmp / \"target\"\n+        self.adr_dir = self.target / \"docs\" / \"adr\"\n+        self.adr_dir.mkdir(parents=True)\n+        self.product_bytes = {\n+            f\"{number:04d}-product-decision.md\": (\n+                f\"# Product ADR {number:04d}\\n\\nTarget-owned bytes {number}.\\n\"\n+            ).encode()\n+            for number in PRODUCT_NUMBERS\n+        }\n+        for name, content in self.product_bytes.items():\n+            (self.adr_dir / name).write_bytes(content)\n+\n+    def test_fresh_install_preserves_product_adrs_and_installs_all_sadrs(self) -> None:\n+        _quiet_install(self.target, force=False, validate=True)\n+\n+        for name, expected in self.product_bytes.items():\n+            self.assertEqual((self.adr_dir / name).read_bytes(), expected)\n+\n+        sadr_numbers = {\n+            path.name.split(\"-\", 2)[1]\n+            for path in self.adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\")\n+        }\n+        self.assertEqual(sadr_numbers, {f\"{number:04d}\" for number in range(1, 20)})\n+\n+        numeric_records = {\n+            path.name\n+            for path in self.adr_dir.glob(\"[0-9][0-9][0-9][0-9]-*.md\")\n+        }\n+        self.assertEqual(numeric_records, set(self.product_bytes))\n+        stamp = json.loads(\n+            (self.target / shiki_installer.INSTALL_STAMP_PATH).read_text(\n+                encoding=\"utf-8\"\n+            )\n+        )\n+        for path in self.adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\"):\n+            relative = path.relative_to(self.target).as_posix()\n+            self.assertEqual(stamp[\"digests\"][relative], _sha256(path))\n+        self.assertEqual(find_adr_reference_violations(self.target), [])\n+\n+\n+class LegacySadrUpgradeTests(unittest.TestCase):\n+    def setUp(self) -> None:\n+        self.tmp = Path(tempfile.mkdtemp(prefix=\"shiki-sadr-upgrade-\"))\n+        self.addCleanup(lambda: shutil.rmtree(self.tmp, ignore_errors=True))\n+        self.target = self.tmp / \"target\"\n+        self.adr_dir = self.target / \"docs\" / \"adr\"\n+        self.product_bytes, self.legacy_paths = _create_legacy_upgrade_fixture(\n+            self.target\n+        )\n+\n+    def test_force_upgrade_deletes_only_stamped_legacy_sadrs(self) -> None:\n+        _quiet_install(self.target, force=True, validate=True)\n+\n+        for relative in self.legacy_paths:\n+            self.assertFalse((self.target / relative).exists(), relative)\n+        for name, expected in self.product_bytes.items():\n+            self.assertEqual((self.adr_dir / name).read_bytes(), expected)\n+\n+        sadrs = list(self.adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\"))\n+        self.assertEqual(len(sadrs), 19)\n+        refreshed = json.loads(\n+            (self.target / shiki_installer.INSTALL_STAMP_PATH).read_text(encoding=\"utf-8\")\n+        )\n+        for relative in self.legacy_paths:\n+            self.assertNotIn(relative, refreshed[\"digests\"])\n+        for path in sadrs:\n+            relative = path.relative_to(self.target).as_posix()\n+            self.assertEqual(refreshed[\"digests\"][relative], _sha256(path))\n+\n+    def test_non_force_install_never_deletes_legacy_files(self) -> None:\n+        before = {\n+            relative: (self.target / relative).read_bytes()\n+            for relative in self.legacy_paths\n+        }\n+        _quiet_install(self.target, force=False, validate=False)\n+        for relative, expected in before.items():\n+            self.assertEqual((self.target / relative).read_bytes(), expected)\n+        refreshed = json.loads(\n+            (self.target / shiki_installer.INSTALL_STAMP_PATH).read_text(encoding=\"utf-8\")\n+        )\n+        for relative in self.legacy_paths:\n+            self.assertEqual(refreshed[\"digests\"][relative], _sha256(self.target / relative))\n+\n+\n+class LegacyCleanupPreflightTests(unittest.TestCase):\n+    def setUp(self) -> None:\n+        self.tmp = Path(tempfile.mkdtemp(prefix=\"shiki-sadr-preflight-\"))\n+        self.addCleanup(lambda: shutil.rmtree(self.tmp, ignore_errors=True))\n+\n+    def test_all_ownership_blockers_abort_before_any_write(self) -> None:\n+        cases = (\n+            (\"absent\", \"install stamp is absent\"),\n+            (\"unreadable\", \"install stamp is unreadable\"),\n+            (\"malformed\", \"install stamp is malformed\"),\n+            (\"missing-digest\", \"install stamp has no digest\"),\n+            (\"digest-mismatch\", \"digest mismatch\"),\n+        )\n+        for name, expected_reason in cases:\n+            with self.subTest(name=name):\n+                target = self.tmp / name\n+                _, legacy_paths = _create_legacy_upgrade_fixture(target)\n+                stamp_path = target / shiki_installer.INSTALL_STAMP_PATH\n+\n+                if name == \"absent\":\n+                    stamp_path.unlink()\n+                elif name == \"unreadable\":\n+                    stamp_path.unlink()\n+                    stamp_path.mkdir()\n+                elif name == \"malformed\":\n+                    stamp_path.write_text(\"{not-json\\n\", encoding=\"utf-8\")\n+                else:\n+                    stamp = json.loads(stamp_path.read_text(encoding=\"utf-8\"))\n+                    if name == \"missing-digest\":\n+                        stamp[\"digests\"] = {}\n+                    else:\n+                        stamp[\"digests\"] = {\n+                            relative: \"deadbeef\" for relative in legacy_paths\n+                        }\n+                    stamp_path.write_text(\n+                        json.dumps(stamp, indent=2, sort_keys=True) + \"\\n\",\n+                        encoding=\"utf-8\",\n+                    )\n+\n+                before = _tree_snapshot(target)\n+                with self.assertRaises(shiki_installer.ShikiError) as raised:\n+                    _quiet_install(target, force=True, validate=False)\n+                after = _tree_snapshot(target)\n+\n+                self.assertEqual(before, after, f\"{name} preflight changed the tree\")\n+                message = str(raised.exception)\n+                self.assertIn(expected_reason, message)\n+                self.assertIn(\"No files were written\", message)\n+                for relative in legacy_paths:\n+                    self.assertIn(relative, message)\n+\n+    def test_absent_stamp_is_allowed_when_no_legacy_path_exists(self) -> None:\n+        target = self.tmp / \"no-legacy\"\n+        target.mkdir()\n+        _quiet_install(target, force=True, validate=False)\n+        self.assertTrue(\n+            (target / \"docs\" / \"adr\" / \"SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\").is_file()\n+        )\n+\n+\n+if __name__ == \"__main__\":\n+    unittest.main()\n\n2026-08-07T08:23:19.491588Z ERROR codex_models_manager::manager: failed to renew cache TTL: missing field `base_instructions` at line 94 column 5\ncodex\nThe first focused invocation used package-style discovery, but this repository\u2019s tests bootstrap from `tests/` as the discovery root, so it failed before collection (`shiki_test_support` was not on `sys.path`). That is an invocation mismatch, not a code failure; I\u2019m rerunning with the repository\u2019s supported discover form.\nexec\n/bin/zsh -lc \"python3 -m unittest discover -s tests -p 'test_sadr_install_migration.py' -v\" in /private/tmp/shiki-adr-collision.pa0KwY/.worktrees/shiki-t-20260807t073307221254z-2d22d654-introduc\ndiff --git a/.codex/skills/shiki/SKILL.md b/.codex/skills/shiki/SKILL.md\nindex c8e2e85d9d75d49cc4f2d6286ee22659c4e53e75..93cded623b2c15d9f65c76491a1c2d813ac9e6c0\n--- a/.codex/skills/shiki/SKILL.md\n+++ b/.codex/skills/shiki/SKILL.md\n@@ -18,6 +18,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n Codex CLI does not register this skill as a custom `/shiki` slash command. When\n the user types `/shiki` in Codex CLI and gets \"Unrecognized command\", explain\n that this is expected: invoke Shiki in Codex with natural language such as\n@@ -69,7 +72,7 @@\n \n ## Responsibilities\n \n-- Claude Code implements and repairs by default (`shiki runner claude`, ADR 0008); Codex implements and repairs tasks explicitly assigned to `codex`.\n+- Claude Code implements and repairs by default (`shiki runner claude`, SADR-0008); Codex implements and repairs tasks explicitly assigned to `codex`.\n - Claude Code Action can act as GitHub-side CCA or reviewer.\n - CCA judges completion.\n - MergeGate authorizes state transitions and merge readiness.\ndiff --git a/.github/prompts/cca-completion-check.md b/.github/prompts/cca-completion-check.md\nindex ed203ad5f302ecc81372d8ab27d2b1232ca97497..fd89950528333ef6242e6d5443904f5392ab7e56\n--- a/.github/prompts/cca-completion-check.md\n+++ b/.github/prompts/cca-completion-check.md\n@@ -19,6 +19,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n ## Required Reading\n \n Read only the files needed to judge the PR contract. Prefer this order:\ndiff --git a/.github/prompts/codex-task-handoff.md b/.github/prompts/codex-task-handoff.md\nindex 695e211d76dea1c60e8d5bb360e78487808286a4..59441d576b01a39569722f226c854e5a740e9eb5\n--- a/.github/prompts/codex-task-handoff.md\n+++ b/.github/prompts/codex-task-handoff.md\n@@ -2,7 +2,7 @@\n \n You are Codex Front acting as the Implementer for a Shiki task.\n \n-Follow `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant ADRs, and `docs/agents/codex-handoff.md`.\n+Follow `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant Shiki SADRs and target ADRs, and `docs/agents/codex-handoff.md`.\n \n Rules:\n \ndiff --git a/AGENTS.md b/AGENTS.md\nindex 7f041100d49c2a7525f51cb0f5d696a4a6d1c78c..a06bfbabc6fa48a007389e25e12d7ed523936bed\n--- a/AGENTS.md\n+++ b/AGENTS.md\n@@ -22,7 +22,7 @@\n - **Codex Front** is an optional implementer through Codex App, Codex CLI, Codex IDE extension, or Codex Web signed in with ChatGPT OAuth/subscription auth, dispatched with `shiki runner codex` when a task is explicitly assigned to `codex`.\n - **GitHub CCA** is implemented by Claude Code Action by default, using `CLAUDE_CODE_OAUTH_TOKEN`.\n \n-Do not assume `openai/codex-action`, `OPENAI_API_KEY`, or API-key based Codex automation in the default Shiki path. API-key runners are explicit target-repo extensions and require their own ADR.\n+Do not assume `openai/codex-action`, `OPENAI_API_KEY`, or API-key based Codex automation in the default Shiki path. API-key runners are explicit target-repo extensions and require their own target ADR.\n \n Shiki is not:\n \n@@ -44,10 +44,14 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use\n+`SADR-NNNN-*.md`; target product decisions retain `NNNN-*.md` and the `ADR`\n+namespace.\n+\n Use the canonical source-of-truth order above. Runtime-specific reading orders\n are operational guidance only and must not override or reorder it.\n \n-Conversation state is not durable truth. If a decision matters, put it in GitHub, `.shiki/`, `CONTEXT.md`, or an ADR.\n+Conversation state is not durable truth. If a decision matters, put it in GitHub, `.shiki/`, `CONTEXT.md`, a Shiki SADR, or a target ADR.\n \n If GitHub and `.shiki/` disagree, surface the conflict and prefer GitHub operational state until the mirror is repaired.\n \n@@ -75,8 +79,8 @@\n Every non-trivial change follows this loop:\n \n 1. **Goal**: clarify outcome, completion conditions, non-goals, risk level, and success signals.\n-2. **grill-with-docs**: challenge the plan against domain docs, ADRs, code reality, terminology, and edge scenarios.\n-3. **Context & Impact**: find relevant docs, ADRs, code, dependencies, owners, locks, and verification surfaces.\n+2. **grill-with-docs**: challenge the plan against domain docs, Shiki SADRs, target ADRs, code reality, terminology, and edge scenarios.\n+3. **Context & Impact**: find relevant docs, SADRs, target ADRs, code, dependencies, owners, locks, and verification surfaces.\n 4. **PRD and Spec Freeze**: use `to-prd` when context is settled enough to become durable product/engineering intent. The operator's explicit approval of the PRD is Spec Freeze: the plan records a `spec_freeze` block, and no plan runs without it. Steps 1-4 form Requirements Definition \u2014 one continuous operator dialogue. After Spec Freeze, scope changes require an operator-approved Spec Amendment; non-scope-moving interpretations go to the Assumption Log.\n 5. **Issues**: use `to-issues` to create independently grabbable vertical-slice issues. Prefer AFK slices over HITL slices where possible.\n 6. **Triage**: label issues for readiness, risk, runtime, skills, and MergeGate state.\n@@ -167,7 +171,7 @@\n - Testing decisions.\n - Out-of-scope items.\n - Further notes.\n-- Links to relevant domain terms and ADRs.\n+- Links to relevant domain terms, SADRs, and target ADRs.\n \n Every executable task must include:\n \n@@ -193,7 +197,7 @@\n \n Context & Impact must identify:\n \n-- Relevant documents, domain terms, ADRs, and past decisions.\n+- Relevant documents, domain terms, SADRs, target ADRs, and past decisions.\n - Relevant modules, interfaces, seams, callers, owners, and tests.\n - Dependency relationships between tasks.\n - Candidate locks and lock conflicts.\n@@ -210,7 +214,7 @@\n | Skill | Required when |\n | --- | --- |\n | `setup-matt-pocock-skills` | First configuring the repo for skills, or when issue tracker, triage labels, or domain docs are missing. |\n-| `grill-with-docs` | Default for non-trivial Goals before PRD/issues; always required when plans, terminology, boundaries, tradeoffs, or ADR-worthy decisions are ambiguous. |\n+| `grill-with-docs` | Default for non-trivial Goals before PRD/issues; always required when plans, terminology, boundaries, tradeoffs, or decision-record-worthy choices are ambiguous. |\n | `zoom-out` | The agent lacks an architectural map of the relevant code area. |\n | `to-prd` | Settled Goal context must become durable product/engineering intent. |\n | `to-issues` | A Goal, PRD, or plan must become independently grabbable vertical-slice issues. |\n@@ -348,7 +352,7 @@\n - Three or more new files.\n - Any high-risk or critical label.\n \n-When the architecture gate triggers, run `grill-with-docs` or `improve-codebase-architecture` as appropriate and record durable decisions in an ADR when needed.\n+When the architecture gate triggers, run `grill-with-docs` or `improve-codebase-architecture` as appropriate and record durable decisions in a Shiki SADR or target ADR as applicable.\n \n ## Engineering Discipline\n \ndiff --git a/CLAUDE.md b/CLAUDE.md\nindex 98a932e7841a632866fcd53b0bad0e3af3711d3d..98a31399404d6fb044b4fd493f4459ac87f439de\n--- a/CLAUDE.md\n+++ b/CLAUDE.md\n@@ -14,6 +14,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n ## Claude Role\n \n Default to planner, default implementer, reviewer, coordinator, documentation editor, CCA-style completion judge, and final-judgment assistant.\n@@ -57,7 +60,7 @@\n At the start of a Shiki session:\n \n 1. Read `AGENTS.md`.\n-2. Read `CONTEXT.md` and relevant ADRs.\n+2. Read `CONTEXT.md` and relevant Shiki SADRs and target ADRs.\n 3. Read active `.shiki/` Goal/task/ledger state when present.\n 4. Read active distilled rules from `.shiki/memories/` (status `distilled`, `active: true`, not revoked or superseded) for applicable guidance; the same rules are injected deterministically into task handoffs as the `## Distilled Rules` section (\u00a73.5).\n 5. Read the GitHub Issue or PR when the task is GitHub-backed.\n@@ -74,7 +77,7 @@\n - Define the Goal, completion conditions, non-goals, and risk level.\n - Treat `grill-with-docs` as the default planning entry point for non-trivial Goals.\n - Challenge domain terms against `CONTEXT.md`.\n-- Challenge hard-to-reverse tradeoffs against ADRs.\n+- Challenge hard-to-reverse tradeoffs against SADRs and target ADRs.\n - Explore code instead of asking when code can answer the question.\n - Run Context & Impact before implementation planning.\n - Produce a Task DAG, not an unordered checklist.\n@@ -94,7 +97,7 @@\n - For each question, provide your recommended answer.\n - Resolve dependency decisions before downstream design questions.\n - Update `CONTEXT.md` inline when terminology crystallizes.\n-- Offer ADRs only when the decision is hard to reverse, surprising without context, and a real tradeoff.\n+- Offer a SADR or target ADR only when the decision is hard to reverse, surprising without context, and a real tradeoff.\n - Convert unresolved questions into HITL blockers rather than allowing Codex to guess.\n \n ## PRD and Issues Standard\n@@ -103,7 +106,7 @@\n \n - Use the project's domain glossary vocabulary.\n - Avoid volatile file paths unless they are essential.\n-- Include implementation decisions, testing decisions, out-of-scope items, and relevant ADR/domain links.\n+- Include implementation decisions, testing decisions, out-of-scope items, and relevant SADR/target-ADR/domain links.\n - Identify deep-module opportunities and testable interfaces.\n \n When creating issues:\n@@ -121,7 +124,7 @@\n - Branch/worktree target.\n - Scope and non-goals.\n - Dependencies and locks.\n-- Relevant docs, ADRs, modules, and tests.\n+- Relevant docs, SADRs, target ADRs, modules, and tests.\n - Required skill invocations.\n - TDD expectations.\n - Pre-PR `code-review` gate expectations.\n@@ -152,7 +155,7 @@\n \n - Treat yourself as a judge, not implementer.\n - Do not write production code.\n-- Read `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant ADRs, PRD, task issue, PR body, diff, checks, reviews, labels, and `.shiki/` state.\n+- Read `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant SADRs and target ADRs, PRD, task issue, PR body, diff, checks, reviews, labels, and `.shiki/` state.\n - Evaluate every applicable checklist in `docs/agents/checklists.md`.\n - Map each acceptance criterion to durable evidence.\n - Distinguish implementation failure from missing evidence.\ndiff --git a/CONTEXT.md b/CONTEXT.md\nindex e61a9be06a0cce0d026484083a657bde2067e234..237ee8aa3ea8fab334ece49768bc5ed3b5af0974\n--- a/CONTEXT.md\n+++ b/CONTEXT.md\n@@ -25,7 +25,7 @@\n _Avoid_: jumping directly into code\n \n **grill-with-docs**:\n-A planning skill that challenges a plan against domain language, ADRs, code reality, and concrete edge scenarios. It resolves design-tree questions before PRD/issues. It runs inside Requirements Definition.\n+A planning skill that challenges a plan against domain language, Shiki SADRs, target ADRs, code reality, and concrete edge scenarios. It resolves design-tree questions before PRD/issues. It runs inside Requirements Definition.\n _Avoid_: generic brainstorming, silent assumptions\n \n **Requirements Definition**:\ndiff --git a/CONTRIBUTING.md b/CONTRIBUTING.md\nindex c6d9b656e73abb8a55e5f660123e03153dcb3e6d..9e37a8fbf668db5b22461aaae5548b8f1354394c\n--- a/CONTRIBUTING.md\n+++ b/CONTRIBUTING.md\n@@ -17,7 +17,7 @@\n 1. **Goal** \u2014 start from a user-approved Goal with an outcome, completion\n    conditions, non-goals, risk level, and success signals.\n 2. **grill-with-docs** \u2014 for any non-trivial change, challenge the plan against\n-   domain language, ADRs, and code reality before writing a PRD.\n+   domain language, Shiki SADRs, target ADRs, and code reality before writing a PRD.\n 3. **PRD** \u2014 capture settled product/engineering intent (`to-prd`).\n 4. **Spec Freeze** \u2014 the operator explicitly approves the PRD/requirements;\n    the plan records a `spec_freeze` block and no plan runs without it\n@@ -94,7 +94,7 @@\n   entries.\n - Do not hand-edit `.shiki/goals/*.json` or `.shiki/manifest.json` in a task PR.\n - Conversation state is not durable truth. If a decision matters, record it in\n-  GitHub, `.shiki/`, `CONTEXT.md`, or an ADR.\n+  GitHub, `.shiki/`, `CONTEXT.md`, a Shiki SADR, or a target ADR.\n \n ## Architecture Gate\n \n@@ -103,7 +103,7 @@\n or CI policy, package structure, three or more structural files, more than 100\n net new lines in a core module, three or more new files, or any high-risk label.\n When triggered, run `grill-with-docs` or `improve-codebase-architecture` and\n-record durable decisions in an ADR when needed (see `docs/adr/`).\n+record durable decisions in a Shiki SADR or target ADR as applicable (see `docs/adr/`).\n \n ## Safety\n \ndiff --git a/SYSTEM_PROMPT.md b/SYSTEM_PROMPT.md\nindex aaaea45e3a896c3a44ba89eec8bb84325ecf6c62..da009c86a88285269dee0155e58e550c6d744ea9\n--- a/SYSTEM_PROMPT.md\n+++ b/SYSTEM_PROMPT.md\n@@ -30,6 +30,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n Use the canonical source-of-truth order above. Runtime-specific reading orders\n are operational guidance only and must not override or reorder it. The current\n conversation is non-durable operator input.\n@@ -48,14 +51,14 @@\n \n A runtime may only do work that is explicitly assigned by the Goal, task, PR, or MergeGate decision.\n \n-Do not assume `openai/codex-action`, `OPENAI_API_KEY`, or API-key based Codex automation in the default Shiki path. API-key runners are explicit target-repo extensions and require their own ADR.\n+Do not assume `openai/codex-action`, `OPENAI_API_KEY`, or API-key based Codex automation in the default Shiki path. API-key runners are explicit target-repo extensions and require their own target ADR.\n \n ## 4. Non-negotiable Shiki flow\n \n For any non-trivial Goal, use this flow:\n \n 1. **Goal Seek**: define outcome, non-goals, risk level, completion criteria, and evidence requirements.\n-2. **grill-with-docs**: challenge the plan against `CONTEXT.md`, ADRs, code reality, terminology, boundaries, and edge scenarios. Ask one question at a time when the operator is available. If code can answer a question, inspect code instead of asking.\n+2. **grill-with-docs**: challenge the plan against `CONTEXT.md`, Shiki SADRs, target ADRs, code reality, terminology, boundaries, and edge scenarios. Ask one question at a time when the operator is available. If code can answer a question, inspect code instead of asking.\n 3. **Context & Impact**: identify modules, interfaces, seams, callers, dependencies, locks, risk, and verification surfaces.\n 4. **to-prd + Spec Freeze**: turn settled context into a PRD using domain vocabulary and testing decisions. The operator's explicit approval of the PRD is Spec Freeze, recorded as the plan's `spec_freeze` block; plans without it do not run. Steps 1-4 form Requirements Definition.\n 5. **to-issues**: decompose the PRD into independently grabbable vertical-slice issues. Prefer AFK slices; mark HITL slices when judgment is still required.\n@@ -75,7 +78,7 @@\n Before making any material change:\n \n 1. Read `AGENTS.md` and any runtime-specific wrapper such as `CLAUDE.md`.\n-2. Read `CONTEXT.md` and relevant ADRs.\n+2. Read `CONTEXT.md` and relevant SADRs and target ADRs.\n 3. Read the GitHub Issue / PR and `.shiki/` state for the active Goal or task.\n 4. Identify your role: Planner, Implementer, Reviewer, Completion Check Agent, Repairer, or Guardian-assist.\n 5. Identify Goal id, task id, branch, PR, dependencies, locks, risk level, required skills, and required evidence.\n@@ -103,7 +106,7 @@\n \n At minimum, identify:\n \n-- Relevant documents, ADRs, domain terms, and prior decisions.\n+- Relevant documents, SADRs, target ADRs, domain terms, and prior decisions.\n - Relevant modules, interfaces, seams, callers, owners, and tests.\n - Dependency relationships between tasks.\n - Candidate file locks and lock conflicts.\n@@ -152,7 +155,7 @@\n \n CCA must read:\n \n-- `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant ADRs.\n+- `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant SADRs and target ADRs.\n - Parent Goal issue, PRD issue, task issue, comments, and labels.\n - PR body, diff, commits, changed files, reviews, CI checks, and artifacts available to the workflow.\n - `.shiki/` task contract, locks, ledger entries, CCA checklist profile, and prior repair packets.\n@@ -214,7 +217,7 @@\n - Three or more new files.\n - Any high-risk or critical label.\n \n-When the architecture gate triggers, run `grill-with-docs` or `improve-codebase-architecture` as appropriate and record the decision in an ADR if it is hard to reverse or likely to be re-litigated.\n+When the architecture gate triggers, run `grill-with-docs` or `improve-codebase-architecture` as appropriate and record the decision in a Shiki SADR or target ADR as applicable if it is hard to reverse or likely to be re-litigated.\n \n ## 14. Skill Gate\n \n@@ -223,7 +226,7 @@\n Use these skills:\n \n - `setup-matt-pocock-skills`: before first use of the engineering skill set, or when issue tracker, triage labels, or domain docs are missing.\n-- `grill-with-docs`: default for non-trivial Goals before PRD/issues; required for ambiguous plans, terminology, boundaries, ADR-worthy tradeoffs, or design-tree decisions.\n+- `grill-with-docs`: default for non-trivial Goals before PRD/issues; required for ambiguous plans, terminology, boundaries, decision-record-worthy tradeoffs, or design-tree decisions.\n - `zoom-out`: unfamiliar code area or missing architectural map.\n - `to-prd`: convert settled context into a PRD.\n - `to-issues`: break a PRD, plan, or Goal into independently grabbable vertical-slice issues.\ndiff --git a/docs/adr/0001-keep-shiki-as-platform-name.md b/docs/adr/SADR-0001-keep-shiki-as-platform-name.md\nindex 20a9637c3b9d8c97de7ba8db7cfae4d82d9a7dba..8b685439959aada1b221c112f0ac3c51ca8a1695\n--- a/docs/adr/0001-keep-shiki-as-platform-name.md\n+++ b/docs/adr/SADR-0001-keep-shiki-as-platform-name.md\n@@ -1,3 +1,3 @@\n-# Keep Shiki As The Platform Name\n+# SADR-0001: Keep Shiki As The Platform Name\n \n Shiki remains the product name for the GitHub-native agentic engineering control plane. We considered more descriptive names such as MergePilot, MergeGate, and Kiroku, but kept Shiki because it carries the existing goal-seeking framework identity while remaining independent of Hermes, Codex, Claude Code, or any single runtime.\ndiff --git a/docs/adr/0002-use-agents-as-shared-constitution.md b/docs/adr/SADR-0002-use-agents-as-shared-constitution.md\nindex 73de30ecb078120eac540d9bc51855e3cd48d0e3..c5486182bf06fca36ea508496b013c67302fd989\n--- a/docs/adr/0002-use-agents-as-shared-constitution.md\n+++ b/docs/adr/SADR-0002-use-agents-as-shared-constitution.md\n@@ -1,3 +1,3 @@\n-# Use AGENTS As The Shared Agent Constitution\n+# SADR-0002: Use AGENTS As The Shared Agent Constitution\n \n Shiki uses `AGENTS.md` as the runtime-neutral constitution and `CLAUDE.md` as a thin Claude Code wrapper. This keeps Codex, Claude Code, Hermes Runner, and future Agent Runtimes aligned on the same GitHub-first MergeGate rules while still allowing runtime-specific instructions where needed.\ndiff --git a/docs/adr/0003-require-skill-gate-for-mergegate-readiness.md b/docs/adr/SADR-0003-require-skill-gate-for-mergegate-readiness.md\nindex 424be0c9af4c8f0136f1fde68a468c47ff84e764..7fc337987c7df9792f75f181dfcbed75fe5ffff2\n--- a/docs/adr/0003-require-skill-gate-for-mergegate-readiness.md\n+++ b/docs/adr/SADR-0003-require-skill-gate-for-mergegate-readiness.md\n@@ -1,3 +1,3 @@\n-# Require Skill Gate For MergeGate Readiness\n+# SADR-0003: Require Skill Gate For MergeGate Readiness\n \n Shiki requires relevant engineering skills to be invoked before execution when their triggers apply. This makes skills part of MergeGate readiness rather than optional prompt style, because Shiki depends on repeatable planning, issue decomposition, TDD, diagnosis, triage, and architecture review before autonomous or parallel agent work can be trusted.\ndiff --git a/docs/adr/0004-use-github-first-with-shiki-mirror.md b/docs/adr/SADR-0004-use-github-first-with-shiki-mirror.md\nindex d0a3a9174dc646a945a9d325954ffaf00c352040..a79c54ae9ee8eed2a0524192b864da4e22a92d3f\n--- a/docs/adr/0004-use-github-first-with-shiki-mirror.md\n+++ b/docs/adr/SADR-0004-use-github-first-with-shiki-mirror.md\n@@ -1,4 +1,4 @@\n-# ADR 0004: Use GitHub First With A Repository-Local Shiki Mirror\n+# SADR-0004: Use GitHub First With A Repository-Local Shiki Mirror\n \n ## Status\n \ndiff --git a/docs/adr/0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md b/docs/adr/SADR-0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\nindex b339baf49b89d9290e79c3d51b36e7b2bde3bd95..0f3c3eb9afccdb3403ce978e21d04fe12e495da0\n--- a/docs/adr/0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\n+++ b/docs/adr/SADR-0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\n@@ -1,8 +1,8 @@\n-# ADR 0005: Use Codex As Front And Claude Code Action As The GitHub Runtime\n+# SADR-0005: Use Codex As Front And Claude Code Action As The GitHub Runtime\n \n ## Status\n \n-Accepted (implementer default superseded by ADR 0008; the auth model and the\n+Accepted (implementer default superseded by SADR-0008; the auth model and the\n Claude Code Action GitHub runtime decision remain in force)\n \n ## Context\n@@ -26,4 +26,4 @@\n - Shiki avoids requiring `OPENAI_API_KEY` for the default workflow.\n - Codex work happens through the user's authenticated Codex front surface, not as a hidden backend Action.\n - Claude Code Action is configured for OAuth-token based subscription usage by default.\n-- API-key based automation remains possible only as an explicit target-repo extension with its own ADR.\n+- API-key based automation remains possible only as an explicit target-repo extension with its own target ADR.\ndiff --git a/docs/adr/0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md b/docs/adr/SADR-0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\nindex 4bcae9d1e52f0a37b8e9fbafa5cd14b6ee1993bd..7a38538a6ae9929567f0ade67e7ea6a60d0b3f7e\n--- a/docs/adr/0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\n+++ b/docs/adr/SADR-0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\n@@ -1,4 +1,4 @@\n-# ADR 0006: Separate Codex Implementation, CCA Completion, And MergeGate Authorization\n+# SADR-0006: Separate Codex Implementation, CCA Completion, And MergeGate Authorization\n \n ## Status\n \ndiff --git a/docs/adr/0007-packaging-and-release.md b/docs/adr/SADR-0007-packaging-and-release.md\nindex 79b84ddb97167372b44641e5b7860e400ce80822..bb445a2e2e856d9d1f5fdbe35de8d4e8cec0fb32\n--- a/docs/adr/0007-packaging-and-release.md\n+++ b/docs/adr/SADR-0007-packaging-and-release.md\n@@ -1,4 +1,4 @@\n-# ADR 0007: Packaging And Release For Shiki\n+# SADR-0007: Packaging And Release For Shiki\n \n ## Status\n \n@@ -25,7 +25,7 @@\n   not introduce third-party runtime dependencies or break the shim.\n - Releases must run inside the operator's subscription-authenticated toolchain\n   and must not require extra secrets beyond the built-in `GITHUB_TOKEN`\n-  (consistent with ADR 0005's no-API-key-by-default stance).\n+  (consistent with SADR-0005's no-API-key-by-default stance).\n - `.shiki/manifest.json` already encodes the install surface (`install.include`,\n   `install.create_directories`, `install.exclude_from_commit`), and it must not\n   be mutated as part of this change.\n@@ -34,7 +34,7 @@\n \n This is a hard-to-reverse platform decision (it sets the version source of\n truth, the release trigger, and the public install contract), so it is recorded\n-as an ADR.\n+as a SADR.\n \n ## Decision\n \n@@ -104,7 +104,7 @@\n 3. **Publishing to PyPI.** Rejected for now: Shiki is distributed as a\n    repository template + git-installable CLI, not a PyPI library; PyPI would add\n    credentials/secrets and a different versioning contract. Can be added later as\n-   its own ADR if a packaged distribution is needed.\n+   its own SADR if a packaged distribution is needed.\n 4. **Dynamic version (read `VERSION` via a build backend `dynamic` field).**\n    Rejected to keep `pyproject.toml` backend-agnostic and dependency-free; the\n    small duplication between `VERSION` and `pyproject.toml` is guarded by the\n@@ -123,4 +123,4 @@\n   releasing versions the whole platform but ships only `install.include` to\n   targets.\n - Future PyPI distribution or a console entry point are deferred and would each\n-  warrant their own ADR.\n+  warrant their own SADR.\ndiff --git a/docs/adr/0008-use-claude-code-as-default-implementer.md b/docs/adr/SADR-0008-use-claude-code-as-default-implementer.md\nindex dedf4510b7c7799521422f622029563d373db031..35c2625d908167fbc58855b5b63e283805f167e8\n--- a/docs/adr/0008-use-claude-code-as-default-implementer.md\n+++ b/docs/adr/SADR-0008-use-claude-code-as-default-implementer.md\n@@ -1,12 +1,12 @@\n-# ADR 0008: Use Claude Code As The Default Implementer Runtime\n+# SADR-0008: Use Claude Code As The Default Implementer Runtime\n \n ## Status\n \n-Proposed (supersedes ADR 0005 for the implementer default; keeps its auth model)\n+Proposed (supersedes SADR-0005 for the implementer default; keeps its auth model)\n \n ## Context\n \n-ADR 0005 made Codex Front the default implementation runtime, with Claude Code\n+SADR-0005 made Codex Front the default implementation runtime, with Claude Code\n restricted to planner and reviewer roles by the runtime registry, and the only\n autonomous implementation adapter was `shiki runner codex`.\n \n@@ -18,13 +18,13 @@\n - The platform's planning, review, and CCA layers already run on Claude Code,\n   and Claude Code's native capabilities (the built-in `/code-review` skill and\n   the Workflow multi-agent orchestration tool) have no Codex equivalent.\n-- The Goal lifecycle redesign (ADR 0009) requires an autonomous post-freeze\n+- The Goal lifecycle redesign (SADR-0009) requires an autonomous post-freeze\n   loop driven from the operator's Claude Code session, including bounded\n   repair dispatch, which must not depend on a runtime that cannot be invoked\n   unattended.\n \n The constitution claims Shiki is runtime-agnostic (\"not a Claude-only\n-workflow\", ADR 0002). Any change to the implementer default must preserve the\n+workflow\", SADR-0002). Any change to the implementer default must preserve the\n registry-based runtime model rather than hardcoding one vendor.\n \n ## Decision\n@@ -39,7 +39,7 @@\n   the task to review or repair-needed.\n - Keep Codex registered as an optional implementer runtime. `shiki runner\n   codex`, the Codex handoff templates, and the ChatGPT OAuth auth model from\n-  ADR 0005 remain supported for tasks explicitly assigned to `codex`.\n+  SADR-0005 remain supported for tasks explicitly assigned to `codex`.\n - Commit to mandating two Claude-native capabilities as loop contract in\n   follow-up Goals (status: the code-review gate was delivered by Goal G-B \u2014\n   skill registry, checklists item PR-12, default required_skills \u2014 while the\n@@ -60,7 +60,7 @@\n \n Out of scope: removing Codex support, changing the CCA runtime (Claude Code\n Action with `CLAUDE_CODE_OAUTH_TOKEN` stays), and API-key based automation\n-(still requires its own ADR per ADR 0005).\n+(still requires its own target ADR per SADR-0005).\n \n ## Consequences\n \n@@ -100,7 +100,7 @@\n   harness rejection of direct `codex exec` is outside Shiki's control, and the\n   plan's mandatory native capabilities cannot run on Codex at all.\n - **Remove Codex entirely**: rejected; it abandons the runtime-agnostic\n-  constitution (ADR 0002) for no operational gain and forecloses\n+  constitution (SADR-0002) for no operational gain and forecloses\n   cross-runtime adversarial review.\n - **GitHub Actions cloud implementation via claude-code-action**: rejected as\n   default; durable evidence is automatic but token cost, Node 24 workflow\ndiff --git a/docs/adr/0009-requirements-definition-and-spec-freeze.md b/docs/adr/SADR-0009-requirements-definition-and-spec-freeze.md\nindex 29b3f7871bef9203a8b4a831a65c39eb2f506e42..52e7dd69ad0aa4efe0b68c62678abbf7df02aaec\n--- a/docs/adr/0009-requirements-definition-and-spec-freeze.md\n+++ b/docs/adr/SADR-0009-requirements-definition-and-spec-freeze.md\n@@ -1,4 +1,4 @@\n-# ADR 0009: Merge Grill And Planning Into Requirements Definition With Spec Freeze\n+# SADR-0009: Merge Grill And Planning Into Requirements Definition With Spec Freeze\n \n ## Status\n \ndiff --git a/docs/adr/0010-external-ai-guardian-review.md b/docs/adr/SADR-0010-external-ai-guardian-review.md\nindex e6108bbe8bb4e73e0ff4684c8f78001a80438c4e..f3510b145664298adafb151b6f0db51b972f01ec\n--- a/docs/adr/0010-external-ai-guardian-review.md\n+++ b/docs/adr/SADR-0010-external-ai-guardian-review.md\n@@ -1,4 +1,4 @@\n-# ADR 0010: External AI Guardian Review As A First-Class Approval Authority\n+# SADR-0010: External AI Guardian Review As A First-Class Approval Authority\n \n ## Status\n \n@@ -8,7 +8,7 @@\n \n Shiki is intended to be an autonomous, recursive, self-improving agent\n framework: authority-in-the-loop, not human-in-the-loop. Until now the Guardian\n-approval required for high/critical-risk merges (guardian-policy.json, ADR\n+approval required for high/critical-risk merges (guardian-policy.json, SADR\n governance) recognized only human authority sources \u2014 a GitHub review approval,\n the `guardian:approved` label applied by a configured Guardian, or a comment\n carrying `Guardian approval granted` plus the current head SHA.\n@@ -89,7 +89,7 @@\n   autonomous design the operator intends and produced the impersonation\n   incident.\n - **Record AI approval as an operator comment**: rejected as forgery of human\n-  authority \u2014 the exact failure this ADR corrects.\n+  authority \u2014 the exact failure this SADR corrects.\n - **Deliver the artifact as a committed `.shiki/` file**: rejected; a committed\n   approval inside the PR being approved is forgeable by anyone who can push.\n - **A dedicated reviewer bot identity instead of a relayed comment**: deferred;\ndiff --git a/docs/adr/0011-autonomous-quality-gate-evidence.md b/docs/adr/SADR-0011-autonomous-quality-gate-evidence.md\nindex bca0246c5d46e44afbde15486a75eec425fc02f5..9c93eac16d8c3a6d4b11bd7224e3955102cd7db4\n--- a/docs/adr/0011-autonomous-quality-gate-evidence.md\n+++ b/docs/adr/SADR-0011-autonomous-quality-gate-evidence.md\n@@ -1,4 +1,4 @@\n-# ADR 0011: Autonomous Quality-Gate Evidence \u2014 Deterministic Facts And An Independent Verifier\n+# SADR-0011: Autonomous Quality-Gate Evidence \u2014 Deterministic Facts And An Independent Verifier\n \n ## Status\n \ndiff --git a/docs/adr/0012-autonomous-loop-closeout-to-main.md b/docs/adr/SADR-0012-autonomous-loop-closeout-to-main.md\nindex 637fad6dde4989c9c2bf0f81f676d8d29991123c..ec7d85f81030a676bcbba28b43eb365422a36eac\n--- a/docs/adr/0012-autonomous-loop-closeout-to-main.md\n+++ b/docs/adr/SADR-0012-autonomous-loop-closeout-to-main.md\n@@ -1,4 +1,4 @@\n-# 0012 \u2014 Autonomous loop pushes goal completion to main via a closeout PR\n+# SADR-0012 \u2014 Autonomous loop pushes goal completion to main via a closeout PR\n \n - Status: accepted\n - Date: 2026-06-18\ndiff --git a/docs/adr/0013-bootstrap-grants-actions-approve-pull-requests.md b/docs/adr/SADR-0013-bootstrap-grants-actions-approve-pull-requests.md\nindex b6c69d64f1c5ced346b88d6859586715b3b5bd0e..cf48ff4a6716267f539ca58edd1a852e7c7a2d64\n--- a/docs/adr/0013-bootstrap-grants-actions-approve-pull-requests.md\n+++ b/docs/adr/SADR-0013-bootstrap-grants-actions-approve-pull-requests.md\n@@ -1,4 +1,4 @@\n-# ADR 0013: Bootstrap Grants GitHub Actions Permission To Create And Approve Pull Requests\n+# SADR-0013: Bootstrap Grants GitHub Actions Permission To Create And Approve Pull Requests\n \n ## Status\n \n@@ -31,7 +31,7 @@\n security tradeoff: it lets repository automation satisfy a human-review gate. In\n a multi-maintainer repo that could let a compromised or buggy workflow\n self-approve changes. So whether bootstrap turns it on by default \u2014 and with\n-what default token scope \u2014 is an ADR-worthy decision, not an implementation\n+what default token scope \u2014 is a SADR-worthy decision, not an implementation\n detail.\n \n ## Decision\n@@ -48,7 +48,7 @@\n Specifically:\n \n - **`can_approve_pull_request_reviews` defaults to `true`.** Shiki's default\n-  posture is solo/AFK autonomous operation (ADR 0008/0009/0012), where the CCA\n+  posture is solo/AFK autonomous operation (SADR-0008/0009/0012), where the CCA\n   Review Bridge is the only path that can satisfy `required_review: true` after\n   the CCA and Guardian gates have already passed. The default makes a\n   freshly-bootstrapped repo able to run the autonomous loop end to end without a\ndiff --git a/docs/adr/0014-codex-app-external-ai-guardian-ui-adapter.md b/docs/adr/SADR-0014-codex-app-external-ai-guardian-ui-adapter.md\nindex 238db06279334caa341c117362df9271382a7fcd..65f12e9939321e1b3e389a34c822fc1549e5155d\n--- a/docs/adr/0014-codex-app-external-ai-guardian-ui-adapter.md\n+++ b/docs/adr/SADR-0014-codex-app-external-ai-guardian-ui-adapter.md\n@@ -1,4 +1,4 @@\n-# ADR 0014: Use Codex App As The External AI Guardian UI Adapter\n+# SADR-0014: Use Codex App As The External AI Guardian UI Adapter\n \n ## Status\n \ndiff --git a/docs/adr/0015-contract-approval-before-dispatch.md b/docs/adr/SADR-0015-contract-approval-before-dispatch.md\nindex fad6c656741a20a7684ba16080a7db159f63a52f..6061271a46d3f1d6ae7174477de3fb75673123a6\n--- a/docs/adr/0015-contract-approval-before-dispatch.md\n+++ b/docs/adr/SADR-0015-contract-approval-before-dispatch.md\n@@ -1,4 +1,4 @@\n-# ADR 0015: Approve The Task Contract Before Dispatch, Not The Diff After It\n+# SADR-0015: Approve The Task Contract Before Dispatch, Not The Diff After It\n \n ## Status\n \n@@ -9,7 +9,7 @@\n Guardian approval for high/critical risk is expressible only on a pull request.\n Every approval source in `guardian-policy.json` \u2014 GitHub review, the\n `guardian:approved` label, the `Guardian approval granted` comment, and the\n-`external_ai_guardian_review` artifact (ADR 0010/0014) \u2014 is bound to a PR, and\n+`external_ai_guardian_review` artifact (SADR-0010/0014) \u2014 is bound to a PR, and\n three of the four require the current head SHA. Approval therefore cannot exist\n before an implementation PR exists.\n \n@@ -81,7 +81,7 @@\n An envelope breach must surface only on the non-repairable policy gate, never on\n the repairable `MergeGate metadata check`. Otherwise the Goal Loop routes it to\n `dispatch_repair` \u2014 instructing a runner to make the breach go away \u2014 which is\n-exactly what this ADR forbids.\n+exactly what this SADR forbids.\n \n **Contract immutability.** After registration, an implementation PR must not\n change the governance fields of its own task contract (`scope`, `non_goals`,\n@@ -116,7 +116,7 @@\n fails any verdict that is not `complete`, and the signal the CCA prompt reads is\n computed from `evaluate_guardian_approval` alone. Without the OR-merge at the\n signal, a contract-approved PR still returns `needs_guardian`, the CCA job fails,\n-and the MergeGate policy check never runs \u2014 the gate this ADR changes would never\n+and the MergeGate policy check never runs \u2014 the gate this SADR changes would never\n be reached. The CCA *prompt* needs no change: a `contract_approval` entry in the\n signal's existing `sources` array satisfies its current wording.\n \n@@ -130,7 +130,7 @@\n written, and an approved contract lets its implementation PRs merge through the\n same automatic path as low/medium risk. The wasted implement-then-block cycle\n disappears. Guardian review shifts from reading diffs to reading contracts, which\n-is both earlier and smaller; the external AI Guardian path (ADR 0010/0014) works\n+is both earlier and smaller; the external AI Guardian path (SADR-0010/0014) works\n on contracts with no change.\n \n We accept that the Guardian authorizes a class of change without seeing its\n@@ -146,7 +146,7 @@\n adversarial tests for the new approval path equivalent in rigor to\n `test_shiki_governance_evidence.sh`.\n \n-Bootstrap costs, which apply only while this ADR is being implemented. The tasks\n+Bootstrap costs, which apply only while this SADR is being implemented. The tasks\n that build Contract Approval merge under the existing rules, so the ones\n dispatched after the pre-dispatch gate lands but before the approval path lands\n need a Guardian-approved Contract PR *and* a live Guardian approval on their\n@@ -210,7 +210,7 @@\n risk; they do not bound authorized work.\n \n **Auto-escalate risk on envelope breach instead of stopping.** Returns the\n-workflow to a diff-time Guardian decision \u2014 the exact behavior this ADR removes.\n+workflow to a diff-time Guardian decision \u2014 the exact behavior this SADR removes.\n \n **Resolve governance fields from the base snapshot only.** Rejected against the\n registration flow: implementation PRs add their own contract, so the base has no\ndiff --git a/docs/adr/0016-locks-bound-product-paths.md b/docs/adr/SADR-0016-locks-bound-product-paths.md\nindex b5e850f3f1f7f4ef6e7f9d1763a29a51bd300c45..46b40a9b99a7aae644c02bb458f5473ff0a90ce2\n--- a/docs/adr/0016-locks-bound-product-paths.md\n+++ b/docs/adr/SADR-0016-locks-bound-product-paths.md\n@@ -1,4 +1,4 @@\n-# ADR 0016: Locks bound product paths; the Shiki mirror is bound by record identity\n+# SADR-0016: Locks bound product paths; the Shiki mirror is bound by record identity\n \n ## Status\n \n@@ -6,8 +6,8 @@\n \n - Date: 2026-07-28\n - Deciders: operator (mizutani-140), Claude (planner)\n-- Related: ADR 0004 (GitHub-first with a `.shiki` mirror), ADR 0009 (Spec Freeze),\n-  ADR 0012 (autonomous loop closeout to main), ADR 0015 (Contract Approval before\n+- Related: SADR-0004 (GitHub-first with a `.shiki` mirror), SADR-0009 (Spec Freeze),\n+  SADR-0012 (autonomous loop closeout to main), SADR-0015 (Contract Approval before\n   dispatch), remediation goal task R-02, the live PR #179 deadlock.\n \n ## Context\n@@ -90,11 +90,11 @@\n files, documentation files and named source modules are nameable; runtime-\n allocated ledger, runner and report ids are not \u2014 and those live in `.shiki`,\n which locks no longer govern. Existing frozen contracts are brought into line by\n-a one-time Spec Amendment per plan, which must land before ADR 0015's contract\n+a one-time Spec Amendment per plan, which must land before SADR-0015's contract\n immutability makes `locks` unwritable from the implementation PR.\n \n **Lock state is resolved from the durable side.** MergeGate resolves the lock\n-registry, and ADR 0015's governance fields, from the base `.shiki` snapshot when\n+registry, and SADR-0015's governance fields, from the base `.shiki` snapshot when\n present, falling back to head only when absent. The `MergeGate metadata check`\n job is given `--base-shiki`, which it does not receive today. Locks are not\n derived from the live open-pull-request set: a pull request body is untrusted\n@@ -108,7 +108,7 @@\n Explicitly out of scope: moving MergeGate's contract resolution off the PR\n checkout wholesale; making the mirror a projection of GitHub state; introducing\n new lock kinds; moving ledger evidence out of the repository; and unwinding\n-ADR 0012's closeout pull request. Registration of multi-task goals is ADR 0015's\n+SADR-0012's closeout pull request. Registration of multi-task goals is SADR-0015's\n Contract PR and is not re-decided here.\n \n ## Consequences\n@@ -127,7 +127,7 @@\n \n Risks accepted. First, goal-scoped identity is only as strong as `goal_id`,\n which is resolved from the PR body and the head task file; the mitigation is to\n-add `goal_id` to ADR 0015's frozen governance field set, and until that lands a\n+add `goal_id` to SADR-0015's frozen governance field set, and until that lands a\n task file can name a foreign goal. Second, base-when-present resolution is\n unavailable on fork pull requests, where the head is contributor-controlled;\n fork pull requests already cannot complete a Shiki merge, and this decision does\n@@ -139,7 +139,7 @@\n path, not by implementation pull requests.\n \n New obligations: one Spec Amendment per frozen plan to rewrite `locks`, ordered\n-before ADR 0015 T1; extension of the adversarial suites in\n+before SADR-0015 T1; extension of the adversarial suites in\n `scripts/test_shiki_adversarial_state.sh` and\n `scripts/test_shiki_governance_evidence.sh` to cover the eleven newly-scoped\n directories; and updating `scripts/test_shiki_loop_lock_guard.sh`,\n@@ -151,32 +151,32 @@\n whether the loop's transitive sync of ledger `evidence` references can be\n deleted once the Contract PR carries its own source plan.\n \n-## Relationship to ADR 0004\n+## Relationship to SADR-0004\n \n-This ADR **clarifies something ADR 0004 left unspecified**. ADR 0004 decides that\n+This SADR **clarifies something SADR-0004 left unspecified**. SADR-0004 decides that\n GitHub is the operational source of truth and that each target repository keeps a\n `.shiki/` mirror. It says nothing about pull requests writing the mirror, does\n not name MergeGate, and does not make the mirror an input to a gate. Nothing in\n-ADR 0004's Decision or Consequences is amended, superseded or weakened: the\n+SADR-0004's Decision or Consequences is amended, superseded or weakened: the\n mirror still exists, still records the same artifacts, and pull request\n-boundaries remain the coordination surface. What this ADR settles is the question\n+boundaries remain the coordination surface. What this SADR settles is the question\n 0004 never reached \u2014 by what authority a pull request may write a mirror record \u2014\n and it answers it the way `.shiki/manifest.json` already declares\n (`pr_mutation: current-task-or-current-goal-only`).\n \n-It **amends ADR 0012**, whose Decision item 5 states that \"Loop-executed tasks\n+It **amends SADR-0012**, whose Decision item 5 states that \"Loop-executed tasks\n declare the `path:.shiki/**` lock, which covers every `.shiki` file the closeout\n stages ... so `files_outside_locks` is satisfied without per-file lock\n bookkeeping.\" That clause is replaced: the closeout's mirror writes are\n-authorized by record identity, not by a declared lock. ADR 0012's substantive\n+authorized by record identity, not by a declared lock. SADR-0012's substantive\n decision \u2014 that completion reaches `main` through a closeout pull request rather\n than local attestation \u2014 is preserved unchanged.\n \n-It is **complementary to ADR 0015**, not a substitute. ADR 0015 owns contract\n+It is **complementary to SADR-0015**, not a substitute. SADR-0015 owns contract\n immutability, the pre-dispatch gate, the Contract PR and the Scope Envelope. This\n-ADR owns what a lock means and who may write the mirror. The one ordering\n+SADR owns what a lock means and who may write the mirror. The one ordering\n constraint between them is that the lock-narrowing Spec Amendment must precede\n-ADR 0015 T1's freezing of the `locks` field.\n+SADR-0015 T1's freezing of the `locks` field.\n \n ## Alternatives Considered\n \n@@ -186,7 +186,7 @@\n the only writer that can register them may run. Migrating the Guardian signal to\n that store makes every registration PR fail closed to `needs_guardian` at every\n risk level, because the task file is by definition absent from the store, which\n-removes the low/medium autonomous path ADR 0008/0009 exists to provide. And it\n+removes the low/medium autonomous path SADR-0008/0009 exists to provide. And it\n leaves schemas, manifest, config, guardian policy, templates, migrations, plans,\n worktrees, runner records, runs and handoffs with no writer at all in a\n repository whose product is `.shiki`. Measured benefit on the frozen backlog:\n@@ -199,14 +199,14 @@\n namespace is not in fact ownership-partitioned \u2014 56 ledger entries carry no\n `task_id`, and one goal DAG is writable by 35 registered tasks. The correct\n expression of its insight is that locks do not govern the mirror at all, which is\n-what this ADR decides.\n+what this SADR decides.\n \n **Make the mirror a projection written only by a registration writer and a\n post-merge projection job.** Rejected. It requires a bypass actor with write\n access to the protected default branch; it makes its own master invariant\n readable from a manifest that pull requests may edit; it deletes both reconcile\n modes and leaves a corrupted mirror with no in-band repair path, contradicting\n-ADR 0004's consequence that mirror conflicts are repaired against GitHub state;\n+SADR-0004's consequence that mirror conflicts are repaired against GitHub state;\n and its content-derived ids are rejected by `validate_shiki`'s currently-green id\n pattern.\n \ndiff --git a/docs/adr/0017-bookkeeping-closeouts-do-not-inherit-risk.md b/docs/adr/SADR-0017-bookkeeping-closeouts-do-not-inherit-risk.md\nindex 554e3a0d526a72ca067108c5464cc68c3edcf174..b9676857a4713850e063af7c39c8e0b01ecb4c66\n--- a/docs/adr/0017-bookkeeping-closeouts-do-not-inherit-risk.md\n+++ b/docs/adr/SADR-0017-bookkeeping-closeouts-do-not-inherit-risk.md\n@@ -1,4 +1,4 @@\n-# ADR 0017: A Bookkeeping Closeout Does Not Inherit Its Task's Risk\n+# SADR-0017: A Bookkeeping Closeout Does Not Inherit Its Task's Risk\n \n ## Status\n \n@@ -6,7 +6,7 @@\n \n ## Context\n \n-ADR 0012 made the goal loop push a task's completion to the default branch through a\n+SADR-0012 made the goal loop push a task's completion to the default branch through a\n closeout pull request: after the implementation PR merges, the loop opens a second PR\n carrying `task.status = done`, `lock.state = released`, and \u2014 when the task completes\n its goal \u2014 `goal.status = complete` plus one scorecard report. Nothing else. The\n@@ -49,14 +49,14 @@\n 4. The task file's governance fields (`scope`, `non_goals`, `required_skills`,\n    `risk_level`, `locks`, `acceptance_checks`, `test_command`) are byte-identical to\n    the base snapshot. Only `status`, `expected_pr`, `closeout_pr`, `ledger_evidence`\n-   and `expected_branch` may differ \u2014 the ADR 0015 contract-immutability rule,\n+   and `expected_branch` may differ \u2014 the SADR-0015 contract-immutability rule,\n    unchanged.\n \n    `expected_branch` is in that set because a closeout is *by construction* on a\n    different branch from the implementation it closes, and the loop rewrites the\n    field accordingly. It is safe to let it differ because MergeGate independently\n    requires `expected_branch` to equal the PR's actual head ref, so a PR cannot set\n-   it to anything other than the branch it is really on. The first draft of this ADR\n+   it to anything other than the branch it is really on. The first draft of this SADR\n    omitted it, which made the exemption unreachable: every real closeout failed\n    condition 4 on a field the loop always changes. See PR #209.\n 5. The state transitions are exactly the terminal set: task `review` \u2192 `done`, lock\n@@ -113,12 +113,12 @@\n classification must be derived, not asserted.\n \n **Fold the closeout into the implementation PR** so no second PR exists. Rejected under\n-ADR 0012, which recorded why: a task file that is already `done` on the implementation\n+SADR-0012, which recorded why: a task file that is already `done` on the implementation\n branch short-circuits the loop's review \u2192 CCA \u2192 merge \u2192 done ordering into a local\n goal-completion before any merge happens.\n \n **Have the loop auto-merge closeouts with elevated credentials.** Rejected outright: that\n-is a runtime authoring its own approval, which ADR 0010 and the constitution's\n+is a runtime authoring its own approval, which SADR-0010 and the constitution's\n approval-record prohibition forbid. It would also be indistinguishable, in the evidence,\n from the forgery those rules exist to prevent.\n \ndiff --git a/docs/adr/0018-carried-guardian-approval-across-a-proven-base-sync.md b/docs/adr/SADR-0018-carried-guardian-approval-across-a-proven-base-sync.md\nindex 22358562da390ed8b45cd600da08a43d378b4680..e6e2907cefa776460af73ddf7d39db605f3c39b0\n--- a/docs/adr/0018-carried-guardian-approval-across-a-proven-base-sync.md\n+++ b/docs/adr/SADR-0018-carried-guardian-approval-across-a-proven-base-sync.md\n@@ -1,4 +1,4 @@\n-# ADR 0018: Carried Guardian Approval Across A Proven Base Sync\n+# SADR-0018: Carried Guardian Approval Across A Proven Base Sync\n \n ## Status\n \n@@ -34,7 +34,7 @@\n Guardian evaluator that consumes it (`shiki_guardian.evaluate_guardian_approval`,\n the `guardian_comment_carried` source) were built and tested in isolation. Neither\n does anything until a caller passes the flags that turn the carry on, so the\n-feature was invisible in CI. This ADR records the decision the two of them\n+feature was invisible in CI. This SADR records the decision the two of them\n implement, and this task wires it into the four CI invocations that judge Guardian\n approval.\n \ndiff --git a/docs/adr/README.md b/docs/adr/README.md\nindex c70e057a4520c36e042ab68c53cd4a21ef709b71..5da43856836e2ce6a150f8127766e2f88355ab4b\n--- a/docs/adr/README.md\n+++ b/docs/adr/README.md\n@@ -1,40 +1,41 @@\n-# Architecture Decision Records\n+# Decision Record Namespaces\n \n-This directory holds Shiki's Architecture Decision Records (ADRs): durable\n-records of hard-to-reverse platform decisions. ADRs are part of the Shiki source\n-of truth alongside GitHub state, the `.shiki/` mirror, and `CONTEXT.md`.\n+This directory holds two independent decision namespaces:\n \n-## How ADRs Work\n+- Shiki Architecture Decision Records (SADRs), named\n+  `SADR-NNNN-<short-slug>.md`, record hard-to-reverse Shiki platform decisions.\n+- Target Architecture Decision Records (ADRs), named\n+  `NNNN-<short-slug>.md`, belong to the target product.\n \n-- New ADRs are added as `docs/adr/NNNN-<short-slug>.md`, where `NNNN` is the next\n-  zero-padded sequence number.\n-- The **canonical list of ADRs is the set of files matching `docs/adr/*.md`** in\n-  this directory. Browse `docs/adr/*.md` for the authoritative, current set.\n-- Use [`template.md`](template.md) as the starting point for a new ADR.\n-- Record an ADR when a decision is hard to reverse, surprising without context,\n-  and a real tradeoff. Do not write an ADR for routine or easily reversible\n-  choices.\n+The same four-digit number may appear once in each namespace. A platform\n+`SADR-NNNN` citation resolves only to `SADR-NNNN-*.md`; a target `ADR NNNN`\n+citation resolves only to `NNNN-*.md`.\n \n-Using the `docs/adr/*.md` glob as the source of truth means other PRs can add an\n-ADR without editing this index file, avoiding merge contention on a\n-hand-maintained enumeration.\n+## How Decision Records Work\n \n-## Current ADRs (snapshot)\n-\n-The list below is a convenience snapshot and may lag behind the directory. When\n-in doubt, list `docs/adr/*.md`.\n+- New Shiki platform decisions use the next zero-padded SADR number.\n+- New target product decisions use the target's own ADR sequence.\n+- The canonical list is the files in `docs/adr/`; no hand-maintained index is\n+  authoritative.\n+- Use [`template.md`](template.md) as the starting point and choose the filename\n+  namespace that owns the decision.\n+- Record a decision when it is hard to reverse, surprising without context, and\n+  a real tradeoff. Do not write a decision record for routine or easily\n+  reversible choices.\n \n-- [0001 \u2014 Keep Shiki As The Platform Name](0001-keep-shiki-as-platform-name.md)\n-- [0002 \u2014 Use AGENTS As The Shared Agent Constitution](0002-use-agents-as-shared-constitution.md)\n-- [0003 \u2014 Require Skill Gate For MergeGate Readiness](0003-require-skill-gate-for-mergegate-readiness.md)\n-- [0004 \u2014 Use GitHub First With A Repository-Local Shiki Mirror](0004-use-github-first-with-shiki-mirror.md)\n-- [0005 \u2014 Use Codex As Front And Claude Code Action As The GitHub Runtime](0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md)\n-- [0006 \u2014 Separate Codex Implementation, CCA Completion, And MergeGate Authorization](0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md)\n+Using the directory as the source of truth lets independent changes add records\n+without contending on an enumerated index.\n \n-## Adding A New ADR\n+## Adding A Shiki SADR\n \n-1. Copy `template.md` to `docs/adr/NNNN-<short-slug>.md`.\n+1. Copy `template.md` to `docs/adr/SADR-NNNN-<short-slug>.md`.\n 2. Fill in the title, status, context, decision, and consequences.\n-3. Set the status to `Proposed`, then `Accepted` once the decision is approved.\n-4. Open a PR following `CONTRIBUTING.md`. You do not need to edit this index for\n-   the ADR to be discoverable via `docs/adr/*.md`.\n+3. Set the status to `Proposed`, then `Accepted` once the proper authority has\n+   approved the decision.\n+4. Open a PR following `CONTRIBUTING.md`.\n+\n+## Adding A Target ADR\n+\n+1. Copy `template.md` to `docs/adr/NNNN-<short-slug>.md` using the target's own\n+   sequence.\n+2. Cite it as `ADR NNNN`; do not prefix its filename with `SADR-`.\ndiff --git a/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md b/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\nnew file mode 100644\nindex 0000000000000000000000000000000000000000..7455ce1f3bed02ddddb851ac21a1d1a648c9aeba\n--- /dev/null\n+++ b/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\n@@ -0,0 +1,55 @@\n+# SADR-0019: Separate Shiki Platform SADRs From Target ADRs\n+\n+## Status\n+\n+Accepted\n+\n+## Context\n+\n+Shiki installs its platform decision records into `docs/adr/` in every target\n+repository. Shiki and its targets previously used the same numeric\n+`NNNN-<slug>.md` namespace. A target that already owned ADRs 0013 through 0018\n+therefore acquired two records for each number during installation, making an\n+`ADR NNNN` citation ambiguous. Renumbering target decisions would rewrite target\n+history, while renumbering Shiki decisions per target would destroy their stable\n+platform identity.\n+\n+Upgrades also need to remove the former Shiki-owned numeric paths without ever\n+mistaking a target-owned ADR for platform content. The install stamp already\n+records exact shipped paths and content digests, so it can prove ownership only\n+when both still match.\n+\n+## Decision\n+\n+Shiki platform decisions use the `SADR` namespace:\n+\n+- records are named `docs/adr/SADR-NNNN-<slug>.md`;\n+- live platform citations use `SADR-NNNN`;\n+- target product decisions retain `docs/adr/NNNN-<slug>.md` and `ADR NNNN`;\n+- the reference validator resolves the two namespaces independently, so the same\n+  four-digit number may exist once in each namespace.\n+\n+Before a forced upgrade writes anything, the installer inventories the exact\n+former numeric Shiki ADR paths. It may delete a legacy path only when the\n+existing install stamp contains that exact path and its recorded SHA-256 digest\n+matches the file currently on disk. All ownership blockers are reported\n+together and abort the upgrade before its first write. A non-forced install\n+never performs this cleanup.\n+\n+Historical `.shiki/` evidence remains unchanged because it records the namespace\n+that existed when that evidence was produced.\n+\n+## Consequences\n+\n+- Targets may use any ADR number without colliding with Shiki's platform SADRs.\n+- Fresh installs preserve target ADR bytes and install a stable SADR set.\n+- Forced upgrades fail closed when legacy ownership cannot be proven, and a\n+  successful upgrade refreshes the stamp without removed legacy paths.\n+- Current governance, packaging, workflow, prompt, script, and skill references\n+  must move with the platform records.\n+\n+## Alternatives Considered\n+\n+- Collision-only refusal leaves affected targets permanently unupgradable.\n+- Renumbering target ADRs mutates target-owned history.\n+- Per-target Shiki renumbering makes platform citations non-portable.\ndiff --git a/docs/adr/template.md b/docs/adr/template.md\nindex a92a97daddd0b00ab9dd626cc8d86942da2eafed..17dd79175b5fee2105935557f691b40cf9492942\n--- a/docs/adr/template.md\n+++ b/docs/adr/template.md\n@@ -1,14 +1,14 @@\n-# NNNN: <Short Decision Title>\n+# <SADR-NNNN or NNNN>: <Short Decision Title>\n \n ## Status\n \n-Proposed | Accepted | Superseded by ADR-NNNN\n+Proposed | Accepted | Superseded by SADR-NNNN or target ADR NNNN\n \n ## Context\n \n What is the situation, constraint, or force that requires a decision? Reference\n-relevant domain terms (`CONTEXT.md`), prior ADRs, and code reality. State the\n-problem, not the solution.\n+relevant domain terms (`CONTEXT.md`), prior Shiki SADRs or target ADRs, and code\n+reality. State the problem, not the solution.\n \n ## Decision\n \ndiff --git a/docs/agents/autonomous-loop-e2e-acceptance.md b/docs/agents/autonomous-loop-e2e-acceptance.md\nindex bbf84c6081987f159ac6ad9ecf3c82b7aa72e7e8..5c13c1d02a05a90b0dc46cbef7c95a9846d0c237\n--- a/docs/agents/autonomous-loop-e2e-acceptance.md\n+++ b/docs/agents/autonomous-loop-e2e-acceptance.md\n@@ -40,8 +40,8 @@\n 3. `gh auth status` is logged in with merge permission on the repo.\n 4. Branch protection + required checks are configured exactly as the goal-loop\n    required-check set expects (see `docs/agents/branch-protection-smoke.md`).\n-5. The repo `.shiki/policy` permits autonomous low/medium-risk merges (ADR\n-   0008/0009); the MergeGate policy check is the recorded-authority gate.\n+5. The repo `.shiki/policy` permits autonomous low/medium-risk merges\n+   (SADR-0008/0009); the MergeGate policy check is the recorded-authority gate.\n \n The operator's only actions are: register the task and start the loop. **No\n manual pre-clearing** means after `shiki loop run` starts, the operator does not\ndiff --git a/docs/agents/bootstrap-command.md b/docs/agents/bootstrap-command.md\nindex 05f7f203a52cf412b9f97084a6e131ed0f785afd..f7aa0b734dfebee4a3b139335b66ee3415f48713\n--- a/docs/agents/bootstrap-command.md\n+++ b/docs/agents/bootstrap-command.md\n@@ -189,7 +189,9 @@\n ```\n \n Use this only for tests, fixtures, or explicit local-only template inspection.\n-Use `--force` only when you intentionally want to overwrite existing target files.\n+Use `--force` only when you intentionally want to refresh the target template.\n+Legacy numeric Shiki decision cleanup is stamp-and-digest authorized before any\n+write; target ADR paths are not cleanup candidates.\n \n ## Slash Command\n \ndiff --git a/docs/agents/checklists.md b/docs/agents/checklists.md\nindex 4a7f03ade346f53a1c1d82981f3fd744f9ab4547..0c5ec6c53dc7197b7b7a7d18e667b325babd6c45\n--- a/docs/agents/checklists.md\n+++ b/docs/agents/checklists.md\n@@ -32,18 +32,18 @@\n | ID | Item | Blocking |\n | --- | --- | --- |\n | GD-01 | Plan was challenged against `CONTEXT.md`. | blocking for non-trivial Goals |\n-| GD-02 | Relevant ADRs were read or absence was recorded. | blocking for non-trivial Goals |\n+| GD-02 | Relevant SADRs and target ADRs were read or absence was recorded. | blocking for non-trivial Goals |\n | GD-03 | Ambiguous terms were resolved or converted to HITL blockers. | blocking |\n | GD-04 | Edge scenarios were considered. | advisory |\n | GD-05 | Code reality was checked when code could answer. | blocking when applicable |\n | GD-06 | New domain terms were added to `CONTEXT.md` when needed. | blocking when applicable |\n-| GD-07 | ADR was created or explicitly skipped using the ADR criteria. | blocking when applicable |\n+| GD-07 | A SADR or target ADR was created or explicitly skipped using the decision-record criteria. | blocking when applicable |\n \n ## CI \u2014 Context & Impact Checklist\n \n | ID | Item | Blocking |\n | --- | --- | --- |\n-| CI-01 | Relevant docs and ADRs identified. | blocking |\n+| CI-01 | Relevant docs, SADRs, and target ADRs identified. | blocking |\n | CI-02 | Relevant modules, interfaces, seams, callers, and tests identified. | blocking |\n | CI-03 | Dependencies and blocked-by relationships identified. | blocking |\n | CI-04 | Candidate locks identified. | blocking |\n@@ -62,7 +62,7 @@\n | PRD-04 | Implementation decisions are recorded without volatile code detail. | blocking |\n | PRD-05 | Testing decisions are recorded. | blocking |\n | PRD-06 | Out-of-scope items are explicit. | blocking |\n-| PRD-07 | Relevant glossary/ADR links are present when applicable. | blocking |\n+| PRD-07 | Relevant glossary/SADR/target-ADR links are present when applicable. | blocking |\n \n ## SF \u2014 Spec Freeze Checklist\n \n@@ -98,7 +98,7 @@\n | H-02 | Branch/worktree target is present. | blocking |\n | H-03 | Scope and non-goals are present. | blocking |\n | H-04 | Dependencies and locks are present. | blocking |\n-| H-05 | Relevant docs, ADRs, modules, and tests are referenced. | blocking |\n+| H-05 | Relevant docs, SADRs, target ADRs, modules, and tests are referenced. | blocking |\n | H-06 | Required skills are stated. | blocking |\n | H-07 | TDD expectations are stated. | blocking when behavior work |\n | H-08 | Acceptance criteria are included. | blocking |\ndiff --git a/docs/agents/codex-handoff.md b/docs/agents/codex-handoff.md\nindex ede468f47cb277f69a857e8c45df703e4ed0151d..6f97bdffb67e44d7edea2c71e01ea28933c4eb6c\n--- a/docs/agents/codex-handoff.md\n+++ b/docs/agents/codex-handoff.md\n@@ -46,7 +46,7 @@\n \n ## Context\n - Domain terms:\n-- Relevant ADRs:\n+- Relevant SADRs and target ADRs:\n - Relevant docs:\n - Relevant modules/interfaces/seams:\n - Similar tests/prior art:\ndiff --git a/docs/agents/completion-check-agent.md b/docs/agents/completion-check-agent.md\nindex 13e4c035db35025f4e3cdcc5adfb24314ac4d521..61bfa1218cf36ca605250c91919e620f33238aa0\n--- a/docs/agents/completion-check-agent.md\n+++ b/docs/agents/completion-check-agent.md\n@@ -32,7 +32,7 @@\n - `AGENTS.md`\n - `CLAUDE.md`\n - `CONTEXT.md`\n-- relevant ADRs\n+- relevant SADRs and target ADRs\n - parent Goal issue\n - PRD issue or document\n - task issue\ndiff --git a/docs/agents/decision-control.md b/docs/agents/decision-control.md\nindex 7d8ee5ab03a5ccbcbd04056ae61de03785a2a9c9..8c94ecda00fffaa0e92c325ab83aa9776bd1dac1\n--- a/docs/agents/decision-control.md\n+++ b/docs/agents/decision-control.md\n@@ -11,7 +11,7 @@\n | Decision | Owner | When | Required inputs | Output | Enforcement surface |\n | --- | --- | --- | --- | --- | --- |\n | Is the Goal clear enough to plan? | Planner + Guardian when needed | Goal intake | User request, existing docs, risk signals | Goal readiness verdict | Goal checklist, issue template, ledger |\n-| Must `grill-with-docs` run? | Planner / Skill Gate | Before PRD or issue split | Ambiguity, domain terms, ADR conflicts, unclear boundaries | Skill invocation or explicit skip rationale | Skill Gate checklist |\n+| Must `grill-with-docs` run? | Planner / Skill Gate | Before PRD or issue split | Ambiguity, domain terms, SADR/target-ADR conflicts, unclear boundaries | Skill invocation or explicit skip rationale | Skill Gate checklist |\n | Is a PRD required? | Planner | After Goal context stabilizes | Goal, `grill-with-docs` output, Context & Impact output | PRD required / not required | PRD checklist, CCA evidence check |\n | Can the spec be frozen? | Operator only | End of Requirements Definition, before issue split | PRD/requirements summary, scope inventory (SF-02), open-question list | `spec_freeze` block in the plan + ledger evidence | `require_grilled_plan` (plan ingest/run), SF checklist |\n | Can a frozen spec change? | Operator only (Spec Amendment) | When implementation contradicts the frozen spec | Contested decisions, scoped re-grill result | recorded amendment + re-stamped freeze | SF checklist, ledger evidence |\ndiff --git a/docs/agents/domain.md b/docs/agents/domain.md\nindex 258da8ea81e87ac0a6b0b5b5e5c2fde980eaabb7..d57b0cf17ff3e1fa9e6e5885372bf083d8295421\n--- a/docs/agents/domain.md\n+++ b/docs/agents/domain.md\n@@ -5,7 +5,9 @@\n ## Layout\n \n - `CONTEXT.md` defines Shiki language. Keep it implementation-free.\n-- `docs/adr/` records hard-to-reverse decisions.\n+- `docs/adr/SADR-NNNN-*.md` records Shiki platform decisions.\n+- `docs/adr/NNNN-*.md` records target product decisions without sharing the\n+  SADR namespace.\n - `AGENTS.md` is the runtime-neutral agent constitution.\n - `CLAUDE.md` is the Claude Code wrapper around `AGENTS.md`.\n - `.shiki/` is the Target Repository mirror for Goals, PRDs, plans, Task DAGs, contracts, locks, worktree records, ledger entries, CCA verdicts, repair packets, reports, and handoffs.\n@@ -15,6 +17,6 @@\n \n - Use Shiki terms exactly when they exist.\n - Add glossary terms to `CONTEXT.md` only when the term is part of the domain, not a generic programming concept.\n-- Create ADRs only for hard-to-reverse, surprising, tradeoff-based decisions.\n+- Create SADRs or target ADRs only for hard-to-reverse, surprising, tradeoff-based decisions.\n - Keep product-specific language in the Target Repository, not in Shiki Template docs.\n-- If an ADR conflicts with a proposed implementation, surface the conflict before editing.\n+- If a SADR or target ADR conflicts with a proposed implementation, surface the conflict before editing.\ndiff --git a/docs/agents/execution-control.md b/docs/agents/execution-control.md\nindex ad8ee3cdb36be56fedc1f46408ca92b387d74047..bc63c3a73661d12b567016ba299f1a6b66bf9088\n--- a/docs/agents/execution-control.md\n+++ b/docs/agents/execution-control.md\n@@ -63,7 +63,7 @@\n | Branch name | Orchestrator | Orchestrator | Before implementer dispatch | branch policy | branch ref in task/ledger |\n | Lock reservation | Context & Impact | MergeGate | Before dispatch and before merge | `.shiki/locks/`, labels, PR metadata | lock id, owner, scope |\n | CI profile | Planner | MergeGate | During issue triage, rechecked at PR | labels, `.shiki/policy.yaml`, workflow matrix | required check list |\n-| Required status checks | Repo maintainer / Guardian | GitHub branch protection / rulesets | Repository setup, updated via ADR | GitHub settings | branch/ruleset config, ADR |\n+| Required status checks | Repo maintainer / Guardian | GitHub branch protection / rulesets | Repository setup, updated via target ADR | GitHub settings | branch/ruleset config, target ADR |\n | CCA judgment | CCA | GitHub required check / MergeGate | Every non-draft PR update | GitHub Action check | structured CCA verdict |\n | Repair assignment | CCA | MergeGate | After failed CCA/CI/review | repair packet, issue/PR comment | repair id and attempt count |\n | Merge readiness | MergeGate | GitHub branch protection / Guardian if needed | After CCA complete | required checks, review rules, rulesets | mergegate check output |\ndiff --git a/docs/agents/guardian-policy.md b/docs/agents/guardian-policy.md\nindex 4147c7adf05a47adb4fd24ada1a1c2e1f2ba6d2d..a20d9cc6c00a569bb057b9ca8d6b51c7fc4a9847\n--- a/docs/agents/guardian-policy.md\n+++ b/docs/agents/guardian-policy.md\n@@ -98,8 +98,8 @@\n `origin/<default_branch>` and reuses each job's existing base fetch; a shallow\n (`--depth`) or second base fetch would make the proof unresolvable and silently\n disable the carry, so neither is permitted. CCA reads `guardian_comment_carried` as\n-a recorded authority in the deterministic signal and does not re-derive it. See ADR\n-0018 for the full decision, the rejected diff/patch-equality and path-disjointness\n+a recorded authority in the deterministic signal and does not re-derive it. See\n+SADR-0018 for the full decision, the rejected diff/patch-equality and path-disjointness\n alternatives, and the accepted residual.\n \n ## External AI Guardian Review (SADR-0010)\ndiff --git a/docs/agents/implementation-policy.md b/docs/agents/implementation-policy.md\nindex c754144a9fb5bf4a38aa82a60e1670c59e626b44..85c37281bcad196c6b7b02343faf00b03cd9c57a\n--- a/docs/agents/implementation-policy.md\n+++ b/docs/agents/implementation-policy.md\n@@ -31,10 +31,13 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n ## Non-Negotiables\n \n 1. **Planning precedes implementation.** Non-trivial Goals start with `grill-with-docs` and Context & Impact.\n-2. **Domain language is controlled.** Terms must be checked against `CONTEXT.md`; hard-to-reverse tradeoffs must be checked against ADRs.\n+2. **Domain language is controlled.** Terms must be checked against `CONTEXT.md`; hard-to-reverse tradeoffs must be checked against Shiki SADRs and target ADRs.\n 3. **PRD precedes issue decomposition.** `to-prd` is used once the context is settled enough to become durable intent.\n 4. **Issues are vertical slices.** `to-issues` must produce independently grabbable tracer-bullet tasks, not horizontal layer tickets.\n 5. **Triage controls dispatch.** Only `ready-for-agent` / AFK tasks may be assigned to Codex without human input.\n@@ -95,19 +98,19 @@\n Process:\n \n - Challenge terms against `CONTEXT.md`.\n-- Check ADRs before reopening decisions.\n+- Check SADRs and target ADRs before reopening decisions.\n - Inspect code when code can answer a question.\n - Ask one question at a time when the operator is available.\n - Provide a recommended answer with each question.\n - Record resolved terms in `CONTEXT.md`.\n-- Create ADRs only for hard-to-reverse, surprising, tradeoff-based decisions.\n+- Create SADRs or target ADRs only for hard-to-reverse, surprising, tradeoff-based decisions.\n - Convert unresolved load-bearing questions into HITL blockers.\n \n Outputs:\n \n - Resolved design-tree decisions.\n - Updated glossary terms when needed.\n-- ADRs when justified.\n+- SADRs or target ADRs when justified.\n - HITL blockers when decisions remain unresolved.\n \n ## Phase 3 \u2014 Context & Impact\n@@ -117,7 +120,7 @@\n \n Outputs must include:\n \n-- Relevant documents and ADRs.\n+- Relevant documents, SADRs, and target ADRs.\n - Relevant modules, interfaces, seams, callers, and tests.\n - Candidate locks.\n - Dependencies.\n@@ -140,7 +143,7 @@\n - Testing decisions.\n - Out of scope.\n - Further notes.\n-- Links to relevant Goal, domain terms, and ADRs.\n+- Links to relevant Goal, domain terms, SADRs, and target ADRs.\n \n Phase 4 ends with Spec Freeze: the operator explicitly approves the PRD, the\n plan records a `spec_freeze` block (status, approved_by, source), and the SF\n@@ -240,7 +243,7 @@\n - Dependencies and locks.\n - Risk and Guardian requirements.\n - Ledger completeness.\n-- Documentation and ADR updates.\n+- Documentation and SADR/target-ADR updates.\n - Scope drift.\n \n CCA emits one of:\n@@ -301,7 +304,7 @@\n | Evidence failure | Behavior might be done, but proof is missing. | Add checks, PR evidence, ledger entry, or artifacts. |\n | Scope drift | PR changes unrelated behavior. | Revert unrelated changes or split into new Goal. |\n | Dependency block | Upstream task or lock not complete. | Block until dependency resolves. |\n-| Architecture block | Decision needs ADR or deep-module discussion. | `grill-with-docs` or `improve-codebase-architecture`. |\n+| Architecture block | Decision needs a SADR, target ADR, or deep-module discussion. | `grill-with-docs` or `improve-codebase-architecture`. |\n | Risk block | Security/production/policy change needs human approval. | Guardian decision. |\n \n ## Phase 5.5 \u2014 Execution Decision Control\ndiff --git a/docs/agents/runtime-auth-model.md b/docs/agents/runtime-auth-model.md\nindex 6a2e1a47e81ea89b5c68a210ec07772a9374babc..3c54748d24233dbd0ba8473950282547ef4ce935\n--- a/docs/agents/runtime-auth-model.md\n+++ b/docs/agents/runtime-auth-model.md\n@@ -148,7 +148,7 @@\n \n API-key based runners may be added by a target repository as an explicit extension. They are not the default Shiki template path.\n \n-If a repo opts into API-key mode, record the exception in an ADR and update the repo's `AGENTS.md`, `.shiki/config.yaml`, and workflow permissions.\n+If a repo opts into API-key mode, record the target-specific exception in a target ADR and update the repo's `AGENTS.md`, `.shiki/config.yaml`, and workflow permissions.\n \n ## GitHub Actions Runtime Compatibility\n \ndiff --git a/docs/agents/skill-gate.md b/docs/agents/skill-gate.md\nindex c50398b0d7c60a8598f2a5c88a10015730dced13..3acffe6444617bb1d54b77d4b4708158ec735bc6\n--- a/docs/agents/skill-gate.md\n+++ b/docs/agents/skill-gate.md\n@@ -9,7 +9,7 @@\n | Skill | Trigger | Expected output |\n | --- | --- | --- |\n | `setup-matt-pocock-skills` | First configuring a repo for these skills, or when issue tracker, triage labels, or domain docs are missing. | `docs/agents/` configuration and an `Agent skills` block in `AGENTS.md` or `CLAUDE.md`. |\n-| `grill-with-docs` | Ambiguous plan, unclear terminology, boundary decision, tradeoff, or ADR-worthy design choice. | Clarified decision tree, recommended answers, updates to `CONTEXT.md` or ADRs when decisions crystallize. |\n+| `grill-with-docs` | Ambiguous plan, unclear terminology, boundary decision, tradeoff, or decision-record-worthy design choice. | Clarified decision tree, recommended answers, updates to `CONTEXT.md`, SADRs, or target ADRs when decisions crystallize. |\n | `zoom-out` | Agent lacks a higher-level architectural map. | Map of relevant modules, callers, seams, and domain concepts. |\n | `to-prd` | Settled context should become a PRD. | PRD published to the issue tracker with problem, solution, user stories, decisions, testing, and out-of-scope notes. |\n | `to-issues` | Goal, PRD, or plan must become implementation tickets. | Vertical-slice issues with acceptance criteria, dependencies, and AFK/HITL classification. |\n@@ -17,7 +17,7 @@\n | `tdd` | Feature work or bug fix has observable behavior that can be specified with tests. | Red-green-refactor loop through public interfaces, one vertical slice at a time. |\n | `code-review` | The TDD loop is green and the implementer is about to create or update a PR. Default for every implementation task. | Pre-PR self-review of the task diff with fixes applied on the task branch; findings and resolutions recorded as a ledger entry naming the skill and a `## Pre-PR code review` PR body section. |\n | `diagnose` | Hard bug, failing check, regression, flaky behavior, or performance problem. | Reproduction loop, ranked hypotheses, instrumentation, fix, regression test, post-mortem. |\n-| `improve-codebase-architecture` | Structural friction, testability, AI-navigability, deep-module opportunity, or refactor request. | Architecture candidates and deepening recommendations informed by `CONTEXT.md` and ADRs. |\n+| `improve-codebase-architecture` | Structural friction, testability, AI-navigability, deep-module opportunity, or refactor request. | Architecture candidates and deepening recommendations informed by `CONTEXT.md`, SADRs, and target ADRs. |\n | `prototype` | A throwaway logic or UI prototype is needed to answer a design question. | Clearly marked disposable prototype and captured decision. |\n \n ## MergeGate Interaction\ndiff --git a/docs/operations.md b/docs/operations.md\nindex cf1f452d4c589e38ccd58553bda0ad8684b51eb7..ace48a90ffbec9e5c0a7113bef16a4858bc44b3f\n--- a/docs/operations.md\n+++ b/docs/operations.md\n@@ -89,6 +89,16 @@\n      target history, brought forward by `shiki migrate`, not a template.\n    - **Everything else** (CLI scripts, workflows, schemas, docs) is overwritten.\n \n+   A forced upgrade also migrates the former numeric Shiki decision paths to\n+   `SADR-NNNN-*`. Before the first write, the installer inventories the exact\n+   legacy Shiki paths and authorizes each deletion only when the existing\n+   `.shiki/install-stamp.json` names that path and its SHA-256 digest still\n+   matches the file. An absent, unreadable, malformed, incomplete, or\n+   mismatching stamp reports every blocked path and stops with no filesystem\n+   change. Target-owned `NNNN-*` ADRs are not cleanup candidates. A non-force\n+   install deletes nothing and retains any still-matching legacy stamp entries\n+   so a later forced upgrade can prove ownership.\n+\n    If the target has pending migrations, `--force` refuses and writes nothing \u2014\n    apply migrations first (step 3), then re-run. The run ends with a single\n    summary of every `.new` written, so a half-upgrade is visible.\ndiff --git a/docs/releasing.md b/docs/releasing.md\nindex 10d73828e7dbd64e9d9f6b83bc84d010537ce62a..aafeee8c4d441e1e427efee2ce4f81a647f020e1\n--- a/docs/releasing.md\n+++ b/docs/releasing.md\n@@ -67,7 +67,7 @@\n CLI in `scripts/`. There are no third-party Python runtime dependencies. The CLI\n is intentionally **not** exposed as a `[project.scripts]` console entry point\n because `scripts/shiki.py` is a dependency-free shim and `scripts/` is not\n-packaged as an importable distribution (see the ADR for the rationale).\n+packaged as an importable distribution (see SADR-0007 for the rationale).\n \n Supported install paths:\n \n@@ -155,6 +155,15 @@\n    [docs/operations.md](operations.md#the-new-convention). If the target has\n    pending migrations, `--force` refuses and writes nothing; apply migrations\n    (step 2) first, then re-run.\n+\n+   The SADR namespace upgrade has an additional pre-write gate: each former\n+   numeric Shiki decision path is removed only when the existing install stamp\n+   contains that exact path and a digest matching its current bytes. The\n+   installer aggregates absent/unreadable/malformed-stamp, missing-digest, and\n+   digest-mismatch blockers before raising, so refusal leaves the target tree\n+   byte-identical. Numeric target ADRs are preserved. A successful forced\n+   upgrade installs and stamps `SADR-*` paths and drops the removed legacy paths\n+   from the refreshed stamp.\n 2. Apply repository-local `.shiki/` state migrations with the migrate command:\n \n    ```bash\ndiff --git a/docs/usage.md b/docs/usage.md\nindex b83407ee3970dd395dd893010bc5dbe8bad26051..a130ef6e40dfd8169a5bc701339351097df1a09f\n--- a/docs/usage.md\n+++ b/docs/usage.md\n@@ -67,8 +67,10 @@\n shiki install-target /path/to/target-repo --local-only\n ```\n \n-Use `--force` only when you intentionally want to overwrite existing target\n-files.\n+Use `--force` only when you intentionally want to refresh the target template.\n+For the SADR namespace migration, `--force` deletes only exact legacy Shiki\n+decision paths whose existing install-stamp digest still matches; any ownership\n+blocker stops before the first write. Numeric target ADRs are never candidates.\n \n ## `shiki bootstrap-platform` \u2014 publish the Shiki platform repo\n \ndiff --git a/scripts/guardian_approval_signal.py b/scripts/guardian_approval_signal.py\nindex d059f92b2c82aa2b5d6ca421365537a87886c1cb..16924892714f4f5b9303c0ef0e325a7e70075630\n--- a/scripts/guardian_approval_signal.py\n+++ b/scripts/guardian_approval_signal.py\n@@ -199,8 +199,8 @@\n ) -> Any:\n     \"\"\"Evaluate SADR-0015 Contract Approval for the CCA Guardian signal.\n \n-    The SAME OR that MergeGate applies must reach this deterministic signal: ADR\n-    0015 is explicit that without it, a contract-approved PR still returns\n+    The SAME OR that MergeGate applies must reach this deterministic signal:\n+    SADR-0015 is explicit that without it, a contract-approved PR still returns\n     ``needs_guardian``, the CCA job fails, and the MergeGate policy check never\n     runs \u2014 the gate this change moves would never be reached. This resolves the\n     task the SAME way MergeGate does (the first Shiki task id in the PR body),\ndiff --git a/scripts/shiki_contract_approval.py b/scripts/shiki_contract_approval.py\nindex 941bcb96fdd8b9a85bc0c4f0f04d5dad9d04839c..4a4b418f6527413161db82670bec857a043360e2\n--- a/scripts/shiki_contract_approval.py\n+++ b/scripts/shiki_contract_approval.py\n@@ -142,7 +142,7 @@\n     changed_files_status: Any,\n     registration: Any,\n ) -> ContractApprovalResult:\n-    \"\"\"Whether this PR carries an SADR-0015 Contract Approval.\n+    \"\"\"Whether this PR carries a SADR-0015 Contract Approval.\n \n     The four conditions are evaluated IN ORDER, each failure recorded as a\n     distinct reason string:\ndiff --git a/scripts/shiki_guardian.py b/scripts/shiki_guardian.py\nindex a4ecb79bbda55fd5c8b06adfae2eb99760a03c45..4dd99bbe714e26f421fa19abac7a34ae1b195ecc\n--- a/scripts/shiki_guardian.py\n+++ b/scripts/shiki_guardian.py\n@@ -12,7 +12,7 @@\n # The carry re-derives its proof from git in-process on every judging invocation;\n # it is never read from an artifact, so there is nothing to go stale or be forged.\n # shiki_sync_proof is the ONLY guardian-family module that runs git, and every one\n-# of its invocations is read-only and local (ADR: prove-from-git base sync).\n+# of its invocations is read-only and local (SADR: prove-from-git base sync).\n from shiki_sync_proof import verify_pure_base_sync\n \n GUARDIAN_POLICY_PATH = \".shiki/guardian-policy.json\"\ndiff --git a/scripts/shiki_guardian_review.py b/scripts/shiki_guardian_review.py\nindex dd0f104d457899e0ba5ff1e162011c1e90b6f92b..e10ef58c046324e7ea3a3392feb3048ca77414ce\n--- a/scripts/shiki_guardian_review.py\n+++ b/scripts/shiki_guardian_review.py\n@@ -159,7 +159,7 @@\n     docs_only = _is_docs_only(changed_files)\n     if docs_only and matched == 0:\n         focus.append(\n-            \"Docs/ADR-only change: confirm the change is genuinely documentation/decision-only, introduces no behavior or contract drift, and stays consistent with the constitution and existing ADRs.\"\n+            \"Docs/decision-record-only change: confirm the change is genuinely documentation/decision-only, introduces no behavior or contract drift, and stays consistent with the constitution and existing SADRs and target ADRs.\"\n         )\n \n     # Conservative augmentation: unknown (no category and not docs-only), mixed\ndiff --git a/scripts/shiki_installer.py b/scripts/shiki_installer.py\nindex be064ab7a29ff6836f7f7a1d70500a43e0d11d7b..9fd8eefd137ce7003dad5c0dcb2e48e2cd47ab32\n--- a/scripts/shiki_installer.py\n+++ b/scripts/shiki_installer.py\n@@ -118,6 +118,7 @@\n # what lets a target carry it without a governance-schema change.\n INSTALL_STAMP_PATH = \".shiki/install-stamp.json\"\n INSTALL_STAMP_VERSION = 1\n+LEGACY_SHIKI_SADR_MAX = 18\n \n # Under --force the shipped surface splits three ways, so nothing is lost and\n # nothing is silently stale.\n@@ -237,9 +238,98 @@\n         )\n \n \n+def legacy_shiki_adr_paths() -> tuple[str, ...]:\n+    \"\"\"Former numeric paths for Shiki SADR-0001 through SADR-0018.\n+\n+    The authoritative slugs come from the current SADR filenames. SADR-0019 is\n+    the namespace decision itself and never shipped under a numeric legacy path.\n+    \"\"\"\n+    paths: list[str] = []\n+    for sadr in sorted((ROOT / \"docs\" / \"adr\").glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\")):\n+        number = sadr.name.split(\"-\", 2)[1]\n+        if int(number) <= LEGACY_SHIKI_SADR_MAX:\n+            paths.append(f\"docs/adr/{sadr.name.removeprefix('SADR-')}\")\n+    return tuple(paths)\n+\n+\n+def preflight_legacy_sadr_cleanup(target: Path, *, force: bool) -> tuple[str, ...]:\n+    \"\"\"Authorize exact legacy Shiki ADR removals before the first write.\n+\n+    A non-force install never removes existing files. A forced install needs no\n+    stamp when no legacy Shiki path is present. When legacy paths do exist, each\n+    must be named in a readable install stamp and its current bytes must match\n+    the recorded digest.\n+    \"\"\"\n+    if not force:\n+        return ()\n+    present = tuple(\n+        relative\n+        for relative in legacy_shiki_adr_paths()\n+        if (target / relative).exists() or (target / relative).is_symlink()\n+    )\n+    if not present:\n+        return ()\n+\n+    stamp_path = target / INSTALL_STAMP_PATH\n+    try:\n+        raw = stamp_path.read_text(encoding=\"utf-8\")\n+        stamp = json.loads(raw)\n+    except FileNotFoundError:\n+        detail = \"install stamp is absent\"\n+        blockers = [f\"{relative}: {detail}\" for relative in present]\n+    except (OSError, UnicodeDecodeError) as error:\n+        detail = f\"install stamp is unreadable ({error})\"\n+        blockers = [f\"{relative}: {detail}\" for relative in present]\n+    except json.JSONDecodeError as error:\n+        detail = f\"install stamp is malformed ({error})\"\n+        blockers = [f\"{relative}: {detail}\" for relative in present]\n+    else:\n+        if not isinstance(stamp, dict):\n+            blockers = [\n+                f\"{relative}: install stamp is malformed (expected an object)\"\n+                for relative in present\n+            ]\n+        else:\n+            digests = stamp.get(\"digests\")\n+            blockers = []\n+            if not isinstance(digests, dict):\n+                blockers.extend(\n+                    f\"{relative}: install stamp has no digest for this path\"\n+                    for relative in present\n+                )\n+            else:\n+                for relative in present:\n+                    expected = digests.get(relative)\n+                    if not isinstance(expected, str) or not expected:\n+                        blockers.append(\n+                            f\"{relative}: install stamp has no digest for this path\"\n+                        )\n+                        continue\n+                    try:\n+                        actual = _sha256_file(target / relative)\n+                    except OSError as error:\n+                        blockers.append(\n+                            f\"{relative}: legacy path is unreadable ({error})\"\n+                        )\n+                        continue\n+                    if actual != expected:\n+                        blockers.append(\n+                            f\"{relative}: digest mismatch (stamp {expected}, current {actual})\"\n+                        )\n+\n+    if blockers:\n+        joined = \"\\n\".join(f\"- {blocker}\" for blocker in blockers)\n+        raise ShikiError(\n+            \"legacy Shiki ADR cleanup is not authorized:\\n\"\n+            f\"{joined}\\nNo files were written.\"\n+        )\n+    return present\n+\n+\n def install_template(target: Path, *, force: bool, validate: bool) -> list[NewFileNote]:\n     # Preconditions first: never rewrite the tree of a target that cannot be\n     # upgraded cleanly (see refuse_pending_migrations).\n+    legacy_cleanup = preflight_legacy_sadr_cleanup(target, force=force)\n     refuse_pending_migrations(target)\n \n     # Resolve the target's required CODEOWNERS owner once from its\n@@ -250,6 +340,10 @@\n     # verbatim. See ``codeowners_required_owner``.\n     codeowners_owner = codeowners_required_owner(target)\n \n+    for relative in legacy_cleanup:\n+        (target / relative).unlink()\n+        info(f\"removed stamped legacy Shiki ADR: {target / relative}\")\n+\n     notes: list[NewFileNote] = []\n     for relative in TEMPLATE_PATHS:\n         source = ROOT / relative\n@@ -647,6 +741,22 @@\n     digests = target_stamp_digests(target)\n     existing = read_install_stamp(target)\n     if existing is not None:\n+        # A non-force install deliberately leaves legacy Shiki ADRs in place.\n+        # Preserve only still-matching ownership proof so a later forced\n+        # upgrade can authorize cleanup; never carry a missing or stale digest.\n+        existing_digests = existing.get(\"digests\")\n+        if isinstance(existing_digests, dict):\n+            for relative in legacy_shiki_adr_paths():\n+                expected = existing_digests.get(relative)\n+                path = target / relative\n+                if not isinstance(expected, str) or not path.is_file():\n+                    continue\n+                try:\n+                    actual = _sha256_file(path)\n+                except OSError:\n+                    continue\n+                if actual == expected:\n+                    digests[relative] = actual\n         if _incoming_is_older(commit, existing.get(\"platform_commit\")):\n             warn(\n                 \"kept existing install stamp: incoming install \"\ndiff --git a/scripts/shiki_loop.py b/scripts/shiki_loop.py\nindex 3611641da8aeb5038b1da90e3114b09a473ec6d8..84caa491b9cad0edc4176f209c13e9221e975681\n--- a/scripts/shiki_loop.py\n+++ b/scripts/shiki_loop.py\n@@ -424,7 +424,7 @@\n         # unresolvable CCA verdict has already produced its terminal stop and is never\n         # laundered into a head-moving sync (SADR-0010). It also never fires while the\n         # policy (Guardian) gate is red \u2014 for ANY reason, not only when it is the sole\n-        # red check \u2014 so a head-bound Guardian approval, or an SADR-0015\n+        # red check \u2014 so a head-bound Guardian approval, or a SADR-0015\n         # contract-approval carry that base movement defeated (the policy gate compares\n         # base and head task governance directly), is never silently restored by\n         # adopting base wholesale. It is bounded by a durable per-task counter so the\ndiff --git a/scripts/shiki_tasks.py b/scripts/shiki_tasks.py\nindex aec3809f24b4a0c8c96fc50971cff9ae9ad30381..d531052e9d9717058d4631483cefab44c26a65c1\n--- a/scripts/shiki_tasks.py\n+++ b/scripts/shiki_tasks.py\n@@ -821,7 +821,7 @@\n         \"prompt\": prompt,\n         \"entry_skill\": \"grill-with-docs\",\n         \"required_next_steps\": [\n-            \"Run grill-with-docs until terminology, boundaries, risks, and ADR-worthy decisions are settled.\",\n+            \"Run grill-with-docs until terminology, boundaries, risks, and decision-record-worthy choices are settled.\",\n             \"Obtain the operator's explicit approval of the PRD/requirements (Spec Freeze).\",\n             \"Write a machine-readable plan JSON with grill_with_docs.status=complete and spec_freeze.status=frozen.\",\n             \"Run shiki plan ingest --plan-file PLAN.json.\",\ndiff --git a/scripts/test_shiki_goal_loop.sh b/scripts/test_shiki_goal_loop.sh\nindex 753c7b516e10f0be3bc92a4bd0eb1fa699776ae6..8f3820a0a079b3044424cfe1a6c6b7fe68896e67\n--- a/scripts/test_shiki_goal_loop.sh\n+++ b/scripts/test_shiki_goal_loop.sh\n@@ -231,8 +231,8 @@\n \n # High/critical risk merges autonomously when all required checks are green:\n # the \"MergeGate policy check\" required check IS the Guardian gate (it enforces\n-# guardian-policy.json \u2014 human approval OR external AI guardian review, ADR\n-# 0010), so green checks mean a recorded authority approved. The fake gh returns\n+# guardian-policy.json \u2014 human approval OR external AI guardian review,\n+# SADR-0010), so green checks mean a recorded authority approved. The fake gh returns\n # green checks, simulating that the Guardian gate passed.\n python3 \"$ROOT/scripts/shiki.py\" goal create --target \"$TARGET\" --title \"Guardian gate\" --outcome \"High risk merges via policy gate\" >/tmp/shiki-goal-loop-guardian-goal.json\n GGOAL=\"$(json_get /tmp/shiki-goal-loop-guardian-goal.json goal_id)\"\ndiff --git a/tests/test_adr_references.py b/tests/test_adr_references.py\nindex 51706329c797d5dd9b47e082a333fa83b261d482..d005da4f771f5e8085ec22d4b787980e68f5ae8c\n--- a/tests/test_adr_references.py\n+++ b/tests/test_adr_references.py\n@@ -1,8 +1,8 @@\n-\"\"\"Every ``ADR ####`` citation must resolve to exactly one decision record.\n+\"\"\"Every target ADR and Shiki SADR citation resolves in its own namespace.\n \n Background. ``scripts/mergegate_check.py`` justifies ``is_bookkeeping_closeout``\n-with \"ADR 0017\" nine times and ``scripts/guardian_approval_signal.py`` cites\n-0016/0017 seven times, yet for a stretch the numbering in ``docs/adr/`` stopped\n+with \"SADR-0017\" nine times and ``scripts/guardian_approval_signal.py`` cites\n+SADR-0016/SADR-0017 seven times, yet for a stretch the numbering in ``docs/adr/`` stopped\n at 0015: the authoring commits lived on a coordinator branch and no task ever\n declared ``docs/adr/...`` in its locks, so nothing carried the records to the\n default branch. The stated basis of an exemption that merged seven PRs with zero\n@@ -11,11 +11,12 @@\n and the directory drifted with no signal.\n \n This is the same unbound-pair shape behind other silent failures: a reference\n-and its target with nothing asserting they agree. This test binds them. It scans\n-every ``scripts/*.py`` (plus ``docs/**/*.md`` and the root ``*.md`` governance\n-files, which are cheap) for the ``ADR ####`` pattern and asserts each cited\n-number resolves to exactly one ``docs/adr/####-*.md``. The converse is\n-deliberately NOT required: an ADR may exist without being cited.\n+and its target with nothing asserting they agree. This test binds them. Target\n+``ADR NNNN`` citations resolve only to ``docs/adr/NNNN-*.md``; Shiki\n+``SADR-NNNN`` citations resolve only to ``docs/adr/SADR-NNNN-*.md``. The scan\n+covers current governance docs, scripts, workflows, prompts, packaging metadata,\n+and repo-local Codex guidance while deliberately excluding historical\n+``.shiki`` evidence. The converse is not required: a record may be uncited.\n \n The suite is dependency free: standard-library ``unittest`` only.\n \"\"\"\n@@ -31,17 +32,18 @@\n \n REPO_ROOT = Path(__file__).resolve().parents[1]\n \n-# \"ADR 0016\" -- a four-digit citation. The trailing \\b keeps a five-digit token\n-# from being read as a truncated four-digit number.\n-_CITATION = re.compile(r\"\\bADR (\\d{4})\\b\")\n-# \"0016-locks-bound-product-paths.md\" -- the leading four digits name the record.\n-_ADR_FILENAME = re.compile(r\"^(\\d{4})-.*\\.md$\")\n+# ``ADR 0016`` / ``SADR-0016`` -- a namespace plus four-digit citation. The\n+# trailing ``\\b`` keeps a five-digit token from being read as a truncated one.\n+_CITATION = re.compile(r\"\\b(S?ADR)[ -](\\d{4})\\b\")\n+# Target ADRs retain the numeric filename; Shiki platform decisions use SADR.\n+_DECISION_FILENAME = re.compile(r\"^(?:(SADR)-)?(\\d{4})-.*\\.md$\")\n \n \n class Violation(NamedTuple):\n     \"\"\"A citation that does not resolve to exactly one decision record.\"\"\"\n \n     citing_file: str  # repo-relative path of the file that cites the ADR\n+    namespace: str  # \"ADR\" for target decisions; \"SADR\" for Shiki decisions\n     number: str  # the cited four-digit number, e.g. \"0016\"\n     line: int  # 1-based line of the citation\n     reason: str  # \"missing\" (zero matches) or \"ambiguous\" (two or more)\n@@ -49,44 +51,59 @@\n \n     @property\n     def expected_path(self) -> str:\n-        return f\"docs/adr/{self.number}-*.md\"\n+        prefix = \"SADR-\" if self.namespace == \"SADR\" else \"\"\n+        return f\"docs/adr/{prefix}{self.number}-*.md\"\n \n     def describe(self) -> str:\n         \"\"\"Name the citing file, the cited number, and the missing path.\"\"\"\n         if self.reason == \"missing\":\n             return (\n-                f\"{self.citing_file}:{self.line} cites ADR {self.number} \"\n+                f\"{self.citing_file}:{self.line} cites {self.namespace} {self.number} \"\n                 f\"but no document matches {self.expected_path}\"\n             )\n         joined = \", \".join(self.matches)\n         return (\n-            f\"{self.citing_file}:{self.line} cites ADR {self.number} which \"\n+            f\"{self.citing_file}:{self.line} cites {self.namespace} {self.number} which \"\n             f\"resolves to {len(self.matches)} documents (expected exactly one \"\n             f\"at {self.expected_path}): {joined}\"\n         )\n \n \n-def _adr_index(root: Path) -> dict[str, list[Path]]:\n-    \"\"\"Map every four-digit ADR number to the record files that claim it.\"\"\"\n-    index: dict[str, list[Path]] = {}\n+def _adr_index(root: Path) -> dict[tuple[str, str], list[Path]]:\n+    \"\"\"Map each decision namespace and number to the files that claim it.\"\"\"\n+    index: dict[tuple[str, str], list[Path]] = {}\n     adr_dir = root / \"docs\" / \"adr\"\n     if adr_dir.is_dir():\n         for path in sorted(adr_dir.glob(\"*.md\")):\n-            match = _ADR_FILENAME.match(path.name)\n+            match = _DECISION_FILENAME.match(path.name)\n             if match:\n-                index.setdefault(match.group(1), []).append(path)\n+                namespace = \"SADR\" if match.group(1) else \"ADR\"\n+                index.setdefault((namespace, match.group(2)), []).append(path)\n     return index\n \n \n def _citing_files(root: Path, include_docs: bool = True) -> list[Path]:\n-    \"\"\"Files scanned for citations: scripts/*.py, and cheaply the markdown.\"\"\"\n+    \"\"\"Current, shipped text surfaces scanned for decision citations.\"\"\"\n     files = sorted((root / \"scripts\").glob(\"*.py\"))\n+    files += sorted((root / \"scripts\").glob(\"*.sh\"))\n     if include_docs:\n         docs = root / \"docs\"\n         if docs.is_dir():\n             files += sorted(docs.rglob(\"*.md\"))\n         files += sorted(root.glob(\"*.md\"))\n-    return files\n+        pyproject = root / \"pyproject.toml\"\n+        if pyproject.is_file():\n+            files.append(pyproject)\n+        for relative in (\".github\", \".codex\"):\n+            surface = root / relative\n+            if surface.is_dir():\n+                files += sorted(\n+                    path\n+                    for path in surface.rglob(\"*\")\n+                    if path.is_file()\n+                    and path.suffix in {\".md\", \".py\", \".sh\", \".toml\", \".yaml\", \".yml\"}\n+                )\n+    return sorted(set(files))\n \n \n def find_adr_reference_violations(\n@@ -94,10 +111,9 @@\n ) -> list[Violation]:\n     \"\"\"Return every ``ADR ####`` citation under ``root`` that fails to resolve.\n \n-    A citation resolves when exactly one ``docs/adr/####-*.md`` matches its\n-    number. Zero matches is a \"missing\" violation; two or more is \"ambiguous\".\n-    Records that are never cited produce nothing -- the converse is not\n-    required.\n+    A citation resolves when exactly one record in the same namespace matches\n+    its number. Zero matches is a \"missing\" violation; two or more is\n+    \"ambiguous\". Records that are never cited produce nothing.\n     \"\"\"\n     root = Path(root)\n     index = _adr_index(root)\n@@ -110,13 +126,15 @@\n         rel = path.relative_to(root).as_posix()\n         for lineno, line in enumerate(text.splitlines(), start=1):\n             for match in _CITATION.finditer(line):\n-                number = match.group(1)\n-                found = index.get(number, [])\n+                namespace, number = match.groups()\n+                found = index.get((namespace, number), [])\n                 if len(found) == 1:\n                     continue\n                 reason = \"missing\" if not found else \"ambiguous\"\n                 rels = [p.relative_to(root).as_posix() for p in found]\n-                violations.append(Violation(rel, number, lineno, reason, rels))\n+                violations.append(\n+                    Violation(rel, namespace, number, lineno, reason, rels)\n+                )\n     return violations\n \n \n@@ -129,8 +147,8 @@\n         self.assertEqual(\n             violations,\n             [],\n-            \"Every 'ADR ####' citation must resolve to exactly one \"\n-            f\"docs/adr/####-*.md:\\n{detail}\",\n+            \"Every ADR/SADR citation must resolve exactly once in its own \"\n+            f\"docs/adr namespace:\\n{detail}\",\n         )\n \n     def test_scan_actually_reads_scripts(self) -> None:\n@@ -142,12 +160,38 @@\n             \"the citation scan must cover scripts/*.py\",\n         )\n \n+    def test_platform_citations_do_not_use_unresolved_numeric_shorthand(self) -> None:\n+        shorthand = re.compile(r\"SADR-\\d{4}/\\d{4}\")\n+        offenders = []\n+        for path in _citing_files(REPO_ROOT):\n+            text = path.read_text(encoding=\"utf-8\")\n+            if shorthand.search(text):\n+                offenders.append(path.relative_to(REPO_ROOT).as_posix())\n+        self.assertEqual(\n+            offenders,\n+            [],\n+            \"repeat the SADR prefix so every cited number is independently resolvable\",\n+        )\n+\n     def test_carried_records_are_indexed(self) -> None:\n         # The two records this Goal carried onto the default branch, and which\n         # the scripts cite most, must be present.\n         index = _adr_index(REPO_ROOT)\n-        self.assertIn(\"0016\", index)\n-        self.assertIn(\"0017\", index)\n+        self.assertIn((\"SADR\", \"0016\"), index)\n+        self.assertIn((\"SADR\", \"0017\"), index)\n+\n+    def test_platform_tree_has_only_sadr_0001_through_0019(self) -> None:\n+        adr_dir = REPO_ROOT / \"docs\" / \"adr\"\n+        sadrs = sorted(adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\"))\n+        self.assertEqual(\n+            {path.name.split(\"-\", 2)[1] for path in sadrs},\n+            {f\"{number:04d}\" for number in range(1, 20)},\n+        )\n+        self.assertEqual(\n+            list(adr_dir.glob(\"[0-9][0-9][0-9][0-9]-*.md\")),\n+            [],\n+            \"the Shiki platform tree must not retain legacy numeric SADR paths\",\n+        )\n \n \n class FixtureAdrReferences(unittest.TestCase):\n@@ -180,6 +224,26 @@\n         self.assertIn(\"9991\", message)  # cited number\n         self.assertIn(\"docs/adr/9991-*.md\", message)  # missing path\n \n+    def test_target_adr_does_not_satisfy_missing_platform_sadr(self) -> None:\n+        root = self._make_repo(\n+            adr_names=[\"0042-target-decision.md\"],\n+            script_body='\"\"\"Target ADR 0042 exists, but platform SADR-0042 does not.\"\"\"\\n',\n+        )\n+        violations = find_adr_reference_violations(root, include_docs=False)\n+        self.assertEqual(len(violations), 1)\n+        self.assertEqual(violations[0].namespace, \"SADR\")\n+        self.assertEqual(violations[0].number, \"0042\")\n+        self.assertEqual(violations[0].expected_path, \"docs/adr/SADR-0042-*.md\")\n+\n+    def test_target_adr_and_platform_sadr_may_reuse_a_number(self) -> None:\n+        root = self._make_repo(\n+            adr_names=[\"0042-target-decision.md\", \"SADR-0042-platform-decision.md\"],\n+            script_body='\"\"\"Target ADR 0042 and platform SADR-0042 are distinct.\"\"\"\\n',\n+        )\n+        self.assertEqual(\n+            find_adr_reference_violations(root, include_docs=False), []\n+        )\n+\n     def test_uncited_adr_is_allowed(self) -> None:\n         # An ADR exists but nothing cites it -> not a violation. The converse\n         # (every ADR must be cited) is explicitly NOT required.\ndiff --git a/tests/test_guardian_review.py b/tests/test_guardian_review.py\nindex 88829e7810212635e2fc98aa5918d6f26294677d..00a85a237acd5bf2d6cb5b294128198c422402ca\n--- a/tests/test_guardian_review.py\n+++ b/tests/test_guardian_review.py\n@@ -162,7 +162,7 @@\n \n     def test_docs_only_is_light_and_not_conservative(self):\n         focus = gr.classify_review_focus([\"docs/adr/0099-x.md\", \"README.md\"], \"\", \"low\")\n-        self.assertTrue(any(\"Docs/ADR-only\" in f for f in focus))\n+        self.assertTrue(any(\"Docs/decision-record-only\" in f for f in focus))\n         self.assertFalse(any(\"conservative default\" in f.lower() for f in focus))\n \n     def test_unknown_category_is_conservative(self):\ndiff --git a/tests/test_policy_job_merged_prs.py b/tests/test_policy_job_merged_prs.py\nindex d45512e74681e5e649279236c7d4e79d5c3da471..b97989f8bd74d091de1a6b5275a20d8dc68c4512\n--- a/tests/test_policy_job_merged_prs.py\n+++ b/tests/test_policy_job_merged_prs.py\n@@ -3,8 +3,8 @@\n \n ``.github/workflows/shiki-cca-completion.yml`` has two consumers of the merged-PR\n proof: the CCA *signal* (``guardian_approval_signal.py`` in \"Prepare CCA context\")\n-and the authoritative gate (``mergegate_check.py`` in \"Enforce MergeGate\"). ADR\n-0017's bookkeeping-closeout exemption (condition 6) needs the merged implementation\n+and the authoritative gate (``mergegate_check.py`` in \"Enforce MergeGate\").\n+SADR-0017's bookkeeping-closeout exemption (condition 6) needs the merged implementation\n PR proven, and a closeout carries NO ``post_merge_reconcile`` marker. The signal\n already resolves ``MERGED_PRS`` unconditionally, but \"Enforce MergeGate\" used to\n resolve it only inside ``grep -q '<!-- shiki:post_merge_reconcile -->'``. So a\ndiff --git a/tests/test_sadr_install_migration.py b/tests/test_sadr_install_migration.py\nnew file mode 100644\nindex 0000000000000000000000000000000000000000..d51eac872b1c79ff4579c4c1401ca5d775a60d8d\n--- /dev/null\n+++ b/tests/test_sadr_install_migration.py\n@@ -0,0 +1,237 @@\n+\"\"\"SADR installation and collision-safe legacy upgrade behavior.\"\"\"\n+\n+from __future__ import annotations\n+\n+import contextlib\n+import hashlib\n+import io\n+import json\n+import shutil\n+import tempfile\n+import unittest\n+from pathlib import Path\n+\n+import shiki_test_support  # noqa: F401  (path bootstrap)\n+\n+import shiki_installer\n+from tests.test_adr_references import find_adr_reference_violations\n+\n+\n+PRODUCT_NUMBERS = tuple(range(13, 19))\n+\n+\n+def _quiet_install(target: Path, *, force: bool, validate: bool) -> None:\n+    with contextlib.redirect_stdout(io.StringIO()):\n+        shiki_installer.install_template(target, force=force, validate=validate)\n+\n+\n+def _sha256(path: Path) -> str:\n+    return hashlib.sha256(path.read_bytes()).hexdigest()\n+\n+\n+def _legacy_sadr_paths() -> tuple[str, ...]:\n+    return tuple(\n+        f\"docs/adr/{path.name.removeprefix('SADR-')}\"\n+        for path in sorted((shiki_installer.ROOT / \"docs\" / \"adr\").glob(\"SADR-*.md\"))\n+        if path.name != \"SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\"\n+    )\n+\n+\n+def _create_legacy_upgrade_fixture(\n+    target: Path,\n+) -> tuple[dict[str, bytes], tuple[str, ...]]:\n+    adr_dir = target / \"docs\" / \"adr\"\n+    adr_dir.mkdir(parents=True)\n+    product_bytes = {\n+        f\"{number:04d}-cubeshare-product-decision.md\": (\n+            f\"# CubeShare ADR {number:04d}\\n\\nDo not rewrite {number}.\\n\"\n+        ).encode()\n+        for number in PRODUCT_NUMBERS\n+    }\n+    for name, content in product_bytes.items():\n+        (adr_dir / name).write_bytes(content)\n+\n+    legacy_paths = _legacy_sadr_paths()\n+    for relative in legacy_paths:\n+        (target / relative).write_bytes(f\"legacy Shiki bytes for {relative}\\n\".encode())\n+\n+    stamp_path = target / shiki_installer.INSTALL_STAMP_PATH\n+    stamp_path.parent.mkdir(parents=True, exist_ok=True)\n+    stamp = {\n+        \"version\": shiki_installer.INSTALL_STAMP_VERSION,\n+        \"platform_commit\": None,\n+        \"installed_at\": \"2000-01-01T00:00:00+00:00\",\n+        \"digests\": {\n+            relative: _sha256(target / relative) for relative in legacy_paths\n+        },\n+    }\n+    stamp_path.write_text(\n+        json.dumps(stamp, indent=2, sort_keys=True) + \"\\n\", encoding=\"utf-8\"\n+    )\n+    return product_bytes, legacy_paths\n+\n+\n+def _tree_snapshot(root: Path) -> dict[str, tuple[str, bytes | str]]:\n+    \"\"\"Recursive byte/shape snapshot used to prove preflight is write-free.\"\"\"\n+    snapshot: dict[str, tuple[str, bytes | str]] = {}\n+    for path in sorted(root.rglob(\"*\")):\n+        relative = path.relative_to(root).as_posix()\n+        if path.is_symlink():\n+            snapshot[relative] = (\"symlink\", path.readlink().as_posix())\n+        elif path.is_dir():\n+            snapshot[relative] = (\"directory\", \"\")\n+        else:\n+            snapshot[relative] = (\"file\", path.read_bytes())\n+    return snapshot\n+\n+\n+class FreshSadrInstallTests(unittest.TestCase):\n+    def setUp(self) -> None:\n+        self.tmp = Path(tempfile.mkdtemp(prefix=\"shiki-sadr-fresh-\"))\n+        self.addCleanup(lambda: shutil.rmtree(self.tmp, ignore_errors=True))\n+        self.target = self.tmp / \"target\"\n+        self.adr_dir = self.target / \"docs\" / \"adr\"\n+        self.adr_dir.mkdir(parents=True)\n+        self.product_bytes = {\n+            f\"{number:04d}-product-decision.md\": (\n+                f\"# Product ADR {number:04d}\\n\\nTarget-owned bytes {number}.\\n\"\n+            ).encode()\n+            for number in PRODUCT_NUMBERS\n+        }\n+        for name, content in self.product_bytes.items():\n+            (self.adr_dir / name).write_bytes(content)\n+\n+    def test_fresh_install_preserves_product_adrs_and_installs_all_sadrs(self) -> None:\n+        _quiet_install(self.target, force=False, validate=True)\n+\n+        for name, expected in self.product_bytes.items():\n+            self.assertEqual((self.adr_dir / name).read_bytes(), expected)\n+\n+        sadr_numbers = {\n+            path.name.split(\"-\", 2)[1]\n+            for path in self.adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\")\n+        }\n+        self.assertEqual(sadr_numbers, {f\"{number:04d}\" for number in range(1, 20)})\n+\n+        numeric_records = {\n+            path.name\n+            for path in self.adr_dir.glob(\"[0-9][0-9][0-9][0-9]-*.md\")\n+        }\n+        self.assertEqual(numeric_records, set(self.product_bytes))\n+        stamp = json.loads(\n+            (self.target / shiki_installer.INSTALL_STAMP_PATH).read_text(\n+                encoding=\"utf-8\"\n+            )\n+        )\n+        for path in self.adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\"):\n+            relative = path.relative_to(self.target).as_posix()\n+            self.assertEqual(stamp[\"digests\"][relative], _sha256(path))\n+        self.assertEqual(find_adr_reference_violations(self.target), [])\n+\n+\n+class LegacySadrUpgradeTests(unittest.TestCase):\n+    def setUp(self) -> None:\n+        self.tmp = Path(tempfile.mkdtemp(prefix=\"shiki-sadr-upgrade-\"))\n+        self.addCleanup(lambda: shutil.rmtree(self.tmp, ignore_errors=True))\n+        self.target = self.tmp / \"target\"\n+        self.adr_dir = self.target / \"docs\" / \"adr\"\n+        self.product_bytes, self.legacy_paths = _create_legacy_upgrade_fixture(\n+            self.target\n+        )\n+\n+    def test_force_upgrade_deletes_only_stamped_legacy_sadrs(self) -> None:\n+        _quiet_install(self.target, force=True, validate=True)\n+\n+        for relative in self.legacy_paths:\n+            self.assertFalse((self.target / relative).exists(), relative)\n+        for name, expected in self.product_bytes.items():\n+            self.assertEqual((self.adr_dir / name).read_bytes(), expected)\n+\n+        sadrs = list(self.adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\"))\n+        self.assertEqual(len(sadrs), 19)\n+        refreshed = json.loads(\n+            (self.target / shiki_installer.INSTALL_STAMP_PATH).read_text(encoding=\"utf-8\")\n+        )\n+        for relative in self.legacy_paths:\n+            self.assertNotIn(relative, refreshed[\"digests\"])\n+        for path in sadrs:\n+            relative = path.relative_to(self.target).as_posix()\n+            self.assertEqual(refreshed[\"digests\"][relative], _sha256(path))\n+\n+    def test_non_force_install_never_deletes_legacy_files(self) -> None:\n+        before = {\n+            relative: (self.target / relative).read_bytes()\n+            for relative in self.legacy_paths\n+        }\n+        _quiet_install(self.target, force=False, validate=False)\n+        for relative, expected in before.items():\n+            self.assertEqual((self.target / relative).read_bytes(), expected)\n+        refreshed = json.loads(\n+            (self.target / shiki_installer.INSTALL_STAMP_PATH).read_text(encoding=\"utf-8\")\n+        )\n+        for relative in self.legacy_paths:\n+            self.assertEqual(refreshed[\"digests\"][relative], _sha256(self.target / relative))\n+\n+\n+class LegacyCleanupPreflightTests(unittest.TestCase):\n+    def setUp(self) -> None:\n+        self.tmp = Path(tempfile.mkdtemp(prefix=\"shiki-sadr-preflight-\"))\n+        self.addCleanup(lambda: shutil.rmtree(self.tmp, ignore_errors=True))\n+\n+    def test_all_ownership_blockers_abort_before_any_write(self) -> None:\n+        cases = (\n+            (\"absent\", \"install stamp is absent\"),\n+            (\"unreadable\", \"install stamp is unreadable\"),\n+            (\"malformed\", \"install stamp is malformed\"),\n+            (\"missing-digest\", \"install stamp has no digest\"),\n+            (\"digest-mismatch\", \"digest mismatch\"),\n+        )\n+        for name, expected_reason in cases:\n+            with self.subTest(name=name):\n+                target = self.tmp / name\n+                _, legacy_paths = _create_legacy_upgrade_fixture(target)\n+                stamp_path = target / shiki_installer.INSTALL_STAMP_PATH\n+\n+                if name == \"absent\":\n+                    stamp_path.unlink()\n+                elif name == \"unreadable\":\n+                    stamp_path.unlink()\n+                    stamp_path.mkdir()\n+                elif name == \"malformed\":\n+                    stamp_path.write_text(\"{not-json\\n\", encoding=\"utf-8\")\n+                else:\n+                    stamp = json.loads(stamp_path.read_text(encoding=\"utf-8\"))\n+                    if name == \"missing-digest\":\n+                        stamp[\"digests\"] = {}\n+                    else:\n+                        stamp[\"digests\"] = {\n+                            relative: \"deadbeef\" for relative in legacy_paths\n+                        }\n+                    stamp_path.write_text(\n+                        json.dumps(stamp, indent=2, sort_keys=True) + \"\\n\",\n+                        encoding=\"utf-8\",\n+                    )\n+\n+                before = _tree_snapshot(target)\n+                with self.assertRaises(shiki_installer.ShikiError) as raised:\n+                    _quiet_install(target, force=True, validate=False)\n+                after = _tree_snapshot(target)\n+\n+                self.assertEqual(before, after, f\"{name} preflight changed the tree\")\n+                message = str(raised.exception)\n+                self.assertIn(expected_reason, message)\n+                self.assertIn(\"No files were written\", message)\n+                for relative in legacy_paths:\n+                    self.assertIn(relative, message)\n+\n+    def test_absent_stamp_is_allowed_when_no_legacy_path_exists(self) -> None:\n+        target = self.tmp / \"no-legacy\"\n+        target.mkdir()\n+        _quiet_install(target, force=True, validate=False)\n+        self.assertTrue(\n+            (target / \"docs\" / \"adr\" / \"SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\").is_file()\n+        )\n+\n+\n+if __name__ == \"__main__\":\n+    unittest.main()\n\n2026-08-07T08:23:27.154056Z ERROR codex_models_manager::manager: failed to renew cache TTL: missing field `base_instructions` at line 94 column 5\n succeeded in 1563ms:\nok\ntest_absent_stamp_is_allowed_when_no_legacy_path_exists (test_sadr_install_migration.LegacyCleanupPreflightTests.test_absent_stamp_is_allowed_when_no_legacy_path_exists) ... ok\ntest_all_ownership_blockers_abort_before_any_write (test_sadr_install_migration.LegacyCleanupPreflightTests.test_all_ownership_blockers_abort_before_any_write) ... ok\ntest_force_upgrade_deletes_only_stamped_legacy_sadrs (test_sadr_install_migration.LegacySadrUpgradeTests.test_force_upgrade_deletes_only_stamped_legacy_sadrs) ... ok\ntest_non_force_install_never_deletes_legacy_files (test_sadr_install_migration.LegacySadrUpgradeTests.test_non_force_install_never_deletes_legacy_files) ... ok\n\n----------------------------------------------------------------------\nRan 5 tests in 1.542s\n\nOK\n\nexec\n/bin/zsh -lc \"rg -n 'SADR-[0-9]{4}/[0-9]{4}' AGENTS.md CLAUDE.md CONTEXT.md CONTRIBUTING.md SYSTEM_PROMPT.md docs scripts .github .codex pyproject.toml\" in /private/tmp/shiki-adr-collision.pa0KwY/.worktrees/shiki-t-20260807t073307221254z-2d22d654-introduc\nexec\n/bin/zsh -lc 'git diff -- scripts/shiki_guardian_review.py tests/test_guardian_review.py' in /private/tmp/shiki-adr-collision.pa0KwY/.worktrees/shiki-t-20260807t073307221254z-2d22d654-introduc\nexec\n/bin/zsh -lc \"rg -n -U '\\\\bADR(?:[ -])(?:000[1-9]|001[0-8])\\\\b|docs/adr/(?:000[1-9]|001[0-8])-' AGENTS.md CLAUDE.md CONTEXT.md CONTRIBUTING.md SYSTEM_PROMPT.md docs scripts .github .codex pyproject.toml --glob '\"'!docs/adr/SADR-0019-*'\"' --glob '\"'!tests/test_sadr_install_migration.py'\"'\" in /private/tmp/shiki-adr-collision.pa0KwY/.worktrees/shiki-t-20260807t073307221254z-2d22d654-introduc\n succeeded in 0ms:\ndiff --git a/scripts/shiki_guardian_review.py b/scripts/shiki_guardian_review.py\nindex aa6e544..e10ef58 100644\n--- a/scripts/shiki_guardian_review.py\n+++ b/scripts/shiki_guardian_review.py\n@@ -1,5 +1,5 @@\n #!/usr/bin/env python3\n-\"\"\"External AI Guardian Review adapter contract (ADR 0010 / ADR 0014).\n+\"\"\"External AI Guardian Review adapter contract (SADR-0010 / SADR-0014).\n \n Deterministic Shiki-side surfaces consumed by the Codex App External AI Guardian\n UI Adapter:\n@@ -12,7 +12,7 @@ UI Adapter:\n   output and validate any approval artifact against the same approval contract\n   the PR-comment path enforces (``shiki_guardian.validate_ai_review_artifact``).\n \n-Role boundary (ADR 0010 / ADR 0014): Claude Code implements these DETERMINISTIC\n+Role boundary (SADR-0010 / SADR-0014): Claude Code implements these DETERMINISTIC\n contracts only. Codex App is the UI adapter that drives ChatGPT Pro; GPT Pro is\n the external Guardian Authority; GitHub carries the live ``external-ai-guardian-\n review`` artifact; MergeGate verifies. Nothing in this module drives a ChatGPT\n@@ -159,7 +159,7 @@ def classify_review_focus(\n     docs_only = _is_docs_only(changed_files)\n     if docs_only and matched == 0:\n         focus.append(\n-            \"Docs/ADR-only change: confirm the change is genuinely documentation/decision-only, introduces no behavior or contract drift, and stays consistent with the constitution and existing ADRs.\"\n+            \"Docs/decision-record-only change: confirm the change is genuinely documentation/decision-only, introduces no behavior or contract drift, and stays consistent with the constitution and existing SADRs and target ADRs.\"\n         )\n \n     # Conservative augmentation: unknown (no category and not docs-only), mixed\ndiff --git a/tests/test_guardian_review.py b/tests/test_guardian_review.py\nindex 8a58c9b..cfe5c03 100644\n--- a/tests/test_guardian_review.py\n+++ b/tests/test_guardian_review.py\n@@ -1,4 +1,4 @@\n-\"\"\"Tests for the External AI Guardian Review adapter contract (ADR 0010/0014).\n+\"\"\"Tests for the External AI Guardian Review adapter contract (SADR-0010/SADR-0014).\n \n Covers the deterministic Shiki-side surfaces consumed by the Codex App External\n AI Guardian UI Adapter: packet schema validation, the PR-type focus classifier,\n@@ -53,7 +53,7 @@ def make_packet(**overrides):\n         task=task,\n         pr_data=pr_data,\n         implementer_report={\"source\": \"claude full response\", \"summary\": \"implemented per handoff\"},\n-        relevant_docs=[\"docs/adr/0010-external-ai-guardian-review.md\"],\n+        relevant_docs=[\"docs/adr/SADR-0010-external-ai-guardian-review.md\"],\n         generated_at=\"20260101T000000000000Z\",\n         source_refs=[\"gh pr view 175\", \".shiki/tasks/T-...json\"],\n     )\n@@ -162,7 +162,7 @@ class FocusClassifierTests(unittest.TestCase):\n \n     def test_docs_only_is_light_and_not_conservative(self):\n         focus = gr.classify_review_focus([\"docs/adr/0099-x.md\", \"README.md\"], \"\", \"low\")\n-        self.assertTrue(any(\"Docs/ADR-only\" in f for f in focus))\n+        self.assertTrue(any(\"Docs/decision-record-only\" in f for f in focus))\n         self.assertFalse(any(\"conservative default\" in f.lower() for f in focus))\n \n     def test_unknown_category_is_conservative(self):\n\n exited 1 in 0ms:\n exited 1 in 0ms:\nexec\n/bin/zsh -lc 'git diff -- scripts/validate_shiki.py pyproject.toml .github/workflows/release.yml' in /private/tmp/shiki-adr-collision.pa0KwY/.worktrees/shiki-t-20260807t073307221254z-2d22d654-introduc\nexec\n/bin/zsh -lc 'git diff -- docs/adr/README.md docs/adr/template.md docs/operations.md docs/releasing.md docs/usage.md docs/agents/bootstrap-command.md' in /private/tmp/shiki-adr-collision.pa0KwY/.worktrees/shiki-t-20260807t073307221254z-2d22d654-introduc\n succeeded in 0ms:\ndiff --git a/.github/workflows/release.yml b/.github/workflows/release.yml\nindex 78d3329..e92c994 100644\n--- a/.github/workflows/release.yml\n+++ b/.github/workflows/release.yml\n@@ -3,7 +3,7 @@ name: Shiki Release\n # Tag-triggered release. Pushing a SemVer tag (e.g. v0.1.0) publishes a GitHub\n # Release for that tag. This workflow uses only the built-in GITHUB_TOKEN and\n # requires no additional secrets. See docs/releasing.md and\n-# docs/adr/0007-packaging-and-release.md.\n+# docs/adr/SADR-0007-packaging-and-release.md.\n \n on:\n   push:\ndiff --git a/pyproject.toml b/pyproject.toml\nindex a32e04a..487787a 100644\n--- a/pyproject.toml\n+++ b/pyproject.toml\n@@ -9,7 +9,7 @@\n # installs are therefore NOT supported; the official install path is the script\n # install (`python3 scripts/shiki.py install-global`) or running the shim\n # directly, as documented in docs/releasing.md. See\n-# docs/adr/0007-packaging-and-release.md.\n+# docs/adr/SADR-0007-packaging-and-release.md.\n #\n # The version below is kept in sync with the canonical ./VERSION file. When\n # bumping the release, update both ./VERSION and the `version` field here.\ndiff --git a/scripts/validate_shiki.py b/scripts/validate_shiki.py\nindex bb5674e..c776170 100755\n--- a/scripts/validate_shiki.py\n+++ b/scripts/validate_shiki.py\n@@ -301,10 +301,10 @@ WORKFLOW_CONTRACTS = {\n #\n # Required per-job invocation flags: {workflow file: {job id: {script token: flags}}}.\n # The metadata check must pass --base-shiki and --merged-prs so a\n-# post_merge_reconcile PR (and the ADR 0017 bookkeeping-closeout exemption) can be\n+# post_merge_reconcile PR (and the SADR-0017 bookkeeping-closeout exemption) can be\n # proven; without them mergegate_check.py fails closed on a missing base snapshot.\n #\n-# ADR 0018 base-sync carry (guardian_comment_carried): every invocation that judges\n+# SADR-0018 base-sync carry (guardian_comment_carried): every invocation that judges\n # Guardian approval must pass --base-sync-carry and --default-branch, or the carry\n # degrades to \"does not apply\" silently \u2014 no error, feature invisible. Four such\n # invocations exist across the two workflows and each is pinned here so a future\n@@ -759,7 +759,7 @@ def validate_guardian_policy_contracts(root: Path = ROOT) -> None:\n     if policy.advisory_claude_review_counts_as_guardian:\n         raise ValidationError(f\"{GUARDIAN_POLICY_PATH}: advisory Claude review must not count as Guardian\")\n     if policy.ai_review_enabled and not policy.ai_review_require_head_sha:\n-        raise ValidationError(f\"{GUARDIAN_POLICY_PATH}: external_ai_guardian_review must bind to the head SHA (ADR 0010)\")\n+        raise ValidationError(f\"{GUARDIAN_POLICY_PATH}: external_ai_guardian_review must bind to the head SHA (SADR-0010)\")\n \n     manifest = load_manifest(root)\n     files = manifest.get(\"files\")\n@@ -2085,7 +2085,7 @@ def loop_lock_warnings(\n ) -> list[str]:\n     \"\"\"WARN-ONLY advisory hook for loop-task mirror-lock coverage.\n \n-    Under the derive-at-judgment-time mirror-lock model (ADR 0016 / A-LOCKS) a\n+    Under the derive-at-judgment-time mirror-lock model (SADR-0016 / A-LOCKS) a\n     registered task's stored ``locks`` are EXACTLY what the plan declared \u2014\n     nothing is injected, and no task declares a blanket ``path:.shiki/**``.\n     MergeGate recomputes the task's id-scoped mirror set at judgment time\n@@ -2099,7 +2099,7 @@ def loop_lock_warnings(\n     (``shiki_tasks.loop_guaranteed_locks``). No such guarantee ever existed, and\n     the warning would fire on every task registered under the new model, so it is\n     removed. This hook is retained WARN-ONLY: the future mirror-identity rule\n-    (ADR 0016 step B / remediation R-02) will decide any blocking behaviour, and\n+    (SADR-0016 step B / remediation R-02) will decide any blocking behaviour, and\n     it must not become an error before R-02 is in force.\n     \"\"\"\n     return []\n\n succeeded in 0ms:\ndiff --git a/docs/adr/README.md b/docs/adr/README.md\nindex c70e057..5da4385 100644\n--- a/docs/adr/README.md\n+++ b/docs/adr/README.md\n@@ -1,40 +1,41 @@\n-# Architecture Decision Records\n+# Decision Record Namespaces\n \n-This directory holds Shiki's Architecture Decision Records (ADRs): durable\n-records of hard-to-reverse platform decisions. ADRs are part of the Shiki source\n-of truth alongside GitHub state, the `.shiki/` mirror, and `CONTEXT.md`.\n+This directory holds two independent decision namespaces:\n \n-## How ADRs Work\n+- Shiki Architecture Decision Records (SADRs), named\n+  `SADR-NNNN-<short-slug>.md`, record hard-to-reverse Shiki platform decisions.\n+- Target Architecture Decision Records (ADRs), named\n+  `NNNN-<short-slug>.md`, belong to the target product.\n \n-- New ADRs are added as `docs/adr/NNNN-<short-slug>.md`, where `NNNN` is the next\n-  zero-padded sequence number.\n-- The **canonical list of ADRs is the set of files matching `docs/adr/*.md`** in\n-  this directory. Browse `docs/adr/*.md` for the authoritative, current set.\n-- Use [`template.md`](template.md) as the starting point for a new ADR.\n-- Record an ADR when a decision is hard to reverse, surprising without context,\n-  and a real tradeoff. Do not write an ADR for routine or easily reversible\n-  choices.\n+The same four-digit number may appear once in each namespace. A platform\n+`SADR-NNNN` citation resolves only to `SADR-NNNN-*.md`; a target `ADR NNNN`\n+citation resolves only to `NNNN-*.md`.\n \n-Using the `docs/adr/*.md` glob as the source of truth means other PRs can add an\n-ADR without editing this index file, avoiding merge contention on a\n-hand-maintained enumeration.\n+## How Decision Records Work\n \n-## Current ADRs (snapshot)\n+- New Shiki platform decisions use the next zero-padded SADR number.\n+- New target product decisions use the target's own ADR sequence.\n+- The canonical list is the files in `docs/adr/`; no hand-maintained index is\n+  authoritative.\n+- Use [`template.md`](template.md) as the starting point and choose the filename\n+  namespace that owns the decision.\n+- Record a decision when it is hard to reverse, surprising without context, and\n+  a real tradeoff. Do not write a decision record for routine or easily\n+  reversible choices.\n \n-The list below is a convenience snapshot and may lag behind the directory. When\n-in doubt, list `docs/adr/*.md`.\n+Using the directory as the source of truth lets independent changes add records\n+without contending on an enumerated index.\n \n-- [0001 \u2014 Keep Shiki As The Platform Name](0001-keep-shiki-as-platform-name.md)\n-- [0002 \u2014 Use AGENTS As The Shared Agent Constitution](0002-use-agents-as-shared-constitution.md)\n-- [0003 \u2014 Require Skill Gate For MergeGate Readiness](0003-require-skill-gate-for-mergegate-readiness.md)\n-- [0004 \u2014 Use GitHub First With A Repository-Local Shiki Mirror](0004-use-github-first-with-shiki-mirror.md)\n-- [0005 \u2014 Use Codex As Front And Claude Code Action As The GitHub Runtime](0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md)\n-- [0006 \u2014 Separate Codex Implementation, CCA Completion, And MergeGate Authorization](0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md)\n+## Adding A Shiki SADR\n \n-## Adding A New ADR\n-\n-1. Copy `template.md` to `docs/adr/NNNN-<short-slug>.md`.\n+1. Copy `template.md` to `docs/adr/SADR-NNNN-<short-slug>.md`.\n 2. Fill in the title, status, context, decision, and consequences.\n-3. Set the status to `Proposed`, then `Accepted` once the decision is approved.\n-4. Open a PR following `CONTRIBUTING.md`. You do not need to edit this index for\n-   the ADR to be discoverable via `docs/adr/*.md`.\n+3. Set the status to `Proposed`, then `Accepted` once the proper authority has\n+   approved the decision.\n+4. Open a PR following `CONTRIBUTING.md`.\n+\n+## Adding A Target ADR\n+\n+1. Copy `template.md` to `docs/adr/NNNN-<short-slug>.md` using the target's own\n+   sequence.\n+2. Cite it as `ADR NNNN`; do not prefix its filename with `SADR-`.\ndiff --git a/docs/adr/template.md b/docs/adr/template.md\nindex a92a97d..17dd791 100644\n--- a/docs/adr/template.md\n+++ b/docs/adr/template.md\n@@ -1,14 +1,14 @@\n-# NNNN: <Short Decision Title>\n+# <SADR-NNNN or NNNN>: <Short Decision Title>\n \n ## Status\n \n-Proposed | Accepted | Superseded by ADR-NNNN\n+Proposed | Accepted | Superseded by SADR-NNNN or target ADR NNNN\n \n ## Context\n \n What is the situation, constraint, or force that requires a decision? Reference\n-relevant domain terms (`CONTEXT.md`), prior ADRs, and code reality. State the\n-problem, not the solution.\n+relevant domain terms (`CONTEXT.md`), prior Shiki SADRs or target ADRs, and code\n+reality. State the problem, not the solution.\n \n ## Decision\n \ndiff --git a/docs/agents/bootstrap-command.md b/docs/agents/bootstrap-command.md\nindex 05f7f20..f7aa0b7 100644\n--- a/docs/agents/bootstrap-command.md\n+++ b/docs/agents/bootstrap-command.md\n@@ -189,7 +189,9 @@ shiki install-target /path/to/target-repo --local-only\n ```\n \n Use this only for tests, fixtures, or explicit local-only template inspection.\n-Use `--force` only when you intentionally want to overwrite existing target files.\n+Use `--force` only when you intentionally want to refresh the target template.\n+Legacy numeric Shiki decision cleanup is stamp-and-digest authorized before any\n+write; target ADR paths are not cleanup candidates.\n \n ## Slash Command\n \ndiff --git a/docs/operations.md b/docs/operations.md\nindex cf1f452..ace48a9 100644\n--- a/docs/operations.md\n+++ b/docs/operations.md\n@@ -89,6 +89,16 @@ template, CLI, and mirror schema.\n      target history, brought forward by `shiki migrate`, not a template.\n    - **Everything else** (CLI scripts, workflows, schemas, docs) is overwritten.\n \n+   A forced upgrade also migrates the former numeric Shiki decision paths to\n+   `SADR-NNNN-*`. Before the first write, the installer inventories the exact\n+   legacy Shiki paths and authorizes each deletion only when the existing\n+   `.shiki/install-stamp.json` names that path and its SHA-256 digest still\n+   matches the file. An absent, unreadable, malformed, incomplete, or\n+   mismatching stamp reports every blocked path and stops with no filesystem\n+   change. Target-owned `NNNN-*` ADRs are not cleanup candidates. A non-force\n+   install deletes nothing and retains any still-matching legacy stamp entries\n+   so a later forced upgrade can prove ownership.\n+\n    If the target has pending migrations, `--force` refuses and writes nothing \u2014\n    apply migrations first (step 3), then re-run. The run ends with a single\n    summary of every `.new` written, so a half-upgrade is visible.\ndiff --git a/docs/releasing.md b/docs/releasing.md\nindex ad35cd5..aafeee8 100644\n--- a/docs/releasing.md\n+++ b/docs/releasing.md\n@@ -3,7 +3,7 @@\n This document defines how Shiki is versioned, released, installed, upgraded, and\n rolled back, and how the platform-runtime boundary differs from the\n target-template boundary. The durable decision record is\n-[docs/adr/0007-packaging-and-release.md](adr/0007-packaging-and-release.md).\n+[docs/adr/SADR-0007-packaging-and-release.md](adr/SADR-0007-packaging-and-release.md).\n \n Shiki is a GitHub-first agentic engineering control plane. The operational\n source of truth is GitHub (Issues, PRs, Checks, Reviews, merges); the\n@@ -67,7 +67,7 @@ Shiki ships as a repository template plus a dependency-free, standard-library\n CLI in `scripts/`. There are no third-party Python runtime dependencies. The CLI\n is intentionally **not** exposed as a `[project.scripts]` console entry point\n because `scripts/shiki.py` is a dependency-free shim and `scripts/` is not\n-packaged as an importable distribution (see the ADR for the rationale).\n+packaged as an importable distribution (see SADR-0007 for the rationale).\n \n Supported install paths:\n \n@@ -102,7 +102,7 @@ Supported install paths:\n   importable distribution, `pipx install git+https://\u2026` would not expose a usable\n   `shiki` command. Use the script-install path above instead. A `pipx`/PyPI\n   install path may be reconsidered only after a future packaging restructure\n-  (see [docs/adr/0007-packaging-and-release.md](adr/0007-packaging-and-release.md)).\n+  (see [docs/adr/SADR-0007-packaging-and-release.md](adr/SADR-0007-packaging-and-release.md)).\n \n - **Install into a target repository:** use the bootstrap path documented in\n   [docs/agents/bootstrap-command.md](agents/bootstrap-command.md) (`bin/shiki`)\n@@ -155,6 +155,15 @@ applying any repository-local state migrations:\n    [docs/operations.md](operations.md#the-new-convention). If the target has\n    pending migrations, `--force` refuses and writes nothing; apply migrations\n    (step 2) first, then re-run.\n+\n+   The SADR namespace upgrade has an additional pre-write gate: each former\n+   numeric Shiki decision path is removed only when the existing install stamp\n+   contains that exact path and a digest matching its current bytes. The\n+   installer aggregates absent/unreadable/malformed-stamp, missing-digest, and\n+   digest-mismatch blockers before raising, so refusal leaves the target tree\n+   byte-identical. Numeric target ADRs are preserved. A successful forced\n+   upgrade installs and stamps `SADR-*` paths and drops the removed legacy paths\n+   from the refreshed stamp.\n 2. Apply repository-local `.shiki/` state migrations with the migrate command:\n \n    ```bash\ndiff --git a/docs/usage.md b/docs/usage.md\nindex b83407e..a130ef6 100644\n--- a/docs/usage.md\n+++ b/docs/usage.md\n@@ -67,8 +67,10 @@ GitHub-first.\n shiki install-target /path/to/target-repo --local-only\n ```\n \n-Use `--force` only when you intentionally want to overwrite existing target\n-files.\n+Use `--force` only when you intentionally want to refresh the target template.\n+For the SADR namespace migration, `--force` deletes only exact legacy Shiki\n+decision paths whose existing install-stamp digest still matches; any ownership\n+blocker stops before the first write. Numeric target ADRs are never candidates.\n \n ## `shiki bootstrap-platform` \u2014 publish the Shiki platform repo\n \n\ndiff --git a/.codex/skills/shiki/SKILL.md b/.codex/skills/shiki/SKILL.md\nindex c8e2e85d9d75d49cc4f2d6286ee22659c4e53e75..93cded623b2c15d9f65c76491a1c2d813ac9e6c0\n--- a/.codex/skills/shiki/SKILL.md\n+++ b/.codex/skills/shiki/SKILL.md\n@@ -18,6 +18,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n Codex CLI does not register this skill as a custom `/shiki` slash command. When\n the user types `/shiki` in Codex CLI and gets \"Unrecognized command\", explain\n that this is expected: invoke Shiki in Codex with natural language such as\n@@ -69,7 +72,7 @@\n \n ## Responsibilities\n \n-- Claude Code implements and repairs by default (`shiki runner claude`, ADR 0008); Codex implements and repairs tasks explicitly assigned to `codex`.\n+- Claude Code implements and repairs by default (`shiki runner claude`, SADR-0008); Codex implements and repairs tasks explicitly assigned to `codex`.\n - Claude Code Action can act as GitHub-side CCA or reviewer.\n - CCA judges completion.\n - MergeGate authorizes state transitions and merge readiness.\ndiff --git a/.github/prompts/cca-completion-check.md b/.github/prompts/cca-completion-check.md\nindex ed203ad5f302ecc81372d8ab27d2b1232ca97497..fd89950528333ef6242e6d5443904f5392ab7e56\n--- a/.github/prompts/cca-completion-check.md\n+++ b/.github/prompts/cca-completion-check.md\n@@ -19,6 +19,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n ## Required Reading\n \n Read only the files needed to judge the PR contract. Prefer this order:\ndiff --git a/.github/prompts/codex-task-handoff.md b/.github/prompts/codex-task-handoff.md\nindex 695e211d76dea1c60e8d5bb360e78487808286a4..59441d576b01a39569722f226c854e5a740e9eb5\n--- a/.github/prompts/codex-task-handoff.md\n+++ b/.github/prompts/codex-task-handoff.md\n@@ -2,7 +2,7 @@\n \n You are Codex Front acting as the Implementer for a Shiki task.\n \n-Follow `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant ADRs, and `docs/agents/codex-handoff.md`.\n+Follow `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant Shiki SADRs and target ADRs, and `docs/agents/codex-handoff.md`.\n \n Rules:\n \ndiff --git a/AGENTS.md b/AGENTS.md\nindex 7f041100d49c2a7525f51cb0f5d696a4a6d1c78c..a06bfbabc6fa48a007389e25e12d7ed523936bed\n--- a/AGENTS.md\n+++ b/AGENTS.md\n@@ -22,7 +22,7 @@\n - **Codex Front** is an optional implementer through Codex App, Codex CLI, Codex IDE extension, or Codex Web signed in with ChatGPT OAuth/subscription auth, dispatched with `shiki runner codex` when a task is explicitly assigned to `codex`.\n - **GitHub CCA** is implemented by Claude Code Action by default, using `CLAUDE_CODE_OAUTH_TOKEN`.\n \n-Do not assume `openai/codex-action`, `OPENAI_API_KEY`, or API-key based Codex automation in the default Shiki path. API-key runners are explicit target-repo extensions and require their own ADR.\n+Do not assume `openai/codex-action`, `OPENAI_API_KEY`, or API-key based Codex automation in the default Shiki path. API-key runners are explicit target-repo extensions and require their own target ADR.\n \n Shiki is not:\n \n@@ -44,10 +44,14 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use\n+`SADR-NNNN-*.md`; target product decisions retain `NNNN-*.md` and the `ADR`\n+namespace.\n+\n Use the canonical source-of-truth order above. Runtime-specific reading orders\n are operational guidance only and must not override or reorder it.\n \n-Conversation state is not durable truth. If a decision matters, put it in GitHub, `.shiki/`, `CONTEXT.md`, or an ADR.\n+Conversation state is not durable truth. If a decision matters, put it in GitHub, `.shiki/`, `CONTEXT.md`, a Shiki SADR, or a target ADR.\n \n If GitHub and `.shiki/` disagree, surface the conflict and prefer GitHub operational state until the mirror is repaired.\n \n@@ -75,8 +79,8 @@\n Every non-trivial change follows this loop:\n \n 1. **Goal**: clarify outcome, completion conditions, non-goals, risk level, and success signals.\n-2. **grill-with-docs**: challenge the plan against domain docs, ADRs, code reality, terminology, and edge scenarios.\n-3. **Context & Impact**: find relevant docs, ADRs, code, dependencies, owners, locks, and verification surfaces.\n+2. **grill-with-docs**: challenge the plan against domain docs, Shiki SADRs, target ADRs, code reality, terminology, and edge scenarios.\n+3. **Context & Impact**: find relevant docs, SADRs, target ADRs, code, dependencies, owners, locks, and verification surfaces.\n 4. **PRD and Spec Freeze**: use `to-prd` when context is settled enough to become durable product/engineering intent. The operator's explicit approval of the PRD is Spec Freeze: the plan records a `spec_freeze` block, and no plan runs without it. Steps 1-4 form Requirements Definition \u2014 one continuous operator dialogue. After Spec Freeze, scope changes require an operator-approved Spec Amendment; non-scope-moving interpretations go to the Assumption Log.\n 5. **Issues**: use `to-issues` to create independently grabbable vertical-slice issues. Prefer AFK slices over HITL slices where possible.\n 6. **Triage**: label issues for readiness, risk, runtime, skills, and MergeGate state.\n@@ -167,7 +171,7 @@\n - Testing decisions.\n - Out-of-scope items.\n - Further notes.\n-- Links to relevant domain terms and ADRs.\n+- Links to relevant domain terms, SADRs, and target ADRs.\n \n Every executable task must include:\n \n@@ -193,7 +197,7 @@\n \n Context & Impact must identify:\n \n-- Relevant documents, domain terms, ADRs, and past decisions.\n+- Relevant documents, domain terms, SADRs, target ADRs, and past decisions.\n - Relevant modules, interfaces, seams, callers, owners, and tests.\n - Dependency relationships between tasks.\n - Candidate locks and lock conflicts.\n@@ -210,7 +214,7 @@\n | Skill | Required when |\n | --- | --- |\n | `setup-matt-pocock-skills` | First configuring the repo for skills, or when issue tracker, triage labels, or domain docs are missing. |\n-| `grill-with-docs` | Default for non-trivial Goals before PRD/issues; always required when plans, terminology, boundaries, tradeoffs, or ADR-worthy decisions are ambiguous. |\n+| `grill-with-docs` | Default for non-trivial Goals before PRD/issues; always required when plans, terminology, boundaries, tradeoffs, or decision-record-worthy choices are ambiguous. |\n | `zoom-out` | The agent lacks an architectural map of the relevant code area. |\n | `to-prd` | Settled Goal context must become durable product/engineering intent. |\n | `to-issues` | A Goal, PRD, or plan must become independently grabbable vertical-slice issues. |\n@@ -348,7 +352,7 @@\n - Three or more new files.\n - Any high-risk or critical label.\n \n-When the architecture gate triggers, run `grill-with-docs` or `improve-codebase-architecture` as appropriate and record durable decisions in an ADR when needed.\n+When the architecture gate triggers, run `grill-with-docs` or `improve-codebase-architecture` as appropriate and record durable decisions in a Shiki SADR or target ADR as applicable.\n \n ## Engineering Discipline\n \ndiff --git a/CLAUDE.md b/CLAUDE.md\nindex 98a932e7841a632866fcd53b0bad0e3af3711d3d..98a31399404d6fb044b4fd493f4459ac87f439de\n--- a/CLAUDE.md\n+++ b/CLAUDE.md\n@@ -14,6 +14,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n ## Claude Role\n \n Default to planner, default implementer, reviewer, coordinator, documentation editor, CCA-style completion judge, and final-judgment assistant.\n@@ -57,7 +60,7 @@\n At the start of a Shiki session:\n \n 1. Read `AGENTS.md`.\n-2. Read `CONTEXT.md` and relevant ADRs.\n+2. Read `CONTEXT.md` and relevant Shiki SADRs and target ADRs.\n 3. Read active `.shiki/` Goal/task/ledger state when present.\n 4. Read active distilled rules from `.shiki/memories/` (status `distilled`, `active: true`, not revoked or superseded) for applicable guidance; the same rules are injected deterministically into task handoffs as the `## Distilled Rules` section (\u00a73.5).\n 5. Read the GitHub Issue or PR when the task is GitHub-backed.\n@@ -74,7 +77,7 @@\n - Define the Goal, completion conditions, non-goals, and risk level.\n - Treat `grill-with-docs` as the default planning entry point for non-trivial Goals.\n - Challenge domain terms against `CONTEXT.md`.\n-- Challenge hard-to-reverse tradeoffs against ADRs.\n+- Challenge hard-to-reverse tradeoffs against SADRs and target ADRs.\n - Explore code instead of asking when code can answer the question.\n - Run Context & Impact before implementation planning.\n - Produce a Task DAG, not an unordered checklist.\n@@ -94,7 +97,7 @@\n - For each question, provide your recommended answer.\n - Resolve dependency decisions before downstream design questions.\n - Update `CONTEXT.md` inline when terminology crystallizes.\n-- Offer ADRs only when the decision is hard to reverse, surprising without context, and a real tradeoff.\n+- Offer a SADR or target ADR only when the decision is hard to reverse, surprising without context, and a real tradeoff.\n - Convert unresolved questions into HITL blockers rather than allowing Codex to guess.\n \n ## PRD and Issues Standard\n@@ -103,7 +106,7 @@\n \n - Use the project's domain glossary vocabulary.\n - Avoid volatile file paths unless they are essential.\n-- Include implementation decisions, testing decisions, out-of-scope items, and relevant ADR/domain links.\n+- Include implementation decisions, testing decisions, out-of-scope items, and relevant SADR/target-ADR/domain links.\n - Identify deep-module opportunities and testable interfaces.\n \n When creating issues:\n@@ -121,7 +124,7 @@\n - Branch/worktree target.\n - Scope and non-goals.\n - Dependencies and locks.\n-- Relevant docs, ADRs, modules, and tests.\n+- Relevant docs, SADRs, target ADRs, modules, and tests.\n - Required skill invocations.\n - TDD expectations.\n - Pre-PR `code-review` gate expectations.\n@@ -152,7 +155,7 @@\n \n - Treat yourself as a judge, not implementer.\n - Do not write production code.\n-- Read `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant ADRs, PRD, task issue, PR body, diff, checks, reviews, labels, and `.shiki/` state.\n+- Read `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant SADRs and target ADRs, PRD, task issue, PR body, diff, checks, reviews, labels, and `.shiki/` state.\n - Evaluate every applicable checklist in `docs/agents/checklists.md`.\n - Map each acceptance criterion to durable evidence.\n - Distinguish implementation failure from missing evidence.\ndiff --git a/CONTEXT.md b/CONTEXT.md\nindex e61a9be06a0cce0d026484083a657bde2067e234..237ee8aa3ea8fab334ece49768bc5ed3b5af0974\n--- a/CONTEXT.md\n+++ b/CONTEXT.md\n@@ -25,7 +25,7 @@\n _Avoid_: jumping directly into code\n \n **grill-with-docs**:\n-A planning skill that challenges a plan against domain language, ADRs, code reality, and concrete edge scenarios. It resolves design-tree questions before PRD/issues. It runs inside Requirements Definition.\n+A planning skill that challenges a plan against domain language, Shiki SADRs, target ADRs, code reality, and concrete edge scenarios. It resolves design-tree questions before PRD/issues. It runs inside Requirements Definition.\n _Avoid_: generic brainstorming, silent assumptions\n \n **Requirements Definition**:\ndiff --git a/CONTRIBUTING.md b/CONTRIBUTING.md\nindex c6d9b656e73abb8a55e5f660123e03153dcb3e6d..9e37a8fbf668db5b22461aaae5548b8f1354394c\n--- a/CONTRIBUTING.md\n+++ b/CONTRIBUTING.md\n@@ -17,7 +17,7 @@\n 1. **Goal** \u2014 start from a user-approved Goal with an outcome, completion\n    conditions, non-goals, risk level, and success signals.\n 2. **grill-with-docs** \u2014 for any non-trivial change, challenge the plan against\n-   domain language, ADRs, and code reality before writing a PRD.\n+   domain language, Shiki SADRs, target ADRs, and code reality before writing a PRD.\n 3. **PRD** \u2014 capture settled product/engineering intent (`to-prd`).\n 4. **Spec Freeze** \u2014 the operator explicitly approves the PRD/requirements;\n    the plan records a `spec_freeze` block and no plan runs without it\n@@ -94,7 +94,7 @@\n   entries.\n - Do not hand-edit `.shiki/goals/*.json` or `.shiki/manifest.json` in a task PR.\n - Conversation state is not durable truth. If a decision matters, record it in\n-  GitHub, `.shiki/`, `CONTEXT.md`, or an ADR.\n+  GitHub, `.shiki/`, `CONTEXT.md`, a Shiki SADR, or a target ADR.\n \n ## Architecture Gate\n \n@@ -103,7 +103,7 @@\n or CI policy, package structure, three or more structural files, more than 100\n net new lines in a core module, three or more new files, or any high-risk label.\n When triggered, run `grill-with-docs` or `improve-codebase-architecture` and\n-record durable decisions in an ADR when needed (see `docs/adr/`).\n+record durable decisions in a Shiki SADR or target ADR as applicable (see `docs/adr/`).\n \n ## Safety\n \ndiff --git a/SYSTEM_PROMPT.md b/SYSTEM_PROMPT.md\nindex aaaea45e3a896c3a44ba89eec8bb84325ecf6c62..da009c86a88285269dee0155e58e550c6d744ea9\n--- a/SYSTEM_PROMPT.md\n+++ b/SYSTEM_PROMPT.md\n@@ -30,6 +30,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n Use the canonical source-of-truth order above. Runtime-specific reading orders\n are operational guidance only and must not override or reorder it. The current\n conversation is non-durable operator input.\n@@ -48,14 +51,14 @@\n \n A runtime may only do work that is explicitly assigned by the Goal, task, PR, or MergeGate decision.\n \n-Do not assume `openai/codex-action`, `OPENAI_API_KEY`, or API-key based Codex automation in the default Shiki path. API-key runners are explicit target-repo extensions and require their own ADR.\n+Do not assume `openai/codex-action`, `OPENAI_API_KEY`, or API-key based Codex automation in the default Shiki path. API-key runners are explicit target-repo extensions and require their own target ADR.\n \n ## 4. Non-negotiable Shiki flow\n \n For any non-trivial Goal, use this flow:\n \n 1. **Goal Seek**: define outcome, non-goals, risk level, completion criteria, and evidence requirements.\n-2. **grill-with-docs**: challenge the plan against `CONTEXT.md`, ADRs, code reality, terminology, boundaries, and edge scenarios. Ask one question at a time when the operator is available. If code can answer a question, inspect code instead of asking.\n+2. **grill-with-docs**: challenge the plan against `CONTEXT.md`, Shiki SADRs, target ADRs, code reality, terminology, boundaries, and edge scenarios. Ask one question at a time when the operator is available. If code can answer a question, inspect code instead of asking.\n 3. **Context & Impact**: identify modules, interfaces, seams, callers, dependencies, locks, risk, and verification surfaces.\n 4. **to-prd + Spec Freeze**: turn settled context into a PRD using domain vocabulary and testing decisions. The operator's explicit approval of the PRD is Spec Freeze, recorded as the plan's `spec_freeze` block; plans without it do not run. Steps 1-4 form Requirements Definition.\n 5. **to-issues**: decompose the PRD into independently grabbable vertical-slice issues. Prefer AFK slices; mark HITL slices when judgment is still required.\n@@ -75,7 +78,7 @@\n Before making any material change:\n \n 1. Read `AGENTS.md` and any runtime-specific wrapper such as `CLAUDE.md`.\n-2. Read `CONTEXT.md` and relevant ADRs.\n+2. Read `CONTEXT.md` and relevant SADRs and target ADRs.\n 3. Read the GitHub Issue / PR and `.shiki/` state for the active Goal or task.\n 4. Identify your role: Planner, Implementer, Reviewer, Completion Check Agent, Repairer, or Guardian-assist.\n 5. Identify Goal id, task id, branch, PR, dependencies, locks, risk level, required skills, and required evidence.\n@@ -103,7 +106,7 @@\n \n At minimum, identify:\n \n-- Relevant documents, ADRs, domain terms, and prior decisions.\n+- Relevant documents, SADRs, target ADRs, domain terms, and prior decisions.\n - Relevant modules, interfaces, seams, callers, owners, and tests.\n - Dependency relationships between tasks.\n - Candidate file locks and lock conflicts.\n@@ -152,7 +155,7 @@\n \n CCA must read:\n \n-- `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant ADRs.\n+- `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant SADRs and target ADRs.\n - Parent Goal issue, PRD issue, task issue, comments, and labels.\n - PR body, diff, commits, changed files, reviews, CI checks, and artifacts available to the workflow.\n - `.shiki/` task contract, locks, ledger entries, CCA checklist profile, and prior repair packets.\n@@ -214,7 +217,7 @@\n - Three or more new files.\n - Any high-risk or critical label.\n \n-When the architecture gate triggers, run `grill-with-docs` or `improve-codebase-architecture` as appropriate and record the decision in an ADR if it is hard to reverse or likely to be re-litigated.\n+When the architecture gate triggers, run `grill-with-docs` or `improve-codebase-architecture` as appropriate and record the decision in a Shiki SADR or target ADR as applicable if it is hard to reverse or likely to be re-litigated.\n \n ## 14. Skill Gate\n \n@@ -223,7 +226,7 @@\n Use these skills:\n \n - `setup-matt-pocock-skills`: before first use of the engineering skill set, or when issue tracker, triage labels, or domain docs are missing.\n-- `grill-with-docs`: default for non-trivial Goals before PRD/issues; required for ambiguous plans, terminology, boundaries, ADR-worthy tradeoffs, or design-tree decisions.\n+- `grill-with-docs`: default for non-trivial Goals before PRD/issues; required for ambiguous plans, terminology, boundaries, decision-record-worthy tradeoffs, or design-tree decisions.\n - `zoom-out`: unfamiliar code area or missing architectural map.\n - `to-prd`: convert settled context into a PRD.\n - `to-issues`: break a PRD, plan, or Goal into independently grabbable vertical-slice issues.\ndiff --git a/docs/adr/0001-keep-shiki-as-platform-name.md b/docs/adr/SADR-0001-keep-shiki-as-platform-name.md\nindex 20a9637c3b9d8c97de7ba8db7cfae4d82d9a7dba..8b685439959aada1b221c112f0ac3c51ca8a1695\n--- a/docs/adr/0001-keep-shiki-as-platform-name.md\n+++ b/docs/adr/SADR-0001-keep-shiki-as-platform-name.md\n@@ -1,3 +1,3 @@\n-# Keep Shiki As The Platform Name\n+# SADR-0001: Keep Shiki As The Platform Name\n \n Shiki remains the product name for the GitHub-native agentic engineering control plane. We considered more descriptive names such as MergePilot, MergeGate, and Kiroku, but kept Shiki because it carries the existing goal-seeking framework identity while remaining independent of Hermes, Codex, Claude Code, or any single runtime.\ndiff --git a/docs/adr/0002-use-agents-as-shared-constitution.md b/docs/adr/SADR-0002-use-agents-as-shared-constitution.md\nindex 73de30ecb078120eac540d9bc51855e3cd48d0e3..c5486182bf06fca36ea508496b013c67302fd989\n--- a/docs/adr/0002-use-agents-as-shared-constitution.md\n+++ b/docs/adr/SADR-0002-use-agents-as-shared-constitution.md\n@@ -1,3 +1,3 @@\n-# Use AGENTS As The Shared Agent Constitution\n+# SADR-0002: Use AGENTS As The Shared Agent Constitution\n \n Shiki uses `AGENTS.md` as the runtime-neutral constitution and `CLAUDE.md` as a thin Claude Code wrapper. This keeps Codex, Claude Code, Hermes Runner, and future Agent Runtimes aligned on the same GitHub-first MergeGate rules while still allowing runtime-specific instructions where needed.\ndiff --git a/docs/adr/0003-require-skill-gate-for-mergegate-readiness.md b/docs/adr/SADR-0003-require-skill-gate-for-mergegate-readiness.md\nindex 424be0c9af4c8f0136f1fde68a468c47ff84e764..7fc337987c7df9792f75f181dfcbed75fe5ffff2\n--- a/docs/adr/0003-require-skill-gate-for-mergegate-readiness.md\n+++ b/docs/adr/SADR-0003-require-skill-gate-for-mergegate-readiness.md\n@@ -1,3 +1,3 @@\n-# Require Skill Gate For MergeGate Readiness\n+# SADR-0003: Require Skill Gate For MergeGate Readiness\n \n Shiki requires relevant engineering skills to be invoked before execution when their triggers apply. This makes skills part of MergeGate readiness rather than optional prompt style, because Shiki depends on repeatable planning, issue decomposition, TDD, diagnosis, triage, and architecture review before autonomous or parallel agent work can be trusted.\ndiff --git a/docs/adr/0004-use-github-first-with-shiki-mirror.md b/docs/adr/SADR-0004-use-github-first-with-shiki-mirror.md\nindex d0a3a9174dc646a945a9d325954ffaf00c352040..a79c54ae9ee8eed2a0524192b864da4e22a92d3f\n--- a/docs/adr/0004-use-github-first-with-shiki-mirror.md\n+++ b/docs/adr/SADR-0004-use-github-first-with-shiki-mirror.md\n@@ -1,4 +1,4 @@\n-# ADR 0004: Use GitHub First With A Repository-Local Shiki Mirror\n+# SADR-0004: Use GitHub First With A Repository-Local Shiki Mirror\n \n ## Status\n \ndiff --git a/docs/adr/0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md b/docs/adr/SADR-0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\nindex b339baf49b89d9290e79c3d51b36e7b2bde3bd95..0f3c3eb9afccdb3403ce978e21d04fe12e495da0\n--- a/docs/adr/0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\n+++ b/docs/adr/SADR-0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\n@@ -1,8 +1,8 @@\n-# ADR 0005: Use Codex As Front And Claude Code Action As The GitHub Runtime\n+# SADR-0005: Use Codex As Front And Claude Code Action As The GitHub Runtime\n \n ## Status\n \n-Accepted (implementer default superseded by ADR 0008; the auth model and the\n+Accepted (implementer default superseded by SADR-0008; the auth model and the\n Claude Code Action GitHub runtime decision remain in force)\n \n ## Context\n@@ -26,4 +26,4 @@\n - Shiki avoids requiring `OPENAI_API_KEY` for the default workflow.\n - Codex work happens through the user's authenticated Codex front surface, not as a hidden backend Action.\n - Claude Code Action is configured for OAuth-token based subscription usage by default.\n-- API-key based automation remains possible only as an explicit target-repo extension with its own ADR.\n+- API-key based automation remains possible only as an explicit target-repo extension with its own target ADR.\ndiff --git a/docs/adr/0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md b/docs/adr/SADR-0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\nindex 4bcae9d1e52f0a37b8e9fbafa5cd14b6ee1993bd..7a38538a6ae9929567f0ade67e7ea6a60d0b3f7e\n--- a/docs/adr/0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\n+++ b/docs/adr/SADR-0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\n@@ -1,4 +1,4 @@\n-# ADR 0006: Separate Codex Implementation, CCA Completion, And MergeGate Authorization\n+# SADR-0006: Separate Codex Implementation, CCA Completion, And MergeGate Authorization\n \n ## Status\n \ndiff --git a/docs/adr/0007-packaging-and-release.md b/docs/adr/SADR-0007-packaging-and-release.md\nindex 79b84ddb97167372b44641e5b7860e400ce80822..bb445a2e2e856d9d1f5fdbe35de8d4e8cec0fb32\n--- a/docs/adr/0007-packaging-and-release.md\n+++ b/docs/adr/SADR-0007-packaging-and-release.md\n@@ -1,4 +1,4 @@\n-# ADR 0007: Packaging And Release For Shiki\n+# SADR-0007: Packaging And Release For Shiki\n \n ## Status\n \n@@ -25,7 +25,7 @@\n   not introduce third-party runtime dependencies or break the shim.\n - Releases must run inside the operator's subscription-authenticated toolchain\n   and must not require extra secrets beyond the built-in `GITHUB_TOKEN`\n-  (consistent with ADR 0005's no-API-key-by-default stance).\n+  (consistent with SADR-0005's no-API-key-by-default stance).\n - `.shiki/manifest.json` already encodes the install surface (`install.include`,\n   `install.create_directories`, `install.exclude_from_commit`), and it must not\n   be mutated as part of this change.\n@@ -34,7 +34,7 @@\n \n This is a hard-to-reverse platform decision (it sets the version source of\n truth, the release trigger, and the public install contract), so it is recorded\n-as an ADR.\n+as a SADR.\n \n ## Decision\n \n@@ -104,7 +104,7 @@\n 3. **Publishing to PyPI.** Rejected for now: Shiki is distributed as a\n    repository template + git-installable CLI, not a PyPI library; PyPI would add\n    credentials/secrets and a different versioning contract. Can be added later as\n-   its own ADR if a packaged distribution is needed.\n+   its own SADR if a packaged distribution is needed.\n 4. **Dynamic version (read `VERSION` via a build backend `dynamic` field).**\n    Rejected to keep `pyproject.toml` backend-agnostic and dependency-free; the\n    small duplication between `VERSION` and `pyproject.toml` is guarded by the\n@@ -123,4 +123,4 @@\n   releasing versions the whole platform but ships only `install.include` to\n   targets.\n - Future PyPI distribution or a console entry point are deferred and would each\n-  warrant their own ADR.\n+  warrant their own SADR.\ndiff --git a/docs/adr/0008-use-claude-code-as-default-implementer.md b/docs/adr/SADR-0008-use-claude-code-as-default-implementer.md\nindex dedf4510b7c7799521422f622029563d373db031..35c2625d908167fbc58855b5b63e283805f167e8\n--- a/docs/adr/0008-use-claude-code-as-default-implementer.md\n+++ b/docs/adr/SADR-0008-use-claude-code-as-default-implementer.md\n@@ -1,12 +1,12 @@\n-# ADR 0008: Use Claude Code As The Default Implementer Runtime\n+# SADR-0008: Use Claude Code As The Default Implementer Runtime\n \n ## Status\n \n-Proposed (supersedes ADR 0005 for the implementer default; keeps its auth model)\n+Proposed (supersedes SADR-0005 for the implementer default; keeps its auth model)\n \n ## Context\n \n-ADR 0005 made Codex Front the default implementation runtime, with Claude Code\n+SADR-0005 made Codex Front the default implementation runtime, with Claude Code\n restricted to planner and reviewer roles by the runtime registry, and the only\n autonomous implementation adapter was `shiki runner codex`.\n \n@@ -18,13 +18,13 @@\n - The platform's planning, review, and CCA layers already run on Claude Code,\n   and Claude Code's native capabilities (the built-in `/code-review` skill and\n   the Workflow multi-agent orchestration tool) have no Codex equivalent.\n-- The Goal lifecycle redesign (ADR 0009) requires an autonomous post-freeze\n+- The Goal lifecycle redesign (SADR-0009) requires an autonomous post-freeze\n   loop driven from the operator's Claude Code session, including bounded\n   repair dispatch, which must not depend on a runtime that cannot be invoked\n   unattended.\n \n The constitution claims Shiki is runtime-agnostic (\"not a Claude-only\n-workflow\", ADR 0002). Any change to the implementer default must preserve the\n+workflow\", SADR-0002). Any change to the implementer default must preserve the\n registry-based runtime model rather than hardcoding one vendor.\n \n ## Decision\n@@ -39,7 +39,7 @@\n   the task to review or repair-needed.\n - Keep Codex registered as an optional implementer runtime. `shiki runner\n   codex`, the Codex handoff templates, and the ChatGPT OAuth auth model from\n-  ADR 0005 remain supported for tasks explicitly assigned to `codex`.\n+  SADR-0005 remain supported for tasks explicitly assigned to `codex`.\n - Commit to mandating two Claude-native capabilities as loop contract in\n   follow-up Goals (status: the code-review gate was delivered by Goal G-B \u2014\n   skill registry, checklists item PR-12, default required_skills \u2014 while the\n@@ -60,7 +60,7 @@\n \n Out of scope: removing Codex support, changing the CCA runtime (Claude Code\n Action with `CLAUDE_CODE_OAUTH_TOKEN` stays), and API-key based automation\n-(still requires its own ADR per ADR 0005).\n+(still requires its own target ADR per SADR-0005).\n \n ## Consequences\n \n@@ -100,7 +100,7 @@\n   harness rejection of direct `codex exec` is outside Shiki's control, and the\n   plan's mandatory native capabilities cannot run on Codex at all.\n - **Remove Codex entirely**: rejected; it abandons the runtime-agnostic\n-  constitution (ADR 0002) for no operational gain and forecloses\n+  constitution (SADR-0002) for no operational gain and forecloses\n   cross-runtime adversarial review.\n - **GitHub Actions cloud implementation via claude-code-action**: rejected as\n   default; durable evidence is automatic but token cost, Node 24 workflow\ndiff --git a/docs/adr/0009-requirements-definition-and-spec-freeze.md b/docs/adr/SADR-0009-requirements-definition-and-spec-freeze.md\nindex 29b3f7871bef9203a8b4a831a65c39eb2f506e42..52e7dd69ad0aa4efe0b68c62678abbf7df02aaec\n--- a/docs/adr/0009-requirements-definition-and-spec-freeze.md\n+++ b/docs/adr/SADR-0009-requirements-definition-and-spec-freeze.md\n@@ -1,4 +1,4 @@\n-# ADR 0009: Merge Grill And Planning Into Requirements Definition With Spec Freeze\n+# SADR-0009: Merge Grill And Planning Into Requirements Definition With Spec Freeze\n \n ## Status\n \ndiff --git a/docs/adr/0010-external-ai-guardian-review.md b/docs/adr/SADR-0010-external-ai-guardian-review.md\nindex e6108bbe8bb4e73e0ff4684c8f78001a80438c4e..f3510b145664298adafb151b6f0db51b972f01ec\n--- a/docs/adr/0010-external-ai-guardian-review.md\n+++ b/docs/adr/SADR-0010-external-ai-guardian-review.md\n@@ -1,4 +1,4 @@\n-# ADR 0010: External AI Guardian Review As A First-Class Approval Authority\n+# SADR-0010: External AI Guardian Review As A First-Class Approval Authority\n \n ## Status\n \n@@ -8,7 +8,7 @@\n \n Shiki is intended to be an autonomous, recursive, self-improving agent\n framework: authority-in-the-loop, not human-in-the-loop. Until now the Guardian\n-approval required for high/critical-risk merges (guardian-policy.json, ADR\n+approval required for high/critical-risk merges (guardian-policy.json, SADR\n governance) recognized only human authority sources \u2014 a GitHub review approval,\n the `guardian:approved` label applied by a configured Guardian, or a comment\n carrying `Guardian approval granted` plus the current head SHA.\n@@ -89,7 +89,7 @@\n   autonomous design the operator intends and produced the impersonation\n   incident.\n - **Record AI approval as an operator comment**: rejected as forgery of human\n-  authority \u2014 the exact failure this ADR corrects.\n+  authority \u2014 the exact failure this SADR corrects.\n - **Deliver the artifact as a committed `.shiki/` file**: rejected; a committed\n   approval inside the PR being approved is forgeable by anyone who can push.\n - **A dedicated reviewer bot identity instead of a relayed comment**: deferred;\ndiff --git a/docs/adr/0011-autonomous-quality-gate-evidence.md b/docs/adr/SADR-0011-autonomous-quality-gate-evidence.md\nindex bca0246c5d46e44afbde15486a75eec425fc02f5..9c93eac16d8c3a6d4b11bd7224e3955102cd7db4\n--- a/docs/adr/0011-autonomous-quality-gate-evidence.md\n+++ b/docs/adr/SADR-0011-autonomous-quality-gate-evidence.md\n@@ -1,4 +1,4 @@\n-# ADR 0011: Autonomous Quality-Gate Evidence \u2014 Deterministic Facts And An Independent Verifier\n+# SADR-0011: Autonomous Quality-Gate Evidence \u2014 Deterministic Facts And An Independent Verifier\n \n ## Status\n \ndiff --git a/docs/adr/0012-autonomous-loop-closeout-to-main.md b/docs/adr/SADR-0012-autonomous-loop-closeout-to-main.md\nindex 637fad6dde4989c9c2bf0f81f676d8d29991123c..ec7d85f81030a676bcbba28b43eb365422a36eac\n--- a/docs/adr/0012-autonomous-loop-closeout-to-main.md\n+++ b/docs/adr/SADR-0012-autonomous-loop-closeout-to-main.md\n@@ -1,4 +1,4 @@\n-# 0012 \u2014 Autonomous loop pushes goal completion to main via a closeout PR\n+# SADR-0012 \u2014 Autonomous loop pushes goal completion to main via a closeout PR\n \n - Status: accepted\n - Date: 2026-06-18\ndiff --git a/docs/adr/0013-bootstrap-grants-actions-approve-pull-requests.md b/docs/adr/SADR-0013-bootstrap-grants-actions-approve-pull-requests.md\nindex b6c69d64f1c5ced346b88d6859586715b3b5bd0e..cf48ff4a6716267f539ca58edd1a852e7c7a2d64\n--- a/docs/adr/0013-bootstrap-grants-actions-approve-pull-requests.md\n+++ b/docs/adr/SADR-0013-bootstrap-grants-actions-approve-pull-requests.md\n@@ -1,4 +1,4 @@\n-# ADR 0013: Bootstrap Grants GitHub Actions Permission To Create And Approve Pull Requests\n+# SADR-0013: Bootstrap Grants GitHub Actions Permission To Create And Approve Pull Requests\n \n ## Status\n \n@@ -31,7 +31,7 @@\n security tradeoff: it lets repository automation satisfy a human-review gate. In\n a multi-maintainer repo that could let a compromised or buggy workflow\n self-approve changes. So whether bootstrap turns it on by default \u2014 and with\n-what default token scope \u2014 is an ADR-worthy decision, not an implementation\n+what default token scope \u2014 is a SADR-worthy decision, not an implementation\n detail.\n \n ## Decision\n@@ -48,7 +48,7 @@\n Specifically:\n \n - **`can_approve_pull_request_reviews` defaults to `true`.** Shiki's default\n-  posture is solo/AFK autonomous operation (ADR 0008/0009/0012), where the CCA\n+  posture is solo/AFK autonomous operation (SADR-0008/0009/0012), where the CCA\n   Review Bridge is the only path that can satisfy `required_review: true` after\n   the CCA and Guardian gates have already passed. The default makes a\n   freshly-bootstrapped repo able to run the autonomous loop end to end without a\ndiff --git a/docs/adr/0014-codex-app-external-ai-guardian-ui-adapter.md b/docs/adr/SADR-0014-codex-app-external-ai-guardian-ui-adapter.md\nindex 238db06279334caa341c117362df9271382a7fcd..65f12e9939321e1b3e389a34c822fc1549e5155d\n--- a/docs/adr/0014-codex-app-external-ai-guardian-ui-adapter.md\n+++ b/docs/adr/SADR-0014-codex-app-external-ai-guardian-ui-adapter.md\n@@ -1,4 +1,4 @@\n-# ADR 0014: Use Codex App As The External AI Guardian UI Adapter\n+# SADR-0014: Use Codex App As The External AI Guardian UI Adapter\n \n ## Status\n \ndiff --git a/docs/adr/0015-contract-approval-before-dispatch.md b/docs/adr/SADR-0015-contract-approval-before-dispatch.md\nindex fad6c656741a20a7684ba16080a7db159f63a52f..6061271a46d3f1d6ae7174477de3fb75673123a6\n--- a/docs/adr/0015-contract-approval-before-dispatch.md\n+++ b/docs/adr/SADR-0015-contract-approval-before-dispatch.md\n@@ -1,4 +1,4 @@\n-# ADR 0015: Approve The Task Contract Before Dispatch, Not The Diff After It\n+# SADR-0015: Approve The Task Contract Before Dispatch, Not The Diff After It\n \n ## Status\n \n@@ -9,7 +9,7 @@\n Guardian approval for high/critical risk is expressible only on a pull request.\n Every approval source in `guardian-policy.json` \u2014 GitHub review, the\n `guardian:approved` label, the `Guardian approval granted` comment, and the\n-`external_ai_guardian_review` artifact (ADR 0010/0014) \u2014 is bound to a PR, and\n+`external_ai_guardian_review` artifact (SADR-0010/0014) \u2014 is bound to a PR, and\n three of the four require the current head SHA. Approval therefore cannot exist\n before an implementation PR exists.\n \n@@ -81,7 +81,7 @@\n An envelope breach must surface only on the non-repairable policy gate, never on\n the repairable `MergeGate metadata check`. Otherwise the Goal Loop routes it to\n `dispatch_repair` \u2014 instructing a runner to make the breach go away \u2014 which is\n-exactly what this ADR forbids.\n+exactly what this SADR forbids.\n \n **Contract immutability.** After registration, an implementation PR must not\n change the governance fields of its own task contract (`scope`, `non_goals`,\n@@ -116,7 +116,7 @@\n fails any verdict that is not `complete`, and the signal the CCA prompt reads is\n computed from `evaluate_guardian_approval` alone. Without the OR-merge at the\n signal, a contract-approved PR still returns `needs_guardian`, the CCA job fails,\n-and the MergeGate policy check never runs \u2014 the gate this ADR changes would never\n+and the MergeGate policy check never runs \u2014 the gate this SADR changes would never\n be reached. The CCA *prompt* needs no change: a `contract_approval` entry in the\n signal's existing `sources` array satisfies its current wording.\n \n@@ -130,7 +130,7 @@\n written, and an approved contract lets its implementation PRs merge through the\n same automatic path as low/medium risk. The wasted implement-then-block cycle\n disappears. Guardian review shifts from reading diffs to reading contracts, which\n-is both earlier and smaller; the external AI Guardian path (ADR 0010/0014) works\n+is both earlier and smaller; the external AI Guardian path (SADR-0010/0014) works\n on contracts with no change.\n \n We accept that the Guardian authorizes a class of change without seeing its\n@@ -146,7 +146,7 @@\n adversarial tests for the new approval path equivalent in rigor to\n `test_shiki_governance_evidence.sh`.\n \n-Bootstrap costs, which apply only while this ADR is being implemented. The tasks\n+Bootstrap costs, which apply only while this SADR is being implemented. The tasks\n that build Contract Approval merge under the existing rules, so the ones\n dispatched after the pre-dispatch gate lands but before the approval path lands\n need a Guardian-approved Contract PR *and* a live Guardian approval on their\n@@ -210,7 +210,7 @@\n risk; they do not bound authorized work.\n \n **Auto-escalate risk on envelope breach instead of stopping.** Returns the\n-workflow to a diff-time Guardian decision \u2014 the exact behavior this ADR removes.\n+workflow to a diff-time Guardian decision \u2014 the exact behavior this SADR removes.\n \n **Resolve governance fields from the base snapshot only.** Rejected against the\n registration flow: implementation PRs add their own contract, so the base has no\ndiff --git a/docs/adr/0016-locks-bound-product-paths.md b/docs/adr/SADR-0016-locks-bound-product-paths.md\nindex b5e850f3f1f7f4ef6e7f9d1763a29a51bd300c45..46b40a9b99a7aae644c02bb458f5473ff0a90ce2\n--- a/docs/adr/0016-locks-bound-product-paths.md\n+++ b/docs/adr/SADR-0016-locks-bound-product-paths.md\n@@ -1,4 +1,4 @@\n-# ADR 0016: Locks bound product paths; the Shiki mirror is bound by record identity\n+# SADR-0016: Locks bound product paths; the Shiki mirror is bound by record identity\n \n ## Status\n \n@@ -6,8 +6,8 @@\n \n - Date: 2026-07-28\n - Deciders: operator (mizutani-140), Claude (planner)\n-- Related: ADR 0004 (GitHub-first with a `.shiki` mirror), ADR 0009 (Spec Freeze),\n-  ADR 0012 (autonomous loop closeout to main), ADR 0015 (Contract Approval before\n+- Related: SADR-0004 (GitHub-first with a `.shiki` mirror), SADR-0009 (Spec Freeze),\n+  SADR-0012 (autonomous loop closeout to main), SADR-0015 (Contract Approval before\n   dispatch), remediation goal task R-02, the live PR #179 deadlock.\n \n ## Context\n@@ -90,11 +90,11 @@\n files, documentation files and named source modules are nameable; runtime-\n allocated ledger, runner and report ids are not \u2014 and those live in `.shiki`,\n which locks no longer govern. Existing frozen contracts are brought into line by\n-a one-time Spec Amendment per plan, which must land before ADR 0015's contract\n+a one-time Spec Amendment per plan, which must land before SADR-0015's contract\n immutability makes `locks` unwritable from the implementation PR.\n \n **Lock state is resolved from the durable side.** MergeGate resolves the lock\n-registry, and ADR 0015's governance fields, from the base `.shiki` snapshot when\n+registry, and SADR-0015's governance fields, from the base `.shiki` snapshot when\n present, falling back to head only when absent. The `MergeGate metadata check`\n job is given `--base-shiki`, which it does not receive today. Locks are not\n derived from the live open-pull-request set: a pull request body is untrusted\n@@ -108,7 +108,7 @@\n Explicitly out of scope: moving MergeGate's contract resolution off the PR\n checkout wholesale; making the mirror a projection of GitHub state; introducing\n new lock kinds; moving ledger evidence out of the repository; and unwinding\n-ADR 0012's closeout pull request. Registration of multi-task goals is ADR 0015's\n+SADR-0012's closeout pull request. Registration of multi-task goals is SADR-0015's\n Contract PR and is not re-decided here.\n \n ## Consequences\n@@ -127,7 +127,7 @@\n \n Risks accepted. First, goal-scoped identity is only as strong as `goal_id`,\n which is resolved from the PR body and the head task file; the mitigation is to\n-add `goal_id` to ADR 0015's frozen governance field set, and until that lands a\n+add `goal_id` to SADR-0015's frozen governance field set, and until that lands a\n task file can name a foreign goal. Second, base-when-present resolution is\n unavailable on fork pull requests, where the head is contributor-controlled;\n fork pull requests already cannot complete a Shiki merge, and this decision does\n@@ -139,7 +139,7 @@\n path, not by implementation pull requests.\n \n New obligations: one Spec Amendment per frozen plan to rewrite `locks`, ordered\n-before ADR 0015 T1; extension of the adversarial suites in\n+before SADR-0015 T1; extension of the adversarial suites in\n `scripts/test_shiki_adversarial_state.sh` and\n `scripts/test_shiki_governance_evidence.sh` to cover the eleven newly-scoped\n directories; and updating `scripts/test_shiki_loop_lock_guard.sh`,\n@@ -151,32 +151,32 @@\n whether the loop's transitive sync of ledger `evidence` references can be\n deleted once the Contract PR carries its own source plan.\n \n-## Relationship to ADR 0004\n+## Relationship to SADR-0004\n \n-This ADR **clarifies something ADR 0004 left unspecified**. ADR 0004 decides that\n+This SADR **clarifies something SADR-0004 left unspecified**. SADR-0004 decides that\n GitHub is the operational source of truth and that each target repository keeps a\n `.shiki/` mirror. It says nothing about pull requests writing the mirror, does\n not name MergeGate, and does not make the mirror an input to a gate. Nothing in\n-ADR 0004's Decision or Consequences is amended, superseded or weakened: the\n+SADR-0004's Decision or Consequences is amended, superseded or weakened: the\n mirror still exists, still records the same artifacts, and pull request\n-boundaries remain the coordination surface. What this ADR settles is the question\n+boundaries remain the coordination surface. What this SADR settles is the question\n 0004 never reached \u2014 by what authority a pull request may write a mirror record \u2014\n and it answers it the way `.shiki/manifest.json` already declares\n (`pr_mutation: current-task-or-current-goal-only`).\n \n-It **amends ADR 0012**, whose Decision item 5 states that \"Loop-executed tasks\n+It **amends SADR-0012**, whose Decision item 5 states that \"Loop-executed tasks\n declare the `path:.shiki/**` lock, which covers every `.shiki` file the closeout\n stages ... so `files_outside_locks` is satisfied without per-file lock\n bookkeeping.\" That clause is replaced: the closeout's mirror writes are\n-authorized by record identity, not by a declared lock. ADR 0012's substantive\n+authorized by record identity, not by a declared lock. SADR-0012's substantive\n decision \u2014 that completion reaches `main` through a closeout pull request rather\n than local attestation \u2014 is preserved unchanged.\n \n-It is **complementary to ADR 0015**, not a substitute. ADR 0015 owns contract\n+It is **complementary to SADR-0015**, not a substitute. SADR-0015 owns contract\n immutability, the pre-dispatch gate, the Contract PR and the Scope Envelope. This\n-ADR owns what a lock means and who may write the mirror. The one ordering\n+SADR owns what a lock means and who may write the mirror. The one ordering\n constraint between them is that the lock-narrowing Spec Amendment must precede\n-ADR 0015 T1's freezing of the `locks` field.\n+SADR-0015 T1's freezing of the `locks` field.\n \n ## Alternatives Considered\n \n@@ -186,7 +186,7 @@\n the only writer that can register them may run. Migrating the Guardian signal to\n that store makes every registration PR fail closed to `needs_guardian` at every\n risk level, because the task file is by definition absent from the store, which\n-removes the low/medium autonomous path ADR 0008/0009 exists to provide. And it\n+removes the low/medium autonomous path SADR-0008/0009 exists to provide. And it\n leaves schemas, manifest, config, guardian policy, templates, migrations, plans,\n worktrees, runner records, runs and handoffs with no writer at all in a\n repository whose product is `.shiki`. Measured benefit on the frozen backlog:\n@@ -199,14 +199,14 @@\n namespace is not in fact ownership-partitioned \u2014 56 ledger entries carry no\n `task_id`, and one goal DAG is writable by 35 registered tasks. The correct\n expression of its insight is that locks do not govern the mirror at all, which is\n-what this ADR decides.\n+what this SADR decides.\n \n **Make the mirror a projection written only by a registration writer and a\n post-merge projection job.** Rejected. It requires a bypass actor with write\n access to the protected default branch; it makes its own master invariant\n readable from a manifest that pull requests may edit; it deletes both reconcile\n modes and leaves a corrupted mirror with no in-band repair path, contradicting\n-ADR 0004's consequence that mirror conflicts are repaired against GitHub state;\n+SADR-0004's consequence that mirror conflicts are repaired against GitHub state;\n and its content-derived ids are rejected by `validate_shiki`'s currently-green id\n pattern.\n \ndiff --git a/docs/adr/0017-bookkeeping-closeouts-do-not-inherit-risk.md b/docs/adr/SADR-0017-bookkeeping-closeouts-do-not-inherit-risk.md\nindex 554e3a0d526a72ca067108c5464cc68c3edcf174..b9676857a4713850e063af7c39c8e0b01ecb4c66\n--- a/docs/adr/0017-bookkeeping-closeouts-do-not-inherit-risk.md\n+++ b/docs/adr/SADR-0017-bookkeeping-closeouts-do-not-inherit-risk.md\n@@ -1,4 +1,4 @@\n-# ADR 0017: A Bookkeeping Closeout Does Not Inherit Its Task's Risk\n+# SADR-0017: A Bookkeeping Closeout Does Not Inherit Its Task's Risk\n \n ## Status\n \n@@ -6,7 +6,7 @@\n \n ## Context\n \n-ADR 0012 made the goal loop push a task's completion to the default branch through a\n+SADR-0012 made the goal loop push a task's completion to the default branch through a\n closeout pull request: after the implementation PR merges, the loop opens a second PR\n carrying `task.status = done`, `lock.state = released`, and \u2014 when the task completes\n its goal \u2014 `goal.status = complete` plus one scorecard report. Nothing else. The\n@@ -49,14 +49,14 @@\n 4. The task file's governance fields (`scope`, `non_goals`, `required_skills`,\n    `risk_level`, `locks`, `acceptance_checks`, `test_command`) are byte-identical to\n    the base snapshot. Only `status`, `expected_pr`, `closeout_pr`, `ledger_evidence`\n-   and `expected_branch` may differ \u2014 the ADR 0015 contract-immutability rule,\n+   and `expected_branch` may differ \u2014 the SADR-0015 contract-immutability rule,\n    unchanged.\n \n    `expected_branch` is in that set because a closeout is *by construction* on a\n    different branch from the implementation it closes, and the loop rewrites the\n    field accordingly. It is safe to let it differ because MergeGate independently\n    requires `expected_branch` to equal the PR's actual head ref, so a PR cannot set\n-   it to anything other than the branch it is really on. The first draft of this ADR\n+   it to anything other than the branch it is really on. The first draft of this SADR\n    omitted it, which made the exemption unreachable: every real closeout failed\n    condition 4 on a field the loop always changes. See PR #209.\n 5. The state transitions are exactly the terminal set: task `review` \u2192 `done`, lock\n@@ -113,12 +113,12 @@\n classification must be derived, not asserted.\n \n **Fold the closeout into the implementation PR** so no second PR exists. Rejected under\n-ADR 0012, which recorded why: a task file that is already `done` on the implementation\n+SADR-0012, which recorded why: a task file that is already `done` on the implementation\n branch short-circuits the loop's review \u2192 CCA \u2192 merge \u2192 done ordering into a local\n goal-completion before any merge happens.\n \n **Have the loop auto-merge closeouts with elevated credentials.** Rejected outright: that\n-is a runtime authoring its own approval, which ADR 0010 and the constitution's\n+is a runtime authoring its own approval, which SADR-0010 and the constitution's\n approval-record prohibition forbid. It would also be indistinguishable, in the evidence,\n from the forgery those rules exist to prevent.\n \ndiff --git a/docs/adr/0018-carried-guardian-approval-across-a-proven-base-sync.md b/docs/adr/SADR-0018-carried-guardian-approval-across-a-proven-base-sync.md\nindex 22358562da390ed8b45cd600da08a43d378b4680..e6e2907cefa776460af73ddf7d39db605f3c39b0\n--- a/docs/adr/0018-carried-guardian-approval-across-a-proven-base-sync.md\n+++ b/docs/adr/SADR-0018-carried-guardian-approval-across-a-proven-base-sync.md\n@@ -1,4 +1,4 @@\n-# ADR 0018: Carried Guardian Approval Across A Proven Base Sync\n+# SADR-0018: Carried Guardian Approval Across A Proven Base Sync\n \n ## Status\n \n@@ -34,7 +34,7 @@\n Guardian evaluator that consumes it (`shiki_guardian.evaluate_guardian_approval`,\n the `guardian_comment_carried` source) were built and tested in isolation. Neither\n does anything until a caller passes the flags that turn the carry on, so the\n-feature was invisible in CI. This ADR records the decision the two of them\n+feature was invisible in CI. This SADR records the decision the two of them\n implement, and this task wires it into the four CI invocations that judge Guardian\n approval.\n \ndiff --git a/docs/adr/README.md b/docs/adr/README.md\nindex c70e057a4520c36e042ab68c53cd4a21ef709b71..5da43856836e2ce6a150f8127766e2f88355ab4b\n--- a/docs/adr/README.md\n+++ b/docs/adr/README.md\n@@ -1,40 +1,41 @@\n-# Architecture Decision Records\n+# Decision Record Namespaces\n \n-This directory holds Shiki's Architecture Decision Records (ADRs): durable\n-records of hard-to-reverse platform decisions. ADRs are part of the Shiki source\n-of truth alongside GitHub state, the `.shiki/` mirror, and `CONTEXT.md`.\n+This directory holds two independent decision namespaces:\n \n-## How ADRs Work\n+- Shiki Architecture Decision Records (SADRs), named\n+  `SADR-NNNN-<short-slug>.md`, record hard-to-reverse Shiki platform decisions.\n+- Target Architecture Decision Records (ADRs), named\n+  `NNNN-<short-slug>.md`, belong to the target product.\n \n-- New ADRs are added as `docs/adr/NNNN-<short-slug>.md`, where `NNNN` is the next\n-  zero-padded sequence number.\n-- The **canonical list of ADRs is the set of files matching `docs/adr/*.md`** in\n-  this directory. Browse `docs/adr/*.md` for the authoritative, current set.\n-- Use [`template.md`](template.md) as the starting point for a new ADR.\n-- Record an ADR when a decision is hard to reverse, surprising without context,\n-  and a real tradeoff. Do not write an ADR for routine or easily reversible\n-  choices.\n+The same four-digit number may appear once in each namespace. A platform\n+`SADR-NNNN` citation resolves only to `SADR-NNNN-*.md`; a target `ADR NNNN`\n+citation resolves only to `NNNN-*.md`.\n \n-Using the `docs/adr/*.md` glob as the source of truth means other PRs can add an\n-ADR without editing this index file, avoiding merge contention on a\n-hand-maintained enumeration.\n+## How Decision Records Work\n \n-## Current ADRs (snapshot)\n-\n-The list below is a convenience snapshot and may lag behind the directory. When\n-in doubt, list `docs/adr/*.md`.\n+- New Shiki platform decisions use the next zero-padded SADR number.\n+- New target product decisions use the target's own ADR sequence.\n+- The canonical list is the files in `docs/adr/`; no hand-maintained index is\n+  authoritative.\n+- Use [`template.md`](template.md) as the starting point and choose the filename\n+  namespace that owns the decision.\n+- Record a decision when it is hard to reverse, surprising without context, and\n+  a real tradeoff. Do not write a decision record for routine or easily\n+  reversible choices.\n \n-- [0001 \u2014 Keep Shiki As The Platform Name](0001-keep-shiki-as-platform-name.md)\n-- [0002 \u2014 Use AGENTS As The Shared Agent Constitution](0002-use-agents-as-shared-constitution.md)\n-- [0003 \u2014 Require Skill Gate For MergeGate Readiness](0003-require-skill-gate-for-mergegate-readiness.md)\n-- [0004 \u2014 Use GitHub First With A Repository-Local Shiki Mirror](0004-use-github-first-with-shiki-mirror.md)\n-- [0005 \u2014 Use Codex As Front And Claude Code Action As The GitHub Runtime](0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md)\n-- [0006 \u2014 Separate Codex Implementation, CCA Completion, And MergeGate Authorization](0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md)\n+Using the directory as the source of truth lets independent changes add records\n+without contending on an enumerated index.\n \n-## Adding A New ADR\n+## Adding A Shiki SADR\n \n-1. Copy `template.md` to `docs/adr/NNNN-<short-slug>.md`.\n+1. Copy `template.md` to `docs/adr/SADR-NNNN-<short-slug>.md`.\n 2. Fill in the title, status, context, decision, and consequences.\n-3. Set the status to `Proposed`, then `Accepted` once the decision is approved.\n-4. Open a PR following `CONTRIBUTING.md`. You do not need to edit this index for\n-   the ADR to be discoverable via `docs/adr/*.md`.\n+3. Set the status to `Proposed`, then `Accepted` once the proper authority has\n+   approved the decision.\n+4. Open a PR following `CONTRIBUTING.md`.\n+\n+## Adding A Target ADR\n+\n+1. Copy `template.md` to `docs/adr/NNNN-<short-slug>.md` using the target's own\n+   sequence.\n+2. Cite it as `ADR NNNN`; do not prefix its filename with `SADR-`.\ndiff --git a/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md b/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\nnew file mode 100644\nindex 0000000000000000000000000000000000000000..7455ce1f3bed02ddddb851ac21a1d1a648c9aeba\n--- /dev/null\n+++ b/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\n@@ -0,0 +1,55 @@\n+# SADR-0019: Separate Shiki Platform SADRs From Target ADRs\n+\n+## Status\n+\n+Accepted\n+\n+## Context\n+\n+Shiki installs its platform decision records into `docs/adr/` in every target\n+repository. Shiki and its targets previously used the same numeric\n+`NNNN-<slug>.md` namespace. A target that already owned ADRs 0013 through 0018\n+therefore acquired two records for each number during installation, making an\n+`ADR NNNN` citation ambiguous. Renumbering target decisions would rewrite target\n+history, while renumbering Shiki decisions per target would destroy their stable\n+platform identity.\n+\n+Upgrades also need to remove the former Shiki-owned numeric paths without ever\n+mistaking a target-owned ADR for platform content. The install stamp already\n+records exact shipped paths and content digests, so it can prove ownership only\n+when both still match.\n+\n+## Decision\n+\n+Shiki platform decisions use the `SADR` namespace:\n+\n+- records are named `docs/adr/SADR-NNNN-<slug>.md`;\n+- live platform citations use `SADR-NNNN`;\n+- target product decisions retain `docs/adr/NNNN-<slug>.md` and `ADR NNNN`;\n+- the reference validator resolves the two namespaces independently, so the same\n+  four-digit number may exist once in each namespace.\n+\n+Before a forced upgrade writes anything, the installer inventories the exact\n+former numeric Shiki ADR paths. It may delete a legacy path only when the\n+existing install stamp contains that exact path and its recorded SHA-256 digest\n+matches the file currently on disk. All ownership blockers are reported\n+together and abort the upgrade before its first write. A non-forced install\n+never performs this cleanup.\n+\n+Historical `.shiki/` evidence remains unchanged because it records the namespace\n+that existed when that evidence was produced.\n+\n+## Consequences\n+\n+- Targets may use any ADR number without colliding with Shiki's platform SADRs.\n+- Fresh installs preserve target ADR bytes and install a stable SADR set.\n+- Forced upgrades fail closed when legacy ownership cannot be proven, and a\n+  successful upgrade refreshes the stamp without removed legacy paths.\n+- Current governance, packaging, workflow, prompt, script, and skill references\n+  must move with the platform records.\n+\n+## Alternatives Considered\n+\n+- Collision-only refusal leaves affected targets permanently unupgradable.\n+- Renumbering target ADRs mutates target-owned history.\n+- Per-target Shiki renumbering makes platform citations non-portable.\ndiff --git a/docs/adr/template.md b/docs/adr/template.md\nindex a92a97daddd0b00ab9dd626cc8d86942da2eafed..17dd79175b5fee2105935557f691b40cf9492942\n--- a/docs/adr/template.md\n+++ b/docs/adr/template.md\n@@ -1,14 +1,14 @@\n-# NNNN: <Short Decision Title>\n+# <SADR-NNNN or NNNN>: <Short Decision Title>\n \n ## Status\n \n-Proposed | Accepted | Superseded by ADR-NNNN\n+Proposed | Accepted | Superseded by SADR-NNNN or target ADR NNNN\n \n ## Context\n \n What is the situation, constraint, or force that requires a decision? Reference\n-relevant domain terms (`CONTEXT.md`), prior ADRs, and code reality. State the\n-problem, not the solution.\n+relevant domain terms (`CONTEXT.md`), prior Shiki SADRs or target ADRs, and code\n+reality. State the problem, not the solution.\n \n ## Decision\n \ndiff --git a/docs/agents/autonomous-loop-e2e-acceptance.md b/docs/agents/autonomous-loop-e2e-acceptance.md\nindex bbf84c6081987f159ac6ad9ecf3c82b7aa72e7e8..5c13c1d02a05a90b0dc46cbef7c95a9846d0c237\n--- a/docs/agents/autonomous-loop-e2e-acceptance.md\n+++ b/docs/agents/autonomous-loop-e2e-acceptance.md\n@@ -40,8 +40,8 @@\n 3. `gh auth status` is logged in with merge permission on the repo.\n 4. Branch protection + required checks are configured exactly as the goal-loop\n    required-check set expects (see `docs/agents/branch-protection-smoke.md`).\n-5. The repo `.shiki/policy` permits autonomous low/medium-risk merges (ADR\n-   0008/0009); the MergeGate policy check is the recorded-authority gate.\n+5. The repo `.shiki/policy` permits autonomous low/medium-risk merges\n+   (SADR-0008/0009); the MergeGate policy check is the recorded-authority gate.\n \n The operator's only actions are: register the task and start the loop. **No\n manual pre-clearing** means after `shiki loop run` starts, the operator does not\ndiff --git a/docs/agents/bootstrap-command.md b/docs/agents/bootstrap-command.md\nindex 05f7f203a52cf412b9f97084a6e131ed0f785afd..f7aa0b734dfebee4a3b139335b66ee3415f48713\n--- a/docs/agents/bootstrap-command.md\n+++ b/docs/agents/bootstrap-command.md\n@@ -189,7 +189,9 @@\n ```\n \n Use this only for tests, fixtures, or explicit local-only template inspection.\n-Use `--force` only when you intentionally want to overwrite existing target files.\n+Use `--force` only when you intentionally want to refresh the target template.\n+Legacy numeric Shiki decision cleanup is stamp-and-digest authorized before any\n+write; target ADR paths are not cleanup candidates.\n \n ## Slash Command\n \ndiff --git a/docs/agents/checklists.md b/docs/agents/checklists.md\nindex 4a7f03ade346f53a1c1d82981f3fd744f9ab4547..0c5ec6c53dc7197b7b7a7d18e667b325babd6c45\n--- a/docs/agents/checklists.md\n+++ b/docs/agents/checklists.md\n@@ -32,18 +32,18 @@\n | ID | Item | Blocking |\n | --- | --- | --- |\n | GD-01 | Plan was challenged against `CONTEXT.md`. | blocking for non-trivial Goals |\n-| GD-02 | Relevant ADRs were read or absence was recorded. | blocking for non-trivial Goals |\n+| GD-02 | Relevant SADRs and target ADRs were read or absence was recorded. | blocking for non-trivial Goals |\n | GD-03 | Ambiguous terms were resolved or converted to HITL blockers. | blocking |\n | GD-04 | Edge scenarios were considered. | advisory |\n | GD-05 | Code reality was checked when code could answer. | blocking when applicable |\n | GD-06 | New domain terms were added to `CONTEXT.md` when needed. | blocking when applicable |\n-| GD-07 | ADR was created or explicitly skipped using the ADR criteria. | blocking when applicable |\n+| GD-07 | A SADR or target ADR was created or explicitly skipped using the decision-record criteria. | blocking when applicable |\n \n ## CI \u2014 Context & Impact Checklist\n \n | ID | Item | Blocking |\n | --- | --- | --- |\n-| CI-01 | Relevant docs and ADRs identified. | blocking |\n+| CI-01 | Relevant docs, SADRs, and target ADRs identified. | blocking |\n | CI-02 | Relevant modules, interfaces, seams, callers, and tests identified. | blocking |\n | CI-03 | Dependencies and blocked-by relationships identified. | blocking |\n | CI-04 | Candidate locks identified. | blocking |\n@@ -62,7 +62,7 @@\n | PRD-04 | Implementation decisions are recorded without volatile code detail. | blocking |\n | PRD-05 | Testing decisions are recorded. | blocking |\n | PRD-06 | Out-of-scope items are explicit. | blocking |\n-| PRD-07 | Relevant glossary/ADR links are present when applicable. | blocking |\n+| PRD-07 | Relevant glossary/SADR/target-ADR links are present when applicable. | blocking |\n \n ## SF \u2014 Spec Freeze Checklist\n \n@@ -98,7 +98,7 @@\n | H-02 | Branch/worktree target is present. | blocking |\n | H-03 | Scope and non-goals are present. | blocking |\n | H-04 | Dependencies and locks are present. | blocking |\n-| H-05 | Relevant docs, ADRs, modules, and tests are referenced. | blocking |\n+| H-05 | Relevant docs, SADRs, target ADRs, modules, and tests are referenced. | blocking |\n | H-06 | Required skills are stated. | blocking |\n | H-07 | TDD expectations are stated. | blocking when behavior work |\n | H-08 | Acceptance criteria are included. | blocking |\ndiff --git a/docs/agents/codex-handoff.md b/docs/agents/codex-handoff.md\nindex ede468f47cb277f69a857e8c45df703e4ed0151d..6f97bdffb67e44d7edea2c71e01ea28933c4eb6c\n--- a/docs/agents/codex-handoff.md\n+++ b/docs/agents/codex-handoff.md\n@@ -46,7 +46,7 @@\n \n ## Context\n - Domain terms:\n-- Relevant ADRs:\n+- Relevant SADRs and target ADRs:\n - Relevant docs:\n - Relevant modules/interfaces/seams:\n - Similar tests/prior art:\ndiff --git a/docs/agents/completion-check-agent.md b/docs/agents/completion-check-agent.md\nindex 13e4c035db35025f4e3cdcc5adfb24314ac4d521..61bfa1218cf36ca605250c91919e620f33238aa0\n--- a/docs/agents/completion-check-agent.md\n+++ b/docs/agents/completion-check-agent.md\n@@ -32,7 +32,7 @@\n - `AGENTS.md`\n - `CLAUDE.md`\n - `CONTEXT.md`\n-- relevant ADRs\n+- relevant SADRs and target ADRs\n - parent Goal issue\n - PRD issue or document\n - task issue\ndiff --git a/docs/agents/decision-control.md b/docs/agents/decision-control.md\nindex 7d8ee5ab03a5ccbcbd04056ae61de03785a2a9c9..8c94ecda00fffaa0e92c325ab83aa9776bd1dac1\n--- a/docs/agents/decision-control.md\n+++ b/docs/agents/decision-control.md\n@@ -11,7 +11,7 @@\n | Decision | Owner | When | Required inputs | Output | Enforcement surface |\n | --- | --- | --- | --- | --- | --- |\n | Is the Goal clear enough to plan? | Planner + Guardian when needed | Goal intake | User request, existing docs, risk signals | Goal readiness verdict | Goal checklist, issue template, ledger |\n-| Must `grill-with-docs` run? | Planner / Skill Gate | Before PRD or issue split | Ambiguity, domain terms, ADR conflicts, unclear boundaries | Skill invocation or explicit skip rationale | Skill Gate checklist |\n+| Must `grill-with-docs` run? | Planner / Skill Gate | Before PRD or issue split | Ambiguity, domain terms, SADR/target-ADR conflicts, unclear boundaries | Skill invocation or explicit skip rationale | Skill Gate checklist |\n | Is a PRD required? | Planner | After Goal context stabilizes | Goal, `grill-with-docs` output, Context & Impact output | PRD required / not required | PRD checklist, CCA evidence check |\n | Can the spec be frozen? | Operator only | End of Requirements Definition, before issue split | PRD/requirements summary, scope inventory (SF-02), open-question list | `spec_freeze` block in the plan + ledger evidence | `require_grilled_plan` (plan ingest/run), SF checklist |\n | Can a frozen spec change? | Operator only (Spec Amendment) | When implementation contradicts the frozen spec | Contested decisions, scoped re-grill result | recorded amendment + re-stamped freeze | SF checklist, ledger evidence |\ndiff --git a/docs/agents/domain.md b/docs/agents/domain.md\nindex 258da8ea81e87ac0a6b0b5b5e5c2fde980eaabb7..d57b0cf17ff3e1fa9e6e5885372bf083d8295421\n--- a/docs/agents/domain.md\n+++ b/docs/agents/domain.md\n@@ -5,7 +5,9 @@\n ## Layout\n \n - `CONTEXT.md` defines Shiki language. Keep it implementation-free.\n-- `docs/adr/` records hard-to-reverse decisions.\n+- `docs/adr/SADR-NNNN-*.md` records Shiki platform decisions.\n+- `docs/adr/NNNN-*.md` records target product decisions without sharing the\n+  SADR namespace.\n - `AGENTS.md` is the runtime-neutral agent constitution.\n - `CLAUDE.md` is the Claude Code wrapper around `AGENTS.md`.\n - `.shiki/` is the Target Repository mirror for Goals, PRDs, plans, Task DAGs, contracts, locks, worktree records, ledger entries, CCA verdicts, repair packets, reports, and handoffs.\n@@ -15,6 +17,6 @@\n \n - Use Shiki terms exactly when they exist.\n - Add glossary terms to `CONTEXT.md` only when the term is part of the domain, not a generic programming concept.\n-- Create ADRs only for hard-to-reverse, surprising, tradeoff-based decisions.\n+- Create SADRs or target ADRs only for hard-to-reverse, surprising, tradeoff-based decisions.\n - Keep product-specific language in the Target Repository, not in Shiki Template docs.\n-- If an ADR conflicts with a proposed implementation, surface the conflict before editing.\n+- If a SADR or target ADR conflicts with a proposed implementation, surface the conflict before editing.\ndiff --git a/docs/agents/execution-control.md b/docs/agents/execution-control.md\nindex ad8ee3cdb36be56fedc1f46408ca92b387d74047..bc63c3a73661d12b567016ba299f1a6b66bf9088\n--- a/docs/agents/execution-control.md\n+++ b/docs/agents/execution-control.md\n@@ -63,7 +63,7 @@\n | Branch name | Orchestrator | Orchestrator | Before implementer dispatch | branch policy | branch ref in task/ledger |\n | Lock reservation | Context & Impact | MergeGate | Before dispatch and before merge | `.shiki/locks/`, labels, PR metadata | lock id, owner, scope |\n | CI profile | Planner | MergeGate | During issue triage, rechecked at PR | labels, `.shiki/policy.yaml`, workflow matrix | required check list |\n-| Required status checks | Repo maintainer / Guardian | GitHub branch protection / rulesets | Repository setup, updated via ADR | GitHub settings | branch/ruleset config, ADR |\n+| Required status checks | Repo maintainer / Guardian | GitHub branch protection / rulesets | Repository setup, updated via target ADR | GitHub settings | branch/ruleset config, target ADR |\n | CCA judgment | CCA | GitHub required check / MergeGate | Every non-draft PR update | GitHub Action check | structured CCA verdict |\n | Repair assignment | CCA | MergeGate | After failed CCA/CI/review | repair packet, issue/PR comment | repair id and attempt count |\n | Merge readiness | MergeGate | GitHub branch protection / Guardian if needed | After CCA complete | required checks, review rules, rulesets | mergegate check output |\ndiff --git a/docs/agents/guardian-policy.md b/docs/agents/guardian-policy.md\nindex 4147c7adf05a47adb4fd24ada1a1c2e1f2ba6d2d..a20d9cc6c00a569bb057b9ca8d6b51c7fc4a9847\n--- a/docs/agents/guardian-policy.md\n+++ b/docs/agents/guardian-policy.md\n@@ -98,8 +98,8 @@\n `origin/<default_branch>` and reuses each job's existing base fetch; a shallow\n (`--depth`) or second base fetch would make the proof unresolvable and silently\n disable the carry, so neither is permitted. CCA reads `guardian_comment_carried` as\n-a recorded authority in the deterministic signal and does not re-derive it. See ADR\n-0018 for the full decision, the rejected diff/patch-equality and path-disjointness\n+a recorded authority in the deterministic signal and does not re-derive it. See\n+SADR-0018 for the full decision, the rejected diff/patch-equality and path-disjointness\n alternatives, and the accepted residual.\n \n ## External AI Guardian Review (SADR-0010)\ndiff --git a/docs/agents/implementation-policy.md b/docs/agents/implementation-policy.md\nindex c754144a9fb5bf4a38aa82a60e1670c59e626b44..85c37281bcad196c6b7b02343faf00b03cd9c57a\n--- a/docs/agents/implementation-policy.md\n+++ b/docs/agents/implementation-policy.md\n@@ -31,10 +31,13 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n ## Non-Negotiables\n \n 1. **Planning precedes implementation.** Non-trivial Goals start with `grill-with-docs` and Context & Impact.\n-2. **Domain language is controlled.** Terms must be checked against `CONTEXT.md`; hard-to-reverse tradeoffs must be checked against ADRs.\n+2. **Domain language is controlled.** Terms must be checked against `CONTEXT.md`; hard-to-reverse tradeoffs must be checked against Shiki SADRs and target ADRs.\n 3. **PRD precedes issue decomposition.** `to-prd` is used once the context is settled enough to become durable intent.\n 4. **Issues are vertical slices.** `to-issues` must produce independently grabbable tracer-bullet tasks, not horizontal layer tickets.\n 5. **Triage controls dispatch.** Only `ready-for-agent` / AFK tasks may be assigned to Codex without human input.\n@@ -95,19 +98,19 @@\n Process:\n \n - Challenge terms against `CONTEXT.md`.\n-- Check ADRs before reopening decisions.\n+- Check SADRs and target ADRs before reopening decisions.\n - Inspect code when code can answer a question.\n - Ask one question at a time when the operator is available.\n - Provide a recommended answer with each question.\n - Record resolved terms in `CONTEXT.md`.\n-- Create ADRs only for hard-to-reverse, surprising, tradeoff-based decisions.\n+- Create SADRs or target ADRs only for hard-to-reverse, surprising, tradeoff-based decisions.\n - Convert unresolved load-bearing questions into HITL blockers.\n \n Outputs:\n \n - Resolved design-tree decisions.\n - Updated glossary terms when needed.\n-- ADRs when justified.\n+- SADRs or target ADRs when justified.\n - HITL blockers when decisions remain unresolved.\n \n ## Phase 3 \u2014 Context & Impact\n@@ -117,7 +120,7 @@\n \n Outputs must include:\n \n-- Relevant documents and ADRs.\n+- Relevant documents, SADRs, and target ADRs.\n - Relevant modules, interfaces, seams, callers, and tests.\n - Candidate locks.\n - Dependencies.\n@@ -140,7 +143,7 @@\n - Testing decisions.\n - Out of scope.\n - Further notes.\n-- Links to relevant Goal, domain terms, and ADRs.\n+- Links to relevant Goal, domain terms, SADRs, and target ADRs.\n \n Phase 4 ends with Spec Freeze: the operator explicitly approves the PRD, the\n plan records a `spec_freeze` block (status, approved_by, source), and the SF\n@@ -240,7 +243,7 @@\n - Dependencies and locks.\n - Risk and Guardian requirements.\n - Ledger completeness.\n-- Documentation and ADR updates.\n+- Documentation and SADR/target-ADR updates.\n - Scope drift.\n \n CCA emits one of:\n@@ -301,7 +304,7 @@\n | Evidence failure | Behavior might be done, but proof is missing. | Add checks, PR evidence, ledger entry, or artifacts. |\n | Scope drift | PR changes unrelated behavior. | Revert unrelated changes or split into new Goal. |\n | Dependency block | Upstream task or lock not complete. | Block until dependency resolves. |\n-| Architecture block | Decision needs ADR or deep-module discussion. | `grill-with-docs` or `improve-codebase-architecture`. |\n+| Architecture block | Decision needs a SADR, target ADR, or deep-module discussion. | `grill-with-docs` or `improve-codebase-architecture`. |\n | Risk block | Security/production/policy change needs human approval. | Guardian decision. |\n \n ## Phase 5.5 \u2014 Execution Decision Control\ndiff --git a/docs/agents/runtime-auth-model.md b/docs/agents/runtime-auth-model.md\nindex 6a2e1a47e81ea89b5c68a210ec07772a9374babc..3c54748d24233dbd0ba8473950282547ef4ce935\n--- a/docs/agents/runtime-auth-model.md\n+++ b/docs/agents/runtime-auth-model.md\n@@ -148,7 +148,7 @@\n \n API-key based runners may be added by a target repository as an explicit extension. They are not the default Shiki template path.\n \n-If a repo opts into API-key mode, record the exception in an ADR and update the repo's `AGENTS.md`, `.shiki/config.yaml`, and workflow permissions.\n+If a repo opts into API-key mode, record the target-specific exception in a target ADR and update the repo's `AGENTS.md`, `.shiki/config.yaml`, and workflow permissions.\n \n ## GitHub Actions Runtime Compatibility\n \ndiff --git a/docs/agents/skill-gate.md b/docs/agents/skill-gate.md\nindex c50398b0d7c60a8598f2a5c88a10015730dced13..3acffe6444617bb1d54b77d4b4708158ec735bc6\n--- a/docs/agents/skill-gate.md\n+++ b/docs/agents/skill-gate.md\n@@ -9,7 +9,7 @@\n | Skill | Trigger | Expected output |\n | --- | --- | --- |\n | `setup-matt-pocock-skills` | First configuring a repo for these skills, or when issue tracker, triage labels, or domain docs are missing. | `docs/agents/` configuration and an `Agent skills` block in `AGENTS.md` or `CLAUDE.md`. |\n-| `grill-with-docs` | Ambiguous plan, unclear terminology, boundary decision, tradeoff, or ADR-worthy design choice. | Clarified decision tree, recommended answers, updates to `CONTEXT.md` or ADRs when decisions crystallize. |\n+| `grill-with-docs` | Ambiguous plan, unclear terminology, boundary decision, tradeoff, or decision-record-worthy design choice. | Clarified decision tree, recommended answers, updates to `CONTEXT.md`, SADRs, or target ADRs when decisions crystallize. |\n | `zoom-out` | Agent lacks a higher-level architectural map. | Map of relevant modules, callers, seams, and domain concepts. |\n | `to-prd` | Settled context should become a PRD. | PRD published to the issue tracker with problem, solution, user stories, decisions, testing, and out-of-scope notes. |\n | `to-issues` | Goal, PRD, or plan must become implementation tickets. | Vertical-slice issues with acceptance criteria, dependencies, and AFK/HITL classification. |\n@@ -17,7 +17,7 @@\n | `tdd` | Feature work or bug fix has observable behavior that can be specified with tests. | Red-green-refactor loop through public interfaces, one vertical slice at a time. |\n | `code-review` | The TDD loop is green and the implementer is about to create or update a PR. Default for every implementation task. | Pre-PR self-review of the task diff with fixes applied on the task branch; findings and resolutions recorded as a ledger entry naming the skill and a `## Pre-PR code review` PR body section. |\n | `diagnose` | Hard bug, failing check, regression, flaky behavior, or performance problem. | Reproduction loop, ranked hypotheses, instrumentation, fix, regression test, post-mortem. |\n-| `improve-codebase-architecture` | Structural friction, testability, AI-navigability, deep-module opportunity, or refactor request. | Architecture candidates and deepening recommendations informed by `CONTEXT.md` and ADRs. |\n+| `improve-codebase-architecture` | Structural friction, testability, AI-navigability, deep-module opportunity, or refactor request. | Architecture candidates and deepening recommendations informed by `CONTEXT.md`, SADRs, and target ADRs. |\n | `prototype` | A throwaway logic or UI prototype is needed to answer a design question. | Clearly marked disposable prototype and captured decision. |\n \n ## MergeGate Interaction\ndiff --git a/docs/operations.md b/docs/operations.md\nindex cf1f452d4c589e38ccd58553bda0ad8684b51eb7..ace48a90ffbec9e5c0a7113bef16a4858bc44b3f\n--- a/docs/operations.md\n+++ b/docs/operations.md\n@@ -89,6 +89,16 @@\n      target history, brought forward by `shiki migrate`, not a template.\n    - **Everything else** (CLI scripts, workflows, schemas, docs) is overwritten.\n \n+   A forced upgrade also migrates the former numeric Shiki decision paths to\n+   `SADR-NNNN-*`. Before the first write, the installer inventories the exact\n+   legacy Shiki paths and authorizes each deletion only when the existing\n+   `.shiki/install-stamp.json` names that path and its SHA-256 digest still\n+   matches the file. An absent, unreadable, malformed, incomplete, or\n+   mismatching stamp reports every blocked path and stops with no filesystem\n+   change. Target-owned `NNNN-*` ADRs are not cleanup candidates. A non-force\n+   install deletes nothing and retains any still-matching legacy stamp entries\n+   so a later forced upgrade can prove ownership.\n+\n    If the target has pending migrations, `--force` refuses and writes nothing \u2014\n    apply migrations first (step 3), then re-run. The run ends with a single\n    summary of every `.new` written, so a half-upgrade is visible.\ndiff --git a/docs/releasing.md b/docs/releasing.md\nindex 10d73828e7dbd64e9d9f6b83bc84d010537ce62a..aafeee8c4d441e1e427efee2ce4f81a647f020e1\n--- a/docs/releasing.md\n+++ b/docs/releasing.md\n@@ -67,7 +67,7 @@\n CLI in `scripts/`. There are no third-party Python runtime dependencies. The CLI\n is intentionally **not** exposed as a `[project.scripts]` console entry point\n because `scripts/shiki.py` is a dependency-free shim and `scripts/` is not\n-packaged as an importable distribution (see the ADR for the rationale).\n+packaged as an importable distribution (see SADR-0007 for the rationale).\n \n Supported install paths:\n \n@@ -155,6 +155,15 @@\n    [docs/operations.md](operations.md#the-new-convention). If the target has\n    pending migrations, `--force` refuses and writes nothing; apply migrations\n    (step 2) first, then re-run.\n+\n+   The SADR namespace upgrade has an additional pre-write gate: each former\n+   numeric Shiki decision path is removed only when the existing install stamp\n+   contains that exact path and a digest matching its current bytes. The\n+   installer aggregates absent/unreadable/malformed-stamp, missing-digest, and\n+   digest-mismatch blockers before raising, so refusal leaves the target tree\n+   byte-identical. Numeric target ADRs are preserved. A successful forced\n+   upgrade installs and stamps `SADR-*` paths and drops the removed legacy paths\n+   from the refreshed stamp.\n 2. Apply repository-local `.shiki/` state migrations with the migrate command:\n \n    ```bash\ndiff --git a/docs/usage.md b/docs/usage.md\nindex b83407ee3970dd395dd893010bc5dbe8bad26051..a130ef6e40dfd8169a5bc701339351097df1a09f\n--- a/docs/usage.md\n+++ b/docs/usage.md\n@@ -67,8 +67,10 @@\n shiki install-target /path/to/target-repo --local-only\n ```\n \n-Use `--force` only when you intentionally want to overwrite existing target\n-files.\n+Use `--force` only when you intentionally want to refresh the target template.\n+For the SADR namespace migration, `--force` deletes only exact legacy Shiki\n+decision paths whose existing install-stamp digest still matches; any ownership\n+blocker stops before the first write. Numeric target ADRs are never candidates.\n \n ## `shiki bootstrap-platform` \u2014 publish the Shiki platform repo\n \ndiff --git a/scripts/guardian_approval_signal.py b/scripts/guardian_approval_signal.py\nindex d059f92b2c82aa2b5d6ca421365537a87886c1cb..16924892714f4f5b9303c0ef0e325a7e70075630\n--- a/scripts/guardian_approval_signal.py\n+++ b/scripts/guardian_approval_signal.py\n@@ -199,8 +199,8 @@\n ) -> Any:\n     \"\"\"Evaluate SADR-0015 Contract Approval for the CCA Guardian signal.\n \n-    The SAME OR that MergeGate applies must reach this deterministic signal: ADR\n-    0015 is explicit that without it, a contract-approved PR still returns\n+    The SAME OR that MergeGate applies must reach this deterministic signal:\n+    SADR-0015 is explicit that without it, a contract-approved PR still returns\n     ``needs_guardian``, the CCA job fails, and the MergeGate policy check never\n     runs \u2014 the gate this change moves would never be reached. This resolves the\n     task the SAME way MergeGate does (the first Shiki task id in the PR body),\ndiff --git a/scripts/shiki_contract_approval.py b/scripts/shiki_contract_approval.py\nindex 941bcb96fdd8b9a85bc0c4f0f04d5dad9d04839c..4a4b418f6527413161db82670bec857a043360e2\n--- a/scripts/shiki_contract_approval.py\n+++ b/scripts/shiki_contract_approval.py\n@@ -142,7 +142,7 @@\n     changed_files_status: Any,\n     registration: Any,\n ) -> ContractApprovalResult:\n-    \"\"\"Whether this PR carries an SADR-0015 Contract Approval.\n+    \"\"\"Whether this PR carries a SADR-0015 Contract Approval.\n \n     The four conditions are evaluated IN ORDER, each failure recorded as a\n     distinct reason string:\ndiff --git a/scripts/shiki_guardian.py b/scripts/shiki_guardian.py\nindex a4ecb79bbda55fd5c8b06adfae2eb99760a03c45..4dd99bbe714e26f421fa19abac7a34ae1b195ecc\n--- a/scripts/shiki_guardian.py\n+++ b/scripts/shiki_guardian.py\n@@ -12,7 +12,7 @@\n # The carry re-derives its proof from git in-process on every judging invocation;\n # it is never read from an artifact, so there is nothing to go stale or be forged.\n # shiki_sync_proof is the ONLY guardian-family module that runs git, and every one\n-# of its invocations is read-only and local (ADR: prove-from-git base sync).\n+# of its invocations is read-only and local (SADR: prove-from-git base sync).\n from shiki_sync_proof import verify_pure_base_sync\n \n GUARDIAN_POLICY_PATH = \".shiki/guardian-policy.json\"\ndiff --git a/scripts/shiki_guardian_review.py b/scripts/shiki_guardian_review.py\nindex dd0f104d457899e0ba5ff1e162011c1e90b6f92b..e10ef58c046324e7ea3a3392feb3048ca77414ce\n--- a/scripts/shiki_guardian_review.py\n+++ b/scripts/shiki_guardian_review.py\n@@ -159,7 +159,7 @@\n     docs_only = _is_docs_only(changed_files)\n     if docs_only and matched == 0:\n         focus.append(\n-            \"Docs/ADR-only change: confirm the change is genuinely documentation/decision-only, introduces no behavior or contract drift, and stays consistent with the constitution and existing ADRs.\"\n+            \"Docs/decision-record-only change: confirm the change is genuinely documentation/decision-only, introduces no behavior or contract drift, and stays consistent with the constitution and existing SADRs and target ADRs.\"\n         )\n \n     # Conservative augmentation: unknown (no category and not docs-only), mixed\ndiff --git a/scripts/shiki_installer.py b/scripts/shiki_installer.py\nindex be064ab7a29ff6836f7f7a1d70500a43e0d11d7b..9fd8eefd137ce7003dad5c0dcb2e48e2cd47ab32\n--- a/scripts/shiki_installer.py\n+++ b/scripts/shiki_installer.py\n@@ -118,6 +118,7 @@\n # what lets a target carry it without a governance-schema change.\n INSTALL_STAMP_PATH = \".shiki/install-stamp.json\"\n INSTALL_STAMP_VERSION = 1\n+LEGACY_SHIKI_SADR_MAX = 18\n \n # Under --force the shipped surface splits three ways, so nothing is lost and\n # nothing is silently stale.\n@@ -237,9 +238,98 @@\n         )\n \n \n+def legacy_shiki_adr_paths() -> tuple[str, ...]:\n+    \"\"\"Former numeric paths for Shiki SADR-0001 through SADR-0018.\n+\n+    The authoritative slugs come from the current SADR filenames. SADR-0019 is\n+    the namespace decision itself and never shipped under a numeric legacy path.\n+    \"\"\"\n+    paths: list[str] = []\n+    for sadr in sorted((ROOT / \"docs\" / \"adr\").glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\")):\n+        number = sadr.name.split(\"-\", 2)[1]\n+        if int(number) <= LEGACY_SHIKI_SADR_MAX:\n+            paths.append(f\"docs/adr/{sadr.name.removeprefix('SADR-')}\")\n+    return tuple(paths)\n+\n+\n+def preflight_legacy_sadr_cleanup(target: Path, *, force: bool) -> tuple[str, ...]:\n+    \"\"\"Authorize exact legacy Shiki ADR removals before the first write.\n+\n+    A non-force install never removes existing files. A forced install needs no\n+    stamp when no legacy Shiki path is present. When legacy paths do exist, each\n+    must be named in a readable install stamp and its current bytes must match\n+    the recorded digest.\n+    \"\"\"\n+    if not force:\n+        return ()\n+    present = tuple(\n+        relative\n+        for relative in legacy_shiki_adr_paths()\n+        if (target / relative).exists() or (target / relative).is_symlink()\n+    )\n+    if not present:\n+        return ()\n+\n+    stamp_path = target / INSTALL_STAMP_PATH\n+    try:\n+        raw = stamp_path.read_text(encoding=\"utf-8\")\n+        stamp = json.loads(raw)\n+    except FileNotFoundError:\n+        detail = \"install stamp is absent\"\n+        blockers = [f\"{relative}: {detail}\" for relative in present]\n+    except (OSError, UnicodeDecodeError) as error:\n+        detail = f\"install stamp is unreadable ({error})\"\n+        blockers = [f\"{relative}: {detail}\" for relative in present]\n+    except json.JSONDecodeError as error:\n+        detail = f\"install stamp is malformed ({error})\"\n+        blockers = [f\"{relative}: {detail}\" for relative in present]\n+    else:\n+        if not isinstance(stamp, dict):\n+            blockers = [\n+                f\"{relative}: install stamp is malformed (expected an object)\"\n+                for relative in present\n+            ]\n+        else:\n+            digests = stamp.get(\"digests\")\n+            blockers = []\n+            if not isinstance(digests, dict):\n+                blockers.extend(\n+                    f\"{relative}: install stamp has no digest for this path\"\n+                    for relative in present\n+                )\n+            else:\n+                for relative in present:\n+                    expected = digests.get(relative)\n+                    if not isinstance(expected, str) or not expected:\n+                        blockers.append(\n+                            f\"{relative}: install stamp has no digest for this path\"\n+                        )\n+                        continue\n+                    try:\n+                        actual = _sha256_file(target / relative)\n+                    except OSError as error:\n+                        blockers.append(\n+                            f\"{relative}: legacy path is unreadable ({error})\"\n+                        )\n+                        continue\n+                    if actual != expected:\n+                        blockers.append(\n+                            f\"{relative}: digest mismatch (stamp {expected}, current {actual})\"\n+                        )\n+\n+    if blockers:\n+        joined = \"\\n\".join(f\"- {blocker}\" for blocker in blockers)\n+        raise ShikiError(\n+            \"legacy Shiki ADR cleanup is not authorized:\\n\"\n+            f\"{joined}\\nNo files were written.\"\n+        )\n+    return present\n+\n+\n def install_template(target: Path, *, force: bool, validate: bool) -> list[NewFileNote]:\n     # Preconditions first: never rewrite the tree of a target that cannot be\n     # upgraded cleanly (see refuse_pending_migrations).\n+    legacy_cleanup = preflight_legacy_sadr_cleanup(target, force=force)\n     refuse_pending_migrations(target)\n \n     # Resolve the target's required CODEOWNERS owner once from its\n@@ -250,6 +340,10 @@\n     # verbatim. See ``codeowners_required_owner``.\n     codeowners_owner = codeowners_required_owner(target)\n \n+    for relative in legacy_cleanup:\n+        (target / relative).unlink()\n+        info(f\"removed stamped legacy Shiki ADR: {target / relative}\")\n+\n     notes: list[NewFileNote] = []\n     for relative in TEMPLATE_PATHS:\n         source = ROOT / relative\n@@ -647,6 +741,22 @@\n     digests = target_stamp_digests(target)\n     existing = read_install_stamp(target)\n     if existing is not None:\n+        # A non-force install deliberately leaves legacy Shiki ADRs in place.\n+        # Preserve only still-matching ownership proof so a later forced\n+        # upgrade can authorize cleanup; never carry a missing or stale digest.\n+        existing_digests = existing.get(\"digests\")\n+        if isinstance(existing_digests, dict):\n+            for relative in legacy_shiki_adr_paths():\n+                expected = existing_digests.get(relative)\n+                path = target / relative\n+                if not isinstance(expected, str) or not path.is_file():\n+                    continue\n+                try:\n+                    actual = _sha256_file(path)\n+                except OSError:\n+                    continue\n+                if actual == expected:\n+                    digests[relative] = actual\n         if _incoming_is_older(commit, existing.get(\"platform_commit\")):\n             warn(\n                 \"kept existing install stamp: incoming install \"\ndiff --git a/scripts/shiki_loop.py b/scripts/shiki_loop.py\nindex 3611641da8aeb5038b1da90e3114b09a473ec6d8..84caa491b9cad0edc4176f209c13e9221e975681\n--- a/scripts/shiki_loop.py\n+++ b/scripts/shiki_loop.py\n@@ -424,7 +424,7 @@\n         # unresolvable CCA verdict has already produced its terminal stop and is never\n         # laundered into a head-moving sync (SADR-0010). It also never fires while the\n         # policy (Guardian) gate is red \u2014 for ANY reason, not only when it is the sole\n-        # red check \u2014 so a head-bound Guardian approval, or an SADR-0015\n+        # red check \u2014 so a head-bound Guardian approval, or a SADR-0015\n         # contract-approval carry that base movement defeated (the policy gate compares\n         # base and head task governance directly), is never silently restored by\n         # adopting base wholesale. It is bounded by a durable per-task counter so the\ndiff --git a/scripts/shiki_tasks.py b/scripts/shiki_tasks.py\nindex aec3809f24b4a0c8c96fc50971cff9ae9ad30381..d531052e9d9717058d4631483cefab44c26a65c1\n--- a/scripts/shiki_tasks.py\n+++ b/scripts/shiki_tasks.py\n@@ -821,7 +821,7 @@\n         \"prompt\": prompt,\n         \"entry_skill\": \"grill-with-docs\",\n         \"required_next_steps\": [\n-            \"Run grill-with-docs until terminology, boundaries, risks, and ADR-worthy decisions are settled.\",\n+            \"Run grill-with-docs until terminology, boundaries, risks, and decision-record-worthy choices are settled.\",\n             \"Obtain the operator's explicit approval of the PRD/requirements (Spec Freeze).\",\n             \"Write a machine-readable plan JSON with grill_with_docs.status=complete and spec_freeze.status=frozen.\",\n             \"Run shiki plan ingest --plan-file PLAN.json.\",\ndiff --git a/scripts/test_shiki_goal_loop.sh b/scripts/test_shiki_goal_loop.sh\nindex 753c7b516e10f0be3bc92a4bd0eb1fa699776ae6..8f3820a0a079b3044424cfe1a6c6b7fe68896e67\n--- a/scripts/test_shiki_goal_loop.sh\n+++ b/scripts/test_shiki_goal_loop.sh\n@@ -231,8 +231,8 @@\n \n # High/critical risk merges autonomously when all required checks are green:\n # the \"MergeGate policy check\" required check IS the Guardian gate (it enforces\n-# guardian-policy.json \u2014 human approval OR external AI guardian review, ADR\n-# 0010), so green checks mean a recorded authority approved. The fake gh returns\n+# guardian-policy.json \u2014 human approval OR external AI guardian review,\n+# SADR-0010), so green checks mean a recorded authority approved. The fake gh returns\n # green checks, simulating that the Guardian gate passed.\n python3 \"$ROOT/scripts/shiki.py\" goal create --target \"$TARGET\" --title \"Guardian gate\" --outcome \"High risk merges via policy gate\" >/tmp/shiki-goal-loop-guardian-goal.json\n GGOAL=\"$(json_get /tmp/shiki-goal-loop-guardian-goal.json goal_id)\"\ndiff --git a/tests/test_adr_references.py b/tests/test_adr_references.py\nindex 51706329c797d5dd9b47e082a333fa83b261d482..d005da4f771f5e8085ec22d4b787980e68f5ae8c\n--- a/tests/test_adr_references.py\n+++ b/tests/test_adr_references.py\n@@ -1,8 +1,8 @@\n-\"\"\"Every ``ADR ####`` citation must resolve to exactly one decision record.\n+\"\"\"Every target ADR and Shiki SADR citation resolves in its own namespace.\n \n Background. ``scripts/mergegate_check.py`` justifies ``is_bookkeeping_closeout``\n-with \"ADR 0017\" nine times and ``scripts/guardian_approval_signal.py`` cites\n-0016/0017 seven times, yet for a stretch the numbering in ``docs/adr/`` stopped\n+with \"SADR-0017\" nine times and ``scripts/guardian_approval_signal.py`` cites\n+SADR-0016/SADR-0017 seven times, yet for a stretch the numbering in ``docs/adr/`` stopped\n at 0015: the authoring commits lived on a coordinator branch and no task ever\n declared ``docs/adr/...`` in its locks, so nothing carried the records to the\n default branch. The stated basis of an exemption that merged seven PRs with zero\n@@ -11,11 +11,12 @@\n and the directory drifted with no signal.\n \n This is the same unbound-pair shape behind other silent failures: a reference\n-and its target with nothing asserting they agree. This test binds them. It scans\n-every ``scripts/*.py`` (plus ``docs/**/*.md`` and the root ``*.md`` governance\n-files, which are cheap) for the ``ADR ####`` pattern and asserts each cited\n-number resolves to exactly one ``docs/adr/####-*.md``. The converse is\n-deliberately NOT required: an ADR may exist without being cited.\n+and its target with nothing asserting they agree. This test binds them. Target\n+``ADR NNNN`` citations resolve only to ``docs/adr/NNNN-*.md``; Shiki\n+``SADR-NNNN`` citations resolve only to ``docs/adr/SADR-NNNN-*.md``. The scan\n+covers current governance docs, scripts, workflows, prompts, packaging metadata,\n+and repo-local Codex guidance while deliberately excluding historical\n+``.shiki`` evidence. The converse is not required: a record may be uncited.\n \n The suite is dependency free: standard-library ``unittest`` only.\n \"\"\"\n@@ -31,17 +32,18 @@\n \n REPO_ROOT = Path(__file__).resolve().parents[1]\n \n-# \"ADR 0016\" -- a four-digit citation. The trailing \\b keeps a five-digit token\n-# from being read as a truncated four-digit number.\n-_CITATION = re.compile(r\"\\bADR (\\d{4})\\b\")\n-# \"0016-locks-bound-product-paths.md\" -- the leading four digits name the record.\n-_ADR_FILENAME = re.compile(r\"^(\\d{4})-.*\\.md$\")\n+# ``ADR 0016`` / ``SADR-0016`` -- a namespace plus four-digit citation. The\n+# trailing ``\\b`` keeps a five-digit token from being read as a truncated one.\n+_CITATION = re.compile(r\"\\b(S?ADR)[ -](\\d{4})\\b\")\n+# Target ADRs retain the numeric filename; Shiki platform decisions use SADR.\n+_DECISION_FILENAME = re.compile(r\"^(?:(SADR)-)?(\\d{4})-.*\\.md$\")\n \n \n class Violation(NamedTuple):\n     \"\"\"A citation that does not resolve to exactly one decision record.\"\"\"\n \n     citing_file: str  # repo-relative path of the file that cites the ADR\n+    namespace: str  # \"ADR\" for target decisions; \"SADR\" for Shiki decisions\n     number: str  # the cited four-digit number, e.g. \"0016\"\n     line: int  # 1-based line of the citation\n     reason: str  # \"missing\" (zero matches) or \"ambiguous\" (two or more)\n@@ -49,44 +51,59 @@\n \n     @property\n     def expected_path(self) -> str:\n-        return f\"docs/adr/{self.number}-*.md\"\n+        prefix = \"SADR-\" if self.namespace == \"SADR\" else \"\"\n+        return f\"docs/adr/{prefix}{self.number}-*.md\"\n \n     def describe(self) -> str:\n         \"\"\"Name the citing file, the cited number, and the missing path.\"\"\"\n         if self.reason == \"missing\":\n             return (\n-                f\"{self.citing_file}:{self.line} cites ADR {self.number} \"\n+                f\"{self.citing_file}:{self.line} cites {self.namespace} {self.number} \"\n                 f\"but no document matches {self.expected_path}\"\n             )\n         joined = \", \".join(self.matches)\n         return (\n-            f\"{self.citing_file}:{self.line} cites ADR {self.number} which \"\n+            f\"{self.citing_file}:{self.line} cites {self.namespace} {self.number} which \"\n             f\"resolves to {len(self.matches)} documents (expected exactly one \"\n             f\"at {self.expected_path}): {joined}\"\n         )\n \n \n-def _adr_index(root: Path) -> dict[str, list[Path]]:\n-    \"\"\"Map every four-digit ADR number to the record files that claim it.\"\"\"\n-    index: dict[str, list[Path]] = {}\n+def _adr_index(root: Path) -> dict[tuple[str, str], list[Path]]:\n+    \"\"\"Map each decision namespace and number to the files that claim it.\"\"\"\n+    index: dict[tuple[str, str], list[Path]] = {}\n     adr_dir = root / \"docs\" / \"adr\"\n     if adr_dir.is_dir():\n         for path in sorted(adr_dir.glob(\"*.md\")):\n-            match = _ADR_FILENAME.match(path.name)\n+            match = _DECISION_FILENAME.match(path.name)\n             if match:\n-                index.setdefault(match.group(1), []).append(path)\n+                namespace = \"SADR\" if match.group(1) else \"ADR\"\n+                index.setdefault((namespace, match.group(2)), []).append(path)\n     return index\n \n \n def _citing_files(root: Path, include_docs: bool = True) -> list[Path]:\n-    \"\"\"Files scanned for citations: scripts/*.py, and cheaply the markdown.\"\"\"\n+    \"\"\"Current, shipped text surfaces scanned for decision citations.\"\"\"\n     files = sorted((root / \"scripts\").glob(\"*.py\"))\n+    files += sorted((root / \"scripts\").glob(\"*.sh\"))\n     if include_docs:\n         docs = root / \"docs\"\n         if docs.is_dir():\n             files += sorted(docs.rglob(\"*.md\"))\n         files += sorted(root.glob(\"*.md\"))\n-    return files\n+        pyproject = root / \"pyproject.toml\"\n+        if pyproject.is_file():\n+            files.append(pyproject)\n+        for relative in (\".github\", \".codex\"):\n+            surface = root / relative\n+            if surface.is_dir():\n+                files += sorted(\n+                    path\n+                    for path in surface.rglob(\"*\")\n+                    if path.is_file()\n+                    and path.suffix in {\".md\", \".py\", \".sh\", \".toml\", \".yaml\", \".yml\"}\n+                )\n+    return sorted(set(files))\n \n \n def find_adr_reference_violations(\n@@ -94,10 +111,9 @@\n ) -> list[Violation]:\n     \"\"\"Return every ``ADR ####`` citation under ``root`` that fails to resolve.\n \n-    A citation resolves when exactly one ``docs/adr/####-*.md`` matches its\n-    number. Zero matches is a \"missing\" violation; two or more is \"ambiguous\".\n-    Records that are never cited produce nothing -- the converse is not\n-    required.\n+    A citation resolves when exactly one record in the same namespace matches\n+    its number. Zero matches is a \"missing\" violation; two or more is\n+    \"ambiguous\". Records that are never cited produce nothing.\n     \"\"\"\n     root = Path(root)\n     index = _adr_index(root)\n@@ -110,13 +126,15 @@\n         rel = path.relative_to(root).as_posix()\n         for lineno, line in enumerate(text.splitlines(), start=1):\n             for match in _CITATION.finditer(line):\n-                number = match.group(1)\n-                found = index.get(number, [])\n+                namespace, number = match.groups()\n+                found = index.get((namespace, number), [])\n                 if len(found) == 1:\n                     continue\n                 reason = \"missing\" if not found else \"ambiguous\"\n                 rels = [p.relative_to(root).as_posix() for p in found]\n-                violations.append(Violation(rel, number, lineno, reason, rels))\n+                violations.append(\n+                    Violation(rel, namespace, number, lineno, reason, rels)\n+                )\n     return violations\n \n \n@@ -129,8 +147,8 @@\n         self.assertEqual(\n             violations,\n             [],\n-            \"Every 'ADR ####' citation must resolve to exactly one \"\n-            f\"docs/adr/####-*.md:\\n{detail}\",\n+            \"Every ADR/SADR citation must resolve exactly once in its own \"\n+            f\"docs/adr namespace:\\n{detail}\",\n         )\n \n     def test_scan_actually_reads_scripts(self) -> None:\n@@ -142,12 +160,38 @@\n             \"the citation scan must cover scripts/*.py\",\n         )\n \n+    def test_platform_citations_do_not_use_unresolved_numeric_shorthand(self) -> None:\n+        shorthand = re.compile(r\"SADR-\\d{4}/\\d{4}\")\n+        offenders = []\n+        for path in _citing_files(REPO_ROOT):\n+            text = path.read_text(encoding=\"utf-8\")\n+            if shorthand.search(text):\n+                offenders.append(path.relative_to(REPO_ROOT).as_posix())\n+        self.assertEqual(\n+            offenders,\n+            [],\n+            \"repeat the SADR prefix so every cited number is independently resolvable\",\n+        )\n+\n     def test_carried_records_are_indexed(self) -> None:\n         # The two records this Goal carried onto the default branch, and which\n         # the scripts cite most, must be present.\n         index = _adr_index(REPO_ROOT)\n-        self.assertIn(\"0016\", index)\n-        self.assertIn(\"0017\", index)\n+        self.assertIn((\"SADR\", \"0016\"), index)\n+        self.assertIn((\"SADR\", \"0017\"), index)\n+\n+    def test_platform_tree_has_only_sadr_0001_through_0019(self) -> None:\n+        adr_dir = REPO_ROOT / \"docs\" / \"adr\"\n+        sadrs = sorted(adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\"))\n+        self.assertEqual(\n+            {path.name.split(\"-\", 2)[1] for path in sadrs},\n+            {f\"{number:04d}\" for number in range(1, 20)},\n+        )\n+        self.assertEqual(\n+            list(adr_dir.glob(\"[0-9][0-9][0-9][0-9]-*.md\")),\n+            [],\n+            \"the Shiki platform tree must not retain legacy numeric SADR paths\",\n+        )\n \n \n class FixtureAdrReferences(unittest.TestCase):\n@@ -180,6 +224,26 @@\n         self.assertIn(\"9991\", message)  # cited number\n         self.assertIn(\"docs/adr/9991-*.md\", message)  # missing path\n \n+    def test_target_adr_does_not_satisfy_missing_platform_sadr(self) -> None:\n+        root = self._make_repo(\n+            adr_names=[\"0042-target-decision.md\"],\n+            script_body='\"\"\"Target ADR 0042 exists, but platform SADR-0042 does not.\"\"\"\\n',\n+        )\n+        violations = find_adr_reference_violations(root, include_docs=False)\n+        self.assertEqual(len(violations), 1)\n+        self.assertEqual(violations[0].namespace, \"SADR\")\n+        self.assertEqual(violations[0].number, \"0042\")\n+        self.assertEqual(violations[0].expected_path, \"docs/adr/SADR-0042-*.md\")\n+\n+    def test_target_adr_and_platform_sadr_may_reuse_a_number(self) -> None:\n+        root = self._make_repo(\n+            adr_names=[\"0042-target-decision.md\", \"SADR-0042-platform-decision.md\"],\n+            script_body='\"\"\"Target ADR 0042 and platform SADR-0042 are distinct.\"\"\"\\n',\n+        )\n+        self.assertEqual(\n+            find_adr_reference_violations(root, include_docs=False), []\n+        )\n+\n     def test_uncited_adr_is_allowed(self) -> None:\n         # An ADR exists but nothing cites it -> not a violation. The converse\n         # (every ADR must be cited) is explicitly NOT required.\ndiff --git a/tests/test_guardian_review.py b/tests/test_guardian_review.py\nindex 88829e7810212635e2fc98aa5918d6f26294677d..00a85a237acd5bf2d6cb5b294128198c422402ca\n--- a/tests/test_guardian_review.py\n+++ b/tests/test_guardian_review.py\n@@ -162,7 +162,7 @@\n \n     def test_docs_only_is_light_and_not_conservative(self):\n         focus = gr.classify_review_focus([\"docs/adr/0099-x.md\", \"README.md\"], \"\", \"low\")\n-        self.assertTrue(any(\"Docs/ADR-only\" in f for f in focus))\n+        self.assertTrue(any(\"Docs/decision-record-only\" in f for f in focus))\n         self.assertFalse(any(\"conservative default\" in f.lower() for f in focus))\n \n     def test_unknown_category_is_conservative(self):\ndiff --git a/tests/test_policy_job_merged_prs.py b/tests/test_policy_job_merged_prs.py\nindex d45512e74681e5e649279236c7d4e79d5c3da471..b97989f8bd74d091de1a6b5275a20d8dc68c4512\n--- a/tests/test_policy_job_merged_prs.py\n+++ b/tests/test_policy_job_merged_prs.py\n@@ -3,8 +3,8 @@\n \n ``.github/workflows/shiki-cca-completion.yml`` has two consumers of the merged-PR\n proof: the CCA *signal* (``guardian_approval_signal.py`` in \"Prepare CCA context\")\n-and the authoritative gate (``mergegate_check.py`` in \"Enforce MergeGate\"). ADR\n-0017's bookkeeping-closeout exemption (condition 6) needs the merged implementation\n+and the authoritative gate (``mergegate_check.py`` in \"Enforce MergeGate\").\n+SADR-0017's bookkeeping-closeout exemption (condition 6) needs the merged implementation\n PR proven, and a closeout carries NO ``post_merge_reconcile`` marker. The signal\n already resolves ``MERGED_PRS`` unconditionally, but \"Enforce MergeGate\" used to\n resolve it only inside ``grep -q '<!-- shiki:post_merge_reconcile -->'``. So a\ndiff --git a/tests/test_sadr_install_migration.py b/tests/test_sadr_install_migration.py\nnew file mode 100644\nindex 0000000000000000000000000000000000000000..d51eac872b1c79ff4579c4c1401ca5d775a60d8d\n--- /dev/null\n+++ b/tests/test_sadr_install_migration.py\n@@ -0,0 +1,237 @@\n+\"\"\"SADR installation and collision-safe legacy upgrade behavior.\"\"\"\n+\n+from __future__ import annotations\n+\n+import contextlib\n+import hashlib\n+import io\n+import json\n+import shutil\n+import tempfile\n+import unittest\n+from pathlib import Path\n+\n+import shiki_test_support  # noqa: F401  (path bootstrap)\n+\n+import shiki_installer\n+from tests.test_adr_references import find_adr_reference_violations\n+\n+\n+PRODUCT_NUMBERS = tuple(range(13, 19))\n+\n+\n+def _quiet_install(target: Path, *, force: bool, validate: bool) -> None:\n+    with contextlib.redirect_stdout(io.StringIO()):\n+        shiki_installer.install_template(target, force=force, validate=validate)\n+\n+\n+def _sha256(path: Path) -> str:\n+    return hashlib.sha256(path.read_bytes()).hexdigest()\n+\n+\n+def _legacy_sadr_paths() -> tuple[str, ...]:\n+    return tuple(\n+        f\"docs/adr/{path.name.removeprefix('SADR-')}\"\n+        for path in sorted((shiki_installer.ROOT / \"docs\" / \"adr\").glob(\"SADR-*.md\"))\n+        if path.name != \"SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\"\n+    )\n+\n+\n+def _create_legacy_upgrade_fixture(\n+    target: Path,\n+) -> tuple[dict[str, bytes], tuple[str, ...]]:\n+    adr_dir = target / \"docs\" / \"adr\"\n+    adr_dir.mkdir(parents=True)\n+    product_bytes = {\n+        f\"{number:04d}-cubeshare-product-decision.md\": (\n+            f\"# CubeShare ADR {number:04d}\\n\\nDo not rewrite {number}.\\n\"\n+        ).encode()\n+        for number in PRODUCT_NUMBERS\n+    }\n+    for name, content in product_bytes.items():\n+        (adr_dir / name).write_bytes(content)\n+\n+    legacy_paths = _legacy_sadr_paths()\n+    for relative in legacy_paths:\n+        (target / relative).write_bytes(f\"legacy Shiki bytes for {relative}\\n\".encode())\n+\n+    stamp_path = target / shiki_installer.INSTALL_STAMP_PATH\n+    stamp_path.parent.mkdir(parents=True, exist_ok=True)\n+    stamp = {\n+        \"version\": shiki_installer.INSTALL_STAMP_VERSION,\n+        \"platform_commit\": None,\n+        \"installed_at\": \"2000-01-01T00:00:00+00:00\",\n+        \"digests\": {\n+            relative: _sha256(target / relative) for relative in legacy_paths\n+        },\n+    }\n+    stamp_path.write_text(\n+        json.dumps(stamp, indent=2, sort_keys=True) + \"\\n\", encoding=\"utf-8\"\n+    )\n+    return product_bytes, legacy_paths\n+\n+\n+def _tree_snapshot(root: Path) -> dict[str, tuple[str, bytes | str]]:\n+    \"\"\"Recursive byte/shape snapshot used to prove preflight is write-free.\"\"\"\n+    snapshot: dict[str, tuple[str, bytes | str]] = {}\n+    for path in sorted(root.rglob(\"*\")):\n+        relative = path.relative_to(root).as_posix()\n+        if path.is_symlink():\n+            snapshot[relative] = (\"symlink\", path.readlink().as_posix())\n+        elif path.is_dir():\n+            snapshot[relative] = (\"directory\", \"\")\n+        else:\n+            snapshot[relative] = (\"file\", path.read_bytes())\n+    return snapshot\n+\n+\n+class FreshSadrInstallTests(unittest.TestCase):\n+    def setUp(self) -> None:\n+        self.tmp = Path(tempfile.mkdtemp(prefix=\"shiki-sadr-fresh-\"))\n+        self.addCleanup(lambda: shutil.rmtree(self.tmp, ignore_errors=True))\n+        self.target = self.tmp / \"target\"\n+        self.adr_dir = self.target / \"docs\" / \"adr\"\n+        self.adr_dir.mkdir(parents=True)\n+        self.product_bytes = {\n+            f\"{number:04d}-product-decision.md\": (\n+                f\"# Product ADR {number:04d}\\n\\nTarget-owned bytes {number}.\\n\"\n+            ).encode()\n+            for number in PRODUCT_NUMBERS\n+        }\n+        for name, content in self.product_bytes.items():\n+            (self.adr_dir / name).write_bytes(content)\n+\n+    def test_fresh_install_preserves_product_adrs_and_installs_all_sadrs(self) -> None:\n+        _quiet_install(self.target, force=False, validate=True)\n+\n+        for name, expected in self.product_bytes.items():\n+            self.assertEqual((self.adr_dir / name).read_bytes(), expected)\n+\n+        sadr_numbers = {\n+            path.name.split(\"-\", 2)[1]\n+            for path in self.adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\")\n+        }\n+        self.assertEqual(sadr_numbers, {f\"{number:04d}\" for number in range(1, 20)})\n+\n+        numeric_records = {\n+            path.name\n+            for path in self.adr_dir.glob(\"[0-9][0-9][0-9][0-9]-*.md\")\n+        }\n+        self.assertEqual(numeric_records, set(self.product_bytes))\n+        stamp = json.loads(\n+            (self.target / shiki_installer.INSTALL_STAMP_PATH).read_text(\n+                encoding=\"utf-8\"\n+            )\n+        )\n+        for path in self.adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\"):\n+            relative = path.relative_to(self.target).as_posix()\n+            self.assertEqual(stamp[\"digests\"][relative], _sha256(path))\n+        self.assertEqual(find_adr_reference_violations(self.target), [])\n+\n+\n+class LegacySadrUpgradeTests(unittest.TestCase):\n+    def setUp(self) -> None:\n+        self.tmp = Path(tempfile.mkdtemp(prefix=\"shiki-sadr-upgrade-\"))\n+        self.addCleanup(lambda: shutil.rmtree(self.tmp, ignore_errors=True))\n+        self.target = self.tmp / \"target\"\n+        self.adr_dir = self.target / \"docs\" / \"adr\"\n+        self.product_bytes, self.legacy_paths = _create_legacy_upgrade_fixture(\n+            self.target\n+        )\n+\n+    def test_force_upgrade_deletes_only_stamped_legacy_sadrs(self) -> None:\n+        _quiet_install(self.target, force=True, validate=True)\n+\n+        for relative in self.legacy_paths:\n+            self.assertFalse((self.target / relative).exists(), relative)\n+        for name, expected in self.product_bytes.items():\n+            self.assertEqual((self.adr_dir / name).read_bytes(), expected)\n+\n+        sadrs = list(self.adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\"))\n+        self.assertEqual(len(sadrs), 19)\n+        refreshed = json.loads(\n+            (self.target / shiki_installer.INSTALL_STAMP_PATH).read_text(encoding=\"utf-8\")\n+        )\n+        for relative in self.legacy_paths:\n+            self.assertNotIn(relative, refreshed[\"digests\"])\n+        for path in sadrs:\n+            relative = path.relative_to(self.target).as_posix()\n+            self.assertEqual(refreshed[\"digests\"][relative], _sha256(path))\n+\n+    def test_non_force_install_never_deletes_legacy_files(self) -> None:\n+        before = {\n+            relative: (self.target / relative).read_bytes()\n+            for relative in self.legacy_paths\n+        }\n+        _quiet_install(self.target, force=False, validate=False)\n+        for relative, expected in before.items():\n+            self.assertEqual((self.target / relative).read_bytes(), expected)\n+        refreshed = json.loads(\n+            (self.target / shiki_installer.INSTALL_STAMP_PATH).read_text(encoding=\"utf-8\")\n+        )\n+        for relative in self.legacy_paths:\n+            self.assertEqual(refreshed[\"digests\"][relative], _sha256(self.target / relative))\n+\n+\n+class LegacyCleanupPreflightTests(unittest.TestCase):\n+    def setUp(self) -> None:\n+        self.tmp = Path(tempfile.mkdtemp(prefix=\"shiki-sadr-preflight-\"))\n+        self.addCleanup(lambda: shutil.rmtree(self.tmp, ignore_errors=True))\n+\n+    def test_all_ownership_blockers_abort_before_any_write(self) -> None:\n+        cases = (\n+            (\"absent\", \"install stamp is absent\"),\n+            (\"unreadable\", \"install stamp is unreadable\"),\n+            (\"malformed\", \"install stamp is malformed\"),\n+            (\"missing-digest\", \"install stamp has no digest\"),\n+            (\"digest-mismatch\", \"digest mismatch\"),\n+        )\n+        for name, expected_reason in cases:\n+            with self.subTest(name=name):\n+                target = self.tmp / name\n+                _, legacy_paths = _create_legacy_upgrade_fixture(target)\n+                stamp_path = target / shiki_installer.INSTALL_STAMP_PATH\n+\n+                if name == \"absent\":\n+                    stamp_path.unlink()\n+                elif name == \"unreadable\":\n+                    stamp_path.unlink()\n+                    stamp_path.mkdir()\n+                elif name == \"malformed\":\n+                    stamp_path.write_text(\"{not-json\\n\", encoding=\"utf-8\")\n+                else:\n+                    stamp = json.loads(stamp_path.read_text(encoding=\"utf-8\"))\n+                    if name == \"missing-digest\":\n+                        stamp[\"digests\"] = {}\n+                    else:\n+                        stamp[\"digests\"] = {\n+                            relative: \"deadbeef\" for relative in legacy_paths\n+                        }\n+                    stamp_path.write_text(\n+                        json.dumps(stamp, indent=2, sort_keys=True) + \"\\n\",\n+                        encoding=\"utf-8\",\n+                    )\n+\n+                before = _tree_snapshot(target)\n+                with self.assertRaises(shiki_installer.ShikiError) as raised:\n+                    _quiet_install(target, force=True, validate=False)\n+                after = _tree_snapshot(target)\n+\n+                self.assertEqual(before, after, f\"{name} preflight changed the tree\")\n+                message = str(raised.exception)\n+                self.assertIn(expected_reason, message)\n+                self.assertIn(\"No files were written\", message)\n+                for relative in legacy_paths:\n+                    self.assertIn(relative, message)\n+\n+    def test_absent_stamp_is_allowed_when_no_legacy_path_exists(self) -> None:\n+        target = self.tmp / \"no-legacy\"\n+        target.mkdir()\n+        _quiet_install(target, force=True, validate=False)\n+        self.assertTrue(\n+            (target / \"docs\" / \"adr\" / \"SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\").is_file()\n+        )\n+\n+\n+if __name__ == \"__main__\":\n+    unittest.main()\n\n2026-08-07T08:23:44.153664Z ERROR codex_models_manager::manager: failed to renew cache TTL: missing field `base_instructions` at line 94 column 5\nexec\n/bin/zsh -lc \"sed -n '1,260p' .shiki/tasks/T-20260807T073307221254Z-2d22d654.json\" in /private/tmp/shiki-adr-collision.pa0KwY/.worktrees/shiki-t-20260807t073307221254z-2d22d654-introduc\n succeeded in 0ms:\n{\n  \"acceptance_checks\": [\n    \"The platform tree has unique SADR-0001 through SADR-0019, no legacy numeric Shiki platform ADR file, and every live SADR citation resolves exactly once.\",\n    \"The validator resolves target ADR NNNN to docs/adr/NNNN-*.md and platform SADR NNNN to docs/adr/SADR-NNNN-*.md; fixtures prove both namespaces may reuse a number.\",\n    \"Current platform references in root governance docs, docs, scripts, workflows/prompts, .codex/skills/shiki/SKILL.md, and packaging use SADR or explicit generic target-ADR examples. Historical .shiki records remain unchanged except this Goal's lifecycle evidence.\",\n    \"A fresh-install fixture with product ADRs 0013-0018 preserves their bytes, installs SADR-0001 through SADR-0019, stamps SADR paths, and validates.\",\n    \"A forced-upgrade fixture with product collisions plus matching stamped legacy Shiki ADRs deletes only legacy platform paths, preserves product ADR bytes, installs SADRs, and removes legacy paths from the refreshed stamp.\",\n    \"Parameterized absent, unreadable/malformed, missing-digest, and digest-mismatch cases report all blockers before ShikiError; recursive snapshots prove no filesystem byte changed.\",\n    \"No legacy path means an absent stamp does not block; a non-force install does not silently delete existing files.\",\n    \"Red-green-refactor and a structured code-review verdict are recorded, with all blocking findings repaired and re-reviewed.\",\n    \"python3 -m unittest discover -s tests, control-plane and adversarial shell suites, validate_shiki, and fresh-install/upgrade tests pass locally and in CI on the exact head.\"\n  ],\n  \"assigned_runtime\": \"codex\",\n  \"cca_checklist_profile\": [\n    \"CCA-01\",\n    \"CCA-02\",\n    \"CCA-03\",\n    \"CCA-04\",\n    \"CCA-05\",\n    \"CCA-06\",\n    \"CCA-07\",\n    \"CCA-08\",\n    \"CCA-09\",\n    \"CCA-10\",\n    \"CCA-11\",\n    \"CCA-12\"\n  ],\n  \"dependencies\": [],\n  \"expected_branch\": \"shiki/t-20260807t073307221254z-2d22d654-introduce-sadr-and-migrate-installs-without-touc\",\n  \"expected_pr\": null,\n  \"github_issue\": 322,\n  \"goal_id\": \"G-20260807T073307219752Z-1ee1ed68\",\n  \"id\": \"T-20260807T073307221254Z-2d22d654\",\n  \"ledger_evidence\": [\n    \"L-20260807T073307221361Z-b3c3a718\",\n    \"L-20260807T080107948254Z-61f24da0\",\n    \"L-20260807T080228312210Z-07ac626e\",\n    \"L-20260807T080228575038Z-d574f629\",\n    \"L-20260807T080256330734Z-5bf40cb6\"\n  ],\n  \"locks\": [\n    \"path:.codex/**\",\n    \"path:.github/**\",\n    \"path:.shiki/**\",\n    \"path:AGENTS.md\",\n    \"path:CLAUDE.md\",\n    \"path:CONTEXT.md\",\n    \"path:CONTRIBUTING.md\",\n    \"path:SYSTEM_PROMPT.md\",\n    \"path:docs/**\",\n    \"path:pyproject.toml\",\n    \"path:scripts/**\",\n    \"path:tests/**\"\n  ],\n  \"non_goals\": [\n    \"Mutating target product ADRs.\",\n    \"Rewriting historical .shiki evidence records.\",\n    \"Deleting any legacy path without exact stamp path and digest match.\",\n    \"Changing approval, CCA, MergeGate, lock, runtime, or repair semantics.\",\n    \"Fixing the separate .gitignore overwrite defect.\"\n  ],\n  \"required_skills\": [\n    \"diagnose\",\n    \"tdd\",\n    \"code-review\"\n  ],\n  \"risk_level\": \"high\",\n  \"scope\": \"Rename Shiki platform decision records to SADR-0001 through SADR-0018; add SADR-0019; update current citations, packaging metadata, documentation, workflows, prompts, repo-local skill guidance, and the reference validator for separate ADR/SADR namespaces; add an installer preflight that inventories legacy numeric Shiki ADR paths and authorizes deletion only from matching existing install-stamp path digests before any write; then apply authorized cleanup, install SADRs, refresh the stamp, and prove fresh-install plus CubeShare-shaped collision-upgrade behavior with TDD.\",\n  \"status\": \"running\",\n  \"test_command\": \"python3 -m unittest discover -s tests && bash scripts/test_shiki_control_plane.sh && bash scripts/test_shiki_adversarial_state.sh && python3 scripts/validate_shiki.py --target .\",\n  \"title\": \"Introduce SADR and migrate installs without touching product ADRs\"\n}\n\nexec\n/bin/zsh -lc \"rg -n 'pre_pr_code_review|ledger_evidence' .shiki/schemas/task.schema.json scripts -g '*.py'\" in /private/tmp/shiki-adr-collision.pa0KwY/.worktrees/shiki-t-20260807t073307221254z-2d22d654-introduc\n succeeded in 0ms:\n.shiki/schemas/task.schema.json:18:    \"ledger_evidence\"\n.shiki/schemas/task.schema.json:58:    \"ledger_evidence\": {\nscripts/shiki_runtime.py:303:    task.setdefault(\"ledger_evidence\", []).append(ledger_id)\nscripts/shiki_runtime.py:411:        task.setdefault(\"ledger_evidence\", []).append(ledger_id)\nscripts/shiki_runtime.py:617:    task.setdefault(\"ledger_evidence\", []).append(ledger_id)\nscripts/shiki_github.py:703:def pre_pr_code_review_section(task: dict[str, Any]) -> list[str]:\nscripts/shiki_github.py:706:    Rendered from the loop-recorded ``pre_pr_code_review`` block \u2014 the verdict of\nscripts/shiki_github.py:712:    review = task.get(\"pre_pr_code_review\") or {}\nscripts/shiki_github.py:766:            *pre_pr_code_review_section(task),\nscripts/shiki_github.py:777:            *[f\"- {entry}\" for entry in task.get(\"ledger_evidence\", [])],\nscripts/shiki_github.py:816:    task.setdefault(\"ledger_evidence\", []).append(ledger_id)\nscripts/shiki_github.py:861:    task.setdefault(\"ledger_evidence\", []).append(ledger_id)\nscripts/validate_shiki.py:141:    \"ledger_evidence\",\nscripts/validate_shiki.py:1233:    require_list(path, data, \"ledger_evidence\", non_empty=True)\nscripts/shiki_tasks.py:307:    payload[\"ledger_evidence\"] = [ledger_id]\nscripts/shiki_tasks.py:349:        \"ledger_evidence\": [ledger_id],\nscripts/shiki_tasks.py:460:    task.setdefault(\"ledger_evidence\", []).append(ledger_id)\nscripts/shiki_tasks.py:574:    task.setdefault(\"ledger_evidence\", []).append(ledger_id)\nscripts/shiki_tasks.py:605:    task.setdefault(\"ledger_evidence\", []).append(ledger_id)\nscripts/shiki_tasks.py:962:        \"ledger_evidence\": [ledger_id],\nscripts/shiki_tasks.py:1025:    task.setdefault(\"ledger_evidence\", []).append(ledger_id)\nscripts/shiki_tasks.py:1129:    task.setdefault(\"ledger_evidence\", []).append(ledger_id)\nscripts/shiki_tasks.py:1253:    task.setdefault(\"ledger_evidence\", []).append(ledger_id)\nscripts/shiki_tasks.py:1314:    task.setdefault(\"ledger_evidence\", []).append(ledger_id)\nscripts/shiki_tasks.py:1374:    task.setdefault(\"ledger_evidence\", []).append(ledger_id)\nscripts/shiki_tasks.py:1431:    goal.setdefault(\"ledger_evidence\", []).append(ledger_id)\nscripts/mergegate_check.py:204:    ``ledger_evidence`` is expanded ONLY when its own ``goal_id`` matches this\nscripts/mergegate_check.py:226:    for ledger_id in task.get(\"ledger_evidence\") or []:\nscripts/mergegate_check.py:239:        # PR-authored task file cannot pad ledger_evidence with someone else's\nscripts/mergegate_check.py:948:# ledger_evidence) that an amendment \u2014 which re-opens a task for re-dispatch, not\nscripts/mergegate_check.py:955:    \"ledger_evidence\",\nscripts/mergegate_check.py:1065:    ``ledger_evidence`` byte-identical to the base snapshot.\"\"\"\nscripts/mergegate_check.py:1134:         expected_pr/closeout_pr/expected_branch/ledger_evidence frozen to base\nscripts/mergegate_check.py:1491:_POST_MERGE_TASK_FIELDS = {\"status\", \"expected_pr\", \"ledger_evidence\"}\nscripts/mergegate_check.py:1976:_CLOSEOUT_MUTABLE_TASK_FIELDS = frozenset({\"status\", \"expected_pr\", \"closeout_pr\", \"ledger_evidence\", \"expected_branch\"})\nscripts/mergegate_check.py:2035:         closeout_pr, ledger_evidence and expected_branch \u2014 see that definition for\nscripts/mergegate_check.py:2328:    for ledger_id in task.get(\"ledger_evidence\") or []:\nscripts/mergegate_check.py:2330:            blocking.append(\"Task ledger_evidence contains a non-string entry\")\nscripts/mergegate_check.py:2487:# expected_branch/ledger_evidence) AND loop-written bookkeeping such as\nscripts/mergegate_check.py:2488:# ``pre_pr_code_review`` / ``cca_rerun_count`` that ``shiki_loop`` writes onto the\nscripts/mergegate_check.py:2686:    allowed_ledger_ids = {str(value) for value in task.get(\"ledger_evidence\") or []}\nscripts/mergegate_check.py:2739:                blocking.append(f\"PR changes ledger {ledger_id} not listed in current task ledger_evidence; state_class={state_class}\")\nscripts/shiki_loop.py:857:    task.setdefault(\"ledger_evidence\", []).append(ledger_id)\nscripts/shiki_loop.py:1150:    loop's ``status`` / ``cca_rerun_count`` / ``ledger_evidence`` bookkeeping. When\nscripts/shiki_loop.py:1219:    ledger in ``task.ledger_evidence``, AND every ``.shiki``-relative path those\nscripts/shiki_loop.py:1271:    for ledger_id in task.get(\"ledger_evidence\") or []:\nscripts/shiki_loop.py:1286:        # carried (it is listed in ledger_evidence); only the transitive\nscripts/shiki_loop.py:1290:        # that looser bound would let a PR-authored task file pad ledger_evidence\nscripts/shiki_loop.py:1309:    and EVERY file referenced by ``task.ledger_evidence`` (each ledger plus the\nscripts/shiki_loop.py:1312:    (already appended to ``ledger_evidence`` by ``create_github_pr_for_task``);\nscripts/shiki_loop.py:1431:def _run_pre_pr_code_review(target: Path, task_id: str) -> dict[str, Any]:\nscripts/shiki_loop.py:1441:      is recorded and ``pre_pr_code_review`` is written onto the task so the\nscripts/shiki_loop.py:1510:            task.setdefault(\"ledger_evidence\", []).append(ledger_id)\nscripts/shiki_loop.py:1514:                task[\"pre_pr_code_review\"] = {\nscripts/shiki_loop.py:1532:            task[\"pre_pr_code_review\"] = {\nscripts/shiki_loop.py:1561:        task.setdefault(\"ledger_evidence\", []).append(ledger_id)\nscripts/shiki_loop.py:1562:        task[\"pre_pr_code_review\"] = {\"verdict\": \"blocking\", \"findings\": findings, \"ledger_id\": ledger_id}\nscripts/shiki_loop.py:1577:    task.setdefault(\"ledger_evidence\", []).append(ledger_id)\nscripts/shiki_loop.py:1578:    task[\"pre_pr_code_review\"] = {\"verdict\": \"clean\", \"findings\": findings, \"ledger_id\": ledger_id}\nscripts/shiki_loop.py:1609:    EXEC record, and append the ledger id to ``task.ledger_evidence``.\nscripts/shiki_loop.py:1676:        task.setdefault(\"ledger_evidence\", []).append(ledger_id)\nscripts/shiki_loop.py:1799:            # TASK's ledger_evidence, so mirror the completion ledger across.\nscripts/shiki_loop.py:1802:            for lid in wt_goal.get(\"ledger_evidence\", []):\nscripts/shiki_loop.py:1810:                if led.get(\"type\") == \"completion\" and lid not in (wt_task.get(\"ledger_evidence\") or []):\nscripts/shiki_loop.py:1811:                    wt_task.setdefault(\"ledger_evidence\", []).append(lid)\nscripts/shiki_loop.py:1853:        if pull_ledger not in wt_task.get(\"ledger_evidence\", []):\nscripts/shiki_loop.py:1854:            wt_task.setdefault(\"ledger_evidence\", []).append(pull_ledger)\nscripts/shiki_loop.py:2107:        for ledger_id in entity.get(\"ledger_evidence\") or []:\nscripts/shiki_loop.py:2300:        review = _run_pre_pr_code_review(target, task_id)\nscripts/shiki_loop.py:2376:        task.setdefault(\"ledger_evidence\", []).append(ledger_id)\nscripts/shiki_loop.py:2380:        # rest of ledger_evidence cite) onto the task branch. The PR already exists,\nscripts/shiki_loop.py:2472:        # evidence ledger_evidence cites, onto the task branch so MergeGate does not\nscripts/shiki_loop.py:2526:        task.setdefault(\"ledger_evidence\", []).append(ledger_id)\n\nexec\n/bin/zsh -lc \"sed -n '1,260p' .shiki/schemas/ledger-entry.schema.json\" in /private/tmp/shiki-adr-collision.pa0KwY/.worktrees/shiki-t-20260807t073307221254z-2d22d654-introduc\n exited 1 in 0ms:\nsed: .shiki/schemas/ledger-entry.schema.json: No such file or directory\n\nexec\n/bin/zsh -lc \"rg -n 'code-review|pre_pr_code_review' .shiki/ledger .shiki/tasks --glob '*.json' | head -80\" in /private/tmp/shiki-adr-collision.pa0KwY/.worktrees/shiki-t-20260807t073307221254z-2d22d654-introduc\n succeeded in 0ms:\n.shiki/tasks/T-20260728T103742824401Z-f198cd24.json:60:  \"pre_pr_code_review\": {\n.shiki/tasks/T-20260728T103742824401Z-f198cd24.json:78:    \"code-review\"\n.shiki/tasks/T-20260729T065622770078Z-3662bff2.json:36:  \"pre_pr_code_review\": {\n.shiki/tasks/T-20260729T065622770078Z-3662bff2.json:43:    \"code-review\"\n.shiki/tasks/T-20260731T045548843829Z-ee2deb90.json:41:  \"pre_pr_code_review\": {\n.shiki/tasks/T-20260731T045548843829Z-ee2deb90.json:48:    \"code-review\"\n.shiki/tasks/T-20260801T041653348134Z-bf8a3c4b.json:46:    \"code-review\"\n.shiki/tasks/T-20260617T090739479224Z-cfdbfa06.json:36:    \"No code-review verifier (T3).\",\n.shiki/tasks/T-20260617T090739479224Z-cfdbfa06.json:41:    \"code-review\"\n.shiki/tasks/T-20260729T065622767723Z-f138eccf.json:47:  \"pre_pr_code_review\": {\n.shiki/tasks/T-20260729T065622767723Z-f138eccf.json:65:    \"code-review\"\n.shiki/tasks/T-20260801T041653423742Z-d5dc4a28.json:39:    \"code-review\"\n.shiki/tasks/T-20260804T082618611335Z-02de0043.json:63:  \"pre_pr_code_review\": {\n.shiki/tasks/T-20260804T082618611335Z-02de0043.json:70:    \"code-review\"\n.shiki/tasks/T-20260618T062324783813Z-f62d375b.json:60:    \"code-review\"\n.shiki/tasks/T-20260805T025939375698Z-94a2fa82.json:47:  \"pre_pr_code_review\": {\n.shiki/tasks/T-20260805T025939375698Z-94a2fa82.json:54:    \"code-review\"\n.shiki/tasks/T-20260729T065622763684Z-ee01f3c4.json:47:  \"pre_pr_code_review\": {\n.shiki/tasks/T-20260729T065622763684Z-ee01f3c4.json:65:    \"code-review\"\n.shiki/tasks/T-20260729T072437602381Z-cd8cdfd8.json:43:  \"pre_pr_code_review\": {\n.shiki/tasks/T-20260729T072437602381Z-cd8cdfd8.json:56:    \"code-review\"\n.shiki/tasks/T-20260801T085710529159Z-575d4080.json:51:  \"pre_pr_code_review\": {\n.shiki/tasks/T-20260801T085710529159Z-575d4080.json:64:    \"code-review\"\n.shiki/tasks/T-20260804T095105534980Z-530c4f17.json:65:  \"pre_pr_code_review\": {\n.shiki/tasks/T-20260804T095105534980Z-530c4f17.json:83:    \"code-review\"\n.shiki/tasks/T-20260625T070242177499Z-a75c7c56.json:35:    \"code-review\"\n.shiki/tasks/T-20260729T092307427377Z-c8531f7e.json:43:  \"pre_pr_code_review\": {\n.shiki/tasks/T-20260729T092307427377Z-c8531f7e.json:50:    \"code-review\"\n.shiki/tasks/T-20260801T041653274008Z-54d3477c.json:49:  \"pre_pr_code_review\": {\n.shiki/tasks/T-20260801T041653274008Z-54d3477c.json:56:    \"code-review\"\n.shiki/tasks/T-20260612T152357707653Z-e200274d.json:50:    \"code-review\"\n.shiki/tasks/T-20260806T065143409817Z-dbdae1cf.json:66:  \"pre_pr_code_review\": {\n.shiki/tasks/T-20260806T065143409817Z-dbdae1cf.json:89:    \"code-review\"\n.shiki/tasks/T-20260729T065622765968Z-234bf98b.json:42:  \"pre_pr_code_review\": {\n.shiki/tasks/T-20260729T065622765968Z-234bf98b.json:49:    \"code-review\"\n.shiki/tasks/T-20260612T152357707056Z-90925f53.json:46:    \"code-review\"\n.shiki/tasks/T-20260727T005125902419Z-6c2db440.json:57:  \"pre_pr_code_review\": {\n.shiki/tasks/T-20260727T005125902419Z-6c2db440.json:80:    \"code-review\"\n.shiki/tasks/T-20260729T065622763023Z-9b974770.json:47:  \"pre_pr_code_review\": {\n.shiki/tasks/T-20260729T065622763023Z-9b974770.json:65:    \"code-review\"\n.shiki/tasks/T-20260801T040836632579Z-2cde7018.json:44:  \"pre_pr_code_review\": {\n.shiki/tasks/T-20260801T040836632579Z-2cde7018.json:51:    \"code-review\"\n.shiki/tasks/T-20260729T065622768255Z-da448d45.json:35:  \"pre_pr_code_review\": {\n.shiki/tasks/T-20260729T065622768255Z-da448d45.json:48:    \"code-review\"\n.shiki/tasks/T-20260612T045948146790Z-a27bcfa2.json:54:    \"No KNOWN_SKILLS or code-review skill changes (G-B)\",\n.shiki/tasks/T-20260804T095105534398Z-c2ef0cfc.json:52:  \"pre_pr_code_review\": {\n.shiki/tasks/T-20260804T095105534398Z-c2ef0cfc.json:65:    \"code-review\"\n.shiki/tasks/T-20260801T003238841526Z-9f3f6f63.json:53:  \"pre_pr_code_review\": {\n.shiki/tasks/T-20260801T003238841526Z-9f3f6f63.json:56:        \"detail\": \"The task file records a pre_pr_code_review verdict of blocking: the new immutability rule broke scripts/test_shiki_control_plane.sh:1317 (assert_ready on a scope change), an out-of-lock required CI check. The current diff addresses it by adding path:scripts/test_shiki_control_plane.sh to the task locks and changing current_task_changed() to mutate closeout_pr (a mutable field, still ready) while adding a new assert_blocked('current-task-governance-frozen', ...) case. The recorded blocking verdict reflects an earlier state, not the reviewed diff.\",\n.shiki/tasks/T-20260801T003238841526Z-9f3f6f63.json:71:    \"code-review\"\n.shiki/tasks/T-20260613T095433721787Z-8d6b04d5.json:18:    \"code-review\"\n.shiki/tasks/T-20260801T041653424169Z-07860c35.json:38:    \"code-review\"\n.shiki/tasks/T-20260801T040836631930Z-97030be9.json:50:  \"pre_pr_code_review\": {\n.shiki/tasks/T-20260801T040836631930Z-97030be9.json:68:    \"code-review\"\n.shiki/tasks/T-20260806T084356215508Z-7a7dd694.json:47:  \"pre_pr_code_review\": {\n.shiki/tasks/T-20260806T084356215508Z-7a7dd694.json:60:    \"code-review\"\n.shiki/tasks/T-20260618T051732214442Z-bae28ee3.json:33:  \"pre_pr_code_review\": {\n.shiki/tasks/T-20260803T013839640984Z-2411811e.json:57:  \"pre_pr_code_review\": {\n.shiki/tasks/T-20260803T013839640984Z-2411811e.json:80:    \"code-review\"\n.shiki/tasks/T-20260806T034026771634Z-52142739.json:43:  \"pre_pr_code_review\": {\n.shiki/tasks/T-20260806T034026771634Z-52142739.json:50:    \"code-review\"\n.shiki/tasks/T-20260801T040948352877Z-eea16def.json:52:  \"pre_pr_code_review\": {\n.shiki/tasks/T-20260801T040948352877Z-eea16def.json:65:    \"code-review\"\n.shiki/tasks/T-20260612T055157606542Z-5205e0bd.json:3:    \"bash scripts/test_shiki_code_review_gate.sh passes: a plan task with required_skills [tdd, code-review] orchestrates and validates; a repair packet with --required-skill code-review passes schema validation; a task registered without explicit skills defaults to [tdd, code-review]\",\n.shiki/tasks/T-20260612T055157606542Z-5205e0bd.json:6:    \"This PR itself carries pre-PR code-review ledger evidence (dogfood of the new gate)\"\n.shiki/tasks/T-20260612T055157606542Z-5205e0bd.json:53:    \"code-review\"\n.shiki/tasks/T-20260612T055157606542Z-5205e0bd.json:56:  \"scope\": \"Add skills/engineering/code-review/SKILL.md (wrapper: run the native code-review skill after TDD green and before PR creation, apply fixes as the implementer on the task branch, record findings+resolutions as a ledger entry whose summary names the skill and as a PR body section). Extend validate_shiki.py KNOWN_SKILLS, .shiki/schemas/repair-packet.schema.json required_skill enum, and shiki_cli.py repair --required-skill choices with code-review. Flip default required_skills to [tdd, code-review] in shiki_tasks.register_task_from_plan and shiki_bootstrap start synthesis so MergeGate's existing skill-evidence rule enforces the gate on every new task. Document the contract: skill-gate.md row + quick map, AGENTS.md Skill Gate table row, checklists.md PR-12 item, triage-labels.md skill:code-review, implementation-policy.md implementer contract (TDD -> code-review -> PR), CLAUDE.md handoff standard line. Add scripts/test_shiki_code_review_gate.sh (plan task and repair packet carrying code-review pass validation; SKILL.md staged) and register it in installer TEMPLATE_PATHS. Guardian-approved implementation by Claude Code in this assigned task.\",\n.shiki/tasks/T-20260612T055157606542Z-5205e0bd.json:58:  \"title\": \"Register code-review as a Skill Gate skill and make it the default pre-PR implementer gate\"\n.shiki/tasks/T-20260729T065622769184Z-628504fe.json:42:  \"pre_pr_code_review\": {\n.shiki/tasks/T-20260729T065622769184Z-628504fe.json:49:    \"code-review\"\n.shiki/tasks/T-20260730T060551622687Z-7bea1bc2.json:43:  \"pre_pr_code_review\": {\n.shiki/tasks/T-20260730T060551622687Z-7bea1bc2.json:56:    \"code-review\"\n.shiki/ledger/L-20260805T013031899680Z-29398772.json:9:  \"summary\": \"Pre-PR code-review verdict BLOCKING for T-20260801T041653274543Z-6df3af7e (2 finding(s)); loop stops for diagnosis\",\n.shiki/tasks/T-20260612T062731178333Z-36e9bdc7.json:7:    \"This PR carries pre-PR code-review ledger evidence (PR-12)\"\n.shiki/tasks/T-20260612T062731178333Z-36e9bdc7.json:58:    \"code-review\"\n.shiki/ledger/L-20260731T061404552195Z-d2dfd80b.json:9:  \"summary\": \"Pre-PR code-review verdict CLEAN for T-20260729T065622770078Z-3662bff2 (independent read-only reviewer, code-review skill)\",\n.shiki/tasks/T-20260801T041653348713Z-6283adcd.json:41:    \"code-review\"\n.shiki/tasks/T-20260805T075725961882Z-91c2ecac.json:58:  \"pre_pr_code_review\": {\n.shiki/tasks/T-20260805T075725961882Z-91c2ecac.json:71:    \"code-review\"\n.shiki/tasks/T-20260805T075725961882Z-91c2ecac.json:74:  \"scope\": \"In `scripts/shiki_loop.py`'s `decide_task_action`, narrow the exclusion that today sends every red-check-plus-BEHIND state to `dispatch_repair`. The new `sync_branch` arm must satisfy ALL of these, and each is a separate assertion:\\n\\n  1. PLACEMENT. It is evaluated INSIDE the `if repairable_failed:` block (`:279-316`) but strictly AFTER `_cca_verdict_stop` (`:298-302`) has returned None. A CCA verdict of `needs_guardian` or `blocked` must still produce its terminal stop with a BEHIND branch \\u2014 the measured incident's own verdict was `blocked`, and laundering it into a head-moving sync is the exact impersonation pathway ADR 0010 forbids.\\n\\n  2. GUARDIAN. It never fires while `POLICY_GATE` is in `failed`, for any reason \\u2014 not merely when the policy gate is the ONLY red check. `repairable_failed` excludes POLICY_GATE (`:275`), so `{policy: fail, CCA: fail}` + BEHIND would otherwise sync and destroy a head-bound Guardian approval that had already been posted for the current head. The same rule also refuses to sync through a state where base movement defeated an ADR 0015 contract-approval carry (`shiki_contract_approval.py:193-198` compares base and head task governance fields directly), which would otherwise be silently restored by adopting base wholesale.\\n\\n  3. BOUND. Gate it on a durable task-file counter `base_sync_attempts` against a new `MAX_BASE_SYNCS`, following the existing `cca_rerun_count`/`MAX_CCA_RERUNS` (`:57`) pattern exactly. `.shiki/schemas/task.schema.json` is `additionalProperties: true` and does not declare `cca_rerun_count` either, so do NOT add the field to the schema \\u2014 that file belongs to another task. Once the cap is reached, fall through to today's behaviour unchanged: `dispatch_repair`, then the repair limit, then `stop_guardian`. The escalation at `:304` must stay reachable.\\n\\n  4. NO ATTEMPT CONSUMED. A sync must not create a repair packet and must not advance `repair_attempts`.\\n\\n  5. EVIDENCE. `_sync_branch` (`:1429-1525`) appends no ledger today; a sync that now remediates a RED check is a remediation decision on the same footing as `dispatch_repair` and must leave a durable record. Use an EXISTING ledger type \\u2014 registering a new one requires `.shiki/schemas/ledger.schema.json` and `validate_shiki.py:1892`'s set-equality binder to agree, and both are outside these locks.\\n\\n  6. WORKTREE RECONCILIATION. `_sync_branch` pushes from a throwaway detached checkout (`:1471`, `:1498`) and never updates the REGISTERED worktree's local branch ref; `ensure_physical_worktree` reuses an existing path without reconciling it. Today that is harmless because a sync is only followed by a merge. This change makes a sync be followed by a REPAIR, whose push (`:836`) is then a non-fast-forward failing with a misleading message. Fix it IN-FILE \\u2014 `scripts/shiki_runtime.py` must not be modified \\u2014 and do not use a destructive reset that could discard uncommitted work in the registered worktree.\\n\\n  7. BASE REF. `_sync_branch`'s `base: str = \\\"main\\\"` default (`:1429`) is hard-coded and the call site (`:1875`) passes nothing. This change promotes that path from rare to common, so resolve the base the way the rest of the module now does. Note the shape mismatch: `_default_base_ref` (`:772-795`) returns `origin/<name>` while `_sync_branch` builds `origin/{base}` \\u2014 do not produce `origin/origin/main`.\\n\\n  8. CLOSEOUT UNCHANGED. The closeout arm shares `_merge_or_sync` (`:260`) but its failing-check arm returns `stop_blocked`. `docs/adr/0012` pins that decision table verbatim. Assert the closeout decision table is unchanged so the ADR stays truthful.\\n\\nTESTS. `tests/test_branch_behind_sync.py:124` `test_behind_but_failing_check_is_not_synced` currently pins the old rule with the comment 'a failing check repairs, never syncs'; invert it and state the new rule and its conditions. Add pure-decision cases for: BEHIND + `needs_guardian` verdict -> stop_guardian; BEHIND + `blocked` -> stop_blocked; BEHIND + policy red + another red check -> NOT sync; BEHIND + pending -> wait_checks (unchanged); the counter at and past `MAX_BASE_SYNCS`; and BEHIND + repairable red under the cap -> sync_branch.\\n\\n`scripts/test_shiki_goal_loop.sh` MUST gain the end-to-end proof that a sync consumes no repair attempt, modelled on the identical assertion it already makes for the Guardian gate at `:408-417` (`find .shiki/repairs -name 'RP-*.json' | wc -l` before and after, asserted equal). The pure-fixture module cannot observe repair packets, so this is the only surface that can prove it.\\n\\n`tests/test_loop_branch_plumbing.py` imports `_commit_and_push_implementation` directly (`:22-27`); extend it to prove a repair dispatched AFTER a sync can still push.\\n\\nDo not introduce a `\\\"verdict\\\": \\\"...\\\"` string literal into `scripts/shiki_loop.py`: `tests/test_paired_invariants.py:546-548` regex-scans this file's source and set-equality-binds those literals to the code-review verdict vocabulary.\\n\\nREPRODUCIBILITY \\u2014 what this task removes, and what to change to get the same result.\\n  Symptom: a correct implementation is answered with three identical repair dispatches that change nothing, then `stop_guardian: required checks failed (MergeGate metadata check) and repair attempt limit reached`. The task is fine; the branch is merely stale.\\n  Measured 2026-08-05 on PR #288 (task T-20260805T040948888613Z-d4e830c9): the sole failing reason was 'PR must not delete base ledger file .shiki/ledger/L-20260805T052931749888Z-3017e935.json; state_class=append-only-evidence'. That file reached main via PR #287 after the branch forked. Each of the three repair packets diagnosed it as a loop-owned base sync and declined to act; the CCA returned verdict=blocked citing the same cause and quoting all three packets. A single `git merge origin/main` plus a push turned every required check green and the PR merged unchanged.\\n  Root cause: the only `sync_branch` emitter is reachable only when every required check is already green (`:101` docstring), while the red-check path returns `dispatch_repair` first (`:279-316`).\\n  Why it was written that way: `tests/test_branch_behind_sync.py:124` deliberately pinned 'a failing check repairs, never syncs'. The case where being behind is the CAUSE of the red check was not considered.\\n  Failure directions that matter, both measured against the source: syncing while the Guardian gate is red destroys a head-bound approval and can silently restore a defeated contract-approval carry; and an unbounded, unrecorded sync makes the repair limit dead code and trades a terminating stop for a livelock.\\n  Reproducible when: the #288 sequence \\u2014 BEHIND branch, red metadata check whose sole reason is a base file the branch never had \\u2014 is answered by one recorded sync and zero repair packets, while a `needs_guardian` verdict and a red policy gate on a BEHIND branch still stop exactly as today.\",\n\ndiff --git a/.codex/skills/shiki/SKILL.md b/.codex/skills/shiki/SKILL.md\nindex c8e2e85d9d75d49cc4f2d6286ee22659c4e53e75..93cded623b2c15d9f65c76491a1c2d813ac9e6c0\n--- a/.codex/skills/shiki/SKILL.md\n+++ b/.codex/skills/shiki/SKILL.md\n@@ -18,6 +18,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n Codex CLI does not register this skill as a custom `/shiki` slash command. When\n the user types `/shiki` in Codex CLI and gets \"Unrecognized command\", explain\n that this is expected: invoke Shiki in Codex with natural language such as\n@@ -69,7 +72,7 @@\n \n ## Responsibilities\n \n-- Claude Code implements and repairs by default (`shiki runner claude`, ADR 0008); Codex implements and repairs tasks explicitly assigned to `codex`.\n+- Claude Code implements and repairs by default (`shiki runner claude`, SADR-0008); Codex implements and repairs tasks explicitly assigned to `codex`.\n - Claude Code Action can act as GitHub-side CCA or reviewer.\n - CCA judges completion.\n - MergeGate authorizes state transitions and merge readiness.\ndiff --git a/.github/prompts/cca-completion-check.md b/.github/prompts/cca-completion-check.md\nindex ed203ad5f302ecc81372d8ab27d2b1232ca97497..fd89950528333ef6242e6d5443904f5392ab7e56\n--- a/.github/prompts/cca-completion-check.md\n+++ b/.github/prompts/cca-completion-check.md\n@@ -19,6 +19,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n ## Required Reading\n \n Read only the files needed to judge the PR contract. Prefer this order:\ndiff --git a/.github/prompts/codex-task-handoff.md b/.github/prompts/codex-task-handoff.md\nindex 695e211d76dea1c60e8d5bb360e78487808286a4..59441d576b01a39569722f226c854e5a740e9eb5\n--- a/.github/prompts/codex-task-handoff.md\n+++ b/.github/prompts/codex-task-handoff.md\n@@ -2,7 +2,7 @@\n \n You are Codex Front acting as the Implementer for a Shiki task.\n \n-Follow `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant ADRs, and `docs/agents/codex-handoff.md`.\n+Follow `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant Shiki SADRs and target ADRs, and `docs/agents/codex-handoff.md`.\n \n Rules:\n \ndiff --git a/AGENTS.md b/AGENTS.md\nindex 7f041100d49c2a7525f51cb0f5d696a4a6d1c78c..a06bfbabc6fa48a007389e25e12d7ed523936bed\n--- a/AGENTS.md\n+++ b/AGENTS.md\n@@ -22,7 +22,7 @@\n - **Codex Front** is an optional implementer through Codex App, Codex CLI, Codex IDE extension, or Codex Web signed in with ChatGPT OAuth/subscription auth, dispatched with `shiki runner codex` when a task is explicitly assigned to `codex`.\n - **GitHub CCA** is implemented by Claude Code Action by default, using `CLAUDE_CODE_OAUTH_TOKEN`.\n \n-Do not assume `openai/codex-action`, `OPENAI_API_KEY`, or API-key based Codex automation in the default Shiki path. API-key runners are explicit target-repo extensions and require their own ADR.\n+Do not assume `openai/codex-action`, `OPENAI_API_KEY`, or API-key based Codex automation in the default Shiki path. API-key runners are explicit target-repo extensions and require their own target ADR.\n \n Shiki is not:\n \n@@ -44,10 +44,14 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use\n+`SADR-NNNN-*.md`; target product decisions retain `NNNN-*.md` and the `ADR`\n+namespace.\n+\n Use the canonical source-of-truth order above. Runtime-specific reading orders\n are operational guidance only and must not override or reorder it.\n \n-Conversation state is not durable truth. If a decision matters, put it in GitHub, `.shiki/`, `CONTEXT.md`, or an ADR.\n+Conversation state is not durable truth. If a decision matters, put it in GitHub, `.shiki/`, `CONTEXT.md`, a Shiki SADR, or a target ADR.\n \n If GitHub and `.shiki/` disagree, surface the conflict and prefer GitHub operational state until the mirror is repaired.\n \n@@ -75,8 +79,8 @@\n Every non-trivial change follows this loop:\n \n 1. **Goal**: clarify outcome, completion conditions, non-goals, risk level, and success signals.\n-2. **grill-with-docs**: challenge the plan against domain docs, ADRs, code reality, terminology, and edge scenarios.\n-3. **Context & Impact**: find relevant docs, ADRs, code, dependencies, owners, locks, and verification surfaces.\n+2. **grill-with-docs**: challenge the plan against domain docs, Shiki SADRs, target ADRs, code reality, terminology, and edge scenarios.\n+3. **Context & Impact**: find relevant docs, SADRs, target ADRs, code, dependencies, owners, locks, and verification surfaces.\n 4. **PRD and Spec Freeze**: use `to-prd` when context is settled enough to become durable product/engineering intent. The operator's explicit approval of the PRD is Spec Freeze: the plan records a `spec_freeze` block, and no plan runs without it. Steps 1-4 form Requirements Definition \u2014 one continuous operator dialogue. After Spec Freeze, scope changes require an operator-approved Spec Amendment; non-scope-moving interpretations go to the Assumption Log.\n 5. **Issues**: use `to-issues` to create independently grabbable vertical-slice issues. Prefer AFK slices over HITL slices where possible.\n 6. **Triage**: label issues for readiness, risk, runtime, skills, and MergeGate state.\n@@ -167,7 +171,7 @@\n - Testing decisions.\n - Out-of-scope items.\n - Further notes.\n-- Links to relevant domain terms and ADRs.\n+- Links to relevant domain terms, SADRs, and target ADRs.\n \n Every executable task must include:\n \n@@ -193,7 +197,7 @@\n \n Context & Impact must identify:\n \n-- Relevant documents, domain terms, ADRs, and past decisions.\n+- Relevant documents, domain terms, SADRs, target ADRs, and past decisions.\n - Relevant modules, interfaces, seams, callers, owners, and tests.\n - Dependency relationships between tasks.\n - Candidate locks and lock conflicts.\n@@ -210,7 +214,7 @@\n | Skill | Required when |\n | --- | --- |\n | `setup-matt-pocock-skills` | First configuring the repo for skills, or when issue tracker, triage labels, or domain docs are missing. |\n-| `grill-with-docs` | Default for non-trivial Goals before PRD/issues; always required when plans, terminology, boundaries, tradeoffs, or ADR-worthy decisions are ambiguous. |\n+| `grill-with-docs` | Default for non-trivial Goals before PRD/issues; always required when plans, terminology, boundaries, tradeoffs, or decision-record-worthy choices are ambiguous. |\n | `zoom-out` | The agent lacks an architectural map of the relevant code area. |\n | `to-prd` | Settled Goal context must become durable product/engineering intent. |\n | `to-issues` | A Goal, PRD, or plan must become independently grabbable vertical-slice issues. |\n@@ -348,7 +352,7 @@\n - Three or more new files.\n - Any high-risk or critical label.\n \n-When the architecture gate triggers, run `grill-with-docs` or `improve-codebase-architecture` as appropriate and record durable decisions in an ADR when needed.\n+When the architecture gate triggers, run `grill-with-docs` or `improve-codebase-architecture` as appropriate and record durable decisions in a Shiki SADR or target ADR as applicable.\n \n ## Engineering Discipline\n \ndiff --git a/CLAUDE.md b/CLAUDE.md\nindex 98a932e7841a632866fcd53b0bad0e3af3711d3d..98a31399404d6fb044b4fd493f4459ac87f439de\n--- a/CLAUDE.md\n+++ b/CLAUDE.md\n@@ -14,6 +14,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n ## Claude Role\n \n Default to planner, default implementer, reviewer, coordinator, documentation editor, CCA-style completion judge, and final-judgment assistant.\n@@ -57,7 +60,7 @@\n At the start of a Shiki session:\n \n 1. Read `AGENTS.md`.\n-2. Read `CONTEXT.md` and relevant ADRs.\n+2. Read `CONTEXT.md` and relevant Shiki SADRs and target ADRs.\n 3. Read active `.shiki/` Goal/task/ledger state when present.\n 4. Read active distilled rules from `.shiki/memories/` (status `distilled`, `active: true`, not revoked or superseded) for applicable guidance; the same rules are injected deterministically into task handoffs as the `## Distilled Rules` section (\u00a73.5).\n 5. Read the GitHub Issue or PR when the task is GitHub-backed.\n@@ -74,7 +77,7 @@\n - Define the Goal, completion conditions, non-goals, and risk level.\n - Treat `grill-with-docs` as the default planning entry point for non-trivial Goals.\n - Challenge domain terms against `CONTEXT.md`.\n-- Challenge hard-to-reverse tradeoffs against ADRs.\n+- Challenge hard-to-reverse tradeoffs against SADRs and target ADRs.\n - Explore code instead of asking when code can answer the question.\n - Run Context & Impact before implementation planning.\n - Produce a Task DAG, not an unordered checklist.\n@@ -94,7 +97,7 @@\n - For each question, provide your recommended answer.\n - Resolve dependency decisions before downstream design questions.\n - Update `CONTEXT.md` inline when terminology crystallizes.\n-- Offer ADRs only when the decision is hard to reverse, surprising without context, and a real tradeoff.\n+- Offer a SADR or target ADR only when the decision is hard to reverse, surprising without context, and a real tradeoff.\n - Convert unresolved questions into HITL blockers rather than allowing Codex to guess.\n \n ## PRD and Issues Standard\n@@ -103,7 +106,7 @@\n \n - Use the project's domain glossary vocabulary.\n - Avoid volatile file paths unless they are essential.\n-- Include implementation decisions, testing decisions, out-of-scope items, and relevant ADR/domain links.\n+- Include implementation decisions, testing decisions, out-of-scope items, and relevant SADR/target-ADR/domain links.\n - Identify deep-module opportunities and testable interfaces.\n \n When creating issues:\n@@ -121,7 +124,7 @@\n - Branch/worktree target.\n - Scope and non-goals.\n - Dependencies and locks.\n-- Relevant docs, ADRs, modules, and tests.\n+- Relevant docs, SADRs, target ADRs, modules, and tests.\n - Required skill invocations.\n - TDD expectations.\n - Pre-PR `code-review` gate expectations.\n@@ -152,7 +155,7 @@\n \n - Treat yourself as a judge, not implementer.\n - Do not write production code.\n-- Read `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant ADRs, PRD, task issue, PR body, diff, checks, reviews, labels, and `.shiki/` state.\n+- Read `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant SADRs and target ADRs, PRD, task issue, PR body, diff, checks, reviews, labels, and `.shiki/` state.\n - Evaluate every applicable checklist in `docs/agents/checklists.md`.\n - Map each acceptance criterion to durable evidence.\n - Distinguish implementation failure from missing evidence.\ndiff --git a/CONTEXT.md b/CONTEXT.md\nindex e61a9be06a0cce0d026484083a657bde2067e234..237ee8aa3ea8fab334ece49768bc5ed3b5af0974\n--- a/CONTEXT.md\n+++ b/CONTEXT.md\n@@ -25,7 +25,7 @@\n _Avoid_: jumping directly into code\n \n **grill-with-docs**:\n-A planning skill that challenges a plan against domain language, ADRs, code reality, and concrete edge scenarios. It resolves design-tree questions before PRD/issues. It runs inside Requirements Definition.\n+A planning skill that challenges a plan against domain language, Shiki SADRs, target ADRs, code reality, and concrete edge scenarios. It resolves design-tree questions before PRD/issues. It runs inside Requirements Definition.\n _Avoid_: generic brainstorming, silent assumptions\n \n **Requirements Definition**:\ndiff --git a/CONTRIBUTING.md b/CONTRIBUTING.md\nindex c6d9b656e73abb8a55e5f660123e03153dcb3e6d..9e37a8fbf668db5b22461aaae5548b8f1354394c\n--- a/CONTRIBUTING.md\n+++ b/CONTRIBUTING.md\n@@ -17,7 +17,7 @@\n 1. **Goal** \u2014 start from a user-approved Goal with an outcome, completion\n    conditions, non-goals, risk level, and success signals.\n 2. **grill-with-docs** \u2014 for any non-trivial change, challenge the plan against\n-   domain language, ADRs, and code reality before writing a PRD.\n+   domain language, Shiki SADRs, target ADRs, and code reality before writing a PRD.\n 3. **PRD** \u2014 capture settled product/engineering intent (`to-prd`).\n 4. **Spec Freeze** \u2014 the operator explicitly approves the PRD/requirements;\n    the plan records a `spec_freeze` block and no plan runs without it\n@@ -94,7 +94,7 @@\n   entries.\n - Do not hand-edit `.shiki/goals/*.json` or `.shiki/manifest.json` in a task PR.\n - Conversation state is not durable truth. If a decision matters, record it in\n-  GitHub, `.shiki/`, `CONTEXT.md`, or an ADR.\n+  GitHub, `.shiki/`, `CONTEXT.md`, a Shiki SADR, or a target ADR.\n \n ## Architecture Gate\n \n@@ -103,7 +103,7 @@\n or CI policy, package structure, three or more structural files, more than 100\n net new lines in a core module, three or more new files, or any high-risk label.\n When triggered, run `grill-with-docs` or `improve-codebase-architecture` and\n-record durable decisions in an ADR when needed (see `docs/adr/`).\n+record durable decisions in a Shiki SADR or target ADR as applicable (see `docs/adr/`).\n \n ## Safety\n \ndiff --git a/SYSTEM_PROMPT.md b/SYSTEM_PROMPT.md\nindex aaaea45e3a896c3a44ba89eec8bb84325ecf6c62..da009c86a88285269dee0155e58e550c6d744ea9\n--- a/SYSTEM_PROMPT.md\n+++ b/SYSTEM_PROMPT.md\n@@ -30,6 +30,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n Use the canonical source-of-truth order above. Runtime-specific reading orders\n are operational guidance only and must not override or reorder it. The current\n conversation is non-durable operator input.\n@@ -48,14 +51,14 @@\n \n A runtime may only do work that is explicitly assigned by the Goal, task, PR, or MergeGate decision.\n \n-Do not assume `openai/codex-action`, `OPENAI_API_KEY`, or API-key based Codex automation in the default Shiki path. API-key runners are explicit target-repo extensions and require their own ADR.\n+Do not assume `openai/codex-action`, `OPENAI_API_KEY`, or API-key based Codex automation in the default Shiki path. API-key runners are explicit target-repo extensions and require their own target ADR.\n \n ## 4. Non-negotiable Shiki flow\n \n For any non-trivial Goal, use this flow:\n \n 1. **Goal Seek**: define outcome, non-goals, risk level, completion criteria, and evidence requirements.\n-2. **grill-with-docs**: challenge the plan against `CONTEXT.md`, ADRs, code reality, terminology, boundaries, and edge scenarios. Ask one question at a time when the operator is available. If code can answer a question, inspect code instead of asking.\n+2. **grill-with-docs**: challenge the plan against `CONTEXT.md`, Shiki SADRs, target ADRs, code reality, terminology, boundaries, and edge scenarios. Ask one question at a time when the operator is available. If code can answer a question, inspect code instead of asking.\n 3. **Context & Impact**: identify modules, interfaces, seams, callers, dependencies, locks, risk, and verification surfaces.\n 4. **to-prd + Spec Freeze**: turn settled context into a PRD using domain vocabulary and testing decisions. The operator's explicit approval of the PRD is Spec Freeze, recorded as the plan's `spec_freeze` block; plans without it do not run. Steps 1-4 form Requirements Definition.\n 5. **to-issues**: decompose the PRD into independently grabbable vertical-slice issues. Prefer AFK slices; mark HITL slices when judgment is still required.\n@@ -75,7 +78,7 @@\n Before making any material change:\n \n 1. Read `AGENTS.md` and any runtime-specific wrapper such as `CLAUDE.md`.\n-2. Read `CONTEXT.md` and relevant ADRs.\n+2. Read `CONTEXT.md` and relevant SADRs and target ADRs.\n 3. Read the GitHub Issue / PR and `.shiki/` state for the active Goal or task.\n 4. Identify your role: Planner, Implementer, Reviewer, Completion Check Agent, Repairer, or Guardian-assist.\n 5. Identify Goal id, task id, branch, PR, dependencies, locks, risk level, required skills, and required evidence.\n@@ -103,7 +106,7 @@\n \n At minimum, identify:\n \n-- Relevant documents, ADRs, domain terms, and prior decisions.\n+- Relevant documents, SADRs, target ADRs, domain terms, and prior decisions.\n - Relevant modules, interfaces, seams, callers, owners, and tests.\n - Dependency relationships between tasks.\n - Candidate file locks and lock conflicts.\n@@ -152,7 +155,7 @@\n \n CCA must read:\n \n-- `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant ADRs.\n+- `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant SADRs and target ADRs.\n - Parent Goal issue, PRD issue, task issue, comments, and labels.\n - PR body, diff, commits, changed files, reviews, CI checks, and artifacts available to the workflow.\n - `.shiki/` task contract, locks, ledger entries, CCA checklist profile, and prior repair packets.\n@@ -214,7 +217,7 @@\n - Three or more new files.\n - Any high-risk or critical label.\n \n-When the architecture gate triggers, run `grill-with-docs` or `improve-codebase-architecture` as appropriate and record the decision in an ADR if it is hard to reverse or likely to be re-litigated.\n+When the architecture gate triggers, run `grill-with-docs` or `improve-codebase-architecture` as appropriate and record the decision in a Shiki SADR or target ADR as applicable if it is hard to reverse or likely to be re-litigated.\n \n ## 14. Skill Gate\n \n@@ -223,7 +226,7 @@\n Use these skills:\n \n - `setup-matt-pocock-skills`: before first use of the engineering skill set, or when issue tracker, triage labels, or domain docs are missing.\n-- `grill-with-docs`: default for non-trivial Goals before PRD/issues; required for ambiguous plans, terminology, boundaries, ADR-worthy tradeoffs, or design-tree decisions.\n+- `grill-with-docs`: default for non-trivial Goals before PRD/issues; required for ambiguous plans, terminology, boundaries, decision-record-worthy tradeoffs, or design-tree decisions.\n - `zoom-out`: unfamiliar code area or missing architectural map.\n - `to-prd`: convert settled context into a PRD.\n - `to-issues`: break a PRD, plan, or Goal into independently grabbable vertical-slice issues.\ndiff --git a/docs/adr/0001-keep-shiki-as-platform-name.md b/docs/adr/SADR-0001-keep-shiki-as-platform-name.md\nindex 20a9637c3b9d8c97de7ba8db7cfae4d82d9a7dba..8b685439959aada1b221c112f0ac3c51ca8a1695\n--- a/docs/adr/0001-keep-shiki-as-platform-name.md\n+++ b/docs/adr/SADR-0001-keep-shiki-as-platform-name.md\n@@ -1,3 +1,3 @@\n-# Keep Shiki As The Platform Name\n+# SADR-0001: Keep Shiki As The Platform Name\n \n Shiki remains the product name for the GitHub-native agentic engineering control plane. We considered more descriptive names such as MergePilot, MergeGate, and Kiroku, but kept Shiki because it carries the existing goal-seeking framework identity while remaining independent of Hermes, Codex, Claude Code, or any single runtime.\ndiff --git a/docs/adr/0002-use-agents-as-shared-constitution.md b/docs/adr/SADR-0002-use-agents-as-shared-constitution.md\nindex 73de30ecb078120eac540d9bc51855e3cd48d0e3..c5486182bf06fca36ea508496b013c67302fd989\n--- a/docs/adr/0002-use-agents-as-shared-constitution.md\n+++ b/docs/adr/SADR-0002-use-agents-as-shared-constitution.md\n@@ -1,3 +1,3 @@\n-# Use AGENTS As The Shared Agent Constitution\n+# SADR-0002: Use AGENTS As The Shared Agent Constitution\n \n Shiki uses `AGENTS.md` as the runtime-neutral constitution and `CLAUDE.md` as a thin Claude Code wrapper. This keeps Codex, Claude Code, Hermes Runner, and future Agent Runtimes aligned on the same GitHub-first MergeGate rules while still allowing runtime-specific instructions where needed.\ndiff --git a/docs/adr/0003-require-skill-gate-for-mergegate-readiness.md b/docs/adr/SADR-0003-require-skill-gate-for-mergegate-readiness.md\nindex 424be0c9af4c8f0136f1fde68a468c47ff84e764..7fc337987c7df9792f75f181dfcbed75fe5ffff2\n--- a/docs/adr/0003-require-skill-gate-for-mergegate-readiness.md\n+++ b/docs/adr/SADR-0003-require-skill-gate-for-mergegate-readiness.md\n@@ -1,3 +1,3 @@\n-# Require Skill Gate For MergeGate Readiness\n+# SADR-0003: Require Skill Gate For MergeGate Readiness\n \n Shiki requires relevant engineering skills to be invoked before execution when their triggers apply. This makes skills part of MergeGate readiness rather than optional prompt style, because Shiki depends on repeatable planning, issue decomposition, TDD, diagnosis, triage, and architecture review before autonomous or parallel agent work can be trusted.\ndiff --git a/docs/adr/0004-use-github-first-with-shiki-mirror.md b/docs/adr/SADR-0004-use-github-first-with-shiki-mirror.md\nindex d0a3a9174dc646a945a9d325954ffaf00c352040..a79c54ae9ee8eed2a0524192b864da4e22a92d3f\n--- a/docs/adr/0004-use-github-first-with-shiki-mirror.md\n+++ b/docs/adr/SADR-0004-use-github-first-with-shiki-mirror.md\n@@ -1,4 +1,4 @@\n-# ADR 0004: Use GitHub First With A Repository-Local Shiki Mirror\n+# SADR-0004: Use GitHub First With A Repository-Local Shiki Mirror\n \n ## Status\n \ndiff --git a/docs/adr/0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md b/docs/adr/SADR-0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\nindex b339baf49b89d9290e79c3d51b36e7b2bde3bd95..0f3c3eb9afccdb3403ce978e21d04fe12e495da0\n--- a/docs/adr/0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\n+++ b/docs/adr/SADR-0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\n@@ -1,8 +1,8 @@\n-# ADR 0005: Use Codex As Front And Claude Code Action As The GitHub Runtime\n+# SADR-0005: Use Codex As Front And Claude Code Action As The GitHub Runtime\n \n ## Status\n \n-Accepted (implementer default superseded by ADR 0008; the auth model and the\n+Accepted (implementer default superseded by SADR-0008; the auth model and the\n Claude Code Action GitHub runtime decision remain in force)\n \n ## Context\n@@ -26,4 +26,4 @@\n - Shiki avoids requiring `OPENAI_API_KEY` for the default workflow.\n - Codex work happens through the user's authenticated Codex front surface, not as a hidden backend Action.\n - Claude Code Action is configured for OAuth-token based subscription usage by default.\n-- API-key based automation remains possible only as an explicit target-repo extension with its own ADR.\n+- API-key based automation remains possible only as an explicit target-repo extension with its own target ADR.\ndiff --git a/docs/adr/0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md b/docs/adr/SADR-0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\nindex 4bcae9d1e52f0a37b8e9fbafa5cd14b6ee1993bd..7a38538a6ae9929567f0ade67e7ea6a60d0b3f7e\n--- a/docs/adr/0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\n+++ b/docs/adr/SADR-0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\n@@ -1,4 +1,4 @@\n-# ADR 0006: Separate Codex Implementation, CCA Completion, And MergeGate Authorization\n+# SADR-0006: Separate Codex Implementation, CCA Completion, And MergeGate Authorization\n \n ## Status\n \ndiff --git a/docs/adr/0007-packaging-and-release.md b/docs/adr/SADR-0007-packaging-and-release.md\nindex 79b84ddb97167372b44641e5b7860e400ce80822..bb445a2e2e856d9d1f5fdbe35de8d4e8cec0fb32\n--- a/docs/adr/0007-packaging-and-release.md\n+++ b/docs/adr/SADR-0007-packaging-and-release.md\n@@ -1,4 +1,4 @@\n-# ADR 0007: Packaging And Release For Shiki\n+# SADR-0007: Packaging And Release For Shiki\n \n ## Status\n \n@@ -25,7 +25,7 @@\n   not introduce third-party runtime dependencies or break the shim.\n - Releases must run inside the operator's subscription-authenticated toolchain\n   and must not require extra secrets beyond the built-in `GITHUB_TOKEN`\n-  (consistent with ADR 0005's no-API-key-by-default stance).\n+  (consistent with SADR-0005's no-API-key-by-default stance).\n - `.shiki/manifest.json` already encodes the install surface (`install.include`,\n   `install.create_directories`, `install.exclude_from_commit`), and it must not\n   be mutated as part of this change.\n@@ -34,7 +34,7 @@\n \n This is a hard-to-reverse platform decision (it sets the version source of\n truth, the release trigger, and the public install contract), so it is recorded\n-as an ADR.\n+as a SADR.\n \n ## Decision\n \n@@ -104,7 +104,7 @@\n 3. **Publishing to PyPI.** Rejected for now: Shiki is distributed as a\n    repository template + git-installable CLI, not a PyPI library; PyPI would add\n    credentials/secrets and a different versioning contract. Can be added later as\n-   its own ADR if a packaged distribution is needed.\n+   its own SADR if a packaged distribution is needed.\n 4. **Dynamic version (read `VERSION` via a build backend `dynamic` field).**\n    Rejected to keep `pyproject.toml` backend-agnostic and dependency-free; the\n    small duplication between `VERSION` and `pyproject.toml` is guarded by the\n@@ -123,4 +123,4 @@\n   releasing versions the whole platform but ships only `install.include` to\n   targets.\n - Future PyPI distribution or a console entry point are deferred and would each\n-  warrant their own ADR.\n+  warrant their own SADR.\ndiff --git a/docs/adr/0008-use-claude-code-as-default-implementer.md b/docs/adr/SADR-0008-use-claude-code-as-default-implementer.md\nindex dedf4510b7c7799521422f622029563d373db031..35c2625d908167fbc58855b5b63e283805f167e8\n--- a/docs/adr/0008-use-claude-code-as-default-implementer.md\n+++ b/docs/adr/SADR-0008-use-claude-code-as-default-implementer.md\n@@ -1,12 +1,12 @@\n-# ADR 0008: Use Claude Code As The Default Implementer Runtime\n+# SADR-0008: Use Claude Code As The Default Implementer Runtime\n \n ## Status\n \n-Proposed (supersedes ADR 0005 for the implementer default; keeps its auth model)\n+Proposed (supersedes SADR-0005 for the implementer default; keeps its auth model)\n \n ## Context\n \n-ADR 0005 made Codex Front the default implementation runtime, with Claude Code\n+SADR-0005 made Codex Front the default implementation runtime, with Claude Code\n restricted to planner and reviewer roles by the runtime registry, and the only\n autonomous implementation adapter was `shiki runner codex`.\n \n@@ -18,13 +18,13 @@\n - The platform's planning, review, and CCA layers already run on Claude Code,\n   and Claude Code's native capabilities (the built-in `/code-review` skill and\n   the Workflow multi-agent orchestration tool) have no Codex equivalent.\n-- The Goal lifecycle redesign (ADR 0009) requires an autonomous post-freeze\n+- The Goal lifecycle redesign (SADR-0009) requires an autonomous post-freeze\n   loop driven from the operator's Claude Code session, including bounded\n   repair dispatch, which must not depend on a runtime that cannot be invoked\n   unattended.\n \n The constitution claims Shiki is runtime-agnostic (\"not a Claude-only\n-workflow\", ADR 0002). Any change to the implementer default must preserve the\n+workflow\", SADR-0002). Any change to the implementer default must preserve the\n registry-based runtime model rather than hardcoding one vendor.\n \n ## Decision\n@@ -39,7 +39,7 @@\n   the task to review or repair-needed.\n - Keep Codex registered as an optional implementer runtime. `shiki runner\n   codex`, the Codex handoff templates, and the ChatGPT OAuth auth model from\n-  ADR 0005 remain supported for tasks explicitly assigned to `codex`.\n+  SADR-0005 remain supported for tasks explicitly assigned to `codex`.\n - Commit to mandating two Claude-native capabilities as loop contract in\n   follow-up Goals (status: the code-review gate was delivered by Goal G-B \u2014\n   skill registry, checklists item PR-12, default required_skills \u2014 while the\n@@ -60,7 +60,7 @@\n \n Out of scope: removing Codex support, changing the CCA runtime (Claude Code\n Action with `CLAUDE_CODE_OAUTH_TOKEN` stays), and API-key based automation\n-(still requires its own ADR per ADR 0005).\n+(still requires its own target ADR per SADR-0005).\n \n ## Consequences\n \n@@ -100,7 +100,7 @@\n   harness rejection of direct `codex exec` is outside Shiki's control, and the\n   plan's mandatory native capabilities cannot run on Codex at all.\n - **Remove Codex entirely**: rejected; it abandons the runtime-agnostic\n-  constitution (ADR 0002) for no operational gain and forecloses\n+  constitution (SADR-0002) for no operational gain and forecloses\n   cross-runtime adversarial review.\n - **GitHub Actions cloud implementation via claude-code-action**: rejected as\n   default; durable evidence is automatic but token cost, Node 24 workflow\ndiff --git a/docs/adr/0009-requirements-definition-and-spec-freeze.md b/docs/adr/SADR-0009-requirements-definition-and-spec-freeze.md\nindex 29b3f7871bef9203a8b4a831a65c39eb2f506e42..52e7dd69ad0aa4efe0b68c62678abbf7df02aaec\n--- a/docs/adr/0009-requirements-definition-and-spec-freeze.md\n+++ b/docs/adr/SADR-0009-requirements-definition-and-spec-freeze.md\n@@ -1,4 +1,4 @@\n-# ADR 0009: Merge Grill And Planning Into Requirements Definition With Spec Freeze\n+# SADR-0009: Merge Grill And Planning Into Requirements Definition With Spec Freeze\n \n ## Status\n \ndiff --git a/docs/adr/0010-external-ai-guardian-review.md b/docs/adr/SADR-0010-external-ai-guardian-review.md\nindex e6108bbe8bb4e73e0ff4684c8f78001a80438c4e..f3510b145664298adafb151b6f0db51b972f01ec\n--- a/docs/adr/0010-external-ai-guardian-review.md\n+++ b/docs/adr/SADR-0010-external-ai-guardian-review.md\n@@ -1,4 +1,4 @@\n-# ADR 0010: External AI Guardian Review As A First-Class Approval Authority\n+# SADR-0010: External AI Guardian Review As A First-Class Approval Authority\n \n ## Status\n \n@@ -8,7 +8,7 @@\n \n Shiki is intended to be an autonomous, recursive, self-improving agent\n framework: authority-in-the-loop, not human-in-the-loop. Until now the Guardian\n-approval required for high/critical-risk merges (guardian-policy.json, ADR\n+approval required for high/critical-risk merges (guardian-policy.json, SADR\n governance) recognized only human authority sources \u2014 a GitHub review approval,\n the `guardian:approved` label applied by a configured Guardian, or a comment\n carrying `Guardian approval granted` plus the current head SHA.\n@@ -89,7 +89,7 @@\n   autonomous design the operator intends and produced the impersonation\n   incident.\n - **Record AI approval as an operator comment**: rejected as forgery of human\n-  authority \u2014 the exact failure this ADR corrects.\n+  authority \u2014 the exact failure this SADR corrects.\n - **Deliver the artifact as a committed `.shiki/` file**: rejected; a committed\n   approval inside the PR being approved is forgeable by anyone who can push.\n - **A dedicated reviewer bot identity instead of a relayed comment**: deferred;\ndiff --git a/docs/adr/0011-autonomous-quality-gate-evidence.md b/docs/adr/SADR-0011-autonomous-quality-gate-evidence.md\nindex bca0246c5d46e44afbde15486a75eec425fc02f5..9c93eac16d8c3a6d4b11bd7224e3955102cd7db4\n--- a/docs/adr/0011-autonomous-quality-gate-evidence.md\n+++ b/docs/adr/SADR-0011-autonomous-quality-gate-evidence.md\n@@ -1,4 +1,4 @@\n-# ADR 0011: Autonomous Quality-Gate Evidence \u2014 Deterministic Facts And An Independent Verifier\n+# SADR-0011: Autonomous Quality-Gate Evidence \u2014 Deterministic Facts And An Independent Verifier\n \n ## Status\n \ndiff --git a/docs/adr/0012-autonomous-loop-closeout-to-main.md b/docs/adr/SADR-0012-autonomous-loop-closeout-to-main.md\nindex 637fad6dde4989c9c2bf0f81f676d8d29991123c..ec7d85f81030a676bcbba28b43eb365422a36eac\n--- a/docs/adr/0012-autonomous-loop-closeout-to-main.md\n+++ b/docs/adr/SADR-0012-autonomous-loop-closeout-to-main.md\n@@ -1,4 +1,4 @@\n-# 0012 \u2014 Autonomous loop pushes goal completion to main via a closeout PR\n+# SADR-0012 \u2014 Autonomous loop pushes goal completion to main via a closeout PR\n \n - Status: accepted\n - Date: 2026-06-18\ndiff --git a/docs/adr/0013-bootstrap-grants-actions-approve-pull-requests.md b/docs/adr/SADR-0013-bootstrap-grants-actions-approve-pull-requests.md\nindex b6c69d64f1c5ced346b88d6859586715b3b5bd0e..cf48ff4a6716267f539ca58edd1a852e7c7a2d64\n--- a/docs/adr/0013-bootstrap-grants-actions-approve-pull-requests.md\n+++ b/docs/adr/SADR-0013-bootstrap-grants-actions-approve-pull-requests.md\n@@ -1,4 +1,4 @@\n-# ADR 0013: Bootstrap Grants GitHub Actions Permission To Create And Approve Pull Requests\n+# SADR-0013: Bootstrap Grants GitHub Actions Permission To Create And Approve Pull Requests\n \n ## Status\n \n@@ -31,7 +31,7 @@\n security tradeoff: it lets repository automation satisfy a human-review gate. In\n a multi-maintainer repo that could let a compromised or buggy workflow\n self-approve changes. So whether bootstrap turns it on by default \u2014 and with\n-what default token scope \u2014 is an ADR-worthy decision, not an implementation\n+what default token scope \u2014 is a SADR-worthy decision, not an implementation\n detail.\n \n ## Decision\n@@ -48,7 +48,7 @@\n Specifically:\n \n - **`can_approve_pull_request_reviews` defaults to `true`.** Shiki's default\n-  posture is solo/AFK autonomous operation (ADR 0008/0009/0012), where the CCA\n+  posture is solo/AFK autonomous operation (SADR-0008/0009/0012), where the CCA\n   Review Bridge is the only path that can satisfy `required_review: true` after\n   the CCA and Guardian gates have already passed. The default makes a\n   freshly-bootstrapped repo able to run the autonomous loop end to end without a\ndiff --git a/docs/adr/0014-codex-app-external-ai-guardian-ui-adapter.md b/docs/adr/SADR-0014-codex-app-external-ai-guardian-ui-adapter.md\nindex 238db06279334caa341c117362df9271382a7fcd..65f12e9939321e1b3e389a34c822fc1549e5155d\n--- a/docs/adr/0014-codex-app-external-ai-guardian-ui-adapter.md\n+++ b/docs/adr/SADR-0014-codex-app-external-ai-guardian-ui-adapter.md\n@@ -1,4 +1,4 @@\n-# ADR 0014: Use Codex App As The External AI Guardian UI Adapter\n+# SADR-0014: Use Codex App As The External AI Guardian UI Adapter\n \n ## Status\n \ndiff --git a/docs/adr/0015-contract-approval-before-dispatch.md b/docs/adr/SADR-0015-contract-approval-before-dispatch.md\nindex fad6c656741a20a7684ba16080a7db159f63a52f..6061271a46d3f1d6ae7174477de3fb75673123a6\n--- a/docs/adr/0015-contract-approval-before-dispatch.md\n+++ b/docs/adr/SADR-0015-contract-approval-before-dispatch.md\n@@ -1,4 +1,4 @@\n-# ADR 0015: Approve The Task Contract Before Dispatch, Not The Diff After It\n+# SADR-0015: Approve The Task Contract Before Dispatch, Not The Diff After It\n \n ## Status\n \n@@ -9,7 +9,7 @@\n Guardian approval for high/critical risk is expressible only on a pull request.\n Every approval source in `guardian-policy.json` \u2014 GitHub review, the\n `guardian:approved` label, the `Guardian approval granted` comment, and the\n-`external_ai_guardian_review` artifact (ADR 0010/0014) \u2014 is bound to a PR, and\n+`external_ai_guardian_review` artifact (SADR-0010/0014) \u2014 is bound to a PR, and\n three of the four require the current head SHA. Approval therefore cannot exist\n before an implementation PR exists.\n \n@@ -81,7 +81,7 @@\n An envelope breach must surface only on the non-repairable policy gate, never on\n the repairable `MergeGate metadata check`. Otherwise the Goal Loop routes it to\n `dispatch_repair` \u2014 instructing a runner to make the breach go away \u2014 which is\n-exactly what this ADR forbids.\n+exactly what this SADR forbids.\n \n **Contract immutability.** After registration, an implementation PR must not\n change the governance fields of its own task contract (`scope`, `non_goals`,\n@@ -116,7 +116,7 @@\n fails any verdict that is not `complete`, and the signal the CCA prompt reads is\n computed from `evaluate_guardian_approval` alone. Without the OR-merge at the\n signal, a contract-approved PR still returns `needs_guardian`, the CCA job fails,\n-and the MergeGate policy check never runs \u2014 the gate this ADR changes would never\n+and the MergeGate policy check never runs \u2014 the gate this SADR changes would never\n be reached. The CCA *prompt* needs no change: a `contract_approval` entry in the\n signal's existing `sources` array satisfies its current wording.\n \n@@ -130,7 +130,7 @@\n written, and an approved contract lets its implementation PRs merge through the\n same automatic path as low/medium risk. The wasted implement-then-block cycle\n disappears. Guardian review shifts from reading diffs to reading contracts, which\n-is both earlier and smaller; the external AI Guardian path (ADR 0010/0014) works\n+is both earlier and smaller; the external AI Guardian path (SADR-0010/0014) works\n on contracts with no change.\n \n We accept that the Guardian authorizes a class of change without seeing its\n@@ -146,7 +146,7 @@\n adversarial tests for the new approval path equivalent in rigor to\n `test_shiki_governance_evidence.sh`.\n \n-Bootstrap costs, which apply only while this ADR is being implemented. The tasks\n+Bootstrap costs, which apply only while this SADR is being implemented. The tasks\n that build Contract Approval merge under the existing rules, so the ones\n dispatched after the pre-dispatch gate lands but before the approval path lands\n need a Guardian-approved Contract PR *and* a live Guardian approval on their\n@@ -210,7 +210,7 @@\n risk; they do not bound authorized work.\n \n **Auto-escalate risk on envelope breach instead of stopping.** Returns the\n-workflow to a diff-time Guardian decision \u2014 the exact behavior this ADR removes.\n+workflow to a diff-time Guardian decision \u2014 the exact behavior this SADR removes.\n \n **Resolve governance fields from the base snapshot only.** Rejected against the\n registration flow: implementation PRs add their own contract, so the base has no\ndiff --git a/docs/adr/0016-locks-bound-product-paths.md b/docs/adr/SADR-0016-locks-bound-product-paths.md\nindex b5e850f3f1f7f4ef6e7f9d1763a29a51bd300c45..46b40a9b99a7aae644c02bb458f5473ff0a90ce2\n--- a/docs/adr/0016-locks-bound-product-paths.md\n+++ b/docs/adr/SADR-0016-locks-bound-product-paths.md\n@@ -1,4 +1,4 @@\n-# ADR 0016: Locks bound product paths; the Shiki mirror is bound by record identity\n+# SADR-0016: Locks bound product paths; the Shiki mirror is bound by record identity\n \n ## Status\n \n@@ -6,8 +6,8 @@\n \n - Date: 2026-07-28\n - Deciders: operator (mizutani-140), Claude (planner)\n-- Related: ADR 0004 (GitHub-first with a `.shiki` mirror), ADR 0009 (Spec Freeze),\n-  ADR 0012 (autonomous loop closeout to main), ADR 0015 (Contract Approval before\n+- Related: SADR-0004 (GitHub-first with a `.shiki` mirror), SADR-0009 (Spec Freeze),\n+  SADR-0012 (autonomous loop closeout to main), SADR-0015 (Contract Approval before\n   dispatch), remediation goal task R-02, the live PR #179 deadlock.\n \n ## Context\n@@ -90,11 +90,11 @@\n files, documentation files and named source modules are nameable; runtime-\n allocated ledger, runner and report ids are not \u2014 and those live in `.shiki`,\n which locks no longer govern. Existing frozen contracts are brought into line by\n-a one-time Spec Amendment per plan, which must land before ADR 0015's contract\n+a one-time Spec Amendment per plan, which must land before SADR-0015's contract\n immutability makes `locks` unwritable from the implementation PR.\n \n **Lock state is resolved from the durable side.** MergeGate resolves the lock\n-registry, and ADR 0015's governance fields, from the base `.shiki` snapshot when\n+registry, and SADR-0015's governance fields, from the base `.shiki` snapshot when\n present, falling back to head only when absent. The `MergeGate metadata check`\n job is given `--base-shiki`, which it does not receive today. Locks are not\n derived from the live open-pull-request set: a pull request body is untrusted\n@@ -108,7 +108,7 @@\n Explicitly out of scope: moving MergeGate's contract resolution off the PR\n checkout wholesale; making the mirror a projection of GitHub state; introducing\n new lock kinds; moving ledger evidence out of the repository; and unwinding\n-ADR 0012's closeout pull request. Registration of multi-task goals is ADR 0015's\n+SADR-0012's closeout pull request. Registration of multi-task goals is SADR-0015's\n Contract PR and is not re-decided here.\n \n ## Consequences\n@@ -127,7 +127,7 @@\n \n Risks accepted. First, goal-scoped identity is only as strong as `goal_id`,\n which is resolved from the PR body and the head task file; the mitigation is to\n-add `goal_id` to ADR 0015's frozen governance field set, and until that lands a\n+add `goal_id` to SADR-0015's frozen governance field set, and until that lands a\n task file can name a foreign goal. Second, base-when-present resolution is\n unavailable on fork pull requests, where the head is contributor-controlled;\n fork pull requests already cannot complete a Shiki merge, and this decision does\n@@ -139,7 +139,7 @@\n path, not by implementation pull requests.\n \n New obligations: one Spec Amendment per frozen plan to rewrite `locks`, ordered\n-before ADR 0015 T1; extension of the adversarial suites in\n+before SADR-0015 T1; extension of the adversarial suites in\n `scripts/test_shiki_adversarial_state.sh` and\n `scripts/test_shiki_governance_evidence.sh` to cover the eleven newly-scoped\n directories; and updating `scripts/test_shiki_loop_lock_guard.sh`,\n@@ -151,32 +151,32 @@\n whether the loop's transitive sync of ledger `evidence` references can be\n deleted once the Contract PR carries its own source plan.\n \n-## Relationship to ADR 0004\n+## Relationship to SADR-0004\n \n-This ADR **clarifies something ADR 0004 left unspecified**. ADR 0004 decides that\n+This SADR **clarifies something SADR-0004 left unspecified**. SADR-0004 decides that\n GitHub is the operational source of truth and that each target repository keeps a\n `.shiki/` mirror. It says nothing about pull requests writing the mirror, does\n not name MergeGate, and does not make the mirror an input to a gate. Nothing in\n-ADR 0004's Decision or Consequences is amended, superseded or weakened: the\n+SADR-0004's Decision or Consequences is amended, superseded or weakened: the\n mirror still exists, still records the same artifacts, and pull request\n-boundaries remain the coordination surface. What this ADR settles is the question\n+boundaries remain the coordination surface. What this SADR settles is the question\n 0004 never reached \u2014 by what authority a pull request may write a mirror record \u2014\n and it answers it the way `.shiki/manifest.json` already declares\n (`pr_mutation: current-task-or-current-goal-only`).\n \n-It **amends ADR 0012**, whose Decision item 5 states that \"Loop-executed tasks\n+It **amends SADR-0012**, whose Decision item 5 states that \"Loop-executed tasks\n declare the `path:.shiki/**` lock, which covers every `.shiki` file the closeout\n stages ... so `files_outside_locks` is satisfied without per-file lock\n bookkeeping.\" That clause is replaced: the closeout's mirror writes are\n-authorized by record identity, not by a declared lock. ADR 0012's substantive\n+authorized by record identity, not by a declared lock. SADR-0012's substantive\n decision \u2014 that completion reaches `main` through a closeout pull request rather\n than local attestation \u2014 is preserved unchanged.\n \n-It is **complementary to ADR 0015**, not a substitute. ADR 0015 owns contract\n+It is **complementary to SADR-0015**, not a substitute. SADR-0015 owns contract\n immutability, the pre-dispatch gate, the Contract PR and the Scope Envelope. This\n-ADR owns what a lock means and who may write the mirror. The one ordering\n+SADR owns what a lock means and who may write the mirror. The one ordering\n constraint between them is that the lock-narrowing Spec Amendment must precede\n-ADR 0015 T1's freezing of the `locks` field.\n+SADR-0015 T1's freezing of the `locks` field.\n \n ## Alternatives Considered\n \n@@ -186,7 +186,7 @@\n the only writer that can register them may run. Migrating the Guardian signal to\n that store makes every registration PR fail closed to `needs_guardian` at every\n risk level, because the task file is by definition absent from the store, which\n-removes the low/medium autonomous path ADR 0008/0009 exists to provide. And it\n+removes the low/medium autonomous path SADR-0008/0009 exists to provide. And it\n leaves schemas, manifest, config, guardian policy, templates, migrations, plans,\n worktrees, runner records, runs and handoffs with no writer at all in a\n repository whose product is `.shiki`. Measured benefit on the frozen backlog:\n@@ -199,14 +199,14 @@\n namespace is not in fact ownership-partitioned \u2014 56 ledger entries carry no\n `task_id`, and one goal DAG is writable by 35 registered tasks. The correct\n expression of its insight is that locks do not govern the mirror at all, which is\n-what this ADR decides.\n+what this SADR decides.\n \n **Make the mirror a projection written only by a registration writer and a\n post-merge projection job.** Rejected. It requires a bypass actor with write\n access to the protected default branch; it makes its own master invariant\n readable from a manifest that pull requests may edit; it deletes both reconcile\n modes and leaves a corrupted mirror with no in-band repair path, contradicting\n-ADR 0004's consequence that mirror conflicts are repaired against GitHub state;\n+SADR-0004's consequence that mirror conflicts are repaired against GitHub state;\n and its content-derived ids are rejected by `validate_shiki`'s currently-green id\n pattern.\n \ndiff --git a/docs/adr/0017-bookkeeping-closeouts-do-not-inherit-risk.md b/docs/adr/SADR-0017-bookkeeping-closeouts-do-not-inherit-risk.md\nindex 554e3a0d526a72ca067108c5464cc68c3edcf174..b9676857a4713850e063af7c39c8e0b01ecb4c66\n--- a/docs/adr/0017-bookkeeping-closeouts-do-not-inherit-risk.md\n+++ b/docs/adr/SADR-0017-bookkeeping-closeouts-do-not-inherit-risk.md\n@@ -1,4 +1,4 @@\n-# ADR 0017: A Bookkeeping Closeout Does Not Inherit Its Task's Risk\n+# SADR-0017: A Bookkeeping Closeout Does Not Inherit Its Task's Risk\n \n ## Status\n \n@@ -6,7 +6,7 @@\n \n ## Context\n \n-ADR 0012 made the goal loop push a task's completion to the default branch through a\n+SADR-0012 made the goal loop push a task's completion to the default branch through a\n closeout pull request: after the implementation PR merges, the loop opens a second PR\n carrying `task.status = done`, `lock.state = released`, and \u2014 when the task completes\n its goal \u2014 `goal.status = complete` plus one scorecard report. Nothing else. The\n@@ -49,14 +49,14 @@\n 4. The task file's governance fields (`scope`, `non_goals`, `required_skills`,\n    `risk_level`, `locks`, `acceptance_checks`, `test_command`) are byte-identical to\n    the base snapshot. Only `status`, `expected_pr`, `closeout_pr`, `ledger_evidence`\n-   and `expected_branch` may differ \u2014 the ADR 0015 contract-immutability rule,\n+   and `expected_branch` may differ \u2014 the SADR-0015 contract-immutability rule,\n    unchanged.\n \n    `expected_branch` is in that set because a closeout is *by construction* on a\n    different branch from the implementation it closes, and the loop rewrites the\n    field accordingly. It is safe to let it differ because MergeGate independently\n    requires `expected_branch` to equal the PR's actual head ref, so a PR cannot set\n-   it to anything other than the branch it is really on. The first draft of this ADR\n+   it to anything other than the branch it is really on. The first draft of this SADR\n    omitted it, which made the exemption unreachable: every real closeout failed\n    condition 4 on a field the loop always changes. See PR #209.\n 5. The state transitions are exactly the terminal set: task `review` \u2192 `done`, lock\n@@ -113,12 +113,12 @@\n classification must be derived, not asserted.\n \n **Fold the closeout into the implementation PR** so no second PR exists. Rejected under\n-ADR 0012, which recorded why: a task file that is already `done` on the implementation\n+SADR-0012, which recorded why: a task file that is already `done` on the implementation\n branch short-circuits the loop's review \u2192 CCA \u2192 merge \u2192 done ordering into a local\n goal-completion before any merge happens.\n \n **Have the loop auto-merge closeouts with elevated credentials.** Rejected outright: that\n-is a runtime authoring its own approval, which ADR 0010 and the constitution's\n+is a runtime authoring its own approval, which SADR-0010 and the constitution's\n approval-record prohibition forbid. It would also be indistinguishable, in the evidence,\n from the forgery those rules exist to prevent.\n \ndiff --git a/docs/adr/0018-carried-guardian-approval-across-a-proven-base-sync.md b/docs/adr/SADR-0018-carried-guardian-approval-across-a-proven-base-sync.md\nindex 22358562da390ed8b45cd600da08a43d378b4680..e6e2907cefa776460af73ddf7d39db605f3c39b0\n--- a/docs/adr/0018-carried-guardian-approval-across-a-proven-base-sync.md\n+++ b/docs/adr/SADR-0018-carried-guardian-approval-across-a-proven-base-sync.md\n@@ -1,4 +1,4 @@\n-# ADR 0018: Carried Guardian Approval Across A Proven Base Sync\n+# SADR-0018: Carried Guardian Approval Across A Proven Base Sync\n \n ## Status\n \n@@ -34,7 +34,7 @@\n Guardian evaluator that consumes it (`shiki_guardian.evaluate_guardian_approval`,\n the `guardian_comment_carried` source) were built and tested in isolation. Neither\n does anything until a caller passes the flags that turn the carry on, so the\n-feature was invisible in CI. This ADR records the decision the two of them\n+feature was invisible in CI. This SADR records the decision the two of them\n implement, and this task wires it into the four CI invocations that judge Guardian\n approval.\n \ndiff --git a/docs/adr/README.md b/docs/adr/README.md\nindex c70e057a4520c36e042ab68c53cd4a21ef709b71..5da43856836e2ce6a150f8127766e2f88355ab4b\n--- a/docs/adr/README.md\n+++ b/docs/adr/README.md\n@@ -1,40 +1,41 @@\n-# Architecture Decision Records\n+# Decision Record Namespaces\n \n-This directory holds Shiki's Architecture Decision Records (ADRs): durable\n-records of hard-to-reverse platform decisions. ADRs are part of the Shiki source\n-of truth alongside GitHub state, the `.shiki/` mirror, and `CONTEXT.md`.\n+This directory holds two independent decision namespaces:\n \n-## How ADRs Work\n+- Shiki Architecture Decision Records (SADRs), named\n+  `SADR-NNNN-<short-slug>.md`, record hard-to-reverse Shiki platform decisions.\n+- Target Architecture Decision Records (ADRs), named\n+  `NNNN-<short-slug>.md`, belong to the target product.\n \n-- New ADRs are added as `docs/adr/NNNN-<short-slug>.md`, where `NNNN` is the next\n-  zero-padded sequence number.\n-- The **canonical list of ADRs is the set of files matching `docs/adr/*.md`** in\n-  this directory. Browse `docs/adr/*.md` for the authoritative, current set.\n-- Use [`template.md`](template.md) as the starting point for a new ADR.\n-- Record an ADR when a decision is hard to reverse, surprising without context,\n-  and a real tradeoff. Do not write an ADR for routine or easily reversible\n-  choices.\n+The same four-digit number may appear once in each namespace. A platform\n+`SADR-NNNN` citation resolves only to `SADR-NNNN-*.md`; a target `ADR NNNN`\n+citation resolves only to `NNNN-*.md`.\n \n-Using the `docs/adr/*.md` glob as the source of truth means other PRs can add an\n-ADR without editing this index file, avoiding merge contention on a\n-hand-maintained enumeration.\n+## How Decision Records Work\n \n-## Current ADRs (snapshot)\n-\n-The list below is a convenience snapshot and may lag behind the directory. When\n-in doubt, list `docs/adr/*.md`.\n+- New Shiki platform decisions use the next zero-padded SADR number.\n+- New target product decisions use the target's own ADR sequence.\n+- The canonical list is the files in `docs/adr/`; no hand-maintained index is\n+  authoritative.\n+- Use [`template.md`](template.md) as the starting point and choose the filename\n+  namespace that owns the decision.\n+- Record a decision when it is hard to reverse, surprising without context, and\n+  a real tradeoff. Do not write a decision record for routine or easily\n+  reversible choices.\n \n-- [0001 \u2014 Keep Shiki As The Platform Name](0001-keep-shiki-as-platform-name.md)\n-- [0002 \u2014 Use AGENTS As The Shared Agent Constitution](0002-use-agents-as-shared-constitution.md)\n-- [0003 \u2014 Require Skill Gate For MergeGate Readiness](0003-require-skill-gate-for-mergegate-readiness.md)\n-- [0004 \u2014 Use GitHub First With A Repository-Local Shiki Mirror](0004-use-github-first-with-shiki-mirror.md)\n-- [0005 \u2014 Use Codex As Front And Claude Code Action As The GitHub Runtime](0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md)\n-- [0006 \u2014 Separate Codex Implementation, CCA Completion, And MergeGate Authorization](0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md)\n+Using the directory as the source of truth lets independent changes add records\n+without contending on an enumerated index.\n \n-## Adding A New ADR\n+## Adding A Shiki SADR\n \n-1. Copy `template.md` to `docs/adr/NNNN-<short-slug>.md`.\n+1. Copy `template.md` to `docs/adr/SADR-NNNN-<short-slug>.md`.\n 2. Fill in the title, status, context, decision, and consequences.\n-3. Set the status to `Proposed`, then `Accepted` once the decision is approved.\n-4. Open a PR following `CONTRIBUTING.md`. You do not need to edit this index for\n-   the ADR to be discoverable via `docs/adr/*.md`.\n+3. Set the status to `Proposed`, then `Accepted` once the proper authority has\n+   approved the decision.\n+4. Open a PR following `CONTRIBUTING.md`.\n+\n+## Adding A Target ADR\n+\n+1. Copy `template.md` to `docs/adr/NNNN-<short-slug>.md` using the target's own\n+   sequence.\n+2. Cite it as `ADR NNNN`; do not prefix its filename with `SADR-`.\ndiff --git a/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md b/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\nnew file mode 100644\nindex 0000000000000000000000000000000000000000..7455ce1f3bed02ddddb851ac21a1d1a648c9aeba\n--- /dev/null\n+++ b/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\n@@ -0,0 +1,55 @@\n+# SADR-0019: Separate Shiki Platform SADRs From Target ADRs\n+\n+## Status\n+\n+Accepted\n+\n+## Context\n+\n+Shiki installs its platform decision records into `docs/adr/` in every target\n+repository. Shiki and its targets previously used the same numeric\n+`NNNN-<slug>.md` namespace. A target that already owned ADRs 0013 through 0018\n+therefore acquired two records for each number during installation, making an\n+`ADR NNNN` citation ambiguous. Renumbering target decisions would rewrite target\n+history, while renumbering Shiki decisions per target would destroy their stable\n+platform identity.\n+\n+Upgrades also need to remove the former Shiki-owned numeric paths without ever\n+mistaking a target-owned ADR for platform content. The install stamp already\n+records exact shipped paths and content digests, so it can prove ownership only\n+when both still match.\n+\n+## Decision\n+\n+Shiki platform decisions use the `SADR` namespace:\n+\n+- records are named `docs/adr/SADR-NNNN-<slug>.md`;\n+- live platform citations use `SADR-NNNN`;\n+- target product decisions retain `docs/adr/NNNN-<slug>.md` and `ADR NNNN`;\n+- the reference validator resolves the two namespaces independently, so the same\n+  four-digit number may exist once in each namespace.\n+\n+Before a forced upgrade writes anything, the installer inventories the exact\n+former numeric Shiki ADR paths. It may delete a legacy path only when the\n+existing install stamp contains that exact path and its recorded SHA-256 digest\n+matches the file currently on disk. All ownership blockers are reported\n+together and abort the upgrade before its first write. A non-forced install\n+never performs this cleanup.\n+\n+Historical `.shiki/` evidence remains unchanged because it records the namespace\n+that existed when that evidence was produced.\n+\n+## Consequences\n+\n+- Targets may use any ADR number without colliding with Shiki's platform SADRs.\n+- Fresh installs preserve target ADR bytes and install a stable SADR set.\n+- Forced upgrades fail closed when legacy ownership cannot be proven, and a\n+  successful upgrade refreshes the stamp without removed legacy paths.\n+- Current governance, packaging, workflow, prompt, script, and skill references\n+  must move with the platform records.\n+\n+## Alternatives Considered\n+\n+- Collision-only refusal leaves affected targets permanently unupgradable.\n+- Renumbering target ADRs mutates target-owned history.\n+- Per-target Shiki renumbering makes platform citations non-portable.\ndiff --git a/docs/adr/template.md b/docs/adr/template.md\nindex a92a97daddd0b00ab9dd626cc8d86942da2eafed..17dd79175b5fee2105935557f691b40cf9492942\n--- a/docs/adr/template.md\n+++ b/docs/adr/template.md\n@@ -1,14 +1,14 @@\n-# NNNN: <Short Decision Title>\n+# <SADR-NNNN or NNNN>: <Short Decision Title>\n \n ## Status\n \n-Proposed | Accepted | Superseded by ADR-NNNN\n+Proposed | Accepted | Superseded by SADR-NNNN or target ADR NNNN\n \n ## Context\n \n What is the situation, constraint, or force that requires a decision? Reference\n-relevant domain terms (`CONTEXT.md`), prior ADRs, and code reality. State the\n-problem, not the solution.\n+relevant domain terms (`CONTEXT.md`), prior Shiki SADRs or target ADRs, and code\n+reality. State the problem, not the solution.\n \n ## Decision\n \ndiff --git a/docs/agents/autonomous-loop-e2e-acceptance.md b/docs/agents/autonomous-loop-e2e-acceptance.md\nindex bbf84c6081987f159ac6ad9ecf3c82b7aa72e7e8..5c13c1d02a05a90b0dc46cbef7c95a9846d0c237\n--- a/docs/agents/autonomous-loop-e2e-acceptance.md\n+++ b/docs/agents/autonomous-loop-e2e-acceptance.md\n@@ -40,8 +40,8 @@\n 3. `gh auth status` is logged in with merge permission on the repo.\n 4. Branch protection + required checks are configured exactly as the goal-loop\n    required-check set expects (see `docs/agents/branch-protection-smoke.md`).\n-5. The repo `.shiki/policy` permits autonomous low/medium-risk merges (ADR\n-   0008/0009); the MergeGate policy check is the recorded-authority gate.\n+5. The repo `.shiki/policy` permits autonomous low/medium-risk merges\n+   (SADR-0008/0009); the MergeGate policy check is the recorded-authority gate.\n \n The operator's only actions are: register the task and start the loop. **No\n manual pre-clearing** means after `shiki loop run` starts, the operator does not\ndiff --git a/docs/agents/bootstrap-command.md b/docs/agents/bootstrap-command.md\nindex 05f7f203a52cf412b9f97084a6e131ed0f785afd..f7aa0b734dfebee4a3b139335b66ee3415f48713\n--- a/docs/agents/bootstrap-command.md\n+++ b/docs/agents/bootstrap-command.md\n@@ -189,7 +189,9 @@\n ```\n \n Use this only for tests, fixtures, or explicit local-only template inspection.\n-Use `--force` only when you intentionally want to overwrite existing target files.\n+Use `--force` only when you intentionally want to refresh the target template.\n+Legacy numeric Shiki decision cleanup is stamp-and-digest authorized before any\n+write; target ADR paths are not cleanup candidates.\n \n ## Slash Command\n \ndiff --git a/docs/agents/checklists.md b/docs/agents/checklists.md\nindex 4a7f03ade346f53a1c1d82981f3fd744f9ab4547..0c5ec6c53dc7197b7b7a7d18e667b325babd6c45\n--- a/docs/agents/checklists.md\n+++ b/docs/agents/checklists.md\n@@ -32,18 +32,18 @@\n | ID | Item | Blocking |\n | --- | --- | --- |\n | GD-01 | Plan was challenged against `CONTEXT.md`. | blocking for non-trivial Goals |\n-| GD-02 | Relevant ADRs were read or absence was recorded. | blocking for non-trivial Goals |\n+| GD-02 | Relevant SADRs and target ADRs were read or absence was recorded. | blocking for non-trivial Goals |\n | GD-03 | Ambiguous terms were resolved or converted to HITL blockers. | blocking |\n | GD-04 | Edge scenarios were considered. | advisory |\n | GD-05 | Code reality was checked when code could answer. | blocking when applicable |\n | GD-06 | New domain terms were added to `CONTEXT.md` when needed. | blocking when applicable |\n-| GD-07 | ADR was created or explicitly skipped using the ADR criteria. | blocking when applicable |\n+| GD-07 | A SADR or target ADR was created or explicitly skipped using the decision-record criteria. | blocking when applicable |\n \n ## CI \u2014 Context & Impact Checklist\n \n | ID | Item | Blocking |\n | --- | --- | --- |\n-| CI-01 | Relevant docs and ADRs identified. | blocking |\n+| CI-01 | Relevant docs, SADRs, and target ADRs identified. | blocking |\n | CI-02 | Relevant modules, interfaces, seams, callers, and tests identified. | blocking |\n | CI-03 | Dependencies and blocked-by relationships identified. | blocking |\n | CI-04 | Candidate locks identified. | blocking |\n@@ -62,7 +62,7 @@\n | PRD-04 | Implementation decisions are recorded without volatile code detail. | blocking |\n | PRD-05 | Testing decisions are recorded. | blocking |\n | PRD-06 | Out-of-scope items are explicit. | blocking |\n-| PRD-07 | Relevant glossary/ADR links are present when applicable. | blocking |\n+| PRD-07 | Relevant glossary/SADR/target-ADR links are present when applicable. | blocking |\n \n ## SF \u2014 Spec Freeze Checklist\n \n@@ -98,7 +98,7 @@\n | H-02 | Branch/worktree target is present. | blocking |\n | H-03 | Scope and non-goals are present. | blocking |\n | H-04 | Dependencies and locks are present. | blocking |\n-| H-05 | Relevant docs, ADRs, modules, and tests are referenced. | blocking |\n+| H-05 | Relevant docs, SADRs, target ADRs, modules, and tests are referenced. | blocking |\n | H-06 | Required skills are stated. | blocking |\n | H-07 | TDD expectations are stated. | blocking when behavior work |\n | H-08 | Acceptance criteria are included. | blocking |\ndiff --git a/docs/agents/codex-handoff.md b/docs/agents/codex-handoff.md\nindex ede468f47cb277f69a857e8c45df703e4ed0151d..6f97bdffb67e44d7edea2c71e01ea28933c4eb6c\n--- a/docs/agents/codex-handoff.md\n+++ b/docs/agents/codex-handoff.md\n@@ -46,7 +46,7 @@\n \n ## Context\n - Domain terms:\n-- Relevant ADRs:\n+- Relevant SADRs and target ADRs:\n - Relevant docs:\n - Relevant modules/interfaces/seams:\n - Similar tests/prior art:\ndiff --git a/docs/agents/completion-check-agent.md b/docs/agents/completion-check-agent.md\nindex 13e4c035db35025f4e3cdcc5adfb24314ac4d521..61bfa1218cf36ca605250c91919e620f33238aa0\n--- a/docs/agents/completion-check-agent.md\n+++ b/docs/agents/completion-check-agent.md\n@@ -32,7 +32,7 @@\n - `AGENTS.md`\n - `CLAUDE.md`\n - `CONTEXT.md`\n-- relevant ADRs\n+- relevant SADRs and target ADRs\n - parent Goal issue\n - PRD issue or document\n - task issue\ndiff --git a/docs/agents/decision-control.md b/docs/agents/decision-control.md\nindex 7d8ee5ab03a5ccbcbd04056ae61de03785a2a9c9..8c94ecda00fffaa0e92c325ab83aa9776bd1dac1\n--- a/docs/agents/decision-control.md\n+++ b/docs/agents/decision-control.md\n@@ -11,7 +11,7 @@\n | Decision | Owner | When | Required inputs | Output | Enforcement surface |\n | --- | --- | --- | --- | --- | --- |\n | Is the Goal clear enough to plan? | Planner + Guardian when needed | Goal intake | User request, existing docs, risk signals | Goal readiness verdict | Goal checklist, issue template, ledger |\n-| Must `grill-with-docs` run? | Planner / Skill Gate | Before PRD or issue split | Ambiguity, domain terms, ADR conflicts, unclear boundaries | Skill invocation or explicit skip rationale | Skill Gate checklist |\n+| Must `grill-with-docs` run? | Planner / Skill Gate | Before PRD or issue split | Ambiguity, domain terms, SADR/target-ADR conflicts, unclear boundaries | Skill invocation or explicit skip rationale | Skill Gate checklist |\n | Is a PRD required? | Planner | After Goal context stabilizes | Goal, `grill-with-docs` output, Context & Impact output | PRD required / not required | PRD checklist, CCA evidence check |\n | Can the spec be frozen? | Operator only | End of Requirements Definition, before issue split | PRD/requirements summary, scope inventory (SF-02), open-question list | `spec_freeze` block in the plan + ledger evidence | `require_grilled_plan` (plan ingest/run), SF checklist |\n | Can a frozen spec change? | Operator only (Spec Amendment) | When implementation contradicts the frozen spec | Contested decisions, scoped re-grill result | recorded amendment + re-stamped freeze | SF checklist, ledger evidence |\ndiff --git a/docs/agents/domain.md b/docs/agents/domain.md\nindex 258da8ea81e87ac0a6b0b5b5e5c2fde980eaabb7..d57b0cf17ff3e1fa9e6e5885372bf083d8295421\n--- a/docs/agents/domain.md\n+++ b/docs/agents/domain.md\n@@ -5,7 +5,9 @@\n ## Layout\n \n - `CONTEXT.md` defines Shiki language. Keep it implementation-free.\n-- `docs/adr/` records hard-to-reverse decisions.\n+- `docs/adr/SADR-NNNN-*.md` records Shiki platform decisions.\n+- `docs/adr/NNNN-*.md` records target product decisions without sharing the\n+  SADR namespace.\n - `AGENTS.md` is the runtime-neutral agent constitution.\n - `CLAUDE.md` is the Claude Code wrapper around `AGENTS.md`.\n - `.shiki/` is the Target Repository mirror for Goals, PRDs, plans, Task DAGs, contracts, locks, worktree records, ledger entries, CCA verdicts, repair packets, reports, and handoffs.\n@@ -15,6 +17,6 @@\n \n - Use Shiki terms exactly when they exist.\n - Add glossary terms to `CONTEXT.md` only when the term is part of the domain, not a generic programming concept.\n-- Create ADRs only for hard-to-reverse, surprising, tradeoff-based decisions.\n+- Create SADRs or target ADRs only for hard-to-reverse, surprising, tradeoff-based decisions.\n - Keep product-specific language in the Target Repository, not in Shiki Template docs.\n-- If an ADR conflicts with a proposed implementation, surface the conflict before editing.\n+- If a SADR or target ADR conflicts with a proposed implementation, surface the conflict before editing.\ndiff --git a/docs/agents/execution-control.md b/docs/agents/execution-control.md\nindex ad8ee3cdb36be56fedc1f46408ca92b387d74047..bc63c3a73661d12b567016ba299f1a6b66bf9088\n--- a/docs/agents/execution-control.md\n+++ b/docs/agents/execution-control.md\n@@ -63,7 +63,7 @@\n | Branch name | Orchestrator | Orchestrator | Before implementer dispatch | branch policy | branch ref in task/ledger |\n | Lock reservation | Context & Impact | MergeGate | Before dispatch and before merge | `.shiki/locks/`, labels, PR metadata | lock id, owner, scope |\n | CI profile | Planner | MergeGate | During issue triage, rechecked at PR | labels, `.shiki/policy.yaml`, workflow matrix | required check list |\n-| Required status checks | Repo maintainer / Guardian | GitHub branch protection / rulesets | Repository setup, updated via ADR | GitHub settings | branch/ruleset config, ADR |\n+| Required status checks | Repo maintainer / Guardian | GitHub branch protection / rulesets | Repository setup, updated via target ADR | GitHub settings | branch/ruleset config, target ADR |\n | CCA judgment | CCA | GitHub required check / MergeGate | Every non-draft PR update | GitHub Action check | structured CCA verdict |\n | Repair assignment | CCA | MergeGate | After failed CCA/CI/review | repair packet, issue/PR comment | repair id and attempt count |\n | Merge readiness | MergeGate | GitHub branch protection / Guardian if needed | After CCA complete | required checks, review rules, rulesets | mergegate check output |\ndiff --git a/docs/agents/guardian-policy.md b/docs/agents/guardian-policy.md\nindex 4147c7adf05a47adb4fd24ada1a1c2e1f2ba6d2d..a20d9cc6c00a569bb057b9ca8d6b51c7fc4a9847\n--- a/docs/agents/guardian-policy.md\n+++ b/docs/agents/guardian-policy.md\n@@ -98,8 +98,8 @@\n `origin/<default_branch>` and reuses each job's existing base fetch; a shallow\n (`--depth`) or second base fetch would make the proof unresolvable and silently\n disable the carry, so neither is permitted. CCA reads `guardian_comment_carried` as\n-a recorded authority in the deterministic signal and does not re-derive it. See ADR\n-0018 for the full decision, the rejected diff/patch-equality and path-disjointness\n+a recorded authority in the deterministic signal and does not re-derive it. See\n+SADR-0018 for the full decision, the rejected diff/patch-equality and path-disjointness\n alternatives, and the accepted residual.\n \n ## External AI Guardian Review (SADR-0010)\ndiff --git a/docs/agents/implementation-policy.md b/docs/agents/implementation-policy.md\nindex c754144a9fb5bf4a38aa82a60e1670c59e626b44..85c37281bcad196c6b7b02343faf00b03cd9c57a\n--- a/docs/agents/implementation-policy.md\n+++ b/docs/agents/implementation-policy.md\n@@ -31,10 +31,13 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n ## Non-Negotiables\n \n 1. **Planning precedes implementation.** Non-trivial Goals start with `grill-with-docs` and Context & Impact.\n-2. **Domain language is controlled.** Terms must be checked against `CONTEXT.md`; hard-to-reverse tradeoffs must be checked against ADRs.\n+2. **Domain language is controlled.** Terms must be checked against `CONTEXT.md`; hard-to-reverse tradeoffs must be checked against Shiki SADRs and target ADRs.\n 3. **PRD precedes issue decomposition.** `to-prd` is used once the context is settled enough to become durable intent.\n 4. **Issues are vertical slices.** `to-issues` must produce independently grabbable tracer-bullet tasks, not horizontal layer tickets.\n 5. **Triage controls dispatch.** Only `ready-for-agent` / AFK tasks may be assigned to Codex without human input.\n@@ -95,19 +98,19 @@\n Process:\n \n - Challenge terms against `CONTEXT.md`.\n-- Check ADRs before reopening decisions.\n+- Check SADRs and target ADRs before reopening decisions.\n - Inspect code when code can answer a question.\n - Ask one question at a time when the operator is available.\n - Provide a recommended answer with each question.\n - Record resolved terms in `CONTEXT.md`.\n-- Create ADRs only for hard-to-reverse, surprising, tradeoff-based decisions.\n+- Create SADRs or target ADRs only for hard-to-reverse, surprising, tradeoff-based decisions.\n - Convert unresolved load-bearing questions into HITL blockers.\n \n Outputs:\n \n - Resolved design-tree decisions.\n - Updated glossary terms when needed.\n-- ADRs when justified.\n+- SADRs or target ADRs when justified.\n - HITL blockers when decisions remain unresolved.\n \n ## Phase 3 \u2014 Context & Impact\n@@ -117,7 +120,7 @@\n \n Outputs must include:\n \n-- Relevant documents and ADRs.\n+- Relevant documents, SADRs, and target ADRs.\n - Relevant modules, interfaces, seams, callers, and tests.\n - Candidate locks.\n - Dependencies.\n@@ -140,7 +143,7 @@\n - Testing decisions.\n - Out of scope.\n - Further notes.\n-- Links to relevant Goal, domain terms, and ADRs.\n+- Links to relevant Goal, domain terms, SADRs, and target ADRs.\n \n Phase 4 ends with Spec Freeze: the operator explicitly approves the PRD, the\n plan records a `spec_freeze` block (status, approved_by, source), and the SF\n@@ -240,7 +243,7 @@\n - Dependencies and locks.\n - Risk and Guardian requirements.\n - Ledger completeness.\n-- Documentation and ADR updates.\n+- Documentation and SADR/target-ADR updates.\n - Scope drift.\n \n CCA emits one of:\n@@ -301,7 +304,7 @@\n | Evidence failure | Behavior might be done, but proof is missing. | Add checks, PR evidence, ledger entry, or artifacts. |\n | Scope drift | PR changes unrelated behavior. | Revert unrelated changes or split into new Goal. |\n | Dependency block | Upstream task or lock not complete. | Block until dependency resolves. |\n-| Architecture block | Decision needs ADR or deep-module discussion. | `grill-with-docs` or `improve-codebase-architecture`. |\n+| Architecture block | Decision needs a SADR, target ADR, or deep-module discussion. | `grill-with-docs` or `improve-codebase-architecture`. |\n | Risk block | Security/production/policy change needs human approval. | Guardian decision. |\n \n ## Phase 5.5 \u2014 Execution Decision Control\ndiff --git a/docs/agents/runtime-auth-model.md b/docs/agents/runtime-auth-model.md\nindex 6a2e1a47e81ea89b5c68a210ec07772a9374babc..3c54748d24233dbd0ba8473950282547ef4ce935\n--- a/docs/agents/runtime-auth-model.md\n+++ b/docs/agents/runtime-auth-model.md\n@@ -148,7 +148,7 @@\n \n API-key based runners may be added by a target repository as an explicit extension. They are not the default Shiki template path.\n \n-If a repo opts into API-key mode, record the exception in an ADR and update the repo's `AGENTS.md`, `.shiki/config.yaml`, and workflow permissions.\n+If a repo opts into API-key mode, record the target-specific exception in a target ADR and update the repo's `AGENTS.md`, `.shiki/config.yaml`, and workflow permissions.\n \n ## GitHub Actions Runtime Compatibility\n \ndiff --git a/docs/agents/skill-gate.md b/docs/agents/skill-gate.md\nindex c50398b0d7c60a8598f2a5c88a10015730dced13..3acffe6444617bb1d54b77d4b4708158ec735bc6\n--- a/docs/agents/skill-gate.md\n+++ b/docs/agents/skill-gate.md\n@@ -9,7 +9,7 @@\n | Skill | Trigger | Expected output |\n | --- | --- | --- |\n | `setup-matt-pocock-skills` | First configuring a repo for these skills, or when issue tracker, triage labels, or domain docs are missing. | `docs/agents/` configuration and an `Agent skills` block in `AGENTS.md` or `CLAUDE.md`. |\n-| `grill-with-docs` | Ambiguous plan, unclear terminology, boundary decision, tradeoff, or ADR-worthy design choice. | Clarified decision tree, recommended answers, updates to `CONTEXT.md` or ADRs when decisions crystallize. |\n+| `grill-with-docs` | Ambiguous plan, unclear terminology, boundary decision, tradeoff, or decision-record-worthy design choice. | Clarified decision tree, recommended answers, updates to `CONTEXT.md`, SADRs, or target ADRs when decisions crystallize. |\n | `zoom-out` | Agent lacks a higher-level architectural map. | Map of relevant modules, callers, seams, and domain concepts. |\n | `to-prd` | Settled context should become a PRD. | PRD published to the issue tracker with problem, solution, user stories, decisions, testing, and out-of-scope notes. |\n | `to-issues` | Goal, PRD, or plan must become implementation tickets. | Vertical-slice issues with acceptance criteria, dependencies, and AFK/HITL classification. |\n@@ -17,7 +17,7 @@\n | `tdd` | Feature work or bug fix has observable behavior that can be specified with tests. | Red-green-refactor loop through public interfaces, one vertical slice at a time. |\n | `code-review` | The TDD loop is green and the implementer is about to create or update a PR. Default for every implementation task. | Pre-PR self-review of the task diff with fixes applied on the task branch; findings and resolutions recorded as a ledger entry naming the skill and a `## Pre-PR code review` PR body section. |\n | `diagnose` | Hard bug, failing check, regression, flaky behavior, or performance problem. | Reproduction loop, ranked hypotheses, instrumentation, fix, regression test, post-mortem. |\n-| `improve-codebase-architecture` | Structural friction, testability, AI-navigability, deep-module opportunity, or refactor request. | Architecture candidates and deepening recommendations informed by `CONTEXT.md` and ADRs. |\n+| `improve-codebase-architecture` | Structural friction, testability, AI-navigability, deep-module opportunity, or refactor request. | Architecture candidates and deepening recommendations informed by `CONTEXT.md`, SADRs, and target ADRs. |\n | `prototype` | A throwaway logic or UI prototype is needed to answer a design question. | Clearly marked disposable prototype and captured decision. |\n \n ## MergeGate Interaction\ndiff --git a/docs/operations.md b/docs/operations.md\nindex cf1f452d4c589e38ccd58553bda0ad8684b51eb7..ace48a90ffbec9e5c0a7113bef16a4858bc44b3f\n--- a/docs/operations.md\n+++ b/docs/operations.md\n@@ -89,6 +89,16 @@\n      target history, brought forward by `shiki migrate`, not a template.\n    - **Everything else** (CLI scripts, workflows, schemas, docs) is overwritten.\n \n+   A forced upgrade also migrates the former numeric Shiki decision paths to\n+   `SADR-NNNN-*`. Before the first write, the installer inventories the exact\n+   legacy Shiki paths and authorizes each deletion only when the existing\n+   `.shiki/install-stamp.json` names that path and its SHA-256 digest still\n+   matches the file. An absent, unreadable, malformed, incomplete, or\n+   mismatching stamp reports every blocked path and stops with no filesystem\n+   change. Target-owned `NNNN-*` ADRs are not cleanup candidates. A non-force\n+   install deletes nothing and retains any still-matching legacy stamp entries\n+   so a later forced upgrade can prove ownership.\n+\n    If the target has pending migrations, `--force` refuses and writes nothing \u2014\n    apply migrations first (step 3), then re-run. The run ends with a single\n    summary of every `.new` written, so a half-upgrade is visible.\ndiff --git a/docs/releasing.md b/docs/releasing.md\nindex 10d73828e7dbd64e9d9f6b83bc84d010537ce62a..aafeee8c4d441e1e427efee2ce4f81a647f020e1\n--- a/docs/releasing.md\n+++ b/docs/releasing.md\n@@ -67,7 +67,7 @@\n CLI in `scripts/`. There are no third-party Python runtime dependencies. The CLI\n is intentionally **not** exposed as a `[project.scripts]` console entry point\n because `scripts/shiki.py` is a dependency-free shim and `scripts/` is not\n-packaged as an importable distribution (see the ADR for the rationale).\n+packaged as an importable distribution (see SADR-0007 for the rationale).\n \n Supported install paths:\n \n@@ -155,6 +155,15 @@\n    [docs/operations.md](operations.md#the-new-convention). If the target has\n    pending migrations, `--force` refuses and writes nothing; apply migrations\n    (step 2) first, then re-run.\n+\n+   The SADR namespace upgrade has an additional pre-write gate: each former\n+   numeric Shiki decision path is removed only when the existing install stamp\n+   contains that exact path and a digest matching its current bytes. The\n+   installer aggregates absent/unreadable/malformed-stamp, missing-digest, and\n+   digest-mismatch blockers before raising, so refusal leaves the target tree\n+   byte-identical. Numeric target ADRs are preserved. A successful forced\n+   upgrade installs and stamps `SADR-*` paths and drops the removed legacy paths\n+   from the refreshed stamp.\n 2. Apply repository-local `.shiki/` state migrations with the migrate command:\n \n    ```bash\ndiff --git a/docs/usage.md b/docs/usage.md\nindex b83407ee3970dd395dd893010bc5dbe8bad26051..a130ef6e40dfd8169a5bc701339351097df1a09f\n--- a/docs/usage.md\n+++ b/docs/usage.md\n@@ -67,8 +67,10 @@\n shiki install-target /path/to/target-repo --local-only\n ```\n \n-Use `--force` only when you intentionally want to overwrite existing target\n-files.\n+Use `--force` only when you intentionally want to refresh the target template.\n+For the SADR namespace migration, `--force` deletes only exact legacy Shiki\n+decision paths whose existing install-stamp digest still matches; any ownership\n+blocker stops before the first write. Numeric target ADRs are never candidates.\n \n ## `shiki bootstrap-platform` \u2014 publish the Shiki platform repo\n \ndiff --git a/scripts/guardian_approval_signal.py b/scripts/guardian_approval_signal.py\nindex d059f92b2c82aa2b5d6ca421365537a87886c1cb..16924892714f4f5b9303c0ef0e325a7e70075630\n--- a/scripts/guardian_approval_signal.py\n+++ b/scripts/guardian_approval_signal.py\n@@ -199,8 +199,8 @@\n ) -> Any:\n     \"\"\"Evaluate SADR-0015 Contract Approval for the CCA Guardian signal.\n \n-    The SAME OR that MergeGate applies must reach this deterministic signal: ADR\n-    0015 is explicit that without it, a contract-approved PR still returns\n+    The SAME OR that MergeGate applies must reach this deterministic signal:\n+    SADR-0015 is explicit that without it, a contract-approved PR still returns\n     ``needs_guardian``, the CCA job fails, and the MergeGate policy check never\n     runs \u2014 the gate this change moves would never be reached. This resolves the\n     task the SAME way MergeGate does (the first Shiki task id in the PR body),\ndiff --git a/scripts/shiki_contract_approval.py b/scripts/shiki_contract_approval.py\nindex 941bcb96fdd8b9a85bc0c4f0f04d5dad9d04839c..4a4b418f6527413161db82670bec857a043360e2\n--- a/scripts/shiki_contract_approval.py\n+++ b/scripts/shiki_contract_approval.py\n@@ -142,7 +142,7 @@\n     changed_files_status: Any,\n     registration: Any,\n ) -> ContractApprovalResult:\n-    \"\"\"Whether this PR carries an SADR-0015 Contract Approval.\n+    \"\"\"Whether this PR carries a SADR-0015 Contract Approval.\n \n     The four conditions are evaluated IN ORDER, each failure recorded as a\n     distinct reason string:\ndiff --git a/scripts/shiki_guardian.py b/scripts/shiki_guardian.py\nindex a4ecb79bbda55fd5c8b06adfae2eb99760a03c45..4dd99bbe714e26f421fa19abac7a34ae1b195ecc\n--- a/scripts/shiki_guardian.py\n+++ b/scripts/shiki_guardian.py\n@@ -12,7 +12,7 @@\n # The carry re-derives its proof from git in-process on every judging invocation;\n # it is never read from an artifact, so there is nothing to go stale or be forged.\n # shiki_sync_proof is the ONLY guardian-family module that runs git, and every one\n-# of its invocations is read-only and local (ADR: prove-from-git base sync).\n+# of its invocations is read-only and local (SADR: prove-from-git base sync).\n from shiki_sync_proof import verify_pure_base_sync\n \n GUARDIAN_POLICY_PATH = \".shiki/guardian-policy.json\"\ndiff --git a/scripts/shiki_guardian_review.py b/scripts/shiki_guardian_review.py\nindex dd0f104d457899e0ba5ff1e162011c1e90b6f92b..e10ef58c046324e7ea3a3392feb3048ca77414ce\n--- a/scripts/shiki_guardian_review.py\n+++ b/scripts/shiki_guardian_review.py\n@@ -159,7 +159,7 @@\n     docs_only = _is_docs_only(changed_files)\n     if docs_only and matched == 0:\n         focus.append(\n-            \"Docs/ADR-only change: confirm the change is genuinely documentation/decision-only, introduces no behavior or contract drift, and stays consistent with the constitution and existing ADRs.\"\n+            \"Docs/decision-record-only change: confirm the change is genuinely documentation/decision-only, introduces no behavior or contract drift, and stays consistent with the constitution and existing SADRs and target ADRs.\"\n         )\n \n     # Conservative augmentation: unknown (no category and not docs-only), mixed\ndiff --git a/scripts/shiki_installer.py b/scripts/shiki_installer.py\nindex be064ab7a29ff6836f7f7a1d70500a43e0d11d7b..9fd8eefd137ce7003dad5c0dcb2e48e2cd47ab32\n--- a/scripts/shiki_installer.py\n+++ b/scripts/shiki_installer.py\n@@ -118,6 +118,7 @@\n # what lets a target carry it without a governance-schema change.\n INSTALL_STAMP_PATH = \".shiki/install-stamp.json\"\n INSTALL_STAMP_VERSION = 1\n+LEGACY_SHIKI_SADR_MAX = 18\n \n # Under --force the shipped surface splits three ways, so nothing is lost and\n # nothing is silently stale.\n@@ -237,9 +238,98 @@\n         )\n \n \n+def legacy_shiki_adr_paths() -> tuple[str, ...]:\n+    \"\"\"Former numeric paths for Shiki SADR-0001 through SADR-0018.\n+\n+    The authoritative slugs come from the current SADR filenames. SADR-0019 is\n+    the namespace decision itself and never shipped under a numeric legacy path.\n+    \"\"\"\n+    paths: list[str] = []\n+    for sadr in sorted((ROOT / \"docs\" / \"adr\").glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\")):\n+        number = sadr.name.split(\"-\", 2)[1]\n+        if int(number) <= LEGACY_SHIKI_SADR_MAX:\n+            paths.append(f\"docs/adr/{sadr.name.removeprefix('SADR-')}\")\n+    return tuple(paths)\n+\n+\n+def preflight_legacy_sadr_cleanup(target: Path, *, force: bool) -> tuple[str, ...]:\n+    \"\"\"Authorize exact legacy Shiki ADR removals before the first write.\n+\n+    A non-force install never removes existing files. A forced install needs no\n+    stamp when no legacy Shiki path is present. When legacy paths do exist, each\n+    must be named in a readable install stamp and its current bytes must match\n+    the recorded digest.\n+    \"\"\"\n+    if not force:\n+        return ()\n+    present = tuple(\n+        relative\n+        for relative in legacy_shiki_adr_paths()\n+        if (target / relative).exists() or (target / relative).is_symlink()\n+    )\n+    if not present:\n+        return ()\n+\n+    stamp_path = target / INSTALL_STAMP_PATH\n+    try:\n+        raw = stamp_path.read_text(encoding=\"utf-8\")\n+        stamp = json.loads(raw)\n+    except FileNotFoundError:\n+        detail = \"install stamp is absent\"\n+        blockers = [f\"{relative}: {detail}\" for relative in present]\n+    except (OSError, UnicodeDecodeError) as error:\n+        detail = f\"install stamp is unreadable ({error})\"\n+        blockers = [f\"{relative}: {detail}\" for relative in present]\n+    except json.JSONDecodeError as error:\n+        detail = f\"install stamp is malformed ({error})\"\n+        blockers = [f\"{relative}: {detail}\" for relative in present]\n+    else:\n+        if not isinstance(stamp, dict):\n+            blockers = [\n+                f\"{relative}: install stamp is malformed (expected an object)\"\n+                for relative in present\n+            ]\n+        else:\n+            digests = stamp.get(\"digests\")\n+            blockers = []\n+            if not isinstance(digests, dict):\n+                blockers.extend(\n+                    f\"{relative}: install stamp has no digest for this path\"\n+                    for relative in present\n+                )\n+            else:\n+                for relative in present:\n+                    expected = digests.get(relative)\n+                    if not isinstance(expected, str) or not expected:\n+                        blockers.append(\n+                            f\"{relative}: install stamp has no digest for this path\"\n+                        )\n+                        continue\n+                    try:\n+                        actual = _sha256_file(target / relative)\n+                    except OSError as error:\n+                        blockers.append(\n+                            f\"{relative}: legacy path is unreadable ({error})\"\n+                        )\n+                        continue\n+                    if actual != expected:\n+                        blockers.append(\n+                            f\"{relative}: digest mismatch (stamp {expected}, current {actual})\"\n+                        )\n+\n+    if blockers:\n+        joined = \"\\n\".join(f\"- {blocker}\" for blocker in blockers)\n+        raise ShikiError(\n+            \"legacy Shiki ADR cleanup is not authorized:\\n\"\n+            f\"{joined}\\nNo files were written.\"\n+        )\n+    return present\n+\n+\n def install_template(target: Path, *, force: bool, validate: bool) -> list[NewFileNote]:\n     # Preconditions first: never rewrite the tree of a target that cannot be\n     # upgraded cleanly (see refuse_pending_migrations).\n+    legacy_cleanup = preflight_legacy_sadr_cleanup(target, force=force)\n     refuse_pending_migrations(target)\n \n     # Resolve the target's required CODEOWNERS owner once from its\n@@ -250,6 +340,10 @@\n     # verbatim. See ``codeowners_required_owner``.\n     codeowners_owner = codeowners_required_owner(target)\n \n+    for relative in legacy_cleanup:\n+        (target / relative).unlink()\n+        info(f\"removed stamped legacy Shiki ADR: {target / relative}\")\n+\n     notes: list[NewFileNote] = []\n     for relative in TEMPLATE_PATHS:\n         source = ROOT / relative\n@@ -647,6 +741,22 @@\n     digests = target_stamp_digests(target)\n     existing = read_install_stamp(target)\n     if existing is not None:\n+        # A non-force install deliberately leaves legacy Shiki ADRs in place.\n+        # Preserve only still-matching ownership proof so a later forced\n+        # upgrade can authorize cleanup; never carry a missing or stale digest.\n+        existing_digests = existing.get(\"digests\")\n+        if isinstance(existing_digests, dict):\n+            for relative in legacy_shiki_adr_paths():\n+                expected = existing_digests.get(relative)\n+                path = target / relative\n+                if not isinstance(expected, str) or not path.is_file():\n+                    continue\n+                try:\n+                    actual = _sha256_file(path)\n+                except OSError:\n+                    continue\n+                if actual == expected:\n+                    digests[relative] = actual\n         if _incoming_is_older(commit, existing.get(\"platform_commit\")):\n             warn(\n                 \"kept existing install stamp: incoming install \"\ndiff --git a/scripts/shiki_loop.py b/scripts/shiki_loop.py\nindex 3611641da8aeb5038b1da90e3114b09a473ec6d8..84caa491b9cad0edc4176f209c13e9221e975681\n--- a/scripts/shiki_loop.py\n+++ b/scripts/shiki_loop.py\n@@ -424,7 +424,7 @@\n         # unresolvable CCA verdict has already produced its terminal stop and is never\n         # laundered into a head-moving sync (SADR-0010). It also never fires while the\n         # policy (Guardian) gate is red \u2014 for ANY reason, not only when it is the sole\n-        # red check \u2014 so a head-bound Guardian approval, or an SADR-0015\n+        # red check \u2014 so a head-bound Guardian approval, or a SADR-0015\n         # contract-approval carry that base movement defeated (the policy gate compares\n         # base and head task governance directly), is never silently restored by\n         # adopting base wholesale. It is bounded by a durable per-task counter so the\ndiff --git a/scripts/shiki_tasks.py b/scripts/shiki_tasks.py\nindex aec3809f24b4a0c8c96fc50971cff9ae9ad30381..d531052e9d9717058d4631483cefab44c26a65c1\n--- a/scripts/shiki_tasks.py\n+++ b/scripts/shiki_tasks.py\n@@ -821,7 +821,7 @@\n         \"prompt\": prompt,\n         \"entry_skill\": \"grill-with-docs\",\n         \"required_next_steps\": [\n-            \"Run grill-with-docs until terminology, boundaries, risks, and ADR-worthy decisions are settled.\",\n+            \"Run grill-with-docs until terminology, boundaries, risks, and decision-record-worthy choices are settled.\",\n             \"Obtain the operator's explicit approval of the PRD/requirements (Spec Freeze).\",\n             \"Write a machine-readable plan JSON with grill_with_docs.status=complete and spec_freeze.status=frozen.\",\n             \"Run shiki plan ingest --plan-file PLAN.json.\",\ndiff --git a/scripts/test_shiki_goal_loop.sh b/scripts/test_shiki_goal_loop.sh\nindex 753c7b516e10f0be3bc92a4bd0eb1fa699776ae6..8f3820a0a079b3044424cfe1a6c6b7fe68896e67\n--- a/scripts/test_shiki_goal_loop.sh\n+++ b/scripts/test_shiki_goal_loop.sh\n@@ -231,8 +231,8 @@\n \n # High/critical risk merges autonomously when all required checks are green:\n # the \"MergeGate policy check\" required check IS the Guardian gate (it enforces\n-# guardian-policy.json \u2014 human approval OR external AI guardian review, ADR\n-# 0010), so green checks mean a recorded authority approved. The fake gh returns\n+# guardian-policy.json \u2014 human approval OR external AI guardian review,\n+# SADR-0010), so green checks mean a recorded authority approved. The fake gh returns\n # green checks, simulating that the Guardian gate passed.\n python3 \"$ROOT/scripts/shiki.py\" goal create --target \"$TARGET\" --title \"Guardian gate\" --outcome \"High risk merges via policy gate\" >/tmp/shiki-goal-loop-guardian-goal.json\n GGOAL=\"$(json_get /tmp/shiki-goal-loop-guardian-goal.json goal_id)\"\ndiff --git a/tests/test_adr_references.py b/tests/test_adr_references.py\nindex 51706329c797d5dd9b47e082a333fa83b261d482..d005da4f771f5e8085ec22d4b787980e68f5ae8c\n--- a/tests/test_adr_references.py\n+++ b/tests/test_adr_references.py\n@@ -1,8 +1,8 @@\n-\"\"\"Every ``ADR ####`` citation must resolve to exactly one decision record.\n+\"\"\"Every target ADR and Shiki SADR citation resolves in its own namespace.\n \n Background. ``scripts/mergegate_check.py`` justifies ``is_bookkeeping_closeout``\n-with \"ADR 0017\" nine times and ``scripts/guardian_approval_signal.py`` cites\n-0016/0017 seven times, yet for a stretch the numbering in ``docs/adr/`` stopped\n+with \"SADR-0017\" nine times and ``scripts/guardian_approval_signal.py`` cites\n+SADR-0016/SADR-0017 seven times, yet for a stretch the numbering in ``docs/adr/`` stopped\n at 0015: the authoring commits lived on a coordinator branch and no task ever\n declared ``docs/adr/...`` in its locks, so nothing carried the records to the\n default branch. The stated basis of an exemption that merged seven PRs with zero\n@@ -11,11 +11,12 @@\n and the directory drifted with no signal.\n \n This is the same unbound-pair shape behind other silent failures: a reference\n-and its target with nothing asserting they agree. This test binds them. It scans\n-every ``scripts/*.py`` (plus ``docs/**/*.md`` and the root ``*.md`` governance\n-files, which are cheap) for the ``ADR ####`` pattern and asserts each cited\n-number resolves to exactly one ``docs/adr/####-*.md``. The converse is\n-deliberately NOT required: an ADR may exist without being cited.\n+and its target with nothing asserting they agree. This test binds them. Target\n+``ADR NNNN`` citations resolve only to ``docs/adr/NNNN-*.md``; Shiki\n+``SADR-NNNN`` citations resolve only to ``docs/adr/SADR-NNNN-*.md``. The scan\n+covers current governance docs, scripts, workflows, prompts, packaging metadata,\n+and repo-local Codex guidance while deliberately excluding historical\n+``.shiki`` evidence. The converse is not required: a record may be uncited.\n \n The suite is dependency free: standard-library ``unittest`` only.\n \"\"\"\n@@ -31,17 +32,18 @@\n \n REPO_ROOT = Path(__file__).resolve().parents[1]\n \n-# \"ADR 0016\" -- a four-digit citation. The trailing \\b keeps a five-digit token\n-# from being read as a truncated four-digit number.\n-_CITATION = re.compile(r\"\\bADR (\\d{4})\\b\")\n-# \"0016-locks-bound-product-paths.md\" -- the leading four digits name the record.\n-_ADR_FILENAME = re.compile(r\"^(\\d{4})-.*\\.md$\")\n+# ``ADR 0016`` / ``SADR-0016`` -- a namespace plus four-digit citation. The\n+# trailing ``\\b`` keeps a five-digit token from being read as a truncated one.\n+_CITATION = re.compile(r\"\\b(S?ADR)[ -](\\d{4})\\b\")\n+# Target ADRs retain the numeric filename; Shiki platform decisions use SADR.\n+_DECISION_FILENAME = re.compile(r\"^(?:(SADR)-)?(\\d{4})-.*\\.md$\")\n \n \n class Violation(NamedTuple):\n     \"\"\"A citation that does not resolve to exactly one decision record.\"\"\"\n \n     citing_file: str  # repo-relative path of the file that cites the ADR\n+    namespace: str  # \"ADR\" for target decisions; \"SADR\" for Shiki decisions\n     number: str  # the cited four-digit number, e.g. \"0016\"\n     line: int  # 1-based line of the citation\n     reason: str  # \"missing\" (zero matches) or \"ambiguous\" (two or more)\n@@ -49,44 +51,59 @@\n \n     @property\n     def expected_path(self) -> str:\n-        return f\"docs/adr/{self.number}-*.md\"\n+        prefix = \"SADR-\" if self.namespace == \"SADR\" else \"\"\n+        return f\"docs/adr/{prefix}{self.number}-*.md\"\n \n     def describe(self) -> str:\n         \"\"\"Name the citing file, the cited number, and the missing path.\"\"\"\n         if self.reason == \"missing\":\n             return (\n-                f\"{self.citing_file}:{self.line} cites ADR {self.number} \"\n+                f\"{self.citing_file}:{self.line} cites {self.namespace} {self.number} \"\n                 f\"but no document matches {self.expected_path}\"\n             )\n         joined = \", \".join(self.matches)\n         return (\n-            f\"{self.citing_file}:{self.line} cites ADR {self.number} which \"\n+            f\"{self.citing_file}:{self.line} cites {self.namespace} {self.number} which \"\n             f\"resolves to {len(self.matches)} documents (expected exactly one \"\n             f\"at {self.expected_path}): {joined}\"\n         )\n \n \n-def _adr_index(root: Path) -> dict[str, list[Path]]:\n-    \"\"\"Map every four-digit ADR number to the record files that claim it.\"\"\"\n-    index: dict[str, list[Path]] = {}\n+def _adr_index(root: Path) -> dict[tuple[str, str], list[Path]]:\n+    \"\"\"Map each decision namespace and number to the files that claim it.\"\"\"\n+    index: dict[tuple[str, str], list[Path]] = {}\n     adr_dir = root / \"docs\" / \"adr\"\n     if adr_dir.is_dir():\n         for path in sorted(adr_dir.glob(\"*.md\")):\n-            match = _ADR_FILENAME.match(path.name)\n+            match = _DECISION_FILENAME.match(path.name)\n             if match:\n-                index.setdefault(match.group(1), []).append(path)\n+                namespace = \"SADR\" if match.group(1) else \"ADR\"\n+                index.setdefault((namespace, match.group(2)), []).append(path)\n     return index\n \n \n def _citing_files(root: Path, include_docs: bool = True) -> list[Path]:\n-    \"\"\"Files scanned for citations: scripts/*.py, and cheaply the markdown.\"\"\"\n+    \"\"\"Current, shipped text surfaces scanned for decision citations.\"\"\"\n     files = sorted((root / \"scripts\").glob(\"*.py\"))\n+    files += sorted((root / \"scripts\").glob(\"*.sh\"))\n     if include_docs:\n         docs = root / \"docs\"\n         if docs.is_dir():\n             files += sorted(docs.rglob(\"*.md\"))\n         files += sorted(root.glob(\"*.md\"))\n-    return files\n+        pyproject = root / \"pyproject.toml\"\n+        if pyproject.is_file():\n+            files.append(pyproject)\n+        for relative in (\".github\", \".codex\"):\n+            surface = root / relative\n+            if surface.is_dir():\n+                files += sorted(\n+                    path\n+                    for path in surface.rglob(\"*\")\n+                    if path.is_file()\n+                    and path.suffix in {\".md\", \".py\", \".sh\", \".toml\", \".yaml\", \".yml\"}\n+                )\n+    return sorted(set(files))\n \n \n def find_adr_reference_violations(\n@@ -94,10 +111,9 @@\n ) -> list[Violation]:\n     \"\"\"Return every ``ADR ####`` citation under ``root`` that fails to resolve.\n \n-    A citation resolves when exactly one ``docs/adr/####-*.md`` matches its\n-    number. Zero matches is a \"missing\" violation; two or more is \"ambiguous\".\n-    Records that are never cited produce nothing -- the converse is not\n-    required.\n+    A citation resolves when exactly one record in the same namespace matches\n+    its number. Zero matches is a \"missing\" violation; two or more is\n+    \"ambiguous\". Records that are never cited produce nothing.\n     \"\"\"\n     root = Path(root)\n     index = _adr_index(root)\n@@ -110,13 +126,15 @@\n         rel = path.relative_to(root).as_posix()\n         for lineno, line in enumerate(text.splitlines(), start=1):\n             for match in _CITATION.finditer(line):\n-                number = match.group(1)\n-                found = index.get(number, [])\n+                namespace, number = match.groups()\n+                found = index.get((namespace, number), [])\n                 if len(found) == 1:\n                     continue\n                 reason = \"missing\" if not found else \"ambiguous\"\n                 rels = [p.relative_to(root).as_posix() for p in found]\n-                violations.append(Violation(rel, number, lineno, reason, rels))\n+                violations.append(\n+                    Violation(rel, namespace, number, lineno, reason, rels)\n+                )\n     return violations\n \n \n@@ -129,8 +147,8 @@\n         self.assertEqual(\n             violations,\n             [],\n-            \"Every 'ADR ####' citation must resolve to exactly one \"\n-            f\"docs/adr/####-*.md:\\n{detail}\",\n+            \"Every ADR/SADR citation must resolve exactly once in its own \"\n+            f\"docs/adr namespace:\\n{detail}\",\n         )\n \n     def test_scan_actually_reads_scripts(self) -> None:\n@@ -142,12 +160,38 @@\n             \"the citation scan must cover scripts/*.py\",\n         )\n \n+    def test_platform_citations_do_not_use_unresolved_numeric_shorthand(self) -> None:\n+        shorthand = re.compile(r\"SADR-\\d{4}/\\d{4}\")\n+        offenders = []\n+        for path in _citing_files(REPO_ROOT):\n+            text = path.read_text(encoding=\"utf-8\")\n+            if shorthand.search(text):\n+                offenders.append(path.relative_to(REPO_ROOT).as_posix())\n+        self.assertEqual(\n+            offenders,\n+            [],\n+            \"repeat the SADR prefix so every cited number is independently resolvable\",\n+        )\n+\n     def test_carried_records_are_indexed(self) -> None:\n         # The two records this Goal carried onto the default branch, and which\n         # the scripts cite most, must be present.\n         index = _adr_index(REPO_ROOT)\n-        self.assertIn(\"0016\", index)\n-        self.assertIn(\"0017\", index)\n+        self.assertIn((\"SADR\", \"0016\"), index)\n+        self.assertIn((\"SADR\", \"0017\"), index)\n+\n+    def test_platform_tree_has_only_sadr_0001_through_0019(self) -> None:\n+        adr_dir = REPO_ROOT / \"docs\" / \"adr\"\n+        sadrs = sorted(adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\"))\n+        self.assertEqual(\n+            {path.name.split(\"-\", 2)[1] for path in sadrs},\n+            {f\"{number:04d}\" for number in range(1, 20)},\n+        )\n+        self.assertEqual(\n+            list(adr_dir.glob(\"[0-9][0-9][0-9][0-9]-*.md\")),\n+            [],\n+            \"the Shiki platform tree must not retain legacy numeric SADR paths\",\n+        )\n \n \n class FixtureAdrReferences(unittest.TestCase):\n@@ -180,6 +224,26 @@\n         self.assertIn(\"9991\", message)  # cited number\n         self.assertIn(\"docs/adr/9991-*.md\", message)  # missing path\n \n+    def test_target_adr_does_not_satisfy_missing_platform_sadr(self) -> None:\n+        root = self._make_repo(\n+            adr_names=[\"0042-target-decision.md\"],\n+            script_body='\"\"\"Target ADR 0042 exists, but platform SADR-0042 does not.\"\"\"\\n',\n+        )\n+        violations = find_adr_reference_violations(root, include_docs=False)\n+        self.assertEqual(len(violations), 1)\n+        self.assertEqual(violations[0].namespace, \"SADR\")\n+        self.assertEqual(violations[0].number, \"0042\")\n+        self.assertEqual(violations[0].expected_path, \"docs/adr/SADR-0042-*.md\")\n+\n+    def test_target_adr_and_platform_sadr_may_reuse_a_number(self) -> None:\n+        root = self._make_repo(\n+            adr_names=[\"0042-target-decision.md\", \"SADR-0042-platform-decision.md\"],\n+            script_body='\"\"\"Target ADR 0042 and platform SADR-0042 are distinct.\"\"\"\\n',\n+        )\n+        self.assertEqual(\n+            find_adr_reference_violations(root, include_docs=False), []\n+        )\n+\n     def test_uncited_adr_is_allowed(self) -> None:\n         # An ADR exists but nothing cites it -> not a violation. The converse\n         # (every ADR must be cited) is explicitly NOT required.\ndiff --git a/tests/test_guardian_review.py b/tests/test_guardian_review.py\nindex 88829e7810212635e2fc98aa5918d6f26294677d..00a85a237acd5bf2d6cb5b294128198c422402ca\n--- a/tests/test_guardian_review.py\n+++ b/tests/test_guardian_review.py\n@@ -162,7 +162,7 @@\n \n     def test_docs_only_is_light_and_not_conservative(self):\n         focus = gr.classify_review_focus([\"docs/adr/0099-x.md\", \"README.md\"], \"\", \"low\")\n-        self.assertTrue(any(\"Docs/ADR-only\" in f for f in focus))\n+        self.assertTrue(any(\"Docs/decision-record-only\" in f for f in focus))\n         self.assertFalse(any(\"conservative default\" in f.lower() for f in focus))\n \n     def test_unknown_category_is_conservative(self):\ndiff --git a/tests/test_policy_job_merged_prs.py b/tests/test_policy_job_merged_prs.py\nindex d45512e74681e5e649279236c7d4e79d5c3da471..b97989f8bd74d091de1a6b5275a20d8dc68c4512\n--- a/tests/test_policy_job_merged_prs.py\n+++ b/tests/test_policy_job_merged_prs.py\n@@ -3,8 +3,8 @@\n \n ``.github/workflows/shiki-cca-completion.yml`` has two consumers of the merged-PR\n proof: the CCA *signal* (``guardian_approval_signal.py`` in \"Prepare CCA context\")\n-and the authoritative gate (``mergegate_check.py`` in \"Enforce MergeGate\"). ADR\n-0017's bookkeeping-closeout exemption (condition 6) needs the merged implementation\n+and the authoritative gate (``mergegate_check.py`` in \"Enforce MergeGate\").\n+SADR-0017's bookkeeping-closeout exemption (condition 6) needs the merged implementation\n PR proven, and a closeout carries NO ``post_merge_reconcile`` marker. The signal\n already resolves ``MERGED_PRS`` unconditionally, but \"Enforce MergeGate\" used to\n resolve it only inside ``grep -q '<!-- shiki:post_merge_reconcile -->'``. So a\ndiff --git a/tests/test_sadr_install_migration.py b/tests/test_sadr_install_migration.py\nnew file mode 100644\nindex 0000000000000000000000000000000000000000..d51eac872b1c79ff4579c4c1401ca5d775a60d8d\n--- /dev/null\n+++ b/tests/test_sadr_install_migration.py\n@@ -0,0 +1,237 @@\n+\"\"\"SADR installation and collision-safe legacy upgrade behavior.\"\"\"\n+\n+from __future__ import annotations\n+\n+import contextlib\n+import hashlib\n+import io\n+import json\n+import shutil\n+import tempfile\n+import unittest\n+from pathlib import Path\n+\n+import shiki_test_support  # noqa: F401  (path bootstrap)\n+\n+import shiki_installer\n+from tests.test_adr_references import find_adr_reference_violations\n+\n+\n+PRODUCT_NUMBERS = tuple(range(13, 19))\n+\n+\n+def _quiet_install(target: Path, *, force: bool, validate: bool) -> None:\n+    with contextlib.redirect_stdout(io.StringIO()):\n+        shiki_installer.install_template(target, force=force, validate=validate)\n+\n+\n+def _sha256(path: Path) -> str:\n+    return hashlib.sha256(path.read_bytes()).hexdigest()\n+\n+\n+def _legacy_sadr_paths() -> tuple[str, ...]:\n+    return tuple(\n+        f\"docs/adr/{path.name.removeprefix('SADR-')}\"\n+        for path in sorted((shiki_installer.ROOT / \"docs\" / \"adr\").glob(\"SADR-*.md\"))\n+        if path.name != \"SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\"\n+    )\n+\n+\n+def _create_legacy_upgrade_fixture(\n+    target: Path,\n+) -> tuple[dict[str, bytes], tuple[str, ...]]:\n+    adr_dir = target / \"docs\" / \"adr\"\n+    adr_dir.mkdir(parents=True)\n+    product_bytes = {\n+        f\"{number:04d}-cubeshare-product-decision.md\": (\n+            f\"# CubeShare ADR {number:04d}\\n\\nDo not rewrite {number}.\\n\"\n+        ).encode()\n+        for number in PRODUCT_NUMBERS\n+    }\n+    for name, content in product_bytes.items():\n+        (adr_dir / name).write_bytes(content)\n+\n+    legacy_paths = _legacy_sadr_paths()\n+    for relative in legacy_paths:\n+        (target / relative).write_bytes(f\"legacy Shiki bytes for {relative}\\n\".encode())\n+\n+    stamp_path = target / shiki_installer.INSTALL_STAMP_PATH\n+    stamp_path.parent.mkdir(parents=True, exist_ok=True)\n+    stamp = {\n+        \"version\": shiki_installer.INSTALL_STAMP_VERSION,\n+        \"platform_commit\": None,\n+        \"installed_at\": \"2000-01-01T00:00:00+00:00\",\n+        \"digests\": {\n+            relative: _sha256(target / relative) for relative in legacy_paths\n+        },\n+    }\n+    stamp_path.write_text(\n+        json.dumps(stamp, indent=2, sort_keys=True) + \"\\n\", encoding=\"utf-8\"\n+    )\n+    return product_bytes, legacy_paths\n+\n+\n+def _tree_snapshot(root: Path) -> dict[str, tuple[str, bytes | str]]:\n+    \"\"\"Recursive byte/shape snapshot used to prove preflight is write-free.\"\"\"\n+    snapshot: dict[str, tuple[str, bytes | str]] = {}\n+    for path in sorted(root.rglob(\"*\")):\n+        relative = path.relative_to(root).as_posix()\n+        if path.is_symlink():\n+            snapshot[relative] = (\"symlink\", path.readlink().as_posix())\n+        elif path.is_dir():\n+            snapshot[relative] = (\"directory\", \"\")\n+        else:\n+            snapshot[relative] = (\"file\", path.read_bytes())\n+    return snapshot\n+\n+\n+class FreshSadrInstallTests(unittest.TestCase):\n+    def setUp(self) -> None:\n+        self.tmp = Path(tempfile.mkdtemp(prefix=\"shiki-sadr-fresh-\"))\n+        self.addCleanup(lambda: shutil.rmtree(self.tmp, ignore_errors=True))\n+        self.target = self.tmp / \"target\"\n+        self.adr_dir = self.target / \"docs\" / \"adr\"\n+        self.adr_dir.mkdir(parents=True)\n+        self.product_bytes = {\n+            f\"{number:04d}-product-decision.md\": (\n+                f\"# Product ADR {number:04d}\\n\\nTarget-owned bytes {number}.\\n\"\n+            ).encode()\n+            for number in PRODUCT_NUMBERS\n+        }\n+        for name, content in self.product_bytes.items():\n+            (self.adr_dir / name).write_bytes(content)\n+\n+    def test_fresh_install_preserves_product_adrs_and_installs_all_sadrs(self) -> None:\n+        _quiet_install(self.target, force=False, validate=True)\n+\n+        for name, expected in self.product_bytes.items():\n+            self.assertEqual((self.adr_dir / name).read_bytes(), expected)\n+\n+        sadr_numbers = {\n+            path.name.split(\"-\", 2)[1]\n+            for path in self.adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\")\n+        }\n+        self.assertEqual(sadr_numbers, {f\"{number:04d}\" for number in range(1, 20)})\n+\n+        numeric_records = {\n+            path.name\n+            for path in self.adr_dir.glob(\"[0-9][0-9][0-9][0-9]-*.md\")\n+        }\n+        self.assertEqual(numeric_records, set(self.product_bytes))\n+        stamp = json.loads(\n+            (self.target / shiki_installer.INSTALL_STAMP_PATH).read_text(\n+                encoding=\"utf-8\"\n+            )\n+        )\n+        for path in self.adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\"):\n+            relative = path.relative_to(self.target).as_posix()\n+            self.assertEqual(stamp[\"digests\"][relative], _sha256(path))\n+        self.assertEqual(find_adr_reference_violations(self.target), [])\n+\n+\n+class LegacySadrUpgradeTests(unittest.TestCase):\n+    def setUp(self) -> None:\n+        self.tmp = Path(tempfile.mkdtemp(prefix=\"shiki-sadr-upgrade-\"))\n+        self.addCleanup(lambda: shutil.rmtree(self.tmp, ignore_errors=True))\n+        self.target = self.tmp / \"target\"\n+        self.adr_dir = self.target / \"docs\" / \"adr\"\n+        self.product_bytes, self.legacy_paths = _create_legacy_upgrade_fixture(\n+            self.target\n+        )\n+\n+    def test_force_upgrade_deletes_only_stamped_legacy_sadrs(self) -> None:\n+        _quiet_install(self.target, force=True, validate=True)\n+\n+        for relative in self.legacy_paths:\n+            self.assertFalse((self.target / relative).exists(), relative)\n+        for name, expected in self.product_bytes.items():\n+            self.assertEqual((self.adr_dir / name).read_bytes(), expected)\n+\n+        sadrs = list(self.adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\"))\n+        self.assertEqual(len(sadrs), 19)\n+        refreshed = json.loads(\n+            (self.target / shiki_installer.INSTALL_STAMP_PATH).read_text(encoding=\"utf-8\")\n+        )\n+        for relative in self.legacy_paths:\n+            self.assertNotIn(relative, refreshed[\"digests\"])\n+        for path in sadrs:\n+            relative = path.relative_to(self.target).as_posix()\n+            self.assertEqual(refreshed[\"digests\"][relative], _sha256(path))\n+\n+    def test_non_force_install_never_deletes_legacy_files(self) -> None:\n+        before = {\n+            relative: (self.target / relative).read_bytes()\n+            for relative in self.legacy_paths\n+        }\n+        _quiet_install(self.target, force=False, validate=False)\n+        for relative, expected in before.items():\n+            self.assertEqual((self.target / relative).read_bytes(), expected)\n+        refreshed = json.loads(\n+            (self.target / shiki_installer.INSTALL_STAMP_PATH).read_text(encoding=\"utf-8\")\n+        )\n+        for relative in self.legacy_paths:\n+            self.assertEqual(refreshed[\"digests\"][relative], _sha256(self.target / relative))\n+\n+\n+class LegacyCleanupPreflightTests(unittest.TestCase):\n+    def setUp(self) -> None:\n+        self.tmp = Path(tempfile.mkdtemp(prefix=\"shiki-sadr-preflight-\"))\n+        self.addCleanup(lambda: shutil.rmtree(self.tmp, ignore_errors=True))\n+\n+    def test_all_ownership_blockers_abort_before_any_write(self) -> None:\n+        cases = (\n+            (\"absent\", \"install stamp is absent\"),\n+            (\"unreadable\", \"install stamp is unreadable\"),\n+            (\"malformed\", \"install stamp is malformed\"),\n+            (\"missing-digest\", \"install stamp has no digest\"),\n+            (\"digest-mismatch\", \"digest mismatch\"),\n+        )\n+        for name, expected_reason in cases:\n+            with self.subTest(name=name):\n+                target = self.tmp / name\n+                _, legacy_paths = _create_legacy_upgrade_fixture(target)\n+                stamp_path = target / shiki_installer.INSTALL_STAMP_PATH\n+\n+                if name == \"absent\":\n+                    stamp_path.unlink()\n+                elif name == \"unreadable\":\n+                    stamp_path.unlink()\n+                    stamp_path.mkdir()\n+                elif name == \"malformed\":\n+                    stamp_path.write_text(\"{not-json\\n\", encoding=\"utf-8\")\n+                else:\n+                    stamp = json.loads(stamp_path.read_text(encoding=\"utf-8\"))\n+                    if name == \"missing-digest\":\n+                        stamp[\"digests\"] = {}\n+                    else:\n+                        stamp[\"digests\"] = {\n+                            relative: \"deadbeef\" for relative in legacy_paths\n+                        }\n+                    stamp_path.write_text(\n+                        json.dumps(stamp, indent=2, sort_keys=True) + \"\\n\",\n+                        encoding=\"utf-8\",\n+                    )\n+\n+                before = _tree_snapshot(target)\n+                with self.assertRaises(shiki_installer.ShikiError) as raised:\n+                    _quiet_install(target, force=True, validate=False)\n+                after = _tree_snapshot(target)\n+\n+                self.assertEqual(before, after, f\"{name} preflight changed the tree\")\n+                message = str(raised.exception)\n+                self.assertIn(expected_reason, message)\n+                self.assertIn(\"No files were written\", message)\n+                for relative in legacy_paths:\n+                    self.assertIn(relative, message)\n+\n+    def test_absent_stamp_is_allowed_when_no_legacy_path_exists(self) -> None:\n+        target = self.tmp / \"no-legacy\"\n+        target.mkdir()\n+        _quiet_install(target, force=True, validate=False)\n+        self.assertTrue(\n+            (target / \"docs\" / \"adr\" / \"SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\").is_file()\n+        )\n+\n+\n+if __name__ == \"__main__\":\n+    unittest.main()\n\n2026-08-07T08:24:00.911634Z ERROR codex_models_manager::manager: failed to renew cache TTL: missing field `base_instructions` at line 94 column 5\nexec\n/bin/zsh -lc \"sed -n '45,100p' .shiki/tasks/T-20260804T095105534980Z-530c4f17.json\" in /private/tmp/shiki-adr-collision.pa0KwY/.worktrees/shiki-t-20260807t073307221254z-2d22d654-introduc\nexec\n/bin/zsh -lc \"sed -n '1431,1590p' scripts/shiki_loop.py\" in /private/tmp/shiki-adr-collision.pa0KwY/.worktrees/shiki-t-20260807t073307221254z-2d22d654-introduc\n succeeded in 0ms:\ndef _run_pre_pr_code_review(target: Path, task_id: str) -> dict[str, Any]:\n    \"\"\"Run the independent read-only code-review verifier over the task diff.\n\n    Loop-owned quality-gate step (SADR-0011): the reviewer is the same model as the\n    implementer but in a separate context, confined to read tools (no edit tools),\n    bound to a structured verdict. The loop parses that verdict deterministically.\n\n    Returns a dict with ``status`` in {clean, blocking, retry, fail}:\n\n    * ``clean``    \u2014 verdict parsed as clean; a type:\"check\" \"code-review\" ledger\n      is recorded and ``pre_pr_code_review`` is written onto the task so the\n      PR-12 ``## Pre-PR code review`` body section renders from it.\n    * ``blocking`` \u2014 verdict parsed as blocking WITH at least one named finding, or\n      a degenerate blocking verdict whose retry budget is exhausted. Fail-closed: a\n      blocking pre-PR review CANNOT anchor a repair packet (no PR exists yet at\n      create_pr time by construction), so the caller stops the loop for diagnosis.\n      NOT a repair.\n    * ``retry``    \u2014 verdict parsed as blocking but naming ZERO findings (degenerate:\n      the reviewer produced nothing usable) while the durable retry budget still\n      allows one more attempt. NEVER treated as clean: the caller re-runs the\n      reviewer next pass rather than opening a PR or stopping. The reviewer's raw\n      output is recorded durably first.\n    * ``fail``     \u2014 dispatch failed, the worktree/diff is unavailable, or the\n      verdict could not be parsed. Fail-closed: review-not-done is never silently\n      passed.\n\n    Never raises into the loop.\n    \"\"\"\n    record = worktree_record(target, task_id)\n    if not record:\n        return {\"status\": \"fail\", \"reason\": \"no worktree record; pre-PR code review skipped\"}\n    worktree_path = Path(record[\"path\"]).expanduser().resolve()\n    if not worktree_path.exists() or worktree_path == target.resolve():\n        return {\"status\": \"fail\", \"reason\": \"worktree unavailable; cannot run the pre-PR code review\"}\n\n    # The review runs BEFORE commit/push, so the implementer's work may still be\n    # uncommitted (and new test files untracked) in the worktree. Stage everything\n    # first (non-destructive \u2014 the commit/push step re-stages anyway) so the diff\n    # is complete, then diff the index against the default branch resolved from\n    # origin. This shows the FULL task change set the reviewer must judge,\n    # committed or not, tracked or new \u2014 and never the already-merged history a\n    # stale local default branch would drag in.\n    run([\"git\", \"add\", \"-A\"], cwd=worktree_path, check=False)\n    base = _default_base_ref(target)\n    diff = run([\"git\", \"diff\", \"--cached\", base], cwd=worktree_path, check=False)\n    if diff.returncode != 0:\n        # Fall back to the committed-only diff (e.g. the default branch is\n        # unrelated/missing so the two-dot diff cannot be computed).\n        diff = run([\"git\", \"diff\", f\"{base}...HEAD\"], cwd=worktree_path, check=False)\n        if diff.returncode != 0:\n            return {\"status\": \"fail\", \"reason\": \"could not compute the task diff for review\"}\n    prompt = _CODE_REVIEW_PROMPT + (diff.stdout or \"\")\n\n    try:\n        exec_result = REVIEWER_ADAPTER.execute(worktree_path, prompt)\n    except Exception:\n        # Effectors fail closed and never raise into the loop (T1 style).\n        return {\"status\": \"fail\", \"reason\": \"reviewer dispatch raised; failing closed\"}\n    if exec_result.returncode != 0:\n        return {\"status\": \"fail\", \"reason\": f\"reviewer exited {exec_result.returncode}; failing closed\"}\n\n    verdict = parse_code_review_verdict(exec_result.stdout)\n    if verdict is None:\n        return {\"status\": \"fail\", \"reason\": \"reviewer verdict could not be parsed; failing closed\"}\n\n    if verdict.get(\"verdict\") == \"blocking\":\n        findings = verdict.get(\"findings\") or []\n        if not findings:\n            # A blocking verdict that names NOTHING is not a usable judgment \u2014 it\n            # blocked the PR while pointing at no concrete issue (Measured\n            # 2026-08-05/06 on work that met every contract requirement; a plain\n            # re-run then passed). Record the reviewer's raw output durably so a\n            # future diagnosis has something to read, then RETRY once (bounded by a\n            # durable per-task counter) before stopping. A degenerate verdict is\n            # NEVER treated as clean: the retry re-runs the reviewer, and once the\n            # budget is exhausted the loop stops for diagnosis with the degenerate\n            # verdict named \u2014 it never becomes a silent pass.\n            ledger_id = _record_reviewer_raw_output(target, task_id, exec_result.stdout)\n            task = load_task(target, task_id)\n            task.setdefault(\"ledger_evidence\", []).append(ledger_id)\n            retries = int(task.get(\"pre_pr_review_empty_retries\") or 0)\n            if retries < MAX_PRE_PR_REVIEW_EMPTY_RETRIES:\n                task[\"pre_pr_review_empty_retries\"] = retries + 1\n                task[\"pre_pr_code_review\"] = {\n                    \"verdict\": \"blocking\",\n                    \"findings\": [],\n                    \"degenerate\": True,\n                    \"empty_retries\": retries + 1,\n                    \"ledger_id\": ledger_id,\n                }\n                _save_task(target, task)\n                return {\n                    \"status\": \"retry\",\n                    \"reason\": (\n                        \"independent pre-PR code review returned a blocking verdict naming zero \"\n                        f\"findings (attempt {retries + 1}/{MAX_PRE_PR_REVIEW_EMPTY_RETRIES + 1}); \"\n                        \"the reviewer produced nothing usable \u2014 re-running it on the same work\"\n                    ),\n                    \"ledger_id\": ledger_id,\n                    \"empty_retries\": retries + 1,\n                }\n            task[\"pre_pr_code_review\"] = {\n                \"verdict\": \"blocking\",\n                \"findings\": [],\n                \"degenerate\": True,\n                \"empty_retries\": retries,\n                \"ledger_id\": ledger_id,\n            }\n            _save_task(target, task)\n            return {\n                \"status\": \"blocking\",\n                \"reason\": (\n                    \"independent pre-PR code review returned a blocking verdict naming zero \"\n                    f\"findings after {retries} retr{'y' if retries == 1 else 'ies'}; the reviewer \"\n                    \"produced nothing usable \u2014 diagnose (its raw output is recorded)\"\n                ),\n                \"ledger_id\": ledger_id,\n                \"degenerate\": True,\n            }\n        # Record the blocking verdict as a check ledger for the audit trail, then\n        # fail closed. No PR exists yet, so this cannot become a repair packet.\n        ledger_id = append_ledger(\n            target,\n            goal_id=load_task(target, task_id)[\"goal_id\"],\n            task_id=task_id,\n            ledger_type=\"check\",\n            summary=f\"Pre-PR code-review verdict BLOCKING for {task_id} ({len(findings)} finding(s)); loop stops for diagnosis\",\n            evidence=[\"independent read-only reviewer (claude -p) \u2014 SADR-0011\"],\n        )\n        task = load_task(target, task_id)\n        task.setdefault(\"ledger_evidence\", []).append(ledger_id)\n        task[\"pre_pr_code_review\"] = {\"verdict\": \"blocking\", \"findings\": findings, \"ledger_id\": ledger_id}\n        _save_task(target, task)\n        return {\"status\": \"blocking\", \"reason\": \"independent pre-PR code review found blocking issues\", \"ledger_id\": ledger_id}\n\n    # Clean verdict: record the code-review check ledger and the PR-12 evidence.\n    findings = verdict.get(\"findings\") or []\n    ledger_id = append_ledger(\n        target,\n        goal_id=load_task(target, task_id)[\"goal_id\"],\n        task_id=task_id,\n        ledger_type=\"check\",\n        summary=f\"Pre-PR code-review verdict CLEAN for {task_id} (independent read-only reviewer, code-review skill)\",\n        evidence=[\"independent read-only reviewer (claude -p) \u2014 SADR-0011\"],\n    )\n    task = load_task(target, task_id)\n    task.setdefault(\"ledger_evidence\", []).append(ledger_id)\n    task[\"pre_pr_code_review\"] = {\"verdict\": \"clean\", \"findings\": findings, \"ledger_id\": ledger_id}\n    _save_task(target, task)\n    return {\"status\": \"clean\", \"reason\": \"independent pre-PR code review passed\", \"ledger_id\": ledger_id}\ndef task_test_command(task: dict[str, Any]) -> str:\n    \"\"\"The structured command the loop exec's for the task's TDD gate.\n\n    Reads the task's ``test_command`` field, falling back to the safe unittest\n    discover default when it is absent or blank. ``acceptance_checks`` is\n    free-form prose+commands and is deliberately NOT consulted here \u2014 it must\n    never be handed to a shell (SADR-0011: a deterministic observable command,\n    not narrative, is what the independent verifier runs).\n    \"\"\"\n    # Lazy import keeps the shiki_loop <-> shiki_tasks edge one-directional.\n\n succeeded in 0ms:\n    \"L-20260804T133927157969Z-ba17fa6f\"\n  ],\n  \"locks\": [\n    \"path:scripts/shiki_guardian.py\",\n    \"path:scripts/mergegate_check.py\",\n    \"path:scripts/guardian_approval_signal.py\",\n    \"path:scripts/shiki_guardian_status.py\",\n    \"path:scripts/shiki_cli.py\",\n    \"path:tests/test_guardian_carry.py\",\n    \"path:tests/test_guardian_carry_parity.py\",\n    \"path:scripts/test_shiki_guardian_policy.sh\"\n  ],\n  \"non_goals\": [\n    \"Carrying a github_review, a guardian_label, or an external AI guardian review\",\n    \"Carrying for critical risk or a label-only high/critical escalation\",\n    \"Persisting the proof to any artifact\",\n    \"Adding any guardian-policy.json key that enables the carry\",\n    \"Changing which risk levels require a Guardian\",\n    \"Touching any file outside the declared locks\"\n  ],\n  \"pre_pr_code_review\": {\n    \"findings\": [\n      {\n        \"detail\": \"git diff main...HEAD reverts unrelated already-merged task T-...c2ef0cfc: status done->review, lock released->active, and deletes closeout ledger L-20260804T115804376798Z-65c06450 (which still exists on the current main tip). This would be a control-plane regression IF merged as-is, but `git status` shows the working tree stages only THIS task's files (guardian scripts, the 530c4f17 lock, the two new tests). The c2ef0cfc delta is stale-base/goal-loop bookkeeping drift from a moving main ref, reconciled by the loop's own base-sync merge (git merge origin/main), not implementer-authored and not fixable by editing this diff. Surfaced for visibility only; not a defect in this task's scope.\",\n        \"severity\": \"low\",\n        \"title\": \"Diff carries an unrelated closeout reversion for task c2ef0cfc (base-sync drift, not this task's code)\"\n      },\n      {\n        \"detail\": \"_carry_global_refusal refuses the carry when the PR's actual labels intersect {high, critical}. Since Shiki derives risk from the task's risk_level (not by auto-labeling the PR object) this correctly targets manual maintainer escalation. If the goal loop ever begins stamping risk:<level> labels on PRs, this would silently disable the carry for every high-risk PR \\u2014 but the direction is fail-closed (refuse carry, require fresh approval), so it is safe, and the feature is currently dormant/unwired. Worth a comment noting the assumption that PRs are not auto-risk-labeled.\",\n        \"severity\": \"low\",\n        \"title\": \"Carry global-refusal rejects any PR whose labels resolve high/critical (fails safe)\"\n      }\n    ],\n    \"ledger_id\": \"L-20260804T130815804255Z-b15290bc\",\n    \"verdict\": \"clean\"\n  },\n  \"required_skills\": [\n    \"tdd\",\n    \"code-review\"\n  ],\n  \"risk_level\": \"critical\",\n  \"scope\": \"Introduce the source token `guardian_comment_carried`. It is produced only when a candidate approved head is proved by `verify_pure_base_sync`, and it joins the human-secondary set alongside `github_review` and `guardian_comment` \\u2014 so the label leg is still required and removing the label remains a working revocation lever.\\n\\nGating conditions evaluated BEFORE the git proof, each refusing the carry with a distinct reason:\\n  - The feature is OFF unless `--base-sync-carry` is passed AND `--default-branch` is non-empty. Absent either, every code path is inert.\\n  - Resolved Guardian risk is exactly `high`. `critical` never carries. If the PR's LABELS alone resolve high or critical, the carry is refused \\u2014 a maintainer escalating by label must get a fresh approval.\\n  - The PR's `baseRefName` equals the default branch, and no `base_ref_changed` entry appears anywhere in the timeline feed.\\n  - Candidate extraction runs in the EXISTING `_comment_source` order \\u2014 marker, not-negating, actor configured, author allowed \\u2014 and only then extracts the SHA. A comment from a non-configured actor is never parsed.\\n  - The carrying comment is unedited (`updated_at == created_at`, or `updated_at` absent).\\n  - Only the newest candidate by (created_at, feed index) is eligible. Any comment from a configured Guardian carrying a negation marker with `created_at` at or after the candidate's refuses the carry AND raises a HARD blocker \\u2014 routed through `_comment_source`'s blockers channel so it survives the demotion the caller applies to soft blockers.\\n\\nWire `--base-sync-carry` and `--default-branch` into `scripts/mergegate_check.py` (argparse, `enforce_guardian_policy`, and BOTH call sites \\u2014 the contract-mode one and the normal-path one; missing either makes the feature unreachable on that path), `scripts/guardian_approval_signal.py` (argparse, the evaluate call, and the emitted JSON, which gains `carried_from_head` and `carry_refused_reasons`), `scripts/shiki_guardian_status.py` (so the operator-facing and CCA-posted status cannot contradict the gate) and the `guardian status` subparser in `scripts/shiki_cli.py`.\\n\\nThe proof is recomputed in-process in every judging invocation. It is never written to `.shiki/gha` and never carried in an artifact, so there is nothing to go stale or be forged.\\n\\nAdd `tests/test_guardian_carry.py` driving the real evaluator, and `tests/test_guardian_carry_parity.py` asserting MergeGate and the signal return identical approved/sources over one fixture set and that the signal is never more permissive. Positive cases assert both `approved` and that `guardian_comment_carried` is in `sources`. EVERY negative case must assert BOTH that the carry did not happen AND that the Guardian requirement still resolves as required \\u2014 the signal reporting `required=true, approved=false`, and MergeGate blocking with a Guardian blocker string \\u2014 never merely a non-zero exit. Cover: a fresh current-head comment still wins over a carry; critical risk refuses; a label-only high escalation refuses; a retargeted base refuses; a `base_ref_changed` timeline entry refuses; an edited comment refuses; a body with two SHAs refuses; a non-configured actor is dropped before extraction; a later negation comment refuses AND produces a hard blocker, asserted not to be a warning; a removed label defeats approval; an empty `--default-branch` behaves exactly as the pre-change gate; the flag omitted entirely produces a byte-identical result on the full existing fixture set; a comment from another PR is never a candidate; and no invented guardian-policy.json key can enable the carry.\\n\\nREPRODUCIBILITY \\u2014 what this task changes, and what to change to get the same result.\\n  Symptom: a PR whose content nobody touched loses its approval because branch protection made it merge main. Measured 2026-08-01 across five Contract PRs: four extra operator approvals, entirely mechanical.\\n  Why the carry must reach BOTH the gate and the signal: the MergeGate policy job depends on the CCA job, `enforce_cca_verdict` fails any verdict that is not complete, and the signal the CCA prompt reads is computed from the guardian evaluation alone. Wiring the carry into MergeGate only means the CCA still returns needs_guardian, its job fails, and the policy check never runs \\u2014 the gate this change exists to move is never reached. This exact duality has been undercounted repeatedly in this repository; both call sites inside mergegate_check.py have the same property.\\n  Why the label leg is still required: the label is the operator's revocation lever that does not depend on any SHA. Keeping it means an operator can always kill a carried approval by removing one label.\\n  Why a negation must be a HARD blocker: the caller demotes soft blockers to warnings once any approval path succeeds, so a revocation routed as a soft blocker would be silently swallowed by the very carry it is trying to revoke.\\n  Reproducible when: a base-synced PR keeps its approval with `guardian_comment_carried` in sources, and every listed attack refuses the carry while leaving the Guardian requirement required.\\n\\nBACKWARD-COMPATIBILITY CONSTRAINT (audited 2026-08-04): every new parameter added to `evaluate_guardian_approval` and to the functions the signal/status paths call MUST be keyword-only WITH a default. Out-of-lock callers exist and must keep working unchanged: `scripts/test_shiki_governance_evidence.sh`, `tests/test_guardian_evidence.py`, `tests/test_guardian_status.py`, `tests/test_guardian_signal*.py`, `tests/test_contract_approval.py`, `tests/test_bookkeeping_closeout.py`. A positional or required parameter breaks all of them and the task cannot repair them from inside its locks.\",\n  \"status\": \"done\",\n  \"test_command\": \"python3 -m unittest discover -s tests && bash scripts/test_shiki_guardian_policy.sh && bash scripts/test_shiki_governance_evidence.sh && bash scripts/test_shiki_adversarial_state.sh && python3 scripts/validate_shiki.py --target .\",\n  \"title\": \"Accept a carried Guardian approval in the gate and the signal\"\n}\n\nexec\n/bin/zsh -lc \"sed -n '1,180p' .shiki/ledger/L-20260731T061404552195Z-d2dfd80b.json\" in /private/tmp/shiki-adr-collision.pa0KwY/.worktrees/shiki-t-20260807t073307221254z-2d22d654-introduc\n succeeded in 0ms:\n{\n  \"actor\": \"shiki-cli\",\n  \"evidence\": [\n    \"independent read-only reviewer (claude -p) \\u2014 ADR 0011\"\n  ],\n  \"goal_id\": \"G-20260729T065622761503Z-4479b0d3\",\n  \"id\": \"L-20260731T061404552195Z-d2dfd80b\",\n  \"links\": [],\n  \"summary\": \"Pre-PR code-review verdict CLEAN for T-20260729T065622770078Z-3662bff2 (independent read-only reviewer, code-review skill)\",\n  \"task_id\": \"T-20260729T065622770078Z-3662bff2\",\n  \"timestamp\": \"2026-07-31T06:14:04+00:00\",\n  \"type\": \"check\"\n}\n\nexec\n/bin/zsh -lc \"rg --files .shiki/schemas | rg 'ledger|task'\" in /private/tmp/shiki-adr-collision.pa0KwY/.worktrees/shiki-t-20260807t073307221254z-2d22d654-introduc\n succeeded in 0ms:\n.shiki/schemas/task.schema.json\n.shiki/schemas/ledger.schema.json\n\ndiff --git a/.codex/skills/shiki/SKILL.md b/.codex/skills/shiki/SKILL.md\nindex c8e2e85d9d75d49cc4f2d6286ee22659c4e53e75..93cded623b2c15d9f65c76491a1c2d813ac9e6c0\n--- a/.codex/skills/shiki/SKILL.md\n+++ b/.codex/skills/shiki/SKILL.md\n@@ -18,6 +18,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n Codex CLI does not register this skill as a custom `/shiki` slash command. When\n the user types `/shiki` in Codex CLI and gets \"Unrecognized command\", explain\n that this is expected: invoke Shiki in Codex with natural language such as\n@@ -69,7 +72,7 @@\n \n ## Responsibilities\n \n-- Claude Code implements and repairs by default (`shiki runner claude`, ADR 0008); Codex implements and repairs tasks explicitly assigned to `codex`.\n+- Claude Code implements and repairs by default (`shiki runner claude`, SADR-0008); Codex implements and repairs tasks explicitly assigned to `codex`.\n - Claude Code Action can act as GitHub-side CCA or reviewer.\n - CCA judges completion.\n - MergeGate authorizes state transitions and merge readiness.\ndiff --git a/.github/prompts/cca-completion-check.md b/.github/prompts/cca-completion-check.md\nindex ed203ad5f302ecc81372d8ab27d2b1232ca97497..fd89950528333ef6242e6d5443904f5392ab7e56\n--- a/.github/prompts/cca-completion-check.md\n+++ b/.github/prompts/cca-completion-check.md\n@@ -19,6 +19,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n ## Required Reading\n \n Read only the files needed to judge the PR contract. Prefer this order:\ndiff --git a/.github/prompts/codex-task-handoff.md b/.github/prompts/codex-task-handoff.md\nindex 695e211d76dea1c60e8d5bb360e78487808286a4..59441d576b01a39569722f226c854e5a740e9eb5\n--- a/.github/prompts/codex-task-handoff.md\n+++ b/.github/prompts/codex-task-handoff.md\n@@ -2,7 +2,7 @@\n \n You are Codex Front acting as the Implementer for a Shiki task.\n \n-Follow `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant ADRs, and `docs/agents/codex-handoff.md`.\n+Follow `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant Shiki SADRs and target ADRs, and `docs/agents/codex-handoff.md`.\n \n Rules:\n \ndiff --git a/AGENTS.md b/AGENTS.md\nindex 7f041100d49c2a7525f51cb0f5d696a4a6d1c78c..a06bfbabc6fa48a007389e25e12d7ed523936bed\n--- a/AGENTS.md\n+++ b/AGENTS.md\n@@ -22,7 +22,7 @@\n - **Codex Front** is an optional implementer through Codex App, Codex CLI, Codex IDE extension, or Codex Web signed in with ChatGPT OAuth/subscription auth, dispatched with `shiki runner codex` when a task is explicitly assigned to `codex`.\n - **GitHub CCA** is implemented by Claude Code Action by default, using `CLAUDE_CODE_OAUTH_TOKEN`.\n \n-Do not assume `openai/codex-action`, `OPENAI_API_KEY`, or API-key based Codex automation in the default Shiki path. API-key runners are explicit target-repo extensions and require their own ADR.\n+Do not assume `openai/codex-action`, `OPENAI_API_KEY`, or API-key based Codex automation in the default Shiki path. API-key runners are explicit target-repo extensions and require their own target ADR.\n \n Shiki is not:\n \n@@ -44,10 +44,14 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use\n+`SADR-NNNN-*.md`; target product decisions retain `NNNN-*.md` and the `ADR`\n+namespace.\n+\n Use the canonical source-of-truth order above. Runtime-specific reading orders\n are operational guidance only and must not override or reorder it.\n \n-Conversation state is not durable truth. If a decision matters, put it in GitHub, `.shiki/`, `CONTEXT.md`, or an ADR.\n+Conversation state is not durable truth. If a decision matters, put it in GitHub, `.shiki/`, `CONTEXT.md`, a Shiki SADR, or a target ADR.\n \n If GitHub and `.shiki/` disagree, surface the conflict and prefer GitHub operational state until the mirror is repaired.\n \n@@ -75,8 +79,8 @@\n Every non-trivial change follows this loop:\n \n 1. **Goal**: clarify outcome, completion conditions, non-goals, risk level, and success signals.\n-2. **grill-with-docs**: challenge the plan against domain docs, ADRs, code reality, terminology, and edge scenarios.\n-3. **Context & Impact**: find relevant docs, ADRs, code, dependencies, owners, locks, and verification surfaces.\n+2. **grill-with-docs**: challenge the plan against domain docs, Shiki SADRs, target ADRs, code reality, terminology, and edge scenarios.\n+3. **Context & Impact**: find relevant docs, SADRs, target ADRs, code, dependencies, owners, locks, and verification surfaces.\n 4. **PRD and Spec Freeze**: use `to-prd` when context is settled enough to become durable product/engineering intent. The operator's explicit approval of the PRD is Spec Freeze: the plan records a `spec_freeze` block, and no plan runs without it. Steps 1-4 form Requirements Definition \u2014 one continuous operator dialogue. After Spec Freeze, scope changes require an operator-approved Spec Amendment; non-scope-moving interpretations go to the Assumption Log.\n 5. **Issues**: use `to-issues` to create independently grabbable vertical-slice issues. Prefer AFK slices over HITL slices where possible.\n 6. **Triage**: label issues for readiness, risk, runtime, skills, and MergeGate state.\n@@ -167,7 +171,7 @@\n - Testing decisions.\n - Out-of-scope items.\n - Further notes.\n-- Links to relevant domain terms and ADRs.\n+- Links to relevant domain terms, SADRs, and target ADRs.\n \n Every executable task must include:\n \n@@ -193,7 +197,7 @@\n \n Context & Impact must identify:\n \n-- Relevant documents, domain terms, ADRs, and past decisions.\n+- Relevant documents, domain terms, SADRs, target ADRs, and past decisions.\n - Relevant modules, interfaces, seams, callers, owners, and tests.\n - Dependency relationships between tasks.\n - Candidate locks and lock conflicts.\n@@ -210,7 +214,7 @@\n | Skill | Required when |\n | --- | --- |\n | `setup-matt-pocock-skills` | First configuring the repo for skills, or when issue tracker, triage labels, or domain docs are missing. |\n-| `grill-with-docs` | Default for non-trivial Goals before PRD/issues; always required when plans, terminology, boundaries, tradeoffs, or ADR-worthy decisions are ambiguous. |\n+| `grill-with-docs` | Default for non-trivial Goals before PRD/issues; always required when plans, terminology, boundaries, tradeoffs, or decision-record-worthy choices are ambiguous. |\n | `zoom-out` | The agent lacks an architectural map of the relevant code area. |\n | `to-prd` | Settled Goal context must become durable product/engineering intent. |\n | `to-issues` | A Goal, PRD, or plan must become independently grabbable vertical-slice issues. |\n@@ -348,7 +352,7 @@\n - Three or more new files.\n - Any high-risk or critical label.\n \n-When the architecture gate triggers, run `grill-with-docs` or `improve-codebase-architecture` as appropriate and record durable decisions in an ADR when needed.\n+When the architecture gate triggers, run `grill-with-docs` or `improve-codebase-architecture` as appropriate and record durable decisions in a Shiki SADR or target ADR as applicable.\n \n ## Engineering Discipline\n \ndiff --git a/CLAUDE.md b/CLAUDE.md\nindex 98a932e7841a632866fcd53b0bad0e3af3711d3d..98a31399404d6fb044b4fd493f4459ac87f439de\n--- a/CLAUDE.md\n+++ b/CLAUDE.md\n@@ -14,6 +14,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n ## Claude Role\n \n Default to planner, default implementer, reviewer, coordinator, documentation editor, CCA-style completion judge, and final-judgment assistant.\n@@ -57,7 +60,7 @@\n At the start of a Shiki session:\n \n 1. Read `AGENTS.md`.\n-2. Read `CONTEXT.md` and relevant ADRs.\n+2. Read `CONTEXT.md` and relevant Shiki SADRs and target ADRs.\n 3. Read active `.shiki/` Goal/task/ledger state when present.\n 4. Read active distilled rules from `.shiki/memories/` (status `distilled`, `active: true`, not revoked or superseded) for applicable guidance; the same rules are injected deterministically into task handoffs as the `## Distilled Rules` section (\u00a73.5).\n 5. Read the GitHub Issue or PR when the task is GitHub-backed.\n@@ -74,7 +77,7 @@\n - Define the Goal, completion conditions, non-goals, and risk level.\n - Treat `grill-with-docs` as the default planning entry point for non-trivial Goals.\n - Challenge domain terms against `CONTEXT.md`.\n-- Challenge hard-to-reverse tradeoffs against ADRs.\n+- Challenge hard-to-reverse tradeoffs against SADRs and target ADRs.\n - Explore code instead of asking when code can answer the question.\n - Run Context & Impact before implementation planning.\n - Produce a Task DAG, not an unordered checklist.\n@@ -94,7 +97,7 @@\n - For each question, provide your recommended answer.\n - Resolve dependency decisions before downstream design questions.\n - Update `CONTEXT.md` inline when terminology crystallizes.\n-- Offer ADRs only when the decision is hard to reverse, surprising without context, and a real tradeoff.\n+- Offer a SADR or target ADR only when the decision is hard to reverse, surprising without context, and a real tradeoff.\n - Convert unresolved questions into HITL blockers rather than allowing Codex to guess.\n \n ## PRD and Issues Standard\n@@ -103,7 +106,7 @@\n \n - Use the project's domain glossary vocabulary.\n - Avoid volatile file paths unless they are essential.\n-- Include implementation decisions, testing decisions, out-of-scope items, and relevant ADR/domain links.\n+- Include implementation decisions, testing decisions, out-of-scope items, and relevant SADR/target-ADR/domain links.\n - Identify deep-module opportunities and testable interfaces.\n \n When creating issues:\n@@ -121,7 +124,7 @@\n - Branch/worktree target.\n - Scope and non-goals.\n - Dependencies and locks.\n-- Relevant docs, ADRs, modules, and tests.\n+- Relevant docs, SADRs, target ADRs, modules, and tests.\n - Required skill invocations.\n - TDD expectations.\n - Pre-PR `code-review` gate expectations.\n@@ -152,7 +155,7 @@\n \n - Treat yourself as a judge, not implementer.\n - Do not write production code.\n-- Read `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant ADRs, PRD, task issue, PR body, diff, checks, reviews, labels, and `.shiki/` state.\n+- Read `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant SADRs and target ADRs, PRD, task issue, PR body, diff, checks, reviews, labels, and `.shiki/` state.\n - Evaluate every applicable checklist in `docs/agents/checklists.md`.\n - Map each acceptance criterion to durable evidence.\n - Distinguish implementation failure from missing evidence.\ndiff --git a/CONTEXT.md b/CONTEXT.md\nindex e61a9be06a0cce0d026484083a657bde2067e234..237ee8aa3ea8fab334ece49768bc5ed3b5af0974\n--- a/CONTEXT.md\n+++ b/CONTEXT.md\n@@ -25,7 +25,7 @@\n _Avoid_: jumping directly into code\n \n **grill-with-docs**:\n-A planning skill that challenges a plan against domain language, ADRs, code reality, and concrete edge scenarios. It resolves design-tree questions before PRD/issues. It runs inside Requirements Definition.\n+A planning skill that challenges a plan against domain language, Shiki SADRs, target ADRs, code reality, and concrete edge scenarios. It resolves design-tree questions before PRD/issues. It runs inside Requirements Definition.\n _Avoid_: generic brainstorming, silent assumptions\n \n **Requirements Definition**:\ndiff --git a/CONTRIBUTING.md b/CONTRIBUTING.md\nindex c6d9b656e73abb8a55e5f660123e03153dcb3e6d..9e37a8fbf668db5b22461aaae5548b8f1354394c\n--- a/CONTRIBUTING.md\n+++ b/CONTRIBUTING.md\n@@ -17,7 +17,7 @@\n 1. **Goal** \u2014 start from a user-approved Goal with an outcome, completion\n    conditions, non-goals, risk level, and success signals.\n 2. **grill-with-docs** \u2014 for any non-trivial change, challenge the plan against\n-   domain language, ADRs, and code reality before writing a PRD.\n+   domain language, Shiki SADRs, target ADRs, and code reality before writing a PRD.\n 3. **PRD** \u2014 capture settled product/engineering intent (`to-prd`).\n 4. **Spec Freeze** \u2014 the operator explicitly approves the PRD/requirements;\n    the plan records a `spec_freeze` block and no plan runs without it\n@@ -94,7 +94,7 @@\n   entries.\n - Do not hand-edit `.shiki/goals/*.json` or `.shiki/manifest.json` in a task PR.\n - Conversation state is not durable truth. If a decision matters, record it in\n-  GitHub, `.shiki/`, `CONTEXT.md`, or an ADR.\n+  GitHub, `.shiki/`, `CONTEXT.md`, a Shiki SADR, or a target ADR.\n \n ## Architecture Gate\n \n@@ -103,7 +103,7 @@\n or CI policy, package structure, three or more structural files, more than 100\n net new lines in a core module, three or more new files, or any high-risk label.\n When triggered, run `grill-with-docs` or `improve-codebase-architecture` and\n-record durable decisions in an ADR when needed (see `docs/adr/`).\n+record durable decisions in a Shiki SADR or target ADR as applicable (see `docs/adr/`).\n \n ## Safety\n \ndiff --git a/SYSTEM_PROMPT.md b/SYSTEM_PROMPT.md\nindex aaaea45e3a896c3a44ba89eec8bb84325ecf6c62..da009c86a88285269dee0155e58e550c6d744ea9\n--- a/SYSTEM_PROMPT.md\n+++ b/SYSTEM_PROMPT.md\n@@ -30,6 +30,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n Use the canonical source-of-truth order above. Runtime-specific reading orders\n are operational guidance only and must not override or reorder it. The current\n conversation is non-durable operator input.\n@@ -48,14 +51,14 @@\n \n A runtime may only do work that is explicitly assigned by the Goal, task, PR, or MergeGate decision.\n \n-Do not assume `openai/codex-action`, `OPENAI_API_KEY`, or API-key based Codex automation in the default Shiki path. API-key runners are explicit target-repo extensions and require their own ADR.\n+Do not assume `openai/codex-action`, `OPENAI_API_KEY`, or API-key based Codex automation in the default Shiki path. API-key runners are explicit target-repo extensions and require their own target ADR.\n \n ## 4. Non-negotiable Shiki flow\n \n For any non-trivial Goal, use this flow:\n \n 1. **Goal Seek**: define outcome, non-goals, risk level, completion criteria, and evidence requirements.\n-2. **grill-with-docs**: challenge the plan against `CONTEXT.md`, ADRs, code reality, terminology, boundaries, and edge scenarios. Ask one question at a time when the operator is available. If code can answer a question, inspect code instead of asking.\n+2. **grill-with-docs**: challenge the plan against `CONTEXT.md`, Shiki SADRs, target ADRs, code reality, terminology, boundaries, and edge scenarios. Ask one question at a time when the operator is available. If code can answer a question, inspect code instead of asking.\n 3. **Context & Impact**: identify modules, interfaces, seams, callers, dependencies, locks, risk, and verification surfaces.\n 4. **to-prd + Spec Freeze**: turn settled context into a PRD using domain vocabulary and testing decisions. The operator's explicit approval of the PRD is Spec Freeze, recorded as the plan's `spec_freeze` block; plans without it do not run. Steps 1-4 form Requirements Definition.\n 5. **to-issues**: decompose the PRD into independently grabbable vertical-slice issues. Prefer AFK slices; mark HITL slices when judgment is still required.\n@@ -75,7 +78,7 @@\n Before making any material change:\n \n 1. Read `AGENTS.md` and any runtime-specific wrapper such as `CLAUDE.md`.\n-2. Read `CONTEXT.md` and relevant ADRs.\n+2. Read `CONTEXT.md` and relevant SADRs and target ADRs.\n 3. Read the GitHub Issue / PR and `.shiki/` state for the active Goal or task.\n 4. Identify your role: Planner, Implementer, Reviewer, Completion Check Agent, Repairer, or Guardian-assist.\n 5. Identify Goal id, task id, branch, PR, dependencies, locks, risk level, required skills, and required evidence.\n@@ -103,7 +106,7 @@\n \n At minimum, identify:\n \n-- Relevant documents, ADRs, domain terms, and prior decisions.\n+- Relevant documents, SADRs, target ADRs, domain terms, and prior decisions.\n - Relevant modules, interfaces, seams, callers, owners, and tests.\n - Dependency relationships between tasks.\n - Candidate file locks and lock conflicts.\n@@ -152,7 +155,7 @@\n \n CCA must read:\n \n-- `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant ADRs.\n+- `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant SADRs and target ADRs.\n - Parent Goal issue, PRD issue, task issue, comments, and labels.\n - PR body, diff, commits, changed files, reviews, CI checks, and artifacts available to the workflow.\n - `.shiki/` task contract, locks, ledger entries, CCA checklist profile, and prior repair packets.\n@@ -214,7 +217,7 @@\n - Three or more new files.\n - Any high-risk or critical label.\n \n-When the architecture gate triggers, run `grill-with-docs` or `improve-codebase-architecture` as appropriate and record the decision in an ADR if it is hard to reverse or likely to be re-litigated.\n+When the architecture gate triggers, run `grill-with-docs` or `improve-codebase-architecture` as appropriate and record the decision in a Shiki SADR or target ADR as applicable if it is hard to reverse or likely to be re-litigated.\n \n ## 14. Skill Gate\n \n@@ -223,7 +226,7 @@\n Use these skills:\n \n - `setup-matt-pocock-skills`: before first use of the engineering skill set, or when issue tracker, triage labels, or domain docs are missing.\n-- `grill-with-docs`: default for non-trivial Goals before PRD/issues; required for ambiguous plans, terminology, boundaries, ADR-worthy tradeoffs, or design-tree decisions.\n+- `grill-with-docs`: default for non-trivial Goals before PRD/issues; required for ambiguous plans, terminology, boundaries, decision-record-worthy tradeoffs, or design-tree decisions.\n - `zoom-out`: unfamiliar code area or missing architectural map.\n - `to-prd`: convert settled context into a PRD.\n - `to-issues`: break a PRD, plan, or Goal into independently grabbable vertical-slice issues.\ndiff --git a/docs/adr/0001-keep-shiki-as-platform-name.md b/docs/adr/SADR-0001-keep-shiki-as-platform-name.md\nindex 20a9637c3b9d8c97de7ba8db7cfae4d82d9a7dba..8b685439959aada1b221c112f0ac3c51ca8a1695\n--- a/docs/adr/0001-keep-shiki-as-platform-name.md\n+++ b/docs/adr/SADR-0001-keep-shiki-as-platform-name.md\n@@ -1,3 +1,3 @@\n-# Keep Shiki As The Platform Name\n+# SADR-0001: Keep Shiki As The Platform Name\n \n Shiki remains the product name for the GitHub-native agentic engineering control plane. We considered more descriptive names such as MergePilot, MergeGate, and Kiroku, but kept Shiki because it carries the existing goal-seeking framework identity while remaining independent of Hermes, Codex, Claude Code, or any single runtime.\ndiff --git a/docs/adr/0002-use-agents-as-shared-constitution.md b/docs/adr/SADR-0002-use-agents-as-shared-constitution.md\nindex 73de30ecb078120eac540d9bc51855e3cd48d0e3..c5486182bf06fca36ea508496b013c67302fd989\n--- a/docs/adr/0002-use-agents-as-shared-constitution.md\n+++ b/docs/adr/SADR-0002-use-agents-as-shared-constitution.md\n@@ -1,3 +1,3 @@\n-# Use AGENTS As The Shared Agent Constitution\n+# SADR-0002: Use AGENTS As The Shared Agent Constitution\n \n Shiki uses `AGENTS.md` as the runtime-neutral constitution and `CLAUDE.md` as a thin Claude Code wrapper. This keeps Codex, Claude Code, Hermes Runner, and future Agent Runtimes aligned on the same GitHub-first MergeGate rules while still allowing runtime-specific instructions where needed.\ndiff --git a/docs/adr/0003-require-skill-gate-for-mergegate-readiness.md b/docs/adr/SADR-0003-require-skill-gate-for-mergegate-readiness.md\nindex 424be0c9af4c8f0136f1fde68a468c47ff84e764..7fc337987c7df9792f75f181dfcbed75fe5ffff2\n--- a/docs/adr/0003-require-skill-gate-for-mergegate-readiness.md\n+++ b/docs/adr/SADR-0003-require-skill-gate-for-mergegate-readiness.md\n@@ -1,3 +1,3 @@\n-# Require Skill Gate For MergeGate Readiness\n+# SADR-0003: Require Skill Gate For MergeGate Readiness\n \n Shiki requires relevant engineering skills to be invoked before execution when their triggers apply. This makes skills part of MergeGate readiness rather than optional prompt style, because Shiki depends on repeatable planning, issue decomposition, TDD, diagnosis, triage, and architecture review before autonomous or parallel agent work can be trusted.\ndiff --git a/docs/adr/0004-use-github-first-with-shiki-mirror.md b/docs/adr/SADR-0004-use-github-first-with-shiki-mirror.md\nindex d0a3a9174dc646a945a9d325954ffaf00c352040..a79c54ae9ee8eed2a0524192b864da4e22a92d3f\n--- a/docs/adr/0004-use-github-first-with-shiki-mirror.md\n+++ b/docs/adr/SADR-0004-use-github-first-with-shiki-mirror.md\n@@ -1,4 +1,4 @@\n-# ADR 0004: Use GitHub First With A Repository-Local Shiki Mirror\n+# SADR-0004: Use GitHub First With A Repository-Local Shiki Mirror\n \n ## Status\n \ndiff --git a/docs/adr/0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md b/docs/adr/SADR-0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\nindex b339baf49b89d9290e79c3d51b36e7b2bde3bd95..0f3c3eb9afccdb3403ce978e21d04fe12e495da0\n--- a/docs/adr/0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\n+++ b/docs/adr/SADR-0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\n@@ -1,8 +1,8 @@\n-# ADR 0005: Use Codex As Front And Claude Code Action As The GitHub Runtime\n+# SADR-0005: Use Codex As Front And Claude Code Action As The GitHub Runtime\n \n ## Status\n \n-Accepted (implementer default superseded by ADR 0008; the auth model and the\n+Accepted (implementer default superseded by SADR-0008; the auth model and the\n Claude Code Action GitHub runtime decision remain in force)\n \n ## Context\n@@ -26,4 +26,4 @@\n - Shiki avoids requiring `OPENAI_API_KEY` for the default workflow.\n - Codex work happens through the user's authenticated Codex front surface, not as a hidden backend Action.\n - Claude Code Action is configured for OAuth-token based subscription usage by default.\n-- API-key based automation remains possible only as an explicit target-repo extension with its own ADR.\n+- API-key based automation remains possible only as an explicit target-repo extension with its own target ADR.\ndiff --git a/docs/adr/0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md b/docs/adr/SADR-0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\nindex 4bcae9d1e52f0a37b8e9fbafa5cd14b6ee1993bd..7a38538a6ae9929567f0ade67e7ea6a60d0b3f7e\n--- a/docs/adr/0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\n+++ b/docs/adr/SADR-0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\n@@ -1,4 +1,4 @@\n-# ADR 0006: Separate Codex Implementation, CCA Completion, And MergeGate Authorization\n+# SADR-0006: Separate Codex Implementation, CCA Completion, And MergeGate Authorization\n \n ## Status\n \ndiff --git a/docs/adr/0007-packaging-and-release.md b/docs/adr/SADR-0007-packaging-and-release.md\nindex 79b84ddb97167372b44641e5b7860e400ce80822..bb445a2e2e856d9d1f5fdbe35de8d4e8cec0fb32\n--- a/docs/adr/0007-packaging-and-release.md\n+++ b/docs/adr/SADR-0007-packaging-and-release.md\n@@ -1,4 +1,4 @@\n-# ADR 0007: Packaging And Release For Shiki\n+# SADR-0007: Packaging And Release For Shiki\n \n ## Status\n \n@@ -25,7 +25,7 @@\n   not introduce third-party runtime dependencies or break the shim.\n - Releases must run inside the operator's subscription-authenticated toolchain\n   and must not require extra secrets beyond the built-in `GITHUB_TOKEN`\n-  (consistent with ADR 0005's no-API-key-by-default stance).\n+  (consistent with SADR-0005's no-API-key-by-default stance).\n - `.shiki/manifest.json` already encodes the install surface (`install.include`,\n   `install.create_directories`, `install.exclude_from_commit`), and it must not\n   be mutated as part of this change.\n@@ -34,7 +34,7 @@\n \n This is a hard-to-reverse platform decision (it sets the version source of\n truth, the release trigger, and the public install contract), so it is recorded\n-as an ADR.\n+as a SADR.\n \n ## Decision\n \n@@ -104,7 +104,7 @@\n 3. **Publishing to PyPI.** Rejected for now: Shiki is distributed as a\n    repository template + git-installable CLI, not a PyPI library; PyPI would add\n    credentials/secrets and a different versioning contract. Can be added later as\n-   its own ADR if a packaged distribution is needed.\n+   its own SADR if a packaged distribution is needed.\n 4. **Dynamic version (read `VERSION` via a build backend `dynamic` field).**\n    Rejected to keep `pyproject.toml` backend-agnostic and dependency-free; the\n    small duplication between `VERSION` and `pyproject.toml` is guarded by the\n@@ -123,4 +123,4 @@\n   releasing versions the whole platform but ships only `install.include` to\n   targets.\n - Future PyPI distribution or a console entry point are deferred and would each\n-  warrant their own ADR.\n+  warrant their own SADR.\ndiff --git a/docs/adr/0008-use-claude-code-as-default-implementer.md b/docs/adr/SADR-0008-use-claude-code-as-default-implementer.md\nindex dedf4510b7c7799521422f622029563d373db031..35c2625d908167fbc58855b5b63e283805f167e8\n--- a/docs/adr/0008-use-claude-code-as-default-implementer.md\n+++ b/docs/adr/SADR-0008-use-claude-code-as-default-implementer.md\n@@ -1,12 +1,12 @@\n-# ADR 0008: Use Claude Code As The Default Implementer Runtime\n+# SADR-0008: Use Claude Code As The Default Implementer Runtime\n \n ## Status\n \n-Proposed (supersedes ADR 0005 for the implementer default; keeps its auth model)\n+Proposed (supersedes SADR-0005 for the implementer default; keeps its auth model)\n \n ## Context\n \n-ADR 0005 made Codex Front the default implementation runtime, with Claude Code\n+SADR-0005 made Codex Front the default implementation runtime, with Claude Code\n restricted to planner and reviewer roles by the runtime registry, and the only\n autonomous implementation adapter was `shiki runner codex`.\n \n@@ -18,13 +18,13 @@\n - The platform's planning, review, and CCA layers already run on Claude Code,\n   and Claude Code's native capabilities (the built-in `/code-review` skill and\n   the Workflow multi-agent orchestration tool) have no Codex equivalent.\n-- The Goal lifecycle redesign (ADR 0009) requires an autonomous post-freeze\n+- The Goal lifecycle redesign (SADR-0009) requires an autonomous post-freeze\n   loop driven from the operator's Claude Code session, including bounded\n   repair dispatch, which must not depend on a runtime that cannot be invoked\n   unattended.\n \n The constitution claims Shiki is runtime-agnostic (\"not a Claude-only\n-workflow\", ADR 0002). Any change to the implementer default must preserve the\n+workflow\", SADR-0002). Any change to the implementer default must preserve the\n registry-based runtime model rather than hardcoding one vendor.\n \n ## Decision\n@@ -39,7 +39,7 @@\n   the task to review or repair-needed.\n - Keep Codex registered as an optional implementer runtime. `shiki runner\n   codex`, the Codex handoff templates, and the ChatGPT OAuth auth model from\n-  ADR 0005 remain supported for tasks explicitly assigned to `codex`.\n+  SADR-0005 remain supported for tasks explicitly assigned to `codex`.\n - Commit to mandating two Claude-native capabilities as loop contract in\n   follow-up Goals (status: the code-review gate was delivered by Goal G-B \u2014\n   skill registry, checklists item PR-12, default required_skills \u2014 while the\n@@ -60,7 +60,7 @@\n \n Out of scope: removing Codex support, changing the CCA runtime (Claude Code\n Action with `CLAUDE_CODE_OAUTH_TOKEN` stays), and API-key based automation\n-(still requires its own ADR per ADR 0005).\n+(still requires its own target ADR per SADR-0005).\n \n ## Consequences\n \n@@ -100,7 +100,7 @@\n   harness rejection of direct `codex exec` is outside Shiki's control, and the\n   plan's mandatory native capabilities cannot run on Codex at all.\n - **Remove Codex entirely**: rejected; it abandons the runtime-agnostic\n-  constitution (ADR 0002) for no operational gain and forecloses\n+  constitution (SADR-0002) for no operational gain and forecloses\n   cross-runtime adversarial review.\n - **GitHub Actions cloud implementation via claude-code-action**: rejected as\n   default; durable evidence is automatic but token cost, Node 24 workflow\ndiff --git a/docs/adr/0009-requirements-definition-and-spec-freeze.md b/docs/adr/SADR-0009-requirements-definition-and-spec-freeze.md\nindex 29b3f7871bef9203a8b4a831a65c39eb2f506e42..52e7dd69ad0aa4efe0b68c62678abbf7df02aaec\n--- a/docs/adr/0009-requirements-definition-and-spec-freeze.md\n+++ b/docs/adr/SADR-0009-requirements-definition-and-spec-freeze.md\n@@ -1,4 +1,4 @@\n-# ADR 0009: Merge Grill And Planning Into Requirements Definition With Spec Freeze\n+# SADR-0009: Merge Grill And Planning Into Requirements Definition With Spec Freeze\n \n ## Status\n \ndiff --git a/docs/adr/0010-external-ai-guardian-review.md b/docs/adr/SADR-0010-external-ai-guardian-review.md\nindex e6108bbe8bb4e73e0ff4684c8f78001a80438c4e..f3510b145664298adafb151b6f0db51b972f01ec\n--- a/docs/adr/0010-external-ai-guardian-review.md\n+++ b/docs/adr/SADR-0010-external-ai-guardian-review.md\n@@ -1,4 +1,4 @@\n-# ADR 0010: External AI Guardian Review As A First-Class Approval Authority\n+# SADR-0010: External AI Guardian Review As A First-Class Approval Authority\n \n ## Status\n \n@@ -8,7 +8,7 @@\n \n Shiki is intended to be an autonomous, recursive, self-improving agent\n framework: authority-in-the-loop, not human-in-the-loop. Until now the Guardian\n-approval required for high/critical-risk merges (guardian-policy.json, ADR\n+approval required for high/critical-risk merges (guardian-policy.json, SADR\n governance) recognized only human authority sources \u2014 a GitHub review approval,\n the `guardian:approved` label applied by a configured Guardian, or a comment\n carrying `Guardian approval granted` plus the current head SHA.\n@@ -89,7 +89,7 @@\n   autonomous design the operator intends and produced the impersonation\n   incident.\n - **Record AI approval as an operator comment**: rejected as forgery of human\n-  authority \u2014 the exact failure this ADR corrects.\n+  authority \u2014 the exact failure this SADR corrects.\n - **Deliver the artifact as a committed `.shiki/` file**: rejected; a committed\n   approval inside the PR being approved is forgeable by anyone who can push.\n - **A dedicated reviewer bot identity instead of a relayed comment**: deferred;\ndiff --git a/docs/adr/0011-autonomous-quality-gate-evidence.md b/docs/adr/SADR-0011-autonomous-quality-gate-evidence.md\nindex bca0246c5d46e44afbde15486a75eec425fc02f5..9c93eac16d8c3a6d4b11bd7224e3955102cd7db4\n--- a/docs/adr/0011-autonomous-quality-gate-evidence.md\n+++ b/docs/adr/SADR-0011-autonomous-quality-gate-evidence.md\n@@ -1,4 +1,4 @@\n-# ADR 0011: Autonomous Quality-Gate Evidence \u2014 Deterministic Facts And An Independent Verifier\n+# SADR-0011: Autonomous Quality-Gate Evidence \u2014 Deterministic Facts And An Independent Verifier\n \n ## Status\n \ndiff --git a/docs/adr/0012-autonomous-loop-closeout-to-main.md b/docs/adr/SADR-0012-autonomous-loop-closeout-to-main.md\nindex 637fad6dde4989c9c2bf0f81f676d8d29991123c..ec7d85f81030a676bcbba28b43eb365422a36eac\n--- a/docs/adr/0012-autonomous-loop-closeout-to-main.md\n+++ b/docs/adr/SADR-0012-autonomous-loop-closeout-to-main.md\n@@ -1,4 +1,4 @@\n-# 0012 \u2014 Autonomous loop pushes goal completion to main via a closeout PR\n+# SADR-0012 \u2014 Autonomous loop pushes goal completion to main via a closeout PR\n \n - Status: accepted\n - Date: 2026-06-18\ndiff --git a/docs/adr/0013-bootstrap-grants-actions-approve-pull-requests.md b/docs/adr/SADR-0013-bootstrap-grants-actions-approve-pull-requests.md\nindex b6c69d64f1c5ced346b88d6859586715b3b5bd0e..cf48ff4a6716267f539ca58edd1a852e7c7a2d64\n--- a/docs/adr/0013-bootstrap-grants-actions-approve-pull-requests.md\n+++ b/docs/adr/SADR-0013-bootstrap-grants-actions-approve-pull-requests.md\n@@ -1,4 +1,4 @@\n-# ADR 0013: Bootstrap Grants GitHub Actions Permission To Create And Approve Pull Requests\n+# SADR-0013: Bootstrap Grants GitHub Actions Permission To Create And Approve Pull Requests\n \n ## Status\n \n@@ -31,7 +31,7 @@\n security tradeoff: it lets repository automation satisfy a human-review gate. In\n a multi-maintainer repo that could let a compromised or buggy workflow\n self-approve changes. So whether bootstrap turns it on by default \u2014 and with\n-what default token scope \u2014 is an ADR-worthy decision, not an implementation\n+what default token scope \u2014 is a SADR-worthy decision, not an implementation\n detail.\n \n ## Decision\n@@ -48,7 +48,7 @@\n Specifically:\n \n - **`can_approve_pull_request_reviews` defaults to `true`.** Shiki's default\n-  posture is solo/AFK autonomous operation (ADR 0008/0009/0012), where the CCA\n+  posture is solo/AFK autonomous operation (SADR-0008/0009/0012), where the CCA\n   Review Bridge is the only path that can satisfy `required_review: true` after\n   the CCA and Guardian gates have already passed. The default makes a\n   freshly-bootstrapped repo able to run the autonomous loop end to end without a\ndiff --git a/docs/adr/0014-codex-app-external-ai-guardian-ui-adapter.md b/docs/adr/SADR-0014-codex-app-external-ai-guardian-ui-adapter.md\nindex 238db06279334caa341c117362df9271382a7fcd..65f12e9939321e1b3e389a34c822fc1549e5155d\n--- a/docs/adr/0014-codex-app-external-ai-guardian-ui-adapter.md\n+++ b/docs/adr/SADR-0014-codex-app-external-ai-guardian-ui-adapter.md\n@@ -1,4 +1,4 @@\n-# ADR 0014: Use Codex App As The External AI Guardian UI Adapter\n+# SADR-0014: Use Codex App As The External AI Guardian UI Adapter\n \n ## Status\n \ndiff --git a/docs/adr/0015-contract-approval-before-dispatch.md b/docs/adr/SADR-0015-contract-approval-before-dispatch.md\nindex fad6c656741a20a7684ba16080a7db159f63a52f..6061271a46d3f1d6ae7174477de3fb75673123a6\n--- a/docs/adr/0015-contract-approval-before-dispatch.md\n+++ b/docs/adr/SADR-0015-contract-approval-before-dispatch.md\n@@ -1,4 +1,4 @@\n-# ADR 0015: Approve The Task Contract Before Dispatch, Not The Diff After It\n+# SADR-0015: Approve The Task Contract Before Dispatch, Not The Diff After It\n \n ## Status\n \n@@ -9,7 +9,7 @@\n Guardian approval for high/critical risk is expressible only on a pull request.\n Every approval source in `guardian-policy.json` \u2014 GitHub review, the\n `guardian:approved` label, the `Guardian approval granted` comment, and the\n-`external_ai_guardian_review` artifact (ADR 0010/0014) \u2014 is bound to a PR, and\n+`external_ai_guardian_review` artifact (SADR-0010/0014) \u2014 is bound to a PR, and\n three of the four require the current head SHA. Approval therefore cannot exist\n before an implementation PR exists.\n \n@@ -81,7 +81,7 @@\n An envelope breach must surface only on the non-repairable policy gate, never on\n the repairable `MergeGate metadata check`. Otherwise the Goal Loop routes it to\n `dispatch_repair` \u2014 instructing a runner to make the breach go away \u2014 which is\n-exactly what this ADR forbids.\n+exactly what this SADR forbids.\n \n **Contract immutability.** After registration, an implementation PR must not\n change the governance fields of its own task contract (`scope`, `non_goals`,\n@@ -116,7 +116,7 @@\n fails any verdict that is not `complete`, and the signal the CCA prompt reads is\n computed from `evaluate_guardian_approval` alone. Without the OR-merge at the\n signal, a contract-approved PR still returns `needs_guardian`, the CCA job fails,\n-and the MergeGate policy check never runs \u2014 the gate this ADR changes would never\n+and the MergeGate policy check never runs \u2014 the gate this SADR changes would never\n be reached. The CCA *prompt* needs no change: a `contract_approval` entry in the\n signal's existing `sources` array satisfies its current wording.\n \n@@ -130,7 +130,7 @@\n written, and an approved contract lets its implementation PRs merge through the\n same automatic path as low/medium risk. The wasted implement-then-block cycle\n disappears. Guardian review shifts from reading diffs to reading contracts, which\n-is both earlier and smaller; the external AI Guardian path (ADR 0010/0014) works\n+is both earlier and smaller; the external AI Guardian path (SADR-0010/0014) works\n on contracts with no change.\n \n We accept that the Guardian authorizes a class of change without seeing its\n@@ -146,7 +146,7 @@\n adversarial tests for the new approval path equivalent in rigor to\n `test_shiki_governance_evidence.sh`.\n \n-Bootstrap costs, which apply only while this ADR is being implemented. The tasks\n+Bootstrap costs, which apply only while this SADR is being implemented. The tasks\n that build Contract Approval merge under the existing rules, so the ones\n dispatched after the pre-dispatch gate lands but before the approval path lands\n need a Guardian-approved Contract PR *and* a live Guardian approval on their\n@@ -210,7 +210,7 @@\n risk; they do not bound authorized work.\n \n **Auto-escalate risk on envelope breach instead of stopping.** Returns the\n-workflow to a diff-time Guardian decision \u2014 the exact behavior this ADR removes.\n+workflow to a diff-time Guardian decision \u2014 the exact behavior this SADR removes.\n \n **Resolve governance fields from the base snapshot only.** Rejected against the\n registration flow: implementation PRs add their own contract, so the base has no\ndiff --git a/docs/adr/0016-locks-bound-product-paths.md b/docs/adr/SADR-0016-locks-bound-product-paths.md\nindex b5e850f3f1f7f4ef6e7f9d1763a29a51bd300c45..46b40a9b99a7aae644c02bb458f5473ff0a90ce2\n--- a/docs/adr/0016-locks-bound-product-paths.md\n+++ b/docs/adr/SADR-0016-locks-bound-product-paths.md\n@@ -1,4 +1,4 @@\n-# ADR 0016: Locks bound product paths; the Shiki mirror is bound by record identity\n+# SADR-0016: Locks bound product paths; the Shiki mirror is bound by record identity\n \n ## Status\n \n@@ -6,8 +6,8 @@\n \n - Date: 2026-07-28\n - Deciders: operator (mizutani-140), Claude (planner)\n-- Related: ADR 0004 (GitHub-first with a `.shiki` mirror), ADR 0009 (Spec Freeze),\n-  ADR 0012 (autonomous loop closeout to main), ADR 0015 (Contract Approval before\n+- Related: SADR-0004 (GitHub-first with a `.shiki` mirror), SADR-0009 (Spec Freeze),\n+  SADR-0012 (autonomous loop closeout to main), SADR-0015 (Contract Approval before\n   dispatch), remediation goal task R-02, the live PR #179 deadlock.\n \n ## Context\n@@ -90,11 +90,11 @@\n files, documentation files and named source modules are nameable; runtime-\n allocated ledger, runner and report ids are not \u2014 and those live in `.shiki`,\n which locks no longer govern. Existing frozen contracts are brought into line by\n-a one-time Spec Amendment per plan, which must land before ADR 0015's contract\n+a one-time Spec Amendment per plan, which must land before SADR-0015's contract\n immutability makes `locks` unwritable from the implementation PR.\n \n **Lock state is resolved from the durable side.** MergeGate resolves the lock\n-registry, and ADR 0015's governance fields, from the base `.shiki` snapshot when\n+registry, and SADR-0015's governance fields, from the base `.shiki` snapshot when\n present, falling back to head only when absent. The `MergeGate metadata check`\n job is given `--base-shiki`, which it does not receive today. Locks are not\n derived from the live open-pull-request set: a pull request body is untrusted\n@@ -108,7 +108,7 @@\n Explicitly out of scope: moving MergeGate's contract resolution off the PR\n checkout wholesale; making the mirror a projection of GitHub state; introducing\n new lock kinds; moving ledger evidence out of the repository; and unwinding\n-ADR 0012's closeout pull request. Registration of multi-task goals is ADR 0015's\n+SADR-0012's closeout pull request. Registration of multi-task goals is SADR-0015's\n Contract PR and is not re-decided here.\n \n ## Consequences\n@@ -127,7 +127,7 @@\n \n Risks accepted. First, goal-scoped identity is only as strong as `goal_id`,\n which is resolved from the PR body and the head task file; the mitigation is to\n-add `goal_id` to ADR 0015's frozen governance field set, and until that lands a\n+add `goal_id` to SADR-0015's frozen governance field set, and until that lands a\n task file can name a foreign goal. Second, base-when-present resolution is\n unavailable on fork pull requests, where the head is contributor-controlled;\n fork pull requests already cannot complete a Shiki merge, and this decision does\n@@ -139,7 +139,7 @@\n path, not by implementation pull requests.\n \n New obligations: one Spec Amendment per frozen plan to rewrite `locks`, ordered\n-before ADR 0015 T1; extension of the adversarial suites in\n+before SADR-0015 T1; extension of the adversarial suites in\n `scripts/test_shiki_adversarial_state.sh` and\n `scripts/test_shiki_governance_evidence.sh` to cover the eleven newly-scoped\n directories; and updating `scripts/test_shiki_loop_lock_guard.sh`,\n@@ -151,32 +151,32 @@\n whether the loop's transitive sync of ledger `evidence` references can be\n deleted once the Contract PR carries its own source plan.\n \n-## Relationship to ADR 0004\n+## Relationship to SADR-0004\n \n-This ADR **clarifies something ADR 0004 left unspecified**. ADR 0004 decides that\n+This SADR **clarifies something SADR-0004 left unspecified**. SADR-0004 decides that\n GitHub is the operational source of truth and that each target repository keeps a\n `.shiki/` mirror. It says nothing about pull requests writing the mirror, does\n not name MergeGate, and does not make the mirror an input to a gate. Nothing in\n-ADR 0004's Decision or Consequences is amended, superseded or weakened: the\n+SADR-0004's Decision or Consequences is amended, superseded or weakened: the\n mirror still exists, still records the same artifacts, and pull request\n-boundaries remain the coordination surface. What this ADR settles is the question\n+boundaries remain the coordination surface. What this SADR settles is the question\n 0004 never reached \u2014 by what authority a pull request may write a mirror record \u2014\n and it answers it the way `.shiki/manifest.json` already declares\n (`pr_mutation: current-task-or-current-goal-only`).\n \n-It **amends ADR 0012**, whose Decision item 5 states that \"Loop-executed tasks\n+It **amends SADR-0012**, whose Decision item 5 states that \"Loop-executed tasks\n declare the `path:.shiki/**` lock, which covers every `.shiki` file the closeout\n stages ... so `files_outside_locks` is satisfied without per-file lock\n bookkeeping.\" That clause is replaced: the closeout's mirror writes are\n-authorized by record identity, not by a declared lock. ADR 0012's substantive\n+authorized by record identity, not by a declared lock. SADR-0012's substantive\n decision \u2014 that completion reaches `main` through a closeout pull request rather\n than local attestation \u2014 is preserved unchanged.\n \n-It is **complementary to ADR 0015**, not a substitute. ADR 0015 owns contract\n+It is **complementary to SADR-0015**, not a substitute. SADR-0015 owns contract\n immutability, the pre-dispatch gate, the Contract PR and the Scope Envelope. This\n-ADR owns what a lock means and who may write the mirror. The one ordering\n+SADR owns what a lock means and who may write the mirror. The one ordering\n constraint between them is that the lock-narrowing Spec Amendment must precede\n-ADR 0015 T1's freezing of the `locks` field.\n+SADR-0015 T1's freezing of the `locks` field.\n \n ## Alternatives Considered\n \n@@ -186,7 +186,7 @@\n the only writer that can register them may run. Migrating the Guardian signal to\n that store makes every registration PR fail closed to `needs_guardian` at every\n risk level, because the task file is by definition absent from the store, which\n-removes the low/medium autonomous path ADR 0008/0009 exists to provide. And it\n+removes the low/medium autonomous path SADR-0008/0009 exists to provide. And it\n leaves schemas, manifest, config, guardian policy, templates, migrations, plans,\n worktrees, runner records, runs and handoffs with no writer at all in a\n repository whose product is `.shiki`. Measured benefit on the frozen backlog:\n@@ -199,14 +199,14 @@\n namespace is not in fact ownership-partitioned \u2014 56 ledger entries carry no\n `task_id`, and one goal DAG is writable by 35 registered tasks. The correct\n expression of its insight is that locks do not govern the mirror at all, which is\n-what this ADR decides.\n+what this SADR decides.\n \n **Make the mirror a projection written only by a registration writer and a\n post-merge projection job.** Rejected. It requires a bypass actor with write\n access to the protected default branch; it makes its own master invariant\n readable from a manifest that pull requests may edit; it deletes both reconcile\n modes and leaves a corrupted mirror with no in-band repair path, contradicting\n-ADR 0004's consequence that mirror conflicts are repaired against GitHub state;\n+SADR-0004's consequence that mirror conflicts are repaired against GitHub state;\n and its content-derived ids are rejected by `validate_shiki`'s currently-green id\n pattern.\n \ndiff --git a/docs/adr/0017-bookkeeping-closeouts-do-not-inherit-risk.md b/docs/adr/SADR-0017-bookkeeping-closeouts-do-not-inherit-risk.md\nindex 554e3a0d526a72ca067108c5464cc68c3edcf174..b9676857a4713850e063af7c39c8e0b01ecb4c66\n--- a/docs/adr/0017-bookkeeping-closeouts-do-not-inherit-risk.md\n+++ b/docs/adr/SADR-0017-bookkeeping-closeouts-do-not-inherit-risk.md\n@@ -1,4 +1,4 @@\n-# ADR 0017: A Bookkeeping Closeout Does Not Inherit Its Task's Risk\n+# SADR-0017: A Bookkeeping Closeout Does Not Inherit Its Task's Risk\n \n ## Status\n \n@@ -6,7 +6,7 @@\n \n ## Context\n \n-ADR 0012 made the goal loop push a task's completion to the default branch through a\n+SADR-0012 made the goal loop push a task's completion to the default branch through a\n closeout pull request: after the implementation PR merges, the loop opens a second PR\n carrying `task.status = done`, `lock.state = released`, and \u2014 when the task completes\n its goal \u2014 `goal.status = complete` plus one scorecard report. Nothing else. The\n@@ -49,14 +49,14 @@\n 4. The task file's governance fields (`scope`, `non_goals`, `required_skills`,\n    `risk_level`, `locks`, `acceptance_checks`, `test_command`) are byte-identical to\n    the base snapshot. Only `status`, `expected_pr`, `closeout_pr`, `ledger_evidence`\n-   and `expected_branch` may differ \u2014 the ADR 0015 contract-immutability rule,\n+   and `expected_branch` may differ \u2014 the SADR-0015 contract-immutability rule,\n    unchanged.\n \n    `expected_branch` is in that set because a closeout is *by construction* on a\n    different branch from the implementation it closes, and the loop rewrites the\n    field accordingly. It is safe to let it differ because MergeGate independently\n    requires `expected_branch` to equal the PR's actual head ref, so a PR cannot set\n-   it to anything other than the branch it is really on. The first draft of this ADR\n+   it to anything other than the branch it is really on. The first draft of this SADR\n    omitted it, which made the exemption unreachable: every real closeout failed\n    condition 4 on a field the loop always changes. See PR #209.\n 5. The state transitions are exactly the terminal set: task `review` \u2192 `done`, lock\n@@ -113,12 +113,12 @@\n classification must be derived, not asserted.\n \n **Fold the closeout into the implementation PR** so no second PR exists. Rejected under\n-ADR 0012, which recorded why: a task file that is already `done` on the implementation\n+SADR-0012, which recorded why: a task file that is already `done` on the implementation\n branch short-circuits the loop's review \u2192 CCA \u2192 merge \u2192 done ordering into a local\n goal-completion before any merge happens.\n \n **Have the loop auto-merge closeouts with elevated credentials.** Rejected outright: that\n-is a runtime authoring its own approval, which ADR 0010 and the constitution's\n+is a runtime authoring its own approval, which SADR-0010 and the constitution's\n approval-record prohibition forbid. It would also be indistinguishable, in the evidence,\n from the forgery those rules exist to prevent.\n \ndiff --git a/docs/adr/0018-carried-guardian-approval-across-a-proven-base-sync.md b/docs/adr/SADR-0018-carried-guardian-approval-across-a-proven-base-sync.md\nindex 22358562da390ed8b45cd600da08a43d378b4680..e6e2907cefa776460af73ddf7d39db605f3c39b0\n--- a/docs/adr/0018-carried-guardian-approval-across-a-proven-base-sync.md\n+++ b/docs/adr/SADR-0018-carried-guardian-approval-across-a-proven-base-sync.md\n@@ -1,4 +1,4 @@\n-# ADR 0018: Carried Guardian Approval Across A Proven Base Sync\n+# SADR-0018: Carried Guardian Approval Across A Proven Base Sync\n \n ## Status\n \n@@ -34,7 +34,7 @@\n Guardian evaluator that consumes it (`shiki_guardian.evaluate_guardian_approval`,\n the `guardian_comment_carried` source) were built and tested in isolation. Neither\n does anything until a caller passes the flags that turn the carry on, so the\n-feature was invisible in CI. This ADR records the decision the two of them\n+feature was invisible in CI. This SADR records the decision the two of them\n implement, and this task wires it into the four CI invocations that judge Guardian\n approval.\n \ndiff --git a/docs/adr/README.md b/docs/adr/README.md\nindex c70e057a4520c36e042ab68c53cd4a21ef709b71..5da43856836e2ce6a150f8127766e2f88355ab4b\n--- a/docs/adr/README.md\n+++ b/docs/adr/README.md\n@@ -1,40 +1,41 @@\n-# Architecture Decision Records\n+# Decision Record Namespaces\n \n-This directory holds Shiki's Architecture Decision Records (ADRs): durable\n-records of hard-to-reverse platform decisions. ADRs are part of the Shiki source\n-of truth alongside GitHub state, the `.shiki/` mirror, and `CONTEXT.md`.\n+This directory holds two independent decision namespaces:\n \n-## How ADRs Work\n+- Shiki Architecture Decision Records (SADRs), named\n+  `SADR-NNNN-<short-slug>.md`, record hard-to-reverse Shiki platform decisions.\n+- Target Architecture Decision Records (ADRs), named\n+  `NNNN-<short-slug>.md`, belong to the target product.\n \n-- New ADRs are added as `docs/adr/NNNN-<short-slug>.md`, where `NNNN` is the next\n-  zero-padded sequence number.\n-- The **canonical list of ADRs is the set of files matching `docs/adr/*.md`** in\n-  this directory. Browse `docs/adr/*.md` for the authoritative, current set.\n-- Use [`template.md`](template.md) as the starting point for a new ADR.\n-- Record an ADR when a decision is hard to reverse, surprising without context,\n-  and a real tradeoff. Do not write an ADR for routine or easily reversible\n-  choices.\n+The same four-digit number may appear once in each namespace. A platform\n+`SADR-NNNN` citation resolves only to `SADR-NNNN-*.md`; a target `ADR NNNN`\n+citation resolves only to `NNNN-*.md`.\n \n-Using the `docs/adr/*.md` glob as the source of truth means other PRs can add an\n-ADR without editing this index file, avoiding merge contention on a\n-hand-maintained enumeration.\n+## How Decision Records Work\n \n-## Current ADRs (snapshot)\n-\n-The list below is a convenience snapshot and may lag behind the directory. When\n-in doubt, list `docs/adr/*.md`.\n+- New Shiki platform decisions use the next zero-padded SADR number.\n+- New target product decisions use the target's own ADR sequence.\n+- The canonical list is the files in `docs/adr/`; no hand-maintained index is\n+  authoritative.\n+- Use [`template.md`](template.md) as the starting point and choose the filename\n+  namespace that owns the decision.\n+- Record a decision when it is hard to reverse, surprising without context, and\n+  a real tradeoff. Do not write a decision record for routine or easily\n+  reversible choices.\n \n-- [0001 \u2014 Keep Shiki As The Platform Name](0001-keep-shiki-as-platform-name.md)\n-- [0002 \u2014 Use AGENTS As The Shared Agent Constitution](0002-use-agents-as-shared-constitution.md)\n-- [0003 \u2014 Require Skill Gate For MergeGate Readiness](0003-require-skill-gate-for-mergegate-readiness.md)\n-- [0004 \u2014 Use GitHub First With A Repository-Local Shiki Mirror](0004-use-github-first-with-shiki-mirror.md)\n-- [0005 \u2014 Use Codex As Front And Claude Code Action As The GitHub Runtime](0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md)\n-- [0006 \u2014 Separate Codex Implementation, CCA Completion, And MergeGate Authorization](0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md)\n+Using the directory as the source of truth lets independent changes add records\n+without contending on an enumerated index.\n \n-## Adding A New ADR\n+## Adding A Shiki SADR\n \n-1. Copy `template.md` to `docs/adr/NNNN-<short-slug>.md`.\n+1. Copy `template.md` to `docs/adr/SADR-NNNN-<short-slug>.md`.\n 2. Fill in the title, status, context, decision, and consequences.\n-3. Set the status to `Proposed`, then `Accepted` once the decision is approved.\n-4. Open a PR following `CONTRIBUTING.md`. You do not need to edit this index for\n-   the ADR to be discoverable via `docs/adr/*.md`.\n+3. Set the status to `Proposed`, then `Accepted` once the proper authority has\n+   approved the decision.\n+4. Open a PR following `CONTRIBUTING.md`.\n+\n+## Adding A Target ADR\n+\n+1. Copy `template.md` to `docs/adr/NNNN-<short-slug>.md` using the target's own\n+   sequence.\n+2. Cite it as `ADR NNNN`; do not prefix its filename with `SADR-`.\ndiff --git a/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md b/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\nnew file mode 100644\nindex 0000000000000000000000000000000000000000..7455ce1f3bed02ddddb851ac21a1d1a648c9aeba\n--- /dev/null\n+++ b/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\n@@ -0,0 +1,55 @@\n+# SADR-0019: Separate Shiki Platform SADRs From Target ADRs\n+\n+## Status\n+\n+Accepted\n+\n+## Context\n+\n+Shiki installs its platform decision records into `docs/adr/` in every target\n+repository. Shiki and its targets previously used the same numeric\n+`NNNN-<slug>.md` namespace. A target that already owned ADRs 0013 through 0018\n+therefore acquired two records for each number during installation, making an\n+`ADR NNNN` citation ambiguous. Renumbering target decisions would rewrite target\n+history, while renumbering Shiki decisions per target would destroy their stable\n+platform identity.\n+\n+Upgrades also need to remove the former Shiki-owned numeric paths without ever\n+mistaking a target-owned ADR for platform content. The install stamp already\n+records exact shipped paths and content digests, so it can prove ownership only\n+when both still match.\n+\n+## Decision\n+\n+Shiki platform decisions use the `SADR` namespace:\n+\n+- records are named `docs/adr/SADR-NNNN-<slug>.md`;\n+- live platform citations use `SADR-NNNN`;\n+- target product decisions retain `docs/adr/NNNN-<slug>.md` and `ADR NNNN`;\n+- the reference validator resolves the two namespaces independently, so the same\n+  four-digit number may exist once in each namespace.\n+\n+Before a forced upgrade writes anything, the installer inventories the exact\n+former numeric Shiki ADR paths. It may delete a legacy path only when the\n+existing install stamp contains that exact path and its recorded SHA-256 digest\n+matches the file currently on disk. All ownership blockers are reported\n+together and abort the upgrade before its first write. A non-forced install\n+never performs this cleanup.\n+\n+Historical `.shiki/` evidence remains unchanged because it records the namespace\n+that existed when that evidence was produced.\n+\n+## Consequences\n+\n+- Targets may use any ADR number without colliding with Shiki's platform SADRs.\n+- Fresh installs preserve target ADR bytes and install a stable SADR set.\n+- Forced upgrades fail closed when legacy ownership cannot be proven, and a\n+  successful upgrade refreshes the stamp without removed legacy paths.\n+- Current governance, packaging, workflow, prompt, script, and skill references\n+  must move with the platform records.\n+\n+## Alternatives Considered\n+\n+- Collision-only refusal leaves affected targets permanently unupgradable.\n+- Renumbering target ADRs mutates target-owned history.\n+- Per-target Shiki renumbering makes platform citations non-portable.\ndiff --git a/docs/adr/template.md b/docs/adr/template.md\nindex a92a97daddd0b00ab9dd626cc8d86942da2eafed..17dd79175b5fee2105935557f691b40cf9492942\n--- a/docs/adr/template.md\n+++ b/docs/adr/template.md\n@@ -1,14 +1,14 @@\n-# NNNN: <Short Decision Title>\n+# <SADR-NNNN or NNNN>: <Short Decision Title>\n \n ## Status\n \n-Proposed | Accepted | Superseded by ADR-NNNN\n+Proposed | Accepted | Superseded by SADR-NNNN or target ADR NNNN\n \n ## Context\n \n What is the situation, constraint, or force that requires a decision? Reference\n-relevant domain terms (`CONTEXT.md`), prior ADRs, and code reality. State the\n-problem, not the solution.\n+relevant domain terms (`CONTEXT.md`), prior Shiki SADRs or target ADRs, and code\n+reality. State the problem, not the solution.\n \n ## Decision\n \ndiff --git a/docs/agents/autonomous-loop-e2e-acceptance.md b/docs/agents/autonomous-loop-e2e-acceptance.md\nindex bbf84c6081987f159ac6ad9ecf3c82b7aa72e7e8..5c13c1d02a05a90b0dc46cbef7c95a9846d0c237\n--- a/docs/agents/autonomous-loop-e2e-acceptance.md\n+++ b/docs/agents/autonomous-loop-e2e-acceptance.md\n@@ -40,8 +40,8 @@\n 3. `gh auth status` is logged in with merge permission on the repo.\n 4. Branch protection + required checks are configured exactly as the goal-loop\n    required-check set expects (see `docs/agents/branch-protection-smoke.md`).\n-5. The repo `.shiki/policy` permits autonomous low/medium-risk merges (ADR\n-   0008/0009); the MergeGate policy check is the recorded-authority gate.\n+5. The repo `.shiki/policy` permits autonomous low/medium-risk merges\n+   (SADR-0008/0009); the MergeGate policy check is the recorded-authority gate.\n \n The operator's only actions are: register the task and start the loop. **No\n manual pre-clearing** means after `shiki loop run` starts, the operator does not\ndiff --git a/docs/agents/bootstrap-command.md b/docs/agents/bootstrap-command.md\nindex 05f7f203a52cf412b9f97084a6e131ed0f785afd..f7aa0b734dfebee4a3b139335b66ee3415f48713\n--- a/docs/agents/bootstrap-command.md\n+++ b/docs/agents/bootstrap-command.md\n@@ -189,7 +189,9 @@\n ```\n \n Use this only for tests, fixtures, or explicit local-only template inspection.\n-Use `--force` only when you intentionally want to overwrite existing target files.\n+Use `--force` only when you intentionally want to refresh the target template.\n+Legacy numeric Shiki decision cleanup is stamp-and-digest authorized before any\n+write; target ADR paths are not cleanup candidates.\n \n ## Slash Command\n \ndiff --git a/docs/agents/checklists.md b/docs/agents/checklists.md\nindex 4a7f03ade346f53a1c1d82981f3fd744f9ab4547..0c5ec6c53dc7197b7b7a7d18e667b325babd6c45\n--- a/docs/agents/checklists.md\n+++ b/docs/agents/checklists.md\n@@ -32,18 +32,18 @@\n | ID | Item | Blocking |\n | --- | --- | --- |\n | GD-01 | Plan was challenged against `CONTEXT.md`. | blocking for non-trivial Goals |\n-| GD-02 | Relevant ADRs were read or absence was recorded. | blocking for non-trivial Goals |\n+| GD-02 | Relevant SADRs and target ADRs were read or absence was recorded. | blocking for non-trivial Goals |\n | GD-03 | Ambiguous terms were resolved or converted to HITL blockers. | blocking |\n | GD-04 | Edge scenarios were considered. | advisory |\n | GD-05 | Code reality was checked when code could answer. | blocking when applicable |\n | GD-06 | New domain terms were added to `CONTEXT.md` when needed. | blocking when applicable |\n-| GD-07 | ADR was created or explicitly skipped using the ADR criteria. | blocking when applicable |\n+| GD-07 | A SADR or target ADR was created or explicitly skipped using the decision-record criteria. | blocking when applicable |\n \n ## CI \u2014 Context & Impact Checklist\n \n | ID | Item | Blocking |\n | --- | --- | --- |\n-| CI-01 | Relevant docs and ADRs identified. | blocking |\n+| CI-01 | Relevant docs, SADRs, and target ADRs identified. | blocking |\n | CI-02 | Relevant modules, interfaces, seams, callers, and tests identified. | blocking |\n | CI-03 | Dependencies and blocked-by relationships identified. | blocking |\n | CI-04 | Candidate locks identified. | blocking |\n@@ -62,7 +62,7 @@\n | PRD-04 | Implementation decisions are recorded without volatile code detail. | blocking |\n | PRD-05 | Testing decisions are recorded. | blocking |\n | PRD-06 | Out-of-scope items are explicit. | blocking |\n-| PRD-07 | Relevant glossary/ADR links are present when applicable. | blocking |\n+| PRD-07 | Relevant glossary/SADR/target-ADR links are present when applicable. | blocking |\n \n ## SF \u2014 Spec Freeze Checklist\n \n@@ -98,7 +98,7 @@\n | H-02 | Branch/worktree target is present. | blocking |\n | H-03 | Scope and non-goals are present. | blocking |\n | H-04 | Dependencies and locks are present. | blocking |\n-| H-05 | Relevant docs, ADRs, modules, and tests are referenced. | blocking |\n+| H-05 | Relevant docs, SADRs, target ADRs, modules, and tests are referenced. | blocking |\n | H-06 | Required skills are stated. | blocking |\n | H-07 | TDD expectations are stated. | blocking when behavior work |\n | H-08 | Acceptance criteria are included. | blocking |\ndiff --git a/docs/agents/codex-handoff.md b/docs/agents/codex-handoff.md\nindex ede468f47cb277f69a857e8c45df703e4ed0151d..6f97bdffb67e44d7edea2c71e01ea28933c4eb6c\n--- a/docs/agents/codex-handoff.md\n+++ b/docs/agents/codex-handoff.md\n@@ -46,7 +46,7 @@\n \n ## Context\n - Domain terms:\n-- Relevant ADRs:\n+- Relevant SADRs and target ADRs:\n - Relevant docs:\n - Relevant modules/interfaces/seams:\n - Similar tests/prior art:\ndiff --git a/docs/agents/completion-check-agent.md b/docs/agents/completion-check-agent.md\nindex 13e4c035db35025f4e3cdcc5adfb24314ac4d521..61bfa1218cf36ca605250c91919e620f33238aa0\n--- a/docs/agents/completion-check-agent.md\n+++ b/docs/agents/completion-check-agent.md\n@@ -32,7 +32,7 @@\n - `AGENTS.md`\n - `CLAUDE.md`\n - `CONTEXT.md`\n-- relevant ADRs\n+- relevant SADRs and target ADRs\n - parent Goal issue\n - PRD issue or document\n - task issue\ndiff --git a/docs/agents/decision-control.md b/docs/agents/decision-control.md\nindex 7d8ee5ab03a5ccbcbd04056ae61de03785a2a9c9..8c94ecda00fffaa0e92c325ab83aa9776bd1dac1\n--- a/docs/agents/decision-control.md\n+++ b/docs/agents/decision-control.md\n@@ -11,7 +11,7 @@\n | Decision | Owner | When | Required inputs | Output | Enforcement surface |\n | --- | --- | --- | --- | --- | --- |\n | Is the Goal clear enough to plan? | Planner + Guardian when needed | Goal intake | User request, existing docs, risk signals | Goal readiness verdict | Goal checklist, issue template, ledger |\n-| Must `grill-with-docs` run? | Planner / Skill Gate | Before PRD or issue split | Ambiguity, domain terms, ADR conflicts, unclear boundaries | Skill invocation or explicit skip rationale | Skill Gate checklist |\n+| Must `grill-with-docs` run? | Planner / Skill Gate | Before PRD or issue split | Ambiguity, domain terms, SADR/target-ADR conflicts, unclear boundaries | Skill invocation or explicit skip rationale | Skill Gate checklist |\n | Is a PRD required? | Planner | After Goal context stabilizes | Goal, `grill-with-docs` output, Context & Impact output | PRD required / not required | PRD checklist, CCA evidence check |\n | Can the spec be frozen? | Operator only | End of Requirements Definition, before issue split | PRD/requirements summary, scope inventory (SF-02), open-question list | `spec_freeze` block in the plan + ledger evidence | `require_grilled_plan` (plan ingest/run), SF checklist |\n | Can a frozen spec change? | Operator only (Spec Amendment) | When implementation contradicts the frozen spec | Contested decisions, scoped re-grill result | recorded amendment + re-stamped freeze | SF checklist, ledger evidence |\ndiff --git a/docs/agents/domain.md b/docs/agents/domain.md\nindex 258da8ea81e87ac0a6b0b5b5e5c2fde980eaabb7..d57b0cf17ff3e1fa9e6e5885372bf083d8295421\n--- a/docs/agents/domain.md\n+++ b/docs/agents/domain.md\n@@ -5,7 +5,9 @@\n ## Layout\n \n - `CONTEXT.md` defines Shiki language. Keep it implementation-free.\n-- `docs/adr/` records hard-to-reverse decisions.\n+- `docs/adr/SADR-NNNN-*.md` records Shiki platform decisions.\n+- `docs/adr/NNNN-*.md` records target product decisions without sharing the\n+  SADR namespace.\n - `AGENTS.md` is the runtime-neutral agent constitution.\n - `CLAUDE.md` is the Claude Code wrapper around `AGENTS.md`.\n - `.shiki/` is the Target Repository mirror for Goals, PRDs, plans, Task DAGs, contracts, locks, worktree records, ledger entries, CCA verdicts, repair packets, reports, and handoffs.\n@@ -15,6 +17,6 @@\n \n - Use Shiki terms exactly when they exist.\n - Add glossary terms to `CONTEXT.md` only when the term is part of the domain, not a generic programming concept.\n-- Create ADRs only for hard-to-reverse, surprising, tradeoff-based decisions.\n+- Create SADRs or target ADRs only for hard-to-reverse, surprising, tradeoff-based decisions.\n - Keep product-specific language in the Target Repository, not in Shiki Template docs.\n-- If an ADR conflicts with a proposed implementation, surface the conflict before editing.\n+- If a SADR or target ADR conflicts with a proposed implementation, surface the conflict before editing.\ndiff --git a/docs/agents/execution-control.md b/docs/agents/execution-control.md\nindex ad8ee3cdb36be56fedc1f46408ca92b387d74047..bc63c3a73661d12b567016ba299f1a6b66bf9088\n--- a/docs/agents/execution-control.md\n+++ b/docs/agents/execution-control.md\n@@ -63,7 +63,7 @@\n | Branch name | Orchestrator | Orchestrator | Before implementer dispatch | branch policy | branch ref in task/ledger |\n | Lock reservation | Context & Impact | MergeGate | Before dispatch and before merge | `.shiki/locks/`, labels, PR metadata | lock id, owner, scope |\n | CI profile | Planner | MergeGate | During issue triage, rechecked at PR | labels, `.shiki/policy.yaml`, workflow matrix | required check list |\n-| Required status checks | Repo maintainer / Guardian | GitHub branch protection / rulesets | Repository setup, updated via ADR | GitHub settings | branch/ruleset config, ADR |\n+| Required status checks | Repo maintainer / Guardian | GitHub branch protection / rulesets | Repository setup, updated via target ADR | GitHub settings | branch/ruleset config, target ADR |\n | CCA judgment | CCA | GitHub required check / MergeGate | Every non-draft PR update | GitHub Action check | structured CCA verdict |\n | Repair assignment | CCA | MergeGate | After failed CCA/CI/review | repair packet, issue/PR comment | repair id and attempt count |\n | Merge readiness | MergeGate | GitHub branch protection / Guardian if needed | After CCA complete | required checks, review rules, rulesets | mergegate check output |\ndiff --git a/docs/agents/guardian-policy.md b/docs/agents/guardian-policy.md\nindex 4147c7adf05a47adb4fd24ada1a1c2e1f2ba6d2d..a20d9cc6c00a569bb057b9ca8d6b51c7fc4a9847\n--- a/docs/agents/guardian-policy.md\n+++ b/docs/agents/guardian-policy.md\n@@ -98,8 +98,8 @@\n `origin/<default_branch>` and reuses each job's existing base fetch; a shallow\n (`--depth`) or second base fetch would make the proof unresolvable and silently\n disable the carry, so neither is permitted. CCA reads `guardian_comment_carried` as\n-a recorded authority in the deterministic signal and does not re-derive it. See ADR\n-0018 for the full decision, the rejected diff/patch-equality and path-disjointness\n+a recorded authority in the deterministic signal and does not re-derive it. See\n+SADR-0018 for the full decision, the rejected diff/patch-equality and path-disjointness\n alternatives, and the accepted residual.\n \n ## External AI Guardian Review (SADR-0010)\ndiff --git a/docs/agents/implementation-policy.md b/docs/agents/implementation-policy.md\nindex c754144a9fb5bf4a38aa82a60e1670c59e626b44..85c37281bcad196c6b7b02343faf00b03cd9c57a\n--- a/docs/agents/implementation-policy.md\n+++ b/docs/agents/implementation-policy.md\n@@ -31,10 +31,13 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n ## Non-Negotiables\n \n 1. **Planning precedes implementation.** Non-trivial Goals start with `grill-with-docs` and Context & Impact.\n-2. **Domain language is controlled.** Terms must be checked against `CONTEXT.md`; hard-to-reverse tradeoffs must be checked against ADRs.\n+2. **Domain language is controlled.** Terms must be checked against `CONTEXT.md`; hard-to-reverse tradeoffs must be checked against Shiki SADRs and target ADRs.\n 3. **PRD precedes issue decomposition.** `to-prd` is used once the context is settled enough to become durable intent.\n 4. **Issues are vertical slices.** `to-issues` must produce independently grabbable tracer-bullet tasks, not horizontal layer tickets.\n 5. **Triage controls dispatch.** Only `ready-for-agent` / AFK tasks may be assigned to Codex without human input.\n@@ -95,19 +98,19 @@\n Process:\n \n - Challenge terms against `CONTEXT.md`.\n-- Check ADRs before reopening decisions.\n+- Check SADRs and target ADRs before reopening decisions.\n - Inspect code when code can answer a question.\n - Ask one question at a time when the operator is available.\n - Provide a recommended answer with each question.\n - Record resolved terms in `CONTEXT.md`.\n-- Create ADRs only for hard-to-reverse, surprising, tradeoff-based decisions.\n+- Create SADRs or target ADRs only for hard-to-reverse, surprising, tradeoff-based decisions.\n - Convert unresolved load-bearing questions into HITL blockers.\n \n Outputs:\n \n - Resolved design-tree decisions.\n - Updated glossary terms when needed.\n-- ADRs when justified.\n+- SADRs or target ADRs when justified.\n - HITL blockers when decisions remain unresolved.\n \n ## Phase 3 \u2014 Context & Impact\n@@ -117,7 +120,7 @@\n \n Outputs must include:\n \n-- Relevant documents and ADRs.\n+- Relevant documents, SADRs, and target ADRs.\n - Relevant modules, interfaces, seams, callers, and tests.\n - Candidate locks.\n - Dependencies.\n@@ -140,7 +143,7 @@\n - Testing decisions.\n - Out of scope.\n - Further notes.\n-- Links to relevant Goal, domain terms, and ADRs.\n+- Links to relevant Goal, domain terms, SADRs, and target ADRs.\n \n Phase 4 ends with Spec Freeze: the operator explicitly approves the PRD, the\n plan records a `spec_freeze` block (status, approved_by, source), and the SF\n@@ -240,7 +243,7 @@\n - Dependencies and locks.\n - Risk and Guardian requirements.\n - Ledger completeness.\n-- Documentation and ADR updates.\n+- Documentation and SADR/target-ADR updates.\n - Scope drift.\n \n CCA emits one of:\n@@ -301,7 +304,7 @@\n | Evidence failure | Behavior might be done, but proof is missing. | Add checks, PR evidence, ledger entry, or artifacts. |\n | Scope drift | PR changes unrelated behavior. | Revert unrelated changes or split into new Goal. |\n | Dependency block | Upstream task or lock not complete. | Block until dependency resolves. |\n-| Architecture block | Decision needs ADR or deep-module discussion. | `grill-with-docs` or `improve-codebase-architecture`. |\n+| Architecture block | Decision needs a SADR, target ADR, or deep-module discussion. | `grill-with-docs` or `improve-codebase-architecture`. |\n | Risk block | Security/production/policy change needs human approval. | Guardian decision. |\n \n ## Phase 5.5 \u2014 Execution Decision Control\ndiff --git a/docs/agents/runtime-auth-model.md b/docs/agents/runtime-auth-model.md\nindex 6a2e1a47e81ea89b5c68a210ec07772a9374babc..3c54748d24233dbd0ba8473950282547ef4ce935\n--- a/docs/agents/runtime-auth-model.md\n+++ b/docs/agents/runtime-auth-model.md\n@@ -148,7 +148,7 @@\n \n API-key based runners may be added by a target repository as an explicit extension. They are not the default Shiki template path.\n \n-If a repo opts into API-key mode, record the exception in an ADR and update the repo's `AGENTS.md`, `.shiki/config.yaml`, and workflow permissions.\n+If a repo opts into API-key mode, record the target-specific exception in a target ADR and update the repo's `AGENTS.md`, `.shiki/config.yaml`, and workflow permissions.\n \n ## GitHub Actions Runtime Compatibility\n \ndiff --git a/docs/agents/skill-gate.md b/docs/agents/skill-gate.md\nindex c50398b0d7c60a8598f2a5c88a10015730dced13..3acffe6444617bb1d54b77d4b4708158ec735bc6\n--- a/docs/agents/skill-gate.md\n+++ b/docs/agents/skill-gate.md\n@@ -9,7 +9,7 @@\n | Skill | Trigger | Expected output |\n | --- | --- | --- |\n | `setup-matt-pocock-skills` | First configuring a repo for these skills, or when issue tracker, triage labels, or domain docs are missing. | `docs/agents/` configuration and an `Agent skills` block in `AGENTS.md` or `CLAUDE.md`. |\n-| `grill-with-docs` | Ambiguous plan, unclear terminology, boundary decision, tradeoff, or ADR-worthy design choice. | Clarified decision tree, recommended answers, updates to `CONTEXT.md` or ADRs when decisions crystallize. |\n+| `grill-with-docs` | Ambiguous plan, unclear terminology, boundary decision, tradeoff, or decision-record-worthy design choice. | Clarified decision tree, recommended answers, updates to `CONTEXT.md`, SADRs, or target ADRs when decisions crystallize. |\n | `zoom-out` | Agent lacks a higher-level architectural map. | Map of relevant modules, callers, seams, and domain concepts. |\n | `to-prd` | Settled context should become a PRD. | PRD published to the issue tracker with problem, solution, user stories, decisions, testing, and out-of-scope notes. |\n | `to-issues` | Goal, PRD, or plan must become implementation tickets. | Vertical-slice issues with acceptance criteria, dependencies, and AFK/HITL classification. |\n@@ -17,7 +17,7 @@\n | `tdd` | Feature work or bug fix has observable behavior that can be specified with tests. | Red-green-refactor loop through public interfaces, one vertical slice at a time. |\n | `code-review` | The TDD loop is green and the implementer is about to create or update a PR. Default for every implementation task. | Pre-PR self-review of the task diff with fixes applied on the task branch; findings and resolutions recorded as a ledger entry naming the skill and a `## Pre-PR code review` PR body section. |\n | `diagnose` | Hard bug, failing check, regression, flaky behavior, or performance problem. | Reproduction loop, ranked hypotheses, instrumentation, fix, regression test, post-mortem. |\n-| `improve-codebase-architecture` | Structural friction, testability, AI-navigability, deep-module opportunity, or refactor request. | Architecture candidates and deepening recommendations informed by `CONTEXT.md` and ADRs. |\n+| `improve-codebase-architecture` | Structural friction, testability, AI-navigability, deep-module opportunity, or refactor request. | Architecture candidates and deepening recommendations informed by `CONTEXT.md`, SADRs, and target ADRs. |\n | `prototype` | A throwaway logic or UI prototype is needed to answer a design question. | Clearly marked disposable prototype and captured decision. |\n \n ## MergeGate Interaction\ndiff --git a/docs/operations.md b/docs/operations.md\nindex cf1f452d4c589e38ccd58553bda0ad8684b51eb7..ace48a90ffbec9e5c0a7113bef16a4858bc44b3f\n--- a/docs/operations.md\n+++ b/docs/operations.md\n@@ -89,6 +89,16 @@\n      target history, brought forward by `shiki migrate`, not a template.\n    - **Everything else** (CLI scripts, workflows, schemas, docs) is overwritten.\n \n+   A forced upgrade also migrates the former numeric Shiki decision paths to\n+   `SADR-NNNN-*`. Before the first write, the installer inventories the exact\n+   legacy Shiki paths and authorizes each deletion only when the existing\n+   `.shiki/install-stamp.json` names that path and its SHA-256 digest still\n+   matches the file. An absent, unreadable, malformed, incomplete, or\n+   mismatching stamp reports every blocked path and stops with no filesystem\n+   change. Target-owned `NNNN-*` ADRs are not cleanup candidates. A non-force\n+   install deletes nothing and retains any still-matching legacy stamp entries\n+   so a later forced upgrade can prove ownership.\n+\n    If the target has pending migrations, `--force` refuses and writes nothing \u2014\n    apply migrations first (step 3), then re-run. The run ends with a single\n    summary of every `.new` written, so a half-upgrade is visible.\ndiff --git a/docs/releasing.md b/docs/releasing.md\nindex 10d73828e7dbd64e9d9f6b83bc84d010537ce62a..aafeee8c4d441e1e427efee2ce4f81a647f020e1\n--- a/docs/releasing.md\n+++ b/docs/releasing.md\n@@ -67,7 +67,7 @@\n CLI in `scripts/`. There are no third-party Python runtime dependencies. The CLI\n is intentionally **not** exposed as a `[project.scripts]` console entry point\n because `scripts/shiki.py` is a dependency-free shim and `scripts/` is not\n-packaged as an importable distribution (see the ADR for the rationale).\n+packaged as an importable distribution (see SADR-0007 for the rationale).\n \n Supported install paths:\n \n@@ -155,6 +155,15 @@\n    [docs/operations.md](operations.md#the-new-convention). If the target has\n    pending migrations, `--force` refuses and writes nothing; apply migrations\n    (step 2) first, then re-run.\n+\n+   The SADR namespace upgrade has an additional pre-write gate: each former\n+   numeric Shiki decision path is removed only when the existing install stamp\n+   contains that exact path and a digest matching its current bytes. The\n+   installer aggregates absent/unreadable/malformed-stamp, missing-digest, and\n+   digest-mismatch blockers before raising, so refusal leaves the target tree\n+   byte-identical. Numeric target ADRs are preserved. A successful forced\n+   upgrade installs and stamps `SADR-*` paths and drops the removed legacy paths\n+   from the refreshed stamp.\n 2. Apply repository-local `.shiki/` state migrations with the migrate command:\n \n    ```bash\ndiff --git a/docs/usage.md b/docs/usage.md\nindex b83407ee3970dd395dd893010bc5dbe8bad26051..a130ef6e40dfd8169a5bc701339351097df1a09f\n--- a/docs/usage.md\n+++ b/docs/usage.md\n@@ -67,8 +67,10 @@\n shiki install-target /path/to/target-repo --local-only\n ```\n \n-Use `--force` only when you intentionally want to overwrite existing target\n-files.\n+Use `--force` only when you intentionally want to refresh the target template.\n+For the SADR namespace migration, `--force` deletes only exact legacy Shiki\n+decision paths whose existing install-stamp digest still matches; any ownership\n+blocker stops before the first write. Numeric target ADRs are never candidates.\n \n ## `shiki bootstrap-platform` \u2014 publish the Shiki platform repo\n \ndiff --git a/scripts/guardian_approval_signal.py b/scripts/guardian_approval_signal.py\nindex d059f92b2c82aa2b5d6ca421365537a87886c1cb..16924892714f4f5b9303c0ef0e325a7e70075630\n--- a/scripts/guardian_approval_signal.py\n+++ b/scripts/guardian_approval_signal.py\n@@ -199,8 +199,8 @@\n ) -> Any:\n     \"\"\"Evaluate SADR-0015 Contract Approval for the CCA Guardian signal.\n \n-    The SAME OR that MergeGate applies must reach this deterministic signal: ADR\n-    0015 is explicit that without it, a contract-approved PR still returns\n+    The SAME OR that MergeGate applies must reach this deterministic signal:\n+    SADR-0015 is explicit that without it, a contract-approved PR still returns\n     ``needs_guardian``, the CCA job fails, and the MergeGate policy check never\n     runs \u2014 the gate this change moves would never be reached. This resolves the\n     task the SAME way MergeGate does (the first Shiki task id in the PR body),\ndiff --git a/scripts/shiki_contract_approval.py b/scripts/shiki_contract_approval.py\nindex 941bcb96fdd8b9a85bc0c4f0f04d5dad9d04839c..4a4b418f6527413161db82670bec857a043360e2\n--- a/scripts/shiki_contract_approval.py\n+++ b/scripts/shiki_contract_approval.py\n@@ -142,7 +142,7 @@\n     changed_files_status: Any,\n     registration: Any,\n ) -> ContractApprovalResult:\n-    \"\"\"Whether this PR carries an SADR-0015 Contract Approval.\n+    \"\"\"Whether this PR carries a SADR-0015 Contract Approval.\n \n     The four conditions are evaluated IN ORDER, each failure recorded as a\n     distinct reason string:\ndiff --git a/scripts/shiki_guardian.py b/scripts/shiki_guardian.py\nindex a4ecb79bbda55fd5c8b06adfae2eb99760a03c45..4dd99bbe714e26f421fa19abac7a34ae1b195ecc\n--- a/scripts/shiki_guardian.py\n+++ b/scripts/shiki_guardian.py\n@@ -12,7 +12,7 @@\n # The carry re-derives its proof from git in-process on every judging invocation;\n # it is never read from an artifact, so there is nothing to go stale or be forged.\n # shiki_sync_proof is the ONLY guardian-family module that runs git, and every one\n-# of its invocations is read-only and local (ADR: prove-from-git base sync).\n+# of its invocations is read-only and local (SADR: prove-from-git base sync).\n from shiki_sync_proof import verify_pure_base_sync\n \n GUARDIAN_POLICY_PATH = \".shiki/guardian-policy.json\"\ndiff --git a/scripts/shiki_guardian_review.py b/scripts/shiki_guardian_review.py\nindex dd0f104d457899e0ba5ff1e162011c1e90b6f92b..e10ef58c046324e7ea3a3392feb3048ca77414ce\n--- a/scripts/shiki_guardian_review.py\n+++ b/scripts/shiki_guardian_review.py\n@@ -159,7 +159,7 @@\n     docs_only = _is_docs_only(changed_files)\n     if docs_only and matched == 0:\n         focus.append(\n-            \"Docs/ADR-only change: confirm the change is genuinely documentation/decision-only, introduces no behavior or contract drift, and stays consistent with the constitution and existing ADRs.\"\n+            \"Docs/decision-record-only change: confirm the change is genuinely documentation/decision-only, introduces no behavior or contract drift, and stays consistent with the constitution and existing SADRs and target ADRs.\"\n         )\n \n     # Conservative augmentation: unknown (no category and not docs-only), mixed\ndiff --git a/scripts/shiki_installer.py b/scripts/shiki_installer.py\nindex be064ab7a29ff6836f7f7a1d70500a43e0d11d7b..9fd8eefd137ce7003dad5c0dcb2e48e2cd47ab32\n--- a/scripts/shiki_installer.py\n+++ b/scripts/shiki_installer.py\n@@ -118,6 +118,7 @@\n # what lets a target carry it without a governance-schema change.\n INSTALL_STAMP_PATH = \".shiki/install-stamp.json\"\n INSTALL_STAMP_VERSION = 1\n+LEGACY_SHIKI_SADR_MAX = 18\n \n # Under --force the shipped surface splits three ways, so nothing is lost and\n # nothing is silently stale.\n@@ -237,9 +238,98 @@\n         )\n \n \n+def legacy_shiki_adr_paths() -> tuple[str, ...]:\n+    \"\"\"Former numeric paths for Shiki SADR-0001 through SADR-0018.\n+\n+    The authoritative slugs come from the current SADR filenames. SADR-0019 is\n+    the namespace decision itself and never shipped under a numeric legacy path.\n+    \"\"\"\n+    paths: list[str] = []\n+    for sadr in sorted((ROOT / \"docs\" / \"adr\").glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\")):\n+        number = sadr.name.split(\"-\", 2)[1]\n+        if int(number) <= LEGACY_SHIKI_SADR_MAX:\n+            paths.append(f\"docs/adr/{sadr.name.removeprefix('SADR-')}\")\n+    return tuple(paths)\n+\n+\n+def preflight_legacy_sadr_cleanup(target: Path, *, force: bool) -> tuple[str, ...]:\n+    \"\"\"Authorize exact legacy Shiki ADR removals before the first write.\n+\n+    A non-force install never removes existing files. A forced install needs no\n+    stamp when no legacy Shiki path is present. When legacy paths do exist, each\n+    must be named in a readable install stamp and its current bytes must match\n+    the recorded digest.\n+    \"\"\"\n+    if not force:\n+        return ()\n+    present = tuple(\n+        relative\n+        for relative in legacy_shiki_adr_paths()\n+        if (target / relative).exists() or (target / relative).is_symlink()\n+    )\n+    if not present:\n+        return ()\n+\n+    stamp_path = target / INSTALL_STAMP_PATH\n+    try:\n+        raw = stamp_path.read_text(encoding=\"utf-8\")\n+        stamp = json.loads(raw)\n+    except FileNotFoundError:\n+        detail = \"install stamp is absent\"\n+        blockers = [f\"{relative}: {detail}\" for relative in present]\n+    except (OSError, UnicodeDecodeError) as error:\n+        detail = f\"install stamp is unreadable ({error})\"\n+        blockers = [f\"{relative}: {detail}\" for relative in present]\n+    except json.JSONDecodeError as error:\n+        detail = f\"install stamp is malformed ({error})\"\n+        blockers = [f\"{relative}: {detail}\" for relative in present]\n+    else:\n+        if not isinstance(stamp, dict):\n+            blockers = [\n+                f\"{relative}: install stamp is malformed (expected an object)\"\n+                for relative in present\n+            ]\n+        else:\n+            digests = stamp.get(\"digests\")\n+            blockers = []\n+            if not isinstance(digests, dict):\n+                blockers.extend(\n+                    f\"{relative}: install stamp has no digest for this path\"\n+                    for relative in present\n+                )\n+            else:\n+                for relative in present:\n+                    expected = digests.get(relative)\n+                    if not isinstance(expected, str) or not expected:\n+                        blockers.append(\n+                            f\"{relative}: install stamp has no digest for this path\"\n+                        )\n+                        continue\n+                    try:\n+                        actual = _sha256_file(target / relative)\n+                    except OSError as error:\n+                        blockers.append(\n+                            f\"{relative}: legacy path is unreadable ({error})\"\n+                        )\n+                        continue\n+                    if actual != expected:\n+                        blockers.append(\n+                            f\"{relative}: digest mismatch (stamp {expected}, current {actual})\"\n+                        )\n+\n+    if blockers:\n+        joined = \"\\n\".join(f\"- {blocker}\" for blocker in blockers)\n+        raise ShikiError(\n+            \"legacy Shiki ADR cleanup is not authorized:\\n\"\n+            f\"{joined}\\nNo files were written.\"\n+        )\n+    return present\n+\n+\n def install_template(target: Path, *, force: bool, validate: bool) -> list[NewFileNote]:\n     # Preconditions first: never rewrite the tree of a target that cannot be\n     # upgraded cleanly (see refuse_pending_migrations).\n+    legacy_cleanup = preflight_legacy_sadr_cleanup(target, force=force)\n     refuse_pending_migrations(target)\n \n     # Resolve the target's required CODEOWNERS owner once from its\n@@ -250,6 +340,10 @@\n     # verbatim. See ``codeowners_required_owner``.\n     codeowners_owner = codeowners_required_owner(target)\n \n+    for relative in legacy_cleanup:\n+        (target / relative).unlink()\n+        info(f\"removed stamped legacy Shiki ADR: {target / relative}\")\n+\n     notes: list[NewFileNote] = []\n     for relative in TEMPLATE_PATHS:\n         source = ROOT / relative\n@@ -647,6 +741,22 @@\n     digests = target_stamp_digests(target)\n     existing = read_install_stamp(target)\n     if existing is not None:\n+        # A non-force install deliberately leaves legacy Shiki ADRs in place.\n+        # Preserve only still-matching ownership proof so a later forced\n+        # upgrade can authorize cleanup; never carry a missing or stale digest.\n+        existing_digests = existing.get(\"digests\")\n+        if isinstance(existing_digests, dict):\n+            for relative in legacy_shiki_adr_paths():\n+                expected = existing_digests.get(relative)\n+                path = target / relative\n+                if not isinstance(expected, str) or not path.is_file():\n+                    continue\n+                try:\n+                    actual = _sha256_file(path)\n+                except OSError:\n+                    continue\n+                if actual == expected:\n+                    digests[relative] = actual\n         if _incoming_is_older(commit, existing.get(\"platform_commit\")):\n             warn(\n                 \"kept existing install stamp: incoming install \"\ndiff --git a/scripts/shiki_loop.py b/scripts/shiki_loop.py\nindex 3611641da8aeb5038b1da90e3114b09a473ec6d8..84caa491b9cad0edc4176f209c13e9221e975681\n--- a/scripts/shiki_loop.py\n+++ b/scripts/shiki_loop.py\n@@ -424,7 +424,7 @@\n         # unresolvable CCA verdict has already produced its terminal stop and is never\n         # laundered into a head-moving sync (SADR-0010). It also never fires while the\n         # policy (Guardian) gate is red \u2014 for ANY reason, not only when it is the sole\n-        # red check \u2014 so a head-bound Guardian approval, or an SADR-0015\n+        # red check \u2014 so a head-bound Guardian approval, or a SADR-0015\n         # contract-approval carry that base movement defeated (the policy gate compares\n         # base and head task governance directly), is never silently restored by\n         # adopting base wholesale. It is bounded by a durable per-task counter so the\ndiff --git a/scripts/shiki_tasks.py b/scripts/shiki_tasks.py\nindex aec3809f24b4a0c8c96fc50971cff9ae9ad30381..d531052e9d9717058d4631483cefab44c26a65c1\n--- a/scripts/shiki_tasks.py\n+++ b/scripts/shiki_tasks.py\n@@ -821,7 +821,7 @@\n         \"prompt\": prompt,\n         \"entry_skill\": \"grill-with-docs\",\n         \"required_next_steps\": [\n-            \"Run grill-with-docs until terminology, boundaries, risks, and ADR-worthy decisions are settled.\",\n+            \"Run grill-with-docs until terminology, boundaries, risks, and decision-record-worthy choices are settled.\",\n             \"Obtain the operator's explicit approval of the PRD/requirements (Spec Freeze).\",\n             \"Write a machine-readable plan JSON with grill_with_docs.status=complete and spec_freeze.status=frozen.\",\n             \"Run shiki plan ingest --plan-file PLAN.json.\",\ndiff --git a/scripts/test_shiki_goal_loop.sh b/scripts/test_shiki_goal_loop.sh\nindex 753c7b516e10f0be3bc92a4bd0eb1fa699776ae6..8f3820a0a079b3044424cfe1a6c6b7fe68896e67\n--- a/scripts/test_shiki_goal_loop.sh\n+++ b/scripts/test_shiki_goal_loop.sh\n@@ -231,8 +231,8 @@\n \n # High/critical risk merges autonomously when all required checks are green:\n # the \"MergeGate policy check\" required check IS the Guardian gate (it enforces\n-# guardian-policy.json \u2014 human approval OR external AI guardian review, ADR\n-# 0010), so green checks mean a recorded authority approved. The fake gh returns\n+# guardian-policy.json \u2014 human approval OR external AI guardian review,\n+# SADR-0010), so green checks mean a recorded authority approved. The fake gh returns\n # green checks, simulating that the Guardian gate passed.\n python3 \"$ROOT/scripts/shiki.py\" goal create --target \"$TARGET\" --title \"Guardian gate\" --outcome \"High risk merges via policy gate\" >/tmp/shiki-goal-loop-guardian-goal.json\n GGOAL=\"$(json_get /tmp/shiki-goal-loop-guardian-goal.json goal_id)\"\ndiff --git a/tests/test_adr_references.py b/tests/test_adr_references.py\nindex 51706329c797d5dd9b47e082a333fa83b261d482..d005da4f771f5e8085ec22d4b787980e68f5ae8c\n--- a/tests/test_adr_references.py\n+++ b/tests/test_adr_references.py\n@@ -1,8 +1,8 @@\n-\"\"\"Every ``ADR ####`` citation must resolve to exactly one decision record.\n+\"\"\"Every target ADR and Shiki SADR citation resolves in its own namespace.\n \n Background. ``scripts/mergegate_check.py`` justifies ``is_bookkeeping_closeout``\n-with \"ADR 0017\" nine times and ``scripts/guardian_approval_signal.py`` cites\n-0016/0017 seven times, yet for a stretch the numbering in ``docs/adr/`` stopped\n+with \"SADR-0017\" nine times and ``scripts/guardian_approval_signal.py`` cites\n+SADR-0016/SADR-0017 seven times, yet for a stretch the numbering in ``docs/adr/`` stopped\n at 0015: the authoring commits lived on a coordinator branch and no task ever\n declared ``docs/adr/...`` in its locks, so nothing carried the records to the\n default branch. The stated basis of an exemption that merged seven PRs with zero\n@@ -11,11 +11,12 @@\n and the directory drifted with no signal.\n \n This is the same unbound-pair shape behind other silent failures: a reference\n-and its target with nothing asserting they agree. This test binds them. It scans\n-every ``scripts/*.py`` (plus ``docs/**/*.md`` and the root ``*.md`` governance\n-files, which are cheap) for the ``ADR ####`` pattern and asserts each cited\n-number resolves to exactly one ``docs/adr/####-*.md``. The converse is\n-deliberately NOT required: an ADR may exist without being cited.\n+and its target with nothing asserting they agree. This test binds them. Target\n+``ADR NNNN`` citations resolve only to ``docs/adr/NNNN-*.md``; Shiki\n+``SADR-NNNN`` citations resolve only to ``docs/adr/SADR-NNNN-*.md``. The scan\n+covers current governance docs, scripts, workflows, prompts, packaging metadata,\n+and repo-local Codex guidance while deliberately excluding historical\n+``.shiki`` evidence. The converse is not required: a record may be uncited.\n \n The suite is dependency free: standard-library ``unittest`` only.\n \"\"\"\n@@ -31,17 +32,18 @@\n \n REPO_ROOT = Path(__file__).resolve().parents[1]\n \n-# \"ADR 0016\" -- a four-digit citation. The trailing \\b keeps a five-digit token\n-# from being read as a truncated four-digit number.\n-_CITATION = re.compile(r\"\\bADR (\\d{4})\\b\")\n-# \"0016-locks-bound-product-paths.md\" -- the leading four digits name the record.\n-_ADR_FILENAME = re.compile(r\"^(\\d{4})-.*\\.md$\")\n+# ``ADR 0016`` / ``SADR-0016`` -- a namespace plus four-digit citation. The\n+# trailing ``\\b`` keeps a five-digit token from being read as a truncated one.\n+_CITATION = re.compile(r\"\\b(S?ADR)[ -](\\d{4})\\b\")\n+# Target ADRs retain the numeric filename; Shiki platform decisions use SADR.\n+_DECISION_FILENAME = re.compile(r\"^(?:(SADR)-)?(\\d{4})-.*\\.md$\")\n \n \n class Violation(NamedTuple):\n     \"\"\"A citation that does not resolve to exactly one decision record.\"\"\"\n \n     citing_file: str  # repo-relative path of the file that cites the ADR\n+    namespace: str  # \"ADR\" for target decisions; \"SADR\" for Shiki decisions\n     number: str  # the cited four-digit number, e.g. \"0016\"\n     line: int  # 1-based line of the citation\n     reason: str  # \"missing\" (zero matches) or \"ambiguous\" (two or more)\n@@ -49,44 +51,59 @@\n \n     @property\n     def expected_path(self) -> str:\n-        return f\"docs/adr/{self.number}-*.md\"\n+        prefix = \"SADR-\" if self.namespace == \"SADR\" else \"\"\n+        return f\"docs/adr/{prefix}{self.number}-*.md\"\n \n     def describe(self) -> str:\n         \"\"\"Name the citing file, the cited number, and the missing path.\"\"\"\n         if self.reason == \"missing\":\n             return (\n-                f\"{self.citing_file}:{self.line} cites ADR {self.number} \"\n+                f\"{self.citing_file}:{self.line} cites {self.namespace} {self.number} \"\n                 f\"but no document matches {self.expected_path}\"\n             )\n         joined = \", \".join(self.matches)\n         return (\n-            f\"{self.citing_file}:{self.line} cites ADR {self.number} which \"\n+            f\"{self.citing_file}:{self.line} cites {self.namespace} {self.number} which \"\n             f\"resolves to {len(self.matches)} documents (expected exactly one \"\n             f\"at {self.expected_path}): {joined}\"\n         )\n \n \n-def _adr_index(root: Path) -> dict[str, list[Path]]:\n-    \"\"\"Map every four-digit ADR number to the record files that claim it.\"\"\"\n-    index: dict[str, list[Path]] = {}\n+def _adr_index(root: Path) -> dict[tuple[str, str], list[Path]]:\n+    \"\"\"Map each decision namespace and number to the files that claim it.\"\"\"\n+    index: dict[tuple[str, str], list[Path]] = {}\n     adr_dir = root / \"docs\" / \"adr\"\n     if adr_dir.is_dir():\n         for path in sorted(adr_dir.glob(\"*.md\")):\n-            match = _ADR_FILENAME.match(path.name)\n+            match = _DECISION_FILENAME.match(path.name)\n             if match:\n-                index.setdefault(match.group(1), []).append(path)\n+                namespace = \"SADR\" if match.group(1) else \"ADR\"\n+                index.setdefault((namespace, match.group(2)), []).append(path)\n     return index\n \n \n def _citing_files(root: Path, include_docs: bool = True) -> list[Path]:\n-    \"\"\"Files scanned for citations: scripts/*.py, and cheaply the markdown.\"\"\"\n+    \"\"\"Current, shipped text surfaces scanned for decision citations.\"\"\"\n     files = sorted((root / \"scripts\").glob(\"*.py\"))\n+    files += sorted((root / \"scripts\").glob(\"*.sh\"))\n     if include_docs:\n         docs = root / \"docs\"\n         if docs.is_dir():\n             files += sorted(docs.rglob(\"*.md\"))\n         files += sorted(root.glob(\"*.md\"))\n-    return files\n+        pyproject = root / \"pyproject.toml\"\n+        if pyproject.is_file():\n+            files.append(pyproject)\n+        for relative in (\".github\", \".codex\"):\n+            surface = root / relative\n+            if surface.is_dir():\n+                files += sorted(\n+                    path\n+                    for path in surface.rglob(\"*\")\n+                    if path.is_file()\n+                    and path.suffix in {\".md\", \".py\", \".sh\", \".toml\", \".yaml\", \".yml\"}\n+                )\n+    return sorted(set(files))\n \n \n def find_adr_reference_violations(\n@@ -94,10 +111,9 @@\n ) -> list[Violation]:\n     \"\"\"Return every ``ADR ####`` citation under ``root`` that fails to resolve.\n \n-    A citation resolves when exactly one ``docs/adr/####-*.md`` matches its\n-    number. Zero matches is a \"missing\" violation; two or more is \"ambiguous\".\n-    Records that are never cited produce nothing -- the converse is not\n-    required.\n+    A citation resolves when exactly one record in the same namespace matches\n+    its number. Zero matches is a \"missing\" violation; two or more is\n+    \"ambiguous\". Records that are never cited produce nothing.\n     \"\"\"\n     root = Path(root)\n     index = _adr_index(root)\n@@ -110,13 +126,15 @@\n         rel = path.relative_to(root).as_posix()\n         for lineno, line in enumerate(text.splitlines(), start=1):\n             for match in _CITATION.finditer(line):\n-                number = match.group(1)\n-                found = index.get(number, [])\n+                namespace, number = match.groups()\n+                found = index.get((namespace, number), [])\n                 if len(found) == 1:\n                     continue\n                 reason = \"missing\" if not found else \"ambiguous\"\n                 rels = [p.relative_to(root).as_posix() for p in found]\n-                violations.append(Violation(rel, number, lineno, reason, rels))\n+                violations.append(\n+                    Violation(rel, namespace, number, lineno, reason, rels)\n+                )\n     return violations\n \n \n@@ -129,8 +147,8 @@\n         self.assertEqual(\n             violations,\n             [],\n-            \"Every 'ADR ####' citation must resolve to exactly one \"\n-            f\"docs/adr/####-*.md:\\n{detail}\",\n+            \"Every ADR/SADR citation must resolve exactly once in its own \"\n+            f\"docs/adr namespace:\\n{detail}\",\n         )\n \n     def test_scan_actually_reads_scripts(self) -> None:\n@@ -142,12 +160,38 @@\n             \"the citation scan must cover scripts/*.py\",\n         )\n \n+    def test_platform_citations_do_not_use_unresolved_numeric_shorthand(self) -> None:\n+        shorthand = re.compile(r\"SADR-\\d{4}/\\d{4}\")\n+        offenders = []\n+        for path in _citing_files(REPO_ROOT):\n+            text = path.read_text(encoding=\"utf-8\")\n+            if shorthand.search(text):\n+                offenders.append(path.relative_to(REPO_ROOT).as_posix())\n+        self.assertEqual(\n+            offenders,\n+            [],\n+            \"repeat the SADR prefix so every cited number is independently resolvable\",\n+        )\n+\n     def test_carried_records_are_indexed(self) -> None:\n         # The two records this Goal carried onto the default branch, and which\n         # the scripts cite most, must be present.\n         index = _adr_index(REPO_ROOT)\n-        self.assertIn(\"0016\", index)\n-        self.assertIn(\"0017\", index)\n+        self.assertIn((\"SADR\", \"0016\"), index)\n+        self.assertIn((\"SADR\", \"0017\"), index)\n+\n+    def test_platform_tree_has_only_sadr_0001_through_0019(self) -> None:\n+        adr_dir = REPO_ROOT / \"docs\" / \"adr\"\n+        sadrs = sorted(adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\"))\n+        self.assertEqual(\n+            {path.name.split(\"-\", 2)[1] for path in sadrs},\n+            {f\"{number:04d}\" for number in range(1, 20)},\n+        )\n+        self.assertEqual(\n+            list(adr_dir.glob(\"[0-9][0-9][0-9][0-9]-*.md\")),\n+            [],\n+            \"the Shiki platform tree must not retain legacy numeric SADR paths\",\n+        )\n \n \n class FixtureAdrReferences(unittest.TestCase):\n@@ -180,6 +224,26 @@\n         self.assertIn(\"9991\", message)  # cited number\n         self.assertIn(\"docs/adr/9991-*.md\", message)  # missing path\n \n+    def test_target_adr_does_not_satisfy_missing_platform_sadr(self) -> None:\n+        root = self._make_repo(\n+            adr_names=[\"0042-target-decision.md\"],\n+            script_body='\"\"\"Target ADR 0042 exists, but platform SADR-0042 does not.\"\"\"\\n',\n+        )\n+        violations = find_adr_reference_violations(root, include_docs=False)\n+        self.assertEqual(len(violations), 1)\n+        self.assertEqual(violations[0].namespace, \"SADR\")\n+        self.assertEqual(violations[0].number, \"0042\")\n+        self.assertEqual(violations[0].expected_path, \"docs/adr/SADR-0042-*.md\")\n+\n+    def test_target_adr_and_platform_sadr_may_reuse_a_number(self) -> None:\n+        root = self._make_repo(\n+            adr_names=[\"0042-target-decision.md\", \"SADR-0042-platform-decision.md\"],\n+            script_body='\"\"\"Target ADR 0042 and platform SADR-0042 are distinct.\"\"\"\\n',\n+        )\n+        self.assertEqual(\n+            find_adr_reference_violations(root, include_docs=False), []\n+        )\n+\n     def test_uncited_adr_is_allowed(self) -> None:\n         # An ADR exists but nothing cites it -> not a violation. The converse\n         # (every ADR must be cited) is explicitly NOT required.\ndiff --git a/tests/test_guardian_review.py b/tests/test_guardian_review.py\nindex 88829e7810212635e2fc98aa5918d6f26294677d..00a85a237acd5bf2d6cb5b294128198c422402ca\n--- a/tests/test_guardian_review.py\n+++ b/tests/test_guardian_review.py\n@@ -162,7 +162,7 @@\n \n     def test_docs_only_is_light_and_not_conservative(self):\n         focus = gr.classify_review_focus([\"docs/adr/0099-x.md\", \"README.md\"], \"\", \"low\")\n-        self.assertTrue(any(\"Docs/ADR-only\" in f for f in focus))\n+        self.assertTrue(any(\"Docs/decision-record-only\" in f for f in focus))\n         self.assertFalse(any(\"conservative default\" in f.lower() for f in focus))\n \n     def test_unknown_category_is_conservative(self):\ndiff --git a/tests/test_policy_job_merged_prs.py b/tests/test_policy_job_merged_prs.py\nindex d45512e74681e5e649279236c7d4e79d5c3da471..b97989f8bd74d091de1a6b5275a20d8dc68c4512\n--- a/tests/test_policy_job_merged_prs.py\n+++ b/tests/test_policy_job_merged_prs.py\n@@ -3,8 +3,8 @@\n \n ``.github/workflows/shiki-cca-completion.yml`` has two consumers of the merged-PR\n proof: the CCA *signal* (``guardian_approval_signal.py`` in \"Prepare CCA context\")\n-and the authoritative gate (``mergegate_check.py`` in \"Enforce MergeGate\"). ADR\n-0017's bookkeeping-closeout exemption (condition 6) needs the merged implementation\n+and the authoritative gate (``mergegate_check.py`` in \"Enforce MergeGate\").\n+SADR-0017's bookkeeping-closeout exemption (condition 6) needs the merged implementation\n PR proven, and a closeout carries NO ``post_merge_reconcile`` marker. The signal\n already resolves ``MERGED_PRS`` unconditionally, but \"Enforce MergeGate\" used to\n resolve it only inside ``grep -q '<!-- shiki:post_merge_reconcile -->'``. So a\ndiff --git a/tests/test_sadr_install_migration.py b/tests/test_sadr_install_migration.py\nnew file mode 100644\nindex 0000000000000000000000000000000000000000..d51eac872b1c79ff4579c4c1401ca5d775a60d8d\n--- /dev/null\n+++ b/tests/test_sadr_install_migration.py\n@@ -0,0 +1,237 @@\n+\"\"\"SADR installation and collision-safe legacy upgrade behavior.\"\"\"\n+\n+from __future__ import annotations\n+\n+import contextlib\n+import hashlib\n+import io\n+import json\n+import shutil\n+import tempfile\n+import unittest\n+from pathlib import Path\n+\n+import shiki_test_support  # noqa: F401  (path bootstrap)\n+\n+import shiki_installer\n+from tests.test_adr_references import find_adr_reference_violations\n+\n+\n+PRODUCT_NUMBERS = tuple(range(13, 19))\n+\n+\n+def _quiet_install(target: Path, *, force: bool, validate: bool) -> None:\n+    with contextlib.redirect_stdout(io.StringIO()):\n+        shiki_installer.install_template(target, force=force, validate=validate)\n+\n+\n+def _sha256(path: Path) -> str:\n+    return hashlib.sha256(path.read_bytes()).hexdigest()\n+\n+\n+def _legacy_sadr_paths() -> tuple[str, ...]:\n+    return tuple(\n+        f\"docs/adr/{path.name.removeprefix('SADR-')}\"\n+        for path in sorted((shiki_installer.ROOT / \"docs\" / \"adr\").glob(\"SADR-*.md\"))\n+        if path.name != \"SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\"\n+    )\n+\n+\n+def _create_legacy_upgrade_fixture(\n+    target: Path,\n+) -> tuple[dict[str, bytes], tuple[str, ...]]:\n+    adr_dir = target / \"docs\" / \"adr\"\n+    adr_dir.mkdir(parents=True)\n+    product_bytes = {\n+        f\"{number:04d}-cubeshare-product-decision.md\": (\n+            f\"# CubeShare ADR {number:04d}\\n\\nDo not rewrite {number}.\\n\"\n+        ).encode()\n+        for number in PRODUCT_NUMBERS\n+    }\n+    for name, content in product_bytes.items():\n+        (adr_dir / name).write_bytes(content)\n+\n+    legacy_paths = _legacy_sadr_paths()\n+    for relative in legacy_paths:\n+        (target / relative).write_bytes(f\"legacy Shiki bytes for {relative}\\n\".encode())\n+\n+    stamp_path = target / shiki_installer.INSTALL_STAMP_PATH\n+    stamp_path.parent.mkdir(parents=True, exist_ok=True)\n+    stamp = {\n+        \"version\": shiki_installer.INSTALL_STAMP_VERSION,\n+        \"platform_commit\": None,\n+        \"installed_at\": \"2000-01-01T00:00:00+00:00\",\n+        \"digests\": {\n+            relative: _sha256(target / relative) for relative in legacy_paths\n+        },\n+    }\n+    stamp_path.write_text(\n+        json.dumps(stamp, indent=2, sort_keys=True) + \"\\n\", encoding=\"utf-8\"\n+    )\n+    return product_bytes, legacy_paths\n+\n+\n+def _tree_snapshot(root: Path) -> dict[str, tuple[str, bytes | str]]:\n+    \"\"\"Recursive byte/shape snapshot used to prove preflight is write-free.\"\"\"\n+    snapshot: dict[str, tuple[str, bytes | str]] = {}\n+    for path in sorted(root.rglob(\"*\")):\n+        relative = path.relative_to(root).as_posix()\n+        if path.is_symlink():\n+            snapshot[relative] = (\"symlink\", path.readlink().as_posix())\n+        elif path.is_dir():\n+            snapshot[relative] = (\"directory\", \"\")\n+        else:\n+            snapshot[relative] = (\"file\", path.read_bytes())\n+    return snapshot\n+\n+\n+class FreshSadrInstallTests(unittest.TestCase):\n+    def setUp(self) -> None:\n+        self.tmp = Path(tempfile.mkdtemp(prefix=\"shiki-sadr-fresh-\"))\n+        self.addCleanup(lambda: shutil.rmtree(self.tmp, ignore_errors=True))\n+        self.target = self.tmp / \"target\"\n+        self.adr_dir = self.target / \"docs\" / \"adr\"\n+        self.adr_dir.mkdir(parents=True)\n+        self.product_bytes = {\n+            f\"{number:04d}-product-decision.md\": (\n+                f\"# Product ADR {number:04d}\\n\\nTarget-owned bytes {number}.\\n\"\n+            ).encode()\n+            for number in PRODUCT_NUMBERS\n+        }\n+        for name, content in self.product_bytes.items():\n+            (self.adr_dir / name).write_bytes(content)\n+\n+    def test_fresh_install_preserves_product_adrs_and_installs_all_sadrs(self) -> None:\n+        _quiet_install(self.target, force=False, validate=True)\n+\n+        for name, expected in self.product_bytes.items():\n+            self.assertEqual((self.adr_dir / name).read_bytes(), expected)\n+\n+        sadr_numbers = {\n+            path.name.split(\"-\", 2)[1]\n+            for path in self.adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\")\n+        }\n+        self.assertEqual(sadr_numbers, {f\"{number:04d}\" for number in range(1, 20)})\n+\n+        numeric_records = {\n+            path.name\n+            for path in self.adr_dir.glob(\"[0-9][0-9][0-9][0-9]-*.md\")\n+        }\n+        self.assertEqual(numeric_records, set(self.product_bytes))\n+        stamp = json.loads(\n+            (self.target / shiki_installer.INSTALL_STAMP_PATH).read_text(\n+                encoding=\"utf-8\"\n+            )\n+        )\n+        for path in self.adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\"):\n+            relative = path.relative_to(self.target).as_posix()\n+            self.assertEqual(stamp[\"digests\"][relative], _sha256(path))\n+        self.assertEqual(find_adr_reference_violations(self.target), [])\n+\n+\n+class LegacySadrUpgradeTests(unittest.TestCase):\n+    def setUp(self) -> None:\n+        self.tmp = Path(tempfile.mkdtemp(prefix=\"shiki-sadr-upgrade-\"))\n+        self.addCleanup(lambda: shutil.rmtree(self.tmp, ignore_errors=True))\n+        self.target = self.tmp / \"target\"\n+        self.adr_dir = self.target / \"docs\" / \"adr\"\n+        self.product_bytes, self.legacy_paths = _create_legacy_upgrade_fixture(\n+            self.target\n+        )\n+\n+    def test_force_upgrade_deletes_only_stamped_legacy_sadrs(self) -> None:\n+        _quiet_install(self.target, force=True, validate=True)\n+\n+        for relative in self.legacy_paths:\n+            self.assertFalse((self.target / relative).exists(), relative)\n+        for name, expected in self.product_bytes.items():\n+            self.assertEqual((self.adr_dir / name).read_bytes(), expected)\n+\n+        sadrs = list(self.adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\"))\n+        self.assertEqual(len(sadrs), 19)\n+        refreshed = json.loads(\n+            (self.target / shiki_installer.INSTALL_STAMP_PATH).read_text(encoding=\"utf-8\")\n+        )\n+        for relative in self.legacy_paths:\n+            self.assertNotIn(relative, refreshed[\"digests\"])\n+        for path in sadrs:\n+            relative = path.relative_to(self.target).as_posix()\n+            self.assertEqual(refreshed[\"digests\"][relative], _sha256(path))\n+\n+    def test_non_force_install_never_deletes_legacy_files(self) -> None:\n+        before = {\n+            relative: (self.target / relative).read_bytes()\n+            for relative in self.legacy_paths\n+        }\n+        _quiet_install(self.target, force=False, validate=False)\n+        for relative, expected in before.items():\n+            self.assertEqual((self.target / relative).read_bytes(), expected)\n+        refreshed = json.loads(\n+            (self.target / shiki_installer.INSTALL_STAMP_PATH).read_text(encoding=\"utf-8\")\n+        )\n+        for relative in self.legacy_paths:\n+            self.assertEqual(refreshed[\"digests\"][relative], _sha256(self.target / relative))\n+\n+\n+class LegacyCleanupPreflightTests(unittest.TestCase):\n+    def setUp(self) -> None:\n+        self.tmp = Path(tempfile.mkdtemp(prefix=\"shiki-sadr-preflight-\"))\n+        self.addCleanup(lambda: shutil.rmtree(self.tmp, ignore_errors=True))\n+\n+    def test_all_ownership_blockers_abort_before_any_write(self) -> None:\n+        cases = (\n+            (\"absent\", \"install stamp is absent\"),\n+            (\"unreadable\", \"install stamp is unreadable\"),\n+            (\"malformed\", \"install stamp is malformed\"),\n+            (\"missing-digest\", \"install stamp has no digest\"),\n+            (\"digest-mismatch\", \"digest mismatch\"),\n+        )\n+        for name, expected_reason in cases:\n+            with self.subTest(name=name):\n+                target = self.tmp / name\n+                _, legacy_paths = _create_legacy_upgrade_fixture(target)\n+                stamp_path = target / shiki_installer.INSTALL_STAMP_PATH\n+\n+                if name == \"absent\":\n+                    stamp_path.unlink()\n+                elif name == \"unreadable\":\n+                    stamp_path.unlink()\n+                    stamp_path.mkdir()\n+                elif name == \"malformed\":\n+                    stamp_path.write_text(\"{not-json\\n\", encoding=\"utf-8\")\n+                else:\n+                    stamp = json.loads(stamp_path.read_text(encoding=\"utf-8\"))\n+                    if name == \"missing-digest\":\n+                        stamp[\"digests\"] = {}\n+                    else:\n+                        stamp[\"digests\"] = {\n+                            relative: \"deadbeef\" for relative in legacy_paths\n+                        }\n+                    stamp_path.write_text(\n+                        json.dumps(stamp, indent=2, sort_keys=True) + \"\\n\",\n+                        encoding=\"utf-8\",\n+                    )\n+\n+                before = _tree_snapshot(target)\n+                with self.assertRaises(shiki_installer.ShikiError) as raised:\n+                    _quiet_install(target, force=True, validate=False)\n+                after = _tree_snapshot(target)\n+\n+                self.assertEqual(before, after, f\"{name} preflight changed the tree\")\n+                message = str(raised.exception)\n+                self.assertIn(expected_reason, message)\n+                self.assertIn(\"No files were written\", message)\n+                for relative in legacy_paths:\n+                    self.assertIn(relative, message)\n+\n+    def test_absent_stamp_is_allowed_when_no_legacy_path_exists(self) -> None:\n+        target = self.tmp / \"no-legacy\"\n+        target.mkdir()\n+        _quiet_install(target, force=True, validate=False)\n+        self.assertTrue(\n+            (target / \"docs\" / \"adr\" / \"SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\").is_file()\n+        )\n+\n+\n+if __name__ == \"__main__\":\n+    unittest.main()\n\n2026-08-07T08:24:08.818654Z ERROR codex_models_manager::manager: failed to renew cache TTL: missing field `base_instructions` at line 94 column 5\nexec\n/bin/zsh -lc \"sed -n '1,260p' .shiki/schemas/ledger.schema.json\" in /private/tmp/shiki-adr-collision.pa0KwY/.worktrees/shiki-t-20260807t073307221254z-2d22d654-introduc\n succeeded in 0ms:\n{\n  \"$schema\": \"https://json-schema.org/draft/2020-12/schema\",\n  \"$id\": \"https://shiki.local/schemas/ledger.schema.json\",\n  \"title\": \"Shiki Ledger Entry\",\n  \"type\": \"object\",\n  \"required\": [\n    \"id\",\n    \"timestamp\",\n    \"goal_id\",\n    \"type\",\n    \"actor\",\n    \"summary\",\n    \"evidence\"\n  ],\n  \"properties\": {\n    \"id\": {\n      \"type\": \"string\",\n      \"pattern\": \"^L-([0-9]{4,}|[0-9]{8}T[0-9]{12}Z-[0-9a-f]{8})$\"\n    },\n    \"timestamp\": {\n      \"type\": \"string\"\n    },\n    \"goal_id\": {\n      \"type\": \"string\",\n      \"pattern\": \"^G-([0-9]{4,}|[0-9]{8}T[0-9]{12}Z-[0-9a-f]{8})$\"\n    },\n    \"task_id\": {\n      \"type\": [\n        \"string\",\n        \"null\"\n      ],\n      \"pattern\": \"^T-([0-9]{4,}|[0-9]{8}T[0-9]{12}Z-[0-9a-f]{8})$\"\n    },\n    \"type\": {\n      \"enum\": [\n        \"goal-created\",\n        \"context-impact\",\n        \"task-registered\",\n        \"lock\",\n        \"check\",\n        \"review\",\n        \"cca-verdict\",\n        \"repair\",\n        \"mergegate\",\n        \"completion\",\n        \"handoff\",\n        \"memory-transition\",\n        \"contract-amended\"\n      ]\n    },\n    \"actor\": {\n      \"type\": \"string\",\n      \"minLength\": 1\n    },\n    \"summary\": {\n      \"type\": \"string\",\n      \"minLength\": 1\n    },\n    \"evidence\": {\n      \"type\": \"array\",\n      \"items\": {\n        \"type\": \"string\",\n        \"minLength\": 1\n      },\n      \"minItems\": 1\n    },\n    \"links\": {\n      \"type\": \"array\",\n      \"items\": {\n        \"type\": \"string\"\n      }\n    },\n    \"evidence_refs\": {\n      \"type\": \"array\",\n      \"items\": {\n        \"type\": \"object\",\n        \"required\": [\n          \"kind\"\n        ],\n        \"properties\": {\n          \"kind\": {\n            \"enum\": [\n              \"github-pr\",\n              \"github-issue\",\n              \"github-workflow-run\",\n              \"github-artifact\",\n              \"ledger-digest\"\n            ]\n          },\n          \"pr\": {\n            \"type\": \"integer\"\n          },\n          \"issue\": {\n            \"type\": \"integer\"\n          },\n          \"head_sha\": {\n            \"type\": \"string\"\n          },\n          \"workflow\": {\n            \"type\": \"string\"\n          },\n          \"run_id\": {\n            \"type\": \"string\"\n          },\n          \"run_attempt\": {\n            \"type\": \"string\"\n          },\n          \"name\": {\n            \"type\": \"string\"\n          },\n          \"manifest_path\": {\n            \"type\": \"string\"\n          },\n          \"manifest_sha256\": {\n            \"type\": \"string\"\n          },\n          \"algorithm\": {\n            \"type\": \"string\"\n          },\n          \"digest\": {\n            \"type\": \"string\"\n          }\n        },\n        \"additionalProperties\": true\n      }\n    },\n    \"ledger_integrity\": {\n      \"type\": \"object\",\n      \"properties\": {\n        \"algorithm\": {\n          \"type\": \"string\"\n        },\n        \"canonical_digest\": {\n          \"type\": \"string\"\n        },\n        \"previous_task_ledger\": {\n          \"type\": \"string\"\n        },\n        \"previous_task_ledger_digest\": {\n          \"type\": \"string\"\n        }\n      },\n      \"additionalProperties\": true\n    }\n  },\n  \"additionalProperties\": true\n}\n\ndiff --git a/.codex/skills/shiki/SKILL.md b/.codex/skills/shiki/SKILL.md\nindex c8e2e85d9d75d49cc4f2d6286ee22659c4e53e75..93cded623b2c15d9f65c76491a1c2d813ac9e6c0\n--- a/.codex/skills/shiki/SKILL.md\n+++ b/.codex/skills/shiki/SKILL.md\n@@ -18,6 +18,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n Codex CLI does not register this skill as a custom `/shiki` slash command. When\n the user types `/shiki` in Codex CLI and gets \"Unrecognized command\", explain\n that this is expected: invoke Shiki in Codex with natural language such as\n@@ -69,7 +72,7 @@\n \n ## Responsibilities\n \n-- Claude Code implements and repairs by default (`shiki runner claude`, ADR 0008); Codex implements and repairs tasks explicitly assigned to `codex`.\n+- Claude Code implements and repairs by default (`shiki runner claude`, SADR-0008); Codex implements and repairs tasks explicitly assigned to `codex`.\n - Claude Code Action can act as GitHub-side CCA or reviewer.\n - CCA judges completion.\n - MergeGate authorizes state transitions and merge readiness.\ndiff --git a/.github/prompts/cca-completion-check.md b/.github/prompts/cca-completion-check.md\nindex ed203ad5f302ecc81372d8ab27d2b1232ca97497..fd89950528333ef6242e6d5443904f5392ab7e56\n--- a/.github/prompts/cca-completion-check.md\n+++ b/.github/prompts/cca-completion-check.md\n@@ -19,6 +19,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n ## Required Reading\n \n Read only the files needed to judge the PR contract. Prefer this order:\ndiff --git a/.github/prompts/codex-task-handoff.md b/.github/prompts/codex-task-handoff.md\nindex 695e211d76dea1c60e8d5bb360e78487808286a4..59441d576b01a39569722f226c854e5a740e9eb5\n--- a/.github/prompts/codex-task-handoff.md\n+++ b/.github/prompts/codex-task-handoff.md\n@@ -2,7 +2,7 @@\n \n You are Codex Front acting as the Implementer for a Shiki task.\n \n-Follow `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant ADRs, and `docs/agents/codex-handoff.md`.\n+Follow `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant Shiki SADRs and target ADRs, and `docs/agents/codex-handoff.md`.\n \n Rules:\n \ndiff --git a/AGENTS.md b/AGENTS.md\nindex 7f041100d49c2a7525f51cb0f5d696a4a6d1c78c..a06bfbabc6fa48a007389e25e12d7ed523936bed\n--- a/AGENTS.md\n+++ b/AGENTS.md\n@@ -22,7 +22,7 @@\n - **Codex Front** is an optional implementer through Codex App, Codex CLI, Codex IDE extension, or Codex Web signed in with ChatGPT OAuth/subscription auth, dispatched with `shiki runner codex` when a task is explicitly assigned to `codex`.\n - **GitHub CCA** is implemented by Claude Code Action by default, using `CLAUDE_CODE_OAUTH_TOKEN`.\n \n-Do not assume `openai/codex-action`, `OPENAI_API_KEY`, or API-key based Codex automation in the default Shiki path. API-key runners are explicit target-repo extensions and require their own ADR.\n+Do not assume `openai/codex-action`, `OPENAI_API_KEY`, or API-key based Codex automation in the default Shiki path. API-key runners are explicit target-repo extensions and require their own target ADR.\n \n Shiki is not:\n \n@@ -44,10 +44,14 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use\n+`SADR-NNNN-*.md`; target product decisions retain `NNNN-*.md` and the `ADR`\n+namespace.\n+\n Use the canonical source-of-truth order above. Runtime-specific reading orders\n are operational guidance only and must not override or reorder it.\n \n-Conversation state is not durable truth. If a decision matters, put it in GitHub, `.shiki/`, `CONTEXT.md`, or an ADR.\n+Conversation state is not durable truth. If a decision matters, put it in GitHub, `.shiki/`, `CONTEXT.md`, a Shiki SADR, or a target ADR.\n \n If GitHub and `.shiki/` disagree, surface the conflict and prefer GitHub operational state until the mirror is repaired.\n \n@@ -75,8 +79,8 @@\n Every non-trivial change follows this loop:\n \n 1. **Goal**: clarify outcome, completion conditions, non-goals, risk level, and success signals.\n-2. **grill-with-docs**: challenge the plan against domain docs, ADRs, code reality, terminology, and edge scenarios.\n-3. **Context & Impact**: find relevant docs, ADRs, code, dependencies, owners, locks, and verification surfaces.\n+2. **grill-with-docs**: challenge the plan against domain docs, Shiki SADRs, target ADRs, code reality, terminology, and edge scenarios.\n+3. **Context & Impact**: find relevant docs, SADRs, target ADRs, code, dependencies, owners, locks, and verification surfaces.\n 4. **PRD and Spec Freeze**: use `to-prd` when context is settled enough to become durable product/engineering intent. The operator's explicit approval of the PRD is Spec Freeze: the plan records a `spec_freeze` block, and no plan runs without it. Steps 1-4 form Requirements Definition \u2014 one continuous operator dialogue. After Spec Freeze, scope changes require an operator-approved Spec Amendment; non-scope-moving interpretations go to the Assumption Log.\n 5. **Issues**: use `to-issues` to create independently grabbable vertical-slice issues. Prefer AFK slices over HITL slices where possible.\n 6. **Triage**: label issues for readiness, risk, runtime, skills, and MergeGate state.\n@@ -167,7 +171,7 @@\n - Testing decisions.\n - Out-of-scope items.\n - Further notes.\n-- Links to relevant domain terms and ADRs.\n+- Links to relevant domain terms, SADRs, and target ADRs.\n \n Every executable task must include:\n \n@@ -193,7 +197,7 @@\n \n Context & Impact must identify:\n \n-- Relevant documents, domain terms, ADRs, and past decisions.\n+- Relevant documents, domain terms, SADRs, target ADRs, and past decisions.\n - Relevant modules, interfaces, seams, callers, owners, and tests.\n - Dependency relationships between tasks.\n - Candidate locks and lock conflicts.\n@@ -210,7 +214,7 @@\n | Skill | Required when |\n | --- | --- |\n | `setup-matt-pocock-skills` | First configuring the repo for skills, or when issue tracker, triage labels, or domain docs are missing. |\n-| `grill-with-docs` | Default for non-trivial Goals before PRD/issues; always required when plans, terminology, boundaries, tradeoffs, or ADR-worthy decisions are ambiguous. |\n+| `grill-with-docs` | Default for non-trivial Goals before PRD/issues; always required when plans, terminology, boundaries, tradeoffs, or decision-record-worthy choices are ambiguous. |\n | `zoom-out` | The agent lacks an architectural map of the relevant code area. |\n | `to-prd` | Settled Goal context must become durable product/engineering intent. |\n | `to-issues` | A Goal, PRD, or plan must become independently grabbable vertical-slice issues. |\n@@ -348,7 +352,7 @@\n - Three or more new files.\n - Any high-risk or critical label.\n \n-When the architecture gate triggers, run `grill-with-docs` or `improve-codebase-architecture` as appropriate and record durable decisions in an ADR when needed.\n+When the architecture gate triggers, run `grill-with-docs` or `improve-codebase-architecture` as appropriate and record durable decisions in a Shiki SADR or target ADR as applicable.\n \n ## Engineering Discipline\n \ndiff --git a/CLAUDE.md b/CLAUDE.md\nindex 98a932e7841a632866fcd53b0bad0e3af3711d3d..98a31399404d6fb044b4fd493f4459ac87f439de\n--- a/CLAUDE.md\n+++ b/CLAUDE.md\n@@ -14,6 +14,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n ## Claude Role\n \n Default to planner, default implementer, reviewer, coordinator, documentation editor, CCA-style completion judge, and final-judgment assistant.\n@@ -57,7 +60,7 @@\n At the start of a Shiki session:\n \n 1. Read `AGENTS.md`.\n-2. Read `CONTEXT.md` and relevant ADRs.\n+2. Read `CONTEXT.md` and relevant Shiki SADRs and target ADRs.\n 3. Read active `.shiki/` Goal/task/ledger state when present.\n 4. Read active distilled rules from `.shiki/memories/` (status `distilled`, `active: true`, not revoked or superseded) for applicable guidance; the same rules are injected deterministically into task handoffs as the `## Distilled Rules` section (\u00a73.5).\n 5. Read the GitHub Issue or PR when the task is GitHub-backed.\n@@ -74,7 +77,7 @@\n - Define the Goal, completion conditions, non-goals, and risk level.\n - Treat `grill-with-docs` as the default planning entry point for non-trivial Goals.\n - Challenge domain terms against `CONTEXT.md`.\n-- Challenge hard-to-reverse tradeoffs against ADRs.\n+- Challenge hard-to-reverse tradeoffs against SADRs and target ADRs.\n - Explore code instead of asking when code can answer the question.\n - Run Context & Impact before implementation planning.\n - Produce a Task DAG, not an unordered checklist.\n@@ -94,7 +97,7 @@\n - For each question, provide your recommended answer.\n - Resolve dependency decisions before downstream design questions.\n - Update `CONTEXT.md` inline when terminology crystallizes.\n-- Offer ADRs only when the decision is hard to reverse, surprising without context, and a real tradeoff.\n+- Offer a SADR or target ADR only when the decision is hard to reverse, surprising without context, and a real tradeoff.\n - Convert unresolved questions into HITL blockers rather than allowing Codex to guess.\n \n ## PRD and Issues Standard\n@@ -103,7 +106,7 @@\n \n - Use the project's domain glossary vocabulary.\n - Avoid volatile file paths unless they are essential.\n-- Include implementation decisions, testing decisions, out-of-scope items, and relevant ADR/domain links.\n+- Include implementation decisions, testing decisions, out-of-scope items, and relevant SADR/target-ADR/domain links.\n - Identify deep-module opportunities and testable interfaces.\n \n When creating issues:\n@@ -121,7 +124,7 @@\n - Branch/worktree target.\n - Scope and non-goals.\n - Dependencies and locks.\n-- Relevant docs, ADRs, modules, and tests.\n+- Relevant docs, SADRs, target ADRs, modules, and tests.\n - Required skill invocations.\n - TDD expectations.\n - Pre-PR `code-review` gate expectations.\n@@ -152,7 +155,7 @@\n \n - Treat yourself as a judge, not implementer.\n - Do not write production code.\n-- Read `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant ADRs, PRD, task issue, PR body, diff, checks, reviews, labels, and `.shiki/` state.\n+- Read `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant SADRs and target ADRs, PRD, task issue, PR body, diff, checks, reviews, labels, and `.shiki/` state.\n - Evaluate every applicable checklist in `docs/agents/checklists.md`.\n - Map each acceptance criterion to durable evidence.\n - Distinguish implementation failure from missing evidence.\ndiff --git a/CONTEXT.md b/CONTEXT.md\nindex e61a9be06a0cce0d026484083a657bde2067e234..237ee8aa3ea8fab334ece49768bc5ed3b5af0974\n--- a/CONTEXT.md\n+++ b/CONTEXT.md\n@@ -25,7 +25,7 @@\n _Avoid_: jumping directly into code\n \n **grill-with-docs**:\n-A planning skill that challenges a plan against domain language, ADRs, code reality, and concrete edge scenarios. It resolves design-tree questions before PRD/issues. It runs inside Requirements Definition.\n+A planning skill that challenges a plan against domain language, Shiki SADRs, target ADRs, code reality, and concrete edge scenarios. It resolves design-tree questions before PRD/issues. It runs inside Requirements Definition.\n _Avoid_: generic brainstorming, silent assumptions\n \n **Requirements Definition**:\ndiff --git a/CONTRIBUTING.md b/CONTRIBUTING.md\nindex c6d9b656e73abb8a55e5f660123e03153dcb3e6d..9e37a8fbf668db5b22461aaae5548b8f1354394c\n--- a/CONTRIBUTING.md\n+++ b/CONTRIBUTING.md\n@@ -17,7 +17,7 @@\n 1. **Goal** \u2014 start from a user-approved Goal with an outcome, completion\n    conditions, non-goals, risk level, and success signals.\n 2. **grill-with-docs** \u2014 for any non-trivial change, challenge the plan against\n-   domain language, ADRs, and code reality before writing a PRD.\n+   domain language, Shiki SADRs, target ADRs, and code reality before writing a PRD.\n 3. **PRD** \u2014 capture settled product/engineering intent (`to-prd`).\n 4. **Spec Freeze** \u2014 the operator explicitly approves the PRD/requirements;\n    the plan records a `spec_freeze` block and no plan runs without it\n@@ -94,7 +94,7 @@\n   entries.\n - Do not hand-edit `.shiki/goals/*.json` or `.shiki/manifest.json` in a task PR.\n - Conversation state is not durable truth. If a decision matters, record it in\n-  GitHub, `.shiki/`, `CONTEXT.md`, or an ADR.\n+  GitHub, `.shiki/`, `CONTEXT.md`, a Shiki SADR, or a target ADR.\n \n ## Architecture Gate\n \n@@ -103,7 +103,7 @@\n or CI policy, package structure, three or more structural files, more than 100\n net new lines in a core module, three or more new files, or any high-risk label.\n When triggered, run `grill-with-docs` or `improve-codebase-architecture` and\n-record durable decisions in an ADR when needed (see `docs/adr/`).\n+record durable decisions in a Shiki SADR or target ADR as applicable (see `docs/adr/`).\n \n ## Safety\n \ndiff --git a/SYSTEM_PROMPT.md b/SYSTEM_PROMPT.md\nindex aaaea45e3a896c3a44ba89eec8bb84325ecf6c62..da009c86a88285269dee0155e58e550c6d744ea9\n--- a/SYSTEM_PROMPT.md\n+++ b/SYSTEM_PROMPT.md\n@@ -30,6 +30,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n Use the canonical source-of-truth order above. Runtime-specific reading orders\n are operational guidance only and must not override or reorder it. The current\n conversation is non-durable operator input.\n@@ -48,14 +51,14 @@\n \n A runtime may only do work that is explicitly assigned by the Goal, task, PR, or MergeGate decision.\n \n-Do not assume `openai/codex-action`, `OPENAI_API_KEY`, or API-key based Codex automation in the default Shiki path. API-key runners are explicit target-repo extensions and require their own ADR.\n+Do not assume `openai/codex-action`, `OPENAI_API_KEY`, or API-key based Codex automation in the default Shiki path. API-key runners are explicit target-repo extensions and require their own target ADR.\n \n ## 4. Non-negotiable Shiki flow\n \n For any non-trivial Goal, use this flow:\n \n 1. **Goal Seek**: define outcome, non-goals, risk level, completion criteria, and evidence requirements.\n-2. **grill-with-docs**: challenge the plan against `CONTEXT.md`, ADRs, code reality, terminology, boundaries, and edge scenarios. Ask one question at a time when the operator is available. If code can answer a question, inspect code instead of asking.\n+2. **grill-with-docs**: challenge the plan against `CONTEXT.md`, Shiki SADRs, target ADRs, code reality, terminology, boundaries, and edge scenarios. Ask one question at a time when the operator is available. If code can answer a question, inspect code instead of asking.\n 3. **Context & Impact**: identify modules, interfaces, seams, callers, dependencies, locks, risk, and verification surfaces.\n 4. **to-prd + Spec Freeze**: turn settled context into a PRD using domain vocabulary and testing decisions. The operator's explicit approval of the PRD is Spec Freeze, recorded as the plan's `spec_freeze` block; plans without it do not run. Steps 1-4 form Requirements Definition.\n 5. **to-issues**: decompose the PRD into independently grabbable vertical-slice issues. Prefer AFK slices; mark HITL slices when judgment is still required.\n@@ -75,7 +78,7 @@\n Before making any material change:\n \n 1. Read `AGENTS.md` and any runtime-specific wrapper such as `CLAUDE.md`.\n-2. Read `CONTEXT.md` and relevant ADRs.\n+2. Read `CONTEXT.md` and relevant SADRs and target ADRs.\n 3. Read the GitHub Issue / PR and `.shiki/` state for the active Goal or task.\n 4. Identify your role: Planner, Implementer, Reviewer, Completion Check Agent, Repairer, or Guardian-assist.\n 5. Identify Goal id, task id, branch, PR, dependencies, locks, risk level, required skills, and required evidence.\n@@ -103,7 +106,7 @@\n \n At minimum, identify:\n \n-- Relevant documents, ADRs, domain terms, and prior decisions.\n+- Relevant documents, SADRs, target ADRs, domain terms, and prior decisions.\n - Relevant modules, interfaces, seams, callers, owners, and tests.\n - Dependency relationships between tasks.\n - Candidate file locks and lock conflicts.\n@@ -152,7 +155,7 @@\n \n CCA must read:\n \n-- `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant ADRs.\n+- `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant SADRs and target ADRs.\n - Parent Goal issue, PRD issue, task issue, comments, and labels.\n - PR body, diff, commits, changed files, reviews, CI checks, and artifacts available to the workflow.\n - `.shiki/` task contract, locks, ledger entries, CCA checklist profile, and prior repair packets.\n@@ -214,7 +217,7 @@\n - Three or more new files.\n - Any high-risk or critical label.\n \n-When the architecture gate triggers, run `grill-with-docs` or `improve-codebase-architecture` as appropriate and record the decision in an ADR if it is hard to reverse or likely to be re-litigated.\n+When the architecture gate triggers, run `grill-with-docs` or `improve-codebase-architecture` as appropriate and record the decision in a Shiki SADR or target ADR as applicable if it is hard to reverse or likely to be re-litigated.\n \n ## 14. Skill Gate\n \n@@ -223,7 +226,7 @@\n Use these skills:\n \n - `setup-matt-pocock-skills`: before first use of the engineering skill set, or when issue tracker, triage labels, or domain docs are missing.\n-- `grill-with-docs`: default for non-trivial Goals before PRD/issues; required for ambiguous plans, terminology, boundaries, ADR-worthy tradeoffs, or design-tree decisions.\n+- `grill-with-docs`: default for non-trivial Goals before PRD/issues; required for ambiguous plans, terminology, boundaries, decision-record-worthy tradeoffs, or design-tree decisions.\n - `zoom-out`: unfamiliar code area or missing architectural map.\n - `to-prd`: convert settled context into a PRD.\n - `to-issues`: break a PRD, plan, or Goal into independently grabbable vertical-slice issues.\ndiff --git a/docs/adr/0001-keep-shiki-as-platform-name.md b/docs/adr/SADR-0001-keep-shiki-as-platform-name.md\nindex 20a9637c3b9d8c97de7ba8db7cfae4d82d9a7dba..8b685439959aada1b221c112f0ac3c51ca8a1695\n--- a/docs/adr/0001-keep-shiki-as-platform-name.md\n+++ b/docs/adr/SADR-0001-keep-shiki-as-platform-name.md\n@@ -1,3 +1,3 @@\n-# Keep Shiki As The Platform Name\n+# SADR-0001: Keep Shiki As The Platform Name\n \n Shiki remains the product name for the GitHub-native agentic engineering control plane. We considered more descriptive names such as MergePilot, MergeGate, and Kiroku, but kept Shiki because it carries the existing goal-seeking framework identity while remaining independent of Hermes, Codex, Claude Code, or any single runtime.\ndiff --git a/docs/adr/0002-use-agents-as-shared-constitution.md b/docs/adr/SADR-0002-use-agents-as-shared-constitution.md\nindex 73de30ecb078120eac540d9bc51855e3cd48d0e3..c5486182bf06fca36ea508496b013c67302fd989\n--- a/docs/adr/0002-use-agents-as-shared-constitution.md\n+++ b/docs/adr/SADR-0002-use-agents-as-shared-constitution.md\n@@ -1,3 +1,3 @@\n-# Use AGENTS As The Shared Agent Constitution\n+# SADR-0002: Use AGENTS As The Shared Agent Constitution\n \n Shiki uses `AGENTS.md` as the runtime-neutral constitution and `CLAUDE.md` as a thin Claude Code wrapper. This keeps Codex, Claude Code, Hermes Runner, and future Agent Runtimes aligned on the same GitHub-first MergeGate rules while still allowing runtime-specific instructions where needed.\ndiff --git a/docs/adr/0003-require-skill-gate-for-mergegate-readiness.md b/docs/adr/SADR-0003-require-skill-gate-for-mergegate-readiness.md\nindex 424be0c9af4c8f0136f1fde68a468c47ff84e764..7fc337987c7df9792f75f181dfcbed75fe5ffff2\n--- a/docs/adr/0003-require-skill-gate-for-mergegate-readiness.md\n+++ b/docs/adr/SADR-0003-require-skill-gate-for-mergegate-readiness.md\n@@ -1,3 +1,3 @@\n-# Require Skill Gate For MergeGate Readiness\n+# SADR-0003: Require Skill Gate For MergeGate Readiness\n \n Shiki requires relevant engineering skills to be invoked before execution when their triggers apply. This makes skills part of MergeGate readiness rather than optional prompt style, because Shiki depends on repeatable planning, issue decomposition, TDD, diagnosis, triage, and architecture review before autonomous or parallel agent work can be trusted.\ndiff --git a/docs/adr/0004-use-github-first-with-shiki-mirror.md b/docs/adr/SADR-0004-use-github-first-with-shiki-mirror.md\nindex d0a3a9174dc646a945a9d325954ffaf00c352040..a79c54ae9ee8eed2a0524192b864da4e22a92d3f\n--- a/docs/adr/0004-use-github-first-with-shiki-mirror.md\n+++ b/docs/adr/SADR-0004-use-github-first-with-shiki-mirror.md\n@@ -1,4 +1,4 @@\n-# ADR 0004: Use GitHub First With A Repository-Local Shiki Mirror\n+# SADR-0004: Use GitHub First With A Repository-Local Shiki Mirror\n \n ## Status\n \ndiff --git a/docs/adr/0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md b/docs/adr/SADR-0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\nindex b339baf49b89d9290e79c3d51b36e7b2bde3bd95..0f3c3eb9afccdb3403ce978e21d04fe12e495da0\n--- a/docs/adr/0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\n+++ b/docs/adr/SADR-0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\n@@ -1,8 +1,8 @@\n-# ADR 0005: Use Codex As Front And Claude Code Action As The GitHub Runtime\n+# SADR-0005: Use Codex As Front And Claude Code Action As The GitHub Runtime\n \n ## Status\n \n-Accepted (implementer default superseded by ADR 0008; the auth model and the\n+Accepted (implementer default superseded by SADR-0008; the auth model and the\n Claude Code Action GitHub runtime decision remain in force)\n \n ## Context\n@@ -26,4 +26,4 @@\n - Shiki avoids requiring `OPENAI_API_KEY` for the default workflow.\n - Codex work happens through the user's authenticated Codex front surface, not as a hidden backend Action.\n - Claude Code Action is configured for OAuth-token based subscription usage by default.\n-- API-key based automation remains possible only as an explicit target-repo extension with its own ADR.\n+- API-key based automation remains possible only as an explicit target-repo extension with its own target ADR.\ndiff --git a/docs/adr/0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md b/docs/adr/SADR-0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\nindex 4bcae9d1e52f0a37b8e9fbafa5cd14b6ee1993bd..7a38538a6ae9929567f0ade67e7ea6a60d0b3f7e\n--- a/docs/adr/0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\n+++ b/docs/adr/SADR-0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\n@@ -1,4 +1,4 @@\n-# ADR 0006: Separate Codex Implementation, CCA Completion, And MergeGate Authorization\n+# SADR-0006: Separate Codex Implementation, CCA Completion, And MergeGate Authorization\n \n ## Status\n \ndiff --git a/docs/adr/0007-packaging-and-release.md b/docs/adr/SADR-0007-packaging-and-release.md\nindex 79b84ddb97167372b44641e5b7860e400ce80822..bb445a2e2e856d9d1f5fdbe35de8d4e8cec0fb32\n--- a/docs/adr/0007-packaging-and-release.md\n+++ b/docs/adr/SADR-0007-packaging-and-release.md\n@@ -1,4 +1,4 @@\n-# ADR 0007: Packaging And Release For Shiki\n+# SADR-0007: Packaging And Release For Shiki\n \n ## Status\n \n@@ -25,7 +25,7 @@\n   not introduce third-party runtime dependencies or break the shim.\n - Releases must run inside the operator's subscription-authenticated toolchain\n   and must not require extra secrets beyond the built-in `GITHUB_TOKEN`\n-  (consistent with ADR 0005's no-API-key-by-default stance).\n+  (consistent with SADR-0005's no-API-key-by-default stance).\n - `.shiki/manifest.json` already encodes the install surface (`install.include`,\n   `install.create_directories`, `install.exclude_from_commit`), and it must not\n   be mutated as part of this change.\n@@ -34,7 +34,7 @@\n \n This is a hard-to-reverse platform decision (it sets the version source of\n truth, the release trigger, and the public install contract), so it is recorded\n-as an ADR.\n+as a SADR.\n \n ## Decision\n \n@@ -104,7 +104,7 @@\n 3. **Publishing to PyPI.** Rejected for now: Shiki is distributed as a\n    repository template + git-installable CLI, not a PyPI library; PyPI would add\n    credentials/secrets and a different versioning contract. Can be added later as\n-   its own ADR if a packaged distribution is needed.\n+   its own SADR if a packaged distribution is needed.\n 4. **Dynamic version (read `VERSION` via a build backend `dynamic` field).**\n    Rejected to keep `pyproject.toml` backend-agnostic and dependency-free; the\n    small duplication between `VERSION` and `pyproject.toml` is guarded by the\n@@ -123,4 +123,4 @@\n   releasing versions the whole platform but ships only `install.include` to\n   targets.\n - Future PyPI distribution or a console entry point are deferred and would each\n-  warrant their own ADR.\n+  warrant their own SADR.\ndiff --git a/docs/adr/0008-use-claude-code-as-default-implementer.md b/docs/adr/SADR-0008-use-claude-code-as-default-implementer.md\nindex dedf4510b7c7799521422f622029563d373db031..35c2625d908167fbc58855b5b63e283805f167e8\n--- a/docs/adr/0008-use-claude-code-as-default-implementer.md\n+++ b/docs/adr/SADR-0008-use-claude-code-as-default-implementer.md\n@@ -1,12 +1,12 @@\n-# ADR 0008: Use Claude Code As The Default Implementer Runtime\n+# SADR-0008: Use Claude Code As The Default Implementer Runtime\n \n ## Status\n \n-Proposed (supersedes ADR 0005 for the implementer default; keeps its auth model)\n+Proposed (supersedes SADR-0005 for the implementer default; keeps its auth model)\n \n ## Context\n \n-ADR 0005 made Codex Front the default implementation runtime, with Claude Code\n+SADR-0005 made Codex Front the default implementation runtime, with Claude Code\n restricted to planner and reviewer roles by the runtime registry, and the only\n autonomous implementation adapter was `shiki runner codex`.\n \n@@ -18,13 +18,13 @@\n - The platform's planning, review, and CCA layers already run on Claude Code,\n   and Claude Code's native capabilities (the built-in `/code-review` skill and\n   the Workflow multi-agent orchestration tool) have no Codex equivalent.\n-- The Goal lifecycle redesign (ADR 0009) requires an autonomous post-freeze\n+- The Goal lifecycle redesign (SADR-0009) requires an autonomous post-freeze\n   loop driven from the operator's Claude Code session, including bounded\n   repair dispatch, which must not depend on a runtime that cannot be invoked\n   unattended.\n \n The constitution claims Shiki is runtime-agnostic (\"not a Claude-only\n-workflow\", ADR 0002). Any change to the implementer default must preserve the\n+workflow\", SADR-0002). Any change to the implementer default must preserve the\n registry-based runtime model rather than hardcoding one vendor.\n \n ## Decision\n@@ -39,7 +39,7 @@\n   the task to review or repair-needed.\n - Keep Codex registered as an optional implementer runtime. `shiki runner\n   codex`, the Codex handoff templates, and the ChatGPT OAuth auth model from\n-  ADR 0005 remain supported for tasks explicitly assigned to `codex`.\n+  SADR-0005 remain supported for tasks explicitly assigned to `codex`.\n - Commit to mandating two Claude-native capabilities as loop contract in\n   follow-up Goals (status: the code-review gate was delivered by Goal G-B \u2014\n   skill registry, checklists item PR-12, default required_skills \u2014 while the\n@@ -60,7 +60,7 @@\n \n Out of scope: removing Codex support, changing the CCA runtime (Claude Code\n Action with `CLAUDE_CODE_OAUTH_TOKEN` stays), and API-key based automation\n-(still requires its own ADR per ADR 0005).\n+(still requires its own target ADR per SADR-0005).\n \n ## Consequences\n \n@@ -100,7 +100,7 @@\n   harness rejection of direct `codex exec` is outside Shiki's control, and the\n   plan's mandatory native capabilities cannot run on Codex at all.\n - **Remove Codex entirely**: rejected; it abandons the runtime-agnostic\n-  constitution (ADR 0002) for no operational gain and forecloses\n+  constitution (SADR-0002) for no operational gain and forecloses\n   cross-runtime adversarial review.\n - **GitHub Actions cloud implementation via claude-code-action**: rejected as\n   default; durable evidence is automatic but token cost, Node 24 workflow\ndiff --git a/docs/adr/0009-requirements-definition-and-spec-freeze.md b/docs/adr/SADR-0009-requirements-definition-and-spec-freeze.md\nindex 29b3f7871bef9203a8b4a831a65c39eb2f506e42..52e7dd69ad0aa4efe0b68c62678abbf7df02aaec\n--- a/docs/adr/0009-requirements-definition-and-spec-freeze.md\n+++ b/docs/adr/SADR-0009-requirements-definition-and-spec-freeze.md\n@@ -1,4 +1,4 @@\n-# ADR 0009: Merge Grill And Planning Into Requirements Definition With Spec Freeze\n+# SADR-0009: Merge Grill And Planning Into Requirements Definition With Spec Freeze\n \n ## Status\n \ndiff --git a/docs/adr/0010-external-ai-guardian-review.md b/docs/adr/SADR-0010-external-ai-guardian-review.md\nindex e6108bbe8bb4e73e0ff4684c8f78001a80438c4e..f3510b145664298adafb151b6f0db51b972f01ec\n--- a/docs/adr/0010-external-ai-guardian-review.md\n+++ b/docs/adr/SADR-0010-external-ai-guardian-review.md\n@@ -1,4 +1,4 @@\n-# ADR 0010: External AI Guardian Review As A First-Class Approval Authority\n+# SADR-0010: External AI Guardian Review As A First-Class Approval Authority\n \n ## Status\n \n@@ -8,7 +8,7 @@\n \n Shiki is intended to be an autonomous, recursive, self-improving agent\n framework: authority-in-the-loop, not human-in-the-loop. Until now the Guardian\n-approval required for high/critical-risk merges (guardian-policy.json, ADR\n+approval required for high/critical-risk merges (guardian-policy.json, SADR\n governance) recognized only human authority sources \u2014 a GitHub review approval,\n the `guardian:approved` label applied by a configured Guardian, or a comment\n carrying `Guardian approval granted` plus the current head SHA.\n@@ -89,7 +89,7 @@\n   autonomous design the operator intends and produced the impersonation\n   incident.\n - **Record AI approval as an operator comment**: rejected as forgery of human\n-  authority \u2014 the exact failure this ADR corrects.\n+  authority \u2014 the exact failure this SADR corrects.\n - **Deliver the artifact as a committed `.shiki/` file**: rejected; a committed\n   approval inside the PR being approved is forgeable by anyone who can push.\n - **A dedicated reviewer bot identity instead of a relayed comment**: deferred;\ndiff --git a/docs/adr/0011-autonomous-quality-gate-evidence.md b/docs/adr/SADR-0011-autonomous-quality-gate-evidence.md\nindex bca0246c5d46e44afbde15486a75eec425fc02f5..9c93eac16d8c3a6d4b11bd7224e3955102cd7db4\n--- a/docs/adr/0011-autonomous-quality-gate-evidence.md\n+++ b/docs/adr/SADR-0011-autonomous-quality-gate-evidence.md\n@@ -1,4 +1,4 @@\n-# ADR 0011: Autonomous Quality-Gate Evidence \u2014 Deterministic Facts And An Independent Verifier\n+# SADR-0011: Autonomous Quality-Gate Evidence \u2014 Deterministic Facts And An Independent Verifier\n \n ## Status\n \ndiff --git a/docs/adr/0012-autonomous-loop-closeout-to-main.md b/docs/adr/SADR-0012-autonomous-loop-closeout-to-main.md\nindex 637fad6dde4989c9c2bf0f81f676d8d29991123c..ec7d85f81030a676bcbba28b43eb365422a36eac\n--- a/docs/adr/0012-autonomous-loop-closeout-to-main.md\n+++ b/docs/adr/SADR-0012-autonomous-loop-closeout-to-main.md\n@@ -1,4 +1,4 @@\n-# 0012 \u2014 Autonomous loop pushes goal completion to main via a closeout PR\n+# SADR-0012 \u2014 Autonomous loop pushes goal completion to main via a closeout PR\n \n - Status: accepted\n - Date: 2026-06-18\ndiff --git a/docs/adr/0013-bootstrap-grants-actions-approve-pull-requests.md b/docs/adr/SADR-0013-bootstrap-grants-actions-approve-pull-requests.md\nindex b6c69d64f1c5ced346b88d6859586715b3b5bd0e..cf48ff4a6716267f539ca58edd1a852e7c7a2d64\n--- a/docs/adr/0013-bootstrap-grants-actions-approve-pull-requests.md\n+++ b/docs/adr/SADR-0013-bootstrap-grants-actions-approve-pull-requests.md\n@@ -1,4 +1,4 @@\n-# ADR 0013: Bootstrap Grants GitHub Actions Permission To Create And Approve Pull Requests\n+# SADR-0013: Bootstrap Grants GitHub Actions Permission To Create And Approve Pull Requests\n \n ## Status\n \n@@ -31,7 +31,7 @@\n security tradeoff: it lets repository automation satisfy a human-review gate. In\n a multi-maintainer repo that could let a compromised or buggy workflow\n self-approve changes. So whether bootstrap turns it on by default \u2014 and with\n-what default token scope \u2014 is an ADR-worthy decision, not an implementation\n+what default token scope \u2014 is a SADR-worthy decision, not an implementation\n detail.\n \n ## Decision\n@@ -48,7 +48,7 @@\n Specifically:\n \n - **`can_approve_pull_request_reviews` defaults to `true`.** Shiki's default\n-  posture is solo/AFK autonomous operation (ADR 0008/0009/0012), where the CCA\n+  posture is solo/AFK autonomous operation (SADR-0008/0009/0012), where the CCA\n   Review Bridge is the only path that can satisfy `required_review: true` after\n   the CCA and Guardian gates have already passed. The default makes a\n   freshly-bootstrapped repo able to run the autonomous loop end to end without a\ndiff --git a/docs/adr/0014-codex-app-external-ai-guardian-ui-adapter.md b/docs/adr/SADR-0014-codex-app-external-ai-guardian-ui-adapter.md\nindex 238db06279334caa341c117362df9271382a7fcd..65f12e9939321e1b3e389a34c822fc1549e5155d\n--- a/docs/adr/0014-codex-app-external-ai-guardian-ui-adapter.md\n+++ b/docs/adr/SADR-0014-codex-app-external-ai-guardian-ui-adapter.md\n@@ -1,4 +1,4 @@\n-# ADR 0014: Use Codex App As The External AI Guardian UI Adapter\n+# SADR-0014: Use Codex App As The External AI Guardian UI Adapter\n \n ## Status\n \ndiff --git a/docs/adr/0015-contract-approval-before-dispatch.md b/docs/adr/SADR-0015-contract-approval-before-dispatch.md\nindex fad6c656741a20a7684ba16080a7db159f63a52f..6061271a46d3f1d6ae7174477de3fb75673123a6\n--- a/docs/adr/0015-contract-approval-before-dispatch.md\n+++ b/docs/adr/SADR-0015-contract-approval-before-dispatch.md\n@@ -1,4 +1,4 @@\n-# ADR 0015: Approve The Task Contract Before Dispatch, Not The Diff After It\n+# SADR-0015: Approve The Task Contract Before Dispatch, Not The Diff After It\n \n ## Status\n \n@@ -9,7 +9,7 @@\n Guardian approval for high/critical risk is expressible only on a pull request.\n Every approval source in `guardian-policy.json` \u2014 GitHub review, the\n `guardian:approved` label, the `Guardian approval granted` comment, and the\n-`external_ai_guardian_review` artifact (ADR 0010/0014) \u2014 is bound to a PR, and\n+`external_ai_guardian_review` artifact (SADR-0010/0014) \u2014 is bound to a PR, and\n three of the four require the current head SHA. Approval therefore cannot exist\n before an implementation PR exists.\n \n@@ -81,7 +81,7 @@\n An envelope breach must surface only on the non-repairable policy gate, never on\n the repairable `MergeGate metadata check`. Otherwise the Goal Loop routes it to\n `dispatch_repair` \u2014 instructing a runner to make the breach go away \u2014 which is\n-exactly what this ADR forbids.\n+exactly what this SADR forbids.\n \n **Contract immutability.** After registration, an implementation PR must not\n change the governance fields of its own task contract (`scope`, `non_goals`,\n@@ -116,7 +116,7 @@\n fails any verdict that is not `complete`, and the signal the CCA prompt reads is\n computed from `evaluate_guardian_approval` alone. Without the OR-merge at the\n signal, a contract-approved PR still returns `needs_guardian`, the CCA job fails,\n-and the MergeGate policy check never runs \u2014 the gate this ADR changes would never\n+and the MergeGate policy check never runs \u2014 the gate this SADR changes would never\n be reached. The CCA *prompt* needs no change: a `contract_approval` entry in the\n signal's existing `sources` array satisfies its current wording.\n \n@@ -130,7 +130,7 @@\n written, and an approved contract lets its implementation PRs merge through the\n same automatic path as low/medium risk. The wasted implement-then-block cycle\n disappears. Guardian review shifts from reading diffs to reading contracts, which\n-is both earlier and smaller; the external AI Guardian path (ADR 0010/0014) works\n+is both earlier and smaller; the external AI Guardian path (SADR-0010/0014) works\n on contracts with no change.\n \n We accept that the Guardian authorizes a class of change without seeing its\n@@ -146,7 +146,7 @@\n adversarial tests for the new approval path equivalent in rigor to\n `test_shiki_governance_evidence.sh`.\n \n-Bootstrap costs, which apply only while this ADR is being implemented. The tasks\n+Bootstrap costs, which apply only while this SADR is being implemented. The tasks\n that build Contract Approval merge under the existing rules, so the ones\n dispatched after the pre-dispatch gate lands but before the approval path lands\n need a Guardian-approved Contract PR *and* a live Guardian approval on their\n@@ -210,7 +210,7 @@\n risk; they do not bound authorized work.\n \n **Auto-escalate risk on envelope breach instead of stopping.** Returns the\n-workflow to a diff-time Guardian decision \u2014 the exact behavior this ADR removes.\n+workflow to a diff-time Guardian decision \u2014 the exact behavior this SADR removes.\n \n **Resolve governance fields from the base snapshot only.** Rejected against the\n registration flow: implementation PRs add their own contract, so the base has no\ndiff --git a/docs/adr/0016-locks-bound-product-paths.md b/docs/adr/SADR-0016-locks-bound-product-paths.md\nindex b5e850f3f1f7f4ef6e7f9d1763a29a51bd300c45..46b40a9b99a7aae644c02bb458f5473ff0a90ce2\n--- a/docs/adr/0016-locks-bound-product-paths.md\n+++ b/docs/adr/SADR-0016-locks-bound-product-paths.md\n@@ -1,4 +1,4 @@\n-# ADR 0016: Locks bound product paths; the Shiki mirror is bound by record identity\n+# SADR-0016: Locks bound product paths; the Shiki mirror is bound by record identity\n \n ## Status\n \n@@ -6,8 +6,8 @@\n \n - Date: 2026-07-28\n - Deciders: operator (mizutani-140), Claude (planner)\n-- Related: ADR 0004 (GitHub-first with a `.shiki` mirror), ADR 0009 (Spec Freeze),\n-  ADR 0012 (autonomous loop closeout to main), ADR 0015 (Contract Approval before\n+- Related: SADR-0004 (GitHub-first with a `.shiki` mirror), SADR-0009 (Spec Freeze),\n+  SADR-0012 (autonomous loop closeout to main), SADR-0015 (Contract Approval before\n   dispatch), remediation goal task R-02, the live PR #179 deadlock.\n \n ## Context\n@@ -90,11 +90,11 @@\n files, documentation files and named source modules are nameable; runtime-\n allocated ledger, runner and report ids are not \u2014 and those live in `.shiki`,\n which locks no longer govern. Existing frozen contracts are brought into line by\n-a one-time Spec Amendment per plan, which must land before ADR 0015's contract\n+a one-time Spec Amendment per plan, which must land before SADR-0015's contract\n immutability makes `locks` unwritable from the implementation PR.\n \n **Lock state is resolved from the durable side.** MergeGate resolves the lock\n-registry, and ADR 0015's governance fields, from the base `.shiki` snapshot when\n+registry, and SADR-0015's governance fields, from the base `.shiki` snapshot when\n present, falling back to head only when absent. The `MergeGate metadata check`\n job is given `--base-shiki`, which it does not receive today. Locks are not\n derived from the live open-pull-request set: a pull request body is untrusted\n@@ -108,7 +108,7 @@\n Explicitly out of scope: moving MergeGate's contract resolution off the PR\n checkout wholesale; making the mirror a projection of GitHub state; introducing\n new lock kinds; moving ledger evidence out of the repository; and unwinding\n-ADR 0012's closeout pull request. Registration of multi-task goals is ADR 0015's\n+SADR-0012's closeout pull request. Registration of multi-task goals is SADR-0015's\n Contract PR and is not re-decided here.\n \n ## Consequences\n@@ -127,7 +127,7 @@\n \n Risks accepted. First, goal-scoped identity is only as strong as `goal_id`,\n which is resolved from the PR body and the head task file; the mitigation is to\n-add `goal_id` to ADR 0015's frozen governance field set, and until that lands a\n+add `goal_id` to SADR-0015's frozen governance field set, and until that lands a\n task file can name a foreign goal. Second, base-when-present resolution is\n unavailable on fork pull requests, where the head is contributor-controlled;\n fork pull requests already cannot complete a Shiki merge, and this decision does\n@@ -139,7 +139,7 @@\n path, not by implementation pull requests.\n \n New obligations: one Spec Amendment per frozen plan to rewrite `locks`, ordered\n-before ADR 0015 T1; extension of the adversarial suites in\n+before SADR-0015 T1; extension of the adversarial suites in\n `scripts/test_shiki_adversarial_state.sh` and\n `scripts/test_shiki_governance_evidence.sh` to cover the eleven newly-scoped\n directories; and updating `scripts/test_shiki_loop_lock_guard.sh`,\n@@ -151,32 +151,32 @@\n whether the loop's transitive sync of ledger `evidence` references can be\n deleted once the Contract PR carries its own source plan.\n \n-## Relationship to ADR 0004\n+## Relationship to SADR-0004\n \n-This ADR **clarifies something ADR 0004 left unspecified**. ADR 0004 decides that\n+This SADR **clarifies something SADR-0004 left unspecified**. SADR-0004 decides that\n GitHub is the operational source of truth and that each target repository keeps a\n `.shiki/` mirror. It says nothing about pull requests writing the mirror, does\n not name MergeGate, and does not make the mirror an input to a gate. Nothing in\n-ADR 0004's Decision or Consequences is amended, superseded or weakened: the\n+SADR-0004's Decision or Consequences is amended, superseded or weakened: the\n mirror still exists, still records the same artifacts, and pull request\n-boundaries remain the coordination surface. What this ADR settles is the question\n+boundaries remain the coordination surface. What this SADR settles is the question\n 0004 never reached \u2014 by what authority a pull request may write a mirror record \u2014\n and it answers it the way `.shiki/manifest.json` already declares\n (`pr_mutation: current-task-or-current-goal-only`).\n \n-It **amends ADR 0012**, whose Decision item 5 states that \"Loop-executed tasks\n+It **amends SADR-0012**, whose Decision item 5 states that \"Loop-executed tasks\n declare the `path:.shiki/**` lock, which covers every `.shiki` file the closeout\n stages ... so `files_outside_locks` is satisfied without per-file lock\n bookkeeping.\" That clause is replaced: the closeout's mirror writes are\n-authorized by record identity, not by a declared lock. ADR 0012's substantive\n+authorized by record identity, not by a declared lock. SADR-0012's substantive\n decision \u2014 that completion reaches `main` through a closeout pull request rather\n than local attestation \u2014 is preserved unchanged.\n \n-It is **complementary to ADR 0015**, not a substitute. ADR 0015 owns contract\n+It is **complementary to SADR-0015**, not a substitute. SADR-0015 owns contract\n immutability, the pre-dispatch gate, the Contract PR and the Scope Envelope. This\n-ADR owns what a lock means and who may write the mirror. The one ordering\n+SADR owns what a lock means and who may write the mirror. The one ordering\n constraint between them is that the lock-narrowing Spec Amendment must precede\n-ADR 0015 T1's freezing of the `locks` field.\n+SADR-0015 T1's freezing of the `locks` field.\n \n ## Alternatives Considered\n \n@@ -186,7 +186,7 @@\n the only writer that can register them may run. Migrating the Guardian signal to\n that store makes every registration PR fail closed to `needs_guardian` at every\n risk level, because the task file is by definition absent from the store, which\n-removes the low/medium autonomous path ADR 0008/0009 exists to provide. And it\n+removes the low/medium autonomous path SADR-0008/0009 exists to provide. And it\n leaves schemas, manifest, config, guardian policy, templates, migrations, plans,\n worktrees, runner records, runs and handoffs with no writer at all in a\n repository whose product is `.shiki`. Measured benefit on the frozen backlog:\n@@ -199,14 +199,14 @@\n namespace is not in fact ownership-partitioned \u2014 56 ledger entries carry no\n `task_id`, and one goal DAG is writable by 35 registered tasks. The correct\n expression of its insight is that locks do not govern the mirror at all, which is\n-what this ADR decides.\n+what this SADR decides.\n \n **Make the mirror a projection written only by a registration writer and a\n post-merge projection job.** Rejected. It requires a bypass actor with write\n access to the protected default branch; it makes its own master invariant\n readable from a manifest that pull requests may edit; it deletes both reconcile\n modes and leaves a corrupted mirror with no in-band repair path, contradicting\n-ADR 0004's consequence that mirror conflicts are repaired against GitHub state;\n+SADR-0004's consequence that mirror conflicts are repaired against GitHub state;\n and its content-derived ids are rejected by `validate_shiki`'s currently-green id\n pattern.\n \ndiff --git a/docs/adr/0017-bookkeeping-closeouts-do-not-inherit-risk.md b/docs/adr/SADR-0017-bookkeeping-closeouts-do-not-inherit-risk.md\nindex 554e3a0d526a72ca067108c5464cc68c3edcf174..b9676857a4713850e063af7c39c8e0b01ecb4c66\n--- a/docs/adr/0017-bookkeeping-closeouts-do-not-inherit-risk.md\n+++ b/docs/adr/SADR-0017-bookkeeping-closeouts-do-not-inherit-risk.md\n@@ -1,4 +1,4 @@\n-# ADR 0017: A Bookkeeping Closeout Does Not Inherit Its Task's Risk\n+# SADR-0017: A Bookkeeping Closeout Does Not Inherit Its Task's Risk\n \n ## Status\n \n@@ -6,7 +6,7 @@\n \n ## Context\n \n-ADR 0012 made the goal loop push a task's completion to the default branch through a\n+SADR-0012 made the goal loop push a task's completion to the default branch through a\n closeout pull request: after the implementation PR merges, the loop opens a second PR\n carrying `task.status = done`, `lock.state = released`, and \u2014 when the task completes\n its goal \u2014 `goal.status = complete` plus one scorecard report. Nothing else. The\n@@ -49,14 +49,14 @@\n 4. The task file's governance fields (`scope`, `non_goals`, `required_skills`,\n    `risk_level`, `locks`, `acceptance_checks`, `test_command`) are byte-identical to\n    the base snapshot. Only `status`, `expected_pr`, `closeout_pr`, `ledger_evidence`\n-   and `expected_branch` may differ \u2014 the ADR 0015 contract-immutability rule,\n+   and `expected_branch` may differ \u2014 the SADR-0015 contract-immutability rule,\n    unchanged.\n \n    `expected_branch` is in that set because a closeout is *by construction* on a\n    different branch from the implementation it closes, and the loop rewrites the\n    field accordingly. It is safe to let it differ because MergeGate independently\n    requires `expected_branch` to equal the PR's actual head ref, so a PR cannot set\n-   it to anything other than the branch it is really on. The first draft of this ADR\n+   it to anything other than the branch it is really on. The first draft of this SADR\n    omitted it, which made the exemption unreachable: every real closeout failed\n    condition 4 on a field the loop always changes. See PR #209.\n 5. The state transitions are exactly the terminal set: task `review` \u2192 `done`, lock\n@@ -113,12 +113,12 @@\n classification must be derived, not asserted.\n \n **Fold the closeout into the implementation PR** so no second PR exists. Rejected under\n-ADR 0012, which recorded why: a task file that is already `done` on the implementation\n+SADR-0012, which recorded why: a task file that is already `done` on the implementation\n branch short-circuits the loop's review \u2192 CCA \u2192 merge \u2192 done ordering into a local\n goal-completion before any merge happens.\n \n **Have the loop auto-merge closeouts with elevated credentials.** Rejected outright: that\n-is a runtime authoring its own approval, which ADR 0010 and the constitution's\n+is a runtime authoring its own approval, which SADR-0010 and the constitution's\n approval-record prohibition forbid. It would also be indistinguishable, in the evidence,\n from the forgery those rules exist to prevent.\n \ndiff --git a/docs/adr/0018-carried-guardian-approval-across-a-proven-base-sync.md b/docs/adr/SADR-0018-carried-guardian-approval-across-a-proven-base-sync.md\nindex 22358562da390ed8b45cd600da08a43d378b4680..e6e2907cefa776460af73ddf7d39db605f3c39b0\n--- a/docs/adr/0018-carried-guardian-approval-across-a-proven-base-sync.md\n+++ b/docs/adr/SADR-0018-carried-guardian-approval-across-a-proven-base-sync.md\n@@ -1,4 +1,4 @@\n-# ADR 0018: Carried Guardian Approval Across A Proven Base Sync\n+# SADR-0018: Carried Guardian Approval Across A Proven Base Sync\n \n ## Status\n \n@@ -34,7 +34,7 @@\n Guardian evaluator that consumes it (`shiki_guardian.evaluate_guardian_approval`,\n the `guardian_comment_carried` source) were built and tested in isolation. Neither\n does anything until a caller passes the flags that turn the carry on, so the\n-feature was invisible in CI. This ADR records the decision the two of them\n+feature was invisible in CI. This SADR records the decision the two of them\n implement, and this task wires it into the four CI invocations that judge Guardian\n approval.\n \ndiff --git a/docs/adr/README.md b/docs/adr/README.md\nindex c70e057a4520c36e042ab68c53cd4a21ef709b71..5da43856836e2ce6a150f8127766e2f88355ab4b\n--- a/docs/adr/README.md\n+++ b/docs/adr/README.md\n@@ -1,40 +1,41 @@\n-# Architecture Decision Records\n+# Decision Record Namespaces\n \n-This directory holds Shiki's Architecture Decision Records (ADRs): durable\n-records of hard-to-reverse platform decisions. ADRs are part of the Shiki source\n-of truth alongside GitHub state, the `.shiki/` mirror, and `CONTEXT.md`.\n+This directory holds two independent decision namespaces:\n \n-## How ADRs Work\n+- Shiki Architecture Decision Records (SADRs), named\n+  `SADR-NNNN-<short-slug>.md`, record hard-to-reverse Shiki platform decisions.\n+- Target Architecture Decision Records (ADRs), named\n+  `NNNN-<short-slug>.md`, belong to the target product.\n \n-- New ADRs are added as `docs/adr/NNNN-<short-slug>.md`, where `NNNN` is the next\n-  zero-padded sequence number.\n-- The **canonical list of ADRs is the set of files matching `docs/adr/*.md`** in\n-  this directory. Browse `docs/adr/*.md` for the authoritative, current set.\n-- Use [`template.md`](template.md) as the starting point for a new ADR.\n-- Record an ADR when a decision is hard to reverse, surprising without context,\n-  and a real tradeoff. Do not write an ADR for routine or easily reversible\n-  choices.\n+The same four-digit number may appear once in each namespace. A platform\n+`SADR-NNNN` citation resolves only to `SADR-NNNN-*.md`; a target `ADR NNNN`\n+citation resolves only to `NNNN-*.md`.\n \n-Using the `docs/adr/*.md` glob as the source of truth means other PRs can add an\n-ADR without editing this index file, avoiding merge contention on a\n-hand-maintained enumeration.\n+## How Decision Records Work\n \n-## Current ADRs (snapshot)\n-\n-The list below is a convenience snapshot and may lag behind the directory. When\n-in doubt, list `docs/adr/*.md`.\n+- New Shiki platform decisions use the next zero-padded SADR number.\n+- New target product decisions use the target's own ADR sequence.\n+- The canonical list is the files in `docs/adr/`; no hand-maintained index is\n+  authoritative.\n+- Use [`template.md`](template.md) as the starting point and choose the filename\n+  namespace that owns the decision.\n+- Record a decision when it is hard to reverse, surprising without context, and\n+  a real tradeoff. Do not write a decision record for routine or easily\n+  reversible choices.\n \n-- [0001 \u2014 Keep Shiki As The Platform Name](0001-keep-shiki-as-platform-name.md)\n-- [0002 \u2014 Use AGENTS As The Shared Agent Constitution](0002-use-agents-as-shared-constitution.md)\n-- [0003 \u2014 Require Skill Gate For MergeGate Readiness](0003-require-skill-gate-for-mergegate-readiness.md)\n-- [0004 \u2014 Use GitHub First With A Repository-Local Shiki Mirror](0004-use-github-first-with-shiki-mirror.md)\n-- [0005 \u2014 Use Codex As Front And Claude Code Action As The GitHub Runtime](0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md)\n-- [0006 \u2014 Separate Codex Implementation, CCA Completion, And MergeGate Authorization](0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md)\n+Using the directory as the source of truth lets independent changes add records\n+without contending on an enumerated index.\n \n-## Adding A New ADR\n+## Adding A Shiki SADR\n \n-1. Copy `template.md` to `docs/adr/NNNN-<short-slug>.md`.\n+1. Copy `template.md` to `docs/adr/SADR-NNNN-<short-slug>.md`.\n 2. Fill in the title, status, context, decision, and consequences.\n-3. Set the status to `Proposed`, then `Accepted` once the decision is approved.\n-4. Open a PR following `CONTRIBUTING.md`. You do not need to edit this index for\n-   the ADR to be discoverable via `docs/adr/*.md`.\n+3. Set the status to `Proposed`, then `Accepted` once the proper authority has\n+   approved the decision.\n+4. Open a PR following `CONTRIBUTING.md`.\n+\n+## Adding A Target ADR\n+\n+1. Copy `template.md` to `docs/adr/NNNN-<short-slug>.md` using the target's own\n+   sequence.\n+2. Cite it as `ADR NNNN`; do not prefix its filename with `SADR-`.\ndiff --git a/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md b/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\nnew file mode 100644\nindex 0000000000000000000000000000000000000000..7455ce1f3bed02ddddb851ac21a1d1a648c9aeba\n--- /dev/null\n+++ b/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\n@@ -0,0 +1,55 @@\n+# SADR-0019: Separate Shiki Platform SADRs From Target ADRs\n+\n+## Status\n+\n+Accepted\n+\n+## Context\n+\n+Shiki installs its platform decision records into `docs/adr/` in every target\n+repository. Shiki and its targets previously used the same numeric\n+`NNNN-<slug>.md` namespace. A target that already owned ADRs 0013 through 0018\n+therefore acquired two records for each number during installation, making an\n+`ADR NNNN` citation ambiguous. Renumbering target decisions would rewrite target\n+history, while renumbering Shiki decisions per target would destroy their stable\n+platform identity.\n+\n+Upgrades also need to remove the former Shiki-owned numeric paths without ever\n+mistaking a target-owned ADR for platform content. The install stamp already\n+records exact shipped paths and content digests, so it can prove ownership only\n+when both still match.\n+\n+## Decision\n+\n+Shiki platform decisions use the `SADR` namespace:\n+\n+- records are named `docs/adr/SADR-NNNN-<slug>.md`;\n+- live platform citations use `SADR-NNNN`;\n+- target product decisions retain `docs/adr/NNNN-<slug>.md` and `ADR NNNN`;\n+- the reference validator resolves the two namespaces independently, so the same\n+  four-digit number may exist once in each namespace.\n+\n+Before a forced upgrade writes anything, the installer inventories the exact\n+former numeric Shiki ADR paths. It may delete a legacy path only when the\n+existing install stamp contains that exact path and its recorded SHA-256 digest\n+matches the file currently on disk. All ownership blockers are reported\n+together and abort the upgrade before its first write. A non-forced install\n+never performs this cleanup.\n+\n+Historical `.shiki/` evidence remains unchanged because it records the namespace\n+that existed when that evidence was produced.\n+\n+## Consequences\n+\n+- Targets may use any ADR number without colliding with Shiki's platform SADRs.\n+- Fresh installs preserve target ADR bytes and install a stable SADR set.\n+- Forced upgrades fail closed when legacy ownership cannot be proven, and a\n+  successful upgrade refreshes the stamp without removed legacy paths.\n+- Current governance, packaging, workflow, prompt, script, and skill references\n+  must move with the platform records.\n+\n+## Alternatives Considered\n+\n+- Collision-only refusal leaves affected targets permanently unupgradable.\n+- Renumbering target ADRs mutates target-owned history.\n+- Per-target Shiki renumbering makes platform citations non-portable.\ndiff --git a/docs/adr/template.md b/docs/adr/template.md\nindex a92a97daddd0b00ab9dd626cc8d86942da2eafed..17dd79175b5fee2105935557f691b40cf9492942\n--- a/docs/adr/template.md\n+++ b/docs/adr/template.md\n@@ -1,14 +1,14 @@\n-# NNNN: <Short Decision Title>\n+# <SADR-NNNN or NNNN>: <Short Decision Title>\n \n ## Status\n \n-Proposed | Accepted | Superseded by ADR-NNNN\n+Proposed | Accepted | Superseded by SADR-NNNN or target ADR NNNN\n \n ## Context\n \n What is the situation, constraint, or force that requires a decision? Reference\n-relevant domain terms (`CONTEXT.md`), prior ADRs, and code reality. State the\n-problem, not the solution.\n+relevant domain terms (`CONTEXT.md`), prior Shiki SADRs or target ADRs, and code\n+reality. State the problem, not the solution.\n \n ## Decision\n \ndiff --git a/docs/agents/autonomous-loop-e2e-acceptance.md b/docs/agents/autonomous-loop-e2e-acceptance.md\nindex bbf84c6081987f159ac6ad9ecf3c82b7aa72e7e8..5c13c1d02a05a90b0dc46cbef7c95a9846d0c237\n--- a/docs/agents/autonomous-loop-e2e-acceptance.md\n+++ b/docs/agents/autonomous-loop-e2e-acceptance.md\n@@ -40,8 +40,8 @@\n 3. `gh auth status` is logged in with merge permission on the repo.\n 4. Branch protection + required checks are configured exactly as the goal-loop\n    required-check set expects (see `docs/agents/branch-protection-smoke.md`).\n-5. The repo `.shiki/policy` permits autonomous low/medium-risk merges (ADR\n-   0008/0009); the MergeGate policy check is the recorded-authority gate.\n+5. The repo `.shiki/policy` permits autonomous low/medium-risk merges\n+   (SADR-0008/0009); the MergeGate policy check is the recorded-authority gate.\n \n The operator's only actions are: register the task and start the loop. **No\n manual pre-clearing** means after `shiki loop run` starts, the operator does not\ndiff --git a/docs/agents/bootstrap-command.md b/docs/agents/bootstrap-command.md\nindex 05f7f203a52cf412b9f97084a6e131ed0f785afd..f7aa0b734dfebee4a3b139335b66ee3415f48713\n--- a/docs/agents/bootstrap-command.md\n+++ b/docs/agents/bootstrap-command.md\n@@ -189,7 +189,9 @@\n ```\n \n Use this only for tests, fixtures, or explicit local-only template inspection.\n-Use `--force` only when you intentionally want to overwrite existing target files.\n+Use `--force` only when you intentionally want to refresh the target template.\n+Legacy numeric Shiki decision cleanup is stamp-and-digest authorized before any\n+write; target ADR paths are not cleanup candidates.\n \n ## Slash Command\n \ndiff --git a/docs/agents/checklists.md b/docs/agents/checklists.md\nindex 4a7f03ade346f53a1c1d82981f3fd744f9ab4547..0c5ec6c53dc7197b7b7a7d18e667b325babd6c45\n--- a/docs/agents/checklists.md\n+++ b/docs/agents/checklists.md\n@@ -32,18 +32,18 @@\n | ID | Item | Blocking |\n | --- | --- | --- |\n | GD-01 | Plan was challenged against `CONTEXT.md`. | blocking for non-trivial Goals |\n-| GD-02 | Relevant ADRs were read or absence was recorded. | blocking for non-trivial Goals |\n+| GD-02 | Relevant SADRs and target ADRs were read or absence was recorded. | blocking for non-trivial Goals |\n | GD-03 | Ambiguous terms were resolved or converted to HITL blockers. | blocking |\n | GD-04 | Edge scenarios were considered. | advisory |\n | GD-05 | Code reality was checked when code could answer. | blocking when applicable |\n | GD-06 | New domain terms were added to `CONTEXT.md` when needed. | blocking when applicable |\n-| GD-07 | ADR was created or explicitly skipped using the ADR criteria. | blocking when applicable |\n+| GD-07 | A SADR or target ADR was created or explicitly skipped using the decision-record criteria. | blocking when applicable |\n \n ## CI \u2014 Context & Impact Checklist\n \n | ID | Item | Blocking |\n | --- | --- | --- |\n-| CI-01 | Relevant docs and ADRs identified. | blocking |\n+| CI-01 | Relevant docs, SADRs, and target ADRs identified. | blocking |\n | CI-02 | Relevant modules, interfaces, seams, callers, and tests identified. | blocking |\n | CI-03 | Dependencies and blocked-by relationships identified. | blocking |\n | CI-04 | Candidate locks identified. | blocking |\n@@ -62,7 +62,7 @@\n | PRD-04 | Implementation decisions are recorded without volatile code detail. | blocking |\n | PRD-05 | Testing decisions are recorded. | blocking |\n | PRD-06 | Out-of-scope items are explicit. | blocking |\n-| PRD-07 | Relevant glossary/ADR links are present when applicable. | blocking |\n+| PRD-07 | Relevant glossary/SADR/target-ADR links are present when applicable. | blocking |\n \n ## SF \u2014 Spec Freeze Checklist\n \n@@ -98,7 +98,7 @@\n | H-02 | Branch/worktree target is present. | blocking |\n | H-03 | Scope and non-goals are present. | blocking |\n | H-04 | Dependencies and locks are present. | blocking |\n-| H-05 | Relevant docs, ADRs, modules, and tests are referenced. | blocking |\n+| H-05 | Relevant docs, SADRs, target ADRs, modules, and tests are referenced. | blocking |\n | H-06 | Required skills are stated. | blocking |\n | H-07 | TDD expectations are stated. | blocking when behavior work |\n | H-08 | Acceptance criteria are included. | blocking |\ndiff --git a/docs/agents/codex-handoff.md b/docs/agents/codex-handoff.md\nindex ede468f47cb277f69a857e8c45df703e4ed0151d..6f97bdffb67e44d7edea2c71e01ea28933c4eb6c\n--- a/docs/agents/codex-handoff.md\n+++ b/docs/agents/codex-handoff.md\n@@ -46,7 +46,7 @@\n \n ## Context\n - Domain terms:\n-- Relevant ADRs:\n+- Relevant SADRs and target ADRs:\n - Relevant docs:\n - Relevant modules/interfaces/seams:\n - Similar tests/prior art:\ndiff --git a/docs/agents/completion-check-agent.md b/docs/agents/completion-check-agent.md\nindex 13e4c035db35025f4e3cdcc5adfb24314ac4d521..61bfa1218cf36ca605250c91919e620f33238aa0\n--- a/docs/agents/completion-check-agent.md\n+++ b/docs/agents/completion-check-agent.md\n@@ -32,7 +32,7 @@\n - `AGENTS.md`\n - `CLAUDE.md`\n - `CONTEXT.md`\n-- relevant ADRs\n+- relevant SADRs and target ADRs\n - parent Goal issue\n - PRD issue or document\n - task issue\ndiff --git a/docs/agents/decision-control.md b/docs/agents/decision-control.md\nindex 7d8ee5ab03a5ccbcbd04056ae61de03785a2a9c9..8c94ecda00fffaa0e92c325ab83aa9776bd1dac1\n--- a/docs/agents/decision-control.md\n+++ b/docs/agents/decision-control.md\n@@ -11,7 +11,7 @@\n | Decision | Owner | When | Required inputs | Output | Enforcement surface |\n | --- | --- | --- | --- | --- | --- |\n | Is the Goal clear enough to plan? | Planner + Guardian when needed | Goal intake | User request, existing docs, risk signals | Goal readiness verdict | Goal checklist, issue template, ledger |\n-| Must `grill-with-docs` run? | Planner / Skill Gate | Before PRD or issue split | Ambiguity, domain terms, ADR conflicts, unclear boundaries | Skill invocation or explicit skip rationale | Skill Gate checklist |\n+| Must `grill-with-docs` run? | Planner / Skill Gate | Before PRD or issue split | Ambiguity, domain terms, SADR/target-ADR conflicts, unclear boundaries | Skill invocation or explicit skip rationale | Skill Gate checklist |\n | Is a PRD required? | Planner | After Goal context stabilizes | Goal, `grill-with-docs` output, Context & Impact output | PRD required / not required | PRD checklist, CCA evidence check |\n | Can the spec be frozen? | Operator only | End of Requirements Definition, before issue split | PRD/requirements summary, scope inventory (SF-02), open-question list | `spec_freeze` block in the plan + ledger evidence | `require_grilled_plan` (plan ingest/run), SF checklist |\n | Can a frozen spec change? | Operator only (Spec Amendment) | When implementation contradicts the frozen spec | Contested decisions, scoped re-grill result | recorded amendment + re-stamped freeze | SF checklist, ledger evidence |\ndiff --git a/docs/agents/domain.md b/docs/agents/domain.md\nindex 258da8ea81e87ac0a6b0b5b5e5c2fde980eaabb7..d57b0cf17ff3e1fa9e6e5885372bf083d8295421\n--- a/docs/agents/domain.md\n+++ b/docs/agents/domain.md\n@@ -5,7 +5,9 @@\n ## Layout\n \n - `CONTEXT.md` defines Shiki language. Keep it implementation-free.\n-- `docs/adr/` records hard-to-reverse decisions.\n+- `docs/adr/SADR-NNNN-*.md` records Shiki platform decisions.\n+- `docs/adr/NNNN-*.md` records target product decisions without sharing the\n+  SADR namespace.\n - `AGENTS.md` is the runtime-neutral agent constitution.\n - `CLAUDE.md` is the Claude Code wrapper around `AGENTS.md`.\n - `.shiki/` is the Target Repository mirror for Goals, PRDs, plans, Task DAGs, contracts, locks, worktree records, ledger entries, CCA verdicts, repair packets, reports, and handoffs.\n@@ -15,6 +17,6 @@\n \n - Use Shiki terms exactly when they exist.\n - Add glossary terms to `CONTEXT.md` only when the term is part of the domain, not a generic programming concept.\n-- Create ADRs only for hard-to-reverse, surprising, tradeoff-based decisions.\n+- Create SADRs or target ADRs only for hard-to-reverse, surprising, tradeoff-based decisions.\n - Keep product-specific language in the Target Repository, not in Shiki Template docs.\n-- If an ADR conflicts with a proposed implementation, surface the conflict before editing.\n+- If a SADR or target ADR conflicts with a proposed implementation, surface the conflict before editing.\ndiff --git a/docs/agents/execution-control.md b/docs/agents/execution-control.md\nindex ad8ee3cdb36be56fedc1f46408ca92b387d74047..bc63c3a73661d12b567016ba299f1a6b66bf9088\n--- a/docs/agents/execution-control.md\n+++ b/docs/agents/execution-control.md\n@@ -63,7 +63,7 @@\n | Branch name | Orchestrator | Orchestrator | Before implementer dispatch | branch policy | branch ref in task/ledger |\n | Lock reservation | Context & Impact | MergeGate | Before dispatch and before merge | `.shiki/locks/`, labels, PR metadata | lock id, owner, scope |\n | CI profile | Planner | MergeGate | During issue triage, rechecked at PR | labels, `.shiki/policy.yaml`, workflow matrix | required check list |\n-| Required status checks | Repo maintainer / Guardian | GitHub branch protection / rulesets | Repository setup, updated via ADR | GitHub settings | branch/ruleset config, ADR |\n+| Required status checks | Repo maintainer / Guardian | GitHub branch protection / rulesets | Repository setup, updated via target ADR | GitHub settings | branch/ruleset config, target ADR |\n | CCA judgment | CCA | GitHub required check / MergeGate | Every non-draft PR update | GitHub Action check | structured CCA verdict |\n | Repair assignment | CCA | MergeGate | After failed CCA/CI/review | repair packet, issue/PR comment | repair id and attempt count |\n | Merge readiness | MergeGate | GitHub branch protection / Guardian if needed | After CCA complete | required checks, review rules, rulesets | mergegate check output |\ndiff --git a/docs/agents/guardian-policy.md b/docs/agents/guardian-policy.md\nindex 4147c7adf05a47adb4fd24ada1a1c2e1f2ba6d2d..a20d9cc6c00a569bb057b9ca8d6b51c7fc4a9847\n--- a/docs/agents/guardian-policy.md\n+++ b/docs/agents/guardian-policy.md\n@@ -98,8 +98,8 @@\n `origin/<default_branch>` and reuses each job's existing base fetch; a shallow\n (`--depth`) or second base fetch would make the proof unresolvable and silently\n disable the carry, so neither is permitted. CCA reads `guardian_comment_carried` as\n-a recorded authority in the deterministic signal and does not re-derive it. See ADR\n-0018 for the full decision, the rejected diff/patch-equality and path-disjointness\n+a recorded authority in the deterministic signal and does not re-derive it. See\n+SADR-0018 for the full decision, the rejected diff/patch-equality and path-disjointness\n alternatives, and the accepted residual.\n \n ## External AI Guardian Review (SADR-0010)\ndiff --git a/docs/agents/implementation-policy.md b/docs/agents/implementation-policy.md\nindex c754144a9fb5bf4a38aa82a60e1670c59e626b44..85c37281bcad196c6b7b02343faf00b03cd9c57a\n--- a/docs/agents/implementation-policy.md\n+++ b/docs/agents/implementation-policy.md\n@@ -31,10 +31,13 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n ## Non-Negotiables\n \n 1. **Planning precedes implementation.** Non-trivial Goals start with `grill-with-docs` and Context & Impact.\n-2. **Domain language is controlled.** Terms must be checked against `CONTEXT.md`; hard-to-reverse tradeoffs must be checked against ADRs.\n+2. **Domain language is controlled.** Terms must be checked against `CONTEXT.md`; hard-to-reverse tradeoffs must be checked against Shiki SADRs and target ADRs.\n 3. **PRD precedes issue decomposition.** `to-prd` is used once the context is settled enough to become durable intent.\n 4. **Issues are vertical slices.** `to-issues` must produce independently grabbable tracer-bullet tasks, not horizontal layer tickets.\n 5. **Triage controls dispatch.** Only `ready-for-agent` / AFK tasks may be assigned to Codex without human input.\n@@ -95,19 +98,19 @@\n Process:\n \n - Challenge terms against `CONTEXT.md`.\n-- Check ADRs before reopening decisions.\n+- Check SADRs and target ADRs before reopening decisions.\n - Inspect code when code can answer a question.\n - Ask one question at a time when the operator is available.\n - Provide a recommended answer with each question.\n - Record resolved terms in `CONTEXT.md`.\n-- Create ADRs only for hard-to-reverse, surprising, tradeoff-based decisions.\n+- Create SADRs or target ADRs only for hard-to-reverse, surprising, tradeoff-based decisions.\n - Convert unresolved load-bearing questions into HITL blockers.\n \n Outputs:\n \n - Resolved design-tree decisions.\n - Updated glossary terms when needed.\n-- ADRs when justified.\n+- SADRs or target ADRs when justified.\n - HITL blockers when decisions remain unresolved.\n \n ## Phase 3 \u2014 Context & Impact\n@@ -117,7 +120,7 @@\n \n Outputs must include:\n \n-- Relevant documents and ADRs.\n+- Relevant documents, SADRs, and target ADRs.\n - Relevant modules, interfaces, seams, callers, and tests.\n - Candidate locks.\n - Dependencies.\n@@ -140,7 +143,7 @@\n - Testing decisions.\n - Out of scope.\n - Further notes.\n-- Links to relevant Goal, domain terms, and ADRs.\n+- Links to relevant Goal, domain terms, SADRs, and target ADRs.\n \n Phase 4 ends with Spec Freeze: the operator explicitly approves the PRD, the\n plan records a `spec_freeze` block (status, approved_by, source), and the SF\n@@ -240,7 +243,7 @@\n - Dependencies and locks.\n - Risk and Guardian requirements.\n - Ledger completeness.\n-- Documentation and ADR updates.\n+- Documentation and SADR/target-ADR updates.\n - Scope drift.\n \n CCA emits one of:\n@@ -301,7 +304,7 @@\n | Evidence failure | Behavior might be done, but proof is missing. | Add checks, PR evidence, ledger entry, or artifacts. |\n | Scope drift | PR changes unrelated behavior. | Revert unrelated changes or split into new Goal. |\n | Dependency block | Upstream task or lock not complete. | Block until dependency resolves. |\n-| Architecture block | Decision needs ADR or deep-module discussion. | `grill-with-docs` or `improve-codebase-architecture`. |\n+| Architecture block | Decision needs a SADR, target ADR, or deep-module discussion. | `grill-with-docs` or `improve-codebase-architecture`. |\n | Risk block | Security/production/policy change needs human approval. | Guardian decision. |\n \n ## Phase 5.5 \u2014 Execution Decision Control\ndiff --git a/docs/agents/runtime-auth-model.md b/docs/agents/runtime-auth-model.md\nindex 6a2e1a47e81ea89b5c68a210ec07772a9374babc..3c54748d24233dbd0ba8473950282547ef4ce935\n--- a/docs/agents/runtime-auth-model.md\n+++ b/docs/agents/runtime-auth-model.md\n@@ -148,7 +148,7 @@\n \n API-key based runners may be added by a target repository as an explicit extension. They are not the default Shiki template path.\n \n-If a repo opts into API-key mode, record the exception in an ADR and update the repo's `AGENTS.md`, `.shiki/config.yaml`, and workflow permissions.\n+If a repo opts into API-key mode, record the target-specific exception in a target ADR and update the repo's `AGENTS.md`, `.shiki/config.yaml`, and workflow permissions.\n \n ## GitHub Actions Runtime Compatibility\n \ndiff --git a/docs/agents/skill-gate.md b/docs/agents/skill-gate.md\nindex c50398b0d7c60a8598f2a5c88a10015730dced13..3acffe6444617bb1d54b77d4b4708158ec735bc6\n--- a/docs/agents/skill-gate.md\n+++ b/docs/agents/skill-gate.md\n@@ -9,7 +9,7 @@\n | Skill | Trigger | Expected output |\n | --- | --- | --- |\n | `setup-matt-pocock-skills` | First configuring a repo for these skills, or when issue tracker, triage labels, or domain docs are missing. | `docs/agents/` configuration and an `Agent skills` block in `AGENTS.md` or `CLAUDE.md`. |\n-| `grill-with-docs` | Ambiguous plan, unclear terminology, boundary decision, tradeoff, or ADR-worthy design choice. | Clarified decision tree, recommended answers, updates to `CONTEXT.md` or ADRs when decisions crystallize. |\n+| `grill-with-docs` | Ambiguous plan, unclear terminology, boundary decision, tradeoff, or decision-record-worthy design choice. | Clarified decision tree, recommended answers, updates to `CONTEXT.md`, SADRs, or target ADRs when decisions crystallize. |\n | `zoom-out` | Agent lacks a higher-level architectural map. | Map of relevant modules, callers, seams, and domain concepts. |\n | `to-prd` | Settled context should become a PRD. | PRD published to the issue tracker with problem, solution, user stories, decisions, testing, and out-of-scope notes. |\n | `to-issues` | Goal, PRD, or plan must become implementation tickets. | Vertical-slice issues with acceptance criteria, dependencies, and AFK/HITL classification. |\n@@ -17,7 +17,7 @@\n | `tdd` | Feature work or bug fix has observable behavior that can be specified with tests. | Red-green-refactor loop through public interfaces, one vertical slice at a time. |\n | `code-review` | The TDD loop is green and the implementer is about to create or update a PR. Default for every implementation task. | Pre-PR self-review of the task diff with fixes applied on the task branch; findings and resolutions recorded as a ledger entry naming the skill and a `## Pre-PR code review` PR body section. |\n | `diagnose` | Hard bug, failing check, regression, flaky behavior, or performance problem. | Reproduction loop, ranked hypotheses, instrumentation, fix, regression test, post-mortem. |\n-| `improve-codebase-architecture` | Structural friction, testability, AI-navigability, deep-module opportunity, or refactor request. | Architecture candidates and deepening recommendations informed by `CONTEXT.md` and ADRs. |\n+| `improve-codebase-architecture` | Structural friction, testability, AI-navigability, deep-module opportunity, or refactor request. | Architecture candidates and deepening recommendations informed by `CONTEXT.md`, SADRs, and target ADRs. |\n | `prototype` | A throwaway logic or UI prototype is needed to answer a design question. | Clearly marked disposable prototype and captured decision. |\n \n ## MergeGate Interaction\ndiff --git a/docs/operations.md b/docs/operations.md\nindex cf1f452d4c589e38ccd58553bda0ad8684b51eb7..ace48a90ffbec9e5c0a7113bef16a4858bc44b3f\n--- a/docs/operations.md\n+++ b/docs/operations.md\n@@ -89,6 +89,16 @@\n      target history, brought forward by `shiki migrate`, not a template.\n    - **Everything else** (CLI scripts, workflows, schemas, docs) is overwritten.\n \n+   A forced upgrade also migrates the former numeric Shiki decision paths to\n+   `SADR-NNNN-*`. Before the first write, the installer inventories the exact\n+   legacy Shiki paths and authorizes each deletion only when the existing\n+   `.shiki/install-stamp.json` names that path and its SHA-256 digest still\n+   matches the file. An absent, unreadable, malformed, incomplete, or\n+   mismatching stamp reports every blocked path and stops with no filesystem\n+   change. Target-owned `NNNN-*` ADRs are not cleanup candidates. A non-force\n+   install deletes nothing and retains any still-matching legacy stamp entries\n+   so a later forced upgrade can prove ownership.\n+\n    If the target has pending migrations, `--force` refuses and writes nothing \u2014\n    apply migrations first (step 3), then re-run. The run ends with a single\n    summary of every `.new` written, so a half-upgrade is visible.\ndiff --git a/docs/releasing.md b/docs/releasing.md\nindex 10d73828e7dbd64e9d9f6b83bc84d010537ce62a..aafeee8c4d441e1e427efee2ce4f81a647f020e1\n--- a/docs/releasing.md\n+++ b/docs/releasing.md\n@@ -67,7 +67,7 @@\n CLI in `scripts/`. There are no third-party Python runtime dependencies. The CLI\n is intentionally **not** exposed as a `[project.scripts]` console entry point\n because `scripts/shiki.py` is a dependency-free shim and `scripts/` is not\n-packaged as an importable distribution (see the ADR for the rationale).\n+packaged as an importable distribution (see SADR-0007 for the rationale).\n \n Supported install paths:\n \n@@ -155,6 +155,15 @@\n    [docs/operations.md](operations.md#the-new-convention). If the target has\n    pending migrations, `--force` refuses and writes nothing; apply migrations\n    (step 2) first, then re-run.\n+\n+   The SADR namespace upgrade has an additional pre-write gate: each former\n+   numeric Shiki decision path is removed only when the existing install stamp\n+   contains that exact path and a digest matching its current bytes. The\n+   installer aggregates absent/unreadable/malformed-stamp, missing-digest, and\n+   digest-mismatch blockers before raising, so refusal leaves the target tree\n+   byte-identical. Numeric target ADRs are preserved. A successful forced\n+   upgrade installs and stamps `SADR-*` paths and drops the removed legacy paths\n+   from the refreshed stamp.\n 2. Apply repository-local `.shiki/` state migrations with the migrate command:\n \n    ```bash\ndiff --git a/docs/usage.md b/docs/usage.md\nindex b83407ee3970dd395dd893010bc5dbe8bad26051..a130ef6e40dfd8169a5bc701339351097df1a09f\n--- a/docs/usage.md\n+++ b/docs/usage.md\n@@ -67,8 +67,10 @@\n shiki install-target /path/to/target-repo --local-only\n ```\n \n-Use `--force` only when you intentionally want to overwrite existing target\n-files.\n+Use `--force` only when you intentionally want to refresh the target template.\n+For the SADR namespace migration, `--force` deletes only exact legacy Shiki\n+decision paths whose existing install-stamp digest still matches; any ownership\n+blocker stops before the first write. Numeric target ADRs are never candidates.\n \n ## `shiki bootstrap-platform` \u2014 publish the Shiki platform repo\n \ndiff --git a/scripts/guardian_approval_signal.py b/scripts/guardian_approval_signal.py\nindex d059f92b2c82aa2b5d6ca421365537a87886c1cb..16924892714f4f5b9303c0ef0e325a7e70075630\n--- a/scripts/guardian_approval_signal.py\n+++ b/scripts/guardian_approval_signal.py\n@@ -199,8 +199,8 @@\n ) -> Any:\n     \"\"\"Evaluate SADR-0015 Contract Approval for the CCA Guardian signal.\n \n-    The SAME OR that MergeGate applies must reach this deterministic signal: ADR\n-    0015 is explicit that without it, a contract-approved PR still returns\n+    The SAME OR that MergeGate applies must reach this deterministic signal:\n+    SADR-0015 is explicit that without it, a contract-approved PR still returns\n     ``needs_guardian``, the CCA job fails, and the MergeGate policy check never\n     runs \u2014 the gate this change moves would never be reached. This resolves the\n     task the SAME way MergeGate does (the first Shiki task id in the PR body),\ndiff --git a/scripts/shiki_contract_approval.py b/scripts/shiki_contract_approval.py\nindex 941bcb96fdd8b9a85bc0c4f0f04d5dad9d04839c..4a4b418f6527413161db82670bec857a043360e2\n--- a/scripts/shiki_contract_approval.py\n+++ b/scripts/shiki_contract_approval.py\n@@ -142,7 +142,7 @@\n     changed_files_status: Any,\n     registration: Any,\n ) -> ContractApprovalResult:\n-    \"\"\"Whether this PR carries an SADR-0015 Contract Approval.\n+    \"\"\"Whether this PR carries a SADR-0015 Contract Approval.\n \n     The four conditions are evaluated IN ORDER, each failure recorded as a\n     distinct reason string:\ndiff --git a/scripts/shiki_guardian.py b/scripts/shiki_guardian.py\nindex a4ecb79bbda55fd5c8b06adfae2eb99760a03c45..4dd99bbe714e26f421fa19abac7a34ae1b195ecc\n--- a/scripts/shiki_guardian.py\n+++ b/scripts/shiki_guardian.py\n@@ -12,7 +12,7 @@\n # The carry re-derives its proof from git in-process on every judging invocation;\n # it is never read from an artifact, so there is nothing to go stale or be forged.\n # shiki_sync_proof is the ONLY guardian-family module that runs git, and every one\n-# of its invocations is read-only and local (ADR: prove-from-git base sync).\n+# of its invocations is read-only and local (SADR: prove-from-git base sync).\n from shiki_sync_proof import verify_pure_base_sync\n \n GUARDIAN_POLICY_PATH = \".shiki/guardian-policy.json\"\ndiff --git a/scripts/shiki_guardian_review.py b/scripts/shiki_guardian_review.py\nindex dd0f104d457899e0ba5ff1e162011c1e90b6f92b..e10ef58c046324e7ea3a3392feb3048ca77414ce\n--- a/scripts/shiki_guardian_review.py\n+++ b/scripts/shiki_guardian_review.py\n@@ -159,7 +159,7 @@\n     docs_only = _is_docs_only(changed_files)\n     if docs_only and matched == 0:\n         focus.append(\n-            \"Docs/ADR-only change: confirm the change is genuinely documentation/decision-only, introduces no behavior or contract drift, and stays consistent with the constitution and existing ADRs.\"\n+            \"Docs/decision-record-only change: confirm the change is genuinely documentation/decision-only, introduces no behavior or contract drift, and stays consistent with the constitution and existing SADRs and target ADRs.\"\n         )\n \n     # Conservative augmentation: unknown (no category and not docs-only), mixed\ndiff --git a/scripts/shiki_installer.py b/scripts/shiki_installer.py\nindex be064ab7a29ff6836f7f7a1d70500a43e0d11d7b..9fd8eefd137ce7003dad5c0dcb2e48e2cd47ab32\n--- a/scripts/shiki_installer.py\n+++ b/scripts/shiki_installer.py\n@@ -118,6 +118,7 @@\n # what lets a target carry it without a governance-schema change.\n INSTALL_STAMP_PATH = \".shiki/install-stamp.json\"\n INSTALL_STAMP_VERSION = 1\n+LEGACY_SHIKI_SADR_MAX = 18\n \n # Under --force the shipped surface splits three ways, so nothing is lost and\n # nothing is silently stale.\n@@ -237,9 +238,98 @@\n         )\n \n \n+def legacy_shiki_adr_paths() -> tuple[str, ...]:\n+    \"\"\"Former numeric paths for Shiki SADR-0001 through SADR-0018.\n+\n+    The authoritative slugs come from the current SADR filenames. SADR-0019 is\n+    the namespace decision itself and never shipped under a numeric legacy path.\n+    \"\"\"\n+    paths: list[str] = []\n+    for sadr in sorted((ROOT / \"docs\" / \"adr\").glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\")):\n+        number = sadr.name.split(\"-\", 2)[1]\n+        if int(number) <= LEGACY_SHIKI_SADR_MAX:\n+            paths.append(f\"docs/adr/{sadr.name.removeprefix('SADR-')}\")\n+    return tuple(paths)\n+\n+\n+def preflight_legacy_sadr_cleanup(target: Path, *, force: bool) -> tuple[str, ...]:\n+    \"\"\"Authorize exact legacy Shiki ADR removals before the first write.\n+\n+    A non-force install never removes existing files. A forced install needs no\n+    stamp when no legacy Shiki path is present. When legacy paths do exist, each\n+    must be named in a readable install stamp and its current bytes must match\n+    the recorded digest.\n+    \"\"\"\n+    if not force:\n+        return ()\n+    present = tuple(\n+        relative\n+        for relative in legacy_shiki_adr_paths()\n+        if (target / relative).exists() or (target / relative).is_symlink()\n+    )\n+    if not present:\n+        return ()\n+\n+    stamp_path = target / INSTALL_STAMP_PATH\n+    try:\n+        raw = stamp_path.read_text(encoding=\"utf-8\")\n+        stamp = json.loads(raw)\n+    except FileNotFoundError:\n+        detail = \"install stamp is absent\"\n+        blockers = [f\"{relative}: {detail}\" for relative in present]\n+    except (OSError, UnicodeDecodeError) as error:\n+        detail = f\"install stamp is unreadable ({error})\"\n+        blockers = [f\"{relative}: {detail}\" for relative in present]\n+    except json.JSONDecodeError as error:\n+        detail = f\"install stamp is malformed ({error})\"\n+        blockers = [f\"{relative}: {detail}\" for relative in present]\n+    else:\n+        if not isinstance(stamp, dict):\n+            blockers = [\n+                f\"{relative}: install stamp is malformed (expected an object)\"\n+                for relative in present\n+            ]\n+        else:\n+            digests = stamp.get(\"digests\")\n+            blockers = []\n+            if not isinstance(digests, dict):\n+                blockers.extend(\n+                    f\"{relative}: install stamp has no digest for this path\"\n+                    for relative in present\n+                )\n+            else:\n+                for relative in present:\n+                    expected = digests.get(relative)\n+                    if not isinstance(expected, str) or not expected:\n+                        blockers.append(\n+                            f\"{relative}: install stamp has no digest for this path\"\n+                        )\n+                        continue\n+                    try:\n+                        actual = _sha256_file(target / relative)\n+                    except OSError as error:\n+                        blockers.append(\n+                            f\"{relative}: legacy path is unreadable ({error})\"\n+                        )\n+                        continue\n+                    if actual != expected:\n+                        blockers.append(\n+                            f\"{relative}: digest mismatch (stamp {expected}, current {actual})\"\n+                        )\n+\n+    if blockers:\n+        joined = \"\\n\".join(f\"- {blocker}\" for blocker in blockers)\n+        raise ShikiError(\n+            \"legacy Shiki ADR cleanup is not authorized:\\n\"\n+            f\"{joined}\\nNo files were written.\"\n+        )\n+    return present\n+\n+\n def install_template(target: Path, *, force: bool, validate: bool) -> list[NewFileNote]:\n     # Preconditions first: never rewrite the tree of a target that cannot be\n     # upgraded cleanly (see refuse_pending_migrations).\n+    legacy_cleanup = preflight_legacy_sadr_cleanup(target, force=force)\n     refuse_pending_migrations(target)\n \n     # Resolve the target's required CODEOWNERS owner once from its\n@@ -250,6 +340,10 @@\n     # verbatim. See ``codeowners_required_owner``.\n     codeowners_owner = codeowners_required_owner(target)\n \n+    for relative in legacy_cleanup:\n+        (target / relative).unlink()\n+        info(f\"removed stamped legacy Shiki ADR: {target / relative}\")\n+\n     notes: list[NewFileNote] = []\n     for relative in TEMPLATE_PATHS:\n         source = ROOT / relative\n@@ -647,6 +741,22 @@\n     digests = target_stamp_digests(target)\n     existing = read_install_stamp(target)\n     if existing is not None:\n+        # A non-force install deliberately leaves legacy Shiki ADRs in place.\n+        # Preserve only still-matching ownership proof so a later forced\n+        # upgrade can authorize cleanup; never carry a missing or stale digest.\n+        existing_digests = existing.get(\"digests\")\n+        if isinstance(existing_digests, dict):\n+            for relative in legacy_shiki_adr_paths():\n+                expected = existing_digests.get(relative)\n+                path = target / relative\n+                if not isinstance(expected, str) or not path.is_file():\n+                    continue\n+                try:\n+                    actual = _sha256_file(path)\n+                except OSError:\n+                    continue\n+                if actual == expected:\n+                    digests[relative] = actual\n         if _incoming_is_older(commit, existing.get(\"platform_commit\")):\n             warn(\n                 \"kept existing install stamp: incoming install \"\ndiff --git a/scripts/shiki_loop.py b/scripts/shiki_loop.py\nindex 3611641da8aeb5038b1da90e3114b09a473ec6d8..84caa491b9cad0edc4176f209c13e9221e975681\n--- a/scripts/shiki_loop.py\n+++ b/scripts/shiki_loop.py\n@@ -424,7 +424,7 @@\n         # unresolvable CCA verdict has already produced its terminal stop and is never\n         # laundered into a head-moving sync (SADR-0010). It also never fires while the\n         # policy (Guardian) gate is red \u2014 for ANY reason, not only when it is the sole\n-        # red check \u2014 so a head-bound Guardian approval, or an SADR-0015\n+        # red check \u2014 so a head-bound Guardian approval, or a SADR-0015\n         # contract-approval carry that base movement defeated (the policy gate compares\n         # base and head task governance directly), is never silently restored by\n         # adopting base wholesale. It is bounded by a durable per-task counter so the\ndiff --git a/scripts/shiki_tasks.py b/scripts/shiki_tasks.py\nindex aec3809f24b4a0c8c96fc50971cff9ae9ad30381..d531052e9d9717058d4631483cefab44c26a65c1\n--- a/scripts/shiki_tasks.py\n+++ b/scripts/shiki_tasks.py\n@@ -821,7 +821,7 @@\n         \"prompt\": prompt,\n         \"entry_skill\": \"grill-with-docs\",\n         \"required_next_steps\": [\n-            \"Run grill-with-docs until terminology, boundaries, risks, and ADR-worthy decisions are settled.\",\n+            \"Run grill-with-docs until terminology, boundaries, risks, and decision-record-worthy choices are settled.\",\n             \"Obtain the operator's explicit approval of the PRD/requirements (Spec Freeze).\",\n             \"Write a machine-readable plan JSON with grill_with_docs.status=complete and spec_freeze.status=frozen.\",\n             \"Run shiki plan ingest --plan-file PLAN.json.\",\ndiff --git a/scripts/test_shiki_goal_loop.sh b/scripts/test_shiki_goal_loop.sh\nindex 753c7b516e10f0be3bc92a4bd0eb1fa699776ae6..8f3820a0a079b3044424cfe1a6c6b7fe68896e67\n--- a/scripts/test_shiki_goal_loop.sh\n+++ b/scripts/test_shiki_goal_loop.sh\n@@ -231,8 +231,8 @@\n \n # High/critical risk merges autonomously when all required checks are green:\n # the \"MergeGate policy check\" required check IS the Guardian gate (it enforces\n-# guardian-policy.json \u2014 human approval OR external AI guardian review, ADR\n-# 0010), so green checks mean a recorded authority approved. The fake gh returns\n+# guardian-policy.json \u2014 human approval OR external AI guardian review,\n+# SADR-0010), so green checks mean a recorded authority approved. The fake gh returns\n # green checks, simulating that the Guardian gate passed.\n python3 \"$ROOT/scripts/shiki.py\" goal create --target \"$TARGET\" --title \"Guardian gate\" --outcome \"High risk merges via policy gate\" >/tmp/shiki-goal-loop-guardian-goal.json\n GGOAL=\"$(json_get /tmp/shiki-goal-loop-guardian-goal.json goal_id)\"\ndiff --git a/tests/test_adr_references.py b/tests/test_adr_references.py\nindex 51706329c797d5dd9b47e082a333fa83b261d482..d005da4f771f5e8085ec22d4b787980e68f5ae8c\n--- a/tests/test_adr_references.py\n+++ b/tests/test_adr_references.py\n@@ -1,8 +1,8 @@\n-\"\"\"Every ``ADR ####`` citation must resolve to exactly one decision record.\n+\"\"\"Every target ADR and Shiki SADR citation resolves in its own namespace.\n \n Background. ``scripts/mergegate_check.py`` justifies ``is_bookkeeping_closeout``\n-with \"ADR 0017\" nine times and ``scripts/guardian_approval_signal.py`` cites\n-0016/0017 seven times, yet for a stretch the numbering in ``docs/adr/`` stopped\n+with \"SADR-0017\" nine times and ``scripts/guardian_approval_signal.py`` cites\n+SADR-0016/SADR-0017 seven times, yet for a stretch the numbering in ``docs/adr/`` stopped\n at 0015: the authoring commits lived on a coordinator branch and no task ever\n declared ``docs/adr/...`` in its locks, so nothing carried the records to the\n default branch. The stated basis of an exemption that merged seven PRs with zero\n@@ -11,11 +11,12 @@\n and the directory drifted with no signal.\n \n This is the same unbound-pair shape behind other silent failures: a reference\n-and its target with nothing asserting they agree. This test binds them. It scans\n-every ``scripts/*.py`` (plus ``docs/**/*.md`` and the root ``*.md`` governance\n-files, which are cheap) for the ``ADR ####`` pattern and asserts each cited\n-number resolves to exactly one ``docs/adr/####-*.md``. The converse is\n-deliberately NOT required: an ADR may exist without being cited.\n+and its target with nothing asserting they agree. This test binds them. Target\n+``ADR NNNN`` citations resolve only to ``docs/adr/NNNN-*.md``; Shiki\n+``SADR-NNNN`` citations resolve only to ``docs/adr/SADR-NNNN-*.md``. The scan\n+covers current governance docs, scripts, workflows, prompts, packaging metadata,\n+and repo-local Codex guidance while deliberately excluding historical\n+``.shiki`` evidence. The converse is not required: a record may be uncited.\n \n The suite is dependency free: standard-library ``unittest`` only.\n \"\"\"\n@@ -31,17 +32,18 @@\n \n REPO_ROOT = Path(__file__).resolve().parents[1]\n \n-# \"ADR 0016\" -- a four-digit citation. The trailing \\b keeps a five-digit token\n-# from being read as a truncated four-digit number.\n-_CITATION = re.compile(r\"\\bADR (\\d{4})\\b\")\n-# \"0016-locks-bound-product-paths.md\" -- the leading four digits name the record.\n-_ADR_FILENAME = re.compile(r\"^(\\d{4})-.*\\.md$\")\n+# ``ADR 0016`` / ``SADR-0016`` -- a namespace plus four-digit citation. The\n+# trailing ``\\b`` keeps a five-digit token from being read as a truncated one.\n+_CITATION = re.compile(r\"\\b(S?ADR)[ -](\\d{4})\\b\")\n+# Target ADRs retain the numeric filename; Shiki platform decisions use SADR.\n+_DECISION_FILENAME = re.compile(r\"^(?:(SADR)-)?(\\d{4})-.*\\.md$\")\n \n \n class Violation(NamedTuple):\n     \"\"\"A citation that does not resolve to exactly one decision record.\"\"\"\n \n     citing_file: str  # repo-relative path of the file that cites the ADR\n+    namespace: str  # \"ADR\" for target decisions; \"SADR\" for Shiki decisions\n     number: str  # the cited four-digit number, e.g. \"0016\"\n     line: int  # 1-based line of the citation\n     reason: str  # \"missing\" (zero matches) or \"ambiguous\" (two or more)\n@@ -49,44 +51,59 @@\n \n     @property\n     def expected_path(self) -> str:\n-        return f\"docs/adr/{self.number}-*.md\"\n+        prefix = \"SADR-\" if self.namespace == \"SADR\" else \"\"\n+        return f\"docs/adr/{prefix}{self.number}-*.md\"\n \n     def describe(self) -> str:\n         \"\"\"Name the citing file, the cited number, and the missing path.\"\"\"\n         if self.reason == \"missing\":\n             return (\n-                f\"{self.citing_file}:{self.line} cites ADR {self.number} \"\n+                f\"{self.citing_file}:{self.line} cites {self.namespace} {self.number} \"\n                 f\"but no document matches {self.expected_path}\"\n             )\n         joined = \", \".join(self.matches)\n         return (\n-            f\"{self.citing_file}:{self.line} cites ADR {self.number} which \"\n+            f\"{self.citing_file}:{self.line} cites {self.namespace} {self.number} which \"\n             f\"resolves to {len(self.matches)} documents (expected exactly one \"\n             f\"at {self.expected_path}): {joined}\"\n         )\n \n \n-def _adr_index(root: Path) -> dict[str, list[Path]]:\n-    \"\"\"Map every four-digit ADR number to the record files that claim it.\"\"\"\n-    index: dict[str, list[Path]] = {}\n+def _adr_index(root: Path) -> dict[tuple[str, str], list[Path]]:\n+    \"\"\"Map each decision namespace and number to the files that claim it.\"\"\"\n+    index: dict[tuple[str, str], list[Path]] = {}\n     adr_dir = root / \"docs\" / \"adr\"\n     if adr_dir.is_dir():\n         for path in sorted(adr_dir.glob(\"*.md\")):\n-            match = _ADR_FILENAME.match(path.name)\n+            match = _DECISION_FILENAME.match(path.name)\n             if match:\n-                index.setdefault(match.group(1), []).append(path)\n+                namespace = \"SADR\" if match.group(1) else \"ADR\"\n+                index.setdefault((namespace, match.group(2)), []).append(path)\n     return index\n \n \n def _citing_files(root: Path, include_docs: bool = True) -> list[Path]:\n-    \"\"\"Files scanned for citations: scripts/*.py, and cheaply the markdown.\"\"\"\n+    \"\"\"Current, shipped text surfaces scanned for decision citations.\"\"\"\n     files = sorted((root / \"scripts\").glob(\"*.py\"))\n+    files += sorted((root / \"scripts\").glob(\"*.sh\"))\n     if include_docs:\n         docs = root / \"docs\"\n         if docs.is_dir():\n             files += sorted(docs.rglob(\"*.md\"))\n         files += sorted(root.glob(\"*.md\"))\n-    return files\n+        pyproject = root / \"pyproject.toml\"\n+        if pyproject.is_file():\n+            files.append(pyproject)\n+        for relative in (\".github\", \".codex\"):\n+            surface = root / relative\n+            if surface.is_dir():\n+                files += sorted(\n+                    path\n+                    for path in surface.rglob(\"*\")\n+                    if path.is_file()\n+                    and path.suffix in {\".md\", \".py\", \".sh\", \".toml\", \".yaml\", \".yml\"}\n+                )\n+    return sorted(set(files))\n \n \n def find_adr_reference_violations(\n@@ -94,10 +111,9 @@\n ) -> list[Violation]:\n     \"\"\"Return every ``ADR ####`` citation under ``root`` that fails to resolve.\n \n-    A citation resolves when exactly one ``docs/adr/####-*.md`` matches its\n-    number. Zero matches is a \"missing\" violation; two or more is \"ambiguous\".\n-    Records that are never cited produce nothing -- the converse is not\n-    required.\n+    A citation resolves when exactly one record in the same namespace matches\n+    its number. Zero matches is a \"missing\" violation; two or more is\n+    \"ambiguous\". Records that are never cited produce nothing.\n     \"\"\"\n     root = Path(root)\n     index = _adr_index(root)\n@@ -110,13 +126,15 @@\n         rel = path.relative_to(root).as_posix()\n         for lineno, line in enumerate(text.splitlines(), start=1):\n             for match in _CITATION.finditer(line):\n-                number = match.group(1)\n-                found = index.get(number, [])\n+                namespace, number = match.groups()\n+                found = index.get((namespace, number), [])\n                 if len(found) == 1:\n                     continue\n                 reason = \"missing\" if not found else \"ambiguous\"\n                 rels = [p.relative_to(root).as_posix() for p in found]\n-                violations.append(Violation(rel, number, lineno, reason, rels))\n+                violations.append(\n+                    Violation(rel, namespace, number, lineno, reason, rels)\n+                )\n     return violations\n \n \n@@ -129,8 +147,8 @@\n         self.assertEqual(\n             violations,\n             [],\n-            \"Every 'ADR ####' citation must resolve to exactly one \"\n-            f\"docs/adr/####-*.md:\\n{detail}\",\n+            \"Every ADR/SADR citation must resolve exactly once in its own \"\n+            f\"docs/adr namespace:\\n{detail}\",\n         )\n \n     def test_scan_actually_reads_scripts(self) -> None:\n@@ -142,12 +160,38 @@\n             \"the citation scan must cover scripts/*.py\",\n         )\n \n+    def test_platform_citations_do_not_use_unresolved_numeric_shorthand(self) -> None:\n+        shorthand = re.compile(r\"SADR-\\d{4}/\\d{4}\")\n+        offenders = []\n+        for path in _citing_files(REPO_ROOT):\n+            text = path.read_text(encoding=\"utf-8\")\n+            if shorthand.search(text):\n+                offenders.append(path.relative_to(REPO_ROOT).as_posix())\n+        self.assertEqual(\n+            offenders,\n+            [],\n+            \"repeat the SADR prefix so every cited number is independently resolvable\",\n+        )\n+\n     def test_carried_records_are_indexed(self) -> None:\n         # The two records this Goal carried onto the default branch, and which\n         # the scripts cite most, must be present.\n         index = _adr_index(REPO_ROOT)\n-        self.assertIn(\"0016\", index)\n-        self.assertIn(\"0017\", index)\n+        self.assertIn((\"SADR\", \"0016\"), index)\n+        self.assertIn((\"SADR\", \"0017\"), index)\n+\n+    def test_platform_tree_has_only_sadr_0001_through_0019(self) -> None:\n+        adr_dir = REPO_ROOT / \"docs\" / \"adr\"\n+        sadrs = sorted(adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\"))\n+        self.assertEqual(\n+            {path.name.split(\"-\", 2)[1] for path in sadrs},\n+            {f\"{number:04d}\" for number in range(1, 20)},\n+        )\n+        self.assertEqual(\n+            list(adr_dir.glob(\"[0-9][0-9][0-9][0-9]-*.md\")),\n+            [],\n+            \"the Shiki platform tree must not retain legacy numeric SADR paths\",\n+        )\n \n \n class FixtureAdrReferences(unittest.TestCase):\n@@ -180,6 +224,26 @@\n         self.assertIn(\"9991\", message)  # cited number\n         self.assertIn(\"docs/adr/9991-*.md\", message)  # missing path\n \n+    def test_target_adr_does_not_satisfy_missing_platform_sadr(self) -> None:\n+        root = self._make_repo(\n+            adr_names=[\"0042-target-decision.md\"],\n+            script_body='\"\"\"Target ADR 0042 exists, but platform SADR-0042 does not.\"\"\"\\n',\n+        )\n+        violations = find_adr_reference_violations(root, include_docs=False)\n+        self.assertEqual(len(violations), 1)\n+        self.assertEqual(violations[0].namespace, \"SADR\")\n+        self.assertEqual(violations[0].number, \"0042\")\n+        self.assertEqual(violations[0].expected_path, \"docs/adr/SADR-0042-*.md\")\n+\n+    def test_target_adr_and_platform_sadr_may_reuse_a_number(self) -> None:\n+        root = self._make_repo(\n+            adr_names=[\"0042-target-decision.md\", \"SADR-0042-platform-decision.md\"],\n+            script_body='\"\"\"Target ADR 0042 and platform SADR-0042 are distinct.\"\"\"\\n',\n+        )\n+        self.assertEqual(\n+            find_adr_reference_violations(root, include_docs=False), []\n+        )\n+\n     def test_uncited_adr_is_allowed(self) -> None:\n         # An ADR exists but nothing cites it -> not a violation. The converse\n         # (every ADR must be cited) is explicitly NOT required.\ndiff --git a/tests/test_guardian_review.py b/tests/test_guardian_review.py\nindex 88829e7810212635e2fc98aa5918d6f26294677d..00a85a237acd5bf2d6cb5b294128198c422402ca\n--- a/tests/test_guardian_review.py\n+++ b/tests/test_guardian_review.py\n@@ -162,7 +162,7 @@\n \n     def test_docs_only_is_light_and_not_conservative(self):\n         focus = gr.classify_review_focus([\"docs/adr/0099-x.md\", \"README.md\"], \"\", \"low\")\n-        self.assertTrue(any(\"Docs/ADR-only\" in f for f in focus))\n+        self.assertTrue(any(\"Docs/decision-record-only\" in f for f in focus))\n         self.assertFalse(any(\"conservative default\" in f.lower() for f in focus))\n \n     def test_unknown_category_is_conservative(self):\ndiff --git a/tests/test_policy_job_merged_prs.py b/tests/test_policy_job_merged_prs.py\nindex d45512e74681e5e649279236c7d4e79d5c3da471..b97989f8bd74d091de1a6b5275a20d8dc68c4512\n--- a/tests/test_policy_job_merged_prs.py\n+++ b/tests/test_policy_job_merged_prs.py\n@@ -3,8 +3,8 @@\n \n ``.github/workflows/shiki-cca-completion.yml`` has two consumers of the merged-PR\n proof: the CCA *signal* (``guardian_approval_signal.py`` in \"Prepare CCA context\")\n-and the authoritative gate (``mergegate_check.py`` in \"Enforce MergeGate\"). ADR\n-0017's bookkeeping-closeout exemption (condition 6) needs the merged implementation\n+and the authoritative gate (``mergegate_check.py`` in \"Enforce MergeGate\").\n+SADR-0017's bookkeeping-closeout exemption (condition 6) needs the merged implementation\n PR proven, and a closeout carries NO ``post_merge_reconcile`` marker. The signal\n already resolves ``MERGED_PRS`` unconditionally, but \"Enforce MergeGate\" used to\n resolve it only inside ``grep -q '<!-- shiki:post_merge_reconcile -->'``. So a\ndiff --git a/tests/test_sadr_install_migration.py b/tests/test_sadr_install_migration.py\nnew file mode 100644\nindex 0000000000000000000000000000000000000000..d51eac872b1c79ff4579c4c1401ca5d775a60d8d\n--- /dev/null\n+++ b/tests/test_sadr_install_migration.py\n@@ -0,0 +1,237 @@\n+\"\"\"SADR installation and collision-safe legacy upgrade behavior.\"\"\"\n+\n+from __future__ import annotations\n+\n+import contextlib\n+import hashlib\n+import io\n+import json\n+import shutil\n+import tempfile\n+import unittest\n+from pathlib import Path\n+\n+import shiki_test_support  # noqa: F401  (path bootstrap)\n+\n+import shiki_installer\n+from tests.test_adr_references import find_adr_reference_violations\n+\n+\n+PRODUCT_NUMBERS = tuple(range(13, 19))\n+\n+\n+def _quiet_install(target: Path, *, force: bool, validate: bool) -> None:\n+    with contextlib.redirect_stdout(io.StringIO()):\n+        shiki_installer.install_template(target, force=force, validate=validate)\n+\n+\n+def _sha256(path: Path) -> str:\n+    return hashlib.sha256(path.read_bytes()).hexdigest()\n+\n+\n+def _legacy_sadr_paths() -> tuple[str, ...]:\n+    return tuple(\n+        f\"docs/adr/{path.name.removeprefix('SADR-')}\"\n+        for path in sorted((shiki_installer.ROOT / \"docs\" / \"adr\").glob(\"SADR-*.md\"))\n+        if path.name != \"SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\"\n+    )\n+\n+\n+def _create_legacy_upgrade_fixture(\n+    target: Path,\n+) -> tuple[dict[str, bytes], tuple[str, ...]]:\n+    adr_dir = target / \"docs\" / \"adr\"\n+    adr_dir.mkdir(parents=True)\n+    product_bytes = {\n+        f\"{number:04d}-cubeshare-product-decision.md\": (\n+            f\"# CubeShare ADR {number:04d}\\n\\nDo not rewrite {number}.\\n\"\n+        ).encode()\n+        for number in PRODUCT_NUMBERS\n+    }\n+    for name, content in product_bytes.items():\n+        (adr_dir / name).write_bytes(content)\n+\n+    legacy_paths = _legacy_sadr_paths()\n+    for relative in legacy_paths:\n+        (target / relative).write_bytes(f\"legacy Shiki bytes for {relative}\\n\".encode())\n+\n+    stamp_path = target / shiki_installer.INSTALL_STAMP_PATH\n+    stamp_path.parent.mkdir(parents=True, exist_ok=True)\n+    stamp = {\n+        \"version\": shiki_installer.INSTALL_STAMP_VERSION,\n+        \"platform_commit\": None,\n+        \"installed_at\": \"2000-01-01T00:00:00+00:00\",\n+        \"digests\": {\n+            relative: _sha256(target / relative) for relative in legacy_paths\n+        },\n+    }\n+    stamp_path.write_text(\n+        json.dumps(stamp, indent=2, sort_keys=True) + \"\\n\", encoding=\"utf-8\"\n+    )\n+    return product_bytes, legacy_paths\n+\n+\n+def _tree_snapshot(root: Path) -> dict[str, tuple[str, bytes | str]]:\n+    \"\"\"Recursive byte/shape snapshot used to prove preflight is write-free.\"\"\"\n+    snapshot: dict[str, tuple[str, bytes | str]] = {}\n+    for path in sorted(root.rglob(\"*\")):\n+        relative = path.relative_to(root).as_posix()\n+        if path.is_symlink():\n+            snapshot[relative] = (\"symlink\", path.readlink().as_posix())\n+        elif path.is_dir():\n+            snapshot[relative] = (\"directory\", \"\")\n+        else:\n+            snapshot[relative] = (\"file\", path.read_bytes())\n+    return snapshot\n+\n+\n+class FreshSadrInstallTests(unittest.TestCase):\n+    def setUp(self) -> None:\n+        self.tmp = Path(tempfile.mkdtemp(prefix=\"shiki-sadr-fresh-\"))\n+        self.addCleanup(lambda: shutil.rmtree(self.tmp, ignore_errors=True))\n+        self.target = self.tmp / \"target\"\n+        self.adr_dir = self.target / \"docs\" / \"adr\"\n+        self.adr_dir.mkdir(parents=True)\n+        self.product_bytes = {\n+            f\"{number:04d}-product-decision.md\": (\n+                f\"# Product ADR {number:04d}\\n\\nTarget-owned bytes {number}.\\n\"\n+            ).encode()\n+            for number in PRODUCT_NUMBERS\n+        }\n+        for name, content in self.product_bytes.items():\n+            (self.adr_dir / name).write_bytes(content)\n+\n+    def test_fresh_install_preserves_product_adrs_and_installs_all_sadrs(self) -> None:\n+        _quiet_install(self.target, force=False, validate=True)\n+\n+        for name, expected in self.product_bytes.items():\n+            self.assertEqual((self.adr_dir / name).read_bytes(), expected)\n+\n+        sadr_numbers = {\n+            path.name.split(\"-\", 2)[1]\n+            for path in self.adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\")\n+        }\n+        self.assertEqual(sadr_numbers, {f\"{number:04d}\" for number in range(1, 20)})\n+\n+        numeric_records = {\n+            path.name\n+            for path in self.adr_dir.glob(\"[0-9][0-9][0-9][0-9]-*.md\")\n+        }\n+        self.assertEqual(numeric_records, set(self.product_bytes))\n+        stamp = json.loads(\n+            (self.target / shiki_installer.INSTALL_STAMP_PATH).read_text(\n+                encoding=\"utf-8\"\n+            )\n+        )\n+        for path in self.adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\"):\n+            relative = path.relative_to(self.target).as_posix()\n+            self.assertEqual(stamp[\"digests\"][relative], _sha256(path))\n+        self.assertEqual(find_adr_reference_violations(self.target), [])\n+\n+\n+class LegacySadrUpgradeTests(unittest.TestCase):\n+    def setUp(self) -> None:\n+        self.tmp = Path(tempfile.mkdtemp(prefix=\"shiki-sadr-upgrade-\"))\n+        self.addCleanup(lambda: shutil.rmtree(self.tmp, ignore_errors=True))\n+        self.target = self.tmp / \"target\"\n+        self.adr_dir = self.target / \"docs\" / \"adr\"\n+        self.product_bytes, self.legacy_paths = _create_legacy_upgrade_fixture(\n+            self.target\n+        )\n+\n+    def test_force_upgrade_deletes_only_stamped_legacy_sadrs(self) -> None:\n+        _quiet_install(self.target, force=True, validate=True)\n+\n+        for relative in self.legacy_paths:\n+            self.assertFalse((self.target / relative).exists(), relative)\n+        for name, expected in self.product_bytes.items():\n+            self.assertEqual((self.adr_dir / name).read_bytes(), expected)\n+\n+        sadrs = list(self.adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\"))\n+        self.assertEqual(len(sadrs), 19)\n+        refreshed = json.loads(\n+            (self.target / shiki_installer.INSTALL_STAMP_PATH).read_text(encoding=\"utf-8\")\n+        )\n+        for relative in self.legacy_paths:\n+            self.assertNotIn(relative, refreshed[\"digests\"])\n+        for path in sadrs:\n+            relative = path.relative_to(self.target).as_posix()\n+            self.assertEqual(refreshed[\"digests\"][relative], _sha256(path))\n+\n+    def test_non_force_install_never_deletes_legacy_files(self) -> None:\n+        before = {\n+            relative: (self.target / relative).read_bytes()\n+            for relative in self.legacy_paths\n+        }\n+        _quiet_install(self.target, force=False, validate=False)\n+        for relative, expected in before.items():\n+            self.assertEqual((self.target / relative).read_bytes(), expected)\n+        refreshed = json.loads(\n+            (self.target / shiki_installer.INSTALL_STAMP_PATH).read_text(encoding=\"utf-8\")\n+        )\n+        for relative in self.legacy_paths:\n+            self.assertEqual(refreshed[\"digests\"][relative], _sha256(self.target / relative))\n+\n+\n+class LegacyCleanupPreflightTests(unittest.TestCase):\n+    def setUp(self) -> None:\n+        self.tmp = Path(tempfile.mkdtemp(prefix=\"shiki-sadr-preflight-\"))\n+        self.addCleanup(lambda: shutil.rmtree(self.tmp, ignore_errors=True))\n+\n+    def test_all_ownership_blockers_abort_before_any_write(self) -> None:\n+        cases = (\n+            (\"absent\", \"install stamp is absent\"),\n+            (\"unreadable\", \"install stamp is unreadable\"),\n+            (\"malformed\", \"install stamp is malformed\"),\n+            (\"missing-digest\", \"install stamp has no digest\"),\n+            (\"digest-mismatch\", \"digest mismatch\"),\n+        )\n+        for name, expected_reason in cases:\n+            with self.subTest(name=name):\n+                target = self.tmp / name\n+                _, legacy_paths = _create_legacy_upgrade_fixture(target)\n+                stamp_path = target / shiki_installer.INSTALL_STAMP_PATH\n+\n+                if name == \"absent\":\n+                    stamp_path.unlink()\n+                elif name == \"unreadable\":\n+                    stamp_path.unlink()\n+                    stamp_path.mkdir()\n+                elif name == \"malformed\":\n+                    stamp_path.write_text(\"{not-json\\n\", encoding=\"utf-8\")\n+                else:\n+                    stamp = json.loads(stamp_path.read_text(encoding=\"utf-8\"))\n+                    if name == \"missing-digest\":\n+                        stamp[\"digests\"] = {}\n+                    else:\n+                        stamp[\"digests\"] = {\n+                            relative: \"deadbeef\" for relative in legacy_paths\n+                        }\n+                    stamp_path.write_text(\n+                        json.dumps(stamp, indent=2, sort_keys=True) + \"\\n\",\n+                        encoding=\"utf-8\",\n+                    )\n+\n+                before = _tree_snapshot(target)\n+                with self.assertRaises(shiki_installer.ShikiError) as raised:\n+                    _quiet_install(target, force=True, validate=False)\n+                after = _tree_snapshot(target)\n+\n+                self.assertEqual(before, after, f\"{name} preflight changed the tree\")\n+                message = str(raised.exception)\n+                self.assertIn(expected_reason, message)\n+                self.assertIn(\"No files were written\", message)\n+                for relative in legacy_paths:\n+                    self.assertIn(relative, message)\n+\n+    def test_absent_stamp_is_allowed_when_no_legacy_path_exists(self) -> None:\n+        target = self.tmp / \"no-legacy\"\n+        target.mkdir()\n+        _quiet_install(target, force=True, validate=False)\n+        self.assertTrue(\n+            (target / \"docs\" / \"adr\" / \"SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\").is_file()\n+        )\n+\n+\n+if __name__ == \"__main__\":\n+    unittest.main()\n\nexec\n/bin/zsh -lc \"sed -n '1,220p' .shiki/ledger/L-20260807T080256330734Z-5bf40cb6.json\" in /private/tmp/shiki-adr-collision.pa0KwY/.worktrees/shiki-t-20260807t073307221254z-2d22d654-introduc\n exited 1 in 0ms:\nsed: .shiki/ledger/L-20260807T080256330734Z-5bf40cb6.json: No such file or directory\n\ndiff --git a/.codex/skills/shiki/SKILL.md b/.codex/skills/shiki/SKILL.md\nindex c8e2e85d9d75d49cc4f2d6286ee22659c4e53e75..93cded623b2c15d9f65c76491a1c2d813ac9e6c0\n--- a/.codex/skills/shiki/SKILL.md\n+++ b/.codex/skills/shiki/SKILL.md\n@@ -18,6 +18,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n Codex CLI does not register this skill as a custom `/shiki` slash command. When\n the user types `/shiki` in Codex CLI and gets \"Unrecognized command\", explain\n that this is expected: invoke Shiki in Codex with natural language such as\n@@ -69,7 +72,7 @@\n \n ## Responsibilities\n \n-- Claude Code implements and repairs by default (`shiki runner claude`, ADR 0008); Codex implements and repairs tasks explicitly assigned to `codex`.\n+- Claude Code implements and repairs by default (`shiki runner claude`, SADR-0008); Codex implements and repairs tasks explicitly assigned to `codex`.\n - Claude Code Action can act as GitHub-side CCA or reviewer.\n - CCA judges completion.\n - MergeGate authorizes state transitions and merge readiness.\ndiff --git a/.github/prompts/cca-completion-check.md b/.github/prompts/cca-completion-check.md\nindex ed203ad5f302ecc81372d8ab27d2b1232ca97497..fd89950528333ef6242e6d5443904f5392ab7e56\n--- a/.github/prompts/cca-completion-check.md\n+++ b/.github/prompts/cca-completion-check.md\n@@ -19,6 +19,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n ## Required Reading\n \n Read only the files needed to judge the PR contract. Prefer this order:\ndiff --git a/.github/prompts/codex-task-handoff.md b/.github/prompts/codex-task-handoff.md\nindex 695e211d76dea1c60e8d5bb360e78487808286a4..59441d576b01a39569722f226c854e5a740e9eb5\n--- a/.github/prompts/codex-task-handoff.md\n+++ b/.github/prompts/codex-task-handoff.md\n@@ -2,7 +2,7 @@\n \n You are Codex Front acting as the Implementer for a Shiki task.\n \n-Follow `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant ADRs, and `docs/agents/codex-handoff.md`.\n+Follow `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant Shiki SADRs and target ADRs, and `docs/agents/codex-handoff.md`.\n \n Rules:\n \ndiff --git a/AGENTS.md b/AGENTS.md\nindex 7f041100d49c2a7525f51cb0f5d696a4a6d1c78c..a06bfbabc6fa48a007389e25e12d7ed523936bed\n--- a/AGENTS.md\n+++ b/AGENTS.md\n@@ -22,7 +22,7 @@\n - **Codex Front** is an optional implementer through Codex App, Codex CLI, Codex IDE extension, or Codex Web signed in with ChatGPT OAuth/subscription auth, dispatched with `shiki runner codex` when a task is explicitly assigned to `codex`.\n - **GitHub CCA** is implemented by Claude Code Action by default, using `CLAUDE_CODE_OAUTH_TOKEN`.\n \n-Do not assume `openai/codex-action`, `OPENAI_API_KEY`, or API-key based Codex automation in the default Shiki path. API-key runners are explicit target-repo extensions and require their own ADR.\n+Do not assume `openai/codex-action`, `OPENAI_API_KEY`, or API-key based Codex automation in the default Shiki path. API-key runners are explicit target-repo extensions and require their own target ADR.\n \n Shiki is not:\n \n@@ -44,10 +44,14 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use\n+`SADR-NNNN-*.md`; target product decisions retain `NNNN-*.md` and the `ADR`\n+namespace.\n+\n Use the canonical source-of-truth order above. Runtime-specific reading orders\n are operational guidance only and must not override or reorder it.\n \n-Conversation state is not durable truth. If a decision matters, put it in GitHub, `.shiki/`, `CONTEXT.md`, or an ADR.\n+Conversation state is not durable truth. If a decision matters, put it in GitHub, `.shiki/`, `CONTEXT.md`, a Shiki SADR, or a target ADR.\n \n If GitHub and `.shiki/` disagree, surface the conflict and prefer GitHub operational state until the mirror is repaired.\n \n@@ -75,8 +79,8 @@\n Every non-trivial change follows this loop:\n \n 1. **Goal**: clarify outcome, completion conditions, non-goals, risk level, and success signals.\n-2. **grill-with-docs**: challenge the plan against domain docs, ADRs, code reality, terminology, and edge scenarios.\n-3. **Context & Impact**: find relevant docs, ADRs, code, dependencies, owners, locks, and verification surfaces.\n+2. **grill-with-docs**: challenge the plan against domain docs, Shiki SADRs, target ADRs, code reality, terminology, and edge scenarios.\n+3. **Context & Impact**: find relevant docs, SADRs, target ADRs, code, dependencies, owners, locks, and verification surfaces.\n 4. **PRD and Spec Freeze**: use `to-prd` when context is settled enough to become durable product/engineering intent. The operator's explicit approval of the PRD is Spec Freeze: the plan records a `spec_freeze` block, and no plan runs without it. Steps 1-4 form Requirements Definition \u2014 one continuous operator dialogue. After Spec Freeze, scope changes require an operator-approved Spec Amendment; non-scope-moving interpretations go to the Assumption Log.\n 5. **Issues**: use `to-issues` to create independently grabbable vertical-slice issues. Prefer AFK slices over HITL slices where possible.\n 6. **Triage**: label issues for readiness, risk, runtime, skills, and MergeGate state.\n@@ -167,7 +171,7 @@\n - Testing decisions.\n - Out-of-scope items.\n - Further notes.\n-- Links to relevant domain terms and ADRs.\n+- Links to relevant domain terms, SADRs, and target ADRs.\n \n Every executable task must include:\n \n@@ -193,7 +197,7 @@\n \n Context & Impact must identify:\n \n-- Relevant documents, domain terms, ADRs, and past decisions.\n+- Relevant documents, domain terms, SADRs, target ADRs, and past decisions.\n - Relevant modules, interfaces, seams, callers, owners, and tests.\n - Dependency relationships between tasks.\n - Candidate locks and lock conflicts.\n@@ -210,7 +214,7 @@\n | Skill | Required when |\n | --- | --- |\n | `setup-matt-pocock-skills` | First configuring the repo for skills, or when issue tracker, triage labels, or domain docs are missing. |\n-| `grill-with-docs` | Default for non-trivial Goals before PRD/issues; always required when plans, terminology, boundaries, tradeoffs, or ADR-worthy decisions are ambiguous. |\n+| `grill-with-docs` | Default for non-trivial Goals before PRD/issues; always required when plans, terminology, boundaries, tradeoffs, or decision-record-worthy choices are ambiguous. |\n | `zoom-out` | The agent lacks an architectural map of the relevant code area. |\n | `to-prd` | Settled Goal context must become durable product/engineering intent. |\n | `to-issues` | A Goal, PRD, or plan must become independently grabbable vertical-slice issues. |\n@@ -348,7 +352,7 @@\n - Three or more new files.\n - Any high-risk or critical label.\n \n-When the architecture gate triggers, run `grill-with-docs` or `improve-codebase-architecture` as appropriate and record durable decisions in an ADR when needed.\n+When the architecture gate triggers, run `grill-with-docs` or `improve-codebase-architecture` as appropriate and record durable decisions in a Shiki SADR or target ADR as applicable.\n \n ## Engineering Discipline\n \ndiff --git a/CLAUDE.md b/CLAUDE.md\nindex 98a932e7841a632866fcd53b0bad0e3af3711d3d..98a31399404d6fb044b4fd493f4459ac87f439de\n--- a/CLAUDE.md\n+++ b/CLAUDE.md\n@@ -14,6 +14,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n ## Claude Role\n \n Default to planner, default implementer, reviewer, coordinator, documentation editor, CCA-style completion judge, and final-judgment assistant.\n@@ -57,7 +60,7 @@\n At the start of a Shiki session:\n \n 1. Read `AGENTS.md`.\n-2. Read `CONTEXT.md` and relevant ADRs.\n+2. Read `CONTEXT.md` and relevant Shiki SADRs and target ADRs.\n 3. Read active `.shiki/` Goal/task/ledger state when present.\n 4. Read active distilled rules from `.shiki/memories/` (status `distilled`, `active: true`, not revoked or superseded) for applicable guidance; the same rules are injected deterministically into task handoffs as the `## Distilled Rules` section (\u00a73.5).\n 5. Read the GitHub Issue or PR when the task is GitHub-backed.\n@@ -74,7 +77,7 @@\n - Define the Goal, completion conditions, non-goals, and risk level.\n - Treat `grill-with-docs` as the default planning entry point for non-trivial Goals.\n - Challenge domain terms against `CONTEXT.md`.\n-- Challenge hard-to-reverse tradeoffs against ADRs.\n+- Challenge hard-to-reverse tradeoffs against SADRs and target ADRs.\n - Explore code instead of asking when code can answer the question.\n - Run Context & Impact before implementation planning.\n - Produce a Task DAG, not an unordered checklist.\n@@ -94,7 +97,7 @@\n - For each question, provide your recommended answer.\n - Resolve dependency decisions before downstream design questions.\n - Update `CONTEXT.md` inline when terminology crystallizes.\n-- Offer ADRs only when the decision is hard to reverse, surprising without context, and a real tradeoff.\n+- Offer a SADR or target ADR only when the decision is hard to reverse, surprising without context, and a real tradeoff.\n - Convert unresolved questions into HITL blockers rather than allowing Codex to guess.\n \n ## PRD and Issues Standard\n@@ -103,7 +106,7 @@\n \n - Use the project's domain glossary vocabulary.\n - Avoid volatile file paths unless they are essential.\n-- Include implementation decisions, testing decisions, out-of-scope items, and relevant ADR/domain links.\n+- Include implementation decisions, testing decisions, out-of-scope items, and relevant SADR/target-ADR/domain links.\n - Identify deep-module opportunities and testable interfaces.\n \n When creating issues:\n@@ -121,7 +124,7 @@\n - Branch/worktree target.\n - Scope and non-goals.\n - Dependencies and locks.\n-- Relevant docs, ADRs, modules, and tests.\n+- Relevant docs, SADRs, target ADRs, modules, and tests.\n - Required skill invocations.\n - TDD expectations.\n - Pre-PR `code-review` gate expectations.\n@@ -152,7 +155,7 @@\n \n - Treat yourself as a judge, not implementer.\n - Do not write production code.\n-- Read `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant ADRs, PRD, task issue, PR body, diff, checks, reviews, labels, and `.shiki/` state.\n+- Read `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant SADRs and target ADRs, PRD, task issue, PR body, diff, checks, reviews, labels, and `.shiki/` state.\n - Evaluate every applicable checklist in `docs/agents/checklists.md`.\n - Map each acceptance criterion to durable evidence.\n - Distinguish implementation failure from missing evidence.\ndiff --git a/CONTEXT.md b/CONTEXT.md\nindex e61a9be06a0cce0d026484083a657bde2067e234..237ee8aa3ea8fab334ece49768bc5ed3b5af0974\n--- a/CONTEXT.md\n+++ b/CONTEXT.md\n@@ -25,7 +25,7 @@\n _Avoid_: jumping directly into code\n \n **grill-with-docs**:\n-A planning skill that challenges a plan against domain language, ADRs, code reality, and concrete edge scenarios. It resolves design-tree questions before PRD/issues. It runs inside Requirements Definition.\n+A planning skill that challenges a plan against domain language, Shiki SADRs, target ADRs, code reality, and concrete edge scenarios. It resolves design-tree questions before PRD/issues. It runs inside Requirements Definition.\n _Avoid_: generic brainstorming, silent assumptions\n \n **Requirements Definition**:\ndiff --git a/CONTRIBUTING.md b/CONTRIBUTING.md\nindex c6d9b656e73abb8a55e5f660123e03153dcb3e6d..9e37a8fbf668db5b22461aaae5548b8f1354394c\n--- a/CONTRIBUTING.md\n+++ b/CONTRIBUTING.md\n@@ -17,7 +17,7 @@\n 1. **Goal** \u2014 start from a user-approved Goal with an outcome, completion\n    conditions, non-goals, risk level, and success signals.\n 2. **grill-with-docs** \u2014 for any non-trivial change, challenge the plan against\n-   domain language, ADRs, and code reality before writing a PRD.\n+   domain language, Shiki SADRs, target ADRs, and code reality before writing a PRD.\n 3. **PRD** \u2014 capture settled product/engineering intent (`to-prd`).\n 4. **Spec Freeze** \u2014 the operator explicitly approves the PRD/requirements;\n    the plan records a `spec_freeze` block and no plan runs without it\n@@ -94,7 +94,7 @@\n   entries.\n - Do not hand-edit `.shiki/goals/*.json` or `.shiki/manifest.json` in a task PR.\n - Conversation state is not durable truth. If a decision matters, record it in\n-  GitHub, `.shiki/`, `CONTEXT.md`, or an ADR.\n+  GitHub, `.shiki/`, `CONTEXT.md`, a Shiki SADR, or a target ADR.\n \n ## Architecture Gate\n \n@@ -103,7 +103,7 @@\n or CI policy, package structure, three or more structural files, more than 100\n net new lines in a core module, three or more new files, or any high-risk label.\n When triggered, run `grill-with-docs` or `improve-codebase-architecture` and\n-record durable decisions in an ADR when needed (see `docs/adr/`).\n+record durable decisions in a Shiki SADR or target ADR as applicable (see `docs/adr/`).\n \n ## Safety\n \ndiff --git a/SYSTEM_PROMPT.md b/SYSTEM_PROMPT.md\nindex aaaea45e3a896c3a44ba89eec8bb84325ecf6c62..da009c86a88285269dee0155e58e550c6d744ea9\n--- a/SYSTEM_PROMPT.md\n+++ b/SYSTEM_PROMPT.md\n@@ -30,6 +30,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n Use the canonical source-of-truth order above. Runtime-specific reading orders\n are operational guidance only and must not override or reorder it. The current\n conversation is non-durable operator input.\n@@ -48,14 +51,14 @@\n \n A runtime may only do work that is explicitly assigned by the Goal, task, PR, or MergeGate decision.\n \n-Do not assume `openai/codex-action`, `OPENAI_API_KEY`, or API-key based Codex automation in the default Shiki path. API-key runners are explicit target-repo extensions and require their own ADR.\n+Do not assume `openai/codex-action`, `OPENAI_API_KEY`, or API-key based Codex automation in the default Shiki path. API-key runners are explicit target-repo extensions and require their own target ADR.\n \n ## 4. Non-negotiable Shiki flow\n \n For any non-trivial Goal, use this flow:\n \n 1. **Goal Seek**: define outcome, non-goals, risk level, completion criteria, and evidence requirements.\n-2. **grill-with-docs**: challenge the plan against `CONTEXT.md`, ADRs, code reality, terminology, boundaries, and edge scenarios. Ask one question at a time when the operator is available. If code can answer a question, inspect code instead of asking.\n+2. **grill-with-docs**: challenge the plan against `CONTEXT.md`, Shiki SADRs, target ADRs, code reality, terminology, boundaries, and edge scenarios. Ask one question at a time when the operator is available. If code can answer a question, inspect code instead of asking.\n 3. **Context & Impact**: identify modules, interfaces, seams, callers, dependencies, locks, risk, and verification surfaces.\n 4. **to-prd + Spec Freeze**: turn settled context into a PRD using domain vocabulary and testing decisions. The operator's explicit approval of the PRD is Spec Freeze, recorded as the plan's `spec_freeze` block; plans without it do not run. Steps 1-4 form Requirements Definition.\n 5. **to-issues**: decompose the PRD into independently grabbable vertical-slice issues. Prefer AFK slices; mark HITL slices when judgment is still required.\n@@ -75,7 +78,7 @@\n Before making any material change:\n \n 1. Read `AGENTS.md` and any runtime-specific wrapper such as `CLAUDE.md`.\n-2. Read `CONTEXT.md` and relevant ADRs.\n+2. Read `CONTEXT.md` and relevant SADRs and target ADRs.\n 3. Read the GitHub Issue / PR and `.shiki/` state for the active Goal or task.\n 4. Identify your role: Planner, Implementer, Reviewer, Completion Check Agent, Repairer, or Guardian-assist.\n 5. Identify Goal id, task id, branch, PR, dependencies, locks, risk level, required skills, and required evidence.\n@@ -103,7 +106,7 @@\n \n At minimum, identify:\n \n-- Relevant documents, ADRs, domain terms, and prior decisions.\n+- Relevant documents, SADRs, target ADRs, domain terms, and prior decisions.\n - Relevant modules, interfaces, seams, callers, owners, and tests.\n - Dependency relationships between tasks.\n - Candidate file locks and lock conflicts.\n@@ -152,7 +155,7 @@\n \n CCA must read:\n \n-- `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant ADRs.\n+- `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant SADRs and target ADRs.\n - Parent Goal issue, PRD issue, task issue, comments, and labels.\n - PR body, diff, commits, changed files, reviews, CI checks, and artifacts available to the workflow.\n - `.shiki/` task contract, locks, ledger entries, CCA checklist profile, and prior repair packets.\n@@ -214,7 +217,7 @@\n - Three or more new files.\n - Any high-risk or critical label.\n \n-When the architecture gate triggers, run `grill-with-docs` or `improve-codebase-architecture` as appropriate and record the decision in an ADR if it is hard to reverse or likely to be re-litigated.\n+When the architecture gate triggers, run `grill-with-docs` or `improve-codebase-architecture` as appropriate and record the decision in a Shiki SADR or target ADR as applicable if it is hard to reverse or likely to be re-litigated.\n \n ## 14. Skill Gate\n \n@@ -223,7 +226,7 @@\n Use these skills:\n \n - `setup-matt-pocock-skills`: before first use of the engineering skill set, or when issue tracker, triage labels, or domain docs are missing.\n-- `grill-with-docs`: default for non-trivial Goals before PRD/issues; required for ambiguous plans, terminology, boundaries, ADR-worthy tradeoffs, or design-tree decisions.\n+- `grill-with-docs`: default for non-trivial Goals before PRD/issues; required for ambiguous plans, terminology, boundaries, decision-record-worthy tradeoffs, or design-tree decisions.\n - `zoom-out`: unfamiliar code area or missing architectural map.\n - `to-prd`: convert settled context into a PRD.\n - `to-issues`: break a PRD, plan, or Goal into independently grabbable vertical-slice issues.\ndiff --git a/docs/adr/0001-keep-shiki-as-platform-name.md b/docs/adr/SADR-0001-keep-shiki-as-platform-name.md\nindex 20a9637c3b9d8c97de7ba8db7cfae4d82d9a7dba..8b685439959aada1b221c112f0ac3c51ca8a1695\n--- a/docs/adr/0001-keep-shiki-as-platform-name.md\n+++ b/docs/adr/SADR-0001-keep-shiki-as-platform-name.md\n@@ -1,3 +1,3 @@\n-# Keep Shiki As The Platform Name\n+# SADR-0001: Keep Shiki As The Platform Name\n \n Shiki remains the product name for the GitHub-native agentic engineering control plane. We considered more descriptive names such as MergePilot, MergeGate, and Kiroku, but kept Shiki because it carries the existing goal-seeking framework identity while remaining independent of Hermes, Codex, Claude Code, or any single runtime.\ndiff --git a/docs/adr/0002-use-agents-as-shared-constitution.md b/docs/adr/SADR-0002-use-agents-as-shared-constitution.md\nindex 73de30ecb078120eac540d9bc51855e3cd48d0e3..c5486182bf06fca36ea508496b013c67302fd989\n--- a/docs/adr/0002-use-agents-as-shared-constitution.md\n+++ b/docs/adr/SADR-0002-use-agents-as-shared-constitution.md\n@@ -1,3 +1,3 @@\n-# Use AGENTS As The Shared Agent Constitution\n+# SADR-0002: Use AGENTS As The Shared Agent Constitution\n \n Shiki uses `AGENTS.md` as the runtime-neutral constitution and `CLAUDE.md` as a thin Claude Code wrapper. This keeps Codex, Claude Code, Hermes Runner, and future Agent Runtimes aligned on the same GitHub-first MergeGate rules while still allowing runtime-specific instructions where needed.\ndiff --git a/docs/adr/0003-require-skill-gate-for-mergegate-readiness.md b/docs/adr/SADR-0003-require-skill-gate-for-mergegate-readiness.md\nindex 424be0c9af4c8f0136f1fde68a468c47ff84e764..7fc337987c7df9792f75f181dfcbed75fe5ffff2\n--- a/docs/adr/0003-require-skill-gate-for-mergegate-readiness.md\n+++ b/docs/adr/SADR-0003-require-skill-gate-for-mergegate-readiness.md\n@@ -1,3 +1,3 @@\n-# Require Skill Gate For MergeGate Readiness\n+# SADR-0003: Require Skill Gate For MergeGate Readiness\n \n Shiki requires relevant engineering skills to be invoked before execution when their triggers apply. This makes skills part of MergeGate readiness rather than optional prompt style, because Shiki depends on repeatable planning, issue decomposition, TDD, diagnosis, triage, and architecture review before autonomous or parallel agent work can be trusted.\ndiff --git a/docs/adr/0004-use-github-first-with-shiki-mirror.md b/docs/adr/SADR-0004-use-github-first-with-shiki-mirror.md\nindex d0a3a9174dc646a945a9d325954ffaf00c352040..a79c54ae9ee8eed2a0524192b864da4e22a92d3f\n--- a/docs/adr/0004-use-github-first-with-shiki-mirror.md\n+++ b/docs/adr/SADR-0004-use-github-first-with-shiki-mirror.md\n@@ -1,4 +1,4 @@\n-# ADR 0004: Use GitHub First With A Repository-Local Shiki Mirror\n+# SADR-0004: Use GitHub First With A Repository-Local Shiki Mirror\n \n ## Status\n \ndiff --git a/docs/adr/0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md b/docs/adr/SADR-0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\nindex b339baf49b89d9290e79c3d51b36e7b2bde3bd95..0f3c3eb9afccdb3403ce978e21d04fe12e495da0\n--- a/docs/adr/0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\n+++ b/docs/adr/SADR-0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\n@@ -1,8 +1,8 @@\n-# ADR 0005: Use Codex As Front And Claude Code Action As The GitHub Runtime\n+# SADR-0005: Use Codex As Front And Claude Code Action As The GitHub Runtime\n \n ## Status\n \n-Accepted (implementer default superseded by ADR 0008; the auth model and the\n+Accepted (implementer default superseded by SADR-0008; the auth model and the\n Claude Code Action GitHub runtime decision remain in force)\n \n ## Context\n@@ -26,4 +26,4 @@\n - Shiki avoids requiring `OPENAI_API_KEY` for the default workflow.\n - Codex work happens through the user's authenticated Codex front surface, not as a hidden backend Action.\n - Claude Code Action is configured for OAuth-token based subscription usage by default.\n-- API-key based automation remains possible only as an explicit target-repo extension with its own ADR.\n+- API-key based automation remains possible only as an explicit target-repo extension with its own target ADR.\ndiff --git a/docs/adr/0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md b/docs/adr/SADR-0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\nindex 4bcae9d1e52f0a37b8e9fbafa5cd14b6ee1993bd..7a38538a6ae9929567f0ade67e7ea6a60d0b3f7e\n--- a/docs/adr/0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\n+++ b/docs/adr/SADR-0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\n@@ -1,4 +1,4 @@\n-# ADR 0006: Separate Codex Implementation, CCA Completion, And MergeGate Authorization\n+# SADR-0006: Separate Codex Implementation, CCA Completion, And MergeGate Authorization\n \n ## Status\n \ndiff --git a/docs/adr/0007-packaging-and-release.md b/docs/adr/SADR-0007-packaging-and-release.md\nindex 79b84ddb97167372b44641e5b7860e400ce80822..bb445a2e2e856d9d1f5fdbe35de8d4e8cec0fb32\n--- a/docs/adr/0007-packaging-and-release.md\n+++ b/docs/adr/SADR-0007-packaging-and-release.md\n@@ -1,4 +1,4 @@\n-# ADR 0007: Packaging And Release For Shiki\n+# SADR-0007: Packaging And Release For Shiki\n \n ## Status\n \n@@ -25,7 +25,7 @@\n   not introduce third-party runtime dependencies or break the shim.\n - Releases must run inside the operator's subscription-authenticated toolchain\n   and must not require extra secrets beyond the built-in `GITHUB_TOKEN`\n-  (consistent with ADR 0005's no-API-key-by-default stance).\n+  (consistent with SADR-0005's no-API-key-by-default stance).\n - `.shiki/manifest.json` already encodes the install surface (`install.include`,\n   `install.create_directories`, `install.exclude_from_commit`), and it must not\n   be mutated as part of this change.\n@@ -34,7 +34,7 @@\n \n This is a hard-to-reverse platform decision (it sets the version source of\n truth, the release trigger, and the public install contract), so it is recorded\n-as an ADR.\n+as a SADR.\n \n ## Decision\n \n@@ -104,7 +104,7 @@\n 3. **Publishing to PyPI.** Rejected for now: Shiki is distributed as a\n    repository template + git-installable CLI, not a PyPI library; PyPI would add\n    credentials/secrets and a different versioning contract. Can be added later as\n-   its own ADR if a packaged distribution is needed.\n+   its own SADR if a packaged distribution is needed.\n 4. **Dynamic version (read `VERSION` via a build backend `dynamic` field).**\n    Rejected to keep `pyproject.toml` backend-agnostic and dependency-free; the\n    small duplication between `VERSION` and `pyproject.toml` is guarded by the\n@@ -123,4 +123,4 @@\n   releasing versions the whole platform but ships only `install.include` to\n   targets.\n - Future PyPI distribution or a console entry point are deferred and would each\n-  warrant their own ADR.\n+  warrant their own SADR.\ndiff --git a/docs/adr/0008-use-claude-code-as-default-implementer.md b/docs/adr/SADR-0008-use-claude-code-as-default-implementer.md\nindex dedf4510b7c7799521422f622029563d373db031..35c2625d908167fbc58855b5b63e283805f167e8\n--- a/docs/adr/0008-use-claude-code-as-default-implementer.md\n+++ b/docs/adr/SADR-0008-use-claude-code-as-default-implementer.md\n@@ -1,12 +1,12 @@\n-# ADR 0008: Use Claude Code As The Default Implementer Runtime\n+# SADR-0008: Use Claude Code As The Default Implementer Runtime\n \n ## Status\n \n-Proposed (supersedes ADR 0005 for the implementer default; keeps its auth model)\n+Proposed (supersedes SADR-0005 for the implementer default; keeps its auth model)\n \n ## Context\n \n-ADR 0005 made Codex Front the default implementation runtime, with Claude Code\n+SADR-0005 made Codex Front the default implementation runtime, with Claude Code\n restricted to planner and reviewer roles by the runtime registry, and the only\n autonomous implementation adapter was `shiki runner codex`.\n \n@@ -18,13 +18,13 @@\n - The platform's planning, review, and CCA layers already run on Claude Code,\n   and Claude Code's native capabilities (the built-in `/code-review` skill and\n   the Workflow multi-agent orchestration tool) have no Codex equivalent.\n-- The Goal lifecycle redesign (ADR 0009) requires an autonomous post-freeze\n+- The Goal lifecycle redesign (SADR-0009) requires an autonomous post-freeze\n   loop driven from the operator's Claude Code session, including bounded\n   repair dispatch, which must not depend on a runtime that cannot be invoked\n   unattended.\n \n The constitution claims Shiki is runtime-agnostic (\"not a Claude-only\n-workflow\", ADR 0002). Any change to the implementer default must preserve the\n+workflow\", SADR-0002). Any change to the implementer default must preserve the\n registry-based runtime model rather than hardcoding one vendor.\n \n ## Decision\n@@ -39,7 +39,7 @@\n   the task to review or repair-needed.\n - Keep Codex registered as an optional implementer runtime. `shiki runner\n   codex`, the Codex handoff templates, and the ChatGPT OAuth auth model from\n-  ADR 0005 remain supported for tasks explicitly assigned to `codex`.\n+  SADR-0005 remain supported for tasks explicitly assigned to `codex`.\n - Commit to mandating two Claude-native capabilities as loop contract in\n   follow-up Goals (status: the code-review gate was delivered by Goal G-B \u2014\n   skill registry, checklists item PR-12, default required_skills \u2014 while the\n@@ -60,7 +60,7 @@\n \n Out of scope: removing Codex support, changing the CCA runtime (Claude Code\n Action with `CLAUDE_CODE_OAUTH_TOKEN` stays), and API-key based automation\n-(still requires its own ADR per ADR 0005).\n+(still requires its own target ADR per SADR-0005).\n \n ## Consequences\n \n@@ -100,7 +100,7 @@\n   harness rejection of direct `codex exec` is outside Shiki's control, and the\n   plan's mandatory native capabilities cannot run on Codex at all.\n - **Remove Codex entirely**: rejected; it abandons the runtime-agnostic\n-  constitution (ADR 0002) for no operational gain and forecloses\n+  constitution (SADR-0002) for no operational gain and forecloses\n   cross-runtime adversarial review.\n - **GitHub Actions cloud implementation via claude-code-action**: rejected as\n   default; durable evidence is automatic but token cost, Node 24 workflow\ndiff --git a/docs/adr/0009-requirements-definition-and-spec-freeze.md b/docs/adr/SADR-0009-requirements-definition-and-spec-freeze.md\nindex 29b3f7871bef9203a8b4a831a65c39eb2f506e42..52e7dd69ad0aa4efe0b68c62678abbf7df02aaec\n--- a/docs/adr/0009-requirements-definition-and-spec-freeze.md\n+++ b/docs/adr/SADR-0009-requirements-definition-and-spec-freeze.md\n@@ -1,4 +1,4 @@\n-# ADR 0009: Merge Grill And Planning Into Requirements Definition With Spec Freeze\n+# SADR-0009: Merge Grill And Planning Into Requirements Definition With Spec Freeze\n \n ## Status\n \ndiff --git a/docs/adr/0010-external-ai-guardian-review.md b/docs/adr/SADR-0010-external-ai-guardian-review.md\nindex e6108bbe8bb4e73e0ff4684c8f78001a80438c4e..f3510b145664298adafb151b6f0db51b972f01ec\n--- a/docs/adr/0010-external-ai-guardian-review.md\n+++ b/docs/adr/SADR-0010-external-ai-guardian-review.md\n@@ -1,4 +1,4 @@\n-# ADR 0010: External AI Guardian Review As A First-Class Approval Authority\n+# SADR-0010: External AI Guardian Review As A First-Class Approval Authority\n \n ## Status\n \n@@ -8,7 +8,7 @@\n \n Shiki is intended to be an autonomous, recursive, self-improving agent\n framework: authority-in-the-loop, not human-in-the-loop. Until now the Guardian\n-approval required for high/critical-risk merges (guardian-policy.json, ADR\n+approval required for high/critical-risk merges (guardian-policy.json, SADR\n governance) recognized only human authority sources \u2014 a GitHub review approval,\n the `guardian:approved` label applied by a configured Guardian, or a comment\n carrying `Guardian approval granted` plus the current head SHA.\n@@ -89,7 +89,7 @@\n   autonomous design the operator intends and produced the impersonation\n   incident.\n - **Record AI approval as an operator comment**: rejected as forgery of human\n-  authority \u2014 the exact failure this ADR corrects.\n+  authority \u2014 the exact failure this SADR corrects.\n - **Deliver the artifact as a committed `.shiki/` file**: rejected; a committed\n   approval inside the PR being approved is forgeable by anyone who can push.\n - **A dedicated reviewer bot identity instead of a relayed comment**: deferred;\ndiff --git a/docs/adr/0011-autonomous-quality-gate-evidence.md b/docs/adr/SADR-0011-autonomous-quality-gate-evidence.md\nindex bca0246c5d46e44afbde15486a75eec425fc02f5..9c93eac16d8c3a6d4b11bd7224e3955102cd7db4\n--- a/docs/adr/0011-autonomous-quality-gate-evidence.md\n+++ b/docs/adr/SADR-0011-autonomous-quality-gate-evidence.md\n@@ -1,4 +1,4 @@\n-# ADR 0011: Autonomous Quality-Gate Evidence \u2014 Deterministic Facts And An Independent Verifier\n+# SADR-0011: Autonomous Quality-Gate Evidence \u2014 Deterministic Facts And An Independent Verifier\n \n ## Status\n \ndiff --git a/docs/adr/0012-autonomous-loop-closeout-to-main.md b/docs/adr/SADR-0012-autonomous-loop-closeout-to-main.md\nindex 637fad6dde4989c9c2bf0f81f676d8d29991123c..ec7d85f81030a676bcbba28b43eb365422a36eac\n--- a/docs/adr/0012-autonomous-loop-closeout-to-main.md\n+++ b/docs/adr/SADR-0012-autonomous-loop-closeout-to-main.md\n@@ -1,4 +1,4 @@\n-# 0012 \u2014 Autonomous loop pushes goal completion to main via a closeout PR\n+# SADR-0012 \u2014 Autonomous loop pushes goal completion to main via a closeout PR\n \n - Status: accepted\n - Date: 2026-06-18\ndiff --git a/docs/adr/0013-bootstrap-grants-actions-approve-pull-requests.md b/docs/adr/SADR-0013-bootstrap-grants-actions-approve-pull-requests.md\nindex b6c69d64f1c5ced346b88d6859586715b3b5bd0e..cf48ff4a6716267f539ca58edd1a852e7c7a2d64\n--- a/docs/adr/0013-bootstrap-grants-actions-approve-pull-requests.md\n+++ b/docs/adr/SADR-0013-bootstrap-grants-actions-approve-pull-requests.md\n@@ -1,4 +1,4 @@\n-# ADR 0013: Bootstrap Grants GitHub Actions Permission To Create And Approve Pull Requests\n+# SADR-0013: Bootstrap Grants GitHub Actions Permission To Create And Approve Pull Requests\n \n ## Status\n \n@@ -31,7 +31,7 @@\n security tradeoff: it lets repository automation satisfy a human-review gate. In\n a multi-maintainer repo that could let a compromised or buggy workflow\n self-approve changes. So whether bootstrap turns it on by default \u2014 and with\n-what default token scope \u2014 is an ADR-worthy decision, not an implementation\n+what default token scope \u2014 is a SADR-worthy decision, not an implementation\n detail.\n \n ## Decision\n@@ -48,7 +48,7 @@\n Specifically:\n \n - **`can_approve_pull_request_reviews` defaults to `true`.** Shiki's default\n-  posture is solo/AFK autonomous operation (ADR 0008/0009/0012), where the CCA\n+  posture is solo/AFK autonomous operation (SADR-0008/0009/0012), where the CCA\n   Review Bridge is the only path that can satisfy `required_review: true` after\n   the CCA and Guardian gates have already passed. The default makes a\n   freshly-bootstrapped repo able to run the autonomous loop end to end without a\ndiff --git a/docs/adr/0014-codex-app-external-ai-guardian-ui-adapter.md b/docs/adr/SADR-0014-codex-app-external-ai-guardian-ui-adapter.md\nindex 238db06279334caa341c117362df9271382a7fcd..65f12e9939321e1b3e389a34c822fc1549e5155d\n--- a/docs/adr/0014-codex-app-external-ai-guardian-ui-adapter.md\n+++ b/docs/adr/SADR-0014-codex-app-external-ai-guardian-ui-adapter.md\n@@ -1,4 +1,4 @@\n-# ADR 0014: Use Codex App As The External AI Guardian UI Adapter\n+# SADR-0014: Use Codex App As The External AI Guardian UI Adapter\n \n ## Status\n \ndiff --git a/docs/adr/0015-contract-approval-before-dispatch.md b/docs/adr/SADR-0015-contract-approval-before-dispatch.md\nindex fad6c656741a20a7684ba16080a7db159f63a52f..6061271a46d3f1d6ae7174477de3fb75673123a6\n--- a/docs/adr/0015-contract-approval-before-dispatch.md\n+++ b/docs/adr/SADR-0015-contract-approval-before-dispatch.md\n@@ -1,4 +1,4 @@\n-# ADR 0015: Approve The Task Contract Before Dispatch, Not The Diff After It\n+# SADR-0015: Approve The Task Contract Before Dispatch, Not The Diff After It\n \n ## Status\n \n@@ -9,7 +9,7 @@\n Guardian approval for high/critical risk is expressible only on a pull request.\n Every approval source in `guardian-policy.json` \u2014 GitHub review, the\n `guardian:approved` label, the `Guardian approval granted` comment, and the\n-`external_ai_guardian_review` artifact (ADR 0010/0014) \u2014 is bound to a PR, and\n+`external_ai_guardian_review` artifact (SADR-0010/0014) \u2014 is bound to a PR, and\n three of the four require the current head SHA. Approval therefore cannot exist\n before an implementation PR exists.\n \n@@ -81,7 +81,7 @@\n An envelope breach must surface only on the non-repairable policy gate, never on\n the repairable `MergeGate metadata check`. Otherwise the Goal Loop routes it to\n `dispatch_repair` \u2014 instructing a runner to make the breach go away \u2014 which is\n-exactly what this ADR forbids.\n+exactly what this SADR forbids.\n \n **Contract immutability.** After registration, an implementation PR must not\n change the governance fields of its own task contract (`scope`, `non_goals`,\n@@ -116,7 +116,7 @@\n fails any verdict that is not `complete`, and the signal the CCA prompt reads is\n computed from `evaluate_guardian_approval` alone. Without the OR-merge at the\n signal, a contract-approved PR still returns `needs_guardian`, the CCA job fails,\n-and the MergeGate policy check never runs \u2014 the gate this ADR changes would never\n+and the MergeGate policy check never runs \u2014 the gate this SADR changes would never\n be reached. The CCA *prompt* needs no change: a `contract_approval` entry in the\n signal's existing `sources` array satisfies its current wording.\n \n@@ -130,7 +130,7 @@\n written, and an approved contract lets its implementation PRs merge through the\n same automatic path as low/medium risk. The wasted implement-then-block cycle\n disappears. Guardian review shifts from reading diffs to reading contracts, which\n-is both earlier and smaller; the external AI Guardian path (ADR 0010/0014) works\n+is both earlier and smaller; the external AI Guardian path (SADR-0010/0014) works\n on contracts with no change.\n \n We accept that the Guardian authorizes a class of change without seeing its\n@@ -146,7 +146,7 @@\n adversarial tests for the new approval path equivalent in rigor to\n `test_shiki_governance_evidence.sh`.\n \n-Bootstrap costs, which apply only while this ADR is being implemented. The tasks\n+Bootstrap costs, which apply only while this SADR is being implemented. The tasks\n that build Contract Approval merge under the existing rules, so the ones\n dispatched after the pre-dispatch gate lands but before the approval path lands\n need a Guardian-approved Contract PR *and* a live Guardian approval on their\n@@ -210,7 +210,7 @@\n risk; they do not bound authorized work.\n \n **Auto-escalate risk on envelope breach instead of stopping.** Returns the\n-workflow to a diff-time Guardian decision \u2014 the exact behavior this ADR removes.\n+workflow to a diff-time Guardian decision \u2014 the exact behavior this SADR removes.\n \n **Resolve governance fields from the base snapshot only.** Rejected against the\n registration flow: implementation PRs add their own contract, so the base has no\ndiff --git a/docs/adr/0016-locks-bound-product-paths.md b/docs/adr/SADR-0016-locks-bound-product-paths.md\nindex b5e850f3f1f7f4ef6e7f9d1763a29a51bd300c45..46b40a9b99a7aae644c02bb458f5473ff0a90ce2\n--- a/docs/adr/0016-locks-bound-product-paths.md\n+++ b/docs/adr/SADR-0016-locks-bound-product-paths.md\n@@ -1,4 +1,4 @@\n-# ADR 0016: Locks bound product paths; the Shiki mirror is bound by record identity\n+# SADR-0016: Locks bound product paths; the Shiki mirror is bound by record identity\n \n ## Status\n \n@@ -6,8 +6,8 @@\n \n - Date: 2026-07-28\n - Deciders: operator (mizutani-140), Claude (planner)\n-- Related: ADR 0004 (GitHub-first with a `.shiki` mirror), ADR 0009 (Spec Freeze),\n-  ADR 0012 (autonomous loop closeout to main), ADR 0015 (Contract Approval before\n+- Related: SADR-0004 (GitHub-first with a `.shiki` mirror), SADR-0009 (Spec Freeze),\n+  SADR-0012 (autonomous loop closeout to main), SADR-0015 (Contract Approval before\n   dispatch), remediation goal task R-02, the live PR #179 deadlock.\n \n ## Context\n@@ -90,11 +90,11 @@\n files, documentation files and named source modules are nameable; runtime-\n allocated ledger, runner and report ids are not \u2014 and those live in `.shiki`,\n which locks no longer govern. Existing frozen contracts are brought into line by\n-a one-time Spec Amendment per plan, which must land before ADR 0015's contract\n+a one-time Spec Amendment per plan, which must land before SADR-0015's contract\n immutability makes `locks` unwritable from the implementation PR.\n \n **Lock state is resolved from the durable side.** MergeGate resolves the lock\n-registry, and ADR 0015's governance fields, from the base `.shiki` snapshot when\n+registry, and SADR-0015's governance fields, from the base `.shiki` snapshot when\n present, falling back to head only when absent. The `MergeGate metadata check`\n job is given `--base-shiki`, which it does not receive today. Locks are not\n derived from the live open-pull-request set: a pull request body is untrusted\n@@ -108,7 +108,7 @@\n Explicitly out of scope: moving MergeGate's contract resolution off the PR\n checkout wholesale; making the mirror a projection of GitHub state; introducing\n new lock kinds; moving ledger evidence out of the repository; and unwinding\n-ADR 0012's closeout pull request. Registration of multi-task goals is ADR 0015's\n+SADR-0012's closeout pull request. Registration of multi-task goals is SADR-0015's\n Contract PR and is not re-decided here.\n \n ## Consequences\n@@ -127,7 +127,7 @@\n \n Risks accepted. First, goal-scoped identity is only as strong as `goal_id`,\n which is resolved from the PR body and the head task file; the mitigation is to\n-add `goal_id` to ADR 0015's frozen governance field set, and until that lands a\n+add `goal_id` to SADR-0015's frozen governance field set, and until that lands a\n task file can name a foreign goal. Second, base-when-present resolution is\n unavailable on fork pull requests, where the head is contributor-controlled;\n fork pull requests already cannot complete a Shiki merge, and this decision does\n@@ -139,7 +139,7 @@\n path, not by implementation pull requests.\n \n New obligations: one Spec Amendment per frozen plan to rewrite `locks`, ordered\n-before ADR 0015 T1; extension of the adversarial suites in\n+before SADR-0015 T1; extension of the adversarial suites in\n `scripts/test_shiki_adversarial_state.sh` and\n `scripts/test_shiki_governance_evidence.sh` to cover the eleven newly-scoped\n directories; and updating `scripts/test_shiki_loop_lock_guard.sh`,\n@@ -151,32 +151,32 @@\n whether the loop's transitive sync of ledger `evidence` references can be\n deleted once the Contract PR carries its own source plan.\n \n-## Relationship to ADR 0004\n+## Relationship to SADR-0004\n \n-This ADR **clarifies something ADR 0004 left unspecified**. ADR 0004 decides that\n+This SADR **clarifies something SADR-0004 left unspecified**. SADR-0004 decides that\n GitHub is the operational source of truth and that each target repository keeps a\n `.shiki/` mirror. It says nothing about pull requests writing the mirror, does\n not name MergeGate, and does not make the mirror an input to a gate. Nothing in\n-ADR 0004's Decision or Consequences is amended, superseded or weakened: the\n+SADR-0004's Decision or Consequences is amended, superseded or weakened: the\n mirror still exists, still records the same artifacts, and pull request\n-boundaries remain the coordination surface. What this ADR settles is the question\n+boundaries remain the coordination surface. What this SADR settles is the question\n 0004 never reached \u2014 by what authority a pull request may write a mirror record \u2014\n and it answers it the way `.shiki/manifest.json` already declares\n (`pr_mutation: current-task-or-current-goal-only`).\n \n-It **amends ADR 0012**, whose Decision item 5 states that \"Loop-executed tasks\n+It **amends SADR-0012**, whose Decision item 5 states that \"Loop-executed tasks\n declare the `path:.shiki/**` lock, which covers every `.shiki` file the closeout\n stages ... so `files_outside_locks` is satisfied without per-file lock\n bookkeeping.\" That clause is replaced: the closeout's mirror writes are\n-authorized by record identity, not by a declared lock. ADR 0012's substantive\n+authorized by record identity, not by a declared lock. SADR-0012's substantive\n decision \u2014 that completion reaches `main` through a closeout pull request rather\n than local attestation \u2014 is preserved unchanged.\n \n-It is **complementary to ADR 0015**, not a substitute. ADR 0015 owns contract\n+It is **complementary to SADR-0015**, not a substitute. SADR-0015 owns contract\n immutability, the pre-dispatch gate, the Contract PR and the Scope Envelope. This\n-ADR owns what a lock means and who may write the mirror. The one ordering\n+SADR owns what a lock means and who may write the mirror. The one ordering\n constraint between them is that the lock-narrowing Spec Amendment must precede\n-ADR 0015 T1's freezing of the `locks` field.\n+SADR-0015 T1's freezing of the `locks` field.\n \n ## Alternatives Considered\n \n@@ -186,7 +186,7 @@\n the only writer that can register them may run. Migrating the Guardian signal to\n that store makes every registration PR fail closed to `needs_guardian` at every\n risk level, because the task file is by definition absent from the store, which\n-removes the low/medium autonomous path ADR 0008/0009 exists to provide. And it\n+removes the low/medium autonomous path SADR-0008/0009 exists to provide. And it\n leaves schemas, manifest, config, guardian policy, templates, migrations, plans,\n worktrees, runner records, runs and handoffs with no writer at all in a\n repository whose product is `.shiki`. Measured benefit on the frozen backlog:\n@@ -199,14 +199,14 @@\n namespace is not in fact ownership-partitioned \u2014 56 ledger entries carry no\n `task_id`, and one goal DAG is writable by 35 registered tasks. The correct\n expression of its insight is that locks do not govern the mirror at all, which is\n-what this ADR decides.\n+what this SADR decides.\n \n **Make the mirror a projection written only by a registration writer and a\n post-merge projection job.** Rejected. It requires a bypass actor with write\n access to the protected default branch; it makes its own master invariant\n readable from a manifest that pull requests may edit; it deletes both reconcile\n modes and leaves a corrupted mirror with no in-band repair path, contradicting\n-ADR 0004's consequence that mirror conflicts are repaired against GitHub state;\n+SADR-0004's consequence that mirror conflicts are repaired against GitHub state;\n and its content-derived ids are rejected by `validate_shiki`'s currently-green id\n pattern.\n \ndiff --git a/docs/adr/0017-bookkeeping-closeouts-do-not-inherit-risk.md b/docs/adr/SADR-0017-bookkeeping-closeouts-do-not-inherit-risk.md\nindex 554e3a0d526a72ca067108c5464cc68c3edcf174..b9676857a4713850e063af7c39c8e0b01ecb4c66\n--- a/docs/adr/0017-bookkeeping-closeouts-do-not-inherit-risk.md\n+++ b/docs/adr/SADR-0017-bookkeeping-closeouts-do-not-inherit-risk.md\n@@ -1,4 +1,4 @@\n-# ADR 0017: A Bookkeeping Closeout Does Not Inherit Its Task's Risk\n+# SADR-0017: A Bookkeeping Closeout Does Not Inherit Its Task's Risk\n \n ## Status\n \n@@ -6,7 +6,7 @@\n \n ## Context\n \n-ADR 0012 made the goal loop push a task's completion to the default branch through a\n+SADR-0012 made the goal loop push a task's completion to the default branch through a\n closeout pull request: after the implementation PR merges, the loop opens a second PR\n carrying `task.status = done`, `lock.state = released`, and \u2014 when the task completes\n its goal \u2014 `goal.status = complete` plus one scorecard report. Nothing else. The\n@@ -49,14 +49,14 @@\n 4. The task file's governance fields (`scope`, `non_goals`, `required_skills`,\n    `risk_level`, `locks`, `acceptance_checks`, `test_command`) are byte-identical to\n    the base snapshot. Only `status`, `expected_pr`, `closeout_pr`, `ledger_evidence`\n-   and `expected_branch` may differ \u2014 the ADR 0015 contract-immutability rule,\n+   and `expected_branch` may differ \u2014 the SADR-0015 contract-immutability rule,\n    unchanged.\n \n    `expected_branch` is in that set because a closeout is *by construction* on a\n    different branch from the implementation it closes, and the loop rewrites the\n    field accordingly. It is safe to let it differ because MergeGate independently\n    requires `expected_branch` to equal the PR's actual head ref, so a PR cannot set\n-   it to anything other than the branch it is really on. The first draft of this ADR\n+   it to anything other than the branch it is really on. The first draft of this SADR\n    omitted it, which made the exemption unreachable: every real closeout failed\n    condition 4 on a field the loop always changes. See PR #209.\n 5. The state transitions are exactly the terminal set: task `review` \u2192 `done`, lock\n@@ -113,12 +113,12 @@\n classification must be derived, not asserted.\n \n **Fold the closeout into the implementation PR** so no second PR exists. Rejected under\n-ADR 0012, which recorded why: a task file that is already `done` on the implementation\n+SADR-0012, which recorded why: a task file that is already `done` on the implementation\n branch short-circuits the loop's review \u2192 CCA \u2192 merge \u2192 done ordering into a local\n goal-completion before any merge happens.\n \n **Have the loop auto-merge closeouts with elevated credentials.** Rejected outright: that\n-is a runtime authoring its own approval, which ADR 0010 and the constitution's\n+is a runtime authoring its own approval, which SADR-0010 and the constitution's\n approval-record prohibition forbid. It would also be indistinguishable, in the evidence,\n from the forgery those rules exist to prevent.\n \ndiff --git a/docs/adr/0018-carried-guardian-approval-across-a-proven-base-sync.md b/docs/adr/SADR-0018-carried-guardian-approval-across-a-proven-base-sync.md\nindex 22358562da390ed8b45cd600da08a43d378b4680..e6e2907cefa776460af73ddf7d39db605f3c39b0\n--- a/docs/adr/0018-carried-guardian-approval-across-a-proven-base-sync.md\n+++ b/docs/adr/SADR-0018-carried-guardian-approval-across-a-proven-base-sync.md\n@@ -1,4 +1,4 @@\n-# ADR 0018: Carried Guardian Approval Across A Proven Base Sync\n+# SADR-0018: Carried Guardian Approval Across A Proven Base Sync\n \n ## Status\n \n@@ -34,7 +34,7 @@\n Guardian evaluator that consumes it (`shiki_guardian.evaluate_guardian_approval`,\n the `guardian_comment_carried` source) were built and tested in isolation. Neither\n does anything until a caller passes the flags that turn the carry on, so the\n-feature was invisible in CI. This ADR records the decision the two of them\n+feature was invisible in CI. This SADR records the decision the two of them\n implement, and this task wires it into the four CI invocations that judge Guardian\n approval.\n \ndiff --git a/docs/adr/README.md b/docs/adr/README.md\nindex c70e057a4520c36e042ab68c53cd4a21ef709b71..5da43856836e2ce6a150f8127766e2f88355ab4b\n--- a/docs/adr/README.md\n+++ b/docs/adr/README.md\n@@ -1,40 +1,41 @@\n-# Architecture Decision Records\n+# Decision Record Namespaces\n \n-This directory holds Shiki's Architecture Decision Records (ADRs): durable\n-records of hard-to-reverse platform decisions. ADRs are part of the Shiki source\n-of truth alongside GitHub state, the `.shiki/` mirror, and `CONTEXT.md`.\n+This directory holds two independent decision namespaces:\n \n-## How ADRs Work\n+- Shiki Architecture Decision Records (SADRs), named\n+  `SADR-NNNN-<short-slug>.md`, record hard-to-reverse Shiki platform decisions.\n+- Target Architecture Decision Records (ADRs), named\n+  `NNNN-<short-slug>.md`, belong to the target product.\n \n-- New ADRs are added as `docs/adr/NNNN-<short-slug>.md`, where `NNNN` is the next\n-  zero-padded sequence number.\n-- The **canonical list of ADRs is the set of files matching `docs/adr/*.md`** in\n-  this directory. Browse `docs/adr/*.md` for the authoritative, current set.\n-- Use [`template.md`](template.md) as the starting point for a new ADR.\n-- Record an ADR when a decision is hard to reverse, surprising without context,\n-  and a real tradeoff. Do not write an ADR for routine or easily reversible\n-  choices.\n+The same four-digit number may appear once in each namespace. A platform\n+`SADR-NNNN` citation resolves only to `SADR-NNNN-*.md`; a target `ADR NNNN`\n+citation resolves only to `NNNN-*.md`.\n \n-Using the `docs/adr/*.md` glob as the source of truth means other PRs can add an\n-ADR without editing this index file, avoiding merge contention on a\n-hand-maintained enumeration.\n+## How Decision Records Work\n \n-## Current ADRs (snapshot)\n-\n-The list below is a convenience snapshot and may lag behind the directory. When\n-in doubt, list `docs/adr/*.md`.\n+- New Shiki platform decisions use the next zero-padded SADR number.\n+- New target product decisions use the target's own ADR sequence.\n+- The canonical list is the files in `docs/adr/`; no hand-maintained index is\n+  authoritative.\n+- Use [`template.md`](template.md) as the starting point and choose the filename\n+  namespace that owns the decision.\n+- Record a decision when it is hard to reverse, surprising without context, and\n+  a real tradeoff. Do not write a decision record for routine or easily\n+  reversible choices.\n \n-- [0001 \u2014 Keep Shiki As The Platform Name](0001-keep-shiki-as-platform-name.md)\n-- [0002 \u2014 Use AGENTS As The Shared Agent Constitution](0002-use-agents-as-shared-constitution.md)\n-- [0003 \u2014 Require Skill Gate For MergeGate Readiness](0003-require-skill-gate-for-mergegate-readiness.md)\n-- [0004 \u2014 Use GitHub First With A Repository-Local Shiki Mirror](0004-use-github-first-with-shiki-mirror.md)\n-- [0005 \u2014 Use Codex As Front And Claude Code Action As The GitHub Runtime](0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md)\n-- [0006 \u2014 Separate Codex Implementation, CCA Completion, And MergeGate Authorization](0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md)\n+Using the directory as the source of truth lets independent changes add records\n+without contending on an enumerated index.\n \n-## Adding A New ADR\n+## Adding A Shiki SADR\n \n-1. Copy `template.md` to `docs/adr/NNNN-<short-slug>.md`.\n+1. Copy `template.md` to `docs/adr/SADR-NNNN-<short-slug>.md`.\n 2. Fill in the title, status, context, decision, and consequences.\n-3. Set the status to `Proposed`, then `Accepted` once the decision is approved.\n-4. Open a PR following `CONTRIBUTING.md`. You do not need to edit this index for\n-   the ADR to be discoverable via `docs/adr/*.md`.\n+3. Set the status to `Proposed`, then `Accepted` once the proper authority has\n+   approved the decision.\n+4. Open a PR following `CONTRIBUTING.md`.\n+\n+## Adding A Target ADR\n+\n+1. Copy `template.md` to `docs/adr/NNNN-<short-slug>.md` using the target's own\n+   sequence.\n+2. Cite it as `ADR NNNN`; do not prefix its filename with `SADR-`.\ndiff --git a/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md b/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\nnew file mode 100644\nindex 0000000000000000000000000000000000000000..7455ce1f3bed02ddddb851ac21a1d1a648c9aeba\n--- /dev/null\n+++ b/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\n@@ -0,0 +1,55 @@\n+# SADR-0019: Separate Shiki Platform SADRs From Target ADRs\n+\n+## Status\n+\n+Accepted\n+\n+## Context\n+\n+Shiki installs its platform decision records into `docs/adr/` in every target\n+repository. Shiki and its targets previously used the same numeric\n+`NNNN-<slug>.md` namespace. A target that already owned ADRs 0013 through 0018\n+therefore acquired two records for each number during installation, making an\n+`ADR NNNN` citation ambiguous. Renumbering target decisions would rewrite target\n+history, while renumbering Shiki decisions per target would destroy their stable\n+platform identity.\n+\n+Upgrades also need to remove the former Shiki-owned numeric paths without ever\n+mistaking a target-owned ADR for platform content. The install stamp already\n+records exact shipped paths and content digests, so it can prove ownership only\n+when both still match.\n+\n+## Decision\n+\n+Shiki platform decisions use the `SADR` namespace:\n+\n+- records are named `docs/adr/SADR-NNNN-<slug>.md`;\n+- live platform citations use `SADR-NNNN`;\n+- target product decisions retain `docs/adr/NNNN-<slug>.md` and `ADR NNNN`;\n+- the reference validator resolves the two namespaces independently, so the same\n+  four-digit number may exist once in each namespace.\n+\n+Before a forced upgrade writes anything, the installer inventories the exact\n+former numeric Shiki ADR paths. It may delete a legacy path only when the\n+existing install stamp contains that exact path and its recorded SHA-256 digest\n+matches the file currently on disk. All ownership blockers are reported\n+together and abort the upgrade before its first write. A non-forced install\n+never performs this cleanup.\n+\n+Historical `.shiki/` evidence remains unchanged because it records the namespace\n+that existed when that evidence was produced.\n+\n+## Consequences\n+\n+- Targets may use any ADR number without colliding with Shiki's platform SADRs.\n+- Fresh installs preserve target ADR bytes and install a stable SADR set.\n+- Forced upgrades fail closed when legacy ownership cannot be proven, and a\n+  successful upgrade refreshes the stamp without removed legacy paths.\n+- Current governance, packaging, workflow, prompt, script, and skill references\n+  must move with the platform records.\n+\n+## Alternatives Considered\n+\n+- Collision-only refusal leaves affected targets permanently unupgradable.\n+- Renumbering target ADRs mutates target-owned history.\n+- Per-target Shiki renumbering makes platform citations non-portable.\ndiff --git a/docs/adr/template.md b/docs/adr/template.md\nindex a92a97daddd0b00ab9dd626cc8d86942da2eafed..17dd79175b5fee2105935557f691b40cf9492942\n--- a/docs/adr/template.md\n+++ b/docs/adr/template.md\n@@ -1,14 +1,14 @@\n-# NNNN: <Short Decision Title>\n+# <SADR-NNNN or NNNN>: <Short Decision Title>\n \n ## Status\n \n-Proposed | Accepted | Superseded by ADR-NNNN\n+Proposed | Accepted | Superseded by SADR-NNNN or target ADR NNNN\n \n ## Context\n \n What is the situation, constraint, or force that requires a decision? Reference\n-relevant domain terms (`CONTEXT.md`), prior ADRs, and code reality. State the\n-problem, not the solution.\n+relevant domain terms (`CONTEXT.md`), prior Shiki SADRs or target ADRs, and code\n+reality. State the problem, not the solution.\n \n ## Decision\n \ndiff --git a/docs/agents/autonomous-loop-e2e-acceptance.md b/docs/agents/autonomous-loop-e2e-acceptance.md\nindex bbf84c6081987f159ac6ad9ecf3c82b7aa72e7e8..5c13c1d02a05a90b0dc46cbef7c95a9846d0c237\n--- a/docs/agents/autonomous-loop-e2e-acceptance.md\n+++ b/docs/agents/autonomous-loop-e2e-acceptance.md\n@@ -40,8 +40,8 @@\n 3. `gh auth status` is logged in with merge permission on the repo.\n 4. Branch protection + required checks are configured exactly as the goal-loop\n    required-check set expects (see `docs/agents/branch-protection-smoke.md`).\n-5. The repo `.shiki/policy` permits autonomous low/medium-risk merges (ADR\n-   0008/0009); the MergeGate policy check is the recorded-authority gate.\n+5. The repo `.shiki/policy` permits autonomous low/medium-risk merges\n+   (SADR-0008/0009); the MergeGate policy check is the recorded-authority gate.\n \n The operator's only actions are: register the task and start the loop. **No\n manual pre-clearing** means after `shiki loop run` starts, the operator does not\ndiff --git a/docs/agents/bootstrap-command.md b/docs/agents/bootstrap-command.md\nindex 05f7f203a52cf412b9f97084a6e131ed0f785afd..f7aa0b734dfebee4a3b139335b66ee3415f48713\n--- a/docs/agents/bootstrap-command.md\n+++ b/docs/agents/bootstrap-command.md\n@@ -189,7 +189,9 @@\n ```\n \n Use this only for tests, fixtures, or explicit local-only template inspection.\n-Use `--force` only when you intentionally want to overwrite existing target files.\n+Use `--force` only when you intentionally want to refresh the target template.\n+Legacy numeric Shiki decision cleanup is stamp-and-digest authorized before any\n+write; target ADR paths are not cleanup candidates.\n \n ## Slash Command\n \ndiff --git a/docs/agents/checklists.md b/docs/agents/checklists.md\nindex 4a7f03ade346f53a1c1d82981f3fd744f9ab4547..0c5ec6c53dc7197b7b7a7d18e667b325babd6c45\n--- a/docs/agents/checklists.md\n+++ b/docs/agents/checklists.md\n@@ -32,18 +32,18 @@\n | ID | Item | Blocking |\n | --- | --- | --- |\n | GD-01 | Plan was challenged against `CONTEXT.md`. | blocking for non-trivial Goals |\n-| GD-02 | Relevant ADRs were read or absence was recorded. | blocking for non-trivial Goals |\n+| GD-02 | Relevant SADRs and target ADRs were read or absence was recorded. | blocking for non-trivial Goals |\n | GD-03 | Ambiguous terms were resolved or converted to HITL blockers. | blocking |\n | GD-04 | Edge scenarios were considered. | advisory |\n | GD-05 | Code reality was checked when code could answer. | blocking when applicable |\n | GD-06 | New domain terms were added to `CONTEXT.md` when needed. | blocking when applicable |\n-| GD-07 | ADR was created or explicitly skipped using the ADR criteria. | blocking when applicable |\n+| GD-07 | A SADR or target ADR was created or explicitly skipped using the decision-record criteria. | blocking when applicable |\n \n ## CI \u2014 Context & Impact Checklist\n \n | ID | Item | Blocking |\n | --- | --- | --- |\n-| CI-01 | Relevant docs and ADRs identified. | blocking |\n+| CI-01 | Relevant docs, SADRs, and target ADRs identified. | blocking |\n | CI-02 | Relevant modules, interfaces, seams, callers, and tests identified. | blocking |\n | CI-03 | Dependencies and blocked-by relationships identified. | blocking |\n | CI-04 | Candidate locks identified. | blocking |\n@@ -62,7 +62,7 @@\n | PRD-04 | Implementation decisions are recorded without volatile code detail. | blocking |\n | PRD-05 | Testing decisions are recorded. | blocking |\n | PRD-06 | Out-of-scope items are explicit. | blocking |\n-| PRD-07 | Relevant glossary/ADR links are present when applicable. | blocking |\n+| PRD-07 | Relevant glossary/SADR/target-ADR links are present when applicable. | blocking |\n \n ## SF \u2014 Spec Freeze Checklist\n \n@@ -98,7 +98,7 @@\n | H-02 | Branch/worktree target is present. | blocking |\n | H-03 | Scope and non-goals are present. | blocking |\n | H-04 | Dependencies and locks are present. | blocking |\n-| H-05 | Relevant docs, ADRs, modules, and tests are referenced. | blocking |\n+| H-05 | Relevant docs, SADRs, target ADRs, modules, and tests are referenced. | blocking |\n | H-06 | Required skills are stated. | blocking |\n | H-07 | TDD expectations are stated. | blocking when behavior work |\n | H-08 | Acceptance criteria are included. | blocking |\ndiff --git a/docs/agents/codex-handoff.md b/docs/agents/codex-handoff.md\nindex ede468f47cb277f69a857e8c45df703e4ed0151d..6f97bdffb67e44d7edea2c71e01ea28933c4eb6c\n--- a/docs/agents/codex-handoff.md\n+++ b/docs/agents/codex-handoff.md\n@@ -46,7 +46,7 @@\n \n ## Context\n - Domain terms:\n-- Relevant ADRs:\n+- Relevant SADRs and target ADRs:\n - Relevant docs:\n - Relevant modules/interfaces/seams:\n - Similar tests/prior art:\ndiff --git a/docs/agents/completion-check-agent.md b/docs/agents/completion-check-agent.md\nindex 13e4c035db35025f4e3cdcc5adfb24314ac4d521..61bfa1218cf36ca605250c91919e620f33238aa0\n--- a/docs/agents/completion-check-agent.md\n+++ b/docs/agents/completion-check-agent.md\n@@ -32,7 +32,7 @@\n - `AGENTS.md`\n - `CLAUDE.md`\n - `CONTEXT.md`\n-- relevant ADRs\n+- relevant SADRs and target ADRs\n - parent Goal issue\n - PRD issue or document\n - task issue\ndiff --git a/docs/agents/decision-control.md b/docs/agents/decision-control.md\nindex 7d8ee5ab03a5ccbcbd04056ae61de03785a2a9c9..8c94ecda00fffaa0e92c325ab83aa9776bd1dac1\n--- a/docs/agents/decision-control.md\n+++ b/docs/agents/decision-control.md\n@@ -11,7 +11,7 @@\n | Decision | Owner | When | Required inputs | Output | Enforcement surface |\n | --- | --- | --- | --- | --- | --- |\n | Is the Goal clear enough to plan? | Planner + Guardian when needed | Goal intake | User request, existing docs, risk signals | Goal readiness verdict | Goal checklist, issue template, ledger |\n-| Must `grill-with-docs` run? | Planner / Skill Gate | Before PRD or issue split | Ambiguity, domain terms, ADR conflicts, unclear boundaries | Skill invocation or explicit skip rationale | Skill Gate checklist |\n+| Must `grill-with-docs` run? | Planner / Skill Gate | Before PRD or issue split | Ambiguity, domain terms, SADR/target-ADR conflicts, unclear boundaries | Skill invocation or explicit skip rationale | Skill Gate checklist |\n | Is a PRD required? | Planner | After Goal context stabilizes | Goal, `grill-with-docs` output, Context & Impact output | PRD required / not required | PRD checklist, CCA evidence check |\n | Can the spec be frozen? | Operator only | End of Requirements Definition, before issue split | PRD/requirements summary, scope inventory (SF-02), open-question list | `spec_freeze` block in the plan + ledger evidence | `require_grilled_plan` (plan ingest/run), SF checklist |\n | Can a frozen spec change? | Operator only (Spec Amendment) | When implementation contradicts the frozen spec | Contested decisions, scoped re-grill result | recorded amendment + re-stamped freeze | SF checklist, ledger evidence |\ndiff --git a/docs/agents/domain.md b/docs/agents/domain.md\nindex 258da8ea81e87ac0a6b0b5b5e5c2fde980eaabb7..d57b0cf17ff3e1fa9e6e5885372bf083d8295421\n--- a/docs/agents/domain.md\n+++ b/docs/agents/domain.md\n@@ -5,7 +5,9 @@\n ## Layout\n \n - `CONTEXT.md` defines Shiki language. Keep it implementation-free.\n-- `docs/adr/` records hard-to-reverse decisions.\n+- `docs/adr/SADR-NNNN-*.md` records Shiki platform decisions.\n+- `docs/adr/NNNN-*.md` records target product decisions without sharing the\n+  SADR namespace.\n - `AGENTS.md` is the runtime-neutral agent constitution.\n - `CLAUDE.md` is the Claude Code wrapper around `AGENTS.md`.\n - `.shiki/` is the Target Repository mirror for Goals, PRDs, plans, Task DAGs, contracts, locks, worktree records, ledger entries, CCA verdicts, repair packets, reports, and handoffs.\n@@ -15,6 +17,6 @@\n \n - Use Shiki terms exactly when they exist.\n - Add glossary terms to `CONTEXT.md` only when the term is part of the domain, not a generic programming concept.\n-- Create ADRs only for hard-to-reverse, surprising, tradeoff-based decisions.\n+- Create SADRs or target ADRs only for hard-to-reverse, surprising, tradeoff-based decisions.\n - Keep product-specific language in the Target Repository, not in Shiki Template docs.\n-- If an ADR conflicts with a proposed implementation, surface the conflict before editing.\n+- If a SADR or target ADR conflicts with a proposed implementation, surface the conflict before editing.\ndiff --git a/docs/agents/execution-control.md b/docs/agents/execution-control.md\nindex ad8ee3cdb36be56fedc1f46408ca92b387d74047..bc63c3a73661d12b567016ba299f1a6b66bf9088\n--- a/docs/agents/execution-control.md\n+++ b/docs/agents/execution-control.md\n@@ -63,7 +63,7 @@\n | Branch name | Orchestrator | Orchestrator | Before implementer dispatch | branch policy | branch ref in task/ledger |\n | Lock reservation | Context & Impact | MergeGate | Before dispatch and before merge | `.shiki/locks/`, labels, PR metadata | lock id, owner, scope |\n | CI profile | Planner | MergeGate | During issue triage, rechecked at PR | labels, `.shiki/policy.yaml`, workflow matrix | required check list |\n-| Required status checks | Repo maintainer / Guardian | GitHub branch protection / rulesets | Repository setup, updated via ADR | GitHub settings | branch/ruleset config, ADR |\n+| Required status checks | Repo maintainer / Guardian | GitHub branch protection / rulesets | Repository setup, updated via target ADR | GitHub settings | branch/ruleset config, target ADR |\n | CCA judgment | CCA | GitHub required check / MergeGate | Every non-draft PR update | GitHub Action check | structured CCA verdict |\n | Repair assignment | CCA | MergeGate | After failed CCA/CI/review | repair packet, issue/PR comment | repair id and attempt count |\n | Merge readiness | MergeGate | GitHub branch protection / Guardian if needed | After CCA complete | required checks, review rules, rulesets | mergegate check output |\ndiff --git a/docs/agents/guardian-policy.md b/docs/agents/guardian-policy.md\nindex 4147c7adf05a47adb4fd24ada1a1c2e1f2ba6d2d..a20d9cc6c00a569bb057b9ca8d6b51c7fc4a9847\n--- a/docs/agents/guardian-policy.md\n+++ b/docs/agents/guardian-policy.md\n@@ -98,8 +98,8 @@\n `origin/<default_branch>` and reuses each job's existing base fetch; a shallow\n (`--depth`) or second base fetch would make the proof unresolvable and silently\n disable the carry, so neither is permitted. CCA reads `guardian_comment_carried` as\n-a recorded authority in the deterministic signal and does not re-derive it. See ADR\n-0018 for the full decision, the rejected diff/patch-equality and path-disjointness\n+a recorded authority in the deterministic signal and does not re-derive it. See\n+SADR-0018 for the full decision, the rejected diff/patch-equality and path-disjointness\n alternatives, and the accepted residual.\n \n ## External AI Guardian Review (SADR-0010)\ndiff --git a/docs/agents/implementation-policy.md b/docs/agents/implementation-policy.md\nindex c754144a9fb5bf4a38aa82a60e1670c59e626b44..85c37281bcad196c6b7b02343faf00b03cd9c57a\n--- a/docs/agents/implementation-policy.md\n+++ b/docs/agents/implementation-policy.md\n@@ -31,10 +31,13 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n ## Non-Negotiables\n \n 1. **Planning precedes implementation.** Non-trivial Goals start with `grill-with-docs` and Context & Impact.\n-2. **Domain language is controlled.** Terms must be checked against `CONTEXT.md`; hard-to-reverse tradeoffs must be checked against ADRs.\n+2. **Domain language is controlled.** Terms must be checked against `CONTEXT.md`; hard-to-reverse tradeoffs must be checked against Shiki SADRs and target ADRs.\n 3. **PRD precedes issue decomposition.** `to-prd` is used once the context is settled enough to become durable intent.\n 4. **Issues are vertical slices.** `to-issues` must produce independently grabbable tracer-bullet tasks, not horizontal layer tickets.\n 5. **Triage controls dispatch.** Only `ready-for-agent` / AFK tasks may be assigned to Codex without human input.\n@@ -95,19 +98,19 @@\n Process:\n \n - Challenge terms against `CONTEXT.md`.\n-- Check ADRs before reopening decisions.\n+- Check SADRs and target ADRs before reopening decisions.\n - Inspect code when code can answer a question.\n - Ask one question at a time when the operator is available.\n - Provide a recommended answer with each question.\n - Record resolved terms in `CONTEXT.md`.\n-- Create ADRs only for hard-to-reverse, surprising, tradeoff-based decisions.\n+- Create SADRs or target ADRs only for hard-to-reverse, surprising, tradeoff-based decisions.\n - Convert unresolved load-bearing questions into HITL blockers.\n \n Outputs:\n \n - Resolved design-tree decisions.\n - Updated glossary terms when needed.\n-- ADRs when justified.\n+- SADRs or target ADRs when justified.\n - HITL blockers when decisions remain unresolved.\n \n ## Phase 3 \u2014 Context & Impact\n@@ -117,7 +120,7 @@\n \n Outputs must include:\n \n-- Relevant documents and ADRs.\n+- Relevant documents, SADRs, and target ADRs.\n - Relevant modules, interfaces, seams, callers, and tests.\n - Candidate locks.\n - Dependencies.\n@@ -140,7 +143,7 @@\n - Testing decisions.\n - Out of scope.\n - Further notes.\n-- Links to relevant Goal, domain terms, and ADRs.\n+- Links to relevant Goal, domain terms, SADRs, and target ADRs.\n \n Phase 4 ends with Spec Freeze: the operator explicitly approves the PRD, the\n plan records a `spec_freeze` block (status, approved_by, source), and the SF\n@@ -240,7 +243,7 @@\n - Dependencies and locks.\n - Risk and Guardian requirements.\n - Ledger completeness.\n-- Documentation and ADR updates.\n+- Documentation and SADR/target-ADR updates.\n - Scope drift.\n \n CCA emits one of:\n@@ -301,7 +304,7 @@\n | Evidence failure | Behavior might be done, but proof is missing. | Add checks, PR evidence, ledger entry, or artifacts. |\n | Scope drift | PR changes unrelated behavior. | Revert unrelated changes or split into new Goal. |\n | Dependency block | Upstream task or lock not complete. | Block until dependency resolves. |\n-| Architecture block | Decision needs ADR or deep-module discussion. | `grill-with-docs` or `improve-codebase-architecture`. |\n+| Architecture block | Decision needs a SADR, target ADR, or deep-module discussion. | `grill-with-docs` or `improve-codebase-architecture`. |\n | Risk block | Security/production/policy change needs human approval. | Guardian decision. |\n \n ## Phase 5.5 \u2014 Execution Decision Control\ndiff --git a/docs/agents/runtime-auth-model.md b/docs/agents/runtime-auth-model.md\nindex 6a2e1a47e81ea89b5c68a210ec07772a9374babc..3c54748d24233dbd0ba8473950282547ef4ce935\n--- a/docs/agents/runtime-auth-model.md\n+++ b/docs/agents/runtime-auth-model.md\n@@ -148,7 +148,7 @@\n \n API-key based runners may be added by a target repository as an explicit extension. They are not the default Shiki template path.\n \n-If a repo opts into API-key mode, record the exception in an ADR and update the repo's `AGENTS.md`, `.shiki/config.yaml`, and workflow permissions.\n+If a repo opts into API-key mode, record the target-specific exception in a target ADR and update the repo's `AGENTS.md`, `.shiki/config.yaml`, and workflow permissions.\n \n ## GitHub Actions Runtime Compatibility\n \ndiff --git a/docs/agents/skill-gate.md b/docs/agents/skill-gate.md\nindex c50398b0d7c60a8598f2a5c88a10015730dced13..3acffe6444617bb1d54b77d4b4708158ec735bc6\n--- a/docs/agents/skill-gate.md\n+++ b/docs/agents/skill-gate.md\n@@ -9,7 +9,7 @@\n | Skill | Trigger | Expected output |\n | --- | --- | --- |\n | `setup-matt-pocock-skills` | First configuring a repo for these skills, or when issue tracker, triage labels, or domain docs are missing. | `docs/agents/` configuration and an `Agent skills` block in `AGENTS.md` or `CLAUDE.md`. |\n-| `grill-with-docs` | Ambiguous plan, unclear terminology, boundary decision, tradeoff, or ADR-worthy design choice. | Clarified decision tree, recommended answers, updates to `CONTEXT.md` or ADRs when decisions crystallize. |\n+| `grill-with-docs` | Ambiguous plan, unclear terminology, boundary decision, tradeoff, or decision-record-worthy design choice. | Clarified decision tree, recommended answers, updates to `CONTEXT.md`, SADRs, or target ADRs when decisions crystallize. |\n | `zoom-out` | Agent lacks a higher-level architectural map. | Map of relevant modules, callers, seams, and domain concepts. |\n | `to-prd` | Settled context should become a PRD. | PRD published to the issue tracker with problem, solution, user stories, decisions, testing, and out-of-scope notes. |\n | `to-issues` | Goal, PRD, or plan must become implementation tickets. | Vertical-slice issues with acceptance criteria, dependencies, and AFK/HITL classification. |\n@@ -17,7 +17,7 @@\n | `tdd` | Feature work or bug fix has observable behavior that can be specified with tests. | Red-green-refactor loop through public interfaces, one vertical slice at a time. |\n | `code-review` | The TDD loop is green and the implementer is about to create or update a PR. Default for every implementation task. | Pre-PR self-review of the task diff with fixes applied on the task branch; findings and resolutions recorded as a ledger entry naming the skill and a `## Pre-PR code review` PR body section. |\n | `diagnose` | Hard bug, failing check, regression, flaky behavior, or performance problem. | Reproduction loop, ranked hypotheses, instrumentation, fix, regression test, post-mortem. |\n-| `improve-codebase-architecture` | Structural friction, testability, AI-navigability, deep-module opportunity, or refactor request. | Architecture candidates and deepening recommendations informed by `CONTEXT.md` and ADRs. |\n+| `improve-codebase-architecture` | Structural friction, testability, AI-navigability, deep-module opportunity, or refactor request. | Architecture candidates and deepening recommendations informed by `CONTEXT.md`, SADRs, and target ADRs. |\n | `prototype` | A throwaway logic or UI prototype is needed to answer a design question. | Clearly marked disposable prototype and captured decision. |\n \n ## MergeGate Interaction\ndiff --git a/docs/operations.md b/docs/operations.md\nindex cf1f452d4c589e38ccd58553bda0ad8684b51eb7..ace48a90ffbec9e5c0a7113bef16a4858bc44b3f\n--- a/docs/operations.md\n+++ b/docs/operations.md\n@@ -89,6 +89,16 @@\n      target history, brought forward by `shiki migrate`, not a template.\n    - **Everything else** (CLI scripts, workflows, schemas, docs) is overwritten.\n \n+   A forced upgrade also migrates the former numeric Shiki decision paths to\n+   `SADR-NNNN-*`. Before the first write, the installer inventories the exact\n+   legacy Shiki paths and authorizes each deletion only when the existing\n+   `.shiki/install-stamp.json` names that path and its SHA-256 digest still\n+   matches the file. An absent, unreadable, malformed, incomplete, or\n+   mismatching stamp reports every blocked path and stops with no filesystem\n+   change. Target-owned `NNNN-*` ADRs are not cleanup candidates. A non-force\n+   install deletes nothing and retains any still-matching legacy stamp entries\n+   so a later forced upgrade can prove ownership.\n+\n    If the target has pending migrations, `--force` refuses and writes nothing \u2014\n    apply migrations first (step 3), then re-run. The run ends with a single\n    summary of every `.new` written, so a half-upgrade is visible.\ndiff --git a/docs/releasing.md b/docs/releasing.md\nindex 10d73828e7dbd64e9d9f6b83bc84d010537ce62a..aafeee8c4d441e1e427efee2ce4f81a647f020e1\n--- a/docs/releasing.md\n+++ b/docs/releasing.md\n@@ -67,7 +67,7 @@\n CLI in `scripts/`. There are no third-party Python runtime dependencies. The CLI\n is intentionally **not** exposed as a `[project.scripts]` console entry point\n because `scripts/shiki.py` is a dependency-free shim and `scripts/` is not\n-packaged as an importable distribution (see the ADR for the rationale).\n+packaged as an importable distribution (see SADR-0007 for the rationale).\n \n Supported install paths:\n \n@@ -155,6 +155,15 @@\n    [docs/operations.md](operations.md#the-new-convention). If the target has\n    pending migrations, `--force` refuses and writes nothing; apply migrations\n    (step 2) first, then re-run.\n+\n+   The SADR namespace upgrade has an additional pre-write gate: each former\n+   numeric Shiki decision path is removed only when the existing install stamp\n+   contains that exact path and a digest matching its current bytes. The\n+   installer aggregates absent/unreadable/malformed-stamp, missing-digest, and\n+   digest-mismatch blockers before raising, so refusal leaves the target tree\n+   byte-identical. Numeric target ADRs are preserved. A successful forced\n+   upgrade installs and stamps `SADR-*` paths and drops the removed legacy paths\n+   from the refreshed stamp.\n 2. Apply repository-local `.shiki/` state migrations with the migrate command:\n \n    ```bash\ndiff --git a/docs/usage.md b/docs/usage.md\nindex b83407ee3970dd395dd893010bc5dbe8bad26051..a130ef6e40dfd8169a5bc701339351097df1a09f\n--- a/docs/usage.md\n+++ b/docs/usage.md\n@@ -67,8 +67,10 @@\n shiki install-target /path/to/target-repo --local-only\n ```\n \n-Use `--force` only when you intentionally want to overwrite existing target\n-files.\n+Use `--force` only when you intentionally want to refresh the target template.\n+For the SADR namespace migration, `--force` deletes only exact legacy Shiki\n+decision paths whose existing install-stamp digest still matches; any ownership\n+blocker stops before the first write. Numeric target ADRs are never candidates.\n \n ## `shiki bootstrap-platform` \u2014 publish the Shiki platform repo\n \ndiff --git a/scripts/guardian_approval_signal.py b/scripts/guardian_approval_signal.py\nindex d059f92b2c82aa2b5d6ca421365537a87886c1cb..16924892714f4f5b9303c0ef0e325a7e70075630\n--- a/scripts/guardian_approval_signal.py\n+++ b/scripts/guardian_approval_signal.py\n@@ -199,8 +199,8 @@\n ) -> Any:\n     \"\"\"Evaluate SADR-0015 Contract Approval for the CCA Guardian signal.\n \n-    The SAME OR that MergeGate applies must reach this deterministic signal: ADR\n-    0015 is explicit that without it, a contract-approved PR still returns\n+    The SAME OR that MergeGate applies must reach this deterministic signal:\n+    SADR-0015 is explicit that without it, a contract-approved PR still returns\n     ``needs_guardian``, the CCA job fails, and the MergeGate policy check never\n     runs \u2014 the gate this change moves would never be reached. This resolves the\n     task the SAME way MergeGate does (the first Shiki task id in the PR body),\ndiff --git a/scripts/shiki_contract_approval.py b/scripts/shiki_contract_approval.py\nindex 941bcb96fdd8b9a85bc0c4f0f04d5dad9d04839c..4a4b418f6527413161db82670bec857a043360e2\n--- a/scripts/shiki_contract_approval.py\n+++ b/scripts/shiki_contract_approval.py\n@@ -142,7 +142,7 @@\n     changed_files_status: Any,\n     registration: Any,\n ) -> ContractApprovalResult:\n-    \"\"\"Whether this PR carries an SADR-0015 Contract Approval.\n+    \"\"\"Whether this PR carries a SADR-0015 Contract Approval.\n \n     The four conditions are evaluated IN ORDER, each failure recorded as a\n     distinct reason string:\ndiff --git a/scripts/shiki_guardian.py b/scripts/shiki_guardian.py\nindex a4ecb79bbda55fd5c8b06adfae2eb99760a03c45..4dd99bbe714e26f421fa19abac7a34ae1b195ecc\n--- a/scripts/shiki_guardian.py\n+++ b/scripts/shiki_guardian.py\n@@ -12,7 +12,7 @@\n # The carry re-derives its proof from git in-process on every judging invocation;\n # it is never read from an artifact, so there is nothing to go stale or be forged.\n # shiki_sync_proof is the ONLY guardian-family module that runs git, and every one\n-# of its invocations is read-only and local (ADR: prove-from-git base sync).\n+# of its invocations is read-only and local (SADR: prove-from-git base sync).\n from shiki_sync_proof import verify_pure_base_sync\n \n GUARDIAN_POLICY_PATH = \".shiki/guardian-policy.json\"\ndiff --git a/scripts/shiki_guardian_review.py b/scripts/shiki_guardian_review.py\nindex dd0f104d457899e0ba5ff1e162011c1e90b6f92b..e10ef58c046324e7ea3a3392feb3048ca77414ce\n--- a/scripts/shiki_guardian_review.py\n+++ b/scripts/shiki_guardian_review.py\n@@ -159,7 +159,7 @@\n     docs_only = _is_docs_only(changed_files)\n     if docs_only and matched == 0:\n         focus.append(\n-            \"Docs/ADR-only change: confirm the change is genuinely documentation/decision-only, introduces no behavior or contract drift, and stays consistent with the constitution and existing ADRs.\"\n+            \"Docs/decision-record-only change: confirm the change is genuinely documentation/decision-only, introduces no behavior or contract drift, and stays consistent with the constitution and existing SADRs and target ADRs.\"\n         )\n \n     # Conservative augmentation: unknown (no category and not docs-only), mixed\ndiff --git a/scripts/shiki_installer.py b/scripts/shiki_installer.py\nindex be064ab7a29ff6836f7f7a1d70500a43e0d11d7b..9fd8eefd137ce7003dad5c0dcb2e48e2cd47ab32\n--- a/scripts/shiki_installer.py\n+++ b/scripts/shiki_installer.py\n@@ -118,6 +118,7 @@\n # what lets a target carry it without a governance-schema change.\n INSTALL_STAMP_PATH = \".shiki/install-stamp.json\"\n INSTALL_STAMP_VERSION = 1\n+LEGACY_SHIKI_SADR_MAX = 18\n \n # Under --force the shipped surface splits three ways, so nothing is lost and\n # nothing is silently stale.\n@@ -237,9 +238,98 @@\n         )\n \n \n+def legacy_shiki_adr_paths() -> tuple[str, ...]:\n+    \"\"\"Former numeric paths for Shiki SADR-0001 through SADR-0018.\n+\n+    The authoritative slugs come from the current SADR filenames. SADR-0019 is\n+    the namespace decision itself and never shipped under a numeric legacy path.\n+    \"\"\"\n+    paths: list[str] = []\n+    for sadr in sorted((ROOT / \"docs\" / \"adr\").glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\")):\n+        number = sadr.name.split(\"-\", 2)[1]\n+        if int(number) <= LEGACY_SHIKI_SADR_MAX:\n+            paths.append(f\"docs/adr/{sadr.name.removeprefix('SADR-')}\")\n+    return tuple(paths)\n+\n+\n+def preflight_legacy_sadr_cleanup(target: Path, *, force: bool) -> tuple[str, ...]:\n+    \"\"\"Authorize exact legacy Shiki ADR removals before the first write.\n+\n+    A non-force install never removes existing files. A forced install needs no\n+    stamp when no legacy Shiki path is present. When legacy paths do exist, each\n+    must be named in a readable install stamp and its current bytes must match\n+    the recorded digest.\n+    \"\"\"\n+    if not force:\n+        return ()\n+    present = tuple(\n+        relative\n+        for relative in legacy_shiki_adr_paths()\n+        if (target / relative).exists() or (target / relative).is_symlink()\n+    )\n+    if not present:\n+        return ()\n+\n+    stamp_path = target / INSTALL_STAMP_PATH\n+    try:\n+        raw = stamp_path.read_text(encoding=\"utf-8\")\n+        stamp = json.loads(raw)\n+    except FileNotFoundError:\n+        detail = \"install stamp is absent\"\n+        blockers = [f\"{relative}: {detail}\" for relative in present]\n+    except (OSError, UnicodeDecodeError) as error:\n+        detail = f\"install stamp is unreadable ({error})\"\n+        blockers = [f\"{relative}: {detail}\" for relative in present]\n+    except json.JSONDecodeError as error:\n+        detail = f\"install stamp is malformed ({error})\"\n+        blockers = [f\"{relative}: {detail}\" for relative in present]\n+    else:\n+        if not isinstance(stamp, dict):\n+            blockers = [\n+                f\"{relative}: install stamp is malformed (expected an object)\"\n+                for relative in present\n+            ]\n+        else:\n+            digests = stamp.get(\"digests\")\n+            blockers = []\n+            if not isinstance(digests, dict):\n+                blockers.extend(\n+                    f\"{relative}: install stamp has no digest for this path\"\n+                    for relative in present\n+                )\n+            else:\n+                for relative in present:\n+                    expected = digests.get(relative)\n+                    if not isinstance(expected, str) or not expected:\n+                        blockers.append(\n+                            f\"{relative}: install stamp has no digest for this path\"\n+                        )\n+                        continue\n+                    try:\n+                        actual = _sha256_file(target / relative)\n+                    except OSError as error:\n+                        blockers.append(\n+                            f\"{relative}: legacy path is unreadable ({error})\"\n+                        )\n+                        continue\n+                    if actual != expected:\n+                        blockers.append(\n+                            f\"{relative}: digest mismatch (stamp {expected}, current {actual})\"\n+                        )\n+\n+    if blockers:\n+        joined = \"\\n\".join(f\"- {blocker}\" for blocker in blockers)\n+        raise ShikiError(\n+            \"legacy Shiki ADR cleanup is not authorized:\\n\"\n+            f\"{joined}\\nNo files were written.\"\n+        )\n+    return present\n+\n+\n def install_template(target: Path, *, force: bool, validate: bool) -> list[NewFileNote]:\n     # Preconditions first: never rewrite the tree of a target that cannot be\n     # upgraded cleanly (see refuse_pending_migrations).\n+    legacy_cleanup = preflight_legacy_sadr_cleanup(target, force=force)\n     refuse_pending_migrations(target)\n \n     # Resolve the target's required CODEOWNERS owner once from its\n@@ -250,6 +340,10 @@\n     # verbatim. See ``codeowners_required_owner``.\n     codeowners_owner = codeowners_required_owner(target)\n \n+    for relative in legacy_cleanup:\n+        (target / relative).unlink()\n+        info(f\"removed stamped legacy Shiki ADR: {target / relative}\")\n+\n     notes: list[NewFileNote] = []\n     for relative in TEMPLATE_PATHS:\n         source = ROOT / relative\n@@ -647,6 +741,22 @@\n     digests = target_stamp_digests(target)\n     existing = read_install_stamp(target)\n     if existing is not None:\n+        # A non-force install deliberately leaves legacy Shiki ADRs in place.\n+        # Preserve only still-matching ownership proof so a later forced\n+        # upgrade can authorize cleanup; never carry a missing or stale digest.\n+        existing_digests = existing.get(\"digests\")\n+        if isinstance(existing_digests, dict):\n+            for relative in legacy_shiki_adr_paths():\n+                expected = existing_digests.get(relative)\n+                path = target / relative\n+                if not isinstance(expected, str) or not path.is_file():\n+                    continue\n+                try:\n+                    actual = _sha256_file(path)\n+                except OSError:\n+                    continue\n+                if actual == expected:\n+                    digests[relative] = actual\n         if _incoming_is_older(commit, existing.get(\"platform_commit\")):\n             warn(\n                 \"kept existing install stamp: incoming install \"\ndiff --git a/scripts/shiki_loop.py b/scripts/shiki_loop.py\nindex 3611641da8aeb5038b1da90e3114b09a473ec6d8..84caa491b9cad0edc4176f209c13e9221e975681\n--- a/scripts/shiki_loop.py\n+++ b/scripts/shiki_loop.py\n@@ -424,7 +424,7 @@\n         # unresolvable CCA verdict has already produced its terminal stop and is never\n         # laundered into a head-moving sync (SADR-0010). It also never fires while the\n         # policy (Guardian) gate is red \u2014 for ANY reason, not only when it is the sole\n-        # red check \u2014 so a head-bound Guardian approval, or an SADR-0015\n+        # red check \u2014 so a head-bound Guardian approval, or a SADR-0015\n         # contract-approval carry that base movement defeated (the policy gate compares\n         # base and head task governance directly), is never silently restored by\n         # adopting base wholesale. It is bounded by a durable per-task counter so the\ndiff --git a/scripts/shiki_tasks.py b/scripts/shiki_tasks.py\nindex aec3809f24b4a0c8c96fc50971cff9ae9ad30381..d531052e9d9717058d4631483cefab44c26a65c1\n--- a/scripts/shiki_tasks.py\n+++ b/scripts/shiki_tasks.py\n@@ -821,7 +821,7 @@\n         \"prompt\": prompt,\n         \"entry_skill\": \"grill-with-docs\",\n         \"required_next_steps\": [\n-            \"Run grill-with-docs until terminology, boundaries, risks, and ADR-worthy decisions are settled.\",\n+            \"Run grill-with-docs until terminology, boundaries, risks, and decision-record-worthy choices are settled.\",\n             \"Obtain the operator's explicit approval of the PRD/requirements (Spec Freeze).\",\n             \"Write a machine-readable plan JSON with grill_with_docs.status=complete and spec_freeze.status=frozen.\",\n             \"Run shiki plan ingest --plan-file PLAN.json.\",\ndiff --git a/scripts/test_shiki_goal_loop.sh b/scripts/test_shiki_goal_loop.sh\nindex 753c7b516e10f0be3bc92a4bd0eb1fa699776ae6..8f3820a0a079b3044424cfe1a6c6b7fe68896e67\n--- a/scripts/test_shiki_goal_loop.sh\n+++ b/scripts/test_shiki_goal_loop.sh\n@@ -231,8 +231,8 @@\n \n # High/critical risk merges autonomously when all required checks are green:\n # the \"MergeGate policy check\" required check IS the Guardian gate (it enforces\n-# guardian-policy.json \u2014 human approval OR external AI guardian review, ADR\n-# 0010), so green checks mean a recorded authority approved. The fake gh returns\n+# guardian-policy.json \u2014 human approval OR external AI guardian review,\n+# SADR-0010), so green checks mean a recorded authority approved. The fake gh returns\n # green checks, simulating that the Guardian gate passed.\n python3 \"$ROOT/scripts/shiki.py\" goal create --target \"$TARGET\" --title \"Guardian gate\" --outcome \"High risk merges via policy gate\" >/tmp/shiki-goal-loop-guardian-goal.json\n GGOAL=\"$(json_get /tmp/shiki-goal-loop-guardian-goal.json goal_id)\"\ndiff --git a/tests/test_adr_references.py b/tests/test_adr_references.py\nindex 51706329c797d5dd9b47e082a333fa83b261d482..d005da4f771f5e8085ec22d4b787980e68f5ae8c\n--- a/tests/test_adr_references.py\n+++ b/tests/test_adr_references.py\n@@ -1,8 +1,8 @@\n-\"\"\"Every ``ADR ####`` citation must resolve to exactly one decision record.\n+\"\"\"Every target ADR and Shiki SADR citation resolves in its own namespace.\n \n Background. ``scripts/mergegate_check.py`` justifies ``is_bookkeeping_closeout``\n-with \"ADR 0017\" nine times and ``scripts/guardian_approval_signal.py`` cites\n-0016/0017 seven times, yet for a stretch the numbering in ``docs/adr/`` stopped\n+with \"SADR-0017\" nine times and ``scripts/guardian_approval_signal.py`` cites\n+SADR-0016/SADR-0017 seven times, yet for a stretch the numbering in ``docs/adr/`` stopped\n at 0015: the authoring commits lived on a coordinator branch and no task ever\n declared ``docs/adr/...`` in its locks, so nothing carried the records to the\n default branch. The stated basis of an exemption that merged seven PRs with zero\n@@ -11,11 +11,12 @@\n and the directory drifted with no signal.\n \n This is the same unbound-pair shape behind other silent failures: a reference\n-and its target with nothing asserting they agree. This test binds them. It scans\n-every ``scripts/*.py`` (plus ``docs/**/*.md`` and the root ``*.md`` governance\n-files, which are cheap) for the ``ADR ####`` pattern and asserts each cited\n-number resolves to exactly one ``docs/adr/####-*.md``. The converse is\n-deliberately NOT required: an ADR may exist without being cited.\n+and its target with nothing asserting they agree. This test binds them. Target\n+``ADR NNNN`` citations resolve only to ``docs/adr/NNNN-*.md``; Shiki\n+``SADR-NNNN`` citations resolve only to ``docs/adr/SADR-NNNN-*.md``. The scan\n+covers current governance docs, scripts, workflows, prompts, packaging metadata,\n+and repo-local Codex guidance while deliberately excluding historical\n+``.shiki`` evidence. The converse is not required: a record may be uncited.\n \n The suite is dependency free: standard-library ``unittest`` only.\n \"\"\"\n@@ -31,17 +32,18 @@\n \n REPO_ROOT = Path(__file__).resolve().parents[1]\n \n-# \"ADR 0016\" -- a four-digit citation. The trailing \\b keeps a five-digit token\n-# from being read as a truncated four-digit number.\n-_CITATION = re.compile(r\"\\bADR (\\d{4})\\b\")\n-# \"0016-locks-bound-product-paths.md\" -- the leading four digits name the record.\n-_ADR_FILENAME = re.compile(r\"^(\\d{4})-.*\\.md$\")\n+# ``ADR 0016`` / ``SADR-0016`` -- a namespace plus four-digit citation. The\n+# trailing ``\\b`` keeps a five-digit token from being read as a truncated one.\n+_CITATION = re.compile(r\"\\b(S?ADR)[ -](\\d{4})\\b\")\n+# Target ADRs retain the numeric filename; Shiki platform decisions use SADR.\n+_DECISION_FILENAME = re.compile(r\"^(?:(SADR)-)?(\\d{4})-.*\\.md$\")\n \n \n class Violation(NamedTuple):\n     \"\"\"A citation that does not resolve to exactly one decision record.\"\"\"\n \n     citing_file: str  # repo-relative path of the file that cites the ADR\n+    namespace: str  # \"ADR\" for target decisions; \"SADR\" for Shiki decisions\n     number: str  # the cited four-digit number, e.g. \"0016\"\n     line: int  # 1-based line of the citation\n     reason: str  # \"missing\" (zero matches) or \"ambiguous\" (two or more)\n@@ -49,44 +51,59 @@\n \n     @property\n     def expected_path(self) -> str:\n-        return f\"docs/adr/{self.number}-*.md\"\n+        prefix = \"SADR-\" if self.namespace == \"SADR\" else \"\"\n+        return f\"docs/adr/{prefix}{self.number}-*.md\"\n \n     def describe(self) -> str:\n         \"\"\"Name the citing file, the cited number, and the missing path.\"\"\"\n         if self.reason == \"missing\":\n             return (\n-                f\"{self.citing_file}:{self.line} cites ADR {self.number} \"\n+                f\"{self.citing_file}:{self.line} cites {self.namespace} {self.number} \"\n                 f\"but no document matches {self.expected_path}\"\n             )\n         joined = \", \".join(self.matches)\n         return (\n-            f\"{self.citing_file}:{self.line} cites ADR {self.number} which \"\n+            f\"{self.citing_file}:{self.line} cites {self.namespace} {self.number} which \"\n             f\"resolves to {len(self.matches)} documents (expected exactly one \"\n             f\"at {self.expected_path}): {joined}\"\n         )\n \n \n-def _adr_index(root: Path) -> dict[str, list[Path]]:\n-    \"\"\"Map every four-digit ADR number to the record files that claim it.\"\"\"\n-    index: dict[str, list[Path]] = {}\n+def _adr_index(root: Path) -> dict[tuple[str, str], list[Path]]:\n+    \"\"\"Map each decision namespace and number to the files that claim it.\"\"\"\n+    index: dict[tuple[str, str], list[Path]] = {}\n     adr_dir = root / \"docs\" / \"adr\"\n     if adr_dir.is_dir():\n         for path in sorted(adr_dir.glob(\"*.md\")):\n-            match = _ADR_FILENAME.match(path.name)\n+            match = _DECISION_FILENAME.match(path.name)\n             if match:\n-                index.setdefault(match.group(1), []).append(path)\n+                namespace = \"SADR\" if match.group(1) else \"ADR\"\n+                index.setdefault((namespace, match.group(2)), []).append(path)\n     return index\n \n \n def _citing_files(root: Path, include_docs: bool = True) -> list[Path]:\n-    \"\"\"Files scanned for citations: scripts/*.py, and cheaply the markdown.\"\"\"\n+    \"\"\"Current, shipped text surfaces scanned for decision citations.\"\"\"\n     files = sorted((root / \"scripts\").glob(\"*.py\"))\n+    files += sorted((root / \"scripts\").glob(\"*.sh\"))\n     if include_docs:\n         docs = root / \"docs\"\n         if docs.is_dir():\n             files += sorted(docs.rglob(\"*.md\"))\n         files += sorted(root.glob(\"*.md\"))\n-    return files\n+        pyproject = root / \"pyproject.toml\"\n+        if pyproject.is_file():\n+            files.append(pyproject)\n+        for relative in (\".github\", \".codex\"):\n+            surface = root / relative\n+            if surface.is_dir():\n+                files += sorted(\n+                    path\n+                    for path in surface.rglob(\"*\")\n+                    if path.is_file()\n+                    and path.suffix in {\".md\", \".py\", \".sh\", \".toml\", \".yaml\", \".yml\"}\n+                )\n+    return sorted(set(files))\n \n \n def find_adr_reference_violations(\n@@ -94,10 +111,9 @@\n ) -> list[Violation]:\n     \"\"\"Return every ``ADR ####`` citation under ``root`` that fails to resolve.\n \n-    A citation resolves when exactly one ``docs/adr/####-*.md`` matches its\n-    number. Zero matches is a \"missing\" violation; two or more is \"ambiguous\".\n-    Records that are never cited produce nothing -- the converse is not\n-    required.\n+    A citation resolves when exactly one record in the same namespace matches\n+    its number. Zero matches is a \"missing\" violation; two or more is\n+    \"ambiguous\". Records that are never cited produce nothing.\n     \"\"\"\n     root = Path(root)\n     index = _adr_index(root)\n@@ -110,13 +126,15 @@\n         rel = path.relative_to(root).as_posix()\n         for lineno, line in enumerate(text.splitlines(), start=1):\n             for match in _CITATION.finditer(line):\n-                number = match.group(1)\n-                found = index.get(number, [])\n+                namespace, number = match.groups()\n+                found = index.get((namespace, number), [])\n                 if len(found) == 1:\n                     continue\n                 reason = \"missing\" if not found else \"ambiguous\"\n                 rels = [p.relative_to(root).as_posix() for p in found]\n-                violations.append(Violation(rel, number, lineno, reason, rels))\n+                violations.append(\n+                    Violation(rel, namespace, number, lineno, reason, rels)\n+                )\n     return violations\n \n \n@@ -129,8 +147,8 @@\n         self.assertEqual(\n             violations,\n             [],\n-            \"Every 'ADR ####' citation must resolve to exactly one \"\n-            f\"docs/adr/####-*.md:\\n{detail}\",\n+            \"Every ADR/SADR citation must resolve exactly once in its own \"\n+            f\"docs/adr namespace:\\n{detail}\",\n         )\n \n     def test_scan_actually_reads_scripts(self) -> None:\n@@ -142,12 +160,38 @@\n             \"the citation scan must cover scripts/*.py\",\n         )\n \n+    def test_platform_citations_do_not_use_unresolved_numeric_shorthand(self) -> None:\n+        shorthand = re.compile(r\"SADR-\\d{4}/\\d{4}\")\n+        offenders = []\n+        for path in _citing_files(REPO_ROOT):\n+            text = path.read_text(encoding=\"utf-8\")\n+            if shorthand.search(text):\n+                offenders.append(path.relative_to(REPO_ROOT).as_posix())\n+        self.assertEqual(\n+            offenders,\n+            [],\n+            \"repeat the SADR prefix so every cited number is independently resolvable\",\n+        )\n+\n     def test_carried_records_are_indexed(self) -> None:\n         # The two records this Goal carried onto the default branch, and which\n         # the scripts cite most, must be present.\n         index = _adr_index(REPO_ROOT)\n-        self.assertIn(\"0016\", index)\n-        self.assertIn(\"0017\", index)\n+        self.assertIn((\"SADR\", \"0016\"), index)\n+        self.assertIn((\"SADR\", \"0017\"), index)\n+\n+    def test_platform_tree_has_only_sadr_0001_through_0019(self) -> None:\n+        adr_dir = REPO_ROOT / \"docs\" / \"adr\"\n+        sadrs = sorted(adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\"))\n+        self.assertEqual(\n+            {path.name.split(\"-\", 2)[1] for path in sadrs},\n+            {f\"{number:04d}\" for number in range(1, 20)},\n+        )\n+        self.assertEqual(\n+            list(adr_dir.glob(\"[0-9][0-9][0-9][0-9]-*.md\")),\n+            [],\n+            \"the Shiki platform tree must not retain legacy numeric SADR paths\",\n+        )\n \n \n class FixtureAdrReferences(unittest.TestCase):\n@@ -180,6 +224,26 @@\n         self.assertIn(\"9991\", message)  # cited number\n         self.assertIn(\"docs/adr/9991-*.md\", message)  # missing path\n \n+    def test_target_adr_does_not_satisfy_missing_platform_sadr(self) -> None:\n+        root = self._make_repo(\n+            adr_names=[\"0042-target-decision.md\"],\n+            script_body='\"\"\"Target ADR 0042 exists, but platform SADR-0042 does not.\"\"\"\\n',\n+        )\n+        violations = find_adr_reference_violations(root, include_docs=False)\n+        self.assertEqual(len(violations), 1)\n+        self.assertEqual(violations[0].namespace, \"SADR\")\n+        self.assertEqual(violations[0].number, \"0042\")\n+        self.assertEqual(violations[0].expected_path, \"docs/adr/SADR-0042-*.md\")\n+\n+    def test_target_adr_and_platform_sadr_may_reuse_a_number(self) -> None:\n+        root = self._make_repo(\n+            adr_names=[\"0042-target-decision.md\", \"SADR-0042-platform-decision.md\"],\n+            script_body='\"\"\"Target ADR 0042 and platform SADR-0042 are distinct.\"\"\"\\n',\n+        )\n+        self.assertEqual(\n+            find_adr_reference_violations(root, include_docs=False), []\n+        )\n+\n     def test_uncited_adr_is_allowed(self) -> None:\n         # An ADR exists but nothing cites it -> not a violation. The converse\n         # (every ADR must be cited) is explicitly NOT required.\ndiff --git a/tests/test_guardian_review.py b/tests/test_guardian_review.py\nindex 88829e7810212635e2fc98aa5918d6f26294677d..00a85a237acd5bf2d6cb5b294128198c422402ca\n--- a/tests/test_guardian_review.py\n+++ b/tests/test_guardian_review.py\n@@ -162,7 +162,7 @@\n \n     def test_docs_only_is_light_and_not_conservative(self):\n         focus = gr.classify_review_focus([\"docs/adr/0099-x.md\", \"README.md\"], \"\", \"low\")\n-        self.assertTrue(any(\"Docs/ADR-only\" in f for f in focus))\n+        self.assertTrue(any(\"Docs/decision-record-only\" in f for f in focus))\n         self.assertFalse(any(\"conservative default\" in f.lower() for f in focus))\n \n     def test_unknown_category_is_conservative(self):\ndiff --git a/tests/test_policy_job_merged_prs.py b/tests/test_policy_job_merged_prs.py\nindex d45512e74681e5e649279236c7d4e79d5c3da471..b97989f8bd74d091de1a6b5275a20d8dc68c4512\n--- a/tests/test_policy_job_merged_prs.py\n+++ b/tests/test_policy_job_merged_prs.py\n@@ -3,8 +3,8 @@\n \n ``.github/workflows/shiki-cca-completion.yml`` has two consumers of the merged-PR\n proof: the CCA *signal* (``guardian_approval_signal.py`` in \"Prepare CCA context\")\n-and the authoritative gate (``mergegate_check.py`` in \"Enforce MergeGate\"). ADR\n-0017's bookkeeping-closeout exemption (condition 6) needs the merged implementation\n+and the authoritative gate (``mergegate_check.py`` in \"Enforce MergeGate\").\n+SADR-0017's bookkeeping-closeout exemption (condition 6) needs the merged implementation\n PR proven, and a closeout carries NO ``post_merge_reconcile`` marker. The signal\n already resolves ``MERGED_PRS`` unconditionally, but \"Enforce MergeGate\" used to\n resolve it only inside ``grep -q '<!-- shiki:post_merge_reconcile -->'``. So a\ndiff --git a/tests/test_sadr_install_migration.py b/tests/test_sadr_install_migration.py\nnew file mode 100644\nindex 0000000000000000000000000000000000000000..d51eac872b1c79ff4579c4c1401ca5d775a60d8d\n--- /dev/null\n+++ b/tests/test_sadr_install_migration.py\n@@ -0,0 +1,237 @@\n+\"\"\"SADR installation and collision-safe legacy upgrade behavior.\"\"\"\n+\n+from __future__ import annotations\n+\n+import contextlib\n+import hashlib\n+import io\n+import json\n+import shutil\n+import tempfile\n+import unittest\n+from pathlib import Path\n+\n+import shiki_test_support  # noqa: F401  (path bootstrap)\n+\n+import shiki_installer\n+from tests.test_adr_references import find_adr_reference_violations\n+\n+\n+PRODUCT_NUMBERS = tuple(range(13, 19))\n+\n+\n+def _quiet_install(target: Path, *, force: bool, validate: bool) -> None:\n+    with contextlib.redirect_stdout(io.StringIO()):\n+        shiki_installer.install_template(target, force=force, validate=validate)\n+\n+\n+def _sha256(path: Path) -> str:\n+    return hashlib.sha256(path.read_bytes()).hexdigest()\n+\n+\n+def _legacy_sadr_paths() -> tuple[str, ...]:\n+    return tuple(\n+        f\"docs/adr/{path.name.removeprefix('SADR-')}\"\n+        for path in sorted((shiki_installer.ROOT / \"docs\" / \"adr\").glob(\"SADR-*.md\"))\n+        if path.name != \"SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\"\n+    )\n+\n+\n+def _create_legacy_upgrade_fixture(\n+    target: Path,\n+) -> tuple[dict[str, bytes], tuple[str, ...]]:\n+    adr_dir = target / \"docs\" / \"adr\"\n+    adr_dir.mkdir(parents=True)\n+    product_bytes = {\n+        f\"{number:04d}-cubeshare-product-decision.md\": (\n+            f\"# CubeShare ADR {number:04d}\\n\\nDo not rewrite {number}.\\n\"\n+        ).encode()\n+        for number in PRODUCT_NUMBERS\n+    }\n+    for name, content in product_bytes.items():\n+        (adr_dir / name).write_bytes(content)\n+\n+    legacy_paths = _legacy_sadr_paths()\n+    for relative in legacy_paths:\n+        (target / relative).write_bytes(f\"legacy Shiki bytes for {relative}\\n\".encode())\n+\n+    stamp_path = target / shiki_installer.INSTALL_STAMP_PATH\n+    stamp_path.parent.mkdir(parents=True, exist_ok=True)\n+    stamp = {\n+        \"version\": shiki_installer.INSTALL_STAMP_VERSION,\n+        \"platform_commit\": None,\n+        \"installed_at\": \"2000-01-01T00:00:00+00:00\",\n+        \"digests\": {\n+            relative: _sha256(target / relative) for relative in legacy_paths\n+        },\n+    }\n+    stamp_path.write_text(\n+        json.dumps(stamp, indent=2, sort_keys=True) + \"\\n\", encoding=\"utf-8\"\n+    )\n+    return product_bytes, legacy_paths\n+\n+\n+def _tree_snapshot(root: Path) -> dict[str, tuple[str, bytes | str]]:\n+    \"\"\"Recursive byte/shape snapshot used to prove preflight is write-free.\"\"\"\n+    snapshot: dict[str, tuple[str, bytes | str]] = {}\n+    for path in sorted(root.rglob(\"*\")):\n+        relative = path.relative_to(root).as_posix()\n+        if path.is_symlink():\n+            snapshot[relative] = (\"symlink\", path.readlink().as_posix())\n+        elif path.is_dir():\n+            snapshot[relative] = (\"directory\", \"\")\n+        else:\n+            snapshot[relative] = (\"file\", path.read_bytes())\n+    return snapshot\n+\n+\n+class FreshSadrInstallTests(unittest.TestCase):\n+    def setUp(self) -> None:\n+        self.tmp = Path(tempfile.mkdtemp(prefix=\"shiki-sadr-fresh-\"))\n+        self.addCleanup(lambda: shutil.rmtree(self.tmp, ignore_errors=True))\n+        self.target = self.tmp / \"target\"\n+        self.adr_dir = self.target / \"docs\" / \"adr\"\n+        self.adr_dir.mkdir(parents=True)\n+        self.product_bytes = {\n+            f\"{number:04d}-product-decision.md\": (\n+                f\"# Product ADR {number:04d}\\n\\nTarget-owned bytes {number}.\\n\"\n+            ).encode()\n+            for number in PRODUCT_NUMBERS\n+        }\n+        for name, content in self.product_bytes.items():\n+            (self.adr_dir / name).write_bytes(content)\n+\n+    def test_fresh_install_preserves_product_adrs_and_installs_all_sadrs(self) -> None:\n+        _quiet_install(self.target, force=False, validate=True)\n+\n+        for name, expected in self.product_bytes.items():\n+            self.assertEqual((self.adr_dir / name).read_bytes(), expected)\n+\n+        sadr_numbers = {\n+            path.name.split(\"-\", 2)[1]\n+            for path in self.adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\")\n+        }\n+        self.assertEqual(sadr_numbers, {f\"{number:04d}\" for number in range(1, 20)})\n+\n+        numeric_records = {\n+            path.name\n+            for path in self.adr_dir.glob(\"[0-9][0-9][0-9][0-9]-*.md\")\n+        }\n+        self.assertEqual(numeric_records, set(self.product_bytes))\n+        stamp = json.loads(\n+            (self.target / shiki_installer.INSTALL_STAMP_PATH).read_text(\n+                encoding=\"utf-8\"\n+            )\n+        )\n+        for path in self.adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\"):\n+            relative = path.relative_to(self.target).as_posix()\n+            self.assertEqual(stamp[\"digests\"][relative], _sha256(path))\n+        self.assertEqual(find_adr_reference_violations(self.target), [])\n+\n+\n+class LegacySadrUpgradeTests(unittest.TestCase):\n+    def setUp(self) -> None:\n+        self.tmp = Path(tempfile.mkdtemp(prefix=\"shiki-sadr-upgrade-\"))\n+        self.addCleanup(lambda: shutil.rmtree(self.tmp, ignore_errors=True))\n+        self.target = self.tmp / \"target\"\n+        self.adr_dir = self.target / \"docs\" / \"adr\"\n+        self.product_bytes, self.legacy_paths = _create_legacy_upgrade_fixture(\n+            self.target\n+        )\n+\n+    def test_force_upgrade_deletes_only_stamped_legacy_sadrs(self) -> None:\n+        _quiet_install(self.target, force=True, validate=True)\n+\n+        for relative in self.legacy_paths:\n+            self.assertFalse((self.target / relative).exists(), relative)\n+        for name, expected in self.product_bytes.items():\n+            self.assertEqual((self.adr_dir / name).read_bytes(), expected)\n+\n+        sadrs = list(self.adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\"))\n+        self.assertEqual(len(sadrs), 19)\n+        refreshed = json.loads(\n+            (self.target / shiki_installer.INSTALL_STAMP_PATH).read_text(encoding=\"utf-8\")\n+        )\n+        for relative in self.legacy_paths:\n+            self.assertNotIn(relative, refreshed[\"digests\"])\n+        for path in sadrs:\n+            relative = path.relative_to(self.target).as_posix()\n+            self.assertEqual(refreshed[\"digests\"][relative], _sha256(path))\n+\n+    def test_non_force_install_never_deletes_legacy_files(self) -> None:\n+        before = {\n+            relative: (self.target / relative).read_bytes()\n+            for relative in self.legacy_paths\n+        }\n+        _quiet_install(self.target, force=False, validate=False)\n+        for relative, expected in before.items():\n+            self.assertEqual((self.target / relative).read_bytes(), expected)\n+        refreshed = json.loads(\n+            (self.target / shiki_installer.INSTALL_STAMP_PATH).read_text(encoding=\"utf-8\")\n+        )\n+        for relative in self.legacy_paths:\n+            self.assertEqual(refreshed[\"digests\"][relative], _sha256(self.target / relative))\n+\n+\n+class LegacyCleanupPreflightTests(unittest.TestCase):\n+    def setUp(self) -> None:\n+        self.tmp = Path(tempfile.mkdtemp(prefix=\"shiki-sadr-preflight-\"))\n+        self.addCleanup(lambda: shutil.rmtree(self.tmp, ignore_errors=True))\n+\n+    def test_all_ownership_blockers_abort_before_any_write(self) -> None:\n+        cases = (\n+            (\"absent\", \"install stamp is absent\"),\n+            (\"unreadable\", \"install stamp is unreadable\"),\n+            (\"malformed\", \"install stamp is malformed\"),\n+            (\"missing-digest\", \"install stamp has no digest\"),\n+            (\"digest-mismatch\", \"digest mismatch\"),\n+        )\n+        for name, expected_reason in cases:\n+            with self.subTest(name=name):\n+                target = self.tmp / name\n+                _, legacy_paths = _create_legacy_upgrade_fixture(target)\n+                stamp_path = target / shiki_installer.INSTALL_STAMP_PATH\n+\n+                if name == \"absent\":\n+                    stamp_path.unlink()\n+                elif name == \"unreadable\":\n+                    stamp_path.unlink()\n+                    stamp_path.mkdir()\n+                elif name == \"malformed\":\n+                    stamp_path.write_text(\"{not-json\\n\", encoding=\"utf-8\")\n+                else:\n+                    stamp = json.loads(stamp_path.read_text(encoding=\"utf-8\"))\n+                    if name == \"missing-digest\":\n+                        stamp[\"digests\"] = {}\n+                    else:\n+                        stamp[\"digests\"] = {\n+                            relative: \"deadbeef\" for relative in legacy_paths\n+                        }\n+                    stamp_path.write_text(\n+                        json.dumps(stamp, indent=2, sort_keys=True) + \"\\n\",\n+                        encoding=\"utf-8\",\n+                    )\n+\n+                before = _tree_snapshot(target)\n+                with self.assertRaises(shiki_installer.ShikiError) as raised:\n+                    _quiet_install(target, force=True, validate=False)\n+                after = _tree_snapshot(target)\n+\n+                self.assertEqual(before, after, f\"{name} preflight changed the tree\")\n+                message = str(raised.exception)\n+                self.assertIn(expected_reason, message)\n+                self.assertIn(\"No files were written\", message)\n+                for relative in legacy_paths:\n+                    self.assertIn(relative, message)\n+\n+    def test_absent_stamp_is_allowed_when_no_legacy_path_exists(self) -> None:\n+        target = self.tmp / \"no-legacy\"\n+        target.mkdir()\n+        _quiet_install(target, force=True, validate=False)\n+        self.assertTrue(\n+            (target / \"docs\" / \"adr\" / \"SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\").is_file()\n+        )\n+\n+\n+if __name__ == \"__main__\":\n+    unittest.main()\n\nexec\n/bin/zsh -lc \"rg -n 'TEMPLATE_PATHS|docs/adr' scripts/shiki_installer.py | head -100\" in /private/tmp/shiki-adr-collision.pa0KwY/.worktrees/shiki-t-20260807t073307221254z-2d22d654-introduc\n succeeded in 0ms:\n20:TEMPLATE_PATHS = [\n40:    \"docs/adr\",\n184:    candidates = list(TEMPLATE_PATHS)\n251:            paths.append(f\"docs/adr/{sadr.name.removeprefix('SADR-')}\")\n348:    for relative in TEMPLATE_PATHS:\n637:    for relative in TEMPLATE_PATHS:\n\nexec\n/bin/zsh -lc \"find docs/adr -maxdepth 1 -type f -name 'SADR-[0-9][0-9][0-9][0-9]-*.md' -print | wc -l\" in /private/tmp/shiki-adr-collision.pa0KwY/.worktrees/shiki-t-20260807t073307221254z-2d22d654-introduc\n succeeded in 0ms:\n      19\n\nexec\n/bin/zsh -lc 'git diff --name-only -- .shiki' in /private/tmp/shiki-adr-collision.pa0KwY/.worktrees/shiki-t-20260807t073307221254z-2d22d654-introduc\n succeeded in 0ms:\n.shiki/tasks/T-20260807T073307221254Z-2d22d654.json\n\nexec\n/bin/zsh -lc \"find docs/adr -maxdepth 1 -type f -name '[0-9][0-9][0-9][0-9]-*.md' -print\" in /private/tmp/shiki-adr-collision.pa0KwY/.worktrees/shiki-t-20260807t073307221254z-2d22d654-introduc\n succeeded in 0ms:\nexec\n/bin/zsh -lc 'git diff --check' in /private/tmp/shiki-adr-collision.pa0KwY/.worktrees/shiki-t-20260807t073307221254z-2d22d654-introduc\n succeeded in 0ms:\ndiff --git a/.codex/skills/shiki/SKILL.md b/.codex/skills/shiki/SKILL.md\nindex c8e2e85d9d75d49cc4f2d6286ee22659c4e53e75..93cded623b2c15d9f65c76491a1c2d813ac9e6c0\n--- a/.codex/skills/shiki/SKILL.md\n+++ b/.codex/skills/shiki/SKILL.md\n@@ -18,6 +18,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n Codex CLI does not register this skill as a custom `/shiki` slash command. When\n the user types `/shiki` in Codex CLI and gets \"Unrecognized command\", explain\n that this is expected: invoke Shiki in Codex with natural language such as\n@@ -69,7 +72,7 @@\n \n ## Responsibilities\n \n-- Claude Code implements and repairs by default (`shiki runner claude`, ADR 0008); Codex implements and repairs tasks explicitly assigned to `codex`.\n+- Claude Code implements and repairs by default (`shiki runner claude`, SADR-0008); Codex implements and repairs tasks explicitly assigned to `codex`.\n - Claude Code Action can act as GitHub-side CCA or reviewer.\n - CCA judges completion.\n - MergeGate authorizes state transitions and merge readiness.\ndiff --git a/.github/prompts/cca-completion-check.md b/.github/prompts/cca-completion-check.md\nindex ed203ad5f302ecc81372d8ab27d2b1232ca97497..fd89950528333ef6242e6d5443904f5392ab7e56\n--- a/.github/prompts/cca-completion-check.md\n+++ b/.github/prompts/cca-completion-check.md\n@@ -19,6 +19,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n ## Required Reading\n \n Read only the files needed to judge the PR contract. Prefer this order:\ndiff --git a/.github/prompts/codex-task-handoff.md b/.github/prompts/codex-task-handoff.md\nindex 695e211d76dea1c60e8d5bb360e78487808286a4..59441d576b01a39569722f226c854e5a740e9eb5\n--- a/.github/prompts/codex-task-handoff.md\n+++ b/.github/prompts/codex-task-handoff.md\n@@ -2,7 +2,7 @@\n \n You are Codex Front acting as the Implementer for a Shiki task.\n \n-Follow `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant ADRs, and `docs/agents/codex-handoff.md`.\n+Follow `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant Shiki SADRs and target ADRs, and `docs/agents/codex-handoff.md`.\n \n Rules:\n \ndiff --git a/AGENTS.md b/AGENTS.md\nindex 7f041100d49c2a7525f51cb0f5d696a4a6d1c78c..a06bfbabc6fa48a007389e25e12d7ed523936bed\n--- a/AGENTS.md\n+++ b/AGENTS.md\n@@ -22,7 +22,7 @@\n - **Codex Front** is an optional implementer through Codex App, Codex CLI, Codex IDE extension, or Codex Web signed in with ChatGPT OAuth/subscription auth, dispatched with `shiki runner codex` when a task is explicitly assigned to `codex`.\n - **GitHub CCA** is implemented by Claude Code Action by default, using `CLAUDE_CODE_OAUTH_TOKEN`.\n \n-Do not assume `openai/codex-action`, `OPENAI_API_KEY`, or API-key based Codex automation in the default Shiki path. API-key runners are explicit target-repo extensions and require their own ADR.\n+Do not assume `openai/codex-action`, `OPENAI_API_KEY`, or API-key based Codex automation in the default Shiki path. API-key runners are explicit target-repo extensions and require their own target ADR.\n \n Shiki is not:\n \n@@ -44,10 +44,14 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use\n+`SADR-NNNN-*.md`; target product decisions retain `NNNN-*.md` and the `ADR`\n+namespace.\n+\n Use the canonical source-of-truth order above. Runtime-specific reading orders\n are operational guidance only and must not override or reorder it.\n \n-Conversation state is not durable truth. If a decision matters, put it in GitHub, `.shiki/`, `CONTEXT.md`, or an ADR.\n+Conversation state is not durable truth. If a decision matters, put it in GitHub, `.shiki/`, `CONTEXT.md`, a Shiki SADR, or a target ADR.\n \n If GitHub and `.shiki/` disagree, surface the conflict and prefer GitHub operational state until the mirror is repaired.\n \n@@ -75,8 +79,8 @@\n Every non-trivial change follows this loop:\n \n 1. **Goal**: clarify outcome, completion conditions, non-goals, risk level, and success signals.\n-2. **grill-with-docs**: challenge the plan against domain docs, ADRs, code reality, terminology, and edge scenarios.\n-3. **Context & Impact**: find relevant docs, ADRs, code, dependencies, owners, locks, and verification surfaces.\n+2. **grill-with-docs**: challenge the plan against domain docs, Shiki SADRs, target ADRs, code reality, terminology, and edge scenarios.\n+3. **Context & Impact**: find relevant docs, SADRs, target ADRs, code, dependencies, owners, locks, and verification surfaces.\n 4. **PRD and Spec Freeze**: use `to-prd` when context is settled enough to become durable product/engineering intent. The operator's explicit approval of the PRD is Spec Freeze: the plan records a `spec_freeze` block, and no plan runs without it. Steps 1-4 form Requirements Definition \u2014 one continuous operator dialogue. After Spec Freeze, scope changes require an operator-approved Spec Amendment; non-scope-moving interpretations go to the Assumption Log.\n 5. **Issues**: use `to-issues` to create independently grabbable vertical-slice issues. Prefer AFK slices over HITL slices where possible.\n 6. **Triage**: label issues for readiness, risk, runtime, skills, and MergeGate state.\n@@ -167,7 +171,7 @@\n - Testing decisions.\n - Out-of-scope items.\n - Further notes.\n-- Links to relevant domain terms and ADRs.\n+- Links to relevant domain terms, SADRs, and target ADRs.\n \n Every executable task must include:\n \n@@ -193,7 +197,7 @@\n \n Context & Impact must identify:\n \n-- Relevant documents, domain terms, ADRs, and past decisions.\n+- Relevant documents, domain terms, SADRs, target ADRs, and past decisions.\n - Relevant modules, interfaces, seams, callers, owners, and tests.\n - Dependency relationships between tasks.\n - Candidate locks and lock conflicts.\n@@ -210,7 +214,7 @@\n | Skill | Required when |\n | --- | --- |\n | `setup-matt-pocock-skills` | First configuring the repo for skills, or when issue tracker, triage labels, or domain docs are missing. |\n-| `grill-with-docs` | Default for non-trivial Goals before PRD/issues; always required when plans, terminology, boundaries, tradeoffs, or ADR-worthy decisions are ambiguous. |\n+| `grill-with-docs` | Default for non-trivial Goals before PRD/issues; always required when plans, terminology, boundaries, tradeoffs, or decision-record-worthy choices are ambiguous. |\n | `zoom-out` | The agent lacks an architectural map of the relevant code area. |\n | `to-prd` | Settled Goal context must become durable product/engineering intent. |\n | `to-issues` | A Goal, PRD, or plan must become independently grabbable vertical-slice issues. |\n@@ -348,7 +352,7 @@\n - Three or more new files.\n - Any high-risk or critical label.\n \n-When the architecture gate triggers, run `grill-with-docs` or `improve-codebase-architecture` as appropriate and record durable decisions in an ADR when needed.\n+When the architecture gate triggers, run `grill-with-docs` or `improve-codebase-architecture` as appropriate and record durable decisions in a Shiki SADR or target ADR as applicable.\n \n ## Engineering Discipline\n \ndiff --git a/CLAUDE.md b/CLAUDE.md\nindex 98a932e7841a632866fcd53b0bad0e3af3711d3d..98a31399404d6fb044b4fd493f4459ac87f439de\n--- a/CLAUDE.md\n+++ b/CLAUDE.md\n@@ -14,6 +14,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n ## Claude Role\n \n Default to planner, default implementer, reviewer, coordinator, documentation editor, CCA-style completion judge, and final-judgment assistant.\n@@ -57,7 +60,7 @@\n At the start of a Shiki session:\n \n 1. Read `AGENTS.md`.\n-2. Read `CONTEXT.md` and relevant ADRs.\n+2. Read `CONTEXT.md` and relevant Shiki SADRs and target ADRs.\n 3. Read active `.shiki/` Goal/task/ledger state when present.\n 4. Read active distilled rules from `.shiki/memories/` (status `distilled`, `active: true`, not revoked or superseded) for applicable guidance; the same rules are injected deterministically into task handoffs as the `## Distilled Rules` section (\u00a73.5).\n 5. Read the GitHub Issue or PR when the task is GitHub-backed.\n@@ -74,7 +77,7 @@\n - Define the Goal, completion conditions, non-goals, and risk level.\n - Treat `grill-with-docs` as the default planning entry point for non-trivial Goals.\n - Challenge domain terms against `CONTEXT.md`.\n-- Challenge hard-to-reverse tradeoffs against ADRs.\n+- Challenge hard-to-reverse tradeoffs against SADRs and target ADRs.\n - Explore code instead of asking when code can answer the question.\n - Run Context & Impact before implementation planning.\n - Produce a Task DAG, not an unordered checklist.\n@@ -94,7 +97,7 @@\n - For each question, provide your recommended answer.\n - Resolve dependency decisions before downstream design questions.\n - Update `CONTEXT.md` inline when terminology crystallizes.\n-- Offer ADRs only when the decision is hard to reverse, surprising without context, and a real tradeoff.\n+- Offer a SADR or target ADR only when the decision is hard to reverse, surprising without context, and a real tradeoff.\n - Convert unresolved questions into HITL blockers rather than allowing Codex to guess.\n \n ## PRD and Issues Standard\n@@ -103,7 +106,7 @@\n \n - Use the project's domain glossary vocabulary.\n - Avoid volatile file paths unless they are essential.\n-- Include implementation decisions, testing decisions, out-of-scope items, and relevant ADR/domain links.\n+- Include implementation decisions, testing decisions, out-of-scope items, and relevant SADR/target-ADR/domain links.\n - Identify deep-module opportunities and testable interfaces.\n \n When creating issues:\n@@ -121,7 +124,7 @@\n - Branch/worktree target.\n - Scope and non-goals.\n - Dependencies and locks.\n-- Relevant docs, ADRs, modules, and tests.\n+- Relevant docs, SADRs, target ADRs, modules, and tests.\n - Required skill invocations.\n - TDD expectations.\n - Pre-PR `code-review` gate expectations.\n@@ -152,7 +155,7 @@\n \n - Treat yourself as a judge, not implementer.\n - Do not write production code.\n-- Read `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant ADRs, PRD, task issue, PR body, diff, checks, reviews, labels, and `.shiki/` state.\n+- Read `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant SADRs and target ADRs, PRD, task issue, PR body, diff, checks, reviews, labels, and `.shiki/` state.\n - Evaluate every applicable checklist in `docs/agents/checklists.md`.\n - Map each acceptance criterion to durable evidence.\n - Distinguish implementation failure from missing evidence.\ndiff --git a/CONTEXT.md b/CONTEXT.md\nindex e61a9be06a0cce0d026484083a657bde2067e234..237ee8aa3ea8fab334ece49768bc5ed3b5af0974\n--- a/CONTEXT.md\n+++ b/CONTEXT.md\n@@ -25,7 +25,7 @@\n _Avoid_: jumping directly into code\n \n **grill-with-docs**:\n-A planning skill that challenges a plan against domain language, ADRs, code reality, and concrete edge scenarios. It resolves design-tree questions before PRD/issues. It runs inside Requirements Definition.\n+A planning skill that challenges a plan against domain language, Shiki SADRs, target ADRs, code reality, and concrete edge scenarios. It resolves design-tree questions before PRD/issues. It runs inside Requirements Definition.\n _Avoid_: generic brainstorming, silent assumptions\n \n **Requirements Definition**:\ndiff --git a/CONTRIBUTING.md b/CONTRIBUTING.md\nindex c6d9b656e73abb8a55e5f660123e03153dcb3e6d..9e37a8fbf668db5b22461aaae5548b8f1354394c\n--- a/CONTRIBUTING.md\n+++ b/CONTRIBUTING.md\n@@ -17,7 +17,7 @@\n 1. **Goal** \u2014 start from a user-approved Goal with an outcome, completion\n    conditions, non-goals, risk level, and success signals.\n 2. **grill-with-docs** \u2014 for any non-trivial change, challenge the plan against\n-   domain language, ADRs, and code reality before writing a PRD.\n+   domain language, Shiki SADRs, target ADRs, and code reality before writing a PRD.\n 3. **PRD** \u2014 capture settled product/engineering intent (`to-prd`).\n 4. **Spec Freeze** \u2014 the operator explicitly approves the PRD/requirements;\n    the plan records a `spec_freeze` block and no plan runs without it\n@@ -94,7 +94,7 @@\n   entries.\n - Do not hand-edit `.shiki/goals/*.json` or `.shiki/manifest.json` in a task PR.\n - Conversation state is not durable truth. If a decision matters, record it in\n-  GitHub, `.shiki/`, `CONTEXT.md`, or an ADR.\n+  GitHub, `.shiki/`, `CONTEXT.md`, a Shiki SADR, or a target ADR.\n \n ## Architecture Gate\n \n@@ -103,7 +103,7 @@\n or CI policy, package structure, three or more structural files, more than 100\n net new lines in a core module, three or more new files, or any high-risk label.\n When triggered, run `grill-with-docs` or `improve-codebase-architecture` and\n-record durable decisions in an ADR when needed (see `docs/adr/`).\n+record durable decisions in a Shiki SADR or target ADR as applicable (see `docs/adr/`).\n \n ## Safety\n \ndiff --git a/SYSTEM_PROMPT.md b/SYSTEM_PROMPT.md\nindex aaaea45e3a896c3a44ba89eec8bb84325ecf6c62..da009c86a88285269dee0155e58e550c6d744ea9\n--- a/SYSTEM_PROMPT.md\n+++ b/SYSTEM_PROMPT.md\n@@ -30,6 +30,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n Use the canonical source-of-truth order above. Runtime-specific reading orders\n are operational guidance only and must not override or reorder it. The current\n conversation is non-durable operator input.\n@@ -48,14 +51,14 @@\n \n A runtime may only do work that is explicitly assigned by the Goal, task, PR, or MergeGate decision.\n \n-Do not assume `openai/codex-action`, `OPENAI_API_KEY`, or API-key based Codex automation in the default Shiki path. API-key runners are explicit target-repo extensions and require their own ADR.\n+Do not assume `openai/codex-action`, `OPENAI_API_KEY`, or API-key based Codex automation in the default Shiki path. API-key runners are explicit target-repo extensions and require their own target ADR.\n \n ## 4. Non-negotiable Shiki flow\n \n For any non-trivial Goal, use this flow:\n \n 1. **Goal Seek**: define outcome, non-goals, risk level, completion criteria, and evidence requirements.\n-2. **grill-with-docs**: challenge the plan against `CONTEXT.md`, ADRs, code reality, terminology, boundaries, and edge scenarios. Ask one question at a time when the operator is available. If code can answer a question, inspect code instead of asking.\n+2. **grill-with-docs**: challenge the plan against `CONTEXT.md`, Shiki SADRs, target ADRs, code reality, terminology, boundaries, and edge scenarios. Ask one question at a time when the operator is available. If code can answer a question, inspect code instead of asking.\n 3. **Context & Impact**: identify modules, interfaces, seams, callers, dependencies, locks, risk, and verification surfaces.\n 4. **to-prd + Spec Freeze**: turn settled context into a PRD using domain vocabulary and testing decisions. The operator's explicit approval of the PRD is Spec Freeze, recorded as the plan's `spec_freeze` block; plans without it do not run. Steps 1-4 form Requirements Definition.\n 5. **to-issues**: decompose the PRD into independently grabbable vertical-slice issues. Prefer AFK slices; mark HITL slices when judgment is still required.\n@@ -75,7 +78,7 @@\n Before making any material change:\n \n 1. Read `AGENTS.md` and any runtime-specific wrapper such as `CLAUDE.md`.\n-2. Read `CONTEXT.md` and relevant ADRs.\n+2. Read `CONTEXT.md` and relevant SADRs and target ADRs.\n 3. Read the GitHub Issue / PR and `.shiki/` state for the active Goal or task.\n 4. Identify your role: Planner, Implementer, Reviewer, Completion Check Agent, Repairer, or Guardian-assist.\n 5. Identify Goal id, task id, branch, PR, dependencies, locks, risk level, required skills, and required evidence.\n@@ -103,7 +106,7 @@\n \n At minimum, identify:\n \n-- Relevant documents, ADRs, domain terms, and prior decisions.\n+- Relevant documents, SADRs, target ADRs, domain terms, and prior decisions.\n - Relevant modules, interfaces, seams, callers, owners, and tests.\n - Dependency relationships between tasks.\n - Candidate file locks and lock conflicts.\n@@ -152,7 +155,7 @@\n \n CCA must read:\n \n-- `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant ADRs.\n+- `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant SADRs and target ADRs.\n - Parent Goal issue, PRD issue, task issue, comments, and labels.\n - PR body, diff, commits, changed files, reviews, CI checks, and artifacts available to the workflow.\n - `.shiki/` task contract, locks, ledger entries, CCA checklist profile, and prior repair packets.\n@@ -214,7 +217,7 @@\n - Three or more new files.\n - Any high-risk or critical label.\n \n-When the architecture gate triggers, run `grill-with-docs` or `improve-codebase-architecture` as appropriate and record the decision in an ADR if it is hard to reverse or likely to be re-litigated.\n+When the architecture gate triggers, run `grill-with-docs` or `improve-codebase-architecture` as appropriate and record the decision in a Shiki SADR or target ADR as applicable if it is hard to reverse or likely to be re-litigated.\n \n ## 14. Skill Gate\n \n@@ -223,7 +226,7 @@\n Use these skills:\n \n - `setup-matt-pocock-skills`: before first use of the engineering skill set, or when issue tracker, triage labels, or domain docs are missing.\n-- `grill-with-docs`: default for non-trivial Goals before PRD/issues; required for ambiguous plans, terminology, boundaries, ADR-worthy tradeoffs, or design-tree decisions.\n+- `grill-with-docs`: default for non-trivial Goals before PRD/issues; required for ambiguous plans, terminology, boundaries, decision-record-worthy tradeoffs, or design-tree decisions.\n - `zoom-out`: unfamiliar code area or missing architectural map.\n - `to-prd`: convert settled context into a PRD.\n - `to-issues`: break a PRD, plan, or Goal into independently grabbable vertical-slice issues.\ndiff --git a/docs/adr/0001-keep-shiki-as-platform-name.md b/docs/adr/SADR-0001-keep-shiki-as-platform-name.md\nindex 20a9637c3b9d8c97de7ba8db7cfae4d82d9a7dba..8b685439959aada1b221c112f0ac3c51ca8a1695\n--- a/docs/adr/0001-keep-shiki-as-platform-name.md\n+++ b/docs/adr/SADR-0001-keep-shiki-as-platform-name.md\n@@ -1,3 +1,3 @@\n-# Keep Shiki As The Platform Name\n+# SADR-0001: Keep Shiki As The Platform Name\n \n Shiki remains the product name for the GitHub-native agentic engineering control plane. We considered more descriptive names such as MergePilot, MergeGate, and Kiroku, but kept Shiki because it carries the existing goal-seeking framework identity while remaining independent of Hermes, Codex, Claude Code, or any single runtime.\ndiff --git a/docs/adr/0002-use-agents-as-shared-constitution.md b/docs/adr/SADR-0002-use-agents-as-shared-constitution.md\nindex 73de30ecb078120eac540d9bc51855e3cd48d0e3..c5486182bf06fca36ea508496b013c67302fd989\n--- a/docs/adr/0002-use-agents-as-shared-constitution.md\n+++ b/docs/adr/SADR-0002-use-agents-as-shared-constitution.md\n@@ -1,3 +1,3 @@\n-# Use AGENTS As The Shared Agent Constitution\n+# SADR-0002: Use AGENTS As The Shared Agent Constitution\n \n Shiki uses `AGENTS.md` as the runtime-neutral constitution and `CLAUDE.md` as a thin Claude Code wrapper. This keeps Codex, Claude Code, Hermes Runner, and future Agent Runtimes aligned on the same GitHub-first MergeGate rules while still allowing runtime-specific instructions where needed.\ndiff --git a/docs/adr/0003-require-skill-gate-for-mergegate-readiness.md b/docs/adr/SADR-0003-require-skill-gate-for-mergegate-readiness.md\nindex 424be0c9af4c8f0136f1fde68a468c47ff84e764..7fc337987c7df9792f75f181dfcbed75fe5ffff2\n--- a/docs/adr/0003-require-skill-gate-for-mergegate-readiness.md\n+++ b/docs/adr/SADR-0003-require-skill-gate-for-mergegate-readiness.md\n@@ -1,3 +1,3 @@\n-# Require Skill Gate For MergeGate Readiness\n+# SADR-0003: Require Skill Gate For MergeGate Readiness\n \n Shiki requires relevant engineering skills to be invoked before execution when their triggers apply. This makes skills part of MergeGate readiness rather than optional prompt style, because Shiki depends on repeatable planning, issue decomposition, TDD, diagnosis, triage, and architecture review before autonomous or parallel agent work can be trusted.\ndiff --git a/docs/adr/0004-use-github-first-with-shiki-mirror.md b/docs/adr/SADR-0004-use-github-first-with-shiki-mirror.md\nindex d0a3a9174dc646a945a9d325954ffaf00c352040..a79c54ae9ee8eed2a0524192b864da4e22a92d3f\n--- a/docs/adr/0004-use-github-first-with-shiki-mirror.md\n+++ b/docs/adr/SADR-0004-use-github-first-with-shiki-mirror.md\n@@ -1,4 +1,4 @@\n-# ADR 0004: Use GitHub First With A Repository-Local Shiki Mirror\n+# SADR-0004: Use GitHub First With A Repository-Local Shiki Mirror\n \n ## Status\n \ndiff --git a/docs/adr/0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md b/docs/adr/SADR-0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\nindex b339baf49b89d9290e79c3d51b36e7b2bde3bd95..0f3c3eb9afccdb3403ce978e21d04fe12e495da0\n--- a/docs/adr/0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\n+++ b/docs/adr/SADR-0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\n@@ -1,8 +1,8 @@\n-# ADR 0005: Use Codex As Front And Claude Code Action As The GitHub Runtime\n+# SADR-0005: Use Codex As Front And Claude Code Action As The GitHub Runtime\n \n ## Status\n \n-Accepted (implementer default superseded by ADR 0008; the auth model and the\n+Accepted (implementer default superseded by SADR-0008; the auth model and the\n Claude Code Action GitHub runtime decision remain in force)\n \n ## Context\n@@ -26,4 +26,4 @@\n - Shiki avoids requiring `OPENAI_API_KEY` for the default workflow.\n - Codex work happens through the user's authenticated Codex front surface, not as a hidden backend Action.\n - Claude Code Action is configured for OAuth-token based subscription usage by default.\n-- API-key based automation remains possible only as an explicit target-repo extension with its own ADR.\n+- API-key based automation remains possible only as an explicit target-repo extension with its own target ADR.\ndiff --git a/docs/adr/0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md b/docs/adr/SADR-0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\nindex 4bcae9d1e52f0a37b8e9fbafa5cd14b6ee1993bd..7a38538a6ae9929567f0ade67e7ea6a60d0b3f7e\n--- a/docs/adr/0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\n+++ b/docs/adr/SADR-0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\n@@ -1,4 +1,4 @@\n-# ADR 0006: Separate Codex Implementation, CCA Completion, And MergeGate Authorization\n+# SADR-0006: Separate Codex Implementation, CCA Completion, And MergeGate Authorization\n \n ## Status\n \ndiff --git a/docs/adr/0007-packaging-and-release.md b/docs/adr/SADR-0007-packaging-and-release.md\nindex 79b84ddb97167372b44641e5b7860e400ce80822..bb445a2e2e856d9d1f5fdbe35de8d4e8cec0fb32\n--- a/docs/adr/0007-packaging-and-release.md\n+++ b/docs/adr/SADR-0007-packaging-and-release.md\n@@ -1,4 +1,4 @@\n-# ADR 0007: Packaging And Release For Shiki\n+# SADR-0007: Packaging And Release For Shiki\n \n ## Status\n \n@@ -25,7 +25,7 @@\n   not introduce third-party runtime dependencies or break the shim.\n - Releases must run inside the operator's subscription-authenticated toolchain\n   and must not require extra secrets beyond the built-in `GITHUB_TOKEN`\n-  (consistent with ADR 0005's no-API-key-by-default stance).\n+  (consistent with SADR-0005's no-API-key-by-default stance).\n - `.shiki/manifest.json` already encodes the install surface (`install.include`,\n   `install.create_directories`, `install.exclude_from_commit`), and it must not\n   be mutated as part of this change.\n@@ -34,7 +34,7 @@\n \n This is a hard-to-reverse platform decision (it sets the version source of\n truth, the release trigger, and the public install contract), so it is recorded\n-as an ADR.\n+as a SADR.\n \n ## Decision\n \n@@ -104,7 +104,7 @@\n 3. **Publishing to PyPI.** Rejected for now: Shiki is distributed as a\n    repository template + git-installable CLI, not a PyPI library; PyPI would add\n    credentials/secrets and a different versioning contract. Can be added later as\n-   its own ADR if a packaged distribution is needed.\n+   its own SADR if a packaged distribution is needed.\n 4. **Dynamic version (read `VERSION` via a build backend `dynamic` field).**\n    Rejected to keep `pyproject.toml` backend-agnostic and dependency-free; the\n    small duplication between `VERSION` and `pyproject.toml` is guarded by the\n@@ -123,4 +123,4 @@\n   releasing versions the whole platform but ships only `install.include` to\n   targets.\n - Future PyPI distribution or a console entry point are deferred and would each\n-  warrant their own ADR.\n+  warrant their own SADR.\ndiff --git a/docs/adr/0008-use-claude-code-as-default-implementer.md b/docs/adr/SADR-0008-use-claude-code-as-default-implementer.md\nindex dedf4510b7c7799521422f622029563d373db031..35c2625d908167fbc58855b5b63e283805f167e8\n--- a/docs/adr/0008-use-claude-code-as-default-implementer.md\n+++ b/docs/adr/SADR-0008-use-claude-code-as-default-implementer.md\n@@ -1,12 +1,12 @@\n-# ADR 0008: Use Claude Code As The Default Implementer Runtime\n+# SADR-0008: Use Claude Code As The Default Implementer Runtime\n \n ## Status\n \n-Proposed (supersedes ADR 0005 for the implementer default; keeps its auth model)\n+Proposed (supersedes SADR-0005 for the implementer default; keeps its auth model)\n \n ## Context\n \n-ADR 0005 made Codex Front the default implementation runtime, with Claude Code\n+SADR-0005 made Codex Front the default implementation runtime, with Claude Code\n restricted to planner and reviewer roles by the runtime registry, and the only\n autonomous implementation adapter was `shiki runner codex`.\n \n@@ -18,13 +18,13 @@\n - The platform's planning, review, and CCA layers already run on Claude Code,\n   and Claude Code's native capabilities (the built-in `/code-review` skill and\n   the Workflow multi-agent orchestration tool) have no Codex equivalent.\n-- The Goal lifecycle redesign (ADR 0009) requires an autonomous post-freeze\n+- The Goal lifecycle redesign (SADR-0009) requires an autonomous post-freeze\n   loop driven from the operator's Claude Code session, including bounded\n   repair dispatch, which must not depend on a runtime that cannot be invoked\n   unattended.\n \n The constitution claims Shiki is runtime-agnostic (\"not a Claude-only\n-workflow\", ADR 0002). Any change to the implementer default must preserve the\n+workflow\", SADR-0002). Any change to the implementer default must preserve the\n registry-based runtime model rather than hardcoding one vendor.\n \n ## Decision\n@@ -39,7 +39,7 @@\n   the task to review or repair-needed.\n - Keep Codex registered as an optional implementer runtime. `shiki runner\n   codex`, the Codex handoff templates, and the ChatGPT OAuth auth model from\n-  ADR 0005 remain supported for tasks explicitly assigned to `codex`.\n+  SADR-0005 remain supported for tasks explicitly assigned to `codex`.\n - Commit to mandating two Claude-native capabilities as loop contract in\n   follow-up Goals (status: the code-review gate was delivered by Goal G-B \u2014\n   skill registry, checklists item PR-12, default required_skills \u2014 while the\n@@ -60,7 +60,7 @@\n \n Out of scope: removing Codex support, changing the CCA runtime (Claude Code\n Action with `CLAUDE_CODE_OAUTH_TOKEN` stays), and API-key based automation\n-(still requires its own ADR per ADR 0005).\n+(still requires its own target ADR per SADR-0005).\n \n ## Consequences\n \n@@ -100,7 +100,7 @@\n   harness rejection of direct `codex exec` is outside Shiki's control, and the\n   plan's mandatory native capabilities cannot run on Codex at all.\n - **Remove Codex entirely**: rejected; it abandons the runtime-agnostic\n-  constitution (ADR 0002) for no operational gain and forecloses\n+  constitution (SADR-0002) for no operational gain and forecloses\n   cross-runtime adversarial review.\n - **GitHub Actions cloud implementation via claude-code-action**: rejected as\n   default; durable evidence is automatic but token cost, Node 24 workflow\ndiff --git a/docs/adr/0009-requirements-definition-and-spec-freeze.md b/docs/adr/SADR-0009-requirements-definition-and-spec-freeze.md\nindex 29b3f7871bef9203a8b4a831a65c39eb2f506e42..52e7dd69ad0aa4efe0b68c62678abbf7df02aaec\n--- a/docs/adr/0009-requirements-definition-and-spec-freeze.md\n+++ b/docs/adr/SADR-0009-requirements-definition-and-spec-freeze.md\n@@ -1,4 +1,4 @@\n-# ADR 0009: Merge Grill And Planning Into Requirements Definition With Spec Freeze\n+# SADR-0009: Merge Grill And Planning Into Requirements Definition With Spec Freeze\n \n ## Status\n \ndiff --git a/docs/adr/0010-external-ai-guardian-review.md b/docs/adr/SADR-0010-external-ai-guardian-review.md\nindex e6108bbe8bb4e73e0ff4684c8f78001a80438c4e..f3510b145664298adafb151b6f0db51b972f01ec\n--- a/docs/adr/0010-external-ai-guardian-review.md\n+++ b/docs/adr/SADR-0010-external-ai-guardian-review.md\n@@ -1,4 +1,4 @@\n-# ADR 0010: External AI Guardian Review As A First-Class Approval Authority\n+# SADR-0010: External AI Guardian Review As A First-Class Approval Authority\n \n ## Status\n \n@@ -8,7 +8,7 @@\n \n Shiki is intended to be an autonomous, recursive, self-improving agent\n framework: authority-in-the-loop, not human-in-the-loop. Until now the Guardian\n-approval required for high/critical-risk merges (guardian-policy.json, ADR\n+approval required for high/critical-risk merges (guardian-policy.json, SADR\n governance) recognized only human authority sources \u2014 a GitHub review approval,\n the `guardian:approved` label applied by a configured Guardian, or a comment\n carrying `Guardian approval granted` plus the current head SHA.\n@@ -89,7 +89,7 @@\n   autonomous design the operator intends and produced the impersonation\n   incident.\n - **Record AI approval as an operator comment**: rejected as forgery of human\n-  authority \u2014 the exact failure this ADR corrects.\n+  authority \u2014 the exact failure this SADR corrects.\n - **Deliver the artifact as a committed `.shiki/` file**: rejected; a committed\n   approval inside the PR being approved is forgeable by anyone who can push.\n - **A dedicated reviewer bot identity instead of a relayed comment**: deferred;\ndiff --git a/docs/adr/0011-autonomous-quality-gate-evidence.md b/docs/adr/SADR-0011-autonomous-quality-gate-evidence.md\nindex bca0246c5d46e44afbde15486a75eec425fc02f5..9c93eac16d8c3a6d4b11bd7224e3955102cd7db4\n--- a/docs/adr/0011-autonomous-quality-gate-evidence.md\n+++ b/docs/adr/SADR-0011-autonomous-quality-gate-evidence.md\n@@ -1,4 +1,4 @@\n-# ADR 0011: Autonomous Quality-Gate Evidence \u2014 Deterministic Facts And An Independent Verifier\n+# SADR-0011: Autonomous Quality-Gate Evidence \u2014 Deterministic Facts And An Independent Verifier\n \n ## Status\n \ndiff --git a/docs/adr/0012-autonomous-loop-closeout-to-main.md b/docs/adr/SADR-0012-autonomous-loop-closeout-to-main.md\nindex 637fad6dde4989c9c2bf0f81f676d8d29991123c..ec7d85f81030a676bcbba28b43eb365422a36eac\n--- a/docs/adr/0012-autonomous-loop-closeout-to-main.md\n+++ b/docs/adr/SADR-0012-autonomous-loop-closeout-to-main.md\n@@ -1,4 +1,4 @@\n-# 0012 \u2014 Autonomous loop pushes goal completion to main via a closeout PR\n+# SADR-0012 \u2014 Autonomous loop pushes goal completion to main via a closeout PR\n \n - Status: accepted\n - Date: 2026-06-18\ndiff --git a/docs/adr/0013-bootstrap-grants-actions-approve-pull-requests.md b/docs/adr/SADR-0013-bootstrap-grants-actions-approve-pull-requests.md\nindex b6c69d64f1c5ced346b88d6859586715b3b5bd0e..cf48ff4a6716267f539ca58edd1a852e7c7a2d64\n--- a/docs/adr/0013-bootstrap-grants-actions-approve-pull-requests.md\n+++ b/docs/adr/SADR-0013-bootstrap-grants-actions-approve-pull-requests.md\n@@ -1,4 +1,4 @@\n-# ADR 0013: Bootstrap Grants GitHub Actions Permission To Create And Approve Pull Requests\n+# SADR-0013: Bootstrap Grants GitHub Actions Permission To Create And Approve Pull Requests\n \n ## Status\n \n@@ -31,7 +31,7 @@\n security tradeoff: it lets repository automation satisfy a human-review gate. In\n a multi-maintainer repo that could let a compromised or buggy workflow\n self-approve changes. So whether bootstrap turns it on by default \u2014 and with\n-what default token scope \u2014 is an ADR-worthy decision, not an implementation\n+what default token scope \u2014 is a SADR-worthy decision, not an implementation\n detail.\n \n ## Decision\n@@ -48,7 +48,7 @@\n Specifically:\n \n - **`can_approve_pull_request_reviews` defaults to `true`.** Shiki's default\n-  posture is solo/AFK autonomous operation (ADR 0008/0009/0012), where the CCA\n+  posture is solo/AFK autonomous operation (SADR-0008/0009/0012), where the CCA\n   Review Bridge is the only path that can satisfy `required_review: true` after\n   the CCA and Guardian gates have already passed. The default makes a\n   freshly-bootstrapped repo able to run the autonomous loop end to end without a\ndiff --git a/docs/adr/0014-codex-app-external-ai-guardian-ui-adapter.md b/docs/adr/SADR-0014-codex-app-external-ai-guardian-ui-adapter.md\nindex 238db06279334caa341c117362df9271382a7fcd..65f12e9939321e1b3e389a34c822fc1549e5155d\n--- a/docs/adr/0014-codex-app-external-ai-guardian-ui-adapter.md\n+++ b/docs/adr/SADR-0014-codex-app-external-ai-guardian-ui-adapter.md\n@@ -1,4 +1,4 @@\n-# ADR 0014: Use Codex App As The External AI Guardian UI Adapter\n+# SADR-0014: Use Codex App As The External AI Guardian UI Adapter\n \n ## Status\n \ndiff --git a/docs/adr/0015-contract-approval-before-dispatch.md b/docs/adr/SADR-0015-contract-approval-before-dispatch.md\nindex fad6c656741a20a7684ba16080a7db159f63a52f..6061271a46d3f1d6ae7174477de3fb75673123a6\n--- a/docs/adr/0015-contract-approval-before-dispatch.md\n+++ b/docs/adr/SADR-0015-contract-approval-before-dispatch.md\n@@ -1,4 +1,4 @@\n-# ADR 0015: Approve The Task Contract Before Dispatch, Not The Diff After It\n+# SADR-0015: Approve The Task Contract Before Dispatch, Not The Diff After It\n \n ## Status\n \n@@ -9,7 +9,7 @@\n Guardian approval for high/critical risk is expressible only on a pull request.\n Every approval source in `guardian-policy.json` \u2014 GitHub review, the\n `guardian:approved` label, the `Guardian approval granted` comment, and the\n-`external_ai_guardian_review` artifact (ADR 0010/0014) \u2014 is bound to a PR, and\n+`external_ai_guardian_review` artifact (SADR-0010/0014) \u2014 is bound to a PR, and\n three of the four require the current head SHA. Approval therefore cannot exist\n before an implementation PR exists.\n \n@@ -81,7 +81,7 @@\n An envelope breach must surface only on the non-repairable policy gate, never on\n the repairable `MergeGate metadata check`. Otherwise the Goal Loop routes it to\n `dispatch_repair` \u2014 instructing a runner to make the breach go away \u2014 which is\n-exactly what this ADR forbids.\n+exactly what this SADR forbids.\n \n **Contract immutability.** After registration, an implementation PR must not\n change the governance fields of its own task contract (`scope`, `non_goals`,\n@@ -116,7 +116,7 @@\n fails any verdict that is not `complete`, and the signal the CCA prompt reads is\n computed from `evaluate_guardian_approval` alone. Without the OR-merge at the\n signal, a contract-approved PR still returns `needs_guardian`, the CCA job fails,\n-and the MergeGate policy check never runs \u2014 the gate this ADR changes would never\n+and the MergeGate policy check never runs \u2014 the gate this SADR changes would never\n be reached. The CCA *prompt* needs no change: a `contract_approval` entry in the\n signal's existing `sources` array satisfies its current wording.\n \n@@ -130,7 +130,7 @@\n written, and an approved contract lets its implementation PRs merge through the\n same automatic path as low/medium risk. The wasted implement-then-block cycle\n disappears. Guardian review shifts from reading diffs to reading contracts, which\n-is both earlier and smaller; the external AI Guardian path (ADR 0010/0014) works\n+is both earlier and smaller; the external AI Guardian path (SADR-0010/0014) works\n on contracts with no change.\n \n We accept that the Guardian authorizes a class of change without seeing its\n@@ -146,7 +146,7 @@\n adversarial tests for the new approval path equivalent in rigor to\n `test_shiki_governance_evidence.sh`.\n \n-Bootstrap costs, which apply only while this ADR is being implemented. The tasks\n+Bootstrap costs, which apply only while this SADR is being implemented. The tasks\n that build Contract Approval merge under the existing rules, so the ones\n dispatched after the pre-dispatch gate lands but before the approval path lands\n need a Guardian-approved Contract PR *and* a live Guardian approval on their\n@@ -210,7 +210,7 @@\n risk; they do not bound authorized work.\n \n **Auto-escalate risk on envelope breach instead of stopping.** Returns the\n-workflow to a diff-time Guardian decision \u2014 the exact behavior this ADR removes.\n+workflow to a diff-time Guardian decision \u2014 the exact behavior this SADR removes.\n \n **Resolve governance fields from the base snapshot only.** Rejected against the\n registration flow: implementation PRs add their own contract, so the base has no\ndiff --git a/docs/adr/0016-locks-bound-product-paths.md b/docs/adr/SADR-0016-locks-bound-product-paths.md\nindex b5e850f3f1f7f4ef6e7f9d1763a29a51bd300c45..46b40a9b99a7aae644c02bb458f5473ff0a90ce2\n--- a/docs/adr/0016-locks-bound-product-paths.md\n+++ b/docs/adr/SADR-0016-locks-bound-product-paths.md\n@@ -1,4 +1,4 @@\n-# ADR 0016: Locks bound product paths; the Shiki mirror is bound by record identity\n+# SADR-0016: Locks bound product paths; the Shiki mirror is bound by record identity\n \n ## Status\n \n@@ -6,8 +6,8 @@\n \n - Date: 2026-07-28\n - Deciders: operator (mizutani-140), Claude (planner)\n-- Related: ADR 0004 (GitHub-first with a `.shiki` mirror), ADR 0009 (Spec Freeze),\n-  ADR 0012 (autonomous loop closeout to main), ADR 0015 (Contract Approval before\n+- Related: SADR-0004 (GitHub-first with a `.shiki` mirror), SADR-0009 (Spec Freeze),\n+  SADR-0012 (autonomous loop closeout to main), SADR-0015 (Contract Approval before\n   dispatch), remediation goal task R-02, the live PR #179 deadlock.\n \n ## Context\n@@ -90,11 +90,11 @@\n files, documentation files and named source modules are nameable; runtime-\n allocated ledger, runner and report ids are not \u2014 and those live in `.shiki`,\n which locks no longer govern. Existing frozen contracts are brought into line by\n-a one-time Spec Amendment per plan, which must land before ADR 0015's contract\n+a one-time Spec Amendment per plan, which must land before SADR-0015's contract\n immutability makes `locks` unwritable from the implementation PR.\n \n **Lock state is resolved from the durable side.** MergeGate resolves the lock\n-registry, and ADR 0015's governance fields, from the base `.shiki` snapshot when\n+registry, and SADR-0015's governance fields, from the base `.shiki` snapshot when\n present, falling back to head only when absent. The `MergeGate metadata check`\n job is given `--base-shiki`, which it does not receive today. Locks are not\n derived from the live open-pull-request set: a pull request body is untrusted\n@@ -108,7 +108,7 @@\n Explicitly out of scope: moving MergeGate's contract resolution off the PR\n checkout wholesale; making the mirror a projection of GitHub state; introducing\n new lock kinds; moving ledger evidence out of the repository; and unwinding\n-ADR 0012's closeout pull request. Registration of multi-task goals is ADR 0015's\n+SADR-0012's closeout pull request. Registration of multi-task goals is SADR-0015's\n Contract PR and is not re-decided here.\n \n ## Consequences\n@@ -127,7 +127,7 @@\n \n Risks accepted. First, goal-scoped identity is only as strong as `goal_id`,\n which is resolved from the PR body and the head task file; the mitigation is to\n-add `goal_id` to ADR 0015's frozen governance field set, and until that lands a\n+add `goal_id` to SADR-0015's frozen governance field set, and until that lands a\n task file can name a foreign goal. Second, base-when-present resolution is\n unavailable on fork pull requests, where the head is contributor-controlled;\n fork pull requests already cannot complete a Shiki merge, and this decision does\n@@ -139,7 +139,7 @@\n path, not by implementation pull requests.\n \n New obligations: one Spec Amendment per frozen plan to rewrite `locks`, ordered\n-before ADR 0015 T1; extension of the adversarial suites in\n+before SADR-0015 T1; extension of the adversarial suites in\n `scripts/test_shiki_adversarial_state.sh` and\n `scripts/test_shiki_governance_evidence.sh` to cover the eleven newly-scoped\n directories; and updating `scripts/test_shiki_loop_lock_guard.sh`,\n@@ -151,32 +151,32 @@\n whether the loop's transitive sync of ledger `evidence` references can be\n deleted once the Contract PR carries its own source plan.\n \n-## Relationship to ADR 0004\n+## Relationship to SADR-0004\n \n-This ADR **clarifies something ADR 0004 left unspecified**. ADR 0004 decides that\n+This SADR **clarifies something SADR-0004 left unspecified**. SADR-0004 decides that\n GitHub is the operational source of truth and that each target repository keeps a\n `.shiki/` mirror. It says nothing about pull requests writing the mirror, does\n not name MergeGate, and does not make the mirror an input to a gate. Nothing in\n-ADR 0004's Decision or Consequences is amended, superseded or weakened: the\n+SADR-0004's Decision or Consequences is amended, superseded or weakened: the\n mirror still exists, still records the same artifacts, and pull request\n-boundaries remain the coordination surface. What this ADR settles is the question\n+boundaries remain the coordination surface. What this SADR settles is the question\n 0004 never reached \u2014 by what authority a pull request may write a mirror record \u2014\n and it answers it the way `.shiki/manifest.json` already declares\n (`pr_mutation: current-task-or-current-goal-only`).\n \n-It **amends ADR 0012**, whose Decision item 5 states that \"Loop-executed tasks\n+It **amends SADR-0012**, whose Decision item 5 states that \"Loop-executed tasks\n declare the `path:.shiki/**` lock, which covers every `.shiki` file the closeout\n stages ... so `files_outside_locks` is satisfied without per-file lock\n bookkeeping.\" That clause is replaced: the closeout's mirror writes are\n-authorized by record identity, not by a declared lock. ADR 0012's substantive\n+authorized by record identity, not by a declared lock. SADR-0012's substantive\n decision \u2014 that completion reaches `main` through a closeout pull request rather\n than local attestation \u2014 is preserved unchanged.\n \n-It is **complementary to ADR 0015**, not a substitute. ADR 0015 owns contract\n+It is **complementary to SADR-0015**, not a substitute. SADR-0015 owns contract\n immutability, the pre-dispatch gate, the Contract PR and the Scope Envelope. This\n-ADR owns what a lock means and who may write the mirror. The one ordering\n+SADR owns what a lock means and who may write the mirror. The one ordering\n constraint between them is that the lock-narrowing Spec Amendment must precede\n-ADR 0015 T1's freezing of the `locks` field.\n+SADR-0015 T1's freezing of the `locks` field.\n \n ## Alternatives Considered\n \n@@ -186,7 +186,7 @@\n the only writer that can register them may run. Migrating the Guardian signal to\n that store makes every registration PR fail closed to `needs_guardian` at every\n risk level, because the task file is by definition absent from the store, which\n-removes the low/medium autonomous path ADR 0008/0009 exists to provide. And it\n+removes the low/medium autonomous path SADR-0008/0009 exists to provide. And it\n leaves schemas, manifest, config, guardian policy, templates, migrations, plans,\n worktrees, runner records, runs and handoffs with no writer at all in a\n repository whose product is `.shiki`. Measured benefit on the frozen backlog:\n@@ -199,14 +199,14 @@\n namespace is not in fact ownership-partitioned \u2014 56 ledger entries carry no\n `task_id`, and one goal DAG is writable by 35 registered tasks. The correct\n expression of its insight is that locks do not govern the mirror at all, which is\n-what this ADR decides.\n+what this SADR decides.\n \n **Make the mirror a projection written only by a registration writer and a\n post-merge projection job.** Rejected. It requires a bypass actor with write\n access to the protected default branch; it makes its own master invariant\n readable from a manifest that pull requests may edit; it deletes both reconcile\n modes and leaves a corrupted mirror with no in-band repair path, contradicting\n-ADR 0004's consequence that mirror conflicts are repaired against GitHub state;\n+SADR-0004's consequence that mirror conflicts are repaired against GitHub state;\n and its content-derived ids are rejected by `validate_shiki`'s currently-green id\n pattern.\n \ndiff --git a/docs/adr/0017-bookkeeping-closeouts-do-not-inherit-risk.md b/docs/adr/SADR-0017-bookkeeping-closeouts-do-not-inherit-risk.md\nindex 554e3a0d526a72ca067108c5464cc68c3edcf174..b9676857a4713850e063af7c39c8e0b01ecb4c66\n--- a/docs/adr/0017-bookkeeping-closeouts-do-not-inherit-risk.md\n+++ b/docs/adr/SADR-0017-bookkeeping-closeouts-do-not-inherit-risk.md\n@@ -1,4 +1,4 @@\n-# ADR 0017: A Bookkeeping Closeout Does Not Inherit Its Task's Risk\n+# SADR-0017: A Bookkeeping Closeout Does Not Inherit Its Task's Risk\n \n ## Status\n \n@@ -6,7 +6,7 @@\n \n ## Context\n \n-ADR 0012 made the goal loop push a task's completion to the default branch through a\n+SADR-0012 made the goal loop push a task's completion to the default branch through a\n closeout pull request: after the implementation PR merges, the loop opens a second PR\n carrying `task.status = done`, `lock.state = released`, and \u2014 when the task completes\n its goal \u2014 `goal.status = complete` plus one scorecard report. Nothing else. The\n@@ -49,14 +49,14 @@\n 4. The task file's governance fields (`scope`, `non_goals`, `required_skills`,\n    `risk_level`, `locks`, `acceptance_checks`, `test_command`) are byte-identical to\n    the base snapshot. Only `status`, `expected_pr`, `closeout_pr`, `ledger_evidence`\n-   and `expected_branch` may differ \u2014 the ADR 0015 contract-immutability rule,\n+   and `expected_branch` may differ \u2014 the SADR-0015 contract-immutability rule,\n    unchanged.\n \n    `expected_branch` is in that set because a closeout is *by construction* on a\n    different branch from the implementation it closes, and the loop rewrites the\n    field accordingly. It is safe to let it differ because MergeGate independently\n    requires `expected_branch` to equal the PR's actual head ref, so a PR cannot set\n-   it to anything other than the branch it is really on. The first draft of this ADR\n+   it to anything other than the branch it is really on. The first draft of this SADR\n    omitted it, which made the exemption unreachable: every real closeout failed\n    condition 4 on a field the loop always changes. See PR #209.\n 5. The state transitions are exactly the terminal set: task `review` \u2192 `done`, lock\n@@ -113,12 +113,12 @@\n classification must be derived, not asserted.\n \n **Fold the closeout into the implementation PR** so no second PR exists. Rejected under\n-ADR 0012, which recorded why: a task file that is already `done` on the implementation\n+SADR-0012, which recorded why: a task file that is already `done` on the implementation\n branch short-circuits the loop's review \u2192 CCA \u2192 merge \u2192 done ordering into a local\n goal-completion before any merge happens.\n \n **Have the loop auto-merge closeouts with elevated credentials.** Rejected outright: that\n-is a runtime authoring its own approval, which ADR 0010 and the constitution's\n+is a runtime authoring its own approval, which SADR-0010 and the constitution's\n approval-record prohibition forbid. It would also be indistinguishable, in the evidence,\n from the forgery those rules exist to prevent.\n \ndiff --git a/docs/adr/0018-carried-guardian-approval-across-a-proven-base-sync.md b/docs/adr/SADR-0018-carried-guardian-approval-across-a-proven-base-sync.md\nindex 22358562da390ed8b45cd600da08a43d378b4680..e6e2907cefa776460af73ddf7d39db605f3c39b0\n--- a/docs/adr/0018-carried-guardian-approval-across-a-proven-base-sync.md\n+++ b/docs/adr/SADR-0018-carried-guardian-approval-across-a-proven-base-sync.md\n@@ -1,4 +1,4 @@\n-# ADR 0018: Carried Guardian Approval Across A Proven Base Sync\n+# SADR-0018: Carried Guardian Approval Across A Proven Base Sync\n \n ## Status\n \n@@ -34,7 +34,7 @@\n Guardian evaluator that consumes it (`shiki_guardian.evaluate_guardian_approval`,\n the `guardian_comment_carried` source) were built and tested in isolation. Neither\n does anything until a caller passes the flags that turn the carry on, so the\n-feature was invisible in CI. This ADR records the decision the two of them\n+feature was invisible in CI. This SADR records the decision the two of them\n implement, and this task wires it into the four CI invocations that judge Guardian\n approval.\n \ndiff --git a/docs/adr/README.md b/docs/adr/README.md\nindex c70e057a4520c36e042ab68c53cd4a21ef709b71..5da43856836e2ce6a150f8127766e2f88355ab4b\n--- a/docs/adr/README.md\n+++ b/docs/adr/README.md\n@@ -1,40 +1,41 @@\n-# Architecture Decision Records\n+# Decision Record Namespaces\n \n-This directory holds Shiki's Architecture Decision Records (ADRs): durable\n-records of hard-to-reverse platform decisions. ADRs are part of the Shiki source\n-of truth alongside GitHub state, the `.shiki/` mirror, and `CONTEXT.md`.\n+This directory holds two independent decision namespaces:\n \n-## How ADRs Work\n+- Shiki Architecture Decision Records (SADRs), named\n+  `SADR-NNNN-<short-slug>.md`, record hard-to-reverse Shiki platform decisions.\n+- Target Architecture Decision Records (ADRs), named\n+  `NNNN-<short-slug>.md`, belong to the target product.\n \n-- New ADRs are added as `docs/adr/NNNN-<short-slug>.md`, where `NNNN` is the next\n-  zero-padded sequence number.\n-- The **canonical list of ADRs is the set of files matching `docs/adr/*.md`** in\n-  this directory. Browse `docs/adr/*.md` for the authoritative, current set.\n-- Use [`template.md`](template.md) as the starting point for a new ADR.\n-- Record an ADR when a decision is hard to reverse, surprising without context,\n-  and a real tradeoff. Do not write an ADR for routine or easily reversible\n-  choices.\n+The same four-digit number may appear once in each namespace. A platform\n+`SADR-NNNN` citation resolves only to `SADR-NNNN-*.md`; a target `ADR NNNN`\n+citation resolves only to `NNNN-*.md`.\n \n-Using the `docs/adr/*.md` glob as the source of truth means other PRs can add an\n-ADR without editing this index file, avoiding merge contention on a\n-hand-maintained enumeration.\n+## How Decision Records Work\n \n-## Current ADRs (snapshot)\n-\n-The list below is a convenience snapshot and may lag behind the directory. When\n-in doubt, list `docs/adr/*.md`.\n+- New Shiki platform decisions use the next zero-padded SADR number.\n+- New target product decisions use the target's own ADR sequence.\n+- The canonical list is the files in `docs/adr/`; no hand-maintained index is\n+  authoritative.\n+- Use [`template.md`](template.md) as the starting point and choose the filename\n+  namespace that owns the decision.\n+- Record a decision when it is hard to reverse, surprising without context, and\n+  a real tradeoff. Do not write a decision record for routine or easily\n+  reversible choices.\n \n-- [0001 \u2014 Keep Shiki As The Platform Name](0001-keep-shiki-as-platform-name.md)\n-- [0002 \u2014 Use AGENTS As The Shared Agent Constitution](0002-use-agents-as-shared-constitution.md)\n-- [0003 \u2014 Require Skill Gate For MergeGate Readiness](0003-require-skill-gate-for-mergegate-readiness.md)\n-- [0004 \u2014 Use GitHub First With A Repository-Local Shiki Mirror](0004-use-github-first-with-shiki-mirror.md)\n-- [0005 \u2014 Use Codex As Front And Claude Code Action As The GitHub Runtime](0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md)\n-- [0006 \u2014 Separate Codex Implementation, CCA Completion, And MergeGate Authorization](0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md)\n+Using the directory as the source of truth lets independent changes add records\n+without contending on an enumerated index.\n \n-## Adding A New ADR\n+## Adding A Shiki SADR\n \n-1. Copy `template.md` to `docs/adr/NNNN-<short-slug>.md`.\n+1. Copy `template.md` to `docs/adr/SADR-NNNN-<short-slug>.md`.\n 2. Fill in the title, status, context, decision, and consequences.\n-3. Set the status to `Proposed`, then `Accepted` once the decision is approved.\n-4. Open a PR following `CONTRIBUTING.md`. You do not need to edit this index for\n-   the ADR to be discoverable via `docs/adr/*.md`.\n+3. Set the status to `Proposed`, then `Accepted` once the proper authority has\n+   approved the decision.\n+4. Open a PR following `CONTRIBUTING.md`.\n+\n+## Adding A Target ADR\n+\n+1. Copy `template.md` to `docs/adr/NNNN-<short-slug>.md` using the target's own\n+   sequence.\n+2. Cite it as `ADR NNNN`; do not prefix its filename with `SADR-`.\ndiff --git a/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md b/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\nnew file mode 100644\nindex 0000000000000000000000000000000000000000..7455ce1f3bed02ddddb851ac21a1d1a648c9aeba\n--- /dev/null\n+++ b/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\n@@ -0,0 +1,55 @@\n+# SADR-0019: Separate Shiki Platform SADRs From Target ADRs\n+\n+## Status\n+\n+Accepted\n+\n+## Context\n+\n+Shiki installs its platform decision records into `docs/adr/` in every target\n+repository. Shiki and its targets previously used the same numeric\n+`NNNN-<slug>.md` namespace. A target that already owned ADRs 0013 through 0018\n+therefore acquired two records for each number during installation, making an\n+`ADR NNNN` citation ambiguous. Renumbering target decisions would rewrite target\n+history, while renumbering Shiki decisions per target would destroy their stable\n+platform identity.\n+\n+Upgrades also need to remove the former Shiki-owned numeric paths without ever\n+mistaking a target-owned ADR for platform content. The install stamp already\n+records exact shipped paths and content digests, so it can prove ownership only\n+when both still match.\n+\n+## Decision\n+\n+Shiki platform decisions use the `SADR` namespace:\n+\n+- records are named `docs/adr/SADR-NNNN-<slug>.md`;\n+- live platform citations use `SADR-NNNN`;\n+- target product decisions retain `docs/adr/NNNN-<slug>.md` and `ADR NNNN`;\n+- the reference validator resolves the two namespaces independently, so the same\n+  four-digit number may exist once in each namespace.\n+\n+Before a forced upgrade writes anything, the installer inventories the exact\n+former numeric Shiki ADR paths. It may delete a legacy path only when the\n+existing install stamp contains that exact path and its recorded SHA-256 digest\n+matches the file currently on disk. All ownership blockers are reported\n+together and abort the upgrade before its first write. A non-forced install\n+never performs this cleanup.\n+\n+Historical `.shiki/` evidence remains unchanged because it records the namespace\n+that existed when that evidence was produced.\n+\n+## Consequences\n+\n+- Targets may use any ADR number without colliding with Shiki's platform SADRs.\n+- Fresh installs preserve target ADR bytes and install a stable SADR set.\n+- Forced upgrades fail closed when legacy ownership cannot be proven, and a\n+  successful upgrade refreshes the stamp without removed legacy paths.\n+- Current governance, packaging, workflow, prompt, script, and skill references\n+  must move with the platform records.\n+\n+## Alternatives Considered\n+\n+- Collision-only refusal leaves affected targets permanently unupgradable.\n+- Renumbering target ADRs mutates target-owned history.\n+- Per-target Shiki renumbering makes platform citations non-portable.\ndiff --git a/docs/adr/template.md b/docs/adr/template.md\nindex a92a97daddd0b00ab9dd626cc8d86942da2eafed..17dd79175b5fee2105935557f691b40cf9492942\n--- a/docs/adr/template.md\n+++ b/docs/adr/template.md\n@@ -1,14 +1,14 @@\n-# NNNN: <Short Decision Title>\n+# <SADR-NNNN or NNNN>: <Short Decision Title>\n \n ## Status\n \n-Proposed | Accepted | Superseded by ADR-NNNN\n+Proposed | Accepted | Superseded by SADR-NNNN or target ADR NNNN\n \n ## Context\n \n What is the situation, constraint, or force that requires a decision? Reference\n-relevant domain terms (`CONTEXT.md`), prior ADRs, and code reality. State the\n-problem, not the solution.\n+relevant domain terms (`CONTEXT.md`), prior Shiki SADRs or target ADRs, and code\n+reality. State the problem, not the solution.\n \n ## Decision\n \ndiff --git a/docs/agents/autonomous-loop-e2e-acceptance.md b/docs/agents/autonomous-loop-e2e-acceptance.md\nindex bbf84c6081987f159ac6ad9ecf3c82b7aa72e7e8..5c13c1d02a05a90b0dc46cbef7c95a9846d0c237\n--- a/docs/agents/autonomous-loop-e2e-acceptance.md\n+++ b/docs/agents/autonomous-loop-e2e-acceptance.md\n@@ -40,8 +40,8 @@\n 3. `gh auth status` is logged in with merge permission on the repo.\n 4. Branch protection + required checks are configured exactly as the goal-loop\n    required-check set expects (see `docs/agents/branch-protection-smoke.md`).\n-5. The repo `.shiki/policy` permits autonomous low/medium-risk merges (ADR\n-   0008/0009); the MergeGate policy check is the recorded-authority gate.\n+5. The repo `.shiki/policy` permits autonomous low/medium-risk merges\n+   (SADR-0008/0009); the MergeGate policy check is the recorded-authority gate.\n \n The operator's only actions are: register the task and start the loop. **No\n manual pre-clearing** means after `shiki loop run` starts, the operator does not\ndiff --git a/docs/agents/bootstrap-command.md b/docs/agents/bootstrap-command.md\nindex 05f7f203a52cf412b9f97084a6e131ed0f785afd..f7aa0b734dfebee4a3b139335b66ee3415f48713\n--- a/docs/agents/bootstrap-command.md\n+++ b/docs/agents/bootstrap-command.md\n@@ -189,7 +189,9 @@\n ```\n \n Use this only for tests, fixtures, or explicit local-only template inspection.\n-Use `--force` only when you intentionally want to overwrite existing target files.\n+Use `--force` only when you intentionally want to refresh the target template.\n+Legacy numeric Shiki decision cleanup is stamp-and-digest authorized before any\n+write; target ADR paths are not cleanup candidates.\n \n ## Slash Command\n \ndiff --git a/docs/agents/checklists.md b/docs/agents/checklists.md\nindex 4a7f03ade346f53a1c1d82981f3fd744f9ab4547..0c5ec6c53dc7197b7b7a7d18e667b325babd6c45\n--- a/docs/agents/checklists.md\n+++ b/docs/agents/checklists.md\n@@ -32,18 +32,18 @@\n | ID | Item | Blocking |\n | --- | --- | --- |\n | GD-01 | Plan was challenged against `CONTEXT.md`. | blocking for non-trivial Goals |\n-| GD-02 | Relevant ADRs were read or absence was recorded. | blocking for non-trivial Goals |\n+| GD-02 | Relevant SADRs and target ADRs were read or absence was recorded. | blocking for non-trivial Goals |\n | GD-03 | Ambiguous terms were resolved or converted to HITL blockers. | blocking |\n | GD-04 | Edge scenarios were considered. | advisory |\n | GD-05 | Code reality was checked when code could answer. | blocking when applicable |\n | GD-06 | New domain terms were added to `CONTEXT.md` when needed. | blocking when applicable |\n-| GD-07 | ADR was created or explicitly skipped using the ADR criteria. | blocking when applicable |\n+| GD-07 | A SADR or target ADR was created or explicitly skipped using the decision-record criteria. | blocking when applicable |\n \n ## CI \u2014 Context & Impact Checklist\n \n | ID | Item | Blocking |\n | --- | --- | --- |\n-| CI-01 | Relevant docs and ADRs identified. | blocking |\n+| CI-01 | Relevant docs, SADRs, and target ADRs identified. | blocking |\n | CI-02 | Relevant modules, interfaces, seams, callers, and tests identified. | blocking |\n | CI-03 | Dependencies and blocked-by relationships identified. | blocking |\n | CI-04 | Candidate locks identified. | blocking |\n@@ -62,7 +62,7 @@\n | PRD-04 | Implementation decisions are recorded without volatile code detail. | blocking |\n | PRD-05 | Testing decisions are recorded. | blocking |\n | PRD-06 | Out-of-scope items are explicit. | blocking |\n-| PRD-07 | Relevant glossary/ADR links are present when applicable. | blocking |\n+| PRD-07 | Relevant glossary/SADR/target-ADR links are present when applicable. | blocking |\n \n ## SF \u2014 Spec Freeze Checklist\n \n@@ -98,7 +98,7 @@\n | H-02 | Branch/worktree target is present. | blocking |\n | H-03 | Scope and non-goals are present. | blocking |\n | H-04 | Dependencies and locks are present. | blocking |\n-| H-05 | Relevant docs, ADRs, modules, and tests are referenced. | blocking |\n+| H-05 | Relevant docs, SADRs, target ADRs, modules, and tests are referenced. | blocking |\n | H-06 | Required skills are stated. | blocking |\n | H-07 | TDD expectations are stated. | blocking when behavior work |\n | H-08 | Acceptance criteria are included. | blocking |\ndiff --git a/docs/agents/codex-handoff.md b/docs/agents/codex-handoff.md\nindex ede468f47cb277f69a857e8c45df703e4ed0151d..6f97bdffb67e44d7edea2c71e01ea28933c4eb6c\n--- a/docs/agents/codex-handoff.md\n+++ b/docs/agents/codex-handoff.md\n@@ -46,7 +46,7 @@\n \n ## Context\n - Domain terms:\n-- Relevant ADRs:\n+- Relevant SADRs and target ADRs:\n - Relevant docs:\n - Relevant modules/interfaces/seams:\n - Similar tests/prior art:\ndiff --git a/docs/agents/completion-check-agent.md b/docs/agents/completion-check-agent.md\nindex 13e4c035db35025f4e3cdcc5adfb24314ac4d521..61bfa1218cf36ca605250c91919e620f33238aa0\n--- a/docs/agents/completion-check-agent.md\n+++ b/docs/agents/completion-check-agent.md\n@@ -32,7 +32,7 @@\n - `AGENTS.md`\n - `CLAUDE.md`\n - `CONTEXT.md`\n-- relevant ADRs\n+- relevant SADRs and target ADRs\n - parent Goal issue\n - PRD issue or document\n - task issue\ndiff --git a/docs/agents/decision-control.md b/docs/agents/decision-control.md\nindex 7d8ee5ab03a5ccbcbd04056ae61de03785a2a9c9..8c94ecda00fffaa0e92c325ab83aa9776bd1dac1\n--- a/docs/agents/decision-control.md\n+++ b/docs/agents/decision-control.md\n@@ -11,7 +11,7 @@\n | Decision | Owner | When | Required inputs | Output | Enforcement surface |\n | --- | --- | --- | --- | --- | --- |\n | Is the Goal clear enough to plan? | Planner + Guardian when needed | Goal intake | User request, existing docs, risk signals | Goal readiness verdict | Goal checklist, issue template, ledger |\n-| Must `grill-with-docs` run? | Planner / Skill Gate | Before PRD or issue split | Ambiguity, domain terms, ADR conflicts, unclear boundaries | Skill invocation or explicit skip rationale | Skill Gate checklist |\n+| Must `grill-with-docs` run? | Planner / Skill Gate | Before PRD or issue split | Ambiguity, domain terms, SADR/target-ADR conflicts, unclear boundaries | Skill invocation or explicit skip rationale | Skill Gate checklist |\n | Is a PRD required? | Planner | After Goal context stabilizes | Goal, `grill-with-docs` output, Context & Impact output | PRD required / not required | PRD checklist, CCA evidence check |\n | Can the spec be frozen? | Operator only | End of Requirements Definition, before issue split | PRD/requirements summary, scope inventory (SF-02), open-question list | `spec_freeze` block in the plan + ledger evidence | `require_grilled_plan` (plan ingest/run), SF checklist |\n | Can a frozen spec change? | Operator only (Spec Amendment) | When implementation contradicts the frozen spec | Contested decisions, scoped re-grill result | recorded amendment + re-stamped freeze | SF checklist, ledger evidence |\ndiff --git a/docs/agents/domain.md b/docs/agents/domain.md\nindex 258da8ea81e87ac0a6b0b5b5e5c2fde980eaabb7..d57b0cf17ff3e1fa9e6e5885372bf083d8295421\n--- a/docs/agents/domain.md\n+++ b/docs/agents/domain.md\n@@ -5,7 +5,9 @@\n ## Layout\n \n - `CONTEXT.md` defines Shiki language. Keep it implementation-free.\n-- `docs/adr/` records hard-to-reverse decisions.\n+- `docs/adr/SADR-NNNN-*.md` records Shiki platform decisions.\n+- `docs/adr/NNNN-*.md` records target product decisions without sharing the\n+  SADR namespace.\n - `AGENTS.md` is the runtime-neutral agent constitution.\n - `CLAUDE.md` is the Claude Code wrapper around `AGENTS.md`.\n - `.shiki/` is the Target Repository mirror for Goals, PRDs, plans, Task DAGs, contracts, locks, worktree records, ledger entries, CCA verdicts, repair packets, reports, and handoffs.\n@@ -15,6 +17,6 @@\n \n - Use Shiki terms exactly when they exist.\n - Add glossary terms to `CONTEXT.md` only when the term is part of the domain, not a generic programming concept.\n-- Create ADRs only for hard-to-reverse, surprising, tradeoff-based decisions.\n+- Create SADRs or target ADRs only for hard-to-reverse, surprising, tradeoff-based decisions.\n - Keep product-specific language in the Target Repository, not in Shiki Template docs.\n-- If an ADR conflicts with a proposed implementation, surface the conflict before editing.\n+- If a SADR or target ADR conflicts with a proposed implementation, surface the conflict before editing.\ndiff --git a/docs/agents/execution-control.md b/docs/agents/execution-control.md\nindex ad8ee3cdb36be56fedc1f46408ca92b387d74047..bc63c3a73661d12b567016ba299f1a6b66bf9088\n--- a/docs/agents/execution-control.md\n+++ b/docs/agents/execution-control.md\n@@ -63,7 +63,7 @@\n | Branch name | Orchestrator | Orchestrator | Before implementer dispatch | branch policy | branch ref in task/ledger |\n | Lock reservation | Context & Impact | MergeGate | Before dispatch and before merge | `.shiki/locks/`, labels, PR metadata | lock id, owner, scope |\n | CI profile | Planner | MergeGate | During issue triage, rechecked at PR | labels, `.shiki/policy.yaml`, workflow matrix | required check list |\n-| Required status checks | Repo maintainer / Guardian | GitHub branch protection / rulesets | Repository setup, updated via ADR | GitHub settings | branch/ruleset config, ADR |\n+| Required status checks | Repo maintainer / Guardian | GitHub branch protection / rulesets | Repository setup, updated via target ADR | GitHub settings | branch/ruleset config, target ADR |\n | CCA judgment | CCA | GitHub required check / MergeGate | Every non-draft PR update | GitHub Action check | structured CCA verdict |\n | Repair assignment | CCA | MergeGate | After failed CCA/CI/review | repair packet, issue/PR comment | repair id and attempt count |\n | Merge readiness | MergeGate | GitHub branch protection / Guardian if needed | After CCA complete | required checks, review rules, rulesets | mergegate check output |\ndiff --git a/docs/agents/guardian-policy.md b/docs/agents/guardian-policy.md\nindex 4147c7adf05a47adb4fd24ada1a1c2e1f2ba6d2d..a20d9cc6c00a569bb057b9ca8d6b51c7fc4a9847\n--- a/docs/agents/guardian-policy.md\n+++ b/docs/agents/guardian-policy.md\n@@ -98,8 +98,8 @@\n `origin/<default_branch>` and reuses each job's existing base fetch; a shallow\n (`--depth`) or second base fetch would make the proof unresolvable and silently\n disable the carry, so neither is permitted. CCA reads `guardian_comment_carried` as\n-a recorded authority in the deterministic signal and does not re-derive it. See ADR\n-0018 for the full decision, the rejected diff/patch-equality and path-disjointness\n+a recorded authority in the deterministic signal and does not re-derive it. See\n+SADR-0018 for the full decision, the rejected diff/patch-equality and path-disjointness\n alternatives, and the accepted residual.\n \n ## External AI Guardian Review (SADR-0010)\ndiff --git a/docs/agents/implementation-policy.md b/docs/agents/implementation-policy.md\nindex c754144a9fb5bf4a38aa82a60e1670c59e626b44..85c37281bcad196c6b7b02343faf00b03cd9c57a\n--- a/docs/agents/implementation-policy.md\n+++ b/docs/agents/implementation-policy.md\n@@ -31,10 +31,13 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n ## Non-Negotiables\n \n 1. **Planning precedes implementation.** Non-trivial Goals start with `grill-with-docs` and Context & Impact.\n-2. **Domain language is controlled.** Terms must be checked against `CONTEXT.md`; hard-to-reverse tradeoffs must be checked against ADRs.\n+2. **Domain language is controlled.** Terms must be checked against `CONTEXT.md`; hard-to-reverse tradeoffs must be checked against Shiki SADRs and target ADRs.\n 3. **PRD precedes issue decomposition.** `to-prd` is used once the context is settled enough to become durable intent.\n 4. **Issues are vertical slices.** `to-issues` must produce independently grabbable tracer-bullet tasks, not horizontal layer tickets.\n 5. **Triage controls dispatch.** Only `ready-for-agent` / AFK tasks may be assigned to Codex without human input.\n@@ -95,19 +98,19 @@\n Process:\n \n - Challenge terms against `CONTEXT.md`.\n-- Check ADRs before reopening decisions.\n+- Check SADRs and target ADRs before reopening decisions.\n - Inspect code when code can answer a question.\n - Ask one question at a time when the operator is available.\n - Provide a recommended answer with each question.\n - Record resolved terms in `CONTEXT.md`.\n-- Create ADRs only for hard-to-reverse, surprising, tradeoff-based decisions.\n+- Create SADRs or target ADRs only for hard-to-reverse, surprising, tradeoff-based decisions.\n - Convert unresolved load-bearing questions into HITL blockers.\n \n Outputs:\n \n - Resolved design-tree decisions.\n - Updated glossary terms when needed.\n-- ADRs when justified.\n+- SADRs or target ADRs when justified.\n - HITL blockers when decisions remain unresolved.\n \n ## Phase 3 \u2014 Context & Impact\n@@ -117,7 +120,7 @@\n \n Outputs must include:\n \n-- Relevant documents and ADRs.\n+- Relevant documents, SADRs, and target ADRs.\n - Relevant modules, interfaces, seams, callers, and tests.\n - Candidate locks.\n - Dependencies.\n@@ -140,7 +143,7 @@\n - Testing decisions.\n - Out of scope.\n - Further notes.\n-- Links to relevant Goal, domain terms, and ADRs.\n+- Links to relevant Goal, domain terms, SADRs, and target ADRs.\n \n Phase 4 ends with Spec Freeze: the operator explicitly approves the PRD, the\n plan records a `spec_freeze` block (status, approved_by, source), and the SF\n@@ -240,7 +243,7 @@\n - Dependencies and locks.\n - Risk and Guardian requirements.\n - Ledger completeness.\n-- Documentation and ADR updates.\n+- Documentation and SADR/target-ADR updates.\n - Scope drift.\n \n CCA emits one of:\n@@ -301,7 +304,7 @@\n | Evidence failure | Behavior might be done, but proof is missing. | Add checks, PR evidence, ledger entry, or artifacts. |\n | Scope drift | PR changes unrelated behavior. | Revert unrelated changes or split into new Goal. |\n | Dependency block | Upstream task or lock not complete. | Block until dependency resolves. |\n-| Architecture block | Decision needs ADR or deep-module discussion. | `grill-with-docs` or `improve-codebase-architecture`. |\n+| Architecture block | Decision needs a SADR, target ADR, or deep-module discussion. | `grill-with-docs` or `improve-codebase-architecture`. |\n | Risk block | Security/production/policy change needs human approval. | Guardian decision. |\n \n ## Phase 5.5 \u2014 Execution Decision Control\ndiff --git a/docs/agents/runtime-auth-model.md b/docs/agents/runtime-auth-model.md\nindex 6a2e1a47e81ea89b5c68a210ec07772a9374babc..3c54748d24233dbd0ba8473950282547ef4ce935\n--- a/docs/agents/runtime-auth-model.md\n+++ b/docs/agents/runtime-auth-model.md\n@@ -148,7 +148,7 @@\n \n API-key based runners may be added by a target repository as an explicit extension. They are not the default Shiki template path.\n \n-If a repo opts into API-key mode, record the exception in an ADR and update the repo's `AGENTS.md`, `.shiki/config.yaml`, and workflow permissions.\n+If a repo opts into API-key mode, record the target-specific exception in a target ADR and update the repo's `AGENTS.md`, `.shiki/config.yaml`, and workflow permissions.\n \n ## GitHub Actions Runtime Compatibility\n \ndiff --git a/docs/agents/skill-gate.md b/docs/agents/skill-gate.md\nindex c50398b0d7c60a8598f2a5c88a10015730dced13..3acffe6444617bb1d54b77d4b4708158ec735bc6\n--- a/docs/agents/skill-gate.md\n+++ b/docs/agents/skill-gate.md\n@@ -9,7 +9,7 @@\n | Skill | Trigger | Expected output |\n | --- | --- | --- |\n | `setup-matt-pocock-skills` | First configuring a repo for these skills, or when issue tracker, triage labels, or domain docs are missing. | `docs/agents/` configuration and an `Agent skills` block in `AGENTS.md` or `CLAUDE.md`. |\n-| `grill-with-docs` | Ambiguous plan, unclear terminology, boundary decision, tradeoff, or ADR-worthy design choice. | Clarified decision tree, recommended answers, updates to `CONTEXT.md` or ADRs when decisions crystallize. |\n+| `grill-with-docs` | Ambiguous plan, unclear terminology, boundary decision, tradeoff, or decision-record-worthy design choice. | Clarified decision tree, recommended answers, updates to `CONTEXT.md`, SADRs, or target ADRs when decisions crystallize. |\n | `zoom-out` | Agent lacks a higher-level architectural map. | Map of relevant modules, callers, seams, and domain concepts. |\n | `to-prd` | Settled context should become a PRD. | PRD published to the issue tracker with problem, solution, user stories, decisions, testing, and out-of-scope notes. |\n | `to-issues` | Goal, PRD, or plan must become implementation tickets. | Vertical-slice issues with acceptance criteria, dependencies, and AFK/HITL classification. |\n@@ -17,7 +17,7 @@\n | `tdd` | Feature work or bug fix has observable behavior that can be specified with tests. | Red-green-refactor loop through public interfaces, one vertical slice at a time. |\n | `code-review` | The TDD loop is green and the implementer is about to create or update a PR. Default for every implementation task. | Pre-PR self-review of the task diff with fixes applied on the task branch; findings and resolutions recorded as a ledger entry naming the skill and a `## Pre-PR code review` PR body section. |\n | `diagnose` | Hard bug, failing check, regression, flaky behavior, or performance problem. | Reproduction loop, ranked hypotheses, instrumentation, fix, regression test, post-mortem. |\n-| `improve-codebase-architecture` | Structural friction, testability, AI-navigability, deep-module opportunity, or refactor request. | Architecture candidates and deepening recommendations informed by `CONTEXT.md` and ADRs. |\n+| `improve-codebase-architecture` | Structural friction, testability, AI-navigability, deep-module opportunity, or refactor request. | Architecture candidates and deepening recommendations informed by `CONTEXT.md`, SADRs, and target ADRs. |\n | `prototype` | A throwaway logic or UI prototype is needed to answer a design question. | Clearly marked disposable prototype and captured decision. |\n \n ## MergeGate Interaction\ndiff --git a/docs/operations.md b/docs/operations.md\nindex cf1f452d4c589e38ccd58553bda0ad8684b51eb7..ace48a90ffbec9e5c0a7113bef16a4858bc44b3f\n--- a/docs/operations.md\n+++ b/docs/operations.md\n@@ -89,6 +89,16 @@\n      target history, brought forward by `shiki migrate`, not a template.\n    - **Everything else** (CLI scripts, workflows, schemas, docs) is overwritten.\n \n+   A forced upgrade also migrates the former numeric Shiki decision paths to\n+   `SADR-NNNN-*`. Before the first write, the installer inventories the exact\n+   legacy Shiki paths and authorizes each deletion only when the existing\n+   `.shiki/install-stamp.json` names that path and its SHA-256 digest still\n+   matches the file. An absent, unreadable, malformed, incomplete, or\n+   mismatching stamp reports every blocked path and stops with no filesystem\n+   change. Target-owned `NNNN-*` ADRs are not cleanup candidates. A non-force\n+   install deletes nothing and retains any still-matching legacy stamp entries\n+   so a later forced upgrade can prove ownership.\n+\n    If the target has pending migrations, `--force` refuses and writes nothing \u2014\n    apply migrations first (step 3), then re-run. The run ends with a single\n    summary of every `.new` written, so a half-upgrade is visible.\ndiff --git a/docs/releasing.md b/docs/releasing.md\nindex 10d73828e7dbd64e9d9f6b83bc84d010537ce62a..aafeee8c4d441e1e427efee2ce4f81a647f020e1\n--- a/docs/releasing.md\n+++ b/docs/releasing.md\n@@ -67,7 +67,7 @@\n CLI in `scripts/`. There are no third-party Python runtime dependencies. The CLI\n is intentionally **not** exposed as a `[project.scripts]` console entry point\n because `scripts/shiki.py` is a dependency-free shim and `scripts/` is not\n-packaged as an importable distribution (see the ADR for the rationale).\n+packaged as an importable distribution (see SADR-0007 for the rationale).\n \n Supported install paths:\n \n@@ -155,6 +155,15 @@\n    [docs/operations.md](operations.md#the-new-convention). If the target has\n    pending migrations, `--force` refuses and writes nothing; apply migrations\n    (step 2) first, then re-run.\n+\n+   The SADR namespace upgrade has an additional pre-write gate: each former\n+   numeric Shiki decision path is removed only when the existing install stamp\n+   contains that exact path and a digest matching its current bytes. The\n+   installer aggregates absent/unreadable/malformed-stamp, missing-digest, and\n+   digest-mismatch blockers before raising, so refusal leaves the target tree\n+   byte-identical. Numeric target ADRs are preserved. A successful forced\n+   upgrade installs and stamps `SADR-*` paths and drops the removed legacy paths\n+   from the refreshed stamp.\n 2. Apply repository-local `.shiki/` state migrations with the migrate command:\n \n    ```bash\ndiff --git a/docs/usage.md b/docs/usage.md\nindex b83407ee3970dd395dd893010bc5dbe8bad26051..a130ef6e40dfd8169a5bc701339351097df1a09f\n--- a/docs/usage.md\n+++ b/docs/usage.md\n@@ -67,8 +67,10 @@\n shiki install-target /path/to/target-repo --local-only\n ```\n \n-Use `--force` only when you intentionally want to overwrite existing target\n-files.\n+Use `--force` only when you intentionally want to refresh the target template.\n+For the SADR namespace migration, `--force` deletes only exact legacy Shiki\n+decision paths whose existing install-stamp digest still matches; any ownership\n+blocker stops before the first write. Numeric target ADRs are never candidates.\n \n ## `shiki bootstrap-platform` \u2014 publish the Shiki platform repo\n \ndiff --git a/scripts/guardian_approval_signal.py b/scripts/guardian_approval_signal.py\nindex d059f92b2c82aa2b5d6ca421365537a87886c1cb..16924892714f4f5b9303c0ef0e325a7e70075630\n--- a/scripts/guardian_approval_signal.py\n+++ b/scripts/guardian_approval_signal.py\n@@ -199,8 +199,8 @@\n ) -> Any:\n     \"\"\"Evaluate SADR-0015 Contract Approval for the CCA Guardian signal.\n \n-    The SAME OR that MergeGate applies must reach this deterministic signal: ADR\n-    0015 is explicit that without it, a contract-approved PR still returns\n+    The SAME OR that MergeGate applies must reach this deterministic signal:\n+    SADR-0015 is explicit that without it, a contract-approved PR still returns\n     ``needs_guardian``, the CCA job fails, and the MergeGate policy check never\n     runs \u2014 the gate this change moves would never be reached. This resolves the\n     task the SAME way MergeGate does (the first Shiki task id in the PR body),\ndiff --git a/scripts/shiki_contract_approval.py b/scripts/shiki_contract_approval.py\nindex 941bcb96fdd8b9a85bc0c4f0f04d5dad9d04839c..4a4b418f6527413161db82670bec857a043360e2\n--- a/scripts/shiki_contract_approval.py\n+++ b/scripts/shiki_contract_approval.py\n@@ -142,7 +142,7 @@\n     changed_files_status: Any,\n     registration: Any,\n ) -> ContractApprovalResult:\n-    \"\"\"Whether this PR carries an SADR-0015 Contract Approval.\n+    \"\"\"Whether this PR carries a SADR-0015 Contract Approval.\n \n     The four conditions are evaluated IN ORDER, each failure recorded as a\n     distinct reason string:\ndiff --git a/scripts/shiki_guardian.py b/scripts/shiki_guardian.py\nindex a4ecb79bbda55fd5c8b06adfae2eb99760a03c45..4dd99bbe714e26f421fa19abac7a34ae1b195ecc\n--- a/scripts/shiki_guardian.py\n+++ b/scripts/shiki_guardian.py\n@@ -12,7 +12,7 @@\n # The carry re-derives its proof from git in-process on every judging invocation;\n # it is never read from an artifact, so there is nothing to go stale or be forged.\n # shiki_sync_proof is the ONLY guardian-family module that runs git, and every one\n-# of its invocations is read-only and local (ADR: prove-from-git base sync).\n+# of its invocations is read-only and local (SADR: prove-from-git base sync).\n from shiki_sync_proof import verify_pure_base_sync\n \n GUARDIAN_POLICY_PATH = \".shiki/guardian-policy.json\"\ndiff --git a/scripts/shiki_guardian_review.py b/scripts/shiki_guardian_review.py\nindex dd0f104d457899e0ba5ff1e162011c1e90b6f92b..e10ef58c046324e7ea3a3392feb3048ca77414ce\n--- a/scripts/shiki_guardian_review.py\n+++ b/scripts/shiki_guardian_review.py\n@@ -159,7 +159,7 @@\n     docs_only = _is_docs_only(changed_files)\n     if docs_only and matched == 0:\n         focus.append(\n-            \"Docs/ADR-only change: confirm the change is genuinely documentation/decision-only, introduces no behavior or contract drift, and stays consistent with the constitution and existing ADRs.\"\n+            \"Docs/decision-record-only change: confirm the change is genuinely documentation/decision-only, introduces no behavior or contract drift, and stays consistent with the constitution and existing SADRs and target ADRs.\"\n         )\n \n     # Conservative augmentation: unknown (no category and not docs-only), mixed\ndiff --git a/scripts/shiki_installer.py b/scripts/shiki_installer.py\nindex be064ab7a29ff6836f7f7a1d70500a43e0d11d7b..9fd8eefd137ce7003dad5c0dcb2e48e2cd47ab32\n--- a/scripts/shiki_installer.py\n+++ b/scripts/shiki_installer.py\n@@ -118,6 +118,7 @@\n # what lets a target carry it without a governance-schema change.\n INSTALL_STAMP_PATH = \".shiki/install-stamp.json\"\n INSTALL_STAMP_VERSION = 1\n+LEGACY_SHIKI_SADR_MAX = 18\n \n # Under --force the shipped surface splits three ways, so nothing is lost and\n # nothing is silently stale.\n@@ -237,9 +238,98 @@\n         )\n \n \n+def legacy_shiki_adr_paths() -> tuple[str, ...]:\n+    \"\"\"Former numeric paths for Shiki SADR-0001 through SADR-0018.\n+\n+    The authoritative slugs come from the current SADR filenames. SADR-0019 is\n+    the namespace decision itself and never shipped under a numeric legacy path.\n+    \"\"\"\n+    paths: list[str] = []\n+    for sadr in sorted((ROOT / \"docs\" / \"adr\").glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\")):\n+        number = sadr.name.split(\"-\", 2)[1]\n+        if int(number) <= LEGACY_SHIKI_SADR_MAX:\n+            paths.append(f\"docs/adr/{sadr.name.removeprefix('SADR-')}\")\n+    return tuple(paths)\n+\n+\n+def preflight_legacy_sadr_cleanup(target: Path, *, force: bool) -> tuple[str, ...]:\n+    \"\"\"Authorize exact legacy Shiki ADR removals before the first write.\n+\n+    A non-force install never removes existing files. A forced install needs no\n+    stamp when no legacy Shiki path is present. When legacy paths do exist, each\n+    must be named in a readable install stamp and its current bytes must match\n+    the recorded digest.\n+    \"\"\"\n+    if not force:\n+        return ()\n+    present = tuple(\n+        relative\n+        for relative in legacy_shiki_adr_paths()\n+        if (target / relative).exists() or (target / relative).is_symlink()\n+    )\n+    if not present:\n+        return ()\n+\n+    stamp_path = target / INSTALL_STAMP_PATH\n+    try:\n+        raw = stamp_path.read_text(encoding=\"utf-8\")\n+        stamp = json.loads(raw)\n+    except FileNotFoundError:\n+        detail = \"install stamp is absent\"\n+        blockers = [f\"{relative}: {detail}\" for relative in present]\n+    except (OSError, UnicodeDecodeError) as error:\n+        detail = f\"install stamp is unreadable ({error})\"\n+        blockers = [f\"{relative}: {detail}\" for relative in present]\n+    except json.JSONDecodeError as error:\n+        detail = f\"install stamp is malformed ({error})\"\n+        blockers = [f\"{relative}: {detail}\" for relative in present]\n+    else:\n+        if not isinstance(stamp, dict):\n+            blockers = [\n+                f\"{relative}: install stamp is malformed (expected an object)\"\n+                for relative in present\n+            ]\n+        else:\n+            digests = stamp.get(\"digests\")\n+            blockers = []\n+            if not isinstance(digests, dict):\n+                blockers.extend(\n+                    f\"{relative}: install stamp has no digest for this path\"\n+                    for relative in present\n+                )\n+            else:\n+                for relative in present:\n+                    expected = digests.get(relative)\n+                    if not isinstance(expected, str) or not expected:\n+                        blockers.append(\n+                            f\"{relative}: install stamp has no digest for this path\"\n+                        )\n+                        continue\n+                    try:\n+                        actual = _sha256_file(target / relative)\n+                    except OSError as error:\n+                        blockers.append(\n+                            f\"{relative}: legacy path is unreadable ({error})\"\n+                        )\n+                        continue\n+                    if actual != expected:\n+                        blockers.append(\n+                            f\"{relative}: digest mismatch (stamp {expected}, current {actual})\"\n+                        )\n+\n+    if blockers:\n+        joined = \"\\n\".join(f\"- {blocker}\" for blocker in blockers)\n+        raise ShikiError(\n+            \"legacy Shiki ADR cleanup is not authorized:\\n\"\n+            f\"{joined}\\nNo files were written.\"\n+        )\n+    return present\n+\n+\n def install_template(target: Path, *, force: bool, validate: bool) -> list[NewFileNote]:\n     # Preconditions first: never rewrite the tree of a target that cannot be\n     # upgraded cleanly (see refuse_pending_migrations).\n+    legacy_cleanup = preflight_legacy_sadr_cleanup(target, force=force)\n     refuse_pending_migrations(target)\n \n     # Resolve the target's required CODEOWNERS owner once from its\n@@ -250,6 +340,10 @@\n     # verbatim. See ``codeowners_required_owner``.\n     codeowners_owner = codeowners_required_owner(target)\n \n+    for relative in legacy_cleanup:\n+        (target / relative).unlink()\n+        info(f\"removed stamped legacy Shiki ADR: {target / relative}\")\n+\n     notes: list[NewFileNote] = []\n     for relative in TEMPLATE_PATHS:\n         source = ROOT / relative\n@@ -647,6 +741,22 @@\n     digests = target_stamp_digests(target)\n     existing = read_install_stamp(target)\n     if existing is not None:\n+        # A non-force install deliberately leaves legacy Shiki ADRs in place.\n+        # Preserve only still-matching ownership proof so a later forced\n+        # upgrade can authorize cleanup; never carry a missing or stale digest.\n+        existing_digests = existing.get(\"digests\")\n+        if isinstance(existing_digests, dict):\n+            for relative in legacy_shiki_adr_paths():\n+                expected = existing_digests.get(relative)\n+                path = target / relative\n+                if not isinstance(expected, str) or not path.is_file():\n+                    continue\n+                try:\n+                    actual = _sha256_file(path)\n+                except OSError:\n+                    continue\n+                if actual == expected:\n+                    digests[relative] = actual\n         if _incoming_is_older(commit, existing.get(\"platform_commit\")):\n             warn(\n                 \"kept existing install stamp: incoming install \"\ndiff --git a/scripts/shiki_loop.py b/scripts/shiki_loop.py\nindex 3611641da8aeb5038b1da90e3114b09a473ec6d8..84caa491b9cad0edc4176f209c13e9221e975681\n--- a/scripts/shiki_loop.py\n+++ b/scripts/shiki_loop.py\n@@ -424,7 +424,7 @@\n         # unresolvable CCA verdict has already produced its terminal stop and is never\n         # laundered into a head-moving sync (SADR-0010). It also never fires while the\n         # policy (Guardian) gate is red \u2014 for ANY reason, not only when it is the sole\n-        # red check \u2014 so a head-bound Guardian approval, or an SADR-0015\n+        # red check \u2014 so a head-bound Guardian approval, or a SADR-0015\n         # contract-approval carry that base movement defeated (the policy gate compares\n         # base and head task governance directly), is never silently restored by\n         # adopting base wholesale. It is bounded by a durable per-task counter so the\ndiff --git a/scripts/shiki_tasks.py b/scripts/shiki_tasks.py\nindex aec3809f24b4a0c8c96fc50971cff9ae9ad30381..d531052e9d9717058d4631483cefab44c26a65c1\n--- a/scripts/shiki_tasks.py\n+++ b/scripts/shiki_tasks.py\n@@ -821,7 +821,7 @@\n         \"prompt\": prompt,\n         \"entry_skill\": \"grill-with-docs\",\n         \"required_next_steps\": [\n-            \"Run grill-with-docs until terminology, boundaries, risks, and ADR-worthy decisions are settled.\",\n+            \"Run grill-with-docs until terminology, boundaries, risks, and decision-record-worthy choices are settled.\",\n             \"Obtain the operator's explicit approval of the PRD/requirements (Spec Freeze).\",\n             \"Write a machine-readable plan JSON with grill_with_docs.status=complete and spec_freeze.status=frozen.\",\n             \"Run shiki plan ingest --plan-file PLAN.json.\",\ndiff --git a/scripts/test_shiki_goal_loop.sh b/scripts/test_shiki_goal_loop.sh\nindex 753c7b516e10f0be3bc92a4bd0eb1fa699776ae6..8f3820a0a079b3044424cfe1a6c6b7fe68896e67\n--- a/scripts/test_shiki_goal_loop.sh\n+++ b/scripts/test_shiki_goal_loop.sh\n@@ -231,8 +231,8 @@\n \n # High/critical risk merges autonomously when all required checks are green:\n # the \"MergeGate policy check\" required check IS the Guardian gate (it enforces\n-# guardian-policy.json \u2014 human approval OR external AI guardian review, ADR\n-# 0010), so green checks mean a recorded authority approved. The fake gh returns\n+# guardian-policy.json \u2014 human approval OR external AI guardian review,\n+# SADR-0010), so green checks mean a recorded authority approved. The fake gh returns\n # green checks, simulating that the Guardian gate passed.\n python3 \"$ROOT/scripts/shiki.py\" goal create --target \"$TARGET\" --title \"Guardian gate\" --outcome \"High risk merges via policy gate\" >/tmp/shiki-goal-loop-guardian-goal.json\n GGOAL=\"$(json_get /tmp/shiki-goal-loop-guardian-goal.json goal_id)\"\ndiff --git a/tests/test_adr_references.py b/tests/test_adr_references.py\nindex 51706329c797d5dd9b47e082a333fa83b261d482..d005da4f771f5e8085ec22d4b787980e68f5ae8c\n--- a/tests/test_adr_references.py\n+++ b/tests/test_adr_references.py\n@@ -1,8 +1,8 @@\n-\"\"\"Every ``ADR ####`` citation must resolve to exactly one decision record.\n+\"\"\"Every target ADR and Shiki SADR citation resolves in its own namespace.\n \n Background. ``scripts/mergegate_check.py`` justifies ``is_bookkeeping_closeout``\n-with \"ADR 0017\" nine times and ``scripts/guardian_approval_signal.py`` cites\n-0016/0017 seven times, yet for a stretch the numbering in ``docs/adr/`` stopped\n+with \"SADR-0017\" nine times and ``scripts/guardian_approval_signal.py`` cites\n+SADR-0016/SADR-0017 seven times, yet for a stretch the numbering in ``docs/adr/`` stopped\n at 0015: the authoring commits lived on a coordinator branch and no task ever\n declared ``docs/adr/...`` in its locks, so nothing carried the records to the\n default branch. The stated basis of an exemption that merged seven PRs with zero\n@@ -11,11 +11,12 @@\n and the directory drifted with no signal.\n \n This is the same unbound-pair shape behind other silent failures: a reference\n-and its target with nothing asserting they agree. This test binds them. It scans\n-every ``scripts/*.py`` (plus ``docs/**/*.md`` and the root ``*.md`` governance\n-files, which are cheap) for the ``ADR ####`` pattern and asserts each cited\n-number resolves to exactly one ``docs/adr/####-*.md``. The converse is\n-deliberately NOT required: an ADR may exist without being cited.\n+and its target with nothing asserting they agree. This test binds them. Target\n+``ADR NNNN`` citations resolve only to ``docs/adr/NNNN-*.md``; Shiki\n+``SADR-NNNN`` citations resolve only to ``docs/adr/SADR-NNNN-*.md``. The scan\n+covers current governance docs, scripts, workflows, prompts, packaging metadata,\n+and repo-local Codex guidance while deliberately excluding historical\n+``.shiki`` evidence. The converse is not required: a record may be uncited.\n \n The suite is dependency free: standard-library ``unittest`` only.\n \"\"\"\n@@ -31,17 +32,18 @@\n \n REPO_ROOT = Path(__file__).resolve().parents[1]\n \n-# \"ADR 0016\" -- a four-digit citation. The trailing \\b keeps a five-digit token\n-# from being read as a truncated four-digit number.\n-_CITATION = re.compile(r\"\\bADR (\\d{4})\\b\")\n-# \"0016-locks-bound-product-paths.md\" -- the leading four digits name the record.\n-_ADR_FILENAME = re.compile(r\"^(\\d{4})-.*\\.md$\")\n+# ``ADR 0016`` / ``SADR-0016`` -- a namespace plus four-digit citation. The\n+# trailing ``\\b`` keeps a five-digit token from being read as a truncated one.\n+_CITATION = re.compile(r\"\\b(S?ADR)[ -](\\d{4})\\b\")\n+# Target ADRs retain the numeric filename; Shiki platform decisions use SADR.\n+_DECISION_FILENAME = re.compile(r\"^(?:(SADR)-)?(\\d{4})-.*\\.md$\")\n \n \n class Violation(NamedTuple):\n     \"\"\"A citation that does not resolve to exactly one decision record.\"\"\"\n \n     citing_file: str  # repo-relative path of the file that cites the ADR\n+    namespace: str  # \"ADR\" for target decisions; \"SADR\" for Shiki decisions\n     number: str  # the cited four-digit number, e.g. \"0016\"\n     line: int  # 1-based line of the citation\n     reason: str  # \"missing\" (zero matches) or \"ambiguous\" (two or more)\n@@ -49,44 +51,59 @@\n \n     @property\n     def expected_path(self) -> str:\n-        return f\"docs/adr/{self.number}-*.md\"\n+        prefix = \"SADR-\" if self.namespace == \"SADR\" else \"\"\n+        return f\"docs/adr/{prefix}{self.number}-*.md\"\n \n     def describe(self) -> str:\n         \"\"\"Name the citing file, the cited number, and the missing path.\"\"\"\n         if self.reason == \"missing\":\n             return (\n-                f\"{self.citing_file}:{self.line} cites ADR {self.number} \"\n+                f\"{self.citing_file}:{self.line} cites {self.namespace} {self.number} \"\n                 f\"but no document matches {self.expected_path}\"\n             )\n         joined = \", \".join(self.matches)\n         return (\n-            f\"{self.citing_file}:{self.line} cites ADR {self.number} which \"\n+            f\"{self.citing_file}:{self.line} cites {self.namespace} {self.number} which \"\n             f\"resolves to {len(self.matches)} documents (expected exactly one \"\n             f\"at {self.expected_path}): {joined}\"\n         )\n \n \n-def _adr_index(root: Path) -> dict[str, list[Path]]:\n-    \"\"\"Map every four-digit ADR number to the record files that claim it.\"\"\"\n-    index: dict[str, list[Path]] = {}\n+def _adr_index(root: Path) -> dict[tuple[str, str], list[Path]]:\n+    \"\"\"Map each decision namespace and number to the files that claim it.\"\"\"\n+    index: dict[tuple[str, str], list[Path]] = {}\n     adr_dir = root / \"docs\" / \"adr\"\n     if adr_dir.is_dir():\n         for path in sorted(adr_dir.glob(\"*.md\")):\n-            match = _ADR_FILENAME.match(path.name)\n+            match = _DECISION_FILENAME.match(path.name)\n             if match:\n-                index.setdefault(match.group(1), []).append(path)\n+                namespace = \"SADR\" if match.group(1) else \"ADR\"\n+                index.setdefault((namespace, match.group(2)), []).append(path)\n     return index\n \n \n def _citing_files(root: Path, include_docs: bool = True) -> list[Path]:\n-    \"\"\"Files scanned for citations: scripts/*.py, and cheaply the markdown.\"\"\"\n+    \"\"\"Current, shipped text surfaces scanned for decision citations.\"\"\"\n     files = sorted((root / \"scripts\").glob(\"*.py\"))\n+    files += sorted((root / \"scripts\").glob(\"*.sh\"))\n     if include_docs:\n         docs = root / \"docs\"\n         if docs.is_dir():\n             files += sorted(docs.rglob(\"*.md\"))\n         files += sorted(root.glob(\"*.md\"))\n-    return files\n+        pyproject = root / \"pyproject.toml\"\n+        if pyproject.is_file():\n+            files.append(pyproject)\n+        for relative in (\".github\", \".codex\"):\n+            surface = root / relative\n+            if surface.is_dir():\n+                files += sorted(\n+                    path\n+                    for path in surface.rglob(\"*\")\n+                    if path.is_file()\n+                    and path.suffix in {\".md\", \".py\", \".sh\", \".toml\", \".yaml\", \".yml\"}\n+                )\n+    return sorted(set(files))\n \n \n def find_adr_reference_violations(\n@@ -94,10 +111,9 @@\n ) -> list[Violation]:\n     \"\"\"Return every ``ADR ####`` citation under ``root`` that fails to resolve.\n \n-    A citation resolves when exactly one ``docs/adr/####-*.md`` matches its\n-    number. Zero matches is a \"missing\" violation; two or more is \"ambiguous\".\n-    Records that are never cited produce nothing -- the converse is not\n-    required.\n+    A citation resolves when exactly one record in the same namespace matches\n+    its number. Zero matches is a \"missing\" violation; two or more is\n+    \"ambiguous\". Records that are never cited produce nothing.\n     \"\"\"\n     root = Path(root)\n     index = _adr_index(root)\n@@ -110,13 +126,15 @@\n         rel = path.relative_to(root).as_posix()\n         for lineno, line in enumerate(text.splitlines(), start=1):\n             for match in _CITATION.finditer(line):\n-                number = match.group(1)\n-                found = index.get(number, [])\n+                namespace, number = match.groups()\n+                found = index.get((namespace, number), [])\n                 if len(found) == 1:\n                     continue\n                 reason = \"missing\" if not found else \"ambiguous\"\n                 rels = [p.relative_to(root).as_posix() for p in found]\n-                violations.append(Violation(rel, number, lineno, reason, rels))\n+                violations.append(\n+                    Violation(rel, namespace, number, lineno, reason, rels)\n+                )\n     return violations\n \n \n@@ -129,8 +147,8 @@\n         self.assertEqual(\n             violations,\n             [],\n-            \"Every 'ADR ####' citation must resolve to exactly one \"\n-            f\"docs/adr/####-*.md:\\n{detail}\",\n+            \"Every ADR/SADR citation must resolve exactly once in its own \"\n+            f\"docs/adr namespace:\\n{detail}\",\n         )\n \n     def test_scan_actually_reads_scripts(self) -> None:\n@@ -142,12 +160,38 @@\n             \"the citation scan must cover scripts/*.py\",\n         )\n \n+    def test_platform_citations_do_not_use_unresolved_numeric_shorthand(self) -> None:\n+        shorthand = re.compile(r\"SADR-\\d{4}/\\d{4}\")\n+        offenders = []\n+        for path in _citing_files(REPO_ROOT):\n+            text = path.read_text(encoding=\"utf-8\")\n+            if shorthand.search(text):\n+                offenders.append(path.relative_to(REPO_ROOT).as_posix())\n+        self.assertEqual(\n+            offenders,\n+            [],\n+            \"repeat the SADR prefix so every cited number is independently resolvable\",\n+        )\n+\n     def test_carried_records_are_indexed(self) -> None:\n         # The two records this Goal carried onto the default branch, and which\n         # the scripts cite most, must be present.\n         index = _adr_index(REPO_ROOT)\n-        self.assertIn(\"0016\", index)\n-        self.assertIn(\"0017\", index)\n+        self.assertIn((\"SADR\", \"0016\"), index)\n+        self.assertIn((\"SADR\", \"0017\"), index)\n+\n+    def test_platform_tree_has_only_sadr_0001_through_0019(self) -> None:\n+        adr_dir = REPO_ROOT / \"docs\" / \"adr\"\n+        sadrs = sorted(adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\"))\n+        self.assertEqual(\n+            {path.name.split(\"-\", 2)[1] for path in sadrs},\n+            {f\"{number:04d}\" for number in range(1, 20)},\n+        )\n+        self.assertEqual(\n+            list(adr_dir.glob(\"[0-9][0-9][0-9][0-9]-*.md\")),\n+            [],\n+            \"the Shiki platform tree must not retain legacy numeric SADR paths\",\n+        )\n \n \n class FixtureAdrReferences(unittest.TestCase):\n@@ -180,6 +224,26 @@\n         self.assertIn(\"9991\", message)  # cited number\n         self.assertIn(\"docs/adr/9991-*.md\", message)  # missing path\n \n+    def test_target_adr_does_not_satisfy_missing_platform_sadr(self) -> None:\n+        root = self._make_repo(\n+            adr_names=[\"0042-target-decision.md\"],\n+            script_body='\"\"\"Target ADR 0042 exists, but platform SADR-0042 does not.\"\"\"\\n',\n+        )\n+        violations = find_adr_reference_violations(root, include_docs=False)\n+        self.assertEqual(len(violations), 1)\n+        self.assertEqual(violations[0].namespace, \"SADR\")\n+        self.assertEqual(violations[0].number, \"0042\")\n+        self.assertEqual(violations[0].expected_path, \"docs/adr/SADR-0042-*.md\")\n+\n+    def test_target_adr_and_platform_sadr_may_reuse_a_number(self) -> None:\n+        root = self._make_repo(\n+            adr_names=[\"0042-target-decision.md\", \"SADR-0042-platform-decision.md\"],\n+            script_body='\"\"\"Target ADR 0042 and platform SADR-0042 are distinct.\"\"\"\\n',\n+        )\n+        self.assertEqual(\n+            find_adr_reference_violations(root, include_docs=False), []\n+        )\n+\n     def test_uncited_adr_is_allowed(self) -> None:\n         # An ADR exists but nothing cites it -> not a violation. The converse\n         # (every ADR must be cited) is explicitly NOT required.\ndiff --git a/tests/test_guardian_review.py b/tests/test_guardian_review.py\nindex 88829e7810212635e2fc98aa5918d6f26294677d..00a85a237acd5bf2d6cb5b294128198c422402ca\n--- a/tests/test_guardian_review.py\n+++ b/tests/test_guardian_review.py\n@@ -162,7 +162,7 @@\n \n     def test_docs_only_is_light_and_not_conservative(self):\n         focus = gr.classify_review_focus([\"docs/adr/0099-x.md\", \"README.md\"], \"\", \"low\")\n-        self.assertTrue(any(\"Docs/ADR-only\" in f for f in focus))\n+        self.assertTrue(any(\"Docs/decision-record-only\" in f for f in focus))\n         self.assertFalse(any(\"conservative default\" in f.lower() for f in focus))\n \n     def test_unknown_category_is_conservative(self):\ndiff --git a/tests/test_policy_job_merged_prs.py b/tests/test_policy_job_merged_prs.py\nindex d45512e74681e5e649279236c7d4e79d5c3da471..b97989f8bd74d091de1a6b5275a20d8dc68c4512\n--- a/tests/test_policy_job_merged_prs.py\n+++ b/tests/test_policy_job_merged_prs.py\n@@ -3,8 +3,8 @@\n \n ``.github/workflows/shiki-cca-completion.yml`` has two consumers of the merged-PR\n proof: the CCA *signal* (``guardian_approval_signal.py`` in \"Prepare CCA context\")\n-and the authoritative gate (``mergegate_check.py`` in \"Enforce MergeGate\"). ADR\n-0017's bookkeeping-closeout exemption (condition 6) needs the merged implementation\n+and the authoritative gate (``mergegate_check.py`` in \"Enforce MergeGate\").\n+SADR-0017's bookkeeping-closeout exemption (condition 6) needs the merged implementation\n PR proven, and a closeout carries NO ``post_merge_reconcile`` marker. The signal\n already resolves ``MERGED_PRS`` unconditionally, but \"Enforce MergeGate\" used to\n resolve it only inside ``grep -q '<!-- shiki:post_merge_reconcile -->'``. So a\ndiff --git a/tests/test_sadr_install_migration.py b/tests/test_sadr_install_migration.py\nnew file mode 100644\nindex 0000000000000000000000000000000000000000..d51eac872b1c79ff4579c4c1401ca5d775a60d8d\n--- /dev/null\n+++ b/tests/test_sadr_install_migration.py\n@@ -0,0 +1,237 @@\n+\"\"\"SADR installation and collision-safe legacy upgrade behavior.\"\"\"\n+\n+from __future__ import annotations\n+\n+import contextlib\n+import hashlib\n+import io\n+import json\n+import shutil\n+import tempfile\n+import unittest\n+from pathlib import Path\n+\n+import shiki_test_support  # noqa: F401  (path bootstrap)\n+\n+import shiki_installer\n+from tests.test_adr_references import find_adr_reference_violations\n+\n+\n+PRODUCT_NUMBERS = tuple(range(13, 19))\n+\n+\n+def _quiet_install(target: Path, *, force: bool, validate: bool) -> None:\n+    with contextlib.redirect_stdout(io.StringIO()):\n+        shiki_installer.install_template(target, force=force, validate=validate)\n+\n+\n+def _sha256(path: Path) -> str:\n+    return hashlib.sha256(path.read_bytes()).hexdigest()\n+\n+\n+def _legacy_sadr_paths() -> tuple[str, ...]:\n+    return tuple(\n+        f\"docs/adr/{path.name.removeprefix('SADR-')}\"\n+        for path in sorted((shiki_installer.ROOT / \"docs\" / \"adr\").glob(\"SADR-*.md\"))\n+        if path.name != \"SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\"\n+    )\n+\n+\n+def _create_legacy_upgrade_fixture(\n+    target: Path,\n+) -> tuple[dict[str, bytes], tuple[str, ...]]:\n+    adr_dir = target / \"docs\" / \"adr\"\n+    adr_dir.mkdir(parents=True)\n+    product_bytes = {\n+        f\"{number:04d}-cubeshare-product-decision.md\": (\n+            f\"# CubeShare ADR {number:04d}\\n\\nDo not rewrite {number}.\\n\"\n+        ).encode()\n+        for number in PRODUCT_NUMBERS\n+    }\n+    for name, content in product_bytes.items():\n+        (adr_dir / name).write_bytes(content)\n+\n+    legacy_paths = _legacy_sadr_paths()\n+    for relative in legacy_paths:\n+        (target / relative).write_bytes(f\"legacy Shiki bytes for {relative}\\n\".encode())\n+\n+    stamp_path = target / shiki_installer.INSTALL_STAMP_PATH\n+    stamp_path.parent.mkdir(parents=True, exist_ok=True)\n+    stamp = {\n+        \"version\": shiki_installer.INSTALL_STAMP_VERSION,\n+        \"platform_commit\": None,\n+        \"installed_at\": \"2000-01-01T00:00:00+00:00\",\n+        \"digests\": {\n+            relative: _sha256(target / relative) for relative in legacy_paths\n+        },\n+    }\n+    stamp_path.write_text(\n+        json.dumps(stamp, indent=2, sort_keys=True) + \"\\n\", encoding=\"utf-8\"\n+    )\n+    return product_bytes, legacy_paths\n+\n+\n+def _tree_snapshot(root: Path) -> dict[str, tuple[str, bytes | str]]:\n+    \"\"\"Recursive byte/shape snapshot used to prove preflight is write-free.\"\"\"\n+    snapshot: dict[str, tuple[str, bytes | str]] = {}\n+    for path in sorted(root.rglob(\"*\")):\n+        relative = path.relative_to(root).as_posix()\n+        if path.is_symlink():\n+            snapshot[relative] = (\"symlink\", path.readlink().as_posix())\n+        elif path.is_dir():\n+            snapshot[relative] = (\"directory\", \"\")\n+        else:\n+            snapshot[relative] = (\"file\", path.read_bytes())\n+    return snapshot\n+\n+\n+class FreshSadrInstallTests(unittest.TestCase):\n+    def setUp(self) -> None:\n+        self.tmp = Path(tempfile.mkdtemp(prefix=\"shiki-sadr-fresh-\"))\n+        self.addCleanup(lambda: shutil.rmtree(self.tmp, ignore_errors=True))\n+        self.target = self.tmp / \"target\"\n+        self.adr_dir = self.target / \"docs\" / \"adr\"\n+        self.adr_dir.mkdir(parents=True)\n+        self.product_bytes = {\n+            f\"{number:04d}-product-decision.md\": (\n+                f\"# Product ADR {number:04d}\\n\\nTarget-owned bytes {number}.\\n\"\n+            ).encode()\n+            for number in PRODUCT_NUMBERS\n+        }\n+        for name, content in self.product_bytes.items():\n+            (self.adr_dir / name).write_bytes(content)\n+\n+    def test_fresh_install_preserves_product_adrs_and_installs_all_sadrs(self) -> None:\n+        _quiet_install(self.target, force=False, validate=True)\n+\n+        for name, expected in self.product_bytes.items():\n+            self.assertEqual((self.adr_dir / name).read_bytes(), expected)\n+\n+        sadr_numbers = {\n+            path.name.split(\"-\", 2)[1]\n+            for path in self.adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\")\n+        }\n+        self.assertEqual(sadr_numbers, {f\"{number:04d}\" for number in range(1, 20)})\n+\n+        numeric_records = {\n+            path.name\n+            for path in self.adr_dir.glob(\"[0-9][0-9][0-9][0-9]-*.md\")\n+        }\n+        self.assertEqual(numeric_records, set(self.product_bytes))\n+        stamp = json.loads(\n+            (self.target / shiki_installer.INSTALL_STAMP_PATH).read_text(\n+                encoding=\"utf-8\"\n+            )\n+        )\n+        for path in self.adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\"):\n+            relative = path.relative_to(self.target).as_posix()\n+            self.assertEqual(stamp[\"digests\"][relative], _sha256(path))\n+        self.assertEqual(find_adr_reference_violations(self.target), [])\n+\n+\n+class LegacySadrUpgradeTests(unittest.TestCase):\n+    def setUp(self) -> None:\n+        self.tmp = Path(tempfile.mkdtemp(prefix=\"shiki-sadr-upgrade-\"))\n+        self.addCleanup(lambda: shutil.rmtree(self.tmp, ignore_errors=True))\n+        self.target = self.tmp / \"target\"\n+        self.adr_dir = self.target / \"docs\" / \"adr\"\n+        self.product_bytes, self.legacy_paths = _create_legacy_upgrade_fixture(\n+            self.target\n+        )\n+\n+    def test_force_upgrade_deletes_only_stamped_legacy_sadrs(self) -> None:\n+        _quiet_install(self.target, force=True, validate=True)\n+\n+        for relative in self.legacy_paths:\n+            self.assertFalse((self.target / relative).exists(), relative)\n+        for name, expected in self.product_bytes.items():\n+            self.assertEqual((self.adr_dir / name).read_bytes(), expected)\n+\n+        sadrs = list(self.adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\"))\n+        self.assertEqual(len(sadrs), 19)\n+        refreshed = json.loads(\n+            (self.target / shiki_installer.INSTALL_STAMP_PATH).read_text(encoding=\"utf-8\")\n+        )\n+        for relative in self.legacy_paths:\n+            self.assertNotIn(relative, refreshed[\"digests\"])\n+        for path in sadrs:\n+            relative = path.relative_to(self.target).as_posix()\n+            self.assertEqual(refreshed[\"digests\"][relative], _sha256(path))\n+\n+    def test_non_force_install_never_deletes_legacy_files(self) -> None:\n+        before = {\n+            relative: (self.target / relative).read_bytes()\n+            for relative in self.legacy_paths\n+        }\n+        _quiet_install(self.target, force=False, validate=False)\n+        for relative, expected in before.items():\n+            self.assertEqual((self.target / relative).read_bytes(), expected)\n+        refreshed = json.loads(\n+            (self.target / shiki_installer.INSTALL_STAMP_PATH).read_text(encoding=\"utf-8\")\n+        )\n+        for relative in self.legacy_paths:\n+            self.assertEqual(refreshed[\"digests\"][relative], _sha256(self.target / relative))\n+\n+\n+class LegacyCleanupPreflightTests(unittest.TestCase):\n+    def setUp(self) -> None:\n+        self.tmp = Path(tempfile.mkdtemp(prefix=\"shiki-sadr-preflight-\"))\n+        self.addCleanup(lambda: shutil.rmtree(self.tmp, ignore_errors=True))\n+\n+    def test_all_ownership_blockers_abort_before_any_write(self) -> None:\n+        cases = (\n+            (\"absent\", \"install stamp is absent\"),\n+            (\"unreadable\", \"install stamp is unreadable\"),\n+            (\"malformed\", \"install stamp is malformed\"),\n+            (\"missing-digest\", \"install stamp has no digest\"),\n+            (\"digest-mismatch\", \"digest mismatch\"),\n+        )\n+        for name, expected_reason in cases:\n+            with self.subTest(name=name):\n+                target = self.tmp / name\n+                _, legacy_paths = _create_legacy_upgrade_fixture(target)\n+                stamp_path = target / shiki_installer.INSTALL_STAMP_PATH\n+\n+                if name == \"absent\":\n+                    stamp_path.unlink()\n+                elif name == \"unreadable\":\n+                    stamp_path.unlink()\n+                    stamp_path.mkdir()\n+                elif name == \"malformed\":\n+                    stamp_path.write_text(\"{not-json\\n\", encoding=\"utf-8\")\n+                else:\n+                    stamp = json.loads(stamp_path.read_text(encoding=\"utf-8\"))\n+                    if name == \"missing-digest\":\n+                        stamp[\"digests\"] = {}\n+                    else:\n+                        stamp[\"digests\"] = {\n+                            relative: \"deadbeef\" for relative in legacy_paths\n+                        }\n+                    stamp_path.write_text(\n+                        json.dumps(stamp, indent=2, sort_keys=True) + \"\\n\",\n+                        encoding=\"utf-8\",\n+                    )\n+\n+                before = _tree_snapshot(target)\n+                with self.assertRaises(shiki_installer.ShikiError) as raised:\n+                    _quiet_install(target, force=True, validate=False)\n+                after = _tree_snapshot(target)\n+\n+                self.assertEqual(before, after, f\"{name} preflight changed the tree\")\n+                message = str(raised.exception)\n+                self.assertIn(expected_reason, message)\n+                self.assertIn(\"No files were written\", message)\n+                for relative in legacy_paths:\n+                    self.assertIn(relative, message)\n+\n+    def test_absent_stamp_is_allowed_when_no_legacy_path_exists(self) -> None:\n+        target = self.tmp / \"no-legacy\"\n+        target.mkdir()\n+        _quiet_install(target, force=True, validate=False)\n+        self.assertTrue(\n+            (target / \"docs\" / \"adr\" / \"SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\").is_file()\n+        )\n+\n+\n+if __name__ == \"__main__\":\n+    unittest.main()\n\nexec\n/bin/zsh -lc \"sed -n '1,90p' scripts/shiki_installer.py && sed -n '330,390p' scripts/shiki_installer.py && sed -n '610,780p' scripts/shiki_installer.py\" in /private/tmp/shiki-adr-collision.pa0KwY/.worktrees/shiki-t-20260807t073307221254z-2d22d654-introduc\n succeeded in 0ms:\n#!/usr/bin/env python3\n\"\"\"Template copy, manifest staging, and local/global install helpers.\"\"\"\n\nfrom __future__ import annotations\n\nfrom dataclasses import dataclass, field\nimport hashlib\nimport json\nfrom pathlib import Path\nimport shutil\nimport subprocess\nfrom typing import Any\n\nfrom shiki_config import load_shiki_config\nfrom shiki_contracts import CODEOWNERS_PATH, CODEOWNERS_REQUIRED_OWNER, codeowners_required_owner\nfrom shiki_manifest import load_manifest, manifest_create_directories, manifest_directories, manifest_exclude_from_commit, manifest_install_include\nfrom shiki_migrations import migration_status\nfrom shiki_process import ROOT, ShikiError, info, warn, validate_target_shiki, load_default_config, utc_now\n\nTEMPLATE_PATHS = [\n    \"bin/shiki\",\n    \"AGENTS.md\",\n    \"CLAUDE.md\",\n    \"CONTEXT.md\",\n    \"SYSTEM_PROMPT.md\",\n    \".gitignore\",\n    \".claude/commands/shiki.md\",\n    \".codex/skills/shiki/SKILL.md\",\n    \".shiki\",\n    \".github/ISSUE_TEMPLATE\",\n    \".github/CODEOWNERS\",\n    \".github/PULL_REQUEST_TEMPLATE\",\n    \".github/prompts\",\n    \".github/workflows/shiki-validate.yml\",\n    \".github/workflows/shiki-claude-review.yml\",\n    \".github/workflows/shiki-cca-completion.yml\",\n    \".github/workflows/shiki-mergegate.yml\",\n    \".github/workflows/shiki-orchestrator.yml\",\n    \"docs/agents\",\n    \"docs/adr\",\n    \"skills/engineering\",\n    \"tests\",\n    \"scripts/shiki_schema.py\",\n    \"scripts/validate_shiki.py\",\n    \"scripts/shiki_contracts.py\",\n    \"scripts/shiki_jsonschema.py\",\n    \"scripts/shiki_evidence.py\",\n    \"scripts/shiki_locks.py\",\n    \"scripts/shiki_loop.py\",\n    \"scripts/shiki_memory.py\",\n    \"scripts/shiki_manifest.py\",\n    \"scripts/shiki_migrations.py\",\n    \"scripts/shiki_provider.py\",\n    \"scripts/shiki_workflows.py\",\n    \"scripts/enforce_cca_verdict.py\",\n    \"scripts/cca_verdict_usable.py\",\n    \"scripts/build_cca_evidence_manifest.py\",\n    \"scripts/guardian_approval_signal.py\",\n    \"scripts/mergegate_check.py\",\n    \"scripts/shiki_sync_proof.py\",\n    \"scripts/shiki.py\",\n    \"scripts/shiki_bootstrap.py\",\n    \"scripts/shiki_cli.py\",\n    \"scripts/shiki_config.py\",\n    \"scripts/shiki_contract_approval.py\",\n    \"scripts/shiki_doctor.py\",\n    \"scripts/shiki_git.py\",\n    \"scripts/shiki_github.py\",\n    \"scripts/shiki_guardian.py\",\n    \"scripts/shiki_guardian_review.py\",\n    \"scripts/shiki_guardian_status.py\",\n    \"scripts/shiki_installer.py\",\n    \"scripts/shiki_process.py\",\n    \"scripts/shiki_runtime.py\",\n    \"scripts/shiki_runtime_adapters.py\",\n    \"scripts/shiki_runtime_registry.py\",\n    \"scripts/shiki_session_lease.py\",\n    \"scripts/shiki_state_classes.py\",\n    \"scripts/shiki_tasks.py\",\n    \"scripts/shiki_state.py\",\n    \"scripts/test_shiki_init.sh\",\n    \"scripts/test_shiki_control_plane.sh\",\n    \"scripts/test_shiki_run_orchestrator.sh\",\n    \"scripts/test_shiki_daemon_runner.sh\",\n    \"scripts/test_shiki_runner_codex.sh\",\n    \"scripts/test_shiki_runner_claude.sh\",\n    \"scripts/test_shiki_goal_loop.sh\",\n    \"scripts/test_shiki_memory_loop.sh\",\n    \"scripts/test_shiki_code_review_gate.sh\",\n    \"scripts/test_shiki_start.sh\",\n    # Preconditions first: never rewrite the tree of a target that cannot be\n    # upgraded cleanly (see refuse_pending_migrations).\n    legacy_cleanup = preflight_legacy_sadr_cleanup(target, force=force)\n    refuse_pending_migrations(target)\n\n    # Resolve the target's required CODEOWNERS owner once from its\n    # ``.shiki/repo.json`` (present when a target already knows its GitHub\n    # identity). The shipped ``.github/CODEOWNERS`` names this maintainer on every\n    # line, so a foreign target's copy is rewritten to name its own owner; when no\n    # owner can be resolved this is the documented fallback and the file is copied\n    # verbatim. See ``codeowners_required_owner``.\n    codeowners_owner = codeowners_required_owner(target)\n\n    for relative in legacy_cleanup:\n        (target / relative).unlink()\n        info(f\"removed stamped legacy Shiki ADR: {target / relative}\")\n\n    notes: list[NewFileNote] = []\n    for relative in TEMPLATE_PATHS:\n        source = ROOT / relative\n        if not source.exists():\n            warn(f\"template path missing, skipped: {relative}\")\n            continue\n        copy_path(\n            source,\n            target / relative,\n            force=force,\n            target_install=True,\n            notes=notes,\n            codeowners_owner=codeowners_owner,\n        )\n\n    manifest = load_manifest(ROOT)\n    directories = manifest_directories(manifest)\n    for relative in manifest_create_directories(manifest):\n        state_dir = target / relative\n        state_dir.mkdir(parents=True, exist_ok=True)\n        metadata = directories.get(relative, {})\n        if metadata.get(\"tracked\") is True and metadata.get(\"required\") is True and not any(state_dir.iterdir()):\n            (state_dir / \".gitkeep\").write_text(\"\", encoding=\"utf-8\")\n        info(f\"ensured empty state directory: {state_dir}\")\n\n    # Stamp the install after the shipped surface is on disk, so the digests\n    # describe exactly what was written. Refreshed on every install and upgrade.\n    # The stamp stays untracked local state, so keep it out of git first.\n    _ensure_stamp_gitignored(target)\n    write_install_stamp(target)\n\n    # A single explicit summary at the end, so a half-upgrade is visible rather\n    # than inferred. Printed before validation so the .new files are always\n    # reported even if a later gate fails.\n    print_new_file_summary(notes)\n\n    if validate:\n        validate_target_shiki(target)\n\n    return notes\n\n\ndef print_new_file_summary(notes: list[NewFileNote]) -> None:\n    if not notes:\n    return (\n        relative not in PROJECT_CONTENT_FILES\n        and relative not in GOVERNANCE_CONTRACT_FILES\n        and relative not in PRESERVE_OUTRIGHT_FILES\n    )\n\n\ndef shipped_stamp_paths() -> list[str]:\n    \"\"\"Relative POSIX paths of every platform-owned file an install writes.\n\n    Enumerated from the platform (``ROOT``) with the same skip rules install\n    uses, so the set names exactly the shipped surface that a target must match.\n    \"\"\"\n    shipped: list[str] = []\n\n    def collect(source: Path) -> None:\n        if should_skip(source, target_install=True):\n            return\n        if source.is_dir():\n            for child in sorted(source.iterdir()):\n                collect(child)\n            return\n        relative = template_relative(source)\n        if relative is None or not _is_stamped_shipped(relative):\n            return\n        shipped.append(relative)\n\n    for relative in TEMPLATE_PATHS:\n        source = ROOT / relative\n        if source.exists():\n            collect(source)\n    return sorted(set(shipped))\n\n\ndef target_stamp_digests(target: Path) -> dict[str, str]:\n    \"\"\"Content digest for every stamped shipped path present in ``target``.\"\"\"\n    return {\n        relative: _sha256_file(target / relative)\n        for relative in shipped_stamp_paths()\n        if (target / relative).is_file()\n    }\n\n\ndef platform_commit(root: Path = ROOT) -> str | None:\n    \"\"\"The git HEAD of the running platform checkout, or None when unavailable.\"\"\"\n    result = subprocess.run(\n        [\"git\", \"-C\", str(root), \"rev-parse\", \"HEAD\"],\n        capture_output=True,\n        text=True,\n        check=False,\n    )\n    commit = result.stdout.strip()\n    return commit if result.returncode == 0 and commit else None\n\n\ndef commit_is_ancestor(root: Path, ancestor: str, descendant: str) -> bool | None:\n    \"\"\"True if ``ancestor`` is an ancestor of ``descendant`` in ``root``'s git.\n\n    None when the answer cannot be determined (git missing, or a commit unknown\n    to this checkout \u2014 which is itself the \"different lineage\" signal).\n    \"\"\"\n    if not ancestor or not descendant:\n        return None\n    result = subprocess.run(\n        [\"git\", \"-C\", str(root), \"merge-base\", \"--is-ancestor\", ancestor, descendant],\n        capture_output=True,\n        text=True,\n        check=False,\n    )\n    if result.returncode == 0:\n        return True\n    if result.returncode == 1:\n        return False\n    return None\n\n\ndef _incoming_is_older(incoming_commit: str | None, existing_commit: str | None) -> bool:\n    \"\"\"Whether a fresh install is provably older than the recorded stamp.\n\n    Only a *proven* older lineage \u2014 the incoming platform commit is a strict\n    ancestor of the recorded one \u2014 blocks the overwrite. Same commit, unrelated\n    lineages, or an undeterminable relationship all refresh, so the guard never\n    silently keeps stale content it cannot prove is newer.\n    \"\"\"\n    if not incoming_commit or not existing_commit or incoming_commit == existing_commit:\n        return False\n    return commit_is_ancestor(ROOT, incoming_commit, existing_commit) is True\n\n\ndef _ensure_stamp_gitignored(target: Path) -> None:\n    \"\"\"Keep the untracked install stamp out of ``git add`` / ``git add -A``.\n\n    The stamp is target-local state that is never committed: validate_shiki\n    rejects an unknown *tracked* ``.shiki/`` path, and the goal loop stages\n    worktrees with ``git add -A``, so an un-ignored stamp would be committed and\n    make the target's own Validate check permanently red. The shipped\n    ``.gitignore`` is platform-owned and cannot name it, so the installer\n    ensures the target's own ``.gitignore`` ignores it. Idempotent: a re-install\n    never appends the entry twice.\n    \"\"\"\n    gitignore = target / \".gitignore\"\n    existing = gitignore.read_text(encoding=\"utf-8\") if gitignore.is_file() else \"\"\n    if INSTALL_STAMP_PATH in {line.strip() for line in existing.splitlines()}:\n        return\n    prefix = \"\" if not existing or existing.endswith(\"\\n\") else \"\\n\"\n    addition = (\n        f\"{prefix}\\n# Local-only install version stamp (see `shiki doctor` drift checks)\\n\"\n        f\"{INSTALL_STAMP_PATH}\\n\"\n    )\n    with gitignore.open(\"a\", encoding=\"utf-8\") as handle:\n        handle.write(addition)\n\n\ndef read_install_stamp(target: Path) -> dict[str, Any] | None:\n    \"\"\"Parse a target's install stamp, or None when absent/unreadable.\"\"\"\n    path = target / INSTALL_STAMP_PATH\n    try:\n        data = json.loads(path.read_text(encoding=\"utf-8\"))\n    except (OSError, json.JSONDecodeError):\n        return None\n    return data if isinstance(data, dict) else None\n\n\ndef write_install_stamp(target: Path) -> None:\n    \"\"\"Write (or refresh) the target's install stamp.\n\n    Never overwrites a stamp from a newer platform commit with older content,\n    and leaves an already-current stamp byte-for-byte untouched so a no-op\n    re-install churns nothing.\n    \"\"\"\n    commit = platform_commit(ROOT)\n    digests = target_stamp_digests(target)\n    existing = read_install_stamp(target)\n    if existing is not None:\n        # A non-force install deliberately leaves legacy Shiki ADRs in place.\n        # Preserve only still-matching ownership proof so a later forced\n        # upgrade can authorize cleanup; never carry a missing or stale digest.\n        existing_digests = existing.get(\"digests\")\n        if isinstance(existing_digests, dict):\n            for relative in legacy_shiki_adr_paths():\n                expected = existing_digests.get(relative)\n                path = target / relative\n                if not isinstance(expected, str) or not path.is_file():\n                    continue\n                try:\n                    actual = _sha256_file(path)\n                except OSError:\n                    continue\n                if actual == expected:\n                    digests[relative] = actual\n        if _incoming_is_older(commit, existing.get(\"platform_commit\")):\n            warn(\n                \"kept existing install stamp: incoming install \"\n                f\"({commit}) is older than the recorded platform commit \"\n                f\"({existing.get('platform_commit')}); not overwriting {INSTALL_STAMP_PATH}\"\n            )\n            return\n        if existing.get(\"platform_commit\") == commit and existing.get(\"digests\") == digests:\n            return\n    stamp = {\n        \"version\": INSTALL_STAMP_VERSION,\n        \"platform_commit\": commit,\n        \"installed_at\": utc_now(),\n        \"digests\": digests,\n    }\n    path = target / INSTALL_STAMP_PATH\n    path.parent.mkdir(parents=True, exist_ok=True)\n    path.write_text(json.dumps(stamp, indent=2, sort_keys=True) + \"\\n\", encoding=\"utf-8\")\n    info(f\"wrote install stamp: {path}\")\n\n\n\ndiff --git a/.codex/skills/shiki/SKILL.md b/.codex/skills/shiki/SKILL.md\nindex c8e2e85d9d75d49cc4f2d6286ee22659c4e53e75..93cded623b2c15d9f65c76491a1c2d813ac9e6c0\n--- a/.codex/skills/shiki/SKILL.md\n+++ b/.codex/skills/shiki/SKILL.md\n@@ -18,6 +18,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n Codex CLI does not register this skill as a custom `/shiki` slash command. When\n the user types `/shiki` in Codex CLI and gets \"Unrecognized command\", explain\n that this is expected: invoke Shiki in Codex with natural language such as\n@@ -69,7 +72,7 @@\n \n ## Responsibilities\n \n-- Claude Code implements and repairs by default (`shiki runner claude`, ADR 0008); Codex implements and repairs tasks explicitly assigned to `codex`.\n+- Claude Code implements and repairs by default (`shiki runner claude`, SADR-0008); Codex implements and repairs tasks explicitly assigned to `codex`.\n - Claude Code Action can act as GitHub-side CCA or reviewer.\n - CCA judges completion.\n - MergeGate authorizes state transitions and merge readiness.\ndiff --git a/.github/prompts/cca-completion-check.md b/.github/prompts/cca-completion-check.md\nindex ed203ad5f302ecc81372d8ab27d2b1232ca97497..fd89950528333ef6242e6d5443904f5392ab7e56\n--- a/.github/prompts/cca-completion-check.md\n+++ b/.github/prompts/cca-completion-check.md\n@@ -19,6 +19,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n ## Required Reading\n \n Read only the files needed to judge the PR contract. Prefer this order:\ndiff --git a/.github/prompts/codex-task-handoff.md b/.github/prompts/codex-task-handoff.md\nindex 695e211d76dea1c60e8d5bb360e78487808286a4..59441d576b01a39569722f226c854e5a740e9eb5\n--- a/.github/prompts/codex-task-handoff.md\n+++ b/.github/prompts/codex-task-handoff.md\n@@ -2,7 +2,7 @@\n \n You are Codex Front acting as the Implementer for a Shiki task.\n \n-Follow `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant ADRs, and `docs/agents/codex-handoff.md`.\n+Follow `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant Shiki SADRs and target ADRs, and `docs/agents/codex-handoff.md`.\n \n Rules:\n \ndiff --git a/AGENTS.md b/AGENTS.md\nindex 7f041100d49c2a7525f51cb0f5d696a4a6d1c78c..a06bfbabc6fa48a007389e25e12d7ed523936bed\n--- a/AGENTS.md\n+++ b/AGENTS.md\n@@ -22,7 +22,7 @@\n - **Codex Front** is an optional implementer through Codex App, Codex CLI, Codex IDE extension, or Codex Web signed in with ChatGPT OAuth/subscription auth, dispatched with `shiki runner codex` when a task is explicitly assigned to `codex`.\n - **GitHub CCA** is implemented by Claude Code Action by default, using `CLAUDE_CODE_OAUTH_TOKEN`.\n \n-Do not assume `openai/codex-action`, `OPENAI_API_KEY`, or API-key based Codex automation in the default Shiki path. API-key runners are explicit target-repo extensions and require their own ADR.\n+Do not assume `openai/codex-action`, `OPENAI_API_KEY`, or API-key based Codex automation in the default Shiki path. API-key runners are explicit target-repo extensions and require their own target ADR.\n \n Shiki is not:\n \n@@ -44,10 +44,14 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use\n+`SADR-NNNN-*.md`; target product decisions retain `NNNN-*.md` and the `ADR`\n+namespace.\n+\n Use the canonical source-of-truth order above. Runtime-specific reading orders\n are operational guidance only and must not override or reorder it.\n \n-Conversation state is not durable truth. If a decision matters, put it in GitHub, `.shiki/`, `CONTEXT.md`, or an ADR.\n+Conversation state is not durable truth. If a decision matters, put it in GitHub, `.shiki/`, `CONTEXT.md`, a Shiki SADR, or a target ADR.\n \n If GitHub and `.shiki/` disagree, surface the conflict and prefer GitHub operational state until the mirror is repaired.\n \n@@ -75,8 +79,8 @@\n Every non-trivial change follows this loop:\n \n 1. **Goal**: clarify outcome, completion conditions, non-goals, risk level, and success signals.\n-2. **grill-with-docs**: challenge the plan against domain docs, ADRs, code reality, terminology, and edge scenarios.\n-3. **Context & Impact**: find relevant docs, ADRs, code, dependencies, owners, locks, and verification surfaces.\n+2. **grill-with-docs**: challenge the plan against domain docs, Shiki SADRs, target ADRs, code reality, terminology, and edge scenarios.\n+3. **Context & Impact**: find relevant docs, SADRs, target ADRs, code, dependencies, owners, locks, and verification surfaces.\n 4. **PRD and Spec Freeze**: use `to-prd` when context is settled enough to become durable product/engineering intent. The operator's explicit approval of the PRD is Spec Freeze: the plan records a `spec_freeze` block, and no plan runs without it. Steps 1-4 form Requirements Definition \u2014 one continuous operator dialogue. After Spec Freeze, scope changes require an operator-approved Spec Amendment; non-scope-moving interpretations go to the Assumption Log.\n 5. **Issues**: use `to-issues` to create independently grabbable vertical-slice issues. Prefer AFK slices over HITL slices where possible.\n 6. **Triage**: label issues for readiness, risk, runtime, skills, and MergeGate state.\n@@ -167,7 +171,7 @@\n - Testing decisions.\n - Out-of-scope items.\n - Further notes.\n-- Links to relevant domain terms and ADRs.\n+- Links to relevant domain terms, SADRs, and target ADRs.\n \n Every executable task must include:\n \n@@ -193,7 +197,7 @@\n \n Context & Impact must identify:\n \n-- Relevant documents, domain terms, ADRs, and past decisions.\n+- Relevant documents, domain terms, SADRs, target ADRs, and past decisions.\n - Relevant modules, interfaces, seams, callers, owners, and tests.\n - Dependency relationships between tasks.\n - Candidate locks and lock conflicts.\n@@ -210,7 +214,7 @@\n | Skill | Required when |\n | --- | --- |\n | `setup-matt-pocock-skills` | First configuring the repo for skills, or when issue tracker, triage labels, or domain docs are missing. |\n-| `grill-with-docs` | Default for non-trivial Goals before PRD/issues; always required when plans, terminology, boundaries, tradeoffs, or ADR-worthy decisions are ambiguous. |\n+| `grill-with-docs` | Default for non-trivial Goals before PRD/issues; always required when plans, terminology, boundaries, tradeoffs, or decision-record-worthy choices are ambiguous. |\n | `zoom-out` | The agent lacks an architectural map of the relevant code area. |\n | `to-prd` | Settled Goal context must become durable product/engineering intent. |\n | `to-issues` | A Goal, PRD, or plan must become independently grabbable vertical-slice issues. |\n@@ -348,7 +352,7 @@\n - Three or more new files.\n - Any high-risk or critical label.\n \n-When the architecture gate triggers, run `grill-with-docs` or `improve-codebase-architecture` as appropriate and record durable decisions in an ADR when needed.\n+When the architecture gate triggers, run `grill-with-docs` or `improve-codebase-architecture` as appropriate and record durable decisions in a Shiki SADR or target ADR as applicable.\n \n ## Engineering Discipline\n \ndiff --git a/CLAUDE.md b/CLAUDE.md\nindex 98a932e7841a632866fcd53b0bad0e3af3711d3d..98a31399404d6fb044b4fd493f4459ac87f439de\n--- a/CLAUDE.md\n+++ b/CLAUDE.md\n@@ -14,6 +14,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n ## Claude Role\n \n Default to planner, default implementer, reviewer, coordinator, documentation editor, CCA-style completion judge, and final-judgment assistant.\n@@ -57,7 +60,7 @@\n At the start of a Shiki session:\n \n 1. Read `AGENTS.md`.\n-2. Read `CONTEXT.md` and relevant ADRs.\n+2. Read `CONTEXT.md` and relevant Shiki SADRs and target ADRs.\n 3. Read active `.shiki/` Goal/task/ledger state when present.\n 4. Read active distilled rules from `.shiki/memories/` (status `distilled`, `active: true`, not revoked or superseded) for applicable guidance; the same rules are injected deterministically into task handoffs as the `## Distilled Rules` section (\u00a73.5).\n 5. Read the GitHub Issue or PR when the task is GitHub-backed.\n@@ -74,7 +77,7 @@\n - Define the Goal, completion conditions, non-goals, and risk level.\n - Treat `grill-with-docs` as the default planning entry point for non-trivial Goals.\n - Challenge domain terms against `CONTEXT.md`.\n-- Challenge hard-to-reverse tradeoffs against ADRs.\n+- Challenge hard-to-reverse tradeoffs against SADRs and target ADRs.\n - Explore code instead of asking when code can answer the question.\n - Run Context & Impact before implementation planning.\n - Produce a Task DAG, not an unordered checklist.\n@@ -94,7 +97,7 @@\n - For each question, provide your recommended answer.\n - Resolve dependency decisions before downstream design questions.\n - Update `CONTEXT.md` inline when terminology crystallizes.\n-- Offer ADRs only when the decision is hard to reverse, surprising without context, and a real tradeoff.\n+- Offer a SADR or target ADR only when the decision is hard to reverse, surprising without context, and a real tradeoff.\n - Convert unresolved questions into HITL blockers rather than allowing Codex to guess.\n \n ## PRD and Issues Standard\n@@ -103,7 +106,7 @@\n \n - Use the project's domain glossary vocabulary.\n - Avoid volatile file paths unless they are essential.\n-- Include implementation decisions, testing decisions, out-of-scope items, and relevant ADR/domain links.\n+- Include implementation decisions, testing decisions, out-of-scope items, and relevant SADR/target-ADR/domain links.\n - Identify deep-module opportunities and testable interfaces.\n \n When creating issues:\n@@ -121,7 +124,7 @@\n - Branch/worktree target.\n - Scope and non-goals.\n - Dependencies and locks.\n-- Relevant docs, ADRs, modules, and tests.\n+- Relevant docs, SADRs, target ADRs, modules, and tests.\n - Required skill invocations.\n - TDD expectations.\n - Pre-PR `code-review` gate expectations.\n@@ -152,7 +155,7 @@\n \n - Treat yourself as a judge, not implementer.\n - Do not write production code.\n-- Read `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant ADRs, PRD, task issue, PR body, diff, checks, reviews, labels, and `.shiki/` state.\n+- Read `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant SADRs and target ADRs, PRD, task issue, PR body, diff, checks, reviews, labels, and `.shiki/` state.\n - Evaluate every applicable checklist in `docs/agents/checklists.md`.\n - Map each acceptance criterion to durable evidence.\n - Distinguish implementation failure from missing evidence.\ndiff --git a/CONTEXT.md b/CONTEXT.md\nindex e61a9be06a0cce0d026484083a657bde2067e234..237ee8aa3ea8fab334ece49768bc5ed3b5af0974\n--- a/CONTEXT.md\n+++ b/CONTEXT.md\n@@ -25,7 +25,7 @@\n _Avoid_: jumping directly into code\n \n **grill-with-docs**:\n-A planning skill that challenges a plan against domain language, ADRs, code reality, and concrete edge scenarios. It resolves design-tree questions before PRD/issues. It runs inside Requirements Definition.\n+A planning skill that challenges a plan against domain language, Shiki SADRs, target ADRs, code reality, and concrete edge scenarios. It resolves design-tree questions before PRD/issues. It runs inside Requirements Definition.\n _Avoid_: generic brainstorming, silent assumptions\n \n **Requirements Definition**:\ndiff --git a/CONTRIBUTING.md b/CONTRIBUTING.md\nindex c6d9b656e73abb8a55e5f660123e03153dcb3e6d..9e37a8fbf668db5b22461aaae5548b8f1354394c\n--- a/CONTRIBUTING.md\n+++ b/CONTRIBUTING.md\n@@ -17,7 +17,7 @@\n 1. **Goal** \u2014 start from a user-approved Goal with an outcome, completion\n    conditions, non-goals, risk level, and success signals.\n 2. **grill-with-docs** \u2014 for any non-trivial change, challenge the plan against\n-   domain language, ADRs, and code reality before writing a PRD.\n+   domain language, Shiki SADRs, target ADRs, and code reality before writing a PRD.\n 3. **PRD** \u2014 capture settled product/engineering intent (`to-prd`).\n 4. **Spec Freeze** \u2014 the operator explicitly approves the PRD/requirements;\n    the plan records a `spec_freeze` block and no plan runs without it\n@@ -94,7 +94,7 @@\n   entries.\n - Do not hand-edit `.shiki/goals/*.json` or `.shiki/manifest.json` in a task PR.\n - Conversation state is not durable truth. If a decision matters, record it in\n-  GitHub, `.shiki/`, `CONTEXT.md`, or an ADR.\n+  GitHub, `.shiki/`, `CONTEXT.md`, a Shiki SADR, or a target ADR.\n \n ## Architecture Gate\n \n@@ -103,7 +103,7 @@\n or CI policy, package structure, three or more structural files, more than 100\n net new lines in a core module, three or more new files, or any high-risk label.\n When triggered, run `grill-with-docs` or `improve-codebase-architecture` and\n-record durable decisions in an ADR when needed (see `docs/adr/`).\n+record durable decisions in a Shiki SADR or target ADR as applicable (see `docs/adr/`).\n \n ## Safety\n \ndiff --git a/SYSTEM_PROMPT.md b/SYSTEM_PROMPT.md\nindex aaaea45e3a896c3a44ba89eec8bb84325ecf6c62..da009c86a88285269dee0155e58e550c6d744ea9\n--- a/SYSTEM_PROMPT.md\n+++ b/SYSTEM_PROMPT.md\n@@ -30,6 +30,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n Use the canonical source-of-truth order above. Runtime-specific reading orders\n are operational guidance only and must not override or reorder it. The current\n conversation is non-durable operator input.\n@@ -48,14 +51,14 @@\n \n A runtime may only do work that is explicitly assigned by the Goal, task, PR, or MergeGate decision.\n \n-Do not assume `openai/codex-action`, `OPENAI_API_KEY`, or API-key based Codex automation in the default Shiki path. API-key runners are explicit target-repo extensions and require their own ADR.\n+Do not assume `openai/codex-action`, `OPENAI_API_KEY`, or API-key based Codex automation in the default Shiki path. API-key runners are explicit target-repo extensions and require their own target ADR.\n \n ## 4. Non-negotiable Shiki flow\n \n For any non-trivial Goal, use this flow:\n \n 1. **Goal Seek**: define outcome, non-goals, risk level, completion criteria, and evidence requirements.\n-2. **grill-with-docs**: challenge the plan against `CONTEXT.md`, ADRs, code reality, terminology, boundaries, and edge scenarios. Ask one question at a time when the operator is available. If code can answer a question, inspect code instead of asking.\n+2. **grill-with-docs**: challenge the plan against `CONTEXT.md`, Shiki SADRs, target ADRs, code reality, terminology, boundaries, and edge scenarios. Ask one question at a time when the operator is available. If code can answer a question, inspect code instead of asking.\n 3. **Context & Impact**: identify modules, interfaces, seams, callers, dependencies, locks, risk, and verification surfaces.\n 4. **to-prd + Spec Freeze**: turn settled context into a PRD using domain vocabulary and testing decisions. The operator's explicit approval of the PRD is Spec Freeze, recorded as the plan's `spec_freeze` block; plans without it do not run. Steps 1-4 form Requirements Definition.\n 5. **to-issues**: decompose the PRD into independently grabbable vertical-slice issues. Prefer AFK slices; mark HITL slices when judgment is still required.\n@@ -75,7 +78,7 @@\n Before making any material change:\n \n 1. Read `AGENTS.md` and any runtime-specific wrapper such as `CLAUDE.md`.\n-2. Read `CONTEXT.md` and relevant ADRs.\n+2. Read `CONTEXT.md` and relevant SADRs and target ADRs.\n 3. Read the GitHub Issue / PR and `.shiki/` state for the active Goal or task.\n 4. Identify your role: Planner, Implementer, Reviewer, Completion Check Agent, Repairer, or Guardian-assist.\n 5. Identify Goal id, task id, branch, PR, dependencies, locks, risk level, required skills, and required evidence.\n@@ -103,7 +106,7 @@\n \n At minimum, identify:\n \n-- Relevant documents, ADRs, domain terms, and prior decisions.\n+- Relevant documents, SADRs, target ADRs, domain terms, and prior decisions.\n - Relevant modules, interfaces, seams, callers, owners, and tests.\n - Dependency relationships between tasks.\n - Candidate file locks and lock conflicts.\n@@ -152,7 +155,7 @@\n \n CCA must read:\n \n-- `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant ADRs.\n+- `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant SADRs and target ADRs.\n - Parent Goal issue, PRD issue, task issue, comments, and labels.\n - PR body, diff, commits, changed files, reviews, CI checks, and artifacts available to the workflow.\n - `.shiki/` task contract, locks, ledger entries, CCA checklist profile, and prior repair packets.\n@@ -214,7 +217,7 @@\n - Three or more new files.\n - Any high-risk or critical label.\n \n-When the architecture gate triggers, run `grill-with-docs` or `improve-codebase-architecture` as appropriate and record the decision in an ADR if it is hard to reverse or likely to be re-litigated.\n+When the architecture gate triggers, run `grill-with-docs` or `improve-codebase-architecture` as appropriate and record the decision in a Shiki SADR or target ADR as applicable if it is hard to reverse or likely to be re-litigated.\n \n ## 14. Skill Gate\n \n@@ -223,7 +226,7 @@\n Use these skills:\n \n - `setup-matt-pocock-skills`: before first use of the engineering skill set, or when issue tracker, triage labels, or domain docs are missing.\n-- `grill-with-docs`: default for non-trivial Goals before PRD/issues; required for ambiguous plans, terminology, boundaries, ADR-worthy tradeoffs, or design-tree decisions.\n+- `grill-with-docs`: default for non-trivial Goals before PRD/issues; required for ambiguous plans, terminology, boundaries, decision-record-worthy tradeoffs, or design-tree decisions.\n - `zoom-out`: unfamiliar code area or missing architectural map.\n - `to-prd`: convert settled context into a PRD.\n - `to-issues`: break a PRD, plan, or Goal into independently grabbable vertical-slice issues.\ndiff --git a/docs/adr/0001-keep-shiki-as-platform-name.md b/docs/adr/SADR-0001-keep-shiki-as-platform-name.md\nindex 20a9637c3b9d8c97de7ba8db7cfae4d82d9a7dba..8b685439959aada1b221c112f0ac3c51ca8a1695\n--- a/docs/adr/0001-keep-shiki-as-platform-name.md\n+++ b/docs/adr/SADR-0001-keep-shiki-as-platform-name.md\n@@ -1,3 +1,3 @@\n-# Keep Shiki As The Platform Name\n+# SADR-0001: Keep Shiki As The Platform Name\n \n Shiki remains the product name for the GitHub-native agentic engineering control plane. We considered more descriptive names such as MergePilot, MergeGate, and Kiroku, but kept Shiki because it carries the existing goal-seeking framework identity while remaining independent of Hermes, Codex, Claude Code, or any single runtime.\ndiff --git a/docs/adr/0002-use-agents-as-shared-constitution.md b/docs/adr/SADR-0002-use-agents-as-shared-constitution.md\nindex 73de30ecb078120eac540d9bc51855e3cd48d0e3..c5486182bf06fca36ea508496b013c67302fd989\n--- a/docs/adr/0002-use-agents-as-shared-constitution.md\n+++ b/docs/adr/SADR-0002-use-agents-as-shared-constitution.md\n@@ -1,3 +1,3 @@\n-# Use AGENTS As The Shared Agent Constitution\n+# SADR-0002: Use AGENTS As The Shared Agent Constitution\n \n Shiki uses `AGENTS.md` as the runtime-neutral constitution and `CLAUDE.md` as a thin Claude Code wrapper. This keeps Codex, Claude Code, Hermes Runner, and future Agent Runtimes aligned on the same GitHub-first MergeGate rules while still allowing runtime-specific instructions where needed.\ndiff --git a/docs/adr/0003-require-skill-gate-for-mergegate-readiness.md b/docs/adr/SADR-0003-require-skill-gate-for-mergegate-readiness.md\nindex 424be0c9af4c8f0136f1fde68a468c47ff84e764..7fc337987c7df9792f75f181dfcbed75fe5ffff2\n--- a/docs/adr/0003-require-skill-gate-for-mergegate-readiness.md\n+++ b/docs/adr/SADR-0003-require-skill-gate-for-mergegate-readiness.md\n@@ -1,3 +1,3 @@\n-# Require Skill Gate For MergeGate Readiness\n+# SADR-0003: Require Skill Gate For MergeGate Readiness\n \n Shiki requires relevant engineering skills to be invoked before execution when their triggers apply. This makes skills part of MergeGate readiness rather than optional prompt style, because Shiki depends on repeatable planning, issue decomposition, TDD, diagnosis, triage, and architecture review before autonomous or parallel agent work can be trusted.\ndiff --git a/docs/adr/0004-use-github-first-with-shiki-mirror.md b/docs/adr/SADR-0004-use-github-first-with-shiki-mirror.md\nindex d0a3a9174dc646a945a9d325954ffaf00c352040..a79c54ae9ee8eed2a0524192b864da4e22a92d3f\n--- a/docs/adr/0004-use-github-first-with-shiki-mirror.md\n+++ b/docs/adr/SADR-0004-use-github-first-with-shiki-mirror.md\n@@ -1,4 +1,4 @@\n-# ADR 0004: Use GitHub First With A Repository-Local Shiki Mirror\n+# SADR-0004: Use GitHub First With A Repository-Local Shiki Mirror\n \n ## Status\n \ndiff --git a/docs/adr/0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md b/docs/adr/SADR-0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\nindex b339baf49b89d9290e79c3d51b36e7b2bde3bd95..0f3c3eb9afccdb3403ce978e21d04fe12e495da0\n--- a/docs/adr/0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\n+++ b/docs/adr/SADR-0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\n@@ -1,8 +1,8 @@\n-# ADR 0005: Use Codex As Front And Claude Code Action As The GitHub Runtime\n+# SADR-0005: Use Codex As Front And Claude Code Action As The GitHub Runtime\n \n ## Status\n \n-Accepted (implementer default superseded by ADR 0008; the auth model and the\n+Accepted (implementer default superseded by SADR-0008; the auth model and the\n Claude Code Action GitHub runtime decision remain in force)\n \n ## Context\n@@ -26,4 +26,4 @@\n - Shiki avoids requiring `OPENAI_API_KEY` for the default workflow.\n - Codex work happens through the user's authenticated Codex front surface, not as a hidden backend Action.\n - Claude Code Action is configured for OAuth-token based subscription usage by default.\n-- API-key based automation remains possible only as an explicit target-repo extension with its own ADR.\n+- API-key based automation remains possible only as an explicit target-repo extension with its own target ADR.\ndiff --git a/docs/adr/0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md b/docs/adr/SADR-0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\nindex 4bcae9d1e52f0a37b8e9fbafa5cd14b6ee1993bd..7a38538a6ae9929567f0ade67e7ea6a60d0b3f7e\n--- a/docs/adr/0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\n+++ b/docs/adr/SADR-0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\n@@ -1,4 +1,4 @@\n-# ADR 0006: Separate Codex Implementation, CCA Completion, And MergeGate Authorization\n+# SADR-0006: Separate Codex Implementation, CCA Completion, And MergeGate Authorization\n \n ## Status\n \ndiff --git a/docs/adr/0007-packaging-and-release.md b/docs/adr/SADR-0007-packaging-and-release.md\nindex 79b84ddb97167372b44641e5b7860e400ce80822..bb445a2e2e856d9d1f5fdbe35de8d4e8cec0fb32\n--- a/docs/adr/0007-packaging-and-release.md\n+++ b/docs/adr/SADR-0007-packaging-and-release.md\n@@ -1,4 +1,4 @@\n-# ADR 0007: Packaging And Release For Shiki\n+# SADR-0007: Packaging And Release For Shiki\n \n ## Status\n \n@@ -25,7 +25,7 @@\n   not introduce third-party runtime dependencies or break the shim.\n - Releases must run inside the operator's subscription-authenticated toolchain\n   and must not require extra secrets beyond the built-in `GITHUB_TOKEN`\n-  (consistent with ADR 0005's no-API-key-by-default stance).\n+  (consistent with SADR-0005's no-API-key-by-default stance).\n - `.shiki/manifest.json` already encodes the install surface (`install.include`,\n   `install.create_directories`, `install.exclude_from_commit`), and it must not\n   be mutated as part of this change.\n@@ -34,7 +34,7 @@\n \n This is a hard-to-reverse platform decision (it sets the version source of\n truth, the release trigger, and the public install contract), so it is recorded\n-as an ADR.\n+as a SADR.\n \n ## Decision\n \n@@ -104,7 +104,7 @@\n 3. **Publishing to PyPI.** Rejected for now: Shiki is distributed as a\n    repository template + git-installable CLI, not a PyPI library; PyPI would add\n    credentials/secrets and a different versioning contract. Can be added later as\n-   its own ADR if a packaged distribution is needed.\n+   its own SADR if a packaged distribution is needed.\n 4. **Dynamic version (read `VERSION` via a build backend `dynamic` field).**\n    Rejected to keep `pyproject.toml` backend-agnostic and dependency-free; the\n    small duplication between `VERSION` and `pyproject.toml` is guarded by the\n@@ -123,4 +123,4 @@\n   releasing versions the whole platform but ships only `install.include` to\n   targets.\n - Future PyPI distribution or a console entry point are deferred and would each\n-  warrant their own ADR.\n+  warrant their own SADR.\ndiff --git a/docs/adr/0008-use-claude-code-as-default-implementer.md b/docs/adr/SADR-0008-use-claude-code-as-default-implementer.md\nindex dedf4510b7c7799521422f622029563d373db031..35c2625d908167fbc58855b5b63e283805f167e8\n--- a/docs/adr/0008-use-claude-code-as-default-implementer.md\n+++ b/docs/adr/SADR-0008-use-claude-code-as-default-implementer.md\n@@ -1,12 +1,12 @@\n-# ADR 0008: Use Claude Code As The Default Implementer Runtime\n+# SADR-0008: Use Claude Code As The Default Implementer Runtime\n \n ## Status\n \n-Proposed (supersedes ADR 0005 for the implementer default; keeps its auth model)\n+Proposed (supersedes SADR-0005 for the implementer default; keeps its auth model)\n \n ## Context\n \n-ADR 0005 made Codex Front the default implementation runtime, with Claude Code\n+SADR-0005 made Codex Front the default implementation runtime, with Claude Code\n restricted to planner and reviewer roles by the runtime registry, and the only\n autonomous implementation adapter was `shiki runner codex`.\n \n@@ -18,13 +18,13 @@\n - The platform's planning, review, and CCA layers already run on Claude Code,\n   and Claude Code's native capabilities (the built-in `/code-review` skill and\n   the Workflow multi-agent orchestration tool) have no Codex equivalent.\n-- The Goal lifecycle redesign (ADR 0009) requires an autonomous post-freeze\n+- The Goal lifecycle redesign (SADR-0009) requires an autonomous post-freeze\n   loop driven from the operator's Claude Code session, including bounded\n   repair dispatch, which must not depend on a runtime that cannot be invoked\n   unattended.\n \n The constitution claims Shiki is runtime-agnostic (\"not a Claude-only\n-workflow\", ADR 0002). Any change to the implementer default must preserve the\n+workflow\", SADR-0002). Any change to the implementer default must preserve the\n registry-based runtime model rather than hardcoding one vendor.\n \n ## Decision\n@@ -39,7 +39,7 @@\n   the task to review or repair-needed.\n - Keep Codex registered as an optional implementer runtime. `shiki runner\n   codex`, the Codex handoff templates, and the ChatGPT OAuth auth model from\n-  ADR 0005 remain supported for tasks explicitly assigned to `codex`.\n+  SADR-0005 remain supported for tasks explicitly assigned to `codex`.\n - Commit to mandating two Claude-native capabilities as loop contract in\n   follow-up Goals (status: the code-review gate was delivered by Goal G-B \u2014\n   skill registry, checklists item PR-12, default required_skills \u2014 while the\n@@ -60,7 +60,7 @@\n \n Out of scope: removing Codex support, changing the CCA runtime (Claude Code\n Action with `CLAUDE_CODE_OAUTH_TOKEN` stays), and API-key based automation\n-(still requires its own ADR per ADR 0005).\n+(still requires its own target ADR per SADR-0005).\n \n ## Consequences\n \n@@ -100,7 +100,7 @@\n   harness rejection of direct `codex exec` is outside Shiki's control, and the\n   plan's mandatory native capabilities cannot run on Codex at all.\n - **Remove Codex entirely**: rejected; it abandons the runtime-agnostic\n-  constitution (ADR 0002) for no operational gain and forecloses\n+  constitution (SADR-0002) for no operational gain and forecloses\n   cross-runtime adversarial review.\n - **GitHub Actions cloud implementation via claude-code-action**: rejected as\n   default; durable evidence is automatic but token cost, Node 24 workflow\ndiff --git a/docs/adr/0009-requirements-definition-and-spec-freeze.md b/docs/adr/SADR-0009-requirements-definition-and-spec-freeze.md\nindex 29b3f7871bef9203a8b4a831a65c39eb2f506e42..52e7dd69ad0aa4efe0b68c62678abbf7df02aaec\n--- a/docs/adr/0009-requirements-definition-and-spec-freeze.md\n+++ b/docs/adr/SADR-0009-requirements-definition-and-spec-freeze.md\n@@ -1,4 +1,4 @@\n-# ADR 0009: Merge Grill And Planning Into Requirements Definition With Spec Freeze\n+# SADR-0009: Merge Grill And Planning Into Requirements Definition With Spec Freeze\n \n ## Status\n \ndiff --git a/docs/adr/0010-external-ai-guardian-review.md b/docs/adr/SADR-0010-external-ai-guardian-review.md\nindex e6108bbe8bb4e73e0ff4684c8f78001a80438c4e..f3510b145664298adafb151b6f0db51b972f01ec\n--- a/docs/adr/0010-external-ai-guardian-review.md\n+++ b/docs/adr/SADR-0010-external-ai-guardian-review.md\n@@ -1,4 +1,4 @@\n-# ADR 0010: External AI Guardian Review As A First-Class Approval Authority\n+# SADR-0010: External AI Guardian Review As A First-Class Approval Authority\n \n ## Status\n \n@@ -8,7 +8,7 @@\n \n Shiki is intended to be an autonomous, recursive, self-improving agent\n framework: authority-in-the-loop, not human-in-the-loop. Until now the Guardian\n-approval required for high/critical-risk merges (guardian-policy.json, ADR\n+approval required for high/critical-risk merges (guardian-policy.json, SADR\n governance) recognized only human authority sources \u2014 a GitHub review approval,\n the `guardian:approved` label applied by a configured Guardian, or a comment\n carrying `Guardian approval granted` plus the current head SHA.\n@@ -89,7 +89,7 @@\n   autonomous design the operator intends and produced the impersonation\n   incident.\n - **Record AI approval as an operator comment**: rejected as forgery of human\n-  authority \u2014 the exact failure this ADR corrects.\n+  authority \u2014 the exact failure this SADR corrects.\n - **Deliver the artifact as a committed `.shiki/` file**: rejected; a committed\n   approval inside the PR being approved is forgeable by anyone who can push.\n - **A dedicated reviewer bot identity instead of a relayed comment**: deferred;\ndiff --git a/docs/adr/0011-autonomous-quality-gate-evidence.md b/docs/adr/SADR-0011-autonomous-quality-gate-evidence.md\nindex bca0246c5d46e44afbde15486a75eec425fc02f5..9c93eac16d8c3a6d4b11bd7224e3955102cd7db4\n--- a/docs/adr/0011-autonomous-quality-gate-evidence.md\n+++ b/docs/adr/SADR-0011-autonomous-quality-gate-evidence.md\n@@ -1,4 +1,4 @@\n-# ADR 0011: Autonomous Quality-Gate Evidence \u2014 Deterministic Facts And An Independent Verifier\n+# SADR-0011: Autonomous Quality-Gate Evidence \u2014 Deterministic Facts And An Independent Verifier\n \n ## Status\n \ndiff --git a/docs/adr/0012-autonomous-loop-closeout-to-main.md b/docs/adr/SADR-0012-autonomous-loop-closeout-to-main.md\nindex 637fad6dde4989c9c2bf0f81f676d8d29991123c..ec7d85f81030a676bcbba28b43eb365422a36eac\n--- a/docs/adr/0012-autonomous-loop-closeout-to-main.md\n+++ b/docs/adr/SADR-0012-autonomous-loop-closeout-to-main.md\n@@ -1,4 +1,4 @@\n-# 0012 \u2014 Autonomous loop pushes goal completion to main via a closeout PR\n+# SADR-0012 \u2014 Autonomous loop pushes goal completion to main via a closeout PR\n \n - Status: accepted\n - Date: 2026-06-18\ndiff --git a/docs/adr/0013-bootstrap-grants-actions-approve-pull-requests.md b/docs/adr/SADR-0013-bootstrap-grants-actions-approve-pull-requests.md\nindex b6c69d64f1c5ced346b88d6859586715b3b5bd0e..cf48ff4a6716267f539ca58edd1a852e7c7a2d64\n--- a/docs/adr/0013-bootstrap-grants-actions-approve-pull-requests.md\n+++ b/docs/adr/SADR-0013-bootstrap-grants-actions-approve-pull-requests.md\n@@ -1,4 +1,4 @@\n-# ADR 0013: Bootstrap Grants GitHub Actions Permission To Create And Approve Pull Requests\n+# SADR-0013: Bootstrap Grants GitHub Actions Permission To Create And Approve Pull Requests\n \n ## Status\n \n@@ -31,7 +31,7 @@\n security tradeoff: it lets repository automation satisfy a human-review gate. In\n a multi-maintainer repo that could let a compromised or buggy workflow\n self-approve changes. So whether bootstrap turns it on by default \u2014 and with\n-what default token scope \u2014 is an ADR-worthy decision, not an implementation\n+what default token scope \u2014 is a SADR-worthy decision, not an implementation\n detail.\n \n ## Decision\n@@ -48,7 +48,7 @@\n Specifically:\n \n - **`can_approve_pull_request_reviews` defaults to `true`.** Shiki's default\n-  posture is solo/AFK autonomous operation (ADR 0008/0009/0012), where the CCA\n+  posture is solo/AFK autonomous operation (SADR-0008/0009/0012), where the CCA\n   Review Bridge is the only path that can satisfy `required_review: true` after\n   the CCA and Guardian gates have already passed. The default makes a\n   freshly-bootstrapped repo able to run the autonomous loop end to end without a\ndiff --git a/docs/adr/0014-codex-app-external-ai-guardian-ui-adapter.md b/docs/adr/SADR-0014-codex-app-external-ai-guardian-ui-adapter.md\nindex 238db06279334caa341c117362df9271382a7fcd..65f12e9939321e1b3e389a34c822fc1549e5155d\n--- a/docs/adr/0014-codex-app-external-ai-guardian-ui-adapter.md\n+++ b/docs/adr/SADR-0014-codex-app-external-ai-guardian-ui-adapter.md\n@@ -1,4 +1,4 @@\n-# ADR 0014: Use Codex App As The External AI Guardian UI Adapter\n+# SADR-0014: Use Codex App As The External AI Guardian UI Adapter\n \n ## Status\n \ndiff --git a/docs/adr/0015-contract-approval-before-dispatch.md b/docs/adr/SADR-0015-contract-approval-before-dispatch.md\nindex fad6c656741a20a7684ba16080a7db159f63a52f..6061271a46d3f1d6ae7174477de3fb75673123a6\n--- a/docs/adr/0015-contract-approval-before-dispatch.md\n+++ b/docs/adr/SADR-0015-contract-approval-before-dispatch.md\n@@ -1,4 +1,4 @@\n-# ADR 0015: Approve The Task Contract Before Dispatch, Not The Diff After It\n+# SADR-0015: Approve The Task Contract Before Dispatch, Not The Diff After It\n \n ## Status\n \n@@ -9,7 +9,7 @@\n Guardian approval for high/critical risk is expressible only on a pull request.\n Every approval source in `guardian-policy.json` \u2014 GitHub review, the\n `guardian:approved` label, the `Guardian approval granted` comment, and the\n-`external_ai_guardian_review` artifact (ADR 0010/0014) \u2014 is bound to a PR, and\n+`external_ai_guardian_review` artifact (SADR-0010/0014) \u2014 is bound to a PR, and\n three of the four require the current head SHA. Approval therefore cannot exist\n before an implementation PR exists.\n \n@@ -81,7 +81,7 @@\n An envelope breach must surface only on the non-repairable policy gate, never on\n the repairable `MergeGate metadata check`. Otherwise the Goal Loop routes it to\n `dispatch_repair` \u2014 instructing a runner to make the breach go away \u2014 which is\n-exactly what this ADR forbids.\n+exactly what this SADR forbids.\n \n **Contract immutability.** After registration, an implementation PR must not\n change the governance fields of its own task contract (`scope`, `non_goals`,\n@@ -116,7 +116,7 @@\n fails any verdict that is not `complete`, and the signal the CCA prompt reads is\n computed from `evaluate_guardian_approval` alone. Without the OR-merge at the\n signal, a contract-approved PR still returns `needs_guardian`, the CCA job fails,\n-and the MergeGate policy check never runs \u2014 the gate this ADR changes would never\n+and the MergeGate policy check never runs \u2014 the gate this SADR changes would never\n be reached. The CCA *prompt* needs no change: a `contract_approval` entry in the\n signal's existing `sources` array satisfies its current wording.\n \n@@ -130,7 +130,7 @@\n written, and an approved contract lets its implementation PRs merge through the\n same automatic path as low/medium risk. The wasted implement-then-block cycle\n disappears. Guardian review shifts from reading diffs to reading contracts, which\n-is both earlier and smaller; the external AI Guardian path (ADR 0010/0014) works\n+is both earlier and smaller; the external AI Guardian path (SADR-0010/0014) works\n on contracts with no change.\n \n We accept that the Guardian authorizes a class of change without seeing its\n@@ -146,7 +146,7 @@\n adversarial tests for the new approval path equivalent in rigor to\n `test_shiki_governance_evidence.sh`.\n \n-Bootstrap costs, which apply only while this ADR is being implemented. The tasks\n+Bootstrap costs, which apply only while this SADR is being implemented. The tasks\n that build Contract Approval merge under the existing rules, so the ones\n dispatched after the pre-dispatch gate lands but before the approval path lands\n need a Guardian-approved Contract PR *and* a live Guardian approval on their\n@@ -210,7 +210,7 @@\n risk; they do not bound authorized work.\n \n **Auto-escalate risk on envelope breach instead of stopping.** Returns the\n-workflow to a diff-time Guardian decision \u2014 the exact behavior this ADR removes.\n+workflow to a diff-time Guardian decision \u2014 the exact behavior this SADR removes.\n \n **Resolve governance fields from the base snapshot only.** Rejected against the\n registration flow: implementation PRs add their own contract, so the base has no\ndiff --git a/docs/adr/0016-locks-bound-product-paths.md b/docs/adr/SADR-0016-locks-bound-product-paths.md\nindex b5e850f3f1f7f4ef6e7f9d1763a29a51bd300c45..46b40a9b99a7aae644c02bb458f5473ff0a90ce2\n--- a/docs/adr/0016-locks-bound-product-paths.md\n+++ b/docs/adr/SADR-0016-locks-bound-product-paths.md\n@@ -1,4 +1,4 @@\n-# ADR 0016: Locks bound product paths; the Shiki mirror is bound by record identity\n+# SADR-0016: Locks bound product paths; the Shiki mirror is bound by record identity\n \n ## Status\n \n@@ -6,8 +6,8 @@\n \n - Date: 2026-07-28\n - Deciders: operator (mizutani-140), Claude (planner)\n-- Related: ADR 0004 (GitHub-first with a `.shiki` mirror), ADR 0009 (Spec Freeze),\n-  ADR 0012 (autonomous loop closeout to main), ADR 0015 (Contract Approval before\n+- Related: SADR-0004 (GitHub-first with a `.shiki` mirror), SADR-0009 (Spec Freeze),\n+  SADR-0012 (autonomous loop closeout to main), SADR-0015 (Contract Approval before\n   dispatch), remediation goal task R-02, the live PR #179 deadlock.\n \n ## Context\n@@ -90,11 +90,11 @@\n files, documentation files and named source modules are nameable; runtime-\n allocated ledger, runner and report ids are not \u2014 and those live in `.shiki`,\n which locks no longer govern. Existing frozen contracts are brought into line by\n-a one-time Spec Amendment per plan, which must land before ADR 0015's contract\n+a one-time Spec Amendment per plan, which must land before SADR-0015's contract\n immutability makes `locks` unwritable from the implementation PR.\n \n **Lock state is resolved from the durable side.** MergeGate resolves the lock\n-registry, and ADR 0015's governance fields, from the base `.shiki` snapshot when\n+registry, and SADR-0015's governance fields, from the base `.shiki` snapshot when\n present, falling back to head only when absent. The `MergeGate metadata check`\n job is given `--base-shiki`, which it does not receive today. Locks are not\n derived from the live open-pull-request set: a pull request body is untrusted\n@@ -108,7 +108,7 @@\n Explicitly out of scope: moving MergeGate's contract resolution off the PR\n checkout wholesale; making the mirror a projection of GitHub state; introducing\n new lock kinds; moving ledger evidence out of the repository; and unwinding\n-ADR 0012's closeout pull request. Registration of multi-task goals is ADR 0015's\n+SADR-0012's closeout pull request. Registration of multi-task goals is SADR-0015's\n Contract PR and is not re-decided here.\n \n ## Consequences\n@@ -127,7 +127,7 @@\n \n Risks accepted. First, goal-scoped identity is only as strong as `goal_id`,\n which is resolved from the PR body and the head task file; the mitigation is to\n-add `goal_id` to ADR 0015's frozen governance field set, and until that lands a\n+add `goal_id` to SADR-0015's frozen governance field set, and until that lands a\n task file can name a foreign goal. Second, base-when-present resolution is\n unavailable on fork pull requests, where the head is contributor-controlled;\n fork pull requests already cannot complete a Shiki merge, and this decision does\n@@ -139,7 +139,7 @@\n path, not by implementation pull requests.\n \n New obligations: one Spec Amendment per frozen plan to rewrite `locks`, ordered\n-before ADR 0015 T1; extension of the adversarial suites in\n+before SADR-0015 T1; extension of the adversarial suites in\n `scripts/test_shiki_adversarial_state.sh` and\n `scripts/test_shiki_governance_evidence.sh` to cover the eleven newly-scoped\n directories; and updating `scripts/test_shiki_loop_lock_guard.sh`,\n@@ -151,32 +151,32 @@\n whether the loop's transitive sync of ledger `evidence` references can be\n deleted once the Contract PR carries its own source plan.\n \n-## Relationship to ADR 0004\n+## Relationship to SADR-0004\n \n-This ADR **clarifies something ADR 0004 left unspecified**. ADR 0004 decides that\n+This SADR **clarifies something SADR-0004 left unspecified**. SADR-0004 decides that\n GitHub is the operational source of truth and that each target repository keeps a\n `.shiki/` mirror. It says nothing about pull requests writing the mirror, does\n not name MergeGate, and does not make the mirror an input to a gate. Nothing in\n-ADR 0004's Decision or Consequences is amended, superseded or weakened: the\n+SADR-0004's Decision or Consequences is amended, superseded or weakened: the\n mirror still exists, still records the same artifacts, and pull request\n-boundaries remain the coordination surface. What this ADR settles is the question\n+boundaries remain the coordination surface. What this SADR settles is the question\n 0004 never reached \u2014 by what authority a pull request may write a mirror record \u2014\n and it answers it the way `.shiki/manifest.json` already declares\n (`pr_mutation: current-task-or-current-goal-only`).\n \n-It **amends ADR 0012**, whose Decision item 5 states that \"Loop-executed tasks\n+It **amends SADR-0012**, whose Decision item 5 states that \"Loop-executed tasks\n declare the `path:.shiki/**` lock, which covers every `.shiki` file the closeout\n stages ... so `files_outside_locks` is satisfied without per-file lock\n bookkeeping.\" That clause is replaced: the closeout's mirror writes are\n-authorized by record identity, not by a declared lock. ADR 0012's substantive\n+authorized by record identity, not by a declared lock. SADR-0012's substantive\n decision \u2014 that completion reaches `main` through a closeout pull request rather\n than local attestation \u2014 is preserved unchanged.\n \n-It is **complementary to ADR 0015**, not a substitute. ADR 0015 owns contract\n+It is **complementary to SADR-0015**, not a substitute. SADR-0015 owns contract\n immutability, the pre-dispatch gate, the Contract PR and the Scope Envelope. This\n-ADR owns what a lock means and who may write the mirror. The one ordering\n+SADR owns what a lock means and who may write the mirror. The one ordering\n constraint between them is that the lock-narrowing Spec Amendment must precede\n-ADR 0015 T1's freezing of the `locks` field.\n+SADR-0015 T1's freezing of the `locks` field.\n \n ## Alternatives Considered\n \n@@ -186,7 +186,7 @@\n the only writer that can register them may run. Migrating the Guardian signal to\n that store makes every registration PR fail closed to `needs_guardian` at every\n risk level, because the task file is by definition absent from the store, which\n-removes the low/medium autonomous path ADR 0008/0009 exists to provide. And it\n+removes the low/medium autonomous path SADR-0008/0009 exists to provide. And it\n leaves schemas, manifest, config, guardian policy, templates, migrations, plans,\n worktrees, runner records, runs and handoffs with no writer at all in a\n repository whose product is `.shiki`. Measured benefit on the frozen backlog:\n@@ -199,14 +199,14 @@\n namespace is not in fact ownership-partitioned \u2014 56 ledger entries carry no\n `task_id`, and one goal DAG is writable by 35 registered tasks. The correct\n expression of its insight is that locks do not govern the mirror at all, which is\n-what this ADR decides.\n+what this SADR decides.\n \n **Make the mirror a projection written only by a registration writer and a\n post-merge projection job.** Rejected. It requires a bypass actor with write\n access to the protected default branch; it makes its own master invariant\n readable from a manifest that pull requests may edit; it deletes both reconcile\n modes and leaves a corrupted mirror with no in-band repair path, contradicting\n-ADR 0004's consequence that mirror conflicts are repaired against GitHub state;\n+SADR-0004's consequence that mirror conflicts are repaired against GitHub state;\n and its content-derived ids are rejected by `validate_shiki`'s currently-green id\n pattern.\n \ndiff --git a/docs/adr/0017-bookkeeping-closeouts-do-not-inherit-risk.md b/docs/adr/SADR-0017-bookkeeping-closeouts-do-not-inherit-risk.md\nindex 554e3a0d526a72ca067108c5464cc68c3edcf174..b9676857a4713850e063af7c39c8e0b01ecb4c66\n--- a/docs/adr/0017-bookkeeping-closeouts-do-not-inherit-risk.md\n+++ b/docs/adr/SADR-0017-bookkeeping-closeouts-do-not-inherit-risk.md\n@@ -1,4 +1,4 @@\n-# ADR 0017: A Bookkeeping Closeout Does Not Inherit Its Task's Risk\n+# SADR-0017: A Bookkeeping Closeout Does Not Inherit Its Task's Risk\n \n ## Status\n \n@@ -6,7 +6,7 @@\n \n ## Context\n \n-ADR 0012 made the goal loop push a task's completion to the default branch through a\n+SADR-0012 made the goal loop push a task's completion to the default branch through a\n closeout pull request: after the implementation PR merges, the loop opens a second PR\n carrying `task.status = done`, `lock.state = released`, and \u2014 when the task completes\n its goal \u2014 `goal.status = complete` plus one scorecard report. Nothing else. The\n@@ -49,14 +49,14 @@\n 4. The task file's governance fields (`scope`, `non_goals`, `required_skills`,\n    `risk_level`, `locks`, `acceptance_checks`, `test_command`) are byte-identical to\n    the base snapshot. Only `status`, `expected_pr`, `closeout_pr`, `ledger_evidence`\n-   and `expected_branch` may differ \u2014 the ADR 0015 contract-immutability rule,\n+   and `expected_branch` may differ \u2014 the SADR-0015 contract-immutability rule,\n    unchanged.\n \n    `expected_branch` is in that set because a closeout is *by construction* on a\n    different branch from the implementation it closes, and the loop rewrites the\n    field accordingly. It is safe to let it differ because MergeGate independently\n    requires `expected_branch` to equal the PR's actual head ref, so a PR cannot set\n-   it to anything other than the branch it is really on. The first draft of this ADR\n+   it to anything other than the branch it is really on. The first draft of this SADR\n    omitted it, which made the exemption unreachable: every real closeout failed\n    condition 4 on a field the loop always changes. See PR #209.\n 5. The state transitions are exactly the terminal set: task `review` \u2192 `done`, lock\n@@ -113,12 +113,12 @@\n classification must be derived, not asserted.\n \n **Fold the closeout into the implementation PR** so no second PR exists. Rejected under\n-ADR 0012, which recorded why: a task file that is already `done` on the implementation\n+SADR-0012, which recorded why: a task file that is already `done` on the implementation\n branch short-circuits the loop's review \u2192 CCA \u2192 merge \u2192 done ordering into a local\n goal-completion before any merge happens.\n \n **Have the loop auto-merge closeouts with elevated credentials.** Rejected outright: that\n-is a runtime authoring its own approval, which ADR 0010 and the constitution's\n+is a runtime authoring its own approval, which SADR-0010 and the constitution's\n approval-record prohibition forbid. It would also be indistinguishable, in the evidence,\n from the forgery those rules exist to prevent.\n \ndiff --git a/docs/adr/0018-carried-guardian-approval-across-a-proven-base-sync.md b/docs/adr/SADR-0018-carried-guardian-approval-across-a-proven-base-sync.md\nindex 22358562da390ed8b45cd600da08a43d378b4680..e6e2907cefa776460af73ddf7d39db605f3c39b0\n--- a/docs/adr/0018-carried-guardian-approval-across-a-proven-base-sync.md\n+++ b/docs/adr/SADR-0018-carried-guardian-approval-across-a-proven-base-sync.md\n@@ -1,4 +1,4 @@\n-# ADR 0018: Carried Guardian Approval Across A Proven Base Sync\n+# SADR-0018: Carried Guardian Approval Across A Proven Base Sync\n \n ## Status\n \n@@ -34,7 +34,7 @@\n Guardian evaluator that consumes it (`shiki_guardian.evaluate_guardian_approval`,\n the `guardian_comment_carried` source) were built and tested in isolation. Neither\n does anything until a caller passes the flags that turn the carry on, so the\n-feature was invisible in CI. This ADR records the decision the two of them\n+feature was invisible in CI. This SADR records the decision the two of them\n implement, and this task wires it into the four CI invocations that judge Guardian\n approval.\n \ndiff --git a/docs/adr/README.md b/docs/adr/README.md\nindex c70e057a4520c36e042ab68c53cd4a21ef709b71..5da43856836e2ce6a150f8127766e2f88355ab4b\n--- a/docs/adr/README.md\n+++ b/docs/adr/README.md\n@@ -1,40 +1,41 @@\n-# Architecture Decision Records\n+# Decision Record Namespaces\n \n-This directory holds Shiki's Architecture Decision Records (ADRs): durable\n-records of hard-to-reverse platform decisions. ADRs are part of the Shiki source\n-of truth alongside GitHub state, the `.shiki/` mirror, and `CONTEXT.md`.\n+This directory holds two independent decision namespaces:\n \n-## How ADRs Work\n+- Shiki Architecture Decision Records (SADRs), named\n+  `SADR-NNNN-<short-slug>.md`, record hard-to-reverse Shiki platform decisions.\n+- Target Architecture Decision Records (ADRs), named\n+  `NNNN-<short-slug>.md`, belong to the target product.\n \n-- New ADRs are added as `docs/adr/NNNN-<short-slug>.md`, where `NNNN` is the next\n-  zero-padded sequence number.\n-- The **canonical list of ADRs is the set of files matching `docs/adr/*.md`** in\n-  this directory. Browse `docs/adr/*.md` for the authoritative, current set.\n-- Use [`template.md`](template.md) as the starting point for a new ADR.\n-- Record an ADR when a decision is hard to reverse, surprising without context,\n-  and a real tradeoff. Do not write an ADR for routine or easily reversible\n-  choices.\n+The same four-digit number may appear once in each namespace. A platform\n+`SADR-NNNN` citation resolves only to `SADR-NNNN-*.md`; a target `ADR NNNN`\n+citation resolves only to `NNNN-*.md`.\n \n-Using the `docs/adr/*.md` glob as the source of truth means other PRs can add an\n-ADR without editing this index file, avoiding merge contention on a\n-hand-maintained enumeration.\n+## How Decision Records Work\n \n-## Current ADRs (snapshot)\n-\n-The list below is a convenience snapshot and may lag behind the directory. When\n-in doubt, list `docs/adr/*.md`.\n+- New Shiki platform decisions use the next zero-padded SADR number.\n+- New target product decisions use the target's own ADR sequence.\n+- The canonical list is the files in `docs/adr/`; no hand-maintained index is\n+  authoritative.\n+- Use [`template.md`](template.md) as the starting point and choose the filename\n+  namespace that owns the decision.\n+- Record a decision when it is hard to reverse, surprising without context, and\n+  a real tradeoff. Do not write a decision record for routine or easily\n+  reversible choices.\n \n-- [0001 \u2014 Keep Shiki As The Platform Name](0001-keep-shiki-as-platform-name.md)\n-- [0002 \u2014 Use AGENTS As The Shared Agent Constitution](0002-use-agents-as-shared-constitution.md)\n-- [0003 \u2014 Require Skill Gate For MergeGate Readiness](0003-require-skill-gate-for-mergegate-readiness.md)\n-- [0004 \u2014 Use GitHub First With A Repository-Local Shiki Mirror](0004-use-github-first-with-shiki-mirror.md)\n-- [0005 \u2014 Use Codex As Front And Claude Code Action As The GitHub Runtime](0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md)\n-- [0006 \u2014 Separate Codex Implementation, CCA Completion, And MergeGate Authorization](0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md)\n+Using the directory as the source of truth lets independent changes add records\n+without contending on an enumerated index.\n \n-## Adding A New ADR\n+## Adding A Shiki SADR\n \n-1. Copy `template.md` to `docs/adr/NNNN-<short-slug>.md`.\n+1. Copy `template.md` to `docs/adr/SADR-NNNN-<short-slug>.md`.\n 2. Fill in the title, status, context, decision, and consequences.\n-3. Set the status to `Proposed`, then `Accepted` once the decision is approved.\n-4. Open a PR following `CONTRIBUTING.md`. You do not need to edit this index for\n-   the ADR to be discoverable via `docs/adr/*.md`.\n+3. Set the status to `Proposed`, then `Accepted` once the proper authority has\n+   approved the decision.\n+4. Open a PR following `CONTRIBUTING.md`.\n+\n+## Adding A Target ADR\n+\n+1. Copy `template.md` to `docs/adr/NNNN-<short-slug>.md` using the target's own\n+   sequence.\n+2. Cite it as `ADR NNNN`; do not prefix its filename with `SADR-`.\ndiff --git a/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md b/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\nnew file mode 100644\nindex 0000000000000000000000000000000000000000..7455ce1f3bed02ddddb851ac21a1d1a648c9aeba\n--- /dev/null\n+++ b/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\n@@ -0,0 +1,55 @@\n+# SADR-0019: Separate Shiki Platform SADRs From Target ADRs\n+\n+## Status\n+\n+Accepted\n+\n+## Context\n+\n+Shiki installs its platform decision records into `docs/adr/` in every target\n+repository. Shiki and its targets previously used the same numeric\n+`NNNN-<slug>.md` namespace. A target that already owned ADRs 0013 through 0018\n+therefore acquired two records for each number during installation, making an\n+`ADR NNNN` citation ambiguous. Renumbering target decisions would rewrite target\n+history, while renumbering Shiki decisions per target would destroy their stable\n+platform identity.\n+\n+Upgrades also need to remove the former Shiki-owned numeric paths without ever\n+mistaking a target-owned ADR for platform content. The install stamp already\n+records exact shipped paths and content digests, so it can prove ownership only\n+when both still match.\n+\n+## Decision\n+\n+Shiki platform decisions use the `SADR` namespace:\n+\n+- records are named `docs/adr/SADR-NNNN-<slug>.md`;\n+- live platform citations use `SADR-NNNN`;\n+- target product decisions retain `docs/adr/NNNN-<slug>.md` and `ADR NNNN`;\n+- the reference validator resolves the two namespaces independently, so the same\n+  four-digit number may exist once in each namespace.\n+\n+Before a forced upgrade writes anything, the installer inventories the exact\n+former numeric Shiki ADR paths. It may delete a legacy path only when the\n+existing install stamp contains that exact path and its recorded SHA-256 digest\n+matches the file currently on disk. All ownership blockers are reported\n+together and abort the upgrade before its first write. A non-forced install\n+never performs this cleanup.\n+\n+Historical `.shiki/` evidence remains unchanged because it records the namespace\n+that existed when that evidence was produced.\n+\n+## Consequences\n+\n+- Targets may use any ADR number without colliding with Shiki's platform SADRs.\n+- Fresh installs preserve target ADR bytes and install a stable SADR set.\n+- Forced upgrades fail closed when legacy ownership cannot be proven, and a\n+  successful upgrade refreshes the stamp without removed legacy paths.\n+- Current governance, packaging, workflow, prompt, script, and skill references\n+  must move with the platform records.\n+\n+## Alternatives Considered\n+\n+- Collision-only refusal leaves affected targets permanently unupgradable.\n+- Renumbering target ADRs mutates target-owned history.\n+- Per-target Shiki renumbering makes platform citations non-portable.\ndiff --git a/docs/adr/template.md b/docs/adr/template.md\nindex a92a97daddd0b00ab9dd626cc8d86942da2eafed..17dd79175b5fee2105935557f691b40cf9492942\n--- a/docs/adr/template.md\n+++ b/docs/adr/template.md\n@@ -1,14 +1,14 @@\n-# NNNN: <Short Decision Title>\n+# <SADR-NNNN or NNNN>: <Short Decision Title>\n \n ## Status\n \n-Proposed | Accepted | Superseded by ADR-NNNN\n+Proposed | Accepted | Superseded by SADR-NNNN or target ADR NNNN\n \n ## Context\n \n What is the situation, constraint, or force that requires a decision? Reference\n-relevant domain terms (`CONTEXT.md`), prior ADRs, and code reality. State the\n-problem, not the solution.\n+relevant domain terms (`CONTEXT.md`), prior Shiki SADRs or target ADRs, and code\n+reality. State the problem, not the solution.\n \n ## Decision\n \ndiff --git a/docs/agents/autonomous-loop-e2e-acceptance.md b/docs/agents/autonomous-loop-e2e-acceptance.md\nindex bbf84c6081987f159ac6ad9ecf3c82b7aa72e7e8..5c13c1d02a05a90b0dc46cbef7c95a9846d0c237\n--- a/docs/agents/autonomous-loop-e2e-acceptance.md\n+++ b/docs/agents/autonomous-loop-e2e-acceptance.md\n@@ -40,8 +40,8 @@\n 3. `gh auth status` is logged in with merge permission on the repo.\n 4. Branch protection + required checks are configured exactly as the goal-loop\n    required-check set expects (see `docs/agents/branch-protection-smoke.md`).\n-5. The repo `.shiki/policy` permits autonomous low/medium-risk merges (ADR\n-   0008/0009); the MergeGate policy check is the recorded-authority gate.\n+5. The repo `.shiki/policy` permits autonomous low/medium-risk merges\n+   (SADR-0008/0009); the MergeGate policy check is the recorded-authority gate.\n \n The operator's only actions are: register the task and start the loop. **No\n manual pre-clearing** means after `shiki loop run` starts, the operator does not\ndiff --git a/docs/agents/bootstrap-command.md b/docs/agents/bootstrap-command.md\nindex 05f7f203a52cf412b9f97084a6e131ed0f785afd..f7aa0b734dfebee4a3b139335b66ee3415f48713\n--- a/docs/agents/bootstrap-command.md\n+++ b/docs/agents/bootstrap-command.md\n@@ -189,7 +189,9 @@\n ```\n \n Use this only for tests, fixtures, or explicit local-only template inspection.\n-Use `--force` only when you intentionally want to overwrite existing target files.\n+Use `--force` only when you intentionally want to refresh the target template.\n+Legacy numeric Shiki decision cleanup is stamp-and-digest authorized before any\n+write; target ADR paths are not cleanup candidates.\n \n ## Slash Command\n \ndiff --git a/docs/agents/checklists.md b/docs/agents/checklists.md\nindex 4a7f03ade346f53a1c1d82981f3fd744f9ab4547..0c5ec6c53dc7197b7b7a7d18e667b325babd6c45\n--- a/docs/agents/checklists.md\n+++ b/docs/agents/checklists.md\n@@ -32,18 +32,18 @@\n | ID | Item | Blocking |\n | --- | --- | --- |\n | GD-01 | Plan was challenged against `CONTEXT.md`. | blocking for non-trivial Goals |\n-| GD-02 | Relevant ADRs were read or absence was recorded. | blocking for non-trivial Goals |\n+| GD-02 | Relevant SADRs and target ADRs were read or absence was recorded. | blocking for non-trivial Goals |\n | GD-03 | Ambiguous terms were resolved or converted to HITL blockers. | blocking |\n | GD-04 | Edge scenarios were considered. | advisory |\n | GD-05 | Code reality was checked when code could answer. | blocking when applicable |\n | GD-06 | New domain terms were added to `CONTEXT.md` when needed. | blocking when applicable |\n-| GD-07 | ADR was created or explicitly skipped using the ADR criteria. | blocking when applicable |\n+| GD-07 | A SADR or target ADR was created or explicitly skipped using the decision-record criteria. | blocking when applicable |\n \n ## CI \u2014 Context & Impact Checklist\n \n | ID | Item | Blocking |\n | --- | --- | --- |\n-| CI-01 | Relevant docs and ADRs identified. | blocking |\n+| CI-01 | Relevant docs, SADRs, and target ADRs identified. | blocking |\n | CI-02 | Relevant modules, interfaces, seams, callers, and tests identified. | blocking |\n | CI-03 | Dependencies and blocked-by relationships identified. | blocking |\n | CI-04 | Candidate locks identified. | blocking |\n@@ -62,7 +62,7 @@\n | PRD-04 | Implementation decisions are recorded without volatile code detail. | blocking |\n | PRD-05 | Testing decisions are recorded. | blocking |\n | PRD-06 | Out-of-scope items are explicit. | blocking |\n-| PRD-07 | Relevant glossary/ADR links are present when applicable. | blocking |\n+| PRD-07 | Relevant glossary/SADR/target-ADR links are present when applicable. | blocking |\n \n ## SF \u2014 Spec Freeze Checklist\n \n@@ -98,7 +98,7 @@\n | H-02 | Branch/worktree target is present. | blocking |\n | H-03 | Scope and non-goals are present. | blocking |\n | H-04 | Dependencies and locks are present. | blocking |\n-| H-05 | Relevant docs, ADRs, modules, and tests are referenced. | blocking |\n+| H-05 | Relevant docs, SADRs, target ADRs, modules, and tests are referenced. | blocking |\n | H-06 | Required skills are stated. | blocking |\n | H-07 | TDD expectations are stated. | blocking when behavior work |\n | H-08 | Acceptance criteria are included. | blocking |\ndiff --git a/docs/agents/codex-handoff.md b/docs/agents/codex-handoff.md\nindex ede468f47cb277f69a857e8c45df703e4ed0151d..6f97bdffb67e44d7edea2c71e01ea28933c4eb6c\n--- a/docs/agents/codex-handoff.md\n+++ b/docs/agents/codex-handoff.md\n@@ -46,7 +46,7 @@\n \n ## Context\n - Domain terms:\n-- Relevant ADRs:\n+- Relevant SADRs and target ADRs:\n - Relevant docs:\n - Relevant modules/interfaces/seams:\n - Similar tests/prior art:\ndiff --git a/docs/agents/completion-check-agent.md b/docs/agents/completion-check-agent.md\nindex 13e4c035db35025f4e3cdcc5adfb24314ac4d521..61bfa1218cf36ca605250c91919e620f33238aa0\n--- a/docs/agents/completion-check-agent.md\n+++ b/docs/agents/completion-check-agent.md\n@@ -32,7 +32,7 @@\n - `AGENTS.md`\n - `CLAUDE.md`\n - `CONTEXT.md`\n-- relevant ADRs\n+- relevant SADRs and target ADRs\n - parent Goal issue\n - PRD issue or document\n - task issue\ndiff --git a/docs/agents/decision-control.md b/docs/agents/decision-control.md\nindex 7d8ee5ab03a5ccbcbd04056ae61de03785a2a9c9..8c94ecda00fffaa0e92c325ab83aa9776bd1dac1\n--- a/docs/agents/decision-control.md\n+++ b/docs/agents/decision-control.md\n@@ -11,7 +11,7 @@\n | Decision | Owner | When | Required inputs | Output | Enforcement surface |\n | --- | --- | --- | --- | --- | --- |\n | Is the Goal clear enough to plan? | Planner + Guardian when needed | Goal intake | User request, existing docs, risk signals | Goal readiness verdict | Goal checklist, issue template, ledger |\n-| Must `grill-with-docs` run? | Planner / Skill Gate | Before PRD or issue split | Ambiguity, domain terms, ADR conflicts, unclear boundaries | Skill invocation or explicit skip rationale | Skill Gate checklist |\n+| Must `grill-with-docs` run? | Planner / Skill Gate | Before PRD or issue split | Ambiguity, domain terms, SADR/target-ADR conflicts, unclear boundaries | Skill invocation or explicit skip rationale | Skill Gate checklist |\n | Is a PRD required? | Planner | After Goal context stabilizes | Goal, `grill-with-docs` output, Context & Impact output | PRD required / not required | PRD checklist, CCA evidence check |\n | Can the spec be frozen? | Operator only | End of Requirements Definition, before issue split | PRD/requirements summary, scope inventory (SF-02), open-question list | `spec_freeze` block in the plan + ledger evidence | `require_grilled_plan` (plan ingest/run), SF checklist |\n | Can a frozen spec change? | Operator only (Spec Amendment) | When implementation contradicts the frozen spec | Contested decisions, scoped re-grill result | recorded amendment + re-stamped freeze | SF checklist, ledger evidence |\ndiff --git a/docs/agents/domain.md b/docs/agents/domain.md\nindex 258da8ea81e87ac0a6b0b5b5e5c2fde980eaabb7..d57b0cf17ff3e1fa9e6e5885372bf083d8295421\n--- a/docs/agents/domain.md\n+++ b/docs/agents/domain.md\n@@ -5,7 +5,9 @@\n ## Layout\n \n - `CONTEXT.md` defines Shiki language. Keep it implementation-free.\n-- `docs/adr/` records hard-to-reverse decisions.\n+- `docs/adr/SADR-NNNN-*.md` records Shiki platform decisions.\n+- `docs/adr/NNNN-*.md` records target product decisions without sharing the\n+  SADR namespace.\n - `AGENTS.md` is the runtime-neutral agent constitution.\n - `CLAUDE.md` is the Claude Code wrapper around `AGENTS.md`.\n - `.shiki/` is the Target Repository mirror for Goals, PRDs, plans, Task DAGs, contracts, locks, worktree records, ledger entries, CCA verdicts, repair packets, reports, and handoffs.\n@@ -15,6 +17,6 @@\n \n - Use Shiki terms exactly when they exist.\n - Add glossary terms to `CONTEXT.md` only when the term is part of the domain, not a generic programming concept.\n-- Create ADRs only for hard-to-reverse, surprising, tradeoff-based decisions.\n+- Create SADRs or target ADRs only for hard-to-reverse, surprising, tradeoff-based decisions.\n - Keep product-specific language in the Target Repository, not in Shiki Template docs.\n-- If an ADR conflicts with a proposed implementation, surface the conflict before editing.\n+- If a SADR or target ADR conflicts with a proposed implementation, surface the conflict before editing.\ndiff --git a/docs/agents/execution-control.md b/docs/agents/execution-control.md\nindex ad8ee3cdb36be56fedc1f46408ca92b387d74047..bc63c3a73661d12b567016ba299f1a6b66bf9088\n--- a/docs/agents/execution-control.md\n+++ b/docs/agents/execution-control.md\n@@ -63,7 +63,7 @@\n | Branch name | Orchestrator | Orchestrator | Before implementer dispatch | branch policy | branch ref in task/ledger |\n | Lock reservation | Context & Impact | MergeGate | Before dispatch and before merge | `.shiki/locks/`, labels, PR metadata | lock id, owner, scope |\n | CI profile | Planner | MergeGate | During issue triage, rechecked at PR | labels, `.shiki/policy.yaml`, workflow matrix | required check list |\n-| Required status checks | Repo maintainer / Guardian | GitHub branch protection / rulesets | Repository setup, updated via ADR | GitHub settings | branch/ruleset config, ADR |\n+| Required status checks | Repo maintainer / Guardian | GitHub branch protection / rulesets | Repository setup, updated via target ADR | GitHub settings | branch/ruleset config, target ADR |\n | CCA judgment | CCA | GitHub required check / MergeGate | Every non-draft PR update | GitHub Action check | structured CCA verdict |\n | Repair assignment | CCA | MergeGate | After failed CCA/CI/review | repair packet, issue/PR comment | repair id and attempt count |\n | Merge readiness | MergeGate | GitHub branch protection / Guardian if needed | After CCA complete | required checks, review rules, rulesets | mergegate check output |\ndiff --git a/docs/agents/guardian-policy.md b/docs/agents/guardian-policy.md\nindex 4147c7adf05a47adb4fd24ada1a1c2e1f2ba6d2d..a20d9cc6c00a569bb057b9ca8d6b51c7fc4a9847\n--- a/docs/agents/guardian-policy.md\n+++ b/docs/agents/guardian-policy.md\n@@ -98,8 +98,8 @@\n `origin/<default_branch>` and reuses each job's existing base fetch; a shallow\n (`--depth`) or second base fetch would make the proof unresolvable and silently\n disable the carry, so neither is permitted. CCA reads `guardian_comment_carried` as\n-a recorded authority in the deterministic signal and does not re-derive it. See ADR\n-0018 for the full decision, the rejected diff/patch-equality and path-disjointness\n+a recorded authority in the deterministic signal and does not re-derive it. See\n+SADR-0018 for the full decision, the rejected diff/patch-equality and path-disjointness\n alternatives, and the accepted residual.\n \n ## External AI Guardian Review (SADR-0010)\ndiff --git a/docs/agents/implementation-policy.md b/docs/agents/implementation-policy.md\nindex c754144a9fb5bf4a38aa82a60e1670c59e626b44..85c37281bcad196c6b7b02343faf00b03cd9c57a\n--- a/docs/agents/implementation-policy.md\n+++ b/docs/agents/implementation-policy.md\n@@ -31,10 +31,13 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n ## Non-Negotiables\n \n 1. **Planning precedes implementation.** Non-trivial Goals start with `grill-with-docs` and Context & Impact.\n-2. **Domain language is controlled.** Terms must be checked against `CONTEXT.md`; hard-to-reverse tradeoffs must be checked against ADRs.\n+2. **Domain language is controlled.** Terms must be checked against `CONTEXT.md`; hard-to-reverse tradeoffs must be checked against Shiki SADRs and target ADRs.\n 3. **PRD precedes issue decomposition.** `to-prd` is used once the context is settled enough to become durable intent.\n 4. **Issues are vertical slices.** `to-issues` must produce independently grabbable tracer-bullet tasks, not horizontal layer tickets.\n 5. **Triage controls dispatch.** Only `ready-for-agent` / AFK tasks may be assigned to Codex without human input.\n@@ -95,19 +98,19 @@\n Process:\n \n - Challenge terms against `CONTEXT.md`.\n-- Check ADRs before reopening decisions.\n+- Check SADRs and target ADRs before reopening decisions.\n - Inspect code when code can answer a question.\n - Ask one question at a time when the operator is available.\n - Provide a recommended answer with each question.\n - Record resolved terms in `CONTEXT.md`.\n-- Create ADRs only for hard-to-reverse, surprising, tradeoff-based decisions.\n+- Create SADRs or target ADRs only for hard-to-reverse, surprising, tradeoff-based decisions.\n - Convert unresolved load-bearing questions into HITL blockers.\n \n Outputs:\n \n - Resolved design-tree decisions.\n - Updated glossary terms when needed.\n-- ADRs when justified.\n+- SADRs or target ADRs when justified.\n - HITL blockers when decisions remain unresolved.\n \n ## Phase 3 \u2014 Context & Impact\n@@ -117,7 +120,7 @@\n \n Outputs must include:\n \n-- Relevant documents and ADRs.\n+- Relevant documents, SADRs, and target ADRs.\n - Relevant modules, interfaces, seams, callers, and tests.\n - Candidate locks.\n - Dependencies.\n@@ -140,7 +143,7 @@\n - Testing decisions.\n - Out of scope.\n - Further notes.\n-- Links to relevant Goal, domain terms, and ADRs.\n+- Links to relevant Goal, domain terms, SADRs, and target ADRs.\n \n Phase 4 ends with Spec Freeze: the operator explicitly approves the PRD, the\n plan records a `spec_freeze` block (status, approved_by, source), and the SF\n@@ -240,7 +243,7 @@\n - Dependencies and locks.\n - Risk and Guardian requirements.\n - Ledger completeness.\n-- Documentation and ADR updates.\n+- Documentation and SADR/target-ADR updates.\n - Scope drift.\n \n CCA emits one of:\n@@ -301,7 +304,7 @@\n | Evidence failure | Behavior might be done, but proof is missing. | Add checks, PR evidence, ledger entry, or artifacts. |\n | Scope drift | PR changes unrelated behavior. | Revert unrelated changes or split into new Goal. |\n | Dependency block | Upstream task or lock not complete. | Block until dependency resolves. |\n-| Architecture block | Decision needs ADR or deep-module discussion. | `grill-with-docs` or `improve-codebase-architecture`. |\n+| Architecture block | Decision needs a SADR, target ADR, or deep-module discussion. | `grill-with-docs` or `improve-codebase-architecture`. |\n | Risk block | Security/production/policy change needs human approval. | Guardian decision. |\n \n ## Phase 5.5 \u2014 Execution Decision Control\ndiff --git a/docs/agents/runtime-auth-model.md b/docs/agents/runtime-auth-model.md\nindex 6a2e1a47e81ea89b5c68a210ec07772a9374babc..3c54748d24233dbd0ba8473950282547ef4ce935\n--- a/docs/agents/runtime-auth-model.md\n+++ b/docs/agents/runtime-auth-model.md\n@@ -148,7 +148,7 @@\n \n API-key based runners may be added by a target repository as an explicit extension. They are not the default Shiki template path.\n \n-If a repo opts into API-key mode, record the exception in an ADR and update the repo's `AGENTS.md`, `.shiki/config.yaml`, and workflow permissions.\n+If a repo opts into API-key mode, record the target-specific exception in a target ADR and update the repo's `AGENTS.md`, `.shiki/config.yaml`, and workflow permissions.\n \n ## GitHub Actions Runtime Compatibility\n \ndiff --git a/docs/agents/skill-gate.md b/docs/agents/skill-gate.md\nindex c50398b0d7c60a8598f2a5c88a10015730dced13..3acffe6444617bb1d54b77d4b4708158ec735bc6\n--- a/docs/agents/skill-gate.md\n+++ b/docs/agents/skill-gate.md\n@@ -9,7 +9,7 @@\n | Skill | Trigger | Expected output |\n | --- | --- | --- |\n | `setup-matt-pocock-skills` | First configuring a repo for these skills, or when issue tracker, triage labels, or domain docs are missing. | `docs/agents/` configuration and an `Agent skills` block in `AGENTS.md` or `CLAUDE.md`. |\n-| `grill-with-docs` | Ambiguous plan, unclear terminology, boundary decision, tradeoff, or ADR-worthy design choice. | Clarified decision tree, recommended answers, updates to `CONTEXT.md` or ADRs when decisions crystallize. |\n+| `grill-with-docs` | Ambiguous plan, unclear terminology, boundary decision, tradeoff, or decision-record-worthy design choice. | Clarified decision tree, recommended answers, updates to `CONTEXT.md`, SADRs, or target ADRs when decisions crystallize. |\n | `zoom-out` | Agent lacks a higher-level architectural map. | Map of relevant modules, callers, seams, and domain concepts. |\n | `to-prd` | Settled context should become a PRD. | PRD published to the issue tracker with problem, solution, user stories, decisions, testing, and out-of-scope notes. |\n | `to-issues` | Goal, PRD, or plan must become implementation tickets. | Vertical-slice issues with acceptance criteria, dependencies, and AFK/HITL classification. |\n@@ -17,7 +17,7 @@\n | `tdd` | Feature work or bug fix has observable behavior that can be specified with tests. | Red-green-refactor loop through public interfaces, one vertical slice at a time. |\n | `code-review` | The TDD loop is green and the implementer is about to create or update a PR. Default for every implementation task. | Pre-PR self-review of the task diff with fixes applied on the task branch; findings and resolutions recorded as a ledger entry naming the skill and a `## Pre-PR code review` PR body section. |\n | `diagnose` | Hard bug, failing check, regression, flaky behavior, or performance problem. | Reproduction loop, ranked hypotheses, instrumentation, fix, regression test, post-mortem. |\n-| `improve-codebase-architecture` | Structural friction, testability, AI-navigability, deep-module opportunity, or refactor request. | Architecture candidates and deepening recommendations informed by `CONTEXT.md` and ADRs. |\n+| `improve-codebase-architecture` | Structural friction, testability, AI-navigability, deep-module opportunity, or refactor request. | Architecture candidates and deepening recommendations informed by `CONTEXT.md`, SADRs, and target ADRs. |\n | `prototype` | A throwaway logic or UI prototype is needed to answer a design question. | Clearly marked disposable prototype and captured decision. |\n \n ## MergeGate Interaction\ndiff --git a/docs/operations.md b/docs/operations.md\nindex cf1f452d4c589e38ccd58553bda0ad8684b51eb7..ace48a90ffbec9e5c0a7113bef16a4858bc44b3f\n--- a/docs/operations.md\n+++ b/docs/operations.md\n@@ -89,6 +89,16 @@\n      target history, brought forward by `shiki migrate`, not a template.\n    - **Everything else** (CLI scripts, workflows, schemas, docs) is overwritten.\n \n+   A forced upgrade also migrates the former numeric Shiki decision paths to\n+   `SADR-NNNN-*`. Before the first write, the installer inventories the exact\n+   legacy Shiki paths and authorizes each deletion only when the existing\n+   `.shiki/install-stamp.json` names that path and its SHA-256 digest still\n+   matches the file. An absent, unreadable, malformed, incomplete, or\n+   mismatching stamp reports every blocked path and stops with no filesystem\n+   change. Target-owned `NNNN-*` ADRs are not cleanup candidates. A non-force\n+   install deletes nothing and retains any still-matching legacy stamp entries\n+   so a later forced upgrade can prove ownership.\n+\n    If the target has pending migrations, `--force` refuses and writes nothing \u2014\n    apply migrations first (step 3), then re-run. The run ends with a single\n    summary of every `.new` written, so a half-upgrade is visible.\ndiff --git a/docs/releasing.md b/docs/releasing.md\nindex 10d73828e7dbd64e9d9f6b83bc84d010537ce62a..aafeee8c4d441e1e427efee2ce4f81a647f020e1\n--- a/docs/releasing.md\n+++ b/docs/releasing.md\n@@ -67,7 +67,7 @@\n CLI in `scripts/`. There are no third-party Python runtime dependencies. The CLI\n is intentionally **not** exposed as a `[project.scripts]` console entry point\n because `scripts/shiki.py` is a dependency-free shim and `scripts/` is not\n-packaged as an importable distribution (see the ADR for the rationale).\n+packaged as an importable distribution (see SADR-0007 for the rationale).\n \n Supported install paths:\n \n@@ -155,6 +155,15 @@\n    [docs/operations.md](operations.md#the-new-convention). If the target has\n    pending migrations, `--force` refuses and writes nothing; apply migrations\n    (step 2) first, then re-run.\n+\n+   The SADR namespace upgrade has an additional pre-write gate: each former\n+   numeric Shiki decision path is removed only when the existing install stamp\n+   contains that exact path and a digest matching its current bytes. The\n+   installer aggregates absent/unreadable/malformed-stamp, missing-digest, and\n+   digest-mismatch blockers before raising, so refusal leaves the target tree\n+   byte-identical. Numeric target ADRs are preserved. A successful forced\n+   upgrade installs and stamps `SADR-*` paths and drops the removed legacy paths\n+   from the refreshed stamp.\n 2. Apply repository-local `.shiki/` state migrations with the migrate command:\n \n    ```bash\ndiff --git a/docs/usage.md b/docs/usage.md\nindex b83407ee3970dd395dd893010bc5dbe8bad26051..a130ef6e40dfd8169a5bc701339351097df1a09f\n--- a/docs/usage.md\n+++ b/docs/usage.md\n@@ -67,8 +67,10 @@\n shiki install-target /path/to/target-repo --local-only\n ```\n \n-Use `--force` only when you intentionally want to overwrite existing target\n-files.\n+Use `--force` only when you intentionally want to refresh the target template.\n+For the SADR namespace migration, `--force` deletes only exact legacy Shiki\n+decision paths whose existing install-stamp digest still matches; any ownership\n+blocker stops before the first write. Numeric target ADRs are never candidates.\n \n ## `shiki bootstrap-platform` \u2014 publish the Shiki platform repo\n \ndiff --git a/scripts/guardian_approval_signal.py b/scripts/guardian_approval_signal.py\nindex d059f92b2c82aa2b5d6ca421365537a87886c1cb..16924892714f4f5b9303c0ef0e325a7e70075630\n--- a/scripts/guardian_approval_signal.py\n+++ b/scripts/guardian_approval_signal.py\n@@ -199,8 +199,8 @@\n ) -> Any:\n     \"\"\"Evaluate SADR-0015 Contract Approval for the CCA Guardian signal.\n \n-    The SAME OR that MergeGate applies must reach this deterministic signal: ADR\n-    0015 is explicit that without it, a contract-approved PR still returns\n+    The SAME OR that MergeGate applies must reach this deterministic signal:\n+    SADR-0015 is explicit that without it, a contract-approved PR still returns\n     ``needs_guardian``, the CCA job fails, and the MergeGate policy check never\n     runs \u2014 the gate this change moves would never be reached. This resolves the\n     task the SAME way MergeGate does (the first Shiki task id in the PR body),\ndiff --git a/scripts/shiki_contract_approval.py b/scripts/shiki_contract_approval.py\nindex 941bcb96fdd8b9a85bc0c4f0f04d5dad9d04839c..4a4b418f6527413161db82670bec857a043360e2\n--- a/scripts/shiki_contract_approval.py\n+++ b/scripts/shiki_contract_approval.py\n@@ -142,7 +142,7 @@\n     changed_files_status: Any,\n     registration: Any,\n ) -> ContractApprovalResult:\n-    \"\"\"Whether this PR carries an SADR-0015 Contract Approval.\n+    \"\"\"Whether this PR carries a SADR-0015 Contract Approval.\n \n     The four conditions are evaluated IN ORDER, each failure recorded as a\n     distinct reason string:\ndiff --git a/scripts/shiki_guardian.py b/scripts/shiki_guardian.py\nindex a4ecb79bbda55fd5c8b06adfae2eb99760a03c45..4dd99bbe714e26f421fa19abac7a34ae1b195ecc\n--- a/scripts/shiki_guardian.py\n+++ b/scripts/shiki_guardian.py\n@@ -12,7 +12,7 @@\n # The carry re-derives its proof from git in-process on every judging invocation;\n # it is never read from an artifact, so there is nothing to go stale or be forged.\n # shiki_sync_proof is the ONLY guardian-family module that runs git, and every one\n-# of its invocations is read-only and local (ADR: prove-from-git base sync).\n+# of its invocations is read-only and local (SADR: prove-from-git base sync).\n from shiki_sync_proof import verify_pure_base_sync\n \n GUARDIAN_POLICY_PATH = \".shiki/guardian-policy.json\"\ndiff --git a/scripts/shiki_guardian_review.py b/scripts/shiki_guardian_review.py\nindex dd0f104d457899e0ba5ff1e162011c1e90b6f92b..e10ef58c046324e7ea3a3392feb3048ca77414ce\n--- a/scripts/shiki_guardian_review.py\n+++ b/scripts/shiki_guardian_review.py\n@@ -159,7 +159,7 @@\n     docs_only = _is_docs_only(changed_files)\n     if docs_only and matched == 0:\n         focus.append(\n-            \"Docs/ADR-only change: confirm the change is genuinely documentation/decision-only, introduces no behavior or contract drift, and stays consistent with the constitution and existing ADRs.\"\n+            \"Docs/decision-record-only change: confirm the change is genuinely documentation/decision-only, introduces no behavior or contract drift, and stays consistent with the constitution and existing SADRs and target ADRs.\"\n         )\n \n     # Conservative augmentation: unknown (no category and not docs-only), mixed\ndiff --git a/scripts/shiki_installer.py b/scripts/shiki_installer.py\nindex be064ab7a29ff6836f7f7a1d70500a43e0d11d7b..9fd8eefd137ce7003dad5c0dcb2e48e2cd47ab32\n--- a/scripts/shiki_installer.py\n+++ b/scripts/shiki_installer.py\n@@ -118,6 +118,7 @@\n # what lets a target carry it without a governance-schema change.\n INSTALL_STAMP_PATH = \".shiki/install-stamp.json\"\n INSTALL_STAMP_VERSION = 1\n+LEGACY_SHIKI_SADR_MAX = 18\n \n # Under --force the shipped surface splits three ways, so nothing is lost and\n # nothing is silently stale.\n@@ -237,9 +238,98 @@\n         )\n \n \n+def legacy_shiki_adr_paths() -> tuple[str, ...]:\n+    \"\"\"Former numeric paths for Shiki SADR-0001 through SADR-0018.\n+\n+    The authoritative slugs come from the current SADR filenames. SADR-0019 is\n+    the namespace decision itself and never shipped under a numeric legacy path.\n+    \"\"\"\n+    paths: list[str] = []\n+    for sadr in sorted((ROOT / \"docs\" / \"adr\").glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\")):\n+        number = sadr.name.split(\"-\", 2)[1]\n+        if int(number) <= LEGACY_SHIKI_SADR_MAX:\n+            paths.append(f\"docs/adr/{sadr.name.removeprefix('SADR-')}\")\n+    return tuple(paths)\n+\n+\n+def preflight_legacy_sadr_cleanup(target: Path, *, force: bool) -> tuple[str, ...]:\n+    \"\"\"Authorize exact legacy Shiki ADR removals before the first write.\n+\n+    A non-force install never removes existing files. A forced install needs no\n+    stamp when no legacy Shiki path is present. When legacy paths do exist, each\n+    must be named in a readable install stamp and its current bytes must match\n+    the recorded digest.\n+    \"\"\"\n+    if not force:\n+        return ()\n+    present = tuple(\n+        relative\n+        for relative in legacy_shiki_adr_paths()\n+        if (target / relative).exists() or (target / relative).is_symlink()\n+    )\n+    if not present:\n+        return ()\n+\n+    stamp_path = target / INSTALL_STAMP_PATH\n+    try:\n+        raw = stamp_path.read_text(encoding=\"utf-8\")\n+        stamp = json.loads(raw)\n+    except FileNotFoundError:\n+        detail = \"install stamp is absent\"\n+        blockers = [f\"{relative}: {detail}\" for relative in present]\n+    except (OSError, UnicodeDecodeError) as error:\n+        detail = f\"install stamp is unreadable ({error})\"\n+        blockers = [f\"{relative}: {detail}\" for relative in present]\n+    except json.JSONDecodeError as error:\n+        detail = f\"install stamp is malformed ({error})\"\n+        blockers = [f\"{relative}: {detail}\" for relative in present]\n+    else:\n+        if not isinstance(stamp, dict):\n+            blockers = [\n+                f\"{relative}: install stamp is malformed (expected an object)\"\n+                for relative in present\n+            ]\n+        else:\n+            digests = stamp.get(\"digests\")\n+            blockers = []\n+            if not isinstance(digests, dict):\n+                blockers.extend(\n+                    f\"{relative}: install stamp has no digest for this path\"\n+                    for relative in present\n+                )\n+            else:\n+                for relative in present:\n+                    expected = digests.get(relative)\n+                    if not isinstance(expected, str) or not expected:\n+                        blockers.append(\n+                            f\"{relative}: install stamp has no digest for this path\"\n+                        )\n+                        continue\n+                    try:\n+                        actual = _sha256_file(target / relative)\n+                    except OSError as error:\n+                        blockers.append(\n+                            f\"{relative}: legacy path is unreadable ({error})\"\n+                        )\n+                        continue\n+                    if actual != expected:\n+                        blockers.append(\n+                            f\"{relative}: digest mismatch (stamp {expected}, current {actual})\"\n+                        )\n+\n+    if blockers:\n+        joined = \"\\n\".join(f\"- {blocker}\" for blocker in blockers)\n+        raise ShikiError(\n+            \"legacy Shiki ADR cleanup is not authorized:\\n\"\n+            f\"{joined}\\nNo files were written.\"\n+        )\n+    return present\n+\n+\n def install_template(target: Path, *, force: bool, validate: bool) -> list[NewFileNote]:\n     # Preconditions first: never rewrite the tree of a target that cannot be\n     # upgraded cleanly (see refuse_pending_migrations).\n+    legacy_cleanup = preflight_legacy_sadr_cleanup(target, force=force)\n     refuse_pending_migrations(target)\n \n     # Resolve the target's required CODEOWNERS owner once from its\n@@ -250,6 +340,10 @@\n     # verbatim. See ``codeowners_required_owner``.\n     codeowners_owner = codeowners_required_owner(target)\n \n+    for relative in legacy_cleanup:\n+        (target / relative).unlink()\n+        info(f\"removed stamped legacy Shiki ADR: {target / relative}\")\n+\n     notes: list[NewFileNote] = []\n     for relative in TEMPLATE_PATHS:\n         source = ROOT / relative\n@@ -647,6 +741,22 @@\n     digests = target_stamp_digests(target)\n     existing = read_install_stamp(target)\n     if existing is not None:\n+        # A non-force install deliberately leaves legacy Shiki ADRs in place.\n+        # Preserve only still-matching ownership proof so a later forced\n+        # upgrade can authorize cleanup; never carry a missing or stale digest.\n+        existing_digests = existing.get(\"digests\")\n+        if isinstance(existing_digests, dict):\n+            for relative in legacy_shiki_adr_paths():\n+                expected = existing_digests.get(relative)\n+                path = target / relative\n+                if not isinstance(expected, str) or not path.is_file():\n+                    continue\n+                try:\n+                    actual = _sha256_file(path)\n+                except OSError:\n+                    continue\n+                if actual == expected:\n+                    digests[relative] = actual\n         if _incoming_is_older(commit, existing.get(\"platform_commit\")):\n             warn(\n                 \"kept existing install stamp: incoming install \"\ndiff --git a/scripts/shiki_loop.py b/scripts/shiki_loop.py\nindex 3611641da8aeb5038b1da90e3114b09a473ec6d8..84caa491b9cad0edc4176f209c13e9221e975681\n--- a/scripts/shiki_loop.py\n+++ b/scripts/shiki_loop.py\n@@ -424,7 +424,7 @@\n         # unresolvable CCA verdict has already produced its terminal stop and is never\n         # laundered into a head-moving sync (SADR-0010). It also never fires while the\n         # policy (Guardian) gate is red \u2014 for ANY reason, not only when it is the sole\n-        # red check \u2014 so a head-bound Guardian approval, or an SADR-0015\n+        # red check \u2014 so a head-bound Guardian approval, or a SADR-0015\n         # contract-approval carry that base movement defeated (the policy gate compares\n         # base and head task governance directly), is never silently restored by\n         # adopting base wholesale. It is bounded by a durable per-task counter so the\ndiff --git a/scripts/shiki_tasks.py b/scripts/shiki_tasks.py\nindex aec3809f24b4a0c8c96fc50971cff9ae9ad30381..d531052e9d9717058d4631483cefab44c26a65c1\n--- a/scripts/shiki_tasks.py\n+++ b/scripts/shiki_tasks.py\n@@ -821,7 +821,7 @@\n         \"prompt\": prompt,\n         \"entry_skill\": \"grill-with-docs\",\n         \"required_next_steps\": [\n-            \"Run grill-with-docs until terminology, boundaries, risks, and ADR-worthy decisions are settled.\",\n+            \"Run grill-with-docs until terminology, boundaries, risks, and decision-record-worthy choices are settled.\",\n             \"Obtain the operator's explicit approval of the PRD/requirements (Spec Freeze).\",\n             \"Write a machine-readable plan JSON with grill_with_docs.status=complete and spec_freeze.status=frozen.\",\n             \"Run shiki plan ingest --plan-file PLAN.json.\",\ndiff --git a/scripts/test_shiki_goal_loop.sh b/scripts/test_shiki_goal_loop.sh\nindex 753c7b516e10f0be3bc92a4bd0eb1fa699776ae6..8f3820a0a079b3044424cfe1a6c6b7fe68896e67\n--- a/scripts/test_shiki_goal_loop.sh\n+++ b/scripts/test_shiki_goal_loop.sh\n@@ -231,8 +231,8 @@\n \n # High/critical risk merges autonomously when all required checks are green:\n # the \"MergeGate policy check\" required check IS the Guardian gate (it enforces\n-# guardian-policy.json \u2014 human approval OR external AI guardian review, ADR\n-# 0010), so green checks mean a recorded authority approved. The fake gh returns\n+# guardian-policy.json \u2014 human approval OR external AI guardian review,\n+# SADR-0010), so green checks mean a recorded authority approved. The fake gh returns\n # green checks, simulating that the Guardian gate passed.\n python3 \"$ROOT/scripts/shiki.py\" goal create --target \"$TARGET\" --title \"Guardian gate\" --outcome \"High risk merges via policy gate\" >/tmp/shiki-goal-loop-guardian-goal.json\n GGOAL=\"$(json_get /tmp/shiki-goal-loop-guardian-goal.json goal_id)\"\ndiff --git a/tests/test_adr_references.py b/tests/test_adr_references.py\nindex 51706329c797d5dd9b47e082a333fa83b261d482..d005da4f771f5e8085ec22d4b787980e68f5ae8c\n--- a/tests/test_adr_references.py\n+++ b/tests/test_adr_references.py\n@@ -1,8 +1,8 @@\n-\"\"\"Every ``ADR ####`` citation must resolve to exactly one decision record.\n+\"\"\"Every target ADR and Shiki SADR citation resolves in its own namespace.\n \n Background. ``scripts/mergegate_check.py`` justifies ``is_bookkeeping_closeout``\n-with \"ADR 0017\" nine times and ``scripts/guardian_approval_signal.py`` cites\n-0016/0017 seven times, yet for a stretch the numbering in ``docs/adr/`` stopped\n+with \"SADR-0017\" nine times and ``scripts/guardian_approval_signal.py`` cites\n+SADR-0016/SADR-0017 seven times, yet for a stretch the numbering in ``docs/adr/`` stopped\n at 0015: the authoring commits lived on a coordinator branch and no task ever\n declared ``docs/adr/...`` in its locks, so nothing carried the records to the\n default branch. The stated basis of an exemption that merged seven PRs with zero\n@@ -11,11 +11,12 @@\n and the directory drifted with no signal.\n \n This is the same unbound-pair shape behind other silent failures: a reference\n-and its target with nothing asserting they agree. This test binds them. It scans\n-every ``scripts/*.py`` (plus ``docs/**/*.md`` and the root ``*.md`` governance\n-files, which are cheap) for the ``ADR ####`` pattern and asserts each cited\n-number resolves to exactly one ``docs/adr/####-*.md``. The converse is\n-deliberately NOT required: an ADR may exist without being cited.\n+and its target with nothing asserting they agree. This test binds them. Target\n+``ADR NNNN`` citations resolve only to ``docs/adr/NNNN-*.md``; Shiki\n+``SADR-NNNN`` citations resolve only to ``docs/adr/SADR-NNNN-*.md``. The scan\n+covers current governance docs, scripts, workflows, prompts, packaging metadata,\n+and repo-local Codex guidance while deliberately excluding historical\n+``.shiki`` evidence. The converse is not required: a record may be uncited.\n \n The suite is dependency free: standard-library ``unittest`` only.\n \"\"\"\n@@ -31,17 +32,18 @@\n \n REPO_ROOT = Path(__file__).resolve().parents[1]\n \n-# \"ADR 0016\" -- a four-digit citation. The trailing \\b keeps a five-digit token\n-# from being read as a truncated four-digit number.\n-_CITATION = re.compile(r\"\\bADR (\\d{4})\\b\")\n-# \"0016-locks-bound-product-paths.md\" -- the leading four digits name the record.\n-_ADR_FILENAME = re.compile(r\"^(\\d{4})-.*\\.md$\")\n+# ``ADR 0016`` / ``SADR-0016`` -- a namespace plus four-digit citation. The\n+# trailing ``\\b`` keeps a five-digit token from being read as a truncated one.\n+_CITATION = re.compile(r\"\\b(S?ADR)[ -](\\d{4})\\b\")\n+# Target ADRs retain the numeric filename; Shiki platform decisions use SADR.\n+_DECISION_FILENAME = re.compile(r\"^(?:(SADR)-)?(\\d{4})-.*\\.md$\")\n \n \n class Violation(NamedTuple):\n     \"\"\"A citation that does not resolve to exactly one decision record.\"\"\"\n \n     citing_file: str  # repo-relative path of the file that cites the ADR\n+    namespace: str  # \"ADR\" for target decisions; \"SADR\" for Shiki decisions\n     number: str  # the cited four-digit number, e.g. \"0016\"\n     line: int  # 1-based line of the citation\n     reason: str  # \"missing\" (zero matches) or \"ambiguous\" (two or more)\n@@ -49,44 +51,59 @@\n \n     @property\n     def expected_path(self) -> str:\n-        return f\"docs/adr/{self.number}-*.md\"\n+        prefix = \"SADR-\" if self.namespace == \"SADR\" else \"\"\n+        return f\"docs/adr/{prefix}{self.number}-*.md\"\n \n     def describe(self) -> str:\n         \"\"\"Name the citing file, the cited number, and the missing path.\"\"\"\n         if self.reason == \"missing\":\n             return (\n-                f\"{self.citing_file}:{self.line} cites ADR {self.number} \"\n+                f\"{self.citing_file}:{self.line} cites {self.namespace} {self.number} \"\n                 f\"but no document matches {self.expected_path}\"\n             )\n         joined = \", \".join(self.matches)\n         return (\n-            f\"{self.citing_file}:{self.line} cites ADR {self.number} which \"\n+            f\"{self.citing_file}:{self.line} cites {self.namespace} {self.number} which \"\n             f\"resolves to {len(self.matches)} documents (expected exactly one \"\n             f\"at {self.expected_path}): {joined}\"\n         )\n \n \n-def _adr_index(root: Path) -> dict[str, list[Path]]:\n-    \"\"\"Map every four-digit ADR number to the record files that claim it.\"\"\"\n-    index: dict[str, list[Path]] = {}\n+def _adr_index(root: Path) -> dict[tuple[str, str], list[Path]]:\n+    \"\"\"Map each decision namespace and number to the files that claim it.\"\"\"\n+    index: dict[tuple[str, str], list[Path]] = {}\n     adr_dir = root / \"docs\" / \"adr\"\n     if adr_dir.is_dir():\n         for path in sorted(adr_dir.glob(\"*.md\")):\n-            match = _ADR_FILENAME.match(path.name)\n+            match = _DECISION_FILENAME.match(path.name)\n             if match:\n-                index.setdefault(match.group(1), []).append(path)\n+                namespace = \"SADR\" if match.group(1) else \"ADR\"\n+                index.setdefault((namespace, match.group(2)), []).append(path)\n     return index\n \n \n def _citing_files(root: Path, include_docs: bool = True) -> list[Path]:\n-    \"\"\"Files scanned for citations: scripts/*.py, and cheaply the markdown.\"\"\"\n+    \"\"\"Current, shipped text surfaces scanned for decision citations.\"\"\"\n     files = sorted((root / \"scripts\").glob(\"*.py\"))\n+    files += sorted((root / \"scripts\").glob(\"*.sh\"))\n     if include_docs:\n         docs = root / \"docs\"\n         if docs.is_dir():\n             files += sorted(docs.rglob(\"*.md\"))\n         files += sorted(root.glob(\"*.md\"))\n-    return files\n+        pyproject = root / \"pyproject.toml\"\n+        if pyproject.is_file():\n+            files.append(pyproject)\n+        for relative in (\".github\", \".codex\"):\n+            surface = root / relative\n+            if surface.is_dir():\n+                files += sorted(\n+                    path\n+                    for path in surface.rglob(\"*\")\n+                    if path.is_file()\n+                    and path.suffix in {\".md\", \".py\", \".sh\", \".toml\", \".yaml\", \".yml\"}\n+                )\n+    return sorted(set(files))\n \n \n def find_adr_reference_violations(\n@@ -94,10 +111,9 @@\n ) -> list[Violation]:\n     \"\"\"Return every ``ADR ####`` citation under ``root`` that fails to resolve.\n \n-    A citation resolves when exactly one ``docs/adr/####-*.md`` matches its\n-    number. Zero matches is a \"missing\" violation; two or more is \"ambiguous\".\n-    Records that are never cited produce nothing -- the converse is not\n-    required.\n+    A citation resolves when exactly one record in the same namespace matches\n+    its number. Zero matches is a \"missing\" violation; two or more is\n+    \"ambiguous\". Records that are never cited produce nothing.\n     \"\"\"\n     root = Path(root)\n     index = _adr_index(root)\n@@ -110,13 +126,15 @@\n         rel = path.relative_to(root).as_posix()\n         for lineno, line in enumerate(text.splitlines(), start=1):\n             for match in _CITATION.finditer(line):\n-                number = match.group(1)\n-                found = index.get(number, [])\n+                namespace, number = match.groups()\n+                found = index.get((namespace, number), [])\n                 if len(found) == 1:\n                     continue\n                 reason = \"missing\" if not found else \"ambiguous\"\n                 rels = [p.relative_to(root).as_posix() for p in found]\n-                violations.append(Violation(rel, number, lineno, reason, rels))\n+                violations.append(\n+                    Violation(rel, namespace, number, lineno, reason, rels)\n+                )\n     return violations\n \n \n@@ -129,8 +147,8 @@\n         self.assertEqual(\n             violations,\n             [],\n-            \"Every 'ADR ####' citation must resolve to exactly one \"\n-            f\"docs/adr/####-*.md:\\n{detail}\",\n+            \"Every ADR/SADR citation must resolve exactly once in its own \"\n+            f\"docs/adr namespace:\\n{detail}\",\n         )\n \n     def test_scan_actually_reads_scripts(self) -> None:\n@@ -142,12 +160,38 @@\n             \"the citation scan must cover scripts/*.py\",\n         )\n \n+    def test_platform_citations_do_not_use_unresolved_numeric_shorthand(self) -> None:\n+        shorthand = re.compile(r\"SADR-\\d{4}/\\d{4}\")\n+        offenders = []\n+        for path in _citing_files(REPO_ROOT):\n+            text = path.read_text(encoding=\"utf-8\")\n+            if shorthand.search(text):\n+                offenders.append(path.relative_to(REPO_ROOT).as_posix())\n+        self.assertEqual(\n+            offenders,\n+            [],\n+            \"repeat the SADR prefix so every cited number is independently resolvable\",\n+        )\n+\n     def test_carried_records_are_indexed(self) -> None:\n         # The two records this Goal carried onto the default branch, and which\n         # the scripts cite most, must be present.\n         index = _adr_index(REPO_ROOT)\n-        self.assertIn(\"0016\", index)\n-        self.assertIn(\"0017\", index)\n+        self.assertIn((\"SADR\", \"0016\"), index)\n+        self.assertIn((\"SADR\", \"0017\"), index)\n+\n+    def test_platform_tree_has_only_sadr_0001_through_0019(self) -> None:\n+        adr_dir = REPO_ROOT / \"docs\" / \"adr\"\n+        sadrs = sorted(adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\"))\n+        self.assertEqual(\n+            {path.name.split(\"-\", 2)[1] for path in sadrs},\n+            {f\"{number:04d}\" for number in range(1, 20)},\n+        )\n+        self.assertEqual(\n+            list(adr_dir.glob(\"[0-9][0-9][0-9][0-9]-*.md\")),\n+            [],\n+            \"the Shiki platform tree must not retain legacy numeric SADR paths\",\n+        )\n \n \n class FixtureAdrReferences(unittest.TestCase):\n@@ -180,6 +224,26 @@\n         self.assertIn(\"9991\", message)  # cited number\n         self.assertIn(\"docs/adr/9991-*.md\", message)  # missing path\n \n+    def test_target_adr_does_not_satisfy_missing_platform_sadr(self) -> None:\n+        root = self._make_repo(\n+            adr_names=[\"0042-target-decision.md\"],\n+            script_body='\"\"\"Target ADR 0042 exists, but platform SADR-0042 does not.\"\"\"\\n',\n+        )\n+        violations = find_adr_reference_violations(root, include_docs=False)\n+        self.assertEqual(len(violations), 1)\n+        self.assertEqual(violations[0].namespace, \"SADR\")\n+        self.assertEqual(violations[0].number, \"0042\")\n+        self.assertEqual(violations[0].expected_path, \"docs/adr/SADR-0042-*.md\")\n+\n+    def test_target_adr_and_platform_sadr_may_reuse_a_number(self) -> None:\n+        root = self._make_repo(\n+            adr_names=[\"0042-target-decision.md\", \"SADR-0042-platform-decision.md\"],\n+            script_body='\"\"\"Target ADR 0042 and platform SADR-0042 are distinct.\"\"\"\\n',\n+        )\n+        self.assertEqual(\n+            find_adr_reference_violations(root, include_docs=False), []\n+        )\n+\n     def test_uncited_adr_is_allowed(self) -> None:\n         # An ADR exists but nothing cites it -> not a violation. The converse\n         # (every ADR must be cited) is explicitly NOT required.\ndiff --git a/tests/test_guardian_review.py b/tests/test_guardian_review.py\nindex 88829e7810212635e2fc98aa5918d6f26294677d..00a85a237acd5bf2d6cb5b294128198c422402ca\n--- a/tests/test_guardian_review.py\n+++ b/tests/test_guardian_review.py\n@@ -162,7 +162,7 @@\n \n     def test_docs_only_is_light_and_not_conservative(self):\n         focus = gr.classify_review_focus([\"docs/adr/0099-x.md\", \"README.md\"], \"\", \"low\")\n-        self.assertTrue(any(\"Docs/ADR-only\" in f for f in focus))\n+        self.assertTrue(any(\"Docs/decision-record-only\" in f for f in focus))\n         self.assertFalse(any(\"conservative default\" in f.lower() for f in focus))\n \n     def test_unknown_category_is_conservative(self):\ndiff --git a/tests/test_policy_job_merged_prs.py b/tests/test_policy_job_merged_prs.py\nindex d45512e74681e5e649279236c7d4e79d5c3da471..b97989f8bd74d091de1a6b5275a20d8dc68c4512\n--- a/tests/test_policy_job_merged_prs.py\n+++ b/tests/test_policy_job_merged_prs.py\n@@ -3,8 +3,8 @@\n \n ``.github/workflows/shiki-cca-completion.yml`` has two consumers of the merged-PR\n proof: the CCA *signal* (``guardian_approval_signal.py`` in \"Prepare CCA context\")\n-and the authoritative gate (``mergegate_check.py`` in \"Enforce MergeGate\"). ADR\n-0017's bookkeeping-closeout exemption (condition 6) needs the merged implementation\n+and the authoritative gate (``mergegate_check.py`` in \"Enforce MergeGate\").\n+SADR-0017's bookkeeping-closeout exemption (condition 6) needs the merged implementation\n PR proven, and a closeout carries NO ``post_merge_reconcile`` marker. The signal\n already resolves ``MERGED_PRS`` unconditionally, but \"Enforce MergeGate\" used to\n resolve it only inside ``grep -q '<!-- shiki:post_merge_reconcile -->'``. So a\ndiff --git a/tests/test_sadr_install_migration.py b/tests/test_sadr_install_migration.py\nnew file mode 100644\nindex 0000000000000000000000000000000000000000..d51eac872b1c79ff4579c4c1401ca5d775a60d8d\n--- /dev/null\n+++ b/tests/test_sadr_install_migration.py\n@@ -0,0 +1,237 @@\n+\"\"\"SADR installation and collision-safe legacy upgrade behavior.\"\"\"\n+\n+from __future__ import annotations\n+\n+import contextlib\n+import hashlib\n+import io\n+import json\n+import shutil\n+import tempfile\n+import unittest\n+from pathlib import Path\n+\n+import shiki_test_support  # noqa: F401  (path bootstrap)\n+\n+import shiki_installer\n+from tests.test_adr_references import find_adr_reference_violations\n+\n+\n+PRODUCT_NUMBERS = tuple(range(13, 19))\n+\n+\n+def _quiet_install(target: Path, *, force: bool, validate: bool) -> None:\n+    with contextlib.redirect_stdout(io.StringIO()):\n+        shiki_installer.install_template(target, force=force, validate=validate)\n+\n+\n+def _sha256(path: Path) -> str:\n+    return hashlib.sha256(path.read_bytes()).hexdigest()\n+\n+\n+def _legacy_sadr_paths() -> tuple[str, ...]:\n+    return tuple(\n+        f\"docs/adr/{path.name.removeprefix('SADR-')}\"\n+        for path in sorted((shiki_installer.ROOT / \"docs\" / \"adr\").glob(\"SADR-*.md\"))\n+        if path.name != \"SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\"\n+    )\n+\n+\n+def _create_legacy_upgrade_fixture(\n+    target: Path,\n+) -> tuple[dict[str, bytes], tuple[str, ...]]:\n+    adr_dir = target / \"docs\" / \"adr\"\n+    adr_dir.mkdir(parents=True)\n+    product_bytes = {\n+        f\"{number:04d}-cubeshare-product-decision.md\": (\n+            f\"# CubeShare ADR {number:04d}\\n\\nDo not rewrite {number}.\\n\"\n+        ).encode()\n+        for number in PRODUCT_NUMBERS\n+    }\n+    for name, content in product_bytes.items():\n+        (adr_dir / name).write_bytes(content)\n+\n+    legacy_paths = _legacy_sadr_paths()\n+    for relative in legacy_paths:\n+        (target / relative).write_bytes(f\"legacy Shiki bytes for {relative}\\n\".encode())\n+\n+    stamp_path = target / shiki_installer.INSTALL_STAMP_PATH\n+    stamp_path.parent.mkdir(parents=True, exist_ok=True)\n+    stamp = {\n+        \"version\": shiki_installer.INSTALL_STAMP_VERSION,\n+        \"platform_commit\": None,\n+        \"installed_at\": \"2000-01-01T00:00:00+00:00\",\n+        \"digests\": {\n+            relative: _sha256(target / relative) for relative in legacy_paths\n+        },\n+    }\n+    stamp_path.write_text(\n+        json.dumps(stamp, indent=2, sort_keys=True) + \"\\n\", encoding=\"utf-8\"\n+    )\n+    return product_bytes, legacy_paths\n+\n+\n+def _tree_snapshot(root: Path) -> dict[str, tuple[str, bytes | str]]:\n+    \"\"\"Recursive byte/shape snapshot used to prove preflight is write-free.\"\"\"\n+    snapshot: dict[str, tuple[str, bytes | str]] = {}\n+    for path in sorted(root.rglob(\"*\")):\n+        relative = path.relative_to(root).as_posix()\n+        if path.is_symlink():\n+            snapshot[relative] = (\"symlink\", path.readlink().as_posix())\n+        elif path.is_dir():\n+            snapshot[relative] = (\"directory\", \"\")\n+        else:\n+            snapshot[relative] = (\"file\", path.read_bytes())\n+    return snapshot\n+\n+\n+class FreshSadrInstallTests(unittest.TestCase):\n+    def setUp(self) -> None:\n+        self.tmp = Path(tempfile.mkdtemp(prefix=\"shiki-sadr-fresh-\"))\n+        self.addCleanup(lambda: shutil.rmtree(self.tmp, ignore_errors=True))\n+        self.target = self.tmp / \"target\"\n+        self.adr_dir = self.target / \"docs\" / \"adr\"\n+        self.adr_dir.mkdir(parents=True)\n+        self.product_bytes = {\n+            f\"{number:04d}-product-decision.md\": (\n+                f\"# Product ADR {number:04d}\\n\\nTarget-owned bytes {number}.\\n\"\n+            ).encode()\n+            for number in PRODUCT_NUMBERS\n+        }\n+        for name, content in self.product_bytes.items():\n+            (self.adr_dir / name).write_bytes(content)\n+\n+    def test_fresh_install_preserves_product_adrs_and_installs_all_sadrs(self) -> None:\n+        _quiet_install(self.target, force=False, validate=True)\n+\n+        for name, expected in self.product_bytes.items():\n+            self.assertEqual((self.adr_dir / name).read_bytes(), expected)\n+\n+        sadr_numbers = {\n+            path.name.split(\"-\", 2)[1]\n+            for path in self.adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\")\n+        }\n+        self.assertEqual(sadr_numbers, {f\"{number:04d}\" for number in range(1, 20)})\n+\n+        numeric_records = {\n+            path.name\n+            for path in self.adr_dir.glob(\"[0-9][0-9][0-9][0-9]-*.md\")\n+        }\n+        self.assertEqual(numeric_records, set(self.product_bytes))\n+        stamp = json.loads(\n+            (self.target / shiki_installer.INSTALL_STAMP_PATH).read_text(\n+                encoding=\"utf-8\"\n+            )\n+        )\n+        for path in self.adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\"):\n+            relative = path.relative_to(self.target).as_posix()\n+            self.assertEqual(stamp[\"digests\"][relative], _sha256(path))\n+        self.assertEqual(find_adr_reference_violations(self.target), [])\n+\n+\n+class LegacySadrUpgradeTests(unittest.TestCase):\n+    def setUp(self) -> None:\n+        self.tmp = Path(tempfile.mkdtemp(prefix=\"shiki-sadr-upgrade-\"))\n+        self.addCleanup(lambda: shutil.rmtree(self.tmp, ignore_errors=True))\n+        self.target = self.tmp / \"target\"\n+        self.adr_dir = self.target / \"docs\" / \"adr\"\n+        self.product_bytes, self.legacy_paths = _create_legacy_upgrade_fixture(\n+            self.target\n+        )\n+\n+    def test_force_upgrade_deletes_only_stamped_legacy_sadrs(self) -> None:\n+        _quiet_install(self.target, force=True, validate=True)\n+\n+        for relative in self.legacy_paths:\n+            self.assertFalse((self.target / relative).exists(), relative)\n+        for name, expected in self.product_bytes.items():\n+            self.assertEqual((self.adr_dir / name).read_bytes(), expected)\n+\n+        sadrs = list(self.adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\"))\n+        self.assertEqual(len(sadrs), 19)\n+        refreshed = json.loads(\n+            (self.target / shiki_installer.INSTALL_STAMP_PATH).read_text(encoding=\"utf-8\")\n+        )\n+        for relative in self.legacy_paths:\n+            self.assertNotIn(relative, refreshed[\"digests\"])\n+        for path in sadrs:\n+            relative = path.relative_to(self.target).as_posix()\n+            self.assertEqual(refreshed[\"digests\"][relative], _sha256(path))\n+\n+    def test_non_force_install_never_deletes_legacy_files(self) -> None:\n+        before = {\n+            relative: (self.target / relative).read_bytes()\n+            for relative in self.legacy_paths\n+        }\n+        _quiet_install(self.target, force=False, validate=False)\n+        for relative, expected in before.items():\n+            self.assertEqual((self.target / relative).read_bytes(), expected)\n+        refreshed = json.loads(\n+            (self.target / shiki_installer.INSTALL_STAMP_PATH).read_text(encoding=\"utf-8\")\n+        )\n+        for relative in self.legacy_paths:\n+            self.assertEqual(refreshed[\"digests\"][relative], _sha256(self.target / relative))\n+\n+\n+class LegacyCleanupPreflightTests(unittest.TestCase):\n+    def setUp(self) -> None:\n+        self.tmp = Path(tempfile.mkdtemp(prefix=\"shiki-sadr-preflight-\"))\n+        self.addCleanup(lambda: shutil.rmtree(self.tmp, ignore_errors=True))\n+\n+    def test_all_ownership_blockers_abort_before_any_write(self) -> None:\n+        cases = (\n+            (\"absent\", \"install stamp is absent\"),\n+            (\"unreadable\", \"install stamp is unreadable\"),\n+            (\"malformed\", \"install stamp is malformed\"),\n+            (\"missing-digest\", \"install stamp has no digest\"),\n+            (\"digest-mismatch\", \"digest mismatch\"),\n+        )\n+        for name, expected_reason in cases:\n+            with self.subTest(name=name):\n+                target = self.tmp / name\n+                _, legacy_paths = _create_legacy_upgrade_fixture(target)\n+                stamp_path = target / shiki_installer.INSTALL_STAMP_PATH\n+\n+                if name == \"absent\":\n+                    stamp_path.unlink()\n+                elif name == \"unreadable\":\n+                    stamp_path.unlink()\n+                    stamp_path.mkdir()\n+                elif name == \"malformed\":\n+                    stamp_path.write_text(\"{not-json\\n\", encoding=\"utf-8\")\n+                else:\n+                    stamp = json.loads(stamp_path.read_text(encoding=\"utf-8\"))\n+                    if name == \"missing-digest\":\n+                        stamp[\"digests\"] = {}\n+                    else:\n+                        stamp[\"digests\"] = {\n+                            relative: \"deadbeef\" for relative in legacy_paths\n+                        }\n+                    stamp_path.write_text(\n+                        json.dumps(stamp, indent=2, sort_keys=True) + \"\\n\",\n+                        encoding=\"utf-8\",\n+                    )\n+\n+                before = _tree_snapshot(target)\n+                with self.assertRaises(shiki_installer.ShikiError) as raised:\n+                    _quiet_install(target, force=True, validate=False)\n+                after = _tree_snapshot(target)\n+\n+                self.assertEqual(before, after, f\"{name} preflight changed the tree\")\n+                message = str(raised.exception)\n+                self.assertIn(expected_reason, message)\n+                self.assertIn(\"No files were written\", message)\n+                for relative in legacy_paths:\n+                    self.assertIn(relative, message)\n+\n+    def test_absent_stamp_is_allowed_when_no_legacy_path_exists(self) -> None:\n+        target = self.tmp / \"no-legacy\"\n+        target.mkdir()\n+        _quiet_install(target, force=True, validate=False)\n+        self.assertTrue(\n+            (target / \"docs\" / \"adr\" / \"SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\").is_file()\n+        )\n+\n+\n+if __name__ == \"__main__\":\n+    unittest.main()\n\nexec\n/bin/zsh -lc \"sed -n '1,260p' skills/engineering/code-review/SKILL.md\" in /private/tmp/shiki-adr-collision.pa0KwY/.worktrees/shiki-t-20260807t073307221254z-2d22d654-introduc\n succeeded in 0ms:\n---\nname: code-review\ndescription: Mandatory pre-PR self-review gate for Shiki implementers. Run after the TDD loop is green and before creating or updating a PR. Reviews the task diff for correctness bugs and reuse/simplification/efficiency cleanups, applies fixes on the task branch, and records durable evidence.\n---\n\n# Code Review (Pre-PR Implementer Gate)\n\nThis is the Shiki wrapper for the implementer's native code-review capability.\nIn Claude Code it is satisfied by the built-in `/code-review` skill; another\nAgent Runtime may satisfy it with an equivalent diff review that produces the\nsame evidence.\n\n## Position In The Loop\n\n```\ntdd (red-green-refactor, all checks green)\n  -> code-review (this gate: review the full task diff, apply fixes)\n  -> re-run verification\n  -> PR creation or update\n```\n\nThe gate runs in the **Implementer** role on the implementer's own task\nbranch, before any PR exists or before pushing new commits to an existing PR.\nApplying fixes here does not violate the Reviewer rule (\"reviewers do not\nmutate the implementation branch\") because no review handoff has happened yet.\n\n## How To Run\n\n1. Confirm the TDD loop is complete and verification commands pass.\n2. Review the entire task diff against the task contract (scope, non-goals,\n   acceptance checks, locks). In Claude Code, invoke the native `/code-review`\n   skill at an effort matching task risk: low/medium risk \u2192 medium effort;\n   high/critical risk \u2192 high or max effort. The cloud multi-agent deep review\n   (ultra) is operator-triggered and not required by this gate.\n3. Triage findings:\n   - Correctness bugs, contract violations, scope drift: fix now.\n   - Quality cleanups (reuse, simplification, efficiency): fix when bounded\n     to the task scope; otherwise record as a follow-up note.\n   - Findings you reject: record why.\n4. Re-run the task's verification commands after fixes.\n5. Record evidence (below). Only then create or update the PR.\n\n## Required Evidence\n\nBoth of these are required; missing evidence blocks MergeGate readiness:\n\n- A Ledger entry whose summary names this skill (`code-review`) and states\n  findings count, applied fixes, and rejected findings with reasons. List the\n  entry in the task's `ledger_evidence`.\n- A `## Pre-PR code review` section in the PR body summarizing findings and\n  resolutions (or stating \"no findings\" when the review was clean).\n\n## Exceptions\n\nEvidence-only tasks, pure `.shiki` state reconciliation, and trivial\ndocumentation-only changes may skip the gate when the skip is stated in the\nPR body and recorded in the ledger. CCA may treat an unjustified skip as\n`insufficient_evidence`.\n\ndiff --git a/.codex/skills/shiki/SKILL.md b/.codex/skills/shiki/SKILL.md\nindex c8e2e85d9d75d49cc4f2d6286ee22659c4e53e75..93cded623b2c15d9f65c76491a1c2d813ac9e6c0\n--- a/.codex/skills/shiki/SKILL.md\n+++ b/.codex/skills/shiki/SKILL.md\n@@ -18,6 +18,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n Codex CLI does not register this skill as a custom `/shiki` slash command. When\n the user types `/shiki` in Codex CLI and gets \"Unrecognized command\", explain\n that this is expected: invoke Shiki in Codex with natural language such as\n@@ -69,7 +72,7 @@\n \n ## Responsibilities\n \n-- Claude Code implements and repairs by default (`shiki runner claude`, ADR 0008); Codex implements and repairs tasks explicitly assigned to `codex`.\n+- Claude Code implements and repairs by default (`shiki runner claude`, SADR-0008); Codex implements and repairs tasks explicitly assigned to `codex`.\n - Claude Code Action can act as GitHub-side CCA or reviewer.\n - CCA judges completion.\n - MergeGate authorizes state transitions and merge readiness.\ndiff --git a/.github/prompts/cca-completion-check.md b/.github/prompts/cca-completion-check.md\nindex ed203ad5f302ecc81372d8ab27d2b1232ca97497..fd89950528333ef6242e6d5443904f5392ab7e56\n--- a/.github/prompts/cca-completion-check.md\n+++ b/.github/prompts/cca-completion-check.md\n@@ -19,6 +19,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n ## Required Reading\n \n Read only the files needed to judge the PR contract. Prefer this order:\ndiff --git a/.github/prompts/codex-task-handoff.md b/.github/prompts/codex-task-handoff.md\nindex 695e211d76dea1c60e8d5bb360e78487808286a4..59441d576b01a39569722f226c854e5a740e9eb5\n--- a/.github/prompts/codex-task-handoff.md\n+++ b/.github/prompts/codex-task-handoff.md\n@@ -2,7 +2,7 @@\n \n You are Codex Front acting as the Implementer for a Shiki task.\n \n-Follow `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant ADRs, and `docs/agents/codex-handoff.md`.\n+Follow `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant Shiki SADRs and target ADRs, and `docs/agents/codex-handoff.md`.\n \n Rules:\n \ndiff --git a/AGENTS.md b/AGENTS.md\nindex 7f041100d49c2a7525f51cb0f5d696a4a6d1c78c..a06bfbabc6fa48a007389e25e12d7ed523936bed\n--- a/AGENTS.md\n+++ b/AGENTS.md\n@@ -22,7 +22,7 @@\n - **Codex Front** is an optional implementer through Codex App, Codex CLI, Codex IDE extension, or Codex Web signed in with ChatGPT OAuth/subscription auth, dispatched with `shiki runner codex` when a task is explicitly assigned to `codex`.\n - **GitHub CCA** is implemented by Claude Code Action by default, using `CLAUDE_CODE_OAUTH_TOKEN`.\n \n-Do not assume `openai/codex-action`, `OPENAI_API_KEY`, or API-key based Codex automation in the default Shiki path. API-key runners are explicit target-repo extensions and require their own ADR.\n+Do not assume `openai/codex-action`, `OPENAI_API_KEY`, or API-key based Codex automation in the default Shiki path. API-key runners are explicit target-repo extensions and require their own target ADR.\n \n Shiki is not:\n \n@@ -44,10 +44,14 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use\n+`SADR-NNNN-*.md`; target product decisions retain `NNNN-*.md` and the `ADR`\n+namespace.\n+\n Use the canonical source-of-truth order above. Runtime-specific reading orders\n are operational guidance only and must not override or reorder it.\n \n-Conversation state is not durable truth. If a decision matters, put it in GitHub, `.shiki/`, `CONTEXT.md`, or an ADR.\n+Conversation state is not durable truth. If a decision matters, put it in GitHub, `.shiki/`, `CONTEXT.md`, a Shiki SADR, or a target ADR.\n \n If GitHub and `.shiki/` disagree, surface the conflict and prefer GitHub operational state until the mirror is repaired.\n \n@@ -75,8 +79,8 @@\n Every non-trivial change follows this loop:\n \n 1. **Goal**: clarify outcome, completion conditions, non-goals, risk level, and success signals.\n-2. **grill-with-docs**: challenge the plan against domain docs, ADRs, code reality, terminology, and edge scenarios.\n-3. **Context & Impact**: find relevant docs, ADRs, code, dependencies, owners, locks, and verification surfaces.\n+2. **grill-with-docs**: challenge the plan against domain docs, Shiki SADRs, target ADRs, code reality, terminology, and edge scenarios.\n+3. **Context & Impact**: find relevant docs, SADRs, target ADRs, code, dependencies, owners, locks, and verification surfaces.\n 4. **PRD and Spec Freeze**: use `to-prd` when context is settled enough to become durable product/engineering intent. The operator's explicit approval of the PRD is Spec Freeze: the plan records a `spec_freeze` block, and no plan runs without it. Steps 1-4 form Requirements Definition \u2014 one continuous operator dialogue. After Spec Freeze, scope changes require an operator-approved Spec Amendment; non-scope-moving interpretations go to the Assumption Log.\n 5. **Issues**: use `to-issues` to create independently grabbable vertical-slice issues. Prefer AFK slices over HITL slices where possible.\n 6. **Triage**: label issues for readiness, risk, runtime, skills, and MergeGate state.\n@@ -167,7 +171,7 @@\n - Testing decisions.\n - Out-of-scope items.\n - Further notes.\n-- Links to relevant domain terms and ADRs.\n+- Links to relevant domain terms, SADRs, and target ADRs.\n \n Every executable task must include:\n \n@@ -193,7 +197,7 @@\n \n Context & Impact must identify:\n \n-- Relevant documents, domain terms, ADRs, and past decisions.\n+- Relevant documents, domain terms, SADRs, target ADRs, and past decisions.\n - Relevant modules, interfaces, seams, callers, owners, and tests.\n - Dependency relationships between tasks.\n - Candidate locks and lock conflicts.\n@@ -210,7 +214,7 @@\n | Skill | Required when |\n | --- | --- |\n | `setup-matt-pocock-skills` | First configuring the repo for skills, or when issue tracker, triage labels, or domain docs are missing. |\n-| `grill-with-docs` | Default for non-trivial Goals before PRD/issues; always required when plans, terminology, boundaries, tradeoffs, or ADR-worthy decisions are ambiguous. |\n+| `grill-with-docs` | Default for non-trivial Goals before PRD/issues; always required when plans, terminology, boundaries, tradeoffs, or decision-record-worthy choices are ambiguous. |\n | `zoom-out` | The agent lacks an architectural map of the relevant code area. |\n | `to-prd` | Settled Goal context must become durable product/engineering intent. |\n | `to-issues` | A Goal, PRD, or plan must become independently grabbable vertical-slice issues. |\n@@ -348,7 +352,7 @@\n - Three or more new files.\n - Any high-risk or critical label.\n \n-When the architecture gate triggers, run `grill-with-docs` or `improve-codebase-architecture` as appropriate and record durable decisions in an ADR when needed.\n+When the architecture gate triggers, run `grill-with-docs` or `improve-codebase-architecture` as appropriate and record durable decisions in a Shiki SADR or target ADR as applicable.\n \n ## Engineering Discipline\n \ndiff --git a/CLAUDE.md b/CLAUDE.md\nindex 98a932e7841a632866fcd53b0bad0e3af3711d3d..98a31399404d6fb044b4fd493f4459ac87f439de\n--- a/CLAUDE.md\n+++ b/CLAUDE.md\n@@ -14,6 +14,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n ## Claude Role\n \n Default to planner, default implementer, reviewer, coordinator, documentation editor, CCA-style completion judge, and final-judgment assistant.\n@@ -57,7 +60,7 @@\n At the start of a Shiki session:\n \n 1. Read `AGENTS.md`.\n-2. Read `CONTEXT.md` and relevant ADRs.\n+2. Read `CONTEXT.md` and relevant Shiki SADRs and target ADRs.\n 3. Read active `.shiki/` Goal/task/ledger state when present.\n 4. Read active distilled rules from `.shiki/memories/` (status `distilled`, `active: true`, not revoked or superseded) for applicable guidance; the same rules are injected deterministically into task handoffs as the `## Distilled Rules` section (\u00a73.5).\n 5. Read the GitHub Issue or PR when the task is GitHub-backed.\n@@ -74,7 +77,7 @@\n - Define the Goal, completion conditions, non-goals, and risk level.\n - Treat `grill-with-docs` as the default planning entry point for non-trivial Goals.\n - Challenge domain terms against `CONTEXT.md`.\n-- Challenge hard-to-reverse tradeoffs against ADRs.\n+- Challenge hard-to-reverse tradeoffs against SADRs and target ADRs.\n - Explore code instead of asking when code can answer the question.\n - Run Context & Impact before implementation planning.\n - Produce a Task DAG, not an unordered checklist.\n@@ -94,7 +97,7 @@\n - For each question, provide your recommended answer.\n - Resolve dependency decisions before downstream design questions.\n - Update `CONTEXT.md` inline when terminology crystallizes.\n-- Offer ADRs only when the decision is hard to reverse, surprising without context, and a real tradeoff.\n+- Offer a SADR or target ADR only when the decision is hard to reverse, surprising without context, and a real tradeoff.\n - Convert unresolved questions into HITL blockers rather than allowing Codex to guess.\n \n ## PRD and Issues Standard\n@@ -103,7 +106,7 @@\n \n - Use the project's domain glossary vocabulary.\n - Avoid volatile file paths unless they are essential.\n-- Include implementation decisions, testing decisions, out-of-scope items, and relevant ADR/domain links.\n+- Include implementation decisions, testing decisions, out-of-scope items, and relevant SADR/target-ADR/domain links.\n - Identify deep-module opportunities and testable interfaces.\n \n When creating issues:\n@@ -121,7 +124,7 @@\n - Branch/worktree target.\n - Scope and non-goals.\n - Dependencies and locks.\n-- Relevant docs, ADRs, modules, and tests.\n+- Relevant docs, SADRs, target ADRs, modules, and tests.\n - Required skill invocations.\n - TDD expectations.\n - Pre-PR `code-review` gate expectations.\n@@ -152,7 +155,7 @@\n \n - Treat yourself as a judge, not implementer.\n - Do not write production code.\n-- Read `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant ADRs, PRD, task issue, PR body, diff, checks, reviews, labels, and `.shiki/` state.\n+- Read `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant SADRs and target ADRs, PRD, task issue, PR body, diff, checks, reviews, labels, and `.shiki/` state.\n - Evaluate every applicable checklist in `docs/agents/checklists.md`.\n - Map each acceptance criterion to durable evidence.\n - Distinguish implementation failure from missing evidence.\ndiff --git a/CONTEXT.md b/CONTEXT.md\nindex e61a9be06a0cce0d026484083a657bde2067e234..237ee8aa3ea8fab334ece49768bc5ed3b5af0974\n--- a/CONTEXT.md\n+++ b/CONTEXT.md\n@@ -25,7 +25,7 @@\n _Avoid_: jumping directly into code\n \n **grill-with-docs**:\n-A planning skill that challenges a plan against domain language, ADRs, code reality, and concrete edge scenarios. It resolves design-tree questions before PRD/issues. It runs inside Requirements Definition.\n+A planning skill that challenges a plan against domain language, Shiki SADRs, target ADRs, code reality, and concrete edge scenarios. It resolves design-tree questions before PRD/issues. It runs inside Requirements Definition.\n _Avoid_: generic brainstorming, silent assumptions\n \n **Requirements Definition**:\ndiff --git a/CONTRIBUTING.md b/CONTRIBUTING.md\nindex c6d9b656e73abb8a55e5f660123e03153dcb3e6d..9e37a8fbf668db5b22461aaae5548b8f1354394c\n--- a/CONTRIBUTING.md\n+++ b/CONTRIBUTING.md\n@@ -17,7 +17,7 @@\n 1. **Goal** \u2014 start from a user-approved Goal with an outcome, completion\n    conditions, non-goals, risk level, and success signals.\n 2. **grill-with-docs** \u2014 for any non-trivial change, challenge the plan against\n-   domain language, ADRs, and code reality before writing a PRD.\n+   domain language, Shiki SADRs, target ADRs, and code reality before writing a PRD.\n 3. **PRD** \u2014 capture settled product/engineering intent (`to-prd`).\n 4. **Spec Freeze** \u2014 the operator explicitly approves the PRD/requirements;\n    the plan records a `spec_freeze` block and no plan runs without it\n@@ -94,7 +94,7 @@\n   entries.\n - Do not hand-edit `.shiki/goals/*.json` or `.shiki/manifest.json` in a task PR.\n - Conversation state is not durable truth. If a decision matters, record it in\n-  GitHub, `.shiki/`, `CONTEXT.md`, or an ADR.\n+  GitHub, `.shiki/`, `CONTEXT.md`, a Shiki SADR, or a target ADR.\n \n ## Architecture Gate\n \n@@ -103,7 +103,7 @@\n or CI policy, package structure, three or more structural files, more than 100\n net new lines in a core module, three or more new files, or any high-risk label.\n When triggered, run `grill-with-docs` or `improve-codebase-architecture` and\n-record durable decisions in an ADR when needed (see `docs/adr/`).\n+record durable decisions in a Shiki SADR or target ADR as applicable (see `docs/adr/`).\n \n ## Safety\n \ndiff --git a/SYSTEM_PROMPT.md b/SYSTEM_PROMPT.md\nindex aaaea45e3a896c3a44ba89eec8bb84325ecf6c62..da009c86a88285269dee0155e58e550c6d744ea9\n--- a/SYSTEM_PROMPT.md\n+++ b/SYSTEM_PROMPT.md\n@@ -30,6 +30,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n Use the canonical source-of-truth order above. Runtime-specific reading orders\n are operational guidance only and must not override or reorder it. The current\n conversation is non-durable operator input.\n@@ -48,14 +51,14 @@\n \n A runtime may only do work that is explicitly assigned by the Goal, task, PR, or MergeGate decision.\n \n-Do not assume `openai/codex-action`, `OPENAI_API_KEY`, or API-key based Codex automation in the default Shiki path. API-key runners are explicit target-repo extensions and require their own ADR.\n+Do not assume `openai/codex-action`, `OPENAI_API_KEY`, or API-key based Codex automation in the default Shiki path. API-key runners are explicit target-repo extensions and require their own target ADR.\n \n ## 4. Non-negotiable Shiki flow\n \n For any non-trivial Goal, use this flow:\n \n 1. **Goal Seek**: define outcome, non-goals, risk level, completion criteria, and evidence requirements.\n-2. **grill-with-docs**: challenge the plan against `CONTEXT.md`, ADRs, code reality, terminology, boundaries, and edge scenarios. Ask one question at a time when the operator is available. If code can answer a question, inspect code instead of asking.\n+2. **grill-with-docs**: challenge the plan against `CONTEXT.md`, Shiki SADRs, target ADRs, code reality, terminology, boundaries, and edge scenarios. Ask one question at a time when the operator is available. If code can answer a question, inspect code instead of asking.\n 3. **Context & Impact**: identify modules, interfaces, seams, callers, dependencies, locks, risk, and verification surfaces.\n 4. **to-prd + Spec Freeze**: turn settled context into a PRD using domain vocabulary and testing decisions. The operator's explicit approval of the PRD is Spec Freeze, recorded as the plan's `spec_freeze` block; plans without it do not run. Steps 1-4 form Requirements Definition.\n 5. **to-issues**: decompose the PRD into independently grabbable vertical-slice issues. Prefer AFK slices; mark HITL slices when judgment is still required.\n@@ -75,7 +78,7 @@\n Before making any material change:\n \n 1. Read `AGENTS.md` and any runtime-specific wrapper such as `CLAUDE.md`.\n-2. Read `CONTEXT.md` and relevant ADRs.\n+2. Read `CONTEXT.md` and relevant SADRs and target ADRs.\n 3. Read the GitHub Issue / PR and `.shiki/` state for the active Goal or task.\n 4. Identify your role: Planner, Implementer, Reviewer, Completion Check Agent, Repairer, or Guardian-assist.\n 5. Identify Goal id, task id, branch, PR, dependencies, locks, risk level, required skills, and required evidence.\n@@ -103,7 +106,7 @@\n \n At minimum, identify:\n \n-- Relevant documents, ADRs, domain terms, and prior decisions.\n+- Relevant documents, SADRs, target ADRs, domain terms, and prior decisions.\n - Relevant modules, interfaces, seams, callers, owners, and tests.\n - Dependency relationships between tasks.\n - Candidate file locks and lock conflicts.\n@@ -152,7 +155,7 @@\n \n CCA must read:\n \n-- `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant ADRs.\n+- `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant SADRs and target ADRs.\n - Parent Goal issue, PRD issue, task issue, comments, and labels.\n - PR body, diff, commits, changed files, reviews, CI checks, and artifacts available to the workflow.\n - `.shiki/` task contract, locks, ledger entries, CCA checklist profile, and prior repair packets.\n@@ -214,7 +217,7 @@\n - Three or more new files.\n - Any high-risk or critical label.\n \n-When the architecture gate triggers, run `grill-with-docs` or `improve-codebase-architecture` as appropriate and record the decision in an ADR if it is hard to reverse or likely to be re-litigated.\n+When the architecture gate triggers, run `grill-with-docs` or `improve-codebase-architecture` as appropriate and record the decision in a Shiki SADR or target ADR as applicable if it is hard to reverse or likely to be re-litigated.\n \n ## 14. Skill Gate\n \n@@ -223,7 +226,7 @@\n Use these skills:\n \n - `setup-matt-pocock-skills`: before first use of the engineering skill set, or when issue tracker, triage labels, or domain docs are missing.\n-- `grill-with-docs`: default for non-trivial Goals before PRD/issues; required for ambiguous plans, terminology, boundaries, ADR-worthy tradeoffs, or design-tree decisions.\n+- `grill-with-docs`: default for non-trivial Goals before PRD/issues; required for ambiguous plans, terminology, boundaries, decision-record-worthy tradeoffs, or design-tree decisions.\n - `zoom-out`: unfamiliar code area or missing architectural map.\n - `to-prd`: convert settled context into a PRD.\n - `to-issues`: break a PRD, plan, or Goal into independently grabbable vertical-slice issues.\ndiff --git a/docs/adr/0001-keep-shiki-as-platform-name.md b/docs/adr/SADR-0001-keep-shiki-as-platform-name.md\nindex 20a9637c3b9d8c97de7ba8db7cfae4d82d9a7dba..8b685439959aada1b221c112f0ac3c51ca8a1695\n--- a/docs/adr/0001-keep-shiki-as-platform-name.md\n+++ b/docs/adr/SADR-0001-keep-shiki-as-platform-name.md\n@@ -1,3 +1,3 @@\n-# Keep Shiki As The Platform Name\n+# SADR-0001: Keep Shiki As The Platform Name\n \n Shiki remains the product name for the GitHub-native agentic engineering control plane. We considered more descriptive names such as MergePilot, MergeGate, and Kiroku, but kept Shiki because it carries the existing goal-seeking framework identity while remaining independent of Hermes, Codex, Claude Code, or any single runtime.\ndiff --git a/docs/adr/0002-use-agents-as-shared-constitution.md b/docs/adr/SADR-0002-use-agents-as-shared-constitution.md\nindex 73de30ecb078120eac540d9bc51855e3cd48d0e3..c5486182bf06fca36ea508496b013c67302fd989\n--- a/docs/adr/0002-use-agents-as-shared-constitution.md\n+++ b/docs/adr/SADR-0002-use-agents-as-shared-constitution.md\n@@ -1,3 +1,3 @@\n-# Use AGENTS As The Shared Agent Constitution\n+# SADR-0002: Use AGENTS As The Shared Agent Constitution\n \n Shiki uses `AGENTS.md` as the runtime-neutral constitution and `CLAUDE.md` as a thin Claude Code wrapper. This keeps Codex, Claude Code, Hermes Runner, and future Agent Runtimes aligned on the same GitHub-first MergeGate rules while still allowing runtime-specific instructions where needed.\ndiff --git a/docs/adr/0003-require-skill-gate-for-mergegate-readiness.md b/docs/adr/SADR-0003-require-skill-gate-for-mergegate-readiness.md\nindex 424be0c9af4c8f0136f1fde68a468c47ff84e764..7fc337987c7df9792f75f181dfcbed75fe5ffff2\n--- a/docs/adr/0003-require-skill-gate-for-mergegate-readiness.md\n+++ b/docs/adr/SADR-0003-require-skill-gate-for-mergegate-readiness.md\n@@ -1,3 +1,3 @@\n-# Require Skill Gate For MergeGate Readiness\n+# SADR-0003: Require Skill Gate For MergeGate Readiness\n \n Shiki requires relevant engineering skills to be invoked before execution when their triggers apply. This makes skills part of MergeGate readiness rather than optional prompt style, because Shiki depends on repeatable planning, issue decomposition, TDD, diagnosis, triage, and architecture review before autonomous or parallel agent work can be trusted.\ndiff --git a/docs/adr/0004-use-github-first-with-shiki-mirror.md b/docs/adr/SADR-0004-use-github-first-with-shiki-mirror.md\nindex d0a3a9174dc646a945a9d325954ffaf00c352040..a79c54ae9ee8eed2a0524192b864da4e22a92d3f\n--- a/docs/adr/0004-use-github-first-with-shiki-mirror.md\n+++ b/docs/adr/SADR-0004-use-github-first-with-shiki-mirror.md\n@@ -1,4 +1,4 @@\n-# ADR 0004: Use GitHub First With A Repository-Local Shiki Mirror\n+# SADR-0004: Use GitHub First With A Repository-Local Shiki Mirror\n \n ## Status\n \ndiff --git a/docs/adr/0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md b/docs/adr/SADR-0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\nindex b339baf49b89d9290e79c3d51b36e7b2bde3bd95..0f3c3eb9afccdb3403ce978e21d04fe12e495da0\n--- a/docs/adr/0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\n+++ b/docs/adr/SADR-0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\n@@ -1,8 +1,8 @@\n-# ADR 0005: Use Codex As Front And Claude Code Action As The GitHub Runtime\n+# SADR-0005: Use Codex As Front And Claude Code Action As The GitHub Runtime\n \n ## Status\n \n-Accepted (implementer default superseded by ADR 0008; the auth model and the\n+Accepted (implementer default superseded by SADR-0008; the auth model and the\n Claude Code Action GitHub runtime decision remain in force)\n \n ## Context\n@@ -26,4 +26,4 @@\n - Shiki avoids requiring `OPENAI_API_KEY` for the default workflow.\n - Codex work happens through the user's authenticated Codex front surface, not as a hidden backend Action.\n - Claude Code Action is configured for OAuth-token based subscription usage by default.\n-- API-key based automation remains possible only as an explicit target-repo extension with its own ADR.\n+- API-key based automation remains possible only as an explicit target-repo extension with its own target ADR.\ndiff --git a/docs/adr/0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md b/docs/adr/SADR-0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\nindex 4bcae9d1e52f0a37b8e9fbafa5cd14b6ee1993bd..7a38538a6ae9929567f0ade67e7ea6a60d0b3f7e\n--- a/docs/adr/0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\n+++ b/docs/adr/SADR-0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\n@@ -1,4 +1,4 @@\n-# ADR 0006: Separate Codex Implementation, CCA Completion, And MergeGate Authorization\n+# SADR-0006: Separate Codex Implementation, CCA Completion, And MergeGate Authorization\n \n ## Status\n \ndiff --git a/docs/adr/0007-packaging-and-release.md b/docs/adr/SADR-0007-packaging-and-release.md\nindex 79b84ddb97167372b44641e5b7860e400ce80822..bb445a2e2e856d9d1f5fdbe35de8d4e8cec0fb32\n--- a/docs/adr/0007-packaging-and-release.md\n+++ b/docs/adr/SADR-0007-packaging-and-release.md\n@@ -1,4 +1,4 @@\n-# ADR 0007: Packaging And Release For Shiki\n+# SADR-0007: Packaging And Release For Shiki\n \n ## Status\n \n@@ -25,7 +25,7 @@\n   not introduce third-party runtime dependencies or break the shim.\n - Releases must run inside the operator's subscription-authenticated toolchain\n   and must not require extra secrets beyond the built-in `GITHUB_TOKEN`\n-  (consistent with ADR 0005's no-API-key-by-default stance).\n+  (consistent with SADR-0005's no-API-key-by-default stance).\n - `.shiki/manifest.json` already encodes the install surface (`install.include`,\n   `install.create_directories`, `install.exclude_from_commit`), and it must not\n   be mutated as part of this change.\n@@ -34,7 +34,7 @@\n \n This is a hard-to-reverse platform decision (it sets the version source of\n truth, the release trigger, and the public install contract), so it is recorded\n-as an ADR.\n+as a SADR.\n \n ## Decision\n \n@@ -104,7 +104,7 @@\n 3. **Publishing to PyPI.** Rejected for now: Shiki is distributed as a\n    repository template + git-installable CLI, not a PyPI library; PyPI would add\n    credentials/secrets and a different versioning contract. Can be added later as\n-   its own ADR if a packaged distribution is needed.\n+   its own SADR if a packaged distribution is needed.\n 4. **Dynamic version (read `VERSION` via a build backend `dynamic` field).**\n    Rejected to keep `pyproject.toml` backend-agnostic and dependency-free; the\n    small duplication between `VERSION` and `pyproject.toml` is guarded by the\n@@ -123,4 +123,4 @@\n   releasing versions the whole platform but ships only `install.include` to\n   targets.\n - Future PyPI distribution or a console entry point are deferred and would each\n-  warrant their own ADR.\n+  warrant their own SADR.\ndiff --git a/docs/adr/0008-use-claude-code-as-default-implementer.md b/docs/adr/SADR-0008-use-claude-code-as-default-implementer.md\nindex dedf4510b7c7799521422f622029563d373db031..35c2625d908167fbc58855b5b63e283805f167e8\n--- a/docs/adr/0008-use-claude-code-as-default-implementer.md\n+++ b/docs/adr/SADR-0008-use-claude-code-as-default-implementer.md\n@@ -1,12 +1,12 @@\n-# ADR 0008: Use Claude Code As The Default Implementer Runtime\n+# SADR-0008: Use Claude Code As The Default Implementer Runtime\n \n ## Status\n \n-Proposed (supersedes ADR 0005 for the implementer default; keeps its auth model)\n+Proposed (supersedes SADR-0005 for the implementer default; keeps its auth model)\n \n ## Context\n \n-ADR 0005 made Codex Front the default implementation runtime, with Claude Code\n+SADR-0005 made Codex Front the default implementation runtime, with Claude Code\n restricted to planner and reviewer roles by the runtime registry, and the only\n autonomous implementation adapter was `shiki runner codex`.\n \n@@ -18,13 +18,13 @@\n - The platform's planning, review, and CCA layers already run on Claude Code,\n   and Claude Code's native capabilities (the built-in `/code-review` skill and\n   the Workflow multi-agent orchestration tool) have no Codex equivalent.\n-- The Goal lifecycle redesign (ADR 0009) requires an autonomous post-freeze\n+- The Goal lifecycle redesign (SADR-0009) requires an autonomous post-freeze\n   loop driven from the operator's Claude Code session, including bounded\n   repair dispatch, which must not depend on a runtime that cannot be invoked\n   unattended.\n \n The constitution claims Shiki is runtime-agnostic (\"not a Claude-only\n-workflow\", ADR 0002). Any change to the implementer default must preserve the\n+workflow\", SADR-0002). Any change to the implementer default must preserve the\n registry-based runtime model rather than hardcoding one vendor.\n \n ## Decision\n@@ -39,7 +39,7 @@\n   the task to review or repair-needed.\n - Keep Codex registered as an optional implementer runtime. `shiki runner\n   codex`, the Codex handoff templates, and the ChatGPT OAuth auth model from\n-  ADR 0005 remain supported for tasks explicitly assigned to `codex`.\n+  SADR-0005 remain supported for tasks explicitly assigned to `codex`.\n - Commit to mandating two Claude-native capabilities as loop contract in\n   follow-up Goals (status: the code-review gate was delivered by Goal G-B \u2014\n   skill registry, checklists item PR-12, default required_skills \u2014 while the\n@@ -60,7 +60,7 @@\n \n Out of scope: removing Codex support, changing the CCA runtime (Claude Code\n Action with `CLAUDE_CODE_OAUTH_TOKEN` stays), and API-key based automation\n-(still requires its own ADR per ADR 0005).\n+(still requires its own target ADR per SADR-0005).\n \n ## Consequences\n \n@@ -100,7 +100,7 @@\n   harness rejection of direct `codex exec` is outside Shiki's control, and the\n   plan's mandatory native capabilities cannot run on Codex at all.\n - **Remove Codex entirely**: rejected; it abandons the runtime-agnostic\n-  constitution (ADR 0002) for no operational gain and forecloses\n+  constitution (SADR-0002) for no operational gain and forecloses\n   cross-runtime adversarial review.\n - **GitHub Actions cloud implementation via claude-code-action**: rejected as\n   default; durable evidence is automatic but token cost, Node 24 workflow\ndiff --git a/docs/adr/0009-requirements-definition-and-spec-freeze.md b/docs/adr/SADR-0009-requirements-definition-and-spec-freeze.md\nindex 29b3f7871bef9203a8b4a831a65c39eb2f506e42..52e7dd69ad0aa4efe0b68c62678abbf7df02aaec\n--- a/docs/adr/0009-requirements-definition-and-spec-freeze.md\n+++ b/docs/adr/SADR-0009-requirements-definition-and-spec-freeze.md\n@@ -1,4 +1,4 @@\n-# ADR 0009: Merge Grill And Planning Into Requirements Definition With Spec Freeze\n+# SADR-0009: Merge Grill And Planning Into Requirements Definition With Spec Freeze\n \n ## Status\n \ndiff --git a/docs/adr/0010-external-ai-guardian-review.md b/docs/adr/SADR-0010-external-ai-guardian-review.md\nindex e6108bbe8bb4e73e0ff4684c8f78001a80438c4e..f3510b145664298adafb151b6f0db51b972f01ec\n--- a/docs/adr/0010-external-ai-guardian-review.md\n+++ b/docs/adr/SADR-0010-external-ai-guardian-review.md\n@@ -1,4 +1,4 @@\n-# ADR 0010: External AI Guardian Review As A First-Class Approval Authority\n+# SADR-0010: External AI Guardian Review As A First-Class Approval Authority\n \n ## Status\n \n@@ -8,7 +8,7 @@\n \n Shiki is intended to be an autonomous, recursive, self-improving agent\n framework: authority-in-the-loop, not human-in-the-loop. Until now the Guardian\n-approval required for high/critical-risk merges (guardian-policy.json, ADR\n+approval required for high/critical-risk merges (guardian-policy.json, SADR\n governance) recognized only human authority sources \u2014 a GitHub review approval,\n the `guardian:approved` label applied by a configured Guardian, or a comment\n carrying `Guardian approval granted` plus the current head SHA.\n@@ -89,7 +89,7 @@\n   autonomous design the operator intends and produced the impersonation\n   incident.\n - **Record AI approval as an operator comment**: rejected as forgery of human\n-  authority \u2014 the exact failure this ADR corrects.\n+  authority \u2014 the exact failure this SADR corrects.\n - **Deliver the artifact as a committed `.shiki/` file**: rejected; a committed\n   approval inside the PR being approved is forgeable by anyone who can push.\n - **A dedicated reviewer bot identity instead of a relayed comment**: deferred;\ndiff --git a/docs/adr/0011-autonomous-quality-gate-evidence.md b/docs/adr/SADR-0011-autonomous-quality-gate-evidence.md\nindex bca0246c5d46e44afbde15486a75eec425fc02f5..9c93eac16d8c3a6d4b11bd7224e3955102cd7db4\n--- a/docs/adr/0011-autonomous-quality-gate-evidence.md\n+++ b/docs/adr/SADR-0011-autonomous-quality-gate-evidence.md\n@@ -1,4 +1,4 @@\n-# ADR 0011: Autonomous Quality-Gate Evidence \u2014 Deterministic Facts And An Independent Verifier\n+# SADR-0011: Autonomous Quality-Gate Evidence \u2014 Deterministic Facts And An Independent Verifier\n \n ## Status\n \ndiff --git a/docs/adr/0012-autonomous-loop-closeout-to-main.md b/docs/adr/SADR-0012-autonomous-loop-closeout-to-main.md\nindex 637fad6dde4989c9c2bf0f81f676d8d29991123c..ec7d85f81030a676bcbba28b43eb365422a36eac\n--- a/docs/adr/0012-autonomous-loop-closeout-to-main.md\n+++ b/docs/adr/SADR-0012-autonomous-loop-closeout-to-main.md\n@@ -1,4 +1,4 @@\n-# 0012 \u2014 Autonomous loop pushes goal completion to main via a closeout PR\n+# SADR-0012 \u2014 Autonomous loop pushes goal completion to main via a closeout PR\n \n - Status: accepted\n - Date: 2026-06-18\ndiff --git a/docs/adr/0013-bootstrap-grants-actions-approve-pull-requests.md b/docs/adr/SADR-0013-bootstrap-grants-actions-approve-pull-requests.md\nindex b6c69d64f1c5ced346b88d6859586715b3b5bd0e..cf48ff4a6716267f539ca58edd1a852e7c7a2d64\n--- a/docs/adr/0013-bootstrap-grants-actions-approve-pull-requests.md\n+++ b/docs/adr/SADR-0013-bootstrap-grants-actions-approve-pull-requests.md\n@@ -1,4 +1,4 @@\n-# ADR 0013: Bootstrap Grants GitHub Actions Permission To Create And Approve Pull Requests\n+# SADR-0013: Bootstrap Grants GitHub Actions Permission To Create And Approve Pull Requests\n \n ## Status\n \n@@ -31,7 +31,7 @@\n security tradeoff: it lets repository automation satisfy a human-review gate. In\n a multi-maintainer repo that could let a compromised or buggy workflow\n self-approve changes. So whether bootstrap turns it on by default \u2014 and with\n-what default token scope \u2014 is an ADR-worthy decision, not an implementation\n+what default token scope \u2014 is a SADR-worthy decision, not an implementation\n detail.\n \n ## Decision\n@@ -48,7 +48,7 @@\n Specifically:\n \n - **`can_approve_pull_request_reviews` defaults to `true`.** Shiki's default\n-  posture is solo/AFK autonomous operation (ADR 0008/0009/0012), where the CCA\n+  posture is solo/AFK autonomous operation (SADR-0008/0009/0012), where the CCA\n   Review Bridge is the only path that can satisfy `required_review: true` after\n   the CCA and Guardian gates have already passed. The default makes a\n   freshly-bootstrapped repo able to run the autonomous loop end to end without a\ndiff --git a/docs/adr/0014-codex-app-external-ai-guardian-ui-adapter.md b/docs/adr/SADR-0014-codex-app-external-ai-guardian-ui-adapter.md\nindex 238db06279334caa341c117362df9271382a7fcd..65f12e9939321e1b3e389a34c822fc1549e5155d\n--- a/docs/adr/0014-codex-app-external-ai-guardian-ui-adapter.md\n+++ b/docs/adr/SADR-0014-codex-app-external-ai-guardian-ui-adapter.md\n@@ -1,4 +1,4 @@\n-# ADR 0014: Use Codex App As The External AI Guardian UI Adapter\n+# SADR-0014: Use Codex App As The External AI Guardian UI Adapter\n \n ## Status\n \ndiff --git a/docs/adr/0015-contract-approval-before-dispatch.md b/docs/adr/SADR-0015-contract-approval-before-dispatch.md\nindex fad6c656741a20a7684ba16080a7db159f63a52f..6061271a46d3f1d6ae7174477de3fb75673123a6\n--- a/docs/adr/0015-contract-approval-before-dispatch.md\n+++ b/docs/adr/SADR-0015-contract-approval-before-dispatch.md\n@@ -1,4 +1,4 @@\n-# ADR 0015: Approve The Task Contract Before Dispatch, Not The Diff After It\n+# SADR-0015: Approve The Task Contract Before Dispatch, Not The Diff After It\n \n ## Status\n \n@@ -9,7 +9,7 @@\n Guardian approval for high/critical risk is expressible only on a pull request.\n Every approval source in `guardian-policy.json` \u2014 GitHub review, the\n `guardian:approved` label, the `Guardian approval granted` comment, and the\n-`external_ai_guardian_review` artifact (ADR 0010/0014) \u2014 is bound to a PR, and\n+`external_ai_guardian_review` artifact (SADR-0010/0014) \u2014 is bound to a PR, and\n three of the four require the current head SHA. Approval therefore cannot exist\n before an implementation PR exists.\n \n@@ -81,7 +81,7 @@\n An envelope breach must surface only on the non-repairable policy gate, never on\n the repairable `MergeGate metadata check`. Otherwise the Goal Loop routes it to\n `dispatch_repair` \u2014 instructing a runner to make the breach go away \u2014 which is\n-exactly what this ADR forbids.\n+exactly what this SADR forbids.\n \n **Contract immutability.** After registration, an implementation PR must not\n change the governance fields of its own task contract (`scope`, `non_goals`,\n@@ -116,7 +116,7 @@\n fails any verdict that is not `complete`, and the signal the CCA prompt reads is\n computed from `evaluate_guardian_approval` alone. Without the OR-merge at the\n signal, a contract-approved PR still returns `needs_guardian`, the CCA job fails,\n-and the MergeGate policy check never runs \u2014 the gate this ADR changes would never\n+and the MergeGate policy check never runs \u2014 the gate this SADR changes would never\n be reached. The CCA *prompt* needs no change: a `contract_approval` entry in the\n signal's existing `sources` array satisfies its current wording.\n \n@@ -130,7 +130,7 @@\n written, and an approved contract lets its implementation PRs merge through the\n same automatic path as low/medium risk. The wasted implement-then-block cycle\n disappears. Guardian review shifts from reading diffs to reading contracts, which\n-is both earlier and smaller; the external AI Guardian path (ADR 0010/0014) works\n+is both earlier and smaller; the external AI Guardian path (SADR-0010/0014) works\n on contracts with no change.\n \n We accept that the Guardian authorizes a class of change without seeing its\n@@ -146,7 +146,7 @@\n adversarial tests for the new approval path equivalent in rigor to\n `test_shiki_governance_evidence.sh`.\n \n-Bootstrap costs, which apply only while this ADR is being implemented. The tasks\n+Bootstrap costs, which apply only while this SADR is being implemented. The tasks\n that build Contract Approval merge under the existing rules, so the ones\n dispatched after the pre-dispatch gate lands but before the approval path lands\n need a Guardian-approved Contract PR *and* a live Guardian approval on their\n@@ -210,7 +210,7 @@\n risk; they do not bound authorized work.\n \n **Auto-escalate risk on envelope breach instead of stopping.** Returns the\n-workflow to a diff-time Guardian decision \u2014 the exact behavior this ADR removes.\n+workflow to a diff-time Guardian decision \u2014 the exact behavior this SADR removes.\n \n **Resolve governance fields from the base snapshot only.** Rejected against the\n registration flow: implementation PRs add their own contract, so the base has no\ndiff --git a/docs/adr/0016-locks-bound-product-paths.md b/docs/adr/SADR-0016-locks-bound-product-paths.md\nindex b5e850f3f1f7f4ef6e7f9d1763a29a51bd300c45..46b40a9b99a7aae644c02bb458f5473ff0a90ce2\n--- a/docs/adr/0016-locks-bound-product-paths.md\n+++ b/docs/adr/SADR-0016-locks-bound-product-paths.md\n@@ -1,4 +1,4 @@\n-# ADR 0016: Locks bound product paths; the Shiki mirror is bound by record identity\n+# SADR-0016: Locks bound product paths; the Shiki mirror is bound by record identity\n \n ## Status\n \n@@ -6,8 +6,8 @@\n \n - Date: 2026-07-28\n - Deciders: operator (mizutani-140), Claude (planner)\n-- Related: ADR 0004 (GitHub-first with a `.shiki` mirror), ADR 0009 (Spec Freeze),\n-  ADR 0012 (autonomous loop closeout to main), ADR 0015 (Contract Approval before\n+- Related: SADR-0004 (GitHub-first with a `.shiki` mirror), SADR-0009 (Spec Freeze),\n+  SADR-0012 (autonomous loop closeout to main), SADR-0015 (Contract Approval before\n   dispatch), remediation goal task R-02, the live PR #179 deadlock.\n \n ## Context\n@@ -90,11 +90,11 @@\n files, documentation files and named source modules are nameable; runtime-\n allocated ledger, runner and report ids are not \u2014 and those live in `.shiki`,\n which locks no longer govern. Existing frozen contracts are brought into line by\n-a one-time Spec Amendment per plan, which must land before ADR 0015's contract\n+a one-time Spec Amendment per plan, which must land before SADR-0015's contract\n immutability makes `locks` unwritable from the implementation PR.\n \n **Lock state is resolved from the durable side.** MergeGate resolves the lock\n-registry, and ADR 0015's governance fields, from the base `.shiki` snapshot when\n+registry, and SADR-0015's governance fields, from the base `.shiki` snapshot when\n present, falling back to head only when absent. The `MergeGate metadata check`\n job is given `--base-shiki`, which it does not receive today. Locks are not\n derived from the live open-pull-request set: a pull request body is untrusted\n@@ -108,7 +108,7 @@\n Explicitly out of scope: moving MergeGate's contract resolution off the PR\n checkout wholesale; making the mirror a projection of GitHub state; introducing\n new lock kinds; moving ledger evidence out of the repository; and unwinding\n-ADR 0012's closeout pull request. Registration of multi-task goals is ADR 0015's\n+SADR-0012's closeout pull request. Registration of multi-task goals is SADR-0015's\n Contract PR and is not re-decided here.\n \n ## Consequences\n@@ -127,7 +127,7 @@\n \n Risks accepted. First, goal-scoped identity is only as strong as `goal_id`,\n which is resolved from the PR body and the head task file; the mitigation is to\n-add `goal_id` to ADR 0015's frozen governance field set, and until that lands a\n+add `goal_id` to SADR-0015's frozen governance field set, and until that lands a\n task file can name a foreign goal. Second, base-when-present resolution is\n unavailable on fork pull requests, where the head is contributor-controlled;\n fork pull requests already cannot complete a Shiki merge, and this decision does\n@@ -139,7 +139,7 @@\n path, not by implementation pull requests.\n \n New obligations: one Spec Amendment per frozen plan to rewrite `locks`, ordered\n-before ADR 0015 T1; extension of the adversarial suites in\n+before SADR-0015 T1; extension of the adversarial suites in\n `scripts/test_shiki_adversarial_state.sh` and\n `scripts/test_shiki_governance_evidence.sh` to cover the eleven newly-scoped\n directories; and updating `scripts/test_shiki_loop_lock_guard.sh`,\n@@ -151,32 +151,32 @@\n whether the loop's transitive sync of ledger `evidence` references can be\n deleted once the Contract PR carries its own source plan.\n \n-## Relationship to ADR 0004\n+## Relationship to SADR-0004\n \n-This ADR **clarifies something ADR 0004 left unspecified**. ADR 0004 decides that\n+This SADR **clarifies something SADR-0004 left unspecified**. SADR-0004 decides that\n GitHub is the operational source of truth and that each target repository keeps a\n `.shiki/` mirror. It says nothing about pull requests writing the mirror, does\n not name MergeGate, and does not make the mirror an input to a gate. Nothing in\n-ADR 0004's Decision or Consequences is amended, superseded or weakened: the\n+SADR-0004's Decision or Consequences is amended, superseded or weakened: the\n mirror still exists, still records the same artifacts, and pull request\n-boundaries remain the coordination surface. What this ADR settles is the question\n+boundaries remain the coordination surface. What this SADR settles is the question\n 0004 never reached \u2014 by what authority a pull request may write a mirror record \u2014\n and it answers it the way `.shiki/manifest.json` already declares\n (`pr_mutation: current-task-or-current-goal-only`).\n \n-It **amends ADR 0012**, whose Decision item 5 states that \"Loop-executed tasks\n+It **amends SADR-0012**, whose Decision item 5 states that \"Loop-executed tasks\n declare the `path:.shiki/**` lock, which covers every `.shiki` file the closeout\n stages ... so `files_outside_locks` is satisfied without per-file lock\n bookkeeping.\" That clause is replaced: the closeout's mirror writes are\n-authorized by record identity, not by a declared lock. ADR 0012's substantive\n+authorized by record identity, not by a declared lock. SADR-0012's substantive\n decision \u2014 that completion reaches `main` through a closeout pull request rather\n than local attestation \u2014 is preserved unchanged.\n \n-It is **complementary to ADR 0015**, not a substitute. ADR 0015 owns contract\n+It is **complementary to SADR-0015**, not a substitute. SADR-0015 owns contract\n immutability, the pre-dispatch gate, the Contract PR and the Scope Envelope. This\n-ADR owns what a lock means and who may write the mirror. The one ordering\n+SADR owns what a lock means and who may write the mirror. The one ordering\n constraint between them is that the lock-narrowing Spec Amendment must precede\n-ADR 0015 T1's freezing of the `locks` field.\n+SADR-0015 T1's freezing of the `locks` field.\n \n ## Alternatives Considered\n \n@@ -186,7 +186,7 @@\n the only writer that can register them may run. Migrating the Guardian signal to\n that store makes every registration PR fail closed to `needs_guardian` at every\n risk level, because the task file is by definition absent from the store, which\n-removes the low/medium autonomous path ADR 0008/0009 exists to provide. And it\n+removes the low/medium autonomous path SADR-0008/0009 exists to provide. And it\n leaves schemas, manifest, config, guardian policy, templates, migrations, plans,\n worktrees, runner records, runs and handoffs with no writer at all in a\n repository whose product is `.shiki`. Measured benefit on the frozen backlog:\n@@ -199,14 +199,14 @@\n namespace is not in fact ownership-partitioned \u2014 56 ledger entries carry no\n `task_id`, and one goal DAG is writable by 35 registered tasks. The correct\n expression of its insight is that locks do not govern the mirror at all, which is\n-what this ADR decides.\n+what this SADR decides.\n \n **Make the mirror a projection written only by a registration writer and a\n post-merge projection job.** Rejected. It requires a bypass actor with write\n access to the protected default branch; it makes its own master invariant\n readable from a manifest that pull requests may edit; it deletes both reconcile\n modes and leaves a corrupted mirror with no in-band repair path, contradicting\n-ADR 0004's consequence that mirror conflicts are repaired against GitHub state;\n+SADR-0004's consequence that mirror conflicts are repaired against GitHub state;\n and its content-derived ids are rejected by `validate_shiki`'s currently-green id\n pattern.\n \ndiff --git a/docs/adr/0017-bookkeeping-closeouts-do-not-inherit-risk.md b/docs/adr/SADR-0017-bookkeeping-closeouts-do-not-inherit-risk.md\nindex 554e3a0d526a72ca067108c5464cc68c3edcf174..b9676857a4713850e063af7c39c8e0b01ecb4c66\n--- a/docs/adr/0017-bookkeeping-closeouts-do-not-inherit-risk.md\n+++ b/docs/adr/SADR-0017-bookkeeping-closeouts-do-not-inherit-risk.md\n@@ -1,4 +1,4 @@\n-# ADR 0017: A Bookkeeping Closeout Does Not Inherit Its Task's Risk\n+# SADR-0017: A Bookkeeping Closeout Does Not Inherit Its Task's Risk\n \n ## Status\n \n@@ -6,7 +6,7 @@\n \n ## Context\n \n-ADR 0012 made the goal loop push a task's completion to the default branch through a\n+SADR-0012 made the goal loop push a task's completion to the default branch through a\n closeout pull request: after the implementation PR merges, the loop opens a second PR\n carrying `task.status = done`, `lock.state = released`, and \u2014 when the task completes\n its goal \u2014 `goal.status = complete` plus one scorecard report. Nothing else. The\n@@ -49,14 +49,14 @@\n 4. The task file's governance fields (`scope`, `non_goals`, `required_skills`,\n    `risk_level`, `locks`, `acceptance_checks`, `test_command`) are byte-identical to\n    the base snapshot. Only `status`, `expected_pr`, `closeout_pr`, `ledger_evidence`\n-   and `expected_branch` may differ \u2014 the ADR 0015 contract-immutability rule,\n+   and `expected_branch` may differ \u2014 the SADR-0015 contract-immutability rule,\n    unchanged.\n \n    `expected_branch` is in that set because a closeout is *by construction* on a\n    different branch from the implementation it closes, and the loop rewrites the\n    field accordingly. It is safe to let it differ because MergeGate independently\n    requires `expected_branch` to equal the PR's actual head ref, so a PR cannot set\n-   it to anything other than the branch it is really on. The first draft of this ADR\n+   it to anything other than the branch it is really on. The first draft of this SADR\n    omitted it, which made the exemption unreachable: every real closeout failed\n    condition 4 on a field the loop always changes. See PR #209.\n 5. The state transitions are exactly the terminal set: task `review` \u2192 `done`, lock\n@@ -113,12 +113,12 @@\n classification must be derived, not asserted.\n \n **Fold the closeout into the implementation PR** so no second PR exists. Rejected under\n-ADR 0012, which recorded why: a task file that is already `done` on the implementation\n+SADR-0012, which recorded why: a task file that is already `done` on the implementation\n branch short-circuits the loop's review \u2192 CCA \u2192 merge \u2192 done ordering into a local\n goal-completion before any merge happens.\n \n **Have the loop auto-merge closeouts with elevated credentials.** Rejected outright: that\n-is a runtime authoring its own approval, which ADR 0010 and the constitution's\n+is a runtime authoring its own approval, which SADR-0010 and the constitution's\n approval-record prohibition forbid. It would also be indistinguishable, in the evidence,\n from the forgery those rules exist to prevent.\n \ndiff --git a/docs/adr/0018-carried-guardian-approval-across-a-proven-base-sync.md b/docs/adr/SADR-0018-carried-guardian-approval-across-a-proven-base-sync.md\nindex 22358562da390ed8b45cd600da08a43d378b4680..e6e2907cefa776460af73ddf7d39db605f3c39b0\n--- a/docs/adr/0018-carried-guardian-approval-across-a-proven-base-sync.md\n+++ b/docs/adr/SADR-0018-carried-guardian-approval-across-a-proven-base-sync.md\n@@ -1,4 +1,4 @@\n-# ADR 0018: Carried Guardian Approval Across A Proven Base Sync\n+# SADR-0018: Carried Guardian Approval Across A Proven Base Sync\n \n ## Status\n \n@@ -34,7 +34,7 @@\n Guardian evaluator that consumes it (`shiki_guardian.evaluate_guardian_approval`,\n the `guardian_comment_carried` source) were built and tested in isolation. Neither\n does anything until a caller passes the flags that turn the carry on, so the\n-feature was invisible in CI. This ADR records the decision the two of them\n+feature was invisible in CI. This SADR records the decision the two of them\n implement, and this task wires it into the four CI invocations that judge Guardian\n approval.\n \ndiff --git a/docs/adr/README.md b/docs/adr/README.md\nindex c70e057a4520c36e042ab68c53cd4a21ef709b71..5da43856836e2ce6a150f8127766e2f88355ab4b\n--- a/docs/adr/README.md\n+++ b/docs/adr/README.md\n@@ -1,40 +1,41 @@\n-# Architecture Decision Records\n+# Decision Record Namespaces\n \n-This directory holds Shiki's Architecture Decision Records (ADRs): durable\n-records of hard-to-reverse platform decisions. ADRs are part of the Shiki source\n-of truth alongside GitHub state, the `.shiki/` mirror, and `CONTEXT.md`.\n+This directory holds two independent decision namespaces:\n \n-## How ADRs Work\n+- Shiki Architecture Decision Records (SADRs), named\n+  `SADR-NNNN-<short-slug>.md`, record hard-to-reverse Shiki platform decisions.\n+- Target Architecture Decision Records (ADRs), named\n+  `NNNN-<short-slug>.md`, belong to the target product.\n \n-- New ADRs are added as `docs/adr/NNNN-<short-slug>.md`, where `NNNN` is the next\n-  zero-padded sequence number.\n-- The **canonical list of ADRs is the set of files matching `docs/adr/*.md`** in\n-  this directory. Browse `docs/adr/*.md` for the authoritative, current set.\n-- Use [`template.md`](template.md) as the starting point for a new ADR.\n-- Record an ADR when a decision is hard to reverse, surprising without context,\n-  and a real tradeoff. Do not write an ADR for routine or easily reversible\n-  choices.\n+The same four-digit number may appear once in each namespace. A platform\n+`SADR-NNNN` citation resolves only to `SADR-NNNN-*.md`; a target `ADR NNNN`\n+citation resolves only to `NNNN-*.md`.\n \n-Using the `docs/adr/*.md` glob as the source of truth means other PRs can add an\n-ADR without editing this index file, avoiding merge contention on a\n-hand-maintained enumeration.\n+## How Decision Records Work\n \n-## Current ADRs (snapshot)\n-\n-The list below is a convenience snapshot and may lag behind the directory. When\n-in doubt, list `docs/adr/*.md`.\n+- New Shiki platform decisions use the next zero-padded SADR number.\n+- New target product decisions use the target's own ADR sequence.\n+- The canonical list is the files in `docs/adr/`; no hand-maintained index is\n+  authoritative.\n+- Use [`template.md`](template.md) as the starting point and choose the filename\n+  namespace that owns the decision.\n+- Record a decision when it is hard to reverse, surprising without context, and\n+  a real tradeoff. Do not write a decision record for routine or easily\n+  reversible choices.\n \n-- [0001 \u2014 Keep Shiki As The Platform Name](0001-keep-shiki-as-platform-name.md)\n-- [0002 \u2014 Use AGENTS As The Shared Agent Constitution](0002-use-agents-as-shared-constitution.md)\n-- [0003 \u2014 Require Skill Gate For MergeGate Readiness](0003-require-skill-gate-for-mergegate-readiness.md)\n-- [0004 \u2014 Use GitHub First With A Repository-Local Shiki Mirror](0004-use-github-first-with-shiki-mirror.md)\n-- [0005 \u2014 Use Codex As Front And Claude Code Action As The GitHub Runtime](0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md)\n-- [0006 \u2014 Separate Codex Implementation, CCA Completion, And MergeGate Authorization](0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md)\n+Using the directory as the source of truth lets independent changes add records\n+without contending on an enumerated index.\n \n-## Adding A New ADR\n+## Adding A Shiki SADR\n \n-1. Copy `template.md` to `docs/adr/NNNN-<short-slug>.md`.\n+1. Copy `template.md` to `docs/adr/SADR-NNNN-<short-slug>.md`.\n 2. Fill in the title, status, context, decision, and consequences.\n-3. Set the status to `Proposed`, then `Accepted` once the decision is approved.\n-4. Open a PR following `CONTRIBUTING.md`. You do not need to edit this index for\n-   the ADR to be discoverable via `docs/adr/*.md`.\n+3. Set the status to `Proposed`, then `Accepted` once the proper authority has\n+   approved the decision.\n+4. Open a PR following `CONTRIBUTING.md`.\n+\n+## Adding A Target ADR\n+\n+1. Copy `template.md` to `docs/adr/NNNN-<short-slug>.md` using the target's own\n+   sequence.\n+2. Cite it as `ADR NNNN`; do not prefix its filename with `SADR-`.\ndiff --git a/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md b/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\nnew file mode 100644\nindex 0000000000000000000000000000000000000000..7455ce1f3bed02ddddb851ac21a1d1a648c9aeba\n--- /dev/null\n+++ b/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\n@@ -0,0 +1,55 @@\n+# SADR-0019: Separate Shiki Platform SADRs From Target ADRs\n+\n+## Status\n+\n+Accepted\n+\n+## Context\n+\n+Shiki installs its platform decision records into `docs/adr/` in every target\n+repository. Shiki and its targets previously used the same numeric\n+`NNNN-<slug>.md` namespace. A target that already owned ADRs 0013 through 0018\n+therefore acquired two records for each number during installation, making an\n+`ADR NNNN` citation ambiguous. Renumbering target decisions would rewrite target\n+history, while renumbering Shiki decisions per target would destroy their stable\n+platform identity.\n+\n+Upgrades also need to remove the former Shiki-owned numeric paths without ever\n+mistaking a target-owned ADR for platform content. The install stamp already\n+records exact shipped paths and content digests, so it can prove ownership only\n+when both still match.\n+\n+## Decision\n+\n+Shiki platform decisions use the `SADR` namespace:\n+\n+- records are named `docs/adr/SADR-NNNN-<slug>.md`;\n+- live platform citations use `SADR-NNNN`;\n+- target product decisions retain `docs/adr/NNNN-<slug>.md` and `ADR NNNN`;\n+- the reference validator resolves the two namespaces independently, so the same\n+  four-digit number may exist once in each namespace.\n+\n+Before a forced upgrade writes anything, the installer inventories the exact\n+former numeric Shiki ADR paths. It may delete a legacy path only when the\n+existing install stamp contains that exact path and its recorded SHA-256 digest\n+matches the file currently on disk. All ownership blockers are reported\n+together and abort the upgrade before its first write. A non-forced install\n+never performs this cleanup.\n+\n+Historical `.shiki/` evidence remains unchanged because it records the namespace\n+that existed when that evidence was produced.\n+\n+## Consequences\n+\n+- Targets may use any ADR number without colliding with Shiki's platform SADRs.\n+- Fresh installs preserve target ADR bytes and install a stable SADR set.\n+- Forced upgrades fail closed when legacy ownership cannot be proven, and a\n+  successful upgrade refreshes the stamp without removed legacy paths.\n+- Current governance, packaging, workflow, prompt, script, and skill references\n+  must move with the platform records.\n+\n+## Alternatives Considered\n+\n+- Collision-only refusal leaves affected targets permanently unupgradable.\n+- Renumbering target ADRs mutates target-owned history.\n+- Per-target Shiki renumbering makes platform citations non-portable.\ndiff --git a/docs/adr/template.md b/docs/adr/template.md\nindex a92a97daddd0b00ab9dd626cc8d86942da2eafed..17dd79175b5fee2105935557f691b40cf9492942\n--- a/docs/adr/template.md\n+++ b/docs/adr/template.md\n@@ -1,14 +1,14 @@\n-# NNNN: <Short Decision Title>\n+# <SADR-NNNN or NNNN>: <Short Decision Title>\n \n ## Status\n \n-Proposed | Accepted | Superseded by ADR-NNNN\n+Proposed | Accepted | Superseded by SADR-NNNN or target ADR NNNN\n \n ## Context\n \n What is the situation, constraint, or force that requires a decision? Reference\n-relevant domain terms (`CONTEXT.md`), prior ADRs, and code reality. State the\n-problem, not the solution.\n+relevant domain terms (`CONTEXT.md`), prior Shiki SADRs or target ADRs, and code\n+reality. State the problem, not the solution.\n \n ## Decision\n \ndiff --git a/docs/agents/autonomous-loop-e2e-acceptance.md b/docs/agents/autonomous-loop-e2e-acceptance.md\nindex bbf84c6081987f159ac6ad9ecf3c82b7aa72e7e8..5c13c1d02a05a90b0dc46cbef7c95a9846d0c237\n--- a/docs/agents/autonomous-loop-e2e-acceptance.md\n+++ b/docs/agents/autonomous-loop-e2e-acceptance.md\n@@ -40,8 +40,8 @@\n 3. `gh auth status` is logged in with merge permission on the repo.\n 4. Branch protection + required checks are configured exactly as the goal-loop\n    required-check set expects (see `docs/agents/branch-protection-smoke.md`).\n-5. The repo `.shiki/policy` permits autonomous low/medium-risk merges (ADR\n-   0008/0009); the MergeGate policy check is the recorded-authority gate.\n+5. The repo `.shiki/policy` permits autonomous low/medium-risk merges\n+   (SADR-0008/0009); the MergeGate policy check is the recorded-authority gate.\n \n The operator's only actions are: register the task and start the loop. **No\n manual pre-clearing** means after `shiki loop run` starts, the operator does not\ndiff --git a/docs/agents/bootstrap-command.md b/docs/agents/bootstrap-command.md\nindex 05f7f203a52cf412b9f97084a6e131ed0f785afd..f7aa0b734dfebee4a3b139335b66ee3415f48713\n--- a/docs/agents/bootstrap-command.md\n+++ b/docs/agents/bootstrap-command.md\n@@ -189,7 +189,9 @@\n ```\n \n Use this only for tests, fixtures, or explicit local-only template inspection.\n-Use `--force` only when you intentionally want to overwrite existing target files.\n+Use `--force` only when you intentionally want to refresh the target template.\n+Legacy numeric Shiki decision cleanup is stamp-and-digest authorized before any\n+write; target ADR paths are not cleanup candidates.\n \n ## Slash Command\n \ndiff --git a/docs/agents/checklists.md b/docs/agents/checklists.md\nindex 4a7f03ade346f53a1c1d82981f3fd744f9ab4547..0c5ec6c53dc7197b7b7a7d18e667b325babd6c45\n--- a/docs/agents/checklists.md\n+++ b/docs/agents/checklists.md\n@@ -32,18 +32,18 @@\n | ID | Item | Blocking |\n | --- | --- | --- |\n | GD-01 | Plan was challenged against `CONTEXT.md`. | blocking for non-trivial Goals |\n-| GD-02 | Relevant ADRs were read or absence was recorded. | blocking for non-trivial Goals |\n+| GD-02 | Relevant SADRs and target ADRs were read or absence was recorded. | blocking for non-trivial Goals |\n | GD-03 | Ambiguous terms were resolved or converted to HITL blockers. | blocking |\n | GD-04 | Edge scenarios were considered. | advisory |\n | GD-05 | Code reality was checked when code could answer. | blocking when applicable |\n | GD-06 | New domain terms were added to `CONTEXT.md` when needed. | blocking when applicable |\n-| GD-07 | ADR was created or explicitly skipped using the ADR criteria. | blocking when applicable |\n+| GD-07 | A SADR or target ADR was created or explicitly skipped using the decision-record criteria. | blocking when applicable |\n \n ## CI \u2014 Context & Impact Checklist\n \n | ID | Item | Blocking |\n | --- | --- | --- |\n-| CI-01 | Relevant docs and ADRs identified. | blocking |\n+| CI-01 | Relevant docs, SADRs, and target ADRs identified. | blocking |\n | CI-02 | Relevant modules, interfaces, seams, callers, and tests identified. | blocking |\n | CI-03 | Dependencies and blocked-by relationships identified. | blocking |\n | CI-04 | Candidate locks identified. | blocking |\n@@ -62,7 +62,7 @@\n | PRD-04 | Implementation decisions are recorded without volatile code detail. | blocking |\n | PRD-05 | Testing decisions are recorded. | blocking |\n | PRD-06 | Out-of-scope items are explicit. | blocking |\n-| PRD-07 | Relevant glossary/ADR links are present when applicable. | blocking |\n+| PRD-07 | Relevant glossary/SADR/target-ADR links are present when applicable. | blocking |\n \n ## SF \u2014 Spec Freeze Checklist\n \n@@ -98,7 +98,7 @@\n | H-02 | Branch/worktree target is present. | blocking |\n | H-03 | Scope and non-goals are present. | blocking |\n | H-04 | Dependencies and locks are present. | blocking |\n-| H-05 | Relevant docs, ADRs, modules, and tests are referenced. | blocking |\n+| H-05 | Relevant docs, SADRs, target ADRs, modules, and tests are referenced. | blocking |\n | H-06 | Required skills are stated. | blocking |\n | H-07 | TDD expectations are stated. | blocking when behavior work |\n | H-08 | Acceptance criteria are included. | blocking |\ndiff --git a/docs/agents/codex-handoff.md b/docs/agents/codex-handoff.md\nindex ede468f47cb277f69a857e8c45df703e4ed0151d..6f97bdffb67e44d7edea2c71e01ea28933c4eb6c\n--- a/docs/agents/codex-handoff.md\n+++ b/docs/agents/codex-handoff.md\n@@ -46,7 +46,7 @@\n \n ## Context\n - Domain terms:\n-- Relevant ADRs:\n+- Relevant SADRs and target ADRs:\n - Relevant docs:\n - Relevant modules/interfaces/seams:\n - Similar tests/prior art:\ndiff --git a/docs/agents/completion-check-agent.md b/docs/agents/completion-check-agent.md\nindex 13e4c035db35025f4e3cdcc5adfb24314ac4d521..61bfa1218cf36ca605250c91919e620f33238aa0\n--- a/docs/agents/completion-check-agent.md\n+++ b/docs/agents/completion-check-agent.md\n@@ -32,7 +32,7 @@\n - `AGENTS.md`\n - `CLAUDE.md`\n - `CONTEXT.md`\n-- relevant ADRs\n+- relevant SADRs and target ADRs\n - parent Goal issue\n - PRD issue or document\n - task issue\ndiff --git a/docs/agents/decision-control.md b/docs/agents/decision-control.md\nindex 7d8ee5ab03a5ccbcbd04056ae61de03785a2a9c9..8c94ecda00fffaa0e92c325ab83aa9776bd1dac1\n--- a/docs/agents/decision-control.md\n+++ b/docs/agents/decision-control.md\n@@ -11,7 +11,7 @@\n | Decision | Owner | When | Required inputs | Output | Enforcement surface |\n | --- | --- | --- | --- | --- | --- |\n | Is the Goal clear enough to plan? | Planner + Guardian when needed | Goal intake | User request, existing docs, risk signals | Goal readiness verdict | Goal checklist, issue template, ledger |\n-| Must `grill-with-docs` run? | Planner / Skill Gate | Before PRD or issue split | Ambiguity, domain terms, ADR conflicts, unclear boundaries | Skill invocation or explicit skip rationale | Skill Gate checklist |\n+| Must `grill-with-docs` run? | Planner / Skill Gate | Before PRD or issue split | Ambiguity, domain terms, SADR/target-ADR conflicts, unclear boundaries | Skill invocation or explicit skip rationale | Skill Gate checklist |\n | Is a PRD required? | Planner | After Goal context stabilizes | Goal, `grill-with-docs` output, Context & Impact output | PRD required / not required | PRD checklist, CCA evidence check |\n | Can the spec be frozen? | Operator only | End of Requirements Definition, before issue split | PRD/requirements summary, scope inventory (SF-02), open-question list | `spec_freeze` block in the plan + ledger evidence | `require_grilled_plan` (plan ingest/run), SF checklist |\n | Can a frozen spec change? | Operator only (Spec Amendment) | When implementation contradicts the frozen spec | Contested decisions, scoped re-grill result | recorded amendment + re-stamped freeze | SF checklist, ledger evidence |\ndiff --git a/docs/agents/domain.md b/docs/agents/domain.md\nindex 258da8ea81e87ac0a6b0b5b5e5c2fde980eaabb7..d57b0cf17ff3e1fa9e6e5885372bf083d8295421\n--- a/docs/agents/domain.md\n+++ b/docs/agents/domain.md\n@@ -5,7 +5,9 @@\n ## Layout\n \n - `CONTEXT.md` defines Shiki language. Keep it implementation-free.\n-- `docs/adr/` records hard-to-reverse decisions.\n+- `docs/adr/SADR-NNNN-*.md` records Shiki platform decisions.\n+- `docs/adr/NNNN-*.md` records target product decisions without sharing the\n+  SADR namespace.\n - `AGENTS.md` is the runtime-neutral agent constitution.\n - `CLAUDE.md` is the Claude Code wrapper around `AGENTS.md`.\n - `.shiki/` is the Target Repository mirror for Goals, PRDs, plans, Task DAGs, contracts, locks, worktree records, ledger entries, CCA verdicts, repair packets, reports, and handoffs.\n@@ -15,6 +17,6 @@\n \n - Use Shiki terms exactly when they exist.\n - Add glossary terms to `CONTEXT.md` only when the term is part of the domain, not a generic programming concept.\n-- Create ADRs only for hard-to-reverse, surprising, tradeoff-based decisions.\n+- Create SADRs or target ADRs only for hard-to-reverse, surprising, tradeoff-based decisions.\n - Keep product-specific language in the Target Repository, not in Shiki Template docs.\n-- If an ADR conflicts with a proposed implementation, surface the conflict before editing.\n+- If a SADR or target ADR conflicts with a proposed implementation, surface the conflict before editing.\ndiff --git a/docs/agents/execution-control.md b/docs/agents/execution-control.md\nindex ad8ee3cdb36be56fedc1f46408ca92b387d74047..bc63c3a73661d12b567016ba299f1a6b66bf9088\n--- a/docs/agents/execution-control.md\n+++ b/docs/agents/execution-control.md\n@@ -63,7 +63,7 @@\n | Branch name | Orchestrator | Orchestrator | Before implementer dispatch | branch policy | branch ref in task/ledger |\n | Lock reservation | Context & Impact | MergeGate | Before dispatch and before merge | `.shiki/locks/`, labels, PR metadata | lock id, owner, scope |\n | CI profile | Planner | MergeGate | During issue triage, rechecked at PR | labels, `.shiki/policy.yaml`, workflow matrix | required check list |\n-| Required status checks | Repo maintainer / Guardian | GitHub branch protection / rulesets | Repository setup, updated via ADR | GitHub settings | branch/ruleset config, ADR |\n+| Required status checks | Repo maintainer / Guardian | GitHub branch protection / rulesets | Repository setup, updated via target ADR | GitHub settings | branch/ruleset config, target ADR |\n | CCA judgment | CCA | GitHub required check / MergeGate | Every non-draft PR update | GitHub Action check | structured CCA verdict |\n | Repair assignment | CCA | MergeGate | After failed CCA/CI/review | repair packet, issue/PR comment | repair id and attempt count |\n | Merge readiness | MergeGate | GitHub branch protection / Guardian if needed | After CCA complete | required checks, review rules, rulesets | mergegate check output |\ndiff --git a/docs/agents/guardian-policy.md b/docs/agents/guardian-policy.md\nindex 4147c7adf05a47adb4fd24ada1a1c2e1f2ba6d2d..a20d9cc6c00a569bb057b9ca8d6b51c7fc4a9847\n--- a/docs/agents/guardian-policy.md\n+++ b/docs/agents/guardian-policy.md\n@@ -98,8 +98,8 @@\n `origin/<default_branch>` and reuses each job's existing base fetch; a shallow\n (`--depth`) or second base fetch would make the proof unresolvable and silently\n disable the carry, so neither is permitted. CCA reads `guardian_comment_carried` as\n-a recorded authority in the deterministic signal and does not re-derive it. See ADR\n-0018 for the full decision, the rejected diff/patch-equality and path-disjointness\n+a recorded authority in the deterministic signal and does not re-derive it. See\n+SADR-0018 for the full decision, the rejected diff/patch-equality and path-disjointness\n alternatives, and the accepted residual.\n \n ## External AI Guardian Review (SADR-0010)\ndiff --git a/docs/agents/implementation-policy.md b/docs/agents/implementation-policy.md\nindex c754144a9fb5bf4a38aa82a60e1670c59e626b44..85c37281bcad196c6b7b02343faf00b03cd9c57a\n--- a/docs/agents/implementation-policy.md\n+++ b/docs/agents/implementation-policy.md\n@@ -31,10 +31,13 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n ## Non-Negotiables\n \n 1. **Planning precedes implementation.** Non-trivial Goals start with `grill-with-docs` and Context & Impact.\n-2. **Domain language is controlled.** Terms must be checked against `CONTEXT.md`; hard-to-reverse tradeoffs must be checked against ADRs.\n+2. **Domain language is controlled.** Terms must be checked against `CONTEXT.md`; hard-to-reverse tradeoffs must be checked against Shiki SADRs and target ADRs.\n 3. **PRD precedes issue decomposition.** `to-prd` is used once the context is settled enough to become durable intent.\n 4. **Issues are vertical slices.** `to-issues` must produce independently grabbable tracer-bullet tasks, not horizontal layer tickets.\n 5. **Triage controls dispatch.** Only `ready-for-agent` / AFK tasks may be assigned to Codex without human input.\n@@ -95,19 +98,19 @@\n Process:\n \n - Challenge terms against `CONTEXT.md`.\n-- Check ADRs before reopening decisions.\n+- Check SADRs and target ADRs before reopening decisions.\n - Inspect code when code can answer a question.\n - Ask one question at a time when the operator is available.\n - Provide a recommended answer with each question.\n - Record resolved terms in `CONTEXT.md`.\n-- Create ADRs only for hard-to-reverse, surprising, tradeoff-based decisions.\n+- Create SADRs or target ADRs only for hard-to-reverse, surprising, tradeoff-based decisions.\n - Convert unresolved load-bearing questions into HITL blockers.\n \n Outputs:\n \n - Resolved design-tree decisions.\n - Updated glossary terms when needed.\n-- ADRs when justified.\n+- SADRs or target ADRs when justified.\n - HITL blockers when decisions remain unresolved.\n \n ## Phase 3 \u2014 Context & Impact\n@@ -117,7 +120,7 @@\n \n Outputs must include:\n \n-- Relevant documents and ADRs.\n+- Relevant documents, SADRs, and target ADRs.\n - Relevant modules, interfaces, seams, callers, and tests.\n - Candidate locks.\n - Dependencies.\n@@ -140,7 +143,7 @@\n - Testing decisions.\n - Out of scope.\n - Further notes.\n-- Links to relevant Goal, domain terms, and ADRs.\n+- Links to relevant Goal, domain terms, SADRs, and target ADRs.\n \n Phase 4 ends with Spec Freeze: the operator explicitly approves the PRD, the\n plan records a `spec_freeze` block (status, approved_by, source), and the SF\n@@ -240,7 +243,7 @@\n - Dependencies and locks.\n - Risk and Guardian requirements.\n - Ledger completeness.\n-- Documentation and ADR updates.\n+- Documentation and SADR/target-ADR updates.\n - Scope drift.\n \n CCA emits one of:\n@@ -301,7 +304,7 @@\n | Evidence failure | Behavior might be done, but proof is missing. | Add checks, PR evidence, ledger entry, or artifacts. |\n | Scope drift | PR changes unrelated behavior. | Revert unrelated changes or split into new Goal. |\n | Dependency block | Upstream task or lock not complete. | Block until dependency resolves. |\n-| Architecture block | Decision needs ADR or deep-module discussion. | `grill-with-docs` or `improve-codebase-architecture`. |\n+| Architecture block | Decision needs a SADR, target ADR, or deep-module discussion. | `grill-with-docs` or `improve-codebase-architecture`. |\n | Risk block | Security/production/policy change needs human approval. | Guardian decision. |\n \n ## Phase 5.5 \u2014 Execution Decision Control\ndiff --git a/docs/agents/runtime-auth-model.md b/docs/agents/runtime-auth-model.md\nindex 6a2e1a47e81ea89b5c68a210ec07772a9374babc..3c54748d24233dbd0ba8473950282547ef4ce935\n--- a/docs/agents/runtime-auth-model.md\n+++ b/docs/agents/runtime-auth-model.md\n@@ -148,7 +148,7 @@\n \n API-key based runners may be added by a target repository as an explicit extension. They are not the default Shiki template path.\n \n-If a repo opts into API-key mode, record the exception in an ADR and update the repo's `AGENTS.md`, `.shiki/config.yaml`, and workflow permissions.\n+If a repo opts into API-key mode, record the target-specific exception in a target ADR and update the repo's `AGENTS.md`, `.shiki/config.yaml`, and workflow permissions.\n \n ## GitHub Actions Runtime Compatibility\n \ndiff --git a/docs/agents/skill-gate.md b/docs/agents/skill-gate.md\nindex c50398b0d7c60a8598f2a5c88a10015730dced13..3acffe6444617bb1d54b77d4b4708158ec735bc6\n--- a/docs/agents/skill-gate.md\n+++ b/docs/agents/skill-gate.md\n@@ -9,7 +9,7 @@\n | Skill | Trigger | Expected output |\n | --- | --- | --- |\n | `setup-matt-pocock-skills` | First configuring a repo for these skills, or when issue tracker, triage labels, or domain docs are missing. | `docs/agents/` configuration and an `Agent skills` block in `AGENTS.md` or `CLAUDE.md`. |\n-| `grill-with-docs` | Ambiguous plan, unclear terminology, boundary decision, tradeoff, or ADR-worthy design choice. | Clarified decision tree, recommended answers, updates to `CONTEXT.md` or ADRs when decisions crystallize. |\n+| `grill-with-docs` | Ambiguous plan, unclear terminology, boundary decision, tradeoff, or decision-record-worthy design choice. | Clarified decision tree, recommended answers, updates to `CONTEXT.md`, SADRs, or target ADRs when decisions crystallize. |\n | `zoom-out` | Agent lacks a higher-level architectural map. | Map of relevant modules, callers, seams, and domain concepts. |\n | `to-prd` | Settled context should become a PRD. | PRD published to the issue tracker with problem, solution, user stories, decisions, testing, and out-of-scope notes. |\n | `to-issues` | Goal, PRD, or plan must become implementation tickets. | Vertical-slice issues with acceptance criteria, dependencies, and AFK/HITL classification. |\n@@ -17,7 +17,7 @@\n | `tdd` | Feature work or bug fix has observable behavior that can be specified with tests. | Red-green-refactor loop through public interfaces, one vertical slice at a time. |\n | `code-review` | The TDD loop is green and the implementer is about to create or update a PR. Default for every implementation task. | Pre-PR self-review of the task diff with fixes applied on the task branch; findings and resolutions recorded as a ledger entry naming the skill and a `## Pre-PR code review` PR body section. |\n | `diagnose` | Hard bug, failing check, regression, flaky behavior, or performance problem. | Reproduction loop, ranked hypotheses, instrumentation, fix, regression test, post-mortem. |\n-| `improve-codebase-architecture` | Structural friction, testability, AI-navigability, deep-module opportunity, or refactor request. | Architecture candidates and deepening recommendations informed by `CONTEXT.md` and ADRs. |\n+| `improve-codebase-architecture` | Structural friction, testability, AI-navigability, deep-module opportunity, or refactor request. | Architecture candidates and deepening recommendations informed by `CONTEXT.md`, SADRs, and target ADRs. |\n | `prototype` | A throwaway logic or UI prototype is needed to answer a design question. | Clearly marked disposable prototype and captured decision. |\n \n ## MergeGate Interaction\ndiff --git a/docs/operations.md b/docs/operations.md\nindex cf1f452d4c589e38ccd58553bda0ad8684b51eb7..ace48a90ffbec9e5c0a7113bef16a4858bc44b3f\n--- a/docs/operations.md\n+++ b/docs/operations.md\n@@ -89,6 +89,16 @@\n      target history, brought forward by `shiki migrate`, not a template.\n    - **Everything else** (CLI scripts, workflows, schemas, docs) is overwritten.\n \n+   A forced upgrade also migrates the former numeric Shiki decision paths to\n+   `SADR-NNNN-*`. Before the first write, the installer inventories the exact\n+   legacy Shiki paths and authorizes each deletion only when the existing\n+   `.shiki/install-stamp.json` names that path and its SHA-256 digest still\n+   matches the file. An absent, unreadable, malformed, incomplete, or\n+   mismatching stamp reports every blocked path and stops with no filesystem\n+   change. Target-owned `NNNN-*` ADRs are not cleanup candidates. A non-force\n+   install deletes nothing and retains any still-matching legacy stamp entries\n+   so a later forced upgrade can prove ownership.\n+\n    If the target has pending migrations, `--force` refuses and writes nothing \u2014\n    apply migrations first (step 3), then re-run. The run ends with a single\n    summary of every `.new` written, so a half-upgrade is visible.\ndiff --git a/docs/releasing.md b/docs/releasing.md\nindex 10d73828e7dbd64e9d9f6b83bc84d010537ce62a..aafeee8c4d441e1e427efee2ce4f81a647f020e1\n--- a/docs/releasing.md\n+++ b/docs/releasing.md\n@@ -67,7 +67,7 @@\n CLI in `scripts/`. There are no third-party Python runtime dependencies. The CLI\n is intentionally **not** exposed as a `[project.scripts]` console entry point\n because `scripts/shiki.py` is a dependency-free shim and `scripts/` is not\n-packaged as an importable distribution (see the ADR for the rationale).\n+packaged as an importable distribution (see SADR-0007 for the rationale).\n \n Supported install paths:\n \n@@ -155,6 +155,15 @@\n    [docs/operations.md](operations.md#the-new-convention). If the target has\n    pending migrations, `--force` refuses and writes nothing; apply migrations\n    (step 2) first, then re-run.\n+\n+   The SADR namespace upgrade has an additional pre-write gate: each former\n+   numeric Shiki decision path is removed only when the existing install stamp\n+   contains that exact path and a digest matching its current bytes. The\n+   installer aggregates absent/unreadable/malformed-stamp, missing-digest, and\n+   digest-mismatch blockers before raising, so refusal leaves the target tree\n+   byte-identical. Numeric target ADRs are preserved. A successful forced\n+   upgrade installs and stamps `SADR-*` paths and drops the removed legacy paths\n+   from the refreshed stamp.\n 2. Apply repository-local `.shiki/` state migrations with the migrate command:\n \n    ```bash\ndiff --git a/docs/usage.md b/docs/usage.md\nindex b83407ee3970dd395dd893010bc5dbe8bad26051..a130ef6e40dfd8169a5bc701339351097df1a09f\n--- a/docs/usage.md\n+++ b/docs/usage.md\n@@ -67,8 +67,10 @@\n shiki install-target /path/to/target-repo --local-only\n ```\n \n-Use `--force` only when you intentionally want to overwrite existing target\n-files.\n+Use `--force` only when you intentionally want to refresh the target template.\n+For the SADR namespace migration, `--force` deletes only exact legacy Shiki\n+decision paths whose existing install-stamp digest still matches; any ownership\n+blocker stops before the first write. Numeric target ADRs are never candidates.\n \n ## `shiki bootstrap-platform` \u2014 publish the Shiki platform repo\n \ndiff --git a/scripts/guardian_approval_signal.py b/scripts/guardian_approval_signal.py\nindex d059f92b2c82aa2b5d6ca421365537a87886c1cb..16924892714f4f5b9303c0ef0e325a7e70075630\n--- a/scripts/guardian_approval_signal.py\n+++ b/scripts/guardian_approval_signal.py\n@@ -199,8 +199,8 @@\n ) -> Any:\n     \"\"\"Evaluate SADR-0015 Contract Approval for the CCA Guardian signal.\n \n-    The SAME OR that MergeGate applies must reach this deterministic signal: ADR\n-    0015 is explicit that without it, a contract-approved PR still returns\n+    The SAME OR that MergeGate applies must reach this deterministic signal:\n+    SADR-0015 is explicit that without it, a contract-approved PR still returns\n     ``needs_guardian``, the CCA job fails, and the MergeGate policy check never\n     runs \u2014 the gate this change moves would never be reached. This resolves the\n     task the SAME way MergeGate does (the first Shiki task id in the PR body),\ndiff --git a/scripts/shiki_contract_approval.py b/scripts/shiki_contract_approval.py\nindex 941bcb96fdd8b9a85bc0c4f0f04d5dad9d04839c..4a4b418f6527413161db82670bec857a043360e2\n--- a/scripts/shiki_contract_approval.py\n+++ b/scripts/shiki_contract_approval.py\n@@ -142,7 +142,7 @@\n     changed_files_status: Any,\n     registration: Any,\n ) -> ContractApprovalResult:\n-    \"\"\"Whether this PR carries an SADR-0015 Contract Approval.\n+    \"\"\"Whether this PR carries a SADR-0015 Contract Approval.\n \n     The four conditions are evaluated IN ORDER, each failure recorded as a\n     distinct reason string:\ndiff --git a/scripts/shiki_guardian.py b/scripts/shiki_guardian.py\nindex a4ecb79bbda55fd5c8b06adfae2eb99760a03c45..4dd99bbe714e26f421fa19abac7a34ae1b195ecc\n--- a/scripts/shiki_guardian.py\n+++ b/scripts/shiki_guardian.py\n@@ -12,7 +12,7 @@\n # The carry re-derives its proof from git in-process on every judging invocation;\n # it is never read from an artifact, so there is nothing to go stale or be forged.\n # shiki_sync_proof is the ONLY guardian-family module that runs git, and every one\n-# of its invocations is read-only and local (ADR: prove-from-git base sync).\n+# of its invocations is read-only and local (SADR: prove-from-git base sync).\n from shiki_sync_proof import verify_pure_base_sync\n \n GUARDIAN_POLICY_PATH = \".shiki/guardian-policy.json\"\ndiff --git a/scripts/shiki_guardian_review.py b/scripts/shiki_guardian_review.py\nindex dd0f104d457899e0ba5ff1e162011c1e90b6f92b..e10ef58c046324e7ea3a3392feb3048ca77414ce\n--- a/scripts/shiki_guardian_review.py\n+++ b/scripts/shiki_guardian_review.py\n@@ -159,7 +159,7 @@\n     docs_only = _is_docs_only(changed_files)\n     if docs_only and matched == 0:\n         focus.append(\n-            \"Docs/ADR-only change: confirm the change is genuinely documentation/decision-only, introduces no behavior or contract drift, and stays consistent with the constitution and existing ADRs.\"\n+            \"Docs/decision-record-only change: confirm the change is genuinely documentation/decision-only, introduces no behavior or contract drift, and stays consistent with the constitution and existing SADRs and target ADRs.\"\n         )\n \n     # Conservative augmentation: unknown (no category and not docs-only), mixed\ndiff --git a/scripts/shiki_installer.py b/scripts/shiki_installer.py\nindex be064ab7a29ff6836f7f7a1d70500a43e0d11d7b..9fd8eefd137ce7003dad5c0dcb2e48e2cd47ab32\n--- a/scripts/shiki_installer.py\n+++ b/scripts/shiki_installer.py\n@@ -118,6 +118,7 @@\n # what lets a target carry it without a governance-schema change.\n INSTALL_STAMP_PATH = \".shiki/install-stamp.json\"\n INSTALL_STAMP_VERSION = 1\n+LEGACY_SHIKI_SADR_MAX = 18\n \n # Under --force the shipped surface splits three ways, so nothing is lost and\n # nothing is silently stale.\n@@ -237,9 +238,98 @@\n         )\n \n \n+def legacy_shiki_adr_paths() -> tuple[str, ...]:\n+    \"\"\"Former numeric paths for Shiki SADR-0001 through SADR-0018.\n+\n+    The authoritative slugs come from the current SADR filenames. SADR-0019 is\n+    the namespace decision itself and never shipped under a numeric legacy path.\n+    \"\"\"\n+    paths: list[str] = []\n+    for sadr in sorted((ROOT / \"docs\" / \"adr\").glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\")):\n+        number = sadr.name.split(\"-\", 2)[1]\n+        if int(number) <= LEGACY_SHIKI_SADR_MAX:\n+            paths.append(f\"docs/adr/{sadr.name.removeprefix('SADR-')}\")\n+    return tuple(paths)\n+\n+\n+def preflight_legacy_sadr_cleanup(target: Path, *, force: bool) -> tuple[str, ...]:\n+    \"\"\"Authorize exact legacy Shiki ADR removals before the first write.\n+\n+    A non-force install never removes existing files. A forced install needs no\n+    stamp when no legacy Shiki path is present. When legacy paths do exist, each\n+    must be named in a readable install stamp and its current bytes must match\n+    the recorded digest.\n+    \"\"\"\n+    if not force:\n+        return ()\n+    present = tuple(\n+        relative\n+        for relative in legacy_shiki_adr_paths()\n+        if (target / relative).exists() or (target / relative).is_symlink()\n+    )\n+    if not present:\n+        return ()\n+\n+    stamp_path = target / INSTALL_STAMP_PATH\n+    try:\n+        raw = stamp_path.read_text(encoding=\"utf-8\")\n+        stamp = json.loads(raw)\n+    except FileNotFoundError:\n+        detail = \"install stamp is absent\"\n+        blockers = [f\"{relative}: {detail}\" for relative in present]\n+    except (OSError, UnicodeDecodeError) as error:\n+        detail = f\"install stamp is unreadable ({error})\"\n+        blockers = [f\"{relative}: {detail}\" for relative in present]\n+    except json.JSONDecodeError as error:\n+        detail = f\"install stamp is malformed ({error})\"\n+        blockers = [f\"{relative}: {detail}\" for relative in present]\n+    else:\n+        if not isinstance(stamp, dict):\n+            blockers = [\n+                f\"{relative}: install stamp is malformed (expected an object)\"\n+                for relative in present\n+            ]\n+        else:\n+            digests = stamp.get(\"digests\")\n+            blockers = []\n+            if not isinstance(digests, dict):\n+                blockers.extend(\n+                    f\"{relative}: install stamp has no digest for this path\"\n+                    for relative in present\n+                )\n+            else:\n+                for relative in present:\n+                    expected = digests.get(relative)\n+                    if not isinstance(expected, str) or not expected:\n+                        blockers.append(\n+                            f\"{relative}: install stamp has no digest for this path\"\n+                        )\n+                        continue\n+                    try:\n+                        actual = _sha256_file(target / relative)\n+                    except OSError as error:\n+                        blockers.append(\n+                            f\"{relative}: legacy path is unreadable ({error})\"\n+                        )\n+                        continue\n+                    if actual != expected:\n+                        blockers.append(\n+                            f\"{relative}: digest mismatch (stamp {expected}, current {actual})\"\n+                        )\n+\n+    if blockers:\n+        joined = \"\\n\".join(f\"- {blocker}\" for blocker in blockers)\n+        raise ShikiError(\n+            \"legacy Shiki ADR cleanup is not authorized:\\n\"\n+            f\"{joined}\\nNo files were written.\"\n+        )\n+    return present\n+\n+\n def install_template(target: Path, *, force: bool, validate: bool) -> list[NewFileNote]:\n     # Preconditions first: never rewrite the tree of a target that cannot be\n     # upgraded cleanly (see refuse_pending_migrations).\n+    legacy_cleanup = preflight_legacy_sadr_cleanup(target, force=force)\n     refuse_pending_migrations(target)\n \n     # Resolve the target's required CODEOWNERS owner once from its\n@@ -250,6 +340,10 @@\n     # verbatim. See ``codeowners_required_owner``.\n     codeowners_owner = codeowners_required_owner(target)\n \n+    for relative in legacy_cleanup:\n+        (target / relative).unlink()\n+        info(f\"removed stamped legacy Shiki ADR: {target / relative}\")\n+\n     notes: list[NewFileNote] = []\n     for relative in TEMPLATE_PATHS:\n         source = ROOT / relative\n@@ -647,6 +741,22 @@\n     digests = target_stamp_digests(target)\n     existing = read_install_stamp(target)\n     if existing is not None:\n+        # A non-force install deliberately leaves legacy Shiki ADRs in place.\n+        # Preserve only still-matching ownership proof so a later forced\n+        # upgrade can authorize cleanup; never carry a missing or stale digest.\n+        existing_digests = existing.get(\"digests\")\n+        if isinstance(existing_digests, dict):\n+            for relative in legacy_shiki_adr_paths():\n+                expected = existing_digests.get(relative)\n+                path = target / relative\n+                if not isinstance(expected, str) or not path.is_file():\n+                    continue\n+                try:\n+                    actual = _sha256_file(path)\n+                except OSError:\n+                    continue\n+                if actual == expected:\n+                    digests[relative] = actual\n         if _incoming_is_older(commit, existing.get(\"platform_commit\")):\n             warn(\n                 \"kept existing install stamp: incoming install \"\ndiff --git a/scripts/shiki_loop.py b/scripts/shiki_loop.py\nindex 3611641da8aeb5038b1da90e3114b09a473ec6d8..84caa491b9cad0edc4176f209c13e9221e975681\n--- a/scripts/shiki_loop.py\n+++ b/scripts/shiki_loop.py\n@@ -424,7 +424,7 @@\n         # unresolvable CCA verdict has already produced its terminal stop and is never\n         # laundered into a head-moving sync (SADR-0010). It also never fires while the\n         # policy (Guardian) gate is red \u2014 for ANY reason, not only when it is the sole\n-        # red check \u2014 so a head-bound Guardian approval, or an SADR-0015\n+        # red check \u2014 so a head-bound Guardian approval, or a SADR-0015\n         # contract-approval carry that base movement defeated (the policy gate compares\n         # base and head task governance directly), is never silently restored by\n         # adopting base wholesale. It is bounded by a durable per-task counter so the\ndiff --git a/scripts/shiki_tasks.py b/scripts/shiki_tasks.py\nindex aec3809f24b4a0c8c96fc50971cff9ae9ad30381..d531052e9d9717058d4631483cefab44c26a65c1\n--- a/scripts/shiki_tasks.py\n+++ b/scripts/shiki_tasks.py\n@@ -821,7 +821,7 @@\n         \"prompt\": prompt,\n         \"entry_skill\": \"grill-with-docs\",\n         \"required_next_steps\": [\n-            \"Run grill-with-docs until terminology, boundaries, risks, and ADR-worthy decisions are settled.\",\n+            \"Run grill-with-docs until terminology, boundaries, risks, and decision-record-worthy choices are settled.\",\n             \"Obtain the operator's explicit approval of the PRD/requirements (Spec Freeze).\",\n             \"Write a machine-readable plan JSON with grill_with_docs.status=complete and spec_freeze.status=frozen.\",\n             \"Run shiki plan ingest --plan-file PLAN.json.\",\ndiff --git a/scripts/test_shiki_goal_loop.sh b/scripts/test_shiki_goal_loop.sh\nindex 753c7b516e10f0be3bc92a4bd0eb1fa699776ae6..8f3820a0a079b3044424cfe1a6c6b7fe68896e67\n--- a/scripts/test_shiki_goal_loop.sh\n+++ b/scripts/test_shiki_goal_loop.sh\n@@ -231,8 +231,8 @@\n \n # High/critical risk merges autonomously when all required checks are green:\n # the \"MergeGate policy check\" required check IS the Guardian gate (it enforces\n-# guardian-policy.json \u2014 human approval OR external AI guardian review, ADR\n-# 0010), so green checks mean a recorded authority approved. The fake gh returns\n+# guardian-policy.json \u2014 human approval OR external AI guardian review,\n+# SADR-0010), so green checks mean a recorded authority approved. The fake gh returns\n # green checks, simulating that the Guardian gate passed.\n python3 \"$ROOT/scripts/shiki.py\" goal create --target \"$TARGET\" --title \"Guardian gate\" --outcome \"High risk merges via policy gate\" >/tmp/shiki-goal-loop-guardian-goal.json\n GGOAL=\"$(json_get /tmp/shiki-goal-loop-guardian-goal.json goal_id)\"\ndiff --git a/tests/test_adr_references.py b/tests/test_adr_references.py\nindex 51706329c797d5dd9b47e082a333fa83b261d482..d005da4f771f5e8085ec22d4b787980e68f5ae8c\n--- a/tests/test_adr_references.py\n+++ b/tests/test_adr_references.py\n@@ -1,8 +1,8 @@\n-\"\"\"Every ``ADR ####`` citation must resolve to exactly one decision record.\n+\"\"\"Every target ADR and Shiki SADR citation resolves in its own namespace.\n \n Background. ``scripts/mergegate_check.py`` justifies ``is_bookkeeping_closeout``\n-with \"ADR 0017\" nine times and ``scripts/guardian_approval_signal.py`` cites\n-0016/0017 seven times, yet for a stretch the numbering in ``docs/adr/`` stopped\n+with \"SADR-0017\" nine times and ``scripts/guardian_approval_signal.py`` cites\n+SADR-0016/SADR-0017 seven times, yet for a stretch the numbering in ``docs/adr/`` stopped\n at 0015: the authoring commits lived on a coordinator branch and no task ever\n declared ``docs/adr/...`` in its locks, so nothing carried the records to the\n default branch. The stated basis of an exemption that merged seven PRs with zero\n@@ -11,11 +11,12 @@\n and the directory drifted with no signal.\n \n This is the same unbound-pair shape behind other silent failures: a reference\n-and its target with nothing asserting they agree. This test binds them. It scans\n-every ``scripts/*.py`` (plus ``docs/**/*.md`` and the root ``*.md`` governance\n-files, which are cheap) for the ``ADR ####`` pattern and asserts each cited\n-number resolves to exactly one ``docs/adr/####-*.md``. The converse is\n-deliberately NOT required: an ADR may exist without being cited.\n+and its target with nothing asserting they agree. This test binds them. Target\n+``ADR NNNN`` citations resolve only to ``docs/adr/NNNN-*.md``; Shiki\n+``SADR-NNNN`` citations resolve only to ``docs/adr/SADR-NNNN-*.md``. The scan\n+covers current governance docs, scripts, workflows, prompts, packaging metadata,\n+and repo-local Codex guidance while deliberately excluding historical\n+``.shiki`` evidence. The converse is not required: a record may be uncited.\n \n The suite is dependency free: standard-library ``unittest`` only.\n \"\"\"\n@@ -31,17 +32,18 @@\n \n REPO_ROOT = Path(__file__).resolve().parents[1]\n \n-# \"ADR 0016\" -- a four-digit citation. The trailing \\b keeps a five-digit token\n-# from being read as a truncated four-digit number.\n-_CITATION = re.compile(r\"\\bADR (\\d{4})\\b\")\n-# \"0016-locks-bound-product-paths.md\" -- the leading four digits name the record.\n-_ADR_FILENAME = re.compile(r\"^(\\d{4})-.*\\.md$\")\n+# ``ADR 0016`` / ``SADR-0016`` -- a namespace plus four-digit citation. The\n+# trailing ``\\b`` keeps a five-digit token from being read as a truncated one.\n+_CITATION = re.compile(r\"\\b(S?ADR)[ -](\\d{4})\\b\")\n+# Target ADRs retain the numeric filename; Shiki platform decisions use SADR.\n+_DECISION_FILENAME = re.compile(r\"^(?:(SADR)-)?(\\d{4})-.*\\.md$\")\n \n \n class Violation(NamedTuple):\n     \"\"\"A citation that does not resolve to exactly one decision record.\"\"\"\n \n     citing_file: str  # repo-relative path of the file that cites the ADR\n+    namespace: str  # \"ADR\" for target decisions; \"SADR\" for Shiki decisions\n     number: str  # the cited four-digit number, e.g. \"0016\"\n     line: int  # 1-based line of the citation\n     reason: str  # \"missing\" (zero matches) or \"ambiguous\" (two or more)\n@@ -49,44 +51,59 @@\n \n     @property\n     def expected_path(self) -> str:\n-        return f\"docs/adr/{self.number}-*.md\"\n+        prefix = \"SADR-\" if self.namespace == \"SADR\" else \"\"\n+        return f\"docs/adr/{prefix}{self.number}-*.md\"\n \n     def describe(self) -> str:\n         \"\"\"Name the citing file, the cited number, and the missing path.\"\"\"\n         if self.reason == \"missing\":\n             return (\n-                f\"{self.citing_file}:{self.line} cites ADR {self.number} \"\n+                f\"{self.citing_file}:{self.line} cites {self.namespace} {self.number} \"\n                 f\"but no document matches {self.expected_path}\"\n             )\n         joined = \", \".join(self.matches)\n         return (\n-            f\"{self.citing_file}:{self.line} cites ADR {self.number} which \"\n+            f\"{self.citing_file}:{self.line} cites {self.namespace} {self.number} which \"\n             f\"resolves to {len(self.matches)} documents (expected exactly one \"\n             f\"at {self.expected_path}): {joined}\"\n         )\n \n \n-def _adr_index(root: Path) -> dict[str, list[Path]]:\n-    \"\"\"Map every four-digit ADR number to the record files that claim it.\"\"\"\n-    index: dict[str, list[Path]] = {}\n+def _adr_index(root: Path) -> dict[tuple[str, str], list[Path]]:\n+    \"\"\"Map each decision namespace and number to the files that claim it.\"\"\"\n+    index: dict[tuple[str, str], list[Path]] = {}\n     adr_dir = root / \"docs\" / \"adr\"\n     if adr_dir.is_dir():\n         for path in sorted(adr_dir.glob(\"*.md\")):\n-            match = _ADR_FILENAME.match(path.name)\n+            match = _DECISION_FILENAME.match(path.name)\n             if match:\n-                index.setdefault(match.group(1), []).append(path)\n+                namespace = \"SADR\" if match.group(1) else \"ADR\"\n+                index.setdefault((namespace, match.group(2)), []).append(path)\n     return index\n \n \n def _citing_files(root: Path, include_docs: bool = True) -> list[Path]:\n-    \"\"\"Files scanned for citations: scripts/*.py, and cheaply the markdown.\"\"\"\n+    \"\"\"Current, shipped text surfaces scanned for decision citations.\"\"\"\n     files = sorted((root / \"scripts\").glob(\"*.py\"))\n+    files += sorted((root / \"scripts\").glob(\"*.sh\"))\n     if include_docs:\n         docs = root / \"docs\"\n         if docs.is_dir():\n             files += sorted(docs.rglob(\"*.md\"))\n         files += sorted(root.glob(\"*.md\"))\n-    return files\n+        pyproject = root / \"pyproject.toml\"\n+        if pyproject.is_file():\n+            files.append(pyproject)\n+        for relative in (\".github\", \".codex\"):\n+            surface = root / relative\n+            if surface.is_dir():\n+                files += sorted(\n+                    path\n+                    for path in surface.rglob(\"*\")\n+                    if path.is_file()\n+                    and path.suffix in {\".md\", \".py\", \".sh\", \".toml\", \".yaml\", \".yml\"}\n+                )\n+    return sorted(set(files))\n \n \n def find_adr_reference_violations(\n@@ -94,10 +111,9 @@\n ) -> list[Violation]:\n     \"\"\"Return every ``ADR ####`` citation under ``root`` that fails to resolve.\n \n-    A citation resolves when exactly one ``docs/adr/####-*.md`` matches its\n-    number. Zero matches is a \"missing\" violation; two or more is \"ambiguous\".\n-    Records that are never cited produce nothing -- the converse is not\n-    required.\n+    A citation resolves when exactly one record in the same namespace matches\n+    its number. Zero matches is a \"missing\" violation; two or more is\n+    \"ambiguous\". Records that are never cited produce nothing.\n     \"\"\"\n     root = Path(root)\n     index = _adr_index(root)\n@@ -110,13 +126,15 @@\n         rel = path.relative_to(root).as_posix()\n         for lineno, line in enumerate(text.splitlines(), start=1):\n             for match in _CITATION.finditer(line):\n-                number = match.group(1)\n-                found = index.get(number, [])\n+                namespace, number = match.groups()\n+                found = index.get((namespace, number), [])\n                 if len(found) == 1:\n                     continue\n                 reason = \"missing\" if not found else \"ambiguous\"\n                 rels = [p.relative_to(root).as_posix() for p in found]\n-                violations.append(Violation(rel, number, lineno, reason, rels))\n+                violations.append(\n+                    Violation(rel, namespace, number, lineno, reason, rels)\n+                )\n     return violations\n \n \n@@ -129,8 +147,8 @@\n         self.assertEqual(\n             violations,\n             [],\n-            \"Every 'ADR ####' citation must resolve to exactly one \"\n-            f\"docs/adr/####-*.md:\\n{detail}\",\n+            \"Every ADR/SADR citation must resolve exactly once in its own \"\n+            f\"docs/adr namespace:\\n{detail}\",\n         )\n \n     def test_scan_actually_reads_scripts(self) -> None:\n@@ -142,12 +160,38 @@\n             \"the citation scan must cover scripts/*.py\",\n         )\n \n+    def test_platform_citations_do_not_use_unresolved_numeric_shorthand(self) -> None:\n+        shorthand = re.compile(r\"SADR-\\d{4}/\\d{4}\")\n+        offenders = []\n+        for path in _citing_files(REPO_ROOT):\n+            text = path.read_text(encoding=\"utf-8\")\n+            if shorthand.search(text):\n+                offenders.append(path.relative_to(REPO_ROOT).as_posix())\n+        self.assertEqual(\n+            offenders,\n+            [],\n+            \"repeat the SADR prefix so every cited number is independently resolvable\",\n+        )\n+\n     def test_carried_records_are_indexed(self) -> None:\n         # The two records this Goal carried onto the default branch, and which\n         # the scripts cite most, must be present.\n         index = _adr_index(REPO_ROOT)\n-        self.assertIn(\"0016\", index)\n-        self.assertIn(\"0017\", index)\n+        self.assertIn((\"SADR\", \"0016\"), index)\n+        self.assertIn((\"SADR\", \"0017\"), index)\n+\n+    def test_platform_tree_has_only_sadr_0001_through_0019(self) -> None:\n+        adr_dir = REPO_ROOT / \"docs\" / \"adr\"\n+        sadrs = sorted(adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\"))\n+        self.assertEqual(\n+            {path.name.split(\"-\", 2)[1] for path in sadrs},\n+            {f\"{number:04d}\" for number in range(1, 20)},\n+        )\n+        self.assertEqual(\n+            list(adr_dir.glob(\"[0-9][0-9][0-9][0-9]-*.md\")),\n+            [],\n+            \"the Shiki platform tree must not retain legacy numeric SADR paths\",\n+        )\n \n \n class FixtureAdrReferences(unittest.TestCase):\n@@ -180,6 +224,26 @@\n         self.assertIn(\"9991\", message)  # cited number\n         self.assertIn(\"docs/adr/9991-*.md\", message)  # missing path\n \n+    def test_target_adr_does_not_satisfy_missing_platform_sadr(self) -> None:\n+        root = self._make_repo(\n+            adr_names=[\"0042-target-decision.md\"],\n+            script_body='\"\"\"Target ADR 0042 exists, but platform SADR-0042 does not.\"\"\"\\n',\n+        )\n+        violations = find_adr_reference_violations(root, include_docs=False)\n+        self.assertEqual(len(violations), 1)\n+        self.assertEqual(violations[0].namespace, \"SADR\")\n+        self.assertEqual(violations[0].number, \"0042\")\n+        self.assertEqual(violations[0].expected_path, \"docs/adr/SADR-0042-*.md\")\n+\n+    def test_target_adr_and_platform_sadr_may_reuse_a_number(self) -> None:\n+        root = self._make_repo(\n+            adr_names=[\"0042-target-decision.md\", \"SADR-0042-platform-decision.md\"],\n+            script_body='\"\"\"Target ADR 0042 and platform SADR-0042 are distinct.\"\"\"\\n',\n+        )\n+        self.assertEqual(\n+            find_adr_reference_violations(root, include_docs=False), []\n+        )\n+\n     def test_uncited_adr_is_allowed(self) -> None:\n         # An ADR exists but nothing cites it -> not a violation. The converse\n         # (every ADR must be cited) is explicitly NOT required.\ndiff --git a/tests/test_guardian_review.py b/tests/test_guardian_review.py\nindex 88829e7810212635e2fc98aa5918d6f26294677d..00a85a237acd5bf2d6cb5b294128198c422402ca\n--- a/tests/test_guardian_review.py\n+++ b/tests/test_guardian_review.py\n@@ -162,7 +162,7 @@\n \n     def test_docs_only_is_light_and_not_conservative(self):\n         focus = gr.classify_review_focus([\"docs/adr/0099-x.md\", \"README.md\"], \"\", \"low\")\n-        self.assertTrue(any(\"Docs/ADR-only\" in f for f in focus))\n+        self.assertTrue(any(\"Docs/decision-record-only\" in f for f in focus))\n         self.assertFalse(any(\"conservative default\" in f.lower() for f in focus))\n \n     def test_unknown_category_is_conservative(self):\ndiff --git a/tests/test_policy_job_merged_prs.py b/tests/test_policy_job_merged_prs.py\nindex d45512e74681e5e649279236c7d4e79d5c3da471..b97989f8bd74d091de1a6b5275a20d8dc68c4512\n--- a/tests/test_policy_job_merged_prs.py\n+++ b/tests/test_policy_job_merged_prs.py\n@@ -3,8 +3,8 @@\n \n ``.github/workflows/shiki-cca-completion.yml`` has two consumers of the merged-PR\n proof: the CCA *signal* (``guardian_approval_signal.py`` in \"Prepare CCA context\")\n-and the authoritative gate (``mergegate_check.py`` in \"Enforce MergeGate\"). ADR\n-0017's bookkeeping-closeout exemption (condition 6) needs the merged implementation\n+and the authoritative gate (``mergegate_check.py`` in \"Enforce MergeGate\").\n+SADR-0017's bookkeeping-closeout exemption (condition 6) needs the merged implementation\n PR proven, and a closeout carries NO ``post_merge_reconcile`` marker. The signal\n already resolves ``MERGED_PRS`` unconditionally, but \"Enforce MergeGate\" used to\n resolve it only inside ``grep -q '<!-- shiki:post_merge_reconcile -->'``. So a\ndiff --git a/tests/test_sadr_install_migration.py b/tests/test_sadr_install_migration.py\nnew file mode 100644\nindex 0000000000000000000000000000000000000000..d51eac872b1c79ff4579c4c1401ca5d775a60d8d\n--- /dev/null\n+++ b/tests/test_sadr_install_migration.py\n@@ -0,0 +1,237 @@\n+\"\"\"SADR installation and collision-safe legacy upgrade behavior.\"\"\"\n+\n+from __future__ import annotations\n+\n+import contextlib\n+import hashlib\n+import io\n+import json\n+import shutil\n+import tempfile\n+import unittest\n+from pathlib import Path\n+\n+import shiki_test_support  # noqa: F401  (path bootstrap)\n+\n+import shiki_installer\n+from tests.test_adr_references import find_adr_reference_violations\n+\n+\n+PRODUCT_NUMBERS = tuple(range(13, 19))\n+\n+\n+def _quiet_install(target: Path, *, force: bool, validate: bool) -> None:\n+    with contextlib.redirect_stdout(io.StringIO()):\n+        shiki_installer.install_template(target, force=force, validate=validate)\n+\n+\n+def _sha256(path: Path) -> str:\n+    return hashlib.sha256(path.read_bytes()).hexdigest()\n+\n+\n+def _legacy_sadr_paths() -> tuple[str, ...]:\n+    return tuple(\n+        f\"docs/adr/{path.name.removeprefix('SADR-')}\"\n+        for path in sorted((shiki_installer.ROOT / \"docs\" / \"adr\").glob(\"SADR-*.md\"))\n+        if path.name != \"SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\"\n+    )\n+\n+\n+def _create_legacy_upgrade_fixture(\n+    target: Path,\n+) -> tuple[dict[str, bytes], tuple[str, ...]]:\n+    adr_dir = target / \"docs\" / \"adr\"\n+    adr_dir.mkdir(parents=True)\n+    product_bytes = {\n+        f\"{number:04d}-cubeshare-product-decision.md\": (\n+            f\"# CubeShare ADR {number:04d}\\n\\nDo not rewrite {number}.\\n\"\n+        ).encode()\n+        for number in PRODUCT_NUMBERS\n+    }\n+    for name, content in product_bytes.items():\n+        (adr_dir / name).write_bytes(content)\n+\n+    legacy_paths = _legacy_sadr_paths()\n+    for relative in legacy_paths:\n+        (target / relative).write_bytes(f\"legacy Shiki bytes for {relative}\\n\".encode())\n+\n+    stamp_path = target / shiki_installer.INSTALL_STAMP_PATH\n+    stamp_path.parent.mkdir(parents=True, exist_ok=True)\n+    stamp = {\n+        \"version\": shiki_installer.INSTALL_STAMP_VERSION,\n+        \"platform_commit\": None,\n+        \"installed_at\": \"2000-01-01T00:00:00+00:00\",\n+        \"digests\": {\n+            relative: _sha256(target / relative) for relative in legacy_paths\n+        },\n+    }\n+    stamp_path.write_text(\n+        json.dumps(stamp, indent=2, sort_keys=True) + \"\\n\", encoding=\"utf-8\"\n+    )\n+    return product_bytes, legacy_paths\n+\n+\n+def _tree_snapshot(root: Path) -> dict[str, tuple[str, bytes | str]]:\n+    \"\"\"Recursive byte/shape snapshot used to prove preflight is write-free.\"\"\"\n+    snapshot: dict[str, tuple[str, bytes | str]] = {}\n+    for path in sorted(root.rglob(\"*\")):\n+        relative = path.relative_to(root).as_posix()\n+        if path.is_symlink():\n+            snapshot[relative] = (\"symlink\", path.readlink().as_posix())\n+        elif path.is_dir():\n+            snapshot[relative] = (\"directory\", \"\")\n+        else:\n+            snapshot[relative] = (\"file\", path.read_bytes())\n+    return snapshot\n+\n+\n+class FreshSadrInstallTests(unittest.TestCase):\n+    def setUp(self) -> None:\n+        self.tmp = Path(tempfile.mkdtemp(prefix=\"shiki-sadr-fresh-\"))\n+        self.addCleanup(lambda: shutil.rmtree(self.tmp, ignore_errors=True))\n+        self.target = self.tmp / \"target\"\n+        self.adr_dir = self.target / \"docs\" / \"adr\"\n+        self.adr_dir.mkdir(parents=True)\n+        self.product_bytes = {\n+            f\"{number:04d}-product-decision.md\": (\n+                f\"# Product ADR {number:04d}\\n\\nTarget-owned bytes {number}.\\n\"\n+            ).encode()\n+            for number in PRODUCT_NUMBERS\n+        }\n+        for name, content in self.product_bytes.items():\n+            (self.adr_dir / name).write_bytes(content)\n+\n+    def test_fresh_install_preserves_product_adrs_and_installs_all_sadrs(self) -> None:\n+        _quiet_install(self.target, force=False, validate=True)\n+\n+        for name, expected in self.product_bytes.items():\n+            self.assertEqual((self.adr_dir / name).read_bytes(), expected)\n+\n+        sadr_numbers = {\n+            path.name.split(\"-\", 2)[1]\n+            for path in self.adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\")\n+        }\n+        self.assertEqual(sadr_numbers, {f\"{number:04d}\" for number in range(1, 20)})\n+\n+        numeric_records = {\n+            path.name\n+            for path in self.adr_dir.glob(\"[0-9][0-9][0-9][0-9]-*.md\")\n+        }\n+        self.assertEqual(numeric_records, set(self.product_bytes))\n+        stamp = json.loads(\n+            (self.target / shiki_installer.INSTALL_STAMP_PATH).read_text(\n+                encoding=\"utf-8\"\n+            )\n+        )\n+        for path in self.adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\"):\n+            relative = path.relative_to(self.target).as_posix()\n+            self.assertEqual(stamp[\"digests\"][relative], _sha256(path))\n+        self.assertEqual(find_adr_reference_violations(self.target), [])\n+\n+\n+class LegacySadrUpgradeTests(unittest.TestCase):\n+    def setUp(self) -> None:\n+        self.tmp = Path(tempfile.mkdtemp(prefix=\"shiki-sadr-upgrade-\"))\n+        self.addCleanup(lambda: shutil.rmtree(self.tmp, ignore_errors=True))\n+        self.target = self.tmp / \"target\"\n+        self.adr_dir = self.target / \"docs\" / \"adr\"\n+        self.product_bytes, self.legacy_paths = _create_legacy_upgrade_fixture(\n+            self.target\n+        )\n+\n+    def test_force_upgrade_deletes_only_stamped_legacy_sadrs(self) -> None:\n+        _quiet_install(self.target, force=True, validate=True)\n+\n+        for relative in self.legacy_paths:\n+            self.assertFalse((self.target / relative).exists(), relative)\n+        for name, expected in self.product_bytes.items():\n+            self.assertEqual((self.adr_dir / name).read_bytes(), expected)\n+\n+        sadrs = list(self.adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\"))\n+        self.assertEqual(len(sadrs), 19)\n+        refreshed = json.loads(\n+            (self.target / shiki_installer.INSTALL_STAMP_PATH).read_text(encoding=\"utf-8\")\n+        )\n+        for relative in self.legacy_paths:\n+            self.assertNotIn(relative, refreshed[\"digests\"])\n+        for path in sadrs:\n+            relative = path.relative_to(self.target).as_posix()\n+            self.assertEqual(refreshed[\"digests\"][relative], _sha256(path))\n+\n+    def test_non_force_install_never_deletes_legacy_files(self) -> None:\n+        before = {\n+            relative: (self.target / relative).read_bytes()\n+            for relative in self.legacy_paths\n+        }\n+        _quiet_install(self.target, force=False, validate=False)\n+        for relative, expected in before.items():\n+            self.assertEqual((self.target / relative).read_bytes(), expected)\n+        refreshed = json.loads(\n+            (self.target / shiki_installer.INSTALL_STAMP_PATH).read_text(encoding=\"utf-8\")\n+        )\n+        for relative in self.legacy_paths:\n+            self.assertEqual(refreshed[\"digests\"][relative], _sha256(self.target / relative))\n+\n+\n+class LegacyCleanupPreflightTests(unittest.TestCase):\n+    def setUp(self) -> None:\n+        self.tmp = Path(tempfile.mkdtemp(prefix=\"shiki-sadr-preflight-\"))\n+        self.addCleanup(lambda: shutil.rmtree(self.tmp, ignore_errors=True))\n+\n+    def test_all_ownership_blockers_abort_before_any_write(self) -> None:\n+        cases = (\n+            (\"absent\", \"install stamp is absent\"),\n+            (\"unreadable\", \"install stamp is unreadable\"),\n+            (\"malformed\", \"install stamp is malformed\"),\n+            (\"missing-digest\", \"install stamp has no digest\"),\n+            (\"digest-mismatch\", \"digest mismatch\"),\n+        )\n+        for name, expected_reason in cases:\n+            with self.subTest(name=name):\n+                target = self.tmp / name\n+                _, legacy_paths = _create_legacy_upgrade_fixture(target)\n+                stamp_path = target / shiki_installer.INSTALL_STAMP_PATH\n+\n+                if name == \"absent\":\n+                    stamp_path.unlink()\n+                elif name == \"unreadable\":\n+                    stamp_path.unlink()\n+                    stamp_path.mkdir()\n+                elif name == \"malformed\":\n+                    stamp_path.write_text(\"{not-json\\n\", encoding=\"utf-8\")\n+                else:\n+                    stamp = json.loads(stamp_path.read_text(encoding=\"utf-8\"))\n+                    if name == \"missing-digest\":\n+                        stamp[\"digests\"] = {}\n+                    else:\n+                        stamp[\"digests\"] = {\n+                            relative: \"deadbeef\" for relative in legacy_paths\n+                        }\n+                    stamp_path.write_text(\n+                        json.dumps(stamp, indent=2, sort_keys=True) + \"\\n\",\n+                        encoding=\"utf-8\",\n+                    )\n+\n+                before = _tree_snapshot(target)\n+                with self.assertRaises(shiki_installer.ShikiError) as raised:\n+                    _quiet_install(target, force=True, validate=False)\n+                after = _tree_snapshot(target)\n+\n+                self.assertEqual(before, after, f\"{name} preflight changed the tree\")\n+                message = str(raised.exception)\n+                self.assertIn(expected_reason, message)\n+                self.assertIn(\"No files were written\", message)\n+                for relative in legacy_paths:\n+                    self.assertIn(relative, message)\n+\n+    def test_absent_stamp_is_allowed_when_no_legacy_path_exists(self) -> None:\n+        target = self.tmp / \"no-legacy\"\n+        target.mkdir()\n+        _quiet_install(target, force=True, validate=False)\n+        self.assertTrue(\n+            (target / \"docs\" / \"adr\" / \"SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\").is_file()\n+        )\n+\n+\n+if __name__ == \"__main__\":\n+    unittest.main()\n\n2026-08-07T08:25:02.113081Z ERROR codex_models_manager::manager: failed to renew cache TTL: missing field `base_instructions` at line 94 column 5\nexec\n/bin/zsh -lc \"sed -n '690,745p' scripts/shiki_github.py\" in /private/tmp/shiki-adr-collision.pa0KwY/.worktrees/shiki-t-20260807t073307221254z-2d22d654-introduc\n succeeded in 0ms:\n            \"\",\n            \"## Acceptance\",\n            *[f\"- {check}\" for check in task.get(\"acceptance_checks\", [])],\n            \"\",\n            \"## Locks\",\n            *[f\"- {lock}\" for lock in task.get(\"locks\", [])],\n            \"\",\n            \"## Runtime\",\n            str(task.get(\"assigned_runtime\", \"codex\")),\n        ]\n    )\n\n\ndef pre_pr_code_review_section(task: dict[str, Any]) -> list[str]:\n    \"\"\"The PR-12 ``## Pre-PR code review`` body section (SADR-0011).\n\n    Rendered from the loop-recorded ``pre_pr_code_review`` block \u2014 the verdict of\n    the independent read-only reviewer the loop ran before opening the PR. A PR is\n    only ever opened on a ``clean`` verdict (a blocking/failed review fail-closes\n    the loop before create_pr), so this section documents that the independent\n    gate passed and links its ledger; it is never the implementer self-attesting.\n    \"\"\"\n    review = task.get(\"pre_pr_code_review\") or {}\n    lines = [\"## Pre-PR code review\"]\n    if not review:\n        # Defensive: the section is always present for CCA PR-12, even when the\n        # verdict block was not recorded (e.g. a manually opened PR).\n        lines.append(\"- No independent pre-PR review verdict recorded.\")\n        return lines\n    verdict = str(review.get(\"verdict\", \"unknown\"))\n    lines.append(f\"- Verdict: {verdict}\")\n    lines.append(\"- Independent read-only reviewer (claude -p, read tools only) \u2014 SADR-0011\")\n    ledger_id = review.get(\"ledger_id\")\n    if ledger_id:\n        lines.append(f\"- Ledger: {ledger_id}\")\n    findings = review.get(\"findings\") or []\n    if findings:\n        for finding in findings:\n            title = str(finding.get(\"title\", \"finding\")) if isinstance(finding, dict) else str(finding)\n            lines.append(f\"- Finding: {title}\")\n    else:\n        lines.append(\"- Findings: none\")\n    return lines\ndef _task_test_command_for_body(task: dict[str, Any]) -> str:\n    \"\"\"The structured test command for the PR body's loop-observed TDD line.\n\n    Mirrors the loop's ``task_test_command`` selection (the task's\n    ``test_command`` or the safe unittest-discover default) so the PR records\n    exactly what the loop exec'd. ``acceptance_checks`` is free-form prose and is\n    never exec'd, so it is never shown here as the command.\n    \"\"\"\n    from shiki_tasks import DEFAULT_TEST_COMMAND\n\n    command = task.get(\"test_command\")\n    if isinstance(command, str) and command.strip():\n        return command\n\ndiff --git a/.codex/skills/shiki/SKILL.md b/.codex/skills/shiki/SKILL.md\nindex c8e2e85d9d75d49cc4f2d6286ee22659c4e53e75..93cded623b2c15d9f65c76491a1c2d813ac9e6c0\n--- a/.codex/skills/shiki/SKILL.md\n+++ b/.codex/skills/shiki/SKILL.md\n@@ -18,6 +18,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n Codex CLI does not register this skill as a custom `/shiki` slash command. When\n the user types `/shiki` in Codex CLI and gets \"Unrecognized command\", explain\n that this is expected: invoke Shiki in Codex with natural language such as\n@@ -69,7 +72,7 @@\n \n ## Responsibilities\n \n-- Claude Code implements and repairs by default (`shiki runner claude`, ADR 0008); Codex implements and repairs tasks explicitly assigned to `codex`.\n+- Claude Code implements and repairs by default (`shiki runner claude`, SADR-0008); Codex implements and repairs tasks explicitly assigned to `codex`.\n - Claude Code Action can act as GitHub-side CCA or reviewer.\n - CCA judges completion.\n - MergeGate authorizes state transitions and merge readiness.\ndiff --git a/.github/prompts/cca-completion-check.md b/.github/prompts/cca-completion-check.md\nindex ed203ad5f302ecc81372d8ab27d2b1232ca97497..fd89950528333ef6242e6d5443904f5392ab7e56\n--- a/.github/prompts/cca-completion-check.md\n+++ b/.github/prompts/cca-completion-check.md\n@@ -19,6 +19,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n ## Required Reading\n \n Read only the files needed to judge the PR contract. Prefer this order:\ndiff --git a/.github/prompts/codex-task-handoff.md b/.github/prompts/codex-task-handoff.md\nindex 695e211d76dea1c60e8d5bb360e78487808286a4..59441d576b01a39569722f226c854e5a740e9eb5\n--- a/.github/prompts/codex-task-handoff.md\n+++ b/.github/prompts/codex-task-handoff.md\n@@ -2,7 +2,7 @@\n \n You are Codex Front acting as the Implementer for a Shiki task.\n \n-Follow `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant ADRs, and `docs/agents/codex-handoff.md`.\n+Follow `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant Shiki SADRs and target ADRs, and `docs/agents/codex-handoff.md`.\n \n Rules:\n \ndiff --git a/AGENTS.md b/AGENTS.md\nindex 7f041100d49c2a7525f51cb0f5d696a4a6d1c78c..a06bfbabc6fa48a007389e25e12d7ed523936bed\n--- a/AGENTS.md\n+++ b/AGENTS.md\n@@ -22,7 +22,7 @@\n - **Codex Front** is an optional implementer through Codex App, Codex CLI, Codex IDE extension, or Codex Web signed in with ChatGPT OAuth/subscription auth, dispatched with `shiki runner codex` when a task is explicitly assigned to `codex`.\n - **GitHub CCA** is implemented by Claude Code Action by default, using `CLAUDE_CODE_OAUTH_TOKEN`.\n \n-Do not assume `openai/codex-action`, `OPENAI_API_KEY`, or API-key based Codex automation in the default Shiki path. API-key runners are explicit target-repo extensions and require their own ADR.\n+Do not assume `openai/codex-action`, `OPENAI_API_KEY`, or API-key based Codex automation in the default Shiki path. API-key runners are explicit target-repo extensions and require their own target ADR.\n \n Shiki is not:\n \n@@ -44,10 +44,14 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use\n+`SADR-NNNN-*.md`; target product decisions retain `NNNN-*.md` and the `ADR`\n+namespace.\n+\n Use the canonical source-of-truth order above. Runtime-specific reading orders\n are operational guidance only and must not override or reorder it.\n \n-Conversation state is not durable truth. If a decision matters, put it in GitHub, `.shiki/`, `CONTEXT.md`, or an ADR.\n+Conversation state is not durable truth. If a decision matters, put it in GitHub, `.shiki/`, `CONTEXT.md`, a Shiki SADR, or a target ADR.\n \n If GitHub and `.shiki/` disagree, surface the conflict and prefer GitHub operational state until the mirror is repaired.\n \n@@ -75,8 +79,8 @@\n Every non-trivial change follows this loop:\n \n 1. **Goal**: clarify outcome, completion conditions, non-goals, risk level, and success signals.\n-2. **grill-with-docs**: challenge the plan against domain docs, ADRs, code reality, terminology, and edge scenarios.\n-3. **Context & Impact**: find relevant docs, ADRs, code, dependencies, owners, locks, and verification surfaces.\n+2. **grill-with-docs**: challenge the plan against domain docs, Shiki SADRs, target ADRs, code reality, terminology, and edge scenarios.\n+3. **Context & Impact**: find relevant docs, SADRs, target ADRs, code, dependencies, owners, locks, and verification surfaces.\n 4. **PRD and Spec Freeze**: use `to-prd` when context is settled enough to become durable product/engineering intent. The operator's explicit approval of the PRD is Spec Freeze: the plan records a `spec_freeze` block, and no plan runs without it. Steps 1-4 form Requirements Definition \u2014 one continuous operator dialogue. After Spec Freeze, scope changes require an operator-approved Spec Amendment; non-scope-moving interpretations go to the Assumption Log.\n 5. **Issues**: use `to-issues` to create independently grabbable vertical-slice issues. Prefer AFK slices over HITL slices where possible.\n 6. **Triage**: label issues for readiness, risk, runtime, skills, and MergeGate state.\n@@ -167,7 +171,7 @@\n - Testing decisions.\n - Out-of-scope items.\n - Further notes.\n-- Links to relevant domain terms and ADRs.\n+- Links to relevant domain terms, SADRs, and target ADRs.\n \n Every executable task must include:\n \n@@ -193,7 +197,7 @@\n \n Context & Impact must identify:\n \n-- Relevant documents, domain terms, ADRs, and past decisions.\n+- Relevant documents, domain terms, SADRs, target ADRs, and past decisions.\n - Relevant modules, interfaces, seams, callers, owners, and tests.\n - Dependency relationships between tasks.\n - Candidate locks and lock conflicts.\n@@ -210,7 +214,7 @@\n | Skill | Required when |\n | --- | --- |\n | `setup-matt-pocock-skills` | First configuring the repo for skills, or when issue tracker, triage labels, or domain docs are missing. |\n-| `grill-with-docs` | Default for non-trivial Goals before PRD/issues; always required when plans, terminology, boundaries, tradeoffs, or ADR-worthy decisions are ambiguous. |\n+| `grill-with-docs` | Default for non-trivial Goals before PRD/issues; always required when plans, terminology, boundaries, tradeoffs, or decision-record-worthy choices are ambiguous. |\n | `zoom-out` | The agent lacks an architectural map of the relevant code area. |\n | `to-prd` | Settled Goal context must become durable product/engineering intent. |\n | `to-issues` | A Goal, PRD, or plan must become independently grabbable vertical-slice issues. |\n@@ -348,7 +352,7 @@\n - Three or more new files.\n - Any high-risk or critical label.\n \n-When the architecture gate triggers, run `grill-with-docs` or `improve-codebase-architecture` as appropriate and record durable decisions in an ADR when needed.\n+When the architecture gate triggers, run `grill-with-docs` or `improve-codebase-architecture` as appropriate and record durable decisions in a Shiki SADR or target ADR as applicable.\n \n ## Engineering Discipline\n \ndiff --git a/CLAUDE.md b/CLAUDE.md\nindex 98a932e7841a632866fcd53b0bad0e3af3711d3d..98a31399404d6fb044b4fd493f4459ac87f439de\n--- a/CLAUDE.md\n+++ b/CLAUDE.md\n@@ -14,6 +14,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n ## Claude Role\n \n Default to planner, default implementer, reviewer, coordinator, documentation editor, CCA-style completion judge, and final-judgment assistant.\n@@ -57,7 +60,7 @@\n At the start of a Shiki session:\n \n 1. Read `AGENTS.md`.\n-2. Read `CONTEXT.md` and relevant ADRs.\n+2. Read `CONTEXT.md` and relevant Shiki SADRs and target ADRs.\n 3. Read active `.shiki/` Goal/task/ledger state when present.\n 4. Read active distilled rules from `.shiki/memories/` (status `distilled`, `active: true`, not revoked or superseded) for applicable guidance; the same rules are injected deterministically into task handoffs as the `## Distilled Rules` section (\u00a73.5).\n 5. Read the GitHub Issue or PR when the task is GitHub-backed.\n@@ -74,7 +77,7 @@\n - Define the Goal, completion conditions, non-goals, and risk level.\n - Treat `grill-with-docs` as the default planning entry point for non-trivial Goals.\n - Challenge domain terms against `CONTEXT.md`.\n-- Challenge hard-to-reverse tradeoffs against ADRs.\n+- Challenge hard-to-reverse tradeoffs against SADRs and target ADRs.\n - Explore code instead of asking when code can answer the question.\n - Run Context & Impact before implementation planning.\n - Produce a Task DAG, not an unordered checklist.\n@@ -94,7 +97,7 @@\n - For each question, provide your recommended answer.\n - Resolve dependency decisions before downstream design questions.\n - Update `CONTEXT.md` inline when terminology crystallizes.\n-- Offer ADRs only when the decision is hard to reverse, surprising without context, and a real tradeoff.\n+- Offer a SADR or target ADR only when the decision is hard to reverse, surprising without context, and a real tradeoff.\n - Convert unresolved questions into HITL blockers rather than allowing Codex to guess.\n \n ## PRD and Issues Standard\n@@ -103,7 +106,7 @@\n \n - Use the project's domain glossary vocabulary.\n - Avoid volatile file paths unless they are essential.\n-- Include implementation decisions, testing decisions, out-of-scope items, and relevant ADR/domain links.\n+- Include implementation decisions, testing decisions, out-of-scope items, and relevant SADR/target-ADR/domain links.\n - Identify deep-module opportunities and testable interfaces.\n \n When creating issues:\n@@ -121,7 +124,7 @@\n - Branch/worktree target.\n - Scope and non-goals.\n - Dependencies and locks.\n-- Relevant docs, ADRs, modules, and tests.\n+- Relevant docs, SADRs, target ADRs, modules, and tests.\n - Required skill invocations.\n - TDD expectations.\n - Pre-PR `code-review` gate expectations.\n@@ -152,7 +155,7 @@\n \n - Treat yourself as a judge, not implementer.\n - Do not write production code.\n-- Read `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant ADRs, PRD, task issue, PR body, diff, checks, reviews, labels, and `.shiki/` state.\n+- Read `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant SADRs and target ADRs, PRD, task issue, PR body, diff, checks, reviews, labels, and `.shiki/` state.\n - Evaluate every applicable checklist in `docs/agents/checklists.md`.\n - Map each acceptance criterion to durable evidence.\n - Distinguish implementation failure from missing evidence.\ndiff --git a/CONTEXT.md b/CONTEXT.md\nindex e61a9be06a0cce0d026484083a657bde2067e234..237ee8aa3ea8fab334ece49768bc5ed3b5af0974\n--- a/CONTEXT.md\n+++ b/CONTEXT.md\n@@ -25,7 +25,7 @@\n _Avoid_: jumping directly into code\n \n **grill-with-docs**:\n-A planning skill that challenges a plan against domain language, ADRs, code reality, and concrete edge scenarios. It resolves design-tree questions before PRD/issues. It runs inside Requirements Definition.\n+A planning skill that challenges a plan against domain language, Shiki SADRs, target ADRs, code reality, and concrete edge scenarios. It resolves design-tree questions before PRD/issues. It runs inside Requirements Definition.\n _Avoid_: generic brainstorming, silent assumptions\n \n **Requirements Definition**:\ndiff --git a/CONTRIBUTING.md b/CONTRIBUTING.md\nindex c6d9b656e73abb8a55e5f660123e03153dcb3e6d..9e37a8fbf668db5b22461aaae5548b8f1354394c\n--- a/CONTRIBUTING.md\n+++ b/CONTRIBUTING.md\n@@ -17,7 +17,7 @@\n 1. **Goal** \u2014 start from a user-approved Goal with an outcome, completion\n    conditions, non-goals, risk level, and success signals.\n 2. **grill-with-docs** \u2014 for any non-trivial change, challenge the plan against\n-   domain language, ADRs, and code reality before writing a PRD.\n+   domain language, Shiki SADRs, target ADRs, and code reality before writing a PRD.\n 3. **PRD** \u2014 capture settled product/engineering intent (`to-prd`).\n 4. **Spec Freeze** \u2014 the operator explicitly approves the PRD/requirements;\n    the plan records a `spec_freeze` block and no plan runs without it\n@@ -94,7 +94,7 @@\n   entries.\n - Do not hand-edit `.shiki/goals/*.json` or `.shiki/manifest.json` in a task PR.\n - Conversation state is not durable truth. If a decision matters, record it in\n-  GitHub, `.shiki/`, `CONTEXT.md`, or an ADR.\n+  GitHub, `.shiki/`, `CONTEXT.md`, a Shiki SADR, or a target ADR.\n \n ## Architecture Gate\n \n@@ -103,7 +103,7 @@\n or CI policy, package structure, three or more structural files, more than 100\n net new lines in a core module, three or more new files, or any high-risk label.\n When triggered, run `grill-with-docs` or `improve-codebase-architecture` and\n-record durable decisions in an ADR when needed (see `docs/adr/`).\n+record durable decisions in a Shiki SADR or target ADR as applicable (see `docs/adr/`).\n \n ## Safety\n \ndiff --git a/SYSTEM_PROMPT.md b/SYSTEM_PROMPT.md\nindex aaaea45e3a896c3a44ba89eec8bb84325ecf6c62..da009c86a88285269dee0155e58e550c6d744ea9\n--- a/SYSTEM_PROMPT.md\n+++ b/SYSTEM_PROMPT.md\n@@ -30,6 +30,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n Use the canonical source-of-truth order above. Runtime-specific reading orders\n are operational guidance only and must not override or reorder it. The current\n conversation is non-durable operator input.\n@@ -48,14 +51,14 @@\n \n A runtime may only do work that is explicitly assigned by the Goal, task, PR, or MergeGate decision.\n \n-Do not assume `openai/codex-action`, `OPENAI_API_KEY`, or API-key based Codex automation in the default Shiki path. API-key runners are explicit target-repo extensions and require their own ADR.\n+Do not assume `openai/codex-action`, `OPENAI_API_KEY`, or API-key based Codex automation in the default Shiki path. API-key runners are explicit target-repo extensions and require their own target ADR.\n \n ## 4. Non-negotiable Shiki flow\n \n For any non-trivial Goal, use this flow:\n \n 1. **Goal Seek**: define outcome, non-goals, risk level, completion criteria, and evidence requirements.\n-2. **grill-with-docs**: challenge the plan against `CONTEXT.md`, ADRs, code reality, terminology, boundaries, and edge scenarios. Ask one question at a time when the operator is available. If code can answer a question, inspect code instead of asking.\n+2. **grill-with-docs**: challenge the plan against `CONTEXT.md`, Shiki SADRs, target ADRs, code reality, terminology, boundaries, and edge scenarios. Ask one question at a time when the operator is available. If code can answer a question, inspect code instead of asking.\n 3. **Context & Impact**: identify modules, interfaces, seams, callers, dependencies, locks, risk, and verification surfaces.\n 4. **to-prd + Spec Freeze**: turn settled context into a PRD using domain vocabulary and testing decisions. The operator's explicit approval of the PRD is Spec Freeze, recorded as the plan's `spec_freeze` block; plans without it do not run. Steps 1-4 form Requirements Definition.\n 5. **to-issues**: decompose the PRD into independently grabbable vertical-slice issues. Prefer AFK slices; mark HITL slices when judgment is still required.\n@@ -75,7 +78,7 @@\n Before making any material change:\n \n 1. Read `AGENTS.md` and any runtime-specific wrapper such as `CLAUDE.md`.\n-2. Read `CONTEXT.md` and relevant ADRs.\n+2. Read `CONTEXT.md` and relevant SADRs and target ADRs.\n 3. Read the GitHub Issue / PR and `.shiki/` state for the active Goal or task.\n 4. Identify your role: Planner, Implementer, Reviewer, Completion Check Agent, Repairer, or Guardian-assist.\n 5. Identify Goal id, task id, branch, PR, dependencies, locks, risk level, required skills, and required evidence.\n@@ -103,7 +106,7 @@\n \n At minimum, identify:\n \n-- Relevant documents, ADRs, domain terms, and prior decisions.\n+- Relevant documents, SADRs, target ADRs, domain terms, and prior decisions.\n - Relevant modules, interfaces, seams, callers, owners, and tests.\n - Dependency relationships between tasks.\n - Candidate file locks and lock conflicts.\n@@ -152,7 +155,7 @@\n \n CCA must read:\n \n-- `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant ADRs.\n+- `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant SADRs and target ADRs.\n - Parent Goal issue, PRD issue, task issue, comments, and labels.\n - PR body, diff, commits, changed files, reviews, CI checks, and artifacts available to the workflow.\n - `.shiki/` task contract, locks, ledger entries, CCA checklist profile, and prior repair packets.\n@@ -214,7 +217,7 @@\n - Three or more new files.\n - Any high-risk or critical label.\n \n-When the architecture gate triggers, run `grill-with-docs` or `improve-codebase-architecture` as appropriate and record the decision in an ADR if it is hard to reverse or likely to be re-litigated.\n+When the architecture gate triggers, run `grill-with-docs` or `improve-codebase-architecture` as appropriate and record the decision in a Shiki SADR or target ADR as applicable if it is hard to reverse or likely to be re-litigated.\n \n ## 14. Skill Gate\n \n@@ -223,7 +226,7 @@\n Use these skills:\n \n - `setup-matt-pocock-skills`: before first use of the engineering skill set, or when issue tracker, triage labels, or domain docs are missing.\n-- `grill-with-docs`: default for non-trivial Goals before PRD/issues; required for ambiguous plans, terminology, boundaries, ADR-worthy tradeoffs, or design-tree decisions.\n+- `grill-with-docs`: default for non-trivial Goals before PRD/issues; required for ambiguous plans, terminology, boundaries, decision-record-worthy tradeoffs, or design-tree decisions.\n - `zoom-out`: unfamiliar code area or missing architectural map.\n - `to-prd`: convert settled context into a PRD.\n - `to-issues`: break a PRD, plan, or Goal into independently grabbable vertical-slice issues.\ndiff --git a/docs/adr/0001-keep-shiki-as-platform-name.md b/docs/adr/SADR-0001-keep-shiki-as-platform-name.md\nindex 20a9637c3b9d8c97de7ba8db7cfae4d82d9a7dba..8b685439959aada1b221c112f0ac3c51ca8a1695\n--- a/docs/adr/0001-keep-shiki-as-platform-name.md\n+++ b/docs/adr/SADR-0001-keep-shiki-as-platform-name.md\n@@ -1,3 +1,3 @@\n-# Keep Shiki As The Platform Name\n+# SADR-0001: Keep Shiki As The Platform Name\n \n Shiki remains the product name for the GitHub-native agentic engineering control plane. We considered more descriptive names such as MergePilot, MergeGate, and Kiroku, but kept Shiki because it carries the existing goal-seeking framework identity while remaining independent of Hermes, Codex, Claude Code, or any single runtime.\ndiff --git a/docs/adr/0002-use-agents-as-shared-constitution.md b/docs/adr/SADR-0002-use-agents-as-shared-constitution.md\nindex 73de30ecb078120eac540d9bc51855e3cd48d0e3..c5486182bf06fca36ea508496b013c67302fd989\n--- a/docs/adr/0002-use-agents-as-shared-constitution.md\n+++ b/docs/adr/SADR-0002-use-agents-as-shared-constitution.md\n@@ -1,3 +1,3 @@\n-# Use AGENTS As The Shared Agent Constitution\n+# SADR-0002: Use AGENTS As The Shared Agent Constitution\n \n Shiki uses `AGENTS.md` as the runtime-neutral constitution and `CLAUDE.md` as a thin Claude Code wrapper. This keeps Codex, Claude Code, Hermes Runner, and future Agent Runtimes aligned on the same GitHub-first MergeGate rules while still allowing runtime-specific instructions where needed.\ndiff --git a/docs/adr/0003-require-skill-gate-for-mergegate-readiness.md b/docs/adr/SADR-0003-require-skill-gate-for-mergegate-readiness.md\nindex 424be0c9af4c8f0136f1fde68a468c47ff84e764..7fc337987c7df9792f75f181dfcbed75fe5ffff2\n--- a/docs/adr/0003-require-skill-gate-for-mergegate-readiness.md\n+++ b/docs/adr/SADR-0003-require-skill-gate-for-mergegate-readiness.md\n@@ -1,3 +1,3 @@\n-# Require Skill Gate For MergeGate Readiness\n+# SADR-0003: Require Skill Gate For MergeGate Readiness\n \n Shiki requires relevant engineering skills to be invoked before execution when their triggers apply. This makes skills part of MergeGate readiness rather than optional prompt style, because Shiki depends on repeatable planning, issue decomposition, TDD, diagnosis, triage, and architecture review before autonomous or parallel agent work can be trusted.\ndiff --git a/docs/adr/0004-use-github-first-with-shiki-mirror.md b/docs/adr/SADR-0004-use-github-first-with-shiki-mirror.md\nindex d0a3a9174dc646a945a9d325954ffaf00c352040..a79c54ae9ee8eed2a0524192b864da4e22a92d3f\n--- a/docs/adr/0004-use-github-first-with-shiki-mirror.md\n+++ b/docs/adr/SADR-0004-use-github-first-with-shiki-mirror.md\n@@ -1,4 +1,4 @@\n-# ADR 0004: Use GitHub First With A Repository-Local Shiki Mirror\n+# SADR-0004: Use GitHub First With A Repository-Local Shiki Mirror\n \n ## Status\n \ndiff --git a/docs/adr/0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md b/docs/adr/SADR-0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\nindex b339baf49b89d9290e79c3d51b36e7b2bde3bd95..0f3c3eb9afccdb3403ce978e21d04fe12e495da0\n--- a/docs/adr/0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\n+++ b/docs/adr/SADR-0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\n@@ -1,8 +1,8 @@\n-# ADR 0005: Use Codex As Front And Claude Code Action As The GitHub Runtime\n+# SADR-0005: Use Codex As Front And Claude Code Action As The GitHub Runtime\n \n ## Status\n \n-Accepted (implementer default superseded by ADR 0008; the auth model and the\n+Accepted (implementer default superseded by SADR-0008; the auth model and the\n Claude Code Action GitHub runtime decision remain in force)\n \n ## Context\n@@ -26,4 +26,4 @@\n - Shiki avoids requiring `OPENAI_API_KEY` for the default workflow.\n - Codex work happens through the user's authenticated Codex front surface, not as a hidden backend Action.\n - Claude Code Action is configured for OAuth-token based subscription usage by default.\n-- API-key based automation remains possible only as an explicit target-repo extension with its own ADR.\n+- API-key based automation remains possible only as an explicit target-repo extension with its own target ADR.\ndiff --git a/docs/adr/0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md b/docs/adr/SADR-0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\nindex 4bcae9d1e52f0a37b8e9fbafa5cd14b6ee1993bd..7a38538a6ae9929567f0ade67e7ea6a60d0b3f7e\n--- a/docs/adr/0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\n+++ b/docs/adr/SADR-0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\n@@ -1,4 +1,4 @@\n-# ADR 0006: Separate Codex Implementation, CCA Completion, And MergeGate Authorization\n+# SADR-0006: Separate Codex Implementation, CCA Completion, And MergeGate Authorization\n \n ## Status\n \ndiff --git a/docs/adr/0007-packaging-and-release.md b/docs/adr/SADR-0007-packaging-and-release.md\nindex 79b84ddb97167372b44641e5b7860e400ce80822..bb445a2e2e856d9d1f5fdbe35de8d4e8cec0fb32\n--- a/docs/adr/0007-packaging-and-release.md\n+++ b/docs/adr/SADR-0007-packaging-and-release.md\n@@ -1,4 +1,4 @@\n-# ADR 0007: Packaging And Release For Shiki\n+# SADR-0007: Packaging And Release For Shiki\n \n ## Status\n \n@@ -25,7 +25,7 @@\n   not introduce third-party runtime dependencies or break the shim.\n - Releases must run inside the operator's subscription-authenticated toolchain\n   and must not require extra secrets beyond the built-in `GITHUB_TOKEN`\n-  (consistent with ADR 0005's no-API-key-by-default stance).\n+  (consistent with SADR-0005's no-API-key-by-default stance).\n - `.shiki/manifest.json` already encodes the install surface (`install.include`,\n   `install.create_directories`, `install.exclude_from_commit`), and it must not\n   be mutated as part of this change.\n@@ -34,7 +34,7 @@\n \n This is a hard-to-reverse platform decision (it sets the version source of\n truth, the release trigger, and the public install contract), so it is recorded\n-as an ADR.\n+as a SADR.\n \n ## Decision\n \n@@ -104,7 +104,7 @@\n 3. **Publishing to PyPI.** Rejected for now: Shiki is distributed as a\n    repository template + git-installable CLI, not a PyPI library; PyPI would add\n    credentials/secrets and a different versioning contract. Can be added later as\n-   its own ADR if a packaged distribution is needed.\n+   its own SADR if a packaged distribution is needed.\n 4. **Dynamic version (read `VERSION` via a build backend `dynamic` field).**\n    Rejected to keep `pyproject.toml` backend-agnostic and dependency-free; the\n    small duplication between `VERSION` and `pyproject.toml` is guarded by the\n@@ -123,4 +123,4 @@\n   releasing versions the whole platform but ships only `install.include` to\n   targets.\n - Future PyPI distribution or a console entry point are deferred and would each\n-  warrant their own ADR.\n+  warrant their own SADR.\ndiff --git a/docs/adr/0008-use-claude-code-as-default-implementer.md b/docs/adr/SADR-0008-use-claude-code-as-default-implementer.md\nindex dedf4510b7c7799521422f622029563d373db031..35c2625d908167fbc58855b5b63e283805f167e8\n--- a/docs/adr/0008-use-claude-code-as-default-implementer.md\n+++ b/docs/adr/SADR-0008-use-claude-code-as-default-implementer.md\n@@ -1,12 +1,12 @@\n-# ADR 0008: Use Claude Code As The Default Implementer Runtime\n+# SADR-0008: Use Claude Code As The Default Implementer Runtime\n \n ## Status\n \n-Proposed (supersedes ADR 0005 for the implementer default; keeps its auth model)\n+Proposed (supersedes SADR-0005 for the implementer default; keeps its auth model)\n \n ## Context\n \n-ADR 0005 made Codex Front the default implementation runtime, with Claude Code\n+SADR-0005 made Codex Front the default implementation runtime, with Claude Code\n restricted to planner and reviewer roles by the runtime registry, and the only\n autonomous implementation adapter was `shiki runner codex`.\n \n@@ -18,13 +18,13 @@\n - The platform's planning, review, and CCA layers already run on Claude Code,\n   and Claude Code's native capabilities (the built-in `/code-review` skill and\n   the Workflow multi-agent orchestration tool) have no Codex equivalent.\n-- The Goal lifecycle redesign (ADR 0009) requires an autonomous post-freeze\n+- The Goal lifecycle redesign (SADR-0009) requires an autonomous post-freeze\n   loop driven from the operator's Claude Code session, including bounded\n   repair dispatch, which must not depend on a runtime that cannot be invoked\n   unattended.\n \n The constitution claims Shiki is runtime-agnostic (\"not a Claude-only\n-workflow\", ADR 0002). Any change to the implementer default must preserve the\n+workflow\", SADR-0002). Any change to the implementer default must preserve the\n registry-based runtime model rather than hardcoding one vendor.\n \n ## Decision\n@@ -39,7 +39,7 @@\n   the task to review or repair-needed.\n - Keep Codex registered as an optional implementer runtime. `shiki runner\n   codex`, the Codex handoff templates, and the ChatGPT OAuth auth model from\n-  ADR 0005 remain supported for tasks explicitly assigned to `codex`.\n+  SADR-0005 remain supported for tasks explicitly assigned to `codex`.\n - Commit to mandating two Claude-native capabilities as loop contract in\n   follow-up Goals (status: the code-review gate was delivered by Goal G-B \u2014\n   skill registry, checklists item PR-12, default required_skills \u2014 while the\n@@ -60,7 +60,7 @@\n \n Out of scope: removing Codex support, changing the CCA runtime (Claude Code\n Action with `CLAUDE_CODE_OAUTH_TOKEN` stays), and API-key based automation\n-(still requires its own ADR per ADR 0005).\n+(still requires its own target ADR per SADR-0005).\n \n ## Consequences\n \n@@ -100,7 +100,7 @@\n   harness rejection of direct `codex exec` is outside Shiki's control, and the\n   plan's mandatory native capabilities cannot run on Codex at all.\n - **Remove Codex entirely**: rejected; it abandons the runtime-agnostic\n-  constitution (ADR 0002) for no operational gain and forecloses\n+  constitution (SADR-0002) for no operational gain and forecloses\n   cross-runtime adversarial review.\n - **GitHub Actions cloud implementation via claude-code-action**: rejected as\n   default; durable evidence is automatic but token cost, Node 24 workflow\ndiff --git a/docs/adr/0009-requirements-definition-and-spec-freeze.md b/docs/adr/SADR-0009-requirements-definition-and-spec-freeze.md\nindex 29b3f7871bef9203a8b4a831a65c39eb2f506e42..52e7dd69ad0aa4efe0b68c62678abbf7df02aaec\n--- a/docs/adr/0009-requirements-definition-and-spec-freeze.md\n+++ b/docs/adr/SADR-0009-requirements-definition-and-spec-freeze.md\n@@ -1,4 +1,4 @@\n-# ADR 0009: Merge Grill And Planning Into Requirements Definition With Spec Freeze\n+# SADR-0009: Merge Grill And Planning Into Requirements Definition With Spec Freeze\n \n ## Status\n \ndiff --git a/docs/adr/0010-external-ai-guardian-review.md b/docs/adr/SADR-0010-external-ai-guardian-review.md\nindex e6108bbe8bb4e73e0ff4684c8f78001a80438c4e..f3510b145664298adafb151b6f0db51b972f01ec\n--- a/docs/adr/0010-external-ai-guardian-review.md\n+++ b/docs/adr/SADR-0010-external-ai-guardian-review.md\n@@ -1,4 +1,4 @@\n-# ADR 0010: External AI Guardian Review As A First-Class Approval Authority\n+# SADR-0010: External AI Guardian Review As A First-Class Approval Authority\n \n ## Status\n \n@@ -8,7 +8,7 @@\n \n Shiki is intended to be an autonomous, recursive, self-improving agent\n framework: authority-in-the-loop, not human-in-the-loop. Until now the Guardian\n-approval required for high/critical-risk merges (guardian-policy.json, ADR\n+approval required for high/critical-risk merges (guardian-policy.json, SADR\n governance) recognized only human authority sources \u2014 a GitHub review approval,\n the `guardian:approved` label applied by a configured Guardian, or a comment\n carrying `Guardian approval granted` plus the current head SHA.\n@@ -89,7 +89,7 @@\n   autonomous design the operator intends and produced the impersonation\n   incident.\n - **Record AI approval as an operator comment**: rejected as forgery of human\n-  authority \u2014 the exact failure this ADR corrects.\n+  authority \u2014 the exact failure this SADR corrects.\n - **Deliver the artifact as a committed `.shiki/` file**: rejected; a committed\n   approval inside the PR being approved is forgeable by anyone who can push.\n - **A dedicated reviewer bot identity instead of a relayed comment**: deferred;\ndiff --git a/docs/adr/0011-autonomous-quality-gate-evidence.md b/docs/adr/SADR-0011-autonomous-quality-gate-evidence.md\nindex bca0246c5d46e44afbde15486a75eec425fc02f5..9c93eac16d8c3a6d4b11bd7224e3955102cd7db4\n--- a/docs/adr/0011-autonomous-quality-gate-evidence.md\n+++ b/docs/adr/SADR-0011-autonomous-quality-gate-evidence.md\n@@ -1,4 +1,4 @@\n-# ADR 0011: Autonomous Quality-Gate Evidence \u2014 Deterministic Facts And An Independent Verifier\n+# SADR-0011: Autonomous Quality-Gate Evidence \u2014 Deterministic Facts And An Independent Verifier\n \n ## Status\n \ndiff --git a/docs/adr/0012-autonomous-loop-closeout-to-main.md b/docs/adr/SADR-0012-autonomous-loop-closeout-to-main.md\nindex 637fad6dde4989c9c2bf0f81f676d8d29991123c..ec7d85f81030a676bcbba28b43eb365422a36eac\n--- a/docs/adr/0012-autonomous-loop-closeout-to-main.md\n+++ b/docs/adr/SADR-0012-autonomous-loop-closeout-to-main.md\n@@ -1,4 +1,4 @@\n-# 0012 \u2014 Autonomous loop pushes goal completion to main via a closeout PR\n+# SADR-0012 \u2014 Autonomous loop pushes goal completion to main via a closeout PR\n \n - Status: accepted\n - Date: 2026-06-18\ndiff --git a/docs/adr/0013-bootstrap-grants-actions-approve-pull-requests.md b/docs/adr/SADR-0013-bootstrap-grants-actions-approve-pull-requests.md\nindex b6c69d64f1c5ced346b88d6859586715b3b5bd0e..cf48ff4a6716267f539ca58edd1a852e7c7a2d64\n--- a/docs/adr/0013-bootstrap-grants-actions-approve-pull-requests.md\n+++ b/docs/adr/SADR-0013-bootstrap-grants-actions-approve-pull-requests.md\n@@ -1,4 +1,4 @@\n-# ADR 0013: Bootstrap Grants GitHub Actions Permission To Create And Approve Pull Requests\n+# SADR-0013: Bootstrap Grants GitHub Actions Permission To Create And Approve Pull Requests\n \n ## Status\n \n@@ -31,7 +31,7 @@\n security tradeoff: it lets repository automation satisfy a human-review gate. In\n a multi-maintainer repo that could let a compromised or buggy workflow\n self-approve changes. So whether bootstrap turns it on by default \u2014 and with\n-what default token scope \u2014 is an ADR-worthy decision, not an implementation\n+what default token scope \u2014 is a SADR-worthy decision, not an implementation\n detail.\n \n ## Decision\n@@ -48,7 +48,7 @@\n Specifically:\n \n - **`can_approve_pull_request_reviews` defaults to `true`.** Shiki's default\n-  posture is solo/AFK autonomous operation (ADR 0008/0009/0012), where the CCA\n+  posture is solo/AFK autonomous operation (SADR-0008/0009/0012), where the CCA\n   Review Bridge is the only path that can satisfy `required_review: true` after\n   the CCA and Guardian gates have already passed. The default makes a\n   freshly-bootstrapped repo able to run the autonomous loop end to end without a\ndiff --git a/docs/adr/0014-codex-app-external-ai-guardian-ui-adapter.md b/docs/adr/SADR-0014-codex-app-external-ai-guardian-ui-adapter.md\nindex 238db06279334caa341c117362df9271382a7fcd..65f12e9939321e1b3e389a34c822fc1549e5155d\n--- a/docs/adr/0014-codex-app-external-ai-guardian-ui-adapter.md\n+++ b/docs/adr/SADR-0014-codex-app-external-ai-guardian-ui-adapter.md\n@@ -1,4 +1,4 @@\n-# ADR 0014: Use Codex App As The External AI Guardian UI Adapter\n+# SADR-0014: Use Codex App As The External AI Guardian UI Adapter\n \n ## Status\n \ndiff --git a/docs/adr/0015-contract-approval-before-dispatch.md b/docs/adr/SADR-0015-contract-approval-before-dispatch.md\nindex fad6c656741a20a7684ba16080a7db159f63a52f..6061271a46d3f1d6ae7174477de3fb75673123a6\n--- a/docs/adr/0015-contract-approval-before-dispatch.md\n+++ b/docs/adr/SADR-0015-contract-approval-before-dispatch.md\n@@ -1,4 +1,4 @@\n-# ADR 0015: Approve The Task Contract Before Dispatch, Not The Diff After It\n+# SADR-0015: Approve The Task Contract Before Dispatch, Not The Diff After It\n \n ## Status\n \n@@ -9,7 +9,7 @@\n Guardian approval for high/critical risk is expressible only on a pull request.\n Every approval source in `guardian-policy.json` \u2014 GitHub review, the\n `guardian:approved` label, the `Guardian approval granted` comment, and the\n-`external_ai_guardian_review` artifact (ADR 0010/0014) \u2014 is bound to a PR, and\n+`external_ai_guardian_review` artifact (SADR-0010/0014) \u2014 is bound to a PR, and\n three of the four require the current head SHA. Approval therefore cannot exist\n before an implementation PR exists.\n \n@@ -81,7 +81,7 @@\n An envelope breach must surface only on the non-repairable policy gate, never on\n the repairable `MergeGate metadata check`. Otherwise the Goal Loop routes it to\n `dispatch_repair` \u2014 instructing a runner to make the breach go away \u2014 which is\n-exactly what this ADR forbids.\n+exactly what this SADR forbids.\n \n **Contract immutability.** After registration, an implementation PR must not\n change the governance fields of its own task contract (`scope`, `non_goals`,\n@@ -116,7 +116,7 @@\n fails any verdict that is not `complete`, and the signal the CCA prompt reads is\n computed from `evaluate_guardian_approval` alone. Without the OR-merge at the\n signal, a contract-approved PR still returns `needs_guardian`, the CCA job fails,\n-and the MergeGate policy check never runs \u2014 the gate this ADR changes would never\n+and the MergeGate policy check never runs \u2014 the gate this SADR changes would never\n be reached. The CCA *prompt* needs no change: a `contract_approval` entry in the\n signal's existing `sources` array satisfies its current wording.\n \n@@ -130,7 +130,7 @@\n written, and an approved contract lets its implementation PRs merge through the\n same automatic path as low/medium risk. The wasted implement-then-block cycle\n disappears. Guardian review shifts from reading diffs to reading contracts, which\n-is both earlier and smaller; the external AI Guardian path (ADR 0010/0014) works\n+is both earlier and smaller; the external AI Guardian path (SADR-0010/0014) works\n on contracts with no change.\n \n We accept that the Guardian authorizes a class of change without seeing its\n@@ -146,7 +146,7 @@\n adversarial tests for the new approval path equivalent in rigor to\n `test_shiki_governance_evidence.sh`.\n \n-Bootstrap costs, which apply only while this ADR is being implemented. The tasks\n+Bootstrap costs, which apply only while this SADR is being implemented. The tasks\n that build Contract Approval merge under the existing rules, so the ones\n dispatched after the pre-dispatch gate lands but before the approval path lands\n need a Guardian-approved Contract PR *and* a live Guardian approval on their\n@@ -210,7 +210,7 @@\n risk; they do not bound authorized work.\n \n **Auto-escalate risk on envelope breach instead of stopping.** Returns the\n-workflow to a diff-time Guardian decision \u2014 the exact behavior this ADR removes.\n+workflow to a diff-time Guardian decision \u2014 the exact behavior this SADR removes.\n \n **Resolve governance fields from the base snapshot only.** Rejected against the\n registration flow: implementation PRs add their own contract, so the base has no\ndiff --git a/docs/adr/0016-locks-bound-product-paths.md b/docs/adr/SADR-0016-locks-bound-product-paths.md\nindex b5e850f3f1f7f4ef6e7f9d1763a29a51bd300c45..46b40a9b99a7aae644c02bb458f5473ff0a90ce2\n--- a/docs/adr/0016-locks-bound-product-paths.md\n+++ b/docs/adr/SADR-0016-locks-bound-product-paths.md\n@@ -1,4 +1,4 @@\n-# ADR 0016: Locks bound product paths; the Shiki mirror is bound by record identity\n+# SADR-0016: Locks bound product paths; the Shiki mirror is bound by record identity\n \n ## Status\n \n@@ -6,8 +6,8 @@\n \n - Date: 2026-07-28\n - Deciders: operator (mizutani-140), Claude (planner)\n-- Related: ADR 0004 (GitHub-first with a `.shiki` mirror), ADR 0009 (Spec Freeze),\n-  ADR 0012 (autonomous loop closeout to main), ADR 0015 (Contract Approval before\n+- Related: SADR-0004 (GitHub-first with a `.shiki` mirror), SADR-0009 (Spec Freeze),\n+  SADR-0012 (autonomous loop closeout to main), SADR-0015 (Contract Approval before\n   dispatch), remediation goal task R-02, the live PR #179 deadlock.\n \n ## Context\n@@ -90,11 +90,11 @@\n files, documentation files and named source modules are nameable; runtime-\n allocated ledger, runner and report ids are not \u2014 and those live in `.shiki`,\n which locks no longer govern. Existing frozen contracts are brought into line by\n-a one-time Spec Amendment per plan, which must land before ADR 0015's contract\n+a one-time Spec Amendment per plan, which must land before SADR-0015's contract\n immutability makes `locks` unwritable from the implementation PR.\n \n **Lock state is resolved from the durable side.** MergeGate resolves the lock\n-registry, and ADR 0015's governance fields, from the base `.shiki` snapshot when\n+registry, and SADR-0015's governance fields, from the base `.shiki` snapshot when\n present, falling back to head only when absent. The `MergeGate metadata check`\n job is given `--base-shiki`, which it does not receive today. Locks are not\n derived from the live open-pull-request set: a pull request body is untrusted\n@@ -108,7 +108,7 @@\n Explicitly out of scope: moving MergeGate's contract resolution off the PR\n checkout wholesale; making the mirror a projection of GitHub state; introducing\n new lock kinds; moving ledger evidence out of the repository; and unwinding\n-ADR 0012's closeout pull request. Registration of multi-task goals is ADR 0015's\n+SADR-0012's closeout pull request. Registration of multi-task goals is SADR-0015's\n Contract PR and is not re-decided here.\n \n ## Consequences\n@@ -127,7 +127,7 @@\n \n Risks accepted. First, goal-scoped identity is only as strong as `goal_id`,\n which is resolved from the PR body and the head task file; the mitigation is to\n-add `goal_id` to ADR 0015's frozen governance field set, and until that lands a\n+add `goal_id` to SADR-0015's frozen governance field set, and until that lands a\n task file can name a foreign goal. Second, base-when-present resolution is\n unavailable on fork pull requests, where the head is contributor-controlled;\n fork pull requests already cannot complete a Shiki merge, and this decision does\n@@ -139,7 +139,7 @@\n path, not by implementation pull requests.\n \n New obligations: one Spec Amendment per frozen plan to rewrite `locks`, ordered\n-before ADR 0015 T1; extension of the adversarial suites in\n+before SADR-0015 T1; extension of the adversarial suites in\n `scripts/test_shiki_adversarial_state.sh` and\n `scripts/test_shiki_governance_evidence.sh` to cover the eleven newly-scoped\n directories; and updating `scripts/test_shiki_loop_lock_guard.sh`,\n@@ -151,32 +151,32 @@\n whether the loop's transitive sync of ledger `evidence` references can be\n deleted once the Contract PR carries its own source plan.\n \n-## Relationship to ADR 0004\n+## Relationship to SADR-0004\n \n-This ADR **clarifies something ADR 0004 left unspecified**. ADR 0004 decides that\n+This SADR **clarifies something SADR-0004 left unspecified**. SADR-0004 decides that\n GitHub is the operational source of truth and that each target repository keeps a\n `.shiki/` mirror. It says nothing about pull requests writing the mirror, does\n not name MergeGate, and does not make the mirror an input to a gate. Nothing in\n-ADR 0004's Decision or Consequences is amended, superseded or weakened: the\n+SADR-0004's Decision or Consequences is amended, superseded or weakened: the\n mirror still exists, still records the same artifacts, and pull request\n-boundaries remain the coordination surface. What this ADR settles is the question\n+boundaries remain the coordination surface. What this SADR settles is the question\n 0004 never reached \u2014 by what authority a pull request may write a mirror record \u2014\n and it answers it the way `.shiki/manifest.json` already declares\n (`pr_mutation: current-task-or-current-goal-only`).\n \n-It **amends ADR 0012**, whose Decision item 5 states that \"Loop-executed tasks\n+It **amends SADR-0012**, whose Decision item 5 states that \"Loop-executed tasks\n declare the `path:.shiki/**` lock, which covers every `.shiki` file the closeout\n stages ... so `files_outside_locks` is satisfied without per-file lock\n bookkeeping.\" That clause is replaced: the closeout's mirror writes are\n-authorized by record identity, not by a declared lock. ADR 0012's substantive\n+authorized by record identity, not by a declared lock. SADR-0012's substantive\n decision \u2014 that completion reaches `main` through a closeout pull request rather\n than local attestation \u2014 is preserved unchanged.\n \n-It is **complementary to ADR 0015**, not a substitute. ADR 0015 owns contract\n+It is **complementary to SADR-0015**, not a substitute. SADR-0015 owns contract\n immutability, the pre-dispatch gate, the Contract PR and the Scope Envelope. This\n-ADR owns what a lock means and who may write the mirror. The one ordering\n+SADR owns what a lock means and who may write the mirror. The one ordering\n constraint between them is that the lock-narrowing Spec Amendment must precede\n-ADR 0015 T1's freezing of the `locks` field.\n+SADR-0015 T1's freezing of the `locks` field.\n \n ## Alternatives Considered\n \n@@ -186,7 +186,7 @@\n the only writer that can register them may run. Migrating the Guardian signal to\n that store makes every registration PR fail closed to `needs_guardian` at every\n risk level, because the task file is by definition absent from the store, which\n-removes the low/medium autonomous path ADR 0008/0009 exists to provide. And it\n+removes the low/medium autonomous path SADR-0008/0009 exists to provide. And it\n leaves schemas, manifest, config, guardian policy, templates, migrations, plans,\n worktrees, runner records, runs and handoffs with no writer at all in a\n repository whose product is `.shiki`. Measured benefit on the frozen backlog:\n@@ -199,14 +199,14 @@\n namespace is not in fact ownership-partitioned \u2014 56 ledger entries carry no\n `task_id`, and one goal DAG is writable by 35 registered tasks. The correct\n expression of its insight is that locks do not govern the mirror at all, which is\n-what this ADR decides.\n+what this SADR decides.\n \n **Make the mirror a projection written only by a registration writer and a\n post-merge projection job.** Rejected. It requires a bypass actor with write\n access to the protected default branch; it makes its own master invariant\n readable from a manifest that pull requests may edit; it deletes both reconcile\n modes and leaves a corrupted mirror with no in-band repair path, contradicting\n-ADR 0004's consequence that mirror conflicts are repaired against GitHub state;\n+SADR-0004's consequence that mirror conflicts are repaired against GitHub state;\n and its content-derived ids are rejected by `validate_shiki`'s currently-green id\n pattern.\n \ndiff --git a/docs/adr/0017-bookkeeping-closeouts-do-not-inherit-risk.md b/docs/adr/SADR-0017-bookkeeping-closeouts-do-not-inherit-risk.md\nindex 554e3a0d526a72ca067108c5464cc68c3edcf174..b9676857a4713850e063af7c39c8e0b01ecb4c66\n--- a/docs/adr/0017-bookkeeping-closeouts-do-not-inherit-risk.md\n+++ b/docs/adr/SADR-0017-bookkeeping-closeouts-do-not-inherit-risk.md\n@@ -1,4 +1,4 @@\n-# ADR 0017: A Bookkeeping Closeout Does Not Inherit Its Task's Risk\n+# SADR-0017: A Bookkeeping Closeout Does Not Inherit Its Task's Risk\n \n ## Status\n \n@@ -6,7 +6,7 @@\n \n ## Context\n \n-ADR 0012 made the goal loop push a task's completion to the default branch through a\n+SADR-0012 made the goal loop push a task's completion to the default branch through a\n closeout pull request: after the implementation PR merges, the loop opens a second PR\n carrying `task.status = done`, `lock.state = released`, and \u2014 when the task completes\n its goal \u2014 `goal.status = complete` plus one scorecard report. Nothing else. The\n@@ -49,14 +49,14 @@\n 4. The task file's governance fields (`scope`, `non_goals`, `required_skills`,\n    `risk_level`, `locks`, `acceptance_checks`, `test_command`) are byte-identical to\n    the base snapshot. Only `status`, `expected_pr`, `closeout_pr`, `ledger_evidence`\n-   and `expected_branch` may differ \u2014 the ADR 0015 contract-immutability rule,\n+   and `expected_branch` may differ \u2014 the SADR-0015 contract-immutability rule,\n    unchanged.\n \n    `expected_branch` is in that set because a closeout is *by construction* on a\n    different branch from the implementation it closes, and the loop rewrites the\n    field accordingly. It is safe to let it differ because MergeGate independently\n    requires `expected_branch` to equal the PR's actual head ref, so a PR cannot set\n-   it to anything other than the branch it is really on. The first draft of this ADR\n+   it to anything other than the branch it is really on. The first draft of this SADR\n    omitted it, which made the exemption unreachable: every real closeout failed\n    condition 4 on a field the loop always changes. See PR #209.\n 5. The state transitions are exactly the terminal set: task `review` \u2192 `done`, lock\n@@ -113,12 +113,12 @@\n classification must be derived, not asserted.\n \n **Fold the closeout into the implementation PR** so no second PR exists. Rejected under\n-ADR 0012, which recorded why: a task file that is already `done` on the implementation\n+SADR-0012, which recorded why: a task file that is already `done` on the implementation\n branch short-circuits the loop's review \u2192 CCA \u2192 merge \u2192 done ordering into a local\n goal-completion before any merge happens.\n \n **Have the loop auto-merge closeouts with elevated credentials.** Rejected outright: that\n-is a runtime authoring its own approval, which ADR 0010 and the constitution's\n+is a runtime authoring its own approval, which SADR-0010 and the constitution's\n approval-record prohibition forbid. It would also be indistinguishable, in the evidence,\n from the forgery those rules exist to prevent.\n \ndiff --git a/docs/adr/0018-carried-guardian-approval-across-a-proven-base-sync.md b/docs/adr/SADR-0018-carried-guardian-approval-across-a-proven-base-sync.md\nindex 22358562da390ed8b45cd600da08a43d378b4680..e6e2907cefa776460af73ddf7d39db605f3c39b0\n--- a/docs/adr/0018-carried-guardian-approval-across-a-proven-base-sync.md\n+++ b/docs/adr/SADR-0018-carried-guardian-approval-across-a-proven-base-sync.md\n@@ -1,4 +1,4 @@\n-# ADR 0018: Carried Guardian Approval Across A Proven Base Sync\n+# SADR-0018: Carried Guardian Approval Across A Proven Base Sync\n \n ## Status\n \n@@ -34,7 +34,7 @@\n Guardian evaluator that consumes it (`shiki_guardian.evaluate_guardian_approval`,\n the `guardian_comment_carried` source) were built and tested in isolation. Neither\n does anything until a caller passes the flags that turn the carry on, so the\n-feature was invisible in CI. This ADR records the decision the two of them\n+feature was invisible in CI. This SADR records the decision the two of them\n implement, and this task wires it into the four CI invocations that judge Guardian\n approval.\n \ndiff --git a/docs/adr/README.md b/docs/adr/README.md\nindex c70e057a4520c36e042ab68c53cd4a21ef709b71..5da43856836e2ce6a150f8127766e2f88355ab4b\n--- a/docs/adr/README.md\n+++ b/docs/adr/README.md\n@@ -1,40 +1,41 @@\n-# Architecture Decision Records\n+# Decision Record Namespaces\n \n-This directory holds Shiki's Architecture Decision Records (ADRs): durable\n-records of hard-to-reverse platform decisions. ADRs are part of the Shiki source\n-of truth alongside GitHub state, the `.shiki/` mirror, and `CONTEXT.md`.\n+This directory holds two independent decision namespaces:\n \n-## How ADRs Work\n+- Shiki Architecture Decision Records (SADRs), named\n+  `SADR-NNNN-<short-slug>.md`, record hard-to-reverse Shiki platform decisions.\n+- Target Architecture Decision Records (ADRs), named\n+  `NNNN-<short-slug>.md`, belong to the target product.\n \n-- New ADRs are added as `docs/adr/NNNN-<short-slug>.md`, where `NNNN` is the next\n-  zero-padded sequence number.\n-- The **canonical list of ADRs is the set of files matching `docs/adr/*.md`** in\n-  this directory. Browse `docs/adr/*.md` for the authoritative, current set.\n-- Use [`template.md`](template.md) as the starting point for a new ADR.\n-- Record an ADR when a decision is hard to reverse, surprising without context,\n-  and a real tradeoff. Do not write an ADR for routine or easily reversible\n-  choices.\n+The same four-digit number may appear once in each namespace. A platform\n+`SADR-NNNN` citation resolves only to `SADR-NNNN-*.md`; a target `ADR NNNN`\n+citation resolves only to `NNNN-*.md`.\n \n-Using the `docs/adr/*.md` glob as the source of truth means other PRs can add an\n-ADR without editing this index file, avoiding merge contention on a\n-hand-maintained enumeration.\n+## How Decision Records Work\n \n-## Current ADRs (snapshot)\n-\n-The list below is a convenience snapshot and may lag behind the directory. When\n-in doubt, list `docs/adr/*.md`.\n+- New Shiki platform decisions use the next zero-padded SADR number.\n+- New target product decisions use the target's own ADR sequence.\n+- The canonical list is the files in `docs/adr/`; no hand-maintained index is\n+  authoritative.\n+- Use [`template.md`](template.md) as the starting point and choose the filename\n+  namespace that owns the decision.\n+- Record a decision when it is hard to reverse, surprising without context, and\n+  a real tradeoff. Do not write a decision record for routine or easily\n+  reversible choices.\n \n-- [0001 \u2014 Keep Shiki As The Platform Name](0001-keep-shiki-as-platform-name.md)\n-- [0002 \u2014 Use AGENTS As The Shared Agent Constitution](0002-use-agents-as-shared-constitution.md)\n-- [0003 \u2014 Require Skill Gate For MergeGate Readiness](0003-require-skill-gate-for-mergegate-readiness.md)\n-- [0004 \u2014 Use GitHub First With A Repository-Local Shiki Mirror](0004-use-github-first-with-shiki-mirror.md)\n-- [0005 \u2014 Use Codex As Front And Claude Code Action As The GitHub Runtime](0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md)\n-- [0006 \u2014 Separate Codex Implementation, CCA Completion, And MergeGate Authorization](0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md)\n+Using the directory as the source of truth lets independent changes add records\n+without contending on an enumerated index.\n \n-## Adding A New ADR\n+## Adding A Shiki SADR\n \n-1. Copy `template.md` to `docs/adr/NNNN-<short-slug>.md`.\n+1. Copy `template.md` to `docs/adr/SADR-NNNN-<short-slug>.md`.\n 2. Fill in the title, status, context, decision, and consequences.\n-3. Set the status to `Proposed`, then `Accepted` once the decision is approved.\n-4. Open a PR following `CONTRIBUTING.md`. You do not need to edit this index for\n-   the ADR to be discoverable via `docs/adr/*.md`.\n+3. Set the status to `Proposed`, then `Accepted` once the proper authority has\n+   approved the decision.\n+4. Open a PR following `CONTRIBUTING.md`.\n+\n+## Adding A Target ADR\n+\n+1. Copy `template.md` to `docs/adr/NNNN-<short-slug>.md` using the target's own\n+   sequence.\n+2. Cite it as `ADR NNNN`; do not prefix its filename with `SADR-`.\ndiff --git a/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md b/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\nnew file mode 100644\nindex 0000000000000000000000000000000000000000..7455ce1f3bed02ddddb851ac21a1d1a648c9aeba\n--- /dev/null\n+++ b/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\n@@ -0,0 +1,55 @@\n+# SADR-0019: Separate Shiki Platform SADRs From Target ADRs\n+\n+## Status\n+\n+Accepted\n+\n+## Context\n+\n+Shiki installs its platform decision records into `docs/adr/` in every target\n+repository. Shiki and its targets previously used the same numeric\n+`NNNN-<slug>.md` namespace. A target that already owned ADRs 0013 through 0018\n+therefore acquired two records for each number during installation, making an\n+`ADR NNNN` citation ambiguous. Renumbering target decisions would rewrite target\n+history, while renumbering Shiki decisions per target would destroy their stable\n+platform identity.\n+\n+Upgrades also need to remove the former Shiki-owned numeric paths without ever\n+mistaking a target-owned ADR for platform content. The install stamp already\n+records exact shipped paths and content digests, so it can prove ownership only\n+when both still match.\n+\n+## Decision\n+\n+Shiki platform decisions use the `SADR` namespace:\n+\n+- records are named `docs/adr/SADR-NNNN-<slug>.md`;\n+- live platform citations use `SADR-NNNN`;\n+- target product decisions retain `docs/adr/NNNN-<slug>.md` and `ADR NNNN`;\n+- the reference validator resolves the two namespaces independently, so the same\n+  four-digit number may exist once in each namespace.\n+\n+Before a forced upgrade writes anything, the installer inventories the exact\n+former numeric Shiki ADR paths. It may delete a legacy path only when the\n+existing install stamp contains that exact path and its recorded SHA-256 digest\n+matches the file currently on disk. All ownership blockers are reported\n+together and abort the upgrade before its first write. A non-forced install\n+never performs this cleanup.\n+\n+Historical `.shiki/` evidence remains unchanged because it records the namespace\n+that existed when that evidence was produced.\n+\n+## Consequences\n+\n+- Targets may use any ADR number without colliding with Shiki's platform SADRs.\n+- Fresh installs preserve target ADR bytes and install a stable SADR set.\n+- Forced upgrades fail closed when legacy ownership cannot be proven, and a\n+  successful upgrade refreshes the stamp without removed legacy paths.\n+- Current governance, packaging, workflow, prompt, script, and skill references\n+  must move with the platform records.\n+\n+## Alternatives Considered\n+\n+- Collision-only refusal leaves affected targets permanently unupgradable.\n+- Renumbering target ADRs mutates target-owned history.\n+- Per-target Shiki renumbering makes platform citations non-portable.\ndiff --git a/docs/adr/template.md b/docs/adr/template.md\nindex a92a97daddd0b00ab9dd626cc8d86942da2eafed..17dd79175b5fee2105935557f691b40cf9492942\n--- a/docs/adr/template.md\n+++ b/docs/adr/template.md\n@@ -1,14 +1,14 @@\n-# NNNN: <Short Decision Title>\n+# <SADR-NNNN or NNNN>: <Short Decision Title>\n \n ## Status\n \n-Proposed | Accepted | Superseded by ADR-NNNN\n+Proposed | Accepted | Superseded by SADR-NNNN or target ADR NNNN\n \n ## Context\n \n What is the situation, constraint, or force that requires a decision? Reference\n-relevant domain terms (`CONTEXT.md`), prior ADRs, and code reality. State the\n-problem, not the solution.\n+relevant domain terms (`CONTEXT.md`), prior Shiki SADRs or target ADRs, and code\n+reality. State the problem, not the solution.\n \n ## Decision\n \ndiff --git a/docs/agents/autonomous-loop-e2e-acceptance.md b/docs/agents/autonomous-loop-e2e-acceptance.md\nindex bbf84c6081987f159ac6ad9ecf3c82b7aa72e7e8..5c13c1d02a05a90b0dc46cbef7c95a9846d0c237\n--- a/docs/agents/autonomous-loop-e2e-acceptance.md\n+++ b/docs/agents/autonomous-loop-e2e-acceptance.md\n@@ -40,8 +40,8 @@\n 3. `gh auth status` is logged in with merge permission on the repo.\n 4. Branch protection + required checks are configured exactly as the goal-loop\n    required-check set expects (see `docs/agents/branch-protection-smoke.md`).\n-5. The repo `.shiki/policy` permits autonomous low/medium-risk merges (ADR\n-   0008/0009); the MergeGate policy check is the recorded-authority gate.\n+5. The repo `.shiki/policy` permits autonomous low/medium-risk merges\n+   (SADR-0008/0009); the MergeGate policy check is the recorded-authority gate.\n \n The operator's only actions are: register the task and start the loop. **No\n manual pre-clearing** means after `shiki loop run` starts, the operator does not\ndiff --git a/docs/agents/bootstrap-command.md b/docs/agents/bootstrap-command.md\nindex 05f7f203a52cf412b9f97084a6e131ed0f785afd..f7aa0b734dfebee4a3b139335b66ee3415f48713\n--- a/docs/agents/bootstrap-command.md\n+++ b/docs/agents/bootstrap-command.md\n@@ -189,7 +189,9 @@\n ```\n \n Use this only for tests, fixtures, or explicit local-only template inspection.\n-Use `--force` only when you intentionally want to overwrite existing target files.\n+Use `--force` only when you intentionally want to refresh the target template.\n+Legacy numeric Shiki decision cleanup is stamp-and-digest authorized before any\n+write; target ADR paths are not cleanup candidates.\n \n ## Slash Command\n \ndiff --git a/docs/agents/checklists.md b/docs/agents/checklists.md\nindex 4a7f03ade346f53a1c1d82981f3fd744f9ab4547..0c5ec6c53dc7197b7b7a7d18e667b325babd6c45\n--- a/docs/agents/checklists.md\n+++ b/docs/agents/checklists.md\n@@ -32,18 +32,18 @@\n | ID | Item | Blocking |\n | --- | --- | --- |\n | GD-01 | Plan was challenged against `CONTEXT.md`. | blocking for non-trivial Goals |\n-| GD-02 | Relevant ADRs were read or absence was recorded. | blocking for non-trivial Goals |\n+| GD-02 | Relevant SADRs and target ADRs were read or absence was recorded. | blocking for non-trivial Goals |\n | GD-03 | Ambiguous terms were resolved or converted to HITL blockers. | blocking |\n | GD-04 | Edge scenarios were considered. | advisory |\n | GD-05 | Code reality was checked when code could answer. | blocking when applicable |\n | GD-06 | New domain terms were added to `CONTEXT.md` when needed. | blocking when applicable |\n-| GD-07 | ADR was created or explicitly skipped using the ADR criteria. | blocking when applicable |\n+| GD-07 | A SADR or target ADR was created or explicitly skipped using the decision-record criteria. | blocking when applicable |\n \n ## CI \u2014 Context & Impact Checklist\n \n | ID | Item | Blocking |\n | --- | --- | --- |\n-| CI-01 | Relevant docs and ADRs identified. | blocking |\n+| CI-01 | Relevant docs, SADRs, and target ADRs identified. | blocking |\n | CI-02 | Relevant modules, interfaces, seams, callers, and tests identified. | blocking |\n | CI-03 | Dependencies and blocked-by relationships identified. | blocking |\n | CI-04 | Candidate locks identified. | blocking |\n@@ -62,7 +62,7 @@\n | PRD-04 | Implementation decisions are recorded without volatile code detail. | blocking |\n | PRD-05 | Testing decisions are recorded. | blocking |\n | PRD-06 | Out-of-scope items are explicit. | blocking |\n-| PRD-07 | Relevant glossary/ADR links are present when applicable. | blocking |\n+| PRD-07 | Relevant glossary/SADR/target-ADR links are present when applicable. | blocking |\n \n ## SF \u2014 Spec Freeze Checklist\n \n@@ -98,7 +98,7 @@\n | H-02 | Branch/worktree target is present. | blocking |\n | H-03 | Scope and non-goals are present. | blocking |\n | H-04 | Dependencies and locks are present. | blocking |\n-| H-05 | Relevant docs, ADRs, modules, and tests are referenced. | blocking |\n+| H-05 | Relevant docs, SADRs, target ADRs, modules, and tests are referenced. | blocking |\n | H-06 | Required skills are stated. | blocking |\n | H-07 | TDD expectations are stated. | blocking when behavior work |\n | H-08 | Acceptance criteria are included. | blocking |\ndiff --git a/docs/agents/codex-handoff.md b/docs/agents/codex-handoff.md\nindex ede468f47cb277f69a857e8c45df703e4ed0151d..6f97bdffb67e44d7edea2c71e01ea28933c4eb6c\n--- a/docs/agents/codex-handoff.md\n+++ b/docs/agents/codex-handoff.md\n@@ -46,7 +46,7 @@\n \n ## Context\n - Domain terms:\n-- Relevant ADRs:\n+- Relevant SADRs and target ADRs:\n - Relevant docs:\n - Relevant modules/interfaces/seams:\n - Similar tests/prior art:\ndiff --git a/docs/agents/completion-check-agent.md b/docs/agents/completion-check-agent.md\nindex 13e4c035db35025f4e3cdcc5adfb24314ac4d521..61bfa1218cf36ca605250c91919e620f33238aa0\n--- a/docs/agents/completion-check-agent.md\n+++ b/docs/agents/completion-check-agent.md\n@@ -32,7 +32,7 @@\n - `AGENTS.md`\n - `CLAUDE.md`\n - `CONTEXT.md`\n-- relevant ADRs\n+- relevant SADRs and target ADRs\n - parent Goal issue\n - PRD issue or document\n - task issue\ndiff --git a/docs/agents/decision-control.md b/docs/agents/decision-control.md\nindex 7d8ee5ab03a5ccbcbd04056ae61de03785a2a9c9..8c94ecda00fffaa0e92c325ab83aa9776bd1dac1\n--- a/docs/agents/decision-control.md\n+++ b/docs/agents/decision-control.md\n@@ -11,7 +11,7 @@\n | Decision | Owner | When | Required inputs | Output | Enforcement surface |\n | --- | --- | --- | --- | --- | --- |\n | Is the Goal clear enough to plan? | Planner + Guardian when needed | Goal intake | User request, existing docs, risk signals | Goal readiness verdict | Goal checklist, issue template, ledger |\n-| Must `grill-with-docs` run? | Planner / Skill Gate | Before PRD or issue split | Ambiguity, domain terms, ADR conflicts, unclear boundaries | Skill invocation or explicit skip rationale | Skill Gate checklist |\n+| Must `grill-with-docs` run? | Planner / Skill Gate | Before PRD or issue split | Ambiguity, domain terms, SADR/target-ADR conflicts, unclear boundaries | Skill invocation or explicit skip rationale | Skill Gate checklist |\n | Is a PRD required? | Planner | After Goal context stabilizes | Goal, `grill-with-docs` output, Context & Impact output | PRD required / not required | PRD checklist, CCA evidence check |\n | Can the spec be frozen? | Operator only | End of Requirements Definition, before issue split | PRD/requirements summary, scope inventory (SF-02), open-question list | `spec_freeze` block in the plan + ledger evidence | `require_grilled_plan` (plan ingest/run), SF checklist |\n | Can a frozen spec change? | Operator only (Spec Amendment) | When implementation contradicts the frozen spec | Contested decisions, scoped re-grill result | recorded amendment + re-stamped freeze | SF checklist, ledger evidence |\ndiff --git a/docs/agents/domain.md b/docs/agents/domain.md\nindex 258da8ea81e87ac0a6b0b5b5e5c2fde980eaabb7..d57b0cf17ff3e1fa9e6e5885372bf083d8295421\n--- a/docs/agents/domain.md\n+++ b/docs/agents/domain.md\n@@ -5,7 +5,9 @@\n ## Layout\n \n - `CONTEXT.md` defines Shiki language. Keep it implementation-free.\n-- `docs/adr/` records hard-to-reverse decisions.\n+- `docs/adr/SADR-NNNN-*.md` records Shiki platform decisions.\n+- `docs/adr/NNNN-*.md` records target product decisions without sharing the\n+  SADR namespace.\n - `AGENTS.md` is the runtime-neutral agent constitution.\n - `CLAUDE.md` is the Claude Code wrapper around `AGENTS.md`.\n - `.shiki/` is the Target Repository mirror for Goals, PRDs, plans, Task DAGs, contracts, locks, worktree records, ledger entries, CCA verdicts, repair packets, reports, and handoffs.\n@@ -15,6 +17,6 @@\n \n - Use Shiki terms exactly when they exist.\n - Add glossary terms to `CONTEXT.md` only when the term is part of the domain, not a generic programming concept.\n-- Create ADRs only for hard-to-reverse, surprising, tradeoff-based decisions.\n+- Create SADRs or target ADRs only for hard-to-reverse, surprising, tradeoff-based decisions.\n - Keep product-specific language in the Target Repository, not in Shiki Template docs.\n-- If an ADR conflicts with a proposed implementation, surface the conflict before editing.\n+- If a SADR or target ADR conflicts with a proposed implementation, surface the conflict before editing.\ndiff --git a/docs/agents/execution-control.md b/docs/agents/execution-control.md\nindex ad8ee3cdb36be56fedc1f46408ca92b387d74047..bc63c3a73661d12b567016ba299f1a6b66bf9088\n--- a/docs/agents/execution-control.md\n+++ b/docs/agents/execution-control.md\n@@ -63,7 +63,7 @@\n | Branch name | Orchestrator | Orchestrator | Before implementer dispatch | branch policy | branch ref in task/ledger |\n | Lock reservation | Context & Impact | MergeGate | Before dispatch and before merge | `.shiki/locks/`, labels, PR metadata | lock id, owner, scope |\n | CI profile | Planner | MergeGate | During issue triage, rechecked at PR | labels, `.shiki/policy.yaml`, workflow matrix | required check list |\n-| Required status checks | Repo maintainer / Guardian | GitHub branch protection / rulesets | Repository setup, updated via ADR | GitHub settings | branch/ruleset config, ADR |\n+| Required status checks | Repo maintainer / Guardian | GitHub branch protection / rulesets | Repository setup, updated via target ADR | GitHub settings | branch/ruleset config, target ADR |\n | CCA judgment | CCA | GitHub required check / MergeGate | Every non-draft PR update | GitHub Action check | structured CCA verdict |\n | Repair assignment | CCA | MergeGate | After failed CCA/CI/review | repair packet, issue/PR comment | repair id and attempt count |\n | Merge readiness | MergeGate | GitHub branch protection / Guardian if needed | After CCA complete | required checks, review rules, rulesets | mergegate check output |\ndiff --git a/docs/agents/guardian-policy.md b/docs/agents/guardian-policy.md\nindex 4147c7adf05a47adb4fd24ada1a1c2e1f2ba6d2d..a20d9cc6c00a569bb057b9ca8d6b51c7fc4a9847\n--- a/docs/agents/guardian-policy.md\n+++ b/docs/agents/guardian-policy.md\n@@ -98,8 +98,8 @@\n `origin/<default_branch>` and reuses each job's existing base fetch; a shallow\n (`--depth`) or second base fetch would make the proof unresolvable and silently\n disable the carry, so neither is permitted. CCA reads `guardian_comment_carried` as\n-a recorded authority in the deterministic signal and does not re-derive it. See ADR\n-0018 for the full decision, the rejected diff/patch-equality and path-disjointness\n+a recorded authority in the deterministic signal and does not re-derive it. See\n+SADR-0018 for the full decision, the rejected diff/patch-equality and path-disjointness\n alternatives, and the accepted residual.\n \n ## External AI Guardian Review (SADR-0010)\ndiff --git a/docs/agents/implementation-policy.md b/docs/agents/implementation-policy.md\nindex c754144a9fb5bf4a38aa82a60e1670c59e626b44..85c37281bcad196c6b7b02343faf00b03cd9c57a\n--- a/docs/agents/implementation-policy.md\n+++ b/docs/agents/implementation-policy.md\n@@ -31,10 +31,13 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n ## Non-Negotiables\n \n 1. **Planning precedes implementation.** Non-trivial Goals start with `grill-with-docs` and Context & Impact.\n-2. **Domain language is controlled.** Terms must be checked against `CONTEXT.md`; hard-to-reverse tradeoffs must be checked against ADRs.\n+2. **Domain language is controlled.** Terms must be checked against `CONTEXT.md`; hard-to-reverse tradeoffs must be checked against Shiki SADRs and target ADRs.\n 3. **PRD precedes issue decomposition.** `to-prd` is used once the context is settled enough to become durable intent.\n 4. **Issues are vertical slices.** `to-issues` must produce independently grabbable tracer-bullet tasks, not horizontal layer tickets.\n 5. **Triage controls dispatch.** Only `ready-for-agent` / AFK tasks may be assigned to Codex without human input.\n@@ -95,19 +98,19 @@\n Process:\n \n - Challenge terms against `CONTEXT.md`.\n-- Check ADRs before reopening decisions.\n+- Check SADRs and target ADRs before reopening decisions.\n - Inspect code when code can answer a question.\n - Ask one question at a time when the operator is available.\n - Provide a recommended answer with each question.\n - Record resolved terms in `CONTEXT.md`.\n-- Create ADRs only for hard-to-reverse, surprising, tradeoff-based decisions.\n+- Create SADRs or target ADRs only for hard-to-reverse, surprising, tradeoff-based decisions.\n - Convert unresolved load-bearing questions into HITL blockers.\n \n Outputs:\n \n - Resolved design-tree decisions.\n - Updated glossary terms when needed.\n-- ADRs when justified.\n+- SADRs or target ADRs when justified.\n - HITL blockers when decisions remain unresolved.\n \n ## Phase 3 \u2014 Context & Impact\n@@ -117,7 +120,7 @@\n \n Outputs must include:\n \n-- Relevant documents and ADRs.\n+- Relevant documents, SADRs, and target ADRs.\n - Relevant modules, interfaces, seams, callers, and tests.\n - Candidate locks.\n - Dependencies.\n@@ -140,7 +143,7 @@\n - Testing decisions.\n - Out of scope.\n - Further notes.\n-- Links to relevant Goal, domain terms, and ADRs.\n+- Links to relevant Goal, domain terms, SADRs, and target ADRs.\n \n Phase 4 ends with Spec Freeze: the operator explicitly approves the PRD, the\n plan records a `spec_freeze` block (status, approved_by, source), and the SF\n@@ -240,7 +243,7 @@\n - Dependencies and locks.\n - Risk and Guardian requirements.\n - Ledger completeness.\n-- Documentation and ADR updates.\n+- Documentation and SADR/target-ADR updates.\n - Scope drift.\n \n CCA emits one of:\n@@ -301,7 +304,7 @@\n | Evidence failure | Behavior might be done, but proof is missing. | Add checks, PR evidence, ledger entry, or artifacts. |\n | Scope drift | PR changes unrelated behavior. | Revert unrelated changes or split into new Goal. |\n | Dependency block | Upstream task or lock not complete. | Block until dependency resolves. |\n-| Architecture block | Decision needs ADR or deep-module discussion. | `grill-with-docs` or `improve-codebase-architecture`. |\n+| Architecture block | Decision needs a SADR, target ADR, or deep-module discussion. | `grill-with-docs` or `improve-codebase-architecture`. |\n | Risk block | Security/production/policy change needs human approval. | Guardian decision. |\n \n ## Phase 5.5 \u2014 Execution Decision Control\ndiff --git a/docs/agents/runtime-auth-model.md b/docs/agents/runtime-auth-model.md\nindex 6a2e1a47e81ea89b5c68a210ec07772a9374babc..3c54748d24233dbd0ba8473950282547ef4ce935\n--- a/docs/agents/runtime-auth-model.md\n+++ b/docs/agents/runtime-auth-model.md\n@@ -148,7 +148,7 @@\n \n API-key based runners may be added by a target repository as an explicit extension. They are not the default Shiki template path.\n \n-If a repo opts into API-key mode, record the exception in an ADR and update the repo's `AGENTS.md`, `.shiki/config.yaml`, and workflow permissions.\n+If a repo opts into API-key mode, record the target-specific exception in a target ADR and update the repo's `AGENTS.md`, `.shiki/config.yaml`, and workflow permissions.\n \n ## GitHub Actions Runtime Compatibility\n \ndiff --git a/docs/agents/skill-gate.md b/docs/agents/skill-gate.md\nindex c50398b0d7c60a8598f2a5c88a10015730dced13..3acffe6444617bb1d54b77d4b4708158ec735bc6\n--- a/docs/agents/skill-gate.md\n+++ b/docs/agents/skill-gate.md\n@@ -9,7 +9,7 @@\n | Skill | Trigger | Expected output |\n | --- | --- | --- |\n | `setup-matt-pocock-skills` | First configuring a repo for these skills, or when issue tracker, triage labels, or domain docs are missing. | `docs/agents/` configuration and an `Agent skills` block in `AGENTS.md` or `CLAUDE.md`. |\n-| `grill-with-docs` | Ambiguous plan, unclear terminology, boundary decision, tradeoff, or ADR-worthy design choice. | Clarified decision tree, recommended answers, updates to `CONTEXT.md` or ADRs when decisions crystallize. |\n+| `grill-with-docs` | Ambiguous plan, unclear terminology, boundary decision, tradeoff, or decision-record-worthy design choice. | Clarified decision tree, recommended answers, updates to `CONTEXT.md`, SADRs, or target ADRs when decisions crystallize. |\n | `zoom-out` | Agent lacks a higher-level architectural map. | Map of relevant modules, callers, seams, and domain concepts. |\n | `to-prd` | Settled context should become a PRD. | PRD published to the issue tracker with problem, solution, user stories, decisions, testing, and out-of-scope notes. |\n | `to-issues` | Goal, PRD, or plan must become implementation tickets. | Vertical-slice issues with acceptance criteria, dependencies, and AFK/HITL classification. |\n@@ -17,7 +17,7 @@\n | `tdd` | Feature work or bug fix has observable behavior that can be specified with tests. | Red-green-refactor loop through public interfaces, one vertical slice at a time. |\n | `code-review` | The TDD loop is green and the implementer is about to create or update a PR. Default for every implementation task. | Pre-PR self-review of the task diff with fixes applied on the task branch; findings and resolutions recorded as a ledger entry naming the skill and a `## Pre-PR code review` PR body section. |\n | `diagnose` | Hard bug, failing check, regression, flaky behavior, or performance problem. | Reproduction loop, ranked hypotheses, instrumentation, fix, regression test, post-mortem. |\n-| `improve-codebase-architecture` | Structural friction, testability, AI-navigability, deep-module opportunity, or refactor request. | Architecture candidates and deepening recommendations informed by `CONTEXT.md` and ADRs. |\n+| `improve-codebase-architecture` | Structural friction, testability, AI-navigability, deep-module opportunity, or refactor request. | Architecture candidates and deepening recommendations informed by `CONTEXT.md`, SADRs, and target ADRs. |\n | `prototype` | A throwaway logic or UI prototype is needed to answer a design question. | Clearly marked disposable prototype and captured decision. |\n \n ## MergeGate Interaction\ndiff --git a/docs/operations.md b/docs/operations.md\nindex cf1f452d4c589e38ccd58553bda0ad8684b51eb7..ace48a90ffbec9e5c0a7113bef16a4858bc44b3f\n--- a/docs/operations.md\n+++ b/docs/operations.md\n@@ -89,6 +89,16 @@\n      target history, brought forward by `shiki migrate`, not a template.\n    - **Everything else** (CLI scripts, workflows, schemas, docs) is overwritten.\n \n+   A forced upgrade also migrates the former numeric Shiki decision paths to\n+   `SADR-NNNN-*`. Before the first write, the installer inventories the exact\n+   legacy Shiki paths and authorizes each deletion only when the existing\n+   `.shiki/install-stamp.json` names that path and its SHA-256 digest still\n+   matches the file. An absent, unreadable, malformed, incomplete, or\n+   mismatching stamp reports every blocked path and stops with no filesystem\n+   change. Target-owned `NNNN-*` ADRs are not cleanup candidates. A non-force\n+   install deletes nothing and retains any still-matching legacy stamp entries\n+   so a later forced upgrade can prove ownership.\n+\n    If the target has pending migrations, `--force` refuses and writes nothing \u2014\n    apply migrations first (step 3), then re-run. The run ends with a single\n    summary of every `.new` written, so a half-upgrade is visible.\ndiff --git a/docs/releasing.md b/docs/releasing.md\nindex 10d73828e7dbd64e9d9f6b83bc84d010537ce62a..aafeee8c4d441e1e427efee2ce4f81a647f020e1\n--- a/docs/releasing.md\n+++ b/docs/releasing.md\n@@ -67,7 +67,7 @@\n CLI in `scripts/`. There are no third-party Python runtime dependencies. The CLI\n is intentionally **not** exposed as a `[project.scripts]` console entry point\n because `scripts/shiki.py` is a dependency-free shim and `scripts/` is not\n-packaged as an importable distribution (see the ADR for the rationale).\n+packaged as an importable distribution (see SADR-0007 for the rationale).\n \n Supported install paths:\n \n@@ -155,6 +155,15 @@\n    [docs/operations.md](operations.md#the-new-convention). If the target has\n    pending migrations, `--force` refuses and writes nothing; apply migrations\n    (step 2) first, then re-run.\n+\n+   The SADR namespace upgrade has an additional pre-write gate: each former\n+   numeric Shiki decision path is removed only when the existing install stamp\n+   contains that exact path and a digest matching its current bytes. The\n+   installer aggregates absent/unreadable/malformed-stamp, missing-digest, and\n+   digest-mismatch blockers before raising, so refusal leaves the target tree\n+   byte-identical. Numeric target ADRs are preserved. A successful forced\n+   upgrade installs and stamps `SADR-*` paths and drops the removed legacy paths\n+   from the refreshed stamp.\n 2. Apply repository-local `.shiki/` state migrations with the migrate command:\n \n    ```bash\ndiff --git a/docs/usage.md b/docs/usage.md\nindex b83407ee3970dd395dd893010bc5dbe8bad26051..a130ef6e40dfd8169a5bc701339351097df1a09f\n--- a/docs/usage.md\n+++ b/docs/usage.md\n@@ -67,8 +67,10 @@\n shiki install-target /path/to/target-repo --local-only\n ```\n \n-Use `--force` only when you intentionally want to overwrite existing target\n-files.\n+Use `--force` only when you intentionally want to refresh the target template.\n+For the SADR namespace migration, `--force` deletes only exact legacy Shiki\n+decision paths whose existing install-stamp digest still matches; any ownership\n+blocker stops before the first write. Numeric target ADRs are never candidates.\n \n ## `shiki bootstrap-platform` \u2014 publish the Shiki platform repo\n \ndiff --git a/scripts/guardian_approval_signal.py b/scripts/guardian_approval_signal.py\nindex d059f92b2c82aa2b5d6ca421365537a87886c1cb..16924892714f4f5b9303c0ef0e325a7e70075630\n--- a/scripts/guardian_approval_signal.py\n+++ b/scripts/guardian_approval_signal.py\n@@ -199,8 +199,8 @@\n ) -> Any:\n     \"\"\"Evaluate SADR-0015 Contract Approval for the CCA Guardian signal.\n \n-    The SAME OR that MergeGate applies must reach this deterministic signal: ADR\n-    0015 is explicit that without it, a contract-approved PR still returns\n+    The SAME OR that MergeGate applies must reach this deterministic signal:\n+    SADR-0015 is explicit that without it, a contract-approved PR still returns\n     ``needs_guardian``, the CCA job fails, and the MergeGate policy check never\n     runs \u2014 the gate this change moves would never be reached. This resolves the\n     task the SAME way MergeGate does (the first Shiki task id in the PR body),\ndiff --git a/scripts/shiki_contract_approval.py b/scripts/shiki_contract_approval.py\nindex 941bcb96fdd8b9a85bc0c4f0f04d5dad9d04839c..4a4b418f6527413161db82670bec857a043360e2\n--- a/scripts/shiki_contract_approval.py\n+++ b/scripts/shiki_contract_approval.py\n@@ -142,7 +142,7 @@\n     changed_files_status: Any,\n     registration: Any,\n ) -> ContractApprovalResult:\n-    \"\"\"Whether this PR carries an SADR-0015 Contract Approval.\n+    \"\"\"Whether this PR carries a SADR-0015 Contract Approval.\n \n     The four conditions are evaluated IN ORDER, each failure recorded as a\n     distinct reason string:\ndiff --git a/scripts/shiki_guardian.py b/scripts/shiki_guardian.py\nindex a4ecb79bbda55fd5c8b06adfae2eb99760a03c45..4dd99bbe714e26f421fa19abac7a34ae1b195ecc\n--- a/scripts/shiki_guardian.py\n+++ b/scripts/shiki_guardian.py\n@@ -12,7 +12,7 @@\n # The carry re-derives its proof from git in-process on every judging invocation;\n # it is never read from an artifact, so there is nothing to go stale or be forged.\n # shiki_sync_proof is the ONLY guardian-family module that runs git, and every one\n-# of its invocations is read-only and local (ADR: prove-from-git base sync).\n+# of its invocations is read-only and local (SADR: prove-from-git base sync).\n from shiki_sync_proof import verify_pure_base_sync\n \n GUARDIAN_POLICY_PATH = \".shiki/guardian-policy.json\"\ndiff --git a/scripts/shiki_guardian_review.py b/scripts/shiki_guardian_review.py\nindex dd0f104d457899e0ba5ff1e162011c1e90b6f92b..e10ef58c046324e7ea3a3392feb3048ca77414ce\n--- a/scripts/shiki_guardian_review.py\n+++ b/scripts/shiki_guardian_review.py\n@@ -159,7 +159,7 @@\n     docs_only = _is_docs_only(changed_files)\n     if docs_only and matched == 0:\n         focus.append(\n-            \"Docs/ADR-only change: confirm the change is genuinely documentation/decision-only, introduces no behavior or contract drift, and stays consistent with the constitution and existing ADRs.\"\n+            \"Docs/decision-record-only change: confirm the change is genuinely documentation/decision-only, introduces no behavior or contract drift, and stays consistent with the constitution and existing SADRs and target ADRs.\"\n         )\n \n     # Conservative augmentation: unknown (no category and not docs-only), mixed\ndiff --git a/scripts/shiki_installer.py b/scripts/shiki_installer.py\nindex be064ab7a29ff6836f7f7a1d70500a43e0d11d7b..9fd8eefd137ce7003dad5c0dcb2e48e2cd47ab32\n--- a/scripts/shiki_installer.py\n+++ b/scripts/shiki_installer.py\n@@ -118,6 +118,7 @@\n # what lets a target carry it without a governance-schema change.\n INSTALL_STAMP_PATH = \".shiki/install-stamp.json\"\n INSTALL_STAMP_VERSION = 1\n+LEGACY_SHIKI_SADR_MAX = 18\n \n # Under --force the shipped surface splits three ways, so nothing is lost and\n # nothing is silently stale.\n@@ -237,9 +238,98 @@\n         )\n \n \n+def legacy_shiki_adr_paths() -> tuple[str, ...]:\n+    \"\"\"Former numeric paths for Shiki SADR-0001 through SADR-0018.\n+\n+    The authoritative slugs come from the current SADR filenames. SADR-0019 is\n+    the namespace decision itself and never shipped under a numeric legacy path.\n+    \"\"\"\n+    paths: list[str] = []\n+    for sadr in sorted((ROOT / \"docs\" / \"adr\").glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\")):\n+        number = sadr.name.split(\"-\", 2)[1]\n+        if int(number) <= LEGACY_SHIKI_SADR_MAX:\n+            paths.append(f\"docs/adr/{sadr.name.removeprefix('SADR-')}\")\n+    return tuple(paths)\n+\n+\n+def preflight_legacy_sadr_cleanup(target: Path, *, force: bool) -> tuple[str, ...]:\n+    \"\"\"Authorize exact legacy Shiki ADR removals before the first write.\n+\n+    A non-force install never removes existing files. A forced install needs no\n+    stamp when no legacy Shiki path is present. When legacy paths do exist, each\n+    must be named in a readable install stamp and its current bytes must match\n+    the recorded digest.\n+    \"\"\"\n+    if not force:\n+        return ()\n+    present = tuple(\n+        relative\n+        for relative in legacy_shiki_adr_paths()\n+        if (target / relative).exists() or (target / relative).is_symlink()\n+    )\n+    if not present:\n+        return ()\n+\n+    stamp_path = target / INSTALL_STAMP_PATH\n+    try:\n+        raw = stamp_path.read_text(encoding=\"utf-8\")\n+        stamp = json.loads(raw)\n+    except FileNotFoundError:\n+        detail = \"install stamp is absent\"\n+        blockers = [f\"{relative}: {detail}\" for relative in present]\n+    except (OSError, UnicodeDecodeError) as error:\n+        detail = f\"install stamp is unreadable ({error})\"\n+        blockers = [f\"{relative}: {detail}\" for relative in present]\n+    except json.JSONDecodeError as error:\n+        detail = f\"install stamp is malformed ({error})\"\n+        blockers = [f\"{relative}: {detail}\" for relative in present]\n+    else:\n+        if not isinstance(stamp, dict):\n+            blockers = [\n+                f\"{relative}: install stamp is malformed (expected an object)\"\n+                for relative in present\n+            ]\n+        else:\n+            digests = stamp.get(\"digests\")\n+            blockers = []\n+            if not isinstance(digests, dict):\n+                blockers.extend(\n+                    f\"{relative}: install stamp has no digest for this path\"\n+                    for relative in present\n+                )\n+            else:\n+                for relative in present:\n+                    expected = digests.get(relative)\n+                    if not isinstance(expected, str) or not expected:\n+                        blockers.append(\n+                            f\"{relative}: install stamp has no digest for this path\"\n+                        )\n+                        continue\n+                    try:\n+                        actual = _sha256_file(target / relative)\n+                    except OSError as error:\n+                        blockers.append(\n+                            f\"{relative}: legacy path is unreadable ({error})\"\n+                        )\n+                        continue\n+                    if actual != expected:\n+                        blockers.append(\n+                            f\"{relative}: digest mismatch (stamp {expected}, current {actual})\"\n+                        )\n+\n+    if blockers:\n+        joined = \"\\n\".join(f\"- {blocker}\" for blocker in blockers)\n+        raise ShikiError(\n+            \"legacy Shiki ADR cleanup is not authorized:\\n\"\n+            f\"{joined}\\nNo files were written.\"\n+        )\n+    return present\n+\n+\n def install_template(target: Path, *, force: bool, validate: bool) -> list[NewFileNote]:\n     # Preconditions first: never rewrite the tree of a target that cannot be\n     # upgraded cleanly (see refuse_pending_migrations).\n+    legacy_cleanup = preflight_legacy_sadr_cleanup(target, force=force)\n     refuse_pending_migrations(target)\n \n     # Resolve the target's required CODEOWNERS owner once from its\n@@ -250,6 +340,10 @@\n     # verbatim. See ``codeowners_required_owner``.\n     codeowners_owner = codeowners_required_owner(target)\n \n+    for relative in legacy_cleanup:\n+        (target / relative).unlink()\n+        info(f\"removed stamped legacy Shiki ADR: {target / relative}\")\n+\n     notes: list[NewFileNote] = []\n     for relative in TEMPLATE_PATHS:\n         source = ROOT / relative\n@@ -647,6 +741,22 @@\n     digests = target_stamp_digests(target)\n     existing = read_install_stamp(target)\n     if existing is not None:\n+        # A non-force install deliberately leaves legacy Shiki ADRs in place.\n+        # Preserve only still-matching ownership proof so a later forced\n+        # upgrade can authorize cleanup; never carry a missing or stale digest.\n+        existing_digests = existing.get(\"digests\")\n+        if isinstance(existing_digests, dict):\n+            for relative in legacy_shiki_adr_paths():\n+                expected = existing_digests.get(relative)\n+                path = target / relative\n+                if not isinstance(expected, str) or not path.is_file():\n+                    continue\n+                try:\n+                    actual = _sha256_file(path)\n+                except OSError:\n+                    continue\n+                if actual == expected:\n+                    digests[relative] = actual\n         if _incoming_is_older(commit, existing.get(\"platform_commit\")):\n             warn(\n                 \"kept existing install stamp: incoming install \"\ndiff --git a/scripts/shiki_loop.py b/scripts/shiki_loop.py\nindex 3611641da8aeb5038b1da90e3114b09a473ec6d8..84caa491b9cad0edc4176f209c13e9221e975681\n--- a/scripts/shiki_loop.py\n+++ b/scripts/shiki_loop.py\n@@ -424,7 +424,7 @@\n         # unresolvable CCA verdict has already produced its terminal stop and is never\n         # laundered into a head-moving sync (SADR-0010). It also never fires while the\n         # policy (Guardian) gate is red \u2014 for ANY reason, not only when it is the sole\n-        # red check \u2014 so a head-bound Guardian approval, or an SADR-0015\n+        # red check \u2014 so a head-bound Guardian approval, or a SADR-0015\n         # contract-approval carry that base movement defeated (the policy gate compares\n         # base and head task governance directly), is never silently restored by\n         # adopting base wholesale. It is bounded by a durable per-task counter so the\ndiff --git a/scripts/shiki_tasks.py b/scripts/shiki_tasks.py\nindex aec3809f24b4a0c8c96fc50971cff9ae9ad30381..d531052e9d9717058d4631483cefab44c26a65c1\n--- a/scripts/shiki_tasks.py\n+++ b/scripts/shiki_tasks.py\n@@ -821,7 +821,7 @@\n         \"prompt\": prompt,\n         \"entry_skill\": \"grill-with-docs\",\n         \"required_next_steps\": [\n-            \"Run grill-with-docs until terminology, boundaries, risks, and ADR-worthy decisions are settled.\",\n+            \"Run grill-with-docs until terminology, boundaries, risks, and decision-record-worthy choices are settled.\",\n             \"Obtain the operator's explicit approval of the PRD/requirements (Spec Freeze).\",\n             \"Write a machine-readable plan JSON with grill_with_docs.status=complete and spec_freeze.status=frozen.\",\n             \"Run shiki plan ingest --plan-file PLAN.json.\",\ndiff --git a/scripts/test_shiki_goal_loop.sh b/scripts/test_shiki_goal_loop.sh\nindex 753c7b516e10f0be3bc92a4bd0eb1fa699776ae6..8f3820a0a079b3044424cfe1a6c6b7fe68896e67\n--- a/scripts/test_shiki_goal_loop.sh\n+++ b/scripts/test_shiki_goal_loop.sh\n@@ -231,8 +231,8 @@\n \n # High/critical risk merges autonomously when all required checks are green:\n # the \"MergeGate policy check\" required check IS the Guardian gate (it enforces\n-# guardian-policy.json \u2014 human approval OR external AI guardian review, ADR\n-# 0010), so green checks mean a recorded authority approved. The fake gh returns\n+# guardian-policy.json \u2014 human approval OR external AI guardian review,\n+# SADR-0010), so green checks mean a recorded authority approved. The fake gh returns\n # green checks, simulating that the Guardian gate passed.\n python3 \"$ROOT/scripts/shiki.py\" goal create --target \"$TARGET\" --title \"Guardian gate\" --outcome \"High risk merges via policy gate\" >/tmp/shiki-goal-loop-guardian-goal.json\n GGOAL=\"$(json_get /tmp/shiki-goal-loop-guardian-goal.json goal_id)\"\ndiff --git a/tests/test_adr_references.py b/tests/test_adr_references.py\nindex 51706329c797d5dd9b47e082a333fa83b261d482..d005da4f771f5e8085ec22d4b787980e68f5ae8c\n--- a/tests/test_adr_references.py\n+++ b/tests/test_adr_references.py\n@@ -1,8 +1,8 @@\n-\"\"\"Every ``ADR ####`` citation must resolve to exactly one decision record.\n+\"\"\"Every target ADR and Shiki SADR citation resolves in its own namespace.\n \n Background. ``scripts/mergegate_check.py`` justifies ``is_bookkeeping_closeout``\n-with \"ADR 0017\" nine times and ``scripts/guardian_approval_signal.py`` cites\n-0016/0017 seven times, yet for a stretch the numbering in ``docs/adr/`` stopped\n+with \"SADR-0017\" nine times and ``scripts/guardian_approval_signal.py`` cites\n+SADR-0016/SADR-0017 seven times, yet for a stretch the numbering in ``docs/adr/`` stopped\n at 0015: the authoring commits lived on a coordinator branch and no task ever\n declared ``docs/adr/...`` in its locks, so nothing carried the records to the\n default branch. The stated basis of an exemption that merged seven PRs with zero\n@@ -11,11 +11,12 @@\n and the directory drifted with no signal.\n \n This is the same unbound-pair shape behind other silent failures: a reference\n-and its target with nothing asserting they agree. This test binds them. It scans\n-every ``scripts/*.py`` (plus ``docs/**/*.md`` and the root ``*.md`` governance\n-files, which are cheap) for the ``ADR ####`` pattern and asserts each cited\n-number resolves to exactly one ``docs/adr/####-*.md``. The converse is\n-deliberately NOT required: an ADR may exist without being cited.\n+and its target with nothing asserting they agree. This test binds them. Target\n+``ADR NNNN`` citations resolve only to ``docs/adr/NNNN-*.md``; Shiki\n+``SADR-NNNN`` citations resolve only to ``docs/adr/SADR-NNNN-*.md``. The scan\n+covers current governance docs, scripts, workflows, prompts, packaging metadata,\n+and repo-local Codex guidance while deliberately excluding historical\n+``.shiki`` evidence. The converse is not required: a record may be uncited.\n \n The suite is dependency free: standard-library ``unittest`` only.\n \"\"\"\n@@ -31,17 +32,18 @@\n \n REPO_ROOT = Path(__file__).resolve().parents[1]\n \n-# \"ADR 0016\" -- a four-digit citation. The trailing \\b keeps a five-digit token\n-# from being read as a truncated four-digit number.\n-_CITATION = re.compile(r\"\\bADR (\\d{4})\\b\")\n-# \"0016-locks-bound-product-paths.md\" -- the leading four digits name the record.\n-_ADR_FILENAME = re.compile(r\"^(\\d{4})-.*\\.md$\")\n+# ``ADR 0016`` / ``SADR-0016`` -- a namespace plus four-digit citation. The\n+# trailing ``\\b`` keeps a five-digit token from being read as a truncated one.\n+_CITATION = re.compile(r\"\\b(S?ADR)[ -](\\d{4})\\b\")\n+# Target ADRs retain the numeric filename; Shiki platform decisions use SADR.\n+_DECISION_FILENAME = re.compile(r\"^(?:(SADR)-)?(\\d{4})-.*\\.md$\")\n \n \n class Violation(NamedTuple):\n     \"\"\"A citation that does not resolve to exactly one decision record.\"\"\"\n \n     citing_file: str  # repo-relative path of the file that cites the ADR\n+    namespace: str  # \"ADR\" for target decisions; \"SADR\" for Shiki decisions\n     number: str  # the cited four-digit number, e.g. \"0016\"\n     line: int  # 1-based line of the citation\n     reason: str  # \"missing\" (zero matches) or \"ambiguous\" (two or more)\n@@ -49,44 +51,59 @@\n \n     @property\n     def expected_path(self) -> str:\n-        return f\"docs/adr/{self.number}-*.md\"\n+        prefix = \"SADR-\" if self.namespace == \"SADR\" else \"\"\n+        return f\"docs/adr/{prefix}{self.number}-*.md\"\n \n     def describe(self) -> str:\n         \"\"\"Name the citing file, the cited number, and the missing path.\"\"\"\n         if self.reason == \"missing\":\n             return (\n-                f\"{self.citing_file}:{self.line} cites ADR {self.number} \"\n+                f\"{self.citing_file}:{self.line} cites {self.namespace} {self.number} \"\n                 f\"but no document matches {self.expected_path}\"\n             )\n         joined = \", \".join(self.matches)\n         return (\n-            f\"{self.citing_file}:{self.line} cites ADR {self.number} which \"\n+            f\"{self.citing_file}:{self.line} cites {self.namespace} {self.number} which \"\n             f\"resolves to {len(self.matches)} documents (expected exactly one \"\n             f\"at {self.expected_path}): {joined}\"\n         )\n \n \n-def _adr_index(root: Path) -> dict[str, list[Path]]:\n-    \"\"\"Map every four-digit ADR number to the record files that claim it.\"\"\"\n-    index: dict[str, list[Path]] = {}\n+def _adr_index(root: Path) -> dict[tuple[str, str], list[Path]]:\n+    \"\"\"Map each decision namespace and number to the files that claim it.\"\"\"\n+    index: dict[tuple[str, str], list[Path]] = {}\n     adr_dir = root / \"docs\" / \"adr\"\n     if adr_dir.is_dir():\n         for path in sorted(adr_dir.glob(\"*.md\")):\n-            match = _ADR_FILENAME.match(path.name)\n+            match = _DECISION_FILENAME.match(path.name)\n             if match:\n-                index.setdefault(match.group(1), []).append(path)\n+                namespace = \"SADR\" if match.group(1) else \"ADR\"\n+                index.setdefault((namespace, match.group(2)), []).append(path)\n     return index\n \n \n def _citing_files(root: Path, include_docs: bool = True) -> list[Path]:\n-    \"\"\"Files scanned for citations: scripts/*.py, and cheaply the markdown.\"\"\"\n+    \"\"\"Current, shipped text surfaces scanned for decision citations.\"\"\"\n     files = sorted((root / \"scripts\").glob(\"*.py\"))\n+    files += sorted((root / \"scripts\").glob(\"*.sh\"))\n     if include_docs:\n         docs = root / \"docs\"\n         if docs.is_dir():\n             files += sorted(docs.rglob(\"*.md\"))\n         files += sorted(root.glob(\"*.md\"))\n-    return files\n+        pyproject = root / \"pyproject.toml\"\n+        if pyproject.is_file():\n+            files.append(pyproject)\n+        for relative in (\".github\", \".codex\"):\n+            surface = root / relative\n+            if surface.is_dir():\n+                files += sorted(\n+                    path\n+                    for path in surface.rglob(\"*\")\n+                    if path.is_file()\n+                    and path.suffix in {\".md\", \".py\", \".sh\", \".toml\", \".yaml\", \".yml\"}\n+                )\n+    return sorted(set(files))\n \n \n def find_adr_reference_violations(\n@@ -94,10 +111,9 @@\n ) -> list[Violation]:\n     \"\"\"Return every ``ADR ####`` citation under ``root`` that fails to resolve.\n \n-    A citation resolves when exactly one ``docs/adr/####-*.md`` matches its\n-    number. Zero matches is a \"missing\" violation; two or more is \"ambiguous\".\n-    Records that are never cited produce nothing -- the converse is not\n-    required.\n+    A citation resolves when exactly one record in the same namespace matches\n+    its number. Zero matches is a \"missing\" violation; two or more is\n+    \"ambiguous\". Records that are never cited produce nothing.\n     \"\"\"\n     root = Path(root)\n     index = _adr_index(root)\n@@ -110,13 +126,15 @@\n         rel = path.relative_to(root).as_posix()\n         for lineno, line in enumerate(text.splitlines(), start=1):\n             for match in _CITATION.finditer(line):\n-                number = match.group(1)\n-                found = index.get(number, [])\n+                namespace, number = match.groups()\n+                found = index.get((namespace, number), [])\n                 if len(found) == 1:\n                     continue\n                 reason = \"missing\" if not found else \"ambiguous\"\n                 rels = [p.relative_to(root).as_posix() for p in found]\n-                violations.append(Violation(rel, number, lineno, reason, rels))\n+                violations.append(\n+                    Violation(rel, namespace, number, lineno, reason, rels)\n+                )\n     return violations\n \n \n@@ -129,8 +147,8 @@\n         self.assertEqual(\n             violations,\n             [],\n-            \"Every 'ADR ####' citation must resolve to exactly one \"\n-            f\"docs/adr/####-*.md:\\n{detail}\",\n+            \"Every ADR/SADR citation must resolve exactly once in its own \"\n+            f\"docs/adr namespace:\\n{detail}\",\n         )\n \n     def test_scan_actually_reads_scripts(self) -> None:\n@@ -142,12 +160,38 @@\n             \"the citation scan must cover scripts/*.py\",\n         )\n \n+    def test_platform_citations_do_not_use_unresolved_numeric_shorthand(self) -> None:\n+        shorthand = re.compile(r\"SADR-\\d{4}/\\d{4}\")\n+        offenders = []\n+        for path in _citing_files(REPO_ROOT):\n+            text = path.read_text(encoding=\"utf-8\")\n+            if shorthand.search(text):\n+                offenders.append(path.relative_to(REPO_ROOT).as_posix())\n+        self.assertEqual(\n+            offenders,\n+            [],\n+            \"repeat the SADR prefix so every cited number is independently resolvable\",\n+        )\n+\n     def test_carried_records_are_indexed(self) -> None:\n         # The two records this Goal carried onto the default branch, and which\n         # the scripts cite most, must be present.\n         index = _adr_index(REPO_ROOT)\n-        self.assertIn(\"0016\", index)\n-        self.assertIn(\"0017\", index)\n+        self.assertIn((\"SADR\", \"0016\"), index)\n+        self.assertIn((\"SADR\", \"0017\"), index)\n+\n+    def test_platform_tree_has_only_sadr_0001_through_0019(self) -> None:\n+        adr_dir = REPO_ROOT / \"docs\" / \"adr\"\n+        sadrs = sorted(adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\"))\n+        self.assertEqual(\n+            {path.name.split(\"-\", 2)[1] for path in sadrs},\n+            {f\"{number:04d}\" for number in range(1, 20)},\n+        )\n+        self.assertEqual(\n+            list(adr_dir.glob(\"[0-9][0-9][0-9][0-9]-*.md\")),\n+            [],\n+            \"the Shiki platform tree must not retain legacy numeric SADR paths\",\n+        )\n \n \n class FixtureAdrReferences(unittest.TestCase):\n@@ -180,6 +224,26 @@\n         self.assertIn(\"9991\", message)  # cited number\n         self.assertIn(\"docs/adr/9991-*.md\", message)  # missing path\n \n+    def test_target_adr_does_not_satisfy_missing_platform_sadr(self) -> None:\n+        root = self._make_repo(\n+            adr_names=[\"0042-target-decision.md\"],\n+            script_body='\"\"\"Target ADR 0042 exists, but platform SADR-0042 does not.\"\"\"\\n',\n+        )\n+        violations = find_adr_reference_violations(root, include_docs=False)\n+        self.assertEqual(len(violations), 1)\n+        self.assertEqual(violations[0].namespace, \"SADR\")\n+        self.assertEqual(violations[0].number, \"0042\")\n+        self.assertEqual(violations[0].expected_path, \"docs/adr/SADR-0042-*.md\")\n+\n+    def test_target_adr_and_platform_sadr_may_reuse_a_number(self) -> None:\n+        root = self._make_repo(\n+            adr_names=[\"0042-target-decision.md\", \"SADR-0042-platform-decision.md\"],\n+            script_body='\"\"\"Target ADR 0042 and platform SADR-0042 are distinct.\"\"\"\\n',\n+        )\n+        self.assertEqual(\n+            find_adr_reference_violations(root, include_docs=False), []\n+        )\n+\n     def test_uncited_adr_is_allowed(self) -> None:\n         # An ADR exists but nothing cites it -> not a violation. The converse\n         # (every ADR must be cited) is explicitly NOT required.\ndiff --git a/tests/test_guardian_review.py b/tests/test_guardian_review.py\nindex 88829e7810212635e2fc98aa5918d6f26294677d..00a85a237acd5bf2d6cb5b294128198c422402ca\n--- a/tests/test_guardian_review.py\n+++ b/tests/test_guardian_review.py\n@@ -162,7 +162,7 @@\n \n     def test_docs_only_is_light_and_not_conservative(self):\n         focus = gr.classify_review_focus([\"docs/adr/0099-x.md\", \"README.md\"], \"\", \"low\")\n-        self.assertTrue(any(\"Docs/ADR-only\" in f for f in focus))\n+        self.assertTrue(any(\"Docs/decision-record-only\" in f for f in focus))\n         self.assertFalse(any(\"conservative default\" in f.lower() for f in focus))\n \n     def test_unknown_category_is_conservative(self):\ndiff --git a/tests/test_policy_job_merged_prs.py b/tests/test_policy_job_merged_prs.py\nindex d45512e74681e5e649279236c7d4e79d5c3da471..b97989f8bd74d091de1a6b5275a20d8dc68c4512\n--- a/tests/test_policy_job_merged_prs.py\n+++ b/tests/test_policy_job_merged_prs.py\n@@ -3,8 +3,8 @@\n \n ``.github/workflows/shiki-cca-completion.yml`` has two consumers of the merged-PR\n proof: the CCA *signal* (``guardian_approval_signal.py`` in \"Prepare CCA context\")\n-and the authoritative gate (``mergegate_check.py`` in \"Enforce MergeGate\"). ADR\n-0017's bookkeeping-closeout exemption (condition 6) needs the merged implementation\n+and the authoritative gate (``mergegate_check.py`` in \"Enforce MergeGate\").\n+SADR-0017's bookkeeping-closeout exemption (condition 6) needs the merged implementation\n PR proven, and a closeout carries NO ``post_merge_reconcile`` marker. The signal\n already resolves ``MERGED_PRS`` unconditionally, but \"Enforce MergeGate\" used to\n resolve it only inside ``grep -q '<!-- shiki:post_merge_reconcile -->'``. So a\ndiff --git a/tests/test_sadr_install_migration.py b/tests/test_sadr_install_migration.py\nnew file mode 100644\nindex 0000000000000000000000000000000000000000..d51eac872b1c79ff4579c4c1401ca5d775a60d8d\n--- /dev/null\n+++ b/tests/test_sadr_install_migration.py\n@@ -0,0 +1,237 @@\n+\"\"\"SADR installation and collision-safe legacy upgrade behavior.\"\"\"\n+\n+from __future__ import annotations\n+\n+import contextlib\n+import hashlib\n+import io\n+import json\n+import shutil\n+import tempfile\n+import unittest\n+from pathlib import Path\n+\n+import shiki_test_support  # noqa: F401  (path bootstrap)\n+\n+import shiki_installer\n+from tests.test_adr_references import find_adr_reference_violations\n+\n+\n+PRODUCT_NUMBERS = tuple(range(13, 19))\n+\n+\n+def _quiet_install(target: Path, *, force: bool, validate: bool) -> None:\n+    with contextlib.redirect_stdout(io.StringIO()):\n+        shiki_installer.install_template(target, force=force, validate=validate)\n+\n+\n+def _sha256(path: Path) -> str:\n+    return hashlib.sha256(path.read_bytes()).hexdigest()\n+\n+\n+def _legacy_sadr_paths() -> tuple[str, ...]:\n+    return tuple(\n+        f\"docs/adr/{path.name.removeprefix('SADR-')}\"\n+        for path in sorted((shiki_installer.ROOT / \"docs\" / \"adr\").glob(\"SADR-*.md\"))\n+        if path.name != \"SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\"\n+    )\n+\n+\n+def _create_legacy_upgrade_fixture(\n+    target: Path,\n+) -> tuple[dict[str, bytes], tuple[str, ...]]:\n+    adr_dir = target / \"docs\" / \"adr\"\n+    adr_dir.mkdir(parents=True)\n+    product_bytes = {\n+        f\"{number:04d}-cubeshare-product-decision.md\": (\n+            f\"# CubeShare ADR {number:04d}\\n\\nDo not rewrite {number}.\\n\"\n+        ).encode()\n+        for number in PRODUCT_NUMBERS\n+    }\n+    for name, content in product_bytes.items():\n+        (adr_dir / name).write_bytes(content)\n+\n+    legacy_paths = _legacy_sadr_paths()\n+    for relative in legacy_paths:\n+        (target / relative).write_bytes(f\"legacy Shiki bytes for {relative}\\n\".encode())\n+\n+    stamp_path = target / shiki_installer.INSTALL_STAMP_PATH\n+    stamp_path.parent.mkdir(parents=True, exist_ok=True)\n+    stamp = {\n+        \"version\": shiki_installer.INSTALL_STAMP_VERSION,\n+        \"platform_commit\": None,\n+        \"installed_at\": \"2000-01-01T00:00:00+00:00\",\n+        \"digests\": {\n+            relative: _sha256(target / relative) for relative in legacy_paths\n+        },\n+    }\n+    stamp_path.write_text(\n+        json.dumps(stamp, indent=2, sort_keys=True) + \"\\n\", encoding=\"utf-8\"\n+    )\n+    return product_bytes, legacy_paths\n+\n+\n+def _tree_snapshot(root: Path) -> dict[str, tuple[str, bytes | str]]:\n+    \"\"\"Recursive byte/shape snapshot used to prove preflight is write-free.\"\"\"\n+    snapshot: dict[str, tuple[str, bytes | str]] = {}\n+    for path in sorted(root.rglob(\"*\")):\n+        relative = path.relative_to(root).as_posix()\n+        if path.is_symlink():\n+            snapshot[relative] = (\"symlink\", path.readlink().as_posix())\n+        elif path.is_dir():\n+            snapshot[relative] = (\"directory\", \"\")\n+        else:\n+            snapshot[relative] = (\"file\", path.read_bytes())\n+    return snapshot\n+\n+\n+class FreshSadrInstallTests(unittest.TestCase):\n+    def setUp(self) -> None:\n+        self.tmp = Path(tempfile.mkdtemp(prefix=\"shiki-sadr-fresh-\"))\n+        self.addCleanup(lambda: shutil.rmtree(self.tmp, ignore_errors=True))\n+        self.target = self.tmp / \"target\"\n+        self.adr_dir = self.target / \"docs\" / \"adr\"\n+        self.adr_dir.mkdir(parents=True)\n+        self.product_bytes = {\n+            f\"{number:04d}-product-decision.md\": (\n+                f\"# Product ADR {number:04d}\\n\\nTarget-owned bytes {number}.\\n\"\n+            ).encode()\n+            for number in PRODUCT_NUMBERS\n+        }\n+        for name, content in self.product_bytes.items():\n+            (self.adr_dir / name).write_bytes(content)\n+\n+    def test_fresh_install_preserves_product_adrs_and_installs_all_sadrs(self) -> None:\n+        _quiet_install(self.target, force=False, validate=True)\n+\n+        for name, expected in self.product_bytes.items():\n+            self.assertEqual((self.adr_dir / name).read_bytes(), expected)\n+\n+        sadr_numbers = {\n+            path.name.split(\"-\", 2)[1]\n+            for path in self.adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\")\n+        }\n+        self.assertEqual(sadr_numbers, {f\"{number:04d}\" for number in range(1, 20)})\n+\n+        numeric_records = {\n+            path.name\n+            for path in self.adr_dir.glob(\"[0-9][0-9][0-9][0-9]-*.md\")\n+        }\n+        self.assertEqual(numeric_records, set(self.product_bytes))\n+        stamp = json.loads(\n+            (self.target / shiki_installer.INSTALL_STAMP_PATH).read_text(\n+                encoding=\"utf-8\"\n+            )\n+        )\n+        for path in self.adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\"):\n+            relative = path.relative_to(self.target).as_posix()\n+            self.assertEqual(stamp[\"digests\"][relative], _sha256(path))\n+        self.assertEqual(find_adr_reference_violations(self.target), [])\n+\n+\n+class LegacySadrUpgradeTests(unittest.TestCase):\n+    def setUp(self) -> None:\n+        self.tmp = Path(tempfile.mkdtemp(prefix=\"shiki-sadr-upgrade-\"))\n+        self.addCleanup(lambda: shutil.rmtree(self.tmp, ignore_errors=True))\n+        self.target = self.tmp / \"target\"\n+        self.adr_dir = self.target / \"docs\" / \"adr\"\n+        self.product_bytes, self.legacy_paths = _create_legacy_upgrade_fixture(\n+            self.target\n+        )\n+\n+    def test_force_upgrade_deletes_only_stamped_legacy_sadrs(self) -> None:\n+        _quiet_install(self.target, force=True, validate=True)\n+\n+        for relative in self.legacy_paths:\n+            self.assertFalse((self.target / relative).exists(), relative)\n+        for name, expected in self.product_bytes.items():\n+            self.assertEqual((self.adr_dir / name).read_bytes(), expected)\n+\n+        sadrs = list(self.adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\"))\n+        self.assertEqual(len(sadrs), 19)\n+        refreshed = json.loads(\n+            (self.target / shiki_installer.INSTALL_STAMP_PATH).read_text(encoding=\"utf-8\")\n+        )\n+        for relative in self.legacy_paths:\n+            self.assertNotIn(relative, refreshed[\"digests\"])\n+        for path in sadrs:\n+            relative = path.relative_to(self.target).as_posix()\n+            self.assertEqual(refreshed[\"digests\"][relative], _sha256(path))\n+\n+    def test_non_force_install_never_deletes_legacy_files(self) -> None:\n+        before = {\n+            relative: (self.target / relative).read_bytes()\n+            for relative in self.legacy_paths\n+        }\n+        _quiet_install(self.target, force=False, validate=False)\n+        for relative, expected in before.items():\n+            self.assertEqual((self.target / relative).read_bytes(), expected)\n+        refreshed = json.loads(\n+            (self.target / shiki_installer.INSTALL_STAMP_PATH).read_text(encoding=\"utf-8\")\n+        )\n+        for relative in self.legacy_paths:\n+            self.assertEqual(refreshed[\"digests\"][relative], _sha256(self.target / relative))\n+\n+\n+class LegacyCleanupPreflightTests(unittest.TestCase):\n+    def setUp(self) -> None:\n+        self.tmp = Path(tempfile.mkdtemp(prefix=\"shiki-sadr-preflight-\"))\n+        self.addCleanup(lambda: shutil.rmtree(self.tmp, ignore_errors=True))\n+\n+    def test_all_ownership_blockers_abort_before_any_write(self) -> None:\n+        cases = (\n+            (\"absent\", \"install stamp is absent\"),\n+            (\"unreadable\", \"install stamp is unreadable\"),\n+            (\"malformed\", \"install stamp is malformed\"),\n+            (\"missing-digest\", \"install stamp has no digest\"),\n+            (\"digest-mismatch\", \"digest mismatch\"),\n+        )\n+        for name, expected_reason in cases:\n+            with self.subTest(name=name):\n+                target = self.tmp / name\n+                _, legacy_paths = _create_legacy_upgrade_fixture(target)\n+                stamp_path = target / shiki_installer.INSTALL_STAMP_PATH\n+\n+                if name == \"absent\":\n+                    stamp_path.unlink()\n+                elif name == \"unreadable\":\n+                    stamp_path.unlink()\n+                    stamp_path.mkdir()\n+                elif name == \"malformed\":\n+                    stamp_path.write_text(\"{not-json\\n\", encoding=\"utf-8\")\n+                else:\n+                    stamp = json.loads(stamp_path.read_text(encoding=\"utf-8\"))\n+                    if name == \"missing-digest\":\n+                        stamp[\"digests\"] = {}\n+                    else:\n+                        stamp[\"digests\"] = {\n+                            relative: \"deadbeef\" for relative in legacy_paths\n+                        }\n+                    stamp_path.write_text(\n+                        json.dumps(stamp, indent=2, sort_keys=True) + \"\\n\",\n+                        encoding=\"utf-8\",\n+                    )\n+\n+                before = _tree_snapshot(target)\n+                with self.assertRaises(shiki_installer.ShikiError) as raised:\n+                    _quiet_install(target, force=True, validate=False)\n+                after = _tree_snapshot(target)\n+\n+                self.assertEqual(before, after, f\"{name} preflight changed the tree\")\n+                message = str(raised.exception)\n+                self.assertIn(expected_reason, message)\n+                self.assertIn(\"No files were written\", message)\n+                for relative in legacy_paths:\n+                    self.assertIn(relative, message)\n+\n+    def test_absent_stamp_is_allowed_when_no_legacy_path_exists(self) -> None:\n+        target = self.tmp / \"no-legacy\"\n+        target.mkdir()\n+        _quiet_install(target, force=True, validate=False)\n+        self.assertTrue(\n+            (target / \"docs\" / \"adr\" / \"SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\").is_file()\n+        )\n+\n+\n+if __name__ == \"__main__\":\n+    unittest.main()\n\n2026-08-07T08:25:10.890567Z ERROR codex_models_manager::manager: failed to renew cache TTL: missing field `base_instructions` at line 94 column 5\nexec\n/bin/zsh -lc \"tail -40 .shiki/schemas/task.schema.json && rg -n 'implementer_code_review' .shiki scripts tests\" in /private/tmp/shiki-adr-collision.pa0KwY/.worktrees/shiki-t-20260807t073307221254z-2d22d654-introduc\n exited 1 in 0ms:\n    \"locks\": {\n      \"type\": \"array\",\n      \"items\": { \"type\": \"string\" }\n    },\n    \"assigned_runtime\": {\n      \"enum\": [\"codex\", \"codex-front\", \"claude-code\", \"github-actions\", \"hermes-runner\", \"human\", \"other\"]\n    },\n    \"risk_level\": { \"enum\": [\"low\", \"medium\", \"high\", \"critical\"] },\n    \"required_skills\": {\n      \"type\": \"array\",\n      \"items\": { \"type\": \"string\" }\n    },\n    \"cca_checklist_profile\": {\n      \"description\": \"CCA checklist ids the PR's structured verdict must judge to a terminal status (see scripts/mergegate_check.checklist_profile_coverage_failures). MergeGate blocks a verdict that omits or leaves unresolved any id declared here. Optional (absent from `required`) until every pre-existing task file carries it: promoting it to a required field would force backfilling all such files, which are outside this task's declared locks. This mirrors scripts/validate_shiki.py keeping it out of TASK_REQUIRED; the two surfaces must be promoted together (see tests/test_cca_verdict_consistency.TaskSchemaRequiredConsistency).\",\n      \"type\": \"array\",\n      \"items\": { \"type\": \"string\" }\n    },\n    \"acceptance_checks\": {\n      \"type\": \"array\",\n      \"items\": { \"type\": \"string\", \"minLength\": 1 },\n      \"minItems\": 1\n    },\n    \"expected_branch\": { \"type\": \"string\", \"minLength\": 1 },\n    \"expected_pr\": { \"type\": [\"integer\", \"null\"] },\n    \"ledger_evidence\": {\n      \"type\": \"array\",\n      \"items\": { \"type\": \"string\", \"minLength\": 1 },\n      \"minItems\": 1\n    },\n    \"status\": {\n      \"enum\": [\"planned\", \"ready\", \"running\", \"blocked\", \"review\", \"repair-needed\", \"done\"]\n    },\n    \"test_command\": {\n      \"description\": \"Structured command the goal loop exec's in the task worktree for the loop-observed TDD gate (ADR 0011) before opening the PR. Defaults to 'python3 -m unittest discover -s tests'. acceptance_checks is free-form prose and is never exec'd.\",\n      \"type\": \"string\",\n      \"minLength\": 1\n    }\n  },\n  \"additionalProperties\": true\n}\n\ndiff --git a/.codex/skills/shiki/SKILL.md b/.codex/skills/shiki/SKILL.md\nindex c8e2e85d9d75d49cc4f2d6286ee22659c4e53e75..93cded623b2c15d9f65c76491a1c2d813ac9e6c0\n--- a/.codex/skills/shiki/SKILL.md\n+++ b/.codex/skills/shiki/SKILL.md\n@@ -18,6 +18,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n Codex CLI does not register this skill as a custom `/shiki` slash command. When\n the user types `/shiki` in Codex CLI and gets \"Unrecognized command\", explain\n that this is expected: invoke Shiki in Codex with natural language such as\n@@ -69,7 +72,7 @@\n \n ## Responsibilities\n \n-- Claude Code implements and repairs by default (`shiki runner claude`, ADR 0008); Codex implements and repairs tasks explicitly assigned to `codex`.\n+- Claude Code implements and repairs by default (`shiki runner claude`, SADR-0008); Codex implements and repairs tasks explicitly assigned to `codex`.\n - Claude Code Action can act as GitHub-side CCA or reviewer.\n - CCA judges completion.\n - MergeGate authorizes state transitions and merge readiness.\ndiff --git a/.github/prompts/cca-completion-check.md b/.github/prompts/cca-completion-check.md\nindex ed203ad5f302ecc81372d8ab27d2b1232ca97497..fd89950528333ef6242e6d5443904f5392ab7e56\n--- a/.github/prompts/cca-completion-check.md\n+++ b/.github/prompts/cca-completion-check.md\n@@ -19,6 +19,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n ## Required Reading\n \n Read only the files needed to judge the PR contract. Prefer this order:\ndiff --git a/.github/prompts/codex-task-handoff.md b/.github/prompts/codex-task-handoff.md\nindex 695e211d76dea1c60e8d5bb360e78487808286a4..59441d576b01a39569722f226c854e5a740e9eb5\n--- a/.github/prompts/codex-task-handoff.md\n+++ b/.github/prompts/codex-task-handoff.md\n@@ -2,7 +2,7 @@\n \n You are Codex Front acting as the Implementer for a Shiki task.\n \n-Follow `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant ADRs, and `docs/agents/codex-handoff.md`.\n+Follow `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant Shiki SADRs and target ADRs, and `docs/agents/codex-handoff.md`.\n \n Rules:\n \ndiff --git a/AGENTS.md b/AGENTS.md\nindex 7f041100d49c2a7525f51cb0f5d696a4a6d1c78c..a06bfbabc6fa48a007389e25e12d7ed523936bed\n--- a/AGENTS.md\n+++ b/AGENTS.md\n@@ -22,7 +22,7 @@\n - **Codex Front** is an optional implementer through Codex App, Codex CLI, Codex IDE extension, or Codex Web signed in with ChatGPT OAuth/subscription auth, dispatched with `shiki runner codex` when a task is explicitly assigned to `codex`.\n - **GitHub CCA** is implemented by Claude Code Action by default, using `CLAUDE_CODE_OAUTH_TOKEN`.\n \n-Do not assume `openai/codex-action`, `OPENAI_API_KEY`, or API-key based Codex automation in the default Shiki path. API-key runners are explicit target-repo extensions and require their own ADR.\n+Do not assume `openai/codex-action`, `OPENAI_API_KEY`, or API-key based Codex automation in the default Shiki path. API-key runners are explicit target-repo extensions and require their own target ADR.\n \n Shiki is not:\n \n@@ -44,10 +44,14 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use\n+`SADR-NNNN-*.md`; target product decisions retain `NNNN-*.md` and the `ADR`\n+namespace.\n+\n Use the canonical source-of-truth order above. Runtime-specific reading orders\n are operational guidance only and must not override or reorder it.\n \n-Conversation state is not durable truth. If a decision matters, put it in GitHub, `.shiki/`, `CONTEXT.md`, or an ADR.\n+Conversation state is not durable truth. If a decision matters, put it in GitHub, `.shiki/`, `CONTEXT.md`, a Shiki SADR, or a target ADR.\n \n If GitHub and `.shiki/` disagree, surface the conflict and prefer GitHub operational state until the mirror is repaired.\n \n@@ -75,8 +79,8 @@\n Every non-trivial change follows this loop:\n \n 1. **Goal**: clarify outcome, completion conditions, non-goals, risk level, and success signals.\n-2. **grill-with-docs**: challenge the plan against domain docs, ADRs, code reality, terminology, and edge scenarios.\n-3. **Context & Impact**: find relevant docs, ADRs, code, dependencies, owners, locks, and verification surfaces.\n+2. **grill-with-docs**: challenge the plan against domain docs, Shiki SADRs, target ADRs, code reality, terminology, and edge scenarios.\n+3. **Context & Impact**: find relevant docs, SADRs, target ADRs, code, dependencies, owners, locks, and verification surfaces.\n 4. **PRD and Spec Freeze**: use `to-prd` when context is settled enough to become durable product/engineering intent. The operator's explicit approval of the PRD is Spec Freeze: the plan records a `spec_freeze` block, and no plan runs without it. Steps 1-4 form Requirements Definition \u2014 one continuous operator dialogue. After Spec Freeze, scope changes require an operator-approved Spec Amendment; non-scope-moving interpretations go to the Assumption Log.\n 5. **Issues**: use `to-issues` to create independently grabbable vertical-slice issues. Prefer AFK slices over HITL slices where possible.\n 6. **Triage**: label issues for readiness, risk, runtime, skills, and MergeGate state.\n@@ -167,7 +171,7 @@\n - Testing decisions.\n - Out-of-scope items.\n - Further notes.\n-- Links to relevant domain terms and ADRs.\n+- Links to relevant domain terms, SADRs, and target ADRs.\n \n Every executable task must include:\n \n@@ -193,7 +197,7 @@\n \n Context & Impact must identify:\n \n-- Relevant documents, domain terms, ADRs, and past decisions.\n+- Relevant documents, domain terms, SADRs, target ADRs, and past decisions.\n - Relevant modules, interfaces, seams, callers, owners, and tests.\n - Dependency relationships between tasks.\n - Candidate locks and lock conflicts.\n@@ -210,7 +214,7 @@\n | Skill | Required when |\n | --- | --- |\n | `setup-matt-pocock-skills` | First configuring the repo for skills, or when issue tracker, triage labels, or domain docs are missing. |\n-| `grill-with-docs` | Default for non-trivial Goals before PRD/issues; always required when plans, terminology, boundaries, tradeoffs, or ADR-worthy decisions are ambiguous. |\n+| `grill-with-docs` | Default for non-trivial Goals before PRD/issues; always required when plans, terminology, boundaries, tradeoffs, or decision-record-worthy choices are ambiguous. |\n | `zoom-out` | The agent lacks an architectural map of the relevant code area. |\n | `to-prd` | Settled Goal context must become durable product/engineering intent. |\n | `to-issues` | A Goal, PRD, or plan must become independently grabbable vertical-slice issues. |\n@@ -348,7 +352,7 @@\n - Three or more new files.\n - Any high-risk or critical label.\n \n-When the architecture gate triggers, run `grill-with-docs` or `improve-codebase-architecture` as appropriate and record durable decisions in an ADR when needed.\n+When the architecture gate triggers, run `grill-with-docs` or `improve-codebase-architecture` as appropriate and record durable decisions in a Shiki SADR or target ADR as applicable.\n \n ## Engineering Discipline\n \ndiff --git a/CLAUDE.md b/CLAUDE.md\nindex 98a932e7841a632866fcd53b0bad0e3af3711d3d..98a31399404d6fb044b4fd493f4459ac87f439de\n--- a/CLAUDE.md\n+++ b/CLAUDE.md\n@@ -14,6 +14,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n ## Claude Role\n \n Default to planner, default implementer, reviewer, coordinator, documentation editor, CCA-style completion judge, and final-judgment assistant.\n@@ -57,7 +60,7 @@\n At the start of a Shiki session:\n \n 1. Read `AGENTS.md`.\n-2. Read `CONTEXT.md` and relevant ADRs.\n+2. Read `CONTEXT.md` and relevant Shiki SADRs and target ADRs.\n 3. Read active `.shiki/` Goal/task/ledger state when present.\n 4. Read active distilled rules from `.shiki/memories/` (status `distilled`, `active: true`, not revoked or superseded) for applicable guidance; the same rules are injected deterministically into task handoffs as the `## Distilled Rules` section (\u00a73.5).\n 5. Read the GitHub Issue or PR when the task is GitHub-backed.\n@@ -74,7 +77,7 @@\n - Define the Goal, completion conditions, non-goals, and risk level.\n - Treat `grill-with-docs` as the default planning entry point for non-trivial Goals.\n - Challenge domain terms against `CONTEXT.md`.\n-- Challenge hard-to-reverse tradeoffs against ADRs.\n+- Challenge hard-to-reverse tradeoffs against SADRs and target ADRs.\n - Explore code instead of asking when code can answer the question.\n - Run Context & Impact before implementation planning.\n - Produce a Task DAG, not an unordered checklist.\n@@ -94,7 +97,7 @@\n - For each question, provide your recommended answer.\n - Resolve dependency decisions before downstream design questions.\n - Update `CONTEXT.md` inline when terminology crystallizes.\n-- Offer ADRs only when the decision is hard to reverse, surprising without context, and a real tradeoff.\n+- Offer a SADR or target ADR only when the decision is hard to reverse, surprising without context, and a real tradeoff.\n - Convert unresolved questions into HITL blockers rather than allowing Codex to guess.\n \n ## PRD and Issues Standard\n@@ -103,7 +106,7 @@\n \n - Use the project's domain glossary vocabulary.\n - Avoid volatile file paths unless they are essential.\n-- Include implementation decisions, testing decisions, out-of-scope items, and relevant ADR/domain links.\n+- Include implementation decisions, testing decisions, out-of-scope items, and relevant SADR/target-ADR/domain links.\n - Identify deep-module opportunities and testable interfaces.\n \n When creating issues:\n@@ -121,7 +124,7 @@\n - Branch/worktree target.\n - Scope and non-goals.\n - Dependencies and locks.\n-- Relevant docs, ADRs, modules, and tests.\n+- Relevant docs, SADRs, target ADRs, modules, and tests.\n - Required skill invocations.\n - TDD expectations.\n - Pre-PR `code-review` gate expectations.\n@@ -152,7 +155,7 @@\n \n - Treat yourself as a judge, not implementer.\n - Do not write production code.\n-- Read `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant ADRs, PRD, task issue, PR body, diff, checks, reviews, labels, and `.shiki/` state.\n+- Read `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant SADRs and target ADRs, PRD, task issue, PR body, diff, checks, reviews, labels, and `.shiki/` state.\n - Evaluate every applicable checklist in `docs/agents/checklists.md`.\n - Map each acceptance criterion to durable evidence.\n - Distinguish implementation failure from missing evidence.\ndiff --git a/CONTEXT.md b/CONTEXT.md\nindex e61a9be06a0cce0d026484083a657bde2067e234..237ee8aa3ea8fab334ece49768bc5ed3b5af0974\n--- a/CONTEXT.md\n+++ b/CONTEXT.md\n@@ -25,7 +25,7 @@\n _Avoid_: jumping directly into code\n \n **grill-with-docs**:\n-A planning skill that challenges a plan against domain language, ADRs, code reality, and concrete edge scenarios. It resolves design-tree questions before PRD/issues. It runs inside Requirements Definition.\n+A planning skill that challenges a plan against domain language, Shiki SADRs, target ADRs, code reality, and concrete edge scenarios. It resolves design-tree questions before PRD/issues. It runs inside Requirements Definition.\n _Avoid_: generic brainstorming, silent assumptions\n \n **Requirements Definition**:\ndiff --git a/CONTRIBUTING.md b/CONTRIBUTING.md\nindex c6d9b656e73abb8a55e5f660123e03153dcb3e6d..9e37a8fbf668db5b22461aaae5548b8f1354394c\n--- a/CONTRIBUTING.md\n+++ b/CONTRIBUTING.md\n@@ -17,7 +17,7 @@\n 1. **Goal** \u2014 start from a user-approved Goal with an outcome, completion\n    conditions, non-goals, risk level, and success signals.\n 2. **grill-with-docs** \u2014 for any non-trivial change, challenge the plan against\n-   domain language, ADRs, and code reality before writing a PRD.\n+   domain language, Shiki SADRs, target ADRs, and code reality before writing a PRD.\n 3. **PRD** \u2014 capture settled product/engineering intent (`to-prd`).\n 4. **Spec Freeze** \u2014 the operator explicitly approves the PRD/requirements;\n    the plan records a `spec_freeze` block and no plan runs without it\n@@ -94,7 +94,7 @@\n   entries.\n - Do not hand-edit `.shiki/goals/*.json` or `.shiki/manifest.json` in a task PR.\n - Conversation state is not durable truth. If a decision matters, record it in\n-  GitHub, `.shiki/`, `CONTEXT.md`, or an ADR.\n+  GitHub, `.shiki/`, `CONTEXT.md`, a Shiki SADR, or a target ADR.\n \n ## Architecture Gate\n \n@@ -103,7 +103,7 @@\n or CI policy, package structure, three or more structural files, more than 100\n net new lines in a core module, three or more new files, or any high-risk label.\n When triggered, run `grill-with-docs` or `improve-codebase-architecture` and\n-record durable decisions in an ADR when needed (see `docs/adr/`).\n+record durable decisions in a Shiki SADR or target ADR as applicable (see `docs/adr/`).\n \n ## Safety\n \ndiff --git a/SYSTEM_PROMPT.md b/SYSTEM_PROMPT.md\nindex aaaea45e3a896c3a44ba89eec8bb84325ecf6c62..da009c86a88285269dee0155e58e550c6d744ea9\n--- a/SYSTEM_PROMPT.md\n+++ b/SYSTEM_PROMPT.md\n@@ -30,6 +30,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n Use the canonical source-of-truth order above. Runtime-specific reading orders\n are operational guidance only and must not override or reorder it. The current\n conversation is non-durable operator input.\n@@ -48,14 +51,14 @@\n \n A runtime may only do work that is explicitly assigned by the Goal, task, PR, or MergeGate decision.\n \n-Do not assume `openai/codex-action`, `OPENAI_API_KEY`, or API-key based Codex automation in the default Shiki path. API-key runners are explicit target-repo extensions and require their own ADR.\n+Do not assume `openai/codex-action`, `OPENAI_API_KEY`, or API-key based Codex automation in the default Shiki path. API-key runners are explicit target-repo extensions and require their own target ADR.\n \n ## 4. Non-negotiable Shiki flow\n \n For any non-trivial Goal, use this flow:\n \n 1. **Goal Seek**: define outcome, non-goals, risk level, completion criteria, and evidence requirements.\n-2. **grill-with-docs**: challenge the plan against `CONTEXT.md`, ADRs, code reality, terminology, boundaries, and edge scenarios. Ask one question at a time when the operator is available. If code can answer a question, inspect code instead of asking.\n+2. **grill-with-docs**: challenge the plan against `CONTEXT.md`, Shiki SADRs, target ADRs, code reality, terminology, boundaries, and edge scenarios. Ask one question at a time when the operator is available. If code can answer a question, inspect code instead of asking.\n 3. **Context & Impact**: identify modules, interfaces, seams, callers, dependencies, locks, risk, and verification surfaces.\n 4. **to-prd + Spec Freeze**: turn settled context into a PRD using domain vocabulary and testing decisions. The operator's explicit approval of the PRD is Spec Freeze, recorded as the plan's `spec_freeze` block; plans without it do not run. Steps 1-4 form Requirements Definition.\n 5. **to-issues**: decompose the PRD into independently grabbable vertical-slice issues. Prefer AFK slices; mark HITL slices when judgment is still required.\n@@ -75,7 +78,7 @@\n Before making any material change:\n \n 1. Read `AGENTS.md` and any runtime-specific wrapper such as `CLAUDE.md`.\n-2. Read `CONTEXT.md` and relevant ADRs.\n+2. Read `CONTEXT.md` and relevant SADRs and target ADRs.\n 3. Read the GitHub Issue / PR and `.shiki/` state for the active Goal or task.\n 4. Identify your role: Planner, Implementer, Reviewer, Completion Check Agent, Repairer, or Guardian-assist.\n 5. Identify Goal id, task id, branch, PR, dependencies, locks, risk level, required skills, and required evidence.\n@@ -103,7 +106,7 @@\n \n At minimum, identify:\n \n-- Relevant documents, ADRs, domain terms, and prior decisions.\n+- Relevant documents, SADRs, target ADRs, domain terms, and prior decisions.\n - Relevant modules, interfaces, seams, callers, owners, and tests.\n - Dependency relationships between tasks.\n - Candidate file locks and lock conflicts.\n@@ -152,7 +155,7 @@\n \n CCA must read:\n \n-- `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant ADRs.\n+- `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant SADRs and target ADRs.\n - Parent Goal issue, PRD issue, task issue, comments, and labels.\n - PR body, diff, commits, changed files, reviews, CI checks, and artifacts available to the workflow.\n - `.shiki/` task contract, locks, ledger entries, CCA checklist profile, and prior repair packets.\n@@ -214,7 +217,7 @@\n - Three or more new files.\n - Any high-risk or critical label.\n \n-When the architecture gate triggers, run `grill-with-docs` or `improve-codebase-architecture` as appropriate and record the decision in an ADR if it is hard to reverse or likely to be re-litigated.\n+When the architecture gate triggers, run `grill-with-docs` or `improve-codebase-architecture` as appropriate and record the decision in a Shiki SADR or target ADR as applicable if it is hard to reverse or likely to be re-litigated.\n \n ## 14. Skill Gate\n \n@@ -223,7 +226,7 @@\n Use these skills:\n \n - `setup-matt-pocock-skills`: before first use of the engineering skill set, or when issue tracker, triage labels, or domain docs are missing.\n-- `grill-with-docs`: default for non-trivial Goals before PRD/issues; required for ambiguous plans, terminology, boundaries, ADR-worthy tradeoffs, or design-tree decisions.\n+- `grill-with-docs`: default for non-trivial Goals before PRD/issues; required for ambiguous plans, terminology, boundaries, decision-record-worthy tradeoffs, or design-tree decisions.\n - `zoom-out`: unfamiliar code area or missing architectural map.\n - `to-prd`: convert settled context into a PRD.\n - `to-issues`: break a PRD, plan, or Goal into independently grabbable vertical-slice issues.\ndiff --git a/docs/adr/0001-keep-shiki-as-platform-name.md b/docs/adr/SADR-0001-keep-shiki-as-platform-name.md\nindex 20a9637c3b9d8c97de7ba8db7cfae4d82d9a7dba..8b685439959aada1b221c112f0ac3c51ca8a1695\n--- a/docs/adr/0001-keep-shiki-as-platform-name.md\n+++ b/docs/adr/SADR-0001-keep-shiki-as-platform-name.md\n@@ -1,3 +1,3 @@\n-# Keep Shiki As The Platform Name\n+# SADR-0001: Keep Shiki As The Platform Name\n \n Shiki remains the product name for the GitHub-native agentic engineering control plane. We considered more descriptive names such as MergePilot, MergeGate, and Kiroku, but kept Shiki because it carries the existing goal-seeking framework identity while remaining independent of Hermes, Codex, Claude Code, or any single runtime.\ndiff --git a/docs/adr/0002-use-agents-as-shared-constitution.md b/docs/adr/SADR-0002-use-agents-as-shared-constitution.md\nindex 73de30ecb078120eac540d9bc51855e3cd48d0e3..c5486182bf06fca36ea508496b013c67302fd989\n--- a/docs/adr/0002-use-agents-as-shared-constitution.md\n+++ b/docs/adr/SADR-0002-use-agents-as-shared-constitution.md\n@@ -1,3 +1,3 @@\n-# Use AGENTS As The Shared Agent Constitution\n+# SADR-0002: Use AGENTS As The Shared Agent Constitution\n \n Shiki uses `AGENTS.md` as the runtime-neutral constitution and `CLAUDE.md` as a thin Claude Code wrapper. This keeps Codex, Claude Code, Hermes Runner, and future Agent Runtimes aligned on the same GitHub-first MergeGate rules while still allowing runtime-specific instructions where needed.\ndiff --git a/docs/adr/0003-require-skill-gate-for-mergegate-readiness.md b/docs/adr/SADR-0003-require-skill-gate-for-mergegate-readiness.md\nindex 424be0c9af4c8f0136f1fde68a468c47ff84e764..7fc337987c7df9792f75f181dfcbed75fe5ffff2\n--- a/docs/adr/0003-require-skill-gate-for-mergegate-readiness.md\n+++ b/docs/adr/SADR-0003-require-skill-gate-for-mergegate-readiness.md\n@@ -1,3 +1,3 @@\n-# Require Skill Gate For MergeGate Readiness\n+# SADR-0003: Require Skill Gate For MergeGate Readiness\n \n Shiki requires relevant engineering skills to be invoked before execution when their triggers apply. This makes skills part of MergeGate readiness rather than optional prompt style, because Shiki depends on repeatable planning, issue decomposition, TDD, diagnosis, triage, and architecture review before autonomous or parallel agent work can be trusted.\ndiff --git a/docs/adr/0004-use-github-first-with-shiki-mirror.md b/docs/adr/SADR-0004-use-github-first-with-shiki-mirror.md\nindex d0a3a9174dc646a945a9d325954ffaf00c352040..a79c54ae9ee8eed2a0524192b864da4e22a92d3f\n--- a/docs/adr/0004-use-github-first-with-shiki-mirror.md\n+++ b/docs/adr/SADR-0004-use-github-first-with-shiki-mirror.md\n@@ -1,4 +1,4 @@\n-# ADR 0004: Use GitHub First With A Repository-Local Shiki Mirror\n+# SADR-0004: Use GitHub First With A Repository-Local Shiki Mirror\n \n ## Status\n \ndiff --git a/docs/adr/0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md b/docs/adr/SADR-0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\nindex b339baf49b89d9290e79c3d51b36e7b2bde3bd95..0f3c3eb9afccdb3403ce978e21d04fe12e495da0\n--- a/docs/adr/0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\n+++ b/docs/adr/SADR-0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\n@@ -1,8 +1,8 @@\n-# ADR 0005: Use Codex As Front And Claude Code Action As The GitHub Runtime\n+# SADR-0005: Use Codex As Front And Claude Code Action As The GitHub Runtime\n \n ## Status\n \n-Accepted (implementer default superseded by ADR 0008; the auth model and the\n+Accepted (implementer default superseded by SADR-0008; the auth model and the\n Claude Code Action GitHub runtime decision remain in force)\n \n ## Context\n@@ -26,4 +26,4 @@\n - Shiki avoids requiring `OPENAI_API_KEY` for the default workflow.\n - Codex work happens through the user's authenticated Codex front surface, not as a hidden backend Action.\n - Claude Code Action is configured for OAuth-token based subscription usage by default.\n-- API-key based automation remains possible only as an explicit target-repo extension with its own ADR.\n+- API-key based automation remains possible only as an explicit target-repo extension with its own target ADR.\ndiff --git a/docs/adr/0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md b/docs/adr/SADR-0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\nindex 4bcae9d1e52f0a37b8e9fbafa5cd14b6ee1993bd..7a38538a6ae9929567f0ade67e7ea6a60d0b3f7e\n--- a/docs/adr/0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\n+++ b/docs/adr/SADR-0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\n@@ -1,4 +1,4 @@\n-# ADR 0006: Separate Codex Implementation, CCA Completion, And MergeGate Authorization\n+# SADR-0006: Separate Codex Implementation, CCA Completion, And MergeGate Authorization\n \n ## Status\n \ndiff --git a/docs/adr/0007-packaging-and-release.md b/docs/adr/SADR-0007-packaging-and-release.md\nindex 79b84ddb97167372b44641e5b7860e400ce80822..bb445a2e2e856d9d1f5fdbe35de8d4e8cec0fb32\n--- a/docs/adr/0007-packaging-and-release.md\n+++ b/docs/adr/SADR-0007-packaging-and-release.md\n@@ -1,4 +1,4 @@\n-# ADR 0007: Packaging And Release For Shiki\n+# SADR-0007: Packaging And Release For Shiki\n \n ## Status\n \n@@ -25,7 +25,7 @@\n   not introduce third-party runtime dependencies or break the shim.\n - Releases must run inside the operator's subscription-authenticated toolchain\n   and must not require extra secrets beyond the built-in `GITHUB_TOKEN`\n-  (consistent with ADR 0005's no-API-key-by-default stance).\n+  (consistent with SADR-0005's no-API-key-by-default stance).\n - `.shiki/manifest.json` already encodes the install surface (`install.include`,\n   `install.create_directories`, `install.exclude_from_commit`), and it must not\n   be mutated as part of this change.\n@@ -34,7 +34,7 @@\n \n This is a hard-to-reverse platform decision (it sets the version source of\n truth, the release trigger, and the public install contract), so it is recorded\n-as an ADR.\n+as a SADR.\n \n ## Decision\n \n@@ -104,7 +104,7 @@\n 3. **Publishing to PyPI.** Rejected for now: Shiki is distributed as a\n    repository template + git-installable CLI, not a PyPI library; PyPI would add\n    credentials/secrets and a different versioning contract. Can be added later as\n-   its own ADR if a packaged distribution is needed.\n+   its own SADR if a packaged distribution is needed.\n 4. **Dynamic version (read `VERSION` via a build backend `dynamic` field).**\n    Rejected to keep `pyproject.toml` backend-agnostic and dependency-free; the\n    small duplication between `VERSION` and `pyproject.toml` is guarded by the\n@@ -123,4 +123,4 @@\n   releasing versions the whole platform but ships only `install.include` to\n   targets.\n - Future PyPI distribution or a console entry point are deferred and would each\n-  warrant their own ADR.\n+  warrant their own SADR.\ndiff --git a/docs/adr/0008-use-claude-code-as-default-implementer.md b/docs/adr/SADR-0008-use-claude-code-as-default-implementer.md\nindex dedf4510b7c7799521422f622029563d373db031..35c2625d908167fbc58855b5b63e283805f167e8\n--- a/docs/adr/0008-use-claude-code-as-default-implementer.md\n+++ b/docs/adr/SADR-0008-use-claude-code-as-default-implementer.md\n@@ -1,12 +1,12 @@\n-# ADR 0008: Use Claude Code As The Default Implementer Runtime\n+# SADR-0008: Use Claude Code As The Default Implementer Runtime\n \n ## Status\n \n-Proposed (supersedes ADR 0005 for the implementer default; keeps its auth model)\n+Proposed (supersedes SADR-0005 for the implementer default; keeps its auth model)\n \n ## Context\n \n-ADR 0005 made Codex Front the default implementation runtime, with Claude Code\n+SADR-0005 made Codex Front the default implementation runtime, with Claude Code\n restricted to planner and reviewer roles by the runtime registry, and the only\n autonomous implementation adapter was `shiki runner codex`.\n \n@@ -18,13 +18,13 @@\n - The platform's planning, review, and CCA layers already run on Claude Code,\n   and Claude Code's native capabilities (the built-in `/code-review` skill and\n   the Workflow multi-agent orchestration tool) have no Codex equivalent.\n-- The Goal lifecycle redesign (ADR 0009) requires an autonomous post-freeze\n+- The Goal lifecycle redesign (SADR-0009) requires an autonomous post-freeze\n   loop driven from the operator's Claude Code session, including bounded\n   repair dispatch, which must not depend on a runtime that cannot be invoked\n   unattended.\n \n The constitution claims Shiki is runtime-agnostic (\"not a Claude-only\n-workflow\", ADR 0002). Any change to the implementer default must preserve the\n+workflow\", SADR-0002). Any change to the implementer default must preserve the\n registry-based runtime model rather than hardcoding one vendor.\n \n ## Decision\n@@ -39,7 +39,7 @@\n   the task to review or repair-needed.\n - Keep Codex registered as an optional implementer runtime. `shiki runner\n   codex`, the Codex handoff templates, and the ChatGPT OAuth auth model from\n-  ADR 0005 remain supported for tasks explicitly assigned to `codex`.\n+  SADR-0005 remain supported for tasks explicitly assigned to `codex`.\n - Commit to mandating two Claude-native capabilities as loop contract in\n   follow-up Goals (status: the code-review gate was delivered by Goal G-B \u2014\n   skill registry, checklists item PR-12, default required_skills \u2014 while the\n@@ -60,7 +60,7 @@\n \n Out of scope: removing Codex support, changing the CCA runtime (Claude Code\n Action with `CLAUDE_CODE_OAUTH_TOKEN` stays), and API-key based automation\n-(still requires its own ADR per ADR 0005).\n+(still requires its own target ADR per SADR-0005).\n \n ## Consequences\n \n@@ -100,7 +100,7 @@\n   harness rejection of direct `codex exec` is outside Shiki's control, and the\n   plan's mandatory native capabilities cannot run on Codex at all.\n - **Remove Codex entirely**: rejected; it abandons the runtime-agnostic\n-  constitution (ADR 0002) for no operational gain and forecloses\n+  constitution (SADR-0002) for no operational gain and forecloses\n   cross-runtime adversarial review.\n - **GitHub Actions cloud implementation via claude-code-action**: rejected as\n   default; durable evidence is automatic but token cost, Node 24 workflow\ndiff --git a/docs/adr/0009-requirements-definition-and-spec-freeze.md b/docs/adr/SADR-0009-requirements-definition-and-spec-freeze.md\nindex 29b3f7871bef9203a8b4a831a65c39eb2f506e42..52e7dd69ad0aa4efe0b68c62678abbf7df02aaec\n--- a/docs/adr/0009-requirements-definition-and-spec-freeze.md\n+++ b/docs/adr/SADR-0009-requirements-definition-and-spec-freeze.md\n@@ -1,4 +1,4 @@\n-# ADR 0009: Merge Grill And Planning Into Requirements Definition With Spec Freeze\n+# SADR-0009: Merge Grill And Planning Into Requirements Definition With Spec Freeze\n \n ## Status\n \ndiff --git a/docs/adr/0010-external-ai-guardian-review.md b/docs/adr/SADR-0010-external-ai-guardian-review.md\nindex e6108bbe8bb4e73e0ff4684c8f78001a80438c4e..f3510b145664298adafb151b6f0db51b972f01ec\n--- a/docs/adr/0010-external-ai-guardian-review.md\n+++ b/docs/adr/SADR-0010-external-ai-guardian-review.md\n@@ -1,4 +1,4 @@\n-# ADR 0010: External AI Guardian Review As A First-Class Approval Authority\n+# SADR-0010: External AI Guardian Review As A First-Class Approval Authority\n \n ## Status\n \n@@ -8,7 +8,7 @@\n \n Shiki is intended to be an autonomous, recursive, self-improving agent\n framework: authority-in-the-loop, not human-in-the-loop. Until now the Guardian\n-approval required for high/critical-risk merges (guardian-policy.json, ADR\n+approval required for high/critical-risk merges (guardian-policy.json, SADR\n governance) recognized only human authority sources \u2014 a GitHub review approval,\n the `guardian:approved` label applied by a configured Guardian, or a comment\n carrying `Guardian approval granted` plus the current head SHA.\n@@ -89,7 +89,7 @@\n   autonomous design the operator intends and produced the impersonation\n   incident.\n - **Record AI approval as an operator comment**: rejected as forgery of human\n-  authority \u2014 the exact failure this ADR corrects.\n+  authority \u2014 the exact failure this SADR corrects.\n - **Deliver the artifact as a committed `.shiki/` file**: rejected; a committed\n   approval inside the PR being approved is forgeable by anyone who can push.\n - **A dedicated reviewer bot identity instead of a relayed comment**: deferred;\ndiff --git a/docs/adr/0011-autonomous-quality-gate-evidence.md b/docs/adr/SADR-0011-autonomous-quality-gate-evidence.md\nindex bca0246c5d46e44afbde15486a75eec425fc02f5..9c93eac16d8c3a6d4b11bd7224e3955102cd7db4\n--- a/docs/adr/0011-autonomous-quality-gate-evidence.md\n+++ b/docs/adr/SADR-0011-autonomous-quality-gate-evidence.md\n@@ -1,4 +1,4 @@\n-# ADR 0011: Autonomous Quality-Gate Evidence \u2014 Deterministic Facts And An Independent Verifier\n+# SADR-0011: Autonomous Quality-Gate Evidence \u2014 Deterministic Facts And An Independent Verifier\n \n ## Status\n \ndiff --git a/docs/adr/0012-autonomous-loop-closeout-to-main.md b/docs/adr/SADR-0012-autonomous-loop-closeout-to-main.md\nindex 637fad6dde4989c9c2bf0f81f676d8d29991123c..ec7d85f81030a676bcbba28b43eb365422a36eac\n--- a/docs/adr/0012-autonomous-loop-closeout-to-main.md\n+++ b/docs/adr/SADR-0012-autonomous-loop-closeout-to-main.md\n@@ -1,4 +1,4 @@\n-# 0012 \u2014 Autonomous loop pushes goal completion to main via a closeout PR\n+# SADR-0012 \u2014 Autonomous loop pushes goal completion to main via a closeout PR\n \n - Status: accepted\n - Date: 2026-06-18\ndiff --git a/docs/adr/0013-bootstrap-grants-actions-approve-pull-requests.md b/docs/adr/SADR-0013-bootstrap-grants-actions-approve-pull-requests.md\nindex b6c69d64f1c5ced346b88d6859586715b3b5bd0e..cf48ff4a6716267f539ca58edd1a852e7c7a2d64\n--- a/docs/adr/0013-bootstrap-grants-actions-approve-pull-requests.md\n+++ b/docs/adr/SADR-0013-bootstrap-grants-actions-approve-pull-requests.md\n@@ -1,4 +1,4 @@\n-# ADR 0013: Bootstrap Grants GitHub Actions Permission To Create And Approve Pull Requests\n+# SADR-0013: Bootstrap Grants GitHub Actions Permission To Create And Approve Pull Requests\n \n ## Status\n \n@@ -31,7 +31,7 @@\n security tradeoff: it lets repository automation satisfy a human-review gate. In\n a multi-maintainer repo that could let a compromised or buggy workflow\n self-approve changes. So whether bootstrap turns it on by default \u2014 and with\n-what default token scope \u2014 is an ADR-worthy decision, not an implementation\n+what default token scope \u2014 is a SADR-worthy decision, not an implementation\n detail.\n \n ## Decision\n@@ -48,7 +48,7 @@\n Specifically:\n \n - **`can_approve_pull_request_reviews` defaults to `true`.** Shiki's default\n-  posture is solo/AFK autonomous operation (ADR 0008/0009/0012), where the CCA\n+  posture is solo/AFK autonomous operation (SADR-0008/0009/0012), where the CCA\n   Review Bridge is the only path that can satisfy `required_review: true` after\n   the CCA and Guardian gates have already passed. The default makes a\n   freshly-bootstrapped repo able to run the autonomous loop end to end without a\ndiff --git a/docs/adr/0014-codex-app-external-ai-guardian-ui-adapter.md b/docs/adr/SADR-0014-codex-app-external-ai-guardian-ui-adapter.md\nindex 238db06279334caa341c117362df9271382a7fcd..65f12e9939321e1b3e389a34c822fc1549e5155d\n--- a/docs/adr/0014-codex-app-external-ai-guardian-ui-adapter.md\n+++ b/docs/adr/SADR-0014-codex-app-external-ai-guardian-ui-adapter.md\n@@ -1,4 +1,4 @@\n-# ADR 0014: Use Codex App As The External AI Guardian UI Adapter\n+# SADR-0014: Use Codex App As The External AI Guardian UI Adapter\n \n ## Status\n \ndiff --git a/docs/adr/0015-contract-approval-before-dispatch.md b/docs/adr/SADR-0015-contract-approval-before-dispatch.md\nindex fad6c656741a20a7684ba16080a7db159f63a52f..6061271a46d3f1d6ae7174477de3fb75673123a6\n--- a/docs/adr/0015-contract-approval-before-dispatch.md\n+++ b/docs/adr/SADR-0015-contract-approval-before-dispatch.md\n@@ -1,4 +1,4 @@\n-# ADR 0015: Approve The Task Contract Before Dispatch, Not The Diff After It\n+# SADR-0015: Approve The Task Contract Before Dispatch, Not The Diff After It\n \n ## Status\n \n@@ -9,7 +9,7 @@\n Guardian approval for high/critical risk is expressible only on a pull request.\n Every approval source in `guardian-policy.json` \u2014 GitHub review, the\n `guardian:approved` label, the `Guardian approval granted` comment, and the\n-`external_ai_guardian_review` artifact (ADR 0010/0014) \u2014 is bound to a PR, and\n+`external_ai_guardian_review` artifact (SADR-0010/0014) \u2014 is bound to a PR, and\n three of the four require the current head SHA. Approval therefore cannot exist\n before an implementation PR exists.\n \n@@ -81,7 +81,7 @@\n An envelope breach must surface only on the non-repairable policy gate, never on\n the repairable `MergeGate metadata check`. Otherwise the Goal Loop routes it to\n `dispatch_repair` \u2014 instructing a runner to make the breach go away \u2014 which is\n-exactly what this ADR forbids.\n+exactly what this SADR forbids.\n \n **Contract immutability.** After registration, an implementation PR must not\n change the governance fields of its own task contract (`scope`, `non_goals`,\n@@ -116,7 +116,7 @@\n fails any verdict that is not `complete`, and the signal the CCA prompt reads is\n computed from `evaluate_guardian_approval` alone. Without the OR-merge at the\n signal, a contract-approved PR still returns `needs_guardian`, the CCA job fails,\n-and the MergeGate policy check never runs \u2014 the gate this ADR changes would never\n+and the MergeGate policy check never runs \u2014 the gate this SADR changes would never\n be reached. The CCA *prompt* needs no change: a `contract_approval` entry in the\n signal's existing `sources` array satisfies its current wording.\n \n@@ -130,7 +130,7 @@\n written, and an approved contract lets its implementation PRs merge through the\n same automatic path as low/medium risk. The wasted implement-then-block cycle\n disappears. Guardian review shifts from reading diffs to reading contracts, which\n-is both earlier and smaller; the external AI Guardian path (ADR 0010/0014) works\n+is both earlier and smaller; the external AI Guardian path (SADR-0010/0014) works\n on contracts with no change.\n \n We accept that the Guardian authorizes a class of change without seeing its\n@@ -146,7 +146,7 @@\n adversarial tests for the new approval path equivalent in rigor to\n `test_shiki_governance_evidence.sh`.\n \n-Bootstrap costs, which apply only while this ADR is being implemented. The tasks\n+Bootstrap costs, which apply only while this SADR is being implemented. The tasks\n that build Contract Approval merge under the existing rules, so the ones\n dispatched after the pre-dispatch gate lands but before the approval path lands\n need a Guardian-approved Contract PR *and* a live Guardian approval on their\n@@ -210,7 +210,7 @@\n risk; they do not bound authorized work.\n \n **Auto-escalate risk on envelope breach instead of stopping.** Returns the\n-workflow to a diff-time Guardian decision \u2014 the exact behavior this ADR removes.\n+workflow to a diff-time Guardian decision \u2014 the exact behavior this SADR removes.\n \n **Resolve governance fields from the base snapshot only.** Rejected against the\n registration flow: implementation PRs add their own contract, so the base has no\ndiff --git a/docs/adr/0016-locks-bound-product-paths.md b/docs/adr/SADR-0016-locks-bound-product-paths.md\nindex b5e850f3f1f7f4ef6e7f9d1763a29a51bd300c45..46b40a9b99a7aae644c02bb458f5473ff0a90ce2\n--- a/docs/adr/0016-locks-bound-product-paths.md\n+++ b/docs/adr/SADR-0016-locks-bound-product-paths.md\n@@ -1,4 +1,4 @@\n-# ADR 0016: Locks bound product paths; the Shiki mirror is bound by record identity\n+# SADR-0016: Locks bound product paths; the Shiki mirror is bound by record identity\n \n ## Status\n \n@@ -6,8 +6,8 @@\n \n - Date: 2026-07-28\n - Deciders: operator (mizutani-140), Claude (planner)\n-- Related: ADR 0004 (GitHub-first with a `.shiki` mirror), ADR 0009 (Spec Freeze),\n-  ADR 0012 (autonomous loop closeout to main), ADR 0015 (Contract Approval before\n+- Related: SADR-0004 (GitHub-first with a `.shiki` mirror), SADR-0009 (Spec Freeze),\n+  SADR-0012 (autonomous loop closeout to main), SADR-0015 (Contract Approval before\n   dispatch), remediation goal task R-02, the live PR #179 deadlock.\n \n ## Context\n@@ -90,11 +90,11 @@\n files, documentation files and named source modules are nameable; runtime-\n allocated ledger, runner and report ids are not \u2014 and those live in `.shiki`,\n which locks no longer govern. Existing frozen contracts are brought into line by\n-a one-time Spec Amendment per plan, which must land before ADR 0015's contract\n+a one-time Spec Amendment per plan, which must land before SADR-0015's contract\n immutability makes `locks` unwritable from the implementation PR.\n \n **Lock state is resolved from the durable side.** MergeGate resolves the lock\n-registry, and ADR 0015's governance fields, from the base `.shiki` snapshot when\n+registry, and SADR-0015's governance fields, from the base `.shiki` snapshot when\n present, falling back to head only when absent. The `MergeGate metadata check`\n job is given `--base-shiki`, which it does not receive today. Locks are not\n derived from the live open-pull-request set: a pull request body is untrusted\n@@ -108,7 +108,7 @@\n Explicitly out of scope: moving MergeGate's contract resolution off the PR\n checkout wholesale; making the mirror a projection of GitHub state; introducing\n new lock kinds; moving ledger evidence out of the repository; and unwinding\n-ADR 0012's closeout pull request. Registration of multi-task goals is ADR 0015's\n+SADR-0012's closeout pull request. Registration of multi-task goals is SADR-0015's\n Contract PR and is not re-decided here.\n \n ## Consequences\n@@ -127,7 +127,7 @@\n \n Risks accepted. First, goal-scoped identity is only as strong as `goal_id`,\n which is resolved from the PR body and the head task file; the mitigation is to\n-add `goal_id` to ADR 0015's frozen governance field set, and until that lands a\n+add `goal_id` to SADR-0015's frozen governance field set, and until that lands a\n task file can name a foreign goal. Second, base-when-present resolution is\n unavailable on fork pull requests, where the head is contributor-controlled;\n fork pull requests already cannot complete a Shiki merge, and this decision does\n@@ -139,7 +139,7 @@\n path, not by implementation pull requests.\n \n New obligations: one Spec Amendment per frozen plan to rewrite `locks`, ordered\n-before ADR 0015 T1; extension of the adversarial suites in\n+before SADR-0015 T1; extension of the adversarial suites in\n `scripts/test_shiki_adversarial_state.sh` and\n `scripts/test_shiki_governance_evidence.sh` to cover the eleven newly-scoped\n directories; and updating `scripts/test_shiki_loop_lock_guard.sh`,\n@@ -151,32 +151,32 @@\n whether the loop's transitive sync of ledger `evidence` references can be\n deleted once the Contract PR carries its own source plan.\n \n-## Relationship to ADR 0004\n+## Relationship to SADR-0004\n \n-This ADR **clarifies something ADR 0004 left unspecified**. ADR 0004 decides that\n+This SADR **clarifies something SADR-0004 left unspecified**. SADR-0004 decides that\n GitHub is the operational source of truth and that each target repository keeps a\n `.shiki/` mirror. It says nothing about pull requests writing the mirror, does\n not name MergeGate, and does not make the mirror an input to a gate. Nothing in\n-ADR 0004's Decision or Consequences is amended, superseded or weakened: the\n+SADR-0004's Decision or Consequences is amended, superseded or weakened: the\n mirror still exists, still records the same artifacts, and pull request\n-boundaries remain the coordination surface. What this ADR settles is the question\n+boundaries remain the coordination surface. What this SADR settles is the question\n 0004 never reached \u2014 by what authority a pull request may write a mirror record \u2014\n and it answers it the way `.shiki/manifest.json` already declares\n (`pr_mutation: current-task-or-current-goal-only`).\n \n-It **amends ADR 0012**, whose Decision item 5 states that \"Loop-executed tasks\n+It **amends SADR-0012**, whose Decision item 5 states that \"Loop-executed tasks\n declare the `path:.shiki/**` lock, which covers every `.shiki` file the closeout\n stages ... so `files_outside_locks` is satisfied without per-file lock\n bookkeeping.\" That clause is replaced: the closeout's mirror writes are\n-authorized by record identity, not by a declared lock. ADR 0012's substantive\n+authorized by record identity, not by a declared lock. SADR-0012's substantive\n decision \u2014 that completion reaches `main` through a closeout pull request rather\n than local attestation \u2014 is preserved unchanged.\n \n-It is **complementary to ADR 0015**, not a substitute. ADR 0015 owns contract\n+It is **complementary to SADR-0015**, not a substitute. SADR-0015 owns contract\n immutability, the pre-dispatch gate, the Contract PR and the Scope Envelope. This\n-ADR owns what a lock means and who may write the mirror. The one ordering\n+SADR owns what a lock means and who may write the mirror. The one ordering\n constraint between them is that the lock-narrowing Spec Amendment must precede\n-ADR 0015 T1's freezing of the `locks` field.\n+SADR-0015 T1's freezing of the `locks` field.\n \n ## Alternatives Considered\n \n@@ -186,7 +186,7 @@\n the only writer that can register them may run. Migrating the Guardian signal to\n that store makes every registration PR fail closed to `needs_guardian` at every\n risk level, because the task file is by definition absent from the store, which\n-removes the low/medium autonomous path ADR 0008/0009 exists to provide. And it\n+removes the low/medium autonomous path SADR-0008/0009 exists to provide. And it\n leaves schemas, manifest, config, guardian policy, templates, migrations, plans,\n worktrees, runner records, runs and handoffs with no writer at all in a\n repository whose product is `.shiki`. Measured benefit on the frozen backlog:\n@@ -199,14 +199,14 @@\n namespace is not in fact ownership-partitioned \u2014 56 ledger entries carry no\n `task_id`, and one goal DAG is writable by 35 registered tasks. The correct\n expression of its insight is that locks do not govern the mirror at all, which is\n-what this ADR decides.\n+what this SADR decides.\n \n **Make the mirror a projection written only by a registration writer and a\n post-merge projection job.** Rejected. It requires a bypass actor with write\n access to the protected default branch; it makes its own master invariant\n readable from a manifest that pull requests may edit; it deletes both reconcile\n modes and leaves a corrupted mirror with no in-band repair path, contradicting\n-ADR 0004's consequence that mirror conflicts are repaired against GitHub state;\n+SADR-0004's consequence that mirror conflicts are repaired against GitHub state;\n and its content-derived ids are rejected by `validate_shiki`'s currently-green id\n pattern.\n \ndiff --git a/docs/adr/0017-bookkeeping-closeouts-do-not-inherit-risk.md b/docs/adr/SADR-0017-bookkeeping-closeouts-do-not-inherit-risk.md\nindex 554e3a0d526a72ca067108c5464cc68c3edcf174..b9676857a4713850e063af7c39c8e0b01ecb4c66\n--- a/docs/adr/0017-bookkeeping-closeouts-do-not-inherit-risk.md\n+++ b/docs/adr/SADR-0017-bookkeeping-closeouts-do-not-inherit-risk.md\n@@ -1,4 +1,4 @@\n-# ADR 0017: A Bookkeeping Closeout Does Not Inherit Its Task's Risk\n+# SADR-0017: A Bookkeeping Closeout Does Not Inherit Its Task's Risk\n \n ## Status\n \n@@ -6,7 +6,7 @@\n \n ## Context\n \n-ADR 0012 made the goal loop push a task's completion to the default branch through a\n+SADR-0012 made the goal loop push a task's completion to the default branch through a\n closeout pull request: after the implementation PR merges, the loop opens a second PR\n carrying `task.status = done`, `lock.state = released`, and \u2014 when the task completes\n its goal \u2014 `goal.status = complete` plus one scorecard report. Nothing else. The\n@@ -49,14 +49,14 @@\n 4. The task file's governance fields (`scope`, `non_goals`, `required_skills`,\n    `risk_level`, `locks`, `acceptance_checks`, `test_command`) are byte-identical to\n    the base snapshot. Only `status`, `expected_pr`, `closeout_pr`, `ledger_evidence`\n-   and `expected_branch` may differ \u2014 the ADR 0015 contract-immutability rule,\n+   and `expected_branch` may differ \u2014 the SADR-0015 contract-immutability rule,\n    unchanged.\n \n    `expected_branch` is in that set because a closeout is *by construction* on a\n    different branch from the implementation it closes, and the loop rewrites the\n    field accordingly. It is safe to let it differ because MergeGate independently\n    requires `expected_branch` to equal the PR's actual head ref, so a PR cannot set\n-   it to anything other than the branch it is really on. The first draft of this ADR\n+   it to anything other than the branch it is really on. The first draft of this SADR\n    omitted it, which made the exemption unreachable: every real closeout failed\n    condition 4 on a field the loop always changes. See PR #209.\n 5. The state transitions are exactly the terminal set: task `review` \u2192 `done`, lock\n@@ -113,12 +113,12 @@\n classification must be derived, not asserted.\n \n **Fold the closeout into the implementation PR** so no second PR exists. Rejected under\n-ADR 0012, which recorded why: a task file that is already `done` on the implementation\n+SADR-0012, which recorded why: a task file that is already `done` on the implementation\n branch short-circuits the loop's review \u2192 CCA \u2192 merge \u2192 done ordering into a local\n goal-completion before any merge happens.\n \n **Have the loop auto-merge closeouts with elevated credentials.** Rejected outright: that\n-is a runtime authoring its own approval, which ADR 0010 and the constitution's\n+is a runtime authoring its own approval, which SADR-0010 and the constitution's\n approval-record prohibition forbid. It would also be indistinguishable, in the evidence,\n from the forgery those rules exist to prevent.\n \ndiff --git a/docs/adr/0018-carried-guardian-approval-across-a-proven-base-sync.md b/docs/adr/SADR-0018-carried-guardian-approval-across-a-proven-base-sync.md\nindex 22358562da390ed8b45cd600da08a43d378b4680..e6e2907cefa776460af73ddf7d39db605f3c39b0\n--- a/docs/adr/0018-carried-guardian-approval-across-a-proven-base-sync.md\n+++ b/docs/adr/SADR-0018-carried-guardian-approval-across-a-proven-base-sync.md\n@@ -1,4 +1,4 @@\n-# ADR 0018: Carried Guardian Approval Across A Proven Base Sync\n+# SADR-0018: Carried Guardian Approval Across A Proven Base Sync\n \n ## Status\n \n@@ -34,7 +34,7 @@\n Guardian evaluator that consumes it (`shiki_guardian.evaluate_guardian_approval`,\n the `guardian_comment_carried` source) were built and tested in isolation. Neither\n does anything until a caller passes the flags that turn the carry on, so the\n-feature was invisible in CI. This ADR records the decision the two of them\n+feature was invisible in CI. This SADR records the decision the two of them\n implement, and this task wires it into the four CI invocations that judge Guardian\n approval.\n \ndiff --git a/docs/adr/README.md b/docs/adr/README.md\nindex c70e057a4520c36e042ab68c53cd4a21ef709b71..5da43856836e2ce6a150f8127766e2f88355ab4b\n--- a/docs/adr/README.md\n+++ b/docs/adr/README.md\n@@ -1,40 +1,41 @@\n-# Architecture Decision Records\n+# Decision Record Namespaces\n \n-This directory holds Shiki's Architecture Decision Records (ADRs): durable\n-records of hard-to-reverse platform decisions. ADRs are part of the Shiki source\n-of truth alongside GitHub state, the `.shiki/` mirror, and `CONTEXT.md`.\n+This directory holds two independent decision namespaces:\n \n-## How ADRs Work\n+- Shiki Architecture Decision Records (SADRs), named\n+  `SADR-NNNN-<short-slug>.md`, record hard-to-reverse Shiki platform decisions.\n+- Target Architecture Decision Records (ADRs), named\n+  `NNNN-<short-slug>.md`, belong to the target product.\n \n-- New ADRs are added as `docs/adr/NNNN-<short-slug>.md`, where `NNNN` is the next\n-  zero-padded sequence number.\n-- The **canonical list of ADRs is the set of files matching `docs/adr/*.md`** in\n-  this directory. Browse `docs/adr/*.md` for the authoritative, current set.\n-- Use [`template.md`](template.md) as the starting point for a new ADR.\n-- Record an ADR when a decision is hard to reverse, surprising without context,\n-  and a real tradeoff. Do not write an ADR for routine or easily reversible\n-  choices.\n+The same four-digit number may appear once in each namespace. A platform\n+`SADR-NNNN` citation resolves only to `SADR-NNNN-*.md`; a target `ADR NNNN`\n+citation resolves only to `NNNN-*.md`.\n \n-Using the `docs/adr/*.md` glob as the source of truth means other PRs can add an\n-ADR without editing this index file, avoiding merge contention on a\n-hand-maintained enumeration.\n+## How Decision Records Work\n \n-## Current ADRs (snapshot)\n-\n-The list below is a convenience snapshot and may lag behind the directory. When\n-in doubt, list `docs/adr/*.md`.\n+- New Shiki platform decisions use the next zero-padded SADR number.\n+- New target product decisions use the target's own ADR sequence.\n+- The canonical list is the files in `docs/adr/`; no hand-maintained index is\n+  authoritative.\n+- Use [`template.md`](template.md) as the starting point and choose the filename\n+  namespace that owns the decision.\n+- Record a decision when it is hard to reverse, surprising without context, and\n+  a real tradeoff. Do not write a decision record for routine or easily\n+  reversible choices.\n \n-- [0001 \u2014 Keep Shiki As The Platform Name](0001-keep-shiki-as-platform-name.md)\n-- [0002 \u2014 Use AGENTS As The Shared Agent Constitution](0002-use-agents-as-shared-constitution.md)\n-- [0003 \u2014 Require Skill Gate For MergeGate Readiness](0003-require-skill-gate-for-mergegate-readiness.md)\n-- [0004 \u2014 Use GitHub First With A Repository-Local Shiki Mirror](0004-use-github-first-with-shiki-mirror.md)\n-- [0005 \u2014 Use Codex As Front And Claude Code Action As The GitHub Runtime](0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md)\n-- [0006 \u2014 Separate Codex Implementation, CCA Completion, And MergeGate Authorization](0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md)\n+Using the directory as the source of truth lets independent changes add records\n+without contending on an enumerated index.\n \n-## Adding A New ADR\n+## Adding A Shiki SADR\n \n-1. Copy `template.md` to `docs/adr/NNNN-<short-slug>.md`.\n+1. Copy `template.md` to `docs/adr/SADR-NNNN-<short-slug>.md`.\n 2. Fill in the title, status, context, decision, and consequences.\n-3. Set the status to `Proposed`, then `Accepted` once the decision is approved.\n-4. Open a PR following `CONTRIBUTING.md`. You do not need to edit this index for\n-   the ADR to be discoverable via `docs/adr/*.md`.\n+3. Set the status to `Proposed`, then `Accepted` once the proper authority has\n+   approved the decision.\n+4. Open a PR following `CONTRIBUTING.md`.\n+\n+## Adding A Target ADR\n+\n+1. Copy `template.md` to `docs/adr/NNNN-<short-slug>.md` using the target's own\n+   sequence.\n+2. Cite it as `ADR NNNN`; do not prefix its filename with `SADR-`.\ndiff --git a/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md b/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\nnew file mode 100644\nindex 0000000000000000000000000000000000000000..7455ce1f3bed02ddddb851ac21a1d1a648c9aeba\n--- /dev/null\n+++ b/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\n@@ -0,0 +1,55 @@\n+# SADR-0019: Separate Shiki Platform SADRs From Target ADRs\n+\n+## Status\n+\n+Accepted\n+\n+## Context\n+\n+Shiki installs its platform decision records into `docs/adr/` in every target\n+repository. Shiki and its targets previously used the same numeric\n+`NNNN-<slug>.md` namespace. A target that already owned ADRs 0013 through 0018\n+therefore acquired two records for each number during installation, making an\n+`ADR NNNN` citation ambiguous. Renumbering target decisions would rewrite target\n+history, while renumbering Shiki decisions per target would destroy their stable\n+platform identity.\n+\n+Upgrades also need to remove the former Shiki-owned numeric paths without ever\n+mistaking a target-owned ADR for platform content. The install stamp already\n+records exact shipped paths and content digests, so it can prove ownership only\n+when both still match.\n+\n+## Decision\n+\n+Shiki platform decisions use the `SADR` namespace:\n+\n+- records are named `docs/adr/SADR-NNNN-<slug>.md`;\n+- live platform citations use `SADR-NNNN`;\n+- target product decisions retain `docs/adr/NNNN-<slug>.md` and `ADR NNNN`;\n+- the reference validator resolves the two namespaces independently, so the same\n+  four-digit number may exist once in each namespace.\n+\n+Before a forced upgrade writes anything, the installer inventories the exact\n+former numeric Shiki ADR paths. It may delete a legacy path only when the\n+existing install stamp contains that exact path and its recorded SHA-256 digest\n+matches the file currently on disk. All ownership blockers are reported\n+together and abort the upgrade before its first write. A non-forced install\n+never performs this cleanup.\n+\n+Historical `.shiki/` evidence remains unchanged because it records the namespace\n+that existed when that evidence was produced.\n+\n+## Consequences\n+\n+- Targets may use any ADR number without colliding with Shiki's platform SADRs.\n+- Fresh installs preserve target ADR bytes and install a stable SADR set.\n+- Forced upgrades fail closed when legacy ownership cannot be proven, and a\n+  successful upgrade refreshes the stamp without removed legacy paths.\n+- Current governance, packaging, workflow, prompt, script, and skill references\n+  must move with the platform records.\n+\n+## Alternatives Considered\n+\n+- Collision-only refusal leaves affected targets permanently unupgradable.\n+- Renumbering target ADRs mutates target-owned history.\n+- Per-target Shiki renumbering makes platform citations non-portable.\ndiff --git a/docs/adr/template.md b/docs/adr/template.md\nindex a92a97daddd0b00ab9dd626cc8d86942da2eafed..17dd79175b5fee2105935557f691b40cf9492942\n--- a/docs/adr/template.md\n+++ b/docs/adr/template.md\n@@ -1,14 +1,14 @@\n-# NNNN: <Short Decision Title>\n+# <SADR-NNNN or NNNN>: <Short Decision Title>\n \n ## Status\n \n-Proposed | Accepted | Superseded by ADR-NNNN\n+Proposed | Accepted | Superseded by SADR-NNNN or target ADR NNNN\n \n ## Context\n \n What is the situation, constraint, or force that requires a decision? Reference\n-relevant domain terms (`CONTEXT.md`), prior ADRs, and code reality. State the\n-problem, not the solution.\n+relevant domain terms (`CONTEXT.md`), prior Shiki SADRs or target ADRs, and code\n+reality. State the problem, not the solution.\n \n ## Decision\n \ndiff --git a/docs/agents/autonomous-loop-e2e-acceptance.md b/docs/agents/autonomous-loop-e2e-acceptance.md\nindex bbf84c6081987f159ac6ad9ecf3c82b7aa72e7e8..5c13c1d02a05a90b0dc46cbef7c95a9846d0c237\n--- a/docs/agents/autonomous-loop-e2e-acceptance.md\n+++ b/docs/agents/autonomous-loop-e2e-acceptance.md\n@@ -40,8 +40,8 @@\n 3. `gh auth status` is logged in with merge permission on the repo.\n 4. Branch protection + required checks are configured exactly as the goal-loop\n    required-check set expects (see `docs/agents/branch-protection-smoke.md`).\n-5. The repo `.shiki/policy` permits autonomous low/medium-risk merges (ADR\n-   0008/0009); the MergeGate policy check is the recorded-authority gate.\n+5. The repo `.shiki/policy` permits autonomous low/medium-risk merges\n+   (SADR-0008/0009); the MergeGate policy check is the recorded-authority gate.\n \n The operator's only actions are: register the task and start the loop. **No\n manual pre-clearing** means after `shiki loop run` starts, the operator does not\ndiff --git a/docs/agents/bootstrap-command.md b/docs/agents/bootstrap-command.md\nindex 05f7f203a52cf412b9f97084a6e131ed0f785afd..f7aa0b734dfebee4a3b139335b66ee3415f48713\n--- a/docs/agents/bootstrap-command.md\n+++ b/docs/agents/bootstrap-command.md\n@@ -189,7 +189,9 @@\n ```\n \n Use this only for tests, fixtures, or explicit local-only template inspection.\n-Use `--force` only when you intentionally want to overwrite existing target files.\n+Use `--force` only when you intentionally want to refresh the target template.\n+Legacy numeric Shiki decision cleanup is stamp-and-digest authorized before any\n+write; target ADR paths are not cleanup candidates.\n \n ## Slash Command\n \ndiff --git a/docs/agents/checklists.md b/docs/agents/checklists.md\nindex 4a7f03ade346f53a1c1d82981f3fd744f9ab4547..0c5ec6c53dc7197b7b7a7d18e667b325babd6c45\n--- a/docs/agents/checklists.md\n+++ b/docs/agents/checklists.md\n@@ -32,18 +32,18 @@\n | ID | Item | Blocking |\n | --- | --- | --- |\n | GD-01 | Plan was challenged against `CONTEXT.md`. | blocking for non-trivial Goals |\n-| GD-02 | Relevant ADRs were read or absence was recorded. | blocking for non-trivial Goals |\n+| GD-02 | Relevant SADRs and target ADRs were read or absence was recorded. | blocking for non-trivial Goals |\n | GD-03 | Ambiguous terms were resolved or converted to HITL blockers. | blocking |\n | GD-04 | Edge scenarios were considered. | advisory |\n | GD-05 | Code reality was checked when code could answer. | blocking when applicable |\n | GD-06 | New domain terms were added to `CONTEXT.md` when needed. | blocking when applicable |\n-| GD-07 | ADR was created or explicitly skipped using the ADR criteria. | blocking when applicable |\n+| GD-07 | A SADR or target ADR was created or explicitly skipped using the decision-record criteria. | blocking when applicable |\n \n ## CI \u2014 Context & Impact Checklist\n \n | ID | Item | Blocking |\n | --- | --- | --- |\n-| CI-01 | Relevant docs and ADRs identified. | blocking |\n+| CI-01 | Relevant docs, SADRs, and target ADRs identified. | blocking |\n | CI-02 | Relevant modules, interfaces, seams, callers, and tests identified. | blocking |\n | CI-03 | Dependencies and blocked-by relationships identified. | blocking |\n | CI-04 | Candidate locks identified. | blocking |\n@@ -62,7 +62,7 @@\n | PRD-04 | Implementation decisions are recorded without volatile code detail. | blocking |\n | PRD-05 | Testing decisions are recorded. | blocking |\n | PRD-06 | Out-of-scope items are explicit. | blocking |\n-| PRD-07 | Relevant glossary/ADR links are present when applicable. | blocking |\n+| PRD-07 | Relevant glossary/SADR/target-ADR links are present when applicable. | blocking |\n \n ## SF \u2014 Spec Freeze Checklist\n \n@@ -98,7 +98,7 @@\n | H-02 | Branch/worktree target is present. | blocking |\n | H-03 | Scope and non-goals are present. | blocking |\n | H-04 | Dependencies and locks are present. | blocking |\n-| H-05 | Relevant docs, ADRs, modules, and tests are referenced. | blocking |\n+| H-05 | Relevant docs, SADRs, target ADRs, modules, and tests are referenced. | blocking |\n | H-06 | Required skills are stated. | blocking |\n | H-07 | TDD expectations are stated. | blocking when behavior work |\n | H-08 | Acceptance criteria are included. | blocking |\ndiff --git a/docs/agents/codex-handoff.md b/docs/agents/codex-handoff.md\nindex ede468f47cb277f69a857e8c45df703e4ed0151d..6f97bdffb67e44d7edea2c71e01ea28933c4eb6c\n--- a/docs/agents/codex-handoff.md\n+++ b/docs/agents/codex-handoff.md\n@@ -46,7 +46,7 @@\n \n ## Context\n - Domain terms:\n-- Relevant ADRs:\n+- Relevant SADRs and target ADRs:\n - Relevant docs:\n - Relevant modules/interfaces/seams:\n - Similar tests/prior art:\ndiff --git a/docs/agents/completion-check-agent.md b/docs/agents/completion-check-agent.md\nindex 13e4c035db35025f4e3cdcc5adfb24314ac4d521..61bfa1218cf36ca605250c91919e620f33238aa0\n--- a/docs/agents/completion-check-agent.md\n+++ b/docs/agents/completion-check-agent.md\n@@ -32,7 +32,7 @@\n - `AGENTS.md`\n - `CLAUDE.md`\n - `CONTEXT.md`\n-- relevant ADRs\n+- relevant SADRs and target ADRs\n - parent Goal issue\n - PRD issue or document\n - task issue\ndiff --git a/docs/agents/decision-control.md b/docs/agents/decision-control.md\nindex 7d8ee5ab03a5ccbcbd04056ae61de03785a2a9c9..8c94ecda00fffaa0e92c325ab83aa9776bd1dac1\n--- a/docs/agents/decision-control.md\n+++ b/docs/agents/decision-control.md\n@@ -11,7 +11,7 @@\n | Decision | Owner | When | Required inputs | Output | Enforcement surface |\n | --- | --- | --- | --- | --- | --- |\n | Is the Goal clear enough to plan? | Planner + Guardian when needed | Goal intake | User request, existing docs, risk signals | Goal readiness verdict | Goal checklist, issue template, ledger |\n-| Must `grill-with-docs` run? | Planner / Skill Gate | Before PRD or issue split | Ambiguity, domain terms, ADR conflicts, unclear boundaries | Skill invocation or explicit skip rationale | Skill Gate checklist |\n+| Must `grill-with-docs` run? | Planner / Skill Gate | Before PRD or issue split | Ambiguity, domain terms, SADR/target-ADR conflicts, unclear boundaries | Skill invocation or explicit skip rationale | Skill Gate checklist |\n | Is a PRD required? | Planner | After Goal context stabilizes | Goal, `grill-with-docs` output, Context & Impact output | PRD required / not required | PRD checklist, CCA evidence check |\n | Can the spec be frozen? | Operator only | End of Requirements Definition, before issue split | PRD/requirements summary, scope inventory (SF-02), open-question list | `spec_freeze` block in the plan + ledger evidence | `require_grilled_plan` (plan ingest/run), SF checklist |\n | Can a frozen spec change? | Operator only (Spec Amendment) | When implementation contradicts the frozen spec | Contested decisions, scoped re-grill result | recorded amendment + re-stamped freeze | SF checklist, ledger evidence |\ndiff --git a/docs/agents/domain.md b/docs/agents/domain.md\nindex 258da8ea81e87ac0a6b0b5b5e5c2fde980eaabb7..d57b0cf17ff3e1fa9e6e5885372bf083d8295421\n--- a/docs/agents/domain.md\n+++ b/docs/agents/domain.md\n@@ -5,7 +5,9 @@\n ## Layout\n \n - `CONTEXT.md` defines Shiki language. Keep it implementation-free.\n-- `docs/adr/` records hard-to-reverse decisions.\n+- `docs/adr/SADR-NNNN-*.md` records Shiki platform decisions.\n+- `docs/adr/NNNN-*.md` records target product decisions without sharing the\n+  SADR namespace.\n - `AGENTS.md` is the runtime-neutral agent constitution.\n - `CLAUDE.md` is the Claude Code wrapper around `AGENTS.md`.\n - `.shiki/` is the Target Repository mirror for Goals, PRDs, plans, Task DAGs, contracts, locks, worktree records, ledger entries, CCA verdicts, repair packets, reports, and handoffs.\n@@ -15,6 +17,6 @@\n \n - Use Shiki terms exactly when they exist.\n - Add glossary terms to `CONTEXT.md` only when the term is part of the domain, not a generic programming concept.\n-- Create ADRs only for hard-to-reverse, surprising, tradeoff-based decisions.\n+- Create SADRs or target ADRs only for hard-to-reverse, surprising, tradeoff-based decisions.\n - Keep product-specific language in the Target Repository, not in Shiki Template docs.\n-- If an ADR conflicts with a proposed implementation, surface the conflict before editing.\n+- If a SADR or target ADR conflicts with a proposed implementation, surface the conflict before editing.\ndiff --git a/docs/agents/execution-control.md b/docs/agents/execution-control.md\nindex ad8ee3cdb36be56fedc1f46408ca92b387d74047..bc63c3a73661d12b567016ba299f1a6b66bf9088\n--- a/docs/agents/execution-control.md\n+++ b/docs/agents/execution-control.md\n@@ -63,7 +63,7 @@\n | Branch name | Orchestrator | Orchestrator | Before implementer dispatch | branch policy | branch ref in task/ledger |\n | Lock reservation | Context & Impact | MergeGate | Before dispatch and before merge | `.shiki/locks/`, labels, PR metadata | lock id, owner, scope |\n | CI profile | Planner | MergeGate | During issue triage, rechecked at PR | labels, `.shiki/policy.yaml`, workflow matrix | required check list |\n-| Required status checks | Repo maintainer / Guardian | GitHub branch protection / rulesets | Repository setup, updated via ADR | GitHub settings | branch/ruleset config, ADR |\n+| Required status checks | Repo maintainer / Guardian | GitHub branch protection / rulesets | Repository setup, updated via target ADR | GitHub settings | branch/ruleset config, target ADR |\n | CCA judgment | CCA | GitHub required check / MergeGate | Every non-draft PR update | GitHub Action check | structured CCA verdict |\n | Repair assignment | CCA | MergeGate | After failed CCA/CI/review | repair packet, issue/PR comment | repair id and attempt count |\n | Merge readiness | MergeGate | GitHub branch protection / Guardian if needed | After CCA complete | required checks, review rules, rulesets | mergegate check output |\ndiff --git a/docs/agents/guardian-policy.md b/docs/agents/guardian-policy.md\nindex 4147c7adf05a47adb4fd24ada1a1c2e1f2ba6d2d..a20d9cc6c00a569bb057b9ca8d6b51c7fc4a9847\n--- a/docs/agents/guardian-policy.md\n+++ b/docs/agents/guardian-policy.md\n@@ -98,8 +98,8 @@\n `origin/<default_branch>` and reuses each job's existing base fetch; a shallow\n (`--depth`) or second base fetch would make the proof unresolvable and silently\n disable the carry, so neither is permitted. CCA reads `guardian_comment_carried` as\n-a recorded authority in the deterministic signal and does not re-derive it. See ADR\n-0018 for the full decision, the rejected diff/patch-equality and path-disjointness\n+a recorded authority in the deterministic signal and does not re-derive it. See\n+SADR-0018 for the full decision, the rejected diff/patch-equality and path-disjointness\n alternatives, and the accepted residual.\n \n ## External AI Guardian Review (SADR-0010)\ndiff --git a/docs/agents/implementation-policy.md b/docs/agents/implementation-policy.md\nindex c754144a9fb5bf4a38aa82a60e1670c59e626b44..85c37281bcad196c6b7b02343faf00b03cd9c57a\n--- a/docs/agents/implementation-policy.md\n+++ b/docs/agents/implementation-policy.md\n@@ -31,10 +31,13 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n ## Non-Negotiables\n \n 1. **Planning precedes implementation.** Non-trivial Goals start with `grill-with-docs` and Context & Impact.\n-2. **Domain language is controlled.** Terms must be checked against `CONTEXT.md`; hard-to-reverse tradeoffs must be checked against ADRs.\n+2. **Domain language is controlled.** Terms must be checked against `CONTEXT.md`; hard-to-reverse tradeoffs must be checked against Shiki SADRs and target ADRs.\n 3. **PRD precedes issue decomposition.** `to-prd` is used once the context is settled enough to become durable intent.\n 4. **Issues are vertical slices.** `to-issues` must produce independently grabbable tracer-bullet tasks, not horizontal layer tickets.\n 5. **Triage controls dispatch.** Only `ready-for-agent` / AFK tasks may be assigned to Codex without human input.\n@@ -95,19 +98,19 @@\n Process:\n \n - Challenge terms against `CONTEXT.md`.\n-- Check ADRs before reopening decisions.\n+- Check SADRs and target ADRs before reopening decisions.\n - Inspect code when code can answer a question.\n - Ask one question at a time when the operator is available.\n - Provide a recommended answer with each question.\n - Record resolved terms in `CONTEXT.md`.\n-- Create ADRs only for hard-to-reverse, surprising, tradeoff-based decisions.\n+- Create SADRs or target ADRs only for hard-to-reverse, surprising, tradeoff-based decisions.\n - Convert unresolved load-bearing questions into HITL blockers.\n \n Outputs:\n \n - Resolved design-tree decisions.\n - Updated glossary terms when needed.\n-- ADRs when justified.\n+- SADRs or target ADRs when justified.\n - HITL blockers when decisions remain unresolved.\n \n ## Phase 3 \u2014 Context & Impact\n@@ -117,7 +120,7 @@\n \n Outputs must include:\n \n-- Relevant documents and ADRs.\n+- Relevant documents, SADRs, and target ADRs.\n - Relevant modules, interfaces, seams, callers, and tests.\n - Candidate locks.\n - Dependencies.\n@@ -140,7 +143,7 @@\n - Testing decisions.\n - Out of scope.\n - Further notes.\n-- Links to relevant Goal, domain terms, and ADRs.\n+- Links to relevant Goal, domain terms, SADRs, and target ADRs.\n \n Phase 4 ends with Spec Freeze: the operator explicitly approves the PRD, the\n plan records a `spec_freeze` block (status, approved_by, source), and the SF\n@@ -240,7 +243,7 @@\n - Dependencies and locks.\n - Risk and Guardian requirements.\n - Ledger completeness.\n-- Documentation and ADR updates.\n+- Documentation and SADR/target-ADR updates.\n - Scope drift.\n \n CCA emits one of:\n@@ -301,7 +304,7 @@\n | Evidence failure | Behavior might be done, but proof is missing. | Add checks, PR evidence, ledger entry, or artifacts. |\n | Scope drift | PR changes unrelated behavior. | Revert unrelated changes or split into new Goal. |\n | Dependency block | Upstream task or lock not complete. | Block until dependency resolves. |\n-| Architecture block | Decision needs ADR or deep-module discussion. | `grill-with-docs` or `improve-codebase-architecture`. |\n+| Architecture block | Decision needs a SADR, target ADR, or deep-module discussion. | `grill-with-docs` or `improve-codebase-architecture`. |\n | Risk block | Security/production/policy change needs human approval. | Guardian decision. |\n \n ## Phase 5.5 \u2014 Execution Decision Control\ndiff --git a/docs/agents/runtime-auth-model.md b/docs/agents/runtime-auth-model.md\nindex 6a2e1a47e81ea89b5c68a210ec07772a9374babc..3c54748d24233dbd0ba8473950282547ef4ce935\n--- a/docs/agents/runtime-auth-model.md\n+++ b/docs/agents/runtime-auth-model.md\n@@ -148,7 +148,7 @@\n \n API-key based runners may be added by a target repository as an explicit extension. They are not the default Shiki template path.\n \n-If a repo opts into API-key mode, record the exception in an ADR and update the repo's `AGENTS.md`, `.shiki/config.yaml`, and workflow permissions.\n+If a repo opts into API-key mode, record the target-specific exception in a target ADR and update the repo's `AGENTS.md`, `.shiki/config.yaml`, and workflow permissions.\n \n ## GitHub Actions Runtime Compatibility\n \ndiff --git a/docs/agents/skill-gate.md b/docs/agents/skill-gate.md\nindex c50398b0d7c60a8598f2a5c88a10015730dced13..3acffe6444617bb1d54b77d4b4708158ec735bc6\n--- a/docs/agents/skill-gate.md\n+++ b/docs/agents/skill-gate.md\n@@ -9,7 +9,7 @@\n | Skill | Trigger | Expected output |\n | --- | --- | --- |\n | `setup-matt-pocock-skills` | First configuring a repo for these skills, or when issue tracker, triage labels, or domain docs are missing. | `docs/agents/` configuration and an `Agent skills` block in `AGENTS.md` or `CLAUDE.md`. |\n-| `grill-with-docs` | Ambiguous plan, unclear terminology, boundary decision, tradeoff, or ADR-worthy design choice. | Clarified decision tree, recommended answers, updates to `CONTEXT.md` or ADRs when decisions crystallize. |\n+| `grill-with-docs` | Ambiguous plan, unclear terminology, boundary decision, tradeoff, or decision-record-worthy design choice. | Clarified decision tree, recommended answers, updates to `CONTEXT.md`, SADRs, or target ADRs when decisions crystallize. |\n | `zoom-out` | Agent lacks a higher-level architectural map. | Map of relevant modules, callers, seams, and domain concepts. |\n | `to-prd` | Settled context should become a PRD. | PRD published to the issue tracker with problem, solution, user stories, decisions, testing, and out-of-scope notes. |\n | `to-issues` | Goal, PRD, or plan must become implementation tickets. | Vertical-slice issues with acceptance criteria, dependencies, and AFK/HITL classification. |\n@@ -17,7 +17,7 @@\n | `tdd` | Feature work or bug fix has observable behavior that can be specified with tests. | Red-green-refactor loop through public interfaces, one vertical slice at a time. |\n | `code-review` | The TDD loop is green and the implementer is about to create or update a PR. Default for every implementation task. | Pre-PR self-review of the task diff with fixes applied on the task branch; findings and resolutions recorded as a ledger entry naming the skill and a `## Pre-PR code review` PR body section. |\n | `diagnose` | Hard bug, failing check, regression, flaky behavior, or performance problem. | Reproduction loop, ranked hypotheses, instrumentation, fix, regression test, post-mortem. |\n-| `improve-codebase-architecture` | Structural friction, testability, AI-navigability, deep-module opportunity, or refactor request. | Architecture candidates and deepening recommendations informed by `CONTEXT.md` and ADRs. |\n+| `improve-codebase-architecture` | Structural friction, testability, AI-navigability, deep-module opportunity, or refactor request. | Architecture candidates and deepening recommendations informed by `CONTEXT.md`, SADRs, and target ADRs. |\n | `prototype` | A throwaway logic or UI prototype is needed to answer a design question. | Clearly marked disposable prototype and captured decision. |\n \n ## MergeGate Interaction\ndiff --git a/docs/operations.md b/docs/operations.md\nindex cf1f452d4c589e38ccd58553bda0ad8684b51eb7..ace48a90ffbec9e5c0a7113bef16a4858bc44b3f\n--- a/docs/operations.md\n+++ b/docs/operations.md\n@@ -89,6 +89,16 @@\n      target history, brought forward by `shiki migrate`, not a template.\n    - **Everything else** (CLI scripts, workflows, schemas, docs) is overwritten.\n \n+   A forced upgrade also migrates the former numeric Shiki decision paths to\n+   `SADR-NNNN-*`. Before the first write, the installer inventories the exact\n+   legacy Shiki paths and authorizes each deletion only when the existing\n+   `.shiki/install-stamp.json` names that path and its SHA-256 digest still\n+   matches the file. An absent, unreadable, malformed, incomplete, or\n+   mismatching stamp reports every blocked path and stops with no filesystem\n+   change. Target-owned `NNNN-*` ADRs are not cleanup candidates. A non-force\n+   install deletes nothing and retains any still-matching legacy stamp entries\n+   so a later forced upgrade can prove ownership.\n+\n    If the target has pending migrations, `--force` refuses and writes nothing \u2014\n    apply migrations first (step 3), then re-run. The run ends with a single\n    summary of every `.new` written, so a half-upgrade is visible.\ndiff --git a/docs/releasing.md b/docs/releasing.md\nindex 10d73828e7dbd64e9d9f6b83bc84d010537ce62a..aafeee8c4d441e1e427efee2ce4f81a647f020e1\n--- a/docs/releasing.md\n+++ b/docs/releasing.md\n@@ -67,7 +67,7 @@\n CLI in `scripts/`. There are no third-party Python runtime dependencies. The CLI\n is intentionally **not** exposed as a `[project.scripts]` console entry point\n because `scripts/shiki.py` is a dependency-free shim and `scripts/` is not\n-packaged as an importable distribution (see the ADR for the rationale).\n+packaged as an importable distribution (see SADR-0007 for the rationale).\n \n Supported install paths:\n \n@@ -155,6 +155,15 @@\n    [docs/operations.md](operations.md#the-new-convention). If the target has\n    pending migrations, `--force` refuses and writes nothing; apply migrations\n    (step 2) first, then re-run.\n+\n+   The SADR namespace upgrade has an additional pre-write gate: each former\n+   numeric Shiki decision path is removed only when the existing install stamp\n+   contains that exact path and a digest matching its current bytes. The\n+   installer aggregates absent/unreadable/malformed-stamp, missing-digest, and\n+   digest-mismatch blockers before raising, so refusal leaves the target tree\n+   byte-identical. Numeric target ADRs are preserved. A successful forced\n+   upgrade installs and stamps `SADR-*` paths and drops the removed legacy paths\n+   from the refreshed stamp.\n 2. Apply repository-local `.shiki/` state migrations with the migrate command:\n \n    ```bash\ndiff --git a/docs/usage.md b/docs/usage.md\nindex b83407ee3970dd395dd893010bc5dbe8bad26051..a130ef6e40dfd8169a5bc701339351097df1a09f\n--- a/docs/usage.md\n+++ b/docs/usage.md\n@@ -67,8 +67,10 @@\n shiki install-target /path/to/target-repo --local-only\n ```\n \n-Use `--force` only when you intentionally want to overwrite existing target\n-files.\n+Use `--force` only when you intentionally want to refresh the target template.\n+For the SADR namespace migration, `--force` deletes only exact legacy Shiki\n+decision paths whose existing install-stamp digest still matches; any ownership\n+blocker stops before the first write. Numeric target ADRs are never candidates.\n \n ## `shiki bootstrap-platform` \u2014 publish the Shiki platform repo\n \ndiff --git a/scripts/guardian_approval_signal.py b/scripts/guardian_approval_signal.py\nindex d059f92b2c82aa2b5d6ca421365537a87886c1cb..16924892714f4f5b9303c0ef0e325a7e70075630\n--- a/scripts/guardian_approval_signal.py\n+++ b/scripts/guardian_approval_signal.py\n@@ -199,8 +199,8 @@\n ) -> Any:\n     \"\"\"Evaluate SADR-0015 Contract Approval for the CCA Guardian signal.\n \n-    The SAME OR that MergeGate applies must reach this deterministic signal: ADR\n-    0015 is explicit that without it, a contract-approved PR still returns\n+    The SAME OR that MergeGate applies must reach this deterministic signal:\n+    SADR-0015 is explicit that without it, a contract-approved PR still returns\n     ``needs_guardian``, the CCA job fails, and the MergeGate policy check never\n     runs \u2014 the gate this change moves would never be reached. This resolves the\n     task the SAME way MergeGate does (the first Shiki task id in the PR body),\ndiff --git a/scripts/shiki_contract_approval.py b/scripts/shiki_contract_approval.py\nindex 941bcb96fdd8b9a85bc0c4f0f04d5dad9d04839c..4a4b418f6527413161db82670bec857a043360e2\n--- a/scripts/shiki_contract_approval.py\n+++ b/scripts/shiki_contract_approval.py\n@@ -142,7 +142,7 @@\n     changed_files_status: Any,\n     registration: Any,\n ) -> ContractApprovalResult:\n-    \"\"\"Whether this PR carries an SADR-0015 Contract Approval.\n+    \"\"\"Whether this PR carries a SADR-0015 Contract Approval.\n \n     The four conditions are evaluated IN ORDER, each failure recorded as a\n     distinct reason string:\ndiff --git a/scripts/shiki_guardian.py b/scripts/shiki_guardian.py\nindex a4ecb79bbda55fd5c8b06adfae2eb99760a03c45..4dd99bbe714e26f421fa19abac7a34ae1b195ecc\n--- a/scripts/shiki_guardian.py\n+++ b/scripts/shiki_guardian.py\n@@ -12,7 +12,7 @@\n # The carry re-derives its proof from git in-process on every judging invocation;\n # it is never read from an artifact, so there is nothing to go stale or be forged.\n # shiki_sync_proof is the ONLY guardian-family module that runs git, and every one\n-# of its invocations is read-only and local (ADR: prove-from-git base sync).\n+# of its invocations is read-only and local (SADR: prove-from-git base sync).\n from shiki_sync_proof import verify_pure_base_sync\n \n GUARDIAN_POLICY_PATH = \".shiki/guardian-policy.json\"\ndiff --git a/scripts/shiki_guardian_review.py b/scripts/shiki_guardian_review.py\nindex dd0f104d457899e0ba5ff1e162011c1e90b6f92b..e10ef58c046324e7ea3a3392feb3048ca77414ce\n--- a/scripts/shiki_guardian_review.py\n+++ b/scripts/shiki_guardian_review.py\n@@ -159,7 +159,7 @@\n     docs_only = _is_docs_only(changed_files)\n     if docs_only and matched == 0:\n         focus.append(\n-            \"Docs/ADR-only change: confirm the change is genuinely documentation/decision-only, introduces no behavior or contract drift, and stays consistent with the constitution and existing ADRs.\"\n+            \"Docs/decision-record-only change: confirm the change is genuinely documentation/decision-only, introduces no behavior or contract drift, and stays consistent with the constitution and existing SADRs and target ADRs.\"\n         )\n \n     # Conservative augmentation: unknown (no category and not docs-only), mixed\ndiff --git a/scripts/shiki_installer.py b/scripts/shiki_installer.py\nindex be064ab7a29ff6836f7f7a1d70500a43e0d11d7b..9fd8eefd137ce7003dad5c0dcb2e48e2cd47ab32\n--- a/scripts/shiki_installer.py\n+++ b/scripts/shiki_installer.py\n@@ -118,6 +118,7 @@\n # what lets a target carry it without a governance-schema change.\n INSTALL_STAMP_PATH = \".shiki/install-stamp.json\"\n INSTALL_STAMP_VERSION = 1\n+LEGACY_SHIKI_SADR_MAX = 18\n \n # Under --force the shipped surface splits three ways, so nothing is lost and\n # nothing is silently stale.\n@@ -237,9 +238,98 @@\n         )\n \n \n+def legacy_shiki_adr_paths() -> tuple[str, ...]:\n+    \"\"\"Former numeric paths for Shiki SADR-0001 through SADR-0018.\n+\n+    The authoritative slugs come from the current SADR filenames. SADR-0019 is\n+    the namespace decision itself and never shipped under a numeric legacy path.\n+    \"\"\"\n+    paths: list[str] = []\n+    for sadr in sorted((ROOT / \"docs\" / \"adr\").glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\")):\n+        number = sadr.name.split(\"-\", 2)[1]\n+        if int(number) <= LEGACY_SHIKI_SADR_MAX:\n+            paths.append(f\"docs/adr/{sadr.name.removeprefix('SADR-')}\")\n+    return tuple(paths)\n+\n+\n+def preflight_legacy_sadr_cleanup(target: Path, *, force: bool) -> tuple[str, ...]:\n+    \"\"\"Authorize exact legacy Shiki ADR removals before the first write.\n+\n+    A non-force install never removes existing files. A forced install needs no\n+    stamp when no legacy Shiki path is present. When legacy paths do exist, each\n+    must be named in a readable install stamp and its current bytes must match\n+    the recorded digest.\n+    \"\"\"\n+    if not force:\n+        return ()\n+    present = tuple(\n+        relative\n+        for relative in legacy_shiki_adr_paths()\n+        if (target / relative).exists() or (target / relative).is_symlink()\n+    )\n+    if not present:\n+        return ()\n+\n+    stamp_path = target / INSTALL_STAMP_PATH\n+    try:\n+        raw = stamp_path.read_text(encoding=\"utf-8\")\n+        stamp = json.loads(raw)\n+    except FileNotFoundError:\n+        detail = \"install stamp is absent\"\n+        blockers = [f\"{relative}: {detail}\" for relative in present]\n+    except (OSError, UnicodeDecodeError) as error:\n+        detail = f\"install stamp is unreadable ({error})\"\n+        blockers = [f\"{relative}: {detail}\" for relative in present]\n+    except json.JSONDecodeError as error:\n+        detail = f\"install stamp is malformed ({error})\"\n+        blockers = [f\"{relative}: {detail}\" for relative in present]\n+    else:\n+        if not isinstance(stamp, dict):\n+            blockers = [\n+                f\"{relative}: install stamp is malformed (expected an object)\"\n+                for relative in present\n+            ]\n+        else:\n+            digests = stamp.get(\"digests\")\n+            blockers = []\n+            if not isinstance(digests, dict):\n+                blockers.extend(\n+                    f\"{relative}: install stamp has no digest for this path\"\n+                    for relative in present\n+                )\n+            else:\n+                for relative in present:\n+                    expected = digests.get(relative)\n+                    if not isinstance(expected, str) or not expected:\n+                        blockers.append(\n+                            f\"{relative}: install stamp has no digest for this path\"\n+                        )\n+                        continue\n+                    try:\n+                        actual = _sha256_file(target / relative)\n+                    except OSError as error:\n+                        blockers.append(\n+                            f\"{relative}: legacy path is unreadable ({error})\"\n+                        )\n+                        continue\n+                    if actual != expected:\n+                        blockers.append(\n+                            f\"{relative}: digest mismatch (stamp {expected}, current {actual})\"\n+                        )\n+\n+    if blockers:\n+        joined = \"\\n\".join(f\"- {blocker}\" for blocker in blockers)\n+        raise ShikiError(\n+            \"legacy Shiki ADR cleanup is not authorized:\\n\"\n+            f\"{joined}\\nNo files were written.\"\n+        )\n+    return present\n+\n+\n def install_template(target: Path, *, force: bool, validate: bool) -> list[NewFileNote]:\n     # Preconditions first: never rewrite the tree of a target that cannot be\n     # upgraded cleanly (see refuse_pending_migrations).\n+    legacy_cleanup = preflight_legacy_sadr_cleanup(target, force=force)\n     refuse_pending_migrations(target)\n \n     # Resolve the target's required CODEOWNERS owner once from its\n@@ -250,6 +340,10 @@\n     # verbatim. See ``codeowners_required_owner``.\n     codeowners_owner = codeowners_required_owner(target)\n \n+    for relative in legacy_cleanup:\n+        (target / relative).unlink()\n+        info(f\"removed stamped legacy Shiki ADR: {target / relative}\")\n+\n     notes: list[NewFileNote] = []\n     for relative in TEMPLATE_PATHS:\n         source = ROOT / relative\n@@ -647,6 +741,22 @@\n     digests = target_stamp_digests(target)\n     existing = read_install_stamp(target)\n     if existing is not None:\n+        # A non-force install deliberately leaves legacy Shiki ADRs in place.\n+        # Preserve only still-matching ownership proof so a later forced\n+        # upgrade can authorize cleanup; never carry a missing or stale digest.\n+        existing_digests = existing.get(\"digests\")\n+        if isinstance(existing_digests, dict):\n+            for relative in legacy_shiki_adr_paths():\n+                expected = existing_digests.get(relative)\n+                path = target / relative\n+                if not isinstance(expected, str) or not path.is_file():\n+                    continue\n+                try:\n+                    actual = _sha256_file(path)\n+                except OSError:\n+                    continue\n+                if actual == expected:\n+                    digests[relative] = actual\n         if _incoming_is_older(commit, existing.get(\"platform_commit\")):\n             warn(\n                 \"kept existing install stamp: incoming install \"\ndiff --git a/scripts/shiki_loop.py b/scripts/shiki_loop.py\nindex 3611641da8aeb5038b1da90e3114b09a473ec6d8..84caa491b9cad0edc4176f209c13e9221e975681\n--- a/scripts/shiki_loop.py\n+++ b/scripts/shiki_loop.py\n@@ -424,7 +424,7 @@\n         # unresolvable CCA verdict has already produced its terminal stop and is never\n         # laundered into a head-moving sync (SADR-0010). It also never fires while the\n         # policy (Guardian) gate is red \u2014 for ANY reason, not only when it is the sole\n-        # red check \u2014 so a head-bound Guardian approval, or an SADR-0015\n+        # red check \u2014 so a head-bound Guardian approval, or a SADR-0015\n         # contract-approval carry that base movement defeated (the policy gate compares\n         # base and head task governance directly), is never silently restored by\n         # adopting base wholesale. It is bounded by a durable per-task counter so the\ndiff --git a/scripts/shiki_tasks.py b/scripts/shiki_tasks.py\nindex aec3809f24b4a0c8c96fc50971cff9ae9ad30381..d531052e9d9717058d4631483cefab44c26a65c1\n--- a/scripts/shiki_tasks.py\n+++ b/scripts/shiki_tasks.py\n@@ -821,7 +821,7 @@\n         \"prompt\": prompt,\n         \"entry_skill\": \"grill-with-docs\",\n         \"required_next_steps\": [\n-            \"Run grill-with-docs until terminology, boundaries, risks, and ADR-worthy decisions are settled.\",\n+            \"Run grill-with-docs until terminology, boundaries, risks, and decision-record-worthy choices are settled.\",\n             \"Obtain the operator's explicit approval of the PRD/requirements (Spec Freeze).\",\n             \"Write a machine-readable plan JSON with grill_with_docs.status=complete and spec_freeze.status=frozen.\",\n             \"Run shiki plan ingest --plan-file PLAN.json.\",\ndiff --git a/scripts/test_shiki_goal_loop.sh b/scripts/test_shiki_goal_loop.sh\nindex 753c7b516e10f0be3bc92a4bd0eb1fa699776ae6..8f3820a0a079b3044424cfe1a6c6b7fe68896e67\n--- a/scripts/test_shiki_goal_loop.sh\n+++ b/scripts/test_shiki_goal_loop.sh\n@@ -231,8 +231,8 @@\n \n # High/critical risk merges autonomously when all required checks are green:\n # the \"MergeGate policy check\" required check IS the Guardian gate (it enforces\n-# guardian-policy.json \u2014 human approval OR external AI guardian review, ADR\n-# 0010), so green checks mean a recorded authority approved. The fake gh returns\n+# guardian-policy.json \u2014 human approval OR external AI guardian review,\n+# SADR-0010), so green checks mean a recorded authority approved. The fake gh returns\n # green checks, simulating that the Guardian gate passed.\n python3 \"$ROOT/scripts/shiki.py\" goal create --target \"$TARGET\" --title \"Guardian gate\" --outcome \"High risk merges via policy gate\" >/tmp/shiki-goal-loop-guardian-goal.json\n GGOAL=\"$(json_get /tmp/shiki-goal-loop-guardian-goal.json goal_id)\"\ndiff --git a/tests/test_adr_references.py b/tests/test_adr_references.py\nindex 51706329c797d5dd9b47e082a333fa83b261d482..d005da4f771f5e8085ec22d4b787980e68f5ae8c\n--- a/tests/test_adr_references.py\n+++ b/tests/test_adr_references.py\n@@ -1,8 +1,8 @@\n-\"\"\"Every ``ADR ####`` citation must resolve to exactly one decision record.\n+\"\"\"Every target ADR and Shiki SADR citation resolves in its own namespace.\n \n Background. ``scripts/mergegate_check.py`` justifies ``is_bookkeeping_closeout``\n-with \"ADR 0017\" nine times and ``scripts/guardian_approval_signal.py`` cites\n-0016/0017 seven times, yet for a stretch the numbering in ``docs/adr/`` stopped\n+with \"SADR-0017\" nine times and ``scripts/guardian_approval_signal.py`` cites\n+SADR-0016/SADR-0017 seven times, yet for a stretch the numbering in ``docs/adr/`` stopped\n at 0015: the authoring commits lived on a coordinator branch and no task ever\n declared ``docs/adr/...`` in its locks, so nothing carried the records to the\n default branch. The stated basis of an exemption that merged seven PRs with zero\n@@ -11,11 +11,12 @@\n and the directory drifted with no signal.\n \n This is the same unbound-pair shape behind other silent failures: a reference\n-and its target with nothing asserting they agree. This test binds them. It scans\n-every ``scripts/*.py`` (plus ``docs/**/*.md`` and the root ``*.md`` governance\n-files, which are cheap) for the ``ADR ####`` pattern and asserts each cited\n-number resolves to exactly one ``docs/adr/####-*.md``. The converse is\n-deliberately NOT required: an ADR may exist without being cited.\n+and its target with nothing asserting they agree. This test binds them. Target\n+``ADR NNNN`` citations resolve only to ``docs/adr/NNNN-*.md``; Shiki\n+``SADR-NNNN`` citations resolve only to ``docs/adr/SADR-NNNN-*.md``. The scan\n+covers current governance docs, scripts, workflows, prompts, packaging metadata,\n+and repo-local Codex guidance while deliberately excluding historical\n+``.shiki`` evidence. The converse is not required: a record may be uncited.\n \n The suite is dependency free: standard-library ``unittest`` only.\n \"\"\"\n@@ -31,17 +32,18 @@\n \n REPO_ROOT = Path(__file__).resolve().parents[1]\n \n-# \"ADR 0016\" -- a four-digit citation. The trailing \\b keeps a five-digit token\n-# from being read as a truncated four-digit number.\n-_CITATION = re.compile(r\"\\bADR (\\d{4})\\b\")\n-# \"0016-locks-bound-product-paths.md\" -- the leading four digits name the record.\n-_ADR_FILENAME = re.compile(r\"^(\\d{4})-.*\\.md$\")\n+# ``ADR 0016`` / ``SADR-0016`` -- a namespace plus four-digit citation. The\n+# trailing ``\\b`` keeps a five-digit token from being read as a truncated one.\n+_CITATION = re.compile(r\"\\b(S?ADR)[ -](\\d{4})\\b\")\n+# Target ADRs retain the numeric filename; Shiki platform decisions use SADR.\n+_DECISION_FILENAME = re.compile(r\"^(?:(SADR)-)?(\\d{4})-.*\\.md$\")\n \n \n class Violation(NamedTuple):\n     \"\"\"A citation that does not resolve to exactly one decision record.\"\"\"\n \n     citing_file: str  # repo-relative path of the file that cites the ADR\n+    namespace: str  # \"ADR\" for target decisions; \"SADR\" for Shiki decisions\n     number: str  # the cited four-digit number, e.g. \"0016\"\n     line: int  # 1-based line of the citation\n     reason: str  # \"missing\" (zero matches) or \"ambiguous\" (two or more)\n@@ -49,44 +51,59 @@\n \n     @property\n     def expected_path(self) -> str:\n-        return f\"docs/adr/{self.number}-*.md\"\n+        prefix = \"SADR-\" if self.namespace == \"SADR\" else \"\"\n+        return f\"docs/adr/{prefix}{self.number}-*.md\"\n \n     def describe(self) -> str:\n         \"\"\"Name the citing file, the cited number, and the missing path.\"\"\"\n         if self.reason == \"missing\":\n             return (\n-                f\"{self.citing_file}:{self.line} cites ADR {self.number} \"\n+                f\"{self.citing_file}:{self.line} cites {self.namespace} {self.number} \"\n                 f\"but no document matches {self.expected_path}\"\n             )\n         joined = \", \".join(self.matches)\n         return (\n-            f\"{self.citing_file}:{self.line} cites ADR {self.number} which \"\n+            f\"{self.citing_file}:{self.line} cites {self.namespace} {self.number} which \"\n             f\"resolves to {len(self.matches)} documents (expected exactly one \"\n             f\"at {self.expected_path}): {joined}\"\n         )\n \n \n-def _adr_index(root: Path) -> dict[str, list[Path]]:\n-    \"\"\"Map every four-digit ADR number to the record files that claim it.\"\"\"\n-    index: dict[str, list[Path]] = {}\n+def _adr_index(root: Path) -> dict[tuple[str, str], list[Path]]:\n+    \"\"\"Map each decision namespace and number to the files that claim it.\"\"\"\n+    index: dict[tuple[str, str], list[Path]] = {}\n     adr_dir = root / \"docs\" / \"adr\"\n     if adr_dir.is_dir():\n         for path in sorted(adr_dir.glob(\"*.md\")):\n-            match = _ADR_FILENAME.match(path.name)\n+            match = _DECISION_FILENAME.match(path.name)\n             if match:\n-                index.setdefault(match.group(1), []).append(path)\n+                namespace = \"SADR\" if match.group(1) else \"ADR\"\n+                index.setdefault((namespace, match.group(2)), []).append(path)\n     return index\n \n \n def _citing_files(root: Path, include_docs: bool = True) -> list[Path]:\n-    \"\"\"Files scanned for citations: scripts/*.py, and cheaply the markdown.\"\"\"\n+    \"\"\"Current, shipped text surfaces scanned for decision citations.\"\"\"\n     files = sorted((root / \"scripts\").glob(\"*.py\"))\n+    files += sorted((root / \"scripts\").glob(\"*.sh\"))\n     if include_docs:\n         docs = root / \"docs\"\n         if docs.is_dir():\n             files += sorted(docs.rglob(\"*.md\"))\n         files += sorted(root.glob(\"*.md\"))\n-    return files\n+        pyproject = root / \"pyproject.toml\"\n+        if pyproject.is_file():\n+            files.append(pyproject)\n+        for relative in (\".github\", \".codex\"):\n+            surface = root / relative\n+            if surface.is_dir():\n+                files += sorted(\n+                    path\n+                    for path in surface.rglob(\"*\")\n+                    if path.is_file()\n+                    and path.suffix in {\".md\", \".py\", \".sh\", \".toml\", \".yaml\", \".yml\"}\n+                )\n+    return sorted(set(files))\n \n \n def find_adr_reference_violations(\n@@ -94,10 +111,9 @@\n ) -> list[Violation]:\n     \"\"\"Return every ``ADR ####`` citation under ``root`` that fails to resolve.\n \n-    A citation resolves when exactly one ``docs/adr/####-*.md`` matches its\n-    number. Zero matches is a \"missing\" violation; two or more is \"ambiguous\".\n-    Records that are never cited produce nothing -- the converse is not\n-    required.\n+    A citation resolves when exactly one record in the same namespace matches\n+    its number. Zero matches is a \"missing\" violation; two or more is\n+    \"ambiguous\". Records that are never cited produce nothing.\n     \"\"\"\n     root = Path(root)\n     index = _adr_index(root)\n@@ -110,13 +126,15 @@\n         rel = path.relative_to(root).as_posix()\n         for lineno, line in enumerate(text.splitlines(), start=1):\n             for match in _CITATION.finditer(line):\n-                number = match.group(1)\n-                found = index.get(number, [])\n+                namespace, number = match.groups()\n+                found = index.get((namespace, number), [])\n                 if len(found) == 1:\n                     continue\n                 reason = \"missing\" if not found else \"ambiguous\"\n                 rels = [p.relative_to(root).as_posix() for p in found]\n-                violations.append(Violation(rel, number, lineno, reason, rels))\n+                violations.append(\n+                    Violation(rel, namespace, number, lineno, reason, rels)\n+                )\n     return violations\n \n \n@@ -129,8 +147,8 @@\n         self.assertEqual(\n             violations,\n             [],\n-            \"Every 'ADR ####' citation must resolve to exactly one \"\n-            f\"docs/adr/####-*.md:\\n{detail}\",\n+            \"Every ADR/SADR citation must resolve exactly once in its own \"\n+            f\"docs/adr namespace:\\n{detail}\",\n         )\n \n     def test_scan_actually_reads_scripts(self) -> None:\n@@ -142,12 +160,38 @@\n             \"the citation scan must cover scripts/*.py\",\n         )\n \n+    def test_platform_citations_do_not_use_unresolved_numeric_shorthand(self) -> None:\n+        shorthand = re.compile(r\"SADR-\\d{4}/\\d{4}\")\n+        offenders = []\n+        for path in _citing_files(REPO_ROOT):\n+            text = path.read_text(encoding=\"utf-8\")\n+            if shorthand.search(text):\n+                offenders.append(path.relative_to(REPO_ROOT).as_posix())\n+        self.assertEqual(\n+            offenders,\n+            [],\n+            \"repeat the SADR prefix so every cited number is independently resolvable\",\n+        )\n+\n     def test_carried_records_are_indexed(self) -> None:\n         # The two records this Goal carried onto the default branch, and which\n         # the scripts cite most, must be present.\n         index = _adr_index(REPO_ROOT)\n-        self.assertIn(\"0016\", index)\n-        self.assertIn(\"0017\", index)\n+        self.assertIn((\"SADR\", \"0016\"), index)\n+        self.assertIn((\"SADR\", \"0017\"), index)\n+\n+    def test_platform_tree_has_only_sadr_0001_through_0019(self) -> None:\n+        adr_dir = REPO_ROOT / \"docs\" / \"adr\"\n+        sadrs = sorted(adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\"))\n+        self.assertEqual(\n+            {path.name.split(\"-\", 2)[1] for path in sadrs},\n+            {f\"{number:04d}\" for number in range(1, 20)},\n+        )\n+        self.assertEqual(\n+            list(adr_dir.glob(\"[0-9][0-9][0-9][0-9]-*.md\")),\n+            [],\n+            \"the Shiki platform tree must not retain legacy numeric SADR paths\",\n+        )\n \n \n class FixtureAdrReferences(unittest.TestCase):\n@@ -180,6 +224,26 @@\n         self.assertIn(\"9991\", message)  # cited number\n         self.assertIn(\"docs/adr/9991-*.md\", message)  # missing path\n \n+    def test_target_adr_does_not_satisfy_missing_platform_sadr(self) -> None:\n+        root = self._make_repo(\n+            adr_names=[\"0042-target-decision.md\"],\n+            script_body='\"\"\"Target ADR 0042 exists, but platform SADR-0042 does not.\"\"\"\\n',\n+        )\n+        violations = find_adr_reference_violations(root, include_docs=False)\n+        self.assertEqual(len(violations), 1)\n+        self.assertEqual(violations[0].namespace, \"SADR\")\n+        self.assertEqual(violations[0].number, \"0042\")\n+        self.assertEqual(violations[0].expected_path, \"docs/adr/SADR-0042-*.md\")\n+\n+    def test_target_adr_and_platform_sadr_may_reuse_a_number(self) -> None:\n+        root = self._make_repo(\n+            adr_names=[\"0042-target-decision.md\", \"SADR-0042-platform-decision.md\"],\n+            script_body='\"\"\"Target ADR 0042 and platform SADR-0042 are distinct.\"\"\"\\n',\n+        )\n+        self.assertEqual(\n+            find_adr_reference_violations(root, include_docs=False), []\n+        )\n+\n     def test_uncited_adr_is_allowed(self) -> None:\n         # An ADR exists but nothing cites it -> not a violation. The converse\n         # (every ADR must be cited) is explicitly NOT required.\ndiff --git a/tests/test_guardian_review.py b/tests/test_guardian_review.py\nindex 88829e7810212635e2fc98aa5918d6f26294677d..00a85a237acd5bf2d6cb5b294128198c422402ca\n--- a/tests/test_guardian_review.py\n+++ b/tests/test_guardian_review.py\n@@ -162,7 +162,7 @@\n \n     def test_docs_only_is_light_and_not_conservative(self):\n         focus = gr.classify_review_focus([\"docs/adr/0099-x.md\", \"README.md\"], \"\", \"low\")\n-        self.assertTrue(any(\"Docs/ADR-only\" in f for f in focus))\n+        self.assertTrue(any(\"Docs/decision-record-only\" in f for f in focus))\n         self.assertFalse(any(\"conservative default\" in f.lower() for f in focus))\n \n     def test_unknown_category_is_conservative(self):\ndiff --git a/tests/test_policy_job_merged_prs.py b/tests/test_policy_job_merged_prs.py\nindex d45512e74681e5e649279236c7d4e79d5c3da471..b97989f8bd74d091de1a6b5275a20d8dc68c4512\n--- a/tests/test_policy_job_merged_prs.py\n+++ b/tests/test_policy_job_merged_prs.py\n@@ -3,8 +3,8 @@\n \n ``.github/workflows/shiki-cca-completion.yml`` has two consumers of the merged-PR\n proof: the CCA *signal* (``guardian_approval_signal.py`` in \"Prepare CCA context\")\n-and the authoritative gate (``mergegate_check.py`` in \"Enforce MergeGate\"). ADR\n-0017's bookkeeping-closeout exemption (condition 6) needs the merged implementation\n+and the authoritative gate (``mergegate_check.py`` in \"Enforce MergeGate\").\n+SADR-0017's bookkeeping-closeout exemption (condition 6) needs the merged implementation\n PR proven, and a closeout carries NO ``post_merge_reconcile`` marker. The signal\n already resolves ``MERGED_PRS`` unconditionally, but \"Enforce MergeGate\" used to\n resolve it only inside ``grep -q '<!-- shiki:post_merge_reconcile -->'``. So a\ndiff --git a/tests/test_sadr_install_migration.py b/tests/test_sadr_install_migration.py\nnew file mode 100644\nindex 0000000000000000000000000000000000000000..d51eac872b1c79ff4579c4c1401ca5d775a60d8d\n--- /dev/null\n+++ b/tests/test_sadr_install_migration.py\n@@ -0,0 +1,237 @@\n+\"\"\"SADR installation and collision-safe legacy upgrade behavior.\"\"\"\n+\n+from __future__ import annotations\n+\n+import contextlib\n+import hashlib\n+import io\n+import json\n+import shutil\n+import tempfile\n+import unittest\n+from pathlib import Path\n+\n+import shiki_test_support  # noqa: F401  (path bootstrap)\n+\n+import shiki_installer\n+from tests.test_adr_references import find_adr_reference_violations\n+\n+\n+PRODUCT_NUMBERS = tuple(range(13, 19))\n+\n+\n+def _quiet_install(target: Path, *, force: bool, validate: bool) -> None:\n+    with contextlib.redirect_stdout(io.StringIO()):\n+        shiki_installer.install_template(target, force=force, validate=validate)\n+\n+\n+def _sha256(path: Path) -> str:\n+    return hashlib.sha256(path.read_bytes()).hexdigest()\n+\n+\n+def _legacy_sadr_paths() -> tuple[str, ...]:\n+    return tuple(\n+        f\"docs/adr/{path.name.removeprefix('SADR-')}\"\n+        for path in sorted((shiki_installer.ROOT / \"docs\" / \"adr\").glob(\"SADR-*.md\"))\n+        if path.name != \"SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\"\n+    )\n+\n+\n+def _create_legacy_upgrade_fixture(\n+    target: Path,\n+) -> tuple[dict[str, bytes], tuple[str, ...]]:\n+    adr_dir = target / \"docs\" / \"adr\"\n+    adr_dir.mkdir(parents=True)\n+    product_bytes = {\n+        f\"{number:04d}-cubeshare-product-decision.md\": (\n+            f\"# CubeShare ADR {number:04d}\\n\\nDo not rewrite {number}.\\n\"\n+        ).encode()\n+        for number in PRODUCT_NUMBERS\n+    }\n+    for name, content in product_bytes.items():\n+        (adr_dir / name).write_bytes(content)\n+\n+    legacy_paths = _legacy_sadr_paths()\n+    for relative in legacy_paths:\n+        (target / relative).write_bytes(f\"legacy Shiki bytes for {relative}\\n\".encode())\n+\n+    stamp_path = target / shiki_installer.INSTALL_STAMP_PATH\n+    stamp_path.parent.mkdir(parents=True, exist_ok=True)\n+    stamp = {\n+        \"version\": shiki_installer.INSTALL_STAMP_VERSION,\n+        \"platform_commit\": None,\n+        \"installed_at\": \"2000-01-01T00:00:00+00:00\",\n+        \"digests\": {\n+            relative: _sha256(target / relative) for relative in legacy_paths\n+        },\n+    }\n+    stamp_path.write_text(\n+        json.dumps(stamp, indent=2, sort_keys=True) + \"\\n\", encoding=\"utf-8\"\n+    )\n+    return product_bytes, legacy_paths\n+\n+\n+def _tree_snapshot(root: Path) -> dict[str, tuple[str, bytes | str]]:\n+    \"\"\"Recursive byte/shape snapshot used to prove preflight is write-free.\"\"\"\n+    snapshot: dict[str, tuple[str, bytes | str]] = {}\n+    for path in sorted(root.rglob(\"*\")):\n+        relative = path.relative_to(root).as_posix()\n+        if path.is_symlink():\n+            snapshot[relative] = (\"symlink\", path.readlink().as_posix())\n+        elif path.is_dir():\n+            snapshot[relative] = (\"directory\", \"\")\n+        else:\n+            snapshot[relative] = (\"file\", path.read_bytes())\n+    return snapshot\n+\n+\n+class FreshSadrInstallTests(unittest.TestCase):\n+    def setUp(self) -> None:\n+        self.tmp = Path(tempfile.mkdtemp(prefix=\"shiki-sadr-fresh-\"))\n+        self.addCleanup(lambda: shutil.rmtree(self.tmp, ignore_errors=True))\n+        self.target = self.tmp / \"target\"\n+        self.adr_dir = self.target / \"docs\" / \"adr\"\n+        self.adr_dir.mkdir(parents=True)\n+        self.product_bytes = {\n+            f\"{number:04d}-product-decision.md\": (\n+                f\"# Product ADR {number:04d}\\n\\nTarget-owned bytes {number}.\\n\"\n+            ).encode()\n+            for number in PRODUCT_NUMBERS\n+        }\n+        for name, content in self.product_bytes.items():\n+            (self.adr_dir / name).write_bytes(content)\n+\n+    def test_fresh_install_preserves_product_adrs_and_installs_all_sadrs(self) -> None:\n+        _quiet_install(self.target, force=False, validate=True)\n+\n+        for name, expected in self.product_bytes.items():\n+            self.assertEqual((self.adr_dir / name).read_bytes(), expected)\n+\n+        sadr_numbers = {\n+            path.name.split(\"-\", 2)[1]\n+            for path in self.adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\")\n+        }\n+        self.assertEqual(sadr_numbers, {f\"{number:04d}\" for number in range(1, 20)})\n+\n+        numeric_records = {\n+            path.name\n+            for path in self.adr_dir.glob(\"[0-9][0-9][0-9][0-9]-*.md\")\n+        }\n+        self.assertEqual(numeric_records, set(self.product_bytes))\n+        stamp = json.loads(\n+            (self.target / shiki_installer.INSTALL_STAMP_PATH).read_text(\n+                encoding=\"utf-8\"\n+            )\n+        )\n+        for path in self.adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\"):\n+            relative = path.relative_to(self.target).as_posix()\n+            self.assertEqual(stamp[\"digests\"][relative], _sha256(path))\n+        self.assertEqual(find_adr_reference_violations(self.target), [])\n+\n+\n+class LegacySadrUpgradeTests(unittest.TestCase):\n+    def setUp(self) -> None:\n+        self.tmp = Path(tempfile.mkdtemp(prefix=\"shiki-sadr-upgrade-\"))\n+        self.addCleanup(lambda: shutil.rmtree(self.tmp, ignore_errors=True))\n+        self.target = self.tmp / \"target\"\n+        self.adr_dir = self.target / \"docs\" / \"adr\"\n+        self.product_bytes, self.legacy_paths = _create_legacy_upgrade_fixture(\n+            self.target\n+        )\n+\n+    def test_force_upgrade_deletes_only_stamped_legacy_sadrs(self) -> None:\n+        _quiet_install(self.target, force=True, validate=True)\n+\n+        for relative in self.legacy_paths:\n+            self.assertFalse((self.target / relative).exists(), relative)\n+        for name, expected in self.product_bytes.items():\n+            self.assertEqual((self.adr_dir / name).read_bytes(), expected)\n+\n+        sadrs = list(self.adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\"))\n+        self.assertEqual(len(sadrs), 19)\n+        refreshed = json.loads(\n+            (self.target / shiki_installer.INSTALL_STAMP_PATH).read_text(encoding=\"utf-8\")\n+        )\n+        for relative in self.legacy_paths:\n+            self.assertNotIn(relative, refreshed[\"digests\"])\n+        for path in sadrs:\n+            relative = path.relative_to(self.target).as_posix()\n+            self.assertEqual(refreshed[\"digests\"][relative], _sha256(path))\n+\n+    def test_non_force_install_never_deletes_legacy_files(self) -> None:\n+        before = {\n+            relative: (self.target / relative).read_bytes()\n+            for relative in self.legacy_paths\n+        }\n+        _quiet_install(self.target, force=False, validate=False)\n+        for relative, expected in before.items():\n+            self.assertEqual((self.target / relative).read_bytes(), expected)\n+        refreshed = json.loads(\n+            (self.target / shiki_installer.INSTALL_STAMP_PATH).read_text(encoding=\"utf-8\")\n+        )\n+        for relative in self.legacy_paths:\n+            self.assertEqual(refreshed[\"digests\"][relative], _sha256(self.target / relative))\n+\n+\n+class LegacyCleanupPreflightTests(unittest.TestCase):\n+    def setUp(self) -> None:\n+        self.tmp = Path(tempfile.mkdtemp(prefix=\"shiki-sadr-preflight-\"))\n+        self.addCleanup(lambda: shutil.rmtree(self.tmp, ignore_errors=True))\n+\n+    def test_all_ownership_blockers_abort_before_any_write(self) -> None:\n+        cases = (\n+            (\"absent\", \"install stamp is absent\"),\n+            (\"unreadable\", \"install stamp is unreadable\"),\n+            (\"malformed\", \"install stamp is malformed\"),\n+            (\"missing-digest\", \"install stamp has no digest\"),\n+            (\"digest-mismatch\", \"digest mismatch\"),\n+        )\n+        for name, expected_reason in cases:\n+            with self.subTest(name=name):\n+                target = self.tmp / name\n+                _, legacy_paths = _create_legacy_upgrade_fixture(target)\n+                stamp_path = target / shiki_installer.INSTALL_STAMP_PATH\n+\n+                if name == \"absent\":\n+                    stamp_path.unlink()\n+                elif name == \"unreadable\":\n+                    stamp_path.unlink()\n+                    stamp_path.mkdir()\n+                elif name == \"malformed\":\n+                    stamp_path.write_text(\"{not-json\\n\", encoding=\"utf-8\")\n+                else:\n+                    stamp = json.loads(stamp_path.read_text(encoding=\"utf-8\"))\n+                    if name == \"missing-digest\":\n+                        stamp[\"digests\"] = {}\n+                    else:\n+                        stamp[\"digests\"] = {\n+                            relative: \"deadbeef\" for relative in legacy_paths\n+                        }\n+                    stamp_path.write_text(\n+                        json.dumps(stamp, indent=2, sort_keys=True) + \"\\n\",\n+                        encoding=\"utf-8\",\n+                    )\n+\n+                before = _tree_snapshot(target)\n+                with self.assertRaises(shiki_installer.ShikiError) as raised:\n+                    _quiet_install(target, force=True, validate=False)\n+                after = _tree_snapshot(target)\n+\n+                self.assertEqual(before, after, f\"{name} preflight changed the tree\")\n+                message = str(raised.exception)\n+                self.assertIn(expected_reason, message)\n+                self.assertIn(\"No files were written\", message)\n+                for relative in legacy_paths:\n+                    self.assertIn(relative, message)\n+\n+    def test_absent_stamp_is_allowed_when_no_legacy_path_exists(self) -> None:\n+        target = self.tmp / \"no-legacy\"\n+        target.mkdir()\n+        _quiet_install(target, force=True, validate=False)\n+        self.assertTrue(\n+            (target / \"docs\" / \"adr\" / \"SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\").is_file()\n+        )\n+\n+\n+if __name__ == \"__main__\":\n+    unittest.main()\n\n2026-08-07T08:25:20.944102Z ERROR codex_models_manager::manager: failed to renew cache TTL: missing field `base_instructions` at line 94 column 5\nexec\n/bin/zsh -lc 'ls .shiki/ledger | sort | tail -8' in /private/tmp/shiki-adr-collision.pa0KwY/.worktrees/shiki-t-20260807t073307221254z-2d22d654-introduc\n succeeded in 0ms:\nL-20260807T052653899184Z-8103dabf.json\nL-20260807T053141876612Z-0d09a7e6.json\nL-20260807T053145980808Z-709d28a4.json\nL-20260807T073307220560Z-fc2d907a.json\nL-20260807T073307221361Z-b3c3a718.json\nL-20260807T075227529801Z-6c97bbc8.json\nL-20260807T075227612709Z-9371d9bc.json\nL-20260807T075227693451Z-abe727ae.json\n\ndiff --git a/.codex/skills/shiki/SKILL.md b/.codex/skills/shiki/SKILL.md\nindex c8e2e85d9d75d49cc4f2d6286ee22659c4e53e75..93cded623b2c15d9f65c76491a1c2d813ac9e6c0\n--- a/.codex/skills/shiki/SKILL.md\n+++ b/.codex/skills/shiki/SKILL.md\n@@ -18,6 +18,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n Codex CLI does not register this skill as a custom `/shiki` slash command. When\n the user types `/shiki` in Codex CLI and gets \"Unrecognized command\", explain\n that this is expected: invoke Shiki in Codex with natural language such as\n@@ -69,7 +72,7 @@\n \n ## Responsibilities\n \n-- Claude Code implements and repairs by default (`shiki runner claude`, ADR 0008); Codex implements and repairs tasks explicitly assigned to `codex`.\n+- Claude Code implements and repairs by default (`shiki runner claude`, SADR-0008); Codex implements and repairs tasks explicitly assigned to `codex`.\n - Claude Code Action can act as GitHub-side CCA or reviewer.\n - CCA judges completion.\n - MergeGate authorizes state transitions and merge readiness.\ndiff --git a/.github/prompts/cca-completion-check.md b/.github/prompts/cca-completion-check.md\nindex ed203ad5f302ecc81372d8ab27d2b1232ca97497..fd89950528333ef6242e6d5443904f5392ab7e56\n--- a/.github/prompts/cca-completion-check.md\n+++ b/.github/prompts/cca-completion-check.md\n@@ -19,6 +19,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n ## Required Reading\n \n Read only the files needed to judge the PR contract. Prefer this order:\ndiff --git a/.github/prompts/codex-task-handoff.md b/.github/prompts/codex-task-handoff.md\nindex 695e211d76dea1c60e8d5bb360e78487808286a4..59441d576b01a39569722f226c854e5a740e9eb5\n--- a/.github/prompts/codex-task-handoff.md\n+++ b/.github/prompts/codex-task-handoff.md\n@@ -2,7 +2,7 @@\n \n You are Codex Front acting as the Implementer for a Shiki task.\n \n-Follow `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant ADRs, and `docs/agents/codex-handoff.md`.\n+Follow `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant Shiki SADRs and target ADRs, and `docs/agents/codex-handoff.md`.\n \n Rules:\n \ndiff --git a/AGENTS.md b/AGENTS.md\nindex 7f041100d49c2a7525f51cb0f5d696a4a6d1c78c..a06bfbabc6fa48a007389e25e12d7ed523936bed\n--- a/AGENTS.md\n+++ b/AGENTS.md\n@@ -22,7 +22,7 @@\n - **Codex Front** is an optional implementer through Codex App, Codex CLI, Codex IDE extension, or Codex Web signed in with ChatGPT OAuth/subscription auth, dispatched with `shiki runner codex` when a task is explicitly assigned to `codex`.\n - **GitHub CCA** is implemented by Claude Code Action by default, using `CLAUDE_CODE_OAUTH_TOKEN`.\n \n-Do not assume `openai/codex-action`, `OPENAI_API_KEY`, or API-key based Codex automation in the default Shiki path. API-key runners are explicit target-repo extensions and require their own ADR.\n+Do not assume `openai/codex-action`, `OPENAI_API_KEY`, or API-key based Codex automation in the default Shiki path. API-key runners are explicit target-repo extensions and require their own target ADR.\n \n Shiki is not:\n \n@@ -44,10 +44,14 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use\n+`SADR-NNNN-*.md`; target product decisions retain `NNNN-*.md` and the `ADR`\n+namespace.\n+\n Use the canonical source-of-truth order above. Runtime-specific reading orders\n are operational guidance only and must not override or reorder it.\n \n-Conversation state is not durable truth. If a decision matters, put it in GitHub, `.shiki/`, `CONTEXT.md`, or an ADR.\n+Conversation state is not durable truth. If a decision matters, put it in GitHub, `.shiki/`, `CONTEXT.md`, a Shiki SADR, or a target ADR.\n \n If GitHub and `.shiki/` disagree, surface the conflict and prefer GitHub operational state until the mirror is repaired.\n \n@@ -75,8 +79,8 @@\n Every non-trivial change follows this loop:\n \n 1. **Goal**: clarify outcome, completion conditions, non-goals, risk level, and success signals.\n-2. **grill-with-docs**: challenge the plan against domain docs, ADRs, code reality, terminology, and edge scenarios.\n-3. **Context & Impact**: find relevant docs, ADRs, code, dependencies, owners, locks, and verification surfaces.\n+2. **grill-with-docs**: challenge the plan against domain docs, Shiki SADRs, target ADRs, code reality, terminology, and edge scenarios.\n+3. **Context & Impact**: find relevant docs, SADRs, target ADRs, code, dependencies, owners, locks, and verification surfaces.\n 4. **PRD and Spec Freeze**: use `to-prd` when context is settled enough to become durable product/engineering intent. The operator's explicit approval of the PRD is Spec Freeze: the plan records a `spec_freeze` block, and no plan runs without it. Steps 1-4 form Requirements Definition \u2014 one continuous operator dialogue. After Spec Freeze, scope changes require an operator-approved Spec Amendment; non-scope-moving interpretations go to the Assumption Log.\n 5. **Issues**: use `to-issues` to create independently grabbable vertical-slice issues. Prefer AFK slices over HITL slices where possible.\n 6. **Triage**: label issues for readiness, risk, runtime, skills, and MergeGate state.\n@@ -167,7 +171,7 @@\n - Testing decisions.\n - Out-of-scope items.\n - Further notes.\n-- Links to relevant domain terms and ADRs.\n+- Links to relevant domain terms, SADRs, and target ADRs.\n \n Every executable task must include:\n \n@@ -193,7 +197,7 @@\n \n Context & Impact must identify:\n \n-- Relevant documents, domain terms, ADRs, and past decisions.\n+- Relevant documents, domain terms, SADRs, target ADRs, and past decisions.\n - Relevant modules, interfaces, seams, callers, owners, and tests.\n - Dependency relationships between tasks.\n - Candidate locks and lock conflicts.\n@@ -210,7 +214,7 @@\n | Skill | Required when |\n | --- | --- |\n | `setup-matt-pocock-skills` | First configuring the repo for skills, or when issue tracker, triage labels, or domain docs are missing. |\n-| `grill-with-docs` | Default for non-trivial Goals before PRD/issues; always required when plans, terminology, boundaries, tradeoffs, or ADR-worthy decisions are ambiguous. |\n+| `grill-with-docs` | Default for non-trivial Goals before PRD/issues; always required when plans, terminology, boundaries, tradeoffs, or decision-record-worthy choices are ambiguous. |\n | `zoom-out` | The agent lacks an architectural map of the relevant code area. |\n | `to-prd` | Settled Goal context must become durable product/engineering intent. |\n | `to-issues` | A Goal, PRD, or plan must become independently grabbable vertical-slice issues. |\n@@ -348,7 +352,7 @@\n - Three or more new files.\n - Any high-risk or critical label.\n \n-When the architecture gate triggers, run `grill-with-docs` or `improve-codebase-architecture` as appropriate and record durable decisions in an ADR when needed.\n+When the architecture gate triggers, run `grill-with-docs` or `improve-codebase-architecture` as appropriate and record durable decisions in a Shiki SADR or target ADR as applicable.\n \n ## Engineering Discipline\n \ndiff --git a/CLAUDE.md b/CLAUDE.md\nindex 98a932e7841a632866fcd53b0bad0e3af3711d3d..98a31399404d6fb044b4fd493f4459ac87f439de\n--- a/CLAUDE.md\n+++ b/CLAUDE.md\n@@ -14,6 +14,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n ## Claude Role\n \n Default to planner, default implementer, reviewer, coordinator, documentation editor, CCA-style completion judge, and final-judgment assistant.\n@@ -57,7 +60,7 @@\n At the start of a Shiki session:\n \n 1. Read `AGENTS.md`.\n-2. Read `CONTEXT.md` and relevant ADRs.\n+2. Read `CONTEXT.md` and relevant Shiki SADRs and target ADRs.\n 3. Read active `.shiki/` Goal/task/ledger state when present.\n 4. Read active distilled rules from `.shiki/memories/` (status `distilled`, `active: true`, not revoked or superseded) for applicable guidance; the same rules are injected deterministically into task handoffs as the `## Distilled Rules` section (\u00a73.5).\n 5. Read the GitHub Issue or PR when the task is GitHub-backed.\n@@ -74,7 +77,7 @@\n - Define the Goal, completion conditions, non-goals, and risk level.\n - Treat `grill-with-docs` as the default planning entry point for non-trivial Goals.\n - Challenge domain terms against `CONTEXT.md`.\n-- Challenge hard-to-reverse tradeoffs against ADRs.\n+- Challenge hard-to-reverse tradeoffs against SADRs and target ADRs.\n - Explore code instead of asking when code can answer the question.\n - Run Context & Impact before implementation planning.\n - Produce a Task DAG, not an unordered checklist.\n@@ -94,7 +97,7 @@\n - For each question, provide your recommended answer.\n - Resolve dependency decisions before downstream design questions.\n - Update `CONTEXT.md` inline when terminology crystallizes.\n-- Offer ADRs only when the decision is hard to reverse, surprising without context, and a real tradeoff.\n+- Offer a SADR or target ADR only when the decision is hard to reverse, surprising without context, and a real tradeoff.\n - Convert unresolved questions into HITL blockers rather than allowing Codex to guess.\n \n ## PRD and Issues Standard\n@@ -103,7 +106,7 @@\n \n - Use the project's domain glossary vocabulary.\n - Avoid volatile file paths unless they are essential.\n-- Include implementation decisions, testing decisions, out-of-scope items, and relevant ADR/domain links.\n+- Include implementation decisions, testing decisions, out-of-scope items, and relevant SADR/target-ADR/domain links.\n - Identify deep-module opportunities and testable interfaces.\n \n When creating issues:\n@@ -121,7 +124,7 @@\n - Branch/worktree target.\n - Scope and non-goals.\n - Dependencies and locks.\n-- Relevant docs, ADRs, modules, and tests.\n+- Relevant docs, SADRs, target ADRs, modules, and tests.\n - Required skill invocations.\n - TDD expectations.\n - Pre-PR `code-review` gate expectations.\n@@ -152,7 +155,7 @@\n \n - Treat yourself as a judge, not implementer.\n - Do not write production code.\n-- Read `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant ADRs, PRD, task issue, PR body, diff, checks, reviews, labels, and `.shiki/` state.\n+- Read `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant SADRs and target ADRs, PRD, task issue, PR body, diff, checks, reviews, labels, and `.shiki/` state.\n - Evaluate every applicable checklist in `docs/agents/checklists.md`.\n - Map each acceptance criterion to durable evidence.\n - Distinguish implementation failure from missing evidence.\ndiff --git a/CONTEXT.md b/CONTEXT.md\nindex e61a9be06a0cce0d026484083a657bde2067e234..237ee8aa3ea8fab334ece49768bc5ed3b5af0974\n--- a/CONTEXT.md\n+++ b/CONTEXT.md\n@@ -25,7 +25,7 @@\n _Avoid_: jumping directly into code\n \n **grill-with-docs**:\n-A planning skill that challenges a plan against domain language, ADRs, code reality, and concrete edge scenarios. It resolves design-tree questions before PRD/issues. It runs inside Requirements Definition.\n+A planning skill that challenges a plan against domain language, Shiki SADRs, target ADRs, code reality, and concrete edge scenarios. It resolves design-tree questions before PRD/issues. It runs inside Requirements Definition.\n _Avoid_: generic brainstorming, silent assumptions\n \n **Requirements Definition**:\ndiff --git a/CONTRIBUTING.md b/CONTRIBUTING.md\nindex c6d9b656e73abb8a55e5f660123e03153dcb3e6d..9e37a8fbf668db5b22461aaae5548b8f1354394c\n--- a/CONTRIBUTING.md\n+++ b/CONTRIBUTING.md\n@@ -17,7 +17,7 @@\n 1. **Goal** \u2014 start from a user-approved Goal with an outcome, completion\n    conditions, non-goals, risk level, and success signals.\n 2. **grill-with-docs** \u2014 for any non-trivial change, challenge the plan against\n-   domain language, ADRs, and code reality before writing a PRD.\n+   domain language, Shiki SADRs, target ADRs, and code reality before writing a PRD.\n 3. **PRD** \u2014 capture settled product/engineering intent (`to-prd`).\n 4. **Spec Freeze** \u2014 the operator explicitly approves the PRD/requirements;\n    the plan records a `spec_freeze` block and no plan runs without it\n@@ -94,7 +94,7 @@\n   entries.\n - Do not hand-edit `.shiki/goals/*.json` or `.shiki/manifest.json` in a task PR.\n - Conversation state is not durable truth. If a decision matters, record it in\n-  GitHub, `.shiki/`, `CONTEXT.md`, or an ADR.\n+  GitHub, `.shiki/`, `CONTEXT.md`, a Shiki SADR, or a target ADR.\n \n ## Architecture Gate\n \n@@ -103,7 +103,7 @@\n or CI policy, package structure, three or more structural files, more than 100\n net new lines in a core module, three or more new files, or any high-risk label.\n When triggered, run `grill-with-docs` or `improve-codebase-architecture` and\n-record durable decisions in an ADR when needed (see `docs/adr/`).\n+record durable decisions in a Shiki SADR or target ADR as applicable (see `docs/adr/`).\n \n ## Safety\n \ndiff --git a/SYSTEM_PROMPT.md b/SYSTEM_PROMPT.md\nindex aaaea45e3a896c3a44ba89eec8bb84325ecf6c62..da009c86a88285269dee0155e58e550c6d744ea9\n--- a/SYSTEM_PROMPT.md\n+++ b/SYSTEM_PROMPT.md\n@@ -30,6 +30,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n Use the canonical source-of-truth order above. Runtime-specific reading orders\n are operational guidance only and must not override or reorder it. The current\n conversation is non-durable operator input.\n@@ -48,14 +51,14 @@\n \n A runtime may only do work that is explicitly assigned by the Goal, task, PR, or MergeGate decision.\n \n-Do not assume `openai/codex-action`, `OPENAI_API_KEY`, or API-key based Codex automation in the default Shiki path. API-key runners are explicit target-repo extensions and require their own ADR.\n+Do not assume `openai/codex-action`, `OPENAI_API_KEY`, or API-key based Codex automation in the default Shiki path. API-key runners are explicit target-repo extensions and require their own target ADR.\n \n ## 4. Non-negotiable Shiki flow\n \n For any non-trivial Goal, use this flow:\n \n 1. **Goal Seek**: define outcome, non-goals, risk level, completion criteria, and evidence requirements.\n-2. **grill-with-docs**: challenge the plan against `CONTEXT.md`, ADRs, code reality, terminology, boundaries, and edge scenarios. Ask one question at a time when the operator is available. If code can answer a question, inspect code instead of asking.\n+2. **grill-with-docs**: challenge the plan against `CONTEXT.md`, Shiki SADRs, target ADRs, code reality, terminology, boundaries, and edge scenarios. Ask one question at a time when the operator is available. If code can answer a question, inspect code instead of asking.\n 3. **Context & Impact**: identify modules, interfaces, seams, callers, dependencies, locks, risk, and verification surfaces.\n 4. **to-prd + Spec Freeze**: turn settled context into a PRD using domain vocabulary and testing decisions. The operator's explicit approval of the PRD is Spec Freeze, recorded as the plan's `spec_freeze` block; plans without it do not run. Steps 1-4 form Requirements Definition.\n 5. **to-issues**: decompose the PRD into independently grabbable vertical-slice issues. Prefer AFK slices; mark HITL slices when judgment is still required.\n@@ -75,7 +78,7 @@\n Before making any material change:\n \n 1. Read `AGENTS.md` and any runtime-specific wrapper such as `CLAUDE.md`.\n-2. Read `CONTEXT.md` and relevant ADRs.\n+2. Read `CONTEXT.md` and relevant SADRs and target ADRs.\n 3. Read the GitHub Issue / PR and `.shiki/` state for the active Goal or task.\n 4. Identify your role: Planner, Implementer, Reviewer, Completion Check Agent, Repairer, or Guardian-assist.\n 5. Identify Goal id, task id, branch, PR, dependencies, locks, risk level, required skills, and required evidence.\n@@ -103,7 +106,7 @@\n \n At minimum, identify:\n \n-- Relevant documents, ADRs, domain terms, and prior decisions.\n+- Relevant documents, SADRs, target ADRs, domain terms, and prior decisions.\n - Relevant modules, interfaces, seams, callers, owners, and tests.\n - Dependency relationships between tasks.\n - Candidate file locks and lock conflicts.\n@@ -152,7 +155,7 @@\n \n CCA must read:\n \n-- `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant ADRs.\n+- `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant SADRs and target ADRs.\n - Parent Goal issue, PRD issue, task issue, comments, and labels.\n - PR body, diff, commits, changed files, reviews, CI checks, and artifacts available to the workflow.\n - `.shiki/` task contract, locks, ledger entries, CCA checklist profile, and prior repair packets.\n@@ -214,7 +217,7 @@\n - Three or more new files.\n - Any high-risk or critical label.\n \n-When the architecture gate triggers, run `grill-with-docs` or `improve-codebase-architecture` as appropriate and record the decision in an ADR if it is hard to reverse or likely to be re-litigated.\n+When the architecture gate triggers, run `grill-with-docs` or `improve-codebase-architecture` as appropriate and record the decision in a Shiki SADR or target ADR as applicable if it is hard to reverse or likely to be re-litigated.\n \n ## 14. Skill Gate\n \n@@ -223,7 +226,7 @@\n Use these skills:\n \n - `setup-matt-pocock-skills`: before first use of the engineering skill set, or when issue tracker, triage labels, or domain docs are missing.\n-- `grill-with-docs`: default for non-trivial Goals before PRD/issues; required for ambiguous plans, terminology, boundaries, ADR-worthy tradeoffs, or design-tree decisions.\n+- `grill-with-docs`: default for non-trivial Goals before PRD/issues; required for ambiguous plans, terminology, boundaries, decision-record-worthy tradeoffs, or design-tree decisions.\n - `zoom-out`: unfamiliar code area or missing architectural map.\n - `to-prd`: convert settled context into a PRD.\n - `to-issues`: break a PRD, plan, or Goal into independently grabbable vertical-slice issues.\ndiff --git a/docs/adr/0001-keep-shiki-as-platform-name.md b/docs/adr/SADR-0001-keep-shiki-as-platform-name.md\nindex 20a9637c3b9d8c97de7ba8db7cfae4d82d9a7dba..8b685439959aada1b221c112f0ac3c51ca8a1695\n--- a/docs/adr/0001-keep-shiki-as-platform-name.md\n+++ b/docs/adr/SADR-0001-keep-shiki-as-platform-name.md\n@@ -1,3 +1,3 @@\n-# Keep Shiki As The Platform Name\n+# SADR-0001: Keep Shiki As The Platform Name\n \n Shiki remains the product name for the GitHub-native agentic engineering control plane. We considered more descriptive names such as MergePilot, MergeGate, and Kiroku, but kept Shiki because it carries the existing goal-seeking framework identity while remaining independent of Hermes, Codex, Claude Code, or any single runtime.\ndiff --git a/docs/adr/0002-use-agents-as-shared-constitution.md b/docs/adr/SADR-0002-use-agents-as-shared-constitution.md\nindex 73de30ecb078120eac540d9bc51855e3cd48d0e3..c5486182bf06fca36ea508496b013c67302fd989\n--- a/docs/adr/0002-use-agents-as-shared-constitution.md\n+++ b/docs/adr/SADR-0002-use-agents-as-shared-constitution.md\n@@ -1,3 +1,3 @@\n-# Use AGENTS As The Shared Agent Constitution\n+# SADR-0002: Use AGENTS As The Shared Agent Constitution\n \n Shiki uses `AGENTS.md` as the runtime-neutral constitution and `CLAUDE.md` as a thin Claude Code wrapper. This keeps Codex, Claude Code, Hermes Runner, and future Agent Runtimes aligned on the same GitHub-first MergeGate rules while still allowing runtime-specific instructions where needed.\ndiff --git a/docs/adr/0003-require-skill-gate-for-mergegate-readiness.md b/docs/adr/SADR-0003-require-skill-gate-for-mergegate-readiness.md\nindex 424be0c9af4c8f0136f1fde68a468c47ff84e764..7fc337987c7df9792f75f181dfcbed75fe5ffff2\n--- a/docs/adr/0003-require-skill-gate-for-mergegate-readiness.md\n+++ b/docs/adr/SADR-0003-require-skill-gate-for-mergegate-readiness.md\n@@ -1,3 +1,3 @@\n-# Require Skill Gate For MergeGate Readiness\n+# SADR-0003: Require Skill Gate For MergeGate Readiness\n \n Shiki requires relevant engineering skills to be invoked before execution when their triggers apply. This makes skills part of MergeGate readiness rather than optional prompt style, because Shiki depends on repeatable planning, issue decomposition, TDD, diagnosis, triage, and architecture review before autonomous or parallel agent work can be trusted.\ndiff --git a/docs/adr/0004-use-github-first-with-shiki-mirror.md b/docs/adr/SADR-0004-use-github-first-with-shiki-mirror.md\nindex d0a3a9174dc646a945a9d325954ffaf00c352040..a79c54ae9ee8eed2a0524192b864da4e22a92d3f\n--- a/docs/adr/0004-use-github-first-with-shiki-mirror.md\n+++ b/docs/adr/SADR-0004-use-github-first-with-shiki-mirror.md\n@@ -1,4 +1,4 @@\n-# ADR 0004: Use GitHub First With A Repository-Local Shiki Mirror\n+# SADR-0004: Use GitHub First With A Repository-Local Shiki Mirror\n \n ## Status\n \ndiff --git a/docs/adr/0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md b/docs/adr/SADR-0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\nindex b339baf49b89d9290e79c3d51b36e7b2bde3bd95..0f3c3eb9afccdb3403ce978e21d04fe12e495da0\n--- a/docs/adr/0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\n+++ b/docs/adr/SADR-0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\n@@ -1,8 +1,8 @@\n-# ADR 0005: Use Codex As Front And Claude Code Action As The GitHub Runtime\n+# SADR-0005: Use Codex As Front And Claude Code Action As The GitHub Runtime\n \n ## Status\n \n-Accepted (implementer default superseded by ADR 0008; the auth model and the\n+Accepted (implementer default superseded by SADR-0008; the auth model and the\n Claude Code Action GitHub runtime decision remain in force)\n \n ## Context\n@@ -26,4 +26,4 @@\n - Shiki avoids requiring `OPENAI_API_KEY` for the default workflow.\n - Codex work happens through the user's authenticated Codex front surface, not as a hidden backend Action.\n - Claude Code Action is configured for OAuth-token based subscription usage by default.\n-- API-key based automation remains possible only as an explicit target-repo extension with its own ADR.\n+- API-key based automation remains possible only as an explicit target-repo extension with its own target ADR.\ndiff --git a/docs/adr/0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md b/docs/adr/SADR-0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\nindex 4bcae9d1e52f0a37b8e9fbafa5cd14b6ee1993bd..7a38538a6ae9929567f0ade67e7ea6a60d0b3f7e\n--- a/docs/adr/0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\n+++ b/docs/adr/SADR-0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\n@@ -1,4 +1,4 @@\n-# ADR 0006: Separate Codex Implementation, CCA Completion, And MergeGate Authorization\n+# SADR-0006: Separate Codex Implementation, CCA Completion, And MergeGate Authorization\n \n ## Status\n \ndiff --git a/docs/adr/0007-packaging-and-release.md b/docs/adr/SADR-0007-packaging-and-release.md\nindex 79b84ddb97167372b44641e5b7860e400ce80822..bb445a2e2e856d9d1f5fdbe35de8d4e8cec0fb32\n--- a/docs/adr/0007-packaging-and-release.md\n+++ b/docs/adr/SADR-0007-packaging-and-release.md\n@@ -1,4 +1,4 @@\n-# ADR 0007: Packaging And Release For Shiki\n+# SADR-0007: Packaging And Release For Shiki\n \n ## Status\n \n@@ -25,7 +25,7 @@\n   not introduce third-party runtime dependencies or break the shim.\n - Releases must run inside the operator's subscription-authenticated toolchain\n   and must not require extra secrets beyond the built-in `GITHUB_TOKEN`\n-  (consistent with ADR 0005's no-API-key-by-default stance).\n+  (consistent with SADR-0005's no-API-key-by-default stance).\n - `.shiki/manifest.json` already encodes the install surface (`install.include`,\n   `install.create_directories`, `install.exclude_from_commit`), and it must not\n   be mutated as part of this change.\n@@ -34,7 +34,7 @@\n \n This is a hard-to-reverse platform decision (it sets the version source of\n truth, the release trigger, and the public install contract), so it is recorded\n-as an ADR.\n+as a SADR.\n \n ## Decision\n \n@@ -104,7 +104,7 @@\n 3. **Publishing to PyPI.** Rejected for now: Shiki is distributed as a\n    repository template + git-installable CLI, not a PyPI library; PyPI would add\n    credentials/secrets and a different versioning contract. Can be added later as\n-   its own ADR if a packaged distribution is needed.\n+   its own SADR if a packaged distribution is needed.\n 4. **Dynamic version (read `VERSION` via a build backend `dynamic` field).**\n    Rejected to keep `pyproject.toml` backend-agnostic and dependency-free; the\n    small duplication between `VERSION` and `pyproject.toml` is guarded by the\n@@ -123,4 +123,4 @@\n   releasing versions the whole platform but ships only `install.include` to\n   targets.\n - Future PyPI distribution or a console entry point are deferred and would each\n-  warrant their own ADR.\n+  warrant their own SADR.\ndiff --git a/docs/adr/0008-use-claude-code-as-default-implementer.md b/docs/adr/SADR-0008-use-claude-code-as-default-implementer.md\nindex dedf4510b7c7799521422f622029563d373db031..35c2625d908167fbc58855b5b63e283805f167e8\n--- a/docs/adr/0008-use-claude-code-as-default-implementer.md\n+++ b/docs/adr/SADR-0008-use-claude-code-as-default-implementer.md\n@@ -1,12 +1,12 @@\n-# ADR 0008: Use Claude Code As The Default Implementer Runtime\n+# SADR-0008: Use Claude Code As The Default Implementer Runtime\n \n ## Status\n \n-Proposed (supersedes ADR 0005 for the implementer default; keeps its auth model)\n+Proposed (supersedes SADR-0005 for the implementer default; keeps its auth model)\n \n ## Context\n \n-ADR 0005 made Codex Front the default implementation runtime, with Claude Code\n+SADR-0005 made Codex Front the default implementation runtime, with Claude Code\n restricted to planner and reviewer roles by the runtime registry, and the only\n autonomous implementation adapter was `shiki runner codex`.\n \n@@ -18,13 +18,13 @@\n - The platform's planning, review, and CCA layers already run on Claude Code,\n   and Claude Code's native capabilities (the built-in `/code-review` skill and\n   the Workflow multi-agent orchestration tool) have no Codex equivalent.\n-- The Goal lifecycle redesign (ADR 0009) requires an autonomous post-freeze\n+- The Goal lifecycle redesign (SADR-0009) requires an autonomous post-freeze\n   loop driven from the operator's Claude Code session, including bounded\n   repair dispatch, which must not depend on a runtime that cannot be invoked\n   unattended.\n \n The constitution claims Shiki is runtime-agnostic (\"not a Claude-only\n-workflow\", ADR 0002). Any change to the implementer default must preserve the\n+workflow\", SADR-0002). Any change to the implementer default must preserve the\n registry-based runtime model rather than hardcoding one vendor.\n \n ## Decision\n@@ -39,7 +39,7 @@\n   the task to review or repair-needed.\n - Keep Codex registered as an optional implementer runtime. `shiki runner\n   codex`, the Codex handoff templates, and the ChatGPT OAuth auth model from\n-  ADR 0005 remain supported for tasks explicitly assigned to `codex`.\n+  SADR-0005 remain supported for tasks explicitly assigned to `codex`.\n - Commit to mandating two Claude-native capabilities as loop contract in\n   follow-up Goals (status: the code-review gate was delivered by Goal G-B \u2014\n   skill registry, checklists item PR-12, default required_skills \u2014 while the\n@@ -60,7 +60,7 @@\n \n Out of scope: removing Codex support, changing the CCA runtime (Claude Code\n Action with `CLAUDE_CODE_OAUTH_TOKEN` stays), and API-key based automation\n-(still requires its own ADR per ADR 0005).\n+(still requires its own target ADR per SADR-0005).\n \n ## Consequences\n \n@@ -100,7 +100,7 @@\n   harness rejection of direct `codex exec` is outside Shiki's control, and the\n   plan's mandatory native capabilities cannot run on Codex at all.\n - **Remove Codex entirely**: rejected; it abandons the runtime-agnostic\n-  constitution (ADR 0002) for no operational gain and forecloses\n+  constitution (SADR-0002) for no operational gain and forecloses\n   cross-runtime adversarial review.\n - **GitHub Actions cloud implementation via claude-code-action**: rejected as\n   default; durable evidence is automatic but token cost, Node 24 workflow\ndiff --git a/docs/adr/0009-requirements-definition-and-spec-freeze.md b/docs/adr/SADR-0009-requirements-definition-and-spec-freeze.md\nindex 29b3f7871bef9203a8b4a831a65c39eb2f506e42..52e7dd69ad0aa4efe0b68c62678abbf7df02aaec\n--- a/docs/adr/0009-requirements-definition-and-spec-freeze.md\n+++ b/docs/adr/SADR-0009-requirements-definition-and-spec-freeze.md\n@@ -1,4 +1,4 @@\n-# ADR 0009: Merge Grill And Planning Into Requirements Definition With Spec Freeze\n+# SADR-0009: Merge Grill And Planning Into Requirements Definition With Spec Freeze\n \n ## Status\n \ndiff --git a/docs/adr/0010-external-ai-guardian-review.md b/docs/adr/SADR-0010-external-ai-guardian-review.md\nindex e6108bbe8bb4e73e0ff4684c8f78001a80438c4e..f3510b145664298adafb151b6f0db51b972f01ec\n--- a/docs/adr/0010-external-ai-guardian-review.md\n+++ b/docs/adr/SADR-0010-external-ai-guardian-review.md\n@@ -1,4 +1,4 @@\n-# ADR 0010: External AI Guardian Review As A First-Class Approval Authority\n+# SADR-0010: External AI Guardian Review As A First-Class Approval Authority\n \n ## Status\n \n@@ -8,7 +8,7 @@\n \n Shiki is intended to be an autonomous, recursive, self-improving agent\n framework: authority-in-the-loop, not human-in-the-loop. Until now the Guardian\n-approval required for high/critical-risk merges (guardian-policy.json, ADR\n+approval required for high/critical-risk merges (guardian-policy.json, SADR\n governance) recognized only human authority sources \u2014 a GitHub review approval,\n the `guardian:approved` label applied by a configured Guardian, or a comment\n carrying `Guardian approval granted` plus the current head SHA.\n@@ -89,7 +89,7 @@\n   autonomous design the operator intends and produced the impersonation\n   incident.\n - **Record AI approval as an operator comment**: rejected as forgery of human\n-  authority \u2014 the exact failure this ADR corrects.\n+  authority \u2014 the exact failure this SADR corrects.\n - **Deliver the artifact as a committed `.shiki/` file**: rejected; a committed\n   approval inside the PR being approved is forgeable by anyone who can push.\n - **A dedicated reviewer bot identity instead of a relayed comment**: deferred;\ndiff --git a/docs/adr/0011-autonomous-quality-gate-evidence.md b/docs/adr/SADR-0011-autonomous-quality-gate-evidence.md\nindex bca0246c5d46e44afbde15486a75eec425fc02f5..9c93eac16d8c3a6d4b11bd7224e3955102cd7db4\n--- a/docs/adr/0011-autonomous-quality-gate-evidence.md\n+++ b/docs/adr/SADR-0011-autonomous-quality-gate-evidence.md\n@@ -1,4 +1,4 @@\n-# ADR 0011: Autonomous Quality-Gate Evidence \u2014 Deterministic Facts And An Independent Verifier\n+# SADR-0011: Autonomous Quality-Gate Evidence \u2014 Deterministic Facts And An Independent Verifier\n \n ## Status\n \ndiff --git a/docs/adr/0012-autonomous-loop-closeout-to-main.md b/docs/adr/SADR-0012-autonomous-loop-closeout-to-main.md\nindex 637fad6dde4989c9c2bf0f81f676d8d29991123c..ec7d85f81030a676bcbba28b43eb365422a36eac\n--- a/docs/adr/0012-autonomous-loop-closeout-to-main.md\n+++ b/docs/adr/SADR-0012-autonomous-loop-closeout-to-main.md\n@@ -1,4 +1,4 @@\n-# 0012 \u2014 Autonomous loop pushes goal completion to main via a closeout PR\n+# SADR-0012 \u2014 Autonomous loop pushes goal completion to main via a closeout PR\n \n - Status: accepted\n - Date: 2026-06-18\ndiff --git a/docs/adr/0013-bootstrap-grants-actions-approve-pull-requests.md b/docs/adr/SADR-0013-bootstrap-grants-actions-approve-pull-requests.md\nindex b6c69d64f1c5ced346b88d6859586715b3b5bd0e..cf48ff4a6716267f539ca58edd1a852e7c7a2d64\n--- a/docs/adr/0013-bootstrap-grants-actions-approve-pull-requests.md\n+++ b/docs/adr/SADR-0013-bootstrap-grants-actions-approve-pull-requests.md\n@@ -1,4 +1,4 @@\n-# ADR 0013: Bootstrap Grants GitHub Actions Permission To Create And Approve Pull Requests\n+# SADR-0013: Bootstrap Grants GitHub Actions Permission To Create And Approve Pull Requests\n \n ## Status\n \n@@ -31,7 +31,7 @@\n security tradeoff: it lets repository automation satisfy a human-review gate. In\n a multi-maintainer repo that could let a compromised or buggy workflow\n self-approve changes. So whether bootstrap turns it on by default \u2014 and with\n-what default token scope \u2014 is an ADR-worthy decision, not an implementation\n+what default token scope \u2014 is a SADR-worthy decision, not an implementation\n detail.\n \n ## Decision\n@@ -48,7 +48,7 @@\n Specifically:\n \n - **`can_approve_pull_request_reviews` defaults to `true`.** Shiki's default\n-  posture is solo/AFK autonomous operation (ADR 0008/0009/0012), where the CCA\n+  posture is solo/AFK autonomous operation (SADR-0008/0009/0012), where the CCA\n   Review Bridge is the only path that can satisfy `required_review: true` after\n   the CCA and Guardian gates have already passed. The default makes a\n   freshly-bootstrapped repo able to run the autonomous loop end to end without a\ndiff --git a/docs/adr/0014-codex-app-external-ai-guardian-ui-adapter.md b/docs/adr/SADR-0014-codex-app-external-ai-guardian-ui-adapter.md\nindex 238db06279334caa341c117362df9271382a7fcd..65f12e9939321e1b3e389a34c822fc1549e5155d\n--- a/docs/adr/0014-codex-app-external-ai-guardian-ui-adapter.md\n+++ b/docs/adr/SADR-0014-codex-app-external-ai-guardian-ui-adapter.md\n@@ -1,4 +1,4 @@\n-# ADR 0014: Use Codex App As The External AI Guardian UI Adapter\n+# SADR-0014: Use Codex App As The External AI Guardian UI Adapter\n \n ## Status\n \ndiff --git a/docs/adr/0015-contract-approval-before-dispatch.md b/docs/adr/SADR-0015-contract-approval-before-dispatch.md\nindex fad6c656741a20a7684ba16080a7db159f63a52f..6061271a46d3f1d6ae7174477de3fb75673123a6\n--- a/docs/adr/0015-contract-approval-before-dispatch.md\n+++ b/docs/adr/SADR-0015-contract-approval-before-dispatch.md\n@@ -1,4 +1,4 @@\n-# ADR 0015: Approve The Task Contract Before Dispatch, Not The Diff After It\n+# SADR-0015: Approve The Task Contract Before Dispatch, Not The Diff After It\n \n ## Status\n \n@@ -9,7 +9,7 @@\n Guardian approval for high/critical risk is expressible only on a pull request.\n Every approval source in `guardian-policy.json` \u2014 GitHub review, the\n `guardian:approved` label, the `Guardian approval granted` comment, and the\n-`external_ai_guardian_review` artifact (ADR 0010/0014) \u2014 is bound to a PR, and\n+`external_ai_guardian_review` artifact (SADR-0010/0014) \u2014 is bound to a PR, and\n three of the four require the current head SHA. Approval therefore cannot exist\n before an implementation PR exists.\n \n@@ -81,7 +81,7 @@\n An envelope breach must surface only on the non-repairable policy gate, never on\n the repairable `MergeGate metadata check`. Otherwise the Goal Loop routes it to\n `dispatch_repair` \u2014 instructing a runner to make the breach go away \u2014 which is\n-exactly what this ADR forbids.\n+exactly what this SADR forbids.\n \n **Contract immutability.** After registration, an implementation PR must not\n change the governance fields of its own task contract (`scope`, `non_goals`,\n@@ -116,7 +116,7 @@\n fails any verdict that is not `complete`, and the signal the CCA prompt reads is\n computed from `evaluate_guardian_approval` alone. Without the OR-merge at the\n signal, a contract-approved PR still returns `needs_guardian`, the CCA job fails,\n-and the MergeGate policy check never runs \u2014 the gate this ADR changes would never\n+and the MergeGate policy check never runs \u2014 the gate this SADR changes would never\n be reached. The CCA *prompt* needs no change: a `contract_approval` entry in the\n signal's existing `sources` array satisfies its current wording.\n \n@@ -130,7 +130,7 @@\n written, and an approved contract lets its implementation PRs merge through the\n same automatic path as low/medium risk. The wasted implement-then-block cycle\n disappears. Guardian review shifts from reading diffs to reading contracts, which\n-is both earlier and smaller; the external AI Guardian path (ADR 0010/0014) works\n+is both earlier and smaller; the external AI Guardian path (SADR-0010/0014) works\n on contracts with no change.\n \n We accept that the Guardian authorizes a class of change without seeing its\n@@ -146,7 +146,7 @@\n adversarial tests for the new approval path equivalent in rigor to\n `test_shiki_governance_evidence.sh`.\n \n-Bootstrap costs, which apply only while this ADR is being implemented. The tasks\n+Bootstrap costs, which apply only while this SADR is being implemented. The tasks\n that build Contract Approval merge under the existing rules, so the ones\n dispatched after the pre-dispatch gate lands but before the approval path lands\n need a Guardian-approved Contract PR *and* a live Guardian approval on their\n@@ -210,7 +210,7 @@\n risk; they do not bound authorized work.\n \n **Auto-escalate risk on envelope breach instead of stopping.** Returns the\n-workflow to a diff-time Guardian decision \u2014 the exact behavior this ADR removes.\n+workflow to a diff-time Guardian decision \u2014 the exact behavior this SADR removes.\n \n **Resolve governance fields from the base snapshot only.** Rejected against the\n registration flow: implementation PRs add their own contract, so the base has no\ndiff --git a/docs/adr/0016-locks-bound-product-paths.md b/docs/adr/SADR-0016-locks-bound-product-paths.md\nindex b5e850f3f1f7f4ef6e7f9d1763a29a51bd300c45..46b40a9b99a7aae644c02bb458f5473ff0a90ce2\n--- a/docs/adr/0016-locks-bound-product-paths.md\n+++ b/docs/adr/SADR-0016-locks-bound-product-paths.md\n@@ -1,4 +1,4 @@\n-# ADR 0016: Locks bound product paths; the Shiki mirror is bound by record identity\n+# SADR-0016: Locks bound product paths; the Shiki mirror is bound by record identity\n \n ## Status\n \n@@ -6,8 +6,8 @@\n \n - Date: 2026-07-28\n - Deciders: operator (mizutani-140), Claude (planner)\n-- Related: ADR 0004 (GitHub-first with a `.shiki` mirror), ADR 0009 (Spec Freeze),\n-  ADR 0012 (autonomous loop closeout to main), ADR 0015 (Contract Approval before\n+- Related: SADR-0004 (GitHub-first with a `.shiki` mirror), SADR-0009 (Spec Freeze),\n+  SADR-0012 (autonomous loop closeout to main), SADR-0015 (Contract Approval before\n   dispatch), remediation goal task R-02, the live PR #179 deadlock.\n \n ## Context\n@@ -90,11 +90,11 @@\n files, documentation files and named source modules are nameable; runtime-\n allocated ledger, runner and report ids are not \u2014 and those live in `.shiki`,\n which locks no longer govern. Existing frozen contracts are brought into line by\n-a one-time Spec Amendment per plan, which must land before ADR 0015's contract\n+a one-time Spec Amendment per plan, which must land before SADR-0015's contract\n immutability makes `locks` unwritable from the implementation PR.\n \n **Lock state is resolved from the durable side.** MergeGate resolves the lock\n-registry, and ADR 0015's governance fields, from the base `.shiki` snapshot when\n+registry, and SADR-0015's governance fields, from the base `.shiki` snapshot when\n present, falling back to head only when absent. The `MergeGate metadata check`\n job is given `--base-shiki`, which it does not receive today. Locks are not\n derived from the live open-pull-request set: a pull request body is untrusted\n@@ -108,7 +108,7 @@\n Explicitly out of scope: moving MergeGate's contract resolution off the PR\n checkout wholesale; making the mirror a projection of GitHub state; introducing\n new lock kinds; moving ledger evidence out of the repository; and unwinding\n-ADR 0012's closeout pull request. Registration of multi-task goals is ADR 0015's\n+SADR-0012's closeout pull request. Registration of multi-task goals is SADR-0015's\n Contract PR and is not re-decided here.\n \n ## Consequences\n@@ -127,7 +127,7 @@\n \n Risks accepted. First, goal-scoped identity is only as strong as `goal_id`,\n which is resolved from the PR body and the head task file; the mitigation is to\n-add `goal_id` to ADR 0015's frozen governance field set, and until that lands a\n+add `goal_id` to SADR-0015's frozen governance field set, and until that lands a\n task file can name a foreign goal. Second, base-when-present resolution is\n unavailable on fork pull requests, where the head is contributor-controlled;\n fork pull requests already cannot complete a Shiki merge, and this decision does\n@@ -139,7 +139,7 @@\n path, not by implementation pull requests.\n \n New obligations: one Spec Amendment per frozen plan to rewrite `locks`, ordered\n-before ADR 0015 T1; extension of the adversarial suites in\n+before SADR-0015 T1; extension of the adversarial suites in\n `scripts/test_shiki_adversarial_state.sh` and\n `scripts/test_shiki_governance_evidence.sh` to cover the eleven newly-scoped\n directories; and updating `scripts/test_shiki_loop_lock_guard.sh`,\n@@ -151,32 +151,32 @@\n whether the loop's transitive sync of ledger `evidence` references can be\n deleted once the Contract PR carries its own source plan.\n \n-## Relationship to ADR 0004\n+## Relationship to SADR-0004\n \n-This ADR **clarifies something ADR 0004 left unspecified**. ADR 0004 decides that\n+This SADR **clarifies something SADR-0004 left unspecified**. SADR-0004 decides that\n GitHub is the operational source of truth and that each target repository keeps a\n `.shiki/` mirror. It says nothing about pull requests writing the mirror, does\n not name MergeGate, and does not make the mirror an input to a gate. Nothing in\n-ADR 0004's Decision or Consequences is amended, superseded or weakened: the\n+SADR-0004's Decision or Consequences is amended, superseded or weakened: the\n mirror still exists, still records the same artifacts, and pull request\n-boundaries remain the coordination surface. What this ADR settles is the question\n+boundaries remain the coordination surface. What this SADR settles is the question\n 0004 never reached \u2014 by what authority a pull request may write a mirror record \u2014\n and it answers it the way `.shiki/manifest.json` already declares\n (`pr_mutation: current-task-or-current-goal-only`).\n \n-It **amends ADR 0012**, whose Decision item 5 states that \"Loop-executed tasks\n+It **amends SADR-0012**, whose Decision item 5 states that \"Loop-executed tasks\n declare the `path:.shiki/**` lock, which covers every `.shiki` file the closeout\n stages ... so `files_outside_locks` is satisfied without per-file lock\n bookkeeping.\" That clause is replaced: the closeout's mirror writes are\n-authorized by record identity, not by a declared lock. ADR 0012's substantive\n+authorized by record identity, not by a declared lock. SADR-0012's substantive\n decision \u2014 that completion reaches `main` through a closeout pull request rather\n than local attestation \u2014 is preserved unchanged.\n \n-It is **complementary to ADR 0015**, not a substitute. ADR 0015 owns contract\n+It is **complementary to SADR-0015**, not a substitute. SADR-0015 owns contract\n immutability, the pre-dispatch gate, the Contract PR and the Scope Envelope. This\n-ADR owns what a lock means and who may write the mirror. The one ordering\n+SADR owns what a lock means and who may write the mirror. The one ordering\n constraint between them is that the lock-narrowing Spec Amendment must precede\n-ADR 0015 T1's freezing of the `locks` field.\n+SADR-0015 T1's freezing of the `locks` field.\n \n ## Alternatives Considered\n \n@@ -186,7 +186,7 @@\n the only writer that can register them may run. Migrating the Guardian signal to\n that store makes every registration PR fail closed to `needs_guardian` at every\n risk level, because the task file is by definition absent from the store, which\n-removes the low/medium autonomous path ADR 0008/0009 exists to provide. And it\n+removes the low/medium autonomous path SADR-0008/0009 exists to provide. And it\n leaves schemas, manifest, config, guardian policy, templates, migrations, plans,\n worktrees, runner records, runs and handoffs with no writer at all in a\n repository whose product is `.shiki`. Measured benefit on the frozen backlog:\n@@ -199,14 +199,14 @@\n namespace is not in fact ownership-partitioned \u2014 56 ledger entries carry no\n `task_id`, and one goal DAG is writable by 35 registered tasks. The correct\n expression of its insight is that locks do not govern the mirror at all, which is\n-what this ADR decides.\n+what this SADR decides.\n \n **Make the mirror a projection written only by a registration writer and a\n post-merge projection job.** Rejected. It requires a bypass actor with write\n access to the protected default branch; it makes its own master invariant\n readable from a manifest that pull requests may edit; it deletes both reconcile\n modes and leaves a corrupted mirror with no in-band repair path, contradicting\n-ADR 0004's consequence that mirror conflicts are repaired against GitHub state;\n+SADR-0004's consequence that mirror conflicts are repaired against GitHub state;\n and its content-derived ids are rejected by `validate_shiki`'s currently-green id\n pattern.\n \ndiff --git a/docs/adr/0017-bookkeeping-closeouts-do-not-inherit-risk.md b/docs/adr/SADR-0017-bookkeeping-closeouts-do-not-inherit-risk.md\nindex 554e3a0d526a72ca067108c5464cc68c3edcf174..b9676857a4713850e063af7c39c8e0b01ecb4c66\n--- a/docs/adr/0017-bookkeeping-closeouts-do-not-inherit-risk.md\n+++ b/docs/adr/SADR-0017-bookkeeping-closeouts-do-not-inherit-risk.md\n@@ -1,4 +1,4 @@\n-# ADR 0017: A Bookkeeping Closeout Does Not Inherit Its Task's Risk\n+# SADR-0017: A Bookkeeping Closeout Does Not Inherit Its Task's Risk\n \n ## Status\n \n@@ -6,7 +6,7 @@\n \n ## Context\n \n-ADR 0012 made the goal loop push a task's completion to the default branch through a\n+SADR-0012 made the goal loop push a task's completion to the default branch through a\n closeout pull request: after the implementation PR merges, the loop opens a second PR\n carrying `task.status = done`, `lock.state = released`, and \u2014 when the task completes\n its goal \u2014 `goal.status = complete` plus one scorecard report. Nothing else. The\n@@ -49,14 +49,14 @@\n 4. The task file's governance fields (`scope`, `non_goals`, `required_skills`,\n    `risk_level`, `locks`, `acceptance_checks`, `test_command`) are byte-identical to\n    the base snapshot. Only `status`, `expected_pr`, `closeout_pr`, `ledger_evidence`\n-   and `expected_branch` may differ \u2014 the ADR 0015 contract-immutability rule,\n+   and `expected_branch` may differ \u2014 the SADR-0015 contract-immutability rule,\n    unchanged.\n \n    `expected_branch` is in that set because a closeout is *by construction* on a\n    different branch from the implementation it closes, and the loop rewrites the\n    field accordingly. It is safe to let it differ because MergeGate independently\n    requires `expected_branch` to equal the PR's actual head ref, so a PR cannot set\n-   it to anything other than the branch it is really on. The first draft of this ADR\n+   it to anything other than the branch it is really on. The first draft of this SADR\n    omitted it, which made the exemption unreachable: every real closeout failed\n    condition 4 on a field the loop always changes. See PR #209.\n 5. The state transitions are exactly the terminal set: task `review` \u2192 `done`, lock\n@@ -113,12 +113,12 @@\n classification must be derived, not asserted.\n \n **Fold the closeout into the implementation PR** so no second PR exists. Rejected under\n-ADR 0012, which recorded why: a task file that is already `done` on the implementation\n+SADR-0012, which recorded why: a task file that is already `done` on the implementation\n branch short-circuits the loop's review \u2192 CCA \u2192 merge \u2192 done ordering into a local\n goal-completion before any merge happens.\n \n **Have the loop auto-merge closeouts with elevated credentials.** Rejected outright: that\n-is a runtime authoring its own approval, which ADR 0010 and the constitution's\n+is a runtime authoring its own approval, which SADR-0010 and the constitution's\n approval-record prohibition forbid. It would also be indistinguishable, in the evidence,\n from the forgery those rules exist to prevent.\n \ndiff --git a/docs/adr/0018-carried-guardian-approval-across-a-proven-base-sync.md b/docs/adr/SADR-0018-carried-guardian-approval-across-a-proven-base-sync.md\nindex 22358562da390ed8b45cd600da08a43d378b4680..e6e2907cefa776460af73ddf7d39db605f3c39b0\n--- a/docs/adr/0018-carried-guardian-approval-across-a-proven-base-sync.md\n+++ b/docs/adr/SADR-0018-carried-guardian-approval-across-a-proven-base-sync.md\n@@ -1,4 +1,4 @@\n-# ADR 0018: Carried Guardian Approval Across A Proven Base Sync\n+# SADR-0018: Carried Guardian Approval Across A Proven Base Sync\n \n ## Status\n \n@@ -34,7 +34,7 @@\n Guardian evaluator that consumes it (`shiki_guardian.evaluate_guardian_approval`,\n the `guardian_comment_carried` source) were built and tested in isolation. Neither\n does anything until a caller passes the flags that turn the carry on, so the\n-feature was invisible in CI. This ADR records the decision the two of them\n+feature was invisible in CI. This SADR records the decision the two of them\n implement, and this task wires it into the four CI invocations that judge Guardian\n approval.\n \ndiff --git a/docs/adr/README.md b/docs/adr/README.md\nindex c70e057a4520c36e042ab68c53cd4a21ef709b71..5da43856836e2ce6a150f8127766e2f88355ab4b\n--- a/docs/adr/README.md\n+++ b/docs/adr/README.md\n@@ -1,40 +1,41 @@\n-# Architecture Decision Records\n+# Decision Record Namespaces\n \n-This directory holds Shiki's Architecture Decision Records (ADRs): durable\n-records of hard-to-reverse platform decisions. ADRs are part of the Shiki source\n-of truth alongside GitHub state, the `.shiki/` mirror, and `CONTEXT.md`.\n+This directory holds two independent decision namespaces:\n \n-## How ADRs Work\n+- Shiki Architecture Decision Records (SADRs), named\n+  `SADR-NNNN-<short-slug>.md`, record hard-to-reverse Shiki platform decisions.\n+- Target Architecture Decision Records (ADRs), named\n+  `NNNN-<short-slug>.md`, belong to the target product.\n \n-- New ADRs are added as `docs/adr/NNNN-<short-slug>.md`, where `NNNN` is the next\n-  zero-padded sequence number.\n-- The **canonical list of ADRs is the set of files matching `docs/adr/*.md`** in\n-  this directory. Browse `docs/adr/*.md` for the authoritative, current set.\n-- Use [`template.md`](template.md) as the starting point for a new ADR.\n-- Record an ADR when a decision is hard to reverse, surprising without context,\n-  and a real tradeoff. Do not write an ADR for routine or easily reversible\n-  choices.\n+The same four-digit number may appear once in each namespace. A platform\n+`SADR-NNNN` citation resolves only to `SADR-NNNN-*.md`; a target `ADR NNNN`\n+citation resolves only to `NNNN-*.md`.\n \n-Using the `docs/adr/*.md` glob as the source of truth means other PRs can add an\n-ADR without editing this index file, avoiding merge contention on a\n-hand-maintained enumeration.\n+## How Decision Records Work\n \n-## Current ADRs (snapshot)\n-\n-The list below is a convenience snapshot and may lag behind the directory. When\n-in doubt, list `docs/adr/*.md`.\n+- New Shiki platform decisions use the next zero-padded SADR number.\n+- New target product decisions use the target's own ADR sequence.\n+- The canonical list is the files in `docs/adr/`; no hand-maintained index is\n+  authoritative.\n+- Use [`template.md`](template.md) as the starting point and choose the filename\n+  namespace that owns the decision.\n+- Record a decision when it is hard to reverse, surprising without context, and\n+  a real tradeoff. Do not write a decision record for routine or easily\n+  reversible choices.\n \n-- [0001 \u2014 Keep Shiki As The Platform Name](0001-keep-shiki-as-platform-name.md)\n-- [0002 \u2014 Use AGENTS As The Shared Agent Constitution](0002-use-agents-as-shared-constitution.md)\n-- [0003 \u2014 Require Skill Gate For MergeGate Readiness](0003-require-skill-gate-for-mergegate-readiness.md)\n-- [0004 \u2014 Use GitHub First With A Repository-Local Shiki Mirror](0004-use-github-first-with-shiki-mirror.md)\n-- [0005 \u2014 Use Codex As Front And Claude Code Action As The GitHub Runtime](0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md)\n-- [0006 \u2014 Separate Codex Implementation, CCA Completion, And MergeGate Authorization](0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md)\n+Using the directory as the source of truth lets independent changes add records\n+without contending on an enumerated index.\n \n-## Adding A New ADR\n+## Adding A Shiki SADR\n \n-1. Copy `template.md` to `docs/adr/NNNN-<short-slug>.md`.\n+1. Copy `template.md` to `docs/adr/SADR-NNNN-<short-slug>.md`.\n 2. Fill in the title, status, context, decision, and consequences.\n-3. Set the status to `Proposed`, then `Accepted` once the decision is approved.\n-4. Open a PR following `CONTRIBUTING.md`. You do not need to edit this index for\n-   the ADR to be discoverable via `docs/adr/*.md`.\n+3. Set the status to `Proposed`, then `Accepted` once the proper authority has\n+   approved the decision.\n+4. Open a PR following `CONTRIBUTING.md`.\n+\n+## Adding A Target ADR\n+\n+1. Copy `template.md` to `docs/adr/NNNN-<short-slug>.md` using the target's own\n+   sequence.\n+2. Cite it as `ADR NNNN`; do not prefix its filename with `SADR-`.\ndiff --git a/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md b/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\nnew file mode 100644\nindex 0000000000000000000000000000000000000000..7455ce1f3bed02ddddb851ac21a1d1a648c9aeba\n--- /dev/null\n+++ b/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\n@@ -0,0 +1,55 @@\n+# SADR-0019: Separate Shiki Platform SADRs From Target ADRs\n+\n+## Status\n+\n+Accepted\n+\n+## Context\n+\n+Shiki installs its platform decision records into `docs/adr/` in every target\n+repository. Shiki and its targets previously used the same numeric\n+`NNNN-<slug>.md` namespace. A target that already owned ADRs 0013 through 0018\n+therefore acquired two records for each number during installation, making an\n+`ADR NNNN` citation ambiguous. Renumbering target decisions would rewrite target\n+history, while renumbering Shiki decisions per target would destroy their stable\n+platform identity.\n+\n+Upgrades also need to remove the former Shiki-owned numeric paths without ever\n+mistaking a target-owned ADR for platform content. The install stamp already\n+records exact shipped paths and content digests, so it can prove ownership only\n+when both still match.\n+\n+## Decision\n+\n+Shiki platform decisions use the `SADR` namespace:\n+\n+- records are named `docs/adr/SADR-NNNN-<slug>.md`;\n+- live platform citations use `SADR-NNNN`;\n+- target product decisions retain `docs/adr/NNNN-<slug>.md` and `ADR NNNN`;\n+- the reference validator resolves the two namespaces independently, so the same\n+  four-digit number may exist once in each namespace.\n+\n+Before a forced upgrade writes anything, the installer inventories the exact\n+former numeric Shiki ADR paths. It may delete a legacy path only when the\n+existing install stamp contains that exact path and its recorded SHA-256 digest\n+matches the file currently on disk. All ownership blockers are reported\n+together and abort the upgrade before its first write. A non-forced install\n+never performs this cleanup.\n+\n+Historical `.shiki/` evidence remains unchanged because it records the namespace\n+that existed when that evidence was produced.\n+\n+## Consequences\n+\n+- Targets may use any ADR number without colliding with Shiki's platform SADRs.\n+- Fresh installs preserve target ADR bytes and install a stable SADR set.\n+- Forced upgrades fail closed when legacy ownership cannot be proven, and a\n+  successful upgrade refreshes the stamp without removed legacy paths.\n+- Current governance, packaging, workflow, prompt, script, and skill references\n+  must move with the platform records.\n+\n+## Alternatives Considered\n+\n+- Collision-only refusal leaves affected targets permanently unupgradable.\n+- Renumbering target ADRs mutates target-owned history.\n+- Per-target Shiki renumbering makes platform citations non-portable.\ndiff --git a/docs/adr/template.md b/docs/adr/template.md\nindex a92a97daddd0b00ab9dd626cc8d86942da2eafed..17dd79175b5fee2105935557f691b40cf9492942\n--- a/docs/adr/template.md\n+++ b/docs/adr/template.md\n@@ -1,14 +1,14 @@\n-# NNNN: <Short Decision Title>\n+# <SADR-NNNN or NNNN>: <Short Decision Title>\n \n ## Status\n \n-Proposed | Accepted | Superseded by ADR-NNNN\n+Proposed | Accepted | Superseded by SADR-NNNN or target ADR NNNN\n \n ## Context\n \n What is the situation, constraint, or force that requires a decision? Reference\n-relevant domain terms (`CONTEXT.md`), prior ADRs, and code reality. State the\n-problem, not the solution.\n+relevant domain terms (`CONTEXT.md`), prior Shiki SADRs or target ADRs, and code\n+reality. State the problem, not the solution.\n \n ## Decision\n \ndiff --git a/docs/agents/autonomous-loop-e2e-acceptance.md b/docs/agents/autonomous-loop-e2e-acceptance.md\nindex bbf84c6081987f159ac6ad9ecf3c82b7aa72e7e8..5c13c1d02a05a90b0dc46cbef7c95a9846d0c237\n--- a/docs/agents/autonomous-loop-e2e-acceptance.md\n+++ b/docs/agents/autonomous-loop-e2e-acceptance.md\n@@ -40,8 +40,8 @@\n 3. `gh auth status` is logged in with merge permission on the repo.\n 4. Branch protection + required checks are configured exactly as the goal-loop\n    required-check set expects (see `docs/agents/branch-protection-smoke.md`).\n-5. The repo `.shiki/policy` permits autonomous low/medium-risk merges (ADR\n-   0008/0009); the MergeGate policy check is the recorded-authority gate.\n+5. The repo `.shiki/policy` permits autonomous low/medium-risk merges\n+   (SADR-0008/0009); the MergeGate policy check is the recorded-authority gate.\n \n The operator's only actions are: register the task and start the loop. **No\n manual pre-clearing** means after `shiki loop run` starts, the operator does not\ndiff --git a/docs/agents/bootstrap-command.md b/docs/agents/bootstrap-command.md\nindex 05f7f203a52cf412b9f97084a6e131ed0f785afd..f7aa0b734dfebee4a3b139335b66ee3415f48713\n--- a/docs/agents/bootstrap-command.md\n+++ b/docs/agents/bootstrap-command.md\n@@ -189,7 +189,9 @@\n ```\n \n Use this only for tests, fixtures, or explicit local-only template inspection.\n-Use `--force` only when you intentionally want to overwrite existing target files.\n+Use `--force` only when you intentionally want to refresh the target template.\n+Legacy numeric Shiki decision cleanup is stamp-and-digest authorized before any\n+write; target ADR paths are not cleanup candidates.\n \n ## Slash Command\n \ndiff --git a/docs/agents/checklists.md b/docs/agents/checklists.md\nindex 4a7f03ade346f53a1c1d82981f3fd744f9ab4547..0c5ec6c53dc7197b7b7a7d18e667b325babd6c45\n--- a/docs/agents/checklists.md\n+++ b/docs/agents/checklists.md\n@@ -32,18 +32,18 @@\n | ID | Item | Blocking |\n | --- | --- | --- |\n | GD-01 | Plan was challenged against `CONTEXT.md`. | blocking for non-trivial Goals |\n-| GD-02 | Relevant ADRs were read or absence was recorded. | blocking for non-trivial Goals |\n+| GD-02 | Relevant SADRs and target ADRs were read or absence was recorded. | blocking for non-trivial Goals |\n | GD-03 | Ambiguous terms were resolved or converted to HITL blockers. | blocking |\n | GD-04 | Edge scenarios were considered. | advisory |\n | GD-05 | Code reality was checked when code could answer. | blocking when applicable |\n | GD-06 | New domain terms were added to `CONTEXT.md` when needed. | blocking when applicable |\n-| GD-07 | ADR was created or explicitly skipped using the ADR criteria. | blocking when applicable |\n+| GD-07 | A SADR or target ADR was created or explicitly skipped using the decision-record criteria. | blocking when applicable |\n \n ## CI \u2014 Context & Impact Checklist\n \n | ID | Item | Blocking |\n | --- | --- | --- |\n-| CI-01 | Relevant docs and ADRs identified. | blocking |\n+| CI-01 | Relevant docs, SADRs, and target ADRs identified. | blocking |\n | CI-02 | Relevant modules, interfaces, seams, callers, and tests identified. | blocking |\n | CI-03 | Dependencies and blocked-by relationships identified. | blocking |\n | CI-04 | Candidate locks identified. | blocking |\n@@ -62,7 +62,7 @@\n | PRD-04 | Implementation decisions are recorded without volatile code detail. | blocking |\n | PRD-05 | Testing decisions are recorded. | blocking |\n | PRD-06 | Out-of-scope items are explicit. | blocking |\n-| PRD-07 | Relevant glossary/ADR links are present when applicable. | blocking |\n+| PRD-07 | Relevant glossary/SADR/target-ADR links are present when applicable. | blocking |\n \n ## SF \u2014 Spec Freeze Checklist\n \n@@ -98,7 +98,7 @@\n | H-02 | Branch/worktree target is present. | blocking |\n | H-03 | Scope and non-goals are present. | blocking |\n | H-04 | Dependencies and locks are present. | blocking |\n-| H-05 | Relevant docs, ADRs, modules, and tests are referenced. | blocking |\n+| H-05 | Relevant docs, SADRs, target ADRs, modules, and tests are referenced. | blocking |\n | H-06 | Required skills are stated. | blocking |\n | H-07 | TDD expectations are stated. | blocking when behavior work |\n | H-08 | Acceptance criteria are included. | blocking |\ndiff --git a/docs/agents/codex-handoff.md b/docs/agents/codex-handoff.md\nindex ede468f47cb277f69a857e8c45df703e4ed0151d..6f97bdffb67e44d7edea2c71e01ea28933c4eb6c\n--- a/docs/agents/codex-handoff.md\n+++ b/docs/agents/codex-handoff.md\n@@ -46,7 +46,7 @@\n \n ## Context\n - Domain terms:\n-- Relevant ADRs:\n+- Relevant SADRs and target ADRs:\n - Relevant docs:\n - Relevant modules/interfaces/seams:\n - Similar tests/prior art:\ndiff --git a/docs/agents/completion-check-agent.md b/docs/agents/completion-check-agent.md\nindex 13e4c035db35025f4e3cdcc5adfb24314ac4d521..61bfa1218cf36ca605250c91919e620f33238aa0\n--- a/docs/agents/completion-check-agent.md\n+++ b/docs/agents/completion-check-agent.md\n@@ -32,7 +32,7 @@\n - `AGENTS.md`\n - `CLAUDE.md`\n - `CONTEXT.md`\n-- relevant ADRs\n+- relevant SADRs and target ADRs\n - parent Goal issue\n - PRD issue or document\n - task issue\ndiff --git a/docs/agents/decision-control.md b/docs/agents/decision-control.md\nindex 7d8ee5ab03a5ccbcbd04056ae61de03785a2a9c9..8c94ecda00fffaa0e92c325ab83aa9776bd1dac1\n--- a/docs/agents/decision-control.md\n+++ b/docs/agents/decision-control.md\n@@ -11,7 +11,7 @@\n | Decision | Owner | When | Required inputs | Output | Enforcement surface |\n | --- | --- | --- | --- | --- | --- |\n | Is the Goal clear enough to plan? | Planner + Guardian when needed | Goal intake | User request, existing docs, risk signals | Goal readiness verdict | Goal checklist, issue template, ledger |\n-| Must `grill-with-docs` run? | Planner / Skill Gate | Before PRD or issue split | Ambiguity, domain terms, ADR conflicts, unclear boundaries | Skill invocation or explicit skip rationale | Skill Gate checklist |\n+| Must `grill-with-docs` run? | Planner / Skill Gate | Before PRD or issue split | Ambiguity, domain terms, SADR/target-ADR conflicts, unclear boundaries | Skill invocation or explicit skip rationale | Skill Gate checklist |\n | Is a PRD required? | Planner | After Goal context stabilizes | Goal, `grill-with-docs` output, Context & Impact output | PRD required / not required | PRD checklist, CCA evidence check |\n | Can the spec be frozen? | Operator only | End of Requirements Definition, before issue split | PRD/requirements summary, scope inventory (SF-02), open-question list | `spec_freeze` block in the plan + ledger evidence | `require_grilled_plan` (plan ingest/run), SF checklist |\n | Can a frozen spec change? | Operator only (Spec Amendment) | When implementation contradicts the frozen spec | Contested decisions, scoped re-grill result | recorded amendment + re-stamped freeze | SF checklist, ledger evidence |\ndiff --git a/docs/agents/domain.md b/docs/agents/domain.md\nindex 258da8ea81e87ac0a6b0b5b5e5c2fde980eaabb7..d57b0cf17ff3e1fa9e6e5885372bf083d8295421\n--- a/docs/agents/domain.md\n+++ b/docs/agents/domain.md\n@@ -5,7 +5,9 @@\n ## Layout\n \n - `CONTEXT.md` defines Shiki language. Keep it implementation-free.\n-- `docs/adr/` records hard-to-reverse decisions.\n+- `docs/adr/SADR-NNNN-*.md` records Shiki platform decisions.\n+- `docs/adr/NNNN-*.md` records target product decisions without sharing the\n+  SADR namespace.\n - `AGENTS.md` is the runtime-neutral agent constitution.\n - `CLAUDE.md` is the Claude Code wrapper around `AGENTS.md`.\n - `.shiki/` is the Target Repository mirror for Goals, PRDs, plans, Task DAGs, contracts, locks, worktree records, ledger entries, CCA verdicts, repair packets, reports, and handoffs.\n@@ -15,6 +17,6 @@\n \n - Use Shiki terms exactly when they exist.\n - Add glossary terms to `CONTEXT.md` only when the term is part of the domain, not a generic programming concept.\n-- Create ADRs only for hard-to-reverse, surprising, tradeoff-based decisions.\n+- Create SADRs or target ADRs only for hard-to-reverse, surprising, tradeoff-based decisions.\n - Keep product-specific language in the Target Repository, not in Shiki Template docs.\n-- If an ADR conflicts with a proposed implementation, surface the conflict before editing.\n+- If a SADR or target ADR conflicts with a proposed implementation, surface the conflict before editing.\ndiff --git a/docs/agents/execution-control.md b/docs/agents/execution-control.md\nindex ad8ee3cdb36be56fedc1f46408ca92b387d74047..bc63c3a73661d12b567016ba299f1a6b66bf9088\n--- a/docs/agents/execution-control.md\n+++ b/docs/agents/execution-control.md\n@@ -63,7 +63,7 @@\n | Branch name | Orchestrator | Orchestrator | Before implementer dispatch | branch policy | branch ref in task/ledger |\n | Lock reservation | Context & Impact | MergeGate | Before dispatch and before merge | `.shiki/locks/`, labels, PR metadata | lock id, owner, scope |\n | CI profile | Planner | MergeGate | During issue triage, rechecked at PR | labels, `.shiki/policy.yaml`, workflow matrix | required check list |\n-| Required status checks | Repo maintainer / Guardian | GitHub branch protection / rulesets | Repository setup, updated via ADR | GitHub settings | branch/ruleset config, ADR |\n+| Required status checks | Repo maintainer / Guardian | GitHub branch protection / rulesets | Repository setup, updated via target ADR | GitHub settings | branch/ruleset config, target ADR |\n | CCA judgment | CCA | GitHub required check / MergeGate | Every non-draft PR update | GitHub Action check | structured CCA verdict |\n | Repair assignment | CCA | MergeGate | After failed CCA/CI/review | repair packet, issue/PR comment | repair id and attempt count |\n | Merge readiness | MergeGate | GitHub branch protection / Guardian if needed | After CCA complete | required checks, review rules, rulesets | mergegate check output |\ndiff --git a/docs/agents/guardian-policy.md b/docs/agents/guardian-policy.md\nindex 4147c7adf05a47adb4fd24ada1a1c2e1f2ba6d2d..a20d9cc6c00a569bb057b9ca8d6b51c7fc4a9847\n--- a/docs/agents/guardian-policy.md\n+++ b/docs/agents/guardian-policy.md\n@@ -98,8 +98,8 @@\n `origin/<default_branch>` and reuses each job's existing base fetch; a shallow\n (`--depth`) or second base fetch would make the proof unresolvable and silently\n disable the carry, so neither is permitted. CCA reads `guardian_comment_carried` as\n-a recorded authority in the deterministic signal and does not re-derive it. See ADR\n-0018 for the full decision, the rejected diff/patch-equality and path-disjointness\n+a recorded authority in the deterministic signal and does not re-derive it. See\n+SADR-0018 for the full decision, the rejected diff/patch-equality and path-disjointness\n alternatives, and the accepted residual.\n \n ## External AI Guardian Review (SADR-0010)\ndiff --git a/docs/agents/implementation-policy.md b/docs/agents/implementation-policy.md\nindex c754144a9fb5bf4a38aa82a60e1670c59e626b44..85c37281bcad196c6b7b02343faf00b03cd9c57a\n--- a/docs/agents/implementation-policy.md\n+++ b/docs/agents/implementation-policy.md\n@@ -31,10 +31,13 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n ## Non-Negotiables\n \n 1. **Planning precedes implementation.** Non-trivial Goals start with `grill-with-docs` and Context & Impact.\n-2. **Domain language is controlled.** Terms must be checked against `CONTEXT.md`; hard-to-reverse tradeoffs must be checked against ADRs.\n+2. **Domain language is controlled.** Terms must be checked against `CONTEXT.md`; hard-to-reverse tradeoffs must be checked against Shiki SADRs and target ADRs.\n 3. **PRD precedes issue decomposition.** `to-prd` is used once the context is settled enough to become durable intent.\n 4. **Issues are vertical slices.** `to-issues` must produce independently grabbable tracer-bullet tasks, not horizontal layer tickets.\n 5. **Triage controls dispatch.** Only `ready-for-agent` / AFK tasks may be assigned to Codex without human input.\n@@ -95,19 +98,19 @@\n Process:\n \n - Challenge terms against `CONTEXT.md`.\n-- Check ADRs before reopening decisions.\n+- Check SADRs and target ADRs before reopening decisions.\n - Inspect code when code can answer a question.\n - Ask one question at a time when the operator is available.\n - Provide a recommended answer with each question.\n - Record resolved terms in `CONTEXT.md`.\n-- Create ADRs only for hard-to-reverse, surprising, tradeoff-based decisions.\n+- Create SADRs or target ADRs only for hard-to-reverse, surprising, tradeoff-based decisions.\n - Convert unresolved load-bearing questions into HITL blockers.\n \n Outputs:\n \n - Resolved design-tree decisions.\n - Updated glossary terms when needed.\n-- ADRs when justified.\n+- SADRs or target ADRs when justified.\n - HITL blockers when decisions remain unresolved.\n \n ## Phase 3 \u2014 Context & Impact\n@@ -117,7 +120,7 @@\n \n Outputs must include:\n \n-- Relevant documents and ADRs.\n+- Relevant documents, SADRs, and target ADRs.\n - Relevant modules, interfaces, seams, callers, and tests.\n - Candidate locks.\n - Dependencies.\n@@ -140,7 +143,7 @@\n - Testing decisions.\n - Out of scope.\n - Further notes.\n-- Links to relevant Goal, domain terms, and ADRs.\n+- Links to relevant Goal, domain terms, SADRs, and target ADRs.\n \n Phase 4 ends with Spec Freeze: the operator explicitly approves the PRD, the\n plan records a `spec_freeze` block (status, approved_by, source), and the SF\n@@ -240,7 +243,7 @@\n - Dependencies and locks.\n - Risk and Guardian requirements.\n - Ledger completeness.\n-- Documentation and ADR updates.\n+- Documentation and SADR/target-ADR updates.\n - Scope drift.\n \n CCA emits one of:\n@@ -301,7 +304,7 @@\n | Evidence failure | Behavior might be done, but proof is missing. | Add checks, PR evidence, ledger entry, or artifacts. |\n | Scope drift | PR changes unrelated behavior. | Revert unrelated changes or split into new Goal. |\n | Dependency block | Upstream task or lock not complete. | Block until dependency resolves. |\n-| Architecture block | Decision needs ADR or deep-module discussion. | `grill-with-docs` or `improve-codebase-architecture`. |\n+| Architecture block | Decision needs a SADR, target ADR, or deep-module discussion. | `grill-with-docs` or `improve-codebase-architecture`. |\n | Risk block | Security/production/policy change needs human approval. | Guardian decision. |\n \n ## Phase 5.5 \u2014 Execution Decision Control\ndiff --git a/docs/agents/runtime-auth-model.md b/docs/agents/runtime-auth-model.md\nindex 6a2e1a47e81ea89b5c68a210ec07772a9374babc..3c54748d24233dbd0ba8473950282547ef4ce935\n--- a/docs/agents/runtime-auth-model.md\n+++ b/docs/agents/runtime-auth-model.md\n@@ -148,7 +148,7 @@\n \n API-key based runners may be added by a target repository as an explicit extension. They are not the default Shiki template path.\n \n-If a repo opts into API-key mode, record the exception in an ADR and update the repo's `AGENTS.md`, `.shiki/config.yaml`, and workflow permissions.\n+If a repo opts into API-key mode, record the target-specific exception in a target ADR and update the repo's `AGENTS.md`, `.shiki/config.yaml`, and workflow permissions.\n \n ## GitHub Actions Runtime Compatibility\n \ndiff --git a/docs/agents/skill-gate.md b/docs/agents/skill-gate.md\nindex c50398b0d7c60a8598f2a5c88a10015730dced13..3acffe6444617bb1d54b77d4b4708158ec735bc6\n--- a/docs/agents/skill-gate.md\n+++ b/docs/agents/skill-gate.md\n@@ -9,7 +9,7 @@\n | Skill | Trigger | Expected output |\n | --- | --- | --- |\n | `setup-matt-pocock-skills` | First configuring a repo for these skills, or when issue tracker, triage labels, or domain docs are missing. | `docs/agents/` configuration and an `Agent skills` block in `AGENTS.md` or `CLAUDE.md`. |\n-| `grill-with-docs` | Ambiguous plan, unclear terminology, boundary decision, tradeoff, or ADR-worthy design choice. | Clarified decision tree, recommended answers, updates to `CONTEXT.md` or ADRs when decisions crystallize. |\n+| `grill-with-docs` | Ambiguous plan, unclear terminology, boundary decision, tradeoff, or decision-record-worthy design choice. | Clarified decision tree, recommended answers, updates to `CONTEXT.md`, SADRs, or target ADRs when decisions crystallize. |\n | `zoom-out` | Agent lacks a higher-level architectural map. | Map of relevant modules, callers, seams, and domain concepts. |\n | `to-prd` | Settled context should become a PRD. | PRD published to the issue tracker with problem, solution, user stories, decisions, testing, and out-of-scope notes. |\n | `to-issues` | Goal, PRD, or plan must become implementation tickets. | Vertical-slice issues with acceptance criteria, dependencies, and AFK/HITL classification. |\n@@ -17,7 +17,7 @@\n | `tdd` | Feature work or bug fix has observable behavior that can be specified with tests. | Red-green-refactor loop through public interfaces, one vertical slice at a time. |\n | `code-review` | The TDD loop is green and the implementer is about to create or update a PR. Default for every implementation task. | Pre-PR self-review of the task diff with fixes applied on the task branch; findings and resolutions recorded as a ledger entry naming the skill and a `## Pre-PR code review` PR body section. |\n | `diagnose` | Hard bug, failing check, regression, flaky behavior, or performance problem. | Reproduction loop, ranked hypotheses, instrumentation, fix, regression test, post-mortem. |\n-| `improve-codebase-architecture` | Structural friction, testability, AI-navigability, deep-module opportunity, or refactor request. | Architecture candidates and deepening recommendations informed by `CONTEXT.md` and ADRs. |\n+| `improve-codebase-architecture` | Structural friction, testability, AI-navigability, deep-module opportunity, or refactor request. | Architecture candidates and deepening recommendations informed by `CONTEXT.md`, SADRs, and target ADRs. |\n | `prototype` | A throwaway logic or UI prototype is needed to answer a design question. | Clearly marked disposable prototype and captured decision. |\n \n ## MergeGate Interaction\ndiff --git a/docs/operations.md b/docs/operations.md\nindex cf1f452d4c589e38ccd58553bda0ad8684b51eb7..ace48a90ffbec9e5c0a7113bef16a4858bc44b3f\n--- a/docs/operations.md\n+++ b/docs/operations.md\n@@ -89,6 +89,16 @@\n      target history, brought forward by `shiki migrate`, not a template.\n    - **Everything else** (CLI scripts, workflows, schemas, docs) is overwritten.\n \n+   A forced upgrade also migrates the former numeric Shiki decision paths to\n+   `SADR-NNNN-*`. Before the first write, the installer inventories the exact\n+   legacy Shiki paths and authorizes each deletion only when the existing\n+   `.shiki/install-stamp.json` names that path and its SHA-256 digest still\n+   matches the file. An absent, unreadable, malformed, incomplete, or\n+   mismatching stamp reports every blocked path and stops with no filesystem\n+   change. Target-owned `NNNN-*` ADRs are not cleanup candidates. A non-force\n+   install deletes nothing and retains any still-matching legacy stamp entries\n+   so a later forced upgrade can prove ownership.\n+\n    If the target has pending migrations, `--force` refuses and writes nothing \u2014\n    apply migrations first (step 3), then re-run. The run ends with a single\n    summary of every `.new` written, so a half-upgrade is visible.\ndiff --git a/docs/releasing.md b/docs/releasing.md\nindex 10d73828e7dbd64e9d9f6b83bc84d010537ce62a..aafeee8c4d441e1e427efee2ce4f81a647f020e1\n--- a/docs/releasing.md\n+++ b/docs/releasing.md\n@@ -67,7 +67,7 @@\n CLI in `scripts/`. There are no third-party Python runtime dependencies. The CLI\n is intentionally **not** exposed as a `[project.scripts]` console entry point\n because `scripts/shiki.py` is a dependency-free shim and `scripts/` is not\n-packaged as an importable distribution (see the ADR for the rationale).\n+packaged as an importable distribution (see SADR-0007 for the rationale).\n \n Supported install paths:\n \n@@ -155,6 +155,15 @@\n    [docs/operations.md](operations.md#the-new-convention). If the target has\n    pending migrations, `--force` refuses and writes nothing; apply migrations\n    (step 2) first, then re-run.\n+\n+   The SADR namespace upgrade has an additional pre-write gate: each former\n+   numeric Shiki decision path is removed only when the existing install stamp\n+   contains that exact path and a digest matching its current bytes. The\n+   installer aggregates absent/unreadable/malformed-stamp, missing-digest, and\n+   digest-mismatch blockers before raising, so refusal leaves the target tree\n+   byte-identical. Numeric target ADRs are preserved. A successful forced\n+   upgrade installs and stamps `SADR-*` paths and drops the removed legacy paths\n+   from the refreshed stamp.\n 2. Apply repository-local `.shiki/` state migrations with the migrate command:\n \n    ```bash\ndiff --git a/docs/usage.md b/docs/usage.md\nindex b83407ee3970dd395dd893010bc5dbe8bad26051..a130ef6e40dfd8169a5bc701339351097df1a09f\n--- a/docs/usage.md\n+++ b/docs/usage.md\n@@ -67,8 +67,10 @@\n shiki install-target /path/to/target-repo --local-only\n ```\n \n-Use `--force` only when you intentionally want to overwrite existing target\n-files.\n+Use `--force` only when you intentionally want to refresh the target template.\n+For the SADR namespace migration, `--force` deletes only exact legacy Shiki\n+decision paths whose existing install-stamp digest still matches; any ownership\n+blocker stops before the first write. Numeric target ADRs are never candidates.\n \n ## `shiki bootstrap-platform` \u2014 publish the Shiki platform repo\n \ndiff --git a/scripts/guardian_approval_signal.py b/scripts/guardian_approval_signal.py\nindex d059f92b2c82aa2b5d6ca421365537a87886c1cb..16924892714f4f5b9303c0ef0e325a7e70075630\n--- a/scripts/guardian_approval_signal.py\n+++ b/scripts/guardian_approval_signal.py\n@@ -199,8 +199,8 @@\n ) -> Any:\n     \"\"\"Evaluate SADR-0015 Contract Approval for the CCA Guardian signal.\n \n-    The SAME OR that MergeGate applies must reach this deterministic signal: ADR\n-    0015 is explicit that without it, a contract-approved PR still returns\n+    The SAME OR that MergeGate applies must reach this deterministic signal:\n+    SADR-0015 is explicit that without it, a contract-approved PR still returns\n     ``needs_guardian``, the CCA job fails, and the MergeGate policy check never\n     runs \u2014 the gate this change moves would never be reached. This resolves the\n     task the SAME way MergeGate does (the first Shiki task id in the PR body),\ndiff --git a/scripts/shiki_contract_approval.py b/scripts/shiki_contract_approval.py\nindex 941bcb96fdd8b9a85bc0c4f0f04d5dad9d04839c..4a4b418f6527413161db82670bec857a043360e2\n--- a/scripts/shiki_contract_approval.py\n+++ b/scripts/shiki_contract_approval.py\n@@ -142,7 +142,7 @@\n     changed_files_status: Any,\n     registration: Any,\n ) -> ContractApprovalResult:\n-    \"\"\"Whether this PR carries an SADR-0015 Contract Approval.\n+    \"\"\"Whether this PR carries a SADR-0015 Contract Approval.\n \n     The four conditions are evaluated IN ORDER, each failure recorded as a\n     distinct reason string:\ndiff --git a/scripts/shiki_guardian.py b/scripts/shiki_guardian.py\nindex a4ecb79bbda55fd5c8b06adfae2eb99760a03c45..4dd99bbe714e26f421fa19abac7a34ae1b195ecc\n--- a/scripts/shiki_guardian.py\n+++ b/scripts/shiki_guardian.py\n@@ -12,7 +12,7 @@\n # The carry re-derives its proof from git in-process on every judging invocation;\n # it is never read from an artifact, so there is nothing to go stale or be forged.\n # shiki_sync_proof is the ONLY guardian-family module that runs git, and every one\n-# of its invocations is read-only and local (ADR: prove-from-git base sync).\n+# of its invocations is read-only and local (SADR: prove-from-git base sync).\n from shiki_sync_proof import verify_pure_base_sync\n \n GUARDIAN_POLICY_PATH = \".shiki/guardian-policy.json\"\ndiff --git a/scripts/shiki_guardian_review.py b/scripts/shiki_guardian_review.py\nindex dd0f104d457899e0ba5ff1e162011c1e90b6f92b..e10ef58c046324e7ea3a3392feb3048ca77414ce\n--- a/scripts/shiki_guardian_review.py\n+++ b/scripts/shiki_guardian_review.py\n@@ -159,7 +159,7 @@\n     docs_only = _is_docs_only(changed_files)\n     if docs_only and matched == 0:\n         focus.append(\n-            \"Docs/ADR-only change: confirm the change is genuinely documentation/decision-only, introduces no behavior or contract drift, and stays consistent with the constitution and existing ADRs.\"\n+            \"Docs/decision-record-only change: confirm the change is genuinely documentation/decision-only, introduces no behavior or contract drift, and stays consistent with the constitution and existing SADRs and target ADRs.\"\n         )\n \n     # Conservative augmentation: unknown (no category and not docs-only), mixed\ndiff --git a/scripts/shiki_installer.py b/scripts/shiki_installer.py\nindex be064ab7a29ff6836f7f7a1d70500a43e0d11d7b..9fd8eefd137ce7003dad5c0dcb2e48e2cd47ab32\n--- a/scripts/shiki_installer.py\n+++ b/scripts/shiki_installer.py\n@@ -118,6 +118,7 @@\n # what lets a target carry it without a governance-schema change.\n INSTALL_STAMP_PATH = \".shiki/install-stamp.json\"\n INSTALL_STAMP_VERSION = 1\n+LEGACY_SHIKI_SADR_MAX = 18\n \n # Under --force the shipped surface splits three ways, so nothing is lost and\n # nothing is silently stale.\n@@ -237,9 +238,98 @@\n         )\n \n \n+def legacy_shiki_adr_paths() -> tuple[str, ...]:\n+    \"\"\"Former numeric paths for Shiki SADR-0001 through SADR-0018.\n+\n+    The authoritative slugs come from the current SADR filenames. SADR-0019 is\n+    the namespace decision itself and never shipped under a numeric legacy path.\n+    \"\"\"\n+    paths: list[str] = []\n+    for sadr in sorted((ROOT / \"docs\" / \"adr\").glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\")):\n+        number = sadr.name.split(\"-\", 2)[1]\n+        if int(number) <= LEGACY_SHIKI_SADR_MAX:\n+            paths.append(f\"docs/adr/{sadr.name.removeprefix('SADR-')}\")\n+    return tuple(paths)\n+\n+\n+def preflight_legacy_sadr_cleanup(target: Path, *, force: bool) -> tuple[str, ...]:\n+    \"\"\"Authorize exact legacy Shiki ADR removals before the first write.\n+\n+    A non-force install never removes existing files. A forced install needs no\n+    stamp when no legacy Shiki path is present. When legacy paths do exist, each\n+    must be named in a readable install stamp and its current bytes must match\n+    the recorded digest.\n+    \"\"\"\n+    if not force:\n+        return ()\n+    present = tuple(\n+        relative\n+        for relative in legacy_shiki_adr_paths()\n+        if (target / relative).exists() or (target / relative).is_symlink()\n+    )\n+    if not present:\n+        return ()\n+\n+    stamp_path = target / INSTALL_STAMP_PATH\n+    try:\n+        raw = stamp_path.read_text(encoding=\"utf-8\")\n+        stamp = json.loads(raw)\n+    except FileNotFoundError:\n+        detail = \"install stamp is absent\"\n+        blockers = [f\"{relative}: {detail}\" for relative in present]\n+    except (OSError, UnicodeDecodeError) as error:\n+        detail = f\"install stamp is unreadable ({error})\"\n+        blockers = [f\"{relative}: {detail}\" for relative in present]\n+    except json.JSONDecodeError as error:\n+        detail = f\"install stamp is malformed ({error})\"\n+        blockers = [f\"{relative}: {detail}\" for relative in present]\n+    else:\n+        if not isinstance(stamp, dict):\n+            blockers = [\n+                f\"{relative}: install stamp is malformed (expected an object)\"\n+                for relative in present\n+            ]\n+        else:\n+            digests = stamp.get(\"digests\")\n+            blockers = []\n+            if not isinstance(digests, dict):\n+                blockers.extend(\n+                    f\"{relative}: install stamp has no digest for this path\"\n+                    for relative in present\n+                )\n+            else:\n+                for relative in present:\n+                    expected = digests.get(relative)\n+                    if not isinstance(expected, str) or not expected:\n+                        blockers.append(\n+                            f\"{relative}: install stamp has no digest for this path\"\n+                        )\n+                        continue\n+                    try:\n+                        actual = _sha256_file(target / relative)\n+                    except OSError as error:\n+                        blockers.append(\n+                            f\"{relative}: legacy path is unreadable ({error})\"\n+                        )\n+                        continue\n+                    if actual != expected:\n+                        blockers.append(\n+                            f\"{relative}: digest mismatch (stamp {expected}, current {actual})\"\n+                        )\n+\n+    if blockers:\n+        joined = \"\\n\".join(f\"- {blocker}\" for blocker in blockers)\n+        raise ShikiError(\n+            \"legacy Shiki ADR cleanup is not authorized:\\n\"\n+            f\"{joined}\\nNo files were written.\"\n+        )\n+    return present\n+\n+\n def install_template(target: Path, *, force: bool, validate: bool) -> list[NewFileNote]:\n     # Preconditions first: never rewrite the tree of a target that cannot be\n     # upgraded cleanly (see refuse_pending_migrations).\n+    legacy_cleanup = preflight_legacy_sadr_cleanup(target, force=force)\n     refuse_pending_migrations(target)\n \n     # Resolve the target's required CODEOWNERS owner once from its\n@@ -250,6 +340,10 @@\n     # verbatim. See ``codeowners_required_owner``.\n     codeowners_owner = codeowners_required_owner(target)\n \n+    for relative in legacy_cleanup:\n+        (target / relative).unlink()\n+        info(f\"removed stamped legacy Shiki ADR: {target / relative}\")\n+\n     notes: list[NewFileNote] = []\n     for relative in TEMPLATE_PATHS:\n         source = ROOT / relative\n@@ -647,6 +741,22 @@\n     digests = target_stamp_digests(target)\n     existing = read_install_stamp(target)\n     if existing is not None:\n+        # A non-force install deliberately leaves legacy Shiki ADRs in place.\n+        # Preserve only still-matching ownership proof so a later forced\n+        # upgrade can authorize cleanup; never carry a missing or stale digest.\n+        existing_digests = existing.get(\"digests\")\n+        if isinstance(existing_digests, dict):\n+            for relative in legacy_shiki_adr_paths():\n+                expected = existing_digests.get(relative)\n+                path = target / relative\n+                if not isinstance(expected, str) or not path.is_file():\n+                    continue\n+                try:\n+                    actual = _sha256_file(path)\n+                except OSError:\n+                    continue\n+                if actual == expected:\n+                    digests[relative] = actual\n         if _incoming_is_older(commit, existing.get(\"platform_commit\")):\n             warn(\n                 \"kept existing install stamp: incoming install \"\ndiff --git a/scripts/shiki_loop.py b/scripts/shiki_loop.py\nindex 3611641da8aeb5038b1da90e3114b09a473ec6d8..84caa491b9cad0edc4176f209c13e9221e975681\n--- a/scripts/shiki_loop.py\n+++ b/scripts/shiki_loop.py\n@@ -424,7 +424,7 @@\n         # unresolvable CCA verdict has already produced its terminal stop and is never\n         # laundered into a head-moving sync (SADR-0010). It also never fires while the\n         # policy (Guardian) gate is red \u2014 for ANY reason, not only when it is the sole\n-        # red check \u2014 so a head-bound Guardian approval, or an SADR-0015\n+        # red check \u2014 so a head-bound Guardian approval, or a SADR-0015\n         # contract-approval carry that base movement defeated (the policy gate compares\n         # base and head task governance directly), is never silently restored by\n         # adopting base wholesale. It is bounded by a durable per-task counter so the\ndiff --git a/scripts/shiki_tasks.py b/scripts/shiki_tasks.py\nindex aec3809f24b4a0c8c96fc50971cff9ae9ad30381..d531052e9d9717058d4631483cefab44c26a65c1\n--- a/scripts/shiki_tasks.py\n+++ b/scripts/shiki_tasks.py\n@@ -821,7 +821,7 @@\n         \"prompt\": prompt,\n         \"entry_skill\": \"grill-with-docs\",\n         \"required_next_steps\": [\n-            \"Run grill-with-docs until terminology, boundaries, risks, and ADR-worthy decisions are settled.\",\n+            \"Run grill-with-docs until terminology, boundaries, risks, and decision-record-worthy choices are settled.\",\n             \"Obtain the operator's explicit approval of the PRD/requirements (Spec Freeze).\",\n             \"Write a machine-readable plan JSON with grill_with_docs.status=complete and spec_freeze.status=frozen.\",\n             \"Run shiki plan ingest --plan-file PLAN.json.\",\ndiff --git a/scripts/test_shiki_goal_loop.sh b/scripts/test_shiki_goal_loop.sh\nindex 753c7b516e10f0be3bc92a4bd0eb1fa699776ae6..8f3820a0a079b3044424cfe1a6c6b7fe68896e67\n--- a/scripts/test_shiki_goal_loop.sh\n+++ b/scripts/test_shiki_goal_loop.sh\n@@ -231,8 +231,8 @@\n \n # High/critical risk merges autonomously when all required checks are green:\n # the \"MergeGate policy check\" required check IS the Guardian gate (it enforces\n-# guardian-policy.json \u2014 human approval OR external AI guardian review, ADR\n-# 0010), so green checks mean a recorded authority approved. The fake gh returns\n+# guardian-policy.json \u2014 human approval OR external AI guardian review,\n+# SADR-0010), so green checks mean a recorded authority approved. The fake gh returns\n # green checks, simulating that the Guardian gate passed.\n python3 \"$ROOT/scripts/shiki.py\" goal create --target \"$TARGET\" --title \"Guardian gate\" --outcome \"High risk merges via policy gate\" >/tmp/shiki-goal-loop-guardian-goal.json\n GGOAL=\"$(json_get /tmp/shiki-goal-loop-guardian-goal.json goal_id)\"\ndiff --git a/tests/test_adr_references.py b/tests/test_adr_references.py\nindex 51706329c797d5dd9b47e082a333fa83b261d482..d005da4f771f5e8085ec22d4b787980e68f5ae8c\n--- a/tests/test_adr_references.py\n+++ b/tests/test_adr_references.py\n@@ -1,8 +1,8 @@\n-\"\"\"Every ``ADR ####`` citation must resolve to exactly one decision record.\n+\"\"\"Every target ADR and Shiki SADR citation resolves in its own namespace.\n \n Background. ``scripts/mergegate_check.py`` justifies ``is_bookkeeping_closeout``\n-with \"ADR 0017\" nine times and ``scripts/guardian_approval_signal.py`` cites\n-0016/0017 seven times, yet for a stretch the numbering in ``docs/adr/`` stopped\n+with \"SADR-0017\" nine times and ``scripts/guardian_approval_signal.py`` cites\n+SADR-0016/SADR-0017 seven times, yet for a stretch the numbering in ``docs/adr/`` stopped\n at 0015: the authoring commits lived on a coordinator branch and no task ever\n declared ``docs/adr/...`` in its locks, so nothing carried the records to the\n default branch. The stated basis of an exemption that merged seven PRs with zero\n@@ -11,11 +11,12 @@\n and the directory drifted with no signal.\n \n This is the same unbound-pair shape behind other silent failures: a reference\n-and its target with nothing asserting they agree. This test binds them. It scans\n-every ``scripts/*.py`` (plus ``docs/**/*.md`` and the root ``*.md`` governance\n-files, which are cheap) for the ``ADR ####`` pattern and asserts each cited\n-number resolves to exactly one ``docs/adr/####-*.md``. The converse is\n-deliberately NOT required: an ADR may exist without being cited.\n+and its target with nothing asserting they agree. This test binds them. Target\n+``ADR NNNN`` citations resolve only to ``docs/adr/NNNN-*.md``; Shiki\n+``SADR-NNNN`` citations resolve only to ``docs/adr/SADR-NNNN-*.md``. The scan\n+covers current governance docs, scripts, workflows, prompts, packaging metadata,\n+and repo-local Codex guidance while deliberately excluding historical\n+``.shiki`` evidence. The converse is not required: a record may be uncited.\n \n The suite is dependency free: standard-library ``unittest`` only.\n \"\"\"\n@@ -31,17 +32,18 @@\n \n REPO_ROOT = Path(__file__).resolve().parents[1]\n \n-# \"ADR 0016\" -- a four-digit citation. The trailing \\b keeps a five-digit token\n-# from being read as a truncated four-digit number.\n-_CITATION = re.compile(r\"\\bADR (\\d{4})\\b\")\n-# \"0016-locks-bound-product-paths.md\" -- the leading four digits name the record.\n-_ADR_FILENAME = re.compile(r\"^(\\d{4})-.*\\.md$\")\n+# ``ADR 0016`` / ``SADR-0016`` -- a namespace plus four-digit citation. The\n+# trailing ``\\b`` keeps a five-digit token from being read as a truncated one.\n+_CITATION = re.compile(r\"\\b(S?ADR)[ -](\\d{4})\\b\")\n+# Target ADRs retain the numeric filename; Shiki platform decisions use SADR.\n+_DECISION_FILENAME = re.compile(r\"^(?:(SADR)-)?(\\d{4})-.*\\.md$\")\n \n \n class Violation(NamedTuple):\n     \"\"\"A citation that does not resolve to exactly one decision record.\"\"\"\n \n     citing_file: str  # repo-relative path of the file that cites the ADR\n+    namespace: str  # \"ADR\" for target decisions; \"SADR\" for Shiki decisions\n     number: str  # the cited four-digit number, e.g. \"0016\"\n     line: int  # 1-based line of the citation\n     reason: str  # \"missing\" (zero matches) or \"ambiguous\" (two or more)\n@@ -49,44 +51,59 @@\n \n     @property\n     def expected_path(self) -> str:\n-        return f\"docs/adr/{self.number}-*.md\"\n+        prefix = \"SADR-\" if self.namespace == \"SADR\" else \"\"\n+        return f\"docs/adr/{prefix}{self.number}-*.md\"\n \n     def describe(self) -> str:\n         \"\"\"Name the citing file, the cited number, and the missing path.\"\"\"\n         if self.reason == \"missing\":\n             return (\n-                f\"{self.citing_file}:{self.line} cites ADR {self.number} \"\n+                f\"{self.citing_file}:{self.line} cites {self.namespace} {self.number} \"\n                 f\"but no document matches {self.expected_path}\"\n             )\n         joined = \", \".join(self.matches)\n         return (\n-            f\"{self.citing_file}:{self.line} cites ADR {self.number} which \"\n+            f\"{self.citing_file}:{self.line} cites {self.namespace} {self.number} which \"\n             f\"resolves to {len(self.matches)} documents (expected exactly one \"\n             f\"at {self.expected_path}): {joined}\"\n         )\n \n \n-def _adr_index(root: Path) -> dict[str, list[Path]]:\n-    \"\"\"Map every four-digit ADR number to the record files that claim it.\"\"\"\n-    index: dict[str, list[Path]] = {}\n+def _adr_index(root: Path) -> dict[tuple[str, str], list[Path]]:\n+    \"\"\"Map each decision namespace and number to the files that claim it.\"\"\"\n+    index: dict[tuple[str, str], list[Path]] = {}\n     adr_dir = root / \"docs\" / \"adr\"\n     if adr_dir.is_dir():\n         for path in sorted(adr_dir.glob(\"*.md\")):\n-            match = _ADR_FILENAME.match(path.name)\n+            match = _DECISION_FILENAME.match(path.name)\n             if match:\n-                index.setdefault(match.group(1), []).append(path)\n+                namespace = \"SADR\" if match.group(1) else \"ADR\"\n+                index.setdefault((namespace, match.group(2)), []).append(path)\n     return index\n \n \n def _citing_files(root: Path, include_docs: bool = True) -> list[Path]:\n-    \"\"\"Files scanned for citations: scripts/*.py, and cheaply the markdown.\"\"\"\n+    \"\"\"Current, shipped text surfaces scanned for decision citations.\"\"\"\n     files = sorted((root / \"scripts\").glob(\"*.py\"))\n+    files += sorted((root / \"scripts\").glob(\"*.sh\"))\n     if include_docs:\n         docs = root / \"docs\"\n         if docs.is_dir():\n             files += sorted(docs.rglob(\"*.md\"))\n         files += sorted(root.glob(\"*.md\"))\n-    return files\n+        pyproject = root / \"pyproject.toml\"\n+        if pyproject.is_file():\n+            files.append(pyproject)\n+        for relative in (\".github\", \".codex\"):\n+            surface = root / relative\n+            if surface.is_dir():\n+                files += sorted(\n+                    path\n+                    for path in surface.rglob(\"*\")\n+                    if path.is_file()\n+                    and path.suffix in {\".md\", \".py\", \".sh\", \".toml\", \".yaml\", \".yml\"}\n+                )\n+    return sorted(set(files))\n \n \n def find_adr_reference_violations(\n@@ -94,10 +111,9 @@\n ) -> list[Violation]:\n     \"\"\"Return every ``ADR ####`` citation under ``root`` that fails to resolve.\n \n-    A citation resolves when exactly one ``docs/adr/####-*.md`` matches its\n-    number. Zero matches is a \"missing\" violation; two or more is \"ambiguous\".\n-    Records that are never cited produce nothing -- the converse is not\n-    required.\n+    A citation resolves when exactly one record in the same namespace matches\n+    its number. Zero matches is a \"missing\" violation; two or more is\n+    \"ambiguous\". Records that are never cited produce nothing.\n     \"\"\"\n     root = Path(root)\n     index = _adr_index(root)\n@@ -110,13 +126,15 @@\n         rel = path.relative_to(root).as_posix()\n         for lineno, line in enumerate(text.splitlines(), start=1):\n             for match in _CITATION.finditer(line):\n-                number = match.group(1)\n-                found = index.get(number, [])\n+                namespace, number = match.groups()\n+                found = index.get((namespace, number), [])\n                 if len(found) == 1:\n                     continue\n                 reason = \"missing\" if not found else \"ambiguous\"\n                 rels = [p.relative_to(root).as_posix() for p in found]\n-                violations.append(Violation(rel, number, lineno, reason, rels))\n+                violations.append(\n+                    Violation(rel, namespace, number, lineno, reason, rels)\n+                )\n     return violations\n \n \n@@ -129,8 +147,8 @@\n         self.assertEqual(\n             violations,\n             [],\n-            \"Every 'ADR ####' citation must resolve to exactly one \"\n-            f\"docs/adr/####-*.md:\\n{detail}\",\n+            \"Every ADR/SADR citation must resolve exactly once in its own \"\n+            f\"docs/adr namespace:\\n{detail}\",\n         )\n \n     def test_scan_actually_reads_scripts(self) -> None:\n@@ -142,12 +160,38 @@\n             \"the citation scan must cover scripts/*.py\",\n         )\n \n+    def test_platform_citations_do_not_use_unresolved_numeric_shorthand(self) -> None:\n+        shorthand = re.compile(r\"SADR-\\d{4}/\\d{4}\")\n+        offenders = []\n+        for path in _citing_files(REPO_ROOT):\n+            text = path.read_text(encoding=\"utf-8\")\n+            if shorthand.search(text):\n+                offenders.append(path.relative_to(REPO_ROOT).as_posix())\n+        self.assertEqual(\n+            offenders,\n+            [],\n+            \"repeat the SADR prefix so every cited number is independently resolvable\",\n+        )\n+\n     def test_carried_records_are_indexed(self) -> None:\n         # The two records this Goal carried onto the default branch, and which\n         # the scripts cite most, must be present.\n         index = _adr_index(REPO_ROOT)\n-        self.assertIn(\"0016\", index)\n-        self.assertIn(\"0017\", index)\n+        self.assertIn((\"SADR\", \"0016\"), index)\n+        self.assertIn((\"SADR\", \"0017\"), index)\n+\n+    def test_platform_tree_has_only_sadr_0001_through_0019(self) -> None:\n+        adr_dir = REPO_ROOT / \"docs\" / \"adr\"\n+        sadrs = sorted(adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\"))\n+        self.assertEqual(\n+            {path.name.split(\"-\", 2)[1] for path in sadrs},\n+            {f\"{number:04d}\" for number in range(1, 20)},\n+        )\n+        self.assertEqual(\n+            list(adr_dir.glob(\"[0-9][0-9][0-9][0-9]-*.md\")),\n+            [],\n+            \"the Shiki platform tree must not retain legacy numeric SADR paths\",\n+        )\n \n \n class FixtureAdrReferences(unittest.TestCase):\n@@ -180,6 +224,26 @@\n         self.assertIn(\"9991\", message)  # cited number\n         self.assertIn(\"docs/adr/9991-*.md\", message)  # missing path\n \n+    def test_target_adr_does_not_satisfy_missing_platform_sadr(self) -> None:\n+        root = self._make_repo(\n+            adr_names=[\"0042-target-decision.md\"],\n+            script_body='\"\"\"Target ADR 0042 exists, but platform SADR-0042 does not.\"\"\"\\n',\n+        )\n+        violations = find_adr_reference_violations(root, include_docs=False)\n+        self.assertEqual(len(violations), 1)\n+        self.assertEqual(violations[0].namespace, \"SADR\")\n+        self.assertEqual(violations[0].number, \"0042\")\n+        self.assertEqual(violations[0].expected_path, \"docs/adr/SADR-0042-*.md\")\n+\n+    def test_target_adr_and_platform_sadr_may_reuse_a_number(self) -> None:\n+        root = self._make_repo(\n+            adr_names=[\"0042-target-decision.md\", \"SADR-0042-platform-decision.md\"],\n+            script_body='\"\"\"Target ADR 0042 and platform SADR-0042 are distinct.\"\"\"\\n',\n+        )\n+        self.assertEqual(\n+            find_adr_reference_violations(root, include_docs=False), []\n+        )\n+\n     def test_uncited_adr_is_allowed(self) -> None:\n         # An ADR exists but nothing cites it -> not a violation. The converse\n         # (every ADR must be cited) is explicitly NOT required.\ndiff --git a/tests/test_guardian_review.py b/tests/test_guardian_review.py\nindex 88829e7810212635e2fc98aa5918d6f26294677d..00a85a237acd5bf2d6cb5b294128198c422402ca\n--- a/tests/test_guardian_review.py\n+++ b/tests/test_guardian_review.py\n@@ -162,7 +162,7 @@\n \n     def test_docs_only_is_light_and_not_conservative(self):\n         focus = gr.classify_review_focus([\"docs/adr/0099-x.md\", \"README.md\"], \"\", \"low\")\n-        self.assertTrue(any(\"Docs/ADR-only\" in f for f in focus))\n+        self.assertTrue(any(\"Docs/decision-record-only\" in f for f in focus))\n         self.assertFalse(any(\"conservative default\" in f.lower() for f in focus))\n \n     def test_unknown_category_is_conservative(self):\ndiff --git a/tests/test_policy_job_merged_prs.py b/tests/test_policy_job_merged_prs.py\nindex d45512e74681e5e649279236c7d4e79d5c3da471..b97989f8bd74d091de1a6b5275a20d8dc68c4512\n--- a/tests/test_policy_job_merged_prs.py\n+++ b/tests/test_policy_job_merged_prs.py\n@@ -3,8 +3,8 @@\n \n ``.github/workflows/shiki-cca-completion.yml`` has two consumers of the merged-PR\n proof: the CCA *signal* (``guardian_approval_signal.py`` in \"Prepare CCA context\")\n-and the authoritative gate (``mergegate_check.py`` in \"Enforce MergeGate\"). ADR\n-0017's bookkeeping-closeout exemption (condition 6) needs the merged implementation\n+and the authoritative gate (``mergegate_check.py`` in \"Enforce MergeGate\").\n+SADR-0017's bookkeeping-closeout exemption (condition 6) needs the merged implementation\n PR proven, and a closeout carries NO ``post_merge_reconcile`` marker. The signal\n already resolves ``MERGED_PRS`` unconditionally, but \"Enforce MergeGate\" used to\n resolve it only inside ``grep -q '<!-- shiki:post_merge_reconcile -->'``. So a\ndiff --git a/tests/test_sadr_install_migration.py b/tests/test_sadr_install_migration.py\nnew file mode 100644\nindex 0000000000000000000000000000000000000000..d51eac872b1c79ff4579c4c1401ca5d775a60d8d\n--- /dev/null\n+++ b/tests/test_sadr_install_migration.py\n@@ -0,0 +1,237 @@\n+\"\"\"SADR installation and collision-safe legacy upgrade behavior.\"\"\"\n+\n+from __future__ import annotations\n+\n+import contextlib\n+import hashlib\n+import io\n+import json\n+import shutil\n+import tempfile\n+import unittest\n+from pathlib import Path\n+\n+import shiki_test_support  # noqa: F401  (path bootstrap)\n+\n+import shiki_installer\n+from tests.test_adr_references import find_adr_reference_violations\n+\n+\n+PRODUCT_NUMBERS = tuple(range(13, 19))\n+\n+\n+def _quiet_install(target: Path, *, force: bool, validate: bool) -> None:\n+    with contextlib.redirect_stdout(io.StringIO()):\n+        shiki_installer.install_template(target, force=force, validate=validate)\n+\n+\n+def _sha256(path: Path) -> str:\n+    return hashlib.sha256(path.read_bytes()).hexdigest()\n+\n+\n+def _legacy_sadr_paths() -> tuple[str, ...]:\n+    return tuple(\n+        f\"docs/adr/{path.name.removeprefix('SADR-')}\"\n+        for path in sorted((shiki_installer.ROOT / \"docs\" / \"adr\").glob(\"SADR-*.md\"))\n+        if path.name != \"SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\"\n+    )\n+\n+\n+def _create_legacy_upgrade_fixture(\n+    target: Path,\n+) -> tuple[dict[str, bytes], tuple[str, ...]]:\n+    adr_dir = target / \"docs\" / \"adr\"\n+    adr_dir.mkdir(parents=True)\n+    product_bytes = {\n+        f\"{number:04d}-cubeshare-product-decision.md\": (\n+            f\"# CubeShare ADR {number:04d}\\n\\nDo not rewrite {number}.\\n\"\n+        ).encode()\n+        for number in PRODUCT_NUMBERS\n+    }\n+    for name, content in product_bytes.items():\n+        (adr_dir / name).write_bytes(content)\n+\n+    legacy_paths = _legacy_sadr_paths()\n+    for relative in legacy_paths:\n+        (target / relative).write_bytes(f\"legacy Shiki bytes for {relative}\\n\".encode())\n+\n+    stamp_path = target / shiki_installer.INSTALL_STAMP_PATH\n+    stamp_path.parent.mkdir(parents=True, exist_ok=True)\n+    stamp = {\n+        \"version\": shiki_installer.INSTALL_STAMP_VERSION,\n+        \"platform_commit\": None,\n+        \"installed_at\": \"2000-01-01T00:00:00+00:00\",\n+        \"digests\": {\n+            relative: _sha256(target / relative) for relative in legacy_paths\n+        },\n+    }\n+    stamp_path.write_text(\n+        json.dumps(stamp, indent=2, sort_keys=True) + \"\\n\", encoding=\"utf-8\"\n+    )\n+    return product_bytes, legacy_paths\n+\n+\n+def _tree_snapshot(root: Path) -> dict[str, tuple[str, bytes | str]]:\n+    \"\"\"Recursive byte/shape snapshot used to prove preflight is write-free.\"\"\"\n+    snapshot: dict[str, tuple[str, bytes | str]] = {}\n+    for path in sorted(root.rglob(\"*\")):\n+        relative = path.relative_to(root).as_posix()\n+        if path.is_symlink():\n+            snapshot[relative] = (\"symlink\", path.readlink().as_posix())\n+        elif path.is_dir():\n+            snapshot[relative] = (\"directory\", \"\")\n+        else:\n+            snapshot[relative] = (\"file\", path.read_bytes())\n+    return snapshot\n+\n+\n+class FreshSadrInstallTests(unittest.TestCase):\n+    def setUp(self) -> None:\n+        self.tmp = Path(tempfile.mkdtemp(prefix=\"shiki-sadr-fresh-\"))\n+        self.addCleanup(lambda: shutil.rmtree(self.tmp, ignore_errors=True))\n+        self.target = self.tmp / \"target\"\n+        self.adr_dir = self.target / \"docs\" / \"adr\"\n+        self.adr_dir.mkdir(parents=True)\n+        self.product_bytes = {\n+            f\"{number:04d}-product-decision.md\": (\n+                f\"# Product ADR {number:04d}\\n\\nTarget-owned bytes {number}.\\n\"\n+            ).encode()\n+            for number in PRODUCT_NUMBERS\n+        }\n+        for name, content in self.product_bytes.items():\n+            (self.adr_dir / name).write_bytes(content)\n+\n+    def test_fresh_install_preserves_product_adrs_and_installs_all_sadrs(self) -> None:\n+        _quiet_install(self.target, force=False, validate=True)\n+\n+        for name, expected in self.product_bytes.items():\n+            self.assertEqual((self.adr_dir / name).read_bytes(), expected)\n+\n+        sadr_numbers = {\n+            path.name.split(\"-\", 2)[1]\n+            for path in self.adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\")\n+        }\n+        self.assertEqual(sadr_numbers, {f\"{number:04d}\" for number in range(1, 20)})\n+\n+        numeric_records = {\n+            path.name\n+            for path in self.adr_dir.glob(\"[0-9][0-9][0-9][0-9]-*.md\")\n+        }\n+        self.assertEqual(numeric_records, set(self.product_bytes))\n+        stamp = json.loads(\n+            (self.target / shiki_installer.INSTALL_STAMP_PATH).read_text(\n+                encoding=\"utf-8\"\n+            )\n+        )\n+        for path in self.adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\"):\n+            relative = path.relative_to(self.target).as_posix()\n+            self.assertEqual(stamp[\"digests\"][relative], _sha256(path))\n+        self.assertEqual(find_adr_reference_violations(self.target), [])\n+\n+\n+class LegacySadrUpgradeTests(unittest.TestCase):\n+    def setUp(self) -> None:\n+        self.tmp = Path(tempfile.mkdtemp(prefix=\"shiki-sadr-upgrade-\"))\n+        self.addCleanup(lambda: shutil.rmtree(self.tmp, ignore_errors=True))\n+        self.target = self.tmp / \"target\"\n+        self.adr_dir = self.target / \"docs\" / \"adr\"\n+        self.product_bytes, self.legacy_paths = _create_legacy_upgrade_fixture(\n+            self.target\n+        )\n+\n+    def test_force_upgrade_deletes_only_stamped_legacy_sadrs(self) -> None:\n+        _quiet_install(self.target, force=True, validate=True)\n+\n+        for relative in self.legacy_paths:\n+            self.assertFalse((self.target / relative).exists(), relative)\n+        for name, expected in self.product_bytes.items():\n+            self.assertEqual((self.adr_dir / name).read_bytes(), expected)\n+\n+        sadrs = list(self.adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\"))\n+        self.assertEqual(len(sadrs), 19)\n+        refreshed = json.loads(\n+            (self.target / shiki_installer.INSTALL_STAMP_PATH).read_text(encoding=\"utf-8\")\n+        )\n+        for relative in self.legacy_paths:\n+            self.assertNotIn(relative, refreshed[\"digests\"])\n+        for path in sadrs:\n+            relative = path.relative_to(self.target).as_posix()\n+            self.assertEqual(refreshed[\"digests\"][relative], _sha256(path))\n+\n+    def test_non_force_install_never_deletes_legacy_files(self) -> None:\n+        before = {\n+            relative: (self.target / relative).read_bytes()\n+            for relative in self.legacy_paths\n+        }\n+        _quiet_install(self.target, force=False, validate=False)\n+        for relative, expected in before.items():\n+            self.assertEqual((self.target / relative).read_bytes(), expected)\n+        refreshed = json.loads(\n+            (self.target / shiki_installer.INSTALL_STAMP_PATH).read_text(encoding=\"utf-8\")\n+        )\n+        for relative in self.legacy_paths:\n+            self.assertEqual(refreshed[\"digests\"][relative], _sha256(self.target / relative))\n+\n+\n+class LegacyCleanupPreflightTests(unittest.TestCase):\n+    def setUp(self) -> None:\n+        self.tmp = Path(tempfile.mkdtemp(prefix=\"shiki-sadr-preflight-\"))\n+        self.addCleanup(lambda: shutil.rmtree(self.tmp, ignore_errors=True))\n+\n+    def test_all_ownership_blockers_abort_before_any_write(self) -> None:\n+        cases = (\n+            (\"absent\", \"install stamp is absent\"),\n+            (\"unreadable\", \"install stamp is unreadable\"),\n+            (\"malformed\", \"install stamp is malformed\"),\n+            (\"missing-digest\", \"install stamp has no digest\"),\n+            (\"digest-mismatch\", \"digest mismatch\"),\n+        )\n+        for name, expected_reason in cases:\n+            with self.subTest(name=name):\n+                target = self.tmp / name\n+                _, legacy_paths = _create_legacy_upgrade_fixture(target)\n+                stamp_path = target / shiki_installer.INSTALL_STAMP_PATH\n+\n+                if name == \"absent\":\n+                    stamp_path.unlink()\n+                elif name == \"unreadable\":\n+                    stamp_path.unlink()\n+                    stamp_path.mkdir()\n+                elif name == \"malformed\":\n+                    stamp_path.write_text(\"{not-json\\n\", encoding=\"utf-8\")\n+                else:\n+                    stamp = json.loads(stamp_path.read_text(encoding=\"utf-8\"))\n+                    if name == \"missing-digest\":\n+                        stamp[\"digests\"] = {}\n+                    else:\n+                        stamp[\"digests\"] = {\n+                            relative: \"deadbeef\" for relative in legacy_paths\n+                        }\n+                    stamp_path.write_text(\n+                        json.dumps(stamp, indent=2, sort_keys=True) + \"\\n\",\n+                        encoding=\"utf-8\",\n+                    )\n+\n+                before = _tree_snapshot(target)\n+                with self.assertRaises(shiki_installer.ShikiError) as raised:\n+                    _quiet_install(target, force=True, validate=False)\n+                after = _tree_snapshot(target)\n+\n+                self.assertEqual(before, after, f\"{name} preflight changed the tree\")\n+                message = str(raised.exception)\n+                self.assertIn(expected_reason, message)\n+                self.assertIn(\"No files were written\", message)\n+                for relative in legacy_paths:\n+                    self.assertIn(relative, message)\n+\n+    def test_absent_stamp_is_allowed_when_no_legacy_path_exists(self) -> None:\n+        target = self.tmp / \"no-legacy\"\n+        target.mkdir()\n+        _quiet_install(target, force=True, validate=False)\n+        self.assertTrue(\n+            (target / \"docs\" / \"adr\" / \"SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\").is_file()\n+        )\n+\n+\n+if __name__ == \"__main__\":\n+    unittest.main()\n\n2026-08-07T08:25:28.664679Z ERROR codex_models_manager::manager: failed to renew cache TTL: missing field `base_instructions` at line 94 column 5\nexec\n/bin/zsh -lc \"sed -n '1,220p' .shiki/ledger/L-20260807T073307221361Z-b3c3a718.json && sed -n '1,220p' .shiki/ledger/L-20260807T075227693451Z-abe727ae.json\" in /private/tmp/shiki-adr-collision.pa0KwY/.worktrees/shiki-t-20260807t073307221254z-2d22d654-introduc\n succeeded in 0ms:\n{\n  \"actor\": \"shiki-cli\",\n  \"evidence\": [\n    \".shiki/tasks/T-20260807T073307221254Z-2d22d654.json\"\n  ],\n  \"goal_id\": \"G-20260807T073307219752Z-1ee1ed68\",\n  \"id\": \"L-20260807T073307221361Z-b3c3a718\",\n  \"links\": [],\n  \"summary\": \"Task registered from plan: Introduce SADR and migrate installs without touching product ADRs\",\n  \"task_id\": \"T-20260807T073307221254Z-2d22d654\",\n  \"timestamp\": \"2026-08-07T07:33:07+00:00\",\n  \"type\": \"task-registered\"\n}\n{\n  \"actor\": \"shiki-cli\",\n  \"evidence\": [\n    \".shiki/tasks/T-20260805T025939375698Z-94a2fa82.json\"\n  ],\n  \"goal_id\": \"G-20260805T025939374293Z-96ec6ced\",\n  \"id\": \"L-20260807T075227693451Z-abe727ae\",\n  \"links\": [],\n  \"summary\": \"Task T-20260805T025939375698Z-94a2fa82 status changed to review\",\n  \"task_id\": \"T-20260805T025939375698Z-94a2fa82\",\n  \"timestamp\": \"2026-08-07T07:52:27+00:00\",\n  \"type\": \"check\"\n}\n\ndiff --git a/.codex/skills/shiki/SKILL.md b/.codex/skills/shiki/SKILL.md\nindex c8e2e85d9d75d49cc4f2d6286ee22659c4e53e75..93cded623b2c15d9f65c76491a1c2d813ac9e6c0\n--- a/.codex/skills/shiki/SKILL.md\n+++ b/.codex/skills/shiki/SKILL.md\n@@ -18,6 +18,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n Codex CLI does not register this skill as a custom `/shiki` slash command. When\n the user types `/shiki` in Codex CLI and gets \"Unrecognized command\", explain\n that this is expected: invoke Shiki in Codex with natural language such as\n@@ -69,7 +72,7 @@\n \n ## Responsibilities\n \n-- Claude Code implements and repairs by default (`shiki runner claude`, ADR 0008); Codex implements and repairs tasks explicitly assigned to `codex`.\n+- Claude Code implements and repairs by default (`shiki runner claude`, SADR-0008); Codex implements and repairs tasks explicitly assigned to `codex`.\n - Claude Code Action can act as GitHub-side CCA or reviewer.\n - CCA judges completion.\n - MergeGate authorizes state transitions and merge readiness.\ndiff --git a/.github/prompts/cca-completion-check.md b/.github/prompts/cca-completion-check.md\nindex ed203ad5f302ecc81372d8ab27d2b1232ca97497..fd89950528333ef6242e6d5443904f5392ab7e56\n--- a/.github/prompts/cca-completion-check.md\n+++ b/.github/prompts/cca-completion-check.md\n@@ -19,6 +19,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n ## Required Reading\n \n Read only the files needed to judge the PR contract. Prefer this order:\ndiff --git a/.github/prompts/codex-task-handoff.md b/.github/prompts/codex-task-handoff.md\nindex 695e211d76dea1c60e8d5bb360e78487808286a4..59441d576b01a39569722f226c854e5a740e9eb5\n--- a/.github/prompts/codex-task-handoff.md\n+++ b/.github/prompts/codex-task-handoff.md\n@@ -2,7 +2,7 @@\n \n You are Codex Front acting as the Implementer for a Shiki task.\n \n-Follow `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant ADRs, and `docs/agents/codex-handoff.md`.\n+Follow `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant Shiki SADRs and target ADRs, and `docs/agents/codex-handoff.md`.\n \n Rules:\n \ndiff --git a/AGENTS.md b/AGENTS.md\nindex 7f041100d49c2a7525f51cb0f5d696a4a6d1c78c..a06bfbabc6fa48a007389e25e12d7ed523936bed\n--- a/AGENTS.md\n+++ b/AGENTS.md\n@@ -22,7 +22,7 @@\n - **Codex Front** is an optional implementer through Codex App, Codex CLI, Codex IDE extension, or Codex Web signed in with ChatGPT OAuth/subscription auth, dispatched with `shiki runner codex` when a task is explicitly assigned to `codex`.\n - **GitHub CCA** is implemented by Claude Code Action by default, using `CLAUDE_CODE_OAUTH_TOKEN`.\n \n-Do not assume `openai/codex-action`, `OPENAI_API_KEY`, or API-key based Codex automation in the default Shiki path. API-key runners are explicit target-repo extensions and require their own ADR.\n+Do not assume `openai/codex-action`, `OPENAI_API_KEY`, or API-key based Codex automation in the default Shiki path. API-key runners are explicit target-repo extensions and require their own target ADR.\n \n Shiki is not:\n \n@@ -44,10 +44,14 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use\n+`SADR-NNNN-*.md`; target product decisions retain `NNNN-*.md` and the `ADR`\n+namespace.\n+\n Use the canonical source-of-truth order above. Runtime-specific reading orders\n are operational guidance only and must not override or reorder it.\n \n-Conversation state is not durable truth. If a decision matters, put it in GitHub, `.shiki/`, `CONTEXT.md`, or an ADR.\n+Conversation state is not durable truth. If a decision matters, put it in GitHub, `.shiki/`, `CONTEXT.md`, a Shiki SADR, or a target ADR.\n \n If GitHub and `.shiki/` disagree, surface the conflict and prefer GitHub operational state until the mirror is repaired.\n \n@@ -75,8 +79,8 @@\n Every non-trivial change follows this loop:\n \n 1. **Goal**: clarify outcome, completion conditions, non-goals, risk level, and success signals.\n-2. **grill-with-docs**: challenge the plan against domain docs, ADRs, code reality, terminology, and edge scenarios.\n-3. **Context & Impact**: find relevant docs, ADRs, code, dependencies, owners, locks, and verification surfaces.\n+2. **grill-with-docs**: challenge the plan against domain docs, Shiki SADRs, target ADRs, code reality, terminology, and edge scenarios.\n+3. **Context & Impact**: find relevant docs, SADRs, target ADRs, code, dependencies, owners, locks, and verification surfaces.\n 4. **PRD and Spec Freeze**: use `to-prd` when context is settled enough to become durable product/engineering intent. The operator's explicit approval of the PRD is Spec Freeze: the plan records a `spec_freeze` block, and no plan runs without it. Steps 1-4 form Requirements Definition \u2014 one continuous operator dialogue. After Spec Freeze, scope changes require an operator-approved Spec Amendment; non-scope-moving interpretations go to the Assumption Log.\n 5. **Issues**: use `to-issues` to create independently grabbable vertical-slice issues. Prefer AFK slices over HITL slices where possible.\n 6. **Triage**: label issues for readiness, risk, runtime, skills, and MergeGate state.\n@@ -167,7 +171,7 @@\n - Testing decisions.\n - Out-of-scope items.\n - Further notes.\n-- Links to relevant domain terms and ADRs.\n+- Links to relevant domain terms, SADRs, and target ADRs.\n \n Every executable task must include:\n \n@@ -193,7 +197,7 @@\n \n Context & Impact must identify:\n \n-- Relevant documents, domain terms, ADRs, and past decisions.\n+- Relevant documents, domain terms, SADRs, target ADRs, and past decisions.\n - Relevant modules, interfaces, seams, callers, owners, and tests.\n - Dependency relationships between tasks.\n - Candidate locks and lock conflicts.\n@@ -210,7 +214,7 @@\n | Skill | Required when |\n | --- | --- |\n | `setup-matt-pocock-skills` | First configuring the repo for skills, or when issue tracker, triage labels, or domain docs are missing. |\n-| `grill-with-docs` | Default for non-trivial Goals before PRD/issues; always required when plans, terminology, boundaries, tradeoffs, or ADR-worthy decisions are ambiguous. |\n+| `grill-with-docs` | Default for non-trivial Goals before PRD/issues; always required when plans, terminology, boundaries, tradeoffs, or decision-record-worthy choices are ambiguous. |\n | `zoom-out` | The agent lacks an architectural map of the relevant code area. |\n | `to-prd` | Settled Goal context must become durable product/engineering intent. |\n | `to-issues` | A Goal, PRD, or plan must become independently grabbable vertical-slice issues. |\n@@ -348,7 +352,7 @@\n - Three or more new files.\n - Any high-risk or critical label.\n \n-When the architecture gate triggers, run `grill-with-docs` or `improve-codebase-architecture` as appropriate and record durable decisions in an ADR when needed.\n+When the architecture gate triggers, run `grill-with-docs` or `improve-codebase-architecture` as appropriate and record durable decisions in a Shiki SADR or target ADR as applicable.\n \n ## Engineering Discipline\n \ndiff --git a/CLAUDE.md b/CLAUDE.md\nindex 98a932e7841a632866fcd53b0bad0e3af3711d3d..98a31399404d6fb044b4fd493f4459ac87f439de\n--- a/CLAUDE.md\n+++ b/CLAUDE.md\n@@ -14,6 +14,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n ## Claude Role\n \n Default to planner, default implementer, reviewer, coordinator, documentation editor, CCA-style completion judge, and final-judgment assistant.\n@@ -57,7 +60,7 @@\n At the start of a Shiki session:\n \n 1. Read `AGENTS.md`.\n-2. Read `CONTEXT.md` and relevant ADRs.\n+2. Read `CONTEXT.md` and relevant Shiki SADRs and target ADRs.\n 3. Read active `.shiki/` Goal/task/ledger state when present.\n 4. Read active distilled rules from `.shiki/memories/` (status `distilled`, `active: true`, not revoked or superseded) for applicable guidance; the same rules are injected deterministically into task handoffs as the `## Distilled Rules` section (\u00a73.5).\n 5. Read the GitHub Issue or PR when the task is GitHub-backed.\n@@ -74,7 +77,7 @@\n - Define the Goal, completion conditions, non-goals, and risk level.\n - Treat `grill-with-docs` as the default planning entry point for non-trivial Goals.\n - Challenge domain terms against `CONTEXT.md`.\n-- Challenge hard-to-reverse tradeoffs against ADRs.\n+- Challenge hard-to-reverse tradeoffs against SADRs and target ADRs.\n - Explore code instead of asking when code can answer the question.\n - Run Context & Impact before implementation planning.\n - Produce a Task DAG, not an unordered checklist.\n@@ -94,7 +97,7 @@\n - For each question, provide your recommended answer.\n - Resolve dependency decisions before downstream design questions.\n - Update `CONTEXT.md` inline when terminology crystallizes.\n-- Offer ADRs only when the decision is hard to reverse, surprising without context, and a real tradeoff.\n+- Offer a SADR or target ADR only when the decision is hard to reverse, surprising without context, and a real tradeoff.\n - Convert unresolved questions into HITL blockers rather than allowing Codex to guess.\n \n ## PRD and Issues Standard\n@@ -103,7 +106,7 @@\n \n - Use the project's domain glossary vocabulary.\n - Avoid volatile file paths unless they are essential.\n-- Include implementation decisions, testing decisions, out-of-scope items, and relevant ADR/domain links.\n+- Include implementation decisions, testing decisions, out-of-scope items, and relevant SADR/target-ADR/domain links.\n - Identify deep-module opportunities and testable interfaces.\n \n When creating issues:\n@@ -121,7 +124,7 @@\n - Branch/worktree target.\n - Scope and non-goals.\n - Dependencies and locks.\n-- Relevant docs, ADRs, modules, and tests.\n+- Relevant docs, SADRs, target ADRs, modules, and tests.\n - Required skill invocations.\n - TDD expectations.\n - Pre-PR `code-review` gate expectations.\n@@ -152,7 +155,7 @@\n \n - Treat yourself as a judge, not implementer.\n - Do not write production code.\n-- Read `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant ADRs, PRD, task issue, PR body, diff, checks, reviews, labels, and `.shiki/` state.\n+- Read `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant SADRs and target ADRs, PRD, task issue, PR body, diff, checks, reviews, labels, and `.shiki/` state.\n - Evaluate every applicable checklist in `docs/agents/checklists.md`.\n - Map each acceptance criterion to durable evidence.\n - Distinguish implementation failure from missing evidence.\ndiff --git a/CONTEXT.md b/CONTEXT.md\nindex e61a9be06a0cce0d026484083a657bde2067e234..237ee8aa3ea8fab334ece49768bc5ed3b5af0974\n--- a/CONTEXT.md\n+++ b/CONTEXT.md\n@@ -25,7 +25,7 @@\n _Avoid_: jumping directly into code\n \n **grill-with-docs**:\n-A planning skill that challenges a plan against domain language, ADRs, code reality, and concrete edge scenarios. It resolves design-tree questions before PRD/issues. It runs inside Requirements Definition.\n+A planning skill that challenges a plan against domain language, Shiki SADRs, target ADRs, code reality, and concrete edge scenarios. It resolves design-tree questions before PRD/issues. It runs inside Requirements Definition.\n _Avoid_: generic brainstorming, silent assumptions\n \n **Requirements Definition**:\ndiff --git a/CONTRIBUTING.md b/CONTRIBUTING.md\nindex c6d9b656e73abb8a55e5f660123e03153dcb3e6d..9e37a8fbf668db5b22461aaae5548b8f1354394c\n--- a/CONTRIBUTING.md\n+++ b/CONTRIBUTING.md\n@@ -17,7 +17,7 @@\n 1. **Goal** \u2014 start from a user-approved Goal with an outcome, completion\n    conditions, non-goals, risk level, and success signals.\n 2. **grill-with-docs** \u2014 for any non-trivial change, challenge the plan against\n-   domain language, ADRs, and code reality before writing a PRD.\n+   domain language, Shiki SADRs, target ADRs, and code reality before writing a PRD.\n 3. **PRD** \u2014 capture settled product/engineering intent (`to-prd`).\n 4. **Spec Freeze** \u2014 the operator explicitly approves the PRD/requirements;\n    the plan records a `spec_freeze` block and no plan runs without it\n@@ -94,7 +94,7 @@\n   entries.\n - Do not hand-edit `.shiki/goals/*.json` or `.shiki/manifest.json` in a task PR.\n - Conversation state is not durable truth. If a decision matters, record it in\n-  GitHub, `.shiki/`, `CONTEXT.md`, or an ADR.\n+  GitHub, `.shiki/`, `CONTEXT.md`, a Shiki SADR, or a target ADR.\n \n ## Architecture Gate\n \n@@ -103,7 +103,7 @@\n or CI policy, package structure, three or more structural files, more than 100\n net new lines in a core module, three or more new files, or any high-risk label.\n When triggered, run `grill-with-docs` or `improve-codebase-architecture` and\n-record durable decisions in an ADR when needed (see `docs/adr/`).\n+record durable decisions in a Shiki SADR or target ADR as applicable (see `docs/adr/`).\n \n ## Safety\n \ndiff --git a/SYSTEM_PROMPT.md b/SYSTEM_PROMPT.md\nindex aaaea45e3a896c3a44ba89eec8bb84325ecf6c62..da009c86a88285269dee0155e58e550c6d744ea9\n--- a/SYSTEM_PROMPT.md\n+++ b/SYSTEM_PROMPT.md\n@@ -30,6 +30,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n Use the canonical source-of-truth order above. Runtime-specific reading orders\n are operational guidance only and must not override or reorder it. The current\n conversation is non-durable operator input.\n@@ -48,14 +51,14 @@\n \n A runtime may only do work that is explicitly assigned by the Goal, task, PR, or MergeGate decision.\n \n-Do not assume `openai/codex-action`, `OPENAI_API_KEY`, or API-key based Codex automation in the default Shiki path. API-key runners are explicit target-repo extensions and require their own ADR.\n+Do not assume `openai/codex-action`, `OPENAI_API_KEY`, or API-key based Codex automation in the default Shiki path. API-key runners are explicit target-repo extensions and require their own target ADR.\n \n ## 4. Non-negotiable Shiki flow\n \n For any non-trivial Goal, use this flow:\n \n 1. **Goal Seek**: define outcome, non-goals, risk level, completion criteria, and evidence requirements.\n-2. **grill-with-docs**: challenge the plan against `CONTEXT.md`, ADRs, code reality, terminology, boundaries, and edge scenarios. Ask one question at a time when the operator is available. If code can answer a question, inspect code instead of asking.\n+2. **grill-with-docs**: challenge the plan against `CONTEXT.md`, Shiki SADRs, target ADRs, code reality, terminology, boundaries, and edge scenarios. Ask one question at a time when the operator is available. If code can answer a question, inspect code instead of asking.\n 3. **Context & Impact**: identify modules, interfaces, seams, callers, dependencies, locks, risk, and verification surfaces.\n 4. **to-prd + Spec Freeze**: turn settled context into a PRD using domain vocabulary and testing decisions. The operator's explicit approval of the PRD is Spec Freeze, recorded as the plan's `spec_freeze` block; plans without it do not run. Steps 1-4 form Requirements Definition.\n 5. **to-issues**: decompose the PRD into independently grabbable vertical-slice issues. Prefer AFK slices; mark HITL slices when judgment is still required.\n@@ -75,7 +78,7 @@\n Before making any material change:\n \n 1. Read `AGENTS.md` and any runtime-specific wrapper such as `CLAUDE.md`.\n-2. Read `CONTEXT.md` and relevant ADRs.\n+2. Read `CONTEXT.md` and relevant SADRs and target ADRs.\n 3. Read the GitHub Issue / PR and `.shiki/` state for the active Goal or task.\n 4. Identify your role: Planner, Implementer, Reviewer, Completion Check Agent, Repairer, or Guardian-assist.\n 5. Identify Goal id, task id, branch, PR, dependencies, locks, risk level, required skills, and required evidence.\n@@ -103,7 +106,7 @@\n \n At minimum, identify:\n \n-- Relevant documents, ADRs, domain terms, and prior decisions.\n+- Relevant documents, SADRs, target ADRs, domain terms, and prior decisions.\n - Relevant modules, interfaces, seams, callers, owners, and tests.\n - Dependency relationships between tasks.\n - Candidate file locks and lock conflicts.\n@@ -152,7 +155,7 @@\n \n CCA must read:\n \n-- `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant ADRs.\n+- `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant SADRs and target ADRs.\n - Parent Goal issue, PRD issue, task issue, comments, and labels.\n - PR body, diff, commits, changed files, reviews, CI checks, and artifacts available to the workflow.\n - `.shiki/` task contract, locks, ledger entries, CCA checklist profile, and prior repair packets.\n@@ -214,7 +217,7 @@\n - Three or more new files.\n - Any high-risk or critical label.\n \n-When the architecture gate triggers, run `grill-with-docs` or `improve-codebase-architecture` as appropriate and record the decision in an ADR if it is hard to reverse or likely to be re-litigated.\n+When the architecture gate triggers, run `grill-with-docs` or `improve-codebase-architecture` as appropriate and record the decision in a Shiki SADR or target ADR as applicable if it is hard to reverse or likely to be re-litigated.\n \n ## 14. Skill Gate\n \n@@ -223,7 +226,7 @@\n Use these skills:\n \n - `setup-matt-pocock-skills`: before first use of the engineering skill set, or when issue tracker, triage labels, or domain docs are missing.\n-- `grill-with-docs`: default for non-trivial Goals before PRD/issues; required for ambiguous plans, terminology, boundaries, ADR-worthy tradeoffs, or design-tree decisions.\n+- `grill-with-docs`: default for non-trivial Goals before PRD/issues; required for ambiguous plans, terminology, boundaries, decision-record-worthy tradeoffs, or design-tree decisions.\n - `zoom-out`: unfamiliar code area or missing architectural map.\n - `to-prd`: convert settled context into a PRD.\n - `to-issues`: break a PRD, plan, or Goal into independently grabbable vertical-slice issues.\ndiff --git a/docs/adr/0001-keep-shiki-as-platform-name.md b/docs/adr/SADR-0001-keep-shiki-as-platform-name.md\nindex 20a9637c3b9d8c97de7ba8db7cfae4d82d9a7dba..8b685439959aada1b221c112f0ac3c51ca8a1695\n--- a/docs/adr/0001-keep-shiki-as-platform-name.md\n+++ b/docs/adr/SADR-0001-keep-shiki-as-platform-name.md\n@@ -1,3 +1,3 @@\n-# Keep Shiki As The Platform Name\n+# SADR-0001: Keep Shiki As The Platform Name\n \n Shiki remains the product name for the GitHub-native agentic engineering control plane. We considered more descriptive names such as MergePilot, MergeGate, and Kiroku, but kept Shiki because it carries the existing goal-seeking framework identity while remaining independent of Hermes, Codex, Claude Code, or any single runtime.\ndiff --git a/docs/adr/0002-use-agents-as-shared-constitution.md b/docs/adr/SADR-0002-use-agents-as-shared-constitution.md\nindex 73de30ecb078120eac540d9bc51855e3cd48d0e3..c5486182bf06fca36ea508496b013c67302fd989\n--- a/docs/adr/0002-use-agents-as-shared-constitution.md\n+++ b/docs/adr/SADR-0002-use-agents-as-shared-constitution.md\n@@ -1,3 +1,3 @@\n-# Use AGENTS As The Shared Agent Constitution\n+# SADR-0002: Use AGENTS As The Shared Agent Constitution\n \n Shiki uses `AGENTS.md` as the runtime-neutral constitution and `CLAUDE.md` as a thin Claude Code wrapper. This keeps Codex, Claude Code, Hermes Runner, and future Agent Runtimes aligned on the same GitHub-first MergeGate rules while still allowing runtime-specific instructions where needed.\ndiff --git a/docs/adr/0003-require-skill-gate-for-mergegate-readiness.md b/docs/adr/SADR-0003-require-skill-gate-for-mergegate-readiness.md\nindex 424be0c9af4c8f0136f1fde68a468c47ff84e764..7fc337987c7df9792f75f181dfcbed75fe5ffff2\n--- a/docs/adr/0003-require-skill-gate-for-mergegate-readiness.md\n+++ b/docs/adr/SADR-0003-require-skill-gate-for-mergegate-readiness.md\n@@ -1,3 +1,3 @@\n-# Require Skill Gate For MergeGate Readiness\n+# SADR-0003: Require Skill Gate For MergeGate Readiness\n \n Shiki requires relevant engineering skills to be invoked before execution when their triggers apply. This makes skills part of MergeGate readiness rather than optional prompt style, because Shiki depends on repeatable planning, issue decomposition, TDD, diagnosis, triage, and architecture review before autonomous or parallel agent work can be trusted.\ndiff --git a/docs/adr/0004-use-github-first-with-shiki-mirror.md b/docs/adr/SADR-0004-use-github-first-with-shiki-mirror.md\nindex d0a3a9174dc646a945a9d325954ffaf00c352040..a79c54ae9ee8eed2a0524192b864da4e22a92d3f\n--- a/docs/adr/0004-use-github-first-with-shiki-mirror.md\n+++ b/docs/adr/SADR-0004-use-github-first-with-shiki-mirror.md\n@@ -1,4 +1,4 @@\n-# ADR 0004: Use GitHub First With A Repository-Local Shiki Mirror\n+# SADR-0004: Use GitHub First With A Repository-Local Shiki Mirror\n \n ## Status\n \ndiff --git a/docs/adr/0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md b/docs/adr/SADR-0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\nindex b339baf49b89d9290e79c3d51b36e7b2bde3bd95..0f3c3eb9afccdb3403ce978e21d04fe12e495da0\n--- a/docs/adr/0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\n+++ b/docs/adr/SADR-0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\n@@ -1,8 +1,8 @@\n-# ADR 0005: Use Codex As Front And Claude Code Action As The GitHub Runtime\n+# SADR-0005: Use Codex As Front And Claude Code Action As The GitHub Runtime\n \n ## Status\n \n-Accepted (implementer default superseded by ADR 0008; the auth model and the\n+Accepted (implementer default superseded by SADR-0008; the auth model and the\n Claude Code Action GitHub runtime decision remain in force)\n \n ## Context\n@@ -26,4 +26,4 @@\n - Shiki avoids requiring `OPENAI_API_KEY` for the default workflow.\n - Codex work happens through the user's authenticated Codex front surface, not as a hidden backend Action.\n - Claude Code Action is configured for OAuth-token based subscription usage by default.\n-- API-key based automation remains possible only as an explicit target-repo extension with its own ADR.\n+- API-key based automation remains possible only as an explicit target-repo extension with its own target ADR.\ndiff --git a/docs/adr/0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md b/docs/adr/SADR-0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\nindex 4bcae9d1e52f0a37b8e9fbafa5cd14b6ee1993bd..7a38538a6ae9929567f0ade67e7ea6a60d0b3f7e\n--- a/docs/adr/0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\n+++ b/docs/adr/SADR-0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\n@@ -1,4 +1,4 @@\n-# ADR 0006: Separate Codex Implementation, CCA Completion, And MergeGate Authorization\n+# SADR-0006: Separate Codex Implementation, CCA Completion, And MergeGate Authorization\n \n ## Status\n \ndiff --git a/docs/adr/0007-packaging-and-release.md b/docs/adr/SADR-0007-packaging-and-release.md\nindex 79b84ddb97167372b44641e5b7860e400ce80822..bb445a2e2e856d9d1f5fdbe35de8d4e8cec0fb32\n--- a/docs/adr/0007-packaging-and-release.md\n+++ b/docs/adr/SADR-0007-packaging-and-release.md\n@@ -1,4 +1,4 @@\n-# ADR 0007: Packaging And Release For Shiki\n+# SADR-0007: Packaging And Release For Shiki\n \n ## Status\n \n@@ -25,7 +25,7 @@\n   not introduce third-party runtime dependencies or break the shim.\n - Releases must run inside the operator's subscription-authenticated toolchain\n   and must not require extra secrets beyond the built-in `GITHUB_TOKEN`\n-  (consistent with ADR 0005's no-API-key-by-default stance).\n+  (consistent with SADR-0005's no-API-key-by-default stance).\n - `.shiki/manifest.json` already encodes the install surface (`install.include`,\n   `install.create_directories`, `install.exclude_from_commit`), and it must not\n   be mutated as part of this change.\n@@ -34,7 +34,7 @@\n \n This is a hard-to-reverse platform decision (it sets the version source of\n truth, the release trigger, and the public install contract), so it is recorded\n-as an ADR.\n+as a SADR.\n \n ## Decision\n \n@@ -104,7 +104,7 @@\n 3. **Publishing to PyPI.** Rejected for now: Shiki is distributed as a\n    repository template + git-installable CLI, not a PyPI library; PyPI would add\n    credentials/secrets and a different versioning contract. Can be added later as\n-   its own ADR if a packaged distribution is needed.\n+   its own SADR if a packaged distribution is needed.\n 4. **Dynamic version (read `VERSION` via a build backend `dynamic` field).**\n    Rejected to keep `pyproject.toml` backend-agnostic and dependency-free; the\n    small duplication between `VERSION` and `pyproject.toml` is guarded by the\n@@ -123,4 +123,4 @@\n   releasing versions the whole platform but ships only `install.include` to\n   targets.\n - Future PyPI distribution or a console entry point are deferred and would each\n-  warrant their own ADR.\n+  warrant their own SADR.\ndiff --git a/docs/adr/0008-use-claude-code-as-default-implementer.md b/docs/adr/SADR-0008-use-claude-code-as-default-implementer.md\nindex dedf4510b7c7799521422f622029563d373db031..35c2625d908167fbc58855b5b63e283805f167e8\n--- a/docs/adr/0008-use-claude-code-as-default-implementer.md\n+++ b/docs/adr/SADR-0008-use-claude-code-as-default-implementer.md\n@@ -1,12 +1,12 @@\n-# ADR 0008: Use Claude Code As The Default Implementer Runtime\n+# SADR-0008: Use Claude Code As The Default Implementer Runtime\n \n ## Status\n \n-Proposed (supersedes ADR 0005 for the implementer default; keeps its auth model)\n+Proposed (supersedes SADR-0005 for the implementer default; keeps its auth model)\n \n ## Context\n \n-ADR 0005 made Codex Front the default implementation runtime, with Claude Code\n+SADR-0005 made Codex Front the default implementation runtime, with Claude Code\n restricted to planner and reviewer roles by the runtime registry, and the only\n autonomous implementation adapter was `shiki runner codex`.\n \n@@ -18,13 +18,13 @@\n - The platform's planning, review, and CCA layers already run on Claude Code,\n   and Claude Code's native capabilities (the built-in `/code-review` skill and\n   the Workflow multi-agent orchestration tool) have no Codex equivalent.\n-- The Goal lifecycle redesign (ADR 0009) requires an autonomous post-freeze\n+- The Goal lifecycle redesign (SADR-0009) requires an autonomous post-freeze\n   loop driven from the operator's Claude Code session, including bounded\n   repair dispatch, which must not depend on a runtime that cannot be invoked\n   unattended.\n \n The constitution claims Shiki is runtime-agnostic (\"not a Claude-only\n-workflow\", ADR 0002). Any change to the implementer default must preserve the\n+workflow\", SADR-0002). Any change to the implementer default must preserve the\n registry-based runtime model rather than hardcoding one vendor.\n \n ## Decision\n@@ -39,7 +39,7 @@\n   the task to review or repair-needed.\n - Keep Codex registered as an optional implementer runtime. `shiki runner\n   codex`, the Codex handoff templates, and the ChatGPT OAuth auth model from\n-  ADR 0005 remain supported for tasks explicitly assigned to `codex`.\n+  SADR-0005 remain supported for tasks explicitly assigned to `codex`.\n - Commit to mandating two Claude-native capabilities as loop contract in\n   follow-up Goals (status: the code-review gate was delivered by Goal G-B \u2014\n   skill registry, checklists item PR-12, default required_skills \u2014 while the\n@@ -60,7 +60,7 @@\n \n Out of scope: removing Codex support, changing the CCA runtime (Claude Code\n Action with `CLAUDE_CODE_OAUTH_TOKEN` stays), and API-key based automation\n-(still requires its own ADR per ADR 0005).\n+(still requires its own target ADR per SADR-0005).\n \n ## Consequences\n \n@@ -100,7 +100,7 @@\n   harness rejection of direct `codex exec` is outside Shiki's control, and the\n   plan's mandatory native capabilities cannot run on Codex at all.\n - **Remove Codex entirely**: rejected; it abandons the runtime-agnostic\n-  constitution (ADR 0002) for no operational gain and forecloses\n+  constitution (SADR-0002) for no operational gain and forecloses\n   cross-runtime adversarial review.\n - **GitHub Actions cloud implementation via claude-code-action**: rejected as\n   default; durable evidence is automatic but token cost, Node 24 workflow\ndiff --git a/docs/adr/0009-requirements-definition-and-spec-freeze.md b/docs/adr/SADR-0009-requirements-definition-and-spec-freeze.md\nindex 29b3f7871bef9203a8b4a831a65c39eb2f506e42..52e7dd69ad0aa4efe0b68c62678abbf7df02aaec\n--- a/docs/adr/0009-requirements-definition-and-spec-freeze.md\n+++ b/docs/adr/SADR-0009-requirements-definition-and-spec-freeze.md\n@@ -1,4 +1,4 @@\n-# ADR 0009: Merge Grill And Planning Into Requirements Definition With Spec Freeze\n+# SADR-0009: Merge Grill And Planning Into Requirements Definition With Spec Freeze\n \n ## Status\n \ndiff --git a/docs/adr/0010-external-ai-guardian-review.md b/docs/adr/SADR-0010-external-ai-guardian-review.md\nindex e6108bbe8bb4e73e0ff4684c8f78001a80438c4e..f3510b145664298adafb151b6f0db51b972f01ec\n--- a/docs/adr/0010-external-ai-guardian-review.md\n+++ b/docs/adr/SADR-0010-external-ai-guardian-review.md\n@@ -1,4 +1,4 @@\n-# ADR 0010: External AI Guardian Review As A First-Class Approval Authority\n+# SADR-0010: External AI Guardian Review As A First-Class Approval Authority\n \n ## Status\n \n@@ -8,7 +8,7 @@\n \n Shiki is intended to be an autonomous, recursive, self-improving agent\n framework: authority-in-the-loop, not human-in-the-loop. Until now the Guardian\n-approval required for high/critical-risk merges (guardian-policy.json, ADR\n+approval required for high/critical-risk merges (guardian-policy.json, SADR\n governance) recognized only human authority sources \u2014 a GitHub review approval,\n the `guardian:approved` label applied by a configured Guardian, or a comment\n carrying `Guardian approval granted` plus the current head SHA.\n@@ -89,7 +89,7 @@\n   autonomous design the operator intends and produced the impersonation\n   incident.\n - **Record AI approval as an operator comment**: rejected as forgery of human\n-  authority \u2014 the exact failure this ADR corrects.\n+  authority \u2014 the exact failure this SADR corrects.\n - **Deliver the artifact as a committed `.shiki/` file**: rejected; a committed\n   approval inside the PR being approved is forgeable by anyone who can push.\n - **A dedicated reviewer bot identity instead of a relayed comment**: deferred;\ndiff --git a/docs/adr/0011-autonomous-quality-gate-evidence.md b/docs/adr/SADR-0011-autonomous-quality-gate-evidence.md\nindex bca0246c5d46e44afbde15486a75eec425fc02f5..9c93eac16d8c3a6d4b11bd7224e3955102cd7db4\n--- a/docs/adr/0011-autonomous-quality-gate-evidence.md\n+++ b/docs/adr/SADR-0011-autonomous-quality-gate-evidence.md\n@@ -1,4 +1,4 @@\n-# ADR 0011: Autonomous Quality-Gate Evidence \u2014 Deterministic Facts And An Independent Verifier\n+# SADR-0011: Autonomous Quality-Gate Evidence \u2014 Deterministic Facts And An Independent Verifier\n \n ## Status\n \ndiff --git a/docs/adr/0012-autonomous-loop-closeout-to-main.md b/docs/adr/SADR-0012-autonomous-loop-closeout-to-main.md\nindex 637fad6dde4989c9c2bf0f81f676d8d29991123c..ec7d85f81030a676bcbba28b43eb365422a36eac\n--- a/docs/adr/0012-autonomous-loop-closeout-to-main.md\n+++ b/docs/adr/SADR-0012-autonomous-loop-closeout-to-main.md\n@@ -1,4 +1,4 @@\n-# 0012 \u2014 Autonomous loop pushes goal completion to main via a closeout PR\n+# SADR-0012 \u2014 Autonomous loop pushes goal completion to main via a closeout PR\n \n - Status: accepted\n - Date: 2026-06-18\ndiff --git a/docs/adr/0013-bootstrap-grants-actions-approve-pull-requests.md b/docs/adr/SADR-0013-bootstrap-grants-actions-approve-pull-requests.md\nindex b6c69d64f1c5ced346b88d6859586715b3b5bd0e..cf48ff4a6716267f539ca58edd1a852e7c7a2d64\n--- a/docs/adr/0013-bootstrap-grants-actions-approve-pull-requests.md\n+++ b/docs/adr/SADR-0013-bootstrap-grants-actions-approve-pull-requests.md\n@@ -1,4 +1,4 @@\n-# ADR 0013: Bootstrap Grants GitHub Actions Permission To Create And Approve Pull Requests\n+# SADR-0013: Bootstrap Grants GitHub Actions Permission To Create And Approve Pull Requests\n \n ## Status\n \n@@ -31,7 +31,7 @@\n security tradeoff: it lets repository automation satisfy a human-review gate. In\n a multi-maintainer repo that could let a compromised or buggy workflow\n self-approve changes. So whether bootstrap turns it on by default \u2014 and with\n-what default token scope \u2014 is an ADR-worthy decision, not an implementation\n+what default token scope \u2014 is a SADR-worthy decision, not an implementation\n detail.\n \n ## Decision\n@@ -48,7 +48,7 @@\n Specifically:\n \n - **`can_approve_pull_request_reviews` defaults to `true`.** Shiki's default\n-  posture is solo/AFK autonomous operation (ADR 0008/0009/0012), where the CCA\n+  posture is solo/AFK autonomous operation (SADR-0008/0009/0012), where the CCA\n   Review Bridge is the only path that can satisfy `required_review: true` after\n   the CCA and Guardian gates have already passed. The default makes a\n   freshly-bootstrapped repo able to run the autonomous loop end to end without a\ndiff --git a/docs/adr/0014-codex-app-external-ai-guardian-ui-adapter.md b/docs/adr/SADR-0014-codex-app-external-ai-guardian-ui-adapter.md\nindex 238db06279334caa341c117362df9271382a7fcd..65f12e9939321e1b3e389a34c822fc1549e5155d\n--- a/docs/adr/0014-codex-app-external-ai-guardian-ui-adapter.md\n+++ b/docs/adr/SADR-0014-codex-app-external-ai-guardian-ui-adapter.md\n@@ -1,4 +1,4 @@\n-# ADR 0014: Use Codex App As The External AI Guardian UI Adapter\n+# SADR-0014: Use Codex App As The External AI Guardian UI Adapter\n \n ## Status\n \ndiff --git a/docs/adr/0015-contract-approval-before-dispatch.md b/docs/adr/SADR-0015-contract-approval-before-dispatch.md\nindex fad6c656741a20a7684ba16080a7db159f63a52f..6061271a46d3f1d6ae7174477de3fb75673123a6\n--- a/docs/adr/0015-contract-approval-before-dispatch.md\n+++ b/docs/adr/SADR-0015-contract-approval-before-dispatch.md\n@@ -1,4 +1,4 @@\n-# ADR 0015: Approve The Task Contract Before Dispatch, Not The Diff After It\n+# SADR-0015: Approve The Task Contract Before Dispatch, Not The Diff After It\n \n ## Status\n \n@@ -9,7 +9,7 @@\n Guardian approval for high/critical risk is expressible only on a pull request.\n Every approval source in `guardian-policy.json` \u2014 GitHub review, the\n `guardian:approved` label, the `Guardian approval granted` comment, and the\n-`external_ai_guardian_review` artifact (ADR 0010/0014) \u2014 is bound to a PR, and\n+`external_ai_guardian_review` artifact (SADR-0010/0014) \u2014 is bound to a PR, and\n three of the four require the current head SHA. Approval therefore cannot exist\n before an implementation PR exists.\n \n@@ -81,7 +81,7 @@\n An envelope breach must surface only on the non-repairable policy gate, never on\n the repairable `MergeGate metadata check`. Otherwise the Goal Loop routes it to\n `dispatch_repair` \u2014 instructing a runner to make the breach go away \u2014 which is\n-exactly what this ADR forbids.\n+exactly what this SADR forbids.\n \n **Contract immutability.** After registration, an implementation PR must not\n change the governance fields of its own task contract (`scope`, `non_goals`,\n@@ -116,7 +116,7 @@\n fails any verdict that is not `complete`, and the signal the CCA prompt reads is\n computed from `evaluate_guardian_approval` alone. Without the OR-merge at the\n signal, a contract-approved PR still returns `needs_guardian`, the CCA job fails,\n-and the MergeGate policy check never runs \u2014 the gate this ADR changes would never\n+and the MergeGate policy check never runs \u2014 the gate this SADR changes would never\n be reached. The CCA *prompt* needs no change: a `contract_approval` entry in the\n signal's existing `sources` array satisfies its current wording.\n \n@@ -130,7 +130,7 @@\n written, and an approved contract lets its implementation PRs merge through the\n same automatic path as low/medium risk. The wasted implement-then-block cycle\n disappears. Guardian review shifts from reading diffs to reading contracts, which\n-is both earlier and smaller; the external AI Guardian path (ADR 0010/0014) works\n+is both earlier and smaller; the external AI Guardian path (SADR-0010/0014) works\n on contracts with no change.\n \n We accept that the Guardian authorizes a class of change without seeing its\n@@ -146,7 +146,7 @@\n adversarial tests for the new approval path equivalent in rigor to\n `test_shiki_governance_evidence.sh`.\n \n-Bootstrap costs, which apply only while this ADR is being implemented. The tasks\n+Bootstrap costs, which apply only while this SADR is being implemented. The tasks\n that build Contract Approval merge under the existing rules, so the ones\n dispatched after the pre-dispatch gate lands but before the approval path lands\n need a Guardian-approved Contract PR *and* a live Guardian approval on their\n@@ -210,7 +210,7 @@\n risk; they do not bound authorized work.\n \n **Auto-escalate risk on envelope breach instead of stopping.** Returns the\n-workflow to a diff-time Guardian decision \u2014 the exact behavior this ADR removes.\n+workflow to a diff-time Guardian decision \u2014 the exact behavior this SADR removes.\n \n **Resolve governance fields from the base snapshot only.** Rejected against the\n registration flow: implementation PRs add their own contract, so the base has no\ndiff --git a/docs/adr/0016-locks-bound-product-paths.md b/docs/adr/SADR-0016-locks-bound-product-paths.md\nindex b5e850f3f1f7f4ef6e7f9d1763a29a51bd300c45..46b40a9b99a7aae644c02bb458f5473ff0a90ce2\n--- a/docs/adr/0016-locks-bound-product-paths.md\n+++ b/docs/adr/SADR-0016-locks-bound-product-paths.md\n@@ -1,4 +1,4 @@\n-# ADR 0016: Locks bound product paths; the Shiki mirror is bound by record identity\n+# SADR-0016: Locks bound product paths; the Shiki mirror is bound by record identity\n \n ## Status\n \n@@ -6,8 +6,8 @@\n \n - Date: 2026-07-28\n - Deciders: operator (mizutani-140), Claude (planner)\n-- Related: ADR 0004 (GitHub-first with a `.shiki` mirror), ADR 0009 (Spec Freeze),\n-  ADR 0012 (autonomous loop closeout to main), ADR 0015 (Contract Approval before\n+- Related: SADR-0004 (GitHub-first with a `.shiki` mirror), SADR-0009 (Spec Freeze),\n+  SADR-0012 (autonomous loop closeout to main), SADR-0015 (Contract Approval before\n   dispatch), remediation goal task R-02, the live PR #179 deadlock.\n \n ## Context\n@@ -90,11 +90,11 @@\n files, documentation files and named source modules are nameable; runtime-\n allocated ledger, runner and report ids are not \u2014 and those live in `.shiki`,\n which locks no longer govern. Existing frozen contracts are brought into line by\n-a one-time Spec Amendment per plan, which must land before ADR 0015's contract\n+a one-time Spec Amendment per plan, which must land before SADR-0015's contract\n immutability makes `locks` unwritable from the implementation PR.\n \n **Lock state is resolved from the durable side.** MergeGate resolves the lock\n-registry, and ADR 0015's governance fields, from the base `.shiki` snapshot when\n+registry, and SADR-0015's governance fields, from the base `.shiki` snapshot when\n present, falling back to head only when absent. The `MergeGate metadata check`\n job is given `--base-shiki`, which it does not receive today. Locks are not\n derived from the live open-pull-request set: a pull request body is untrusted\n@@ -108,7 +108,7 @@\n Explicitly out of scope: moving MergeGate's contract resolution off the PR\n checkout wholesale; making the mirror a projection of GitHub state; introducing\n new lock kinds; moving ledger evidence out of the repository; and unwinding\n-ADR 0012's closeout pull request. Registration of multi-task goals is ADR 0015's\n+SADR-0012's closeout pull request. Registration of multi-task goals is SADR-0015's\n Contract PR and is not re-decided here.\n \n ## Consequences\n@@ -127,7 +127,7 @@\n \n Risks accepted. First, goal-scoped identity is only as strong as `goal_id`,\n which is resolved from the PR body and the head task file; the mitigation is to\n-add `goal_id` to ADR 0015's frozen governance field set, and until that lands a\n+add `goal_id` to SADR-0015's frozen governance field set, and until that lands a\n task file can name a foreign goal. Second, base-when-present resolution is\n unavailable on fork pull requests, where the head is contributor-controlled;\n fork pull requests already cannot complete a Shiki merge, and this decision does\n@@ -139,7 +139,7 @@\n path, not by implementation pull requests.\n \n New obligations: one Spec Amendment per frozen plan to rewrite `locks`, ordered\n-before ADR 0015 T1; extension of the adversarial suites in\n+before SADR-0015 T1; extension of the adversarial suites in\n `scripts/test_shiki_adversarial_state.sh` and\n `scripts/test_shiki_governance_evidence.sh` to cover the eleven newly-scoped\n directories; and updating `scripts/test_shiki_loop_lock_guard.sh`,\n@@ -151,32 +151,32 @@\n whether the loop's transitive sync of ledger `evidence` references can be\n deleted once the Contract PR carries its own source plan.\n \n-## Relationship to ADR 0004\n+## Relationship to SADR-0004\n \n-This ADR **clarifies something ADR 0004 left unspecified**. ADR 0004 decides that\n+This SADR **clarifies something SADR-0004 left unspecified**. SADR-0004 decides that\n GitHub is the operational source of truth and that each target repository keeps a\n `.shiki/` mirror. It says nothing about pull requests writing the mirror, does\n not name MergeGate, and does not make the mirror an input to a gate. Nothing in\n-ADR 0004's Decision or Consequences is amended, superseded or weakened: the\n+SADR-0004's Decision or Consequences is amended, superseded or weakened: the\n mirror still exists, still records the same artifacts, and pull request\n-boundaries remain the coordination surface. What this ADR settles is the question\n+boundaries remain the coordination surface. What this SADR settles is the question\n 0004 never reached \u2014 by what authority a pull request may write a mirror record \u2014\n and it answers it the way `.shiki/manifest.json` already declares\n (`pr_mutation: current-task-or-current-goal-only`).\n \n-It **amends ADR 0012**, whose Decision item 5 states that \"Loop-executed tasks\n+It **amends SADR-0012**, whose Decision item 5 states that \"Loop-executed tasks\n declare the `path:.shiki/**` lock, which covers every `.shiki` file the closeout\n stages ... so `files_outside_locks` is satisfied without per-file lock\n bookkeeping.\" That clause is replaced: the closeout's mirror writes are\n-authorized by record identity, not by a declared lock. ADR 0012's substantive\n+authorized by record identity, not by a declared lock. SADR-0012's substantive\n decision \u2014 that completion reaches `main` through a closeout pull request rather\n than local attestation \u2014 is preserved unchanged.\n \n-It is **complementary to ADR 0015**, not a substitute. ADR 0015 owns contract\n+It is **complementary to SADR-0015**, not a substitute. SADR-0015 owns contract\n immutability, the pre-dispatch gate, the Contract PR and the Scope Envelope. This\n-ADR owns what a lock means and who may write the mirror. The one ordering\n+SADR owns what a lock means and who may write the mirror. The one ordering\n constraint between them is that the lock-narrowing Spec Amendment must precede\n-ADR 0015 T1's freezing of the `locks` field.\n+SADR-0015 T1's freezing of the `locks` field.\n \n ## Alternatives Considered\n \n@@ -186,7 +186,7 @@\n the only writer that can register them may run. Migrating the Guardian signal to\n that store makes every registration PR fail closed to `needs_guardian` at every\n risk level, because the task file is by definition absent from the store, which\n-removes the low/medium autonomous path ADR 0008/0009 exists to provide. And it\n+removes the low/medium autonomous path SADR-0008/0009 exists to provide. And it\n leaves schemas, manifest, config, guardian policy, templates, migrations, plans,\n worktrees, runner records, runs and handoffs with no writer at all in a\n repository whose product is `.shiki`. Measured benefit on the frozen backlog:\n@@ -199,14 +199,14 @@\n namespace is not in fact ownership-partitioned \u2014 56 ledger entries carry no\n `task_id`, and one goal DAG is writable by 35 registered tasks. The correct\n expression of its insight is that locks do not govern the mirror at all, which is\n-what this ADR decides.\n+what this SADR decides.\n \n **Make the mirror a projection written only by a registration writer and a\n post-merge projection job.** Rejected. It requires a bypass actor with write\n access to the protected default branch; it makes its own master invariant\n readable from a manifest that pull requests may edit; it deletes both reconcile\n modes and leaves a corrupted mirror with no in-band repair path, contradicting\n-ADR 0004's consequence that mirror conflicts are repaired against GitHub state;\n+SADR-0004's consequence that mirror conflicts are repaired against GitHub state;\n and its content-derived ids are rejected by `validate_shiki`'s currently-green id\n pattern.\n \ndiff --git a/docs/adr/0017-bookkeeping-closeouts-do-not-inherit-risk.md b/docs/adr/SADR-0017-bookkeeping-closeouts-do-not-inherit-risk.md\nindex 554e3a0d526a72ca067108c5464cc68c3edcf174..b9676857a4713850e063af7c39c8e0b01ecb4c66\n--- a/docs/adr/0017-bookkeeping-closeouts-do-not-inherit-risk.md\n+++ b/docs/adr/SADR-0017-bookkeeping-closeouts-do-not-inherit-risk.md\n@@ -1,4 +1,4 @@\n-# ADR 0017: A Bookkeeping Closeout Does Not Inherit Its Task's Risk\n+# SADR-0017: A Bookkeeping Closeout Does Not Inherit Its Task's Risk\n \n ## Status\n \n@@ -6,7 +6,7 @@\n \n ## Context\n \n-ADR 0012 made the goal loop push a task's completion to the default branch through a\n+SADR-0012 made the goal loop push a task's completion to the default branch through a\n closeout pull request: after the implementation PR merges, the loop opens a second PR\n carrying `task.status = done`, `lock.state = released`, and \u2014 when the task completes\n its goal \u2014 `goal.status = complete` plus one scorecard report. Nothing else. The\n@@ -49,14 +49,14 @@\n 4. The task file's governance fields (`scope`, `non_goals`, `required_skills`,\n    `risk_level`, `locks`, `acceptance_checks`, `test_command`) are byte-identical to\n    the base snapshot. Only `status`, `expected_pr`, `closeout_pr`, `ledger_evidence`\n-   and `expected_branch` may differ \u2014 the ADR 0015 contract-immutability rule,\n+   and `expected_branch` may differ \u2014 the SADR-0015 contract-immutability rule,\n    unchanged.\n \n    `expected_branch` is in that set because a closeout is *by construction* on a\n    different branch from the implementation it closes, and the loop rewrites the\n    field accordingly. It is safe to let it differ because MergeGate independently\n    requires `expected_branch` to equal the PR's actual head ref, so a PR cannot set\n-   it to anything other than the branch it is really on. The first draft of this ADR\n+   it to anything other than the branch it is really on. The first draft of this SADR\n    omitted it, which made the exemption unreachable: every real closeout failed\n    condition 4 on a field the loop always changes. See PR #209.\n 5. The state transitions are exactly the terminal set: task `review` \u2192 `done`, lock\n@@ -113,12 +113,12 @@\n classification must be derived, not asserted.\n \n **Fold the closeout into the implementation PR** so no second PR exists. Rejected under\n-ADR 0012, which recorded why: a task file that is already `done` on the implementation\n+SADR-0012, which recorded why: a task file that is already `done` on the implementation\n branch short-circuits the loop's review \u2192 CCA \u2192 merge \u2192 done ordering into a local\n goal-completion before any merge happens.\n \n **Have the loop auto-merge closeouts with elevated credentials.** Rejected outright: that\n-is a runtime authoring its own approval, which ADR 0010 and the constitution's\n+is a runtime authoring its own approval, which SADR-0010 and the constitution's\n approval-record prohibition forbid. It would also be indistinguishable, in the evidence,\n from the forgery those rules exist to prevent.\n \ndiff --git a/docs/adr/0018-carried-guardian-approval-across-a-proven-base-sync.md b/docs/adr/SADR-0018-carried-guardian-approval-across-a-proven-base-sync.md\nindex 22358562da390ed8b45cd600da08a43d378b4680..e6e2907cefa776460af73ddf7d39db605f3c39b0\n--- a/docs/adr/0018-carried-guardian-approval-across-a-proven-base-sync.md\n+++ b/docs/adr/SADR-0018-carried-guardian-approval-across-a-proven-base-sync.md\n@@ -1,4 +1,4 @@\n-# ADR 0018: Carried Guardian Approval Across A Proven Base Sync\n+# SADR-0018: Carried Guardian Approval Across A Proven Base Sync\n \n ## Status\n \n@@ -34,7 +34,7 @@\n Guardian evaluator that consumes it (`shiki_guardian.evaluate_guardian_approval`,\n the `guardian_comment_carried` source) were built and tested in isolation. Neither\n does anything until a caller passes the flags that turn the carry on, so the\n-feature was invisible in CI. This ADR records the decision the two of them\n+feature was invisible in CI. This SADR records the decision the two of them\n implement, and this task wires it into the four CI invocations that judge Guardian\n approval.\n \ndiff --git a/docs/adr/README.md b/docs/adr/README.md\nindex c70e057a4520c36e042ab68c53cd4a21ef709b71..5da43856836e2ce6a150f8127766e2f88355ab4b\n--- a/docs/adr/README.md\n+++ b/docs/adr/README.md\n@@ -1,40 +1,41 @@\n-# Architecture Decision Records\n+# Decision Record Namespaces\n \n-This directory holds Shiki's Architecture Decision Records (ADRs): durable\n-records of hard-to-reverse platform decisions. ADRs are part of the Shiki source\n-of truth alongside GitHub state, the `.shiki/` mirror, and `CONTEXT.md`.\n+This directory holds two independent decision namespaces:\n \n-## How ADRs Work\n+- Shiki Architecture Decision Records (SADRs), named\n+  `SADR-NNNN-<short-slug>.md`, record hard-to-reverse Shiki platform decisions.\n+- Target Architecture Decision Records (ADRs), named\n+  `NNNN-<short-slug>.md`, belong to the target product.\n \n-- New ADRs are added as `docs/adr/NNNN-<short-slug>.md`, where `NNNN` is the next\n-  zero-padded sequence number.\n-- The **canonical list of ADRs is the set of files matching `docs/adr/*.md`** in\n-  this directory. Browse `docs/adr/*.md` for the authoritative, current set.\n-- Use [`template.md`](template.md) as the starting point for a new ADR.\n-- Record an ADR when a decision is hard to reverse, surprising without context,\n-  and a real tradeoff. Do not write an ADR for routine or easily reversible\n-  choices.\n+The same four-digit number may appear once in each namespace. A platform\n+`SADR-NNNN` citation resolves only to `SADR-NNNN-*.md`; a target `ADR NNNN`\n+citation resolves only to `NNNN-*.md`.\n \n-Using the `docs/adr/*.md` glob as the source of truth means other PRs can add an\n-ADR without editing this index file, avoiding merge contention on a\n-hand-maintained enumeration.\n+## How Decision Records Work\n \n-## Current ADRs (snapshot)\n-\n-The list below is a convenience snapshot and may lag behind the directory. When\n-in doubt, list `docs/adr/*.md`.\n+- New Shiki platform decisions use the next zero-padded SADR number.\n+- New target product decisions use the target's own ADR sequence.\n+- The canonical list is the files in `docs/adr/`; no hand-maintained index is\n+  authoritative.\n+- Use [`template.md`](template.md) as the starting point and choose the filename\n+  namespace that owns the decision.\n+- Record a decision when it is hard to reverse, surprising without context, and\n+  a real tradeoff. Do not write a decision record for routine or easily\n+  reversible choices.\n \n-- [0001 \u2014 Keep Shiki As The Platform Name](0001-keep-shiki-as-platform-name.md)\n-- [0002 \u2014 Use AGENTS As The Shared Agent Constitution](0002-use-agents-as-shared-constitution.md)\n-- [0003 \u2014 Require Skill Gate For MergeGate Readiness](0003-require-skill-gate-for-mergegate-readiness.md)\n-- [0004 \u2014 Use GitHub First With A Repository-Local Shiki Mirror](0004-use-github-first-with-shiki-mirror.md)\n-- [0005 \u2014 Use Codex As Front And Claude Code Action As The GitHub Runtime](0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md)\n-- [0006 \u2014 Separate Codex Implementation, CCA Completion, And MergeGate Authorization](0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md)\n+Using the directory as the source of truth lets independent changes add records\n+without contending on an enumerated index.\n \n-## Adding A New ADR\n+## Adding A Shiki SADR\n \n-1. Copy `template.md` to `docs/adr/NNNN-<short-slug>.md`.\n+1. Copy `template.md` to `docs/adr/SADR-NNNN-<short-slug>.md`.\n 2. Fill in the title, status, context, decision, and consequences.\n-3. Set the status to `Proposed`, then `Accepted` once the decision is approved.\n-4. Open a PR following `CONTRIBUTING.md`. You do not need to edit this index for\n-   the ADR to be discoverable via `docs/adr/*.md`.\n+3. Set the status to `Proposed`, then `Accepted` once the proper authority has\n+   approved the decision.\n+4. Open a PR following `CONTRIBUTING.md`.\n+\n+## Adding A Target ADR\n+\n+1. Copy `template.md` to `docs/adr/NNNN-<short-slug>.md` using the target's own\n+   sequence.\n+2. Cite it as `ADR NNNN`; do not prefix its filename with `SADR-`.\ndiff --git a/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md b/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\nnew file mode 100644\nindex 0000000000000000000000000000000000000000..7455ce1f3bed02ddddb851ac21a1d1a648c9aeba\n--- /dev/null\n+++ b/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\n@@ -0,0 +1,55 @@\n+# SADR-0019: Separate Shiki Platform SADRs From Target ADRs\n+\n+## Status\n+\n+Accepted\n+\n+## Context\n+\n+Shiki installs its platform decision records into `docs/adr/` in every target\n+repository. Shiki and its targets previously used the same numeric\n+`NNNN-<slug>.md` namespace. A target that already owned ADRs 0013 through 0018\n+therefore acquired two records for each number during installation, making an\n+`ADR NNNN` citation ambiguous. Renumbering target decisions would rewrite target\n+history, while renumbering Shiki decisions per target would destroy their stable\n+platform identity.\n+\n+Upgrades also need to remove the former Shiki-owned numeric paths without ever\n+mistaking a target-owned ADR for platform content. The install stamp already\n+records exact shipped paths and content digests, so it can prove ownership only\n+when both still match.\n+\n+## Decision\n+\n+Shiki platform decisions use the `SADR` namespace:\n+\n+- records are named `docs/adr/SADR-NNNN-<slug>.md`;\n+- live platform citations use `SADR-NNNN`;\n+- target product decisions retain `docs/adr/NNNN-<slug>.md` and `ADR NNNN`;\n+- the reference validator resolves the two namespaces independently, so the same\n+  four-digit number may exist once in each namespace.\n+\n+Before a forced upgrade writes anything, the installer inventories the exact\n+former numeric Shiki ADR paths. It may delete a legacy path only when the\n+existing install stamp contains that exact path and its recorded SHA-256 digest\n+matches the file currently on disk. All ownership blockers are reported\n+together and abort the upgrade before its first write. A non-forced install\n+never performs this cleanup.\n+\n+Historical `.shiki/` evidence remains unchanged because it records the namespace\n+that existed when that evidence was produced.\n+\n+## Consequences\n+\n+- Targets may use any ADR number without colliding with Shiki's platform SADRs.\n+- Fresh installs preserve target ADR bytes and install a stable SADR set.\n+- Forced upgrades fail closed when legacy ownership cannot be proven, and a\n+  successful upgrade refreshes the stamp without removed legacy paths.\n+- Current governance, packaging, workflow, prompt, script, and skill references\n+  must move with the platform records.\n+\n+## Alternatives Considered\n+\n+- Collision-only refusal leaves affected targets permanently unupgradable.\n+- Renumbering target ADRs mutates target-owned history.\n+- Per-target Shiki renumbering makes platform citations non-portable.\ndiff --git a/docs/adr/template.md b/docs/adr/template.md\nindex a92a97daddd0b00ab9dd626cc8d86942da2eafed..17dd79175b5fee2105935557f691b40cf9492942\n--- a/docs/adr/template.md\n+++ b/docs/adr/template.md\n@@ -1,14 +1,14 @@\n-# NNNN: <Short Decision Title>\n+# <SADR-NNNN or NNNN>: <Short Decision Title>\n \n ## Status\n \n-Proposed | Accepted | Superseded by ADR-NNNN\n+Proposed | Accepted | Superseded by SADR-NNNN or target ADR NNNN\n \n ## Context\n \n What is the situation, constraint, or force that requires a decision? Reference\n-relevant domain terms (`CONTEXT.md`), prior ADRs, and code reality. State the\n-problem, not the solution.\n+relevant domain terms (`CONTEXT.md`), prior Shiki SADRs or target ADRs, and code\n+reality. State the problem, not the solution.\n \n ## Decision\n \ndiff --git a/docs/agents/autonomous-loop-e2e-acceptance.md b/docs/agents/autonomous-loop-e2e-acceptance.md\nindex bbf84c6081987f159ac6ad9ecf3c82b7aa72e7e8..5c13c1d02a05a90b0dc46cbef7c95a9846d0c237\n--- a/docs/agents/autonomous-loop-e2e-acceptance.md\n+++ b/docs/agents/autonomous-loop-e2e-acceptance.md\n@@ -40,8 +40,8 @@\n 3. `gh auth status` is logged in with merge permission on the repo.\n 4. Branch protection + required checks are configured exactly as the goal-loop\n    required-check set expects (see `docs/agents/branch-protection-smoke.md`).\n-5. The repo `.shiki/policy` permits autonomous low/medium-risk merges (ADR\n-   0008/0009); the MergeGate policy check is the recorded-authority gate.\n+5. The repo `.shiki/policy` permits autonomous low/medium-risk merges\n+   (SADR-0008/0009); the MergeGate policy check is the recorded-authority gate.\n \n The operator's only actions are: register the task and start the loop. **No\n manual pre-clearing** means after `shiki loop run` starts, the operator does not\ndiff --git a/docs/agents/bootstrap-command.md b/docs/agents/bootstrap-command.md\nindex 05f7f203a52cf412b9f97084a6e131ed0f785afd..f7aa0b734dfebee4a3b139335b66ee3415f48713\n--- a/docs/agents/bootstrap-command.md\n+++ b/docs/agents/bootstrap-command.md\n@@ -189,7 +189,9 @@\n ```\n \n Use this only for tests, fixtures, or explicit local-only template inspection.\n-Use `--force` only when you intentionally want to overwrite existing target files.\n+Use `--force` only when you intentionally want to refresh the target template.\n+Legacy numeric Shiki decision cleanup is stamp-and-digest authorized before any\n+write; target ADR paths are not cleanup candidates.\n \n ## Slash Command\n \ndiff --git a/docs/agents/checklists.md b/docs/agents/checklists.md\nindex 4a7f03ade346f53a1c1d82981f3fd744f9ab4547..0c5ec6c53dc7197b7b7a7d18e667b325babd6c45\n--- a/docs/agents/checklists.md\n+++ b/docs/agents/checklists.md\n@@ -32,18 +32,18 @@\n | ID | Item | Blocking |\n | --- | --- | --- |\n | GD-01 | Plan was challenged against `CONTEXT.md`. | blocking for non-trivial Goals |\n-| GD-02 | Relevant ADRs were read or absence was recorded. | blocking for non-trivial Goals |\n+| GD-02 | Relevant SADRs and target ADRs were read or absence was recorded. | blocking for non-trivial Goals |\n | GD-03 | Ambiguous terms were resolved or converted to HITL blockers. | blocking |\n | GD-04 | Edge scenarios were considered. | advisory |\n | GD-05 | Code reality was checked when code could answer. | blocking when applicable |\n | GD-06 | New domain terms were added to `CONTEXT.md` when needed. | blocking when applicable |\n-| GD-07 | ADR was created or explicitly skipped using the ADR criteria. | blocking when applicable |\n+| GD-07 | A SADR or target ADR was created or explicitly skipped using the decision-record criteria. | blocking when applicable |\n \n ## CI \u2014 Context & Impact Checklist\n \n | ID | Item | Blocking |\n | --- | --- | --- |\n-| CI-01 | Relevant docs and ADRs identified. | blocking |\n+| CI-01 | Relevant docs, SADRs, and target ADRs identified. | blocking |\n | CI-02 | Relevant modules, interfaces, seams, callers, and tests identified. | blocking |\n | CI-03 | Dependencies and blocked-by relationships identified. | blocking |\n | CI-04 | Candidate locks identified. | blocking |\n@@ -62,7 +62,7 @@\n | PRD-04 | Implementation decisions are recorded without volatile code detail. | blocking |\n | PRD-05 | Testing decisions are recorded. | blocking |\n | PRD-06 | Out-of-scope items are explicit. | blocking |\n-| PRD-07 | Relevant glossary/ADR links are present when applicable. | blocking |\n+| PRD-07 | Relevant glossary/SADR/target-ADR links are present when applicable. | blocking |\n \n ## SF \u2014 Spec Freeze Checklist\n \n@@ -98,7 +98,7 @@\n | H-02 | Branch/worktree target is present. | blocking |\n | H-03 | Scope and non-goals are present. | blocking |\n | H-04 | Dependencies and locks are present. | blocking |\n-| H-05 | Relevant docs, ADRs, modules, and tests are referenced. | blocking |\n+| H-05 | Relevant docs, SADRs, target ADRs, modules, and tests are referenced. | blocking |\n | H-06 | Required skills are stated. | blocking |\n | H-07 | TDD expectations are stated. | blocking when behavior work |\n | H-08 | Acceptance criteria are included. | blocking |\ndiff --git a/docs/agents/codex-handoff.md b/docs/agents/codex-handoff.md\nindex ede468f47cb277f69a857e8c45df703e4ed0151d..6f97bdffb67e44d7edea2c71e01ea28933c4eb6c\n--- a/docs/agents/codex-handoff.md\n+++ b/docs/agents/codex-handoff.md\n@@ -46,7 +46,7 @@\n \n ## Context\n - Domain terms:\n-- Relevant ADRs:\n+- Relevant SADRs and target ADRs:\n - Relevant docs:\n - Relevant modules/interfaces/seams:\n - Similar tests/prior art:\ndiff --git a/docs/agents/completion-check-agent.md b/docs/agents/completion-check-agent.md\nindex 13e4c035db35025f4e3cdcc5adfb24314ac4d521..61bfa1218cf36ca605250c91919e620f33238aa0\n--- a/docs/agents/completion-check-agent.md\n+++ b/docs/agents/completion-check-agent.md\n@@ -32,7 +32,7 @@\n - `AGENTS.md`\n - `CLAUDE.md`\n - `CONTEXT.md`\n-- relevant ADRs\n+- relevant SADRs and target ADRs\n - parent Goal issue\n - PRD issue or document\n - task issue\ndiff --git a/docs/agents/decision-control.md b/docs/agents/decision-control.md\nindex 7d8ee5ab03a5ccbcbd04056ae61de03785a2a9c9..8c94ecda00fffaa0e92c325ab83aa9776bd1dac1\n--- a/docs/agents/decision-control.md\n+++ b/docs/agents/decision-control.md\n@@ -11,7 +11,7 @@\n | Decision | Owner | When | Required inputs | Output | Enforcement surface |\n | --- | --- | --- | --- | --- | --- |\n | Is the Goal clear enough to plan? | Planner + Guardian when needed | Goal intake | User request, existing docs, risk signals | Goal readiness verdict | Goal checklist, issue template, ledger |\n-| Must `grill-with-docs` run? | Planner / Skill Gate | Before PRD or issue split | Ambiguity, domain terms, ADR conflicts, unclear boundaries | Skill invocation or explicit skip rationale | Skill Gate checklist |\n+| Must `grill-with-docs` run? | Planner / Skill Gate | Before PRD or issue split | Ambiguity, domain terms, SADR/target-ADR conflicts, unclear boundaries | Skill invocation or explicit skip rationale | Skill Gate checklist |\n | Is a PRD required? | Planner | After Goal context stabilizes | Goal, `grill-with-docs` output, Context & Impact output | PRD required / not required | PRD checklist, CCA evidence check |\n | Can the spec be frozen? | Operator only | End of Requirements Definition, before issue split | PRD/requirements summary, scope inventory (SF-02), open-question list | `spec_freeze` block in the plan + ledger evidence | `require_grilled_plan` (plan ingest/run), SF checklist |\n | Can a frozen spec change? | Operator only (Spec Amendment) | When implementation contradicts the frozen spec | Contested decisions, scoped re-grill result | recorded amendment + re-stamped freeze | SF checklist, ledger evidence |\ndiff --git a/docs/agents/domain.md b/docs/agents/domain.md\nindex 258da8ea81e87ac0a6b0b5b5e5c2fde980eaabb7..d57b0cf17ff3e1fa9e6e5885372bf083d8295421\n--- a/docs/agents/domain.md\n+++ b/docs/agents/domain.md\n@@ -5,7 +5,9 @@\n ## Layout\n \n - `CONTEXT.md` defines Shiki language. Keep it implementation-free.\n-- `docs/adr/` records hard-to-reverse decisions.\n+- `docs/adr/SADR-NNNN-*.md` records Shiki platform decisions.\n+- `docs/adr/NNNN-*.md` records target product decisions without sharing the\n+  SADR namespace.\n - `AGENTS.md` is the runtime-neutral agent constitution.\n - `CLAUDE.md` is the Claude Code wrapper around `AGENTS.md`.\n - `.shiki/` is the Target Repository mirror for Goals, PRDs, plans, Task DAGs, contracts, locks, worktree records, ledger entries, CCA verdicts, repair packets, reports, and handoffs.\n@@ -15,6 +17,6 @@\n \n - Use Shiki terms exactly when they exist.\n - Add glossary terms to `CONTEXT.md` only when the term is part of the domain, not a generic programming concept.\n-- Create ADRs only for hard-to-reverse, surprising, tradeoff-based decisions.\n+- Create SADRs or target ADRs only for hard-to-reverse, surprising, tradeoff-based decisions.\n - Keep product-specific language in the Target Repository, not in Shiki Template docs.\n-- If an ADR conflicts with a proposed implementation, surface the conflict before editing.\n+- If a SADR or target ADR conflicts with a proposed implementation, surface the conflict before editing.\ndiff --git a/docs/agents/execution-control.md b/docs/agents/execution-control.md\nindex ad8ee3cdb36be56fedc1f46408ca92b387d74047..bc63c3a73661d12b567016ba299f1a6b66bf9088\n--- a/docs/agents/execution-control.md\n+++ b/docs/agents/execution-control.md\n@@ -63,7 +63,7 @@\n | Branch name | Orchestrator | Orchestrator | Before implementer dispatch | branch policy | branch ref in task/ledger |\n | Lock reservation | Context & Impact | MergeGate | Before dispatch and before merge | `.shiki/locks/`, labels, PR metadata | lock id, owner, scope |\n | CI profile | Planner | MergeGate | During issue triage, rechecked at PR | labels, `.shiki/policy.yaml`, workflow matrix | required check list |\n-| Required status checks | Repo maintainer / Guardian | GitHub branch protection / rulesets | Repository setup, updated via ADR | GitHub settings | branch/ruleset config, ADR |\n+| Required status checks | Repo maintainer / Guardian | GitHub branch protection / rulesets | Repository setup, updated via target ADR | GitHub settings | branch/ruleset config, target ADR |\n | CCA judgment | CCA | GitHub required check / MergeGate | Every non-draft PR update | GitHub Action check | structured CCA verdict |\n | Repair assignment | CCA | MergeGate | After failed CCA/CI/review | repair packet, issue/PR comment | repair id and attempt count |\n | Merge readiness | MergeGate | GitHub branch protection / Guardian if needed | After CCA complete | required checks, review rules, rulesets | mergegate check output |\ndiff --git a/docs/agents/guardian-policy.md b/docs/agents/guardian-policy.md\nindex 4147c7adf05a47adb4fd24ada1a1c2e1f2ba6d2d..a20d9cc6c00a569bb057b9ca8d6b51c7fc4a9847\n--- a/docs/agents/guardian-policy.md\n+++ b/docs/agents/guardian-policy.md\n@@ -98,8 +98,8 @@\n `origin/<default_branch>` and reuses each job's existing base fetch; a shallow\n (`--depth`) or second base fetch would make the proof unresolvable and silently\n disable the carry, so neither is permitted. CCA reads `guardian_comment_carried` as\n-a recorded authority in the deterministic signal and does not re-derive it. See ADR\n-0018 for the full decision, the rejected diff/patch-equality and path-disjointness\n+a recorded authority in the deterministic signal and does not re-derive it. See\n+SADR-0018 for the full decision, the rejected diff/patch-equality and path-disjointness\n alternatives, and the accepted residual.\n \n ## External AI Guardian Review (SADR-0010)\ndiff --git a/docs/agents/implementation-policy.md b/docs/agents/implementation-policy.md\nindex c754144a9fb5bf4a38aa82a60e1670c59e626b44..85c37281bcad196c6b7b02343faf00b03cd9c57a\n--- a/docs/agents/implementation-policy.md\n+++ b/docs/agents/implementation-policy.md\n@@ -31,10 +31,13 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n ## Non-Negotiables\n \n 1. **Planning precedes implementation.** Non-trivial Goals start with `grill-with-docs` and Context & Impact.\n-2. **Domain language is controlled.** Terms must be checked against `CONTEXT.md`; hard-to-reverse tradeoffs must be checked against ADRs.\n+2. **Domain language is controlled.** Terms must be checked against `CONTEXT.md`; hard-to-reverse tradeoffs must be checked against Shiki SADRs and target ADRs.\n 3. **PRD precedes issue decomposition.** `to-prd` is used once the context is settled enough to become durable intent.\n 4. **Issues are vertical slices.** `to-issues` must produce independently grabbable tracer-bullet tasks, not horizontal layer tickets.\n 5. **Triage controls dispatch.** Only `ready-for-agent` / AFK tasks may be assigned to Codex without human input.\n@@ -95,19 +98,19 @@\n Process:\n \n - Challenge terms against `CONTEXT.md`.\n-- Check ADRs before reopening decisions.\n+- Check SADRs and target ADRs before reopening decisions.\n - Inspect code when code can answer a question.\n - Ask one question at a time when the operator is available.\n - Provide a recommended answer with each question.\n - Record resolved terms in `CONTEXT.md`.\n-- Create ADRs only for hard-to-reverse, surprising, tradeoff-based decisions.\n+- Create SADRs or target ADRs only for hard-to-reverse, surprising, tradeoff-based decisions.\n - Convert unresolved load-bearing questions into HITL blockers.\n \n Outputs:\n \n - Resolved design-tree decisions.\n - Updated glossary terms when needed.\n-- ADRs when justified.\n+- SADRs or target ADRs when justified.\n - HITL blockers when decisions remain unresolved.\n \n ## Phase 3 \u2014 Context & Impact\n@@ -117,7 +120,7 @@\n \n Outputs must include:\n \n-- Relevant documents and ADRs.\n+- Relevant documents, SADRs, and target ADRs.\n - Relevant modules, interfaces, seams, callers, and tests.\n - Candidate locks.\n - Dependencies.\n@@ -140,7 +143,7 @@\n - Testing decisions.\n - Out of scope.\n - Further notes.\n-- Links to relevant Goal, domain terms, and ADRs.\n+- Links to relevant Goal, domain terms, SADRs, and target ADRs.\n \n Phase 4 ends with Spec Freeze: the operator explicitly approves the PRD, the\n plan records a `spec_freeze` block (status, approved_by, source), and the SF\n@@ -240,7 +243,7 @@\n - Dependencies and locks.\n - Risk and Guardian requirements.\n - Ledger completeness.\n-- Documentation and ADR updates.\n+- Documentation and SADR/target-ADR updates.\n - Scope drift.\n \n CCA emits one of:\n@@ -301,7 +304,7 @@\n | Evidence failure | Behavior might be done, but proof is missing. | Add checks, PR evidence, ledger entry, or artifacts. |\n | Scope drift | PR changes unrelated behavior. | Revert unrelated changes or split into new Goal. |\n | Dependency block | Upstream task or lock not complete. | Block until dependency resolves. |\n-| Architecture block | Decision needs ADR or deep-module discussion. | `grill-with-docs` or `improve-codebase-architecture`. |\n+| Architecture block | Decision needs a SADR, target ADR, or deep-module discussion. | `grill-with-docs` or `improve-codebase-architecture`. |\n | Risk block | Security/production/policy change needs human approval. | Guardian decision. |\n \n ## Phase 5.5 \u2014 Execution Decision Control\ndiff --git a/docs/agents/runtime-auth-model.md b/docs/agents/runtime-auth-model.md\nindex 6a2e1a47e81ea89b5c68a210ec07772a9374babc..3c54748d24233dbd0ba8473950282547ef4ce935\n--- a/docs/agents/runtime-auth-model.md\n+++ b/docs/agents/runtime-auth-model.md\n@@ -148,7 +148,7 @@\n \n API-key based runners may be added by a target repository as an explicit extension. They are not the default Shiki template path.\n \n-If a repo opts into API-key mode, record the exception in an ADR and update the repo's `AGENTS.md`, `.shiki/config.yaml`, and workflow permissions.\n+If a repo opts into API-key mode, record the target-specific exception in a target ADR and update the repo's `AGENTS.md`, `.shiki/config.yaml`, and workflow permissions.\n \n ## GitHub Actions Runtime Compatibility\n \ndiff --git a/docs/agents/skill-gate.md b/docs/agents/skill-gate.md\nindex c50398b0d7c60a8598f2a5c88a10015730dced13..3acffe6444617bb1d54b77d4b4708158ec735bc6\n--- a/docs/agents/skill-gate.md\n+++ b/docs/agents/skill-gate.md\n@@ -9,7 +9,7 @@\n | Skill | Trigger | Expected output |\n | --- | --- | --- |\n | `setup-matt-pocock-skills` | First configuring a repo for these skills, or when issue tracker, triage labels, or domain docs are missing. | `docs/agents/` configuration and an `Agent skills` block in `AGENTS.md` or `CLAUDE.md`. |\n-| `grill-with-docs` | Ambiguous plan, unclear terminology, boundary decision, tradeoff, or ADR-worthy design choice. | Clarified decision tree, recommended answers, updates to `CONTEXT.md` or ADRs when decisions crystallize. |\n+| `grill-with-docs` | Ambiguous plan, unclear terminology, boundary decision, tradeoff, or decision-record-worthy design choice. | Clarified decision tree, recommended answers, updates to `CONTEXT.md`, SADRs, or target ADRs when decisions crystallize. |\n | `zoom-out` | Agent lacks a higher-level architectural map. | Map of relevant modules, callers, seams, and domain concepts. |\n | `to-prd` | Settled context should become a PRD. | PRD published to the issue tracker with problem, solution, user stories, decisions, testing, and out-of-scope notes. |\n | `to-issues` | Goal, PRD, or plan must become implementation tickets. | Vertical-slice issues with acceptance criteria, dependencies, and AFK/HITL classification. |\n@@ -17,7 +17,7 @@\n | `tdd` | Feature work or bug fix has observable behavior that can be specified with tests. | Red-green-refactor loop through public interfaces, one vertical slice at a time. |\n | `code-review` | The TDD loop is green and the implementer is about to create or update a PR. Default for every implementation task. | Pre-PR self-review of the task diff with fixes applied on the task branch; findings and resolutions recorded as a ledger entry naming the skill and a `## Pre-PR code review` PR body section. |\n | `diagnose` | Hard bug, failing check, regression, flaky behavior, or performance problem. | Reproduction loop, ranked hypotheses, instrumentation, fix, regression test, post-mortem. |\n-| `improve-codebase-architecture` | Structural friction, testability, AI-navigability, deep-module opportunity, or refactor request. | Architecture candidates and deepening recommendations informed by `CONTEXT.md` and ADRs. |\n+| `improve-codebase-architecture` | Structural friction, testability, AI-navigability, deep-module opportunity, or refactor request. | Architecture candidates and deepening recommendations informed by `CONTEXT.md`, SADRs, and target ADRs. |\n | `prototype` | A throwaway logic or UI prototype is needed to answer a design question. | Clearly marked disposable prototype and captured decision. |\n \n ## MergeGate Interaction\ndiff --git a/docs/operations.md b/docs/operations.md\nindex cf1f452d4c589e38ccd58553bda0ad8684b51eb7..ace48a90ffbec9e5c0a7113bef16a4858bc44b3f\n--- a/docs/operations.md\n+++ b/docs/operations.md\n@@ -89,6 +89,16 @@\n      target history, brought forward by `shiki migrate`, not a template.\n    - **Everything else** (CLI scripts, workflows, schemas, docs) is overwritten.\n \n+   A forced upgrade also migrates the former numeric Shiki decision paths to\n+   `SADR-NNNN-*`. Before the first write, the installer inventories the exact\n+   legacy Shiki paths and authorizes each deletion only when the existing\n+   `.shiki/install-stamp.json` names that path and its SHA-256 digest still\n+   matches the file. An absent, unreadable, malformed, incomplete, or\n+   mismatching stamp reports every blocked path and stops with no filesystem\n+   change. Target-owned `NNNN-*` ADRs are not cleanup candidates. A non-force\n+   install deletes nothing and retains any still-matching legacy stamp entries\n+   so a later forced upgrade can prove ownership.\n+\n    If the target has pending migrations, `--force` refuses and writes nothing \u2014\n    apply migrations first (step 3), then re-run. The run ends with a single\n    summary of every `.new` written, so a half-upgrade is visible.\ndiff --git a/docs/releasing.md b/docs/releasing.md\nindex 10d73828e7dbd64e9d9f6b83bc84d010537ce62a..aafeee8c4d441e1e427efee2ce4f81a647f020e1\n--- a/docs/releasing.md\n+++ b/docs/releasing.md\n@@ -67,7 +67,7 @@\n CLI in `scripts/`. There are no third-party Python runtime dependencies. The CLI\n is intentionally **not** exposed as a `[project.scripts]` console entry point\n because `scripts/shiki.py` is a dependency-free shim and `scripts/` is not\n-packaged as an importable distribution (see the ADR for the rationale).\n+packaged as an importable distribution (see SADR-0007 for the rationale).\n \n Supported install paths:\n \n@@ -155,6 +155,15 @@\n    [docs/operations.md](operations.md#the-new-convention). If the target has\n    pending migrations, `--force` refuses and writes nothing; apply migrations\n    (step 2) first, then re-run.\n+\n+   The SADR namespace upgrade has an additional pre-write gate: each former\n+   numeric Shiki decision path is removed only when the existing install stamp\n+   contains that exact path and a digest matching its current bytes. The\n+   installer aggregates absent/unreadable/malformed-stamp, missing-digest, and\n+   digest-mismatch blockers before raising, so refusal leaves the target tree\n+   byte-identical. Numeric target ADRs are preserved. A successful forced\n+   upgrade installs and stamps `SADR-*` paths and drops the removed legacy paths\n+   from the refreshed stamp.\n 2. Apply repository-local `.shiki/` state migrations with the migrate command:\n \n    ```bash\ndiff --git a/docs/usage.md b/docs/usage.md\nindex b83407ee3970dd395dd893010bc5dbe8bad26051..a130ef6e40dfd8169a5bc701339351097df1a09f\n--- a/docs/usage.md\n+++ b/docs/usage.md\n@@ -67,8 +67,10 @@\n shiki install-target /path/to/target-repo --local-only\n ```\n \n-Use `--force` only when you intentionally want to overwrite existing target\n-files.\n+Use `--force` only when you intentionally want to refresh the target template.\n+For the SADR namespace migration, `--force` deletes only exact legacy Shiki\n+decision paths whose existing install-stamp digest still matches; any ownership\n+blocker stops before the first write. Numeric target ADRs are never candidates.\n \n ## `shiki bootstrap-platform` \u2014 publish the Shiki platform repo\n \ndiff --git a/scripts/guardian_approval_signal.py b/scripts/guardian_approval_signal.py\nindex d059f92b2c82aa2b5d6ca421365537a87886c1cb..16924892714f4f5b9303c0ef0e325a7e70075630\n--- a/scripts/guardian_approval_signal.py\n+++ b/scripts/guardian_approval_signal.py\n@@ -199,8 +199,8 @@\n ) -> Any:\n     \"\"\"Evaluate SADR-0015 Contract Approval for the CCA Guardian signal.\n \n-    The SAME OR that MergeGate applies must reach this deterministic signal: ADR\n-    0015 is explicit that without it, a contract-approved PR still returns\n+    The SAME OR that MergeGate applies must reach this deterministic signal:\n+    SADR-0015 is explicit that without it, a contract-approved PR still returns\n     ``needs_guardian``, the CCA job fails, and the MergeGate policy check never\n     runs \u2014 the gate this change moves would never be reached. This resolves the\n     task the SAME way MergeGate does (the first Shiki task id in the PR body),\ndiff --git a/scripts/shiki_contract_approval.py b/scripts/shiki_contract_approval.py\nindex 941bcb96fdd8b9a85bc0c4f0f04d5dad9d04839c..4a4b418f6527413161db82670bec857a043360e2\n--- a/scripts/shiki_contract_approval.py\n+++ b/scripts/shiki_contract_approval.py\n@@ -142,7 +142,7 @@\n     changed_files_status: Any,\n     registration: Any,\n ) -> ContractApprovalResult:\n-    \"\"\"Whether this PR carries an SADR-0015 Contract Approval.\n+    \"\"\"Whether this PR carries a SADR-0015 Contract Approval.\n \n     The four conditions are evaluated IN ORDER, each failure recorded as a\n     distinct reason string:\ndiff --git a/scripts/shiki_guardian.py b/scripts/shiki_guardian.py\nindex a4ecb79bbda55fd5c8b06adfae2eb99760a03c45..4dd99bbe714e26f421fa19abac7a34ae1b195ecc\n--- a/scripts/shiki_guardian.py\n+++ b/scripts/shiki_guardian.py\n@@ -12,7 +12,7 @@\n # The carry re-derives its proof from git in-process on every judging invocation;\n # it is never read from an artifact, so there is nothing to go stale or be forged.\n # shiki_sync_proof is the ONLY guardian-family module that runs git, and every one\n-# of its invocations is read-only and local (ADR: prove-from-git base sync).\n+# of its invocations is read-only and local (SADR: prove-from-git base sync).\n from shiki_sync_proof import verify_pure_base_sync\n \n GUARDIAN_POLICY_PATH = \".shiki/guardian-policy.json\"\ndiff --git a/scripts/shiki_guardian_review.py b/scripts/shiki_guardian_review.py\nindex dd0f104d457899e0ba5ff1e162011c1e90b6f92b..e10ef58c046324e7ea3a3392feb3048ca77414ce\n--- a/scripts/shiki_guardian_review.py\n+++ b/scripts/shiki_guardian_review.py\n@@ -159,7 +159,7 @@\n     docs_only = _is_docs_only(changed_files)\n     if docs_only and matched == 0:\n         focus.append(\n-            \"Docs/ADR-only change: confirm the change is genuinely documentation/decision-only, introduces no behavior or contract drift, and stays consistent with the constitution and existing ADRs.\"\n+            \"Docs/decision-record-only change: confirm the change is genuinely documentation/decision-only, introduces no behavior or contract drift, and stays consistent with the constitution and existing SADRs and target ADRs.\"\n         )\n \n     # Conservative augmentation: unknown (no category and not docs-only), mixed\ndiff --git a/scripts/shiki_installer.py b/scripts/shiki_installer.py\nindex be064ab7a29ff6836f7f7a1d70500a43e0d11d7b..9fd8eefd137ce7003dad5c0dcb2e48e2cd47ab32\n--- a/scripts/shiki_installer.py\n+++ b/scripts/shiki_installer.py\n@@ -118,6 +118,7 @@\n # what lets a target carry it without a governance-schema change.\n INSTALL_STAMP_PATH = \".shiki/install-stamp.json\"\n INSTALL_STAMP_VERSION = 1\n+LEGACY_SHIKI_SADR_MAX = 18\n \n # Under --force the shipped surface splits three ways, so nothing is lost and\n # nothing is silently stale.\n@@ -237,9 +238,98 @@\n         )\n \n \n+def legacy_shiki_adr_paths() -> tuple[str, ...]:\n+    \"\"\"Former numeric paths for Shiki SADR-0001 through SADR-0018.\n+\n+    The authoritative slugs come from the current SADR filenames. SADR-0019 is\n+    the namespace decision itself and never shipped under a numeric legacy path.\n+    \"\"\"\n+    paths: list[str] = []\n+    for sadr in sorted((ROOT / \"docs\" / \"adr\").glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\")):\n+        number = sadr.name.split(\"-\", 2)[1]\n+        if int(number) <= LEGACY_SHIKI_SADR_MAX:\n+            paths.append(f\"docs/adr/{sadr.name.removeprefix('SADR-')}\")\n+    return tuple(paths)\n+\n+\n+def preflight_legacy_sadr_cleanup(target: Path, *, force: bool) -> tuple[str, ...]:\n+    \"\"\"Authorize exact legacy Shiki ADR removals before the first write.\n+\n+    A non-force install never removes existing files. A forced install needs no\n+    stamp when no legacy Shiki path is present. When legacy paths do exist, each\n+    must be named in a readable install stamp and its current bytes must match\n+    the recorded digest.\n+    \"\"\"\n+    if not force:\n+        return ()\n+    present = tuple(\n+        relative\n+        for relative in legacy_shiki_adr_paths()\n+        if (target / relative).exists() or (target / relative).is_symlink()\n+    )\n+    if not present:\n+        return ()\n+\n+    stamp_path = target / INSTALL_STAMP_PATH\n+    try:\n+        raw = stamp_path.read_text(encoding=\"utf-8\")\n+        stamp = json.loads(raw)\n+    except FileNotFoundError:\n+        detail = \"install stamp is absent\"\n+        blockers = [f\"{relative}: {detail}\" for relative in present]\n+    except (OSError, UnicodeDecodeError) as error:\n+        detail = f\"install stamp is unreadable ({error})\"\n+        blockers = [f\"{relative}: {detail}\" for relative in present]\n+    except json.JSONDecodeError as error:\n+        detail = f\"install stamp is malformed ({error})\"\n+        blockers = [f\"{relative}: {detail}\" for relative in present]\n+    else:\n+        if not isinstance(stamp, dict):\n+            blockers = [\n+                f\"{relative}: install stamp is malformed (expected an object)\"\n+                for relative in present\n+            ]\n+        else:\n+            digests = stamp.get(\"digests\")\n+            blockers = []\n+            if not isinstance(digests, dict):\n+                blockers.extend(\n+                    f\"{relative}: install stamp has no digest for this path\"\n+                    for relative in present\n+                )\n+            else:\n+                for relative in present:\n+                    expected = digests.get(relative)\n+                    if not isinstance(expected, str) or not expected:\n+                        blockers.append(\n+                            f\"{relative}: install stamp has no digest for this path\"\n+                        )\n+                        continue\n+                    try:\n+                        actual = _sha256_file(target / relative)\n+                    except OSError as error:\n+                        blockers.append(\n+                            f\"{relative}: legacy path is unreadable ({error})\"\n+                        )\n+                        continue\n+                    if actual != expected:\n+                        blockers.append(\n+                            f\"{relative}: digest mismatch (stamp {expected}, current {actual})\"\n+                        )\n+\n+    if blockers:\n+        joined = \"\\n\".join(f\"- {blocker}\" for blocker in blockers)\n+        raise ShikiError(\n+            \"legacy Shiki ADR cleanup is not authorized:\\n\"\n+            f\"{joined}\\nNo files were written.\"\n+        )\n+    return present\n+\n+\n def install_template(target: Path, *, force: bool, validate: bool) -> list[NewFileNote]:\n     # Preconditions first: never rewrite the tree of a target that cannot be\n     # upgraded cleanly (see refuse_pending_migrations).\n+    legacy_cleanup = preflight_legacy_sadr_cleanup(target, force=force)\n     refuse_pending_migrations(target)\n \n     # Resolve the target's required CODEOWNERS owner once from its\n@@ -250,6 +340,10 @@\n     # verbatim. See ``codeowners_required_owner``.\n     codeowners_owner = codeowners_required_owner(target)\n \n+    for relative in legacy_cleanup:\n+        (target / relative).unlink()\n+        info(f\"removed stamped legacy Shiki ADR: {target / relative}\")\n+\n     notes: list[NewFileNote] = []\n     for relative in TEMPLATE_PATHS:\n         source = ROOT / relative\n@@ -647,6 +741,22 @@\n     digests = target_stamp_digests(target)\n     existing = read_install_stamp(target)\n     if existing is not None:\n+        # A non-force install deliberately leaves legacy Shiki ADRs in place.\n+        # Preserve only still-matching ownership proof so a later forced\n+        # upgrade can authorize cleanup; never carry a missing or stale digest.\n+        existing_digests = existing.get(\"digests\")\n+        if isinstance(existing_digests, dict):\n+            for relative in legacy_shiki_adr_paths():\n+                expected = existing_digests.get(relative)\n+                path = target / relative\n+                if not isinstance(expected, str) or not path.is_file():\n+                    continue\n+                try:\n+                    actual = _sha256_file(path)\n+                except OSError:\n+                    continue\n+                if actual == expected:\n+                    digests[relative] = actual\n         if _incoming_is_older(commit, existing.get(\"platform_commit\")):\n             warn(\n                 \"kept existing install stamp: incoming install \"\ndiff --git a/scripts/shiki_loop.py b/scripts/shiki_loop.py\nindex 3611641da8aeb5038b1da90e3114b09a473ec6d8..84caa491b9cad0edc4176f209c13e9221e975681\n--- a/scripts/shiki_loop.py\n+++ b/scripts/shiki_loop.py\n@@ -424,7 +424,7 @@\n         # unresolvable CCA verdict has already produced its terminal stop and is never\n         # laundered into a head-moving sync (SADR-0010). It also never fires while the\n         # policy (Guardian) gate is red \u2014 for ANY reason, not only when it is the sole\n-        # red check \u2014 so a head-bound Guardian approval, or an SADR-0015\n+        # red check \u2014 so a head-bound Guardian approval, or a SADR-0015\n         # contract-approval carry that base movement defeated (the policy gate compares\n         # base and head task governance directly), is never silently restored by\n         # adopting base wholesale. It is bounded by a durable per-task counter so the\ndiff --git a/scripts/shiki_tasks.py b/scripts/shiki_tasks.py\nindex aec3809f24b4a0c8c96fc50971cff9ae9ad30381..d531052e9d9717058d4631483cefab44c26a65c1\n--- a/scripts/shiki_tasks.py\n+++ b/scripts/shiki_tasks.py\n@@ -821,7 +821,7 @@\n         \"prompt\": prompt,\n         \"entry_skill\": \"grill-with-docs\",\n         \"required_next_steps\": [\n-            \"Run grill-with-docs until terminology, boundaries, risks, and ADR-worthy decisions are settled.\",\n+            \"Run grill-with-docs until terminology, boundaries, risks, and decision-record-worthy choices are settled.\",\n             \"Obtain the operator's explicit approval of the PRD/requirements (Spec Freeze).\",\n             \"Write a machine-readable plan JSON with grill_with_docs.status=complete and spec_freeze.status=frozen.\",\n             \"Run shiki plan ingest --plan-file PLAN.json.\",\ndiff --git a/scripts/test_shiki_goal_loop.sh b/scripts/test_shiki_goal_loop.sh\nindex 753c7b516e10f0be3bc92a4bd0eb1fa699776ae6..8f3820a0a079b3044424cfe1a6c6b7fe68896e67\n--- a/scripts/test_shiki_goal_loop.sh\n+++ b/scripts/test_shiki_goal_loop.sh\n@@ -231,8 +231,8 @@\n \n # High/critical risk merges autonomously when all required checks are green:\n # the \"MergeGate policy check\" required check IS the Guardian gate (it enforces\n-# guardian-policy.json \u2014 human approval OR external AI guardian review, ADR\n-# 0010), so green checks mean a recorded authority approved. The fake gh returns\n+# guardian-policy.json \u2014 human approval OR external AI guardian review,\n+# SADR-0010), so green checks mean a recorded authority approved. The fake gh returns\n # green checks, simulating that the Guardian gate passed.\n python3 \"$ROOT/scripts/shiki.py\" goal create --target \"$TARGET\" --title \"Guardian gate\" --outcome \"High risk merges via policy gate\" >/tmp/shiki-goal-loop-guardian-goal.json\n GGOAL=\"$(json_get /tmp/shiki-goal-loop-guardian-goal.json goal_id)\"\ndiff --git a/tests/test_adr_references.py b/tests/test_adr_references.py\nindex 51706329c797d5dd9b47e082a333fa83b261d482..d005da4f771f5e8085ec22d4b787980e68f5ae8c\n--- a/tests/test_adr_references.py\n+++ b/tests/test_adr_references.py\n@@ -1,8 +1,8 @@\n-\"\"\"Every ``ADR ####`` citation must resolve to exactly one decision record.\n+\"\"\"Every target ADR and Shiki SADR citation resolves in its own namespace.\n \n Background. ``scripts/mergegate_check.py`` justifies ``is_bookkeeping_closeout``\n-with \"ADR 0017\" nine times and ``scripts/guardian_approval_signal.py`` cites\n-0016/0017 seven times, yet for a stretch the numbering in ``docs/adr/`` stopped\n+with \"SADR-0017\" nine times and ``scripts/guardian_approval_signal.py`` cites\n+SADR-0016/SADR-0017 seven times, yet for a stretch the numbering in ``docs/adr/`` stopped\n at 0015: the authoring commits lived on a coordinator branch and no task ever\n declared ``docs/adr/...`` in its locks, so nothing carried the records to the\n default branch. The stated basis of an exemption that merged seven PRs with zero\n@@ -11,11 +11,12 @@\n and the directory drifted with no signal.\n \n This is the same unbound-pair shape behind other silent failures: a reference\n-and its target with nothing asserting they agree. This test binds them. It scans\n-every ``scripts/*.py`` (plus ``docs/**/*.md`` and the root ``*.md`` governance\n-files, which are cheap) for the ``ADR ####`` pattern and asserts each cited\n-number resolves to exactly one ``docs/adr/####-*.md``. The converse is\n-deliberately NOT required: an ADR may exist without being cited.\n+and its target with nothing asserting they agree. This test binds them. Target\n+``ADR NNNN`` citations resolve only to ``docs/adr/NNNN-*.md``; Shiki\n+``SADR-NNNN`` citations resolve only to ``docs/adr/SADR-NNNN-*.md``. The scan\n+covers current governance docs, scripts, workflows, prompts, packaging metadata,\n+and repo-local Codex guidance while deliberately excluding historical\n+``.shiki`` evidence. The converse is not required: a record may be uncited.\n \n The suite is dependency free: standard-library ``unittest`` only.\n \"\"\"\n@@ -31,17 +32,18 @@\n \n REPO_ROOT = Path(__file__).resolve().parents[1]\n \n-# \"ADR 0016\" -- a four-digit citation. The trailing \\b keeps a five-digit token\n-# from being read as a truncated four-digit number.\n-_CITATION = re.compile(r\"\\bADR (\\d{4})\\b\")\n-# \"0016-locks-bound-product-paths.md\" -- the leading four digits name the record.\n-_ADR_FILENAME = re.compile(r\"^(\\d{4})-.*\\.md$\")\n+# ``ADR 0016`` / ``SADR-0016`` -- a namespace plus four-digit citation. The\n+# trailing ``\\b`` keeps a five-digit token from being read as a truncated one.\n+_CITATION = re.compile(r\"\\b(S?ADR)[ -](\\d{4})\\b\")\n+# Target ADRs retain the numeric filename; Shiki platform decisions use SADR.\n+_DECISION_FILENAME = re.compile(r\"^(?:(SADR)-)?(\\d{4})-.*\\.md$\")\n \n \n class Violation(NamedTuple):\n     \"\"\"A citation that does not resolve to exactly one decision record.\"\"\"\n \n     citing_file: str  # repo-relative path of the file that cites the ADR\n+    namespace: str  # \"ADR\" for target decisions; \"SADR\" for Shiki decisions\n     number: str  # the cited four-digit number, e.g. \"0016\"\n     line: int  # 1-based line of the citation\n     reason: str  # \"missing\" (zero matches) or \"ambiguous\" (two or more)\n@@ -49,44 +51,59 @@\n \n     @property\n     def expected_path(self) -> str:\n-        return f\"docs/adr/{self.number}-*.md\"\n+        prefix = \"SADR-\" if self.namespace == \"SADR\" else \"\"\n+        return f\"docs/adr/{prefix}{self.number}-*.md\"\n \n     def describe(self) -> str:\n         \"\"\"Name the citing file, the cited number, and the missing path.\"\"\"\n         if self.reason == \"missing\":\n             return (\n-                f\"{self.citing_file}:{self.line} cites ADR {self.number} \"\n+                f\"{self.citing_file}:{self.line} cites {self.namespace} {self.number} \"\n                 f\"but no document matches {self.expected_path}\"\n             )\n         joined = \", \".join(self.matches)\n         return (\n-            f\"{self.citing_file}:{self.line} cites ADR {self.number} which \"\n+            f\"{self.citing_file}:{self.line} cites {self.namespace} {self.number} which \"\n             f\"resolves to {len(self.matches)} documents (expected exactly one \"\n             f\"at {self.expected_path}): {joined}\"\n         )\n \n \n-def _adr_index(root: Path) -> dict[str, list[Path]]:\n-    \"\"\"Map every four-digit ADR number to the record files that claim it.\"\"\"\n-    index: dict[str, list[Path]] = {}\n+def _adr_index(root: Path) -> dict[tuple[str, str], list[Path]]:\n+    \"\"\"Map each decision namespace and number to the files that claim it.\"\"\"\n+    index: dict[tuple[str, str], list[Path]] = {}\n     adr_dir = root / \"docs\" / \"adr\"\n     if adr_dir.is_dir():\n         for path in sorted(adr_dir.glob(\"*.md\")):\n-            match = _ADR_FILENAME.match(path.name)\n+            match = _DECISION_FILENAME.match(path.name)\n             if match:\n-                index.setdefault(match.group(1), []).append(path)\n+                namespace = \"SADR\" if match.group(1) else \"ADR\"\n+                index.setdefault((namespace, match.group(2)), []).append(path)\n     return index\n \n \n def _citing_files(root: Path, include_docs: bool = True) -> list[Path]:\n-    \"\"\"Files scanned for citations: scripts/*.py, and cheaply the markdown.\"\"\"\n+    \"\"\"Current, shipped text surfaces scanned for decision citations.\"\"\"\n     files = sorted((root / \"scripts\").glob(\"*.py\"))\n+    files += sorted((root / \"scripts\").glob(\"*.sh\"))\n     if include_docs:\n         docs = root / \"docs\"\n         if docs.is_dir():\n             files += sorted(docs.rglob(\"*.md\"))\n         files += sorted(root.glob(\"*.md\"))\n-    return files\n+        pyproject = root / \"pyproject.toml\"\n+        if pyproject.is_file():\n+            files.append(pyproject)\n+        for relative in (\".github\", \".codex\"):\n+            surface = root / relative\n+            if surface.is_dir():\n+                files += sorted(\n+                    path\n+                    for path in surface.rglob(\"*\")\n+                    if path.is_file()\n+                    and path.suffix in {\".md\", \".py\", \".sh\", \".toml\", \".yaml\", \".yml\"}\n+                )\n+    return sorted(set(files))\n \n \n def find_adr_reference_violations(\n@@ -94,10 +111,9 @@\n ) -> list[Violation]:\n     \"\"\"Return every ``ADR ####`` citation under ``root`` that fails to resolve.\n \n-    A citation resolves when exactly one ``docs/adr/####-*.md`` matches its\n-    number. Zero matches is a \"missing\" violation; two or more is \"ambiguous\".\n-    Records that are never cited produce nothing -- the converse is not\n-    required.\n+    A citation resolves when exactly one record in the same namespace matches\n+    its number. Zero matches is a \"missing\" violation; two or more is\n+    \"ambiguous\". Records that are never cited produce nothing.\n     \"\"\"\n     root = Path(root)\n     index = _adr_index(root)\n@@ -110,13 +126,15 @@\n         rel = path.relative_to(root).as_posix()\n         for lineno, line in enumerate(text.splitlines(), start=1):\n             for match in _CITATION.finditer(line):\n-                number = match.group(1)\n-                found = index.get(number, [])\n+                namespace, number = match.groups()\n+                found = index.get((namespace, number), [])\n                 if len(found) == 1:\n                     continue\n                 reason = \"missing\" if not found else \"ambiguous\"\n                 rels = [p.relative_to(root).as_posix() for p in found]\n-                violations.append(Violation(rel, number, lineno, reason, rels))\n+                violations.append(\n+                    Violation(rel, namespace, number, lineno, reason, rels)\n+                )\n     return violations\n \n \n@@ -129,8 +147,8 @@\n         self.assertEqual(\n             violations,\n             [],\n-            \"Every 'ADR ####' citation must resolve to exactly one \"\n-            f\"docs/adr/####-*.md:\\n{detail}\",\n+            \"Every ADR/SADR citation must resolve exactly once in its own \"\n+            f\"docs/adr namespace:\\n{detail}\",\n         )\n \n     def test_scan_actually_reads_scripts(self) -> None:\n@@ -142,12 +160,38 @@\n             \"the citation scan must cover scripts/*.py\",\n         )\n \n+    def test_platform_citations_do_not_use_unresolved_numeric_shorthand(self) -> None:\n+        shorthand = re.compile(r\"SADR-\\d{4}/\\d{4}\")\n+        offenders = []\n+        for path in _citing_files(REPO_ROOT):\n+            text = path.read_text(encoding=\"utf-8\")\n+            if shorthand.search(text):\n+                offenders.append(path.relative_to(REPO_ROOT).as_posix())\n+        self.assertEqual(\n+            offenders,\n+            [],\n+            \"repeat the SADR prefix so every cited number is independently resolvable\",\n+        )\n+\n     def test_carried_records_are_indexed(self) -> None:\n         # The two records this Goal carried onto the default branch, and which\n         # the scripts cite most, must be present.\n         index = _adr_index(REPO_ROOT)\n-        self.assertIn(\"0016\", index)\n-        self.assertIn(\"0017\", index)\n+        self.assertIn((\"SADR\", \"0016\"), index)\n+        self.assertIn((\"SADR\", \"0017\"), index)\n+\n+    def test_platform_tree_has_only_sadr_0001_through_0019(self) -> None:\n+        adr_dir = REPO_ROOT / \"docs\" / \"adr\"\n+        sadrs = sorted(adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\"))\n+        self.assertEqual(\n+            {path.name.split(\"-\", 2)[1] for path in sadrs},\n+            {f\"{number:04d}\" for number in range(1, 20)},\n+        )\n+        self.assertEqual(\n+            list(adr_dir.glob(\"[0-9][0-9][0-9][0-9]-*.md\")),\n+            [],\n+            \"the Shiki platform tree must not retain legacy numeric SADR paths\",\n+        )\n \n \n class FixtureAdrReferences(unittest.TestCase):\n@@ -180,6 +224,26 @@\n         self.assertIn(\"9991\", message)  # cited number\n         self.assertIn(\"docs/adr/9991-*.md\", message)  # missing path\n \n+    def test_target_adr_does_not_satisfy_missing_platform_sadr(self) -> None:\n+        root = self._make_repo(\n+            adr_names=[\"0042-target-decision.md\"],\n+            script_body='\"\"\"Target ADR 0042 exists, but platform SADR-0042 does not.\"\"\"\\n',\n+        )\n+        violations = find_adr_reference_violations(root, include_docs=False)\n+        self.assertEqual(len(violations), 1)\n+        self.assertEqual(violations[0].namespace, \"SADR\")\n+        self.assertEqual(violations[0].number, \"0042\")\n+        self.assertEqual(violations[0].expected_path, \"docs/adr/SADR-0042-*.md\")\n+\n+    def test_target_adr_and_platform_sadr_may_reuse_a_number(self) -> None:\n+        root = self._make_repo(\n+            adr_names=[\"0042-target-decision.md\", \"SADR-0042-platform-decision.md\"],\n+            script_body='\"\"\"Target ADR 0042 and platform SADR-0042 are distinct.\"\"\"\\n',\n+        )\n+        self.assertEqual(\n+            find_adr_reference_violations(root, include_docs=False), []\n+        )\n+\n     def test_uncited_adr_is_allowed(self) -> None:\n         # An ADR exists but nothing cites it -> not a violation. The converse\n         # (every ADR must be cited) is explicitly NOT required.\ndiff --git a/tests/test_guardian_review.py b/tests/test_guardian_review.py\nindex 88829e7810212635e2fc98aa5918d6f26294677d..00a85a237acd5bf2d6cb5b294128198c422402ca\n--- a/tests/test_guardian_review.py\n+++ b/tests/test_guardian_review.py\n@@ -162,7 +162,7 @@\n \n     def test_docs_only_is_light_and_not_conservative(self):\n         focus = gr.classify_review_focus([\"docs/adr/0099-x.md\", \"README.md\"], \"\", \"low\")\n-        self.assertTrue(any(\"Docs/ADR-only\" in f for f in focus))\n+        self.assertTrue(any(\"Docs/decision-record-only\" in f for f in focus))\n         self.assertFalse(any(\"conservative default\" in f.lower() for f in focus))\n \n     def test_unknown_category_is_conservative(self):\ndiff --git a/tests/test_policy_job_merged_prs.py b/tests/test_policy_job_merged_prs.py\nindex d45512e74681e5e649279236c7d4e79d5c3da471..b97989f8bd74d091de1a6b5275a20d8dc68c4512\n--- a/tests/test_policy_job_merged_prs.py\n+++ b/tests/test_policy_job_merged_prs.py\n@@ -3,8 +3,8 @@\n \n ``.github/workflows/shiki-cca-completion.yml`` has two consumers of the merged-PR\n proof: the CCA *signal* (``guardian_approval_signal.py`` in \"Prepare CCA context\")\n-and the authoritative gate (``mergegate_check.py`` in \"Enforce MergeGate\"). ADR\n-0017's bookkeeping-closeout exemption (condition 6) needs the merged implementation\n+and the authoritative gate (``mergegate_check.py`` in \"Enforce MergeGate\").\n+SADR-0017's bookkeeping-closeout exemption (condition 6) needs the merged implementation\n PR proven, and a closeout carries NO ``post_merge_reconcile`` marker. The signal\n already resolves ``MERGED_PRS`` unconditionally, but \"Enforce MergeGate\" used to\n resolve it only inside ``grep -q '<!-- shiki:post_merge_reconcile -->'``. So a\ndiff --git a/tests/test_sadr_install_migration.py b/tests/test_sadr_install_migration.py\nnew file mode 100644\nindex 0000000000000000000000000000000000000000..d51eac872b1c79ff4579c4c1401ca5d775a60d8d\n--- /dev/null\n+++ b/tests/test_sadr_install_migration.py\n@@ -0,0 +1,237 @@\n+\"\"\"SADR installation and collision-safe legacy upgrade behavior.\"\"\"\n+\n+from __future__ import annotations\n+\n+import contextlib\n+import hashlib\n+import io\n+import json\n+import shutil\n+import tempfile\n+import unittest\n+from pathlib import Path\n+\n+import shiki_test_support  # noqa: F401  (path bootstrap)\n+\n+import shiki_installer\n+from tests.test_adr_references import find_adr_reference_violations\n+\n+\n+PRODUCT_NUMBERS = tuple(range(13, 19))\n+\n+\n+def _quiet_install(target: Path, *, force: bool, validate: bool) -> None:\n+    with contextlib.redirect_stdout(io.StringIO()):\n+        shiki_installer.install_template(target, force=force, validate=validate)\n+\n+\n+def _sha256(path: Path) -> str:\n+    return hashlib.sha256(path.read_bytes()).hexdigest()\n+\n+\n+def _legacy_sadr_paths() -> tuple[str, ...]:\n+    return tuple(\n+        f\"docs/adr/{path.name.removeprefix('SADR-')}\"\n+        for path in sorted((shiki_installer.ROOT / \"docs\" / \"adr\").glob(\"SADR-*.md\"))\n+        if path.name != \"SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\"\n+    )\n+\n+\n+def _create_legacy_upgrade_fixture(\n+    target: Path,\n+) -> tuple[dict[str, bytes], tuple[str, ...]]:\n+    adr_dir = target / \"docs\" / \"adr\"\n+    adr_dir.mkdir(parents=True)\n+    product_bytes = {\n+        f\"{number:04d}-cubeshare-product-decision.md\": (\n+            f\"# CubeShare ADR {number:04d}\\n\\nDo not rewrite {number}.\\n\"\n+        ).encode()\n+        for number in PRODUCT_NUMBERS\n+    }\n+    for name, content in product_bytes.items():\n+        (adr_dir / name).write_bytes(content)\n+\n+    legacy_paths = _legacy_sadr_paths()\n+    for relative in legacy_paths:\n+        (target / relative).write_bytes(f\"legacy Shiki bytes for {relative}\\n\".encode())\n+\n+    stamp_path = target / shiki_installer.INSTALL_STAMP_PATH\n+    stamp_path.parent.mkdir(parents=True, exist_ok=True)\n+    stamp = {\n+        \"version\": shiki_installer.INSTALL_STAMP_VERSION,\n+        \"platform_commit\": None,\n+        \"installed_at\": \"2000-01-01T00:00:00+00:00\",\n+        \"digests\": {\n+            relative: _sha256(target / relative) for relative in legacy_paths\n+        },\n+    }\n+    stamp_path.write_text(\n+        json.dumps(stamp, indent=2, sort_keys=True) + \"\\n\", encoding=\"utf-8\"\n+    )\n+    return product_bytes, legacy_paths\n+\n+\n+def _tree_snapshot(root: Path) -> dict[str, tuple[str, bytes | str]]:\n+    \"\"\"Recursive byte/shape snapshot used to prove preflight is write-free.\"\"\"\n+    snapshot: dict[str, tuple[str, bytes | str]] = {}\n+    for path in sorted(root.rglob(\"*\")):\n+        relative = path.relative_to(root).as_posix()\n+        if path.is_symlink():\n+            snapshot[relative] = (\"symlink\", path.readlink().as_posix())\n+        elif path.is_dir():\n+            snapshot[relative] = (\"directory\", \"\")\n+        else:\n+            snapshot[relative] = (\"file\", path.read_bytes())\n+    return snapshot\n+\n+\n+class FreshSadrInstallTests(unittest.TestCase):\n+    def setUp(self) -> None:\n+        self.tmp = Path(tempfile.mkdtemp(prefix=\"shiki-sadr-fresh-\"))\n+        self.addCleanup(lambda: shutil.rmtree(self.tmp, ignore_errors=True))\n+        self.target = self.tmp / \"target\"\n+        self.adr_dir = self.target / \"docs\" / \"adr\"\n+        self.adr_dir.mkdir(parents=True)\n+        self.product_bytes = {\n+            f\"{number:04d}-product-decision.md\": (\n+                f\"# Product ADR {number:04d}\\n\\nTarget-owned bytes {number}.\\n\"\n+            ).encode()\n+            for number in PRODUCT_NUMBERS\n+        }\n+        for name, content in self.product_bytes.items():\n+            (self.adr_dir / name).write_bytes(content)\n+\n+    def test_fresh_install_preserves_product_adrs_and_installs_all_sadrs(self) -> None:\n+        _quiet_install(self.target, force=False, validate=True)\n+\n+        for name, expected in self.product_bytes.items():\n+            self.assertEqual((self.adr_dir / name).read_bytes(), expected)\n+\n+        sadr_numbers = {\n+            path.name.split(\"-\", 2)[1]\n+            for path in self.adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\")\n+        }\n+        self.assertEqual(sadr_numbers, {f\"{number:04d}\" for number in range(1, 20)})\n+\n+        numeric_records = {\n+            path.name\n+            for path in self.adr_dir.glob(\"[0-9][0-9][0-9][0-9]-*.md\")\n+        }\n+        self.assertEqual(numeric_records, set(self.product_bytes))\n+        stamp = json.loads(\n+            (self.target / shiki_installer.INSTALL_STAMP_PATH).read_text(\n+                encoding=\"utf-8\"\n+            )\n+        )\n+        for path in self.adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\"):\n+            relative = path.relative_to(self.target).as_posix()\n+            self.assertEqual(stamp[\"digests\"][relative], _sha256(path))\n+        self.assertEqual(find_adr_reference_violations(self.target), [])\n+\n+\n+class LegacySadrUpgradeTests(unittest.TestCase):\n+    def setUp(self) -> None:\n+        self.tmp = Path(tempfile.mkdtemp(prefix=\"shiki-sadr-upgrade-\"))\n+        self.addCleanup(lambda: shutil.rmtree(self.tmp, ignore_errors=True))\n+        self.target = self.tmp / \"target\"\n+        self.adr_dir = self.target / \"docs\" / \"adr\"\n+        self.product_bytes, self.legacy_paths = _create_legacy_upgrade_fixture(\n+            self.target\n+        )\n+\n+    def test_force_upgrade_deletes_only_stamped_legacy_sadrs(self) -> None:\n+        _quiet_install(self.target, force=True, validate=True)\n+\n+        for relative in self.legacy_paths:\n+            self.assertFalse((self.target / relative).exists(), relative)\n+        for name, expected in self.product_bytes.items():\n+            self.assertEqual((self.adr_dir / name).read_bytes(), expected)\n+\n+        sadrs = list(self.adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\"))\n+        self.assertEqual(len(sadrs), 19)\n+        refreshed = json.loads(\n+            (self.target / shiki_installer.INSTALL_STAMP_PATH).read_text(encoding=\"utf-8\")\n+        )\n+        for relative in self.legacy_paths:\n+            self.assertNotIn(relative, refreshed[\"digests\"])\n+        for path in sadrs:\n+            relative = path.relative_to(self.target).as_posix()\n+            self.assertEqual(refreshed[\"digests\"][relative], _sha256(path))\n+\n+    def test_non_force_install_never_deletes_legacy_files(self) -> None:\n+        before = {\n+            relative: (self.target / relative).read_bytes()\n+            for relative in self.legacy_paths\n+        }\n+        _quiet_install(self.target, force=False, validate=False)\n+        for relative, expected in before.items():\n+            self.assertEqual((self.target / relative).read_bytes(), expected)\n+        refreshed = json.loads(\n+            (self.target / shiki_installer.INSTALL_STAMP_PATH).read_text(encoding=\"utf-8\")\n+        )\n+        for relative in self.legacy_paths:\n+            self.assertEqual(refreshed[\"digests\"][relative], _sha256(self.target / relative))\n+\n+\n+class LegacyCleanupPreflightTests(unittest.TestCase):\n+    def setUp(self) -> None:\n+        self.tmp = Path(tempfile.mkdtemp(prefix=\"shiki-sadr-preflight-\"))\n+        self.addCleanup(lambda: shutil.rmtree(self.tmp, ignore_errors=True))\n+\n+    def test_all_ownership_blockers_abort_before_any_write(self) -> None:\n+        cases = (\n+            (\"absent\", \"install stamp is absent\"),\n+            (\"unreadable\", \"install stamp is unreadable\"),\n+            (\"malformed\", \"install stamp is malformed\"),\n+            (\"missing-digest\", \"install stamp has no digest\"),\n+            (\"digest-mismatch\", \"digest mismatch\"),\n+        )\n+        for name, expected_reason in cases:\n+            with self.subTest(name=name):\n+                target = self.tmp / name\n+                _, legacy_paths = _create_legacy_upgrade_fixture(target)\n+                stamp_path = target / shiki_installer.INSTALL_STAMP_PATH\n+\n+                if name == \"absent\":\n+                    stamp_path.unlink()\n+                elif name == \"unreadable\":\n+                    stamp_path.unlink()\n+                    stamp_path.mkdir()\n+                elif name == \"malformed\":\n+                    stamp_path.write_text(\"{not-json\\n\", encoding=\"utf-8\")\n+                else:\n+                    stamp = json.loads(stamp_path.read_text(encoding=\"utf-8\"))\n+                    if name == \"missing-digest\":\n+                        stamp[\"digests\"] = {}\n+                    else:\n+                        stamp[\"digests\"] = {\n+                            relative: \"deadbeef\" for relative in legacy_paths\n+                        }\n+                    stamp_path.write_text(\n+                        json.dumps(stamp, indent=2, sort_keys=True) + \"\\n\",\n+                        encoding=\"utf-8\",\n+                    )\n+\n+                before = _tree_snapshot(target)\n+                with self.assertRaises(shiki_installer.ShikiError) as raised:\n+                    _quiet_install(target, force=True, validate=False)\n+                after = _tree_snapshot(target)\n+\n+                self.assertEqual(before, after, f\"{name} preflight changed the tree\")\n+                message = str(raised.exception)\n+                self.assertIn(expected_reason, message)\n+                self.assertIn(\"No files were written\", message)\n+                for relative in legacy_paths:\n+                    self.assertIn(relative, message)\n+\n+    def test_absent_stamp_is_allowed_when_no_legacy_path_exists(self) -> None:\n+        target = self.tmp / \"no-legacy\"\n+        target.mkdir()\n+        _quiet_install(target, force=True, validate=False)\n+        self.assertTrue(\n+            (target / \"docs\" / \"adr\" / \"SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\").is_file()\n+        )\n+\n+\n+if __name__ == \"__main__\":\n+    unittest.main()\n\n2026-08-07T08:25:38.005736Z ERROR codex_models_manager::manager: failed to renew cache TTL: missing field `base_instructions` at line 94 column 5\ncodex\nThe re-review is clean after the test fix: deletion candidates are a fixed 18-path allowlist, stamp entries cannot introduce arbitrary paths, every present candidate must pass an exact SHA-256 check, and all refusal paths remain read-only. I\u2019m starting the final full verification now; the long unittest run is expected to take about 90 seconds.\nexec\n/bin/zsh -lc 'python3 -m unittest discover -s tests' in /private/tmp/shiki-adr-collision.pa0KwY/.worktrees/shiki-t-20260807t073307221254z-2d22d654-introduc\ndiff --git a/.codex/skills/shiki/SKILL.md b/.codex/skills/shiki/SKILL.md\nindex c8e2e85d9d75d49cc4f2d6286ee22659c4e53e75..93cded623b2c15d9f65c76491a1c2d813ac9e6c0\n--- a/.codex/skills/shiki/SKILL.md\n+++ b/.codex/skills/shiki/SKILL.md\n@@ -18,6 +18,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n Codex CLI does not register this skill as a custom `/shiki` slash command. When\n the user types `/shiki` in Codex CLI and gets \"Unrecognized command\", explain\n that this is expected: invoke Shiki in Codex with natural language such as\n@@ -69,7 +72,7 @@\n \n ## Responsibilities\n \n-- Claude Code implements and repairs by default (`shiki runner claude`, ADR 0008); Codex implements and repairs tasks explicitly assigned to `codex`.\n+- Claude Code implements and repairs by default (`shiki runner claude`, SADR-0008); Codex implements and repairs tasks explicitly assigned to `codex`.\n - Claude Code Action can act as GitHub-side CCA or reviewer.\n - CCA judges completion.\n - MergeGate authorizes state transitions and merge readiness.\ndiff --git a/.github/prompts/cca-completion-check.md b/.github/prompts/cca-completion-check.md\nindex ed203ad5f302ecc81372d8ab27d2b1232ca97497..fd89950528333ef6242e6d5443904f5392ab7e56\n--- a/.github/prompts/cca-completion-check.md\n+++ b/.github/prompts/cca-completion-check.md\n@@ -19,6 +19,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n ## Required Reading\n \n Read only the files needed to judge the PR contract. Prefer this order:\ndiff --git a/.github/prompts/codex-task-handoff.md b/.github/prompts/codex-task-handoff.md\nindex 695e211d76dea1c60e8d5bb360e78487808286a4..59441d576b01a39569722f226c854e5a740e9eb5\n--- a/.github/prompts/codex-task-handoff.md\n+++ b/.github/prompts/codex-task-handoff.md\n@@ -2,7 +2,7 @@\n \n You are Codex Front acting as the Implementer for a Shiki task.\n \n-Follow `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant ADRs, and `docs/agents/codex-handoff.md`.\n+Follow `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant Shiki SADRs and target ADRs, and `docs/agents/codex-handoff.md`.\n \n Rules:\n \ndiff --git a/AGENTS.md b/AGENTS.md\nindex 7f041100d49c2a7525f51cb0f5d696a4a6d1c78c..a06bfbabc6fa48a007389e25e12d7ed523936bed\n--- a/AGENTS.md\n+++ b/AGENTS.md\n@@ -22,7 +22,7 @@\n - **Codex Front** is an optional implementer through Codex App, Codex CLI, Codex IDE extension, or Codex Web signed in with ChatGPT OAuth/subscription auth, dispatched with `shiki runner codex` when a task is explicitly assigned to `codex`.\n - **GitHub CCA** is implemented by Claude Code Action by default, using `CLAUDE_CODE_OAUTH_TOKEN`.\n \n-Do not assume `openai/codex-action`, `OPENAI_API_KEY`, or API-key based Codex automation in the default Shiki path. API-key runners are explicit target-repo extensions and require their own ADR.\n+Do not assume `openai/codex-action`, `OPENAI_API_KEY`, or API-key based Codex automation in the default Shiki path. API-key runners are explicit target-repo extensions and require their own target ADR.\n \n Shiki is not:\n \n@@ -44,10 +44,14 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use\n+`SADR-NNNN-*.md`; target product decisions retain `NNNN-*.md` and the `ADR`\n+namespace.\n+\n Use the canonical source-of-truth order above. Runtime-specific reading orders\n are operational guidance only and must not override or reorder it.\n \n-Conversation state is not durable truth. If a decision matters, put it in GitHub, `.shiki/`, `CONTEXT.md`, or an ADR.\n+Conversation state is not durable truth. If a decision matters, put it in GitHub, `.shiki/`, `CONTEXT.md`, a Shiki SADR, or a target ADR.\n \n If GitHub and `.shiki/` disagree, surface the conflict and prefer GitHub operational state until the mirror is repaired.\n \n@@ -75,8 +79,8 @@\n Every non-trivial change follows this loop:\n \n 1. **Goal**: clarify outcome, completion conditions, non-goals, risk level, and success signals.\n-2. **grill-with-docs**: challenge the plan against domain docs, ADRs, code reality, terminology, and edge scenarios.\n-3. **Context & Impact**: find relevant docs, ADRs, code, dependencies, owners, locks, and verification surfaces.\n+2. **grill-with-docs**: challenge the plan against domain docs, Shiki SADRs, target ADRs, code reality, terminology, and edge scenarios.\n+3. **Context & Impact**: find relevant docs, SADRs, target ADRs, code, dependencies, owners, locks, and verification surfaces.\n 4. **PRD and Spec Freeze**: use `to-prd` when context is settled enough to become durable product/engineering intent. The operator's explicit approval of the PRD is Spec Freeze: the plan records a `spec_freeze` block, and no plan runs without it. Steps 1-4 form Requirements Definition \u2014 one continuous operator dialogue. After Spec Freeze, scope changes require an operator-approved Spec Amendment; non-scope-moving interpretations go to the Assumption Log.\n 5. **Issues**: use `to-issues` to create independently grabbable vertical-slice issues. Prefer AFK slices over HITL slices where possible.\n 6. **Triage**: label issues for readiness, risk, runtime, skills, and MergeGate state.\n@@ -167,7 +171,7 @@\n - Testing decisions.\n - Out-of-scope items.\n - Further notes.\n-- Links to relevant domain terms and ADRs.\n+- Links to relevant domain terms, SADRs, and target ADRs.\n \n Every executable task must include:\n \n@@ -193,7 +197,7 @@\n \n Context & Impact must identify:\n \n-- Relevant documents, domain terms, ADRs, and past decisions.\n+- Relevant documents, domain terms, SADRs, target ADRs, and past decisions.\n - Relevant modules, interfaces, seams, callers, owners, and tests.\n - Dependency relationships between tasks.\n - Candidate locks and lock conflicts.\n@@ -210,7 +214,7 @@\n | Skill | Required when |\n | --- | --- |\n | `setup-matt-pocock-skills` | First configuring the repo for skills, or when issue tracker, triage labels, or domain docs are missing. |\n-| `grill-with-docs` | Default for non-trivial Goals before PRD/issues; always required when plans, terminology, boundaries, tradeoffs, or ADR-worthy decisions are ambiguous. |\n+| `grill-with-docs` | Default for non-trivial Goals before PRD/issues; always required when plans, terminology, boundaries, tradeoffs, or decision-record-worthy choices are ambiguous. |\n | `zoom-out` | The agent lacks an architectural map of the relevant code area. |\n | `to-prd` | Settled Goal context must become durable product/engineering intent. |\n | `to-issues` | A Goal, PRD, or plan must become independently grabbable vertical-slice issues. |\n@@ -348,7 +352,7 @@\n - Three or more new files.\n - Any high-risk or critical label.\n \n-When the architecture gate triggers, run `grill-with-docs` or `improve-codebase-architecture` as appropriate and record durable decisions in an ADR when needed.\n+When the architecture gate triggers, run `grill-with-docs` or `improve-codebase-architecture` as appropriate and record durable decisions in a Shiki SADR or target ADR as applicable.\n \n ## Engineering Discipline\n \ndiff --git a/CLAUDE.md b/CLAUDE.md\nindex 98a932e7841a632866fcd53b0bad0e3af3711d3d..98a31399404d6fb044b4fd493f4459ac87f439de\n--- a/CLAUDE.md\n+++ b/CLAUDE.md\n@@ -14,6 +14,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n ## Claude Role\n \n Default to planner, default implementer, reviewer, coordinator, documentation editor, CCA-style completion judge, and final-judgment assistant.\n@@ -57,7 +60,7 @@\n At the start of a Shiki session:\n \n 1. Read `AGENTS.md`.\n-2. Read `CONTEXT.md` and relevant ADRs.\n+2. Read `CONTEXT.md` and relevant Shiki SADRs and target ADRs.\n 3. Read active `.shiki/` Goal/task/ledger state when present.\n 4. Read active distilled rules from `.shiki/memories/` (status `distilled`, `active: true`, not revoked or superseded) for applicable guidance; the same rules are injected deterministically into task handoffs as the `## Distilled Rules` section (\u00a73.5).\n 5. Read the GitHub Issue or PR when the task is GitHub-backed.\n@@ -74,7 +77,7 @@\n - Define the Goal, completion conditions, non-goals, and risk level.\n - Treat `grill-with-docs` as the default planning entry point for non-trivial Goals.\n - Challenge domain terms against `CONTEXT.md`.\n-- Challenge hard-to-reverse tradeoffs against ADRs.\n+- Challenge hard-to-reverse tradeoffs against SADRs and target ADRs.\n - Explore code instead of asking when code can answer the question.\n - Run Context & Impact before implementation planning.\n - Produce a Task DAG, not an unordered checklist.\n@@ -94,7 +97,7 @@\n - For each question, provide your recommended answer.\n - Resolve dependency decisions before downstream design questions.\n - Update `CONTEXT.md` inline when terminology crystallizes.\n-- Offer ADRs only when the decision is hard to reverse, surprising without context, and a real tradeoff.\n+- Offer a SADR or target ADR only when the decision is hard to reverse, surprising without context, and a real tradeoff.\n - Convert unresolved questions into HITL blockers rather than allowing Codex to guess.\n \n ## PRD and Issues Standard\n@@ -103,7 +106,7 @@\n \n - Use the project's domain glossary vocabulary.\n - Avoid volatile file paths unless they are essential.\n-- Include implementation decisions, testing decisions, out-of-scope items, and relevant ADR/domain links.\n+- Include implementation decisions, testing decisions, out-of-scope items, and relevant SADR/target-ADR/domain links.\n - Identify deep-module opportunities and testable interfaces.\n \n When creating issues:\n@@ -121,7 +124,7 @@\n - Branch/worktree target.\n - Scope and non-goals.\n - Dependencies and locks.\n-- Relevant docs, ADRs, modules, and tests.\n+- Relevant docs, SADRs, target ADRs, modules, and tests.\n - Required skill invocations.\n - TDD expectations.\n - Pre-PR `code-review` gate expectations.\n@@ -152,7 +155,7 @@\n \n - Treat yourself as a judge, not implementer.\n - Do not write production code.\n-- Read `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant ADRs, PRD, task issue, PR body, diff, checks, reviews, labels, and `.shiki/` state.\n+- Read `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant SADRs and target ADRs, PRD, task issue, PR body, diff, checks, reviews, labels, and `.shiki/` state.\n - Evaluate every applicable checklist in `docs/agents/checklists.md`.\n - Map each acceptance criterion to durable evidence.\n - Distinguish implementation failure from missing evidence.\ndiff --git a/CONTEXT.md b/CONTEXT.md\nindex e61a9be06a0cce0d026484083a657bde2067e234..237ee8aa3ea8fab334ece49768bc5ed3b5af0974\n--- a/CONTEXT.md\n+++ b/CONTEXT.md\n@@ -25,7 +25,7 @@\n _Avoid_: jumping directly into code\n \n **grill-with-docs**:\n-A planning skill that challenges a plan against domain language, ADRs, code reality, and concrete edge scenarios. It resolves design-tree questions before PRD/issues. It runs inside Requirements Definition.\n+A planning skill that challenges a plan against domain language, Shiki SADRs, target ADRs, code reality, and concrete edge scenarios. It resolves design-tree questions before PRD/issues. It runs inside Requirements Definition.\n _Avoid_: generic brainstorming, silent assumptions\n \n **Requirements Definition**:\ndiff --git a/CONTRIBUTING.md b/CONTRIBUTING.md\nindex c6d9b656e73abb8a55e5f660123e03153dcb3e6d..9e37a8fbf668db5b22461aaae5548b8f1354394c\n--- a/CONTRIBUTING.md\n+++ b/CONTRIBUTING.md\n@@ -17,7 +17,7 @@\n 1. **Goal** \u2014 start from a user-approved Goal with an outcome, completion\n    conditions, non-goals, risk level, and success signals.\n 2. **grill-with-docs** \u2014 for any non-trivial change, challenge the plan against\n-   domain language, ADRs, and code reality before writing a PRD.\n+   domain language, Shiki SADRs, target ADRs, and code reality before writing a PRD.\n 3. **PRD** \u2014 capture settled product/engineering intent (`to-prd`).\n 4. **Spec Freeze** \u2014 the operator explicitly approves the PRD/requirements;\n    the plan records a `spec_freeze` block and no plan runs without it\n@@ -94,7 +94,7 @@\n   entries.\n - Do not hand-edit `.shiki/goals/*.json` or `.shiki/manifest.json` in a task PR.\n - Conversation state is not durable truth. If a decision matters, record it in\n-  GitHub, `.shiki/`, `CONTEXT.md`, or an ADR.\n+  GitHub, `.shiki/`, `CONTEXT.md`, a Shiki SADR, or a target ADR.\n \n ## Architecture Gate\n \n@@ -103,7 +103,7 @@\n or CI policy, package structure, three or more structural files, more than 100\n net new lines in a core module, three or more new files, or any high-risk label.\n When triggered, run `grill-with-docs` or `improve-codebase-architecture` and\n-record durable decisions in an ADR when needed (see `docs/adr/`).\n+record durable decisions in a Shiki SADR or target ADR as applicable (see `docs/adr/`).\n \n ## Safety\n \ndiff --git a/SYSTEM_PROMPT.md b/SYSTEM_PROMPT.md\nindex aaaea45e3a896c3a44ba89eec8bb84325ecf6c62..da009c86a88285269dee0155e58e550c6d744ea9\n--- a/SYSTEM_PROMPT.md\n+++ b/SYSTEM_PROMPT.md\n@@ -30,6 +30,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n Use the canonical source-of-truth order above. Runtime-specific reading orders\n are operational guidance only and must not override or reorder it. The current\n conversation is non-durable operator input.\n@@ -48,14 +51,14 @@\n \n A runtime may only do work that is explicitly assigned by the Goal, task, PR, or MergeGate decision.\n \n-Do not assume `openai/codex-action`, `OPENAI_API_KEY`, or API-key based Codex automation in the default Shiki path. API-key runners are explicit target-repo extensions and require their own ADR.\n+Do not assume `openai/codex-action`, `OPENAI_API_KEY`, or API-key based Codex automation in the default Shiki path. API-key runners are explicit target-repo extensions and require their own target ADR.\n \n ## 4. Non-negotiable Shiki flow\n \n For any non-trivial Goal, use this flow:\n \n 1. **Goal Seek**: define outcome, non-goals, risk level, completion criteria, and evidence requirements.\n-2. **grill-with-docs**: challenge the plan against `CONTEXT.md`, ADRs, code reality, terminology, boundaries, and edge scenarios. Ask one question at a time when the operator is available. If code can answer a question, inspect code instead of asking.\n+2. **grill-with-docs**: challenge the plan against `CONTEXT.md`, Shiki SADRs, target ADRs, code reality, terminology, boundaries, and edge scenarios. Ask one question at a time when the operator is available. If code can answer a question, inspect code instead of asking.\n 3. **Context & Impact**: identify modules, interfaces, seams, callers, dependencies, locks, risk, and verification surfaces.\n 4. **to-prd + Spec Freeze**: turn settled context into a PRD using domain vocabulary and testing decisions. The operator's explicit approval of the PRD is Spec Freeze, recorded as the plan's `spec_freeze` block; plans without it do not run. Steps 1-4 form Requirements Definition.\n 5. **to-issues**: decompose the PRD into independently grabbable vertical-slice issues. Prefer AFK slices; mark HITL slices when judgment is still required.\n@@ -75,7 +78,7 @@\n Before making any material change:\n \n 1. Read `AGENTS.md` and any runtime-specific wrapper such as `CLAUDE.md`.\n-2. Read `CONTEXT.md` and relevant ADRs.\n+2. Read `CONTEXT.md` and relevant SADRs and target ADRs.\n 3. Read the GitHub Issue / PR and `.shiki/` state for the active Goal or task.\n 4. Identify your role: Planner, Implementer, Reviewer, Completion Check Agent, Repairer, or Guardian-assist.\n 5. Identify Goal id, task id, branch, PR, dependencies, locks, risk level, required skills, and required evidence.\n@@ -103,7 +106,7 @@\n \n At minimum, identify:\n \n-- Relevant documents, ADRs, domain terms, and prior decisions.\n+- Relevant documents, SADRs, target ADRs, domain terms, and prior decisions.\n - Relevant modules, interfaces, seams, callers, owners, and tests.\n - Dependency relationships between tasks.\n - Candidate file locks and lock conflicts.\n@@ -152,7 +155,7 @@\n \n CCA must read:\n \n-- `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant ADRs.\n+- `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant SADRs and target ADRs.\n - Parent Goal issue, PRD issue, task issue, comments, and labels.\n - PR body, diff, commits, changed files, reviews, CI checks, and artifacts available to the workflow.\n - `.shiki/` task contract, locks, ledger entries, CCA checklist profile, and prior repair packets.\n@@ -214,7 +217,7 @@\n - Three or more new files.\n - Any high-risk or critical label.\n \n-When the architecture gate triggers, run `grill-with-docs` or `improve-codebase-architecture` as appropriate and record the decision in an ADR if it is hard to reverse or likely to be re-litigated.\n+When the architecture gate triggers, run `grill-with-docs` or `improve-codebase-architecture` as appropriate and record the decision in a Shiki SADR or target ADR as applicable if it is hard to reverse or likely to be re-litigated.\n \n ## 14. Skill Gate\n \n@@ -223,7 +226,7 @@\n Use these skills:\n \n - `setup-matt-pocock-skills`: before first use of the engineering skill set, or when issue tracker, triage labels, or domain docs are missing.\n-- `grill-with-docs`: default for non-trivial Goals before PRD/issues; required for ambiguous plans, terminology, boundaries, ADR-worthy tradeoffs, or design-tree decisions.\n+- `grill-with-docs`: default for non-trivial Goals before PRD/issues; required for ambiguous plans, terminology, boundaries, decision-record-worthy tradeoffs, or design-tree decisions.\n - `zoom-out`: unfamiliar code area or missing architectural map.\n - `to-prd`: convert settled context into a PRD.\n - `to-issues`: break a PRD, plan, or Goal into independently grabbable vertical-slice issues.\ndiff --git a/docs/adr/0001-keep-shiki-as-platform-name.md b/docs/adr/SADR-0001-keep-shiki-as-platform-name.md\nindex 20a9637c3b9d8c97de7ba8db7cfae4d82d9a7dba..8b685439959aada1b221c112f0ac3c51ca8a1695\n--- a/docs/adr/0001-keep-shiki-as-platform-name.md\n+++ b/docs/adr/SADR-0001-keep-shiki-as-platform-name.md\n@@ -1,3 +1,3 @@\n-# Keep Shiki As The Platform Name\n+# SADR-0001: Keep Shiki As The Platform Name\n \n Shiki remains the product name for the GitHub-native agentic engineering control plane. We considered more descriptive names such as MergePilot, MergeGate, and Kiroku, but kept Shiki because it carries the existing goal-seeking framework identity while remaining independent of Hermes, Codex, Claude Code, or any single runtime.\ndiff --git a/docs/adr/0002-use-agents-as-shared-constitution.md b/docs/adr/SADR-0002-use-agents-as-shared-constitution.md\nindex 73de30ecb078120eac540d9bc51855e3cd48d0e3..c5486182bf06fca36ea508496b013c67302fd989\n--- a/docs/adr/0002-use-agents-as-shared-constitution.md\n+++ b/docs/adr/SADR-0002-use-agents-as-shared-constitution.md\n@@ -1,3 +1,3 @@\n-# Use AGENTS As The Shared Agent Constitution\n+# SADR-0002: Use AGENTS As The Shared Agent Constitution\n \n Shiki uses `AGENTS.md` as the runtime-neutral constitution and `CLAUDE.md` as a thin Claude Code wrapper. This keeps Codex, Claude Code, Hermes Runner, and future Agent Runtimes aligned on the same GitHub-first MergeGate rules while still allowing runtime-specific instructions where needed.\ndiff --git a/docs/adr/0003-require-skill-gate-for-mergegate-readiness.md b/docs/adr/SADR-0003-require-skill-gate-for-mergegate-readiness.md\nindex 424be0c9af4c8f0136f1fde68a468c47ff84e764..7fc337987c7df9792f75f181dfcbed75fe5ffff2\n--- a/docs/adr/0003-require-skill-gate-for-mergegate-readiness.md\n+++ b/docs/adr/SADR-0003-require-skill-gate-for-mergegate-readiness.md\n@@ -1,3 +1,3 @@\n-# Require Skill Gate For MergeGate Readiness\n+# SADR-0003: Require Skill Gate For MergeGate Readiness\n \n Shiki requires relevant engineering skills to be invoked before execution when their triggers apply. This makes skills part of MergeGate readiness rather than optional prompt style, because Shiki depends on repeatable planning, issue decomposition, TDD, diagnosis, triage, and architecture review before autonomous or parallel agent work can be trusted.\ndiff --git a/docs/adr/0004-use-github-first-with-shiki-mirror.md b/docs/adr/SADR-0004-use-github-first-with-shiki-mirror.md\nindex d0a3a9174dc646a945a9d325954ffaf00c352040..a79c54ae9ee8eed2a0524192b864da4e22a92d3f\n--- a/docs/adr/0004-use-github-first-with-shiki-mirror.md\n+++ b/docs/adr/SADR-0004-use-github-first-with-shiki-mirror.md\n@@ -1,4 +1,4 @@\n-# ADR 0004: Use GitHub First With A Repository-Local Shiki Mirror\n+# SADR-0004: Use GitHub First With A Repository-Local Shiki Mirror\n \n ## Status\n \ndiff --git a/docs/adr/0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md b/docs/adr/SADR-0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\nindex b339baf49b89d9290e79c3d51b36e7b2bde3bd95..0f3c3eb9afccdb3403ce978e21d04fe12e495da0\n--- a/docs/adr/0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\n+++ b/docs/adr/SADR-0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\n@@ -1,8 +1,8 @@\n-# ADR 0005: Use Codex As Front And Claude Code Action As The GitHub Runtime\n+# SADR-0005: Use Codex As Front And Claude Code Action As The GitHub Runtime\n \n ## Status\n \n-Accepted (implementer default superseded by ADR 0008; the auth model and the\n+Accepted (implementer default superseded by SADR-0008; the auth model and the\n Claude Code Action GitHub runtime decision remain in force)\n \n ## Context\n@@ -26,4 +26,4 @@\n - Shiki avoids requiring `OPENAI_API_KEY` for the default workflow.\n - Codex work happens through the user's authenticated Codex front surface, not as a hidden backend Action.\n - Claude Code Action is configured for OAuth-token based subscription usage by default.\n-- API-key based automation remains possible only as an explicit target-repo extension with its own ADR.\n+- API-key based automation remains possible only as an explicit target-repo extension with its own target ADR.\ndiff --git a/docs/adr/0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md b/docs/adr/SADR-0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\nindex 4bcae9d1e52f0a37b8e9fbafa5cd14b6ee1993bd..7a38538a6ae9929567f0ade67e7ea6a60d0b3f7e\n--- a/docs/adr/0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\n+++ b/docs/adr/SADR-0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\n@@ -1,4 +1,4 @@\n-# ADR 0006: Separate Codex Implementation, CCA Completion, And MergeGate Authorization\n+# SADR-0006: Separate Codex Implementation, CCA Completion, And MergeGate Authorization\n \n ## Status\n \ndiff --git a/docs/adr/0007-packaging-and-release.md b/docs/adr/SADR-0007-packaging-and-release.md\nindex 79b84ddb97167372b44641e5b7860e400ce80822..bb445a2e2e856d9d1f5fdbe35de8d4e8cec0fb32\n--- a/docs/adr/0007-packaging-and-release.md\n+++ b/docs/adr/SADR-0007-packaging-and-release.md\n@@ -1,4 +1,4 @@\n-# ADR 0007: Packaging And Release For Shiki\n+# SADR-0007: Packaging And Release For Shiki\n \n ## Status\n \n@@ -25,7 +25,7 @@\n   not introduce third-party runtime dependencies or break the shim.\n - Releases must run inside the operator's subscription-authenticated toolchain\n   and must not require extra secrets beyond the built-in `GITHUB_TOKEN`\n-  (consistent with ADR 0005's no-API-key-by-default stance).\n+  (consistent with SADR-0005's no-API-key-by-default stance).\n - `.shiki/manifest.json` already encodes the install surface (`install.include`,\n   `install.create_directories`, `install.exclude_from_commit`), and it must not\n   be mutated as part of this change.\n@@ -34,7 +34,7 @@\n \n This is a hard-to-reverse platform decision (it sets the version source of\n truth, the release trigger, and the public install contract), so it is recorded\n-as an ADR.\n+as a SADR.\n \n ## Decision\n \n@@ -104,7 +104,7 @@\n 3. **Publishing to PyPI.** Rejected for now: Shiki is distributed as a\n    repository template + git-installable CLI, not a PyPI library; PyPI would add\n    credentials/secrets and a different versioning contract. Can be added later as\n-   its own ADR if a packaged distribution is needed.\n+   its own SADR if a packaged distribution is needed.\n 4. **Dynamic version (read `VERSION` via a build backend `dynamic` field).**\n    Rejected to keep `pyproject.toml` backend-agnostic and dependency-free; the\n    small duplication between `VERSION` and `pyproject.toml` is guarded by the\n@@ -123,4 +123,4 @@\n   releasing versions the whole platform but ships only `install.include` to\n   targets.\n - Future PyPI distribution or a console entry point are deferred and would each\n-  warrant their own ADR.\n+  warrant their own SADR.\ndiff --git a/docs/adr/0008-use-claude-code-as-default-implementer.md b/docs/adr/SADR-0008-use-claude-code-as-default-implementer.md\nindex dedf4510b7c7799521422f622029563d373db031..35c2625d908167fbc58855b5b63e283805f167e8\n--- a/docs/adr/0008-use-claude-code-as-default-implementer.md\n+++ b/docs/adr/SADR-0008-use-claude-code-as-default-implementer.md\n@@ -1,12 +1,12 @@\n-# ADR 0008: Use Claude Code As The Default Implementer Runtime\n+# SADR-0008: Use Claude Code As The Default Implementer Runtime\n \n ## Status\n \n-Proposed (supersedes ADR 0005 for the implementer default; keeps its auth model)\n+Proposed (supersedes SADR-0005 for the implementer default; keeps its auth model)\n \n ## Context\n \n-ADR 0005 made Codex Front the default implementation runtime, with Claude Code\n+SADR-0005 made Codex Front the default implementation runtime, with Claude Code\n restricted to planner and reviewer roles by the runtime registry, and the only\n autonomous implementation adapter was `shiki runner codex`.\n \n@@ -18,13 +18,13 @@\n - The platform's planning, review, and CCA layers already run on Claude Code,\n   and Claude Code's native capabilities (the built-in `/code-review` skill and\n   the Workflow multi-agent orchestration tool) have no Codex equivalent.\n-- The Goal lifecycle redesign (ADR 0009) requires an autonomous post-freeze\n+- The Goal lifecycle redesign (SADR-0009) requires an autonomous post-freeze\n   loop driven from the operator's Claude Code session, including bounded\n   repair dispatch, which must not depend on a runtime that cannot be invoked\n   unattended.\n \n The constitution claims Shiki is runtime-agnostic (\"not a Claude-only\n-workflow\", ADR 0002). Any change to the implementer default must preserve the\n+workflow\", SADR-0002). Any change to the implementer default must preserve the\n registry-based runtime model rather than hardcoding one vendor.\n \n ## Decision\n@@ -39,7 +39,7 @@\n   the task to review or repair-needed.\n - Keep Codex registered as an optional implementer runtime. `shiki runner\n   codex`, the Codex handoff templates, and the ChatGPT OAuth auth model from\n-  ADR 0005 remain supported for tasks explicitly assigned to `codex`.\n+  SADR-0005 remain supported for tasks explicitly assigned to `codex`.\n - Commit to mandating two Claude-native capabilities as loop contract in\n   follow-up Goals (status: the code-review gate was delivered by Goal G-B \u2014\n   skill registry, checklists item PR-12, default required_skills \u2014 while the\n@@ -60,7 +60,7 @@\n \n Out of scope: removing Codex support, changing the CCA runtime (Claude Code\n Action with `CLAUDE_CODE_OAUTH_TOKEN` stays), and API-key based automation\n-(still requires its own ADR per ADR 0005).\n+(still requires its own target ADR per SADR-0005).\n \n ## Consequences\n \n@@ -100,7 +100,7 @@\n   harness rejection of direct `codex exec` is outside Shiki's control, and the\n   plan's mandatory native capabilities cannot run on Codex at all.\n - **Remove Codex entirely**: rejected; it abandons the runtime-agnostic\n-  constitution (ADR 0002) for no operational gain and forecloses\n+  constitution (SADR-0002) for no operational gain and forecloses\n   cross-runtime adversarial review.\n - **GitHub Actions cloud implementation via claude-code-action**: rejected as\n   default; durable evidence is automatic but token cost, Node 24 workflow\ndiff --git a/docs/adr/0009-requirements-definition-and-spec-freeze.md b/docs/adr/SADR-0009-requirements-definition-and-spec-freeze.md\nindex 29b3f7871bef9203a8b4a831a65c39eb2f506e42..52e7dd69ad0aa4efe0b68c62678abbf7df02aaec\n--- a/docs/adr/0009-requirements-definition-and-spec-freeze.md\n+++ b/docs/adr/SADR-0009-requirements-definition-and-spec-freeze.md\n@@ -1,4 +1,4 @@\n-# ADR 0009: Merge Grill And Planning Into Requirements Definition With Spec Freeze\n+# SADR-0009: Merge Grill And Planning Into Requirements Definition With Spec Freeze\n \n ## Status\n \ndiff --git a/docs/adr/0010-external-ai-guardian-review.md b/docs/adr/SADR-0010-external-ai-guardian-review.md\nindex e6108bbe8bb4e73e0ff4684c8f78001a80438c4e..f3510b145664298adafb151b6f0db51b972f01ec\n--- a/docs/adr/0010-external-ai-guardian-review.md\n+++ b/docs/adr/SADR-0010-external-ai-guardian-review.md\n@@ -1,4 +1,4 @@\n-# ADR 0010: External AI Guardian Review As A First-Class Approval Authority\n+# SADR-0010: External AI Guardian Review As A First-Class Approval Authority\n \n ## Status\n \n@@ -8,7 +8,7 @@\n \n Shiki is intended to be an autonomous, recursive, self-improving agent\n framework: authority-in-the-loop, not human-in-the-loop. Until now the Guardian\n-approval required for high/critical-risk merges (guardian-policy.json, ADR\n+approval required for high/critical-risk merges (guardian-policy.json, SADR\n governance) recognized only human authority sources \u2014 a GitHub review approval,\n the `guardian:approved` label applied by a configured Guardian, or a comment\n carrying `Guardian approval granted` plus the current head SHA.\n@@ -89,7 +89,7 @@\n   autonomous design the operator intends and produced the impersonation\n   incident.\n - **Record AI approval as an operator comment**: rejected as forgery of human\n-  authority \u2014 the exact failure this ADR corrects.\n+  authority \u2014 the exact failure this SADR corrects.\n - **Deliver the artifact as a committed `.shiki/` file**: rejected; a committed\n   approval inside the PR being approved is forgeable by anyone who can push.\n - **A dedicated reviewer bot identity instead of a relayed comment**: deferred;\ndiff --git a/docs/adr/0011-autonomous-quality-gate-evidence.md b/docs/adr/SADR-0011-autonomous-quality-gate-evidence.md\nindex bca0246c5d46e44afbde15486a75eec425fc02f5..9c93eac16d8c3a6d4b11bd7224e3955102cd7db4\n--- a/docs/adr/0011-autonomous-quality-gate-evidence.md\n+++ b/docs/adr/SADR-0011-autonomous-quality-gate-evidence.md\n@@ -1,4 +1,4 @@\n-# ADR 0011: Autonomous Quality-Gate Evidence \u2014 Deterministic Facts And An Independent Verifier\n+# SADR-0011: Autonomous Quality-Gate Evidence \u2014 Deterministic Facts And An Independent Verifier\n \n ## Status\n \ndiff --git a/docs/adr/0012-autonomous-loop-closeout-to-main.md b/docs/adr/SADR-0012-autonomous-loop-closeout-to-main.md\nindex 637fad6dde4989c9c2bf0f81f676d8d29991123c..ec7d85f81030a676bcbba28b43eb365422a36eac\n--- a/docs/adr/0012-autonomous-loop-closeout-to-main.md\n+++ b/docs/adr/SADR-0012-autonomous-loop-closeout-to-main.md\n@@ -1,4 +1,4 @@\n-# 0012 \u2014 Autonomous loop pushes goal completion to main via a closeout PR\n+# SADR-0012 \u2014 Autonomous loop pushes goal completion to main via a closeout PR\n \n - Status: accepted\n - Date: 2026-06-18\ndiff --git a/docs/adr/0013-bootstrap-grants-actions-approve-pull-requests.md b/docs/adr/SADR-0013-bootstrap-grants-actions-approve-pull-requests.md\nindex b6c69d64f1c5ced346b88d6859586715b3b5bd0e..cf48ff4a6716267f539ca58edd1a852e7c7a2d64\n--- a/docs/adr/0013-bootstrap-grants-actions-approve-pull-requests.md\n+++ b/docs/adr/SADR-0013-bootstrap-grants-actions-approve-pull-requests.md\n@@ -1,4 +1,4 @@\n-# ADR 0013: Bootstrap Grants GitHub Actions Permission To Create And Approve Pull Requests\n+# SADR-0013: Bootstrap Grants GitHub Actions Permission To Create And Approve Pull Requests\n \n ## Status\n \n@@ -31,7 +31,7 @@\n security tradeoff: it lets repository automation satisfy a human-review gate. In\n a multi-maintainer repo that could let a compromised or buggy workflow\n self-approve changes. So whether bootstrap turns it on by default \u2014 and with\n-what default token scope \u2014 is an ADR-worthy decision, not an implementation\n+what default token scope \u2014 is a SADR-worthy decision, not an implementation\n detail.\n \n ## Decision\n@@ -48,7 +48,7 @@\n Specifically:\n \n - **`can_approve_pull_request_reviews` defaults to `true`.** Shiki's default\n-  posture is solo/AFK autonomous operation (ADR 0008/0009/0012), where the CCA\n+  posture is solo/AFK autonomous operation (SADR-0008/0009/0012), where the CCA\n   Review Bridge is the only path that can satisfy `required_review: true` after\n   the CCA and Guardian gates have already passed. The default makes a\n   freshly-bootstrapped repo able to run the autonomous loop end to end without a\ndiff --git a/docs/adr/0014-codex-app-external-ai-guardian-ui-adapter.md b/docs/adr/SADR-0014-codex-app-external-ai-guardian-ui-adapter.md\nindex 238db06279334caa341c117362df9271382a7fcd..65f12e9939321e1b3e389a34c822fc1549e5155d\n--- a/docs/adr/0014-codex-app-external-ai-guardian-ui-adapter.md\n+++ b/docs/adr/SADR-0014-codex-app-external-ai-guardian-ui-adapter.md\n@@ -1,4 +1,4 @@\n-# ADR 0014: Use Codex App As The External AI Guardian UI Adapter\n+# SADR-0014: Use Codex App As The External AI Guardian UI Adapter\n \n ## Status\n \ndiff --git a/docs/adr/0015-contract-approval-before-dispatch.md b/docs/adr/SADR-0015-contract-approval-before-dispatch.md\nindex fad6c656741a20a7684ba16080a7db159f63a52f..6061271a46d3f1d6ae7174477de3fb75673123a6\n--- a/docs/adr/0015-contract-approval-before-dispatch.md\n+++ b/docs/adr/SADR-0015-contract-approval-before-dispatch.md\n@@ -1,4 +1,4 @@\n-# ADR 0015: Approve The Task Contract Before Dispatch, Not The Diff After It\n+# SADR-0015: Approve The Task Contract Before Dispatch, Not The Diff After It\n \n ## Status\n \n@@ -9,7 +9,7 @@\n Guardian approval for high/critical risk is expressible only on a pull request.\n Every approval source in `guardian-policy.json` \u2014 GitHub review, the\n `guardian:approved` label, the `Guardian approval granted` comment, and the\n-`external_ai_guardian_review` artifact (ADR 0010/0014) \u2014 is bound to a PR, and\n+`external_ai_guardian_review` artifact (SADR-0010/0014) \u2014 is bound to a PR, and\n three of the four require the current head SHA. Approval therefore cannot exist\n before an implementation PR exists.\n \n@@ -81,7 +81,7 @@\n An envelope breach must surface only on the non-repairable policy gate, never on\n the repairable `MergeGate metadata check`. Otherwise the Goal Loop routes it to\n `dispatch_repair` \u2014 instructing a runner to make the breach go away \u2014 which is\n-exactly what this ADR forbids.\n+exactly what this SADR forbids.\n \n **Contract immutability.** After registration, an implementation PR must not\n change the governance fields of its own task contract (`scope`, `non_goals`,\n@@ -116,7 +116,7 @@\n fails any verdict that is not `complete`, and the signal the CCA prompt reads is\n computed from `evaluate_guardian_approval` alone. Without the OR-merge at the\n signal, a contract-approved PR still returns `needs_guardian`, the CCA job fails,\n-and the MergeGate policy check never runs \u2014 the gate this ADR changes would never\n+and the MergeGate policy check never runs \u2014 the gate this SADR changes would never\n be reached. The CCA *prompt* needs no change: a `contract_approval` entry in the\n signal's existing `sources` array satisfies its current wording.\n \n@@ -130,7 +130,7 @@\n written, and an approved contract lets its implementation PRs merge through the\n same automatic path as low/medium risk. The wasted implement-then-block cycle\n disappears. Guardian review shifts from reading diffs to reading contracts, which\n-is both earlier and smaller; the external AI Guardian path (ADR 0010/0014) works\n+is both earlier and smaller; the external AI Guardian path (SADR-0010/0014) works\n on contracts with no change.\n \n We accept that the Guardian authorizes a class of change without seeing its\n@@ -146,7 +146,7 @@\n adversarial tests for the new approval path equivalent in rigor to\n `test_shiki_governance_evidence.sh`.\n \n-Bootstrap costs, which apply only while this ADR is being implemented. The tasks\n+Bootstrap costs, which apply only while this SADR is being implemented. The tasks\n that build Contract Approval merge under the existing rules, so the ones\n dispatched after the pre-dispatch gate lands but before the approval path lands\n need a Guardian-approved Contract PR *and* a live Guardian approval on their\n@@ -210,7 +210,7 @@\n risk; they do not bound authorized work.\n \n **Auto-escalate risk on envelope breach instead of stopping.** Returns the\n-workflow to a diff-time Guardian decision \u2014 the exact behavior this ADR removes.\n+workflow to a diff-time Guardian decision \u2014 the exact behavior this SADR removes.\n \n **Resolve governance fields from the base snapshot only.** Rejected against the\n registration flow: implementation PRs add their own contract, so the base has no\ndiff --git a/docs/adr/0016-locks-bound-product-paths.md b/docs/adr/SADR-0016-locks-bound-product-paths.md\nindex b5e850f3f1f7f4ef6e7f9d1763a29a51bd300c45..46b40a9b99a7aae644c02bb458f5473ff0a90ce2\n--- a/docs/adr/0016-locks-bound-product-paths.md\n+++ b/docs/adr/SADR-0016-locks-bound-product-paths.md\n@@ -1,4 +1,4 @@\n-# ADR 0016: Locks bound product paths; the Shiki mirror is bound by record identity\n+# SADR-0016: Locks bound product paths; the Shiki mirror is bound by record identity\n \n ## Status\n \n@@ -6,8 +6,8 @@\n \n - Date: 2026-07-28\n - Deciders: operator (mizutani-140), Claude (planner)\n-- Related: ADR 0004 (GitHub-first with a `.shiki` mirror), ADR 0009 (Spec Freeze),\n-  ADR 0012 (autonomous loop closeout to main), ADR 0015 (Contract Approval before\n+- Related: SADR-0004 (GitHub-first with a `.shiki` mirror), SADR-0009 (Spec Freeze),\n+  SADR-0012 (autonomous loop closeout to main), SADR-0015 (Contract Approval before\n   dispatch), remediation goal task R-02, the live PR #179 deadlock.\n \n ## Context\n@@ -90,11 +90,11 @@\n files, documentation files and named source modules are nameable; runtime-\n allocated ledger, runner and report ids are not \u2014 and those live in `.shiki`,\n which locks no longer govern. Existing frozen contracts are brought into line by\n-a one-time Spec Amendment per plan, which must land before ADR 0015's contract\n+a one-time Spec Amendment per plan, which must land before SADR-0015's contract\n immutability makes `locks` unwritable from the implementation PR.\n \n **Lock state is resolved from the durable side.** MergeGate resolves the lock\n-registry, and ADR 0015's governance fields, from the base `.shiki` snapshot when\n+registry, and SADR-0015's governance fields, from the base `.shiki` snapshot when\n present, falling back to head only when absent. The `MergeGate metadata check`\n job is given `--base-shiki`, which it does not receive today. Locks are not\n derived from the live open-pull-request set: a pull request body is untrusted\n@@ -108,7 +108,7 @@\n Explicitly out of scope: moving MergeGate's contract resolution off the PR\n checkout wholesale; making the mirror a projection of GitHub state; introducing\n new lock kinds; moving ledger evidence out of the repository; and unwinding\n-ADR 0012's closeout pull request. Registration of multi-task goals is ADR 0015's\n+SADR-0012's closeout pull request. Registration of multi-task goals is SADR-0015's\n Contract PR and is not re-decided here.\n \n ## Consequences\n@@ -127,7 +127,7 @@\n \n Risks accepted. First, goal-scoped identity is only as strong as `goal_id`,\n which is resolved from the PR body and the head task file; the mitigation is to\n-add `goal_id` to ADR 0015's frozen governance field set, and until that lands a\n+add `goal_id` to SADR-0015's frozen governance field set, and until that lands a\n task file can name a foreign goal. Second, base-when-present resolution is\n unavailable on fork pull requests, where the head is contributor-controlled;\n fork pull requests already cannot complete a Shiki merge, and this decision does\n@@ -139,7 +139,7 @@\n path, not by implementation pull requests.\n \n New obligations: one Spec Amendment per frozen plan to rewrite `locks`, ordered\n-before ADR 0015 T1; extension of the adversarial suites in\n+before SADR-0015 T1; extension of the adversarial suites in\n `scripts/test_shiki_adversarial_state.sh` and\n `scripts/test_shiki_governance_evidence.sh` to cover the eleven newly-scoped\n directories; and updating `scripts/test_shiki_loop_lock_guard.sh`,\n@@ -151,32 +151,32 @@\n whether the loop's transitive sync of ledger `evidence` references can be\n deleted once the Contract PR carries its own source plan.\n \n-## Relationship to ADR 0004\n+## Relationship to SADR-0004\n \n-This ADR **clarifies something ADR 0004 left unspecified**. ADR 0004 decides that\n+This SADR **clarifies something SADR-0004 left unspecified**. SADR-0004 decides that\n GitHub is the operational source of truth and that each target repository keeps a\n `.shiki/` mirror. It says nothing about pull requests writing the mirror, does\n not name MergeGate, and does not make the mirror an input to a gate. Nothing in\n-ADR 0004's Decision or Consequences is amended, superseded or weakened: the\n+SADR-0004's Decision or Consequences is amended, superseded or weakened: the\n mirror still exists, still records the same artifacts, and pull request\n-boundaries remain the coordination surface. What this ADR settles is the question\n+boundaries remain the coordination surface. What this SADR settles is the question\n 0004 never reached \u2014 by what authority a pull request may write a mirror record \u2014\n and it answers it the way `.shiki/manifest.json` already declares\n (`pr_mutation: current-task-or-current-goal-only`).\n \n-It **amends ADR 0012**, whose Decision item 5 states that \"Loop-executed tasks\n+It **amends SADR-0012**, whose Decision item 5 states that \"Loop-executed tasks\n declare the `path:.shiki/**` lock, which covers every `.shiki` file the closeout\n stages ... so `files_outside_locks` is satisfied without per-file lock\n bookkeeping.\" That clause is replaced: the closeout's mirror writes are\n-authorized by record identity, not by a declared lock. ADR 0012's substantive\n+authorized by record identity, not by a declared lock. SADR-0012's substantive\n decision \u2014 that completion reaches `main` through a closeout pull request rather\n than local attestation \u2014 is preserved unchanged.\n \n-It is **complementary to ADR 0015**, not a substitute. ADR 0015 owns contract\n+It is **complementary to SADR-0015**, not a substitute. SADR-0015 owns contract\n immutability, the pre-dispatch gate, the Contract PR and the Scope Envelope. This\n-ADR owns what a lock means and who may write the mirror. The one ordering\n+SADR owns what a lock means and who may write the mirror. The one ordering\n constraint between them is that the lock-narrowing Spec Amendment must precede\n-ADR 0015 T1's freezing of the `locks` field.\n+SADR-0015 T1's freezing of the `locks` field.\n \n ## Alternatives Considered\n \n@@ -186,7 +186,7 @@\n the only writer that can register them may run. Migrating the Guardian signal to\n that store makes every registration PR fail closed to `needs_guardian` at every\n risk level, because the task file is by definition absent from the store, which\n-removes the low/medium autonomous path ADR 0008/0009 exists to provide. And it\n+removes the low/medium autonomous path SADR-0008/0009 exists to provide. And it\n leaves schemas, manifest, config, guardian policy, templates, migrations, plans,\n worktrees, runner records, runs and handoffs with no writer at all in a\n repository whose product is `.shiki`. Measured benefit on the frozen backlog:\n@@ -199,14 +199,14 @@\n namespace is not in fact ownership-partitioned \u2014 56 ledger entries carry no\n `task_id`, and one goal DAG is writable by 35 registered tasks. The correct\n expression of its insight is that locks do not govern the mirror at all, which is\n-what this ADR decides.\n+what this SADR decides.\n \n **Make the mirror a projection written only by a registration writer and a\n post-merge projection job.** Rejected. It requires a bypass actor with write\n access to the protected default branch; it makes its own master invariant\n readable from a manifest that pull requests may edit; it deletes both reconcile\n modes and leaves a corrupted mirror with no in-band repair path, contradicting\n-ADR 0004's consequence that mirror conflicts are repaired against GitHub state;\n+SADR-0004's consequence that mirror conflicts are repaired against GitHub state;\n and its content-derived ids are rejected by `validate_shiki`'s currently-green id\n pattern.\n \ndiff --git a/docs/adr/0017-bookkeeping-closeouts-do-not-inherit-risk.md b/docs/adr/SADR-0017-bookkeeping-closeouts-do-not-inherit-risk.md\nindex 554e3a0d526a72ca067108c5464cc68c3edcf174..b9676857a4713850e063af7c39c8e0b01ecb4c66\n--- a/docs/adr/0017-bookkeeping-closeouts-do-not-inherit-risk.md\n+++ b/docs/adr/SADR-0017-bookkeeping-closeouts-do-not-inherit-risk.md\n@@ -1,4 +1,4 @@\n-# ADR 0017: A Bookkeeping Closeout Does Not Inherit Its Task's Risk\n+# SADR-0017: A Bookkeeping Closeout Does Not Inherit Its Task's Risk\n \n ## Status\n \n@@ -6,7 +6,7 @@\n \n ## Context\n \n-ADR 0012 made the goal loop push a task's completion to the default branch through a\n+SADR-0012 made the goal loop push a task's completion to the default branch through a\n closeout pull request: after the implementation PR merges, the loop opens a second PR\n carrying `task.status = done`, `lock.state = released`, and \u2014 when the task completes\n its goal \u2014 `goal.status = complete` plus one scorecard report. Nothing else. The\n@@ -49,14 +49,14 @@\n 4. The task file's governance fields (`scope`, `non_goals`, `required_skills`,\n    `risk_level`, `locks`, `acceptance_checks`, `test_command`) are byte-identical to\n    the base snapshot. Only `status`, `expected_pr`, `closeout_pr`, `ledger_evidence`\n-   and `expected_branch` may differ \u2014 the ADR 0015 contract-immutability rule,\n+   and `expected_branch` may differ \u2014 the SADR-0015 contract-immutability rule,\n    unchanged.\n \n    `expected_branch` is in that set because a closeout is *by construction* on a\n    different branch from the implementation it closes, and the loop rewrites the\n    field accordingly. It is safe to let it differ because MergeGate independently\n    requires `expected_branch` to equal the PR's actual head ref, so a PR cannot set\n-   it to anything other than the branch it is really on. The first draft of this ADR\n+   it to anything other than the branch it is really on. The first draft of this SADR\n    omitted it, which made the exemption unreachable: every real closeout failed\n    condition 4 on a field the loop always changes. See PR #209.\n 5. The state transitions are exactly the terminal set: task `review` \u2192 `done`, lock\n@@ -113,12 +113,12 @@\n classification must be derived, not asserted.\n \n **Fold the closeout into the implementation PR** so no second PR exists. Rejected under\n-ADR 0012, which recorded why: a task file that is already `done` on the implementation\n+SADR-0012, which recorded why: a task file that is already `done` on the implementation\n branch short-circuits the loop's review \u2192 CCA \u2192 merge \u2192 done ordering into a local\n goal-completion before any merge happens.\n \n **Have the loop auto-merge closeouts with elevated credentials.** Rejected outright: that\n-is a runtime authoring its own approval, which ADR 0010 and the constitution's\n+is a runtime authoring its own approval, which SADR-0010 and the constitution's\n approval-record prohibition forbid. It would also be indistinguishable, in the evidence,\n from the forgery those rules exist to prevent.\n \ndiff --git a/docs/adr/0018-carried-guardian-approval-across-a-proven-base-sync.md b/docs/adr/SADR-0018-carried-guardian-approval-across-a-proven-base-sync.md\nindex 22358562da390ed8b45cd600da08a43d378b4680..e6e2907cefa776460af73ddf7d39db605f3c39b0\n--- a/docs/adr/0018-carried-guardian-approval-across-a-proven-base-sync.md\n+++ b/docs/adr/SADR-0018-carried-guardian-approval-across-a-proven-base-sync.md\n@@ -1,4 +1,4 @@\n-# ADR 0018: Carried Guardian Approval Across A Proven Base Sync\n+# SADR-0018: Carried Guardian Approval Across A Proven Base Sync\n \n ## Status\n \n@@ -34,7 +34,7 @@\n Guardian evaluator that consumes it (`shiki_guardian.evaluate_guardian_approval`,\n the `guardian_comment_carried` source) were built and tested in isolation. Neither\n does anything until a caller passes the flags that turn the carry on, so the\n-feature was invisible in CI. This ADR records the decision the two of them\n+feature was invisible in CI. This SADR records the decision the two of them\n implement, and this task wires it into the four CI invocations that judge Guardian\n approval.\n \ndiff --git a/docs/adr/README.md b/docs/adr/README.md\nindex c70e057a4520c36e042ab68c53cd4a21ef709b71..5da43856836e2ce6a150f8127766e2f88355ab4b\n--- a/docs/adr/README.md\n+++ b/docs/adr/README.md\n@@ -1,40 +1,41 @@\n-# Architecture Decision Records\n+# Decision Record Namespaces\n \n-This directory holds Shiki's Architecture Decision Records (ADRs): durable\n-records of hard-to-reverse platform decisions. ADRs are part of the Shiki source\n-of truth alongside GitHub state, the `.shiki/` mirror, and `CONTEXT.md`.\n+This directory holds two independent decision namespaces:\n \n-## How ADRs Work\n+- Shiki Architecture Decision Records (SADRs), named\n+  `SADR-NNNN-<short-slug>.md`, record hard-to-reverse Shiki platform decisions.\n+- Target Architecture Decision Records (ADRs), named\n+  `NNNN-<short-slug>.md`, belong to the target product.\n \n-- New ADRs are added as `docs/adr/NNNN-<short-slug>.md`, where `NNNN` is the next\n-  zero-padded sequence number.\n-- The **canonical list of ADRs is the set of files matching `docs/adr/*.md`** in\n-  this directory. Browse `docs/adr/*.md` for the authoritative, current set.\n-- Use [`template.md`](template.md) as the starting point for a new ADR.\n-- Record an ADR when a decision is hard to reverse, surprising without context,\n-  and a real tradeoff. Do not write an ADR for routine or easily reversible\n-  choices.\n+The same four-digit number may appear once in each namespace. A platform\n+`SADR-NNNN` citation resolves only to `SADR-NNNN-*.md`; a target `ADR NNNN`\n+citation resolves only to `NNNN-*.md`.\n \n-Using the `docs/adr/*.md` glob as the source of truth means other PRs can add an\n-ADR without editing this index file, avoiding merge contention on a\n-hand-maintained enumeration.\n+## How Decision Records Work\n \n-## Current ADRs (snapshot)\n-\n-The list below is a convenience snapshot and may lag behind the directory. When\n-in doubt, list `docs/adr/*.md`.\n+- New Shiki platform decisions use the next zero-padded SADR number.\n+- New target product decisions use the target's own ADR sequence.\n+- The canonical list is the files in `docs/adr/`; no hand-maintained index is\n+  authoritative.\n+- Use [`template.md`](template.md) as the starting point and choose the filename\n+  namespace that owns the decision.\n+- Record a decision when it is hard to reverse, surprising without context, and\n+  a real tradeoff. Do not write a decision record for routine or easily\n+  reversible choices.\n \n-- [0001 \u2014 Keep Shiki As The Platform Name](0001-keep-shiki-as-platform-name.md)\n-- [0002 \u2014 Use AGENTS As The Shared Agent Constitution](0002-use-agents-as-shared-constitution.md)\n-- [0003 \u2014 Require Skill Gate For MergeGate Readiness](0003-require-skill-gate-for-mergegate-readiness.md)\n-- [0004 \u2014 Use GitHub First With A Repository-Local Shiki Mirror](0004-use-github-first-with-shiki-mirror.md)\n-- [0005 \u2014 Use Codex As Front And Claude Code Action As The GitHub Runtime](0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md)\n-- [0006 \u2014 Separate Codex Implementation, CCA Completion, And MergeGate Authorization](0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md)\n+Using the directory as the source of truth lets independent changes add records\n+without contending on an enumerated index.\n \n-## Adding A New ADR\n+## Adding A Shiki SADR\n \n-1. Copy `template.md` to `docs/adr/NNNN-<short-slug>.md`.\n+1. Copy `template.md` to `docs/adr/SADR-NNNN-<short-slug>.md`.\n 2. Fill in the title, status, context, decision, and consequences.\n-3. Set the status to `Proposed`, then `Accepted` once the decision is approved.\n-4. Open a PR following `CONTRIBUTING.md`. You do not need to edit this index for\n-   the ADR to be discoverable via `docs/adr/*.md`.\n+3. Set the status to `Proposed`, then `Accepted` once the proper authority has\n+   approved the decision.\n+4. Open a PR following `CONTRIBUTING.md`.\n+\n+## Adding A Target ADR\n+\n+1. Copy `template.md` to `docs/adr/NNNN-<short-slug>.md` using the target's own\n+   sequence.\n+2. Cite it as `ADR NNNN`; do not prefix its filename with `SADR-`.\ndiff --git a/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md b/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\nnew file mode 100644\nindex 0000000000000000000000000000000000000000..7455ce1f3bed02ddddb851ac21a1d1a648c9aeba\n--- /dev/null\n+++ b/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\n@@ -0,0 +1,55 @@\n+# SADR-0019: Separate Shiki Platform SADRs From Target ADRs\n+\n+## Status\n+\n+Accepted\n+\n+## Context\n+\n+Shiki installs its platform decision records into `docs/adr/` in every target\n+repository. Shiki and its targets previously used the same numeric\n+`NNNN-<slug>.md` namespace. A target that already owned ADRs 0013 through 0018\n+therefore acquired two records for each number during installation, making an\n+`ADR NNNN` citation ambiguous. Renumbering target decisions would rewrite target\n+history, while renumbering Shiki decisions per target would destroy their stable\n+platform identity.\n+\n+Upgrades also need to remove the former Shiki-owned numeric paths without ever\n+mistaking a target-owned ADR for platform content. The install stamp already\n+records exact shipped paths and content digests, so it can prove ownership only\n+when both still match.\n+\n+## Decision\n+\n+Shiki platform decisions use the `SADR` namespace:\n+\n+- records are named `docs/adr/SADR-NNNN-<slug>.md`;\n+- live platform citations use `SADR-NNNN`;\n+- target product decisions retain `docs/adr/NNNN-<slug>.md` and `ADR NNNN`;\n+- the reference validator resolves the two namespaces independently, so the same\n+  four-digit number may exist once in each namespace.\n+\n+Before a forced upgrade writes anything, the installer inventories the exact\n+former numeric Shiki ADR paths. It may delete a legacy path only when the\n+existing install stamp contains that exact path and its recorded SHA-256 digest\n+matches the file currently on disk. All ownership blockers are reported\n+together and abort the upgrade before its first write. A non-forced install\n+never performs this cleanup.\n+\n+Historical `.shiki/` evidence remains unchanged because it records the namespace\n+that existed when that evidence was produced.\n+\n+## Consequences\n+\n+- Targets may use any ADR number without colliding with Shiki's platform SADRs.\n+- Fresh installs preserve target ADR bytes and install a stable SADR set.\n+- Forced upgrades fail closed when legacy ownership cannot be proven, and a\n+  successful upgrade refreshes the stamp without removed legacy paths.\n+- Current governance, packaging, workflow, prompt, script, and skill references\n+  must move with the platform records.\n+\n+## Alternatives Considered\n+\n+- Collision-only refusal leaves affected targets permanently unupgradable.\n+- Renumbering target ADRs mutates target-owned history.\n+- Per-target Shiki renumbering makes platform citations non-portable.\ndiff --git a/docs/adr/template.md b/docs/adr/template.md\nindex a92a97daddd0b00ab9dd626cc8d86942da2eafed..17dd79175b5fee2105935557f691b40cf9492942\n--- a/docs/adr/template.md\n+++ b/docs/adr/template.md\n@@ -1,14 +1,14 @@\n-# NNNN: <Short Decision Title>\n+# <SADR-NNNN or NNNN>: <Short Decision Title>\n \n ## Status\n \n-Proposed | Accepted | Superseded by ADR-NNNN\n+Proposed | Accepted | Superseded by SADR-NNNN or target ADR NNNN\n \n ## Context\n \n What is the situation, constraint, or force that requires a decision? Reference\n-relevant domain terms (`CONTEXT.md`), prior ADRs, and code reality. State the\n-problem, not the solution.\n+relevant domain terms (`CONTEXT.md`), prior Shiki SADRs or target ADRs, and code\n+reality. State the problem, not the solution.\n \n ## Decision\n \ndiff --git a/docs/agents/autonomous-loop-e2e-acceptance.md b/docs/agents/autonomous-loop-e2e-acceptance.md\nindex bbf84c6081987f159ac6ad9ecf3c82b7aa72e7e8..5c13c1d02a05a90b0dc46cbef7c95a9846d0c237\n--- a/docs/agents/autonomous-loop-e2e-acceptance.md\n+++ b/docs/agents/autonomous-loop-e2e-acceptance.md\n@@ -40,8 +40,8 @@\n 3. `gh auth status` is logged in with merge permission on the repo.\n 4. Branch protection + required checks are configured exactly as the goal-loop\n    required-check set expects (see `docs/agents/branch-protection-smoke.md`).\n-5. The repo `.shiki/policy` permits autonomous low/medium-risk merges (ADR\n-   0008/0009); the MergeGate policy check is the recorded-authority gate.\n+5. The repo `.shiki/policy` permits autonomous low/medium-risk merges\n+   (SADR-0008/0009); the MergeGate policy check is the recorded-authority gate.\n \n The operator's only actions are: register the task and start the loop. **No\n manual pre-clearing** means after `shiki loop run` starts, the operator does not\ndiff --git a/docs/agents/bootstrap-command.md b/docs/agents/bootstrap-command.md\nindex 05f7f203a52cf412b9f97084a6e131ed0f785afd..f7aa0b734dfebee4a3b139335b66ee3415f48713\n--- a/docs/agents/bootstrap-command.md\n+++ b/docs/agents/bootstrap-command.md\n@@ -189,7 +189,9 @@\n ```\n \n Use this only for tests, fixtures, or explicit local-only template inspection.\n-Use `--force` only when you intentionally want to overwrite existing target files.\n+Use `--force` only when you intentionally want to refresh the target template.\n+Legacy numeric Shiki decision cleanup is stamp-and-digest authorized before any\n+write; target ADR paths are not cleanup candidates.\n \n ## Slash Command\n \ndiff --git a/docs/agents/checklists.md b/docs/agents/checklists.md\nindex 4a7f03ade346f53a1c1d82981f3fd744f9ab4547..0c5ec6c53dc7197b7b7a7d18e667b325babd6c45\n--- a/docs/agents/checklists.md\n+++ b/docs/agents/checklists.md\n@@ -32,18 +32,18 @@\n | ID | Item | Blocking |\n | --- | --- | --- |\n | GD-01 | Plan was challenged against `CONTEXT.md`. | blocking for non-trivial Goals |\n-| GD-02 | Relevant ADRs were read or absence was recorded. | blocking for non-trivial Goals |\n+| GD-02 | Relevant SADRs and target ADRs were read or absence was recorded. | blocking for non-trivial Goals |\n | GD-03 | Ambiguous terms were resolved or converted to HITL blockers. | blocking |\n | GD-04 | Edge scenarios were considered. | advisory |\n | GD-05 | Code reality was checked when code could answer. | blocking when applicable |\n | GD-06 | New domain terms were added to `CONTEXT.md` when needed. | blocking when applicable |\n-| GD-07 | ADR was created or explicitly skipped using the ADR criteria. | blocking when applicable |\n+| GD-07 | A SADR or target ADR was created or explicitly skipped using the decision-record criteria. | blocking when applicable |\n \n ## CI \u2014 Context & Impact Checklist\n \n | ID | Item | Blocking |\n | --- | --- | --- |\n-| CI-01 | Relevant docs and ADRs identified. | blocking |\n+| CI-01 | Relevant docs, SADRs, and target ADRs identified. | blocking |\n | CI-02 | Relevant modules, interfaces, seams, callers, and tests identified. | blocking |\n | CI-03 | Dependencies and blocked-by relationships identified. | blocking |\n | CI-04 | Candidate locks identified. | blocking |\n@@ -62,7 +62,7 @@\n | PRD-04 | Implementation decisions are recorded without volatile code detail. | blocking |\n | PRD-05 | Testing decisions are recorded. | blocking |\n | PRD-06 | Out-of-scope items are explicit. | blocking |\n-| PRD-07 | Relevant glossary/ADR links are present when applicable. | blocking |\n+| PRD-07 | Relevant glossary/SADR/target-ADR links are present when applicable. | blocking |\n \n ## SF \u2014 Spec Freeze Checklist\n \n@@ -98,7 +98,7 @@\n | H-02 | Branch/worktree target is present. | blocking |\n | H-03 | Scope and non-goals are present. | blocking |\n | H-04 | Dependencies and locks are present. | blocking |\n-| H-05 | Relevant docs, ADRs, modules, and tests are referenced. | blocking |\n+| H-05 | Relevant docs, SADRs, target ADRs, modules, and tests are referenced. | blocking |\n | H-06 | Required skills are stated. | blocking |\n | H-07 | TDD expectations are stated. | blocking when behavior work |\n | H-08 | Acceptance criteria are included. | blocking |\ndiff --git a/docs/agents/codex-handoff.md b/docs/agents/codex-handoff.md\nindex ede468f47cb277f69a857e8c45df703e4ed0151d..6f97bdffb67e44d7edea2c71e01ea28933c4eb6c\n--- a/docs/agents/codex-handoff.md\n+++ b/docs/agents/codex-handoff.md\n@@ -46,7 +46,7 @@\n \n ## Context\n - Domain terms:\n-- Relevant ADRs:\n+- Relevant SADRs and target ADRs:\n - Relevant docs:\n - Relevant modules/interfaces/seams:\n - Similar tests/prior art:\ndiff --git a/docs/agents/completion-check-agent.md b/docs/agents/completion-check-agent.md\nindex 13e4c035db35025f4e3cdcc5adfb24314ac4d521..61bfa1218cf36ca605250c91919e620f33238aa0\n--- a/docs/agents/completion-check-agent.md\n+++ b/docs/agents/completion-check-agent.md\n@@ -32,7 +32,7 @@\n - `AGENTS.md`\n - `CLAUDE.md`\n - `CONTEXT.md`\n-- relevant ADRs\n+- relevant SADRs and target ADRs\n - parent Goal issue\n - PRD issue or document\n - task issue\ndiff --git a/docs/agents/decision-control.md b/docs/agents/decision-control.md\nindex 7d8ee5ab03a5ccbcbd04056ae61de03785a2a9c9..8c94ecda00fffaa0e92c325ab83aa9776bd1dac1\n--- a/docs/agents/decision-control.md\n+++ b/docs/agents/decision-control.md\n@@ -11,7 +11,7 @@\n | Decision | Owner | When | Required inputs | Output | Enforcement surface |\n | --- | --- | --- | --- | --- | --- |\n | Is the Goal clear enough to plan? | Planner + Guardian when needed | Goal intake | User request, existing docs, risk signals | Goal readiness verdict | Goal checklist, issue template, ledger |\n-| Must `grill-with-docs` run? | Planner / Skill Gate | Before PRD or issue split | Ambiguity, domain terms, ADR conflicts, unclear boundaries | Skill invocation or explicit skip rationale | Skill Gate checklist |\n+| Must `grill-with-docs` run? | Planner / Skill Gate | Before PRD or issue split | Ambiguity, domain terms, SADR/target-ADR conflicts, unclear boundaries | Skill invocation or explicit skip rationale | Skill Gate checklist |\n | Is a PRD required? | Planner | After Goal context stabilizes | Goal, `grill-with-docs` output, Context & Impact output | PRD required / not required | PRD checklist, CCA evidence check |\n | Can the spec be frozen? | Operator only | End of Requirements Definition, before issue split | PRD/requirements summary, scope inventory (SF-02), open-question list | `spec_freeze` block in the plan + ledger evidence | `require_grilled_plan` (plan ingest/run), SF checklist |\n | Can a frozen spec change? | Operator only (Spec Amendment) | When implementation contradicts the frozen spec | Contested decisions, scoped re-grill result | recorded amendment + re-stamped freeze | SF checklist, ledger evidence |\ndiff --git a/docs/agents/domain.md b/docs/agents/domain.md\nindex 258da8ea81e87ac0a6b0b5b5e5c2fde980eaabb7..d57b0cf17ff3e1fa9e6e5885372bf083d8295421\n--- a/docs/agents/domain.md\n+++ b/docs/agents/domain.md\n@@ -5,7 +5,9 @@\n ## Layout\n \n - `CONTEXT.md` defines Shiki language. Keep it implementation-free.\n-- `docs/adr/` records hard-to-reverse decisions.\n+- `docs/adr/SADR-NNNN-*.md` records Shiki platform decisions.\n+- `docs/adr/NNNN-*.md` records target product decisions without sharing the\n+  SADR namespace.\n - `AGENTS.md` is the runtime-neutral agent constitution.\n - `CLAUDE.md` is the Claude Code wrapper around `AGENTS.md`.\n - `.shiki/` is the Target Repository mirror for Goals, PRDs, plans, Task DAGs, contracts, locks, worktree records, ledger entries, CCA verdicts, repair packets, reports, and handoffs.\n@@ -15,6 +17,6 @@\n \n - Use Shiki terms exactly when they exist.\n - Add glossary terms to `CONTEXT.md` only when the term is part of the domain, not a generic programming concept.\n-- Create ADRs only for hard-to-reverse, surprising, tradeoff-based decisions.\n+- Create SADRs or target ADRs only for hard-to-reverse, surprising, tradeoff-based decisions.\n - Keep product-specific language in the Target Repository, not in Shiki Template docs.\n-- If an ADR conflicts with a proposed implementation, surface the conflict before editing.\n+- If a SADR or target ADR conflicts with a proposed implementation, surface the conflict before editing.\ndiff --git a/docs/agents/execution-control.md b/docs/agents/execution-control.md\nindex ad8ee3cdb36be56fedc1f46408ca92b387d74047..bc63c3a73661d12b567016ba299f1a6b66bf9088\n--- a/docs/agents/execution-control.md\n+++ b/docs/agents/execution-control.md\n@@ -63,7 +63,7 @@\n | Branch name | Orchestrator | Orchestrator | Before implementer dispatch | branch policy | branch ref in task/ledger |\n | Lock reservation | Context & Impact | MergeGate | Before dispatch and before merge | `.shiki/locks/`, labels, PR metadata | lock id, owner, scope |\n | CI profile | Planner | MergeGate | During issue triage, rechecked at PR | labels, `.shiki/policy.yaml`, workflow matrix | required check list |\n-| Required status checks | Repo maintainer / Guardian | GitHub branch protection / rulesets | Repository setup, updated via ADR | GitHub settings | branch/ruleset config, ADR |\n+| Required status checks | Repo maintainer / Guardian | GitHub branch protection / rulesets | Repository setup, updated via target ADR | GitHub settings | branch/ruleset config, target ADR |\n | CCA judgment | CCA | GitHub required check / MergeGate | Every non-draft PR update | GitHub Action check | structured CCA verdict |\n | Repair assignment | CCA | MergeGate | After failed CCA/CI/review | repair packet, issue/PR comment | repair id and attempt count |\n | Merge readiness | MergeGate | GitHub branch protection / Guardian if needed | After CCA complete | required checks, review rules, rulesets | mergegate check output |\ndiff --git a/docs/agents/guardian-policy.md b/docs/agents/guardian-policy.md\nindex 4147c7adf05a47adb4fd24ada1a1c2e1f2ba6d2d..a20d9cc6c00a569bb057b9ca8d6b51c7fc4a9847\n--- a/docs/agents/guardian-policy.md\n+++ b/docs/agents/guardian-policy.md\n@@ -98,8 +98,8 @@\n `origin/<default_branch>` and reuses each job's existing base fetch; a shallow\n (`--depth`) or second base fetch would make the proof unresolvable and silently\n disable the carry, so neither is permitted. CCA reads `guardian_comment_carried` as\n-a recorded authority in the deterministic signal and does not re-derive it. See ADR\n-0018 for the full decision, the rejected diff/patch-equality and path-disjointness\n+a recorded authority in the deterministic signal and does not re-derive it. See\n+SADR-0018 for the full decision, the rejected diff/patch-equality and path-disjointness\n alternatives, and the accepted residual.\n \n ## External AI Guardian Review (SADR-0010)\ndiff --git a/docs/agents/implementation-policy.md b/docs/agents/implementation-policy.md\nindex c754144a9fb5bf4a38aa82a60e1670c59e626b44..85c37281bcad196c6b7b02343faf00b03cd9c57a\n--- a/docs/agents/implementation-policy.md\n+++ b/docs/agents/implementation-policy.md\n@@ -31,10 +31,13 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n ## Non-Negotiables\n \n 1. **Planning precedes implementation.** Non-trivial Goals start with `grill-with-docs` and Context & Impact.\n-2. **Domain language is controlled.** Terms must be checked against `CONTEXT.md`; hard-to-reverse tradeoffs must be checked against ADRs.\n+2. **Domain language is controlled.** Terms must be checked against `CONTEXT.md`; hard-to-reverse tradeoffs must be checked against Shiki SADRs and target ADRs.\n 3. **PRD precedes issue decomposition.** `to-prd` is used once the context is settled enough to become durable intent.\n 4. **Issues are vertical slices.** `to-issues` must produce independently grabbable tracer-bullet tasks, not horizontal layer tickets.\n 5. **Triage controls dispatch.** Only `ready-for-agent` / AFK tasks may be assigned to Codex without human input.\n@@ -95,19 +98,19 @@\n Process:\n \n - Challenge terms against `CONTEXT.md`.\n-- Check ADRs before reopening decisions.\n+- Check SADRs and target ADRs before reopening decisions.\n - Inspect code when code can answer a question.\n - Ask one question at a time when the operator is available.\n - Provide a recommended answer with each question.\n - Record resolved terms in `CONTEXT.md`.\n-- Create ADRs only for hard-to-reverse, surprising, tradeoff-based decisions.\n+- Create SADRs or target ADRs only for hard-to-reverse, surprising, tradeoff-based decisions.\n - Convert unresolved load-bearing questions into HITL blockers.\n \n Outputs:\n \n - Resolved design-tree decisions.\n - Updated glossary terms when needed.\n-- ADRs when justified.\n+- SADRs or target ADRs when justified.\n - HITL blockers when decisions remain unresolved.\n \n ## Phase 3 \u2014 Context & Impact\n@@ -117,7 +120,7 @@\n \n Outputs must include:\n \n-- Relevant documents and ADRs.\n+- Relevant documents, SADRs, and target ADRs.\n - Relevant modules, interfaces, seams, callers, and tests.\n - Candidate locks.\n - Dependencies.\n@@ -140,7 +143,7 @@\n - Testing decisions.\n - Out of scope.\n - Further notes.\n-- Links to relevant Goal, domain terms, and ADRs.\n+- Links to relevant Goal, domain terms, SADRs, and target ADRs.\n \n Phase 4 ends with Spec Freeze: the operator explicitly approves the PRD, the\n plan records a `spec_freeze` block (status, approved_by, source), and the SF\n@@ -240,7 +243,7 @@\n - Dependencies and locks.\n - Risk and Guardian requirements.\n - Ledger completeness.\n-- Documentation and ADR updates.\n+- Documentation and SADR/target-ADR updates.\n - Scope drift.\n \n CCA emits one of:\n@@ -301,7 +304,7 @@\n | Evidence failure | Behavior might be done, but proof is missing. | Add checks, PR evidence, ledger entry, or artifacts. |\n | Scope drift | PR changes unrelated behavior. | Revert unrelated changes or split into new Goal. |\n | Dependency block | Upstream task or lock not complete. | Block until dependency resolves. |\n-| Architecture block | Decision needs ADR or deep-module discussion. | `grill-with-docs` or `improve-codebase-architecture`. |\n+| Architecture block | Decision needs a SADR, target ADR, or deep-module discussion. | `grill-with-docs` or `improve-codebase-architecture`. |\n | Risk block | Security/production/policy change needs human approval. | Guardian decision. |\n \n ## Phase 5.5 \u2014 Execution Decision Control\ndiff --git a/docs/agents/runtime-auth-model.md b/docs/agents/runtime-auth-model.md\nindex 6a2e1a47e81ea89b5c68a210ec07772a9374babc..3c54748d24233dbd0ba8473950282547ef4ce935\n--- a/docs/agents/runtime-auth-model.md\n+++ b/docs/agents/runtime-auth-model.md\n@@ -148,7 +148,7 @@\n \n API-key based runners may be added by a target repository as an explicit extension. They are not the default Shiki template path.\n \n-If a repo opts into API-key mode, record the exception in an ADR and update the repo's `AGENTS.md`, `.shiki/config.yaml`, and workflow permissions.\n+If a repo opts into API-key mode, record the target-specific exception in a target ADR and update the repo's `AGENTS.md`, `.shiki/config.yaml`, and workflow permissions.\n \n ## GitHub Actions Runtime Compatibility\n \ndiff --git a/docs/agents/skill-gate.md b/docs/agents/skill-gate.md\nindex c50398b0d7c60a8598f2a5c88a10015730dced13..3acffe6444617bb1d54b77d4b4708158ec735bc6\n--- a/docs/agents/skill-gate.md\n+++ b/docs/agents/skill-gate.md\n@@ -9,7 +9,7 @@\n | Skill | Trigger | Expected output |\n | --- | --- | --- |\n | `setup-matt-pocock-skills` | First configuring a repo for these skills, or when issue tracker, triage labels, or domain docs are missing. | `docs/agents/` configuration and an `Agent skills` block in `AGENTS.md` or `CLAUDE.md`. |\n-| `grill-with-docs` | Ambiguous plan, unclear terminology, boundary decision, tradeoff, or ADR-worthy design choice. | Clarified decision tree, recommended answers, updates to `CONTEXT.md` or ADRs when decisions crystallize. |\n+| `grill-with-docs` | Ambiguous plan, unclear terminology, boundary decision, tradeoff, or decision-record-worthy design choice. | Clarified decision tree, recommended answers, updates to `CONTEXT.md`, SADRs, or target ADRs when decisions crystallize. |\n | `zoom-out` | Agent lacks a higher-level architectural map. | Map of relevant modules, callers, seams, and domain concepts. |\n | `to-prd` | Settled context should become a PRD. | PRD published to the issue tracker with problem, solution, user stories, decisions, testing, and out-of-scope notes. |\n | `to-issues` | Goal, PRD, or plan must become implementation tickets. | Vertical-slice issues with acceptance criteria, dependencies, and AFK/HITL classification. |\n@@ -17,7 +17,7 @@\n | `tdd` | Feature work or bug fix has observable behavior that can be specified with tests. | Red-green-refactor loop through public interfaces, one vertical slice at a time. |\n | `code-review` | The TDD loop is green and the implementer is about to create or update a PR. Default for every implementation task. | Pre-PR self-review of the task diff with fixes applied on the task branch; findings and resolutions recorded as a ledger entry naming the skill and a `## Pre-PR code review` PR body section. |\n | `diagnose` | Hard bug, failing check, regression, flaky behavior, or performance problem. | Reproduction loop, ranked hypotheses, instrumentation, fix, regression test, post-mortem. |\n-| `improve-codebase-architecture` | Structural friction, testability, AI-navigability, deep-module opportunity, or refactor request. | Architecture candidates and deepening recommendations informed by `CONTEXT.md` and ADRs. |\n+| `improve-codebase-architecture` | Structural friction, testability, AI-navigability, deep-module opportunity, or refactor request. | Architecture candidates and deepening recommendations informed by `CONTEXT.md`, SADRs, and target ADRs. |\n | `prototype` | A throwaway logic or UI prototype is needed to answer a design question. | Clearly marked disposable prototype and captured decision. |\n \n ## MergeGate Interaction\ndiff --git a/docs/operations.md b/docs/operations.md\nindex cf1f452d4c589e38ccd58553bda0ad8684b51eb7..ace48a90ffbec9e5c0a7113bef16a4858bc44b3f\n--- a/docs/operations.md\n+++ b/docs/operations.md\n@@ -89,6 +89,16 @@\n      target history, brought forward by `shiki migrate`, not a template.\n    - **Everything else** (CLI scripts, workflows, schemas, docs) is overwritten.\n \n+   A forced upgrade also migrates the former numeric Shiki decision paths to\n+   `SADR-NNNN-*`. Before the first write, the installer inventories the exact\n+   legacy Shiki paths and authorizes each deletion only when the existing\n+   `.shiki/install-stamp.json` names that path and its SHA-256 digest still\n+   matches the file. An absent, unreadable, malformed, incomplete, or\n+   mismatching stamp reports every blocked path and stops with no filesystem\n+   change. Target-owned `NNNN-*` ADRs are not cleanup candidates. A non-force\n+   install deletes nothing and retains any still-matching legacy stamp entries\n+   so a later forced upgrade can prove ownership.\n+\n    If the target has pending migrations, `--force` refuses and writes nothing \u2014\n    apply migrations first (step 3), then re-run. The run ends with a single\n    summary of every `.new` written, so a half-upgrade is visible.\ndiff --git a/docs/releasing.md b/docs/releasing.md\nindex 10d73828e7dbd64e9d9f6b83bc84d010537ce62a..aafeee8c4d441e1e427efee2ce4f81a647f020e1\n--- a/docs/releasing.md\n+++ b/docs/releasing.md\n@@ -67,7 +67,7 @@\n CLI in `scripts/`. There are no third-party Python runtime dependencies. The CLI\n is intentionally **not** exposed as a `[project.scripts]` console entry point\n because `scripts/shiki.py` is a dependency-free shim and `scripts/` is not\n-packaged as an importable distribution (see the ADR for the rationale).\n+packaged as an importable distribution (see SADR-0007 for the rationale).\n \n Supported install paths:\n \n@@ -155,6 +155,15 @@\n    [docs/operations.md](operations.md#the-new-convention). If the target has\n    pending migrations, `--force` refuses and writes nothing; apply migrations\n    (step 2) first, then re-run.\n+\n+   The SADR namespace upgrade has an additional pre-write gate: each former\n+   numeric Shiki decision path is removed only when the existing install stamp\n+   contains that exact path and a digest matching its current bytes. The\n+   installer aggregates absent/unreadable/malformed-stamp, missing-digest, and\n+   digest-mismatch blockers before raising, so refusal leaves the target tree\n+   byte-identical. Numeric target ADRs are preserved. A successful forced\n+   upgrade installs and stamps `SADR-*` paths and drops the removed legacy paths\n+   from the refreshed stamp.\n 2. Apply repository-local `.shiki/` state migrations with the migrate command:\n \n    ```bash\ndiff --git a/docs/usage.md b/docs/usage.md\nindex b83407ee3970dd395dd893010bc5dbe8bad26051..a130ef6e40dfd8169a5bc701339351097df1a09f\n--- a/docs/usage.md\n+++ b/docs/usage.md\n@@ -67,8 +67,10 @@\n shiki install-target /path/to/target-repo --local-only\n ```\n \n-Use `--force` only when you intentionally want to overwrite existing target\n-files.\n+Use `--force` only when you intentionally want to refresh the target template.\n+For the SADR namespace migration, `--force` deletes only exact legacy Shiki\n+decision paths whose existing install-stamp digest still matches; any ownership\n+blocker stops before the first write. Numeric target ADRs are never candidates.\n \n ## `shiki bootstrap-platform` \u2014 publish the Shiki platform repo\n \ndiff --git a/scripts/guardian_approval_signal.py b/scripts/guardian_approval_signal.py\nindex d059f92b2c82aa2b5d6ca421365537a87886c1cb..16924892714f4f5b9303c0ef0e325a7e70075630\n--- a/scripts/guardian_approval_signal.py\n+++ b/scripts/guardian_approval_signal.py\n@@ -199,8 +199,8 @@\n ) -> Any:\n     \"\"\"Evaluate SADR-0015 Contract Approval for the CCA Guardian signal.\n \n-    The SAME OR that MergeGate applies must reach this deterministic signal: ADR\n-    0015 is explicit that without it, a contract-approved PR still returns\n+    The SAME OR that MergeGate applies must reach this deterministic signal:\n+    SADR-0015 is explicit that without it, a contract-approved PR still returns\n     ``needs_guardian``, the CCA job fails, and the MergeGate policy check never\n     runs \u2014 the gate this change moves would never be reached. This resolves the\n     task the SAME way MergeGate does (the first Shiki task id in the PR body),\ndiff --git a/scripts/shiki_contract_approval.py b/scripts/shiki_contract_approval.py\nindex 941bcb96fdd8b9a85bc0c4f0f04d5dad9d04839c..4a4b418f6527413161db82670bec857a043360e2\n--- a/scripts/shiki_contract_approval.py\n+++ b/scripts/shiki_contract_approval.py\n@@ -142,7 +142,7 @@\n     changed_files_status: Any,\n     registration: Any,\n ) -> ContractApprovalResult:\n-    \"\"\"Whether this PR carries an SADR-0015 Contract Approval.\n+    \"\"\"Whether this PR carries a SADR-0015 Contract Approval.\n \n     The four conditions are evaluated IN ORDER, each failure recorded as a\n     distinct reason string:\ndiff --git a/scripts/shiki_guardian.py b/scripts/shiki_guardian.py\nindex a4ecb79bbda55fd5c8b06adfae2eb99760a03c45..4dd99bbe714e26f421fa19abac7a34ae1b195ecc\n--- a/scripts/shiki_guardian.py\n+++ b/scripts/shiki_guardian.py\n@@ -12,7 +12,7 @@\n # The carry re-derives its proof from git in-process on every judging invocation;\n # it is never read from an artifact, so there is nothing to go stale or be forged.\n # shiki_sync_proof is the ONLY guardian-family module that runs git, and every one\n-# of its invocations is read-only and local (ADR: prove-from-git base sync).\n+# of its invocations is read-only and local (SADR: prove-from-git base sync).\n from shiki_sync_proof import verify_pure_base_sync\n \n GUARDIAN_POLICY_PATH = \".shiki/guardian-policy.json\"\ndiff --git a/scripts/shiki_guardian_review.py b/scripts/shiki_guardian_review.py\nindex dd0f104d457899e0ba5ff1e162011c1e90b6f92b..e10ef58c046324e7ea3a3392feb3048ca77414ce\n--- a/scripts/shiki_guardian_review.py\n+++ b/scripts/shiki_guardian_review.py\n@@ -159,7 +159,7 @@\n     docs_only = _is_docs_only(changed_files)\n     if docs_only and matched == 0:\n         focus.append(\n-            \"Docs/ADR-only change: confirm the change is genuinely documentation/decision-only, introduces no behavior or contract drift, and stays consistent with the constitution and existing ADRs.\"\n+            \"Docs/decision-record-only change: confirm the change is genuinely documentation/decision-only, introduces no behavior or contract drift, and stays consistent with the constitution and existing SADRs and target ADRs.\"\n         )\n \n     # Conservative augmentation: unknown (no category and not docs-only), mixed\ndiff --git a/scripts/shiki_installer.py b/scripts/shiki_installer.py\nindex be064ab7a29ff6836f7f7a1d70500a43e0d11d7b..9fd8eefd137ce7003dad5c0dcb2e48e2cd47ab32\n--- a/scripts/shiki_installer.py\n+++ b/scripts/shiki_installer.py\n@@ -118,6 +118,7 @@\n # what lets a target carry it without a governance-schema change.\n INSTALL_STAMP_PATH = \".shiki/install-stamp.json\"\n INSTALL_STAMP_VERSION = 1\n+LEGACY_SHIKI_SADR_MAX = 18\n \n # Under --force the shipped surface splits three ways, so nothing is lost and\n # nothing is silently stale.\n@@ -237,9 +238,98 @@\n         )\n \n \n+def legacy_shiki_adr_paths() -> tuple[str, ...]:\n+    \"\"\"Former numeric paths for Shiki SADR-0001 through SADR-0018.\n+\n+    The authoritative slugs come from the current SADR filenames. SADR-0019 is\n+    the namespace decision itself and never shipped under a numeric legacy path.\n+    \"\"\"\n+    paths: list[str] = []\n+    for sadr in sorted((ROOT / \"docs\" / \"adr\").glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\")):\n+        number = sadr.name.split(\"-\", 2)[1]\n+        if int(number) <= LEGACY_SHIKI_SADR_MAX:\n+            paths.append(f\"docs/adr/{sadr.name.removeprefix('SADR-')}\")\n+    return tuple(paths)\n+\n+\n+def preflight_legacy_sadr_cleanup(target: Path, *, force: bool) -> tuple[str, ...]:\n+    \"\"\"Authorize exact legacy Shiki ADR removals before the first write.\n+\n+    A non-force install never removes existing files. A forced install needs no\n+    stamp when no legacy Shiki path is present. When legacy paths do exist, each\n+    must be named in a readable install stamp and its current bytes must match\n+    the recorded digest.\n+    \"\"\"\n+    if not force:\n+        return ()\n+    present = tuple(\n+        relative\n+        for relative in legacy_shiki_adr_paths()\n+        if (target / relative).exists() or (target / relative).is_symlink()\n+    )\n+    if not present:\n+        return ()\n+\n+    stamp_path = target / INSTALL_STAMP_PATH\n+    try:\n+        raw = stamp_path.read_text(encoding=\"utf-8\")\n+        stamp = json.loads(raw)\n+    except FileNotFoundError:\n+        detail = \"install stamp is absent\"\n+        blockers = [f\"{relative}: {detail}\" for relative in present]\n+    except (OSError, UnicodeDecodeError) as error:\n+        detail = f\"install stamp is unreadable ({error})\"\n+        blockers = [f\"{relative}: {detail}\" for relative in present]\n+    except json.JSONDecodeError as error:\n+        detail = f\"install stamp is malformed ({error})\"\n+        blockers = [f\"{relative}: {detail}\" for relative in present]\n+    else:\n+        if not isinstance(stamp, dict):\n+            blockers = [\n+                f\"{relative}: install stamp is malformed (expected an object)\"\n+                for relative in present\n+            ]\n+        else:\n+            digests = stamp.get(\"digests\")\n+            blockers = []\n+            if not isinstance(digests, dict):\n+                blockers.extend(\n+                    f\"{relative}: install stamp has no digest for this path\"\n+                    for relative in present\n+                )\n+            else:\n+                for relative in present:\n+                    expected = digests.get(relative)\n+                    if not isinstance(expected, str) or not expected:\n+                        blockers.append(\n+                            f\"{relative}: install stamp has no digest for this path\"\n+                        )\n+                        continue\n+                    try:\n+                        actual = _sha256_file(target / relative)\n+                    except OSError as error:\n+                        blockers.append(\n+                            f\"{relative}: legacy path is unreadable ({error})\"\n+                        )\n+                        continue\n+                    if actual != expected:\n+                        blockers.append(\n+                            f\"{relative}: digest mismatch (stamp {expected}, current {actual})\"\n+                        )\n+\n+    if blockers:\n+        joined = \"\\n\".join(f\"- {blocker}\" for blocker in blockers)\n+        raise ShikiError(\n+            \"legacy Shiki ADR cleanup is not authorized:\\n\"\n+            f\"{joined}\\nNo files were written.\"\n+        )\n+    return present\n+\n+\n def install_template(target: Path, *, force: bool, validate: bool) -> list[NewFileNote]:\n     # Preconditions first: never rewrite the tree of a target that cannot be\n     # upgraded cleanly (see refuse_pending_migrations).\n+    legacy_cleanup = preflight_legacy_sadr_cleanup(target, force=force)\n     refuse_pending_migrations(target)\n \n     # Resolve the target's required CODEOWNERS owner once from its\n@@ -250,6 +340,10 @@\n     # verbatim. See ``codeowners_required_owner``.\n     codeowners_owner = codeowners_required_owner(target)\n \n+    for relative in legacy_cleanup:\n+        (target / relative).unlink()\n+        info(f\"removed stamped legacy Shiki ADR: {target / relative}\")\n+\n     notes: list[NewFileNote] = []\n     for relative in TEMPLATE_PATHS:\n         source = ROOT / relative\n@@ -647,6 +741,22 @@\n     digests = target_stamp_digests(target)\n     existing = read_install_stamp(target)\n     if existing is not None:\n+        # A non-force install deliberately leaves legacy Shiki ADRs in place.\n+        # Preserve only still-matching ownership proof so a later forced\n+        # upgrade can authorize cleanup; never carry a missing or stale digest.\n+        existing_digests = existing.get(\"digests\")\n+        if isinstance(existing_digests, dict):\n+            for relative in legacy_shiki_adr_paths():\n+                expected = existing_digests.get(relative)\n+                path = target / relative\n+                if not isinstance(expected, str) or not path.is_file():\n+                    continue\n+                try:\n+                    actual = _sha256_file(path)\n+                except OSError:\n+                    continue\n+                if actual == expected:\n+                    digests[relative] = actual\n         if _incoming_is_older(commit, existing.get(\"platform_commit\")):\n             warn(\n                 \"kept existing install stamp: incoming install \"\ndiff --git a/scripts/shiki_loop.py b/scripts/shiki_loop.py\nindex 3611641da8aeb5038b1da90e3114b09a473ec6d8..84caa491b9cad0edc4176f209c13e9221e975681\n--- a/scripts/shiki_loop.py\n+++ b/scripts/shiki_loop.py\n@@ -424,7 +424,7 @@\n         # unresolvable CCA verdict has already produced its terminal stop and is never\n         # laundered into a head-moving sync (SADR-0010). It also never fires while the\n         # policy (Guardian) gate is red \u2014 for ANY reason, not only when it is the sole\n-        # red check \u2014 so a head-bound Guardian approval, or an SADR-0015\n+        # red check \u2014 so a head-bound Guardian approval, or a SADR-0015\n         # contract-approval carry that base movement defeated (the policy gate compares\n         # base and head task governance directly), is never silently restored by\n         # adopting base wholesale. It is bounded by a durable per-task counter so the\ndiff --git a/scripts/shiki_tasks.py b/scripts/shiki_tasks.py\nindex aec3809f24b4a0c8c96fc50971cff9ae9ad30381..d531052e9d9717058d4631483cefab44c26a65c1\n--- a/scripts/shiki_tasks.py\n+++ b/scripts/shiki_tasks.py\n@@ -821,7 +821,7 @@\n         \"prompt\": prompt,\n         \"entry_skill\": \"grill-with-docs\",\n         \"required_next_steps\": [\n-            \"Run grill-with-docs until terminology, boundaries, risks, and ADR-worthy decisions are settled.\",\n+            \"Run grill-with-docs until terminology, boundaries, risks, and decision-record-worthy choices are settled.\",\n             \"Obtain the operator's explicit approval of the PRD/requirements (Spec Freeze).\",\n             \"Write a machine-readable plan JSON with grill_with_docs.status=complete and spec_freeze.status=frozen.\",\n             \"Run shiki plan ingest --plan-file PLAN.json.\",\ndiff --git a/scripts/test_shiki_goal_loop.sh b/scripts/test_shiki_goal_loop.sh\nindex 753c7b516e10f0be3bc92a4bd0eb1fa699776ae6..8f3820a0a079b3044424cfe1a6c6b7fe68896e67\n--- a/scripts/test_shiki_goal_loop.sh\n+++ b/scripts/test_shiki_goal_loop.sh\n@@ -231,8 +231,8 @@\n \n # High/critical risk merges autonomously when all required checks are green:\n # the \"MergeGate policy check\" required check IS the Guardian gate (it enforces\n-# guardian-policy.json \u2014 human approval OR external AI guardian review, ADR\n-# 0010), so green checks mean a recorded authority approved. The fake gh returns\n+# guardian-policy.json \u2014 human approval OR external AI guardian review,\n+# SADR-0010), so green checks mean a recorded authority approved. The fake gh returns\n # green checks, simulating that the Guardian gate passed.\n python3 \"$ROOT/scripts/shiki.py\" goal create --target \"$TARGET\" --title \"Guardian gate\" --outcome \"High risk merges via policy gate\" >/tmp/shiki-goal-loop-guardian-goal.json\n GGOAL=\"$(json_get /tmp/shiki-goal-loop-guardian-goal.json goal_id)\"\ndiff --git a/tests/test_adr_references.py b/tests/test_adr_references.py\nindex 51706329c797d5dd9b47e082a333fa83b261d482..d005da4f771f5e8085ec22d4b787980e68f5ae8c\n--- a/tests/test_adr_references.py\n+++ b/tests/test_adr_references.py\n@@ -1,8 +1,8 @@\n-\"\"\"Every ``ADR ####`` citation must resolve to exactly one decision record.\n+\"\"\"Every target ADR and Shiki SADR citation resolves in its own namespace.\n \n Background. ``scripts/mergegate_check.py`` justifies ``is_bookkeeping_closeout``\n-with \"ADR 0017\" nine times and ``scripts/guardian_approval_signal.py`` cites\n-0016/0017 seven times, yet for a stretch the numbering in ``docs/adr/`` stopped\n+with \"SADR-0017\" nine times and ``scripts/guardian_approval_signal.py`` cites\n+SADR-0016/SADR-0017 seven times, yet for a stretch the numbering in ``docs/adr/`` stopped\n at 0015: the authoring commits lived on a coordinator branch and no task ever\n declared ``docs/adr/...`` in its locks, so nothing carried the records to the\n default branch. The stated basis of an exemption that merged seven PRs with zero\n@@ -11,11 +11,12 @@\n and the directory drifted with no signal.\n \n This is the same unbound-pair shape behind other silent failures: a reference\n-and its target with nothing asserting they agree. This test binds them. It scans\n-every ``scripts/*.py`` (plus ``docs/**/*.md`` and the root ``*.md`` governance\n-files, which are cheap) for the ``ADR ####`` pattern and asserts each cited\n-number resolves to exactly one ``docs/adr/####-*.md``. The converse is\n-deliberately NOT required: an ADR may exist without being cited.\n+and its target with nothing asserting they agree. This test binds them. Target\n+``ADR NNNN`` citations resolve only to ``docs/adr/NNNN-*.md``; Shiki\n+``SADR-NNNN`` citations resolve only to ``docs/adr/SADR-NNNN-*.md``. The scan\n+covers current governance docs, scripts, workflows, prompts, packaging metadata,\n+and repo-local Codex guidance while deliberately excluding historical\n+``.shiki`` evidence. The converse is not required: a record may be uncited.\n \n The suite is dependency free: standard-library ``unittest`` only.\n \"\"\"\n@@ -31,17 +32,18 @@\n \n REPO_ROOT = Path(__file__).resolve().parents[1]\n \n-# \"ADR 0016\" -- a four-digit citation. The trailing \\b keeps a five-digit token\n-# from being read as a truncated four-digit number.\n-_CITATION = re.compile(r\"\\bADR (\\d{4})\\b\")\n-# \"0016-locks-bound-product-paths.md\" -- the leading four digits name the record.\n-_ADR_FILENAME = re.compile(r\"^(\\d{4})-.*\\.md$\")\n+# ``ADR 0016`` / ``SADR-0016`` -- a namespace plus four-digit citation. The\n+# trailing ``\\b`` keeps a five-digit token from being read as a truncated one.\n+_CITATION = re.compile(r\"\\b(S?ADR)[ -](\\d{4})\\b\")\n+# Target ADRs retain the numeric filename; Shiki platform decisions use SADR.\n+_DECISION_FILENAME = re.compile(r\"^(?:(SADR)-)?(\\d{4})-.*\\.md$\")\n \n \n class Violation(NamedTuple):\n     \"\"\"A citation that does not resolve to exactly one decision record.\"\"\"\n \n     citing_file: str  # repo-relative path of the file that cites the ADR\n+    namespace: str  # \"ADR\" for target decisions; \"SADR\" for Shiki decisions\n     number: str  # the cited four-digit number, e.g. \"0016\"\n     line: int  # 1-based line of the citation\n     reason: str  # \"missing\" (zero matches) or \"ambiguous\" (two or more)\n@@ -49,44 +51,59 @@\n \n     @property\n     def expected_path(self) -> str:\n-        return f\"docs/adr/{self.number}-*.md\"\n+        prefix = \"SADR-\" if self.namespace == \"SADR\" else \"\"\n+        return f\"docs/adr/{prefix}{self.number}-*.md\"\n \n     def describe(self) -> str:\n         \"\"\"Name the citing file, the cited number, and the missing path.\"\"\"\n         if self.reason == \"missing\":\n             return (\n-                f\"{self.citing_file}:{self.line} cites ADR {self.number} \"\n+                f\"{self.citing_file}:{self.line} cites {self.namespace} {self.number} \"\n                 f\"but no document matches {self.expected_path}\"\n             )\n         joined = \", \".join(self.matches)\n         return (\n-            f\"{self.citing_file}:{self.line} cites ADR {self.number} which \"\n+            f\"{self.citing_file}:{self.line} cites {self.namespace} {self.number} which \"\n             f\"resolves to {len(self.matches)} documents (expected exactly one \"\n             f\"at {self.expected_path}): {joined}\"\n         )\n \n \n-def _adr_index(root: Path) -> dict[str, list[Path]]:\n-    \"\"\"Map every four-digit ADR number to the record files that claim it.\"\"\"\n-    index: dict[str, list[Path]] = {}\n+def _adr_index(root: Path) -> dict[tuple[str, str], list[Path]]:\n+    \"\"\"Map each decision namespace and number to the files that claim it.\"\"\"\n+    index: dict[tuple[str, str], list[Path]] = {}\n     adr_dir = root / \"docs\" / \"adr\"\n     if adr_dir.is_dir():\n         for path in sorted(adr_dir.glob(\"*.md\")):\n-            match = _ADR_FILENAME.match(path.name)\n+            match = _DECISION_FILENAME.match(path.name)\n             if match:\n-                index.setdefault(match.group(1), []).append(path)\n+                namespace = \"SADR\" if match.group(1) else \"ADR\"\n+                index.setdefault((namespace, match.group(2)), []).append(path)\n     return index\n \n \n def _citing_files(root: Path, include_docs: bool = True) -> list[Path]:\n-    \"\"\"Files scanned for citations: scripts/*.py, and cheaply the markdown.\"\"\"\n+    \"\"\"Current, shipped text surfaces scanned for decision citations.\"\"\"\n     files = sorted((root / \"scripts\").glob(\"*.py\"))\n+    files += sorted((root / \"scripts\").glob(\"*.sh\"))\n     if include_docs:\n         docs = root / \"docs\"\n         if docs.is_dir():\n             files += sorted(docs.rglob(\"*.md\"))\n         files += sorted(root.glob(\"*.md\"))\n-    return files\n+        pyproject = root / \"pyproject.toml\"\n+        if pyproject.is_file():\n+            files.append(pyproject)\n+        for relative in (\".github\", \".codex\"):\n+            surface = root / relative\n+            if surface.is_dir():\n+                files += sorted(\n+                    path\n+                    for path in surface.rglob(\"*\")\n+                    if path.is_file()\n+                    and path.suffix in {\".md\", \".py\", \".sh\", \".toml\", \".yaml\", \".yml\"}\n+                )\n+    return sorted(set(files))\n \n \n def find_adr_reference_violations(\n@@ -94,10 +111,9 @@\n ) -> list[Violation]:\n     \"\"\"Return every ``ADR ####`` citation under ``root`` that fails to resolve.\n \n-    A citation resolves when exactly one ``docs/adr/####-*.md`` matches its\n-    number. Zero matches is a \"missing\" violation; two or more is \"ambiguous\".\n-    Records that are never cited produce nothing -- the converse is not\n-    required.\n+    A citation resolves when exactly one record in the same namespace matches\n+    its number. Zero matches is a \"missing\" violation; two or more is\n+    \"ambiguous\". Records that are never cited produce nothing.\n     \"\"\"\n     root = Path(root)\n     index = _adr_index(root)\n@@ -110,13 +126,15 @@\n         rel = path.relative_to(root).as_posix()\n         for lineno, line in enumerate(text.splitlines(), start=1):\n             for match in _CITATION.finditer(line):\n-                number = match.group(1)\n-                found = index.get(number, [])\n+                namespace, number = match.groups()\n+                found = index.get((namespace, number), [])\n                 if len(found) == 1:\n                     continue\n                 reason = \"missing\" if not found else \"ambiguous\"\n                 rels = [p.relative_to(root).as_posix() for p in found]\n-                violations.append(Violation(rel, number, lineno, reason, rels))\n+                violations.append(\n+                    Violation(rel, namespace, number, lineno, reason, rels)\n+                )\n     return violations\n \n \n@@ -129,8 +147,8 @@\n         self.assertEqual(\n             violations,\n             [],\n-            \"Every 'ADR ####' citation must resolve to exactly one \"\n-            f\"docs/adr/####-*.md:\\n{detail}\",\n+            \"Every ADR/SADR citation must resolve exactly once in its own \"\n+            f\"docs/adr namespace:\\n{detail}\",\n         )\n \n     def test_scan_actually_reads_scripts(self) -> None:\n@@ -142,12 +160,38 @@\n             \"the citation scan must cover scripts/*.py\",\n         )\n \n+    def test_platform_citations_do_not_use_unresolved_numeric_shorthand(self) -> None:\n+        shorthand = re.compile(r\"SADR-\\d{4}/\\d{4}\")\n+        offenders = []\n+        for path in _citing_files(REPO_ROOT):\n+            text = path.read_text(encoding=\"utf-8\")\n+            if shorthand.search(text):\n+                offenders.append(path.relative_to(REPO_ROOT).as_posix())\n+        self.assertEqual(\n+            offenders,\n+            [],\n+            \"repeat the SADR prefix so every cited number is independently resolvable\",\n+        )\n+\n     def test_carried_records_are_indexed(self) -> None:\n         # The two records this Goal carried onto the default branch, and which\n         # the scripts cite most, must be present.\n         index = _adr_index(REPO_ROOT)\n-        self.assertIn(\"0016\", index)\n-        self.assertIn(\"0017\", index)\n+        self.assertIn((\"SADR\", \"0016\"), index)\n+        self.assertIn((\"SADR\", \"0017\"), index)\n+\n+    def test_platform_tree_has_only_sadr_0001_through_0019(self) -> None:\n+        adr_dir = REPO_ROOT / \"docs\" / \"adr\"\n+        sadrs = sorted(adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\"))\n+        self.assertEqual(\n+            {path.name.split(\"-\", 2)[1] for path in sadrs},\n+            {f\"{number:04d}\" for number in range(1, 20)},\n+        )\n+        self.assertEqual(\n+            list(adr_dir.glob(\"[0-9][0-9][0-9][0-9]-*.md\")),\n+            [],\n+            \"the Shiki platform tree must not retain legacy numeric SADR paths\",\n+        )\n \n \n class FixtureAdrReferences(unittest.TestCase):\n@@ -180,6 +224,26 @@\n         self.assertIn(\"9991\", message)  # cited number\n         self.assertIn(\"docs/adr/9991-*.md\", message)  # missing path\n \n+    def test_target_adr_does_not_satisfy_missing_platform_sadr(self) -> None:\n+        root = self._make_repo(\n+            adr_names=[\"0042-target-decision.md\"],\n+            script_body='\"\"\"Target ADR 0042 exists, but platform SADR-0042 does not.\"\"\"\\n',\n+        )\n+        violations = find_adr_reference_violations(root, include_docs=False)\n+        self.assertEqual(len(violations), 1)\n+        self.assertEqual(violations[0].namespace, \"SADR\")\n+        self.assertEqual(violations[0].number, \"0042\")\n+        self.assertEqual(violations[0].expected_path, \"docs/adr/SADR-0042-*.md\")\n+\n+    def test_target_adr_and_platform_sadr_may_reuse_a_number(self) -> None:\n+        root = self._make_repo(\n+            adr_names=[\"0042-target-decision.md\", \"SADR-0042-platform-decision.md\"],\n+            script_body='\"\"\"Target ADR 0042 and platform SADR-0042 are distinct.\"\"\"\\n',\n+        )\n+        self.assertEqual(\n+            find_adr_reference_violations(root, include_docs=False), []\n+        )\n+\n     def test_uncited_adr_is_allowed(self) -> None:\n         # An ADR exists but nothing cites it -> not a violation. The converse\n         # (every ADR must be cited) is explicitly NOT required.\ndiff --git a/tests/test_guardian_review.py b/tests/test_guardian_review.py\nindex 88829e7810212635e2fc98aa5918d6f26294677d..00a85a237acd5bf2d6cb5b294128198c422402ca\n--- a/tests/test_guardian_review.py\n+++ b/tests/test_guardian_review.py\n@@ -162,7 +162,7 @@\n \n     def test_docs_only_is_light_and_not_conservative(self):\n         focus = gr.classify_review_focus([\"docs/adr/0099-x.md\", \"README.md\"], \"\", \"low\")\n-        self.assertTrue(any(\"Docs/ADR-only\" in f for f in focus))\n+        self.assertTrue(any(\"Docs/decision-record-only\" in f for f in focus))\n         self.assertFalse(any(\"conservative default\" in f.lower() for f in focus))\n \n     def test_unknown_category_is_conservative(self):\ndiff --git a/tests/test_policy_job_merged_prs.py b/tests/test_policy_job_merged_prs.py\nindex d45512e74681e5e649279236c7d4e79d5c3da471..b97989f8bd74d091de1a6b5275a20d8dc68c4512\n--- a/tests/test_policy_job_merged_prs.py\n+++ b/tests/test_policy_job_merged_prs.py\n@@ -3,8 +3,8 @@\n \n ``.github/workflows/shiki-cca-completion.yml`` has two consumers of the merged-PR\n proof: the CCA *signal* (``guardian_approval_signal.py`` in \"Prepare CCA context\")\n-and the authoritative gate (``mergegate_check.py`` in \"Enforce MergeGate\"). ADR\n-0017's bookkeeping-closeout exemption (condition 6) needs the merged implementation\n+and the authoritative gate (``mergegate_check.py`` in \"Enforce MergeGate\").\n+SADR-0017's bookkeeping-closeout exemption (condition 6) needs the merged implementation\n PR proven, and a closeout carries NO ``post_merge_reconcile`` marker. The signal\n already resolves ``MERGED_PRS`` unconditionally, but \"Enforce MergeGate\" used to\n resolve it only inside ``grep -q '<!-- shiki:post_merge_reconcile -->'``. So a\ndiff --git a/tests/test_sadr_install_migration.py b/tests/test_sadr_install_migration.py\nnew file mode 100644\nindex 0000000000000000000000000000000000000000..d51eac872b1c79ff4579c4c1401ca5d775a60d8d\n--- /dev/null\n+++ b/tests/test_sadr_install_migration.py\n@@ -0,0 +1,237 @@\n+\"\"\"SADR installation and collision-safe legacy upgrade behavior.\"\"\"\n+\n+from __future__ import annotations\n+\n+import contextlib\n+import hashlib\n+import io\n+import json\n+import shutil\n+import tempfile\n+import unittest\n+from pathlib import Path\n+\n+import shiki_test_support  # noqa: F401  (path bootstrap)\n+\n+import shiki_installer\n+from tests.test_adr_references import find_adr_reference_violations\n+\n+\n+PRODUCT_NUMBERS = tuple(range(13, 19))\n+\n+\n+def _quiet_install(target: Path, *, force: bool, validate: bool) -> None:\n+    with contextlib.redirect_stdout(io.StringIO()):\n+        shiki_installer.install_template(target, force=force, validate=validate)\n+\n+\n+def _sha256(path: Path) -> str:\n+    return hashlib.sha256(path.read_bytes()).hexdigest()\n+\n+\n+def _legacy_sadr_paths() -> tuple[str, ...]:\n+    return tuple(\n+        f\"docs/adr/{path.name.removeprefix('SADR-')}\"\n+        for path in sorted((shiki_installer.ROOT / \"docs\" / \"adr\").glob(\"SADR-*.md\"))\n+        if path.name != \"SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\"\n+    )\n+\n+\n+def _create_legacy_upgrade_fixture(\n+    target: Path,\n+) -> tuple[dict[str, bytes], tuple[str, ...]]:\n+    adr_dir = target / \"docs\" / \"adr\"\n+    adr_dir.mkdir(parents=True)\n+    product_bytes = {\n+        f\"{number:04d}-cubeshare-product-decision.md\": (\n+            f\"# CubeShare ADR {number:04d}\\n\\nDo not rewrite {number}.\\n\"\n+        ).encode()\n+        for number in PRODUCT_NUMBERS\n+    }\n+    for name, content in product_bytes.items():\n+        (adr_dir / name).write_bytes(content)\n+\n+    legacy_paths = _legacy_sadr_paths()\n+    for relative in legacy_paths:\n+        (target / relative).write_bytes(f\"legacy Shiki bytes for {relative}\\n\".encode())\n+\n+    stamp_path = target / shiki_installer.INSTALL_STAMP_PATH\n+    stamp_path.parent.mkdir(parents=True, exist_ok=True)\n+    stamp = {\n+        \"version\": shiki_installer.INSTALL_STAMP_VERSION,\n+        \"platform_commit\": None,\n+        \"installed_at\": \"2000-01-01T00:00:00+00:00\",\n+        \"digests\": {\n+            relative: _sha256(target / relative) for relative in legacy_paths\n+        },\n+    }\n+    stamp_path.write_text(\n+        json.dumps(stamp, indent=2, sort_keys=True) + \"\\n\", encoding=\"utf-8\"\n+    )\n+    return product_bytes, legacy_paths\n+\n+\n+def _tree_snapshot(root: Path) -> dict[str, tuple[str, bytes | str]]:\n+    \"\"\"Recursive byte/shape snapshot used to prove preflight is write-free.\"\"\"\n+    snapshot: dict[str, tuple[str, bytes | str]] = {}\n+    for path in sorted(root.rglob(\"*\")):\n+        relative = path.relative_to(root).as_posix()\n+        if path.is_symlink():\n+            snapshot[relative] = (\"symlink\", path.readlink().as_posix())\n+        elif path.is_dir():\n+            snapshot[relative] = (\"directory\", \"\")\n+        else:\n+            snapshot[relative] = (\"file\", path.read_bytes())\n+    return snapshot\n+\n+\n+class FreshSadrInstallTests(unittest.TestCase):\n+    def setUp(self) -> None:\n+        self.tmp = Path(tempfile.mkdtemp(prefix=\"shiki-sadr-fresh-\"))\n+        self.addCleanup(lambda: shutil.rmtree(self.tmp, ignore_errors=True))\n+        self.target = self.tmp / \"target\"\n+        self.adr_dir = self.target / \"docs\" / \"adr\"\n+        self.adr_dir.mkdir(parents=True)\n+        self.product_bytes = {\n+            f\"{number:04d}-product-decision.md\": (\n+                f\"# Product ADR {number:04d}\\n\\nTarget-owned bytes {number}.\\n\"\n+            ).encode()\n+            for number in PRODUCT_NUMBERS\n+        }\n+        for name, content in self.product_bytes.items():\n+            (self.adr_dir / name).write_bytes(content)\n+\n+    def test_fresh_install_preserves_product_adrs_and_installs_all_sadrs(self) -> None:\n+        _quiet_install(self.target, force=False, validate=True)\n+\n+        for name, expected in self.product_bytes.items():\n+            self.assertEqual((self.adr_dir / name).read_bytes(), expected)\n+\n+        sadr_numbers = {\n+            path.name.split(\"-\", 2)[1]\n+            for path in self.adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\")\n+        }\n+        self.assertEqual(sadr_numbers, {f\"{number:04d}\" for number in range(1, 20)})\n+\n+        numeric_records = {\n+            path.name\n+            for path in self.adr_dir.glob(\"[0-9][0-9][0-9][0-9]-*.md\")\n+        }\n+        self.assertEqual(numeric_records, set(self.product_bytes))\n+        stamp = json.loads(\n+            (self.target / shiki_installer.INSTALL_STAMP_PATH).read_text(\n+                encoding=\"utf-8\"\n+            )\n+        )\n+        for path in self.adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\"):\n+            relative = path.relative_to(self.target).as_posix()\n+            self.assertEqual(stamp[\"digests\"][relative], _sha256(path))\n+        self.assertEqual(find_adr_reference_violations(self.target), [])\n+\n+\n+class LegacySadrUpgradeTests(unittest.TestCase):\n+    def setUp(self) -> None:\n+        self.tmp = Path(tempfile.mkdtemp(prefix=\"shiki-sadr-upgrade-\"))\n+        self.addCleanup(lambda: shutil.rmtree(self.tmp, ignore_errors=True))\n+        self.target = self.tmp / \"target\"\n+        self.adr_dir = self.target / \"docs\" / \"adr\"\n+        self.product_bytes, self.legacy_paths = _create_legacy_upgrade_fixture(\n+            self.target\n+        )\n+\n+    def test_force_upgrade_deletes_only_stamped_legacy_sadrs(self) -> None:\n+        _quiet_install(self.target, force=True, validate=True)\n+\n+        for relative in self.legacy_paths:\n+            self.assertFalse((self.target / relative).exists(), relative)\n+        for name, expected in self.product_bytes.items():\n+            self.assertEqual((self.adr_dir / name).read_bytes(), expected)\n+\n+        sadrs = list(self.adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\"))\n+        self.assertEqual(len(sadrs), 19)\n+        refreshed = json.loads(\n+            (self.target / shiki_installer.INSTALL_STAMP_PATH).read_text(encoding=\"utf-8\")\n+        )\n+        for relative in self.legacy_paths:\n+            self.assertNotIn(relative, refreshed[\"digests\"])\n+        for path in sadrs:\n+            relative = path.relative_to(self.target).as_posix()\n+            self.assertEqual(refreshed[\"digests\"][relative], _sha256(path))\n+\n+    def test_non_force_install_never_deletes_legacy_files(self) -> None:\n+        before = {\n+            relative: (self.target / relative).read_bytes()\n+            for relative in self.legacy_paths\n+        }\n+        _quiet_install(self.target, force=False, validate=False)\n+        for relative, expected in before.items():\n+            self.assertEqual((self.target / relative).read_bytes(), expected)\n+        refreshed = json.loads(\n+            (self.target / shiki_installer.INSTALL_STAMP_PATH).read_text(encoding=\"utf-8\")\n+        )\n+        for relative in self.legacy_paths:\n+            self.assertEqual(refreshed[\"digests\"][relative], _sha256(self.target / relative))\n+\n+\n+class LegacyCleanupPreflightTests(unittest.TestCase):\n+    def setUp(self) -> None:\n+        self.tmp = Path(tempfile.mkdtemp(prefix=\"shiki-sadr-preflight-\"))\n+        self.addCleanup(lambda: shutil.rmtree(self.tmp, ignore_errors=True))\n+\n+    def test_all_ownership_blockers_abort_before_any_write(self) -> None:\n+        cases = (\n+            (\"absent\", \"install stamp is absent\"),\n+            (\"unreadable\", \"install stamp is unreadable\"),\n+            (\"malformed\", \"install stamp is malformed\"),\n+            (\"missing-digest\", \"install stamp has no digest\"),\n+            (\"digest-mismatch\", \"digest mismatch\"),\n+        )\n+        for name, expected_reason in cases:\n+            with self.subTest(name=name):\n+                target = self.tmp / name\n+                _, legacy_paths = _create_legacy_upgrade_fixture(target)\n+                stamp_path = target / shiki_installer.INSTALL_STAMP_PATH\n+\n+                if name == \"absent\":\n+                    stamp_path.unlink()\n+                elif name == \"unreadable\":\n+                    stamp_path.unlink()\n+                    stamp_path.mkdir()\n+                elif name == \"malformed\":\n+                    stamp_path.write_text(\"{not-json\\n\", encoding=\"utf-8\")\n+                else:\n+                    stamp = json.loads(stamp_path.read_text(encoding=\"utf-8\"))\n+                    if name == \"missing-digest\":\n+                        stamp[\"digests\"] = {}\n+                    else:\n+                        stamp[\"digests\"] = {\n+                            relative: \"deadbeef\" for relative in legacy_paths\n+                        }\n+                    stamp_path.write_text(\n+                        json.dumps(stamp, indent=2, sort_keys=True) + \"\\n\",\n+                        encoding=\"utf-8\",\n+                    )\n+\n+                before = _tree_snapshot(target)\n+                with self.assertRaises(shiki_installer.ShikiError) as raised:\n+                    _quiet_install(target, force=True, validate=False)\n+                after = _tree_snapshot(target)\n+\n+                self.assertEqual(before, after, f\"{name} preflight changed the tree\")\n+                message = str(raised.exception)\n+                self.assertIn(expected_reason, message)\n+                self.assertIn(\"No files were written\", message)\n+                for relative in legacy_paths:\n+                    self.assertIn(relative, message)\n+\n+    def test_absent_stamp_is_allowed_when_no_legacy_path_exists(self) -> None:\n+        target = self.tmp / \"no-legacy\"\n+        target.mkdir()\n+        _quiet_install(target, force=True, validate=False)\n+        self.assertTrue(\n+            (target / \"docs\" / \"adr\" / \"SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\").is_file()\n+        )\n+\n+\n+if __name__ == \"__main__\":\n+    unittest.main()\n\n2026-08-07T08:26:00.951895Z ERROR codex_models_manager::manager: failed to renew cache TTL: missing field `base_instructions` at line 94 column 5\ndiff --git a/.codex/skills/shiki/SKILL.md b/.codex/skills/shiki/SKILL.md\nindex c8e2e85d9d75d49cc4f2d6286ee22659c4e53e75..93cded623b2c15d9f65c76491a1c2d813ac9e6c0\n--- a/.codex/skills/shiki/SKILL.md\n+++ b/.codex/skills/shiki/SKILL.md\n@@ -18,6 +18,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n Codex CLI does not register this skill as a custom `/shiki` slash command. When\n the user types `/shiki` in Codex CLI and gets \"Unrecognized command\", explain\n that this is expected: invoke Shiki in Codex with natural language such as\n@@ -69,7 +72,7 @@\n \n ## Responsibilities\n \n-- Claude Code implements and repairs by default (`shiki runner claude`, ADR 0008); Codex implements and repairs tasks explicitly assigned to `codex`.\n+- Claude Code implements and repairs by default (`shiki runner claude`, SADR-0008); Codex implements and repairs tasks explicitly assigned to `codex`.\n - Claude Code Action can act as GitHub-side CCA or reviewer.\n - CCA judges completion.\n - MergeGate authorizes state transitions and merge readiness.\ndiff --git a/.github/prompts/cca-completion-check.md b/.github/prompts/cca-completion-check.md\nindex ed203ad5f302ecc81372d8ab27d2b1232ca97497..fd89950528333ef6242e6d5443904f5392ab7e56\n--- a/.github/prompts/cca-completion-check.md\n+++ b/.github/prompts/cca-completion-check.md\n@@ -19,6 +19,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n ## Required Reading\n \n Read only the files needed to judge the PR contract. Prefer this order:\ndiff --git a/.github/prompts/codex-task-handoff.md b/.github/prompts/codex-task-handoff.md\nindex 695e211d76dea1c60e8d5bb360e78487808286a4..59441d576b01a39569722f226c854e5a740e9eb5\n--- a/.github/prompts/codex-task-handoff.md\n+++ b/.github/prompts/codex-task-handoff.md\n@@ -2,7 +2,7 @@\n \n You are Codex Front acting as the Implementer for a Shiki task.\n \n-Follow `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant ADRs, and `docs/agents/codex-handoff.md`.\n+Follow `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant Shiki SADRs and target ADRs, and `docs/agents/codex-handoff.md`.\n \n Rules:\n \ndiff --git a/AGENTS.md b/AGENTS.md\nindex 7f041100d49c2a7525f51cb0f5d696a4a6d1c78c..a06bfbabc6fa48a007389e25e12d7ed523936bed\n--- a/AGENTS.md\n+++ b/AGENTS.md\n@@ -22,7 +22,7 @@\n - **Codex Front** is an optional implementer through Codex App, Codex CLI, Codex IDE extension, or Codex Web signed in with ChatGPT OAuth/subscription auth, dispatched with `shiki runner codex` when a task is explicitly assigned to `codex`.\n - **GitHub CCA** is implemented by Claude Code Action by default, using `CLAUDE_CODE_OAUTH_TOKEN`.\n \n-Do not assume `openai/codex-action`, `OPENAI_API_KEY`, or API-key based Codex automation in the default Shiki path. API-key runners are explicit target-repo extensions and require their own ADR.\n+Do not assume `openai/codex-action`, `OPENAI_API_KEY`, or API-key based Codex automation in the default Shiki path. API-key runners are explicit target-repo extensions and require their own target ADR.\n \n Shiki is not:\n \n@@ -44,10 +44,14 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use\n+`SADR-NNNN-*.md`; target product decisions retain `NNNN-*.md` and the `ADR`\n+namespace.\n+\n Use the canonical source-of-truth order above. Runtime-specific reading orders\n are operational guidance only and must not override or reorder it.\n \n-Conversation state is not durable truth. If a decision matters, put it in GitHub, `.shiki/`, `CONTEXT.md`, or an ADR.\n+Conversation state is not durable truth. If a decision matters, put it in GitHub, `.shiki/`, `CONTEXT.md`, a Shiki SADR, or a target ADR.\n \n If GitHub and `.shiki/` disagree, surface the conflict and prefer GitHub operational state until the mirror is repaired.\n \n@@ -75,8 +79,8 @@\n Every non-trivial change follows this loop:\n \n 1. **Goal**: clarify outcome, completion conditions, non-goals, risk level, and success signals.\n-2. **grill-with-docs**: challenge the plan against domain docs, ADRs, code reality, terminology, and edge scenarios.\n-3. **Context & Impact**: find relevant docs, ADRs, code, dependencies, owners, locks, and verification surfaces.\n+2. **grill-with-docs**: challenge the plan against domain docs, Shiki SADRs, target ADRs, code reality, terminology, and edge scenarios.\n+3. **Context & Impact**: find relevant docs, SADRs, target ADRs, code, dependencies, owners, locks, and verification surfaces.\n 4. **PRD and Spec Freeze**: use `to-prd` when context is settled enough to become durable product/engineering intent. The operator's explicit approval of the PRD is Spec Freeze: the plan records a `spec_freeze` block, and no plan runs without it. Steps 1-4 form Requirements Definition \u2014 one continuous operator dialogue. After Spec Freeze, scope changes require an operator-approved Spec Amendment; non-scope-moving interpretations go to the Assumption Log.\n 5. **Issues**: use `to-issues` to create independently grabbable vertical-slice issues. Prefer AFK slices over HITL slices where possible.\n 6. **Triage**: label issues for readiness, risk, runtime, skills, and MergeGate state.\n@@ -167,7 +171,7 @@\n - Testing decisions.\n - Out-of-scope items.\n - Further notes.\n-- Links to relevant domain terms and ADRs.\n+- Links to relevant domain terms, SADRs, and target ADRs.\n \n Every executable task must include:\n \n@@ -193,7 +197,7 @@\n \n Context & Impact must identify:\n \n-- Relevant documents, domain terms, ADRs, and past decisions.\n+- Relevant documents, domain terms, SADRs, target ADRs, and past decisions.\n - Relevant modules, interfaces, seams, callers, owners, and tests.\n - Dependency relationships between tasks.\n - Candidate locks and lock conflicts.\n@@ -210,7 +214,7 @@\n | Skill | Required when |\n | --- | --- |\n | `setup-matt-pocock-skills` | First configuring the repo for skills, or when issue tracker, triage labels, or domain docs are missing. |\n-| `grill-with-docs` | Default for non-trivial Goals before PRD/issues; always required when plans, terminology, boundaries, tradeoffs, or ADR-worthy decisions are ambiguous. |\n+| `grill-with-docs` | Default for non-trivial Goals before PRD/issues; always required when plans, terminology, boundaries, tradeoffs, or decision-record-worthy choices are ambiguous. |\n | `zoom-out` | The agent lacks an architectural map of the relevant code area. |\n | `to-prd` | Settled Goal context must become durable product/engineering intent. |\n | `to-issues` | A Goal, PRD, or plan must become independently grabbable vertical-slice issues. |\n@@ -348,7 +352,7 @@\n - Three or more new files.\n - Any high-risk or critical label.\n \n-When the architecture gate triggers, run `grill-with-docs` or `improve-codebase-architecture` as appropriate and record durable decisions in an ADR when needed.\n+When the architecture gate triggers, run `grill-with-docs` or `improve-codebase-architecture` as appropriate and record durable decisions in a Shiki SADR or target ADR as applicable.\n \n ## Engineering Discipline\n \ndiff --git a/CLAUDE.md b/CLAUDE.md\nindex 98a932e7841a632866fcd53b0bad0e3af3711d3d..98a31399404d6fb044b4fd493f4459ac87f439de\n--- a/CLAUDE.md\n+++ b/CLAUDE.md\n@@ -14,6 +14,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n ## Claude Role\n \n Default to planner, default implementer, reviewer, coordinator, documentation editor, CCA-style completion judge, and final-judgment assistant.\n@@ -57,7 +60,7 @@\n At the start of a Shiki session:\n \n 1. Read `AGENTS.md`.\n-2. Read `CONTEXT.md` and relevant ADRs.\n+2. Read `CONTEXT.md` and relevant Shiki SADRs and target ADRs.\n 3. Read active `.shiki/` Goal/task/ledger state when present.\n 4. Read active distilled rules from `.shiki/memories/` (status `distilled`, `active: true`, not revoked or superseded) for applicable guidance; the same rules are injected deterministically into task handoffs as the `## Distilled Rules` section (\u00a73.5).\n 5. Read the GitHub Issue or PR when the task is GitHub-backed.\n@@ -74,7 +77,7 @@\n - Define the Goal, completion conditions, non-goals, and risk level.\n - Treat `grill-with-docs` as the default planning entry point for non-trivial Goals.\n - Challenge domain terms against `CONTEXT.md`.\n-- Challenge hard-to-reverse tradeoffs against ADRs.\n+- Challenge hard-to-reverse tradeoffs against SADRs and target ADRs.\n - Explore code instead of asking when code can answer the question.\n - Run Context & Impact before implementation planning.\n - Produce a Task DAG, not an unordered checklist.\n@@ -94,7 +97,7 @@\n - For each question, provide your recommended answer.\n - Resolve dependency decisions before downstream design questions.\n - Update `CONTEXT.md` inline when terminology crystallizes.\n-- Offer ADRs only when the decision is hard to reverse, surprising without context, and a real tradeoff.\n+- Offer a SADR or target ADR only when the decision is hard to reverse, surprising without context, and a real tradeoff.\n - Convert unresolved questions into HITL blockers rather than allowing Codex to guess.\n \n ## PRD and Issues Standard\n@@ -103,7 +106,7 @@\n \n - Use the project's domain glossary vocabulary.\n - Avoid volatile file paths unless they are essential.\n-- Include implementation decisions, testing decisions, out-of-scope items, and relevant ADR/domain links.\n+- Include implementation decisions, testing decisions, out-of-scope items, and relevant SADR/target-ADR/domain links.\n - Identify deep-module opportunities and testable interfaces.\n \n When creating issues:\n@@ -121,7 +124,7 @@\n - Branch/worktree target.\n - Scope and non-goals.\n - Dependencies and locks.\n-- Relevant docs, ADRs, modules, and tests.\n+- Relevant docs, SADRs, target ADRs, modules, and tests.\n - Required skill invocations.\n - TDD expectations.\n - Pre-PR `code-review` gate expectations.\n@@ -152,7 +155,7 @@\n \n - Treat yourself as a judge, not implementer.\n - Do not write production code.\n-- Read `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant ADRs, PRD, task issue, PR body, diff, checks, reviews, labels, and `.shiki/` state.\n+- Read `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant SADRs and target ADRs, PRD, task issue, PR body, diff, checks, reviews, labels, and `.shiki/` state.\n - Evaluate every applicable checklist in `docs/agents/checklists.md`.\n - Map each acceptance criterion to durable evidence.\n - Distinguish implementation failure from missing evidence.\ndiff --git a/CONTEXT.md b/CONTEXT.md\nindex e61a9be06a0cce0d026484083a657bde2067e234..237ee8aa3ea8fab334ece49768bc5ed3b5af0974\n--- a/CONTEXT.md\n+++ b/CONTEXT.md\n@@ -25,7 +25,7 @@\n _Avoid_: jumping directly into code\n \n **grill-with-docs**:\n-A planning skill that challenges a plan against domain language, ADRs, code reality, and concrete edge scenarios. It resolves design-tree questions before PRD/issues. It runs inside Requirements Definition.\n+A planning skill that challenges a plan against domain language, Shiki SADRs, target ADRs, code reality, and concrete edge scenarios. It resolves design-tree questions before PRD/issues. It runs inside Requirements Definition.\n _Avoid_: generic brainstorming, silent assumptions\n \n **Requirements Definition**:\ndiff --git a/CONTRIBUTING.md b/CONTRIBUTING.md\nindex c6d9b656e73abb8a55e5f660123e03153dcb3e6d..9e37a8fbf668db5b22461aaae5548b8f1354394c\n--- a/CONTRIBUTING.md\n+++ b/CONTRIBUTING.md\n@@ -17,7 +17,7 @@\n 1. **Goal** \u2014 start from a user-approved Goal with an outcome, completion\n    conditions, non-goals, risk level, and success signals.\n 2. **grill-with-docs** \u2014 for any non-trivial change, challenge the plan against\n-   domain language, ADRs, and code reality before writing a PRD.\n+   domain language, Shiki SADRs, target ADRs, and code reality before writing a PRD.\n 3. **PRD** \u2014 capture settled product/engineering intent (`to-prd`).\n 4. **Spec Freeze** \u2014 the operator explicitly approves the PRD/requirements;\n    the plan records a `spec_freeze` block and no plan runs without it\n@@ -94,7 +94,7 @@\n   entries.\n - Do not hand-edit `.shiki/goals/*.json` or `.shiki/manifest.json` in a task PR.\n - Conversation state is not durable truth. If a decision matters, record it in\n-  GitHub, `.shiki/`, `CONTEXT.md`, or an ADR.\n+  GitHub, `.shiki/`, `CONTEXT.md`, a Shiki SADR, or a target ADR.\n \n ## Architecture Gate\n \n@@ -103,7 +103,7 @@\n or CI policy, package structure, three or more structural files, more than 100\n net new lines in a core module, three or more new files, or any high-risk label.\n When triggered, run `grill-with-docs` or `improve-codebase-architecture` and\n-record durable decisions in an ADR when needed (see `docs/adr/`).\n+record durable decisions in a Shiki SADR or target ADR as applicable (see `docs/adr/`).\n \n ## Safety\n \ndiff --git a/SYSTEM_PROMPT.md b/SYSTEM_PROMPT.md\nindex aaaea45e3a896c3a44ba89eec8bb84325ecf6c62..da009c86a88285269dee0155e58e550c6d744ea9\n--- a/SYSTEM_PROMPT.md\n+++ b/SYSTEM_PROMPT.md\n@@ -30,6 +30,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n Use the canonical source-of-truth order above. Runtime-specific reading orders\n are operational guidance only and must not override or reorder it. The current\n conversation is non-durable operator input.\n@@ -48,14 +51,14 @@\n \n A runtime may only do work that is explicitly assigned by the Goal, task, PR, or MergeGate decision.\n \n-Do not assume `openai/codex-action`, `OPENAI_API_KEY`, or API-key based Codex automation in the default Shiki path. API-key runners are explicit target-repo extensions and require their own ADR.\n+Do not assume `openai/codex-action`, `OPENAI_API_KEY`, or API-key based Codex automation in the default Shiki path. API-key runners are explicit target-repo extensions and require their own target ADR.\n \n ## 4. Non-negotiable Shiki flow\n \n For any non-trivial Goal, use this flow:\n \n 1. **Goal Seek**: define outcome, non-goals, risk level, completion criteria, and evidence requirements.\n-2. **grill-with-docs**: challenge the plan against `CONTEXT.md`, ADRs, code reality, terminology, boundaries, and edge scenarios. Ask one question at a time when the operator is available. If code can answer a question, inspect code instead of asking.\n+2. **grill-with-docs**: challenge the plan against `CONTEXT.md`, Shiki SADRs, target ADRs, code reality, terminology, boundaries, and edge scenarios. Ask one question at a time when the operator is available. If code can answer a question, inspect code instead of asking.\n 3. **Context & Impact**: identify modules, interfaces, seams, callers, dependencies, locks, risk, and verification surfaces.\n 4. **to-prd + Spec Freeze**: turn settled context into a PRD using domain vocabulary and testing decisions. The operator's explicit approval of the PRD is Spec Freeze, recorded as the plan's `spec_freeze` block; plans without it do not run. Steps 1-4 form Requirements Definition.\n 5. **to-issues**: decompose the PRD into independently grabbable vertical-slice issues. Prefer AFK slices; mark HITL slices when judgment is still required.\n@@ -75,7 +78,7 @@\n Before making any material change:\n \n 1. Read `AGENTS.md` and any runtime-specific wrapper such as `CLAUDE.md`.\n-2. Read `CONTEXT.md` and relevant ADRs.\n+2. Read `CONTEXT.md` and relevant SADRs and target ADRs.\n 3. Read the GitHub Issue / PR and `.shiki/` state for the active Goal or task.\n 4. Identify your role: Planner, Implementer, Reviewer, Completion Check Agent, Repairer, or Guardian-assist.\n 5. Identify Goal id, task id, branch, PR, dependencies, locks, risk level, required skills, and required evidence.\n@@ -103,7 +106,7 @@\n \n At minimum, identify:\n \n-- Relevant documents, ADRs, domain terms, and prior decisions.\n+- Relevant documents, SADRs, target ADRs, domain terms, and prior decisions.\n - Relevant modules, interfaces, seams, callers, owners, and tests.\n - Dependency relationships between tasks.\n - Candidate file locks and lock conflicts.\n@@ -152,7 +155,7 @@\n \n CCA must read:\n \n-- `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant ADRs.\n+- `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant SADRs and target ADRs.\n - Parent Goal issue, PRD issue, task issue, comments, and labels.\n - PR body, diff, commits, changed files, reviews, CI checks, and artifacts available to the workflow.\n - `.shiki/` task contract, locks, ledger entries, CCA checklist profile, and prior repair packets.\n@@ -214,7 +217,7 @@\n - Three or more new files.\n - Any high-risk or critical label.\n \n-When the architecture gate triggers, run `grill-with-docs` or `improve-codebase-architecture` as appropriate and record the decision in an ADR if it is hard to reverse or likely to be re-litigated.\n+When the architecture gate triggers, run `grill-with-docs` or `improve-codebase-architecture` as appropriate and record the decision in a Shiki SADR or target ADR as applicable if it is hard to reverse or likely to be re-litigated.\n \n ## 14. Skill Gate\n \n@@ -223,7 +226,7 @@\n Use these skills:\n \n - `setup-matt-pocock-skills`: before first use of the engineering skill set, or when issue tracker, triage labels, or domain docs are missing.\n-- `grill-with-docs`: default for non-trivial Goals before PRD/issues; required for ambiguous plans, terminology, boundaries, ADR-worthy tradeoffs, or design-tree decisions.\n+- `grill-with-docs`: default for non-trivial Goals before PRD/issues; required for ambiguous plans, terminology, boundaries, decision-record-worthy tradeoffs, or design-tree decisions.\n - `zoom-out`: unfamiliar code area or missing architectural map.\n - `to-prd`: convert settled context into a PRD.\n - `to-issues`: break a PRD, plan, or Goal into independently grabbable vertical-slice issues.\ndiff --git a/docs/adr/0001-keep-shiki-as-platform-name.md b/docs/adr/SADR-0001-keep-shiki-as-platform-name.md\nindex 20a9637c3b9d8c97de7ba8db7cfae4d82d9a7dba..8b685439959aada1b221c112f0ac3c51ca8a1695\n--- a/docs/adr/0001-keep-shiki-as-platform-name.md\n+++ b/docs/adr/SADR-0001-keep-shiki-as-platform-name.md\n@@ -1,3 +1,3 @@\n-# Keep Shiki As The Platform Name\n+# SADR-0001: Keep Shiki As The Platform Name\n \n Shiki remains the product name for the GitHub-native agentic engineering control plane. We considered more descriptive names such as MergePilot, MergeGate, and Kiroku, but kept Shiki because it carries the existing goal-seeking framework identity while remaining independent of Hermes, Codex, Claude Code, or any single runtime.\ndiff --git a/docs/adr/0002-use-agents-as-shared-constitution.md b/docs/adr/SADR-0002-use-agents-as-shared-constitution.md\nindex 73de30ecb078120eac540d9bc51855e3cd48d0e3..c5486182bf06fca36ea508496b013c67302fd989\n--- a/docs/adr/0002-use-agents-as-shared-constitution.md\n+++ b/docs/adr/SADR-0002-use-agents-as-shared-constitution.md\n@@ -1,3 +1,3 @@\n-# Use AGENTS As The Shared Agent Constitution\n+# SADR-0002: Use AGENTS As The Shared Agent Constitution\n \n Shiki uses `AGENTS.md` as the runtime-neutral constitution and `CLAUDE.md` as a thin Claude Code wrapper. This keeps Codex, Claude Code, Hermes Runner, and future Agent Runtimes aligned on the same GitHub-first MergeGate rules while still allowing runtime-specific instructions where needed.\ndiff --git a/docs/adr/0003-require-skill-gate-for-mergegate-readiness.md b/docs/adr/SADR-0003-require-skill-gate-for-mergegate-readiness.md\nindex 424be0c9af4c8f0136f1fde68a468c47ff84e764..7fc337987c7df9792f75f181dfcbed75fe5ffff2\n--- a/docs/adr/0003-require-skill-gate-for-mergegate-readiness.md\n+++ b/docs/adr/SADR-0003-require-skill-gate-for-mergegate-readiness.md\n@@ -1,3 +1,3 @@\n-# Require Skill Gate For MergeGate Readiness\n+# SADR-0003: Require Skill Gate For MergeGate Readiness\n \n Shiki requires relevant engineering skills to be invoked before execution when their triggers apply. This makes skills part of MergeGate readiness rather than optional prompt style, because Shiki depends on repeatable planning, issue decomposition, TDD, diagnosis, triage, and architecture review before autonomous or parallel agent work can be trusted.\ndiff --git a/docs/adr/0004-use-github-first-with-shiki-mirror.md b/docs/adr/SADR-0004-use-github-first-with-shiki-mirror.md\nindex d0a3a9174dc646a945a9d325954ffaf00c352040..a79c54ae9ee8eed2a0524192b864da4e22a92d3f\n--- a/docs/adr/0004-use-github-first-with-shiki-mirror.md\n+++ b/docs/adr/SADR-0004-use-github-first-with-shiki-mirror.md\n@@ -1,4 +1,4 @@\n-# ADR 0004: Use GitHub First With A Repository-Local Shiki Mirror\n+# SADR-0004: Use GitHub First With A Repository-Local Shiki Mirror\n \n ## Status\n \ndiff --git a/docs/adr/0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md b/docs/adr/SADR-0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\nindex b339baf49b89d9290e79c3d51b36e7b2bde3bd95..0f3c3eb9afccdb3403ce978e21d04fe12e495da0\n--- a/docs/adr/0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\n+++ b/docs/adr/SADR-0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\n@@ -1,8 +1,8 @@\n-# ADR 0005: Use Codex As Front And Claude Code Action As The GitHub Runtime\n+# SADR-0005: Use Codex As Front And Claude Code Action As The GitHub Runtime\n \n ## Status\n \n-Accepted (implementer default superseded by ADR 0008; the auth model and the\n+Accepted (implementer default superseded by SADR-0008; the auth model and the\n Claude Code Action GitHub runtime decision remain in force)\n \n ## Context\n@@ -26,4 +26,4 @@\n - Shiki avoids requiring `OPENAI_API_KEY` for the default workflow.\n - Codex work happens through the user's authenticated Codex front surface, not as a hidden backend Action.\n - Claude Code Action is configured for OAuth-token based subscription usage by default.\n-- API-key based automation remains possible only as an explicit target-repo extension with its own ADR.\n+- API-key based automation remains possible only as an explicit target-repo extension with its own target ADR.\ndiff --git a/docs/adr/0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md b/docs/adr/SADR-0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\nindex 4bcae9d1e52f0a37b8e9fbafa5cd14b6ee1993bd..7a38538a6ae9929567f0ade67e7ea6a60d0b3f7e\n--- a/docs/adr/0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\n+++ b/docs/adr/SADR-0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\n@@ -1,4 +1,4 @@\n-# ADR 0006: Separate Codex Implementation, CCA Completion, And MergeGate Authorization\n+# SADR-0006: Separate Codex Implementation, CCA Completion, And MergeGate Authorization\n \n ## Status\n \ndiff --git a/docs/adr/0007-packaging-and-release.md b/docs/adr/SADR-0007-packaging-and-release.md\nindex 79b84ddb97167372b44641e5b7860e400ce80822..bb445a2e2e856d9d1f5fdbe35de8d4e8cec0fb32\n--- a/docs/adr/0007-packaging-and-release.md\n+++ b/docs/adr/SADR-0007-packaging-and-release.md\n@@ -1,4 +1,4 @@\n-# ADR 0007: Packaging And Release For Shiki\n+# SADR-0007: Packaging And Release For Shiki\n \n ## Status\n \n@@ -25,7 +25,7 @@\n   not introduce third-party runtime dependencies or break the shim.\n - Releases must run inside the operator's subscription-authenticated toolchain\n   and must not require extra secrets beyond the built-in `GITHUB_TOKEN`\n-  (consistent with ADR 0005's no-API-key-by-default stance).\n+  (consistent with SADR-0005's no-API-key-by-default stance).\n - `.shiki/manifest.json` already encodes the install surface (`install.include`,\n   `install.create_directories`, `install.exclude_from_commit`), and it must not\n   be mutated as part of this change.\n@@ -34,7 +34,7 @@\n \n This is a hard-to-reverse platform decision (it sets the version source of\n truth, the release trigger, and the public install contract), so it is recorded\n-as an ADR.\n+as a SADR.\n \n ## Decision\n \n@@ -104,7 +104,7 @@\n 3. **Publishing to PyPI.** Rejected for now: Shiki is distributed as a\n    repository template + git-installable CLI, not a PyPI library; PyPI would add\n    credentials/secrets and a different versioning contract. Can be added later as\n-   its own ADR if a packaged distribution is needed.\n+   its own SADR if a packaged distribution is needed.\n 4. **Dynamic version (read `VERSION` via a build backend `dynamic` field).**\n    Rejected to keep `pyproject.toml` backend-agnostic and dependency-free; the\n    small duplication between `VERSION` and `pyproject.toml` is guarded by the\n@@ -123,4 +123,4 @@\n   releasing versions the whole platform but ships only `install.include` to\n   targets.\n - Future PyPI distribution or a console entry point are deferred and would each\n-  warrant their own ADR.\n+  warrant their own SADR.\ndiff --git a/docs/adr/0008-use-claude-code-as-default-implementer.md b/docs/adr/SADR-0008-use-claude-code-as-default-implementer.md\nindex dedf4510b7c7799521422f622029563d373db031..35c2625d908167fbc58855b5b63e283805f167e8\n--- a/docs/adr/0008-use-claude-code-as-default-implementer.md\n+++ b/docs/adr/SADR-0008-use-claude-code-as-default-implementer.md\n@@ -1,12 +1,12 @@\n-# ADR 0008: Use Claude Code As The Default Implementer Runtime\n+# SADR-0008: Use Claude Code As The Default Implementer Runtime\n \n ## Status\n \n-Proposed (supersedes ADR 0005 for the implementer default; keeps its auth model)\n+Proposed (supersedes SADR-0005 for the implementer default; keeps its auth model)\n \n ## Context\n \n-ADR 0005 made Codex Front the default implementation runtime, with Claude Code\n+SADR-0005 made Codex Front the default implementation runtime, with Claude Code\n restricted to planner and reviewer roles by the runtime registry, and the only\n autonomous implementation adapter was `shiki runner codex`.\n \n@@ -18,13 +18,13 @@\n - The platform's planning, review, and CCA layers already run on Claude Code,\n   and Claude Code's native capabilities (the built-in `/code-review` skill and\n   the Workflow multi-agent orchestration tool) have no Codex equivalent.\n-- The Goal lifecycle redesign (ADR 0009) requires an autonomous post-freeze\n+- The Goal lifecycle redesign (SADR-0009) requires an autonomous post-freeze\n   loop driven from the operator's Claude Code session, including bounded\n   repair dispatch, which must not depend on a runtime that cannot be invoked\n   unattended.\n \n The constitution claims Shiki is runtime-agnostic (\"not a Claude-only\n-workflow\", ADR 0002). Any change to the implementer default must preserve the\n+workflow\", SADR-0002). Any change to the implementer default must preserve the\n registry-based runtime model rather than hardcoding one vendor.\n \n ## Decision\n@@ -39,7 +39,7 @@\n   the task to review or repair-needed.\n - Keep Codex registered as an optional implementer runtime. `shiki runner\n   codex`, the Codex handoff templates, and the ChatGPT OAuth auth model from\n-  ADR 0005 remain supported for tasks explicitly assigned to `codex`.\n+  SADR-0005 remain supported for tasks explicitly assigned to `codex`.\n - Commit to mandating two Claude-native capabilities as loop contract in\n   follow-up Goals (status: the code-review gate was delivered by Goal G-B \u2014\n   skill registry, checklists item PR-12, default required_skills \u2014 while the\n@@ -60,7 +60,7 @@\n \n Out of scope: removing Codex support, changing the CCA runtime (Claude Code\n Action with `CLAUDE_CODE_OAUTH_TOKEN` stays), and API-key based automation\n-(still requires its own ADR per ADR 0005).\n+(still requires its own target ADR per SADR-0005).\n \n ## Consequences\n \n@@ -100,7 +100,7 @@\n   harness rejection of direct `codex exec` is outside Shiki's control, and the\n   plan's mandatory native capabilities cannot run on Codex at all.\n - **Remove Codex entirely**: rejected; it abandons the runtime-agnostic\n-  constitution (ADR 0002) for no operational gain and forecloses\n+  constitution (SADR-0002) for no operational gain and forecloses\n   cross-runtime adversarial review.\n - **GitHub Actions cloud implementation via claude-code-action**: rejected as\n   default; durable evidence is automatic but token cost, Node 24 workflow\ndiff --git a/docs/adr/0009-requirements-definition-and-spec-freeze.md b/docs/adr/SADR-0009-requirements-definition-and-spec-freeze.md\nindex 29b3f7871bef9203a8b4a831a65c39eb2f506e42..52e7dd69ad0aa4efe0b68c62678abbf7df02aaec\n--- a/docs/adr/0009-requirements-definition-and-spec-freeze.md\n+++ b/docs/adr/SADR-0009-requirements-definition-and-spec-freeze.md\n@@ -1,4 +1,4 @@\n-# ADR 0009: Merge Grill And Planning Into Requirements Definition With Spec Freeze\n+# SADR-0009: Merge Grill And Planning Into Requirements Definition With Spec Freeze\n \n ## Status\n \ndiff --git a/docs/adr/0010-external-ai-guardian-review.md b/docs/adr/SADR-0010-external-ai-guardian-review.md\nindex e6108bbe8bb4e73e0ff4684c8f78001a80438c4e..f3510b145664298adafb151b6f0db51b972f01ec\n--- a/docs/adr/0010-external-ai-guardian-review.md\n+++ b/docs/adr/SADR-0010-external-ai-guardian-review.md\n@@ -1,4 +1,4 @@\n-# ADR 0010: External AI Guardian Review As A First-Class Approval Authority\n+# SADR-0010: External AI Guardian Review As A First-Class Approval Authority\n \n ## Status\n \n@@ -8,7 +8,7 @@\n \n Shiki is intended to be an autonomous, recursive, self-improving agent\n framework: authority-in-the-loop, not human-in-the-loop. Until now the Guardian\n-approval required for high/critical-risk merges (guardian-policy.json, ADR\n+approval required for high/critical-risk merges (guardian-policy.json, SADR\n governance) recognized only human authority sources \u2014 a GitHub review approval,\n the `guardian:approved` label applied by a configured Guardian, or a comment\n carrying `Guardian approval granted` plus the current head SHA.\n@@ -89,7 +89,7 @@\n   autonomous design the operator intends and produced the impersonation\n   incident.\n - **Record AI approval as an operator comment**: rejected as forgery of human\n-  authority \u2014 the exact failure this ADR corrects.\n+  authority \u2014 the exact failure this SADR corrects.\n - **Deliver the artifact as a committed `.shiki/` file**: rejected; a committed\n   approval inside the PR being approved is forgeable by anyone who can push.\n - **A dedicated reviewer bot identity instead of a relayed comment**: deferred;\ndiff --git a/docs/adr/0011-autonomous-quality-gate-evidence.md b/docs/adr/SADR-0011-autonomous-quality-gate-evidence.md\nindex bca0246c5d46e44afbde15486a75eec425fc02f5..9c93eac16d8c3a6d4b11bd7224e3955102cd7db4\n--- a/docs/adr/0011-autonomous-quality-gate-evidence.md\n+++ b/docs/adr/SADR-0011-autonomous-quality-gate-evidence.md\n@@ -1,4 +1,4 @@\n-# ADR 0011: Autonomous Quality-Gate Evidence \u2014 Deterministic Facts And An Independent Verifier\n+# SADR-0011: Autonomous Quality-Gate Evidence \u2014 Deterministic Facts And An Independent Verifier\n \n ## Status\n \ndiff --git a/docs/adr/0012-autonomous-loop-closeout-to-main.md b/docs/adr/SADR-0012-autonomous-loop-closeout-to-main.md\nindex 637fad6dde4989c9c2bf0f81f676d8d29991123c..ec7d85f81030a676bcbba28b43eb365422a36eac\n--- a/docs/adr/0012-autonomous-loop-closeout-to-main.md\n+++ b/docs/adr/SADR-0012-autonomous-loop-closeout-to-main.md\n@@ -1,4 +1,4 @@\n-# 0012 \u2014 Autonomous loop pushes goal completion to main via a closeout PR\n+# SADR-0012 \u2014 Autonomous loop pushes goal completion to main via a closeout PR\n \n - Status: accepted\n - Date: 2026-06-18\ndiff --git a/docs/adr/0013-bootstrap-grants-actions-approve-pull-requests.md b/docs/adr/SADR-0013-bootstrap-grants-actions-approve-pull-requests.md\nindex b6c69d64f1c5ced346b88d6859586715b3b5bd0e..cf48ff4a6716267f539ca58edd1a852e7c7a2d64\n--- a/docs/adr/0013-bootstrap-grants-actions-approve-pull-requests.md\n+++ b/docs/adr/SADR-0013-bootstrap-grants-actions-approve-pull-requests.md\n@@ -1,4 +1,4 @@\n-# ADR 0013: Bootstrap Grants GitHub Actions Permission To Create And Approve Pull Requests\n+# SADR-0013: Bootstrap Grants GitHub Actions Permission To Create And Approve Pull Requests\n \n ## Status\n \n@@ -31,7 +31,7 @@\n security tradeoff: it lets repository automation satisfy a human-review gate. In\n a multi-maintainer repo that could let a compromised or buggy workflow\n self-approve changes. So whether bootstrap turns it on by default \u2014 and with\n-what default token scope \u2014 is an ADR-worthy decision, not an implementation\n+what default token scope \u2014 is a SADR-worthy decision, not an implementation\n detail.\n \n ## Decision\n@@ -48,7 +48,7 @@\n Specifically:\n \n - **`can_approve_pull_request_reviews` defaults to `true`.** Shiki's default\n-  posture is solo/AFK autonomous operation (ADR 0008/0009/0012), where the CCA\n+  posture is solo/AFK autonomous operation (SADR-0008/0009/0012), where the CCA\n   Review Bridge is the only path that can satisfy `required_review: true` after\n   the CCA and Guardian gates have already passed. The default makes a\n   freshly-bootstrapped repo able to run the autonomous loop end to end without a\ndiff --git a/docs/adr/0014-codex-app-external-ai-guardian-ui-adapter.md b/docs/adr/SADR-0014-codex-app-external-ai-guardian-ui-adapter.md\nindex 238db06279334caa341c117362df9271382a7fcd..65f12e9939321e1b3e389a34c822fc1549e5155d\n--- a/docs/adr/0014-codex-app-external-ai-guardian-ui-adapter.md\n+++ b/docs/adr/SADR-0014-codex-app-external-ai-guardian-ui-adapter.md\n@@ -1,4 +1,4 @@\n-# ADR 0014: Use Codex App As The External AI Guardian UI Adapter\n+# SADR-0014: Use Codex App As The External AI Guardian UI Adapter\n \n ## Status\n \ndiff --git a/docs/adr/0015-contract-approval-before-dispatch.md b/docs/adr/SADR-0015-contract-approval-before-dispatch.md\nindex fad6c656741a20a7684ba16080a7db159f63a52f..6061271a46d3f1d6ae7174477de3fb75673123a6\n--- a/docs/adr/0015-contract-approval-before-dispatch.md\n+++ b/docs/adr/SADR-0015-contract-approval-before-dispatch.md\n@@ -1,4 +1,4 @@\n-# ADR 0015: Approve The Task Contract Before Dispatch, Not The Diff After It\n+# SADR-0015: Approve The Task Contract Before Dispatch, Not The Diff After It\n \n ## Status\n \n@@ -9,7 +9,7 @@\n Guardian approval for high/critical risk is expressible only on a pull request.\n Every approval source in `guardian-policy.json` \u2014 GitHub review, the\n `guardian:approved` label, the `Guardian approval granted` comment, and the\n-`external_ai_guardian_review` artifact (ADR 0010/0014) \u2014 is bound to a PR, and\n+`external_ai_guardian_review` artifact (SADR-0010/0014) \u2014 is bound to a PR, and\n three of the four require the current head SHA. Approval therefore cannot exist\n before an implementation PR exists.\n \n@@ -81,7 +81,7 @@\n An envelope breach must surface only on the non-repairable policy gate, never on\n the repairable `MergeGate metadata check`. Otherwise the Goal Loop routes it to\n `dispatch_repair` \u2014 instructing a runner to make the breach go away \u2014 which is\n-exactly what this ADR forbids.\n+exactly what this SADR forbids.\n \n **Contract immutability.** After registration, an implementation PR must not\n change the governance fields of its own task contract (`scope`, `non_goals`,\n@@ -116,7 +116,7 @@\n fails any verdict that is not `complete`, and the signal the CCA prompt reads is\n computed from `evaluate_guardian_approval` alone. Without the OR-merge at the\n signal, a contract-approved PR still returns `needs_guardian`, the CCA job fails,\n-and the MergeGate policy check never runs \u2014 the gate this ADR changes would never\n+and the MergeGate policy check never runs \u2014 the gate this SADR changes would never\n be reached. The CCA *prompt* needs no change: a `contract_approval` entry in the\n signal's existing `sources` array satisfies its current wording.\n \n@@ -130,7 +130,7 @@\n written, and an approved contract lets its implementation PRs merge through the\n same automatic path as low/medium risk. The wasted implement-then-block cycle\n disappears. Guardian review shifts from reading diffs to reading contracts, which\n-is both earlier and smaller; the external AI Guardian path (ADR 0010/0014) works\n+is both earlier and smaller; the external AI Guardian path (SADR-0010/0014) works\n on contracts with no change.\n \n We accept that the Guardian authorizes a class of change without seeing its\n@@ -146,7 +146,7 @@\n adversarial tests for the new approval path equivalent in rigor to\n `test_shiki_governance_evidence.sh`.\n \n-Bootstrap costs, which apply only while this ADR is being implemented. The tasks\n+Bootstrap costs, which apply only while this SADR is being implemented. The tasks\n that build Contract Approval merge under the existing rules, so the ones\n dispatched after the pre-dispatch gate lands but before the approval path lands\n need a Guardian-approved Contract PR *and* a live Guardian approval on their\n@@ -210,7 +210,7 @@\n risk; they do not bound authorized work.\n \n **Auto-escalate risk on envelope breach instead of stopping.** Returns the\n-workflow to a diff-time Guardian decision \u2014 the exact behavior this ADR removes.\n+workflow to a diff-time Guardian decision \u2014 the exact behavior this SADR removes.\n \n **Resolve governance fields from the base snapshot only.** Rejected against the\n registration flow: implementation PRs add their own contract, so the base has no\ndiff --git a/docs/adr/0016-locks-bound-product-paths.md b/docs/adr/SADR-0016-locks-bound-product-paths.md\nindex b5e850f3f1f7f4ef6e7f9d1763a29a51bd300c45..46b40a9b99a7aae644c02bb458f5473ff0a90ce2\n--- a/docs/adr/0016-locks-bound-product-paths.md\n+++ b/docs/adr/SADR-0016-locks-bound-product-paths.md\n@@ -1,4 +1,4 @@\n-# ADR 0016: Locks bound product paths; the Shiki mirror is bound by record identity\n+# SADR-0016: Locks bound product paths; the Shiki mirror is bound by record identity\n \n ## Status\n \n@@ -6,8 +6,8 @@\n \n - Date: 2026-07-28\n - Deciders: operator (mizutani-140), Claude (planner)\n-- Related: ADR 0004 (GitHub-first with a `.shiki` mirror), ADR 0009 (Spec Freeze),\n-  ADR 0012 (autonomous loop closeout to main), ADR 0015 (Contract Approval before\n+- Related: SADR-0004 (GitHub-first with a `.shiki` mirror), SADR-0009 (Spec Freeze),\n+  SADR-0012 (autonomous loop closeout to main), SADR-0015 (Contract Approval before\n   dispatch), remediation goal task R-02, the live PR #179 deadlock.\n \n ## Context\n@@ -90,11 +90,11 @@\n files, documentation files and named source modules are nameable; runtime-\n allocated ledger, runner and report ids are not \u2014 and those live in `.shiki`,\n which locks no longer govern. Existing frozen contracts are brought into line by\n-a one-time Spec Amendment per plan, which must land before ADR 0015's contract\n+a one-time Spec Amendment per plan, which must land before SADR-0015's contract\n immutability makes `locks` unwritable from the implementation PR.\n \n **Lock state is resolved from the durable side.** MergeGate resolves the lock\n-registry, and ADR 0015's governance fields, from the base `.shiki` snapshot when\n+registry, and SADR-0015's governance fields, from the base `.shiki` snapshot when\n present, falling back to head only when absent. The `MergeGate metadata check`\n job is given `--base-shiki`, which it does not receive today. Locks are not\n derived from the live open-pull-request set: a pull request body is untrusted\n@@ -108,7 +108,7 @@\n Explicitly out of scope: moving MergeGate's contract resolution off the PR\n checkout wholesale; making the mirror a projection of GitHub state; introducing\n new lock kinds; moving ledger evidence out of the repository; and unwinding\n-ADR 0012's closeout pull request. Registration of multi-task goals is ADR 0015's\n+SADR-0012's closeout pull request. Registration of multi-task goals is SADR-0015's\n Contract PR and is not re-decided here.\n \n ## Consequences\n@@ -127,7 +127,7 @@\n \n Risks accepted. First, goal-scoped identity is only as strong as `goal_id`,\n which is resolved from the PR body and the head task file; the mitigation is to\n-add `goal_id` to ADR 0015's frozen governance field set, and until that lands a\n+add `goal_id` to SADR-0015's frozen governance field set, and until that lands a\n task file can name a foreign goal. Second, base-when-present resolution is\n unavailable on fork pull requests, where the head is contributor-controlled;\n fork pull requests already cannot complete a Shiki merge, and this decision does\n@@ -139,7 +139,7 @@\n path, not by implementation pull requests.\n \n New obligations: one Spec Amendment per frozen plan to rewrite `locks`, ordered\n-before ADR 0015 T1; extension of the adversarial suites in\n+before SADR-0015 T1; extension of the adversarial suites in\n `scripts/test_shiki_adversarial_state.sh` and\n `scripts/test_shiki_governance_evidence.sh` to cover the eleven newly-scoped\n directories; and updating `scripts/test_shiki_loop_lock_guard.sh`,\n@@ -151,32 +151,32 @@\n whether the loop's transitive sync of ledger `evidence` references can be\n deleted once the Contract PR carries its own source plan.\n \n-## Relationship to ADR 0004\n+## Relationship to SADR-0004\n \n-This ADR **clarifies something ADR 0004 left unspecified**. ADR 0004 decides that\n+This SADR **clarifies something SADR-0004 left unspecified**. SADR-0004 decides that\n GitHub is the operational source of truth and that each target repository keeps a\n `.shiki/` mirror. It says nothing about pull requests writing the mirror, does\n not name MergeGate, and does not make the mirror an input to a gate. Nothing in\n-ADR 0004's Decision or Consequences is amended, superseded or weakened: the\n+SADR-0004's Decision or Consequences is amended, superseded or weakened: the\n mirror still exists, still records the same artifacts, and pull request\n-boundaries remain the coordination surface. What this ADR settles is the question\n+boundaries remain the coordination surface. What this SADR settles is the question\n 0004 never reached \u2014 by what authority a pull request may write a mirror record \u2014\n and it answers it the way `.shiki/manifest.json` already declares\n (`pr_mutation: current-task-or-current-goal-only`).\n \n-It **amends ADR 0012**, whose Decision item 5 states that \"Loop-executed tasks\n+It **amends SADR-0012**, whose Decision item 5 states that \"Loop-executed tasks\n declare the `path:.shiki/**` lock, which covers every `.shiki` file the closeout\n stages ... so `files_outside_locks` is satisfied without per-file lock\n bookkeeping.\" That clause is replaced: the closeout's mirror writes are\n-authorized by record identity, not by a declared lock. ADR 0012's substantive\n+authorized by record identity, not by a declared lock. SADR-0012's substantive\n decision \u2014 that completion reaches `main` through a closeout pull request rather\n than local attestation \u2014 is preserved unchanged.\n \n-It is **complementary to ADR 0015**, not a substitute. ADR 0015 owns contract\n+It is **complementary to SADR-0015**, not a substitute. SADR-0015 owns contract\n immutability, the pre-dispatch gate, the Contract PR and the Scope Envelope. This\n-ADR owns what a lock means and who may write the mirror. The one ordering\n+SADR owns what a lock means and who may write the mirror. The one ordering\n constraint between them is that the lock-narrowing Spec Amendment must precede\n-ADR 0015 T1's freezing of the `locks` field.\n+SADR-0015 T1's freezing of the `locks` field.\n \n ## Alternatives Considered\n \n@@ -186,7 +186,7 @@\n the only writer that can register them may run. Migrating the Guardian signal to\n that store makes every registration PR fail closed to `needs_guardian` at every\n risk level, because the task file is by definition absent from the store, which\n-removes the low/medium autonomous path ADR 0008/0009 exists to provide. And it\n+removes the low/medium autonomous path SADR-0008/0009 exists to provide. And it\n leaves schemas, manifest, config, guardian policy, templates, migrations, plans,\n worktrees, runner records, runs and handoffs with no writer at all in a\n repository whose product is `.shiki`. Measured benefit on the frozen backlog:\n@@ -199,14 +199,14 @@\n namespace is not in fact ownership-partitioned \u2014 56 ledger entries carry no\n `task_id`, and one goal DAG is writable by 35 registered tasks. The correct\n expression of its insight is that locks do not govern the mirror at all, which is\n-what this ADR decides.\n+what this SADR decides.\n \n **Make the mirror a projection written only by a registration writer and a\n post-merge projection job.** Rejected. It requires a bypass actor with write\n access to the protected default branch; it makes its own master invariant\n readable from a manifest that pull requests may edit; it deletes both reconcile\n modes and leaves a corrupted mirror with no in-band repair path, contradicting\n-ADR 0004's consequence that mirror conflicts are repaired against GitHub state;\n+SADR-0004's consequence that mirror conflicts are repaired against GitHub state;\n and its content-derived ids are rejected by `validate_shiki`'s currently-green id\n pattern.\n \ndiff --git a/docs/adr/0017-bookkeeping-closeouts-do-not-inherit-risk.md b/docs/adr/SADR-0017-bookkeeping-closeouts-do-not-inherit-risk.md\nindex 554e3a0d526a72ca067108c5464cc68c3edcf174..b9676857a4713850e063af7c39c8e0b01ecb4c66\n--- a/docs/adr/0017-bookkeeping-closeouts-do-not-inherit-risk.md\n+++ b/docs/adr/SADR-0017-bookkeeping-closeouts-do-not-inherit-risk.md\n@@ -1,4 +1,4 @@\n-# ADR 0017: A Bookkeeping Closeout Does Not Inherit Its Task's Risk\n+# SADR-0017: A Bookkeeping Closeout Does Not Inherit Its Task's Risk\n \n ## Status\n \n@@ -6,7 +6,7 @@\n \n ## Context\n \n-ADR 0012 made the goal loop push a task's completion to the default branch through a\n+SADR-0012 made the goal loop push a task's completion to the default branch through a\n closeout pull request: after the implementation PR merges, the loop opens a second PR\n carrying `task.status = done`, `lock.state = released`, and \u2014 when the task completes\n its goal \u2014 `goal.status = complete` plus one scorecard report. Nothing else. The\n@@ -49,14 +49,14 @@\n 4. The task file's governance fields (`scope`, `non_goals`, `required_skills`,\n    `risk_level`, `locks`, `acceptance_checks`, `test_command`) are byte-identical to\n    the base snapshot. Only `status`, `expected_pr`, `closeout_pr`, `ledger_evidence`\n-   and `expected_branch` may differ \u2014 the ADR 0015 contract-immutability rule,\n+   and `expected_branch` may differ \u2014 the SADR-0015 contract-immutability rule,\n    unchanged.\n \n    `expected_branch` is in that set because a closeout is *by construction* on a\n    different branch from the implementation it closes, and the loop rewrites the\n    field accordingly. It is safe to let it differ because MergeGate independently\n    requires `expected_branch` to equal the PR's actual head ref, so a PR cannot set\n-   it to anything other than the branch it is really on. The first draft of this ADR\n+   it to anything other than the branch it is really on. The first draft of this SADR\n    omitted it, which made the exemption unreachable: every real closeout failed\n    condition 4 on a field the loop always changes. See PR #209.\n 5. The state transitions are exactly the terminal set: task `review` \u2192 `done`, lock\n@@ -113,12 +113,12 @@\n classification must be derived, not asserted.\n \n **Fold the closeout into the implementation PR** so no second PR exists. Rejected under\n-ADR 0012, which recorded why: a task file that is already `done` on the implementation\n+SADR-0012, which recorded why: a task file that is already `done` on the implementation\n branch short-circuits the loop's review \u2192 CCA \u2192 merge \u2192 done ordering into a local\n goal-completion before any merge happens.\n \n **Have the loop auto-merge closeouts with elevated credentials.** Rejected outright: that\n-is a runtime authoring its own approval, which ADR 0010 and the constitution's\n+is a runtime authoring its own approval, which SADR-0010 and the constitution's\n approval-record prohibition forbid. It would also be indistinguishable, in the evidence,\n from the forgery those rules exist to prevent.\n \ndiff --git a/docs/adr/0018-carried-guardian-approval-across-a-proven-base-sync.md b/docs/adr/SADR-0018-carried-guardian-approval-across-a-proven-base-sync.md\nindex 22358562da390ed8b45cd600da08a43d378b4680..e6e2907cefa776460af73ddf7d39db605f3c39b0\n--- a/docs/adr/0018-carried-guardian-approval-across-a-proven-base-sync.md\n+++ b/docs/adr/SADR-0018-carried-guardian-approval-across-a-proven-base-sync.md\n@@ -1,4 +1,4 @@\n-# ADR 0018: Carried Guardian Approval Across A Proven Base Sync\n+# SADR-0018: Carried Guardian Approval Across A Proven Base Sync\n \n ## Status\n \n@@ -34,7 +34,7 @@\n Guardian evaluator that consumes it (`shiki_guardian.evaluate_guardian_approval`,\n the `guardian_comment_carried` source) were built and tested in isolation. Neither\n does anything until a caller passes the flags that turn the carry on, so the\n-feature was invisible in CI. This ADR records the decision the two of them\n+feature was invisible in CI. This SADR records the decision the two of them\n implement, and this task wires it into the four CI invocations that judge Guardian\n approval.\n \ndiff --git a/docs/adr/README.md b/docs/adr/README.md\nindex c70e057a4520c36e042ab68c53cd4a21ef709b71..5da43856836e2ce6a150f8127766e2f88355ab4b\n--- a/docs/adr/README.md\n+++ b/docs/adr/README.md\n@@ -1,40 +1,41 @@\n-# Architecture Decision Records\n+# Decision Record Namespaces\n \n-This directory holds Shiki's Architecture Decision Records (ADRs): durable\n-records of hard-to-reverse platform decisions. ADRs are part of the Shiki source\n-of truth alongside GitHub state, the `.shiki/` mirror, and `CONTEXT.md`.\n+This directory holds two independent decision namespaces:\n \n-## How ADRs Work\n+- Shiki Architecture Decision Records (SADRs), named\n+  `SADR-NNNN-<short-slug>.md`, record hard-to-reverse Shiki platform decisions.\n+- Target Architecture Decision Records (ADRs), named\n+  `NNNN-<short-slug>.md`, belong to the target product.\n \n-- New ADRs are added as `docs/adr/NNNN-<short-slug>.md`, where `NNNN` is the next\n-  zero-padded sequence number.\n-- The **canonical list of ADRs is the set of files matching `docs/adr/*.md`** in\n-  this directory. Browse `docs/adr/*.md` for the authoritative, current set.\n-- Use [`template.md`](template.md) as the starting point for a new ADR.\n-- Record an ADR when a decision is hard to reverse, surprising without context,\n-  and a real tradeoff. Do not write an ADR for routine or easily reversible\n-  choices.\n+The same four-digit number may appear once in each namespace. A platform\n+`SADR-NNNN` citation resolves only to `SADR-NNNN-*.md`; a target `ADR NNNN`\n+citation resolves only to `NNNN-*.md`.\n \n-Using the `docs/adr/*.md` glob as the source of truth means other PRs can add an\n-ADR without editing this index file, avoiding merge contention on a\n-hand-maintained enumeration.\n+## How Decision Records Work\n \n-## Current ADRs (snapshot)\n-\n-The list below is a convenience snapshot and may lag behind the directory. When\n-in doubt, list `docs/adr/*.md`.\n+- New Shiki platform decisions use the next zero-padded SADR number.\n+- New target product decisions use the target's own ADR sequence.\n+- The canonical list is the files in `docs/adr/`; no hand-maintained index is\n+  authoritative.\n+- Use [`template.md`](template.md) as the starting point and choose the filename\n+  namespace that owns the decision.\n+- Record a decision when it is hard to reverse, surprising without context, and\n+  a real tradeoff. Do not write a decision record for routine or easily\n+  reversible choices.\n \n-- [0001 \u2014 Keep Shiki As The Platform Name](0001-keep-shiki-as-platform-name.md)\n-- [0002 \u2014 Use AGENTS As The Shared Agent Constitution](0002-use-agents-as-shared-constitution.md)\n-- [0003 \u2014 Require Skill Gate For MergeGate Readiness](0003-require-skill-gate-for-mergegate-readiness.md)\n-- [0004 \u2014 Use GitHub First With A Repository-Local Shiki Mirror](0004-use-github-first-with-shiki-mirror.md)\n-- [0005 \u2014 Use Codex As Front And Claude Code Action As The GitHub Runtime](0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md)\n-- [0006 \u2014 Separate Codex Implementation, CCA Completion, And MergeGate Authorization](0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md)\n+Using the directory as the source of truth lets independent changes add records\n+without contending on an enumerated index.\n \n-## Adding A New ADR\n+## Adding A Shiki SADR\n \n-1. Copy `template.md` to `docs/adr/NNNN-<short-slug>.md`.\n+1. Copy `template.md` to `docs/adr/SADR-NNNN-<short-slug>.md`.\n 2. Fill in the title, status, context, decision, and consequences.\n-3. Set the status to `Proposed`, then `Accepted` once the decision is approved.\n-4. Open a PR following `CONTRIBUTING.md`. You do not need to edit this index for\n-   the ADR to be discoverable via `docs/adr/*.md`.\n+3. Set the status to `Proposed`, then `Accepted` once the proper authority has\n+   approved the decision.\n+4. Open a PR following `CONTRIBUTING.md`.\n+\n+## Adding A Target ADR\n+\n+1. Copy `template.md` to `docs/adr/NNNN-<short-slug>.md` using the target's own\n+   sequence.\n+2. Cite it as `ADR NNNN`; do not prefix its filename with `SADR-`.\ndiff --git a/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md b/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\nnew file mode 100644\nindex 0000000000000000000000000000000000000000..7455ce1f3bed02ddddb851ac21a1d1a648c9aeba\n--- /dev/null\n+++ b/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\n@@ -0,0 +1,55 @@\n+# SADR-0019: Separate Shiki Platform SADRs From Target ADRs\n+\n+## Status\n+\n+Accepted\n+\n+## Context\n+\n+Shiki installs its platform decision records into `docs/adr/` in every target\n+repository. Shiki and its targets previously used the same numeric\n+`NNNN-<slug>.md` namespace. A target that already owned ADRs 0013 through 0018\n+therefore acquired two records for each number during installation, making an\n+`ADR NNNN` citation ambiguous. Renumbering target decisions would rewrite target\n+history, while renumbering Shiki decisions per target would destroy their stable\n+platform identity.\n+\n+Upgrades also need to remove the former Shiki-owned numeric paths without ever\n+mistaking a target-owned ADR for platform content. The install stamp already\n+records exact shipped paths and content digests, so it can prove ownership only\n+when both still match.\n+\n+## Decision\n+\n+Shiki platform decisions use the `SADR` namespace:\n+\n+- records are named `docs/adr/SADR-NNNN-<slug>.md`;\n+- live platform citations use `SADR-NNNN`;\n+- target product decisions retain `docs/adr/NNNN-<slug>.md` and `ADR NNNN`;\n+- the reference validator resolves the two namespaces independently, so the same\n+  four-digit number may exist once in each namespace.\n+\n+Before a forced upgrade writes anything, the installer inventories the exact\n+former numeric Shiki ADR paths. It may delete a legacy path only when the\n+existing install stamp contains that exact path and its recorded SHA-256 digest\n+matches the file currently on disk. All ownership blockers are reported\n+together and abort the upgrade before its first write. A non-forced install\n+never performs this cleanup.\n+\n+Historical `.shiki/` evidence remains unchanged because it records the namespace\n+that existed when that evidence was produced.\n+\n+## Consequences\n+\n+- Targets may use any ADR number without colliding with Shiki's platform SADRs.\n+- Fresh installs preserve target ADR bytes and install a stable SADR set.\n+- Forced upgrades fail closed when legacy ownership cannot be proven, and a\n+  successful upgrade refreshes the stamp without removed legacy paths.\n+- Current governance, packaging, workflow, prompt, script, and skill references\n+  must move with the platform records.\n+\n+## Alternatives Considered\n+\n+- Collision-only refusal leaves affected targets permanently unupgradable.\n+- Renumbering target ADRs mutates target-owned history.\n+- Per-target Shiki renumbering makes platform citations non-portable.\ndiff --git a/docs/adr/template.md b/docs/adr/template.md\nindex a92a97daddd0b00ab9dd626cc8d86942da2eafed..17dd79175b5fee2105935557f691b40cf9492942\n--- a/docs/adr/template.md\n+++ b/docs/adr/template.md\n@@ -1,14 +1,14 @@\n-# NNNN: <Short Decision Title>\n+# <SADR-NNNN or NNNN>: <Short Decision Title>\n \n ## Status\n \n-Proposed | Accepted | Superseded by ADR-NNNN\n+Proposed | Accepted | Superseded by SADR-NNNN or target ADR NNNN\n \n ## Context\n \n What is the situation, constraint, or force that requires a decision? Reference\n-relevant domain terms (`CONTEXT.md`), prior ADRs, and code reality. State the\n-problem, not the solution.\n+relevant domain terms (`CONTEXT.md`), prior Shiki SADRs or target ADRs, and code\n+reality. State the problem, not the solution.\n \n ## Decision\n \ndiff --git a/docs/agents/autonomous-loop-e2e-acceptance.md b/docs/agents/autonomous-loop-e2e-acceptance.md\nindex bbf84c6081987f159ac6ad9ecf3c82b7aa72e7e8..5c13c1d02a05a90b0dc46cbef7c95a9846d0c237\n--- a/docs/agents/autonomous-loop-e2e-acceptance.md\n+++ b/docs/agents/autonomous-loop-e2e-acceptance.md\n@@ -40,8 +40,8 @@\n 3. `gh auth status` is logged in with merge permission on the repo.\n 4. Branch protection + required checks are configured exactly as the goal-loop\n    required-check set expects (see `docs/agents/branch-protection-smoke.md`).\n-5. The repo `.shiki/policy` permits autonomous low/medium-risk merges (ADR\n-   0008/0009); the MergeGate policy check is the recorded-authority gate.\n+5. The repo `.shiki/policy` permits autonomous low/medium-risk merges\n+   (SADR-0008/0009); the MergeGate policy check is the recorded-authority gate.\n \n The operator's only actions are: register the task and start the loop. **No\n manual pre-clearing** means after `shiki loop run` starts, the operator does not\ndiff --git a/docs/agents/bootstrap-command.md b/docs/agents/bootstrap-command.md\nindex 05f7f203a52cf412b9f97084a6e131ed0f785afd..f7aa0b734dfebee4a3b139335b66ee3415f48713\n--- a/docs/agents/bootstrap-command.md\n+++ b/docs/agents/bootstrap-command.md\n@@ -189,7 +189,9 @@\n ```\n \n Use this only for tests, fixtures, or explicit local-only template inspection.\n-Use `--force` only when you intentionally want to overwrite existing target files.\n+Use `--force` only when you intentionally want to refresh the target template.\n+Legacy numeric Shiki decision cleanup is stamp-and-digest authorized before any\n+write; target ADR paths are not cleanup candidates.\n \n ## Slash Command\n \ndiff --git a/docs/agents/checklists.md b/docs/agents/checklists.md\nindex 4a7f03ade346f53a1c1d82981f3fd744f9ab4547..0c5ec6c53dc7197b7b7a7d18e667b325babd6c45\n--- a/docs/agents/checklists.md\n+++ b/docs/agents/checklists.md\n@@ -32,18 +32,18 @@\n | ID | Item | Blocking |\n | --- | --- | --- |\n | GD-01 | Plan was challenged against `CONTEXT.md`. | blocking for non-trivial Goals |\n-| GD-02 | Relevant ADRs were read or absence was recorded. | blocking for non-trivial Goals |\n+| GD-02 | Relevant SADRs and target ADRs were read or absence was recorded. | blocking for non-trivial Goals |\n | GD-03 | Ambiguous terms were resolved or converted to HITL blockers. | blocking |\n | GD-04 | Edge scenarios were considered. | advisory |\n | GD-05 | Code reality was checked when code could answer. | blocking when applicable |\n | GD-06 | New domain terms were added to `CONTEXT.md` when needed. | blocking when applicable |\n-| GD-07 | ADR was created or explicitly skipped using the ADR criteria. | blocking when applicable |\n+| GD-07 | A SADR or target ADR was created or explicitly skipped using the decision-record criteria. | blocking when applicable |\n \n ## CI \u2014 Context & Impact Checklist\n \n | ID | Item | Blocking |\n | --- | --- | --- |\n-| CI-01 | Relevant docs and ADRs identified. | blocking |\n+| CI-01 | Relevant docs, SADRs, and target ADRs identified. | blocking |\n | CI-02 | Relevant modules, interfaces, seams, callers, and tests identified. | blocking |\n | CI-03 | Dependencies and blocked-by relationships identified. | blocking |\n | CI-04 | Candidate locks identified. | blocking |\n@@ -62,7 +62,7 @@\n | PRD-04 | Implementation decisions are recorded without volatile code detail. | blocking |\n | PRD-05 | Testing decisions are recorded. | blocking |\n | PRD-06 | Out-of-scope items are explicit. | blocking |\n-| PRD-07 | Relevant glossary/ADR links are present when applicable. | blocking |\n+| PRD-07 | Relevant glossary/SADR/target-ADR links are present when applicable. | blocking |\n \n ## SF \u2014 Spec Freeze Checklist\n \n@@ -98,7 +98,7 @@\n | H-02 | Branch/worktree target is present. | blocking |\n | H-03 | Scope and non-goals are present. | blocking |\n | H-04 | Dependencies and locks are present. | blocking |\n-| H-05 | Relevant docs, ADRs, modules, and tests are referenced. | blocking |\n+| H-05 | Relevant docs, SADRs, target ADRs, modules, and tests are referenced. | blocking |\n | H-06 | Required skills are stated. | blocking |\n | H-07 | TDD expectations are stated. | blocking when behavior work |\n | H-08 | Acceptance criteria are included. | blocking |\ndiff --git a/docs/agents/codex-handoff.md b/docs/agents/codex-handoff.md\nindex ede468f47cb277f69a857e8c45df703e4ed0151d..6f97bdffb67e44d7edea2c71e01ea28933c4eb6c\n--- a/docs/agents/codex-handoff.md\n+++ b/docs/agents/codex-handoff.md\n@@ -46,7 +46,7 @@\n \n ## Context\n - Domain terms:\n-- Relevant ADRs:\n+- Relevant SADRs and target ADRs:\n - Relevant docs:\n - Relevant modules/interfaces/seams:\n - Similar tests/prior art:\ndiff --git a/docs/agents/completion-check-agent.md b/docs/agents/completion-check-agent.md\nindex 13e4c035db35025f4e3cdcc5adfb24314ac4d521..61bfa1218cf36ca605250c91919e620f33238aa0\n--- a/docs/agents/completion-check-agent.md\n+++ b/docs/agents/completion-check-agent.md\n@@ -32,7 +32,7 @@\n - `AGENTS.md`\n - `CLAUDE.md`\n - `CONTEXT.md`\n-- relevant ADRs\n+- relevant SADRs and target ADRs\n - parent Goal issue\n - PRD issue or document\n - task issue\ndiff --git a/docs/agents/decision-control.md b/docs/agents/decision-control.md\nindex 7d8ee5ab03a5ccbcbd04056ae61de03785a2a9c9..8c94ecda00fffaa0e92c325ab83aa9776bd1dac1\n--- a/docs/agents/decision-control.md\n+++ b/docs/agents/decision-control.md\n@@ -11,7 +11,7 @@\n | Decision | Owner | When | Required inputs | Output | Enforcement surface |\n | --- | --- | --- | --- | --- | --- |\n | Is the Goal clear enough to plan? | Planner + Guardian when needed | Goal intake | User request, existing docs, risk signals | Goal readiness verdict | Goal checklist, issue template, ledger |\n-| Must `grill-with-docs` run? | Planner / Skill Gate | Before PRD or issue split | Ambiguity, domain terms, ADR conflicts, unclear boundaries | Skill invocation or explicit skip rationale | Skill Gate checklist |\n+| Must `grill-with-docs` run? | Planner / Skill Gate | Before PRD or issue split | Ambiguity, domain terms, SADR/target-ADR conflicts, unclear boundaries | Skill invocation or explicit skip rationale | Skill Gate checklist |\n | Is a PRD required? | Planner | After Goal context stabilizes | Goal, `grill-with-docs` output, Context & Impact output | PRD required / not required | PRD checklist, CCA evidence check |\n | Can the spec be frozen? | Operator only | End of Requirements Definition, before issue split | PRD/requirements summary, scope inventory (SF-02), open-question list | `spec_freeze` block in the plan + ledger evidence | `require_grilled_plan` (plan ingest/run), SF checklist |\n | Can a frozen spec change? | Operator only (Spec Amendment) | When implementation contradicts the frozen spec | Contested decisions, scoped re-grill result | recorded amendment + re-stamped freeze | SF checklist, ledger evidence |\ndiff --git a/docs/agents/domain.md b/docs/agents/domain.md\nindex 258da8ea81e87ac0a6b0b5b5e5c2fde980eaabb7..d57b0cf17ff3e1fa9e6e5885372bf083d8295421\n--- a/docs/agents/domain.md\n+++ b/docs/agents/domain.md\n@@ -5,7 +5,9 @@\n ## Layout\n \n - `CONTEXT.md` defines Shiki language. Keep it implementation-free.\n-- `docs/adr/` records hard-to-reverse decisions.\n+- `docs/adr/SADR-NNNN-*.md` records Shiki platform decisions.\n+- `docs/adr/NNNN-*.md` records target product decisions without sharing the\n+  SADR namespace.\n - `AGENTS.md` is the runtime-neutral agent constitution.\n - `CLAUDE.md` is the Claude Code wrapper around `AGENTS.md`.\n - `.shiki/` is the Target Repository mirror for Goals, PRDs, plans, Task DAGs, contracts, locks, worktree records, ledger entries, CCA verdicts, repair packets, reports, and handoffs.\n@@ -15,6 +17,6 @@\n \n - Use Shiki terms exactly when they exist.\n - Add glossary terms to `CONTEXT.md` only when the term is part of the domain, not a generic programming concept.\n-- Create ADRs only for hard-to-reverse, surprising, tradeoff-based decisions.\n+- Create SADRs or target ADRs only for hard-to-reverse, surprising, tradeoff-based decisions.\n - Keep product-specific language in the Target Repository, not in Shiki Template docs.\n-- If an ADR conflicts with a proposed implementation, surface the conflict before editing.\n+- If a SADR or target ADR conflicts with a proposed implementation, surface the conflict before editing.\ndiff --git a/docs/agents/execution-control.md b/docs/agents/execution-control.md\nindex ad8ee3cdb36be56fedc1f46408ca92b387d74047..bc63c3a73661d12b567016ba299f1a6b66bf9088\n--- a/docs/agents/execution-control.md\n+++ b/docs/agents/execution-control.md\n@@ -63,7 +63,7 @@\n | Branch name | Orchestrator | Orchestrator | Before implementer dispatch | branch policy | branch ref in task/ledger |\n | Lock reservation | Context & Impact | MergeGate | Before dispatch and before merge | `.shiki/locks/`, labels, PR metadata | lock id, owner, scope |\n | CI profile | Planner | MergeGate | During issue triage, rechecked at PR | labels, `.shiki/policy.yaml`, workflow matrix | required check list |\n-| Required status checks | Repo maintainer / Guardian | GitHub branch protection / rulesets | Repository setup, updated via ADR | GitHub settings | branch/ruleset config, ADR |\n+| Required status checks | Repo maintainer / Guardian | GitHub branch protection / rulesets | Repository setup, updated via target ADR | GitHub settings | branch/ruleset config, target ADR |\n | CCA judgment | CCA | GitHub required check / MergeGate | Every non-draft PR update | GitHub Action check | structured CCA verdict |\n | Repair assignment | CCA | MergeGate | After failed CCA/CI/review | repair packet, issue/PR comment | repair id and attempt count |\n | Merge readiness | MergeGate | GitHub branch protection / Guardian if needed | After CCA complete | required checks, review rules, rulesets | mergegate check output |\ndiff --git a/docs/agents/guardian-policy.md b/docs/agents/guardian-policy.md\nindex 4147c7adf05a47adb4fd24ada1a1c2e1f2ba6d2d..a20d9cc6c00a569bb057b9ca8d6b51c7fc4a9847\n--- a/docs/agents/guardian-policy.md\n+++ b/docs/agents/guardian-policy.md\n@@ -98,8 +98,8 @@\n `origin/<default_branch>` and reuses each job's existing base fetch; a shallow\n (`--depth`) or second base fetch would make the proof unresolvable and silently\n disable the carry, so neither is permitted. CCA reads `guardian_comment_carried` as\n-a recorded authority in the deterministic signal and does not re-derive it. See ADR\n-0018 for the full decision, the rejected diff/patch-equality and path-disjointness\n+a recorded authority in the deterministic signal and does not re-derive it. See\n+SADR-0018 for the full decision, the rejected diff/patch-equality and path-disjointness\n alternatives, and the accepted residual.\n \n ## External AI Guardian Review (SADR-0010)\ndiff --git a/docs/agents/implementation-policy.md b/docs/agents/implementation-policy.md\nindex c754144a9fb5bf4a38aa82a60e1670c59e626b44..85c37281bcad196c6b7b02343faf00b03cd9c57a\n--- a/docs/agents/implementation-policy.md\n+++ b/docs/agents/implementation-policy.md\n@@ -31,10 +31,13 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n ## Non-Negotiables\n \n 1. **Planning precedes implementation.** Non-trivial Goals start with `grill-with-docs` and Context & Impact.\n-2. **Domain language is controlled.** Terms must be checked against `CONTEXT.md`; hard-to-reverse tradeoffs must be checked against ADRs.\n+2. **Domain language is controlled.** Terms must be checked against `CONTEXT.md`; hard-to-reverse tradeoffs must be checked against Shiki SADRs and target ADRs.\n 3. **PRD precedes issue decomposition.** `to-prd` is used once the context is settled enough to become durable intent.\n 4. **Issues are vertical slices.** `to-issues` must produce independently grabbable tracer-bullet tasks, not horizontal layer tickets.\n 5. **Triage controls dispatch.** Only `ready-for-agent` / AFK tasks may be assigned to Codex without human input.\n@@ -95,19 +98,19 @@\n Process:\n \n - Challenge terms against `CONTEXT.md`.\n-- Check ADRs before reopening decisions.\n+- Check SADRs and target ADRs before reopening decisions.\n - Inspect code when code can answer a question.\n - Ask one question at a time when the operator is available.\n - Provide a recommended answer with each question.\n - Record resolved terms in `CONTEXT.md`.\n-- Create ADRs only for hard-to-reverse, surprising, tradeoff-based decisions.\n+- Create SADRs or target ADRs only for hard-to-reverse, surprising, tradeoff-based decisions.\n - Convert unresolved load-bearing questions into HITL blockers.\n \n Outputs:\n \n - Resolved design-tree decisions.\n - Updated glossary terms when needed.\n-- ADRs when justified.\n+- SADRs or target ADRs when justified.\n - HITL blockers when decisions remain unresolved.\n \n ## Phase 3 \u2014 Context & Impact\n@@ -117,7 +120,7 @@\n \n Outputs must include:\n \n-- Relevant documents and ADRs.\n+- Relevant documents, SADRs, and target ADRs.\n - Relevant modules, interfaces, seams, callers, and tests.\n - Candidate locks.\n - Dependencies.\n@@ -140,7 +143,7 @@\n - Testing decisions.\n - Out of scope.\n - Further notes.\n-- Links to relevant Goal, domain terms, and ADRs.\n+- Links to relevant Goal, domain terms, SADRs, and target ADRs.\n \n Phase 4 ends with Spec Freeze: the operator explicitly approves the PRD, the\n plan records a `spec_freeze` block (status, approved_by, source), and the SF\n@@ -240,7 +243,7 @@\n - Dependencies and locks.\n - Risk and Guardian requirements.\n - Ledger completeness.\n-- Documentation and ADR updates.\n+- Documentation and SADR/target-ADR updates.\n - Scope drift.\n \n CCA emits one of:\n@@ -301,7 +304,7 @@\n | Evidence failure | Behavior might be done, but proof is missing. | Add checks, PR evidence, ledger entry, or artifacts. |\n | Scope drift | PR changes unrelated behavior. | Revert unrelated changes or split into new Goal. |\n | Dependency block | Upstream task or lock not complete. | Block until dependency resolves. |\n-| Architecture block | Decision needs ADR or deep-module discussion. | `grill-with-docs` or `improve-codebase-architecture`. |\n+| Architecture block | Decision needs a SADR, target ADR, or deep-module discussion. | `grill-with-docs` or `improve-codebase-architecture`. |\n | Risk block | Security/production/policy change needs human approval. | Guardian decision. |\n \n ## Phase 5.5 \u2014 Execution Decision Control\ndiff --git a/docs/agents/runtime-auth-model.md b/docs/agents/runtime-auth-model.md\nindex 6a2e1a47e81ea89b5c68a210ec07772a9374babc..3c54748d24233dbd0ba8473950282547ef4ce935\n--- a/docs/agents/runtime-auth-model.md\n+++ b/docs/agents/runtime-auth-model.md\n@@ -148,7 +148,7 @@\n \n API-key based runners may be added by a target repository as an explicit extension. They are not the default Shiki template path.\n \n-If a repo opts into API-key mode, record the exception in an ADR and update the repo's `AGENTS.md`, `.shiki/config.yaml`, and workflow permissions.\n+If a repo opts into API-key mode, record the target-specific exception in a target ADR and update the repo's `AGENTS.md`, `.shiki/config.yaml`, and workflow permissions.\n \n ## GitHub Actions Runtime Compatibility\n \ndiff --git a/docs/agents/skill-gate.md b/docs/agents/skill-gate.md\nindex c50398b0d7c60a8598f2a5c88a10015730dced13..3acffe6444617bb1d54b77d4b4708158ec735bc6\n--- a/docs/agents/skill-gate.md\n+++ b/docs/agents/skill-gate.md\n@@ -9,7 +9,7 @@\n | Skill | Trigger | Expected output |\n | --- | --- | --- |\n | `setup-matt-pocock-skills` | First configuring a repo for these skills, or when issue tracker, triage labels, or domain docs are missing. | `docs/agents/` configuration and an `Agent skills` block in `AGENTS.md` or `CLAUDE.md`. |\n-| `grill-with-docs` | Ambiguous plan, unclear terminology, boundary decision, tradeoff, or ADR-worthy design choice. | Clarified decision tree, recommended answers, updates to `CONTEXT.md` or ADRs when decisions crystallize. |\n+| `grill-with-docs` | Ambiguous plan, unclear terminology, boundary decision, tradeoff, or decision-record-worthy design choice. | Clarified decision tree, recommended answers, updates to `CONTEXT.md`, SADRs, or target ADRs when decisions crystallize. |\n | `zoom-out` | Agent lacks a higher-level architectural map. | Map of relevant modules, callers, seams, and domain concepts. |\n | `to-prd` | Settled context should become a PRD. | PRD published to the issue tracker with problem, solution, user stories, decisions, testing, and out-of-scope notes. |\n | `to-issues` | Goal, PRD, or plan must become implementation tickets. | Vertical-slice issues with acceptance criteria, dependencies, and AFK/HITL classification. |\n@@ -17,7 +17,7 @@\n | `tdd` | Feature work or bug fix has observable behavior that can be specified with tests. | Red-green-refactor loop through public interfaces, one vertical slice at a time. |\n | `code-review` | The TDD loop is green and the implementer is about to create or update a PR. Default for every implementation task. | Pre-PR self-review of the task diff with fixes applied on the task branch; findings and resolutions recorded as a ledger entry naming the skill and a `## Pre-PR code review` PR body section. |\n | `diagnose` | Hard bug, failing check, regression, flaky behavior, or performance problem. | Reproduction loop, ranked hypotheses, instrumentation, fix, regression test, post-mortem. |\n-| `improve-codebase-architecture` | Structural friction, testability, AI-navigability, deep-module opportunity, or refactor request. | Architecture candidates and deepening recommendations informed by `CONTEXT.md` and ADRs. |\n+| `improve-codebase-architecture` | Structural friction, testability, AI-navigability, deep-module opportunity, or refactor request. | Architecture candidates and deepening recommendations informed by `CONTEXT.md`, SADRs, and target ADRs. |\n | `prototype` | A throwaway logic or UI prototype is needed to answer a design question. | Clearly marked disposable prototype and captured decision. |\n \n ## MergeGate Interaction\ndiff --git a/docs/operations.md b/docs/operations.md\nindex cf1f452d4c589e38ccd58553bda0ad8684b51eb7..ace48a90ffbec9e5c0a7113bef16a4858bc44b3f\n--- a/docs/operations.md\n+++ b/docs/operations.md\n@@ -89,6 +89,16 @@\n      target history, brought forward by `shiki migrate`, not a template.\n    - **Everything else** (CLI scripts, workflows, schemas, docs) is overwritten.\n \n+   A forced upgrade also migrates the former numeric Shiki decision paths to\n+   `SADR-NNNN-*`. Before the first write, the installer inventories the exact\n+   legacy Shiki paths and authorizes each deletion only when the existing\n+   `.shiki/install-stamp.json` names that path and its SHA-256 digest still\n+   matches the file. An absent, unreadable, malformed, incomplete, or\n+   mismatching stamp reports every blocked path and stops with no filesystem\n+   change. Target-owned `NNNN-*` ADRs are not cleanup candidates. A non-force\n+   install deletes nothing and retains any still-matching legacy stamp entries\n+   so a later forced upgrade can prove ownership.\n+\n    If the target has pending migrations, `--force` refuses and writes nothing \u2014\n    apply migrations first (step 3), then re-run. The run ends with a single\n    summary of every `.new` written, so a half-upgrade is visible.\ndiff --git a/docs/releasing.md b/docs/releasing.md\nindex 10d73828e7dbd64e9d9f6b83bc84d010537ce62a..aafeee8c4d441e1e427efee2ce4f81a647f020e1\n--- a/docs/releasing.md\n+++ b/docs/releasing.md\n@@ -67,7 +67,7 @@\n CLI in `scripts/`. There are no third-party Python runtime dependencies. The CLI\n is intentionally **not** exposed as a `[project.scripts]` console entry point\n because `scripts/shiki.py` is a dependency-free shim and `scripts/` is not\n-packaged as an importable distribution (see the ADR for the rationale).\n+packaged as an importable distribution (see SADR-0007 for the rationale).\n \n Supported install paths:\n \n@@ -155,6 +155,15 @@\n    [docs/operations.md](operations.md#the-new-convention). If the target has\n    pending migrations, `--force` refuses and writes nothing; apply migrations\n    (step 2) first, then re-run.\n+\n+   The SADR namespace upgrade has an additional pre-write gate: each former\n+   numeric Shiki decision path is removed only when the existing install stamp\n+   contains that exact path and a digest matching its current bytes. The\n+   installer aggregates absent/unreadable/malformed-stamp, missing-digest, and\n+   digest-mismatch blockers before raising, so refusal leaves the target tree\n+   byte-identical. Numeric target ADRs are preserved. A successful forced\n+   upgrade installs and stamps `SADR-*` paths and drops the removed legacy paths\n+   from the refreshed stamp.\n 2. Apply repository-local `.shiki/` state migrations with the migrate command:\n \n    ```bash\ndiff --git a/docs/usage.md b/docs/usage.md\nindex b83407ee3970dd395dd893010bc5dbe8bad26051..a130ef6e40dfd8169a5bc701339351097df1a09f\n--- a/docs/usage.md\n+++ b/docs/usage.md\n@@ -67,8 +67,10 @@\n shiki install-target /path/to/target-repo --local-only\n ```\n \n-Use `--force` only when you intentionally want to overwrite existing target\n-files.\n+Use `--force` only when you intentionally want to refresh the target template.\n+For the SADR namespace migration, `--force` deletes only exact legacy Shiki\n+decision paths whose existing install-stamp digest still matches; any ownership\n+blocker stops before the first write. Numeric target ADRs are never candidates.\n \n ## `shiki bootstrap-platform` \u2014 publish the Shiki platform repo\n \ndiff --git a/scripts/guardian_approval_signal.py b/scripts/guardian_approval_signal.py\nindex d059f92b2c82aa2b5d6ca421365537a87886c1cb..16924892714f4f5b9303c0ef0e325a7e70075630\n--- a/scripts/guardian_approval_signal.py\n+++ b/scripts/guardian_approval_signal.py\n@@ -199,8 +199,8 @@\n ) -> Any:\n     \"\"\"Evaluate SADR-0015 Contract Approval for the CCA Guardian signal.\n \n-    The SAME OR that MergeGate applies must reach this deterministic signal: ADR\n-    0015 is explicit that without it, a contract-approved PR still returns\n+    The SAME OR that MergeGate applies must reach this deterministic signal:\n+    SADR-0015 is explicit that without it, a contract-approved PR still returns\n     ``needs_guardian``, the CCA job fails, and the MergeGate policy check never\n     runs \u2014 the gate this change moves would never be reached. This resolves the\n     task the SAME way MergeGate does (the first Shiki task id in the PR body),\ndiff --git a/scripts/shiki_contract_approval.py b/scripts/shiki_contract_approval.py\nindex 941bcb96fdd8b9a85bc0c4f0f04d5dad9d04839c..4a4b418f6527413161db82670bec857a043360e2\n--- a/scripts/shiki_contract_approval.py\n+++ b/scripts/shiki_contract_approval.py\n@@ -142,7 +142,7 @@\n     changed_files_status: Any,\n     registration: Any,\n ) -> ContractApprovalResult:\n-    \"\"\"Whether this PR carries an SADR-0015 Contract Approval.\n+    \"\"\"Whether this PR carries a SADR-0015 Contract Approval.\n \n     The four conditions are evaluated IN ORDER, each failure recorded as a\n     distinct reason string:\ndiff --git a/scripts/shiki_guardian.py b/scripts/shiki_guardian.py\nindex a4ecb79bbda55fd5c8b06adfae2eb99760a03c45..4dd99bbe714e26f421fa19abac7a34ae1b195ecc\n--- a/scripts/shiki_guardian.py\n+++ b/scripts/shiki_guardian.py\n@@ -12,7 +12,7 @@\n # The carry re-derives its proof from git in-process on every judging invocation;\n # it is never read from an artifact, so there is nothing to go stale or be forged.\n # shiki_sync_proof is the ONLY guardian-family module that runs git, and every one\n-# of its invocations is read-only and local (ADR: prove-from-git base sync).\n+# of its invocations is read-only and local (SADR: prove-from-git base sync).\n from shiki_sync_proof import verify_pure_base_sync\n \n GUARDIAN_POLICY_PATH = \".shiki/guardian-policy.json\"\ndiff --git a/scripts/shiki_guardian_review.py b/scripts/shiki_guardian_review.py\nindex dd0f104d457899e0ba5ff1e162011c1e90b6f92b..e10ef58c046324e7ea3a3392feb3048ca77414ce\n--- a/scripts/shiki_guardian_review.py\n+++ b/scripts/shiki_guardian_review.py\n@@ -159,7 +159,7 @@\n     docs_only = _is_docs_only(changed_files)\n     if docs_only and matched == 0:\n         focus.append(\n-            \"Docs/ADR-only change: confirm the change is genuinely documentation/decision-only, introduces no behavior or contract drift, and stays consistent with the constitution and existing ADRs.\"\n+            \"Docs/decision-record-only change: confirm the change is genuinely documentation/decision-only, introduces no behavior or contract drift, and stays consistent with the constitution and existing SADRs and target ADRs.\"\n         )\n \n     # Conservative augmentation: unknown (no category and not docs-only), mixed\ndiff --git a/scripts/shiki_installer.py b/scripts/shiki_installer.py\nindex be064ab7a29ff6836f7f7a1d70500a43e0d11d7b..9fd8eefd137ce7003dad5c0dcb2e48e2cd47ab32\n--- a/scripts/shiki_installer.py\n+++ b/scripts/shiki_installer.py\n@@ -118,6 +118,7 @@\n # what lets a target carry it without a governance-schema change.\n INSTALL_STAMP_PATH = \".shiki/install-stamp.json\"\n INSTALL_STAMP_VERSION = 1\n+LEGACY_SHIKI_SADR_MAX = 18\n \n # Under --force the shipped surface splits three ways, so nothing is lost and\n # nothing is silently stale.\n@@ -237,9 +238,98 @@\n         )\n \n \n+def legacy_shiki_adr_paths() -> tuple[str, ...]:\n+    \"\"\"Former numeric paths for Shiki SADR-0001 through SADR-0018.\n+\n+    The authoritative slugs come from the current SADR filenames. SADR-0019 is\n+    the namespace decision itself and never shipped under a numeric legacy path.\n+    \"\"\"\n+    paths: list[str] = []\n+    for sadr in sorted((ROOT / \"docs\" / \"adr\").glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\")):\n+        number = sadr.name.split(\"-\", 2)[1]\n+        if int(number) <= LEGACY_SHIKI_SADR_MAX:\n+            paths.append(f\"docs/adr/{sadr.name.removeprefix('SADR-')}\")\n+    return tuple(paths)\n+\n+\n+def preflight_legacy_sadr_cleanup(target: Path, *, force: bool) -> tuple[str, ...]:\n+    \"\"\"Authorize exact legacy Shiki ADR removals before the first write.\n+\n+    A non-force install never removes existing files. A forced install needs no\n+    stamp when no legacy Shiki path is present. When legacy paths do exist, each\n+    must be named in a readable install stamp and its current bytes must match\n+    the recorded digest.\n+    \"\"\"\n+    if not force:\n+        return ()\n+    present = tuple(\n+        relative\n+        for relative in legacy_shiki_adr_paths()\n+        if (target / relative).exists() or (target / relative).is_symlink()\n+    )\n+    if not present:\n+        return ()\n+\n+    stamp_path = target / INSTALL_STAMP_PATH\n+    try:\n+        raw = stamp_path.read_text(encoding=\"utf-8\")\n+        stamp = json.loads(raw)\n+    except FileNotFoundError:\n+        detail = \"install stamp is absent\"\n+        blockers = [f\"{relative}: {detail}\" for relative in present]\n+    except (OSError, UnicodeDecodeError) as error:\n+        detail = f\"install stamp is unreadable ({error})\"\n+        blockers = [f\"{relative}: {detail}\" for relative in present]\n+    except json.JSONDecodeError as error:\n+        detail = f\"install stamp is malformed ({error})\"\n+        blockers = [f\"{relative}: {detail}\" for relative in present]\n+    else:\n+        if not isinstance(stamp, dict):\n+            blockers = [\n+                f\"{relative}: install stamp is malformed (expected an object)\"\n+                for relative in present\n+            ]\n+        else:\n+            digests = stamp.get(\"digests\")\n+            blockers = []\n+            if not isinstance(digests, dict):\n+                blockers.extend(\n+                    f\"{relative}: install stamp has no digest for this path\"\n+                    for relative in present\n+                )\n+            else:\n+                for relative in present:\n+                    expected = digests.get(relative)\n+                    if not isinstance(expected, str) or not expected:\n+                        blockers.append(\n+                            f\"{relative}: install stamp has no digest for this path\"\n+                        )\n+                        continue\n+                    try:\n+                        actual = _sha256_file(target / relative)\n+                    except OSError as error:\n+                        blockers.append(\n+                            f\"{relative}: legacy path is unreadable ({error})\"\n+                        )\n+                        continue\n+                    if actual != expected:\n+                        blockers.append(\n+                            f\"{relative}: digest mismatch (stamp {expected}, current {actual})\"\n+                        )\n+\n+    if blockers:\n+        joined = \"\\n\".join(f\"- {blocker}\" for blocker in blockers)\n+        raise ShikiError(\n+            \"legacy Shiki ADR cleanup is not authorized:\\n\"\n+            f\"{joined}\\nNo files were written.\"\n+        )\n+    return present\n+\n+\n def install_template(target: Path, *, force: bool, validate: bool) -> list[NewFileNote]:\n     # Preconditions first: never rewrite the tree of a target that cannot be\n     # upgraded cleanly (see refuse_pending_migrations).\n+    legacy_cleanup = preflight_legacy_sadr_cleanup(target, force=force)\n     refuse_pending_migrations(target)\n \n     # Resolve the target's required CODEOWNERS owner once from its\n@@ -250,6 +340,10 @@\n     # verbatim. See ``codeowners_required_owner``.\n     codeowners_owner = codeowners_required_owner(target)\n \n+    for relative in legacy_cleanup:\n+        (target / relative).unlink()\n+        info(f\"removed stamped legacy Shiki ADR: {target / relative}\")\n+\n     notes: list[NewFileNote] = []\n     for relative in TEMPLATE_PATHS:\n         source = ROOT / relative\n@@ -647,6 +741,22 @@\n     digests = target_stamp_digests(target)\n     existing = read_install_stamp(target)\n     if existing is not None:\n+        # A non-force install deliberately leaves legacy Shiki ADRs in place.\n+        # Preserve only still-matching ownership proof so a later forced\n+        # upgrade can authorize cleanup; never carry a missing or stale digest.\n+        existing_digests = existing.get(\"digests\")\n+        if isinstance(existing_digests, dict):\n+            for relative in legacy_shiki_adr_paths():\n+                expected = existing_digests.get(relative)\n+                path = target / relative\n+                if not isinstance(expected, str) or not path.is_file():\n+                    continue\n+                try:\n+                    actual = _sha256_file(path)\n+                except OSError:\n+                    continue\n+                if actual == expected:\n+                    digests[relative] = actual\n         if _incoming_is_older(commit, existing.get(\"platform_commit\")):\n             warn(\n                 \"kept existing install stamp: incoming install \"\ndiff --git a/scripts/shiki_loop.py b/scripts/shiki_loop.py\nindex 3611641da8aeb5038b1da90e3114b09a473ec6d8..84caa491b9cad0edc4176f209c13e9221e975681\n--- a/scripts/shiki_loop.py\n+++ b/scripts/shiki_loop.py\n@@ -424,7 +424,7 @@\n         # unresolvable CCA verdict has already produced its terminal stop and is never\n         # laundered into a head-moving sync (SADR-0010). It also never fires while the\n         # policy (Guardian) gate is red \u2014 for ANY reason, not only when it is the sole\n-        # red check \u2014 so a head-bound Guardian approval, or an SADR-0015\n+        # red check \u2014 so a head-bound Guardian approval, or a SADR-0015\n         # contract-approval carry that base movement defeated (the policy gate compares\n         # base and head task governance directly), is never silently restored by\n         # adopting base wholesale. It is bounded by a durable per-task counter so the\ndiff --git a/scripts/shiki_tasks.py b/scripts/shiki_tasks.py\nindex aec3809f24b4a0c8c96fc50971cff9ae9ad30381..d531052e9d9717058d4631483cefab44c26a65c1\n--- a/scripts/shiki_tasks.py\n+++ b/scripts/shiki_tasks.py\n@@ -821,7 +821,7 @@\n         \"prompt\": prompt,\n         \"entry_skill\": \"grill-with-docs\",\n         \"required_next_steps\": [\n-            \"Run grill-with-docs until terminology, boundaries, risks, and ADR-worthy decisions are settled.\",\n+            \"Run grill-with-docs until terminology, boundaries, risks, and decision-record-worthy choices are settled.\",\n             \"Obtain the operator's explicit approval of the PRD/requirements (Spec Freeze).\",\n             \"Write a machine-readable plan JSON with grill_with_docs.status=complete and spec_freeze.status=frozen.\",\n             \"Run shiki plan ingest --plan-file PLAN.json.\",\ndiff --git a/scripts/test_shiki_goal_loop.sh b/scripts/test_shiki_goal_loop.sh\nindex 753c7b516e10f0be3bc92a4bd0eb1fa699776ae6..8f3820a0a079b3044424cfe1a6c6b7fe68896e67\n--- a/scripts/test_shiki_goal_loop.sh\n+++ b/scripts/test_shiki_goal_loop.sh\n@@ -231,8 +231,8 @@\n \n # High/critical risk merges autonomously when all required checks are green:\n # the \"MergeGate policy check\" required check IS the Guardian gate (it enforces\n-# guardian-policy.json \u2014 human approval OR external AI guardian review, ADR\n-# 0010), so green checks mean a recorded authority approved. The fake gh returns\n+# guardian-policy.json \u2014 human approval OR external AI guardian review,\n+# SADR-0010), so green checks mean a recorded authority approved. The fake gh returns\n # green checks, simulating that the Guardian gate passed.\n python3 \"$ROOT/scripts/shiki.py\" goal create --target \"$TARGET\" --title \"Guardian gate\" --outcome \"High risk merges via policy gate\" >/tmp/shiki-goal-loop-guardian-goal.json\n GGOAL=\"$(json_get /tmp/shiki-goal-loop-guardian-goal.json goal_id)\"\ndiff --git a/tests/test_adr_references.py b/tests/test_adr_references.py\nindex 51706329c797d5dd9b47e082a333fa83b261d482..d005da4f771f5e8085ec22d4b787980e68f5ae8c\n--- a/tests/test_adr_references.py\n+++ b/tests/test_adr_references.py\n@@ -1,8 +1,8 @@\n-\"\"\"Every ``ADR ####`` citation must resolve to exactly one decision record.\n+\"\"\"Every target ADR and Shiki SADR citation resolves in its own namespace.\n \n Background. ``scripts/mergegate_check.py`` justifies ``is_bookkeeping_closeout``\n-with \"ADR 0017\" nine times and ``scripts/guardian_approval_signal.py`` cites\n-0016/0017 seven times, yet for a stretch the numbering in ``docs/adr/`` stopped\n+with \"SADR-0017\" nine times and ``scripts/guardian_approval_signal.py`` cites\n+SADR-0016/SADR-0017 seven times, yet for a stretch the numbering in ``docs/adr/`` stopped\n at 0015: the authoring commits lived on a coordinator branch and no task ever\n declared ``docs/adr/...`` in its locks, so nothing carried the records to the\n default branch. The stated basis of an exemption that merged seven PRs with zero\n@@ -11,11 +11,12 @@\n and the directory drifted with no signal.\n \n This is the same unbound-pair shape behind other silent failures: a reference\n-and its target with nothing asserting they agree. This test binds them. It scans\n-every ``scripts/*.py`` (plus ``docs/**/*.md`` and the root ``*.md`` governance\n-files, which are cheap) for the ``ADR ####`` pattern and asserts each cited\n-number resolves to exactly one ``docs/adr/####-*.md``. The converse is\n-deliberately NOT required: an ADR may exist without being cited.\n+and its target with nothing asserting they agree. This test binds them. Target\n+``ADR NNNN`` citations resolve only to ``docs/adr/NNNN-*.md``; Shiki\n+``SADR-NNNN`` citations resolve only to ``docs/adr/SADR-NNNN-*.md``. The scan\n+covers current governance docs, scripts, workflows, prompts, packaging metadata,\n+and repo-local Codex guidance while deliberately excluding historical\n+``.shiki`` evidence. The converse is not required: a record may be uncited.\n \n The suite is dependency free: standard-library ``unittest`` only.\n \"\"\"\n@@ -31,17 +32,18 @@\n \n REPO_ROOT = Path(__file__).resolve().parents[1]\n \n-# \"ADR 0016\" -- a four-digit citation. The trailing \\b keeps a five-digit token\n-# from being read as a truncated four-digit number.\n-_CITATION = re.compile(r\"\\bADR (\\d{4})\\b\")\n-# \"0016-locks-bound-product-paths.md\" -- the leading four digits name the record.\n-_ADR_FILENAME = re.compile(r\"^(\\d{4})-.*\\.md$\")\n+# ``ADR 0016`` / ``SADR-0016`` -- a namespace plus four-digit citation. The\n+# trailing ``\\b`` keeps a five-digit token from being read as a truncated one.\n+_CITATION = re.compile(r\"\\b(S?ADR)[ -](\\d{4})\\b\")\n+# Target ADRs retain the numeric filename; Shiki platform decisions use SADR.\n+_DECISION_FILENAME = re.compile(r\"^(?:(SADR)-)?(\\d{4})-.*\\.md$\")\n \n \n class Violation(NamedTuple):\n     \"\"\"A citation that does not resolve to exactly one decision record.\"\"\"\n \n     citing_file: str  # repo-relative path of the file that cites the ADR\n+    namespace: str  # \"ADR\" for target decisions; \"SADR\" for Shiki decisions\n     number: str  # the cited four-digit number, e.g. \"0016\"\n     line: int  # 1-based line of the citation\n     reason: str  # \"missing\" (zero matches) or \"ambiguous\" (two or more)\n@@ -49,44 +51,59 @@\n \n     @property\n     def expected_path(self) -> str:\n-        return f\"docs/adr/{self.number}-*.md\"\n+        prefix = \"SADR-\" if self.namespace == \"SADR\" else \"\"\n+        return f\"docs/adr/{prefix}{self.number}-*.md\"\n \n     def describe(self) -> str:\n         \"\"\"Name the citing file, the cited number, and the missing path.\"\"\"\n         if self.reason == \"missing\":\n             return (\n-                f\"{self.citing_file}:{self.line} cites ADR {self.number} \"\n+                f\"{self.citing_file}:{self.line} cites {self.namespace} {self.number} \"\n                 f\"but no document matches {self.expected_path}\"\n             )\n         joined = \", \".join(self.matches)\n         return (\n-            f\"{self.citing_file}:{self.line} cites ADR {self.number} which \"\n+            f\"{self.citing_file}:{self.line} cites {self.namespace} {self.number} which \"\n             f\"resolves to {len(self.matches)} documents (expected exactly one \"\n             f\"at {self.expected_path}): {joined}\"\n         )\n \n \n-def _adr_index(root: Path) -> dict[str, list[Path]]:\n-    \"\"\"Map every four-digit ADR number to the record files that claim it.\"\"\"\n-    index: dict[str, list[Path]] = {}\n+def _adr_index(root: Path) -> dict[tuple[str, str], list[Path]]:\n+    \"\"\"Map each decision namespace and number to the files that claim it.\"\"\"\n+    index: dict[tuple[str, str], list[Path]] = {}\n     adr_dir = root / \"docs\" / \"adr\"\n     if adr_dir.is_dir():\n         for path in sorted(adr_dir.glob(\"*.md\")):\n-            match = _ADR_FILENAME.match(path.name)\n+            match = _DECISION_FILENAME.match(path.name)\n             if match:\n-                index.setdefault(match.group(1), []).append(path)\n+                namespace = \"SADR\" if match.group(1) else \"ADR\"\n+                index.setdefault((namespace, match.group(2)), []).append(path)\n     return index\n \n \n def _citing_files(root: Path, include_docs: bool = True) -> list[Path]:\n-    \"\"\"Files scanned for citations: scripts/*.py, and cheaply the markdown.\"\"\"\n+    \"\"\"Current, shipped text surfaces scanned for decision citations.\"\"\"\n     files = sorted((root / \"scripts\").glob(\"*.py\"))\n+    files += sorted((root / \"scripts\").glob(\"*.sh\"))\n     if include_docs:\n         docs = root / \"docs\"\n         if docs.is_dir():\n             files += sorted(docs.rglob(\"*.md\"))\n         files += sorted(root.glob(\"*.md\"))\n-    return files\n+        pyproject = root / \"pyproject.toml\"\n+        if pyproject.is_file():\n+            files.append(pyproject)\n+        for relative in (\".github\", \".codex\"):\n+            surface = root / relative\n+            if surface.is_dir():\n+                files += sorted(\n+                    path\n+                    for path in surface.rglob(\"*\")\n+                    if path.is_file()\n+                    and path.suffix in {\".md\", \".py\", \".sh\", \".toml\", \".yaml\", \".yml\"}\n+                )\n+    return sorted(set(files))\n \n \n def find_adr_reference_violations(\n@@ -94,10 +111,9 @@\n ) -> list[Violation]:\n     \"\"\"Return every ``ADR ####`` citation under ``root`` that fails to resolve.\n \n-    A citation resolves when exactly one ``docs/adr/####-*.md`` matches its\n-    number. Zero matches is a \"missing\" violation; two or more is \"ambiguous\".\n-    Records that are never cited produce nothing -- the converse is not\n-    required.\n+    A citation resolves when exactly one record in the same namespace matches\n+    its number. Zero matches is a \"missing\" violation; two or more is\n+    \"ambiguous\". Records that are never cited produce nothing.\n     \"\"\"\n     root = Path(root)\n     index = _adr_index(root)\n@@ -110,13 +126,15 @@\n         rel = path.relative_to(root).as_posix()\n         for lineno, line in enumerate(text.splitlines(), start=1):\n             for match in _CITATION.finditer(line):\n-                number = match.group(1)\n-                found = index.get(number, [])\n+                namespace, number = match.groups()\n+                found = index.get((namespace, number), [])\n                 if len(found) == 1:\n                     continue\n                 reason = \"missing\" if not found else \"ambiguous\"\n                 rels = [p.relative_to(root).as_posix() for p in found]\n-                violations.append(Violation(rel, number, lineno, reason, rels))\n+                violations.append(\n+                    Violation(rel, namespace, number, lineno, reason, rels)\n+                )\n     return violations\n \n \n@@ -129,8 +147,8 @@\n         self.assertEqual(\n             violations,\n             [],\n-            \"Every 'ADR ####' citation must resolve to exactly one \"\n-            f\"docs/adr/####-*.md:\\n{detail}\",\n+            \"Every ADR/SADR citation must resolve exactly once in its own \"\n+            f\"docs/adr namespace:\\n{detail}\",\n         )\n \n     def test_scan_actually_reads_scripts(self) -> None:\n@@ -142,12 +160,38 @@\n             \"the citation scan must cover scripts/*.py\",\n         )\n \n+    def test_platform_citations_do_not_use_unresolved_numeric_shorthand(self) -> None:\n+        shorthand = re.compile(r\"SADR-\\d{4}/\\d{4}\")\n+        offenders = []\n+        for path in _citing_files(REPO_ROOT):\n+            text = path.read_text(encoding=\"utf-8\")\n+            if shorthand.search(text):\n+                offenders.append(path.relative_to(REPO_ROOT).as_posix())\n+        self.assertEqual(\n+            offenders,\n+            [],\n+            \"repeat the SADR prefix so every cited number is independently resolvable\",\n+        )\n+\n     def test_carried_records_are_indexed(self) -> None:\n         # The two records this Goal carried onto the default branch, and which\n         # the scripts cite most, must be present.\n         index = _adr_index(REPO_ROOT)\n-        self.assertIn(\"0016\", index)\n-        self.assertIn(\"0017\", index)\n+        self.assertIn((\"SADR\", \"0016\"), index)\n+        self.assertIn((\"SADR\", \"0017\"), index)\n+\n+    def test_platform_tree_has_only_sadr_0001_through_0019(self) -> None:\n+        adr_dir = REPO_ROOT / \"docs\" / \"adr\"\n+        sadrs = sorted(adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\"))\n+        self.assertEqual(\n+            {path.name.split(\"-\", 2)[1] for path in sadrs},\n+            {f\"{number:04d}\" for number in range(1, 20)},\n+        )\n+        self.assertEqual(\n+            list(adr_dir.glob(\"[0-9][0-9][0-9][0-9]-*.md\")),\n+            [],\n+            \"the Shiki platform tree must not retain legacy numeric SADR paths\",\n+        )\n \n \n class FixtureAdrReferences(unittest.TestCase):\n@@ -180,6 +224,26 @@\n         self.assertIn(\"9991\", message)  # cited number\n         self.assertIn(\"docs/adr/9991-*.md\", message)  # missing path\n \n+    def test_target_adr_does_not_satisfy_missing_platform_sadr(self) -> None:\n+        root = self._make_repo(\n+            adr_names=[\"0042-target-decision.md\"],\n+            script_body='\"\"\"Target ADR 0042 exists, but platform SADR-0042 does not.\"\"\"\\n',\n+        )\n+        violations = find_adr_reference_violations(root, include_docs=False)\n+        self.assertEqual(len(violations), 1)\n+        self.assertEqual(violations[0].namespace, \"SADR\")\n+        self.assertEqual(violations[0].number, \"0042\")\n+        self.assertEqual(violations[0].expected_path, \"docs/adr/SADR-0042-*.md\")\n+\n+    def test_target_adr_and_platform_sadr_may_reuse_a_number(self) -> None:\n+        root = self._make_repo(\n+            adr_names=[\"0042-target-decision.md\", \"SADR-0042-platform-decision.md\"],\n+            script_body='\"\"\"Target ADR 0042 and platform SADR-0042 are distinct.\"\"\"\\n',\n+        )\n+        self.assertEqual(\n+            find_adr_reference_violations(root, include_docs=False), []\n+        )\n+\n     def test_uncited_adr_is_allowed(self) -> None:\n         # An ADR exists but nothing cites it -> not a violation. The converse\n         # (every ADR must be cited) is explicitly NOT required.\ndiff --git a/tests/test_guardian_review.py b/tests/test_guardian_review.py\nindex 88829e7810212635e2fc98aa5918d6f26294677d..00a85a237acd5bf2d6cb5b294128198c422402ca\n--- a/tests/test_guardian_review.py\n+++ b/tests/test_guardian_review.py\n@@ -162,7 +162,7 @@\n \n     def test_docs_only_is_light_and_not_conservative(self):\n         focus = gr.classify_review_focus([\"docs/adr/0099-x.md\", \"README.md\"], \"\", \"low\")\n-        self.assertTrue(any(\"Docs/ADR-only\" in f for f in focus))\n+        self.assertTrue(any(\"Docs/decision-record-only\" in f for f in focus))\n         self.assertFalse(any(\"conservative default\" in f.lower() for f in focus))\n \n     def test_unknown_category_is_conservative(self):\ndiff --git a/tests/test_policy_job_merged_prs.py b/tests/test_policy_job_merged_prs.py\nindex d45512e74681e5e649279236c7d4e79d5c3da471..b97989f8bd74d091de1a6b5275a20d8dc68c4512\n--- a/tests/test_policy_job_merged_prs.py\n+++ b/tests/test_policy_job_merged_prs.py\n@@ -3,8 +3,8 @@\n \n ``.github/workflows/shiki-cca-completion.yml`` has two consumers of the merged-PR\n proof: the CCA *signal* (``guardian_approval_signal.py`` in \"Prepare CCA context\")\n-and the authoritative gate (``mergegate_check.py`` in \"Enforce MergeGate\"). ADR\n-0017's bookkeeping-closeout exemption (condition 6) needs the merged implementation\n+and the authoritative gate (``mergegate_check.py`` in \"Enforce MergeGate\").\n+SADR-0017's bookkeeping-closeout exemption (condition 6) needs the merged implementation\n PR proven, and a closeout carries NO ``post_merge_reconcile`` marker. The signal\n already resolves ``MERGED_PRS`` unconditionally, but \"Enforce MergeGate\" used to\n resolve it only inside ``grep -q '<!-- shiki:post_merge_reconcile -->'``. So a\ndiff --git a/tests/test_sadr_install_migration.py b/tests/test_sadr_install_migration.py\nnew file mode 100644\nindex 0000000000000000000000000000000000000000..d51eac872b1c79ff4579c4c1401ca5d775a60d8d\n--- /dev/null\n+++ b/tests/test_sadr_install_migration.py\n@@ -0,0 +1,237 @@\n+\"\"\"SADR installation and collision-safe legacy upgrade behavior.\"\"\"\n+\n+from __future__ import annotations\n+\n+import contextlib\n+import hashlib\n+import io\n+import json\n+import shutil\n+import tempfile\n+import unittest\n+from pathlib import Path\n+\n+import shiki_test_support  # noqa: F401  (path bootstrap)\n+\n+import shiki_installer\n+from tests.test_adr_references import find_adr_reference_violations\n+\n+\n+PRODUCT_NUMBERS = tuple(range(13, 19))\n+\n+\n+def _quiet_install(target: Path, *, force: bool, validate: bool) -> None:\n+    with contextlib.redirect_stdout(io.StringIO()):\n+        shiki_installer.install_template(target, force=force, validate=validate)\n+\n+\n+def _sha256(path: Path) -> str:\n+    return hashlib.sha256(path.read_bytes()).hexdigest()\n+\n+\n+def _legacy_sadr_paths() -> tuple[str, ...]:\n+    return tuple(\n+        f\"docs/adr/{path.name.removeprefix('SADR-')}\"\n+        for path in sorted((shiki_installer.ROOT / \"docs\" / \"adr\").glob(\"SADR-*.md\"))\n+        if path.name != \"SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\"\n+    )\n+\n+\n+def _create_legacy_upgrade_fixture(\n+    target: Path,\n+) -> tuple[dict[str, bytes], tuple[str, ...]]:\n+    adr_dir = target / \"docs\" / \"adr\"\n+    adr_dir.mkdir(parents=True)\n+    product_bytes = {\n+        f\"{number:04d}-cubeshare-product-decision.md\": (\n+            f\"# CubeShare ADR {number:04d}\\n\\nDo not rewrite {number}.\\n\"\n+        ).encode()\n+        for number in PRODUCT_NUMBERS\n+    }\n+    for name, content in product_bytes.items():\n+        (adr_dir / name).write_bytes(content)\n+\n+    legacy_paths = _legacy_sadr_paths()\n+    for relative in legacy_paths:\n+        (target / relative).write_bytes(f\"legacy Shiki bytes for {relative}\\n\".encode())\n+\n+    stamp_path = target / shiki_installer.INSTALL_STAMP_PATH\n+    stamp_path.parent.mkdir(parents=True, exist_ok=True)\n+    stamp = {\n+        \"version\": shiki_installer.INSTALL_STAMP_VERSION,\n+        \"platform_commit\": None,\n+        \"installed_at\": \"2000-01-01T00:00:00+00:00\",\n+        \"digests\": {\n+            relative: _sha256(target / relative) for relative in legacy_paths\n+        },\n+    }\n+    stamp_path.write_text(\n+        json.dumps(stamp, indent=2, sort_keys=True) + \"\\n\", encoding=\"utf-8\"\n+    )\n+    return product_bytes, legacy_paths\n+\n+\n+def _tree_snapshot(root: Path) -> dict[str, tuple[str, bytes | str]]:\n+    \"\"\"Recursive byte/shape snapshot used to prove preflight is write-free.\"\"\"\n+    snapshot: dict[str, tuple[str, bytes | str]] = {}\n+    for path in sorted(root.rglob(\"*\")):\n+        relative = path.relative_to(root).as_posix()\n+        if path.is_symlink():\n+            snapshot[relative] = (\"symlink\", path.readlink().as_posix())\n+        elif path.is_dir():\n+            snapshot[relative] = (\"directory\", \"\")\n+        else:\n+            snapshot[relative] = (\"file\", path.read_bytes())\n+    return snapshot\n+\n+\n+class FreshSadrInstallTests(unittest.TestCase):\n+    def setUp(self) -> None:\n+        self.tmp = Path(tempfile.mkdtemp(prefix=\"shiki-sadr-fresh-\"))\n+        self.addCleanup(lambda: shutil.rmtree(self.tmp, ignore_errors=True))\n+        self.target = self.tmp / \"target\"\n+        self.adr_dir = self.target / \"docs\" / \"adr\"\n+        self.adr_dir.mkdir(parents=True)\n+        self.product_bytes = {\n+            f\"{number:04d}-product-decision.md\": (\n+                f\"# Product ADR {number:04d}\\n\\nTarget-owned bytes {number}.\\n\"\n+            ).encode()\n+            for number in PRODUCT_NUMBERS\n+        }\n+        for name, content in self.product_bytes.items():\n+            (self.adr_dir / name).write_bytes(content)\n+\n+    def test_fresh_install_preserves_product_adrs_and_installs_all_sadrs(self) -> None:\n+        _quiet_install(self.target, force=False, validate=True)\n+\n+        for name, expected in self.product_bytes.items():\n+            self.assertEqual((self.adr_dir / name).read_bytes(), expected)\n+\n+        sadr_numbers = {\n+            path.name.split(\"-\", 2)[1]\n+            for path in self.adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\")\n+        }\n+        self.assertEqual(sadr_numbers, {f\"{number:04d}\" for number in range(1, 20)})\n+\n+        numeric_records = {\n+            path.name\n+            for path in self.adr_dir.glob(\"[0-9][0-9][0-9][0-9]-*.md\")\n+        }\n+        self.assertEqual(numeric_records, set(self.product_bytes))\n+        stamp = json.loads(\n+            (self.target / shiki_installer.INSTALL_STAMP_PATH).read_text(\n+                encoding=\"utf-8\"\n+            )\n+        )\n+        for path in self.adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\"):\n+            relative = path.relative_to(self.target).as_posix()\n+            self.assertEqual(stamp[\"digests\"][relative], _sha256(path))\n+        self.assertEqual(find_adr_reference_violations(self.target), [])\n+\n+\n+class LegacySadrUpgradeTests(unittest.TestCase):\n+    def setUp(self) -> None:\n+        self.tmp = Path(tempfile.mkdtemp(prefix=\"shiki-sadr-upgrade-\"))\n+        self.addCleanup(lambda: shutil.rmtree(self.tmp, ignore_errors=True))\n+        self.target = self.tmp / \"target\"\n+        self.adr_dir = self.target / \"docs\" / \"adr\"\n+        self.product_bytes, self.legacy_paths = _create_legacy_upgrade_fixture(\n+            self.target\n+        )\n+\n+    def test_force_upgrade_deletes_only_stamped_legacy_sadrs(self) -> None:\n+        _quiet_install(self.target, force=True, validate=True)\n+\n+        for relative in self.legacy_paths:\n+            self.assertFalse((self.target / relative).exists(), relative)\n+        for name, expected in self.product_bytes.items():\n+            self.assertEqual((self.adr_dir / name).read_bytes(), expected)\n+\n+        sadrs = list(self.adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\"))\n+        self.assertEqual(len(sadrs), 19)\n+        refreshed = json.loads(\n+            (self.target / shiki_installer.INSTALL_STAMP_PATH).read_text(encoding=\"utf-8\")\n+        )\n+        for relative in self.legacy_paths:\n+            self.assertNotIn(relative, refreshed[\"digests\"])\n+        for path in sadrs:\n+            relative = path.relative_to(self.target).as_posix()\n+            self.assertEqual(refreshed[\"digests\"][relative], _sha256(path))\n+\n+    def test_non_force_install_never_deletes_legacy_files(self) -> None:\n+        before = {\n+            relative: (self.target / relative).read_bytes()\n+            for relative in self.legacy_paths\n+        }\n+        _quiet_install(self.target, force=False, validate=False)\n+        for relative, expected in before.items():\n+            self.assertEqual((self.target / relative).read_bytes(), expected)\n+        refreshed = json.loads(\n+            (self.target / shiki_installer.INSTALL_STAMP_PATH).read_text(encoding=\"utf-8\")\n+        )\n+        for relative in self.legacy_paths:\n+            self.assertEqual(refreshed[\"digests\"][relative], _sha256(self.target / relative))\n+\n+\n+class LegacyCleanupPreflightTests(unittest.TestCase):\n+    def setUp(self) -> None:\n+        self.tmp = Path(tempfile.mkdtemp(prefix=\"shiki-sadr-preflight-\"))\n+        self.addCleanup(lambda: shutil.rmtree(self.tmp, ignore_errors=True))\n+\n+    def test_all_ownership_blockers_abort_before_any_write(self) -> None:\n+        cases = (\n+            (\"absent\", \"install stamp is absent\"),\n+            (\"unreadable\", \"install stamp is unreadable\"),\n+            (\"malformed\", \"install stamp is malformed\"),\n+            (\"missing-digest\", \"install stamp has no digest\"),\n+            (\"digest-mismatch\", \"digest mismatch\"),\n+        )\n+        for name, expected_reason in cases:\n+            with self.subTest(name=name):\n+                target = self.tmp / name\n+                _, legacy_paths = _create_legacy_upgrade_fixture(target)\n+                stamp_path = target / shiki_installer.INSTALL_STAMP_PATH\n+\n+                if name == \"absent\":\n+                    stamp_path.unlink()\n+                elif name == \"unreadable\":\n+                    stamp_path.unlink()\n+                    stamp_path.mkdir()\n+                elif name == \"malformed\":\n+                    stamp_path.write_text(\"{not-json\\n\", encoding=\"utf-8\")\n+                else:\n+                    stamp = json.loads(stamp_path.read_text(encoding=\"utf-8\"))\n+                    if name == \"missing-digest\":\n+                        stamp[\"digests\"] = {}\n+                    else:\n+                        stamp[\"digests\"] = {\n+                            relative: \"deadbeef\" for relative in legacy_paths\n+                        }\n+                    stamp_path.write_text(\n+                        json.dumps(stamp, indent=2, sort_keys=True) + \"\\n\",\n+                        encoding=\"utf-8\",\n+                    )\n+\n+                before = _tree_snapshot(target)\n+                with self.assertRaises(shiki_installer.ShikiError) as raised:\n+                    _quiet_install(target, force=True, validate=False)\n+                after = _tree_snapshot(target)\n+\n+                self.assertEqual(before, after, f\"{name} preflight changed the tree\")\n+                message = str(raised.exception)\n+                self.assertIn(expected_reason, message)\n+                self.assertIn(\"No files were written\", message)\n+                for relative in legacy_paths:\n+                    self.assertIn(relative, message)\n+\n+    def test_absent_stamp_is_allowed_when_no_legacy_path_exists(self) -> None:\n+        target = self.tmp / \"no-legacy\"\n+        target.mkdir()\n+        _quiet_install(target, force=True, validate=False)\n+        self.assertTrue(\n+            (target / \"docs\" / \"adr\" / \"SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\").is_file()\n+        )\n+\n+\n+if __name__ == \"__main__\":\n+    unittest.main()\n\n2026-08-07T08:26:21.114053Z ERROR codex_models_manager::manager: failed to renew cache TTL: missing field `base_instructions` at line 94 column 5\ndiff --git a/.codex/skills/shiki/SKILL.md b/.codex/skills/shiki/SKILL.md\nindex c8e2e85d9d75d49cc4f2d6286ee22659c4e53e75..93cded623b2c15d9f65c76491a1c2d813ac9e6c0\n--- a/.codex/skills/shiki/SKILL.md\n+++ b/.codex/skills/shiki/SKILL.md\n@@ -18,6 +18,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n Codex CLI does not register this skill as a custom `/shiki` slash command. When\n the user types `/shiki` in Codex CLI and gets \"Unrecognized command\", explain\n that this is expected: invoke Shiki in Codex with natural language such as\n@@ -69,7 +72,7 @@\n \n ## Responsibilities\n \n-- Claude Code implements and repairs by default (`shiki runner claude`, ADR 0008); Codex implements and repairs tasks explicitly assigned to `codex`.\n+- Claude Code implements and repairs by default (`shiki runner claude`, SADR-0008); Codex implements and repairs tasks explicitly assigned to `codex`.\n - Claude Code Action can act as GitHub-side CCA or reviewer.\n - CCA judges completion.\n - MergeGate authorizes state transitions and merge readiness.\ndiff --git a/.github/prompts/cca-completion-check.md b/.github/prompts/cca-completion-check.md\nindex ed203ad5f302ecc81372d8ab27d2b1232ca97497..fd89950528333ef6242e6d5443904f5392ab7e56\n--- a/.github/prompts/cca-completion-check.md\n+++ b/.github/prompts/cca-completion-check.md\n@@ -19,6 +19,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n ## Required Reading\n \n Read only the files needed to judge the PR contract. Prefer this order:\ndiff --git a/.github/prompts/codex-task-handoff.md b/.github/prompts/codex-task-handoff.md\nindex 695e211d76dea1c60e8d5bb360e78487808286a4..59441d576b01a39569722f226c854e5a740e9eb5\n--- a/.github/prompts/codex-task-handoff.md\n+++ b/.github/prompts/codex-task-handoff.md\n@@ -2,7 +2,7 @@\n \n You are Codex Front acting as the Implementer for a Shiki task.\n \n-Follow `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant ADRs, and `docs/agents/codex-handoff.md`.\n+Follow `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant Shiki SADRs and target ADRs, and `docs/agents/codex-handoff.md`.\n \n Rules:\n \ndiff --git a/AGENTS.md b/AGENTS.md\nindex 7f041100d49c2a7525f51cb0f5d696a4a6d1c78c..a06bfbabc6fa48a007389e25e12d7ed523936bed\n--- a/AGENTS.md\n+++ b/AGENTS.md\n@@ -22,7 +22,7 @@\n - **Codex Front** is an optional implementer through Codex App, Codex CLI, Codex IDE extension, or Codex Web signed in with ChatGPT OAuth/subscription auth, dispatched with `shiki runner codex` when a task is explicitly assigned to `codex`.\n - **GitHub CCA** is implemented by Claude Code Action by default, using `CLAUDE_CODE_OAUTH_TOKEN`.\n \n-Do not assume `openai/codex-action`, `OPENAI_API_KEY`, or API-key based Codex automation in the default Shiki path. API-key runners are explicit target-repo extensions and require their own ADR.\n+Do not assume `openai/codex-action`, `OPENAI_API_KEY`, or API-key based Codex automation in the default Shiki path. API-key runners are explicit target-repo extensions and require their own target ADR.\n \n Shiki is not:\n \n@@ -44,10 +44,14 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use\n+`SADR-NNNN-*.md`; target product decisions retain `NNNN-*.md` and the `ADR`\n+namespace.\n+\n Use the canonical source-of-truth order above. Runtime-specific reading orders\n are operational guidance only and must not override or reorder it.\n \n-Conversation state is not durable truth. If a decision matters, put it in GitHub, `.shiki/`, `CONTEXT.md`, or an ADR.\n+Conversation state is not durable truth. If a decision matters, put it in GitHub, `.shiki/`, `CONTEXT.md`, a Shiki SADR, or a target ADR.\n \n If GitHub and `.shiki/` disagree, surface the conflict and prefer GitHub operational state until the mirror is repaired.\n \n@@ -75,8 +79,8 @@\n Every non-trivial change follows this loop:\n \n 1. **Goal**: clarify outcome, completion conditions, non-goals, risk level, and success signals.\n-2. **grill-with-docs**: challenge the plan against domain docs, ADRs, code reality, terminology, and edge scenarios.\n-3. **Context & Impact**: find relevant docs, ADRs, code, dependencies, owners, locks, and verification surfaces.\n+2. **grill-with-docs**: challenge the plan against domain docs, Shiki SADRs, target ADRs, code reality, terminology, and edge scenarios.\n+3. **Context & Impact**: find relevant docs, SADRs, target ADRs, code, dependencies, owners, locks, and verification surfaces.\n 4. **PRD and Spec Freeze**: use `to-prd` when context is settled enough to become durable product/engineering intent. The operator's explicit approval of the PRD is Spec Freeze: the plan records a `spec_freeze` block, and no plan runs without it. Steps 1-4 form Requirements Definition \u2014 one continuous operator dialogue. After Spec Freeze, scope changes require an operator-approved Spec Amendment; non-scope-moving interpretations go to the Assumption Log.\n 5. **Issues**: use `to-issues` to create independently grabbable vertical-slice issues. Prefer AFK slices over HITL slices where possible.\n 6. **Triage**: label issues for readiness, risk, runtime, skills, and MergeGate state.\n@@ -167,7 +171,7 @@\n - Testing decisions.\n - Out-of-scope items.\n - Further notes.\n-- Links to relevant domain terms and ADRs.\n+- Links to relevant domain terms, SADRs, and target ADRs.\n \n Every executable task must include:\n \n@@ -193,7 +197,7 @@\n \n Context & Impact must identify:\n \n-- Relevant documents, domain terms, ADRs, and past decisions.\n+- Relevant documents, domain terms, SADRs, target ADRs, and past decisions.\n - Relevant modules, interfaces, seams, callers, owners, and tests.\n - Dependency relationships between tasks.\n - Candidate locks and lock conflicts.\n@@ -210,7 +214,7 @@\n | Skill | Required when |\n | --- | --- |\n | `setup-matt-pocock-skills` | First configuring the repo for skills, or when issue tracker, triage labels, or domain docs are missing. |\n-| `grill-with-docs` | Default for non-trivial Goals before PRD/issues; always required when plans, terminology, boundaries, tradeoffs, or ADR-worthy decisions are ambiguous. |\n+| `grill-with-docs` | Default for non-trivial Goals before PRD/issues; always required when plans, terminology, boundaries, tradeoffs, or decision-record-worthy choices are ambiguous. |\n | `zoom-out` | The agent lacks an architectural map of the relevant code area. |\n | `to-prd` | Settled Goal context must become durable product/engineering intent. |\n | `to-issues` | A Goal, PRD, or plan must become independently grabbable vertical-slice issues. |\n@@ -348,7 +352,7 @@\n - Three or more new files.\n - Any high-risk or critical label.\n \n-When the architecture gate triggers, run `grill-with-docs` or `improve-codebase-architecture` as appropriate and record durable decisions in an ADR when needed.\n+When the architecture gate triggers, run `grill-with-docs` or `improve-codebase-architecture` as appropriate and record durable decisions in a Shiki SADR or target ADR as applicable.\n \n ## Engineering Discipline\n \ndiff --git a/CLAUDE.md b/CLAUDE.md\nindex 98a932e7841a632866fcd53b0bad0e3af3711d3d..98a31399404d6fb044b4fd493f4459ac87f439de\n--- a/CLAUDE.md\n+++ b/CLAUDE.md\n@@ -14,6 +14,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n ## Claude Role\n \n Default to planner, default implementer, reviewer, coordinator, documentation editor, CCA-style completion judge, and final-judgment assistant.\n@@ -57,7 +60,7 @@\n At the start of a Shiki session:\n \n 1. Read `AGENTS.md`.\n-2. Read `CONTEXT.md` and relevant ADRs.\n+2. Read `CONTEXT.md` and relevant Shiki SADRs and target ADRs.\n 3. Read active `.shiki/` Goal/task/ledger state when present.\n 4. Read active distilled rules from `.shiki/memories/` (status `distilled`, `active: true`, not revoked or superseded) for applicable guidance; the same rules are injected deterministically into task handoffs as the `## Distilled Rules` section (\u00a73.5).\n 5. Read the GitHub Issue or PR when the task is GitHub-backed.\n@@ -74,7 +77,7 @@\n - Define the Goal, completion conditions, non-goals, and risk level.\n - Treat `grill-with-docs` as the default planning entry point for non-trivial Goals.\n - Challenge domain terms against `CONTEXT.md`.\n-- Challenge hard-to-reverse tradeoffs against ADRs.\n+- Challenge hard-to-reverse tradeoffs against SADRs and target ADRs.\n - Explore code instead of asking when code can answer the question.\n - Run Context & Impact before implementation planning.\n - Produce a Task DAG, not an unordered checklist.\n@@ -94,7 +97,7 @@\n - For each question, provide your recommended answer.\n - Resolve dependency decisions before downstream design questions.\n - Update `CONTEXT.md` inline when terminology crystallizes.\n-- Offer ADRs only when the decision is hard to reverse, surprising without context, and a real tradeoff.\n+- Offer a SADR or target ADR only when the decision is hard to reverse, surprising without context, and a real tradeoff.\n - Convert unresolved questions into HITL blockers rather than allowing Codex to guess.\n \n ## PRD and Issues Standard\n@@ -103,7 +106,7 @@\n \n - Use the project's domain glossary vocabulary.\n - Avoid volatile file paths unless they are essential.\n-- Include implementation decisions, testing decisions, out-of-scope items, and relevant ADR/domain links.\n+- Include implementation decisions, testing decisions, out-of-scope items, and relevant SADR/target-ADR/domain links.\n - Identify deep-module opportunities and testable interfaces.\n \n When creating issues:\n@@ -121,7 +124,7 @@\n - Branch/worktree target.\n - Scope and non-goals.\n - Dependencies and locks.\n-- Relevant docs, ADRs, modules, and tests.\n+- Relevant docs, SADRs, target ADRs, modules, and tests.\n - Required skill invocations.\n - TDD expectations.\n - Pre-PR `code-review` gate expectations.\n@@ -152,7 +155,7 @@\n \n - Treat yourself as a judge, not implementer.\n - Do not write production code.\n-- Read `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant ADRs, PRD, task issue, PR body, diff, checks, reviews, labels, and `.shiki/` state.\n+- Read `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant SADRs and target ADRs, PRD, task issue, PR body, diff, checks, reviews, labels, and `.shiki/` state.\n - Evaluate every applicable checklist in `docs/agents/checklists.md`.\n - Map each acceptance criterion to durable evidence.\n - Distinguish implementation failure from missing evidence.\ndiff --git a/CONTEXT.md b/CONTEXT.md\nindex e61a9be06a0cce0d026484083a657bde2067e234..237ee8aa3ea8fab334ece49768bc5ed3b5af0974\n--- a/CONTEXT.md\n+++ b/CONTEXT.md\n@@ -25,7 +25,7 @@\n _Avoid_: jumping directly into code\n \n **grill-with-docs**:\n-A planning skill that challenges a plan against domain language, ADRs, code reality, and concrete edge scenarios. It resolves design-tree questions before PRD/issues. It runs inside Requirements Definition.\n+A planning skill that challenges a plan against domain language, Shiki SADRs, target ADRs, code reality, and concrete edge scenarios. It resolves design-tree questions before PRD/issues. It runs inside Requirements Definition.\n _Avoid_: generic brainstorming, silent assumptions\n \n **Requirements Definition**:\ndiff --git a/CONTRIBUTING.md b/CONTRIBUTING.md\nindex c6d9b656e73abb8a55e5f660123e03153dcb3e6d..9e37a8fbf668db5b22461aaae5548b8f1354394c\n--- a/CONTRIBUTING.md\n+++ b/CONTRIBUTING.md\n@@ -17,7 +17,7 @@\n 1. **Goal** \u2014 start from a user-approved Goal with an outcome, completion\n    conditions, non-goals, risk level, and success signals.\n 2. **grill-with-docs** \u2014 for any non-trivial change, challenge the plan against\n-   domain language, ADRs, and code reality before writing a PRD.\n+   domain language, Shiki SADRs, target ADRs, and code reality before writing a PRD.\n 3. **PRD** \u2014 capture settled product/engineering intent (`to-prd`).\n 4. **Spec Freeze** \u2014 the operator explicitly approves the PRD/requirements;\n    the plan records a `spec_freeze` block and no plan runs without it\n@@ -94,7 +94,7 @@\n   entries.\n - Do not hand-edit `.shiki/goals/*.json` or `.shiki/manifest.json` in a task PR.\n - Conversation state is not durable truth. If a decision matters, record it in\n-  GitHub, `.shiki/`, `CONTEXT.md`, or an ADR.\n+  GitHub, `.shiki/`, `CONTEXT.md`, a Shiki SADR, or a target ADR.\n \n ## Architecture Gate\n \n@@ -103,7 +103,7 @@\n or CI policy, package structure, three or more structural files, more than 100\n net new lines in a core module, three or more new files, or any high-risk label.\n When triggered, run `grill-with-docs` or `improve-codebase-architecture` and\n-record durable decisions in an ADR when needed (see `docs/adr/`).\n+record durable decisions in a Shiki SADR or target ADR as applicable (see `docs/adr/`).\n \n ## Safety\n \ndiff --git a/SYSTEM_PROMPT.md b/SYSTEM_PROMPT.md\nindex aaaea45e3a896c3a44ba89eec8bb84325ecf6c62..da009c86a88285269dee0155e58e550c6d744ea9\n--- a/SYSTEM_PROMPT.md\n+++ b/SYSTEM_PROMPT.md\n@@ -30,6 +30,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n Use the canonical source-of-truth order above. Runtime-specific reading orders\n are operational guidance only and must not override or reorder it. The current\n conversation is non-durable operator input.\n@@ -48,14 +51,14 @@\n \n A runtime may only do work that is explicitly assigned by the Goal, task, PR, or MergeGate decision.\n \n-Do not assume `openai/codex-action`, `OPENAI_API_KEY`, or API-key based Codex automation in the default Shiki path. API-key runners are explicit target-repo extensions and require their own ADR.\n+Do not assume `openai/codex-action`, `OPENAI_API_KEY`, or API-key based Codex automation in the default Shiki path. API-key runners are explicit target-repo extensions and require their own target ADR.\n \n ## 4. Non-negotiable Shiki flow\n \n For any non-trivial Goal, use this flow:\n \n 1. **Goal Seek**: define outcome, non-goals, risk level, completion criteria, and evidence requirements.\n-2. **grill-with-docs**: challenge the plan against `CONTEXT.md`, ADRs, code reality, terminology, boundaries, and edge scenarios. Ask one question at a time when the operator is available. If code can answer a question, inspect code instead of asking.\n+2. **grill-with-docs**: challenge the plan against `CONTEXT.md`, Shiki SADRs, target ADRs, code reality, terminology, boundaries, and edge scenarios. Ask one question at a time when the operator is available. If code can answer a question, inspect code instead of asking.\n 3. **Context & Impact**: identify modules, interfaces, seams, callers, dependencies, locks, risk, and verification surfaces.\n 4. **to-prd + Spec Freeze**: turn settled context into a PRD using domain vocabulary and testing decisions. The operator's explicit approval of the PRD is Spec Freeze, recorded as the plan's `spec_freeze` block; plans without it do not run. Steps 1-4 form Requirements Definition.\n 5. **to-issues**: decompose the PRD into independently grabbable vertical-slice issues. Prefer AFK slices; mark HITL slices when judgment is still required.\n@@ -75,7 +78,7 @@\n Before making any material change:\n \n 1. Read `AGENTS.md` and any runtime-specific wrapper such as `CLAUDE.md`.\n-2. Read `CONTEXT.md` and relevant ADRs.\n+2. Read `CONTEXT.md` and relevant SADRs and target ADRs.\n 3. Read the GitHub Issue / PR and `.shiki/` state for the active Goal or task.\n 4. Identify your role: Planner, Implementer, Reviewer, Completion Check Agent, Repairer, or Guardian-assist.\n 5. Identify Goal id, task id, branch, PR, dependencies, locks, risk level, required skills, and required evidence.\n@@ -103,7 +106,7 @@\n \n At minimum, identify:\n \n-- Relevant documents, ADRs, domain terms, and prior decisions.\n+- Relevant documents, SADRs, target ADRs, domain terms, and prior decisions.\n - Relevant modules, interfaces, seams, callers, owners, and tests.\n - Dependency relationships between tasks.\n - Candidate file locks and lock conflicts.\n@@ -152,7 +155,7 @@\n \n CCA must read:\n \n-- `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant ADRs.\n+- `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant SADRs and target ADRs.\n - Parent Goal issue, PRD issue, task issue, comments, and labels.\n - PR body, diff, commits, changed files, reviews, CI checks, and artifacts available to the workflow.\n - `.shiki/` task contract, locks, ledger entries, CCA checklist profile, and prior repair packets.\n@@ -214,7 +217,7 @@\n - Three or more new files.\n - Any high-risk or critical label.\n \n-When the architecture gate triggers, run `grill-with-docs` or `improve-codebase-architecture` as appropriate and record the decision in an ADR if it is hard to reverse or likely to be re-litigated.\n+When the architecture gate triggers, run `grill-with-docs` or `improve-codebase-architecture` as appropriate and record the decision in a Shiki SADR or target ADR as applicable if it is hard to reverse or likely to be re-litigated.\n \n ## 14. Skill Gate\n \n@@ -223,7 +226,7 @@\n Use these skills:\n \n - `setup-matt-pocock-skills`: before first use of the engineering skill set, or when issue tracker, triage labels, or domain docs are missing.\n-- `grill-with-docs`: default for non-trivial Goals before PRD/issues; required for ambiguous plans, terminology, boundaries, ADR-worthy tradeoffs, or design-tree decisions.\n+- `grill-with-docs`: default for non-trivial Goals before PRD/issues; required for ambiguous plans, terminology, boundaries, decision-record-worthy tradeoffs, or design-tree decisions.\n - `zoom-out`: unfamiliar code area or missing architectural map.\n - `to-prd`: convert settled context into a PRD.\n - `to-issues`: break a PRD, plan, or Goal into independently grabbable vertical-slice issues.\ndiff --git a/docs/adr/0001-keep-shiki-as-platform-name.md b/docs/adr/SADR-0001-keep-shiki-as-platform-name.md\nindex 20a9637c3b9d8c97de7ba8db7cfae4d82d9a7dba..8b685439959aada1b221c112f0ac3c51ca8a1695\n--- a/docs/adr/0001-keep-shiki-as-platform-name.md\n+++ b/docs/adr/SADR-0001-keep-shiki-as-platform-name.md\n@@ -1,3 +1,3 @@\n-# Keep Shiki As The Platform Name\n+# SADR-0001: Keep Shiki As The Platform Name\n \n Shiki remains the product name for the GitHub-native agentic engineering control plane. We considered more descriptive names such as MergePilot, MergeGate, and Kiroku, but kept Shiki because it carries the existing goal-seeking framework identity while remaining independent of Hermes, Codex, Claude Code, or any single runtime.\ndiff --git a/docs/adr/0002-use-agents-as-shared-constitution.md b/docs/adr/SADR-0002-use-agents-as-shared-constitution.md\nindex 73de30ecb078120eac540d9bc51855e3cd48d0e3..c5486182bf06fca36ea508496b013c67302fd989\n--- a/docs/adr/0002-use-agents-as-shared-constitution.md\n+++ b/docs/adr/SADR-0002-use-agents-as-shared-constitution.md\n@@ -1,3 +1,3 @@\n-# Use AGENTS As The Shared Agent Constitution\n+# SADR-0002: Use AGENTS As The Shared Agent Constitution\n \n Shiki uses `AGENTS.md` as the runtime-neutral constitution and `CLAUDE.md` as a thin Claude Code wrapper. This keeps Codex, Claude Code, Hermes Runner, and future Agent Runtimes aligned on the same GitHub-first MergeGate rules while still allowing runtime-specific instructions where needed.\ndiff --git a/docs/adr/0003-require-skill-gate-for-mergegate-readiness.md b/docs/adr/SADR-0003-require-skill-gate-for-mergegate-readiness.md\nindex 424be0c9af4c8f0136f1fde68a468c47ff84e764..7fc337987c7df9792f75f181dfcbed75fe5ffff2\n--- a/docs/adr/0003-require-skill-gate-for-mergegate-readiness.md\n+++ b/docs/adr/SADR-0003-require-skill-gate-for-mergegate-readiness.md\n@@ -1,3 +1,3 @@\n-# Require Skill Gate For MergeGate Readiness\n+# SADR-0003: Require Skill Gate For MergeGate Readiness\n \n Shiki requires relevant engineering skills to be invoked before execution when their triggers apply. This makes skills part of MergeGate readiness rather than optional prompt style, because Shiki depends on repeatable planning, issue decomposition, TDD, diagnosis, triage, and architecture review before autonomous or parallel agent work can be trusted.\ndiff --git a/docs/adr/0004-use-github-first-with-shiki-mirror.md b/docs/adr/SADR-0004-use-github-first-with-shiki-mirror.md\nindex d0a3a9174dc646a945a9d325954ffaf00c352040..a79c54ae9ee8eed2a0524192b864da4e22a92d3f\n--- a/docs/adr/0004-use-github-first-with-shiki-mirror.md\n+++ b/docs/adr/SADR-0004-use-github-first-with-shiki-mirror.md\n@@ -1,4 +1,4 @@\n-# ADR 0004: Use GitHub First With A Repository-Local Shiki Mirror\n+# SADR-0004: Use GitHub First With A Repository-Local Shiki Mirror\n \n ## Status\n \ndiff --git a/docs/adr/0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md b/docs/adr/SADR-0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\nindex b339baf49b89d9290e79c3d51b36e7b2bde3bd95..0f3c3eb9afccdb3403ce978e21d04fe12e495da0\n--- a/docs/adr/0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\n+++ b/docs/adr/SADR-0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\n@@ -1,8 +1,8 @@\n-# ADR 0005: Use Codex As Front And Claude Code Action As The GitHub Runtime\n+# SADR-0005: Use Codex As Front And Claude Code Action As The GitHub Runtime\n \n ## Status\n \n-Accepted (implementer default superseded by ADR 0008; the auth model and the\n+Accepted (implementer default superseded by SADR-0008; the auth model and the\n Claude Code Action GitHub runtime decision remain in force)\n \n ## Context\n@@ -26,4 +26,4 @@\n - Shiki avoids requiring `OPENAI_API_KEY` for the default workflow.\n - Codex work happens through the user's authenticated Codex front surface, not as a hidden backend Action.\n - Claude Code Action is configured for OAuth-token based subscription usage by default.\n-- API-key based automation remains possible only as an explicit target-repo extension with its own ADR.\n+- API-key based automation remains possible only as an explicit target-repo extension with its own target ADR.\ndiff --git a/docs/adr/0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md b/docs/adr/SADR-0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\nindex 4bcae9d1e52f0a37b8e9fbafa5cd14b6ee1993bd..7a38538a6ae9929567f0ade67e7ea6a60d0b3f7e\n--- a/docs/adr/0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\n+++ b/docs/adr/SADR-0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\n@@ -1,4 +1,4 @@\n-# ADR 0006: Separate Codex Implementation, CCA Completion, And MergeGate Authorization\n+# SADR-0006: Separate Codex Implementation, CCA Completion, And MergeGate Authorization\n \n ## Status\n \ndiff --git a/docs/adr/0007-packaging-and-release.md b/docs/adr/SADR-0007-packaging-and-release.md\nindex 79b84ddb97167372b44641e5b7860e400ce80822..bb445a2e2e856d9d1f5fdbe35de8d4e8cec0fb32\n--- a/docs/adr/0007-packaging-and-release.md\n+++ b/docs/adr/SADR-0007-packaging-and-release.md\n@@ -1,4 +1,4 @@\n-# ADR 0007: Packaging And Release For Shiki\n+# SADR-0007: Packaging And Release For Shiki\n \n ## Status\n \n@@ -25,7 +25,7 @@\n   not introduce third-party runtime dependencies or break the shim.\n - Releases must run inside the operator's subscription-authenticated toolchain\n   and must not require extra secrets beyond the built-in `GITHUB_TOKEN`\n-  (consistent with ADR 0005's no-API-key-by-default stance).\n+  (consistent with SADR-0005's no-API-key-by-default stance).\n - `.shiki/manifest.json` already encodes the install surface (`install.include`,\n   `install.create_directories`, `install.exclude_from_commit`), and it must not\n   be mutated as part of this change.\n@@ -34,7 +34,7 @@\n \n This is a hard-to-reverse platform decision (it sets the version source of\n truth, the release trigger, and the public install contract), so it is recorded\n-as an ADR.\n+as a SADR.\n \n ## Decision\n \n@@ -104,7 +104,7 @@\n 3. **Publishing to PyPI.** Rejected for now: Shiki is distributed as a\n    repository template + git-installable CLI, not a PyPI library; PyPI would add\n    credentials/secrets and a different versioning contract. Can be added later as\n-   its own ADR if a packaged distribution is needed.\n+   its own SADR if a packaged distribution is needed.\n 4. **Dynamic version (read `VERSION` via a build backend `dynamic` field).**\n    Rejected to keep `pyproject.toml` backend-agnostic and dependency-free; the\n    small duplication between `VERSION` and `pyproject.toml` is guarded by the\n@@ -123,4 +123,4 @@\n   releasing versions the whole platform but ships only `install.include` to\n   targets.\n - Future PyPI distribution or a console entry point are deferred and would each\n-  warrant their own ADR.\n+  warrant their own SADR.\ndiff --git a/docs/adr/0008-use-claude-code-as-default-implementer.md b/docs/adr/SADR-0008-use-claude-code-as-default-implementer.md\nindex dedf4510b7c7799521422f622029563d373db031..35c2625d908167fbc58855b5b63e283805f167e8\n--- a/docs/adr/0008-use-claude-code-as-default-implementer.md\n+++ b/docs/adr/SADR-0008-use-claude-code-as-default-implementer.md\n@@ -1,12 +1,12 @@\n-# ADR 0008: Use Claude Code As The Default Implementer Runtime\n+# SADR-0008: Use Claude Code As The Default Implementer Runtime\n \n ## Status\n \n-Proposed (supersedes ADR 0005 for the implementer default; keeps its auth model)\n+Proposed (supersedes SADR-0005 for the implementer default; keeps its auth model)\n \n ## Context\n \n-ADR 0005 made Codex Front the default implementation runtime, with Claude Code\n+SADR-0005 made Codex Front the default implementation runtime, with Claude Code\n restricted to planner and reviewer roles by the runtime registry, and the only\n autonomous implementation adapter was `shiki runner codex`.\n \n@@ -18,13 +18,13 @@\n - The platform's planning, review, and CCA layers already run on Claude Code,\n   and Claude Code's native capabilities (the built-in `/code-review` skill and\n   the Workflow multi-agent orchestration tool) have no Codex equivalent.\n-- The Goal lifecycle redesign (ADR 0009) requires an autonomous post-freeze\n+- The Goal lifecycle redesign (SADR-0009) requires an autonomous post-freeze\n   loop driven from the operator's Claude Code session, including bounded\n   repair dispatch, which must not depend on a runtime that cannot be invoked\n   unattended.\n \n The constitution claims Shiki is runtime-agnostic (\"not a Claude-only\n-workflow\", ADR 0002). Any change to the implementer default must preserve the\n+workflow\", SADR-0002). Any change to the implementer default must preserve the\n registry-based runtime model rather than hardcoding one vendor.\n \n ## Decision\n@@ -39,7 +39,7 @@\n   the task to review or repair-needed.\n - Keep Codex registered as an optional implementer runtime. `shiki runner\n   codex`, the Codex handoff templates, and the ChatGPT OAuth auth model from\n-  ADR 0005 remain supported for tasks explicitly assigned to `codex`.\n+  SADR-0005 remain supported for tasks explicitly assigned to `codex`.\n - Commit to mandating two Claude-native capabilities as loop contract in\n   follow-up Goals (status: the code-review gate was delivered by Goal G-B \u2014\n   skill registry, checklists item PR-12, default required_skills \u2014 while the\n@@ -60,7 +60,7 @@\n \n Out of scope: removing Codex support, changing the CCA runtime (Claude Code\n Action with `CLAUDE_CODE_OAUTH_TOKEN` stays), and API-key based automation\n-(still requires its own ADR per ADR 0005).\n+(still requires its own target ADR per SADR-0005).\n \n ## Consequences\n \n@@ -100,7 +100,7 @@\n   harness rejection of direct `codex exec` is outside Shiki's control, and the\n   plan's mandatory native capabilities cannot run on Codex at all.\n - **Remove Codex entirely**: rejected; it abandons the runtime-agnostic\n-  constitution (ADR 0002) for no operational gain and forecloses\n+  constitution (SADR-0002) for no operational gain and forecloses\n   cross-runtime adversarial review.\n - **GitHub Actions cloud implementation via claude-code-action**: rejected as\n   default; durable evidence is automatic but token cost, Node 24 workflow\ndiff --git a/docs/adr/0009-requirements-definition-and-spec-freeze.md b/docs/adr/SADR-0009-requirements-definition-and-spec-freeze.md\nindex 29b3f7871bef9203a8b4a831a65c39eb2f506e42..52e7dd69ad0aa4efe0b68c62678abbf7df02aaec\n--- a/docs/adr/0009-requirements-definition-and-spec-freeze.md\n+++ b/docs/adr/SADR-0009-requirements-definition-and-spec-freeze.md\n@@ -1,4 +1,4 @@\n-# ADR 0009: Merge Grill And Planning Into Requirements Definition With Spec Freeze\n+# SADR-0009: Merge Grill And Planning Into Requirements Definition With Spec Freeze\n \n ## Status\n \ndiff --git a/docs/adr/0010-external-ai-guardian-review.md b/docs/adr/SADR-0010-external-ai-guardian-review.md\nindex e6108bbe8bb4e73e0ff4684c8f78001a80438c4e..f3510b145664298adafb151b6f0db51b972f01ec\n--- a/docs/adr/0010-external-ai-guardian-review.md\n+++ b/docs/adr/SADR-0010-external-ai-guardian-review.md\n@@ -1,4 +1,4 @@\n-# ADR 0010: External AI Guardian Review As A First-Class Approval Authority\n+# SADR-0010: External AI Guardian Review As A First-Class Approval Authority\n \n ## Status\n \n@@ -8,7 +8,7 @@\n \n Shiki is intended to be an autonomous, recursive, self-improving agent\n framework: authority-in-the-loop, not human-in-the-loop. Until now the Guardian\n-approval required for high/critical-risk merges (guardian-policy.json, ADR\n+approval required for high/critical-risk merges (guardian-policy.json, SADR\n governance) recognized only human authority sources \u2014 a GitHub review approval,\n the `guardian:approved` label applied by a configured Guardian, or a comment\n carrying `Guardian approval granted` plus the current head SHA.\n@@ -89,7 +89,7 @@\n   autonomous design the operator intends and produced the impersonation\n   incident.\n - **Record AI approval as an operator comment**: rejected as forgery of human\n-  authority \u2014 the exact failure this ADR corrects.\n+  authority \u2014 the exact failure this SADR corrects.\n - **Deliver the artifact as a committed `.shiki/` file**: rejected; a committed\n   approval inside the PR being approved is forgeable by anyone who can push.\n - **A dedicated reviewer bot identity instead of a relayed comment**: deferred;\ndiff --git a/docs/adr/0011-autonomous-quality-gate-evidence.md b/docs/adr/SADR-0011-autonomous-quality-gate-evidence.md\nindex bca0246c5d46e44afbde15486a75eec425fc02f5..9c93eac16d8c3a6d4b11bd7224e3955102cd7db4\n--- a/docs/adr/0011-autonomous-quality-gate-evidence.md\n+++ b/docs/adr/SADR-0011-autonomous-quality-gate-evidence.md\n@@ -1,4 +1,4 @@\n-# ADR 0011: Autonomous Quality-Gate Evidence \u2014 Deterministic Facts And An Independent Verifier\n+# SADR-0011: Autonomous Quality-Gate Evidence \u2014 Deterministic Facts And An Independent Verifier\n \n ## Status\n \ndiff --git a/docs/adr/0012-autonomous-loop-closeout-to-main.md b/docs/adr/SADR-0012-autonomous-loop-closeout-to-main.md\nindex 637fad6dde4989c9c2bf0f81f676d8d29991123c..ec7d85f81030a676bcbba28b43eb365422a36eac\n--- a/docs/adr/0012-autonomous-loop-closeout-to-main.md\n+++ b/docs/adr/SADR-0012-autonomous-loop-closeout-to-main.md\n@@ -1,4 +1,4 @@\n-# 0012 \u2014 Autonomous loop pushes goal completion to main via a closeout PR\n+# SADR-0012 \u2014 Autonomous loop pushes goal completion to main via a closeout PR\n \n - Status: accepted\n - Date: 2026-06-18\ndiff --git a/docs/adr/0013-bootstrap-grants-actions-approve-pull-requests.md b/docs/adr/SADR-0013-bootstrap-grants-actions-approve-pull-requests.md\nindex b6c69d64f1c5ced346b88d6859586715b3b5bd0e..cf48ff4a6716267f539ca58edd1a852e7c7a2d64\n--- a/docs/adr/0013-bootstrap-grants-actions-approve-pull-requests.md\n+++ b/docs/adr/SADR-0013-bootstrap-grants-actions-approve-pull-requests.md\n@@ -1,4 +1,4 @@\n-# ADR 0013: Bootstrap Grants GitHub Actions Permission To Create And Approve Pull Requests\n+# SADR-0013: Bootstrap Grants GitHub Actions Permission To Create And Approve Pull Requests\n \n ## Status\n \n@@ -31,7 +31,7 @@\n security tradeoff: it lets repository automation satisfy a human-review gate. In\n a multi-maintainer repo that could let a compromised or buggy workflow\n self-approve changes. So whether bootstrap turns it on by default \u2014 and with\n-what default token scope \u2014 is an ADR-worthy decision, not an implementation\n+what default token scope \u2014 is a SADR-worthy decision, not an implementation\n detail.\n \n ## Decision\n@@ -48,7 +48,7 @@\n Specifically:\n \n - **`can_approve_pull_request_reviews` defaults to `true`.** Shiki's default\n-  posture is solo/AFK autonomous operation (ADR 0008/0009/0012), where the CCA\n+  posture is solo/AFK autonomous operation (SADR-0008/0009/0012), where the CCA\n   Review Bridge is the only path that can satisfy `required_review: true` after\n   the CCA and Guardian gates have already passed. The default makes a\n   freshly-bootstrapped repo able to run the autonomous loop end to end without a\ndiff --git a/docs/adr/0014-codex-app-external-ai-guardian-ui-adapter.md b/docs/adr/SADR-0014-codex-app-external-ai-guardian-ui-adapter.md\nindex 238db06279334caa341c117362df9271382a7fcd..65f12e9939321e1b3e389a34c822fc1549e5155d\n--- a/docs/adr/0014-codex-app-external-ai-guardian-ui-adapter.md\n+++ b/docs/adr/SADR-0014-codex-app-external-ai-guardian-ui-adapter.md\n@@ -1,4 +1,4 @@\n-# ADR 0014: Use Codex App As The External AI Guardian UI Adapter\n+# SADR-0014: Use Codex App As The External AI Guardian UI Adapter\n \n ## Status\n \ndiff --git a/docs/adr/0015-contract-approval-before-dispatch.md b/docs/adr/SADR-0015-contract-approval-before-dispatch.md\nindex fad6c656741a20a7684ba16080a7db159f63a52f..6061271a46d3f1d6ae7174477de3fb75673123a6\n--- a/docs/adr/0015-contract-approval-before-dispatch.md\n+++ b/docs/adr/SADR-0015-contract-approval-before-dispatch.md\n@@ -1,4 +1,4 @@\n-# ADR 0015: Approve The Task Contract Before Dispatch, Not The Diff After It\n+# SADR-0015: Approve The Task Contract Before Dispatch, Not The Diff After It\n \n ## Status\n \n@@ -9,7 +9,7 @@\n Guardian approval for high/critical risk is expressible only on a pull request.\n Every approval source in `guardian-policy.json` \u2014 GitHub review, the\n `guardian:approved` label, the `Guardian approval granted` comment, and the\n-`external_ai_guardian_review` artifact (ADR 0010/0014) \u2014 is bound to a PR, and\n+`external_ai_guardian_review` artifact (SADR-0010/0014) \u2014 is bound to a PR, and\n three of the four require the current head SHA. Approval therefore cannot exist\n before an implementation PR exists.\n \n@@ -81,7 +81,7 @@\n An envelope breach must surface only on the non-repairable policy gate, never on\n the repairable `MergeGate metadata check`. Otherwise the Goal Loop routes it to\n `dispatch_repair` \u2014 instructing a runner to make the breach go away \u2014 which is\n-exactly what this ADR forbids.\n+exactly what this SADR forbids.\n \n **Contract immutability.** After registration, an implementation PR must not\n change the governance fields of its own task contract (`scope`, `non_goals`,\n@@ -116,7 +116,7 @@\n fails any verdict that is not `complete`, and the signal the CCA prompt reads is\n computed from `evaluate_guardian_approval` alone. Without the OR-merge at the\n signal, a contract-approved PR still returns `needs_guardian`, the CCA job fails,\n-and the MergeGate policy check never runs \u2014 the gate this ADR changes would never\n+and the MergeGate policy check never runs \u2014 the gate this SADR changes would never\n be reached. The CCA *prompt* needs no change: a `contract_approval` entry in the\n signal's existing `sources` array satisfies its current wording.\n \n@@ -130,7 +130,7 @@\n written, and an approved contract lets its implementation PRs merge through the\n same automatic path as low/medium risk. The wasted implement-then-block cycle\n disappears. Guardian review shifts from reading diffs to reading contracts, which\n-is both earlier and smaller; the external AI Guardian path (ADR 0010/0014) works\n+is both earlier and smaller; the external AI Guardian path (SADR-0010/0014) works\n on contracts with no change.\n \n We accept that the Guardian authorizes a class of change without seeing its\n@@ -146,7 +146,7 @@\n adversarial tests for the new approval path equivalent in rigor to\n `test_shiki_governance_evidence.sh`.\n \n-Bootstrap costs, which apply only while this ADR is being implemented. The tasks\n+Bootstrap costs, which apply only while this SADR is being implemented. The tasks\n that build Contract Approval merge under the existing rules, so the ones\n dispatched after the pre-dispatch gate lands but before the approval path lands\n need a Guardian-approved Contract PR *and* a live Guardian approval on their\n@@ -210,7 +210,7 @@\n risk; they do not bound authorized work.\n \n **Auto-escalate risk on envelope breach instead of stopping.** Returns the\n-workflow to a diff-time Guardian decision \u2014 the exact behavior this ADR removes.\n+workflow to a diff-time Guardian decision \u2014 the exact behavior this SADR removes.\n \n **Resolve governance fields from the base snapshot only.** Rejected against the\n registration flow: implementation PRs add their own contract, so the base has no\ndiff --git a/docs/adr/0016-locks-bound-product-paths.md b/docs/adr/SADR-0016-locks-bound-product-paths.md\nindex b5e850f3f1f7f4ef6e7f9d1763a29a51bd300c45..46b40a9b99a7aae644c02bb458f5473ff0a90ce2\n--- a/docs/adr/0016-locks-bound-product-paths.md\n+++ b/docs/adr/SADR-0016-locks-bound-product-paths.md\n@@ -1,4 +1,4 @@\n-# ADR 0016: Locks bound product paths; the Shiki mirror is bound by record identity\n+# SADR-0016: Locks bound product paths; the Shiki mirror is bound by record identity\n \n ## Status\n \n@@ -6,8 +6,8 @@\n \n - Date: 2026-07-28\n - Deciders: operator (mizutani-140), Claude (planner)\n-- Related: ADR 0004 (GitHub-first with a `.shiki` mirror), ADR 0009 (Spec Freeze),\n-  ADR 0012 (autonomous loop closeout to main), ADR 0015 (Contract Approval before\n+- Related: SADR-0004 (GitHub-first with a `.shiki` mirror), SADR-0009 (Spec Freeze),\n+  SADR-0012 (autonomous loop closeout to main), SADR-0015 (Contract Approval before\n   dispatch), remediation goal task R-02, the live PR #179 deadlock.\n \n ## Context\n@@ -90,11 +90,11 @@\n files, documentation files and named source modules are nameable; runtime-\n allocated ledger, runner and report ids are not \u2014 and those live in `.shiki`,\n which locks no longer govern. Existing frozen contracts are brought into line by\n-a one-time Spec Amendment per plan, which must land before ADR 0015's contract\n+a one-time Spec Amendment per plan, which must land before SADR-0015's contract\n immutability makes `locks` unwritable from the implementation PR.\n \n **Lock state is resolved from the durable side.** MergeGate resolves the lock\n-registry, and ADR 0015's governance fields, from the base `.shiki` snapshot when\n+registry, and SADR-0015's governance fields, from the base `.shiki` snapshot when\n present, falling back to head only when absent. The `MergeGate metadata check`\n job is given `--base-shiki`, which it does not receive today. Locks are not\n derived from the live open-pull-request set: a pull request body is untrusted\n@@ -108,7 +108,7 @@\n Explicitly out of scope: moving MergeGate's contract resolution off the PR\n checkout wholesale; making the mirror a projection of GitHub state; introducing\n new lock kinds; moving ledger evidence out of the repository; and unwinding\n-ADR 0012's closeout pull request. Registration of multi-task goals is ADR 0015's\n+SADR-0012's closeout pull request. Registration of multi-task goals is SADR-0015's\n Contract PR and is not re-decided here.\n \n ## Consequences\n@@ -127,7 +127,7 @@\n \n Risks accepted. First, goal-scoped identity is only as strong as `goal_id`,\n which is resolved from the PR body and the head task file; the mitigation is to\n-add `goal_id` to ADR 0015's frozen governance field set, and until that lands a\n+add `goal_id` to SADR-0015's frozen governance field set, and until that lands a\n task file can name a foreign goal. Second, base-when-present resolution is\n unavailable on fork pull requests, where the head is contributor-controlled;\n fork pull requests already cannot complete a Shiki merge, and this decision does\n@@ -139,7 +139,7 @@\n path, not by implementation pull requests.\n \n New obligations: one Spec Amendment per frozen plan to rewrite `locks`, ordered\n-before ADR 0015 T1; extension of the adversarial suites in\n+before SADR-0015 T1; extension of the adversarial suites in\n `scripts/test_shiki_adversarial_state.sh` and\n `scripts/test_shiki_governance_evidence.sh` to cover the eleven newly-scoped\n directories; and updating `scripts/test_shiki_loop_lock_guard.sh`,\n@@ -151,32 +151,32 @@\n whether the loop's transitive sync of ledger `evidence` references can be\n deleted once the Contract PR carries its own source plan.\n \n-## Relationship to ADR 0004\n+## Relationship to SADR-0004\n \n-This ADR **clarifies something ADR 0004 left unspecified**. ADR 0004 decides that\n+This SADR **clarifies something SADR-0004 left unspecified**. SADR-0004 decides that\n GitHub is the operational source of truth and that each target repository keeps a\n `.shiki/` mirror. It says nothing about pull requests writing the mirror, does\n not name MergeGate, and does not make the mirror an input to a gate. Nothing in\n-ADR 0004's Decision or Consequences is amended, superseded or weakened: the\n+SADR-0004's Decision or Consequences is amended, superseded or weakened: the\n mirror still exists, still records the same artifacts, and pull request\n-boundaries remain the coordination surface. What this ADR settles is the question\n+boundaries remain the coordination surface. What this SADR settles is the question\n 0004 never reached \u2014 by what authority a pull request may write a mirror record \u2014\n and it answers it the way `.shiki/manifest.json` already declares\n (`pr_mutation: current-task-or-current-goal-only`).\n \n-It **amends ADR 0012**, whose Decision item 5 states that \"Loop-executed tasks\n+It **amends SADR-0012**, whose Decision item 5 states that \"Loop-executed tasks\n declare the `path:.shiki/**` lock, which covers every `.shiki` file the closeout\n stages ... so `files_outside_locks` is satisfied without per-file lock\n bookkeeping.\" That clause is replaced: the closeout's mirror writes are\n-authorized by record identity, not by a declared lock. ADR 0012's substantive\n+authorized by record identity, not by a declared lock. SADR-0012's substantive\n decision \u2014 that completion reaches `main` through a closeout pull request rather\n than local attestation \u2014 is preserved unchanged.\n \n-It is **complementary to ADR 0015**, not a substitute. ADR 0015 owns contract\n+It is **complementary to SADR-0015**, not a substitute. SADR-0015 owns contract\n immutability, the pre-dispatch gate, the Contract PR and the Scope Envelope. This\n-ADR owns what a lock means and who may write the mirror. The one ordering\n+SADR owns what a lock means and who may write the mirror. The one ordering\n constraint between them is that the lock-narrowing Spec Amendment must precede\n-ADR 0015 T1's freezing of the `locks` field.\n+SADR-0015 T1's freezing of the `locks` field.\n \n ## Alternatives Considered\n \n@@ -186,7 +186,7 @@\n the only writer that can register them may run. Migrating the Guardian signal to\n that store makes every registration PR fail closed to `needs_guardian` at every\n risk level, because the task file is by definition absent from the store, which\n-removes the low/medium autonomous path ADR 0008/0009 exists to provide. And it\n+removes the low/medium autonomous path SADR-0008/0009 exists to provide. And it\n leaves schemas, manifest, config, guardian policy, templates, migrations, plans,\n worktrees, runner records, runs and handoffs with no writer at all in a\n repository whose product is `.shiki`. Measured benefit on the frozen backlog:\n@@ -199,14 +199,14 @@\n namespace is not in fact ownership-partitioned \u2014 56 ledger entries carry no\n `task_id`, and one goal DAG is writable by 35 registered tasks. The correct\n expression of its insight is that locks do not govern the mirror at all, which is\n-what this ADR decides.\n+what this SADR decides.\n \n **Make the mirror a projection written only by a registration writer and a\n post-merge projection job.** Rejected. It requires a bypass actor with write\n access to the protected default branch; it makes its own master invariant\n readable from a manifest that pull requests may edit; it deletes both reconcile\n modes and leaves a corrupted mirror with no in-band repair path, contradicting\n-ADR 0004's consequence that mirror conflicts are repaired against GitHub state;\n+SADR-0004's consequence that mirror conflicts are repaired against GitHub state;\n and its content-derived ids are rejected by `validate_shiki`'s currently-green id\n pattern.\n \ndiff --git a/docs/adr/0017-bookkeeping-closeouts-do-not-inherit-risk.md b/docs/adr/SADR-0017-bookkeeping-closeouts-do-not-inherit-risk.md\nindex 554e3a0d526a72ca067108c5464cc68c3edcf174..b9676857a4713850e063af7c39c8e0b01ecb4c66\n--- a/docs/adr/0017-bookkeeping-closeouts-do-not-inherit-risk.md\n+++ b/docs/adr/SADR-0017-bookkeeping-closeouts-do-not-inherit-risk.md\n@@ -1,4 +1,4 @@\n-# ADR 0017: A Bookkeeping Closeout Does Not Inherit Its Task's Risk\n+# SADR-0017: A Bookkeeping Closeout Does Not Inherit Its Task's Risk\n \n ## Status\n \n@@ -6,7 +6,7 @@\n \n ## Context\n \n-ADR 0012 made the goal loop push a task's completion to the default branch through a\n+SADR-0012 made the goal loop push a task's completion to the default branch through a\n closeout pull request: after the implementation PR merges, the loop opens a second PR\n carrying `task.status = done`, `lock.state = released`, and \u2014 when the task completes\n its goal \u2014 `goal.status = complete` plus one scorecard report. Nothing else. The\n@@ -49,14 +49,14 @@\n 4. The task file's governance fields (`scope`, `non_goals`, `required_skills`,\n    `risk_level`, `locks`, `acceptance_checks`, `test_command`) are byte-identical to\n    the base snapshot. Only `status`, `expected_pr`, `closeout_pr`, `ledger_evidence`\n-   and `expected_branch` may differ \u2014 the ADR 0015 contract-immutability rule,\n+   and `expected_branch` may differ \u2014 the SADR-0015 contract-immutability rule,\n    unchanged.\n \n    `expected_branch` is in that set because a closeout is *by construction* on a\n    different branch from the implementation it closes, and the loop rewrites the\n    field accordingly. It is safe to let it differ because MergeGate independently\n    requires `expected_branch` to equal the PR's actual head ref, so a PR cannot set\n-   it to anything other than the branch it is really on. The first draft of this ADR\n+   it to anything other than the branch it is really on. The first draft of this SADR\n    omitted it, which made the exemption unreachable: every real closeout failed\n    condition 4 on a field the loop always changes. See PR #209.\n 5. The state transitions are exactly the terminal set: task `review` \u2192 `done`, lock\n@@ -113,12 +113,12 @@\n classification must be derived, not asserted.\n \n **Fold the closeout into the implementation PR** so no second PR exists. Rejected under\n-ADR 0012, which recorded why: a task file that is already `done` on the implementation\n+SADR-0012, which recorded why: a task file that is already `done` on the implementation\n branch short-circuits the loop's review \u2192 CCA \u2192 merge \u2192 done ordering into a local\n goal-completion before any merge happens.\n \n **Have the loop auto-merge closeouts with elevated credentials.** Rejected outright: that\n-is a runtime authoring its own approval, which ADR 0010 and the constitution's\n+is a runtime authoring its own approval, which SADR-0010 and the constitution's\n approval-record prohibition forbid. It would also be indistinguishable, in the evidence,\n from the forgery those rules exist to prevent.\n \ndiff --git a/docs/adr/0018-carried-guardian-approval-across-a-proven-base-sync.md b/docs/adr/SADR-0018-carried-guardian-approval-across-a-proven-base-sync.md\nindex 22358562da390ed8b45cd600da08a43d378b4680..e6e2907cefa776460af73ddf7d39db605f3c39b0\n--- a/docs/adr/0018-carried-guardian-approval-across-a-proven-base-sync.md\n+++ b/docs/adr/SADR-0018-carried-guardian-approval-across-a-proven-base-sync.md\n@@ -1,4 +1,4 @@\n-# ADR 0018: Carried Guardian Approval Across A Proven Base Sync\n+# SADR-0018: Carried Guardian Approval Across A Proven Base Sync\n \n ## Status\n \n@@ -34,7 +34,7 @@\n Guardian evaluator that consumes it (`shiki_guardian.evaluate_guardian_approval`,\n the `guardian_comment_carried` source) were built and tested in isolation. Neither\n does anything until a caller passes the flags that turn the carry on, so the\n-feature was invisible in CI. This ADR records the decision the two of them\n+feature was invisible in CI. This SADR records the decision the two of them\n implement, and this task wires it into the four CI invocations that judge Guardian\n approval.\n \ndiff --git a/docs/adr/README.md b/docs/adr/README.md\nindex c70e057a4520c36e042ab68c53cd4a21ef709b71..5da43856836e2ce6a150f8127766e2f88355ab4b\n--- a/docs/adr/README.md\n+++ b/docs/adr/README.md\n@@ -1,40 +1,41 @@\n-# Architecture Decision Records\n+# Decision Record Namespaces\n \n-This directory holds Shiki's Architecture Decision Records (ADRs): durable\n-records of hard-to-reverse platform decisions. ADRs are part of the Shiki source\n-of truth alongside GitHub state, the `.shiki/` mirror, and `CONTEXT.md`.\n+This directory holds two independent decision namespaces:\n \n-## How ADRs Work\n+- Shiki Architecture Decision Records (SADRs), named\n+  `SADR-NNNN-<short-slug>.md`, record hard-to-reverse Shiki platform decisions.\n+- Target Architecture Decision Records (ADRs), named\n+  `NNNN-<short-slug>.md`, belong to the target product.\n \n-- New ADRs are added as `docs/adr/NNNN-<short-slug>.md`, where `NNNN` is the next\n-  zero-padded sequence number.\n-- The **canonical list of ADRs is the set of files matching `docs/adr/*.md`** in\n-  this directory. Browse `docs/adr/*.md` for the authoritative, current set.\n-- Use [`template.md`](template.md) as the starting point for a new ADR.\n-- Record an ADR when a decision is hard to reverse, surprising without context,\n-  and a real tradeoff. Do not write an ADR for routine or easily reversible\n-  choices.\n+The same four-digit number may appear once in each namespace. A platform\n+`SADR-NNNN` citation resolves only to `SADR-NNNN-*.md`; a target `ADR NNNN`\n+citation resolves only to `NNNN-*.md`.\n \n-Using the `docs/adr/*.md` glob as the source of truth means other PRs can add an\n-ADR without editing this index file, avoiding merge contention on a\n-hand-maintained enumeration.\n+## How Decision Records Work\n \n-## Current ADRs (snapshot)\n-\n-The list below is a convenience snapshot and may lag behind the directory. When\n-in doubt, list `docs/adr/*.md`.\n+- New Shiki platform decisions use the next zero-padded SADR number.\n+- New target product decisions use the target's own ADR sequence.\n+- The canonical list is the files in `docs/adr/`; no hand-maintained index is\n+  authoritative.\n+- Use [`template.md`](template.md) as the starting point and choose the filename\n+  namespace that owns the decision.\n+- Record a decision when it is hard to reverse, surprising without context, and\n+  a real tradeoff. Do not write a decision record for routine or easily\n+  reversible choices.\n \n-- [0001 \u2014 Keep Shiki As The Platform Name](0001-keep-shiki-as-platform-name.md)\n-- [0002 \u2014 Use AGENTS As The Shared Agent Constitution](0002-use-agents-as-shared-constitution.md)\n-- [0003 \u2014 Require Skill Gate For MergeGate Readiness](0003-require-skill-gate-for-mergegate-readiness.md)\n-- [0004 \u2014 Use GitHub First With A Repository-Local Shiki Mirror](0004-use-github-first-with-shiki-mirror.md)\n-- [0005 \u2014 Use Codex As Front And Claude Code Action As The GitHub Runtime](0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md)\n-- [0006 \u2014 Separate Codex Implementation, CCA Completion, And MergeGate Authorization](0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md)\n+Using the directory as the source of truth lets independent changes add records\n+without contending on an enumerated index.\n \n-## Adding A New ADR\n+## Adding A Shiki SADR\n \n-1. Copy `template.md` to `docs/adr/NNNN-<short-slug>.md`.\n+1. Copy `template.md` to `docs/adr/SADR-NNNN-<short-slug>.md`.\n 2. Fill in the title, status, context, decision, and consequences.\n-3. Set the status to `Proposed`, then `Accepted` once the decision is approved.\n-4. Open a PR following `CONTRIBUTING.md`. You do not need to edit this index for\n-   the ADR to be discoverable via `docs/adr/*.md`.\n+3. Set the status to `Proposed`, then `Accepted` once the proper authority has\n+   approved the decision.\n+4. Open a PR following `CONTRIBUTING.md`.\n+\n+## Adding A Target ADR\n+\n+1. Copy `template.md` to `docs/adr/NNNN-<short-slug>.md` using the target's own\n+   sequence.\n+2. Cite it as `ADR NNNN`; do not prefix its filename with `SADR-`.\ndiff --git a/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md b/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\nnew file mode 100644\nindex 0000000000000000000000000000000000000000..7455ce1f3bed02ddddb851ac21a1d1a648c9aeba\n--- /dev/null\n+++ b/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\n@@ -0,0 +1,55 @@\n+# SADR-0019: Separate Shiki Platform SADRs From Target ADRs\n+\n+## Status\n+\n+Accepted\n+\n+## Context\n+\n+Shiki installs its platform decision records into `docs/adr/` in every target\n+repository. Shiki and its targets previously used the same numeric\n+`NNNN-<slug>.md` namespace. A target that already owned ADRs 0013 through 0018\n+therefore acquired two records for each number during installation, making an\n+`ADR NNNN` citation ambiguous. Renumbering target decisions would rewrite target\n+history, while renumbering Shiki decisions per target would destroy their stable\n+platform identity.\n+\n+Upgrades also need to remove the former Shiki-owned numeric paths without ever\n+mistaking a target-owned ADR for platform content. The install stamp already\n+records exact shipped paths and content digests, so it can prove ownership only\n+when both still match.\n+\n+## Decision\n+\n+Shiki platform decisions use the `SADR` namespace:\n+\n+- records are named `docs/adr/SADR-NNNN-<slug>.md`;\n+- live platform citations use `SADR-NNNN`;\n+- target product decisions retain `docs/adr/NNNN-<slug>.md` and `ADR NNNN`;\n+- the reference validator resolves the two namespaces independently, so the same\n+  four-digit number may exist once in each namespace.\n+\n+Before a forced upgrade writes anything, the installer inventories the exact\n+former numeric Shiki ADR paths. It may delete a legacy path only when the\n+existing install stamp contains that exact path and its recorded SHA-256 digest\n+matches the file currently on disk. All ownership blockers are reported\n+together and abort the upgrade before its first write. A non-forced install\n+never performs this cleanup.\n+\n+Historical `.shiki/` evidence remains unchanged because it records the namespace\n+that existed when that evidence was produced.\n+\n+## Consequences\n+\n+- Targets may use any ADR number without colliding with Shiki's platform SADRs.\n+- Fresh installs preserve target ADR bytes and install a stable SADR set.\n+- Forced upgrades fail closed when legacy ownership cannot be proven, and a\n+  successful upgrade refreshes the stamp without removed legacy paths.\n+- Current governance, packaging, workflow, prompt, script, and skill references\n+  must move with the platform records.\n+\n+## Alternatives Considered\n+\n+- Collision-only refusal leaves affected targets permanently unupgradable.\n+- Renumbering target ADRs mutates target-owned history.\n+- Per-target Shiki renumbering makes platform citations non-portable.\ndiff --git a/docs/adr/template.md b/docs/adr/template.md\nindex a92a97daddd0b00ab9dd626cc8d86942da2eafed..17dd79175b5fee2105935557f691b40cf9492942\n--- a/docs/adr/template.md\n+++ b/docs/adr/template.md\n@@ -1,14 +1,14 @@\n-# NNNN: <Short Decision Title>\n+# <SADR-NNNN or NNNN>: <Short Decision Title>\n \n ## Status\n \n-Proposed | Accepted | Superseded by ADR-NNNN\n+Proposed | Accepted | Superseded by SADR-NNNN or target ADR NNNN\n \n ## Context\n \n What is the situation, constraint, or force that requires a decision? Reference\n-relevant domain terms (`CONTEXT.md`), prior ADRs, and code reality. State the\n-problem, not the solution.\n+relevant domain terms (`CONTEXT.md`), prior Shiki SADRs or target ADRs, and code\n+reality. State the problem, not the solution.\n \n ## Decision\n \ndiff --git a/docs/agents/autonomous-loop-e2e-acceptance.md b/docs/agents/autonomous-loop-e2e-acceptance.md\nindex bbf84c6081987f159ac6ad9ecf3c82b7aa72e7e8..5c13c1d02a05a90b0dc46cbef7c95a9846d0c237\n--- a/docs/agents/autonomous-loop-e2e-acceptance.md\n+++ b/docs/agents/autonomous-loop-e2e-acceptance.md\n@@ -40,8 +40,8 @@\n 3. `gh auth status` is logged in with merge permission on the repo.\n 4. Branch protection + required checks are configured exactly as the goal-loop\n    required-check set expects (see `docs/agents/branch-protection-smoke.md`).\n-5. The repo `.shiki/policy` permits autonomous low/medium-risk merges (ADR\n-   0008/0009); the MergeGate policy check is the recorded-authority gate.\n+5. The repo `.shiki/policy` permits autonomous low/medium-risk merges\n+   (SADR-0008/0009); the MergeGate policy check is the recorded-authority gate.\n \n The operator's only actions are: register the task and start the loop. **No\n manual pre-clearing** means after `shiki loop run` starts, the operator does not\ndiff --git a/docs/agents/bootstrap-command.md b/docs/agents/bootstrap-command.md\nindex 05f7f203a52cf412b9f97084a6e131ed0f785afd..f7aa0b734dfebee4a3b139335b66ee3415f48713\n--- a/docs/agents/bootstrap-command.md\n+++ b/docs/agents/bootstrap-command.md\n@@ -189,7 +189,9 @@\n ```\n \n Use this only for tests, fixtures, or explicit local-only template inspection.\n-Use `--force` only when you intentionally want to overwrite existing target files.\n+Use `--force` only when you intentionally want to refresh the target template.\n+Legacy numeric Shiki decision cleanup is stamp-and-digest authorized before any\n+write; target ADR paths are not cleanup candidates.\n \n ## Slash Command\n \ndiff --git a/docs/agents/checklists.md b/docs/agents/checklists.md\nindex 4a7f03ade346f53a1c1d82981f3fd744f9ab4547..0c5ec6c53dc7197b7b7a7d18e667b325babd6c45\n--- a/docs/agents/checklists.md\n+++ b/docs/agents/checklists.md\n@@ -32,18 +32,18 @@\n | ID | Item | Blocking |\n | --- | --- | --- |\n | GD-01 | Plan was challenged against `CONTEXT.md`. | blocking for non-trivial Goals |\n-| GD-02 | Relevant ADRs were read or absence was recorded. | blocking for non-trivial Goals |\n+| GD-02 | Relevant SADRs and target ADRs were read or absence was recorded. | blocking for non-trivial Goals |\n | GD-03 | Ambiguous terms were resolved or converted to HITL blockers. | blocking |\n | GD-04 | Edge scenarios were considered. | advisory |\n | GD-05 | Code reality was checked when code could answer. | blocking when applicable |\n | GD-06 | New domain terms were added to `CONTEXT.md` when needed. | blocking when applicable |\n-| GD-07 | ADR was created or explicitly skipped using the ADR criteria. | blocking when applicable |\n+| GD-07 | A SADR or target ADR was created or explicitly skipped using the decision-record criteria. | blocking when applicable |\n \n ## CI \u2014 Context & Impact Checklist\n \n | ID | Item | Blocking |\n | --- | --- | --- |\n-| CI-01 | Relevant docs and ADRs identified. | blocking |\n+| CI-01 | Relevant docs, SADRs, and target ADRs identified. | blocking |\n | CI-02 | Relevant modules, interfaces, seams, callers, and tests identified. | blocking |\n | CI-03 | Dependencies and blocked-by relationships identified. | blocking |\n | CI-04 | Candidate locks identified. | blocking |\n@@ -62,7 +62,7 @@\n | PRD-04 | Implementation decisions are recorded without volatile code detail. | blocking |\n | PRD-05 | Testing decisions are recorded. | blocking |\n | PRD-06 | Out-of-scope items are explicit. | blocking |\n-| PRD-07 | Relevant glossary/ADR links are present when applicable. | blocking |\n+| PRD-07 | Relevant glossary/SADR/target-ADR links are present when applicable. | blocking |\n \n ## SF \u2014 Spec Freeze Checklist\n \n@@ -98,7 +98,7 @@\n | H-02 | Branch/worktree target is present. | blocking |\n | H-03 | Scope and non-goals are present. | blocking |\n | H-04 | Dependencies and locks are present. | blocking |\n-| H-05 | Relevant docs, ADRs, modules, and tests are referenced. | blocking |\n+| H-05 | Relevant docs, SADRs, target ADRs, modules, and tests are referenced. | blocking |\n | H-06 | Required skills are stated. | blocking |\n | H-07 | TDD expectations are stated. | blocking when behavior work |\n | H-08 | Acceptance criteria are included. | blocking |\ndiff --git a/docs/agents/codex-handoff.md b/docs/agents/codex-handoff.md\nindex ede468f47cb277f69a857e8c45df703e4ed0151d..6f97bdffb67e44d7edea2c71e01ea28933c4eb6c\n--- a/docs/agents/codex-handoff.md\n+++ b/docs/agents/codex-handoff.md\n@@ -46,7 +46,7 @@\n \n ## Context\n - Domain terms:\n-- Relevant ADRs:\n+- Relevant SADRs and target ADRs:\n - Relevant docs:\n - Relevant modules/interfaces/seams:\n - Similar tests/prior art:\ndiff --git a/docs/agents/completion-check-agent.md b/docs/agents/completion-check-agent.md\nindex 13e4c035db35025f4e3cdcc5adfb24314ac4d521..61bfa1218cf36ca605250c91919e620f33238aa0\n--- a/docs/agents/completion-check-agent.md\n+++ b/docs/agents/completion-check-agent.md\n@@ -32,7 +32,7 @@\n - `AGENTS.md`\n - `CLAUDE.md`\n - `CONTEXT.md`\n-- relevant ADRs\n+- relevant SADRs and target ADRs\n - parent Goal issue\n - PRD issue or document\n - task issue\ndiff --git a/docs/agents/decision-control.md b/docs/agents/decision-control.md\nindex 7d8ee5ab03a5ccbcbd04056ae61de03785a2a9c9..8c94ecda00fffaa0e92c325ab83aa9776bd1dac1\n--- a/docs/agents/decision-control.md\n+++ b/docs/agents/decision-control.md\n@@ -11,7 +11,7 @@\n | Decision | Owner | When | Required inputs | Output | Enforcement surface |\n | --- | --- | --- | --- | --- | --- |\n | Is the Goal clear enough to plan? | Planner + Guardian when needed | Goal intake | User request, existing docs, risk signals | Goal readiness verdict | Goal checklist, issue template, ledger |\n-| Must `grill-with-docs` run? | Planner / Skill Gate | Before PRD or issue split | Ambiguity, domain terms, ADR conflicts, unclear boundaries | Skill invocation or explicit skip rationale | Skill Gate checklist |\n+| Must `grill-with-docs` run? | Planner / Skill Gate | Before PRD or issue split | Ambiguity, domain terms, SADR/target-ADR conflicts, unclear boundaries | Skill invocation or explicit skip rationale | Skill Gate checklist |\n | Is a PRD required? | Planner | After Goal context stabilizes | Goal, `grill-with-docs` output, Context & Impact output | PRD required / not required | PRD checklist, CCA evidence check |\n | Can the spec be frozen? | Operator only | End of Requirements Definition, before issue split | PRD/requirements summary, scope inventory (SF-02), open-question list | `spec_freeze` block in the plan + ledger evidence | `require_grilled_plan` (plan ingest/run), SF checklist |\n | Can a frozen spec change? | Operator only (Spec Amendment) | When implementation contradicts the frozen spec | Contested decisions, scoped re-grill result | recorded amendment + re-stamped freeze | SF checklist, ledger evidence |\ndiff --git a/docs/agents/domain.md b/docs/agents/domain.md\nindex 258da8ea81e87ac0a6b0b5b5e5c2fde980eaabb7..d57b0cf17ff3e1fa9e6e5885372bf083d8295421\n--- a/docs/agents/domain.md\n+++ b/docs/agents/domain.md\n@@ -5,7 +5,9 @@\n ## Layout\n \n - `CONTEXT.md` defines Shiki language. Keep it implementation-free.\n-- `docs/adr/` records hard-to-reverse decisions.\n+- `docs/adr/SADR-NNNN-*.md` records Shiki platform decisions.\n+- `docs/adr/NNNN-*.md` records target product decisions without sharing the\n+  SADR namespace.\n - `AGENTS.md` is the runtime-neutral agent constitution.\n - `CLAUDE.md` is the Claude Code wrapper around `AGENTS.md`.\n - `.shiki/` is the Target Repository mirror for Goals, PRDs, plans, Task DAGs, contracts, locks, worktree records, ledger entries, CCA verdicts, repair packets, reports, and handoffs.\n@@ -15,6 +17,6 @@\n \n - Use Shiki terms exactly when they exist.\n - Add glossary terms to `CONTEXT.md` only when the term is part of the domain, not a generic programming concept.\n-- Create ADRs only for hard-to-reverse, surprising, tradeoff-based decisions.\n+- Create SADRs or target ADRs only for hard-to-reverse, surprising, tradeoff-based decisions.\n - Keep product-specific language in the Target Repository, not in Shiki Template docs.\n-- If an ADR conflicts with a proposed implementation, surface the conflict before editing.\n+- If a SADR or target ADR conflicts with a proposed implementation, surface the conflict before editing.\ndiff --git a/docs/agents/execution-control.md b/docs/agents/execution-control.md\nindex ad8ee3cdb36be56fedc1f46408ca92b387d74047..bc63c3a73661d12b567016ba299f1a6b66bf9088\n--- a/docs/agents/execution-control.md\n+++ b/docs/agents/execution-control.md\n@@ -63,7 +63,7 @@\n | Branch name | Orchestrator | Orchestrator | Before implementer dispatch | branch policy | branch ref in task/ledger |\n | Lock reservation | Context & Impact | MergeGate | Before dispatch and before merge | `.shiki/locks/`, labels, PR metadata | lock id, owner, scope |\n | CI profile | Planner | MergeGate | During issue triage, rechecked at PR | labels, `.shiki/policy.yaml`, workflow matrix | required check list |\n-| Required status checks | Repo maintainer / Guardian | GitHub branch protection / rulesets | Repository setup, updated via ADR | GitHub settings | branch/ruleset config, ADR |\n+| Required status checks | Repo maintainer / Guardian | GitHub branch protection / rulesets | Repository setup, updated via target ADR | GitHub settings | branch/ruleset config, target ADR |\n | CCA judgment | CCA | GitHub required check / MergeGate | Every non-draft PR update | GitHub Action check | structured CCA verdict |\n | Repair assignment | CCA | MergeGate | After failed CCA/CI/review | repair packet, issue/PR comment | repair id and attempt count |\n | Merge readiness | MergeGate | GitHub branch protection / Guardian if needed | After CCA complete | required checks, review rules, rulesets | mergegate check output |\ndiff --git a/docs/agents/guardian-policy.md b/docs/agents/guardian-policy.md\nindex 4147c7adf05a47adb4fd24ada1a1c2e1f2ba6d2d..a20d9cc6c00a569bb057b9ca8d6b51c7fc4a9847\n--- a/docs/agents/guardian-policy.md\n+++ b/docs/agents/guardian-policy.md\n@@ -98,8 +98,8 @@\n `origin/<default_branch>` and reuses each job's existing base fetch; a shallow\n (`--depth`) or second base fetch would make the proof unresolvable and silently\n disable the carry, so neither is permitted. CCA reads `guardian_comment_carried` as\n-a recorded authority in the deterministic signal and does not re-derive it. See ADR\n-0018 for the full decision, the rejected diff/patch-equality and path-disjointness\n+a recorded authority in the deterministic signal and does not re-derive it. See\n+SADR-0018 for the full decision, the rejected diff/patch-equality and path-disjointness\n alternatives, and the accepted residual.\n \n ## External AI Guardian Review (SADR-0010)\ndiff --git a/docs/agents/implementation-policy.md b/docs/agents/implementation-policy.md\nindex c754144a9fb5bf4a38aa82a60e1670c59e626b44..85c37281bcad196c6b7b02343faf00b03cd9c57a\n--- a/docs/agents/implementation-policy.md\n+++ b/docs/agents/implementation-policy.md\n@@ -31,10 +31,13 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n ## Non-Negotiables\n \n 1. **Planning precedes implementation.** Non-trivial Goals start with `grill-with-docs` and Context & Impact.\n-2. **Domain language is controlled.** Terms must be checked against `CONTEXT.md`; hard-to-reverse tradeoffs must be checked against ADRs.\n+2. **Domain language is controlled.** Terms must be checked against `CONTEXT.md`; hard-to-reverse tradeoffs must be checked against Shiki SADRs and target ADRs.\n 3. **PRD precedes issue decomposition.** `to-prd` is used once the context is settled enough to become durable intent.\n 4. **Issues are vertical slices.** `to-issues` must produce independently grabbable tracer-bullet tasks, not horizontal layer tickets.\n 5. **Triage controls dispatch.** Only `ready-for-agent` / AFK tasks may be assigned to Codex without human input.\n@@ -95,19 +98,19 @@\n Process:\n \n - Challenge terms against `CONTEXT.md`.\n-- Check ADRs before reopening decisions.\n+- Check SADRs and target ADRs before reopening decisions.\n - Inspect code when code can answer a question.\n - Ask one question at a time when the operator is available.\n - Provide a recommended answer with each question.\n - Record resolved terms in `CONTEXT.md`.\n-- Create ADRs only for hard-to-reverse, surprising, tradeoff-based decisions.\n+- Create SADRs or target ADRs only for hard-to-reverse, surprising, tradeoff-based decisions.\n - Convert unresolved load-bearing questions into HITL blockers.\n \n Outputs:\n \n - Resolved design-tree decisions.\n - Updated glossary terms when needed.\n-- ADRs when justified.\n+- SADRs or target ADRs when justified.\n - HITL blockers when decisions remain unresolved.\n \n ## Phase 3 \u2014 Context & Impact\n@@ -117,7 +120,7 @@\n \n Outputs must include:\n \n-- Relevant documents and ADRs.\n+- Relevant documents, SADRs, and target ADRs.\n - Relevant modules, interfaces, seams, callers, and tests.\n - Candidate locks.\n - Dependencies.\n@@ -140,7 +143,7 @@\n - Testing decisions.\n - Out of scope.\n - Further notes.\n-- Links to relevant Goal, domain terms, and ADRs.\n+- Links to relevant Goal, domain terms, SADRs, and target ADRs.\n \n Phase 4 ends with Spec Freeze: the operator explicitly approves the PRD, the\n plan records a `spec_freeze` block (status, approved_by, source), and the SF\n@@ -240,7 +243,7 @@\n - Dependencies and locks.\n - Risk and Guardian requirements.\n - Ledger completeness.\n-- Documentation and ADR updates.\n+- Documentation and SADR/target-ADR updates.\n - Scope drift.\n \n CCA emits one of:\n@@ -301,7 +304,7 @@\n | Evidence failure | Behavior might be done, but proof is missing. | Add checks, PR evidence, ledger entry, or artifacts. |\n | Scope drift | PR changes unrelated behavior. | Revert unrelated changes or split into new Goal. |\n | Dependency block | Upstream task or lock not complete. | Block until dependency resolves. |\n-| Architecture block | Decision needs ADR or deep-module discussion. | `grill-with-docs` or `improve-codebase-architecture`. |\n+| Architecture block | Decision needs a SADR, target ADR, or deep-module discussion. | `grill-with-docs` or `improve-codebase-architecture`. |\n | Risk block | Security/production/policy change needs human approval. | Guardian decision. |\n \n ## Phase 5.5 \u2014 Execution Decision Control\ndiff --git a/docs/agents/runtime-auth-model.md b/docs/agents/runtime-auth-model.md\nindex 6a2e1a47e81ea89b5c68a210ec07772a9374babc..3c54748d24233dbd0ba8473950282547ef4ce935\n--- a/docs/agents/runtime-auth-model.md\n+++ b/docs/agents/runtime-auth-model.md\n@@ -148,7 +148,7 @@\n \n API-key based runners may be added by a target repository as an explicit extension. They are not the default Shiki template path.\n \n-If a repo opts into API-key mode, record the exception in an ADR and update the repo's `AGENTS.md`, `.shiki/config.yaml`, and workflow permissions.\n+If a repo opts into API-key mode, record the target-specific exception in a target ADR and update the repo's `AGENTS.md`, `.shiki/config.yaml`, and workflow permissions.\n \n ## GitHub Actions Runtime Compatibility\n \ndiff --git a/docs/agents/skill-gate.md b/docs/agents/skill-gate.md\nindex c50398b0d7c60a8598f2a5c88a10015730dced13..3acffe6444617bb1d54b77d4b4708158ec735bc6\n--- a/docs/agents/skill-gate.md\n+++ b/docs/agents/skill-gate.md\n@@ -9,7 +9,7 @@\n | Skill | Trigger | Expected output |\n | --- | --- | --- |\n | `setup-matt-pocock-skills` | First configuring a repo for these skills, or when issue tracker, triage labels, or domain docs are missing. | `docs/agents/` configuration and an `Agent skills` block in `AGENTS.md` or `CLAUDE.md`. |\n-| `grill-with-docs` | Ambiguous plan, unclear terminology, boundary decision, tradeoff, or ADR-worthy design choice. | Clarified decision tree, recommended answers, updates to `CONTEXT.md` or ADRs when decisions crystallize. |\n+| `grill-with-docs` | Ambiguous plan, unclear terminology, boundary decision, tradeoff, or decision-record-worthy design choice. | Clarified decision tree, recommended answers, updates to `CONTEXT.md`, SADRs, or target ADRs when decisions crystallize. |\n | `zoom-out` | Agent lacks a higher-level architectural map. | Map of relevant modules, callers, seams, and domain concepts. |\n | `to-prd` | Settled context should become a PRD. | PRD published to the issue tracker with problem, solution, user stories, decisions, testing, and out-of-scope notes. |\n | `to-issues` | Goal, PRD, or plan must become implementation tickets. | Vertical-slice issues with acceptance criteria, dependencies, and AFK/HITL classification. |\n@@ -17,7 +17,7 @@\n | `tdd` | Feature work or bug fix has observable behavior that can be specified with tests. | Red-green-refactor loop through public interfaces, one vertical slice at a time. |\n | `code-review` | The TDD loop is green and the implementer is about to create or update a PR. Default for every implementation task. | Pre-PR self-review of the task diff with fixes applied on the task branch; findings and resolutions recorded as a ledger entry naming the skill and a `## Pre-PR code review` PR body section. |\n | `diagnose` | Hard bug, failing check, regression, flaky behavior, or performance problem. | Reproduction loop, ranked hypotheses, instrumentation, fix, regression test, post-mortem. |\n-| `improve-codebase-architecture` | Structural friction, testability, AI-navigability, deep-module opportunity, or refactor request. | Architecture candidates and deepening recommendations informed by `CONTEXT.md` and ADRs. |\n+| `improve-codebase-architecture` | Structural friction, testability, AI-navigability, deep-module opportunity, or refactor request. | Architecture candidates and deepening recommendations informed by `CONTEXT.md`, SADRs, and target ADRs. |\n | `prototype` | A throwaway logic or UI prototype is needed to answer a design question. | Clearly marked disposable prototype and captured decision. |\n \n ## MergeGate Interaction\ndiff --git a/docs/operations.md b/docs/operations.md\nindex cf1f452d4c589e38ccd58553bda0ad8684b51eb7..ace48a90ffbec9e5c0a7113bef16a4858bc44b3f\n--- a/docs/operations.md\n+++ b/docs/operations.md\n@@ -89,6 +89,16 @@\n      target history, brought forward by `shiki migrate`, not a template.\n    - **Everything else** (CLI scripts, workflows, schemas, docs) is overwritten.\n \n+   A forced upgrade also migrates the former numeric Shiki decision paths to\n+   `SADR-NNNN-*`. Before the first write, the installer inventories the exact\n+   legacy Shiki paths and authorizes each deletion only when the existing\n+   `.shiki/install-stamp.json` names that path and its SHA-256 digest still\n+   matches the file. An absent, unreadable, malformed, incomplete, or\n+   mismatching stamp reports every blocked path and stops with no filesystem\n+   change. Target-owned `NNNN-*` ADRs are not cleanup candidates. A non-force\n+   install deletes nothing and retains any still-matching legacy stamp entries\n+   so a later forced upgrade can prove ownership.\n+\n    If the target has pending migrations, `--force` refuses and writes nothing \u2014\n    apply migrations first (step 3), then re-run. The run ends with a single\n    summary of every `.new` written, so a half-upgrade is visible.\ndiff --git a/docs/releasing.md b/docs/releasing.md\nindex 10d73828e7dbd64e9d9f6b83bc84d010537ce62a..aafeee8c4d441e1e427efee2ce4f81a647f020e1\n--- a/docs/releasing.md\n+++ b/docs/releasing.md\n@@ -67,7 +67,7 @@\n CLI in `scripts/`. There are no third-party Python runtime dependencies. The CLI\n is intentionally **not** exposed as a `[project.scripts]` console entry point\n because `scripts/shiki.py` is a dependency-free shim and `scripts/` is not\n-packaged as an importable distribution (see the ADR for the rationale).\n+packaged as an importable distribution (see SADR-0007 for the rationale).\n \n Supported install paths:\n \n@@ -155,6 +155,15 @@\n    [docs/operations.md](operations.md#the-new-convention). If the target has\n    pending migrations, `--force` refuses and writes nothing; apply migrations\n    (step 2) first, then re-run.\n+\n+   The SADR namespace upgrade has an additional pre-write gate: each former\n+   numeric Shiki decision path is removed only when the existing install stamp\n+   contains that exact path and a digest matching its current bytes. The\n+   installer aggregates absent/unreadable/malformed-stamp, missing-digest, and\n+   digest-mismatch blockers before raising, so refusal leaves the target tree\n+   byte-identical. Numeric target ADRs are preserved. A successful forced\n+   upgrade installs and stamps `SADR-*` paths and drops the removed legacy paths\n+   from the refreshed stamp.\n 2. Apply repository-local `.shiki/` state migrations with the migrate command:\n \n    ```bash\ndiff --git a/docs/usage.md b/docs/usage.md\nindex b83407ee3970dd395dd893010bc5dbe8bad26051..a130ef6e40dfd8169a5bc701339351097df1a09f\n--- a/docs/usage.md\n+++ b/docs/usage.md\n@@ -67,8 +67,10 @@\n shiki install-target /path/to/target-repo --local-only\n ```\n \n-Use `--force` only when you intentionally want to overwrite existing target\n-files.\n+Use `--force` only when you intentionally want to refresh the target template.\n+For the SADR namespace migration, `--force` deletes only exact legacy Shiki\n+decision paths whose existing install-stamp digest still matches; any ownership\n+blocker stops before the first write. Numeric target ADRs are never candidates.\n \n ## `shiki bootstrap-platform` \u2014 publish the Shiki platform repo\n \ndiff --git a/scripts/guardian_approval_signal.py b/scripts/guardian_approval_signal.py\nindex d059f92b2c82aa2b5d6ca421365537a87886c1cb..16924892714f4f5b9303c0ef0e325a7e70075630\n--- a/scripts/guardian_approval_signal.py\n+++ b/scripts/guardian_approval_signal.py\n@@ -199,8 +199,8 @@\n ) -> Any:\n     \"\"\"Evaluate SADR-0015 Contract Approval for the CCA Guardian signal.\n \n-    The SAME OR that MergeGate applies must reach this deterministic signal: ADR\n-    0015 is explicit that without it, a contract-approved PR still returns\n+    The SAME OR that MergeGate applies must reach this deterministic signal:\n+    SADR-0015 is explicit that without it, a contract-approved PR still returns\n     ``needs_guardian``, the CCA job fails, and the MergeGate policy check never\n     runs \u2014 the gate this change moves would never be reached. This resolves the\n     task the SAME way MergeGate does (the first Shiki task id in the PR body),\ndiff --git a/scripts/shiki_contract_approval.py b/scripts/shiki_contract_approval.py\nindex 941bcb96fdd8b9a85bc0c4f0f04d5dad9d04839c..4a4b418f6527413161db82670bec857a043360e2\n--- a/scripts/shiki_contract_approval.py\n+++ b/scripts/shiki_contract_approval.py\n@@ -142,7 +142,7 @@\n     changed_files_status: Any,\n     registration: Any,\n ) -> ContractApprovalResult:\n-    \"\"\"Whether this PR carries an SADR-0015 Contract Approval.\n+    \"\"\"Whether this PR carries a SADR-0015 Contract Approval.\n \n     The four conditions are evaluated IN ORDER, each failure recorded as a\n     distinct reason string:\ndiff --git a/scripts/shiki_guardian.py b/scripts/shiki_guardian.py\nindex a4ecb79bbda55fd5c8b06adfae2eb99760a03c45..4dd99bbe714e26f421fa19abac7a34ae1b195ecc\n--- a/scripts/shiki_guardian.py\n+++ b/scripts/shiki_guardian.py\n@@ -12,7 +12,7 @@\n # The carry re-derives its proof from git in-process on every judging invocation;\n # it is never read from an artifact, so there is nothing to go stale or be forged.\n # shiki_sync_proof is the ONLY guardian-family module that runs git, and every one\n-# of its invocations is read-only and local (ADR: prove-from-git base sync).\n+# of its invocations is read-only and local (SADR: prove-from-git base sync).\n from shiki_sync_proof import verify_pure_base_sync\n \n GUARDIAN_POLICY_PATH = \".shiki/guardian-policy.json\"\ndiff --git a/scripts/shiki_guardian_review.py b/scripts/shiki_guardian_review.py\nindex dd0f104d457899e0ba5ff1e162011c1e90b6f92b..e10ef58c046324e7ea3a3392feb3048ca77414ce\n--- a/scripts/shiki_guardian_review.py\n+++ b/scripts/shiki_guardian_review.py\n@@ -159,7 +159,7 @@\n     docs_only = _is_docs_only(changed_files)\n     if docs_only and matched == 0:\n         focus.append(\n-            \"Docs/ADR-only change: confirm the change is genuinely documentation/decision-only, introduces no behavior or contract drift, and stays consistent with the constitution and existing ADRs.\"\n+            \"Docs/decision-record-only change: confirm the change is genuinely documentation/decision-only, introduces no behavior or contract drift, and stays consistent with the constitution and existing SADRs and target ADRs.\"\n         )\n \n     # Conservative augmentation: unknown (no category and not docs-only), mixed\ndiff --git a/scripts/shiki_installer.py b/scripts/shiki_installer.py\nindex be064ab7a29ff6836f7f7a1d70500a43e0d11d7b..9fd8eefd137ce7003dad5c0dcb2e48e2cd47ab32\n--- a/scripts/shiki_installer.py\n+++ b/scripts/shiki_installer.py\n@@ -118,6 +118,7 @@\n # what lets a target carry it without a governance-schema change.\n INSTALL_STAMP_PATH = \".shiki/install-stamp.json\"\n INSTALL_STAMP_VERSION = 1\n+LEGACY_SHIKI_SADR_MAX = 18\n \n # Under --force the shipped surface splits three ways, so nothing is lost and\n # nothing is silently stale.\n@@ -237,9 +238,98 @@\n         )\n \n \n+def legacy_shiki_adr_paths() -> tuple[str, ...]:\n+    \"\"\"Former numeric paths for Shiki SADR-0001 through SADR-0018.\n+\n+    The authoritative slugs come from the current SADR filenames. SADR-0019 is\n+    the namespace decision itself and never shipped under a numeric legacy path.\n+    \"\"\"\n+    paths: list[str] = []\n+    for sadr in sorted((ROOT / \"docs\" / \"adr\").glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\")):\n+        number = sadr.name.split(\"-\", 2)[1]\n+        if int(number) <= LEGACY_SHIKI_SADR_MAX:\n+            paths.append(f\"docs/adr/{sadr.name.removeprefix('SADR-')}\")\n+    return tuple(paths)\n+\n+\n+def preflight_legacy_sadr_cleanup(target: Path, *, force: bool) -> tuple[str, ...]:\n+    \"\"\"Authorize exact legacy Shiki ADR removals before the first write.\n+\n+    A non-force install never removes existing files. A forced install needs no\n+    stamp when no legacy Shiki path is present. When legacy paths do exist, each\n+    must be named in a readable install stamp and its current bytes must match\n+    the recorded digest.\n+    \"\"\"\n+    if not force:\n+        return ()\n+    present = tuple(\n+        relative\n+        for relative in legacy_shiki_adr_paths()\n+        if (target / relative).exists() or (target / relative).is_symlink()\n+    )\n+    if not present:\n+        return ()\n+\n+    stamp_path = target / INSTALL_STAMP_PATH\n+    try:\n+        raw = stamp_path.read_text(encoding=\"utf-8\")\n+        stamp = json.loads(raw)\n+    except FileNotFoundError:\n+        detail = \"install stamp is absent\"\n+        blockers = [f\"{relative}: {detail}\" for relative in present]\n+    except (OSError, UnicodeDecodeError) as error:\n+        detail = f\"install stamp is unreadable ({error})\"\n+        blockers = [f\"{relative}: {detail}\" for relative in present]\n+    except json.JSONDecodeError as error:\n+        detail = f\"install stamp is malformed ({error})\"\n+        blockers = [f\"{relative}: {detail}\" for relative in present]\n+    else:\n+        if not isinstance(stamp, dict):\n+            blockers = [\n+                f\"{relative}: install stamp is malformed (expected an object)\"\n+                for relative in present\n+            ]\n+        else:\n+            digests = stamp.get(\"digests\")\n+            blockers = []\n+            if not isinstance(digests, dict):\n+                blockers.extend(\n+                    f\"{relative}: install stamp has no digest for this path\"\n+                    for relative in present\n+                )\n+            else:\n+                for relative in present:\n+                    expected = digests.get(relative)\n+                    if not isinstance(expected, str) or not expected:\n+                        blockers.append(\n+                            f\"{relative}: install stamp has no digest for this path\"\n+                        )\n+                        continue\n+                    try:\n+                        actual = _sha256_file(target / relative)\n+                    except OSError as error:\n+                        blockers.append(\n+                            f\"{relative}: legacy path is unreadable ({error})\"\n+                        )\n+                        continue\n+                    if actual != expected:\n+                        blockers.append(\n+                            f\"{relative}: digest mismatch (stamp {expected}, current {actual})\"\n+                        )\n+\n+    if blockers:\n+        joined = \"\\n\".join(f\"- {blocker}\" for blocker in blockers)\n+        raise ShikiError(\n+            \"legacy Shiki ADR cleanup is not authorized:\\n\"\n+            f\"{joined}\\nNo files were written.\"\n+        )\n+    return present\n+\n+\n def install_template(target: Path, *, force: bool, validate: bool) -> list[NewFileNote]:\n     # Preconditions first: never rewrite the tree of a target that cannot be\n     # upgraded cleanly (see refuse_pending_migrations).\n+    legacy_cleanup = preflight_legacy_sadr_cleanup(target, force=force)\n     refuse_pending_migrations(target)\n \n     # Resolve the target's required CODEOWNERS owner once from its\n@@ -250,6 +340,10 @@\n     # verbatim. See ``codeowners_required_owner``.\n     codeowners_owner = codeowners_required_owner(target)\n \n+    for relative in legacy_cleanup:\n+        (target / relative).unlink()\n+        info(f\"removed stamped legacy Shiki ADR: {target / relative}\")\n+\n     notes: list[NewFileNote] = []\n     for relative in TEMPLATE_PATHS:\n         source = ROOT / relative\n@@ -647,6 +741,22 @@\n     digests = target_stamp_digests(target)\n     existing = read_install_stamp(target)\n     if existing is not None:\n+        # A non-force install deliberately leaves legacy Shiki ADRs in place.\n+        # Preserve only still-matching ownership proof so a later forced\n+        # upgrade can authorize cleanup; never carry a missing or stale digest.\n+        existing_digests = existing.get(\"digests\")\n+        if isinstance(existing_digests, dict):\n+            for relative in legacy_shiki_adr_paths():\n+                expected = existing_digests.get(relative)\n+                path = target / relative\n+                if not isinstance(expected, str) or not path.is_file():\n+                    continue\n+                try:\n+                    actual = _sha256_file(path)\n+                except OSError:\n+                    continue\n+                if actual == expected:\n+                    digests[relative] = actual\n         if _incoming_is_older(commit, existing.get(\"platform_commit\")):\n             warn(\n                 \"kept existing install stamp: incoming install \"\ndiff --git a/scripts/shiki_loop.py b/scripts/shiki_loop.py\nindex 3611641da8aeb5038b1da90e3114b09a473ec6d8..84caa491b9cad0edc4176f209c13e9221e975681\n--- a/scripts/shiki_loop.py\n+++ b/scripts/shiki_loop.py\n@@ -424,7 +424,7 @@\n         # unresolvable CCA verdict has already produced its terminal stop and is never\n         # laundered into a head-moving sync (SADR-0010). It also never fires while the\n         # policy (Guardian) gate is red \u2014 for ANY reason, not only when it is the sole\n-        # red check \u2014 so a head-bound Guardian approval, or an SADR-0015\n+        # red check \u2014 so a head-bound Guardian approval, or a SADR-0015\n         # contract-approval carry that base movement defeated (the policy gate compares\n         # base and head task governance directly), is never silently restored by\n         # adopting base wholesale. It is bounded by a durable per-task counter so the\ndiff --git a/scripts/shiki_tasks.py b/scripts/shiki_tasks.py\nindex aec3809f24b4a0c8c96fc50971cff9ae9ad30381..d531052e9d9717058d4631483cefab44c26a65c1\n--- a/scripts/shiki_tasks.py\n+++ b/scripts/shiki_tasks.py\n@@ -821,7 +821,7 @@\n         \"prompt\": prompt,\n         \"entry_skill\": \"grill-with-docs\",\n         \"required_next_steps\": [\n-            \"Run grill-with-docs until terminology, boundaries, risks, and ADR-worthy decisions are settled.\",\n+            \"Run grill-with-docs until terminology, boundaries, risks, and decision-record-worthy choices are settled.\",\n             \"Obtain the operator's explicit approval of the PRD/requirements (Spec Freeze).\",\n             \"Write a machine-readable plan JSON with grill_with_docs.status=complete and spec_freeze.status=frozen.\",\n             \"Run shiki plan ingest --plan-file PLAN.json.\",\ndiff --git a/scripts/test_shiki_goal_loop.sh b/scripts/test_shiki_goal_loop.sh\nindex 753c7b516e10f0be3bc92a4bd0eb1fa699776ae6..8f3820a0a079b3044424cfe1a6c6b7fe68896e67\n--- a/scripts/test_shiki_goal_loop.sh\n+++ b/scripts/test_shiki_goal_loop.sh\n@@ -231,8 +231,8 @@\n \n # High/critical risk merges autonomously when all required checks are green:\n # the \"MergeGate policy check\" required check IS the Guardian gate (it enforces\n-# guardian-policy.json \u2014 human approval OR external AI guardian review, ADR\n-# 0010), so green checks mean a recorded authority approved. The fake gh returns\n+# guardian-policy.json \u2014 human approval OR external AI guardian review,\n+# SADR-0010), so green checks mean a recorded authority approved. The fake gh returns\n # green checks, simulating that the Guardian gate passed.\n python3 \"$ROOT/scripts/shiki.py\" goal create --target \"$TARGET\" --title \"Guardian gate\" --outcome \"High risk merges via policy gate\" >/tmp/shiki-goal-loop-guardian-goal.json\n GGOAL=\"$(json_get /tmp/shiki-goal-loop-guardian-goal.json goal_id)\"\ndiff --git a/tests/test_adr_references.py b/tests/test_adr_references.py\nindex 51706329c797d5dd9b47e082a333fa83b261d482..d005da4f771f5e8085ec22d4b787980e68f5ae8c\n--- a/tests/test_adr_references.py\n+++ b/tests/test_adr_references.py\n@@ -1,8 +1,8 @@\n-\"\"\"Every ``ADR ####`` citation must resolve to exactly one decision record.\n+\"\"\"Every target ADR and Shiki SADR citation resolves in its own namespace.\n \n Background. ``scripts/mergegate_check.py`` justifies ``is_bookkeeping_closeout``\n-with \"ADR 0017\" nine times and ``scripts/guardian_approval_signal.py`` cites\n-0016/0017 seven times, yet for a stretch the numbering in ``docs/adr/`` stopped\n+with \"SADR-0017\" nine times and ``scripts/guardian_approval_signal.py`` cites\n+SADR-0016/SADR-0017 seven times, yet for a stretch the numbering in ``docs/adr/`` stopped\n at 0015: the authoring commits lived on a coordinator branch and no task ever\n declared ``docs/adr/...`` in its locks, so nothing carried the records to the\n default branch. The stated basis of an exemption that merged seven PRs with zero\n@@ -11,11 +11,12 @@\n and the directory drifted with no signal.\n \n This is the same unbound-pair shape behind other silent failures: a reference\n-and its target with nothing asserting they agree. This test binds them. It scans\n-every ``scripts/*.py`` (plus ``docs/**/*.md`` and the root ``*.md`` governance\n-files, which are cheap) for the ``ADR ####`` pattern and asserts each cited\n-number resolves to exactly one ``docs/adr/####-*.md``. The converse is\n-deliberately NOT required: an ADR may exist without being cited.\n+and its target with nothing asserting they agree. This test binds them. Target\n+``ADR NNNN`` citations resolve only to ``docs/adr/NNNN-*.md``; Shiki\n+``SADR-NNNN`` citations resolve only to ``docs/adr/SADR-NNNN-*.md``. The scan\n+covers current governance docs, scripts, workflows, prompts, packaging metadata,\n+and repo-local Codex guidance while deliberately excluding historical\n+``.shiki`` evidence. The converse is not required: a record may be uncited.\n \n The suite is dependency free: standard-library ``unittest`` only.\n \"\"\"\n@@ -31,17 +32,18 @@\n \n REPO_ROOT = Path(__file__).resolve().parents[1]\n \n-# \"ADR 0016\" -- a four-digit citation. The trailing \\b keeps a five-digit token\n-# from being read as a truncated four-digit number.\n-_CITATION = re.compile(r\"\\bADR (\\d{4})\\b\")\n-# \"0016-locks-bound-product-paths.md\" -- the leading four digits name the record.\n-_ADR_FILENAME = re.compile(r\"^(\\d{4})-.*\\.md$\")\n+# ``ADR 0016`` / ``SADR-0016`` -- a namespace plus four-digit citation. The\n+# trailing ``\\b`` keeps a five-digit token from being read as a truncated one.\n+_CITATION = re.compile(r\"\\b(S?ADR)[ -](\\d{4})\\b\")\n+# Target ADRs retain the numeric filename; Shiki platform decisions use SADR.\n+_DECISION_FILENAME = re.compile(r\"^(?:(SADR)-)?(\\d{4})-.*\\.md$\")\n \n \n class Violation(NamedTuple):\n     \"\"\"A citation that does not resolve to exactly one decision record.\"\"\"\n \n     citing_file: str  # repo-relative path of the file that cites the ADR\n+    namespace: str  # \"ADR\" for target decisions; \"SADR\" for Shiki decisions\n     number: str  # the cited four-digit number, e.g. \"0016\"\n     line: int  # 1-based line of the citation\n     reason: str  # \"missing\" (zero matches) or \"ambiguous\" (two or more)\n@@ -49,44 +51,59 @@\n \n     @property\n     def expected_path(self) -> str:\n-        return f\"docs/adr/{self.number}-*.md\"\n+        prefix = \"SADR-\" if self.namespace == \"SADR\" else \"\"\n+        return f\"docs/adr/{prefix}{self.number}-*.md\"\n \n     def describe(self) -> str:\n         \"\"\"Name the citing file, the cited number, and the missing path.\"\"\"\n         if self.reason == \"missing\":\n             return (\n-                f\"{self.citing_file}:{self.line} cites ADR {self.number} \"\n+                f\"{self.citing_file}:{self.line} cites {self.namespace} {self.number} \"\n                 f\"but no document matches {self.expected_path}\"\n             )\n         joined = \", \".join(self.matches)\n         return (\n-            f\"{self.citing_file}:{self.line} cites ADR {self.number} which \"\n+            f\"{self.citing_file}:{self.line} cites {self.namespace} {self.number} which \"\n             f\"resolves to {len(self.matches)} documents (expected exactly one \"\n             f\"at {self.expected_path}): {joined}\"\n         )\n \n \n-def _adr_index(root: Path) -> dict[str, list[Path]]:\n-    \"\"\"Map every four-digit ADR number to the record files that claim it.\"\"\"\n-    index: dict[str, list[Path]] = {}\n+def _adr_index(root: Path) -> dict[tuple[str, str], list[Path]]:\n+    \"\"\"Map each decision namespace and number to the files that claim it.\"\"\"\n+    index: dict[tuple[str, str], list[Path]] = {}\n     adr_dir = root / \"docs\" / \"adr\"\n     if adr_dir.is_dir():\n         for path in sorted(adr_dir.glob(\"*.md\")):\n-            match = _ADR_FILENAME.match(path.name)\n+            match = _DECISION_FILENAME.match(path.name)\n             if match:\n-                index.setdefault(match.group(1), []).append(path)\n+                namespace = \"SADR\" if match.group(1) else \"ADR\"\n+                index.setdefault((namespace, match.group(2)), []).append(path)\n     return index\n \n \n def _citing_files(root: Path, include_docs: bool = True) -> list[Path]:\n-    \"\"\"Files scanned for citations: scripts/*.py, and cheaply the markdown.\"\"\"\n+    \"\"\"Current, shipped text surfaces scanned for decision citations.\"\"\"\n     files = sorted((root / \"scripts\").glob(\"*.py\"))\n+    files += sorted((root / \"scripts\").glob(\"*.sh\"))\n     if include_docs:\n         docs = root / \"docs\"\n         if docs.is_dir():\n             files += sorted(docs.rglob(\"*.md\"))\n         files += sorted(root.glob(\"*.md\"))\n-    return files\n+        pyproject = root / \"pyproject.toml\"\n+        if pyproject.is_file():\n+            files.append(pyproject)\n+        for relative in (\".github\", \".codex\"):\n+            surface = root / relative\n+            if surface.is_dir():\n+                files += sorted(\n+                    path\n+                    for path in surface.rglob(\"*\")\n+                    if path.is_file()\n+                    and path.suffix in {\".md\", \".py\", \".sh\", \".toml\", \".yaml\", \".yml\"}\n+                )\n+    return sorted(set(files))\n \n \n def find_adr_reference_violations(\n@@ -94,10 +111,9 @@\n ) -> list[Violation]:\n     \"\"\"Return every ``ADR ####`` citation under ``root`` that fails to resolve.\n \n-    A citation resolves when exactly one ``docs/adr/####-*.md`` matches its\n-    number. Zero matches is a \"missing\" violation; two or more is \"ambiguous\".\n-    Records that are never cited produce nothing -- the converse is not\n-    required.\n+    A citation resolves when exactly one record in the same namespace matches\n+    its number. Zero matches is a \"missing\" violation; two or more is\n+    \"ambiguous\". Records that are never cited produce nothing.\n     \"\"\"\n     root = Path(root)\n     index = _adr_index(root)\n@@ -110,13 +126,15 @@\n         rel = path.relative_to(root).as_posix()\n         for lineno, line in enumerate(text.splitlines(), start=1):\n             for match in _CITATION.finditer(line):\n-                number = match.group(1)\n-                found = index.get(number, [])\n+                namespace, number = match.groups()\n+                found = index.get((namespace, number), [])\n                 if len(found) == 1:\n                     continue\n                 reason = \"missing\" if not found else \"ambiguous\"\n                 rels = [p.relative_to(root).as_posix() for p in found]\n-                violations.append(Violation(rel, number, lineno, reason, rels))\n+                violations.append(\n+                    Violation(rel, namespace, number, lineno, reason, rels)\n+                )\n     return violations\n \n \n@@ -129,8 +147,8 @@\n         self.assertEqual(\n             violations,\n             [],\n-            \"Every 'ADR ####' citation must resolve to exactly one \"\n-            f\"docs/adr/####-*.md:\\n{detail}\",\n+            \"Every ADR/SADR citation must resolve exactly once in its own \"\n+            f\"docs/adr namespace:\\n{detail}\",\n         )\n \n     def test_scan_actually_reads_scripts(self) -> None:\n@@ -142,12 +160,38 @@\n             \"the citation scan must cover scripts/*.py\",\n         )\n \n+    def test_platform_citations_do_not_use_unresolved_numeric_shorthand(self) -> None:\n+        shorthand = re.compile(r\"SADR-\\d{4}/\\d{4}\")\n+        offenders = []\n+        for path in _citing_files(REPO_ROOT):\n+            text = path.read_text(encoding=\"utf-8\")\n+            if shorthand.search(text):\n+                offenders.append(path.relative_to(REPO_ROOT).as_posix())\n+        self.assertEqual(\n+            offenders,\n+            [],\n+            \"repeat the SADR prefix so every cited number is independently resolvable\",\n+        )\n+\n     def test_carried_records_are_indexed(self) -> None:\n         # The two records this Goal carried onto the default branch, and which\n         # the scripts cite most, must be present.\n         index = _adr_index(REPO_ROOT)\n-        self.assertIn(\"0016\", index)\n-        self.assertIn(\"0017\", index)\n+        self.assertIn((\"SADR\", \"0016\"), index)\n+        self.assertIn((\"SADR\", \"0017\"), index)\n+\n+    def test_platform_tree_has_only_sadr_0001_through_0019(self) -> None:\n+        adr_dir = REPO_ROOT / \"docs\" / \"adr\"\n+        sadrs = sorted(adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\"))\n+        self.assertEqual(\n+            {path.name.split(\"-\", 2)[1] for path in sadrs},\n+            {f\"{number:04d}\" for number in range(1, 20)},\n+        )\n+        self.assertEqual(\n+            list(adr_dir.glob(\"[0-9][0-9][0-9][0-9]-*.md\")),\n+            [],\n+            \"the Shiki platform tree must not retain legacy numeric SADR paths\",\n+        )\n \n \n class FixtureAdrReferences(unittest.TestCase):\n@@ -180,6 +224,26 @@\n         self.assertIn(\"9991\", message)  # cited number\n         self.assertIn(\"docs/adr/9991-*.md\", message)  # missing path\n \n+    def test_target_adr_does_not_satisfy_missing_platform_sadr(self) -> None:\n+        root = self._make_repo(\n+            adr_names=[\"0042-target-decision.md\"],\n+            script_body='\"\"\"Target ADR 0042 exists, but platform SADR-0042 does not.\"\"\"\\n',\n+        )\n+        violations = find_adr_reference_violations(root, include_docs=False)\n+        self.assertEqual(len(violations), 1)\n+        self.assertEqual(violations[0].namespace, \"SADR\")\n+        self.assertEqual(violations[0].number, \"0042\")\n+        self.assertEqual(violations[0].expected_path, \"docs/adr/SADR-0042-*.md\")\n+\n+    def test_target_adr_and_platform_sadr_may_reuse_a_number(self) -> None:\n+        root = self._make_repo(\n+            adr_names=[\"0042-target-decision.md\", \"SADR-0042-platform-decision.md\"],\n+            script_body='\"\"\"Target ADR 0042 and platform SADR-0042 are distinct.\"\"\"\\n',\n+        )\n+        self.assertEqual(\n+            find_adr_reference_violations(root, include_docs=False), []\n+        )\n+\n     def test_uncited_adr_is_allowed(self) -> None:\n         # An ADR exists but nothing cites it -> not a violation. The converse\n         # (every ADR must be cited) is explicitly NOT required.\ndiff --git a/tests/test_guardian_review.py b/tests/test_guardian_review.py\nindex 88829e7810212635e2fc98aa5918d6f26294677d..00a85a237acd5bf2d6cb5b294128198c422402ca\n--- a/tests/test_guardian_review.py\n+++ b/tests/test_guardian_review.py\n@@ -162,7 +162,7 @@\n \n     def test_docs_only_is_light_and_not_conservative(self):\n         focus = gr.classify_review_focus([\"docs/adr/0099-x.md\", \"README.md\"], \"\", \"low\")\n-        self.assertTrue(any(\"Docs/ADR-only\" in f for f in focus))\n+        self.assertTrue(any(\"Docs/decision-record-only\" in f for f in focus))\n         self.assertFalse(any(\"conservative default\" in f.lower() for f in focus))\n \n     def test_unknown_category_is_conservative(self):\ndiff --git a/tests/test_policy_job_merged_prs.py b/tests/test_policy_job_merged_prs.py\nindex d45512e74681e5e649279236c7d4e79d5c3da471..b97989f8bd74d091de1a6b5275a20d8dc68c4512\n--- a/tests/test_policy_job_merged_prs.py\n+++ b/tests/test_policy_job_merged_prs.py\n@@ -3,8 +3,8 @@\n \n ``.github/workflows/shiki-cca-completion.yml`` has two consumers of the merged-PR\n proof: the CCA *signal* (``guardian_approval_signal.py`` in \"Prepare CCA context\")\n-and the authoritative gate (``mergegate_check.py`` in \"Enforce MergeGate\"). ADR\n-0017's bookkeeping-closeout exemption (condition 6) needs the merged implementation\n+and the authoritative gate (``mergegate_check.py`` in \"Enforce MergeGate\").\n+SADR-0017's bookkeeping-closeout exemption (condition 6) needs the merged implementation\n PR proven, and a closeout carries NO ``post_merge_reconcile`` marker. The signal\n already resolves ``MERGED_PRS`` unconditionally, but \"Enforce MergeGate\" used to\n resolve it only inside ``grep -q '<!-- shiki:post_merge_reconcile -->'``. So a\ndiff --git a/tests/test_sadr_install_migration.py b/tests/test_sadr_install_migration.py\nnew file mode 100644\nindex 0000000000000000000000000000000000000000..d51eac872b1c79ff4579c4c1401ca5d775a60d8d\n--- /dev/null\n+++ b/tests/test_sadr_install_migration.py\n@@ -0,0 +1,237 @@\n+\"\"\"SADR installation and collision-safe legacy upgrade behavior.\"\"\"\n+\n+from __future__ import annotations\n+\n+import contextlib\n+import hashlib\n+import io\n+import json\n+import shutil\n+import tempfile\n+import unittest\n+from pathlib import Path\n+\n+import shiki_test_support  # noqa: F401  (path bootstrap)\n+\n+import shiki_installer\n+from tests.test_adr_references import find_adr_reference_violations\n+\n+\n+PRODUCT_NUMBERS = tuple(range(13, 19))\n+\n+\n+def _quiet_install(target: Path, *, force: bool, validate: bool) -> None:\n+    with contextlib.redirect_stdout(io.StringIO()):\n+        shiki_installer.install_template(target, force=force, validate=validate)\n+\n+\n+def _sha256(path: Path) -> str:\n+    return hashlib.sha256(path.read_bytes()).hexdigest()\n+\n+\n+def _legacy_sadr_paths() -> tuple[str, ...]:\n+    return tuple(\n+        f\"docs/adr/{path.name.removeprefix('SADR-')}\"\n+        for path in sorted((shiki_installer.ROOT / \"docs\" / \"adr\").glob(\"SADR-*.md\"))\n+        if path.name != \"SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\"\n+    )\n+\n+\n+def _create_legacy_upgrade_fixture(\n+    target: Path,\n+) -> tuple[dict[str, bytes], tuple[str, ...]]:\n+    adr_dir = target / \"docs\" / \"adr\"\n+    adr_dir.mkdir(parents=True)\n+    product_bytes = {\n+        f\"{number:04d}-cubeshare-product-decision.md\": (\n+            f\"# CubeShare ADR {number:04d}\\n\\nDo not rewrite {number}.\\n\"\n+        ).encode()\n+        for number in PRODUCT_NUMBERS\n+    }\n+    for name, content in product_bytes.items():\n+        (adr_dir / name).write_bytes(content)\n+\n+    legacy_paths = _legacy_sadr_paths()\n+    for relative in legacy_paths:\n+        (target / relative).write_bytes(f\"legacy Shiki bytes for {relative}\\n\".encode())\n+\n+    stamp_path = target / shiki_installer.INSTALL_STAMP_PATH\n+    stamp_path.parent.mkdir(parents=True, exist_ok=True)\n+    stamp = {\n+        \"version\": shiki_installer.INSTALL_STAMP_VERSION,\n+        \"platform_commit\": None,\n+        \"installed_at\": \"2000-01-01T00:00:00+00:00\",\n+        \"digests\": {\n+            relative: _sha256(target / relative) for relative in legacy_paths\n+        },\n+    }\n+    stamp_path.write_text(\n+        json.dumps(stamp, indent=2, sort_keys=True) + \"\\n\", encoding=\"utf-8\"\n+    )\n+    return product_bytes, legacy_paths\n+\n+\n+def _tree_snapshot(root: Path) -> dict[str, tuple[str, bytes | str]]:\n+    \"\"\"Recursive byte/shape snapshot used to prove preflight is write-free.\"\"\"\n+    snapshot: dict[str, tuple[str, bytes | str]] = {}\n+    for path in sorted(root.rglob(\"*\")):\n+        relative = path.relative_to(root).as_posix()\n+        if path.is_symlink():\n+            snapshot[relative] = (\"symlink\", path.readlink().as_posix())\n+        elif path.is_dir():\n+            snapshot[relative] = (\"directory\", \"\")\n+        else:\n+            snapshot[relative] = (\"file\", path.read_bytes())\n+    return snapshot\n+\n+\n+class FreshSadrInstallTests(unittest.TestCase):\n+    def setUp(self) -> None:\n+        self.tmp = Path(tempfile.mkdtemp(prefix=\"shiki-sadr-fresh-\"))\n+        self.addCleanup(lambda: shutil.rmtree(self.tmp, ignore_errors=True))\n+        self.target = self.tmp / \"target\"\n+        self.adr_dir = self.target / \"docs\" / \"adr\"\n+        self.adr_dir.mkdir(parents=True)\n+        self.product_bytes = {\n+            f\"{number:04d}-product-decision.md\": (\n+                f\"# Product ADR {number:04d}\\n\\nTarget-owned bytes {number}.\\n\"\n+            ).encode()\n+            for number in PRODUCT_NUMBERS\n+        }\n+        for name, content in self.product_bytes.items():\n+            (self.adr_dir / name).write_bytes(content)\n+\n+    def test_fresh_install_preserves_product_adrs_and_installs_all_sadrs(self) -> None:\n+        _quiet_install(self.target, force=False, validate=True)\n+\n+        for name, expected in self.product_bytes.items():\n+            self.assertEqual((self.adr_dir / name).read_bytes(), expected)\n+\n+        sadr_numbers = {\n+            path.name.split(\"-\", 2)[1]\n+            for path in self.adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\")\n+        }\n+        self.assertEqual(sadr_numbers, {f\"{number:04d}\" for number in range(1, 20)})\n+\n+        numeric_records = {\n+            path.name\n+            for path in self.adr_dir.glob(\"[0-9][0-9][0-9][0-9]-*.md\")\n+        }\n+        self.assertEqual(numeric_records, set(self.product_bytes))\n+        stamp = json.loads(\n+            (self.target / shiki_installer.INSTALL_STAMP_PATH).read_text(\n+                encoding=\"utf-8\"\n+            )\n+        )\n+        for path in self.adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\"):\n+            relative = path.relative_to(self.target).as_posix()\n+            self.assertEqual(stamp[\"digests\"][relative], _sha256(path))\n+        self.assertEqual(find_adr_reference_violations(self.target), [])\n+\n+\n+class LegacySadrUpgradeTests(unittest.TestCase):\n+    def setUp(self) -> None:\n+        self.tmp = Path(tempfile.mkdtemp(prefix=\"shiki-sadr-upgrade-\"))\n+        self.addCleanup(lambda: shutil.rmtree(self.tmp, ignore_errors=True))\n+        self.target = self.tmp / \"target\"\n+        self.adr_dir = self.target / \"docs\" / \"adr\"\n+        self.product_bytes, self.legacy_paths = _create_legacy_upgrade_fixture(\n+            self.target\n+        )\n+\n+    def test_force_upgrade_deletes_only_stamped_legacy_sadrs(self) -> None:\n+        _quiet_install(self.target, force=True, validate=True)\n+\n+        for relative in self.legacy_paths:\n+            self.assertFalse((self.target / relative).exists(), relative)\n+        for name, expected in self.product_bytes.items():\n+            self.assertEqual((self.adr_dir / name).read_bytes(), expected)\n+\n+        sadrs = list(self.adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\"))\n+        self.assertEqual(len(sadrs), 19)\n+        refreshed = json.loads(\n+            (self.target / shiki_installer.INSTALL_STAMP_PATH).read_text(encoding=\"utf-8\")\n+        )\n+        for relative in self.legacy_paths:\n+            self.assertNotIn(relative, refreshed[\"digests\"])\n+        for path in sadrs:\n+            relative = path.relative_to(self.target).as_posix()\n+            self.assertEqual(refreshed[\"digests\"][relative], _sha256(path))\n+\n+    def test_non_force_install_never_deletes_legacy_files(self) -> None:\n+        before = {\n+            relative: (self.target / relative).read_bytes()\n+            for relative in self.legacy_paths\n+        }\n+        _quiet_install(self.target, force=False, validate=False)\n+        for relative, expected in before.items():\n+            self.assertEqual((self.target / relative).read_bytes(), expected)\n+        refreshed = json.loads(\n+            (self.target / shiki_installer.INSTALL_STAMP_PATH).read_text(encoding=\"utf-8\")\n+        )\n+        for relative in self.legacy_paths:\n+            self.assertEqual(refreshed[\"digests\"][relative], _sha256(self.target / relative))\n+\n+\n+class LegacyCleanupPreflightTests(unittest.TestCase):\n+    def setUp(self) -> None:\n+        self.tmp = Path(tempfile.mkdtemp(prefix=\"shiki-sadr-preflight-\"))\n+        self.addCleanup(lambda: shutil.rmtree(self.tmp, ignore_errors=True))\n+\n+    def test_all_ownership_blockers_abort_before_any_write(self) -> None:\n+        cases = (\n+            (\"absent\", \"install stamp is absent\"),\n+            (\"unreadable\", \"install stamp is unreadable\"),\n+            (\"malformed\", \"install stamp is malformed\"),\n+            (\"missing-digest\", \"install stamp has no digest\"),\n+            (\"digest-mismatch\", \"digest mismatch\"),\n+        )\n+        for name, expected_reason in cases:\n+            with self.subTest(name=name):\n+                target = self.tmp / name\n+                _, legacy_paths = _create_legacy_upgrade_fixture(target)\n+                stamp_path = target / shiki_installer.INSTALL_STAMP_PATH\n+\n+                if name == \"absent\":\n+                    stamp_path.unlink()\n+                elif name == \"unreadable\":\n+                    stamp_path.unlink()\n+                    stamp_path.mkdir()\n+                elif name == \"malformed\":\n+                    stamp_path.write_text(\"{not-json\\n\", encoding=\"utf-8\")\n+                else:\n+                    stamp = json.loads(stamp_path.read_text(encoding=\"utf-8\"))\n+                    if name == \"missing-digest\":\n+                        stamp[\"digests\"] = {}\n+                    else:\n+                        stamp[\"digests\"] = {\n+                            relative: \"deadbeef\" for relative in legacy_paths\n+                        }\n+                    stamp_path.write_text(\n+                        json.dumps(stamp, indent=2, sort_keys=True) + \"\\n\",\n+                        encoding=\"utf-8\",\n+                    )\n+\n+                before = _tree_snapshot(target)\n+                with self.assertRaises(shiki_installer.ShikiError) as raised:\n+                    _quiet_install(target, force=True, validate=False)\n+                after = _tree_snapshot(target)\n+\n+                self.assertEqual(before, after, f\"{name} preflight changed the tree\")\n+                message = str(raised.exception)\n+                self.assertIn(expected_reason, message)\n+                self.assertIn(\"No files were written\", message)\n+                for relative in legacy_paths:\n+                    self.assertIn(relative, message)\n+\n+    def test_absent_stamp_is_allowed_when_no_legacy_path_exists(self) -> None:\n+        target = self.tmp / \"no-legacy\"\n+        target.mkdir()\n+        _quiet_install(target, force=True, validate=False)\n+        self.assertTrue(\n+            (target / \"docs\" / \"adr\" / \"SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\").is_file()\n+        )\n+\n+\n+if __name__ == \"__main__\":\n+    unittest.main()\n\n2026-08-07T08:26:35.896124Z ERROR codex_models_manager::manager: failed to renew cache TTL: missing field `base_instructions` at line 94 column 5\ndiff --git a/.codex/skills/shiki/SKILL.md b/.codex/skills/shiki/SKILL.md\nindex c8e2e85d9d75d49cc4f2d6286ee22659c4e53e75..93cded623b2c15d9f65c76491a1c2d813ac9e6c0\n--- a/.codex/skills/shiki/SKILL.md\n+++ b/.codex/skills/shiki/SKILL.md\n@@ -18,6 +18,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n Codex CLI does not register this skill as a custom `/shiki` slash command. When\n the user types `/shiki` in Codex CLI and gets \"Unrecognized command\", explain\n that this is expected: invoke Shiki in Codex with natural language such as\n@@ -69,7 +72,7 @@\n \n ## Responsibilities\n \n-- Claude Code implements and repairs by default (`shiki runner claude`, ADR 0008); Codex implements and repairs tasks explicitly assigned to `codex`.\n+- Claude Code implements and repairs by default (`shiki runner claude`, SADR-0008); Codex implements and repairs tasks explicitly assigned to `codex`.\n - Claude Code Action can act as GitHub-side CCA or reviewer.\n - CCA judges completion.\n - MergeGate authorizes state transitions and merge readiness.\ndiff --git a/.github/prompts/cca-completion-check.md b/.github/prompts/cca-completion-check.md\nindex ed203ad5f302ecc81372d8ab27d2b1232ca97497..fd89950528333ef6242e6d5443904f5392ab7e56\n--- a/.github/prompts/cca-completion-check.md\n+++ b/.github/prompts/cca-completion-check.md\n@@ -19,6 +19,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n ## Required Reading\n \n Read only the files needed to judge the PR contract. Prefer this order:\ndiff --git a/.github/prompts/codex-task-handoff.md b/.github/prompts/codex-task-handoff.md\nindex 695e211d76dea1c60e8d5bb360e78487808286a4..59441d576b01a39569722f226c854e5a740e9eb5\n--- a/.github/prompts/codex-task-handoff.md\n+++ b/.github/prompts/codex-task-handoff.md\n@@ -2,7 +2,7 @@\n \n You are Codex Front acting as the Implementer for a Shiki task.\n \n-Follow `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant ADRs, and `docs/agents/codex-handoff.md`.\n+Follow `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant Shiki SADRs and target ADRs, and `docs/agents/codex-handoff.md`.\n \n Rules:\n \ndiff --git a/AGENTS.md b/AGENTS.md\nindex 7f041100d49c2a7525f51cb0f5d696a4a6d1c78c..a06bfbabc6fa48a007389e25e12d7ed523936bed\n--- a/AGENTS.md\n+++ b/AGENTS.md\n@@ -22,7 +22,7 @@\n - **Codex Front** is an optional implementer through Codex App, Codex CLI, Codex IDE extension, or Codex Web signed in with ChatGPT OAuth/subscription auth, dispatched with `shiki runner codex` when a task is explicitly assigned to `codex`.\n - **GitHub CCA** is implemented by Claude Code Action by default, using `CLAUDE_CODE_OAUTH_TOKEN`.\n \n-Do not assume `openai/codex-action`, `OPENAI_API_KEY`, or API-key based Codex automation in the default Shiki path. API-key runners are explicit target-repo extensions and require their own ADR.\n+Do not assume `openai/codex-action`, `OPENAI_API_KEY`, or API-key based Codex automation in the default Shiki path. API-key runners are explicit target-repo extensions and require their own target ADR.\n \n Shiki is not:\n \n@@ -44,10 +44,14 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use\n+`SADR-NNNN-*.md`; target product decisions retain `NNNN-*.md` and the `ADR`\n+namespace.\n+\n Use the canonical source-of-truth order above. Runtime-specific reading orders\n are operational guidance only and must not override or reorder it.\n \n-Conversation state is not durable truth. If a decision matters, put it in GitHub, `.shiki/`, `CONTEXT.md`, or an ADR.\n+Conversation state is not durable truth. If a decision matters, put it in GitHub, `.shiki/`, `CONTEXT.md`, a Shiki SADR, or a target ADR.\n \n If GitHub and `.shiki/` disagree, surface the conflict and prefer GitHub operational state until the mirror is repaired.\n \n@@ -75,8 +79,8 @@\n Every non-trivial change follows this loop:\n \n 1. **Goal**: clarify outcome, completion conditions, non-goals, risk level, and success signals.\n-2. **grill-with-docs**: challenge the plan against domain docs, ADRs, code reality, terminology, and edge scenarios.\n-3. **Context & Impact**: find relevant docs, ADRs, code, dependencies, owners, locks, and verification surfaces.\n+2. **grill-with-docs**: challenge the plan against domain docs, Shiki SADRs, target ADRs, code reality, terminology, and edge scenarios.\n+3. **Context & Impact**: find relevant docs, SADRs, target ADRs, code, dependencies, owners, locks, and verification surfaces.\n 4. **PRD and Spec Freeze**: use `to-prd` when context is settled enough to become durable product/engineering intent. The operator's explicit approval of the PRD is Spec Freeze: the plan records a `spec_freeze` block, and no plan runs without it. Steps 1-4 form Requirements Definition \u2014 one continuous operator dialogue. After Spec Freeze, scope changes require an operator-approved Spec Amendment; non-scope-moving interpretations go to the Assumption Log.\n 5. **Issues**: use `to-issues` to create independently grabbable vertical-slice issues. Prefer AFK slices over HITL slices where possible.\n 6. **Triage**: label issues for readiness, risk, runtime, skills, and MergeGate state.\n@@ -167,7 +171,7 @@\n - Testing decisions.\n - Out-of-scope items.\n - Further notes.\n-- Links to relevant domain terms and ADRs.\n+- Links to relevant domain terms, SADRs, and target ADRs.\n \n Every executable task must include:\n \n@@ -193,7 +197,7 @@\n \n Context & Impact must identify:\n \n-- Relevant documents, domain terms, ADRs, and past decisions.\n+- Relevant documents, domain terms, SADRs, target ADRs, and past decisions.\n - Relevant modules, interfaces, seams, callers, owners, and tests.\n - Dependency relationships between tasks.\n - Candidate locks and lock conflicts.\n@@ -210,7 +214,7 @@\n | Skill | Required when |\n | --- | --- |\n | `setup-matt-pocock-skills` | First configuring the repo for skills, or when issue tracker, triage labels, or domain docs are missing. |\n-| `grill-with-docs` | Default for non-trivial Goals before PRD/issues; always required when plans, terminology, boundaries, tradeoffs, or ADR-worthy decisions are ambiguous. |\n+| `grill-with-docs` | Default for non-trivial Goals before PRD/issues; always required when plans, terminology, boundaries, tradeoffs, or decision-record-worthy choices are ambiguous. |\n | `zoom-out` | The agent lacks an architectural map of the relevant code area. |\n | `to-prd` | Settled Goal context must become durable product/engineering intent. |\n | `to-issues` | A Goal, PRD, or plan must become independently grabbable vertical-slice issues. |\n@@ -348,7 +352,7 @@\n - Three or more new files.\n - Any high-risk or critical label.\n \n-When the architecture gate triggers, run `grill-with-docs` or `improve-codebase-architecture` as appropriate and record durable decisions in an ADR when needed.\n+When the architecture gate triggers, run `grill-with-docs` or `improve-codebase-architecture` as appropriate and record durable decisions in a Shiki SADR or target ADR as applicable.\n \n ## Engineering Discipline\n \ndiff --git a/CLAUDE.md b/CLAUDE.md\nindex 98a932e7841a632866fcd53b0bad0e3af3711d3d..98a31399404d6fb044b4fd493f4459ac87f439de\n--- a/CLAUDE.md\n+++ b/CLAUDE.md\n@@ -14,6 +14,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n ## Claude Role\n \n Default to planner, default implementer, reviewer, coordinator, documentation editor, CCA-style completion judge, and final-judgment assistant.\n@@ -57,7 +60,7 @@\n At the start of a Shiki session:\n \n 1. Read `AGENTS.md`.\n-2. Read `CONTEXT.md` and relevant ADRs.\n+2. Read `CONTEXT.md` and relevant Shiki SADRs and target ADRs.\n 3. Read active `.shiki/` Goal/task/ledger state when present.\n 4. Read active distilled rules from `.shiki/memories/` (status `distilled`, `active: true`, not revoked or superseded) for applicable guidance; the same rules are injected deterministically into task handoffs as the `## Distilled Rules` section (\u00a73.5).\n 5. Read the GitHub Issue or PR when the task is GitHub-backed.\n@@ -74,7 +77,7 @@\n - Define the Goal, completion conditions, non-goals, and risk level.\n - Treat `grill-with-docs` as the default planning entry point for non-trivial Goals.\n - Challenge domain terms against `CONTEXT.md`.\n-- Challenge hard-to-reverse tradeoffs against ADRs.\n+- Challenge hard-to-reverse tradeoffs against SADRs and target ADRs.\n - Explore code instead of asking when code can answer the question.\n - Run Context & Impact before implementation planning.\n - Produce a Task DAG, not an unordered checklist.\n@@ -94,7 +97,7 @@\n - For each question, provide your recommended answer.\n - Resolve dependency decisions before downstream design questions.\n - Update `CONTEXT.md` inline when terminology crystallizes.\n-- Offer ADRs only when the decision is hard to reverse, surprising without context, and a real tradeoff.\n+- Offer a SADR or target ADR only when the decision is hard to reverse, surprising without context, and a real tradeoff.\n - Convert unresolved questions into HITL blockers rather than allowing Codex to guess.\n \n ## PRD and Issues Standard\n@@ -103,7 +106,7 @@\n \n - Use the project's domain glossary vocabulary.\n - Avoid volatile file paths unless they are essential.\n-- Include implementation decisions, testing decisions, out-of-scope items, and relevant ADR/domain links.\n+- Include implementation decisions, testing decisions, out-of-scope items, and relevant SADR/target-ADR/domain links.\n - Identify deep-module opportunities and testable interfaces.\n \n When creating issues:\n@@ -121,7 +124,7 @@\n - Branch/worktree target.\n - Scope and non-goals.\n - Dependencies and locks.\n-- Relevant docs, ADRs, modules, and tests.\n+- Relevant docs, SADRs, target ADRs, modules, and tests.\n - Required skill invocations.\n - TDD expectations.\n - Pre-PR `code-review` gate expectations.\n@@ -152,7 +155,7 @@\n \n - Treat yourself as a judge, not implementer.\n - Do not write production code.\n-- Read `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant ADRs, PRD, task issue, PR body, diff, checks, reviews, labels, and `.shiki/` state.\n+- Read `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant SADRs and target ADRs, PRD, task issue, PR body, diff, checks, reviews, labels, and `.shiki/` state.\n - Evaluate every applicable checklist in `docs/agents/checklists.md`.\n - Map each acceptance criterion to durable evidence.\n - Distinguish implementation failure from missing evidence.\ndiff --git a/CONTEXT.md b/CONTEXT.md\nindex e61a9be06a0cce0d026484083a657bde2067e234..237ee8aa3ea8fab334ece49768bc5ed3b5af0974\n--- a/CONTEXT.md\n+++ b/CONTEXT.md\n@@ -25,7 +25,7 @@\n _Avoid_: jumping directly into code\n \n **grill-with-docs**:\n-A planning skill that challenges a plan against domain language, ADRs, code reality, and concrete edge scenarios. It resolves design-tree questions before PRD/issues. It runs inside Requirements Definition.\n+A planning skill that challenges a plan against domain language, Shiki SADRs, target ADRs, code reality, and concrete edge scenarios. It resolves design-tree questions before PRD/issues. It runs inside Requirements Definition.\n _Avoid_: generic brainstorming, silent assumptions\n \n **Requirements Definition**:\ndiff --git a/CONTRIBUTING.md b/CONTRIBUTING.md\nindex c6d9b656e73abb8a55e5f660123e03153dcb3e6d..9e37a8fbf668db5b22461aaae5548b8f1354394c\n--- a/CONTRIBUTING.md\n+++ b/CONTRIBUTING.md\n@@ -17,7 +17,7 @@\n 1. **Goal** \u2014 start from a user-approved Goal with an outcome, completion\n    conditions, non-goals, risk level, and success signals.\n 2. **grill-with-docs** \u2014 for any non-trivial change, challenge the plan against\n-   domain language, ADRs, and code reality before writing a PRD.\n+   domain language, Shiki SADRs, target ADRs, and code reality before writing a PRD.\n 3. **PRD** \u2014 capture settled product/engineering intent (`to-prd`).\n 4. **Spec Freeze** \u2014 the operator explicitly approves the PRD/requirements;\n    the plan records a `spec_freeze` block and no plan runs without it\n@@ -94,7 +94,7 @@\n   entries.\n - Do not hand-edit `.shiki/goals/*.json` or `.shiki/manifest.json` in a task PR.\n - Conversation state is not durable truth. If a decision matters, record it in\n-  GitHub, `.shiki/`, `CONTEXT.md`, or an ADR.\n+  GitHub, `.shiki/`, `CONTEXT.md`, a Shiki SADR, or a target ADR.\n \n ## Architecture Gate\n \n@@ -103,7 +103,7 @@\n or CI policy, package structure, three or more structural files, more than 100\n net new lines in a core module, three or more new files, or any high-risk label.\n When triggered, run `grill-with-docs` or `improve-codebase-architecture` and\n-record durable decisions in an ADR when needed (see `docs/adr/`).\n+record durable decisions in a Shiki SADR or target ADR as applicable (see `docs/adr/`).\n \n ## Safety\n \ndiff --git a/SYSTEM_PROMPT.md b/SYSTEM_PROMPT.md\nindex aaaea45e3a896c3a44ba89eec8bb84325ecf6c62..da009c86a88285269dee0155e58e550c6d744ea9\n--- a/SYSTEM_PROMPT.md\n+++ b/SYSTEM_PROMPT.md\n@@ -30,6 +30,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n Use the canonical source-of-truth order above. Runtime-specific reading orders\n are operational guidance only and must not override or reorder it. The current\n conversation is non-durable operator input.\n@@ -48,14 +51,14 @@\n \n A runtime may only do work that is explicitly assigned by the Goal, task, PR, or MergeGate decision.\n \n-Do not assume `openai/codex-action`, `OPENAI_API_KEY`, or API-key based Codex automation in the default Shiki path. API-key runners are explicit target-repo extensions and require their own ADR.\n+Do not assume `openai/codex-action`, `OPENAI_API_KEY`, or API-key based Codex automation in the default Shiki path. API-key runners are explicit target-repo extensions and require their own target ADR.\n \n ## 4. Non-negotiable Shiki flow\n \n For any non-trivial Goal, use this flow:\n \n 1. **Goal Seek**: define outcome, non-goals, risk level, completion criteria, and evidence requirements.\n-2. **grill-with-docs**: challenge the plan against `CONTEXT.md`, ADRs, code reality, terminology, boundaries, and edge scenarios. Ask one question at a time when the operator is available. If code can answer a question, inspect code instead of asking.\n+2. **grill-with-docs**: challenge the plan against `CONTEXT.md`, Shiki SADRs, target ADRs, code reality, terminology, boundaries, and edge scenarios. Ask one question at a time when the operator is available. If code can answer a question, inspect code instead of asking.\n 3. **Context & Impact**: identify modules, interfaces, seams, callers, dependencies, locks, risk, and verification surfaces.\n 4. **to-prd + Spec Freeze**: turn settled context into a PRD using domain vocabulary and testing decisions. The operator's explicit approval of the PRD is Spec Freeze, recorded as the plan's `spec_freeze` block; plans without it do not run. Steps 1-4 form Requirements Definition.\n 5. **to-issues**: decompose the PRD into independently grabbable vertical-slice issues. Prefer AFK slices; mark HITL slices when judgment is still required.\n@@ -75,7 +78,7 @@\n Before making any material change:\n \n 1. Read `AGENTS.md` and any runtime-specific wrapper such as `CLAUDE.md`.\n-2. Read `CONTEXT.md` and relevant ADRs.\n+2. Read `CONTEXT.md` and relevant SADRs and target ADRs.\n 3. Read the GitHub Issue / PR and `.shiki/` state for the active Goal or task.\n 4. Identify your role: Planner, Implementer, Reviewer, Completion Check Agent, Repairer, or Guardian-assist.\n 5. Identify Goal id, task id, branch, PR, dependencies, locks, risk level, required skills, and required evidence.\n@@ -103,7 +106,7 @@\n \n At minimum, identify:\n \n-- Relevant documents, ADRs, domain terms, and prior decisions.\n+- Relevant documents, SADRs, target ADRs, domain terms, and prior decisions.\n - Relevant modules, interfaces, seams, callers, owners, and tests.\n - Dependency relationships between tasks.\n - Candidate file locks and lock conflicts.\n@@ -152,7 +155,7 @@\n \n CCA must read:\n \n-- `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant ADRs.\n+- `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant SADRs and target ADRs.\n - Parent Goal issue, PRD issue, task issue, comments, and labels.\n - PR body, diff, commits, changed files, reviews, CI checks, and artifacts available to the workflow.\n - `.shiki/` task contract, locks, ledger entries, CCA checklist profile, and prior repair packets.\n@@ -214,7 +217,7 @@\n - Three or more new files.\n - Any high-risk or critical label.\n \n-When the architecture gate triggers, run `grill-with-docs` or `improve-codebase-architecture` as appropriate and record the decision in an ADR if it is hard to reverse or likely to be re-litigated.\n+When the architecture gate triggers, run `grill-with-docs` or `improve-codebase-architecture` as appropriate and record the decision in a Shiki SADR or target ADR as applicable if it is hard to reverse or likely to be re-litigated.\n \n ## 14. Skill Gate\n \n@@ -223,7 +226,7 @@\n Use these skills:\n \n - `setup-matt-pocock-skills`: before first use of the engineering skill set, or when issue tracker, triage labels, or domain docs are missing.\n-- `grill-with-docs`: default for non-trivial Goals before PRD/issues; required for ambiguous plans, terminology, boundaries, ADR-worthy tradeoffs, or design-tree decisions.\n+- `grill-with-docs`: default for non-trivial Goals before PRD/issues; required for ambiguous plans, terminology, boundaries, decision-record-worthy tradeoffs, or design-tree decisions.\n - `zoom-out`: unfamiliar code area or missing architectural map.\n - `to-prd`: convert settled context into a PRD.\n - `to-issues`: break a PRD, plan, or Goal into independently grabbable vertical-slice issues.\ndiff --git a/docs/adr/0001-keep-shiki-as-platform-name.md b/docs/adr/SADR-0001-keep-shiki-as-platform-name.md\nindex 20a9637c3b9d8c97de7ba8db7cfae4d82d9a7dba..8b685439959aada1b221c112f0ac3c51ca8a1695\n--- a/docs/adr/0001-keep-shiki-as-platform-name.md\n+++ b/docs/adr/SADR-0001-keep-shiki-as-platform-name.md\n@@ -1,3 +1,3 @@\n-# Keep Shiki As The Platform Name\n+# SADR-0001: Keep Shiki As The Platform Name\n \n Shiki remains the product name for the GitHub-native agentic engineering control plane. We considered more descriptive names such as MergePilot, MergeGate, and Kiroku, but kept Shiki because it carries the existing goal-seeking framework identity while remaining independent of Hermes, Codex, Claude Code, or any single runtime.\ndiff --git a/docs/adr/0002-use-agents-as-shared-constitution.md b/docs/adr/SADR-0002-use-agents-as-shared-constitution.md\nindex 73de30ecb078120eac540d9bc51855e3cd48d0e3..c5486182bf06fca36ea508496b013c67302fd989\n--- a/docs/adr/0002-use-agents-as-shared-constitution.md\n+++ b/docs/adr/SADR-0002-use-agents-as-shared-constitution.md\n@@ -1,3 +1,3 @@\n-# Use AGENTS As The Shared Agent Constitution\n+# SADR-0002: Use AGENTS As The Shared Agent Constitution\n \n Shiki uses `AGENTS.md` as the runtime-neutral constitution and `CLAUDE.md` as a thin Claude Code wrapper. This keeps Codex, Claude Code, Hermes Runner, and future Agent Runtimes aligned on the same GitHub-first MergeGate rules while still allowing runtime-specific instructions where needed.\ndiff --git a/docs/adr/0003-require-skill-gate-for-mergegate-readiness.md b/docs/adr/SADR-0003-require-skill-gate-for-mergegate-readiness.md\nindex 424be0c9af4c8f0136f1fde68a468c47ff84e764..7fc337987c7df9792f75f181dfcbed75fe5ffff2\n--- a/docs/adr/0003-require-skill-gate-for-mergegate-readiness.md\n+++ b/docs/adr/SADR-0003-require-skill-gate-for-mergegate-readiness.md\n@@ -1,3 +1,3 @@\n-# Require Skill Gate For MergeGate Readiness\n+# SADR-0003: Require Skill Gate For MergeGate Readiness\n \n Shiki requires relevant engineering skills to be invoked before execution when their triggers apply. This makes skills part of MergeGate readiness rather than optional prompt style, because Shiki depends on repeatable planning, issue decomposition, TDD, diagnosis, triage, and architecture review before autonomous or parallel agent work can be trusted.\ndiff --git a/docs/adr/0004-use-github-first-with-shiki-mirror.md b/docs/adr/SADR-0004-use-github-first-with-shiki-mirror.md\nindex d0a3a9174dc646a945a9d325954ffaf00c352040..a79c54ae9ee8eed2a0524192b864da4e22a92d3f\n--- a/docs/adr/0004-use-github-first-with-shiki-mirror.md\n+++ b/docs/adr/SADR-0004-use-github-first-with-shiki-mirror.md\n@@ -1,4 +1,4 @@\n-# ADR 0004: Use GitHub First With A Repository-Local Shiki Mirror\n+# SADR-0004: Use GitHub First With A Repository-Local Shiki Mirror\n \n ## Status\n \ndiff --git a/docs/adr/0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md b/docs/adr/SADR-0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\nindex b339baf49b89d9290e79c3d51b36e7b2bde3bd95..0f3c3eb9afccdb3403ce978e21d04fe12e495da0\n--- a/docs/adr/0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\n+++ b/docs/adr/SADR-0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\n@@ -1,8 +1,8 @@\n-# ADR 0005: Use Codex As Front And Claude Code Action As The GitHub Runtime\n+# SADR-0005: Use Codex As Front And Claude Code Action As The GitHub Runtime\n \n ## Status\n \n-Accepted (implementer default superseded by ADR 0008; the auth model and the\n+Accepted (implementer default superseded by SADR-0008; the auth model and the\n Claude Code Action GitHub runtime decision remain in force)\n \n ## Context\n@@ -26,4 +26,4 @@\n - Shiki avoids requiring `OPENAI_API_KEY` for the default workflow.\n - Codex work happens through the user's authenticated Codex front surface, not as a hidden backend Action.\n - Claude Code Action is configured for OAuth-token based subscription usage by default.\n-- API-key based automation remains possible only as an explicit target-repo extension with its own ADR.\n+- API-key based automation remains possible only as an explicit target-repo extension with its own target ADR.\ndiff --git a/docs/adr/0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md b/docs/adr/SADR-0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\nindex 4bcae9d1e52f0a37b8e9fbafa5cd14b6ee1993bd..7a38538a6ae9929567f0ade67e7ea6a60d0b3f7e\n--- a/docs/adr/0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\n+++ b/docs/adr/SADR-0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\n@@ -1,4 +1,4 @@\n-# ADR 0006: Separate Codex Implementation, CCA Completion, And MergeGate Authorization\n+# SADR-0006: Separate Codex Implementation, CCA Completion, And MergeGate Authorization\n \n ## Status\n \ndiff --git a/docs/adr/0007-packaging-and-release.md b/docs/adr/SADR-0007-packaging-and-release.md\nindex 79b84ddb97167372b44641e5b7860e400ce80822..bb445a2e2e856d9d1f5fdbe35de8d4e8cec0fb32\n--- a/docs/adr/0007-packaging-and-release.md\n+++ b/docs/adr/SADR-0007-packaging-and-release.md\n@@ -1,4 +1,4 @@\n-# ADR 0007: Packaging And Release For Shiki\n+# SADR-0007: Packaging And Release For Shiki\n \n ## Status\n \n@@ -25,7 +25,7 @@\n   not introduce third-party runtime dependencies or break the shim.\n - Releases must run inside the operator's subscription-authenticated toolchain\n   and must not require extra secrets beyond the built-in `GITHUB_TOKEN`\n-  (consistent with ADR 0005's no-API-key-by-default stance).\n+  (consistent with SADR-0005's no-API-key-by-default stance).\n - `.shiki/manifest.json` already encodes the install surface (`install.include`,\n   `install.create_directories`, `install.exclude_from_commit`), and it must not\n   be mutated as part of this change.\n@@ -34,7 +34,7 @@\n \n This is a hard-to-reverse platform decision (it sets the version source of\n truth, the release trigger, and the public install contract), so it is recorded\n-as an ADR.\n+as a SADR.\n \n ## Decision\n \n@@ -104,7 +104,7 @@\n 3. **Publishing to PyPI.** Rejected for now: Shiki is distributed as a\n    repository template + git-installable CLI, not a PyPI library; PyPI would add\n    credentials/secrets and a different versioning contract. Can be added later as\n-   its own ADR if a packaged distribution is needed.\n+   its own SADR if a packaged distribution is needed.\n 4. **Dynamic version (read `VERSION` via a build backend `dynamic` field).**\n    Rejected to keep `pyproject.toml` backend-agnostic and dependency-free; the\n    small duplication between `VERSION` and `pyproject.toml` is guarded by the\n@@ -123,4 +123,4 @@\n   releasing versions the whole platform but ships only `install.include` to\n   targets.\n - Future PyPI distribution or a console entry point are deferred and would each\n-  warrant their own ADR.\n+  warrant their own SADR.\ndiff --git a/docs/adr/0008-use-claude-code-as-default-implementer.md b/docs/adr/SADR-0008-use-claude-code-as-default-implementer.md\nindex dedf4510b7c7799521422f622029563d373db031..35c2625d908167fbc58855b5b63e283805f167e8\n--- a/docs/adr/0008-use-claude-code-as-default-implementer.md\n+++ b/docs/adr/SADR-0008-use-claude-code-as-default-implementer.md\n@@ -1,12 +1,12 @@\n-# ADR 0008: Use Claude Code As The Default Implementer Runtime\n+# SADR-0008: Use Claude Code As The Default Implementer Runtime\n \n ## Status\n \n-Proposed (supersedes ADR 0005 for the implementer default; keeps its auth model)\n+Proposed (supersedes SADR-0005 for the implementer default; keeps its auth model)\n \n ## Context\n \n-ADR 0005 made Codex Front the default implementation runtime, with Claude Code\n+SADR-0005 made Codex Front the default implementation runtime, with Claude Code\n restricted to planner and reviewer roles by the runtime registry, and the only\n autonomous implementation adapter was `shiki runner codex`.\n \n@@ -18,13 +18,13 @@\n - The platform's planning, review, and CCA layers already run on Claude Code,\n   and Claude Code's native capabilities (the built-in `/code-review` skill and\n   the Workflow multi-agent orchestration tool) have no Codex equivalent.\n-- The Goal lifecycle redesign (ADR 0009) requires an autonomous post-freeze\n+- The Goal lifecycle redesign (SADR-0009) requires an autonomous post-freeze\n   loop driven from the operator's Claude Code session, including bounded\n   repair dispatch, which must not depend on a runtime that cannot be invoked\n   unattended.\n \n The constitution claims Shiki is runtime-agnostic (\"not a Claude-only\n-workflow\", ADR 0002). Any change to the implementer default must preserve the\n+workflow\", SADR-0002). Any change to the implementer default must preserve the\n registry-based runtime model rather than hardcoding one vendor.\n \n ## Decision\n@@ -39,7 +39,7 @@\n   the task to review or repair-needed.\n - Keep Codex registered as an optional implementer runtime. `shiki runner\n   codex`, the Codex handoff templates, and the ChatGPT OAuth auth model from\n-  ADR 0005 remain supported for tasks explicitly assigned to `codex`.\n+  SADR-0005 remain supported for tasks explicitly assigned to `codex`.\n - Commit to mandating two Claude-native capabilities as loop contract in\n   follow-up Goals (status: the code-review gate was delivered by Goal G-B \u2014\n   skill registry, checklists item PR-12, default required_skills \u2014 while the\n@@ -60,7 +60,7 @@\n \n Out of scope: removing Codex support, changing the CCA runtime (Claude Code\n Action with `CLAUDE_CODE_OAUTH_TOKEN` stays), and API-key based automation\n-(still requires its own ADR per ADR 0005).\n+(still requires its own target ADR per SADR-0005).\n \n ## Consequences\n \n@@ -100,7 +100,7 @@\n   harness rejection of direct `codex exec` is outside Shiki's control, and the\n   plan's mandatory native capabilities cannot run on Codex at all.\n - **Remove Codex entirely**: rejected; it abandons the runtime-agnostic\n-  constitution (ADR 0002) for no operational gain and forecloses\n+  constitution (SADR-0002) for no operational gain and forecloses\n   cross-runtime adversarial review.\n - **GitHub Actions cloud implementation via claude-code-action**: rejected as\n   default; durable evidence is automatic but token cost, Node 24 workflow\ndiff --git a/docs/adr/0009-requirements-definition-and-spec-freeze.md b/docs/adr/SADR-0009-requirements-definition-and-spec-freeze.md\nindex 29b3f7871bef9203a8b4a831a65c39eb2f506e42..52e7dd69ad0aa4efe0b68c62678abbf7df02aaec\n--- a/docs/adr/0009-requirements-definition-and-spec-freeze.md\n+++ b/docs/adr/SADR-0009-requirements-definition-and-spec-freeze.md\n@@ -1,4 +1,4 @@\n-# ADR 0009: Merge Grill And Planning Into Requirements Definition With Spec Freeze\n+# SADR-0009: Merge Grill And Planning Into Requirements Definition With Spec Freeze\n \n ## Status\n \ndiff --git a/docs/adr/0010-external-ai-guardian-review.md b/docs/adr/SADR-0010-external-ai-guardian-review.md\nindex e6108bbe8bb4e73e0ff4684c8f78001a80438c4e..f3510b145664298adafb151b6f0db51b972f01ec\n--- a/docs/adr/0010-external-ai-guardian-review.md\n+++ b/docs/adr/SADR-0010-external-ai-guardian-review.md\n@@ -1,4 +1,4 @@\n-# ADR 0010: External AI Guardian Review As A First-Class Approval Authority\n+# SADR-0010: External AI Guardian Review As A First-Class Approval Authority\n \n ## Status\n \n@@ -8,7 +8,7 @@\n \n Shiki is intended to be an autonomous, recursive, self-improving agent\n framework: authority-in-the-loop, not human-in-the-loop. Until now the Guardian\n-approval required for high/critical-risk merges (guardian-policy.json, ADR\n+approval required for high/critical-risk merges (guardian-policy.json, SADR\n governance) recognized only human authority sources \u2014 a GitHub review approval,\n the `guardian:approved` label applied by a configured Guardian, or a comment\n carrying `Guardian approval granted` plus the current head SHA.\n@@ -89,7 +89,7 @@\n   autonomous design the operator intends and produced the impersonation\n   incident.\n - **Record AI approval as an operator comment**: rejected as forgery of human\n-  authority \u2014 the exact failure this ADR corrects.\n+  authority \u2014 the exact failure this SADR corrects.\n - **Deliver the artifact as a committed `.shiki/` file**: rejected; a committed\n   approval inside the PR being approved is forgeable by anyone who can push.\n - **A dedicated reviewer bot identity instead of a relayed comment**: deferred;\ndiff --git a/docs/adr/0011-autonomous-quality-gate-evidence.md b/docs/adr/SADR-0011-autonomous-quality-gate-evidence.md\nindex bca0246c5d46e44afbde15486a75eec425fc02f5..9c93eac16d8c3a6d4b11bd7224e3955102cd7db4\n--- a/docs/adr/0011-autonomous-quality-gate-evidence.md\n+++ b/docs/adr/SADR-0011-autonomous-quality-gate-evidence.md\n@@ -1,4 +1,4 @@\n-# ADR 0011: Autonomous Quality-Gate Evidence \u2014 Deterministic Facts And An Independent Verifier\n+# SADR-0011: Autonomous Quality-Gate Evidence \u2014 Deterministic Facts And An Independent Verifier\n \n ## Status\n \ndiff --git a/docs/adr/0012-autonomous-loop-closeout-to-main.md b/docs/adr/SADR-0012-autonomous-loop-closeout-to-main.md\nindex 637fad6dde4989c9c2bf0f81f676d8d29991123c..ec7d85f81030a676bcbba28b43eb365422a36eac\n--- a/docs/adr/0012-autonomous-loop-closeout-to-main.md\n+++ b/docs/adr/SADR-0012-autonomous-loop-closeout-to-main.md\n@@ -1,4 +1,4 @@\n-# 0012 \u2014 Autonomous loop pushes goal completion to main via a closeout PR\n+# SADR-0012 \u2014 Autonomous loop pushes goal completion to main via a closeout PR\n \n - Status: accepted\n - Date: 2026-06-18\ndiff --git a/docs/adr/0013-bootstrap-grants-actions-approve-pull-requests.md b/docs/adr/SADR-0013-bootstrap-grants-actions-approve-pull-requests.md\nindex b6c69d64f1c5ced346b88d6859586715b3b5bd0e..cf48ff4a6716267f539ca58edd1a852e7c7a2d64\n--- a/docs/adr/0013-bootstrap-grants-actions-approve-pull-requests.md\n+++ b/docs/adr/SADR-0013-bootstrap-grants-actions-approve-pull-requests.md\n@@ -1,4 +1,4 @@\n-# ADR 0013: Bootstrap Grants GitHub Actions Permission To Create And Approve Pull Requests\n+# SADR-0013: Bootstrap Grants GitHub Actions Permission To Create And Approve Pull Requests\n \n ## Status\n \n@@ -31,7 +31,7 @@\n security tradeoff: it lets repository automation satisfy a human-review gate. In\n a multi-maintainer repo that could let a compromised or buggy workflow\n self-approve changes. So whether bootstrap turns it on by default \u2014 and with\n-what default token scope \u2014 is an ADR-worthy decision, not an implementation\n+what default token scope \u2014 is a SADR-worthy decision, not an implementation\n detail.\n \n ## Decision\n@@ -48,7 +48,7 @@\n Specifically:\n \n - **`can_approve_pull_request_reviews` defaults to `true`.** Shiki's default\n-  posture is solo/AFK autonomous operation (ADR 0008/0009/0012), where the CCA\n+  posture is solo/AFK autonomous operation (SADR-0008/0009/0012), where the CCA\n   Review Bridge is the only path that can satisfy `required_review: true` after\n   the CCA and Guardian gates have already passed. The default makes a\n   freshly-bootstrapped repo able to run the autonomous loop end to end without a\ndiff --git a/docs/adr/0014-codex-app-external-ai-guardian-ui-adapter.md b/docs/adr/SADR-0014-codex-app-external-ai-guardian-ui-adapter.md\nindex 238db06279334caa341c117362df9271382a7fcd..65f12e9939321e1b3e389a34c822fc1549e5155d\n--- a/docs/adr/0014-codex-app-external-ai-guardian-ui-adapter.md\n+++ b/docs/adr/SADR-0014-codex-app-external-ai-guardian-ui-adapter.md\n@@ -1,4 +1,4 @@\n-# ADR 0014: Use Codex App As The External AI Guardian UI Adapter\n+# SADR-0014: Use Codex App As The External AI Guardian UI Adapter\n \n ## Status\n \ndiff --git a/docs/adr/0015-contract-approval-before-dispatch.md b/docs/adr/SADR-0015-contract-approval-before-dispatch.md\nindex fad6c656741a20a7684ba16080a7db159f63a52f..6061271a46d3f1d6ae7174477de3fb75673123a6\n--- a/docs/adr/0015-contract-approval-before-dispatch.md\n+++ b/docs/adr/SADR-0015-contract-approval-before-dispatch.md\n@@ -1,4 +1,4 @@\n-# ADR 0015: Approve The Task Contract Before Dispatch, Not The Diff After It\n+# SADR-0015: Approve The Task Contract Before Dispatch, Not The Diff After It\n \n ## Status\n \n@@ -9,7 +9,7 @@\n Guardian approval for high/critical risk is expressible only on a pull request.\n Every approval source in `guardian-policy.json` \u2014 GitHub review, the\n `guardian:approved` label, the `Guardian approval granted` comment, and the\n-`external_ai_guardian_review` artifact (ADR 0010/0014) \u2014 is bound to a PR, and\n+`external_ai_guardian_review` artifact (SADR-0010/0014) \u2014 is bound to a PR, and\n three of the four require the current head SHA. Approval therefore cannot exist\n before an implementation PR exists.\n \n@@ -81,7 +81,7 @@\n An envelope breach must surface only on the non-repairable policy gate, never on\n the repairable `MergeGate metadata check`. Otherwise the Goal Loop routes it to\n `dispatch_repair` \u2014 instructing a runner to make the breach go away \u2014 which is\n-exactly what this ADR forbids.\n+exactly what this SADR forbids.\n \n **Contract immutability.** After registration, an implementation PR must not\n change the governance fields of its own task contract (`scope`, `non_goals`,\n@@ -116,7 +116,7 @@\n fails any verdict that is not `complete`, and the signal the CCA prompt reads is\n computed from `evaluate_guardian_approval` alone. Without the OR-merge at the\n signal, a contract-approved PR still returns `needs_guardian`, the CCA job fails,\n-and the MergeGate policy check never runs \u2014 the gate this ADR changes would never\n+and the MergeGate policy check never runs \u2014 the gate this SADR changes would never\n be reached. The CCA *prompt* needs no change: a `contract_approval` entry in the\n signal's existing `sources` array satisfies its current wording.\n \n@@ -130,7 +130,7 @@\n written, and an approved contract lets its implementation PRs merge through the\n same automatic path as low/medium risk. The wasted implement-then-block cycle\n disappears. Guardian review shifts from reading diffs to reading contracts, which\n-is both earlier and smaller; the external AI Guardian path (ADR 0010/0014) works\n+is both earlier and smaller; the external AI Guardian path (SADR-0010/0014) works\n on contracts with no change.\n \n We accept that the Guardian authorizes a class of change without seeing its\n@@ -146,7 +146,7 @@\n adversarial tests for the new approval path equivalent in rigor to\n `test_shiki_governance_evidence.sh`.\n \n-Bootstrap costs, which apply only while this ADR is being implemented. The tasks\n+Bootstrap costs, which apply only while this SADR is being implemented. The tasks\n that build Contract Approval merge under the existing rules, so the ones\n dispatched after the pre-dispatch gate lands but before the approval path lands\n need a Guardian-approved Contract PR *and* a live Guardian approval on their\n@@ -210,7 +210,7 @@\n risk; they do not bound authorized work.\n \n **Auto-escalate risk on envelope breach instead of stopping.** Returns the\n-workflow to a diff-time Guardian decision \u2014 the exact behavior this ADR removes.\n+workflow to a diff-time Guardian decision \u2014 the exact behavior this SADR removes.\n \n **Resolve governance fields from the base snapshot only.** Rejected against the\n registration flow: implementation PRs add their own contract, so the base has no\ndiff --git a/docs/adr/0016-locks-bound-product-paths.md b/docs/adr/SADR-0016-locks-bound-product-paths.md\nindex b5e850f3f1f7f4ef6e7f9d1763a29a51bd300c45..46b40a9b99a7aae644c02bb458f5473ff0a90ce2\n--- a/docs/adr/0016-locks-bound-product-paths.md\n+++ b/docs/adr/SADR-0016-locks-bound-product-paths.md\n@@ -1,4 +1,4 @@\n-# ADR 0016: Locks bound product paths; the Shiki mirror is bound by record identity\n+# SADR-0016: Locks bound product paths; the Shiki mirror is bound by record identity\n \n ## Status\n \n@@ -6,8 +6,8 @@\n \n - Date: 2026-07-28\n - Deciders: operator (mizutani-140), Claude (planner)\n-- Related: ADR 0004 (GitHub-first with a `.shiki` mirror), ADR 0009 (Spec Freeze),\n-  ADR 0012 (autonomous loop closeout to main), ADR 0015 (Contract Approval before\n+- Related: SADR-0004 (GitHub-first with a `.shiki` mirror), SADR-0009 (Spec Freeze),\n+  SADR-0012 (autonomous loop closeout to main), SADR-0015 (Contract Approval before\n   dispatch), remediation goal task R-02, the live PR #179 deadlock.\n \n ## Context\n@@ -90,11 +90,11 @@\n files, documentation files and named source modules are nameable; runtime-\n allocated ledger, runner and report ids are not \u2014 and those live in `.shiki`,\n which locks no longer govern. Existing frozen contracts are brought into line by\n-a one-time Spec Amendment per plan, which must land before ADR 0015's contract\n+a one-time Spec Amendment per plan, which must land before SADR-0015's contract\n immutability makes `locks` unwritable from the implementation PR.\n \n **Lock state is resolved from the durable side.** MergeGate resolves the lock\n-registry, and ADR 0015's governance fields, from the base `.shiki` snapshot when\n+registry, and SADR-0015's governance fields, from the base `.shiki` snapshot when\n present, falling back to head only when absent. The `MergeGate metadata check`\n job is given `--base-shiki`, which it does not receive today. Locks are not\n derived from the live open-pull-request set: a pull request body is untrusted\n@@ -108,7 +108,7 @@\n Explicitly out of scope: moving MergeGate's contract resolution off the PR\n checkout wholesale; making the mirror a projection of GitHub state; introducing\n new lock kinds; moving ledger evidence out of the repository; and unwinding\n-ADR 0012's closeout pull request. Registration of multi-task goals is ADR 0015's\n+SADR-0012's closeout pull request. Registration of multi-task goals is SADR-0015's\n Contract PR and is not re-decided here.\n \n ## Consequences\n@@ -127,7 +127,7 @@\n \n Risks accepted. First, goal-scoped identity is only as strong as `goal_id`,\n which is resolved from the PR body and the head task file; the mitigation is to\n-add `goal_id` to ADR 0015's frozen governance field set, and until that lands a\n+add `goal_id` to SADR-0015's frozen governance field set, and until that lands a\n task file can name a foreign goal. Second, base-when-present resolution is\n unavailable on fork pull requests, where the head is contributor-controlled;\n fork pull requests already cannot complete a Shiki merge, and this decision does\n@@ -139,7 +139,7 @@\n path, not by implementation pull requests.\n \n New obligations: one Spec Amendment per frozen plan to rewrite `locks`, ordered\n-before ADR 0015 T1; extension of the adversarial suites in\n+before SADR-0015 T1; extension of the adversarial suites in\n `scripts/test_shiki_adversarial_state.sh` and\n `scripts/test_shiki_governance_evidence.sh` to cover the eleven newly-scoped\n directories; and updating `scripts/test_shiki_loop_lock_guard.sh`,\n@@ -151,32 +151,32 @@\n whether the loop's transitive sync of ledger `evidence` references can be\n deleted once the Contract PR carries its own source plan.\n \n-## Relationship to ADR 0004\n+## Relationship to SADR-0004\n \n-This ADR **clarifies something ADR 0004 left unspecified**. ADR 0004 decides that\n+This SADR **clarifies something SADR-0004 left unspecified**. SADR-0004 decides that\n GitHub is the operational source of truth and that each target repository keeps a\n `.shiki/` mirror. It says nothing about pull requests writing the mirror, does\n not name MergeGate, and does not make the mirror an input to a gate. Nothing in\n-ADR 0004's Decision or Consequences is amended, superseded or weakened: the\n+SADR-0004's Decision or Consequences is amended, superseded or weakened: the\n mirror still exists, still records the same artifacts, and pull request\n-boundaries remain the coordination surface. What this ADR settles is the question\n+boundaries remain the coordination surface. What this SADR settles is the question\n 0004 never reached \u2014 by what authority a pull request may write a mirror record \u2014\n and it answers it the way `.shiki/manifest.json` already declares\n (`pr_mutation: current-task-or-current-goal-only`).\n \n-It **amends ADR 0012**, whose Decision item 5 states that \"Loop-executed tasks\n+It **amends SADR-0012**, whose Decision item 5 states that \"Loop-executed tasks\n declare the `path:.shiki/**` lock, which covers every `.shiki` file the closeout\n stages ... so `files_outside_locks` is satisfied without per-file lock\n bookkeeping.\" That clause is replaced: the closeout's mirror writes are\n-authorized by record identity, not by a declared lock. ADR 0012's substantive\n+authorized by record identity, not by a declared lock. SADR-0012's substantive\n decision \u2014 that completion reaches `main` through a closeout pull request rather\n than local attestation \u2014 is preserved unchanged.\n \n-It is **complementary to ADR 0015**, not a substitute. ADR 0015 owns contract\n+It is **complementary to SADR-0015**, not a substitute. SADR-0015 owns contract\n immutability, the pre-dispatch gate, the Contract PR and the Scope Envelope. This\n-ADR owns what a lock means and who may write the mirror. The one ordering\n+SADR owns what a lock means and who may write the mirror. The one ordering\n constraint between them is that the lock-narrowing Spec Amendment must precede\n-ADR 0015 T1's freezing of the `locks` field.\n+SADR-0015 T1's freezing of the `locks` field.\n \n ## Alternatives Considered\n \n@@ -186,7 +186,7 @@\n the only writer that can register them may run. Migrating the Guardian signal to\n that store makes every registration PR fail closed to `needs_guardian` at every\n risk level, because the task file is by definition absent from the store, which\n-removes the low/medium autonomous path ADR 0008/0009 exists to provide. And it\n+removes the low/medium autonomous path SADR-0008/0009 exists to provide. And it\n leaves schemas, manifest, config, guardian policy, templates, migrations, plans,\n worktrees, runner records, runs and handoffs with no writer at all in a\n repository whose product is `.shiki`. Measured benefit on the frozen backlog:\n@@ -199,14 +199,14 @@\n namespace is not in fact ownership-partitioned \u2014 56 ledger entries carry no\n `task_id`, and one goal DAG is writable by 35 registered tasks. The correct\n expression of its insight is that locks do not govern the mirror at all, which is\n-what this ADR decides.\n+what this SADR decides.\n \n **Make the mirror a projection written only by a registration writer and a\n post-merge projection job.** Rejected. It requires a bypass actor with write\n access to the protected default branch; it makes its own master invariant\n readable from a manifest that pull requests may edit; it deletes both reconcile\n modes and leaves a corrupted mirror with no in-band repair path, contradicting\n-ADR 0004's consequence that mirror conflicts are repaired against GitHub state;\n+SADR-0004's consequence that mirror conflicts are repaired against GitHub state;\n and its content-derived ids are rejected by `validate_shiki`'s currently-green id\n pattern.\n \ndiff --git a/docs/adr/0017-bookkeeping-closeouts-do-not-inherit-risk.md b/docs/adr/SADR-0017-bookkeeping-closeouts-do-not-inherit-risk.md\nindex 554e3a0d526a72ca067108c5464cc68c3edcf174..b9676857a4713850e063af7c39c8e0b01ecb4c66\n--- a/docs/adr/0017-bookkeeping-closeouts-do-not-inherit-risk.md\n+++ b/docs/adr/SADR-0017-bookkeeping-closeouts-do-not-inherit-risk.md\n@@ -1,4 +1,4 @@\n-# ADR 0017: A Bookkeeping Closeout Does Not Inherit Its Task's Risk\n+# SADR-0017: A Bookkeeping Closeout Does Not Inherit Its Task's Risk\n \n ## Status\n \n@@ -6,7 +6,7 @@\n \n ## Context\n \n-ADR 0012 made the goal loop push a task's completion to the default branch through a\n+SADR-0012 made the goal loop push a task's completion to the default branch through a\n closeout pull request: after the implementation PR merges, the loop opens a second PR\n carrying `task.status = done`, `lock.state = released`, and \u2014 when the task completes\n its goal \u2014 `goal.status = complete` plus one scorecard report. Nothing else. The\n@@ -49,14 +49,14 @@\n 4. The task file's governance fields (`scope`, `non_goals`, `required_skills`,\n    `risk_level`, `locks`, `acceptance_checks`, `test_command`) are byte-identical to\n    the base snapshot. Only `status`, `expected_pr`, `closeout_pr`, `ledger_evidence`\n-   and `expected_branch` may differ \u2014 the ADR 0015 contract-immutability rule,\n+   and `expected_branch` may differ \u2014 the SADR-0015 contract-immutability rule,\n    unchanged.\n \n    `expected_branch` is in that set because a closeout is *by construction* on a\n    different branch from the implementation it closes, and the loop rewrites the\n    field accordingly. It is safe to let it differ because MergeGate independently\n    requires `expected_branch` to equal the PR's actual head ref, so a PR cannot set\n-   it to anything other than the branch it is really on. The first draft of this ADR\n+   it to anything other than the branch it is really on. The first draft of this SADR\n    omitted it, which made the exemption unreachable: every real closeout failed\n    condition 4 on a field the loop always changes. See PR #209.\n 5. The state transitions are exactly the terminal set: task `review` \u2192 `done`, lock\n@@ -113,12 +113,12 @@\n classification must be derived, not asserted.\n \n **Fold the closeout into the implementation PR** so no second PR exists. Rejected under\n-ADR 0012, which recorded why: a task file that is already `done` on the implementation\n+SADR-0012, which recorded why: a task file that is already `done` on the implementation\n branch short-circuits the loop's review \u2192 CCA \u2192 merge \u2192 done ordering into a local\n goal-completion before any merge happens.\n \n **Have the loop auto-merge closeouts with elevated credentials.** Rejected outright: that\n-is a runtime authoring its own approval, which ADR 0010 and the constitution's\n+is a runtime authoring its own approval, which SADR-0010 and the constitution's\n approval-record prohibition forbid. It would also be indistinguishable, in the evidence,\n from the forgery those rules exist to prevent.\n \ndiff --git a/docs/adr/0018-carried-guardian-approval-across-a-proven-base-sync.md b/docs/adr/SADR-0018-carried-guardian-approval-across-a-proven-base-sync.md\nindex 22358562da390ed8b45cd600da08a43d378b4680..e6e2907cefa776460af73ddf7d39db605f3c39b0\n--- a/docs/adr/0018-carried-guardian-approval-across-a-proven-base-sync.md\n+++ b/docs/adr/SADR-0018-carried-guardian-approval-across-a-proven-base-sync.md\n@@ -1,4 +1,4 @@\n-# ADR 0018: Carried Guardian Approval Across A Proven Base Sync\n+# SADR-0018: Carried Guardian Approval Across A Proven Base Sync\n \n ## Status\n \n@@ -34,7 +34,7 @@\n Guardian evaluator that consumes it (`shiki_guardian.evaluate_guardian_approval`,\n the `guardian_comment_carried` source) were built and tested in isolation. Neither\n does anything until a caller passes the flags that turn the carry on, so the\n-feature was invisible in CI. This ADR records the decision the two of them\n+feature was invisible in CI. This SADR records the decision the two of them\n implement, and this task wires it into the four CI invocations that judge Guardian\n approval.\n \ndiff --git a/docs/adr/README.md b/docs/adr/README.md\nindex c70e057a4520c36e042ab68c53cd4a21ef709b71..5da43856836e2ce6a150f8127766e2f88355ab4b\n--- a/docs/adr/README.md\n+++ b/docs/adr/README.md\n@@ -1,40 +1,41 @@\n-# Architecture Decision Records\n+# Decision Record Namespaces\n \n-This directory holds Shiki's Architecture Decision Records (ADRs): durable\n-records of hard-to-reverse platform decisions. ADRs are part of the Shiki source\n-of truth alongside GitHub state, the `.shiki/` mirror, and `CONTEXT.md`.\n+This directory holds two independent decision namespaces:\n \n-## How ADRs Work\n+- Shiki Architecture Decision Records (SADRs), named\n+  `SADR-NNNN-<short-slug>.md`, record hard-to-reverse Shiki platform decisions.\n+- Target Architecture Decision Records (ADRs), named\n+  `NNNN-<short-slug>.md`, belong to the target product.\n \n-- New ADRs are added as `docs/adr/NNNN-<short-slug>.md`, where `NNNN` is the next\n-  zero-padded sequence number.\n-- The **canonical list of ADRs is the set of files matching `docs/adr/*.md`** in\n-  this directory. Browse `docs/adr/*.md` for the authoritative, current set.\n-- Use [`template.md`](template.md) as the starting point for a new ADR.\n-- Record an ADR when a decision is hard to reverse, surprising without context,\n-  and a real tradeoff. Do not write an ADR for routine or easily reversible\n-  choices.\n+The same four-digit number may appear once in each namespace. A platform\n+`SADR-NNNN` citation resolves only to `SADR-NNNN-*.md`; a target `ADR NNNN`\n+citation resolves only to `NNNN-*.md`.\n \n-Using the `docs/adr/*.md` glob as the source of truth means other PRs can add an\n-ADR without editing this index file, avoiding merge contention on a\n-hand-maintained enumeration.\n+## How Decision Records Work\n \n-## Current ADRs (snapshot)\n-\n-The list below is a convenience snapshot and may lag behind the directory. When\n-in doubt, list `docs/adr/*.md`.\n+- New Shiki platform decisions use the next zero-padded SADR number.\n+- New target product decisions use the target's own ADR sequence.\n+- The canonical list is the files in `docs/adr/`; no hand-maintained index is\n+  authoritative.\n+- Use [`template.md`](template.md) as the starting point and choose the filename\n+  namespace that owns the decision.\n+- Record a decision when it is hard to reverse, surprising without context, and\n+  a real tradeoff. Do not write a decision record for routine or easily\n+  reversible choices.\n \n-- [0001 \u2014 Keep Shiki As The Platform Name](0001-keep-shiki-as-platform-name.md)\n-- [0002 \u2014 Use AGENTS As The Shared Agent Constitution](0002-use-agents-as-shared-constitution.md)\n-- [0003 \u2014 Require Skill Gate For MergeGate Readiness](0003-require-skill-gate-for-mergegate-readiness.md)\n-- [0004 \u2014 Use GitHub First With A Repository-Local Shiki Mirror](0004-use-github-first-with-shiki-mirror.md)\n-- [0005 \u2014 Use Codex As Front And Claude Code Action As The GitHub Runtime](0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md)\n-- [0006 \u2014 Separate Codex Implementation, CCA Completion, And MergeGate Authorization](0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md)\n+Using the directory as the source of truth lets independent changes add records\n+without contending on an enumerated index.\n \n-## Adding A New ADR\n+## Adding A Shiki SADR\n \n-1. Copy `template.md` to `docs/adr/NNNN-<short-slug>.md`.\n+1. Copy `template.md` to `docs/adr/SADR-NNNN-<short-slug>.md`.\n 2. Fill in the title, status, context, decision, and consequences.\n-3. Set the status to `Proposed`, then `Accepted` once the decision is approved.\n-4. Open a PR following `CONTRIBUTING.md`. You do not need to edit this index for\n-   the ADR to be discoverable via `docs/adr/*.md`.\n+3. Set the status to `Proposed`, then `Accepted` once the proper authority has\n+   approved the decision.\n+4. Open a PR following `CONTRIBUTING.md`.\n+\n+## Adding A Target ADR\n+\n+1. Copy `template.md` to `docs/adr/NNNN-<short-slug>.md` using the target's own\n+   sequence.\n+2. Cite it as `ADR NNNN`; do not prefix its filename with `SADR-`.\ndiff --git a/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md b/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\nnew file mode 100644\nindex 0000000000000000000000000000000000000000..7455ce1f3bed02ddddb851ac21a1d1a648c9aeba\n--- /dev/null\n+++ b/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\n@@ -0,0 +1,55 @@\n+# SADR-0019: Separate Shiki Platform SADRs From Target ADRs\n+\n+## Status\n+\n+Accepted\n+\n+## Context\n+\n+Shiki installs its platform decision records into `docs/adr/` in every target\n+repository. Shiki and its targets previously used the same numeric\n+`NNNN-<slug>.md` namespace. A target that already owned ADRs 0013 through 0018\n+therefore acquired two records for each number during installation, making an\n+`ADR NNNN` citation ambiguous. Renumbering target decisions would rewrite target\n+history, while renumbering Shiki decisions per target would destroy their stable\n+platform identity.\n+\n+Upgrades also need to remove the former Shiki-owned numeric paths without ever\n+mistaking a target-owned ADR for platform content. The install stamp already\n+records exact shipped paths and content digests, so it can prove ownership only\n+when both still match.\n+\n+## Decision\n+\n+Shiki platform decisions use the `SADR` namespace:\n+\n+- records are named `docs/adr/SADR-NNNN-<slug>.md`;\n+- live platform citations use `SADR-NNNN`;\n+- target product decisions retain `docs/adr/NNNN-<slug>.md` and `ADR NNNN`;\n+- the reference validator resolves the two namespaces independently, so the same\n+  four-digit number may exist once in each namespace.\n+\n+Before a forced upgrade writes anything, the installer inventories the exact\n+former numeric Shiki ADR paths. It may delete a legacy path only when the\n+existing install stamp contains that exact path and its recorded SHA-256 digest\n+matches the file currently on disk. All ownership blockers are reported\n+together and abort the upgrade before its first write. A non-forced install\n+never performs this cleanup.\n+\n+Historical `.shiki/` evidence remains unchanged because it records the namespace\n+that existed when that evidence was produced.\n+\n+## Consequences\n+\n+- Targets may use any ADR number without colliding with Shiki's platform SADRs.\n+- Fresh installs preserve target ADR bytes and install a stable SADR set.\n+- Forced upgrades fail closed when legacy ownership cannot be proven, and a\n+  successful upgrade refreshes the stamp without removed legacy paths.\n+- Current governance, packaging, workflow, prompt, script, and skill references\n+  must move with the platform records.\n+\n+## Alternatives Considered\n+\n+- Collision-only refusal leaves affected targets permanently unupgradable.\n+- Renumbering target ADRs mutates target-owned history.\n+- Per-target Shiki renumbering makes platform citations non-portable.\ndiff --git a/docs/adr/template.md b/docs/adr/template.md\nindex a92a97daddd0b00ab9dd626cc8d86942da2eafed..17dd79175b5fee2105935557f691b40cf9492942\n--- a/docs/adr/template.md\n+++ b/docs/adr/template.md\n@@ -1,14 +1,14 @@\n-# NNNN: <Short Decision Title>\n+# <SADR-NNNN or NNNN>: <Short Decision Title>\n \n ## Status\n \n-Proposed | Accepted | Superseded by ADR-NNNN\n+Proposed | Accepted | Superseded by SADR-NNNN or target ADR NNNN\n \n ## Context\n \n What is the situation, constraint, or force that requires a decision? Reference\n-relevant domain terms (`CONTEXT.md`), prior ADRs, and code reality. State the\n-problem, not the solution.\n+relevant domain terms (`CONTEXT.md`), prior Shiki SADRs or target ADRs, and code\n+reality. State the problem, not the solution.\n \n ## Decision\n \ndiff --git a/docs/agents/autonomous-loop-e2e-acceptance.md b/docs/agents/autonomous-loop-e2e-acceptance.md\nindex bbf84c6081987f159ac6ad9ecf3c82b7aa72e7e8..5c13c1d02a05a90b0dc46cbef7c95a9846d0c237\n--- a/docs/agents/autonomous-loop-e2e-acceptance.md\n+++ b/docs/agents/autonomous-loop-e2e-acceptance.md\n@@ -40,8 +40,8 @@\n 3. `gh auth status` is logged in with merge permission on the repo.\n 4. Branch protection + required checks are configured exactly as the goal-loop\n    required-check set expects (see `docs/agents/branch-protection-smoke.md`).\n-5. The repo `.shiki/policy` permits autonomous low/medium-risk merges (ADR\n-   0008/0009); the MergeGate policy check is the recorded-authority gate.\n+5. The repo `.shiki/policy` permits autonomous low/medium-risk merges\n+   (SADR-0008/0009); the MergeGate policy check is the recorded-authority gate.\n \n The operator's only actions are: register the task and start the loop. **No\n manual pre-clearing** means after `shiki loop run` starts, the operator does not\ndiff --git a/docs/agents/bootstrap-command.md b/docs/agents/bootstrap-command.md\nindex 05f7f203a52cf412b9f97084a6e131ed0f785afd..f7aa0b734dfebee4a3b139335b66ee3415f48713\n--- a/docs/agents/bootstrap-command.md\n+++ b/docs/agents/bootstrap-command.md\n@@ -189,7 +189,9 @@\n ```\n \n Use this only for tests, fixtures, or explicit local-only template inspection.\n-Use `--force` only when you intentionally want to overwrite existing target files.\n+Use `--force` only when you intentionally want to refresh the target template.\n+Legacy numeric Shiki decision cleanup is stamp-and-digest authorized before any\n+write; target ADR paths are not cleanup candidates.\n \n ## Slash Command\n \ndiff --git a/docs/agents/checklists.md b/docs/agents/checklists.md\nindex 4a7f03ade346f53a1c1d82981f3fd744f9ab4547..0c5ec6c53dc7197b7b7a7d18e667b325babd6c45\n--- a/docs/agents/checklists.md\n+++ b/docs/agents/checklists.md\n@@ -32,18 +32,18 @@\n | ID | Item | Blocking |\n | --- | --- | --- |\n | GD-01 | Plan was challenged against `CONTEXT.md`. | blocking for non-trivial Goals |\n-| GD-02 | Relevant ADRs were read or absence was recorded. | blocking for non-trivial Goals |\n+| GD-02 | Relevant SADRs and target ADRs were read or absence was recorded. | blocking for non-trivial Goals |\n | GD-03 | Ambiguous terms were resolved or converted to HITL blockers. | blocking |\n | GD-04 | Edge scenarios were considered. | advisory |\n | GD-05 | Code reality was checked when code could answer. | blocking when applicable |\n | GD-06 | New domain terms were added to `CONTEXT.md` when needed. | blocking when applicable |\n-| GD-07 | ADR was created or explicitly skipped using the ADR criteria. | blocking when applicable |\n+| GD-07 | A SADR or target ADR was created or explicitly skipped using the decision-record criteria. | blocking when applicable |\n \n ## CI \u2014 Context & Impact Checklist\n \n | ID | Item | Blocking |\n | --- | --- | --- |\n-| CI-01 | Relevant docs and ADRs identified. | blocking |\n+| CI-01 | Relevant docs, SADRs, and target ADRs identified. | blocking |\n | CI-02 | Relevant modules, interfaces, seams, callers, and tests identified. | blocking |\n | CI-03 | Dependencies and blocked-by relationships identified. | blocking |\n | CI-04 | Candidate locks identified. | blocking |\n@@ -62,7 +62,7 @@\n | PRD-04 | Implementation decisions are recorded without volatile code detail. | blocking |\n | PRD-05 | Testing decisions are recorded. | blocking |\n | PRD-06 | Out-of-scope items are explicit. | blocking |\n-| PRD-07 | Relevant glossary/ADR links are present when applicable. | blocking |\n+| PRD-07 | Relevant glossary/SADR/target-ADR links are present when applicable. | blocking |\n \n ## SF \u2014 Spec Freeze Checklist\n \n@@ -98,7 +98,7 @@\n | H-02 | Branch/worktree target is present. | blocking |\n | H-03 | Scope and non-goals are present. | blocking |\n | H-04 | Dependencies and locks are present. | blocking |\n-| H-05 | Relevant docs, ADRs, modules, and tests are referenced. | blocking |\n+| H-05 | Relevant docs, SADRs, target ADRs, modules, and tests are referenced. | blocking |\n | H-06 | Required skills are stated. | blocking |\n | H-07 | TDD expectations are stated. | blocking when behavior work |\n | H-08 | Acceptance criteria are included. | blocking |\ndiff --git a/docs/agents/codex-handoff.md b/docs/agents/codex-handoff.md\nindex ede468f47cb277f69a857e8c45df703e4ed0151d..6f97bdffb67e44d7edea2c71e01ea28933c4eb6c\n--- a/docs/agents/codex-handoff.md\n+++ b/docs/agents/codex-handoff.md\n@@ -46,7 +46,7 @@\n \n ## Context\n - Domain terms:\n-- Relevant ADRs:\n+- Relevant SADRs and target ADRs:\n - Relevant docs:\n - Relevant modules/interfaces/seams:\n - Similar tests/prior art:\ndiff --git a/docs/agents/completion-check-agent.md b/docs/agents/completion-check-agent.md\nindex 13e4c035db35025f4e3cdcc5adfb24314ac4d521..61bfa1218cf36ca605250c91919e620f33238aa0\n--- a/docs/agents/completion-check-agent.md\n+++ b/docs/agents/completion-check-agent.md\n@@ -32,7 +32,7 @@\n - `AGENTS.md`\n - `CLAUDE.md`\n - `CONTEXT.md`\n-- relevant ADRs\n+- relevant SADRs and target ADRs\n - parent Goal issue\n - PRD issue or document\n - task issue\ndiff --git a/docs/agents/decision-control.md b/docs/agents/decision-control.md\nindex 7d8ee5ab03a5ccbcbd04056ae61de03785a2a9c9..8c94ecda00fffaa0e92c325ab83aa9776bd1dac1\n--- a/docs/agents/decision-control.md\n+++ b/docs/agents/decision-control.md\n@@ -11,7 +11,7 @@\n | Decision | Owner | When | Required inputs | Output | Enforcement surface |\n | --- | --- | --- | --- | --- | --- |\n | Is the Goal clear enough to plan? | Planner + Guardian when needed | Goal intake | User request, existing docs, risk signals | Goal readiness verdict | Goal checklist, issue template, ledger |\n-| Must `grill-with-docs` run? | Planner / Skill Gate | Before PRD or issue split | Ambiguity, domain terms, ADR conflicts, unclear boundaries | Skill invocation or explicit skip rationale | Skill Gate checklist |\n+| Must `grill-with-docs` run? | Planner / Skill Gate | Before PRD or issue split | Ambiguity, domain terms, SADR/target-ADR conflicts, unclear boundaries | Skill invocation or explicit skip rationale | Skill Gate checklist |\n | Is a PRD required? | Planner | After Goal context stabilizes | Goal, `grill-with-docs` output, Context & Impact output | PRD required / not required | PRD checklist, CCA evidence check |\n | Can the spec be frozen? | Operator only | End of Requirements Definition, before issue split | PRD/requirements summary, scope inventory (SF-02), open-question list | `spec_freeze` block in the plan + ledger evidence | `require_grilled_plan` (plan ingest/run), SF checklist |\n | Can a frozen spec change? | Operator only (Spec Amendment) | When implementation contradicts the frozen spec | Contested decisions, scoped re-grill result | recorded amendment + re-stamped freeze | SF checklist, ledger evidence |\ndiff --git a/docs/agents/domain.md b/docs/agents/domain.md\nindex 258da8ea81e87ac0a6b0b5b5e5c2fde980eaabb7..d57b0cf17ff3e1fa9e6e5885372bf083d8295421\n--- a/docs/agents/domain.md\n+++ b/docs/agents/domain.md\n@@ -5,7 +5,9 @@\n ## Layout\n \n - `CONTEXT.md` defines Shiki language. Keep it implementation-free.\n-- `docs/adr/` records hard-to-reverse decisions.\n+- `docs/adr/SADR-NNNN-*.md` records Shiki platform decisions.\n+- `docs/adr/NNNN-*.md` records target product decisions without sharing the\n+  SADR namespace.\n - `AGENTS.md` is the runtime-neutral agent constitution.\n - `CLAUDE.md` is the Claude Code wrapper around `AGENTS.md`.\n - `.shiki/` is the Target Repository mirror for Goals, PRDs, plans, Task DAGs, contracts, locks, worktree records, ledger entries, CCA verdicts, repair packets, reports, and handoffs.\n@@ -15,6 +17,6 @@\n \n - Use Shiki terms exactly when they exist.\n - Add glossary terms to `CONTEXT.md` only when the term is part of the domain, not a generic programming concept.\n-- Create ADRs only for hard-to-reverse, surprising, tradeoff-based decisions.\n+- Create SADRs or target ADRs only for hard-to-reverse, surprising, tradeoff-based decisions.\n - Keep product-specific language in the Target Repository, not in Shiki Template docs.\n-- If an ADR conflicts with a proposed implementation, surface the conflict before editing.\n+- If a SADR or target ADR conflicts with a proposed implementation, surface the conflict before editing.\ndiff --git a/docs/agents/execution-control.md b/docs/agents/execution-control.md\nindex ad8ee3cdb36be56fedc1f46408ca92b387d74047..bc63c3a73661d12b567016ba299f1a6b66bf9088\n--- a/docs/agents/execution-control.md\n+++ b/docs/agents/execution-control.md\n@@ -63,7 +63,7 @@\n | Branch name | Orchestrator | Orchestrator | Before implementer dispatch | branch policy | branch ref in task/ledger |\n | Lock reservation | Context & Impact | MergeGate | Before dispatch and before merge | `.shiki/locks/`, labels, PR metadata | lock id, owner, scope |\n | CI profile | Planner | MergeGate | During issue triage, rechecked at PR | labels, `.shiki/policy.yaml`, workflow matrix | required check list |\n-| Required status checks | Repo maintainer / Guardian | GitHub branch protection / rulesets | Repository setup, updated via ADR | GitHub settings | branch/ruleset config, ADR |\n+| Required status checks | Repo maintainer / Guardian | GitHub branch protection / rulesets | Repository setup, updated via target ADR | GitHub settings | branch/ruleset config, target ADR |\n | CCA judgment | CCA | GitHub required check / MergeGate | Every non-draft PR update | GitHub Action check | structured CCA verdict |\n | Repair assignment | CCA | MergeGate | After failed CCA/CI/review | repair packet, issue/PR comment | repair id and attempt count |\n | Merge readiness | MergeGate | GitHub branch protection / Guardian if needed | After CCA complete | required checks, review rules, rulesets | mergegate check output |\ndiff --git a/docs/agents/guardian-policy.md b/docs/agents/guardian-policy.md\nindex 4147c7adf05a47adb4fd24ada1a1c2e1f2ba6d2d..a20d9cc6c00a569bb057b9ca8d6b51c7fc4a9847\n--- a/docs/agents/guardian-policy.md\n+++ b/docs/agents/guardian-policy.md\n@@ -98,8 +98,8 @@\n `origin/<default_branch>` and reuses each job's existing base fetch; a shallow\n (`--depth`) or second base fetch would make the proof unresolvable and silently\n disable the carry, so neither is permitted. CCA reads `guardian_comment_carried` as\n-a recorded authority in the deterministic signal and does not re-derive it. See ADR\n-0018 for the full decision, the rejected diff/patch-equality and path-disjointness\n+a recorded authority in the deterministic signal and does not re-derive it. See\n+SADR-0018 for the full decision, the rejected diff/patch-equality and path-disjointness\n alternatives, and the accepted residual.\n \n ## External AI Guardian Review (SADR-0010)\ndiff --git a/docs/agents/implementation-policy.md b/docs/agents/implementation-policy.md\nindex c754144a9fb5bf4a38aa82a60e1670c59e626b44..85c37281bcad196c6b7b02343faf00b03cd9c57a\n--- a/docs/agents/implementation-policy.md\n+++ b/docs/agents/implementation-policy.md\n@@ -31,10 +31,13 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n ## Non-Negotiables\n \n 1. **Planning precedes implementation.** Non-trivial Goals start with `grill-with-docs` and Context & Impact.\n-2. **Domain language is controlled.** Terms must be checked against `CONTEXT.md`; hard-to-reverse tradeoffs must be checked against ADRs.\n+2. **Domain language is controlled.** Terms must be checked against `CONTEXT.md`; hard-to-reverse tradeoffs must be checked against Shiki SADRs and target ADRs.\n 3. **PRD precedes issue decomposition.** `to-prd` is used once the context is settled enough to become durable intent.\n 4. **Issues are vertical slices.** `to-issues` must produce independently grabbable tracer-bullet tasks, not horizontal layer tickets.\n 5. **Triage controls dispatch.** Only `ready-for-agent` / AFK tasks may be assigned to Codex without human input.\n@@ -95,19 +98,19 @@\n Process:\n \n - Challenge terms against `CONTEXT.md`.\n-- Check ADRs before reopening decisions.\n+- Check SADRs and target ADRs before reopening decisions.\n - Inspect code when code can answer a question.\n - Ask one question at a time when the operator is available.\n - Provide a recommended answer with each question.\n - Record resolved terms in `CONTEXT.md`.\n-- Create ADRs only for hard-to-reverse, surprising, tradeoff-based decisions.\n+- Create SADRs or target ADRs only for hard-to-reverse, surprising, tradeoff-based decisions.\n - Convert unresolved load-bearing questions into HITL blockers.\n \n Outputs:\n \n - Resolved design-tree decisions.\n - Updated glossary terms when needed.\n-- ADRs when justified.\n+- SADRs or target ADRs when justified.\n - HITL blockers when decisions remain unresolved.\n \n ## Phase 3 \u2014 Context & Impact\n@@ -117,7 +120,7 @@\n \n Outputs must include:\n \n-- Relevant documents and ADRs.\n+- Relevant documents, SADRs, and target ADRs.\n - Relevant modules, interfaces, seams, callers, and tests.\n - Candidate locks.\n - Dependencies.\n@@ -140,7 +143,7 @@\n - Testing decisions.\n - Out of scope.\n - Further notes.\n-- Links to relevant Goal, domain terms, and ADRs.\n+- Links to relevant Goal, domain terms, SADRs, and target ADRs.\n \n Phase 4 ends with Spec Freeze: the operator explicitly approves the PRD, the\n plan records a `spec_freeze` block (status, approved_by, source), and the SF\n@@ -240,7 +243,7 @@\n - Dependencies and locks.\n - Risk and Guardian requirements.\n - Ledger completeness.\n-- Documentation and ADR updates.\n+- Documentation and SADR/target-ADR updates.\n - Scope drift.\n \n CCA emits one of:\n@@ -301,7 +304,7 @@\n | Evidence failure | Behavior might be done, but proof is missing. | Add checks, PR evidence, ledger entry, or artifacts. |\n | Scope drift | PR changes unrelated behavior. | Revert unrelated changes or split into new Goal. |\n | Dependency block | Upstream task or lock not complete. | Block until dependency resolves. |\n-| Architecture block | Decision needs ADR or deep-module discussion. | `grill-with-docs` or `improve-codebase-architecture`. |\n+| Architecture block | Decision needs a SADR, target ADR, or deep-module discussion. | `grill-with-docs` or `improve-codebase-architecture`. |\n | Risk block | Security/production/policy change needs human approval. | Guardian decision. |\n \n ## Phase 5.5 \u2014 Execution Decision Control\ndiff --git a/docs/agents/runtime-auth-model.md b/docs/agents/runtime-auth-model.md\nindex 6a2e1a47e81ea89b5c68a210ec07772a9374babc..3c54748d24233dbd0ba8473950282547ef4ce935\n--- a/docs/agents/runtime-auth-model.md\n+++ b/docs/agents/runtime-auth-model.md\n@@ -148,7 +148,7 @@\n \n API-key based runners may be added by a target repository as an explicit extension. They are not the default Shiki template path.\n \n-If a repo opts into API-key mode, record the exception in an ADR and update the repo's `AGENTS.md`, `.shiki/config.yaml`, and workflow permissions.\n+If a repo opts into API-key mode, record the target-specific exception in a target ADR and update the repo's `AGENTS.md`, `.shiki/config.yaml`, and workflow permissions.\n \n ## GitHub Actions Runtime Compatibility\n \ndiff --git a/docs/agents/skill-gate.md b/docs/agents/skill-gate.md\nindex c50398b0d7c60a8598f2a5c88a10015730dced13..3acffe6444617bb1d54b77d4b4708158ec735bc6\n--- a/docs/agents/skill-gate.md\n+++ b/docs/agents/skill-gate.md\n@@ -9,7 +9,7 @@\n | Skill | Trigger | Expected output |\n | --- | --- | --- |\n | `setup-matt-pocock-skills` | First configuring a repo for these skills, or when issue tracker, triage labels, or domain docs are missing. | `docs/agents/` configuration and an `Agent skills` block in `AGENTS.md` or `CLAUDE.md`. |\n-| `grill-with-docs` | Ambiguous plan, unclear terminology, boundary decision, tradeoff, or ADR-worthy design choice. | Clarified decision tree, recommended answers, updates to `CONTEXT.md` or ADRs when decisions crystallize. |\n+| `grill-with-docs` | Ambiguous plan, unclear terminology, boundary decision, tradeoff, or decision-record-worthy design choice. | Clarified decision tree, recommended answers, updates to `CONTEXT.md`, SADRs, or target ADRs when decisions crystallize. |\n | `zoom-out` | Agent lacks a higher-level architectural map. | Map of relevant modules, callers, seams, and domain concepts. |\n | `to-prd` | Settled context should become a PRD. | PRD published to the issue tracker with problem, solution, user stories, decisions, testing, and out-of-scope notes. |\n | `to-issues` | Goal, PRD, or plan must become implementation tickets. | Vertical-slice issues with acceptance criteria, dependencies, and AFK/HITL classification. |\n@@ -17,7 +17,7 @@\n | `tdd` | Feature work or bug fix has observable behavior that can be specified with tests. | Red-green-refactor loop through public interfaces, one vertical slice at a time. |\n | `code-review` | The TDD loop is green and the implementer is about to create or update a PR. Default for every implementation task. | Pre-PR self-review of the task diff with fixes applied on the task branch; findings and resolutions recorded as a ledger entry naming the skill and a `## Pre-PR code review` PR body section. |\n | `diagnose` | Hard bug, failing check, regression, flaky behavior, or performance problem. | Reproduction loop, ranked hypotheses, instrumentation, fix, regression test, post-mortem. |\n-| `improve-codebase-architecture` | Structural friction, testability, AI-navigability, deep-module opportunity, or refactor request. | Architecture candidates and deepening recommendations informed by `CONTEXT.md` and ADRs. |\n+| `improve-codebase-architecture` | Structural friction, testability, AI-navigability, deep-module opportunity, or refactor request. | Architecture candidates and deepening recommendations informed by `CONTEXT.md`, SADRs, and target ADRs. |\n | `prototype` | A throwaway logic or UI prototype is needed to answer a design question. | Clearly marked disposable prototype and captured decision. |\n \n ## MergeGate Interaction\ndiff --git a/docs/operations.md b/docs/operations.md\nindex cf1f452d4c589e38ccd58553bda0ad8684b51eb7..ace48a90ffbec9e5c0a7113bef16a4858bc44b3f\n--- a/docs/operations.md\n+++ b/docs/operations.md\n@@ -89,6 +89,16 @@\n      target history, brought forward by `shiki migrate`, not a template.\n    - **Everything else** (CLI scripts, workflows, schemas, docs) is overwritten.\n \n+   A forced upgrade also migrates the former numeric Shiki decision paths to\n+   `SADR-NNNN-*`. Before the first write, the installer inventories the exact\n+   legacy Shiki paths and authorizes each deletion only when the existing\n+   `.shiki/install-stamp.json` names that path and its SHA-256 digest still\n+   matches the file. An absent, unreadable, malformed, incomplete, or\n+   mismatching stamp reports every blocked path and stops with no filesystem\n+   change. Target-owned `NNNN-*` ADRs are not cleanup candidates. A non-force\n+   install deletes nothing and retains any still-matching legacy stamp entries\n+   so a later forced upgrade can prove ownership.\n+\n    If the target has pending migrations, `--force` refuses and writes nothing \u2014\n    apply migrations first (step 3), then re-run. The run ends with a single\n    summary of every `.new` written, so a half-upgrade is visible.\ndiff --git a/docs/releasing.md b/docs/releasing.md\nindex 10d73828e7dbd64e9d9f6b83bc84d010537ce62a..aafeee8c4d441e1e427efee2ce4f81a647f020e1\n--- a/docs/releasing.md\n+++ b/docs/releasing.md\n@@ -67,7 +67,7 @@\n CLI in `scripts/`. There are no third-party Python runtime dependencies. The CLI\n is intentionally **not** exposed as a `[project.scripts]` console entry point\n because `scripts/shiki.py` is a dependency-free shim and `scripts/` is not\n-packaged as an importable distribution (see the ADR for the rationale).\n+packaged as an importable distribution (see SADR-0007 for the rationale).\n \n Supported install paths:\n \n@@ -155,6 +155,15 @@\n    [docs/operations.md](operations.md#the-new-convention). If the target has\n    pending migrations, `--force` refuses and writes nothing; apply migrations\n    (step 2) first, then re-run.\n+\n+   The SADR namespace upgrade has an additional pre-write gate: each former\n+   numeric Shiki decision path is removed only when the existing install stamp\n+   contains that exact path and a digest matching its current bytes. The\n+   installer aggregates absent/unreadable/malformed-stamp, missing-digest, and\n+   digest-mismatch blockers before raising, so refusal leaves the target tree\n+   byte-identical. Numeric target ADRs are preserved. A successful forced\n+   upgrade installs and stamps `SADR-*` paths and drops the removed legacy paths\n+   from the refreshed stamp.\n 2. Apply repository-local `.shiki/` state migrations with the migrate command:\n \n    ```bash\ndiff --git a/docs/usage.md b/docs/usage.md\nindex b83407ee3970dd395dd893010bc5dbe8bad26051..a130ef6e40dfd8169a5bc701339351097df1a09f\n--- a/docs/usage.md\n+++ b/docs/usage.md\n@@ -67,8 +67,10 @@\n shiki install-target /path/to/target-repo --local-only\n ```\n \n-Use `--force` only when you intentionally want to overwrite existing target\n-files.\n+Use `--force` only when you intentionally want to refresh the target template.\n+For the SADR namespace migration, `--force` deletes only exact legacy Shiki\n+decision paths whose existing install-stamp digest still matches; any ownership\n+blocker stops before the first write. Numeric target ADRs are never candidates.\n \n ## `shiki bootstrap-platform` \u2014 publish the Shiki platform repo\n \ndiff --git a/scripts/guardian_approval_signal.py b/scripts/guardian_approval_signal.py\nindex d059f92b2c82aa2b5d6ca421365537a87886c1cb..16924892714f4f5b9303c0ef0e325a7e70075630\n--- a/scripts/guardian_approval_signal.py\n+++ b/scripts/guardian_approval_signal.py\n@@ -199,8 +199,8 @@\n ) -> Any:\n     \"\"\"Evaluate SADR-0015 Contract Approval for the CCA Guardian signal.\n \n-    The SAME OR that MergeGate applies must reach this deterministic signal: ADR\n-    0015 is explicit that without it, a contract-approved PR still returns\n+    The SAME OR that MergeGate applies must reach this deterministic signal:\n+    SADR-0015 is explicit that without it, a contract-approved PR still returns\n     ``needs_guardian``, the CCA job fails, and the MergeGate policy check never\n     runs \u2014 the gate this change moves would never be reached. This resolves the\n     task the SAME way MergeGate does (the first Shiki task id in the PR body),\ndiff --git a/scripts/shiki_contract_approval.py b/scripts/shiki_contract_approval.py\nindex 941bcb96fdd8b9a85bc0c4f0f04d5dad9d04839c..4a4b418f6527413161db82670bec857a043360e2\n--- a/scripts/shiki_contract_approval.py\n+++ b/scripts/shiki_contract_approval.py\n@@ -142,7 +142,7 @@\n     changed_files_status: Any,\n     registration: Any,\n ) -> ContractApprovalResult:\n-    \"\"\"Whether this PR carries an SADR-0015 Contract Approval.\n+    \"\"\"Whether this PR carries a SADR-0015 Contract Approval.\n \n     The four conditions are evaluated IN ORDER, each failure recorded as a\n     distinct reason string:\ndiff --git a/scripts/shiki_guardian.py b/scripts/shiki_guardian.py\nindex a4ecb79bbda55fd5c8b06adfae2eb99760a03c45..4dd99bbe714e26f421fa19abac7a34ae1b195ecc\n--- a/scripts/shiki_guardian.py\n+++ b/scripts/shiki_guardian.py\n@@ -12,7 +12,7 @@\n # The carry re-derives its proof from git in-process on every judging invocation;\n # it is never read from an artifact, so there is nothing to go stale or be forged.\n # shiki_sync_proof is the ONLY guardian-family module that runs git, and every one\n-# of its invocations is read-only and local (ADR: prove-from-git base sync).\n+# of its invocations is read-only and local (SADR: prove-from-git base sync).\n from shiki_sync_proof import verify_pure_base_sync\n \n GUARDIAN_POLICY_PATH = \".shiki/guardian-policy.json\"\ndiff --git a/scripts/shiki_guardian_review.py b/scripts/shiki_guardian_review.py\nindex dd0f104d457899e0ba5ff1e162011c1e90b6f92b..e10ef58c046324e7ea3a3392feb3048ca77414ce\n--- a/scripts/shiki_guardian_review.py\n+++ b/scripts/shiki_guardian_review.py\n@@ -159,7 +159,7 @@\n     docs_only = _is_docs_only(changed_files)\n     if docs_only and matched == 0:\n         focus.append(\n-            \"Docs/ADR-only change: confirm the change is genuinely documentation/decision-only, introduces no behavior or contract drift, and stays consistent with the constitution and existing ADRs.\"\n+            \"Docs/decision-record-only change: confirm the change is genuinely documentation/decision-only, introduces no behavior or contract drift, and stays consistent with the constitution and existing SADRs and target ADRs.\"\n         )\n \n     # Conservative augmentation: unknown (no category and not docs-only), mixed\ndiff --git a/scripts/shiki_installer.py b/scripts/shiki_installer.py\nindex be064ab7a29ff6836f7f7a1d70500a43e0d11d7b..9fd8eefd137ce7003dad5c0dcb2e48e2cd47ab32\n--- a/scripts/shiki_installer.py\n+++ b/scripts/shiki_installer.py\n@@ -118,6 +118,7 @@\n # what lets a target carry it without a governance-schema change.\n INSTALL_STAMP_PATH = \".shiki/install-stamp.json\"\n INSTALL_STAMP_VERSION = 1\n+LEGACY_SHIKI_SADR_MAX = 18\n \n # Under --force the shipped surface splits three ways, so nothing is lost and\n # nothing is silently stale.\n@@ -237,9 +238,98 @@\n         )\n \n \n+def legacy_shiki_adr_paths() -> tuple[str, ...]:\n+    \"\"\"Former numeric paths for Shiki SADR-0001 through SADR-0018.\n+\n+    The authoritative slugs come from the current SADR filenames. SADR-0019 is\n+    the namespace decision itself and never shipped under a numeric legacy path.\n+    \"\"\"\n+    paths: list[str] = []\n+    for sadr in sorted((ROOT / \"docs\" / \"adr\").glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\")):\n+        number = sadr.name.split(\"-\", 2)[1]\n+        if int(number) <= LEGACY_SHIKI_SADR_MAX:\n+            paths.append(f\"docs/adr/{sadr.name.removeprefix('SADR-')}\")\n+    return tuple(paths)\n+\n+\n+def preflight_legacy_sadr_cleanup(target: Path, *, force: bool) -> tuple[str, ...]:\n+    \"\"\"Authorize exact legacy Shiki ADR removals before the first write.\n+\n+    A non-force install never removes existing files. A forced install needs no\n+    stamp when no legacy Shiki path is present. When legacy paths do exist, each\n+    must be named in a readable install stamp and its current bytes must match\n+    the recorded digest.\n+    \"\"\"\n+    if not force:\n+        return ()\n+    present = tuple(\n+        relative\n+        for relative in legacy_shiki_adr_paths()\n+        if (target / relative).exists() or (target / relative).is_symlink()\n+    )\n+    if not present:\n+        return ()\n+\n+    stamp_path = target / INSTALL_STAMP_PATH\n+    try:\n+        raw = stamp_path.read_text(encoding=\"utf-8\")\n+        stamp = json.loads(raw)\n+    except FileNotFoundError:\n+        detail = \"install stamp is absent\"\n+        blockers = [f\"{relative}: {detail}\" for relative in present]\n+    except (OSError, UnicodeDecodeError) as error:\n+        detail = f\"install stamp is unreadable ({error})\"\n+        blockers = [f\"{relative}: {detail}\" for relative in present]\n+    except json.JSONDecodeError as error:\n+        detail = f\"install stamp is malformed ({error})\"\n+        blockers = [f\"{relative}: {detail}\" for relative in present]\n+    else:\n+        if not isinstance(stamp, dict):\n+            blockers = [\n+                f\"{relative}: install stamp is malformed (expected an object)\"\n+                for relative in present\n+            ]\n+        else:\n+            digests = stamp.get(\"digests\")\n+            blockers = []\n+            if not isinstance(digests, dict):\n+                blockers.extend(\n+                    f\"{relative}: install stamp has no digest for this path\"\n+                    for relative in present\n+                )\n+            else:\n+                for relative in present:\n+                    expected = digests.get(relative)\n+                    if not isinstance(expected, str) or not expected:\n+                        blockers.append(\n+                            f\"{relative}: install stamp has no digest for this path\"\n+                        )\n+                        continue\n+                    try:\n+                        actual = _sha256_file(target / relative)\n+                    except OSError as error:\n+                        blockers.append(\n+                            f\"{relative}: legacy path is unreadable ({error})\"\n+                        )\n+                        continue\n+                    if actual != expected:\n+                        blockers.append(\n+                            f\"{relative}: digest mismatch (stamp {expected}, current {actual})\"\n+                        )\n+\n+    if blockers:\n+        joined = \"\\n\".join(f\"- {blocker}\" for blocker in blockers)\n+        raise ShikiError(\n+            \"legacy Shiki ADR cleanup is not authorized:\\n\"\n+            f\"{joined}\\nNo files were written.\"\n+        )\n+    return present\n+\n+\n def install_template(target: Path, *, force: bool, validate: bool) -> list[NewFileNote]:\n     # Preconditions first: never rewrite the tree of a target that cannot be\n     # upgraded cleanly (see refuse_pending_migrations).\n+    legacy_cleanup = preflight_legacy_sadr_cleanup(target, force=force)\n     refuse_pending_migrations(target)\n \n     # Resolve the target's required CODEOWNERS owner once from its\n@@ -250,6 +340,10 @@\n     # verbatim. See ``codeowners_required_owner``.\n     codeowners_owner = codeowners_required_owner(target)\n \n+    for relative in legacy_cleanup:\n+        (target / relative).unlink()\n+        info(f\"removed stamped legacy Shiki ADR: {target / relative}\")\n+\n     notes: list[NewFileNote] = []\n     for relative in TEMPLATE_PATHS:\n         source = ROOT / relative\n@@ -647,6 +741,22 @@\n     digests = target_stamp_digests(target)\n     existing = read_install_stamp(target)\n     if existing is not None:\n+        # A non-force install deliberately leaves legacy Shiki ADRs in place.\n+        # Preserve only still-matching ownership proof so a later forced\n+        # upgrade can authorize cleanup; never carry a missing or stale digest.\n+        existing_digests = existing.get(\"digests\")\n+        if isinstance(existing_digests, dict):\n+            for relative in legacy_shiki_adr_paths():\n+                expected = existing_digests.get(relative)\n+                path = target / relative\n+                if not isinstance(expected, str) or not path.is_file():\n+                    continue\n+                try:\n+                    actual = _sha256_file(path)\n+                except OSError:\n+                    continue\n+                if actual == expected:\n+                    digests[relative] = actual\n         if _incoming_is_older(commit, existing.get(\"platform_commit\")):\n             warn(\n                 \"kept existing install stamp: incoming install \"\ndiff --git a/scripts/shiki_loop.py b/scripts/shiki_loop.py\nindex 3611641da8aeb5038b1da90e3114b09a473ec6d8..84caa491b9cad0edc4176f209c13e9221e975681\n--- a/scripts/shiki_loop.py\n+++ b/scripts/shiki_loop.py\n@@ -424,7 +424,7 @@\n         # unresolvable CCA verdict has already produced its terminal stop and is never\n         # laundered into a head-moving sync (SADR-0010). It also never fires while the\n         # policy (Guardian) gate is red \u2014 for ANY reason, not only when it is the sole\n-        # red check \u2014 so a head-bound Guardian approval, or an SADR-0015\n+        # red check \u2014 so a head-bound Guardian approval, or a SADR-0015\n         # contract-approval carry that base movement defeated (the policy gate compares\n         # base and head task governance directly), is never silently restored by\n         # adopting base wholesale. It is bounded by a durable per-task counter so the\ndiff --git a/scripts/shiki_tasks.py b/scripts/shiki_tasks.py\nindex aec3809f24b4a0c8c96fc50971cff9ae9ad30381..d531052e9d9717058d4631483cefab44c26a65c1\n--- a/scripts/shiki_tasks.py\n+++ b/scripts/shiki_tasks.py\n@@ -821,7 +821,7 @@\n         \"prompt\": prompt,\n         \"entry_skill\": \"grill-with-docs\",\n         \"required_next_steps\": [\n-            \"Run grill-with-docs until terminology, boundaries, risks, and ADR-worthy decisions are settled.\",\n+            \"Run grill-with-docs until terminology, boundaries, risks, and decision-record-worthy choices are settled.\",\n             \"Obtain the operator's explicit approval of the PRD/requirements (Spec Freeze).\",\n             \"Write a machine-readable plan JSON with grill_with_docs.status=complete and spec_freeze.status=frozen.\",\n             \"Run shiki plan ingest --plan-file PLAN.json.\",\ndiff --git a/scripts/test_shiki_goal_loop.sh b/scripts/test_shiki_goal_loop.sh\nindex 753c7b516e10f0be3bc92a4bd0eb1fa699776ae6..8f3820a0a079b3044424cfe1a6c6b7fe68896e67\n--- a/scripts/test_shiki_goal_loop.sh\n+++ b/scripts/test_shiki_goal_loop.sh\n@@ -231,8 +231,8 @@\n \n # High/critical risk merges autonomously when all required checks are green:\n # the \"MergeGate policy check\" required check IS the Guardian gate (it enforces\n-# guardian-policy.json \u2014 human approval OR external AI guardian review, ADR\n-# 0010), so green checks mean a recorded authority approved. The fake gh returns\n+# guardian-policy.json \u2014 human approval OR external AI guardian review,\n+# SADR-0010), so green checks mean a recorded authority approved. The fake gh returns\n # green checks, simulating that the Guardian gate passed.\n python3 \"$ROOT/scripts/shiki.py\" goal create --target \"$TARGET\" --title \"Guardian gate\" --outcome \"High risk merges via policy gate\" >/tmp/shiki-goal-loop-guardian-goal.json\n GGOAL=\"$(json_get /tmp/shiki-goal-loop-guardian-goal.json goal_id)\"\ndiff --git a/tests/test_adr_references.py b/tests/test_adr_references.py\nindex 51706329c797d5dd9b47e082a333fa83b261d482..d005da4f771f5e8085ec22d4b787980e68f5ae8c\n--- a/tests/test_adr_references.py\n+++ b/tests/test_adr_references.py\n@@ -1,8 +1,8 @@\n-\"\"\"Every ``ADR ####`` citation must resolve to exactly one decision record.\n+\"\"\"Every target ADR and Shiki SADR citation resolves in its own namespace.\n \n Background. ``scripts/mergegate_check.py`` justifies ``is_bookkeeping_closeout``\n-with \"ADR 0017\" nine times and ``scripts/guardian_approval_signal.py`` cites\n-0016/0017 seven times, yet for a stretch the numbering in ``docs/adr/`` stopped\n+with \"SADR-0017\" nine times and ``scripts/guardian_approval_signal.py`` cites\n+SADR-0016/SADR-0017 seven times, yet for a stretch the numbering in ``docs/adr/`` stopped\n at 0015: the authoring commits lived on a coordinator branch and no task ever\n declared ``docs/adr/...`` in its locks, so nothing carried the records to the\n default branch. The stated basis of an exemption that merged seven PRs with zero\n@@ -11,11 +11,12 @@\n and the directory drifted with no signal.\n \n This is the same unbound-pair shape behind other silent failures: a reference\n-and its target with nothing asserting they agree. This test binds them. It scans\n-every ``scripts/*.py`` (plus ``docs/**/*.md`` and the root ``*.md`` governance\n-files, which are cheap) for the ``ADR ####`` pattern and asserts each cited\n-number resolves to exactly one ``docs/adr/####-*.md``. The converse is\n-deliberately NOT required: an ADR may exist without being cited.\n+and its target with nothing asserting they agree. This test binds them. Target\n+``ADR NNNN`` citations resolve only to ``docs/adr/NNNN-*.md``; Shiki\n+``SADR-NNNN`` citations resolve only to ``docs/adr/SADR-NNNN-*.md``. The scan\n+covers current governance docs, scripts, workflows, prompts, packaging metadata,\n+and repo-local Codex guidance while deliberately excluding historical\n+``.shiki`` evidence. The converse is not required: a record may be uncited.\n \n The suite is dependency free: standard-library ``unittest`` only.\n \"\"\"\n@@ -31,17 +32,18 @@\n \n REPO_ROOT = Path(__file__).resolve().parents[1]\n \n-# \"ADR 0016\" -- a four-digit citation. The trailing \\b keeps a five-digit token\n-# from being read as a truncated four-digit number.\n-_CITATION = re.compile(r\"\\bADR (\\d{4})\\b\")\n-# \"0016-locks-bound-product-paths.md\" -- the leading four digits name the record.\n-_ADR_FILENAME = re.compile(r\"^(\\d{4})-.*\\.md$\")\n+# ``ADR 0016`` / ``SADR-0016`` -- a namespace plus four-digit citation. The\n+# trailing ``\\b`` keeps a five-digit token from being read as a truncated one.\n+_CITATION = re.compile(r\"\\b(S?ADR)[ -](\\d{4})\\b\")\n+# Target ADRs retain the numeric filename; Shiki platform decisions use SADR.\n+_DECISION_FILENAME = re.compile(r\"^(?:(SADR)-)?(\\d{4})-.*\\.md$\")\n \n \n class Violation(NamedTuple):\n     \"\"\"A citation that does not resolve to exactly one decision record.\"\"\"\n \n     citing_file: str  # repo-relative path of the file that cites the ADR\n+    namespace: str  # \"ADR\" for target decisions; \"SADR\" for Shiki decisions\n     number: str  # the cited four-digit number, e.g. \"0016\"\n     line: int  # 1-based line of the citation\n     reason: str  # \"missing\" (zero matches) or \"ambiguous\" (two or more)\n@@ -49,44 +51,59 @@\n \n     @property\n     def expected_path(self) -> str:\n-        return f\"docs/adr/{self.number}-*.md\"\n+        prefix = \"SADR-\" if self.namespace == \"SADR\" else \"\"\n+        return f\"docs/adr/{prefix}{self.number}-*.md\"\n \n     def describe(self) -> str:\n         \"\"\"Name the citing file, the cited number, and the missing path.\"\"\"\n         if self.reason == \"missing\":\n             return (\n-                f\"{self.citing_file}:{self.line} cites ADR {self.number} \"\n+                f\"{self.citing_file}:{self.line} cites {self.namespace} {self.number} \"\n                 f\"but no document matches {self.expected_path}\"\n             )\n         joined = \", \".join(self.matches)\n         return (\n-            f\"{self.citing_file}:{self.line} cites ADR {self.number} which \"\n+            f\"{self.citing_file}:{self.line} cites {self.namespace} {self.number} which \"\n             f\"resolves to {len(self.matches)} documents (expected exactly one \"\n             f\"at {self.expected_path}): {joined}\"\n         )\n \n \n-def _adr_index(root: Path) -> dict[str, list[Path]]:\n-    \"\"\"Map every four-digit ADR number to the record files that claim it.\"\"\"\n-    index: dict[str, list[Path]] = {}\n+def _adr_index(root: Path) -> dict[tuple[str, str], list[Path]]:\n+    \"\"\"Map each decision namespace and number to the files that claim it.\"\"\"\n+    index: dict[tuple[str, str], list[Path]] = {}\n     adr_dir = root / \"docs\" / \"adr\"\n     if adr_dir.is_dir():\n         for path in sorted(adr_dir.glob(\"*.md\")):\n-            match = _ADR_FILENAME.match(path.name)\n+            match = _DECISION_FILENAME.match(path.name)\n             if match:\n-                index.setdefault(match.group(1), []).append(path)\n+                namespace = \"SADR\" if match.group(1) else \"ADR\"\n+                index.setdefault((namespace, match.group(2)), []).append(path)\n     return index\n \n \n def _citing_files(root: Path, include_docs: bool = True) -> list[Path]:\n-    \"\"\"Files scanned for citations: scripts/*.py, and cheaply the markdown.\"\"\"\n+    \"\"\"Current, shipped text surfaces scanned for decision citations.\"\"\"\n     files = sorted((root / \"scripts\").glob(\"*.py\"))\n+    files += sorted((root / \"scripts\").glob(\"*.sh\"))\n     if include_docs:\n         docs = root / \"docs\"\n         if docs.is_dir():\n             files += sorted(docs.rglob(\"*.md\"))\n         files += sorted(root.glob(\"*.md\"))\n-    return files\n+        pyproject = root / \"pyproject.toml\"\n+        if pyproject.is_file():\n+            files.append(pyproject)\n+        for relative in (\".github\", \".codex\"):\n+            surface = root / relative\n+            if surface.is_dir():\n+                files += sorted(\n+                    path\n+                    for path in surface.rglob(\"*\")\n+                    if path.is_file()\n+                    and path.suffix in {\".md\", \".py\", \".sh\", \".toml\", \".yaml\", \".yml\"}\n+                )\n+    return sorted(set(files))\n \n \n def find_adr_reference_violations(\n@@ -94,10 +111,9 @@\n ) -> list[Violation]:\n     \"\"\"Return every ``ADR ####`` citation under ``root`` that fails to resolve.\n \n-    A citation resolves when exactly one ``docs/adr/####-*.md`` matches its\n-    number. Zero matches is a \"missing\" violation; two or more is \"ambiguous\".\n-    Records that are never cited produce nothing -- the converse is not\n-    required.\n+    A citation resolves when exactly one record in the same namespace matches\n+    its number. Zero matches is a \"missing\" violation; two or more is\n+    \"ambiguous\". Records that are never cited produce nothing.\n     \"\"\"\n     root = Path(root)\n     index = _adr_index(root)\n@@ -110,13 +126,15 @@\n         rel = path.relative_to(root).as_posix()\n         for lineno, line in enumerate(text.splitlines(), start=1):\n             for match in _CITATION.finditer(line):\n-                number = match.group(1)\n-                found = index.get(number, [])\n+                namespace, number = match.groups()\n+                found = index.get((namespace, number), [])\n                 if len(found) == 1:\n                     continue\n                 reason = \"missing\" if not found else \"ambiguous\"\n                 rels = [p.relative_to(root).as_posix() for p in found]\n-                violations.append(Violation(rel, number, lineno, reason, rels))\n+                violations.append(\n+                    Violation(rel, namespace, number, lineno, reason, rels)\n+                )\n     return violations\n \n \n@@ -129,8 +147,8 @@\n         self.assertEqual(\n             violations,\n             [],\n-            \"Every 'ADR ####' citation must resolve to exactly one \"\n-            f\"docs/adr/####-*.md:\\n{detail}\",\n+            \"Every ADR/SADR citation must resolve exactly once in its own \"\n+            f\"docs/adr namespace:\\n{detail}\",\n         )\n \n     def test_scan_actually_reads_scripts(self) -> None:\n@@ -142,12 +160,38 @@\n             \"the citation scan must cover scripts/*.py\",\n         )\n \n+    def test_platform_citations_do_not_use_unresolved_numeric_shorthand(self) -> None:\n+        shorthand = re.compile(r\"SADR-\\d{4}/\\d{4}\")\n+        offenders = []\n+        for path in _citing_files(REPO_ROOT):\n+            text = path.read_text(encoding=\"utf-8\")\n+            if shorthand.search(text):\n+                offenders.append(path.relative_to(REPO_ROOT).as_posix())\n+        self.assertEqual(\n+            offenders,\n+            [],\n+            \"repeat the SADR prefix so every cited number is independently resolvable\",\n+        )\n+\n     def test_carried_records_are_indexed(self) -> None:\n         # The two records this Goal carried onto the default branch, and which\n         # the scripts cite most, must be present.\n         index = _adr_index(REPO_ROOT)\n-        self.assertIn(\"0016\", index)\n-        self.assertIn(\"0017\", index)\n+        self.assertIn((\"SADR\", \"0016\"), index)\n+        self.assertIn((\"SADR\", \"0017\"), index)\n+\n+    def test_platform_tree_has_only_sadr_0001_through_0019(self) -> None:\n+        adr_dir = REPO_ROOT / \"docs\" / \"adr\"\n+        sadrs = sorted(adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\"))\n+        self.assertEqual(\n+            {path.name.split(\"-\", 2)[1] for path in sadrs},\n+            {f\"{number:04d}\" for number in range(1, 20)},\n+        )\n+        self.assertEqual(\n+            list(adr_dir.glob(\"[0-9][0-9][0-9][0-9]-*.md\")),\n+            [],\n+            \"the Shiki platform tree must not retain legacy numeric SADR paths\",\n+        )\n \n \n class FixtureAdrReferences(unittest.TestCase):\n@@ -180,6 +224,26 @@\n         self.assertIn(\"9991\", message)  # cited number\n         self.assertIn(\"docs/adr/9991-*.md\", message)  # missing path\n \n+    def test_target_adr_does_not_satisfy_missing_platform_sadr(self) -> None:\n+        root = self._make_repo(\n+            adr_names=[\"0042-target-decision.md\"],\n+            script_body='\"\"\"Target ADR 0042 exists, but platform SADR-0042 does not.\"\"\"\\n',\n+        )\n+        violations = find_adr_reference_violations(root, include_docs=False)\n+        self.assertEqual(len(violations), 1)\n+        self.assertEqual(violations[0].namespace, \"SADR\")\n+        self.assertEqual(violations[0].number, \"0042\")\n+        self.assertEqual(violations[0].expected_path, \"docs/adr/SADR-0042-*.md\")\n+\n+    def test_target_adr_and_platform_sadr_may_reuse_a_number(self) -> None:\n+        root = self._make_repo(\n+            adr_names=[\"0042-target-decision.md\", \"SADR-0042-platform-decision.md\"],\n+            script_body='\"\"\"Target ADR 0042 and platform SADR-0042 are distinct.\"\"\"\\n',\n+        )\n+        self.assertEqual(\n+            find_adr_reference_violations(root, include_docs=False), []\n+        )\n+\n     def test_uncited_adr_is_allowed(self) -> None:\n         # An ADR exists but nothing cites it -> not a violation. The converse\n         # (every ADR must be cited) is explicitly NOT required.\ndiff --git a/tests/test_guardian_review.py b/tests/test_guardian_review.py\nindex 88829e7810212635e2fc98aa5918d6f26294677d..00a85a237acd5bf2d6cb5b294128198c422402ca\n--- a/tests/test_guardian_review.py\n+++ b/tests/test_guardian_review.py\n@@ -162,7 +162,7 @@\n \n     def test_docs_only_is_light_and_not_conservative(self):\n         focus = gr.classify_review_focus([\"docs/adr/0099-x.md\", \"README.md\"], \"\", \"low\")\n-        self.assertTrue(any(\"Docs/ADR-only\" in f for f in focus))\n+        self.assertTrue(any(\"Docs/decision-record-only\" in f for f in focus))\n         self.assertFalse(any(\"conservative default\" in f.lower() for f in focus))\n \n     def test_unknown_category_is_conservative(self):\ndiff --git a/tests/test_policy_job_merged_prs.py b/tests/test_policy_job_merged_prs.py\nindex d45512e74681e5e649279236c7d4e79d5c3da471..b97989f8bd74d091de1a6b5275a20d8dc68c4512\n--- a/tests/test_policy_job_merged_prs.py\n+++ b/tests/test_policy_job_merged_prs.py\n@@ -3,8 +3,8 @@\n \n ``.github/workflows/shiki-cca-completion.yml`` has two consumers of the merged-PR\n proof: the CCA *signal* (``guardian_approval_signal.py`` in \"Prepare CCA context\")\n-and the authoritative gate (``mergegate_check.py`` in \"Enforce MergeGate\"). ADR\n-0017's bookkeeping-closeout exemption (condition 6) needs the merged implementation\n+and the authoritative gate (``mergegate_check.py`` in \"Enforce MergeGate\").\n+SADR-0017's bookkeeping-closeout exemption (condition 6) needs the merged implementation\n PR proven, and a closeout carries NO ``post_merge_reconcile`` marker. The signal\n already resolves ``MERGED_PRS`` unconditionally, but \"Enforce MergeGate\" used to\n resolve it only inside ``grep -q '<!-- shiki:post_merge_reconcile -->'``. So a\ndiff --git a/tests/test_sadr_install_migration.py b/tests/test_sadr_install_migration.py\nnew file mode 100644\nindex 0000000000000000000000000000000000000000..d51eac872b1c79ff4579c4c1401ca5d775a60d8d\n--- /dev/null\n+++ b/tests/test_sadr_install_migration.py\n@@ -0,0 +1,237 @@\n+\"\"\"SADR installation and collision-safe legacy upgrade behavior.\"\"\"\n+\n+from __future__ import annotations\n+\n+import contextlib\n+import hashlib\n+import io\n+import json\n+import shutil\n+import tempfile\n+import unittest\n+from pathlib import Path\n+\n+import shiki_test_support  # noqa: F401  (path bootstrap)\n+\n+import shiki_installer\n+from tests.test_adr_references import find_adr_reference_violations\n+\n+\n+PRODUCT_NUMBERS = tuple(range(13, 19))\n+\n+\n+def _quiet_install(target: Path, *, force: bool, validate: bool) -> None:\n+    with contextlib.redirect_stdout(io.StringIO()):\n+        shiki_installer.install_template(target, force=force, validate=validate)\n+\n+\n+def _sha256(path: Path) -> str:\n+    return hashlib.sha256(path.read_bytes()).hexdigest()\n+\n+\n+def _legacy_sadr_paths() -> tuple[str, ...]:\n+    return tuple(\n+        f\"docs/adr/{path.name.removeprefix('SADR-')}\"\n+        for path in sorted((shiki_installer.ROOT / \"docs\" / \"adr\").glob(\"SADR-*.md\"))\n+        if path.name != \"SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\"\n+    )\n+\n+\n+def _create_legacy_upgrade_fixture(\n+    target: Path,\n+) -> tuple[dict[str, bytes], tuple[str, ...]]:\n+    adr_dir = target / \"docs\" / \"adr\"\n+    adr_dir.mkdir(parents=True)\n+    product_bytes = {\n+        f\"{number:04d}-cubeshare-product-decision.md\": (\n+            f\"# CubeShare ADR {number:04d}\\n\\nDo not rewrite {number}.\\n\"\n+        ).encode()\n+        for number in PRODUCT_NUMBERS\n+    }\n+    for name, content in product_bytes.items():\n+        (adr_dir / name).write_bytes(content)\n+\n+    legacy_paths = _legacy_sadr_paths()\n+    for relative in legacy_paths:\n+        (target / relative).write_bytes(f\"legacy Shiki bytes for {relative}\\n\".encode())\n+\n+    stamp_path = target / shiki_installer.INSTALL_STAMP_PATH\n+    stamp_path.parent.mkdir(parents=True, exist_ok=True)\n+    stamp = {\n+        \"version\": shiki_installer.INSTALL_STAMP_VERSION,\n+        \"platform_commit\": None,\n+        \"installed_at\": \"2000-01-01T00:00:00+00:00\",\n+        \"digests\": {\n+            relative: _sha256(target / relative) for relative in legacy_paths\n+        },\n+    }\n+    stamp_path.write_text(\n+        json.dumps(stamp, indent=2, sort_keys=True) + \"\\n\", encoding=\"utf-8\"\n+    )\n+    return product_bytes, legacy_paths\n+\n+\n+def _tree_snapshot(root: Path) -> dict[str, tuple[str, bytes | str]]:\n+    \"\"\"Recursive byte/shape snapshot used to prove preflight is write-free.\"\"\"\n+    snapshot: dict[str, tuple[str, bytes | str]] = {}\n+    for path in sorted(root.rglob(\"*\")):\n+        relative = path.relative_to(root).as_posix()\n+        if path.is_symlink():\n+            snapshot[relative] = (\"symlink\", path.readlink().as_posix())\n+        elif path.is_dir():\n+            snapshot[relative] = (\"directory\", \"\")\n+        else:\n+            snapshot[relative] = (\"file\", path.read_bytes())\n+    return snapshot\n+\n+\n+class FreshSadrInstallTests(unittest.TestCase):\n+    def setUp(self) -> None:\n+        self.tmp = Path(tempfile.mkdtemp(prefix=\"shiki-sadr-fresh-\"))\n+        self.addCleanup(lambda: shutil.rmtree(self.tmp, ignore_errors=True))\n+        self.target = self.tmp / \"target\"\n+        self.adr_dir = self.target / \"docs\" / \"adr\"\n+        self.adr_dir.mkdir(parents=True)\n+        self.product_bytes = {\n+            f\"{number:04d}-product-decision.md\": (\n+                f\"# Product ADR {number:04d}\\n\\nTarget-owned bytes {number}.\\n\"\n+            ).encode()\n+            for number in PRODUCT_NUMBERS\n+        }\n+        for name, content in self.product_bytes.items():\n+            (self.adr_dir / name).write_bytes(content)\n+\n+    def test_fresh_install_preserves_product_adrs_and_installs_all_sadrs(self) -> None:\n+        _quiet_install(self.target, force=False, validate=True)\n+\n+        for name, expected in self.product_bytes.items():\n+            self.assertEqual((self.adr_dir / name).read_bytes(), expected)\n+\n+        sadr_numbers = {\n+            path.name.split(\"-\", 2)[1]\n+            for path in self.adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\")\n+        }\n+        self.assertEqual(sadr_numbers, {f\"{number:04d}\" for number in range(1, 20)})\n+\n+        numeric_records = {\n+            path.name\n+            for path in self.adr_dir.glob(\"[0-9][0-9][0-9][0-9]-*.md\")\n+        }\n+        self.assertEqual(numeric_records, set(self.product_bytes))\n+        stamp = json.loads(\n+            (self.target / shiki_installer.INSTALL_STAMP_PATH).read_text(\n+                encoding=\"utf-8\"\n+            )\n+        )\n+        for path in self.adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\"):\n+            relative = path.relative_to(self.target).as_posix()\n+            self.assertEqual(stamp[\"digests\"][relative], _sha256(path))\n+        self.assertEqual(find_adr_reference_violations(self.target), [])\n+\n+\n+class LegacySadrUpgradeTests(unittest.TestCase):\n+    def setUp(self) -> None:\n+        self.tmp = Path(tempfile.mkdtemp(prefix=\"shiki-sadr-upgrade-\"))\n+        self.addCleanup(lambda: shutil.rmtree(self.tmp, ignore_errors=True))\n+        self.target = self.tmp / \"target\"\n+        self.adr_dir = self.target / \"docs\" / \"adr\"\n+        self.product_bytes, self.legacy_paths = _create_legacy_upgrade_fixture(\n+            self.target\n+        )\n+\n+    def test_force_upgrade_deletes_only_stamped_legacy_sadrs(self) -> None:\n+        _quiet_install(self.target, force=True, validate=True)\n+\n+        for relative in self.legacy_paths:\n+            self.assertFalse((self.target / relative).exists(), relative)\n+        for name, expected in self.product_bytes.items():\n+            self.assertEqual((self.adr_dir / name).read_bytes(), expected)\n+\n+        sadrs = list(self.adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\"))\n+        self.assertEqual(len(sadrs), 19)\n+        refreshed = json.loads(\n+            (self.target / shiki_installer.INSTALL_STAMP_PATH).read_text(encoding=\"utf-8\")\n+        )\n+        for relative in self.legacy_paths:\n+            self.assertNotIn(relative, refreshed[\"digests\"])\n+        for path in sadrs:\n+            relative = path.relative_to(self.target).as_posix()\n+            self.assertEqual(refreshed[\"digests\"][relative], _sha256(path))\n+\n+    def test_non_force_install_never_deletes_legacy_files(self) -> None:\n+        before = {\n+            relative: (self.target / relative).read_bytes()\n+            for relative in self.legacy_paths\n+        }\n+        _quiet_install(self.target, force=False, validate=False)\n+        for relative, expected in before.items():\n+            self.assertEqual((self.target / relative).read_bytes(), expected)\n+        refreshed = json.loads(\n+            (self.target / shiki_installer.INSTALL_STAMP_PATH).read_text(encoding=\"utf-8\")\n+        )\n+        for relative in self.legacy_paths:\n+            self.assertEqual(refreshed[\"digests\"][relative], _sha256(self.target / relative))\n+\n+\n+class LegacyCleanupPreflightTests(unittest.TestCase):\n+    def setUp(self) -> None:\n+        self.tmp = Path(tempfile.mkdtemp(prefix=\"shiki-sadr-preflight-\"))\n+        self.addCleanup(lambda: shutil.rmtree(self.tmp, ignore_errors=True))\n+\n+    def test_all_ownership_blockers_abort_before_any_write(self) -> None:\n+        cases = (\n+            (\"absent\", \"install stamp is absent\"),\n+            (\"unreadable\", \"install stamp is unreadable\"),\n+            (\"malformed\", \"install stamp is malformed\"),\n+            (\"missing-digest\", \"install stamp has no digest\"),\n+            (\"digest-mismatch\", \"digest mismatch\"),\n+        )\n+        for name, expected_reason in cases:\n+            with self.subTest(name=name):\n+                target = self.tmp / name\n+                _, legacy_paths = _create_legacy_upgrade_fixture(target)\n+                stamp_path = target / shiki_installer.INSTALL_STAMP_PATH\n+\n+                if name == \"absent\":\n+                    stamp_path.unlink()\n+                elif name == \"unreadable\":\n+                    stamp_path.unlink()\n+                    stamp_path.mkdir()\n+                elif name == \"malformed\":\n+                    stamp_path.write_text(\"{not-json\\n\", encoding=\"utf-8\")\n+                else:\n+                    stamp = json.loads(stamp_path.read_text(encoding=\"utf-8\"))\n+                    if name == \"missing-digest\":\n+                        stamp[\"digests\"] = {}\n+                    else:\n+                        stamp[\"digests\"] = {\n+                            relative: \"deadbeef\" for relative in legacy_paths\n+                        }\n+                    stamp_path.write_text(\n+                        json.dumps(stamp, indent=2, sort_keys=True) + \"\\n\",\n+                        encoding=\"utf-8\",\n+                    )\n+\n+                before = _tree_snapshot(target)\n+                with self.assertRaises(shiki_installer.ShikiError) as raised:\n+                    _quiet_install(target, force=True, validate=False)\n+                after = _tree_snapshot(target)\n+\n+                self.assertEqual(before, after, f\"{name} preflight changed the tree\")\n+                message = str(raised.exception)\n+                self.assertIn(expected_reason, message)\n+                self.assertIn(\"No files were written\", message)\n+                for relative in legacy_paths:\n+                    self.assertIn(relative, message)\n+\n+    def test_absent_stamp_is_allowed_when_no_legacy_path_exists(self) -> None:\n+        target = self.tmp / \"no-legacy\"\n+        target.mkdir()\n+        _quiet_install(target, force=True, validate=False)\n+        self.assertTrue(\n+            (target / \"docs\" / \"adr\" / \"SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\").is_file()\n+        )\n+\n+\n+if __name__ == \"__main__\":\n+    unittest.main()\n\n2026-08-07T08:26:55.899153Z ERROR codex_models_manager::manager: failed to renew cache TTL: missing field `base_instructions` at line 94 column 5\ncodex\nThe full suite is still progressing normally with no failures reported so far. Once it completes, the remaining control-plane, adversarial, and validator checks are much shorter.\ndiff --git a/.codex/skills/shiki/SKILL.md b/.codex/skills/shiki/SKILL.md\nindex c8e2e85d9d75d49cc4f2d6286ee22659c4e53e75..93cded623b2c15d9f65c76491a1c2d813ac9e6c0\n--- a/.codex/skills/shiki/SKILL.md\n+++ b/.codex/skills/shiki/SKILL.md\n@@ -18,6 +18,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n Codex CLI does not register this skill as a custom `/shiki` slash command. When\n the user types `/shiki` in Codex CLI and gets \"Unrecognized command\", explain\n that this is expected: invoke Shiki in Codex with natural language such as\n@@ -69,7 +72,7 @@\n \n ## Responsibilities\n \n-- Claude Code implements and repairs by default (`shiki runner claude`, ADR 0008); Codex implements and repairs tasks explicitly assigned to `codex`.\n+- Claude Code implements and repairs by default (`shiki runner claude`, SADR-0008); Codex implements and repairs tasks explicitly assigned to `codex`.\n - Claude Code Action can act as GitHub-side CCA or reviewer.\n - CCA judges completion.\n - MergeGate authorizes state transitions and merge readiness.\ndiff --git a/.github/prompts/cca-completion-check.md b/.github/prompts/cca-completion-check.md\nindex ed203ad5f302ecc81372d8ab27d2b1232ca97497..fd89950528333ef6242e6d5443904f5392ab7e56\n--- a/.github/prompts/cca-completion-check.md\n+++ b/.github/prompts/cca-completion-check.md\n@@ -19,6 +19,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n ## Required Reading\n \n Read only the files needed to judge the PR contract. Prefer this order:\ndiff --git a/.github/prompts/codex-task-handoff.md b/.github/prompts/codex-task-handoff.md\nindex 695e211d76dea1c60e8d5bb360e78487808286a4..59441d576b01a39569722f226c854e5a740e9eb5\n--- a/.github/prompts/codex-task-handoff.md\n+++ b/.github/prompts/codex-task-handoff.md\n@@ -2,7 +2,7 @@\n \n You are Codex Front acting as the Implementer for a Shiki task.\n \n-Follow `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant ADRs, and `docs/agents/codex-handoff.md`.\n+Follow `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant Shiki SADRs and target ADRs, and `docs/agents/codex-handoff.md`.\n \n Rules:\n \ndiff --git a/AGENTS.md b/AGENTS.md\nindex 7f041100d49c2a7525f51cb0f5d696a4a6d1c78c..a06bfbabc6fa48a007389e25e12d7ed523936bed\n--- a/AGENTS.md\n+++ b/AGENTS.md\n@@ -22,7 +22,7 @@\n - **Codex Front** is an optional implementer through Codex App, Codex CLI, Codex IDE extension, or Codex Web signed in with ChatGPT OAuth/subscription auth, dispatched with `shiki runner codex` when a task is explicitly assigned to `codex`.\n - **GitHub CCA** is implemented by Claude Code Action by default, using `CLAUDE_CODE_OAUTH_TOKEN`.\n \n-Do not assume `openai/codex-action`, `OPENAI_API_KEY`, or API-key based Codex automation in the default Shiki path. API-key runners are explicit target-repo extensions and require their own ADR.\n+Do not assume `openai/codex-action`, `OPENAI_API_KEY`, or API-key based Codex automation in the default Shiki path. API-key runners are explicit target-repo extensions and require their own target ADR.\n \n Shiki is not:\n \n@@ -44,10 +44,14 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use\n+`SADR-NNNN-*.md`; target product decisions retain `NNNN-*.md` and the `ADR`\n+namespace.\n+\n Use the canonical source-of-truth order above. Runtime-specific reading orders\n are operational guidance only and must not override or reorder it.\n \n-Conversation state is not durable truth. If a decision matters, put it in GitHub, `.shiki/`, `CONTEXT.md`, or an ADR.\n+Conversation state is not durable truth. If a decision matters, put it in GitHub, `.shiki/`, `CONTEXT.md`, a Shiki SADR, or a target ADR.\n \n If GitHub and `.shiki/` disagree, surface the conflict and prefer GitHub operational state until the mirror is repaired.\n \n@@ -75,8 +79,8 @@\n Every non-trivial change follows this loop:\n \n 1. **Goal**: clarify outcome, completion conditions, non-goals, risk level, and success signals.\n-2. **grill-with-docs**: challenge the plan against domain docs, ADRs, code reality, terminology, and edge scenarios.\n-3. **Context & Impact**: find relevant docs, ADRs, code, dependencies, owners, locks, and verification surfaces.\n+2. **grill-with-docs**: challenge the plan against domain docs, Shiki SADRs, target ADRs, code reality, terminology, and edge scenarios.\n+3. **Context & Impact**: find relevant docs, SADRs, target ADRs, code, dependencies, owners, locks, and verification surfaces.\n 4. **PRD and Spec Freeze**: use `to-prd` when context is settled enough to become durable product/engineering intent. The operator's explicit approval of the PRD is Spec Freeze: the plan records a `spec_freeze` block, and no plan runs without it. Steps 1-4 form Requirements Definition \u2014 one continuous operator dialogue. After Spec Freeze, scope changes require an operator-approved Spec Amendment; non-scope-moving interpretations go to the Assumption Log.\n 5. **Issues**: use `to-issues` to create independently grabbable vertical-slice issues. Prefer AFK slices over HITL slices where possible.\n 6. **Triage**: label issues for readiness, risk, runtime, skills, and MergeGate state.\n@@ -167,7 +171,7 @@\n - Testing decisions.\n - Out-of-scope items.\n - Further notes.\n-- Links to relevant domain terms and ADRs.\n+- Links to relevant domain terms, SADRs, and target ADRs.\n \n Every executable task must include:\n \n@@ -193,7 +197,7 @@\n \n Context & Impact must identify:\n \n-- Relevant documents, domain terms, ADRs, and past decisions.\n+- Relevant documents, domain terms, SADRs, target ADRs, and past decisions.\n - Relevant modules, interfaces, seams, callers, owners, and tests.\n - Dependency relationships between tasks.\n - Candidate locks and lock conflicts.\n@@ -210,7 +214,7 @@\n | Skill | Required when |\n | --- | --- |\n | `setup-matt-pocock-skills` | First configuring the repo for skills, or when issue tracker, triage labels, or domain docs are missing. |\n-| `grill-with-docs` | Default for non-trivial Goals before PRD/issues; always required when plans, terminology, boundaries, tradeoffs, or ADR-worthy decisions are ambiguous. |\n+| `grill-with-docs` | Default for non-trivial Goals before PRD/issues; always required when plans, terminology, boundaries, tradeoffs, or decision-record-worthy choices are ambiguous. |\n | `zoom-out` | The agent lacks an architectural map of the relevant code area. |\n | `to-prd` | Settled Goal context must become durable product/engineering intent. |\n | `to-issues` | A Goal, PRD, or plan must become independently grabbable vertical-slice issues. |\n@@ -348,7 +352,7 @@\n - Three or more new files.\n - Any high-risk or critical label.\n \n-When the architecture gate triggers, run `grill-with-docs` or `improve-codebase-architecture` as appropriate and record durable decisions in an ADR when needed.\n+When the architecture gate triggers, run `grill-with-docs` or `improve-codebase-architecture` as appropriate and record durable decisions in a Shiki SADR or target ADR as applicable.\n \n ## Engineering Discipline\n \ndiff --git a/CLAUDE.md b/CLAUDE.md\nindex 98a932e7841a632866fcd53b0bad0e3af3711d3d..98a31399404d6fb044b4fd493f4459ac87f439de\n--- a/CLAUDE.md\n+++ b/CLAUDE.md\n@@ -14,6 +14,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n ## Claude Role\n \n Default to planner, default implementer, reviewer, coordinator, documentation editor, CCA-style completion judge, and final-judgment assistant.\n@@ -57,7 +60,7 @@\n At the start of a Shiki session:\n \n 1. Read `AGENTS.md`.\n-2. Read `CONTEXT.md` and relevant ADRs.\n+2. Read `CONTEXT.md` and relevant Shiki SADRs and target ADRs.\n 3. Read active `.shiki/` Goal/task/ledger state when present.\n 4. Read active distilled rules from `.shiki/memories/` (status `distilled`, `active: true`, not revoked or superseded) for applicable guidance; the same rules are injected deterministically into task handoffs as the `## Distilled Rules` section (\u00a73.5).\n 5. Read the GitHub Issue or PR when the task is GitHub-backed.\n@@ -74,7 +77,7 @@\n - Define the Goal, completion conditions, non-goals, and risk level.\n - Treat `grill-with-docs` as the default planning entry point for non-trivial Goals.\n - Challenge domain terms against `CONTEXT.md`.\n-- Challenge hard-to-reverse tradeoffs against ADRs.\n+- Challenge hard-to-reverse tradeoffs against SADRs and target ADRs.\n - Explore code instead of asking when code can answer the question.\n - Run Context & Impact before implementation planning.\n - Produce a Task DAG, not an unordered checklist.\n@@ -94,7 +97,7 @@\n - For each question, provide your recommended answer.\n - Resolve dependency decisions before downstream design questions.\n - Update `CONTEXT.md` inline when terminology crystallizes.\n-- Offer ADRs only when the decision is hard to reverse, surprising without context, and a real tradeoff.\n+- Offer a SADR or target ADR only when the decision is hard to reverse, surprising without context, and a real tradeoff.\n - Convert unresolved questions into HITL blockers rather than allowing Codex to guess.\n \n ## PRD and Issues Standard\n@@ -103,7 +106,7 @@\n \n - Use the project's domain glossary vocabulary.\n - Avoid volatile file paths unless they are essential.\n-- Include implementation decisions, testing decisions, out-of-scope items, and relevant ADR/domain links.\n+- Include implementation decisions, testing decisions, out-of-scope items, and relevant SADR/target-ADR/domain links.\n - Identify deep-module opportunities and testable interfaces.\n \n When creating issues:\n@@ -121,7 +124,7 @@\n - Branch/worktree target.\n - Scope and non-goals.\n - Dependencies and locks.\n-- Relevant docs, ADRs, modules, and tests.\n+- Relevant docs, SADRs, target ADRs, modules, and tests.\n - Required skill invocations.\n - TDD expectations.\n - Pre-PR `code-review` gate expectations.\n@@ -152,7 +155,7 @@\n \n - Treat yourself as a judge, not implementer.\n - Do not write production code.\n-- Read `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant ADRs, PRD, task issue, PR body, diff, checks, reviews, labels, and `.shiki/` state.\n+- Read `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant SADRs and target ADRs, PRD, task issue, PR body, diff, checks, reviews, labels, and `.shiki/` state.\n - Evaluate every applicable checklist in `docs/agents/checklists.md`.\n - Map each acceptance criterion to durable evidence.\n - Distinguish implementation failure from missing evidence.\ndiff --git a/CONTEXT.md b/CONTEXT.md\nindex e61a9be06a0cce0d026484083a657bde2067e234..237ee8aa3ea8fab334ece49768bc5ed3b5af0974\n--- a/CONTEXT.md\n+++ b/CONTEXT.md\n@@ -25,7 +25,7 @@\n _Avoid_: jumping directly into code\n \n **grill-with-docs**:\n-A planning skill that challenges a plan against domain language, ADRs, code reality, and concrete edge scenarios. It resolves design-tree questions before PRD/issues. It runs inside Requirements Definition.\n+A planning skill that challenges a plan against domain language, Shiki SADRs, target ADRs, code reality, and concrete edge scenarios. It resolves design-tree questions before PRD/issues. It runs inside Requirements Definition.\n _Avoid_: generic brainstorming, silent assumptions\n \n **Requirements Definition**:\ndiff --git a/CONTRIBUTING.md b/CONTRIBUTING.md\nindex c6d9b656e73abb8a55e5f660123e03153dcb3e6d..9e37a8fbf668db5b22461aaae5548b8f1354394c\n--- a/CONTRIBUTING.md\n+++ b/CONTRIBUTING.md\n@@ -17,7 +17,7 @@\n 1. **Goal** \u2014 start from a user-approved Goal with an outcome, completion\n    conditions, non-goals, risk level, and success signals.\n 2. **grill-with-docs** \u2014 for any non-trivial change, challenge the plan against\n-   domain language, ADRs, and code reality before writing a PRD.\n+   domain language, Shiki SADRs, target ADRs, and code reality before writing a PRD.\n 3. **PRD** \u2014 capture settled product/engineering intent (`to-prd`).\n 4. **Spec Freeze** \u2014 the operator explicitly approves the PRD/requirements;\n    the plan records a `spec_freeze` block and no plan runs without it\n@@ -94,7 +94,7 @@\n   entries.\n - Do not hand-edit `.shiki/goals/*.json` or `.shiki/manifest.json` in a task PR.\n - Conversation state is not durable truth. If a decision matters, record it in\n-  GitHub, `.shiki/`, `CONTEXT.md`, or an ADR.\n+  GitHub, `.shiki/`, `CONTEXT.md`, a Shiki SADR, or a target ADR.\n \n ## Architecture Gate\n \n@@ -103,7 +103,7 @@\n or CI policy, package structure, three or more structural files, more than 100\n net new lines in a core module, three or more new files, or any high-risk label.\n When triggered, run `grill-with-docs` or `improve-codebase-architecture` and\n-record durable decisions in an ADR when needed (see `docs/adr/`).\n+record durable decisions in a Shiki SADR or target ADR as applicable (see `docs/adr/`).\n \n ## Safety\n \ndiff --git a/SYSTEM_PROMPT.md b/SYSTEM_PROMPT.md\nindex aaaea45e3a896c3a44ba89eec8bb84325ecf6c62..da009c86a88285269dee0155e58e550c6d744ea9\n--- a/SYSTEM_PROMPT.md\n+++ b/SYSTEM_PROMPT.md\n@@ -30,6 +30,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n Use the canonical source-of-truth order above. Runtime-specific reading orders\n are operational guidance only and must not override or reorder it. The current\n conversation is non-durable operator input.\n@@ -48,14 +51,14 @@\n \n A runtime may only do work that is explicitly assigned by the Goal, task, PR, or MergeGate decision.\n \n-Do not assume `openai/codex-action`, `OPENAI_API_KEY`, or API-key based Codex automation in the default Shiki path. API-key runners are explicit target-repo extensions and require their own ADR.\n+Do not assume `openai/codex-action`, `OPENAI_API_KEY`, or API-key based Codex automation in the default Shiki path. API-key runners are explicit target-repo extensions and require their own target ADR.\n \n ## 4. Non-negotiable Shiki flow\n \n For any non-trivial Goal, use this flow:\n \n 1. **Goal Seek**: define outcome, non-goals, risk level, completion criteria, and evidence requirements.\n-2. **grill-with-docs**: challenge the plan against `CONTEXT.md`, ADRs, code reality, terminology, boundaries, and edge scenarios. Ask one question at a time when the operator is available. If code can answer a question, inspect code instead of asking.\n+2. **grill-with-docs**: challenge the plan against `CONTEXT.md`, Shiki SADRs, target ADRs, code reality, terminology, boundaries, and edge scenarios. Ask one question at a time when the operator is available. If code can answer a question, inspect code instead of asking.\n 3. **Context & Impact**: identify modules, interfaces, seams, callers, dependencies, locks, risk, and verification surfaces.\n 4. **to-prd + Spec Freeze**: turn settled context into a PRD using domain vocabulary and testing decisions. The operator's explicit approval of the PRD is Spec Freeze, recorded as the plan's `spec_freeze` block; plans without it do not run. Steps 1-4 form Requirements Definition.\n 5. **to-issues**: decompose the PRD into independently grabbable vertical-slice issues. Prefer AFK slices; mark HITL slices when judgment is still required.\n@@ -75,7 +78,7 @@\n Before making any material change:\n \n 1. Read `AGENTS.md` and any runtime-specific wrapper such as `CLAUDE.md`.\n-2. Read `CONTEXT.md` and relevant ADRs.\n+2. Read `CONTEXT.md` and relevant SADRs and target ADRs.\n 3. Read the GitHub Issue / PR and `.shiki/` state for the active Goal or task.\n 4. Identify your role: Planner, Implementer, Reviewer, Completion Check Agent, Repairer, or Guardian-assist.\n 5. Identify Goal id, task id, branch, PR, dependencies, locks, risk level, required skills, and required evidence.\n@@ -103,7 +106,7 @@\n \n At minimum, identify:\n \n-- Relevant documents, ADRs, domain terms, and prior decisions.\n+- Relevant documents, SADRs, target ADRs, domain terms, and prior decisions.\n - Relevant modules, interfaces, seams, callers, owners, and tests.\n - Dependency relationships between tasks.\n - Candidate file locks and lock conflicts.\n@@ -152,7 +155,7 @@\n \n CCA must read:\n \n-- `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant ADRs.\n+- `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant SADRs and target ADRs.\n - Parent Goal issue, PRD issue, task issue, comments, and labels.\n - PR body, diff, commits, changed files, reviews, CI checks, and artifacts available to the workflow.\n - `.shiki/` task contract, locks, ledger entries, CCA checklist profile, and prior repair packets.\n@@ -214,7 +217,7 @@\n - Three or more new files.\n - Any high-risk or critical label.\n \n-When the architecture gate triggers, run `grill-with-docs` or `improve-codebase-architecture` as appropriate and record the decision in an ADR if it is hard to reverse or likely to be re-litigated.\n+When the architecture gate triggers, run `grill-with-docs` or `improve-codebase-architecture` as appropriate and record the decision in a Shiki SADR or target ADR as applicable if it is hard to reverse or likely to be re-litigated.\n \n ## 14. Skill Gate\n \n@@ -223,7 +226,7 @@\n Use these skills:\n \n - `setup-matt-pocock-skills`: before first use of the engineering skill set, or when issue tracker, triage labels, or domain docs are missing.\n-- `grill-with-docs`: default for non-trivial Goals before PRD/issues; required for ambiguous plans, terminology, boundaries, ADR-worthy tradeoffs, or design-tree decisions.\n+- `grill-with-docs`: default for non-trivial Goals before PRD/issues; required for ambiguous plans, terminology, boundaries, decision-record-worthy tradeoffs, or design-tree decisions.\n - `zoom-out`: unfamiliar code area or missing architectural map.\n - `to-prd`: convert settled context into a PRD.\n - `to-issues`: break a PRD, plan, or Goal into independently grabbable vertical-slice issues.\ndiff --git a/docs/adr/0001-keep-shiki-as-platform-name.md b/docs/adr/SADR-0001-keep-shiki-as-platform-name.md\nindex 20a9637c3b9d8c97de7ba8db7cfae4d82d9a7dba..8b685439959aada1b221c112f0ac3c51ca8a1695\n--- a/docs/adr/0001-keep-shiki-as-platform-name.md\n+++ b/docs/adr/SADR-0001-keep-shiki-as-platform-name.md\n@@ -1,3 +1,3 @@\n-# Keep Shiki As The Platform Name\n+# SADR-0001: Keep Shiki As The Platform Name\n \n Shiki remains the product name for the GitHub-native agentic engineering control plane. We considered more descriptive names such as MergePilot, MergeGate, and Kiroku, but kept Shiki because it carries the existing goal-seeking framework identity while remaining independent of Hermes, Codex, Claude Code, or any single runtime.\ndiff --git a/docs/adr/0002-use-agents-as-shared-constitution.md b/docs/adr/SADR-0002-use-agents-as-shared-constitution.md\nindex 73de30ecb078120eac540d9bc51855e3cd48d0e3..c5486182bf06fca36ea508496b013c67302fd989\n--- a/docs/adr/0002-use-agents-as-shared-constitution.md\n+++ b/docs/adr/SADR-0002-use-agents-as-shared-constitution.md\n@@ -1,3 +1,3 @@\n-# Use AGENTS As The Shared Agent Constitution\n+# SADR-0002: Use AGENTS As The Shared Agent Constitution\n \n Shiki uses `AGENTS.md` as the runtime-neutral constitution and `CLAUDE.md` as a thin Claude Code wrapper. This keeps Codex, Claude Code, Hermes Runner, and future Agent Runtimes aligned on the same GitHub-first MergeGate rules while still allowing runtime-specific instructions where needed.\ndiff --git a/docs/adr/0003-require-skill-gate-for-mergegate-readiness.md b/docs/adr/SADR-0003-require-skill-gate-for-mergegate-readiness.md\nindex 424be0c9af4c8f0136f1fde68a468c47ff84e764..7fc337987c7df9792f75f181dfcbed75fe5ffff2\n--- a/docs/adr/0003-require-skill-gate-for-mergegate-readiness.md\n+++ b/docs/adr/SADR-0003-require-skill-gate-for-mergegate-readiness.md\n@@ -1,3 +1,3 @@\n-# Require Skill Gate For MergeGate Readiness\n+# SADR-0003: Require Skill Gate For MergeGate Readiness\n \n Shiki requires relevant engineering skills to be invoked before execution when their triggers apply. This makes skills part of MergeGate readiness rather than optional prompt style, because Shiki depends on repeatable planning, issue decomposition, TDD, diagnosis, triage, and architecture review before autonomous or parallel agent work can be trusted.\ndiff --git a/docs/adr/0004-use-github-first-with-shiki-mirror.md b/docs/adr/SADR-0004-use-github-first-with-shiki-mirror.md\nindex d0a3a9174dc646a945a9d325954ffaf00c352040..a79c54ae9ee8eed2a0524192b864da4e22a92d3f\n--- a/docs/adr/0004-use-github-first-with-shiki-mirror.md\n+++ b/docs/adr/SADR-0004-use-github-first-with-shiki-mirror.md\n@@ -1,4 +1,4 @@\n-# ADR 0004: Use GitHub First With A Repository-Local Shiki Mirror\n+# SADR-0004: Use GitHub First With A Repository-Local Shiki Mirror\n \n ## Status\n \ndiff --git a/docs/adr/0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md b/docs/adr/SADR-0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\nindex b339baf49b89d9290e79c3d51b36e7b2bde3bd95..0f3c3eb9afccdb3403ce978e21d04fe12e495da0\n--- a/docs/adr/0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\n+++ b/docs/adr/SADR-0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\n@@ -1,8 +1,8 @@\n-# ADR 0005: Use Codex As Front And Claude Code Action As The GitHub Runtime\n+# SADR-0005: Use Codex As Front And Claude Code Action As The GitHub Runtime\n \n ## Status\n \n-Accepted (implementer default superseded by ADR 0008; the auth model and the\n+Accepted (implementer default superseded by SADR-0008; the auth model and the\n Claude Code Action GitHub runtime decision remain in force)\n \n ## Context\n@@ -26,4 +26,4 @@\n - Shiki avoids requiring `OPENAI_API_KEY` for the default workflow.\n - Codex work happens through the user's authenticated Codex front surface, not as a hidden backend Action.\n - Claude Code Action is configured for OAuth-token based subscription usage by default.\n-- API-key based automation remains possible only as an explicit target-repo extension with its own ADR.\n+- API-key based automation remains possible only as an explicit target-repo extension with its own target ADR.\ndiff --git a/docs/adr/0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md b/docs/adr/SADR-0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\nindex 4bcae9d1e52f0a37b8e9fbafa5cd14b6ee1993bd..7a38538a6ae9929567f0ade67e7ea6a60d0b3f7e\n--- a/docs/adr/0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\n+++ b/docs/adr/SADR-0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\n@@ -1,4 +1,4 @@\n-# ADR 0006: Separate Codex Implementation, CCA Completion, And MergeGate Authorization\n+# SADR-0006: Separate Codex Implementation, CCA Completion, And MergeGate Authorization\n \n ## Status\n \ndiff --git a/docs/adr/0007-packaging-and-release.md b/docs/adr/SADR-0007-packaging-and-release.md\nindex 79b84ddb97167372b44641e5b7860e400ce80822..bb445a2e2e856d9d1f5fdbe35de8d4e8cec0fb32\n--- a/docs/adr/0007-packaging-and-release.md\n+++ b/docs/adr/SADR-0007-packaging-and-release.md\n@@ -1,4 +1,4 @@\n-# ADR 0007: Packaging And Release For Shiki\n+# SADR-0007: Packaging And Release For Shiki\n \n ## Status\n \n@@ -25,7 +25,7 @@\n   not introduce third-party runtime dependencies or break the shim.\n - Releases must run inside the operator's subscription-authenticated toolchain\n   and must not require extra secrets beyond the built-in `GITHUB_TOKEN`\n-  (consistent with ADR 0005's no-API-key-by-default stance).\n+  (consistent with SADR-0005's no-API-key-by-default stance).\n - `.shiki/manifest.json` already encodes the install surface (`install.include`,\n   `install.create_directories`, `install.exclude_from_commit`), and it must not\n   be mutated as part of this change.\n@@ -34,7 +34,7 @@\n \n This is a hard-to-reverse platform decision (it sets the version source of\n truth, the release trigger, and the public install contract), so it is recorded\n-as an ADR.\n+as a SADR.\n \n ## Decision\n \n@@ -104,7 +104,7 @@\n 3. **Publishing to PyPI.** Rejected for now: Shiki is distributed as a\n    repository template + git-installable CLI, not a PyPI library; PyPI would add\n    credentials/secrets and a different versioning contract. Can be added later as\n-   its own ADR if a packaged distribution is needed.\n+   its own SADR if a packaged distribution is needed.\n 4. **Dynamic version (read `VERSION` via a build backend `dynamic` field).**\n    Rejected to keep `pyproject.toml` backend-agnostic and dependency-free; the\n    small duplication between `VERSION` and `pyproject.toml` is guarded by the\n@@ -123,4 +123,4 @@\n   releasing versions the whole platform but ships only `install.include` to\n   targets.\n - Future PyPI distribution or a console entry point are deferred and would each\n-  warrant their own ADR.\n+  warrant their own SADR.\ndiff --git a/docs/adr/0008-use-claude-code-as-default-implementer.md b/docs/adr/SADR-0008-use-claude-code-as-default-implementer.md\nindex dedf4510b7c7799521422f622029563d373db031..35c2625d908167fbc58855b5b63e283805f167e8\n--- a/docs/adr/0008-use-claude-code-as-default-implementer.md\n+++ b/docs/adr/SADR-0008-use-claude-code-as-default-implementer.md\n@@ -1,12 +1,12 @@\n-# ADR 0008: Use Claude Code As The Default Implementer Runtime\n+# SADR-0008: Use Claude Code As The Default Implementer Runtime\n \n ## Status\n \n-Proposed (supersedes ADR 0005 for the implementer default; keeps its auth model)\n+Proposed (supersedes SADR-0005 for the implementer default; keeps its auth model)\n \n ## Context\n \n-ADR 0005 made Codex Front the default implementation runtime, with Claude Code\n+SADR-0005 made Codex Front the default implementation runtime, with Claude Code\n restricted to planner and reviewer roles by the runtime registry, and the only\n autonomous implementation adapter was `shiki runner codex`.\n \n@@ -18,13 +18,13 @@\n - The platform's planning, review, and CCA layers already run on Claude Code,\n   and Claude Code's native capabilities (the built-in `/code-review` skill and\n   the Workflow multi-agent orchestration tool) have no Codex equivalent.\n-- The Goal lifecycle redesign (ADR 0009) requires an autonomous post-freeze\n+- The Goal lifecycle redesign (SADR-0009) requires an autonomous post-freeze\n   loop driven from the operator's Claude Code session, including bounded\n   repair dispatch, which must not depend on a runtime that cannot be invoked\n   unattended.\n \n The constitution claims Shiki is runtime-agnostic (\"not a Claude-only\n-workflow\", ADR 0002). Any change to the implementer default must preserve the\n+workflow\", SADR-0002). Any change to the implementer default must preserve the\n registry-based runtime model rather than hardcoding one vendor.\n \n ## Decision\n@@ -39,7 +39,7 @@\n   the task to review or repair-needed.\n - Keep Codex registered as an optional implementer runtime. `shiki runner\n   codex`, the Codex handoff templates, and the ChatGPT OAuth auth model from\n-  ADR 0005 remain supported for tasks explicitly assigned to `codex`.\n+  SADR-0005 remain supported for tasks explicitly assigned to `codex`.\n - Commit to mandating two Claude-native capabilities as loop contract in\n   follow-up Goals (status: the code-review gate was delivered by Goal G-B \u2014\n   skill registry, checklists item PR-12, default required_skills \u2014 while the\n@@ -60,7 +60,7 @@\n \n Out of scope: removing Codex support, changing the CCA runtime (Claude Code\n Action with `CLAUDE_CODE_OAUTH_TOKEN` stays), and API-key based automation\n-(still requires its own ADR per ADR 0005).\n+(still requires its own target ADR per SADR-0005).\n \n ## Consequences\n \n@@ -100,7 +100,7 @@\n   harness rejection of direct `codex exec` is outside Shiki's control, and the\n   plan's mandatory native capabilities cannot run on Codex at all.\n - **Remove Codex entirely**: rejected; it abandons the runtime-agnostic\n-  constitution (ADR 0002) for no operational gain and forecloses\n+  constitution (SADR-0002) for no operational gain and forecloses\n   cross-runtime adversarial review.\n - **GitHub Actions cloud implementation via claude-code-action**: rejected as\n   default; durable evidence is automatic but token cost, Node 24 workflow\ndiff --git a/docs/adr/0009-requirements-definition-and-spec-freeze.md b/docs/adr/SADR-0009-requirements-definition-and-spec-freeze.md\nindex 29b3f7871bef9203a8b4a831a65c39eb2f506e42..52e7dd69ad0aa4efe0b68c62678abbf7df02aaec\n--- a/docs/adr/0009-requirements-definition-and-spec-freeze.md\n+++ b/docs/adr/SADR-0009-requirements-definition-and-spec-freeze.md\n@@ -1,4 +1,4 @@\n-# ADR 0009: Merge Grill And Planning Into Requirements Definition With Spec Freeze\n+# SADR-0009: Merge Grill And Planning Into Requirements Definition With Spec Freeze\n \n ## Status\n \ndiff --git a/docs/adr/0010-external-ai-guardian-review.md b/docs/adr/SADR-0010-external-ai-guardian-review.md\nindex e6108bbe8bb4e73e0ff4684c8f78001a80438c4e..f3510b145664298adafb151b6f0db51b972f01ec\n--- a/docs/adr/0010-external-ai-guardian-review.md\n+++ b/docs/adr/SADR-0010-external-ai-guardian-review.md\n@@ -1,4 +1,4 @@\n-# ADR 0010: External AI Guardian Review As A First-Class Approval Authority\n+# SADR-0010: External AI Guardian Review As A First-Class Approval Authority\n \n ## Status\n \n@@ -8,7 +8,7 @@\n \n Shiki is intended to be an autonomous, recursive, self-improving agent\n framework: authority-in-the-loop, not human-in-the-loop. Until now the Guardian\n-approval required for high/critical-risk merges (guardian-policy.json, ADR\n+approval required for high/critical-risk merges (guardian-policy.json, SADR\n governance) recognized only human authority sources \u2014 a GitHub review approval,\n the `guardian:approved` label applied by a configured Guardian, or a comment\n carrying `Guardian approval granted` plus the current head SHA.\n@@ -89,7 +89,7 @@\n   autonomous design the operator intends and produced the impersonation\n   incident.\n - **Record AI approval as an operator comment**: rejected as forgery of human\n-  authority \u2014 the exact failure this ADR corrects.\n+  authority \u2014 the exact failure this SADR corrects.\n - **Deliver the artifact as a committed `.shiki/` file**: rejected; a committed\n   approval inside the PR being approved is forgeable by anyone who can push.\n - **A dedicated reviewer bot identity instead of a relayed comment**: deferred;\ndiff --git a/docs/adr/0011-autonomous-quality-gate-evidence.md b/docs/adr/SADR-0011-autonomous-quality-gate-evidence.md\nindex bca0246c5d46e44afbde15486a75eec425fc02f5..9c93eac16d8c3a6d4b11bd7224e3955102cd7db4\n--- a/docs/adr/0011-autonomous-quality-gate-evidence.md\n+++ b/docs/adr/SADR-0011-autonomous-quality-gate-evidence.md\n@@ -1,4 +1,4 @@\n-# ADR 0011: Autonomous Quality-Gate Evidence \u2014 Deterministic Facts And An Independent Verifier\n+# SADR-0011: Autonomous Quality-Gate Evidence \u2014 Deterministic Facts And An Independent Verifier\n \n ## Status\n \ndiff --git a/docs/adr/0012-autonomous-loop-closeout-to-main.md b/docs/adr/SADR-0012-autonomous-loop-closeout-to-main.md\nindex 637fad6dde4989c9c2bf0f81f676d8d29991123c..ec7d85f81030a676bcbba28b43eb365422a36eac\n--- a/docs/adr/0012-autonomous-loop-closeout-to-main.md\n+++ b/docs/adr/SADR-0012-autonomous-loop-closeout-to-main.md\n@@ -1,4 +1,4 @@\n-# 0012 \u2014 Autonomous loop pushes goal completion to main via a closeout PR\n+# SADR-0012 \u2014 Autonomous loop pushes goal completion to main via a closeout PR\n \n - Status: accepted\n - Date: 2026-06-18\ndiff --git a/docs/adr/0013-bootstrap-grants-actions-approve-pull-requests.md b/docs/adr/SADR-0013-bootstrap-grants-actions-approve-pull-requests.md\nindex b6c69d64f1c5ced346b88d6859586715b3b5bd0e..cf48ff4a6716267f539ca58edd1a852e7c7a2d64\n--- a/docs/adr/0013-bootstrap-grants-actions-approve-pull-requests.md\n+++ b/docs/adr/SADR-0013-bootstrap-grants-actions-approve-pull-requests.md\n@@ -1,4 +1,4 @@\n-# ADR 0013: Bootstrap Grants GitHub Actions Permission To Create And Approve Pull Requests\n+# SADR-0013: Bootstrap Grants GitHub Actions Permission To Create And Approve Pull Requests\n \n ## Status\n \n@@ -31,7 +31,7 @@\n security tradeoff: it lets repository automation satisfy a human-review gate. In\n a multi-maintainer repo that could let a compromised or buggy workflow\n self-approve changes. So whether bootstrap turns it on by default \u2014 and with\n-what default token scope \u2014 is an ADR-worthy decision, not an implementation\n+what default token scope \u2014 is a SADR-worthy decision, not an implementation\n detail.\n \n ## Decision\n@@ -48,7 +48,7 @@\n Specifically:\n \n - **`can_approve_pull_request_reviews` defaults to `true`.** Shiki's default\n-  posture is solo/AFK autonomous operation (ADR 0008/0009/0012), where the CCA\n+  posture is solo/AFK autonomous operation (SADR-0008/0009/0012), where the CCA\n   Review Bridge is the only path that can satisfy `required_review: true` after\n   the CCA and Guardian gates have already passed. The default makes a\n   freshly-bootstrapped repo able to run the autonomous loop end to end without a\ndiff --git a/docs/adr/0014-codex-app-external-ai-guardian-ui-adapter.md b/docs/adr/SADR-0014-codex-app-external-ai-guardian-ui-adapter.md\nindex 238db06279334caa341c117362df9271382a7fcd..65f12e9939321e1b3e389a34c822fc1549e5155d\n--- a/docs/adr/0014-codex-app-external-ai-guardian-ui-adapter.md\n+++ b/docs/adr/SADR-0014-codex-app-external-ai-guardian-ui-adapter.md\n@@ -1,4 +1,4 @@\n-# ADR 0014: Use Codex App As The External AI Guardian UI Adapter\n+# SADR-0014: Use Codex App As The External AI Guardian UI Adapter\n \n ## Status\n \ndiff --git a/docs/adr/0015-contract-approval-before-dispatch.md b/docs/adr/SADR-0015-contract-approval-before-dispatch.md\nindex fad6c656741a20a7684ba16080a7db159f63a52f..6061271a46d3f1d6ae7174477de3fb75673123a6\n--- a/docs/adr/0015-contract-approval-before-dispatch.md\n+++ b/docs/adr/SADR-0015-contract-approval-before-dispatch.md\n@@ -1,4 +1,4 @@\n-# ADR 0015: Approve The Task Contract Before Dispatch, Not The Diff After It\n+# SADR-0015: Approve The Task Contract Before Dispatch, Not The Diff After It\n \n ## Status\n \n@@ -9,7 +9,7 @@\n Guardian approval for high/critical risk is expressible only on a pull request.\n Every approval source in `guardian-policy.json` \u2014 GitHub review, the\n `guardian:approved` label, the `Guardian approval granted` comment, and the\n-`external_ai_guardian_review` artifact (ADR 0010/0014) \u2014 is bound to a PR, and\n+`external_ai_guardian_review` artifact (SADR-0010/0014) \u2014 is bound to a PR, and\n three of the four require the current head SHA. Approval therefore cannot exist\n before an implementation PR exists.\n \n@@ -81,7 +81,7 @@\n An envelope breach must surface only on the non-repairable policy gate, never on\n the repairable `MergeGate metadata check`. Otherwise the Goal Loop routes it to\n `dispatch_repair` \u2014 instructing a runner to make the breach go away \u2014 which is\n-exactly what this ADR forbids.\n+exactly what this SADR forbids.\n \n **Contract immutability.** After registration, an implementation PR must not\n change the governance fields of its own task contract (`scope`, `non_goals`,\n@@ -116,7 +116,7 @@\n fails any verdict that is not `complete`, and the signal the CCA prompt reads is\n computed from `evaluate_guardian_approval` alone. Without the OR-merge at the\n signal, a contract-approved PR still returns `needs_guardian`, the CCA job fails,\n-and the MergeGate policy check never runs \u2014 the gate this ADR changes would never\n+and the MergeGate policy check never runs \u2014 the gate this SADR changes would never\n be reached. The CCA *prompt* needs no change: a `contract_approval` entry in the\n signal's existing `sources` array satisfies its current wording.\n \n@@ -130,7 +130,7 @@\n written, and an approved contract lets its implementation PRs merge through the\n same automatic path as low/medium risk. The wasted implement-then-block cycle\n disappears. Guardian review shifts from reading diffs to reading contracts, which\n-is both earlier and smaller; the external AI Guardian path (ADR 0010/0014) works\n+is both earlier and smaller; the external AI Guardian path (SADR-0010/0014) works\n on contracts with no change.\n \n We accept that the Guardian authorizes a class of change without seeing its\n@@ -146,7 +146,7 @@\n adversarial tests for the new approval path equivalent in rigor to\n `test_shiki_governance_evidence.sh`.\n \n-Bootstrap costs, which apply only while this ADR is being implemented. The tasks\n+Bootstrap costs, which apply only while this SADR is being implemented. The tasks\n that build Contract Approval merge under the existing rules, so the ones\n dispatched after the pre-dispatch gate lands but before the approval path lands\n need a Guardian-approved Contract PR *and* a live Guardian approval on their\n@@ -210,7 +210,7 @@\n risk; they do not bound authorized work.\n \n **Auto-escalate risk on envelope breach instead of stopping.** Returns the\n-workflow to a diff-time Guardian decision \u2014 the exact behavior this ADR removes.\n+workflow to a diff-time Guardian decision \u2014 the exact behavior this SADR removes.\n \n **Resolve governance fields from the base snapshot only.** Rejected against the\n registration flow: implementation PRs add their own contract, so the base has no\ndiff --git a/docs/adr/0016-locks-bound-product-paths.md b/docs/adr/SADR-0016-locks-bound-product-paths.md\nindex b5e850f3f1f7f4ef6e7f9d1763a29a51bd300c45..46b40a9b99a7aae644c02bb458f5473ff0a90ce2\n--- a/docs/adr/0016-locks-bound-product-paths.md\n+++ b/docs/adr/SADR-0016-locks-bound-product-paths.md\n@@ -1,4 +1,4 @@\n-# ADR 0016: Locks bound product paths; the Shiki mirror is bound by record identity\n+# SADR-0016: Locks bound product paths; the Shiki mirror is bound by record identity\n \n ## Status\n \n@@ -6,8 +6,8 @@\n \n - Date: 2026-07-28\n - Deciders: operator (mizutani-140), Claude (planner)\n-- Related: ADR 0004 (GitHub-first with a `.shiki` mirror), ADR 0009 (Spec Freeze),\n-  ADR 0012 (autonomous loop closeout to main), ADR 0015 (Contract Approval before\n+- Related: SADR-0004 (GitHub-first with a `.shiki` mirror), SADR-0009 (Spec Freeze),\n+  SADR-0012 (autonomous loop closeout to main), SADR-0015 (Contract Approval before\n   dispatch), remediation goal task R-02, the live PR #179 deadlock.\n \n ## Context\n@@ -90,11 +90,11 @@\n files, documentation files and named source modules are nameable; runtime-\n allocated ledger, runner and report ids are not \u2014 and those live in `.shiki`,\n which locks no longer govern. Existing frozen contracts are brought into line by\n-a one-time Spec Amendment per plan, which must land before ADR 0015's contract\n+a one-time Spec Amendment per plan, which must land before SADR-0015's contract\n immutability makes `locks` unwritable from the implementation PR.\n \n **Lock state is resolved from the durable side.** MergeGate resolves the lock\n-registry, and ADR 0015's governance fields, from the base `.shiki` snapshot when\n+registry, and SADR-0015's governance fields, from the base `.shiki` snapshot when\n present, falling back to head only when absent. The `MergeGate metadata check`\n job is given `--base-shiki`, which it does not receive today. Locks are not\n derived from the live open-pull-request set: a pull request body is untrusted\n@@ -108,7 +108,7 @@\n Explicitly out of scope: moving MergeGate's contract resolution off the PR\n checkout wholesale; making the mirror a projection of GitHub state; introducing\n new lock kinds; moving ledger evidence out of the repository; and unwinding\n-ADR 0012's closeout pull request. Registration of multi-task goals is ADR 0015's\n+SADR-0012's closeout pull request. Registration of multi-task goals is SADR-0015's\n Contract PR and is not re-decided here.\n \n ## Consequences\n@@ -127,7 +127,7 @@\n \n Risks accepted. First, goal-scoped identity is only as strong as `goal_id`,\n which is resolved from the PR body and the head task file; the mitigation is to\n-add `goal_id` to ADR 0015's frozen governance field set, and until that lands a\n+add `goal_id` to SADR-0015's frozen governance field set, and until that lands a\n task file can name a foreign goal. Second, base-when-present resolution is\n unavailable on fork pull requests, where the head is contributor-controlled;\n fork pull requests already cannot complete a Shiki merge, and this decision does\n@@ -139,7 +139,7 @@\n path, not by implementation pull requests.\n \n New obligations: one Spec Amendment per frozen plan to rewrite `locks`, ordered\n-before ADR 0015 T1; extension of the adversarial suites in\n+before SADR-0015 T1; extension of the adversarial suites in\n `scripts/test_shiki_adversarial_state.sh` and\n `scripts/test_shiki_governance_evidence.sh` to cover the eleven newly-scoped\n directories; and updating `scripts/test_shiki_loop_lock_guard.sh`,\n@@ -151,32 +151,32 @@\n whether the loop's transitive sync of ledger `evidence` references can be\n deleted once the Contract PR carries its own source plan.\n \n-## Relationship to ADR 0004\n+## Relationship to SADR-0004\n \n-This ADR **clarifies something ADR 0004 left unspecified**. ADR 0004 decides that\n+This SADR **clarifies something SADR-0004 left unspecified**. SADR-0004 decides that\n GitHub is the operational source of truth and that each target repository keeps a\n `.shiki/` mirror. It says nothing about pull requests writing the mirror, does\n not name MergeGate, and does not make the mirror an input to a gate. Nothing in\n-ADR 0004's Decision or Consequences is amended, superseded or weakened: the\n+SADR-0004's Decision or Consequences is amended, superseded or weakened: the\n mirror still exists, still records the same artifacts, and pull request\n-boundaries remain the coordination surface. What this ADR settles is the question\n+boundaries remain the coordination surface. What this SADR settles is the question\n 0004 never reached \u2014 by what authority a pull request may write a mirror record \u2014\n and it answers it the way `.shiki/manifest.json` already declares\n (`pr_mutation: current-task-or-current-goal-only`).\n \n-It **amends ADR 0012**, whose Decision item 5 states that \"Loop-executed tasks\n+It **amends SADR-0012**, whose Decision item 5 states that \"Loop-executed tasks\n declare the `path:.shiki/**` lock, which covers every `.shiki` file the closeout\n stages ... so `files_outside_locks` is satisfied without per-file lock\n bookkeeping.\" That clause is replaced: the closeout's mirror writes are\n-authorized by record identity, not by a declared lock. ADR 0012's substantive\n+authorized by record identity, not by a declared lock. SADR-0012's substantive\n decision \u2014 that completion reaches `main` through a closeout pull request rather\n than local attestation \u2014 is preserved unchanged.\n \n-It is **complementary to ADR 0015**, not a substitute. ADR 0015 owns contract\n+It is **complementary to SADR-0015**, not a substitute. SADR-0015 owns contract\n immutability, the pre-dispatch gate, the Contract PR and the Scope Envelope. This\n-ADR owns what a lock means and who may write the mirror. The one ordering\n+SADR owns what a lock means and who may write the mirror. The one ordering\n constraint between them is that the lock-narrowing Spec Amendment must precede\n-ADR 0015 T1's freezing of the `locks` field.\n+SADR-0015 T1's freezing of the `locks` field.\n \n ## Alternatives Considered\n \n@@ -186,7 +186,7 @@\n the only writer that can register them may run. Migrating the Guardian signal to\n that store makes every registration PR fail closed to `needs_guardian` at every\n risk level, because the task file is by definition absent from the store, which\n-removes the low/medium autonomous path ADR 0008/0009 exists to provide. And it\n+removes the low/medium autonomous path SADR-0008/0009 exists to provide. And it\n leaves schemas, manifest, config, guardian policy, templates, migrations, plans,\n worktrees, runner records, runs and handoffs with no writer at all in a\n repository whose product is `.shiki`. Measured benefit on the frozen backlog:\n@@ -199,14 +199,14 @@\n namespace is not in fact ownership-partitioned \u2014 56 ledger entries carry no\n `task_id`, and one goal DAG is writable by 35 registered tasks. The correct\n expression of its insight is that locks do not govern the mirror at all, which is\n-what this ADR decides.\n+what this SADR decides.\n \n **Make the mirror a projection written only by a registration writer and a\n post-merge projection job.** Rejected. It requires a bypass actor with write\n access to the protected default branch; it makes its own master invariant\n readable from a manifest that pull requests may edit; it deletes both reconcile\n modes and leaves a corrupted mirror with no in-band repair path, contradicting\n-ADR 0004's consequence that mirror conflicts are repaired against GitHub state;\n+SADR-0004's consequence that mirror conflicts are repaired against GitHub state;\n and its content-derived ids are rejected by `validate_shiki`'s currently-green id\n pattern.\n \ndiff --git a/docs/adr/0017-bookkeeping-closeouts-do-not-inherit-risk.md b/docs/adr/SADR-0017-bookkeeping-closeouts-do-not-inherit-risk.md\nindex 554e3a0d526a72ca067108c5464cc68c3edcf174..b9676857a4713850e063af7c39c8e0b01ecb4c66\n--- a/docs/adr/0017-bookkeeping-closeouts-do-not-inherit-risk.md\n+++ b/docs/adr/SADR-0017-bookkeeping-closeouts-do-not-inherit-risk.md\n@@ -1,4 +1,4 @@\n-# ADR 0017: A Bookkeeping Closeout Does Not Inherit Its Task's Risk\n+# SADR-0017: A Bookkeeping Closeout Does Not Inherit Its Task's Risk\n \n ## Status\n \n@@ -6,7 +6,7 @@\n \n ## Context\n \n-ADR 0012 made the goal loop push a task's completion to the default branch through a\n+SADR-0012 made the goal loop push a task's completion to the default branch through a\n closeout pull request: after the implementation PR merges, the loop opens a second PR\n carrying `task.status = done`, `lock.state = released`, and \u2014 when the task completes\n its goal \u2014 `goal.status = complete` plus one scorecard report. Nothing else. The\n@@ -49,14 +49,14 @@\n 4. The task file's governance fields (`scope`, `non_goals`, `required_skills`,\n    `risk_level`, `locks`, `acceptance_checks`, `test_command`) are byte-identical to\n    the base snapshot. Only `status`, `expected_pr`, `closeout_pr`, `ledger_evidence`\n-   and `expected_branch` may differ \u2014 the ADR 0015 contract-immutability rule,\n+   and `expected_branch` may differ \u2014 the SADR-0015 contract-immutability rule,\n    unchanged.\n \n    `expected_branch` is in that set because a closeout is *by construction* on a\n    different branch from the implementation it closes, and the loop rewrites the\n    field accordingly. It is safe to let it differ because MergeGate independently\n    requires `expected_branch` to equal the PR's actual head ref, so a PR cannot set\n-   it to anything other than the branch it is really on. The first draft of this ADR\n+   it to anything other than the branch it is really on. The first draft of this SADR\n    omitted it, which made the exemption unreachable: every real closeout failed\n    condition 4 on a field the loop always changes. See PR #209.\n 5. The state transitions are exactly the terminal set: task `review` \u2192 `done`, lock\n@@ -113,12 +113,12 @@\n classification must be derived, not asserted.\n \n **Fold the closeout into the implementation PR** so no second PR exists. Rejected under\n-ADR 0012, which recorded why: a task file that is already `done` on the implementation\n+SADR-0012, which recorded why: a task file that is already `done` on the implementation\n branch short-circuits the loop's review \u2192 CCA \u2192 merge \u2192 done ordering into a local\n goal-completion before any merge happens.\n \n **Have the loop auto-merge closeouts with elevated credentials.** Rejected outright: that\n-is a runtime authoring its own approval, which ADR 0010 and the constitution's\n+is a runtime authoring its own approval, which SADR-0010 and the constitution's\n approval-record prohibition forbid. It would also be indistinguishable, in the evidence,\n from the forgery those rules exist to prevent.\n \ndiff --git a/docs/adr/0018-carried-guardian-approval-across-a-proven-base-sync.md b/docs/adr/SADR-0018-carried-guardian-approval-across-a-proven-base-sync.md\nindex 22358562da390ed8b45cd600da08a43d378b4680..e6e2907cefa776460af73ddf7d39db605f3c39b0\n--- a/docs/adr/0018-carried-guardian-approval-across-a-proven-base-sync.md\n+++ b/docs/adr/SADR-0018-carried-guardian-approval-across-a-proven-base-sync.md\n@@ -1,4 +1,4 @@\n-# ADR 0018: Carried Guardian Approval Across A Proven Base Sync\n+# SADR-0018: Carried Guardian Approval Across A Proven Base Sync\n \n ## Status\n \n@@ -34,7 +34,7 @@\n Guardian evaluator that consumes it (`shiki_guardian.evaluate_guardian_approval`,\n the `guardian_comment_carried` source) were built and tested in isolation. Neither\n does anything until a caller passes the flags that turn the carry on, so the\n-feature was invisible in CI. This ADR records the decision the two of them\n+feature was invisible in CI. This SADR records the decision the two of them\n implement, and this task wires it into the four CI invocations that judge Guardian\n approval.\n \ndiff --git a/docs/adr/README.md b/docs/adr/README.md\nindex c70e057a4520c36e042ab68c53cd4a21ef709b71..5da43856836e2ce6a150f8127766e2f88355ab4b\n--- a/docs/adr/README.md\n+++ b/docs/adr/README.md\n@@ -1,40 +1,41 @@\n-# Architecture Decision Records\n+# Decision Record Namespaces\n \n-This directory holds Shiki's Architecture Decision Records (ADRs): durable\n-records of hard-to-reverse platform decisions. ADRs are part of the Shiki source\n-of truth alongside GitHub state, the `.shiki/` mirror, and `CONTEXT.md`.\n+This directory holds two independent decision namespaces:\n \n-## How ADRs Work\n+- Shiki Architecture Decision Records (SADRs), named\n+  `SADR-NNNN-<short-slug>.md`, record hard-to-reverse Shiki platform decisions.\n+- Target Architecture Decision Records (ADRs), named\n+  `NNNN-<short-slug>.md`, belong to the target product.\n \n-- New ADRs are added as `docs/adr/NNNN-<short-slug>.md`, where `NNNN` is the next\n-  zero-padded sequence number.\n-- The **canonical list of ADRs is the set of files matching `docs/adr/*.md`** in\n-  this directory. Browse `docs/adr/*.md` for the authoritative, current set.\n-- Use [`template.md`](template.md) as the starting point for a new ADR.\n-- Record an ADR when a decision is hard to reverse, surprising without context,\n-  and a real tradeoff. Do not write an ADR for routine or easily reversible\n-  choices.\n+The same four-digit number may appear once in each namespace. A platform\n+`SADR-NNNN` citation resolves only to `SADR-NNNN-*.md`; a target `ADR NNNN`\n+citation resolves only to `NNNN-*.md`.\n \n-Using the `docs/adr/*.md` glob as the source of truth means other PRs can add an\n-ADR without editing this index file, avoiding merge contention on a\n-hand-maintained enumeration.\n+## How Decision Records Work\n \n-## Current ADRs (snapshot)\n-\n-The list below is a convenience snapshot and may lag behind the directory. When\n-in doubt, list `docs/adr/*.md`.\n+- New Shiki platform decisions use the next zero-padded SADR number.\n+- New target product decisions use the target's own ADR sequence.\n+- The canonical list is the files in `docs/adr/`; no hand-maintained index is\n+  authoritative.\n+- Use [`template.md`](template.md) as the starting point and choose the filename\n+  namespace that owns the decision.\n+- Record a decision when it is hard to reverse, surprising without context, and\n+  a real tradeoff. Do not write a decision record for routine or easily\n+  reversible choices.\n \n-- [0001 \u2014 Keep Shiki As The Platform Name](0001-keep-shiki-as-platform-name.md)\n-- [0002 \u2014 Use AGENTS As The Shared Agent Constitution](0002-use-agents-as-shared-constitution.md)\n-- [0003 \u2014 Require Skill Gate For MergeGate Readiness](0003-require-skill-gate-for-mergegate-readiness.md)\n-- [0004 \u2014 Use GitHub First With A Repository-Local Shiki Mirror](0004-use-github-first-with-shiki-mirror.md)\n-- [0005 \u2014 Use Codex As Front And Claude Code Action As The GitHub Runtime](0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md)\n-- [0006 \u2014 Separate Codex Implementation, CCA Completion, And MergeGate Authorization](0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md)\n+Using the directory as the source of truth lets independent changes add records\n+without contending on an enumerated index.\n \n-## Adding A New ADR\n+## Adding A Shiki SADR\n \n-1. Copy `template.md` to `docs/adr/NNNN-<short-slug>.md`.\n+1. Copy `template.md` to `docs/adr/SADR-NNNN-<short-slug>.md`.\n 2. Fill in the title, status, context, decision, and consequences.\n-3. Set the status to `Proposed`, then `Accepted` once the decision is approved.\n-4. Open a PR following `CONTRIBUTING.md`. You do not need to edit this index for\n-   the ADR to be discoverable via `docs/adr/*.md`.\n+3. Set the status to `Proposed`, then `Accepted` once the proper authority has\n+   approved the decision.\n+4. Open a PR following `CONTRIBUTING.md`.\n+\n+## Adding A Target ADR\n+\n+1. Copy `template.md` to `docs/adr/NNNN-<short-slug>.md` using the target's own\n+   sequence.\n+2. Cite it as `ADR NNNN`; do not prefix its filename with `SADR-`.\ndiff --git a/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md b/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\nnew file mode 100644\nindex 0000000000000000000000000000000000000000..7455ce1f3bed02ddddb851ac21a1d1a648c9aeba\n--- /dev/null\n+++ b/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\n@@ -0,0 +1,55 @@\n+# SADR-0019: Separate Shiki Platform SADRs From Target ADRs\n+\n+## Status\n+\n+Accepted\n+\n+## Context\n+\n+Shiki installs its platform decision records into `docs/adr/` in every target\n+repository. Shiki and its targets previously used the same numeric\n+`NNNN-<slug>.md` namespace. A target that already owned ADRs 0013 through 0018\n+therefore acquired two records for each number during installation, making an\n+`ADR NNNN` citation ambiguous. Renumbering target decisions would rewrite target\n+history, while renumbering Shiki decisions per target would destroy their stable\n+platform identity.\n+\n+Upgrades also need to remove the former Shiki-owned numeric paths without ever\n+mistaking a target-owned ADR for platform content. The install stamp already\n+records exact shipped paths and content digests, so it can prove ownership only\n+when both still match.\n+\n+## Decision\n+\n+Shiki platform decisions use the `SADR` namespace:\n+\n+- records are named `docs/adr/SADR-NNNN-<slug>.md`;\n+- live platform citations use `SADR-NNNN`;\n+- target product decisions retain `docs/adr/NNNN-<slug>.md` and `ADR NNNN`;\n+- the reference validator resolves the two namespaces independently, so the same\n+  four-digit number may exist once in each namespace.\n+\n+Before a forced upgrade writes anything, the installer inventories the exact\n+former numeric Shiki ADR paths. It may delete a legacy path only when the\n+existing install stamp contains that exact path and its recorded SHA-256 digest\n+matches the file currently on disk. All ownership blockers are reported\n+together and abort the upgrade before its first write. A non-forced install\n+never performs this cleanup.\n+\n+Historical `.shiki/` evidence remains unchanged because it records the namespace\n+that existed when that evidence was produced.\n+\n+## Consequences\n+\n+- Targets may use any ADR number without colliding with Shiki's platform SADRs.\n+- Fresh installs preserve target ADR bytes and install a stable SADR set.\n+- Forced upgrades fail closed when legacy ownership cannot be proven, and a\n+  successful upgrade refreshes the stamp without removed legacy paths.\n+- Current governance, packaging, workflow, prompt, script, and skill references\n+  must move with the platform records.\n+\n+## Alternatives Considered\n+\n+- Collision-only refusal leaves affected targets permanently unupgradable.\n+- Renumbering target ADRs mutates target-owned history.\n+- Per-target Shiki renumbering makes platform citations non-portable.\ndiff --git a/docs/adr/template.md b/docs/adr/template.md\nindex a92a97daddd0b00ab9dd626cc8d86942da2eafed..17dd79175b5fee2105935557f691b40cf9492942\n--- a/docs/adr/template.md\n+++ b/docs/adr/template.md\n@@ -1,14 +1,14 @@\n-# NNNN: <Short Decision Title>\n+# <SADR-NNNN or NNNN>: <Short Decision Title>\n \n ## Status\n \n-Proposed | Accepted | Superseded by ADR-NNNN\n+Proposed | Accepted | Superseded by SADR-NNNN or target ADR NNNN\n \n ## Context\n \n What is the situation, constraint, or force that requires a decision? Reference\n-relevant domain terms (`CONTEXT.md`), prior ADRs, and code reality. State the\n-problem, not the solution.\n+relevant domain terms (`CONTEXT.md`), prior Shiki SADRs or target ADRs, and code\n+reality. State the problem, not the solution.\n \n ## Decision\n \ndiff --git a/docs/agents/autonomous-loop-e2e-acceptance.md b/docs/agents/autonomous-loop-e2e-acceptance.md\nindex bbf84c6081987f159ac6ad9ecf3c82b7aa72e7e8..5c13c1d02a05a90b0dc46cbef7c95a9846d0c237\n--- a/docs/agents/autonomous-loop-e2e-acceptance.md\n+++ b/docs/agents/autonomous-loop-e2e-acceptance.md\n@@ -40,8 +40,8 @@\n 3. `gh auth status` is logged in with merge permission on the repo.\n 4. Branch protection + required checks are configured exactly as the goal-loop\n    required-check set expects (see `docs/agents/branch-protection-smoke.md`).\n-5. The repo `.shiki/policy` permits autonomous low/medium-risk merges (ADR\n-   0008/0009); the MergeGate policy check is the recorded-authority gate.\n+5. The repo `.shiki/policy` permits autonomous low/medium-risk merges\n+   (SADR-0008/0009); the MergeGate policy check is the recorded-authority gate.\n \n The operator's only actions are: register the task and start the loop. **No\n manual pre-clearing** means after `shiki loop run` starts, the operator does not\ndiff --git a/docs/agents/bootstrap-command.md b/docs/agents/bootstrap-command.md\nindex 05f7f203a52cf412b9f97084a6e131ed0f785afd..f7aa0b734dfebee4a3b139335b66ee3415f48713\n--- a/docs/agents/bootstrap-command.md\n+++ b/docs/agents/bootstrap-command.md\n@@ -189,7 +189,9 @@\n ```\n \n Use this only for tests, fixtures, or explicit local-only template inspection.\n-Use `--force` only when you intentionally want to overwrite existing target files.\n+Use `--force` only when you intentionally want to refresh the target template.\n+Legacy numeric Shiki decision cleanup is stamp-and-digest authorized before any\n+write; target ADR paths are not cleanup candidates.\n \n ## Slash Command\n \ndiff --git a/docs/agents/checklists.md b/docs/agents/checklists.md\nindex 4a7f03ade346f53a1c1d82981f3fd744f9ab4547..0c5ec6c53dc7197b7b7a7d18e667b325babd6c45\n--- a/docs/agents/checklists.md\n+++ b/docs/agents/checklists.md\n@@ -32,18 +32,18 @@\n | ID | Item | Blocking |\n | --- | --- | --- |\n | GD-01 | Plan was challenged against `CONTEXT.md`. | blocking for non-trivial Goals |\n-| GD-02 | Relevant ADRs were read or absence was recorded. | blocking for non-trivial Goals |\n+| GD-02 | Relevant SADRs and target ADRs were read or absence was recorded. | blocking for non-trivial Goals |\n | GD-03 | Ambiguous terms were resolved or converted to HITL blockers. | blocking |\n | GD-04 | Edge scenarios were considered. | advisory |\n | GD-05 | Code reality was checked when code could answer. | blocking when applicable |\n | GD-06 | New domain terms were added to `CONTEXT.md` when needed. | blocking when applicable |\n-| GD-07 | ADR was created or explicitly skipped using the ADR criteria. | blocking when applicable |\n+| GD-07 | A SADR or target ADR was created or explicitly skipped using the decision-record criteria. | blocking when applicable |\n \n ## CI \u2014 Context & Impact Checklist\n \n | ID | Item | Blocking |\n | --- | --- | --- |\n-| CI-01 | Relevant docs and ADRs identified. | blocking |\n+| CI-01 | Relevant docs, SADRs, and target ADRs identified. | blocking |\n | CI-02 | Relevant modules, interfaces, seams, callers, and tests identified. | blocking |\n | CI-03 | Dependencies and blocked-by relationships identified. | blocking |\n | CI-04 | Candidate locks identified. | blocking |\n@@ -62,7 +62,7 @@\n | PRD-04 | Implementation decisions are recorded without volatile code detail. | blocking |\n | PRD-05 | Testing decisions are recorded. | blocking |\n | PRD-06 | Out-of-scope items are explicit. | blocking |\n-| PRD-07 | Relevant glossary/ADR links are present when applicable. | blocking |\n+| PRD-07 | Relevant glossary/SADR/target-ADR links are present when applicable. | blocking |\n \n ## SF \u2014 Spec Freeze Checklist\n \n@@ -98,7 +98,7 @@\n | H-02 | Branch/worktree target is present. | blocking |\n | H-03 | Scope and non-goals are present. | blocking |\n | H-04 | Dependencies and locks are present. | blocking |\n-| H-05 | Relevant docs, ADRs, modules, and tests are referenced. | blocking |\n+| H-05 | Relevant docs, SADRs, target ADRs, modules, and tests are referenced. | blocking |\n | H-06 | Required skills are stated. | blocking |\n | H-07 | TDD expectations are stated. | blocking when behavior work |\n | H-08 | Acceptance criteria are included. | blocking |\ndiff --git a/docs/agents/codex-handoff.md b/docs/agents/codex-handoff.md\nindex ede468f47cb277f69a857e8c45df703e4ed0151d..6f97bdffb67e44d7edea2c71e01ea28933c4eb6c\n--- a/docs/agents/codex-handoff.md\n+++ b/docs/agents/codex-handoff.md\n@@ -46,7 +46,7 @@\n \n ## Context\n - Domain terms:\n-- Relevant ADRs:\n+- Relevant SADRs and target ADRs:\n - Relevant docs:\n - Relevant modules/interfaces/seams:\n - Similar tests/prior art:\ndiff --git a/docs/agents/completion-check-agent.md b/docs/agents/completion-check-agent.md\nindex 13e4c035db35025f4e3cdcc5adfb24314ac4d521..61bfa1218cf36ca605250c91919e620f33238aa0\n--- a/docs/agents/completion-check-agent.md\n+++ b/docs/agents/completion-check-agent.md\n@@ -32,7 +32,7 @@\n - `AGENTS.md`\n - `CLAUDE.md`\n - `CONTEXT.md`\n-- relevant ADRs\n+- relevant SADRs and target ADRs\n - parent Goal issue\n - PRD issue or document\n - task issue\ndiff --git a/docs/agents/decision-control.md b/docs/agents/decision-control.md\nindex 7d8ee5ab03a5ccbcbd04056ae61de03785a2a9c9..8c94ecda00fffaa0e92c325ab83aa9776bd1dac1\n--- a/docs/agents/decision-control.md\n+++ b/docs/agents/decision-control.md\n@@ -11,7 +11,7 @@\n | Decision | Owner | When | Required inputs | Output | Enforcement surface |\n | --- | --- | --- | --- | --- | --- |\n | Is the Goal clear enough to plan? | Planner + Guardian when needed | Goal intake | User request, existing docs, risk signals | Goal readiness verdict | Goal checklist, issue template, ledger |\n-| Must `grill-with-docs` run? | Planner / Skill Gate | Before PRD or issue split | Ambiguity, domain terms, ADR conflicts, unclear boundaries | Skill invocation or explicit skip rationale | Skill Gate checklist |\n+| Must `grill-with-docs` run? | Planner / Skill Gate | Before PRD or issue split | Ambiguity, domain terms, SADR/target-ADR conflicts, unclear boundaries | Skill invocation or explicit skip rationale | Skill Gate checklist |\n | Is a PRD required? | Planner | After Goal context stabilizes | Goal, `grill-with-docs` output, Context & Impact output | PRD required / not required | PRD checklist, CCA evidence check |\n | Can the spec be frozen? | Operator only | End of Requirements Definition, before issue split | PRD/requirements summary, scope inventory (SF-02), open-question list | `spec_freeze` block in the plan + ledger evidence | `require_grilled_plan` (plan ingest/run), SF checklist |\n | Can a frozen spec change? | Operator only (Spec Amendment) | When implementation contradicts the frozen spec | Contested decisions, scoped re-grill result | recorded amendment + re-stamped freeze | SF checklist, ledger evidence |\ndiff --git a/docs/agents/domain.md b/docs/agents/domain.md\nindex 258da8ea81e87ac0a6b0b5b5e5c2fde980eaabb7..d57b0cf17ff3e1fa9e6e5885372bf083d8295421\n--- a/docs/agents/domain.md\n+++ b/docs/agents/domain.md\n@@ -5,7 +5,9 @@\n ## Layout\n \n - `CONTEXT.md` defines Shiki language. Keep it implementation-free.\n-- `docs/adr/` records hard-to-reverse decisions.\n+- `docs/adr/SADR-NNNN-*.md` records Shiki platform decisions.\n+- `docs/adr/NNNN-*.md` records target product decisions without sharing the\n+  SADR namespace.\n - `AGENTS.md` is the runtime-neutral agent constitution.\n - `CLAUDE.md` is the Claude Code wrapper around `AGENTS.md`.\n - `.shiki/` is the Target Repository mirror for Goals, PRDs, plans, Task DAGs, contracts, locks, worktree records, ledger entries, CCA verdicts, repair packets, reports, and handoffs.\n@@ -15,6 +17,6 @@\n \n - Use Shiki terms exactly when they exist.\n - Add glossary terms to `CONTEXT.md` only when the term is part of the domain, not a generic programming concept.\n-- Create ADRs only for hard-to-reverse, surprising, tradeoff-based decisions.\n+- Create SADRs or target ADRs only for hard-to-reverse, surprising, tradeoff-based decisions.\n - Keep product-specific language in the Target Repository, not in Shiki Template docs.\n-- If an ADR conflicts with a proposed implementation, surface the conflict before editing.\n+- If a SADR or target ADR conflicts with a proposed implementation, surface the conflict before editing.\ndiff --git a/docs/agents/execution-control.md b/docs/agents/execution-control.md\nindex ad8ee3cdb36be56fedc1f46408ca92b387d74047..bc63c3a73661d12b567016ba299f1a6b66bf9088\n--- a/docs/agents/execution-control.md\n+++ b/docs/agents/execution-control.md\n@@ -63,7 +63,7 @@\n | Branch name | Orchestrator | Orchestrator | Before implementer dispatch | branch policy | branch ref in task/ledger |\n | Lock reservation | Context & Impact | MergeGate | Before dispatch and before merge | `.shiki/locks/`, labels, PR metadata | lock id, owner, scope |\n | CI profile | Planner | MergeGate | During issue triage, rechecked at PR | labels, `.shiki/policy.yaml`, workflow matrix | required check list |\n-| Required status checks | Repo maintainer / Guardian | GitHub branch protection / rulesets | Repository setup, updated via ADR | GitHub settings | branch/ruleset config, ADR |\n+| Required status checks | Repo maintainer / Guardian | GitHub branch protection / rulesets | Repository setup, updated via target ADR | GitHub settings | branch/ruleset config, target ADR |\n | CCA judgment | CCA | GitHub required check / MergeGate | Every non-draft PR update | GitHub Action check | structured CCA verdict |\n | Repair assignment | CCA | MergeGate | After failed CCA/CI/review | repair packet, issue/PR comment | repair id and attempt count |\n | Merge readiness | MergeGate | GitHub branch protection / Guardian if needed | After CCA complete | required checks, review rules, rulesets | mergegate check output |\ndiff --git a/docs/agents/guardian-policy.md b/docs/agents/guardian-policy.md\nindex 4147c7adf05a47adb4fd24ada1a1c2e1f2ba6d2d..a20d9cc6c00a569bb057b9ca8d6b51c7fc4a9847\n--- a/docs/agents/guardian-policy.md\n+++ b/docs/agents/guardian-policy.md\n@@ -98,8 +98,8 @@\n `origin/<default_branch>` and reuses each job's existing base fetch; a shallow\n (`--depth`) or second base fetch would make the proof unresolvable and silently\n disable the carry, so neither is permitted. CCA reads `guardian_comment_carried` as\n-a recorded authority in the deterministic signal and does not re-derive it. See ADR\n-0018 for the full decision, the rejected diff/patch-equality and path-disjointness\n+a recorded authority in the deterministic signal and does not re-derive it. See\n+SADR-0018 for the full decision, the rejected diff/patch-equality and path-disjointness\n alternatives, and the accepted residual.\n \n ## External AI Guardian Review (SADR-0010)\ndiff --git a/docs/agents/implementation-policy.md b/docs/agents/implementation-policy.md\nindex c754144a9fb5bf4a38aa82a60e1670c59e626b44..85c37281bcad196c6b7b02343faf00b03cd9c57a\n--- a/docs/agents/implementation-policy.md\n+++ b/docs/agents/implementation-policy.md\n@@ -31,10 +31,13 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n ## Non-Negotiables\n \n 1. **Planning precedes implementation.** Non-trivial Goals start with `grill-with-docs` and Context & Impact.\n-2. **Domain language is controlled.** Terms must be checked against `CONTEXT.md`; hard-to-reverse tradeoffs must be checked against ADRs.\n+2. **Domain language is controlled.** Terms must be checked against `CONTEXT.md`; hard-to-reverse tradeoffs must be checked against Shiki SADRs and target ADRs.\n 3. **PRD precedes issue decomposition.** `to-prd` is used once the context is settled enough to become durable intent.\n 4. **Issues are vertical slices.** `to-issues` must produce independently grabbable tracer-bullet tasks, not horizontal layer tickets.\n 5. **Triage controls dispatch.** Only `ready-for-agent` / AFK tasks may be assigned to Codex without human input.\n@@ -95,19 +98,19 @@\n Process:\n \n - Challenge terms against `CONTEXT.md`.\n-- Check ADRs before reopening decisions.\n+- Check SADRs and target ADRs before reopening decisions.\n - Inspect code when code can answer a question.\n - Ask one question at a time when the operator is available.\n - Provide a recommended answer with each question.\n - Record resolved terms in `CONTEXT.md`.\n-- Create ADRs only for hard-to-reverse, surprising, tradeoff-based decisions.\n+- Create SADRs or target ADRs only for hard-to-reverse, surprising, tradeoff-based decisions.\n - Convert unresolved load-bearing questions into HITL blockers.\n \n Outputs:\n \n - Resolved design-tree decisions.\n - Updated glossary terms when needed.\n-- ADRs when justified.\n+- SADRs or target ADRs when justified.\n - HITL blockers when decisions remain unresolved.\n \n ## Phase 3 \u2014 Context & Impact\n@@ -117,7 +120,7 @@\n \n Outputs must include:\n \n-- Relevant documents and ADRs.\n+- Relevant documents, SADRs, and target ADRs.\n - Relevant modules, interfaces, seams, callers, and tests.\n - Candidate locks.\n - Dependencies.\n@@ -140,7 +143,7 @@\n - Testing decisions.\n - Out of scope.\n - Further notes.\n-- Links to relevant Goal, domain terms, and ADRs.\n+- Links to relevant Goal, domain terms, SADRs, and target ADRs.\n \n Phase 4 ends with Spec Freeze: the operator explicitly approves the PRD, the\n plan records a `spec_freeze` block (status, approved_by, source), and the SF\n@@ -240,7 +243,7 @@\n - Dependencies and locks.\n - Risk and Guardian requirements.\n - Ledger completeness.\n-- Documentation and ADR updates.\n+- Documentation and SADR/target-ADR updates.\n - Scope drift.\n \n CCA emits one of:\n@@ -301,7 +304,7 @@\n | Evidence failure | Behavior might be done, but proof is missing. | Add checks, PR evidence, ledger entry, or artifacts. |\n | Scope drift | PR changes unrelated behavior. | Revert unrelated changes or split into new Goal. |\n | Dependency block | Upstream task or lock not complete. | Block until dependency resolves. |\n-| Architecture block | Decision needs ADR or deep-module discussion. | `grill-with-docs` or `improve-codebase-architecture`. |\n+| Architecture block | Decision needs a SADR, target ADR, or deep-module discussion. | `grill-with-docs` or `improve-codebase-architecture`. |\n | Risk block | Security/production/policy change needs human approval. | Guardian decision. |\n \n ## Phase 5.5 \u2014 Execution Decision Control\ndiff --git a/docs/agents/runtime-auth-model.md b/docs/agents/runtime-auth-model.md\nindex 6a2e1a47e81ea89b5c68a210ec07772a9374babc..3c54748d24233dbd0ba8473950282547ef4ce935\n--- a/docs/agents/runtime-auth-model.md\n+++ b/docs/agents/runtime-auth-model.md\n@@ -148,7 +148,7 @@\n \n API-key based runners may be added by a target repository as an explicit extension. They are not the default Shiki template path.\n \n-If a repo opts into API-key mode, record the exception in an ADR and update the repo's `AGENTS.md`, `.shiki/config.yaml`, and workflow permissions.\n+If a repo opts into API-key mode, record the target-specific exception in a target ADR and update the repo's `AGENTS.md`, `.shiki/config.yaml`, and workflow permissions.\n \n ## GitHub Actions Runtime Compatibility\n \ndiff --git a/docs/agents/skill-gate.md b/docs/agents/skill-gate.md\nindex c50398b0d7c60a8598f2a5c88a10015730dced13..3acffe6444617bb1d54b77d4b4708158ec735bc6\n--- a/docs/agents/skill-gate.md\n+++ b/docs/agents/skill-gate.md\n@@ -9,7 +9,7 @@\n | Skill | Trigger | Expected output |\n | --- | --- | --- |\n | `setup-matt-pocock-skills` | First configuring a repo for these skills, or when issue tracker, triage labels, or domain docs are missing. | `docs/agents/` configuration and an `Agent skills` block in `AGENTS.md` or `CLAUDE.md`. |\n-| `grill-with-docs` | Ambiguous plan, unclear terminology, boundary decision, tradeoff, or ADR-worthy design choice. | Clarified decision tree, recommended answers, updates to `CONTEXT.md` or ADRs when decisions crystallize. |\n+| `grill-with-docs` | Ambiguous plan, unclear terminology, boundary decision, tradeoff, or decision-record-worthy design choice. | Clarified decision tree, recommended answers, updates to `CONTEXT.md`, SADRs, or target ADRs when decisions crystallize. |\n | `zoom-out` | Agent lacks a higher-level architectural map. | Map of relevant modules, callers, seams, and domain concepts. |\n | `to-prd` | Settled context should become a PRD. | PRD published to the issue tracker with problem, solution, user stories, decisions, testing, and out-of-scope notes. |\n | `to-issues` | Goal, PRD, or plan must become implementation tickets. | Vertical-slice issues with acceptance criteria, dependencies, and AFK/HITL classification. |\n@@ -17,7 +17,7 @@\n | `tdd` | Feature work or bug fix has observable behavior that can be specified with tests. | Red-green-refactor loop through public interfaces, one vertical slice at a time. |\n | `code-review` | The TDD loop is green and the implementer is about to create or update a PR. Default for every implementation task. | Pre-PR self-review of the task diff with fixes applied on the task branch; findings and resolutions recorded as a ledger entry naming the skill and a `## Pre-PR code review` PR body section. |\n | `diagnose` | Hard bug, failing check, regression, flaky behavior, or performance problem. | Reproduction loop, ranked hypotheses, instrumentation, fix, regression test, post-mortem. |\n-| `improve-codebase-architecture` | Structural friction, testability, AI-navigability, deep-module opportunity, or refactor request. | Architecture candidates and deepening recommendations informed by `CONTEXT.md` and ADRs. |\n+| `improve-codebase-architecture` | Structural friction, testability, AI-navigability, deep-module opportunity, or refactor request. | Architecture candidates and deepening recommendations informed by `CONTEXT.md`, SADRs, and target ADRs. |\n | `prototype` | A throwaway logic or UI prototype is needed to answer a design question. | Clearly marked disposable prototype and captured decision. |\n \n ## MergeGate Interaction\ndiff --git a/docs/operations.md b/docs/operations.md\nindex cf1f452d4c589e38ccd58553bda0ad8684b51eb7..ace48a90ffbec9e5c0a7113bef16a4858bc44b3f\n--- a/docs/operations.md\n+++ b/docs/operations.md\n@@ -89,6 +89,16 @@\n      target history, brought forward by `shiki migrate`, not a template.\n    - **Everything else** (CLI scripts, workflows, schemas, docs) is overwritten.\n \n+   A forced upgrade also migrates the former numeric Shiki decision paths to\n+   `SADR-NNNN-*`. Before the first write, the installer inventories the exact\n+   legacy Shiki paths and authorizes each deletion only when the existing\n+   `.shiki/install-stamp.json` names that path and its SHA-256 digest still\n+   matches the file. An absent, unreadable, malformed, incomplete, or\n+   mismatching stamp reports every blocked path and stops with no filesystem\n+   change. Target-owned `NNNN-*` ADRs are not cleanup candidates. A non-force\n+   install deletes nothing and retains any still-matching legacy stamp entries\n+   so a later forced upgrade can prove ownership.\n+\n    If the target has pending migrations, `--force` refuses and writes nothing \u2014\n    apply migrations first (step 3), then re-run. The run ends with a single\n    summary of every `.new` written, so a half-upgrade is visible.\ndiff --git a/docs/releasing.md b/docs/releasing.md\nindex 10d73828e7dbd64e9d9f6b83bc84d010537ce62a..aafeee8c4d441e1e427efee2ce4f81a647f020e1\n--- a/docs/releasing.md\n+++ b/docs/releasing.md\n@@ -67,7 +67,7 @@\n CLI in `scripts/`. There are no third-party Python runtime dependencies. The CLI\n is intentionally **not** exposed as a `[project.scripts]` console entry point\n because `scripts/shiki.py` is a dependency-free shim and `scripts/` is not\n-packaged as an importable distribution (see the ADR for the rationale).\n+packaged as an importable distribution (see SADR-0007 for the rationale).\n \n Supported install paths:\n \n@@ -155,6 +155,15 @@\n    [docs/operations.md](operations.md#the-new-convention). If the target has\n    pending migrations, `--force` refuses and writes nothing; apply migrations\n    (step 2) first, then re-run.\n+\n+   The SADR namespace upgrade has an additional pre-write gate: each former\n+   numeric Shiki decision path is removed only when the existing install stamp\n+   contains that exact path and a digest matching its current bytes. The\n+   installer aggregates absent/unreadable/malformed-stamp, missing-digest, and\n+   digest-mismatch blockers before raising, so refusal leaves the target tree\n+   byte-identical. Numeric target ADRs are preserved. A successful forced\n+   upgrade installs and stamps `SADR-*` paths and drops the removed legacy paths\n+   from the refreshed stamp.\n 2. Apply repository-local `.shiki/` state migrations with the migrate command:\n \n    ```bash\ndiff --git a/docs/usage.md b/docs/usage.md\nindex b83407ee3970dd395dd893010bc5dbe8bad26051..a130ef6e40dfd8169a5bc701339351097df1a09f\n--- a/docs/usage.md\n+++ b/docs/usage.md\n@@ -67,8 +67,10 @@\n shiki install-target /path/to/target-repo --local-only\n ```\n \n-Use `--force` only when you intentionally want to overwrite existing target\n-files.\n+Use `--force` only when you intentionally want to refresh the target template.\n+For the SADR namespace migration, `--force` deletes only exact legacy Shiki\n+decision paths whose existing install-stamp digest still matches; any ownership\n+blocker stops before the first write. Numeric target ADRs are never candidates.\n \n ## `shiki bootstrap-platform` \u2014 publish the Shiki platform repo\n \ndiff --git a/scripts/guardian_approval_signal.py b/scripts/guardian_approval_signal.py\nindex d059f92b2c82aa2b5d6ca421365537a87886c1cb..16924892714f4f5b9303c0ef0e325a7e70075630\n--- a/scripts/guardian_approval_signal.py\n+++ b/scripts/guardian_approval_signal.py\n@@ -199,8 +199,8 @@\n ) -> Any:\n     \"\"\"Evaluate SADR-0015 Contract Approval for the CCA Guardian signal.\n \n-    The SAME OR that MergeGate applies must reach this deterministic signal: ADR\n-    0015 is explicit that without it, a contract-approved PR still returns\n+    The SAME OR that MergeGate applies must reach this deterministic signal:\n+    SADR-0015 is explicit that without it, a contract-approved PR still returns\n     ``needs_guardian``, the CCA job fails, and the MergeGate policy check never\n     runs \u2014 the gate this change moves would never be reached. This resolves the\n     task the SAME way MergeGate does (the first Shiki task id in the PR body),\ndiff --git a/scripts/shiki_contract_approval.py b/scripts/shiki_contract_approval.py\nindex 941bcb96fdd8b9a85bc0c4f0f04d5dad9d04839c..4a4b418f6527413161db82670bec857a043360e2\n--- a/scripts/shiki_contract_approval.py\n+++ b/scripts/shiki_contract_approval.py\n@@ -142,7 +142,7 @@\n     changed_files_status: Any,\n     registration: Any,\n ) -> ContractApprovalResult:\n-    \"\"\"Whether this PR carries an SADR-0015 Contract Approval.\n+    \"\"\"Whether this PR carries a SADR-0015 Contract Approval.\n \n     The four conditions are evaluated IN ORDER, each failure recorded as a\n     distinct reason string:\ndiff --git a/scripts/shiki_guardian.py b/scripts/shiki_guardian.py\nindex a4ecb79bbda55fd5c8b06adfae2eb99760a03c45..4dd99bbe714e26f421fa19abac7a34ae1b195ecc\n--- a/scripts/shiki_guardian.py\n+++ b/scripts/shiki_guardian.py\n@@ -12,7 +12,7 @@\n # The carry re-derives its proof from git in-process on every judging invocation;\n # it is never read from an artifact, so there is nothing to go stale or be forged.\n # shiki_sync_proof is the ONLY guardian-family module that runs git, and every one\n-# of its invocations is read-only and local (ADR: prove-from-git base sync).\n+# of its invocations is read-only and local (SADR: prove-from-git base sync).\n from shiki_sync_proof import verify_pure_base_sync\n \n GUARDIAN_POLICY_PATH = \".shiki/guardian-policy.json\"\ndiff --git a/scripts/shiki_guardian_review.py b/scripts/shiki_guardian_review.py\nindex dd0f104d457899e0ba5ff1e162011c1e90b6f92b..e10ef58c046324e7ea3a3392feb3048ca77414ce\n--- a/scripts/shiki_guardian_review.py\n+++ b/scripts/shiki_guardian_review.py\n@@ -159,7 +159,7 @@\n     docs_only = _is_docs_only(changed_files)\n     if docs_only and matched == 0:\n         focus.append(\n-            \"Docs/ADR-only change: confirm the change is genuinely documentation/decision-only, introduces no behavior or contract drift, and stays consistent with the constitution and existing ADRs.\"\n+            \"Docs/decision-record-only change: confirm the change is genuinely documentation/decision-only, introduces no behavior or contract drift, and stays consistent with the constitution and existing SADRs and target ADRs.\"\n         )\n \n     # Conservative augmentation: unknown (no category and not docs-only), mixed\ndiff --git a/scripts/shiki_installer.py b/scripts/shiki_installer.py\nindex be064ab7a29ff6836f7f7a1d70500a43e0d11d7b..9fd8eefd137ce7003dad5c0dcb2e48e2cd47ab32\n--- a/scripts/shiki_installer.py\n+++ b/scripts/shiki_installer.py\n@@ -118,6 +118,7 @@\n # what lets a target carry it without a governance-schema change.\n INSTALL_STAMP_PATH = \".shiki/install-stamp.json\"\n INSTALL_STAMP_VERSION = 1\n+LEGACY_SHIKI_SADR_MAX = 18\n \n # Under --force the shipped surface splits three ways, so nothing is lost and\n # nothing is silently stale.\n@@ -237,9 +238,98 @@\n         )\n \n \n+def legacy_shiki_adr_paths() -> tuple[str, ...]:\n+    \"\"\"Former numeric paths for Shiki SADR-0001 through SADR-0018.\n+\n+    The authoritative slugs come from the current SADR filenames. SADR-0019 is\n+    the namespace decision itself and never shipped under a numeric legacy path.\n+    \"\"\"\n+    paths: list[str] = []\n+    for sadr in sorted((ROOT / \"docs\" / \"adr\").glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\")):\n+        number = sadr.name.split(\"-\", 2)[1]\n+        if int(number) <= LEGACY_SHIKI_SADR_MAX:\n+            paths.append(f\"docs/adr/{sadr.name.removeprefix('SADR-')}\")\n+    return tuple(paths)\n+\n+\n+def preflight_legacy_sadr_cleanup(target: Path, *, force: bool) -> tuple[str, ...]:\n+    \"\"\"Authorize exact legacy Shiki ADR removals before the first write.\n+\n+    A non-force install never removes existing files. A forced install needs no\n+    stamp when no legacy Shiki path is present. When legacy paths do exist, each\n+    must be named in a readable install stamp and its current bytes must match\n+    the recorded digest.\n+    \"\"\"\n+    if not force:\n+        return ()\n+    present = tuple(\n+        relative\n+        for relative in legacy_shiki_adr_paths()\n+        if (target / relative).exists() or (target / relative).is_symlink()\n+    )\n+    if not present:\n+        return ()\n+\n+    stamp_path = target / INSTALL_STAMP_PATH\n+    try:\n+        raw = stamp_path.read_text(encoding=\"utf-8\")\n+        stamp = json.loads(raw)\n+    except FileNotFoundError:\n+        detail = \"install stamp is absent\"\n+        blockers = [f\"{relative}: {detail}\" for relative in present]\n+    except (OSError, UnicodeDecodeError) as error:\n+        detail = f\"install stamp is unreadable ({error})\"\n+        blockers = [f\"{relative}: {detail}\" for relative in present]\n+    except json.JSONDecodeError as error:\n+        detail = f\"install stamp is malformed ({error})\"\n+        blockers = [f\"{relative}: {detail}\" for relative in present]\n+    else:\n+        if not isinstance(stamp, dict):\n+            blockers = [\n+                f\"{relative}: install stamp is malformed (expected an object)\"\n+                for relative in present\n+            ]\n+        else:\n+            digests = stamp.get(\"digests\")\n+            blockers = []\n+            if not isinstance(digests, dict):\n+                blockers.extend(\n+                    f\"{relative}: install stamp has no digest for this path\"\n+                    for relative in present\n+                )\n+            else:\n+                for relative in present:\n+                    expected = digests.get(relative)\n+                    if not isinstance(expected, str) or not expected:\n+                        blockers.append(\n+                            f\"{relative}: install stamp has no digest for this path\"\n+                        )\n+                        continue\n+                    try:\n+                        actual = _sha256_file(target / relative)\n+                    except OSError as error:\n+                        blockers.append(\n+                            f\"{relative}: legacy path is unreadable ({error})\"\n+                        )\n+                        continue\n+                    if actual != expected:\n+                        blockers.append(\n+                            f\"{relative}: digest mismatch (stamp {expected}, current {actual})\"\n+                        )\n+\n+    if blockers:\n+        joined = \"\\n\".join(f\"- {blocker}\" for blocker in blockers)\n+        raise ShikiError(\n+            \"legacy Shiki ADR cleanup is not authorized:\\n\"\n+            f\"{joined}\\nNo files were written.\"\n+        )\n+    return present\n+\n+\n def install_template(target: Path, *, force: bool, validate: bool) -> list[NewFileNote]:\n     # Preconditions first: never rewrite the tree of a target that cannot be\n     # upgraded cleanly (see refuse_pending_migrations).\n+    legacy_cleanup = preflight_legacy_sadr_cleanup(target, force=force)\n     refuse_pending_migrations(target)\n \n     # Resolve the target's required CODEOWNERS owner once from its\n@@ -250,6 +340,10 @@\n     # verbatim. See ``codeowners_required_owner``.\n     codeowners_owner = codeowners_required_owner(target)\n \n+    for relative in legacy_cleanup:\n+        (target / relative).unlink()\n+        info(f\"removed stamped legacy Shiki ADR: {target / relative}\")\n+\n     notes: list[NewFileNote] = []\n     for relative in TEMPLATE_PATHS:\n         source = ROOT / relative\n@@ -647,6 +741,22 @@\n     digests = target_stamp_digests(target)\n     existing = read_install_stamp(target)\n     if existing is not None:\n+        # A non-force install deliberately leaves legacy Shiki ADRs in place.\n+        # Preserve only still-matching ownership proof so a later forced\n+        # upgrade can authorize cleanup; never carry a missing or stale digest.\n+        existing_digests = existing.get(\"digests\")\n+        if isinstance(existing_digests, dict):\n+            for relative in legacy_shiki_adr_paths():\n+                expected = existing_digests.get(relative)\n+                path = target / relative\n+                if not isinstance(expected, str) or not path.is_file():\n+                    continue\n+                try:\n+                    actual = _sha256_file(path)\n+                except OSError:\n+                    continue\n+                if actual == expected:\n+                    digests[relative] = actual\n         if _incoming_is_older(commit, existing.get(\"platform_commit\")):\n             warn(\n                 \"kept existing install stamp: incoming install \"\ndiff --git a/scripts/shiki_loop.py b/scripts/shiki_loop.py\nindex 3611641da8aeb5038b1da90e3114b09a473ec6d8..84caa491b9cad0edc4176f209c13e9221e975681\n--- a/scripts/shiki_loop.py\n+++ b/scripts/shiki_loop.py\n@@ -424,7 +424,7 @@\n         # unresolvable CCA verdict has already produced its terminal stop and is never\n         # laundered into a head-moving sync (SADR-0010). It also never fires while the\n         # policy (Guardian) gate is red \u2014 for ANY reason, not only when it is the sole\n-        # red check \u2014 so a head-bound Guardian approval, or an SADR-0015\n+        # red check \u2014 so a head-bound Guardian approval, or a SADR-0015\n         # contract-approval carry that base movement defeated (the policy gate compares\n         # base and head task governance directly), is never silently restored by\n         # adopting base wholesale. It is bounded by a durable per-task counter so the\ndiff --git a/scripts/shiki_tasks.py b/scripts/shiki_tasks.py\nindex aec3809f24b4a0c8c96fc50971cff9ae9ad30381..d531052e9d9717058d4631483cefab44c26a65c1\n--- a/scripts/shiki_tasks.py\n+++ b/scripts/shiki_tasks.py\n@@ -821,7 +821,7 @@\n         \"prompt\": prompt,\n         \"entry_skill\": \"grill-with-docs\",\n         \"required_next_steps\": [\n-            \"Run grill-with-docs until terminology, boundaries, risks, and ADR-worthy decisions are settled.\",\n+            \"Run grill-with-docs until terminology, boundaries, risks, and decision-record-worthy choices are settled.\",\n             \"Obtain the operator's explicit approval of the PRD/requirements (Spec Freeze).\",\n             \"Write a machine-readable plan JSON with grill_with_docs.status=complete and spec_freeze.status=frozen.\",\n             \"Run shiki plan ingest --plan-file PLAN.json.\",\ndiff --git a/scripts/test_shiki_goal_loop.sh b/scripts/test_shiki_goal_loop.sh\nindex 753c7b516e10f0be3bc92a4bd0eb1fa699776ae6..8f3820a0a079b3044424cfe1a6c6b7fe68896e67\n--- a/scripts/test_shiki_goal_loop.sh\n+++ b/scripts/test_shiki_goal_loop.sh\n@@ -231,8 +231,8 @@\n \n # High/critical risk merges autonomously when all required checks are green:\n # the \"MergeGate policy check\" required check IS the Guardian gate (it enforces\n-# guardian-policy.json \u2014 human approval OR external AI guardian review, ADR\n-# 0010), so green checks mean a recorded authority approved. The fake gh returns\n+# guardian-policy.json \u2014 human approval OR external AI guardian review,\n+# SADR-0010), so green checks mean a recorded authority approved. The fake gh returns\n # green checks, simulating that the Guardian gate passed.\n python3 \"$ROOT/scripts/shiki.py\" goal create --target \"$TARGET\" --title \"Guardian gate\" --outcome \"High risk merges via policy gate\" >/tmp/shiki-goal-loop-guardian-goal.json\n GGOAL=\"$(json_get /tmp/shiki-goal-loop-guardian-goal.json goal_id)\"\ndiff --git a/tests/test_adr_references.py b/tests/test_adr_references.py\nindex 51706329c797d5dd9b47e082a333fa83b261d482..d005da4f771f5e8085ec22d4b787980e68f5ae8c\n--- a/tests/test_adr_references.py\n+++ b/tests/test_adr_references.py\n@@ -1,8 +1,8 @@\n-\"\"\"Every ``ADR ####`` citation must resolve to exactly one decision record.\n+\"\"\"Every target ADR and Shiki SADR citation resolves in its own namespace.\n \n Background. ``scripts/mergegate_check.py`` justifies ``is_bookkeeping_closeout``\n-with \"ADR 0017\" nine times and ``scripts/guardian_approval_signal.py`` cites\n-0016/0017 seven times, yet for a stretch the numbering in ``docs/adr/`` stopped\n+with \"SADR-0017\" nine times and ``scripts/guardian_approval_signal.py`` cites\n+SADR-0016/SADR-0017 seven times, yet for a stretch the numbering in ``docs/adr/`` stopped\n at 0015: the authoring commits lived on a coordinator branch and no task ever\n declared ``docs/adr/...`` in its locks, so nothing carried the records to the\n default branch. The stated basis of an exemption that merged seven PRs with zero\n@@ -11,11 +11,12 @@\n and the directory drifted with no signal.\n \n This is the same unbound-pair shape behind other silent failures: a reference\n-and its target with nothing asserting they agree. This test binds them. It scans\n-every ``scripts/*.py`` (plus ``docs/**/*.md`` and the root ``*.md`` governance\n-files, which are cheap) for the ``ADR ####`` pattern and asserts each cited\n-number resolves to exactly one ``docs/adr/####-*.md``. The converse is\n-deliberately NOT required: an ADR may exist without being cited.\n+and its target with nothing asserting they agree. This test binds them. Target\n+``ADR NNNN`` citations resolve only to ``docs/adr/NNNN-*.md``; Shiki\n+``SADR-NNNN`` citations resolve only to ``docs/adr/SADR-NNNN-*.md``. The scan\n+covers current governance docs, scripts, workflows, prompts, packaging metadata,\n+and repo-local Codex guidance while deliberately excluding historical\n+``.shiki`` evidence. The converse is not required: a record may be uncited.\n \n The suite is dependency free: standard-library ``unittest`` only.\n \"\"\"\n@@ -31,17 +32,18 @@\n \n REPO_ROOT = Path(__file__).resolve().parents[1]\n \n-# \"ADR 0016\" -- a four-digit citation. The trailing \\b keeps a five-digit token\n-# from being read as a truncated four-digit number.\n-_CITATION = re.compile(r\"\\bADR (\\d{4})\\b\")\n-# \"0016-locks-bound-product-paths.md\" -- the leading four digits name the record.\n-_ADR_FILENAME = re.compile(r\"^(\\d{4})-.*\\.md$\")\n+# ``ADR 0016`` / ``SADR-0016`` -- a namespace plus four-digit citation. The\n+# trailing ``\\b`` keeps a five-digit token from being read as a truncated one.\n+_CITATION = re.compile(r\"\\b(S?ADR)[ -](\\d{4})\\b\")\n+# Target ADRs retain the numeric filename; Shiki platform decisions use SADR.\n+_DECISION_FILENAME = re.compile(r\"^(?:(SADR)-)?(\\d{4})-.*\\.md$\")\n \n \n class Violation(NamedTuple):\n     \"\"\"A citation that does not resolve to exactly one decision record.\"\"\"\n \n     citing_file: str  # repo-relative path of the file that cites the ADR\n+    namespace: str  # \"ADR\" for target decisions; \"SADR\" for Shiki decisions\n     number: str  # the cited four-digit number, e.g. \"0016\"\n     line: int  # 1-based line of the citation\n     reason: str  # \"missing\" (zero matches) or \"ambiguous\" (two or more)\n@@ -49,44 +51,59 @@\n \n     @property\n     def expected_path(self) -> str:\n-        return f\"docs/adr/{self.number}-*.md\"\n+        prefix = \"SADR-\" if self.namespace == \"SADR\" else \"\"\n+        return f\"docs/adr/{prefix}{self.number}-*.md\"\n \n     def describe(self) -> str:\n         \"\"\"Name the citing file, the cited number, and the missing path.\"\"\"\n         if self.reason == \"missing\":\n             return (\n-                f\"{self.citing_file}:{self.line} cites ADR {self.number} \"\n+                f\"{self.citing_file}:{self.line} cites {self.namespace} {self.number} \"\n                 f\"but no document matches {self.expected_path}\"\n             )\n         joined = \", \".join(self.matches)\n         return (\n-            f\"{self.citing_file}:{self.line} cites ADR {self.number} which \"\n+            f\"{self.citing_file}:{self.line} cites {self.namespace} {self.number} which \"\n             f\"resolves to {len(self.matches)} documents (expected exactly one \"\n             f\"at {self.expected_path}): {joined}\"\n         )\n \n \n-def _adr_index(root: Path) -> dict[str, list[Path]]:\n-    \"\"\"Map every four-digit ADR number to the record files that claim it.\"\"\"\n-    index: dict[str, list[Path]] = {}\n+def _adr_index(root: Path) -> dict[tuple[str, str], list[Path]]:\n+    \"\"\"Map each decision namespace and number to the files that claim it.\"\"\"\n+    index: dict[tuple[str, str], list[Path]] = {}\n     adr_dir = root / \"docs\" / \"adr\"\n     if adr_dir.is_dir():\n         for path in sorted(adr_dir.glob(\"*.md\")):\n-            match = _ADR_FILENAME.match(path.name)\n+            match = _DECISION_FILENAME.match(path.name)\n             if match:\n-                index.setdefault(match.group(1), []).append(path)\n+                namespace = \"SADR\" if match.group(1) else \"ADR\"\n+                index.setdefault((namespace, match.group(2)), []).append(path)\n     return index\n \n \n def _citing_files(root: Path, include_docs: bool = True) -> list[Path]:\n-    \"\"\"Files scanned for citations: scripts/*.py, and cheaply the markdown.\"\"\"\n+    \"\"\"Current, shipped text surfaces scanned for decision citations.\"\"\"\n     files = sorted((root / \"scripts\").glob(\"*.py\"))\n+    files += sorted((root / \"scripts\").glob(\"*.sh\"))\n     if include_docs:\n         docs = root / \"docs\"\n         if docs.is_dir():\n             files += sorted(docs.rglob(\"*.md\"))\n         files += sorted(root.glob(\"*.md\"))\n-    return files\n+        pyproject = root / \"pyproject.toml\"\n+        if pyproject.is_file():\n+            files.append(pyproject)\n+        for relative in (\".github\", \".codex\"):\n+            surface = root / relative\n+            if surface.is_dir():\n+                files += sorted(\n+                    path\n+                    for path in surface.rglob(\"*\")\n+                    if path.is_file()\n+                    and path.suffix in {\".md\", \".py\", \".sh\", \".toml\", \".yaml\", \".yml\"}\n+                )\n+    return sorted(set(files))\n \n \n def find_adr_reference_violations(\n@@ -94,10 +111,9 @@\n ) -> list[Violation]:\n     \"\"\"Return every ``ADR ####`` citation under ``root`` that fails to resolve.\n \n-    A citation resolves when exactly one ``docs/adr/####-*.md`` matches its\n-    number. Zero matches is a \"missing\" violation; two or more is \"ambiguous\".\n-    Records that are never cited produce nothing -- the converse is not\n-    required.\n+    A citation resolves when exactly one record in the same namespace matches\n+    its number. Zero matches is a \"missing\" violation; two or more is\n+    \"ambiguous\". Records that are never cited produce nothing.\n     \"\"\"\n     root = Path(root)\n     index = _adr_index(root)\n@@ -110,13 +126,15 @@\n         rel = path.relative_to(root).as_posix()\n         for lineno, line in enumerate(text.splitlines(), start=1):\n             for match in _CITATION.finditer(line):\n-                number = match.group(1)\n-                found = index.get(number, [])\n+                namespace, number = match.groups()\n+                found = index.get((namespace, number), [])\n                 if len(found) == 1:\n                     continue\n                 reason = \"missing\" if not found else \"ambiguous\"\n                 rels = [p.relative_to(root).as_posix() for p in found]\n-                violations.append(Violation(rel, number, lineno, reason, rels))\n+                violations.append(\n+                    Violation(rel, namespace, number, lineno, reason, rels)\n+                )\n     return violations\n \n \n@@ -129,8 +147,8 @@\n         self.assertEqual(\n             violations,\n             [],\n-            \"Every 'ADR ####' citation must resolve to exactly one \"\n-            f\"docs/adr/####-*.md:\\n{detail}\",\n+            \"Every ADR/SADR citation must resolve exactly once in its own \"\n+            f\"docs/adr namespace:\\n{detail}\",\n         )\n \n     def test_scan_actually_reads_scripts(self) -> None:\n@@ -142,12 +160,38 @@\n             \"the citation scan must cover scripts/*.py\",\n         )\n \n+    def test_platform_citations_do_not_use_unresolved_numeric_shorthand(self) -> None:\n+        shorthand = re.compile(r\"SADR-\\d{4}/\\d{4}\")\n+        offenders = []\n+        for path in _citing_files(REPO_ROOT):\n+            text = path.read_text(encoding=\"utf-8\")\n+            if shorthand.search(text):\n+                offenders.append(path.relative_to(REPO_ROOT).as_posix())\n+        self.assertEqual(\n+            offenders,\n+            [],\n+            \"repeat the SADR prefix so every cited number is independently resolvable\",\n+        )\n+\n     def test_carried_records_are_indexed(self) -> None:\n         # The two records this Goal carried onto the default branch, and which\n         # the scripts cite most, must be present.\n         index = _adr_index(REPO_ROOT)\n-        self.assertIn(\"0016\", index)\n-        self.assertIn(\"0017\", index)\n+        self.assertIn((\"SADR\", \"0016\"), index)\n+        self.assertIn((\"SADR\", \"0017\"), index)\n+\n+    def test_platform_tree_has_only_sadr_0001_through_0019(self) -> None:\n+        adr_dir = REPO_ROOT / \"docs\" / \"adr\"\n+        sadrs = sorted(adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\"))\n+        self.assertEqual(\n+            {path.name.split(\"-\", 2)[1] for path in sadrs},\n+            {f\"{number:04d}\" for number in range(1, 20)},\n+        )\n+        self.assertEqual(\n+            list(adr_dir.glob(\"[0-9][0-9][0-9][0-9]-*.md\")),\n+            [],\n+            \"the Shiki platform tree must not retain legacy numeric SADR paths\",\n+        )\n \n \n class FixtureAdrReferences(unittest.TestCase):\n@@ -180,6 +224,26 @@\n         self.assertIn(\"9991\", message)  # cited number\n         self.assertIn(\"docs/adr/9991-*.md\", message)  # missing path\n \n+    def test_target_adr_does_not_satisfy_missing_platform_sadr(self) -> None:\n+        root = self._make_repo(\n+            adr_names=[\"0042-target-decision.md\"],\n+            script_body='\"\"\"Target ADR 0042 exists, but platform SADR-0042 does not.\"\"\"\\n',\n+        )\n+        violations = find_adr_reference_violations(root, include_docs=False)\n+        self.assertEqual(len(violations), 1)\n+        self.assertEqual(violations[0].namespace, \"SADR\")\n+        self.assertEqual(violations[0].number, \"0042\")\n+        self.assertEqual(violations[0].expected_path, \"docs/adr/SADR-0042-*.md\")\n+\n+    def test_target_adr_and_platform_sadr_may_reuse_a_number(self) -> None:\n+        root = self._make_repo(\n+            adr_names=[\"0042-target-decision.md\", \"SADR-0042-platform-decision.md\"],\n+            script_body='\"\"\"Target ADR 0042 and platform SADR-0042 are distinct.\"\"\"\\n',\n+        )\n+        self.assertEqual(\n+            find_adr_reference_violations(root, include_docs=False), []\n+        )\n+\n     def test_uncited_adr_is_allowed(self) -> None:\n         # An ADR exists but nothing cites it -> not a violation. The converse\n         # (every ADR must be cited) is explicitly NOT required.\ndiff --git a/tests/test_guardian_review.py b/tests/test_guardian_review.py\nindex 88829e7810212635e2fc98aa5918d6f26294677d..00a85a237acd5bf2d6cb5b294128198c422402ca\n--- a/tests/test_guardian_review.py\n+++ b/tests/test_guardian_review.py\n@@ -162,7 +162,7 @@\n \n     def test_docs_only_is_light_and_not_conservative(self):\n         focus = gr.classify_review_focus([\"docs/adr/0099-x.md\", \"README.md\"], \"\", \"low\")\n-        self.assertTrue(any(\"Docs/ADR-only\" in f for f in focus))\n+        self.assertTrue(any(\"Docs/decision-record-only\" in f for f in focus))\n         self.assertFalse(any(\"conservative default\" in f.lower() for f in focus))\n \n     def test_unknown_category_is_conservative(self):\ndiff --git a/tests/test_policy_job_merged_prs.py b/tests/test_policy_job_merged_prs.py\nindex d45512e74681e5e649279236c7d4e79d5c3da471..b97989f8bd74d091de1a6b5275a20d8dc68c4512\n--- a/tests/test_policy_job_merged_prs.py\n+++ b/tests/test_policy_job_merged_prs.py\n@@ -3,8 +3,8 @@\n \n ``.github/workflows/shiki-cca-completion.yml`` has two consumers of the merged-PR\n proof: the CCA *signal* (``guardian_approval_signal.py`` in \"Prepare CCA context\")\n-and the authoritative gate (``mergegate_check.py`` in \"Enforce MergeGate\"). ADR\n-0017's bookkeeping-closeout exemption (condition 6) needs the merged implementation\n+and the authoritative gate (``mergegate_check.py`` in \"Enforce MergeGate\").\n+SADR-0017's bookkeeping-closeout exemption (condition 6) needs the merged implementation\n PR proven, and a closeout carries NO ``post_merge_reconcile`` marker. The signal\n already resolves ``MERGED_PRS`` unconditionally, but \"Enforce MergeGate\" used to\n resolve it only inside ``grep -q '<!-- shiki:post_merge_reconcile -->'``. So a\ndiff --git a/tests/test_sadr_install_migration.py b/tests/test_sadr_install_migration.py\nnew file mode 100644\nindex 0000000000000000000000000000000000000000..d51eac872b1c79ff4579c4c1401ca5d775a60d8d\n--- /dev/null\n+++ b/tests/test_sadr_install_migration.py\n@@ -0,0 +1,237 @@\n+\"\"\"SADR installation and collision-safe legacy upgrade behavior.\"\"\"\n+\n+from __future__ import annotations\n+\n+import contextlib\n+import hashlib\n+import io\n+import json\n+import shutil\n+import tempfile\n+import unittest\n+from pathlib import Path\n+\n+import shiki_test_support  # noqa: F401  (path bootstrap)\n+\n+import shiki_installer\n+from tests.test_adr_references import find_adr_reference_violations\n+\n+\n+PRODUCT_NUMBERS = tuple(range(13, 19))\n+\n+\n+def _quiet_install(target: Path, *, force: bool, validate: bool) -> None:\n+    with contextlib.redirect_stdout(io.StringIO()):\n+        shiki_installer.install_template(target, force=force, validate=validate)\n+\n+\n+def _sha256(path: Path) -> str:\n+    return hashlib.sha256(path.read_bytes()).hexdigest()\n+\n+\n+def _legacy_sadr_paths() -> tuple[str, ...]:\n+    return tuple(\n+        f\"docs/adr/{path.name.removeprefix('SADR-')}\"\n+        for path in sorted((shiki_installer.ROOT / \"docs\" / \"adr\").glob(\"SADR-*.md\"))\n+        if path.name != \"SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\"\n+    )\n+\n+\n+def _create_legacy_upgrade_fixture(\n+    target: Path,\n+) -> tuple[dict[str, bytes], tuple[str, ...]]:\n+    adr_dir = target / \"docs\" / \"adr\"\n+    adr_dir.mkdir(parents=True)\n+    product_bytes = {\n+        f\"{number:04d}-cubeshare-product-decision.md\": (\n+            f\"# CubeShare ADR {number:04d}\\n\\nDo not rewrite {number}.\\n\"\n+        ).encode()\n+        for number in PRODUCT_NUMBERS\n+    }\n+    for name, content in product_bytes.items():\n+        (adr_dir / name).write_bytes(content)\n+\n+    legacy_paths = _legacy_sadr_paths()\n+    for relative in legacy_paths:\n+        (target / relative).write_bytes(f\"legacy Shiki bytes for {relative}\\n\".encode())\n+\n+    stamp_path = target / shiki_installer.INSTALL_STAMP_PATH\n+    stamp_path.parent.mkdir(parents=True, exist_ok=True)\n+    stamp = {\n+        \"version\": shiki_installer.INSTALL_STAMP_VERSION,\n+        \"platform_commit\": None,\n+        \"installed_at\": \"2000-01-01T00:00:00+00:00\",\n+        \"digests\": {\n+            relative: _sha256(target / relative) for relative in legacy_paths\n+        },\n+    }\n+    stamp_path.write_text(\n+        json.dumps(stamp, indent=2, sort_keys=True) + \"\\n\", encoding=\"utf-8\"\n+    )\n+    return product_bytes, legacy_paths\n+\n+\n+def _tree_snapshot(root: Path) -> dict[str, tuple[str, bytes | str]]:\n+    \"\"\"Recursive byte/shape snapshot used to prove preflight is write-free.\"\"\"\n+    snapshot: dict[str, tuple[str, bytes | str]] = {}\n+    for path in sorted(root.rglob(\"*\")):\n+        relative = path.relative_to(root).as_posix()\n+        if path.is_symlink():\n+            snapshot[relative] = (\"symlink\", path.readlink().as_posix())\n+        elif path.is_dir():\n+            snapshot[relative] = (\"directory\", \"\")\n+        else:\n+            snapshot[relative] = (\"file\", path.read_bytes())\n+    return snapshot\n+\n+\n+class FreshSadrInstallTests(unittest.TestCase):\n+    def setUp(self) -> None:\n+        self.tmp = Path(tempfile.mkdtemp(prefix=\"shiki-sadr-fresh-\"))\n+        self.addCleanup(lambda: shutil.rmtree(self.tmp, ignore_errors=True))\n+        self.target = self.tmp / \"target\"\n+        self.adr_dir = self.target / \"docs\" / \"adr\"\n+        self.adr_dir.mkdir(parents=True)\n+        self.product_bytes = {\n+            f\"{number:04d}-product-decision.md\": (\n+                f\"# Product ADR {number:04d}\\n\\nTarget-owned bytes {number}.\\n\"\n+            ).encode()\n+            for number in PRODUCT_NUMBERS\n+        }\n+        for name, content in self.product_bytes.items():\n+            (self.adr_dir / name).write_bytes(content)\n+\n+    def test_fresh_install_preserves_product_adrs_and_installs_all_sadrs(self) -> None:\n+        _quiet_install(self.target, force=False, validate=True)\n+\n+        for name, expected in self.product_bytes.items():\n+            self.assertEqual((self.adr_dir / name).read_bytes(), expected)\n+\n+        sadr_numbers = {\n+            path.name.split(\"-\", 2)[1]\n+            for path in self.adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\")\n+        }\n+        self.assertEqual(sadr_numbers, {f\"{number:04d}\" for number in range(1, 20)})\n+\n+        numeric_records = {\n+            path.name\n+            for path in self.adr_dir.glob(\"[0-9][0-9][0-9][0-9]-*.md\")\n+        }\n+        self.assertEqual(numeric_records, set(self.product_bytes))\n+        stamp = json.loads(\n+            (self.target / shiki_installer.INSTALL_STAMP_PATH).read_text(\n+                encoding=\"utf-8\"\n+            )\n+        )\n+        for path in self.adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\"):\n+            relative = path.relative_to(self.target).as_posix()\n+            self.assertEqual(stamp[\"digests\"][relative], _sha256(path))\n+        self.assertEqual(find_adr_reference_violations(self.target), [])\n+\n+\n+class LegacySadrUpgradeTests(unittest.TestCase):\n+    def setUp(self) -> None:\n+        self.tmp = Path(tempfile.mkdtemp(prefix=\"shiki-sadr-upgrade-\"))\n+        self.addCleanup(lambda: shutil.rmtree(self.tmp, ignore_errors=True))\n+        self.target = self.tmp / \"target\"\n+        self.adr_dir = self.target / \"docs\" / \"adr\"\n+        self.product_bytes, self.legacy_paths = _create_legacy_upgrade_fixture(\n+            self.target\n+        )\n+\n+    def test_force_upgrade_deletes_only_stamped_legacy_sadrs(self) -> None:\n+        _quiet_install(self.target, force=True, validate=True)\n+\n+        for relative in self.legacy_paths:\n+            self.assertFalse((self.target / relative).exists(), relative)\n+        for name, expected in self.product_bytes.items():\n+            self.assertEqual((self.adr_dir / name).read_bytes(), expected)\n+\n+        sadrs = list(self.adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\"))\n+        self.assertEqual(len(sadrs), 19)\n+        refreshed = json.loads(\n+            (self.target / shiki_installer.INSTALL_STAMP_PATH).read_text(encoding=\"utf-8\")\n+        )\n+        for relative in self.legacy_paths:\n+            self.assertNotIn(relative, refreshed[\"digests\"])\n+        for path in sadrs:\n+            relative = path.relative_to(self.target).as_posix()\n+            self.assertEqual(refreshed[\"digests\"][relative], _sha256(path))\n+\n+    def test_non_force_install_never_deletes_legacy_files(self) -> None:\n+        before = {\n+            relative: (self.target / relative).read_bytes()\n+            for relative in self.legacy_paths\n+        }\n+        _quiet_install(self.target, force=False, validate=False)\n+        for relative, expected in before.items():\n+            self.assertEqual((self.target / relative).read_bytes(), expected)\n+        refreshed = json.loads(\n+            (self.target / shiki_installer.INSTALL_STAMP_PATH).read_text(encoding=\"utf-8\")\n+        )\n+        for relative in self.legacy_paths:\n+            self.assertEqual(refreshed[\"digests\"][relative], _sha256(self.target / relative))\n+\n+\n+class LegacyCleanupPreflightTests(unittest.TestCase):\n+    def setUp(self) -> None:\n+        self.tmp = Path(tempfile.mkdtemp(prefix=\"shiki-sadr-preflight-\"))\n+        self.addCleanup(lambda: shutil.rmtree(self.tmp, ignore_errors=True))\n+\n+    def test_all_ownership_blockers_abort_before_any_write(self) -> None:\n+        cases = (\n+            (\"absent\", \"install stamp is absent\"),\n+            (\"unreadable\", \"install stamp is unreadable\"),\n+            (\"malformed\", \"install stamp is malformed\"),\n+            (\"missing-digest\", \"install stamp has no digest\"),\n+            (\"digest-mismatch\", \"digest mismatch\"),\n+        )\n+        for name, expected_reason in cases:\n+            with self.subTest(name=name):\n+                target = self.tmp / name\n+                _, legacy_paths = _create_legacy_upgrade_fixture(target)\n+                stamp_path = target / shiki_installer.INSTALL_STAMP_PATH\n+\n+                if name == \"absent\":\n+                    stamp_path.unlink()\n+                elif name == \"unreadable\":\n+                    stamp_path.unlink()\n+                    stamp_path.mkdir()\n+                elif name == \"malformed\":\n+                    stamp_path.write_text(\"{not-json\\n\", encoding=\"utf-8\")\n+                else:\n+                    stamp = json.loads(stamp_path.read_text(encoding=\"utf-8\"))\n+                    if name == \"missing-digest\":\n+                        stamp[\"digests\"] = {}\n+                    else:\n+                        stamp[\"digests\"] = {\n+                            relative: \"deadbeef\" for relative in legacy_paths\n+                        }\n+                    stamp_path.write_text(\n+                        json.dumps(stamp, indent=2, sort_keys=True) + \"\\n\",\n+                        encoding=\"utf-8\",\n+                    )\n+\n+                before = _tree_snapshot(target)\n+                with self.assertRaises(shiki_installer.ShikiError) as raised:\n+                    _quiet_install(target, force=True, validate=False)\n+                after = _tree_snapshot(target)\n+\n+                self.assertEqual(before, after, f\"{name} preflight changed the tree\")\n+                message = str(raised.exception)\n+                self.assertIn(expected_reason, message)\n+                self.assertIn(\"No files were written\", message)\n+                for relative in legacy_paths:\n+                    self.assertIn(relative, message)\n+\n+    def test_absent_stamp_is_allowed_when_no_legacy_path_exists(self) -> None:\n+        target = self.tmp / \"no-legacy\"\n+        target.mkdir()\n+        _quiet_install(target, force=True, validate=False)\n+        self.assertTrue(\n+            (target / \"docs\" / \"adr\" / \"SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\").is_file()\n+        )\n+\n+\n+if __name__ == \"__main__\":\n+    unittest.main()\n\n2026-08-07T08:27:09.772600Z ERROR codex_models_manager::manager: failed to renew cache TTL: missing field `base_instructions` at line 94 column 5\ndiff --git a/.codex/skills/shiki/SKILL.md b/.codex/skills/shiki/SKILL.md\nindex c8e2e85d9d75d49cc4f2d6286ee22659c4e53e75..93cded623b2c15d9f65c76491a1c2d813ac9e6c0\n--- a/.codex/skills/shiki/SKILL.md\n+++ b/.codex/skills/shiki/SKILL.md\n@@ -18,6 +18,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n Codex CLI does not register this skill as a custom `/shiki` slash command. When\n the user types `/shiki` in Codex CLI and gets \"Unrecognized command\", explain\n that this is expected: invoke Shiki in Codex with natural language such as\n@@ -69,7 +72,7 @@\n \n ## Responsibilities\n \n-- Claude Code implements and repairs by default (`shiki runner claude`, ADR 0008); Codex implements and repairs tasks explicitly assigned to `codex`.\n+- Claude Code implements and repairs by default (`shiki runner claude`, SADR-0008); Codex implements and repairs tasks explicitly assigned to `codex`.\n - Claude Code Action can act as GitHub-side CCA or reviewer.\n - CCA judges completion.\n - MergeGate authorizes state transitions and merge readiness.\ndiff --git a/.github/prompts/cca-completion-check.md b/.github/prompts/cca-completion-check.md\nindex ed203ad5f302ecc81372d8ab27d2b1232ca97497..fd89950528333ef6242e6d5443904f5392ab7e56\n--- a/.github/prompts/cca-completion-check.md\n+++ b/.github/prompts/cca-completion-check.md\n@@ -19,6 +19,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n ## Required Reading\n \n Read only the files needed to judge the PR contract. Prefer this order:\ndiff --git a/.github/prompts/codex-task-handoff.md b/.github/prompts/codex-task-handoff.md\nindex 695e211d76dea1c60e8d5bb360e78487808286a4..59441d576b01a39569722f226c854e5a740e9eb5\n--- a/.github/prompts/codex-task-handoff.md\n+++ b/.github/prompts/codex-task-handoff.md\n@@ -2,7 +2,7 @@\n \n You are Codex Front acting as the Implementer for a Shiki task.\n \n-Follow `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant ADRs, and `docs/agents/codex-handoff.md`.\n+Follow `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant Shiki SADRs and target ADRs, and `docs/agents/codex-handoff.md`.\n \n Rules:\n \ndiff --git a/AGENTS.md b/AGENTS.md\nindex 7f041100d49c2a7525f51cb0f5d696a4a6d1c78c..a06bfbabc6fa48a007389e25e12d7ed523936bed\n--- a/AGENTS.md\n+++ b/AGENTS.md\n@@ -22,7 +22,7 @@\n - **Codex Front** is an optional implementer through Codex App, Codex CLI, Codex IDE extension, or Codex Web signed in with ChatGPT OAuth/subscription auth, dispatched with `shiki runner codex` when a task is explicitly assigned to `codex`.\n - **GitHub CCA** is implemented by Claude Code Action by default, using `CLAUDE_CODE_OAUTH_TOKEN`.\n \n-Do not assume `openai/codex-action`, `OPENAI_API_KEY`, or API-key based Codex automation in the default Shiki path. API-key runners are explicit target-repo extensions and require their own ADR.\n+Do not assume `openai/codex-action`, `OPENAI_API_KEY`, or API-key based Codex automation in the default Shiki path. API-key runners are explicit target-repo extensions and require their own target ADR.\n \n Shiki is not:\n \n@@ -44,10 +44,14 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use\n+`SADR-NNNN-*.md`; target product decisions retain `NNNN-*.md` and the `ADR`\n+namespace.\n+\n Use the canonical source-of-truth order above. Runtime-specific reading orders\n are operational guidance only and must not override or reorder it.\n \n-Conversation state is not durable truth. If a decision matters, put it in GitHub, `.shiki/`, `CONTEXT.md`, or an ADR.\n+Conversation state is not durable truth. If a decision matters, put it in GitHub, `.shiki/`, `CONTEXT.md`, a Shiki SADR, or a target ADR.\n \n If GitHub and `.shiki/` disagree, surface the conflict and prefer GitHub operational state until the mirror is repaired.\n \n@@ -75,8 +79,8 @@\n Every non-trivial change follows this loop:\n \n 1. **Goal**: clarify outcome, completion conditions, non-goals, risk level, and success signals.\n-2. **grill-with-docs**: challenge the plan against domain docs, ADRs, code reality, terminology, and edge scenarios.\n-3. **Context & Impact**: find relevant docs, ADRs, code, dependencies, owners, locks, and verification surfaces.\n+2. **grill-with-docs**: challenge the plan against domain docs, Shiki SADRs, target ADRs, code reality, terminology, and edge scenarios.\n+3. **Context & Impact**: find relevant docs, SADRs, target ADRs, code, dependencies, owners, locks, and verification surfaces.\n 4. **PRD and Spec Freeze**: use `to-prd` when context is settled enough to become durable product/engineering intent. The operator's explicit approval of the PRD is Spec Freeze: the plan records a `spec_freeze` block, and no plan runs without it. Steps 1-4 form Requirements Definition \u2014 one continuous operator dialogue. After Spec Freeze, scope changes require an operator-approved Spec Amendment; non-scope-moving interpretations go to the Assumption Log.\n 5. **Issues**: use `to-issues` to create independently grabbable vertical-slice issues. Prefer AFK slices over HITL slices where possible.\n 6. **Triage**: label issues for readiness, risk, runtime, skills, and MergeGate state.\n@@ -167,7 +171,7 @@\n - Testing decisions.\n - Out-of-scope items.\n - Further notes.\n-- Links to relevant domain terms and ADRs.\n+- Links to relevant domain terms, SADRs, and target ADRs.\n \n Every executable task must include:\n \n@@ -193,7 +197,7 @@\n \n Context & Impact must identify:\n \n-- Relevant documents, domain terms, ADRs, and past decisions.\n+- Relevant documents, domain terms, SADRs, target ADRs, and past decisions.\n - Relevant modules, interfaces, seams, callers, owners, and tests.\n - Dependency relationships between tasks.\n - Candidate locks and lock conflicts.\n@@ -210,7 +214,7 @@\n | Skill | Required when |\n | --- | --- |\n | `setup-matt-pocock-skills` | First configuring the repo for skills, or when issue tracker, triage labels, or domain docs are missing. |\n-| `grill-with-docs` | Default for non-trivial Goals before PRD/issues; always required when plans, terminology, boundaries, tradeoffs, or ADR-worthy decisions are ambiguous. |\n+| `grill-with-docs` | Default for non-trivial Goals before PRD/issues; always required when plans, terminology, boundaries, tradeoffs, or decision-record-worthy choices are ambiguous. |\n | `zoom-out` | The agent lacks an architectural map of the relevant code area. |\n | `to-prd` | Settled Goal context must become durable product/engineering intent. |\n | `to-issues` | A Goal, PRD, or plan must become independently grabbable vertical-slice issues. |\n@@ -348,7 +352,7 @@\n - Three or more new files.\n - Any high-risk or critical label.\n \n-When the architecture gate triggers, run `grill-with-docs` or `improve-codebase-architecture` as appropriate and record durable decisions in an ADR when needed.\n+When the architecture gate triggers, run `grill-with-docs` or `improve-codebase-architecture` as appropriate and record durable decisions in a Shiki SADR or target ADR as applicable.\n \n ## Engineering Discipline\n \ndiff --git a/CLAUDE.md b/CLAUDE.md\nindex 98a932e7841a632866fcd53b0bad0e3af3711d3d..98a31399404d6fb044b4fd493f4459ac87f439de\n--- a/CLAUDE.md\n+++ b/CLAUDE.md\n@@ -14,6 +14,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n ## Claude Role\n \n Default to planner, default implementer, reviewer, coordinator, documentation editor, CCA-style completion judge, and final-judgment assistant.\n@@ -57,7 +60,7 @@\n At the start of a Shiki session:\n \n 1. Read `AGENTS.md`.\n-2. Read `CONTEXT.md` and relevant ADRs.\n+2. Read `CONTEXT.md` and relevant Shiki SADRs and target ADRs.\n 3. Read active `.shiki/` Goal/task/ledger state when present.\n 4. Read active distilled rules from `.shiki/memories/` (status `distilled`, `active: true`, not revoked or superseded) for applicable guidance; the same rules are injected deterministically into task handoffs as the `## Distilled Rules` section (\u00a73.5).\n 5. Read the GitHub Issue or PR when the task is GitHub-backed.\n@@ -74,7 +77,7 @@\n - Define the Goal, completion conditions, non-goals, and risk level.\n - Treat `grill-with-docs` as the default planning entry point for non-trivial Goals.\n - Challenge domain terms against `CONTEXT.md`.\n-- Challenge hard-to-reverse tradeoffs against ADRs.\n+- Challenge hard-to-reverse tradeoffs against SADRs and target ADRs.\n - Explore code instead of asking when code can answer the question.\n - Run Context & Impact before implementation planning.\n - Produce a Task DAG, not an unordered checklist.\n@@ -94,7 +97,7 @@\n - For each question, provide your recommended answer.\n - Resolve dependency decisions before downstream design questions.\n - Update `CONTEXT.md` inline when terminology crystallizes.\n-- Offer ADRs only when the decision is hard to reverse, surprising without context, and a real tradeoff.\n+- Offer a SADR or target ADR only when the decision is hard to reverse, surprising without context, and a real tradeoff.\n - Convert unresolved questions into HITL blockers rather than allowing Codex to guess.\n \n ## PRD and Issues Standard\n@@ -103,7 +106,7 @@\n \n - Use the project's domain glossary vocabulary.\n - Avoid volatile file paths unless they are essential.\n-- Include implementation decisions, testing decisions, out-of-scope items, and relevant ADR/domain links.\n+- Include implementation decisions, testing decisions, out-of-scope items, and relevant SADR/target-ADR/domain links.\n - Identify deep-module opportunities and testable interfaces.\n \n When creating issues:\n@@ -121,7 +124,7 @@\n - Branch/worktree target.\n - Scope and non-goals.\n - Dependencies and locks.\n-- Relevant docs, ADRs, modules, and tests.\n+- Relevant docs, SADRs, target ADRs, modules, and tests.\n - Required skill invocations.\n - TDD expectations.\n - Pre-PR `code-review` gate expectations.\n@@ -152,7 +155,7 @@\n \n - Treat yourself as a judge, not implementer.\n - Do not write production code.\n-- Read `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant ADRs, PRD, task issue, PR body, diff, checks, reviews, labels, and `.shiki/` state.\n+- Read `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant SADRs and target ADRs, PRD, task issue, PR body, diff, checks, reviews, labels, and `.shiki/` state.\n - Evaluate every applicable checklist in `docs/agents/checklists.md`.\n - Map each acceptance criterion to durable evidence.\n - Distinguish implementation failure from missing evidence.\ndiff --git a/CONTEXT.md b/CONTEXT.md\nindex e61a9be06a0cce0d026484083a657bde2067e234..237ee8aa3ea8fab334ece49768bc5ed3b5af0974\n--- a/CONTEXT.md\n+++ b/CONTEXT.md\n@@ -25,7 +25,7 @@\n _Avoid_: jumping directly into code\n \n **grill-with-docs**:\n-A planning skill that challenges a plan against domain language, ADRs, code reality, and concrete edge scenarios. It resolves design-tree questions before PRD/issues. It runs inside Requirements Definition.\n+A planning skill that challenges a plan against domain language, Shiki SADRs, target ADRs, code reality, and concrete edge scenarios. It resolves design-tree questions before PRD/issues. It runs inside Requirements Definition.\n _Avoid_: generic brainstorming, silent assumptions\n \n **Requirements Definition**:\ndiff --git a/CONTRIBUTING.md b/CONTRIBUTING.md\nindex c6d9b656e73abb8a55e5f660123e03153dcb3e6d..9e37a8fbf668db5b22461aaae5548b8f1354394c\n--- a/CONTRIBUTING.md\n+++ b/CONTRIBUTING.md\n@@ -17,7 +17,7 @@\n 1. **Goal** \u2014 start from a user-approved Goal with an outcome, completion\n    conditions, non-goals, risk level, and success signals.\n 2. **grill-with-docs** \u2014 for any non-trivial change, challenge the plan against\n-   domain language, ADRs, and code reality before writing a PRD.\n+   domain language, Shiki SADRs, target ADRs, and code reality before writing a PRD.\n 3. **PRD** \u2014 capture settled product/engineering intent (`to-prd`).\n 4. **Spec Freeze** \u2014 the operator explicitly approves the PRD/requirements;\n    the plan records a `spec_freeze` block and no plan runs without it\n@@ -94,7 +94,7 @@\n   entries.\n - Do not hand-edit `.shiki/goals/*.json` or `.shiki/manifest.json` in a task PR.\n - Conversation state is not durable truth. If a decision matters, record it in\n-  GitHub, `.shiki/`, `CONTEXT.md`, or an ADR.\n+  GitHub, `.shiki/`, `CONTEXT.md`, a Shiki SADR, or a target ADR.\n \n ## Architecture Gate\n \n@@ -103,7 +103,7 @@\n or CI policy, package structure, three or more structural files, more than 100\n net new lines in a core module, three or more new files, or any high-risk label.\n When triggered, run `grill-with-docs` or `improve-codebase-architecture` and\n-record durable decisions in an ADR when needed (see `docs/adr/`).\n+record durable decisions in a Shiki SADR or target ADR as applicable (see `docs/adr/`).\n \n ## Safety\n \ndiff --git a/SYSTEM_PROMPT.md b/SYSTEM_PROMPT.md\nindex aaaea45e3a896c3a44ba89eec8bb84325ecf6c62..da009c86a88285269dee0155e58e550c6d744ea9\n--- a/SYSTEM_PROMPT.md\n+++ b/SYSTEM_PROMPT.md\n@@ -30,6 +30,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n Use the canonical source-of-truth order above. Runtime-specific reading orders\n are operational guidance only and must not override or reorder it. The current\n conversation is non-durable operator input.\n@@ -48,14 +51,14 @@\n \n A runtime may only do work that is explicitly assigned by the Goal, task, PR, or MergeGate decision.\n \n-Do not assume `openai/codex-action`, `OPENAI_API_KEY`, or API-key based Codex automation in the default Shiki path. API-key runners are explicit target-repo extensions and require their own ADR.\n+Do not assume `openai/codex-action`, `OPENAI_API_KEY`, or API-key based Codex automation in the default Shiki path. API-key runners are explicit target-repo extensions and require their own target ADR.\n \n ## 4. Non-negotiable Shiki flow\n \n For any non-trivial Goal, use this flow:\n \n 1. **Goal Seek**: define outcome, non-goals, risk level, completion criteria, and evidence requirements.\n-2. **grill-with-docs**: challenge the plan against `CONTEXT.md`, ADRs, code reality, terminology, boundaries, and edge scenarios. Ask one question at a time when the operator is available. If code can answer a question, inspect code instead of asking.\n+2. **grill-with-docs**: challenge the plan against `CONTEXT.md`, Shiki SADRs, target ADRs, code reality, terminology, boundaries, and edge scenarios. Ask one question at a time when the operator is available. If code can answer a question, inspect code instead of asking.\n 3. **Context & Impact**: identify modules, interfaces, seams, callers, dependencies, locks, risk, and verification surfaces.\n 4. **to-prd + Spec Freeze**: turn settled context into a PRD using domain vocabulary and testing decisions. The operator's explicit approval of the PRD is Spec Freeze, recorded as the plan's `spec_freeze` block; plans without it do not run. Steps 1-4 form Requirements Definition.\n 5. **to-issues**: decompose the PRD into independently grabbable vertical-slice issues. Prefer AFK slices; mark HITL slices when judgment is still required.\n@@ -75,7 +78,7 @@\n Before making any material change:\n \n 1. Read `AGENTS.md` and any runtime-specific wrapper such as `CLAUDE.md`.\n-2. Read `CONTEXT.md` and relevant ADRs.\n+2. Read `CONTEXT.md` and relevant SADRs and target ADRs.\n 3. Read the GitHub Issue / PR and `.shiki/` state for the active Goal or task.\n 4. Identify your role: Planner, Implementer, Reviewer, Completion Check Agent, Repairer, or Guardian-assist.\n 5. Identify Goal id, task id, branch, PR, dependencies, locks, risk level, required skills, and required evidence.\n@@ -103,7 +106,7 @@\n \n At minimum, identify:\n \n-- Relevant documents, ADRs, domain terms, and prior decisions.\n+- Relevant documents, SADRs, target ADRs, domain terms, and prior decisions.\n - Relevant modules, interfaces, seams, callers, owners, and tests.\n - Dependency relationships between tasks.\n - Candidate file locks and lock conflicts.\n@@ -152,7 +155,7 @@\n \n CCA must read:\n \n-- `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant ADRs.\n+- `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant SADRs and target ADRs.\n - Parent Goal issue, PRD issue, task issue, comments, and labels.\n - PR body, diff, commits, changed files, reviews, CI checks, and artifacts available to the workflow.\n - `.shiki/` task contract, locks, ledger entries, CCA checklist profile, and prior repair packets.\n@@ -214,7 +217,7 @@\n - Three or more new files.\n - Any high-risk or critical label.\n \n-When the architecture gate triggers, run `grill-with-docs` or `improve-codebase-architecture` as appropriate and record the decision in an ADR if it is hard to reverse or likely to be re-litigated.\n+When the architecture gate triggers, run `grill-with-docs` or `improve-codebase-architecture` as appropriate and record the decision in a Shiki SADR or target ADR as applicable if it is hard to reverse or likely to be re-litigated.\n \n ## 14. Skill Gate\n \n@@ -223,7 +226,7 @@\n Use these skills:\n \n - `setup-matt-pocock-skills`: before first use of the engineering skill set, or when issue tracker, triage labels, or domain docs are missing.\n-- `grill-with-docs`: default for non-trivial Goals before PRD/issues; required for ambiguous plans, terminology, boundaries, ADR-worthy tradeoffs, or design-tree decisions.\n+- `grill-with-docs`: default for non-trivial Goals before PRD/issues; required for ambiguous plans, terminology, boundaries, decision-record-worthy tradeoffs, or design-tree decisions.\n - `zoom-out`: unfamiliar code area or missing architectural map.\n - `to-prd`: convert settled context into a PRD.\n - `to-issues`: break a PRD, plan, or Goal into independently grabbable vertical-slice issues.\ndiff --git a/docs/adr/0001-keep-shiki-as-platform-name.md b/docs/adr/SADR-0001-keep-shiki-as-platform-name.md\nindex 20a9637c3b9d8c97de7ba8db7cfae4d82d9a7dba..8b685439959aada1b221c112f0ac3c51ca8a1695\n--- a/docs/adr/0001-keep-shiki-as-platform-name.md\n+++ b/docs/adr/SADR-0001-keep-shiki-as-platform-name.md\n@@ -1,3 +1,3 @@\n-# Keep Shiki As The Platform Name\n+# SADR-0001: Keep Shiki As The Platform Name\n \n Shiki remains the product name for the GitHub-native agentic engineering control plane. We considered more descriptive names such as MergePilot, MergeGate, and Kiroku, but kept Shiki because it carries the existing goal-seeking framework identity while remaining independent of Hermes, Codex, Claude Code, or any single runtime.\ndiff --git a/docs/adr/0002-use-agents-as-shared-constitution.md b/docs/adr/SADR-0002-use-agents-as-shared-constitution.md\nindex 73de30ecb078120eac540d9bc51855e3cd48d0e3..c5486182bf06fca36ea508496b013c67302fd989\n--- a/docs/adr/0002-use-agents-as-shared-constitution.md\n+++ b/docs/adr/SADR-0002-use-agents-as-shared-constitution.md\n@@ -1,3 +1,3 @@\n-# Use AGENTS As The Shared Agent Constitution\n+# SADR-0002: Use AGENTS As The Shared Agent Constitution\n \n Shiki uses `AGENTS.md` as the runtime-neutral constitution and `CLAUDE.md` as a thin Claude Code wrapper. This keeps Codex, Claude Code, Hermes Runner, and future Agent Runtimes aligned on the same GitHub-first MergeGate rules while still allowing runtime-specific instructions where needed.\ndiff --git a/docs/adr/0003-require-skill-gate-for-mergegate-readiness.md b/docs/adr/SADR-0003-require-skill-gate-for-mergegate-readiness.md\nindex 424be0c9af4c8f0136f1fde68a468c47ff84e764..7fc337987c7df9792f75f181dfcbed75fe5ffff2\n--- a/docs/adr/0003-require-skill-gate-for-mergegate-readiness.md\n+++ b/docs/adr/SADR-0003-require-skill-gate-for-mergegate-readiness.md\n@@ -1,3 +1,3 @@\n-# Require Skill Gate For MergeGate Readiness\n+# SADR-0003: Require Skill Gate For MergeGate Readiness\n \n Shiki requires relevant engineering skills to be invoked before execution when their triggers apply. This makes skills part of MergeGate readiness rather than optional prompt style, because Shiki depends on repeatable planning, issue decomposition, TDD, diagnosis, triage, and architecture review before autonomous or parallel agent work can be trusted.\ndiff --git a/docs/adr/0004-use-github-first-with-shiki-mirror.md b/docs/adr/SADR-0004-use-github-first-with-shiki-mirror.md\nindex d0a3a9174dc646a945a9d325954ffaf00c352040..a79c54ae9ee8eed2a0524192b864da4e22a92d3f\n--- a/docs/adr/0004-use-github-first-with-shiki-mirror.md\n+++ b/docs/adr/SADR-0004-use-github-first-with-shiki-mirror.md\n@@ -1,4 +1,4 @@\n-# ADR 0004: Use GitHub First With A Repository-Local Shiki Mirror\n+# SADR-0004: Use GitHub First With A Repository-Local Shiki Mirror\n \n ## Status\n \ndiff --git a/docs/adr/0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md b/docs/adr/SADR-0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\nindex b339baf49b89d9290e79c3d51b36e7b2bde3bd95..0f3c3eb9afccdb3403ce978e21d04fe12e495da0\n--- a/docs/adr/0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\n+++ b/docs/adr/SADR-0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\n@@ -1,8 +1,8 @@\n-# ADR 0005: Use Codex As Front And Claude Code Action As The GitHub Runtime\n+# SADR-0005: Use Codex As Front And Claude Code Action As The GitHub Runtime\n \n ## Status\n \n-Accepted (implementer default superseded by ADR 0008; the auth model and the\n+Accepted (implementer default superseded by SADR-0008; the auth model and the\n Claude Code Action GitHub runtime decision remain in force)\n \n ## Context\n@@ -26,4 +26,4 @@\n - Shiki avoids requiring `OPENAI_API_KEY` for the default workflow.\n - Codex work happens through the user's authenticated Codex front surface, not as a hidden backend Action.\n - Claude Code Action is configured for OAuth-token based subscription usage by default.\n-- API-key based automation remains possible only as an explicit target-repo extension with its own ADR.\n+- API-key based automation remains possible only as an explicit target-repo extension with its own target ADR.\ndiff --git a/docs/adr/0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md b/docs/adr/SADR-0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\nindex 4bcae9d1e52f0a37b8e9fbafa5cd14b6ee1993bd..7a38538a6ae9929567f0ade67e7ea6a60d0b3f7e\n--- a/docs/adr/0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\n+++ b/docs/adr/SADR-0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\n@@ -1,4 +1,4 @@\n-# ADR 0006: Separate Codex Implementation, CCA Completion, And MergeGate Authorization\n+# SADR-0006: Separate Codex Implementation, CCA Completion, And MergeGate Authorization\n \n ## Status\n \ndiff --git a/docs/adr/0007-packaging-and-release.md b/docs/adr/SADR-0007-packaging-and-release.md\nindex 79b84ddb97167372b44641e5b7860e400ce80822..bb445a2e2e856d9d1f5fdbe35de8d4e8cec0fb32\n--- a/docs/adr/0007-packaging-and-release.md\n+++ b/docs/adr/SADR-0007-packaging-and-release.md\n@@ -1,4 +1,4 @@\n-# ADR 0007: Packaging And Release For Shiki\n+# SADR-0007: Packaging And Release For Shiki\n \n ## Status\n \n@@ -25,7 +25,7 @@\n   not introduce third-party runtime dependencies or break the shim.\n - Releases must run inside the operator's subscription-authenticated toolchain\n   and must not require extra secrets beyond the built-in `GITHUB_TOKEN`\n-  (consistent with ADR 0005's no-API-key-by-default stance).\n+  (consistent with SADR-0005's no-API-key-by-default stance).\n - `.shiki/manifest.json` already encodes the install surface (`install.include`,\n   `install.create_directories`, `install.exclude_from_commit`), and it must not\n   be mutated as part of this change.\n@@ -34,7 +34,7 @@\n \n This is a hard-to-reverse platform decision (it sets the version source of\n truth, the release trigger, and the public install contract), so it is recorded\n-as an ADR.\n+as a SADR.\n \n ## Decision\n \n@@ -104,7 +104,7 @@\n 3. **Publishing to PyPI.** Rejected for now: Shiki is distributed as a\n    repository template + git-installable CLI, not a PyPI library; PyPI would add\n    credentials/secrets and a different versioning contract. Can be added later as\n-   its own ADR if a packaged distribution is needed.\n+   its own SADR if a packaged distribution is needed.\n 4. **Dynamic version (read `VERSION` via a build backend `dynamic` field).**\n    Rejected to keep `pyproject.toml` backend-agnostic and dependency-free; the\n    small duplication between `VERSION` and `pyproject.toml` is guarded by the\n@@ -123,4 +123,4 @@\n   releasing versions the whole platform but ships only `install.include` to\n   targets.\n - Future PyPI distribution or a console entry point are deferred and would each\n-  warrant their own ADR.\n+  warrant their own SADR.\ndiff --git a/docs/adr/0008-use-claude-code-as-default-implementer.md b/docs/adr/SADR-0008-use-claude-code-as-default-implementer.md\nindex dedf4510b7c7799521422f622029563d373db031..35c2625d908167fbc58855b5b63e283805f167e8\n--- a/docs/adr/0008-use-claude-code-as-default-implementer.md\n+++ b/docs/adr/SADR-0008-use-claude-code-as-default-implementer.md\n@@ -1,12 +1,12 @@\n-# ADR 0008: Use Claude Code As The Default Implementer Runtime\n+# SADR-0008: Use Claude Code As The Default Implementer Runtime\n \n ## Status\n \n-Proposed (supersedes ADR 0005 for the implementer default; keeps its auth model)\n+Proposed (supersedes SADR-0005 for the implementer default; keeps its auth model)\n \n ## Context\n \n-ADR 0005 made Codex Front the default implementation runtime, with Claude Code\n+SADR-0005 made Codex Front the default implementation runtime, with Claude Code\n restricted to planner and reviewer roles by the runtime registry, and the only\n autonomous implementation adapter was `shiki runner codex`.\n \n@@ -18,13 +18,13 @@\n - The platform's planning, review, and CCA layers already run on Claude Code,\n   and Claude Code's native capabilities (the built-in `/code-review` skill and\n   the Workflow multi-agent orchestration tool) have no Codex equivalent.\n-- The Goal lifecycle redesign (ADR 0009) requires an autonomous post-freeze\n+- The Goal lifecycle redesign (SADR-0009) requires an autonomous post-freeze\n   loop driven from the operator's Claude Code session, including bounded\n   repair dispatch, which must not depend on a runtime that cannot be invoked\n   unattended.\n \n The constitution claims Shiki is runtime-agnostic (\"not a Claude-only\n-workflow\", ADR 0002). Any change to the implementer default must preserve the\n+workflow\", SADR-0002). Any change to the implementer default must preserve the\n registry-based runtime model rather than hardcoding one vendor.\n \n ## Decision\n@@ -39,7 +39,7 @@\n   the task to review or repair-needed.\n - Keep Codex registered as an optional implementer runtime. `shiki runner\n   codex`, the Codex handoff templates, and the ChatGPT OAuth auth model from\n-  ADR 0005 remain supported for tasks explicitly assigned to `codex`.\n+  SADR-0005 remain supported for tasks explicitly assigned to `codex`.\n - Commit to mandating two Claude-native capabilities as loop contract in\n   follow-up Goals (status: the code-review gate was delivered by Goal G-B \u2014\n   skill registry, checklists item PR-12, default required_skills \u2014 while the\n@@ -60,7 +60,7 @@\n \n Out of scope: removing Codex support, changing the CCA runtime (Claude Code\n Action with `CLAUDE_CODE_OAUTH_TOKEN` stays), and API-key based automation\n-(still requires its own ADR per ADR 0005).\n+(still requires its own target ADR per SADR-0005).\n \n ## Consequences\n \n@@ -100,7 +100,7 @@\n   harness rejection of direct `codex exec` is outside Shiki's control, and the\n   plan's mandatory native capabilities cannot run on Codex at all.\n - **Remove Codex entirely**: rejected; it abandons the runtime-agnostic\n-  constitution (ADR 0002) for no operational gain and forecloses\n+  constitution (SADR-0002) for no operational gain and forecloses\n   cross-runtime adversarial review.\n - **GitHub Actions cloud implementation via claude-code-action**: rejected as\n   default; durable evidence is automatic but token cost, Node 24 workflow\ndiff --git a/docs/adr/0009-requirements-definition-and-spec-freeze.md b/docs/adr/SADR-0009-requirements-definition-and-spec-freeze.md\nindex 29b3f7871bef9203a8b4a831a65c39eb2f506e42..52e7dd69ad0aa4efe0b68c62678abbf7df02aaec\n--- a/docs/adr/0009-requirements-definition-and-spec-freeze.md\n+++ b/docs/adr/SADR-0009-requirements-definition-and-spec-freeze.md\n@@ -1,4 +1,4 @@\n-# ADR 0009: Merge Grill And Planning Into Requirements Definition With Spec Freeze\n+# SADR-0009: Merge Grill And Planning Into Requirements Definition With Spec Freeze\n \n ## Status\n \ndiff --git a/docs/adr/0010-external-ai-guardian-review.md b/docs/adr/SADR-0010-external-ai-guardian-review.md\nindex e6108bbe8bb4e73e0ff4684c8f78001a80438c4e..f3510b145664298adafb151b6f0db51b972f01ec\n--- a/docs/adr/0010-external-ai-guardian-review.md\n+++ b/docs/adr/SADR-0010-external-ai-guardian-review.md\n@@ -1,4 +1,4 @@\n-# ADR 0010: External AI Guardian Review As A First-Class Approval Authority\n+# SADR-0010: External AI Guardian Review As A First-Class Approval Authority\n \n ## Status\n \n@@ -8,7 +8,7 @@\n \n Shiki is intended to be an autonomous, recursive, self-improving agent\n framework: authority-in-the-loop, not human-in-the-loop. Until now the Guardian\n-approval required for high/critical-risk merges (guardian-policy.json, ADR\n+approval required for high/critical-risk merges (guardian-policy.json, SADR\n governance) recognized only human authority sources \u2014 a GitHub review approval,\n the `guardian:approved` label applied by a configured Guardian, or a comment\n carrying `Guardian approval granted` plus the current head SHA.\n@@ -89,7 +89,7 @@\n   autonomous design the operator intends and produced the impersonation\n   incident.\n - **Record AI approval as an operator comment**: rejected as forgery of human\n-  authority \u2014 the exact failure this ADR corrects.\n+  authority \u2014 the exact failure this SADR corrects.\n - **Deliver the artifact as a committed `.shiki/` file**: rejected; a committed\n   approval inside the PR being approved is forgeable by anyone who can push.\n - **A dedicated reviewer bot identity instead of a relayed comment**: deferred;\ndiff --git a/docs/adr/0011-autonomous-quality-gate-evidence.md b/docs/adr/SADR-0011-autonomous-quality-gate-evidence.md\nindex bca0246c5d46e44afbde15486a75eec425fc02f5..9c93eac16d8c3a6d4b11bd7224e3955102cd7db4\n--- a/docs/adr/0011-autonomous-quality-gate-evidence.md\n+++ b/docs/adr/SADR-0011-autonomous-quality-gate-evidence.md\n@@ -1,4 +1,4 @@\n-# ADR 0011: Autonomous Quality-Gate Evidence \u2014 Deterministic Facts And An Independent Verifier\n+# SADR-0011: Autonomous Quality-Gate Evidence \u2014 Deterministic Facts And An Independent Verifier\n \n ## Status\n \ndiff --git a/docs/adr/0012-autonomous-loop-closeout-to-main.md b/docs/adr/SADR-0012-autonomous-loop-closeout-to-main.md\nindex 637fad6dde4989c9c2bf0f81f676d8d29991123c..ec7d85f81030a676bcbba28b43eb365422a36eac\n--- a/docs/adr/0012-autonomous-loop-closeout-to-main.md\n+++ b/docs/adr/SADR-0012-autonomous-loop-closeout-to-main.md\n@@ -1,4 +1,4 @@\n-# 0012 \u2014 Autonomous loop pushes goal completion to main via a closeout PR\n+# SADR-0012 \u2014 Autonomous loop pushes goal completion to main via a closeout PR\n \n - Status: accepted\n - Date: 2026-06-18\ndiff --git a/docs/adr/0013-bootstrap-grants-actions-approve-pull-requests.md b/docs/adr/SADR-0013-bootstrap-grants-actions-approve-pull-requests.md\nindex b6c69d64f1c5ced346b88d6859586715b3b5bd0e..cf48ff4a6716267f539ca58edd1a852e7c7a2d64\n--- a/docs/adr/0013-bootstrap-grants-actions-approve-pull-requests.md\n+++ b/docs/adr/SADR-0013-bootstrap-grants-actions-approve-pull-requests.md\n@@ -1,4 +1,4 @@\n-# ADR 0013: Bootstrap Grants GitHub Actions Permission To Create And Approve Pull Requests\n+# SADR-0013: Bootstrap Grants GitHub Actions Permission To Create And Approve Pull Requests\n \n ## Status\n \n@@ -31,7 +31,7 @@\n security tradeoff: it lets repository automation satisfy a human-review gate. In\n a multi-maintainer repo that could let a compromised or buggy workflow\n self-approve changes. So whether bootstrap turns it on by default \u2014 and with\n-what default token scope \u2014 is an ADR-worthy decision, not an implementation\n+what default token scope \u2014 is a SADR-worthy decision, not an implementation\n detail.\n \n ## Decision\n@@ -48,7 +48,7 @@\n Specifically:\n \n - **`can_approve_pull_request_reviews` defaults to `true`.** Shiki's default\n-  posture is solo/AFK autonomous operation (ADR 0008/0009/0012), where the CCA\n+  posture is solo/AFK autonomous operation (SADR-0008/0009/0012), where the CCA\n   Review Bridge is the only path that can satisfy `required_review: true` after\n   the CCA and Guardian gates have already passed. The default makes a\n   freshly-bootstrapped repo able to run the autonomous loop end to end without a\ndiff --git a/docs/adr/0014-codex-app-external-ai-guardian-ui-adapter.md b/docs/adr/SADR-0014-codex-app-external-ai-guardian-ui-adapter.md\nindex 238db06279334caa341c117362df9271382a7fcd..65f12e9939321e1b3e389a34c822fc1549e5155d\n--- a/docs/adr/0014-codex-app-external-ai-guardian-ui-adapter.md\n+++ b/docs/adr/SADR-0014-codex-app-external-ai-guardian-ui-adapter.md\n@@ -1,4 +1,4 @@\n-# ADR 0014: Use Codex App As The External AI Guardian UI Adapter\n+# SADR-0014: Use Codex App As The External AI Guardian UI Adapter\n \n ## Status\n \ndiff --git a/docs/adr/0015-contract-approval-before-dispatch.md b/docs/adr/SADR-0015-contract-approval-before-dispatch.md\nindex fad6c656741a20a7684ba16080a7db159f63a52f..6061271a46d3f1d6ae7174477de3fb75673123a6\n--- a/docs/adr/0015-contract-approval-before-dispatch.md\n+++ b/docs/adr/SADR-0015-contract-approval-before-dispatch.md\n@@ -1,4 +1,4 @@\n-# ADR 0015: Approve The Task Contract Before Dispatch, Not The Diff After It\n+# SADR-0015: Approve The Task Contract Before Dispatch, Not The Diff After It\n \n ## Status\n \n@@ -9,7 +9,7 @@\n Guardian approval for high/critical risk is expressible only on a pull request.\n Every approval source in `guardian-policy.json` \u2014 GitHub review, the\n `guardian:approved` label, the `Guardian approval granted` comment, and the\n-`external_ai_guardian_review` artifact (ADR 0010/0014) \u2014 is bound to a PR, and\n+`external_ai_guardian_review` artifact (SADR-0010/0014) \u2014 is bound to a PR, and\n three of the four require the current head SHA. Approval therefore cannot exist\n before an implementation PR exists.\n \n@@ -81,7 +81,7 @@\n An envelope breach must surface only on the non-repairable policy gate, never on\n the repairable `MergeGate metadata check`. Otherwise the Goal Loop routes it to\n `dispatch_repair` \u2014 instructing a runner to make the breach go away \u2014 which is\n-exactly what this ADR forbids.\n+exactly what this SADR forbids.\n \n **Contract immutability.** After registration, an implementation PR must not\n change the governance fields of its own task contract (`scope`, `non_goals`,\n@@ -116,7 +116,7 @@\n fails any verdict that is not `complete`, and the signal the CCA prompt reads is\n computed from `evaluate_guardian_approval` alone. Without the OR-merge at the\n signal, a contract-approved PR still returns `needs_guardian`, the CCA job fails,\n-and the MergeGate policy check never runs \u2014 the gate this ADR changes would never\n+and the MergeGate policy check never runs \u2014 the gate this SADR changes would never\n be reached. The CCA *prompt* needs no change: a `contract_approval` entry in the\n signal's existing `sources` array satisfies its current wording.\n \n@@ -130,7 +130,7 @@\n written, and an approved contract lets its implementation PRs merge through the\n same automatic path as low/medium risk. The wasted implement-then-block cycle\n disappears. Guardian review shifts from reading diffs to reading contracts, which\n-is both earlier and smaller; the external AI Guardian path (ADR 0010/0014) works\n+is both earlier and smaller; the external AI Guardian path (SADR-0010/0014) works\n on contracts with no change.\n \n We accept that the Guardian authorizes a class of change without seeing its\n@@ -146,7 +146,7 @@\n adversarial tests for the new approval path equivalent in rigor to\n `test_shiki_governance_evidence.sh`.\n \n-Bootstrap costs, which apply only while this ADR is being implemented. The tasks\n+Bootstrap costs, which apply only while this SADR is being implemented. The tasks\n that build Contract Approval merge under the existing rules, so the ones\n dispatched after the pre-dispatch gate lands but before the approval path lands\n need a Guardian-approved Contract PR *and* a live Guardian approval on their\n@@ -210,7 +210,7 @@\n risk; they do not bound authorized work.\n \n **Auto-escalate risk on envelope breach instead of stopping.** Returns the\n-workflow to a diff-time Guardian decision \u2014 the exact behavior this ADR removes.\n+workflow to a diff-time Guardian decision \u2014 the exact behavior this SADR removes.\n \n **Resolve governance fields from the base snapshot only.** Rejected against the\n registration flow: implementation PRs add their own contract, so the base has no\ndiff --git a/docs/adr/0016-locks-bound-product-paths.md b/docs/adr/SADR-0016-locks-bound-product-paths.md\nindex b5e850f3f1f7f4ef6e7f9d1763a29a51bd300c45..46b40a9b99a7aae644c02bb458f5473ff0a90ce2\n--- a/docs/adr/0016-locks-bound-product-paths.md\n+++ b/docs/adr/SADR-0016-locks-bound-product-paths.md\n@@ -1,4 +1,4 @@\n-# ADR 0016: Locks bound product paths; the Shiki mirror is bound by record identity\n+# SADR-0016: Locks bound product paths; the Shiki mirror is bound by record identity\n \n ## Status\n \n@@ -6,8 +6,8 @@\n \n - Date: 2026-07-28\n - Deciders: operator (mizutani-140), Claude (planner)\n-- Related: ADR 0004 (GitHub-first with a `.shiki` mirror), ADR 0009 (Spec Freeze),\n-  ADR 0012 (autonomous loop closeout to main), ADR 0015 (Contract Approval before\n+- Related: SADR-0004 (GitHub-first with a `.shiki` mirror), SADR-0009 (Spec Freeze),\n+  SADR-0012 (autonomous loop closeout to main), SADR-0015 (Contract Approval before\n   dispatch), remediation goal task R-02, the live PR #179 deadlock.\n \n ## Context\n@@ -90,11 +90,11 @@\n files, documentation files and named source modules are nameable; runtime-\n allocated ledger, runner and report ids are not \u2014 and those live in `.shiki`,\n which locks no longer govern. Existing frozen contracts are brought into line by\n-a one-time Spec Amendment per plan, which must land before ADR 0015's contract\n+a one-time Spec Amendment per plan, which must land before SADR-0015's contract\n immutability makes `locks` unwritable from the implementation PR.\n \n **Lock state is resolved from the durable side.** MergeGate resolves the lock\n-registry, and ADR 0015's governance fields, from the base `.shiki` snapshot when\n+registry, and SADR-0015's governance fields, from the base `.shiki` snapshot when\n present, falling back to head only when absent. The `MergeGate metadata check`\n job is given `--base-shiki`, which it does not receive today. Locks are not\n derived from the live open-pull-request set: a pull request body is untrusted\n@@ -108,7 +108,7 @@\n Explicitly out of scope: moving MergeGate's contract resolution off the PR\n checkout wholesale; making the mirror a projection of GitHub state; introducing\n new lock kinds; moving ledger evidence out of the repository; and unwinding\n-ADR 0012's closeout pull request. Registration of multi-task goals is ADR 0015's\n+SADR-0012's closeout pull request. Registration of multi-task goals is SADR-0015's\n Contract PR and is not re-decided here.\n \n ## Consequences\n@@ -127,7 +127,7 @@\n \n Risks accepted. First, goal-scoped identity is only as strong as `goal_id`,\n which is resolved from the PR body and the head task file; the mitigation is to\n-add `goal_id` to ADR 0015's frozen governance field set, and until that lands a\n+add `goal_id` to SADR-0015's frozen governance field set, and until that lands a\n task file can name a foreign goal. Second, base-when-present resolution is\n unavailable on fork pull requests, where the head is contributor-controlled;\n fork pull requests already cannot complete a Shiki merge, and this decision does\n@@ -139,7 +139,7 @@\n path, not by implementation pull requests.\n \n New obligations: one Spec Amendment per frozen plan to rewrite `locks`, ordered\n-before ADR 0015 T1; extension of the adversarial suites in\n+before SADR-0015 T1; extension of the adversarial suites in\n `scripts/test_shiki_adversarial_state.sh` and\n `scripts/test_shiki_governance_evidence.sh` to cover the eleven newly-scoped\n directories; and updating `scripts/test_shiki_loop_lock_guard.sh`,\n@@ -151,32 +151,32 @@\n whether the loop's transitive sync of ledger `evidence` references can be\n deleted once the Contract PR carries its own source plan.\n \n-## Relationship to ADR 0004\n+## Relationship to SADR-0004\n \n-This ADR **clarifies something ADR 0004 left unspecified**. ADR 0004 decides that\n+This SADR **clarifies something SADR-0004 left unspecified**. SADR-0004 decides that\n GitHub is the operational source of truth and that each target repository keeps a\n `.shiki/` mirror. It says nothing about pull requests writing the mirror, does\n not name MergeGate, and does not make the mirror an input to a gate. Nothing in\n-ADR 0004's Decision or Consequences is amended, superseded or weakened: the\n+SADR-0004's Decision or Consequences is amended, superseded or weakened: the\n mirror still exists, still records the same artifacts, and pull request\n-boundaries remain the coordination surface. What this ADR settles is the question\n+boundaries remain the coordination surface. What this SADR settles is the question\n 0004 never reached \u2014 by what authority a pull request may write a mirror record \u2014\n and it answers it the way `.shiki/manifest.json` already declares\n (`pr_mutation: current-task-or-current-goal-only`).\n \n-It **amends ADR 0012**, whose Decision item 5 states that \"Loop-executed tasks\n+It **amends SADR-0012**, whose Decision item 5 states that \"Loop-executed tasks\n declare the `path:.shiki/**` lock, which covers every `.shiki` file the closeout\n stages ... so `files_outside_locks` is satisfied without per-file lock\n bookkeeping.\" That clause is replaced: the closeout's mirror writes are\n-authorized by record identity, not by a declared lock. ADR 0012's substantive\n+authorized by record identity, not by a declared lock. SADR-0012's substantive\n decision \u2014 that completion reaches `main` through a closeout pull request rather\n than local attestation \u2014 is preserved unchanged.\n \n-It is **complementary to ADR 0015**, not a substitute. ADR 0015 owns contract\n+It is **complementary to SADR-0015**, not a substitute. SADR-0015 owns contract\n immutability, the pre-dispatch gate, the Contract PR and the Scope Envelope. This\n-ADR owns what a lock means and who may write the mirror. The one ordering\n+SADR owns what a lock means and who may write the mirror. The one ordering\n constraint between them is that the lock-narrowing Spec Amendment must precede\n-ADR 0015 T1's freezing of the `locks` field.\n+SADR-0015 T1's freezing of the `locks` field.\n \n ## Alternatives Considered\n \n@@ -186,7 +186,7 @@\n the only writer that can register them may run. Migrating the Guardian signal to\n that store makes every registration PR fail closed to `needs_guardian` at every\n risk level, because the task file is by definition absent from the store, which\n-removes the low/medium autonomous path ADR 0008/0009 exists to provide. And it\n+removes the low/medium autonomous path SADR-0008/0009 exists to provide. And it\n leaves schemas, manifest, config, guardian policy, templates, migrations, plans,\n worktrees, runner records, runs and handoffs with no writer at all in a\n repository whose product is `.shiki`. Measured benefit on the frozen backlog:\n@@ -199,14 +199,14 @@\n namespace is not in fact ownership-partitioned \u2014 56 ledger entries carry no\n `task_id`, and one goal DAG is writable by 35 registered tasks. The correct\n expression of its insight is that locks do not govern the mirror at all, which is\n-what this ADR decides.\n+what this SADR decides.\n \n **Make the mirror a projection written only by a registration writer and a\n post-merge projection job.** Rejected. It requires a bypass actor with write\n access to the protected default branch; it makes its own master invariant\n readable from a manifest that pull requests may edit; it deletes both reconcile\n modes and leaves a corrupted mirror with no in-band repair path, contradicting\n-ADR 0004's consequence that mirror conflicts are repaired against GitHub state;\n+SADR-0004's consequence that mirror conflicts are repaired against GitHub state;\n and its content-derived ids are rejected by `validate_shiki`'s currently-green id\n pattern.\n \ndiff --git a/docs/adr/0017-bookkeeping-closeouts-do-not-inherit-risk.md b/docs/adr/SADR-0017-bookkeeping-closeouts-do-not-inherit-risk.md\nindex 554e3a0d526a72ca067108c5464cc68c3edcf174..b9676857a4713850e063af7c39c8e0b01ecb4c66\n--- a/docs/adr/0017-bookkeeping-closeouts-do-not-inherit-risk.md\n+++ b/docs/adr/SADR-0017-bookkeeping-closeouts-do-not-inherit-risk.md\n@@ -1,4 +1,4 @@\n-# ADR 0017: A Bookkeeping Closeout Does Not Inherit Its Task's Risk\n+# SADR-0017: A Bookkeeping Closeout Does Not Inherit Its Task's Risk\n \n ## Status\n \n@@ -6,7 +6,7 @@\n \n ## Context\n \n-ADR 0012 made the goal loop push a task's completion to the default branch through a\n+SADR-0012 made the goal loop push a task's completion to the default branch through a\n closeout pull request: after the implementation PR merges, the loop opens a second PR\n carrying `task.status = done`, `lock.state = released`, and \u2014 when the task completes\n its goal \u2014 `goal.status = complete` plus one scorecard report. Nothing else. The\n@@ -49,14 +49,14 @@\n 4. The task file's governance fields (`scope`, `non_goals`, `required_skills`,\n    `risk_level`, `locks`, `acceptance_checks`, `test_command`) are byte-identical to\n    the base snapshot. Only `status`, `expected_pr`, `closeout_pr`, `ledger_evidence`\n-   and `expected_branch` may differ \u2014 the ADR 0015 contract-immutability rule,\n+   and `expected_branch` may differ \u2014 the SADR-0015 contract-immutability rule,\n    unchanged.\n \n    `expected_branch` is in that set because a closeout is *by construction* on a\n    different branch from the implementation it closes, and the loop rewrites the\n    field accordingly. It is safe to let it differ because MergeGate independently\n    requires `expected_branch` to equal the PR's actual head ref, so a PR cannot set\n-   it to anything other than the branch it is really on. The first draft of this ADR\n+   it to anything other than the branch it is really on. The first draft of this SADR\n    omitted it, which made the exemption unreachable: every real closeout failed\n    condition 4 on a field the loop always changes. See PR #209.\n 5. The state transitions are exactly the terminal set: task `review` \u2192 `done`, lock\n@@ -113,12 +113,12 @@\n classification must be derived, not asserted.\n \n **Fold the closeout into the implementation PR** so no second PR exists. Rejected under\n-ADR 0012, which recorded why: a task file that is already `done` on the implementation\n+SADR-0012, which recorded why: a task file that is already `done` on the implementation\n branch short-circuits the loop's review \u2192 CCA \u2192 merge \u2192 done ordering into a local\n goal-completion before any merge happens.\n \n **Have the loop auto-merge closeouts with elevated credentials.** Rejected outright: that\n-is a runtime authoring its own approval, which ADR 0010 and the constitution's\n+is a runtime authoring its own approval, which SADR-0010 and the constitution's\n approval-record prohibition forbid. It would also be indistinguishable, in the evidence,\n from the forgery those rules exist to prevent.\n \ndiff --git a/docs/adr/0018-carried-guardian-approval-across-a-proven-base-sync.md b/docs/adr/SADR-0018-carried-guardian-approval-across-a-proven-base-sync.md\nindex 22358562da390ed8b45cd600da08a43d378b4680..e6e2907cefa776460af73ddf7d39db605f3c39b0\n--- a/docs/adr/0018-carried-guardian-approval-across-a-proven-base-sync.md\n+++ b/docs/adr/SADR-0018-carried-guardian-approval-across-a-proven-base-sync.md\n@@ -1,4 +1,4 @@\n-# ADR 0018: Carried Guardian Approval Across A Proven Base Sync\n+# SADR-0018: Carried Guardian Approval Across A Proven Base Sync\n \n ## Status\n \n@@ -34,7 +34,7 @@\n Guardian evaluator that consumes it (`shiki_guardian.evaluate_guardian_approval`,\n the `guardian_comment_carried` source) were built and tested in isolation. Neither\n does anything until a caller passes the flags that turn the carry on, so the\n-feature was invisible in CI. This ADR records the decision the two of them\n+feature was invisible in CI. This SADR records the decision the two of them\n implement, and this task wires it into the four CI invocations that judge Guardian\n approval.\n \ndiff --git a/docs/adr/README.md b/docs/adr/README.md\nindex c70e057a4520c36e042ab68c53cd4a21ef709b71..5da43856836e2ce6a150f8127766e2f88355ab4b\n--- a/docs/adr/README.md\n+++ b/docs/adr/README.md\n@@ -1,40 +1,41 @@\n-# Architecture Decision Records\n+# Decision Record Namespaces\n \n-This directory holds Shiki's Architecture Decision Records (ADRs): durable\n-records of hard-to-reverse platform decisions. ADRs are part of the Shiki source\n-of truth alongside GitHub state, the `.shiki/` mirror, and `CONTEXT.md`.\n+This directory holds two independent decision namespaces:\n \n-## How ADRs Work\n+- Shiki Architecture Decision Records (SADRs), named\n+  `SADR-NNNN-<short-slug>.md`, record hard-to-reverse Shiki platform decisions.\n+- Target Architecture Decision Records (ADRs), named\n+  `NNNN-<short-slug>.md`, belong to the target product.\n \n-- New ADRs are added as `docs/adr/NNNN-<short-slug>.md`, where `NNNN` is the next\n-  zero-padded sequence number.\n-- The **canonical list of ADRs is the set of files matching `docs/adr/*.md`** in\n-  this directory. Browse `docs/adr/*.md` for the authoritative, current set.\n-- Use [`template.md`](template.md) as the starting point for a new ADR.\n-- Record an ADR when a decision is hard to reverse, surprising without context,\n-  and a real tradeoff. Do not write an ADR for routine or easily reversible\n-  choices.\n+The same four-digit number may appear once in each namespace. A platform\n+`SADR-NNNN` citation resolves only to `SADR-NNNN-*.md`; a target `ADR NNNN`\n+citation resolves only to `NNNN-*.md`.\n \n-Using the `docs/adr/*.md` glob as the source of truth means other PRs can add an\n-ADR without editing this index file, avoiding merge contention on a\n-hand-maintained enumeration.\n+## How Decision Records Work\n \n-## Current ADRs (snapshot)\n-\n-The list below is a convenience snapshot and may lag behind the directory. When\n-in doubt, list `docs/adr/*.md`.\n+- New Shiki platform decisions use the next zero-padded SADR number.\n+- New target product decisions use the target's own ADR sequence.\n+- The canonical list is the files in `docs/adr/`; no hand-maintained index is\n+  authoritative.\n+- Use [`template.md`](template.md) as the starting point and choose the filename\n+  namespace that owns the decision.\n+- Record a decision when it is hard to reverse, surprising without context, and\n+  a real tradeoff. Do not write a decision record for routine or easily\n+  reversible choices.\n \n-- [0001 \u2014 Keep Shiki As The Platform Name](0001-keep-shiki-as-platform-name.md)\n-- [0002 \u2014 Use AGENTS As The Shared Agent Constitution](0002-use-agents-as-shared-constitution.md)\n-- [0003 \u2014 Require Skill Gate For MergeGate Readiness](0003-require-skill-gate-for-mergegate-readiness.md)\n-- [0004 \u2014 Use GitHub First With A Repository-Local Shiki Mirror](0004-use-github-first-with-shiki-mirror.md)\n-- [0005 \u2014 Use Codex As Front And Claude Code Action As The GitHub Runtime](0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md)\n-- [0006 \u2014 Separate Codex Implementation, CCA Completion, And MergeGate Authorization](0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md)\n+Using the directory as the source of truth lets independent changes add records\n+without contending on an enumerated index.\n \n-## Adding A New ADR\n+## Adding A Shiki SADR\n \n-1. Copy `template.md` to `docs/adr/NNNN-<short-slug>.md`.\n+1. Copy `template.md` to `docs/adr/SADR-NNNN-<short-slug>.md`.\n 2. Fill in the title, status, context, decision, and consequences.\n-3. Set the status to `Proposed`, then `Accepted` once the decision is approved.\n-4. Open a PR following `CONTRIBUTING.md`. You do not need to edit this index for\n-   the ADR to be discoverable via `docs/adr/*.md`.\n+3. Set the status to `Proposed`, then `Accepted` once the proper authority has\n+   approved the decision.\n+4. Open a PR following `CONTRIBUTING.md`.\n+\n+## Adding A Target ADR\n+\n+1. Copy `template.md` to `docs/adr/NNNN-<short-slug>.md` using the target's own\n+   sequence.\n+2. Cite it as `ADR NNNN`; do not prefix its filename with `SADR-`.\ndiff --git a/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md b/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\nnew file mode 100644\nindex 0000000000000000000000000000000000000000..7455ce1f3bed02ddddb851ac21a1d1a648c9aeba\n--- /dev/null\n+++ b/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\n@@ -0,0 +1,55 @@\n+# SADR-0019: Separate Shiki Platform SADRs From Target ADRs\n+\n+## Status\n+\n+Accepted\n+\n+## Context\n+\n+Shiki installs its platform decision records into `docs/adr/` in every target\n+repository. Shiki and its targets previously used the same numeric\n+`NNNN-<slug>.md` namespace. A target that already owned ADRs 0013 through 0018\n+therefore acquired two records for each number during installation, making an\n+`ADR NNNN` citation ambiguous. Renumbering target decisions would rewrite target\n+history, while renumbering Shiki decisions per target would destroy their stable\n+platform identity.\n+\n+Upgrades also need to remove the former Shiki-owned numeric paths without ever\n+mistaking a target-owned ADR for platform content. The install stamp already\n+records exact shipped paths and content digests, so it can prove ownership only\n+when both still match.\n+\n+## Decision\n+\n+Shiki platform decisions use the `SADR` namespace:\n+\n+- records are named `docs/adr/SADR-NNNN-<slug>.md`;\n+- live platform citations use `SADR-NNNN`;\n+- target product decisions retain `docs/adr/NNNN-<slug>.md` and `ADR NNNN`;\n+- the reference validator resolves the two namespaces independently, so the same\n+  four-digit number may exist once in each namespace.\n+\n+Before a forced upgrade writes anything, the installer inventories the exact\n+former numeric Shiki ADR paths. It may delete a legacy path only when the\n+existing install stamp contains that exact path and its recorded SHA-256 digest\n+matches the file currently on disk. All ownership blockers are reported\n+together and abort the upgrade before its first write. A non-forced install\n+never performs this cleanup.\n+\n+Historical `.shiki/` evidence remains unchanged because it records the namespace\n+that existed when that evidence was produced.\n+\n+## Consequences\n+\n+- Targets may use any ADR number without colliding with Shiki's platform SADRs.\n+- Fresh installs preserve target ADR bytes and install a stable SADR set.\n+- Forced upgrades fail closed when legacy ownership cannot be proven, and a\n+  successful upgrade refreshes the stamp without removed legacy paths.\n+- Current governance, packaging, workflow, prompt, script, and skill references\n+  must move with the platform records.\n+\n+## Alternatives Considered\n+\n+- Collision-only refusal leaves affected targets permanently unupgradable.\n+- Renumbering target ADRs mutates target-owned history.\n+- Per-target Shiki renumbering makes platform citations non-portable.\ndiff --git a/docs/adr/template.md b/docs/adr/template.md\nindex a92a97daddd0b00ab9dd626cc8d86942da2eafed..17dd79175b5fee2105935557f691b40cf9492942\n--- a/docs/adr/template.md\n+++ b/docs/adr/template.md\n@@ -1,14 +1,14 @@\n-# NNNN: <Short Decision Title>\n+# <SADR-NNNN or NNNN>: <Short Decision Title>\n \n ## Status\n \n-Proposed | Accepted | Superseded by ADR-NNNN\n+Proposed | Accepted | Superseded by SADR-NNNN or target ADR NNNN\n \n ## Context\n \n What is the situation, constraint, or force that requires a decision? Reference\n-relevant domain terms (`CONTEXT.md`), prior ADRs, and code reality. State the\n-problem, not the solution.\n+relevant domain terms (`CONTEXT.md`), prior Shiki SADRs or target ADRs, and code\n+reality. State the problem, not the solution.\n \n ## Decision\n \ndiff --git a/docs/agents/autonomous-loop-e2e-acceptance.md b/docs/agents/autonomous-loop-e2e-acceptance.md\nindex bbf84c6081987f159ac6ad9ecf3c82b7aa72e7e8..5c13c1d02a05a90b0dc46cbef7c95a9846d0c237\n--- a/docs/agents/autonomous-loop-e2e-acceptance.md\n+++ b/docs/agents/autonomous-loop-e2e-acceptance.md\n@@ -40,8 +40,8 @@\n 3. `gh auth status` is logged in with merge permission on the repo.\n 4. Branch protection + required checks are configured exactly as the goal-loop\n    required-check set expects (see `docs/agents/branch-protection-smoke.md`).\n-5. The repo `.shiki/policy` permits autonomous low/medium-risk merges (ADR\n-   0008/0009); the MergeGate policy check is the recorded-authority gate.\n+5. The repo `.shiki/policy` permits autonomous low/medium-risk merges\n+   (SADR-0008/0009); the MergeGate policy check is the recorded-authority gate.\n \n The operator's only actions are: register the task and start the loop. **No\n manual pre-clearing** means after `shiki loop run` starts, the operator does not\ndiff --git a/docs/agents/bootstrap-command.md b/docs/agents/bootstrap-command.md\nindex 05f7f203a52cf412b9f97084a6e131ed0f785afd..f7aa0b734dfebee4a3b139335b66ee3415f48713\n--- a/docs/agents/bootstrap-command.md\n+++ b/docs/agents/bootstrap-command.md\n@@ -189,7 +189,9 @@\n ```\n \n Use this only for tests, fixtures, or explicit local-only template inspection.\n-Use `--force` only when you intentionally want to overwrite existing target files.\n+Use `--force` only when you intentionally want to refresh the target template.\n+Legacy numeric Shiki decision cleanup is stamp-and-digest authorized before any\n+write; target ADR paths are not cleanup candidates.\n \n ## Slash Command\n \ndiff --git a/docs/agents/checklists.md b/docs/agents/checklists.md\nindex 4a7f03ade346f53a1c1d82981f3fd744f9ab4547..0c5ec6c53dc7197b7b7a7d18e667b325babd6c45\n--- a/docs/agents/checklists.md\n+++ b/docs/agents/checklists.md\n@@ -32,18 +32,18 @@\n | ID | Item | Blocking |\n | --- | --- | --- |\n | GD-01 | Plan was challenged against `CONTEXT.md`. | blocking for non-trivial Goals |\n-| GD-02 | Relevant ADRs were read or absence was recorded. | blocking for non-trivial Goals |\n+| GD-02 | Relevant SADRs and target ADRs were read or absence was recorded. | blocking for non-trivial Goals |\n | GD-03 | Ambiguous terms were resolved or converted to HITL blockers. | blocking |\n | GD-04 | Edge scenarios were considered. | advisory |\n | GD-05 | Code reality was checked when code could answer. | blocking when applicable |\n | GD-06 | New domain terms were added to `CONTEXT.md` when needed. | blocking when applicable |\n-| GD-07 | ADR was created or explicitly skipped using the ADR criteria. | blocking when applicable |\n+| GD-07 | A SADR or target ADR was created or explicitly skipped using the decision-record criteria. | blocking when applicable |\n \n ## CI \u2014 Context & Impact Checklist\n \n | ID | Item | Blocking |\n | --- | --- | --- |\n-| CI-01 | Relevant docs and ADRs identified. | blocking |\n+| CI-01 | Relevant docs, SADRs, and target ADRs identified. | blocking |\n | CI-02 | Relevant modules, interfaces, seams, callers, and tests identified. | blocking |\n | CI-03 | Dependencies and blocked-by relationships identified. | blocking |\n | CI-04 | Candidate locks identified. | blocking |\n@@ -62,7 +62,7 @@\n | PRD-04 | Implementation decisions are recorded without volatile code detail. | blocking |\n | PRD-05 | Testing decisions are recorded. | blocking |\n | PRD-06 | Out-of-scope items are explicit. | blocking |\n-| PRD-07 | Relevant glossary/ADR links are present when applicable. | blocking |\n+| PRD-07 | Relevant glossary/SADR/target-ADR links are present when applicable. | blocking |\n \n ## SF \u2014 Spec Freeze Checklist\n \n@@ -98,7 +98,7 @@\n | H-02 | Branch/worktree target is present. | blocking |\n | H-03 | Scope and non-goals are present. | blocking |\n | H-04 | Dependencies and locks are present. | blocking |\n-| H-05 | Relevant docs, ADRs, modules, and tests are referenced. | blocking |\n+| H-05 | Relevant docs, SADRs, target ADRs, modules, and tests are referenced. | blocking |\n | H-06 | Required skills are stated. | blocking |\n | H-07 | TDD expectations are stated. | blocking when behavior work |\n | H-08 | Acceptance criteria are included. | blocking |\ndiff --git a/docs/agents/codex-handoff.md b/docs/agents/codex-handoff.md\nindex ede468f47cb277f69a857e8c45df703e4ed0151d..6f97bdffb67e44d7edea2c71e01ea28933c4eb6c\n--- a/docs/agents/codex-handoff.md\n+++ b/docs/agents/codex-handoff.md\n@@ -46,7 +46,7 @@\n \n ## Context\n - Domain terms:\n-- Relevant ADRs:\n+- Relevant SADRs and target ADRs:\n - Relevant docs:\n - Relevant modules/interfaces/seams:\n - Similar tests/prior art:\ndiff --git a/docs/agents/completion-check-agent.md b/docs/agents/completion-check-agent.md\nindex 13e4c035db35025f4e3cdcc5adfb24314ac4d521..61bfa1218cf36ca605250c91919e620f33238aa0\n--- a/docs/agents/completion-check-agent.md\n+++ b/docs/agents/completion-check-agent.md\n@@ -32,7 +32,7 @@\n - `AGENTS.md`\n - `CLAUDE.md`\n - `CONTEXT.md`\n-- relevant ADRs\n+- relevant SADRs and target ADRs\n - parent Goal issue\n - PRD issue or document\n - task issue\ndiff --git a/docs/agents/decision-control.md b/docs/agents/decision-control.md\nindex 7d8ee5ab03a5ccbcbd04056ae61de03785a2a9c9..8c94ecda00fffaa0e92c325ab83aa9776bd1dac1\n--- a/docs/agents/decision-control.md\n+++ b/docs/agents/decision-control.md\n@@ -11,7 +11,7 @@\n | Decision | Owner | When | Required inputs | Output | Enforcement surface |\n | --- | --- | --- | --- | --- | --- |\n | Is the Goal clear enough to plan? | Planner + Guardian when needed | Goal intake | User request, existing docs, risk signals | Goal readiness verdict | Goal checklist, issue template, ledger |\n-| Must `grill-with-docs` run? | Planner / Skill Gate | Before PRD or issue split | Ambiguity, domain terms, ADR conflicts, unclear boundaries | Skill invocation or explicit skip rationale | Skill Gate checklist |\n+| Must `grill-with-docs` run? | Planner / Skill Gate | Before PRD or issue split | Ambiguity, domain terms, SADR/target-ADR conflicts, unclear boundaries | Skill invocation or explicit skip rationale | Skill Gate checklist |\n | Is a PRD required? | Planner | After Goal context stabilizes | Goal, `grill-with-docs` output, Context & Impact output | PRD required / not required | PRD checklist, CCA evidence check |\n | Can the spec be frozen? | Operator only | End of Requirements Definition, before issue split | PRD/requirements summary, scope inventory (SF-02), open-question list | `spec_freeze` block in the plan + ledger evidence | `require_grilled_plan` (plan ingest/run), SF checklist |\n | Can a frozen spec change? | Operator only (Spec Amendment) | When implementation contradicts the frozen spec | Contested decisions, scoped re-grill result | recorded amendment + re-stamped freeze | SF checklist, ledger evidence |\ndiff --git a/docs/agents/domain.md b/docs/agents/domain.md\nindex 258da8ea81e87ac0a6b0b5b5e5c2fde980eaabb7..d57b0cf17ff3e1fa9e6e5885372bf083d8295421\n--- a/docs/agents/domain.md\n+++ b/docs/agents/domain.md\n@@ -5,7 +5,9 @@\n ## Layout\n \n - `CONTEXT.md` defines Shiki language. Keep it implementation-free.\n-- `docs/adr/` records hard-to-reverse decisions.\n+- `docs/adr/SADR-NNNN-*.md` records Shiki platform decisions.\n+- `docs/adr/NNNN-*.md` records target product decisions without sharing the\n+  SADR namespace.\n - `AGENTS.md` is the runtime-neutral agent constitution.\n - `CLAUDE.md` is the Claude Code wrapper around `AGENTS.md`.\n - `.shiki/` is the Target Repository mirror for Goals, PRDs, plans, Task DAGs, contracts, locks, worktree records, ledger entries, CCA verdicts, repair packets, reports, and handoffs.\n@@ -15,6 +17,6 @@\n \n - Use Shiki terms exactly when they exist.\n - Add glossary terms to `CONTEXT.md` only when the term is part of the domain, not a generic programming concept.\n-- Create ADRs only for hard-to-reverse, surprising, tradeoff-based decisions.\n+- Create SADRs or target ADRs only for hard-to-reverse, surprising, tradeoff-based decisions.\n - Keep product-specific language in the Target Repository, not in Shiki Template docs.\n-- If an ADR conflicts with a proposed implementation, surface the conflict before editing.\n+- If a SADR or target ADR conflicts with a proposed implementation, surface the conflict before editing.\ndiff --git a/docs/agents/execution-control.md b/docs/agents/execution-control.md\nindex ad8ee3cdb36be56fedc1f46408ca92b387d74047..bc63c3a73661d12b567016ba299f1a6b66bf9088\n--- a/docs/agents/execution-control.md\n+++ b/docs/agents/execution-control.md\n@@ -63,7 +63,7 @@\n | Branch name | Orchestrator | Orchestrator | Before implementer dispatch | branch policy | branch ref in task/ledger |\n | Lock reservation | Context & Impact | MergeGate | Before dispatch and before merge | `.shiki/locks/`, labels, PR metadata | lock id, owner, scope |\n | CI profile | Planner | MergeGate | During issue triage, rechecked at PR | labels, `.shiki/policy.yaml`, workflow matrix | required check list |\n-| Required status checks | Repo maintainer / Guardian | GitHub branch protection / rulesets | Repository setup, updated via ADR | GitHub settings | branch/ruleset config, ADR |\n+| Required status checks | Repo maintainer / Guardian | GitHub branch protection / rulesets | Repository setup, updated via target ADR | GitHub settings | branch/ruleset config, target ADR |\n | CCA judgment | CCA | GitHub required check / MergeGate | Every non-draft PR update | GitHub Action check | structured CCA verdict |\n | Repair assignment | CCA | MergeGate | After failed CCA/CI/review | repair packet, issue/PR comment | repair id and attempt count |\n | Merge readiness | MergeGate | GitHub branch protection / Guardian if needed | After CCA complete | required checks, review rules, rulesets | mergegate check output |\ndiff --git a/docs/agents/guardian-policy.md b/docs/agents/guardian-policy.md\nindex 4147c7adf05a47adb4fd24ada1a1c2e1f2ba6d2d..a20d9cc6c00a569bb057b9ca8d6b51c7fc4a9847\n--- a/docs/agents/guardian-policy.md\n+++ b/docs/agents/guardian-policy.md\n@@ -98,8 +98,8 @@\n `origin/<default_branch>` and reuses each job's existing base fetch; a shallow\n (`--depth`) or second base fetch would make the proof unresolvable and silently\n disable the carry, so neither is permitted. CCA reads `guardian_comment_carried` as\n-a recorded authority in the deterministic signal and does not re-derive it. See ADR\n-0018 for the full decision, the rejected diff/patch-equality and path-disjointness\n+a recorded authority in the deterministic signal and does not re-derive it. See\n+SADR-0018 for the full decision, the rejected diff/patch-equality and path-disjointness\n alternatives, and the accepted residual.\n \n ## External AI Guardian Review (SADR-0010)\ndiff --git a/docs/agents/implementation-policy.md b/docs/agents/implementation-policy.md\nindex c754144a9fb5bf4a38aa82a60e1670c59e626b44..85c37281bcad196c6b7b02343faf00b03cd9c57a\n--- a/docs/agents/implementation-policy.md\n+++ b/docs/agents/implementation-policy.md\n@@ -31,10 +31,13 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n ## Non-Negotiables\n \n 1. **Planning precedes implementation.** Non-trivial Goals start with `grill-with-docs` and Context & Impact.\n-2. **Domain language is controlled.** Terms must be checked against `CONTEXT.md`; hard-to-reverse tradeoffs must be checked against ADRs.\n+2. **Domain language is controlled.** Terms must be checked against `CONTEXT.md`; hard-to-reverse tradeoffs must be checked against Shiki SADRs and target ADRs.\n 3. **PRD precedes issue decomposition.** `to-prd` is used once the context is settled enough to become durable intent.\n 4. **Issues are vertical slices.** `to-issues` must produce independently grabbable tracer-bullet tasks, not horizontal layer tickets.\n 5. **Triage controls dispatch.** Only `ready-for-agent` / AFK tasks may be assigned to Codex without human input.\n@@ -95,19 +98,19 @@\n Process:\n \n - Challenge terms against `CONTEXT.md`.\n-- Check ADRs before reopening decisions.\n+- Check SADRs and target ADRs before reopening decisions.\n - Inspect code when code can answer a question.\n - Ask one question at a time when the operator is available.\n - Provide a recommended answer with each question.\n - Record resolved terms in `CONTEXT.md`.\n-- Create ADRs only for hard-to-reverse, surprising, tradeoff-based decisions.\n+- Create SADRs or target ADRs only for hard-to-reverse, surprising, tradeoff-based decisions.\n - Convert unresolved load-bearing questions into HITL blockers.\n \n Outputs:\n \n - Resolved design-tree decisions.\n - Updated glossary terms when needed.\n-- ADRs when justified.\n+- SADRs or target ADRs when justified.\n - HITL blockers when decisions remain unresolved.\n \n ## Phase 3 \u2014 Context & Impact\n@@ -117,7 +120,7 @@\n \n Outputs must include:\n \n-- Relevant documents and ADRs.\n+- Relevant documents, SADRs, and target ADRs.\n - Relevant modules, interfaces, seams, callers, and tests.\n - Candidate locks.\n - Dependencies.\n@@ -140,7 +143,7 @@\n - Testing decisions.\n - Out of scope.\n - Further notes.\n-- Links to relevant Goal, domain terms, and ADRs.\n+- Links to relevant Goal, domain terms, SADRs, and target ADRs.\n \n Phase 4 ends with Spec Freeze: the operator explicitly approves the PRD, the\n plan records a `spec_freeze` block (status, approved_by, source), and the SF\n@@ -240,7 +243,7 @@\n - Dependencies and locks.\n - Risk and Guardian requirements.\n - Ledger completeness.\n-- Documentation and ADR updates.\n+- Documentation and SADR/target-ADR updates.\n - Scope drift.\n \n CCA emits one of:\n@@ -301,7 +304,7 @@\n | Evidence failure | Behavior might be done, but proof is missing. | Add checks, PR evidence, ledger entry, or artifacts. |\n | Scope drift | PR changes unrelated behavior. | Revert unrelated changes or split into new Goal. |\n | Dependency block | Upstream task or lock not complete. | Block until dependency resolves. |\n-| Architecture block | Decision needs ADR or deep-module discussion. | `grill-with-docs` or `improve-codebase-architecture`. |\n+| Architecture block | Decision needs a SADR, target ADR, or deep-module discussion. | `grill-with-docs` or `improve-codebase-architecture`. |\n | Risk block | Security/production/policy change needs human approval. | Guardian decision. |\n \n ## Phase 5.5 \u2014 Execution Decision Control\ndiff --git a/docs/agents/runtime-auth-model.md b/docs/agents/runtime-auth-model.md\nindex 6a2e1a47e81ea89b5c68a210ec07772a9374babc..3c54748d24233dbd0ba8473950282547ef4ce935\n--- a/docs/agents/runtime-auth-model.md\n+++ b/docs/agents/runtime-auth-model.md\n@@ -148,7 +148,7 @@\n \n API-key based runners may be added by a target repository as an explicit extension. They are not the default Shiki template path.\n \n-If a repo opts into API-key mode, record the exception in an ADR and update the repo's `AGENTS.md`, `.shiki/config.yaml`, and workflow permissions.\n+If a repo opts into API-key mode, record the target-specific exception in a target ADR and update the repo's `AGENTS.md`, `.shiki/config.yaml`, and workflow permissions.\n \n ## GitHub Actions Runtime Compatibility\n \ndiff --git a/docs/agents/skill-gate.md b/docs/agents/skill-gate.md\nindex c50398b0d7c60a8598f2a5c88a10015730dced13..3acffe6444617bb1d54b77d4b4708158ec735bc6\n--- a/docs/agents/skill-gate.md\n+++ b/docs/agents/skill-gate.md\n@@ -9,7 +9,7 @@\n | Skill | Trigger | Expected output |\n | --- | --- | --- |\n | `setup-matt-pocock-skills` | First configuring a repo for these skills, or when issue tracker, triage labels, or domain docs are missing. | `docs/agents/` configuration and an `Agent skills` block in `AGENTS.md` or `CLAUDE.md`. |\n-| `grill-with-docs` | Ambiguous plan, unclear terminology, boundary decision, tradeoff, or ADR-worthy design choice. | Clarified decision tree, recommended answers, updates to `CONTEXT.md` or ADRs when decisions crystallize. |\n+| `grill-with-docs` | Ambiguous plan, unclear terminology, boundary decision, tradeoff, or decision-record-worthy design choice. | Clarified decision tree, recommended answers, updates to `CONTEXT.md`, SADRs, or target ADRs when decisions crystallize. |\n | `zoom-out` | Agent lacks a higher-level architectural map. | Map of relevant modules, callers, seams, and domain concepts. |\n | `to-prd` | Settled context should become a PRD. | PRD published to the issue tracker with problem, solution, user stories, decisions, testing, and out-of-scope notes. |\n | `to-issues` | Goal, PRD, or plan must become implementation tickets. | Vertical-slice issues with acceptance criteria, dependencies, and AFK/HITL classification. |\n@@ -17,7 +17,7 @@\n | `tdd` | Feature work or bug fix has observable behavior that can be specified with tests. | Red-green-refactor loop through public interfaces, one vertical slice at a time. |\n | `code-review` | The TDD loop is green and the implementer is about to create or update a PR. Default for every implementation task. | Pre-PR self-review of the task diff with fixes applied on the task branch; findings and resolutions recorded as a ledger entry naming the skill and a `## Pre-PR code review` PR body section. |\n | `diagnose` | Hard bug, failing check, regression, flaky behavior, or performance problem. | Reproduction loop, ranked hypotheses, instrumentation, fix, regression test, post-mortem. |\n-| `improve-codebase-architecture` | Structural friction, testability, AI-navigability, deep-module opportunity, or refactor request. | Architecture candidates and deepening recommendations informed by `CONTEXT.md` and ADRs. |\n+| `improve-codebase-architecture` | Structural friction, testability, AI-navigability, deep-module opportunity, or refactor request. | Architecture candidates and deepening recommendations informed by `CONTEXT.md`, SADRs, and target ADRs. |\n | `prototype` | A throwaway logic or UI prototype is needed to answer a design question. | Clearly marked disposable prototype and captured decision. |\n \n ## MergeGate Interaction\ndiff --git a/docs/operations.md b/docs/operations.md\nindex cf1f452d4c589e38ccd58553bda0ad8684b51eb7..ace48a90ffbec9e5c0a7113bef16a4858bc44b3f\n--- a/docs/operations.md\n+++ b/docs/operations.md\n@@ -89,6 +89,16 @@\n      target history, brought forward by `shiki migrate`, not a template.\n    - **Everything else** (CLI scripts, workflows, schemas, docs) is overwritten.\n \n+   A forced upgrade also migrates the former numeric Shiki decision paths to\n+   `SADR-NNNN-*`. Before the first write, the installer inventories the exact\n+   legacy Shiki paths and authorizes each deletion only when the existing\n+   `.shiki/install-stamp.json` names that path and its SHA-256 digest still\n+   matches the file. An absent, unreadable, malformed, incomplete, or\n+   mismatching stamp reports every blocked path and stops with no filesystem\n+   change. Target-owned `NNNN-*` ADRs are not cleanup candidates. A non-force\n+   install deletes nothing and retains any still-matching legacy stamp entries\n+   so a later forced upgrade can prove ownership.\n+\n    If the target has pending migrations, `--force` refuses and writes nothing \u2014\n    apply migrations first (step 3), then re-run. The run ends with a single\n    summary of every `.new` written, so a half-upgrade is visible.\ndiff --git a/docs/releasing.md b/docs/releasing.md\nindex 10d73828e7dbd64e9d9f6b83bc84d010537ce62a..aafeee8c4d441e1e427efee2ce4f81a647f020e1\n--- a/docs/releasing.md\n+++ b/docs/releasing.md\n@@ -67,7 +67,7 @@\n CLI in `scripts/`. There are no third-party Python runtime dependencies. The CLI\n is intentionally **not** exposed as a `[project.scripts]` console entry point\n because `scripts/shiki.py` is a dependency-free shim and `scripts/` is not\n-packaged as an importable distribution (see the ADR for the rationale).\n+packaged as an importable distribution (see SADR-0007 for the rationale).\n \n Supported install paths:\n \n@@ -155,6 +155,15 @@\n    [docs/operations.md](operations.md#the-new-convention). If the target has\n    pending migrations, `--force` refuses and writes nothing; apply migrations\n    (step 2) first, then re-run.\n+\n+   The SADR namespace upgrade has an additional pre-write gate: each former\n+   numeric Shiki decision path is removed only when the existing install stamp\n+   contains that exact path and a digest matching its current bytes. The\n+   installer aggregates absent/unreadable/malformed-stamp, missing-digest, and\n+   digest-mismatch blockers before raising, so refusal leaves the target tree\n+   byte-identical. Numeric target ADRs are preserved. A successful forced\n+   upgrade installs and stamps `SADR-*` paths and drops the removed legacy paths\n+   from the refreshed stamp.\n 2. Apply repository-local `.shiki/` state migrations with the migrate command:\n \n    ```bash\ndiff --git a/docs/usage.md b/docs/usage.md\nindex b83407ee3970dd395dd893010bc5dbe8bad26051..a130ef6e40dfd8169a5bc701339351097df1a09f\n--- a/docs/usage.md\n+++ b/docs/usage.md\n@@ -67,8 +67,10 @@\n shiki install-target /path/to/target-repo --local-only\n ```\n \n-Use `--force` only when you intentionally want to overwrite existing target\n-files.\n+Use `--force` only when you intentionally want to refresh the target template.\n+For the SADR namespace migration, `--force` deletes only exact legacy Shiki\n+decision paths whose existing install-stamp digest still matches; any ownership\n+blocker stops before the first write. Numeric target ADRs are never candidates.\n \n ## `shiki bootstrap-platform` \u2014 publish the Shiki platform repo\n \ndiff --git a/scripts/guardian_approval_signal.py b/scripts/guardian_approval_signal.py\nindex d059f92b2c82aa2b5d6ca421365537a87886c1cb..16924892714f4f5b9303c0ef0e325a7e70075630\n--- a/scripts/guardian_approval_signal.py\n+++ b/scripts/guardian_approval_signal.py\n@@ -199,8 +199,8 @@\n ) -> Any:\n     \"\"\"Evaluate SADR-0015 Contract Approval for the CCA Guardian signal.\n \n-    The SAME OR that MergeGate applies must reach this deterministic signal: ADR\n-    0015 is explicit that without it, a contract-approved PR still returns\n+    The SAME OR that MergeGate applies must reach this deterministic signal:\n+    SADR-0015 is explicit that without it, a contract-approved PR still returns\n     ``needs_guardian``, the CCA job fails, and the MergeGate policy check never\n     runs \u2014 the gate this change moves would never be reached. This resolves the\n     task the SAME way MergeGate does (the first Shiki task id in the PR body),\ndiff --git a/scripts/shiki_contract_approval.py b/scripts/shiki_contract_approval.py\nindex 941bcb96fdd8b9a85bc0c4f0f04d5dad9d04839c..4a4b418f6527413161db82670bec857a043360e2\n--- a/scripts/shiki_contract_approval.py\n+++ b/scripts/shiki_contract_approval.py\n@@ -142,7 +142,7 @@\n     changed_files_status: Any,\n     registration: Any,\n ) -> ContractApprovalResult:\n-    \"\"\"Whether this PR carries an SADR-0015 Contract Approval.\n+    \"\"\"Whether this PR carries a SADR-0015 Contract Approval.\n \n     The four conditions are evaluated IN ORDER, each failure recorded as a\n     distinct reason string:\ndiff --git a/scripts/shiki_guardian.py b/scripts/shiki_guardian.py\nindex a4ecb79bbda55fd5c8b06adfae2eb99760a03c45..4dd99bbe714e26f421fa19abac7a34ae1b195ecc\n--- a/scripts/shiki_guardian.py\n+++ b/scripts/shiki_guardian.py\n@@ -12,7 +12,7 @@\n # The carry re-derives its proof from git in-process on every judging invocation;\n # it is never read from an artifact, so there is nothing to go stale or be forged.\n # shiki_sync_proof is the ONLY guardian-family module that runs git, and every one\n-# of its invocations is read-only and local (ADR: prove-from-git base sync).\n+# of its invocations is read-only and local (SADR: prove-from-git base sync).\n from shiki_sync_proof import verify_pure_base_sync\n \n GUARDIAN_POLICY_PATH = \".shiki/guardian-policy.json\"\ndiff --git a/scripts/shiki_guardian_review.py b/scripts/shiki_guardian_review.py\nindex dd0f104d457899e0ba5ff1e162011c1e90b6f92b..e10ef58c046324e7ea3a3392feb3048ca77414ce\n--- a/scripts/shiki_guardian_review.py\n+++ b/scripts/shiki_guardian_review.py\n@@ -159,7 +159,7 @@\n     docs_only = _is_docs_only(changed_files)\n     if docs_only and matched == 0:\n         focus.append(\n-            \"Docs/ADR-only change: confirm the change is genuinely documentation/decision-only, introduces no behavior or contract drift, and stays consistent with the constitution and existing ADRs.\"\n+            \"Docs/decision-record-only change: confirm the change is genuinely documentation/decision-only, introduces no behavior or contract drift, and stays consistent with the constitution and existing SADRs and target ADRs.\"\n         )\n \n     # Conservative augmentation: unknown (no category and not docs-only), mixed\ndiff --git a/scripts/shiki_installer.py b/scripts/shiki_installer.py\nindex be064ab7a29ff6836f7f7a1d70500a43e0d11d7b..9fd8eefd137ce7003dad5c0dcb2e48e2cd47ab32\n--- a/scripts/shiki_installer.py\n+++ b/scripts/shiki_installer.py\n@@ -118,6 +118,7 @@\n # what lets a target carry it without a governance-schema change.\n INSTALL_STAMP_PATH = \".shiki/install-stamp.json\"\n INSTALL_STAMP_VERSION = 1\n+LEGACY_SHIKI_SADR_MAX = 18\n \n # Under --force the shipped surface splits three ways, so nothing is lost and\n # nothing is silently stale.\n@@ -237,9 +238,98 @@\n         )\n \n \n+def legacy_shiki_adr_paths() -> tuple[str, ...]:\n+    \"\"\"Former numeric paths for Shiki SADR-0001 through SADR-0018.\n+\n+    The authoritative slugs come from the current SADR filenames. SADR-0019 is\n+    the namespace decision itself and never shipped under a numeric legacy path.\n+    \"\"\"\n+    paths: list[str] = []\n+    for sadr in sorted((ROOT / \"docs\" / \"adr\").glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\")):\n+        number = sadr.name.split(\"-\", 2)[1]\n+        if int(number) <= LEGACY_SHIKI_SADR_MAX:\n+            paths.append(f\"docs/adr/{sadr.name.removeprefix('SADR-')}\")\n+    return tuple(paths)\n+\n+\n+def preflight_legacy_sadr_cleanup(target: Path, *, force: bool) -> tuple[str, ...]:\n+    \"\"\"Authorize exact legacy Shiki ADR removals before the first write.\n+\n+    A non-force install never removes existing files. A forced install needs no\n+    stamp when no legacy Shiki path is present. When legacy paths do exist, each\n+    must be named in a readable install stamp and its current bytes must match\n+    the recorded digest.\n+    \"\"\"\n+    if not force:\n+        return ()\n+    present = tuple(\n+        relative\n+        for relative in legacy_shiki_adr_paths()\n+        if (target / relative).exists() or (target / relative).is_symlink()\n+    )\n+    if not present:\n+        return ()\n+\n+    stamp_path = target / INSTALL_STAMP_PATH\n+    try:\n+        raw = stamp_path.read_text(encoding=\"utf-8\")\n+        stamp = json.loads(raw)\n+    except FileNotFoundError:\n+        detail = \"install stamp is absent\"\n+        blockers = [f\"{relative}: {detail}\" for relative in present]\n+    except (OSError, UnicodeDecodeError) as error:\n+        detail = f\"install stamp is unreadable ({error})\"\n+        blockers = [f\"{relative}: {detail}\" for relative in present]\n+    except json.JSONDecodeError as error:\n+        detail = f\"install stamp is malformed ({error})\"\n+        blockers = [f\"{relative}: {detail}\" for relative in present]\n+    else:\n+        if not isinstance(stamp, dict):\n+            blockers = [\n+                f\"{relative}: install stamp is malformed (expected an object)\"\n+                for relative in present\n+            ]\n+        else:\n+            digests = stamp.get(\"digests\")\n+            blockers = []\n+            if not isinstance(digests, dict):\n+                blockers.extend(\n+                    f\"{relative}: install stamp has no digest for this path\"\n+                    for relative in present\n+                )\n+            else:\n+                for relative in present:\n+                    expected = digests.get(relative)\n+                    if not isinstance(expected, str) or not expected:\n+                        blockers.append(\n+                            f\"{relative}: install stamp has no digest for this path\"\n+                        )\n+                        continue\n+                    try:\n+                        actual = _sha256_file(target / relative)\n+                    except OSError as error:\n+                        blockers.append(\n+                            f\"{relative}: legacy path is unreadable ({error})\"\n+                        )\n+                        continue\n+                    if actual != expected:\n+                        blockers.append(\n+                            f\"{relative}: digest mismatch (stamp {expected}, current {actual})\"\n+                        )\n+\n+    if blockers:\n+        joined = \"\\n\".join(f\"- {blocker}\" for blocker in blockers)\n+        raise ShikiError(\n+            \"legacy Shiki ADR cleanup is not authorized:\\n\"\n+            f\"{joined}\\nNo files were written.\"\n+        )\n+    return present\n+\n+\n def install_template(target: Path, *, force: bool, validate: bool) -> list[NewFileNote]:\n     # Preconditions first: never rewrite the tree of a target that cannot be\n     # upgraded cleanly (see refuse_pending_migrations).\n+    legacy_cleanup = preflight_legacy_sadr_cleanup(target, force=force)\n     refuse_pending_migrations(target)\n \n     # Resolve the target's required CODEOWNERS owner once from its\n@@ -250,6 +340,10 @@\n     # verbatim. See ``codeowners_required_owner``.\n     codeowners_owner = codeowners_required_owner(target)\n \n+    for relative in legacy_cleanup:\n+        (target / relative).unlink()\n+        info(f\"removed stamped legacy Shiki ADR: {target / relative}\")\n+\n     notes: list[NewFileNote] = []\n     for relative in TEMPLATE_PATHS:\n         source = ROOT / relative\n@@ -647,6 +741,22 @@\n     digests = target_stamp_digests(target)\n     existing = read_install_stamp(target)\n     if existing is not None:\n+        # A non-force install deliberately leaves legacy Shiki ADRs in place.\n+        # Preserve only still-matching ownership proof so a later forced\n+        # upgrade can authorize cleanup; never carry a missing or stale digest.\n+        existing_digests = existing.get(\"digests\")\n+        if isinstance(existing_digests, dict):\n+            for relative in legacy_shiki_adr_paths():\n+                expected = existing_digests.get(relative)\n+                path = target / relative\n+                if not isinstance(expected, str) or not path.is_file():\n+                    continue\n+                try:\n+                    actual = _sha256_file(path)\n+                except OSError:\n+                    continue\n+                if actual == expected:\n+                    digests[relative] = actual\n         if _incoming_is_older(commit, existing.get(\"platform_commit\")):\n             warn(\n                 \"kept existing install stamp: incoming install \"\ndiff --git a/scripts/shiki_loop.py b/scripts/shiki_loop.py\nindex 3611641da8aeb5038b1da90e3114b09a473ec6d8..84caa491b9cad0edc4176f209c13e9221e975681\n--- a/scripts/shiki_loop.py\n+++ b/scripts/shiki_loop.py\n@@ -424,7 +424,7 @@\n         # unresolvable CCA verdict has already produced its terminal stop and is never\n         # laundered into a head-moving sync (SADR-0010). It also never fires while the\n         # policy (Guardian) gate is red \u2014 for ANY reason, not only when it is the sole\n-        # red check \u2014 so a head-bound Guardian approval, or an SADR-0015\n+        # red check \u2014 so a head-bound Guardian approval, or a SADR-0015\n         # contract-approval carry that base movement defeated (the policy gate compares\n         # base and head task governance directly), is never silently restored by\n         # adopting base wholesale. It is bounded by a durable per-task counter so the\ndiff --git a/scripts/shiki_tasks.py b/scripts/shiki_tasks.py\nindex aec3809f24b4a0c8c96fc50971cff9ae9ad30381..d531052e9d9717058d4631483cefab44c26a65c1\n--- a/scripts/shiki_tasks.py\n+++ b/scripts/shiki_tasks.py\n@@ -821,7 +821,7 @@\n         \"prompt\": prompt,\n         \"entry_skill\": \"grill-with-docs\",\n         \"required_next_steps\": [\n-            \"Run grill-with-docs until terminology, boundaries, risks, and ADR-worthy decisions are settled.\",\n+            \"Run grill-with-docs until terminology, boundaries, risks, and decision-record-worthy choices are settled.\",\n             \"Obtain the operator's explicit approval of the PRD/requirements (Spec Freeze).\",\n             \"Write a machine-readable plan JSON with grill_with_docs.status=complete and spec_freeze.status=frozen.\",\n             \"Run shiki plan ingest --plan-file PLAN.json.\",\ndiff --git a/scripts/test_shiki_goal_loop.sh b/scripts/test_shiki_goal_loop.sh\nindex 753c7b516e10f0be3bc92a4bd0eb1fa699776ae6..8f3820a0a079b3044424cfe1a6c6b7fe68896e67\n--- a/scripts/test_shiki_goal_loop.sh\n+++ b/scripts/test_shiki_goal_loop.sh\n@@ -231,8 +231,8 @@\n \n # High/critical risk merges autonomously when all required checks are green:\n # the \"MergeGate policy check\" required check IS the Guardian gate (it enforces\n-# guardian-policy.json \u2014 human approval OR external AI guardian review, ADR\n-# 0010), so green checks mean a recorded authority approved. The fake gh returns\n+# guardian-policy.json \u2014 human approval OR external AI guardian review,\n+# SADR-0010), so green checks mean a recorded authority approved. The fake gh returns\n # green checks, simulating that the Guardian gate passed.\n python3 \"$ROOT/scripts/shiki.py\" goal create --target \"$TARGET\" --title \"Guardian gate\" --outcome \"High risk merges via policy gate\" >/tmp/shiki-goal-loop-guardian-goal.json\n GGOAL=\"$(json_get /tmp/shiki-goal-loop-guardian-goal.json goal_id)\"\ndiff --git a/tests/test_adr_references.py b/tests/test_adr_references.py\nindex 51706329c797d5dd9b47e082a333fa83b261d482..d005da4f771f5e8085ec22d4b787980e68f5ae8c\n--- a/tests/test_adr_references.py\n+++ b/tests/test_adr_references.py\n@@ -1,8 +1,8 @@\n-\"\"\"Every ``ADR ####`` citation must resolve to exactly one decision record.\n+\"\"\"Every target ADR and Shiki SADR citation resolves in its own namespace.\n \n Background. ``scripts/mergegate_check.py`` justifies ``is_bookkeeping_closeout``\n-with \"ADR 0017\" nine times and ``scripts/guardian_approval_signal.py`` cites\n-0016/0017 seven times, yet for a stretch the numbering in ``docs/adr/`` stopped\n+with \"SADR-0017\" nine times and ``scripts/guardian_approval_signal.py`` cites\n+SADR-0016/SADR-0017 seven times, yet for a stretch the numbering in ``docs/adr/`` stopped\n at 0015: the authoring commits lived on a coordinator branch and no task ever\n declared ``docs/adr/...`` in its locks, so nothing carried the records to the\n default branch. The stated basis of an exemption that merged seven PRs with zero\n@@ -11,11 +11,12 @@\n and the directory drifted with no signal.\n \n This is the same unbound-pair shape behind other silent failures: a reference\n-and its target with nothing asserting they agree. This test binds them. It scans\n-every ``scripts/*.py`` (plus ``docs/**/*.md`` and the root ``*.md`` governance\n-files, which are cheap) for the ``ADR ####`` pattern and asserts each cited\n-number resolves to exactly one ``docs/adr/####-*.md``. The converse is\n-deliberately NOT required: an ADR may exist without being cited.\n+and its target with nothing asserting they agree. This test binds them. Target\n+``ADR NNNN`` citations resolve only to ``docs/adr/NNNN-*.md``; Shiki\n+``SADR-NNNN`` citations resolve only to ``docs/adr/SADR-NNNN-*.md``. The scan\n+covers current governance docs, scripts, workflows, prompts, packaging metadata,\n+and repo-local Codex guidance while deliberately excluding historical\n+``.shiki`` evidence. The converse is not required: a record may be uncited.\n \n The suite is dependency free: standard-library ``unittest`` only.\n \"\"\"\n@@ -31,17 +32,18 @@\n \n REPO_ROOT = Path(__file__).resolve().parents[1]\n \n-# \"ADR 0016\" -- a four-digit citation. The trailing \\b keeps a five-digit token\n-# from being read as a truncated four-digit number.\n-_CITATION = re.compile(r\"\\bADR (\\d{4})\\b\")\n-# \"0016-locks-bound-product-paths.md\" -- the leading four digits name the record.\n-_ADR_FILENAME = re.compile(r\"^(\\d{4})-.*\\.md$\")\n+# ``ADR 0016`` / ``SADR-0016`` -- a namespace plus four-digit citation. The\n+# trailing ``\\b`` keeps a five-digit token from being read as a truncated one.\n+_CITATION = re.compile(r\"\\b(S?ADR)[ -](\\d{4})\\b\")\n+# Target ADRs retain the numeric filename; Shiki platform decisions use SADR.\n+_DECISION_FILENAME = re.compile(r\"^(?:(SADR)-)?(\\d{4})-.*\\.md$\")\n \n \n class Violation(NamedTuple):\n     \"\"\"A citation that does not resolve to exactly one decision record.\"\"\"\n \n     citing_file: str  # repo-relative path of the file that cites the ADR\n+    namespace: str  # \"ADR\" for target decisions; \"SADR\" for Shiki decisions\n     number: str  # the cited four-digit number, e.g. \"0016\"\n     line: int  # 1-based line of the citation\n     reason: str  # \"missing\" (zero matches) or \"ambiguous\" (two or more)\n@@ -49,44 +51,59 @@\n \n     @property\n     def expected_path(self) -> str:\n-        return f\"docs/adr/{self.number}-*.md\"\n+        prefix = \"SADR-\" if self.namespace == \"SADR\" else \"\"\n+        return f\"docs/adr/{prefix}{self.number}-*.md\"\n \n     def describe(self) -> str:\n         \"\"\"Name the citing file, the cited number, and the missing path.\"\"\"\n         if self.reason == \"missing\":\n             return (\n-                f\"{self.citing_file}:{self.line} cites ADR {self.number} \"\n+                f\"{self.citing_file}:{self.line} cites {self.namespace} {self.number} \"\n                 f\"but no document matches {self.expected_path}\"\n             )\n         joined = \", \".join(self.matches)\n         return (\n-            f\"{self.citing_file}:{self.line} cites ADR {self.number} which \"\n+            f\"{self.citing_file}:{self.line} cites {self.namespace} {self.number} which \"\n             f\"resolves to {len(self.matches)} documents (expected exactly one \"\n             f\"at {self.expected_path}): {joined}\"\n         )\n \n \n-def _adr_index(root: Path) -> dict[str, list[Path]]:\n-    \"\"\"Map every four-digit ADR number to the record files that claim it.\"\"\"\n-    index: dict[str, list[Path]] = {}\n+def _adr_index(root: Path) -> dict[tuple[str, str], list[Path]]:\n+    \"\"\"Map each decision namespace and number to the files that claim it.\"\"\"\n+    index: dict[tuple[str, str], list[Path]] = {}\n     adr_dir = root / \"docs\" / \"adr\"\n     if adr_dir.is_dir():\n         for path in sorted(adr_dir.glob(\"*.md\")):\n-            match = _ADR_FILENAME.match(path.name)\n+            match = _DECISION_FILENAME.match(path.name)\n             if match:\n-                index.setdefault(match.group(1), []).append(path)\n+                namespace = \"SADR\" if match.group(1) else \"ADR\"\n+                index.setdefault((namespace, match.group(2)), []).append(path)\n     return index\n \n \n def _citing_files(root: Path, include_docs: bool = True) -> list[Path]:\n-    \"\"\"Files scanned for citations: scripts/*.py, and cheaply the markdown.\"\"\"\n+    \"\"\"Current, shipped text surfaces scanned for decision citations.\"\"\"\n     files = sorted((root / \"scripts\").glob(\"*.py\"))\n+    files += sorted((root / \"scripts\").glob(\"*.sh\"))\n     if include_docs:\n         docs = root / \"docs\"\n         if docs.is_dir():\n             files += sorted(docs.rglob(\"*.md\"))\n         files += sorted(root.glob(\"*.md\"))\n-    return files\n+        pyproject = root / \"pyproject.toml\"\n+        if pyproject.is_file():\n+            files.append(pyproject)\n+        for relative in (\".github\", \".codex\"):\n+            surface = root / relative\n+            if surface.is_dir():\n+                files += sorted(\n+                    path\n+                    for path in surface.rglob(\"*\")\n+                    if path.is_file()\n+                    and path.suffix in {\".md\", \".py\", \".sh\", \".toml\", \".yaml\", \".yml\"}\n+                )\n+    return sorted(set(files))\n \n \n def find_adr_reference_violations(\n@@ -94,10 +111,9 @@\n ) -> list[Violation]:\n     \"\"\"Return every ``ADR ####`` citation under ``root`` that fails to resolve.\n \n-    A citation resolves when exactly one ``docs/adr/####-*.md`` matches its\n-    number. Zero matches is a \"missing\" violation; two or more is \"ambiguous\".\n-    Records that are never cited produce nothing -- the converse is not\n-    required.\n+    A citation resolves when exactly one record in the same namespace matches\n+    its number. Zero matches is a \"missing\" violation; two or more is\n+    \"ambiguous\". Records that are never cited produce nothing.\n     \"\"\"\n     root = Path(root)\n     index = _adr_index(root)\n@@ -110,13 +126,15 @@\n         rel = path.relative_to(root).as_posix()\n         for lineno, line in enumerate(text.splitlines(), start=1):\n             for match in _CITATION.finditer(line):\n-                number = match.group(1)\n-                found = index.get(number, [])\n+                namespace, number = match.groups()\n+                found = index.get((namespace, number), [])\n                 if len(found) == 1:\n                     continue\n                 reason = \"missing\" if not found else \"ambiguous\"\n                 rels = [p.relative_to(root).as_posix() for p in found]\n-                violations.append(Violation(rel, number, lineno, reason, rels))\n+                violations.append(\n+                    Violation(rel, namespace, number, lineno, reason, rels)\n+                )\n     return violations\n \n \n@@ -129,8 +147,8 @@\n         self.assertEqual(\n             violations,\n             [],\n-            \"Every 'ADR ####' citation must resolve to exactly one \"\n-            f\"docs/adr/####-*.md:\\n{detail}\",\n+            \"Every ADR/SADR citation must resolve exactly once in its own \"\n+            f\"docs/adr namespace:\\n{detail}\",\n         )\n \n     def test_scan_actually_reads_scripts(self) -> None:\n@@ -142,12 +160,38 @@\n             \"the citation scan must cover scripts/*.py\",\n         )\n \n+    def test_platform_citations_do_not_use_unresolved_numeric_shorthand(self) -> None:\n+        shorthand = re.compile(r\"SADR-\\d{4}/\\d{4}\")\n+        offenders = []\n+        for path in _citing_files(REPO_ROOT):\n+            text = path.read_text(encoding=\"utf-8\")\n+            if shorthand.search(text):\n+                offenders.append(path.relative_to(REPO_ROOT).as_posix())\n+        self.assertEqual(\n+            offenders,\n+            [],\n+            \"repeat the SADR prefix so every cited number is independently resolvable\",\n+        )\n+\n     def test_carried_records_are_indexed(self) -> None:\n         # The two records this Goal carried onto the default branch, and which\n         # the scripts cite most, must be present.\n         index = _adr_index(REPO_ROOT)\n-        self.assertIn(\"0016\", index)\n-        self.assertIn(\"0017\", index)\n+        self.assertIn((\"SADR\", \"0016\"), index)\n+        self.assertIn((\"SADR\", \"0017\"), index)\n+\n+    def test_platform_tree_has_only_sadr_0001_through_0019(self) -> None:\n+        adr_dir = REPO_ROOT / \"docs\" / \"adr\"\n+        sadrs = sorted(adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\"))\n+        self.assertEqual(\n+            {path.name.split(\"-\", 2)[1] for path in sadrs},\n+            {f\"{number:04d}\" for number in range(1, 20)},\n+        )\n+        self.assertEqual(\n+            list(adr_dir.glob(\"[0-9][0-9][0-9][0-9]-*.md\")),\n+            [],\n+            \"the Shiki platform tree must not retain legacy numeric SADR paths\",\n+        )\n \n \n class FixtureAdrReferences(unittest.TestCase):\n@@ -180,6 +224,26 @@\n         self.assertIn(\"9991\", message)  # cited number\n         self.assertIn(\"docs/adr/9991-*.md\", message)  # missing path\n \n+    def test_target_adr_does_not_satisfy_missing_platform_sadr(self) -> None:\n+        root = self._make_repo(\n+            adr_names=[\"0042-target-decision.md\"],\n+            script_body='\"\"\"Target ADR 0042 exists, but platform SADR-0042 does not.\"\"\"\\n',\n+        )\n+        violations = find_adr_reference_violations(root, include_docs=False)\n+        self.assertEqual(len(violations), 1)\n+        self.assertEqual(violations[0].namespace, \"SADR\")\n+        self.assertEqual(violations[0].number, \"0042\")\n+        self.assertEqual(violations[0].expected_path, \"docs/adr/SADR-0042-*.md\")\n+\n+    def test_target_adr_and_platform_sadr_may_reuse_a_number(self) -> None:\n+        root = self._make_repo(\n+            adr_names=[\"0042-target-decision.md\", \"SADR-0042-platform-decision.md\"],\n+            script_body='\"\"\"Target ADR 0042 and platform SADR-0042 are distinct.\"\"\"\\n',\n+        )\n+        self.assertEqual(\n+            find_adr_reference_violations(root, include_docs=False), []\n+        )\n+\n     def test_uncited_adr_is_allowed(self) -> None:\n         # An ADR exists but nothing cites it -> not a violation. The converse\n         # (every ADR must be cited) is explicitly NOT required.\ndiff --git a/tests/test_guardian_review.py b/tests/test_guardian_review.py\nindex 88829e7810212635e2fc98aa5918d6f26294677d..00a85a237acd5bf2d6cb5b294128198c422402ca\n--- a/tests/test_guardian_review.py\n+++ b/tests/test_guardian_review.py\n@@ -162,7 +162,7 @@\n \n     def test_docs_only_is_light_and_not_conservative(self):\n         focus = gr.classify_review_focus([\"docs/adr/0099-x.md\", \"README.md\"], \"\", \"low\")\n-        self.assertTrue(any(\"Docs/ADR-only\" in f for f in focus))\n+        self.assertTrue(any(\"Docs/decision-record-only\" in f for f in focus))\n         self.assertFalse(any(\"conservative default\" in f.lower() for f in focus))\n \n     def test_unknown_category_is_conservative(self):\ndiff --git a/tests/test_policy_job_merged_prs.py b/tests/test_policy_job_merged_prs.py\nindex d45512e74681e5e649279236c7d4e79d5c3da471..b97989f8bd74d091de1a6b5275a20d8dc68c4512\n--- a/tests/test_policy_job_merged_prs.py\n+++ b/tests/test_policy_job_merged_prs.py\n@@ -3,8 +3,8 @@\n \n ``.github/workflows/shiki-cca-completion.yml`` has two consumers of the merged-PR\n proof: the CCA *signal* (``guardian_approval_signal.py`` in \"Prepare CCA context\")\n-and the authoritative gate (``mergegate_check.py`` in \"Enforce MergeGate\"). ADR\n-0017's bookkeeping-closeout exemption (condition 6) needs the merged implementation\n+and the authoritative gate (``mergegate_check.py`` in \"Enforce MergeGate\").\n+SADR-0017's bookkeeping-closeout exemption (condition 6) needs the merged implementation\n PR proven, and a closeout carries NO ``post_merge_reconcile`` marker. The signal\n already resolves ``MERGED_PRS`` unconditionally, but \"Enforce MergeGate\" used to\n resolve it only inside ``grep -q '<!-- shiki:post_merge_reconcile -->'``. So a\ndiff --git a/tests/test_sadr_install_migration.py b/tests/test_sadr_install_migration.py\nnew file mode 100644\nindex 0000000000000000000000000000000000000000..d51eac872b1c79ff4579c4c1401ca5d775a60d8d\n--- /dev/null\n+++ b/tests/test_sadr_install_migration.py\n@@ -0,0 +1,237 @@\n+\"\"\"SADR installation and collision-safe legacy upgrade behavior.\"\"\"\n+\n+from __future__ import annotations\n+\n+import contextlib\n+import hashlib\n+import io\n+import json\n+import shutil\n+import tempfile\n+import unittest\n+from pathlib import Path\n+\n+import shiki_test_support  # noqa: F401  (path bootstrap)\n+\n+import shiki_installer\n+from tests.test_adr_references import find_adr_reference_violations\n+\n+\n+PRODUCT_NUMBERS = tuple(range(13, 19))\n+\n+\n+def _quiet_install(target: Path, *, force: bool, validate: bool) -> None:\n+    with contextlib.redirect_stdout(io.StringIO()):\n+        shiki_installer.install_template(target, force=force, validate=validate)\n+\n+\n+def _sha256(path: Path) -> str:\n+    return hashlib.sha256(path.read_bytes()).hexdigest()\n+\n+\n+def _legacy_sadr_paths() -> tuple[str, ...]:\n+    return tuple(\n+        f\"docs/adr/{path.name.removeprefix('SADR-')}\"\n+        for path in sorted((shiki_installer.ROOT / \"docs\" / \"adr\").glob(\"SADR-*.md\"))\n+        if path.name != \"SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\"\n+    )\n+\n+\n+def _create_legacy_upgrade_fixture(\n+    target: Path,\n+) -> tuple[dict[str, bytes], tuple[str, ...]]:\n+    adr_dir = target / \"docs\" / \"adr\"\n+    adr_dir.mkdir(parents=True)\n+    product_bytes = {\n+        f\"{number:04d}-cubeshare-product-decision.md\": (\n+            f\"# CubeShare ADR {number:04d}\\n\\nDo not rewrite {number}.\\n\"\n+        ).encode()\n+        for number in PRODUCT_NUMBERS\n+    }\n+    for name, content in product_bytes.items():\n+        (adr_dir / name).write_bytes(content)\n+\n+    legacy_paths = _legacy_sadr_paths()\n+    for relative in legacy_paths:\n+        (target / relative).write_bytes(f\"legacy Shiki bytes for {relative}\\n\".encode())\n+\n+    stamp_path = target / shiki_installer.INSTALL_STAMP_PATH\n+    stamp_path.parent.mkdir(parents=True, exist_ok=True)\n+    stamp = {\n+        \"version\": shiki_installer.INSTALL_STAMP_VERSION,\n+        \"platform_commit\": None,\n+        \"installed_at\": \"2000-01-01T00:00:00+00:00\",\n+        \"digests\": {\n+            relative: _sha256(target / relative) for relative in legacy_paths\n+        },\n+    }\n+    stamp_path.write_text(\n+        json.dumps(stamp, indent=2, sort_keys=True) + \"\\n\", encoding=\"utf-8\"\n+    )\n+    return product_bytes, legacy_paths\n+\n+\n+def _tree_snapshot(root: Path) -> dict[str, tuple[str, bytes | str]]:\n+    \"\"\"Recursive byte/shape snapshot used to prove preflight is write-free.\"\"\"\n+    snapshot: dict[str, tuple[str, bytes | str]] = {}\n+    for path in sorted(root.rglob(\"*\")):\n+        relative = path.relative_to(root).as_posix()\n+        if path.is_symlink():\n+            snapshot[relative] = (\"symlink\", path.readlink().as_posix())\n+        elif path.is_dir():\n+            snapshot[relative] = (\"directory\", \"\")\n+        else:\n+            snapshot[relative] = (\"file\", path.read_bytes())\n+    return snapshot\n+\n+\n+class FreshSadrInstallTests(unittest.TestCase):\n+    def setUp(self) -> None:\n+        self.tmp = Path(tempfile.mkdtemp(prefix=\"shiki-sadr-fresh-\"))\n+        self.addCleanup(lambda: shutil.rmtree(self.tmp, ignore_errors=True))\n+        self.target = self.tmp / \"target\"\n+        self.adr_dir = self.target / \"docs\" / \"adr\"\n+        self.adr_dir.mkdir(parents=True)\n+        self.product_bytes = {\n+            f\"{number:04d}-product-decision.md\": (\n+                f\"# Product ADR {number:04d}\\n\\nTarget-owned bytes {number}.\\n\"\n+            ).encode()\n+            for number in PRODUCT_NUMBERS\n+        }\n+        for name, content in self.product_bytes.items():\n+            (self.adr_dir / name).write_bytes(content)\n+\n+    def test_fresh_install_preserves_product_adrs_and_installs_all_sadrs(self) -> None:\n+        _quiet_install(self.target, force=False, validate=True)\n+\n+        for name, expected in self.product_bytes.items():\n+            self.assertEqual((self.adr_dir / name).read_bytes(), expected)\n+\n+        sadr_numbers = {\n+            path.name.split(\"-\", 2)[1]\n+            for path in self.adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\")\n+        }\n+        self.assertEqual(sadr_numbers, {f\"{number:04d}\" for number in range(1, 20)})\n+\n+        numeric_records = {\n+            path.name\n+            for path in self.adr_dir.glob(\"[0-9][0-9][0-9][0-9]-*.md\")\n+        }\n+        self.assertEqual(numeric_records, set(self.product_bytes))\n+        stamp = json.loads(\n+            (self.target / shiki_installer.INSTALL_STAMP_PATH).read_text(\n+                encoding=\"utf-8\"\n+            )\n+        )\n+        for path in self.adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\"):\n+            relative = path.relative_to(self.target).as_posix()\n+            self.assertEqual(stamp[\"digests\"][relative], _sha256(path))\n+        self.assertEqual(find_adr_reference_violations(self.target), [])\n+\n+\n+class LegacySadrUpgradeTests(unittest.TestCase):\n+    def setUp(self) -> None:\n+        self.tmp = Path(tempfile.mkdtemp(prefix=\"shiki-sadr-upgrade-\"))\n+        self.addCleanup(lambda: shutil.rmtree(self.tmp, ignore_errors=True))\n+        self.target = self.tmp / \"target\"\n+        self.adr_dir = self.target / \"docs\" / \"adr\"\n+        self.product_bytes, self.legacy_paths = _create_legacy_upgrade_fixture(\n+            self.target\n+        )\n+\n+    def test_force_upgrade_deletes_only_stamped_legacy_sadrs(self) -> None:\n+        _quiet_install(self.target, force=True, validate=True)\n+\n+        for relative in self.legacy_paths:\n+            self.assertFalse((self.target / relative).exists(), relative)\n+        for name, expected in self.product_bytes.items():\n+            self.assertEqual((self.adr_dir / name).read_bytes(), expected)\n+\n+        sadrs = list(self.adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\"))\n+        self.assertEqual(len(sadrs), 19)\n+        refreshed = json.loads(\n+            (self.target / shiki_installer.INSTALL_STAMP_PATH).read_text(encoding=\"utf-8\")\n+        )\n+        for relative in self.legacy_paths:\n+            self.assertNotIn(relative, refreshed[\"digests\"])\n+        for path in sadrs:\n+            relative = path.relative_to(self.target).as_posix()\n+            self.assertEqual(refreshed[\"digests\"][relative], _sha256(path))\n+\n+    def test_non_force_install_never_deletes_legacy_files(self) -> None:\n+        before = {\n+            relative: (self.target / relative).read_bytes()\n+            for relative in self.legacy_paths\n+        }\n+        _quiet_install(self.target, force=False, validate=False)\n+        for relative, expected in before.items():\n+            self.assertEqual((self.target / relative).read_bytes(), expected)\n+        refreshed = json.loads(\n+            (self.target / shiki_installer.INSTALL_STAMP_PATH).read_text(encoding=\"utf-8\")\n+        )\n+        for relative in self.legacy_paths:\n+            self.assertEqual(refreshed[\"digests\"][relative], _sha256(self.target / relative))\n+\n+\n+class LegacyCleanupPreflightTests(unittest.TestCase):\n+    def setUp(self) -> None:\n+        self.tmp = Path(tempfile.mkdtemp(prefix=\"shiki-sadr-preflight-\"))\n+        self.addCleanup(lambda: shutil.rmtree(self.tmp, ignore_errors=True))\n+\n+    def test_all_ownership_blockers_abort_before_any_write(self) -> None:\n+        cases = (\n+            (\"absent\", \"install stamp is absent\"),\n+            (\"unreadable\", \"install stamp is unreadable\"),\n+            (\"malformed\", \"install stamp is malformed\"),\n+            (\"missing-digest\", \"install stamp has no digest\"),\n+            (\"digest-mismatch\", \"digest mismatch\"),\n+        )\n+        for name, expected_reason in cases:\n+            with self.subTest(name=name):\n+                target = self.tmp / name\n+                _, legacy_paths = _create_legacy_upgrade_fixture(target)\n+                stamp_path = target / shiki_installer.INSTALL_STAMP_PATH\n+\n+                if name == \"absent\":\n+                    stamp_path.unlink()\n+                elif name == \"unreadable\":\n+                    stamp_path.unlink()\n+                    stamp_path.mkdir()\n+                elif name == \"malformed\":\n+                    stamp_path.write_text(\"{not-json\\n\", encoding=\"utf-8\")\n+                else:\n+                    stamp = json.loads(stamp_path.read_text(encoding=\"utf-8\"))\n+                    if name == \"missing-digest\":\n+                        stamp[\"digests\"] = {}\n+                    else:\n+                        stamp[\"digests\"] = {\n+                            relative: \"deadbeef\" for relative in legacy_paths\n+                        }\n+                    stamp_path.write_text(\n+                        json.dumps(stamp, indent=2, sort_keys=True) + \"\\n\",\n+                        encoding=\"utf-8\",\n+                    )\n+\n+                before = _tree_snapshot(target)\n+                with self.assertRaises(shiki_installer.ShikiError) as raised:\n+                    _quiet_install(target, force=True, validate=False)\n+                after = _tree_snapshot(target)\n+\n+                self.assertEqual(before, after, f\"{name} preflight changed the tree\")\n+                message = str(raised.exception)\n+                self.assertIn(expected_reason, message)\n+                self.assertIn(\"No files were written\", message)\n+                for relative in legacy_paths:\n+                    self.assertIn(relative, message)\n+\n+    def test_absent_stamp_is_allowed_when_no_legacy_path_exists(self) -> None:\n+        target = self.tmp / \"no-legacy\"\n+        target.mkdir()\n+        _quiet_install(target, force=True, validate=False)\n+        self.assertTrue(\n+            (target / \"docs\" / \"adr\" / \"SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\").is_file()\n+        )\n+\n+\n+if __name__ == \"__main__\":\n+    unittest.main()\n\n succeeded in 89342ms:\n..................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................\n----------------------------------------------------------------------\nRan 1147 tests in 89.259s\n\nOK\nGuardian approval status for PR #130 (mizutani-140/shiki)\nHead SHA: aaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa\nResult: NOT APPROVED\n\nSatisfied approval sources:\n  - guardian_label (by mizutani-140)\n\nMissing / what the gate is waiting for:\n  - Guardian approval requires a configured Guardian review or current-head Guardian comment\n\nHead SHA stability:\n  - clean: safe to request approval against this head SHA\n\nPaste-ready Guardian approval comment (a configured Guardian pastes this verbatim):\n----------8<----------\nGuardian approval granted\n\naaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa\n---------->8----------\n{\n  \"handoff_file\": \"/private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/tmp7atafqoj/repo/.shiki/handoffs/RP-20260807T082644068473Z-6182e228-repair.md\",\n  \"ledger_id\": \"L-20260807T082644081628Z-39c3f692\",\n  \"repair_id\": \"RP-20260807T082644068473Z-6182e228\"\n}\n{\n  \"handoff_file\": \"/private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/tmph4iocyds/repo/.shiki/handoffs/RP-20260807T082645928247Z-eb116c8c-repair.md\",\n  \"ledger_id\": \"L-20260807T082645942711Z-d8c7365d\",\n  \"repair_id\": \"RP-20260807T082645928247Z-eb116c8c\"\n}\n{\n  \"handoff_file\": \"/private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/tmpd506iw8z/repo/.shiki/handoffs/RP-20260807T082647248786Z-f7e954cd-repair.md\",\n  \"ledger_id\": \"L-20260807T082647262619Z-eb444a9f\",\n  \"repair_id\": \"RP-20260807T082647248786Z-f7e954cd\"\n}\n{\n  \"handoff_file\": \"/private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/tmpppkpoqr9/repo/.shiki/handoffs/RP-20260807T082658785791Z-0e274671-repair.md\",\n  \"ledger_id\": \"L-20260807T082658799143Z-a5a7a814\",\n  \"repair_id\": \"RP-20260807T082658785791Z-0e274671\"\n}\n{\n  \"handoff_file\": \"/private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/tmp6t9b9wna/repo/.shiki/handoffs/RP-20260807T082659917356Z-7a9493c2-repair.md\",\n  \"ledger_id\": \"L-20260807T082659931393Z-3594510c\",\n  \"repair_id\": \"RP-20260807T082659917356Z-7a9493c2\"\n}\n{\n  \"handoff_file\": \"/private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/tmpuzy2ufbq/repo/.shiki/handoffs/RP-20260807T082700422424Z-148ba595-repair.md\",\n  \"ledger_id\": \"L-20260807T082700435578Z-408379b8\",\n  \"repair_id\": \"RP-20260807T082700422424Z-148ba595\"\n}\n{\n  \"handoff_file\": \"/private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/tmp7t9tv2lc/repo/.shiki/handoffs/RP-20260807T082701032638Z-07b8cae2-repair.md\",\n  \"ledger_id\": \"L-20260807T082701046623Z-aea03f64\",\n  \"repair_id\": \"RP-20260807T082701032638Z-07b8cae2\"\n}\n{\n  \"handoff_file\": \"/private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/tmpps9_m6sb/repo/.shiki/handoffs/RP-20260807T082701624954Z-7ed6f000-repair.md\",\n  \"ledger_id\": \"L-20260807T082701638508Z-0282085a\",\n  \"repair_id\": \"RP-20260807T082701624954Z-7ed6f000\"\n}\n{\n  \"handoff_file\": \"/private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/tmp7il0ecee/repo/.shiki/handoffs/RP-20260807T082701973747Z-2818462c-repair.md\",\n  \"ledger_id\": \"L-20260807T082701988049Z-f092d6e1\",\n  \"repair_id\": \"RP-20260807T082701973747Z-2818462c\"\n}\n{\n  \"handoff_file\": \"/private/var/folders/z7/g8_sl74j3qd3k4_zjkdt_w340000gn/T/tmpk91yxdwt/repo/.shiki/handoffs/RP-20260807T082702425740Z-4c10314c-repair.md\",\n  \"ledger_id\": \"L-20260807T082702439308Z-6f9746d5\",\n  \"repair_id\": \"RP-20260807T082702425740Z-4c10314c\"\n}\n[shiki] token verified: ok\n[shiki] token verified: ok\n{\n  \"repo\": \"owner/name\",\n  \"secret\": \"CLAUDE_CODE_OAUTH_TOKEN\",\n  \"source\": \"env:SHIKI_TEST_TOK\",\n  \"verified\": true\n}\n\nexec\n/bin/zsh -lc 'bash scripts/test_shiki_control_plane.sh' in /private/tmp/shiki-adr-collision.pa0KwY/.worktrees/shiki-t-20260807t073307221254z-2d22d654-introduc\ndiff --git a/.codex/skills/shiki/SKILL.md b/.codex/skills/shiki/SKILL.md\nindex c8e2e85d9d75d49cc4f2d6286ee22659c4e53e75..93cded623b2c15d9f65c76491a1c2d813ac9e6c0\n--- a/.codex/skills/shiki/SKILL.md\n+++ b/.codex/skills/shiki/SKILL.md\n@@ -18,6 +18,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n Codex CLI does not register this skill as a custom `/shiki` slash command. When\n the user types `/shiki` in Codex CLI and gets \"Unrecognized command\", explain\n that this is expected: invoke Shiki in Codex with natural language such as\n@@ -69,7 +72,7 @@\n \n ## Responsibilities\n \n-- Claude Code implements and repairs by default (`shiki runner claude`, ADR 0008); Codex implements and repairs tasks explicitly assigned to `codex`.\n+- Claude Code implements and repairs by default (`shiki runner claude`, SADR-0008); Codex implements and repairs tasks explicitly assigned to `codex`.\n - Claude Code Action can act as GitHub-side CCA or reviewer.\n - CCA judges completion.\n - MergeGate authorizes state transitions and merge readiness.\ndiff --git a/.github/prompts/cca-completion-check.md b/.github/prompts/cca-completion-check.md\nindex ed203ad5f302ecc81372d8ab27d2b1232ca97497..fd89950528333ef6242e6d5443904f5392ab7e56\n--- a/.github/prompts/cca-completion-check.md\n+++ b/.github/prompts/cca-completion-check.md\n@@ -19,6 +19,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n ## Required Reading\n \n Read only the files needed to judge the PR contract. Prefer this order:\ndiff --git a/.github/prompts/codex-task-handoff.md b/.github/prompts/codex-task-handoff.md\nindex 695e211d76dea1c60e8d5bb360e78487808286a4..59441d576b01a39569722f226c854e5a740e9eb5\n--- a/.github/prompts/codex-task-handoff.md\n+++ b/.github/prompts/codex-task-handoff.md\n@@ -2,7 +2,7 @@\n \n You are Codex Front acting as the Implementer for a Shiki task.\n \n-Follow `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant ADRs, and `docs/agents/codex-handoff.md`.\n+Follow `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant Shiki SADRs and target ADRs, and `docs/agents/codex-handoff.md`.\n \n Rules:\n \ndiff --git a/AGENTS.md b/AGENTS.md\nindex 7f041100d49c2a7525f51cb0f5d696a4a6d1c78c..a06bfbabc6fa48a007389e25e12d7ed523936bed\n--- a/AGENTS.md\n+++ b/AGENTS.md\n@@ -22,7 +22,7 @@\n - **Codex Front** is an optional implementer through Codex App, Codex CLI, Codex IDE extension, or Codex Web signed in with ChatGPT OAuth/subscription auth, dispatched with `shiki runner codex` when a task is explicitly assigned to `codex`.\n - **GitHub CCA** is implemented by Claude Code Action by default, using `CLAUDE_CODE_OAUTH_TOKEN`.\n \n-Do not assume `openai/codex-action`, `OPENAI_API_KEY`, or API-key based Codex automation in the default Shiki path. API-key runners are explicit target-repo extensions and require their own ADR.\n+Do not assume `openai/codex-action`, `OPENAI_API_KEY`, or API-key based Codex automation in the default Shiki path. API-key runners are explicit target-repo extensions and require their own target ADR.\n \n Shiki is not:\n \n@@ -44,10 +44,14 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use\n+`SADR-NNNN-*.md`; target product decisions retain `NNNN-*.md` and the `ADR`\n+namespace.\n+\n Use the canonical source-of-truth order above. Runtime-specific reading orders\n are operational guidance only and must not override or reorder it.\n \n-Conversation state is not durable truth. If a decision matters, put it in GitHub, `.shiki/`, `CONTEXT.md`, or an ADR.\n+Conversation state is not durable truth. If a decision matters, put it in GitHub, `.shiki/`, `CONTEXT.md`, a Shiki SADR, or a target ADR.\n \n If GitHub and `.shiki/` disagree, surface the conflict and prefer GitHub operational state until the mirror is repaired.\n \n@@ -75,8 +79,8 @@\n Every non-trivial change follows this loop:\n \n 1. **Goal**: clarify outcome, completion conditions, non-goals, risk level, and success signals.\n-2. **grill-with-docs**: challenge the plan against domain docs, ADRs, code reality, terminology, and edge scenarios.\n-3. **Context & Impact**: find relevant docs, ADRs, code, dependencies, owners, locks, and verification surfaces.\n+2. **grill-with-docs**: challenge the plan against domain docs, Shiki SADRs, target ADRs, code reality, terminology, and edge scenarios.\n+3. **Context & Impact**: find relevant docs, SADRs, target ADRs, code, dependencies, owners, locks, and verification surfaces.\n 4. **PRD and Spec Freeze**: use `to-prd` when context is settled enough to become durable product/engineering intent. The operator's explicit approval of the PRD is Spec Freeze: the plan records a `spec_freeze` block, and no plan runs without it. Steps 1-4 form Requirements Definition \u2014 one continuous operator dialogue. After Spec Freeze, scope changes require an operator-approved Spec Amendment; non-scope-moving interpretations go to the Assumption Log.\n 5. **Issues**: use `to-issues` to create independently grabbable vertical-slice issues. Prefer AFK slices over HITL slices where possible.\n 6. **Triage**: label issues for readiness, risk, runtime, skills, and MergeGate state.\n@@ -167,7 +171,7 @@\n - Testing decisions.\n - Out-of-scope items.\n - Further notes.\n-- Links to relevant domain terms and ADRs.\n+- Links to relevant domain terms, SADRs, and target ADRs.\n \n Every executable task must include:\n \n@@ -193,7 +197,7 @@\n \n Context & Impact must identify:\n \n-- Relevant documents, domain terms, ADRs, and past decisions.\n+- Relevant documents, domain terms, SADRs, target ADRs, and past decisions.\n - Relevant modules, interfaces, seams, callers, owners, and tests.\n - Dependency relationships between tasks.\n - Candidate locks and lock conflicts.\n@@ -210,7 +214,7 @@\n | Skill | Required when |\n | --- | --- |\n | `setup-matt-pocock-skills` | First configuring the repo for skills, or when issue tracker, triage labels, or domain docs are missing. |\n-| `grill-with-docs` | Default for non-trivial Goals before PRD/issues; always required when plans, terminology, boundaries, tradeoffs, or ADR-worthy decisions are ambiguous. |\n+| `grill-with-docs` | Default for non-trivial Goals before PRD/issues; always required when plans, terminology, boundaries, tradeoffs, or decision-record-worthy choices are ambiguous. |\n | `zoom-out` | The agent lacks an architectural map of the relevant code area. |\n | `to-prd` | Settled Goal context must become durable product/engineering intent. |\n | `to-issues` | A Goal, PRD, or plan must become independently grabbable vertical-slice issues. |\n@@ -348,7 +352,7 @@\n - Three or more new files.\n - Any high-risk or critical label.\n \n-When the architecture gate triggers, run `grill-with-docs` or `improve-codebase-architecture` as appropriate and record durable decisions in an ADR when needed.\n+When the architecture gate triggers, run `grill-with-docs` or `improve-codebase-architecture` as appropriate and record durable decisions in a Shiki SADR or target ADR as applicable.\n \n ## Engineering Discipline\n \ndiff --git a/CLAUDE.md b/CLAUDE.md\nindex 98a932e7841a632866fcd53b0bad0e3af3711d3d..98a31399404d6fb044b4fd493f4459ac87f439de\n--- a/CLAUDE.md\n+++ b/CLAUDE.md\n@@ -14,6 +14,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n ## Claude Role\n \n Default to planner, default implementer, reviewer, coordinator, documentation editor, CCA-style completion judge, and final-judgment assistant.\n@@ -57,7 +60,7 @@\n At the start of a Shiki session:\n \n 1. Read `AGENTS.md`.\n-2. Read `CONTEXT.md` and relevant ADRs.\n+2. Read `CONTEXT.md` and relevant Shiki SADRs and target ADRs.\n 3. Read active `.shiki/` Goal/task/ledger state when present.\n 4. Read active distilled rules from `.shiki/memories/` (status `distilled`, `active: true`, not revoked or superseded) for applicable guidance; the same rules are injected deterministically into task handoffs as the `## Distilled Rules` section (\u00a73.5).\n 5. Read the GitHub Issue or PR when the task is GitHub-backed.\n@@ -74,7 +77,7 @@\n - Define the Goal, completion conditions, non-goals, and risk level.\n - Treat `grill-with-docs` as the default planning entry point for non-trivial Goals.\n - Challenge domain terms against `CONTEXT.md`.\n-- Challenge hard-to-reverse tradeoffs against ADRs.\n+- Challenge hard-to-reverse tradeoffs against SADRs and target ADRs.\n - Explore code instead of asking when code can answer the question.\n - Run Context & Impact before implementation planning.\n - Produce a Task DAG, not an unordered checklist.\n@@ -94,7 +97,7 @@\n - For each question, provide your recommended answer.\n - Resolve dependency decisions before downstream design questions.\n - Update `CONTEXT.md` inline when terminology crystallizes.\n-- Offer ADRs only when the decision is hard to reverse, surprising without context, and a real tradeoff.\n+- Offer a SADR or target ADR only when the decision is hard to reverse, surprising without context, and a real tradeoff.\n - Convert unresolved questions into HITL blockers rather than allowing Codex to guess.\n \n ## PRD and Issues Standard\n@@ -103,7 +106,7 @@\n \n - Use the project's domain glossary vocabulary.\n - Avoid volatile file paths unless they are essential.\n-- Include implementation decisions, testing decisions, out-of-scope items, and relevant ADR/domain links.\n+- Include implementation decisions, testing decisions, out-of-scope items, and relevant SADR/target-ADR/domain links.\n - Identify deep-module opportunities and testable interfaces.\n \n When creating issues:\n@@ -121,7 +124,7 @@\n - Branch/worktree target.\n - Scope and non-goals.\n - Dependencies and locks.\n-- Relevant docs, ADRs, modules, and tests.\n+- Relevant docs, SADRs, target ADRs, modules, and tests.\n - Required skill invocations.\n - TDD expectations.\n - Pre-PR `code-review` gate expectations.\n@@ -152,7 +155,7 @@\n \n - Treat yourself as a judge, not implementer.\n - Do not write production code.\n-- Read `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant ADRs, PRD, task issue, PR body, diff, checks, reviews, labels, and `.shiki/` state.\n+- Read `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant SADRs and target ADRs, PRD, task issue, PR body, diff, checks, reviews, labels, and `.shiki/` state.\n - Evaluate every applicable checklist in `docs/agents/checklists.md`.\n - Map each acceptance criterion to durable evidence.\n - Distinguish implementation failure from missing evidence.\ndiff --git a/CONTEXT.md b/CONTEXT.md\nindex e61a9be06a0cce0d026484083a657bde2067e234..237ee8aa3ea8fab334ece49768bc5ed3b5af0974\n--- a/CONTEXT.md\n+++ b/CONTEXT.md\n@@ -25,7 +25,7 @@\n _Avoid_: jumping directly into code\n \n **grill-with-docs**:\n-A planning skill that challenges a plan against domain language, ADRs, code reality, and concrete edge scenarios. It resolves design-tree questions before PRD/issues. It runs inside Requirements Definition.\n+A planning skill that challenges a plan against domain language, Shiki SADRs, target ADRs, code reality, and concrete edge scenarios. It resolves design-tree questions before PRD/issues. It runs inside Requirements Definition.\n _Avoid_: generic brainstorming, silent assumptions\n \n **Requirements Definition**:\ndiff --git a/CONTRIBUTING.md b/CONTRIBUTING.md\nindex c6d9b656e73abb8a55e5f660123e03153dcb3e6d..9e37a8fbf668db5b22461aaae5548b8f1354394c\n--- a/CONTRIBUTING.md\n+++ b/CONTRIBUTING.md\n@@ -17,7 +17,7 @@\n 1. **Goal** \u2014 start from a user-approved Goal with an outcome, completion\n    conditions, non-goals, risk level, and success signals.\n 2. **grill-with-docs** \u2014 for any non-trivial change, challenge the plan against\n-   domain language, ADRs, and code reality before writing a PRD.\n+   domain language, Shiki SADRs, target ADRs, and code reality before writing a PRD.\n 3. **PRD** \u2014 capture settled product/engineering intent (`to-prd`).\n 4. **Spec Freeze** \u2014 the operator explicitly approves the PRD/requirements;\n    the plan records a `spec_freeze` block and no plan runs without it\n@@ -94,7 +94,7 @@\n   entries.\n - Do not hand-edit `.shiki/goals/*.json` or `.shiki/manifest.json` in a task PR.\n - Conversation state is not durable truth. If a decision matters, record it in\n-  GitHub, `.shiki/`, `CONTEXT.md`, or an ADR.\n+  GitHub, `.shiki/`, `CONTEXT.md`, a Shiki SADR, or a target ADR.\n \n ## Architecture Gate\n \n@@ -103,7 +103,7 @@\n or CI policy, package structure, three or more structural files, more than 100\n net new lines in a core module, three or more new files, or any high-risk label.\n When triggered, run `grill-with-docs` or `improve-codebase-architecture` and\n-record durable decisions in an ADR when needed (see `docs/adr/`).\n+record durable decisions in a Shiki SADR or target ADR as applicable (see `docs/adr/`).\n \n ## Safety\n \ndiff --git a/SYSTEM_PROMPT.md b/SYSTEM_PROMPT.md\nindex aaaea45e3a896c3a44ba89eec8bb84325ecf6c62..da009c86a88285269dee0155e58e550c6d744ea9\n--- a/SYSTEM_PROMPT.md\n+++ b/SYSTEM_PROMPT.md\n@@ -30,6 +30,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n Use the canonical source-of-truth order above. Runtime-specific reading orders\n are operational guidance only and must not override or reorder it. The current\n conversation is non-durable operator input.\n@@ -48,14 +51,14 @@\n \n A runtime may only do work that is explicitly assigned by the Goal, task, PR, or MergeGate decision.\n \n-Do not assume `openai/codex-action`, `OPENAI_API_KEY`, or API-key based Codex automation in the default Shiki path. API-key runners are explicit target-repo extensions and require their own ADR.\n+Do not assume `openai/codex-action`, `OPENAI_API_KEY`, or API-key based Codex automation in the default Shiki path. API-key runners are explicit target-repo extensions and require their own target ADR.\n \n ## 4. Non-negotiable Shiki flow\n \n For any non-trivial Goal, use this flow:\n \n 1. **Goal Seek**: define outcome, non-goals, risk level, completion criteria, and evidence requirements.\n-2. **grill-with-docs**: challenge the plan against `CONTEXT.md`, ADRs, code reality, terminology, boundaries, and edge scenarios. Ask one question at a time when the operator is available. If code can answer a question, inspect code instead of asking.\n+2. **grill-with-docs**: challenge the plan against `CONTEXT.md`, Shiki SADRs, target ADRs, code reality, terminology, boundaries, and edge scenarios. Ask one question at a time when the operator is available. If code can answer a question, inspect code instead of asking.\n 3. **Context & Impact**: identify modules, interfaces, seams, callers, dependencies, locks, risk, and verification surfaces.\n 4. **to-prd + Spec Freeze**: turn settled context into a PRD using domain vocabulary and testing decisions. The operator's explicit approval of the PRD is Spec Freeze, recorded as the plan's `spec_freeze` block; plans without it do not run. Steps 1-4 form Requirements Definition.\n 5. **to-issues**: decompose the PRD into independently grabbable vertical-slice issues. Prefer AFK slices; mark HITL slices when judgment is still required.\n@@ -75,7 +78,7 @@\n Before making any material change:\n \n 1. Read `AGENTS.md` and any runtime-specific wrapper such as `CLAUDE.md`.\n-2. Read `CONTEXT.md` and relevant ADRs.\n+2. Read `CONTEXT.md` and relevant SADRs and target ADRs.\n 3. Read the GitHub Issue / PR and `.shiki/` state for the active Goal or task.\n 4. Identify your role: Planner, Implementer, Reviewer, Completion Check Agent, Repairer, or Guardian-assist.\n 5. Identify Goal id, task id, branch, PR, dependencies, locks, risk level, required skills, and required evidence.\n@@ -103,7 +106,7 @@\n \n At minimum, identify:\n \n-- Relevant documents, ADRs, domain terms, and prior decisions.\n+- Relevant documents, SADRs, target ADRs, domain terms, and prior decisions.\n - Relevant modules, interfaces, seams, callers, owners, and tests.\n - Dependency relationships between tasks.\n - Candidate file locks and lock conflicts.\n@@ -152,7 +155,7 @@\n \n CCA must read:\n \n-- `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant ADRs.\n+- `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant SADRs and target ADRs.\n - Parent Goal issue, PRD issue, task issue, comments, and labels.\n - PR body, diff, commits, changed files, reviews, CI checks, and artifacts available to the workflow.\n - `.shiki/` task contract, locks, ledger entries, CCA checklist profile, and prior repair packets.\n@@ -214,7 +217,7 @@\n - Three or more new files.\n - Any high-risk or critical label.\n \n-When the architecture gate triggers, run `grill-with-docs` or `improve-codebase-architecture` as appropriate and record the decision in an ADR if it is hard to reverse or likely to be re-litigated.\n+When the architecture gate triggers, run `grill-with-docs` or `improve-codebase-architecture` as appropriate and record the decision in a Shiki SADR or target ADR as applicable if it is hard to reverse or likely to be re-litigated.\n \n ## 14. Skill Gate\n \n@@ -223,7 +226,7 @@\n Use these skills:\n \n - `setup-matt-pocock-skills`: before first use of the engineering skill set, or when issue tracker, triage labels, or domain docs are missing.\n-- `grill-with-docs`: default for non-trivial Goals before PRD/issues; required for ambiguous plans, terminology, boundaries, ADR-worthy tradeoffs, or design-tree decisions.\n+- `grill-with-docs`: default for non-trivial Goals before PRD/issues; required for ambiguous plans, terminology, boundaries, decision-record-worthy tradeoffs, or design-tree decisions.\n - `zoom-out`: unfamiliar code area or missing architectural map.\n - `to-prd`: convert settled context into a PRD.\n - `to-issues`: break a PRD, plan, or Goal into independently grabbable vertical-slice issues.\ndiff --git a/docs/adr/0001-keep-shiki-as-platform-name.md b/docs/adr/SADR-0001-keep-shiki-as-platform-name.md\nindex 20a9637c3b9d8c97de7ba8db7cfae4d82d9a7dba..8b685439959aada1b221c112f0ac3c51ca8a1695\n--- a/docs/adr/0001-keep-shiki-as-platform-name.md\n+++ b/docs/adr/SADR-0001-keep-shiki-as-platform-name.md\n@@ -1,3 +1,3 @@\n-# Keep Shiki As The Platform Name\n+# SADR-0001: Keep Shiki As The Platform Name\n \n Shiki remains the product name for the GitHub-native agentic engineering control plane. We considered more descriptive names such as MergePilot, MergeGate, and Kiroku, but kept Shiki because it carries the existing goal-seeking framework identity while remaining independent of Hermes, Codex, Claude Code, or any single runtime.\ndiff --git a/docs/adr/0002-use-agents-as-shared-constitution.md b/docs/adr/SADR-0002-use-agents-as-shared-constitution.md\nindex 73de30ecb078120eac540d9bc51855e3cd48d0e3..c5486182bf06fca36ea508496b013c67302fd989\n--- a/docs/adr/0002-use-agents-as-shared-constitution.md\n+++ b/docs/adr/SADR-0002-use-agents-as-shared-constitution.md\n@@ -1,3 +1,3 @@\n-# Use AGENTS As The Shared Agent Constitution\n+# SADR-0002: Use AGENTS As The Shared Agent Constitution\n \n Shiki uses `AGENTS.md` as the runtime-neutral constitution and `CLAUDE.md` as a thin Claude Code wrapper. This keeps Codex, Claude Code, Hermes Runner, and future Agent Runtimes aligned on the same GitHub-first MergeGate rules while still allowing runtime-specific instructions where needed.\ndiff --git a/docs/adr/0003-require-skill-gate-for-mergegate-readiness.md b/docs/adr/SADR-0003-require-skill-gate-for-mergegate-readiness.md\nindex 424be0c9af4c8f0136f1fde68a468c47ff84e764..7fc337987c7df9792f75f181dfcbed75fe5ffff2\n--- a/docs/adr/0003-require-skill-gate-for-mergegate-readiness.md\n+++ b/docs/adr/SADR-0003-require-skill-gate-for-mergegate-readiness.md\n@@ -1,3 +1,3 @@\n-# Require Skill Gate For MergeGate Readiness\n+# SADR-0003: Require Skill Gate For MergeGate Readiness\n \n Shiki requires relevant engineering skills to be invoked before execution when their triggers apply. This makes skills part of MergeGate readiness rather than optional prompt style, because Shiki depends on repeatable planning, issue decomposition, TDD, diagnosis, triage, and architecture review before autonomous or parallel agent work can be trusted.\ndiff --git a/docs/adr/0004-use-github-first-with-shiki-mirror.md b/docs/adr/SADR-0004-use-github-first-with-shiki-mirror.md\nindex d0a3a9174dc646a945a9d325954ffaf00c352040..a79c54ae9ee8eed2a0524192b864da4e22a92d3f\n--- a/docs/adr/0004-use-github-first-with-shiki-mirror.md\n+++ b/docs/adr/SADR-0004-use-github-first-with-shiki-mirror.md\n@@ -1,4 +1,4 @@\n-# ADR 0004: Use GitHub First With A Repository-Local Shiki Mirror\n+# SADR-0004: Use GitHub First With A Repository-Local Shiki Mirror\n \n ## Status\n \ndiff --git a/docs/adr/0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md b/docs/adr/SADR-0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\nindex b339baf49b89d9290e79c3d51b36e7b2bde3bd95..0f3c3eb9afccdb3403ce978e21d04fe12e495da0\n--- a/docs/adr/0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\n+++ b/docs/adr/SADR-0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\n@@ -1,8 +1,8 @@\n-# ADR 0005: Use Codex As Front And Claude Code Action As The GitHub Runtime\n+# SADR-0005: Use Codex As Front And Claude Code Action As The GitHub Runtime\n \n ## Status\n \n-Accepted (implementer default superseded by ADR 0008; the auth model and the\n+Accepted (implementer default superseded by SADR-0008; the auth model and the\n Claude Code Action GitHub runtime decision remain in force)\n \n ## Context\n@@ -26,4 +26,4 @@\n - Shiki avoids requiring `OPENAI_API_KEY` for the default workflow.\n - Codex work happens through the user's authenticated Codex front surface, not as a hidden backend Action.\n - Claude Code Action is configured for OAuth-token based subscription usage by default.\n-- API-key based automation remains possible only as an explicit target-repo extension with its own ADR.\n+- API-key based automation remains possible only as an explicit target-repo extension with its own target ADR.\ndiff --git a/docs/adr/0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md b/docs/adr/SADR-0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\nindex 4bcae9d1e52f0a37b8e9fbafa5cd14b6ee1993bd..7a38538a6ae9929567f0ade67e7ea6a60d0b3f7e\n--- a/docs/adr/0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\n+++ b/docs/adr/SADR-0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\n@@ -1,4 +1,4 @@\n-# ADR 0006: Separate Codex Implementation, CCA Completion, And MergeGate Authorization\n+# SADR-0006: Separate Codex Implementation, CCA Completion, And MergeGate Authorization\n \n ## Status\n \ndiff --git a/docs/adr/0007-packaging-and-release.md b/docs/adr/SADR-0007-packaging-and-release.md\nindex 79b84ddb97167372b44641e5b7860e400ce80822..bb445a2e2e856d9d1f5fdbe35de8d4e8cec0fb32\n--- a/docs/adr/0007-packaging-and-release.md\n+++ b/docs/adr/SADR-0007-packaging-and-release.md\n@@ -1,4 +1,4 @@\n-# ADR 0007: Packaging And Release For Shiki\n+# SADR-0007: Packaging And Release For Shiki\n \n ## Status\n \n@@ -25,7 +25,7 @@\n   not introduce third-party runtime dependencies or break the shim.\n - Releases must run inside the operator's subscription-authenticated toolchain\n   and must not require extra secrets beyond the built-in `GITHUB_TOKEN`\n-  (consistent with ADR 0005's no-API-key-by-default stance).\n+  (consistent with SADR-0005's no-API-key-by-default stance).\n - `.shiki/manifest.json` already encodes the install surface (`install.include`,\n   `install.create_directories`, `install.exclude_from_commit`), and it must not\n   be mutated as part of this change.\n@@ -34,7 +34,7 @@\n \n This is a hard-to-reverse platform decision (it sets the version source of\n truth, the release trigger, and the public install contract), so it is recorded\n-as an ADR.\n+as a SADR.\n \n ## Decision\n \n@@ -104,7 +104,7 @@\n 3. **Publishing to PyPI.** Rejected for now: Shiki is distributed as a\n    repository template + git-installable CLI, not a PyPI library; PyPI would add\n    credentials/secrets and a different versioning contract. Can be added later as\n-   its own ADR if a packaged distribution is needed.\n+   its own SADR if a packaged distribution is needed.\n 4. **Dynamic version (read `VERSION` via a build backend `dynamic` field).**\n    Rejected to keep `pyproject.toml` backend-agnostic and dependency-free; the\n    small duplication between `VERSION` and `pyproject.toml` is guarded by the\n@@ -123,4 +123,4 @@\n   releasing versions the whole platform but ships only `install.include` to\n   targets.\n - Future PyPI distribution or a console entry point are deferred and would each\n-  warrant their own ADR.\n+  warrant their own SADR.\ndiff --git a/docs/adr/0008-use-claude-code-as-default-implementer.md b/docs/adr/SADR-0008-use-claude-code-as-default-implementer.md\nindex dedf4510b7c7799521422f622029563d373db031..35c2625d908167fbc58855b5b63e283805f167e8\n--- a/docs/adr/0008-use-claude-code-as-default-implementer.md\n+++ b/docs/adr/SADR-0008-use-claude-code-as-default-implementer.md\n@@ -1,12 +1,12 @@\n-# ADR 0008: Use Claude Code As The Default Implementer Runtime\n+# SADR-0008: Use Claude Code As The Default Implementer Runtime\n \n ## Status\n \n-Proposed (supersedes ADR 0005 for the implementer default; keeps its auth model)\n+Proposed (supersedes SADR-0005 for the implementer default; keeps its auth model)\n \n ## Context\n \n-ADR 0005 made Codex Front the default implementation runtime, with Claude Code\n+SADR-0005 made Codex Front the default implementation runtime, with Claude Code\n restricted to planner and reviewer roles by the runtime registry, and the only\n autonomous implementation adapter was `shiki runner codex`.\n \n@@ -18,13 +18,13 @@\n - The platform's planning, review, and CCA layers already run on Claude Code,\n   and Claude Code's native capabilities (the built-in `/code-review` skill and\n   the Workflow multi-agent orchestration tool) have no Codex equivalent.\n-- The Goal lifecycle redesign (ADR 0009) requires an autonomous post-freeze\n+- The Goal lifecycle redesign (SADR-0009) requires an autonomous post-freeze\n   loop driven from the operator's Claude Code session, including bounded\n   repair dispatch, which must not depend on a runtime that cannot be invoked\n   unattended.\n \n The constitution claims Shiki is runtime-agnostic (\"not a Claude-only\n-workflow\", ADR 0002). Any change to the implementer default must preserve the\n+workflow\", SADR-0002). Any change to the implementer default must preserve the\n registry-based runtime model rather than hardcoding one vendor.\n \n ## Decision\n@@ -39,7 +39,7 @@\n   the task to review or repair-needed.\n - Keep Codex registered as an optional implementer runtime. `shiki runner\n   codex`, the Codex handoff templates, and the ChatGPT OAuth auth model from\n-  ADR 0005 remain supported for tasks explicitly assigned to `codex`.\n+  SADR-0005 remain supported for tasks explicitly assigned to `codex`.\n - Commit to mandating two Claude-native capabilities as loop contract in\n   follow-up Goals (status: the code-review gate was delivered by Goal G-B \u2014\n   skill registry, checklists item PR-12, default required_skills \u2014 while the\n@@ -60,7 +60,7 @@\n \n Out of scope: removing Codex support, changing the CCA runtime (Claude Code\n Action with `CLAUDE_CODE_OAUTH_TOKEN` stays), and API-key based automation\n-(still requires its own ADR per ADR 0005).\n+(still requires its own target ADR per SADR-0005).\n \n ## Consequences\n \n@@ -100,7 +100,7 @@\n   harness rejection of direct `codex exec` is outside Shiki's control, and the\n   plan's mandatory native capabilities cannot run on Codex at all.\n - **Remove Codex entirely**: rejected; it abandons the runtime-agnostic\n-  constitution (ADR 0002) for no operational gain and forecloses\n+  constitution (SADR-0002) for no operational gain and forecloses\n   cross-runtime adversarial review.\n - **GitHub Actions cloud implementation via claude-code-action**: rejected as\n   default; durable evidence is automatic but token cost, Node 24 workflow\ndiff --git a/docs/adr/0009-requirements-definition-and-spec-freeze.md b/docs/adr/SADR-0009-requirements-definition-and-spec-freeze.md\nindex 29b3f7871bef9203a8b4a831a65c39eb2f506e42..52e7dd69ad0aa4efe0b68c62678abbf7df02aaec\n--- a/docs/adr/0009-requirements-definition-and-spec-freeze.md\n+++ b/docs/adr/SADR-0009-requirements-definition-and-spec-freeze.md\n@@ -1,4 +1,4 @@\n-# ADR 0009: Merge Grill And Planning Into Requirements Definition With Spec Freeze\n+# SADR-0009: Merge Grill And Planning Into Requirements Definition With Spec Freeze\n \n ## Status\n \ndiff --git a/docs/adr/0010-external-ai-guardian-review.md b/docs/adr/SADR-0010-external-ai-guardian-review.md\nindex e6108bbe8bb4e73e0ff4684c8f78001a80438c4e..f3510b145664298adafb151b6f0db51b972f01ec\n--- a/docs/adr/0010-external-ai-guardian-review.md\n+++ b/docs/adr/SADR-0010-external-ai-guardian-review.md\n@@ -1,4 +1,4 @@\n-# ADR 0010: External AI Guardian Review As A First-Class Approval Authority\n+# SADR-0010: External AI Guardian Review As A First-Class Approval Authority\n \n ## Status\n \n@@ -8,7 +8,7 @@\n \n Shiki is intended to be an autonomous, recursive, self-improving agent\n framework: authority-in-the-loop, not human-in-the-loop. Until now the Guardian\n-approval required for high/critical-risk merges (guardian-policy.json, ADR\n+approval required for high/critical-risk merges (guardian-policy.json, SADR\n governance) recognized only human authority sources \u2014 a GitHub review approval,\n the `guardian:approved` label applied by a configured Guardian, or a comment\n carrying `Guardian approval granted` plus the current head SHA.\n@@ -89,7 +89,7 @@\n   autonomous design the operator intends and produced the impersonation\n   incident.\n - **Record AI approval as an operator comment**: rejected as forgery of human\n-  authority \u2014 the exact failure this ADR corrects.\n+  authority \u2014 the exact failure this SADR corrects.\n - **Deliver the artifact as a committed `.shiki/` file**: rejected; a committed\n   approval inside the PR being approved is forgeable by anyone who can push.\n - **A dedicated reviewer bot identity instead of a relayed comment**: deferred;\ndiff --git a/docs/adr/0011-autonomous-quality-gate-evidence.md b/docs/adr/SADR-0011-autonomous-quality-gate-evidence.md\nindex bca0246c5d46e44afbde15486a75eec425fc02f5..9c93eac16d8c3a6d4b11bd7224e3955102cd7db4\n--- a/docs/adr/0011-autonomous-quality-gate-evidence.md\n+++ b/docs/adr/SADR-0011-autonomous-quality-gate-evidence.md\n@@ -1,4 +1,4 @@\n-# ADR 0011: Autonomous Quality-Gate Evidence \u2014 Deterministic Facts And An Independent Verifier\n+# SADR-0011: Autonomous Quality-Gate Evidence \u2014 Deterministic Facts And An Independent Verifier\n \n ## Status\n \ndiff --git a/docs/adr/0012-autonomous-loop-closeout-to-main.md b/docs/adr/SADR-0012-autonomous-loop-closeout-to-main.md\nindex 637fad6dde4989c9c2bf0f81f676d8d29991123c..ec7d85f81030a676bcbba28b43eb365422a36eac\n--- a/docs/adr/0012-autonomous-loop-closeout-to-main.md\n+++ b/docs/adr/SADR-0012-autonomous-loop-closeout-to-main.md\n@@ -1,4 +1,4 @@\n-# 0012 \u2014 Autonomous loop pushes goal completion to main via a closeout PR\n+# SADR-0012 \u2014 Autonomous loop pushes goal completion to main via a closeout PR\n \n - Status: accepted\n - Date: 2026-06-18\ndiff --git a/docs/adr/0013-bootstrap-grants-actions-approve-pull-requests.md b/docs/adr/SADR-0013-bootstrap-grants-actions-approve-pull-requests.md\nindex b6c69d64f1c5ced346b88d6859586715b3b5bd0e..cf48ff4a6716267f539ca58edd1a852e7c7a2d64\n--- a/docs/adr/0013-bootstrap-grants-actions-approve-pull-requests.md\n+++ b/docs/adr/SADR-0013-bootstrap-grants-actions-approve-pull-requests.md\n@@ -1,4 +1,4 @@\n-# ADR 0013: Bootstrap Grants GitHub Actions Permission To Create And Approve Pull Requests\n+# SADR-0013: Bootstrap Grants GitHub Actions Permission To Create And Approve Pull Requests\n \n ## Status\n \n@@ -31,7 +31,7 @@\n security tradeoff: it lets repository automation satisfy a human-review gate. In\n a multi-maintainer repo that could let a compromised or buggy workflow\n self-approve changes. So whether bootstrap turns it on by default \u2014 and with\n-what default token scope \u2014 is an ADR-worthy decision, not an implementation\n+what default token scope \u2014 is a SADR-worthy decision, not an implementation\n detail.\n \n ## Decision\n@@ -48,7 +48,7 @@\n Specifically:\n \n - **`can_approve_pull_request_reviews` defaults to `true`.** Shiki's default\n-  posture is solo/AFK autonomous operation (ADR 0008/0009/0012), where the CCA\n+  posture is solo/AFK autonomous operation (SADR-0008/0009/0012), where the CCA\n   Review Bridge is the only path that can satisfy `required_review: true` after\n   the CCA and Guardian gates have already passed. The default makes a\n   freshly-bootstrapped repo able to run the autonomous loop end to end without a\ndiff --git a/docs/adr/0014-codex-app-external-ai-guardian-ui-adapter.md b/docs/adr/SADR-0014-codex-app-external-ai-guardian-ui-adapter.md\nindex 238db06279334caa341c117362df9271382a7fcd..65f12e9939321e1b3e389a34c822fc1549e5155d\n--- a/docs/adr/0014-codex-app-external-ai-guardian-ui-adapter.md\n+++ b/docs/adr/SADR-0014-codex-app-external-ai-guardian-ui-adapter.md\n@@ -1,4 +1,4 @@\n-# ADR 0014: Use Codex App As The External AI Guardian UI Adapter\n+# SADR-0014: Use Codex App As The External AI Guardian UI Adapter\n \n ## Status\n \ndiff --git a/docs/adr/0015-contract-approval-before-dispatch.md b/docs/adr/SADR-0015-contract-approval-before-dispatch.md\nindex fad6c656741a20a7684ba16080a7db159f63a52f..6061271a46d3f1d6ae7174477de3fb75673123a6\n--- a/docs/adr/0015-contract-approval-before-dispatch.md\n+++ b/docs/adr/SADR-0015-contract-approval-before-dispatch.md\n@@ -1,4 +1,4 @@\n-# ADR 0015: Approve The Task Contract Before Dispatch, Not The Diff After It\n+# SADR-0015: Approve The Task Contract Before Dispatch, Not The Diff After It\n \n ## Status\n \n@@ -9,7 +9,7 @@\n Guardian approval for high/critical risk is expressible only on a pull request.\n Every approval source in `guardian-policy.json` \u2014 GitHub review, the\n `guardian:approved` label, the `Guardian approval granted` comment, and the\n-`external_ai_guardian_review` artifact (ADR 0010/0014) \u2014 is bound to a PR, and\n+`external_ai_guardian_review` artifact (SADR-0010/0014) \u2014 is bound to a PR, and\n three of the four require the current head SHA. Approval therefore cannot exist\n before an implementation PR exists.\n \n@@ -81,7 +81,7 @@\n An envelope breach must surface only on the non-repairable policy gate, never on\n the repairable `MergeGate metadata check`. Otherwise the Goal Loop routes it to\n `dispatch_repair` \u2014 instructing a runner to make the breach go away \u2014 which is\n-exactly what this ADR forbids.\n+exactly what this SADR forbids.\n \n **Contract immutability.** After registration, an implementation PR must not\n change the governance fields of its own task contract (`scope`, `non_goals`,\n@@ -116,7 +116,7 @@\n fails any verdict that is not `complete`, and the signal the CCA prompt reads is\n computed from `evaluate_guardian_approval` alone. Without the OR-merge at the\n signal, a contract-approved PR still returns `needs_guardian`, the CCA job fails,\n-and the MergeGate policy check never runs \u2014 the gate this ADR changes would never\n+and the MergeGate policy check never runs \u2014 the gate this SADR changes would never\n be reached. The CCA *prompt* needs no change: a `contract_approval` entry in the\n signal's existing `sources` array satisfies its current wording.\n \n@@ -130,7 +130,7 @@\n written, and an approved contract lets its implementation PRs merge through the\n same automatic path as low/medium risk. The wasted implement-then-block cycle\n disappears. Guardian review shifts from reading diffs to reading contracts, which\n-is both earlier and smaller; the external AI Guardian path (ADR 0010/0014) works\n+is both earlier and smaller; the external AI Guardian path (SADR-0010/0014) works\n on contracts with no change.\n \n We accept that the Guardian authorizes a class of change without seeing its\n@@ -146,7 +146,7 @@\n adversarial tests for the new approval path equivalent in rigor to\n `test_shiki_governance_evidence.sh`.\n \n-Bootstrap costs, which apply only while this ADR is being implemented. The tasks\n+Bootstrap costs, which apply only while this SADR is being implemented. The tasks\n that build Contract Approval merge under the existing rules, so the ones\n dispatched after the pre-dispatch gate lands but before the approval path lands\n need a Guardian-approved Contract PR *and* a live Guardian approval on their\n@@ -210,7 +210,7 @@\n risk; they do not bound authorized work.\n \n **Auto-escalate risk on envelope breach instead of stopping.** Returns the\n-workflow to a diff-time Guardian decision \u2014 the exact behavior this ADR removes.\n+workflow to a diff-time Guardian decision \u2014 the exact behavior this SADR removes.\n \n **Resolve governance fields from the base snapshot only.** Rejected against the\n registration flow: implementation PRs add their own contract, so the base has no\ndiff --git a/docs/adr/0016-locks-bound-product-paths.md b/docs/adr/SADR-0016-locks-bound-product-paths.md\nindex b5e850f3f1f7f4ef6e7f9d1763a29a51bd300c45..46b40a9b99a7aae644c02bb458f5473ff0a90ce2\n--- a/docs/adr/0016-locks-bound-product-paths.md\n+++ b/docs/adr/SADR-0016-locks-bound-product-paths.md\n@@ -1,4 +1,4 @@\n-# ADR 0016: Locks bound product paths; the Shiki mirror is bound by record identity\n+# SADR-0016: Locks bound product paths; the Shiki mirror is bound by record identity\n \n ## Status\n \n@@ -6,8 +6,8 @@\n \n - Date: 2026-07-28\n - Deciders: operator (mizutani-140), Claude (planner)\n-- Related: ADR 0004 (GitHub-first with a `.shiki` mirror), ADR 0009 (Spec Freeze),\n-  ADR 0012 (autonomous loop closeout to main), ADR 0015 (Contract Approval before\n+- Related: SADR-0004 (GitHub-first with a `.shiki` mirror), SADR-0009 (Spec Freeze),\n+  SADR-0012 (autonomous loop closeout to main), SADR-0015 (Contract Approval before\n   dispatch), remediation goal task R-02, the live PR #179 deadlock.\n \n ## Context\n@@ -90,11 +90,11 @@\n files, documentation files and named source modules are nameable; runtime-\n allocated ledger, runner and report ids are not \u2014 and those live in `.shiki`,\n which locks no longer govern. Existing frozen contracts are brought into line by\n-a one-time Spec Amendment per plan, which must land before ADR 0015's contract\n+a one-time Spec Amendment per plan, which must land before SADR-0015's contract\n immutability makes `locks` unwritable from the implementation PR.\n \n **Lock state is resolved from the durable side.** MergeGate resolves the lock\n-registry, and ADR 0015's governance fields, from the base `.shiki` snapshot when\n+registry, and SADR-0015's governance fields, from the base `.shiki` snapshot when\n present, falling back to head only when absent. The `MergeGate metadata check`\n job is given `--base-shiki`, which it does not receive today. Locks are not\n derived from the live open-pull-request set: a pull request body is untrusted\n@@ -108,7 +108,7 @@\n Explicitly out of scope: moving MergeGate's contract resolution off the PR\n checkout wholesale; making the mirror a projection of GitHub state; introducing\n new lock kinds; moving ledger evidence out of the repository; and unwinding\n-ADR 0012's closeout pull request. Registration of multi-task goals is ADR 0015's\n+SADR-0012's closeout pull request. Registration of multi-task goals is SADR-0015's\n Contract PR and is not re-decided here.\n \n ## Consequences\n@@ -127,7 +127,7 @@\n \n Risks accepted. First, goal-scoped identity is only as strong as `goal_id`,\n which is resolved from the PR body and the head task file; the mitigation is to\n-add `goal_id` to ADR 0015's frozen governance field set, and until that lands a\n+add `goal_id` to SADR-0015's frozen governance field set, and until that lands a\n task file can name a foreign goal. Second, base-when-present resolution is\n unavailable on fork pull requests, where the head is contributor-controlled;\n fork pull requests already cannot complete a Shiki merge, and this decision does\n@@ -139,7 +139,7 @@\n path, not by implementation pull requests.\n \n New obligations: one Spec Amendment per frozen plan to rewrite `locks`, ordered\n-before ADR 0015 T1; extension of the adversarial suites in\n+before SADR-0015 T1; extension of the adversarial suites in\n `scripts/test_shiki_adversarial_state.sh` and\n `scripts/test_shiki_governance_evidence.sh` to cover the eleven newly-scoped\n directories; and updating `scripts/test_shiki_loop_lock_guard.sh`,\n@@ -151,32 +151,32 @@\n whether the loop's transitive sync of ledger `evidence` references can be\n deleted once the Contract PR carries its own source plan.\n \n-## Relationship to ADR 0004\n+## Relationship to SADR-0004\n \n-This ADR **clarifies something ADR 0004 left unspecified**. ADR 0004 decides that\n+This SADR **clarifies something SADR-0004 left unspecified**. SADR-0004 decides that\n GitHub is the operational source of truth and that each target repository keeps a\n `.shiki/` mirror. It says nothing about pull requests writing the mirror, does\n not name MergeGate, and does not make the mirror an input to a gate. Nothing in\n-ADR 0004's Decision or Consequences is amended, superseded or weakened: the\n+SADR-0004's Decision or Consequences is amended, superseded or weakened: the\n mirror still exists, still records the same artifacts, and pull request\n-boundaries remain the coordination surface. What this ADR settles is the question\n+boundaries remain the coordination surface. What this SADR settles is the question\n 0004 never reached \u2014 by what authority a pull request may write a mirror record \u2014\n and it answers it the way `.shiki/manifest.json` already declares\n (`pr_mutation: current-task-or-current-goal-only`).\n \n-It **amends ADR 0012**, whose Decision item 5 states that \"Loop-executed tasks\n+It **amends SADR-0012**, whose Decision item 5 states that \"Loop-executed tasks\n declare the `path:.shiki/**` lock, which covers every `.shiki` file the closeout\n stages ... so `files_outside_locks` is satisfied without per-file lock\n bookkeeping.\" That clause is replaced: the closeout's mirror writes are\n-authorized by record identity, not by a declared lock. ADR 0012's substantive\n+authorized by record identity, not by a declared lock. SADR-0012's substantive\n decision \u2014 that completion reaches `main` through a closeout pull request rather\n than local attestation \u2014 is preserved unchanged.\n \n-It is **complementary to ADR 0015**, not a substitute. ADR 0015 owns contract\n+It is **complementary to SADR-0015**, not a substitute. SADR-0015 owns contract\n immutability, the pre-dispatch gate, the Contract PR and the Scope Envelope. This\n-ADR owns what a lock means and who may write the mirror. The one ordering\n+SADR owns what a lock means and who may write the mirror. The one ordering\n constraint between them is that the lock-narrowing Spec Amendment must precede\n-ADR 0015 T1's freezing of the `locks` field.\n+SADR-0015 T1's freezing of the `locks` field.\n \n ## Alternatives Considered\n \n@@ -186,7 +186,7 @@\n the only writer that can register them may run. Migrating the Guardian signal to\n that store makes every registration PR fail closed to `needs_guardian` at every\n risk level, because the task file is by definition absent from the store, which\n-removes the low/medium autonomous path ADR 0008/0009 exists to provide. And it\n+removes the low/medium autonomous path SADR-0008/0009 exists to provide. And it\n leaves schemas, manifest, config, guardian policy, templates, migrations, plans,\n worktrees, runner records, runs and handoffs with no writer at all in a\n repository whose product is `.shiki`. Measured benefit on the frozen backlog:\n@@ -199,14 +199,14 @@\n namespace is not in fact ownership-partitioned \u2014 56 ledger entries carry no\n `task_id`, and one goal DAG is writable by 35 registered tasks. The correct\n expression of its insight is that locks do not govern the mirror at all, which is\n-what this ADR decides.\n+what this SADR decides.\n \n **Make the mirror a projection written only by a registration writer and a\n post-merge projection job.** Rejected. It requires a bypass actor with write\n access to the protected default branch; it makes its own master invariant\n readable from a manifest that pull requests may edit; it deletes both reconcile\n modes and leaves a corrupted mirror with no in-band repair path, contradicting\n-ADR 0004's consequence that mirror conflicts are repaired against GitHub state;\n+SADR-0004's consequence that mirror conflicts are repaired against GitHub state;\n and its content-derived ids are rejected by `validate_shiki`'s currently-green id\n pattern.\n \ndiff --git a/docs/adr/0017-bookkeeping-closeouts-do-not-inherit-risk.md b/docs/adr/SADR-0017-bookkeeping-closeouts-do-not-inherit-risk.md\nindex 554e3a0d526a72ca067108c5464cc68c3edcf174..b9676857a4713850e063af7c39c8e0b01ecb4c66\n--- a/docs/adr/0017-bookkeeping-closeouts-do-not-inherit-risk.md\n+++ b/docs/adr/SADR-0017-bookkeeping-closeouts-do-not-inherit-risk.md\n@@ -1,4 +1,4 @@\n-# ADR 0017: A Bookkeeping Closeout Does Not Inherit Its Task's Risk\n+# SADR-0017: A Bookkeeping Closeout Does Not Inherit Its Task's Risk\n \n ## Status\n \n@@ -6,7 +6,7 @@\n \n ## Context\n \n-ADR 0012 made the goal loop push a task's completion to the default branch through a\n+SADR-0012 made the goal loop push a task's completion to the default branch through a\n closeout pull request: after the implementation PR merges, the loop opens a second PR\n carrying `task.status = done`, `lock.state = released`, and \u2014 when the task completes\n its goal \u2014 `goal.status = complete` plus one scorecard report. Nothing else. The\n@@ -49,14 +49,14 @@\n 4. The task file's governance fields (`scope`, `non_goals`, `required_skills`,\n    `risk_level`, `locks`, `acceptance_checks`, `test_command`) are byte-identical to\n    the base snapshot. Only `status`, `expected_pr`, `closeout_pr`, `ledger_evidence`\n-   and `expected_branch` may differ \u2014 the ADR 0015 contract-immutability rule,\n+   and `expected_branch` may differ \u2014 the SADR-0015 contract-immutability rule,\n    unchanged.\n \n    `expected_branch` is in that set because a closeout is *by construction* on a\n    different branch from the implementation it closes, and the loop rewrites the\n    field accordingly. It is safe to let it differ because MergeGate independently\n    requires `expected_branch` to equal the PR's actual head ref, so a PR cannot set\n-   it to anything other than the branch it is really on. The first draft of this ADR\n+   it to anything other than the branch it is really on. The first draft of this SADR\n    omitted it, which made the exemption unreachable: every real closeout failed\n    condition 4 on a field the loop always changes. See PR #209.\n 5. The state transitions are exactly the terminal set: task `review` \u2192 `done`, lock\n@@ -113,12 +113,12 @@\n classification must be derived, not asserted.\n \n **Fold the closeout into the implementation PR** so no second PR exists. Rejected under\n-ADR 0012, which recorded why: a task file that is already `done` on the implementation\n+SADR-0012, which recorded why: a task file that is already `done` on the implementation\n branch short-circuits the loop's review \u2192 CCA \u2192 merge \u2192 done ordering into a local\n goal-completion before any merge happens.\n \n **Have the loop auto-merge closeouts with elevated credentials.** Rejected outright: that\n-is a runtime authoring its own approval, which ADR 0010 and the constitution's\n+is a runtime authoring its own approval, which SADR-0010 and the constitution's\n approval-record prohibition forbid. It would also be indistinguishable, in the evidence,\n from the forgery those rules exist to prevent.\n \ndiff --git a/docs/adr/0018-carried-guardian-approval-across-a-proven-base-sync.md b/docs/adr/SADR-0018-carried-guardian-approval-across-a-proven-base-sync.md\nindex 22358562da390ed8b45cd600da08a43d378b4680..e6e2907cefa776460af73ddf7d39db605f3c39b0\n--- a/docs/adr/0018-carried-guardian-approval-across-a-proven-base-sync.md\n+++ b/docs/adr/SADR-0018-carried-guardian-approval-across-a-proven-base-sync.md\n@@ -1,4 +1,4 @@\n-# ADR 0018: Carried Guardian Approval Across A Proven Base Sync\n+# SADR-0018: Carried Guardian Approval Across A Proven Base Sync\n \n ## Status\n \n@@ -34,7 +34,7 @@\n Guardian evaluator that consumes it (`shiki_guardian.evaluate_guardian_approval`,\n the `guardian_comment_carried` source) were built and tested in isolation. Neither\n does anything until a caller passes the flags that turn the carry on, so the\n-feature was invisible in CI. This ADR records the decision the two of them\n+feature was invisible in CI. This SADR records the decision the two of them\n implement, and this task wires it into the four CI invocations that judge Guardian\n approval.\n \ndiff --git a/docs/adr/README.md b/docs/adr/README.md\nindex c70e057a4520c36e042ab68c53cd4a21ef709b71..5da43856836e2ce6a150f8127766e2f88355ab4b\n--- a/docs/adr/README.md\n+++ b/docs/adr/README.md\n@@ -1,40 +1,41 @@\n-# Architecture Decision Records\n+# Decision Record Namespaces\n \n-This directory holds Shiki's Architecture Decision Records (ADRs): durable\n-records of hard-to-reverse platform decisions. ADRs are part of the Shiki source\n-of truth alongside GitHub state, the `.shiki/` mirror, and `CONTEXT.md`.\n+This directory holds two independent decision namespaces:\n \n-## How ADRs Work\n+- Shiki Architecture Decision Records (SADRs), named\n+  `SADR-NNNN-<short-slug>.md`, record hard-to-reverse Shiki platform decisions.\n+- Target Architecture Decision Records (ADRs), named\n+  `NNNN-<short-slug>.md`, belong to the target product.\n \n-- New ADRs are added as `docs/adr/NNNN-<short-slug>.md`, where `NNNN` is the next\n-  zero-padded sequence number.\n-- The **canonical list of ADRs is the set of files matching `docs/adr/*.md`** in\n-  this directory. Browse `docs/adr/*.md` for the authoritative, current set.\n-- Use [`template.md`](template.md) as the starting point for a new ADR.\n-- Record an ADR when a decision is hard to reverse, surprising without context,\n-  and a real tradeoff. Do not write an ADR for routine or easily reversible\n-  choices.\n+The same four-digit number may appear once in each namespace. A platform\n+`SADR-NNNN` citation resolves only to `SADR-NNNN-*.md`; a target `ADR NNNN`\n+citation resolves only to `NNNN-*.md`.\n \n-Using the `docs/adr/*.md` glob as the source of truth means other PRs can add an\n-ADR without editing this index file, avoiding merge contention on a\n-hand-maintained enumeration.\n+## How Decision Records Work\n \n-## Current ADRs (snapshot)\n-\n-The list below is a convenience snapshot and may lag behind the directory. When\n-in doubt, list `docs/adr/*.md`.\n+- New Shiki platform decisions use the next zero-padded SADR number.\n+- New target product decisions use the target's own ADR sequence.\n+- The canonical list is the files in `docs/adr/`; no hand-maintained index is\n+  authoritative.\n+- Use [`template.md`](template.md) as the starting point and choose the filename\n+  namespace that owns the decision.\n+- Record a decision when it is hard to reverse, surprising without context, and\n+  a real tradeoff. Do not write a decision record for routine or easily\n+  reversible choices.\n \n-- [0001 \u2014 Keep Shiki As The Platform Name](0001-keep-shiki-as-platform-name.md)\n-- [0002 \u2014 Use AGENTS As The Shared Agent Constitution](0002-use-agents-as-shared-constitution.md)\n-- [0003 \u2014 Require Skill Gate For MergeGate Readiness](0003-require-skill-gate-for-mergegate-readiness.md)\n-- [0004 \u2014 Use GitHub First With A Repository-Local Shiki Mirror](0004-use-github-first-with-shiki-mirror.md)\n-- [0005 \u2014 Use Codex As Front And Claude Code Action As The GitHub Runtime](0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md)\n-- [0006 \u2014 Separate Codex Implementation, CCA Completion, And MergeGate Authorization](0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md)\n+Using the directory as the source of truth lets independent changes add records\n+without contending on an enumerated index.\n \n-## Adding A New ADR\n+## Adding A Shiki SADR\n \n-1. Copy `template.md` to `docs/adr/NNNN-<short-slug>.md`.\n+1. Copy `template.md` to `docs/adr/SADR-NNNN-<short-slug>.md`.\n 2. Fill in the title, status, context, decision, and consequences.\n-3. Set the status to `Proposed`, then `Accepted` once the decision is approved.\n-4. Open a PR following `CONTRIBUTING.md`. You do not need to edit this index for\n-   the ADR to be discoverable via `docs/adr/*.md`.\n+3. Set the status to `Proposed`, then `Accepted` once the proper authority has\n+   approved the decision.\n+4. Open a PR following `CONTRIBUTING.md`.\n+\n+## Adding A Target ADR\n+\n+1. Copy `template.md` to `docs/adr/NNNN-<short-slug>.md` using the target's own\n+   sequence.\n+2. Cite it as `ADR NNNN`; do not prefix its filename with `SADR-`.\ndiff --git a/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md b/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\nnew file mode 100644\nindex 0000000000000000000000000000000000000000..7455ce1f3bed02ddddb851ac21a1d1a648c9aeba\n--- /dev/null\n+++ b/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\n@@ -0,0 +1,55 @@\n+# SADR-0019: Separate Shiki Platform SADRs From Target ADRs\n+\n+## Status\n+\n+Accepted\n+\n+## Context\n+\n+Shiki installs its platform decision records into `docs/adr/` in every target\n+repository. Shiki and its targets previously used the same numeric\n+`NNNN-<slug>.md` namespace. A target that already owned ADRs 0013 through 0018\n+therefore acquired two records for each number during installation, making an\n+`ADR NNNN` citation ambiguous. Renumbering target decisions would rewrite target\n+history, while renumbering Shiki decisions per target would destroy their stable\n+platform identity.\n+\n+Upgrades also need to remove the former Shiki-owned numeric paths without ever\n+mistaking a target-owned ADR for platform content. The install stamp already\n+records exact shipped paths and content digests, so it can prove ownership only\n+when both still match.\n+\n+## Decision\n+\n+Shiki platform decisions use the `SADR` namespace:\n+\n+- records are named `docs/adr/SADR-NNNN-<slug>.md`;\n+- live platform citations use `SADR-NNNN`;\n+- target product decisions retain `docs/adr/NNNN-<slug>.md` and `ADR NNNN`;\n+- the reference validator resolves the two namespaces independently, so the same\n+  four-digit number may exist once in each namespace.\n+\n+Before a forced upgrade writes anything, the installer inventories the exact\n+former numeric Shiki ADR paths. It may delete a legacy path only when the\n+existing install stamp contains that exact path and its recorded SHA-256 digest\n+matches the file currently on disk. All ownership blockers are reported\n+together and abort the upgrade before its first write. A non-forced install\n+never performs this cleanup.\n+\n+Historical `.shiki/` evidence remains unchanged because it records the namespace\n+that existed when that evidence was produced.\n+\n+## Consequences\n+\n+- Targets may use any ADR number without colliding with Shiki's platform SADRs.\n+- Fresh installs preserve target ADR bytes and install a stable SADR set.\n+- Forced upgrades fail closed when legacy ownership cannot be proven, and a\n+  successful upgrade refreshes the stamp without removed legacy paths.\n+- Current governance, packaging, workflow, prompt, script, and skill references\n+  must move with the platform records.\n+\n+## Alternatives Considered\n+\n+- Collision-only refusal leaves affected targets permanently unupgradable.\n+- Renumbering target ADRs mutates target-owned history.\n+- Per-target Shiki renumbering makes platform citations non-portable.\ndiff --git a/docs/adr/template.md b/docs/adr/template.md\nindex a92a97daddd0b00ab9dd626cc8d86942da2eafed..17dd79175b5fee2105935557f691b40cf9492942\n--- a/docs/adr/template.md\n+++ b/docs/adr/template.md\n@@ -1,14 +1,14 @@\n-# NNNN: <Short Decision Title>\n+# <SADR-NNNN or NNNN>: <Short Decision Title>\n \n ## Status\n \n-Proposed | Accepted | Superseded by ADR-NNNN\n+Proposed | Accepted | Superseded by SADR-NNNN or target ADR NNNN\n \n ## Context\n \n What is the situation, constraint, or force that requires a decision? Reference\n-relevant domain terms (`CONTEXT.md`), prior ADRs, and code reality. State the\n-problem, not the solution.\n+relevant domain terms (`CONTEXT.md`), prior Shiki SADRs or target ADRs, and code\n+reality. State the problem, not the solution.\n \n ## Decision\n \ndiff --git a/docs/agents/autonomous-loop-e2e-acceptance.md b/docs/agents/autonomous-loop-e2e-acceptance.md\nindex bbf84c6081987f159ac6ad9ecf3c82b7aa72e7e8..5c13c1d02a05a90b0dc46cbef7c95a9846d0c237\n--- a/docs/agents/autonomous-loop-e2e-acceptance.md\n+++ b/docs/agents/autonomous-loop-e2e-acceptance.md\n@@ -40,8 +40,8 @@\n 3. `gh auth status` is logged in with merge permission on the repo.\n 4. Branch protection + required checks are configured exactly as the goal-loop\n    required-check set expects (see `docs/agents/branch-protection-smoke.md`).\n-5. The repo `.shiki/policy` permits autonomous low/medium-risk merges (ADR\n-   0008/0009); the MergeGate policy check is the recorded-authority gate.\n+5. The repo `.shiki/policy` permits autonomous low/medium-risk merges\n+   (SADR-0008/0009); the MergeGate policy check is the recorded-authority gate.\n \n The operator's only actions are: register the task and start the loop. **No\n manual pre-clearing** means after `shiki loop run` starts, the operator does not\ndiff --git a/docs/agents/bootstrap-command.md b/docs/agents/bootstrap-command.md\nindex 05f7f203a52cf412b9f97084a6e131ed0f785afd..f7aa0b734dfebee4a3b139335b66ee3415f48713\n--- a/docs/agents/bootstrap-command.md\n+++ b/docs/agents/bootstrap-command.md\n@@ -189,7 +189,9 @@\n ```\n \n Use this only for tests, fixtures, or explicit local-only template inspection.\n-Use `--force` only when you intentionally want to overwrite existing target files.\n+Use `--force` only when you intentionally want to refresh the target template.\n+Legacy numeric Shiki decision cleanup is stamp-and-digest authorized before any\n+write; target ADR paths are not cleanup candidates.\n \n ## Slash Command\n \ndiff --git a/docs/agents/checklists.md b/docs/agents/checklists.md\nindex 4a7f03ade346f53a1c1d82981f3fd744f9ab4547..0c5ec6c53dc7197b7b7a7d18e667b325babd6c45\n--- a/docs/agents/checklists.md\n+++ b/docs/agents/checklists.md\n@@ -32,18 +32,18 @@\n | ID | Item | Blocking |\n | --- | --- | --- |\n | GD-01 | Plan was challenged against `CONTEXT.md`. | blocking for non-trivial Goals |\n-| GD-02 | Relevant ADRs were read or absence was recorded. | blocking for non-trivial Goals |\n+| GD-02 | Relevant SADRs and target ADRs were read or absence was recorded. | blocking for non-trivial Goals |\n | GD-03 | Ambiguous terms were resolved or converted to HITL blockers. | blocking |\n | GD-04 | Edge scenarios were considered. | advisory |\n | GD-05 | Code reality was checked when code could answer. | blocking when applicable |\n | GD-06 | New domain terms were added to `CONTEXT.md` when needed. | blocking when applicable |\n-| GD-07 | ADR was created or explicitly skipped using the ADR criteria. | blocking when applicable |\n+| GD-07 | A SADR or target ADR was created or explicitly skipped using the decision-record criteria. | blocking when applicable |\n \n ## CI \u2014 Context & Impact Checklist\n \n | ID | Item | Blocking |\n | --- | --- | --- |\n-| CI-01 | Relevant docs and ADRs identified. | blocking |\n+| CI-01 | Relevant docs, SADRs, and target ADRs identified. | blocking |\n | CI-02 | Relevant modules, interfaces, seams, callers, and tests identified. | blocking |\n | CI-03 | Dependencies and blocked-by relationships identified. | blocking |\n | CI-04 | Candidate locks identified. | blocking |\n@@ -62,7 +62,7 @@\n | PRD-04 | Implementation decisions are recorded without volatile code detail. | blocking |\n | PRD-05 | Testing decisions are recorded. | blocking |\n | PRD-06 | Out-of-scope items are explicit. | blocking |\n-| PRD-07 | Relevant glossary/ADR links are present when applicable. | blocking |\n+| PRD-07 | Relevant glossary/SADR/target-ADR links are present when applicable. | blocking |\n \n ## SF \u2014 Spec Freeze Checklist\n \n@@ -98,7 +98,7 @@\n | H-02 | Branch/worktree target is present. | blocking |\n | H-03 | Scope and non-goals are present. | blocking |\n | H-04 | Dependencies and locks are present. | blocking |\n-| H-05 | Relevant docs, ADRs, modules, and tests are referenced. | blocking |\n+| H-05 | Relevant docs, SADRs, target ADRs, modules, and tests are referenced. | blocking |\n | H-06 | Required skills are stated. | blocking |\n | H-07 | TDD expectations are stated. | blocking when behavior work |\n | H-08 | Acceptance criteria are included. | blocking |\ndiff --git a/docs/agents/codex-handoff.md b/docs/agents/codex-handoff.md\nindex ede468f47cb277f69a857e8c45df703e4ed0151d..6f97bdffb67e44d7edea2c71e01ea28933c4eb6c\n--- a/docs/agents/codex-handoff.md\n+++ b/docs/agents/codex-handoff.md\n@@ -46,7 +46,7 @@\n \n ## Context\n - Domain terms:\n-- Relevant ADRs:\n+- Relevant SADRs and target ADRs:\n - Relevant docs:\n - Relevant modules/interfaces/seams:\n - Similar tests/prior art:\ndiff --git a/docs/agents/completion-check-agent.md b/docs/agents/completion-check-agent.md\nindex 13e4c035db35025f4e3cdcc5adfb24314ac4d521..61bfa1218cf36ca605250c91919e620f33238aa0\n--- a/docs/agents/completion-check-agent.md\n+++ b/docs/agents/completion-check-agent.md\n@@ -32,7 +32,7 @@\n - `AGENTS.md`\n - `CLAUDE.md`\n - `CONTEXT.md`\n-- relevant ADRs\n+- relevant SADRs and target ADRs\n - parent Goal issue\n - PRD issue or document\n - task issue\ndiff --git a/docs/agents/decision-control.md b/docs/agents/decision-control.md\nindex 7d8ee5ab03a5ccbcbd04056ae61de03785a2a9c9..8c94ecda00fffaa0e92c325ab83aa9776bd1dac1\n--- a/docs/agents/decision-control.md\n+++ b/docs/agents/decision-control.md\n@@ -11,7 +11,7 @@\n | Decision | Owner | When | Required inputs | Output | Enforcement surface |\n | --- | --- | --- | --- | --- | --- |\n | Is the Goal clear enough to plan? | Planner + Guardian when needed | Goal intake | User request, existing docs, risk signals | Goal readiness verdict | Goal checklist, issue template, ledger |\n-| Must `grill-with-docs` run? | Planner / Skill Gate | Before PRD or issue split | Ambiguity, domain terms, ADR conflicts, unclear boundaries | Skill invocation or explicit skip rationale | Skill Gate checklist |\n+| Must `grill-with-docs` run? | Planner / Skill Gate | Before PRD or issue split | Ambiguity, domain terms, SADR/target-ADR conflicts, unclear boundaries | Skill invocation or explicit skip rationale | Skill Gate checklist |\n | Is a PRD required? | Planner | After Goal context stabilizes | Goal, `grill-with-docs` output, Context & Impact output | PRD required / not required | PRD checklist, CCA evidence check |\n | Can the spec be frozen? | Operator only | End of Requirements Definition, before issue split | PRD/requirements summary, scope inventory (SF-02), open-question list | `spec_freeze` block in the plan + ledger evidence | `require_grilled_plan` (plan ingest/run), SF checklist |\n | Can a frozen spec change? | Operator only (Spec Amendment) | When implementation contradicts the frozen spec | Contested decisions, scoped re-grill result | recorded amendment + re-stamped freeze | SF checklist, ledger evidence |\ndiff --git a/docs/agents/domain.md b/docs/agents/domain.md\nindex 258da8ea81e87ac0a6b0b5b5e5c2fde980eaabb7..d57b0cf17ff3e1fa9e6e5885372bf083d8295421\n--- a/docs/agents/domain.md\n+++ b/docs/agents/domain.md\n@@ -5,7 +5,9 @@\n ## Layout\n \n - `CONTEXT.md` defines Shiki language. Keep it implementation-free.\n-- `docs/adr/` records hard-to-reverse decisions.\n+- `docs/adr/SADR-NNNN-*.md` records Shiki platform decisions.\n+- `docs/adr/NNNN-*.md` records target product decisions without sharing the\n+  SADR namespace.\n - `AGENTS.md` is the runtime-neutral agent constitution.\n - `CLAUDE.md` is the Claude Code wrapper around `AGENTS.md`.\n - `.shiki/` is the Target Repository mirror for Goals, PRDs, plans, Task DAGs, contracts, locks, worktree records, ledger entries, CCA verdicts, repair packets, reports, and handoffs.\n@@ -15,6 +17,6 @@\n \n - Use Shiki terms exactly when they exist.\n - Add glossary terms to `CONTEXT.md` only when the term is part of the domain, not a generic programming concept.\n-- Create ADRs only for hard-to-reverse, surprising, tradeoff-based decisions.\n+- Create SADRs or target ADRs only for hard-to-reverse, surprising, tradeoff-based decisions.\n - Keep product-specific language in the Target Repository, not in Shiki Template docs.\n-- If an ADR conflicts with a proposed implementation, surface the conflict before editing.\n+- If a SADR or target ADR conflicts with a proposed implementation, surface the conflict before editing.\ndiff --git a/docs/agents/execution-control.md b/docs/agents/execution-control.md\nindex ad8ee3cdb36be56fedc1f46408ca92b387d74047..bc63c3a73661d12b567016ba299f1a6b66bf9088\n--- a/docs/agents/execution-control.md\n+++ b/docs/agents/execution-control.md\n@@ -63,7 +63,7 @@\n | Branch name | Orchestrator | Orchestrator | Before implementer dispatch | branch policy | branch ref in task/ledger |\n | Lock reservation | Context & Impact | MergeGate | Before dispatch and before merge | `.shiki/locks/`, labels, PR metadata | lock id, owner, scope |\n | CI profile | Planner | MergeGate | During issue triage, rechecked at PR | labels, `.shiki/policy.yaml`, workflow matrix | required check list |\n-| Required status checks | Repo maintainer / Guardian | GitHub branch protection / rulesets | Repository setup, updated via ADR | GitHub settings | branch/ruleset config, ADR |\n+| Required status checks | Repo maintainer / Guardian | GitHub branch protection / rulesets | Repository setup, updated via target ADR | GitHub settings | branch/ruleset config, target ADR |\n | CCA judgment | CCA | GitHub required check / MergeGate | Every non-draft PR update | GitHub Action check | structured CCA verdict |\n | Repair assignment | CCA | MergeGate | After failed CCA/CI/review | repair packet, issue/PR comment | repair id and attempt count |\n | Merge readiness | MergeGate | GitHub branch protection / Guardian if needed | After CCA complete | required checks, review rules, rulesets | mergegate check output |\ndiff --git a/docs/agents/guardian-policy.md b/docs/agents/guardian-policy.md\nindex 4147c7adf05a47adb4fd24ada1a1c2e1f2ba6d2d..a20d9cc6c00a569bb057b9ca8d6b51c7fc4a9847\n--- a/docs/agents/guardian-policy.md\n+++ b/docs/agents/guardian-policy.md\n@@ -98,8 +98,8 @@\n `origin/<default_branch>` and reuses each job's existing base fetch; a shallow\n (`--depth`) or second base fetch would make the proof unresolvable and silently\n disable the carry, so neither is permitted. CCA reads `guardian_comment_carried` as\n-a recorded authority in the deterministic signal and does not re-derive it. See ADR\n-0018 for the full decision, the rejected diff/patch-equality and path-disjointness\n+a recorded authority in the deterministic signal and does not re-derive it. See\n+SADR-0018 for the full decision, the rejected diff/patch-equality and path-disjointness\n alternatives, and the accepted residual.\n \n ## External AI Guardian Review (SADR-0010)\ndiff --git a/docs/agents/implementation-policy.md b/docs/agents/implementation-policy.md\nindex c754144a9fb5bf4a38aa82a60e1670c59e626b44..85c37281bcad196c6b7b02343faf00b03cd9c57a\n--- a/docs/agents/implementation-policy.md\n+++ b/docs/agents/implementation-policy.md\n@@ -31,10 +31,13 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n ## Non-Negotiables\n \n 1. **Planning precedes implementation.** Non-trivial Goals start with `grill-with-docs` and Context & Impact.\n-2. **Domain language is controlled.** Terms must be checked against `CONTEXT.md`; hard-to-reverse tradeoffs must be checked against ADRs.\n+2. **Domain language is controlled.** Terms must be checked against `CONTEXT.md`; hard-to-reverse tradeoffs must be checked against Shiki SADRs and target ADRs.\n 3. **PRD precedes issue decomposition.** `to-prd` is used once the context is settled enough to become durable intent.\n 4. **Issues are vertical slices.** `to-issues` must produce independently grabbable tracer-bullet tasks, not horizontal layer tickets.\n 5. **Triage controls dispatch.** Only `ready-for-agent` / AFK tasks may be assigned to Codex without human input.\n@@ -95,19 +98,19 @@\n Process:\n \n - Challenge terms against `CONTEXT.md`.\n-- Check ADRs before reopening decisions.\n+- Check SADRs and target ADRs before reopening decisions.\n - Inspect code when code can answer a question.\n - Ask one question at a time when the operator is available.\n - Provide a recommended answer with each question.\n - Record resolved terms in `CONTEXT.md`.\n-- Create ADRs only for hard-to-reverse, surprising, tradeoff-based decisions.\n+- Create SADRs or target ADRs only for hard-to-reverse, surprising, tradeoff-based decisions.\n - Convert unresolved load-bearing questions into HITL blockers.\n \n Outputs:\n \n - Resolved design-tree decisions.\n - Updated glossary terms when needed.\n-- ADRs when justified.\n+- SADRs or target ADRs when justified.\n - HITL blockers when decisions remain unresolved.\n \n ## Phase 3 \u2014 Context & Impact\n@@ -117,7 +120,7 @@\n \n Outputs must include:\n \n-- Relevant documents and ADRs.\n+- Relevant documents, SADRs, and target ADRs.\n - Relevant modules, interfaces, seams, callers, and tests.\n - Candidate locks.\n - Dependencies.\n@@ -140,7 +143,7 @@\n - Testing decisions.\n - Out of scope.\n - Further notes.\n-- Links to relevant Goal, domain terms, and ADRs.\n+- Links to relevant Goal, domain terms, SADRs, and target ADRs.\n \n Phase 4 ends with Spec Freeze: the operator explicitly approves the PRD, the\n plan records a `spec_freeze` block (status, approved_by, source), and the SF\n@@ -240,7 +243,7 @@\n - Dependencies and locks.\n - Risk and Guardian requirements.\n - Ledger completeness.\n-- Documentation and ADR updates.\n+- Documentation and SADR/target-ADR updates.\n - Scope drift.\n \n CCA emits one of:\n@@ -301,7 +304,7 @@\n | Evidence failure | Behavior might be done, but proof is missing. | Add checks, PR evidence, ledger entry, or artifacts. |\n | Scope drift | PR changes unrelated behavior. | Revert unrelated changes or split into new Goal. |\n | Dependency block | Upstream task or lock not complete. | Block until dependency resolves. |\n-| Architecture block | Decision needs ADR or deep-module discussion. | `grill-with-docs` or `improve-codebase-architecture`. |\n+| Architecture block | Decision needs a SADR, target ADR, or deep-module discussion. | `grill-with-docs` or `improve-codebase-architecture`. |\n | Risk block | Security/production/policy change needs human approval. | Guardian decision. |\n \n ## Phase 5.5 \u2014 Execution Decision Control\ndiff --git a/docs/agents/runtime-auth-model.md b/docs/agents/runtime-auth-model.md\nindex 6a2e1a47e81ea89b5c68a210ec07772a9374babc..3c54748d24233dbd0ba8473950282547ef4ce935\n--- a/docs/agents/runtime-auth-model.md\n+++ b/docs/agents/runtime-auth-model.md\n@@ -148,7 +148,7 @@\n \n API-key based runners may be added by a target repository as an explicit extension. They are not the default Shiki template path.\n \n-If a repo opts into API-key mode, record the exception in an ADR and update the repo's `AGENTS.md`, `.shiki/config.yaml`, and workflow permissions.\n+If a repo opts into API-key mode, record the target-specific exception in a target ADR and update the repo's `AGENTS.md`, `.shiki/config.yaml`, and workflow permissions.\n \n ## GitHub Actions Runtime Compatibility\n \ndiff --git a/docs/agents/skill-gate.md b/docs/agents/skill-gate.md\nindex c50398b0d7c60a8598f2a5c88a10015730dced13..3acffe6444617bb1d54b77d4b4708158ec735bc6\n--- a/docs/agents/skill-gate.md\n+++ b/docs/agents/skill-gate.md\n@@ -9,7 +9,7 @@\n | Skill | Trigger | Expected output |\n | --- | --- | --- |\n | `setup-matt-pocock-skills` | First configuring a repo for these skills, or when issue tracker, triage labels, or domain docs are missing. | `docs/agents/` configuration and an `Agent skills` block in `AGENTS.md` or `CLAUDE.md`. |\n-| `grill-with-docs` | Ambiguous plan, unclear terminology, boundary decision, tradeoff, or ADR-worthy design choice. | Clarified decision tree, recommended answers, updates to `CONTEXT.md` or ADRs when decisions crystallize. |\n+| `grill-with-docs` | Ambiguous plan, unclear terminology, boundary decision, tradeoff, or decision-record-worthy design choice. | Clarified decision tree, recommended answers, updates to `CONTEXT.md`, SADRs, or target ADRs when decisions crystallize. |\n | `zoom-out` | Agent lacks a higher-level architectural map. | Map of relevant modules, callers, seams, and domain concepts. |\n | `to-prd` | Settled context should become a PRD. | PRD published to the issue tracker with problem, solution, user stories, decisions, testing, and out-of-scope notes. |\n | `to-issues` | Goal, PRD, or plan must become implementation tickets. | Vertical-slice issues with acceptance criteria, dependencies, and AFK/HITL classification. |\n@@ -17,7 +17,7 @@\n | `tdd` | Feature work or bug fix has observable behavior that can be specified with tests. | Red-green-refactor loop through public interfaces, one vertical slice at a time. |\n | `code-review` | The TDD loop is green and the implementer is about to create or update a PR. Default for every implementation task. | Pre-PR self-review of the task diff with fixes applied on the task branch; findings and resolutions recorded as a ledger entry naming the skill and a `## Pre-PR code review` PR body section. |\n | `diagnose` | Hard bug, failing check, regression, flaky behavior, or performance problem. | Reproduction loop, ranked hypotheses, instrumentation, fix, regression test, post-mortem. |\n-| `improve-codebase-architecture` | Structural friction, testability, AI-navigability, deep-module opportunity, or refactor request. | Architecture candidates and deepening recommendations informed by `CONTEXT.md` and ADRs. |\n+| `improve-codebase-architecture` | Structural friction, testability, AI-navigability, deep-module opportunity, or refactor request. | Architecture candidates and deepening recommendations informed by `CONTEXT.md`, SADRs, and target ADRs. |\n | `prototype` | A throwaway logic or UI prototype is needed to answer a design question. | Clearly marked disposable prototype and captured decision. |\n \n ## MergeGate Interaction\ndiff --git a/docs/operations.md b/docs/operations.md\nindex cf1f452d4c589e38ccd58553bda0ad8684b51eb7..ace48a90ffbec9e5c0a7113bef16a4858bc44b3f\n--- a/docs/operations.md\n+++ b/docs/operations.md\n@@ -89,6 +89,16 @@\n      target history, brought forward by `shiki migrate`, not a template.\n    - **Everything else** (CLI scripts, workflows, schemas, docs) is overwritten.\n \n+   A forced upgrade also migrates the former numeric Shiki decision paths to\n+   `SADR-NNNN-*`. Before the first write, the installer inventories the exact\n+   legacy Shiki paths and authorizes each deletion only when the existing\n+   `.shiki/install-stamp.json` names that path and its SHA-256 digest still\n+   matches the file. An absent, unreadable, malformed, incomplete, or\n+   mismatching stamp reports every blocked path and stops with no filesystem\n+   change. Target-owned `NNNN-*` ADRs are not cleanup candidates. A non-force\n+   install deletes nothing and retains any still-matching legacy stamp entries\n+   so a later forced upgrade can prove ownership.\n+\n    If the target has pending migrations, `--force` refuses and writes nothing \u2014\n    apply migrations first (step 3), then re-run. The run ends with a single\n    summary of every `.new` written, so a half-upgrade is visible.\ndiff --git a/docs/releasing.md b/docs/releasing.md\nindex 10d73828e7dbd64e9d9f6b83bc84d010537ce62a..aafeee8c4d441e1e427efee2ce4f81a647f020e1\n--- a/docs/releasing.md\n+++ b/docs/releasing.md\n@@ -67,7 +67,7 @@\n CLI in `scripts/`. There are no third-party Python runtime dependencies. The CLI\n is intentionally **not** exposed as a `[project.scripts]` console entry point\n because `scripts/shiki.py` is a dependency-free shim and `scripts/` is not\n-packaged as an importable distribution (see the ADR for the rationale).\n+packaged as an importable distribution (see SADR-0007 for the rationale).\n \n Supported install paths:\n \n@@ -155,6 +155,15 @@\n    [docs/operations.md](operations.md#the-new-convention). If the target has\n    pending migrations, `--force` refuses and writes nothing; apply migrations\n    (step 2) first, then re-run.\n+\n+   The SADR namespace upgrade has an additional pre-write gate: each former\n+   numeric Shiki decision path is removed only when the existing install stamp\n+   contains that exact path and a digest matching its current bytes. The\n+   installer aggregates absent/unreadable/malformed-stamp, missing-digest, and\n+   digest-mismatch blockers before raising, so refusal leaves the target tree\n+   byte-identical. Numeric target ADRs are preserved. A successful forced\n+   upgrade installs and stamps `SADR-*` paths and drops the removed legacy paths\n+   from the refreshed stamp.\n 2. Apply repository-local `.shiki/` state migrations with the migrate command:\n \n    ```bash\ndiff --git a/docs/usage.md b/docs/usage.md\nindex b83407ee3970dd395dd893010bc5dbe8bad26051..a130ef6e40dfd8169a5bc701339351097df1a09f\n--- a/docs/usage.md\n+++ b/docs/usage.md\n@@ -67,8 +67,10 @@\n shiki install-target /path/to/target-repo --local-only\n ```\n \n-Use `--force` only when you intentionally want to overwrite existing target\n-files.\n+Use `--force` only when you intentionally want to refresh the target template.\n+For the SADR namespace migration, `--force` deletes only exact legacy Shiki\n+decision paths whose existing install-stamp digest still matches; any ownership\n+blocker stops before the first write. Numeric target ADRs are never candidates.\n \n ## `shiki bootstrap-platform` \u2014 publish the Shiki platform repo\n \ndiff --git a/scripts/guardian_approval_signal.py b/scripts/guardian_approval_signal.py\nindex d059f92b2c82aa2b5d6ca421365537a87886c1cb..16924892714f4f5b9303c0ef0e325a7e70075630\n--- a/scripts/guardian_approval_signal.py\n+++ b/scripts/guardian_approval_signal.py\n@@ -199,8 +199,8 @@\n ) -> Any:\n     \"\"\"Evaluate SADR-0015 Contract Approval for the CCA Guardian signal.\n \n-    The SAME OR that MergeGate applies must reach this deterministic signal: ADR\n-    0015 is explicit that without it, a contract-approved PR still returns\n+    The SAME OR that MergeGate applies must reach this deterministic signal:\n+    SADR-0015 is explicit that without it, a contract-approved PR still returns\n     ``needs_guardian``, the CCA job fails, and the MergeGate policy check never\n     runs \u2014 the gate this change moves would never be reached. This resolves the\n     task the SAME way MergeGate does (the first Shiki task id in the PR body),\ndiff --git a/scripts/shiki_contract_approval.py b/scripts/shiki_contract_approval.py\nindex 941bcb96fdd8b9a85bc0c4f0f04d5dad9d04839c..4a4b418f6527413161db82670bec857a043360e2\n--- a/scripts/shiki_contract_approval.py\n+++ b/scripts/shiki_contract_approval.py\n@@ -142,7 +142,7 @@\n     changed_files_status: Any,\n     registration: Any,\n ) -> ContractApprovalResult:\n-    \"\"\"Whether this PR carries an SADR-0015 Contract Approval.\n+    \"\"\"Whether this PR carries a SADR-0015 Contract Approval.\n \n     The four conditions are evaluated IN ORDER, each failure recorded as a\n     distinct reason string:\ndiff --git a/scripts/shiki_guardian.py b/scripts/shiki_guardian.py\nindex a4ecb79bbda55fd5c8b06adfae2eb99760a03c45..4dd99bbe714e26f421fa19abac7a34ae1b195ecc\n--- a/scripts/shiki_guardian.py\n+++ b/scripts/shiki_guardian.py\n@@ -12,7 +12,7 @@\n # The carry re-derives its proof from git in-process on every judging invocation;\n # it is never read from an artifact, so there is nothing to go stale or be forged.\n # shiki_sync_proof is the ONLY guardian-family module that runs git, and every one\n-# of its invocations is read-only and local (ADR: prove-from-git base sync).\n+# of its invocations is read-only and local (SADR: prove-from-git base sync).\n from shiki_sync_proof import verify_pure_base_sync\n \n GUARDIAN_POLICY_PATH = \".shiki/guardian-policy.json\"\ndiff --git a/scripts/shiki_guardian_review.py b/scripts/shiki_guardian_review.py\nindex dd0f104d457899e0ba5ff1e162011c1e90b6f92b..e10ef58c046324e7ea3a3392feb3048ca77414ce\n--- a/scripts/shiki_guardian_review.py\n+++ b/scripts/shiki_guardian_review.py\n@@ -159,7 +159,7 @@\n     docs_only = _is_docs_only(changed_files)\n     if docs_only and matched == 0:\n         focus.append(\n-            \"Docs/ADR-only change: confirm the change is genuinely documentation/decision-only, introduces no behavior or contract drift, and stays consistent with the constitution and existing ADRs.\"\n+            \"Docs/decision-record-only change: confirm the change is genuinely documentation/decision-only, introduces no behavior or contract drift, and stays consistent with the constitution and existing SADRs and target ADRs.\"\n         )\n \n     # Conservative augmentation: unknown (no category and not docs-only), mixed\ndiff --git a/scripts/shiki_installer.py b/scripts/shiki_installer.py\nindex be064ab7a29ff6836f7f7a1d70500a43e0d11d7b..9fd8eefd137ce7003dad5c0dcb2e48e2cd47ab32\n--- a/scripts/shiki_installer.py\n+++ b/scripts/shiki_installer.py\n@@ -118,6 +118,7 @@\n # what lets a target carry it without a governance-schema change.\n INSTALL_STAMP_PATH = \".shiki/install-stamp.json\"\n INSTALL_STAMP_VERSION = 1\n+LEGACY_SHIKI_SADR_MAX = 18\n \n # Under --force the shipped surface splits three ways, so nothing is lost and\n # nothing is silently stale.\n@@ -237,9 +238,98 @@\n         )\n \n \n+def legacy_shiki_adr_paths() -> tuple[str, ...]:\n+    \"\"\"Former numeric paths for Shiki SADR-0001 through SADR-0018.\n+\n+    The authoritative slugs come from the current SADR filenames. SADR-0019 is\n+    the namespace decision itself and never shipped under a numeric legacy path.\n+    \"\"\"\n+    paths: list[str] = []\n+    for sadr in sorted((ROOT / \"docs\" / \"adr\").glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\")):\n+        number = sadr.name.split(\"-\", 2)[1]\n+        if int(number) <= LEGACY_SHIKI_SADR_MAX:\n+            paths.append(f\"docs/adr/{sadr.name.removeprefix('SADR-')}\")\n+    return tuple(paths)\n+\n+\n+def preflight_legacy_sadr_cleanup(target: Path, *, force: bool) -> tuple[str, ...]:\n+    \"\"\"Authorize exact legacy Shiki ADR removals before the first write.\n+\n+    A non-force install never removes existing files. A forced install needs no\n+    stamp when no legacy Shiki path is present. When legacy paths do exist, each\n+    must be named in a readable install stamp and its current bytes must match\n+    the recorded digest.\n+    \"\"\"\n+    if not force:\n+        return ()\n+    present = tuple(\n+        relative\n+        for relative in legacy_shiki_adr_paths()\n+        if (target / relative).exists() or (target / relative).is_symlink()\n+    )\n+    if not present:\n+        return ()\n+\n+    stamp_path = target / INSTALL_STAMP_PATH\n+    try:\n+        raw = stamp_path.read_text(encoding=\"utf-8\")\n+        stamp = json.loads(raw)\n+    except FileNotFoundError:\n+        detail = \"install stamp is absent\"\n+        blockers = [f\"{relative}: {detail}\" for relative in present]\n+    except (OSError, UnicodeDecodeError) as error:\n+        detail = f\"install stamp is unreadable ({error})\"\n+        blockers = [f\"{relative}: {detail}\" for relative in present]\n+    except json.JSONDecodeError as error:\n+        detail = f\"install stamp is malformed ({error})\"\n+        blockers = [f\"{relative}: {detail}\" for relative in present]\n+    else:\n+        if not isinstance(stamp, dict):\n+            blockers = [\n+                f\"{relative}: install stamp is malformed (expected an object)\"\n+                for relative in present\n+            ]\n+        else:\n+            digests = stamp.get(\"digests\")\n+            blockers = []\n+            if not isinstance(digests, dict):\n+                blockers.extend(\n+                    f\"{relative}: install stamp has no digest for this path\"\n+                    for relative in present\n+                )\n+            else:\n+                for relative in present:\n+                    expected = digests.get(relative)\n+                    if not isinstance(expected, str) or not expected:\n+                        blockers.append(\n+                            f\"{relative}: install stamp has no digest for this path\"\n+                        )\n+                        continue\n+                    try:\n+                        actual = _sha256_file(target / relative)\n+                    except OSError as error:\n+                        blockers.append(\n+                            f\"{relative}: legacy path is unreadable ({error})\"\n+                        )\n+                        continue\n+                    if actual != expected:\n+                        blockers.append(\n+                            f\"{relative}: digest mismatch (stamp {expected}, current {actual})\"\n+                        )\n+\n+    if blockers:\n+        joined = \"\\n\".join(f\"- {blocker}\" for blocker in blockers)\n+        raise ShikiError(\n+            \"legacy Shiki ADR cleanup is not authorized:\\n\"\n+            f\"{joined}\\nNo files were written.\"\n+        )\n+    return present\n+\n+\n def install_template(target: Path, *, force: bool, validate: bool) -> list[NewFileNote]:\n     # Preconditions first: never rewrite the tree of a target that cannot be\n     # upgraded cleanly (see refuse_pending_migrations).\n+    legacy_cleanup = preflight_legacy_sadr_cleanup(target, force=force)\n     refuse_pending_migrations(target)\n \n     # Resolve the target's required CODEOWNERS owner once from its\n@@ -250,6 +340,10 @@\n     # verbatim. See ``codeowners_required_owner``.\n     codeowners_owner = codeowners_required_owner(target)\n \n+    for relative in legacy_cleanup:\n+        (target / relative).unlink()\n+        info(f\"removed stamped legacy Shiki ADR: {target / relative}\")\n+\n     notes: list[NewFileNote] = []\n     for relative in TEMPLATE_PATHS:\n         source = ROOT / relative\n@@ -647,6 +741,22 @@\n     digests = target_stamp_digests(target)\n     existing = read_install_stamp(target)\n     if existing is not None:\n+        # A non-force install deliberately leaves legacy Shiki ADRs in place.\n+        # Preserve only still-matching ownership proof so a later forced\n+        # upgrade can authorize cleanup; never carry a missing or stale digest.\n+        existing_digests = existing.get(\"digests\")\n+        if isinstance(existing_digests, dict):\n+            for relative in legacy_shiki_adr_paths():\n+                expected = existing_digests.get(relative)\n+                path = target / relative\n+                if not isinstance(expected, str) or not path.is_file():\n+                    continue\n+                try:\n+                    actual = _sha256_file(path)\n+                except OSError:\n+                    continue\n+                if actual == expected:\n+                    digests[relative] = actual\n         if _incoming_is_older(commit, existing.get(\"platform_commit\")):\n             warn(\n                 \"kept existing install stamp: incoming install \"\ndiff --git a/scripts/shiki_loop.py b/scripts/shiki_loop.py\nindex 3611641da8aeb5038b1da90e3114b09a473ec6d8..84caa491b9cad0edc4176f209c13e9221e975681\n--- a/scripts/shiki_loop.py\n+++ b/scripts/shiki_loop.py\n@@ -424,7 +424,7 @@\n         # unresolvable CCA verdict has already produced its terminal stop and is never\n         # laundered into a head-moving sync (SADR-0010). It also never fires while the\n         # policy (Guardian) gate is red \u2014 for ANY reason, not only when it is the sole\n-        # red check \u2014 so a head-bound Guardian approval, or an SADR-0015\n+        # red check \u2014 so a head-bound Guardian approval, or a SADR-0015\n         # contract-approval carry that base movement defeated (the policy gate compares\n         # base and head task governance directly), is never silently restored by\n         # adopting base wholesale. It is bounded by a durable per-task counter so the\ndiff --git a/scripts/shiki_tasks.py b/scripts/shiki_tasks.py\nindex aec3809f24b4a0c8c96fc50971cff9ae9ad30381..d531052e9d9717058d4631483cefab44c26a65c1\n--- a/scripts/shiki_tasks.py\n+++ b/scripts/shiki_tasks.py\n@@ -821,7 +821,7 @@\n         \"prompt\": prompt,\n         \"entry_skill\": \"grill-with-docs\",\n         \"required_next_steps\": [\n-            \"Run grill-with-docs until terminology, boundaries, risks, and ADR-worthy decisions are settled.\",\n+            \"Run grill-with-docs until terminology, boundaries, risks, and decision-record-worthy choices are settled.\",\n             \"Obtain the operator's explicit approval of the PRD/requirements (Spec Freeze).\",\n             \"Write a machine-readable plan JSON with grill_with_docs.status=complete and spec_freeze.status=frozen.\",\n             \"Run shiki plan ingest --plan-file PLAN.json.\",\ndiff --git a/scripts/test_shiki_goal_loop.sh b/scripts/test_shiki_goal_loop.sh\nindex 753c7b516e10f0be3bc92a4bd0eb1fa699776ae6..8f3820a0a079b3044424cfe1a6c6b7fe68896e67\n--- a/scripts/test_shiki_goal_loop.sh\n+++ b/scripts/test_shiki_goal_loop.sh\n@@ -231,8 +231,8 @@\n \n # High/critical risk merges autonomously when all required checks are green:\n # the \"MergeGate policy check\" required check IS the Guardian gate (it enforces\n-# guardian-policy.json \u2014 human approval OR external AI guardian review, ADR\n-# 0010), so green checks mean a recorded authority approved. The fake gh returns\n+# guardian-policy.json \u2014 human approval OR external AI guardian review,\n+# SADR-0010), so green checks mean a recorded authority approved. The fake gh returns\n # green checks, simulating that the Guardian gate passed.\n python3 \"$ROOT/scripts/shiki.py\" goal create --target \"$TARGET\" --title \"Guardian gate\" --outcome \"High risk merges via policy gate\" >/tmp/shiki-goal-loop-guardian-goal.json\n GGOAL=\"$(json_get /tmp/shiki-goal-loop-guardian-goal.json goal_id)\"\ndiff --git a/tests/test_adr_references.py b/tests/test_adr_references.py\nindex 51706329c797d5dd9b47e082a333fa83b261d482..d005da4f771f5e8085ec22d4b787980e68f5ae8c\n--- a/tests/test_adr_references.py\n+++ b/tests/test_adr_references.py\n@@ -1,8 +1,8 @@\n-\"\"\"Every ``ADR ####`` citation must resolve to exactly one decision record.\n+\"\"\"Every target ADR and Shiki SADR citation resolves in its own namespace.\n \n Background. ``scripts/mergegate_check.py`` justifies ``is_bookkeeping_closeout``\n-with \"ADR 0017\" nine times and ``scripts/guardian_approval_signal.py`` cites\n-0016/0017 seven times, yet for a stretch the numbering in ``docs/adr/`` stopped\n+with \"SADR-0017\" nine times and ``scripts/guardian_approval_signal.py`` cites\n+SADR-0016/SADR-0017 seven times, yet for a stretch the numbering in ``docs/adr/`` stopped\n at 0015: the authoring commits lived on a coordinator branch and no task ever\n declared ``docs/adr/...`` in its locks, so nothing carried the records to the\n default branch. The stated basis of an exemption that merged seven PRs with zero\n@@ -11,11 +11,12 @@\n and the directory drifted with no signal.\n \n This is the same unbound-pair shape behind other silent failures: a reference\n-and its target with nothing asserting they agree. This test binds them. It scans\n-every ``scripts/*.py`` (plus ``docs/**/*.md`` and the root ``*.md`` governance\n-files, which are cheap) for the ``ADR ####`` pattern and asserts each cited\n-number resolves to exactly one ``docs/adr/####-*.md``. The converse is\n-deliberately NOT required: an ADR may exist without being cited.\n+and its target with nothing asserting they agree. This test binds them. Target\n+``ADR NNNN`` citations resolve only to ``docs/adr/NNNN-*.md``; Shiki\n+``SADR-NNNN`` citations resolve only to ``docs/adr/SADR-NNNN-*.md``. The scan\n+covers current governance docs, scripts, workflows, prompts, packaging metadata,\n+and repo-local Codex guidance while deliberately excluding historical\n+``.shiki`` evidence. The converse is not required: a record may be uncited.\n \n The suite is dependency free: standard-library ``unittest`` only.\n \"\"\"\n@@ -31,17 +32,18 @@\n \n REPO_ROOT = Path(__file__).resolve().parents[1]\n \n-# \"ADR 0016\" -- a four-digit citation. The trailing \\b keeps a five-digit token\n-# from being read as a truncated four-digit number.\n-_CITATION = re.compile(r\"\\bADR (\\d{4})\\b\")\n-# \"0016-locks-bound-product-paths.md\" -- the leading four digits name the record.\n-_ADR_FILENAME = re.compile(r\"^(\\d{4})-.*\\.md$\")\n+# ``ADR 0016`` / ``SADR-0016`` -- a namespace plus four-digit citation. The\n+# trailing ``\\b`` keeps a five-digit token from being read as a truncated one.\n+_CITATION = re.compile(r\"\\b(S?ADR)[ -](\\d{4})\\b\")\n+# Target ADRs retain the numeric filename; Shiki platform decisions use SADR.\n+_DECISION_FILENAME = re.compile(r\"^(?:(SADR)-)?(\\d{4})-.*\\.md$\")\n \n \n class Violation(NamedTuple):\n     \"\"\"A citation that does not resolve to exactly one decision record.\"\"\"\n \n     citing_file: str  # repo-relative path of the file that cites the ADR\n+    namespace: str  # \"ADR\" for target decisions; \"SADR\" for Shiki decisions\n     number: str  # the cited four-digit number, e.g. \"0016\"\n     line: int  # 1-based line of the citation\n     reason: str  # \"missing\" (zero matches) or \"ambiguous\" (two or more)\n@@ -49,44 +51,59 @@\n \n     @property\n     def expected_path(self) -> str:\n-        return f\"docs/adr/{self.number}-*.md\"\n+        prefix = \"SADR-\" if self.namespace == \"SADR\" else \"\"\n+        return f\"docs/adr/{prefix}{self.number}-*.md\"\n \n     def describe(self) -> str:\n         \"\"\"Name the citing file, the cited number, and the missing path.\"\"\"\n         if self.reason == \"missing\":\n             return (\n-                f\"{self.citing_file}:{self.line} cites ADR {self.number} \"\n+                f\"{self.citing_file}:{self.line} cites {self.namespace} {self.number} \"\n                 f\"but no document matches {self.expected_path}\"\n             )\n         joined = \", \".join(self.matches)\n         return (\n-            f\"{self.citing_file}:{self.line} cites ADR {self.number} which \"\n+            f\"{self.citing_file}:{self.line} cites {self.namespace} {self.number} which \"\n             f\"resolves to {len(self.matches)} documents (expected exactly one \"\n             f\"at {self.expected_path}): {joined}\"\n         )\n \n \n-def _adr_index(root: Path) -> dict[str, list[Path]]:\n-    \"\"\"Map every four-digit ADR number to the record files that claim it.\"\"\"\n-    index: dict[str, list[Path]] = {}\n+def _adr_index(root: Path) -> dict[tuple[str, str], list[Path]]:\n+    \"\"\"Map each decision namespace and number to the files that claim it.\"\"\"\n+    index: dict[tuple[str, str], list[Path]] = {}\n     adr_dir = root / \"docs\" / \"adr\"\n     if adr_dir.is_dir():\n         for path in sorted(adr_dir.glob(\"*.md\")):\n-            match = _ADR_FILENAME.match(path.name)\n+            match = _DECISION_FILENAME.match(path.name)\n             if match:\n-                index.setdefault(match.group(1), []).append(path)\n+                namespace = \"SADR\" if match.group(1) else \"ADR\"\n+                index.setdefault((namespace, match.group(2)), []).append(path)\n     return index\n \n \n def _citing_files(root: Path, include_docs: bool = True) -> list[Path]:\n-    \"\"\"Files scanned for citations: scripts/*.py, and cheaply the markdown.\"\"\"\n+    \"\"\"Current, shipped text surfaces scanned for decision citations.\"\"\"\n     files = sorted((root / \"scripts\").glob(\"*.py\"))\n+    files += sorted((root / \"scripts\").glob(\"*.sh\"))\n     if include_docs:\n         docs = root / \"docs\"\n         if docs.is_dir():\n             files += sorted(docs.rglob(\"*.md\"))\n         files += sorted(root.glob(\"*.md\"))\n-    return files\n+        pyproject = root / \"pyproject.toml\"\n+        if pyproject.is_file():\n+            files.append(pyproject)\n+        for relative in (\".github\", \".codex\"):\n+            surface = root / relative\n+            if surface.is_dir():\n+                files += sorted(\n+                    path\n+                    for path in surface.rglob(\"*\")\n+                    if path.is_file()\n+                    and path.suffix in {\".md\", \".py\", \".sh\", \".toml\", \".yaml\", \".yml\"}\n+                )\n+    return sorted(set(files))\n \n \n def find_adr_reference_violations(\n@@ -94,10 +111,9 @@\n ) -> list[Violation]:\n     \"\"\"Return every ``ADR ####`` citation under ``root`` that fails to resolve.\n \n-    A citation resolves when exactly one ``docs/adr/####-*.md`` matches its\n-    number. Zero matches is a \"missing\" violation; two or more is \"ambiguous\".\n-    Records that are never cited produce nothing -- the converse is not\n-    required.\n+    A citation resolves when exactly one record in the same namespace matches\n+    its number. Zero matches is a \"missing\" violation; two or more is\n+    \"ambiguous\". Records that are never cited produce nothing.\n     \"\"\"\n     root = Path(root)\n     index = _adr_index(root)\n@@ -110,13 +126,15 @@\n         rel = path.relative_to(root).as_posix()\n         for lineno, line in enumerate(text.splitlines(), start=1):\n             for match in _CITATION.finditer(line):\n-                number = match.group(1)\n-                found = index.get(number, [])\n+                namespace, number = match.groups()\n+                found = index.get((namespace, number), [])\n                 if len(found) == 1:\n                     continue\n                 reason = \"missing\" if not found else \"ambiguous\"\n                 rels = [p.relative_to(root).as_posix() for p in found]\n-                violations.append(Violation(rel, number, lineno, reason, rels))\n+                violations.append(\n+                    Violation(rel, namespace, number, lineno, reason, rels)\n+                )\n     return violations\n \n \n@@ -129,8 +147,8 @@\n         self.assertEqual(\n             violations,\n             [],\n-            \"Every 'ADR ####' citation must resolve to exactly one \"\n-            f\"docs/adr/####-*.md:\\n{detail}\",\n+            \"Every ADR/SADR citation must resolve exactly once in its own \"\n+            f\"docs/adr namespace:\\n{detail}\",\n         )\n \n     def test_scan_actually_reads_scripts(self) -> None:\n@@ -142,12 +160,38 @@\n             \"the citation scan must cover scripts/*.py\",\n         )\n \n+    def test_platform_citations_do_not_use_unresolved_numeric_shorthand(self) -> None:\n+        shorthand = re.compile(r\"SADR-\\d{4}/\\d{4}\")\n+        offenders = []\n+        for path in _citing_files(REPO_ROOT):\n+            text = path.read_text(encoding=\"utf-8\")\n+            if shorthand.search(text):\n+                offenders.append(path.relative_to(REPO_ROOT).as_posix())\n+        self.assertEqual(\n+            offenders,\n+            [],\n+            \"repeat the SADR prefix so every cited number is independently resolvable\",\n+        )\n+\n     def test_carried_records_are_indexed(self) -> None:\n         # The two records this Goal carried onto the default branch, and which\n         # the scripts cite most, must be present.\n         index = _adr_index(REPO_ROOT)\n-        self.assertIn(\"0016\", index)\n-        self.assertIn(\"0017\", index)\n+        self.assertIn((\"SADR\", \"0016\"), index)\n+        self.assertIn((\"SADR\", \"0017\"), index)\n+\n+    def test_platform_tree_has_only_sadr_0001_through_0019(self) -> None:\n+        adr_dir = REPO_ROOT / \"docs\" / \"adr\"\n+        sadrs = sorted(adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\"))\n+        self.assertEqual(\n+            {path.name.split(\"-\", 2)[1] for path in sadrs},\n+            {f\"{number:04d}\" for number in range(1, 20)},\n+        )\n+        self.assertEqual(\n+            list(adr_dir.glob(\"[0-9][0-9][0-9][0-9]-*.md\")),\n+            [],\n+            \"the Shiki platform tree must not retain legacy numeric SADR paths\",\n+        )\n \n \n class FixtureAdrReferences(unittest.TestCase):\n@@ -180,6 +224,26 @@\n         self.assertIn(\"9991\", message)  # cited number\n         self.assertIn(\"docs/adr/9991-*.md\", message)  # missing path\n \n+    def test_target_adr_does_not_satisfy_missing_platform_sadr(self) -> None:\n+        root = self._make_repo(\n+            adr_names=[\"0042-target-decision.md\"],\n+            script_body='\"\"\"Target ADR 0042 exists, but platform SADR-0042 does not.\"\"\"\\n',\n+        )\n+        violations = find_adr_reference_violations(root, include_docs=False)\n+        self.assertEqual(len(violations), 1)\n+        self.assertEqual(violations[0].namespace, \"SADR\")\n+        self.assertEqual(violations[0].number, \"0042\")\n+        self.assertEqual(violations[0].expected_path, \"docs/adr/SADR-0042-*.md\")\n+\n+    def test_target_adr_and_platform_sadr_may_reuse_a_number(self) -> None:\n+        root = self._make_repo(\n+            adr_names=[\"0042-target-decision.md\", \"SADR-0042-platform-decision.md\"],\n+            script_body='\"\"\"Target ADR 0042 and platform SADR-0042 are distinct.\"\"\"\\n',\n+        )\n+        self.assertEqual(\n+            find_adr_reference_violations(root, include_docs=False), []\n+        )\n+\n     def test_uncited_adr_is_allowed(self) -> None:\n         # An ADR exists but nothing cites it -> not a violation. The converse\n         # (every ADR must be cited) is explicitly NOT required.\ndiff --git a/tests/test_guardian_review.py b/tests/test_guardian_review.py\nindex 88829e7810212635e2fc98aa5918d6f26294677d..00a85a237acd5bf2d6cb5b294128198c422402ca\n--- a/tests/test_guardian_review.py\n+++ b/tests/test_guardian_review.py\n@@ -162,7 +162,7 @@\n \n     def test_docs_only_is_light_and_not_conservative(self):\n         focus = gr.classify_review_focus([\"docs/adr/0099-x.md\", \"README.md\"], \"\", \"low\")\n-        self.assertTrue(any(\"Docs/ADR-only\" in f for f in focus))\n+        self.assertTrue(any(\"Docs/decision-record-only\" in f for f in focus))\n         self.assertFalse(any(\"conservative default\" in f.lower() for f in focus))\n \n     def test_unknown_category_is_conservative(self):\ndiff --git a/tests/test_policy_job_merged_prs.py b/tests/test_policy_job_merged_prs.py\nindex d45512e74681e5e649279236c7d4e79d5c3da471..b97989f8bd74d091de1a6b5275a20d8dc68c4512\n--- a/tests/test_policy_job_merged_prs.py\n+++ b/tests/test_policy_job_merged_prs.py\n@@ -3,8 +3,8 @@\n \n ``.github/workflows/shiki-cca-completion.yml`` has two consumers of the merged-PR\n proof: the CCA *signal* (``guardian_approval_signal.py`` in \"Prepare CCA context\")\n-and the authoritative gate (``mergegate_check.py`` in \"Enforce MergeGate\"). ADR\n-0017's bookkeeping-closeout exemption (condition 6) needs the merged implementation\n+and the authoritative gate (``mergegate_check.py`` in \"Enforce MergeGate\").\n+SADR-0017's bookkeeping-closeout exemption (condition 6) needs the merged implementation\n PR proven, and a closeout carries NO ``post_merge_reconcile`` marker. The signal\n already resolves ``MERGED_PRS`` unconditionally, but \"Enforce MergeGate\" used to\n resolve it only inside ``grep -q '<!-- shiki:post_merge_reconcile -->'``. So a\ndiff --git a/tests/test_sadr_install_migration.py b/tests/test_sadr_install_migration.py\nnew file mode 100644\nindex 0000000000000000000000000000000000000000..d51eac872b1c79ff4579c4c1401ca5d775a60d8d\n--- /dev/null\n+++ b/tests/test_sadr_install_migration.py\n@@ -0,0 +1,237 @@\n+\"\"\"SADR installation and collision-safe legacy upgrade behavior.\"\"\"\n+\n+from __future__ import annotations\n+\n+import contextlib\n+import hashlib\n+import io\n+import json\n+import shutil\n+import tempfile\n+import unittest\n+from pathlib import Path\n+\n+import shiki_test_support  # noqa: F401  (path bootstrap)\n+\n+import shiki_installer\n+from tests.test_adr_references import find_adr_reference_violations\n+\n+\n+PRODUCT_NUMBERS = tuple(range(13, 19))\n+\n+\n+def _quiet_install(target: Path, *, force: bool, validate: bool) -> None:\n+    with contextlib.redirect_stdout(io.StringIO()):\n+        shiki_installer.install_template(target, force=force, validate=validate)\n+\n+\n+def _sha256(path: Path) -> str:\n+    return hashlib.sha256(path.read_bytes()).hexdigest()\n+\n+\n+def _legacy_sadr_paths() -> tuple[str, ...]:\n+    return tuple(\n+        f\"docs/adr/{path.name.removeprefix('SADR-')}\"\n+        for path in sorted((shiki_installer.ROOT / \"docs\" / \"adr\").glob(\"SADR-*.md\"))\n+        if path.name != \"SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\"\n+    )\n+\n+\n+def _create_legacy_upgrade_fixture(\n+    target: Path,\n+) -> tuple[dict[str, bytes], tuple[str, ...]]:\n+    adr_dir = target / \"docs\" / \"adr\"\n+    adr_dir.mkdir(parents=True)\n+    product_bytes = {\n+        f\"{number:04d}-cubeshare-product-decision.md\": (\n+            f\"# CubeShare ADR {number:04d}\\n\\nDo not rewrite {number}.\\n\"\n+        ).encode()\n+        for number in PRODUCT_NUMBERS\n+    }\n+    for name, content in product_bytes.items():\n+        (adr_dir / name).write_bytes(content)\n+\n+    legacy_paths = _legacy_sadr_paths()\n+    for relative in legacy_paths:\n+        (target / relative).write_bytes(f\"legacy Shiki bytes for {relative}\\n\".encode())\n+\n+    stamp_path = target / shiki_installer.INSTALL_STAMP_PATH\n+    stamp_path.parent.mkdir(parents=True, exist_ok=True)\n+    stamp = {\n+        \"version\": shiki_installer.INSTALL_STAMP_VERSION,\n+        \"platform_commit\": None,\n+        \"installed_at\": \"2000-01-01T00:00:00+00:00\",\n+        \"digests\": {\n+            relative: _sha256(target / relative) for relative in legacy_paths\n+        },\n+    }\n+    stamp_path.write_text(\n+        json.dumps(stamp, indent=2, sort_keys=True) + \"\\n\", encoding=\"utf-8\"\n+    )\n+    return product_bytes, legacy_paths\n+\n+\n+def _tree_snapshot(root: Path) -> dict[str, tuple[str, bytes | str]]:\n+    \"\"\"Recursive byte/shape snapshot used to prove preflight is write-free.\"\"\"\n+    snapshot: dict[str, tuple[str, bytes | str]] = {}\n+    for path in sorted(root.rglob(\"*\")):\n+        relative = path.relative_to(root).as_posix()\n+        if path.is_symlink():\n+            snapshot[relative] = (\"symlink\", path.readlink().as_posix())\n+        elif path.is_dir():\n+            snapshot[relative] = (\"directory\", \"\")\n+        else:\n+            snapshot[relative] = (\"file\", path.read_bytes())\n+    return snapshot\n+\n+\n+class FreshSadrInstallTests(unittest.TestCase):\n+    def setUp(self) -> None:\n+        self.tmp = Path(tempfile.mkdtemp(prefix=\"shiki-sadr-fresh-\"))\n+        self.addCleanup(lambda: shutil.rmtree(self.tmp, ignore_errors=True))\n+        self.target = self.tmp / \"target\"\n+        self.adr_dir = self.target / \"docs\" / \"adr\"\n+        self.adr_dir.mkdir(parents=True)\n+        self.product_bytes = {\n+            f\"{number:04d}-product-decision.md\": (\n+                f\"# Product ADR {number:04d}\\n\\nTarget-owned bytes {number}.\\n\"\n+            ).encode()\n+            for number in PRODUCT_NUMBERS\n+        }\n+        for name, content in self.product_bytes.items():\n+            (self.adr_dir / name).write_bytes(content)\n+\n+    def test_fresh_install_preserves_product_adrs_and_installs_all_sadrs(self) -> None:\n+        _quiet_install(self.target, force=False, validate=True)\n+\n+        for name, expected in self.product_bytes.items():\n+            self.assertEqual((self.adr_dir / name).read_bytes(), expected)\n+\n+        sadr_numbers = {\n+            path.name.split(\"-\", 2)[1]\n+            for path in self.adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\")\n+        }\n+        self.assertEqual(sadr_numbers, {f\"{number:04d}\" for number in range(1, 20)})\n+\n+        numeric_records = {\n+            path.name\n+            for path in self.adr_dir.glob(\"[0-9][0-9][0-9][0-9]-*.md\")\n+        }\n+        self.assertEqual(numeric_records, set(self.product_bytes))\n+        stamp = json.loads(\n+            (self.target / shiki_installer.INSTALL_STAMP_PATH).read_text(\n+                encoding=\"utf-8\"\n+            )\n+        )\n+        for path in self.adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\"):\n+            relative = path.relative_to(self.target).as_posix()\n+            self.assertEqual(stamp[\"digests\"][relative], _sha256(path))\n+        self.assertEqual(find_adr_reference_violations(self.target), [])\n+\n+\n+class LegacySadrUpgradeTests(unittest.TestCase):\n+    def setUp(self) -> None:\n+        self.tmp = Path(tempfile.mkdtemp(prefix=\"shiki-sadr-upgrade-\"))\n+        self.addCleanup(lambda: shutil.rmtree(self.tmp, ignore_errors=True))\n+        self.target = self.tmp / \"target\"\n+        self.adr_dir = self.target / \"docs\" / \"adr\"\n+        self.product_bytes, self.legacy_paths = _create_legacy_upgrade_fixture(\n+            self.target\n+        )\n+\n+    def test_force_upgrade_deletes_only_stamped_legacy_sadrs(self) -> None:\n+        _quiet_install(self.target, force=True, validate=True)\n+\n+        for relative in self.legacy_paths:\n+            self.assertFalse((self.target / relative).exists(), relative)\n+        for name, expected in self.product_bytes.items():\n+            self.assertEqual((self.adr_dir / name).read_bytes(), expected)\n+\n+        sadrs = list(self.adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\"))\n+        self.assertEqual(len(sadrs), 19)\n+        refreshed = json.loads(\n+            (self.target / shiki_installer.INSTALL_STAMP_PATH).read_text(encoding=\"utf-8\")\n+        )\n+        for relative in self.legacy_paths:\n+            self.assertNotIn(relative, refreshed[\"digests\"])\n+        for path in sadrs:\n+            relative = path.relative_to(self.target).as_posix()\n+            self.assertEqual(refreshed[\"digests\"][relative], _sha256(path))\n+\n+    def test_non_force_install_never_deletes_legacy_files(self) -> None:\n+        before = {\n+            relative: (self.target / relative).read_bytes()\n+            for relative in self.legacy_paths\n+        }\n+        _quiet_install(self.target, force=False, validate=False)\n+        for relative, expected in before.items():\n+            self.assertEqual((self.target / relative).read_bytes(), expected)\n+        refreshed = json.loads(\n+            (self.target / shiki_installer.INSTALL_STAMP_PATH).read_text(encoding=\"utf-8\")\n+        )\n+        for relative in self.legacy_paths:\n+            self.assertEqual(refreshed[\"digests\"][relative], _sha256(self.target / relative))\n+\n+\n+class LegacyCleanupPreflightTests(unittest.TestCase):\n+    def setUp(self) -> None:\n+        self.tmp = Path(tempfile.mkdtemp(prefix=\"shiki-sadr-preflight-\"))\n+        self.addCleanup(lambda: shutil.rmtree(self.tmp, ignore_errors=True))\n+\n+    def test_all_ownership_blockers_abort_before_any_write(self) -> None:\n+        cases = (\n+            (\"absent\", \"install stamp is absent\"),\n+            (\"unreadable\", \"install stamp is unreadable\"),\n+            (\"malformed\", \"install stamp is malformed\"),\n+            (\"missing-digest\", \"install stamp has no digest\"),\n+            (\"digest-mismatch\", \"digest mismatch\"),\n+        )\n+        for name, expected_reason in cases:\n+            with self.subTest(name=name):\n+                target = self.tmp / name\n+                _, legacy_paths = _create_legacy_upgrade_fixture(target)\n+                stamp_path = target / shiki_installer.INSTALL_STAMP_PATH\n+\n+                if name == \"absent\":\n+                    stamp_path.unlink()\n+                elif name == \"unreadable\":\n+                    stamp_path.unlink()\n+                    stamp_path.mkdir()\n+                elif name == \"malformed\":\n+                    stamp_path.write_text(\"{not-json\\n\", encoding=\"utf-8\")\n+                else:\n+                    stamp = json.loads(stamp_path.read_text(encoding=\"utf-8\"))\n+                    if name == \"missing-digest\":\n+                        stamp[\"digests\"] = {}\n+                    else:\n+                        stamp[\"digests\"] = {\n+                            relative: \"deadbeef\" for relative in legacy_paths\n+                        }\n+                    stamp_path.write_text(\n+                        json.dumps(stamp, indent=2, sort_keys=True) + \"\\n\",\n+                        encoding=\"utf-8\",\n+                    )\n+\n+                before = _tree_snapshot(target)\n+                with self.assertRaises(shiki_installer.ShikiError) as raised:\n+                    _quiet_install(target, force=True, validate=False)\n+                after = _tree_snapshot(target)\n+\n+                self.assertEqual(before, after, f\"{name} preflight changed the tree\")\n+                message = str(raised.exception)\n+                self.assertIn(expected_reason, message)\n+                self.assertIn(\"No files were written\", message)\n+                for relative in legacy_paths:\n+                    self.assertIn(relative, message)\n+\n+    def test_absent_stamp_is_allowed_when_no_legacy_path_exists(self) -> None:\n+        target = self.tmp / \"no-legacy\"\n+        target.mkdir()\n+        _quiet_install(target, force=True, validate=False)\n+        self.assertTrue(\n+            (target / \"docs\" / \"adr\" / \"SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\").is_file()\n+        )\n+\n+\n+if __name__ == \"__main__\":\n+    unittest.main()\n\n succeeded in 9799ms:\nShiki validation passed\nShiki validation passed\nShiki validation passed\nshiki control-plane tests passed\n\nexec\n/bin/zsh -lc 'bash scripts/test_shiki_adversarial_state.sh' in /private/tmp/shiki-adr-collision.pa0KwY/.worktrees/shiki-t-20260807t073307221254z-2d22d654-introduc\ndiff --git a/.codex/skills/shiki/SKILL.md b/.codex/skills/shiki/SKILL.md\nindex c8e2e85d9d75d49cc4f2d6286ee22659c4e53e75..93cded623b2c15d9f65c76491a1c2d813ac9e6c0\n--- a/.codex/skills/shiki/SKILL.md\n+++ b/.codex/skills/shiki/SKILL.md\n@@ -18,6 +18,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n Codex CLI does not register this skill as a custom `/shiki` slash command. When\n the user types `/shiki` in Codex CLI and gets \"Unrecognized command\", explain\n that this is expected: invoke Shiki in Codex with natural language such as\n@@ -69,7 +72,7 @@\n \n ## Responsibilities\n \n-- Claude Code implements and repairs by default (`shiki runner claude`, ADR 0008); Codex implements and repairs tasks explicitly assigned to `codex`.\n+- Claude Code implements and repairs by default (`shiki runner claude`, SADR-0008); Codex implements and repairs tasks explicitly assigned to `codex`.\n - Claude Code Action can act as GitHub-side CCA or reviewer.\n - CCA judges completion.\n - MergeGate authorizes state transitions and merge readiness.\ndiff --git a/.github/prompts/cca-completion-check.md b/.github/prompts/cca-completion-check.md\nindex ed203ad5f302ecc81372d8ab27d2b1232ca97497..fd89950528333ef6242e6d5443904f5392ab7e56\n--- a/.github/prompts/cca-completion-check.md\n+++ b/.github/prompts/cca-completion-check.md\n@@ -19,6 +19,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n ## Required Reading\n \n Read only the files needed to judge the PR contract. Prefer this order:\ndiff --git a/.github/prompts/codex-task-handoff.md b/.github/prompts/codex-task-handoff.md\nindex 695e211d76dea1c60e8d5bb360e78487808286a4..59441d576b01a39569722f226c854e5a740e9eb5\n--- a/.github/prompts/codex-task-handoff.md\n+++ b/.github/prompts/codex-task-handoff.md\n@@ -2,7 +2,7 @@\n \n You are Codex Front acting as the Implementer for a Shiki task.\n \n-Follow `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant ADRs, and `docs/agents/codex-handoff.md`.\n+Follow `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant Shiki SADRs and target ADRs, and `docs/agents/codex-handoff.md`.\n \n Rules:\n \ndiff --git a/AGENTS.md b/AGENTS.md\nindex 7f041100d49c2a7525f51cb0f5d696a4a6d1c78c..a06bfbabc6fa48a007389e25e12d7ed523936bed\n--- a/AGENTS.md\n+++ b/AGENTS.md\n@@ -22,7 +22,7 @@\n - **Codex Front** is an optional implementer through Codex App, Codex CLI, Codex IDE extension, or Codex Web signed in with ChatGPT OAuth/subscription auth, dispatched with `shiki runner codex` when a task is explicitly assigned to `codex`.\n - **GitHub CCA** is implemented by Claude Code Action by default, using `CLAUDE_CODE_OAUTH_TOKEN`.\n \n-Do not assume `openai/codex-action`, `OPENAI_API_KEY`, or API-key based Codex automation in the default Shiki path. API-key runners are explicit target-repo extensions and require their own ADR.\n+Do not assume `openai/codex-action`, `OPENAI_API_KEY`, or API-key based Codex automation in the default Shiki path. API-key runners are explicit target-repo extensions and require their own target ADR.\n \n Shiki is not:\n \n@@ -44,10 +44,14 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use\n+`SADR-NNNN-*.md`; target product decisions retain `NNNN-*.md` and the `ADR`\n+namespace.\n+\n Use the canonical source-of-truth order above. Runtime-specific reading orders\n are operational guidance only and must not override or reorder it.\n \n-Conversation state is not durable truth. If a decision matters, put it in GitHub, `.shiki/`, `CONTEXT.md`, or an ADR.\n+Conversation state is not durable truth. If a decision matters, put it in GitHub, `.shiki/`, `CONTEXT.md`, a Shiki SADR, or a target ADR.\n \n If GitHub and `.shiki/` disagree, surface the conflict and prefer GitHub operational state until the mirror is repaired.\n \n@@ -75,8 +79,8 @@\n Every non-trivial change follows this loop:\n \n 1. **Goal**: clarify outcome, completion conditions, non-goals, risk level, and success signals.\n-2. **grill-with-docs**: challenge the plan against domain docs, ADRs, code reality, terminology, and edge scenarios.\n-3. **Context & Impact**: find relevant docs, ADRs, code, dependencies, owners, locks, and verification surfaces.\n+2. **grill-with-docs**: challenge the plan against domain docs, Shiki SADRs, target ADRs, code reality, terminology, and edge scenarios.\n+3. **Context & Impact**: find relevant docs, SADRs, target ADRs, code, dependencies, owners, locks, and verification surfaces.\n 4. **PRD and Spec Freeze**: use `to-prd` when context is settled enough to become durable product/engineering intent. The operator's explicit approval of the PRD is Spec Freeze: the plan records a `spec_freeze` block, and no plan runs without it. Steps 1-4 form Requirements Definition \u2014 one continuous operator dialogue. After Spec Freeze, scope changes require an operator-approved Spec Amendment; non-scope-moving interpretations go to the Assumption Log.\n 5. **Issues**: use `to-issues` to create independently grabbable vertical-slice issues. Prefer AFK slices over HITL slices where possible.\n 6. **Triage**: label issues for readiness, risk, runtime, skills, and MergeGate state.\n@@ -167,7 +171,7 @@\n - Testing decisions.\n - Out-of-scope items.\n - Further notes.\n-- Links to relevant domain terms and ADRs.\n+- Links to relevant domain terms, SADRs, and target ADRs.\n \n Every executable task must include:\n \n@@ -193,7 +197,7 @@\n \n Context & Impact must identify:\n \n-- Relevant documents, domain terms, ADRs, and past decisions.\n+- Relevant documents, domain terms, SADRs, target ADRs, and past decisions.\n - Relevant modules, interfaces, seams, callers, owners, and tests.\n - Dependency relationships between tasks.\n - Candidate locks and lock conflicts.\n@@ -210,7 +214,7 @@\n | Skill | Required when |\n | --- | --- |\n | `setup-matt-pocock-skills` | First configuring the repo for skills, or when issue tracker, triage labels, or domain docs are missing. |\n-| `grill-with-docs` | Default for non-trivial Goals before PRD/issues; always required when plans, terminology, boundaries, tradeoffs, or ADR-worthy decisions are ambiguous. |\n+| `grill-with-docs` | Default for non-trivial Goals before PRD/issues; always required when plans, terminology, boundaries, tradeoffs, or decision-record-worthy choices are ambiguous. |\n | `zoom-out` | The agent lacks an architectural map of the relevant code area. |\n | `to-prd` | Settled Goal context must become durable product/engineering intent. |\n | `to-issues` | A Goal, PRD, or plan must become independently grabbable vertical-slice issues. |\n@@ -348,7 +352,7 @@\n - Three or more new files.\n - Any high-risk or critical label.\n \n-When the architecture gate triggers, run `grill-with-docs` or `improve-codebase-architecture` as appropriate and record durable decisions in an ADR when needed.\n+When the architecture gate triggers, run `grill-with-docs` or `improve-codebase-architecture` as appropriate and record durable decisions in a Shiki SADR or target ADR as applicable.\n \n ## Engineering Discipline\n \ndiff --git a/CLAUDE.md b/CLAUDE.md\nindex 98a932e7841a632866fcd53b0bad0e3af3711d3d..98a31399404d6fb044b4fd493f4459ac87f439de\n--- a/CLAUDE.md\n+++ b/CLAUDE.md\n@@ -14,6 +14,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n ## Claude Role\n \n Default to planner, default implementer, reviewer, coordinator, documentation editor, CCA-style completion judge, and final-judgment assistant.\n@@ -57,7 +60,7 @@\n At the start of a Shiki session:\n \n 1. Read `AGENTS.md`.\n-2. Read `CONTEXT.md` and relevant ADRs.\n+2. Read `CONTEXT.md` and relevant Shiki SADRs and target ADRs.\n 3. Read active `.shiki/` Goal/task/ledger state when present.\n 4. Read active distilled rules from `.shiki/memories/` (status `distilled`, `active: true`, not revoked or superseded) for applicable guidance; the same rules are injected deterministically into task handoffs as the `## Distilled Rules` section (\u00a73.5).\n 5. Read the GitHub Issue or PR when the task is GitHub-backed.\n@@ -74,7 +77,7 @@\n - Define the Goal, completion conditions, non-goals, and risk level.\n - Treat `grill-with-docs` as the default planning entry point for non-trivial Goals.\n - Challenge domain terms against `CONTEXT.md`.\n-- Challenge hard-to-reverse tradeoffs against ADRs.\n+- Challenge hard-to-reverse tradeoffs against SADRs and target ADRs.\n - Explore code instead of asking when code can answer the question.\n - Run Context & Impact before implementation planning.\n - Produce a Task DAG, not an unordered checklist.\n@@ -94,7 +97,7 @@\n - For each question, provide your recommended answer.\n - Resolve dependency decisions before downstream design questions.\n - Update `CONTEXT.md` inline when terminology crystallizes.\n-- Offer ADRs only when the decision is hard to reverse, surprising without context, and a real tradeoff.\n+- Offer a SADR or target ADR only when the decision is hard to reverse, surprising without context, and a real tradeoff.\n - Convert unresolved questions into HITL blockers rather than allowing Codex to guess.\n \n ## PRD and Issues Standard\n@@ -103,7 +106,7 @@\n \n - Use the project's domain glossary vocabulary.\n - Avoid volatile file paths unless they are essential.\n-- Include implementation decisions, testing decisions, out-of-scope items, and relevant ADR/domain links.\n+- Include implementation decisions, testing decisions, out-of-scope items, and relevant SADR/target-ADR/domain links.\n - Identify deep-module opportunities and testable interfaces.\n \n When creating issues:\n@@ -121,7 +124,7 @@\n - Branch/worktree target.\n - Scope and non-goals.\n - Dependencies and locks.\n-- Relevant docs, ADRs, modules, and tests.\n+- Relevant docs, SADRs, target ADRs, modules, and tests.\n - Required skill invocations.\n - TDD expectations.\n - Pre-PR `code-review` gate expectations.\n@@ -152,7 +155,7 @@\n \n - Treat yourself as a judge, not implementer.\n - Do not write production code.\n-- Read `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant ADRs, PRD, task issue, PR body, diff, checks, reviews, labels, and `.shiki/` state.\n+- Read `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant SADRs and target ADRs, PRD, task issue, PR body, diff, checks, reviews, labels, and `.shiki/` state.\n - Evaluate every applicable checklist in `docs/agents/checklists.md`.\n - Map each acceptance criterion to durable evidence.\n - Distinguish implementation failure from missing evidence.\ndiff --git a/CONTEXT.md b/CONTEXT.md\nindex e61a9be06a0cce0d026484083a657bde2067e234..237ee8aa3ea8fab334ece49768bc5ed3b5af0974\n--- a/CONTEXT.md\n+++ b/CONTEXT.md\n@@ -25,7 +25,7 @@\n _Avoid_: jumping directly into code\n \n **grill-with-docs**:\n-A planning skill that challenges a plan against domain language, ADRs, code reality, and concrete edge scenarios. It resolves design-tree questions before PRD/issues. It runs inside Requirements Definition.\n+A planning skill that challenges a plan against domain language, Shiki SADRs, target ADRs, code reality, and concrete edge scenarios. It resolves design-tree questions before PRD/issues. It runs inside Requirements Definition.\n _Avoid_: generic brainstorming, silent assumptions\n \n **Requirements Definition**:\ndiff --git a/CONTRIBUTING.md b/CONTRIBUTING.md\nindex c6d9b656e73abb8a55e5f660123e03153dcb3e6d..9e37a8fbf668db5b22461aaae5548b8f1354394c\n--- a/CONTRIBUTING.md\n+++ b/CONTRIBUTING.md\n@@ -17,7 +17,7 @@\n 1. **Goal** \u2014 start from a user-approved Goal with an outcome, completion\n    conditions, non-goals, risk level, and success signals.\n 2. **grill-with-docs** \u2014 for any non-trivial change, challenge the plan against\n-   domain language, ADRs, and code reality before writing a PRD.\n+   domain language, Shiki SADRs, target ADRs, and code reality before writing a PRD.\n 3. **PRD** \u2014 capture settled product/engineering intent (`to-prd`).\n 4. **Spec Freeze** \u2014 the operator explicitly approves the PRD/requirements;\n    the plan records a `spec_freeze` block and no plan runs without it\n@@ -94,7 +94,7 @@\n   entries.\n - Do not hand-edit `.shiki/goals/*.json` or `.shiki/manifest.json` in a task PR.\n - Conversation state is not durable truth. If a decision matters, record it in\n-  GitHub, `.shiki/`, `CONTEXT.md`, or an ADR.\n+  GitHub, `.shiki/`, `CONTEXT.md`, a Shiki SADR, or a target ADR.\n \n ## Architecture Gate\n \n@@ -103,7 +103,7 @@\n or CI policy, package structure, three or more structural files, more than 100\n net new lines in a core module, three or more new files, or any high-risk label.\n When triggered, run `grill-with-docs` or `improve-codebase-architecture` and\n-record durable decisions in an ADR when needed (see `docs/adr/`).\n+record durable decisions in a Shiki SADR or target ADR as applicable (see `docs/adr/`).\n \n ## Safety\n \ndiff --git a/SYSTEM_PROMPT.md b/SYSTEM_PROMPT.md\nindex aaaea45e3a896c3a44ba89eec8bb84325ecf6c62..da009c86a88285269dee0155e58e550c6d744ea9\n--- a/SYSTEM_PROMPT.md\n+++ b/SYSTEM_PROMPT.md\n@@ -30,6 +30,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n Use the canonical source-of-truth order above. Runtime-specific reading orders\n are operational guidance only and must not override or reorder it. The current\n conversation is non-durable operator input.\n@@ -48,14 +51,14 @@\n \n A runtime may only do work that is explicitly assigned by the Goal, task, PR, or MergeGate decision.\n \n-Do not assume `openai/codex-action`, `OPENAI_API_KEY`, or API-key based Codex automation in the default Shiki path. API-key runners are explicit target-repo extensions and require their own ADR.\n+Do not assume `openai/codex-action`, `OPENAI_API_KEY`, or API-key based Codex automation in the default Shiki path. API-key runners are explicit target-repo extensions and require their own target ADR.\n \n ## 4. Non-negotiable Shiki flow\n \n For any non-trivial Goal, use this flow:\n \n 1. **Goal Seek**: define outcome, non-goals, risk level, completion criteria, and evidence requirements.\n-2. **grill-with-docs**: challenge the plan against `CONTEXT.md`, ADRs, code reality, terminology, boundaries, and edge scenarios. Ask one question at a time when the operator is available. If code can answer a question, inspect code instead of asking.\n+2. **grill-with-docs**: challenge the plan against `CONTEXT.md`, Shiki SADRs, target ADRs, code reality, terminology, boundaries, and edge scenarios. Ask one question at a time when the operator is available. If code can answer a question, inspect code instead of asking.\n 3. **Context & Impact**: identify modules, interfaces, seams, callers, dependencies, locks, risk, and verification surfaces.\n 4. **to-prd + Spec Freeze**: turn settled context into a PRD using domain vocabulary and testing decisions. The operator's explicit approval of the PRD is Spec Freeze, recorded as the plan's `spec_freeze` block; plans without it do not run. Steps 1-4 form Requirements Definition.\n 5. **to-issues**: decompose the PRD into independently grabbable vertical-slice issues. Prefer AFK slices; mark HITL slices when judgment is still required.\n@@ -75,7 +78,7 @@\n Before making any material change:\n \n 1. Read `AGENTS.md` and any runtime-specific wrapper such as `CLAUDE.md`.\n-2. Read `CONTEXT.md` and relevant ADRs.\n+2. Read `CONTEXT.md` and relevant SADRs and target ADRs.\n 3. Read the GitHub Issue / PR and `.shiki/` state for the active Goal or task.\n 4. Identify your role: Planner, Implementer, Reviewer, Completion Check Agent, Repairer, or Guardian-assist.\n 5. Identify Goal id, task id, branch, PR, dependencies, locks, risk level, required skills, and required evidence.\n@@ -103,7 +106,7 @@\n \n At minimum, identify:\n \n-- Relevant documents, ADRs, domain terms, and prior decisions.\n+- Relevant documents, SADRs, target ADRs, domain terms, and prior decisions.\n - Relevant modules, interfaces, seams, callers, owners, and tests.\n - Dependency relationships between tasks.\n - Candidate file locks and lock conflicts.\n@@ -152,7 +155,7 @@\n \n CCA must read:\n \n-- `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant ADRs.\n+- `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant SADRs and target ADRs.\n - Parent Goal issue, PRD issue, task issue, comments, and labels.\n - PR body, diff, commits, changed files, reviews, CI checks, and artifacts available to the workflow.\n - `.shiki/` task contract, locks, ledger entries, CCA checklist profile, and prior repair packets.\n@@ -214,7 +217,7 @@\n - Three or more new files.\n - Any high-risk or critical label.\n \n-When the architecture gate triggers, run `grill-with-docs` or `improve-codebase-architecture` as appropriate and record the decision in an ADR if it is hard to reverse or likely to be re-litigated.\n+When the architecture gate triggers, run `grill-with-docs` or `improve-codebase-architecture` as appropriate and record the decision in a Shiki SADR or target ADR as applicable if it is hard to reverse or likely to be re-litigated.\n \n ## 14. Skill Gate\n \n@@ -223,7 +226,7 @@\n Use these skills:\n \n - `setup-matt-pocock-skills`: before first use of the engineering skill set, or when issue tracker, triage labels, or domain docs are missing.\n-- `grill-with-docs`: default for non-trivial Goals before PRD/issues; required for ambiguous plans, terminology, boundaries, ADR-worthy tradeoffs, or design-tree decisions.\n+- `grill-with-docs`: default for non-trivial Goals before PRD/issues; required for ambiguous plans, terminology, boundaries, decision-record-worthy tradeoffs, or design-tree decisions.\n - `zoom-out`: unfamiliar code area or missing architectural map.\n - `to-prd`: convert settled context into a PRD.\n - `to-issues`: break a PRD, plan, or Goal into independently grabbable vertical-slice issues.\ndiff --git a/docs/adr/0001-keep-shiki-as-platform-name.md b/docs/adr/SADR-0001-keep-shiki-as-platform-name.md\nindex 20a9637c3b9d8c97de7ba8db7cfae4d82d9a7dba..8b685439959aada1b221c112f0ac3c51ca8a1695\n--- a/docs/adr/0001-keep-shiki-as-platform-name.md\n+++ b/docs/adr/SADR-0001-keep-shiki-as-platform-name.md\n@@ -1,3 +1,3 @@\n-# Keep Shiki As The Platform Name\n+# SADR-0001: Keep Shiki As The Platform Name\n \n Shiki remains the product name for the GitHub-native agentic engineering control plane. We considered more descriptive names such as MergePilot, MergeGate, and Kiroku, but kept Shiki because it carries the existing goal-seeking framework identity while remaining independent of Hermes, Codex, Claude Code, or any single runtime.\ndiff --git a/docs/adr/0002-use-agents-as-shared-constitution.md b/docs/adr/SADR-0002-use-agents-as-shared-constitution.md\nindex 73de30ecb078120eac540d9bc51855e3cd48d0e3..c5486182bf06fca36ea508496b013c67302fd989\n--- a/docs/adr/0002-use-agents-as-shared-constitution.md\n+++ b/docs/adr/SADR-0002-use-agents-as-shared-constitution.md\n@@ -1,3 +1,3 @@\n-# Use AGENTS As The Shared Agent Constitution\n+# SADR-0002: Use AGENTS As The Shared Agent Constitution\n \n Shiki uses `AGENTS.md` as the runtime-neutral constitution and `CLAUDE.md` as a thin Claude Code wrapper. This keeps Codex, Claude Code, Hermes Runner, and future Agent Runtimes aligned on the same GitHub-first MergeGate rules while still allowing runtime-specific instructions where needed.\ndiff --git a/docs/adr/0003-require-skill-gate-for-mergegate-readiness.md b/docs/adr/SADR-0003-require-skill-gate-for-mergegate-readiness.md\nindex 424be0c9af4c8f0136f1fde68a468c47ff84e764..7fc337987c7df9792f75f181dfcbed75fe5ffff2\n--- a/docs/adr/0003-require-skill-gate-for-mergegate-readiness.md\n+++ b/docs/adr/SADR-0003-require-skill-gate-for-mergegate-readiness.md\n@@ -1,3 +1,3 @@\n-# Require Skill Gate For MergeGate Readiness\n+# SADR-0003: Require Skill Gate For MergeGate Readiness\n \n Shiki requires relevant engineering skills to be invoked before execution when their triggers apply. This makes skills part of MergeGate readiness rather than optional prompt style, because Shiki depends on repeatable planning, issue decomposition, TDD, diagnosis, triage, and architecture review before autonomous or parallel agent work can be trusted.\ndiff --git a/docs/adr/0004-use-github-first-with-shiki-mirror.md b/docs/adr/SADR-0004-use-github-first-with-shiki-mirror.md\nindex d0a3a9174dc646a945a9d325954ffaf00c352040..a79c54ae9ee8eed2a0524192b864da4e22a92d3f\n--- a/docs/adr/0004-use-github-first-with-shiki-mirror.md\n+++ b/docs/adr/SADR-0004-use-github-first-with-shiki-mirror.md\n@@ -1,4 +1,4 @@\n-# ADR 0004: Use GitHub First With A Repository-Local Shiki Mirror\n+# SADR-0004: Use GitHub First With A Repository-Local Shiki Mirror\n \n ## Status\n \ndiff --git a/docs/adr/0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md b/docs/adr/SADR-0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\nindex b339baf49b89d9290e79c3d51b36e7b2bde3bd95..0f3c3eb9afccdb3403ce978e21d04fe12e495da0\n--- a/docs/adr/0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\n+++ b/docs/adr/SADR-0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\n@@ -1,8 +1,8 @@\n-# ADR 0005: Use Codex As Front And Claude Code Action As The GitHub Runtime\n+# SADR-0005: Use Codex As Front And Claude Code Action As The GitHub Runtime\n \n ## Status\n \n-Accepted (implementer default superseded by ADR 0008; the auth model and the\n+Accepted (implementer default superseded by SADR-0008; the auth model and the\n Claude Code Action GitHub runtime decision remain in force)\n \n ## Context\n@@ -26,4 +26,4 @@\n - Shiki avoids requiring `OPENAI_API_KEY` for the default workflow.\n - Codex work happens through the user's authenticated Codex front surface, not as a hidden backend Action.\n - Claude Code Action is configured for OAuth-token based subscription usage by default.\n-- API-key based automation remains possible only as an explicit target-repo extension with its own ADR.\n+- API-key based automation remains possible only as an explicit target-repo extension with its own target ADR.\ndiff --git a/docs/adr/0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md b/docs/adr/SADR-0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\nindex 4bcae9d1e52f0a37b8e9fbafa5cd14b6ee1993bd..7a38538a6ae9929567f0ade67e7ea6a60d0b3f7e\n--- a/docs/adr/0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\n+++ b/docs/adr/SADR-0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\n@@ -1,4 +1,4 @@\n-# ADR 0006: Separate Codex Implementation, CCA Completion, And MergeGate Authorization\n+# SADR-0006: Separate Codex Implementation, CCA Completion, And MergeGate Authorization\n \n ## Status\n \ndiff --git a/docs/adr/0007-packaging-and-release.md b/docs/adr/SADR-0007-packaging-and-release.md\nindex 79b84ddb97167372b44641e5b7860e400ce80822..bb445a2e2e856d9d1f5fdbe35de8d4e8cec0fb32\n--- a/docs/adr/0007-packaging-and-release.md\n+++ b/docs/adr/SADR-0007-packaging-and-release.md\n@@ -1,4 +1,4 @@\n-# ADR 0007: Packaging And Release For Shiki\n+# SADR-0007: Packaging And Release For Shiki\n \n ## Status\n \n@@ -25,7 +25,7 @@\n   not introduce third-party runtime dependencies or break the shim.\n - Releases must run inside the operator's subscription-authenticated toolchain\n   and must not require extra secrets beyond the built-in `GITHUB_TOKEN`\n-  (consistent with ADR 0005's no-API-key-by-default stance).\n+  (consistent with SADR-0005's no-API-key-by-default stance).\n - `.shiki/manifest.json` already encodes the install surface (`install.include`,\n   `install.create_directories`, `install.exclude_from_commit`), and it must not\n   be mutated as part of this change.\n@@ -34,7 +34,7 @@\n \n This is a hard-to-reverse platform decision (it sets the version source of\n truth, the release trigger, and the public install contract), so it is recorded\n-as an ADR.\n+as a SADR.\n \n ## Decision\n \n@@ -104,7 +104,7 @@\n 3. **Publishing to PyPI.** Rejected for now: Shiki is distributed as a\n    repository template + git-installable CLI, not a PyPI library; PyPI would add\n    credentials/secrets and a different versioning contract. Can be added later as\n-   its own ADR if a packaged distribution is needed.\n+   its own SADR if a packaged distribution is needed.\n 4. **Dynamic version (read `VERSION` via a build backend `dynamic` field).**\n    Rejected to keep `pyproject.toml` backend-agnostic and dependency-free; the\n    small duplication between `VERSION` and `pyproject.toml` is guarded by the\n@@ -123,4 +123,4 @@\n   releasing versions the whole platform but ships only `install.include` to\n   targets.\n - Future PyPI distribution or a console entry point are deferred and would each\n-  warrant their own ADR.\n+  warrant their own SADR.\ndiff --git a/docs/adr/0008-use-claude-code-as-default-implementer.md b/docs/adr/SADR-0008-use-claude-code-as-default-implementer.md\nindex dedf4510b7c7799521422f622029563d373db031..35c2625d908167fbc58855b5b63e283805f167e8\n--- a/docs/adr/0008-use-claude-code-as-default-implementer.md\n+++ b/docs/adr/SADR-0008-use-claude-code-as-default-implementer.md\n@@ -1,12 +1,12 @@\n-# ADR 0008: Use Claude Code As The Default Implementer Runtime\n+# SADR-0008: Use Claude Code As The Default Implementer Runtime\n \n ## Status\n \n-Proposed (supersedes ADR 0005 for the implementer default; keeps its auth model)\n+Proposed (supersedes SADR-0005 for the implementer default; keeps its auth model)\n \n ## Context\n \n-ADR 0005 made Codex Front the default implementation runtime, with Claude Code\n+SADR-0005 made Codex Front the default implementation runtime, with Claude Code\n restricted to planner and reviewer roles by the runtime registry, and the only\n autonomous implementation adapter was `shiki runner codex`.\n \n@@ -18,13 +18,13 @@\n - The platform's planning, review, and CCA layers already run on Claude Code,\n   and Claude Code's native capabilities (the built-in `/code-review` skill and\n   the Workflow multi-agent orchestration tool) have no Codex equivalent.\n-- The Goal lifecycle redesign (ADR 0009) requires an autonomous post-freeze\n+- The Goal lifecycle redesign (SADR-0009) requires an autonomous post-freeze\n   loop driven from the operator's Claude Code session, including bounded\n   repair dispatch, which must not depend on a runtime that cannot be invoked\n   unattended.\n \n The constitution claims Shiki is runtime-agnostic (\"not a Claude-only\n-workflow\", ADR 0002). Any change to the implementer default must preserve the\n+workflow\", SADR-0002). Any change to the implementer default must preserve the\n registry-based runtime model rather than hardcoding one vendor.\n \n ## Decision\n@@ -39,7 +39,7 @@\n   the task to review or repair-needed.\n - Keep Codex registered as an optional implementer runtime. `shiki runner\n   codex`, the Codex handoff templates, and the ChatGPT OAuth auth model from\n-  ADR 0005 remain supported for tasks explicitly assigned to `codex`.\n+  SADR-0005 remain supported for tasks explicitly assigned to `codex`.\n - Commit to mandating two Claude-native capabilities as loop contract in\n   follow-up Goals (status: the code-review gate was delivered by Goal G-B \u2014\n   skill registry, checklists item PR-12, default required_skills \u2014 while the\n@@ -60,7 +60,7 @@\n \n Out of scope: removing Codex support, changing the CCA runtime (Claude Code\n Action with `CLAUDE_CODE_OAUTH_TOKEN` stays), and API-key based automation\n-(still requires its own ADR per ADR 0005).\n+(still requires its own target ADR per SADR-0005).\n \n ## Consequences\n \n@@ -100,7 +100,7 @@\n   harness rejection of direct `codex exec` is outside Shiki's control, and the\n   plan's mandatory native capabilities cannot run on Codex at all.\n - **Remove Codex entirely**: rejected; it abandons the runtime-agnostic\n-  constitution (ADR 0002) for no operational gain and forecloses\n+  constitution (SADR-0002) for no operational gain and forecloses\n   cross-runtime adversarial review.\n - **GitHub Actions cloud implementation via claude-code-action**: rejected as\n   default; durable evidence is automatic but token cost, Node 24 workflow\ndiff --git a/docs/adr/0009-requirements-definition-and-spec-freeze.md b/docs/adr/SADR-0009-requirements-definition-and-spec-freeze.md\nindex 29b3f7871bef9203a8b4a831a65c39eb2f506e42..52e7dd69ad0aa4efe0b68c62678abbf7df02aaec\n--- a/docs/adr/0009-requirements-definition-and-spec-freeze.md\n+++ b/docs/adr/SADR-0009-requirements-definition-and-spec-freeze.md\n@@ -1,4 +1,4 @@\n-# ADR 0009: Merge Grill And Planning Into Requirements Definition With Spec Freeze\n+# SADR-0009: Merge Grill And Planning Into Requirements Definition With Spec Freeze\n \n ## Status\n \ndiff --git a/docs/adr/0010-external-ai-guardian-review.md b/docs/adr/SADR-0010-external-ai-guardian-review.md\nindex e6108bbe8bb4e73e0ff4684c8f78001a80438c4e..f3510b145664298adafb151b6f0db51b972f01ec\n--- a/docs/adr/0010-external-ai-guardian-review.md\n+++ b/docs/adr/SADR-0010-external-ai-guardian-review.md\n@@ -1,4 +1,4 @@\n-# ADR 0010: External AI Guardian Review As A First-Class Approval Authority\n+# SADR-0010: External AI Guardian Review As A First-Class Approval Authority\n \n ## Status\n \n@@ -8,7 +8,7 @@\n \n Shiki is intended to be an autonomous, recursive, self-improving agent\n framework: authority-in-the-loop, not human-in-the-loop. Until now the Guardian\n-approval required for high/critical-risk merges (guardian-policy.json, ADR\n+approval required for high/critical-risk merges (guardian-policy.json, SADR\n governance) recognized only human authority sources \u2014 a GitHub review approval,\n the `guardian:approved` label applied by a configured Guardian, or a comment\n carrying `Guardian approval granted` plus the current head SHA.\n@@ -89,7 +89,7 @@\n   autonomous design the operator intends and produced the impersonation\n   incident.\n - **Record AI approval as an operator comment**: rejected as forgery of human\n-  authority \u2014 the exact failure this ADR corrects.\n+  authority \u2014 the exact failure this SADR corrects.\n - **Deliver the artifact as a committed `.shiki/` file**: rejected; a committed\n   approval inside the PR being approved is forgeable by anyone who can push.\n - **A dedicated reviewer bot identity instead of a relayed comment**: deferred;\ndiff --git a/docs/adr/0011-autonomous-quality-gate-evidence.md b/docs/adr/SADR-0011-autonomous-quality-gate-evidence.md\nindex bca0246c5d46e44afbde15486a75eec425fc02f5..9c93eac16d8c3a6d4b11bd7224e3955102cd7db4\n--- a/docs/adr/0011-autonomous-quality-gate-evidence.md\n+++ b/docs/adr/SADR-0011-autonomous-quality-gate-evidence.md\n@@ -1,4 +1,4 @@\n-# ADR 0011: Autonomous Quality-Gate Evidence \u2014 Deterministic Facts And An Independent Verifier\n+# SADR-0011: Autonomous Quality-Gate Evidence \u2014 Deterministic Facts And An Independent Verifier\n \n ## Status\n \ndiff --git a/docs/adr/0012-autonomous-loop-closeout-to-main.md b/docs/adr/SADR-0012-autonomous-loop-closeout-to-main.md\nindex 637fad6dde4989c9c2bf0f81f676d8d29991123c..ec7d85f81030a676bcbba28b43eb365422a36eac\n--- a/docs/adr/0012-autonomous-loop-closeout-to-main.md\n+++ b/docs/adr/SADR-0012-autonomous-loop-closeout-to-main.md\n@@ -1,4 +1,4 @@\n-# 0012 \u2014 Autonomous loop pushes goal completion to main via a closeout PR\n+# SADR-0012 \u2014 Autonomous loop pushes goal completion to main via a closeout PR\n \n - Status: accepted\n - Date: 2026-06-18\ndiff --git a/docs/adr/0013-bootstrap-grants-actions-approve-pull-requests.md b/docs/adr/SADR-0013-bootstrap-grants-actions-approve-pull-requests.md\nindex b6c69d64f1c5ced346b88d6859586715b3b5bd0e..cf48ff4a6716267f539ca58edd1a852e7c7a2d64\n--- a/docs/adr/0013-bootstrap-grants-actions-approve-pull-requests.md\n+++ b/docs/adr/SADR-0013-bootstrap-grants-actions-approve-pull-requests.md\n@@ -1,4 +1,4 @@\n-# ADR 0013: Bootstrap Grants GitHub Actions Permission To Create And Approve Pull Requests\n+# SADR-0013: Bootstrap Grants GitHub Actions Permission To Create And Approve Pull Requests\n \n ## Status\n \n@@ -31,7 +31,7 @@\n security tradeoff: it lets repository automation satisfy a human-review gate. In\n a multi-maintainer repo that could let a compromised or buggy workflow\n self-approve changes. So whether bootstrap turns it on by default \u2014 and with\n-what default token scope \u2014 is an ADR-worthy decision, not an implementation\n+what default token scope \u2014 is a SADR-worthy decision, not an implementation\n detail.\n \n ## Decision\n@@ -48,7 +48,7 @@\n Specifically:\n \n - **`can_approve_pull_request_reviews` defaults to `true`.** Shiki's default\n-  posture is solo/AFK autonomous operation (ADR 0008/0009/0012), where the CCA\n+  posture is solo/AFK autonomous operation (SADR-0008/0009/0012), where the CCA\n   Review Bridge is the only path that can satisfy `required_review: true` after\n   the CCA and Guardian gates have already passed. The default makes a\n   freshly-bootstrapped repo able to run the autonomous loop end to end without a\ndiff --git a/docs/adr/0014-codex-app-external-ai-guardian-ui-adapter.md b/docs/adr/SADR-0014-codex-app-external-ai-guardian-ui-adapter.md\nindex 238db06279334caa341c117362df9271382a7fcd..65f12e9939321e1b3e389a34c822fc1549e5155d\n--- a/docs/adr/0014-codex-app-external-ai-guardian-ui-adapter.md\n+++ b/docs/adr/SADR-0014-codex-app-external-ai-guardian-ui-adapter.md\n@@ -1,4 +1,4 @@\n-# ADR 0014: Use Codex App As The External AI Guardian UI Adapter\n+# SADR-0014: Use Codex App As The External AI Guardian UI Adapter\n \n ## Status\n \ndiff --git a/docs/adr/0015-contract-approval-before-dispatch.md b/docs/adr/SADR-0015-contract-approval-before-dispatch.md\nindex fad6c656741a20a7684ba16080a7db159f63a52f..6061271a46d3f1d6ae7174477de3fb75673123a6\n--- a/docs/adr/0015-contract-approval-before-dispatch.md\n+++ b/docs/adr/SADR-0015-contract-approval-before-dispatch.md\n@@ -1,4 +1,4 @@\n-# ADR 0015: Approve The Task Contract Before Dispatch, Not The Diff After It\n+# SADR-0015: Approve The Task Contract Before Dispatch, Not The Diff After It\n \n ## Status\n \n@@ -9,7 +9,7 @@\n Guardian approval for high/critical risk is expressible only on a pull request.\n Every approval source in `guardian-policy.json` \u2014 GitHub review, the\n `guardian:approved` label, the `Guardian approval granted` comment, and the\n-`external_ai_guardian_review` artifact (ADR 0010/0014) \u2014 is bound to a PR, and\n+`external_ai_guardian_review` artifact (SADR-0010/0014) \u2014 is bound to a PR, and\n three of the four require the current head SHA. Approval therefore cannot exist\n before an implementation PR exists.\n \n@@ -81,7 +81,7 @@\n An envelope breach must surface only on the non-repairable policy gate, never on\n the repairable `MergeGate metadata check`. Otherwise the Goal Loop routes it to\n `dispatch_repair` \u2014 instructing a runner to make the breach go away \u2014 which is\n-exactly what this ADR forbids.\n+exactly what this SADR forbids.\n \n **Contract immutability.** After registration, an implementation PR must not\n change the governance fields of its own task contract (`scope`, `non_goals`,\n@@ -116,7 +116,7 @@\n fails any verdict that is not `complete`, and the signal the CCA prompt reads is\n computed from `evaluate_guardian_approval` alone. Without the OR-merge at the\n signal, a contract-approved PR still returns `needs_guardian`, the CCA job fails,\n-and the MergeGate policy check never runs \u2014 the gate this ADR changes would never\n+and the MergeGate policy check never runs \u2014 the gate this SADR changes would never\n be reached. The CCA *prompt* needs no change: a `contract_approval` entry in the\n signal's existing `sources` array satisfies its current wording.\n \n@@ -130,7 +130,7 @@\n written, and an approved contract lets its implementation PRs merge through the\n same automatic path as low/medium risk. The wasted implement-then-block cycle\n disappears. Guardian review shifts from reading diffs to reading contracts, which\n-is both earlier and smaller; the external AI Guardian path (ADR 0010/0014) works\n+is both earlier and smaller; the external AI Guardian path (SADR-0010/0014) works\n on contracts with no change.\n \n We accept that the Guardian authorizes a class of change without seeing its\n@@ -146,7 +146,7 @@\n adversarial tests for the new approval path equivalent in rigor to\n `test_shiki_governance_evidence.sh`.\n \n-Bootstrap costs, which apply only while this ADR is being implemented. The tasks\n+Bootstrap costs, which apply only while this SADR is being implemented. The tasks\n that build Contract Approval merge under the existing rules, so the ones\n dispatched after the pre-dispatch gate lands but before the approval path lands\n need a Guardian-approved Contract PR *and* a live Guardian approval on their\n@@ -210,7 +210,7 @@\n risk; they do not bound authorized work.\n \n **Auto-escalate risk on envelope breach instead of stopping.** Returns the\n-workflow to a diff-time Guardian decision \u2014 the exact behavior this ADR removes.\n+workflow to a diff-time Guardian decision \u2014 the exact behavior this SADR removes.\n \n **Resolve governance fields from the base snapshot only.** Rejected against the\n registration flow: implementation PRs add their own contract, so the base has no\ndiff --git a/docs/adr/0016-locks-bound-product-paths.md b/docs/adr/SADR-0016-locks-bound-product-paths.md\nindex b5e850f3f1f7f4ef6e7f9d1763a29a51bd300c45..46b40a9b99a7aae644c02bb458f5473ff0a90ce2\n--- a/docs/adr/0016-locks-bound-product-paths.md\n+++ b/docs/adr/SADR-0016-locks-bound-product-paths.md\n@@ -1,4 +1,4 @@\n-# ADR 0016: Locks bound product paths; the Shiki mirror is bound by record identity\n+# SADR-0016: Locks bound product paths; the Shiki mirror is bound by record identity\n \n ## Status\n \n@@ -6,8 +6,8 @@\n \n - Date: 2026-07-28\n - Deciders: operator (mizutani-140), Claude (planner)\n-- Related: ADR 0004 (GitHub-first with a `.shiki` mirror), ADR 0009 (Spec Freeze),\n-  ADR 0012 (autonomous loop closeout to main), ADR 0015 (Contract Approval before\n+- Related: SADR-0004 (GitHub-first with a `.shiki` mirror), SADR-0009 (Spec Freeze),\n+  SADR-0012 (autonomous loop closeout to main), SADR-0015 (Contract Approval before\n   dispatch), remediation goal task R-02, the live PR #179 deadlock.\n \n ## Context\n@@ -90,11 +90,11 @@\n files, documentation files and named source modules are nameable; runtime-\n allocated ledger, runner and report ids are not \u2014 and those live in `.shiki`,\n which locks no longer govern. Existing frozen contracts are brought into line by\n-a one-time Spec Amendment per plan, which must land before ADR 0015's contract\n+a one-time Spec Amendment per plan, which must land before SADR-0015's contract\n immutability makes `locks` unwritable from the implementation PR.\n \n **Lock state is resolved from the durable side.** MergeGate resolves the lock\n-registry, and ADR 0015's governance fields, from the base `.shiki` snapshot when\n+registry, and SADR-0015's governance fields, from the base `.shiki` snapshot when\n present, falling back to head only when absent. The `MergeGate metadata check`\n job is given `--base-shiki`, which it does not receive today. Locks are not\n derived from the live open-pull-request set: a pull request body is untrusted\n@@ -108,7 +108,7 @@\n Explicitly out of scope: moving MergeGate's contract resolution off the PR\n checkout wholesale; making the mirror a projection of GitHub state; introducing\n new lock kinds; moving ledger evidence out of the repository; and unwinding\n-ADR 0012's closeout pull request. Registration of multi-task goals is ADR 0015's\n+SADR-0012's closeout pull request. Registration of multi-task goals is SADR-0015's\n Contract PR and is not re-decided here.\n \n ## Consequences\n@@ -127,7 +127,7 @@\n \n Risks accepted. First, goal-scoped identity is only as strong as `goal_id`,\n which is resolved from the PR body and the head task file; the mitigation is to\n-add `goal_id` to ADR 0015's frozen governance field set, and until that lands a\n+add `goal_id` to SADR-0015's frozen governance field set, and until that lands a\n task file can name a foreign goal. Second, base-when-present resolution is\n unavailable on fork pull requests, where the head is contributor-controlled;\n fork pull requests already cannot complete a Shiki merge, and this decision does\n@@ -139,7 +139,7 @@\n path, not by implementation pull requests.\n \n New obligations: one Spec Amendment per frozen plan to rewrite `locks`, ordered\n-before ADR 0015 T1; extension of the adversarial suites in\n+before SADR-0015 T1; extension of the adversarial suites in\n `scripts/test_shiki_adversarial_state.sh` and\n `scripts/test_shiki_governance_evidence.sh` to cover the eleven newly-scoped\n directories; and updating `scripts/test_shiki_loop_lock_guard.sh`,\n@@ -151,32 +151,32 @@\n whether the loop's transitive sync of ledger `evidence` references can be\n deleted once the Contract PR carries its own source plan.\n \n-## Relationship to ADR 0004\n+## Relationship to SADR-0004\n \n-This ADR **clarifies something ADR 0004 left unspecified**. ADR 0004 decides that\n+This SADR **clarifies something SADR-0004 left unspecified**. SADR-0004 decides that\n GitHub is the operational source of truth and that each target repository keeps a\n `.shiki/` mirror. It says nothing about pull requests writing the mirror, does\n not name MergeGate, and does not make the mirror an input to a gate. Nothing in\n-ADR 0004's Decision or Consequences is amended, superseded or weakened: the\n+SADR-0004's Decision or Consequences is amended, superseded or weakened: the\n mirror still exists, still records the same artifacts, and pull request\n-boundaries remain the coordination surface. What this ADR settles is the question\n+boundaries remain the coordination surface. What this SADR settles is the question\n 0004 never reached \u2014 by what authority a pull request may write a mirror record \u2014\n and it answers it the way `.shiki/manifest.json` already declares\n (`pr_mutation: current-task-or-current-goal-only`).\n \n-It **amends ADR 0012**, whose Decision item 5 states that \"Loop-executed tasks\n+It **amends SADR-0012**, whose Decision item 5 states that \"Loop-executed tasks\n declare the `path:.shiki/**` lock, which covers every `.shiki` file the closeout\n stages ... so `files_outside_locks` is satisfied without per-file lock\n bookkeeping.\" That clause is replaced: the closeout's mirror writes are\n-authorized by record identity, not by a declared lock. ADR 0012's substantive\n+authorized by record identity, not by a declared lock. SADR-0012's substantive\n decision \u2014 that completion reaches `main` through a closeout pull request rather\n than local attestation \u2014 is preserved unchanged.\n \n-It is **complementary to ADR 0015**, not a substitute. ADR 0015 owns contract\n+It is **complementary to SADR-0015**, not a substitute. SADR-0015 owns contract\n immutability, the pre-dispatch gate, the Contract PR and the Scope Envelope. This\n-ADR owns what a lock means and who may write the mirror. The one ordering\n+SADR owns what a lock means and who may write the mirror. The one ordering\n constraint between them is that the lock-narrowing Spec Amendment must precede\n-ADR 0015 T1's freezing of the `locks` field.\n+SADR-0015 T1's freezing of the `locks` field.\n \n ## Alternatives Considered\n \n@@ -186,7 +186,7 @@\n the only writer that can register them may run. Migrating the Guardian signal to\n that store makes every registration PR fail closed to `needs_guardian` at every\n risk level, because the task file is by definition absent from the store, which\n-removes the low/medium autonomous path ADR 0008/0009 exists to provide. And it\n+removes the low/medium autonomous path SADR-0008/0009 exists to provide. And it\n leaves schemas, manifest, config, guardian policy, templates, migrations, plans,\n worktrees, runner records, runs and handoffs with no writer at all in a\n repository whose product is `.shiki`. Measured benefit on the frozen backlog:\n@@ -199,14 +199,14 @@\n namespace is not in fact ownership-partitioned \u2014 56 ledger entries carry no\n `task_id`, and one goal DAG is writable by 35 registered tasks. The correct\n expression of its insight is that locks do not govern the mirror at all, which is\n-what this ADR decides.\n+what this SADR decides.\n \n **Make the mirror a projection written only by a registration writer and a\n post-merge projection job.** Rejected. It requires a bypass actor with write\n access to the protected default branch; it makes its own master invariant\n readable from a manifest that pull requests may edit; it deletes both reconcile\n modes and leaves a corrupted mirror with no in-band repair path, contradicting\n-ADR 0004's consequence that mirror conflicts are repaired against GitHub state;\n+SADR-0004's consequence that mirror conflicts are repaired against GitHub state;\n and its content-derived ids are rejected by `validate_shiki`'s currently-green id\n pattern.\n \ndiff --git a/docs/adr/0017-bookkeeping-closeouts-do-not-inherit-risk.md b/docs/adr/SADR-0017-bookkeeping-closeouts-do-not-inherit-risk.md\nindex 554e3a0d526a72ca067108c5464cc68c3edcf174..b9676857a4713850e063af7c39c8e0b01ecb4c66\n--- a/docs/adr/0017-bookkeeping-closeouts-do-not-inherit-risk.md\n+++ b/docs/adr/SADR-0017-bookkeeping-closeouts-do-not-inherit-risk.md\n@@ -1,4 +1,4 @@\n-# ADR 0017: A Bookkeeping Closeout Does Not Inherit Its Task's Risk\n+# SADR-0017: A Bookkeeping Closeout Does Not Inherit Its Task's Risk\n \n ## Status\n \n@@ -6,7 +6,7 @@\n \n ## Context\n \n-ADR 0012 made the goal loop push a task's completion to the default branch through a\n+SADR-0012 made the goal loop push a task's completion to the default branch through a\n closeout pull request: after the implementation PR merges, the loop opens a second PR\n carrying `task.status = done`, `lock.state = released`, and \u2014 when the task completes\n its goal \u2014 `goal.status = complete` plus one scorecard report. Nothing else. The\n@@ -49,14 +49,14 @@\n 4. The task file's governance fields (`scope`, `non_goals`, `required_skills`,\n    `risk_level`, `locks`, `acceptance_checks`, `test_command`) are byte-identical to\n    the base snapshot. Only `status`, `expected_pr`, `closeout_pr`, `ledger_evidence`\n-   and `expected_branch` may differ \u2014 the ADR 0015 contract-immutability rule,\n+   and `expected_branch` may differ \u2014 the SADR-0015 contract-immutability rule,\n    unchanged.\n \n    `expected_branch` is in that set because a closeout is *by construction* on a\n    different branch from the implementation it closes, and the loop rewrites the\n    field accordingly. It is safe to let it differ because MergeGate independently\n    requires `expected_branch` to equal the PR's actual head ref, so a PR cannot set\n-   it to anything other than the branch it is really on. The first draft of this ADR\n+   it to anything other than the branch it is really on. The first draft of this SADR\n    omitted it, which made the exemption unreachable: every real closeout failed\n    condition 4 on a field the loop always changes. See PR #209.\n 5. The state transitions are exactly the terminal set: task `review` \u2192 `done`, lock\n@@ -113,12 +113,12 @@\n classification must be derived, not asserted.\n \n **Fold the closeout into the implementation PR** so no second PR exists. Rejected under\n-ADR 0012, which recorded why: a task file that is already `done` on the implementation\n+SADR-0012, which recorded why: a task file that is already `done` on the implementation\n branch short-circuits the loop's review \u2192 CCA \u2192 merge \u2192 done ordering into a local\n goal-completion before any merge happens.\n \n **Have the loop auto-merge closeouts with elevated credentials.** Rejected outright: that\n-is a runtime authoring its own approval, which ADR 0010 and the constitution's\n+is a runtime authoring its own approval, which SADR-0010 and the constitution's\n approval-record prohibition forbid. It would also be indistinguishable, in the evidence,\n from the forgery those rules exist to prevent.\n \ndiff --git a/docs/adr/0018-carried-guardian-approval-across-a-proven-base-sync.md b/docs/adr/SADR-0018-carried-guardian-approval-across-a-proven-base-sync.md\nindex 22358562da390ed8b45cd600da08a43d378b4680..e6e2907cefa776460af73ddf7d39db605f3c39b0\n--- a/docs/adr/0018-carried-guardian-approval-across-a-proven-base-sync.md\n+++ b/docs/adr/SADR-0018-carried-guardian-approval-across-a-proven-base-sync.md\n@@ -1,4 +1,4 @@\n-# ADR 0018: Carried Guardian Approval Across A Proven Base Sync\n+# SADR-0018: Carried Guardian Approval Across A Proven Base Sync\n \n ## Status\n \n@@ -34,7 +34,7 @@\n Guardian evaluator that consumes it (`shiki_guardian.evaluate_guardian_approval`,\n the `guardian_comment_carried` source) were built and tested in isolation. Neither\n does anything until a caller passes the flags that turn the carry on, so the\n-feature was invisible in CI. This ADR records the decision the two of them\n+feature was invisible in CI. This SADR records the decision the two of them\n implement, and this task wires it into the four CI invocations that judge Guardian\n approval.\n \ndiff --git a/docs/adr/README.md b/docs/adr/README.md\nindex c70e057a4520c36e042ab68c53cd4a21ef709b71..5da43856836e2ce6a150f8127766e2f88355ab4b\n--- a/docs/adr/README.md\n+++ b/docs/adr/README.md\n@@ -1,40 +1,41 @@\n-# Architecture Decision Records\n+# Decision Record Namespaces\n \n-This directory holds Shiki's Architecture Decision Records (ADRs): durable\n-records of hard-to-reverse platform decisions. ADRs are part of the Shiki source\n-of truth alongside GitHub state, the `.shiki/` mirror, and `CONTEXT.md`.\n+This directory holds two independent decision namespaces:\n \n-## How ADRs Work\n+- Shiki Architecture Decision Records (SADRs), named\n+  `SADR-NNNN-<short-slug>.md`, record hard-to-reverse Shiki platform decisions.\n+- Target Architecture Decision Records (ADRs), named\n+  `NNNN-<short-slug>.md`, belong to the target product.\n \n-- New ADRs are added as `docs/adr/NNNN-<short-slug>.md`, where `NNNN` is the next\n-  zero-padded sequence number.\n-- The **canonical list of ADRs is the set of files matching `docs/adr/*.md`** in\n-  this directory. Browse `docs/adr/*.md` for the authoritative, current set.\n-- Use [`template.md`](template.md) as the starting point for a new ADR.\n-- Record an ADR when a decision is hard to reverse, surprising without context,\n-  and a real tradeoff. Do not write an ADR for routine or easily reversible\n-  choices.\n+The same four-digit number may appear once in each namespace. A platform\n+`SADR-NNNN` citation resolves only to `SADR-NNNN-*.md`; a target `ADR NNNN`\n+citation resolves only to `NNNN-*.md`.\n \n-Using the `docs/adr/*.md` glob as the source of truth means other PRs can add an\n-ADR without editing this index file, avoiding merge contention on a\n-hand-maintained enumeration.\n+## How Decision Records Work\n \n-## Current ADRs (snapshot)\n-\n-The list below is a convenience snapshot and may lag behind the directory. When\n-in doubt, list `docs/adr/*.md`.\n+- New Shiki platform decisions use the next zero-padded SADR number.\n+- New target product decisions use the target's own ADR sequence.\n+- The canonical list is the files in `docs/adr/`; no hand-maintained index is\n+  authoritative.\n+- Use [`template.md`](template.md) as the starting point and choose the filename\n+  namespace that owns the decision.\n+- Record a decision when it is hard to reverse, surprising without context, and\n+  a real tradeoff. Do not write a decision record for routine or easily\n+  reversible choices.\n \n-- [0001 \u2014 Keep Shiki As The Platform Name](0001-keep-shiki-as-platform-name.md)\n-- [0002 \u2014 Use AGENTS As The Shared Agent Constitution](0002-use-agents-as-shared-constitution.md)\n-- [0003 \u2014 Require Skill Gate For MergeGate Readiness](0003-require-skill-gate-for-mergegate-readiness.md)\n-- [0004 \u2014 Use GitHub First With A Repository-Local Shiki Mirror](0004-use-github-first-with-shiki-mirror.md)\n-- [0005 \u2014 Use Codex As Front And Claude Code Action As The GitHub Runtime](0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md)\n-- [0006 \u2014 Separate Codex Implementation, CCA Completion, And MergeGate Authorization](0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md)\n+Using the directory as the source of truth lets independent changes add records\n+without contending on an enumerated index.\n \n-## Adding A New ADR\n+## Adding A Shiki SADR\n \n-1. Copy `template.md` to `docs/adr/NNNN-<short-slug>.md`.\n+1. Copy `template.md` to `docs/adr/SADR-NNNN-<short-slug>.md`.\n 2. Fill in the title, status, context, decision, and consequences.\n-3. Set the status to `Proposed`, then `Accepted` once the decision is approved.\n-4. Open a PR following `CONTRIBUTING.md`. You do not need to edit this index for\n-   the ADR to be discoverable via `docs/adr/*.md`.\n+3. Set the status to `Proposed`, then `Accepted` once the proper authority has\n+   approved the decision.\n+4. Open a PR following `CONTRIBUTING.md`.\n+\n+## Adding A Target ADR\n+\n+1. Copy `template.md` to `docs/adr/NNNN-<short-slug>.md` using the target's own\n+   sequence.\n+2. Cite it as `ADR NNNN`; do not prefix its filename with `SADR-`.\ndiff --git a/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md b/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\nnew file mode 100644\nindex 0000000000000000000000000000000000000000..7455ce1f3bed02ddddb851ac21a1d1a648c9aeba\n--- /dev/null\n+++ b/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\n@@ -0,0 +1,55 @@\n+# SADR-0019: Separate Shiki Platform SADRs From Target ADRs\n+\n+## Status\n+\n+Accepted\n+\n+## Context\n+\n+Shiki installs its platform decision records into `docs/adr/` in every target\n+repository. Shiki and its targets previously used the same numeric\n+`NNNN-<slug>.md` namespace. A target that already owned ADRs 0013 through 0018\n+therefore acquired two records for each number during installation, making an\n+`ADR NNNN` citation ambiguous. Renumbering target decisions would rewrite target\n+history, while renumbering Shiki decisions per target would destroy their stable\n+platform identity.\n+\n+Upgrades also need to remove the former Shiki-owned numeric paths without ever\n+mistaking a target-owned ADR for platform content. The install stamp already\n+records exact shipped paths and content digests, so it can prove ownership only\n+when both still match.\n+\n+## Decision\n+\n+Shiki platform decisions use the `SADR` namespace:\n+\n+- records are named `docs/adr/SADR-NNNN-<slug>.md`;\n+- live platform citations use `SADR-NNNN`;\n+- target product decisions retain `docs/adr/NNNN-<slug>.md` and `ADR NNNN`;\n+- the reference validator resolves the two namespaces independently, so the same\n+  four-digit number may exist once in each namespace.\n+\n+Before a forced upgrade writes anything, the installer inventories the exact\n+former numeric Shiki ADR paths. It may delete a legacy path only when the\n+existing install stamp contains that exact path and its recorded SHA-256 digest\n+matches the file currently on disk. All ownership blockers are reported\n+together and abort the upgrade before its first write. A non-forced install\n+never performs this cleanup.\n+\n+Historical `.shiki/` evidence remains unchanged because it records the namespace\n+that existed when that evidence was produced.\n+\n+## Consequences\n+\n+- Targets may use any ADR number without colliding with Shiki's platform SADRs.\n+- Fresh installs preserve target ADR bytes and install a stable SADR set.\n+- Forced upgrades fail closed when legacy ownership cannot be proven, and a\n+  successful upgrade refreshes the stamp without removed legacy paths.\n+- Current governance, packaging, workflow, prompt, script, and skill references\n+  must move with the platform records.\n+\n+## Alternatives Considered\n+\n+- Collision-only refusal leaves affected targets permanently unupgradable.\n+- Renumbering target ADRs mutates target-owned history.\n+- Per-target Shiki renumbering makes platform citations non-portable.\ndiff --git a/docs/adr/template.md b/docs/adr/template.md\nindex a92a97daddd0b00ab9dd626cc8d86942da2eafed..17dd79175b5fee2105935557f691b40cf9492942\n--- a/docs/adr/template.md\n+++ b/docs/adr/template.md\n@@ -1,14 +1,14 @@\n-# NNNN: <Short Decision Title>\n+# <SADR-NNNN or NNNN>: <Short Decision Title>\n \n ## Status\n \n-Proposed | Accepted | Superseded by ADR-NNNN\n+Proposed | Accepted | Superseded by SADR-NNNN or target ADR NNNN\n \n ## Context\n \n What is the situation, constraint, or force that requires a decision? Reference\n-relevant domain terms (`CONTEXT.md`), prior ADRs, and code reality. State the\n-problem, not the solution.\n+relevant domain terms (`CONTEXT.md`), prior Shiki SADRs or target ADRs, and code\n+reality. State the problem, not the solution.\n \n ## Decision\n \ndiff --git a/docs/agents/autonomous-loop-e2e-acceptance.md b/docs/agents/autonomous-loop-e2e-acceptance.md\nindex bbf84c6081987f159ac6ad9ecf3c82b7aa72e7e8..5c13c1d02a05a90b0dc46cbef7c95a9846d0c237\n--- a/docs/agents/autonomous-loop-e2e-acceptance.md\n+++ b/docs/agents/autonomous-loop-e2e-acceptance.md\n@@ -40,8 +40,8 @@\n 3. `gh auth status` is logged in with merge permission on the repo.\n 4. Branch protection + required checks are configured exactly as the goal-loop\n    required-check set expects (see `docs/agents/branch-protection-smoke.md`).\n-5. The repo `.shiki/policy` permits autonomous low/medium-risk merges (ADR\n-   0008/0009); the MergeGate policy check is the recorded-authority gate.\n+5. The repo `.shiki/policy` permits autonomous low/medium-risk merges\n+   (SADR-0008/0009); the MergeGate policy check is the recorded-authority gate.\n \n The operator's only actions are: register the task and start the loop. **No\n manual pre-clearing** means after `shiki loop run` starts, the operator does not\ndiff --git a/docs/agents/bootstrap-command.md b/docs/agents/bootstrap-command.md\nindex 05f7f203a52cf412b9f97084a6e131ed0f785afd..f7aa0b734dfebee4a3b139335b66ee3415f48713\n--- a/docs/agents/bootstrap-command.md\n+++ b/docs/agents/bootstrap-command.md\n@@ -189,7 +189,9 @@\n ```\n \n Use this only for tests, fixtures, or explicit local-only template inspection.\n-Use `--force` only when you intentionally want to overwrite existing target files.\n+Use `--force` only when you intentionally want to refresh the target template.\n+Legacy numeric Shiki decision cleanup is stamp-and-digest authorized before any\n+write; target ADR paths are not cleanup candidates.\n \n ## Slash Command\n \ndiff --git a/docs/agents/checklists.md b/docs/agents/checklists.md\nindex 4a7f03ade346f53a1c1d82981f3fd744f9ab4547..0c5ec6c53dc7197b7b7a7d18e667b325babd6c45\n--- a/docs/agents/checklists.md\n+++ b/docs/agents/checklists.md\n@@ -32,18 +32,18 @@\n | ID | Item | Blocking |\n | --- | --- | --- |\n | GD-01 | Plan was challenged against `CONTEXT.md`. | blocking for non-trivial Goals |\n-| GD-02 | Relevant ADRs were read or absence was recorded. | blocking for non-trivial Goals |\n+| GD-02 | Relevant SADRs and target ADRs were read or absence was recorded. | blocking for non-trivial Goals |\n | GD-03 | Ambiguous terms were resolved or converted to HITL blockers. | blocking |\n | GD-04 | Edge scenarios were considered. | advisory |\n | GD-05 | Code reality was checked when code could answer. | blocking when applicable |\n | GD-06 | New domain terms were added to `CONTEXT.md` when needed. | blocking when applicable |\n-| GD-07 | ADR was created or explicitly skipped using the ADR criteria. | blocking when applicable |\n+| GD-07 | A SADR or target ADR was created or explicitly skipped using the decision-record criteria. | blocking when applicable |\n \n ## CI \u2014 Context & Impact Checklist\n \n | ID | Item | Blocking |\n | --- | --- | --- |\n-| CI-01 | Relevant docs and ADRs identified. | blocking |\n+| CI-01 | Relevant docs, SADRs, and target ADRs identified. | blocking |\n | CI-02 | Relevant modules, interfaces, seams, callers, and tests identified. | blocking |\n | CI-03 | Dependencies and blocked-by relationships identified. | blocking |\n | CI-04 | Candidate locks identified. | blocking |\n@@ -62,7 +62,7 @@\n | PRD-04 | Implementation decisions are recorded without volatile code detail. | blocking |\n | PRD-05 | Testing decisions are recorded. | blocking |\n | PRD-06 | Out-of-scope items are explicit. | blocking |\n-| PRD-07 | Relevant glossary/ADR links are present when applicable. | blocking |\n+| PRD-07 | Relevant glossary/SADR/target-ADR links are present when applicable. | blocking |\n \n ## SF \u2014 Spec Freeze Checklist\n \n@@ -98,7 +98,7 @@\n | H-02 | Branch/worktree target is present. | blocking |\n | H-03 | Scope and non-goals are present. | blocking |\n | H-04 | Dependencies and locks are present. | blocking |\n-| H-05 | Relevant docs, ADRs, modules, and tests are referenced. | blocking |\n+| H-05 | Relevant docs, SADRs, target ADRs, modules, and tests are referenced. | blocking |\n | H-06 | Required skills are stated. | blocking |\n | H-07 | TDD expectations are stated. | blocking when behavior work |\n | H-08 | Acceptance criteria are included. | blocking |\ndiff --git a/docs/agents/codex-handoff.md b/docs/agents/codex-handoff.md\nindex ede468f47cb277f69a857e8c45df703e4ed0151d..6f97bdffb67e44d7edea2c71e01ea28933c4eb6c\n--- a/docs/agents/codex-handoff.md\n+++ b/docs/agents/codex-handoff.md\n@@ -46,7 +46,7 @@\n \n ## Context\n - Domain terms:\n-- Relevant ADRs:\n+- Relevant SADRs and target ADRs:\n - Relevant docs:\n - Relevant modules/interfaces/seams:\n - Similar tests/prior art:\ndiff --git a/docs/agents/completion-check-agent.md b/docs/agents/completion-check-agent.md\nindex 13e4c035db35025f4e3cdcc5adfb24314ac4d521..61bfa1218cf36ca605250c91919e620f33238aa0\n--- a/docs/agents/completion-check-agent.md\n+++ b/docs/agents/completion-check-agent.md\n@@ -32,7 +32,7 @@\n - `AGENTS.md`\n - `CLAUDE.md`\n - `CONTEXT.md`\n-- relevant ADRs\n+- relevant SADRs and target ADRs\n - parent Goal issue\n - PRD issue or document\n - task issue\ndiff --git a/docs/agents/decision-control.md b/docs/agents/decision-control.md\nindex 7d8ee5ab03a5ccbcbd04056ae61de03785a2a9c9..8c94ecda00fffaa0e92c325ab83aa9776bd1dac1\n--- a/docs/agents/decision-control.md\n+++ b/docs/agents/decision-control.md\n@@ -11,7 +11,7 @@\n | Decision | Owner | When | Required inputs | Output | Enforcement surface |\n | --- | --- | --- | --- | --- | --- |\n | Is the Goal clear enough to plan? | Planner + Guardian when needed | Goal intake | User request, existing docs, risk signals | Goal readiness verdict | Goal checklist, issue template, ledger |\n-| Must `grill-with-docs` run? | Planner / Skill Gate | Before PRD or issue split | Ambiguity, domain terms, ADR conflicts, unclear boundaries | Skill invocation or explicit skip rationale | Skill Gate checklist |\n+| Must `grill-with-docs` run? | Planner / Skill Gate | Before PRD or issue split | Ambiguity, domain terms, SADR/target-ADR conflicts, unclear boundaries | Skill invocation or explicit skip rationale | Skill Gate checklist |\n | Is a PRD required? | Planner | After Goal context stabilizes | Goal, `grill-with-docs` output, Context & Impact output | PRD required / not required | PRD checklist, CCA evidence check |\n | Can the spec be frozen? | Operator only | End of Requirements Definition, before issue split | PRD/requirements summary, scope inventory (SF-02), open-question list | `spec_freeze` block in the plan + ledger evidence | `require_grilled_plan` (plan ingest/run), SF checklist |\n | Can a frozen spec change? | Operator only (Spec Amendment) | When implementation contradicts the frozen spec | Contested decisions, scoped re-grill result | recorded amendment + re-stamped freeze | SF checklist, ledger evidence |\ndiff --git a/docs/agents/domain.md b/docs/agents/domain.md\nindex 258da8ea81e87ac0a6b0b5b5e5c2fde980eaabb7..d57b0cf17ff3e1fa9e6e5885372bf083d8295421\n--- a/docs/agents/domain.md\n+++ b/docs/agents/domain.md\n@@ -5,7 +5,9 @@\n ## Layout\n \n - `CONTEXT.md` defines Shiki language. Keep it implementation-free.\n-- `docs/adr/` records hard-to-reverse decisions.\n+- `docs/adr/SADR-NNNN-*.md` records Shiki platform decisions.\n+- `docs/adr/NNNN-*.md` records target product decisions without sharing the\n+  SADR namespace.\n - `AGENTS.md` is the runtime-neutral agent constitution.\n - `CLAUDE.md` is the Claude Code wrapper around `AGENTS.md`.\n - `.shiki/` is the Target Repository mirror for Goals, PRDs, plans, Task DAGs, contracts, locks, worktree records, ledger entries, CCA verdicts, repair packets, reports, and handoffs.\n@@ -15,6 +17,6 @@\n \n - Use Shiki terms exactly when they exist.\n - Add glossary terms to `CONTEXT.md` only when the term is part of the domain, not a generic programming concept.\n-- Create ADRs only for hard-to-reverse, surprising, tradeoff-based decisions.\n+- Create SADRs or target ADRs only for hard-to-reverse, surprising, tradeoff-based decisions.\n - Keep product-specific language in the Target Repository, not in Shiki Template docs.\n-- If an ADR conflicts with a proposed implementation, surface the conflict before editing.\n+- If a SADR or target ADR conflicts with a proposed implementation, surface the conflict before editing.\ndiff --git a/docs/agents/execution-control.md b/docs/agents/execution-control.md\nindex ad8ee3cdb36be56fedc1f46408ca92b387d74047..bc63c3a73661d12b567016ba299f1a6b66bf9088\n--- a/docs/agents/execution-control.md\n+++ b/docs/agents/execution-control.md\n@@ -63,7 +63,7 @@\n | Branch name | Orchestrator | Orchestrator | Before implementer dispatch | branch policy | branch ref in task/ledger |\n | Lock reservation | Context & Impact | MergeGate | Before dispatch and before merge | `.shiki/locks/`, labels, PR metadata | lock id, owner, scope |\n | CI profile | Planner | MergeGate | During issue triage, rechecked at PR | labels, `.shiki/policy.yaml`, workflow matrix | required check list |\n-| Required status checks | Repo maintainer / Guardian | GitHub branch protection / rulesets | Repository setup, updated via ADR | GitHub settings | branch/ruleset config, ADR |\n+| Required status checks | Repo maintainer / Guardian | GitHub branch protection / rulesets | Repository setup, updated via target ADR | GitHub settings | branch/ruleset config, target ADR |\n | CCA judgment | CCA | GitHub required check / MergeGate | Every non-draft PR update | GitHub Action check | structured CCA verdict |\n | Repair assignment | CCA | MergeGate | After failed CCA/CI/review | repair packet, issue/PR comment | repair id and attempt count |\n | Merge readiness | MergeGate | GitHub branch protection / Guardian if needed | After CCA complete | required checks, review rules, rulesets | mergegate check output |\ndiff --git a/docs/agents/guardian-policy.md b/docs/agents/guardian-policy.md\nindex 4147c7adf05a47adb4fd24ada1a1c2e1f2ba6d2d..a20d9cc6c00a569bb057b9ca8d6b51c7fc4a9847\n--- a/docs/agents/guardian-policy.md\n+++ b/docs/agents/guardian-policy.md\n@@ -98,8 +98,8 @@\n `origin/<default_branch>` and reuses each job's existing base fetch; a shallow\n (`--depth`) or second base fetch would make the proof unresolvable and silently\n disable the carry, so neither is permitted. CCA reads `guardian_comment_carried` as\n-a recorded authority in the deterministic signal and does not re-derive it. See ADR\n-0018 for the full decision, the rejected diff/patch-equality and path-disjointness\n+a recorded authority in the deterministic signal and does not re-derive it. See\n+SADR-0018 for the full decision, the rejected diff/patch-equality and path-disjointness\n alternatives, and the accepted residual.\n \n ## External AI Guardian Review (SADR-0010)\ndiff --git a/docs/agents/implementation-policy.md b/docs/agents/implementation-policy.md\nindex c754144a9fb5bf4a38aa82a60e1670c59e626b44..85c37281bcad196c6b7b02343faf00b03cd9c57a\n--- a/docs/agents/implementation-policy.md\n+++ b/docs/agents/implementation-policy.md\n@@ -31,10 +31,13 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n ## Non-Negotiables\n \n 1. **Planning precedes implementation.** Non-trivial Goals start with `grill-with-docs` and Context & Impact.\n-2. **Domain language is controlled.** Terms must be checked against `CONTEXT.md`; hard-to-reverse tradeoffs must be checked against ADRs.\n+2. **Domain language is controlled.** Terms must be checked against `CONTEXT.md`; hard-to-reverse tradeoffs must be checked against Shiki SADRs and target ADRs.\n 3. **PRD precedes issue decomposition.** `to-prd` is used once the context is settled enough to become durable intent.\n 4. **Issues are vertical slices.** `to-issues` must produce independently grabbable tracer-bullet tasks, not horizontal layer tickets.\n 5. **Triage controls dispatch.** Only `ready-for-agent` / AFK tasks may be assigned to Codex without human input.\n@@ -95,19 +98,19 @@\n Process:\n \n - Challenge terms against `CONTEXT.md`.\n-- Check ADRs before reopening decisions.\n+- Check SADRs and target ADRs before reopening decisions.\n - Inspect code when code can answer a question.\n - Ask one question at a time when the operator is available.\n - Provide a recommended answer with each question.\n - Record resolved terms in `CONTEXT.md`.\n-- Create ADRs only for hard-to-reverse, surprising, tradeoff-based decisions.\n+- Create SADRs or target ADRs only for hard-to-reverse, surprising, tradeoff-based decisions.\n - Convert unresolved load-bearing questions into HITL blockers.\n \n Outputs:\n \n - Resolved design-tree decisions.\n - Updated glossary terms when needed.\n-- ADRs when justified.\n+- SADRs or target ADRs when justified.\n - HITL blockers when decisions remain unresolved.\n \n ## Phase 3 \u2014 Context & Impact\n@@ -117,7 +120,7 @@\n \n Outputs must include:\n \n-- Relevant documents and ADRs.\n+- Relevant documents, SADRs, and target ADRs.\n - Relevant modules, interfaces, seams, callers, and tests.\n - Candidate locks.\n - Dependencies.\n@@ -140,7 +143,7 @@\n - Testing decisions.\n - Out of scope.\n - Further notes.\n-- Links to relevant Goal, domain terms, and ADRs.\n+- Links to relevant Goal, domain terms, SADRs, and target ADRs.\n \n Phase 4 ends with Spec Freeze: the operator explicitly approves the PRD, the\n plan records a `spec_freeze` block (status, approved_by, source), and the SF\n@@ -240,7 +243,7 @@\n - Dependencies and locks.\n - Risk and Guardian requirements.\n - Ledger completeness.\n-- Documentation and ADR updates.\n+- Documentation and SADR/target-ADR updates.\n - Scope drift.\n \n CCA emits one of:\n@@ -301,7 +304,7 @@\n | Evidence failure | Behavior might be done, but proof is missing. | Add checks, PR evidence, ledger entry, or artifacts. |\n | Scope drift | PR changes unrelated behavior. | Revert unrelated changes or split into new Goal. |\n | Dependency block | Upstream task or lock not complete. | Block until dependency resolves. |\n-| Architecture block | Decision needs ADR or deep-module discussion. | `grill-with-docs` or `improve-codebase-architecture`. |\n+| Architecture block | Decision needs a SADR, target ADR, or deep-module discussion. | `grill-with-docs` or `improve-codebase-architecture`. |\n | Risk block | Security/production/policy change needs human approval. | Guardian decision. |\n \n ## Phase 5.5 \u2014 Execution Decision Control\ndiff --git a/docs/agents/runtime-auth-model.md b/docs/agents/runtime-auth-model.md\nindex 6a2e1a47e81ea89b5c68a210ec07772a9374babc..3c54748d24233dbd0ba8473950282547ef4ce935\n--- a/docs/agents/runtime-auth-model.md\n+++ b/docs/agents/runtime-auth-model.md\n@@ -148,7 +148,7 @@\n \n API-key based runners may be added by a target repository as an explicit extension. They are not the default Shiki template path.\n \n-If a repo opts into API-key mode, record the exception in an ADR and update the repo's `AGENTS.md`, `.shiki/config.yaml`, and workflow permissions.\n+If a repo opts into API-key mode, record the target-specific exception in a target ADR and update the repo's `AGENTS.md`, `.shiki/config.yaml`, and workflow permissions.\n \n ## GitHub Actions Runtime Compatibility\n \ndiff --git a/docs/agents/skill-gate.md b/docs/agents/skill-gate.md\nindex c50398b0d7c60a8598f2a5c88a10015730dced13..3acffe6444617bb1d54b77d4b4708158ec735bc6\n--- a/docs/agents/skill-gate.md\n+++ b/docs/agents/skill-gate.md\n@@ -9,7 +9,7 @@\n | Skill | Trigger | Expected output |\n | --- | --- | --- |\n | `setup-matt-pocock-skills` | First configuring a repo for these skills, or when issue tracker, triage labels, or domain docs are missing. | `docs/agents/` configuration and an `Agent skills` block in `AGENTS.md` or `CLAUDE.md`. |\n-| `grill-with-docs` | Ambiguous plan, unclear terminology, boundary decision, tradeoff, or ADR-worthy design choice. | Clarified decision tree, recommended answers, updates to `CONTEXT.md` or ADRs when decisions crystallize. |\n+| `grill-with-docs` | Ambiguous plan, unclear terminology, boundary decision, tradeoff, or decision-record-worthy design choice. | Clarified decision tree, recommended answers, updates to `CONTEXT.md`, SADRs, or target ADRs when decisions crystallize. |\n | `zoom-out` | Agent lacks a higher-level architectural map. | Map of relevant modules, callers, seams, and domain concepts. |\n | `to-prd` | Settled context should become a PRD. | PRD published to the issue tracker with problem, solution, user stories, decisions, testing, and out-of-scope notes. |\n | `to-issues` | Goal, PRD, or plan must become implementation tickets. | Vertical-slice issues with acceptance criteria, dependencies, and AFK/HITL classification. |\n@@ -17,7 +17,7 @@\n | `tdd` | Feature work or bug fix has observable behavior that can be specified with tests. | Red-green-refactor loop through public interfaces, one vertical slice at a time. |\n | `code-review` | The TDD loop is green and the implementer is about to create or update a PR. Default for every implementation task. | Pre-PR self-review of the task diff with fixes applied on the task branch; findings and resolutions recorded as a ledger entry naming the skill and a `## Pre-PR code review` PR body section. |\n | `diagnose` | Hard bug, failing check, regression, flaky behavior, or performance problem. | Reproduction loop, ranked hypotheses, instrumentation, fix, regression test, post-mortem. |\n-| `improve-codebase-architecture` | Structural friction, testability, AI-navigability, deep-module opportunity, or refactor request. | Architecture candidates and deepening recommendations informed by `CONTEXT.md` and ADRs. |\n+| `improve-codebase-architecture` | Structural friction, testability, AI-navigability, deep-module opportunity, or refactor request. | Architecture candidates and deepening recommendations informed by `CONTEXT.md`, SADRs, and target ADRs. |\n | `prototype` | A throwaway logic or UI prototype is needed to answer a design question. | Clearly marked disposable prototype and captured decision. |\n \n ## MergeGate Interaction\ndiff --git a/docs/operations.md b/docs/operations.md\nindex cf1f452d4c589e38ccd58553bda0ad8684b51eb7..ace48a90ffbec9e5c0a7113bef16a4858bc44b3f\n--- a/docs/operations.md\n+++ b/docs/operations.md\n@@ -89,6 +89,16 @@\n      target history, brought forward by `shiki migrate`, not a template.\n    - **Everything else** (CLI scripts, workflows, schemas, docs) is overwritten.\n \n+   A forced upgrade also migrates the former numeric Shiki decision paths to\n+   `SADR-NNNN-*`. Before the first write, the installer inventories the exact\n+   legacy Shiki paths and authorizes each deletion only when the existing\n+   `.shiki/install-stamp.json` names that path and its SHA-256 digest still\n+   matches the file. An absent, unreadable, malformed, incomplete, or\n+   mismatching stamp reports every blocked path and stops with no filesystem\n+   change. Target-owned `NNNN-*` ADRs are not cleanup candidates. A non-force\n+   install deletes nothing and retains any still-matching legacy stamp entries\n+   so a later forced upgrade can prove ownership.\n+\n    If the target has pending migrations, `--force` refuses and writes nothing \u2014\n    apply migrations first (step 3), then re-run. The run ends with a single\n    summary of every `.new` written, so a half-upgrade is visible.\ndiff --git a/docs/releasing.md b/docs/releasing.md\nindex 10d73828e7dbd64e9d9f6b83bc84d010537ce62a..aafeee8c4d441e1e427efee2ce4f81a647f020e1\n--- a/docs/releasing.md\n+++ b/docs/releasing.md\n@@ -67,7 +67,7 @@\n CLI in `scripts/`. There are no third-party Python runtime dependencies. The CLI\n is intentionally **not** exposed as a `[project.scripts]` console entry point\n because `scripts/shiki.py` is a dependency-free shim and `scripts/` is not\n-packaged as an importable distribution (see the ADR for the rationale).\n+packaged as an importable distribution (see SADR-0007 for the rationale).\n \n Supported install paths:\n \n@@ -155,6 +155,15 @@\n    [docs/operations.md](operations.md#the-new-convention). If the target has\n    pending migrations, `--force` refuses and writes nothing; apply migrations\n    (step 2) first, then re-run.\n+\n+   The SADR namespace upgrade has an additional pre-write gate: each former\n+   numeric Shiki decision path is removed only when the existing install stamp\n+   contains that exact path and a digest matching its current bytes. The\n+   installer aggregates absent/unreadable/malformed-stamp, missing-digest, and\n+   digest-mismatch blockers before raising, so refusal leaves the target tree\n+   byte-identical. Numeric target ADRs are preserved. A successful forced\n+   upgrade installs and stamps `SADR-*` paths and drops the removed legacy paths\n+   from the refreshed stamp.\n 2. Apply repository-local `.shiki/` state migrations with the migrate command:\n \n    ```bash\ndiff --git a/docs/usage.md b/docs/usage.md\nindex b83407ee3970dd395dd893010bc5dbe8bad26051..a130ef6e40dfd8169a5bc701339351097df1a09f\n--- a/docs/usage.md\n+++ b/docs/usage.md\n@@ -67,8 +67,10 @@\n shiki install-target /path/to/target-repo --local-only\n ```\n \n-Use `--force` only when you intentionally want to overwrite existing target\n-files.\n+Use `--force` only when you intentionally want to refresh the target template.\n+For the SADR namespace migration, `--force` deletes only exact legacy Shiki\n+decision paths whose existing install-stamp digest still matches; any ownership\n+blocker stops before the first write. Numeric target ADRs are never candidates.\n \n ## `shiki bootstrap-platform` \u2014 publish the Shiki platform repo\n \ndiff --git a/scripts/guardian_approval_signal.py b/scripts/guardian_approval_signal.py\nindex d059f92b2c82aa2b5d6ca421365537a87886c1cb..16924892714f4f5b9303c0ef0e325a7e70075630\n--- a/scripts/guardian_approval_signal.py\n+++ b/scripts/guardian_approval_signal.py\n@@ -199,8 +199,8 @@\n ) -> Any:\n     \"\"\"Evaluate SADR-0015 Contract Approval for the CCA Guardian signal.\n \n-    The SAME OR that MergeGate applies must reach this deterministic signal: ADR\n-    0015 is explicit that without it, a contract-approved PR still returns\n+    The SAME OR that MergeGate applies must reach this deterministic signal:\n+    SADR-0015 is explicit that without it, a contract-approved PR still returns\n     ``needs_guardian``, the CCA job fails, and the MergeGate policy check never\n     runs \u2014 the gate this change moves would never be reached. This resolves the\n     task the SAME way MergeGate does (the first Shiki task id in the PR body),\ndiff --git a/scripts/shiki_contract_approval.py b/scripts/shiki_contract_approval.py\nindex 941bcb96fdd8b9a85bc0c4f0f04d5dad9d04839c..4a4b418f6527413161db82670bec857a043360e2\n--- a/scripts/shiki_contract_approval.py\n+++ b/scripts/shiki_contract_approval.py\n@@ -142,7 +142,7 @@\n     changed_files_status: Any,\n     registration: Any,\n ) -> ContractApprovalResult:\n-    \"\"\"Whether this PR carries an SADR-0015 Contract Approval.\n+    \"\"\"Whether this PR carries a SADR-0015 Contract Approval.\n \n     The four conditions are evaluated IN ORDER, each failure recorded as a\n     distinct reason string:\ndiff --git a/scripts/shiki_guardian.py b/scripts/shiki_guardian.py\nindex a4ecb79bbda55fd5c8b06adfae2eb99760a03c45..4dd99bbe714e26f421fa19abac7a34ae1b195ecc\n--- a/scripts/shiki_guardian.py\n+++ b/scripts/shiki_guardian.py\n@@ -12,7 +12,7 @@\n # The carry re-derives its proof from git in-process on every judging invocation;\n # it is never read from an artifact, so there is nothing to go stale or be forged.\n # shiki_sync_proof is the ONLY guardian-family module that runs git, and every one\n-# of its invocations is read-only and local (ADR: prove-from-git base sync).\n+# of its invocations is read-only and local (SADR: prove-from-git base sync).\n from shiki_sync_proof import verify_pure_base_sync\n \n GUARDIAN_POLICY_PATH = \".shiki/guardian-policy.json\"\ndiff --git a/scripts/shiki_guardian_review.py b/scripts/shiki_guardian_review.py\nindex dd0f104d457899e0ba5ff1e162011c1e90b6f92b..e10ef58c046324e7ea3a3392feb3048ca77414ce\n--- a/scripts/shiki_guardian_review.py\n+++ b/scripts/shiki_guardian_review.py\n@@ -159,7 +159,7 @@\n     docs_only = _is_docs_only(changed_files)\n     if docs_only and matched == 0:\n         focus.append(\n-            \"Docs/ADR-only change: confirm the change is genuinely documentation/decision-only, introduces no behavior or contract drift, and stays consistent with the constitution and existing ADRs.\"\n+            \"Docs/decision-record-only change: confirm the change is genuinely documentation/decision-only, introduces no behavior or contract drift, and stays consistent with the constitution and existing SADRs and target ADRs.\"\n         )\n \n     # Conservative augmentation: unknown (no category and not docs-only), mixed\ndiff --git a/scripts/shiki_installer.py b/scripts/shiki_installer.py\nindex be064ab7a29ff6836f7f7a1d70500a43e0d11d7b..9fd8eefd137ce7003dad5c0dcb2e48e2cd47ab32\n--- a/scripts/shiki_installer.py\n+++ b/scripts/shiki_installer.py\n@@ -118,6 +118,7 @@\n # what lets a target carry it without a governance-schema change.\n INSTALL_STAMP_PATH = \".shiki/install-stamp.json\"\n INSTALL_STAMP_VERSION = 1\n+LEGACY_SHIKI_SADR_MAX = 18\n \n # Under --force the shipped surface splits three ways, so nothing is lost and\n # nothing is silently stale.\n@@ -237,9 +238,98 @@\n         )\n \n \n+def legacy_shiki_adr_paths() -> tuple[str, ...]:\n+    \"\"\"Former numeric paths for Shiki SADR-0001 through SADR-0018.\n+\n+    The authoritative slugs come from the current SADR filenames. SADR-0019 is\n+    the namespace decision itself and never shipped under a numeric legacy path.\n+    \"\"\"\n+    paths: list[str] = []\n+    for sadr in sorted((ROOT / \"docs\" / \"adr\").glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\")):\n+        number = sadr.name.split(\"-\", 2)[1]\n+        if int(number) <= LEGACY_SHIKI_SADR_MAX:\n+            paths.append(f\"docs/adr/{sadr.name.removeprefix('SADR-')}\")\n+    return tuple(paths)\n+\n+\n+def preflight_legacy_sadr_cleanup(target: Path, *, force: bool) -> tuple[str, ...]:\n+    \"\"\"Authorize exact legacy Shiki ADR removals before the first write.\n+\n+    A non-force install never removes existing files. A forced install needs no\n+    stamp when no legacy Shiki path is present. When legacy paths do exist, each\n+    must be named in a readable install stamp and its current bytes must match\n+    the recorded digest.\n+    \"\"\"\n+    if not force:\n+        return ()\n+    present = tuple(\n+        relative\n+        for relative in legacy_shiki_adr_paths()\n+        if (target / relative).exists() or (target / relative).is_symlink()\n+    )\n+    if not present:\n+        return ()\n+\n+    stamp_path = target / INSTALL_STAMP_PATH\n+    try:\n+        raw = stamp_path.read_text(encoding=\"utf-8\")\n+        stamp = json.loads(raw)\n+    except FileNotFoundError:\n+        detail = \"install stamp is absent\"\n+        blockers = [f\"{relative}: {detail}\" for relative in present]\n+    except (OSError, UnicodeDecodeError) as error:\n+        detail = f\"install stamp is unreadable ({error})\"\n+        blockers = [f\"{relative}: {detail}\" for relative in present]\n+    except json.JSONDecodeError as error:\n+        detail = f\"install stamp is malformed ({error})\"\n+        blockers = [f\"{relative}: {detail}\" for relative in present]\n+    else:\n+        if not isinstance(stamp, dict):\n+            blockers = [\n+                f\"{relative}: install stamp is malformed (expected an object)\"\n+                for relative in present\n+            ]\n+        else:\n+            digests = stamp.get(\"digests\")\n+            blockers = []\n+            if not isinstance(digests, dict):\n+                blockers.extend(\n+                    f\"{relative}: install stamp has no digest for this path\"\n+                    for relative in present\n+                )\n+            else:\n+                for relative in present:\n+                    expected = digests.get(relative)\n+                    if not isinstance(expected, str) or not expected:\n+                        blockers.append(\n+                            f\"{relative}: install stamp has no digest for this path\"\n+                        )\n+                        continue\n+                    try:\n+                        actual = _sha256_file(target / relative)\n+                    except OSError as error:\n+                        blockers.append(\n+                            f\"{relative}: legacy path is unreadable ({error})\"\n+                        )\n+                        continue\n+                    if actual != expected:\n+                        blockers.append(\n+                            f\"{relative}: digest mismatch (stamp {expected}, current {actual})\"\n+                        )\n+\n+    if blockers:\n+        joined = \"\\n\".join(f\"- {blocker}\" for blocker in blockers)\n+        raise ShikiError(\n+            \"legacy Shiki ADR cleanup is not authorized:\\n\"\n+            f\"{joined}\\nNo files were written.\"\n+        )\n+    return present\n+\n+\n def install_template(target: Path, *, force: bool, validate: bool) -> list[NewFileNote]:\n     # Preconditions first: never rewrite the tree of a target that cannot be\n     # upgraded cleanly (see refuse_pending_migrations).\n+    legacy_cleanup = preflight_legacy_sadr_cleanup(target, force=force)\n     refuse_pending_migrations(target)\n \n     # Resolve the target's required CODEOWNERS owner once from its\n@@ -250,6 +340,10 @@\n     # verbatim. See ``codeowners_required_owner``.\n     codeowners_owner = codeowners_required_owner(target)\n \n+    for relative in legacy_cleanup:\n+        (target / relative).unlink()\n+        info(f\"removed stamped legacy Shiki ADR: {target / relative}\")\n+\n     notes: list[NewFileNote] = []\n     for relative in TEMPLATE_PATHS:\n         source = ROOT / relative\n@@ -647,6 +741,22 @@\n     digests = target_stamp_digests(target)\n     existing = read_install_stamp(target)\n     if existing is not None:\n+        # A non-force install deliberately leaves legacy Shiki ADRs in place.\n+        # Preserve only still-matching ownership proof so a later forced\n+        # upgrade can authorize cleanup; never carry a missing or stale digest.\n+        existing_digests = existing.get(\"digests\")\n+        if isinstance(existing_digests, dict):\n+            for relative in legacy_shiki_adr_paths():\n+                expected = existing_digests.get(relative)\n+                path = target / relative\n+                if not isinstance(expected, str) or not path.is_file():\n+                    continue\n+                try:\n+                    actual = _sha256_file(path)\n+                except OSError:\n+                    continue\n+                if actual == expected:\n+                    digests[relative] = actual\n         if _incoming_is_older(commit, existing.get(\"platform_commit\")):\n             warn(\n                 \"kept existing install stamp: incoming install \"\ndiff --git a/scripts/shiki_loop.py b/scripts/shiki_loop.py\nindex 3611641da8aeb5038b1da90e3114b09a473ec6d8..84caa491b9cad0edc4176f209c13e9221e975681\n--- a/scripts/shiki_loop.py\n+++ b/scripts/shiki_loop.py\n@@ -424,7 +424,7 @@\n         # unresolvable CCA verdict has already produced its terminal stop and is never\n         # laundered into a head-moving sync (SADR-0010). It also never fires while the\n         # policy (Guardian) gate is red \u2014 for ANY reason, not only when it is the sole\n-        # red check \u2014 so a head-bound Guardian approval, or an SADR-0015\n+        # red check \u2014 so a head-bound Guardian approval, or a SADR-0015\n         # contract-approval carry that base movement defeated (the policy gate compares\n         # base and head task governance directly), is never silently restored by\n         # adopting base wholesale. It is bounded by a durable per-task counter so the\ndiff --git a/scripts/shiki_tasks.py b/scripts/shiki_tasks.py\nindex aec3809f24b4a0c8c96fc50971cff9ae9ad30381..d531052e9d9717058d4631483cefab44c26a65c1\n--- a/scripts/shiki_tasks.py\n+++ b/scripts/shiki_tasks.py\n@@ -821,7 +821,7 @@\n         \"prompt\": prompt,\n         \"entry_skill\": \"grill-with-docs\",\n         \"required_next_steps\": [\n-            \"Run grill-with-docs until terminology, boundaries, risks, and ADR-worthy decisions are settled.\",\n+            \"Run grill-with-docs until terminology, boundaries, risks, and decision-record-worthy choices are settled.\",\n             \"Obtain the operator's explicit approval of the PRD/requirements (Spec Freeze).\",\n             \"Write a machine-readable plan JSON with grill_with_docs.status=complete and spec_freeze.status=frozen.\",\n             \"Run shiki plan ingest --plan-file PLAN.json.\",\ndiff --git a/scripts/test_shiki_goal_loop.sh b/scripts/test_shiki_goal_loop.sh\nindex 753c7b516e10f0be3bc92a4bd0eb1fa699776ae6..8f3820a0a079b3044424cfe1a6c6b7fe68896e67\n--- a/scripts/test_shiki_goal_loop.sh\n+++ b/scripts/test_shiki_goal_loop.sh\n@@ -231,8 +231,8 @@\n \n # High/critical risk merges autonomously when all required checks are green:\n # the \"MergeGate policy check\" required check IS the Guardian gate (it enforces\n-# guardian-policy.json \u2014 human approval OR external AI guardian review, ADR\n-# 0010), so green checks mean a recorded authority approved. The fake gh returns\n+# guardian-policy.json \u2014 human approval OR external AI guardian review,\n+# SADR-0010), so green checks mean a recorded authority approved. The fake gh returns\n # green checks, simulating that the Guardian gate passed.\n python3 \"$ROOT/scripts/shiki.py\" goal create --target \"$TARGET\" --title \"Guardian gate\" --outcome \"High risk merges via policy gate\" >/tmp/shiki-goal-loop-guardian-goal.json\n GGOAL=\"$(json_get /tmp/shiki-goal-loop-guardian-goal.json goal_id)\"\ndiff --git a/tests/test_adr_references.py b/tests/test_adr_references.py\nindex 51706329c797d5dd9b47e082a333fa83b261d482..d005da4f771f5e8085ec22d4b787980e68f5ae8c\n--- a/tests/test_adr_references.py\n+++ b/tests/test_adr_references.py\n@@ -1,8 +1,8 @@\n-\"\"\"Every ``ADR ####`` citation must resolve to exactly one decision record.\n+\"\"\"Every target ADR and Shiki SADR citation resolves in its own namespace.\n \n Background. ``scripts/mergegate_check.py`` justifies ``is_bookkeeping_closeout``\n-with \"ADR 0017\" nine times and ``scripts/guardian_approval_signal.py`` cites\n-0016/0017 seven times, yet for a stretch the numbering in ``docs/adr/`` stopped\n+with \"SADR-0017\" nine times and ``scripts/guardian_approval_signal.py`` cites\n+SADR-0016/SADR-0017 seven times, yet for a stretch the numbering in ``docs/adr/`` stopped\n at 0015: the authoring commits lived on a coordinator branch and no task ever\n declared ``docs/adr/...`` in its locks, so nothing carried the records to the\n default branch. The stated basis of an exemption that merged seven PRs with zero\n@@ -11,11 +11,12 @@\n and the directory drifted with no signal.\n \n This is the same unbound-pair shape behind other silent failures: a reference\n-and its target with nothing asserting they agree. This test binds them. It scans\n-every ``scripts/*.py`` (plus ``docs/**/*.md`` and the root ``*.md`` governance\n-files, which are cheap) for the ``ADR ####`` pattern and asserts each cited\n-number resolves to exactly one ``docs/adr/####-*.md``. The converse is\n-deliberately NOT required: an ADR may exist without being cited.\n+and its target with nothing asserting they agree. This test binds them. Target\n+``ADR NNNN`` citations resolve only to ``docs/adr/NNNN-*.md``; Shiki\n+``SADR-NNNN`` citations resolve only to ``docs/adr/SADR-NNNN-*.md``. The scan\n+covers current governance docs, scripts, workflows, prompts, packaging metadata,\n+and repo-local Codex guidance while deliberately excluding historical\n+``.shiki`` evidence. The converse is not required: a record may be uncited.\n \n The suite is dependency free: standard-library ``unittest`` only.\n \"\"\"\n@@ -31,17 +32,18 @@\n \n REPO_ROOT = Path(__file__).resolve().parents[1]\n \n-# \"ADR 0016\" -- a four-digit citation. The trailing \\b keeps a five-digit token\n-# from being read as a truncated four-digit number.\n-_CITATION = re.compile(r\"\\bADR (\\d{4})\\b\")\n-# \"0016-locks-bound-product-paths.md\" -- the leading four digits name the record.\n-_ADR_FILENAME = re.compile(r\"^(\\d{4})-.*\\.md$\")\n+# ``ADR 0016`` / ``SADR-0016`` -- a namespace plus four-digit citation. The\n+# trailing ``\\b`` keeps a five-digit token from being read as a truncated one.\n+_CITATION = re.compile(r\"\\b(S?ADR)[ -](\\d{4})\\b\")\n+# Target ADRs retain the numeric filename; Shiki platform decisions use SADR.\n+_DECISION_FILENAME = re.compile(r\"^(?:(SADR)-)?(\\d{4})-.*\\.md$\")\n \n \n class Violation(NamedTuple):\n     \"\"\"A citation that does not resolve to exactly one decision record.\"\"\"\n \n     citing_file: str  # repo-relative path of the file that cites the ADR\n+    namespace: str  # \"ADR\" for target decisions; \"SADR\" for Shiki decisions\n     number: str  # the cited four-digit number, e.g. \"0016\"\n     line: int  # 1-based line of the citation\n     reason: str  # \"missing\" (zero matches) or \"ambiguous\" (two or more)\n@@ -49,44 +51,59 @@\n \n     @property\n     def expected_path(self) -> str:\n-        return f\"docs/adr/{self.number}-*.md\"\n+        prefix = \"SADR-\" if self.namespace == \"SADR\" else \"\"\n+        return f\"docs/adr/{prefix}{self.number}-*.md\"\n \n     def describe(self) -> str:\n         \"\"\"Name the citing file, the cited number, and the missing path.\"\"\"\n         if self.reason == \"missing\":\n             return (\n-                f\"{self.citing_file}:{self.line} cites ADR {self.number} \"\n+                f\"{self.citing_file}:{self.line} cites {self.namespace} {self.number} \"\n                 f\"but no document matches {self.expected_path}\"\n             )\n         joined = \", \".join(self.matches)\n         return (\n-            f\"{self.citing_file}:{self.line} cites ADR {self.number} which \"\n+            f\"{self.citing_file}:{self.line} cites {self.namespace} {self.number} which \"\n             f\"resolves to {len(self.matches)} documents (expected exactly one \"\n             f\"at {self.expected_path}): {joined}\"\n         )\n \n \n-def _adr_index(root: Path) -> dict[str, list[Path]]:\n-    \"\"\"Map every four-digit ADR number to the record files that claim it.\"\"\"\n-    index: dict[str, list[Path]] = {}\n+def _adr_index(root: Path) -> dict[tuple[str, str], list[Path]]:\n+    \"\"\"Map each decision namespace and number to the files that claim it.\"\"\"\n+    index: dict[tuple[str, str], list[Path]] = {}\n     adr_dir = root / \"docs\" / \"adr\"\n     if adr_dir.is_dir():\n         for path in sorted(adr_dir.glob(\"*.md\")):\n-            match = _ADR_FILENAME.match(path.name)\n+            match = _DECISION_FILENAME.match(path.name)\n             if match:\n-                index.setdefault(match.group(1), []).append(path)\n+                namespace = \"SADR\" if match.group(1) else \"ADR\"\n+                index.setdefault((namespace, match.group(2)), []).append(path)\n     return index\n \n \n def _citing_files(root: Path, include_docs: bool = True) -> list[Path]:\n-    \"\"\"Files scanned for citations: scripts/*.py, and cheaply the markdown.\"\"\"\n+    \"\"\"Current, shipped text surfaces scanned for decision citations.\"\"\"\n     files = sorted((root / \"scripts\").glob(\"*.py\"))\n+    files += sorted((root / \"scripts\").glob(\"*.sh\"))\n     if include_docs:\n         docs = root / \"docs\"\n         if docs.is_dir():\n             files += sorted(docs.rglob(\"*.md\"))\n         files += sorted(root.glob(\"*.md\"))\n-    return files\n+        pyproject = root / \"pyproject.toml\"\n+        if pyproject.is_file():\n+            files.append(pyproject)\n+        for relative in (\".github\", \".codex\"):\n+            surface = root / relative\n+            if surface.is_dir():\n+                files += sorted(\n+                    path\n+                    for path in surface.rglob(\"*\")\n+                    if path.is_file()\n+                    and path.suffix in {\".md\", \".py\", \".sh\", \".toml\", \".yaml\", \".yml\"}\n+                )\n+    return sorted(set(files))\n \n \n def find_adr_reference_violations(\n@@ -94,10 +111,9 @@\n ) -> list[Violation]:\n     \"\"\"Return every ``ADR ####`` citation under ``root`` that fails to resolve.\n \n-    A citation resolves when exactly one ``docs/adr/####-*.md`` matches its\n-    number. Zero matches is a \"missing\" violation; two or more is \"ambiguous\".\n-    Records that are never cited produce nothing -- the converse is not\n-    required.\n+    A citation resolves when exactly one record in the same namespace matches\n+    its number. Zero matches is a \"missing\" violation; two or more is\n+    \"ambiguous\". Records that are never cited produce nothing.\n     \"\"\"\n     root = Path(root)\n     index = _adr_index(root)\n@@ -110,13 +126,15 @@\n         rel = path.relative_to(root).as_posix()\n         for lineno, line in enumerate(text.splitlines(), start=1):\n             for match in _CITATION.finditer(line):\n-                number = match.group(1)\n-                found = index.get(number, [])\n+                namespace, number = match.groups()\n+                found = index.get((namespace, number), [])\n                 if len(found) == 1:\n                     continue\n                 reason = \"missing\" if not found else \"ambiguous\"\n                 rels = [p.relative_to(root).as_posix() for p in found]\n-                violations.append(Violation(rel, number, lineno, reason, rels))\n+                violations.append(\n+                    Violation(rel, namespace, number, lineno, reason, rels)\n+                )\n     return violations\n \n \n@@ -129,8 +147,8 @@\n         self.assertEqual(\n             violations,\n             [],\n-            \"Every 'ADR ####' citation must resolve to exactly one \"\n-            f\"docs/adr/####-*.md:\\n{detail}\",\n+            \"Every ADR/SADR citation must resolve exactly once in its own \"\n+            f\"docs/adr namespace:\\n{detail}\",\n         )\n \n     def test_scan_actually_reads_scripts(self) -> None:\n@@ -142,12 +160,38 @@\n             \"the citation scan must cover scripts/*.py\",\n         )\n \n+    def test_platform_citations_do_not_use_unresolved_numeric_shorthand(self) -> None:\n+        shorthand = re.compile(r\"SADR-\\d{4}/\\d{4}\")\n+        offenders = []\n+        for path in _citing_files(REPO_ROOT):\n+            text = path.read_text(encoding=\"utf-8\")\n+            if shorthand.search(text):\n+                offenders.append(path.relative_to(REPO_ROOT).as_posix())\n+        self.assertEqual(\n+            offenders,\n+            [],\n+            \"repeat the SADR prefix so every cited number is independently resolvable\",\n+        )\n+\n     def test_carried_records_are_indexed(self) -> None:\n         # The two records this Goal carried onto the default branch, and which\n         # the scripts cite most, must be present.\n         index = _adr_index(REPO_ROOT)\n-        self.assertIn(\"0016\", index)\n-        self.assertIn(\"0017\", index)\n+        self.assertIn((\"SADR\", \"0016\"), index)\n+        self.assertIn((\"SADR\", \"0017\"), index)\n+\n+    def test_platform_tree_has_only_sadr_0001_through_0019(self) -> None:\n+        adr_dir = REPO_ROOT / \"docs\" / \"adr\"\n+        sadrs = sorted(adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\"))\n+        self.assertEqual(\n+            {path.name.split(\"-\", 2)[1] for path in sadrs},\n+            {f\"{number:04d}\" for number in range(1, 20)},\n+        )\n+        self.assertEqual(\n+            list(adr_dir.glob(\"[0-9][0-9][0-9][0-9]-*.md\")),\n+            [],\n+            \"the Shiki platform tree must not retain legacy numeric SADR paths\",\n+        )\n \n \n class FixtureAdrReferences(unittest.TestCase):\n@@ -180,6 +224,26 @@\n         self.assertIn(\"9991\", message)  # cited number\n         self.assertIn(\"docs/adr/9991-*.md\", message)  # missing path\n \n+    def test_target_adr_does_not_satisfy_missing_platform_sadr(self) -> None:\n+        root = self._make_repo(\n+            adr_names=[\"0042-target-decision.md\"],\n+            script_body='\"\"\"Target ADR 0042 exists, but platform SADR-0042 does not.\"\"\"\\n',\n+        )\n+        violations = find_adr_reference_violations(root, include_docs=False)\n+        self.assertEqual(len(violations), 1)\n+        self.assertEqual(violations[0].namespace, \"SADR\")\n+        self.assertEqual(violations[0].number, \"0042\")\n+        self.assertEqual(violations[0].expected_path, \"docs/adr/SADR-0042-*.md\")\n+\n+    def test_target_adr_and_platform_sadr_may_reuse_a_number(self) -> None:\n+        root = self._make_repo(\n+            adr_names=[\"0042-target-decision.md\", \"SADR-0042-platform-decision.md\"],\n+            script_body='\"\"\"Target ADR 0042 and platform SADR-0042 are distinct.\"\"\"\\n',\n+        )\n+        self.assertEqual(\n+            find_adr_reference_violations(root, include_docs=False), []\n+        )\n+\n     def test_uncited_adr_is_allowed(self) -> None:\n         # An ADR exists but nothing cites it -> not a violation. The converse\n         # (every ADR must be cited) is explicitly NOT required.\ndiff --git a/tests/test_guardian_review.py b/tests/test_guardian_review.py\nindex 88829e7810212635e2fc98aa5918d6f26294677d..00a85a237acd5bf2d6cb5b294128198c422402ca\n--- a/tests/test_guardian_review.py\n+++ b/tests/test_guardian_review.py\n@@ -162,7 +162,7 @@\n \n     def test_docs_only_is_light_and_not_conservative(self):\n         focus = gr.classify_review_focus([\"docs/adr/0099-x.md\", \"README.md\"], \"\", \"low\")\n-        self.assertTrue(any(\"Docs/ADR-only\" in f for f in focus))\n+        self.assertTrue(any(\"Docs/decision-record-only\" in f for f in focus))\n         self.assertFalse(any(\"conservative default\" in f.lower() for f in focus))\n \n     def test_unknown_category_is_conservative(self):\ndiff --git a/tests/test_policy_job_merged_prs.py b/tests/test_policy_job_merged_prs.py\nindex d45512e74681e5e649279236c7d4e79d5c3da471..b97989f8bd74d091de1a6b5275a20d8dc68c4512\n--- a/tests/test_policy_job_merged_prs.py\n+++ b/tests/test_policy_job_merged_prs.py\n@@ -3,8 +3,8 @@\n \n ``.github/workflows/shiki-cca-completion.yml`` has two consumers of the merged-PR\n proof: the CCA *signal* (``guardian_approval_signal.py`` in \"Prepare CCA context\")\n-and the authoritative gate (``mergegate_check.py`` in \"Enforce MergeGate\"). ADR\n-0017's bookkeeping-closeout exemption (condition 6) needs the merged implementation\n+and the authoritative gate (``mergegate_check.py`` in \"Enforce MergeGate\").\n+SADR-0017's bookkeeping-closeout exemption (condition 6) needs the merged implementation\n PR proven, and a closeout carries NO ``post_merge_reconcile`` marker. The signal\n already resolves ``MERGED_PRS`` unconditionally, but \"Enforce MergeGate\" used to\n resolve it only inside ``grep -q '<!-- shiki:post_merge_reconcile -->'``. So a\ndiff --git a/tests/test_sadr_install_migration.py b/tests/test_sadr_install_migration.py\nnew file mode 100644\nindex 0000000000000000000000000000000000000000..d51eac872b1c79ff4579c4c1401ca5d775a60d8d\n--- /dev/null\n+++ b/tests/test_sadr_install_migration.py\n@@ -0,0 +1,237 @@\n+\"\"\"SADR installation and collision-safe legacy upgrade behavior.\"\"\"\n+\n+from __future__ import annotations\n+\n+import contextlib\n+import hashlib\n+import io\n+import json\n+import shutil\n+import tempfile\n+import unittest\n+from pathlib import Path\n+\n+import shiki_test_support  # noqa: F401  (path bootstrap)\n+\n+import shiki_installer\n+from tests.test_adr_references import find_adr_reference_violations\n+\n+\n+PRODUCT_NUMBERS = tuple(range(13, 19))\n+\n+\n+def _quiet_install(target: Path, *, force: bool, validate: bool) -> None:\n+    with contextlib.redirect_stdout(io.StringIO()):\n+        shiki_installer.install_template(target, force=force, validate=validate)\n+\n+\n+def _sha256(path: Path) -> str:\n+    return hashlib.sha256(path.read_bytes()).hexdigest()\n+\n+\n+def _legacy_sadr_paths() -> tuple[str, ...]:\n+    return tuple(\n+        f\"docs/adr/{path.name.removeprefix('SADR-')}\"\n+        for path in sorted((shiki_installer.ROOT / \"docs\" / \"adr\").glob(\"SADR-*.md\"))\n+        if path.name != \"SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\"\n+    )\n+\n+\n+def _create_legacy_upgrade_fixture(\n+    target: Path,\n+) -> tuple[dict[str, bytes], tuple[str, ...]]:\n+    adr_dir = target / \"docs\" / \"adr\"\n+    adr_dir.mkdir(parents=True)\n+    product_bytes = {\n+        f\"{number:04d}-cubeshare-product-decision.md\": (\n+            f\"# CubeShare ADR {number:04d}\\n\\nDo not rewrite {number}.\\n\"\n+        ).encode()\n+        for number in PRODUCT_NUMBERS\n+    }\n+    for name, content in product_bytes.items():\n+        (adr_dir / name).write_bytes(content)\n+\n+    legacy_paths = _legacy_sadr_paths()\n+    for relative in legacy_paths:\n+        (target / relative).write_bytes(f\"legacy Shiki bytes for {relative}\\n\".encode())\n+\n+    stamp_path = target / shiki_installer.INSTALL_STAMP_PATH\n+    stamp_path.parent.mkdir(parents=True, exist_ok=True)\n+    stamp = {\n+        \"version\": shiki_installer.INSTALL_STAMP_VERSION,\n+        \"platform_commit\": None,\n+        \"installed_at\": \"2000-01-01T00:00:00+00:00\",\n+        \"digests\": {\n+            relative: _sha256(target / relative) for relative in legacy_paths\n+        },\n+    }\n+    stamp_path.write_text(\n+        json.dumps(stamp, indent=2, sort_keys=True) + \"\\n\", encoding=\"utf-8\"\n+    )\n+    return product_bytes, legacy_paths\n+\n+\n+def _tree_snapshot(root: Path) -> dict[str, tuple[str, bytes | str]]:\n+    \"\"\"Recursive byte/shape snapshot used to prove preflight is write-free.\"\"\"\n+    snapshot: dict[str, tuple[str, bytes | str]] = {}\n+    for path in sorted(root.rglob(\"*\")):\n+        relative = path.relative_to(root).as_posix()\n+        if path.is_symlink():\n+            snapshot[relative] = (\"symlink\", path.readlink().as_posix())\n+        elif path.is_dir():\n+            snapshot[relative] = (\"directory\", \"\")\n+        else:\n+            snapshot[relative] = (\"file\", path.read_bytes())\n+    return snapshot\n+\n+\n+class FreshSadrInstallTests(unittest.TestCase):\n+    def setUp(self) -> None:\n+        self.tmp = Path(tempfile.mkdtemp(prefix=\"shiki-sadr-fresh-\"))\n+        self.addCleanup(lambda: shutil.rmtree(self.tmp, ignore_errors=True))\n+        self.target = self.tmp / \"target\"\n+        self.adr_dir = self.target / \"docs\" / \"adr\"\n+        self.adr_dir.mkdir(parents=True)\n+        self.product_bytes = {\n+            f\"{number:04d}-product-decision.md\": (\n+                f\"# Product ADR {number:04d}\\n\\nTarget-owned bytes {number}.\\n\"\n+            ).encode()\n+            for number in PRODUCT_NUMBERS\n+        }\n+        for name, content in self.product_bytes.items():\n+            (self.adr_dir / name).write_bytes(content)\n+\n+    def test_fresh_install_preserves_product_adrs_and_installs_all_sadrs(self) -> None:\n+        _quiet_install(self.target, force=False, validate=True)\n+\n+        for name, expected in self.product_bytes.items():\n+            self.assertEqual((self.adr_dir / name).read_bytes(), expected)\n+\n+        sadr_numbers = {\n+            path.name.split(\"-\", 2)[1]\n+            for path in self.adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\")\n+        }\n+        self.assertEqual(sadr_numbers, {f\"{number:04d}\" for number in range(1, 20)})\n+\n+        numeric_records = {\n+            path.name\n+            for path in self.adr_dir.glob(\"[0-9][0-9][0-9][0-9]-*.md\")\n+        }\n+        self.assertEqual(numeric_records, set(self.product_bytes))\n+        stamp = json.loads(\n+            (self.target / shiki_installer.INSTALL_STAMP_PATH).read_text(\n+                encoding=\"utf-8\"\n+            )\n+        )\n+        for path in self.adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\"):\n+            relative = path.relative_to(self.target).as_posix()\n+            self.assertEqual(stamp[\"digests\"][relative], _sha256(path))\n+        self.assertEqual(find_adr_reference_violations(self.target), [])\n+\n+\n+class LegacySadrUpgradeTests(unittest.TestCase):\n+    def setUp(self) -> None:\n+        self.tmp = Path(tempfile.mkdtemp(prefix=\"shiki-sadr-upgrade-\"))\n+        self.addCleanup(lambda: shutil.rmtree(self.tmp, ignore_errors=True))\n+        self.target = self.tmp / \"target\"\n+        self.adr_dir = self.target / \"docs\" / \"adr\"\n+        self.product_bytes, self.legacy_paths = _create_legacy_upgrade_fixture(\n+            self.target\n+        )\n+\n+    def test_force_upgrade_deletes_only_stamped_legacy_sadrs(self) -> None:\n+        _quiet_install(self.target, force=True, validate=True)\n+\n+        for relative in self.legacy_paths:\n+            self.assertFalse((self.target / relative).exists(), relative)\n+        for name, expected in self.product_bytes.items():\n+            self.assertEqual((self.adr_dir / name).read_bytes(), expected)\n+\n+        sadrs = list(self.adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\"))\n+        self.assertEqual(len(sadrs), 19)\n+        refreshed = json.loads(\n+            (self.target / shiki_installer.INSTALL_STAMP_PATH).read_text(encoding=\"utf-8\")\n+        )\n+        for relative in self.legacy_paths:\n+            self.assertNotIn(relative, refreshed[\"digests\"])\n+        for path in sadrs:\n+            relative = path.relative_to(self.target).as_posix()\n+            self.assertEqual(refreshed[\"digests\"][relative], _sha256(path))\n+\n+    def test_non_force_install_never_deletes_legacy_files(self) -> None:\n+        before = {\n+            relative: (self.target / relative).read_bytes()\n+            for relative in self.legacy_paths\n+        }\n+        _quiet_install(self.target, force=False, validate=False)\n+        for relative, expected in before.items():\n+            self.assertEqual((self.target / relative).read_bytes(), expected)\n+        refreshed = json.loads(\n+            (self.target / shiki_installer.INSTALL_STAMP_PATH).read_text(encoding=\"utf-8\")\n+        )\n+        for relative in self.legacy_paths:\n+            self.assertEqual(refreshed[\"digests\"][relative], _sha256(self.target / relative))\n+\n+\n+class LegacyCleanupPreflightTests(unittest.TestCase):\n+    def setUp(self) -> None:\n+        self.tmp = Path(tempfile.mkdtemp(prefix=\"shiki-sadr-preflight-\"))\n+        self.addCleanup(lambda: shutil.rmtree(self.tmp, ignore_errors=True))\n+\n+    def test_all_ownership_blockers_abort_before_any_write(self) -> None:\n+        cases = (\n+            (\"absent\", \"install stamp is absent\"),\n+            (\"unreadable\", \"install stamp is unreadable\"),\n+            (\"malformed\", \"install stamp is malformed\"),\n+            (\"missing-digest\", \"install stamp has no digest\"),\n+            (\"digest-mismatch\", \"digest mismatch\"),\n+        )\n+        for name, expected_reason in cases:\n+            with self.subTest(name=name):\n+                target = self.tmp / name\n+                _, legacy_paths = _create_legacy_upgrade_fixture(target)\n+                stamp_path = target / shiki_installer.INSTALL_STAMP_PATH\n+\n+                if name == \"absent\":\n+                    stamp_path.unlink()\n+                elif name == \"unreadable\":\n+                    stamp_path.unlink()\n+                    stamp_path.mkdir()\n+                elif name == \"malformed\":\n+                    stamp_path.write_text(\"{not-json\\n\", encoding=\"utf-8\")\n+                else:\n+                    stamp = json.loads(stamp_path.read_text(encoding=\"utf-8\"))\n+                    if name == \"missing-digest\":\n+                        stamp[\"digests\"] = {}\n+                    else:\n+                        stamp[\"digests\"] = {\n+                            relative: \"deadbeef\" for relative in legacy_paths\n+                        }\n+                    stamp_path.write_text(\n+                        json.dumps(stamp, indent=2, sort_keys=True) + \"\\n\",\n+                        encoding=\"utf-8\",\n+                    )\n+\n+                before = _tree_snapshot(target)\n+                with self.assertRaises(shiki_installer.ShikiError) as raised:\n+                    _quiet_install(target, force=True, validate=False)\n+                after = _tree_snapshot(target)\n+\n+                self.assertEqual(before, after, f\"{name} preflight changed the tree\")\n+                message = str(raised.exception)\n+                self.assertIn(expected_reason, message)\n+                self.assertIn(\"No files were written\", message)\n+                for relative in legacy_paths:\n+                    self.assertIn(relative, message)\n+\n+    def test_absent_stamp_is_allowed_when_no_legacy_path_exists(self) -> None:\n+        target = self.tmp / \"no-legacy\"\n+        target.mkdir()\n+        _quiet_install(target, force=True, validate=False)\n+        self.assertTrue(\n+            (target / \"docs\" / \"adr\" / \"SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\").is_file()\n+        )\n+\n+\n+if __name__ == \"__main__\":\n+    unittest.main()\n\n succeeded in 4880ms:\nadversarial state/evidence/lock regression suite passed\n\nexec\n/bin/zsh -lc 'python3 scripts/validate_shiki.py --target .' in /private/tmp/shiki-adr-collision.pa0KwY/.worktrees/shiki-t-20260807t073307221254z-2d22d654-introduc\ndiff --git a/.codex/skills/shiki/SKILL.md b/.codex/skills/shiki/SKILL.md\nindex c8e2e85d9d75d49cc4f2d6286ee22659c4e53e75..93cded623b2c15d9f65c76491a1c2d813ac9e6c0\n--- a/.codex/skills/shiki/SKILL.md\n+++ b/.codex/skills/shiki/SKILL.md\n@@ -18,6 +18,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n Codex CLI does not register this skill as a custom `/shiki` slash command. When\n the user types `/shiki` in Codex CLI and gets \"Unrecognized command\", explain\n that this is expected: invoke Shiki in Codex with natural language such as\n@@ -69,7 +72,7 @@\n \n ## Responsibilities\n \n-- Claude Code implements and repairs by default (`shiki runner claude`, ADR 0008); Codex implements and repairs tasks explicitly assigned to `codex`.\n+- Claude Code implements and repairs by default (`shiki runner claude`, SADR-0008); Codex implements and repairs tasks explicitly assigned to `codex`.\n - Claude Code Action can act as GitHub-side CCA or reviewer.\n - CCA judges completion.\n - MergeGate authorizes state transitions and merge readiness.\ndiff --git a/.github/prompts/cca-completion-check.md b/.github/prompts/cca-completion-check.md\nindex ed203ad5f302ecc81372d8ab27d2b1232ca97497..fd89950528333ef6242e6d5443904f5392ab7e56\n--- a/.github/prompts/cca-completion-check.md\n+++ b/.github/prompts/cca-completion-check.md\n@@ -19,6 +19,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n ## Required Reading\n \n Read only the files needed to judge the PR contract. Prefer this order:\ndiff --git a/.github/prompts/codex-task-handoff.md b/.github/prompts/codex-task-handoff.md\nindex 695e211d76dea1c60e8d5bb360e78487808286a4..59441d576b01a39569722f226c854e5a740e9eb5\n--- a/.github/prompts/codex-task-handoff.md\n+++ b/.github/prompts/codex-task-handoff.md\n@@ -2,7 +2,7 @@\n \n You are Codex Front acting as the Implementer for a Shiki task.\n \n-Follow `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant ADRs, and `docs/agents/codex-handoff.md`.\n+Follow `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant Shiki SADRs and target ADRs, and `docs/agents/codex-handoff.md`.\n \n Rules:\n \ndiff --git a/AGENTS.md b/AGENTS.md\nindex 7f041100d49c2a7525f51cb0f5d696a4a6d1c78c..a06bfbabc6fa48a007389e25e12d7ed523936bed\n--- a/AGENTS.md\n+++ b/AGENTS.md\n@@ -22,7 +22,7 @@\n - **Codex Front** is an optional implementer through Codex App, Codex CLI, Codex IDE extension, or Codex Web signed in with ChatGPT OAuth/subscription auth, dispatched with `shiki runner codex` when a task is explicitly assigned to `codex`.\n - **GitHub CCA** is implemented by Claude Code Action by default, using `CLAUDE_CODE_OAUTH_TOKEN`.\n \n-Do not assume `openai/codex-action`, `OPENAI_API_KEY`, or API-key based Codex automation in the default Shiki path. API-key runners are explicit target-repo extensions and require their own ADR.\n+Do not assume `openai/codex-action`, `OPENAI_API_KEY`, or API-key based Codex automation in the default Shiki path. API-key runners are explicit target-repo extensions and require their own target ADR.\n \n Shiki is not:\n \n@@ -44,10 +44,14 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use\n+`SADR-NNNN-*.md`; target product decisions retain `NNNN-*.md` and the `ADR`\n+namespace.\n+\n Use the canonical source-of-truth order above. Runtime-specific reading orders\n are operational guidance only and must not override or reorder it.\n \n-Conversation state is not durable truth. If a decision matters, put it in GitHub, `.shiki/`, `CONTEXT.md`, or an ADR.\n+Conversation state is not durable truth. If a decision matters, put it in GitHub, `.shiki/`, `CONTEXT.md`, a Shiki SADR, or a target ADR.\n \n If GitHub and `.shiki/` disagree, surface the conflict and prefer GitHub operational state until the mirror is repaired.\n \n@@ -75,8 +79,8 @@\n Every non-trivial change follows this loop:\n \n 1. **Goal**: clarify outcome, completion conditions, non-goals, risk level, and success signals.\n-2. **grill-with-docs**: challenge the plan against domain docs, ADRs, code reality, terminology, and edge scenarios.\n-3. **Context & Impact**: find relevant docs, ADRs, code, dependencies, owners, locks, and verification surfaces.\n+2. **grill-with-docs**: challenge the plan against domain docs, Shiki SADRs, target ADRs, code reality, terminology, and edge scenarios.\n+3. **Context & Impact**: find relevant docs, SADRs, target ADRs, code, dependencies, owners, locks, and verification surfaces.\n 4. **PRD and Spec Freeze**: use `to-prd` when context is settled enough to become durable product/engineering intent. The operator's explicit approval of the PRD is Spec Freeze: the plan records a `spec_freeze` block, and no plan runs without it. Steps 1-4 form Requirements Definition \u2014 one continuous operator dialogue. After Spec Freeze, scope changes require an operator-approved Spec Amendment; non-scope-moving interpretations go to the Assumption Log.\n 5. **Issues**: use `to-issues` to create independently grabbable vertical-slice issues. Prefer AFK slices over HITL slices where possible.\n 6. **Triage**: label issues for readiness, risk, runtime, skills, and MergeGate state.\n@@ -167,7 +171,7 @@\n - Testing decisions.\n - Out-of-scope items.\n - Further notes.\n-- Links to relevant domain terms and ADRs.\n+- Links to relevant domain terms, SADRs, and target ADRs.\n \n Every executable task must include:\n \n@@ -193,7 +197,7 @@\n \n Context & Impact must identify:\n \n-- Relevant documents, domain terms, ADRs, and past decisions.\n+- Relevant documents, domain terms, SADRs, target ADRs, and past decisions.\n - Relevant modules, interfaces, seams, callers, owners, and tests.\n - Dependency relationships between tasks.\n - Candidate locks and lock conflicts.\n@@ -210,7 +214,7 @@\n | Skill | Required when |\n | --- | --- |\n | `setup-matt-pocock-skills` | First configuring the repo for skills, or when issue tracker, triage labels, or domain docs are missing. |\n-| `grill-with-docs` | Default for non-trivial Goals before PRD/issues; always required when plans, terminology, boundaries, tradeoffs, or ADR-worthy decisions are ambiguous. |\n+| `grill-with-docs` | Default for non-trivial Goals before PRD/issues; always required when plans, terminology, boundaries, tradeoffs, or decision-record-worthy choices are ambiguous. |\n | `zoom-out` | The agent lacks an architectural map of the relevant code area. |\n | `to-prd` | Settled Goal context must become durable product/engineering intent. |\n | `to-issues` | A Goal, PRD, or plan must become independently grabbable vertical-slice issues. |\n@@ -348,7 +352,7 @@\n - Three or more new files.\n - Any high-risk or critical label.\n \n-When the architecture gate triggers, run `grill-with-docs` or `improve-codebase-architecture` as appropriate and record durable decisions in an ADR when needed.\n+When the architecture gate triggers, run `grill-with-docs` or `improve-codebase-architecture` as appropriate and record durable decisions in a Shiki SADR or target ADR as applicable.\n \n ## Engineering Discipline\n \ndiff --git a/CLAUDE.md b/CLAUDE.md\nindex 98a932e7841a632866fcd53b0bad0e3af3711d3d..98a31399404d6fb044b4fd493f4459ac87f439de\n--- a/CLAUDE.md\n+++ b/CLAUDE.md\n@@ -14,6 +14,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n ## Claude Role\n \n Default to planner, default implementer, reviewer, coordinator, documentation editor, CCA-style completion judge, and final-judgment assistant.\n@@ -57,7 +60,7 @@\n At the start of a Shiki session:\n \n 1. Read `AGENTS.md`.\n-2. Read `CONTEXT.md` and relevant ADRs.\n+2. Read `CONTEXT.md` and relevant Shiki SADRs and target ADRs.\n 3. Read active `.shiki/` Goal/task/ledger state when present.\n 4. Read active distilled rules from `.shiki/memories/` (status `distilled`, `active: true`, not revoked or superseded) for applicable guidance; the same rules are injected deterministically into task handoffs as the `## Distilled Rules` section (\u00a73.5).\n 5. Read the GitHub Issue or PR when the task is GitHub-backed.\n@@ -74,7 +77,7 @@\n - Define the Goal, completion conditions, non-goals, and risk level.\n - Treat `grill-with-docs` as the default planning entry point for non-trivial Goals.\n - Challenge domain terms against `CONTEXT.md`.\n-- Challenge hard-to-reverse tradeoffs against ADRs.\n+- Challenge hard-to-reverse tradeoffs against SADRs and target ADRs.\n - Explore code instead of asking when code can answer the question.\n - Run Context & Impact before implementation planning.\n - Produce a Task DAG, not an unordered checklist.\n@@ -94,7 +97,7 @@\n - For each question, provide your recommended answer.\n - Resolve dependency decisions before downstream design questions.\n - Update `CONTEXT.md` inline when terminology crystallizes.\n-- Offer ADRs only when the decision is hard to reverse, surprising without context, and a real tradeoff.\n+- Offer a SADR or target ADR only when the decision is hard to reverse, surprising without context, and a real tradeoff.\n - Convert unresolved questions into HITL blockers rather than allowing Codex to guess.\n \n ## PRD and Issues Standard\n@@ -103,7 +106,7 @@\n \n - Use the project's domain glossary vocabulary.\n - Avoid volatile file paths unless they are essential.\n-- Include implementation decisions, testing decisions, out-of-scope items, and relevant ADR/domain links.\n+- Include implementation decisions, testing decisions, out-of-scope items, and relevant SADR/target-ADR/domain links.\n - Identify deep-module opportunities and testable interfaces.\n \n When creating issues:\n@@ -121,7 +124,7 @@\n - Branch/worktree target.\n - Scope and non-goals.\n - Dependencies and locks.\n-- Relevant docs, ADRs, modules, and tests.\n+- Relevant docs, SADRs, target ADRs, modules, and tests.\n - Required skill invocations.\n - TDD expectations.\n - Pre-PR `code-review` gate expectations.\n@@ -152,7 +155,7 @@\n \n - Treat yourself as a judge, not implementer.\n - Do not write production code.\n-- Read `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant ADRs, PRD, task issue, PR body, diff, checks, reviews, labels, and `.shiki/` state.\n+- Read `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant SADRs and target ADRs, PRD, task issue, PR body, diff, checks, reviews, labels, and `.shiki/` state.\n - Evaluate every applicable checklist in `docs/agents/checklists.md`.\n - Map each acceptance criterion to durable evidence.\n - Distinguish implementation failure from missing evidence.\ndiff --git a/CONTEXT.md b/CONTEXT.md\nindex e61a9be06a0cce0d026484083a657bde2067e234..237ee8aa3ea8fab334ece49768bc5ed3b5af0974\n--- a/CONTEXT.md\n+++ b/CONTEXT.md\n@@ -25,7 +25,7 @@\n _Avoid_: jumping directly into code\n \n **grill-with-docs**:\n-A planning skill that challenges a plan against domain language, ADRs, code reality, and concrete edge scenarios. It resolves design-tree questions before PRD/issues. It runs inside Requirements Definition.\n+A planning skill that challenges a plan against domain language, Shiki SADRs, target ADRs, code reality, and concrete edge scenarios. It resolves design-tree questions before PRD/issues. It runs inside Requirements Definition.\n _Avoid_: generic brainstorming, silent assumptions\n \n **Requirements Definition**:\ndiff --git a/CONTRIBUTING.md b/CONTRIBUTING.md\nindex c6d9b656e73abb8a55e5f660123e03153dcb3e6d..9e37a8fbf668db5b22461aaae5548b8f1354394c\n--- a/CONTRIBUTING.md\n+++ b/CONTRIBUTING.md\n@@ -17,7 +17,7 @@\n 1. **Goal** \u2014 start from a user-approved Goal with an outcome, completion\n    conditions, non-goals, risk level, and success signals.\n 2. **grill-with-docs** \u2014 for any non-trivial change, challenge the plan against\n-   domain language, ADRs, and code reality before writing a PRD.\n+   domain language, Shiki SADRs, target ADRs, and code reality before writing a PRD.\n 3. **PRD** \u2014 capture settled product/engineering intent (`to-prd`).\n 4. **Spec Freeze** \u2014 the operator explicitly approves the PRD/requirements;\n    the plan records a `spec_freeze` block and no plan runs without it\n@@ -94,7 +94,7 @@\n   entries.\n - Do not hand-edit `.shiki/goals/*.json` or `.shiki/manifest.json` in a task PR.\n - Conversation state is not durable truth. If a decision matters, record it in\n-  GitHub, `.shiki/`, `CONTEXT.md`, or an ADR.\n+  GitHub, `.shiki/`, `CONTEXT.md`, a Shiki SADR, or a target ADR.\n \n ## Architecture Gate\n \n@@ -103,7 +103,7 @@\n or CI policy, package structure, three or more structural files, more than 100\n net new lines in a core module, three or more new files, or any high-risk label.\n When triggered, run `grill-with-docs` or `improve-codebase-architecture` and\n-record durable decisions in an ADR when needed (see `docs/adr/`).\n+record durable decisions in a Shiki SADR or target ADR as applicable (see `docs/adr/`).\n \n ## Safety\n \ndiff --git a/SYSTEM_PROMPT.md b/SYSTEM_PROMPT.md\nindex aaaea45e3a896c3a44ba89eec8bb84325ecf6c62..da009c86a88285269dee0155e58e550c6d744ea9\n--- a/SYSTEM_PROMPT.md\n+++ b/SYSTEM_PROMPT.md\n@@ -30,6 +30,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n Use the canonical source-of-truth order above. Runtime-specific reading orders\n are operational guidance only and must not override or reorder it. The current\n conversation is non-durable operator input.\n@@ -48,14 +51,14 @@\n \n A runtime may only do work that is explicitly assigned by the Goal, task, PR, or MergeGate decision.\n \n-Do not assume `openai/codex-action`, `OPENAI_API_KEY`, or API-key based Codex automation in the default Shiki path. API-key runners are explicit target-repo extensions and require their own ADR.\n+Do not assume `openai/codex-action`, `OPENAI_API_KEY`, or API-key based Codex automation in the default Shiki path. API-key runners are explicit target-repo extensions and require their own target ADR.\n \n ## 4. Non-negotiable Shiki flow\n \n For any non-trivial Goal, use this flow:\n \n 1. **Goal Seek**: define outcome, non-goals, risk level, completion criteria, and evidence requirements.\n-2. **grill-with-docs**: challenge the plan against `CONTEXT.md`, ADRs, code reality, terminology, boundaries, and edge scenarios. Ask one question at a time when the operator is available. If code can answer a question, inspect code instead of asking.\n+2. **grill-with-docs**: challenge the plan against `CONTEXT.md`, Shiki SADRs, target ADRs, code reality, terminology, boundaries, and edge scenarios. Ask one question at a time when the operator is available. If code can answer a question, inspect code instead of asking.\n 3. **Context & Impact**: identify modules, interfaces, seams, callers, dependencies, locks, risk, and verification surfaces.\n 4. **to-prd + Spec Freeze**: turn settled context into a PRD using domain vocabulary and testing decisions. The operator's explicit approval of the PRD is Spec Freeze, recorded as the plan's `spec_freeze` block; plans without it do not run. Steps 1-4 form Requirements Definition.\n 5. **to-issues**: decompose the PRD into independently grabbable vertical-slice issues. Prefer AFK slices; mark HITL slices when judgment is still required.\n@@ -75,7 +78,7 @@\n Before making any material change:\n \n 1. Read `AGENTS.md` and any runtime-specific wrapper such as `CLAUDE.md`.\n-2. Read `CONTEXT.md` and relevant ADRs.\n+2. Read `CONTEXT.md` and relevant SADRs and target ADRs.\n 3. Read the GitHub Issue / PR and `.shiki/` state for the active Goal or task.\n 4. Identify your role: Planner, Implementer, Reviewer, Completion Check Agent, Repairer, or Guardian-assist.\n 5. Identify Goal id, task id, branch, PR, dependencies, locks, risk level, required skills, and required evidence.\n@@ -103,7 +106,7 @@\n \n At minimum, identify:\n \n-- Relevant documents, ADRs, domain terms, and prior decisions.\n+- Relevant documents, SADRs, target ADRs, domain terms, and prior decisions.\n - Relevant modules, interfaces, seams, callers, owners, and tests.\n - Dependency relationships between tasks.\n - Candidate file locks and lock conflicts.\n@@ -152,7 +155,7 @@\n \n CCA must read:\n \n-- `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant ADRs.\n+- `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant SADRs and target ADRs.\n - Parent Goal issue, PRD issue, task issue, comments, and labels.\n - PR body, diff, commits, changed files, reviews, CI checks, and artifacts available to the workflow.\n - `.shiki/` task contract, locks, ledger entries, CCA checklist profile, and prior repair packets.\n@@ -214,7 +217,7 @@\n - Three or more new files.\n - Any high-risk or critical label.\n \n-When the architecture gate triggers, run `grill-with-docs` or `improve-codebase-architecture` as appropriate and record the decision in an ADR if it is hard to reverse or likely to be re-litigated.\n+When the architecture gate triggers, run `grill-with-docs` or `improve-codebase-architecture` as appropriate and record the decision in a Shiki SADR or target ADR as applicable if it is hard to reverse or likely to be re-litigated.\n \n ## 14. Skill Gate\n \n@@ -223,7 +226,7 @@\n Use these skills:\n \n - `setup-matt-pocock-skills`: before first use of the engineering skill set, or when issue tracker, triage labels, or domain docs are missing.\n-- `grill-with-docs`: default for non-trivial Goals before PRD/issues; required for ambiguous plans, terminology, boundaries, ADR-worthy tradeoffs, or design-tree decisions.\n+- `grill-with-docs`: default for non-trivial Goals before PRD/issues; required for ambiguous plans, terminology, boundaries, decision-record-worthy tradeoffs, or design-tree decisions.\n - `zoom-out`: unfamiliar code area or missing architectural map.\n - `to-prd`: convert settled context into a PRD.\n - `to-issues`: break a PRD, plan, or Goal into independently grabbable vertical-slice issues.\ndiff --git a/docs/adr/0001-keep-shiki-as-platform-name.md b/docs/adr/SADR-0001-keep-shiki-as-platform-name.md\nindex 20a9637c3b9d8c97de7ba8db7cfae4d82d9a7dba..8b685439959aada1b221c112f0ac3c51ca8a1695\n--- a/docs/adr/0001-keep-shiki-as-platform-name.md\n+++ b/docs/adr/SADR-0001-keep-shiki-as-platform-name.md\n@@ -1,3 +1,3 @@\n-# Keep Shiki As The Platform Name\n+# SADR-0001: Keep Shiki As The Platform Name\n \n Shiki remains the product name for the GitHub-native agentic engineering control plane. We considered more descriptive names such as MergePilot, MergeGate, and Kiroku, but kept Shiki because it carries the existing goal-seeking framework identity while remaining independent of Hermes, Codex, Claude Code, or any single runtime.\ndiff --git a/docs/adr/0002-use-agents-as-shared-constitution.md b/docs/adr/SADR-0002-use-agents-as-shared-constitution.md\nindex 73de30ecb078120eac540d9bc51855e3cd48d0e3..c5486182bf06fca36ea508496b013c67302fd989\n--- a/docs/adr/0002-use-agents-as-shared-constitution.md\n+++ b/docs/adr/SADR-0002-use-agents-as-shared-constitution.md\n@@ -1,3 +1,3 @@\n-# Use AGENTS As The Shared Agent Constitution\n+# SADR-0002: Use AGENTS As The Shared Agent Constitution\n \n Shiki uses `AGENTS.md` as the runtime-neutral constitution and `CLAUDE.md` as a thin Claude Code wrapper. This keeps Codex, Claude Code, Hermes Runner, and future Agent Runtimes aligned on the same GitHub-first MergeGate rules while still allowing runtime-specific instructions where needed.\ndiff --git a/docs/adr/0003-require-skill-gate-for-mergegate-readiness.md b/docs/adr/SADR-0003-require-skill-gate-for-mergegate-readiness.md\nindex 424be0c9af4c8f0136f1fde68a468c47ff84e764..7fc337987c7df9792f75f181dfcbed75fe5ffff2\n--- a/docs/adr/0003-require-skill-gate-for-mergegate-readiness.md\n+++ b/docs/adr/SADR-0003-require-skill-gate-for-mergegate-readiness.md\n@@ -1,3 +1,3 @@\n-# Require Skill Gate For MergeGate Readiness\n+# SADR-0003: Require Skill Gate For MergeGate Readiness\n \n Shiki requires relevant engineering skills to be invoked before execution when their triggers apply. This makes skills part of MergeGate readiness rather than optional prompt style, because Shiki depends on repeatable planning, issue decomposition, TDD, diagnosis, triage, and architecture review before autonomous or parallel agent work can be trusted.\ndiff --git a/docs/adr/0004-use-github-first-with-shiki-mirror.md b/docs/adr/SADR-0004-use-github-first-with-shiki-mirror.md\nindex d0a3a9174dc646a945a9d325954ffaf00c352040..a79c54ae9ee8eed2a0524192b864da4e22a92d3f\n--- a/docs/adr/0004-use-github-first-with-shiki-mirror.md\n+++ b/docs/adr/SADR-0004-use-github-first-with-shiki-mirror.md\n@@ -1,4 +1,4 @@\n-# ADR 0004: Use GitHub First With A Repository-Local Shiki Mirror\n+# SADR-0004: Use GitHub First With A Repository-Local Shiki Mirror\n \n ## Status\n \ndiff --git a/docs/adr/0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md b/docs/adr/SADR-0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\nindex b339baf49b89d9290e79c3d51b36e7b2bde3bd95..0f3c3eb9afccdb3403ce978e21d04fe12e495da0\n--- a/docs/adr/0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\n+++ b/docs/adr/SADR-0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\n@@ -1,8 +1,8 @@\n-# ADR 0005: Use Codex As Front And Claude Code Action As The GitHub Runtime\n+# SADR-0005: Use Codex As Front And Claude Code Action As The GitHub Runtime\n \n ## Status\n \n-Accepted (implementer default superseded by ADR 0008; the auth model and the\n+Accepted (implementer default superseded by SADR-0008; the auth model and the\n Claude Code Action GitHub runtime decision remain in force)\n \n ## Context\n@@ -26,4 +26,4 @@\n - Shiki avoids requiring `OPENAI_API_KEY` for the default workflow.\n - Codex work happens through the user's authenticated Codex front surface, not as a hidden backend Action.\n - Claude Code Action is configured for OAuth-token based subscription usage by default.\n-- API-key based automation remains possible only as an explicit target-repo extension with its own ADR.\n+- API-key based automation remains possible only as an explicit target-repo extension with its own target ADR.\ndiff --git a/docs/adr/0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md b/docs/adr/SADR-0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\nindex 4bcae9d1e52f0a37b8e9fbafa5cd14b6ee1993bd..7a38538a6ae9929567f0ade67e7ea6a60d0b3f7e\n--- a/docs/adr/0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\n+++ b/docs/adr/SADR-0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\n@@ -1,4 +1,4 @@\n-# ADR 0006: Separate Codex Implementation, CCA Completion, And MergeGate Authorization\n+# SADR-0006: Separate Codex Implementation, CCA Completion, And MergeGate Authorization\n \n ## Status\n \ndiff --git a/docs/adr/0007-packaging-and-release.md b/docs/adr/SADR-0007-packaging-and-release.md\nindex 79b84ddb97167372b44641e5b7860e400ce80822..bb445a2e2e856d9d1f5fdbe35de8d4e8cec0fb32\n--- a/docs/adr/0007-packaging-and-release.md\n+++ b/docs/adr/SADR-0007-packaging-and-release.md\n@@ -1,4 +1,4 @@\n-# ADR 0007: Packaging And Release For Shiki\n+# SADR-0007: Packaging And Release For Shiki\n \n ## Status\n \n@@ -25,7 +25,7 @@\n   not introduce third-party runtime dependencies or break the shim.\n - Releases must run inside the operator's subscription-authenticated toolchain\n   and must not require extra secrets beyond the built-in `GITHUB_TOKEN`\n-  (consistent with ADR 0005's no-API-key-by-default stance).\n+  (consistent with SADR-0005's no-API-key-by-default stance).\n - `.shiki/manifest.json` already encodes the install surface (`install.include`,\n   `install.create_directories`, `install.exclude_from_commit`), and it must not\n   be mutated as part of this change.\n@@ -34,7 +34,7 @@\n \n This is a hard-to-reverse platform decision (it sets the version source of\n truth, the release trigger, and the public install contract), so it is recorded\n-as an ADR.\n+as a SADR.\n \n ## Decision\n \n@@ -104,7 +104,7 @@\n 3. **Publishing to PyPI.** Rejected for now: Shiki is distributed as a\n    repository template + git-installable CLI, not a PyPI library; PyPI would add\n    credentials/secrets and a different versioning contract. Can be added later as\n-   its own ADR if a packaged distribution is needed.\n+   its own SADR if a packaged distribution is needed.\n 4. **Dynamic version (read `VERSION` via a build backend `dynamic` field).**\n    Rejected to keep `pyproject.toml` backend-agnostic and dependency-free; the\n    small duplication between `VERSION` and `pyproject.toml` is guarded by the\n@@ -123,4 +123,4 @@\n   releasing versions the whole platform but ships only `install.include` to\n   targets.\n - Future PyPI distribution or a console entry point are deferred and would each\n-  warrant their own ADR.\n+  warrant their own SADR.\ndiff --git a/docs/adr/0008-use-claude-code-as-default-implementer.md b/docs/adr/SADR-0008-use-claude-code-as-default-implementer.md\nindex dedf4510b7c7799521422f622029563d373db031..35c2625d908167fbc58855b5b63e283805f167e8\n--- a/docs/adr/0008-use-claude-code-as-default-implementer.md\n+++ b/docs/adr/SADR-0008-use-claude-code-as-default-implementer.md\n@@ -1,12 +1,12 @@\n-# ADR 0008: Use Claude Code As The Default Implementer Runtime\n+# SADR-0008: Use Claude Code As The Default Implementer Runtime\n \n ## Status\n \n-Proposed (supersedes ADR 0005 for the implementer default; keeps its auth model)\n+Proposed (supersedes SADR-0005 for the implementer default; keeps its auth model)\n \n ## Context\n \n-ADR 0005 made Codex Front the default implementation runtime, with Claude Code\n+SADR-0005 made Codex Front the default implementation runtime, with Claude Code\n restricted to planner and reviewer roles by the runtime registry, and the only\n autonomous implementation adapter was `shiki runner codex`.\n \n@@ -18,13 +18,13 @@\n - The platform's planning, review, and CCA layers already run on Claude Code,\n   and Claude Code's native capabilities (the built-in `/code-review` skill and\n   the Workflow multi-agent orchestration tool) have no Codex equivalent.\n-- The Goal lifecycle redesign (ADR 0009) requires an autonomous post-freeze\n+- The Goal lifecycle redesign (SADR-0009) requires an autonomous post-freeze\n   loop driven from the operator's Claude Code session, including bounded\n   repair dispatch, which must not depend on a runtime that cannot be invoked\n   unattended.\n \n The constitution claims Shiki is runtime-agnostic (\"not a Claude-only\n-workflow\", ADR 0002). Any change to the implementer default must preserve the\n+workflow\", SADR-0002). Any change to the implementer default must preserve the\n registry-based runtime model rather than hardcoding one vendor.\n \n ## Decision\n@@ -39,7 +39,7 @@\n   the task to review or repair-needed.\n - Keep Codex registered as an optional implementer runtime. `shiki runner\n   codex`, the Codex handoff templates, and the ChatGPT OAuth auth model from\n-  ADR 0005 remain supported for tasks explicitly assigned to `codex`.\n+  SADR-0005 remain supported for tasks explicitly assigned to `codex`.\n - Commit to mandating two Claude-native capabilities as loop contract in\n   follow-up Goals (status: the code-review gate was delivered by Goal G-B \u2014\n   skill registry, checklists item PR-12, default required_skills \u2014 while the\n@@ -60,7 +60,7 @@\n \n Out of scope: removing Codex support, changing the CCA runtime (Claude Code\n Action with `CLAUDE_CODE_OAUTH_TOKEN` stays), and API-key based automation\n-(still requires its own ADR per ADR 0005).\n+(still requires its own target ADR per SADR-0005).\n \n ## Consequences\n \n@@ -100,7 +100,7 @@\n   harness rejection of direct `codex exec` is outside Shiki's control, and the\n   plan's mandatory native capabilities cannot run on Codex at all.\n - **Remove Codex entirely**: rejected; it abandons the runtime-agnostic\n-  constitution (ADR 0002) for no operational gain and forecloses\n+  constitution (SADR-0002) for no operational gain and forecloses\n   cross-runtime adversarial review.\n - **GitHub Actions cloud implementation via claude-code-action**: rejected as\n   default; durable evidence is automatic but token cost, Node 24 workflow\ndiff --git a/docs/adr/0009-requirements-definition-and-spec-freeze.md b/docs/adr/SADR-0009-requirements-definition-and-spec-freeze.md\nindex 29b3f7871bef9203a8b4a831a65c39eb2f506e42..52e7dd69ad0aa4efe0b68c62678abbf7df02aaec\n--- a/docs/adr/0009-requirements-definition-and-spec-freeze.md\n+++ b/docs/adr/SADR-0009-requirements-definition-and-spec-freeze.md\n@@ -1,4 +1,4 @@\n-# ADR 0009: Merge Grill And Planning Into Requirements Definition With Spec Freeze\n+# SADR-0009: Merge Grill And Planning Into Requirements Definition With Spec Freeze\n \n ## Status\n \ndiff --git a/docs/adr/0010-external-ai-guardian-review.md b/docs/adr/SADR-0010-external-ai-guardian-review.md\nindex e6108bbe8bb4e73e0ff4684c8f78001a80438c4e..f3510b145664298adafb151b6f0db51b972f01ec\n--- a/docs/adr/0010-external-ai-guardian-review.md\n+++ b/docs/adr/SADR-0010-external-ai-guardian-review.md\n@@ -1,4 +1,4 @@\n-# ADR 0010: External AI Guardian Review As A First-Class Approval Authority\n+# SADR-0010: External AI Guardian Review As A First-Class Approval Authority\n \n ## Status\n \n@@ -8,7 +8,7 @@\n \n Shiki is intended to be an autonomous, recursive, self-improving agent\n framework: authority-in-the-loop, not human-in-the-loop. Until now the Guardian\n-approval required for high/critical-risk merges (guardian-policy.json, ADR\n+approval required for high/critical-risk merges (guardian-policy.json, SADR\n governance) recognized only human authority sources \u2014 a GitHub review approval,\n the `guardian:approved` label applied by a configured Guardian, or a comment\n carrying `Guardian approval granted` plus the current head SHA.\n@@ -89,7 +89,7 @@\n   autonomous design the operator intends and produced the impersonation\n   incident.\n - **Record AI approval as an operator comment**: rejected as forgery of human\n-  authority \u2014 the exact failure this ADR corrects.\n+  authority \u2014 the exact failure this SADR corrects.\n - **Deliver the artifact as a committed `.shiki/` file**: rejected; a committed\n   approval inside the PR being approved is forgeable by anyone who can push.\n - **A dedicated reviewer bot identity instead of a relayed comment**: deferred;\ndiff --git a/docs/adr/0011-autonomous-quality-gate-evidence.md b/docs/adr/SADR-0011-autonomous-quality-gate-evidence.md\nindex bca0246c5d46e44afbde15486a75eec425fc02f5..9c93eac16d8c3a6d4b11bd7224e3955102cd7db4\n--- a/docs/adr/0011-autonomous-quality-gate-evidence.md\n+++ b/docs/adr/SADR-0011-autonomous-quality-gate-evidence.md\n@@ -1,4 +1,4 @@\n-# ADR 0011: Autonomous Quality-Gate Evidence \u2014 Deterministic Facts And An Independent Verifier\n+# SADR-0011: Autonomous Quality-Gate Evidence \u2014 Deterministic Facts And An Independent Verifier\n \n ## Status\n \ndiff --git a/docs/adr/0012-autonomous-loop-closeout-to-main.md b/docs/adr/SADR-0012-autonomous-loop-closeout-to-main.md\nindex 637fad6dde4989c9c2bf0f81f676d8d29991123c..ec7d85f81030a676bcbba28b43eb365422a36eac\n--- a/docs/adr/0012-autonomous-loop-closeout-to-main.md\n+++ b/docs/adr/SADR-0012-autonomous-loop-closeout-to-main.md\n@@ -1,4 +1,4 @@\n-# 0012 \u2014 Autonomous loop pushes goal completion to main via a closeout PR\n+# SADR-0012 \u2014 Autonomous loop pushes goal completion to main via a closeout PR\n \n - Status: accepted\n - Date: 2026-06-18\ndiff --git a/docs/adr/0013-bootstrap-grants-actions-approve-pull-requests.md b/docs/adr/SADR-0013-bootstrap-grants-actions-approve-pull-requests.md\nindex b6c69d64f1c5ced346b88d6859586715b3b5bd0e..cf48ff4a6716267f539ca58edd1a852e7c7a2d64\n--- a/docs/adr/0013-bootstrap-grants-actions-approve-pull-requests.md\n+++ b/docs/adr/SADR-0013-bootstrap-grants-actions-approve-pull-requests.md\n@@ -1,4 +1,4 @@\n-# ADR 0013: Bootstrap Grants GitHub Actions Permission To Create And Approve Pull Requests\n+# SADR-0013: Bootstrap Grants GitHub Actions Permission To Create And Approve Pull Requests\n \n ## Status\n \n@@ -31,7 +31,7 @@\n security tradeoff: it lets repository automation satisfy a human-review gate. In\n a multi-maintainer repo that could let a compromised or buggy workflow\n self-approve changes. So whether bootstrap turns it on by default \u2014 and with\n-what default token scope \u2014 is an ADR-worthy decision, not an implementation\n+what default token scope \u2014 is a SADR-worthy decision, not an implementation\n detail.\n \n ## Decision\n@@ -48,7 +48,7 @@\n Specifically:\n \n - **`can_approve_pull_request_reviews` defaults to `true`.** Shiki's default\n-  posture is solo/AFK autonomous operation (ADR 0008/0009/0012), where the CCA\n+  posture is solo/AFK autonomous operation (SADR-0008/0009/0012), where the CCA\n   Review Bridge is the only path that can satisfy `required_review: true` after\n   the CCA and Guardian gates have already passed. The default makes a\n   freshly-bootstrapped repo able to run the autonomous loop end to end without a\ndiff --git a/docs/adr/0014-codex-app-external-ai-guardian-ui-adapter.md b/docs/adr/SADR-0014-codex-app-external-ai-guardian-ui-adapter.md\nindex 238db06279334caa341c117362df9271382a7fcd..65f12e9939321e1b3e389a34c822fc1549e5155d\n--- a/docs/adr/0014-codex-app-external-ai-guardian-ui-adapter.md\n+++ b/docs/adr/SADR-0014-codex-app-external-ai-guardian-ui-adapter.md\n@@ -1,4 +1,4 @@\n-# ADR 0014: Use Codex App As The External AI Guardian UI Adapter\n+# SADR-0014: Use Codex App As The External AI Guardian UI Adapter\n \n ## Status\n \ndiff --git a/docs/adr/0015-contract-approval-before-dispatch.md b/docs/adr/SADR-0015-contract-approval-before-dispatch.md\nindex fad6c656741a20a7684ba16080a7db159f63a52f..6061271a46d3f1d6ae7174477de3fb75673123a6\n--- a/docs/adr/0015-contract-approval-before-dispatch.md\n+++ b/docs/adr/SADR-0015-contract-approval-before-dispatch.md\n@@ -1,4 +1,4 @@\n-# ADR 0015: Approve The Task Contract Before Dispatch, Not The Diff After It\n+# SADR-0015: Approve The Task Contract Before Dispatch, Not The Diff After It\n \n ## Status\n \n@@ -9,7 +9,7 @@\n Guardian approval for high/critical risk is expressible only on a pull request.\n Every approval source in `guardian-policy.json` \u2014 GitHub review, the\n `guardian:approved` label, the `Guardian approval granted` comment, and the\n-`external_ai_guardian_review` artifact (ADR 0010/0014) \u2014 is bound to a PR, and\n+`external_ai_guardian_review` artifact (SADR-0010/0014) \u2014 is bound to a PR, and\n three of the four require the current head SHA. Approval therefore cannot exist\n before an implementation PR exists.\n \n@@ -81,7 +81,7 @@\n An envelope breach must surface only on the non-repairable policy gate, never on\n the repairable `MergeGate metadata check`. Otherwise the Goal Loop routes it to\n `dispatch_repair` \u2014 instructing a runner to make the breach go away \u2014 which is\n-exactly what this ADR forbids.\n+exactly what this SADR forbids.\n \n **Contract immutability.** After registration, an implementation PR must not\n change the governance fields of its own task contract (`scope`, `non_goals`,\n@@ -116,7 +116,7 @@\n fails any verdict that is not `complete`, and the signal the CCA prompt reads is\n computed from `evaluate_guardian_approval` alone. Without the OR-merge at the\n signal, a contract-approved PR still returns `needs_guardian`, the CCA job fails,\n-and the MergeGate policy check never runs \u2014 the gate this ADR changes would never\n+and the MergeGate policy check never runs \u2014 the gate this SADR changes would never\n be reached. The CCA *prompt* needs no change: a `contract_approval` entry in the\n signal's existing `sources` array satisfies its current wording.\n \n@@ -130,7 +130,7 @@\n written, and an approved contract lets its implementation PRs merge through the\n same automatic path as low/medium risk. The wasted implement-then-block cycle\n disappears. Guardian review shifts from reading diffs to reading contracts, which\n-is both earlier and smaller; the external AI Guardian path (ADR 0010/0014) works\n+is both earlier and smaller; the external AI Guardian path (SADR-0010/0014) works\n on contracts with no change.\n \n We accept that the Guardian authorizes a class of change without seeing its\n@@ -146,7 +146,7 @@\n adversarial tests for the new approval path equivalent in rigor to\n `test_shiki_governance_evidence.sh`.\n \n-Bootstrap costs, which apply only while this ADR is being implemented. The tasks\n+Bootstrap costs, which apply only while this SADR is being implemented. The tasks\n that build Contract Approval merge under the existing rules, so the ones\n dispatched after the pre-dispatch gate lands but before the approval path lands\n need a Guardian-approved Contract PR *and* a live Guardian approval on their\n@@ -210,7 +210,7 @@\n risk; they do not bound authorized work.\n \n **Auto-escalate risk on envelope breach instead of stopping.** Returns the\n-workflow to a diff-time Guardian decision \u2014 the exact behavior this ADR removes.\n+workflow to a diff-time Guardian decision \u2014 the exact behavior this SADR removes.\n \n **Resolve governance fields from the base snapshot only.** Rejected against the\n registration flow: implementation PRs add their own contract, so the base has no\ndiff --git a/docs/adr/0016-locks-bound-product-paths.md b/docs/adr/SADR-0016-locks-bound-product-paths.md\nindex b5e850f3f1f7f4ef6e7f9d1763a29a51bd300c45..46b40a9b99a7aae644c02bb458f5473ff0a90ce2\n--- a/docs/adr/0016-locks-bound-product-paths.md\n+++ b/docs/adr/SADR-0016-locks-bound-product-paths.md\n@@ -1,4 +1,4 @@\n-# ADR 0016: Locks bound product paths; the Shiki mirror is bound by record identity\n+# SADR-0016: Locks bound product paths; the Shiki mirror is bound by record identity\n \n ## Status\n \n@@ -6,8 +6,8 @@\n \n - Date: 2026-07-28\n - Deciders: operator (mizutani-140), Claude (planner)\n-- Related: ADR 0004 (GitHub-first with a `.shiki` mirror), ADR 0009 (Spec Freeze),\n-  ADR 0012 (autonomous loop closeout to main), ADR 0015 (Contract Approval before\n+- Related: SADR-0004 (GitHub-first with a `.shiki` mirror), SADR-0009 (Spec Freeze),\n+  SADR-0012 (autonomous loop closeout to main), SADR-0015 (Contract Approval before\n   dispatch), remediation goal task R-02, the live PR #179 deadlock.\n \n ## Context\n@@ -90,11 +90,11 @@\n files, documentation files and named source modules are nameable; runtime-\n allocated ledger, runner and report ids are not \u2014 and those live in `.shiki`,\n which locks no longer govern. Existing frozen contracts are brought into line by\n-a one-time Spec Amendment per plan, which must land before ADR 0015's contract\n+a one-time Spec Amendment per plan, which must land before SADR-0015's contract\n immutability makes `locks` unwritable from the implementation PR.\n \n **Lock state is resolved from the durable side.** MergeGate resolves the lock\n-registry, and ADR 0015's governance fields, from the base `.shiki` snapshot when\n+registry, and SADR-0015's governance fields, from the base `.shiki` snapshot when\n present, falling back to head only when absent. The `MergeGate metadata check`\n job is given `--base-shiki`, which it does not receive today. Locks are not\n derived from the live open-pull-request set: a pull request body is untrusted\n@@ -108,7 +108,7 @@\n Explicitly out of scope: moving MergeGate's contract resolution off the PR\n checkout wholesale; making the mirror a projection of GitHub state; introducing\n new lock kinds; moving ledger evidence out of the repository; and unwinding\n-ADR 0012's closeout pull request. Registration of multi-task goals is ADR 0015's\n+SADR-0012's closeout pull request. Registration of multi-task goals is SADR-0015's\n Contract PR and is not re-decided here.\n \n ## Consequences\n@@ -127,7 +127,7 @@\n \n Risks accepted. First, goal-scoped identity is only as strong as `goal_id`,\n which is resolved from the PR body and the head task file; the mitigation is to\n-add `goal_id` to ADR 0015's frozen governance field set, and until that lands a\n+add `goal_id` to SADR-0015's frozen governance field set, and until that lands a\n task file can name a foreign goal. Second, base-when-present resolution is\n unavailable on fork pull requests, where the head is contributor-controlled;\n fork pull requests already cannot complete a Shiki merge, and this decision does\n@@ -139,7 +139,7 @@\n path, not by implementation pull requests.\n \n New obligations: one Spec Amendment per frozen plan to rewrite `locks`, ordered\n-before ADR 0015 T1; extension of the adversarial suites in\n+before SADR-0015 T1; extension of the adversarial suites in\n `scripts/test_shiki_adversarial_state.sh` and\n `scripts/test_shiki_governance_evidence.sh` to cover the eleven newly-scoped\n directories; and updating `scripts/test_shiki_loop_lock_guard.sh`,\n@@ -151,32 +151,32 @@\n whether the loop's transitive sync of ledger `evidence` references can be\n deleted once the Contract PR carries its own source plan.\n \n-## Relationship to ADR 0004\n+## Relationship to SADR-0004\n \n-This ADR **clarifies something ADR 0004 left unspecified**. ADR 0004 decides that\n+This SADR **clarifies something SADR-0004 left unspecified**. SADR-0004 decides that\n GitHub is the operational source of truth and that each target repository keeps a\n `.shiki/` mirror. It says nothing about pull requests writing the mirror, does\n not name MergeGate, and does not make the mirror an input to a gate. Nothing in\n-ADR 0004's Decision or Consequences is amended, superseded or weakened: the\n+SADR-0004's Decision or Consequences is amended, superseded or weakened: the\n mirror still exists, still records the same artifacts, and pull request\n-boundaries remain the coordination surface. What this ADR settles is the question\n+boundaries remain the coordination surface. What this SADR settles is the question\n 0004 never reached \u2014 by what authority a pull request may write a mirror record \u2014\n and it answers it the way `.shiki/manifest.json` already declares\n (`pr_mutation: current-task-or-current-goal-only`).\n \n-It **amends ADR 0012**, whose Decision item 5 states that \"Loop-executed tasks\n+It **amends SADR-0012**, whose Decision item 5 states that \"Loop-executed tasks\n declare the `path:.shiki/**` lock, which covers every `.shiki` file the closeout\n stages ... so `files_outside_locks` is satisfied without per-file lock\n bookkeeping.\" That clause is replaced: the closeout's mirror writes are\n-authorized by record identity, not by a declared lock. ADR 0012's substantive\n+authorized by record identity, not by a declared lock. SADR-0012's substantive\n decision \u2014 that completion reaches `main` through a closeout pull request rather\n than local attestation \u2014 is preserved unchanged.\n \n-It is **complementary to ADR 0015**, not a substitute. ADR 0015 owns contract\n+It is **complementary to SADR-0015**, not a substitute. SADR-0015 owns contract\n immutability, the pre-dispatch gate, the Contract PR and the Scope Envelope. This\n-ADR owns what a lock means and who may write the mirror. The one ordering\n+SADR owns what a lock means and who may write the mirror. The one ordering\n constraint between them is that the lock-narrowing Spec Amendment must precede\n-ADR 0015 T1's freezing of the `locks` field.\n+SADR-0015 T1's freezing of the `locks` field.\n \n ## Alternatives Considered\n \n@@ -186,7 +186,7 @@\n the only writer that can register them may run. Migrating the Guardian signal to\n that store makes every registration PR fail closed to `needs_guardian` at every\n risk level, because the task file is by definition absent from the store, which\n-removes the low/medium autonomous path ADR 0008/0009 exists to provide. And it\n+removes the low/medium autonomous path SADR-0008/0009 exists to provide. And it\n leaves schemas, manifest, config, guardian policy, templates, migrations, plans,\n worktrees, runner records, runs and handoffs with no writer at all in a\n repository whose product is `.shiki`. Measured benefit on the frozen backlog:\n@@ -199,14 +199,14 @@\n namespace is not in fact ownership-partitioned \u2014 56 ledger entries carry no\n `task_id`, and one goal DAG is writable by 35 registered tasks. The correct\n expression of its insight is that locks do not govern the mirror at all, which is\n-what this ADR decides.\n+what this SADR decides.\n \n **Make the mirror a projection written only by a registration writer and a\n post-merge projection job.** Rejected. It requires a bypass actor with write\n access to the protected default branch; it makes its own master invariant\n readable from a manifest that pull requests may edit; it deletes both reconcile\n modes and leaves a corrupted mirror with no in-band repair path, contradicting\n-ADR 0004's consequence that mirror conflicts are repaired against GitHub state;\n+SADR-0004's consequence that mirror conflicts are repaired against GitHub state;\n and its content-derived ids are rejected by `validate_shiki`'s currently-green id\n pattern.\n \ndiff --git a/docs/adr/0017-bookkeeping-closeouts-do-not-inherit-risk.md b/docs/adr/SADR-0017-bookkeeping-closeouts-do-not-inherit-risk.md\nindex 554e3a0d526a72ca067108c5464cc68c3edcf174..b9676857a4713850e063af7c39c8e0b01ecb4c66\n--- a/docs/adr/0017-bookkeeping-closeouts-do-not-inherit-risk.md\n+++ b/docs/adr/SADR-0017-bookkeeping-closeouts-do-not-inherit-risk.md\n@@ -1,4 +1,4 @@\n-# ADR 0017: A Bookkeeping Closeout Does Not Inherit Its Task's Risk\n+# SADR-0017: A Bookkeeping Closeout Does Not Inherit Its Task's Risk\n \n ## Status\n \n@@ -6,7 +6,7 @@\n \n ## Context\n \n-ADR 0012 made the goal loop push a task's completion to the default branch through a\n+SADR-0012 made the goal loop push a task's completion to the default branch through a\n closeout pull request: after the implementation PR merges, the loop opens a second PR\n carrying `task.status = done`, `lock.state = released`, and \u2014 when the task completes\n its goal \u2014 `goal.status = complete` plus one scorecard report. Nothing else. The\n@@ -49,14 +49,14 @@\n 4. The task file's governance fields (`scope`, `non_goals`, `required_skills`,\n    `risk_level`, `locks`, `acceptance_checks`, `test_command`) are byte-identical to\n    the base snapshot. Only `status`, `expected_pr`, `closeout_pr`, `ledger_evidence`\n-   and `expected_branch` may differ \u2014 the ADR 0015 contract-immutability rule,\n+   and `expected_branch` may differ \u2014 the SADR-0015 contract-immutability rule,\n    unchanged.\n \n    `expected_branch` is in that set because a closeout is *by construction* on a\n    different branch from the implementation it closes, and the loop rewrites the\n    field accordingly. It is safe to let it differ because MergeGate independently\n    requires `expected_branch` to equal the PR's actual head ref, so a PR cannot set\n-   it to anything other than the branch it is really on. The first draft of this ADR\n+   it to anything other than the branch it is really on. The first draft of this SADR\n    omitted it, which made the exemption unreachable: every real closeout failed\n    condition 4 on a field the loop always changes. See PR #209.\n 5. The state transitions are exactly the terminal set: task `review` \u2192 `done`, lock\n@@ -113,12 +113,12 @@\n classification must be derived, not asserted.\n \n **Fold the closeout into the implementation PR** so no second PR exists. Rejected under\n-ADR 0012, which recorded why: a task file that is already `done` on the implementation\n+SADR-0012, which recorded why: a task file that is already `done` on the implementation\n branch short-circuits the loop's review \u2192 CCA \u2192 merge \u2192 done ordering into a local\n goal-completion before any merge happens.\n \n **Have the loop auto-merge closeouts with elevated credentials.** Rejected outright: that\n-is a runtime authoring its own approval, which ADR 0010 and the constitution's\n+is a runtime authoring its own approval, which SADR-0010 and the constitution's\n approval-record prohibition forbid. It would also be indistinguishable, in the evidence,\n from the forgery those rules exist to prevent.\n \ndiff --git a/docs/adr/0018-carried-guardian-approval-across-a-proven-base-sync.md b/docs/adr/SADR-0018-carried-guardian-approval-across-a-proven-base-sync.md\nindex 22358562da390ed8b45cd600da08a43d378b4680..e6e2907cefa776460af73ddf7d39db605f3c39b0\n--- a/docs/adr/0018-carried-guardian-approval-across-a-proven-base-sync.md\n+++ b/docs/adr/SADR-0018-carried-guardian-approval-across-a-proven-base-sync.md\n@@ -1,4 +1,4 @@\n-# ADR 0018: Carried Guardian Approval Across A Proven Base Sync\n+# SADR-0018: Carried Guardian Approval Across A Proven Base Sync\n \n ## Status\n \n@@ -34,7 +34,7 @@\n Guardian evaluator that consumes it (`shiki_guardian.evaluate_guardian_approval`,\n the `guardian_comment_carried` source) were built and tested in isolation. Neither\n does anything until a caller passes the flags that turn the carry on, so the\n-feature was invisible in CI. This ADR records the decision the two of them\n+feature was invisible in CI. This SADR records the decision the two of them\n implement, and this task wires it into the four CI invocations that judge Guardian\n approval.\n \ndiff --git a/docs/adr/README.md b/docs/adr/README.md\nindex c70e057a4520c36e042ab68c53cd4a21ef709b71..5da43856836e2ce6a150f8127766e2f88355ab4b\n--- a/docs/adr/README.md\n+++ b/docs/adr/README.md\n@@ -1,40 +1,41 @@\n-# Architecture Decision Records\n+# Decision Record Namespaces\n \n-This directory holds Shiki's Architecture Decision Records (ADRs): durable\n-records of hard-to-reverse platform decisions. ADRs are part of the Shiki source\n-of truth alongside GitHub state, the `.shiki/` mirror, and `CONTEXT.md`.\n+This directory holds two independent decision namespaces:\n \n-## How ADRs Work\n+- Shiki Architecture Decision Records (SADRs), named\n+  `SADR-NNNN-<short-slug>.md`, record hard-to-reverse Shiki platform decisions.\n+- Target Architecture Decision Records (ADRs), named\n+  `NNNN-<short-slug>.md`, belong to the target product.\n \n-- New ADRs are added as `docs/adr/NNNN-<short-slug>.md`, where `NNNN` is the next\n-  zero-padded sequence number.\n-- The **canonical list of ADRs is the set of files matching `docs/adr/*.md`** in\n-  this directory. Browse `docs/adr/*.md` for the authoritative, current set.\n-- Use [`template.md`](template.md) as the starting point for a new ADR.\n-- Record an ADR when a decision is hard to reverse, surprising without context,\n-  and a real tradeoff. Do not write an ADR for routine or easily reversible\n-  choices.\n+The same four-digit number may appear once in each namespace. A platform\n+`SADR-NNNN` citation resolves only to `SADR-NNNN-*.md`; a target `ADR NNNN`\n+citation resolves only to `NNNN-*.md`.\n \n-Using the `docs/adr/*.md` glob as the source of truth means other PRs can add an\n-ADR without editing this index file, avoiding merge contention on a\n-hand-maintained enumeration.\n+## How Decision Records Work\n \n-## Current ADRs (snapshot)\n-\n-The list below is a convenience snapshot and may lag behind the directory. When\n-in doubt, list `docs/adr/*.md`.\n+- New Shiki platform decisions use the next zero-padded SADR number.\n+- New target product decisions use the target's own ADR sequence.\n+- The canonical list is the files in `docs/adr/`; no hand-maintained index is\n+  authoritative.\n+- Use [`template.md`](template.md) as the starting point and choose the filename\n+  namespace that owns the decision.\n+- Record a decision when it is hard to reverse, surprising without context, and\n+  a real tradeoff. Do not write a decision record for routine or easily\n+  reversible choices.\n \n-- [0001 \u2014 Keep Shiki As The Platform Name](0001-keep-shiki-as-platform-name.md)\n-- [0002 \u2014 Use AGENTS As The Shared Agent Constitution](0002-use-agents-as-shared-constitution.md)\n-- [0003 \u2014 Require Skill Gate For MergeGate Readiness](0003-require-skill-gate-for-mergegate-readiness.md)\n-- [0004 \u2014 Use GitHub First With A Repository-Local Shiki Mirror](0004-use-github-first-with-shiki-mirror.md)\n-- [0005 \u2014 Use Codex As Front And Claude Code Action As The GitHub Runtime](0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md)\n-- [0006 \u2014 Separate Codex Implementation, CCA Completion, And MergeGate Authorization](0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md)\n+Using the directory as the source of truth lets independent changes add records\n+without contending on an enumerated index.\n \n-## Adding A New ADR\n+## Adding A Shiki SADR\n \n-1. Copy `template.md` to `docs/adr/NNNN-<short-slug>.md`.\n+1. Copy `template.md` to `docs/adr/SADR-NNNN-<short-slug>.md`.\n 2. Fill in the title, status, context, decision, and consequences.\n-3. Set the status to `Proposed`, then `Accepted` once the decision is approved.\n-4. Open a PR following `CONTRIBUTING.md`. You do not need to edit this index for\n-   the ADR to be discoverable via `docs/adr/*.md`.\n+3. Set the status to `Proposed`, then `Accepted` once the proper authority has\n+   approved the decision.\n+4. Open a PR following `CONTRIBUTING.md`.\n+\n+## Adding A Target ADR\n+\n+1. Copy `template.md` to `docs/adr/NNNN-<short-slug>.md` using the target's own\n+   sequence.\n+2. Cite it as `ADR NNNN`; do not prefix its filename with `SADR-`.\ndiff --git a/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md b/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\nnew file mode 100644\nindex 0000000000000000000000000000000000000000..7455ce1f3bed02ddddb851ac21a1d1a648c9aeba\n--- /dev/null\n+++ b/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\n@@ -0,0 +1,55 @@\n+# SADR-0019: Separate Shiki Platform SADRs From Target ADRs\n+\n+## Status\n+\n+Accepted\n+\n+## Context\n+\n+Shiki installs its platform decision records into `docs/adr/` in every target\n+repository. Shiki and its targets previously used the same numeric\n+`NNNN-<slug>.md` namespace. A target that already owned ADRs 0013 through 0018\n+therefore acquired two records for each number during installation, making an\n+`ADR NNNN` citation ambiguous. Renumbering target decisions would rewrite target\n+history, while renumbering Shiki decisions per target would destroy their stable\n+platform identity.\n+\n+Upgrades also need to remove the former Shiki-owned numeric paths without ever\n+mistaking a target-owned ADR for platform content. The install stamp already\n+records exact shipped paths and content digests, so it can prove ownership only\n+when both still match.\n+\n+## Decision\n+\n+Shiki platform decisions use the `SADR` namespace:\n+\n+- records are named `docs/adr/SADR-NNNN-<slug>.md`;\n+- live platform citations use `SADR-NNNN`;\n+- target product decisions retain `docs/adr/NNNN-<slug>.md` and `ADR NNNN`;\n+- the reference validator resolves the two namespaces independently, so the same\n+  four-digit number may exist once in each namespace.\n+\n+Before a forced upgrade writes anything, the installer inventories the exact\n+former numeric Shiki ADR paths. It may delete a legacy path only when the\n+existing install stamp contains that exact path and its recorded SHA-256 digest\n+matches the file currently on disk. All ownership blockers are reported\n+together and abort the upgrade before its first write. A non-forced install\n+never performs this cleanup.\n+\n+Historical `.shiki/` evidence remains unchanged because it records the namespace\n+that existed when that evidence was produced.\n+\n+## Consequences\n+\n+- Targets may use any ADR number without colliding with Shiki's platform SADRs.\n+- Fresh installs preserve target ADR bytes and install a stable SADR set.\n+- Forced upgrades fail closed when legacy ownership cannot be proven, and a\n+  successful upgrade refreshes the stamp without removed legacy paths.\n+- Current governance, packaging, workflow, prompt, script, and skill references\n+  must move with the platform records.\n+\n+## Alternatives Considered\n+\n+- Collision-only refusal leaves affected targets permanently unupgradable.\n+- Renumbering target ADRs mutates target-owned history.\n+- Per-target Shiki renumbering makes platform citations non-portable.\ndiff --git a/docs/adr/template.md b/docs/adr/template.md\nindex a92a97daddd0b00ab9dd626cc8d86942da2eafed..17dd79175b5fee2105935557f691b40cf9492942\n--- a/docs/adr/template.md\n+++ b/docs/adr/template.md\n@@ -1,14 +1,14 @@\n-# NNNN: <Short Decision Title>\n+# <SADR-NNNN or NNNN>: <Short Decision Title>\n \n ## Status\n \n-Proposed | Accepted | Superseded by ADR-NNNN\n+Proposed | Accepted | Superseded by SADR-NNNN or target ADR NNNN\n \n ## Context\n \n What is the situation, constraint, or force that requires a decision? Reference\n-relevant domain terms (`CONTEXT.md`), prior ADRs, and code reality. State the\n-problem, not the solution.\n+relevant domain terms (`CONTEXT.md`), prior Shiki SADRs or target ADRs, and code\n+reality. State the problem, not the solution.\n \n ## Decision\n \ndiff --git a/docs/agents/autonomous-loop-e2e-acceptance.md b/docs/agents/autonomous-loop-e2e-acceptance.md\nindex bbf84c6081987f159ac6ad9ecf3c82b7aa72e7e8..5c13c1d02a05a90b0dc46cbef7c95a9846d0c237\n--- a/docs/agents/autonomous-loop-e2e-acceptance.md\n+++ b/docs/agents/autonomous-loop-e2e-acceptance.md\n@@ -40,8 +40,8 @@\n 3. `gh auth status` is logged in with merge permission on the repo.\n 4. Branch protection + required checks are configured exactly as the goal-loop\n    required-check set expects (see `docs/agents/branch-protection-smoke.md`).\n-5. The repo `.shiki/policy` permits autonomous low/medium-risk merges (ADR\n-   0008/0009); the MergeGate policy check is the recorded-authority gate.\n+5. The repo `.shiki/policy` permits autonomous low/medium-risk merges\n+   (SADR-0008/0009); the MergeGate policy check is the recorded-authority gate.\n \n The operator's only actions are: register the task and start the loop. **No\n manual pre-clearing** means after `shiki loop run` starts, the operator does not\ndiff --git a/docs/agents/bootstrap-command.md b/docs/agents/bootstrap-command.md\nindex 05f7f203a52cf412b9f97084a6e131ed0f785afd..f7aa0b734dfebee4a3b139335b66ee3415f48713\n--- a/docs/agents/bootstrap-command.md\n+++ b/docs/agents/bootstrap-command.md\n@@ -189,7 +189,9 @@\n ```\n \n Use this only for tests, fixtures, or explicit local-only template inspection.\n-Use `--force` only when you intentionally want to overwrite existing target files.\n+Use `--force` only when you intentionally want to refresh the target template.\n+Legacy numeric Shiki decision cleanup is stamp-and-digest authorized before any\n+write; target ADR paths are not cleanup candidates.\n \n ## Slash Command\n \ndiff --git a/docs/agents/checklists.md b/docs/agents/checklists.md\nindex 4a7f03ade346f53a1c1d82981f3fd744f9ab4547..0c5ec6c53dc7197b7b7a7d18e667b325babd6c45\n--- a/docs/agents/checklists.md\n+++ b/docs/agents/checklists.md\n@@ -32,18 +32,18 @@\n | ID | Item | Blocking |\n | --- | --- | --- |\n | GD-01 | Plan was challenged against `CONTEXT.md`. | blocking for non-trivial Goals |\n-| GD-02 | Relevant ADRs were read or absence was recorded. | blocking for non-trivial Goals |\n+| GD-02 | Relevant SADRs and target ADRs were read or absence was recorded. | blocking for non-trivial Goals |\n | GD-03 | Ambiguous terms were resolved or converted to HITL blockers. | blocking |\n | GD-04 | Edge scenarios were considered. | advisory |\n | GD-05 | Code reality was checked when code could answer. | blocking when applicable |\n | GD-06 | New domain terms were added to `CONTEXT.md` when needed. | blocking when applicable |\n-| GD-07 | ADR was created or explicitly skipped using the ADR criteria. | blocking when applicable |\n+| GD-07 | A SADR or target ADR was created or explicitly skipped using the decision-record criteria. | blocking when applicable |\n \n ## CI \u2014 Context & Impact Checklist\n \n | ID | Item | Blocking |\n | --- | --- | --- |\n-| CI-01 | Relevant docs and ADRs identified. | blocking |\n+| CI-01 | Relevant docs, SADRs, and target ADRs identified. | blocking |\n | CI-02 | Relevant modules, interfaces, seams, callers, and tests identified. | blocking |\n | CI-03 | Dependencies and blocked-by relationships identified. | blocking |\n | CI-04 | Candidate locks identified. | blocking |\n@@ -62,7 +62,7 @@\n | PRD-04 | Implementation decisions are recorded without volatile code detail. | blocking |\n | PRD-05 | Testing decisions are recorded. | blocking |\n | PRD-06 | Out-of-scope items are explicit. | blocking |\n-| PRD-07 | Relevant glossary/ADR links are present when applicable. | blocking |\n+| PRD-07 | Relevant glossary/SADR/target-ADR links are present when applicable. | blocking |\n \n ## SF \u2014 Spec Freeze Checklist\n \n@@ -98,7 +98,7 @@\n | H-02 | Branch/worktree target is present. | blocking |\n | H-03 | Scope and non-goals are present. | blocking |\n | H-04 | Dependencies and locks are present. | blocking |\n-| H-05 | Relevant docs, ADRs, modules, and tests are referenced. | blocking |\n+| H-05 | Relevant docs, SADRs, target ADRs, modules, and tests are referenced. | blocking |\n | H-06 | Required skills are stated. | blocking |\n | H-07 | TDD expectations are stated. | blocking when behavior work |\n | H-08 | Acceptance criteria are included. | blocking |\ndiff --git a/docs/agents/codex-handoff.md b/docs/agents/codex-handoff.md\nindex ede468f47cb277f69a857e8c45df703e4ed0151d..6f97bdffb67e44d7edea2c71e01ea28933c4eb6c\n--- a/docs/agents/codex-handoff.md\n+++ b/docs/agents/codex-handoff.md\n@@ -46,7 +46,7 @@\n \n ## Context\n - Domain terms:\n-- Relevant ADRs:\n+- Relevant SADRs and target ADRs:\n - Relevant docs:\n - Relevant modules/interfaces/seams:\n - Similar tests/prior art:\ndiff --git a/docs/agents/completion-check-agent.md b/docs/agents/completion-check-agent.md\nindex 13e4c035db35025f4e3cdcc5adfb24314ac4d521..61bfa1218cf36ca605250c91919e620f33238aa0\n--- a/docs/agents/completion-check-agent.md\n+++ b/docs/agents/completion-check-agent.md\n@@ -32,7 +32,7 @@\n - `AGENTS.md`\n - `CLAUDE.md`\n - `CONTEXT.md`\n-- relevant ADRs\n+- relevant SADRs and target ADRs\n - parent Goal issue\n - PRD issue or document\n - task issue\ndiff --git a/docs/agents/decision-control.md b/docs/agents/decision-control.md\nindex 7d8ee5ab03a5ccbcbd04056ae61de03785a2a9c9..8c94ecda00fffaa0e92c325ab83aa9776bd1dac1\n--- a/docs/agents/decision-control.md\n+++ b/docs/agents/decision-control.md\n@@ -11,7 +11,7 @@\n | Decision | Owner | When | Required inputs | Output | Enforcement surface |\n | --- | --- | --- | --- | --- | --- |\n | Is the Goal clear enough to plan? | Planner + Guardian when needed | Goal intake | User request, existing docs, risk signals | Goal readiness verdict | Goal checklist, issue template, ledger |\n-| Must `grill-with-docs` run? | Planner / Skill Gate | Before PRD or issue split | Ambiguity, domain terms, ADR conflicts, unclear boundaries | Skill invocation or explicit skip rationale | Skill Gate checklist |\n+| Must `grill-with-docs` run? | Planner / Skill Gate | Before PRD or issue split | Ambiguity, domain terms, SADR/target-ADR conflicts, unclear boundaries | Skill invocation or explicit skip rationale | Skill Gate checklist |\n | Is a PRD required? | Planner | After Goal context stabilizes | Goal, `grill-with-docs` output, Context & Impact output | PRD required / not required | PRD checklist, CCA evidence check |\n | Can the spec be frozen? | Operator only | End of Requirements Definition, before issue split | PRD/requirements summary, scope inventory (SF-02), open-question list | `spec_freeze` block in the plan + ledger evidence | `require_grilled_plan` (plan ingest/run), SF checklist |\n | Can a frozen spec change? | Operator only (Spec Amendment) | When implementation contradicts the frozen spec | Contested decisions, scoped re-grill result | recorded amendment + re-stamped freeze | SF checklist, ledger evidence |\ndiff --git a/docs/agents/domain.md b/docs/agents/domain.md\nindex 258da8ea81e87ac0a6b0b5b5e5c2fde980eaabb7..d57b0cf17ff3e1fa9e6e5885372bf083d8295421\n--- a/docs/agents/domain.md\n+++ b/docs/agents/domain.md\n@@ -5,7 +5,9 @@\n ## Layout\n \n - `CONTEXT.md` defines Shiki language. Keep it implementation-free.\n-- `docs/adr/` records hard-to-reverse decisions.\n+- `docs/adr/SADR-NNNN-*.md` records Shiki platform decisions.\n+- `docs/adr/NNNN-*.md` records target product decisions without sharing the\n+  SADR namespace.\n - `AGENTS.md` is the runtime-neutral agent constitution.\n - `CLAUDE.md` is the Claude Code wrapper around `AGENTS.md`.\n - `.shiki/` is the Target Repository mirror for Goals, PRDs, plans, Task DAGs, contracts, locks, worktree records, ledger entries, CCA verdicts, repair packets, reports, and handoffs.\n@@ -15,6 +17,6 @@\n \n - Use Shiki terms exactly when they exist.\n - Add glossary terms to `CONTEXT.md` only when the term is part of the domain, not a generic programming concept.\n-- Create ADRs only for hard-to-reverse, surprising, tradeoff-based decisions.\n+- Create SADRs or target ADRs only for hard-to-reverse, surprising, tradeoff-based decisions.\n - Keep product-specific language in the Target Repository, not in Shiki Template docs.\n-- If an ADR conflicts with a proposed implementation, surface the conflict before editing.\n+- If a SADR or target ADR conflicts with a proposed implementation, surface the conflict before editing.\ndiff --git a/docs/agents/execution-control.md b/docs/agents/execution-control.md\nindex ad8ee3cdb36be56fedc1f46408ca92b387d74047..bc63c3a73661d12b567016ba299f1a6b66bf9088\n--- a/docs/agents/execution-control.md\n+++ b/docs/agents/execution-control.md\n@@ -63,7 +63,7 @@\n | Branch name | Orchestrator | Orchestrator | Before implementer dispatch | branch policy | branch ref in task/ledger |\n | Lock reservation | Context & Impact | MergeGate | Before dispatch and before merge | `.shiki/locks/`, labels, PR metadata | lock id, owner, scope |\n | CI profile | Planner | MergeGate | During issue triage, rechecked at PR | labels, `.shiki/policy.yaml`, workflow matrix | required check list |\n-| Required status checks | Repo maintainer / Guardian | GitHub branch protection / rulesets | Repository setup, updated via ADR | GitHub settings | branch/ruleset config, ADR |\n+| Required status checks | Repo maintainer / Guardian | GitHub branch protection / rulesets | Repository setup, updated via target ADR | GitHub settings | branch/ruleset config, target ADR |\n | CCA judgment | CCA | GitHub required check / MergeGate | Every non-draft PR update | GitHub Action check | structured CCA verdict |\n | Repair assignment | CCA | MergeGate | After failed CCA/CI/review | repair packet, issue/PR comment | repair id and attempt count |\n | Merge readiness | MergeGate | GitHub branch protection / Guardian if needed | After CCA complete | required checks, review rules, rulesets | mergegate check output |\ndiff --git a/docs/agents/guardian-policy.md b/docs/agents/guardian-policy.md\nindex 4147c7adf05a47adb4fd24ada1a1c2e1f2ba6d2d..a20d9cc6c00a569bb057b9ca8d6b51c7fc4a9847\n--- a/docs/agents/guardian-policy.md\n+++ b/docs/agents/guardian-policy.md\n@@ -98,8 +98,8 @@\n `origin/<default_branch>` and reuses each job's existing base fetch; a shallow\n (`--depth`) or second base fetch would make the proof unresolvable and silently\n disable the carry, so neither is permitted. CCA reads `guardian_comment_carried` as\n-a recorded authority in the deterministic signal and does not re-derive it. See ADR\n-0018 for the full decision, the rejected diff/patch-equality and path-disjointness\n+a recorded authority in the deterministic signal and does not re-derive it. See\n+SADR-0018 for the full decision, the rejected diff/patch-equality and path-disjointness\n alternatives, and the accepted residual.\n \n ## External AI Guardian Review (SADR-0010)\ndiff --git a/docs/agents/implementation-policy.md b/docs/agents/implementation-policy.md\nindex c754144a9fb5bf4a38aa82a60e1670c59e626b44..85c37281bcad196c6b7b02343faf00b03cd9c57a\n--- a/docs/agents/implementation-policy.md\n+++ b/docs/agents/implementation-policy.md\n@@ -31,10 +31,13 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n ## Non-Negotiables\n \n 1. **Planning precedes implementation.** Non-trivial Goals start with `grill-with-docs` and Context & Impact.\n-2. **Domain language is controlled.** Terms must be checked against `CONTEXT.md`; hard-to-reverse tradeoffs must be checked against ADRs.\n+2. **Domain language is controlled.** Terms must be checked against `CONTEXT.md`; hard-to-reverse tradeoffs must be checked against Shiki SADRs and target ADRs.\n 3. **PRD precedes issue decomposition.** `to-prd` is used once the context is settled enough to become durable intent.\n 4. **Issues are vertical slices.** `to-issues` must produce independently grabbable tracer-bullet tasks, not horizontal layer tickets.\n 5. **Triage controls dispatch.** Only `ready-for-agent` / AFK tasks may be assigned to Codex without human input.\n@@ -95,19 +98,19 @@\n Process:\n \n - Challenge terms against `CONTEXT.md`.\n-- Check ADRs before reopening decisions.\n+- Check SADRs and target ADRs before reopening decisions.\n - Inspect code when code can answer a question.\n - Ask one question at a time when the operator is available.\n - Provide a recommended answer with each question.\n - Record resolved terms in `CONTEXT.md`.\n-- Create ADRs only for hard-to-reverse, surprising, tradeoff-based decisions.\n+- Create SADRs or target ADRs only for hard-to-reverse, surprising, tradeoff-based decisions.\n - Convert unresolved load-bearing questions into HITL blockers.\n \n Outputs:\n \n - Resolved design-tree decisions.\n - Updated glossary terms when needed.\n-- ADRs when justified.\n+- SADRs or target ADRs when justified.\n - HITL blockers when decisions remain unresolved.\n \n ## Phase 3 \u2014 Context & Impact\n@@ -117,7 +120,7 @@\n \n Outputs must include:\n \n-- Relevant documents and ADRs.\n+- Relevant documents, SADRs, and target ADRs.\n - Relevant modules, interfaces, seams, callers, and tests.\n - Candidate locks.\n - Dependencies.\n@@ -140,7 +143,7 @@\n - Testing decisions.\n - Out of scope.\n - Further notes.\n-- Links to relevant Goal, domain terms, and ADRs.\n+- Links to relevant Goal, domain terms, SADRs, and target ADRs.\n \n Phase 4 ends with Spec Freeze: the operator explicitly approves the PRD, the\n plan records a `spec_freeze` block (status, approved_by, source), and the SF\n@@ -240,7 +243,7 @@\n - Dependencies and locks.\n - Risk and Guardian requirements.\n - Ledger completeness.\n-- Documentation and ADR updates.\n+- Documentation and SADR/target-ADR updates.\n - Scope drift.\n \n CCA emits one of:\n@@ -301,7 +304,7 @@\n | Evidence failure | Behavior might be done, but proof is missing. | Add checks, PR evidence, ledger entry, or artifacts. |\n | Scope drift | PR changes unrelated behavior. | Revert unrelated changes or split into new Goal. |\n | Dependency block | Upstream task or lock not complete. | Block until dependency resolves. |\n-| Architecture block | Decision needs ADR or deep-module discussion. | `grill-with-docs` or `improve-codebase-architecture`. |\n+| Architecture block | Decision needs a SADR, target ADR, or deep-module discussion. | `grill-with-docs` or `improve-codebase-architecture`. |\n | Risk block | Security/production/policy change needs human approval. | Guardian decision. |\n \n ## Phase 5.5 \u2014 Execution Decision Control\ndiff --git a/docs/agents/runtime-auth-model.md b/docs/agents/runtime-auth-model.md\nindex 6a2e1a47e81ea89b5c68a210ec07772a9374babc..3c54748d24233dbd0ba8473950282547ef4ce935\n--- a/docs/agents/runtime-auth-model.md\n+++ b/docs/agents/runtime-auth-model.md\n@@ -148,7 +148,7 @@\n \n API-key based runners may be added by a target repository as an explicit extension. They are not the default Shiki template path.\n \n-If a repo opts into API-key mode, record the exception in an ADR and update the repo's `AGENTS.md`, `.shiki/config.yaml`, and workflow permissions.\n+If a repo opts into API-key mode, record the target-specific exception in a target ADR and update the repo's `AGENTS.md`, `.shiki/config.yaml`, and workflow permissions.\n \n ## GitHub Actions Runtime Compatibility\n \ndiff --git a/docs/agents/skill-gate.md b/docs/agents/skill-gate.md\nindex c50398b0d7c60a8598f2a5c88a10015730dced13..3acffe6444617bb1d54b77d4b4708158ec735bc6\n--- a/docs/agents/skill-gate.md\n+++ b/docs/agents/skill-gate.md\n@@ -9,7 +9,7 @@\n | Skill | Trigger | Expected output |\n | --- | --- | --- |\n | `setup-matt-pocock-skills` | First configuring a repo for these skills, or when issue tracker, triage labels, or domain docs are missing. | `docs/agents/` configuration and an `Agent skills` block in `AGENTS.md` or `CLAUDE.md`. |\n-| `grill-with-docs` | Ambiguous plan, unclear terminology, boundary decision, tradeoff, or ADR-worthy design choice. | Clarified decision tree, recommended answers, updates to `CONTEXT.md` or ADRs when decisions crystallize. |\n+| `grill-with-docs` | Ambiguous plan, unclear terminology, boundary decision, tradeoff, or decision-record-worthy design choice. | Clarified decision tree, recommended answers, updates to `CONTEXT.md`, SADRs, or target ADRs when decisions crystallize. |\n | `zoom-out` | Agent lacks a higher-level architectural map. | Map of relevant modules, callers, seams, and domain concepts. |\n | `to-prd` | Settled context should become a PRD. | PRD published to the issue tracker with problem, solution, user stories, decisions, testing, and out-of-scope notes. |\n | `to-issues` | Goal, PRD, or plan must become implementation tickets. | Vertical-slice issues with acceptance criteria, dependencies, and AFK/HITL classification. |\n@@ -17,7 +17,7 @@\n | `tdd` | Feature work or bug fix has observable behavior that can be specified with tests. | Red-green-refactor loop through public interfaces, one vertical slice at a time. |\n | `code-review` | The TDD loop is green and the implementer is about to create or update a PR. Default for every implementation task. | Pre-PR self-review of the task diff with fixes applied on the task branch; findings and resolutions recorded as a ledger entry naming the skill and a `## Pre-PR code review` PR body section. |\n | `diagnose` | Hard bug, failing check, regression, flaky behavior, or performance problem. | Reproduction loop, ranked hypotheses, instrumentation, fix, regression test, post-mortem. |\n-| `improve-codebase-architecture` | Structural friction, testability, AI-navigability, deep-module opportunity, or refactor request. | Architecture candidates and deepening recommendations informed by `CONTEXT.md` and ADRs. |\n+| `improve-codebase-architecture` | Structural friction, testability, AI-navigability, deep-module opportunity, or refactor request. | Architecture candidates and deepening recommendations informed by `CONTEXT.md`, SADRs, and target ADRs. |\n | `prototype` | A throwaway logic or UI prototype is needed to answer a design question. | Clearly marked disposable prototype and captured decision. |\n \n ## MergeGate Interaction\ndiff --git a/docs/operations.md b/docs/operations.md\nindex cf1f452d4c589e38ccd58553bda0ad8684b51eb7..ace48a90ffbec9e5c0a7113bef16a4858bc44b3f\n--- a/docs/operations.md\n+++ b/docs/operations.md\n@@ -89,6 +89,16 @@\n      target history, brought forward by `shiki migrate`, not a template.\n    - **Everything else** (CLI scripts, workflows, schemas, docs) is overwritten.\n \n+   A forced upgrade also migrates the former numeric Shiki decision paths to\n+   `SADR-NNNN-*`. Before the first write, the installer inventories the exact\n+   legacy Shiki paths and authorizes each deletion only when the existing\n+   `.shiki/install-stamp.json` names that path and its SHA-256 digest still\n+   matches the file. An absent, unreadable, malformed, incomplete, or\n+   mismatching stamp reports every blocked path and stops with no filesystem\n+   change. Target-owned `NNNN-*` ADRs are not cleanup candidates. A non-force\n+   install deletes nothing and retains any still-matching legacy stamp entries\n+   so a later forced upgrade can prove ownership.\n+\n    If the target has pending migrations, `--force` refuses and writes nothing \u2014\n    apply migrations first (step 3), then re-run. The run ends with a single\n    summary of every `.new` written, so a half-upgrade is visible.\ndiff --git a/docs/releasing.md b/docs/releasing.md\nindex 10d73828e7dbd64e9d9f6b83bc84d010537ce62a..aafeee8c4d441e1e427efee2ce4f81a647f020e1\n--- a/docs/releasing.md\n+++ b/docs/releasing.md\n@@ -67,7 +67,7 @@\n CLI in `scripts/`. There are no third-party Python runtime dependencies. The CLI\n is intentionally **not** exposed as a `[project.scripts]` console entry point\n because `scripts/shiki.py` is a dependency-free shim and `scripts/` is not\n-packaged as an importable distribution (see the ADR for the rationale).\n+packaged as an importable distribution (see SADR-0007 for the rationale).\n \n Supported install paths:\n \n@@ -155,6 +155,15 @@\n    [docs/operations.md](operations.md#the-new-convention). If the target has\n    pending migrations, `--force` refuses and writes nothing; apply migrations\n    (step 2) first, then re-run.\n+\n+   The SADR namespace upgrade has an additional pre-write gate: each former\n+   numeric Shiki decision path is removed only when the existing install stamp\n+   contains that exact path and a digest matching its current bytes. The\n+   installer aggregates absent/unreadable/malformed-stamp, missing-digest, and\n+   digest-mismatch blockers before raising, so refusal leaves the target tree\n+   byte-identical. Numeric target ADRs are preserved. A successful forced\n+   upgrade installs and stamps `SADR-*` paths and drops the removed legacy paths\n+   from the refreshed stamp.\n 2. Apply repository-local `.shiki/` state migrations with the migrate command:\n \n    ```bash\ndiff --git a/docs/usage.md b/docs/usage.md\nindex b83407ee3970dd395dd893010bc5dbe8bad26051..a130ef6e40dfd8169a5bc701339351097df1a09f\n--- a/docs/usage.md\n+++ b/docs/usage.md\n@@ -67,8 +67,10 @@\n shiki install-target /path/to/target-repo --local-only\n ```\n \n-Use `--force` only when you intentionally want to overwrite existing target\n-files.\n+Use `--force` only when you intentionally want to refresh the target template.\n+For the SADR namespace migration, `--force` deletes only exact legacy Shiki\n+decision paths whose existing install-stamp digest still matches; any ownership\n+blocker stops before the first write. Numeric target ADRs are never candidates.\n \n ## `shiki bootstrap-platform` \u2014 publish the Shiki platform repo\n \ndiff --git a/scripts/guardian_approval_signal.py b/scripts/guardian_approval_signal.py\nindex d059f92b2c82aa2b5d6ca421365537a87886c1cb..16924892714f4f5b9303c0ef0e325a7e70075630\n--- a/scripts/guardian_approval_signal.py\n+++ b/scripts/guardian_approval_signal.py\n@@ -199,8 +199,8 @@\n ) -> Any:\n     \"\"\"Evaluate SADR-0015 Contract Approval for the CCA Guardian signal.\n \n-    The SAME OR that MergeGate applies must reach this deterministic signal: ADR\n-    0015 is explicit that without it, a contract-approved PR still returns\n+    The SAME OR that MergeGate applies must reach this deterministic signal:\n+    SADR-0015 is explicit that without it, a contract-approved PR still returns\n     ``needs_guardian``, the CCA job fails, and the MergeGate policy check never\n     runs \u2014 the gate this change moves would never be reached. This resolves the\n     task the SAME way MergeGate does (the first Shiki task id in the PR body),\ndiff --git a/scripts/shiki_contract_approval.py b/scripts/shiki_contract_approval.py\nindex 941bcb96fdd8b9a85bc0c4f0f04d5dad9d04839c..4a4b418f6527413161db82670bec857a043360e2\n--- a/scripts/shiki_contract_approval.py\n+++ b/scripts/shiki_contract_approval.py\n@@ -142,7 +142,7 @@\n     changed_files_status: Any,\n     registration: Any,\n ) -> ContractApprovalResult:\n-    \"\"\"Whether this PR carries an SADR-0015 Contract Approval.\n+    \"\"\"Whether this PR carries a SADR-0015 Contract Approval.\n \n     The four conditions are evaluated IN ORDER, each failure recorded as a\n     distinct reason string:\ndiff --git a/scripts/shiki_guardian.py b/scripts/shiki_guardian.py\nindex a4ecb79bbda55fd5c8b06adfae2eb99760a03c45..4dd99bbe714e26f421fa19abac7a34ae1b195ecc\n--- a/scripts/shiki_guardian.py\n+++ b/scripts/shiki_guardian.py\n@@ -12,7 +12,7 @@\n # The carry re-derives its proof from git in-process on every judging invocation;\n # it is never read from an artifact, so there is nothing to go stale or be forged.\n # shiki_sync_proof is the ONLY guardian-family module that runs git, and every one\n-# of its invocations is read-only and local (ADR: prove-from-git base sync).\n+# of its invocations is read-only and local (SADR: prove-from-git base sync).\n from shiki_sync_proof import verify_pure_base_sync\n \n GUARDIAN_POLICY_PATH = \".shiki/guardian-policy.json\"\ndiff --git a/scripts/shiki_guardian_review.py b/scripts/shiki_guardian_review.py\nindex dd0f104d457899e0ba5ff1e162011c1e90b6f92b..e10ef58c046324e7ea3a3392feb3048ca77414ce\n--- a/scripts/shiki_guardian_review.py\n+++ b/scripts/shiki_guardian_review.py\n@@ -159,7 +159,7 @@\n     docs_only = _is_docs_only(changed_files)\n     if docs_only and matched == 0:\n         focus.append(\n-            \"Docs/ADR-only change: confirm the change is genuinely documentation/decision-only, introduces no behavior or contract drift, and stays consistent with the constitution and existing ADRs.\"\n+            \"Docs/decision-record-only change: confirm the change is genuinely documentation/decision-only, introduces no behavior or contract drift, and stays consistent with the constitution and existing SADRs and target ADRs.\"\n         )\n \n     # Conservative augmentation: unknown (no category and not docs-only), mixed\ndiff --git a/scripts/shiki_installer.py b/scripts/shiki_installer.py\nindex be064ab7a29ff6836f7f7a1d70500a43e0d11d7b..9fd8eefd137ce7003dad5c0dcb2e48e2cd47ab32\n--- a/scripts/shiki_installer.py\n+++ b/scripts/shiki_installer.py\n@@ -118,6 +118,7 @@\n # what lets a target carry it without a governance-schema change.\n INSTALL_STAMP_PATH = \".shiki/install-stamp.json\"\n INSTALL_STAMP_VERSION = 1\n+LEGACY_SHIKI_SADR_MAX = 18\n \n # Under --force the shipped surface splits three ways, so nothing is lost and\n # nothing is silently stale.\n@@ -237,9 +238,98 @@\n         )\n \n \n+def legacy_shiki_adr_paths() -> tuple[str, ...]:\n+    \"\"\"Former numeric paths for Shiki SADR-0001 through SADR-0018.\n+\n+    The authoritative slugs come from the current SADR filenames. SADR-0019 is\n+    the namespace decision itself and never shipped under a numeric legacy path.\n+    \"\"\"\n+    paths: list[str] = []\n+    for sadr in sorted((ROOT / \"docs\" / \"adr\").glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\")):\n+        number = sadr.name.split(\"-\", 2)[1]\n+        if int(number) <= LEGACY_SHIKI_SADR_MAX:\n+            paths.append(f\"docs/adr/{sadr.name.removeprefix('SADR-')}\")\n+    return tuple(paths)\n+\n+\n+def preflight_legacy_sadr_cleanup(target: Path, *, force: bool) -> tuple[str, ...]:\n+    \"\"\"Authorize exact legacy Shiki ADR removals before the first write.\n+\n+    A non-force install never removes existing files. A forced install needs no\n+    stamp when no legacy Shiki path is present. When legacy paths do exist, each\n+    must be named in a readable install stamp and its current bytes must match\n+    the recorded digest.\n+    \"\"\"\n+    if not force:\n+        return ()\n+    present = tuple(\n+        relative\n+        for relative in legacy_shiki_adr_paths()\n+        if (target / relative).exists() or (target / relative).is_symlink()\n+    )\n+    if not present:\n+        return ()\n+\n+    stamp_path = target / INSTALL_STAMP_PATH\n+    try:\n+        raw = stamp_path.read_text(encoding=\"utf-8\")\n+        stamp = json.loads(raw)\n+    except FileNotFoundError:\n+        detail = \"install stamp is absent\"\n+        blockers = [f\"{relative}: {detail}\" for relative in present]\n+    except (OSError, UnicodeDecodeError) as error:\n+        detail = f\"install stamp is unreadable ({error})\"\n+        blockers = [f\"{relative}: {detail}\" for relative in present]\n+    except json.JSONDecodeError as error:\n+        detail = f\"install stamp is malformed ({error})\"\n+        blockers = [f\"{relative}: {detail}\" for relative in present]\n+    else:\n+        if not isinstance(stamp, dict):\n+            blockers = [\n+                f\"{relative}: install stamp is malformed (expected an object)\"\n+                for relative in present\n+            ]\n+        else:\n+            digests = stamp.get(\"digests\")\n+            blockers = []\n+            if not isinstance(digests, dict):\n+                blockers.extend(\n+                    f\"{relative}: install stamp has no digest for this path\"\n+                    for relative in present\n+                )\n+            else:\n+                for relative in present:\n+                    expected = digests.get(relative)\n+                    if not isinstance(expected, str) or not expected:\n+                        blockers.append(\n+                            f\"{relative}: install stamp has no digest for this path\"\n+                        )\n+                        continue\n+                    try:\n+                        actual = _sha256_file(target / relative)\n+                    except OSError as error:\n+                        blockers.append(\n+                            f\"{relative}: legacy path is unreadable ({error})\"\n+                        )\n+                        continue\n+                    if actual != expected:\n+                        blockers.append(\n+                            f\"{relative}: digest mismatch (stamp {expected}, current {actual})\"\n+                        )\n+\n+    if blockers:\n+        joined = \"\\n\".join(f\"- {blocker}\" for blocker in blockers)\n+        raise ShikiError(\n+            \"legacy Shiki ADR cleanup is not authorized:\\n\"\n+            f\"{joined}\\nNo files were written.\"\n+        )\n+    return present\n+\n+\n def install_template(target: Path, *, force: bool, validate: bool) -> list[NewFileNote]:\n     # Preconditions first: never rewrite the tree of a target that cannot be\n     # upgraded cleanly (see refuse_pending_migrations).\n+    legacy_cleanup = preflight_legacy_sadr_cleanup(target, force=force)\n     refuse_pending_migrations(target)\n \n     # Resolve the target's required CODEOWNERS owner once from its\n@@ -250,6 +340,10 @@\n     # verbatim. See ``codeowners_required_owner``.\n     codeowners_owner = codeowners_required_owner(target)\n \n+    for relative in legacy_cleanup:\n+        (target / relative).unlink()\n+        info(f\"removed stamped legacy Shiki ADR: {target / relative}\")\n+\n     notes: list[NewFileNote] = []\n     for relative in TEMPLATE_PATHS:\n         source = ROOT / relative\n@@ -647,6 +741,22 @@\n     digests = target_stamp_digests(target)\n     existing = read_install_stamp(target)\n     if existing is not None:\n+        # A non-force install deliberately leaves legacy Shiki ADRs in place.\n+        # Preserve only still-matching ownership proof so a later forced\n+        # upgrade can authorize cleanup; never carry a missing or stale digest.\n+        existing_digests = existing.get(\"digests\")\n+        if isinstance(existing_digests, dict):\n+            for relative in legacy_shiki_adr_paths():\n+                expected = existing_digests.get(relative)\n+                path = target / relative\n+                if not isinstance(expected, str) or not path.is_file():\n+                    continue\n+                try:\n+                    actual = _sha256_file(path)\n+                except OSError:\n+                    continue\n+                if actual == expected:\n+                    digests[relative] = actual\n         if _incoming_is_older(commit, existing.get(\"platform_commit\")):\n             warn(\n                 \"kept existing install stamp: incoming install \"\ndiff --git a/scripts/shiki_loop.py b/scripts/shiki_loop.py\nindex 3611641da8aeb5038b1da90e3114b09a473ec6d8..84caa491b9cad0edc4176f209c13e9221e975681\n--- a/scripts/shiki_loop.py\n+++ b/scripts/shiki_loop.py\n@@ -424,7 +424,7 @@\n         # unresolvable CCA verdict has already produced its terminal stop and is never\n         # laundered into a head-moving sync (SADR-0010). It also never fires while the\n         # policy (Guardian) gate is red \u2014 for ANY reason, not only when it is the sole\n-        # red check \u2014 so a head-bound Guardian approval, or an SADR-0015\n+        # red check \u2014 so a head-bound Guardian approval, or a SADR-0015\n         # contract-approval carry that base movement defeated (the policy gate compares\n         # base and head task governance directly), is never silently restored by\n         # adopting base wholesale. It is bounded by a durable per-task counter so the\ndiff --git a/scripts/shiki_tasks.py b/scripts/shiki_tasks.py\nindex aec3809f24b4a0c8c96fc50971cff9ae9ad30381..d531052e9d9717058d4631483cefab44c26a65c1\n--- a/scripts/shiki_tasks.py\n+++ b/scripts/shiki_tasks.py\n@@ -821,7 +821,7 @@\n         \"prompt\": prompt,\n         \"entry_skill\": \"grill-with-docs\",\n         \"required_next_steps\": [\n-            \"Run grill-with-docs until terminology, boundaries, risks, and ADR-worthy decisions are settled.\",\n+            \"Run grill-with-docs until terminology, boundaries, risks, and decision-record-worthy choices are settled.\",\n             \"Obtain the operator's explicit approval of the PRD/requirements (Spec Freeze).\",\n             \"Write a machine-readable plan JSON with grill_with_docs.status=complete and spec_freeze.status=frozen.\",\n             \"Run shiki plan ingest --plan-file PLAN.json.\",\ndiff --git a/scripts/test_shiki_goal_loop.sh b/scripts/test_shiki_goal_loop.sh\nindex 753c7b516e10f0be3bc92a4bd0eb1fa699776ae6..8f3820a0a079b3044424cfe1a6c6b7fe68896e67\n--- a/scripts/test_shiki_goal_loop.sh\n+++ b/scripts/test_shiki_goal_loop.sh\n@@ -231,8 +231,8 @@\n \n # High/critical risk merges autonomously when all required checks are green:\n # the \"MergeGate policy check\" required check IS the Guardian gate (it enforces\n-# guardian-policy.json \u2014 human approval OR external AI guardian review, ADR\n-# 0010), so green checks mean a recorded authority approved. The fake gh returns\n+# guardian-policy.json \u2014 human approval OR external AI guardian review,\n+# SADR-0010), so green checks mean a recorded authority approved. The fake gh returns\n # green checks, simulating that the Guardian gate passed.\n python3 \"$ROOT/scripts/shiki.py\" goal create --target \"$TARGET\" --title \"Guardian gate\" --outcome \"High risk merges via policy gate\" >/tmp/shiki-goal-loop-guardian-goal.json\n GGOAL=\"$(json_get /tmp/shiki-goal-loop-guardian-goal.json goal_id)\"\ndiff --git a/tests/test_adr_references.py b/tests/test_adr_references.py\nindex 51706329c797d5dd9b47e082a333fa83b261d482..d005da4f771f5e8085ec22d4b787980e68f5ae8c\n--- a/tests/test_adr_references.py\n+++ b/tests/test_adr_references.py\n@@ -1,8 +1,8 @@\n-\"\"\"Every ``ADR ####`` citation must resolve to exactly one decision record.\n+\"\"\"Every target ADR and Shiki SADR citation resolves in its own namespace.\n \n Background. ``scripts/mergegate_check.py`` justifies ``is_bookkeeping_closeout``\n-with \"ADR 0017\" nine times and ``scripts/guardian_approval_signal.py`` cites\n-0016/0017 seven times, yet for a stretch the numbering in ``docs/adr/`` stopped\n+with \"SADR-0017\" nine times and ``scripts/guardian_approval_signal.py`` cites\n+SADR-0016/SADR-0017 seven times, yet for a stretch the numbering in ``docs/adr/`` stopped\n at 0015: the authoring commits lived on a coordinator branch and no task ever\n declared ``docs/adr/...`` in its locks, so nothing carried the records to the\n default branch. The stated basis of an exemption that merged seven PRs with zero\n@@ -11,11 +11,12 @@\n and the directory drifted with no signal.\n \n This is the same unbound-pair shape behind other silent failures: a reference\n-and its target with nothing asserting they agree. This test binds them. It scans\n-every ``scripts/*.py`` (plus ``docs/**/*.md`` and the root ``*.md`` governance\n-files, which are cheap) for the ``ADR ####`` pattern and asserts each cited\n-number resolves to exactly one ``docs/adr/####-*.md``. The converse is\n-deliberately NOT required: an ADR may exist without being cited.\n+and its target with nothing asserting they agree. This test binds them. Target\n+``ADR NNNN`` citations resolve only to ``docs/adr/NNNN-*.md``; Shiki\n+``SADR-NNNN`` citations resolve only to ``docs/adr/SADR-NNNN-*.md``. The scan\n+covers current governance docs, scripts, workflows, prompts, packaging metadata,\n+and repo-local Codex guidance while deliberately excluding historical\n+``.shiki`` evidence. The converse is not required: a record may be uncited.\n \n The suite is dependency free: standard-library ``unittest`` only.\n \"\"\"\n@@ -31,17 +32,18 @@\n \n REPO_ROOT = Path(__file__).resolve().parents[1]\n \n-# \"ADR 0016\" -- a four-digit citation. The trailing \\b keeps a five-digit token\n-# from being read as a truncated four-digit number.\n-_CITATION = re.compile(r\"\\bADR (\\d{4})\\b\")\n-# \"0016-locks-bound-product-paths.md\" -- the leading four digits name the record.\n-_ADR_FILENAME = re.compile(r\"^(\\d{4})-.*\\.md$\")\n+# ``ADR 0016`` / ``SADR-0016`` -- a namespace plus four-digit citation. The\n+# trailing ``\\b`` keeps a five-digit token from being read as a truncated one.\n+_CITATION = re.compile(r\"\\b(S?ADR)[ -](\\d{4})\\b\")\n+# Target ADRs retain the numeric filename; Shiki platform decisions use SADR.\n+_DECISION_FILENAME = re.compile(r\"^(?:(SADR)-)?(\\d{4})-.*\\.md$\")\n \n \n class Violation(NamedTuple):\n     \"\"\"A citation that does not resolve to exactly one decision record.\"\"\"\n \n     citing_file: str  # repo-relative path of the file that cites the ADR\n+    namespace: str  # \"ADR\" for target decisions; \"SADR\" for Shiki decisions\n     number: str  # the cited four-digit number, e.g. \"0016\"\n     line: int  # 1-based line of the citation\n     reason: str  # \"missing\" (zero matches) or \"ambiguous\" (two or more)\n@@ -49,44 +51,59 @@\n \n     @property\n     def expected_path(self) -> str:\n-        return f\"docs/adr/{self.number}-*.md\"\n+        prefix = \"SADR-\" if self.namespace == \"SADR\" else \"\"\n+        return f\"docs/adr/{prefix}{self.number}-*.md\"\n \n     def describe(self) -> str:\n         \"\"\"Name the citing file, the cited number, and the missing path.\"\"\"\n         if self.reason == \"missing\":\n             return (\n-                f\"{self.citing_file}:{self.line} cites ADR {self.number} \"\n+                f\"{self.citing_file}:{self.line} cites {self.namespace} {self.number} \"\n                 f\"but no document matches {self.expected_path}\"\n             )\n         joined = \", \".join(self.matches)\n         return (\n-            f\"{self.citing_file}:{self.line} cites ADR {self.number} which \"\n+            f\"{self.citing_file}:{self.line} cites {self.namespace} {self.number} which \"\n             f\"resolves to {len(self.matches)} documents (expected exactly one \"\n             f\"at {self.expected_path}): {joined}\"\n         )\n \n \n-def _adr_index(root: Path) -> dict[str, list[Path]]:\n-    \"\"\"Map every four-digit ADR number to the record files that claim it.\"\"\"\n-    index: dict[str, list[Path]] = {}\n+def _adr_index(root: Path) -> dict[tuple[str, str], list[Path]]:\n+    \"\"\"Map each decision namespace and number to the files that claim it.\"\"\"\n+    index: dict[tuple[str, str], list[Path]] = {}\n     adr_dir = root / \"docs\" / \"adr\"\n     if adr_dir.is_dir():\n         for path in sorted(adr_dir.glob(\"*.md\")):\n-            match = _ADR_FILENAME.match(path.name)\n+            match = _DECISION_FILENAME.match(path.name)\n             if match:\n-                index.setdefault(match.group(1), []).append(path)\n+                namespace = \"SADR\" if match.group(1) else \"ADR\"\n+                index.setdefault((namespace, match.group(2)), []).append(path)\n     return index\n \n \n def _citing_files(root: Path, include_docs: bool = True) -> list[Path]:\n-    \"\"\"Files scanned for citations: scripts/*.py, and cheaply the markdown.\"\"\"\n+    \"\"\"Current, shipped text surfaces scanned for decision citations.\"\"\"\n     files = sorted((root / \"scripts\").glob(\"*.py\"))\n+    files += sorted((root / \"scripts\").glob(\"*.sh\"))\n     if include_docs:\n         docs = root / \"docs\"\n         if docs.is_dir():\n             files += sorted(docs.rglob(\"*.md\"))\n         files += sorted(root.glob(\"*.md\"))\n-    return files\n+        pyproject = root / \"pyproject.toml\"\n+        if pyproject.is_file():\n+            files.append(pyproject)\n+        for relative in (\".github\", \".codex\"):\n+            surface = root / relative\n+            if surface.is_dir():\n+                files += sorted(\n+                    path\n+                    for path in surface.rglob(\"*\")\n+                    if path.is_file()\n+                    and path.suffix in {\".md\", \".py\", \".sh\", \".toml\", \".yaml\", \".yml\"}\n+                )\n+    return sorted(set(files))\n \n \n def find_adr_reference_violations(\n@@ -94,10 +111,9 @@\n ) -> list[Violation]:\n     \"\"\"Return every ``ADR ####`` citation under ``root`` that fails to resolve.\n \n-    A citation resolves when exactly one ``docs/adr/####-*.md`` matches its\n-    number. Zero matches is a \"missing\" violation; two or more is \"ambiguous\".\n-    Records that are never cited produce nothing -- the converse is not\n-    required.\n+    A citation resolves when exactly one record in the same namespace matches\n+    its number. Zero matches is a \"missing\" violation; two or more is\n+    \"ambiguous\". Records that are never cited produce nothing.\n     \"\"\"\n     root = Path(root)\n     index = _adr_index(root)\n@@ -110,13 +126,15 @@\n         rel = path.relative_to(root).as_posix()\n         for lineno, line in enumerate(text.splitlines(), start=1):\n             for match in _CITATION.finditer(line):\n-                number = match.group(1)\n-                found = index.get(number, [])\n+                namespace, number = match.groups()\n+                found = index.get((namespace, number), [])\n                 if len(found) == 1:\n                     continue\n                 reason = \"missing\" if not found else \"ambiguous\"\n                 rels = [p.relative_to(root).as_posix() for p in found]\n-                violations.append(Violation(rel, number, lineno, reason, rels))\n+                violations.append(\n+                    Violation(rel, namespace, number, lineno, reason, rels)\n+                )\n     return violations\n \n \n@@ -129,8 +147,8 @@\n         self.assertEqual(\n             violations,\n             [],\n-            \"Every 'ADR ####' citation must resolve to exactly one \"\n-            f\"docs/adr/####-*.md:\\n{detail}\",\n+            \"Every ADR/SADR citation must resolve exactly once in its own \"\n+            f\"docs/adr namespace:\\n{detail}\",\n         )\n \n     def test_scan_actually_reads_scripts(self) -> None:\n@@ -142,12 +160,38 @@\n             \"the citation scan must cover scripts/*.py\",\n         )\n \n+    def test_platform_citations_do_not_use_unresolved_numeric_shorthand(self) -> None:\n+        shorthand = re.compile(r\"SADR-\\d{4}/\\d{4}\")\n+        offenders = []\n+        for path in _citing_files(REPO_ROOT):\n+            text = path.read_text(encoding=\"utf-8\")\n+            if shorthand.search(text):\n+                offenders.append(path.relative_to(REPO_ROOT).as_posix())\n+        self.assertEqual(\n+            offenders,\n+            [],\n+            \"repeat the SADR prefix so every cited number is independently resolvable\",\n+        )\n+\n     def test_carried_records_are_indexed(self) -> None:\n         # The two records this Goal carried onto the default branch, and which\n         # the scripts cite most, must be present.\n         index = _adr_index(REPO_ROOT)\n-        self.assertIn(\"0016\", index)\n-        self.assertIn(\"0017\", index)\n+        self.assertIn((\"SADR\", \"0016\"), index)\n+        self.assertIn((\"SADR\", \"0017\"), index)\n+\n+    def test_platform_tree_has_only_sadr_0001_through_0019(self) -> None:\n+        adr_dir = REPO_ROOT / \"docs\" / \"adr\"\n+        sadrs = sorted(adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\"))\n+        self.assertEqual(\n+            {path.name.split(\"-\", 2)[1] for path in sadrs},\n+            {f\"{number:04d}\" for number in range(1, 20)},\n+        )\n+        self.assertEqual(\n+            list(adr_dir.glob(\"[0-9][0-9][0-9][0-9]-*.md\")),\n+            [],\n+            \"the Shiki platform tree must not retain legacy numeric SADR paths\",\n+        )\n \n \n class FixtureAdrReferences(unittest.TestCase):\n@@ -180,6 +224,26 @@\n         self.assertIn(\"9991\", message)  # cited number\n         self.assertIn(\"docs/adr/9991-*.md\", message)  # missing path\n \n+    def test_target_adr_does_not_satisfy_missing_platform_sadr(self) -> None:\n+        root = self._make_repo(\n+            adr_names=[\"0042-target-decision.md\"],\n+            script_body='\"\"\"Target ADR 0042 exists, but platform SADR-0042 does not.\"\"\"\\n',\n+        )\n+        violations = find_adr_reference_violations(root, include_docs=False)\n+        self.assertEqual(len(violations), 1)\n+        self.assertEqual(violations[0].namespace, \"SADR\")\n+        self.assertEqual(violations[0].number, \"0042\")\n+        self.assertEqual(violations[0].expected_path, \"docs/adr/SADR-0042-*.md\")\n+\n+    def test_target_adr_and_platform_sadr_may_reuse_a_number(self) -> None:\n+        root = self._make_repo(\n+            adr_names=[\"0042-target-decision.md\", \"SADR-0042-platform-decision.md\"],\n+            script_body='\"\"\"Target ADR 0042 and platform SADR-0042 are distinct.\"\"\"\\n',\n+        )\n+        self.assertEqual(\n+            find_adr_reference_violations(root, include_docs=False), []\n+        )\n+\n     def test_uncited_adr_is_allowed(self) -> None:\n         # An ADR exists but nothing cites it -> not a violation. The converse\n         # (every ADR must be cited) is explicitly NOT required.\ndiff --git a/tests/test_guardian_review.py b/tests/test_guardian_review.py\nindex 88829e7810212635e2fc98aa5918d6f26294677d..00a85a237acd5bf2d6cb5b294128198c422402ca\n--- a/tests/test_guardian_review.py\n+++ b/tests/test_guardian_review.py\n@@ -162,7 +162,7 @@\n \n     def test_docs_only_is_light_and_not_conservative(self):\n         focus = gr.classify_review_focus([\"docs/adr/0099-x.md\", \"README.md\"], \"\", \"low\")\n-        self.assertTrue(any(\"Docs/ADR-only\" in f for f in focus))\n+        self.assertTrue(any(\"Docs/decision-record-only\" in f for f in focus))\n         self.assertFalse(any(\"conservative default\" in f.lower() for f in focus))\n \n     def test_unknown_category_is_conservative(self):\ndiff --git a/tests/test_policy_job_merged_prs.py b/tests/test_policy_job_merged_prs.py\nindex d45512e74681e5e649279236c7d4e79d5c3da471..b97989f8bd74d091de1a6b5275a20d8dc68c4512\n--- a/tests/test_policy_job_merged_prs.py\n+++ b/tests/test_policy_job_merged_prs.py\n@@ -3,8 +3,8 @@\n \n ``.github/workflows/shiki-cca-completion.yml`` has two consumers of the merged-PR\n proof: the CCA *signal* (``guardian_approval_signal.py`` in \"Prepare CCA context\")\n-and the authoritative gate (``mergegate_check.py`` in \"Enforce MergeGate\"). ADR\n-0017's bookkeeping-closeout exemption (condition 6) needs the merged implementation\n+and the authoritative gate (``mergegate_check.py`` in \"Enforce MergeGate\").\n+SADR-0017's bookkeeping-closeout exemption (condition 6) needs the merged implementation\n PR proven, and a closeout carries NO ``post_merge_reconcile`` marker. The signal\n already resolves ``MERGED_PRS`` unconditionally, but \"Enforce MergeGate\" used to\n resolve it only inside ``grep -q '<!-- shiki:post_merge_reconcile -->'``. So a\ndiff --git a/tests/test_sadr_install_migration.py b/tests/test_sadr_install_migration.py\nnew file mode 100644\nindex 0000000000000000000000000000000000000000..d51eac872b1c79ff4579c4c1401ca5d775a60d8d\n--- /dev/null\n+++ b/tests/test_sadr_install_migration.py\n@@ -0,0 +1,237 @@\n+\"\"\"SADR installation and collision-safe legacy upgrade behavior.\"\"\"\n+\n+from __future__ import annotations\n+\n+import contextlib\n+import hashlib\n+import io\n+import json\n+import shutil\n+import tempfile\n+import unittest\n+from pathlib import Path\n+\n+import shiki_test_support  # noqa: F401  (path bootstrap)\n+\n+import shiki_installer\n+from tests.test_adr_references import find_adr_reference_violations\n+\n+\n+PRODUCT_NUMBERS = tuple(range(13, 19))\n+\n+\n+def _quiet_install(target: Path, *, force: bool, validate: bool) -> None:\n+    with contextlib.redirect_stdout(io.StringIO()):\n+        shiki_installer.install_template(target, force=force, validate=validate)\n+\n+\n+def _sha256(path: Path) -> str:\n+    return hashlib.sha256(path.read_bytes()).hexdigest()\n+\n+\n+def _legacy_sadr_paths() -> tuple[str, ...]:\n+    return tuple(\n+        f\"docs/adr/{path.name.removeprefix('SADR-')}\"\n+        for path in sorted((shiki_installer.ROOT / \"docs\" / \"adr\").glob(\"SADR-*.md\"))\n+        if path.name != \"SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\"\n+    )\n+\n+\n+def _create_legacy_upgrade_fixture(\n+    target: Path,\n+) -> tuple[dict[str, bytes], tuple[str, ...]]:\n+    adr_dir = target / \"docs\" / \"adr\"\n+    adr_dir.mkdir(parents=True)\n+    product_bytes = {\n+        f\"{number:04d}-cubeshare-product-decision.md\": (\n+            f\"# CubeShare ADR {number:04d}\\n\\nDo not rewrite {number}.\\n\"\n+        ).encode()\n+        for number in PRODUCT_NUMBERS\n+    }\n+    for name, content in product_bytes.items():\n+        (adr_dir / name).write_bytes(content)\n+\n+    legacy_paths = _legacy_sadr_paths()\n+    for relative in legacy_paths:\n+        (target / relative).write_bytes(f\"legacy Shiki bytes for {relative}\\n\".encode())\n+\n+    stamp_path = target / shiki_installer.INSTALL_STAMP_PATH\n+    stamp_path.parent.mkdir(parents=True, exist_ok=True)\n+    stamp = {\n+        \"version\": shiki_installer.INSTALL_STAMP_VERSION,\n+        \"platform_commit\": None,\n+        \"installed_at\": \"2000-01-01T00:00:00+00:00\",\n+        \"digests\": {\n+            relative: _sha256(target / relative) for relative in legacy_paths\n+        },\n+    }\n+    stamp_path.write_text(\n+        json.dumps(stamp, indent=2, sort_keys=True) + \"\\n\", encoding=\"utf-8\"\n+    )\n+    return product_bytes, legacy_paths\n+\n+\n+def _tree_snapshot(root: Path) -> dict[str, tuple[str, bytes | str]]:\n+    \"\"\"Recursive byte/shape snapshot used to prove preflight is write-free.\"\"\"\n+    snapshot: dict[str, tuple[str, bytes | str]] = {}\n+    for path in sorted(root.rglob(\"*\")):\n+        relative = path.relative_to(root).as_posix()\n+        if path.is_symlink():\n+            snapshot[relative] = (\"symlink\", path.readlink().as_posix())\n+        elif path.is_dir():\n+            snapshot[relative] = (\"directory\", \"\")\n+        else:\n+            snapshot[relative] = (\"file\", path.read_bytes())\n+    return snapshot\n+\n+\n+class FreshSadrInstallTests(unittest.TestCase):\n+    def setUp(self) -> None:\n+        self.tmp = Path(tempfile.mkdtemp(prefix=\"shiki-sadr-fresh-\"))\n+        self.addCleanup(lambda: shutil.rmtree(self.tmp, ignore_errors=True))\n+        self.target = self.tmp / \"target\"\n+        self.adr_dir = self.target / \"docs\" / \"adr\"\n+        self.adr_dir.mkdir(parents=True)\n+        self.product_bytes = {\n+            f\"{number:04d}-product-decision.md\": (\n+                f\"# Product ADR {number:04d}\\n\\nTarget-owned bytes {number}.\\n\"\n+            ).encode()\n+            for number in PRODUCT_NUMBERS\n+        }\n+        for name, content in self.product_bytes.items():\n+            (self.adr_dir / name).write_bytes(content)\n+\n+    def test_fresh_install_preserves_product_adrs_and_installs_all_sadrs(self) -> None:\n+        _quiet_install(self.target, force=False, validate=True)\n+\n+        for name, expected in self.product_bytes.items():\n+            self.assertEqual((self.adr_dir / name).read_bytes(), expected)\n+\n+        sadr_numbers = {\n+            path.name.split(\"-\", 2)[1]\n+            for path in self.adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\")\n+        }\n+        self.assertEqual(sadr_numbers, {f\"{number:04d}\" for number in range(1, 20)})\n+\n+        numeric_records = {\n+            path.name\n+            for path in self.adr_dir.glob(\"[0-9][0-9][0-9][0-9]-*.md\")\n+        }\n+        self.assertEqual(numeric_records, set(self.product_bytes))\n+        stamp = json.loads(\n+            (self.target / shiki_installer.INSTALL_STAMP_PATH).read_text(\n+                encoding=\"utf-8\"\n+            )\n+        )\n+        for path in self.adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\"):\n+            relative = path.relative_to(self.target).as_posix()\n+            self.assertEqual(stamp[\"digests\"][relative], _sha256(path))\n+        self.assertEqual(find_adr_reference_violations(self.target), [])\n+\n+\n+class LegacySadrUpgradeTests(unittest.TestCase):\n+    def setUp(self) -> None:\n+        self.tmp = Path(tempfile.mkdtemp(prefix=\"shiki-sadr-upgrade-\"))\n+        self.addCleanup(lambda: shutil.rmtree(self.tmp, ignore_errors=True))\n+        self.target = self.tmp / \"target\"\n+        self.adr_dir = self.target / \"docs\" / \"adr\"\n+        self.product_bytes, self.legacy_paths = _create_legacy_upgrade_fixture(\n+            self.target\n+        )\n+\n+    def test_force_upgrade_deletes_only_stamped_legacy_sadrs(self) -> None:\n+        _quiet_install(self.target, force=True, validate=True)\n+\n+        for relative in self.legacy_paths:\n+            self.assertFalse((self.target / relative).exists(), relative)\n+        for name, expected in self.product_bytes.items():\n+            self.assertEqual((self.adr_dir / name).read_bytes(), expected)\n+\n+        sadrs = list(self.adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\"))\n+        self.assertEqual(len(sadrs), 19)\n+        refreshed = json.loads(\n+            (self.target / shiki_installer.INSTALL_STAMP_PATH).read_text(encoding=\"utf-8\")\n+        )\n+        for relative in self.legacy_paths:\n+            self.assertNotIn(relative, refreshed[\"digests\"])\n+        for path in sadrs:\n+            relative = path.relative_to(self.target).as_posix()\n+            self.assertEqual(refreshed[\"digests\"][relative], _sha256(path))\n+\n+    def test_non_force_install_never_deletes_legacy_files(self) -> None:\n+        before = {\n+            relative: (self.target / relative).read_bytes()\n+            for relative in self.legacy_paths\n+        }\n+        _quiet_install(self.target, force=False, validate=False)\n+        for relative, expected in before.items():\n+            self.assertEqual((self.target / relative).read_bytes(), expected)\n+        refreshed = json.loads(\n+            (self.target / shiki_installer.INSTALL_STAMP_PATH).read_text(encoding=\"utf-8\")\n+        )\n+        for relative in self.legacy_paths:\n+            self.assertEqual(refreshed[\"digests\"][relative], _sha256(self.target / relative))\n+\n+\n+class LegacyCleanupPreflightTests(unittest.TestCase):\n+    def setUp(self) -> None:\n+        self.tmp = Path(tempfile.mkdtemp(prefix=\"shiki-sadr-preflight-\"))\n+        self.addCleanup(lambda: shutil.rmtree(self.tmp, ignore_errors=True))\n+\n+    def test_all_ownership_blockers_abort_before_any_write(self) -> None:\n+        cases = (\n+            (\"absent\", \"install stamp is absent\"),\n+            (\"unreadable\", \"install stamp is unreadable\"),\n+            (\"malformed\", \"install stamp is malformed\"),\n+            (\"missing-digest\", \"install stamp has no digest\"),\n+            (\"digest-mismatch\", \"digest mismatch\"),\n+        )\n+        for name, expected_reason in cases:\n+            with self.subTest(name=name):\n+                target = self.tmp / name\n+                _, legacy_paths = _create_legacy_upgrade_fixture(target)\n+                stamp_path = target / shiki_installer.INSTALL_STAMP_PATH\n+\n+                if name == \"absent\":\n+                    stamp_path.unlink()\n+                elif name == \"unreadable\":\n+                    stamp_path.unlink()\n+                    stamp_path.mkdir()\n+                elif name == \"malformed\":\n+                    stamp_path.write_text(\"{not-json\\n\", encoding=\"utf-8\")\n+                else:\n+                    stamp = json.loads(stamp_path.read_text(encoding=\"utf-8\"))\n+                    if name == \"missing-digest\":\n+                        stamp[\"digests\"] = {}\n+                    else:\n+                        stamp[\"digests\"] = {\n+                            relative: \"deadbeef\" for relative in legacy_paths\n+                        }\n+                    stamp_path.write_text(\n+                        json.dumps(stamp, indent=2, sort_keys=True) + \"\\n\",\n+                        encoding=\"utf-8\",\n+                    )\n+\n+                before = _tree_snapshot(target)\n+                with self.assertRaises(shiki_installer.ShikiError) as raised:\n+                    _quiet_install(target, force=True, validate=False)\n+                after = _tree_snapshot(target)\n+\n+                self.assertEqual(before, after, f\"{name} preflight changed the tree\")\n+                message = str(raised.exception)\n+                self.assertIn(expected_reason, message)\n+                self.assertIn(\"No files were written\", message)\n+                for relative in legacy_paths:\n+                    self.assertIn(relative, message)\n+\n+    def test_absent_stamp_is_allowed_when_no_legacy_path_exists(self) -> None:\n+        target = self.tmp / \"no-legacy\"\n+        target.mkdir()\n+        _quiet_install(target, force=True, validate=False)\n+        self.assertTrue(\n+            (target / \"docs\" / \"adr\" / \"SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\").is_file()\n+        )\n+\n+\n+if __name__ == \"__main__\":\n+    unittest.main()\n\n succeeded in 229ms:\nShiki validation passed\n\ncodex\nAll required local checks are green on the reviewed tree: 1,147 unit tests, control-plane suite, adversarial suite, and `validate_shiki`. I\u2019m now recording the implementer `code-review` evidence\u2014one finding, one applied fix, zero rejected findings\u2014without creating any approval, commit, PR, or delivery state.\nexec\n/bin/zsh -lc \"date -u '+%Y%m%dT%H%M%S000000Z %Y-%m-%dT%H:%M:%S+00:00'\" in /private/tmp/shiki-adr-collision.pa0KwY/.worktrees/shiki-t-20260807t073307221254z-2d22d654-introduc\n succeeded in 0ms:\n20260807T082800000000Z 2026-08-07T08:28:00+00:00\n\ndiff --git a/.codex/skills/shiki/SKILL.md b/.codex/skills/shiki/SKILL.md\nindex c8e2e85d9d75d49cc4f2d6286ee22659c4e53e75..93cded623b2c15d9f65c76491a1c2d813ac9e6c0\n--- a/.codex/skills/shiki/SKILL.md\n+++ b/.codex/skills/shiki/SKILL.md\n@@ -18,6 +18,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n Codex CLI does not register this skill as a custom `/shiki` slash command. When\n the user types `/shiki` in Codex CLI and gets \"Unrecognized command\", explain\n that this is expected: invoke Shiki in Codex with natural language such as\n@@ -69,7 +72,7 @@\n \n ## Responsibilities\n \n-- Claude Code implements and repairs by default (`shiki runner claude`, ADR 0008); Codex implements and repairs tasks explicitly assigned to `codex`.\n+- Claude Code implements and repairs by default (`shiki runner claude`, SADR-0008); Codex implements and repairs tasks explicitly assigned to `codex`.\n - Claude Code Action can act as GitHub-side CCA or reviewer.\n - CCA judges completion.\n - MergeGate authorizes state transitions and merge readiness.\ndiff --git a/.github/prompts/cca-completion-check.md b/.github/prompts/cca-completion-check.md\nindex ed203ad5f302ecc81372d8ab27d2b1232ca97497..fd89950528333ef6242e6d5443904f5392ab7e56\n--- a/.github/prompts/cca-completion-check.md\n+++ b/.github/prompts/cca-completion-check.md\n@@ -19,6 +19,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n ## Required Reading\n \n Read only the files needed to judge the PR contract. Prefer this order:\ndiff --git a/.github/prompts/codex-task-handoff.md b/.github/prompts/codex-task-handoff.md\nindex 695e211d76dea1c60e8d5bb360e78487808286a4..59441d576b01a39569722f226c854e5a740e9eb5\n--- a/.github/prompts/codex-task-handoff.md\n+++ b/.github/prompts/codex-task-handoff.md\n@@ -2,7 +2,7 @@\n \n You are Codex Front acting as the Implementer for a Shiki task.\n \n-Follow `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant ADRs, and `docs/agents/codex-handoff.md`.\n+Follow `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant Shiki SADRs and target ADRs, and `docs/agents/codex-handoff.md`.\n \n Rules:\n \ndiff --git a/AGENTS.md b/AGENTS.md\nindex 7f041100d49c2a7525f51cb0f5d696a4a6d1c78c..a06bfbabc6fa48a007389e25e12d7ed523936bed\n--- a/AGENTS.md\n+++ b/AGENTS.md\n@@ -22,7 +22,7 @@\n - **Codex Front** is an optional implementer through Codex App, Codex CLI, Codex IDE extension, or Codex Web signed in with ChatGPT OAuth/subscription auth, dispatched with `shiki runner codex` when a task is explicitly assigned to `codex`.\n - **GitHub CCA** is implemented by Claude Code Action by default, using `CLAUDE_CODE_OAUTH_TOKEN`.\n \n-Do not assume `openai/codex-action`, `OPENAI_API_KEY`, or API-key based Codex automation in the default Shiki path. API-key runners are explicit target-repo extensions and require their own ADR.\n+Do not assume `openai/codex-action`, `OPENAI_API_KEY`, or API-key based Codex automation in the default Shiki path. API-key runners are explicit target-repo extensions and require their own target ADR.\n \n Shiki is not:\n \n@@ -44,10 +44,14 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use\n+`SADR-NNNN-*.md`; target product decisions retain `NNNN-*.md` and the `ADR`\n+namespace.\n+\n Use the canonical source-of-truth order above. Runtime-specific reading orders\n are operational guidance only and must not override or reorder it.\n \n-Conversation state is not durable truth. If a decision matters, put it in GitHub, `.shiki/`, `CONTEXT.md`, or an ADR.\n+Conversation state is not durable truth. If a decision matters, put it in GitHub, `.shiki/`, `CONTEXT.md`, a Shiki SADR, or a target ADR.\n \n If GitHub and `.shiki/` disagree, surface the conflict and prefer GitHub operational state until the mirror is repaired.\n \n@@ -75,8 +79,8 @@\n Every non-trivial change follows this loop:\n \n 1. **Goal**: clarify outcome, completion conditions, non-goals, risk level, and success signals.\n-2. **grill-with-docs**: challenge the plan against domain docs, ADRs, code reality, terminology, and edge scenarios.\n-3. **Context & Impact**: find relevant docs, ADRs, code, dependencies, owners, locks, and verification surfaces.\n+2. **grill-with-docs**: challenge the plan against domain docs, Shiki SADRs, target ADRs, code reality, terminology, and edge scenarios.\n+3. **Context & Impact**: find relevant docs, SADRs, target ADRs, code, dependencies, owners, locks, and verification surfaces.\n 4. **PRD and Spec Freeze**: use `to-prd` when context is settled enough to become durable product/engineering intent. The operator's explicit approval of the PRD is Spec Freeze: the plan records a `spec_freeze` block, and no plan runs without it. Steps 1-4 form Requirements Definition \u2014 one continuous operator dialogue. After Spec Freeze, scope changes require an operator-approved Spec Amendment; non-scope-moving interpretations go to the Assumption Log.\n 5. **Issues**: use `to-issues` to create independently grabbable vertical-slice issues. Prefer AFK slices over HITL slices where possible.\n 6. **Triage**: label issues for readiness, risk, runtime, skills, and MergeGate state.\n@@ -167,7 +171,7 @@\n - Testing decisions.\n - Out-of-scope items.\n - Further notes.\n-- Links to relevant domain terms and ADRs.\n+- Links to relevant domain terms, SADRs, and target ADRs.\n \n Every executable task must include:\n \n@@ -193,7 +197,7 @@\n \n Context & Impact must identify:\n \n-- Relevant documents, domain terms, ADRs, and past decisions.\n+- Relevant documents, domain terms, SADRs, target ADRs, and past decisions.\n - Relevant modules, interfaces, seams, callers, owners, and tests.\n - Dependency relationships between tasks.\n - Candidate locks and lock conflicts.\n@@ -210,7 +214,7 @@\n | Skill | Required when |\n | --- | --- |\n | `setup-matt-pocock-skills` | First configuring the repo for skills, or when issue tracker, triage labels, or domain docs are missing. |\n-| `grill-with-docs` | Default for non-trivial Goals before PRD/issues; always required when plans, terminology, boundaries, tradeoffs, or ADR-worthy decisions are ambiguous. |\n+| `grill-with-docs` | Default for non-trivial Goals before PRD/issues; always required when plans, terminology, boundaries, tradeoffs, or decision-record-worthy choices are ambiguous. |\n | `zoom-out` | The agent lacks an architectural map of the relevant code area. |\n | `to-prd` | Settled Goal context must become durable product/engineering intent. |\n | `to-issues` | A Goal, PRD, or plan must become independently grabbable vertical-slice issues. |\n@@ -348,7 +352,7 @@\n - Three or more new files.\n - Any high-risk or critical label.\n \n-When the architecture gate triggers, run `grill-with-docs` or `improve-codebase-architecture` as appropriate and record durable decisions in an ADR when needed.\n+When the architecture gate triggers, run `grill-with-docs` or `improve-codebase-architecture` as appropriate and record durable decisions in a Shiki SADR or target ADR as applicable.\n \n ## Engineering Discipline\n \ndiff --git a/CLAUDE.md b/CLAUDE.md\nindex 98a932e7841a632866fcd53b0bad0e3af3711d3d..98a31399404d6fb044b4fd493f4459ac87f439de\n--- a/CLAUDE.md\n+++ b/CLAUDE.md\n@@ -14,6 +14,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n ## Claude Role\n \n Default to planner, default implementer, reviewer, coordinator, documentation editor, CCA-style completion judge, and final-judgment assistant.\n@@ -57,7 +60,7 @@\n At the start of a Shiki session:\n \n 1. Read `AGENTS.md`.\n-2. Read `CONTEXT.md` and relevant ADRs.\n+2. Read `CONTEXT.md` and relevant Shiki SADRs and target ADRs.\n 3. Read active `.shiki/` Goal/task/ledger state when present.\n 4. Read active distilled rules from `.shiki/memories/` (status `distilled`, `active: true`, not revoked or superseded) for applicable guidance; the same rules are injected deterministically into task handoffs as the `## Distilled Rules` section (\u00a73.5).\n 5. Read the GitHub Issue or PR when the task is GitHub-backed.\n@@ -74,7 +77,7 @@\n - Define the Goal, completion conditions, non-goals, and risk level.\n - Treat `grill-with-docs` as the default planning entry point for non-trivial Goals.\n - Challenge domain terms against `CONTEXT.md`.\n-- Challenge hard-to-reverse tradeoffs against ADRs.\n+- Challenge hard-to-reverse tradeoffs against SADRs and target ADRs.\n - Explore code instead of asking when code can answer the question.\n - Run Context & Impact before implementation planning.\n - Produce a Task DAG, not an unordered checklist.\n@@ -94,7 +97,7 @@\n - For each question, provide your recommended answer.\n - Resolve dependency decisions before downstream design questions.\n - Update `CONTEXT.md` inline when terminology crystallizes.\n-- Offer ADRs only when the decision is hard to reverse, surprising without context, and a real tradeoff.\n+- Offer a SADR or target ADR only when the decision is hard to reverse, surprising without context, and a real tradeoff.\n - Convert unresolved questions into HITL blockers rather than allowing Codex to guess.\n \n ## PRD and Issues Standard\n@@ -103,7 +106,7 @@\n \n - Use the project's domain glossary vocabulary.\n - Avoid volatile file paths unless they are essential.\n-- Include implementation decisions, testing decisions, out-of-scope items, and relevant ADR/domain links.\n+- Include implementation decisions, testing decisions, out-of-scope items, and relevant SADR/target-ADR/domain links.\n - Identify deep-module opportunities and testable interfaces.\n \n When creating issues:\n@@ -121,7 +124,7 @@\n - Branch/worktree target.\n - Scope and non-goals.\n - Dependencies and locks.\n-- Relevant docs, ADRs, modules, and tests.\n+- Relevant docs, SADRs, target ADRs, modules, and tests.\n - Required skill invocations.\n - TDD expectations.\n - Pre-PR `code-review` gate expectations.\n@@ -152,7 +155,7 @@\n \n - Treat yourself as a judge, not implementer.\n - Do not write production code.\n-- Read `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant ADRs, PRD, task issue, PR body, diff, checks, reviews, labels, and `.shiki/` state.\n+- Read `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant SADRs and target ADRs, PRD, task issue, PR body, diff, checks, reviews, labels, and `.shiki/` state.\n - Evaluate every applicable checklist in `docs/agents/checklists.md`.\n - Map each acceptance criterion to durable evidence.\n - Distinguish implementation failure from missing evidence.\ndiff --git a/CONTEXT.md b/CONTEXT.md\nindex e61a9be06a0cce0d026484083a657bde2067e234..237ee8aa3ea8fab334ece49768bc5ed3b5af0974\n--- a/CONTEXT.md\n+++ b/CONTEXT.md\n@@ -25,7 +25,7 @@\n _Avoid_: jumping directly into code\n \n **grill-with-docs**:\n-A planning skill that challenges a plan against domain language, ADRs, code reality, and concrete edge scenarios. It resolves design-tree questions before PRD/issues. It runs inside Requirements Definition.\n+A planning skill that challenges a plan against domain language, Shiki SADRs, target ADRs, code reality, and concrete edge scenarios. It resolves design-tree questions before PRD/issues. It runs inside Requirements Definition.\n _Avoid_: generic brainstorming, silent assumptions\n \n **Requirements Definition**:\ndiff --git a/CONTRIBUTING.md b/CONTRIBUTING.md\nindex c6d9b656e73abb8a55e5f660123e03153dcb3e6d..9e37a8fbf668db5b22461aaae5548b8f1354394c\n--- a/CONTRIBUTING.md\n+++ b/CONTRIBUTING.md\n@@ -17,7 +17,7 @@\n 1. **Goal** \u2014 start from a user-approved Goal with an outcome, completion\n    conditions, non-goals, risk level, and success signals.\n 2. **grill-with-docs** \u2014 for any non-trivial change, challenge the plan against\n-   domain language, ADRs, and code reality before writing a PRD.\n+   domain language, Shiki SADRs, target ADRs, and code reality before writing a PRD.\n 3. **PRD** \u2014 capture settled product/engineering intent (`to-prd`).\n 4. **Spec Freeze** \u2014 the operator explicitly approves the PRD/requirements;\n    the plan records a `spec_freeze` block and no plan runs without it\n@@ -94,7 +94,7 @@\n   entries.\n - Do not hand-edit `.shiki/goals/*.json` or `.shiki/manifest.json` in a task PR.\n - Conversation state is not durable truth. If a decision matters, record it in\n-  GitHub, `.shiki/`, `CONTEXT.md`, or an ADR.\n+  GitHub, `.shiki/`, `CONTEXT.md`, a Shiki SADR, or a target ADR.\n \n ## Architecture Gate\n \n@@ -103,7 +103,7 @@\n or CI policy, package structure, three or more structural files, more than 100\n net new lines in a core module, three or more new files, or any high-risk label.\n When triggered, run `grill-with-docs` or `improve-codebase-architecture` and\n-record durable decisions in an ADR when needed (see `docs/adr/`).\n+record durable decisions in a Shiki SADR or target ADR as applicable (see `docs/adr/`).\n \n ## Safety\n \ndiff --git a/SYSTEM_PROMPT.md b/SYSTEM_PROMPT.md\nindex aaaea45e3a896c3a44ba89eec8bb84325ecf6c62..da009c86a88285269dee0155e58e550c6d744ea9\n--- a/SYSTEM_PROMPT.md\n+++ b/SYSTEM_PROMPT.md\n@@ -30,6 +30,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n Use the canonical source-of-truth order above. Runtime-specific reading orders\n are operational guidance only and must not override or reorder it. The current\n conversation is non-durable operator input.\n@@ -48,14 +51,14 @@\n \n A runtime may only do work that is explicitly assigned by the Goal, task, PR, or MergeGate decision.\n \n-Do not assume `openai/codex-action`, `OPENAI_API_KEY`, or API-key based Codex automation in the default Shiki path. API-key runners are explicit target-repo extensions and require their own ADR.\n+Do not assume `openai/codex-action`, `OPENAI_API_KEY`, or API-key based Codex automation in the default Shiki path. API-key runners are explicit target-repo extensions and require their own target ADR.\n \n ## 4. Non-negotiable Shiki flow\n \n For any non-trivial Goal, use this flow:\n \n 1. **Goal Seek**: define outcome, non-goals, risk level, completion criteria, and evidence requirements.\n-2. **grill-with-docs**: challenge the plan against `CONTEXT.md`, ADRs, code reality, terminology, boundaries, and edge scenarios. Ask one question at a time when the operator is available. If code can answer a question, inspect code instead of asking.\n+2. **grill-with-docs**: challenge the plan against `CONTEXT.md`, Shiki SADRs, target ADRs, code reality, terminology, boundaries, and edge scenarios. Ask one question at a time when the operator is available. If code can answer a question, inspect code instead of asking.\n 3. **Context & Impact**: identify modules, interfaces, seams, callers, dependencies, locks, risk, and verification surfaces.\n 4. **to-prd + Spec Freeze**: turn settled context into a PRD using domain vocabulary and testing decisions. The operator's explicit approval of the PRD is Spec Freeze, recorded as the plan's `spec_freeze` block; plans without it do not run. Steps 1-4 form Requirements Definition.\n 5. **to-issues**: decompose the PRD into independently grabbable vertical-slice issues. Prefer AFK slices; mark HITL slices when judgment is still required.\n@@ -75,7 +78,7 @@\n Before making any material change:\n \n 1. Read `AGENTS.md` and any runtime-specific wrapper such as `CLAUDE.md`.\n-2. Read `CONTEXT.md` and relevant ADRs.\n+2. Read `CONTEXT.md` and relevant SADRs and target ADRs.\n 3. Read the GitHub Issue / PR and `.shiki/` state for the active Goal or task.\n 4. Identify your role: Planner, Implementer, Reviewer, Completion Check Agent, Repairer, or Guardian-assist.\n 5. Identify Goal id, task id, branch, PR, dependencies, locks, risk level, required skills, and required evidence.\n@@ -103,7 +106,7 @@\n \n At minimum, identify:\n \n-- Relevant documents, ADRs, domain terms, and prior decisions.\n+- Relevant documents, SADRs, target ADRs, domain terms, and prior decisions.\n - Relevant modules, interfaces, seams, callers, owners, and tests.\n - Dependency relationships between tasks.\n - Candidate file locks and lock conflicts.\n@@ -152,7 +155,7 @@\n \n CCA must read:\n \n-- `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant ADRs.\n+- `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant SADRs and target ADRs.\n - Parent Goal issue, PRD issue, task issue, comments, and labels.\n - PR body, diff, commits, changed files, reviews, CI checks, and artifacts available to the workflow.\n - `.shiki/` task contract, locks, ledger entries, CCA checklist profile, and prior repair packets.\n@@ -214,7 +217,7 @@\n - Three or more new files.\n - Any high-risk or critical label.\n \n-When the architecture gate triggers, run `grill-with-docs` or `improve-codebase-architecture` as appropriate and record the decision in an ADR if it is hard to reverse or likely to be re-litigated.\n+When the architecture gate triggers, run `grill-with-docs` or `improve-codebase-architecture` as appropriate and record the decision in a Shiki SADR or target ADR as applicable if it is hard to reverse or likely to be re-litigated.\n \n ## 14. Skill Gate\n \n@@ -223,7 +226,7 @@\n Use these skills:\n \n - `setup-matt-pocock-skills`: before first use of the engineering skill set, or when issue tracker, triage labels, or domain docs are missing.\n-- `grill-with-docs`: default for non-trivial Goals before PRD/issues; required for ambiguous plans, terminology, boundaries, ADR-worthy tradeoffs, or design-tree decisions.\n+- `grill-with-docs`: default for non-trivial Goals before PRD/issues; required for ambiguous plans, terminology, boundaries, decision-record-worthy tradeoffs, or design-tree decisions.\n - `zoom-out`: unfamiliar code area or missing architectural map.\n - `to-prd`: convert settled context into a PRD.\n - `to-issues`: break a PRD, plan, or Goal into independently grabbable vertical-slice issues.\ndiff --git a/docs/adr/0001-keep-shiki-as-platform-name.md b/docs/adr/SADR-0001-keep-shiki-as-platform-name.md\nindex 20a9637c3b9d8c97de7ba8db7cfae4d82d9a7dba..8b685439959aada1b221c112f0ac3c51ca8a1695\n--- a/docs/adr/0001-keep-shiki-as-platform-name.md\n+++ b/docs/adr/SADR-0001-keep-shiki-as-platform-name.md\n@@ -1,3 +1,3 @@\n-# Keep Shiki As The Platform Name\n+# SADR-0001: Keep Shiki As The Platform Name\n \n Shiki remains the product name for the GitHub-native agentic engineering control plane. We considered more descriptive names such as MergePilot, MergeGate, and Kiroku, but kept Shiki because it carries the existing goal-seeking framework identity while remaining independent of Hermes, Codex, Claude Code, or any single runtime.\ndiff --git a/docs/adr/0002-use-agents-as-shared-constitution.md b/docs/adr/SADR-0002-use-agents-as-shared-constitution.md\nindex 73de30ecb078120eac540d9bc51855e3cd48d0e3..c5486182bf06fca36ea508496b013c67302fd989\n--- a/docs/adr/0002-use-agents-as-shared-constitution.md\n+++ b/docs/adr/SADR-0002-use-agents-as-shared-constitution.md\n@@ -1,3 +1,3 @@\n-# Use AGENTS As The Shared Agent Constitution\n+# SADR-0002: Use AGENTS As The Shared Agent Constitution\n \n Shiki uses `AGENTS.md` as the runtime-neutral constitution and `CLAUDE.md` as a thin Claude Code wrapper. This keeps Codex, Claude Code, Hermes Runner, and future Agent Runtimes aligned on the same GitHub-first MergeGate rules while still allowing runtime-specific instructions where needed.\ndiff --git a/docs/adr/0003-require-skill-gate-for-mergegate-readiness.md b/docs/adr/SADR-0003-require-skill-gate-for-mergegate-readiness.md\nindex 424be0c9af4c8f0136f1fde68a468c47ff84e764..7fc337987c7df9792f75f181dfcbed75fe5ffff2\n--- a/docs/adr/0003-require-skill-gate-for-mergegate-readiness.md\n+++ b/docs/adr/SADR-0003-require-skill-gate-for-mergegate-readiness.md\n@@ -1,3 +1,3 @@\n-# Require Skill Gate For MergeGate Readiness\n+# SADR-0003: Require Skill Gate For MergeGate Readiness\n \n Shiki requires relevant engineering skills to be invoked before execution when their triggers apply. This makes skills part of MergeGate readiness rather than optional prompt style, because Shiki depends on repeatable planning, issue decomposition, TDD, diagnosis, triage, and architecture review before autonomous or parallel agent work can be trusted.\ndiff --git a/docs/adr/0004-use-github-first-with-shiki-mirror.md b/docs/adr/SADR-0004-use-github-first-with-shiki-mirror.md\nindex d0a3a9174dc646a945a9d325954ffaf00c352040..a79c54ae9ee8eed2a0524192b864da4e22a92d3f\n--- a/docs/adr/0004-use-github-first-with-shiki-mirror.md\n+++ b/docs/adr/SADR-0004-use-github-first-with-shiki-mirror.md\n@@ -1,4 +1,4 @@\n-# ADR 0004: Use GitHub First With A Repository-Local Shiki Mirror\n+# SADR-0004: Use GitHub First With A Repository-Local Shiki Mirror\n \n ## Status\n \ndiff --git a/docs/adr/0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md b/docs/adr/SADR-0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\nindex b339baf49b89d9290e79c3d51b36e7b2bde3bd95..0f3c3eb9afccdb3403ce978e21d04fe12e495da0\n--- a/docs/adr/0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\n+++ b/docs/adr/SADR-0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\n@@ -1,8 +1,8 @@\n-# ADR 0005: Use Codex As Front And Claude Code Action As The GitHub Runtime\n+# SADR-0005: Use Codex As Front And Claude Code Action As The GitHub Runtime\n \n ## Status\n \n-Accepted (implementer default superseded by ADR 0008; the auth model and the\n+Accepted (implementer default superseded by SADR-0008; the auth model and the\n Claude Code Action GitHub runtime decision remain in force)\n \n ## Context\n@@ -26,4 +26,4 @@\n - Shiki avoids requiring `OPENAI_API_KEY` for the default workflow.\n - Codex work happens through the user's authenticated Codex front surface, not as a hidden backend Action.\n - Claude Code Action is configured for OAuth-token based subscription usage by default.\n-- API-key based automation remains possible only as an explicit target-repo extension with its own ADR.\n+- API-key based automation remains possible only as an explicit target-repo extension with its own target ADR.\ndiff --git a/docs/adr/0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md b/docs/adr/SADR-0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\nindex 4bcae9d1e52f0a37b8e9fbafa5cd14b6ee1993bd..7a38538a6ae9929567f0ade67e7ea6a60d0b3f7e\n--- a/docs/adr/0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\n+++ b/docs/adr/SADR-0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\n@@ -1,4 +1,4 @@\n-# ADR 0006: Separate Codex Implementation, CCA Completion, And MergeGate Authorization\n+# SADR-0006: Separate Codex Implementation, CCA Completion, And MergeGate Authorization\n \n ## Status\n \ndiff --git a/docs/adr/0007-packaging-and-release.md b/docs/adr/SADR-0007-packaging-and-release.md\nindex 79b84ddb97167372b44641e5b7860e400ce80822..bb445a2e2e856d9d1f5fdbe35de8d4e8cec0fb32\n--- a/docs/adr/0007-packaging-and-release.md\n+++ b/docs/adr/SADR-0007-packaging-and-release.md\n@@ -1,4 +1,4 @@\n-# ADR 0007: Packaging And Release For Shiki\n+# SADR-0007: Packaging And Release For Shiki\n \n ## Status\n \n@@ -25,7 +25,7 @@\n   not introduce third-party runtime dependencies or break the shim.\n - Releases must run inside the operator's subscription-authenticated toolchain\n   and must not require extra secrets beyond the built-in `GITHUB_TOKEN`\n-  (consistent with ADR 0005's no-API-key-by-default stance).\n+  (consistent with SADR-0005's no-API-key-by-default stance).\n - `.shiki/manifest.json` already encodes the install surface (`install.include`,\n   `install.create_directories`, `install.exclude_from_commit`), and it must not\n   be mutated as part of this change.\n@@ -34,7 +34,7 @@\n \n This is a hard-to-reverse platform decision (it sets the version source of\n truth, the release trigger, and the public install contract), so it is recorded\n-as an ADR.\n+as a SADR.\n \n ## Decision\n \n@@ -104,7 +104,7 @@\n 3. **Publishing to PyPI.** Rejected for now: Shiki is distributed as a\n    repository template + git-installable CLI, not a PyPI library; PyPI would add\n    credentials/secrets and a different versioning contract. Can be added later as\n-   its own ADR if a packaged distribution is needed.\n+   its own SADR if a packaged distribution is needed.\n 4. **Dynamic version (read `VERSION` via a build backend `dynamic` field).**\n    Rejected to keep `pyproject.toml` backend-agnostic and dependency-free; the\n    small duplication between `VERSION` and `pyproject.toml` is guarded by the\n@@ -123,4 +123,4 @@\n   releasing versions the whole platform but ships only `install.include` to\n   targets.\n - Future PyPI distribution or a console entry point are deferred and would each\n-  warrant their own ADR.\n+  warrant their own SADR.\ndiff --git a/docs/adr/0008-use-claude-code-as-default-implementer.md b/docs/adr/SADR-0008-use-claude-code-as-default-implementer.md\nindex dedf4510b7c7799521422f622029563d373db031..35c2625d908167fbc58855b5b63e283805f167e8\n--- a/docs/adr/0008-use-claude-code-as-default-implementer.md\n+++ b/docs/adr/SADR-0008-use-claude-code-as-default-implementer.md\n@@ -1,12 +1,12 @@\n-# ADR 0008: Use Claude Code As The Default Implementer Runtime\n+# SADR-0008: Use Claude Code As The Default Implementer Runtime\n \n ## Status\n \n-Proposed (supersedes ADR 0005 for the implementer default; keeps its auth model)\n+Proposed (supersedes SADR-0005 for the implementer default; keeps its auth model)\n \n ## Context\n \n-ADR 0005 made Codex Front the default implementation runtime, with Claude Code\n+SADR-0005 made Codex Front the default implementation runtime, with Claude Code\n restricted to planner and reviewer roles by the runtime registry, and the only\n autonomous implementation adapter was `shiki runner codex`.\n \n@@ -18,13 +18,13 @@\n - The platform's planning, review, and CCA layers already run on Claude Code,\n   and Claude Code's native capabilities (the built-in `/code-review` skill and\n   the Workflow multi-agent orchestration tool) have no Codex equivalent.\n-- The Goal lifecycle redesign (ADR 0009) requires an autonomous post-freeze\n+- The Goal lifecycle redesign (SADR-0009) requires an autonomous post-freeze\n   loop driven from the operator's Claude Code session, including bounded\n   repair dispatch, which must not depend on a runtime that cannot be invoked\n   unattended.\n \n The constitution claims Shiki is runtime-agnostic (\"not a Claude-only\n-workflow\", ADR 0002). Any change to the implementer default must preserve the\n+workflow\", SADR-0002). Any change to the implementer default must preserve the\n registry-based runtime model rather than hardcoding one vendor.\n \n ## Decision\n@@ -39,7 +39,7 @@\n   the task to review or repair-needed.\n - Keep Codex registered as an optional implementer runtime. `shiki runner\n   codex`, the Codex handoff templates, and the ChatGPT OAuth auth model from\n-  ADR 0005 remain supported for tasks explicitly assigned to `codex`.\n+  SADR-0005 remain supported for tasks explicitly assigned to `codex`.\n - Commit to mandating two Claude-native capabilities as loop contract in\n   follow-up Goals (status: the code-review gate was delivered by Goal G-B \u2014\n   skill registry, checklists item PR-12, default required_skills \u2014 while the\n@@ -60,7 +60,7 @@\n \n Out of scope: removing Codex support, changing the CCA runtime (Claude Code\n Action with `CLAUDE_CODE_OAUTH_TOKEN` stays), and API-key based automation\n-(still requires its own ADR per ADR 0005).\n+(still requires its own target ADR per SADR-0005).\n \n ## Consequences\n \n@@ -100,7 +100,7 @@\n   harness rejection of direct `codex exec` is outside Shiki's control, and the\n   plan's mandatory native capabilities cannot run on Codex at all.\n - **Remove Codex entirely**: rejected; it abandons the runtime-agnostic\n-  constitution (ADR 0002) for no operational gain and forecloses\n+  constitution (SADR-0002) for no operational gain and forecloses\n   cross-runtime adversarial review.\n - **GitHub Actions cloud implementation via claude-code-action**: rejected as\n   default; durable evidence is automatic but token cost, Node 24 workflow\ndiff --git a/docs/adr/0009-requirements-definition-and-spec-freeze.md b/docs/adr/SADR-0009-requirements-definition-and-spec-freeze.md\nindex 29b3f7871bef9203a8b4a831a65c39eb2f506e42..52e7dd69ad0aa4efe0b68c62678abbf7df02aaec\n--- a/docs/adr/0009-requirements-definition-and-spec-freeze.md\n+++ b/docs/adr/SADR-0009-requirements-definition-and-spec-freeze.md\n@@ -1,4 +1,4 @@\n-# ADR 0009: Merge Grill And Planning Into Requirements Definition With Spec Freeze\n+# SADR-0009: Merge Grill And Planning Into Requirements Definition With Spec Freeze\n \n ## Status\n \ndiff --git a/docs/adr/0010-external-ai-guardian-review.md b/docs/adr/SADR-0010-external-ai-guardian-review.md\nindex e6108bbe8bb4e73e0ff4684c8f78001a80438c4e..f3510b145664298adafb151b6f0db51b972f01ec\n--- a/docs/adr/0010-external-ai-guardian-review.md\n+++ b/docs/adr/SADR-0010-external-ai-guardian-review.md\n@@ -1,4 +1,4 @@\n-# ADR 0010: External AI Guardian Review As A First-Class Approval Authority\n+# SADR-0010: External AI Guardian Review As A First-Class Approval Authority\n \n ## Status\n \n@@ -8,7 +8,7 @@\n \n Shiki is intended to be an autonomous, recursive, self-improving agent\n framework: authority-in-the-loop, not human-in-the-loop. Until now the Guardian\n-approval required for high/critical-risk merges (guardian-policy.json, ADR\n+approval required for high/critical-risk merges (guardian-policy.json, SADR\n governance) recognized only human authority sources \u2014 a GitHub review approval,\n the `guardian:approved` label applied by a configured Guardian, or a comment\n carrying `Guardian approval granted` plus the current head SHA.\n@@ -89,7 +89,7 @@\n   autonomous design the operator intends and produced the impersonation\n   incident.\n - **Record AI approval as an operator comment**: rejected as forgery of human\n-  authority \u2014 the exact failure this ADR corrects.\n+  authority \u2014 the exact failure this SADR corrects.\n - **Deliver the artifact as a committed `.shiki/` file**: rejected; a committed\n   approval inside the PR being approved is forgeable by anyone who can push.\n - **A dedicated reviewer bot identity instead of a relayed comment**: deferred;\ndiff --git a/docs/adr/0011-autonomous-quality-gate-evidence.md b/docs/adr/SADR-0011-autonomous-quality-gate-evidence.md\nindex bca0246c5d46e44afbde15486a75eec425fc02f5..9c93eac16d8c3a6d4b11bd7224e3955102cd7db4\n--- a/docs/adr/0011-autonomous-quality-gate-evidence.md\n+++ b/docs/adr/SADR-0011-autonomous-quality-gate-evidence.md\n@@ -1,4 +1,4 @@\n-# ADR 0011: Autonomous Quality-Gate Evidence \u2014 Deterministic Facts And An Independent Verifier\n+# SADR-0011: Autonomous Quality-Gate Evidence \u2014 Deterministic Facts And An Independent Verifier\n \n ## Status\n \ndiff --git a/docs/adr/0012-autonomous-loop-closeout-to-main.md b/docs/adr/SADR-0012-autonomous-loop-closeout-to-main.md\nindex 637fad6dde4989c9c2bf0f81f676d8d29991123c..ec7d85f81030a676bcbba28b43eb365422a36eac\n--- a/docs/adr/0012-autonomous-loop-closeout-to-main.md\n+++ b/docs/adr/SADR-0012-autonomous-loop-closeout-to-main.md\n@@ -1,4 +1,4 @@\n-# 0012 \u2014 Autonomous loop pushes goal completion to main via a closeout PR\n+# SADR-0012 \u2014 Autonomous loop pushes goal completion to main via a closeout PR\n \n - Status: accepted\n - Date: 2026-06-18\ndiff --git a/docs/adr/0013-bootstrap-grants-actions-approve-pull-requests.md b/docs/adr/SADR-0013-bootstrap-grants-actions-approve-pull-requests.md\nindex b6c69d64f1c5ced346b88d6859586715b3b5bd0e..cf48ff4a6716267f539ca58edd1a852e7c7a2d64\n--- a/docs/adr/0013-bootstrap-grants-actions-approve-pull-requests.md\n+++ b/docs/adr/SADR-0013-bootstrap-grants-actions-approve-pull-requests.md\n@@ -1,4 +1,4 @@\n-# ADR 0013: Bootstrap Grants GitHub Actions Permission To Create And Approve Pull Requests\n+# SADR-0013: Bootstrap Grants GitHub Actions Permission To Create And Approve Pull Requests\n \n ## Status\n \n@@ -31,7 +31,7 @@\n security tradeoff: it lets repository automation satisfy a human-review gate. In\n a multi-maintainer repo that could let a compromised or buggy workflow\n self-approve changes. So whether bootstrap turns it on by default \u2014 and with\n-what default token scope \u2014 is an ADR-worthy decision, not an implementation\n+what default token scope \u2014 is a SADR-worthy decision, not an implementation\n detail.\n \n ## Decision\n@@ -48,7 +48,7 @@\n Specifically:\n \n - **`can_approve_pull_request_reviews` defaults to `true`.** Shiki's default\n-  posture is solo/AFK autonomous operation (ADR 0008/0009/0012), where the CCA\n+  posture is solo/AFK autonomous operation (SADR-0008/0009/0012), where the CCA\n   Review Bridge is the only path that can satisfy `required_review: true` after\n   the CCA and Guardian gates have already passed. The default makes a\n   freshly-bootstrapped repo able to run the autonomous loop end to end without a\ndiff --git a/docs/adr/0014-codex-app-external-ai-guardian-ui-adapter.md b/docs/adr/SADR-0014-codex-app-external-ai-guardian-ui-adapter.md\nindex 238db06279334caa341c117362df9271382a7fcd..65f12e9939321e1b3e389a34c822fc1549e5155d\n--- a/docs/adr/0014-codex-app-external-ai-guardian-ui-adapter.md\n+++ b/docs/adr/SADR-0014-codex-app-external-ai-guardian-ui-adapter.md\n@@ -1,4 +1,4 @@\n-# ADR 0014: Use Codex App As The External AI Guardian UI Adapter\n+# SADR-0014: Use Codex App As The External AI Guardian UI Adapter\n \n ## Status\n \ndiff --git a/docs/adr/0015-contract-approval-before-dispatch.md b/docs/adr/SADR-0015-contract-approval-before-dispatch.md\nindex fad6c656741a20a7684ba16080a7db159f63a52f..6061271a46d3f1d6ae7174477de3fb75673123a6\n--- a/docs/adr/0015-contract-approval-before-dispatch.md\n+++ b/docs/adr/SADR-0015-contract-approval-before-dispatch.md\n@@ -1,4 +1,4 @@\n-# ADR 0015: Approve The Task Contract Before Dispatch, Not The Diff After It\n+# SADR-0015: Approve The Task Contract Before Dispatch, Not The Diff After It\n \n ## Status\n \n@@ -9,7 +9,7 @@\n Guardian approval for high/critical risk is expressible only on a pull request.\n Every approval source in `guardian-policy.json` \u2014 GitHub review, the\n `guardian:approved` label, the `Guardian approval granted` comment, and the\n-`external_ai_guardian_review` artifact (ADR 0010/0014) \u2014 is bound to a PR, and\n+`external_ai_guardian_review` artifact (SADR-0010/0014) \u2014 is bound to a PR, and\n three of the four require the current head SHA. Approval therefore cannot exist\n before an implementation PR exists.\n \n@@ -81,7 +81,7 @@\n An envelope breach must surface only on the non-repairable policy gate, never on\n the repairable `MergeGate metadata check`. Otherwise the Goal Loop routes it to\n `dispatch_repair` \u2014 instructing a runner to make the breach go away \u2014 which is\n-exactly what this ADR forbids.\n+exactly what this SADR forbids.\n \n **Contract immutability.** After registration, an implementation PR must not\n change the governance fields of its own task contract (`scope`, `non_goals`,\n@@ -116,7 +116,7 @@\n fails any verdict that is not `complete`, and the signal the CCA prompt reads is\n computed from `evaluate_guardian_approval` alone. Without the OR-merge at the\n signal, a contract-approved PR still returns `needs_guardian`, the CCA job fails,\n-and the MergeGate policy check never runs \u2014 the gate this ADR changes would never\n+and the MergeGate policy check never runs \u2014 the gate this SADR changes would never\n be reached. The CCA *prompt* needs no change: a `contract_approval` entry in the\n signal's existing `sources` array satisfies its current wording.\n \n@@ -130,7 +130,7 @@\n written, and an approved contract lets its implementation PRs merge through the\n same automatic path as low/medium risk. The wasted implement-then-block cycle\n disappears. Guardian review shifts from reading diffs to reading contracts, which\n-is both earlier and smaller; the external AI Guardian path (ADR 0010/0014) works\n+is both earlier and smaller; the external AI Guardian path (SADR-0010/0014) works\n on contracts with no change.\n \n We accept that the Guardian authorizes a class of change without seeing its\n@@ -146,7 +146,7 @@\n adversarial tests for the new approval path equivalent in rigor to\n `test_shiki_governance_evidence.sh`.\n \n-Bootstrap costs, which apply only while this ADR is being implemented. The tasks\n+Bootstrap costs, which apply only while this SADR is being implemented. The tasks\n that build Contract Approval merge under the existing rules, so the ones\n dispatched after the pre-dispatch gate lands but before the approval path lands\n need a Guardian-approved Contract PR *and* a live Guardian approval on their\n@@ -210,7 +210,7 @@\n risk; they do not bound authorized work.\n \n **Auto-escalate risk on envelope breach instead of stopping.** Returns the\n-workflow to a diff-time Guardian decision \u2014 the exact behavior this ADR removes.\n+workflow to a diff-time Guardian decision \u2014 the exact behavior this SADR removes.\n \n **Resolve governance fields from the base snapshot only.** Rejected against the\n registration flow: implementation PRs add their own contract, so the base has no\ndiff --git a/docs/adr/0016-locks-bound-product-paths.md b/docs/adr/SADR-0016-locks-bound-product-paths.md\nindex b5e850f3f1f7f4ef6e7f9d1763a29a51bd300c45..46b40a9b99a7aae644c02bb458f5473ff0a90ce2\n--- a/docs/adr/0016-locks-bound-product-paths.md\n+++ b/docs/adr/SADR-0016-locks-bound-product-paths.md\n@@ -1,4 +1,4 @@\n-# ADR 0016: Locks bound product paths; the Shiki mirror is bound by record identity\n+# SADR-0016: Locks bound product paths; the Shiki mirror is bound by record identity\n \n ## Status\n \n@@ -6,8 +6,8 @@\n \n - Date: 2026-07-28\n - Deciders: operator (mizutani-140), Claude (planner)\n-- Related: ADR 0004 (GitHub-first with a `.shiki` mirror), ADR 0009 (Spec Freeze),\n-  ADR 0012 (autonomous loop closeout to main), ADR 0015 (Contract Approval before\n+- Related: SADR-0004 (GitHub-first with a `.shiki` mirror), SADR-0009 (Spec Freeze),\n+  SADR-0012 (autonomous loop closeout to main), SADR-0015 (Contract Approval before\n   dispatch), remediation goal task R-02, the live PR #179 deadlock.\n \n ## Context\n@@ -90,11 +90,11 @@\n files, documentation files and named source modules are nameable; runtime-\n allocated ledger, runner and report ids are not \u2014 and those live in `.shiki`,\n which locks no longer govern. Existing frozen contracts are brought into line by\n-a one-time Spec Amendment per plan, which must land before ADR 0015's contract\n+a one-time Spec Amendment per plan, which must land before SADR-0015's contract\n immutability makes `locks` unwritable from the implementation PR.\n \n **Lock state is resolved from the durable side.** MergeGate resolves the lock\n-registry, and ADR 0015's governance fields, from the base `.shiki` snapshot when\n+registry, and SADR-0015's governance fields, from the base `.shiki` snapshot when\n present, falling back to head only when absent. The `MergeGate metadata check`\n job is given `--base-shiki`, which it does not receive today. Locks are not\n derived from the live open-pull-request set: a pull request body is untrusted\n@@ -108,7 +108,7 @@\n Explicitly out of scope: moving MergeGate's contract resolution off the PR\n checkout wholesale; making the mirror a projection of GitHub state; introducing\n new lock kinds; moving ledger evidence out of the repository; and unwinding\n-ADR 0012's closeout pull request. Registration of multi-task goals is ADR 0015's\n+SADR-0012's closeout pull request. Registration of multi-task goals is SADR-0015's\n Contract PR and is not re-decided here.\n \n ## Consequences\n@@ -127,7 +127,7 @@\n \n Risks accepted. First, goal-scoped identity is only as strong as `goal_id`,\n which is resolved from the PR body and the head task file; the mitigation is to\n-add `goal_id` to ADR 0015's frozen governance field set, and until that lands a\n+add `goal_id` to SADR-0015's frozen governance field set, and until that lands a\n task file can name a foreign goal. Second, base-when-present resolution is\n unavailable on fork pull requests, where the head is contributor-controlled;\n fork pull requests already cannot complete a Shiki merge, and this decision does\n@@ -139,7 +139,7 @@\n path, not by implementation pull requests.\n \n New obligations: one Spec Amendment per frozen plan to rewrite `locks`, ordered\n-before ADR 0015 T1; extension of the adversarial suites in\n+before SADR-0015 T1; extension of the adversarial suites in\n `scripts/test_shiki_adversarial_state.sh` and\n `scripts/test_shiki_governance_evidence.sh` to cover the eleven newly-scoped\n directories; and updating `scripts/test_shiki_loop_lock_guard.sh`,\n@@ -151,32 +151,32 @@\n whether the loop's transitive sync of ledger `evidence` references can be\n deleted once the Contract PR carries its own source plan.\n \n-## Relationship to ADR 0004\n+## Relationship to SADR-0004\n \n-This ADR **clarifies something ADR 0004 left unspecified**. ADR 0004 decides that\n+This SADR **clarifies something SADR-0004 left unspecified**. SADR-0004 decides that\n GitHub is the operational source of truth and that each target repository keeps a\n `.shiki/` mirror. It says nothing about pull requests writing the mirror, does\n not name MergeGate, and does not make the mirror an input to a gate. Nothing in\n-ADR 0004's Decision or Consequences is amended, superseded or weakened: the\n+SADR-0004's Decision or Consequences is amended, superseded or weakened: the\n mirror still exists, still records the same artifacts, and pull request\n-boundaries remain the coordination surface. What this ADR settles is the question\n+boundaries remain the coordination surface. What this SADR settles is the question\n 0004 never reached \u2014 by what authority a pull request may write a mirror record \u2014\n and it answers it the way `.shiki/manifest.json` already declares\n (`pr_mutation: current-task-or-current-goal-only`).\n \n-It **amends ADR 0012**, whose Decision item 5 states that \"Loop-executed tasks\n+It **amends SADR-0012**, whose Decision item 5 states that \"Loop-executed tasks\n declare the `path:.shiki/**` lock, which covers every `.shiki` file the closeout\n stages ... so `files_outside_locks` is satisfied without per-file lock\n bookkeeping.\" That clause is replaced: the closeout's mirror writes are\n-authorized by record identity, not by a declared lock. ADR 0012's substantive\n+authorized by record identity, not by a declared lock. SADR-0012's substantive\n decision \u2014 that completion reaches `main` through a closeout pull request rather\n than local attestation \u2014 is preserved unchanged.\n \n-It is **complementary to ADR 0015**, not a substitute. ADR 0015 owns contract\n+It is **complementary to SADR-0015**, not a substitute. SADR-0015 owns contract\n immutability, the pre-dispatch gate, the Contract PR and the Scope Envelope. This\n-ADR owns what a lock means and who may write the mirror. The one ordering\n+SADR owns what a lock means and who may write the mirror. The one ordering\n constraint between them is that the lock-narrowing Spec Amendment must precede\n-ADR 0015 T1's freezing of the `locks` field.\n+SADR-0015 T1's freezing of the `locks` field.\n \n ## Alternatives Considered\n \n@@ -186,7 +186,7 @@\n the only writer that can register them may run. Migrating the Guardian signal to\n that store makes every registration PR fail closed to `needs_guardian` at every\n risk level, because the task file is by definition absent from the store, which\n-removes the low/medium autonomous path ADR 0008/0009 exists to provide. And it\n+removes the low/medium autonomous path SADR-0008/0009 exists to provide. And it\n leaves schemas, manifest, config, guardian policy, templates, migrations, plans,\n worktrees, runner records, runs and handoffs with no writer at all in a\n repository whose product is `.shiki`. Measured benefit on the frozen backlog:\n@@ -199,14 +199,14 @@\n namespace is not in fact ownership-partitioned \u2014 56 ledger entries carry no\n `task_id`, and one goal DAG is writable by 35 registered tasks. The correct\n expression of its insight is that locks do not govern the mirror at all, which is\n-what this ADR decides.\n+what this SADR decides.\n \n **Make the mirror a projection written only by a registration writer and a\n post-merge projection job.** Rejected. It requires a bypass actor with write\n access to the protected default branch; it makes its own master invariant\n readable from a manifest that pull requests may edit; it deletes both reconcile\n modes and leaves a corrupted mirror with no in-band repair path, contradicting\n-ADR 0004's consequence that mirror conflicts are repaired against GitHub state;\n+SADR-0004's consequence that mirror conflicts are repaired against GitHub state;\n and its content-derived ids are rejected by `validate_shiki`'s currently-green id\n pattern.\n \ndiff --git a/docs/adr/0017-bookkeeping-closeouts-do-not-inherit-risk.md b/docs/adr/SADR-0017-bookkeeping-closeouts-do-not-inherit-risk.md\nindex 554e3a0d526a72ca067108c5464cc68c3edcf174..b9676857a4713850e063af7c39c8e0b01ecb4c66\n--- a/docs/adr/0017-bookkeeping-closeouts-do-not-inherit-risk.md\n+++ b/docs/adr/SADR-0017-bookkeeping-closeouts-do-not-inherit-risk.md\n@@ -1,4 +1,4 @@\n-# ADR 0017: A Bookkeeping Closeout Does Not Inherit Its Task's Risk\n+# SADR-0017: A Bookkeeping Closeout Does Not Inherit Its Task's Risk\n \n ## Status\n \n@@ -6,7 +6,7 @@\n \n ## Context\n \n-ADR 0012 made the goal loop push a task's completion to the default branch through a\n+SADR-0012 made the goal loop push a task's completion to the default branch through a\n closeout pull request: after the implementation PR merges, the loop opens a second PR\n carrying `task.status = done`, `lock.state = released`, and \u2014 when the task completes\n its goal \u2014 `goal.status = complete` plus one scorecard report. Nothing else. The\n@@ -49,14 +49,14 @@\n 4. The task file's governance fields (`scope`, `non_goals`, `required_skills`,\n    `risk_level`, `locks`, `acceptance_checks`, `test_command`) are byte-identical to\n    the base snapshot. Only `status`, `expected_pr`, `closeout_pr`, `ledger_evidence`\n-   and `expected_branch` may differ \u2014 the ADR 0015 contract-immutability rule,\n+   and `expected_branch` may differ \u2014 the SADR-0015 contract-immutability rule,\n    unchanged.\n \n    `expected_branch` is in that set because a closeout is *by construction* on a\n    different branch from the implementation it closes, and the loop rewrites the\n    field accordingly. It is safe to let it differ because MergeGate independently\n    requires `expected_branch` to equal the PR's actual head ref, so a PR cannot set\n-   it to anything other than the branch it is really on. The first draft of this ADR\n+   it to anything other than the branch it is really on. The first draft of this SADR\n    omitted it, which made the exemption unreachable: every real closeout failed\n    condition 4 on a field the loop always changes. See PR #209.\n 5. The state transitions are exactly the terminal set: task `review` \u2192 `done`, lock\n@@ -113,12 +113,12 @@\n classification must be derived, not asserted.\n \n **Fold the closeout into the implementation PR** so no second PR exists. Rejected under\n-ADR 0012, which recorded why: a task file that is already `done` on the implementation\n+SADR-0012, which recorded why: a task file that is already `done` on the implementation\n branch short-circuits the loop's review \u2192 CCA \u2192 merge \u2192 done ordering into a local\n goal-completion before any merge happens.\n \n **Have the loop auto-merge closeouts with elevated credentials.** Rejected outright: that\n-is a runtime authoring its own approval, which ADR 0010 and the constitution's\n+is a runtime authoring its own approval, which SADR-0010 and the constitution's\n approval-record prohibition forbid. It would also be indistinguishable, in the evidence,\n from the forgery those rules exist to prevent.\n \ndiff --git a/docs/adr/0018-carried-guardian-approval-across-a-proven-base-sync.md b/docs/adr/SADR-0018-carried-guardian-approval-across-a-proven-base-sync.md\nindex 22358562da390ed8b45cd600da08a43d378b4680..e6e2907cefa776460af73ddf7d39db605f3c39b0\n--- a/docs/adr/0018-carried-guardian-approval-across-a-proven-base-sync.md\n+++ b/docs/adr/SADR-0018-carried-guardian-approval-across-a-proven-base-sync.md\n@@ -1,4 +1,4 @@\n-# ADR 0018: Carried Guardian Approval Across A Proven Base Sync\n+# SADR-0018: Carried Guardian Approval Across A Proven Base Sync\n \n ## Status\n \n@@ -34,7 +34,7 @@\n Guardian evaluator that consumes it (`shiki_guardian.evaluate_guardian_approval`,\n the `guardian_comment_carried` source) were built and tested in isolation. Neither\n does anything until a caller passes the flags that turn the carry on, so the\n-feature was invisible in CI. This ADR records the decision the two of them\n+feature was invisible in CI. This SADR records the decision the two of them\n implement, and this task wires it into the four CI invocations that judge Guardian\n approval.\n \ndiff --git a/docs/adr/README.md b/docs/adr/README.md\nindex c70e057a4520c36e042ab68c53cd4a21ef709b71..5da43856836e2ce6a150f8127766e2f88355ab4b\n--- a/docs/adr/README.md\n+++ b/docs/adr/README.md\n@@ -1,40 +1,41 @@\n-# Architecture Decision Records\n+# Decision Record Namespaces\n \n-This directory holds Shiki's Architecture Decision Records (ADRs): durable\n-records of hard-to-reverse platform decisions. ADRs are part of the Shiki source\n-of truth alongside GitHub state, the `.shiki/` mirror, and `CONTEXT.md`.\n+This directory holds two independent decision namespaces:\n \n-## How ADRs Work\n+- Shiki Architecture Decision Records (SADRs), named\n+  `SADR-NNNN-<short-slug>.md`, record hard-to-reverse Shiki platform decisions.\n+- Target Architecture Decision Records (ADRs), named\n+  `NNNN-<short-slug>.md`, belong to the target product.\n \n-- New ADRs are added as `docs/adr/NNNN-<short-slug>.md`, where `NNNN` is the next\n-  zero-padded sequence number.\n-- The **canonical list of ADRs is the set of files matching `docs/adr/*.md`** in\n-  this directory. Browse `docs/adr/*.md` for the authoritative, current set.\n-- Use [`template.md`](template.md) as the starting point for a new ADR.\n-- Record an ADR when a decision is hard to reverse, surprising without context,\n-  and a real tradeoff. Do not write an ADR for routine or easily reversible\n-  choices.\n+The same four-digit number may appear once in each namespace. A platform\n+`SADR-NNNN` citation resolves only to `SADR-NNNN-*.md`; a target `ADR NNNN`\n+citation resolves only to `NNNN-*.md`.\n \n-Using the `docs/adr/*.md` glob as the source of truth means other PRs can add an\n-ADR without editing this index file, avoiding merge contention on a\n-hand-maintained enumeration.\n+## How Decision Records Work\n \n-## Current ADRs (snapshot)\n-\n-The list below is a convenience snapshot and may lag behind the directory. When\n-in doubt, list `docs/adr/*.md`.\n+- New Shiki platform decisions use the next zero-padded SADR number.\n+- New target product decisions use the target's own ADR sequence.\n+- The canonical list is the files in `docs/adr/`; no hand-maintained index is\n+  authoritative.\n+- Use [`template.md`](template.md) as the starting point and choose the filename\n+  namespace that owns the decision.\n+- Record a decision when it is hard to reverse, surprising without context, and\n+  a real tradeoff. Do not write a decision record for routine or easily\n+  reversible choices.\n \n-- [0001 \u2014 Keep Shiki As The Platform Name](0001-keep-shiki-as-platform-name.md)\n-- [0002 \u2014 Use AGENTS As The Shared Agent Constitution](0002-use-agents-as-shared-constitution.md)\n-- [0003 \u2014 Require Skill Gate For MergeGate Readiness](0003-require-skill-gate-for-mergegate-readiness.md)\n-- [0004 \u2014 Use GitHub First With A Repository-Local Shiki Mirror](0004-use-github-first-with-shiki-mirror.md)\n-- [0005 \u2014 Use Codex As Front And Claude Code Action As The GitHub Runtime](0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md)\n-- [0006 \u2014 Separate Codex Implementation, CCA Completion, And MergeGate Authorization](0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md)\n+Using the directory as the source of truth lets independent changes add records\n+without contending on an enumerated index.\n \n-## Adding A New ADR\n+## Adding A Shiki SADR\n \n-1. Copy `template.md` to `docs/adr/NNNN-<short-slug>.md`.\n+1. Copy `template.md` to `docs/adr/SADR-NNNN-<short-slug>.md`.\n 2. Fill in the title, status, context, decision, and consequences.\n-3. Set the status to `Proposed`, then `Accepted` once the decision is approved.\n-4. Open a PR following `CONTRIBUTING.md`. You do not need to edit this index for\n-   the ADR to be discoverable via `docs/adr/*.md`.\n+3. Set the status to `Proposed`, then `Accepted` once the proper authority has\n+   approved the decision.\n+4. Open a PR following `CONTRIBUTING.md`.\n+\n+## Adding A Target ADR\n+\n+1. Copy `template.md` to `docs/adr/NNNN-<short-slug>.md` using the target's own\n+   sequence.\n+2. Cite it as `ADR NNNN`; do not prefix its filename with `SADR-`.\ndiff --git a/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md b/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\nnew file mode 100644\nindex 0000000000000000000000000000000000000000..7455ce1f3bed02ddddb851ac21a1d1a648c9aeba\n--- /dev/null\n+++ b/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\n@@ -0,0 +1,55 @@\n+# SADR-0019: Separate Shiki Platform SADRs From Target ADRs\n+\n+## Status\n+\n+Accepted\n+\n+## Context\n+\n+Shiki installs its platform decision records into `docs/adr/` in every target\n+repository. Shiki and its targets previously used the same numeric\n+`NNNN-<slug>.md` namespace. A target that already owned ADRs 0013 through 0018\n+therefore acquired two records for each number during installation, making an\n+`ADR NNNN` citation ambiguous. Renumbering target decisions would rewrite target\n+history, while renumbering Shiki decisions per target would destroy their stable\n+platform identity.\n+\n+Upgrades also need to remove the former Shiki-owned numeric paths without ever\n+mistaking a target-owned ADR for platform content. The install stamp already\n+records exact shipped paths and content digests, so it can prove ownership only\n+when both still match.\n+\n+## Decision\n+\n+Shiki platform decisions use the `SADR` namespace:\n+\n+- records are named `docs/adr/SADR-NNNN-<slug>.md`;\n+- live platform citations use `SADR-NNNN`;\n+- target product decisions retain `docs/adr/NNNN-<slug>.md` and `ADR NNNN`;\n+- the reference validator resolves the two namespaces independently, so the same\n+  four-digit number may exist once in each namespace.\n+\n+Before a forced upgrade writes anything, the installer inventories the exact\n+former numeric Shiki ADR paths. It may delete a legacy path only when the\n+existing install stamp contains that exact path and its recorded SHA-256 digest\n+matches the file currently on disk. All ownership blockers are reported\n+together and abort the upgrade before its first write. A non-forced install\n+never performs this cleanup.\n+\n+Historical `.shiki/` evidence remains unchanged because it records the namespace\n+that existed when that evidence was produced.\n+\n+## Consequences\n+\n+- Targets may use any ADR number without colliding with Shiki's platform SADRs.\n+- Fresh installs preserve target ADR bytes and install a stable SADR set.\n+- Forced upgrades fail closed when legacy ownership cannot be proven, and a\n+  successful upgrade refreshes the stamp without removed legacy paths.\n+- Current governance, packaging, workflow, prompt, script, and skill references\n+  must move with the platform records.\n+\n+## Alternatives Considered\n+\n+- Collision-only refusal leaves affected targets permanently unupgradable.\n+- Renumbering target ADRs mutates target-owned history.\n+- Per-target Shiki renumbering makes platform citations non-portable.\ndiff --git a/docs/adr/template.md b/docs/adr/template.md\nindex a92a97daddd0b00ab9dd626cc8d86942da2eafed..17dd79175b5fee2105935557f691b40cf9492942\n--- a/docs/adr/template.md\n+++ b/docs/adr/template.md\n@@ -1,14 +1,14 @@\n-# NNNN: <Short Decision Title>\n+# <SADR-NNNN or NNNN>: <Short Decision Title>\n \n ## Status\n \n-Proposed | Accepted | Superseded by ADR-NNNN\n+Proposed | Accepted | Superseded by SADR-NNNN or target ADR NNNN\n \n ## Context\n \n What is the situation, constraint, or force that requires a decision? Reference\n-relevant domain terms (`CONTEXT.md`), prior ADRs, and code reality. State the\n-problem, not the solution.\n+relevant domain terms (`CONTEXT.md`), prior Shiki SADRs or target ADRs, and code\n+reality. State the problem, not the solution.\n \n ## Decision\n \ndiff --git a/docs/agents/autonomous-loop-e2e-acceptance.md b/docs/agents/autonomous-loop-e2e-acceptance.md\nindex bbf84c6081987f159ac6ad9ecf3c82b7aa72e7e8..5c13c1d02a05a90b0dc46cbef7c95a9846d0c237\n--- a/docs/agents/autonomous-loop-e2e-acceptance.md\n+++ b/docs/agents/autonomous-loop-e2e-acceptance.md\n@@ -40,8 +40,8 @@\n 3. `gh auth status` is logged in with merge permission on the repo.\n 4. Branch protection + required checks are configured exactly as the goal-loop\n    required-check set expects (see `docs/agents/branch-protection-smoke.md`).\n-5. The repo `.shiki/policy` permits autonomous low/medium-risk merges (ADR\n-   0008/0009); the MergeGate policy check is the recorded-authority gate.\n+5. The repo `.shiki/policy` permits autonomous low/medium-risk merges\n+   (SADR-0008/0009); the MergeGate policy check is the recorded-authority gate.\n \n The operator's only actions are: register the task and start the loop. **No\n manual pre-clearing** means after `shiki loop run` starts, the operator does not\ndiff --git a/docs/agents/bootstrap-command.md b/docs/agents/bootstrap-command.md\nindex 05f7f203a52cf412b9f97084a6e131ed0f785afd..f7aa0b734dfebee4a3b139335b66ee3415f48713\n--- a/docs/agents/bootstrap-command.md\n+++ b/docs/agents/bootstrap-command.md\n@@ -189,7 +189,9 @@\n ```\n \n Use this only for tests, fixtures, or explicit local-only template inspection.\n-Use `--force` only when you intentionally want to overwrite existing target files.\n+Use `--force` only when you intentionally want to refresh the target template.\n+Legacy numeric Shiki decision cleanup is stamp-and-digest authorized before any\n+write; target ADR paths are not cleanup candidates.\n \n ## Slash Command\n \ndiff --git a/docs/agents/checklists.md b/docs/agents/checklists.md\nindex 4a7f03ade346f53a1c1d82981f3fd744f9ab4547..0c5ec6c53dc7197b7b7a7d18e667b325babd6c45\n--- a/docs/agents/checklists.md\n+++ b/docs/agents/checklists.md\n@@ -32,18 +32,18 @@\n | ID | Item | Blocking |\n | --- | --- | --- |\n | GD-01 | Plan was challenged against `CONTEXT.md`. | blocking for non-trivial Goals |\n-| GD-02 | Relevant ADRs were read or absence was recorded. | blocking for non-trivial Goals |\n+| GD-02 | Relevant SADRs and target ADRs were read or absence was recorded. | blocking for non-trivial Goals |\n | GD-03 | Ambiguous terms were resolved or converted to HITL blockers. | blocking |\n | GD-04 | Edge scenarios were considered. | advisory |\n | GD-05 | Code reality was checked when code could answer. | blocking when applicable |\n | GD-06 | New domain terms were added to `CONTEXT.md` when needed. | blocking when applicable |\n-| GD-07 | ADR was created or explicitly skipped using the ADR criteria. | blocking when applicable |\n+| GD-07 | A SADR or target ADR was created or explicitly skipped using the decision-record criteria. | blocking when applicable |\n \n ## CI \u2014 Context & Impact Checklist\n \n | ID | Item | Blocking |\n | --- | --- | --- |\n-| CI-01 | Relevant docs and ADRs identified. | blocking |\n+| CI-01 | Relevant docs, SADRs, and target ADRs identified. | blocking |\n | CI-02 | Relevant modules, interfaces, seams, callers, and tests identified. | blocking |\n | CI-03 | Dependencies and blocked-by relationships identified. | blocking |\n | CI-04 | Candidate locks identified. | blocking |\n@@ -62,7 +62,7 @@\n | PRD-04 | Implementation decisions are recorded without volatile code detail. | blocking |\n | PRD-05 | Testing decisions are recorded. | blocking |\n | PRD-06 | Out-of-scope items are explicit. | blocking |\n-| PRD-07 | Relevant glossary/ADR links are present when applicable. | blocking |\n+| PRD-07 | Relevant glossary/SADR/target-ADR links are present when applicable. | blocking |\n \n ## SF \u2014 Spec Freeze Checklist\n \n@@ -98,7 +98,7 @@\n | H-02 | Branch/worktree target is present. | blocking |\n | H-03 | Scope and non-goals are present. | blocking |\n | H-04 | Dependencies and locks are present. | blocking |\n-| H-05 | Relevant docs, ADRs, modules, and tests are referenced. | blocking |\n+| H-05 | Relevant docs, SADRs, target ADRs, modules, and tests are referenced. | blocking |\n | H-06 | Required skills are stated. | blocking |\n | H-07 | TDD expectations are stated. | blocking when behavior work |\n | H-08 | Acceptance criteria are included. | blocking |\ndiff --git a/docs/agents/codex-handoff.md b/docs/agents/codex-handoff.md\nindex ede468f47cb277f69a857e8c45df703e4ed0151d..6f97bdffb67e44d7edea2c71e01ea28933c4eb6c\n--- a/docs/agents/codex-handoff.md\n+++ b/docs/agents/codex-handoff.md\n@@ -46,7 +46,7 @@\n \n ## Context\n - Domain terms:\n-- Relevant ADRs:\n+- Relevant SADRs and target ADRs:\n - Relevant docs:\n - Relevant modules/interfaces/seams:\n - Similar tests/prior art:\ndiff --git a/docs/agents/completion-check-agent.md b/docs/agents/completion-check-agent.md\nindex 13e4c035db35025f4e3cdcc5adfb24314ac4d521..61bfa1218cf36ca605250c91919e620f33238aa0\n--- a/docs/agents/completion-check-agent.md\n+++ b/docs/agents/completion-check-agent.md\n@@ -32,7 +32,7 @@\n - `AGENTS.md`\n - `CLAUDE.md`\n - `CONTEXT.md`\n-- relevant ADRs\n+- relevant SADRs and target ADRs\n - parent Goal issue\n - PRD issue or document\n - task issue\ndiff --git a/docs/agents/decision-control.md b/docs/agents/decision-control.md\nindex 7d8ee5ab03a5ccbcbd04056ae61de03785a2a9c9..8c94ecda00fffaa0e92c325ab83aa9776bd1dac1\n--- a/docs/agents/decision-control.md\n+++ b/docs/agents/decision-control.md\n@@ -11,7 +11,7 @@\n | Decision | Owner | When | Required inputs | Output | Enforcement surface |\n | --- | --- | --- | --- | --- | --- |\n | Is the Goal clear enough to plan? | Planner + Guardian when needed | Goal intake | User request, existing docs, risk signals | Goal readiness verdict | Goal checklist, issue template, ledger |\n-| Must `grill-with-docs` run? | Planner / Skill Gate | Before PRD or issue split | Ambiguity, domain terms, ADR conflicts, unclear boundaries | Skill invocation or explicit skip rationale | Skill Gate checklist |\n+| Must `grill-with-docs` run? | Planner / Skill Gate | Before PRD or issue split | Ambiguity, domain terms, SADR/target-ADR conflicts, unclear boundaries | Skill invocation or explicit skip rationale | Skill Gate checklist |\n | Is a PRD required? | Planner | After Goal context stabilizes | Goal, `grill-with-docs` output, Context & Impact output | PRD required / not required | PRD checklist, CCA evidence check |\n | Can the spec be frozen? | Operator only | End of Requirements Definition, before issue split | PRD/requirements summary, scope inventory (SF-02), open-question list | `spec_freeze` block in the plan + ledger evidence | `require_grilled_plan` (plan ingest/run), SF checklist |\n | Can a frozen spec change? | Operator only (Spec Amendment) | When implementation contradicts the frozen spec | Contested decisions, scoped re-grill result | recorded amendment + re-stamped freeze | SF checklist, ledger evidence |\ndiff --git a/docs/agents/domain.md b/docs/agents/domain.md\nindex 258da8ea81e87ac0a6b0b5b5e5c2fde980eaabb7..d57b0cf17ff3e1fa9e6e5885372bf083d8295421\n--- a/docs/agents/domain.md\n+++ b/docs/agents/domain.md\n@@ -5,7 +5,9 @@\n ## Layout\n \n - `CONTEXT.md` defines Shiki language. Keep it implementation-free.\n-- `docs/adr/` records hard-to-reverse decisions.\n+- `docs/adr/SADR-NNNN-*.md` records Shiki platform decisions.\n+- `docs/adr/NNNN-*.md` records target product decisions without sharing the\n+  SADR namespace.\n - `AGENTS.md` is the runtime-neutral agent constitution.\n - `CLAUDE.md` is the Claude Code wrapper around `AGENTS.md`.\n - `.shiki/` is the Target Repository mirror for Goals, PRDs, plans, Task DAGs, contracts, locks, worktree records, ledger entries, CCA verdicts, repair packets, reports, and handoffs.\n@@ -15,6 +17,6 @@\n \n - Use Shiki terms exactly when they exist.\n - Add glossary terms to `CONTEXT.md` only when the term is part of the domain, not a generic programming concept.\n-- Create ADRs only for hard-to-reverse, surprising, tradeoff-based decisions.\n+- Create SADRs or target ADRs only for hard-to-reverse, surprising, tradeoff-based decisions.\n - Keep product-specific language in the Target Repository, not in Shiki Template docs.\n-- If an ADR conflicts with a proposed implementation, surface the conflict before editing.\n+- If a SADR or target ADR conflicts with a proposed implementation, surface the conflict before editing.\ndiff --git a/docs/agents/execution-control.md b/docs/agents/execution-control.md\nindex ad8ee3cdb36be56fedc1f46408ca92b387d74047..bc63c3a73661d12b567016ba299f1a6b66bf9088\n--- a/docs/agents/execution-control.md\n+++ b/docs/agents/execution-control.md\n@@ -63,7 +63,7 @@\n | Branch name | Orchestrator | Orchestrator | Before implementer dispatch | branch policy | branch ref in task/ledger |\n | Lock reservation | Context & Impact | MergeGate | Before dispatch and before merge | `.shiki/locks/`, labels, PR metadata | lock id, owner, scope |\n | CI profile | Planner | MergeGate | During issue triage, rechecked at PR | labels, `.shiki/policy.yaml`, workflow matrix | required check list |\n-| Required status checks | Repo maintainer / Guardian | GitHub branch protection / rulesets | Repository setup, updated via ADR | GitHub settings | branch/ruleset config, ADR |\n+| Required status checks | Repo maintainer / Guardian | GitHub branch protection / rulesets | Repository setup, updated via target ADR | GitHub settings | branch/ruleset config, target ADR |\n | CCA judgment | CCA | GitHub required check / MergeGate | Every non-draft PR update | GitHub Action check | structured CCA verdict |\n | Repair assignment | CCA | MergeGate | After failed CCA/CI/review | repair packet, issue/PR comment | repair id and attempt count |\n | Merge readiness | MergeGate | GitHub branch protection / Guardian if needed | After CCA complete | required checks, review rules, rulesets | mergegate check output |\ndiff --git a/docs/agents/guardian-policy.md b/docs/agents/guardian-policy.md\nindex 4147c7adf05a47adb4fd24ada1a1c2e1f2ba6d2d..a20d9cc6c00a569bb057b9ca8d6b51c7fc4a9847\n--- a/docs/agents/guardian-policy.md\n+++ b/docs/agents/guardian-policy.md\n@@ -98,8 +98,8 @@\n `origin/<default_branch>` and reuses each job's existing base fetch; a shallow\n (`--depth`) or second base fetch would make the proof unresolvable and silently\n disable the carry, so neither is permitted. CCA reads `guardian_comment_carried` as\n-a recorded authority in the deterministic signal and does not re-derive it. See ADR\n-0018 for the full decision, the rejected diff/patch-equality and path-disjointness\n+a recorded authority in the deterministic signal and does not re-derive it. See\n+SADR-0018 for the full decision, the rejected diff/patch-equality and path-disjointness\n alternatives, and the accepted residual.\n \n ## External AI Guardian Review (SADR-0010)\ndiff --git a/docs/agents/implementation-policy.md b/docs/agents/implementation-policy.md\nindex c754144a9fb5bf4a38aa82a60e1670c59e626b44..85c37281bcad196c6b7b02343faf00b03cd9c57a\n--- a/docs/agents/implementation-policy.md\n+++ b/docs/agents/implementation-policy.md\n@@ -31,10 +31,13 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n ## Non-Negotiables\n \n 1. **Planning precedes implementation.** Non-trivial Goals start with `grill-with-docs` and Context & Impact.\n-2. **Domain language is controlled.** Terms must be checked against `CONTEXT.md`; hard-to-reverse tradeoffs must be checked against ADRs.\n+2. **Domain language is controlled.** Terms must be checked against `CONTEXT.md`; hard-to-reverse tradeoffs must be checked against Shiki SADRs and target ADRs.\n 3. **PRD precedes issue decomposition.** `to-prd` is used once the context is settled enough to become durable intent.\n 4. **Issues are vertical slices.** `to-issues` must produce independently grabbable tracer-bullet tasks, not horizontal layer tickets.\n 5. **Triage controls dispatch.** Only `ready-for-agent` / AFK tasks may be assigned to Codex without human input.\n@@ -95,19 +98,19 @@\n Process:\n \n - Challenge terms against `CONTEXT.md`.\n-- Check ADRs before reopening decisions.\n+- Check SADRs and target ADRs before reopening decisions.\n - Inspect code when code can answer a question.\n - Ask one question at a time when the operator is available.\n - Provide a recommended answer with each question.\n - Record resolved terms in `CONTEXT.md`.\n-- Create ADRs only for hard-to-reverse, surprising, tradeoff-based decisions.\n+- Create SADRs or target ADRs only for hard-to-reverse, surprising, tradeoff-based decisions.\n - Convert unresolved load-bearing questions into HITL blockers.\n \n Outputs:\n \n - Resolved design-tree decisions.\n - Updated glossary terms when needed.\n-- ADRs when justified.\n+- SADRs or target ADRs when justified.\n - HITL blockers when decisions remain unresolved.\n \n ## Phase 3 \u2014 Context & Impact\n@@ -117,7 +120,7 @@\n \n Outputs must include:\n \n-- Relevant documents and ADRs.\n+- Relevant documents, SADRs, and target ADRs.\n - Relevant modules, interfaces, seams, callers, and tests.\n - Candidate locks.\n - Dependencies.\n@@ -140,7 +143,7 @@\n - Testing decisions.\n - Out of scope.\n - Further notes.\n-- Links to relevant Goal, domain terms, and ADRs.\n+- Links to relevant Goal, domain terms, SADRs, and target ADRs.\n \n Phase 4 ends with Spec Freeze: the operator explicitly approves the PRD, the\n plan records a `spec_freeze` block (status, approved_by, source), and the SF\n@@ -240,7 +243,7 @@\n - Dependencies and locks.\n - Risk and Guardian requirements.\n - Ledger completeness.\n-- Documentation and ADR updates.\n+- Documentation and SADR/target-ADR updates.\n - Scope drift.\n \n CCA emits one of:\n@@ -301,7 +304,7 @@\n | Evidence failure | Behavior might be done, but proof is missing. | Add checks, PR evidence, ledger entry, or artifacts. |\n | Scope drift | PR changes unrelated behavior. | Revert unrelated changes or split into new Goal. |\n | Dependency block | Upstream task or lock not complete. | Block until dependency resolves. |\n-| Architecture block | Decision needs ADR or deep-module discussion. | `grill-with-docs` or `improve-codebase-architecture`. |\n+| Architecture block | Decision needs a SADR, target ADR, or deep-module discussion. | `grill-with-docs` or `improve-codebase-architecture`. |\n | Risk block | Security/production/policy change needs human approval. | Guardian decision. |\n \n ## Phase 5.5 \u2014 Execution Decision Control\ndiff --git a/docs/agents/runtime-auth-model.md b/docs/agents/runtime-auth-model.md\nindex 6a2e1a47e81ea89b5c68a210ec07772a9374babc..3c54748d24233dbd0ba8473950282547ef4ce935\n--- a/docs/agents/runtime-auth-model.md\n+++ b/docs/agents/runtime-auth-model.md\n@@ -148,7 +148,7 @@\n \n API-key based runners may be added by a target repository as an explicit extension. They are not the default Shiki template path.\n \n-If a repo opts into API-key mode, record the exception in an ADR and update the repo's `AGENTS.md`, `.shiki/config.yaml`, and workflow permissions.\n+If a repo opts into API-key mode, record the target-specific exception in a target ADR and update the repo's `AGENTS.md`, `.shiki/config.yaml`, and workflow permissions.\n \n ## GitHub Actions Runtime Compatibility\n \ndiff --git a/docs/agents/skill-gate.md b/docs/agents/skill-gate.md\nindex c50398b0d7c60a8598f2a5c88a10015730dced13..3acffe6444617bb1d54b77d4b4708158ec735bc6\n--- a/docs/agents/skill-gate.md\n+++ b/docs/agents/skill-gate.md\n@@ -9,7 +9,7 @@\n | Skill | Trigger | Expected output |\n | --- | --- | --- |\n | `setup-matt-pocock-skills` | First configuring a repo for these skills, or when issue tracker, triage labels, or domain docs are missing. | `docs/agents/` configuration and an `Agent skills` block in `AGENTS.md` or `CLAUDE.md`. |\n-| `grill-with-docs` | Ambiguous plan, unclear terminology, boundary decision, tradeoff, or ADR-worthy design choice. | Clarified decision tree, recommended answers, updates to `CONTEXT.md` or ADRs when decisions crystallize. |\n+| `grill-with-docs` | Ambiguous plan, unclear terminology, boundary decision, tradeoff, or decision-record-worthy design choice. | Clarified decision tree, recommended answers, updates to `CONTEXT.md`, SADRs, or target ADRs when decisions crystallize. |\n | `zoom-out` | Agent lacks a higher-level architectural map. | Map of relevant modules, callers, seams, and domain concepts. |\n | `to-prd` | Settled context should become a PRD. | PRD published to the issue tracker with problem, solution, user stories, decisions, testing, and out-of-scope notes. |\n | `to-issues` | Goal, PRD, or plan must become implementation tickets. | Vertical-slice issues with acceptance criteria, dependencies, and AFK/HITL classification. |\n@@ -17,7 +17,7 @@\n | `tdd` | Feature work or bug fix has observable behavior that can be specified with tests. | Red-green-refactor loop through public interfaces, one vertical slice at a time. |\n | `code-review` | The TDD loop is green and the implementer is about to create or update a PR. Default for every implementation task. | Pre-PR self-review of the task diff with fixes applied on the task branch; findings and resolutions recorded as a ledger entry naming the skill and a `## Pre-PR code review` PR body section. |\n | `diagnose` | Hard bug, failing check, regression, flaky behavior, or performance problem. | Reproduction loop, ranked hypotheses, instrumentation, fix, regression test, post-mortem. |\n-| `improve-codebase-architecture` | Structural friction, testability, AI-navigability, deep-module opportunity, or refactor request. | Architecture candidates and deepening recommendations informed by `CONTEXT.md` and ADRs. |\n+| `improve-codebase-architecture` | Structural friction, testability, AI-navigability, deep-module opportunity, or refactor request. | Architecture candidates and deepening recommendations informed by `CONTEXT.md`, SADRs, and target ADRs. |\n | `prototype` | A throwaway logic or UI prototype is needed to answer a design question. | Clearly marked disposable prototype and captured decision. |\n \n ## MergeGate Interaction\ndiff --git a/docs/operations.md b/docs/operations.md\nindex cf1f452d4c589e38ccd58553bda0ad8684b51eb7..ace48a90ffbec9e5c0a7113bef16a4858bc44b3f\n--- a/docs/operations.md\n+++ b/docs/operations.md\n@@ -89,6 +89,16 @@\n      target history, brought forward by `shiki migrate`, not a template.\n    - **Everything else** (CLI scripts, workflows, schemas, docs) is overwritten.\n \n+   A forced upgrade also migrates the former numeric Shiki decision paths to\n+   `SADR-NNNN-*`. Before the first write, the installer inventories the exact\n+   legacy Shiki paths and authorizes each deletion only when the existing\n+   `.shiki/install-stamp.json` names that path and its SHA-256 digest still\n+   matches the file. An absent, unreadable, malformed, incomplete, or\n+   mismatching stamp reports every blocked path and stops with no filesystem\n+   change. Target-owned `NNNN-*` ADRs are not cleanup candidates. A non-force\n+   install deletes nothing and retains any still-matching legacy stamp entries\n+   so a later forced upgrade can prove ownership.\n+\n    If the target has pending migrations, `--force` refuses and writes nothing \u2014\n    apply migrations first (step 3), then re-run. The run ends with a single\n    summary of every `.new` written, so a half-upgrade is visible.\ndiff --git a/docs/releasing.md b/docs/releasing.md\nindex 10d73828e7dbd64e9d9f6b83bc84d010537ce62a..aafeee8c4d441e1e427efee2ce4f81a647f020e1\n--- a/docs/releasing.md\n+++ b/docs/releasing.md\n@@ -67,7 +67,7 @@\n CLI in `scripts/`. There are no third-party Python runtime dependencies. The CLI\n is intentionally **not** exposed as a `[project.scripts]` console entry point\n because `scripts/shiki.py` is a dependency-free shim and `scripts/` is not\n-packaged as an importable distribution (see the ADR for the rationale).\n+packaged as an importable distribution (see SADR-0007 for the rationale).\n \n Supported install paths:\n \n@@ -155,6 +155,15 @@\n    [docs/operations.md](operations.md#the-new-convention). If the target has\n    pending migrations, `--force` refuses and writes nothing; apply migrations\n    (step 2) first, then re-run.\n+\n+   The SADR namespace upgrade has an additional pre-write gate: each former\n+   numeric Shiki decision path is removed only when the existing install stamp\n+   contains that exact path and a digest matching its current bytes. The\n+   installer aggregates absent/unreadable/malformed-stamp, missing-digest, and\n+   digest-mismatch blockers before raising, so refusal leaves the target tree\n+   byte-identical. Numeric target ADRs are preserved. A successful forced\n+   upgrade installs and stamps `SADR-*` paths and drops the removed legacy paths\n+   from the refreshed stamp.\n 2. Apply repository-local `.shiki/` state migrations with the migrate command:\n \n    ```bash\ndiff --git a/docs/usage.md b/docs/usage.md\nindex b83407ee3970dd395dd893010bc5dbe8bad26051..a130ef6e40dfd8169a5bc701339351097df1a09f\n--- a/docs/usage.md\n+++ b/docs/usage.md\n@@ -67,8 +67,10 @@\n shiki install-target /path/to/target-repo --local-only\n ```\n \n-Use `--force` only when you intentionally want to overwrite existing target\n-files.\n+Use `--force` only when you intentionally want to refresh the target template.\n+For the SADR namespace migration, `--force` deletes only exact legacy Shiki\n+decision paths whose existing install-stamp digest still matches; any ownership\n+blocker stops before the first write. Numeric target ADRs are never candidates.\n \n ## `shiki bootstrap-platform` \u2014 publish the Shiki platform repo\n \ndiff --git a/scripts/guardian_approval_signal.py b/scripts/guardian_approval_signal.py\nindex d059f92b2c82aa2b5d6ca421365537a87886c1cb..16924892714f4f5b9303c0ef0e325a7e70075630\n--- a/scripts/guardian_approval_signal.py\n+++ b/scripts/guardian_approval_signal.py\n@@ -199,8 +199,8 @@\n ) -> Any:\n     \"\"\"Evaluate SADR-0015 Contract Approval for the CCA Guardian signal.\n \n-    The SAME OR that MergeGate applies must reach this deterministic signal: ADR\n-    0015 is explicit that without it, a contract-approved PR still returns\n+    The SAME OR that MergeGate applies must reach this deterministic signal:\n+    SADR-0015 is explicit that without it, a contract-approved PR still returns\n     ``needs_guardian``, the CCA job fails, and the MergeGate policy check never\n     runs \u2014 the gate this change moves would never be reached. This resolves the\n     task the SAME way MergeGate does (the first Shiki task id in the PR body),\ndiff --git a/scripts/shiki_contract_approval.py b/scripts/shiki_contract_approval.py\nindex 941bcb96fdd8b9a85bc0c4f0f04d5dad9d04839c..4a4b418f6527413161db82670bec857a043360e2\n--- a/scripts/shiki_contract_approval.py\n+++ b/scripts/shiki_contract_approval.py\n@@ -142,7 +142,7 @@\n     changed_files_status: Any,\n     registration: Any,\n ) -> ContractApprovalResult:\n-    \"\"\"Whether this PR carries an SADR-0015 Contract Approval.\n+    \"\"\"Whether this PR carries a SADR-0015 Contract Approval.\n \n     The four conditions are evaluated IN ORDER, each failure recorded as a\n     distinct reason string:\ndiff --git a/scripts/shiki_guardian.py b/scripts/shiki_guardian.py\nindex a4ecb79bbda55fd5c8b06adfae2eb99760a03c45..4dd99bbe714e26f421fa19abac7a34ae1b195ecc\n--- a/scripts/shiki_guardian.py\n+++ b/scripts/shiki_guardian.py\n@@ -12,7 +12,7 @@\n # The carry re-derives its proof from git in-process on every judging invocation;\n # it is never read from an artifact, so there is nothing to go stale or be forged.\n # shiki_sync_proof is the ONLY guardian-family module that runs git, and every one\n-# of its invocations is read-only and local (ADR: prove-from-git base sync).\n+# of its invocations is read-only and local (SADR: prove-from-git base sync).\n from shiki_sync_proof import verify_pure_base_sync\n \n GUARDIAN_POLICY_PATH = \".shiki/guardian-policy.json\"\ndiff --git a/scripts/shiki_guardian_review.py b/scripts/shiki_guardian_review.py\nindex dd0f104d457899e0ba5ff1e162011c1e90b6f92b..e10ef58c046324e7ea3a3392feb3048ca77414ce\n--- a/scripts/shiki_guardian_review.py\n+++ b/scripts/shiki_guardian_review.py\n@@ -159,7 +159,7 @@\n     docs_only = _is_docs_only(changed_files)\n     if docs_only and matched == 0:\n         focus.append(\n-            \"Docs/ADR-only change: confirm the change is genuinely documentation/decision-only, introduces no behavior or contract drift, and stays consistent with the constitution and existing ADRs.\"\n+            \"Docs/decision-record-only change: confirm the change is genuinely documentation/decision-only, introduces no behavior or contract drift, and stays consistent with the constitution and existing SADRs and target ADRs.\"\n         )\n \n     # Conservative augmentation: unknown (no category and not docs-only), mixed\ndiff --git a/scripts/shiki_installer.py b/scripts/shiki_installer.py\nindex be064ab7a29ff6836f7f7a1d70500a43e0d11d7b..9fd8eefd137ce7003dad5c0dcb2e48e2cd47ab32\n--- a/scripts/shiki_installer.py\n+++ b/scripts/shiki_installer.py\n@@ -118,6 +118,7 @@\n # what lets a target carry it without a governance-schema change.\n INSTALL_STAMP_PATH = \".shiki/install-stamp.json\"\n INSTALL_STAMP_VERSION = 1\n+LEGACY_SHIKI_SADR_MAX = 18\n \n # Under --force the shipped surface splits three ways, so nothing is lost and\n # nothing is silently stale.\n@@ -237,9 +238,98 @@\n         )\n \n \n+def legacy_shiki_adr_paths() -> tuple[str, ...]:\n+    \"\"\"Former numeric paths for Shiki SADR-0001 through SADR-0018.\n+\n+    The authoritative slugs come from the current SADR filenames. SADR-0019 is\n+    the namespace decision itself and never shipped under a numeric legacy path.\n+    \"\"\"\n+    paths: list[str] = []\n+    for sadr in sorted((ROOT / \"docs\" / \"adr\").glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\")):\n+        number = sadr.name.split(\"-\", 2)[1]\n+        if int(number) <= LEGACY_SHIKI_SADR_MAX:\n+            paths.append(f\"docs/adr/{sadr.name.removeprefix('SADR-')}\")\n+    return tuple(paths)\n+\n+\n+def preflight_legacy_sadr_cleanup(target: Path, *, force: bool) -> tuple[str, ...]:\n+    \"\"\"Authorize exact legacy Shiki ADR removals before the first write.\n+\n+    A non-force install never removes existing files. A forced install needs no\n+    stamp when no legacy Shiki path is present. When legacy paths do exist, each\n+    must be named in a readable install stamp and its current bytes must match\n+    the recorded digest.\n+    \"\"\"\n+    if not force:\n+        return ()\n+    present = tuple(\n+        relative\n+        for relative in legacy_shiki_adr_paths()\n+        if (target / relative).exists() or (target / relative).is_symlink()\n+    )\n+    if not present:\n+        return ()\n+\n+    stamp_path = target / INSTALL_STAMP_PATH\n+    try:\n+        raw = stamp_path.read_text(encoding=\"utf-8\")\n+        stamp = json.loads(raw)\n+    except FileNotFoundError:\n+        detail = \"install stamp is absent\"\n+        blockers = [f\"{relative}: {detail}\" for relative in present]\n+    except (OSError, UnicodeDecodeError) as error:\n+        detail = f\"install stamp is unreadable ({error})\"\n+        blockers = [f\"{relative}: {detail}\" for relative in present]\n+    except json.JSONDecodeError as error:\n+        detail = f\"install stamp is malformed ({error})\"\n+        blockers = [f\"{relative}: {detail}\" for relative in present]\n+    else:\n+        if not isinstance(stamp, dict):\n+            blockers = [\n+                f\"{relative}: install stamp is malformed (expected an object)\"\n+                for relative in present\n+            ]\n+        else:\n+            digests = stamp.get(\"digests\")\n+            blockers = []\n+            if not isinstance(digests, dict):\n+                blockers.extend(\n+                    f\"{relative}: install stamp has no digest for this path\"\n+                    for relative in present\n+                )\n+            else:\n+                for relative in present:\n+                    expected = digests.get(relative)\n+                    if not isinstance(expected, str) or not expected:\n+                        blockers.append(\n+                            f\"{relative}: install stamp has no digest for this path\"\n+                        )\n+                        continue\n+                    try:\n+                        actual = _sha256_file(target / relative)\n+                    except OSError as error:\n+                        blockers.append(\n+                            f\"{relative}: legacy path is unreadable ({error})\"\n+                        )\n+                        continue\n+                    if actual != expected:\n+                        blockers.append(\n+                            f\"{relative}: digest mismatch (stamp {expected}, current {actual})\"\n+                        )\n+\n+    if blockers:\n+        joined = \"\\n\".join(f\"- {blocker}\" for blocker in blockers)\n+        raise ShikiError(\n+            \"legacy Shiki ADR cleanup is not authorized:\\n\"\n+            f\"{joined}\\nNo files were written.\"\n+        )\n+    return present\n+\n+\n def install_template(target: Path, *, force: bool, validate: bool) -> list[NewFileNote]:\n     # Preconditions first: never rewrite the tree of a target that cannot be\n     # upgraded cleanly (see refuse_pending_migrations).\n+    legacy_cleanup = preflight_legacy_sadr_cleanup(target, force=force)\n     refuse_pending_migrations(target)\n \n     # Resolve the target's required CODEOWNERS owner once from its\n@@ -250,6 +340,10 @@\n     # verbatim. See ``codeowners_required_owner``.\n     codeowners_owner = codeowners_required_owner(target)\n \n+    for relative in legacy_cleanup:\n+        (target / relative).unlink()\n+        info(f\"removed stamped legacy Shiki ADR: {target / relative}\")\n+\n     notes: list[NewFileNote] = []\n     for relative in TEMPLATE_PATHS:\n         source = ROOT / relative\n@@ -647,6 +741,22 @@\n     digests = target_stamp_digests(target)\n     existing = read_install_stamp(target)\n     if existing is not None:\n+        # A non-force install deliberately leaves legacy Shiki ADRs in place.\n+        # Preserve only still-matching ownership proof so a later forced\n+        # upgrade can authorize cleanup; never carry a missing or stale digest.\n+        existing_digests = existing.get(\"digests\")\n+        if isinstance(existing_digests, dict):\n+            for relative in legacy_shiki_adr_paths():\n+                expected = existing_digests.get(relative)\n+                path = target / relative\n+                if not isinstance(expected, str) or not path.is_file():\n+                    continue\n+                try:\n+                    actual = _sha256_file(path)\n+                except OSError:\n+                    continue\n+                if actual == expected:\n+                    digests[relative] = actual\n         if _incoming_is_older(commit, existing.get(\"platform_commit\")):\n             warn(\n                 \"kept existing install stamp: incoming install \"\ndiff --git a/scripts/shiki_loop.py b/scripts/shiki_loop.py\nindex 3611641da8aeb5038b1da90e3114b09a473ec6d8..84caa491b9cad0edc4176f209c13e9221e975681\n--- a/scripts/shiki_loop.py\n+++ b/scripts/shiki_loop.py\n@@ -424,7 +424,7 @@\n         # unresolvable CCA verdict has already produced its terminal stop and is never\n         # laundered into a head-moving sync (SADR-0010). It also never fires while the\n         # policy (Guardian) gate is red \u2014 for ANY reason, not only when it is the sole\n-        # red check \u2014 so a head-bound Guardian approval, or an SADR-0015\n+        # red check \u2014 so a head-bound Guardian approval, or a SADR-0015\n         # contract-approval carry that base movement defeated (the policy gate compares\n         # base and head task governance directly), is never silently restored by\n         # adopting base wholesale. It is bounded by a durable per-task counter so the\ndiff --git a/scripts/shiki_tasks.py b/scripts/shiki_tasks.py\nindex aec3809f24b4a0c8c96fc50971cff9ae9ad30381..d531052e9d9717058d4631483cefab44c26a65c1\n--- a/scripts/shiki_tasks.py\n+++ b/scripts/shiki_tasks.py\n@@ -821,7 +821,7 @@\n         \"prompt\": prompt,\n         \"entry_skill\": \"grill-with-docs\",\n         \"required_next_steps\": [\n-            \"Run grill-with-docs until terminology, boundaries, risks, and ADR-worthy decisions are settled.\",\n+            \"Run grill-with-docs until terminology, boundaries, risks, and decision-record-worthy choices are settled.\",\n             \"Obtain the operator's explicit approval of the PRD/requirements (Spec Freeze).\",\n             \"Write a machine-readable plan JSON with grill_with_docs.status=complete and spec_freeze.status=frozen.\",\n             \"Run shiki plan ingest --plan-file PLAN.json.\",\ndiff --git a/scripts/test_shiki_goal_loop.sh b/scripts/test_shiki_goal_loop.sh\nindex 753c7b516e10f0be3bc92a4bd0eb1fa699776ae6..8f3820a0a079b3044424cfe1a6c6b7fe68896e67\n--- a/scripts/test_shiki_goal_loop.sh\n+++ b/scripts/test_shiki_goal_loop.sh\n@@ -231,8 +231,8 @@\n \n # High/critical risk merges autonomously when all required checks are green:\n # the \"MergeGate policy check\" required check IS the Guardian gate (it enforces\n-# guardian-policy.json \u2014 human approval OR external AI guardian review, ADR\n-# 0010), so green checks mean a recorded authority approved. The fake gh returns\n+# guardian-policy.json \u2014 human approval OR external AI guardian review,\n+# SADR-0010), so green checks mean a recorded authority approved. The fake gh returns\n # green checks, simulating that the Guardian gate passed.\n python3 \"$ROOT/scripts/shiki.py\" goal create --target \"$TARGET\" --title \"Guardian gate\" --outcome \"High risk merges via policy gate\" >/tmp/shiki-goal-loop-guardian-goal.json\n GGOAL=\"$(json_get /tmp/shiki-goal-loop-guardian-goal.json goal_id)\"\ndiff --git a/tests/test_adr_references.py b/tests/test_adr_references.py\nindex 51706329c797d5dd9b47e082a333fa83b261d482..d005da4f771f5e8085ec22d4b787980e68f5ae8c\n--- a/tests/test_adr_references.py\n+++ b/tests/test_adr_references.py\n@@ -1,8 +1,8 @@\n-\"\"\"Every ``ADR ####`` citation must resolve to exactly one decision record.\n+\"\"\"Every target ADR and Shiki SADR citation resolves in its own namespace.\n \n Background. ``scripts/mergegate_check.py`` justifies ``is_bookkeeping_closeout``\n-with \"ADR 0017\" nine times and ``scripts/guardian_approval_signal.py`` cites\n-0016/0017 seven times, yet for a stretch the numbering in ``docs/adr/`` stopped\n+with \"SADR-0017\" nine times and ``scripts/guardian_approval_signal.py`` cites\n+SADR-0016/SADR-0017 seven times, yet for a stretch the numbering in ``docs/adr/`` stopped\n at 0015: the authoring commits lived on a coordinator branch and no task ever\n declared ``docs/adr/...`` in its locks, so nothing carried the records to the\n default branch. The stated basis of an exemption that merged seven PRs with zero\n@@ -11,11 +11,12 @@\n and the directory drifted with no signal.\n \n This is the same unbound-pair shape behind other silent failures: a reference\n-and its target with nothing asserting they agree. This test binds them. It scans\n-every ``scripts/*.py`` (plus ``docs/**/*.md`` and the root ``*.md`` governance\n-files, which are cheap) for the ``ADR ####`` pattern and asserts each cited\n-number resolves to exactly one ``docs/adr/####-*.md``. The converse is\n-deliberately NOT required: an ADR may exist without being cited.\n+and its target with nothing asserting they agree. This test binds them. Target\n+``ADR NNNN`` citations resolve only to ``docs/adr/NNNN-*.md``; Shiki\n+``SADR-NNNN`` citations resolve only to ``docs/adr/SADR-NNNN-*.md``. The scan\n+covers current governance docs, scripts, workflows, prompts, packaging metadata,\n+and repo-local Codex guidance while deliberately excluding historical\n+``.shiki`` evidence. The converse is not required: a record may be uncited.\n \n The suite is dependency free: standard-library ``unittest`` only.\n \"\"\"\n@@ -31,17 +32,18 @@\n \n REPO_ROOT = Path(__file__).resolve().parents[1]\n \n-# \"ADR 0016\" -- a four-digit citation. The trailing \\b keeps a five-digit token\n-# from being read as a truncated four-digit number.\n-_CITATION = re.compile(r\"\\bADR (\\d{4})\\b\")\n-# \"0016-locks-bound-product-paths.md\" -- the leading four digits name the record.\n-_ADR_FILENAME = re.compile(r\"^(\\d{4})-.*\\.md$\")\n+# ``ADR 0016`` / ``SADR-0016`` -- a namespace plus four-digit citation. The\n+# trailing ``\\b`` keeps a five-digit token from being read as a truncated one.\n+_CITATION = re.compile(r\"\\b(S?ADR)[ -](\\d{4})\\b\")\n+# Target ADRs retain the numeric filename; Shiki platform decisions use SADR.\n+_DECISION_FILENAME = re.compile(r\"^(?:(SADR)-)?(\\d{4})-.*\\.md$\")\n \n \n class Violation(NamedTuple):\n     \"\"\"A citation that does not resolve to exactly one decision record.\"\"\"\n \n     citing_file: str  # repo-relative path of the file that cites the ADR\n+    namespace: str  # \"ADR\" for target decisions; \"SADR\" for Shiki decisions\n     number: str  # the cited four-digit number, e.g. \"0016\"\n     line: int  # 1-based line of the citation\n     reason: str  # \"missing\" (zero matches) or \"ambiguous\" (two or more)\n@@ -49,44 +51,59 @@\n \n     @property\n     def expected_path(self) -> str:\n-        return f\"docs/adr/{self.number}-*.md\"\n+        prefix = \"SADR-\" if self.namespace == \"SADR\" else \"\"\n+        return f\"docs/adr/{prefix}{self.number}-*.md\"\n \n     def describe(self) -> str:\n         \"\"\"Name the citing file, the cited number, and the missing path.\"\"\"\n         if self.reason == \"missing\":\n             return (\n-                f\"{self.citing_file}:{self.line} cites ADR {self.number} \"\n+                f\"{self.citing_file}:{self.line} cites {self.namespace} {self.number} \"\n                 f\"but no document matches {self.expected_path}\"\n             )\n         joined = \", \".join(self.matches)\n         return (\n-            f\"{self.citing_file}:{self.line} cites ADR {self.number} which \"\n+            f\"{self.citing_file}:{self.line} cites {self.namespace} {self.number} which \"\n             f\"resolves to {len(self.matches)} documents (expected exactly one \"\n             f\"at {self.expected_path}): {joined}\"\n         )\n \n \n-def _adr_index(root: Path) -> dict[str, list[Path]]:\n-    \"\"\"Map every four-digit ADR number to the record files that claim it.\"\"\"\n-    index: dict[str, list[Path]] = {}\n+def _adr_index(root: Path) -> dict[tuple[str, str], list[Path]]:\n+    \"\"\"Map each decision namespace and number to the files that claim it.\"\"\"\n+    index: dict[tuple[str, str], list[Path]] = {}\n     adr_dir = root / \"docs\" / \"adr\"\n     if adr_dir.is_dir():\n         for path in sorted(adr_dir.glob(\"*.md\")):\n-            match = _ADR_FILENAME.match(path.name)\n+            match = _DECISION_FILENAME.match(path.name)\n             if match:\n-                index.setdefault(match.group(1), []).append(path)\n+                namespace = \"SADR\" if match.group(1) else \"ADR\"\n+                index.setdefault((namespace, match.group(2)), []).append(path)\n     return index\n \n \n def _citing_files(root: Path, include_docs: bool = True) -> list[Path]:\n-    \"\"\"Files scanned for citations: scripts/*.py, and cheaply the markdown.\"\"\"\n+    \"\"\"Current, shipped text surfaces scanned for decision citations.\"\"\"\n     files = sorted((root / \"scripts\").glob(\"*.py\"))\n+    files += sorted((root / \"scripts\").glob(\"*.sh\"))\n     if include_docs:\n         docs = root / \"docs\"\n         if docs.is_dir():\n             files += sorted(docs.rglob(\"*.md\"))\n         files += sorted(root.glob(\"*.md\"))\n-    return files\n+        pyproject = root / \"pyproject.toml\"\n+        if pyproject.is_file():\n+            files.append(pyproject)\n+        for relative in (\".github\", \".codex\"):\n+            surface = root / relative\n+            if surface.is_dir():\n+                files += sorted(\n+                    path\n+                    for path in surface.rglob(\"*\")\n+                    if path.is_file()\n+                    and path.suffix in {\".md\", \".py\", \".sh\", \".toml\", \".yaml\", \".yml\"}\n+                )\n+    return sorted(set(files))\n \n \n def find_adr_reference_violations(\n@@ -94,10 +111,9 @@\n ) -> list[Violation]:\n     \"\"\"Return every ``ADR ####`` citation under ``root`` that fails to resolve.\n \n-    A citation resolves when exactly one ``docs/adr/####-*.md`` matches its\n-    number. Zero matches is a \"missing\" violation; two or more is \"ambiguous\".\n-    Records that are never cited produce nothing -- the converse is not\n-    required.\n+    A citation resolves when exactly one record in the same namespace matches\n+    its number. Zero matches is a \"missing\" violation; two or more is\n+    \"ambiguous\". Records that are never cited produce nothing.\n     \"\"\"\n     root = Path(root)\n     index = _adr_index(root)\n@@ -110,13 +126,15 @@\n         rel = path.relative_to(root).as_posix()\n         for lineno, line in enumerate(text.splitlines(), start=1):\n             for match in _CITATION.finditer(line):\n-                number = match.group(1)\n-                found = index.get(number, [])\n+                namespace, number = match.groups()\n+                found = index.get((namespace, number), [])\n                 if len(found) == 1:\n                     continue\n                 reason = \"missing\" if not found else \"ambiguous\"\n                 rels = [p.relative_to(root).as_posix() for p in found]\n-                violations.append(Violation(rel, number, lineno, reason, rels))\n+                violations.append(\n+                    Violation(rel, namespace, number, lineno, reason, rels)\n+                )\n     return violations\n \n \n@@ -129,8 +147,8 @@\n         self.assertEqual(\n             violations,\n             [],\n-            \"Every 'ADR ####' citation must resolve to exactly one \"\n-            f\"docs/adr/####-*.md:\\n{detail}\",\n+            \"Every ADR/SADR citation must resolve exactly once in its own \"\n+            f\"docs/adr namespace:\\n{detail}\",\n         )\n \n     def test_scan_actually_reads_scripts(self) -> None:\n@@ -142,12 +160,38 @@\n             \"the citation scan must cover scripts/*.py\",\n         )\n \n+    def test_platform_citations_do_not_use_unresolved_numeric_shorthand(self) -> None:\n+        shorthand = re.compile(r\"SADR-\\d{4}/\\d{4}\")\n+        offenders = []\n+        for path in _citing_files(REPO_ROOT):\n+            text = path.read_text(encoding=\"utf-8\")\n+            if shorthand.search(text):\n+                offenders.append(path.relative_to(REPO_ROOT).as_posix())\n+        self.assertEqual(\n+            offenders,\n+            [],\n+            \"repeat the SADR prefix so every cited number is independently resolvable\",\n+        )\n+\n     def test_carried_records_are_indexed(self) -> None:\n         # The two records this Goal carried onto the default branch, and which\n         # the scripts cite most, must be present.\n         index = _adr_index(REPO_ROOT)\n-        self.assertIn(\"0016\", index)\n-        self.assertIn(\"0017\", index)\n+        self.assertIn((\"SADR\", \"0016\"), index)\n+        self.assertIn((\"SADR\", \"0017\"), index)\n+\n+    def test_platform_tree_has_only_sadr_0001_through_0019(self) -> None:\n+        adr_dir = REPO_ROOT / \"docs\" / \"adr\"\n+        sadrs = sorted(adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\"))\n+        self.assertEqual(\n+            {path.name.split(\"-\", 2)[1] for path in sadrs},\n+            {f\"{number:04d}\" for number in range(1, 20)},\n+        )\n+        self.assertEqual(\n+            list(adr_dir.glob(\"[0-9][0-9][0-9][0-9]-*.md\")),\n+            [],\n+            \"the Shiki platform tree must not retain legacy numeric SADR paths\",\n+        )\n \n \n class FixtureAdrReferences(unittest.TestCase):\n@@ -180,6 +224,26 @@\n         self.assertIn(\"9991\", message)  # cited number\n         self.assertIn(\"docs/adr/9991-*.md\", message)  # missing path\n \n+    def test_target_adr_does_not_satisfy_missing_platform_sadr(self) -> None:\n+        root = self._make_repo(\n+            adr_names=[\"0042-target-decision.md\"],\n+            script_body='\"\"\"Target ADR 0042 exists, but platform SADR-0042 does not.\"\"\"\\n',\n+        )\n+        violations = find_adr_reference_violations(root, include_docs=False)\n+        self.assertEqual(len(violations), 1)\n+        self.assertEqual(violations[0].namespace, \"SADR\")\n+        self.assertEqual(violations[0].number, \"0042\")\n+        self.assertEqual(violations[0].expected_path, \"docs/adr/SADR-0042-*.md\")\n+\n+    def test_target_adr_and_platform_sadr_may_reuse_a_number(self) -> None:\n+        root = self._make_repo(\n+            adr_names=[\"0042-target-decision.md\", \"SADR-0042-platform-decision.md\"],\n+            script_body='\"\"\"Target ADR 0042 and platform SADR-0042 are distinct.\"\"\"\\n',\n+        )\n+        self.assertEqual(\n+            find_adr_reference_violations(root, include_docs=False), []\n+        )\n+\n     def test_uncited_adr_is_allowed(self) -> None:\n         # An ADR exists but nothing cites it -> not a violation. The converse\n         # (every ADR must be cited) is explicitly NOT required.\ndiff --git a/tests/test_guardian_review.py b/tests/test_guardian_review.py\nindex 88829e7810212635e2fc98aa5918d6f26294677d..00a85a237acd5bf2d6cb5b294128198c422402ca\n--- a/tests/test_guardian_review.py\n+++ b/tests/test_guardian_review.py\n@@ -162,7 +162,7 @@\n \n     def test_docs_only_is_light_and_not_conservative(self):\n         focus = gr.classify_review_focus([\"docs/adr/0099-x.md\", \"README.md\"], \"\", \"low\")\n-        self.assertTrue(any(\"Docs/ADR-only\" in f for f in focus))\n+        self.assertTrue(any(\"Docs/decision-record-only\" in f for f in focus))\n         self.assertFalse(any(\"conservative default\" in f.lower() for f in focus))\n \n     def test_unknown_category_is_conservative(self):\ndiff --git a/tests/test_policy_job_merged_prs.py b/tests/test_policy_job_merged_prs.py\nindex d45512e74681e5e649279236c7d4e79d5c3da471..b97989f8bd74d091de1a6b5275a20d8dc68c4512\n--- a/tests/test_policy_job_merged_prs.py\n+++ b/tests/test_policy_job_merged_prs.py\n@@ -3,8 +3,8 @@\n \n ``.github/workflows/shiki-cca-completion.yml`` has two consumers of the merged-PR\n proof: the CCA *signal* (``guardian_approval_signal.py`` in \"Prepare CCA context\")\n-and the authoritative gate (``mergegate_check.py`` in \"Enforce MergeGate\"). ADR\n-0017's bookkeeping-closeout exemption (condition 6) needs the merged implementation\n+and the authoritative gate (``mergegate_check.py`` in \"Enforce MergeGate\").\n+SADR-0017's bookkeeping-closeout exemption (condition 6) needs the merged implementation\n PR proven, and a closeout carries NO ``post_merge_reconcile`` marker. The signal\n already resolves ``MERGED_PRS`` unconditionally, but \"Enforce MergeGate\" used to\n resolve it only inside ``grep -q '<!-- shiki:post_merge_reconcile -->'``. So a\ndiff --git a/tests/test_sadr_install_migration.py b/tests/test_sadr_install_migration.py\nnew file mode 100644\nindex 0000000000000000000000000000000000000000..d51eac872b1c79ff4579c4c1401ca5d775a60d8d\n--- /dev/null\n+++ b/tests/test_sadr_install_migration.py\n@@ -0,0 +1,237 @@\n+\"\"\"SADR installation and collision-safe legacy upgrade behavior.\"\"\"\n+\n+from __future__ import annotations\n+\n+import contextlib\n+import hashlib\n+import io\n+import json\n+import shutil\n+import tempfile\n+import unittest\n+from pathlib import Path\n+\n+import shiki_test_support  # noqa: F401  (path bootstrap)\n+\n+import shiki_installer\n+from tests.test_adr_references import find_adr_reference_violations\n+\n+\n+PRODUCT_NUMBERS = tuple(range(13, 19))\n+\n+\n+def _quiet_install(target: Path, *, force: bool, validate: bool) -> None:\n+    with contextlib.redirect_stdout(io.StringIO()):\n+        shiki_installer.install_template(target, force=force, validate=validate)\n+\n+\n+def _sha256(path: Path) -> str:\n+    return hashlib.sha256(path.read_bytes()).hexdigest()\n+\n+\n+def _legacy_sadr_paths() -> tuple[str, ...]:\n+    return tuple(\n+        f\"docs/adr/{path.name.removeprefix('SADR-')}\"\n+        for path in sorted((shiki_installer.ROOT / \"docs\" / \"adr\").glob(\"SADR-*.md\"))\n+        if path.name != \"SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\"\n+    )\n+\n+\n+def _create_legacy_upgrade_fixture(\n+    target: Path,\n+) -> tuple[dict[str, bytes], tuple[str, ...]]:\n+    adr_dir = target / \"docs\" / \"adr\"\n+    adr_dir.mkdir(parents=True)\n+    product_bytes = {\n+        f\"{number:04d}-cubeshare-product-decision.md\": (\n+            f\"# CubeShare ADR {number:04d}\\n\\nDo not rewrite {number}.\\n\"\n+        ).encode()\n+        for number in PRODUCT_NUMBERS\n+    }\n+    for name, content in product_bytes.items():\n+        (adr_dir / name).write_bytes(content)\n+\n+    legacy_paths = _legacy_sadr_paths()\n+    for relative in legacy_paths:\n+        (target / relative).write_bytes(f\"legacy Shiki bytes for {relative}\\n\".encode())\n+\n+    stamp_path = target / shiki_installer.INSTALL_STAMP_PATH\n+    stamp_path.parent.mkdir(parents=True, exist_ok=True)\n+    stamp = {\n+        \"version\": shiki_installer.INSTALL_STAMP_VERSION,\n+        \"platform_commit\": None,\n+        \"installed_at\": \"2000-01-01T00:00:00+00:00\",\n+        \"digests\": {\n+            relative: _sha256(target / relative) for relative in legacy_paths\n+        },\n+    }\n+    stamp_path.write_text(\n+        json.dumps(stamp, indent=2, sort_keys=True) + \"\\n\", encoding=\"utf-8\"\n+    )\n+    return product_bytes, legacy_paths\n+\n+\n+def _tree_snapshot(root: Path) -> dict[str, tuple[str, bytes | str]]:\n+    \"\"\"Recursive byte/shape snapshot used to prove preflight is write-free.\"\"\"\n+    snapshot: dict[str, tuple[str, bytes | str]] = {}\n+    for path in sorted(root.rglob(\"*\")):\n+        relative = path.relative_to(root).as_posix()\n+        if path.is_symlink():\n+            snapshot[relative] = (\"symlink\", path.readlink().as_posix())\n+        elif path.is_dir():\n+            snapshot[relative] = (\"directory\", \"\")\n+        else:\n+            snapshot[relative] = (\"file\", path.read_bytes())\n+    return snapshot\n+\n+\n+class FreshSadrInstallTests(unittest.TestCase):\n+    def setUp(self) -> None:\n+        self.tmp = Path(tempfile.mkdtemp(prefix=\"shiki-sadr-fresh-\"))\n+        self.addCleanup(lambda: shutil.rmtree(self.tmp, ignore_errors=True))\n+        self.target = self.tmp / \"target\"\n+        self.adr_dir = self.target / \"docs\" / \"adr\"\n+        self.adr_dir.mkdir(parents=True)\n+        self.product_bytes = {\n+            f\"{number:04d}-product-decision.md\": (\n+                f\"# Product ADR {number:04d}\\n\\nTarget-owned bytes {number}.\\n\"\n+            ).encode()\n+            for number in PRODUCT_NUMBERS\n+        }\n+        for name, content in self.product_bytes.items():\n+            (self.adr_dir / name).write_bytes(content)\n+\n+    def test_fresh_install_preserves_product_adrs_and_installs_all_sadrs(self) -> None:\n+        _quiet_install(self.target, force=False, validate=True)\n+\n+        for name, expected in self.product_bytes.items():\n+            self.assertEqual((self.adr_dir / name).read_bytes(), expected)\n+\n+        sadr_numbers = {\n+            path.name.split(\"-\", 2)[1]\n+            for path in self.adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\")\n+        }\n+        self.assertEqual(sadr_numbers, {f\"{number:04d}\" for number in range(1, 20)})\n+\n+        numeric_records = {\n+            path.name\n+            for path in self.adr_dir.glob(\"[0-9][0-9][0-9][0-9]-*.md\")\n+        }\n+        self.assertEqual(numeric_records, set(self.product_bytes))\n+        stamp = json.loads(\n+            (self.target / shiki_installer.INSTALL_STAMP_PATH).read_text(\n+                encoding=\"utf-8\"\n+            )\n+        )\n+        for path in self.adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\"):\n+            relative = path.relative_to(self.target).as_posix()\n+            self.assertEqual(stamp[\"digests\"][relative], _sha256(path))\n+        self.assertEqual(find_adr_reference_violations(self.target), [])\n+\n+\n+class LegacySadrUpgradeTests(unittest.TestCase):\n+    def setUp(self) -> None:\n+        self.tmp = Path(tempfile.mkdtemp(prefix=\"shiki-sadr-upgrade-\"))\n+        self.addCleanup(lambda: shutil.rmtree(self.tmp, ignore_errors=True))\n+        self.target = self.tmp / \"target\"\n+        self.adr_dir = self.target / \"docs\" / \"adr\"\n+        self.product_bytes, self.legacy_paths = _create_legacy_upgrade_fixture(\n+            self.target\n+        )\n+\n+    def test_force_upgrade_deletes_only_stamped_legacy_sadrs(self) -> None:\n+        _quiet_install(self.target, force=True, validate=True)\n+\n+        for relative in self.legacy_paths:\n+            self.assertFalse((self.target / relative).exists(), relative)\n+        for name, expected in self.product_bytes.items():\n+            self.assertEqual((self.adr_dir / name).read_bytes(), expected)\n+\n+        sadrs = list(self.adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\"))\n+        self.assertEqual(len(sadrs), 19)\n+        refreshed = json.loads(\n+            (self.target / shiki_installer.INSTALL_STAMP_PATH).read_text(encoding=\"utf-8\")\n+        )\n+        for relative in self.legacy_paths:\n+            self.assertNotIn(relative, refreshed[\"digests\"])\n+        for path in sadrs:\n+            relative = path.relative_to(self.target).as_posix()\n+            self.assertEqual(refreshed[\"digests\"][relative], _sha256(path))\n+\n+    def test_non_force_install_never_deletes_legacy_files(self) -> None:\n+        before = {\n+            relative: (self.target / relative).read_bytes()\n+            for relative in self.legacy_paths\n+        }\n+        _quiet_install(self.target, force=False, validate=False)\n+        for relative, expected in before.items():\n+            self.assertEqual((self.target / relative).read_bytes(), expected)\n+        refreshed = json.loads(\n+            (self.target / shiki_installer.INSTALL_STAMP_PATH).read_text(encoding=\"utf-8\")\n+        )\n+        for relative in self.legacy_paths:\n+            self.assertEqual(refreshed[\"digests\"][relative], _sha256(self.target / relative))\n+\n+\n+class LegacyCleanupPreflightTests(unittest.TestCase):\n+    def setUp(self) -> None:\n+        self.tmp = Path(tempfile.mkdtemp(prefix=\"shiki-sadr-preflight-\"))\n+        self.addCleanup(lambda: shutil.rmtree(self.tmp, ignore_errors=True))\n+\n+    def test_all_ownership_blockers_abort_before_any_write(self) -> None:\n+        cases = (\n+            (\"absent\", \"install stamp is absent\"),\n+            (\"unreadable\", \"install stamp is unreadable\"),\n+            (\"malformed\", \"install stamp is malformed\"),\n+            (\"missing-digest\", \"install stamp has no digest\"),\n+            (\"digest-mismatch\", \"digest mismatch\"),\n+        )\n+        for name, expected_reason in cases:\n+            with self.subTest(name=name):\n+                target = self.tmp / name\n+                _, legacy_paths = _create_legacy_upgrade_fixture(target)\n+                stamp_path = target / shiki_installer.INSTALL_STAMP_PATH\n+\n+                if name == \"absent\":\n+                    stamp_path.unlink()\n+                elif name == \"unreadable\":\n+                    stamp_path.unlink()\n+                    stamp_path.mkdir()\n+                elif name == \"malformed\":\n+                    stamp_path.write_text(\"{not-json\\n\", encoding=\"utf-8\")\n+                else:\n+                    stamp = json.loads(stamp_path.read_text(encoding=\"utf-8\"))\n+                    if name == \"missing-digest\":\n+                        stamp[\"digests\"] = {}\n+                    else:\n+                        stamp[\"digests\"] = {\n+                            relative: \"deadbeef\" for relative in legacy_paths\n+                        }\n+                    stamp_path.write_text(\n+                        json.dumps(stamp, indent=2, sort_keys=True) + \"\\n\",\n+                        encoding=\"utf-8\",\n+                    )\n+\n+                before = _tree_snapshot(target)\n+                with self.assertRaises(shiki_installer.ShikiError) as raised:\n+                    _quiet_install(target, force=True, validate=False)\n+                after = _tree_snapshot(target)\n+\n+                self.assertEqual(before, after, f\"{name} preflight changed the tree\")\n+                message = str(raised.exception)\n+                self.assertIn(expected_reason, message)\n+                self.assertIn(\"No files were written\", message)\n+                for relative in legacy_paths:\n+                    self.assertIn(relative, message)\n+\n+    def test_absent_stamp_is_allowed_when_no_legacy_path_exists(self) -> None:\n+        target = self.tmp / \"no-legacy\"\n+        target.mkdir()\n+        _quiet_install(target, force=True, validate=False)\n+        self.assertTrue(\n+            (target / \"docs\" / \"adr\" / \"SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\").is_file()\n+        )\n+\n+\n+if __name__ == \"__main__\":\n+    unittest.main()\n\n2026-08-07T08:28:00.073466Z ERROR codex_models_manager::manager: failed to renew cache TTL: missing field `base_instructions` at line 94 column 5\napply patch\npatch: completed\n/private/tmp/shiki-adr-collision.pa0KwY/.worktrees/shiki-t-20260807t073307221254z-2d22d654-introduc/.shiki/ledger/L-20260807T082800000000Z-c0de0019.json\n/private/tmp/shiki-adr-collision.pa0KwY/.worktrees/shiki-t-20260807t073307221254z-2d22d654-introduc/.shiki/tasks/T-20260807T073307221254Z-2d22d654.json\ndiff --git a/.codex/skills/shiki/SKILL.md b/.codex/skills/shiki/SKILL.md\nindex c8e2e85d9d75d49cc4f2d6286ee22659c4e53e75..93cded623b2c15d9f65c76491a1c2d813ac9e6c0\n--- a/.codex/skills/shiki/SKILL.md\n+++ b/.codex/skills/shiki/SKILL.md\n@@ -18,6 +18,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n Codex CLI does not register this skill as a custom `/shiki` slash command. When\n the user types `/shiki` in Codex CLI and gets \"Unrecognized command\", explain\n that this is expected: invoke Shiki in Codex with natural language such as\n@@ -69,7 +72,7 @@\n \n ## Responsibilities\n \n-- Claude Code implements and repairs by default (`shiki runner claude`, ADR 0008); Codex implements and repairs tasks explicitly assigned to `codex`.\n+- Claude Code implements and repairs by default (`shiki runner claude`, SADR-0008); Codex implements and repairs tasks explicitly assigned to `codex`.\n - Claude Code Action can act as GitHub-side CCA or reviewer.\n - CCA judges completion.\n - MergeGate authorizes state transitions and merge readiness.\ndiff --git a/.github/prompts/cca-completion-check.md b/.github/prompts/cca-completion-check.md\nindex ed203ad5f302ecc81372d8ab27d2b1232ca97497..fd89950528333ef6242e6d5443904f5392ab7e56\n--- a/.github/prompts/cca-completion-check.md\n+++ b/.github/prompts/cca-completion-check.md\n@@ -19,6 +19,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n ## Required Reading\n \n Read only the files needed to judge the PR contract. Prefer this order:\ndiff --git a/.github/prompts/codex-task-handoff.md b/.github/prompts/codex-task-handoff.md\nindex 695e211d76dea1c60e8d5bb360e78487808286a4..59441d576b01a39569722f226c854e5a740e9eb5\n--- a/.github/prompts/codex-task-handoff.md\n+++ b/.github/prompts/codex-task-handoff.md\n@@ -2,7 +2,7 @@\n \n You are Codex Front acting as the Implementer for a Shiki task.\n \n-Follow `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant ADRs, and `docs/agents/codex-handoff.md`.\n+Follow `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant Shiki SADRs and target ADRs, and `docs/agents/codex-handoff.md`.\n \n Rules:\n \ndiff --git a/.shiki/ledger/L-20260807T082800000000Z-c0de0019.json b/.shiki/ledger/L-20260807T082800000000Z-c0de0019.json\nnew file mode 100644\nindex 0000000000000000000000000000000000000000..bac355ecea44037859b3715f58bc32f3353cfcfa\n--- /dev/null\n+++ b/.shiki/ledger/L-20260807T082800000000Z-c0de0019.json\n@@ -0,0 +1,34 @@\n+{\n+  \"actor\": \"codex\",\n+  \"evidence\": [\n+    \"Full task diff reviewed against scope, non-goals, locks, acceptance checks, failure atomicity, path ownership, and compatibility requirements\",\n+    \"Finding repaired: tests/test_sadr_install_migration.py now asserts every fresh-installed SADR path has the matching SHA-256 digest in .shiki/install-stamp.json\",\n+    \"Re-review found no remaining blocking issues; rejected findings: none\",\n+    \"python3 -m unittest discover -s tests: 1147 tests passed\",\n+    \"bash scripts/test_shiki_control_plane.sh: passed\",\n+    \"bash scripts/test_shiki_adversarial_state.sh: passed\",\n+    \"python3 scripts/validate_shiki.py --target .: passed\"\n+  ],\n+  \"goal_id\": \"G-20260807T073307219752Z-1ee1ed68\",\n+  \"id\": \"L-20260807T082800000000Z-c0de0019\",\n+  \"links\": [],\n+  \"review\": {\n+    \"applied_fixes\": [\n+      \"Added direct fresh-install stamp path/digest assertions and reran focused plus full verification\"\n+    ],\n+    \"findings\": [\n+      {\n+        \"detail\": \"The fresh-install test proved SADR files were installed but did not directly prove their paths and digests were recorded in the refreshed install stamp.\",\n+        \"resolution\": \"fixed_and_re_reviewed\",\n+        \"severity\": \"blocking\",\n+        \"title\": \"Fresh-install stamp evidence was indirect\"\n+      }\n+    ],\n+    \"rejected_findings\": [],\n+    \"verdict\": \"clean_after_repairs\"\n+  },\n+  \"summary\": \"code-review implementer verdict CLEAN after re-review: findings=1; applied_fixes=1 (fresh-install stamp-digest assertion); rejected_findings=0 (none)\",\n+  \"task_id\": \"T-20260807T073307221254Z-2d22d654\",\n+  \"timestamp\": \"2026-08-07T08:28:00+00:00\",\n+  \"type\": \"review\"\n+}\ndiff --git a/.shiki/tasks/T-20260807T073307221254Z-2d22d654.json b/.shiki/tasks/T-20260807T073307221254Z-2d22d654.json\nindex e7e150cc1637f1cfac2199e04055e93b53630dcb..78f7fb5ef2ab3dad7e3731673ac2c34b2a8ed149\n--- a/.shiki/tasks/T-20260807T073307221254Z-2d22d654.json\n+++ b/.shiki/tasks/T-20260807T073307221254Z-2d22d654.json\n@@ -36,7 +36,8 @@\n     \"L-20260807T080107948254Z-61f24da0\",\n     \"L-20260807T080228312210Z-07ac626e\",\n     \"L-20260807T080228575038Z-d574f629\",\n-    \"L-20260807T080256330734Z-5bf40cb6\"\n+    \"L-20260807T080256330734Z-5bf40cb6\",\n+    \"L-20260807T082800000000Z-c0de0019\"\n   ],\n   \"locks\": [\n     \"path:.codex/**\",\ndiff --git a/AGENTS.md b/AGENTS.md\nindex 7f041100d49c2a7525f51cb0f5d696a4a6d1c78c..a06bfbabc6fa48a007389e25e12d7ed523936bed\n--- a/AGENTS.md\n+++ b/AGENTS.md\n@@ -22,7 +22,7 @@\n - **Codex Front** is an optional implementer through Codex App, Codex CLI, Codex IDE extension, or Codex Web signed in with ChatGPT OAuth/subscription auth, dispatched with `shiki runner codex` when a task is explicitly assigned to `codex`.\n - **GitHub CCA** is implemented by Claude Code Action by default, using `CLAUDE_CODE_OAUTH_TOKEN`.\n \n-Do not assume `openai/codex-action`, `OPENAI_API_KEY`, or API-key based Codex automation in the default Shiki path. API-key runners are explicit target-repo extensions and require their own ADR.\n+Do not assume `openai/codex-action`, `OPENAI_API_KEY`, or API-key based Codex automation in the default Shiki path. API-key runners are explicit target-repo extensions and require their own target ADR.\n \n Shiki is not:\n \n@@ -44,10 +44,14 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use\n+`SADR-NNNN-*.md`; target product decisions retain `NNNN-*.md` and the `ADR`\n+namespace.\n+\n Use the canonical source-of-truth order above. Runtime-specific reading orders\n are operational guidance only and must not override or reorder it.\n \n-Conversation state is not durable truth. If a decision matters, put it in GitHub, `.shiki/`, `CONTEXT.md`, or an ADR.\n+Conversation state is not durable truth. If a decision matters, put it in GitHub, `.shiki/`, `CONTEXT.md`, a Shiki SADR, or a target ADR.\n \n If GitHub and `.shiki/` disagree, surface the conflict and prefer GitHub operational state until the mirror is repaired.\n \n@@ -75,8 +79,8 @@\n Every non-trivial change follows this loop:\n \n 1. **Goal**: clarify outcome, completion conditions, non-goals, risk level, and success signals.\n-2. **grill-with-docs**: challenge the plan against domain docs, ADRs, code reality, terminology, and edge scenarios.\n-3. **Context & Impact**: find relevant docs, ADRs, code, dependencies, owners, locks, and verification surfaces.\n+2. **grill-with-docs**: challenge the plan against domain docs, Shiki SADRs, target ADRs, code reality, terminology, and edge scenarios.\n+3. **Context & Impact**: find relevant docs, SADRs, target ADRs, code, dependencies, owners, locks, and verification surfaces.\n 4. **PRD and Spec Freeze**: use `to-prd` when context is settled enough to become durable product/engineering intent. The operator's explicit approval of the PRD is Spec Freeze: the plan records a `spec_freeze` block, and no plan runs without it. Steps 1-4 form Requirements Definition \u2014 one continuous operator dialogue. After Spec Freeze, scope changes require an operator-approved Spec Amendment; non-scope-moving interpretations go to the Assumption Log.\n 5. **Issues**: use `to-issues` to create independently grabbable vertical-slice issues. Prefer AFK slices over HITL slices where possible.\n 6. **Triage**: label issues for readiness, risk, runtime, skills, and MergeGate state.\n@@ -167,7 +171,7 @@\n - Testing decisions.\n - Out-of-scope items.\n - Further notes.\n-- Links to relevant domain terms and ADRs.\n+- Links to relevant domain terms, SADRs, and target ADRs.\n \n Every executable task must include:\n \n@@ -193,7 +197,7 @@\n \n Context & Impact must identify:\n \n-- Relevant documents, domain terms, ADRs, and past decisions.\n+- Relevant documents, domain terms, SADRs, target ADRs, and past decisions.\n - Relevant modules, interfaces, seams, callers, owners, and tests.\n - Dependency relationships between tasks.\n - Candidate locks and lock conflicts.\n@@ -210,7 +214,7 @@\n | Skill | Required when |\n | --- | --- |\n | `setup-matt-pocock-skills` | First configuring the repo for skills, or when issue tracker, triage labels, or domain docs are missing. |\n-| `grill-with-docs` | Default for non-trivial Goals before PRD/issues; always required when plans, terminology, boundaries, tradeoffs, or ADR-worthy decisions are ambiguous. |\n+| `grill-with-docs` | Default for non-trivial Goals before PRD/issues; always required when plans, terminology, boundaries, tradeoffs, or decision-record-worthy choices are ambiguous. |\n | `zoom-out` | The agent lacks an architectural map of the relevant code area. |\n | `to-prd` | Settled Goal context must become durable product/engineering intent. |\n | `to-issues` | A Goal, PRD, or plan must become independently grabbable vertical-slice issues. |\n@@ -348,7 +352,7 @@\n - Three or more new files.\n - Any high-risk or critical label.\n \n-When the architecture gate triggers, run `grill-with-docs` or `improve-codebase-architecture` as appropriate and record durable decisions in an ADR when needed.\n+When the architecture gate triggers, run `grill-with-docs` or `improve-codebase-architecture` as appropriate and record durable decisions in a Shiki SADR or target ADR as applicable.\n \n ## Engineering Discipline\n \ndiff --git a/CLAUDE.md b/CLAUDE.md\nindex 98a932e7841a632866fcd53b0bad0e3af3711d3d..98a31399404d6fb044b4fd493f4459ac87f439de\n--- a/CLAUDE.md\n+++ b/CLAUDE.md\n@@ -14,6 +14,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n ## Claude Role\n \n Default to planner, default implementer, reviewer, coordinator, documentation editor, CCA-style completion judge, and final-judgment assistant.\n@@ -57,7 +60,7 @@\n At the start of a Shiki session:\n \n 1. Read `AGENTS.md`.\n-2. Read `CONTEXT.md` and relevant ADRs.\n+2. Read `CONTEXT.md` and relevant Shiki SADRs and target ADRs.\n 3. Read active `.shiki/` Goal/task/ledger state when present.\n 4. Read active distilled rules from `.shiki/memories/` (status `distilled`, `active: true`, not revoked or superseded) for applicable guidance; the same rules are injected deterministically into task handoffs as the `## Distilled Rules` section (\u00a73.5).\n 5. Read the GitHub Issue or PR when the task is GitHub-backed.\n@@ -74,7 +77,7 @@\n - Define the Goal, completion conditions, non-goals, and risk level.\n - Treat `grill-with-docs` as the default planning entry point for non-trivial Goals.\n - Challenge domain terms against `CONTEXT.md`.\n-- Challenge hard-to-reverse tradeoffs against ADRs.\n+- Challenge hard-to-reverse tradeoffs against SADRs and target ADRs.\n - Explore code instead of asking when code can answer the question.\n - Run Context & Impact before implementation planning.\n - Produce a Task DAG, not an unordered checklist.\n@@ -94,7 +97,7 @@\n - For each question, provide your recommended answer.\n - Resolve dependency decisions before downstream design questions.\n - Update `CONTEXT.md` inline when terminology crystallizes.\n-- Offer ADRs only when the decision is hard to reverse, surprising without context, and a real tradeoff.\n+- Offer a SADR or target ADR only when the decision is hard to reverse, surprising without context, and a real tradeoff.\n - Convert unresolved questions into HITL blockers rather than allowing Codex to guess.\n \n ## PRD and Issues Standard\n@@ -103,7 +106,7 @@\n \n - Use the project's domain glossary vocabulary.\n - Avoid volatile file paths unless they are essential.\n-- Include implementation decisions, testing decisions, out-of-scope items, and relevant ADR/domain links.\n+- Include implementation decisions, testing decisions, out-of-scope items, and relevant SADR/target-ADR/domain links.\n - Identify deep-module opportunities and testable interfaces.\n \n When creating issues:\n@@ -121,7 +124,7 @@\n - Branch/worktree target.\n - Scope and non-goals.\n - Dependencies and locks.\n-- Relevant docs, ADRs, modules, and tests.\n+- Relevant docs, SADRs, target ADRs, modules, and tests.\n - Required skill invocations.\n - TDD expectations.\n - Pre-PR `code-review` gate expectations.\n@@ -152,7 +155,7 @@\n \n - Treat yourself as a judge, not implementer.\n - Do not write production code.\n-- Read `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant ADRs, PRD, task issue, PR body, diff, checks, reviews, labels, and `.shiki/` state.\n+- Read `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant SADRs and target ADRs, PRD, task issue, PR body, diff, checks, reviews, labels, and `.shiki/` state.\n - Evaluate every applicable checklist in `docs/agents/checklists.md`.\n - Map each acceptance criterion to durable evidence.\n - Distinguish implementation failure from missing evidence.\ndiff --git a/CONTEXT.md b/CONTEXT.md\nindex e61a9be06a0cce0d026484083a657bde2067e234..237ee8aa3ea8fab334ece49768bc5ed3b5af0974\n--- a/CONTEXT.md\n+++ b/CONTEXT.md\n@@ -25,7 +25,7 @@\n _Avoid_: jumping directly into code\n \n **grill-with-docs**:\n-A planning skill that challenges a plan against domain language, ADRs, code reality, and concrete edge scenarios. It resolves design-tree questions before PRD/issues. It runs inside Requirements Definition.\n+A planning skill that challenges a plan against domain language, Shiki SADRs, target ADRs, code reality, and concrete edge scenarios. It resolves design-tree questions before PRD/issues. It runs inside Requirements Definition.\n _Avoid_: generic brainstorming, silent assumptions\n \n **Requirements Definition**:\ndiff --git a/CONTRIBUTING.md b/CONTRIBUTING.md\nindex c6d9b656e73abb8a55e5f660123e03153dcb3e6d..9e37a8fbf668db5b22461aaae5548b8f1354394c\n--- a/CONTRIBUTING.md\n+++ b/CONTRIBUTING.md\n@@ -17,7 +17,7 @@\n 1. **Goal** \u2014 start from a user-approved Goal with an outcome, completion\n    conditions, non-goals, risk level, and success signals.\n 2. **grill-with-docs** \u2014 for any non-trivial change, challenge the plan against\n-   domain language, ADRs, and code reality before writing a PRD.\n+   domain language, Shiki SADRs, target ADRs, and code reality before writing a PRD.\n 3. **PRD** \u2014 capture settled product/engineering intent (`to-prd`).\n 4. **Spec Freeze** \u2014 the operator explicitly approves the PRD/requirements;\n    the plan records a `spec_freeze` block and no plan runs without it\n@@ -94,7 +94,7 @@\n   entries.\n - Do not hand-edit `.shiki/goals/*.json` or `.shiki/manifest.json` in a task PR.\n - Conversation state is not durable truth. If a decision matters, record it in\n-  GitHub, `.shiki/`, `CONTEXT.md`, or an ADR.\n+  GitHub, `.shiki/`, `CONTEXT.md`, a Shiki SADR, or a target ADR.\n \n ## Architecture Gate\n \n@@ -103,7 +103,7 @@\n or CI policy, package structure, three or more structural files, more than 100\n net new lines in a core module, three or more new files, or any high-risk label.\n When triggered, run `grill-with-docs` or `improve-codebase-architecture` and\n-record durable decisions in an ADR when needed (see `docs/adr/`).\n+record durable decisions in a Shiki SADR or target ADR as applicable (see `docs/adr/`).\n \n ## Safety\n \ndiff --git a/SYSTEM_PROMPT.md b/SYSTEM_PROMPT.md\nindex aaaea45e3a896c3a44ba89eec8bb84325ecf6c62..da009c86a88285269dee0155e58e550c6d744ea9\n--- a/SYSTEM_PROMPT.md\n+++ b/SYSTEM_PROMPT.md\n@@ -30,6 +30,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n Use the canonical source-of-truth order above. Runtime-specific reading orders\n are operational guidance only and must not override or reorder it. The current\n conversation is non-durable operator input.\n@@ -48,14 +51,14 @@\n \n A runtime may only do work that is explicitly assigned by the Goal, task, PR, or MergeGate decision.\n \n-Do not assume `openai/codex-action`, `OPENAI_API_KEY`, or API-key based Codex automation in the default Shiki path. API-key runners are explicit target-repo extensions and require their own ADR.\n+Do not assume `openai/codex-action`, `OPENAI_API_KEY`, or API-key based Codex automation in the default Shiki path. API-key runners are explicit target-repo extensions and require their own target ADR.\n \n ## 4. Non-negotiable Shiki flow\n \n For any non-trivial Goal, use this flow:\n \n 1. **Goal Seek**: define outcome, non-goals, risk level, completion criteria, and evidence requirements.\n-2. **grill-with-docs**: challenge the plan against `CONTEXT.md`, ADRs, code reality, terminology, boundaries, and edge scenarios. Ask one question at a time when the operator is available. If code can answer a question, inspect code instead of asking.\n+2. **grill-with-docs**: challenge the plan against `CONTEXT.md`, Shiki SADRs, target ADRs, code reality, terminology, boundaries, and edge scenarios. Ask one question at a time when the operator is available. If code can answer a question, inspect code instead of asking.\n 3. **Context & Impact**: identify modules, interfaces, seams, callers, dependencies, locks, risk, and verification surfaces.\n 4. **to-prd + Spec Freeze**: turn settled context into a PRD using domain vocabulary and testing decisions. The operator's explicit approval of the PRD is Spec Freeze, recorded as the plan's `spec_freeze` block; plans without it do not run. Steps 1-4 form Requirements Definition.\n 5. **to-issues**: decompose the PRD into independently grabbable vertical-slice issues. Prefer AFK slices; mark HITL slices when judgment is still required.\n@@ -75,7 +78,7 @@\n Before making any material change:\n \n 1. Read `AGENTS.md` and any runtime-specific wrapper such as `CLAUDE.md`.\n-2. Read `CONTEXT.md` and relevant ADRs.\n+2. Read `CONTEXT.md` and relevant SADRs and target ADRs.\n 3. Read the GitHub Issue / PR and `.shiki/` state for the active Goal or task.\n 4. Identify your role: Planner, Implementer, Reviewer, Completion Check Agent, Repairer, or Guardian-assist.\n 5. Identify Goal id, task id, branch, PR, dependencies, locks, risk level, required skills, and required evidence.\n@@ -103,7 +106,7 @@\n \n At minimum, identify:\n \n-- Relevant documents, ADRs, domain terms, and prior decisions.\n+- Relevant documents, SADRs, target ADRs, domain terms, and prior decisions.\n - Relevant modules, interfaces, seams, callers, owners, and tests.\n - Dependency relationships between tasks.\n - Candidate file locks and lock conflicts.\n@@ -152,7 +155,7 @@\n \n CCA must read:\n \n-- `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant ADRs.\n+- `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant SADRs and target ADRs.\n - Parent Goal issue, PRD issue, task issue, comments, and labels.\n - PR body, diff, commits, changed files, reviews, CI checks, and artifacts available to the workflow.\n - `.shiki/` task contract, locks, ledger entries, CCA checklist profile, and prior repair packets.\n@@ -214,7 +217,7 @@\n - Three or more new files.\n - Any high-risk or critical label.\n \n-When the architecture gate triggers, run `grill-with-docs` or `improve-codebase-architecture` as appropriate and record the decision in an ADR if it is hard to reverse or likely to be re-litigated.\n+When the architecture gate triggers, run `grill-with-docs` or `improve-codebase-architecture` as appropriate and record the decision in a Shiki SADR or target ADR as applicable if it is hard to reverse or likely to be re-litigated.\n \n ## 14. Skill Gate\n \n@@ -223,7 +226,7 @@\n Use these skills:\n \n - `setup-matt-pocock-skills`: before first use of the engineering skill set, or when issue tracker, triage labels, or domain docs are missing.\n-- `grill-with-docs`: default for non-trivial Goals before PRD/issues; required for ambiguous plans, terminology, boundaries, ADR-worthy tradeoffs, or design-tree decisions.\n+- `grill-with-docs`: default for non-trivial Goals before PRD/issues; required for ambiguous plans, terminology, boundaries, decision-record-worthy tradeoffs, or design-tree decisions.\n - `zoom-out`: unfamiliar code area or missing architectural map.\n - `to-prd`: convert settled context into a PRD.\n - `to-issues`: break a PRD, plan, or Goal into independently grabbable vertical-slice issues.\ndiff --git a/docs/adr/0001-keep-shiki-as-platform-name.md b/docs/adr/SADR-0001-keep-shiki-as-platform-name.md\nindex 20a9637c3b9d8c97de7ba8db7cfae4d82d9a7dba..8b685439959aada1b221c112f0ac3c51ca8a1695\n--- a/docs/adr/0001-keep-shiki-as-platform-name.md\n+++ b/docs/adr/SADR-0001-keep-shiki-as-platform-name.md\n@@ -1,3 +1,3 @@\n-# Keep Shiki As The Platform Name\n+# SADR-0001: Keep Shiki As The Platform Name\n \n Shiki remains the product name for the GitHub-native agentic engineering control plane. We considered more descriptive names such as MergePilot, MergeGate, and Kiroku, but kept Shiki because it carries the existing goal-seeking framework identity while remaining independent of Hermes, Codex, Claude Code, or any single runtime.\ndiff --git a/docs/adr/0002-use-agents-as-shared-constitution.md b/docs/adr/SADR-0002-use-agents-as-shared-constitution.md\nindex 73de30ecb078120eac540d9bc51855e3cd48d0e3..c5486182bf06fca36ea508496b013c67302fd989\n--- a/docs/adr/0002-use-agents-as-shared-constitution.md\n+++ b/docs/adr/SADR-0002-use-agents-as-shared-constitution.md\n@@ -1,3 +1,3 @@\n-# Use AGENTS As The Shared Agent Constitution\n+# SADR-0002: Use AGENTS As The Shared Agent Constitution\n \n Shiki uses `AGENTS.md` as the runtime-neutral constitution and `CLAUDE.md` as a thin Claude Code wrapper. This keeps Codex, Claude Code, Hermes Runner, and future Agent Runtimes aligned on the same GitHub-first MergeGate rules while still allowing runtime-specific instructions where needed.\ndiff --git a/docs/adr/0003-require-skill-gate-for-mergegate-readiness.md b/docs/adr/SADR-0003-require-skill-gate-for-mergegate-readiness.md\nindex 424be0c9af4c8f0136f1fde68a468c47ff84e764..7fc337987c7df9792f75f181dfcbed75fe5ffff2\n--- a/docs/adr/0003-require-skill-gate-for-mergegate-readiness.md\n+++ b/docs/adr/SADR-0003-require-skill-gate-for-mergegate-readiness.md\n@@ -1,3 +1,3 @@\n-# Require Skill Gate For MergeGate Readiness\n+# SADR-0003: Require Skill Gate For MergeGate Readiness\n \n Shiki requires relevant engineering skills to be invoked before execution when their triggers apply. This makes skills part of MergeGate readiness rather than optional prompt style, because Shiki depends on repeatable planning, issue decomposition, TDD, diagnosis, triage, and architecture review before autonomous or parallel agent work can be trusted.\ndiff --git a/docs/adr/0004-use-github-first-with-shiki-mirror.md b/docs/adr/SADR-0004-use-github-first-with-shiki-mirror.md\nindex d0a3a9174dc646a945a9d325954ffaf00c352040..a79c54ae9ee8eed2a0524192b864da4e22a92d3f\n--- a/docs/adr/0004-use-github-first-with-shiki-mirror.md\n+++ b/docs/adr/SADR-0004-use-github-first-with-shiki-mirror.md\n@@ -1,4 +1,4 @@\n-# ADR 0004: Use GitHub First With A Repository-Local Shiki Mirror\n+# SADR-0004: Use GitHub First With A Repository-Local Shiki Mirror\n \n ## Status\n \ndiff --git a/docs/adr/0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md b/docs/adr/SADR-0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\nindex b339baf49b89d9290e79c3d51b36e7b2bde3bd95..0f3c3eb9afccdb3403ce978e21d04fe12e495da0\n--- a/docs/adr/0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\n+++ b/docs/adr/SADR-0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\n@@ -1,8 +1,8 @@\n-# ADR 0005: Use Codex As Front And Claude Code Action As The GitHub Runtime\n+# SADR-0005: Use Codex As Front And Claude Code Action As The GitHub Runtime\n \n ## Status\n \n-Accepted (implementer default superseded by ADR 0008; the auth model and the\n+Accepted (implementer default superseded by SADR-0008; the auth model and the\n Claude Code Action GitHub runtime decision remain in force)\n \n ## Context\n@@ -26,4 +26,4 @@\n - Shiki avoids requiring `OPENAI_API_KEY` for the default workflow.\n - Codex work happens through the user's authenticated Codex front surface, not as a hidden backend Action.\n - Claude Code Action is configured for OAuth-token based subscription usage by default.\n-- API-key based automation remains possible only as an explicit target-repo extension with its own ADR.\n+- API-key based automation remains possible only as an explicit target-repo extension with its own target ADR.\ndiff --git a/docs/adr/0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md b/docs/adr/SADR-0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\nindex 4bcae9d1e52f0a37b8e9fbafa5cd14b6ee1993bd..7a38538a6ae9929567f0ade67e7ea6a60d0b3f7e\n--- a/docs/adr/0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\n+++ b/docs/adr/SADR-0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\n@@ -1,4 +1,4 @@\n-# ADR 0006: Separate Codex Implementation, CCA Completion, And MergeGate Authorization\n+# SADR-0006: Separate Codex Implementation, CCA Completion, And MergeGate Authorization\n \n ## Status\n \ndiff --git a/docs/adr/0007-packaging-and-release.md b/docs/adr/SADR-0007-packaging-and-release.md\nindex 79b84ddb97167372b44641e5b7860e400ce80822..bb445a2e2e856d9d1f5fdbe35de8d4e8cec0fb32\n--- a/docs/adr/0007-packaging-and-release.md\n+++ b/docs/adr/SADR-0007-packaging-and-release.md\n@@ -1,4 +1,4 @@\n-# ADR 0007: Packaging And Release For Shiki\n+# SADR-0007: Packaging And Release For Shiki\n \n ## Status\n \n@@ -25,7 +25,7 @@\n   not introduce third-party runtime dependencies or break the shim.\n - Releases must run inside the operator's subscription-authenticated toolchain\n   and must not require extra secrets beyond the built-in `GITHUB_TOKEN`\n-  (consistent with ADR 0005's no-API-key-by-default stance).\n+  (consistent with SADR-0005's no-API-key-by-default stance).\n - `.shiki/manifest.json` already encodes the install surface (`install.include`,\n   `install.create_directories`, `install.exclude_from_commit`), and it must not\n   be mutated as part of this change.\n@@ -34,7 +34,7 @@\n \n This is a hard-to-reverse platform decision (it sets the version source of\n truth, the release trigger, and the public install contract), so it is recorded\n-as an ADR.\n+as a SADR.\n \n ## Decision\n \n@@ -104,7 +104,7 @@\n 3. **Publishing to PyPI.** Rejected for now: Shiki is distributed as a\n    repository template + git-installable CLI, not a PyPI library; PyPI would add\n    credentials/secrets and a different versioning contract. Can be added later as\n-   its own ADR if a packaged distribution is needed.\n+   its own SADR if a packaged distribution is needed.\n 4. **Dynamic version (read `VERSION` via a build backend `dynamic` field).**\n    Rejected to keep `pyproject.toml` backend-agnostic and dependency-free; the\n    small duplication between `VERSION` and `pyproject.toml` is guarded by the\n@@ -123,4 +123,4 @@\n   releasing versions the whole platform but ships only `install.include` to\n   targets.\n - Future PyPI distribution or a console entry point are deferred and would each\n-  warrant their own ADR.\n+  warrant their own SADR.\ndiff --git a/docs/adr/0008-use-claude-code-as-default-implementer.md b/docs/adr/SADR-0008-use-claude-code-as-default-implementer.md\nindex dedf4510b7c7799521422f622029563d373db031..35c2625d908167fbc58855b5b63e283805f167e8\n--- a/docs/adr/0008-use-claude-code-as-default-implementer.md\n+++ b/docs/adr/SADR-0008-use-claude-code-as-default-implementer.md\n@@ -1,12 +1,12 @@\n-# ADR 0008: Use Claude Code As The Default Implementer Runtime\n+# SADR-0008: Use Claude Code As The Default Implementer Runtime\n \n ## Status\n \n-Proposed (supersedes ADR 0005 for the implementer default; keeps its auth model)\n+Proposed (supersedes SADR-0005 for the implementer default; keeps its auth model)\n \n ## Context\n \n-ADR 0005 made Codex Front the default implementation runtime, with Claude Code\n+SADR-0005 made Codex Front the default implementation runtime, with Claude Code\n restricted to planner and reviewer roles by the runtime registry, and the only\n autonomous implementation adapter was `shiki runner codex`.\n \n@@ -18,13 +18,13 @@\n - The platform's planning, review, and CCA layers already run on Claude Code,\n   and Claude Code's native capabilities (the built-in `/code-review` skill and\n   the Workflow multi-agent orchestration tool) have no Codex equivalent.\n-- The Goal lifecycle redesign (ADR 0009) requires an autonomous post-freeze\n+- The Goal lifecycle redesign (SADR-0009) requires an autonomous post-freeze\n   loop driven from the operator's Claude Code session, including bounded\n   repair dispatch, which must not depend on a runtime that cannot be invoked\n   unattended.\n \n The constitution claims Shiki is runtime-agnostic (\"not a Claude-only\n-workflow\", ADR 0002). Any change to the implementer default must preserve the\n+workflow\", SADR-0002). Any change to the implementer default must preserve the\n registry-based runtime model rather than hardcoding one vendor.\n \n ## Decision\n@@ -39,7 +39,7 @@\n   the task to review or repair-needed.\n - Keep Codex registered as an optional implementer runtime. `shiki runner\n   codex`, the Codex handoff templates, and the ChatGPT OAuth auth model from\n-  ADR 0005 remain supported for tasks explicitly assigned to `codex`.\n+  SADR-0005 remain supported for tasks explicitly assigned to `codex`.\n - Commit to mandating two Claude-native capabilities as loop contract in\n   follow-up Goals (status: the code-review gate was delivered by Goal G-B \u2014\n   skill registry, checklists item PR-12, default required_skills \u2014 while the\n@@ -60,7 +60,7 @@\n \n Out of scope: removing Codex support, changing the CCA runtime (Claude Code\n Action with `CLAUDE_CODE_OAUTH_TOKEN` stays), and API-key based automation\n-(still requires its own ADR per ADR 0005).\n+(still requires its own target ADR per SADR-0005).\n \n ## Consequences\n \n@@ -100,7 +100,7 @@\n   harness rejection of direct `codex exec` is outside Shiki's control, and the\n   plan's mandatory native capabilities cannot run on Codex at all.\n - **Remove Codex entirely**: rejected; it abandons the runtime-agnostic\n-  constitution (ADR 0002) for no operational gain and forecloses\n+  constitution (SADR-0002) for no operational gain and forecloses\n   cross-runtime adversarial review.\n - **GitHub Actions cloud implementation via claude-code-action**: rejected as\n   default; durable evidence is automatic but token cost, Node 24 workflow\ndiff --git a/docs/adr/0009-requirements-definition-and-spec-freeze.md b/docs/adr/SADR-0009-requirements-definition-and-spec-freeze.md\nindex 29b3f7871bef9203a8b4a831a65c39eb2f506e42..52e7dd69ad0aa4efe0b68c62678abbf7df02aaec\n--- a/docs/adr/0009-requirements-definition-and-spec-freeze.md\n+++ b/docs/adr/SADR-0009-requirements-definition-and-spec-freeze.md\n@@ -1,4 +1,4 @@\n-# ADR 0009: Merge Grill And Planning Into Requirements Definition With Spec Freeze\n+# SADR-0009: Merge Grill And Planning Into Requirements Definition With Spec Freeze\n \n ## Status\n \ndiff --git a/docs/adr/0010-external-ai-guardian-review.md b/docs/adr/SADR-0010-external-ai-guardian-review.md\nindex e6108bbe8bb4e73e0ff4684c8f78001a80438c4e..f3510b145664298adafb151b6f0db51b972f01ec\n--- a/docs/adr/0010-external-ai-guardian-review.md\n+++ b/docs/adr/SADR-0010-external-ai-guardian-review.md\n@@ -1,4 +1,4 @@\n-# ADR 0010: External AI Guardian Review As A First-Class Approval Authority\n+# SADR-0010: External AI Guardian Review As A First-Class Approval Authority\n \n ## Status\n \n@@ -8,7 +8,7 @@\n \n Shiki is intended to be an autonomous, recursive, self-improving agent\n framework: authority-in-the-loop, not human-in-the-loop. Until now the Guardian\n-approval required for high/critical-risk merges (guardian-policy.json, ADR\n+approval required for high/critical-risk merges (guardian-policy.json, SADR\n governance) recognized only human authority sources \u2014 a GitHub review approval,\n the `guardian:approved` label applied by a configured Guardian, or a comment\n carrying `Guardian approval granted` plus the current head SHA.\n@@ -89,7 +89,7 @@\n   autonomous design the operator intends and produced the impersonation\n   incident.\n - **Record AI approval as an operator comment**: rejected as forgery of human\n-  authority \u2014 the exact failure this ADR corrects.\n+  authority \u2014 the exact failure this SADR corrects.\n - **Deliver the artifact as a committed `.shiki/` file**: rejected; a committed\n   approval inside the PR being approved is forgeable by anyone who can push.\n - **A dedicated reviewer bot identity instead of a relayed comment**: deferred;\ndiff --git a/docs/adr/0011-autonomous-quality-gate-evidence.md b/docs/adr/SADR-0011-autonomous-quality-gate-evidence.md\nindex bca0246c5d46e44afbde15486a75eec425fc02f5..9c93eac16d8c3a6d4b11bd7224e3955102cd7db4\n--- a/docs/adr/0011-autonomous-quality-gate-evidence.md\n+++ b/docs/adr/SADR-0011-autonomous-quality-gate-evidence.md\n@@ -1,4 +1,4 @@\n-# ADR 0011: Autonomous Quality-Gate Evidence \u2014 Deterministic Facts And An Independent Verifier\n+# SADR-0011: Autonomous Quality-Gate Evidence \u2014 Deterministic Facts And An Independent Verifier\n \n ## Status\n \ndiff --git a/docs/adr/0012-autonomous-loop-closeout-to-main.md b/docs/adr/SADR-0012-autonomous-loop-closeout-to-main.md\nindex 637fad6dde4989c9c2bf0f81f676d8d29991123c..ec7d85f81030a676bcbba28b43eb365422a36eac\n--- a/docs/adr/0012-autonomous-loop-closeout-to-main.md\n+++ b/docs/adr/SADR-0012-autonomous-loop-closeout-to-main.md\n@@ -1,4 +1,4 @@\n-# 0012 \u2014 Autonomous loop pushes goal completion to main via a closeout PR\n+# SADR-0012 \u2014 Autonomous loop pushes goal completion to main via a closeout PR\n \n - Status: accepted\n - Date: 2026-06-18\ndiff --git a/docs/adr/0013-bootstrap-grants-actions-approve-pull-requests.md b/docs/adr/SADR-0013-bootstrap-grants-actions-approve-pull-requests.md\nindex b6c69d64f1c5ced346b88d6859586715b3b5bd0e..cf48ff4a6716267f539ca58edd1a852e7c7a2d64\n--- a/docs/adr/0013-bootstrap-grants-actions-approve-pull-requests.md\n+++ b/docs/adr/SADR-0013-bootstrap-grants-actions-approve-pull-requests.md\n@@ -1,4 +1,4 @@\n-# ADR 0013: Bootstrap Grants GitHub Actions Permission To Create And Approve Pull Requests\n+# SADR-0013: Bootstrap Grants GitHub Actions Permission To Create And Approve Pull Requests\n \n ## Status\n \n@@ -31,7 +31,7 @@\n security tradeoff: it lets repository automation satisfy a human-review gate. In\n a multi-maintainer repo that could let a compromised or buggy workflow\n self-approve changes. So whether bootstrap turns it on by default \u2014 and with\n-what default token scope \u2014 is an ADR-worthy decision, not an implementation\n+what default token scope \u2014 is a SADR-worthy decision, not an implementation\n detail.\n \n ## Decision\n@@ -48,7 +48,7 @@\n Specifically:\n \n - **`can_approve_pull_request_reviews` defaults to `true`.** Shiki's default\n-  posture is solo/AFK autonomous operation (ADR 0008/0009/0012), where the CCA\n+  posture is solo/AFK autonomous operation (SADR-0008/0009/0012), where the CCA\n   Review Bridge is the only path that can satisfy `required_review: true` after\n   the CCA and Guardian gates have already passed. The default makes a\n   freshly-bootstrapped repo able to run the autonomous loop end to end without a\ndiff --git a/docs/adr/0014-codex-app-external-ai-guardian-ui-adapter.md b/docs/adr/SADR-0014-codex-app-external-ai-guardian-ui-adapter.md\nindex 238db06279334caa341c117362df9271382a7fcd..65f12e9939321e1b3e389a34c822fc1549e5155d\n--- a/docs/adr/0014-codex-app-external-ai-guardian-ui-adapter.md\n+++ b/docs/adr/SADR-0014-codex-app-external-ai-guardian-ui-adapter.md\n@@ -1,4 +1,4 @@\n-# ADR 0014: Use Codex App As The External AI Guardian UI Adapter\n+# SADR-0014: Use Codex App As The External AI Guardian UI Adapter\n \n ## Status\n \ndiff --git a/docs/adr/0015-contract-approval-before-dispatch.md b/docs/adr/SADR-0015-contract-approval-before-dispatch.md\nindex fad6c656741a20a7684ba16080a7db159f63a52f..6061271a46d3f1d6ae7174477de3fb75673123a6\n--- a/docs/adr/0015-contract-approval-before-dispatch.md\n+++ b/docs/adr/SADR-0015-contract-approval-before-dispatch.md\n@@ -1,4 +1,4 @@\n-# ADR 0015: Approve The Task Contract Before Dispatch, Not The Diff After It\n+# SADR-0015: Approve The Task Contract Before Dispatch, Not The Diff After It\n \n ## Status\n \n@@ -9,7 +9,7 @@\n Guardian approval for high/critical risk is expressible only on a pull request.\n Every approval source in `guardian-policy.json` \u2014 GitHub review, the\n `guardian:approved` label, the `Guardian approval granted` comment, and the\n-`external_ai_guardian_review` artifact (ADR 0010/0014) \u2014 is bound to a PR, and\n+`external_ai_guardian_review` artifact (SADR-0010/0014) \u2014 is bound to a PR, and\n three of the four require the current head SHA. Approval therefore cannot exist\n before an implementation PR exists.\n \n@@ -81,7 +81,7 @@\n An envelope breach must surface only on the non-repairable policy gate, never on\n the repairable `MergeGate metadata check`. Otherwise the Goal Loop routes it to\n `dispatch_repair` \u2014 instructing a runner to make the breach go away \u2014 which is\n-exactly what this ADR forbids.\n+exactly what this SADR forbids.\n \n **Contract immutability.** After registration, an implementation PR must not\n change the governance fields of its own task contract (`scope`, `non_goals`,\n@@ -116,7 +116,7 @@\n fails any verdict that is not `complete`, and the signal the CCA prompt reads is\n computed from `evaluate_guardian_approval` alone. Without the OR-merge at the\n signal, a contract-approved PR still returns `needs_guardian`, the CCA job fails,\n-and the MergeGate policy check never runs \u2014 the gate this ADR changes would never\n+and the MergeGate policy check never runs \u2014 the gate this SADR changes would never\n be reached. The CCA *prompt* needs no change: a `contract_approval` entry in the\n signal's existing `sources` array satisfies its current wording.\n \n@@ -130,7 +130,7 @@\n written, and an approved contract lets its implementation PRs merge through the\n same automatic path as low/medium risk. The wasted implement-then-block cycle\n disappears. Guardian review shifts from reading diffs to reading contracts, which\n-is both earlier and smaller; the external AI Guardian path (ADR 0010/0014) works\n+is both earlier and smaller; the external AI Guardian path (SADR-0010/0014) works\n on contracts with no change.\n \n We accept that the Guardian authorizes a class of change without seeing its\n@@ -146,7 +146,7 @@\n adversarial tests for the new approval path equivalent in rigor to\n `test_shiki_governance_evidence.sh`.\n \n-Bootstrap costs, which apply only while this ADR is being implemented. The tasks\n+Bootstrap costs, which apply only while this SADR is being implemented. The tasks\n that build Contract Approval merge under the existing rules, so the ones\n dispatched after the pre-dispatch gate lands but before the approval path lands\n need a Guardian-approved Contract PR *and* a live Guardian approval on their\n@@ -210,7 +210,7 @@\n risk; they do not bound authorized work.\n \n **Auto-escalate risk on envelope breach instead of stopping.** Returns the\n-workflow to a diff-time Guardian decision \u2014 the exact behavior this ADR removes.\n+workflow to a diff-time Guardian decision \u2014 the exact behavior this SADR removes.\n \n **Resolve governance fields from the base snapshot only.** Rejected against the\n registration flow: implementation PRs add their own contract, so the base has no\ndiff --git a/docs/adr/0016-locks-bound-product-paths.md b/docs/adr/SADR-0016-locks-bound-product-paths.md\nindex b5e850f3f1f7f4ef6e7f9d1763a29a51bd300c45..46b40a9b99a7aae644c02bb458f5473ff0a90ce2\n--- a/docs/adr/0016-locks-bound-product-paths.md\n+++ b/docs/adr/SADR-0016-locks-bound-product-paths.md\n@@ -1,4 +1,4 @@\n-# ADR 0016: Locks bound product paths; the Shiki mirror is bound by record identity\n+# SADR-0016: Locks bound product paths; the Shiki mirror is bound by record identity\n \n ## Status\n \n@@ -6,8 +6,8 @@\n \n - Date: 2026-07-28\n - Deciders: operator (mizutani-140), Claude (planner)\n-- Related: ADR 0004 (GitHub-first with a `.shiki` mirror), ADR 0009 (Spec Freeze),\n-  ADR 0012 (autonomous loop closeout to main), ADR 0015 (Contract Approval before\n+- Related: SADR-0004 (GitHub-first with a `.shiki` mirror), SADR-0009 (Spec Freeze),\n+  SADR-0012 (autonomous loop closeout to main), SADR-0015 (Contract Approval before\n   dispatch), remediation goal task R-02, the live PR #179 deadlock.\n \n ## Context\n@@ -90,11 +90,11 @@\n files, documentation files and named source modules are nameable; runtime-\n allocated ledger, runner and report ids are not \u2014 and those live in `.shiki`,\n which locks no longer govern. Existing frozen contracts are brought into line by\n-a one-time Spec Amendment per plan, which must land before ADR 0015's contract\n+a one-time Spec Amendment per plan, which must land before SADR-0015's contract\n immutability makes `locks` unwritable from the implementation PR.\n \n **Lock state is resolved from the durable side.** MergeGate resolves the lock\n-registry, and ADR 0015's governance fields, from the base `.shiki` snapshot when\n+registry, and SADR-0015's governance fields, from the base `.shiki` snapshot when\n present, falling back to head only when absent. The `MergeGate metadata check`\n job is given `--base-shiki`, which it does not receive today. Locks are not\n derived from the live open-pull-request set: a pull request body is untrusted\n@@ -108,7 +108,7 @@\n Explicitly out of scope: moving MergeGate's contract resolution off the PR\n checkout wholesale; making the mirror a projection of GitHub state; introducing\n new lock kinds; moving ledger evidence out of the repository; and unwinding\n-ADR 0012's closeout pull request. Registration of multi-task goals is ADR 0015's\n+SADR-0012's closeout pull request. Registration of multi-task goals is SADR-0015's\n Contract PR and is not re-decided here.\n \n ## Consequences\n@@ -127,7 +127,7 @@\n \n Risks accepted. First, goal-scoped identity is only as strong as `goal_id`,\n which is resolved from the PR body and the head task file; the mitigation is to\n-add `goal_id` to ADR 0015's frozen governance field set, and until that lands a\n+add `goal_id` to SADR-0015's frozen governance field set, and until that lands a\n task file can name a foreign goal. Second, base-when-present resolution is\n unavailable on fork pull requests, where the head is contributor-controlled;\n fork pull requests already cannot complete a Shiki merge, and this decision does\n@@ -139,7 +139,7 @@\n path, not by implementation pull requests.\n \n New obligations: one Spec Amendment per frozen plan to rewrite `locks`, ordered\n-before ADR 0015 T1; extension of the adversarial suites in\n+before SADR-0015 T1; extension of the adversarial suites in\n `scripts/test_shiki_adversarial_state.sh` and\n `scripts/test_shiki_governance_evidence.sh` to cover the eleven newly-scoped\n directories; and updating `scripts/test_shiki_loop_lock_guard.sh`,\n@@ -151,32 +151,32 @@\n whether the loop's transitive sync of ledger `evidence` references can be\n deleted once the Contract PR carries its own source plan.\n \n-## Relationship to ADR 0004\n+## Relationship to SADR-0004\n \n-This ADR **clarifies something ADR 0004 left unspecified**. ADR 0004 decides that\n+This SADR **clarifies something SADR-0004 left unspecified**. SADR-0004 decides that\n GitHub is the operational source of truth and that each target repository keeps a\n `.shiki/` mirror. It says nothing about pull requests writing the mirror, does\n not name MergeGate, and does not make the mirror an input to a gate. Nothing in\n-ADR 0004's Decision or Consequences is amended, superseded or weakened: the\n+SADR-0004's Decision or Consequences is amended, superseded or weakened: the\n mirror still exists, still records the same artifacts, and pull request\n-boundaries remain the coordination surface. What this ADR settles is the question\n+boundaries remain the coordination surface. What this SADR settles is the question\n 0004 never reached \u2014 by what authority a pull request may write a mirror record \u2014\n and it answers it the way `.shiki/manifest.json` already declares\n (`pr_mutation: current-task-or-current-goal-only`).\n \n-It **amends ADR 0012**, whose Decision item 5 states that \"Loop-executed tasks\n+It **amends SADR-0012**, whose Decision item 5 states that \"Loop-executed tasks\n declare the `path:.shiki/**` lock, which covers every `.shiki` file the closeout\n stages ... so `files_outside_locks` is satisfied without per-file lock\n bookkeeping.\" That clause is replaced: the closeout's mirror writes are\n-authorized by record identity, not by a declared lock. ADR 0012's substantive\n+authorized by record identity, not by a declared lock. SADR-0012's substantive\n decision \u2014 that completion reaches `main` through a closeout pull request rather\n than local attestation \u2014 is preserved unchanged.\n \n-It is **complementary to ADR 0015**, not a substitute. ADR 0015 owns contract\n+It is **complementary to SADR-0015**, not a substitute. SADR-0015 owns contract\n immutability, the pre-dispatch gate, the Contract PR and the Scope Envelope. This\n-ADR owns what a lock means and who may write the mirror. The one ordering\n+SADR owns what a lock means and who may write the mirror. The one ordering\n constraint between them is that the lock-narrowing Spec Amendment must precede\n-ADR 0015 T1's freezing of the `locks` field.\n+SADR-0015 T1's freezing of the `locks` field.\n \n ## Alternatives Considered\n \n@@ -186,7 +186,7 @@\n the only writer that can register them may run. Migrating the Guardian signal to\n that store makes every registration PR fail closed to `needs_guardian` at every\n risk level, because the task file is by definition absent from the store, which\n-removes the low/medium autonomous path ADR 0008/0009 exists to provide. And it\n+removes the low/medium autonomous path SADR-0008/0009 exists to provide. And it\n leaves schemas, manifest, config, guardian policy, templates, migrations, plans,\n worktrees, runner records, runs and handoffs with no writer at all in a\n repository whose product is `.shiki`. Measured benefit on the frozen backlog:\n@@ -199,14 +199,14 @@\n namespace is not in fact ownership-partitioned \u2014 56 ledger entries carry no\n `task_id`, and one goal DAG is writable by 35 registered tasks. The correct\n expression of its insight is that locks do not govern the mirror at all, which is\n-what this ADR decides.\n+what this SADR decides.\n \n **Make the mirror a projection written only by a registration writer and a\n post-merge projection job.** Rejected. It requires a bypass actor with write\n access to the protected default branch; it makes its own master invariant\n readable from a manifest that pull requests may edit; it deletes both reconcile\n modes and leaves a corrupted mirror with no in-band repair path, contradicting\n-ADR 0004's consequence that mirror conflicts are repaired against GitHub state;\n+SADR-0004's consequence that mirror conflicts are repaired against GitHub state;\n and its content-derived ids are rejected by `validate_shiki`'s currently-green id\n pattern.\n \ndiff --git a/docs/adr/0017-bookkeeping-closeouts-do-not-inherit-risk.md b/docs/adr/SADR-0017-bookkeeping-closeouts-do-not-inherit-risk.md\nindex 554e3a0d526a72ca067108c5464cc68c3edcf174..b9676857a4713850e063af7c39c8e0b01ecb4c66\n--- a/docs/adr/0017-bookkeeping-closeouts-do-not-inherit-risk.md\n+++ b/docs/adr/SADR-0017-bookkeeping-closeouts-do-not-inherit-risk.md\n@@ -1,4 +1,4 @@\n-# ADR 0017: A Bookkeeping Closeout Does Not Inherit Its Task's Risk\n+# SADR-0017: A Bookkeeping Closeout Does Not Inherit Its Task's Risk\n \n ## Status\n \n@@ -6,7 +6,7 @@\n \n ## Context\n \n-ADR 0012 made the goal loop push a task's completion to the default branch through a\n+SADR-0012 made the goal loop push a task's completion to the default branch through a\n closeout pull request: after the implementation PR merges, the loop opens a second PR\n carrying `task.status = done`, `lock.state = released`, and \u2014 when the task completes\n its goal \u2014 `goal.status = complete` plus one scorecard report. Nothing else. The\n@@ -49,14 +49,14 @@\n 4. The task file's governance fields (`scope`, `non_goals`, `required_skills`,\n    `risk_level`, `locks`, `acceptance_checks`, `test_command`) are byte-identical to\n    the base snapshot. Only `status`, `expected_pr`, `closeout_pr`, `ledger_evidence`\n-   and `expected_branch` may differ \u2014 the ADR 0015 contract-immutability rule,\n+   and `expected_branch` may differ \u2014 the SADR-0015 contract-immutability rule,\n    unchanged.\n \n    `expected_branch` is in that set because a closeout is *by construction* on a\n    different branch from the implementation it closes, and the loop rewrites the\n    field accordingly. It is safe to let it differ because MergeGate independently\n    requires `expected_branch` to equal the PR's actual head ref, so a PR cannot set\n-   it to anything other than the branch it is really on. The first draft of this ADR\n+   it to anything other than the branch it is really on. The first draft of this SADR\n    omitted it, which made the exemption unreachable: every real closeout failed\n    condition 4 on a field the loop always changes. See PR #209.\n 5. The state transitions are exactly the terminal set: task `review` \u2192 `done`, lock\n@@ -113,12 +113,12 @@\n classification must be derived, not asserted.\n \n **Fold the closeout into the implementation PR** so no second PR exists. Rejected under\n-ADR 0012, which recorded why: a task file that is already `done` on the implementation\n+SADR-0012, which recorded why: a task file that is already `done` on the implementation\n branch short-circuits the loop's review \u2192 CCA \u2192 merge \u2192 done ordering into a local\n goal-completion before any merge happens.\n \n **Have the loop auto-merge closeouts with elevated credentials.** Rejected outright: that\n-is a runtime authoring its own approval, which ADR 0010 and the constitution's\n+is a runtime authoring its own approval, which SADR-0010 and the constitution's\n approval-record prohibition forbid. It would also be indistinguishable, in the evidence,\n from the forgery those rules exist to prevent.\n \ndiff --git a/docs/adr/0018-carried-guardian-approval-across-a-proven-base-sync.md b/docs/adr/SADR-0018-carried-guardian-approval-across-a-proven-base-sync.md\nindex 22358562da390ed8b45cd600da08a43d378b4680..e6e2907cefa776460af73ddf7d39db605f3c39b0\n--- a/docs/adr/0018-carried-guardian-approval-across-a-proven-base-sync.md\n+++ b/docs/adr/SADR-0018-carried-guardian-approval-across-a-proven-base-sync.md\n@@ -1,4 +1,4 @@\n-# ADR 0018: Carried Guardian Approval Across A Proven Base Sync\n+# SADR-0018: Carried Guardian Approval Across A Proven Base Sync\n \n ## Status\n \n@@ -34,7 +34,7 @@\n Guardian evaluator that consumes it (`shiki_guardian.evaluate_guardian_approval`,\n the `guardian_comment_carried` source) were built and tested in isolation. Neither\n does anything until a caller passes the flags that turn the carry on, so the\n-feature was invisible in CI. This ADR records the decision the two of them\n+feature was invisible in CI. This SADR records the decision the two of them\n implement, and this task wires it into the four CI invocations that judge Guardian\n approval.\n \ndiff --git a/docs/adr/README.md b/docs/adr/README.md\nindex c70e057a4520c36e042ab68c53cd4a21ef709b71..5da43856836e2ce6a150f8127766e2f88355ab4b\n--- a/docs/adr/README.md\n+++ b/docs/adr/README.md\n@@ -1,40 +1,41 @@\n-# Architecture Decision Records\n+# Decision Record Namespaces\n \n-This directory holds Shiki's Architecture Decision Records (ADRs): durable\n-records of hard-to-reverse platform decisions. ADRs are part of the Shiki source\n-of truth alongside GitHub state, the `.shiki/` mirror, and `CONTEXT.md`.\n+This directory holds two independent decision namespaces:\n \n-## How ADRs Work\n+- Shiki Architecture Decision Records (SADRs), named\n+  `SADR-NNNN-<short-slug>.md`, record hard-to-reverse Shiki platform decisions.\n+- Target Architecture Decision Records (ADRs), named\n+  `NNNN-<short-slug>.md`, belong to the target product.\n \n-- New ADRs are added as `docs/adr/NNNN-<short-slug>.md`, where `NNNN` is the next\n-  zero-padded sequence number.\n-- The **canonical list of ADRs is the set of files matching `docs/adr/*.md`** in\n-  this directory. Browse `docs/adr/*.md` for the authoritative, current set.\n-- Use [`template.md`](template.md) as the starting point for a new ADR.\n-- Record an ADR when a decision is hard to reverse, surprising without context,\n-  and a real tradeoff. Do not write an ADR for routine or easily reversible\n-  choices.\n+The same four-digit number may appear once in each namespace. A platform\n+`SADR-NNNN` citation resolves only to `SADR-NNNN-*.md`; a target `ADR NNNN`\n+citation resolves only to `NNNN-*.md`.\n \n-Using the `docs/adr/*.md` glob as the source of truth means other PRs can add an\n-ADR without editing this index file, avoiding merge contention on a\n-hand-maintained enumeration.\n+## How Decision Records Work\n \n-## Current ADRs (snapshot)\n-\n-The list below is a convenience snapshot and may lag behind the directory. When\n-in doubt, list `docs/adr/*.md`.\n+- New Shiki platform decisions use the next zero-padded SADR number.\n+- New target product decisions use the target's own ADR sequence.\n+- The canonical list is the files in `docs/adr/`; no hand-maintained index is\n+  authoritative.\n+- Use [`template.md`](template.md) as the starting point and choose the filename\n+  namespace that owns the decision.\n+- Record a decision when it is hard to reverse, surprising without context, and\n+  a real tradeoff. Do not write a decision record for routine or easily\n+  reversible choices.\n \n-- [0001 \u2014 Keep Shiki As The Platform Name](0001-keep-shiki-as-platform-name.md)\n-- [0002 \u2014 Use AGENTS As The Shared Agent Constitution](0002-use-agents-as-shared-constitution.md)\n-- [0003 \u2014 Require Skill Gate For MergeGate Readiness](0003-require-skill-gate-for-mergegate-readiness.md)\n-- [0004 \u2014 Use GitHub First With A Repository-Local Shiki Mirror](0004-use-github-first-with-shiki-mirror.md)\n-- [0005 \u2014 Use Codex As Front And Claude Code Action As The GitHub Runtime](0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md)\n-- [0006 \u2014 Separate Codex Implementation, CCA Completion, And MergeGate Authorization](0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md)\n+Using the directory as the source of truth lets independent changes add records\n+without contending on an enumerated index.\n \n-## Adding A New ADR\n+## Adding A Shiki SADR\n \n-1. Copy `template.md` to `docs/adr/NNNN-<short-slug>.md`.\n+1. Copy `template.md` to `docs/adr/SADR-NNNN-<short-slug>.md`.\n 2. Fill in the title, status, context, decision, and consequences.\n-3. Set the status to `Proposed`, then `Accepted` once the decision is approved.\n-4. Open a PR following `CONTRIBUTING.md`. You do not need to edit this index for\n-   the ADR to be discoverable via `docs/adr/*.md`.\n+3. Set the status to `Proposed`, then `Accepted` once the proper authority has\n+   approved the decision.\n+4. Open a PR following `CONTRIBUTING.md`.\n+\n+## Adding A Target ADR\n+\n+1. Copy `template.md` to `docs/adr/NNNN-<short-slug>.md` using the target's own\n+   sequence.\n+2. Cite it as `ADR NNNN`; do not prefix its filename with `SADR-`.\ndiff --git a/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md b/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\nnew file mode 100644\nindex 0000000000000000000000000000000000000000..7455ce1f3bed02ddddb851ac21a1d1a648c9aeba\n--- /dev/null\n+++ b/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\n@@ -0,0 +1,55 @@\n+# SADR-0019: Separate Shiki Platform SADRs From Target ADRs\n+\n+## Status\n+\n+Accepted\n+\n+## Context\n+\n+Shiki installs its platform decision records into `docs/adr/` in every target\n+repository. Shiki and its targets previously used the same numeric\n+`NNNN-<slug>.md` namespace. A target that already owned ADRs 0013 through 0018\n+therefore acquired two records for each number during installation, making an\n+`ADR NNNN` citation ambiguous. Renumbering target decisions would rewrite target\n+history, while renumbering Shiki decisions per target would destroy their stable\n+platform identity.\n+\n+Upgrades also need to remove the former Shiki-owned numeric paths without ever\n+mistaking a target-owned ADR for platform content. The install stamp already\n+records exact shipped paths and content digests, so it can prove ownership only\n+when both still match.\n+\n+## Decision\n+\n+Shiki platform decisions use the `SADR` namespace:\n+\n+- records are named `docs/adr/SADR-NNNN-<slug>.md`;\n+- live platform citations use `SADR-NNNN`;\n+- target product decisions retain `docs/adr/NNNN-<slug>.md` and `ADR NNNN`;\n+- the reference validator resolves the two namespaces independently, so the same\n+  four-digit number may exist once in each namespace.\n+\n+Before a forced upgrade writes anything, the installer inventories the exact\n+former numeric Shiki ADR paths. It may delete a legacy path only when the\n+existing install stamp contains that exact path and its recorded SHA-256 digest\n+matches the file currently on disk. All ownership blockers are reported\n+together and abort the upgrade before its first write. A non-forced install\n+never performs this cleanup.\n+\n+Historical `.shiki/` evidence remains unchanged because it records the namespace\n+that existed when that evidence was produced.\n+\n+## Consequences\n+\n+- Targets may use any ADR number without colliding with Shiki's platform SADRs.\n+- Fresh installs preserve target ADR bytes and install a stable SADR set.\n+- Forced upgrades fail closed when legacy ownership cannot be proven, and a\n+  successful upgrade refreshes the stamp without removed legacy paths.\n+- Current governance, packaging, workflow, prompt, script, and skill references\n+  must move with the platform records.\n+\n+## Alternatives Considered\n+\n+- Collision-only refusal leaves affected targets permanently unupgradable.\n+- Renumbering target ADRs mutates target-owned history.\n+- Per-target Shiki renumbering makes platform citations non-portable.\ndiff --git a/docs/adr/template.md b/docs/adr/template.md\nindex a92a97daddd0b00ab9dd626cc8d86942da2eafed..17dd79175b5fee2105935557f691b40cf9492942\n--- a/docs/adr/template.md\n+++ b/docs/adr/template.md\n@@ -1,14 +1,14 @@\n-# NNNN: <Short Decision Title>\n+# <SADR-NNNN or NNNN>: <Short Decision Title>\n \n ## Status\n \n-Proposed | Accepted | Superseded by ADR-NNNN\n+Proposed | Accepted | Superseded by SADR-NNNN or target ADR NNNN\n \n ## Context\n \n What is the situation, constraint, or force that requires a decision? Reference\n-relevant domain terms (`CONTEXT.md`), prior ADRs, and code reality. State the\n-problem, not the solution.\n+relevant domain terms (`CONTEXT.md`), prior Shiki SADRs or target ADRs, and code\n+reality. State the problem, not the solution.\n \n ## Decision\n \ndiff --git a/docs/agents/autonomous-loop-e2e-acceptance.md b/docs/agents/autonomous-loop-e2e-acceptance.md\nindex bbf84c6081987f159ac6ad9ecf3c82b7aa72e7e8..5c13c1d02a05a90b0dc46cbef7c95a9846d0c237\n--- a/docs/agents/autonomous-loop-e2e-acceptance.md\n+++ b/docs/agents/autonomous-loop-e2e-acceptance.md\n@@ -40,8 +40,8 @@\n 3. `gh auth status` is logged in with merge permission on the repo.\n 4. Branch protection + required checks are configured exactly as the goal-loop\n    required-check set expects (see `docs/agents/branch-protection-smoke.md`).\n-5. The repo `.shiki/policy` permits autonomous low/medium-risk merges (ADR\n-   0008/0009); the MergeGate policy check is the recorded-authority gate.\n+5. The repo `.shiki/policy` permits autonomous low/medium-risk merges\n+   (SADR-0008/0009); the MergeGate policy check is the recorded-authority gate.\n \n The operator's only actions are: register the task and start the loop. **No\n manual pre-clearing** means after `shiki loop run` starts, the operator does not\ndiff --git a/docs/agents/bootstrap-command.md b/docs/agents/bootstrap-command.md\nindex 05f7f203a52cf412b9f97084a6e131ed0f785afd..f7aa0b734dfebee4a3b139335b66ee3415f48713\n--- a/docs/agents/bootstrap-command.md\n+++ b/docs/agents/bootstrap-command.md\n@@ -189,7 +189,9 @@\n ```\n \n Use this only for tests, fixtures, or explicit local-only template inspection.\n-Use `--force` only when you intentionally want to overwrite existing target files.\n+Use `--force` only when you intentionally want to refresh the target template.\n+Legacy numeric Shiki decision cleanup is stamp-and-digest authorized before any\n+write; target ADR paths are not cleanup candidates.\n \n ## Slash Command\n \ndiff --git a/docs/agents/checklists.md b/docs/agents/checklists.md\nindex 4a7f03ade346f53a1c1d82981f3fd744f9ab4547..0c5ec6c53dc7197b7b7a7d18e667b325babd6c45\n--- a/docs/agents/checklists.md\n+++ b/docs/agents/checklists.md\n@@ -32,18 +32,18 @@\n | ID | Item | Blocking |\n | --- | --- | --- |\n | GD-01 | Plan was challenged against `CONTEXT.md`. | blocking for non-trivial Goals |\n-| GD-02 | Relevant ADRs were read or absence was recorded. | blocking for non-trivial Goals |\n+| GD-02 | Relevant SADRs and target ADRs were read or absence was recorded. | blocking for non-trivial Goals |\n | GD-03 | Ambiguous terms were resolved or converted to HITL blockers. | blocking |\n | GD-04 | Edge scenarios were considered. | advisory |\n | GD-05 | Code reality was checked when code could answer. | blocking when applicable |\n | GD-06 | New domain terms were added to `CONTEXT.md` when needed. | blocking when applicable |\n-| GD-07 | ADR was created or explicitly skipped using the ADR criteria. | blocking when applicable |\n+| GD-07 | A SADR or target ADR was created or explicitly skipped using the decision-record criteria. | blocking when applicable |\n \n ## CI \u2014 Context & Impact Checklist\n \n | ID | Item | Blocking |\n | --- | --- | --- |\n-| CI-01 | Relevant docs and ADRs identified. | blocking |\n+| CI-01 | Relevant docs, SADRs, and target ADRs identified. | blocking |\n | CI-02 | Relevant modules, interfaces, seams, callers, and tests identified. | blocking |\n | CI-03 | Dependencies and blocked-by relationships identified. | blocking |\n | CI-04 | Candidate locks identified. | blocking |\n@@ -62,7 +62,7 @@\n | PRD-04 | Implementation decisions are recorded without volatile code detail. | blocking |\n | PRD-05 | Testing decisions are recorded. | blocking |\n | PRD-06 | Out-of-scope items are explicit. | blocking |\n-| PRD-07 | Relevant glossary/ADR links are present when applicable. | blocking |\n+| PRD-07 | Relevant glossary/SADR/target-ADR links are present when applicable. | blocking |\n \n ## SF \u2014 Spec Freeze Checklist\n \n@@ -98,7 +98,7 @@\n | H-02 | Branch/worktree target is present. | blocking |\n | H-03 | Scope and non-goals are present. | blocking |\n | H-04 | Dependencies and locks are present. | blocking |\n-| H-05 | Relevant docs, ADRs, modules, and tests are referenced. | blocking |\n+| H-05 | Relevant docs, SADRs, target ADRs, modules, and tests are referenced. | blocking |\n | H-06 | Required skills are stated. | blocking |\n | H-07 | TDD expectations are stated. | blocking when behavior work |\n | H-08 | Acceptance criteria are included. | blocking |\ndiff --git a/docs/agents/codex-handoff.md b/docs/agents/codex-handoff.md\nindex ede468f47cb277f69a857e8c45df703e4ed0151d..6f97bdffb67e44d7edea2c71e01ea28933c4eb6c\n--- a/docs/agents/codex-handoff.md\n+++ b/docs/agents/codex-handoff.md\n@@ -46,7 +46,7 @@\n \n ## Context\n - Domain terms:\n-- Relevant ADRs:\n+- Relevant SADRs and target ADRs:\n - Relevant docs:\n - Relevant modules/interfaces/seams:\n - Similar tests/prior art:\ndiff --git a/docs/agents/completion-check-agent.md b/docs/agents/completion-check-agent.md\nindex 13e4c035db35025f4e3cdcc5adfb24314ac4d521..61bfa1218cf36ca605250c91919e620f33238aa0\n--- a/docs/agents/completion-check-agent.md\n+++ b/docs/agents/completion-check-agent.md\n@@ -32,7 +32,7 @@\n - `AGENTS.md`\n - `CLAUDE.md`\n - `CONTEXT.md`\n-- relevant ADRs\n+- relevant SADRs and target ADRs\n - parent Goal issue\n - PRD issue or document\n - task issue\ndiff --git a/docs/agents/decision-control.md b/docs/agents/decision-control.md\nindex 7d8ee5ab03a5ccbcbd04056ae61de03785a2a9c9..8c94ecda00fffaa0e92c325ab83aa9776bd1dac1\n--- a/docs/agents/decision-control.md\n+++ b/docs/agents/decision-control.md\n@@ -11,7 +11,7 @@\n | Decision | Owner | When | Required inputs | Output | Enforcement surface |\n | --- | --- | --- | --- | --- | --- |\n | Is the Goal clear enough to plan? | Planner + Guardian when needed | Goal intake | User request, existing docs, risk signals | Goal readiness verdict | Goal checklist, issue template, ledger |\n-| Must `grill-with-docs` run? | Planner / Skill Gate | Before PRD or issue split | Ambiguity, domain terms, ADR conflicts, unclear boundaries | Skill invocation or explicit skip rationale | Skill Gate checklist |\n+| Must `grill-with-docs` run? | Planner / Skill Gate | Before PRD or issue split | Ambiguity, domain terms, SADR/target-ADR conflicts, unclear boundaries | Skill invocation or explicit skip rationale | Skill Gate checklist |\n | Is a PRD required? | Planner | After Goal context stabilizes | Goal, `grill-with-docs` output, Context & Impact output | PRD required / not required | PRD checklist, CCA evidence check |\n | Can the spec be frozen? | Operator only | End of Requirements Definition, before issue split | PRD/requirements summary, scope inventory (SF-02), open-question list | `spec_freeze` block in the plan + ledger evidence | `require_grilled_plan` (plan ingest/run), SF checklist |\n | Can a frozen spec change? | Operator only (Spec Amendment) | When implementation contradicts the frozen spec | Contested decisions, scoped re-grill result | recorded amendment + re-stamped freeze | SF checklist, ledger evidence |\ndiff --git a/docs/agents/domain.md b/docs/agents/domain.md\nindex 258da8ea81e87ac0a6b0b5b5e5c2fde980eaabb7..d57b0cf17ff3e1fa9e6e5885372bf083d8295421\n--- a/docs/agents/domain.md\n+++ b/docs/agents/domain.md\n@@ -5,7 +5,9 @@\n ## Layout\n \n - `CONTEXT.md` defines Shiki language. Keep it implementation-free.\n-- `docs/adr/` records hard-to-reverse decisions.\n+- `docs/adr/SADR-NNNN-*.md` records Shiki platform decisions.\n+- `docs/adr/NNNN-*.md` records target product decisions without sharing the\n+  SADR namespace.\n - `AGENTS.md` is the runtime-neutral agent constitution.\n - `CLAUDE.md` is the Claude Code wrapper around `AGENTS.md`.\n - `.shiki/` is the Target Repository mirror for Goals, PRDs, plans, Task DAGs, contracts, locks, worktree records, ledger entries, CCA verdicts, repair packets, reports, and handoffs.\n@@ -15,6 +17,6 @@\n \n - Use Shiki terms exactly when they exist.\n - Add glossary terms to `CONTEXT.md` only when the term is part of the domain, not a generic programming concept.\n-- Create ADRs only for hard-to-reverse, surprising, tradeoff-based decisions.\n+- Create SADRs or target ADRs only for hard-to-reverse, surprising, tradeoff-based decisions.\n - Keep product-specific language in the Target Repository, not in Shiki Template docs.\n-- If an ADR conflicts with a proposed implementation, surface the conflict before editing.\n+- If a SADR or target ADR conflicts with a proposed implementation, surface the conflict before editing.\ndiff --git a/docs/agents/execution-control.md b/docs/agents/execution-control.md\nindex ad8ee3cdb36be56fedc1f46408ca92b387d74047..bc63c3a73661d12b567016ba299f1a6b66bf9088\n--- a/docs/agents/execution-control.md\n+++ b/docs/agents/execution-control.md\n@@ -63,7 +63,7 @@\n | Branch name | Orchestrator | Orchestrator | Before implementer dispatch | branch policy | branch ref in task/ledger |\n | Lock reservation | Context & Impact | MergeGate | Before dispatch and before merge | `.shiki/locks/`, labels, PR metadata | lock id, owner, scope |\n | CI profile | Planner | MergeGate | During issue triage, rechecked at PR | labels, `.shiki/policy.yaml`, workflow matrix | required check list |\n-| Required status checks | Repo maintainer / Guardian | GitHub branch protection / rulesets | Repository setup, updated via ADR | GitHub settings | branch/ruleset config, ADR |\n+| Required status checks | Repo maintainer / Guardian | GitHub branch protection / rulesets | Repository setup, updated via target ADR | GitHub settings | branch/ruleset config, target ADR |\n | CCA judgment | CCA | GitHub required check / MergeGate | Every non-draft PR update | GitHub Action check | structured CCA verdict |\n | Repair assignment | CCA | MergeGate | After failed CCA/CI/review | repair packet, issue/PR comment | repair id and attempt count |\n | Merge readiness | MergeGate | GitHub branch protection / Guardian if needed | After CCA complete | required checks, review rules, rulesets | mergegate check output |\ndiff --git a/docs/agents/guardian-policy.md b/docs/agents/guardian-policy.md\nindex 4147c7adf05a47adb4fd24ada1a1c2e1f2ba6d2d..a20d9cc6c00a569bb057b9ca8d6b51c7fc4a9847\n--- a/docs/agents/guardian-policy.md\n+++ b/docs/agents/guardian-policy.md\n@@ -98,8 +98,8 @@\n `origin/<default_branch>` and reuses each job's existing base fetch; a shallow\n (`--depth`) or second base fetch would make the proof unresolvable and silently\n disable the carry, so neither is permitted. CCA reads `guardian_comment_carried` as\n-a recorded authority in the deterministic signal and does not re-derive it. See ADR\n-0018 for the full decision, the rejected diff/patch-equality and path-disjointness\n+a recorded authority in the deterministic signal and does not re-derive it. See\n+SADR-0018 for the full decision, the rejected diff/patch-equality and path-disjointness\n alternatives, and the accepted residual.\n \n ## External AI Guardian Review (SADR-0010)\ndiff --git a/docs/agents/implementation-policy.md b/docs/agents/implementation-policy.md\nindex c754144a9fb5bf4a38aa82a60e1670c59e626b44..85c37281bcad196c6b7b02343faf00b03cd9c57a\n--- a/docs/agents/implementation-policy.md\n+++ b/docs/agents/implementation-policy.md\n@@ -31,10 +31,13 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n ## Non-Negotiables\n \n 1. **Planning precedes implementation.** Non-trivial Goals start with `grill-with-docs` and Context & Impact.\n-2. **Domain language is controlled.** Terms must be checked against `CONTEXT.md`; hard-to-reverse tradeoffs must be checked against ADRs.\n+2. **Domain language is controlled.** Terms must be checked against `CONTEXT.md`; hard-to-reverse tradeoffs must be checked against Shiki SADRs and target ADRs.\n 3. **PRD precedes issue decomposition.** `to-prd` is used once the context is settled enough to become durable intent.\n 4. **Issues are vertical slices.** `to-issues` must produce independently grabbable tracer-bullet tasks, not horizontal layer tickets.\n 5. **Triage controls dispatch.** Only `ready-for-agent` / AFK tasks may be assigned to Codex without human input.\n@@ -95,19 +98,19 @@\n Process:\n \n - Challenge terms against `CONTEXT.md`.\n-- Check ADRs before reopening decisions.\n+- Check SADRs and target ADRs before reopening decisions.\n - Inspect code when code can answer a question.\n - Ask one question at a time when the operator is available.\n - Provide a recommended answer with each question.\n - Record resolved terms in `CONTEXT.md`.\n-- Create ADRs only for hard-to-reverse, surprising, tradeoff-based decisions.\n+- Create SADRs or target ADRs only for hard-to-reverse, surprising, tradeoff-based decisions.\n - Convert unresolved load-bearing questions into HITL blockers.\n \n Outputs:\n \n - Resolved design-tree decisions.\n - Updated glossary terms when needed.\n-- ADRs when justified.\n+- SADRs or target ADRs when justified.\n - HITL blockers when decisions remain unresolved.\n \n ## Phase 3 \u2014 Context & Impact\n@@ -117,7 +120,7 @@\n \n Outputs must include:\n \n-- Relevant documents and ADRs.\n+- Relevant documents, SADRs, and target ADRs.\n - Relevant modules, interfaces, seams, callers, and tests.\n - Candidate locks.\n - Dependencies.\n@@ -140,7 +143,7 @@\n - Testing decisions.\n - Out of scope.\n - Further notes.\n-- Links to relevant Goal, domain terms, and ADRs.\n+- Links to relevant Goal, domain terms, SADRs, and target ADRs.\n \n Phase 4 ends with Spec Freeze: the operator explicitly approves the PRD, the\n plan records a `spec_freeze` block (status, approved_by, source), and the SF\n@@ -240,7 +243,7 @@\n - Dependencies and locks.\n - Risk and Guardian requirements.\n - Ledger completeness.\n-- Documentation and ADR updates.\n+- Documentation and SADR/target-ADR updates.\n - Scope drift.\n \n CCA emits one of:\n@@ -301,7 +304,7 @@\n | Evidence failure | Behavior might be done, but proof is missing. | Add checks, PR evidence, ledger entry, or artifacts. |\n | Scope drift | PR changes unrelated behavior. | Revert unrelated changes or split into new Goal. |\n | Dependency block | Upstream task or lock not complete. | Block until dependency resolves. |\n-| Architecture block | Decision needs ADR or deep-module discussion. | `grill-with-docs` or `improve-codebase-architecture`. |\n+| Architecture block | Decision needs a SADR, target ADR, or deep-module discussion. | `grill-with-docs` or `improve-codebase-architecture`. |\n | Risk block | Security/production/policy change needs human approval. | Guardian decision. |\n \n ## Phase 5.5 \u2014 Execution Decision Control\ndiff --git a/docs/agents/runtime-auth-model.md b/docs/agents/runtime-auth-model.md\nindex 6a2e1a47e81ea89b5c68a210ec07772a9374babc..3c54748d24233dbd0ba8473950282547ef4ce935\n--- a/docs/agents/runtime-auth-model.md\n+++ b/docs/agents/runtime-auth-model.md\n@@ -148,7 +148,7 @@\n \n API-key based runners may be added by a target repository as an explicit extension. They are not the default Shiki template path.\n \n-If a repo opts into API-key mode, record the exception in an ADR and update the repo's `AGENTS.md`, `.shiki/config.yaml`, and workflow permissions.\n+If a repo opts into API-key mode, record the target-specific exception in a target ADR and update the repo's `AGENTS.md`, `.shiki/config.yaml`, and workflow permissions.\n \n ## GitHub Actions Runtime Compatibility\n \ndiff --git a/docs/agents/skill-gate.md b/docs/agents/skill-gate.md\nindex c50398b0d7c60a8598f2a5c88a10015730dced13..3acffe6444617bb1d54b77d4b4708158ec735bc6\n--- a/docs/agents/skill-gate.md\n+++ b/docs/agents/skill-gate.md\n@@ -9,7 +9,7 @@\n | Skill | Trigger | Expected output |\n | --- | --- | --- |\n | `setup-matt-pocock-skills` | First configuring a repo for these skills, or when issue tracker, triage labels, or domain docs are missing. | `docs/agents/` configuration and an `Agent skills` block in `AGENTS.md` or `CLAUDE.md`. |\n-| `grill-with-docs` | Ambiguous plan, unclear terminology, boundary decision, tradeoff, or ADR-worthy design choice. | Clarified decision tree, recommended answers, updates to `CONTEXT.md` or ADRs when decisions crystallize. |\n+| `grill-with-docs` | Ambiguous plan, unclear terminology, boundary decision, tradeoff, or decision-record-worthy design choice. | Clarified decision tree, recommended answers, updates to `CONTEXT.md`, SADRs, or target ADRs when decisions crystallize. |\n | `zoom-out` | Agent lacks a higher-level architectural map. | Map of relevant modules, callers, seams, and domain concepts. |\n | `to-prd` | Settled context should become a PRD. | PRD published to the issue tracker with problem, solution, user stories, decisions, testing, and out-of-scope notes. |\n | `to-issues` | Goal, PRD, or plan must become implementation tickets. | Vertical-slice issues with acceptance criteria, dependencies, and AFK/HITL classification. |\n@@ -17,7 +17,7 @@\n | `tdd` | Feature work or bug fix has observable behavior that can be specified with tests. | Red-green-refactor loop through public interfaces, one vertical slice at a time. |\n | `code-review` | The TDD loop is green and the implementer is about to create or update a PR. Default for every implementation task. | Pre-PR self-review of the task diff with fixes applied on the task branch; findings and resolutions recorded as a ledger entry naming the skill and a `## Pre-PR code review` PR body section. |\n | `diagnose` | Hard bug, failing check, regression, flaky behavior, or performance problem. | Reproduction loop, ranked hypotheses, instrumentation, fix, regression test, post-mortem. |\n-| `improve-codebase-architecture` | Structural friction, testability, AI-navigability, deep-module opportunity, or refactor request. | Architecture candidates and deepening recommendations informed by `CONTEXT.md` and ADRs. |\n+| `improve-codebase-architecture` | Structural friction, testability, AI-navigability, deep-module opportunity, or refactor request. | Architecture candidates and deepening recommendations informed by `CONTEXT.md`, SADRs, and target ADRs. |\n | `prototype` | A throwaway logic or UI prototype is needed to answer a design question. | Clearly marked disposable prototype and captured decision. |\n \n ## MergeGate Interaction\ndiff --git a/docs/operations.md b/docs/operations.md\nindex cf1f452d4c589e38ccd58553bda0ad8684b51eb7..ace48a90ffbec9e5c0a7113bef16a4858bc44b3f\n--- a/docs/operations.md\n+++ b/docs/operations.md\n@@ -89,6 +89,16 @@\n      target history, brought forward by `shiki migrate`, not a template.\n    - **Everything else** (CLI scripts, workflows, schemas, docs) is overwritten.\n \n+   A forced upgrade also migrates the former numeric Shiki decision paths to\n+   `SADR-NNNN-*`. Before the first write, the installer inventories the exact\n+   legacy Shiki paths and authorizes each deletion only when the existing\n+   `.shiki/install-stamp.json` names that path and its SHA-256 digest still\n+   matches the file. An absent, unreadable, malformed, incomplete, or\n+   mismatching stamp reports every blocked path and stops with no filesystem\n+   change. Target-owned `NNNN-*` ADRs are not cleanup candidates. A non-force\n+   install deletes nothing and retains any still-matching legacy stamp entries\n+   so a later forced upgrade can prove ownership.\n+\n    If the target has pending migrations, `--force` refuses and writes nothing \u2014\n    apply migrations first (step 3), then re-run. The run ends with a single\n    summary of every `.new` written, so a half-upgrade is visible.\ndiff --git a/docs/releasing.md b/docs/releasing.md\nindex 10d73828e7dbd64e9d9f6b83bc84d010537ce62a..aafeee8c4d441e1e427efee2ce4f81a647f020e1\n--- a/docs/releasing.md\n+++ b/docs/releasing.md\n@@ -67,7 +67,7 @@\n CLI in `scripts/`. There are no third-party Python runtime dependencies. The CLI\n is intentionally **not** exposed as a `[project.scripts]` console entry point\n because `scripts/shiki.py` is a dependency-free shim and `scripts/` is not\n-packaged as an importable distribution (see the ADR for the rationale).\n+packaged as an importable distribution (see SADR-0007 for the rationale).\n \n Supported install paths:\n \n@@ -155,6 +155,15 @@\n    [docs/operations.md](operations.md#the-new-convention). If the target has\n    pending migrations, `--force` refuses and writes nothing; apply migrations\n    (step 2) first, then re-run.\n+\n+   The SADR namespace upgrade has an additional pre-write gate: each former\n+   numeric Shiki decision path is removed only when the existing install stamp\n+   contains that exact path and a digest matching its current bytes. The\n+   installer aggregates absent/unreadable/malformed-stamp, missing-digest, and\n+   digest-mismatch blockers before raising, so refusal leaves the target tree\n+   byte-identical. Numeric target ADRs are preserved. A successful forced\n+   upgrade installs and stamps `SADR-*` paths and drops the removed legacy paths\n+   from the refreshed stamp.\n 2. Apply repository-local `.shiki/` state migrations with the migrate command:\n \n    ```bash\ndiff --git a/docs/usage.md b/docs/usage.md\nindex b83407ee3970dd395dd893010bc5dbe8bad26051..a130ef6e40dfd8169a5bc701339351097df1a09f\n--- a/docs/usage.md\n+++ b/docs/usage.md\n@@ -67,8 +67,10 @@\n shiki install-target /path/to/target-repo --local-only\n ```\n \n-Use `--force` only when you intentionally want to overwrite existing target\n-files.\n+Use `--force` only when you intentionally want to refresh the target template.\n+For the SADR namespace migration, `--force` deletes only exact legacy Shiki\n+decision paths whose existing install-stamp digest still matches; any ownership\n+blocker stops before the first write. Numeric target ADRs are never candidates.\n \n ## `shiki bootstrap-platform` \u2014 publish the Shiki platform repo\n \ndiff --git a/scripts/guardian_approval_signal.py b/scripts/guardian_approval_signal.py\nindex d059f92b2c82aa2b5d6ca421365537a87886c1cb..16924892714f4f5b9303c0ef0e325a7e70075630\n--- a/scripts/guardian_approval_signal.py\n+++ b/scripts/guardian_approval_signal.py\n@@ -199,8 +199,8 @@\n ) -> Any:\n     \"\"\"Evaluate SADR-0015 Contract Approval for the CCA Guardian signal.\n \n-    The SAME OR that MergeGate applies must reach this deterministic signal: ADR\n-    0015 is explicit that without it, a contract-approved PR still returns\n+    The SAME OR that MergeGate applies must reach this deterministic signal:\n+    SADR-0015 is explicit that without it, a contract-approved PR still returns\n     ``needs_guardian``, the CCA job fails, and the MergeGate policy check never\n     runs \u2014 the gate this change moves would never be reached. This resolves the\n     task the SAME way MergeGate does (the first Shiki task id in the PR body),\ndiff --git a/scripts/shiki_contract_approval.py b/scripts/shiki_contract_approval.py\nindex 941bcb96fdd8b9a85bc0c4f0f04d5dad9d04839c..4a4b418f6527413161db82670bec857a043360e2\n--- a/scripts/shiki_contract_approval.py\n+++ b/scripts/shiki_contract_approval.py\n@@ -142,7 +142,7 @@\n     changed_files_status: Any,\n     registration: Any,\n ) -> ContractApprovalResult:\n-    \"\"\"Whether this PR carries an SADR-0015 Contract Approval.\n+    \"\"\"Whether this PR carries a SADR-0015 Contract Approval.\n \n     The four conditions are evaluated IN ORDER, each failure recorded as a\n     distinct reason string:\ndiff --git a/scripts/shiki_guardian.py b/scripts/shiki_guardian.py\nindex a4ecb79bbda55fd5c8b06adfae2eb99760a03c45..4dd99bbe714e26f421fa19abac7a34ae1b195ecc\n--- a/scripts/shiki_guardian.py\n+++ b/scripts/shiki_guardian.py\n@@ -12,7 +12,7 @@\n # The carry re-derives its proof from git in-process on every judging invocation;\n # it is never read from an artifact, so there is nothing to go stale or be forged.\n # shiki_sync_proof is the ONLY guardian-family module that runs git, and every one\n-# of its invocations is read-only and local (ADR: prove-from-git base sync).\n+# of its invocations is read-only and local (SADR: prove-from-git base sync).\n from shiki_sync_proof import verify_pure_base_sync\n \n GUARDIAN_POLICY_PATH = \".shiki/guardian-policy.json\"\ndiff --git a/scripts/shiki_guardian_review.py b/scripts/shiki_guardian_review.py\nindex dd0f104d457899e0ba5ff1e162011c1e90b6f92b..e10ef58c046324e7ea3a3392feb3048ca77414ce\n--- a/scripts/shiki_guardian_review.py\n+++ b/scripts/shiki_guardian_review.py\n@@ -159,7 +159,7 @@\n     docs_only = _is_docs_only(changed_files)\n     if docs_only and matched == 0:\n         focus.append(\n-            \"Docs/ADR-only change: confirm the change is genuinely documentation/decision-only, introduces no behavior or contract drift, and stays consistent with the constitution and existing ADRs.\"\n+            \"Docs/decision-record-only change: confirm the change is genuinely documentation/decision-only, introduces no behavior or contract drift, and stays consistent with the constitution and existing SADRs and target ADRs.\"\n         )\n \n     # Conservative augmentation: unknown (no category and not docs-only), mixed\ndiff --git a/scripts/shiki_installer.py b/scripts/shiki_installer.py\nindex be064ab7a29ff6836f7f7a1d70500a43e0d11d7b..9fd8eefd137ce7003dad5c0dcb2e48e2cd47ab32\n--- a/scripts/shiki_installer.py\n+++ b/scripts/shiki_installer.py\n@@ -118,6 +118,7 @@\n # what lets a target carry it without a governance-schema change.\n INSTALL_STAMP_PATH = \".shiki/install-stamp.json\"\n INSTALL_STAMP_VERSION = 1\n+LEGACY_SHIKI_SADR_MAX = 18\n \n # Under --force the shipped surface splits three ways, so nothing is lost and\n # nothing is silently stale.\n@@ -237,9 +238,98 @@\n         )\n \n \n+def legacy_shiki_adr_paths() -> tuple[str, ...]:\n+    \"\"\"Former numeric paths for Shiki SADR-0001 through SADR-0018.\n+\n+    The authoritative slugs come from the current SADR filenames. SADR-0019 is\n+    the namespace decision itself and never shipped under a numeric legacy path.\n+    \"\"\"\n+    paths: list[str] = []\n+    for sadr in sorted((ROOT / \"docs\" / \"adr\").glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\")):\n+        number = sadr.name.split(\"-\", 2)[1]\n+        if int(number) <= LEGACY_SHIKI_SADR_MAX:\n+            paths.append(f\"docs/adr/{sadr.name.removeprefix('SADR-')}\")\n+    return tuple(paths)\n+\n+\n+def preflight_legacy_sadr_cleanup(target: Path, *, force: bool) -> tuple[str, ...]:\n+    \"\"\"Authorize exact legacy Shiki ADR removals before the first write.\n+\n+    A non-force install never removes existing files. A forced install needs no\n+    stamp when no legacy Shiki path is present. When legacy paths do exist, each\n+    must be named in a readable install stamp and its current bytes must match\n+    the recorded digest.\n+    \"\"\"\n+    if not force:\n+        return ()\n+    present = tuple(\n+        relative\n+        for relative in legacy_shiki_adr_paths()\n+        if (target / relative).exists() or (target / relative).is_symlink()\n+    )\n+    if not present:\n+        return ()\n+\n+    stamp_path = target / INSTALL_STAMP_PATH\n+    try:\n+        raw = stamp_path.read_text(encoding=\"utf-8\")\n+        stamp = json.loads(raw)\n+    except FileNotFoundError:\n+        detail = \"install stamp is absent\"\n+        blockers = [f\"{relative}: {detail}\" for relative in present]\n+    except (OSError, UnicodeDecodeError) as error:\n+        detail = f\"install stamp is unreadable ({error})\"\n+        blockers = [f\"{relative}: {detail}\" for relative in present]\n+    except json.JSONDecodeError as error:\n+        detail = f\"install stamp is malformed ({error})\"\n+        blockers = [f\"{relative}: {detail}\" for relative in present]\n+    else:\n+        if not isinstance(stamp, dict):\n+            blockers = [\n+                f\"{relative}: install stamp is malformed (expected an object)\"\n+                for relative in present\n+            ]\n+        else:\n+            digests = stamp.get(\"digests\")\n+            blockers = []\n+            if not isinstance(digests, dict):\n+                blockers.extend(\n+                    f\"{relative}: install stamp has no digest for this path\"\n+                    for relative in present\n+                )\n+            else:\n+                for relative in present:\n+                    expected = digests.get(relative)\n+                    if not isinstance(expected, str) or not expected:\n+                        blockers.append(\n+                            f\"{relative}: install stamp has no digest for this path\"\n+                        )\n+                        continue\n+                    try:\n+                        actual = _sha256_file(target / relative)\n+                    except OSError as error:\n+                        blockers.append(\n+                            f\"{relative}: legacy path is unreadable ({error})\"\n+                        )\n+                        continue\n+                    if actual != expected:\n+                        blockers.append(\n+                            f\"{relative}: digest mismatch (stamp {expected}, current {actual})\"\n+                        )\n+\n+    if blockers:\n+        joined = \"\\n\".join(f\"- {blocker}\" for blocker in blockers)\n+        raise ShikiError(\n+            \"legacy Shiki ADR cleanup is not authorized:\\n\"\n+            f\"{joined}\\nNo files were written.\"\n+        )\n+    return present\n+\n+\n def install_template(target: Path, *, force: bool, validate: bool) -> list[NewFileNote]:\n     # Preconditions first: never rewrite the tree of a target that cannot be\n     # upgraded cleanly (see refuse_pending_migrations).\n+    legacy_cleanup = preflight_legacy_sadr_cleanup(target, force=force)\n     refuse_pending_migrations(target)\n \n     # Resolve the target's required CODEOWNERS owner once from its\n@@ -250,6 +340,10 @@\n     # verbatim. See ``codeowners_required_owner``.\n     codeowners_owner = codeowners_required_owner(target)\n \n+    for relative in legacy_cleanup:\n+        (target / relative).unlink()\n+        info(f\"removed stamped legacy Shiki ADR: {target / relative}\")\n+\n     notes: list[NewFileNote] = []\n     for relative in TEMPLATE_PATHS:\n         source = ROOT / relative\n@@ -647,6 +741,22 @@\n     digests = target_stamp_digests(target)\n     existing = read_install_stamp(target)\n     if existing is not None:\n+        # A non-force install deliberately leaves legacy Shiki ADRs in place.\n+        # Preserve only still-matching ownership proof so a later forced\n+        # upgrade can authorize cleanup; never carry a missing or stale digest.\n+        existing_digests = existing.get(\"digests\")\n+        if isinstance(existing_digests, dict):\n+            for relative in legacy_shiki_adr_paths():\n+                expected = existing_digests.get(relative)\n+                path = target / relative\n+                if not isinstance(expected, str) or not path.is_file():\n+                    continue\n+                try:\n+                    actual = _sha256_file(path)\n+                except OSError:\n+                    continue\n+                if actual == expected:\n+                    digests[relative] = actual\n         if _incoming_is_older(commit, existing.get(\"platform_commit\")):\n             warn(\n                 \"kept existing install stamp: incoming install \"\ndiff --git a/scripts/shiki_loop.py b/scripts/shiki_loop.py\nindex 3611641da8aeb5038b1da90e3114b09a473ec6d8..84caa491b9cad0edc4176f209c13e9221e975681\n--- a/scripts/shiki_loop.py\n+++ b/scripts/shiki_loop.py\n@@ -424,7 +424,7 @@\n         # unresolvable CCA verdict has already produced its terminal stop and is never\n         # laundered into a head-moving sync (SADR-0010). It also never fires while the\n         # policy (Guardian) gate is red \u2014 for ANY reason, not only when it is the sole\n-        # red check \u2014 so a head-bound Guardian approval, or an SADR-0015\n+        # red check \u2014 so a head-bound Guardian approval, or a SADR-0015\n         # contract-approval carry that base movement defeated (the policy gate compares\n         # base and head task governance directly), is never silently restored by\n         # adopting base wholesale. It is bounded by a durable per-task counter so the\ndiff --git a/scripts/shiki_tasks.py b/scripts/shiki_tasks.py\nindex aec3809f24b4a0c8c96fc50971cff9ae9ad30381..d531052e9d9717058d4631483cefab44c26a65c1\n--- a/scripts/shiki_tasks.py\n+++ b/scripts/shiki_tasks.py\n@@ -821,7 +821,7 @@\n         \"prompt\": prompt,\n         \"entry_skill\": \"grill-with-docs\",\n         \"required_next_steps\": [\n-            \"Run grill-with-docs until terminology, boundaries, risks, and ADR-worthy decisions are settled.\",\n+            \"Run grill-with-docs until terminology, boundaries, risks, and decision-record-worthy choices are settled.\",\n             \"Obtain the operator's explicit approval of the PRD/requirements (Spec Freeze).\",\n             \"Write a machine-readable plan JSON with grill_with_docs.status=complete and spec_freeze.status=frozen.\",\n             \"Run shiki plan ingest --plan-file PLAN.json.\",\ndiff --git a/scripts/test_shiki_goal_loop.sh b/scripts/test_shiki_goal_loop.sh\nindex 753c7b516e10f0be3bc92a4bd0eb1fa699776ae6..8f3820a0a079b3044424cfe1a6c6b7fe68896e67\n--- a/scripts/test_shiki_goal_loop.sh\n+++ b/scripts/test_shiki_goal_loop.sh\n@@ -231,8 +231,8 @@\n \n # High/critical risk merges autonomously when all required checks are green:\n # the \"MergeGate policy check\" required check IS the Guardian gate (it enforces\n-# guardian-policy.json \u2014 human approval OR external AI guardian review, ADR\n-# 0010), so green checks mean a recorded authority approved. The fake gh returns\n+# guardian-policy.json \u2014 human approval OR external AI guardian review,\n+# SADR-0010), so green checks mean a recorded authority approved. The fake gh returns\n # green checks, simulating that the Guardian gate passed.\n python3 \"$ROOT/scripts/shiki.py\" goal create --target \"$TARGET\" --title \"Guardian gate\" --outcome \"High risk merges via policy gate\" >/tmp/shiki-goal-loop-guardian-goal.json\n GGOAL=\"$(json_get /tmp/shiki-goal-loop-guardian-goal.json goal_id)\"\ndiff --git a/tests/test_adr_references.py b/tests/test_adr_references.py\nindex 51706329c797d5dd9b47e082a333fa83b261d482..d005da4f771f5e8085ec22d4b787980e68f5ae8c\n--- a/tests/test_adr_references.py\n+++ b/tests/test_adr_references.py\n@@ -1,8 +1,8 @@\n-\"\"\"Every ``ADR ####`` citation must resolve to exactly one decision record.\n+\"\"\"Every target ADR and Shiki SADR citation resolves in its own namespace.\n \n Background. ``scripts/mergegate_check.py`` justifies ``is_bookkeeping_closeout``\n-with \"ADR 0017\" nine times and ``scripts/guardian_approval_signal.py`` cites\n-0016/0017 seven times, yet for a stretch the numbering in ``docs/adr/`` stopped\n+with \"SADR-0017\" nine times and ``scripts/guardian_approval_signal.py`` cites\n+SADR-0016/SADR-0017 seven times, yet for a stretch the numbering in ``docs/adr/`` stopped\n at 0015: the authoring commits lived on a coordinator branch and no task ever\n declared ``docs/adr/...`` in its locks, so nothing carried the records to the\n default branch. The stated basis of an exemption that merged seven PRs with zero\n@@ -11,11 +11,12 @@\n and the directory drifted with no signal.\n \n This is the same unbound-pair shape behind other silent failures: a reference\n-and its target with nothing asserting they agree. This test binds them. It scans\n-every ``scripts/*.py`` (plus ``docs/**/*.md`` and the root ``*.md`` governance\n-files, which are cheap) for the ``ADR ####`` pattern and asserts each cited\n-number resolves to exactly one ``docs/adr/####-*.md``. The converse is\n-deliberately NOT required: an ADR may exist without being cited.\n+and its target with nothing asserting they agree. This test binds them. Target\n+``ADR NNNN`` citations resolve only to ``docs/adr/NNNN-*.md``; Shiki\n+``SADR-NNNN`` citations resolve only to ``docs/adr/SADR-NNNN-*.md``. The scan\n+covers current governance docs, scripts, workflows, prompts, packaging metadata,\n+and repo-local Codex guidance while deliberately excluding historical\n+``.shiki`` evidence. The converse is not required: a record may be uncited.\n \n The suite is dependency free: standard-library ``unittest`` only.\n \"\"\"\n@@ -31,17 +32,18 @@\n \n REPO_ROOT = Path(__file__).resolve().parents[1]\n \n-# \"ADR 0016\" -- a four-digit citation. The trailing \\b keeps a five-digit token\n-# from being read as a truncated four-digit number.\n-_CITATION = re.compile(r\"\\bADR (\\d{4})\\b\")\n-# \"0016-locks-bound-product-paths.md\" -- the leading four digits name the record.\n-_ADR_FILENAME = re.compile(r\"^(\\d{4})-.*\\.md$\")\n+# ``ADR 0016`` / ``SADR-0016`` -- a namespace plus four-digit citation. The\n+# trailing ``\\b`` keeps a five-digit token from being read as a truncated one.\n+_CITATION = re.compile(r\"\\b(S?ADR)[ -](\\d{4})\\b\")\n+# Target ADRs retain the numeric filename; Shiki platform decisions use SADR.\n+_DECISION_FILENAME = re.compile(r\"^(?:(SADR)-)?(\\d{4})-.*\\.md$\")\n \n \n class Violation(NamedTuple):\n     \"\"\"A citation that does not resolve to exactly one decision record.\"\"\"\n \n     citing_file: str  # repo-relative path of the file that cites the ADR\n+    namespace: str  # \"ADR\" for target decisions; \"SADR\" for Shiki decisions\n     number: str  # the cited four-digit number, e.g. \"0016\"\n     line: int  # 1-based line of the citation\n     reason: str  # \"missing\" (zero matches) or \"ambiguous\" (two or more)\n@@ -49,44 +51,59 @@\n \n     @property\n     def expected_path(self) -> str:\n-        return f\"docs/adr/{self.number}-*.md\"\n+        prefix = \"SADR-\" if self.namespace == \"SADR\" else \"\"\n+        return f\"docs/adr/{prefix}{self.number}-*.md\"\n \n     def describe(self) -> str:\n         \"\"\"Name the citing file, the cited number, and the missing path.\"\"\"\n         if self.reason == \"missing\":\n             return (\n-                f\"{self.citing_file}:{self.line} cites ADR {self.number} \"\n+                f\"{self.citing_file}:{self.line} cites {self.namespace} {self.number} \"\n                 f\"but no document matches {self.expected_path}\"\n             )\n         joined = \", \".join(self.matches)\n         return (\n-            f\"{self.citing_file}:{self.line} cites ADR {self.number} which \"\n+            f\"{self.citing_file}:{self.line} cites {self.namespace} {self.number} which \"\n             f\"resolves to {len(self.matches)} documents (expected exactly one \"\n             f\"at {self.expected_path}): {joined}\"\n         )\n \n \n-def _adr_index(root: Path) -> dict[str, list[Path]]:\n-    \"\"\"Map every four-digit ADR number to the record files that claim it.\"\"\"\n-    index: dict[str, list[Path]] = {}\n+def _adr_index(root: Path) -> dict[tuple[str, str], list[Path]]:\n+    \"\"\"Map each decision namespace and number to the files that claim it.\"\"\"\n+    index: dict[tuple[str, str], list[Path]] = {}\n     adr_dir = root / \"docs\" / \"adr\"\n     if adr_dir.is_dir():\n         for path in sorted(adr_dir.glob(\"*.md\")):\n-            match = _ADR_FILENAME.match(path.name)\n+            match = _DECISION_FILENAME.match(path.name)\n             if match:\n-                index.setdefault(match.group(1), []).append(path)\n+                namespace = \"SADR\" if match.group(1) else \"ADR\"\n+                index.setdefault((namespace, match.group(2)), []).append(path)\n     return index\n \n \n def _citing_files(root: Path, include_docs: bool = True) -> list[Path]:\n-    \"\"\"Files scanned for citations: scripts/*.py, and cheaply the markdown.\"\"\"\n+    \"\"\"Current, shipped text surfaces scanned for decision citations.\"\"\"\n     files = sorted((root / \"scripts\").glob(\"*.py\"))\n+    files += sorted((root / \"scripts\").glob(\"*.sh\"))\n     if include_docs:\n         docs = root / \"docs\"\n         if docs.is_dir():\n             files += sorted(docs.rglob(\"*.md\"))\n         files += sorted(root.glob(\"*.md\"))\n-    return files\n+        pyproject = root / \"pyproject.toml\"\n+        if pyproject.is_file():\n+            files.append(pyproject)\n+        for relative in (\".github\", \".codex\"):\n+            surface = root / relative\n+            if surface.is_dir():\n+                files += sorted(\n+                    path\n+                    for path in surface.rglob(\"*\")\n+                    if path.is_file()\n+                    and path.suffix in {\".md\", \".py\", \".sh\", \".toml\", \".yaml\", \".yml\"}\n+                )\n+    return sorted(set(files))\n \n \n def find_adr_reference_violations(\n@@ -94,10 +111,9 @@\n ) -> list[Violation]:\n     \"\"\"Return every ``ADR ####`` citation under ``root`` that fails to resolve.\n \n-    A citation resolves when exactly one ``docs/adr/####-*.md`` matches its\n-    number. Zero matches is a \"missing\" violation; two or more is \"ambiguous\".\n-    Records that are never cited produce nothing -- the converse is not\n-    required.\n+    A citation resolves when exactly one record in the same namespace matches\n+    its number. Zero matches is a \"missing\" violation; two or more is\n+    \"ambiguous\". Records that are never cited produce nothing.\n     \"\"\"\n     root = Path(root)\n     index = _adr_index(root)\n@@ -110,13 +126,15 @@\n         rel = path.relative_to(root).as_posix()\n         for lineno, line in enumerate(text.splitlines(), start=1):\n             for match in _CITATION.finditer(line):\n-                number = match.group(1)\n-                found = index.get(number, [])\n+                namespace, number = match.groups()\n+                found = index.get((namespace, number), [])\n                 if len(found) == 1:\n                     continue\n                 reason = \"missing\" if not found else \"ambiguous\"\n                 rels = [p.relative_to(root).as_posix() for p in found]\n-                violations.append(Violation(rel, number, lineno, reason, rels))\n+                violations.append(\n+                    Violation(rel, namespace, number, lineno, reason, rels)\n+                )\n     return violations\n \n \n@@ -129,8 +147,8 @@\n         self.assertEqual(\n             violations,\n             [],\n-            \"Every 'ADR ####' citation must resolve to exactly one \"\n-            f\"docs/adr/####-*.md:\\n{detail}\",\n+            \"Every ADR/SADR citation must resolve exactly once in its own \"\n+            f\"docs/adr namespace:\\n{detail}\",\n         )\n \n     def test_scan_actually_reads_scripts(self) -> None:\n@@ -142,12 +160,38 @@\n             \"the citation scan must cover scripts/*.py\",\n         )\n \n+    def test_platform_citations_do_not_use_unresolved_numeric_shorthand(self) -> None:\n+        shorthand = re.compile(r\"SADR-\\d{4}/\\d{4}\")\n+        offenders = []\n+        for path in _citing_files(REPO_ROOT):\n+            text = path.read_text(encoding=\"utf-8\")\n+            if shorthand.search(text):\n+                offenders.append(path.relative_to(REPO_ROOT).as_posix())\n+        self.assertEqual(\n+            offenders,\n+            [],\n+            \"repeat the SADR prefix so every cited number is independently resolvable\",\n+        )\n+\n     def test_carried_records_are_indexed(self) -> None:\n         # The two records this Goal carried onto the default branch, and which\n         # the scripts cite most, must be present.\n         index = _adr_index(REPO_ROOT)\n-        self.assertIn(\"0016\", index)\n-        self.assertIn(\"0017\", index)\n+        self.assertIn((\"SADR\", \"0016\"), index)\n+        self.assertIn((\"SADR\", \"0017\"), index)\n+\n+    def test_platform_tree_has_only_sadr_0001_through_0019(self) -> None:\n+        adr_dir = REPO_ROOT / \"docs\" / \"adr\"\n+        sadrs = sorted(adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\"))\n+        self.assertEqual(\n+            {path.name.split(\"-\", 2)[1] for path in sadrs},\n+            {f\"{number:04d}\" for number in range(1, 20)},\n+        )\n+        self.assertEqual(\n+            list(adr_dir.glob(\"[0-9][0-9][0-9][0-9]-*.md\")),\n+            [],\n+            \"the Shiki platform tree must not retain legacy numeric SADR paths\",\n+        )\n \n \n class FixtureAdrReferences(unittest.TestCase):\n@@ -180,6 +224,26 @@\n         self.assertIn(\"9991\", message)  # cited number\n         self.assertIn(\"docs/adr/9991-*.md\", message)  # missing path\n \n+    def test_target_adr_does_not_satisfy_missing_platform_sadr(self) -> None:\n+        root = self._make_repo(\n+            adr_names=[\"0042-target-decision.md\"],\n+            script_body='\"\"\"Target ADR 0042 exists, but platform SADR-0042 does not.\"\"\"\\n',\n+        )\n+        violations = find_adr_reference_violations(root, include_docs=False)\n+        self.assertEqual(len(violations), 1)\n+        self.assertEqual(violations[0].namespace, \"SADR\")\n+        self.assertEqual(violations[0].number, \"0042\")\n+        self.assertEqual(violations[0].expected_path, \"docs/adr/SADR-0042-*.md\")\n+\n+    def test_target_adr_and_platform_sadr_may_reuse_a_number(self) -> None:\n+        root = self._make_repo(\n+            adr_names=[\"0042-target-decision.md\", \"SADR-0042-platform-decision.md\"],\n+            script_body='\"\"\"Target ADR 0042 and platform SADR-0042 are distinct.\"\"\"\\n',\n+        )\n+        self.assertEqual(\n+            find_adr_reference_violations(root, include_docs=False), []\n+        )\n+\n     def test_uncited_adr_is_allowed(self) -> None:\n         # An ADR exists but nothing cites it -> not a violation. The converse\n         # (every ADR must be cited) is explicitly NOT required.\ndiff --git a/tests/test_guardian_review.py b/tests/test_guardian_review.py\nindex 88829e7810212635e2fc98aa5918d6f26294677d..00a85a237acd5bf2d6cb5b294128198c422402ca\n--- a/tests/test_guardian_review.py\n+++ b/tests/test_guardian_review.py\n@@ -162,7 +162,7 @@\n \n     def test_docs_only_is_light_and_not_conservative(self):\n         focus = gr.classify_review_focus([\"docs/adr/0099-x.md\", \"README.md\"], \"\", \"low\")\n-        self.assertTrue(any(\"Docs/ADR-only\" in f for f in focus))\n+        self.assertTrue(any(\"Docs/decision-record-only\" in f for f in focus))\n         self.assertFalse(any(\"conservative default\" in f.lower() for f in focus))\n \n     def test_unknown_category_is_conservative(self):\ndiff --git a/tests/test_policy_job_merged_prs.py b/tests/test_policy_job_merged_prs.py\nindex d45512e74681e5e649279236c7d4e79d5c3da471..b97989f8bd74d091de1a6b5275a20d8dc68c4512\n--- a/tests/test_policy_job_merged_prs.py\n+++ b/tests/test_policy_job_merged_prs.py\n@@ -3,8 +3,8 @@\n \n ``.github/workflows/shiki-cca-completion.yml`` has two consumers of the merged-PR\n proof: the CCA *signal* (``guardian_approval_signal.py`` in \"Prepare CCA context\")\n-and the authoritative gate (``mergegate_check.py`` in \"Enforce MergeGate\"). ADR\n-0017's bookkeeping-closeout exemption (condition 6) needs the merged implementation\n+and the authoritative gate (``mergegate_check.py`` in \"Enforce MergeGate\").\n+SADR-0017's bookkeeping-closeout exemption (condition 6) needs the merged implementation\n PR proven, and a closeout carries NO ``post_merge_reconcile`` marker. The signal\n already resolves ``MERGED_PRS`` unconditionally, but \"Enforce MergeGate\" used to\n resolve it only inside ``grep -q '<!-- shiki:post_merge_reconcile -->'``. So a\ndiff --git a/tests/test_sadr_install_migration.py b/tests/test_sadr_install_migration.py\nnew file mode 100644\nindex 0000000000000000000000000000000000000000..d51eac872b1c79ff4579c4c1401ca5d775a60d8d\n--- /dev/null\n+++ b/tests/test_sadr_install_migration.py\n@@ -0,0 +1,237 @@\n+\"\"\"SADR installation and collision-safe legacy upgrade behavior.\"\"\"\n+\n+from __future__ import annotations\n+\n+import contextlib\n+import hashlib\n+import io\n+import json\n+import shutil\n+import tempfile\n+import unittest\n+from pathlib import Path\n+\n+import shiki_test_support  # noqa: F401  (path bootstrap)\n+\n+import shiki_installer\n+from tests.test_adr_references import find_adr_reference_violations\n+\n+\n+PRODUCT_NUMBERS = tuple(range(13, 19))\n+\n+\n+def _quiet_install(target: Path, *, force: bool, validate: bool) -> None:\n+    with contextlib.redirect_stdout(io.StringIO()):\n+        shiki_installer.install_template(target, force=force, validate=validate)\n+\n+\n+def _sha256(path: Path) -> str:\n+    return hashlib.sha256(path.read_bytes()).hexdigest()\n+\n+\n+def _legacy_sadr_paths() -> tuple[str, ...]:\n+    return tuple(\n+        f\"docs/adr/{path.name.removeprefix('SADR-')}\"\n+        for path in sorted((shiki_installer.ROOT / \"docs\" / \"adr\").glob(\"SADR-*.md\"))\n+        if path.name != \"SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\"\n+    )\n+\n+\n+def _create_legacy_upgrade_fixture(\n+    target: Path,\n+) -> tuple[dict[str, bytes], tuple[str, ...]]:\n+    adr_dir = target / \"docs\" / \"adr\"\n+    adr_dir.mkdir(parents=True)\n+    product_bytes = {\n+        f\"{number:04d}-cubeshare-product-decision.md\": (\n+            f\"# CubeShare ADR {number:04d}\\n\\nDo not rewrite {number}.\\n\"\n+        ).encode()\n+        for number in PRODUCT_NUMBERS\n+    }\n+    for name, content in product_bytes.items():\n+        (adr_dir / name).write_bytes(content)\n+\n+    legacy_paths = _legacy_sadr_paths()\n+    for relative in legacy_paths:\n+        (target / relative).write_bytes(f\"legacy Shiki bytes for {relative}\\n\".encode())\n+\n+    stamp_path = target / shiki_installer.INSTALL_STAMP_PATH\n+    stamp_path.parent.mkdir(parents=True, exist_ok=True)\n+    stamp = {\n+        \"version\": shiki_installer.INSTALL_STAMP_VERSION,\n+        \"platform_commit\": None,\n+        \"installed_at\": \"2000-01-01T00:00:00+00:00\",\n+        \"digests\": {\n+            relative: _sha256(target / relative) for relative in legacy_paths\n+        },\n+    }\n+    stamp_path.write_text(\n+        json.dumps(stamp, indent=2, sort_keys=True) + \"\\n\", encoding=\"utf-8\"\n+    )\n+    return product_bytes, legacy_paths\n+\n+\n+def _tree_snapshot(root: Path) -> dict[str, tuple[str, bytes | str]]:\n+    \"\"\"Recursive byte/shape snapshot used to prove preflight is write-free.\"\"\"\n+    snapshot: dict[str, tuple[str, bytes | str]] = {}\n+    for path in sorted(root.rglob(\"*\")):\n+        relative = path.relative_to(root).as_posix()\n+        if path.is_symlink():\n+            snapshot[relative] = (\"symlink\", path.readlink().as_posix())\n+        elif path.is_dir():\n+            snapshot[relative] = (\"directory\", \"\")\n+        else:\n+            snapshot[relative] = (\"file\", path.read_bytes())\n+    return snapshot\n+\n+\n+class FreshSadrInstallTests(unittest.TestCase):\n+    def setUp(self) -> None:\n+        self.tmp = Path(tempfile.mkdtemp(prefix=\"shiki-sadr-fresh-\"))\n+        self.addCleanup(lambda: shutil.rmtree(self.tmp, ignore_errors=True))\n+        self.target = self.tmp / \"target\"\n+        self.adr_dir = self.target / \"docs\" / \"adr\"\n+        self.adr_dir.mkdir(parents=True)\n+        self.product_bytes = {\n+            f\"{number:04d}-product-decision.md\": (\n+                f\"# Product ADR {number:04d}\\n\\nTarget-owned bytes {number}.\\n\"\n+            ).encode()\n+            for number in PRODUCT_NUMBERS\n+        }\n+        for name, content in self.product_bytes.items():\n+            (self.adr_dir / name).write_bytes(content)\n+\n+    def test_fresh_install_preserves_product_adrs_and_installs_all_sadrs(self) -> None:\n+        _quiet_install(self.target, force=False, validate=True)\n+\n+        for name, expected in self.product_bytes.items():\n+            self.assertEqual((self.adr_dir / name).read_bytes(), expected)\n+\n+        sadr_numbers = {\n+            path.name.split(\"-\", 2)[1]\n+            for path in self.adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\")\n+        }\n+        self.assertEqual(sadr_numbers, {f\"{number:04d}\" for number in range(1, 20)})\n+\n+        numeric_records = {\n+            path.name\n+            for path in self.adr_dir.glob(\"[0-9][0-9][0-9][0-9]-*.md\")\n+        }\n+        self.assertEqual(numeric_records, set(self.product_bytes))\n+        stamp = json.loads(\n+            (self.target / shiki_installer.INSTALL_STAMP_PATH).read_text(\n+                encoding=\"utf-8\"\n+            )\n+        )\n+        for path in self.adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\"):\n+            relative = path.relative_to(self.target).as_posix()\n+            self.assertEqual(stamp[\"digests\"][relative], _sha256(path))\n+        self.assertEqual(find_adr_reference_violations(self.target), [])\n+\n+\n+class LegacySadrUpgradeTests(unittest.TestCase):\n+    def setUp(self) -> None:\n+        self.tmp = Path(tempfile.mkdtemp(prefix=\"shiki-sadr-upgrade-\"))\n+        self.addCleanup(lambda: shutil.rmtree(self.tmp, ignore_errors=True))\n+        self.target = self.tmp / \"target\"\n+        self.adr_dir = self.target / \"docs\" / \"adr\"\n+        self.product_bytes, self.legacy_paths = _create_legacy_upgrade_fixture(\n+            self.target\n+        )\n+\n+    def test_force_upgrade_deletes_only_stamped_legacy_sadrs(self) -> None:\n+        _quiet_install(self.target, force=True, validate=True)\n+\n+        for relative in self.legacy_paths:\n+            self.assertFalse((self.target / relative).exists(), relative)\n+        for name, expected in self.product_bytes.items():\n+            self.assertEqual((self.adr_dir / name).read_bytes(), expected)\n+\n+        sadrs = list(self.adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\"))\n+        self.assertEqual(len(sadrs), 19)\n+        refreshed = json.loads(\n+            (self.target / shiki_installer.INSTALL_STAMP_PATH).read_text(encoding=\"utf-8\")\n+        )\n+        for relative in self.legacy_paths:\n+            self.assertNotIn(relative, refreshed[\"digests\"])\n+        for path in sadrs:\n+            relative = path.relative_to(self.target).as_posix()\n+            self.assertEqual(refreshed[\"digests\"][relative], _sha256(path))\n+\n+    def test_non_force_install_never_deletes_legacy_files(self) -> None:\n+        before = {\n+            relative: (self.target / relative).read_bytes()\n+            for relative in self.legacy_paths\n+        }\n+        _quiet_install(self.target, force=False, validate=False)\n+        for relative, expected in before.items():\n+            self.assertEqual((self.target / relative).read_bytes(), expected)\n+        refreshed = json.loads(\n+            (self.target / shiki_installer.INSTALL_STAMP_PATH).read_text(encoding=\"utf-8\")\n+        )\n+        for relative in self.legacy_paths:\n+            self.assertEqual(refreshed[\"digests\"][relative], _sha256(self.target / relative))\n+\n+\n+class LegacyCleanupPreflightTests(unittest.TestCase):\n+    def setUp(self) -> None:\n+        self.tmp = Path(tempfile.mkdtemp(prefix=\"shiki-sadr-preflight-\"))\n+        self.addCleanup(lambda: shutil.rmtree(self.tmp, ignore_errors=True))\n+\n+    def test_all_ownership_blockers_abort_before_any_write(self) -> None:\n+        cases = (\n+            (\"absent\", \"install stamp is absent\"),\n+            (\"unreadable\", \"install stamp is unreadable\"),\n+            (\"malformed\", \"install stamp is malformed\"),\n+            (\"missing-digest\", \"install stamp has no digest\"),\n+            (\"digest-mismatch\", \"digest mismatch\"),\n+        )\n+        for name, expected_reason in cases:\n+            with self.subTest(name=name):\n+                target = self.tmp / name\n+                _, legacy_paths = _create_legacy_upgrade_fixture(target)\n+                stamp_path = target / shiki_installer.INSTALL_STAMP_PATH\n+\n+                if name == \"absent\":\n+                    stamp_path.unlink()\n+                elif name == \"unreadable\":\n+                    stamp_path.unlink()\n+                    stamp_path.mkdir()\n+                elif name == \"malformed\":\n+                    stamp_path.write_text(\"{not-json\\n\", encoding=\"utf-8\")\n+                else:\n+                    stamp = json.loads(stamp_path.read_text(encoding=\"utf-8\"))\n+                    if name == \"missing-digest\":\n+                        stamp[\"digests\"] = {}\n+                    else:\n+                        stamp[\"digests\"] = {\n+                            relative: \"deadbeef\" for relative in legacy_paths\n+                        }\n+                    stamp_path.write_text(\n+                        json.dumps(stamp, indent=2, sort_keys=True) + \"\\n\",\n+                        encoding=\"utf-8\",\n+                    )\n+\n+                before = _tree_snapshot(target)\n+                with self.assertRaises(shiki_installer.ShikiError) as raised:\n+                    _quiet_install(target, force=True, validate=False)\n+                after = _tree_snapshot(target)\n+\n+                self.assertEqual(before, after, f\"{name} preflight changed the tree\")\n+                message = str(raised.exception)\n+                self.assertIn(expected_reason, message)\n+                self.assertIn(\"No files were written\", message)\n+                for relative in legacy_paths:\n+                    self.assertIn(relative, message)\n+\n+    def test_absent_stamp_is_allowed_when_no_legacy_path_exists(self) -> None:\n+        target = self.tmp / \"no-legacy\"\n+        target.mkdir()\n+        _quiet_install(target, force=True, validate=False)\n+        self.assertTrue(\n+            (target / \"docs\" / \"adr\" / \"SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\").is_file()\n+        )\n+\n+\n+if __name__ == \"__main__\":\n+    unittest.main()\n\ndiff --git a/.codex/skills/shiki/SKILL.md b/.codex/skills/shiki/SKILL.md\nindex c8e2e85d9d75d49cc4f2d6286ee22659c4e53e75..93cded623b2c15d9f65c76491a1c2d813ac9e6c0\n--- a/.codex/skills/shiki/SKILL.md\n+++ b/.codex/skills/shiki/SKILL.md\n@@ -18,6 +18,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n Codex CLI does not register this skill as a custom `/shiki` slash command. When\n the user types `/shiki` in Codex CLI and gets \"Unrecognized command\", explain\n that this is expected: invoke Shiki in Codex with natural language such as\n@@ -69,7 +72,7 @@\n \n ## Responsibilities\n \n-- Claude Code implements and repairs by default (`shiki runner claude`, ADR 0008); Codex implements and repairs tasks explicitly assigned to `codex`.\n+- Claude Code implements and repairs by default (`shiki runner claude`, SADR-0008); Codex implements and repairs tasks explicitly assigned to `codex`.\n - Claude Code Action can act as GitHub-side CCA or reviewer.\n - CCA judges completion.\n - MergeGate authorizes state transitions and merge readiness.\ndiff --git a/.github/prompts/cca-completion-check.md b/.github/prompts/cca-completion-check.md\nindex ed203ad5f302ecc81372d8ab27d2b1232ca97497..fd89950528333ef6242e6d5443904f5392ab7e56\n--- a/.github/prompts/cca-completion-check.md\n+++ b/.github/prompts/cca-completion-check.md\n@@ -19,6 +19,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n ## Required Reading\n \n Read only the files needed to judge the PR contract. Prefer this order:\ndiff --git a/.github/prompts/codex-task-handoff.md b/.github/prompts/codex-task-handoff.md\nindex 695e211d76dea1c60e8d5bb360e78487808286a4..59441d576b01a39569722f226c854e5a740e9eb5\n--- a/.github/prompts/codex-task-handoff.md\n+++ b/.github/prompts/codex-task-handoff.md\n@@ -2,7 +2,7 @@\n \n You are Codex Front acting as the Implementer for a Shiki task.\n \n-Follow `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant ADRs, and `docs/agents/codex-handoff.md`.\n+Follow `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant Shiki SADRs and target ADRs, and `docs/agents/codex-handoff.md`.\n \n Rules:\n \ndiff --git a/.shiki/ledger/L-20260807T082800000000Z-c0de0019.json b/.shiki/ledger/L-20260807T082800000000Z-c0de0019.json\nnew file mode 100644\nindex 0000000000000000000000000000000000000000..bac355ecea44037859b3715f58bc32f3353cfcfa\n--- /dev/null\n+++ b/.shiki/ledger/L-20260807T082800000000Z-c0de0019.json\n@@ -0,0 +1,34 @@\n+{\n+  \"actor\": \"codex\",\n+  \"evidence\": [\n+    \"Full task diff reviewed against scope, non-goals, locks, acceptance checks, failure atomicity, path ownership, and compatibility requirements\",\n+    \"Finding repaired: tests/test_sadr_install_migration.py now asserts every fresh-installed SADR path has the matching SHA-256 digest in .shiki/install-stamp.json\",\n+    \"Re-review found no remaining blocking issues; rejected findings: none\",\n+    \"python3 -m unittest discover -s tests: 1147 tests passed\",\n+    \"bash scripts/test_shiki_control_plane.sh: passed\",\n+    \"bash scripts/test_shiki_adversarial_state.sh: passed\",\n+    \"python3 scripts/validate_shiki.py --target .: passed\"\n+  ],\n+  \"goal_id\": \"G-20260807T073307219752Z-1ee1ed68\",\n+  \"id\": \"L-20260807T082800000000Z-c0de0019\",\n+  \"links\": [],\n+  \"review\": {\n+    \"applied_fixes\": [\n+      \"Added direct fresh-install stamp path/digest assertions and reran focused plus full verification\"\n+    ],\n+    \"findings\": [\n+      {\n+        \"detail\": \"The fresh-install test proved SADR files were installed but did not directly prove their paths and digests were recorded in the refreshed install stamp.\",\n+        \"resolution\": \"fixed_and_re_reviewed\",\n+        \"severity\": \"blocking\",\n+        \"title\": \"Fresh-install stamp evidence was indirect\"\n+      }\n+    ],\n+    \"rejected_findings\": [],\n+    \"verdict\": \"clean_after_repairs\"\n+  },\n+  \"summary\": \"code-review implementer verdict CLEAN after re-review: findings=1; applied_fixes=1 (fresh-install stamp-digest assertion); rejected_findings=0 (none)\",\n+  \"task_id\": \"T-20260807T073307221254Z-2d22d654\",\n+  \"timestamp\": \"2026-08-07T08:28:00+00:00\",\n+  \"type\": \"review\"\n+}\ndiff --git a/.shiki/tasks/T-20260807T073307221254Z-2d22d654.json b/.shiki/tasks/T-20260807T073307221254Z-2d22d654.json\nindex e7e150cc1637f1cfac2199e04055e93b53630dcb..78f7fb5ef2ab3dad7e3731673ac2c34b2a8ed149\n--- a/.shiki/tasks/T-20260807T073307221254Z-2d22d654.json\n+++ b/.shiki/tasks/T-20260807T073307221254Z-2d22d654.json\n@@ -36,7 +36,8 @@\n     \"L-20260807T080107948254Z-61f24da0\",\n     \"L-20260807T080228312210Z-07ac626e\",\n     \"L-20260807T080228575038Z-d574f629\",\n-    \"L-20260807T080256330734Z-5bf40cb6\"\n+    \"L-20260807T080256330734Z-5bf40cb6\",\n+    \"L-20260807T082800000000Z-c0de0019\"\n   ],\n   \"locks\": [\n     \"path:.codex/**\",\ndiff --git a/AGENTS.md b/AGENTS.md\nindex 7f041100d49c2a7525f51cb0f5d696a4a6d1c78c..a06bfbabc6fa48a007389e25e12d7ed523936bed\n--- a/AGENTS.md\n+++ b/AGENTS.md\n@@ -22,7 +22,7 @@\n - **Codex Front** is an optional implementer through Codex App, Codex CLI, Codex IDE extension, or Codex Web signed in with ChatGPT OAuth/subscription auth, dispatched with `shiki runner codex` when a task is explicitly assigned to `codex`.\n - **GitHub CCA** is implemented by Claude Code Action by default, using `CLAUDE_CODE_OAUTH_TOKEN`.\n \n-Do not assume `openai/codex-action`, `OPENAI_API_KEY`, or API-key based Codex automation in the default Shiki path. API-key runners are explicit target-repo extensions and require their own ADR.\n+Do not assume `openai/codex-action`, `OPENAI_API_KEY`, or API-key based Codex automation in the default Shiki path. API-key runners are explicit target-repo extensions and require their own target ADR.\n \n Shiki is not:\n \n@@ -44,10 +44,14 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use\n+`SADR-NNNN-*.md`; target product decisions retain `NNNN-*.md` and the `ADR`\n+namespace.\n+\n Use the canonical source-of-truth order above. Runtime-specific reading orders\n are operational guidance only and must not override or reorder it.\n \n-Conversation state is not durable truth. If a decision matters, put it in GitHub, `.shiki/`, `CONTEXT.md`, or an ADR.\n+Conversation state is not durable truth. If a decision matters, put it in GitHub, `.shiki/`, `CONTEXT.md`, a Shiki SADR, or a target ADR.\n \n If GitHub and `.shiki/` disagree, surface the conflict and prefer GitHub operational state until the mirror is repaired.\n \n@@ -75,8 +79,8 @@\n Every non-trivial change follows this loop:\n \n 1. **Goal**: clarify outcome, completion conditions, non-goals, risk level, and success signals.\n-2. **grill-with-docs**: challenge the plan against domain docs, ADRs, code reality, terminology, and edge scenarios.\n-3. **Context & Impact**: find relevant docs, ADRs, code, dependencies, owners, locks, and verification surfaces.\n+2. **grill-with-docs**: challenge the plan against domain docs, Shiki SADRs, target ADRs, code reality, terminology, and edge scenarios.\n+3. **Context & Impact**: find relevant docs, SADRs, target ADRs, code, dependencies, owners, locks, and verification surfaces.\n 4. **PRD and Spec Freeze**: use `to-prd` when context is settled enough to become durable product/engineering intent. The operator's explicit approval of the PRD is Spec Freeze: the plan records a `spec_freeze` block, and no plan runs without it. Steps 1-4 form Requirements Definition \u2014 one continuous operator dialogue. After Spec Freeze, scope changes require an operator-approved Spec Amendment; non-scope-moving interpretations go to the Assumption Log.\n 5. **Issues**: use `to-issues` to create independently grabbable vertical-slice issues. Prefer AFK slices over HITL slices where possible.\n 6. **Triage**: label issues for readiness, risk, runtime, skills, and MergeGate state.\n@@ -167,7 +171,7 @@\n - Testing decisions.\n - Out-of-scope items.\n - Further notes.\n-- Links to relevant domain terms and ADRs.\n+- Links to relevant domain terms, SADRs, and target ADRs.\n \n Every executable task must include:\n \n@@ -193,7 +197,7 @@\n \n Context & Impact must identify:\n \n-- Relevant documents, domain terms, ADRs, and past decisions.\n+- Relevant documents, domain terms, SADRs, target ADRs, and past decisions.\n - Relevant modules, interfaces, seams, callers, owners, and tests.\n - Dependency relationships between tasks.\n - Candidate locks and lock conflicts.\n@@ -210,7 +214,7 @@\n | Skill | Required when |\n | --- | --- |\n | `setup-matt-pocock-skills` | First configuring the repo for skills, or when issue tracker, triage labels, or domain docs are missing. |\n-| `grill-with-docs` | Default for non-trivial Goals before PRD/issues; always required when plans, terminology, boundaries, tradeoffs, or ADR-worthy decisions are ambiguous. |\n+| `grill-with-docs` | Default for non-trivial Goals before PRD/issues; always required when plans, terminology, boundaries, tradeoffs, or decision-record-worthy choices are ambiguous. |\n | `zoom-out` | The agent lacks an architectural map of the relevant code area. |\n | `to-prd` | Settled Goal context must become durable product/engineering intent. |\n | `to-issues` | A Goal, PRD, or plan must become independently grabbable vertical-slice issues. |\n@@ -348,7 +352,7 @@\n - Three or more new files.\n - Any high-risk or critical label.\n \n-When the architecture gate triggers, run `grill-with-docs` or `improve-codebase-architecture` as appropriate and record durable decisions in an ADR when needed.\n+When the architecture gate triggers, run `grill-with-docs` or `improve-codebase-architecture` as appropriate and record durable decisions in a Shiki SADR or target ADR as applicable.\n \n ## Engineering Discipline\n \ndiff --git a/CLAUDE.md b/CLAUDE.md\nindex 98a932e7841a632866fcd53b0bad0e3af3711d3d..98a31399404d6fb044b4fd493f4459ac87f439de\n--- a/CLAUDE.md\n+++ b/CLAUDE.md\n@@ -14,6 +14,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n ## Claude Role\n \n Default to planner, default implementer, reviewer, coordinator, documentation editor, CCA-style completion judge, and final-judgment assistant.\n@@ -57,7 +60,7 @@\n At the start of a Shiki session:\n \n 1. Read `AGENTS.md`.\n-2. Read `CONTEXT.md` and relevant ADRs.\n+2. Read `CONTEXT.md` and relevant Shiki SADRs and target ADRs.\n 3. Read active `.shiki/` Goal/task/ledger state when present.\n 4. Read active distilled rules from `.shiki/memories/` (status `distilled`, `active: true`, not revoked or superseded) for applicable guidance; the same rules are injected deterministically into task handoffs as the `## Distilled Rules` section (\u00a73.5).\n 5. Read the GitHub Issue or PR when the task is GitHub-backed.\n@@ -74,7 +77,7 @@\n - Define the Goal, completion conditions, non-goals, and risk level.\n - Treat `grill-with-docs` as the default planning entry point for non-trivial Goals.\n - Challenge domain terms against `CONTEXT.md`.\n-- Challenge hard-to-reverse tradeoffs against ADRs.\n+- Challenge hard-to-reverse tradeoffs against SADRs and target ADRs.\n - Explore code instead of asking when code can answer the question.\n - Run Context & Impact before implementation planning.\n - Produce a Task DAG, not an unordered checklist.\n@@ -94,7 +97,7 @@\n - For each question, provide your recommended answer.\n - Resolve dependency decisions before downstream design questions.\n - Update `CONTEXT.md` inline when terminology crystallizes.\n-- Offer ADRs only when the decision is hard to reverse, surprising without context, and a real tradeoff.\n+- Offer a SADR or target ADR only when the decision is hard to reverse, surprising without context, and a real tradeoff.\n - Convert unresolved questions into HITL blockers rather than allowing Codex to guess.\n \n ## PRD and Issues Standard\n@@ -103,7 +106,7 @@\n \n - Use the project's domain glossary vocabulary.\n - Avoid volatile file paths unless they are essential.\n-- Include implementation decisions, testing decisions, out-of-scope items, and relevant ADR/domain links.\n+- Include implementation decisions, testing decisions, out-of-scope items, and relevant SADR/target-ADR/domain links.\n - Identify deep-module opportunities and testable interfaces.\n \n When creating issues:\n@@ -121,7 +124,7 @@\n - Branch/worktree target.\n - Scope and non-goals.\n - Dependencies and locks.\n-- Relevant docs, ADRs, modules, and tests.\n+- Relevant docs, SADRs, target ADRs, modules, and tests.\n - Required skill invocations.\n - TDD expectations.\n - Pre-PR `code-review` gate expectations.\n@@ -152,7 +155,7 @@\n \n - Treat yourself as a judge, not implementer.\n - Do not write production code.\n-- Read `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant ADRs, PRD, task issue, PR body, diff, checks, reviews, labels, and `.shiki/` state.\n+- Read `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant SADRs and target ADRs, PRD, task issue, PR body, diff, checks, reviews, labels, and `.shiki/` state.\n - Evaluate every applicable checklist in `docs/agents/checklists.md`.\n - Map each acceptance criterion to durable evidence.\n - Distinguish implementation failure from missing evidence.\ndiff --git a/CONTEXT.md b/CONTEXT.md\nindex e61a9be06a0cce0d026484083a657bde2067e234..237ee8aa3ea8fab334ece49768bc5ed3b5af0974\n--- a/CONTEXT.md\n+++ b/CONTEXT.md\n@@ -25,7 +25,7 @@\n _Avoid_: jumping directly into code\n \n **grill-with-docs**:\n-A planning skill that challenges a plan against domain language, ADRs, code reality, and concrete edge scenarios. It resolves design-tree questions before PRD/issues. It runs inside Requirements Definition.\n+A planning skill that challenges a plan against domain language, Shiki SADRs, target ADRs, code reality, and concrete edge scenarios. It resolves design-tree questions before PRD/issues. It runs inside Requirements Definition.\n _Avoid_: generic brainstorming, silent assumptions\n \n **Requirements Definition**:\ndiff --git a/CONTRIBUTING.md b/CONTRIBUTING.md\nindex c6d9b656e73abb8a55e5f660123e03153dcb3e6d..9e37a8fbf668db5b22461aaae5548b8f1354394c\n--- a/CONTRIBUTING.md\n+++ b/CONTRIBUTING.md\n@@ -17,7 +17,7 @@\n 1. **Goal** \u2014 start from a user-approved Goal with an outcome, completion\n    conditions, non-goals, risk level, and success signals.\n 2. **grill-with-docs** \u2014 for any non-trivial change, challenge the plan against\n-   domain language, ADRs, and code reality before writing a PRD.\n+   domain language, Shiki SADRs, target ADRs, and code reality before writing a PRD.\n 3. **PRD** \u2014 capture settled product/engineering intent (`to-prd`).\n 4. **Spec Freeze** \u2014 the operator explicitly approves the PRD/requirements;\n    the plan records a `spec_freeze` block and no plan runs without it\n@@ -94,7 +94,7 @@\n   entries.\n - Do not hand-edit `.shiki/goals/*.json` or `.shiki/manifest.json` in a task PR.\n - Conversation state is not durable truth. If a decision matters, record it in\n-  GitHub, `.shiki/`, `CONTEXT.md`, or an ADR.\n+  GitHub, `.shiki/`, `CONTEXT.md`, a Shiki SADR, or a target ADR.\n \n ## Architecture Gate\n \n@@ -103,7 +103,7 @@\n or CI policy, package structure, three or more structural files, more than 100\n net new lines in a core module, three or more new files, or any high-risk label.\n When triggered, run `grill-with-docs` or `improve-codebase-architecture` and\n-record durable decisions in an ADR when needed (see `docs/adr/`).\n+record durable decisions in a Shiki SADR or target ADR as applicable (see `docs/adr/`).\n \n ## Safety\n \ndiff --git a/SYSTEM_PROMPT.md b/SYSTEM_PROMPT.md\nindex aaaea45e3a896c3a44ba89eec8bb84325ecf6c62..da009c86a88285269dee0155e58e550c6d744ea9\n--- a/SYSTEM_PROMPT.md\n+++ b/SYSTEM_PROMPT.md\n@@ -30,6 +30,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n Use the canonical source-of-truth order above. Runtime-specific reading orders\n are operational guidance only and must not override or reorder it. The current\n conversation is non-durable operator input.\n@@ -48,14 +51,14 @@\n \n A runtime may only do work that is explicitly assigned by the Goal, task, PR, or MergeGate decision.\n \n-Do not assume `openai/codex-action`, `OPENAI_API_KEY`, or API-key based Codex automation in the default Shiki path. API-key runners are explicit target-repo extensions and require their own ADR.\n+Do not assume `openai/codex-action`, `OPENAI_API_KEY`, or API-key based Codex automation in the default Shiki path. API-key runners are explicit target-repo extensions and require their own target ADR.\n \n ## 4. Non-negotiable Shiki flow\n \n For any non-trivial Goal, use this flow:\n \n 1. **Goal Seek**: define outcome, non-goals, risk level, completion criteria, and evidence requirements.\n-2. **grill-with-docs**: challenge the plan against `CONTEXT.md`, ADRs, code reality, terminology, boundaries, and edge scenarios. Ask one question at a time when the operator is available. If code can answer a question, inspect code instead of asking.\n+2. **grill-with-docs**: challenge the plan against `CONTEXT.md`, Shiki SADRs, target ADRs, code reality, terminology, boundaries, and edge scenarios. Ask one question at a time when the operator is available. If code can answer a question, inspect code instead of asking.\n 3. **Context & Impact**: identify modules, interfaces, seams, callers, dependencies, locks, risk, and verification surfaces.\n 4. **to-prd + Spec Freeze**: turn settled context into a PRD using domain vocabulary and testing decisions. The operator's explicit approval of the PRD is Spec Freeze, recorded as the plan's `spec_freeze` block; plans without it do not run. Steps 1-4 form Requirements Definition.\n 5. **to-issues**: decompose the PRD into independently grabbable vertical-slice issues. Prefer AFK slices; mark HITL slices when judgment is still required.\n@@ -75,7 +78,7 @@\n Before making any material change:\n \n 1. Read `AGENTS.md` and any runtime-specific wrapper such as `CLAUDE.md`.\n-2. Read `CONTEXT.md` and relevant ADRs.\n+2. Read `CONTEXT.md` and relevant SADRs and target ADRs.\n 3. Read the GitHub Issue / PR and `.shiki/` state for the active Goal or task.\n 4. Identify your role: Planner, Implementer, Reviewer, Completion Check Agent, Repairer, or Guardian-assist.\n 5. Identify Goal id, task id, branch, PR, dependencies, locks, risk level, required skills, and required evidence.\n@@ -103,7 +106,7 @@\n \n At minimum, identify:\n \n-- Relevant documents, ADRs, domain terms, and prior decisions.\n+- Relevant documents, SADRs, target ADRs, domain terms, and prior decisions.\n - Relevant modules, interfaces, seams, callers, owners, and tests.\n - Dependency relationships between tasks.\n - Candidate file locks and lock conflicts.\n@@ -152,7 +155,7 @@\n \n CCA must read:\n \n-- `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant ADRs.\n+- `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant SADRs and target ADRs.\n - Parent Goal issue, PRD issue, task issue, comments, and labels.\n - PR body, diff, commits, changed files, reviews, CI checks, and artifacts available to the workflow.\n - `.shiki/` task contract, locks, ledger entries, CCA checklist profile, and prior repair packets.\n@@ -214,7 +217,7 @@\n - Three or more new files.\n - Any high-risk or critical label.\n \n-When the architecture gate triggers, run `grill-with-docs` or `improve-codebase-architecture` as appropriate and record the decision in an ADR if it is hard to reverse or likely to be re-litigated.\n+When the architecture gate triggers, run `grill-with-docs` or `improve-codebase-architecture` as appropriate and record the decision in a Shiki SADR or target ADR as applicable if it is hard to reverse or likely to be re-litigated.\n \n ## 14. Skill Gate\n \n@@ -223,7 +226,7 @@\n Use these skills:\n \n - `setup-matt-pocock-skills`: before first use of the engineering skill set, or when issue tracker, triage labels, or domain docs are missing.\n-- `grill-with-docs`: default for non-trivial Goals before PRD/issues; required for ambiguous plans, terminology, boundaries, ADR-worthy tradeoffs, or design-tree decisions.\n+- `grill-with-docs`: default for non-trivial Goals before PRD/issues; required for ambiguous plans, terminology, boundaries, decision-record-worthy tradeoffs, or design-tree decisions.\n - `zoom-out`: unfamiliar code area or missing architectural map.\n - `to-prd`: convert settled context into a PRD.\n - `to-issues`: break a PRD, plan, or Goal into independently grabbable vertical-slice issues.\ndiff --git a/docs/adr/0001-keep-shiki-as-platform-name.md b/docs/adr/SADR-0001-keep-shiki-as-platform-name.md\nindex 20a9637c3b9d8c97de7ba8db7cfae4d82d9a7dba..8b685439959aada1b221c112f0ac3c51ca8a1695\n--- a/docs/adr/0001-keep-shiki-as-platform-name.md\n+++ b/docs/adr/SADR-0001-keep-shiki-as-platform-name.md\n@@ -1,3 +1,3 @@\n-# Keep Shiki As The Platform Name\n+# SADR-0001: Keep Shiki As The Platform Name\n \n Shiki remains the product name for the GitHub-native agentic engineering control plane. We considered more descriptive names such as MergePilot, MergeGate, and Kiroku, but kept Shiki because it carries the existing goal-seeking framework identity while remaining independent of Hermes, Codex, Claude Code, or any single runtime.\ndiff --git a/docs/adr/0002-use-agents-as-shared-constitution.md b/docs/adr/SADR-0002-use-agents-as-shared-constitution.md\nindex 73de30ecb078120eac540d9bc51855e3cd48d0e3..c5486182bf06fca36ea508496b013c67302fd989\n--- a/docs/adr/0002-use-agents-as-shared-constitution.md\n+++ b/docs/adr/SADR-0002-use-agents-as-shared-constitution.md\n@@ -1,3 +1,3 @@\n-# Use AGENTS As The Shared Agent Constitution\n+# SADR-0002: Use AGENTS As The Shared Agent Constitution\n \n Shiki uses `AGENTS.md` as the runtime-neutral constitution and `CLAUDE.md` as a thin Claude Code wrapper. This keeps Codex, Claude Code, Hermes Runner, and future Agent Runtimes aligned on the same GitHub-first MergeGate rules while still allowing runtime-specific instructions where needed.\ndiff --git a/docs/adr/0003-require-skill-gate-for-mergegate-readiness.md b/docs/adr/SADR-0003-require-skill-gate-for-mergegate-readiness.md\nindex 424be0c9af4c8f0136f1fde68a468c47ff84e764..7fc337987c7df9792f75f181dfcbed75fe5ffff2\n--- a/docs/adr/0003-require-skill-gate-for-mergegate-readiness.md\n+++ b/docs/adr/SADR-0003-require-skill-gate-for-mergegate-readiness.md\n@@ -1,3 +1,3 @@\n-# Require Skill Gate For MergeGate Readiness\n+# SADR-0003: Require Skill Gate For MergeGate Readiness\n \n Shiki requires relevant engineering skills to be invoked before execution when their triggers apply. This makes skills part of MergeGate readiness rather than optional prompt style, because Shiki depends on repeatable planning, issue decomposition, TDD, diagnosis, triage, and architecture review before autonomous or parallel agent work can be trusted.\ndiff --git a/docs/adr/0004-use-github-first-with-shiki-mirror.md b/docs/adr/SADR-0004-use-github-first-with-shiki-mirror.md\nindex d0a3a9174dc646a945a9d325954ffaf00c352040..a79c54ae9ee8eed2a0524192b864da4e22a92d3f\n--- a/docs/adr/0004-use-github-first-with-shiki-mirror.md\n+++ b/docs/adr/SADR-0004-use-github-first-with-shiki-mirror.md\n@@ -1,4 +1,4 @@\n-# ADR 0004: Use GitHub First With A Repository-Local Shiki Mirror\n+# SADR-0004: Use GitHub First With A Repository-Local Shiki Mirror\n \n ## Status\n \ndiff --git a/docs/adr/0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md b/docs/adr/SADR-0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\nindex b339baf49b89d9290e79c3d51b36e7b2bde3bd95..0f3c3eb9afccdb3403ce978e21d04fe12e495da0\n--- a/docs/adr/0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\n+++ b/docs/adr/SADR-0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\n@@ -1,8 +1,8 @@\n-# ADR 0005: Use Codex As Front And Claude Code Action As The GitHub Runtime\n+# SADR-0005: Use Codex As Front And Claude Code Action As The GitHub Runtime\n \n ## Status\n \n-Accepted (implementer default superseded by ADR 0008; the auth model and the\n+Accepted (implementer default superseded by SADR-0008; the auth model and the\n Claude Code Action GitHub runtime decision remain in force)\n \n ## Context\n@@ -26,4 +26,4 @@\n - Shiki avoids requiring `OPENAI_API_KEY` for the default workflow.\n - Codex work happens through the user's authenticated Codex front surface, not as a hidden backend Action.\n - Claude Code Action is configured for OAuth-token based subscription usage by default.\n-- API-key based automation remains possible only as an explicit target-repo extension with its own ADR.\n+- API-key based automation remains possible only as an explicit target-repo extension with its own target ADR.\ndiff --git a/docs/adr/0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md b/docs/adr/SADR-0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\nindex 4bcae9d1e52f0a37b8e9fbafa5cd14b6ee1993bd..7a38538a6ae9929567f0ade67e7ea6a60d0b3f7e\n--- a/docs/adr/0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\n+++ b/docs/adr/SADR-0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\n@@ -1,4 +1,4 @@\n-# ADR 0006: Separate Codex Implementation, CCA Completion, And MergeGate Authorization\n+# SADR-0006: Separate Codex Implementation, CCA Completion, And MergeGate Authorization\n \n ## Status\n \ndiff --git a/docs/adr/0007-packaging-and-release.md b/docs/adr/SADR-0007-packaging-and-release.md\nindex 79b84ddb97167372b44641e5b7860e400ce80822..bb445a2e2e856d9d1f5fdbe35de8d4e8cec0fb32\n--- a/docs/adr/0007-packaging-and-release.md\n+++ b/docs/adr/SADR-0007-packaging-and-release.md\n@@ -1,4 +1,4 @@\n-# ADR 0007: Packaging And Release For Shiki\n+# SADR-0007: Packaging And Release For Shiki\n \n ## Status\n \n@@ -25,7 +25,7 @@\n   not introduce third-party runtime dependencies or break the shim.\n - Releases must run inside the operator's subscription-authenticated toolchain\n   and must not require extra secrets beyond the built-in `GITHUB_TOKEN`\n-  (consistent with ADR 0005's no-API-key-by-default stance).\n+  (consistent with SADR-0005's no-API-key-by-default stance).\n - `.shiki/manifest.json` already encodes the install surface (`install.include`,\n   `install.create_directories`, `install.exclude_from_commit`), and it must not\n   be mutated as part of this change.\n@@ -34,7 +34,7 @@\n \n This is a hard-to-reverse platform decision (it sets the version source of\n truth, the release trigger, and the public install contract), so it is recorded\n-as an ADR.\n+as a SADR.\n \n ## Decision\n \n@@ -104,7 +104,7 @@\n 3. **Publishing to PyPI.** Rejected for now: Shiki is distributed as a\n    repository template + git-installable CLI, not a PyPI library; PyPI would add\n    credentials/secrets and a different versioning contract. Can be added later as\n-   its own ADR if a packaged distribution is needed.\n+   its own SADR if a packaged distribution is needed.\n 4. **Dynamic version (read `VERSION` via a build backend `dynamic` field).**\n    Rejected to keep `pyproject.toml` backend-agnostic and dependency-free; the\n    small duplication between `VERSION` and `pyproject.toml` is guarded by the\n@@ -123,4 +123,4 @@\n   releasing versions the whole platform but ships only `install.include` to\n   targets.\n - Future PyPI distribution or a console entry point are deferred and would each\n-  warrant their own ADR.\n+  warrant their own SADR.\ndiff --git a/docs/adr/0008-use-claude-code-as-default-implementer.md b/docs/adr/SADR-0008-use-claude-code-as-default-implementer.md\nindex dedf4510b7c7799521422f622029563d373db031..35c2625d908167fbc58855b5b63e283805f167e8\n--- a/docs/adr/0008-use-claude-code-as-default-implementer.md\n+++ b/docs/adr/SADR-0008-use-claude-code-as-default-implementer.md\n@@ -1,12 +1,12 @@\n-# ADR 0008: Use Claude Code As The Default Implementer Runtime\n+# SADR-0008: Use Claude Code As The Default Implementer Runtime\n \n ## Status\n \n-Proposed (supersedes ADR 0005 for the implementer default; keeps its auth model)\n+Proposed (supersedes SADR-0005 for the implementer default; keeps its auth model)\n \n ## Context\n \n-ADR 0005 made Codex Front the default implementation runtime, with Claude Code\n+SADR-0005 made Codex Front the default implementation runtime, with Claude Code\n restricted to planner and reviewer roles by the runtime registry, and the only\n autonomous implementation adapter was `shiki runner codex`.\n \n@@ -18,13 +18,13 @@\n - The platform's planning, review, and CCA layers already run on Claude Code,\n   and Claude Code's native capabilities (the built-in `/code-review` skill and\n   the Workflow multi-agent orchestration tool) have no Codex equivalent.\n-- The Goal lifecycle redesign (ADR 0009) requires an autonomous post-freeze\n+- The Goal lifecycle redesign (SADR-0009) requires an autonomous post-freeze\n   loop driven from the operator's Claude Code session, including bounded\n   repair dispatch, which must not depend on a runtime that cannot be invoked\n   unattended.\n \n The constitution claims Shiki is runtime-agnostic (\"not a Claude-only\n-workflow\", ADR 0002). Any change to the implementer default must preserve the\n+workflow\", SADR-0002). Any change to the implementer default must preserve the\n registry-based runtime model rather than hardcoding one vendor.\n \n ## Decision\n@@ -39,7 +39,7 @@\n   the task to review or repair-needed.\n - Keep Codex registered as an optional implementer runtime. `shiki runner\n   codex`, the Codex handoff templates, and the ChatGPT OAuth auth model from\n-  ADR 0005 remain supported for tasks explicitly assigned to `codex`.\n+  SADR-0005 remain supported for tasks explicitly assigned to `codex`.\n - Commit to mandating two Claude-native capabilities as loop contract in\n   follow-up Goals (status: the code-review gate was delivered by Goal G-B \u2014\n   skill registry, checklists item PR-12, default required_skills \u2014 while the\n@@ -60,7 +60,7 @@\n \n Out of scope: removing Codex support, changing the CCA runtime (Claude Code\n Action with `CLAUDE_CODE_OAUTH_TOKEN` stays), and API-key based automation\n-(still requires its own ADR per ADR 0005).\n+(still requires its own target ADR per SADR-0005).\n \n ## Consequences\n \n@@ -100,7 +100,7 @@\n   harness rejection of direct `codex exec` is outside Shiki's control, and the\n   plan's mandatory native capabilities cannot run on Codex at all.\n - **Remove Codex entirely**: rejected; it abandons the runtime-agnostic\n-  constitution (ADR 0002) for no operational gain and forecloses\n+  constitution (SADR-0002) for no operational gain and forecloses\n   cross-runtime adversarial review.\n - **GitHub Actions cloud implementation via claude-code-action**: rejected as\n   default; durable evidence is automatic but token cost, Node 24 workflow\ndiff --git a/docs/adr/0009-requirements-definition-and-spec-freeze.md b/docs/adr/SADR-0009-requirements-definition-and-spec-freeze.md\nindex 29b3f7871bef9203a8b4a831a65c39eb2f506e42..52e7dd69ad0aa4efe0b68c62678abbf7df02aaec\n--- a/docs/adr/0009-requirements-definition-and-spec-freeze.md\n+++ b/docs/adr/SADR-0009-requirements-definition-and-spec-freeze.md\n@@ -1,4 +1,4 @@\n-# ADR 0009: Merge Grill And Planning Into Requirements Definition With Spec Freeze\n+# SADR-0009: Merge Grill And Planning Into Requirements Definition With Spec Freeze\n \n ## Status\n \ndiff --git a/docs/adr/0010-external-ai-guardian-review.md b/docs/adr/SADR-0010-external-ai-guardian-review.md\nindex e6108bbe8bb4e73e0ff4684c8f78001a80438c4e..f3510b145664298adafb151b6f0db51b972f01ec\n--- a/docs/adr/0010-external-ai-guardian-review.md\n+++ b/docs/adr/SADR-0010-external-ai-guardian-review.md\n@@ -1,4 +1,4 @@\n-# ADR 0010: External AI Guardian Review As A First-Class Approval Authority\n+# SADR-0010: External AI Guardian Review As A First-Class Approval Authority\n \n ## Status\n \n@@ -8,7 +8,7 @@\n \n Shiki is intended to be an autonomous, recursive, self-improving agent\n framework: authority-in-the-loop, not human-in-the-loop. Until now the Guardian\n-approval required for high/critical-risk merges (guardian-policy.json, ADR\n+approval required for high/critical-risk merges (guardian-policy.json, SADR\n governance) recognized only human authority sources \u2014 a GitHub review approval,\n the `guardian:approved` label applied by a configured Guardian, or a comment\n carrying `Guardian approval granted` plus the current head SHA.\n@@ -89,7 +89,7 @@\n   autonomous design the operator intends and produced the impersonation\n   incident.\n - **Record AI approval as an operator comment**: rejected as forgery of human\n-  authority \u2014 the exact failure this ADR corrects.\n+  authority \u2014 the exact failure this SADR corrects.\n - **Deliver the artifact as a committed `.shiki/` file**: rejected; a committed\n   approval inside the PR being approved is forgeable by anyone who can push.\n - **A dedicated reviewer bot identity instead of a relayed comment**: deferred;\ndiff --git a/docs/adr/0011-autonomous-quality-gate-evidence.md b/docs/adr/SADR-0011-autonomous-quality-gate-evidence.md\nindex bca0246c5d46e44afbde15486a75eec425fc02f5..9c93eac16d8c3a6d4b11bd7224e3955102cd7db4\n--- a/docs/adr/0011-autonomous-quality-gate-evidence.md\n+++ b/docs/adr/SADR-0011-autonomous-quality-gate-evidence.md\n@@ -1,4 +1,4 @@\n-# ADR 0011: Autonomous Quality-Gate Evidence \u2014 Deterministic Facts And An Independent Verifier\n+# SADR-0011: Autonomous Quality-Gate Evidence \u2014 Deterministic Facts And An Independent Verifier\n \n ## Status\n \ndiff --git a/docs/adr/0012-autonomous-loop-closeout-to-main.md b/docs/adr/SADR-0012-autonomous-loop-closeout-to-main.md\nindex 637fad6dde4989c9c2bf0f81f676d8d29991123c..ec7d85f81030a676bcbba28b43eb365422a36eac\n--- a/docs/adr/0012-autonomous-loop-closeout-to-main.md\n+++ b/docs/adr/SADR-0012-autonomous-loop-closeout-to-main.md\n@@ -1,4 +1,4 @@\n-# 0012 \u2014 Autonomous loop pushes goal completion to main via a closeout PR\n+# SADR-0012 \u2014 Autonomous loop pushes goal completion to main via a closeout PR\n \n - Status: accepted\n - Date: 2026-06-18\ndiff --git a/docs/adr/0013-bootstrap-grants-actions-approve-pull-requests.md b/docs/adr/SADR-0013-bootstrap-grants-actions-approve-pull-requests.md\nindex b6c69d64f1c5ced346b88d6859586715b3b5bd0e..cf48ff4a6716267f539ca58edd1a852e7c7a2d64\n--- a/docs/adr/0013-bootstrap-grants-actions-approve-pull-requests.md\n+++ b/docs/adr/SADR-0013-bootstrap-grants-actions-approve-pull-requests.md\n@@ -1,4 +1,4 @@\n-# ADR 0013: Bootstrap Grants GitHub Actions Permission To Create And Approve Pull Requests\n+# SADR-0013: Bootstrap Grants GitHub Actions Permission To Create And Approve Pull Requests\n \n ## Status\n \n@@ -31,7 +31,7 @@\n security tradeoff: it lets repository automation satisfy a human-review gate. In\n a multi-maintainer repo that could let a compromised or buggy workflow\n self-approve changes. So whether bootstrap turns it on by default \u2014 and with\n-what default token scope \u2014 is an ADR-worthy decision, not an implementation\n+what default token scope \u2014 is a SADR-worthy decision, not an implementation\n detail.\n \n ## Decision\n@@ -48,7 +48,7 @@\n Specifically:\n \n - **`can_approve_pull_request_reviews` defaults to `true`.** Shiki's default\n-  posture is solo/AFK autonomous operation (ADR 0008/0009/0012), where the CCA\n+  posture is solo/AFK autonomous operation (SADR-0008/0009/0012), where the CCA\n   Review Bridge is the only path that can satisfy `required_review: true` after\n   the CCA and Guardian gates have already passed. The default makes a\n   freshly-bootstrapped repo able to run the autonomous loop end to end without a\ndiff --git a/docs/adr/0014-codex-app-external-ai-guardian-ui-adapter.md b/docs/adr/SADR-0014-codex-app-external-ai-guardian-ui-adapter.md\nindex 238db06279334caa341c117362df9271382a7fcd..65f12e9939321e1b3e389a34c822fc1549e5155d\n--- a/docs/adr/0014-codex-app-external-ai-guardian-ui-adapter.md\n+++ b/docs/adr/SADR-0014-codex-app-external-ai-guardian-ui-adapter.md\n@@ -1,4 +1,4 @@\n-# ADR 0014: Use Codex App As The External AI Guardian UI Adapter\n+# SADR-0014: Use Codex App As The External AI Guardian UI Adapter\n \n ## Status\n \ndiff --git a/docs/adr/0015-contract-approval-before-dispatch.md b/docs/adr/SADR-0015-contract-approval-before-dispatch.md\nindex fad6c656741a20a7684ba16080a7db159f63a52f..6061271a46d3f1d6ae7174477de3fb75673123a6\n--- a/docs/adr/0015-contract-approval-before-dispatch.md\n+++ b/docs/adr/SADR-0015-contract-approval-before-dispatch.md\n@@ -1,4 +1,4 @@\n-# ADR 0015: Approve The Task Contract Before Dispatch, Not The Diff After It\n+# SADR-0015: Approve The Task Contract Before Dispatch, Not The Diff After It\n \n ## Status\n \n@@ -9,7 +9,7 @@\n Guardian approval for high/critical risk is expressible only on a pull request.\n Every approval source in `guardian-policy.json` \u2014 GitHub review, the\n `guardian:approved` label, the `Guardian approval granted` comment, and the\n-`external_ai_guardian_review` artifact (ADR 0010/0014) \u2014 is bound to a PR, and\n+`external_ai_guardian_review` artifact (SADR-0010/0014) \u2014 is bound to a PR, and\n three of the four require the current head SHA. Approval therefore cannot exist\n before an implementation PR exists.\n \n@@ -81,7 +81,7 @@\n An envelope breach must surface only on the non-repairable policy gate, never on\n the repairable `MergeGate metadata check`. Otherwise the Goal Loop routes it to\n `dispatch_repair` \u2014 instructing a runner to make the breach go away \u2014 which is\n-exactly what this ADR forbids.\n+exactly what this SADR forbids.\n \n **Contract immutability.** After registration, an implementation PR must not\n change the governance fields of its own task contract (`scope`, `non_goals`,\n@@ -116,7 +116,7 @@\n fails any verdict that is not `complete`, and the signal the CCA prompt reads is\n computed from `evaluate_guardian_approval` alone. Without the OR-merge at the\n signal, a contract-approved PR still returns `needs_guardian`, the CCA job fails,\n-and the MergeGate policy check never runs \u2014 the gate this ADR changes would never\n+and the MergeGate policy check never runs \u2014 the gate this SADR changes would never\n be reached. The CCA *prompt* needs no change: a `contract_approval` entry in the\n signal's existing `sources` array satisfies its current wording.\n \n@@ -130,7 +130,7 @@\n written, and an approved contract lets its implementation PRs merge through the\n same automatic path as low/medium risk. The wasted implement-then-block cycle\n disappears. Guardian review shifts from reading diffs to reading contracts, which\n-is both earlier and smaller; the external AI Guardian path (ADR 0010/0014) works\n+is both earlier and smaller; the external AI Guardian path (SADR-0010/0014) works\n on contracts with no change.\n \n We accept that the Guardian authorizes a class of change without seeing its\n@@ -146,7 +146,7 @@\n adversarial tests for the new approval path equivalent in rigor to\n `test_shiki_governance_evidence.sh`.\n \n-Bootstrap costs, which apply only while this ADR is being implemented. The tasks\n+Bootstrap costs, which apply only while this SADR is being implemented. The tasks\n that build Contract Approval merge under the existing rules, so the ones\n dispatched after the pre-dispatch gate lands but before the approval path lands\n need a Guardian-approved Contract PR *and* a live Guardian approval on their\n@@ -210,7 +210,7 @@\n risk; they do not bound authorized work.\n \n **Auto-escalate risk on envelope breach instead of stopping.** Returns the\n-workflow to a diff-time Guardian decision \u2014 the exact behavior this ADR removes.\n+workflow to a diff-time Guardian decision \u2014 the exact behavior this SADR removes.\n \n **Resolve governance fields from the base snapshot only.** Rejected against the\n registration flow: implementation PRs add their own contract, so the base has no\ndiff --git a/docs/adr/0016-locks-bound-product-paths.md b/docs/adr/SADR-0016-locks-bound-product-paths.md\nindex b5e850f3f1f7f4ef6e7f9d1763a29a51bd300c45..46b40a9b99a7aae644c02bb458f5473ff0a90ce2\n--- a/docs/adr/0016-locks-bound-product-paths.md\n+++ b/docs/adr/SADR-0016-locks-bound-product-paths.md\n@@ -1,4 +1,4 @@\n-# ADR 0016: Locks bound product paths; the Shiki mirror is bound by record identity\n+# SADR-0016: Locks bound product paths; the Shiki mirror is bound by record identity\n \n ## Status\n \n@@ -6,8 +6,8 @@\n \n - Date: 2026-07-28\n - Deciders: operator (mizutani-140), Claude (planner)\n-- Related: ADR 0004 (GitHub-first with a `.shiki` mirror), ADR 0009 (Spec Freeze),\n-  ADR 0012 (autonomous loop closeout to main), ADR 0015 (Contract Approval before\n+- Related: SADR-0004 (GitHub-first with a `.shiki` mirror), SADR-0009 (Spec Freeze),\n+  SADR-0012 (autonomous loop closeout to main), SADR-0015 (Contract Approval before\n   dispatch), remediation goal task R-02, the live PR #179 deadlock.\n \n ## Context\n@@ -90,11 +90,11 @@\n files, documentation files and named source modules are nameable; runtime-\n allocated ledger, runner and report ids are not \u2014 and those live in `.shiki`,\n which locks no longer govern. Existing frozen contracts are brought into line by\n-a one-time Spec Amendment per plan, which must land before ADR 0015's contract\n+a one-time Spec Amendment per plan, which must land before SADR-0015's contract\n immutability makes `locks` unwritable from the implementation PR.\n \n **Lock state is resolved from the durable side.** MergeGate resolves the lock\n-registry, and ADR 0015's governance fields, from the base `.shiki` snapshot when\n+registry, and SADR-0015's governance fields, from the base `.shiki` snapshot when\n present, falling back to head only when absent. The `MergeGate metadata check`\n job is given `--base-shiki`, which it does not receive today. Locks are not\n derived from the live open-pull-request set: a pull request body is untrusted\n@@ -108,7 +108,7 @@\n Explicitly out of scope: moving MergeGate's contract resolution off the PR\n checkout wholesale; making the mirror a projection of GitHub state; introducing\n new lock kinds; moving ledger evidence out of the repository; and unwinding\n-ADR 0012's closeout pull request. Registration of multi-task goals is ADR 0015's\n+SADR-0012's closeout pull request. Registration of multi-task goals is SADR-0015's\n Contract PR and is not re-decided here.\n \n ## Consequences\n@@ -127,7 +127,7 @@\n \n Risks accepted. First, goal-scoped identity is only as strong as `goal_id`,\n which is resolved from the PR body and the head task file; the mitigation is to\n-add `goal_id` to ADR 0015's frozen governance field set, and until that lands a\n+add `goal_id` to SADR-0015's frozen governance field set, and until that lands a\n task file can name a foreign goal. Second, base-when-present resolution is\n unavailable on fork pull requests, where the head is contributor-controlled;\n fork pull requests already cannot complete a Shiki merge, and this decision does\n@@ -139,7 +139,7 @@\n path, not by implementation pull requests.\n \n New obligations: one Spec Amendment per frozen plan to rewrite `locks`, ordered\n-before ADR 0015 T1; extension of the adversarial suites in\n+before SADR-0015 T1; extension of the adversarial suites in\n `scripts/test_shiki_adversarial_state.sh` and\n `scripts/test_shiki_governance_evidence.sh` to cover the eleven newly-scoped\n directories; and updating `scripts/test_shiki_loop_lock_guard.sh`,\n@@ -151,32 +151,32 @@\n whether the loop's transitive sync of ledger `evidence` references can be\n deleted once the Contract PR carries its own source plan.\n \n-## Relationship to ADR 0004\n+## Relationship to SADR-0004\n \n-This ADR **clarifies something ADR 0004 left unspecified**. ADR 0004 decides that\n+This SADR **clarifies something SADR-0004 left unspecified**. SADR-0004 decides that\n GitHub is the operational source of truth and that each target repository keeps a\n `.shiki/` mirror. It says nothing about pull requests writing the mirror, does\n not name MergeGate, and does not make the mirror an input to a gate. Nothing in\n-ADR 0004's Decision or Consequences is amended, superseded or weakened: the\n+SADR-0004's Decision or Consequences is amended, superseded or weakened: the\n mirror still exists, still records the same artifacts, and pull request\n-boundaries remain the coordination surface. What this ADR settles is the question\n+boundaries remain the coordination surface. What this SADR settles is the question\n 0004 never reached \u2014 by what authority a pull request may write a mirror record \u2014\n and it answers it the way `.shiki/manifest.json` already declares\n (`pr_mutation: current-task-or-current-goal-only`).\n \n-It **amends ADR 0012**, whose Decision item 5 states that \"Loop-executed tasks\n+It **amends SADR-0012**, whose Decision item 5 states that \"Loop-executed tasks\n declare the `path:.shiki/**` lock, which covers every `.shiki` file the closeout\n stages ... so `files_outside_locks` is satisfied without per-file lock\n bookkeeping.\" That clause is replaced: the closeout's mirror writes are\n-authorized by record identity, not by a declared lock. ADR 0012's substantive\n+authorized by record identity, not by a declared lock. SADR-0012's substantive\n decision \u2014 that completion reaches `main` through a closeout pull request rather\n than local attestation \u2014 is preserved unchanged.\n \n-It is **complementary to ADR 0015**, not a substitute. ADR 0015 owns contract\n+It is **complementary to SADR-0015**, not a substitute. SADR-0015 owns contract\n immutability, the pre-dispatch gate, the Contract PR and the Scope Envelope. This\n-ADR owns what a lock means and who may write the mirror. The one ordering\n+SADR owns what a lock means and who may write the mirror. The one ordering\n constraint between them is that the lock-narrowing Spec Amendment must precede\n-ADR 0015 T1's freezing of the `locks` field.\n+SADR-0015 T1's freezing of the `locks` field.\n \n ## Alternatives Considered\n \n@@ -186,7 +186,7 @@\n the only writer that can register them may run. Migrating the Guardian signal to\n that store makes every registration PR fail closed to `needs_guardian` at every\n risk level, because the task file is by definition absent from the store, which\n-removes the low/medium autonomous path ADR 0008/0009 exists to provide. And it\n+removes the low/medium autonomous path SADR-0008/0009 exists to provide. And it\n leaves schemas, manifest, config, guardian policy, templates, migrations, plans,\n worktrees, runner records, runs and handoffs with no writer at all in a\n repository whose product is `.shiki`. Measured benefit on the frozen backlog:\n@@ -199,14 +199,14 @@\n namespace is not in fact ownership-partitioned \u2014 56 ledger entries carry no\n `task_id`, and one goal DAG is writable by 35 registered tasks. The correct\n expression of its insight is that locks do not govern the mirror at all, which is\n-what this ADR decides.\n+what this SADR decides.\n \n **Make the mirror a projection written only by a registration writer and a\n post-merge projection job.** Rejected. It requires a bypass actor with write\n access to the protected default branch; it makes its own master invariant\n readable from a manifest that pull requests may edit; it deletes both reconcile\n modes and leaves a corrupted mirror with no in-band repair path, contradicting\n-ADR 0004's consequence that mirror conflicts are repaired against GitHub state;\n+SADR-0004's consequence that mirror conflicts are repaired against GitHub state;\n and its content-derived ids are rejected by `validate_shiki`'s currently-green id\n pattern.\n \ndiff --git a/docs/adr/0017-bookkeeping-closeouts-do-not-inherit-risk.md b/docs/adr/SADR-0017-bookkeeping-closeouts-do-not-inherit-risk.md\nindex 554e3a0d526a72ca067108c5464cc68c3edcf174..b9676857a4713850e063af7c39c8e0b01ecb4c66\n--- a/docs/adr/0017-bookkeeping-closeouts-do-not-inherit-risk.md\n+++ b/docs/adr/SADR-0017-bookkeeping-closeouts-do-not-inherit-risk.md\n@@ -1,4 +1,4 @@\n-# ADR 0017: A Bookkeeping Closeout Does Not Inherit Its Task's Risk\n+# SADR-0017: A Bookkeeping Closeout Does Not Inherit Its Task's Risk\n \n ## Status\n \n@@ -6,7 +6,7 @@\n \n ## Context\n \n-ADR 0012 made the goal loop push a task's completion to the default branch through a\n+SADR-0012 made the goal loop push a task's completion to the default branch through a\n closeout pull request: after the implementation PR merges, the loop opens a second PR\n carrying `task.status = done`, `lock.state = released`, and \u2014 when the task completes\n its goal \u2014 `goal.status = complete` plus one scorecard report. Nothing else. The\n@@ -49,14 +49,14 @@\n 4. The task file's governance fields (`scope`, `non_goals`, `required_skills`,\n    `risk_level`, `locks`, `acceptance_checks`, `test_command`) are byte-identical to\n    the base snapshot. Only `status`, `expected_pr`, `closeout_pr`, `ledger_evidence`\n-   and `expected_branch` may differ \u2014 the ADR 0015 contract-immutability rule,\n+   and `expected_branch` may differ \u2014 the SADR-0015 contract-immutability rule,\n    unchanged.\n \n    `expected_branch` is in that set because a closeout is *by construction* on a\n    different branch from the implementation it closes, and the loop rewrites the\n    field accordingly. It is safe to let it differ because MergeGate independently\n    requires `expected_branch` to equal the PR's actual head ref, so a PR cannot set\n-   it to anything other than the branch it is really on. The first draft of this ADR\n+   it to anything other than the branch it is really on. The first draft of this SADR\n    omitted it, which made the exemption unreachable: every real closeout failed\n    condition 4 on a field the loop always changes. See PR #209.\n 5. The state transitions are exactly the terminal set: task `review` \u2192 `done`, lock\n@@ -113,12 +113,12 @@\n classification must be derived, not asserted.\n \n **Fold the closeout into the implementation PR** so no second PR exists. Rejected under\n-ADR 0012, which recorded why: a task file that is already `done` on the implementation\n+SADR-0012, which recorded why: a task file that is already `done` on the implementation\n branch short-circuits the loop's review \u2192 CCA \u2192 merge \u2192 done ordering into a local\n goal-completion before any merge happens.\n \n **Have the loop auto-merge closeouts with elevated credentials.** Rejected outright: that\n-is a runtime authoring its own approval, which ADR 0010 and the constitution's\n+is a runtime authoring its own approval, which SADR-0010 and the constitution's\n approval-record prohibition forbid. It would also be indistinguishable, in the evidence,\n from the forgery those rules exist to prevent.\n \ndiff --git a/docs/adr/0018-carried-guardian-approval-across-a-proven-base-sync.md b/docs/adr/SADR-0018-carried-guardian-approval-across-a-proven-base-sync.md\nindex 22358562da390ed8b45cd600da08a43d378b4680..e6e2907cefa776460af73ddf7d39db605f3c39b0\n--- a/docs/adr/0018-carried-guardian-approval-across-a-proven-base-sync.md\n+++ b/docs/adr/SADR-0018-carried-guardian-approval-across-a-proven-base-sync.md\n@@ -1,4 +1,4 @@\n-# ADR 0018: Carried Guardian Approval Across A Proven Base Sync\n+# SADR-0018: Carried Guardian Approval Across A Proven Base Sync\n \n ## Status\n \n@@ -34,7 +34,7 @@\n Guardian evaluator that consumes it (`shiki_guardian.evaluate_guardian_approval`,\n the `guardian_comment_carried` source) were built and tested in isolation. Neither\n does anything until a caller passes the flags that turn the carry on, so the\n-feature was invisible in CI. This ADR records the decision the two of them\n+feature was invisible in CI. This SADR records the decision the two of them\n implement, and this task wires it into the four CI invocations that judge Guardian\n approval.\n \ndiff --git a/docs/adr/README.md b/docs/adr/README.md\nindex c70e057a4520c36e042ab68c53cd4a21ef709b71..5da43856836e2ce6a150f8127766e2f88355ab4b\n--- a/docs/adr/README.md\n+++ b/docs/adr/README.md\n@@ -1,40 +1,41 @@\n-# Architecture Decision Records\n+# Decision Record Namespaces\n \n-This directory holds Shiki's Architecture Decision Records (ADRs): durable\n-records of hard-to-reverse platform decisions. ADRs are part of the Shiki source\n-of truth alongside GitHub state, the `.shiki/` mirror, and `CONTEXT.md`.\n+This directory holds two independent decision namespaces:\n \n-## How ADRs Work\n+- Shiki Architecture Decision Records (SADRs), named\n+  `SADR-NNNN-<short-slug>.md`, record hard-to-reverse Shiki platform decisions.\n+- Target Architecture Decision Records (ADRs), named\n+  `NNNN-<short-slug>.md`, belong to the target product.\n \n-- New ADRs are added as `docs/adr/NNNN-<short-slug>.md`, where `NNNN` is the next\n-  zero-padded sequence number.\n-- The **canonical list of ADRs is the set of files matching `docs/adr/*.md`** in\n-  this directory. Browse `docs/adr/*.md` for the authoritative, current set.\n-- Use [`template.md`](template.md) as the starting point for a new ADR.\n-- Record an ADR when a decision is hard to reverse, surprising without context,\n-  and a real tradeoff. Do not write an ADR for routine or easily reversible\n-  choices.\n+The same four-digit number may appear once in each namespace. A platform\n+`SADR-NNNN` citation resolves only to `SADR-NNNN-*.md`; a target `ADR NNNN`\n+citation resolves only to `NNNN-*.md`.\n \n-Using the `docs/adr/*.md` glob as the source of truth means other PRs can add an\n-ADR without editing this index file, avoiding merge contention on a\n-hand-maintained enumeration.\n+## How Decision Records Work\n \n-## Current ADRs (snapshot)\n-\n-The list below is a convenience snapshot and may lag behind the directory. When\n-in doubt, list `docs/adr/*.md`.\n+- New Shiki platform decisions use the next zero-padded SADR number.\n+- New target product decisions use the target's own ADR sequence.\n+- The canonical list is the files in `docs/adr/`; no hand-maintained index is\n+  authoritative.\n+- Use [`template.md`](template.md) as the starting point and choose the filename\n+  namespace that owns the decision.\n+- Record a decision when it is hard to reverse, surprising without context, and\n+  a real tradeoff. Do not write a decision record for routine or easily\n+  reversible choices.\n \n-- [0001 \u2014 Keep Shiki As The Platform Name](0001-keep-shiki-as-platform-name.md)\n-- [0002 \u2014 Use AGENTS As The Shared Agent Constitution](0002-use-agents-as-shared-constitution.md)\n-- [0003 \u2014 Require Skill Gate For MergeGate Readiness](0003-require-skill-gate-for-mergegate-readiness.md)\n-- [0004 \u2014 Use GitHub First With A Repository-Local Shiki Mirror](0004-use-github-first-with-shiki-mirror.md)\n-- [0005 \u2014 Use Codex As Front And Claude Code Action As The GitHub Runtime](0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md)\n-- [0006 \u2014 Separate Codex Implementation, CCA Completion, And MergeGate Authorization](0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md)\n+Using the directory as the source of truth lets independent changes add records\n+without contending on an enumerated index.\n \n-## Adding A New ADR\n+## Adding A Shiki SADR\n \n-1. Copy `template.md` to `docs/adr/NNNN-<short-slug>.md`.\n+1. Copy `template.md` to `docs/adr/SADR-NNNN-<short-slug>.md`.\n 2. Fill in the title, status, context, decision, and consequences.\n-3. Set the status to `Proposed`, then `Accepted` once the decision is approved.\n-4. Open a PR following `CONTRIBUTING.md`. You do not need to edit this index for\n-   the ADR to be discoverable via `docs/adr/*.md`.\n+3. Set the status to `Proposed`, then `Accepted` once the proper authority has\n+   approved the decision.\n+4. Open a PR following `CONTRIBUTING.md`.\n+\n+## Adding A Target ADR\n+\n+1. Copy `template.md` to `docs/adr/NNNN-<short-slug>.md` using the target's own\n+   sequence.\n+2. Cite it as `ADR NNNN`; do not prefix its filename with `SADR-`.\ndiff --git a/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md b/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\nnew file mode 100644\nindex 0000000000000000000000000000000000000000..7455ce1f3bed02ddddb851ac21a1d1a648c9aeba\n--- /dev/null\n+++ b/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\n@@ -0,0 +1,55 @@\n+# SADR-0019: Separate Shiki Platform SADRs From Target ADRs\n+\n+## Status\n+\n+Accepted\n+\n+## Context\n+\n+Shiki installs its platform decision records into `docs/adr/` in every target\n+repository. Shiki and its targets previously used the same numeric\n+`NNNN-<slug>.md` namespace. A target that already owned ADRs 0013 through 0018\n+therefore acquired two records for each number during installation, making an\n+`ADR NNNN` citation ambiguous. Renumbering target decisions would rewrite target\n+history, while renumbering Shiki decisions per target would destroy their stable\n+platform identity.\n+\n+Upgrades also need to remove the former Shiki-owned numeric paths without ever\n+mistaking a target-owned ADR for platform content. The install stamp already\n+records exact shipped paths and content digests, so it can prove ownership only\n+when both still match.\n+\n+## Decision\n+\n+Shiki platform decisions use the `SADR` namespace:\n+\n+- records are named `docs/adr/SADR-NNNN-<slug>.md`;\n+- live platform citations use `SADR-NNNN`;\n+- target product decisions retain `docs/adr/NNNN-<slug>.md` and `ADR NNNN`;\n+- the reference validator resolves the two namespaces independently, so the same\n+  four-digit number may exist once in each namespace.\n+\n+Before a forced upgrade writes anything, the installer inventories the exact\n+former numeric Shiki ADR paths. It may delete a legacy path only when the\n+existing install stamp contains that exact path and its recorded SHA-256 digest\n+matches the file currently on disk. All ownership blockers are reported\n+together and abort the upgrade before its first write. A non-forced install\n+never performs this cleanup.\n+\n+Historical `.shiki/` evidence remains unchanged because it records the namespace\n+that existed when that evidence was produced.\n+\n+## Consequences\n+\n+- Targets may use any ADR number without colliding with Shiki's platform SADRs.\n+- Fresh installs preserve target ADR bytes and install a stable SADR set.\n+- Forced upgrades fail closed when legacy ownership cannot be proven, and a\n+  successful upgrade refreshes the stamp without removed legacy paths.\n+- Current governance, packaging, workflow, prompt, script, and skill references\n+  must move with the platform records.\n+\n+## Alternatives Considered\n+\n+- Collision-only refusal leaves affected targets permanently unupgradable.\n+- Renumbering target ADRs mutates target-owned history.\n+- Per-target Shiki renumbering makes platform citations non-portable.\ndiff --git a/docs/adr/template.md b/docs/adr/template.md\nindex a92a97daddd0b00ab9dd626cc8d86942da2eafed..17dd79175b5fee2105935557f691b40cf9492942\n--- a/docs/adr/template.md\n+++ b/docs/adr/template.md\n@@ -1,14 +1,14 @@\n-# NNNN: <Short Decision Title>\n+# <SADR-NNNN or NNNN>: <Short Decision Title>\n \n ## Status\n \n-Proposed | Accepted | Superseded by ADR-NNNN\n+Proposed | Accepted | Superseded by SADR-NNNN or target ADR NNNN\n \n ## Context\n \n What is the situation, constraint, or force that requires a decision? Reference\n-relevant domain terms (`CONTEXT.md`), prior ADRs, and code reality. State the\n-problem, not the solution.\n+relevant domain terms (`CONTEXT.md`), prior Shiki SADRs or target ADRs, and code\n+reality. State the problem, not the solution.\n \n ## Decision\n \ndiff --git a/docs/agents/autonomous-loop-e2e-acceptance.md b/docs/agents/autonomous-loop-e2e-acceptance.md\nindex bbf84c6081987f159ac6ad9ecf3c82b7aa72e7e8..5c13c1d02a05a90b0dc46cbef7c95a9846d0c237\n--- a/docs/agents/autonomous-loop-e2e-acceptance.md\n+++ b/docs/agents/autonomous-loop-e2e-acceptance.md\n@@ -40,8 +40,8 @@\n 3. `gh auth status` is logged in with merge permission on the repo.\n 4. Branch protection + required checks are configured exactly as the goal-loop\n    required-check set expects (see `docs/agents/branch-protection-smoke.md`).\n-5. The repo `.shiki/policy` permits autonomous low/medium-risk merges (ADR\n-   0008/0009); the MergeGate policy check is the recorded-authority gate.\n+5. The repo `.shiki/policy` permits autonomous low/medium-risk merges\n+   (SADR-0008/0009); the MergeGate policy check is the recorded-authority gate.\n \n The operator's only actions are: register the task and start the loop. **No\n manual pre-clearing** means after `shiki loop run` starts, the operator does not\ndiff --git a/docs/agents/bootstrap-command.md b/docs/agents/bootstrap-command.md\nindex 05f7f203a52cf412b9f97084a6e131ed0f785afd..f7aa0b734dfebee4a3b139335b66ee3415f48713\n--- a/docs/agents/bootstrap-command.md\n+++ b/docs/agents/bootstrap-command.md\n@@ -189,7 +189,9 @@\n ```\n \n Use this only for tests, fixtures, or explicit local-only template inspection.\n-Use `--force` only when you intentionally want to overwrite existing target files.\n+Use `--force` only when you intentionally want to refresh the target template.\n+Legacy numeric Shiki decision cleanup is stamp-and-digest authorized before any\n+write; target ADR paths are not cleanup candidates.\n \n ## Slash Command\n \ndiff --git a/docs/agents/checklists.md b/docs/agents/checklists.md\nindex 4a7f03ade346f53a1c1d82981f3fd744f9ab4547..0c5ec6c53dc7197b7b7a7d18e667b325babd6c45\n--- a/docs/agents/checklists.md\n+++ b/docs/agents/checklists.md\n@@ -32,18 +32,18 @@\n | ID | Item | Blocking |\n | --- | --- | --- |\n | GD-01 | Plan was challenged against `CONTEXT.md`. | blocking for non-trivial Goals |\n-| GD-02 | Relevant ADRs were read or absence was recorded. | blocking for non-trivial Goals |\n+| GD-02 | Relevant SADRs and target ADRs were read or absence was recorded. | blocking for non-trivial Goals |\n | GD-03 | Ambiguous terms were resolved or converted to HITL blockers. | blocking |\n | GD-04 | Edge scenarios were considered. | advisory |\n | GD-05 | Code reality was checked when code could answer. | blocking when applicable |\n | GD-06 | New domain terms were added to `CONTEXT.md` when needed. | blocking when applicable |\n-| GD-07 | ADR was created or explicitly skipped using the ADR criteria. | blocking when applicable |\n+| GD-07 | A SADR or target ADR was created or explicitly skipped using the decision-record criteria. | blocking when applicable |\n \n ## CI \u2014 Context & Impact Checklist\n \n | ID | Item | Blocking |\n | --- | --- | --- |\n-| CI-01 | Relevant docs and ADRs identified. | blocking |\n+| CI-01 | Relevant docs, SADRs, and target ADRs identified. | blocking |\n | CI-02 | Relevant modules, interfaces, seams, callers, and tests identified. | blocking |\n | CI-03 | Dependencies and blocked-by relationships identified. | blocking |\n | CI-04 | Candidate locks identified. | blocking |\n@@ -62,7 +62,7 @@\n | PRD-04 | Implementation decisions are recorded without volatile code detail. | blocking |\n | PRD-05 | Testing decisions are recorded. | blocking |\n | PRD-06 | Out-of-scope items are explicit. | blocking |\n-| PRD-07 | Relevant glossary/ADR links are present when applicable. | blocking |\n+| PRD-07 | Relevant glossary/SADR/target-ADR links are present when applicable. | blocking |\n \n ## SF \u2014 Spec Freeze Checklist\n \n@@ -98,7 +98,7 @@\n | H-02 | Branch/worktree target is present. | blocking |\n | H-03 | Scope and non-goals are present. | blocking |\n | H-04 | Dependencies and locks are present. | blocking |\n-| H-05 | Relevant docs, ADRs, modules, and tests are referenced. | blocking |\n+| H-05 | Relevant docs, SADRs, target ADRs, modules, and tests are referenced. | blocking |\n | H-06 | Required skills are stated. | blocking |\n | H-07 | TDD expectations are stated. | blocking when behavior work |\n | H-08 | Acceptance criteria are included. | blocking |\ndiff --git a/docs/agents/codex-handoff.md b/docs/agents/codex-handoff.md\nindex ede468f47cb277f69a857e8c45df703e4ed0151d..6f97bdffb67e44d7edea2c71e01ea28933c4eb6c\n--- a/docs/agents/codex-handoff.md\n+++ b/docs/agents/codex-handoff.md\n@@ -46,7 +46,7 @@\n \n ## Context\n - Domain terms:\n-- Relevant ADRs:\n+- Relevant SADRs and target ADRs:\n - Relevant docs:\n - Relevant modules/interfaces/seams:\n - Similar tests/prior art:\ndiff --git a/docs/agents/completion-check-agent.md b/docs/agents/completion-check-agent.md\nindex 13e4c035db35025f4e3cdcc5adfb24314ac4d521..61bfa1218cf36ca605250c91919e620f33238aa0\n--- a/docs/agents/completion-check-agent.md\n+++ b/docs/agents/completion-check-agent.md\n@@ -32,7 +32,7 @@\n - `AGENTS.md`\n - `CLAUDE.md`\n - `CONTEXT.md`\n-- relevant ADRs\n+- relevant SADRs and target ADRs\n - parent Goal issue\n - PRD issue or document\n - task issue\ndiff --git a/docs/agents/decision-control.md b/docs/agents/decision-control.md\nindex 7d8ee5ab03a5ccbcbd04056ae61de03785a2a9c9..8c94ecda00fffaa0e92c325ab83aa9776bd1dac1\n--- a/docs/agents/decision-control.md\n+++ b/docs/agents/decision-control.md\n@@ -11,7 +11,7 @@\n | Decision | Owner | When | Required inputs | Output | Enforcement surface |\n | --- | --- | --- | --- | --- | --- |\n | Is the Goal clear enough to plan? | Planner + Guardian when needed | Goal intake | User request, existing docs, risk signals | Goal readiness verdict | Goal checklist, issue template, ledger |\n-| Must `grill-with-docs` run? | Planner / Skill Gate | Before PRD or issue split | Ambiguity, domain terms, ADR conflicts, unclear boundaries | Skill invocation or explicit skip rationale | Skill Gate checklist |\n+| Must `grill-with-docs` run? | Planner / Skill Gate | Before PRD or issue split | Ambiguity, domain terms, SADR/target-ADR conflicts, unclear boundaries | Skill invocation or explicit skip rationale | Skill Gate checklist |\n | Is a PRD required? | Planner | After Goal context stabilizes | Goal, `grill-with-docs` output, Context & Impact output | PRD required / not required | PRD checklist, CCA evidence check |\n | Can the spec be frozen? | Operator only | End of Requirements Definition, before issue split | PRD/requirements summary, scope inventory (SF-02), open-question list | `spec_freeze` block in the plan + ledger evidence | `require_grilled_plan` (plan ingest/run), SF checklist |\n | Can a frozen spec change? | Operator only (Spec Amendment) | When implementation contradicts the frozen spec | Contested decisions, scoped re-grill result | recorded amendment + re-stamped freeze | SF checklist, ledger evidence |\ndiff --git a/docs/agents/domain.md b/docs/agents/domain.md\nindex 258da8ea81e87ac0a6b0b5b5e5c2fde980eaabb7..d57b0cf17ff3e1fa9e6e5885372bf083d8295421\n--- a/docs/agents/domain.md\n+++ b/docs/agents/domain.md\n@@ -5,7 +5,9 @@\n ## Layout\n \n - `CONTEXT.md` defines Shiki language. Keep it implementation-free.\n-- `docs/adr/` records hard-to-reverse decisions.\n+- `docs/adr/SADR-NNNN-*.md` records Shiki platform decisions.\n+- `docs/adr/NNNN-*.md` records target product decisions without sharing the\n+  SADR namespace.\n - `AGENTS.md` is the runtime-neutral agent constitution.\n - `CLAUDE.md` is the Claude Code wrapper around `AGENTS.md`.\n - `.shiki/` is the Target Repository mirror for Goals, PRDs, plans, Task DAGs, contracts, locks, worktree records, ledger entries, CCA verdicts, repair packets, reports, and handoffs.\n@@ -15,6 +17,6 @@\n \n - Use Shiki terms exactly when they exist.\n - Add glossary terms to `CONTEXT.md` only when the term is part of the domain, not a generic programming concept.\n-- Create ADRs only for hard-to-reverse, surprising, tradeoff-based decisions.\n+- Create SADRs or target ADRs only for hard-to-reverse, surprising, tradeoff-based decisions.\n - Keep product-specific language in the Target Repository, not in Shiki Template docs.\n-- If an ADR conflicts with a proposed implementation, surface the conflict before editing.\n+- If a SADR or target ADR conflicts with a proposed implementation, surface the conflict before editing.\ndiff --git a/docs/agents/execution-control.md b/docs/agents/execution-control.md\nindex ad8ee3cdb36be56fedc1f46408ca92b387d74047..bc63c3a73661d12b567016ba299f1a6b66bf9088\n--- a/docs/agents/execution-control.md\n+++ b/docs/agents/execution-control.md\n@@ -63,7 +63,7 @@\n | Branch name | Orchestrator | Orchestrator | Before implementer dispatch | branch policy | branch ref in task/ledger |\n | Lock reservation | Context & Impact | MergeGate | Before dispatch and before merge | `.shiki/locks/`, labels, PR metadata | lock id, owner, scope |\n | CI profile | Planner | MergeGate | During issue triage, rechecked at PR | labels, `.shiki/policy.yaml`, workflow matrix | required check list |\n-| Required status checks | Repo maintainer / Guardian | GitHub branch protection / rulesets | Repository setup, updated via ADR | GitHub settings | branch/ruleset config, ADR |\n+| Required status checks | Repo maintainer / Guardian | GitHub branch protection / rulesets | Repository setup, updated via target ADR | GitHub settings | branch/ruleset config, target ADR |\n | CCA judgment | CCA | GitHub required check / MergeGate | Every non-draft PR update | GitHub Action check | structured CCA verdict |\n | Repair assignment | CCA | MergeGate | After failed CCA/CI/review | repair packet, issue/PR comment | repair id and attempt count |\n | Merge readiness | MergeGate | GitHub branch protection / Guardian if needed | After CCA complete | required checks, review rules, rulesets | mergegate check output |\ndiff --git a/docs/agents/guardian-policy.md b/docs/agents/guardian-policy.md\nindex 4147c7adf05a47adb4fd24ada1a1c2e1f2ba6d2d..a20d9cc6c00a569bb057b9ca8d6b51c7fc4a9847\n--- a/docs/agents/guardian-policy.md\n+++ b/docs/agents/guardian-policy.md\n@@ -98,8 +98,8 @@\n `origin/<default_branch>` and reuses each job's existing base fetch; a shallow\n (`--depth`) or second base fetch would make the proof unresolvable and silently\n disable the carry, so neither is permitted. CCA reads `guardian_comment_carried` as\n-a recorded authority in the deterministic signal and does not re-derive it. See ADR\n-0018 for the full decision, the rejected diff/patch-equality and path-disjointness\n+a recorded authority in the deterministic signal and does not re-derive it. See\n+SADR-0018 for the full decision, the rejected diff/patch-equality and path-disjointness\n alternatives, and the accepted residual.\n \n ## External AI Guardian Review (SADR-0010)\ndiff --git a/docs/agents/implementation-policy.md b/docs/agents/implementation-policy.md\nindex c754144a9fb5bf4a38aa82a60e1670c59e626b44..85c37281bcad196c6b7b02343faf00b03cd9c57a\n--- a/docs/agents/implementation-policy.md\n+++ b/docs/agents/implementation-policy.md\n@@ -31,10 +31,13 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n ## Non-Negotiables\n \n 1. **Planning precedes implementation.** Non-trivial Goals start with `grill-with-docs` and Context & Impact.\n-2. **Domain language is controlled.** Terms must be checked against `CONTEXT.md`; hard-to-reverse tradeoffs must be checked against ADRs.\n+2. **Domain language is controlled.** Terms must be checked against `CONTEXT.md`; hard-to-reverse tradeoffs must be checked against Shiki SADRs and target ADRs.\n 3. **PRD precedes issue decomposition.** `to-prd` is used once the context is settled enough to become durable intent.\n 4. **Issues are vertical slices.** `to-issues` must produce independently grabbable tracer-bullet tasks, not horizontal layer tickets.\n 5. **Triage controls dispatch.** Only `ready-for-agent` / AFK tasks may be assigned to Codex without human input.\n@@ -95,19 +98,19 @@\n Process:\n \n - Challenge terms against `CONTEXT.md`.\n-- Check ADRs before reopening decisions.\n+- Check SADRs and target ADRs before reopening decisions.\n - Inspect code when code can answer a question.\n - Ask one question at a time when the operator is available.\n - Provide a recommended answer with each question.\n - Record resolved terms in `CONTEXT.md`.\n-- Create ADRs only for hard-to-reverse, surprising, tradeoff-based decisions.\n+- Create SADRs or target ADRs only for hard-to-reverse, surprising, tradeoff-based decisions.\n - Convert unresolved load-bearing questions into HITL blockers.\n \n Outputs:\n \n - Resolved design-tree decisions.\n - Updated glossary terms when needed.\n-- ADRs when justified.\n+- SADRs or target ADRs when justified.\n - HITL blockers when decisions remain unresolved.\n \n ## Phase 3 \u2014 Context & Impact\n@@ -117,7 +120,7 @@\n \n Outputs must include:\n \n-- Relevant documents and ADRs.\n+- Relevant documents, SADRs, and target ADRs.\n - Relevant modules, interfaces, seams, callers, and tests.\n - Candidate locks.\n - Dependencies.\n@@ -140,7 +143,7 @@\n - Testing decisions.\n - Out of scope.\n - Further notes.\n-- Links to relevant Goal, domain terms, and ADRs.\n+- Links to relevant Goal, domain terms, SADRs, and target ADRs.\n \n Phase 4 ends with Spec Freeze: the operator explicitly approves the PRD, the\n plan records a `spec_freeze` block (status, approved_by, source), and the SF\n@@ -240,7 +243,7 @@\n - Dependencies and locks.\n - Risk and Guardian requirements.\n - Ledger completeness.\n-- Documentation and ADR updates.\n+- Documentation and SADR/target-ADR updates.\n - Scope drift.\n \n CCA emits one of:\n@@ -301,7 +304,7 @@\n | Evidence failure | Behavior might be done, but proof is missing. | Add checks, PR evidence, ledger entry, or artifacts. |\n | Scope drift | PR changes unrelated behavior. | Revert unrelated changes or split into new Goal. |\n | Dependency block | Upstream task or lock not complete. | Block until dependency resolves. |\n-| Architecture block | Decision needs ADR or deep-module discussion. | `grill-with-docs` or `improve-codebase-architecture`. |\n+| Architecture block | Decision needs a SADR, target ADR, or deep-module discussion. | `grill-with-docs` or `improve-codebase-architecture`. |\n | Risk block | Security/production/policy change needs human approval. | Guardian decision. |\n \n ## Phase 5.5 \u2014 Execution Decision Control\ndiff --git a/docs/agents/runtime-auth-model.md b/docs/agents/runtime-auth-model.md\nindex 6a2e1a47e81ea89b5c68a210ec07772a9374babc..3c54748d24233dbd0ba8473950282547ef4ce935\n--- a/docs/agents/runtime-auth-model.md\n+++ b/docs/agents/runtime-auth-model.md\n@@ -148,7 +148,7 @@\n \n API-key based runners may be added by a target repository as an explicit extension. They are not the default Shiki template path.\n \n-If a repo opts into API-key mode, record the exception in an ADR and update the repo's `AGENTS.md`, `.shiki/config.yaml`, and workflow permissions.\n+If a repo opts into API-key mode, record the target-specific exception in a target ADR and update the repo's `AGENTS.md`, `.shiki/config.yaml`, and workflow permissions.\n \n ## GitHub Actions Runtime Compatibility\n \ndiff --git a/docs/agents/skill-gate.md b/docs/agents/skill-gate.md\nindex c50398b0d7c60a8598f2a5c88a10015730dced13..3acffe6444617bb1d54b77d4b4708158ec735bc6\n--- a/docs/agents/skill-gate.md\n+++ b/docs/agents/skill-gate.md\n@@ -9,7 +9,7 @@\n | Skill | Trigger | Expected output |\n | --- | --- | --- |\n | `setup-matt-pocock-skills` | First configuring a repo for these skills, or when issue tracker, triage labels, or domain docs are missing. | `docs/agents/` configuration and an `Agent skills` block in `AGENTS.md` or `CLAUDE.md`. |\n-| `grill-with-docs` | Ambiguous plan, unclear terminology, boundary decision, tradeoff, or ADR-worthy design choice. | Clarified decision tree, recommended answers, updates to `CONTEXT.md` or ADRs when decisions crystallize. |\n+| `grill-with-docs` | Ambiguous plan, unclear terminology, boundary decision, tradeoff, or decision-record-worthy design choice. | Clarified decision tree, recommended answers, updates to `CONTEXT.md`, SADRs, or target ADRs when decisions crystallize. |\n | `zoom-out` | Agent lacks a higher-level architectural map. | Map of relevant modules, callers, seams, and domain concepts. |\n | `to-prd` | Settled context should become a PRD. | PRD published to the issue tracker with problem, solution, user stories, decisions, testing, and out-of-scope notes. |\n | `to-issues` | Goal, PRD, or plan must become implementation tickets. | Vertical-slice issues with acceptance criteria, dependencies, and AFK/HITL classification. |\n@@ -17,7 +17,7 @@\n | `tdd` | Feature work or bug fix has observable behavior that can be specified with tests. | Red-green-refactor loop through public interfaces, one vertical slice at a time. |\n | `code-review` | The TDD loop is green and the implementer is about to create or update a PR. Default for every implementation task. | Pre-PR self-review of the task diff with fixes applied on the task branch; findings and resolutions recorded as a ledger entry naming the skill and a `## Pre-PR code review` PR body section. |\n | `diagnose` | Hard bug, failing check, regression, flaky behavior, or performance problem. | Reproduction loop, ranked hypotheses, instrumentation, fix, regression test, post-mortem. |\n-| `improve-codebase-architecture` | Structural friction, testability, AI-navigability, deep-module opportunity, or refactor request. | Architecture candidates and deepening recommendations informed by `CONTEXT.md` and ADRs. |\n+| `improve-codebase-architecture` | Structural friction, testability, AI-navigability, deep-module opportunity, or refactor request. | Architecture candidates and deepening recommendations informed by `CONTEXT.md`, SADRs, and target ADRs. |\n | `prototype` | A throwaway logic or UI prototype is needed to answer a design question. | Clearly marked disposable prototype and captured decision. |\n \n ## MergeGate Interaction\ndiff --git a/docs/operations.md b/docs/operations.md\nindex cf1f452d4c589e38ccd58553bda0ad8684b51eb7..ace48a90ffbec9e5c0a7113bef16a4858bc44b3f\n--- a/docs/operations.md\n+++ b/docs/operations.md\n@@ -89,6 +89,16 @@\n      target history, brought forward by `shiki migrate`, not a template.\n    - **Everything else** (CLI scripts, workflows, schemas, docs) is overwritten.\n \n+   A forced upgrade also migrates the former numeric Shiki decision paths to\n+   `SADR-NNNN-*`. Before the first write, the installer inventories the exact\n+   legacy Shiki paths and authorizes each deletion only when the existing\n+   `.shiki/install-stamp.json` names that path and its SHA-256 digest still\n+   matches the file. An absent, unreadable, malformed, incomplete, or\n+   mismatching stamp reports every blocked path and stops with no filesystem\n+   change. Target-owned `NNNN-*` ADRs are not cleanup candidates. A non-force\n+   install deletes nothing and retains any still-matching legacy stamp entries\n+   so a later forced upgrade can prove ownership.\n+\n    If the target has pending migrations, `--force` refuses and writes nothing \u2014\n    apply migrations first (step 3), then re-run. The run ends with a single\n    summary of every `.new` written, so a half-upgrade is visible.\ndiff --git a/docs/releasing.md b/docs/releasing.md\nindex 10d73828e7dbd64e9d9f6b83bc84d010537ce62a..aafeee8c4d441e1e427efee2ce4f81a647f020e1\n--- a/docs/releasing.md\n+++ b/docs/releasing.md\n@@ -67,7 +67,7 @@\n CLI in `scripts/`. There are no third-party Python runtime dependencies. The CLI\n is intentionally **not** exposed as a `[project.scripts]` console entry point\n because `scripts/shiki.py` is a dependency-free shim and `scripts/` is not\n-packaged as an importable distribution (see the ADR for the rationale).\n+packaged as an importable distribution (see SADR-0007 for the rationale).\n \n Supported install paths:\n \n@@ -155,6 +155,15 @@\n    [docs/operations.md](operations.md#the-new-convention). If the target has\n    pending migrations, `--force` refuses and writes nothing; apply migrations\n    (step 2) first, then re-run.\n+\n+   The SADR namespace upgrade has an additional pre-write gate: each former\n+   numeric Shiki decision path is removed only when the existing install stamp\n+   contains that exact path and a digest matching its current bytes. The\n+   installer aggregates absent/unreadable/malformed-stamp, missing-digest, and\n+   digest-mismatch blockers before raising, so refusal leaves the target tree\n+   byte-identical. Numeric target ADRs are preserved. A successful forced\n+   upgrade installs and stamps `SADR-*` paths and drops the removed legacy paths\n+   from the refreshed stamp.\n 2. Apply repository-local `.shiki/` state migrations with the migrate command:\n \n    ```bash\ndiff --git a/docs/usage.md b/docs/usage.md\nindex b83407ee3970dd395dd893010bc5dbe8bad26051..a130ef6e40dfd8169a5bc701339351097df1a09f\n--- a/docs/usage.md\n+++ b/docs/usage.md\n@@ -67,8 +67,10 @@\n shiki install-target /path/to/target-repo --local-only\n ```\n \n-Use `--force` only when you intentionally want to overwrite existing target\n-files.\n+Use `--force` only when you intentionally want to refresh the target template.\n+For the SADR namespace migration, `--force` deletes only exact legacy Shiki\n+decision paths whose existing install-stamp digest still matches; any ownership\n+blocker stops before the first write. Numeric target ADRs are never candidates.\n \n ## `shiki bootstrap-platform` \u2014 publish the Shiki platform repo\n \ndiff --git a/scripts/guardian_approval_signal.py b/scripts/guardian_approval_signal.py\nindex d059f92b2c82aa2b5d6ca421365537a87886c1cb..16924892714f4f5b9303c0ef0e325a7e70075630\n--- a/scripts/guardian_approval_signal.py\n+++ b/scripts/guardian_approval_signal.py\n@@ -199,8 +199,8 @@\n ) -> Any:\n     \"\"\"Evaluate SADR-0015 Contract Approval for the CCA Guardian signal.\n \n-    The SAME OR that MergeGate applies must reach this deterministic signal: ADR\n-    0015 is explicit that without it, a contract-approved PR still returns\n+    The SAME OR that MergeGate applies must reach this deterministic signal:\n+    SADR-0015 is explicit that without it, a contract-approved PR still returns\n     ``needs_guardian``, the CCA job fails, and the MergeGate policy check never\n     runs \u2014 the gate this change moves would never be reached. This resolves the\n     task the SAME way MergeGate does (the first Shiki task id in the PR body),\ndiff --git a/scripts/shiki_contract_approval.py b/scripts/shiki_contract_approval.py\nindex 941bcb96fdd8b9a85bc0c4f0f04d5dad9d04839c..4a4b418f6527413161db82670bec857a043360e2\n--- a/scripts/shiki_contract_approval.py\n+++ b/scripts/shiki_contract_approval.py\n@@ -142,7 +142,7 @@\n     changed_files_status: Any,\n     registration: Any,\n ) -> ContractApprovalResult:\n-    \"\"\"Whether this PR carries an SADR-0015 Contract Approval.\n+    \"\"\"Whether this PR carries a SADR-0015 Contract Approval.\n \n     The four conditions are evaluated IN ORDER, each failure recorded as a\n     distinct reason string:\ndiff --git a/scripts/shiki_guardian.py b/scripts/shiki_guardian.py\nindex a4ecb79bbda55fd5c8b06adfae2eb99760a03c45..4dd99bbe714e26f421fa19abac7a34ae1b195ecc\n--- a/scripts/shiki_guardian.py\n+++ b/scripts/shiki_guardian.py\n@@ -12,7 +12,7 @@\n # The carry re-derives its proof from git in-process on every judging invocation;\n # it is never read from an artifact, so there is nothing to go stale or be forged.\n # shiki_sync_proof is the ONLY guardian-family module that runs git, and every one\n-# of its invocations is read-only and local (ADR: prove-from-git base sync).\n+# of its invocations is read-only and local (SADR: prove-from-git base sync).\n from shiki_sync_proof import verify_pure_base_sync\n \n GUARDIAN_POLICY_PATH = \".shiki/guardian-policy.json\"\ndiff --git a/scripts/shiki_guardian_review.py b/scripts/shiki_guardian_review.py\nindex dd0f104d457899e0ba5ff1e162011c1e90b6f92b..e10ef58c046324e7ea3a3392feb3048ca77414ce\n--- a/scripts/shiki_guardian_review.py\n+++ b/scripts/shiki_guardian_review.py\n@@ -159,7 +159,7 @@\n     docs_only = _is_docs_only(changed_files)\n     if docs_only and matched == 0:\n         focus.append(\n-            \"Docs/ADR-only change: confirm the change is genuinely documentation/decision-only, introduces no behavior or contract drift, and stays consistent with the constitution and existing ADRs.\"\n+            \"Docs/decision-record-only change: confirm the change is genuinely documentation/decision-only, introduces no behavior or contract drift, and stays consistent with the constitution and existing SADRs and target ADRs.\"\n         )\n \n     # Conservative augmentation: unknown (no category and not docs-only), mixed\ndiff --git a/scripts/shiki_installer.py b/scripts/shiki_installer.py\nindex be064ab7a29ff6836f7f7a1d70500a43e0d11d7b..9fd8eefd137ce7003dad5c0dcb2e48e2cd47ab32\n--- a/scripts/shiki_installer.py\n+++ b/scripts/shiki_installer.py\n@@ -118,6 +118,7 @@\n # what lets a target carry it without a governance-schema change.\n INSTALL_STAMP_PATH = \".shiki/install-stamp.json\"\n INSTALL_STAMP_VERSION = 1\n+LEGACY_SHIKI_SADR_MAX = 18\n \n # Under --force the shipped surface splits three ways, so nothing is lost and\n # nothing is silently stale.\n@@ -237,9 +238,98 @@\n         )\n \n \n+def legacy_shiki_adr_paths() -> tuple[str, ...]:\n+    \"\"\"Former numeric paths for Shiki SADR-0001 through SADR-0018.\n+\n+    The authoritative slugs come from the current SADR filenames. SADR-0019 is\n+    the namespace decision itself and never shipped under a numeric legacy path.\n+    \"\"\"\n+    paths: list[str] = []\n+    for sadr in sorted((ROOT / \"docs\" / \"adr\").glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\")):\n+        number = sadr.name.split(\"-\", 2)[1]\n+        if int(number) <= LEGACY_SHIKI_SADR_MAX:\n+            paths.append(f\"docs/adr/{sadr.name.removeprefix('SADR-')}\")\n+    return tuple(paths)\n+\n+\n+def preflight_legacy_sadr_cleanup(target: Path, *, force: bool) -> tuple[str, ...]:\n+    \"\"\"Authorize exact legacy Shiki ADR removals before the first write.\n+\n+    A non-force install never removes existing files. A forced install needs no\n+    stamp when no legacy Shiki path is present. When legacy paths do exist, each\n+    must be named in a readable install stamp and its current bytes must match\n+    the recorded digest.\n+    \"\"\"\n+    if not force:\n+        return ()\n+    present = tuple(\n+        relative\n+        for relative in legacy_shiki_adr_paths()\n+        if (target / relative).exists() or (target / relative).is_symlink()\n+    )\n+    if not present:\n+        return ()\n+\n+    stamp_path = target / INSTALL_STAMP_PATH\n+    try:\n+        raw = stamp_path.read_text(encoding=\"utf-8\")\n+        stamp = json.loads(raw)\n+    except FileNotFoundError:\n+        detail = \"install stamp is absent\"\n+        blockers = [f\"{relative}: {detail}\" for relative in present]\n+    except (OSError, UnicodeDecodeError) as error:\n+        detail = f\"install stamp is unreadable ({error})\"\n+        blockers = [f\"{relative}: {detail}\" for relative in present]\n+    except json.JSONDecodeError as error:\n+        detail = f\"install stamp is malformed ({error})\"\n+        blockers = [f\"{relative}: {detail}\" for relative in present]\n+    else:\n+        if not isinstance(stamp, dict):\n+            blockers = [\n+                f\"{relative}: install stamp is malformed (expected an object)\"\n+                for relative in present\n+            ]\n+        else:\n+            digests = stamp.get(\"digests\")\n+            blockers = []\n+            if not isinstance(digests, dict):\n+                blockers.extend(\n+                    f\"{relative}: install stamp has no digest for this path\"\n+                    for relative in present\n+                )\n+            else:\n+                for relative in present:\n+                    expected = digests.get(relative)\n+                    if not isinstance(expected, str) or not expected:\n+                        blockers.append(\n+                            f\"{relative}: install stamp has no digest for this path\"\n+                        )\n+                        continue\n+                    try:\n+                        actual = _sha256_file(target / relative)\n+                    except OSError as error:\n+                        blockers.append(\n+                            f\"{relative}: legacy path is unreadable ({error})\"\n+                        )\n+                        continue\n+                    if actual != expected:\n+                        blockers.append(\n+                            f\"{relative}: digest mismatch (stamp {expected}, current {actual})\"\n+                        )\n+\n+    if blockers:\n+        joined = \"\\n\".join(f\"- {blocker}\" for blocker in blockers)\n+        raise ShikiError(\n+            \"legacy Shiki ADR cleanup is not authorized:\\n\"\n+            f\"{joined}\\nNo files were written.\"\n+        )\n+    return present\n+\n+\n def install_template(target: Path, *, force: bool, validate: bool) -> list[NewFileNote]:\n     # Preconditions first: never rewrite the tree of a target that cannot be\n     # upgraded cleanly (see refuse_pending_migrations).\n+    legacy_cleanup = preflight_legacy_sadr_cleanup(target, force=force)\n     refuse_pending_migrations(target)\n \n     # Resolve the target's required CODEOWNERS owner once from its\n@@ -250,6 +340,10 @@\n     # verbatim. See ``codeowners_required_owner``.\n     codeowners_owner = codeowners_required_owner(target)\n \n+    for relative in legacy_cleanup:\n+        (target / relative).unlink()\n+        info(f\"removed stamped legacy Shiki ADR: {target / relative}\")\n+\n     notes: list[NewFileNote] = []\n     for relative in TEMPLATE_PATHS:\n         source = ROOT / relative\n@@ -647,6 +741,22 @@\n     digests = target_stamp_digests(target)\n     existing = read_install_stamp(target)\n     if existing is not None:\n+        # A non-force install deliberately leaves legacy Shiki ADRs in place.\n+        # Preserve only still-matching ownership proof so a later forced\n+        # upgrade can authorize cleanup; never carry a missing or stale digest.\n+        existing_digests = existing.get(\"digests\")\n+        if isinstance(existing_digests, dict):\n+            for relative in legacy_shiki_adr_paths():\n+                expected = existing_digests.get(relative)\n+                path = target / relative\n+                if not isinstance(expected, str) or not path.is_file():\n+                    continue\n+                try:\n+                    actual = _sha256_file(path)\n+                except OSError:\n+                    continue\n+                if actual == expected:\n+                    digests[relative] = actual\n         if _incoming_is_older(commit, existing.get(\"platform_commit\")):\n             warn(\n                 \"kept existing install stamp: incoming install \"\ndiff --git a/scripts/shiki_loop.py b/scripts/shiki_loop.py\nindex 3611641da8aeb5038b1da90e3114b09a473ec6d8..84caa491b9cad0edc4176f209c13e9221e975681\n--- a/scripts/shiki_loop.py\n+++ b/scripts/shiki_loop.py\n@@ -424,7 +424,7 @@\n         # unresolvable CCA verdict has already produced its terminal stop and is never\n         # laundered into a head-moving sync (SADR-0010). It also never fires while the\n         # policy (Guardian) gate is red \u2014 for ANY reason, not only when it is the sole\n-        # red check \u2014 so a head-bound Guardian approval, or an SADR-0015\n+        # red check \u2014 so a head-bound Guardian approval, or a SADR-0015\n         # contract-approval carry that base movement defeated (the policy gate compares\n         # base and head task governance directly), is never silently restored by\n         # adopting base wholesale. It is bounded by a durable per-task counter so the\ndiff --git a/scripts/shiki_tasks.py b/scripts/shiki_tasks.py\nindex aec3809f24b4a0c8c96fc50971cff9ae9ad30381..d531052e9d9717058d4631483cefab44c26a65c1\n--- a/scripts/shiki_tasks.py\n+++ b/scripts/shiki_tasks.py\n@@ -821,7 +821,7 @@\n         \"prompt\": prompt,\n         \"entry_skill\": \"grill-with-docs\",\n         \"required_next_steps\": [\n-            \"Run grill-with-docs until terminology, boundaries, risks, and ADR-worthy decisions are settled.\",\n+            \"Run grill-with-docs until terminology, boundaries, risks, and decision-record-worthy choices are settled.\",\n             \"Obtain the operator's explicit approval of the PRD/requirements (Spec Freeze).\",\n             \"Write a machine-readable plan JSON with grill_with_docs.status=complete and spec_freeze.status=frozen.\",\n             \"Run shiki plan ingest --plan-file PLAN.json.\",\ndiff --git a/scripts/test_shiki_goal_loop.sh b/scripts/test_shiki_goal_loop.sh\nindex 753c7b516e10f0be3bc92a4bd0eb1fa699776ae6..8f3820a0a079b3044424cfe1a6c6b7fe68896e67\n--- a/scripts/test_shiki_goal_loop.sh\n+++ b/scripts/test_shiki_goal_loop.sh\n@@ -231,8 +231,8 @@\n \n # High/critical risk merges autonomously when all required checks are green:\n # the \"MergeGate policy check\" required check IS the Guardian gate (it enforces\n-# guardian-policy.json \u2014 human approval OR external AI guardian review, ADR\n-# 0010), so green checks mean a recorded authority approved. The fake gh returns\n+# guardian-policy.json \u2014 human approval OR external AI guardian review,\n+# SADR-0010), so green checks mean a recorded authority approved. The fake gh returns\n # green checks, simulating that the Guardian gate passed.\n python3 \"$ROOT/scripts/shiki.py\" goal create --target \"$TARGET\" --title \"Guardian gate\" --outcome \"High risk merges via policy gate\" >/tmp/shiki-goal-loop-guardian-goal.json\n GGOAL=\"$(json_get /tmp/shiki-goal-loop-guardian-goal.json goal_id)\"\ndiff --git a/tests/test_adr_references.py b/tests/test_adr_references.py\nindex 51706329c797d5dd9b47e082a333fa83b261d482..d005da4f771f5e8085ec22d4b787980e68f5ae8c\n--- a/tests/test_adr_references.py\n+++ b/tests/test_adr_references.py\n@@ -1,8 +1,8 @@\n-\"\"\"Every ``ADR ####`` citation must resolve to exactly one decision record.\n+\"\"\"Every target ADR and Shiki SADR citation resolves in its own namespace.\n \n Background. ``scripts/mergegate_check.py`` justifies ``is_bookkeeping_closeout``\n-with \"ADR 0017\" nine times and ``scripts/guardian_approval_signal.py`` cites\n-0016/0017 seven times, yet for a stretch the numbering in ``docs/adr/`` stopped\n+with \"SADR-0017\" nine times and ``scripts/guardian_approval_signal.py`` cites\n+SADR-0016/SADR-0017 seven times, yet for a stretch the numbering in ``docs/adr/`` stopped\n at 0015: the authoring commits lived on a coordinator branch and no task ever\n declared ``docs/adr/...`` in its locks, so nothing carried the records to the\n default branch. The stated basis of an exemption that merged seven PRs with zero\n@@ -11,11 +11,12 @@\n and the directory drifted with no signal.\n \n This is the same unbound-pair shape behind other silent failures: a reference\n-and its target with nothing asserting they agree. This test binds them. It scans\n-every ``scripts/*.py`` (plus ``docs/**/*.md`` and the root ``*.md`` governance\n-files, which are cheap) for the ``ADR ####`` pattern and asserts each cited\n-number resolves to exactly one ``docs/adr/####-*.md``. The converse is\n-deliberately NOT required: an ADR may exist without being cited.\n+and its target with nothing asserting they agree. This test binds them. Target\n+``ADR NNNN`` citations resolve only to ``docs/adr/NNNN-*.md``; Shiki\n+``SADR-NNNN`` citations resolve only to ``docs/adr/SADR-NNNN-*.md``. The scan\n+covers current governance docs, scripts, workflows, prompts, packaging metadata,\n+and repo-local Codex guidance while deliberately excluding historical\n+``.shiki`` evidence. The converse is not required: a record may be uncited.\n \n The suite is dependency free: standard-library ``unittest`` only.\n \"\"\"\n@@ -31,17 +32,18 @@\n \n REPO_ROOT = Path(__file__).resolve().parents[1]\n \n-# \"ADR 0016\" -- a four-digit citation. The trailing \\b keeps a five-digit token\n-# from being read as a truncated four-digit number.\n-_CITATION = re.compile(r\"\\bADR (\\d{4})\\b\")\n-# \"0016-locks-bound-product-paths.md\" -- the leading four digits name the record.\n-_ADR_FILENAME = re.compile(r\"^(\\d{4})-.*\\.md$\")\n+# ``ADR 0016`` / ``SADR-0016`` -- a namespace plus four-digit citation. The\n+# trailing ``\\b`` keeps a five-digit token from being read as a truncated one.\n+_CITATION = re.compile(r\"\\b(S?ADR)[ -](\\d{4})\\b\")\n+# Target ADRs retain the numeric filename; Shiki platform decisions use SADR.\n+_DECISION_FILENAME = re.compile(r\"^(?:(SADR)-)?(\\d{4})-.*\\.md$\")\n \n \n class Violation(NamedTuple):\n     \"\"\"A citation that does not resolve to exactly one decision record.\"\"\"\n \n     citing_file: str  # repo-relative path of the file that cites the ADR\n+    namespace: str  # \"ADR\" for target decisions; \"SADR\" for Shiki decisions\n     number: str  # the cited four-digit number, e.g. \"0016\"\n     line: int  # 1-based line of the citation\n     reason: str  # \"missing\" (zero matches) or \"ambiguous\" (two or more)\n@@ -49,44 +51,59 @@\n \n     @property\n     def expected_path(self) -> str:\n-        return f\"docs/adr/{self.number}-*.md\"\n+        prefix = \"SADR-\" if self.namespace == \"SADR\" else \"\"\n+        return f\"docs/adr/{prefix}{self.number}-*.md\"\n \n     def describe(self) -> str:\n         \"\"\"Name the citing file, the cited number, and the missing path.\"\"\"\n         if self.reason == \"missing\":\n             return (\n-                f\"{self.citing_file}:{self.line} cites ADR {self.number} \"\n+                f\"{self.citing_file}:{self.line} cites {self.namespace} {self.number} \"\n                 f\"but no document matches {self.expected_path}\"\n             )\n         joined = \", \".join(self.matches)\n         return (\n-            f\"{self.citing_file}:{self.line} cites ADR {self.number} which \"\n+            f\"{self.citing_file}:{self.line} cites {self.namespace} {self.number} which \"\n             f\"resolves to {len(self.matches)} documents (expected exactly one \"\n             f\"at {self.expected_path}): {joined}\"\n         )\n \n \n-def _adr_index(root: Path) -> dict[str, list[Path]]:\n-    \"\"\"Map every four-digit ADR number to the record files that claim it.\"\"\"\n-    index: dict[str, list[Path]] = {}\n+def _adr_index(root: Path) -> dict[tuple[str, str], list[Path]]:\n+    \"\"\"Map each decision namespace and number to the files that claim it.\"\"\"\n+    index: dict[tuple[str, str], list[Path]] = {}\n     adr_dir = root / \"docs\" / \"adr\"\n     if adr_dir.is_dir():\n         for path in sorted(adr_dir.glob(\"*.md\")):\n-            match = _ADR_FILENAME.match(path.name)\n+            match = _DECISION_FILENAME.match(path.name)\n             if match:\n-                index.setdefault(match.group(1), []).append(path)\n+                namespace = \"SADR\" if match.group(1) else \"ADR\"\n+                index.setdefault((namespace, match.group(2)), []).append(path)\n     return index\n \n \n def _citing_files(root: Path, include_docs: bool = True) -> list[Path]:\n-    \"\"\"Files scanned for citations: scripts/*.py, and cheaply the markdown.\"\"\"\n+    \"\"\"Current, shipped text surfaces scanned for decision citations.\"\"\"\n     files = sorted((root / \"scripts\").glob(\"*.py\"))\n+    files += sorted((root / \"scripts\").glob(\"*.sh\"))\n     if include_docs:\n         docs = root / \"docs\"\n         if docs.is_dir():\n             files += sorted(docs.rglob(\"*.md\"))\n         files += sorted(root.glob(\"*.md\"))\n-    return files\n+        pyproject = root / \"pyproject.toml\"\n+        if pyproject.is_file():\n+            files.append(pyproject)\n+        for relative in (\".github\", \".codex\"):\n+            surface = root / relative\n+            if surface.is_dir():\n+                files += sorted(\n+                    path\n+                    for path in surface.rglob(\"*\")\n+                    if path.is_file()\n+                    and path.suffix in {\".md\", \".py\", \".sh\", \".toml\", \".yaml\", \".yml\"}\n+                )\n+    return sorted(set(files))\n \n \n def find_adr_reference_violations(\n@@ -94,10 +111,9 @@\n ) -> list[Violation]:\n     \"\"\"Return every ``ADR ####`` citation under ``root`` that fails to resolve.\n \n-    A citation resolves when exactly one ``docs/adr/####-*.md`` matches its\n-    number. Zero matches is a \"missing\" violation; two or more is \"ambiguous\".\n-    Records that are never cited produce nothing -- the converse is not\n-    required.\n+    A citation resolves when exactly one record in the same namespace matches\n+    its number. Zero matches is a \"missing\" violation; two or more is\n+    \"ambiguous\". Records that are never cited produce nothing.\n     \"\"\"\n     root = Path(root)\n     index = _adr_index(root)\n@@ -110,13 +126,15 @@\n         rel = path.relative_to(root).as_posix()\n         for lineno, line in enumerate(text.splitlines(), start=1):\n             for match in _CITATION.finditer(line):\n-                number = match.group(1)\n-                found = index.get(number, [])\n+                namespace, number = match.groups()\n+                found = index.get((namespace, number), [])\n                 if len(found) == 1:\n                     continue\n                 reason = \"missing\" if not found else \"ambiguous\"\n                 rels = [p.relative_to(root).as_posix() for p in found]\n-                violations.append(Violation(rel, number, lineno, reason, rels))\n+                violations.append(\n+                    Violation(rel, namespace, number, lineno, reason, rels)\n+                )\n     return violations\n \n \n@@ -129,8 +147,8 @@\n         self.assertEqual(\n             violations,\n             [],\n-            \"Every 'ADR ####' citation must resolve to exactly one \"\n-            f\"docs/adr/####-*.md:\\n{detail}\",\n+            \"Every ADR/SADR citation must resolve exactly once in its own \"\n+            f\"docs/adr namespace:\\n{detail}\",\n         )\n \n     def test_scan_actually_reads_scripts(self) -> None:\n@@ -142,12 +160,38 @@\n             \"the citation scan must cover scripts/*.py\",\n         )\n \n+    def test_platform_citations_do_not_use_unresolved_numeric_shorthand(self) -> None:\n+        shorthand = re.compile(r\"SADR-\\d{4}/\\d{4}\")\n+        offenders = []\n+        for path in _citing_files(REPO_ROOT):\n+            text = path.read_text(encoding=\"utf-8\")\n+            if shorthand.search(text):\n+                offenders.append(path.relative_to(REPO_ROOT).as_posix())\n+        self.assertEqual(\n+            offenders,\n+            [],\n+            \"repeat the SADR prefix so every cited number is independently resolvable\",\n+        )\n+\n     def test_carried_records_are_indexed(self) -> None:\n         # The two records this Goal carried onto the default branch, and which\n         # the scripts cite most, must be present.\n         index = _adr_index(REPO_ROOT)\n-        self.assertIn(\"0016\", index)\n-        self.assertIn(\"0017\", index)\n+        self.assertIn((\"SADR\", \"0016\"), index)\n+        self.assertIn((\"SADR\", \"0017\"), index)\n+\n+    def test_platform_tree_has_only_sadr_0001_through_0019(self) -> None:\n+        adr_dir = REPO_ROOT / \"docs\" / \"adr\"\n+        sadrs = sorted(adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\"))\n+        self.assertEqual(\n+            {path.name.split(\"-\", 2)[1] for path in sadrs},\n+            {f\"{number:04d}\" for number in range(1, 20)},\n+        )\n+        self.assertEqual(\n+            list(adr_dir.glob(\"[0-9][0-9][0-9][0-9]-*.md\")),\n+            [],\n+            \"the Shiki platform tree must not retain legacy numeric SADR paths\",\n+        )\n \n \n class FixtureAdrReferences(unittest.TestCase):\n@@ -180,6 +224,26 @@\n         self.assertIn(\"9991\", message)  # cited number\n         self.assertIn(\"docs/adr/9991-*.md\", message)  # missing path\n \n+    def test_target_adr_does_not_satisfy_missing_platform_sadr(self) -> None:\n+        root = self._make_repo(\n+            adr_names=[\"0042-target-decision.md\"],\n+            script_body='\"\"\"Target ADR 0042 exists, but platform SADR-0042 does not.\"\"\"\\n',\n+        )\n+        violations = find_adr_reference_violations(root, include_docs=False)\n+        self.assertEqual(len(violations), 1)\n+        self.assertEqual(violations[0].namespace, \"SADR\")\n+        self.assertEqual(violations[0].number, \"0042\")\n+        self.assertEqual(violations[0].expected_path, \"docs/adr/SADR-0042-*.md\")\n+\n+    def test_target_adr_and_platform_sadr_may_reuse_a_number(self) -> None:\n+        root = self._make_repo(\n+            adr_names=[\"0042-target-decision.md\", \"SADR-0042-platform-decision.md\"],\n+            script_body='\"\"\"Target ADR 0042 and platform SADR-0042 are distinct.\"\"\"\\n',\n+        )\n+        self.assertEqual(\n+            find_adr_reference_violations(root, include_docs=False), []\n+        )\n+\n     def test_uncited_adr_is_allowed(self) -> None:\n         # An ADR exists but nothing cites it -> not a violation. The converse\n         # (every ADR must be cited) is explicitly NOT required.\ndiff --git a/tests/test_guardian_review.py b/tests/test_guardian_review.py\nindex 88829e7810212635e2fc98aa5918d6f26294677d..00a85a237acd5bf2d6cb5b294128198c422402ca\n--- a/tests/test_guardian_review.py\n+++ b/tests/test_guardian_review.py\n@@ -162,7 +162,7 @@\n \n     def test_docs_only_is_light_and_not_conservative(self):\n         focus = gr.classify_review_focus([\"docs/adr/0099-x.md\", \"README.md\"], \"\", \"low\")\n-        self.assertTrue(any(\"Docs/ADR-only\" in f for f in focus))\n+        self.assertTrue(any(\"Docs/decision-record-only\" in f for f in focus))\n         self.assertFalse(any(\"conservative default\" in f.lower() for f in focus))\n \n     def test_unknown_category_is_conservative(self):\ndiff --git a/tests/test_policy_job_merged_prs.py b/tests/test_policy_job_merged_prs.py\nindex d45512e74681e5e649279236c7d4e79d5c3da471..b97989f8bd74d091de1a6b5275a20d8dc68c4512\n--- a/tests/test_policy_job_merged_prs.py\n+++ b/tests/test_policy_job_merged_prs.py\n@@ -3,8 +3,8 @@\n \n ``.github/workflows/shiki-cca-completion.yml`` has two consumers of the merged-PR\n proof: the CCA *signal* (``guardian_approval_signal.py`` in \"Prepare CCA context\")\n-and the authoritative gate (``mergegate_check.py`` in \"Enforce MergeGate\"). ADR\n-0017's bookkeeping-closeout exemption (condition 6) needs the merged implementation\n+and the authoritative gate (``mergegate_check.py`` in \"Enforce MergeGate\").\n+SADR-0017's bookkeeping-closeout exemption (condition 6) needs the merged implementation\n PR proven, and a closeout carries NO ``post_merge_reconcile`` marker. The signal\n already resolves ``MERGED_PRS`` unconditionally, but \"Enforce MergeGate\" used to\n resolve it only inside ``grep -q '<!-- shiki:post_merge_reconcile -->'``. So a\ndiff --git a/tests/test_sadr_install_migration.py b/tests/test_sadr_install_migration.py\nnew file mode 100644\nindex 0000000000000000000000000000000000000000..d51eac872b1c79ff4579c4c1401ca5d775a60d8d\n--- /dev/null\n+++ b/tests/test_sadr_install_migration.py\n@@ -0,0 +1,237 @@\n+\"\"\"SADR installation and collision-safe legacy upgrade behavior.\"\"\"\n+\n+from __future__ import annotations\n+\n+import contextlib\n+import hashlib\n+import io\n+import json\n+import shutil\n+import tempfile\n+import unittest\n+from pathlib import Path\n+\n+import shiki_test_support  # noqa: F401  (path bootstrap)\n+\n+import shiki_installer\n+from tests.test_adr_references import find_adr_reference_violations\n+\n+\n+PRODUCT_NUMBERS = tuple(range(13, 19))\n+\n+\n+def _quiet_install(target: Path, *, force: bool, validate: bool) -> None:\n+    with contextlib.redirect_stdout(io.StringIO()):\n+        shiki_installer.install_template(target, force=force, validate=validate)\n+\n+\n+def _sha256(path: Path) -> str:\n+    return hashlib.sha256(path.read_bytes()).hexdigest()\n+\n+\n+def _legacy_sadr_paths() -> tuple[str, ...]:\n+    return tuple(\n+        f\"docs/adr/{path.name.removeprefix('SADR-')}\"\n+        for path in sorted((shiki_installer.ROOT / \"docs\" / \"adr\").glob(\"SADR-*.md\"))\n+        if path.name != \"SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\"\n+    )\n+\n+\n+def _create_legacy_upgrade_fixture(\n+    target: Path,\n+) -> tuple[dict[str, bytes], tuple[str, ...]]:\n+    adr_dir = target / \"docs\" / \"adr\"\n+    adr_dir.mkdir(parents=True)\n+    product_bytes = {\n+        f\"{number:04d}-cubeshare-product-decision.md\": (\n+            f\"# CubeShare ADR {number:04d}\\n\\nDo not rewrite {number}.\\n\"\n+        ).encode()\n+        for number in PRODUCT_NUMBERS\n+    }\n+    for name, content in product_bytes.items():\n+        (adr_dir / name).write_bytes(content)\n+\n+    legacy_paths = _legacy_sadr_paths()\n+    for relative in legacy_paths:\n+        (target / relative).write_bytes(f\"legacy Shiki bytes for {relative}\\n\".encode())\n+\n+    stamp_path = target / shiki_installer.INSTALL_STAMP_PATH\n+    stamp_path.parent.mkdir(parents=True, exist_ok=True)\n+    stamp = {\n+        \"version\": shiki_installer.INSTALL_STAMP_VERSION,\n+        \"platform_commit\": None,\n+        \"installed_at\": \"2000-01-01T00:00:00+00:00\",\n+        \"digests\": {\n+            relative: _sha256(target / relative) for relative in legacy_paths\n+        },\n+    }\n+    stamp_path.write_text(\n+        json.dumps(stamp, indent=2, sort_keys=True) + \"\\n\", encoding=\"utf-8\"\n+    )\n+    return product_bytes, legacy_paths\n+\n+\n+def _tree_snapshot(root: Path) -> dict[str, tuple[str, bytes | str]]:\n+    \"\"\"Recursive byte/shape snapshot used to prove preflight is write-free.\"\"\"\n+    snapshot: dict[str, tuple[str, bytes | str]] = {}\n+    for path in sorted(root.rglob(\"*\")):\n+        relative = path.relative_to(root).as_posix()\n+        if path.is_symlink():\n+            snapshot[relative] = (\"symlink\", path.readlink().as_posix())\n+        elif path.is_dir():\n+            snapshot[relative] = (\"directory\", \"\")\n+        else:\n+            snapshot[relative] = (\"file\", path.read_bytes())\n+    return snapshot\n+\n+\n+class FreshSadrInstallTests(unittest.TestCase):\n+    def setUp(self) -> None:\n+        self.tmp = Path(tempfile.mkdtemp(prefix=\"shiki-sadr-fresh-\"))\n+        self.addCleanup(lambda: shutil.rmtree(self.tmp, ignore_errors=True))\n+        self.target = self.tmp / \"target\"\n+        self.adr_dir = self.target / \"docs\" / \"adr\"\n+        self.adr_dir.mkdir(parents=True)\n+        self.product_bytes = {\n+            f\"{number:04d}-product-decision.md\": (\n+                f\"# Product ADR {number:04d}\\n\\nTarget-owned bytes {number}.\\n\"\n+            ).encode()\n+            for number in PRODUCT_NUMBERS\n+        }\n+        for name, content in self.product_bytes.items():\n+            (self.adr_dir / name).write_bytes(content)\n+\n+    def test_fresh_install_preserves_product_adrs_and_installs_all_sadrs(self) -> None:\n+        _quiet_install(self.target, force=False, validate=True)\n+\n+        for name, expected in self.product_bytes.items():\n+            self.assertEqual((self.adr_dir / name).read_bytes(), expected)\n+\n+        sadr_numbers = {\n+            path.name.split(\"-\", 2)[1]\n+            for path in self.adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\")\n+        }\n+        self.assertEqual(sadr_numbers, {f\"{number:04d}\" for number in range(1, 20)})\n+\n+        numeric_records = {\n+            path.name\n+            for path in self.adr_dir.glob(\"[0-9][0-9][0-9][0-9]-*.md\")\n+        }\n+        self.assertEqual(numeric_records, set(self.product_bytes))\n+        stamp = json.loads(\n+            (self.target / shiki_installer.INSTALL_STAMP_PATH).read_text(\n+                encoding=\"utf-8\"\n+            )\n+        )\n+        for path in self.adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\"):\n+            relative = path.relative_to(self.target).as_posix()\n+            self.assertEqual(stamp[\"digests\"][relative], _sha256(path))\n+        self.assertEqual(find_adr_reference_violations(self.target), [])\n+\n+\n+class LegacySadrUpgradeTests(unittest.TestCase):\n+    def setUp(self) -> None:\n+        self.tmp = Path(tempfile.mkdtemp(prefix=\"shiki-sadr-upgrade-\"))\n+        self.addCleanup(lambda: shutil.rmtree(self.tmp, ignore_errors=True))\n+        self.target = self.tmp / \"target\"\n+        self.adr_dir = self.target / \"docs\" / \"adr\"\n+        self.product_bytes, self.legacy_paths = _create_legacy_upgrade_fixture(\n+            self.target\n+        )\n+\n+    def test_force_upgrade_deletes_only_stamped_legacy_sadrs(self) -> None:\n+        _quiet_install(self.target, force=True, validate=True)\n+\n+        for relative in self.legacy_paths:\n+            self.assertFalse((self.target / relative).exists(), relative)\n+        for name, expected in self.product_bytes.items():\n+            self.assertEqual((self.adr_dir / name).read_bytes(), expected)\n+\n+        sadrs = list(self.adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\"))\n+        self.assertEqual(len(sadrs), 19)\n+        refreshed = json.loads(\n+            (self.target / shiki_installer.INSTALL_STAMP_PATH).read_text(encoding=\"utf-8\")\n+        )\n+        for relative in self.legacy_paths:\n+            self.assertNotIn(relative, refreshed[\"digests\"])\n+        for path in sadrs:\n+            relative = path.relative_to(self.target).as_posix()\n+            self.assertEqual(refreshed[\"digests\"][relative], _sha256(path))\n+\n+    def test_non_force_install_never_deletes_legacy_files(self) -> None:\n+        before = {\n+            relative: (self.target / relative).read_bytes()\n+            for relative in self.legacy_paths\n+        }\n+        _quiet_install(self.target, force=False, validate=False)\n+        for relative, expected in before.items():\n+            self.assertEqual((self.target / relative).read_bytes(), expected)\n+        refreshed = json.loads(\n+            (self.target / shiki_installer.INSTALL_STAMP_PATH).read_text(encoding=\"utf-8\")\n+        )\n+        for relative in self.legacy_paths:\n+            self.assertEqual(refreshed[\"digests\"][relative], _sha256(self.target / relative))\n+\n+\n+class LegacyCleanupPreflightTests(unittest.TestCase):\n+    def setUp(self) -> None:\n+        self.tmp = Path(tempfile.mkdtemp(prefix=\"shiki-sadr-preflight-\"))\n+        self.addCleanup(lambda: shutil.rmtree(self.tmp, ignore_errors=True))\n+\n+    def test_all_ownership_blockers_abort_before_any_write(self) -> None:\n+        cases = (\n+            (\"absent\", \"install stamp is absent\"),\n+            (\"unreadable\", \"install stamp is unreadable\"),\n+            (\"malformed\", \"install stamp is malformed\"),\n+            (\"missing-digest\", \"install stamp has no digest\"),\n+            (\"digest-mismatch\", \"digest mismatch\"),\n+        )\n+        for name, expected_reason in cases:\n+            with self.subTest(name=name):\n+                target = self.tmp / name\n+                _, legacy_paths = _create_legacy_upgrade_fixture(target)\n+                stamp_path = target / shiki_installer.INSTALL_STAMP_PATH\n+\n+                if name == \"absent\":\n+                    stamp_path.unlink()\n+                elif name == \"unreadable\":\n+                    stamp_path.unlink()\n+                    stamp_path.mkdir()\n+                elif name == \"malformed\":\n+                    stamp_path.write_text(\"{not-json\\n\", encoding=\"utf-8\")\n+                else:\n+                    stamp = json.loads(stamp_path.read_text(encoding=\"utf-8\"))\n+                    if name == \"missing-digest\":\n+                        stamp[\"digests\"] = {}\n+                    else:\n+                        stamp[\"digests\"] = {\n+                            relative: \"deadbeef\" for relative in legacy_paths\n+                        }\n+                    stamp_path.write_text(\n+                        json.dumps(stamp, indent=2, sort_keys=True) + \"\\n\",\n+                        encoding=\"utf-8\",\n+                    )\n+\n+                before = _tree_snapshot(target)\n+                with self.assertRaises(shiki_installer.ShikiError) as raised:\n+                    _quiet_install(target, force=True, validate=False)\n+                after = _tree_snapshot(target)\n+\n+                self.assertEqual(before, after, f\"{name} preflight changed the tree\")\n+                message = str(raised.exception)\n+                self.assertIn(expected_reason, message)\n+                self.assertIn(\"No files were written\", message)\n+                for relative in legacy_paths:\n+                    self.assertIn(relative, message)\n+\n+    def test_absent_stamp_is_allowed_when_no_legacy_path_exists(self) -> None:\n+        target = self.tmp / \"no-legacy\"\n+        target.mkdir()\n+        _quiet_install(target, force=True, validate=False)\n+        self.assertTrue(\n+            (target / \"docs\" / \"adr\" / \"SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\").is_file()\n+        )\n+\n+\n+if __name__ == \"__main__\":\n+    unittest.main()\n\n2026-08-07T08:28:26.712649Z ERROR codex_models_manager::manager: failed to renew cache TTL: missing field `base_instructions` at line 94 column 5\nexec\n/bin/zsh -lc 'git status --short' in /private/tmp/shiki-adr-collision.pa0KwY/.worktrees/shiki-t-20260807t073307221254z-2d22d654-introduc\n succeeded in 0ms:\n M .codex/skills/shiki/SKILL.md\n M .github/prompts/cca-completion-check.md\n M .github/prompts/codex-task-handoff.md\n M .github/workflows/release.yml\n M .github/workflows/shiki-cca-completion.yml\n M .github/workflows/shiki-mergegate.yml\n M .shiki/tasks/T-20260807T073307221254Z-2d22d654.json\n M AGENTS.md\n M CLAUDE.md\n M CONTEXT.md\n M CONTRIBUTING.md\n M SYSTEM_PROMPT.md\n D docs/adr/0001-keep-shiki-as-platform-name.md\n D docs/adr/0002-use-agents-as-shared-constitution.md\n D docs/adr/0003-require-skill-gate-for-mergegate-readiness.md\n D docs/adr/0004-use-github-first-with-shiki-mirror.md\n D docs/adr/0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\n D docs/adr/0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\n D docs/adr/0007-packaging-and-release.md\n D docs/adr/0008-use-claude-code-as-default-implementer.md\n D docs/adr/0009-requirements-definition-and-spec-freeze.md\n D docs/adr/0010-external-ai-guardian-review.md\n D docs/adr/0011-autonomous-quality-gate-evidence.md\n D docs/adr/0012-autonomous-loop-closeout-to-main.md\n D docs/adr/0013-bootstrap-grants-actions-approve-pull-requests.md\n D docs/adr/0014-codex-app-external-ai-guardian-ui-adapter.md\n D docs/adr/0015-contract-approval-before-dispatch.md\n D docs/adr/0016-locks-bound-product-paths.md\n D docs/adr/0017-bookkeeping-closeouts-do-not-inherit-risk.md\n D docs/adr/0018-carried-guardian-approval-across-a-proven-base-sync.md\n M docs/adr/README.md\n M docs/adr/template.md\n M docs/agents/autonomous-loop-e2e-acceptance.md\n M docs/agents/bootstrap-command.md\n M docs/agents/checklists.md\n M docs/agents/codex-handoff.md\n M docs/agents/completion-check-agent.md\n M docs/agents/decision-control.md\n M docs/agents/domain.md\n M docs/agents/execution-control.md\n M docs/agents/guardian-policy.md\n M docs/agents/implementation-policy.md\n M docs/agents/runtime-auth-model.md\n M docs/agents/runtime-registry.md\n M docs/agents/shiki-cli-architecture.md\n M docs/agents/skill-gate.md\n M docs/agents/triage-labels.md\n M docs/operations.md\n M docs/proposals/0001-memory-loop-spec-freeze-review.md\n M docs/proposals/0002-autonomous-loop-completion.md\n M docs/releasing.md\n M docs/usage.md\n M pyproject.toml\n M scripts/guardian_approval_signal.py\n M scripts/mergegate_check.py\n M scripts/shiki_bootstrap.py\n M scripts/shiki_cli.py\n M scripts/shiki_contract_approval.py\n M scripts/shiki_github.py\n M scripts/shiki_guardian.py\n M scripts/shiki_guardian_review.py\n M scripts/shiki_installer.py\n M scripts/shiki_loop.py\n M scripts/shiki_migrations.py\n M scripts/shiki_runtime_adapters.py\n M scripts/shiki_runtime_registry.py\n M scripts/shiki_tasks.py\n M scripts/test_shiki_adversarial_state.sh\n M scripts/test_shiki_code_review_gate.sh\n M scripts/test_shiki_control_plane.sh\n M scripts/test_shiki_goal_loop.sh\n M scripts/test_shiki_governance_evidence.sh\n M scripts/test_shiki_guardian_policy.sh\n M scripts/test_shiki_loop_e2e.sh\n M scripts/test_shiki_loop_lock_guard.sh\n M scripts/test_shiki_run_orchestrator.sh\n M scripts/test_shiki_runner_claude.sh\n M scripts/test_shiki_runtime_registry.sh\n M scripts/test_shiki_start.sh\n M scripts/validate_shiki.py\n M tests/test_adr_references.py\n M tests/test_bookkeeping_closeout.py\n M tests/test_branch_behind_sync.py\n M tests/test_cca_evidence_manifest_taskless.py\n M tests/test_closeout.py\n M tests/test_completion_lock_scope.py\n M tests/test_contract_amendment.py\n M tests/test_contract_approval.py\n M tests/test_contract_immutability.py\n M tests/test_contract_mode.py\n M tests/test_contract_open.py\n M tests/test_contract_plan_registration.py\n M tests/test_dispatch_lock_precondition.py\n M tests/test_guardian_review.py\n M tests/test_guardian_signal.py\n M tests/test_guardian_signal_closeout.py\n M tests/test_loop_branch_plumbing.py\n M tests/test_loop_code_review.py\n M tests/test_loop_e2e_contract.py\n M tests/test_loop_engine.py\n M tests/test_loop_lock_validator_hint.py\n M tests/test_loop_tdd_evidence.py\n M tests/test_mirror_scope.py\n M tests/test_paired_invariants.py\n M tests/test_policy_job_merged_prs.py\n M tests/test_repair_delivery.py\n M tests/test_runner_adapters.py\n M tests/test_workflow_invocation.py\n M tests/test_workflow_invocation_carry.py\n?? .shiki/ledger/L-20260807T082800000000Z-c0de0019.json\n?? .shiki/locks/T-20260807T073307221254Z-2d22d654.json\n?? docs/adr/SADR-0001-keep-shiki-as-platform-name.md\n?? docs/adr/SADR-0002-use-agents-as-shared-constitution.md\n?? docs/adr/SADR-0003-require-skill-gate-for-mergegate-readiness.md\n?? docs/adr/SADR-0004-use-github-first-with-shiki-mirror.md\n?? docs/adr/SADR-0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\n?? docs/adr/SADR-0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\n?? docs/adr/SADR-0007-packaging-and-release.md\n?? docs/adr/SADR-0008-use-claude-code-as-default-implementer.md\n?? docs/adr/SADR-0009-requirements-definition-and-spec-freeze.md\n?? docs/adr/SADR-0010-external-ai-guardian-review.md\n?? docs/adr/SADR-0011-autonomous-quality-gate-evidence.md\n?? docs/adr/SADR-0012-autonomous-loop-closeout-to-main.md\n?? docs/adr/SADR-0013-bootstrap-grants-actions-approve-pull-requests.md\n?? docs/adr/SADR-0014-codex-app-external-ai-guardian-ui-adapter.md\n?? docs/adr/SADR-0015-contract-approval-before-dispatch.md\n?? docs/adr/SADR-0016-locks-bound-product-paths.md\n?? docs/adr/SADR-0017-bookkeeping-closeouts-do-not-inherit-risk.md\n?? docs/adr/SADR-0018-carried-guardian-approval-across-a-proven-base-sync.md\n?? docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\n?? tests/test_sadr_install_migration.py\n\nexec\n/bin/zsh -lc 'git diff --check' in /private/tmp/shiki-adr-collision.pa0KwY/.worktrees/shiki-t-20260807t073307221254z-2d22d654-introduc\n succeeded in 0ms:\nexec\n/bin/zsh -lc \"python3 -m unittest discover -s tests -p 'test_adr_references.py' -v\" in /private/tmp/shiki-adr-collision.pa0KwY/.worktrees/shiki-t-20260807t073307221254z-2d22d654-introduc\n succeeded in 0ms:\ntest_ambiguous_when_two_documents_share_a_number (test_adr_references.FixtureAdrReferences.test_ambiguous_when_two_documents_share_a_number) ... ok\ntest_fails_when_citation_has_no_document (test_adr_references.FixtureAdrReferences.test_fails_when_citation_has_no_document) ... ok\ntest_passes_when_citation_resolves (test_adr_references.FixtureAdrReferences.test_passes_when_citation_resolves) ... ok\ntest_target_adr_and_platform_sadr_may_reuse_a_number (test_adr_references.FixtureAdrReferences.test_target_adr_and_platform_sadr_may_reuse_a_number) ... ok\ntest_target_adr_does_not_satisfy_missing_platform_sadr (test_adr_references.FixtureAdrReferences.test_target_adr_does_not_satisfy_missing_platform_sadr) ... ok\ntest_uncited_adr_is_allowed (test_adr_references.FixtureAdrReferences.test_uncited_adr_is_allowed) ... ok\ntest_carried_records_are_indexed (test_adr_references.RealRepoAdrReferences.test_carried_records_are_indexed) ... ok\ntest_every_cited_adr_resolves (test_adr_references.RealRepoAdrReferences.test_every_cited_adr_resolves) ... ok\ntest_platform_citations_do_not_use_unresolved_numeric_shorthand (test_adr_references.RealRepoAdrReferences.test_platform_citations_do_not_use_unresolved_numeric_shorthand) ... ok\ntest_platform_tree_has_only_sadr_0001_through_0019 (test_adr_references.RealRepoAdrReferences.test_platform_tree_has_only_sadr_0001_through_0019) ... ok\ntest_scan_actually_reads_scripts (test_adr_references.RealRepoAdrReferences.test_scan_actually_reads_scripts) ... ok\n\n----------------------------------------------------------------------\nRan 11 tests in 0.041s\n\nOK\n\nexec\n/bin/zsh -lc 'python3 scripts/validate_shiki.py --target .' in /private/tmp/shiki-adr-collision.pa0KwY/.worktrees/shiki-t-20260807t073307221254z-2d22d654-introduc\nexec\n/bin/zsh -lc \"python3 -m unittest discover -s tests -p 'test_sadr_install_migration.py' -v\" in /private/tmp/shiki-adr-collision.pa0KwY/.worktrees/shiki-t-20260807t073307221254z-2d22d654-introduc\n succeeded in 241ms:\nShiki validation passed\n\ndiff --git a/.codex/skills/shiki/SKILL.md b/.codex/skills/shiki/SKILL.md\nindex c8e2e85d9d75d49cc4f2d6286ee22659c4e53e75..93cded623b2c15d9f65c76491a1c2d813ac9e6c0\n--- a/.codex/skills/shiki/SKILL.md\n+++ b/.codex/skills/shiki/SKILL.md\n@@ -18,6 +18,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n Codex CLI does not register this skill as a custom `/shiki` slash command. When\n the user types `/shiki` in Codex CLI and gets \"Unrecognized command\", explain\n that this is expected: invoke Shiki in Codex with natural language such as\n@@ -69,7 +72,7 @@\n \n ## Responsibilities\n \n-- Claude Code implements and repairs by default (`shiki runner claude`, ADR 0008); Codex implements and repairs tasks explicitly assigned to `codex`.\n+- Claude Code implements and repairs by default (`shiki runner claude`, SADR-0008); Codex implements and repairs tasks explicitly assigned to `codex`.\n - Claude Code Action can act as GitHub-side CCA or reviewer.\n - CCA judges completion.\n - MergeGate authorizes state transitions and merge readiness.\ndiff --git a/.github/prompts/cca-completion-check.md b/.github/prompts/cca-completion-check.md\nindex ed203ad5f302ecc81372d8ab27d2b1232ca97497..fd89950528333ef6242e6d5443904f5392ab7e56\n--- a/.github/prompts/cca-completion-check.md\n+++ b/.github/prompts/cca-completion-check.md\n@@ -19,6 +19,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n ## Required Reading\n \n Read only the files needed to judge the PR contract. Prefer this order:\ndiff --git a/.github/prompts/codex-task-handoff.md b/.github/prompts/codex-task-handoff.md\nindex 695e211d76dea1c60e8d5bb360e78487808286a4..59441d576b01a39569722f226c854e5a740e9eb5\n--- a/.github/prompts/codex-task-handoff.md\n+++ b/.github/prompts/codex-task-handoff.md\n@@ -2,7 +2,7 @@\n \n You are Codex Front acting as the Implementer for a Shiki task.\n \n-Follow `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant ADRs, and `docs/agents/codex-handoff.md`.\n+Follow `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant Shiki SADRs and target ADRs, and `docs/agents/codex-handoff.md`.\n \n Rules:\n \ndiff --git a/.shiki/ledger/L-20260807T082800000000Z-c0de0019.json b/.shiki/ledger/L-20260807T082800000000Z-c0de0019.json\nnew file mode 100644\nindex 0000000000000000000000000000000000000000..bac355ecea44037859b3715f58bc32f3353cfcfa\n--- /dev/null\n+++ b/.shiki/ledger/L-20260807T082800000000Z-c0de0019.json\n@@ -0,0 +1,34 @@\n+{\n+  \"actor\": \"codex\",\n+  \"evidence\": [\n+    \"Full task diff reviewed against scope, non-goals, locks, acceptance checks, failure atomicity, path ownership, and compatibility requirements\",\n+    \"Finding repaired: tests/test_sadr_install_migration.py now asserts every fresh-installed SADR path has the matching SHA-256 digest in .shiki/install-stamp.json\",\n+    \"Re-review found no remaining blocking issues; rejected findings: none\",\n+    \"python3 -m unittest discover -s tests: 1147 tests passed\",\n+    \"bash scripts/test_shiki_control_plane.sh: passed\",\n+    \"bash scripts/test_shiki_adversarial_state.sh: passed\",\n+    \"python3 scripts/validate_shiki.py --target .: passed\"\n+  ],\n+  \"goal_id\": \"G-20260807T073307219752Z-1ee1ed68\",\n+  \"id\": \"L-20260807T082800000000Z-c0de0019\",\n+  \"links\": [],\n+  \"review\": {\n+    \"applied_fixes\": [\n+      \"Added direct fresh-install stamp path/digest assertions and reran focused plus full verification\"\n+    ],\n+    \"findings\": [\n+      {\n+        \"detail\": \"The fresh-install test proved SADR files were installed but did not directly prove their paths and digests were recorded in the refreshed install stamp.\",\n+        \"resolution\": \"fixed_and_re_reviewed\",\n+        \"severity\": \"blocking\",\n+        \"title\": \"Fresh-install stamp evidence was indirect\"\n+      }\n+    ],\n+    \"rejected_findings\": [],\n+    \"verdict\": \"clean_after_repairs\"\n+  },\n+  \"summary\": \"code-review implementer verdict CLEAN after re-review: findings=1; applied_fixes=1 (fresh-install stamp-digest assertion); rejected_findings=0 (none)\",\n+  \"task_id\": \"T-20260807T073307221254Z-2d22d654\",\n+  \"timestamp\": \"2026-08-07T08:28:00+00:00\",\n+  \"type\": \"review\"\n+}\ndiff --git a/.shiki/tasks/T-20260807T073307221254Z-2d22d654.json b/.shiki/tasks/T-20260807T073307221254Z-2d22d654.json\nindex e7e150cc1637f1cfac2199e04055e93b53630dcb..78f7fb5ef2ab3dad7e3731673ac2c34b2a8ed149\n--- a/.shiki/tasks/T-20260807T073307221254Z-2d22d654.json\n+++ b/.shiki/tasks/T-20260807T073307221254Z-2d22d654.json\n@@ -36,7 +36,8 @@\n     \"L-20260807T080107948254Z-61f24da0\",\n     \"L-20260807T080228312210Z-07ac626e\",\n     \"L-20260807T080228575038Z-d574f629\",\n-    \"L-20260807T080256330734Z-5bf40cb6\"\n+    \"L-20260807T080256330734Z-5bf40cb6\",\n+    \"L-20260807T082800000000Z-c0de0019\"\n   ],\n   \"locks\": [\n     \"path:.codex/**\",\ndiff --git a/AGENTS.md b/AGENTS.md\nindex 7f041100d49c2a7525f51cb0f5d696a4a6d1c78c..a06bfbabc6fa48a007389e25e12d7ed523936bed\n--- a/AGENTS.md\n+++ b/AGENTS.md\n@@ -22,7 +22,7 @@\n - **Codex Front** is an optional implementer through Codex App, Codex CLI, Codex IDE extension, or Codex Web signed in with ChatGPT OAuth/subscription auth, dispatched with `shiki runner codex` when a task is explicitly assigned to `codex`.\n - **GitHub CCA** is implemented by Claude Code Action by default, using `CLAUDE_CODE_OAUTH_TOKEN`.\n \n-Do not assume `openai/codex-action`, `OPENAI_API_KEY`, or API-key based Codex automation in the default Shiki path. API-key runners are explicit target-repo extensions and require their own ADR.\n+Do not assume `openai/codex-action`, `OPENAI_API_KEY`, or API-key based Codex automation in the default Shiki path. API-key runners are explicit target-repo extensions and require their own target ADR.\n \n Shiki is not:\n \n@@ -44,10 +44,14 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use\n+`SADR-NNNN-*.md`; target product decisions retain `NNNN-*.md` and the `ADR`\n+namespace.\n+\n Use the canonical source-of-truth order above. Runtime-specific reading orders\n are operational guidance only and must not override or reorder it.\n \n-Conversation state is not durable truth. If a decision matters, put it in GitHub, `.shiki/`, `CONTEXT.md`, or an ADR.\n+Conversation state is not durable truth. If a decision matters, put it in GitHub, `.shiki/`, `CONTEXT.md`, a Shiki SADR, or a target ADR.\n \n If GitHub and `.shiki/` disagree, surface the conflict and prefer GitHub operational state until the mirror is repaired.\n \n@@ -75,8 +79,8 @@\n Every non-trivial change follows this loop:\n \n 1. **Goal**: clarify outcome, completion conditions, non-goals, risk level, and success signals.\n-2. **grill-with-docs**: challenge the plan against domain docs, ADRs, code reality, terminology, and edge scenarios.\n-3. **Context & Impact**: find relevant docs, ADRs, code, dependencies, owners, locks, and verification surfaces.\n+2. **grill-with-docs**: challenge the plan against domain docs, Shiki SADRs, target ADRs, code reality, terminology, and edge scenarios.\n+3. **Context & Impact**: find relevant docs, SADRs, target ADRs, code, dependencies, owners, locks, and verification surfaces.\n 4. **PRD and Spec Freeze**: use `to-prd` when context is settled enough to become durable product/engineering intent. The operator's explicit approval of the PRD is Spec Freeze: the plan records a `spec_freeze` block, and no plan runs without it. Steps 1-4 form Requirements Definition \u2014 one continuous operator dialogue. After Spec Freeze, scope changes require an operator-approved Spec Amendment; non-scope-moving interpretations go to the Assumption Log.\n 5. **Issues**: use `to-issues` to create independently grabbable vertical-slice issues. Prefer AFK slices over HITL slices where possible.\n 6. **Triage**: label issues for readiness, risk, runtime, skills, and MergeGate state.\n@@ -167,7 +171,7 @@\n - Testing decisions.\n - Out-of-scope items.\n - Further notes.\n-- Links to relevant domain terms and ADRs.\n+- Links to relevant domain terms, SADRs, and target ADRs.\n \n Every executable task must include:\n \n@@ -193,7 +197,7 @@\n \n Context & Impact must identify:\n \n-- Relevant documents, domain terms, ADRs, and past decisions.\n+- Relevant documents, domain terms, SADRs, target ADRs, and past decisions.\n - Relevant modules, interfaces, seams, callers, owners, and tests.\n - Dependency relationships between tasks.\n - Candidate locks and lock conflicts.\n@@ -210,7 +214,7 @@\n | Skill | Required when |\n | --- | --- |\n | `setup-matt-pocock-skills` | First configuring the repo for skills, or when issue tracker, triage labels, or domain docs are missing. |\n-| `grill-with-docs` | Default for non-trivial Goals before PRD/issues; always required when plans, terminology, boundaries, tradeoffs, or ADR-worthy decisions are ambiguous. |\n+| `grill-with-docs` | Default for non-trivial Goals before PRD/issues; always required when plans, terminology, boundaries, tradeoffs, or decision-record-worthy choices are ambiguous. |\n | `zoom-out` | The agent lacks an architectural map of the relevant code area. |\n | `to-prd` | Settled Goal context must become durable product/engineering intent. |\n | `to-issues` | A Goal, PRD, or plan must become independently grabbable vertical-slice issues. |\n@@ -348,7 +352,7 @@\n - Three or more new files.\n - Any high-risk or critical label.\n \n-When the architecture gate triggers, run `grill-with-docs` or `improve-codebase-architecture` as appropriate and record durable decisions in an ADR when needed.\n+When the architecture gate triggers, run `grill-with-docs` or `improve-codebase-architecture` as appropriate and record durable decisions in a Shiki SADR or target ADR as applicable.\n \n ## Engineering Discipline\n \ndiff --git a/CLAUDE.md b/CLAUDE.md\nindex 98a932e7841a632866fcd53b0bad0e3af3711d3d..98a31399404d6fb044b4fd493f4459ac87f439de\n--- a/CLAUDE.md\n+++ b/CLAUDE.md\n@@ -14,6 +14,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n ## Claude Role\n \n Default to planner, default implementer, reviewer, coordinator, documentation editor, CCA-style completion judge, and final-judgment assistant.\n@@ -57,7 +60,7 @@\n At the start of a Shiki session:\n \n 1. Read `AGENTS.md`.\n-2. Read `CONTEXT.md` and relevant ADRs.\n+2. Read `CONTEXT.md` and relevant Shiki SADRs and target ADRs.\n 3. Read active `.shiki/` Goal/task/ledger state when present.\n 4. Read active distilled rules from `.shiki/memories/` (status `distilled`, `active: true`, not revoked or superseded) for applicable guidance; the same rules are injected deterministically into task handoffs as the `## Distilled Rules` section (\u00a73.5).\n 5. Read the GitHub Issue or PR when the task is GitHub-backed.\n@@ -74,7 +77,7 @@\n - Define the Goal, completion conditions, non-goals, and risk level.\n - Treat `grill-with-docs` as the default planning entry point for non-trivial Goals.\n - Challenge domain terms against `CONTEXT.md`.\n-- Challenge hard-to-reverse tradeoffs against ADRs.\n+- Challenge hard-to-reverse tradeoffs against SADRs and target ADRs.\n - Explore code instead of asking when code can answer the question.\n - Run Context & Impact before implementation planning.\n - Produce a Task DAG, not an unordered checklist.\n@@ -94,7 +97,7 @@\n - For each question, provide your recommended answer.\n - Resolve dependency decisions before downstream design questions.\n - Update `CONTEXT.md` inline when terminology crystallizes.\n-- Offer ADRs only when the decision is hard to reverse, surprising without context, and a real tradeoff.\n+- Offer a SADR or target ADR only when the decision is hard to reverse, surprising without context, and a real tradeoff.\n - Convert unresolved questions into HITL blockers rather than allowing Codex to guess.\n \n ## PRD and Issues Standard\n@@ -103,7 +106,7 @@\n \n - Use the project's domain glossary vocabulary.\n - Avoid volatile file paths unless they are essential.\n-- Include implementation decisions, testing decisions, out-of-scope items, and relevant ADR/domain links.\n+- Include implementation decisions, testing decisions, out-of-scope items, and relevant SADR/target-ADR/domain links.\n - Identify deep-module opportunities and testable interfaces.\n \n When creating issues:\n@@ -121,7 +124,7 @@\n - Branch/worktree target.\n - Scope and non-goals.\n - Dependencies and locks.\n-- Relevant docs, ADRs, modules, and tests.\n+- Relevant docs, SADRs, target ADRs, modules, and tests.\n - Required skill invocations.\n - TDD expectations.\n - Pre-PR `code-review` gate expectations.\n@@ -152,7 +155,7 @@\n \n - Treat yourself as a judge, not implementer.\n - Do not write production code.\n-- Read `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant ADRs, PRD, task issue, PR body, diff, checks, reviews, labels, and `.shiki/` state.\n+- Read `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant SADRs and target ADRs, PRD, task issue, PR body, diff, checks, reviews, labels, and `.shiki/` state.\n - Evaluate every applicable checklist in `docs/agents/checklists.md`.\n - Map each acceptance criterion to durable evidence.\n - Distinguish implementation failure from missing evidence.\ndiff --git a/CONTEXT.md b/CONTEXT.md\nindex e61a9be06a0cce0d026484083a657bde2067e234..237ee8aa3ea8fab334ece49768bc5ed3b5af0974\n--- a/CONTEXT.md\n+++ b/CONTEXT.md\n@@ -25,7 +25,7 @@\n _Avoid_: jumping directly into code\n \n **grill-with-docs**:\n-A planning skill that challenges a plan against domain language, ADRs, code reality, and concrete edge scenarios. It resolves design-tree questions before PRD/issues. It runs inside Requirements Definition.\n+A planning skill that challenges a plan against domain language, Shiki SADRs, target ADRs, code reality, and concrete edge scenarios. It resolves design-tree questions before PRD/issues. It runs inside Requirements Definition.\n _Avoid_: generic brainstorming, silent assumptions\n \n **Requirements Definition**:\ndiff --git a/CONTRIBUTING.md b/CONTRIBUTING.md\nindex c6d9b656e73abb8a55e5f660123e03153dcb3e6d..9e37a8fbf668db5b22461aaae5548b8f1354394c\n--- a/CONTRIBUTING.md\n+++ b/CONTRIBUTING.md\n@@ -17,7 +17,7 @@\n 1. **Goal** \u2014 start from a user-approved Goal with an outcome, completion\n    conditions, non-goals, risk level, and success signals.\n 2. **grill-with-docs** \u2014 for any non-trivial change, challenge the plan against\n-   domain language, ADRs, and code reality before writing a PRD.\n+   domain language, Shiki SADRs, target ADRs, and code reality before writing a PRD.\n 3. **PRD** \u2014 capture settled product/engineering intent (`to-prd`).\n 4. **Spec Freeze** \u2014 the operator explicitly approves the PRD/requirements;\n    the plan records a `spec_freeze` block and no plan runs without it\n@@ -94,7 +94,7 @@\n   entries.\n - Do not hand-edit `.shiki/goals/*.json` or `.shiki/manifest.json` in a task PR.\n - Conversation state is not durable truth. If a decision matters, record it in\n-  GitHub, `.shiki/`, `CONTEXT.md`, or an ADR.\n+  GitHub, `.shiki/`, `CONTEXT.md`, a Shiki SADR, or a target ADR.\n \n ## Architecture Gate\n \n@@ -103,7 +103,7 @@\n or CI policy, package structure, three or more structural files, more than 100\n net new lines in a core module, three or more new files, or any high-risk label.\n When triggered, run `grill-with-docs` or `improve-codebase-architecture` and\n-record durable decisions in an ADR when needed (see `docs/adr/`).\n+record durable decisions in a Shiki SADR or target ADR as applicable (see `docs/adr/`).\n \n ## Safety\n \ndiff --git a/SYSTEM_PROMPT.md b/SYSTEM_PROMPT.md\nindex aaaea45e3a896c3a44ba89eec8bb84325ecf6c62..da009c86a88285269dee0155e58e550c6d744ea9\n--- a/SYSTEM_PROMPT.md\n+++ b/SYSTEM_PROMPT.md\n@@ -30,6 +30,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n Use the canonical source-of-truth order above. Runtime-specific reading orders\n are operational guidance only and must not override or reorder it. The current\n conversation is non-durable operator input.\n@@ -48,14 +51,14 @@\n \n A runtime may only do work that is explicitly assigned by the Goal, task, PR, or MergeGate decision.\n \n-Do not assume `openai/codex-action`, `OPENAI_API_KEY`, or API-key based Codex automation in the default Shiki path. API-key runners are explicit target-repo extensions and require their own ADR.\n+Do not assume `openai/codex-action`, `OPENAI_API_KEY`, or API-key based Codex automation in the default Shiki path. API-key runners are explicit target-repo extensions and require their own target ADR.\n \n ## 4. Non-negotiable Shiki flow\n \n For any non-trivial Goal, use this flow:\n \n 1. **Goal Seek**: define outcome, non-goals, risk level, completion criteria, and evidence requirements.\n-2. **grill-with-docs**: challenge the plan against `CONTEXT.md`, ADRs, code reality, terminology, boundaries, and edge scenarios. Ask one question at a time when the operator is available. If code can answer a question, inspect code instead of asking.\n+2. **grill-with-docs**: challenge the plan against `CONTEXT.md`, Shiki SADRs, target ADRs, code reality, terminology, boundaries, and edge scenarios. Ask one question at a time when the operator is available. If code can answer a question, inspect code instead of asking.\n 3. **Context & Impact**: identify modules, interfaces, seams, callers, dependencies, locks, risk, and verification surfaces.\n 4. **to-prd + Spec Freeze**: turn settled context into a PRD using domain vocabulary and testing decisions. The operator's explicit approval of the PRD is Spec Freeze, recorded as the plan's `spec_freeze` block; plans without it do not run. Steps 1-4 form Requirements Definition.\n 5. **to-issues**: decompose the PRD into independently grabbable vertical-slice issues. Prefer AFK slices; mark HITL slices when judgment is still required.\n@@ -75,7 +78,7 @@\n Before making any material change:\n \n 1. Read `AGENTS.md` and any runtime-specific wrapper such as `CLAUDE.md`.\n-2. Read `CONTEXT.md` and relevant ADRs.\n+2. Read `CONTEXT.md` and relevant SADRs and target ADRs.\n 3. Read the GitHub Issue / PR and `.shiki/` state for the active Goal or task.\n 4. Identify your role: Planner, Implementer, Reviewer, Completion Check Agent, Repairer, or Guardian-assist.\n 5. Identify Goal id, task id, branch, PR, dependencies, locks, risk level, required skills, and required evidence.\n@@ -103,7 +106,7 @@\n \n At minimum, identify:\n \n-- Relevant documents, ADRs, domain terms, and prior decisions.\n+- Relevant documents, SADRs, target ADRs, domain terms, and prior decisions.\n - Relevant modules, interfaces, seams, callers, owners, and tests.\n - Dependency relationships between tasks.\n - Candidate file locks and lock conflicts.\n@@ -152,7 +155,7 @@\n \n CCA must read:\n \n-- `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant ADRs.\n+- `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant SADRs and target ADRs.\n - Parent Goal issue, PRD issue, task issue, comments, and labels.\n - PR body, diff, commits, changed files, reviews, CI checks, and artifacts available to the workflow.\n - `.shiki/` task contract, locks, ledger entries, CCA checklist profile, and prior repair packets.\n@@ -214,7 +217,7 @@\n - Three or more new files.\n - Any high-risk or critical label.\n \n-When the architecture gate triggers, run `grill-with-docs` or `improve-codebase-architecture` as appropriate and record the decision in an ADR if it is hard to reverse or likely to be re-litigated.\n+When the architecture gate triggers, run `grill-with-docs` or `improve-codebase-architecture` as appropriate and record the decision in a Shiki SADR or target ADR as applicable if it is hard to reverse or likely to be re-litigated.\n \n ## 14. Skill Gate\n \n@@ -223,7 +226,7 @@\n Use these skills:\n \n - `setup-matt-pocock-skills`: before first use of the engineering skill set, or when issue tracker, triage labels, or domain docs are missing.\n-- `grill-with-docs`: default for non-trivial Goals before PRD/issues; required for ambiguous plans, terminology, boundaries, ADR-worthy tradeoffs, or design-tree decisions.\n+- `grill-with-docs`: default for non-trivial Goals before PRD/issues; required for ambiguous plans, terminology, boundaries, decision-record-worthy tradeoffs, or design-tree decisions.\n - `zoom-out`: unfamiliar code area or missing architectural map.\n - `to-prd`: convert settled context into a PRD.\n - `to-issues`: break a PRD, plan, or Goal into independently grabbable vertical-slice issues.\ndiff --git a/docs/adr/0001-keep-shiki-as-platform-name.md b/docs/adr/SADR-0001-keep-shiki-as-platform-name.md\nindex 20a9637c3b9d8c97de7ba8db7cfae4d82d9a7dba..8b685439959aada1b221c112f0ac3c51ca8a1695\n--- a/docs/adr/0001-keep-shiki-as-platform-name.md\n+++ b/docs/adr/SADR-0001-keep-shiki-as-platform-name.md\n@@ -1,3 +1,3 @@\n-# Keep Shiki As The Platform Name\n+# SADR-0001: Keep Shiki As The Platform Name\n \n Shiki remains the product name for the GitHub-native agentic engineering control plane. We considered more descriptive names such as MergePilot, MergeGate, and Kiroku, but kept Shiki because it carries the existing goal-seeking framework identity while remaining independent of Hermes, Codex, Claude Code, or any single runtime.\ndiff --git a/docs/adr/0002-use-agents-as-shared-constitution.md b/docs/adr/SADR-0002-use-agents-as-shared-constitution.md\nindex 73de30ecb078120eac540d9bc51855e3cd48d0e3..c5486182bf06fca36ea508496b013c67302fd989\n--- a/docs/adr/0002-use-agents-as-shared-constitution.md\n+++ b/docs/adr/SADR-0002-use-agents-as-shared-constitution.md\n@@ -1,3 +1,3 @@\n-# Use AGENTS As The Shared Agent Constitution\n+# SADR-0002: Use AGENTS As The Shared Agent Constitution\n \n Shiki uses `AGENTS.md` as the runtime-neutral constitution and `CLAUDE.md` as a thin Claude Code wrapper. This keeps Codex, Claude Code, Hermes Runner, and future Agent Runtimes aligned on the same GitHub-first MergeGate rules while still allowing runtime-specific instructions where needed.\ndiff --git a/docs/adr/0003-require-skill-gate-for-mergegate-readiness.md b/docs/adr/SADR-0003-require-skill-gate-for-mergegate-readiness.md\nindex 424be0c9af4c8f0136f1fde68a468c47ff84e764..7fc337987c7df9792f75f181dfcbed75fe5ffff2\n--- a/docs/adr/0003-require-skill-gate-for-mergegate-readiness.md\n+++ b/docs/adr/SADR-0003-require-skill-gate-for-mergegate-readiness.md\n@@ -1,3 +1,3 @@\n-# Require Skill Gate For MergeGate Readiness\n+# SADR-0003: Require Skill Gate For MergeGate Readiness\n \n Shiki requires relevant engineering skills to be invoked before execution when their triggers apply. This makes skills part of MergeGate readiness rather than optional prompt style, because Shiki depends on repeatable planning, issue decomposition, TDD, diagnosis, triage, and architecture review before autonomous or parallel agent work can be trusted.\ndiff --git a/docs/adr/0004-use-github-first-with-shiki-mirror.md b/docs/adr/SADR-0004-use-github-first-with-shiki-mirror.md\nindex d0a3a9174dc646a945a9d325954ffaf00c352040..a79c54ae9ee8eed2a0524192b864da4e22a92d3f\n--- a/docs/adr/0004-use-github-first-with-shiki-mirror.md\n+++ b/docs/adr/SADR-0004-use-github-first-with-shiki-mirror.md\n@@ -1,4 +1,4 @@\n-# ADR 0004: Use GitHub First With A Repository-Local Shiki Mirror\n+# SADR-0004: Use GitHub First With A Repository-Local Shiki Mirror\n \n ## Status\n \ndiff --git a/docs/adr/0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md b/docs/adr/SADR-0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\nindex b339baf49b89d9290e79c3d51b36e7b2bde3bd95..0f3c3eb9afccdb3403ce978e21d04fe12e495da0\n--- a/docs/adr/0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\n+++ b/docs/adr/SADR-0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\n@@ -1,8 +1,8 @@\n-# ADR 0005: Use Codex As Front And Claude Code Action As The GitHub Runtime\n+# SADR-0005: Use Codex As Front And Claude Code Action As The GitHub Runtime\n \n ## Status\n \n-Accepted (implementer default superseded by ADR 0008; the auth model and the\n+Accepted (implementer default superseded by SADR-0008; the auth model and the\n Claude Code Action GitHub runtime decision remain in force)\n \n ## Context\n@@ -26,4 +26,4 @@\n - Shiki avoids requiring `OPENAI_API_KEY` for the default workflow.\n - Codex work happens through the user's authenticated Codex front surface, not as a hidden backend Action.\n - Claude Code Action is configured for OAuth-token based subscription usage by default.\n-- API-key based automation remains possible only as an explicit target-repo extension with its own ADR.\n+- API-key based automation remains possible only as an explicit target-repo extension with its own target ADR.\ndiff --git a/docs/adr/0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md b/docs/adr/SADR-0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\nindex 4bcae9d1e52f0a37b8e9fbafa5cd14b6ee1993bd..7a38538a6ae9929567f0ade67e7ea6a60d0b3f7e\n--- a/docs/adr/0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\n+++ b/docs/adr/SADR-0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\n@@ -1,4 +1,4 @@\n-# ADR 0006: Separate Codex Implementation, CCA Completion, And MergeGate Authorization\n+# SADR-0006: Separate Codex Implementation, CCA Completion, And MergeGate Authorization\n \n ## Status\n \ndiff --git a/docs/adr/0007-packaging-and-release.md b/docs/adr/SADR-0007-packaging-and-release.md\nindex 79b84ddb97167372b44641e5b7860e400ce80822..bb445a2e2e856d9d1f5fdbe35de8d4e8cec0fb32\n--- a/docs/adr/0007-packaging-and-release.md\n+++ b/docs/adr/SADR-0007-packaging-and-release.md\n@@ -1,4 +1,4 @@\n-# ADR 0007: Packaging And Release For Shiki\n+# SADR-0007: Packaging And Release For Shiki\n \n ## Status\n \n@@ -25,7 +25,7 @@\n   not introduce third-party runtime dependencies or break the shim.\n - Releases must run inside the operator's subscription-authenticated toolchain\n   and must not require extra secrets beyond the built-in `GITHUB_TOKEN`\n-  (consistent with ADR 0005's no-API-key-by-default stance).\n+  (consistent with SADR-0005's no-API-key-by-default stance).\n - `.shiki/manifest.json` already encodes the install surface (`install.include`,\n   `install.create_directories`, `install.exclude_from_commit`), and it must not\n   be mutated as part of this change.\n@@ -34,7 +34,7 @@\n \n This is a hard-to-reverse platform decision (it sets the version source of\n truth, the release trigger, and the public install contract), so it is recorded\n-as an ADR.\n+as a SADR.\n \n ## Decision\n \n@@ -104,7 +104,7 @@\n 3. **Publishing to PyPI.** Rejected for now: Shiki is distributed as a\n    repository template + git-installable CLI, not a PyPI library; PyPI would add\n    credentials/secrets and a different versioning contract. Can be added later as\n-   its own ADR if a packaged distribution is needed.\n+   its own SADR if a packaged distribution is needed.\n 4. **Dynamic version (read `VERSION` via a build backend `dynamic` field).**\n    Rejected to keep `pyproject.toml` backend-agnostic and dependency-free; the\n    small duplication between `VERSION` and `pyproject.toml` is guarded by the\n@@ -123,4 +123,4 @@\n   releasing versions the whole platform but ships only `install.include` to\n   targets.\n - Future PyPI distribution or a console entry point are deferred and would each\n-  warrant their own ADR.\n+  warrant their own SADR.\ndiff --git a/docs/adr/0008-use-claude-code-as-default-implementer.md b/docs/adr/SADR-0008-use-claude-code-as-default-implementer.md\nindex dedf4510b7c7799521422f622029563d373db031..35c2625d908167fbc58855b5b63e283805f167e8\n--- a/docs/adr/0008-use-claude-code-as-default-implementer.md\n+++ b/docs/adr/SADR-0008-use-claude-code-as-default-implementer.md\n@@ -1,12 +1,12 @@\n-# ADR 0008: Use Claude Code As The Default Implementer Runtime\n+# SADR-0008: Use Claude Code As The Default Implementer Runtime\n \n ## Status\n \n-Proposed (supersedes ADR 0005 for the implementer default; keeps its auth model)\n+Proposed (supersedes SADR-0005 for the implementer default; keeps its auth model)\n \n ## Context\n \n-ADR 0005 made Codex Front the default implementation runtime, with Claude Code\n+SADR-0005 made Codex Front the default implementation runtime, with Claude Code\n restricted to planner and reviewer roles by the runtime registry, and the only\n autonomous implementation adapter was `shiki runner codex`.\n \n@@ -18,13 +18,13 @@\n - The platform's planning, review, and CCA layers already run on Claude Code,\n   and Claude Code's native capabilities (the built-in `/code-review` skill and\n   the Workflow multi-agent orchestration tool) have no Codex equivalent.\n-- The Goal lifecycle redesign (ADR 0009) requires an autonomous post-freeze\n+- The Goal lifecycle redesign (SADR-0009) requires an autonomous post-freeze\n   loop driven from the operator's Claude Code session, including bounded\n   repair dispatch, which must not depend on a runtime that cannot be invoked\n   unattended.\n \n The constitution claims Shiki is runtime-agnostic (\"not a Claude-only\n-workflow\", ADR 0002). Any change to the implementer default must preserve the\n+workflow\", SADR-0002). Any change to the implementer default must preserve the\n registry-based runtime model rather than hardcoding one vendor.\n \n ## Decision\n@@ -39,7 +39,7 @@\n   the task to review or repair-needed.\n - Keep Codex registered as an optional implementer runtime. `shiki runner\n   codex`, the Codex handoff templates, and the ChatGPT OAuth auth model from\n-  ADR 0005 remain supported for tasks explicitly assigned to `codex`.\n+  SADR-0005 remain supported for tasks explicitly assigned to `codex`.\n - Commit to mandating two Claude-native capabilities as loop contract in\n   follow-up Goals (status: the code-review gate was delivered by Goal G-B \u2014\n   skill registry, checklists item PR-12, default required_skills \u2014 while the\n@@ -60,7 +60,7 @@\n \n Out of scope: removing Codex support, changing the CCA runtime (Claude Code\n Action with `CLAUDE_CODE_OAUTH_TOKEN` stays), and API-key based automation\n-(still requires its own ADR per ADR 0005).\n+(still requires its own target ADR per SADR-0005).\n \n ## Consequences\n \n@@ -100,7 +100,7 @@\n   harness rejection of direct `codex exec` is outside Shiki's control, and the\n   plan's mandatory native capabilities cannot run on Codex at all.\n - **Remove Codex entirely**: rejected; it abandons the runtime-agnostic\n-  constitution (ADR 0002) for no operational gain and forecloses\n+  constitution (SADR-0002) for no operational gain and forecloses\n   cross-runtime adversarial review.\n - **GitHub Actions cloud implementation via claude-code-action**: rejected as\n   default; durable evidence is automatic but token cost, Node 24 workflow\ndiff --git a/docs/adr/0009-requirements-definition-and-spec-freeze.md b/docs/adr/SADR-0009-requirements-definition-and-spec-freeze.md\nindex 29b3f7871bef9203a8b4a831a65c39eb2f506e42..52e7dd69ad0aa4efe0b68c62678abbf7df02aaec\n--- a/docs/adr/0009-requirements-definition-and-spec-freeze.md\n+++ b/docs/adr/SADR-0009-requirements-definition-and-spec-freeze.md\n@@ -1,4 +1,4 @@\n-# ADR 0009: Merge Grill And Planning Into Requirements Definition With Spec Freeze\n+# SADR-0009: Merge Grill And Planning Into Requirements Definition With Spec Freeze\n \n ## Status\n \ndiff --git a/docs/adr/0010-external-ai-guardian-review.md b/docs/adr/SADR-0010-external-ai-guardian-review.md\nindex e6108bbe8bb4e73e0ff4684c8f78001a80438c4e..f3510b145664298adafb151b6f0db51b972f01ec\n--- a/docs/adr/0010-external-ai-guardian-review.md\n+++ b/docs/adr/SADR-0010-external-ai-guardian-review.md\n@@ -1,4 +1,4 @@\n-# ADR 0010: External AI Guardian Review As A First-Class Approval Authority\n+# SADR-0010: External AI Guardian Review As A First-Class Approval Authority\n \n ## Status\n \n@@ -8,7 +8,7 @@\n \n Shiki is intended to be an autonomous, recursive, self-improving agent\n framework: authority-in-the-loop, not human-in-the-loop. Until now the Guardian\n-approval required for high/critical-risk merges (guardian-policy.json, ADR\n+approval required for high/critical-risk merges (guardian-policy.json, SADR\n governance) recognized only human authority sources \u2014 a GitHub review approval,\n the `guardian:approved` label applied by a configured Guardian, or a comment\n carrying `Guardian approval granted` plus the current head SHA.\n@@ -89,7 +89,7 @@\n   autonomous design the operator intends and produced the impersonation\n   incident.\n - **Record AI approval as an operator comment**: rejected as forgery of human\n-  authority \u2014 the exact failure this ADR corrects.\n+  authority \u2014 the exact failure this SADR corrects.\n - **Deliver the artifact as a committed `.shiki/` file**: rejected; a committed\n   approval inside the PR being approved is forgeable by anyone who can push.\n - **A dedicated reviewer bot identity instead of a relayed comment**: deferred;\ndiff --git a/docs/adr/0011-autonomous-quality-gate-evidence.md b/docs/adr/SADR-0011-autonomous-quality-gate-evidence.md\nindex bca0246c5d46e44afbde15486a75eec425fc02f5..9c93eac16d8c3a6d4b11bd7224e3955102cd7db4\n--- a/docs/adr/0011-autonomous-quality-gate-evidence.md\n+++ b/docs/adr/SADR-0011-autonomous-quality-gate-evidence.md\n@@ -1,4 +1,4 @@\n-# ADR 0011: Autonomous Quality-Gate Evidence \u2014 Deterministic Facts And An Independent Verifier\n+# SADR-0011: Autonomous Quality-Gate Evidence \u2014 Deterministic Facts And An Independent Verifier\n \n ## Status\n \ndiff --git a/docs/adr/0012-autonomous-loop-closeout-to-main.md b/docs/adr/SADR-0012-autonomous-loop-closeout-to-main.md\nindex 637fad6dde4989c9c2bf0f81f676d8d29991123c..ec7d85f81030a676bcbba28b43eb365422a36eac\n--- a/docs/adr/0012-autonomous-loop-closeout-to-main.md\n+++ b/docs/adr/SADR-0012-autonomous-loop-closeout-to-main.md\n@@ -1,4 +1,4 @@\n-# 0012 \u2014 Autonomous loop pushes goal completion to main via a closeout PR\n+# SADR-0012 \u2014 Autonomous loop pushes goal completion to main via a closeout PR\n \n - Status: accepted\n - Date: 2026-06-18\ndiff --git a/docs/adr/0013-bootstrap-grants-actions-approve-pull-requests.md b/docs/adr/SADR-0013-bootstrap-grants-actions-approve-pull-requests.md\nindex b6c69d64f1c5ced346b88d6859586715b3b5bd0e..cf48ff4a6716267f539ca58edd1a852e7c7a2d64\n--- a/docs/adr/0013-bootstrap-grants-actions-approve-pull-requests.md\n+++ b/docs/adr/SADR-0013-bootstrap-grants-actions-approve-pull-requests.md\n@@ -1,4 +1,4 @@\n-# ADR 0013: Bootstrap Grants GitHub Actions Permission To Create And Approve Pull Requests\n+# SADR-0013: Bootstrap Grants GitHub Actions Permission To Create And Approve Pull Requests\n \n ## Status\n \n@@ -31,7 +31,7 @@\n security tradeoff: it lets repository automation satisfy a human-review gate. In\n a multi-maintainer repo that could let a compromised or buggy workflow\n self-approve changes. So whether bootstrap turns it on by default \u2014 and with\n-what default token scope \u2014 is an ADR-worthy decision, not an implementation\n+what default token scope \u2014 is a SADR-worthy decision, not an implementation\n detail.\n \n ## Decision\n@@ -48,7 +48,7 @@\n Specifically:\n \n - **`can_approve_pull_request_reviews` defaults to `true`.** Shiki's default\n-  posture is solo/AFK autonomous operation (ADR 0008/0009/0012), where the CCA\n+  posture is solo/AFK autonomous operation (SADR-0008/0009/0012), where the CCA\n   Review Bridge is the only path that can satisfy `required_review: true` after\n   the CCA and Guardian gates have already passed. The default makes a\n   freshly-bootstrapped repo able to run the autonomous loop end to end without a\ndiff --git a/docs/adr/0014-codex-app-external-ai-guardian-ui-adapter.md b/docs/adr/SADR-0014-codex-app-external-ai-guardian-ui-adapter.md\nindex 238db06279334caa341c117362df9271382a7fcd..65f12e9939321e1b3e389a34c822fc1549e5155d\n--- a/docs/adr/0014-codex-app-external-ai-guardian-ui-adapter.md\n+++ b/docs/adr/SADR-0014-codex-app-external-ai-guardian-ui-adapter.md\n@@ -1,4 +1,4 @@\n-# ADR 0014: Use Codex App As The External AI Guardian UI Adapter\n+# SADR-0014: Use Codex App As The External AI Guardian UI Adapter\n \n ## Status\n \ndiff --git a/docs/adr/0015-contract-approval-before-dispatch.md b/docs/adr/SADR-0015-contract-approval-before-dispatch.md\nindex fad6c656741a20a7684ba16080a7db159f63a52f..6061271a46d3f1d6ae7174477de3fb75673123a6\n--- a/docs/adr/0015-contract-approval-before-dispatch.md\n+++ b/docs/adr/SADR-0015-contract-approval-before-dispatch.md\n@@ -1,4 +1,4 @@\n-# ADR 0015: Approve The Task Contract Before Dispatch, Not The Diff After It\n+# SADR-0015: Approve The Task Contract Before Dispatch, Not The Diff After It\n \n ## Status\n \n@@ -9,7 +9,7 @@\n Guardian approval for high/critical risk is expressible only on a pull request.\n Every approval source in `guardian-policy.json` \u2014 GitHub review, the\n `guardian:approved` label, the `Guardian approval granted` comment, and the\n-`external_ai_guardian_review` artifact (ADR 0010/0014) \u2014 is bound to a PR, and\n+`external_ai_guardian_review` artifact (SADR-0010/0014) \u2014 is bound to a PR, and\n three of the four require the current head SHA. Approval therefore cannot exist\n before an implementation PR exists.\n \n@@ -81,7 +81,7 @@\n An envelope breach must surface only on the non-repairable policy gate, never on\n the repairable `MergeGate metadata check`. Otherwise the Goal Loop routes it to\n `dispatch_repair` \u2014 instructing a runner to make the breach go away \u2014 which is\n-exactly what this ADR forbids.\n+exactly what this SADR forbids.\n \n **Contract immutability.** After registration, an implementation PR must not\n change the governance fields of its own task contract (`scope`, `non_goals`,\n@@ -116,7 +116,7 @@\n fails any verdict that is not `complete`, and the signal the CCA prompt reads is\n computed from `evaluate_guardian_approval` alone. Without the OR-merge at the\n signal, a contract-approved PR still returns `needs_guardian`, the CCA job fails,\n-and the MergeGate policy check never runs \u2014 the gate this ADR changes would never\n+and the MergeGate policy check never runs \u2014 the gate this SADR changes would never\n be reached. The CCA *prompt* needs no change: a `contract_approval` entry in the\n signal's existing `sources` array satisfies its current wording.\n \n@@ -130,7 +130,7 @@\n written, and an approved contract lets its implementation PRs merge through the\n same automatic path as low/medium risk. The wasted implement-then-block cycle\n disappears. Guardian review shifts from reading diffs to reading contracts, which\n-is both earlier and smaller; the external AI Guardian path (ADR 0010/0014) works\n+is both earlier and smaller; the external AI Guardian path (SADR-0010/0014) works\n on contracts with no change.\n \n We accept that the Guardian authorizes a class of change without seeing its\n@@ -146,7 +146,7 @@\n adversarial tests for the new approval path equivalent in rigor to\n `test_shiki_governance_evidence.sh`.\n \n-Bootstrap costs, which apply only while this ADR is being implemented. The tasks\n+Bootstrap costs, which apply only while this SADR is being implemented. The tasks\n that build Contract Approval merge under the existing rules, so the ones\n dispatched after the pre-dispatch gate lands but before the approval path lands\n need a Guardian-approved Contract PR *and* a live Guardian approval on their\n@@ -210,7 +210,7 @@\n risk; they do not bound authorized work.\n \n **Auto-escalate risk on envelope breach instead of stopping.** Returns the\n-workflow to a diff-time Guardian decision \u2014 the exact behavior this ADR removes.\n+workflow to a diff-time Guardian decision \u2014 the exact behavior this SADR removes.\n \n **Resolve governance fields from the base snapshot only.** Rejected against the\n registration flow: implementation PRs add their own contract, so the base has no\ndiff --git a/docs/adr/0016-locks-bound-product-paths.md b/docs/adr/SADR-0016-locks-bound-product-paths.md\nindex b5e850f3f1f7f4ef6e7f9d1763a29a51bd300c45..46b40a9b99a7aae644c02bb458f5473ff0a90ce2\n--- a/docs/adr/0016-locks-bound-product-paths.md\n+++ b/docs/adr/SADR-0016-locks-bound-product-paths.md\n@@ -1,4 +1,4 @@\n-# ADR 0016: Locks bound product paths; the Shiki mirror is bound by record identity\n+# SADR-0016: Locks bound product paths; the Shiki mirror is bound by record identity\n \n ## Status\n \n@@ -6,8 +6,8 @@\n \n - Date: 2026-07-28\n - Deciders: operator (mizutani-140), Claude (planner)\n-- Related: ADR 0004 (GitHub-first with a `.shiki` mirror), ADR 0009 (Spec Freeze),\n-  ADR 0012 (autonomous loop closeout to main), ADR 0015 (Contract Approval before\n+- Related: SADR-0004 (GitHub-first with a `.shiki` mirror), SADR-0009 (Spec Freeze),\n+  SADR-0012 (autonomous loop closeout to main), SADR-0015 (Contract Approval before\n   dispatch), remediation goal task R-02, the live PR #179 deadlock.\n \n ## Context\n@@ -90,11 +90,11 @@\n files, documentation files and named source modules are nameable; runtime-\n allocated ledger, runner and report ids are not \u2014 and those live in `.shiki`,\n which locks no longer govern. Existing frozen contracts are brought into line by\n-a one-time Spec Amendment per plan, which must land before ADR 0015's contract\n+a one-time Spec Amendment per plan, which must land before SADR-0015's contract\n immutability makes `locks` unwritable from the implementation PR.\n \n **Lock state is resolved from the durable side.** MergeGate resolves the lock\n-registry, and ADR 0015's governance fields, from the base `.shiki` snapshot when\n+registry, and SADR-0015's governance fields, from the base `.shiki` snapshot when\n present, falling back to head only when absent. The `MergeGate metadata check`\n job is given `--base-shiki`, which it does not receive today. Locks are not\n derived from the live open-pull-request set: a pull request body is untrusted\n@@ -108,7 +108,7 @@\n Explicitly out of scope: moving MergeGate's contract resolution off the PR\n checkout wholesale; making the mirror a projection of GitHub state; introducing\n new lock kinds; moving ledger evidence out of the repository; and unwinding\n-ADR 0012's closeout pull request. Registration of multi-task goals is ADR 0015's\n+SADR-0012's closeout pull request. Registration of multi-task goals is SADR-0015's\n Contract PR and is not re-decided here.\n \n ## Consequences\n@@ -127,7 +127,7 @@\n \n Risks accepted. First, goal-scoped identity is only as strong as `goal_id`,\n which is resolved from the PR body and the head task file; the mitigation is to\n-add `goal_id` to ADR 0015's frozen governance field set, and until that lands a\n+add `goal_id` to SADR-0015's frozen governance field set, and until that lands a\n task file can name a foreign goal. Second, base-when-present resolution is\n unavailable on fork pull requests, where the head is contributor-controlled;\n fork pull requests already cannot complete a Shiki merge, and this decision does\n@@ -139,7 +139,7 @@\n path, not by implementation pull requests.\n \n New obligations: one Spec Amendment per frozen plan to rewrite `locks`, ordered\n-before ADR 0015 T1; extension of the adversarial suites in\n+before SADR-0015 T1; extension of the adversarial suites in\n `scripts/test_shiki_adversarial_state.sh` and\n `scripts/test_shiki_governance_evidence.sh` to cover the eleven newly-scoped\n directories; and updating `scripts/test_shiki_loop_lock_guard.sh`,\n@@ -151,32 +151,32 @@\n whether the loop's transitive sync of ledger `evidence` references can be\n deleted once the Contract PR carries its own source plan.\n \n-## Relationship to ADR 0004\n+## Relationship to SADR-0004\n \n-This ADR **clarifies something ADR 0004 left unspecified**. ADR 0004 decides that\n+This SADR **clarifies something SADR-0004 left unspecified**. SADR-0004 decides that\n GitHub is the operational source of truth and that each target repository keeps a\n `.shiki/` mirror. It says nothing about pull requests writing the mirror, does\n not name MergeGate, and does not make the mirror an input to a gate. Nothing in\n-ADR 0004's Decision or Consequences is amended, superseded or weakened: the\n+SADR-0004's Decision or Consequences is amended, superseded or weakened: the\n mirror still exists, still records the same artifacts, and pull request\n-boundaries remain the coordination surface. What this ADR settles is the question\n+boundaries remain the coordination surface. What this SADR settles is the question\n 0004 never reached \u2014 by what authority a pull request may write a mirror record \u2014\n and it answers it the way `.shiki/manifest.json` already declares\n (`pr_mutation: current-task-or-current-goal-only`).\n \n-It **amends ADR 0012**, whose Decision item 5 states that \"Loop-executed tasks\n+It **amends SADR-0012**, whose Decision item 5 states that \"Loop-executed tasks\n declare the `path:.shiki/**` lock, which covers every `.shiki` file the closeout\n stages ... so `files_outside_locks` is satisfied without per-file lock\n bookkeeping.\" That clause is replaced: the closeout's mirror writes are\n-authorized by record identity, not by a declared lock. ADR 0012's substantive\n+authorized by record identity, not by a declared lock. SADR-0012's substantive\n decision \u2014 that completion reaches `main` through a closeout pull request rather\n than local attestation \u2014 is preserved unchanged.\n \n-It is **complementary to ADR 0015**, not a substitute. ADR 0015 owns contract\n+It is **complementary to SADR-0015**, not a substitute. SADR-0015 owns contract\n immutability, the pre-dispatch gate, the Contract PR and the Scope Envelope. This\n-ADR owns what a lock means and who may write the mirror. The one ordering\n+SADR owns what a lock means and who may write the mirror. The one ordering\n constraint between them is that the lock-narrowing Spec Amendment must precede\n-ADR 0015 T1's freezing of the `locks` field.\n+SADR-0015 T1's freezing of the `locks` field.\n \n ## Alternatives Considered\n \n@@ -186,7 +186,7 @@\n the only writer that can register them may run. Migrating the Guardian signal to\n that store makes every registration PR fail closed to `needs_guardian` at every\n risk level, because the task file is by definition absent from the store, which\n-removes the low/medium autonomous path ADR 0008/0009 exists to provide. And it\n+removes the low/medium autonomous path SADR-0008/0009 exists to provide. And it\n leaves schemas, manifest, config, guardian policy, templates, migrations, plans,\n worktrees, runner records, runs and handoffs with no writer at all in a\n repository whose product is `.shiki`. Measured benefit on the frozen backlog:\n@@ -199,14 +199,14 @@\n namespace is not in fact ownership-partitioned \u2014 56 ledger entries carry no\n `task_id`, and one goal DAG is writable by 35 registered tasks. The correct\n expression of its insight is that locks do not govern the mirror at all, which is\n-what this ADR decides.\n+what this SADR decides.\n \n **Make the mirror a projection written only by a registration writer and a\n post-merge projection job.** Rejected. It requires a bypass actor with write\n access to the protected default branch; it makes its own master invariant\n readable from a manifest that pull requests may edit; it deletes both reconcile\n modes and leaves a corrupted mirror with no in-band repair path, contradicting\n-ADR 0004's consequence that mirror conflicts are repaired against GitHub state;\n+SADR-0004's consequence that mirror conflicts are repaired against GitHub state;\n and its content-derived ids are rejected by `validate_shiki`'s currently-green id\n pattern.\n \ndiff --git a/docs/adr/0017-bookkeeping-closeouts-do-not-inherit-risk.md b/docs/adr/SADR-0017-bookkeeping-closeouts-do-not-inherit-risk.md\nindex 554e3a0d526a72ca067108c5464cc68c3edcf174..b9676857a4713850e063af7c39c8e0b01ecb4c66\n--- a/docs/adr/0017-bookkeeping-closeouts-do-not-inherit-risk.md\n+++ b/docs/adr/SADR-0017-bookkeeping-closeouts-do-not-inherit-risk.md\n@@ -1,4 +1,4 @@\n-# ADR 0017: A Bookkeeping Closeout Does Not Inherit Its Task's Risk\n+# SADR-0017: A Bookkeeping Closeout Does Not Inherit Its Task's Risk\n \n ## Status\n \n@@ -6,7 +6,7 @@\n \n ## Context\n \n-ADR 0012 made the goal loop push a task's completion to the default branch through a\n+SADR-0012 made the goal loop push a task's completion to the default branch through a\n closeout pull request: after the implementation PR merges, the loop opens a second PR\n carrying `task.status = done`, `lock.state = released`, and \u2014 when the task completes\n its goal \u2014 `goal.status = complete` plus one scorecard report. Nothing else. The\n@@ -49,14 +49,14 @@\n 4. The task file's governance fields (`scope`, `non_goals`, `required_skills`,\n    `risk_level`, `locks`, `acceptance_checks`, `test_command`) are byte-identical to\n    the base snapshot. Only `status`, `expected_pr`, `closeout_pr`, `ledger_evidence`\n-   and `expected_branch` may differ \u2014 the ADR 0015 contract-immutability rule,\n+   and `expected_branch` may differ \u2014 the SADR-0015 contract-immutability rule,\n    unchanged.\n \n    `expected_branch` is in that set because a closeout is *by construction* on a\n    different branch from the implementation it closes, and the loop rewrites the\n    field accordingly. It is safe to let it differ because MergeGate independently\n    requires `expected_branch` to equal the PR's actual head ref, so a PR cannot set\n-   it to anything other than the branch it is really on. The first draft of this ADR\n+   it to anything other than the branch it is really on. The first draft of this SADR\n    omitted it, which made the exemption unreachable: every real closeout failed\n    condition 4 on a field the loop always changes. See PR #209.\n 5. The state transitions are exactly the terminal set: task `review` \u2192 `done`, lock\n@@ -113,12 +113,12 @@\n classification must be derived, not asserted.\n \n **Fold the closeout into the implementation PR** so no second PR exists. Rejected under\n-ADR 0012, which recorded why: a task file that is already `done` on the implementation\n+SADR-0012, which recorded why: a task file that is already `done` on the implementation\n branch short-circuits the loop's review \u2192 CCA \u2192 merge \u2192 done ordering into a local\n goal-completion before any merge happens.\n \n **Have the loop auto-merge closeouts with elevated credentials.** Rejected outright: that\n-is a runtime authoring its own approval, which ADR 0010 and the constitution's\n+is a runtime authoring its own approval, which SADR-0010 and the constitution's\n approval-record prohibition forbid. It would also be indistinguishable, in the evidence,\n from the forgery those rules exist to prevent.\n \ndiff --git a/docs/adr/0018-carried-guardian-approval-across-a-proven-base-sync.md b/docs/adr/SADR-0018-carried-guardian-approval-across-a-proven-base-sync.md\nindex 22358562da390ed8b45cd600da08a43d378b4680..e6e2907cefa776460af73ddf7d39db605f3c39b0\n--- a/docs/adr/0018-carried-guardian-approval-across-a-proven-base-sync.md\n+++ b/docs/adr/SADR-0018-carried-guardian-approval-across-a-proven-base-sync.md\n@@ -1,4 +1,4 @@\n-# ADR 0018: Carried Guardian Approval Across A Proven Base Sync\n+# SADR-0018: Carried Guardian Approval Across A Proven Base Sync\n \n ## Status\n \n@@ -34,7 +34,7 @@\n Guardian evaluator that consumes it (`shiki_guardian.evaluate_guardian_approval`,\n the `guardian_comment_carried` source) were built and tested in isolation. Neither\n does anything until a caller passes the flags that turn the carry on, so the\n-feature was invisible in CI. This ADR records the decision the two of them\n+feature was invisible in CI. This SADR records the decision the two of them\n implement, and this task wires it into the four CI invocations that judge Guardian\n approval.\n \ndiff --git a/docs/adr/README.md b/docs/adr/README.md\nindex c70e057a4520c36e042ab68c53cd4a21ef709b71..5da43856836e2ce6a150f8127766e2f88355ab4b\n--- a/docs/adr/README.md\n+++ b/docs/adr/README.md\n@@ -1,40 +1,41 @@\n-# Architecture Decision Records\n+# Decision Record Namespaces\n \n-This directory holds Shiki's Architecture Decision Records (ADRs): durable\n-records of hard-to-reverse platform decisions. ADRs are part of the Shiki source\n-of truth alongside GitHub state, the `.shiki/` mirror, and `CONTEXT.md`.\n+This directory holds two independent decision namespaces:\n \n-## How ADRs Work\n+- Shiki Architecture Decision Records (SADRs), named\n+  `SADR-NNNN-<short-slug>.md`, record hard-to-reverse Shiki platform decisions.\n+- Target Architecture Decision Records (ADRs), named\n+  `NNNN-<short-slug>.md`, belong to the target product.\n \n-- New ADRs are added as `docs/adr/NNNN-<short-slug>.md`, where `NNNN` is the next\n-  zero-padded sequence number.\n-- The **canonical list of ADRs is the set of files matching `docs/adr/*.md`** in\n-  this directory. Browse `docs/adr/*.md` for the authoritative, current set.\n-- Use [`template.md`](template.md) as the starting point for a new ADR.\n-- Record an ADR when a decision is hard to reverse, surprising without context,\n-  and a real tradeoff. Do not write an ADR for routine or easily reversible\n-  choices.\n+The same four-digit number may appear once in each namespace. A platform\n+`SADR-NNNN` citation resolves only to `SADR-NNNN-*.md`; a target `ADR NNNN`\n+citation resolves only to `NNNN-*.md`.\n \n-Using the `docs/adr/*.md` glob as the source of truth means other PRs can add an\n-ADR without editing this index file, avoiding merge contention on a\n-hand-maintained enumeration.\n+## How Decision Records Work\n \n-## Current ADRs (snapshot)\n-\n-The list below is a convenience snapshot and may lag behind the directory. When\n-in doubt, list `docs/adr/*.md`.\n+- New Shiki platform decisions use the next zero-padded SADR number.\n+- New target product decisions use the target's own ADR sequence.\n+- The canonical list is the files in `docs/adr/`; no hand-maintained index is\n+  authoritative.\n+- Use [`template.md`](template.md) as the starting point and choose the filename\n+  namespace that owns the decision.\n+- Record a decision when it is hard to reverse, surprising without context, and\n+  a real tradeoff. Do not write a decision record for routine or easily\n+  reversible choices.\n \n-- [0001 \u2014 Keep Shiki As The Platform Name](0001-keep-shiki-as-platform-name.md)\n-- [0002 \u2014 Use AGENTS As The Shared Agent Constitution](0002-use-agents-as-shared-constitution.md)\n-- [0003 \u2014 Require Skill Gate For MergeGate Readiness](0003-require-skill-gate-for-mergegate-readiness.md)\n-- [0004 \u2014 Use GitHub First With A Repository-Local Shiki Mirror](0004-use-github-first-with-shiki-mirror.md)\n-- [0005 \u2014 Use Codex As Front And Claude Code Action As The GitHub Runtime](0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md)\n-- [0006 \u2014 Separate Codex Implementation, CCA Completion, And MergeGate Authorization](0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md)\n+Using the directory as the source of truth lets independent changes add records\n+without contending on an enumerated index.\n \n-## Adding A New ADR\n+## Adding A Shiki SADR\n \n-1. Copy `template.md` to `docs/adr/NNNN-<short-slug>.md`.\n+1. Copy `template.md` to `docs/adr/SADR-NNNN-<short-slug>.md`.\n 2. Fill in the title, status, context, decision, and consequences.\n-3. Set the status to `Proposed`, then `Accepted` once the decision is approved.\n-4. Open a PR following `CONTRIBUTING.md`. You do not need to edit this index for\n-   the ADR to be discoverable via `docs/adr/*.md`.\n+3. Set the status to `Proposed`, then `Accepted` once the proper authority has\n+   approved the decision.\n+4. Open a PR following `CONTRIBUTING.md`.\n+\n+## Adding A Target ADR\n+\n+1. Copy `template.md` to `docs/adr/NNNN-<short-slug>.md` using the target's own\n+   sequence.\n+2. Cite it as `ADR NNNN`; do not prefix its filename with `SADR-`.\ndiff --git a/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md b/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\nnew file mode 100644\nindex 0000000000000000000000000000000000000000..7455ce1f3bed02ddddb851ac21a1d1a648c9aeba\n--- /dev/null\n+++ b/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\n@@ -0,0 +1,55 @@\n+# SADR-0019: Separate Shiki Platform SADRs From Target ADRs\n+\n+## Status\n+\n+Accepted\n+\n+## Context\n+\n+Shiki installs its platform decision records into `docs/adr/` in every target\n+repository. Shiki and its targets previously used the same numeric\n+`NNNN-<slug>.md` namespace. A target that already owned ADRs 0013 through 0018\n+therefore acquired two records for each number during installation, making an\n+`ADR NNNN` citation ambiguous. Renumbering target decisions would rewrite target\n+history, while renumbering Shiki decisions per target would destroy their stable\n+platform identity.\n+\n+Upgrades also need to remove the former Shiki-owned numeric paths without ever\n+mistaking a target-owned ADR for platform content. The install stamp already\n+records exact shipped paths and content digests, so it can prove ownership only\n+when both still match.\n+\n+## Decision\n+\n+Shiki platform decisions use the `SADR` namespace:\n+\n+- records are named `docs/adr/SADR-NNNN-<slug>.md`;\n+- live platform citations use `SADR-NNNN`;\n+- target product decisions retain `docs/adr/NNNN-<slug>.md` and `ADR NNNN`;\n+- the reference validator resolves the two namespaces independently, so the same\n+  four-digit number may exist once in each namespace.\n+\n+Before a forced upgrade writes anything, the installer inventories the exact\n+former numeric Shiki ADR paths. It may delete a legacy path only when the\n+existing install stamp contains that exact path and its recorded SHA-256 digest\n+matches the file currently on disk. All ownership blockers are reported\n+together and abort the upgrade before its first write. A non-forced install\n+never performs this cleanup.\n+\n+Historical `.shiki/` evidence remains unchanged because it records the namespace\n+that existed when that evidence was produced.\n+\n+## Consequences\n+\n+- Targets may use any ADR number without colliding with Shiki's platform SADRs.\n+- Fresh installs preserve target ADR bytes and install a stable SADR set.\n+- Forced upgrades fail closed when legacy ownership cannot be proven, and a\n+  successful upgrade refreshes the stamp without removed legacy paths.\n+- Current governance, packaging, workflow, prompt, script, and skill references\n+  must move with the platform records.\n+\n+## Alternatives Considered\n+\n+- Collision-only refusal leaves affected targets permanently unupgradable.\n+- Renumbering target ADRs mutates target-owned history.\n+- Per-target Shiki renumbering makes platform citations non-portable.\ndiff --git a/docs/adr/template.md b/docs/adr/template.md\nindex a92a97daddd0b00ab9dd626cc8d86942da2eafed..17dd79175b5fee2105935557f691b40cf9492942\n--- a/docs/adr/template.md\n+++ b/docs/adr/template.md\n@@ -1,14 +1,14 @@\n-# NNNN: <Short Decision Title>\n+# <SADR-NNNN or NNNN>: <Short Decision Title>\n \n ## Status\n \n-Proposed | Accepted | Superseded by ADR-NNNN\n+Proposed | Accepted | Superseded by SADR-NNNN or target ADR NNNN\n \n ## Context\n \n What is the situation, constraint, or force that requires a decision? Reference\n-relevant domain terms (`CONTEXT.md`), prior ADRs, and code reality. State the\n-problem, not the solution.\n+relevant domain terms (`CONTEXT.md`), prior Shiki SADRs or target ADRs, and code\n+reality. State the problem, not the solution.\n \n ## Decision\n \ndiff --git a/docs/agents/autonomous-loop-e2e-acceptance.md b/docs/agents/autonomous-loop-e2e-acceptance.md\nindex bbf84c6081987f159ac6ad9ecf3c82b7aa72e7e8..5c13c1d02a05a90b0dc46cbef7c95a9846d0c237\n--- a/docs/agents/autonomous-loop-e2e-acceptance.md\n+++ b/docs/agents/autonomous-loop-e2e-acceptance.md\n@@ -40,8 +40,8 @@\n 3. `gh auth status` is logged in with merge permission on the repo.\n 4. Branch protection + required checks are configured exactly as the goal-loop\n    required-check set expects (see `docs/agents/branch-protection-smoke.md`).\n-5. The repo `.shiki/policy` permits autonomous low/medium-risk merges (ADR\n-   0008/0009); the MergeGate policy check is the recorded-authority gate.\n+5. The repo `.shiki/policy` permits autonomous low/medium-risk merges\n+   (SADR-0008/0009); the MergeGate policy check is the recorded-authority gate.\n \n The operator's only actions are: register the task and start the loop. **No\n manual pre-clearing** means after `shiki loop run` starts, the operator does not\ndiff --git a/docs/agents/bootstrap-command.md b/docs/agents/bootstrap-command.md\nindex 05f7f203a52cf412b9f97084a6e131ed0f785afd..f7aa0b734dfebee4a3b139335b66ee3415f48713\n--- a/docs/agents/bootstrap-command.md\n+++ b/docs/agents/bootstrap-command.md\n@@ -189,7 +189,9 @@\n ```\n \n Use this only for tests, fixtures, or explicit local-only template inspection.\n-Use `--force` only when you intentionally want to overwrite existing target files.\n+Use `--force` only when you intentionally want to refresh the target template.\n+Legacy numeric Shiki decision cleanup is stamp-and-digest authorized before any\n+write; target ADR paths are not cleanup candidates.\n \n ## Slash Command\n \ndiff --git a/docs/agents/checklists.md b/docs/agents/checklists.md\nindex 4a7f03ade346f53a1c1d82981f3fd744f9ab4547..0c5ec6c53dc7197b7b7a7d18e667b325babd6c45\n--- a/docs/agents/checklists.md\n+++ b/docs/agents/checklists.md\n@@ -32,18 +32,18 @@\n | ID | Item | Blocking |\n | --- | --- | --- |\n | GD-01 | Plan was challenged against `CONTEXT.md`. | blocking for non-trivial Goals |\n-| GD-02 | Relevant ADRs were read or absence was recorded. | blocking for non-trivial Goals |\n+| GD-02 | Relevant SADRs and target ADRs were read or absence was recorded. | blocking for non-trivial Goals |\n | GD-03 | Ambiguous terms were resolved or converted to HITL blockers. | blocking |\n | GD-04 | Edge scenarios were considered. | advisory |\n | GD-05 | Code reality was checked when code could answer. | blocking when applicable |\n | GD-06 | New domain terms were added to `CONTEXT.md` when needed. | blocking when applicable |\n-| GD-07 | ADR was created or explicitly skipped using the ADR criteria. | blocking when applicable |\n+| GD-07 | A SADR or target ADR was created or explicitly skipped using the decision-record criteria. | blocking when applicable |\n \n ## CI \u2014 Context & Impact Checklist\n \n | ID | Item | Blocking |\n | --- | --- | --- |\n-| CI-01 | Relevant docs and ADRs identified. | blocking |\n+| CI-01 | Relevant docs, SADRs, and target ADRs identified. | blocking |\n | CI-02 | Relevant modules, interfaces, seams, callers, and tests identified. | blocking |\n | CI-03 | Dependencies and blocked-by relationships identified. | blocking |\n | CI-04 | Candidate locks identified. | blocking |\n@@ -62,7 +62,7 @@\n | PRD-04 | Implementation decisions are recorded without volatile code detail. | blocking |\n | PRD-05 | Testing decisions are recorded. | blocking |\n | PRD-06 | Out-of-scope items are explicit. | blocking |\n-| PRD-07 | Relevant glossary/ADR links are present when applicable. | blocking |\n+| PRD-07 | Relevant glossary/SADR/target-ADR links are present when applicable. | blocking |\n \n ## SF \u2014 Spec Freeze Checklist\n \n@@ -98,7 +98,7 @@\n | H-02 | Branch/worktree target is present. | blocking |\n | H-03 | Scope and non-goals are present. | blocking |\n | H-04 | Dependencies and locks are present. | blocking |\n-| H-05 | Relevant docs, ADRs, modules, and tests are referenced. | blocking |\n+| H-05 | Relevant docs, SADRs, target ADRs, modules, and tests are referenced. | blocking |\n | H-06 | Required skills are stated. | blocking |\n | H-07 | TDD expectations are stated. | blocking when behavior work |\n | H-08 | Acceptance criteria are included. | blocking |\ndiff --git a/docs/agents/codex-handoff.md b/docs/agents/codex-handoff.md\nindex ede468f47cb277f69a857e8c45df703e4ed0151d..6f97bdffb67e44d7edea2c71e01ea28933c4eb6c\n--- a/docs/agents/codex-handoff.md\n+++ b/docs/agents/codex-handoff.md\n@@ -46,7 +46,7 @@\n \n ## Context\n - Domain terms:\n-- Relevant ADRs:\n+- Relevant SADRs and target ADRs:\n - Relevant docs:\n - Relevant modules/interfaces/seams:\n - Similar tests/prior art:\ndiff --git a/docs/agents/completion-check-agent.md b/docs/agents/completion-check-agent.md\nindex 13e4c035db35025f4e3cdcc5adfb24314ac4d521..61bfa1218cf36ca605250c91919e620f33238aa0\n--- a/docs/agents/completion-check-agent.md\n+++ b/docs/agents/completion-check-agent.md\n@@ -32,7 +32,7 @@\n - `AGENTS.md`\n - `CLAUDE.md`\n - `CONTEXT.md`\n-- relevant ADRs\n+- relevant SADRs and target ADRs\n - parent Goal issue\n - PRD issue or document\n - task issue\ndiff --git a/docs/agents/decision-control.md b/docs/agents/decision-control.md\nindex 7d8ee5ab03a5ccbcbd04056ae61de03785a2a9c9..8c94ecda00fffaa0e92c325ab83aa9776bd1dac1\n--- a/docs/agents/decision-control.md\n+++ b/docs/agents/decision-control.md\n@@ -11,7 +11,7 @@\n | Decision | Owner | When | Required inputs | Output | Enforcement surface |\n | --- | --- | --- | --- | --- | --- |\n | Is the Goal clear enough to plan? | Planner + Guardian when needed | Goal intake | User request, existing docs, risk signals | Goal readiness verdict | Goal checklist, issue template, ledger |\n-| Must `grill-with-docs` run? | Planner / Skill Gate | Before PRD or issue split | Ambiguity, domain terms, ADR conflicts, unclear boundaries | Skill invocation or explicit skip rationale | Skill Gate checklist |\n+| Must `grill-with-docs` run? | Planner / Skill Gate | Before PRD or issue split | Ambiguity, domain terms, SADR/target-ADR conflicts, unclear boundaries | Skill invocation or explicit skip rationale | Skill Gate checklist |\n | Is a PRD required? | Planner | After Goal context stabilizes | Goal, `grill-with-docs` output, Context & Impact output | PRD required / not required | PRD checklist, CCA evidence check |\n | Can the spec be frozen? | Operator only | End of Requirements Definition, before issue split | PRD/requirements summary, scope inventory (SF-02), open-question list | `spec_freeze` block in the plan + ledger evidence | `require_grilled_plan` (plan ingest/run), SF checklist |\n | Can a frozen spec change? | Operator only (Spec Amendment) | When implementation contradicts the frozen spec | Contested decisions, scoped re-grill result | recorded amendment + re-stamped freeze | SF checklist, ledger evidence |\ndiff --git a/docs/agents/domain.md b/docs/agents/domain.md\nindex 258da8ea81e87ac0a6b0b5b5e5c2fde980eaabb7..d57b0cf17ff3e1fa9e6e5885372bf083d8295421\n--- a/docs/agents/domain.md\n+++ b/docs/agents/domain.md\n@@ -5,7 +5,9 @@\n ## Layout\n \n - `CONTEXT.md` defines Shiki language. Keep it implementation-free.\n-- `docs/adr/` records hard-to-reverse decisions.\n+- `docs/adr/SADR-NNNN-*.md` records Shiki platform decisions.\n+- `docs/adr/NNNN-*.md` records target product decisions without sharing the\n+  SADR namespace.\n - `AGENTS.md` is the runtime-neutral agent constitution.\n - `CLAUDE.md` is the Claude Code wrapper around `AGENTS.md`.\n - `.shiki/` is the Target Repository mirror for Goals, PRDs, plans, Task DAGs, contracts, locks, worktree records, ledger entries, CCA verdicts, repair packets, reports, and handoffs.\n@@ -15,6 +17,6 @@\n \n - Use Shiki terms exactly when they exist.\n - Add glossary terms to `CONTEXT.md` only when the term is part of the domain, not a generic programming concept.\n-- Create ADRs only for hard-to-reverse, surprising, tradeoff-based decisions.\n+- Create SADRs or target ADRs only for hard-to-reverse, surprising, tradeoff-based decisions.\n - Keep product-specific language in the Target Repository, not in Shiki Template docs.\n-- If an ADR conflicts with a proposed implementation, surface the conflict before editing.\n+- If a SADR or target ADR conflicts with a proposed implementation, surface the conflict before editing.\ndiff --git a/docs/agents/execution-control.md b/docs/agents/execution-control.md\nindex ad8ee3cdb36be56fedc1f46408ca92b387d74047..bc63c3a73661d12b567016ba299f1a6b66bf9088\n--- a/docs/agents/execution-control.md\n+++ b/docs/agents/execution-control.md\n@@ -63,7 +63,7 @@\n | Branch name | Orchestrator | Orchestrator | Before implementer dispatch | branch policy | branch ref in task/ledger |\n | Lock reservation | Context & Impact | MergeGate | Before dispatch and before merge | `.shiki/locks/`, labels, PR metadata | lock id, owner, scope |\n | CI profile | Planner | MergeGate | During issue triage, rechecked at PR | labels, `.shiki/policy.yaml`, workflow matrix | required check list |\n-| Required status checks | Repo maintainer / Guardian | GitHub branch protection / rulesets | Repository setup, updated via ADR | GitHub settings | branch/ruleset config, ADR |\n+| Required status checks | Repo maintainer / Guardian | GitHub branch protection / rulesets | Repository setup, updated via target ADR | GitHub settings | branch/ruleset config, target ADR |\n | CCA judgment | CCA | GitHub required check / MergeGate | Every non-draft PR update | GitHub Action check | structured CCA verdict |\n | Repair assignment | CCA | MergeGate | After failed CCA/CI/review | repair packet, issue/PR comment | repair id and attempt count |\n | Merge readiness | MergeGate | GitHub branch protection / Guardian if needed | After CCA complete | required checks, review rules, rulesets | mergegate check output |\ndiff --git a/docs/agents/guardian-policy.md b/docs/agents/guardian-policy.md\nindex 4147c7adf05a47adb4fd24ada1a1c2e1f2ba6d2d..a20d9cc6c00a569bb057b9ca8d6b51c7fc4a9847\n--- a/docs/agents/guardian-policy.md\n+++ b/docs/agents/guardian-policy.md\n@@ -98,8 +98,8 @@\n `origin/<default_branch>` and reuses each job's existing base fetch; a shallow\n (`--depth`) or second base fetch would make the proof unresolvable and silently\n disable the carry, so neither is permitted. CCA reads `guardian_comment_carried` as\n-a recorded authority in the deterministic signal and does not re-derive it. See ADR\n-0018 for the full decision, the rejected diff/patch-equality and path-disjointness\n+a recorded authority in the deterministic signal and does not re-derive it. See\n+SADR-0018 for the full decision, the rejected diff/patch-equality and path-disjointness\n alternatives, and the accepted residual.\n \n ## External AI Guardian Review (SADR-0010)\ndiff --git a/docs/agents/implementation-policy.md b/docs/agents/implementation-policy.md\nindex c754144a9fb5bf4a38aa82a60e1670c59e626b44..85c37281bcad196c6b7b02343faf00b03cd9c57a\n--- a/docs/agents/implementation-policy.md\n+++ b/docs/agents/implementation-policy.md\n@@ -31,10 +31,13 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n ## Non-Negotiables\n \n 1. **Planning precedes implementation.** Non-trivial Goals start with `grill-with-docs` and Context & Impact.\n-2. **Domain language is controlled.** Terms must be checked against `CONTEXT.md`; hard-to-reverse tradeoffs must be checked against ADRs.\n+2. **Domain language is controlled.** Terms must be checked against `CONTEXT.md`; hard-to-reverse tradeoffs must be checked against Shiki SADRs and target ADRs.\n 3. **PRD precedes issue decomposition.** `to-prd` is used once the context is settled enough to become durable intent.\n 4. **Issues are vertical slices.** `to-issues` must produce independently grabbable tracer-bullet tasks, not horizontal layer tickets.\n 5. **Triage controls dispatch.** Only `ready-for-agent` / AFK tasks may be assigned to Codex without human input.\n@@ -95,19 +98,19 @@\n Process:\n \n - Challenge terms against `CONTEXT.md`.\n-- Check ADRs before reopening decisions.\n+- Check SADRs and target ADRs before reopening decisions.\n - Inspect code when code can answer a question.\n - Ask one question at a time when the operator is available.\n - Provide a recommended answer with each question.\n - Record resolved terms in `CONTEXT.md`.\n-- Create ADRs only for hard-to-reverse, surprising, tradeoff-based decisions.\n+- Create SADRs or target ADRs only for hard-to-reverse, surprising, tradeoff-based decisions.\n - Convert unresolved load-bearing questions into HITL blockers.\n \n Outputs:\n \n - Resolved design-tree decisions.\n - Updated glossary terms when needed.\n-- ADRs when justified.\n+- SADRs or target ADRs when justified.\n - HITL blockers when decisions remain unresolved.\n \n ## Phase 3 \u2014 Context & Impact\n@@ -117,7 +120,7 @@\n \n Outputs must include:\n \n-- Relevant documents and ADRs.\n+- Relevant documents, SADRs, and target ADRs.\n - Relevant modules, interfaces, seams, callers, and tests.\n - Candidate locks.\n - Dependencies.\n@@ -140,7 +143,7 @@\n - Testing decisions.\n - Out of scope.\n - Further notes.\n-- Links to relevant Goal, domain terms, and ADRs.\n+- Links to relevant Goal, domain terms, SADRs, and target ADRs.\n \n Phase 4 ends with Spec Freeze: the operator explicitly approves the PRD, the\n plan records a `spec_freeze` block (status, approved_by, source), and the SF\n@@ -240,7 +243,7 @@\n - Dependencies and locks.\n - Risk and Guardian requirements.\n - Ledger completeness.\n-- Documentation and ADR updates.\n+- Documentation and SADR/target-ADR updates.\n - Scope drift.\n \n CCA emits one of:\n@@ -301,7 +304,7 @@\n | Evidence failure | Behavior might be done, but proof is missing. | Add checks, PR evidence, ledger entry, or artifacts. |\n | Scope drift | PR changes unrelated behavior. | Revert unrelated changes or split into new Goal. |\n | Dependency block | Upstream task or lock not complete. | Block until dependency resolves. |\n-| Architecture block | Decision needs ADR or deep-module discussion. | `grill-with-docs` or `improve-codebase-architecture`. |\n+| Architecture block | Decision needs a SADR, target ADR, or deep-module discussion. | `grill-with-docs` or `improve-codebase-architecture`. |\n | Risk block | Security/production/policy change needs human approval. | Guardian decision. |\n \n ## Phase 5.5 \u2014 Execution Decision Control\ndiff --git a/docs/agents/runtime-auth-model.md b/docs/agents/runtime-auth-model.md\nindex 6a2e1a47e81ea89b5c68a210ec07772a9374babc..3c54748d24233dbd0ba8473950282547ef4ce935\n--- a/docs/agents/runtime-auth-model.md\n+++ b/docs/agents/runtime-auth-model.md\n@@ -148,7 +148,7 @@\n \n API-key based runners may be added by a target repository as an explicit extension. They are not the default Shiki template path.\n \n-If a repo opts into API-key mode, record the exception in an ADR and update the repo's `AGENTS.md`, `.shiki/config.yaml`, and workflow permissions.\n+If a repo opts into API-key mode, record the target-specific exception in a target ADR and update the repo's `AGENTS.md`, `.shiki/config.yaml`, and workflow permissions.\n \n ## GitHub Actions Runtime Compatibility\n \ndiff --git a/docs/agents/skill-gate.md b/docs/agents/skill-gate.md\nindex c50398b0d7c60a8598f2a5c88a10015730dced13..3acffe6444617bb1d54b77d4b4708158ec735bc6\n--- a/docs/agents/skill-gate.md\n+++ b/docs/agents/skill-gate.md\n@@ -9,7 +9,7 @@\n | Skill | Trigger | Expected output |\n | --- | --- | --- |\n | `setup-matt-pocock-skills` | First configuring a repo for these skills, or when issue tracker, triage labels, or domain docs are missing. | `docs/agents/` configuration and an `Agent skills` block in `AGENTS.md` or `CLAUDE.md`. |\n-| `grill-with-docs` | Ambiguous plan, unclear terminology, boundary decision, tradeoff, or ADR-worthy design choice. | Clarified decision tree, recommended answers, updates to `CONTEXT.md` or ADRs when decisions crystallize. |\n+| `grill-with-docs` | Ambiguous plan, unclear terminology, boundary decision, tradeoff, or decision-record-worthy design choice. | Clarified decision tree, recommended answers, updates to `CONTEXT.md`, SADRs, or target ADRs when decisions crystallize. |\n | `zoom-out` | Agent lacks a higher-level architectural map. | Map of relevant modules, callers, seams, and domain concepts. |\n | `to-prd` | Settled context should become a PRD. | PRD published to the issue tracker with problem, solution, user stories, decisions, testing, and out-of-scope notes. |\n | `to-issues` | Goal, PRD, or plan must become implementation tickets. | Vertical-slice issues with acceptance criteria, dependencies, and AFK/HITL classification. |\n@@ -17,7 +17,7 @@\n | `tdd` | Feature work or bug fix has observable behavior that can be specified with tests. | Red-green-refactor loop through public interfaces, one vertical slice at a time. |\n | `code-review` | The TDD loop is green and the implementer is about to create or update a PR. Default for every implementation task. | Pre-PR self-review of the task diff with fixes applied on the task branch; findings and resolutions recorded as a ledger entry naming the skill and a `## Pre-PR code review` PR body section. |\n | `diagnose` | Hard bug, failing check, regression, flaky behavior, or performance problem. | Reproduction loop, ranked hypotheses, instrumentation, fix, regression test, post-mortem. |\n-| `improve-codebase-architecture` | Structural friction, testability, AI-navigability, deep-module opportunity, or refactor request. | Architecture candidates and deepening recommendations informed by `CONTEXT.md` and ADRs. |\n+| `improve-codebase-architecture` | Structural friction, testability, AI-navigability, deep-module opportunity, or refactor request. | Architecture candidates and deepening recommendations informed by `CONTEXT.md`, SADRs, and target ADRs. |\n | `prototype` | A throwaway logic or UI prototype is needed to answer a design question. | Clearly marked disposable prototype and captured decision. |\n \n ## MergeGate Interaction\ndiff --git a/docs/operations.md b/docs/operations.md\nindex cf1f452d4c589e38ccd58553bda0ad8684b51eb7..ace48a90ffbec9e5c0a7113bef16a4858bc44b3f\n--- a/docs/operations.md\n+++ b/docs/operations.md\n@@ -89,6 +89,16 @@\n      target history, brought forward by `shiki migrate`, not a template.\n    - **Everything else** (CLI scripts, workflows, schemas, docs) is overwritten.\n \n+   A forced upgrade also migrates the former numeric Shiki decision paths to\n+   `SADR-NNNN-*`. Before the first write, the installer inventories the exact\n+   legacy Shiki paths and authorizes each deletion only when the existing\n+   `.shiki/install-stamp.json` names that path and its SHA-256 digest still\n+   matches the file. An absent, unreadable, malformed, incomplete, or\n+   mismatching stamp reports every blocked path and stops with no filesystem\n+   change. Target-owned `NNNN-*` ADRs are not cleanup candidates. A non-force\n+   install deletes nothing and retains any still-matching legacy stamp entries\n+   so a later forced upgrade can prove ownership.\n+\n    If the target has pending migrations, `--force` refuses and writes nothing \u2014\n    apply migrations first (step 3), then re-run. The run ends with a single\n    summary of every `.new` written, so a half-upgrade is visible.\ndiff --git a/docs/releasing.md b/docs/releasing.md\nindex 10d73828e7dbd64e9d9f6b83bc84d010537ce62a..aafeee8c4d441e1e427efee2ce4f81a647f020e1\n--- a/docs/releasing.md\n+++ b/docs/releasing.md\n@@ -67,7 +67,7 @@\n CLI in `scripts/`. There are no third-party Python runtime dependencies. The CLI\n is intentionally **not** exposed as a `[project.scripts]` console entry point\n because `scripts/shiki.py` is a dependency-free shim and `scripts/` is not\n-packaged as an importable distribution (see the ADR for the rationale).\n+packaged as an importable distribution (see SADR-0007 for the rationale).\n \n Supported install paths:\n \n@@ -155,6 +155,15 @@\n    [docs/operations.md](operations.md#the-new-convention). If the target has\n    pending migrations, `--force` refuses and writes nothing; apply migrations\n    (step 2) first, then re-run.\n+\n+   The SADR namespace upgrade has an additional pre-write gate: each former\n+   numeric Shiki decision path is removed only when the existing install stamp\n+   contains that exact path and a digest matching its current bytes. The\n+   installer aggregates absent/unreadable/malformed-stamp, missing-digest, and\n+   digest-mismatch blockers before raising, so refusal leaves the target tree\n+   byte-identical. Numeric target ADRs are preserved. A successful forced\n+   upgrade installs and stamps `SADR-*` paths and drops the removed legacy paths\n+   from the refreshed stamp.\n 2. Apply repository-local `.shiki/` state migrations with the migrate command:\n \n    ```bash\ndiff --git a/docs/usage.md b/docs/usage.md\nindex b83407ee3970dd395dd893010bc5dbe8bad26051..a130ef6e40dfd8169a5bc701339351097df1a09f\n--- a/docs/usage.md\n+++ b/docs/usage.md\n@@ -67,8 +67,10 @@\n shiki install-target /path/to/target-repo --local-only\n ```\n \n-Use `--force` only when you intentionally want to overwrite existing target\n-files.\n+Use `--force` only when you intentionally want to refresh the target template.\n+For the SADR namespace migration, `--force` deletes only exact legacy Shiki\n+decision paths whose existing install-stamp digest still matches; any ownership\n+blocker stops before the first write. Numeric target ADRs are never candidates.\n \n ## `shiki bootstrap-platform` \u2014 publish the Shiki platform repo\n \ndiff --git a/scripts/guardian_approval_signal.py b/scripts/guardian_approval_signal.py\nindex d059f92b2c82aa2b5d6ca421365537a87886c1cb..16924892714f4f5b9303c0ef0e325a7e70075630\n--- a/scripts/guardian_approval_signal.py\n+++ b/scripts/guardian_approval_signal.py\n@@ -199,8 +199,8 @@\n ) -> Any:\n     \"\"\"Evaluate SADR-0015 Contract Approval for the CCA Guardian signal.\n \n-    The SAME OR that MergeGate applies must reach this deterministic signal: ADR\n-    0015 is explicit that without it, a contract-approved PR still returns\n+    The SAME OR that MergeGate applies must reach this deterministic signal:\n+    SADR-0015 is explicit that without it, a contract-approved PR still returns\n     ``needs_guardian``, the CCA job fails, and the MergeGate policy check never\n     runs \u2014 the gate this change moves would never be reached. This resolves the\n     task the SAME way MergeGate does (the first Shiki task id in the PR body),\ndiff --git a/scripts/shiki_contract_approval.py b/scripts/shiki_contract_approval.py\nindex 941bcb96fdd8b9a85bc0c4f0f04d5dad9d04839c..4a4b418f6527413161db82670bec857a043360e2\n--- a/scripts/shiki_contract_approval.py\n+++ b/scripts/shiki_contract_approval.py\n@@ -142,7 +142,7 @@\n     changed_files_status: Any,\n     registration: Any,\n ) -> ContractApprovalResult:\n-    \"\"\"Whether this PR carries an SADR-0015 Contract Approval.\n+    \"\"\"Whether this PR carries a SADR-0015 Contract Approval.\n \n     The four conditions are evaluated IN ORDER, each failure recorded as a\n     distinct reason string:\ndiff --git a/scripts/shiki_guardian.py b/scripts/shiki_guardian.py\nindex a4ecb79bbda55fd5c8b06adfae2eb99760a03c45..4dd99bbe714e26f421fa19abac7a34ae1b195ecc\n--- a/scripts/shiki_guardian.py\n+++ b/scripts/shiki_guardian.py\n@@ -12,7 +12,7 @@\n # The carry re-derives its proof from git in-process on every judging invocation;\n # it is never read from an artifact, so there is nothing to go stale or be forged.\n # shiki_sync_proof is the ONLY guardian-family module that runs git, and every one\n-# of its invocations is read-only and local (ADR: prove-from-git base sync).\n+# of its invocations is read-only and local (SADR: prove-from-git base sync).\n from shiki_sync_proof import verify_pure_base_sync\n \n GUARDIAN_POLICY_PATH = \".shiki/guardian-policy.json\"\ndiff --git a/scripts/shiki_guardian_review.py b/scripts/shiki_guardian_review.py\nindex dd0f104d457899e0ba5ff1e162011c1e90b6f92b..e10ef58c046324e7ea3a3392feb3048ca77414ce\n--- a/scripts/shiki_guardian_review.py\n+++ b/scripts/shiki_guardian_review.py\n@@ -159,7 +159,7 @@\n     docs_only = _is_docs_only(changed_files)\n     if docs_only and matched == 0:\n         focus.append(\n-            \"Docs/ADR-only change: confirm the change is genuinely documentation/decision-only, introduces no behavior or contract drift, and stays consistent with the constitution and existing ADRs.\"\n+            \"Docs/decision-record-only change: confirm the change is genuinely documentation/decision-only, introduces no behavior or contract drift, and stays consistent with the constitution and existing SADRs and target ADRs.\"\n         )\n \n     # Conservative augmentation: unknown (no category and not docs-only), mixed\ndiff --git a/scripts/shiki_installer.py b/scripts/shiki_installer.py\nindex be064ab7a29ff6836f7f7a1d70500a43e0d11d7b..9fd8eefd137ce7003dad5c0dcb2e48e2cd47ab32\n--- a/scripts/shiki_installer.py\n+++ b/scripts/shiki_installer.py\n@@ -118,6 +118,7 @@\n # what lets a target carry it without a governance-schema change.\n INSTALL_STAMP_PATH = \".shiki/install-stamp.json\"\n INSTALL_STAMP_VERSION = 1\n+LEGACY_SHIKI_SADR_MAX = 18\n \n # Under --force the shipped surface splits three ways, so nothing is lost and\n # nothing is silently stale.\n@@ -237,9 +238,98 @@\n         )\n \n \n+def legacy_shiki_adr_paths() -> tuple[str, ...]:\n+    \"\"\"Former numeric paths for Shiki SADR-0001 through SADR-0018.\n+\n+    The authoritative slugs come from the current SADR filenames. SADR-0019 is\n+    the namespace decision itself and never shipped under a numeric legacy path.\n+    \"\"\"\n+    paths: list[str] = []\n+    for sadr in sorted((ROOT / \"docs\" / \"adr\").glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\")):\n+        number = sadr.name.split(\"-\", 2)[1]\n+        if int(number) <= LEGACY_SHIKI_SADR_MAX:\n+            paths.append(f\"docs/adr/{sadr.name.removeprefix('SADR-')}\")\n+    return tuple(paths)\n+\n+\n+def preflight_legacy_sadr_cleanup(target: Path, *, force: bool) -> tuple[str, ...]:\n+    \"\"\"Authorize exact legacy Shiki ADR removals before the first write.\n+\n+    A non-force install never removes existing files. A forced install needs no\n+    stamp when no legacy Shiki path is present. When legacy paths do exist, each\n+    must be named in a readable install stamp and its current bytes must match\n+    the recorded digest.\n+    \"\"\"\n+    if not force:\n+        return ()\n+    present = tuple(\n+        relative\n+        for relative in legacy_shiki_adr_paths()\n+        if (target / relative).exists() or (target / relative).is_symlink()\n+    )\n+    if not present:\n+        return ()\n+\n+    stamp_path = target / INSTALL_STAMP_PATH\n+    try:\n+        raw = stamp_path.read_text(encoding=\"utf-8\")\n+        stamp = json.loads(raw)\n+    except FileNotFoundError:\n+        detail = \"install stamp is absent\"\n+        blockers = [f\"{relative}: {detail}\" for relative in present]\n+    except (OSError, UnicodeDecodeError) as error:\n+        detail = f\"install stamp is unreadable ({error})\"\n+        blockers = [f\"{relative}: {detail}\" for relative in present]\n+    except json.JSONDecodeError as error:\n+        detail = f\"install stamp is malformed ({error})\"\n+        blockers = [f\"{relative}: {detail}\" for relative in present]\n+    else:\n+        if not isinstance(stamp, dict):\n+            blockers = [\n+                f\"{relative}: install stamp is malformed (expected an object)\"\n+                for relative in present\n+            ]\n+        else:\n+            digests = stamp.get(\"digests\")\n+            blockers = []\n+            if not isinstance(digests, dict):\n+                blockers.extend(\n+                    f\"{relative}: install stamp has no digest for this path\"\n+                    for relative in present\n+                )\n+            else:\n+                for relative in present:\n+                    expected = digests.get(relative)\n+                    if not isinstance(expected, str) or not expected:\n+                        blockers.append(\n+                            f\"{relative}: install stamp has no digest for this path\"\n+                        )\n+                        continue\n+                    try:\n+                        actual = _sha256_file(target / relative)\n+                    except OSError as error:\n+                        blockers.append(\n+                            f\"{relative}: legacy path is unreadable ({error})\"\n+                        )\n+                        continue\n+                    if actual != expected:\n+                        blockers.append(\n+                            f\"{relative}: digest mismatch (stamp {expected}, current {actual})\"\n+                        )\n+\n+    if blockers:\n+        joined = \"\\n\".join(f\"- {blocker}\" for blocker in blockers)\n+        raise ShikiError(\n+            \"legacy Shiki ADR cleanup is not authorized:\\n\"\n+            f\"{joined}\\nNo files were written.\"\n+        )\n+    return present\n+\n+\n def install_template(target: Path, *, force: bool, validate: bool) -> list[NewFileNote]:\n     # Preconditions first: never rewrite the tree of a target that cannot be\n     # upgraded cleanly (see refuse_pending_migrations).\n+    legacy_cleanup = preflight_legacy_sadr_cleanup(target, force=force)\n     refuse_pending_migrations(target)\n \n     # Resolve the target's required CODEOWNERS owner once from its\n@@ -250,6 +340,10 @@\n     # verbatim. See ``codeowners_required_owner``.\n     codeowners_owner = codeowners_required_owner(target)\n \n+    for relative in legacy_cleanup:\n+        (target / relative).unlink()\n+        info(f\"removed stamped legacy Shiki ADR: {target / relative}\")\n+\n     notes: list[NewFileNote] = []\n     for relative in TEMPLATE_PATHS:\n         source = ROOT / relative\n@@ -647,6 +741,22 @@\n     digests = target_stamp_digests(target)\n     existing = read_install_stamp(target)\n     if existing is not None:\n+        # A non-force install deliberately leaves legacy Shiki ADRs in place.\n+        # Preserve only still-matching ownership proof so a later forced\n+        # upgrade can authorize cleanup; never carry a missing or stale digest.\n+        existing_digests = existing.get(\"digests\")\n+        if isinstance(existing_digests, dict):\n+            for relative in legacy_shiki_adr_paths():\n+                expected = existing_digests.get(relative)\n+                path = target / relative\n+                if not isinstance(expected, str) or not path.is_file():\n+                    continue\n+                try:\n+                    actual = _sha256_file(path)\n+                except OSError:\n+                    continue\n+                if actual == expected:\n+                    digests[relative] = actual\n         if _incoming_is_older(commit, existing.get(\"platform_commit\")):\n             warn(\n                 \"kept existing install stamp: incoming install \"\ndiff --git a/scripts/shiki_loop.py b/scripts/shiki_loop.py\nindex 3611641da8aeb5038b1da90e3114b09a473ec6d8..84caa491b9cad0edc4176f209c13e9221e975681\n--- a/scripts/shiki_loop.py\n+++ b/scripts/shiki_loop.py\n@@ -424,7 +424,7 @@\n         # unresolvable CCA verdict has already produced its terminal stop and is never\n         # laundered into a head-moving sync (SADR-0010). It also never fires while the\n         # policy (Guardian) gate is red \u2014 for ANY reason, not only when it is the sole\n-        # red check \u2014 so a head-bound Guardian approval, or an SADR-0015\n+        # red check \u2014 so a head-bound Guardian approval, or a SADR-0015\n         # contract-approval carry that base movement defeated (the policy gate compares\n         # base and head task governance directly), is never silently restored by\n         # adopting base wholesale. It is bounded by a durable per-task counter so the\ndiff --git a/scripts/shiki_tasks.py b/scripts/shiki_tasks.py\nindex aec3809f24b4a0c8c96fc50971cff9ae9ad30381..d531052e9d9717058d4631483cefab44c26a65c1\n--- a/scripts/shiki_tasks.py\n+++ b/scripts/shiki_tasks.py\n@@ -821,7 +821,7 @@\n         \"prompt\": prompt,\n         \"entry_skill\": \"grill-with-docs\",\n         \"required_next_steps\": [\n-            \"Run grill-with-docs until terminology, boundaries, risks, and ADR-worthy decisions are settled.\",\n+            \"Run grill-with-docs until terminology, boundaries, risks, and decision-record-worthy choices are settled.\",\n             \"Obtain the operator's explicit approval of the PRD/requirements (Spec Freeze).\",\n             \"Write a machine-readable plan JSON with grill_with_docs.status=complete and spec_freeze.status=frozen.\",\n             \"Run shiki plan ingest --plan-file PLAN.json.\",\ndiff --git a/scripts/test_shiki_goal_loop.sh b/scripts/test_shiki_goal_loop.sh\nindex 753c7b516e10f0be3bc92a4bd0eb1fa699776ae6..8f3820a0a079b3044424cfe1a6c6b7fe68896e67\n--- a/scripts/test_shiki_goal_loop.sh\n+++ b/scripts/test_shiki_goal_loop.sh\n@@ -231,8 +231,8 @@\n \n # High/critical risk merges autonomously when all required checks are green:\n # the \"MergeGate policy check\" required check IS the Guardian gate (it enforces\n-# guardian-policy.json \u2014 human approval OR external AI guardian review, ADR\n-# 0010), so green checks mean a recorded authority approved. The fake gh returns\n+# guardian-policy.json \u2014 human approval OR external AI guardian review,\n+# SADR-0010), so green checks mean a recorded authority approved. The fake gh returns\n # green checks, simulating that the Guardian gate passed.\n python3 \"$ROOT/scripts/shiki.py\" goal create --target \"$TARGET\" --title \"Guardian gate\" --outcome \"High risk merges via policy gate\" >/tmp/shiki-goal-loop-guardian-goal.json\n GGOAL=\"$(json_get /tmp/shiki-goal-loop-guardian-goal.json goal_id)\"\ndiff --git a/tests/test_adr_references.py b/tests/test_adr_references.py\nindex 51706329c797d5dd9b47e082a333fa83b261d482..d005da4f771f5e8085ec22d4b787980e68f5ae8c\n--- a/tests/test_adr_references.py\n+++ b/tests/test_adr_references.py\n@@ -1,8 +1,8 @@\n-\"\"\"Every ``ADR ####`` citation must resolve to exactly one decision record.\n+\"\"\"Every target ADR and Shiki SADR citation resolves in its own namespace.\n \n Background. ``scripts/mergegate_check.py`` justifies ``is_bookkeeping_closeout``\n-with \"ADR 0017\" nine times and ``scripts/guardian_approval_signal.py`` cites\n-0016/0017 seven times, yet for a stretch the numbering in ``docs/adr/`` stopped\n+with \"SADR-0017\" nine times and ``scripts/guardian_approval_signal.py`` cites\n+SADR-0016/SADR-0017 seven times, yet for a stretch the numbering in ``docs/adr/`` stopped\n at 0015: the authoring commits lived on a coordinator branch and no task ever\n declared ``docs/adr/...`` in its locks, so nothing carried the records to the\n default branch. The stated basis of an exemption that merged seven PRs with zero\n@@ -11,11 +11,12 @@\n and the directory drifted with no signal.\n \n This is the same unbound-pair shape behind other silent failures: a reference\n-and its target with nothing asserting they agree. This test binds them. It scans\n-every ``scripts/*.py`` (plus ``docs/**/*.md`` and the root ``*.md`` governance\n-files, which are cheap) for the ``ADR ####`` pattern and asserts each cited\n-number resolves to exactly one ``docs/adr/####-*.md``. The converse is\n-deliberately NOT required: an ADR may exist without being cited.\n+and its target with nothing asserting they agree. This test binds them. Target\n+``ADR NNNN`` citations resolve only to ``docs/adr/NNNN-*.md``; Shiki\n+``SADR-NNNN`` citations resolve only to ``docs/adr/SADR-NNNN-*.md``. The scan\n+covers current governance docs, scripts, workflows, prompts, packaging metadata,\n+and repo-local Codex guidance while deliberately excluding historical\n+``.shiki`` evidence. The converse is not required: a record may be uncited.\n \n The suite is dependency free: standard-library ``unittest`` only.\n \"\"\"\n@@ -31,17 +32,18 @@\n \n REPO_ROOT = Path(__file__).resolve().parents[1]\n \n-# \"ADR 0016\" -- a four-digit citation. The trailing \\b keeps a five-digit token\n-# from being read as a truncated four-digit number.\n-_CITATION = re.compile(r\"\\bADR (\\d{4})\\b\")\n-# \"0016-locks-bound-product-paths.md\" -- the leading four digits name the record.\n-_ADR_FILENAME = re.compile(r\"^(\\d{4})-.*\\.md$\")\n+# ``ADR 0016`` / ``SADR-0016`` -- a namespace plus four-digit citation. The\n+# trailing ``\\b`` keeps a five-digit token from being read as a truncated one.\n+_CITATION = re.compile(r\"\\b(S?ADR)[ -](\\d{4})\\b\")\n+# Target ADRs retain the numeric filename; Shiki platform decisions use SADR.\n+_DECISION_FILENAME = re.compile(r\"^(?:(SADR)-)?(\\d{4})-.*\\.md$\")\n \n \n class Violation(NamedTuple):\n     \"\"\"A citation that does not resolve to exactly one decision record.\"\"\"\n \n     citing_file: str  # repo-relative path of the file that cites the ADR\n+    namespace: str  # \"ADR\" for target decisions; \"SADR\" for Shiki decisions\n     number: str  # the cited four-digit number, e.g. \"0016\"\n     line: int  # 1-based line of the citation\n     reason: str  # \"missing\" (zero matches) or \"ambiguous\" (two or more)\n@@ -49,44 +51,59 @@\n \n     @property\n     def expected_path(self) -> str:\n-        return f\"docs/adr/{self.number}-*.md\"\n+        prefix = \"SADR-\" if self.namespace == \"SADR\" else \"\"\n+        return f\"docs/adr/{prefix}{self.number}-*.md\"\n \n     def describe(self) -> str:\n         \"\"\"Name the citing file, the cited number, and the missing path.\"\"\"\n         if self.reason == \"missing\":\n             return (\n-                f\"{self.citing_file}:{self.line} cites ADR {self.number} \"\n+                f\"{self.citing_file}:{self.line} cites {self.namespace} {self.number} \"\n                 f\"but no document matches {self.expected_path}\"\n             )\n         joined = \", \".join(self.matches)\n         return (\n-            f\"{self.citing_file}:{self.line} cites ADR {self.number} which \"\n+            f\"{self.citing_file}:{self.line} cites {self.namespace} {self.number} which \"\n             f\"resolves to {len(self.matches)} documents (expected exactly one \"\n             f\"at {self.expected_path}): {joined}\"\n         )\n \n \n-def _adr_index(root: Path) -> dict[str, list[Path]]:\n-    \"\"\"Map every four-digit ADR number to the record files that claim it.\"\"\"\n-    index: dict[str, list[Path]] = {}\n+def _adr_index(root: Path) -> dict[tuple[str, str], list[Path]]:\n+    \"\"\"Map each decision namespace and number to the files that claim it.\"\"\"\n+    index: dict[tuple[str, str], list[Path]] = {}\n     adr_dir = root / \"docs\" / \"adr\"\n     if adr_dir.is_dir():\n         for path in sorted(adr_dir.glob(\"*.md\")):\n-            match = _ADR_FILENAME.match(path.name)\n+            match = _DECISION_FILENAME.match(path.name)\n             if match:\n-                index.setdefault(match.group(1), []).append(path)\n+                namespace = \"SADR\" if match.group(1) else \"ADR\"\n+                index.setdefault((namespace, match.group(2)), []).append(path)\n     return index\n \n \n def _citing_files(root: Path, include_docs: bool = True) -> list[Path]:\n-    \"\"\"Files scanned for citations: scripts/*.py, and cheaply the markdown.\"\"\"\n+    \"\"\"Current, shipped text surfaces scanned for decision citations.\"\"\"\n     files = sorted((root / \"scripts\").glob(\"*.py\"))\n+    files += sorted((root / \"scripts\").glob(\"*.sh\"))\n     if include_docs:\n         docs = root / \"docs\"\n         if docs.is_dir():\n             files += sorted(docs.rglob(\"*.md\"))\n         files += sorted(root.glob(\"*.md\"))\n-    return files\n+        pyproject = root / \"pyproject.toml\"\n+        if pyproject.is_file():\n+            files.append(pyproject)\n+        for relative in (\".github\", \".codex\"):\n+            surface = root / relative\n+            if surface.is_dir():\n+                files += sorted(\n+                    path\n+                    for path in surface.rglob(\"*\")\n+                    if path.is_file()\n+                    and path.suffix in {\".md\", \".py\", \".sh\", \".toml\", \".yaml\", \".yml\"}\n+                )\n+    return sorted(set(files))\n \n \n def find_adr_reference_violations(\n@@ -94,10 +111,9 @@\n ) -> list[Violation]:\n     \"\"\"Return every ``ADR ####`` citation under ``root`` that fails to resolve.\n \n-    A citation resolves when exactly one ``docs/adr/####-*.md`` matches its\n-    number. Zero matches is a \"missing\" violation; two or more is \"ambiguous\".\n-    Records that are never cited produce nothing -- the converse is not\n-    required.\n+    A citation resolves when exactly one record in the same namespace matches\n+    its number. Zero matches is a \"missing\" violation; two or more is\n+    \"ambiguous\". Records that are never cited produce nothing.\n     \"\"\"\n     root = Path(root)\n     index = _adr_index(root)\n@@ -110,13 +126,15 @@\n         rel = path.relative_to(root).as_posix()\n         for lineno, line in enumerate(text.splitlines(), start=1):\n             for match in _CITATION.finditer(line):\n-                number = match.group(1)\n-                found = index.get(number, [])\n+                namespace, number = match.groups()\n+                found = index.get((namespace, number), [])\n                 if len(found) == 1:\n                     continue\n                 reason = \"missing\" if not found else \"ambiguous\"\n                 rels = [p.relative_to(root).as_posix() for p in found]\n-                violations.append(Violation(rel, number, lineno, reason, rels))\n+                violations.append(\n+                    Violation(rel, namespace, number, lineno, reason, rels)\n+                )\n     return violations\n \n \n@@ -129,8 +147,8 @@\n         self.assertEqual(\n             violations,\n             [],\n-            \"Every 'ADR ####' citation must resolve to exactly one \"\n-            f\"docs/adr/####-*.md:\\n{detail}\",\n+            \"Every ADR/SADR citation must resolve exactly once in its own \"\n+            f\"docs/adr namespace:\\n{detail}\",\n         )\n \n     def test_scan_actually_reads_scripts(self) -> None:\n@@ -142,12 +160,38 @@\n             \"the citation scan must cover scripts/*.py\",\n         )\n \n+    def test_platform_citations_do_not_use_unresolved_numeric_shorthand(self) -> None:\n+        shorthand = re.compile(r\"SADR-\\d{4}/\\d{4}\")\n+        offenders = []\n+        for path in _citing_files(REPO_ROOT):\n+            text = path.read_text(encoding=\"utf-8\")\n+            if shorthand.search(text):\n+                offenders.append(path.relative_to(REPO_ROOT).as_posix())\n+        self.assertEqual(\n+            offenders,\n+            [],\n+            \"repeat the SADR prefix so every cited number is independently resolvable\",\n+        )\n+\n     def test_carried_records_are_indexed(self) -> None:\n         # The two records this Goal carried onto the default branch, and which\n         # the scripts cite most, must be present.\n         index = _adr_index(REPO_ROOT)\n-        self.assertIn(\"0016\", index)\n-        self.assertIn(\"0017\", index)\n+        self.assertIn((\"SADR\", \"0016\"), index)\n+        self.assertIn((\"SADR\", \"0017\"), index)\n+\n+    def test_platform_tree_has_only_sadr_0001_through_0019(self) -> None:\n+        adr_dir = REPO_ROOT / \"docs\" / \"adr\"\n+        sadrs = sorted(adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\"))\n+        self.assertEqual(\n+            {path.name.split(\"-\", 2)[1] for path in sadrs},\n+            {f\"{number:04d}\" for number in range(1, 20)},\n+        )\n+        self.assertEqual(\n+            list(adr_dir.glob(\"[0-9][0-9][0-9][0-9]-*.md\")),\n+            [],\n+            \"the Shiki platform tree must not retain legacy numeric SADR paths\",\n+        )\n \n \n class FixtureAdrReferences(unittest.TestCase):\n@@ -180,6 +224,26 @@\n         self.assertIn(\"9991\", message)  # cited number\n         self.assertIn(\"docs/adr/9991-*.md\", message)  # missing path\n \n+    def test_target_adr_does_not_satisfy_missing_platform_sadr(self) -> None:\n+        root = self._make_repo(\n+            adr_names=[\"0042-target-decision.md\"],\n+            script_body='\"\"\"Target ADR 0042 exists, but platform SADR-0042 does not.\"\"\"\\n',\n+        )\n+        violations = find_adr_reference_violations(root, include_docs=False)\n+        self.assertEqual(len(violations), 1)\n+        self.assertEqual(violations[0].namespace, \"SADR\")\n+        self.assertEqual(violations[0].number, \"0042\")\n+        self.assertEqual(violations[0].expected_path, \"docs/adr/SADR-0042-*.md\")\n+\n+    def test_target_adr_and_platform_sadr_may_reuse_a_number(self) -> None:\n+        root = self._make_repo(\n+            adr_names=[\"0042-target-decision.md\", \"SADR-0042-platform-decision.md\"],\n+            script_body='\"\"\"Target ADR 0042 and platform SADR-0042 are distinct.\"\"\"\\n',\n+        )\n+        self.assertEqual(\n+            find_adr_reference_violations(root, include_docs=False), []\n+        )\n+\n     def test_uncited_adr_is_allowed(self) -> None:\n         # An ADR exists but nothing cites it -> not a violation. The converse\n         # (every ADR must be cited) is explicitly NOT required.\ndiff --git a/tests/test_guardian_review.py b/tests/test_guardian_review.py\nindex 88829e7810212635e2fc98aa5918d6f26294677d..00a85a237acd5bf2d6cb5b294128198c422402ca\n--- a/tests/test_guardian_review.py\n+++ b/tests/test_guardian_review.py\n@@ -162,7 +162,7 @@\n \n     def test_docs_only_is_light_and_not_conservative(self):\n         focus = gr.classify_review_focus([\"docs/adr/0099-x.md\", \"README.md\"], \"\", \"low\")\n-        self.assertTrue(any(\"Docs/ADR-only\" in f for f in focus))\n+        self.assertTrue(any(\"Docs/decision-record-only\" in f for f in focus))\n         self.assertFalse(any(\"conservative default\" in f.lower() for f in focus))\n \n     def test_unknown_category_is_conservative(self):\ndiff --git a/tests/test_policy_job_merged_prs.py b/tests/test_policy_job_merged_prs.py\nindex d45512e74681e5e649279236c7d4e79d5c3da471..b97989f8bd74d091de1a6b5275a20d8dc68c4512\n--- a/tests/test_policy_job_merged_prs.py\n+++ b/tests/test_policy_job_merged_prs.py\n@@ -3,8 +3,8 @@\n \n ``.github/workflows/shiki-cca-completion.yml`` has two consumers of the merged-PR\n proof: the CCA *signal* (``guardian_approval_signal.py`` in \"Prepare CCA context\")\n-and the authoritative gate (``mergegate_check.py`` in \"Enforce MergeGate\"). ADR\n-0017's bookkeeping-closeout exemption (condition 6) needs the merged implementation\n+and the authoritative gate (``mergegate_check.py`` in \"Enforce MergeGate\").\n+SADR-0017's bookkeeping-closeout exemption (condition 6) needs the merged implementation\n PR proven, and a closeout carries NO ``post_merge_reconcile`` marker. The signal\n already resolves ``MERGED_PRS`` unconditionally, but \"Enforce MergeGate\" used to\n resolve it only inside ``grep -q '<!-- shiki:post_merge_reconcile -->'``. So a\ndiff --git a/tests/test_sadr_install_migration.py b/tests/test_sadr_install_migration.py\nnew file mode 100644\nindex 0000000000000000000000000000000000000000..d51eac872b1c79ff4579c4c1401ca5d775a60d8d\n--- /dev/null\n+++ b/tests/test_sadr_install_migration.py\n@@ -0,0 +1,237 @@\n+\"\"\"SADR installation and collision-safe legacy upgrade behavior.\"\"\"\n+\n+from __future__ import annotations\n+\n+import contextlib\n+import hashlib\n+import io\n+import json\n+import shutil\n+import tempfile\n+import unittest\n+from pathlib import Path\n+\n+import shiki_test_support  # noqa: F401  (path bootstrap)\n+\n+import shiki_installer\n+from tests.test_adr_references import find_adr_reference_violations\n+\n+\n+PRODUCT_NUMBERS = tuple(range(13, 19))\n+\n+\n+def _quiet_install(target: Path, *, force: bool, validate: bool) -> None:\n+    with contextlib.redirect_stdout(io.StringIO()):\n+        shiki_installer.install_template(target, force=force, validate=validate)\n+\n+\n+def _sha256(path: Path) -> str:\n+    return hashlib.sha256(path.read_bytes()).hexdigest()\n+\n+\n+def _legacy_sadr_paths() -> tuple[str, ...]:\n+    return tuple(\n+        f\"docs/adr/{path.name.removeprefix('SADR-')}\"\n+        for path in sorted((shiki_installer.ROOT / \"docs\" / \"adr\").glob(\"SADR-*.md\"))\n+        if path.name != \"SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\"\n+    )\n+\n+\n+def _create_legacy_upgrade_fixture(\n+    target: Path,\n+) -> tuple[dict[str, bytes], tuple[str, ...]]:\n+    adr_dir = target / \"docs\" / \"adr\"\n+    adr_dir.mkdir(parents=True)\n+    product_bytes = {\n+        f\"{number:04d}-cubeshare-product-decision.md\": (\n+            f\"# CubeShare ADR {number:04d}\\n\\nDo not rewrite {number}.\\n\"\n+        ).encode()\n+        for number in PRODUCT_NUMBERS\n+    }\n+    for name, content in product_bytes.items():\n+        (adr_dir / name).write_bytes(content)\n+\n+    legacy_paths = _legacy_sadr_paths()\n+    for relative in legacy_paths:\n+        (target / relative).write_bytes(f\"legacy Shiki bytes for {relative}\\n\".encode())\n+\n+    stamp_path = target / shiki_installer.INSTALL_STAMP_PATH\n+    stamp_path.parent.mkdir(parents=True, exist_ok=True)\n+    stamp = {\n+        \"version\": shiki_installer.INSTALL_STAMP_VERSION,\n+        \"platform_commit\": None,\n+        \"installed_at\": \"2000-01-01T00:00:00+00:00\",\n+        \"digests\": {\n+            relative: _sha256(target / relative) for relative in legacy_paths\n+        },\n+    }\n+    stamp_path.write_text(\n+        json.dumps(stamp, indent=2, sort_keys=True) + \"\\n\", encoding=\"utf-8\"\n+    )\n+    return product_bytes, legacy_paths\n+\n+\n+def _tree_snapshot(root: Path) -> dict[str, tuple[str, bytes | str]]:\n+    \"\"\"Recursive byte/shape snapshot used to prove preflight is write-free.\"\"\"\n+    snapshot: dict[str, tuple[str, bytes | str]] = {}\n+    for path in sorted(root.rglob(\"*\")):\n+        relative = path.relative_to(root).as_posix()\n+        if path.is_symlink():\n+            snapshot[relative] = (\"symlink\", path.readlink().as_posix())\n+        elif path.is_dir():\n+            snapshot[relative] = (\"directory\", \"\")\n+        else:\n+            snapshot[relative] = (\"file\", path.read_bytes())\n+    return snapshot\n+\n+\n+class FreshSadrInstallTests(unittest.TestCase):\n+    def setUp(self) -> None:\n+        self.tmp = Path(tempfile.mkdtemp(prefix=\"shiki-sadr-fresh-\"))\n+        self.addCleanup(lambda: shutil.rmtree(self.tmp, ignore_errors=True))\n+        self.target = self.tmp / \"target\"\n+        self.adr_dir = self.target / \"docs\" / \"adr\"\n+        self.adr_dir.mkdir(parents=True)\n+        self.product_bytes = {\n+            f\"{number:04d}-product-decision.md\": (\n+                f\"# Product ADR {number:04d}\\n\\nTarget-owned bytes {number}.\\n\"\n+            ).encode()\n+            for number in PRODUCT_NUMBERS\n+        }\n+        for name, content in self.product_bytes.items():\n+            (self.adr_dir / name).write_bytes(content)\n+\n+    def test_fresh_install_preserves_product_adrs_and_installs_all_sadrs(self) -> None:\n+        _quiet_install(self.target, force=False, validate=True)\n+\n+        for name, expected in self.product_bytes.items():\n+            self.assertEqual((self.adr_dir / name).read_bytes(), expected)\n+\n+        sadr_numbers = {\n+            path.name.split(\"-\", 2)[1]\n+            for path in self.adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\")\n+        }\n+        self.assertEqual(sadr_numbers, {f\"{number:04d}\" for number in range(1, 20)})\n+\n+        numeric_records = {\n+            path.name\n+            for path in self.adr_dir.glob(\"[0-9][0-9][0-9][0-9]-*.md\")\n+        }\n+        self.assertEqual(numeric_records, set(self.product_bytes))\n+        stamp = json.loads(\n+            (self.target / shiki_installer.INSTALL_STAMP_PATH).read_text(\n+                encoding=\"utf-8\"\n+            )\n+        )\n+        for path in self.adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\"):\n+            relative = path.relative_to(self.target).as_posix()\n+            self.assertEqual(stamp[\"digests\"][relative], _sha256(path))\n+        self.assertEqual(find_adr_reference_violations(self.target), [])\n+\n+\n+class LegacySadrUpgradeTests(unittest.TestCase):\n+    def setUp(self) -> None:\n+        self.tmp = Path(tempfile.mkdtemp(prefix=\"shiki-sadr-upgrade-\"))\n+        self.addCleanup(lambda: shutil.rmtree(self.tmp, ignore_errors=True))\n+        self.target = self.tmp / \"target\"\n+        self.adr_dir = self.target / \"docs\" / \"adr\"\n+        self.product_bytes, self.legacy_paths = _create_legacy_upgrade_fixture(\n+            self.target\n+        )\n+\n+    def test_force_upgrade_deletes_only_stamped_legacy_sadrs(self) -> None:\n+        _quiet_install(self.target, force=True, validate=True)\n+\n+        for relative in self.legacy_paths:\n+            self.assertFalse((self.target / relative).exists(), relative)\n+        for name, expected in self.product_bytes.items():\n+            self.assertEqual((self.adr_dir / name).read_bytes(), expected)\n+\n+        sadrs = list(self.adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\"))\n+        self.assertEqual(len(sadrs), 19)\n+        refreshed = json.loads(\n+            (self.target / shiki_installer.INSTALL_STAMP_PATH).read_text(encoding=\"utf-8\")\n+        )\n+        for relative in self.legacy_paths:\n+            self.assertNotIn(relative, refreshed[\"digests\"])\n+        for path in sadrs:\n+            relative = path.relative_to(self.target).as_posix()\n+            self.assertEqual(refreshed[\"digests\"][relative], _sha256(path))\n+\n+    def test_non_force_install_never_deletes_legacy_files(self) -> None:\n+        before = {\n+            relative: (self.target / relative).read_bytes()\n+            for relative in self.legacy_paths\n+        }\n+        _quiet_install(self.target, force=False, validate=False)\n+        for relative, expected in before.items():\n+            self.assertEqual((self.target / relative).read_bytes(), expected)\n+        refreshed = json.loads(\n+            (self.target / shiki_installer.INSTALL_STAMP_PATH).read_text(encoding=\"utf-8\")\n+        )\n+        for relative in self.legacy_paths:\n+            self.assertEqual(refreshed[\"digests\"][relative], _sha256(self.target / relative))\n+\n+\n+class LegacyCleanupPreflightTests(unittest.TestCase):\n+    def setUp(self) -> None:\n+        self.tmp = Path(tempfile.mkdtemp(prefix=\"shiki-sadr-preflight-\"))\n+        self.addCleanup(lambda: shutil.rmtree(self.tmp, ignore_errors=True))\n+\n+    def test_all_ownership_blockers_abort_before_any_write(self) -> None:\n+        cases = (\n+            (\"absent\", \"install stamp is absent\"),\n+            (\"unreadable\", \"install stamp is unreadable\"),\n+            (\"malformed\", \"install stamp is malformed\"),\n+            (\"missing-digest\", \"install stamp has no digest\"),\n+            (\"digest-mismatch\", \"digest mismatch\"),\n+        )\n+        for name, expected_reason in cases:\n+            with self.subTest(name=name):\n+                target = self.tmp / name\n+                _, legacy_paths = _create_legacy_upgrade_fixture(target)\n+                stamp_path = target / shiki_installer.INSTALL_STAMP_PATH\n+\n+                if name == \"absent\":\n+                    stamp_path.unlink()\n+                elif name == \"unreadable\":\n+                    stamp_path.unlink()\n+                    stamp_path.mkdir()\n+                elif name == \"malformed\":\n+                    stamp_path.write_text(\"{not-json\\n\", encoding=\"utf-8\")\n+                else:\n+                    stamp = json.loads(stamp_path.read_text(encoding=\"utf-8\"))\n+                    if name == \"missing-digest\":\n+                        stamp[\"digests\"] = {}\n+                    else:\n+                        stamp[\"digests\"] = {\n+                            relative: \"deadbeef\" for relative in legacy_paths\n+                        }\n+                    stamp_path.write_text(\n+                        json.dumps(stamp, indent=2, sort_keys=True) + \"\\n\",\n+                        encoding=\"utf-8\",\n+                    )\n+\n+                before = _tree_snapshot(target)\n+                with self.assertRaises(shiki_installer.ShikiError) as raised:\n+                    _quiet_install(target, force=True, validate=False)\n+                after = _tree_snapshot(target)\n+\n+                self.assertEqual(before, after, f\"{name} preflight changed the tree\")\n+                message = str(raised.exception)\n+                self.assertIn(expected_reason, message)\n+                self.assertIn(\"No files were written\", message)\n+                for relative in legacy_paths:\n+                    self.assertIn(relative, message)\n+\n+    def test_absent_stamp_is_allowed_when_no_legacy_path_exists(self) -> None:\n+        target = self.tmp / \"no-legacy\"\n+        target.mkdir()\n+        _quiet_install(target, force=True, validate=False)\n+        self.assertTrue(\n+            (target / \"docs\" / \"adr\" / \"SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\").is_file()\n+        )\n+\n+\n+if __name__ == \"__main__\":\n+    unittest.main()\n\n2026-08-07T08:28:36.071951Z ERROR codex_models_manager::manager: failed to renew cache TTL: missing field `base_instructions` at line 94 column 5\n succeeded in 1544ms:\nok\ntest_absent_stamp_is_allowed_when_no_legacy_path_exists (test_sadr_install_migration.LegacyCleanupPreflightTests.test_absent_stamp_is_allowed_when_no_legacy_path_exists) ... ok\ntest_all_ownership_blockers_abort_before_any_write (test_sadr_install_migration.LegacyCleanupPreflightTests.test_all_ownership_blockers_abort_before_any_write) ... ok\ntest_force_upgrade_deletes_only_stamped_legacy_sadrs (test_sadr_install_migration.LegacySadrUpgradeTests.test_force_upgrade_deletes_only_stamped_legacy_sadrs) ... ok\ntest_non_force_install_never_deletes_legacy_files (test_sadr_install_migration.LegacySadrUpgradeTests.test_non_force_install_never_deletes_legacy_files) ... ok\n\n----------------------------------------------------------------------\nRan 5 tests in 1.527s\n\nOK\n\nexec\n/bin/zsh -lc \"nl -ba scripts/shiki_installer.py | sed -n '235,360p;735,785p'\" in /private/tmp/shiki-adr-collision.pa0KwY/.worktrees/shiki-t-20260807t073307221254z-2d22d654-introduc\n succeeded in 0ms:\n   235\t        raise ShikiError(\n   236\t            f\"target has pending .shiki migrations ({pending}); run \"\n   237\t            \"`shiki migrate apply --execute` before upgrading. No files were written.\"\n   238\t        )\n   239\t\n   240\t\n   241\tdef legacy_shiki_adr_paths() -> tuple[str, ...]:\n   242\t    \"\"\"Former numeric paths for Shiki SADR-0001 through SADR-0018.\n   243\t\n   244\t    The authoritative slugs come from the current SADR filenames. SADR-0019 is\n   245\t    the namespace decision itself and never shipped under a numeric legacy path.\n   246\t    \"\"\"\n   247\t    paths: list[str] = []\n   248\t    for sadr in sorted((ROOT / \"docs\" / \"adr\").glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\")):\n   249\t        number = sadr.name.split(\"-\", 2)[1]\n   250\t        if int(number) <= LEGACY_SHIKI_SADR_MAX:\n   251\t            paths.append(f\"docs/adr/{sadr.name.removeprefix('SADR-')}\")\n   252\t    return tuple(paths)\n   253\t\n   254\t\n   255\tdef preflight_legacy_sadr_cleanup(target: Path, *, force: bool) -> tuple[str, ...]:\n   256\t    \"\"\"Authorize exact legacy Shiki ADR removals before the first write.\n   257\t\n   258\t    A non-force install never removes existing files. A forced install needs no\n   259\t    stamp when no legacy Shiki path is present. When legacy paths do exist, each\n   260\t    must be named in a readable install stamp and its current bytes must match\n   261\t    the recorded digest.\n   262\t    \"\"\"\n   263\t    if not force:\n   264\t        return ()\n   265\t    present = tuple(\n   266\t        relative\n   267\t        for relative in legacy_shiki_adr_paths()\n   268\t        if (target / relative).exists() or (target / relative).is_symlink()\n   269\t    )\n   270\t    if not present:\n   271\t        return ()\n   272\t\n   273\t    stamp_path = target / INSTALL_STAMP_PATH\n   274\t    try:\n   275\t        raw = stamp_path.read_text(encoding=\"utf-8\")\n   276\t        stamp = json.loads(raw)\n   277\t    except FileNotFoundError:\n   278\t        detail = \"install stamp is absent\"\n   279\t        blockers = [f\"{relative}: {detail}\" for relative in present]\n   280\t    except (OSError, UnicodeDecodeError) as error:\n   281\t        detail = f\"install stamp is unreadable ({error})\"\n   282\t        blockers = [f\"{relative}: {detail}\" for relative in present]\n   283\t    except json.JSONDecodeError as error:\n   284\t        detail = f\"install stamp is malformed ({error})\"\n   285\t        blockers = [f\"{relative}: {detail}\" for relative in present]\n   286\t    else:\n   287\t        if not isinstance(stamp, dict):\n   288\t            blockers = [\n   289\t                f\"{relative}: install stamp is malformed (expected an object)\"\n   290\t                for relative in present\n   291\t            ]\n   292\t        else:\n   293\t            digests = stamp.get(\"digests\")\n   294\t            blockers = []\n   295\t            if not isinstance(digests, dict):\n   296\t                blockers.extend(\n   297\t                    f\"{relative}: install stamp has no digest for this path\"\n   298\t                    for relative in present\n   299\t                )\n   300\t            else:\n   301\t                for relative in present:\n   302\t                    expected = digests.get(relative)\n   303\t                    if not isinstance(expected, str) or not expected:\n   304\t                        blockers.append(\n   305\t                            f\"{relative}: install stamp has no digest for this path\"\n   306\t                        )\n   307\t                        continue\n   308\t                    try:\n   309\t                        actual = _sha256_file(target / relative)\n   310\t                    except OSError as error:\n   311\t                        blockers.append(\n   312\t                            f\"{relative}: legacy path is unreadable ({error})\"\n   313\t                        )\n   314\t                        continue\n   315\t                    if actual != expected:\n   316\t                        blockers.append(\n   317\t                            f\"{relative}: digest mismatch (stamp {expected}, current {actual})\"\n   318\t                        )\n   319\t\n   320\t    if blockers:\n   321\t        joined = \"\\n\".join(f\"- {blocker}\" for blocker in blockers)\n   322\t        raise ShikiError(\n   323\t            \"legacy Shiki ADR cleanup is not authorized:\\n\"\n   324\t            f\"{joined}\\nNo files were written.\"\n   325\t        )\n   326\t    return present\n   327\t\n   328\t\n   329\tdef install_template(target: Path, *, force: bool, validate: bool) -> list[NewFileNote]:\n   330\t    # Preconditions first: never rewrite the tree of a target that cannot be\n   331\t    # upgraded cleanly (see refuse_pending_migrations).\n   332\t    legacy_cleanup = preflight_legacy_sadr_cleanup(target, force=force)\n   333\t    refuse_pending_migrations(target)\n   334\t\n   335\t    # Resolve the target's required CODEOWNERS owner once from its\n   336\t    # ``.shiki/repo.json`` (present when a target already knows its GitHub\n   337\t    # identity). The shipped ``.github/CODEOWNERS`` names this maintainer on every\n   338\t    # line, so a foreign target's copy is rewritten to name its own owner; when no\n   339\t    # owner can be resolved this is the documented fallback and the file is copied\n   340\t    # verbatim. See ``codeowners_required_owner``.\n   341\t    codeowners_owner = codeowners_required_owner(target)\n   342\t\n   343\t    for relative in legacy_cleanup:\n   344\t        (target / relative).unlink()\n   345\t        info(f\"removed stamped legacy Shiki ADR: {target / relative}\")\n   346\t\n   347\t    notes: list[NewFileNote] = []\n   348\t    for relative in TEMPLATE_PATHS:\n   349\t        source = ROOT / relative\n   350\t        if not source.exists():\n   351\t            warn(f\"template path missing, skipped: {relative}\")\n   352\t            continue\n   353\t        copy_path(\n   354\t            source,\n   355\t            target / relative,\n   356\t            force=force,\n   357\t            target_install=True,\n   358\t            notes=notes,\n   359\t            codeowners_owner=codeowners_owner,\n   360\t        )\n   735\t\n   736\t    Never overwrites a stamp from a newer platform commit with older content,\n   737\t    and leaves an already-current stamp byte-for-byte untouched so a no-op\n   738\t    re-install churns nothing.\n   739\t    \"\"\"\n   740\t    commit = platform_commit(ROOT)\n   741\t    digests = target_stamp_digests(target)\n   742\t    existing = read_install_stamp(target)\n   743\t    if existing is not None:\n   744\t        # A non-force install deliberately leaves legacy Shiki ADRs in place.\n   745\t        # Preserve only still-matching ownership proof so a later forced\n   746\t        # upgrade can authorize cleanup; never carry a missing or stale digest.\n   747\t        existing_digests = existing.get(\"digests\")\n   748\t        if isinstance(existing_digests, dict):\n   749\t            for relative in legacy_shiki_adr_paths():\n   750\t                expected = existing_digests.get(relative)\n   751\t                path = target / relative\n   752\t                if not isinstance(expected, str) or not path.is_file():\n   753\t                    continue\n   754\t                try:\n   755\t                    actual = _sha256_file(path)\n   756\t                except OSError:\n   757\t                    continue\n   758\t                if actual == expected:\n   759\t                    digests[relative] = actual\n   760\t        if _incoming_is_older(commit, existing.get(\"platform_commit\")):\n   761\t            warn(\n   762\t                \"kept existing install stamp: incoming install \"\n   763\t                f\"({commit}) is older than the recorded platform commit \"\n   764\t                f\"({existing.get('platform_commit')}); not overwriting {INSTALL_STAMP_PATH}\"\n   765\t            )\n   766\t            return\n   767\t        if existing.get(\"platform_commit\") == commit and existing.get(\"digests\") == digests:\n   768\t            return\n   769\t    stamp = {\n   770\t        \"version\": INSTALL_STAMP_VERSION,\n   771\t        \"platform_commit\": commit,\n   772\t        \"installed_at\": utc_now(),\n   773\t        \"digests\": digests,\n   774\t    }\n   775\t    path = target / INSTALL_STAMP_PATH\n   776\t    path.parent.mkdir(parents=True, exist_ok=True)\n   777\t    path.write_text(json.dumps(stamp, indent=2, sort_keys=True) + \"\\n\", encoding=\"utf-8\")\n   778\t    info(f\"wrote install stamp: {path}\")\n   779\t\n   780\t\n   781\tdef cmd_install_target(args: argparse.Namespace) -> int:\n   782\t    target = Path(args.target).expanduser().resolve()\n   783\t    if not target.exists():\n   784\t        raise ShikiError(f\"target does not exist: {target}\")\n   785\t    if not target.is_dir():\n\nexec\n/bin/zsh -lc \"nl -ba tests/test_sadr_install_migration.py | sed -n '90,255p'\" in /private/tmp/shiki-adr-collision.pa0KwY/.worktrees/shiki-t-20260807t073307221254z-2d22d654-introduc\nexec\n/bin/zsh -lc \"nl -ba .shiki/ledger/L-20260807T082800000000Z-c0de0019.json | sed -n '1,90p'\" in /private/tmp/shiki-adr-collision.pa0KwY/.worktrees/shiki-t-20260807t073307221254z-2d22d654-introduc\n succeeded in 0ms:\n    90\t        self.tmp = Path(tempfile.mkdtemp(prefix=\"shiki-sadr-fresh-\"))\n    91\t        self.addCleanup(lambda: shutil.rmtree(self.tmp, ignore_errors=True))\n    92\t        self.target = self.tmp / \"target\"\n    93\t        self.adr_dir = self.target / \"docs\" / \"adr\"\n    94\t        self.adr_dir.mkdir(parents=True)\n    95\t        self.product_bytes = {\n    96\t            f\"{number:04d}-product-decision.md\": (\n    97\t                f\"# Product ADR {number:04d}\\n\\nTarget-owned bytes {number}.\\n\"\n    98\t            ).encode()\n    99\t            for number in PRODUCT_NUMBERS\n   100\t        }\n   101\t        for name, content in self.product_bytes.items():\n   102\t            (self.adr_dir / name).write_bytes(content)\n   103\t\n   104\t    def test_fresh_install_preserves_product_adrs_and_installs_all_sadrs(self) -> None:\n   105\t        _quiet_install(self.target, force=False, validate=True)\n   106\t\n   107\t        for name, expected in self.product_bytes.items():\n   108\t            self.assertEqual((self.adr_dir / name).read_bytes(), expected)\n   109\t\n   110\t        sadr_numbers = {\n   111\t            path.name.split(\"-\", 2)[1]\n   112\t            for path in self.adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\")\n   113\t        }\n   114\t        self.assertEqual(sadr_numbers, {f\"{number:04d}\" for number in range(1, 20)})\n   115\t\n   116\t        numeric_records = {\n   117\t            path.name\n   118\t            for path in self.adr_dir.glob(\"[0-9][0-9][0-9][0-9]-*.md\")\n   119\t        }\n   120\t        self.assertEqual(numeric_records, set(self.product_bytes))\n   121\t        stamp = json.loads(\n   122\t            (self.target / shiki_installer.INSTALL_STAMP_PATH).read_text(\n   123\t                encoding=\"utf-8\"\n   124\t            )\n   125\t        )\n   126\t        for path in self.adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\"):\n   127\t            relative = path.relative_to(self.target).as_posix()\n   128\t            self.assertEqual(stamp[\"digests\"][relative], _sha256(path))\n   129\t        self.assertEqual(find_adr_reference_violations(self.target), [])\n   130\t\n   131\t\n   132\tclass LegacySadrUpgradeTests(unittest.TestCase):\n   133\t    def setUp(self) -> None:\n   134\t        self.tmp = Path(tempfile.mkdtemp(prefix=\"shiki-sadr-upgrade-\"))\n   135\t        self.addCleanup(lambda: shutil.rmtree(self.tmp, ignore_errors=True))\n   136\t        self.target = self.tmp / \"target\"\n   137\t        self.adr_dir = self.target / \"docs\" / \"adr\"\n   138\t        self.product_bytes, self.legacy_paths = _create_legacy_upgrade_fixture(\n   139\t            self.target\n   140\t        )\n   141\t\n   142\t    def test_force_upgrade_deletes_only_stamped_legacy_sadrs(self) -> None:\n   143\t        _quiet_install(self.target, force=True, validate=True)\n   144\t\n   145\t        for relative in self.legacy_paths:\n   146\t            self.assertFalse((self.target / relative).exists(), relative)\n   147\t        for name, expected in self.product_bytes.items():\n   148\t            self.assertEqual((self.adr_dir / name).read_bytes(), expected)\n   149\t\n   150\t        sadrs = list(self.adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\"))\n   151\t        self.assertEqual(len(sadrs), 19)\n   152\t        refreshed = json.loads(\n   153\t            (self.target / shiki_installer.INSTALL_STAMP_PATH).read_text(encoding=\"utf-8\")\n   154\t        )\n   155\t        for relative in self.legacy_paths:\n   156\t            self.assertNotIn(relative, refreshed[\"digests\"])\n   157\t        for path in sadrs:\n   158\t            relative = path.relative_to(self.target).as_posix()\n   159\t            self.assertEqual(refreshed[\"digests\"][relative], _sha256(path))\n   160\t\n   161\t    def test_non_force_install_never_deletes_legacy_files(self) -> None:\n   162\t        before = {\n   163\t            relative: (self.target / relative).read_bytes()\n   164\t            for relative in self.legacy_paths\n   165\t        }\n   166\t        _quiet_install(self.target, force=False, validate=False)\n   167\t        for relative, expected in before.items():\n   168\t            self.assertEqual((self.target / relative).read_bytes(), expected)\n   169\t        refreshed = json.loads(\n   170\t            (self.target / shiki_installer.INSTALL_STAMP_PATH).read_text(encoding=\"utf-8\")\n   171\t        )\n   172\t        for relative in self.legacy_paths:\n   173\t            self.assertEqual(refreshed[\"digests\"][relative], _sha256(self.target / relative))\n   174\t\n   175\t\n   176\tclass LegacyCleanupPreflightTests(unittest.TestCase):\n   177\t    def setUp(self) -> None:\n   178\t        self.tmp = Path(tempfile.mkdtemp(prefix=\"shiki-sadr-preflight-\"))\n   179\t        self.addCleanup(lambda: shutil.rmtree(self.tmp, ignore_errors=True))\n   180\t\n   181\t    def test_all_ownership_blockers_abort_before_any_write(self) -> None:\n   182\t        cases = (\n   183\t            (\"absent\", \"install stamp is absent\"),\n   184\t            (\"unreadable\", \"install stamp is unreadable\"),\n   185\t            (\"malformed\", \"install stamp is malformed\"),\n   186\t            (\"missing-digest\", \"install stamp has no digest\"),\n   187\t            (\"digest-mismatch\", \"digest mismatch\"),\n   188\t        )\n   189\t        for name, expected_reason in cases:\n   190\t            with self.subTest(name=name):\n   191\t                target = self.tmp / name\n   192\t                _, legacy_paths = _create_legacy_upgrade_fixture(target)\n   193\t                stamp_path = target / shiki_installer.INSTALL_STAMP_PATH\n   194\t\n   195\t                if name == \"absent\":\n   196\t                    stamp_path.unlink()\n   197\t                elif name == \"unreadable\":\n   198\t                    stamp_path.unlink()\n   199\t                    stamp_path.mkdir()\n   200\t                elif name == \"malformed\":\n   201\t                    stamp_path.write_text(\"{not-json\\n\", encoding=\"utf-8\")\n   202\t                else:\n   203\t                    stamp = json.loads(stamp_path.read_text(encoding=\"utf-8\"))\n   204\t                    if name == \"missing-digest\":\n   205\t                        stamp[\"digests\"] = {}\n   206\t                    else:\n   207\t                        stamp[\"digests\"] = {\n   208\t                            relative: \"deadbeef\" for relative in legacy_paths\n   209\t                        }\n   210\t                    stamp_path.write_text(\n   211\t                        json.dumps(stamp, indent=2, sort_keys=True) + \"\\n\",\n   212\t                        encoding=\"utf-8\",\n   213\t                    )\n   214\t\n   215\t                before = _tree_snapshot(target)\n   216\t                with self.assertRaises(shiki_installer.ShikiError) as raised:\n   217\t                    _quiet_install(target, force=True, validate=False)\n   218\t                after = _tree_snapshot(target)\n   219\t\n   220\t                self.assertEqual(before, after, f\"{name} preflight changed the tree\")\n   221\t                message = str(raised.exception)\n   222\t                self.assertIn(expected_reason, message)\n   223\t                self.assertIn(\"No files were written\", message)\n   224\t                for relative in legacy_paths:\n   225\t                    self.assertIn(relative, message)\n   226\t\n   227\t    def test_absent_stamp_is_allowed_when_no_legacy_path_exists(self) -> None:\n   228\t        target = self.tmp / \"no-legacy\"\n   229\t        target.mkdir()\n   230\t        _quiet_install(target, force=True, validate=False)\n   231\t        self.assertTrue(\n   232\t            (target / \"docs\" / \"adr\" / \"SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\").is_file()\n   233\t        )\n   234\t\n   235\t\n   236\tif __name__ == \"__main__\":\n   237\t    unittest.main()\n\n succeeded in 0ms:\n     1\t{\n     2\t  \"actor\": \"codex\",\n     3\t  \"evidence\": [\n     4\t    \"Full task diff reviewed against scope, non-goals, locks, acceptance checks, failure atomicity, path ownership, and compatibility requirements\",\n     5\t    \"Finding repaired: tests/test_sadr_install_migration.py now asserts every fresh-installed SADR path has the matching SHA-256 digest in .shiki/install-stamp.json\",\n     6\t    \"Re-review found no remaining blocking issues; rejected findings: none\",\n     7\t    \"python3 -m unittest discover -s tests: 1147 tests passed\",\n     8\t    \"bash scripts/test_shiki_control_plane.sh: passed\",\n     9\t    \"bash scripts/test_shiki_adversarial_state.sh: passed\",\n    10\t    \"python3 scripts/validate_shiki.py --target .: passed\"\n    11\t  ],\n    12\t  \"goal_id\": \"G-20260807T073307219752Z-1ee1ed68\",\n    13\t  \"id\": \"L-20260807T082800000000Z-c0de0019\",\n    14\t  \"links\": [],\n    15\t  \"review\": {\n    16\t    \"applied_fixes\": [\n    17\t      \"Added direct fresh-install stamp path/digest assertions and reran focused plus full verification\"\n    18\t    ],\n    19\t    \"findings\": [\n    20\t      {\n    21\t        \"detail\": \"The fresh-install test proved SADR files were installed but did not directly prove their paths and digests were recorded in the refreshed install stamp.\",\n    22\t        \"resolution\": \"fixed_and_re_reviewed\",\n    23\t        \"severity\": \"blocking\",\n    24\t        \"title\": \"Fresh-install stamp evidence was indirect\"\n    25\t      }\n    26\t    ],\n    27\t    \"rejected_findings\": [],\n    28\t    \"verdict\": \"clean_after_repairs\"\n    29\t  },\n    30\t  \"summary\": \"code-review implementer verdict CLEAN after re-review: findings=1; applied_fixes=1 (fresh-install stamp-digest assertion); rejected_findings=0 (none)\",\n    31\t  \"task_id\": \"T-20260807T073307221254Z-2d22d654\",\n    32\t  \"timestamp\": \"2026-08-07T08:28:00+00:00\",\n    33\t  \"type\": \"review\"\n    34\t}\n\nexec\n/bin/zsh -lc \"nl -ba tests/test_adr_references.py | sed -n '28,140p;150,265p'\" in /private/tmp/shiki-adr-collision.pa0KwY/.worktrees/shiki-t-20260807t073307221254z-2d22d654-introduc\n succeeded in 0ms:\n    28\timport tempfile\n    29\timport unittest\n    30\tfrom pathlib import Path\n    31\tfrom typing import NamedTuple, Sequence\n    32\t\n    33\tREPO_ROOT = Path(__file__).resolve().parents[1]\n    34\t\n    35\t# ``ADR 0016`` / ``SADR-0016`` -- a namespace plus four-digit citation. The\n    36\t# trailing ``\\b`` keeps a five-digit token from being read as a truncated one.\n    37\t_CITATION = re.compile(r\"\\b(S?ADR)[ -](\\d{4})\\b\")\n    38\t# Target ADRs retain the numeric filename; Shiki platform decisions use SADR.\n    39\t_DECISION_FILENAME = re.compile(r\"^(?:(SADR)-)?(\\d{4})-.*\\.md$\")\n    40\t\n    41\t\n    42\tclass Violation(NamedTuple):\n    43\t    \"\"\"A citation that does not resolve to exactly one decision record.\"\"\"\n    44\t\n    45\t    citing_file: str  # repo-relative path of the file that cites the ADR\n    46\t    namespace: str  # \"ADR\" for target decisions; \"SADR\" for Shiki decisions\n    47\t    number: str  # the cited four-digit number, e.g. \"0016\"\n    48\t    line: int  # 1-based line of the citation\n    49\t    reason: str  # \"missing\" (zero matches) or \"ambiguous\" (two or more)\n    50\t    matches: Sequence[str]  # repo-relative paths matching docs/adr/<n>-*.md\n    51\t\n    52\t    @property\n    53\t    def expected_path(self) -> str:\n    54\t        prefix = \"SADR-\" if self.namespace == \"SADR\" else \"\"\n    55\t        return f\"docs/adr/{prefix}{self.number}-*.md\"\n    56\t\n    57\t    def describe(self) -> str:\n    58\t        \"\"\"Name the citing file, the cited number, and the missing path.\"\"\"\n    59\t        if self.reason == \"missing\":\n    60\t            return (\n    61\t                f\"{self.citing_file}:{self.line} cites {self.namespace} {self.number} \"\n    62\t                f\"but no document matches {self.expected_path}\"\n    63\t            )\n    64\t        joined = \", \".join(self.matches)\n    65\t        return (\n    66\t            f\"{self.citing_file}:{self.line} cites {self.namespace} {self.number} which \"\n    67\t            f\"resolves to {len(self.matches)} documents (expected exactly one \"\n    68\t            f\"at {self.expected_path}): {joined}\"\n    69\t        )\n    70\t\n    71\t\n    72\tdef _adr_index(root: Path) -> dict[tuple[str, str], list[Path]]:\n    73\t    \"\"\"Map each decision namespace and number to the files that claim it.\"\"\"\n    74\t    index: dict[tuple[str, str], list[Path]] = {}\n    75\t    adr_dir = root / \"docs\" / \"adr\"\n    76\t    if adr_dir.is_dir():\n    77\t        for path in sorted(adr_dir.glob(\"*.md\")):\n    78\t            match = _DECISION_FILENAME.match(path.name)\n    79\t            if match:\n    80\t                namespace = \"SADR\" if match.group(1) else \"ADR\"\n    81\t                index.setdefault((namespace, match.group(2)), []).append(path)\n    82\t    return index\n    83\t\n    84\t\n    85\tdef _citing_files(root: Path, include_docs: bool = True) -> list[Path]:\n    86\t    \"\"\"Current, shipped text surfaces scanned for decision citations.\"\"\"\n    87\t    files = sorted((root / \"scripts\").glob(\"*.py\"))\n    88\t    files += sorted((root / \"scripts\").glob(\"*.sh\"))\n    89\t    if include_docs:\n    90\t        docs = root / \"docs\"\n    91\t        if docs.is_dir():\n    92\t            files += sorted(docs.rglob(\"*.md\"))\n    93\t        files += sorted(root.glob(\"*.md\"))\n    94\t        pyproject = root / \"pyproject.toml\"\n    95\t        if pyproject.is_file():\n    96\t            files.append(pyproject)\n    97\t        for relative in (\".github\", \".codex\"):\n    98\t            surface = root / relative\n    99\t            if surface.is_dir():\n   100\t                files += sorted(\n   101\t                    path\n   102\t                    for path in surface.rglob(\"*\")\n   103\t                    if path.is_file()\n   104\t                    and path.suffix in {\".md\", \".py\", \".sh\", \".toml\", \".yaml\", \".yml\"}\n   105\t                )\n   106\t    return sorted(set(files))\n   107\t\n   108\t\n   109\tdef find_adr_reference_violations(\n   110\t    root: Path | str, include_docs: bool = True\n   111\t) -> list[Violation]:\n   112\t    \"\"\"Return every ``ADR ####`` citation under ``root`` that fails to resolve.\n   113\t\n   114\t    A citation resolves when exactly one record in the same namespace matches\n   115\t    its number. Zero matches is a \"missing\" violation; two or more is\n   116\t    \"ambiguous\". Records that are never cited produce nothing.\n   117\t    \"\"\"\n   118\t    root = Path(root)\n   119\t    index = _adr_index(root)\n   120\t    violations: list[Violation] = []\n   121\t    for path in _citing_files(root, include_docs=include_docs):\n   122\t        try:\n   123\t            text = path.read_text(encoding=\"utf-8\")\n   124\t        except (OSError, UnicodeDecodeError):\n   125\t            continue\n   126\t        rel = path.relative_to(root).as_posix()\n   127\t        for lineno, line in enumerate(text.splitlines(), start=1):\n   128\t            for match in _CITATION.finditer(line):\n   129\t                namespace, number = match.groups()\n   130\t                found = index.get((namespace, number), [])\n   131\t                if len(found) == 1:\n   132\t                    continue\n   133\t                reason = \"missing\" if not found else \"ambiguous\"\n   134\t                rels = [p.relative_to(root).as_posix() for p in found]\n   135\t                violations.append(\n   136\t                    Violation(rel, namespace, number, lineno, reason, rels)\n   137\t                )\n   138\t    return violations\n   139\t\n   140\t\n   150\t            \"Every ADR/SADR citation must resolve exactly once in its own \"\n   151\t            f\"docs/adr namespace:\\n{detail}\",\n   152\t        )\n   153\t\n   154\t    def test_scan_actually_reads_scripts(self) -> None:\n   155\t        # Guard against a silent pass caused by scanning nothing: the scan must\n   156\t        # include scripts/*.py, the surface the acceptance check names.\n   157\t        files = _citing_files(REPO_ROOT)\n   158\t        self.assertTrue(\n   159\t            any(f.match(\"scripts/*.py\") for f in files),\n   160\t            \"the citation scan must cover scripts/*.py\",\n   161\t        )\n   162\t\n   163\t    def test_platform_citations_do_not_use_unresolved_numeric_shorthand(self) -> None:\n   164\t        shorthand = re.compile(r\"SADR-\\d{4}/\\d{4}\")\n   165\t        offenders = []\n   166\t        for path in _citing_files(REPO_ROOT):\n   167\t            text = path.read_text(encoding=\"utf-8\")\n   168\t            if shorthand.search(text):\n   169\t                offenders.append(path.relative_to(REPO_ROOT).as_posix())\n   170\t        self.assertEqual(\n   171\t            offenders,\n   172\t            [],\n   173\t            \"repeat the SADR prefix so every cited number is independently resolvable\",\n   174\t        )\n   175\t\n   176\t    def test_carried_records_are_indexed(self) -> None:\n   177\t        # The two records this Goal carried onto the default branch, and which\n   178\t        # the scripts cite most, must be present.\n   179\t        index = _adr_index(REPO_ROOT)\n   180\t        self.assertIn((\"SADR\", \"0016\"), index)\n   181\t        self.assertIn((\"SADR\", \"0017\"), index)\n   182\t\n   183\t    def test_platform_tree_has_only_sadr_0001_through_0019(self) -> None:\n   184\t        adr_dir = REPO_ROOT / \"docs\" / \"adr\"\n   185\t        sadrs = sorted(adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\"))\n   186\t        self.assertEqual(\n   187\t            {path.name.split(\"-\", 2)[1] for path in sadrs},\n   188\t            {f\"{number:04d}\" for number in range(1, 20)},\n   189\t        )\n   190\t        self.assertEqual(\n   191\t            list(adr_dir.glob(\"[0-9][0-9][0-9][0-9]-*.md\")),\n   192\t            [],\n   193\t            \"the Shiki platform tree must not retain legacy numeric SADR paths\",\n   194\t        )\n   195\t\n   196\t\n   197\tclass FixtureAdrReferences(unittest.TestCase):\n   198\t    \"\"\"Prove the check fails on a broken citation -- not merely passes today.\"\"\"\n   199\t\n   200\t    def _make_repo(self, adr_names: Sequence[str], script_body: str) -> Path:\n   201\t        tmp = tempfile.mkdtemp()\n   202\t        self.addCleanup(shutil.rmtree, tmp, ignore_errors=True)\n   203\t        root = Path(tmp)\n   204\t        (root / \"scripts\").mkdir()\n   205\t        (root / \"docs\" / \"adr\").mkdir(parents=True)\n   206\t        for name in adr_names:\n   207\t            (root / \"docs\" / \"adr\" / name).write_text(\"# stub\\n\", encoding=\"utf-8\")\n   208\t        (root / \"scripts\" / \"sample.py\").write_text(script_body, encoding=\"utf-8\")\n   209\t        return root\n   210\t\n   211\t    def test_fails_when_citation_has_no_document(self) -> None:\n   212\t        # A script cites ADR 9991, but no docs/adr/9991-*.md exists.\n   213\t        root = self._make_repo(\n   214\t            adr_names=[\"0001-real.md\"],\n   215\t            script_body='\"\"\"See ADR 9991 for the rule.\"\"\"\\n',\n   216\t        )\n   217\t        violations = find_adr_reference_violations(root, include_docs=False)\n   218\t        self.assertEqual(len(violations), 1)\n   219\t        found = violations[0]\n   220\t        self.assertEqual(found.number, \"9991\")\n   221\t        self.assertEqual(found.reason, \"missing\")\n   222\t        message = found.describe()\n   223\t        self.assertIn(\"scripts/sample.py\", message)  # citing file\n   224\t        self.assertIn(\"9991\", message)  # cited number\n   225\t        self.assertIn(\"docs/adr/9991-*.md\", message)  # missing path\n   226\t\n   227\t    def test_target_adr_does_not_satisfy_missing_platform_sadr(self) -> None:\n   228\t        root = self._make_repo(\n   229\t            adr_names=[\"0042-target-decision.md\"],\n   230\t            script_body='\"\"\"Target ADR 0042 exists, but platform SADR-0042 does not.\"\"\"\\n',\n   231\t        )\n   232\t        violations = find_adr_reference_violations(root, include_docs=False)\n   233\t        self.assertEqual(len(violations), 1)\n   234\t        self.assertEqual(violations[0].namespace, \"SADR\")\n   235\t        self.assertEqual(violations[0].number, \"0042\")\n   236\t        self.assertEqual(violations[0].expected_path, \"docs/adr/SADR-0042-*.md\")\n   237\t\n   238\t    def test_target_adr_and_platform_sadr_may_reuse_a_number(self) -> None:\n   239\t        root = self._make_repo(\n   240\t            adr_names=[\"0042-target-decision.md\", \"SADR-0042-platform-decision.md\"],\n   241\t            script_body='\"\"\"Target ADR 0042 and platform SADR-0042 are distinct.\"\"\"\\n',\n   242\t        )\n   243\t        self.assertEqual(\n   244\t            find_adr_reference_violations(root, include_docs=False), []\n   245\t        )\n   246\t\n   247\t    def test_uncited_adr_is_allowed(self) -> None:\n   248\t        # An ADR exists but nothing cites it -> not a violation. The converse\n   249\t        # (every ADR must be cited) is explicitly NOT required.\n   250\t        root = self._make_repo(\n   251\t            adr_names=[\"0001-real.md\", \"0042-never-cited.md\"],\n   252\t            script_body='\"\"\"Only mentions ADR 0001 here.\"\"\"\\n',\n   253\t        )\n   254\t        self.assertEqual(\n   255\t            find_adr_reference_violations(root, include_docs=False), []\n   256\t        )\n   257\t\n   258\t    def test_ambiguous_when_two_documents_share_a_number(self) -> None:\n   259\t        # \"Exactly one\" -- two records claiming the same number is a violation.\n   260\t        root = self._make_repo(\n   261\t            adr_names=[\"0007-a.md\", \"0007-b.md\"],\n   262\t            script_body='\"\"\"Cites ADR 0007 once.\"\"\"\\n',\n   263\t        )\n   264\t        violations = find_adr_reference_violations(root, include_docs=False)\n   265\t        self.assertEqual(len(violations), 1)\n\ndiff --git a/.codex/skills/shiki/SKILL.md b/.codex/skills/shiki/SKILL.md\nindex c8e2e85d9d75d49cc4f2d6286ee22659c4e53e75..93cded623b2c15d9f65c76491a1c2d813ac9e6c0\n--- a/.codex/skills/shiki/SKILL.md\n+++ b/.codex/skills/shiki/SKILL.md\n@@ -18,6 +18,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n Codex CLI does not register this skill as a custom `/shiki` slash command. When\n the user types `/shiki` in Codex CLI and gets \"Unrecognized command\", explain\n that this is expected: invoke Shiki in Codex with natural language such as\n@@ -69,7 +72,7 @@\n \n ## Responsibilities\n \n-- Claude Code implements and repairs by default (`shiki runner claude`, ADR 0008); Codex implements and repairs tasks explicitly assigned to `codex`.\n+- Claude Code implements and repairs by default (`shiki runner claude`, SADR-0008); Codex implements and repairs tasks explicitly assigned to `codex`.\n - Claude Code Action can act as GitHub-side CCA or reviewer.\n - CCA judges completion.\n - MergeGate authorizes state transitions and merge readiness.\ndiff --git a/.github/prompts/cca-completion-check.md b/.github/prompts/cca-completion-check.md\nindex ed203ad5f302ecc81372d8ab27d2b1232ca97497..fd89950528333ef6242e6d5443904f5392ab7e56\n--- a/.github/prompts/cca-completion-check.md\n+++ b/.github/prompts/cca-completion-check.md\n@@ -19,6 +19,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n ## Required Reading\n \n Read only the files needed to judge the PR contract. Prefer this order:\ndiff --git a/.github/prompts/codex-task-handoff.md b/.github/prompts/codex-task-handoff.md\nindex 695e211d76dea1c60e8d5bb360e78487808286a4..59441d576b01a39569722f226c854e5a740e9eb5\n--- a/.github/prompts/codex-task-handoff.md\n+++ b/.github/prompts/codex-task-handoff.md\n@@ -2,7 +2,7 @@\n \n You are Codex Front acting as the Implementer for a Shiki task.\n \n-Follow `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant ADRs, and `docs/agents/codex-handoff.md`.\n+Follow `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant Shiki SADRs and target ADRs, and `docs/agents/codex-handoff.md`.\n \n Rules:\n \ndiff --git a/.shiki/ledger/L-20260807T082800000000Z-c0de0019.json b/.shiki/ledger/L-20260807T082800000000Z-c0de0019.json\nnew file mode 100644\nindex 0000000000000000000000000000000000000000..bac355ecea44037859b3715f58bc32f3353cfcfa\n--- /dev/null\n+++ b/.shiki/ledger/L-20260807T082800000000Z-c0de0019.json\n@@ -0,0 +1,34 @@\n+{\n+  \"actor\": \"codex\",\n+  \"evidence\": [\n+    \"Full task diff reviewed against scope, non-goals, locks, acceptance checks, failure atomicity, path ownership, and compatibility requirements\",\n+    \"Finding repaired: tests/test_sadr_install_migration.py now asserts every fresh-installed SADR path has the matching SHA-256 digest in .shiki/install-stamp.json\",\n+    \"Re-review found no remaining blocking issues; rejected findings: none\",\n+    \"python3 -m unittest discover -s tests: 1147 tests passed\",\n+    \"bash scripts/test_shiki_control_plane.sh: passed\",\n+    \"bash scripts/test_shiki_adversarial_state.sh: passed\",\n+    \"python3 scripts/validate_shiki.py --target .: passed\"\n+  ],\n+  \"goal_id\": \"G-20260807T073307219752Z-1ee1ed68\",\n+  \"id\": \"L-20260807T082800000000Z-c0de0019\",\n+  \"links\": [],\n+  \"review\": {\n+    \"applied_fixes\": [\n+      \"Added direct fresh-install stamp path/digest assertions and reran focused plus full verification\"\n+    ],\n+    \"findings\": [\n+      {\n+        \"detail\": \"The fresh-install test proved SADR files were installed but did not directly prove their paths and digests were recorded in the refreshed install stamp.\",\n+        \"resolution\": \"fixed_and_re_reviewed\",\n+        \"severity\": \"blocking\",\n+        \"title\": \"Fresh-install stamp evidence was indirect\"\n+      }\n+    ],\n+    \"rejected_findings\": [],\n+    \"verdict\": \"clean_after_repairs\"\n+  },\n+  \"summary\": \"code-review implementer verdict CLEAN after re-review: findings=1; applied_fixes=1 (fresh-install stamp-digest assertion); rejected_findings=0 (none)\",\n+  \"task_id\": \"T-20260807T073307221254Z-2d22d654\",\n+  \"timestamp\": \"2026-08-07T08:28:00+00:00\",\n+  \"type\": \"review\"\n+}\ndiff --git a/.shiki/tasks/T-20260807T073307221254Z-2d22d654.json b/.shiki/tasks/T-20260807T073307221254Z-2d22d654.json\nindex e7e150cc1637f1cfac2199e04055e93b53630dcb..78f7fb5ef2ab3dad7e3731673ac2c34b2a8ed149\n--- a/.shiki/tasks/T-20260807T073307221254Z-2d22d654.json\n+++ b/.shiki/tasks/T-20260807T073307221254Z-2d22d654.json\n@@ -36,7 +36,8 @@\n     \"L-20260807T080107948254Z-61f24da0\",\n     \"L-20260807T080228312210Z-07ac626e\",\n     \"L-20260807T080228575038Z-d574f629\",\n-    \"L-20260807T080256330734Z-5bf40cb6\"\n+    \"L-20260807T080256330734Z-5bf40cb6\",\n+    \"L-20260807T082800000000Z-c0de0019\"\n   ],\n   \"locks\": [\n     \"path:.codex/**\",\ndiff --git a/AGENTS.md b/AGENTS.md\nindex 7f041100d49c2a7525f51cb0f5d696a4a6d1c78c..a06bfbabc6fa48a007389e25e12d7ed523936bed\n--- a/AGENTS.md\n+++ b/AGENTS.md\n@@ -22,7 +22,7 @@\n - **Codex Front** is an optional implementer through Codex App, Codex CLI, Codex IDE extension, or Codex Web signed in with ChatGPT OAuth/subscription auth, dispatched with `shiki runner codex` when a task is explicitly assigned to `codex`.\n - **GitHub CCA** is implemented by Claude Code Action by default, using `CLAUDE_CODE_OAUTH_TOKEN`.\n \n-Do not assume `openai/codex-action`, `OPENAI_API_KEY`, or API-key based Codex automation in the default Shiki path. API-key runners are explicit target-repo extensions and require their own ADR.\n+Do not assume `openai/codex-action`, `OPENAI_API_KEY`, or API-key based Codex automation in the default Shiki path. API-key runners are explicit target-repo extensions and require their own target ADR.\n \n Shiki is not:\n \n@@ -44,10 +44,14 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use\n+`SADR-NNNN-*.md`; target product decisions retain `NNNN-*.md` and the `ADR`\n+namespace.\n+\n Use the canonical source-of-truth order above. Runtime-specific reading orders\n are operational guidance only and must not override or reorder it.\n \n-Conversation state is not durable truth. If a decision matters, put it in GitHub, `.shiki/`, `CONTEXT.md`, or an ADR.\n+Conversation state is not durable truth. If a decision matters, put it in GitHub, `.shiki/`, `CONTEXT.md`, a Shiki SADR, or a target ADR.\n \n If GitHub and `.shiki/` disagree, surface the conflict and prefer GitHub operational state until the mirror is repaired.\n \n@@ -75,8 +79,8 @@\n Every non-trivial change follows this loop:\n \n 1. **Goal**: clarify outcome, completion conditions, non-goals, risk level, and success signals.\n-2. **grill-with-docs**: challenge the plan against domain docs, ADRs, code reality, terminology, and edge scenarios.\n-3. **Context & Impact**: find relevant docs, ADRs, code, dependencies, owners, locks, and verification surfaces.\n+2. **grill-with-docs**: challenge the plan against domain docs, Shiki SADRs, target ADRs, code reality, terminology, and edge scenarios.\n+3. **Context & Impact**: find relevant docs, SADRs, target ADRs, code, dependencies, owners, locks, and verification surfaces.\n 4. **PRD and Spec Freeze**: use `to-prd` when context is settled enough to become durable product/engineering intent. The operator's explicit approval of the PRD is Spec Freeze: the plan records a `spec_freeze` block, and no plan runs without it. Steps 1-4 form Requirements Definition \u2014 one continuous operator dialogue. After Spec Freeze, scope changes require an operator-approved Spec Amendment; non-scope-moving interpretations go to the Assumption Log.\n 5. **Issues**: use `to-issues` to create independently grabbable vertical-slice issues. Prefer AFK slices over HITL slices where possible.\n 6. **Triage**: label issues for readiness, risk, runtime, skills, and MergeGate state.\n@@ -167,7 +171,7 @@\n - Testing decisions.\n - Out-of-scope items.\n - Further notes.\n-- Links to relevant domain terms and ADRs.\n+- Links to relevant domain terms, SADRs, and target ADRs.\n \n Every executable task must include:\n \n@@ -193,7 +197,7 @@\n \n Context & Impact must identify:\n \n-- Relevant documents, domain terms, ADRs, and past decisions.\n+- Relevant documents, domain terms, SADRs, target ADRs, and past decisions.\n - Relevant modules, interfaces, seams, callers, owners, and tests.\n - Dependency relationships between tasks.\n - Candidate locks and lock conflicts.\n@@ -210,7 +214,7 @@\n | Skill | Required when |\n | --- | --- |\n | `setup-matt-pocock-skills` | First configuring the repo for skills, or when issue tracker, triage labels, or domain docs are missing. |\n-| `grill-with-docs` | Default for non-trivial Goals before PRD/issues; always required when plans, terminology, boundaries, tradeoffs, or ADR-worthy decisions are ambiguous. |\n+| `grill-with-docs` | Default for non-trivial Goals before PRD/issues; always required when plans, terminology, boundaries, tradeoffs, or decision-record-worthy choices are ambiguous. |\n | `zoom-out` | The agent lacks an architectural map of the relevant code area. |\n | `to-prd` | Settled Goal context must become durable product/engineering intent. |\n | `to-issues` | A Goal, PRD, or plan must become independently grabbable vertical-slice issues. |\n@@ -348,7 +352,7 @@\n - Three or more new files.\n - Any high-risk or critical label.\n \n-When the architecture gate triggers, run `grill-with-docs` or `improve-codebase-architecture` as appropriate and record durable decisions in an ADR when needed.\n+When the architecture gate triggers, run `grill-with-docs` or `improve-codebase-architecture` as appropriate and record durable decisions in a Shiki SADR or target ADR as applicable.\n \n ## Engineering Discipline\n \ndiff --git a/CLAUDE.md b/CLAUDE.md\nindex 98a932e7841a632866fcd53b0bad0e3af3711d3d..98a31399404d6fb044b4fd493f4459ac87f439de\n--- a/CLAUDE.md\n+++ b/CLAUDE.md\n@@ -14,6 +14,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n ## Claude Role\n \n Default to planner, default implementer, reviewer, coordinator, documentation editor, CCA-style completion judge, and final-judgment assistant.\n@@ -57,7 +60,7 @@\n At the start of a Shiki session:\n \n 1. Read `AGENTS.md`.\n-2. Read `CONTEXT.md` and relevant ADRs.\n+2. Read `CONTEXT.md` and relevant Shiki SADRs and target ADRs.\n 3. Read active `.shiki/` Goal/task/ledger state when present.\n 4. Read active distilled rules from `.shiki/memories/` (status `distilled`, `active: true`, not revoked or superseded) for applicable guidance; the same rules are injected deterministically into task handoffs as the `## Distilled Rules` section (\u00a73.5).\n 5. Read the GitHub Issue or PR when the task is GitHub-backed.\n@@ -74,7 +77,7 @@\n - Define the Goal, completion conditions, non-goals, and risk level.\n - Treat `grill-with-docs` as the default planning entry point for non-trivial Goals.\n - Challenge domain terms against `CONTEXT.md`.\n-- Challenge hard-to-reverse tradeoffs against ADRs.\n+- Challenge hard-to-reverse tradeoffs against SADRs and target ADRs.\n - Explore code instead of asking when code can answer the question.\n - Run Context & Impact before implementation planning.\n - Produce a Task DAG, not an unordered checklist.\n@@ -94,7 +97,7 @@\n - For each question, provide your recommended answer.\n - Resolve dependency decisions before downstream design questions.\n - Update `CONTEXT.md` inline when terminology crystallizes.\n-- Offer ADRs only when the decision is hard to reverse, surprising without context, and a real tradeoff.\n+- Offer a SADR or target ADR only when the decision is hard to reverse, surprising without context, and a real tradeoff.\n - Convert unresolved questions into HITL blockers rather than allowing Codex to guess.\n \n ## PRD and Issues Standard\n@@ -103,7 +106,7 @@\n \n - Use the project's domain glossary vocabulary.\n - Avoid volatile file paths unless they are essential.\n-- Include implementation decisions, testing decisions, out-of-scope items, and relevant ADR/domain links.\n+- Include implementation decisions, testing decisions, out-of-scope items, and relevant SADR/target-ADR/domain links.\n - Identify deep-module opportunities and testable interfaces.\n \n When creating issues:\n@@ -121,7 +124,7 @@\n - Branch/worktree target.\n - Scope and non-goals.\n - Dependencies and locks.\n-- Relevant docs, ADRs, modules, and tests.\n+- Relevant docs, SADRs, target ADRs, modules, and tests.\n - Required skill invocations.\n - TDD expectations.\n - Pre-PR `code-review` gate expectations.\n@@ -152,7 +155,7 @@\n \n - Treat yourself as a judge, not implementer.\n - Do not write production code.\n-- Read `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant ADRs, PRD, task issue, PR body, diff, checks, reviews, labels, and `.shiki/` state.\n+- Read `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant SADRs and target ADRs, PRD, task issue, PR body, diff, checks, reviews, labels, and `.shiki/` state.\n - Evaluate every applicable checklist in `docs/agents/checklists.md`.\n - Map each acceptance criterion to durable evidence.\n - Distinguish implementation failure from missing evidence.\ndiff --git a/CONTEXT.md b/CONTEXT.md\nindex e61a9be06a0cce0d026484083a657bde2067e234..237ee8aa3ea8fab334ece49768bc5ed3b5af0974\n--- a/CONTEXT.md\n+++ b/CONTEXT.md\n@@ -25,7 +25,7 @@\n _Avoid_: jumping directly into code\n \n **grill-with-docs**:\n-A planning skill that challenges a plan against domain language, ADRs, code reality, and concrete edge scenarios. It resolves design-tree questions before PRD/issues. It runs inside Requirements Definition.\n+A planning skill that challenges a plan against domain language, Shiki SADRs, target ADRs, code reality, and concrete edge scenarios. It resolves design-tree questions before PRD/issues. It runs inside Requirements Definition.\n _Avoid_: generic brainstorming, silent assumptions\n \n **Requirements Definition**:\ndiff --git a/CONTRIBUTING.md b/CONTRIBUTING.md\nindex c6d9b656e73abb8a55e5f660123e03153dcb3e6d..9e37a8fbf668db5b22461aaae5548b8f1354394c\n--- a/CONTRIBUTING.md\n+++ b/CONTRIBUTING.md\n@@ -17,7 +17,7 @@\n 1. **Goal** \u2014 start from a user-approved Goal with an outcome, completion\n    conditions, non-goals, risk level, and success signals.\n 2. **grill-with-docs** \u2014 for any non-trivial change, challenge the plan against\n-   domain language, ADRs, and code reality before writing a PRD.\n+   domain language, Shiki SADRs, target ADRs, and code reality before writing a PRD.\n 3. **PRD** \u2014 capture settled product/engineering intent (`to-prd`).\n 4. **Spec Freeze** \u2014 the operator explicitly approves the PRD/requirements;\n    the plan records a `spec_freeze` block and no plan runs without it\n@@ -94,7 +94,7 @@\n   entries.\n - Do not hand-edit `.shiki/goals/*.json` or `.shiki/manifest.json` in a task PR.\n - Conversation state is not durable truth. If a decision matters, record it in\n-  GitHub, `.shiki/`, `CONTEXT.md`, or an ADR.\n+  GitHub, `.shiki/`, `CONTEXT.md`, a Shiki SADR, or a target ADR.\n \n ## Architecture Gate\n \n@@ -103,7 +103,7 @@\n or CI policy, package structure, three or more structural files, more than 100\n net new lines in a core module, three or more new files, or any high-risk label.\n When triggered, run `grill-with-docs` or `improve-codebase-architecture` and\n-record durable decisions in an ADR when needed (see `docs/adr/`).\n+record durable decisions in a Shiki SADR or target ADR as applicable (see `docs/adr/`).\n \n ## Safety\n \ndiff --git a/SYSTEM_PROMPT.md b/SYSTEM_PROMPT.md\nindex aaaea45e3a896c3a44ba89eec8bb84325ecf6c62..da009c86a88285269dee0155e58e550c6d744ea9\n--- a/SYSTEM_PROMPT.md\n+++ b/SYSTEM_PROMPT.md\n@@ -30,6 +30,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n Use the canonical source-of-truth order above. Runtime-specific reading orders\n are operational guidance only and must not override or reorder it. The current\n conversation is non-durable operator input.\n@@ -48,14 +51,14 @@\n \n A runtime may only do work that is explicitly assigned by the Goal, task, PR, or MergeGate decision.\n \n-Do not assume `openai/codex-action`, `OPENAI_API_KEY`, or API-key based Codex automation in the default Shiki path. API-key runners are explicit target-repo extensions and require their own ADR.\n+Do not assume `openai/codex-action`, `OPENAI_API_KEY`, or API-key based Codex automation in the default Shiki path. API-key runners are explicit target-repo extensions and require their own target ADR.\n \n ## 4. Non-negotiable Shiki flow\n \n For any non-trivial Goal, use this flow:\n \n 1. **Goal Seek**: define outcome, non-goals, risk level, completion criteria, and evidence requirements.\n-2. **grill-with-docs**: challenge the plan against `CONTEXT.md`, ADRs, code reality, terminology, boundaries, and edge scenarios. Ask one question at a time when the operator is available. If code can answer a question, inspect code instead of asking.\n+2. **grill-with-docs**: challenge the plan against `CONTEXT.md`, Shiki SADRs, target ADRs, code reality, terminology, boundaries, and edge scenarios. Ask one question at a time when the operator is available. If code can answer a question, inspect code instead of asking.\n 3. **Context & Impact**: identify modules, interfaces, seams, callers, dependencies, locks, risk, and verification surfaces.\n 4. **to-prd + Spec Freeze**: turn settled context into a PRD using domain vocabulary and testing decisions. The operator's explicit approval of the PRD is Spec Freeze, recorded as the plan's `spec_freeze` block; plans without it do not run. Steps 1-4 form Requirements Definition.\n 5. **to-issues**: decompose the PRD into independently grabbable vertical-slice issues. Prefer AFK slices; mark HITL slices when judgment is still required.\n@@ -75,7 +78,7 @@\n Before making any material change:\n \n 1. Read `AGENTS.md` and any runtime-specific wrapper such as `CLAUDE.md`.\n-2. Read `CONTEXT.md` and relevant ADRs.\n+2. Read `CONTEXT.md` and relevant SADRs and target ADRs.\n 3. Read the GitHub Issue / PR and `.shiki/` state for the active Goal or task.\n 4. Identify your role: Planner, Implementer, Reviewer, Completion Check Agent, Repairer, or Guardian-assist.\n 5. Identify Goal id, task id, branch, PR, dependencies, locks, risk level, required skills, and required evidence.\n@@ -103,7 +106,7 @@\n \n At minimum, identify:\n \n-- Relevant documents, ADRs, domain terms, and prior decisions.\n+- Relevant documents, SADRs, target ADRs, domain terms, and prior decisions.\n - Relevant modules, interfaces, seams, callers, owners, and tests.\n - Dependency relationships between tasks.\n - Candidate file locks and lock conflicts.\n@@ -152,7 +155,7 @@\n \n CCA must read:\n \n-- `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant ADRs.\n+- `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant SADRs and target ADRs.\n - Parent Goal issue, PRD issue, task issue, comments, and labels.\n - PR body, diff, commits, changed files, reviews, CI checks, and artifacts available to the workflow.\n - `.shiki/` task contract, locks, ledger entries, CCA checklist profile, and prior repair packets.\n@@ -214,7 +217,7 @@\n - Three or more new files.\n - Any high-risk or critical label.\n \n-When the architecture gate triggers, run `grill-with-docs` or `improve-codebase-architecture` as appropriate and record the decision in an ADR if it is hard to reverse or likely to be re-litigated.\n+When the architecture gate triggers, run `grill-with-docs` or `improve-codebase-architecture` as appropriate and record the decision in a Shiki SADR or target ADR as applicable if it is hard to reverse or likely to be re-litigated.\n \n ## 14. Skill Gate\n \n@@ -223,7 +226,7 @@\n Use these skills:\n \n - `setup-matt-pocock-skills`: before first use of the engineering skill set, or when issue tracker, triage labels, or domain docs are missing.\n-- `grill-with-docs`: default for non-trivial Goals before PRD/issues; required for ambiguous plans, terminology, boundaries, ADR-worthy tradeoffs, or design-tree decisions.\n+- `grill-with-docs`: default for non-trivial Goals before PRD/issues; required for ambiguous plans, terminology, boundaries, decision-record-worthy tradeoffs, or design-tree decisions.\n - `zoom-out`: unfamiliar code area or missing architectural map.\n - `to-prd`: convert settled context into a PRD.\n - `to-issues`: break a PRD, plan, or Goal into independently grabbable vertical-slice issues.\ndiff --git a/docs/adr/0001-keep-shiki-as-platform-name.md b/docs/adr/SADR-0001-keep-shiki-as-platform-name.md\nindex 20a9637c3b9d8c97de7ba8db7cfae4d82d9a7dba..8b685439959aada1b221c112f0ac3c51ca8a1695\n--- a/docs/adr/0001-keep-shiki-as-platform-name.md\n+++ b/docs/adr/SADR-0001-keep-shiki-as-platform-name.md\n@@ -1,3 +1,3 @@\n-# Keep Shiki As The Platform Name\n+# SADR-0001: Keep Shiki As The Platform Name\n \n Shiki remains the product name for the GitHub-native agentic engineering control plane. We considered more descriptive names such as MergePilot, MergeGate, and Kiroku, but kept Shiki because it carries the existing goal-seeking framework identity while remaining independent of Hermes, Codex, Claude Code, or any single runtime.\ndiff --git a/docs/adr/0002-use-agents-as-shared-constitution.md b/docs/adr/SADR-0002-use-agents-as-shared-constitution.md\nindex 73de30ecb078120eac540d9bc51855e3cd48d0e3..c5486182bf06fca36ea508496b013c67302fd989\n--- a/docs/adr/0002-use-agents-as-shared-constitution.md\n+++ b/docs/adr/SADR-0002-use-agents-as-shared-constitution.md\n@@ -1,3 +1,3 @@\n-# Use AGENTS As The Shared Agent Constitution\n+# SADR-0002: Use AGENTS As The Shared Agent Constitution\n \n Shiki uses `AGENTS.md` as the runtime-neutral constitution and `CLAUDE.md` as a thin Claude Code wrapper. This keeps Codex, Claude Code, Hermes Runner, and future Agent Runtimes aligned on the same GitHub-first MergeGate rules while still allowing runtime-specific instructions where needed.\ndiff --git a/docs/adr/0003-require-skill-gate-for-mergegate-readiness.md b/docs/adr/SADR-0003-require-skill-gate-for-mergegate-readiness.md\nindex 424be0c9af4c8f0136f1fde68a468c47ff84e764..7fc337987c7df9792f75f181dfcbed75fe5ffff2\n--- a/docs/adr/0003-require-skill-gate-for-mergegate-readiness.md\n+++ b/docs/adr/SADR-0003-require-skill-gate-for-mergegate-readiness.md\n@@ -1,3 +1,3 @@\n-# Require Skill Gate For MergeGate Readiness\n+# SADR-0003: Require Skill Gate For MergeGate Readiness\n \n Shiki requires relevant engineering skills to be invoked before execution when their triggers apply. This makes skills part of MergeGate readiness rather than optional prompt style, because Shiki depends on repeatable planning, issue decomposition, TDD, diagnosis, triage, and architecture review before autonomous or parallel agent work can be trusted.\ndiff --git a/docs/adr/0004-use-github-first-with-shiki-mirror.md b/docs/adr/SADR-0004-use-github-first-with-shiki-mirror.md\nindex d0a3a9174dc646a945a9d325954ffaf00c352040..a79c54ae9ee8eed2a0524192b864da4e22a92d3f\n--- a/docs/adr/0004-use-github-first-with-shiki-mirror.md\n+++ b/docs/adr/SADR-0004-use-github-first-with-shiki-mirror.md\n@@ -1,4 +1,4 @@\n-# ADR 0004: Use GitHub First With A Repository-Local Shiki Mirror\n+# SADR-0004: Use GitHub First With A Repository-Local Shiki Mirror\n \n ## Status\n \ndiff --git a/docs/adr/0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md b/docs/adr/SADR-0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\nindex b339baf49b89d9290e79c3d51b36e7b2bde3bd95..0f3c3eb9afccdb3403ce978e21d04fe12e495da0\n--- a/docs/adr/0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\n+++ b/docs/adr/SADR-0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\n@@ -1,8 +1,8 @@\n-# ADR 0005: Use Codex As Front And Claude Code Action As The GitHub Runtime\n+# SADR-0005: Use Codex As Front And Claude Code Action As The GitHub Runtime\n \n ## Status\n \n-Accepted (implementer default superseded by ADR 0008; the auth model and the\n+Accepted (implementer default superseded by SADR-0008; the auth model and the\n Claude Code Action GitHub runtime decision remain in force)\n \n ## Context\n@@ -26,4 +26,4 @@\n - Shiki avoids requiring `OPENAI_API_KEY` for the default workflow.\n - Codex work happens through the user's authenticated Codex front surface, not as a hidden backend Action.\n - Claude Code Action is configured for OAuth-token based subscription usage by default.\n-- API-key based automation remains possible only as an explicit target-repo extension with its own ADR.\n+- API-key based automation remains possible only as an explicit target-repo extension with its own target ADR.\ndiff --git a/docs/adr/0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md b/docs/adr/SADR-0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\nindex 4bcae9d1e52f0a37b8e9fbafa5cd14b6ee1993bd..7a38538a6ae9929567f0ade67e7ea6a60d0b3f7e\n--- a/docs/adr/0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\n+++ b/docs/adr/SADR-0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\n@@ -1,4 +1,4 @@\n-# ADR 0006: Separate Codex Implementation, CCA Completion, And MergeGate Authorization\n+# SADR-0006: Separate Codex Implementation, CCA Completion, And MergeGate Authorization\n \n ## Status\n \ndiff --git a/docs/adr/0007-packaging-and-release.md b/docs/adr/SADR-0007-packaging-and-release.md\nindex 79b84ddb97167372b44641e5b7860e400ce80822..bb445a2e2e856d9d1f5fdbe35de8d4e8cec0fb32\n--- a/docs/adr/0007-packaging-and-release.md\n+++ b/docs/adr/SADR-0007-packaging-and-release.md\n@@ -1,4 +1,4 @@\n-# ADR 0007: Packaging And Release For Shiki\n+# SADR-0007: Packaging And Release For Shiki\n \n ## Status\n \n@@ -25,7 +25,7 @@\n   not introduce third-party runtime dependencies or break the shim.\n - Releases must run inside the operator's subscription-authenticated toolchain\n   and must not require extra secrets beyond the built-in `GITHUB_TOKEN`\n-  (consistent with ADR 0005's no-API-key-by-default stance).\n+  (consistent with SADR-0005's no-API-key-by-default stance).\n - `.shiki/manifest.json` already encodes the install surface (`install.include`,\n   `install.create_directories`, `install.exclude_from_commit`), and it must not\n   be mutated as part of this change.\n@@ -34,7 +34,7 @@\n \n This is a hard-to-reverse platform decision (it sets the version source of\n truth, the release trigger, and the public install contract), so it is recorded\n-as an ADR.\n+as a SADR.\n \n ## Decision\n \n@@ -104,7 +104,7 @@\n 3. **Publishing to PyPI.** Rejected for now: Shiki is distributed as a\n    repository template + git-installable CLI, not a PyPI library; PyPI would add\n    credentials/secrets and a different versioning contract. Can be added later as\n-   its own ADR if a packaged distribution is needed.\n+   its own SADR if a packaged distribution is needed.\n 4. **Dynamic version (read `VERSION` via a build backend `dynamic` field).**\n    Rejected to keep `pyproject.toml` backend-agnostic and dependency-free; the\n    small duplication between `VERSION` and `pyproject.toml` is guarded by the\n@@ -123,4 +123,4 @@\n   releasing versions the whole platform but ships only `install.include` to\n   targets.\n - Future PyPI distribution or a console entry point are deferred and would each\n-  warrant their own ADR.\n+  warrant their own SADR.\ndiff --git a/docs/adr/0008-use-claude-code-as-default-implementer.md b/docs/adr/SADR-0008-use-claude-code-as-default-implementer.md\nindex dedf4510b7c7799521422f622029563d373db031..35c2625d908167fbc58855b5b63e283805f167e8\n--- a/docs/adr/0008-use-claude-code-as-default-implementer.md\n+++ b/docs/adr/SADR-0008-use-claude-code-as-default-implementer.md\n@@ -1,12 +1,12 @@\n-# ADR 0008: Use Claude Code As The Default Implementer Runtime\n+# SADR-0008: Use Claude Code As The Default Implementer Runtime\n \n ## Status\n \n-Proposed (supersedes ADR 0005 for the implementer default; keeps its auth model)\n+Proposed (supersedes SADR-0005 for the implementer default; keeps its auth model)\n \n ## Context\n \n-ADR 0005 made Codex Front the default implementation runtime, with Claude Code\n+SADR-0005 made Codex Front the default implementation runtime, with Claude Code\n restricted to planner and reviewer roles by the runtime registry, and the only\n autonomous implementation adapter was `shiki runner codex`.\n \n@@ -18,13 +18,13 @@\n - The platform's planning, review, and CCA layers already run on Claude Code,\n   and Claude Code's native capabilities (the built-in `/code-review` skill and\n   the Workflow multi-agent orchestration tool) have no Codex equivalent.\n-- The Goal lifecycle redesign (ADR 0009) requires an autonomous post-freeze\n+- The Goal lifecycle redesign (SADR-0009) requires an autonomous post-freeze\n   loop driven from the operator's Claude Code session, including bounded\n   repair dispatch, which must not depend on a runtime that cannot be invoked\n   unattended.\n \n The constitution claims Shiki is runtime-agnostic (\"not a Claude-only\n-workflow\", ADR 0002). Any change to the implementer default must preserve the\n+workflow\", SADR-0002). Any change to the implementer default must preserve the\n registry-based runtime model rather than hardcoding one vendor.\n \n ## Decision\n@@ -39,7 +39,7 @@\n   the task to review or repair-needed.\n - Keep Codex registered as an optional implementer runtime. `shiki runner\n   codex`, the Codex handoff templates, and the ChatGPT OAuth auth model from\n-  ADR 0005 remain supported for tasks explicitly assigned to `codex`.\n+  SADR-0005 remain supported for tasks explicitly assigned to `codex`.\n - Commit to mandating two Claude-native capabilities as loop contract in\n   follow-up Goals (status: the code-review gate was delivered by Goal G-B \u2014\n   skill registry, checklists item PR-12, default required_skills \u2014 while the\n@@ -60,7 +60,7 @@\n \n Out of scope: removing Codex support, changing the CCA runtime (Claude Code\n Action with `CLAUDE_CODE_OAUTH_TOKEN` stays), and API-key based automation\n-(still requires its own ADR per ADR 0005).\n+(still requires its own target ADR per SADR-0005).\n \n ## Consequences\n \n@@ -100,7 +100,7 @@\n   harness rejection of direct `codex exec` is outside Shiki's control, and the\n   plan's mandatory native capabilities cannot run on Codex at all.\n - **Remove Codex entirely**: rejected; it abandons the runtime-agnostic\n-  constitution (ADR 0002) for no operational gain and forecloses\n+  constitution (SADR-0002) for no operational gain and forecloses\n   cross-runtime adversarial review.\n - **GitHub Actions cloud implementation via claude-code-action**: rejected as\n   default; durable evidence is automatic but token cost, Node 24 workflow\ndiff --git a/docs/adr/0009-requirements-definition-and-spec-freeze.md b/docs/adr/SADR-0009-requirements-definition-and-spec-freeze.md\nindex 29b3f7871bef9203a8b4a831a65c39eb2f506e42..52e7dd69ad0aa4efe0b68c62678abbf7df02aaec\n--- a/docs/adr/0009-requirements-definition-and-spec-freeze.md\n+++ b/docs/adr/SADR-0009-requirements-definition-and-spec-freeze.md\n@@ -1,4 +1,4 @@\n-# ADR 0009: Merge Grill And Planning Into Requirements Definition With Spec Freeze\n+# SADR-0009: Merge Grill And Planning Into Requirements Definition With Spec Freeze\n \n ## Status\n \ndiff --git a/docs/adr/0010-external-ai-guardian-review.md b/docs/adr/SADR-0010-external-ai-guardian-review.md\nindex e6108bbe8bb4e73e0ff4684c8f78001a80438c4e..f3510b145664298adafb151b6f0db51b972f01ec\n--- a/docs/adr/0010-external-ai-guardian-review.md\n+++ b/docs/adr/SADR-0010-external-ai-guardian-review.md\n@@ -1,4 +1,4 @@\n-# ADR 0010: External AI Guardian Review As A First-Class Approval Authority\n+# SADR-0010: External AI Guardian Review As A First-Class Approval Authority\n \n ## Status\n \n@@ -8,7 +8,7 @@\n \n Shiki is intended to be an autonomous, recursive, self-improving agent\n framework: authority-in-the-loop, not human-in-the-loop. Until now the Guardian\n-approval required for high/critical-risk merges (guardian-policy.json, ADR\n+approval required for high/critical-risk merges (guardian-policy.json, SADR\n governance) recognized only human authority sources \u2014 a GitHub review approval,\n the `guardian:approved` label applied by a configured Guardian, or a comment\n carrying `Guardian approval granted` plus the current head SHA.\n@@ -89,7 +89,7 @@\n   autonomous design the operator intends and produced the impersonation\n   incident.\n - **Record AI approval as an operator comment**: rejected as forgery of human\n-  authority \u2014 the exact failure this ADR corrects.\n+  authority \u2014 the exact failure this SADR corrects.\n - **Deliver the artifact as a committed `.shiki/` file**: rejected; a committed\n   approval inside the PR being approved is forgeable by anyone who can push.\n - **A dedicated reviewer bot identity instead of a relayed comment**: deferred;\ndiff --git a/docs/adr/0011-autonomous-quality-gate-evidence.md b/docs/adr/SADR-0011-autonomous-quality-gate-evidence.md\nindex bca0246c5d46e44afbde15486a75eec425fc02f5..9c93eac16d8c3a6d4b11bd7224e3955102cd7db4\n--- a/docs/adr/0011-autonomous-quality-gate-evidence.md\n+++ b/docs/adr/SADR-0011-autonomous-quality-gate-evidence.md\n@@ -1,4 +1,4 @@\n-# ADR 0011: Autonomous Quality-Gate Evidence \u2014 Deterministic Facts And An Independent Verifier\n+# SADR-0011: Autonomous Quality-Gate Evidence \u2014 Deterministic Facts And An Independent Verifier\n \n ## Status\n \ndiff --git a/docs/adr/0012-autonomous-loop-closeout-to-main.md b/docs/adr/SADR-0012-autonomous-loop-closeout-to-main.md\nindex 637fad6dde4989c9c2bf0f81f676d8d29991123c..ec7d85f81030a676bcbba28b43eb365422a36eac\n--- a/docs/adr/0012-autonomous-loop-closeout-to-main.md\n+++ b/docs/adr/SADR-0012-autonomous-loop-closeout-to-main.md\n@@ -1,4 +1,4 @@\n-# 0012 \u2014 Autonomous loop pushes goal completion to main via a closeout PR\n+# SADR-0012 \u2014 Autonomous loop pushes goal completion to main via a closeout PR\n \n - Status: accepted\n - Date: 2026-06-18\ndiff --git a/docs/adr/0013-bootstrap-grants-actions-approve-pull-requests.md b/docs/adr/SADR-0013-bootstrap-grants-actions-approve-pull-requests.md\nindex b6c69d64f1c5ced346b88d6859586715b3b5bd0e..cf48ff4a6716267f539ca58edd1a852e7c7a2d64\n--- a/docs/adr/0013-bootstrap-grants-actions-approve-pull-requests.md\n+++ b/docs/adr/SADR-0013-bootstrap-grants-actions-approve-pull-requests.md\n@@ -1,4 +1,4 @@\n-# ADR 0013: Bootstrap Grants GitHub Actions Permission To Create And Approve Pull Requests\n+# SADR-0013: Bootstrap Grants GitHub Actions Permission To Create And Approve Pull Requests\n \n ## Status\n \n@@ -31,7 +31,7 @@\n security tradeoff: it lets repository automation satisfy a human-review gate. In\n a multi-maintainer repo that could let a compromised or buggy workflow\n self-approve changes. So whether bootstrap turns it on by default \u2014 and with\n-what default token scope \u2014 is an ADR-worthy decision, not an implementation\n+what default token scope \u2014 is a SADR-worthy decision, not an implementation\n detail.\n \n ## Decision\n@@ -48,7 +48,7 @@\n Specifically:\n \n - **`can_approve_pull_request_reviews` defaults to `true`.** Shiki's default\n-  posture is solo/AFK autonomous operation (ADR 0008/0009/0012), where the CCA\n+  posture is solo/AFK autonomous operation (SADR-0008/0009/0012), where the CCA\n   Review Bridge is the only path that can satisfy `required_review: true` after\n   the CCA and Guardian gates have already passed. The default makes a\n   freshly-bootstrapped repo able to run the autonomous loop end to end without a\ndiff --git a/docs/adr/0014-codex-app-external-ai-guardian-ui-adapter.md b/docs/adr/SADR-0014-codex-app-external-ai-guardian-ui-adapter.md\nindex 238db06279334caa341c117362df9271382a7fcd..65f12e9939321e1b3e389a34c822fc1549e5155d\n--- a/docs/adr/0014-codex-app-external-ai-guardian-ui-adapter.md\n+++ b/docs/adr/SADR-0014-codex-app-external-ai-guardian-ui-adapter.md\n@@ -1,4 +1,4 @@\n-# ADR 0014: Use Codex App As The External AI Guardian UI Adapter\n+# SADR-0014: Use Codex App As The External AI Guardian UI Adapter\n \n ## Status\n \ndiff --git a/docs/adr/0015-contract-approval-before-dispatch.md b/docs/adr/SADR-0015-contract-approval-before-dispatch.md\nindex fad6c656741a20a7684ba16080a7db159f63a52f..6061271a46d3f1d6ae7174477de3fb75673123a6\n--- a/docs/adr/0015-contract-approval-before-dispatch.md\n+++ b/docs/adr/SADR-0015-contract-approval-before-dispatch.md\n@@ -1,4 +1,4 @@\n-# ADR 0015: Approve The Task Contract Before Dispatch, Not The Diff After It\n+# SADR-0015: Approve The Task Contract Before Dispatch, Not The Diff After It\n \n ## Status\n \n@@ -9,7 +9,7 @@\n Guardian approval for high/critical risk is expressible only on a pull request.\n Every approval source in `guardian-policy.json` \u2014 GitHub review, the\n `guardian:approved` label, the `Guardian approval granted` comment, and the\n-`external_ai_guardian_review` artifact (ADR 0010/0014) \u2014 is bound to a PR, and\n+`external_ai_guardian_review` artifact (SADR-0010/0014) \u2014 is bound to a PR, and\n three of the four require the current head SHA. Approval therefore cannot exist\n before an implementation PR exists.\n \n@@ -81,7 +81,7 @@\n An envelope breach must surface only on the non-repairable policy gate, never on\n the repairable `MergeGate metadata check`. Otherwise the Goal Loop routes it to\n `dispatch_repair` \u2014 instructing a runner to make the breach go away \u2014 which is\n-exactly what this ADR forbids.\n+exactly what this SADR forbids.\n \n **Contract immutability.** After registration, an implementation PR must not\n change the governance fields of its own task contract (`scope`, `non_goals`,\n@@ -116,7 +116,7 @@\n fails any verdict that is not `complete`, and the signal the CCA prompt reads is\n computed from `evaluate_guardian_approval` alone. Without the OR-merge at the\n signal, a contract-approved PR still returns `needs_guardian`, the CCA job fails,\n-and the MergeGate policy check never runs \u2014 the gate this ADR changes would never\n+and the MergeGate policy check never runs \u2014 the gate this SADR changes would never\n be reached. The CCA *prompt* needs no change: a `contract_approval` entry in the\n signal's existing `sources` array satisfies its current wording.\n \n@@ -130,7 +130,7 @@\n written, and an approved contract lets its implementation PRs merge through the\n same automatic path as low/medium risk. The wasted implement-then-block cycle\n disappears. Guardian review shifts from reading diffs to reading contracts, which\n-is both earlier and smaller; the external AI Guardian path (ADR 0010/0014) works\n+is both earlier and smaller; the external AI Guardian path (SADR-0010/0014) works\n on contracts with no change.\n \n We accept that the Guardian authorizes a class of change without seeing its\n@@ -146,7 +146,7 @@\n adversarial tests for the new approval path equivalent in rigor to\n `test_shiki_governance_evidence.sh`.\n \n-Bootstrap costs, which apply only while this ADR is being implemented. The tasks\n+Bootstrap costs, which apply only while this SADR is being implemented. The tasks\n that build Contract Approval merge under the existing rules, so the ones\n dispatched after the pre-dispatch gate lands but before the approval path lands\n need a Guardian-approved Contract PR *and* a live Guardian approval on their\n@@ -210,7 +210,7 @@\n risk; they do not bound authorized work.\n \n **Auto-escalate risk on envelope breach instead of stopping.** Returns the\n-workflow to a diff-time Guardian decision \u2014 the exact behavior this ADR removes.\n+workflow to a diff-time Guardian decision \u2014 the exact behavior this SADR removes.\n \n **Resolve governance fields from the base snapshot only.** Rejected against the\n registration flow: implementation PRs add their own contract, so the base has no\ndiff --git a/docs/adr/0016-locks-bound-product-paths.md b/docs/adr/SADR-0016-locks-bound-product-paths.md\nindex b5e850f3f1f7f4ef6e7f9d1763a29a51bd300c45..46b40a9b99a7aae644c02bb458f5473ff0a90ce2\n--- a/docs/adr/0016-locks-bound-product-paths.md\n+++ b/docs/adr/SADR-0016-locks-bound-product-paths.md\n@@ -1,4 +1,4 @@\n-# ADR 0016: Locks bound product paths; the Shiki mirror is bound by record identity\n+# SADR-0016: Locks bound product paths; the Shiki mirror is bound by record identity\n \n ## Status\n \n@@ -6,8 +6,8 @@\n \n - Date: 2026-07-28\n - Deciders: operator (mizutani-140), Claude (planner)\n-- Related: ADR 0004 (GitHub-first with a `.shiki` mirror), ADR 0009 (Spec Freeze),\n-  ADR 0012 (autonomous loop closeout to main), ADR 0015 (Contract Approval before\n+- Related: SADR-0004 (GitHub-first with a `.shiki` mirror), SADR-0009 (Spec Freeze),\n+  SADR-0012 (autonomous loop closeout to main), SADR-0015 (Contract Approval before\n   dispatch), remediation goal task R-02, the live PR #179 deadlock.\n \n ## Context\n@@ -90,11 +90,11 @@\n files, documentation files and named source modules are nameable; runtime-\n allocated ledger, runner and report ids are not \u2014 and those live in `.shiki`,\n which locks no longer govern. Existing frozen contracts are brought into line by\n-a one-time Spec Amendment per plan, which must land before ADR 0015's contract\n+a one-time Spec Amendment per plan, which must land before SADR-0015's contract\n immutability makes `locks` unwritable from the implementation PR.\n \n **Lock state is resolved from the durable side.** MergeGate resolves the lock\n-registry, and ADR 0015's governance fields, from the base `.shiki` snapshot when\n+registry, and SADR-0015's governance fields, from the base `.shiki` snapshot when\n present, falling back to head only when absent. The `MergeGate metadata check`\n job is given `--base-shiki`, which it does not receive today. Locks are not\n derived from the live open-pull-request set: a pull request body is untrusted\n@@ -108,7 +108,7 @@\n Explicitly out of scope: moving MergeGate's contract resolution off the PR\n checkout wholesale; making the mirror a projection of GitHub state; introducing\n new lock kinds; moving ledger evidence out of the repository; and unwinding\n-ADR 0012's closeout pull request. Registration of multi-task goals is ADR 0015's\n+SADR-0012's closeout pull request. Registration of multi-task goals is SADR-0015's\n Contract PR and is not re-decided here.\n \n ## Consequences\n@@ -127,7 +127,7 @@\n \n Risks accepted. First, goal-scoped identity is only as strong as `goal_id`,\n which is resolved from the PR body and the head task file; the mitigation is to\n-add `goal_id` to ADR 0015's frozen governance field set, and until that lands a\n+add `goal_id` to SADR-0015's frozen governance field set, and until that lands a\n task file can name a foreign goal. Second, base-when-present resolution is\n unavailable on fork pull requests, where the head is contributor-controlled;\n fork pull requests already cannot complete a Shiki merge, and this decision does\n@@ -139,7 +139,7 @@\n path, not by implementation pull requests.\n \n New obligations: one Spec Amendment per frozen plan to rewrite `locks`, ordered\n-before ADR 0015 T1; extension of the adversarial suites in\n+before SADR-0015 T1; extension of the adversarial suites in\n `scripts/test_shiki_adversarial_state.sh` and\n `scripts/test_shiki_governance_evidence.sh` to cover the eleven newly-scoped\n directories; and updating `scripts/test_shiki_loop_lock_guard.sh`,\n@@ -151,32 +151,32 @@\n whether the loop's transitive sync of ledger `evidence` references can be\n deleted once the Contract PR carries its own source plan.\n \n-## Relationship to ADR 0004\n+## Relationship to SADR-0004\n \n-This ADR **clarifies something ADR 0004 left unspecified**. ADR 0004 decides that\n+This SADR **clarifies something SADR-0004 left unspecified**. SADR-0004 decides that\n GitHub is the operational source of truth and that each target repository keeps a\n `.shiki/` mirror. It says nothing about pull requests writing the mirror, does\n not name MergeGate, and does not make the mirror an input to a gate. Nothing in\n-ADR 0004's Decision or Consequences is amended, superseded or weakened: the\n+SADR-0004's Decision or Consequences is amended, superseded or weakened: the\n mirror still exists, still records the same artifacts, and pull request\n-boundaries remain the coordination surface. What this ADR settles is the question\n+boundaries remain the coordination surface. What this SADR settles is the question\n 0004 never reached \u2014 by what authority a pull request may write a mirror record \u2014\n and it answers it the way `.shiki/manifest.json` already declares\n (`pr_mutation: current-task-or-current-goal-only`).\n \n-It **amends ADR 0012**, whose Decision item 5 states that \"Loop-executed tasks\n+It **amends SADR-0012**, whose Decision item 5 states that \"Loop-executed tasks\n declare the `path:.shiki/**` lock, which covers every `.shiki` file the closeout\n stages ... so `files_outside_locks` is satisfied without per-file lock\n bookkeeping.\" That clause is replaced: the closeout's mirror writes are\n-authorized by record identity, not by a declared lock. ADR 0012's substantive\n+authorized by record identity, not by a declared lock. SADR-0012's substantive\n decision \u2014 that completion reaches `main` through a closeout pull request rather\n than local attestation \u2014 is preserved unchanged.\n \n-It is **complementary to ADR 0015**, not a substitute. ADR 0015 owns contract\n+It is **complementary to SADR-0015**, not a substitute. SADR-0015 owns contract\n immutability, the pre-dispatch gate, the Contract PR and the Scope Envelope. This\n-ADR owns what a lock means and who may write the mirror. The one ordering\n+SADR owns what a lock means and who may write the mirror. The one ordering\n constraint between them is that the lock-narrowing Spec Amendment must precede\n-ADR 0015 T1's freezing of the `locks` field.\n+SADR-0015 T1's freezing of the `locks` field.\n \n ## Alternatives Considered\n \n@@ -186,7 +186,7 @@\n the only writer that can register them may run. Migrating the Guardian signal to\n that store makes every registration PR fail closed to `needs_guardian` at every\n risk level, because the task file is by definition absent from the store, which\n-removes the low/medium autonomous path ADR 0008/0009 exists to provide. And it\n+removes the low/medium autonomous path SADR-0008/0009 exists to provide. And it\n leaves schemas, manifest, config, guardian policy, templates, migrations, plans,\n worktrees, runner records, runs and handoffs with no writer at all in a\n repository whose product is `.shiki`. Measured benefit on the frozen backlog:\n@@ -199,14 +199,14 @@\n namespace is not in fact ownership-partitioned \u2014 56 ledger entries carry no\n `task_id`, and one goal DAG is writable by 35 registered tasks. The correct\n expression of its insight is that locks do not govern the mirror at all, which is\n-what this ADR decides.\n+what this SADR decides.\n \n **Make the mirror a projection written only by a registration writer and a\n post-merge projection job.** Rejected. It requires a bypass actor with write\n access to the protected default branch; it makes its own master invariant\n readable from a manifest that pull requests may edit; it deletes both reconcile\n modes and leaves a corrupted mirror with no in-band repair path, contradicting\n-ADR 0004's consequence that mirror conflicts are repaired against GitHub state;\n+SADR-0004's consequence that mirror conflicts are repaired against GitHub state;\n and its content-derived ids are rejected by `validate_shiki`'s currently-green id\n pattern.\n \ndiff --git a/docs/adr/0017-bookkeeping-closeouts-do-not-inherit-risk.md b/docs/adr/SADR-0017-bookkeeping-closeouts-do-not-inherit-risk.md\nindex 554e3a0d526a72ca067108c5464cc68c3edcf174..b9676857a4713850e063af7c39c8e0b01ecb4c66\n--- a/docs/adr/0017-bookkeeping-closeouts-do-not-inherit-risk.md\n+++ b/docs/adr/SADR-0017-bookkeeping-closeouts-do-not-inherit-risk.md\n@@ -1,4 +1,4 @@\n-# ADR 0017: A Bookkeeping Closeout Does Not Inherit Its Task's Risk\n+# SADR-0017: A Bookkeeping Closeout Does Not Inherit Its Task's Risk\n \n ## Status\n \n@@ -6,7 +6,7 @@\n \n ## Context\n \n-ADR 0012 made the goal loop push a task's completion to the default branch through a\n+SADR-0012 made the goal loop push a task's completion to the default branch through a\n closeout pull request: after the implementation PR merges, the loop opens a second PR\n carrying `task.status = done`, `lock.state = released`, and \u2014 when the task completes\n its goal \u2014 `goal.status = complete` plus one scorecard report. Nothing else. The\n@@ -49,14 +49,14 @@\n 4. The task file's governance fields (`scope`, `non_goals`, `required_skills`,\n    `risk_level`, `locks`, `acceptance_checks`, `test_command`) are byte-identical to\n    the base snapshot. Only `status`, `expected_pr`, `closeout_pr`, `ledger_evidence`\n-   and `expected_branch` may differ \u2014 the ADR 0015 contract-immutability rule,\n+   and `expected_branch` may differ \u2014 the SADR-0015 contract-immutability rule,\n    unchanged.\n \n    `expected_branch` is in that set because a closeout is *by construction* on a\n    different branch from the implementation it closes, and the loop rewrites the\n    field accordingly. It is safe to let it differ because MergeGate independently\n    requires `expected_branch` to equal the PR's actual head ref, so a PR cannot set\n-   it to anything other than the branch it is really on. The first draft of this ADR\n+   it to anything other than the branch it is really on. The first draft of this SADR\n    omitted it, which made the exemption unreachable: every real closeout failed\n    condition 4 on a field the loop always changes. See PR #209.\n 5. The state transitions are exactly the terminal set: task `review` \u2192 `done`, lock\n@@ -113,12 +113,12 @@\n classification must be derived, not asserted.\n \n **Fold the closeout into the implementation PR** so no second PR exists. Rejected under\n-ADR 0012, which recorded why: a task file that is already `done` on the implementation\n+SADR-0012, which recorded why: a task file that is already `done` on the implementation\n branch short-circuits the loop's review \u2192 CCA \u2192 merge \u2192 done ordering into a local\n goal-completion before any merge happens.\n \n **Have the loop auto-merge closeouts with elevated credentials.** Rejected outright: that\n-is a runtime authoring its own approval, which ADR 0010 and the constitution's\n+is a runtime authoring its own approval, which SADR-0010 and the constitution's\n approval-record prohibition forbid. It would also be indistinguishable, in the evidence,\n from the forgery those rules exist to prevent.\n \ndiff --git a/docs/adr/0018-carried-guardian-approval-across-a-proven-base-sync.md b/docs/adr/SADR-0018-carried-guardian-approval-across-a-proven-base-sync.md\nindex 22358562da390ed8b45cd600da08a43d378b4680..e6e2907cefa776460af73ddf7d39db605f3c39b0\n--- a/docs/adr/0018-carried-guardian-approval-across-a-proven-base-sync.md\n+++ b/docs/adr/SADR-0018-carried-guardian-approval-across-a-proven-base-sync.md\n@@ -1,4 +1,4 @@\n-# ADR 0018: Carried Guardian Approval Across A Proven Base Sync\n+# SADR-0018: Carried Guardian Approval Across A Proven Base Sync\n \n ## Status\n \n@@ -34,7 +34,7 @@\n Guardian evaluator that consumes it (`shiki_guardian.evaluate_guardian_approval`,\n the `guardian_comment_carried` source) were built and tested in isolation. Neither\n does anything until a caller passes the flags that turn the carry on, so the\n-feature was invisible in CI. This ADR records the decision the two of them\n+feature was invisible in CI. This SADR records the decision the two of them\n implement, and this task wires it into the four CI invocations that judge Guardian\n approval.\n \ndiff --git a/docs/adr/README.md b/docs/adr/README.md\nindex c70e057a4520c36e042ab68c53cd4a21ef709b71..5da43856836e2ce6a150f8127766e2f88355ab4b\n--- a/docs/adr/README.md\n+++ b/docs/adr/README.md\n@@ -1,40 +1,41 @@\n-# Architecture Decision Records\n+# Decision Record Namespaces\n \n-This directory holds Shiki's Architecture Decision Records (ADRs): durable\n-records of hard-to-reverse platform decisions. ADRs are part of the Shiki source\n-of truth alongside GitHub state, the `.shiki/` mirror, and `CONTEXT.md`.\n+This directory holds two independent decision namespaces:\n \n-## How ADRs Work\n+- Shiki Architecture Decision Records (SADRs), named\n+  `SADR-NNNN-<short-slug>.md`, record hard-to-reverse Shiki platform decisions.\n+- Target Architecture Decision Records (ADRs), named\n+  `NNNN-<short-slug>.md`, belong to the target product.\n \n-- New ADRs are added as `docs/adr/NNNN-<short-slug>.md`, where `NNNN` is the next\n-  zero-padded sequence number.\n-- The **canonical list of ADRs is the set of files matching `docs/adr/*.md`** in\n-  this directory. Browse `docs/adr/*.md` for the authoritative, current set.\n-- Use [`template.md`](template.md) as the starting point for a new ADR.\n-- Record an ADR when a decision is hard to reverse, surprising without context,\n-  and a real tradeoff. Do not write an ADR for routine or easily reversible\n-  choices.\n+The same four-digit number may appear once in each namespace. A platform\n+`SADR-NNNN` citation resolves only to `SADR-NNNN-*.md`; a target `ADR NNNN`\n+citation resolves only to `NNNN-*.md`.\n \n-Using the `docs/adr/*.md` glob as the source of truth means other PRs can add an\n-ADR without editing this index file, avoiding merge contention on a\n-hand-maintained enumeration.\n+## How Decision Records Work\n \n-## Current ADRs (snapshot)\n-\n-The list below is a convenience snapshot and may lag behind the directory. When\n-in doubt, list `docs/adr/*.md`.\n+- New Shiki platform decisions use the next zero-padded SADR number.\n+- New target product decisions use the target's own ADR sequence.\n+- The canonical list is the files in `docs/adr/`; no hand-maintained index is\n+  authoritative.\n+- Use [`template.md`](template.md) as the starting point and choose the filename\n+  namespace that owns the decision.\n+- Record a decision when it is hard to reverse, surprising without context, and\n+  a real tradeoff. Do not write a decision record for routine or easily\n+  reversible choices.\n \n-- [0001 \u2014 Keep Shiki As The Platform Name](0001-keep-shiki-as-platform-name.md)\n-- [0002 \u2014 Use AGENTS As The Shared Agent Constitution](0002-use-agents-as-shared-constitution.md)\n-- [0003 \u2014 Require Skill Gate For MergeGate Readiness](0003-require-skill-gate-for-mergegate-readiness.md)\n-- [0004 \u2014 Use GitHub First With A Repository-Local Shiki Mirror](0004-use-github-first-with-shiki-mirror.md)\n-- [0005 \u2014 Use Codex As Front And Claude Code Action As The GitHub Runtime](0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md)\n-- [0006 \u2014 Separate Codex Implementation, CCA Completion, And MergeGate Authorization](0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md)\n+Using the directory as the source of truth lets independent changes add records\n+without contending on an enumerated index.\n \n-## Adding A New ADR\n+## Adding A Shiki SADR\n \n-1. Copy `template.md` to `docs/adr/NNNN-<short-slug>.md`.\n+1. Copy `template.md` to `docs/adr/SADR-NNNN-<short-slug>.md`.\n 2. Fill in the title, status, context, decision, and consequences.\n-3. Set the status to `Proposed`, then `Accepted` once the decision is approved.\n-4. Open a PR following `CONTRIBUTING.md`. You do not need to edit this index for\n-   the ADR to be discoverable via `docs/adr/*.md`.\n+3. Set the status to `Proposed`, then `Accepted` once the proper authority has\n+   approved the decision.\n+4. Open a PR following `CONTRIBUTING.md`.\n+\n+## Adding A Target ADR\n+\n+1. Copy `template.md` to `docs/adr/NNNN-<short-slug>.md` using the target's own\n+   sequence.\n+2. Cite it as `ADR NNNN`; do not prefix its filename with `SADR-`.\ndiff --git a/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md b/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\nnew file mode 100644\nindex 0000000000000000000000000000000000000000..7455ce1f3bed02ddddb851ac21a1d1a648c9aeba\n--- /dev/null\n+++ b/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\n@@ -0,0 +1,55 @@\n+# SADR-0019: Separate Shiki Platform SADRs From Target ADRs\n+\n+## Status\n+\n+Accepted\n+\n+## Context\n+\n+Shiki installs its platform decision records into `docs/adr/` in every target\n+repository. Shiki and its targets previously used the same numeric\n+`NNNN-<slug>.md` namespace. A target that already owned ADRs 0013 through 0018\n+therefore acquired two records for each number during installation, making an\n+`ADR NNNN` citation ambiguous. Renumbering target decisions would rewrite target\n+history, while renumbering Shiki decisions per target would destroy their stable\n+platform identity.\n+\n+Upgrades also need to remove the former Shiki-owned numeric paths without ever\n+mistaking a target-owned ADR for platform content. The install stamp already\n+records exact shipped paths and content digests, so it can prove ownership only\n+when both still match.\n+\n+## Decision\n+\n+Shiki platform decisions use the `SADR` namespace:\n+\n+- records are named `docs/adr/SADR-NNNN-<slug>.md`;\n+- live platform citations use `SADR-NNNN`;\n+- target product decisions retain `docs/adr/NNNN-<slug>.md` and `ADR NNNN`;\n+- the reference validator resolves the two namespaces independently, so the same\n+  four-digit number may exist once in each namespace.\n+\n+Before a forced upgrade writes anything, the installer inventories the exact\n+former numeric Shiki ADR paths. It may delete a legacy path only when the\n+existing install stamp contains that exact path and its recorded SHA-256 digest\n+matches the file currently on disk. All ownership blockers are reported\n+together and abort the upgrade before its first write. A non-forced install\n+never performs this cleanup.\n+\n+Historical `.shiki/` evidence remains unchanged because it records the namespace\n+that existed when that evidence was produced.\n+\n+## Consequences\n+\n+- Targets may use any ADR number without colliding with Shiki's platform SADRs.\n+- Fresh installs preserve target ADR bytes and install a stable SADR set.\n+- Forced upgrades fail closed when legacy ownership cannot be proven, and a\n+  successful upgrade refreshes the stamp without removed legacy paths.\n+- Current governance, packaging, workflow, prompt, script, and skill references\n+  must move with the platform records.\n+\n+## Alternatives Considered\n+\n+- Collision-only refusal leaves affected targets permanently unupgradable.\n+- Renumbering target ADRs mutates target-owned history.\n+- Per-target Shiki renumbering makes platform citations non-portable.\ndiff --git a/docs/adr/template.md b/docs/adr/template.md\nindex a92a97daddd0b00ab9dd626cc8d86942da2eafed..17dd79175b5fee2105935557f691b40cf9492942\n--- a/docs/adr/template.md\n+++ b/docs/adr/template.md\n@@ -1,14 +1,14 @@\n-# NNNN: <Short Decision Title>\n+# <SADR-NNNN or NNNN>: <Short Decision Title>\n \n ## Status\n \n-Proposed | Accepted | Superseded by ADR-NNNN\n+Proposed | Accepted | Superseded by SADR-NNNN or target ADR NNNN\n \n ## Context\n \n What is the situation, constraint, or force that requires a decision? Reference\n-relevant domain terms (`CONTEXT.md`), prior ADRs, and code reality. State the\n-problem, not the solution.\n+relevant domain terms (`CONTEXT.md`), prior Shiki SADRs or target ADRs, and code\n+reality. State the problem, not the solution.\n \n ## Decision\n \ndiff --git a/docs/agents/autonomous-loop-e2e-acceptance.md b/docs/agents/autonomous-loop-e2e-acceptance.md\nindex bbf84c6081987f159ac6ad9ecf3c82b7aa72e7e8..5c13c1d02a05a90b0dc46cbef7c95a9846d0c237\n--- a/docs/agents/autonomous-loop-e2e-acceptance.md\n+++ b/docs/agents/autonomous-loop-e2e-acceptance.md\n@@ -40,8 +40,8 @@\n 3. `gh auth status` is logged in with merge permission on the repo.\n 4. Branch protection + required checks are configured exactly as the goal-loop\n    required-check set expects (see `docs/agents/branch-protection-smoke.md`).\n-5. The repo `.shiki/policy` permits autonomous low/medium-risk merges (ADR\n-   0008/0009); the MergeGate policy check is the recorded-authority gate.\n+5. The repo `.shiki/policy` permits autonomous low/medium-risk merges\n+   (SADR-0008/0009); the MergeGate policy check is the recorded-authority gate.\n \n The operator's only actions are: register the task and start the loop. **No\n manual pre-clearing** means after `shiki loop run` starts, the operator does not\ndiff --git a/docs/agents/bootstrap-command.md b/docs/agents/bootstrap-command.md\nindex 05f7f203a52cf412b9f97084a6e131ed0f785afd..f7aa0b734dfebee4a3b139335b66ee3415f48713\n--- a/docs/agents/bootstrap-command.md\n+++ b/docs/agents/bootstrap-command.md\n@@ -189,7 +189,9 @@\n ```\n \n Use this only for tests, fixtures, or explicit local-only template inspection.\n-Use `--force` only when you intentionally want to overwrite existing target files.\n+Use `--force` only when you intentionally want to refresh the target template.\n+Legacy numeric Shiki decision cleanup is stamp-and-digest authorized before any\n+write; target ADR paths are not cleanup candidates.\n \n ## Slash Command\n \ndiff --git a/docs/agents/checklists.md b/docs/agents/checklists.md\nindex 4a7f03ade346f53a1c1d82981f3fd744f9ab4547..0c5ec6c53dc7197b7b7a7d18e667b325babd6c45\n--- a/docs/agents/checklists.md\n+++ b/docs/agents/checklists.md\n@@ -32,18 +32,18 @@\n | ID | Item | Blocking |\n | --- | --- | --- |\n | GD-01 | Plan was challenged against `CONTEXT.md`. | blocking for non-trivial Goals |\n-| GD-02 | Relevant ADRs were read or absence was recorded. | blocking for non-trivial Goals |\n+| GD-02 | Relevant SADRs and target ADRs were read or absence was recorded. | blocking for non-trivial Goals |\n | GD-03 | Ambiguous terms were resolved or converted to HITL blockers. | blocking |\n | GD-04 | Edge scenarios were considered. | advisory |\n | GD-05 | Code reality was checked when code could answer. | blocking when applicable |\n | GD-06 | New domain terms were added to `CONTEXT.md` when needed. | blocking when applicable |\n-| GD-07 | ADR was created or explicitly skipped using the ADR criteria. | blocking when applicable |\n+| GD-07 | A SADR or target ADR was created or explicitly skipped using the decision-record criteria. | blocking when applicable |\n \n ## CI \u2014 Context & Impact Checklist\n \n | ID | Item | Blocking |\n | --- | --- | --- |\n-| CI-01 | Relevant docs and ADRs identified. | blocking |\n+| CI-01 | Relevant docs, SADRs, and target ADRs identified. | blocking |\n | CI-02 | Relevant modules, interfaces, seams, callers, and tests identified. | blocking |\n | CI-03 | Dependencies and blocked-by relationships identified. | blocking |\n | CI-04 | Candidate locks identified. | blocking |\n@@ -62,7 +62,7 @@\n | PRD-04 | Implementation decisions are recorded without volatile code detail. | blocking |\n | PRD-05 | Testing decisions are recorded. | blocking |\n | PRD-06 | Out-of-scope items are explicit. | blocking |\n-| PRD-07 | Relevant glossary/ADR links are present when applicable. | blocking |\n+| PRD-07 | Relevant glossary/SADR/target-ADR links are present when applicable. | blocking |\n \n ## SF \u2014 Spec Freeze Checklist\n \n@@ -98,7 +98,7 @@\n | H-02 | Branch/worktree target is present. | blocking |\n | H-03 | Scope and non-goals are present. | blocking |\n | H-04 | Dependencies and locks are present. | blocking |\n-| H-05 | Relevant docs, ADRs, modules, and tests are referenced. | blocking |\n+| H-05 | Relevant docs, SADRs, target ADRs, modules, and tests are referenced. | blocking |\n | H-06 | Required skills are stated. | blocking |\n | H-07 | TDD expectations are stated. | blocking when behavior work |\n | H-08 | Acceptance criteria are included. | blocking |\ndiff --git a/docs/agents/codex-handoff.md b/docs/agents/codex-handoff.md\nindex ede468f47cb277f69a857e8c45df703e4ed0151d..6f97bdffb67e44d7edea2c71e01ea28933c4eb6c\n--- a/docs/agents/codex-handoff.md\n+++ b/docs/agents/codex-handoff.md\n@@ -46,7 +46,7 @@\n \n ## Context\n - Domain terms:\n-- Relevant ADRs:\n+- Relevant SADRs and target ADRs:\n - Relevant docs:\n - Relevant modules/interfaces/seams:\n - Similar tests/prior art:\ndiff --git a/docs/agents/completion-check-agent.md b/docs/agents/completion-check-agent.md\nindex 13e4c035db35025f4e3cdcc5adfb24314ac4d521..61bfa1218cf36ca605250c91919e620f33238aa0\n--- a/docs/agents/completion-check-agent.md\n+++ b/docs/agents/completion-check-agent.md\n@@ -32,7 +32,7 @@\n - `AGENTS.md`\n - `CLAUDE.md`\n - `CONTEXT.md`\n-- relevant ADRs\n+- relevant SADRs and target ADRs\n - parent Goal issue\n - PRD issue or document\n - task issue\ndiff --git a/docs/agents/decision-control.md b/docs/agents/decision-control.md\nindex 7d8ee5ab03a5ccbcbd04056ae61de03785a2a9c9..8c94ecda00fffaa0e92c325ab83aa9776bd1dac1\n--- a/docs/agents/decision-control.md\n+++ b/docs/agents/decision-control.md\n@@ -11,7 +11,7 @@\n | Decision | Owner | When | Required inputs | Output | Enforcement surface |\n | --- | --- | --- | --- | --- | --- |\n | Is the Goal clear enough to plan? | Planner + Guardian when needed | Goal intake | User request, existing docs, risk signals | Goal readiness verdict | Goal checklist, issue template, ledger |\n-| Must `grill-with-docs` run? | Planner / Skill Gate | Before PRD or issue split | Ambiguity, domain terms, ADR conflicts, unclear boundaries | Skill invocation or explicit skip rationale | Skill Gate checklist |\n+| Must `grill-with-docs` run? | Planner / Skill Gate | Before PRD or issue split | Ambiguity, domain terms, SADR/target-ADR conflicts, unclear boundaries | Skill invocation or explicit skip rationale | Skill Gate checklist |\n | Is a PRD required? | Planner | After Goal context stabilizes | Goal, `grill-with-docs` output, Context & Impact output | PRD required / not required | PRD checklist, CCA evidence check |\n | Can the spec be frozen? | Operator only | End of Requirements Definition, before issue split | PRD/requirements summary, scope inventory (SF-02), open-question list | `spec_freeze` block in the plan + ledger evidence | `require_grilled_plan` (plan ingest/run), SF checklist |\n | Can a frozen spec change? | Operator only (Spec Amendment) | When implementation contradicts the frozen spec | Contested decisions, scoped re-grill result | recorded amendment + re-stamped freeze | SF checklist, ledger evidence |\ndiff --git a/docs/agents/domain.md b/docs/agents/domain.md\nindex 258da8ea81e87ac0a6b0b5b5e5c2fde980eaabb7..d57b0cf17ff3e1fa9e6e5885372bf083d8295421\n--- a/docs/agents/domain.md\n+++ b/docs/agents/domain.md\n@@ -5,7 +5,9 @@\n ## Layout\n \n - `CONTEXT.md` defines Shiki language. Keep it implementation-free.\n-- `docs/adr/` records hard-to-reverse decisions.\n+- `docs/adr/SADR-NNNN-*.md` records Shiki platform decisions.\n+- `docs/adr/NNNN-*.md` records target product decisions without sharing the\n+  SADR namespace.\n - `AGENTS.md` is the runtime-neutral agent constitution.\n - `CLAUDE.md` is the Claude Code wrapper around `AGENTS.md`.\n - `.shiki/` is the Target Repository mirror for Goals, PRDs, plans, Task DAGs, contracts, locks, worktree records, ledger entries, CCA verdicts, repair packets, reports, and handoffs.\n@@ -15,6 +17,6 @@\n \n - Use Shiki terms exactly when they exist.\n - Add glossary terms to `CONTEXT.md` only when the term is part of the domain, not a generic programming concept.\n-- Create ADRs only for hard-to-reverse, surprising, tradeoff-based decisions.\n+- Create SADRs or target ADRs only for hard-to-reverse, surprising, tradeoff-based decisions.\n - Keep product-specific language in the Target Repository, not in Shiki Template docs.\n-- If an ADR conflicts with a proposed implementation, surface the conflict before editing.\n+- If a SADR or target ADR conflicts with a proposed implementation, surface the conflict before editing.\ndiff --git a/docs/agents/execution-control.md b/docs/agents/execution-control.md\nindex ad8ee3cdb36be56fedc1f46408ca92b387d74047..bc63c3a73661d12b567016ba299f1a6b66bf9088\n--- a/docs/agents/execution-control.md\n+++ b/docs/agents/execution-control.md\n@@ -63,7 +63,7 @@\n | Branch name | Orchestrator | Orchestrator | Before implementer dispatch | branch policy | branch ref in task/ledger |\n | Lock reservation | Context & Impact | MergeGate | Before dispatch and before merge | `.shiki/locks/`, labels, PR metadata | lock id, owner, scope |\n | CI profile | Planner | MergeGate | During issue triage, rechecked at PR | labels, `.shiki/policy.yaml`, workflow matrix | required check list |\n-| Required status checks | Repo maintainer / Guardian | GitHub branch protection / rulesets | Repository setup, updated via ADR | GitHub settings | branch/ruleset config, ADR |\n+| Required status checks | Repo maintainer / Guardian | GitHub branch protection / rulesets | Repository setup, updated via target ADR | GitHub settings | branch/ruleset config, target ADR |\n | CCA judgment | CCA | GitHub required check / MergeGate | Every non-draft PR update | GitHub Action check | structured CCA verdict |\n | Repair assignment | CCA | MergeGate | After failed CCA/CI/review | repair packet, issue/PR comment | repair id and attempt count |\n | Merge readiness | MergeGate | GitHub branch protection / Guardian if needed | After CCA complete | required checks, review rules, rulesets | mergegate check output |\ndiff --git a/docs/agents/guardian-policy.md b/docs/agents/guardian-policy.md\nindex 4147c7adf05a47adb4fd24ada1a1c2e1f2ba6d2d..a20d9cc6c00a569bb057b9ca8d6b51c7fc4a9847\n--- a/docs/agents/guardian-policy.md\n+++ b/docs/agents/guardian-policy.md\n@@ -98,8 +98,8 @@\n `origin/<default_branch>` and reuses each job's existing base fetch; a shallow\n (`--depth`) or second base fetch would make the proof unresolvable and silently\n disable the carry, so neither is permitted. CCA reads `guardian_comment_carried` as\n-a recorded authority in the deterministic signal and does not re-derive it. See ADR\n-0018 for the full decision, the rejected diff/patch-equality and path-disjointness\n+a recorded authority in the deterministic signal and does not re-derive it. See\n+SADR-0018 for the full decision, the rejected diff/patch-equality and path-disjointness\n alternatives, and the accepted residual.\n \n ## External AI Guardian Review (SADR-0010)\ndiff --git a/docs/agents/implementation-policy.md b/docs/agents/implementation-policy.md\nindex c754144a9fb5bf4a38aa82a60e1670c59e626b44..85c37281bcad196c6b7b02343faf00b03cd9c57a\n--- a/docs/agents/implementation-policy.md\n+++ b/docs/agents/implementation-policy.md\n@@ -31,10 +31,13 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n ## Non-Negotiables\n \n 1. **Planning precedes implementation.** Non-trivial Goals start with `grill-with-docs` and Context & Impact.\n-2. **Domain language is controlled.** Terms must be checked against `CONTEXT.md`; hard-to-reverse tradeoffs must be checked against ADRs.\n+2. **Domain language is controlled.** Terms must be checked against `CONTEXT.md`; hard-to-reverse tradeoffs must be checked against Shiki SADRs and target ADRs.\n 3. **PRD precedes issue decomposition.** `to-prd` is used once the context is settled enough to become durable intent.\n 4. **Issues are vertical slices.** `to-issues` must produce independently grabbable tracer-bullet tasks, not horizontal layer tickets.\n 5. **Triage controls dispatch.** Only `ready-for-agent` / AFK tasks may be assigned to Codex without human input.\n@@ -95,19 +98,19 @@\n Process:\n \n - Challenge terms against `CONTEXT.md`.\n-- Check ADRs before reopening decisions.\n+- Check SADRs and target ADRs before reopening decisions.\n - Inspect code when code can answer a question.\n - Ask one question at a time when the operator is available.\n - Provide a recommended answer with each question.\n - Record resolved terms in `CONTEXT.md`.\n-- Create ADRs only for hard-to-reverse, surprising, tradeoff-based decisions.\n+- Create SADRs or target ADRs only for hard-to-reverse, surprising, tradeoff-based decisions.\n - Convert unresolved load-bearing questions into HITL blockers.\n \n Outputs:\n \n - Resolved design-tree decisions.\n - Updated glossary terms when needed.\n-- ADRs when justified.\n+- SADRs or target ADRs when justified.\n - HITL blockers when decisions remain unresolved.\n \n ## Phase 3 \u2014 Context & Impact\n@@ -117,7 +120,7 @@\n \n Outputs must include:\n \n-- Relevant documents and ADRs.\n+- Relevant documents, SADRs, and target ADRs.\n - Relevant modules, interfaces, seams, callers, and tests.\n - Candidate locks.\n - Dependencies.\n@@ -140,7 +143,7 @@\n - Testing decisions.\n - Out of scope.\n - Further notes.\n-- Links to relevant Goal, domain terms, and ADRs.\n+- Links to relevant Goal, domain terms, SADRs, and target ADRs.\n \n Phase 4 ends with Spec Freeze: the operator explicitly approves the PRD, the\n plan records a `spec_freeze` block (status, approved_by, source), and the SF\n@@ -240,7 +243,7 @@\n - Dependencies and locks.\n - Risk and Guardian requirements.\n - Ledger completeness.\n-- Documentation and ADR updates.\n+- Documentation and SADR/target-ADR updates.\n - Scope drift.\n \n CCA emits one of:\n@@ -301,7 +304,7 @@\n | Evidence failure | Behavior might be done, but proof is missing. | Add checks, PR evidence, ledger entry, or artifacts. |\n | Scope drift | PR changes unrelated behavior. | Revert unrelated changes or split into new Goal. |\n | Dependency block | Upstream task or lock not complete. | Block until dependency resolves. |\n-| Architecture block | Decision needs ADR or deep-module discussion. | `grill-with-docs` or `improve-codebase-architecture`. |\n+| Architecture block | Decision needs a SADR, target ADR, or deep-module discussion. | `grill-with-docs` or `improve-codebase-architecture`. |\n | Risk block | Security/production/policy change needs human approval. | Guardian decision. |\n \n ## Phase 5.5 \u2014 Execution Decision Control\ndiff --git a/docs/agents/runtime-auth-model.md b/docs/agents/runtime-auth-model.md\nindex 6a2e1a47e81ea89b5c68a210ec07772a9374babc..3c54748d24233dbd0ba8473950282547ef4ce935\n--- a/docs/agents/runtime-auth-model.md\n+++ b/docs/agents/runtime-auth-model.md\n@@ -148,7 +148,7 @@\n \n API-key based runners may be added by a target repository as an explicit extension. They are not the default Shiki template path.\n \n-If a repo opts into API-key mode, record the exception in an ADR and update the repo's `AGENTS.md`, `.shiki/config.yaml`, and workflow permissions.\n+If a repo opts into API-key mode, record the target-specific exception in a target ADR and update the repo's `AGENTS.md`, `.shiki/config.yaml`, and workflow permissions.\n \n ## GitHub Actions Runtime Compatibility\n \ndiff --git a/docs/agents/skill-gate.md b/docs/agents/skill-gate.md\nindex c50398b0d7c60a8598f2a5c88a10015730dced13..3acffe6444617bb1d54b77d4b4708158ec735bc6\n--- a/docs/agents/skill-gate.md\n+++ b/docs/agents/skill-gate.md\n@@ -9,7 +9,7 @@\n | Skill | Trigger | Expected output |\n | --- | --- | --- |\n | `setup-matt-pocock-skills` | First configuring a repo for these skills, or when issue tracker, triage labels, or domain docs are missing. | `docs/agents/` configuration and an `Agent skills` block in `AGENTS.md` or `CLAUDE.md`. |\n-| `grill-with-docs` | Ambiguous plan, unclear terminology, boundary decision, tradeoff, or ADR-worthy design choice. | Clarified decision tree, recommended answers, updates to `CONTEXT.md` or ADRs when decisions crystallize. |\n+| `grill-with-docs` | Ambiguous plan, unclear terminology, boundary decision, tradeoff, or decision-record-worthy design choice. | Clarified decision tree, recommended answers, updates to `CONTEXT.md`, SADRs, or target ADRs when decisions crystallize. |\n | `zoom-out` | Agent lacks a higher-level architectural map. | Map of relevant modules, callers, seams, and domain concepts. |\n | `to-prd` | Settled context should become a PRD. | PRD published to the issue tracker with problem, solution, user stories, decisions, testing, and out-of-scope notes. |\n | `to-issues` | Goal, PRD, or plan must become implementation tickets. | Vertical-slice issues with acceptance criteria, dependencies, and AFK/HITL classification. |\n@@ -17,7 +17,7 @@\n | `tdd` | Feature work or bug fix has observable behavior that can be specified with tests. | Red-green-refactor loop through public interfaces, one vertical slice at a time. |\n | `code-review` | The TDD loop is green and the implementer is about to create or update a PR. Default for every implementation task. | Pre-PR self-review of the task diff with fixes applied on the task branch; findings and resolutions recorded as a ledger entry naming the skill and a `## Pre-PR code review` PR body section. |\n | `diagnose` | Hard bug, failing check, regression, flaky behavior, or performance problem. | Reproduction loop, ranked hypotheses, instrumentation, fix, regression test, post-mortem. |\n-| `improve-codebase-architecture` | Structural friction, testability, AI-navigability, deep-module opportunity, or refactor request. | Architecture candidates and deepening recommendations informed by `CONTEXT.md` and ADRs. |\n+| `improve-codebase-architecture` | Structural friction, testability, AI-navigability, deep-module opportunity, or refactor request. | Architecture candidates and deepening recommendations informed by `CONTEXT.md`, SADRs, and target ADRs. |\n | `prototype` | A throwaway logic or UI prototype is needed to answer a design question. | Clearly marked disposable prototype and captured decision. |\n \n ## MergeGate Interaction\ndiff --git a/docs/operations.md b/docs/operations.md\nindex cf1f452d4c589e38ccd58553bda0ad8684b51eb7..ace48a90ffbec9e5c0a7113bef16a4858bc44b3f\n--- a/docs/operations.md\n+++ b/docs/operations.md\n@@ -89,6 +89,16 @@\n      target history, brought forward by `shiki migrate`, not a template.\n    - **Everything else** (CLI scripts, workflows, schemas, docs) is overwritten.\n \n+   A forced upgrade also migrates the former numeric Shiki decision paths to\n+   `SADR-NNNN-*`. Before the first write, the installer inventories the exact\n+   legacy Shiki paths and authorizes each deletion only when the existing\n+   `.shiki/install-stamp.json` names that path and its SHA-256 digest still\n+   matches the file. An absent, unreadable, malformed, incomplete, or\n+   mismatching stamp reports every blocked path and stops with no filesystem\n+   change. Target-owned `NNNN-*` ADRs are not cleanup candidates. A non-force\n+   install deletes nothing and retains any still-matching legacy stamp entries\n+   so a later forced upgrade can prove ownership.\n+\n    If the target has pending migrations, `--force` refuses and writes nothing \u2014\n    apply migrations first (step 3), then re-run. The run ends with a single\n    summary of every `.new` written, so a half-upgrade is visible.\ndiff --git a/docs/releasing.md b/docs/releasing.md\nindex 10d73828e7dbd64e9d9f6b83bc84d010537ce62a..aafeee8c4d441e1e427efee2ce4f81a647f020e1\n--- a/docs/releasing.md\n+++ b/docs/releasing.md\n@@ -67,7 +67,7 @@\n CLI in `scripts/`. There are no third-party Python runtime dependencies. The CLI\n is intentionally **not** exposed as a `[project.scripts]` console entry point\n because `scripts/shiki.py` is a dependency-free shim and `scripts/` is not\n-packaged as an importable distribution (see the ADR for the rationale).\n+packaged as an importable distribution (see SADR-0007 for the rationale).\n \n Supported install paths:\n \n@@ -155,6 +155,15 @@\n    [docs/operations.md](operations.md#the-new-convention). If the target has\n    pending migrations, `--force` refuses and writes nothing; apply migrations\n    (step 2) first, then re-run.\n+\n+   The SADR namespace upgrade has an additional pre-write gate: each former\n+   numeric Shiki decision path is removed only when the existing install stamp\n+   contains that exact path and a digest matching its current bytes. The\n+   installer aggregates absent/unreadable/malformed-stamp, missing-digest, and\n+   digest-mismatch blockers before raising, so refusal leaves the target tree\n+   byte-identical. Numeric target ADRs are preserved. A successful forced\n+   upgrade installs and stamps `SADR-*` paths and drops the removed legacy paths\n+   from the refreshed stamp.\n 2. Apply repository-local `.shiki/` state migrations with the migrate command:\n \n    ```bash\ndiff --git a/docs/usage.md b/docs/usage.md\nindex b83407ee3970dd395dd893010bc5dbe8bad26051..a130ef6e40dfd8169a5bc701339351097df1a09f\n--- a/docs/usage.md\n+++ b/docs/usage.md\n@@ -67,8 +67,10 @@\n shiki install-target /path/to/target-repo --local-only\n ```\n \n-Use `--force` only when you intentionally want to overwrite existing target\n-files.\n+Use `--force` only when you intentionally want to refresh the target template.\n+For the SADR namespace migration, `--force` deletes only exact legacy Shiki\n+decision paths whose existing install-stamp digest still matches; any ownership\n+blocker stops before the first write. Numeric target ADRs are never candidates.\n \n ## `shiki bootstrap-platform` \u2014 publish the Shiki platform repo\n \ndiff --git a/scripts/guardian_approval_signal.py b/scripts/guardian_approval_signal.py\nindex d059f92b2c82aa2b5d6ca421365537a87886c1cb..16924892714f4f5b9303c0ef0e325a7e70075630\n--- a/scripts/guardian_approval_signal.py\n+++ b/scripts/guardian_approval_signal.py\n@@ -199,8 +199,8 @@\n ) -> Any:\n     \"\"\"Evaluate SADR-0015 Contract Approval for the CCA Guardian signal.\n \n-    The SAME OR that MergeGate applies must reach this deterministic signal: ADR\n-    0015 is explicit that without it, a contract-approved PR still returns\n+    The SAME OR that MergeGate applies must reach this deterministic signal:\n+    SADR-0015 is explicit that without it, a contract-approved PR still returns\n     ``needs_guardian``, the CCA job fails, and the MergeGate policy check never\n     runs \u2014 the gate this change moves would never be reached. This resolves the\n     task the SAME way MergeGate does (the first Shiki task id in the PR body),\ndiff --git a/scripts/shiki_contract_approval.py b/scripts/shiki_contract_approval.py\nindex 941bcb96fdd8b9a85bc0c4f0f04d5dad9d04839c..4a4b418f6527413161db82670bec857a043360e2\n--- a/scripts/shiki_contract_approval.py\n+++ b/scripts/shiki_contract_approval.py\n@@ -142,7 +142,7 @@\n     changed_files_status: Any,\n     registration: Any,\n ) -> ContractApprovalResult:\n-    \"\"\"Whether this PR carries an SADR-0015 Contract Approval.\n+    \"\"\"Whether this PR carries a SADR-0015 Contract Approval.\n \n     The four conditions are evaluated IN ORDER, each failure recorded as a\n     distinct reason string:\ndiff --git a/scripts/shiki_guardian.py b/scripts/shiki_guardian.py\nindex a4ecb79bbda55fd5c8b06adfae2eb99760a03c45..4dd99bbe714e26f421fa19abac7a34ae1b195ecc\n--- a/scripts/shiki_guardian.py\n+++ b/scripts/shiki_guardian.py\n@@ -12,7 +12,7 @@\n # The carry re-derives its proof from git in-process on every judging invocation;\n # it is never read from an artifact, so there is nothing to go stale or be forged.\n # shiki_sync_proof is the ONLY guardian-family module that runs git, and every one\n-# of its invocations is read-only and local (ADR: prove-from-git base sync).\n+# of its invocations is read-only and local (SADR: prove-from-git base sync).\n from shiki_sync_proof import verify_pure_base_sync\n \n GUARDIAN_POLICY_PATH = \".shiki/guardian-policy.json\"\ndiff --git a/scripts/shiki_guardian_review.py b/scripts/shiki_guardian_review.py\nindex dd0f104d457899e0ba5ff1e162011c1e90b6f92b..e10ef58c046324e7ea3a3392feb3048ca77414ce\n--- a/scripts/shiki_guardian_review.py\n+++ b/scripts/shiki_guardian_review.py\n@@ -159,7 +159,7 @@\n     docs_only = _is_docs_only(changed_files)\n     if docs_only and matched == 0:\n         focus.append(\n-            \"Docs/ADR-only change: confirm the change is genuinely documentation/decision-only, introduces no behavior or contract drift, and stays consistent with the constitution and existing ADRs.\"\n+            \"Docs/decision-record-only change: confirm the change is genuinely documentation/decision-only, introduces no behavior or contract drift, and stays consistent with the constitution and existing SADRs and target ADRs.\"\n         )\n \n     # Conservative augmentation: unknown (no category and not docs-only), mixed\ndiff --git a/scripts/shiki_installer.py b/scripts/shiki_installer.py\nindex be064ab7a29ff6836f7f7a1d70500a43e0d11d7b..9fd8eefd137ce7003dad5c0dcb2e48e2cd47ab32\n--- a/scripts/shiki_installer.py\n+++ b/scripts/shiki_installer.py\n@@ -118,6 +118,7 @@\n # what lets a target carry it without a governance-schema change.\n INSTALL_STAMP_PATH = \".shiki/install-stamp.json\"\n INSTALL_STAMP_VERSION = 1\n+LEGACY_SHIKI_SADR_MAX = 18\n \n # Under --force the shipped surface splits three ways, so nothing is lost and\n # nothing is silently stale.\n@@ -237,9 +238,98 @@\n         )\n \n \n+def legacy_shiki_adr_paths() -> tuple[str, ...]:\n+    \"\"\"Former numeric paths for Shiki SADR-0001 through SADR-0018.\n+\n+    The authoritative slugs come from the current SADR filenames. SADR-0019 is\n+    the namespace decision itself and never shipped under a numeric legacy path.\n+    \"\"\"\n+    paths: list[str] = []\n+    for sadr in sorted((ROOT / \"docs\" / \"adr\").glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\")):\n+        number = sadr.name.split(\"-\", 2)[1]\n+        if int(number) <= LEGACY_SHIKI_SADR_MAX:\n+            paths.append(f\"docs/adr/{sadr.name.removeprefix('SADR-')}\")\n+    return tuple(paths)\n+\n+\n+def preflight_legacy_sadr_cleanup(target: Path, *, force: bool) -> tuple[str, ...]:\n+    \"\"\"Authorize exact legacy Shiki ADR removals before the first write.\n+\n+    A non-force install never removes existing files. A forced install needs no\n+    stamp when no legacy Shiki path is present. When legacy paths do exist, each\n+    must be named in a readable install stamp and its current bytes must match\n+    the recorded digest.\n+    \"\"\"\n+    if not force:\n+        return ()\n+    present = tuple(\n+        relative\n+        for relative in legacy_shiki_adr_paths()\n+        if (target / relative).exists() or (target / relative).is_symlink()\n+    )\n+    if not present:\n+        return ()\n+\n+    stamp_path = target / INSTALL_STAMP_PATH\n+    try:\n+        raw = stamp_path.read_text(encoding=\"utf-8\")\n+        stamp = json.loads(raw)\n+    except FileNotFoundError:\n+        detail = \"install stamp is absent\"\n+        blockers = [f\"{relative}: {detail}\" for relative in present]\n+    except (OSError, UnicodeDecodeError) as error:\n+        detail = f\"install stamp is unreadable ({error})\"\n+        blockers = [f\"{relative}: {detail}\" for relative in present]\n+    except json.JSONDecodeError as error:\n+        detail = f\"install stamp is malformed ({error})\"\n+        blockers = [f\"{relative}: {detail}\" for relative in present]\n+    else:\n+        if not isinstance(stamp, dict):\n+            blockers = [\n+                f\"{relative}: install stamp is malformed (expected an object)\"\n+                for relative in present\n+            ]\n+        else:\n+            digests = stamp.get(\"digests\")\n+            blockers = []\n+            if not isinstance(digests, dict):\n+                blockers.extend(\n+                    f\"{relative}: install stamp has no digest for this path\"\n+                    for relative in present\n+                )\n+            else:\n+                for relative in present:\n+                    expected = digests.get(relative)\n+                    if not isinstance(expected, str) or not expected:\n+                        blockers.append(\n+                            f\"{relative}: install stamp has no digest for this path\"\n+                        )\n+                        continue\n+                    try:\n+                        actual = _sha256_file(target / relative)\n+                    except OSError as error:\n+                        blockers.append(\n+                            f\"{relative}: legacy path is unreadable ({error})\"\n+                        )\n+                        continue\n+                    if actual != expected:\n+                        blockers.append(\n+                            f\"{relative}: digest mismatch (stamp {expected}, current {actual})\"\n+                        )\n+\n+    if blockers:\n+        joined = \"\\n\".join(f\"- {blocker}\" for blocker in blockers)\n+        raise ShikiError(\n+            \"legacy Shiki ADR cleanup is not authorized:\\n\"\n+            f\"{joined}\\nNo files were written.\"\n+        )\n+    return present\n+\n+\n def install_template(target: Path, *, force: bool, validate: bool) -> list[NewFileNote]:\n     # Preconditions first: never rewrite the tree of a target that cannot be\n     # upgraded cleanly (see refuse_pending_migrations).\n+    legacy_cleanup = preflight_legacy_sadr_cleanup(target, force=force)\n     refuse_pending_migrations(target)\n \n     # Resolve the target's required CODEOWNERS owner once from its\n@@ -250,6 +340,10 @@\n     # verbatim. See ``codeowners_required_owner``.\n     codeowners_owner = codeowners_required_owner(target)\n \n+    for relative in legacy_cleanup:\n+        (target / relative).unlink()\n+        info(f\"removed stamped legacy Shiki ADR: {target / relative}\")\n+\n     notes: list[NewFileNote] = []\n     for relative in TEMPLATE_PATHS:\n         source = ROOT / relative\n@@ -647,6 +741,22 @@\n     digests = target_stamp_digests(target)\n     existing = read_install_stamp(target)\n     if existing is not None:\n+        # A non-force install deliberately leaves legacy Shiki ADRs in place.\n+        # Preserve only still-matching ownership proof so a later forced\n+        # upgrade can authorize cleanup; never carry a missing or stale digest.\n+        existing_digests = existing.get(\"digests\")\n+        if isinstance(existing_digests, dict):\n+            for relative in legacy_shiki_adr_paths():\n+                expected = existing_digests.get(relative)\n+                path = target / relative\n+                if not isinstance(expected, str) or not path.is_file():\n+                    continue\n+                try:\n+                    actual = _sha256_file(path)\n+                except OSError:\n+                    continue\n+                if actual == expected:\n+                    digests[relative] = actual\n         if _incoming_is_older(commit, existing.get(\"platform_commit\")):\n             warn(\n                 \"kept existing install stamp: incoming install \"\ndiff --git a/scripts/shiki_loop.py b/scripts/shiki_loop.py\nindex 3611641da8aeb5038b1da90e3114b09a473ec6d8..84caa491b9cad0edc4176f209c13e9221e975681\n--- a/scripts/shiki_loop.py\n+++ b/scripts/shiki_loop.py\n@@ -424,7 +424,7 @@\n         # unresolvable CCA verdict has already produced its terminal stop and is never\n         # laundered into a head-moving sync (SADR-0010). It also never fires while the\n         # policy (Guardian) gate is red \u2014 for ANY reason, not only when it is the sole\n-        # red check \u2014 so a head-bound Guardian approval, or an SADR-0015\n+        # red check \u2014 so a head-bound Guardian approval, or a SADR-0015\n         # contract-approval carry that base movement defeated (the policy gate compares\n         # base and head task governance directly), is never silently restored by\n         # adopting base wholesale. It is bounded by a durable per-task counter so the\ndiff --git a/scripts/shiki_tasks.py b/scripts/shiki_tasks.py\nindex aec3809f24b4a0c8c96fc50971cff9ae9ad30381..d531052e9d9717058d4631483cefab44c26a65c1\n--- a/scripts/shiki_tasks.py\n+++ b/scripts/shiki_tasks.py\n@@ -821,7 +821,7 @@\n         \"prompt\": prompt,\n         \"entry_skill\": \"grill-with-docs\",\n         \"required_next_steps\": [\n-            \"Run grill-with-docs until terminology, boundaries, risks, and ADR-worthy decisions are settled.\",\n+            \"Run grill-with-docs until terminology, boundaries, risks, and decision-record-worthy choices are settled.\",\n             \"Obtain the operator's explicit approval of the PRD/requirements (Spec Freeze).\",\n             \"Write a machine-readable plan JSON with grill_with_docs.status=complete and spec_freeze.status=frozen.\",\n             \"Run shiki plan ingest --plan-file PLAN.json.\",\ndiff --git a/scripts/test_shiki_goal_loop.sh b/scripts/test_shiki_goal_loop.sh\nindex 753c7b516e10f0be3bc92a4bd0eb1fa699776ae6..8f3820a0a079b3044424cfe1a6c6b7fe68896e67\n--- a/scripts/test_shiki_goal_loop.sh\n+++ b/scripts/test_shiki_goal_loop.sh\n@@ -231,8 +231,8 @@\n \n # High/critical risk merges autonomously when all required checks are green:\n # the \"MergeGate policy check\" required check IS the Guardian gate (it enforces\n-# guardian-policy.json \u2014 human approval OR external AI guardian review, ADR\n-# 0010), so green checks mean a recorded authority approved. The fake gh returns\n+# guardian-policy.json \u2014 human approval OR external AI guardian review,\n+# SADR-0010), so green checks mean a recorded authority approved. The fake gh returns\n # green checks, simulating that the Guardian gate passed.\n python3 \"$ROOT/scripts/shiki.py\" goal create --target \"$TARGET\" --title \"Guardian gate\" --outcome \"High risk merges via policy gate\" >/tmp/shiki-goal-loop-guardian-goal.json\n GGOAL=\"$(json_get /tmp/shiki-goal-loop-guardian-goal.json goal_id)\"\ndiff --git a/tests/test_adr_references.py b/tests/test_adr_references.py\nindex 51706329c797d5dd9b47e082a333fa83b261d482..d005da4f771f5e8085ec22d4b787980e68f5ae8c\n--- a/tests/test_adr_references.py\n+++ b/tests/test_adr_references.py\n@@ -1,8 +1,8 @@\n-\"\"\"Every ``ADR ####`` citation must resolve to exactly one decision record.\n+\"\"\"Every target ADR and Shiki SADR citation resolves in its own namespace.\n \n Background. ``scripts/mergegate_check.py`` justifies ``is_bookkeeping_closeout``\n-with \"ADR 0017\" nine times and ``scripts/guardian_approval_signal.py`` cites\n-0016/0017 seven times, yet for a stretch the numbering in ``docs/adr/`` stopped\n+with \"SADR-0017\" nine times and ``scripts/guardian_approval_signal.py`` cites\n+SADR-0016/SADR-0017 seven times, yet for a stretch the numbering in ``docs/adr/`` stopped\n at 0015: the authoring commits lived on a coordinator branch and no task ever\n declared ``docs/adr/...`` in its locks, so nothing carried the records to the\n default branch. The stated basis of an exemption that merged seven PRs with zero\n@@ -11,11 +11,12 @@\n and the directory drifted with no signal.\n \n This is the same unbound-pair shape behind other silent failures: a reference\n-and its target with nothing asserting they agree. This test binds them. It scans\n-every ``scripts/*.py`` (plus ``docs/**/*.md`` and the root ``*.md`` governance\n-files, which are cheap) for the ``ADR ####`` pattern and asserts each cited\n-number resolves to exactly one ``docs/adr/####-*.md``. The converse is\n-deliberately NOT required: an ADR may exist without being cited.\n+and its target with nothing asserting they agree. This test binds them. Target\n+``ADR NNNN`` citations resolve only to ``docs/adr/NNNN-*.md``; Shiki\n+``SADR-NNNN`` citations resolve only to ``docs/adr/SADR-NNNN-*.md``. The scan\n+covers current governance docs, scripts, workflows, prompts, packaging metadata,\n+and repo-local Codex guidance while deliberately excluding historical\n+``.shiki`` evidence. The converse is not required: a record may be uncited.\n \n The suite is dependency free: standard-library ``unittest`` only.\n \"\"\"\n@@ -31,17 +32,18 @@\n \n REPO_ROOT = Path(__file__).resolve().parents[1]\n \n-# \"ADR 0016\" -- a four-digit citation. The trailing \\b keeps a five-digit token\n-# from being read as a truncated four-digit number.\n-_CITATION = re.compile(r\"\\bADR (\\d{4})\\b\")\n-# \"0016-locks-bound-product-paths.md\" -- the leading four digits name the record.\n-_ADR_FILENAME = re.compile(r\"^(\\d{4})-.*\\.md$\")\n+# ``ADR 0016`` / ``SADR-0016`` -- a namespace plus four-digit citation. The\n+# trailing ``\\b`` keeps a five-digit token from being read as a truncated one.\n+_CITATION = re.compile(r\"\\b(S?ADR)[ -](\\d{4})\\b\")\n+# Target ADRs retain the numeric filename; Shiki platform decisions use SADR.\n+_DECISION_FILENAME = re.compile(r\"^(?:(SADR)-)?(\\d{4})-.*\\.md$\")\n \n \n class Violation(NamedTuple):\n     \"\"\"A citation that does not resolve to exactly one decision record.\"\"\"\n \n     citing_file: str  # repo-relative path of the file that cites the ADR\n+    namespace: str  # \"ADR\" for target decisions; \"SADR\" for Shiki decisions\n     number: str  # the cited four-digit number, e.g. \"0016\"\n     line: int  # 1-based line of the citation\n     reason: str  # \"missing\" (zero matches) or \"ambiguous\" (two or more)\n@@ -49,44 +51,59 @@\n \n     @property\n     def expected_path(self) -> str:\n-        return f\"docs/adr/{self.number}-*.md\"\n+        prefix = \"SADR-\" if self.namespace == \"SADR\" else \"\"\n+        return f\"docs/adr/{prefix}{self.number}-*.md\"\n \n     def describe(self) -> str:\n         \"\"\"Name the citing file, the cited number, and the missing path.\"\"\"\n         if self.reason == \"missing\":\n             return (\n-                f\"{self.citing_file}:{self.line} cites ADR {self.number} \"\n+                f\"{self.citing_file}:{self.line} cites {self.namespace} {self.number} \"\n                 f\"but no document matches {self.expected_path}\"\n             )\n         joined = \", \".join(self.matches)\n         return (\n-            f\"{self.citing_file}:{self.line} cites ADR {self.number} which \"\n+            f\"{self.citing_file}:{self.line} cites {self.namespace} {self.number} which \"\n             f\"resolves to {len(self.matches)} documents (expected exactly one \"\n             f\"at {self.expected_path}): {joined}\"\n         )\n \n \n-def _adr_index(root: Path) -> dict[str, list[Path]]:\n-    \"\"\"Map every four-digit ADR number to the record files that claim it.\"\"\"\n-    index: dict[str, list[Path]] = {}\n+def _adr_index(root: Path) -> dict[tuple[str, str], list[Path]]:\n+    \"\"\"Map each decision namespace and number to the files that claim it.\"\"\"\n+    index: dict[tuple[str, str], list[Path]] = {}\n     adr_dir = root / \"docs\" / \"adr\"\n     if adr_dir.is_dir():\n         for path in sorted(adr_dir.glob(\"*.md\")):\n-            match = _ADR_FILENAME.match(path.name)\n+            match = _DECISION_FILENAME.match(path.name)\n             if match:\n-                index.setdefault(match.group(1), []).append(path)\n+                namespace = \"SADR\" if match.group(1) else \"ADR\"\n+                index.setdefault((namespace, match.group(2)), []).append(path)\n     return index\n \n \n def _citing_files(root: Path, include_docs: bool = True) -> list[Path]:\n-    \"\"\"Files scanned for citations: scripts/*.py, and cheaply the markdown.\"\"\"\n+    \"\"\"Current, shipped text surfaces scanned for decision citations.\"\"\"\n     files = sorted((root / \"scripts\").glob(\"*.py\"))\n+    files += sorted((root / \"scripts\").glob(\"*.sh\"))\n     if include_docs:\n         docs = root / \"docs\"\n         if docs.is_dir():\n             files += sorted(docs.rglob(\"*.md\"))\n         files += sorted(root.glob(\"*.md\"))\n-    return files\n+        pyproject = root / \"pyproject.toml\"\n+        if pyproject.is_file():\n+            files.append(pyproject)\n+        for relative in (\".github\", \".codex\"):\n+            surface = root / relative\n+            if surface.is_dir():\n+                files += sorted(\n+                    path\n+                    for path in surface.rglob(\"*\")\n+                    if path.is_file()\n+                    and path.suffix in {\".md\", \".py\", \".sh\", \".toml\", \".yaml\", \".yml\"}\n+                )\n+    return sorted(set(files))\n \n \n def find_adr_reference_violations(\n@@ -94,10 +111,9 @@\n ) -> list[Violation]:\n     \"\"\"Return every ``ADR ####`` citation under ``root`` that fails to resolve.\n \n-    A citation resolves when exactly one ``docs/adr/####-*.md`` matches its\n-    number. Zero matches is a \"missing\" violation; two or more is \"ambiguous\".\n-    Records that are never cited produce nothing -- the converse is not\n-    required.\n+    A citation resolves when exactly one record in the same namespace matches\n+    its number. Zero matches is a \"missing\" violation; two or more is\n+    \"ambiguous\". Records that are never cited produce nothing.\n     \"\"\"\n     root = Path(root)\n     index = _adr_index(root)\n@@ -110,13 +126,15 @@\n         rel = path.relative_to(root).as_posix()\n         for lineno, line in enumerate(text.splitlines(), start=1):\n             for match in _CITATION.finditer(line):\n-                number = match.group(1)\n-                found = index.get(number, [])\n+                namespace, number = match.groups()\n+                found = index.get((namespace, number), [])\n                 if len(found) == 1:\n                     continue\n                 reason = \"missing\" if not found else \"ambiguous\"\n                 rels = [p.relative_to(root).as_posix() for p in found]\n-                violations.append(Violation(rel, number, lineno, reason, rels))\n+                violations.append(\n+                    Violation(rel, namespace, number, lineno, reason, rels)\n+                )\n     return violations\n \n \n@@ -129,8 +147,8 @@\n         self.assertEqual(\n             violations,\n             [],\n-            \"Every 'ADR ####' citation must resolve to exactly one \"\n-            f\"docs/adr/####-*.md:\\n{detail}\",\n+            \"Every ADR/SADR citation must resolve exactly once in its own \"\n+            f\"docs/adr namespace:\\n{detail}\",\n         )\n \n     def test_scan_actually_reads_scripts(self) -> None:\n@@ -142,12 +160,38 @@\n             \"the citation scan must cover scripts/*.py\",\n         )\n \n+    def test_platform_citations_do_not_use_unresolved_numeric_shorthand(self) -> None:\n+        shorthand = re.compile(r\"SADR-\\d{4}/\\d{4}\")\n+        offenders = []\n+        for path in _citing_files(REPO_ROOT):\n+            text = path.read_text(encoding=\"utf-8\")\n+            if shorthand.search(text):\n+                offenders.append(path.relative_to(REPO_ROOT).as_posix())\n+        self.assertEqual(\n+            offenders,\n+            [],\n+            \"repeat the SADR prefix so every cited number is independently resolvable\",\n+        )\n+\n     def test_carried_records_are_indexed(self) -> None:\n         # The two records this Goal carried onto the default branch, and which\n         # the scripts cite most, must be present.\n         index = _adr_index(REPO_ROOT)\n-        self.assertIn(\"0016\", index)\n-        self.assertIn(\"0017\", index)\n+        self.assertIn((\"SADR\", \"0016\"), index)\n+        self.assertIn((\"SADR\", \"0017\"), index)\n+\n+    def test_platform_tree_has_only_sadr_0001_through_0019(self) -> None:\n+        adr_dir = REPO_ROOT / \"docs\" / \"adr\"\n+        sadrs = sorted(adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\"))\n+        self.assertEqual(\n+            {path.name.split(\"-\", 2)[1] for path in sadrs},\n+            {f\"{number:04d}\" for number in range(1, 20)},\n+        )\n+        self.assertEqual(\n+            list(adr_dir.glob(\"[0-9][0-9][0-9][0-9]-*.md\")),\n+            [],\n+            \"the Shiki platform tree must not retain legacy numeric SADR paths\",\n+        )\n \n \n class FixtureAdrReferences(unittest.TestCase):\n@@ -180,6 +224,26 @@\n         self.assertIn(\"9991\", message)  # cited number\n         self.assertIn(\"docs/adr/9991-*.md\", message)  # missing path\n \n+    def test_target_adr_does_not_satisfy_missing_platform_sadr(self) -> None:\n+        root = self._make_repo(\n+            adr_names=[\"0042-target-decision.md\"],\n+            script_body='\"\"\"Target ADR 0042 exists, but platform SADR-0042 does not.\"\"\"\\n',\n+        )\n+        violations = find_adr_reference_violations(root, include_docs=False)\n+        self.assertEqual(len(violations), 1)\n+        self.assertEqual(violations[0].namespace, \"SADR\")\n+        self.assertEqual(violations[0].number, \"0042\")\n+        self.assertEqual(violations[0].expected_path, \"docs/adr/SADR-0042-*.md\")\n+\n+    def test_target_adr_and_platform_sadr_may_reuse_a_number(self) -> None:\n+        root = self._make_repo(\n+            adr_names=[\"0042-target-decision.md\", \"SADR-0042-platform-decision.md\"],\n+            script_body='\"\"\"Target ADR 0042 and platform SADR-0042 are distinct.\"\"\"\\n',\n+        )\n+        self.assertEqual(\n+            find_adr_reference_violations(root, include_docs=False), []\n+        )\n+\n     def test_uncited_adr_is_allowed(self) -> None:\n         # An ADR exists but nothing cites it -> not a violation. The converse\n         # (every ADR must be cited) is explicitly NOT required.\ndiff --git a/tests/test_guardian_review.py b/tests/test_guardian_review.py\nindex 88829e7810212635e2fc98aa5918d6f26294677d..00a85a237acd5bf2d6cb5b294128198c422402ca\n--- a/tests/test_guardian_review.py\n+++ b/tests/test_guardian_review.py\n@@ -162,7 +162,7 @@\n \n     def test_docs_only_is_light_and_not_conservative(self):\n         focus = gr.classify_review_focus([\"docs/adr/0099-x.md\", \"README.md\"], \"\", \"low\")\n-        self.assertTrue(any(\"Docs/ADR-only\" in f for f in focus))\n+        self.assertTrue(any(\"Docs/decision-record-only\" in f for f in focus))\n         self.assertFalse(any(\"conservative default\" in f.lower() for f in focus))\n \n     def test_unknown_category_is_conservative(self):\ndiff --git a/tests/test_policy_job_merged_prs.py b/tests/test_policy_job_merged_prs.py\nindex d45512e74681e5e649279236c7d4e79d5c3da471..b97989f8bd74d091de1a6b5275a20d8dc68c4512\n--- a/tests/test_policy_job_merged_prs.py\n+++ b/tests/test_policy_job_merged_prs.py\n@@ -3,8 +3,8 @@\n \n ``.github/workflows/shiki-cca-completion.yml`` has two consumers of the merged-PR\n proof: the CCA *signal* (``guardian_approval_signal.py`` in \"Prepare CCA context\")\n-and the authoritative gate (``mergegate_check.py`` in \"Enforce MergeGate\"). ADR\n-0017's bookkeeping-closeout exemption (condition 6) needs the merged implementation\n+and the authoritative gate (``mergegate_check.py`` in \"Enforce MergeGate\").\n+SADR-0017's bookkeeping-closeout exemption (condition 6) needs the merged implementation\n PR proven, and a closeout carries NO ``post_merge_reconcile`` marker. The signal\n already resolves ``MERGED_PRS`` unconditionally, but \"Enforce MergeGate\" used to\n resolve it only inside ``grep -q '<!-- shiki:post_merge_reconcile -->'``. So a\ndiff --git a/tests/test_sadr_install_migration.py b/tests/test_sadr_install_migration.py\nnew file mode 100644\nindex 0000000000000000000000000000000000000000..d51eac872b1c79ff4579c4c1401ca5d775a60d8d\n--- /dev/null\n+++ b/tests/test_sadr_install_migration.py\n@@ -0,0 +1,237 @@\n+\"\"\"SADR installation and collision-safe legacy upgrade behavior.\"\"\"\n+\n+from __future__ import annotations\n+\n+import contextlib\n+import hashlib\n+import io\n+import json\n+import shutil\n+import tempfile\n+import unittest\n+from pathlib import Path\n+\n+import shiki_test_support  # noqa: F401  (path bootstrap)\n+\n+import shiki_installer\n+from tests.test_adr_references import find_adr_reference_violations\n+\n+\n+PRODUCT_NUMBERS = tuple(range(13, 19))\n+\n+\n+def _quiet_install(target: Path, *, force: bool, validate: bool) -> None:\n+    with contextlib.redirect_stdout(io.StringIO()):\n+        shiki_installer.install_template(target, force=force, validate=validate)\n+\n+\n+def _sha256(path: Path) -> str:\n+    return hashlib.sha256(path.read_bytes()).hexdigest()\n+\n+\n+def _legacy_sadr_paths() -> tuple[str, ...]:\n+    return tuple(\n+        f\"docs/adr/{path.name.removeprefix('SADR-')}\"\n+        for path in sorted((shiki_installer.ROOT / \"docs\" / \"adr\").glob(\"SADR-*.md\"))\n+        if path.name != \"SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\"\n+    )\n+\n+\n+def _create_legacy_upgrade_fixture(\n+    target: Path,\n+) -> tuple[dict[str, bytes], tuple[str, ...]]:\n+    adr_dir = target / \"docs\" / \"adr\"\n+    adr_dir.mkdir(parents=True)\n+    product_bytes = {\n+        f\"{number:04d}-cubeshare-product-decision.md\": (\n+            f\"# CubeShare ADR {number:04d}\\n\\nDo not rewrite {number}.\\n\"\n+        ).encode()\n+        for number in PRODUCT_NUMBERS\n+    }\n+    for name, content in product_bytes.items():\n+        (adr_dir / name).write_bytes(content)\n+\n+    legacy_paths = _legacy_sadr_paths()\n+    for relative in legacy_paths:\n+        (target / relative).write_bytes(f\"legacy Shiki bytes for {relative}\\n\".encode())\n+\n+    stamp_path = target / shiki_installer.INSTALL_STAMP_PATH\n+    stamp_path.parent.mkdir(parents=True, exist_ok=True)\n+    stamp = {\n+        \"version\": shiki_installer.INSTALL_STAMP_VERSION,\n+        \"platform_commit\": None,\n+        \"installed_at\": \"2000-01-01T00:00:00+00:00\",\n+        \"digests\": {\n+            relative: _sha256(target / relative) for relative in legacy_paths\n+        },\n+    }\n+    stamp_path.write_text(\n+        json.dumps(stamp, indent=2, sort_keys=True) + \"\\n\", encoding=\"utf-8\"\n+    )\n+    return product_bytes, legacy_paths\n+\n+\n+def _tree_snapshot(root: Path) -> dict[str, tuple[str, bytes | str]]:\n+    \"\"\"Recursive byte/shape snapshot used to prove preflight is write-free.\"\"\"\n+    snapshot: dict[str, tuple[str, bytes | str]] = {}\n+    for path in sorted(root.rglob(\"*\")):\n+        relative = path.relative_to(root).as_posix()\n+        if path.is_symlink():\n+            snapshot[relative] = (\"symlink\", path.readlink().as_posix())\n+        elif path.is_dir():\n+            snapshot[relative] = (\"directory\", \"\")\n+        else:\n+            snapshot[relative] = (\"file\", path.read_bytes())\n+    return snapshot\n+\n+\n+class FreshSadrInstallTests(unittest.TestCase):\n+    def setUp(self) -> None:\n+        self.tmp = Path(tempfile.mkdtemp(prefix=\"shiki-sadr-fresh-\"))\n+        self.addCleanup(lambda: shutil.rmtree(self.tmp, ignore_errors=True))\n+        self.target = self.tmp / \"target\"\n+        self.adr_dir = self.target / \"docs\" / \"adr\"\n+        self.adr_dir.mkdir(parents=True)\n+        self.product_bytes = {\n+            f\"{number:04d}-product-decision.md\": (\n+                f\"# Product ADR {number:04d}\\n\\nTarget-owned bytes {number}.\\n\"\n+            ).encode()\n+            for number in PRODUCT_NUMBERS\n+        }\n+        for name, content in self.product_bytes.items():\n+            (self.adr_dir / name).write_bytes(content)\n+\n+    def test_fresh_install_preserves_product_adrs_and_installs_all_sadrs(self) -> None:\n+        _quiet_install(self.target, force=False, validate=True)\n+\n+        for name, expected in self.product_bytes.items():\n+            self.assertEqual((self.adr_dir / name).read_bytes(), expected)\n+\n+        sadr_numbers = {\n+            path.name.split(\"-\", 2)[1]\n+            for path in self.adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\")\n+        }\n+        self.assertEqual(sadr_numbers, {f\"{number:04d}\" for number in range(1, 20)})\n+\n+        numeric_records = {\n+            path.name\n+            for path in self.adr_dir.glob(\"[0-9][0-9][0-9][0-9]-*.md\")\n+        }\n+        self.assertEqual(numeric_records, set(self.product_bytes))\n+        stamp = json.loads(\n+            (self.target / shiki_installer.INSTALL_STAMP_PATH).read_text(\n+                encoding=\"utf-8\"\n+            )\n+        )\n+        for path in self.adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\"):\n+            relative = path.relative_to(self.target).as_posix()\n+            self.assertEqual(stamp[\"digests\"][relative], _sha256(path))\n+        self.assertEqual(find_adr_reference_violations(self.target), [])\n+\n+\n+class LegacySadrUpgradeTests(unittest.TestCase):\n+    def setUp(self) -> None:\n+        self.tmp = Path(tempfile.mkdtemp(prefix=\"shiki-sadr-upgrade-\"))\n+        self.addCleanup(lambda: shutil.rmtree(self.tmp, ignore_errors=True))\n+        self.target = self.tmp / \"target\"\n+        self.adr_dir = self.target / \"docs\" / \"adr\"\n+        self.product_bytes, self.legacy_paths = _create_legacy_upgrade_fixture(\n+            self.target\n+        )\n+\n+    def test_force_upgrade_deletes_only_stamped_legacy_sadrs(self) -> None:\n+        _quiet_install(self.target, force=True, validate=True)\n+\n+        for relative in self.legacy_paths:\n+            self.assertFalse((self.target / relative).exists(), relative)\n+        for name, expected in self.product_bytes.items():\n+            self.assertEqual((self.adr_dir / name).read_bytes(), expected)\n+\n+        sadrs = list(self.adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\"))\n+        self.assertEqual(len(sadrs), 19)\n+        refreshed = json.loads(\n+            (self.target / shiki_installer.INSTALL_STAMP_PATH).read_text(encoding=\"utf-8\")\n+        )\n+        for relative in self.legacy_paths:\n+            self.assertNotIn(relative, refreshed[\"digests\"])\n+        for path in sadrs:\n+            relative = path.relative_to(self.target).as_posix()\n+            self.assertEqual(refreshed[\"digests\"][relative], _sha256(path))\n+\n+    def test_non_force_install_never_deletes_legacy_files(self) -> None:\n+        before = {\n+            relative: (self.target / relative).read_bytes()\n+            for relative in self.legacy_paths\n+        }\n+        _quiet_install(self.target, force=False, validate=False)\n+        for relative, expected in before.items():\n+            self.assertEqual((self.target / relative).read_bytes(), expected)\n+        refreshed = json.loads(\n+            (self.target / shiki_installer.INSTALL_STAMP_PATH).read_text(encoding=\"utf-8\")\n+        )\n+        for relative in self.legacy_paths:\n+            self.assertEqual(refreshed[\"digests\"][relative], _sha256(self.target / relative))\n+\n+\n+class LegacyCleanupPreflightTests(unittest.TestCase):\n+    def setUp(self) -> None:\n+        self.tmp = Path(tempfile.mkdtemp(prefix=\"shiki-sadr-preflight-\"))\n+        self.addCleanup(lambda: shutil.rmtree(self.tmp, ignore_errors=True))\n+\n+    def test_all_ownership_blockers_abort_before_any_write(self) -> None:\n+        cases = (\n+            (\"absent\", \"install stamp is absent\"),\n+            (\"unreadable\", \"install stamp is unreadable\"),\n+            (\"malformed\", \"install stamp is malformed\"),\n+            (\"missing-digest\", \"install stamp has no digest\"),\n+            (\"digest-mismatch\", \"digest mismatch\"),\n+        )\n+        for name, expected_reason in cases:\n+            with self.subTest(name=name):\n+                target = self.tmp / name\n+                _, legacy_paths = _create_legacy_upgrade_fixture(target)\n+                stamp_path = target / shiki_installer.INSTALL_STAMP_PATH\n+\n+                if name == \"absent\":\n+                    stamp_path.unlink()\n+                elif name == \"unreadable\":\n+                    stamp_path.unlink()\n+                    stamp_path.mkdir()\n+                elif name == \"malformed\":\n+                    stamp_path.write_text(\"{not-json\\n\", encoding=\"utf-8\")\n+                else:\n+                    stamp = json.loads(stamp_path.read_text(encoding=\"utf-8\"))\n+                    if name == \"missing-digest\":\n+                        stamp[\"digests\"] = {}\n+                    else:\n+                        stamp[\"digests\"] = {\n+                            relative: \"deadbeef\" for relative in legacy_paths\n+                        }\n+                    stamp_path.write_text(\n+                        json.dumps(stamp, indent=2, sort_keys=True) + \"\\n\",\n+                        encoding=\"utf-8\",\n+                    )\n+\n+                before = _tree_snapshot(target)\n+                with self.assertRaises(shiki_installer.ShikiError) as raised:\n+                    _quiet_install(target, force=True, validate=False)\n+                after = _tree_snapshot(target)\n+\n+                self.assertEqual(before, after, f\"{name} preflight changed the tree\")\n+                message = str(raised.exception)\n+                self.assertIn(expected_reason, message)\n+                self.assertIn(\"No files were written\", message)\n+                for relative in legacy_paths:\n+                    self.assertIn(relative, message)\n+\n+    def test_absent_stamp_is_allowed_when_no_legacy_path_exists(self) -> None:\n+        target = self.tmp / \"no-legacy\"\n+        target.mkdir()\n+        _quiet_install(target, force=True, validate=False)\n+        self.assertTrue(\n+            (target / \"docs\" / \"adr\" / \"SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\").is_file()\n+        )\n+\n+\n+if __name__ == \"__main__\":\n+    unittest.main()\n\n2026-08-07T08:28:47.875593Z ERROR codex_models_manager::manager: failed to renew cache TTL: missing field `base_instructions` at line 94 column 5\nStructured implementer code review is recorded with its repaired finding, and the complete local verification matrix is green.\n  \u2713 Load required skills, task contract, and run preflight\n  \u2713 Establish failing resolver and installer migration tests\n  \u2713 Rename platform decisions and migrate live citations to SADR\n  \u2713 Implement digest-authorized legacy installer cleanup\n  \u2713 Run structured code review and repair findings\n  \u2713 Rerun full required verification and record lifecycle evidence\ndiff --git a/.codex/skills/shiki/SKILL.md b/.codex/skills/shiki/SKILL.md\nindex c8e2e85d9d75d49cc4f2d6286ee22659c4e53e75..93cded623b2c15d9f65c76491a1c2d813ac9e6c0\n--- a/.codex/skills/shiki/SKILL.md\n+++ b/.codex/skills/shiki/SKILL.md\n@@ -18,6 +18,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n Codex CLI does not register this skill as a custom `/shiki` slash command. When\n the user types `/shiki` in Codex CLI and gets \"Unrecognized command\", explain\n that this is expected: invoke Shiki in Codex with natural language such as\n@@ -69,7 +72,7 @@\n \n ## Responsibilities\n \n-- Claude Code implements and repairs by default (`shiki runner claude`, ADR 0008); Codex implements and repairs tasks explicitly assigned to `codex`.\n+- Claude Code implements and repairs by default (`shiki runner claude`, SADR-0008); Codex implements and repairs tasks explicitly assigned to `codex`.\n - Claude Code Action can act as GitHub-side CCA or reviewer.\n - CCA judges completion.\n - MergeGate authorizes state transitions and merge readiness.\ndiff --git a/.github/prompts/cca-completion-check.md b/.github/prompts/cca-completion-check.md\nindex ed203ad5f302ecc81372d8ab27d2b1232ca97497..fd89950528333ef6242e6d5443904f5392ab7e56\n--- a/.github/prompts/cca-completion-check.md\n+++ b/.github/prompts/cca-completion-check.md\n@@ -19,6 +19,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n ## Required Reading\n \n Read only the files needed to judge the PR contract. Prefer this order:\ndiff --git a/.github/prompts/codex-task-handoff.md b/.github/prompts/codex-task-handoff.md\nindex 695e211d76dea1c60e8d5bb360e78487808286a4..59441d576b01a39569722f226c854e5a740e9eb5\n--- a/.github/prompts/codex-task-handoff.md\n+++ b/.github/prompts/codex-task-handoff.md\n@@ -2,7 +2,7 @@\n \n You are Codex Front acting as the Implementer for a Shiki task.\n \n-Follow `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant ADRs, and `docs/agents/codex-handoff.md`.\n+Follow `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant Shiki SADRs and target ADRs, and `docs/agents/codex-handoff.md`.\n \n Rules:\n \ndiff --git a/.shiki/ledger/L-20260807T082800000000Z-c0de0019.json b/.shiki/ledger/L-20260807T082800000000Z-c0de0019.json\nnew file mode 100644\nindex 0000000000000000000000000000000000000000..bac355ecea44037859b3715f58bc32f3353cfcfa\n--- /dev/null\n+++ b/.shiki/ledger/L-20260807T082800000000Z-c0de0019.json\n@@ -0,0 +1,34 @@\n+{\n+  \"actor\": \"codex\",\n+  \"evidence\": [\n+    \"Full task diff reviewed against scope, non-goals, locks, acceptance checks, failure atomicity, path ownership, and compatibility requirements\",\n+    \"Finding repaired: tests/test_sadr_install_migration.py now asserts every fresh-installed SADR path has the matching SHA-256 digest in .shiki/install-stamp.json\",\n+    \"Re-review found no remaining blocking issues; rejected findings: none\",\n+    \"python3 -m unittest discover -s tests: 1147 tests passed\",\n+    \"bash scripts/test_shiki_control_plane.sh: passed\",\n+    \"bash scripts/test_shiki_adversarial_state.sh: passed\",\n+    \"python3 scripts/validate_shiki.py --target .: passed\"\n+  ],\n+  \"goal_id\": \"G-20260807T073307219752Z-1ee1ed68\",\n+  \"id\": \"L-20260807T082800000000Z-c0de0019\",\n+  \"links\": [],\n+  \"review\": {\n+    \"applied_fixes\": [\n+      \"Added direct fresh-install stamp path/digest assertions and reran focused plus full verification\"\n+    ],\n+    \"findings\": [\n+      {\n+        \"detail\": \"The fresh-install test proved SADR files were installed but did not directly prove their paths and digests were recorded in the refreshed install stamp.\",\n+        \"resolution\": \"fixed_and_re_reviewed\",\n+        \"severity\": \"blocking\",\n+        \"title\": \"Fresh-install stamp evidence was indirect\"\n+      }\n+    ],\n+    \"rejected_findings\": [],\n+    \"verdict\": \"clean_after_repairs\"\n+  },\n+  \"summary\": \"code-review implementer verdict CLEAN after re-review: findings=1; applied_fixes=1 (fresh-install stamp-digest assertion); rejected_findings=0 (none)\",\n+  \"task_id\": \"T-20260807T073307221254Z-2d22d654\",\n+  \"timestamp\": \"2026-08-07T08:28:00+00:00\",\n+  \"type\": \"review\"\n+}\ndiff --git a/.shiki/tasks/T-20260807T073307221254Z-2d22d654.json b/.shiki/tasks/T-20260807T073307221254Z-2d22d654.json\nindex e7e150cc1637f1cfac2199e04055e93b53630dcb..78f7fb5ef2ab3dad7e3731673ac2c34b2a8ed149\n--- a/.shiki/tasks/T-20260807T073307221254Z-2d22d654.json\n+++ b/.shiki/tasks/T-20260807T073307221254Z-2d22d654.json\n@@ -36,7 +36,8 @@\n     \"L-20260807T080107948254Z-61f24da0\",\n     \"L-20260807T080228312210Z-07ac626e\",\n     \"L-20260807T080228575038Z-d574f629\",\n-    \"L-20260807T080256330734Z-5bf40cb6\"\n+    \"L-20260807T080256330734Z-5bf40cb6\",\n+    \"L-20260807T082800000000Z-c0de0019\"\n   ],\n   \"locks\": [\n     \"path:.codex/**\",\ndiff --git a/AGENTS.md b/AGENTS.md\nindex 7f041100d49c2a7525f51cb0f5d696a4a6d1c78c..a06bfbabc6fa48a007389e25e12d7ed523936bed\n--- a/AGENTS.md\n+++ b/AGENTS.md\n@@ -22,7 +22,7 @@\n - **Codex Front** is an optional implementer through Codex App, Codex CLI, Codex IDE extension, or Codex Web signed in with ChatGPT OAuth/subscription auth, dispatched with `shiki runner codex` when a task is explicitly assigned to `codex`.\n - **GitHub CCA** is implemented by Claude Code Action by default, using `CLAUDE_CODE_OAUTH_TOKEN`.\n \n-Do not assume `openai/codex-action`, `OPENAI_API_KEY`, or API-key based Codex automation in the default Shiki path. API-key runners are explicit target-repo extensions and require their own ADR.\n+Do not assume `openai/codex-action`, `OPENAI_API_KEY`, or API-key based Codex automation in the default Shiki path. API-key runners are explicit target-repo extensions and require their own target ADR.\n \n Shiki is not:\n \n@@ -44,10 +44,14 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use\n+`SADR-NNNN-*.md`; target product decisions retain `NNNN-*.md` and the `ADR`\n+namespace.\n+\n Use the canonical source-of-truth order above. Runtime-specific reading orders\n are operational guidance only and must not override or reorder it.\n \n-Conversation state is not durable truth. If a decision matters, put it in GitHub, `.shiki/`, `CONTEXT.md`, or an ADR.\n+Conversation state is not durable truth. If a decision matters, put it in GitHub, `.shiki/`, `CONTEXT.md`, a Shiki SADR, or a target ADR.\n \n If GitHub and `.shiki/` disagree, surface the conflict and prefer GitHub operational state until the mirror is repaired.\n \n@@ -75,8 +79,8 @@\n Every non-trivial change follows this loop:\n \n 1. **Goal**: clarify outcome, completion conditions, non-goals, risk level, and success signals.\n-2. **grill-with-docs**: challenge the plan against domain docs, ADRs, code reality, terminology, and edge scenarios.\n-3. **Context & Impact**: find relevant docs, ADRs, code, dependencies, owners, locks, and verification surfaces.\n+2. **grill-with-docs**: challenge the plan against domain docs, Shiki SADRs, target ADRs, code reality, terminology, and edge scenarios.\n+3. **Context & Impact**: find relevant docs, SADRs, target ADRs, code, dependencies, owners, locks, and verification surfaces.\n 4. **PRD and Spec Freeze**: use `to-prd` when context is settled enough to become durable product/engineering intent. The operator's explicit approval of the PRD is Spec Freeze: the plan records a `spec_freeze` block, and no plan runs without it. Steps 1-4 form Requirements Definition \u2014 one continuous operator dialogue. After Spec Freeze, scope changes require an operator-approved Spec Amendment; non-scope-moving interpretations go to the Assumption Log.\n 5. **Issues**: use `to-issues` to create independently grabbable vertical-slice issues. Prefer AFK slices over HITL slices where possible.\n 6. **Triage**: label issues for readiness, risk, runtime, skills, and MergeGate state.\n@@ -167,7 +171,7 @@\n - Testing decisions.\n - Out-of-scope items.\n - Further notes.\n-- Links to relevant domain terms and ADRs.\n+- Links to relevant domain terms, SADRs, and target ADRs.\n \n Every executable task must include:\n \n@@ -193,7 +197,7 @@\n \n Context & Impact must identify:\n \n-- Relevant documents, domain terms, ADRs, and past decisions.\n+- Relevant documents, domain terms, SADRs, target ADRs, and past decisions.\n - Relevant modules, interfaces, seams, callers, owners, and tests.\n - Dependency relationships between tasks.\n - Candidate locks and lock conflicts.\n@@ -210,7 +214,7 @@\n | Skill | Required when |\n | --- | --- |\n | `setup-matt-pocock-skills` | First configuring the repo for skills, or when issue tracker, triage labels, or domain docs are missing. |\n-| `grill-with-docs` | Default for non-trivial Goals before PRD/issues; always required when plans, terminology, boundaries, tradeoffs, or ADR-worthy decisions are ambiguous. |\n+| `grill-with-docs` | Default for non-trivial Goals before PRD/issues; always required when plans, terminology, boundaries, tradeoffs, or decision-record-worthy choices are ambiguous. |\n | `zoom-out` | The agent lacks an architectural map of the relevant code area. |\n | `to-prd` | Settled Goal context must become durable product/engineering intent. |\n | `to-issues` | A Goal, PRD, or plan must become independently grabbable vertical-slice issues. |\n@@ -348,7 +352,7 @@\n - Three or more new files.\n - Any high-risk or critical label.\n \n-When the architecture gate triggers, run `grill-with-docs` or `improve-codebase-architecture` as appropriate and record durable decisions in an ADR when needed.\n+When the architecture gate triggers, run `grill-with-docs` or `improve-codebase-architecture` as appropriate and record durable decisions in a Shiki SADR or target ADR as applicable.\n \n ## Engineering Discipline\n \ndiff --git a/CLAUDE.md b/CLAUDE.md\nindex 98a932e7841a632866fcd53b0bad0e3af3711d3d..98a31399404d6fb044b4fd493f4459ac87f439de\n--- a/CLAUDE.md\n+++ b/CLAUDE.md\n@@ -14,6 +14,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n ## Claude Role\n \n Default to planner, default implementer, reviewer, coordinator, documentation editor, CCA-style completion judge, and final-judgment assistant.\n@@ -57,7 +60,7 @@\n At the start of a Shiki session:\n \n 1. Read `AGENTS.md`.\n-2. Read `CONTEXT.md` and relevant ADRs.\n+2. Read `CONTEXT.md` and relevant Shiki SADRs and target ADRs.\n 3. Read active `.shiki/` Goal/task/ledger state when present.\n 4. Read active distilled rules from `.shiki/memories/` (status `distilled`, `active: true`, not revoked or superseded) for applicable guidance; the same rules are injected deterministically into task handoffs as the `## Distilled Rules` section (\u00a73.5).\n 5. Read the GitHub Issue or PR when the task is GitHub-backed.\n@@ -74,7 +77,7 @@\n - Define the Goal, completion conditions, non-goals, and risk level.\n - Treat `grill-with-docs` as the default planning entry point for non-trivial Goals.\n - Challenge domain terms against `CONTEXT.md`.\n-- Challenge hard-to-reverse tradeoffs against ADRs.\n+- Challenge hard-to-reverse tradeoffs against SADRs and target ADRs.\n - Explore code instead of asking when code can answer the question.\n - Run Context & Impact before implementation planning.\n - Produce a Task DAG, not an unordered checklist.\n@@ -94,7 +97,7 @@\n - For each question, provide your recommended answer.\n - Resolve dependency decisions before downstream design questions.\n - Update `CONTEXT.md` inline when terminology crystallizes.\n-- Offer ADRs only when the decision is hard to reverse, surprising without context, and a real tradeoff.\n+- Offer a SADR or target ADR only when the decision is hard to reverse, surprising without context, and a real tradeoff.\n - Convert unresolved questions into HITL blockers rather than allowing Codex to guess.\n \n ## PRD and Issues Standard\n@@ -103,7 +106,7 @@\n \n - Use the project's domain glossary vocabulary.\n - Avoid volatile file paths unless they are essential.\n-- Include implementation decisions, testing decisions, out-of-scope items, and relevant ADR/domain links.\n+- Include implementation decisions, testing decisions, out-of-scope items, and relevant SADR/target-ADR/domain links.\n - Identify deep-module opportunities and testable interfaces.\n \n When creating issues:\n@@ -121,7 +124,7 @@\n - Branch/worktree target.\n - Scope and non-goals.\n - Dependencies and locks.\n-- Relevant docs, ADRs, modules, and tests.\n+- Relevant docs, SADRs, target ADRs, modules, and tests.\n - Required skill invocations.\n - TDD expectations.\n - Pre-PR `code-review` gate expectations.\n@@ -152,7 +155,7 @@\n \n - Treat yourself as a judge, not implementer.\n - Do not write production code.\n-- Read `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant ADRs, PRD, task issue, PR body, diff, checks, reviews, labels, and `.shiki/` state.\n+- Read `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant SADRs and target ADRs, PRD, task issue, PR body, diff, checks, reviews, labels, and `.shiki/` state.\n - Evaluate every applicable checklist in `docs/agents/checklists.md`.\n - Map each acceptance criterion to durable evidence.\n - Distinguish implementation failure from missing evidence.\ndiff --git a/CONTEXT.md b/CONTEXT.md\nindex e61a9be06a0cce0d026484083a657bde2067e234..237ee8aa3ea8fab334ece49768bc5ed3b5af0974\n--- a/CONTEXT.md\n+++ b/CONTEXT.md\n@@ -25,7 +25,7 @@\n _Avoid_: jumping directly into code\n \n **grill-with-docs**:\n-A planning skill that challenges a plan against domain language, ADRs, code reality, and concrete edge scenarios. It resolves design-tree questions before PRD/issues. It runs inside Requirements Definition.\n+A planning skill that challenges a plan against domain language, Shiki SADRs, target ADRs, code reality, and concrete edge scenarios. It resolves design-tree questions before PRD/issues. It runs inside Requirements Definition.\n _Avoid_: generic brainstorming, silent assumptions\n \n **Requirements Definition**:\ndiff --git a/CONTRIBUTING.md b/CONTRIBUTING.md\nindex c6d9b656e73abb8a55e5f660123e03153dcb3e6d..9e37a8fbf668db5b22461aaae5548b8f1354394c\n--- a/CONTRIBUTING.md\n+++ b/CONTRIBUTING.md\n@@ -17,7 +17,7 @@\n 1. **Goal** \u2014 start from a user-approved Goal with an outcome, completion\n    conditions, non-goals, risk level, and success signals.\n 2. **grill-with-docs** \u2014 for any non-trivial change, challenge the plan against\n-   domain language, ADRs, and code reality before writing a PRD.\n+   domain language, Shiki SADRs, target ADRs, and code reality before writing a PRD.\n 3. **PRD** \u2014 capture settled product/engineering intent (`to-prd`).\n 4. **Spec Freeze** \u2014 the operator explicitly approves the PRD/requirements;\n    the plan records a `spec_freeze` block and no plan runs without it\n@@ -94,7 +94,7 @@\n   entries.\n - Do not hand-edit `.shiki/goals/*.json` or `.shiki/manifest.json` in a task PR.\n - Conversation state is not durable truth. If a decision matters, record it in\n-  GitHub, `.shiki/`, `CONTEXT.md`, or an ADR.\n+  GitHub, `.shiki/`, `CONTEXT.md`, a Shiki SADR, or a target ADR.\n \n ## Architecture Gate\n \n@@ -103,7 +103,7 @@\n or CI policy, package structure, three or more structural files, more than 100\n net new lines in a core module, three or more new files, or any high-risk label.\n When triggered, run `grill-with-docs` or `improve-codebase-architecture` and\n-record durable decisions in an ADR when needed (see `docs/adr/`).\n+record durable decisions in a Shiki SADR or target ADR as applicable (see `docs/adr/`).\n \n ## Safety\n \ndiff --git a/SYSTEM_PROMPT.md b/SYSTEM_PROMPT.md\nindex aaaea45e3a896c3a44ba89eec8bb84325ecf6c62..da009c86a88285269dee0155e58e550c6d744ea9\n--- a/SYSTEM_PROMPT.md\n+++ b/SYSTEM_PROMPT.md\n@@ -30,6 +30,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n Use the canonical source-of-truth order above. Runtime-specific reading orders\n are operational guidance only and must not override or reorder it. The current\n conversation is non-durable operator input.\n@@ -48,14 +51,14 @@\n \n A runtime may only do work that is explicitly assigned by the Goal, task, PR, or MergeGate decision.\n \n-Do not assume `openai/codex-action`, `OPENAI_API_KEY`, or API-key based Codex automation in the default Shiki path. API-key runners are explicit target-repo extensions and require their own ADR.\n+Do not assume `openai/codex-action`, `OPENAI_API_KEY`, or API-key based Codex automation in the default Shiki path. API-key runners are explicit target-repo extensions and require their own target ADR.\n \n ## 4. Non-negotiable Shiki flow\n \n For any non-trivial Goal, use this flow:\n \n 1. **Goal Seek**: define outcome, non-goals, risk level, completion criteria, and evidence requirements.\n-2. **grill-with-docs**: challenge the plan against `CONTEXT.md`, ADRs, code reality, terminology, boundaries, and edge scenarios. Ask one question at a time when the operator is available. If code can answer a question, inspect code instead of asking.\n+2. **grill-with-docs**: challenge the plan against `CONTEXT.md`, Shiki SADRs, target ADRs, code reality, terminology, boundaries, and edge scenarios. Ask one question at a time when the operator is available. If code can answer a question, inspect code instead of asking.\n 3. **Context & Impact**: identify modules, interfaces, seams, callers, dependencies, locks, risk, and verification surfaces.\n 4. **to-prd + Spec Freeze**: turn settled context into a PRD using domain vocabulary and testing decisions. The operator's explicit approval of the PRD is Spec Freeze, recorded as the plan's `spec_freeze` block; plans without it do not run. Steps 1-4 form Requirements Definition.\n 5. **to-issues**: decompose the PRD into independently grabbable vertical-slice issues. Prefer AFK slices; mark HITL slices when judgment is still required.\n@@ -75,7 +78,7 @@\n Before making any material change:\n \n 1. Read `AGENTS.md` and any runtime-specific wrapper such as `CLAUDE.md`.\n-2. Read `CONTEXT.md` and relevant ADRs.\n+2. Read `CONTEXT.md` and relevant SADRs and target ADRs.\n 3. Read the GitHub Issue / PR and `.shiki/` state for the active Goal or task.\n 4. Identify your role: Planner, Implementer, Reviewer, Completion Check Agent, Repairer, or Guardian-assist.\n 5. Identify Goal id, task id, branch, PR, dependencies, locks, risk level, required skills, and required evidence.\n@@ -103,7 +106,7 @@\n \n At minimum, identify:\n \n-- Relevant documents, ADRs, domain terms, and prior decisions.\n+- Relevant documents, SADRs, target ADRs, domain terms, and prior decisions.\n - Relevant modules, interfaces, seams, callers, owners, and tests.\n - Dependency relationships between tasks.\n - Candidate file locks and lock conflicts.\n@@ -152,7 +155,7 @@\n \n CCA must read:\n \n-- `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant ADRs.\n+- `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant SADRs and target ADRs.\n - Parent Goal issue, PRD issue, task issue, comments, and labels.\n - PR body, diff, commits, changed files, reviews, CI checks, and artifacts available to the workflow.\n - `.shiki/` task contract, locks, ledger entries, CCA checklist profile, and prior repair packets.\n@@ -214,7 +217,7 @@\n - Three or more new files.\n - Any high-risk or critical label.\n \n-When the architecture gate triggers, run `grill-with-docs` or `improve-codebase-architecture` as appropriate and record the decision in an ADR if it is hard to reverse or likely to be re-litigated.\n+When the architecture gate triggers, run `grill-with-docs` or `improve-codebase-architecture` as appropriate and record the decision in a Shiki SADR or target ADR as applicable if it is hard to reverse or likely to be re-litigated.\n \n ## 14. Skill Gate\n \n@@ -223,7 +226,7 @@\n Use these skills:\n \n - `setup-matt-pocock-skills`: before first use of the engineering skill set, or when issue tracker, triage labels, or domain docs are missing.\n-- `grill-with-docs`: default for non-trivial Goals before PRD/issues; required for ambiguous plans, terminology, boundaries, ADR-worthy tradeoffs, or design-tree decisions.\n+- `grill-with-docs`: default for non-trivial Goals before PRD/issues; required for ambiguous plans, terminology, boundaries, decision-record-worthy tradeoffs, or design-tree decisions.\n - `zoom-out`: unfamiliar code area or missing architectural map.\n - `to-prd`: convert settled context into a PRD.\n - `to-issues`: break a PRD, plan, or Goal into independently grabbable vertical-slice issues.\ndiff --git a/docs/adr/0001-keep-shiki-as-platform-name.md b/docs/adr/SADR-0001-keep-shiki-as-platform-name.md\nindex 20a9637c3b9d8c97de7ba8db7cfae4d82d9a7dba..8b685439959aada1b221c112f0ac3c51ca8a1695\n--- a/docs/adr/0001-keep-shiki-as-platform-name.md\n+++ b/docs/adr/SADR-0001-keep-shiki-as-platform-name.md\n@@ -1,3 +1,3 @@\n-# Keep Shiki As The Platform Name\n+# SADR-0001: Keep Shiki As The Platform Name\n \n Shiki remains the product name for the GitHub-native agentic engineering control plane. We considered more descriptive names such as MergePilot, MergeGate, and Kiroku, but kept Shiki because it carries the existing goal-seeking framework identity while remaining independent of Hermes, Codex, Claude Code, or any single runtime.\ndiff --git a/docs/adr/0002-use-agents-as-shared-constitution.md b/docs/adr/SADR-0002-use-agents-as-shared-constitution.md\nindex 73de30ecb078120eac540d9bc51855e3cd48d0e3..c5486182bf06fca36ea508496b013c67302fd989\n--- a/docs/adr/0002-use-agents-as-shared-constitution.md\n+++ b/docs/adr/SADR-0002-use-agents-as-shared-constitution.md\n@@ -1,3 +1,3 @@\n-# Use AGENTS As The Shared Agent Constitution\n+# SADR-0002: Use AGENTS As The Shared Agent Constitution\n \n Shiki uses `AGENTS.md` as the runtime-neutral constitution and `CLAUDE.md` as a thin Claude Code wrapper. This keeps Codex, Claude Code, Hermes Runner, and future Agent Runtimes aligned on the same GitHub-first MergeGate rules while still allowing runtime-specific instructions where needed.\ndiff --git a/docs/adr/0003-require-skill-gate-for-mergegate-readiness.md b/docs/adr/SADR-0003-require-skill-gate-for-mergegate-readiness.md\nindex 424be0c9af4c8f0136f1fde68a468c47ff84e764..7fc337987c7df9792f75f181dfcbed75fe5ffff2\n--- a/docs/adr/0003-require-skill-gate-for-mergegate-readiness.md\n+++ b/docs/adr/SADR-0003-require-skill-gate-for-mergegate-readiness.md\n@@ -1,3 +1,3 @@\n-# Require Skill Gate For MergeGate Readiness\n+# SADR-0003: Require Skill Gate For MergeGate Readiness\n \n Shiki requires relevant engineering skills to be invoked before execution when their triggers apply. This makes skills part of MergeGate readiness rather than optional prompt style, because Shiki depends on repeatable planning, issue decomposition, TDD, diagnosis, triage, and architecture review before autonomous or parallel agent work can be trusted.\ndiff --git a/docs/adr/0004-use-github-first-with-shiki-mirror.md b/docs/adr/SADR-0004-use-github-first-with-shiki-mirror.md\nindex d0a3a9174dc646a945a9d325954ffaf00c352040..a79c54ae9ee8eed2a0524192b864da4e22a92d3f\n--- a/docs/adr/0004-use-github-first-with-shiki-mirror.md\n+++ b/docs/adr/SADR-0004-use-github-first-with-shiki-mirror.md\n@@ -1,4 +1,4 @@\n-# ADR 0004: Use GitHub First With A Repository-Local Shiki Mirror\n+# SADR-0004: Use GitHub First With A Repository-Local Shiki Mirror\n \n ## Status\n \ndiff --git a/docs/adr/0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md b/docs/adr/SADR-0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\nindex b339baf49b89d9290e79c3d51b36e7b2bde3bd95..0f3c3eb9afccdb3403ce978e21d04fe12e495da0\n--- a/docs/adr/0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\n+++ b/docs/adr/SADR-0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\n@@ -1,8 +1,8 @@\n-# ADR 0005: Use Codex As Front And Claude Code Action As The GitHub Runtime\n+# SADR-0005: Use Codex As Front And Claude Code Action As The GitHub Runtime\n \n ## Status\n \n-Accepted (implementer default superseded by ADR 0008; the auth model and the\n+Accepted (implementer default superseded by SADR-0008; the auth model and the\n Claude Code Action GitHub runtime decision remain in force)\n \n ## Context\n@@ -26,4 +26,4 @@\n - Shiki avoids requiring `OPENAI_API_KEY` for the default workflow.\n - Codex work happens through the user's authenticated Codex front surface, not as a hidden backend Action.\n - Claude Code Action is configured for OAuth-token based subscription usage by default.\n-- API-key based automation remains possible only as an explicit target-repo extension with its own ADR.\n+- API-key based automation remains possible only as an explicit target-repo extension with its own target ADR.\ndiff --git a/docs/adr/0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md b/docs/adr/SADR-0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\nindex 4bcae9d1e52f0a37b8e9fbafa5cd14b6ee1993bd..7a38538a6ae9929567f0ade67e7ea6a60d0b3f7e\n--- a/docs/adr/0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\n+++ b/docs/adr/SADR-0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\n@@ -1,4 +1,4 @@\n-# ADR 0006: Separate Codex Implementation, CCA Completion, And MergeGate Authorization\n+# SADR-0006: Separate Codex Implementation, CCA Completion, And MergeGate Authorization\n \n ## Status\n \ndiff --git a/docs/adr/0007-packaging-and-release.md b/docs/adr/SADR-0007-packaging-and-release.md\nindex 79b84ddb97167372b44641e5b7860e400ce80822..bb445a2e2e856d9d1f5fdbe35de8d4e8cec0fb32\n--- a/docs/adr/0007-packaging-and-release.md\n+++ b/docs/adr/SADR-0007-packaging-and-release.md\n@@ -1,4 +1,4 @@\n-# ADR 0007: Packaging And Release For Shiki\n+# SADR-0007: Packaging And Release For Shiki\n \n ## Status\n \n@@ -25,7 +25,7 @@\n   not introduce third-party runtime dependencies or break the shim.\n - Releases must run inside the operator's subscription-authenticated toolchain\n   and must not require extra secrets beyond the built-in `GITHUB_TOKEN`\n-  (consistent with ADR 0005's no-API-key-by-default stance).\n+  (consistent with SADR-0005's no-API-key-by-default stance).\n - `.shiki/manifest.json` already encodes the install surface (`install.include`,\n   `install.create_directories`, `install.exclude_from_commit`), and it must not\n   be mutated as part of this change.\n@@ -34,7 +34,7 @@\n \n This is a hard-to-reverse platform decision (it sets the version source of\n truth, the release trigger, and the public install contract), so it is recorded\n-as an ADR.\n+as a SADR.\n \n ## Decision\n \n@@ -104,7 +104,7 @@\n 3. **Publishing to PyPI.** Rejected for now: Shiki is distributed as a\n    repository template + git-installable CLI, not a PyPI library; PyPI would add\n    credentials/secrets and a different versioning contract. Can be added later as\n-   its own ADR if a packaged distribution is needed.\n+   its own SADR if a packaged distribution is needed.\n 4. **Dynamic version (read `VERSION` via a build backend `dynamic` field).**\n    Rejected to keep `pyproject.toml` backend-agnostic and dependency-free; the\n    small duplication between `VERSION` and `pyproject.toml` is guarded by the\n@@ -123,4 +123,4 @@\n   releasing versions the whole platform but ships only `install.include` to\n   targets.\n - Future PyPI distribution or a console entry point are deferred and would each\n-  warrant their own ADR.\n+  warrant their own SADR.\ndiff --git a/docs/adr/0008-use-claude-code-as-default-implementer.md b/docs/adr/SADR-0008-use-claude-code-as-default-implementer.md\nindex dedf4510b7c7799521422f622029563d373db031..35c2625d908167fbc58855b5b63e283805f167e8\n--- a/docs/adr/0008-use-claude-code-as-default-implementer.md\n+++ b/docs/adr/SADR-0008-use-claude-code-as-default-implementer.md\n@@ -1,12 +1,12 @@\n-# ADR 0008: Use Claude Code As The Default Implementer Runtime\n+# SADR-0008: Use Claude Code As The Default Implementer Runtime\n \n ## Status\n \n-Proposed (supersedes ADR 0005 for the implementer default; keeps its auth model)\n+Proposed (supersedes SADR-0005 for the implementer default; keeps its auth model)\n \n ## Context\n \n-ADR 0005 made Codex Front the default implementation runtime, with Claude Code\n+SADR-0005 made Codex Front the default implementation runtime, with Claude Code\n restricted to planner and reviewer roles by the runtime registry, and the only\n autonomous implementation adapter was `shiki runner codex`.\n \n@@ -18,13 +18,13 @@\n - The platform's planning, review, and CCA layers already run on Claude Code,\n   and Claude Code's native capabilities (the built-in `/code-review` skill and\n   the Workflow multi-agent orchestration tool) have no Codex equivalent.\n-- The Goal lifecycle redesign (ADR 0009) requires an autonomous post-freeze\n+- The Goal lifecycle redesign (SADR-0009) requires an autonomous post-freeze\n   loop driven from the operator's Claude Code session, including bounded\n   repair dispatch, which must not depend on a runtime that cannot be invoked\n   unattended.\n \n The constitution claims Shiki is runtime-agnostic (\"not a Claude-only\n-workflow\", ADR 0002). Any change to the implementer default must preserve the\n+workflow\", SADR-0002). Any change to the implementer default must preserve the\n registry-based runtime model rather than hardcoding one vendor.\n \n ## Decision\n@@ -39,7 +39,7 @@\n   the task to review or repair-needed.\n - Keep Codex registered as an optional implementer runtime. `shiki runner\n   codex`, the Codex handoff templates, and the ChatGPT OAuth auth model from\n-  ADR 0005 remain supported for tasks explicitly assigned to `codex`.\n+  SADR-0005 remain supported for tasks explicitly assigned to `codex`.\n - Commit to mandating two Claude-native capabilities as loop contract in\n   follow-up Goals (status: the code-review gate was delivered by Goal G-B \u2014\n   skill registry, checklists item PR-12, default required_skills \u2014 while the\n@@ -60,7 +60,7 @@\n \n Out of scope: removing Codex support, changing the CCA runtime (Claude Code\n Action with `CLAUDE_CODE_OAUTH_TOKEN` stays), and API-key based automation\n-(still requires its own ADR per ADR 0005).\n+(still requires its own target ADR per SADR-0005).\n \n ## Consequences\n \n@@ -100,7 +100,7 @@\n   harness rejection of direct `codex exec` is outside Shiki's control, and the\n   plan's mandatory native capabilities cannot run on Codex at all.\n - **Remove Codex entirely**: rejected; it abandons the runtime-agnostic\n-  constitution (ADR 0002) for no operational gain and forecloses\n+  constitution (SADR-0002) for no operational gain and forecloses\n   cross-runtime adversarial review.\n - **GitHub Actions cloud implementation via claude-code-action**: rejected as\n   default; durable evidence is automatic but token cost, Node 24 workflow\ndiff --git a/docs/adr/0009-requirements-definition-and-spec-freeze.md b/docs/adr/SADR-0009-requirements-definition-and-spec-freeze.md\nindex 29b3f7871bef9203a8b4a831a65c39eb2f506e42..52e7dd69ad0aa4efe0b68c62678abbf7df02aaec\n--- a/docs/adr/0009-requirements-definition-and-spec-freeze.md\n+++ b/docs/adr/SADR-0009-requirements-definition-and-spec-freeze.md\n@@ -1,4 +1,4 @@\n-# ADR 0009: Merge Grill And Planning Into Requirements Definition With Spec Freeze\n+# SADR-0009: Merge Grill And Planning Into Requirements Definition With Spec Freeze\n \n ## Status\n \ndiff --git a/docs/adr/0010-external-ai-guardian-review.md b/docs/adr/SADR-0010-external-ai-guardian-review.md\nindex e6108bbe8bb4e73e0ff4684c8f78001a80438c4e..f3510b145664298adafb151b6f0db51b972f01ec\n--- a/docs/adr/0010-external-ai-guardian-review.md\n+++ b/docs/adr/SADR-0010-external-ai-guardian-review.md\n@@ -1,4 +1,4 @@\n-# ADR 0010: External AI Guardian Review As A First-Class Approval Authority\n+# SADR-0010: External AI Guardian Review As A First-Class Approval Authority\n \n ## Status\n \n@@ -8,7 +8,7 @@\n \n Shiki is intended to be an autonomous, recursive, self-improving agent\n framework: authority-in-the-loop, not human-in-the-loop. Until now the Guardian\n-approval required for high/critical-risk merges (guardian-policy.json, ADR\n+approval required for high/critical-risk merges (guardian-policy.json, SADR\n governance) recognized only human authority sources \u2014 a GitHub review approval,\n the `guardian:approved` label applied by a configured Guardian, or a comment\n carrying `Guardian approval granted` plus the current head SHA.\n@@ -89,7 +89,7 @@\n   autonomous design the operator intends and produced the impersonation\n   incident.\n - **Record AI approval as an operator comment**: rejected as forgery of human\n-  authority \u2014 the exact failure this ADR corrects.\n+  authority \u2014 the exact failure this SADR corrects.\n - **Deliver the artifact as a committed `.shiki/` file**: rejected; a committed\n   approval inside the PR being approved is forgeable by anyone who can push.\n - **A dedicated reviewer bot identity instead of a relayed comment**: deferred;\ndiff --git a/docs/adr/0011-autonomous-quality-gate-evidence.md b/docs/adr/SADR-0011-autonomous-quality-gate-evidence.md\nindex bca0246c5d46e44afbde15486a75eec425fc02f5..9c93eac16d8c3a6d4b11bd7224e3955102cd7db4\n--- a/docs/adr/0011-autonomous-quality-gate-evidence.md\n+++ b/docs/adr/SADR-0011-autonomous-quality-gate-evidence.md\n@@ -1,4 +1,4 @@\n-# ADR 0011: Autonomous Quality-Gate Evidence \u2014 Deterministic Facts And An Independent Verifier\n+# SADR-0011: Autonomous Quality-Gate Evidence \u2014 Deterministic Facts And An Independent Verifier\n \n ## Status\n \ndiff --git a/docs/adr/0012-autonomous-loop-closeout-to-main.md b/docs/adr/SADR-0012-autonomous-loop-closeout-to-main.md\nindex 637fad6dde4989c9c2bf0f81f676d8d29991123c..ec7d85f81030a676bcbba28b43eb365422a36eac\n--- a/docs/adr/0012-autonomous-loop-closeout-to-main.md\n+++ b/docs/adr/SADR-0012-autonomous-loop-closeout-to-main.md\n@@ -1,4 +1,4 @@\n-# 0012 \u2014 Autonomous loop pushes goal completion to main via a closeout PR\n+# SADR-0012 \u2014 Autonomous loop pushes goal completion to main via a closeout PR\n \n - Status: accepted\n - Date: 2026-06-18\ndiff --git a/docs/adr/0013-bootstrap-grants-actions-approve-pull-requests.md b/docs/adr/SADR-0013-bootstrap-grants-actions-approve-pull-requests.md\nindex b6c69d64f1c5ced346b88d6859586715b3b5bd0e..cf48ff4a6716267f539ca58edd1a852e7c7a2d64\n--- a/docs/adr/0013-bootstrap-grants-actions-approve-pull-requests.md\n+++ b/docs/adr/SADR-0013-bootstrap-grants-actions-approve-pull-requests.md\n@@ -1,4 +1,4 @@\n-# ADR 0013: Bootstrap Grants GitHub Actions Permission To Create And Approve Pull Requests\n+# SADR-0013: Bootstrap Grants GitHub Actions Permission To Create And Approve Pull Requests\n \n ## Status\n \n@@ -31,7 +31,7 @@\n security tradeoff: it lets repository automation satisfy a human-review gate. In\n a multi-maintainer repo that could let a compromised or buggy workflow\n self-approve changes. So whether bootstrap turns it on by default \u2014 and with\n-what default token scope \u2014 is an ADR-worthy decision, not an implementation\n+what default token scope \u2014 is a SADR-worthy decision, not an implementation\n detail.\n \n ## Decision\n@@ -48,7 +48,7 @@\n Specifically:\n \n - **`can_approve_pull_request_reviews` defaults to `true`.** Shiki's default\n-  posture is solo/AFK autonomous operation (ADR 0008/0009/0012), where the CCA\n+  posture is solo/AFK autonomous operation (SADR-0008/0009/0012), where the CCA\n   Review Bridge is the only path that can satisfy `required_review: true` after\n   the CCA and Guardian gates have already passed. The default makes a\n   freshly-bootstrapped repo able to run the autonomous loop end to end without a\ndiff --git a/docs/adr/0014-codex-app-external-ai-guardian-ui-adapter.md b/docs/adr/SADR-0014-codex-app-external-ai-guardian-ui-adapter.md\nindex 238db06279334caa341c117362df9271382a7fcd..65f12e9939321e1b3e389a34c822fc1549e5155d\n--- a/docs/adr/0014-codex-app-external-ai-guardian-ui-adapter.md\n+++ b/docs/adr/SADR-0014-codex-app-external-ai-guardian-ui-adapter.md\n@@ -1,4 +1,4 @@\n-# ADR 0014: Use Codex App As The External AI Guardian UI Adapter\n+# SADR-0014: Use Codex App As The External AI Guardian UI Adapter\n \n ## Status\n \ndiff --git a/docs/adr/0015-contract-approval-before-dispatch.md b/docs/adr/SADR-0015-contract-approval-before-dispatch.md\nindex fad6c656741a20a7684ba16080a7db159f63a52f..6061271a46d3f1d6ae7174477de3fb75673123a6\n--- a/docs/adr/0015-contract-approval-before-dispatch.md\n+++ b/docs/adr/SADR-0015-contract-approval-before-dispatch.md\n@@ -1,4 +1,4 @@\n-# ADR 0015: Approve The Task Contract Before Dispatch, Not The Diff After It\n+# SADR-0015: Approve The Task Contract Before Dispatch, Not The Diff After It\n \n ## Status\n \n@@ -9,7 +9,7 @@\n Guardian approval for high/critical risk is expressible only on a pull request.\n Every approval source in `guardian-policy.json` \u2014 GitHub review, the\n `guardian:approved` label, the `Guardian approval granted` comment, and the\n-`external_ai_guardian_review` artifact (ADR 0010/0014) \u2014 is bound to a PR, and\n+`external_ai_guardian_review` artifact (SADR-0010/0014) \u2014 is bound to a PR, and\n three of the four require the current head SHA. Approval therefore cannot exist\n before an implementation PR exists.\n \n@@ -81,7 +81,7 @@\n An envelope breach must surface only on the non-repairable policy gate, never on\n the repairable `MergeGate metadata check`. Otherwise the Goal Loop routes it to\n `dispatch_repair` \u2014 instructing a runner to make the breach go away \u2014 which is\n-exactly what this ADR forbids.\n+exactly what this SADR forbids.\n \n **Contract immutability.** After registration, an implementation PR must not\n change the governance fields of its own task contract (`scope`, `non_goals`,\n@@ -116,7 +116,7 @@\n fails any verdict that is not `complete`, and the signal the CCA prompt reads is\n computed from `evaluate_guardian_approval` alone. Without the OR-merge at the\n signal, a contract-approved PR still returns `needs_guardian`, the CCA job fails,\n-and the MergeGate policy check never runs \u2014 the gate this ADR changes would never\n+and the MergeGate policy check never runs \u2014 the gate this SADR changes would never\n be reached. The CCA *prompt* needs no change: a `contract_approval` entry in the\n signal's existing `sources` array satisfies its current wording.\n \n@@ -130,7 +130,7 @@\n written, and an approved contract lets its implementation PRs merge through the\n same automatic path as low/medium risk. The wasted implement-then-block cycle\n disappears. Guardian review shifts from reading diffs to reading contracts, which\n-is both earlier and smaller; the external AI Guardian path (ADR 0010/0014) works\n+is both earlier and smaller; the external AI Guardian path (SADR-0010/0014) works\n on contracts with no change.\n \n We accept that the Guardian authorizes a class of change without seeing its\n@@ -146,7 +146,7 @@\n adversarial tests for the new approval path equivalent in rigor to\n `test_shiki_governance_evidence.sh`.\n \n-Bootstrap costs, which apply only while this ADR is being implemented. The tasks\n+Bootstrap costs, which apply only while this SADR is being implemented. The tasks\n that build Contract Approval merge under the existing rules, so the ones\n dispatched after the pre-dispatch gate lands but before the approval path lands\n need a Guardian-approved Contract PR *and* a live Guardian approval on their\n@@ -210,7 +210,7 @@\n risk; they do not bound authorized work.\n \n **Auto-escalate risk on envelope breach instead of stopping.** Returns the\n-workflow to a diff-time Guardian decision \u2014 the exact behavior this ADR removes.\n+workflow to a diff-time Guardian decision \u2014 the exact behavior this SADR removes.\n \n **Resolve governance fields from the base snapshot only.** Rejected against the\n registration flow: implementation PRs add their own contract, so the base has no\ndiff --git a/docs/adr/0016-locks-bound-product-paths.md b/docs/adr/SADR-0016-locks-bound-product-paths.md\nindex b5e850f3f1f7f4ef6e7f9d1763a29a51bd300c45..46b40a9b99a7aae644c02bb458f5473ff0a90ce2\n--- a/docs/adr/0016-locks-bound-product-paths.md\n+++ b/docs/adr/SADR-0016-locks-bound-product-paths.md\n@@ -1,4 +1,4 @@\n-# ADR 0016: Locks bound product paths; the Shiki mirror is bound by record identity\n+# SADR-0016: Locks bound product paths; the Shiki mirror is bound by record identity\n \n ## Status\n \n@@ -6,8 +6,8 @@\n \n - Date: 2026-07-28\n - Deciders: operator (mizutani-140), Claude (planner)\n-- Related: ADR 0004 (GitHub-first with a `.shiki` mirror), ADR 0009 (Spec Freeze),\n-  ADR 0012 (autonomous loop closeout to main), ADR 0015 (Contract Approval before\n+- Related: SADR-0004 (GitHub-first with a `.shiki` mirror), SADR-0009 (Spec Freeze),\n+  SADR-0012 (autonomous loop closeout to main), SADR-0015 (Contract Approval before\n   dispatch), remediation goal task R-02, the live PR #179 deadlock.\n \n ## Context\n@@ -90,11 +90,11 @@\n files, documentation files and named source modules are nameable; runtime-\n allocated ledger, runner and report ids are not \u2014 and those live in `.shiki`,\n which locks no longer govern. Existing frozen contracts are brought into line by\n-a one-time Spec Amendment per plan, which must land before ADR 0015's contract\n+a one-time Spec Amendment per plan, which must land before SADR-0015's contract\n immutability makes `locks` unwritable from the implementation PR.\n \n **Lock state is resolved from the durable side.** MergeGate resolves the lock\n-registry, and ADR 0015's governance fields, from the base `.shiki` snapshot when\n+registry, and SADR-0015's governance fields, from the base `.shiki` snapshot when\n present, falling back to head only when absent. The `MergeGate metadata check`\n job is given `--base-shiki`, which it does not receive today. Locks are not\n derived from the live open-pull-request set: a pull request body is untrusted\n@@ -108,7 +108,7 @@\n Explicitly out of scope: moving MergeGate's contract resolution off the PR\n checkout wholesale; making the mirror a projection of GitHub state; introducing\n new lock kinds; moving ledger evidence out of the repository; and unwinding\n-ADR 0012's closeout pull request. Registration of multi-task goals is ADR 0015's\n+SADR-0012's closeout pull request. Registration of multi-task goals is SADR-0015's\n Contract PR and is not re-decided here.\n \n ## Consequences\n@@ -127,7 +127,7 @@\n \n Risks accepted. First, goal-scoped identity is only as strong as `goal_id`,\n which is resolved from the PR body and the head task file; the mitigation is to\n-add `goal_id` to ADR 0015's frozen governance field set, and until that lands a\n+add `goal_id` to SADR-0015's frozen governance field set, and until that lands a\n task file can name a foreign goal. Second, base-when-present resolution is\n unavailable on fork pull requests, where the head is contributor-controlled;\n fork pull requests already cannot complete a Shiki merge, and this decision does\n@@ -139,7 +139,7 @@\n path, not by implementation pull requests.\n \n New obligations: one Spec Amendment per frozen plan to rewrite `locks`, ordered\n-before ADR 0015 T1; extension of the adversarial suites in\n+before SADR-0015 T1; extension of the adversarial suites in\n `scripts/test_shiki_adversarial_state.sh` and\n `scripts/test_shiki_governance_evidence.sh` to cover the eleven newly-scoped\n directories; and updating `scripts/test_shiki_loop_lock_guard.sh`,\n@@ -151,32 +151,32 @@\n whether the loop's transitive sync of ledger `evidence` references can be\n deleted once the Contract PR carries its own source plan.\n \n-## Relationship to ADR 0004\n+## Relationship to SADR-0004\n \n-This ADR **clarifies something ADR 0004 left unspecified**. ADR 0004 decides that\n+This SADR **clarifies something SADR-0004 left unspecified**. SADR-0004 decides that\n GitHub is the operational source of truth and that each target repository keeps a\n `.shiki/` mirror. It says nothing about pull requests writing the mirror, does\n not name MergeGate, and does not make the mirror an input to a gate. Nothing in\n-ADR 0004's Decision or Consequences is amended, superseded or weakened: the\n+SADR-0004's Decision or Consequences is amended, superseded or weakened: the\n mirror still exists, still records the same artifacts, and pull request\n-boundaries remain the coordination surface. What this ADR settles is the question\n+boundaries remain the coordination surface. What this SADR settles is the question\n 0004 never reached \u2014 by what authority a pull request may write a mirror record \u2014\n and it answers it the way `.shiki/manifest.json` already declares\n (`pr_mutation: current-task-or-current-goal-only`).\n \n-It **amends ADR 0012**, whose Decision item 5 states that \"Loop-executed tasks\n+It **amends SADR-0012**, whose Decision item 5 states that \"Loop-executed tasks\n declare the `path:.shiki/**` lock, which covers every `.shiki` file the closeout\n stages ... so `files_outside_locks` is satisfied without per-file lock\n bookkeeping.\" That clause is replaced: the closeout's mirror writes are\n-authorized by record identity, not by a declared lock. ADR 0012's substantive\n+authorized by record identity, not by a declared lock. SADR-0012's substantive\n decision \u2014 that completion reaches `main` through a closeout pull request rather\n than local attestation \u2014 is preserved unchanged.\n \n-It is **complementary to ADR 0015**, not a substitute. ADR 0015 owns contract\n+It is **complementary to SADR-0015**, not a substitute. SADR-0015 owns contract\n immutability, the pre-dispatch gate, the Contract PR and the Scope Envelope. This\n-ADR owns what a lock means and who may write the mirror. The one ordering\n+SADR owns what a lock means and who may write the mirror. The one ordering\n constraint between them is that the lock-narrowing Spec Amendment must precede\n-ADR 0015 T1's freezing of the `locks` field.\n+SADR-0015 T1's freezing of the `locks` field.\n \n ## Alternatives Considered\n \n@@ -186,7 +186,7 @@\n the only writer that can register them may run. Migrating the Guardian signal to\n that store makes every registration PR fail closed to `needs_guardian` at every\n risk level, because the task file is by definition absent from the store, which\n-removes the low/medium autonomous path ADR 0008/0009 exists to provide. And it\n+removes the low/medium autonomous path SADR-0008/0009 exists to provide. And it\n leaves schemas, manifest, config, guardian policy, templates, migrations, plans,\n worktrees, runner records, runs and handoffs with no writer at all in a\n repository whose product is `.shiki`. Measured benefit on the frozen backlog:\n@@ -199,14 +199,14 @@\n namespace is not in fact ownership-partitioned \u2014 56 ledger entries carry no\n `task_id`, and one goal DAG is writable by 35 registered tasks. The correct\n expression of its insight is that locks do not govern the mirror at all, which is\n-what this ADR decides.\n+what this SADR decides.\n \n **Make the mirror a projection written only by a registration writer and a\n post-merge projection job.** Rejected. It requires a bypass actor with write\n access to the protected default branch; it makes its own master invariant\n readable from a manifest that pull requests may edit; it deletes both reconcile\n modes and leaves a corrupted mirror with no in-band repair path, contradicting\n-ADR 0004's consequence that mirror conflicts are repaired against GitHub state;\n+SADR-0004's consequence that mirror conflicts are repaired against GitHub state;\n and its content-derived ids are rejected by `validate_shiki`'s currently-green id\n pattern.\n \ndiff --git a/docs/adr/0017-bookkeeping-closeouts-do-not-inherit-risk.md b/docs/adr/SADR-0017-bookkeeping-closeouts-do-not-inherit-risk.md\nindex 554e3a0d526a72ca067108c5464cc68c3edcf174..b9676857a4713850e063af7c39c8e0b01ecb4c66\n--- a/docs/adr/0017-bookkeeping-closeouts-do-not-inherit-risk.md\n+++ b/docs/adr/SADR-0017-bookkeeping-closeouts-do-not-inherit-risk.md\n@@ -1,4 +1,4 @@\n-# ADR 0017: A Bookkeeping Closeout Does Not Inherit Its Task's Risk\n+# SADR-0017: A Bookkeeping Closeout Does Not Inherit Its Task's Risk\n \n ## Status\n \n@@ -6,7 +6,7 @@\n \n ## Context\n \n-ADR 0012 made the goal loop push a task's completion to the default branch through a\n+SADR-0012 made the goal loop push a task's completion to the default branch through a\n closeout pull request: after the implementation PR merges, the loop opens a second PR\n carrying `task.status = done`, `lock.state = released`, and \u2014 when the task completes\n its goal \u2014 `goal.status = complete` plus one scorecard report. Nothing else. The\n@@ -49,14 +49,14 @@\n 4. The task file's governance fields (`scope`, `non_goals`, `required_skills`,\n    `risk_level`, `locks`, `acceptance_checks`, `test_command`) are byte-identical to\n    the base snapshot. Only `status`, `expected_pr`, `closeout_pr`, `ledger_evidence`\n-   and `expected_branch` may differ \u2014 the ADR 0015 contract-immutability rule,\n+   and `expected_branch` may differ \u2014 the SADR-0015 contract-immutability rule,\n    unchanged.\n \n    `expected_branch` is in that set because a closeout is *by construction* on a\n    different branch from the implementation it closes, and the loop rewrites the\n    field accordingly. It is safe to let it differ because MergeGate independently\n    requires `expected_branch` to equal the PR's actual head ref, so a PR cannot set\n-   it to anything other than the branch it is really on. The first draft of this ADR\n+   it to anything other than the branch it is really on. The first draft of this SADR\n    omitted it, which made the exemption unreachable: every real closeout failed\n    condition 4 on a field the loop always changes. See PR #209.\n 5. The state transitions are exactly the terminal set: task `review` \u2192 `done`, lock\n@@ -113,12 +113,12 @@\n classification must be derived, not asserted.\n \n **Fold the closeout into the implementation PR** so no second PR exists. Rejected under\n-ADR 0012, which recorded why: a task file that is already `done` on the implementation\n+SADR-0012, which recorded why: a task file that is already `done` on the implementation\n branch short-circuits the loop's review \u2192 CCA \u2192 merge \u2192 done ordering into a local\n goal-completion before any merge happens.\n \n **Have the loop auto-merge closeouts with elevated credentials.** Rejected outright: that\n-is a runtime authoring its own approval, which ADR 0010 and the constitution's\n+is a runtime authoring its own approval, which SADR-0010 and the constitution's\n approval-record prohibition forbid. It would also be indistinguishable, in the evidence,\n from the forgery those rules exist to prevent.\n \ndiff --git a/docs/adr/0018-carried-guardian-approval-across-a-proven-base-sync.md b/docs/adr/SADR-0018-carried-guardian-approval-across-a-proven-base-sync.md\nindex 22358562da390ed8b45cd600da08a43d378b4680..e6e2907cefa776460af73ddf7d39db605f3c39b0\n--- a/docs/adr/0018-carried-guardian-approval-across-a-proven-base-sync.md\n+++ b/docs/adr/SADR-0018-carried-guardian-approval-across-a-proven-base-sync.md\n@@ -1,4 +1,4 @@\n-# ADR 0018: Carried Guardian Approval Across A Proven Base Sync\n+# SADR-0018: Carried Guardian Approval Across A Proven Base Sync\n \n ## Status\n \n@@ -34,7 +34,7 @@\n Guardian evaluator that consumes it (`shiki_guardian.evaluate_guardian_approval`,\n the `guardian_comment_carried` source) were built and tested in isolation. Neither\n does anything until a caller passes the flags that turn the carry on, so the\n-feature was invisible in CI. This ADR records the decision the two of them\n+feature was invisible in CI. This SADR records the decision the two of them\n implement, and this task wires it into the four CI invocations that judge Guardian\n approval.\n \ndiff --git a/docs/adr/README.md b/docs/adr/README.md\nindex c70e057a4520c36e042ab68c53cd4a21ef709b71..5da43856836e2ce6a150f8127766e2f88355ab4b\n--- a/docs/adr/README.md\n+++ b/docs/adr/README.md\n@@ -1,40 +1,41 @@\n-# Architecture Decision Records\n+# Decision Record Namespaces\n \n-This directory holds Shiki's Architecture Decision Records (ADRs): durable\n-records of hard-to-reverse platform decisions. ADRs are part of the Shiki source\n-of truth alongside GitHub state, the `.shiki/` mirror, and `CONTEXT.md`.\n+This directory holds two independent decision namespaces:\n \n-## How ADRs Work\n+- Shiki Architecture Decision Records (SADRs), named\n+  `SADR-NNNN-<short-slug>.md`, record hard-to-reverse Shiki platform decisions.\n+- Target Architecture Decision Records (ADRs), named\n+  `NNNN-<short-slug>.md`, belong to the target product.\n \n-- New ADRs are added as `docs/adr/NNNN-<short-slug>.md`, where `NNNN` is the next\n-  zero-padded sequence number.\n-- The **canonical list of ADRs is the set of files matching `docs/adr/*.md`** in\n-  this directory. Browse `docs/adr/*.md` for the authoritative, current set.\n-- Use [`template.md`](template.md) as the starting point for a new ADR.\n-- Record an ADR when a decision is hard to reverse, surprising without context,\n-  and a real tradeoff. Do not write an ADR for routine or easily reversible\n-  choices.\n+The same four-digit number may appear once in each namespace. A platform\n+`SADR-NNNN` citation resolves only to `SADR-NNNN-*.md`; a target `ADR NNNN`\n+citation resolves only to `NNNN-*.md`.\n \n-Using the `docs/adr/*.md` glob as the source of truth means other PRs can add an\n-ADR without editing this index file, avoiding merge contention on a\n-hand-maintained enumeration.\n+## How Decision Records Work\n \n-## Current ADRs (snapshot)\n-\n-The list below is a convenience snapshot and may lag behind the directory. When\n-in doubt, list `docs/adr/*.md`.\n+- New Shiki platform decisions use the next zero-padded SADR number.\n+- New target product decisions use the target's own ADR sequence.\n+- The canonical list is the files in `docs/adr/`; no hand-maintained index is\n+  authoritative.\n+- Use [`template.md`](template.md) as the starting point and choose the filename\n+  namespace that owns the decision.\n+- Record a decision when it is hard to reverse, surprising without context, and\n+  a real tradeoff. Do not write a decision record for routine or easily\n+  reversible choices.\n \n-- [0001 \u2014 Keep Shiki As The Platform Name](0001-keep-shiki-as-platform-name.md)\n-- [0002 \u2014 Use AGENTS As The Shared Agent Constitution](0002-use-agents-as-shared-constitution.md)\n-- [0003 \u2014 Require Skill Gate For MergeGate Readiness](0003-require-skill-gate-for-mergegate-readiness.md)\n-- [0004 \u2014 Use GitHub First With A Repository-Local Shiki Mirror](0004-use-github-first-with-shiki-mirror.md)\n-- [0005 \u2014 Use Codex As Front And Claude Code Action As The GitHub Runtime](0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md)\n-- [0006 \u2014 Separate Codex Implementation, CCA Completion, And MergeGate Authorization](0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md)\n+Using the directory as the source of truth lets independent changes add records\n+without contending on an enumerated index.\n \n-## Adding A New ADR\n+## Adding A Shiki SADR\n \n-1. Copy `template.md` to `docs/adr/NNNN-<short-slug>.md`.\n+1. Copy `template.md` to `docs/adr/SADR-NNNN-<short-slug>.md`.\n 2. Fill in the title, status, context, decision, and consequences.\n-3. Set the status to `Proposed`, then `Accepted` once the decision is approved.\n-4. Open a PR following `CONTRIBUTING.md`. You do not need to edit this index for\n-   the ADR to be discoverable via `docs/adr/*.md`.\n+3. Set the status to `Proposed`, then `Accepted` once the proper authority has\n+   approved the decision.\n+4. Open a PR following `CONTRIBUTING.md`.\n+\n+## Adding A Target ADR\n+\n+1. Copy `template.md` to `docs/adr/NNNN-<short-slug>.md` using the target's own\n+   sequence.\n+2. Cite it as `ADR NNNN`; do not prefix its filename with `SADR-`.\ndiff --git a/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md b/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\nnew file mode 100644\nindex 0000000000000000000000000000000000000000..7455ce1f3bed02ddddb851ac21a1d1a648c9aeba\n--- /dev/null\n+++ b/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\n@@ -0,0 +1,55 @@\n+# SADR-0019: Separate Shiki Platform SADRs From Target ADRs\n+\n+## Status\n+\n+Accepted\n+\n+## Context\n+\n+Shiki installs its platform decision records into `docs/adr/` in every target\n+repository. Shiki and its targets previously used the same numeric\n+`NNNN-<slug>.md` namespace. A target that already owned ADRs 0013 through 0018\n+therefore acquired two records for each number during installation, making an\n+`ADR NNNN` citation ambiguous. Renumbering target decisions would rewrite target\n+history, while renumbering Shiki decisions per target would destroy their stable\n+platform identity.\n+\n+Upgrades also need to remove the former Shiki-owned numeric paths without ever\n+mistaking a target-owned ADR for platform content. The install stamp already\n+records exact shipped paths and content digests, so it can prove ownership only\n+when both still match.\n+\n+## Decision\n+\n+Shiki platform decisions use the `SADR` namespace:\n+\n+- records are named `docs/adr/SADR-NNNN-<slug>.md`;\n+- live platform citations use `SADR-NNNN`;\n+- target product decisions retain `docs/adr/NNNN-<slug>.md` and `ADR NNNN`;\n+- the reference validator resolves the two namespaces independently, so the same\n+  four-digit number may exist once in each namespace.\n+\n+Before a forced upgrade writes anything, the installer inventories the exact\n+former numeric Shiki ADR paths. It may delete a legacy path only when the\n+existing install stamp contains that exact path and its recorded SHA-256 digest\n+matches the file currently on disk. All ownership blockers are reported\n+together and abort the upgrade before its first write. A non-forced install\n+never performs this cleanup.\n+\n+Historical `.shiki/` evidence remains unchanged because it records the namespace\n+that existed when that evidence was produced.\n+\n+## Consequences\n+\n+- Targets may use any ADR number without colliding with Shiki's platform SADRs.\n+- Fresh installs preserve target ADR bytes and install a stable SADR set.\n+- Forced upgrades fail closed when legacy ownership cannot be proven, and a\n+  successful upgrade refreshes the stamp without removed legacy paths.\n+- Current governance, packaging, workflow, prompt, script, and skill references\n+  must move with the platform records.\n+\n+## Alternatives Considered\n+\n+- Collision-only refusal leaves affected targets permanently unupgradable.\n+- Renumbering target ADRs mutates target-owned history.\n+- Per-target Shiki renumbering makes platform citations non-portable.\ndiff --git a/docs/adr/template.md b/docs/adr/template.md\nindex a92a97daddd0b00ab9dd626cc8d86942da2eafed..17dd79175b5fee2105935557f691b40cf9492942\n--- a/docs/adr/template.md\n+++ b/docs/adr/template.md\n@@ -1,14 +1,14 @@\n-# NNNN: <Short Decision Title>\n+# <SADR-NNNN or NNNN>: <Short Decision Title>\n \n ## Status\n \n-Proposed | Accepted | Superseded by ADR-NNNN\n+Proposed | Accepted | Superseded by SADR-NNNN or target ADR NNNN\n \n ## Context\n \n What is the situation, constraint, or force that requires a decision? Reference\n-relevant domain terms (`CONTEXT.md`), prior ADRs, and code reality. State the\n-problem, not the solution.\n+relevant domain terms (`CONTEXT.md`), prior Shiki SADRs or target ADRs, and code\n+reality. State the problem, not the solution.\n \n ## Decision\n \ndiff --git a/docs/agents/autonomous-loop-e2e-acceptance.md b/docs/agents/autonomous-loop-e2e-acceptance.md\nindex bbf84c6081987f159ac6ad9ecf3c82b7aa72e7e8..5c13c1d02a05a90b0dc46cbef7c95a9846d0c237\n--- a/docs/agents/autonomous-loop-e2e-acceptance.md\n+++ b/docs/agents/autonomous-loop-e2e-acceptance.md\n@@ -40,8 +40,8 @@\n 3. `gh auth status` is logged in with merge permission on the repo.\n 4. Branch protection + required checks are configured exactly as the goal-loop\n    required-check set expects (see `docs/agents/branch-protection-smoke.md`).\n-5. The repo `.shiki/policy` permits autonomous low/medium-risk merges (ADR\n-   0008/0009); the MergeGate policy check is the recorded-authority gate.\n+5. The repo `.shiki/policy` permits autonomous low/medium-risk merges\n+   (SADR-0008/0009); the MergeGate policy check is the recorded-authority gate.\n \n The operator's only actions are: register the task and start the loop. **No\n manual pre-clearing** means after `shiki loop run` starts, the operator does not\ndiff --git a/docs/agents/bootstrap-command.md b/docs/agents/bootstrap-command.md\nindex 05f7f203a52cf412b9f97084a6e131ed0f785afd..f7aa0b734dfebee4a3b139335b66ee3415f48713\n--- a/docs/agents/bootstrap-command.md\n+++ b/docs/agents/bootstrap-command.md\n@@ -189,7 +189,9 @@\n ```\n \n Use this only for tests, fixtures, or explicit local-only template inspection.\n-Use `--force` only when you intentionally want to overwrite existing target files.\n+Use `--force` only when you intentionally want to refresh the target template.\n+Legacy numeric Shiki decision cleanup is stamp-and-digest authorized before any\n+write; target ADR paths are not cleanup candidates.\n \n ## Slash Command\n \ndiff --git a/docs/agents/checklists.md b/docs/agents/checklists.md\nindex 4a7f03ade346f53a1c1d82981f3fd744f9ab4547..0c5ec6c53dc7197b7b7a7d18e667b325babd6c45\n--- a/docs/agents/checklists.md\n+++ b/docs/agents/checklists.md\n@@ -32,18 +32,18 @@\n | ID | Item | Blocking |\n | --- | --- | --- |\n | GD-01 | Plan was challenged against `CONTEXT.md`. | blocking for non-trivial Goals |\n-| GD-02 | Relevant ADRs were read or absence was recorded. | blocking for non-trivial Goals |\n+| GD-02 | Relevant SADRs and target ADRs were read or absence was recorded. | blocking for non-trivial Goals |\n | GD-03 | Ambiguous terms were resolved or converted to HITL blockers. | blocking |\n | GD-04 | Edge scenarios were considered. | advisory |\n | GD-05 | Code reality was checked when code could answer. | blocking when applicable |\n | GD-06 | New domain terms were added to `CONTEXT.md` when needed. | blocking when applicable |\n-| GD-07 | ADR was created or explicitly skipped using the ADR criteria. | blocking when applicable |\n+| GD-07 | A SADR or target ADR was created or explicitly skipped using the decision-record criteria. | blocking when applicable |\n \n ## CI \u2014 Context & Impact Checklist\n \n | ID | Item | Blocking |\n | --- | --- | --- |\n-| CI-01 | Relevant docs and ADRs identified. | blocking |\n+| CI-01 | Relevant docs, SADRs, and target ADRs identified. | blocking |\n | CI-02 | Relevant modules, interfaces, seams, callers, and tests identified. | blocking |\n | CI-03 | Dependencies and blocked-by relationships identified. | blocking |\n | CI-04 | Candidate locks identified. | blocking |\n@@ -62,7 +62,7 @@\n | PRD-04 | Implementation decisions are recorded without volatile code detail. | blocking |\n | PRD-05 | Testing decisions are recorded. | blocking |\n | PRD-06 | Out-of-scope items are explicit. | blocking |\n-| PRD-07 | Relevant glossary/ADR links are present when applicable. | blocking |\n+| PRD-07 | Relevant glossary/SADR/target-ADR links are present when applicable. | blocking |\n \n ## SF \u2014 Spec Freeze Checklist\n \n@@ -98,7 +98,7 @@\n | H-02 | Branch/worktree target is present. | blocking |\n | H-03 | Scope and non-goals are present. | blocking |\n | H-04 | Dependencies and locks are present. | blocking |\n-| H-05 | Relevant docs, ADRs, modules, and tests are referenced. | blocking |\n+| H-05 | Relevant docs, SADRs, target ADRs, modules, and tests are referenced. | blocking |\n | H-06 | Required skills are stated. | blocking |\n | H-07 | TDD expectations are stated. | blocking when behavior work |\n | H-08 | Acceptance criteria are included. | blocking |\ndiff --git a/docs/agents/codex-handoff.md b/docs/agents/codex-handoff.md\nindex ede468f47cb277f69a857e8c45df703e4ed0151d..6f97bdffb67e44d7edea2c71e01ea28933c4eb6c\n--- a/docs/agents/codex-handoff.md\n+++ b/docs/agents/codex-handoff.md\n@@ -46,7 +46,7 @@\n \n ## Context\n - Domain terms:\n-- Relevant ADRs:\n+- Relevant SADRs and target ADRs:\n - Relevant docs:\n - Relevant modules/interfaces/seams:\n - Similar tests/prior art:\ndiff --git a/docs/agents/completion-check-agent.md b/docs/agents/completion-check-agent.md\nindex 13e4c035db35025f4e3cdcc5adfb24314ac4d521..61bfa1218cf36ca605250c91919e620f33238aa0\n--- a/docs/agents/completion-check-agent.md\n+++ b/docs/agents/completion-check-agent.md\n@@ -32,7 +32,7 @@\n - `AGENTS.md`\n - `CLAUDE.md`\n - `CONTEXT.md`\n-- relevant ADRs\n+- relevant SADRs and target ADRs\n - parent Goal issue\n - PRD issue or document\n - task issue\ndiff --git a/docs/agents/decision-control.md b/docs/agents/decision-control.md\nindex 7d8ee5ab03a5ccbcbd04056ae61de03785a2a9c9..8c94ecda00fffaa0e92c325ab83aa9776bd1dac1\n--- a/docs/agents/decision-control.md\n+++ b/docs/agents/decision-control.md\n@@ -11,7 +11,7 @@\n | Decision | Owner | When | Required inputs | Output | Enforcement surface |\n | --- | --- | --- | --- | --- | --- |\n | Is the Goal clear enough to plan? | Planner + Guardian when needed | Goal intake | User request, existing docs, risk signals | Goal readiness verdict | Goal checklist, issue template, ledger |\n-| Must `grill-with-docs` run? | Planner / Skill Gate | Before PRD or issue split | Ambiguity, domain terms, ADR conflicts, unclear boundaries | Skill invocation or explicit skip rationale | Skill Gate checklist |\n+| Must `grill-with-docs` run? | Planner / Skill Gate | Before PRD or issue split | Ambiguity, domain terms, SADR/target-ADR conflicts, unclear boundaries | Skill invocation or explicit skip rationale | Skill Gate checklist |\n | Is a PRD required? | Planner | After Goal context stabilizes | Goal, `grill-with-docs` output, Context & Impact output | PRD required / not required | PRD checklist, CCA evidence check |\n | Can the spec be frozen? | Operator only | End of Requirements Definition, before issue split | PRD/requirements summary, scope inventory (SF-02), open-question list | `spec_freeze` block in the plan + ledger evidence | `require_grilled_plan` (plan ingest/run), SF checklist |\n | Can a frozen spec change? | Operator only (Spec Amendment) | When implementation contradicts the frozen spec | Contested decisions, scoped re-grill result | recorded amendment + re-stamped freeze | SF checklist, ledger evidence |\ndiff --git a/docs/agents/domain.md b/docs/agents/domain.md\nindex 258da8ea81e87ac0a6b0b5b5e5c2fde980eaabb7..d57b0cf17ff3e1fa9e6e5885372bf083d8295421\n--- a/docs/agents/domain.md\n+++ b/docs/agents/domain.md\n@@ -5,7 +5,9 @@\n ## Layout\n \n - `CONTEXT.md` defines Shiki language. Keep it implementation-free.\n-- `docs/adr/` records hard-to-reverse decisions.\n+- `docs/adr/SADR-NNNN-*.md` records Shiki platform decisions.\n+- `docs/adr/NNNN-*.md` records target product decisions without sharing the\n+  SADR namespace.\n - `AGENTS.md` is the runtime-neutral agent constitution.\n - `CLAUDE.md` is the Claude Code wrapper around `AGENTS.md`.\n - `.shiki/` is the Target Repository mirror for Goals, PRDs, plans, Task DAGs, contracts, locks, worktree records, ledger entries, CCA verdicts, repair packets, reports, and handoffs.\n@@ -15,6 +17,6 @@\n \n - Use Shiki terms exactly when they exist.\n - Add glossary terms to `CONTEXT.md` only when the term is part of the domain, not a generic programming concept.\n-- Create ADRs only for hard-to-reverse, surprising, tradeoff-based decisions.\n+- Create SADRs or target ADRs only for hard-to-reverse, surprising, tradeoff-based decisions.\n - Keep product-specific language in the Target Repository, not in Shiki Template docs.\n-- If an ADR conflicts with a proposed implementation, surface the conflict before editing.\n+- If a SADR or target ADR conflicts with a proposed implementation, surface the conflict before editing.\ndiff --git a/docs/agents/execution-control.md b/docs/agents/execution-control.md\nindex ad8ee3cdb36be56fedc1f46408ca92b387d74047..bc63c3a73661d12b567016ba299f1a6b66bf9088\n--- a/docs/agents/execution-control.md\n+++ b/docs/agents/execution-control.md\n@@ -63,7 +63,7 @@\n | Branch name | Orchestrator | Orchestrator | Before implementer dispatch | branch policy | branch ref in task/ledger |\n | Lock reservation | Context & Impact | MergeGate | Before dispatch and before merge | `.shiki/locks/`, labels, PR metadata | lock id, owner, scope |\n | CI profile | Planner | MergeGate | During issue triage, rechecked at PR | labels, `.shiki/policy.yaml`, workflow matrix | required check list |\n-| Required status checks | Repo maintainer / Guardian | GitHub branch protection / rulesets | Repository setup, updated via ADR | GitHub settings | branch/ruleset config, ADR |\n+| Required status checks | Repo maintainer / Guardian | GitHub branch protection / rulesets | Repository setup, updated via target ADR | GitHub settings | branch/ruleset config, target ADR |\n | CCA judgment | CCA | GitHub required check / MergeGate | Every non-draft PR update | GitHub Action check | structured CCA verdict |\n | Repair assignment | CCA | MergeGate | After failed CCA/CI/review | repair packet, issue/PR comment | repair id and attempt count |\n | Merge readiness | MergeGate | GitHub branch protection / Guardian if needed | After CCA complete | required checks, review rules, rulesets | mergegate check output |\ndiff --git a/docs/agents/guardian-policy.md b/docs/agents/guardian-policy.md\nindex 4147c7adf05a47adb4fd24ada1a1c2e1f2ba6d2d..a20d9cc6c00a569bb057b9ca8d6b51c7fc4a9847\n--- a/docs/agents/guardian-policy.md\n+++ b/docs/agents/guardian-policy.md\n@@ -98,8 +98,8 @@\n `origin/<default_branch>` and reuses each job's existing base fetch; a shallow\n (`--depth`) or second base fetch would make the proof unresolvable and silently\n disable the carry, so neither is permitted. CCA reads `guardian_comment_carried` as\n-a recorded authority in the deterministic signal and does not re-derive it. See ADR\n-0018 for the full decision, the rejected diff/patch-equality and path-disjointness\n+a recorded authority in the deterministic signal and does not re-derive it. See\n+SADR-0018 for the full decision, the rejected diff/patch-equality and path-disjointness\n alternatives, and the accepted residual.\n \n ## External AI Guardian Review (SADR-0010)\ndiff --git a/docs/agents/implementation-policy.md b/docs/agents/implementation-policy.md\nindex c754144a9fb5bf4a38aa82a60e1670c59e626b44..85c37281bcad196c6b7b02343faf00b03cd9c57a\n--- a/docs/agents/implementation-policy.md\n+++ b/docs/agents/implementation-policy.md\n@@ -31,10 +31,13 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n ## Non-Negotiables\n \n 1. **Planning precedes implementation.** Non-trivial Goals start with `grill-with-docs` and Context & Impact.\n-2. **Domain language is controlled.** Terms must be checked against `CONTEXT.md`; hard-to-reverse tradeoffs must be checked against ADRs.\n+2. **Domain language is controlled.** Terms must be checked against `CONTEXT.md`; hard-to-reverse tradeoffs must be checked against Shiki SADRs and target ADRs.\n 3. **PRD precedes issue decomposition.** `to-prd` is used once the context is settled enough to become durable intent.\n 4. **Issues are vertical slices.** `to-issues` must produce independently grabbable tracer-bullet tasks, not horizontal layer tickets.\n 5. **Triage controls dispatch.** Only `ready-for-agent` / AFK tasks may be assigned to Codex without human input.\n@@ -95,19 +98,19 @@\n Process:\n \n - Challenge terms against `CONTEXT.md`.\n-- Check ADRs before reopening decisions.\n+- Check SADRs and target ADRs before reopening decisions.\n - Inspect code when code can answer a question.\n - Ask one question at a time when the operator is available.\n - Provide a recommended answer with each question.\n - Record resolved terms in `CONTEXT.md`.\n-- Create ADRs only for hard-to-reverse, surprising, tradeoff-based decisions.\n+- Create SADRs or target ADRs only for hard-to-reverse, surprising, tradeoff-based decisions.\n - Convert unresolved load-bearing questions into HITL blockers.\n \n Outputs:\n \n - Resolved design-tree decisions.\n - Updated glossary terms when needed.\n-- ADRs when justified.\n+- SADRs or target ADRs when justified.\n - HITL blockers when decisions remain unresolved.\n \n ## Phase 3 \u2014 Context & Impact\n@@ -117,7 +120,7 @@\n \n Outputs must include:\n \n-- Relevant documents and ADRs.\n+- Relevant documents, SADRs, and target ADRs.\n - Relevant modules, interfaces, seams, callers, and tests.\n - Candidate locks.\n - Dependencies.\n@@ -140,7 +143,7 @@\n - Testing decisions.\n - Out of scope.\n - Further notes.\n-- Links to relevant Goal, domain terms, and ADRs.\n+- Links to relevant Goal, domain terms, SADRs, and target ADRs.\n \n Phase 4 ends with Spec Freeze: the operator explicitly approves the PRD, the\n plan records a `spec_freeze` block (status, approved_by, source), and the SF\n@@ -240,7 +243,7 @@\n - Dependencies and locks.\n - Risk and Guardian requirements.\n - Ledger completeness.\n-- Documentation and ADR updates.\n+- Documentation and SADR/target-ADR updates.\n - Scope drift.\n \n CCA emits one of:\n@@ -301,7 +304,7 @@\n | Evidence failure | Behavior might be done, but proof is missing. | Add checks, PR evidence, ledger entry, or artifacts. |\n | Scope drift | PR changes unrelated behavior. | Revert unrelated changes or split into new Goal. |\n | Dependency block | Upstream task or lock not complete. | Block until dependency resolves. |\n-| Architecture block | Decision needs ADR or deep-module discussion. | `grill-with-docs` or `improve-codebase-architecture`. |\n+| Architecture block | Decision needs a SADR, target ADR, or deep-module discussion. | `grill-with-docs` or `improve-codebase-architecture`. |\n | Risk block | Security/production/policy change needs human approval. | Guardian decision. |\n \n ## Phase 5.5 \u2014 Execution Decision Control\ndiff --git a/docs/agents/runtime-auth-model.md b/docs/agents/runtime-auth-model.md\nindex 6a2e1a47e81ea89b5c68a210ec07772a9374babc..3c54748d24233dbd0ba8473950282547ef4ce935\n--- a/docs/agents/runtime-auth-model.md\n+++ b/docs/agents/runtime-auth-model.md\n@@ -148,7 +148,7 @@\n \n API-key based runners may be added by a target repository as an explicit extension. They are not the default Shiki template path.\n \n-If a repo opts into API-key mode, record the exception in an ADR and update the repo's `AGENTS.md`, `.shiki/config.yaml`, and workflow permissions.\n+If a repo opts into API-key mode, record the target-specific exception in a target ADR and update the repo's `AGENTS.md`, `.shiki/config.yaml`, and workflow permissions.\n \n ## GitHub Actions Runtime Compatibility\n \ndiff --git a/docs/agents/skill-gate.md b/docs/agents/skill-gate.md\nindex c50398b0d7c60a8598f2a5c88a10015730dced13..3acffe6444617bb1d54b77d4b4708158ec735bc6\n--- a/docs/agents/skill-gate.md\n+++ b/docs/agents/skill-gate.md\n@@ -9,7 +9,7 @@\n | Skill | Trigger | Expected output |\n | --- | --- | --- |\n | `setup-matt-pocock-skills` | First configuring a repo for these skills, or when issue tracker, triage labels, or domain docs are missing. | `docs/agents/` configuration and an `Agent skills` block in `AGENTS.md` or `CLAUDE.md`. |\n-| `grill-with-docs` | Ambiguous plan, unclear terminology, boundary decision, tradeoff, or ADR-worthy design choice. | Clarified decision tree, recommended answers, updates to `CONTEXT.md` or ADRs when decisions crystallize. |\n+| `grill-with-docs` | Ambiguous plan, unclear terminology, boundary decision, tradeoff, or decision-record-worthy design choice. | Clarified decision tree, recommended answers, updates to `CONTEXT.md`, SADRs, or target ADRs when decisions crystallize. |\n | `zoom-out` | Agent lacks a higher-level architectural map. | Map of relevant modules, callers, seams, and domain concepts. |\n | `to-prd` | Settled context should become a PRD. | PRD published to the issue tracker with problem, solution, user stories, decisions, testing, and out-of-scope notes. |\n | `to-issues` | Goal, PRD, or plan must become implementation tickets. | Vertical-slice issues with acceptance criteria, dependencies, and AFK/HITL classification. |\n@@ -17,7 +17,7 @@\n | `tdd` | Feature work or bug fix has observable behavior that can be specified with tests. | Red-green-refactor loop through public interfaces, one vertical slice at a time. |\n | `code-review` | The TDD loop is green and the implementer is about to create or update a PR. Default for every implementation task. | Pre-PR self-review of the task diff with fixes applied on the task branch; findings and resolutions recorded as a ledger entry naming the skill and a `## Pre-PR code review` PR body section. |\n | `diagnose` | Hard bug, failing check, regression, flaky behavior, or performance problem. | Reproduction loop, ranked hypotheses, instrumentation, fix, regression test, post-mortem. |\n-| `improve-codebase-architecture` | Structural friction, testability, AI-navigability, deep-module opportunity, or refactor request. | Architecture candidates and deepening recommendations informed by `CONTEXT.md` and ADRs. |\n+| `improve-codebase-architecture` | Structural friction, testability, AI-navigability, deep-module opportunity, or refactor request. | Architecture candidates and deepening recommendations informed by `CONTEXT.md`, SADRs, and target ADRs. |\n | `prototype` | A throwaway logic or UI prototype is needed to answer a design question. | Clearly marked disposable prototype and captured decision. |\n \n ## MergeGate Interaction\ndiff --git a/docs/operations.md b/docs/operations.md\nindex cf1f452d4c589e38ccd58553bda0ad8684b51eb7..ace48a90ffbec9e5c0a7113bef16a4858bc44b3f\n--- a/docs/operations.md\n+++ b/docs/operations.md\n@@ -89,6 +89,16 @@\n      target history, brought forward by `shiki migrate`, not a template.\n    - **Everything else** (CLI scripts, workflows, schemas, docs) is overwritten.\n \n+   A forced upgrade also migrates the former numeric Shiki decision paths to\n+   `SADR-NNNN-*`. Before the first write, the installer inventories the exact\n+   legacy Shiki paths and authorizes each deletion only when the existing\n+   `.shiki/install-stamp.json` names that path and its SHA-256 digest still\n+   matches the file. An absent, unreadable, malformed, incomplete, or\n+   mismatching stamp reports every blocked path and stops with no filesystem\n+   change. Target-owned `NNNN-*` ADRs are not cleanup candidates. A non-force\n+   install deletes nothing and retains any still-matching legacy stamp entries\n+   so a later forced upgrade can prove ownership.\n+\n    If the target has pending migrations, `--force` refuses and writes nothing \u2014\n    apply migrations first (step 3), then re-run. The run ends with a single\n    summary of every `.new` written, so a half-upgrade is visible.\ndiff --git a/docs/releasing.md b/docs/releasing.md\nindex 10d73828e7dbd64e9d9f6b83bc84d010537ce62a..aafeee8c4d441e1e427efee2ce4f81a647f020e1\n--- a/docs/releasing.md\n+++ b/docs/releasing.md\n@@ -67,7 +67,7 @@\n CLI in `scripts/`. There are no third-party Python runtime dependencies. The CLI\n is intentionally **not** exposed as a `[project.scripts]` console entry point\n because `scripts/shiki.py` is a dependency-free shim and `scripts/` is not\n-packaged as an importable distribution (see the ADR for the rationale).\n+packaged as an importable distribution (see SADR-0007 for the rationale).\n \n Supported install paths:\n \n@@ -155,6 +155,15 @@\n    [docs/operations.md](operations.md#the-new-convention). If the target has\n    pending migrations, `--force` refuses and writes nothing; apply migrations\n    (step 2) first, then re-run.\n+\n+   The SADR namespace upgrade has an additional pre-write gate: each former\n+   numeric Shiki decision path is removed only when the existing install stamp\n+   contains that exact path and a digest matching its current bytes. The\n+   installer aggregates absent/unreadable/malformed-stamp, missing-digest, and\n+   digest-mismatch blockers before raising, so refusal leaves the target tree\n+   byte-identical. Numeric target ADRs are preserved. A successful forced\n+   upgrade installs and stamps `SADR-*` paths and drops the removed legacy paths\n+   from the refreshed stamp.\n 2. Apply repository-local `.shiki/` state migrations with the migrate command:\n \n    ```bash\ndiff --git a/docs/usage.md b/docs/usage.md\nindex b83407ee3970dd395dd893010bc5dbe8bad26051..a130ef6e40dfd8169a5bc701339351097df1a09f\n--- a/docs/usage.md\n+++ b/docs/usage.md\n@@ -67,8 +67,10 @@\n shiki install-target /path/to/target-repo --local-only\n ```\n \n-Use `--force` only when you intentionally want to overwrite existing target\n-files.\n+Use `--force` only when you intentionally want to refresh the target template.\n+For the SADR namespace migration, `--force` deletes only exact legacy Shiki\n+decision paths whose existing install-stamp digest still matches; any ownership\n+blocker stops before the first write. Numeric target ADRs are never candidates.\n \n ## `shiki bootstrap-platform` \u2014 publish the Shiki platform repo\n \ndiff --git a/scripts/guardian_approval_signal.py b/scripts/guardian_approval_signal.py\nindex d059f92b2c82aa2b5d6ca421365537a87886c1cb..16924892714f4f5b9303c0ef0e325a7e70075630\n--- a/scripts/guardian_approval_signal.py\n+++ b/scripts/guardian_approval_signal.py\n@@ -199,8 +199,8 @@\n ) -> Any:\n     \"\"\"Evaluate SADR-0015 Contract Approval for the CCA Guardian signal.\n \n-    The SAME OR that MergeGate applies must reach this deterministic signal: ADR\n-    0015 is explicit that without it, a contract-approved PR still returns\n+    The SAME OR that MergeGate applies must reach this deterministic signal:\n+    SADR-0015 is explicit that without it, a contract-approved PR still returns\n     ``needs_guardian``, the CCA job fails, and the MergeGate policy check never\n     runs \u2014 the gate this change moves would never be reached. This resolves the\n     task the SAME way MergeGate does (the first Shiki task id in the PR body),\ndiff --git a/scripts/shiki_contract_approval.py b/scripts/shiki_contract_approval.py\nindex 941bcb96fdd8b9a85bc0c4f0f04d5dad9d04839c..4a4b418f6527413161db82670bec857a043360e2\n--- a/scripts/shiki_contract_approval.py\n+++ b/scripts/shiki_contract_approval.py\n@@ -142,7 +142,7 @@\n     changed_files_status: Any,\n     registration: Any,\n ) -> ContractApprovalResult:\n-    \"\"\"Whether this PR carries an SADR-0015 Contract Approval.\n+    \"\"\"Whether this PR carries a SADR-0015 Contract Approval.\n \n     The four conditions are evaluated IN ORDER, each failure recorded as a\n     distinct reason string:\ndiff --git a/scripts/shiki_guardian.py b/scripts/shiki_guardian.py\nindex a4ecb79bbda55fd5c8b06adfae2eb99760a03c45..4dd99bbe714e26f421fa19abac7a34ae1b195ecc\n--- a/scripts/shiki_guardian.py\n+++ b/scripts/shiki_guardian.py\n@@ -12,7 +12,7 @@\n # The carry re-derives its proof from git in-process on every judging invocation;\n # it is never read from an artifact, so there is nothing to go stale or be forged.\n # shiki_sync_proof is the ONLY guardian-family module that runs git, and every one\n-# of its invocations is read-only and local (ADR: prove-from-git base sync).\n+# of its invocations is read-only and local (SADR: prove-from-git base sync).\n from shiki_sync_proof import verify_pure_base_sync\n \n GUARDIAN_POLICY_PATH = \".shiki/guardian-policy.json\"\ndiff --git a/scripts/shiki_guardian_review.py b/scripts/shiki_guardian_review.py\nindex dd0f104d457899e0ba5ff1e162011c1e90b6f92b..e10ef58c046324e7ea3a3392feb3048ca77414ce\n--- a/scripts/shiki_guardian_review.py\n+++ b/scripts/shiki_guardian_review.py\n@@ -159,7 +159,7 @@\n     docs_only = _is_docs_only(changed_files)\n     if docs_only and matched == 0:\n         focus.append(\n-            \"Docs/ADR-only change: confirm the change is genuinely documentation/decision-only, introduces no behavior or contract drift, and stays consistent with the constitution and existing ADRs.\"\n+            \"Docs/decision-record-only change: confirm the change is genuinely documentation/decision-only, introduces no behavior or contract drift, and stays consistent with the constitution and existing SADRs and target ADRs.\"\n         )\n \n     # Conservative augmentation: unknown (no category and not docs-only), mixed\ndiff --git a/scripts/shiki_installer.py b/scripts/shiki_installer.py\nindex be064ab7a29ff6836f7f7a1d70500a43e0d11d7b..9fd8eefd137ce7003dad5c0dcb2e48e2cd47ab32\n--- a/scripts/shiki_installer.py\n+++ b/scripts/shiki_installer.py\n@@ -118,6 +118,7 @@\n # what lets a target carry it without a governance-schema change.\n INSTALL_STAMP_PATH = \".shiki/install-stamp.json\"\n INSTALL_STAMP_VERSION = 1\n+LEGACY_SHIKI_SADR_MAX = 18\n \n # Under --force the shipped surface splits three ways, so nothing is lost and\n # nothing is silently stale.\n@@ -237,9 +238,98 @@\n         )\n \n \n+def legacy_shiki_adr_paths() -> tuple[str, ...]:\n+    \"\"\"Former numeric paths for Shiki SADR-0001 through SADR-0018.\n+\n+    The authoritative slugs come from the current SADR filenames. SADR-0019 is\n+    the namespace decision itself and never shipped under a numeric legacy path.\n+    \"\"\"\n+    paths: list[str] = []\n+    for sadr in sorted((ROOT / \"docs\" / \"adr\").glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\")):\n+        number = sadr.name.split(\"-\", 2)[1]\n+        if int(number) <= LEGACY_SHIKI_SADR_MAX:\n+            paths.append(f\"docs/adr/{sadr.name.removeprefix('SADR-')}\")\n+    return tuple(paths)\n+\n+\n+def preflight_legacy_sadr_cleanup(target: Path, *, force: bool) -> tuple[str, ...]:\n+    \"\"\"Authorize exact legacy Shiki ADR removals before the first write.\n+\n+    A non-force install never removes existing files. A forced install needs no\n+    stamp when no legacy Shiki path is present. When legacy paths do exist, each\n+    must be named in a readable install stamp and its current bytes must match\n+    the recorded digest.\n+    \"\"\"\n+    if not force:\n+        return ()\n+    present = tuple(\n+        relative\n+        for relative in legacy_shiki_adr_paths()\n+        if (target / relative).exists() or (target / relative).is_symlink()\n+    )\n+    if not present:\n+        return ()\n+\n+    stamp_path = target / INSTALL_STAMP_PATH\n+    try:\n+        raw = stamp_path.read_text(encoding=\"utf-8\")\n+        stamp = json.loads(raw)\n+    except FileNotFoundError:\n+        detail = \"install stamp is absent\"\n+        blockers = [f\"{relative}: {detail}\" for relative in present]\n+    except (OSError, UnicodeDecodeError) as error:\n+        detail = f\"install stamp is unreadable ({error})\"\n+        blockers = [f\"{relative}: {detail}\" for relative in present]\n+    except json.JSONDecodeError as error:\n+        detail = f\"install stamp is malformed ({error})\"\n+        blockers = [f\"{relative}: {detail}\" for relative in present]\n+    else:\n+        if not isinstance(stamp, dict):\n+            blockers = [\n+                f\"{relative}: install stamp is malformed (expected an object)\"\n+                for relative in present\n+            ]\n+        else:\n+            digests = stamp.get(\"digests\")\n+            blockers = []\n+            if not isinstance(digests, dict):\n+                blockers.extend(\n+                    f\"{relative}: install stamp has no digest for this path\"\n+                    for relative in present\n+                )\n+            else:\n+                for relative in present:\n+                    expected = digests.get(relative)\n+                    if not isinstance(expected, str) or not expected:\n+                        blockers.append(\n+                            f\"{relative}: install stamp has no digest for this path\"\n+                        )\n+                        continue\n+                    try:\n+                        actual = _sha256_file(target / relative)\n+                    except OSError as error:\n+                        blockers.append(\n+                            f\"{relative}: legacy path is unreadable ({error})\"\n+                        )\n+                        continue\n+                    if actual != expected:\n+                        blockers.append(\n+                            f\"{relative}: digest mismatch (stamp {expected}, current {actual})\"\n+                        )\n+\n+    if blockers:\n+        joined = \"\\n\".join(f\"- {blocker}\" for blocker in blockers)\n+        raise ShikiError(\n+            \"legacy Shiki ADR cleanup is not authorized:\\n\"\n+            f\"{joined}\\nNo files were written.\"\n+        )\n+    return present\n+\n+\n def install_template(target: Path, *, force: bool, validate: bool) -> list[NewFileNote]:\n     # Preconditions first: never rewrite the tree of a target that cannot be\n     # upgraded cleanly (see refuse_pending_migrations).\n+    legacy_cleanup = preflight_legacy_sadr_cleanup(target, force=force)\n     refuse_pending_migrations(target)\n \n     # Resolve the target's required CODEOWNERS owner once from its\n@@ -250,6 +340,10 @@\n     # verbatim. See ``codeowners_required_owner``.\n     codeowners_owner = codeowners_required_owner(target)\n \n+    for relative in legacy_cleanup:\n+        (target / relative).unlink()\n+        info(f\"removed stamped legacy Shiki ADR: {target / relative}\")\n+\n     notes: list[NewFileNote] = []\n     for relative in TEMPLATE_PATHS:\n         source = ROOT / relative\n@@ -647,6 +741,22 @@\n     digests = target_stamp_digests(target)\n     existing = read_install_stamp(target)\n     if existing is not None:\n+        # A non-force install deliberately leaves legacy Shiki ADRs in place.\n+        # Preserve only still-matching ownership proof so a later forced\n+        # upgrade can authorize cleanup; never carry a missing or stale digest.\n+        existing_digests = existing.get(\"digests\")\n+        if isinstance(existing_digests, dict):\n+            for relative in legacy_shiki_adr_paths():\n+                expected = existing_digests.get(relative)\n+                path = target / relative\n+                if not isinstance(expected, str) or not path.is_file():\n+                    continue\n+                try:\n+                    actual = _sha256_file(path)\n+                except OSError:\n+                    continue\n+                if actual == expected:\n+                    digests[relative] = actual\n         if _incoming_is_older(commit, existing.get(\"platform_commit\")):\n             warn(\n                 \"kept existing install stamp: incoming install \"\ndiff --git a/scripts/shiki_loop.py b/scripts/shiki_loop.py\nindex 3611641da8aeb5038b1da90e3114b09a473ec6d8..84caa491b9cad0edc4176f209c13e9221e975681\n--- a/scripts/shiki_loop.py\n+++ b/scripts/shiki_loop.py\n@@ -424,7 +424,7 @@\n         # unresolvable CCA verdict has already produced its terminal stop and is never\n         # laundered into a head-moving sync (SADR-0010). It also never fires while the\n         # policy (Guardian) gate is red \u2014 for ANY reason, not only when it is the sole\n-        # red check \u2014 so a head-bound Guardian approval, or an SADR-0015\n+        # red check \u2014 so a head-bound Guardian approval, or a SADR-0015\n         # contract-approval carry that base movement defeated (the policy gate compares\n         # base and head task governance directly), is never silently restored by\n         # adopting base wholesale. It is bounded by a durable per-task counter so the\ndiff --git a/scripts/shiki_tasks.py b/scripts/shiki_tasks.py\nindex aec3809f24b4a0c8c96fc50971cff9ae9ad30381..d531052e9d9717058d4631483cefab44c26a65c1\n--- a/scripts/shiki_tasks.py\n+++ b/scripts/shiki_tasks.py\n@@ -821,7 +821,7 @@\n         \"prompt\": prompt,\n         \"entry_skill\": \"grill-with-docs\",\n         \"required_next_steps\": [\n-            \"Run grill-with-docs until terminology, boundaries, risks, and ADR-worthy decisions are settled.\",\n+            \"Run grill-with-docs until terminology, boundaries, risks, and decision-record-worthy choices are settled.\",\n             \"Obtain the operator's explicit approval of the PRD/requirements (Spec Freeze).\",\n             \"Write a machine-readable plan JSON with grill_with_docs.status=complete and spec_freeze.status=frozen.\",\n             \"Run shiki plan ingest --plan-file PLAN.json.\",\ndiff --git a/scripts/test_shiki_goal_loop.sh b/scripts/test_shiki_goal_loop.sh\nindex 753c7b516e10f0be3bc92a4bd0eb1fa699776ae6..8f3820a0a079b3044424cfe1a6c6b7fe68896e67\n--- a/scripts/test_shiki_goal_loop.sh\n+++ b/scripts/test_shiki_goal_loop.sh\n@@ -231,8 +231,8 @@\n \n # High/critical risk merges autonomously when all required checks are green:\n # the \"MergeGate policy check\" required check IS the Guardian gate (it enforces\n-# guardian-policy.json \u2014 human approval OR external AI guardian review, ADR\n-# 0010), so green checks mean a recorded authority approved. The fake gh returns\n+# guardian-policy.json \u2014 human approval OR external AI guardian review,\n+# SADR-0010), so green checks mean a recorded authority approved. The fake gh returns\n # green checks, simulating that the Guardian gate passed.\n python3 \"$ROOT/scripts/shiki.py\" goal create --target \"$TARGET\" --title \"Guardian gate\" --outcome \"High risk merges via policy gate\" >/tmp/shiki-goal-loop-guardian-goal.json\n GGOAL=\"$(json_get /tmp/shiki-goal-loop-guardian-goal.json goal_id)\"\ndiff --git a/tests/test_adr_references.py b/tests/test_adr_references.py\nindex 51706329c797d5dd9b47e082a333fa83b261d482..d005da4f771f5e8085ec22d4b787980e68f5ae8c\n--- a/tests/test_adr_references.py\n+++ b/tests/test_adr_references.py\n@@ -1,8 +1,8 @@\n-\"\"\"Every ``ADR ####`` citation must resolve to exactly one decision record.\n+\"\"\"Every target ADR and Shiki SADR citation resolves in its own namespace.\n \n Background. ``scripts/mergegate_check.py`` justifies ``is_bookkeeping_closeout``\n-with \"ADR 0017\" nine times and ``scripts/guardian_approval_signal.py`` cites\n-0016/0017 seven times, yet for a stretch the numbering in ``docs/adr/`` stopped\n+with \"SADR-0017\" nine times and ``scripts/guardian_approval_signal.py`` cites\n+SADR-0016/SADR-0017 seven times, yet for a stretch the numbering in ``docs/adr/`` stopped\n at 0015: the authoring commits lived on a coordinator branch and no task ever\n declared ``docs/adr/...`` in its locks, so nothing carried the records to the\n default branch. The stated basis of an exemption that merged seven PRs with zero\n@@ -11,11 +11,12 @@\n and the directory drifted with no signal.\n \n This is the same unbound-pair shape behind other silent failures: a reference\n-and its target with nothing asserting they agree. This test binds them. It scans\n-every ``scripts/*.py`` (plus ``docs/**/*.md`` and the root ``*.md`` governance\n-files, which are cheap) for the ``ADR ####`` pattern and asserts each cited\n-number resolves to exactly one ``docs/adr/####-*.md``. The converse is\n-deliberately NOT required: an ADR may exist without being cited.\n+and its target with nothing asserting they agree. This test binds them. Target\n+``ADR NNNN`` citations resolve only to ``docs/adr/NNNN-*.md``; Shiki\n+``SADR-NNNN`` citations resolve only to ``docs/adr/SADR-NNNN-*.md``. The scan\n+covers current governance docs, scripts, workflows, prompts, packaging metadata,\n+and repo-local Codex guidance while deliberately excluding historical\n+``.shiki`` evidence. The converse is not required: a record may be uncited.\n \n The suite is dependency free: standard-library ``unittest`` only.\n \"\"\"\n@@ -31,17 +32,18 @@\n \n REPO_ROOT = Path(__file__).resolve().parents[1]\n \n-# \"ADR 0016\" -- a four-digit citation. The trailing \\b keeps a five-digit token\n-# from being read as a truncated four-digit number.\n-_CITATION = re.compile(r\"\\bADR (\\d{4})\\b\")\n-# \"0016-locks-bound-product-paths.md\" -- the leading four digits name the record.\n-_ADR_FILENAME = re.compile(r\"^(\\d{4})-.*\\.md$\")\n+# ``ADR 0016`` / ``SADR-0016`` -- a namespace plus four-digit citation. The\n+# trailing ``\\b`` keeps a five-digit token from being read as a truncated one.\n+_CITATION = re.compile(r\"\\b(S?ADR)[ -](\\d{4})\\b\")\n+# Target ADRs retain the numeric filename; Shiki platform decisions use SADR.\n+_DECISION_FILENAME = re.compile(r\"^(?:(SADR)-)?(\\d{4})-.*\\.md$\")\n \n \n class Violation(NamedTuple):\n     \"\"\"A citation that does not resolve to exactly one decision record.\"\"\"\n \n     citing_file: str  # repo-relative path of the file that cites the ADR\n+    namespace: str  # \"ADR\" for target decisions; \"SADR\" for Shiki decisions\n     number: str  # the cited four-digit number, e.g. \"0016\"\n     line: int  # 1-based line of the citation\n     reason: str  # \"missing\" (zero matches) or \"ambiguous\" (two or more)\n@@ -49,44 +51,59 @@\n \n     @property\n     def expected_path(self) -> str:\n-        return f\"docs/adr/{self.number}-*.md\"\n+        prefix = \"SADR-\" if self.namespace == \"SADR\" else \"\"\n+        return f\"docs/adr/{prefix}{self.number}-*.md\"\n \n     def describe(self) -> str:\n         \"\"\"Name the citing file, the cited number, and the missing path.\"\"\"\n         if self.reason == \"missing\":\n             return (\n-                f\"{self.citing_file}:{self.line} cites ADR {self.number} \"\n+                f\"{self.citing_file}:{self.line} cites {self.namespace} {self.number} \"\n                 f\"but no document matches {self.expected_path}\"\n             )\n         joined = \", \".join(self.matches)\n         return (\n-            f\"{self.citing_file}:{self.line} cites ADR {self.number} which \"\n+            f\"{self.citing_file}:{self.line} cites {self.namespace} {self.number} which \"\n             f\"resolves to {len(self.matches)} documents (expected exactly one \"\n             f\"at {self.expected_path}): {joined}\"\n         )\n \n \n-def _adr_index(root: Path) -> dict[str, list[Path]]:\n-    \"\"\"Map every four-digit ADR number to the record files that claim it.\"\"\"\n-    index: dict[str, list[Path]] = {}\n+def _adr_index(root: Path) -> dict[tuple[str, str], list[Path]]:\n+    \"\"\"Map each decision namespace and number to the files that claim it.\"\"\"\n+    index: dict[tuple[str, str], list[Path]] = {}\n     adr_dir = root / \"docs\" / \"adr\"\n     if adr_dir.is_dir():\n         for path in sorted(adr_dir.glob(\"*.md\")):\n-            match = _ADR_FILENAME.match(path.name)\n+            match = _DECISION_FILENAME.match(path.name)\n             if match:\n-                index.setdefault(match.group(1), []).append(path)\n+                namespace = \"SADR\" if match.group(1) else \"ADR\"\n+                index.setdefault((namespace, match.group(2)), []).append(path)\n     return index\n \n \n def _citing_files(root: Path, include_docs: bool = True) -> list[Path]:\n-    \"\"\"Files scanned for citations: scripts/*.py, and cheaply the markdown.\"\"\"\n+    \"\"\"Current, shipped text surfaces scanned for decision citations.\"\"\"\n     files = sorted((root / \"scripts\").glob(\"*.py\"))\n+    files += sorted((root / \"scripts\").glob(\"*.sh\"))\n     if include_docs:\n         docs = root / \"docs\"\n         if docs.is_dir():\n             files += sorted(docs.rglob(\"*.md\"))\n         files += sorted(root.glob(\"*.md\"))\n-    return files\n+        pyproject = root / \"pyproject.toml\"\n+        if pyproject.is_file():\n+            files.append(pyproject)\n+        for relative in (\".github\", \".codex\"):\n+            surface = root / relative\n+            if surface.is_dir():\n+                files += sorted(\n+                    path\n+                    for path in surface.rglob(\"*\")\n+                    if path.is_file()\n+                    and path.suffix in {\".md\", \".py\", \".sh\", \".toml\", \".yaml\", \".yml\"}\n+                )\n+    return sorted(set(files))\n \n \n def find_adr_reference_violations(\n@@ -94,10 +111,9 @@\n ) -> list[Violation]:\n     \"\"\"Return every ``ADR ####`` citation under ``root`` that fails to resolve.\n \n-    A citation resolves when exactly one ``docs/adr/####-*.md`` matches its\n-    number. Zero matches is a \"missing\" violation; two or more is \"ambiguous\".\n-    Records that are never cited produce nothing -- the converse is not\n-    required.\n+    A citation resolves when exactly one record in the same namespace matches\n+    its number. Zero matches is a \"missing\" violation; two or more is\n+    \"ambiguous\". Records that are never cited produce nothing.\n     \"\"\"\n     root = Path(root)\n     index = _adr_index(root)\n@@ -110,13 +126,15 @@\n         rel = path.relative_to(root).as_posix()\n         for lineno, line in enumerate(text.splitlines(), start=1):\n             for match in _CITATION.finditer(line):\n-                number = match.group(1)\n-                found = index.get(number, [])\n+                namespace, number = match.groups()\n+                found = index.get((namespace, number), [])\n                 if len(found) == 1:\n                     continue\n                 reason = \"missing\" if not found else \"ambiguous\"\n                 rels = [p.relative_to(root).as_posix() for p in found]\n-                violations.append(Violation(rel, number, lineno, reason, rels))\n+                violations.append(\n+                    Violation(rel, namespace, number, lineno, reason, rels)\n+                )\n     return violations\n \n \n@@ -129,8 +147,8 @@\n         self.assertEqual(\n             violations,\n             [],\n-            \"Every 'ADR ####' citation must resolve to exactly one \"\n-            f\"docs/adr/####-*.md:\\n{detail}\",\n+            \"Every ADR/SADR citation must resolve exactly once in its own \"\n+            f\"docs/adr namespace:\\n{detail}\",\n         )\n \n     def test_scan_actually_reads_scripts(self) -> None:\n@@ -142,12 +160,38 @@\n             \"the citation scan must cover scripts/*.py\",\n         )\n \n+    def test_platform_citations_do_not_use_unresolved_numeric_shorthand(self) -> None:\n+        shorthand = re.compile(r\"SADR-\\d{4}/\\d{4}\")\n+        offenders = []\n+        for path in _citing_files(REPO_ROOT):\n+            text = path.read_text(encoding=\"utf-8\")\n+            if shorthand.search(text):\n+                offenders.append(path.relative_to(REPO_ROOT).as_posix())\n+        self.assertEqual(\n+            offenders,\n+            [],\n+            \"repeat the SADR prefix so every cited number is independently resolvable\",\n+        )\n+\n     def test_carried_records_are_indexed(self) -> None:\n         # The two records this Goal carried onto the default branch, and which\n         # the scripts cite most, must be present.\n         index = _adr_index(REPO_ROOT)\n-        self.assertIn(\"0016\", index)\n-        self.assertIn(\"0017\", index)\n+        self.assertIn((\"SADR\", \"0016\"), index)\n+        self.assertIn((\"SADR\", \"0017\"), index)\n+\n+    def test_platform_tree_has_only_sadr_0001_through_0019(self) -> None:\n+        adr_dir = REPO_ROOT / \"docs\" / \"adr\"\n+        sadrs = sorted(adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\"))\n+        self.assertEqual(\n+            {path.name.split(\"-\", 2)[1] for path in sadrs},\n+            {f\"{number:04d}\" for number in range(1, 20)},\n+        )\n+        self.assertEqual(\n+            list(adr_dir.glob(\"[0-9][0-9][0-9][0-9]-*.md\")),\n+            [],\n+            \"the Shiki platform tree must not retain legacy numeric SADR paths\",\n+        )\n \n \n class FixtureAdrReferences(unittest.TestCase):\n@@ -180,6 +224,26 @@\n         self.assertIn(\"9991\", message)  # cited number\n         self.assertIn(\"docs/adr/9991-*.md\", message)  # missing path\n \n+    def test_target_adr_does_not_satisfy_missing_platform_sadr(self) -> None:\n+        root = self._make_repo(\n+            adr_names=[\"0042-target-decision.md\"],\n+            script_body='\"\"\"Target ADR 0042 exists, but platform SADR-0042 does not.\"\"\"\\n',\n+        )\n+        violations = find_adr_reference_violations(root, include_docs=False)\n+        self.assertEqual(len(violations), 1)\n+        self.assertEqual(violations[0].namespace, \"SADR\")\n+        self.assertEqual(violations[0].number, \"0042\")\n+        self.assertEqual(violations[0].expected_path, \"docs/adr/SADR-0042-*.md\")\n+\n+    def test_target_adr_and_platform_sadr_may_reuse_a_number(self) -> None:\n+        root = self._make_repo(\n+            adr_names=[\"0042-target-decision.md\", \"SADR-0042-platform-decision.md\"],\n+            script_body='\"\"\"Target ADR 0042 and platform SADR-0042 are distinct.\"\"\"\\n',\n+        )\n+        self.assertEqual(\n+            find_adr_reference_violations(root, include_docs=False), []\n+        )\n+\n     def test_uncited_adr_is_allowed(self) -> None:\n         # An ADR exists but nothing cites it -> not a violation. The converse\n         # (every ADR must be cited) is explicitly NOT required.\ndiff --git a/tests/test_guardian_review.py b/tests/test_guardian_review.py\nindex 88829e7810212635e2fc98aa5918d6f26294677d..00a85a237acd5bf2d6cb5b294128198c422402ca\n--- a/tests/test_guardian_review.py\n+++ b/tests/test_guardian_review.py\n@@ -162,7 +162,7 @@\n \n     def test_docs_only_is_light_and_not_conservative(self):\n         focus = gr.classify_review_focus([\"docs/adr/0099-x.md\", \"README.md\"], \"\", \"low\")\n-        self.assertTrue(any(\"Docs/ADR-only\" in f for f in focus))\n+        self.assertTrue(any(\"Docs/decision-record-only\" in f for f in focus))\n         self.assertFalse(any(\"conservative default\" in f.lower() for f in focus))\n \n     def test_unknown_category_is_conservative(self):\ndiff --git a/tests/test_policy_job_merged_prs.py b/tests/test_policy_job_merged_prs.py\nindex d45512e74681e5e649279236c7d4e79d5c3da471..b97989f8bd74d091de1a6b5275a20d8dc68c4512\n--- a/tests/test_policy_job_merged_prs.py\n+++ b/tests/test_policy_job_merged_prs.py\n@@ -3,8 +3,8 @@\n \n ``.github/workflows/shiki-cca-completion.yml`` has two consumers of the merged-PR\n proof: the CCA *signal* (``guardian_approval_signal.py`` in \"Prepare CCA context\")\n-and the authoritative gate (``mergegate_check.py`` in \"Enforce MergeGate\"). ADR\n-0017's bookkeeping-closeout exemption (condition 6) needs the merged implementation\n+and the authoritative gate (``mergegate_check.py`` in \"Enforce MergeGate\").\n+SADR-0017's bookkeeping-closeout exemption (condition 6) needs the merged implementation\n PR proven, and a closeout carries NO ``post_merge_reconcile`` marker. The signal\n already resolves ``MERGED_PRS`` unconditionally, but \"Enforce MergeGate\" used to\n resolve it only inside ``grep -q '<!-- shiki:post_merge_reconcile -->'``. So a\ndiff --git a/tests/test_sadr_install_migration.py b/tests/test_sadr_install_migration.py\nnew file mode 100644\nindex 0000000000000000000000000000000000000000..d51eac872b1c79ff4579c4c1401ca5d775a60d8d\n--- /dev/null\n+++ b/tests/test_sadr_install_migration.py\n@@ -0,0 +1,237 @@\n+\"\"\"SADR installation and collision-safe legacy upgrade behavior.\"\"\"\n+\n+from __future__ import annotations\n+\n+import contextlib\n+import hashlib\n+import io\n+import json\n+import shutil\n+import tempfile\n+import unittest\n+from pathlib import Path\n+\n+import shiki_test_support  # noqa: F401  (path bootstrap)\n+\n+import shiki_installer\n+from tests.test_adr_references import find_adr_reference_violations\n+\n+\n+PRODUCT_NUMBERS = tuple(range(13, 19))\n+\n+\n+def _quiet_install(target: Path, *, force: bool, validate: bool) -> None:\n+    with contextlib.redirect_stdout(io.StringIO()):\n+        shiki_installer.install_template(target, force=force, validate=validate)\n+\n+\n+def _sha256(path: Path) -> str:\n+    return hashlib.sha256(path.read_bytes()).hexdigest()\n+\n+\n+def _legacy_sadr_paths() -> tuple[str, ...]:\n+    return tuple(\n+        f\"docs/adr/{path.name.removeprefix('SADR-')}\"\n+        for path in sorted((shiki_installer.ROOT / \"docs\" / \"adr\").glob(\"SADR-*.md\"))\n+        if path.name != \"SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\"\n+    )\n+\n+\n+def _create_legacy_upgrade_fixture(\n+    target: Path,\n+) -> tuple[dict[str, bytes], tuple[str, ...]]:\n+    adr_dir = target / \"docs\" / \"adr\"\n+    adr_dir.mkdir(parents=True)\n+    product_bytes = {\n+        f\"{number:04d}-cubeshare-product-decision.md\": (\n+            f\"# CubeShare ADR {number:04d}\\n\\nDo not rewrite {number}.\\n\"\n+        ).encode()\n+        for number in PRODUCT_NUMBERS\n+    }\n+    for name, content in product_bytes.items():\n+        (adr_dir / name).write_bytes(content)\n+\n+    legacy_paths = _legacy_sadr_paths()\n+    for relative in legacy_paths:\n+        (target / relative).write_bytes(f\"legacy Shiki bytes for {relative}\\n\".encode())\n+\n+    stamp_path = target / shiki_installer.INSTALL_STAMP_PATH\n+    stamp_path.parent.mkdir(parents=True, exist_ok=True)\n+    stamp = {\n+        \"version\": shiki_installer.INSTALL_STAMP_VERSION,\n+        \"platform_commit\": None,\n+        \"installed_at\": \"2000-01-01T00:00:00+00:00\",\n+        \"digests\": {\n+            relative: _sha256(target / relative) for relative in legacy_paths\n+        },\n+    }\n+    stamp_path.write_text(\n+        json.dumps(stamp, indent=2, sort_keys=True) + \"\\n\", encoding=\"utf-8\"\n+    )\n+    return product_bytes, legacy_paths\n+\n+\n+def _tree_snapshot(root: Path) -> dict[str, tuple[str, bytes | str]]:\n+    \"\"\"Recursive byte/shape snapshot used to prove preflight is write-free.\"\"\"\n+    snapshot: dict[str, tuple[str, bytes | str]] = {}\n+    for path in sorted(root.rglob(\"*\")):\n+        relative = path.relative_to(root).as_posix()\n+        if path.is_symlink():\n+            snapshot[relative] = (\"symlink\", path.readlink().as_posix())\n+        elif path.is_dir():\n+            snapshot[relative] = (\"directory\", \"\")\n+        else:\n+            snapshot[relative] = (\"file\", path.read_bytes())\n+    return snapshot\n+\n+\n+class FreshSadrInstallTests(unittest.TestCase):\n+    def setUp(self) -> None:\n+        self.tmp = Path(tempfile.mkdtemp(prefix=\"shiki-sadr-fresh-\"))\n+        self.addCleanup(lambda: shutil.rmtree(self.tmp, ignore_errors=True))\n+        self.target = self.tmp / \"target\"\n+        self.adr_dir = self.target / \"docs\" / \"adr\"\n+        self.adr_dir.mkdir(parents=True)\n+        self.product_bytes = {\n+            f\"{number:04d}-product-decision.md\": (\n+                f\"# Product ADR {number:04d}\\n\\nTarget-owned bytes {number}.\\n\"\n+            ).encode()\n+            for number in PRODUCT_NUMBERS\n+        }\n+        for name, content in self.product_bytes.items():\n+            (self.adr_dir / name).write_bytes(content)\n+\n+    def test_fresh_install_preserves_product_adrs_and_installs_all_sadrs(self) -> None:\n+        _quiet_install(self.target, force=False, validate=True)\n+\n+        for name, expected in self.product_bytes.items():\n+            self.assertEqual((self.adr_dir / name).read_bytes(), expected)\n+\n+        sadr_numbers = {\n+            path.name.split(\"-\", 2)[1]\n+            for path in self.adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\")\n+        }\n+        self.assertEqual(sadr_numbers, {f\"{number:04d}\" for number in range(1, 20)})\n+\n+        numeric_records = {\n+            path.name\n+            for path in self.adr_dir.glob(\"[0-9][0-9][0-9][0-9]-*.md\")\n+        }\n+        self.assertEqual(numeric_records, set(self.product_bytes))\n+        stamp = json.loads(\n+            (self.target / shiki_installer.INSTALL_STAMP_PATH).read_text(\n+                encoding=\"utf-8\"\n+            )\n+        )\n+        for path in self.adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\"):\n+            relative = path.relative_to(self.target).as_posix()\n+            self.assertEqual(stamp[\"digests\"][relative], _sha256(path))\n+        self.assertEqual(find_adr_reference_violations(self.target), [])\n+\n+\n+class LegacySadrUpgradeTests(unittest.TestCase):\n+    def setUp(self) -> None:\n+        self.tmp = Path(tempfile.mkdtemp(prefix=\"shiki-sadr-upgrade-\"))\n+        self.addCleanup(lambda: shutil.rmtree(self.tmp, ignore_errors=True))\n+        self.target = self.tmp / \"target\"\n+        self.adr_dir = self.target / \"docs\" / \"adr\"\n+        self.product_bytes, self.legacy_paths = _create_legacy_upgrade_fixture(\n+            self.target\n+        )\n+\n+    def test_force_upgrade_deletes_only_stamped_legacy_sadrs(self) -> None:\n+        _quiet_install(self.target, force=True, validate=True)\n+\n+        for relative in self.legacy_paths:\n+            self.assertFalse((self.target / relative).exists(), relative)\n+        for name, expected in self.product_bytes.items():\n+            self.assertEqual((self.adr_dir / name).read_bytes(), expected)\n+\n+        sadrs = list(self.adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\"))\n+        self.assertEqual(len(sadrs), 19)\n+        refreshed = json.loads(\n+            (self.target / shiki_installer.INSTALL_STAMP_PATH).read_text(encoding=\"utf-8\")\n+        )\n+        for relative in self.legacy_paths:\n+            self.assertNotIn(relative, refreshed[\"digests\"])\n+        for path in sadrs:\n+            relative = path.relative_to(self.target).as_posix()\n+            self.assertEqual(refreshed[\"digests\"][relative], _sha256(path))\n+\n+    def test_non_force_install_never_deletes_legacy_files(self) -> None:\n+        before = {\n+            relative: (self.target / relative).read_bytes()\n+            for relative in self.legacy_paths\n+        }\n+        _quiet_install(self.target, force=False, validate=False)\n+        for relative, expected in before.items():\n+            self.assertEqual((self.target / relative).read_bytes(), expected)\n+        refreshed = json.loads(\n+            (self.target / shiki_installer.INSTALL_STAMP_PATH).read_text(encoding=\"utf-8\")\n+        )\n+        for relative in self.legacy_paths:\n+            self.assertEqual(refreshed[\"digests\"][relative], _sha256(self.target / relative))\n+\n+\n+class LegacyCleanupPreflightTests(unittest.TestCase):\n+    def setUp(self) -> None:\n+        self.tmp = Path(tempfile.mkdtemp(prefix=\"shiki-sadr-preflight-\"))\n+        self.addCleanup(lambda: shutil.rmtree(self.tmp, ignore_errors=True))\n+\n+    def test_all_ownership_blockers_abort_before_any_write(self) -> None:\n+        cases = (\n+            (\"absent\", \"install stamp is absent\"),\n+            (\"unreadable\", \"install stamp is unreadable\"),\n+            (\"malformed\", \"install stamp is malformed\"),\n+            (\"missing-digest\", \"install stamp has no digest\"),\n+            (\"digest-mismatch\", \"digest mismatch\"),\n+        )\n+        for name, expected_reason in cases:\n+            with self.subTest(name=name):\n+                target = self.tmp / name\n+                _, legacy_paths = _create_legacy_upgrade_fixture(target)\n+                stamp_path = target / shiki_installer.INSTALL_STAMP_PATH\n+\n+                if name == \"absent\":\n+                    stamp_path.unlink()\n+                elif name == \"unreadable\":\n+                    stamp_path.unlink()\n+                    stamp_path.mkdir()\n+                elif name == \"malformed\":\n+                    stamp_path.write_text(\"{not-json\\n\", encoding=\"utf-8\")\n+                else:\n+                    stamp = json.loads(stamp_path.read_text(encoding=\"utf-8\"))\n+                    if name == \"missing-digest\":\n+                        stamp[\"digests\"] = {}\n+                    else:\n+                        stamp[\"digests\"] = {\n+                            relative: \"deadbeef\" for relative in legacy_paths\n+                        }\n+                    stamp_path.write_text(\n+                        json.dumps(stamp, indent=2, sort_keys=True) + \"\\n\",\n+                        encoding=\"utf-8\",\n+                    )\n+\n+                before = _tree_snapshot(target)\n+                with self.assertRaises(shiki_installer.ShikiError) as raised:\n+                    _quiet_install(target, force=True, validate=False)\n+                after = _tree_snapshot(target)\n+\n+                self.assertEqual(before, after, f\"{name} preflight changed the tree\")\n+                message = str(raised.exception)\n+                self.assertIn(expected_reason, message)\n+                self.assertIn(\"No files were written\", message)\n+                for relative in legacy_paths:\n+                    self.assertIn(relative, message)\n+\n+    def test_absent_stamp_is_allowed_when_no_legacy_path_exists(self) -> None:\n+        target = self.tmp / \"no-legacy\"\n+        target.mkdir()\n+        _quiet_install(target, force=True, validate=False)\n+        self.assertTrue(\n+            (target / \"docs\" / \"adr\" / \"SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\").is_file()\n+        )\n+\n+\n+if __name__ == \"__main__\":\n+    unittest.main()\n\n2026-08-07T08:28:55.196296Z ERROR codex_models_manager::manager: failed to renew cache TTL: missing field `base_instructions` at line 94 column 5\ncodex\nOne lifecycle check remained: the red-green-refactor observations also need their own durable record, separate from the review verdict. I\u2019m adding that evidence now, then validating the mirror once more; this changes no implementation or approval state.\nexec\n/bin/zsh -lc \"date -u '+%Y%m%dT%H%M%S000000Z %Y-%m-%dT%H:%M:%S+00:00'\" in /private/tmp/shiki-adr-collision.pa0KwY/.worktrees/shiki-t-20260807t073307221254z-2d22d654-introduc\n succeeded in 0ms:\n20260807T082914000000Z 2026-08-07T08:29:14+00:00\n\ndiff --git a/.codex/skills/shiki/SKILL.md b/.codex/skills/shiki/SKILL.md\nindex c8e2e85d9d75d49cc4f2d6286ee22659c4e53e75..93cded623b2c15d9f65c76491a1c2d813ac9e6c0\n--- a/.codex/skills/shiki/SKILL.md\n+++ b/.codex/skills/shiki/SKILL.md\n@@ -18,6 +18,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n Codex CLI does not register this skill as a custom `/shiki` slash command. When\n the user types `/shiki` in Codex CLI and gets \"Unrecognized command\", explain\n that this is expected: invoke Shiki in Codex with natural language such as\n@@ -69,7 +72,7 @@\n \n ## Responsibilities\n \n-- Claude Code implements and repairs by default (`shiki runner claude`, ADR 0008); Codex implements and repairs tasks explicitly assigned to `codex`.\n+- Claude Code implements and repairs by default (`shiki runner claude`, SADR-0008); Codex implements and repairs tasks explicitly assigned to `codex`.\n - Claude Code Action can act as GitHub-side CCA or reviewer.\n - CCA judges completion.\n - MergeGate authorizes state transitions and merge readiness.\ndiff --git a/.github/prompts/cca-completion-check.md b/.github/prompts/cca-completion-check.md\nindex ed203ad5f302ecc81372d8ab27d2b1232ca97497..fd89950528333ef6242e6d5443904f5392ab7e56\n--- a/.github/prompts/cca-completion-check.md\n+++ b/.github/prompts/cca-completion-check.md\n@@ -19,6 +19,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n ## Required Reading\n \n Read only the files needed to judge the PR contract. Prefer this order:\ndiff --git a/.github/prompts/codex-task-handoff.md b/.github/prompts/codex-task-handoff.md\nindex 695e211d76dea1c60e8d5bb360e78487808286a4..59441d576b01a39569722f226c854e5a740e9eb5\n--- a/.github/prompts/codex-task-handoff.md\n+++ b/.github/prompts/codex-task-handoff.md\n@@ -2,7 +2,7 @@\n \n You are Codex Front acting as the Implementer for a Shiki task.\n \n-Follow `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant ADRs, and `docs/agents/codex-handoff.md`.\n+Follow `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant Shiki SADRs and target ADRs, and `docs/agents/codex-handoff.md`.\n \n Rules:\n \ndiff --git a/.shiki/ledger/L-20260807T082800000000Z-c0de0019.json b/.shiki/ledger/L-20260807T082800000000Z-c0de0019.json\nnew file mode 100644\nindex 0000000000000000000000000000000000000000..bac355ecea44037859b3715f58bc32f3353cfcfa\n--- /dev/null\n+++ b/.shiki/ledger/L-20260807T082800000000Z-c0de0019.json\n@@ -0,0 +1,34 @@\n+{\n+  \"actor\": \"codex\",\n+  \"evidence\": [\n+    \"Full task diff reviewed against scope, non-goals, locks, acceptance checks, failure atomicity, path ownership, and compatibility requirements\",\n+    \"Finding repaired: tests/test_sadr_install_migration.py now asserts every fresh-installed SADR path has the matching SHA-256 digest in .shiki/install-stamp.json\",\n+    \"Re-review found no remaining blocking issues; rejected findings: none\",\n+    \"python3 -m unittest discover -s tests: 1147 tests passed\",\n+    \"bash scripts/test_shiki_control_plane.sh: passed\",\n+    \"bash scripts/test_shiki_adversarial_state.sh: passed\",\n+    \"python3 scripts/validate_shiki.py --target .: passed\"\n+  ],\n+  \"goal_id\": \"G-20260807T073307219752Z-1ee1ed68\",\n+  \"id\": \"L-20260807T082800000000Z-c0de0019\",\n+  \"links\": [],\n+  \"review\": {\n+    \"applied_fixes\": [\n+      \"Added direct fresh-install stamp path/digest assertions and reran focused plus full verification\"\n+    ],\n+    \"findings\": [\n+      {\n+        \"detail\": \"The fresh-install test proved SADR files were installed but did not directly prove their paths and digests were recorded in the refreshed install stamp.\",\n+        \"resolution\": \"fixed_and_re_reviewed\",\n+        \"severity\": \"blocking\",\n+        \"title\": \"Fresh-install stamp evidence was indirect\"\n+      }\n+    ],\n+    \"rejected_findings\": [],\n+    \"verdict\": \"clean_after_repairs\"\n+  },\n+  \"summary\": \"code-review implementer verdict CLEAN after re-review: findings=1; applied_fixes=1 (fresh-install stamp-digest assertion); rejected_findings=0 (none)\",\n+  \"task_id\": \"T-20260807T073307221254Z-2d22d654\",\n+  \"timestamp\": \"2026-08-07T08:28:00+00:00\",\n+  \"type\": \"review\"\n+}\ndiff --git a/.shiki/tasks/T-20260807T073307221254Z-2d22d654.json b/.shiki/tasks/T-20260807T073307221254Z-2d22d654.json\nindex e7e150cc1637f1cfac2199e04055e93b53630dcb..78f7fb5ef2ab3dad7e3731673ac2c34b2a8ed149\n--- a/.shiki/tasks/T-20260807T073307221254Z-2d22d654.json\n+++ b/.shiki/tasks/T-20260807T073307221254Z-2d22d654.json\n@@ -36,7 +36,8 @@\n     \"L-20260807T080107948254Z-61f24da0\",\n     \"L-20260807T080228312210Z-07ac626e\",\n     \"L-20260807T080228575038Z-d574f629\",\n-    \"L-20260807T080256330734Z-5bf40cb6\"\n+    \"L-20260807T080256330734Z-5bf40cb6\",\n+    \"L-20260807T082800000000Z-c0de0019\"\n   ],\n   \"locks\": [\n     \"path:.codex/**\",\ndiff --git a/AGENTS.md b/AGENTS.md\nindex 7f041100d49c2a7525f51cb0f5d696a4a6d1c78c..a06bfbabc6fa48a007389e25e12d7ed523936bed\n--- a/AGENTS.md\n+++ b/AGENTS.md\n@@ -22,7 +22,7 @@\n - **Codex Front** is an optional implementer through Codex App, Codex CLI, Codex IDE extension, or Codex Web signed in with ChatGPT OAuth/subscription auth, dispatched with `shiki runner codex` when a task is explicitly assigned to `codex`.\n - **GitHub CCA** is implemented by Claude Code Action by default, using `CLAUDE_CODE_OAUTH_TOKEN`.\n \n-Do not assume `openai/codex-action`, `OPENAI_API_KEY`, or API-key based Codex automation in the default Shiki path. API-key runners are explicit target-repo extensions and require their own ADR.\n+Do not assume `openai/codex-action`, `OPENAI_API_KEY`, or API-key based Codex automation in the default Shiki path. API-key runners are explicit target-repo extensions and require their own target ADR.\n \n Shiki is not:\n \n@@ -44,10 +44,14 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use\n+`SADR-NNNN-*.md`; target product decisions retain `NNNN-*.md` and the `ADR`\n+namespace.\n+\n Use the canonical source-of-truth order above. Runtime-specific reading orders\n are operational guidance only and must not override or reorder it.\n \n-Conversation state is not durable truth. If a decision matters, put it in GitHub, `.shiki/`, `CONTEXT.md`, or an ADR.\n+Conversation state is not durable truth. If a decision matters, put it in GitHub, `.shiki/`, `CONTEXT.md`, a Shiki SADR, or a target ADR.\n \n If GitHub and `.shiki/` disagree, surface the conflict and prefer GitHub operational state until the mirror is repaired.\n \n@@ -75,8 +79,8 @@\n Every non-trivial change follows this loop:\n \n 1. **Goal**: clarify outcome, completion conditions, non-goals, risk level, and success signals.\n-2. **grill-with-docs**: challenge the plan against domain docs, ADRs, code reality, terminology, and edge scenarios.\n-3. **Context & Impact**: find relevant docs, ADRs, code, dependencies, owners, locks, and verification surfaces.\n+2. **grill-with-docs**: challenge the plan against domain docs, Shiki SADRs, target ADRs, code reality, terminology, and edge scenarios.\n+3. **Context & Impact**: find relevant docs, SADRs, target ADRs, code, dependencies, owners, locks, and verification surfaces.\n 4. **PRD and Spec Freeze**: use `to-prd` when context is settled enough to become durable product/engineering intent. The operator's explicit approval of the PRD is Spec Freeze: the plan records a `spec_freeze` block, and no plan runs without it. Steps 1-4 form Requirements Definition \u2014 one continuous operator dialogue. After Spec Freeze, scope changes require an operator-approved Spec Amendment; non-scope-moving interpretations go to the Assumption Log.\n 5. **Issues**: use `to-issues` to create independently grabbable vertical-slice issues. Prefer AFK slices over HITL slices where possible.\n 6. **Triage**: label issues for readiness, risk, runtime, skills, and MergeGate state.\n@@ -167,7 +171,7 @@\n - Testing decisions.\n - Out-of-scope items.\n - Further notes.\n-- Links to relevant domain terms and ADRs.\n+- Links to relevant domain terms, SADRs, and target ADRs.\n \n Every executable task must include:\n \n@@ -193,7 +197,7 @@\n \n Context & Impact must identify:\n \n-- Relevant documents, domain terms, ADRs, and past decisions.\n+- Relevant documents, domain terms, SADRs, target ADRs, and past decisions.\n - Relevant modules, interfaces, seams, callers, owners, and tests.\n - Dependency relationships between tasks.\n - Candidate locks and lock conflicts.\n@@ -210,7 +214,7 @@\n | Skill | Required when |\n | --- | --- |\n | `setup-matt-pocock-skills` | First configuring the repo for skills, or when issue tracker, triage labels, or domain docs are missing. |\n-| `grill-with-docs` | Default for non-trivial Goals before PRD/issues; always required when plans, terminology, boundaries, tradeoffs, or ADR-worthy decisions are ambiguous. |\n+| `grill-with-docs` | Default for non-trivial Goals before PRD/issues; always required when plans, terminology, boundaries, tradeoffs, or decision-record-worthy choices are ambiguous. |\n | `zoom-out` | The agent lacks an architectural map of the relevant code area. |\n | `to-prd` | Settled Goal context must become durable product/engineering intent. |\n | `to-issues` | A Goal, PRD, or plan must become independently grabbable vertical-slice issues. |\n@@ -348,7 +352,7 @@\n - Three or more new files.\n - Any high-risk or critical label.\n \n-When the architecture gate triggers, run `grill-with-docs` or `improve-codebase-architecture` as appropriate and record durable decisions in an ADR when needed.\n+When the architecture gate triggers, run `grill-with-docs` or `improve-codebase-architecture` as appropriate and record durable decisions in a Shiki SADR or target ADR as applicable.\n \n ## Engineering Discipline\n \ndiff --git a/CLAUDE.md b/CLAUDE.md\nindex 98a932e7841a632866fcd53b0bad0e3af3711d3d..98a31399404d6fb044b4fd493f4459ac87f439de\n--- a/CLAUDE.md\n+++ b/CLAUDE.md\n@@ -14,6 +14,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n ## Claude Role\n \n Default to planner, default implementer, reviewer, coordinator, documentation editor, CCA-style completion judge, and final-judgment assistant.\n@@ -57,7 +60,7 @@\n At the start of a Shiki session:\n \n 1. Read `AGENTS.md`.\n-2. Read `CONTEXT.md` and relevant ADRs.\n+2. Read `CONTEXT.md` and relevant Shiki SADRs and target ADRs.\n 3. Read active `.shiki/` Goal/task/ledger state when present.\n 4. Read active distilled rules from `.shiki/memories/` (status `distilled`, `active: true`, not revoked or superseded) for applicable guidance; the same rules are injected deterministically into task handoffs as the `## Distilled Rules` section (\u00a73.5).\n 5. Read the GitHub Issue or PR when the task is GitHub-backed.\n@@ -74,7 +77,7 @@\n - Define the Goal, completion conditions, non-goals, and risk level.\n - Treat `grill-with-docs` as the default planning entry point for non-trivial Goals.\n - Challenge domain terms against `CONTEXT.md`.\n-- Challenge hard-to-reverse tradeoffs against ADRs.\n+- Challenge hard-to-reverse tradeoffs against SADRs and target ADRs.\n - Explore code instead of asking when code can answer the question.\n - Run Context & Impact before implementation planning.\n - Produce a Task DAG, not an unordered checklist.\n@@ -94,7 +97,7 @@\n - For each question, provide your recommended answer.\n - Resolve dependency decisions before downstream design questions.\n - Update `CONTEXT.md` inline when terminology crystallizes.\n-- Offer ADRs only when the decision is hard to reverse, surprising without context, and a real tradeoff.\n+- Offer a SADR or target ADR only when the decision is hard to reverse, surprising without context, and a real tradeoff.\n - Convert unresolved questions into HITL blockers rather than allowing Codex to guess.\n \n ## PRD and Issues Standard\n@@ -103,7 +106,7 @@\n \n - Use the project's domain glossary vocabulary.\n - Avoid volatile file paths unless they are essential.\n-- Include implementation decisions, testing decisions, out-of-scope items, and relevant ADR/domain links.\n+- Include implementation decisions, testing decisions, out-of-scope items, and relevant SADR/target-ADR/domain links.\n - Identify deep-module opportunities and testable interfaces.\n \n When creating issues:\n@@ -121,7 +124,7 @@\n - Branch/worktree target.\n - Scope and non-goals.\n - Dependencies and locks.\n-- Relevant docs, ADRs, modules, and tests.\n+- Relevant docs, SADRs, target ADRs, modules, and tests.\n - Required skill invocations.\n - TDD expectations.\n - Pre-PR `code-review` gate expectations.\n@@ -152,7 +155,7 @@\n \n - Treat yourself as a judge, not implementer.\n - Do not write production code.\n-- Read `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant ADRs, PRD, task issue, PR body, diff, checks, reviews, labels, and `.shiki/` state.\n+- Read `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant SADRs and target ADRs, PRD, task issue, PR body, diff, checks, reviews, labels, and `.shiki/` state.\n - Evaluate every applicable checklist in `docs/agents/checklists.md`.\n - Map each acceptance criterion to durable evidence.\n - Distinguish implementation failure from missing evidence.\ndiff --git a/CONTEXT.md b/CONTEXT.md\nindex e61a9be06a0cce0d026484083a657bde2067e234..237ee8aa3ea8fab334ece49768bc5ed3b5af0974\n--- a/CONTEXT.md\n+++ b/CONTEXT.md\n@@ -25,7 +25,7 @@\n _Avoid_: jumping directly into code\n \n **grill-with-docs**:\n-A planning skill that challenges a plan against domain language, ADRs, code reality, and concrete edge scenarios. It resolves design-tree questions before PRD/issues. It runs inside Requirements Definition.\n+A planning skill that challenges a plan against domain language, Shiki SADRs, target ADRs, code reality, and concrete edge scenarios. It resolves design-tree questions before PRD/issues. It runs inside Requirements Definition.\n _Avoid_: generic brainstorming, silent assumptions\n \n **Requirements Definition**:\ndiff --git a/CONTRIBUTING.md b/CONTRIBUTING.md\nindex c6d9b656e73abb8a55e5f660123e03153dcb3e6d..9e37a8fbf668db5b22461aaae5548b8f1354394c\n--- a/CONTRIBUTING.md\n+++ b/CONTRIBUTING.md\n@@ -17,7 +17,7 @@\n 1. **Goal** \u2014 start from a user-approved Goal with an outcome, completion\n    conditions, non-goals, risk level, and success signals.\n 2. **grill-with-docs** \u2014 for any non-trivial change, challenge the plan against\n-   domain language, ADRs, and code reality before writing a PRD.\n+   domain language, Shiki SADRs, target ADRs, and code reality before writing a PRD.\n 3. **PRD** \u2014 capture settled product/engineering intent (`to-prd`).\n 4. **Spec Freeze** \u2014 the operator explicitly approves the PRD/requirements;\n    the plan records a `spec_freeze` block and no plan runs without it\n@@ -94,7 +94,7 @@\n   entries.\n - Do not hand-edit `.shiki/goals/*.json` or `.shiki/manifest.json` in a task PR.\n - Conversation state is not durable truth. If a decision matters, record it in\n-  GitHub, `.shiki/`, `CONTEXT.md`, or an ADR.\n+  GitHub, `.shiki/`, `CONTEXT.md`, a Shiki SADR, or a target ADR.\n \n ## Architecture Gate\n \n@@ -103,7 +103,7 @@\n or CI policy, package structure, three or more structural files, more than 100\n net new lines in a core module, three or more new files, or any high-risk label.\n When triggered, run `grill-with-docs` or `improve-codebase-architecture` and\n-record durable decisions in an ADR when needed (see `docs/adr/`).\n+record durable decisions in a Shiki SADR or target ADR as applicable (see `docs/adr/`).\n \n ## Safety\n \ndiff --git a/SYSTEM_PROMPT.md b/SYSTEM_PROMPT.md\nindex aaaea45e3a896c3a44ba89eec8bb84325ecf6c62..da009c86a88285269dee0155e58e550c6d744ea9\n--- a/SYSTEM_PROMPT.md\n+++ b/SYSTEM_PROMPT.md\n@@ -30,6 +30,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n Use the canonical source-of-truth order above. Runtime-specific reading orders\n are operational guidance only and must not override or reorder it. The current\n conversation is non-durable operator input.\n@@ -48,14 +51,14 @@\n \n A runtime may only do work that is explicitly assigned by the Goal, task, PR, or MergeGate decision.\n \n-Do not assume `openai/codex-action`, `OPENAI_API_KEY`, or API-key based Codex automation in the default Shiki path. API-key runners are explicit target-repo extensions and require their own ADR.\n+Do not assume `openai/codex-action`, `OPENAI_API_KEY`, or API-key based Codex automation in the default Shiki path. API-key runners are explicit target-repo extensions and require their own target ADR.\n \n ## 4. Non-negotiable Shiki flow\n \n For any non-trivial Goal, use this flow:\n \n 1. **Goal Seek**: define outcome, non-goals, risk level, completion criteria, and evidence requirements.\n-2. **grill-with-docs**: challenge the plan against `CONTEXT.md`, ADRs, code reality, terminology, boundaries, and edge scenarios. Ask one question at a time when the operator is available. If code can answer a question, inspect code instead of asking.\n+2. **grill-with-docs**: challenge the plan against `CONTEXT.md`, Shiki SADRs, target ADRs, code reality, terminology, boundaries, and edge scenarios. Ask one question at a time when the operator is available. If code can answer a question, inspect code instead of asking.\n 3. **Context & Impact**: identify modules, interfaces, seams, callers, dependencies, locks, risk, and verification surfaces.\n 4. **to-prd + Spec Freeze**: turn settled context into a PRD using domain vocabulary and testing decisions. The operator's explicit approval of the PRD is Spec Freeze, recorded as the plan's `spec_freeze` block; plans without it do not run. Steps 1-4 form Requirements Definition.\n 5. **to-issues**: decompose the PRD into independently grabbable vertical-slice issues. Prefer AFK slices; mark HITL slices when judgment is still required.\n@@ -75,7 +78,7 @@\n Before making any material change:\n \n 1. Read `AGENTS.md` and any runtime-specific wrapper such as `CLAUDE.md`.\n-2. Read `CONTEXT.md` and relevant ADRs.\n+2. Read `CONTEXT.md` and relevant SADRs and target ADRs.\n 3. Read the GitHub Issue / PR and `.shiki/` state for the active Goal or task.\n 4. Identify your role: Planner, Implementer, Reviewer, Completion Check Agent, Repairer, or Guardian-assist.\n 5. Identify Goal id, task id, branch, PR, dependencies, locks, risk level, required skills, and required evidence.\n@@ -103,7 +106,7 @@\n \n At minimum, identify:\n \n-- Relevant documents, ADRs, domain terms, and prior decisions.\n+- Relevant documents, SADRs, target ADRs, domain terms, and prior decisions.\n - Relevant modules, interfaces, seams, callers, owners, and tests.\n - Dependency relationships between tasks.\n - Candidate file locks and lock conflicts.\n@@ -152,7 +155,7 @@\n \n CCA must read:\n \n-- `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant ADRs.\n+- `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant SADRs and target ADRs.\n - Parent Goal issue, PRD issue, task issue, comments, and labels.\n - PR body, diff, commits, changed files, reviews, CI checks, and artifacts available to the workflow.\n - `.shiki/` task contract, locks, ledger entries, CCA checklist profile, and prior repair packets.\n@@ -214,7 +217,7 @@\n - Three or more new files.\n - Any high-risk or critical label.\n \n-When the architecture gate triggers, run `grill-with-docs` or `improve-codebase-architecture` as appropriate and record the decision in an ADR if it is hard to reverse or likely to be re-litigated.\n+When the architecture gate triggers, run `grill-with-docs` or `improve-codebase-architecture` as appropriate and record the decision in a Shiki SADR or target ADR as applicable if it is hard to reverse or likely to be re-litigated.\n \n ## 14. Skill Gate\n \n@@ -223,7 +226,7 @@\n Use these skills:\n \n - `setup-matt-pocock-skills`: before first use of the engineering skill set, or when issue tracker, triage labels, or domain docs are missing.\n-- `grill-with-docs`: default for non-trivial Goals before PRD/issues; required for ambiguous plans, terminology, boundaries, ADR-worthy tradeoffs, or design-tree decisions.\n+- `grill-with-docs`: default for non-trivial Goals before PRD/issues; required for ambiguous plans, terminology, boundaries, decision-record-worthy tradeoffs, or design-tree decisions.\n - `zoom-out`: unfamiliar code area or missing architectural map.\n - `to-prd`: convert settled context into a PRD.\n - `to-issues`: break a PRD, plan, or Goal into independently grabbable vertical-slice issues.\ndiff --git a/docs/adr/0001-keep-shiki-as-platform-name.md b/docs/adr/SADR-0001-keep-shiki-as-platform-name.md\nindex 20a9637c3b9d8c97de7ba8db7cfae4d82d9a7dba..8b685439959aada1b221c112f0ac3c51ca8a1695\n--- a/docs/adr/0001-keep-shiki-as-platform-name.md\n+++ b/docs/adr/SADR-0001-keep-shiki-as-platform-name.md\n@@ -1,3 +1,3 @@\n-# Keep Shiki As The Platform Name\n+# SADR-0001: Keep Shiki As The Platform Name\n \n Shiki remains the product name for the GitHub-native agentic engineering control plane. We considered more descriptive names such as MergePilot, MergeGate, and Kiroku, but kept Shiki because it carries the existing goal-seeking framework identity while remaining independent of Hermes, Codex, Claude Code, or any single runtime.\ndiff --git a/docs/adr/0002-use-agents-as-shared-constitution.md b/docs/adr/SADR-0002-use-agents-as-shared-constitution.md\nindex 73de30ecb078120eac540d9bc51855e3cd48d0e3..c5486182bf06fca36ea508496b013c67302fd989\n--- a/docs/adr/0002-use-agents-as-shared-constitution.md\n+++ b/docs/adr/SADR-0002-use-agents-as-shared-constitution.md\n@@ -1,3 +1,3 @@\n-# Use AGENTS As The Shared Agent Constitution\n+# SADR-0002: Use AGENTS As The Shared Agent Constitution\n \n Shiki uses `AGENTS.md` as the runtime-neutral constitution and `CLAUDE.md` as a thin Claude Code wrapper. This keeps Codex, Claude Code, Hermes Runner, and future Agent Runtimes aligned on the same GitHub-first MergeGate rules while still allowing runtime-specific instructions where needed.\ndiff --git a/docs/adr/0003-require-skill-gate-for-mergegate-readiness.md b/docs/adr/SADR-0003-require-skill-gate-for-mergegate-readiness.md\nindex 424be0c9af4c8f0136f1fde68a468c47ff84e764..7fc337987c7df9792f75f181dfcbed75fe5ffff2\n--- a/docs/adr/0003-require-skill-gate-for-mergegate-readiness.md\n+++ b/docs/adr/SADR-0003-require-skill-gate-for-mergegate-readiness.md\n@@ -1,3 +1,3 @@\n-# Require Skill Gate For MergeGate Readiness\n+# SADR-0003: Require Skill Gate For MergeGate Readiness\n \n Shiki requires relevant engineering skills to be invoked before execution when their triggers apply. This makes skills part of MergeGate readiness rather than optional prompt style, because Shiki depends on repeatable planning, issue decomposition, TDD, diagnosis, triage, and architecture review before autonomous or parallel agent work can be trusted.\ndiff --git a/docs/adr/0004-use-github-first-with-shiki-mirror.md b/docs/adr/SADR-0004-use-github-first-with-shiki-mirror.md\nindex d0a3a9174dc646a945a9d325954ffaf00c352040..a79c54ae9ee8eed2a0524192b864da4e22a92d3f\n--- a/docs/adr/0004-use-github-first-with-shiki-mirror.md\n+++ b/docs/adr/SADR-0004-use-github-first-with-shiki-mirror.md\n@@ -1,4 +1,4 @@\n-# ADR 0004: Use GitHub First With A Repository-Local Shiki Mirror\n+# SADR-0004: Use GitHub First With A Repository-Local Shiki Mirror\n \n ## Status\n \ndiff --git a/docs/adr/0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md b/docs/adr/SADR-0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\nindex b339baf49b89d9290e79c3d51b36e7b2bde3bd95..0f3c3eb9afccdb3403ce978e21d04fe12e495da0\n--- a/docs/adr/0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\n+++ b/docs/adr/SADR-0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\n@@ -1,8 +1,8 @@\n-# ADR 0005: Use Codex As Front And Claude Code Action As The GitHub Runtime\n+# SADR-0005: Use Codex As Front And Claude Code Action As The GitHub Runtime\n \n ## Status\n \n-Accepted (implementer default superseded by ADR 0008; the auth model and the\n+Accepted (implementer default superseded by SADR-0008; the auth model and the\n Claude Code Action GitHub runtime decision remain in force)\n \n ## Context\n@@ -26,4 +26,4 @@\n - Shiki avoids requiring `OPENAI_API_KEY` for the default workflow.\n - Codex work happens through the user's authenticated Codex front surface, not as a hidden backend Action.\n - Claude Code Action is configured for OAuth-token based subscription usage by default.\n-- API-key based automation remains possible only as an explicit target-repo extension with its own ADR.\n+- API-key based automation remains possible only as an explicit target-repo extension with its own target ADR.\ndiff --git a/docs/adr/0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md b/docs/adr/SADR-0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\nindex 4bcae9d1e52f0a37b8e9fbafa5cd14b6ee1993bd..7a38538a6ae9929567f0ade67e7ea6a60d0b3f7e\n--- a/docs/adr/0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\n+++ b/docs/adr/SADR-0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\n@@ -1,4 +1,4 @@\n-# ADR 0006: Separate Codex Implementation, CCA Completion, And MergeGate Authorization\n+# SADR-0006: Separate Codex Implementation, CCA Completion, And MergeGate Authorization\n \n ## Status\n \ndiff --git a/docs/adr/0007-packaging-and-release.md b/docs/adr/SADR-0007-packaging-and-release.md\nindex 79b84ddb97167372b44641e5b7860e400ce80822..bb445a2e2e856d9d1f5fdbe35de8d4e8cec0fb32\n--- a/docs/adr/0007-packaging-and-release.md\n+++ b/docs/adr/SADR-0007-packaging-and-release.md\n@@ -1,4 +1,4 @@\n-# ADR 0007: Packaging And Release For Shiki\n+# SADR-0007: Packaging And Release For Shiki\n \n ## Status\n \n@@ -25,7 +25,7 @@\n   not introduce third-party runtime dependencies or break the shim.\n - Releases must run inside the operator's subscription-authenticated toolchain\n   and must not require extra secrets beyond the built-in `GITHUB_TOKEN`\n-  (consistent with ADR 0005's no-API-key-by-default stance).\n+  (consistent with SADR-0005's no-API-key-by-default stance).\n - `.shiki/manifest.json` already encodes the install surface (`install.include`,\n   `install.create_directories`, `install.exclude_from_commit`), and it must not\n   be mutated as part of this change.\n@@ -34,7 +34,7 @@\n \n This is a hard-to-reverse platform decision (it sets the version source of\n truth, the release trigger, and the public install contract), so it is recorded\n-as an ADR.\n+as a SADR.\n \n ## Decision\n \n@@ -104,7 +104,7 @@\n 3. **Publishing to PyPI.** Rejected for now: Shiki is distributed as a\n    repository template + git-installable CLI, not a PyPI library; PyPI would add\n    credentials/secrets and a different versioning contract. Can be added later as\n-   its own ADR if a packaged distribution is needed.\n+   its own SADR if a packaged distribution is needed.\n 4. **Dynamic version (read `VERSION` via a build backend `dynamic` field).**\n    Rejected to keep `pyproject.toml` backend-agnostic and dependency-free; the\n    small duplication between `VERSION` and `pyproject.toml` is guarded by the\n@@ -123,4 +123,4 @@\n   releasing versions the whole platform but ships only `install.include` to\n   targets.\n - Future PyPI distribution or a console entry point are deferred and would each\n-  warrant their own ADR.\n+  warrant their own SADR.\ndiff --git a/docs/adr/0008-use-claude-code-as-default-implementer.md b/docs/adr/SADR-0008-use-claude-code-as-default-implementer.md\nindex dedf4510b7c7799521422f622029563d373db031..35c2625d908167fbc58855b5b63e283805f167e8\n--- a/docs/adr/0008-use-claude-code-as-default-implementer.md\n+++ b/docs/adr/SADR-0008-use-claude-code-as-default-implementer.md\n@@ -1,12 +1,12 @@\n-# ADR 0008: Use Claude Code As The Default Implementer Runtime\n+# SADR-0008: Use Claude Code As The Default Implementer Runtime\n \n ## Status\n \n-Proposed (supersedes ADR 0005 for the implementer default; keeps its auth model)\n+Proposed (supersedes SADR-0005 for the implementer default; keeps its auth model)\n \n ## Context\n \n-ADR 0005 made Codex Front the default implementation runtime, with Claude Code\n+SADR-0005 made Codex Front the default implementation runtime, with Claude Code\n restricted to planner and reviewer roles by the runtime registry, and the only\n autonomous implementation adapter was `shiki runner codex`.\n \n@@ -18,13 +18,13 @@\n - The platform's planning, review, and CCA layers already run on Claude Code,\n   and Claude Code's native capabilities (the built-in `/code-review` skill and\n   the Workflow multi-agent orchestration tool) have no Codex equivalent.\n-- The Goal lifecycle redesign (ADR 0009) requires an autonomous post-freeze\n+- The Goal lifecycle redesign (SADR-0009) requires an autonomous post-freeze\n   loop driven from the operator's Claude Code session, including bounded\n   repair dispatch, which must not depend on a runtime that cannot be invoked\n   unattended.\n \n The constitution claims Shiki is runtime-agnostic (\"not a Claude-only\n-workflow\", ADR 0002). Any change to the implementer default must preserve the\n+workflow\", SADR-0002). Any change to the implementer default must preserve the\n registry-based runtime model rather than hardcoding one vendor.\n \n ## Decision\n@@ -39,7 +39,7 @@\n   the task to review or repair-needed.\n - Keep Codex registered as an optional implementer runtime. `shiki runner\n   codex`, the Codex handoff templates, and the ChatGPT OAuth auth model from\n-  ADR 0005 remain supported for tasks explicitly assigned to `codex`.\n+  SADR-0005 remain supported for tasks explicitly assigned to `codex`.\n - Commit to mandating two Claude-native capabilities as loop contract in\n   follow-up Goals (status: the code-review gate was delivered by Goal G-B \u2014\n   skill registry, checklists item PR-12, default required_skills \u2014 while the\n@@ -60,7 +60,7 @@\n \n Out of scope: removing Codex support, changing the CCA runtime (Claude Code\n Action with `CLAUDE_CODE_OAUTH_TOKEN` stays), and API-key based automation\n-(still requires its own ADR per ADR 0005).\n+(still requires its own target ADR per SADR-0005).\n \n ## Consequences\n \n@@ -100,7 +100,7 @@\n   harness rejection of direct `codex exec` is outside Shiki's control, and the\n   plan's mandatory native capabilities cannot run on Codex at all.\n - **Remove Codex entirely**: rejected; it abandons the runtime-agnostic\n-  constitution (ADR 0002) for no operational gain and forecloses\n+  constitution (SADR-0002) for no operational gain and forecloses\n   cross-runtime adversarial review.\n - **GitHub Actions cloud implementation via claude-code-action**: rejected as\n   default; durable evidence is automatic but token cost, Node 24 workflow\ndiff --git a/docs/adr/0009-requirements-definition-and-spec-freeze.md b/docs/adr/SADR-0009-requirements-definition-and-spec-freeze.md\nindex 29b3f7871bef9203a8b4a831a65c39eb2f506e42..52e7dd69ad0aa4efe0b68c62678abbf7df02aaec\n--- a/docs/adr/0009-requirements-definition-and-spec-freeze.md\n+++ b/docs/adr/SADR-0009-requirements-definition-and-spec-freeze.md\n@@ -1,4 +1,4 @@\n-# ADR 0009: Merge Grill And Planning Into Requirements Definition With Spec Freeze\n+# SADR-0009: Merge Grill And Planning Into Requirements Definition With Spec Freeze\n \n ## Status\n \ndiff --git a/docs/adr/0010-external-ai-guardian-review.md b/docs/adr/SADR-0010-external-ai-guardian-review.md\nindex e6108bbe8bb4e73e0ff4684c8f78001a80438c4e..f3510b145664298adafb151b6f0db51b972f01ec\n--- a/docs/adr/0010-external-ai-guardian-review.md\n+++ b/docs/adr/SADR-0010-external-ai-guardian-review.md\n@@ -1,4 +1,4 @@\n-# ADR 0010: External AI Guardian Review As A First-Class Approval Authority\n+# SADR-0010: External AI Guardian Review As A First-Class Approval Authority\n \n ## Status\n \n@@ -8,7 +8,7 @@\n \n Shiki is intended to be an autonomous, recursive, self-improving agent\n framework: authority-in-the-loop, not human-in-the-loop. Until now the Guardian\n-approval required for high/critical-risk merges (guardian-policy.json, ADR\n+approval required for high/critical-risk merges (guardian-policy.json, SADR\n governance) recognized only human authority sources \u2014 a GitHub review approval,\n the `guardian:approved` label applied by a configured Guardian, or a comment\n carrying `Guardian approval granted` plus the current head SHA.\n@@ -89,7 +89,7 @@\n   autonomous design the operator intends and produced the impersonation\n   incident.\n - **Record AI approval as an operator comment**: rejected as forgery of human\n-  authority \u2014 the exact failure this ADR corrects.\n+  authority \u2014 the exact failure this SADR corrects.\n - **Deliver the artifact as a committed `.shiki/` file**: rejected; a committed\n   approval inside the PR being approved is forgeable by anyone who can push.\n - **A dedicated reviewer bot identity instead of a relayed comment**: deferred;\ndiff --git a/docs/adr/0011-autonomous-quality-gate-evidence.md b/docs/adr/SADR-0011-autonomous-quality-gate-evidence.md\nindex bca0246c5d46e44afbde15486a75eec425fc02f5..9c93eac16d8c3a6d4b11bd7224e3955102cd7db4\n--- a/docs/adr/0011-autonomous-quality-gate-evidence.md\n+++ b/docs/adr/SADR-0011-autonomous-quality-gate-evidence.md\n@@ -1,4 +1,4 @@\n-# ADR 0011: Autonomous Quality-Gate Evidence \u2014 Deterministic Facts And An Independent Verifier\n+# SADR-0011: Autonomous Quality-Gate Evidence \u2014 Deterministic Facts And An Independent Verifier\n \n ## Status\n \ndiff --git a/docs/adr/0012-autonomous-loop-closeout-to-main.md b/docs/adr/SADR-0012-autonomous-loop-closeout-to-main.md\nindex 637fad6dde4989c9c2bf0f81f676d8d29991123c..ec7d85f81030a676bcbba28b43eb365422a36eac\n--- a/docs/adr/0012-autonomous-loop-closeout-to-main.md\n+++ b/docs/adr/SADR-0012-autonomous-loop-closeout-to-main.md\n@@ -1,4 +1,4 @@\n-# 0012 \u2014 Autonomous loop pushes goal completion to main via a closeout PR\n+# SADR-0012 \u2014 Autonomous loop pushes goal completion to main via a closeout PR\n \n - Status: accepted\n - Date: 2026-06-18\ndiff --git a/docs/adr/0013-bootstrap-grants-actions-approve-pull-requests.md b/docs/adr/SADR-0013-bootstrap-grants-actions-approve-pull-requests.md\nindex b6c69d64f1c5ced346b88d6859586715b3b5bd0e..cf48ff4a6716267f539ca58edd1a852e7c7a2d64\n--- a/docs/adr/0013-bootstrap-grants-actions-approve-pull-requests.md\n+++ b/docs/adr/SADR-0013-bootstrap-grants-actions-approve-pull-requests.md\n@@ -1,4 +1,4 @@\n-# ADR 0013: Bootstrap Grants GitHub Actions Permission To Create And Approve Pull Requests\n+# SADR-0013: Bootstrap Grants GitHub Actions Permission To Create And Approve Pull Requests\n \n ## Status\n \n@@ -31,7 +31,7 @@\n security tradeoff: it lets repository automation satisfy a human-review gate. In\n a multi-maintainer repo that could let a compromised or buggy workflow\n self-approve changes. So whether bootstrap turns it on by default \u2014 and with\n-what default token scope \u2014 is an ADR-worthy decision, not an implementation\n+what default token scope \u2014 is a SADR-worthy decision, not an implementation\n detail.\n \n ## Decision\n@@ -48,7 +48,7 @@\n Specifically:\n \n - **`can_approve_pull_request_reviews` defaults to `true`.** Shiki's default\n-  posture is solo/AFK autonomous operation (ADR 0008/0009/0012), where the CCA\n+  posture is solo/AFK autonomous operation (SADR-0008/0009/0012), where the CCA\n   Review Bridge is the only path that can satisfy `required_review: true` after\n   the CCA and Guardian gates have already passed. The default makes a\n   freshly-bootstrapped repo able to run the autonomous loop end to end without a\ndiff --git a/docs/adr/0014-codex-app-external-ai-guardian-ui-adapter.md b/docs/adr/SADR-0014-codex-app-external-ai-guardian-ui-adapter.md\nindex 238db06279334caa341c117362df9271382a7fcd..65f12e9939321e1b3e389a34c822fc1549e5155d\n--- a/docs/adr/0014-codex-app-external-ai-guardian-ui-adapter.md\n+++ b/docs/adr/SADR-0014-codex-app-external-ai-guardian-ui-adapter.md\n@@ -1,4 +1,4 @@\n-# ADR 0014: Use Codex App As The External AI Guardian UI Adapter\n+# SADR-0014: Use Codex App As The External AI Guardian UI Adapter\n \n ## Status\n \ndiff --git a/docs/adr/0015-contract-approval-before-dispatch.md b/docs/adr/SADR-0015-contract-approval-before-dispatch.md\nindex fad6c656741a20a7684ba16080a7db159f63a52f..6061271a46d3f1d6ae7174477de3fb75673123a6\n--- a/docs/adr/0015-contract-approval-before-dispatch.md\n+++ b/docs/adr/SADR-0015-contract-approval-before-dispatch.md\n@@ -1,4 +1,4 @@\n-# ADR 0015: Approve The Task Contract Before Dispatch, Not The Diff After It\n+# SADR-0015: Approve The Task Contract Before Dispatch, Not The Diff After It\n \n ## Status\n \n@@ -9,7 +9,7 @@\n Guardian approval for high/critical risk is expressible only on a pull request.\n Every approval source in `guardian-policy.json` \u2014 GitHub review, the\n `guardian:approved` label, the `Guardian approval granted` comment, and the\n-`external_ai_guardian_review` artifact (ADR 0010/0014) \u2014 is bound to a PR, and\n+`external_ai_guardian_review` artifact (SADR-0010/0014) \u2014 is bound to a PR, and\n three of the four require the current head SHA. Approval therefore cannot exist\n before an implementation PR exists.\n \n@@ -81,7 +81,7 @@\n An envelope breach must surface only on the non-repairable policy gate, never on\n the repairable `MergeGate metadata check`. Otherwise the Goal Loop routes it to\n `dispatch_repair` \u2014 instructing a runner to make the breach go away \u2014 which is\n-exactly what this ADR forbids.\n+exactly what this SADR forbids.\n \n **Contract immutability.** After registration, an implementation PR must not\n change the governance fields of its own task contract (`scope`, `non_goals`,\n@@ -116,7 +116,7 @@\n fails any verdict that is not `complete`, and the signal the CCA prompt reads is\n computed from `evaluate_guardian_approval` alone. Without the OR-merge at the\n signal, a contract-approved PR still returns `needs_guardian`, the CCA job fails,\n-and the MergeGate policy check never runs \u2014 the gate this ADR changes would never\n+and the MergeGate policy check never runs \u2014 the gate this SADR changes would never\n be reached. The CCA *prompt* needs no change: a `contract_approval` entry in the\n signal's existing `sources` array satisfies its current wording.\n \n@@ -130,7 +130,7 @@\n written, and an approved contract lets its implementation PRs merge through the\n same automatic path as low/medium risk. The wasted implement-then-block cycle\n disappears. Guardian review shifts from reading diffs to reading contracts, which\n-is both earlier and smaller; the external AI Guardian path (ADR 0010/0014) works\n+is both earlier and smaller; the external AI Guardian path (SADR-0010/0014) works\n on contracts with no change.\n \n We accept that the Guardian authorizes a class of change without seeing its\n@@ -146,7 +146,7 @@\n adversarial tests for the new approval path equivalent in rigor to\n `test_shiki_governance_evidence.sh`.\n \n-Bootstrap costs, which apply only while this ADR is being implemented. The tasks\n+Bootstrap costs, which apply only while this SADR is being implemented. The tasks\n that build Contract Approval merge under the existing rules, so the ones\n dispatched after the pre-dispatch gate lands but before the approval path lands\n need a Guardian-approved Contract PR *and* a live Guardian approval on their\n@@ -210,7 +210,7 @@\n risk; they do not bound authorized work.\n \n **Auto-escalate risk on envelope breach instead of stopping.** Returns the\n-workflow to a diff-time Guardian decision \u2014 the exact behavior this ADR removes.\n+workflow to a diff-time Guardian decision \u2014 the exact behavior this SADR removes.\n \n **Resolve governance fields from the base snapshot only.** Rejected against the\n registration flow: implementation PRs add their own contract, so the base has no\ndiff --git a/docs/adr/0016-locks-bound-product-paths.md b/docs/adr/SADR-0016-locks-bound-product-paths.md\nindex b5e850f3f1f7f4ef6e7f9d1763a29a51bd300c45..46b40a9b99a7aae644c02bb458f5473ff0a90ce2\n--- a/docs/adr/0016-locks-bound-product-paths.md\n+++ b/docs/adr/SADR-0016-locks-bound-product-paths.md\n@@ -1,4 +1,4 @@\n-# ADR 0016: Locks bound product paths; the Shiki mirror is bound by record identity\n+# SADR-0016: Locks bound product paths; the Shiki mirror is bound by record identity\n \n ## Status\n \n@@ -6,8 +6,8 @@\n \n - Date: 2026-07-28\n - Deciders: operator (mizutani-140), Claude (planner)\n-- Related: ADR 0004 (GitHub-first with a `.shiki` mirror), ADR 0009 (Spec Freeze),\n-  ADR 0012 (autonomous loop closeout to main), ADR 0015 (Contract Approval before\n+- Related: SADR-0004 (GitHub-first with a `.shiki` mirror), SADR-0009 (Spec Freeze),\n+  SADR-0012 (autonomous loop closeout to main), SADR-0015 (Contract Approval before\n   dispatch), remediation goal task R-02, the live PR #179 deadlock.\n \n ## Context\n@@ -90,11 +90,11 @@\n files, documentation files and named source modules are nameable; runtime-\n allocated ledger, runner and report ids are not \u2014 and those live in `.shiki`,\n which locks no longer govern. Existing frozen contracts are brought into line by\n-a one-time Spec Amendment per plan, which must land before ADR 0015's contract\n+a one-time Spec Amendment per plan, which must land before SADR-0015's contract\n immutability makes `locks` unwritable from the implementation PR.\n \n **Lock state is resolved from the durable side.** MergeGate resolves the lock\n-registry, and ADR 0015's governance fields, from the base `.shiki` snapshot when\n+registry, and SADR-0015's governance fields, from the base `.shiki` snapshot when\n present, falling back to head only when absent. The `MergeGate metadata check`\n job is given `--base-shiki`, which it does not receive today. Locks are not\n derived from the live open-pull-request set: a pull request body is untrusted\n@@ -108,7 +108,7 @@\n Explicitly out of scope: moving MergeGate's contract resolution off the PR\n checkout wholesale; making the mirror a projection of GitHub state; introducing\n new lock kinds; moving ledger evidence out of the repository; and unwinding\n-ADR 0012's closeout pull request. Registration of multi-task goals is ADR 0015's\n+SADR-0012's closeout pull request. Registration of multi-task goals is SADR-0015's\n Contract PR and is not re-decided here.\n \n ## Consequences\n@@ -127,7 +127,7 @@\n \n Risks accepted. First, goal-scoped identity is only as strong as `goal_id`,\n which is resolved from the PR body and the head task file; the mitigation is to\n-add `goal_id` to ADR 0015's frozen governance field set, and until that lands a\n+add `goal_id` to SADR-0015's frozen governance field set, and until that lands a\n task file can name a foreign goal. Second, base-when-present resolution is\n unavailable on fork pull requests, where the head is contributor-controlled;\n fork pull requests already cannot complete a Shiki merge, and this decision does\n@@ -139,7 +139,7 @@\n path, not by implementation pull requests.\n \n New obligations: one Spec Amendment per frozen plan to rewrite `locks`, ordered\n-before ADR 0015 T1; extension of the adversarial suites in\n+before SADR-0015 T1; extension of the adversarial suites in\n `scripts/test_shiki_adversarial_state.sh` and\n `scripts/test_shiki_governance_evidence.sh` to cover the eleven newly-scoped\n directories; and updating `scripts/test_shiki_loop_lock_guard.sh`,\n@@ -151,32 +151,32 @@\n whether the loop's transitive sync of ledger `evidence` references can be\n deleted once the Contract PR carries its own source plan.\n \n-## Relationship to ADR 0004\n+## Relationship to SADR-0004\n \n-This ADR **clarifies something ADR 0004 left unspecified**. ADR 0004 decides that\n+This SADR **clarifies something SADR-0004 left unspecified**. SADR-0004 decides that\n GitHub is the operational source of truth and that each target repository keeps a\n `.shiki/` mirror. It says nothing about pull requests writing the mirror, does\n not name MergeGate, and does not make the mirror an input to a gate. Nothing in\n-ADR 0004's Decision or Consequences is amended, superseded or weakened: the\n+SADR-0004's Decision or Consequences is amended, superseded or weakened: the\n mirror still exists, still records the same artifacts, and pull request\n-boundaries remain the coordination surface. What this ADR settles is the question\n+boundaries remain the coordination surface. What this SADR settles is the question\n 0004 never reached \u2014 by what authority a pull request may write a mirror record \u2014\n and it answers it the way `.shiki/manifest.json` already declares\n (`pr_mutation: current-task-or-current-goal-only`).\n \n-It **amends ADR 0012**, whose Decision item 5 states that \"Loop-executed tasks\n+It **amends SADR-0012**, whose Decision item 5 states that \"Loop-executed tasks\n declare the `path:.shiki/**` lock, which covers every `.shiki` file the closeout\n stages ... so `files_outside_locks` is satisfied without per-file lock\n bookkeeping.\" That clause is replaced: the closeout's mirror writes are\n-authorized by record identity, not by a declared lock. ADR 0012's substantive\n+authorized by record identity, not by a declared lock. SADR-0012's substantive\n decision \u2014 that completion reaches `main` through a closeout pull request rather\n than local attestation \u2014 is preserved unchanged.\n \n-It is **complementary to ADR 0015**, not a substitute. ADR 0015 owns contract\n+It is **complementary to SADR-0015**, not a substitute. SADR-0015 owns contract\n immutability, the pre-dispatch gate, the Contract PR and the Scope Envelope. This\n-ADR owns what a lock means and who may write the mirror. The one ordering\n+SADR owns what a lock means and who may write the mirror. The one ordering\n constraint between them is that the lock-narrowing Spec Amendment must precede\n-ADR 0015 T1's freezing of the `locks` field.\n+SADR-0015 T1's freezing of the `locks` field.\n \n ## Alternatives Considered\n \n@@ -186,7 +186,7 @@\n the only writer that can register them may run. Migrating the Guardian signal to\n that store makes every registration PR fail closed to `needs_guardian` at every\n risk level, because the task file is by definition absent from the store, which\n-removes the low/medium autonomous path ADR 0008/0009 exists to provide. And it\n+removes the low/medium autonomous path SADR-0008/0009 exists to provide. And it\n leaves schemas, manifest, config, guardian policy, templates, migrations, plans,\n worktrees, runner records, runs and handoffs with no writer at all in a\n repository whose product is `.shiki`. Measured benefit on the frozen backlog:\n@@ -199,14 +199,14 @@\n namespace is not in fact ownership-partitioned \u2014 56 ledger entries carry no\n `task_id`, and one goal DAG is writable by 35 registered tasks. The correct\n expression of its insight is that locks do not govern the mirror at all, which is\n-what this ADR decides.\n+what this SADR decides.\n \n **Make the mirror a projection written only by a registration writer and a\n post-merge projection job.** Rejected. It requires a bypass actor with write\n access to the protected default branch; it makes its own master invariant\n readable from a manifest that pull requests may edit; it deletes both reconcile\n modes and leaves a corrupted mirror with no in-band repair path, contradicting\n-ADR 0004's consequence that mirror conflicts are repaired against GitHub state;\n+SADR-0004's consequence that mirror conflicts are repaired against GitHub state;\n and its content-derived ids are rejected by `validate_shiki`'s currently-green id\n pattern.\n \ndiff --git a/docs/adr/0017-bookkeeping-closeouts-do-not-inherit-risk.md b/docs/adr/SADR-0017-bookkeeping-closeouts-do-not-inherit-risk.md\nindex 554e3a0d526a72ca067108c5464cc68c3edcf174..b9676857a4713850e063af7c39c8e0b01ecb4c66\n--- a/docs/adr/0017-bookkeeping-closeouts-do-not-inherit-risk.md\n+++ b/docs/adr/SADR-0017-bookkeeping-closeouts-do-not-inherit-risk.md\n@@ -1,4 +1,4 @@\n-# ADR 0017: A Bookkeeping Closeout Does Not Inherit Its Task's Risk\n+# SADR-0017: A Bookkeeping Closeout Does Not Inherit Its Task's Risk\n \n ## Status\n \n@@ -6,7 +6,7 @@\n \n ## Context\n \n-ADR 0012 made the goal loop push a task's completion to the default branch through a\n+SADR-0012 made the goal loop push a task's completion to the default branch through a\n closeout pull request: after the implementation PR merges, the loop opens a second PR\n carrying `task.status = done`, `lock.state = released`, and \u2014 when the task completes\n its goal \u2014 `goal.status = complete` plus one scorecard report. Nothing else. The\n@@ -49,14 +49,14 @@\n 4. The task file's governance fields (`scope`, `non_goals`, `required_skills`,\n    `risk_level`, `locks`, `acceptance_checks`, `test_command`) are byte-identical to\n    the base snapshot. Only `status`, `expected_pr`, `closeout_pr`, `ledger_evidence`\n-   and `expected_branch` may differ \u2014 the ADR 0015 contract-immutability rule,\n+   and `expected_branch` may differ \u2014 the SADR-0015 contract-immutability rule,\n    unchanged.\n \n    `expected_branch` is in that set because a closeout is *by construction* on a\n    different branch from the implementation it closes, and the loop rewrites the\n    field accordingly. It is safe to let it differ because MergeGate independently\n    requires `expected_branch` to equal the PR's actual head ref, so a PR cannot set\n-   it to anything other than the branch it is really on. The first draft of this ADR\n+   it to anything other than the branch it is really on. The first draft of this SADR\n    omitted it, which made the exemption unreachable: every real closeout failed\n    condition 4 on a field the loop always changes. See PR #209.\n 5. The state transitions are exactly the terminal set: task `review` \u2192 `done`, lock\n@@ -113,12 +113,12 @@\n classification must be derived, not asserted.\n \n **Fold the closeout into the implementation PR** so no second PR exists. Rejected under\n-ADR 0012, which recorded why: a task file that is already `done` on the implementation\n+SADR-0012, which recorded why: a task file that is already `done` on the implementation\n branch short-circuits the loop's review \u2192 CCA \u2192 merge \u2192 done ordering into a local\n goal-completion before any merge happens.\n \n **Have the loop auto-merge closeouts with elevated credentials.** Rejected outright: that\n-is a runtime authoring its own approval, which ADR 0010 and the constitution's\n+is a runtime authoring its own approval, which SADR-0010 and the constitution's\n approval-record prohibition forbid. It would also be indistinguishable, in the evidence,\n from the forgery those rules exist to prevent.\n \ndiff --git a/docs/adr/0018-carried-guardian-approval-across-a-proven-base-sync.md b/docs/adr/SADR-0018-carried-guardian-approval-across-a-proven-base-sync.md\nindex 22358562da390ed8b45cd600da08a43d378b4680..e6e2907cefa776460af73ddf7d39db605f3c39b0\n--- a/docs/adr/0018-carried-guardian-approval-across-a-proven-base-sync.md\n+++ b/docs/adr/SADR-0018-carried-guardian-approval-across-a-proven-base-sync.md\n@@ -1,4 +1,4 @@\n-# ADR 0018: Carried Guardian Approval Across A Proven Base Sync\n+# SADR-0018: Carried Guardian Approval Across A Proven Base Sync\n \n ## Status\n \n@@ -34,7 +34,7 @@\n Guardian evaluator that consumes it (`shiki_guardian.evaluate_guardian_approval`,\n the `guardian_comment_carried` source) were built and tested in isolation. Neither\n does anything until a caller passes the flags that turn the carry on, so the\n-feature was invisible in CI. This ADR records the decision the two of them\n+feature was invisible in CI. This SADR records the decision the two of them\n implement, and this task wires it into the four CI invocations that judge Guardian\n approval.\n \ndiff --git a/docs/adr/README.md b/docs/adr/README.md\nindex c70e057a4520c36e042ab68c53cd4a21ef709b71..5da43856836e2ce6a150f8127766e2f88355ab4b\n--- a/docs/adr/README.md\n+++ b/docs/adr/README.md\n@@ -1,40 +1,41 @@\n-# Architecture Decision Records\n+# Decision Record Namespaces\n \n-This directory holds Shiki's Architecture Decision Records (ADRs): durable\n-records of hard-to-reverse platform decisions. ADRs are part of the Shiki source\n-of truth alongside GitHub state, the `.shiki/` mirror, and `CONTEXT.md`.\n+This directory holds two independent decision namespaces:\n \n-## How ADRs Work\n+- Shiki Architecture Decision Records (SADRs), named\n+  `SADR-NNNN-<short-slug>.md`, record hard-to-reverse Shiki platform decisions.\n+- Target Architecture Decision Records (ADRs), named\n+  `NNNN-<short-slug>.md`, belong to the target product.\n \n-- New ADRs are added as `docs/adr/NNNN-<short-slug>.md`, where `NNNN` is the next\n-  zero-padded sequence number.\n-- The **canonical list of ADRs is the set of files matching `docs/adr/*.md`** in\n-  this directory. Browse `docs/adr/*.md` for the authoritative, current set.\n-- Use [`template.md`](template.md) as the starting point for a new ADR.\n-- Record an ADR when a decision is hard to reverse, surprising without context,\n-  and a real tradeoff. Do not write an ADR for routine or easily reversible\n-  choices.\n+The same four-digit number may appear once in each namespace. A platform\n+`SADR-NNNN` citation resolves only to `SADR-NNNN-*.md`; a target `ADR NNNN`\n+citation resolves only to `NNNN-*.md`.\n \n-Using the `docs/adr/*.md` glob as the source of truth means other PRs can add an\n-ADR without editing this index file, avoiding merge contention on a\n-hand-maintained enumeration.\n+## How Decision Records Work\n \n-## Current ADRs (snapshot)\n-\n-The list below is a convenience snapshot and may lag behind the directory. When\n-in doubt, list `docs/adr/*.md`.\n+- New Shiki platform decisions use the next zero-padded SADR number.\n+- New target product decisions use the target's own ADR sequence.\n+- The canonical list is the files in `docs/adr/`; no hand-maintained index is\n+  authoritative.\n+- Use [`template.md`](template.md) as the starting point and choose the filename\n+  namespace that owns the decision.\n+- Record a decision when it is hard to reverse, surprising without context, and\n+  a real tradeoff. Do not write a decision record for routine or easily\n+  reversible choices.\n \n-- [0001 \u2014 Keep Shiki As The Platform Name](0001-keep-shiki-as-platform-name.md)\n-- [0002 \u2014 Use AGENTS As The Shared Agent Constitution](0002-use-agents-as-shared-constitution.md)\n-- [0003 \u2014 Require Skill Gate For MergeGate Readiness](0003-require-skill-gate-for-mergegate-readiness.md)\n-- [0004 \u2014 Use GitHub First With A Repository-Local Shiki Mirror](0004-use-github-first-with-shiki-mirror.md)\n-- [0005 \u2014 Use Codex As Front And Claude Code Action As The GitHub Runtime](0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md)\n-- [0006 \u2014 Separate Codex Implementation, CCA Completion, And MergeGate Authorization](0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md)\n+Using the directory as the source of truth lets independent changes add records\n+without contending on an enumerated index.\n \n-## Adding A New ADR\n+## Adding A Shiki SADR\n \n-1. Copy `template.md` to `docs/adr/NNNN-<short-slug>.md`.\n+1. Copy `template.md` to `docs/adr/SADR-NNNN-<short-slug>.md`.\n 2. Fill in the title, status, context, decision, and consequences.\n-3. Set the status to `Proposed`, then `Accepted` once the decision is approved.\n-4. Open a PR following `CONTRIBUTING.md`. You do not need to edit this index for\n-   the ADR to be discoverable via `docs/adr/*.md`.\n+3. Set the status to `Proposed`, then `Accepted` once the proper authority has\n+   approved the decision.\n+4. Open a PR following `CONTRIBUTING.md`.\n+\n+## Adding A Target ADR\n+\n+1. Copy `template.md` to `docs/adr/NNNN-<short-slug>.md` using the target's own\n+   sequence.\n+2. Cite it as `ADR NNNN`; do not prefix its filename with `SADR-`.\ndiff --git a/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md b/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\nnew file mode 100644\nindex 0000000000000000000000000000000000000000..7455ce1f3bed02ddddb851ac21a1d1a648c9aeba\n--- /dev/null\n+++ b/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\n@@ -0,0 +1,55 @@\n+# SADR-0019: Separate Shiki Platform SADRs From Target ADRs\n+\n+## Status\n+\n+Accepted\n+\n+## Context\n+\n+Shiki installs its platform decision records into `docs/adr/` in every target\n+repository. Shiki and its targets previously used the same numeric\n+`NNNN-<slug>.md` namespace. A target that already owned ADRs 0013 through 0018\n+therefore acquired two records for each number during installation, making an\n+`ADR NNNN` citation ambiguous. Renumbering target decisions would rewrite target\n+history, while renumbering Shiki decisions per target would destroy their stable\n+platform identity.\n+\n+Upgrades also need to remove the former Shiki-owned numeric paths without ever\n+mistaking a target-owned ADR for platform content. The install stamp already\n+records exact shipped paths and content digests, so it can prove ownership only\n+when both still match.\n+\n+## Decision\n+\n+Shiki platform decisions use the `SADR` namespace:\n+\n+- records are named `docs/adr/SADR-NNNN-<slug>.md`;\n+- live platform citations use `SADR-NNNN`;\n+- target product decisions retain `docs/adr/NNNN-<slug>.md` and `ADR NNNN`;\n+- the reference validator resolves the two namespaces independently, so the same\n+  four-digit number may exist once in each namespace.\n+\n+Before a forced upgrade writes anything, the installer inventories the exact\n+former numeric Shiki ADR paths. It may delete a legacy path only when the\n+existing install stamp contains that exact path and its recorded SHA-256 digest\n+matches the file currently on disk. All ownership blockers are reported\n+together and abort the upgrade before its first write. A non-forced install\n+never performs this cleanup.\n+\n+Historical `.shiki/` evidence remains unchanged because it records the namespace\n+that existed when that evidence was produced.\n+\n+## Consequences\n+\n+- Targets may use any ADR number without colliding with Shiki's platform SADRs.\n+- Fresh installs preserve target ADR bytes and install a stable SADR set.\n+- Forced upgrades fail closed when legacy ownership cannot be proven, and a\n+  successful upgrade refreshes the stamp without removed legacy paths.\n+- Current governance, packaging, workflow, prompt, script, and skill references\n+  must move with the platform records.\n+\n+## Alternatives Considered\n+\n+- Collision-only refusal leaves affected targets permanently unupgradable.\n+- Renumbering target ADRs mutates target-owned history.\n+- Per-target Shiki renumbering makes platform citations non-portable.\ndiff --git a/docs/adr/template.md b/docs/adr/template.md\nindex a92a97daddd0b00ab9dd626cc8d86942da2eafed..17dd79175b5fee2105935557f691b40cf9492942\n--- a/docs/adr/template.md\n+++ b/docs/adr/template.md\n@@ -1,14 +1,14 @@\n-# NNNN: <Short Decision Title>\n+# <SADR-NNNN or NNNN>: <Short Decision Title>\n \n ## Status\n \n-Proposed | Accepted | Superseded by ADR-NNNN\n+Proposed | Accepted | Superseded by SADR-NNNN or target ADR NNNN\n \n ## Context\n \n What is the situation, constraint, or force that requires a decision? Reference\n-relevant domain terms (`CONTEXT.md`), prior ADRs, and code reality. State the\n-problem, not the solution.\n+relevant domain terms (`CONTEXT.md`), prior Shiki SADRs or target ADRs, and code\n+reality. State the problem, not the solution.\n \n ## Decision\n \ndiff --git a/docs/agents/autonomous-loop-e2e-acceptance.md b/docs/agents/autonomous-loop-e2e-acceptance.md\nindex bbf84c6081987f159ac6ad9ecf3c82b7aa72e7e8..5c13c1d02a05a90b0dc46cbef7c95a9846d0c237\n--- a/docs/agents/autonomous-loop-e2e-acceptance.md\n+++ b/docs/agents/autonomous-loop-e2e-acceptance.md\n@@ -40,8 +40,8 @@\n 3. `gh auth status` is logged in with merge permission on the repo.\n 4. Branch protection + required checks are configured exactly as the goal-loop\n    required-check set expects (see `docs/agents/branch-protection-smoke.md`).\n-5. The repo `.shiki/policy` permits autonomous low/medium-risk merges (ADR\n-   0008/0009); the MergeGate policy check is the recorded-authority gate.\n+5. The repo `.shiki/policy` permits autonomous low/medium-risk merges\n+   (SADR-0008/0009); the MergeGate policy check is the recorded-authority gate.\n \n The operator's only actions are: register the task and start the loop. **No\n manual pre-clearing** means after `shiki loop run` starts, the operator does not\ndiff --git a/docs/agents/bootstrap-command.md b/docs/agents/bootstrap-command.md\nindex 05f7f203a52cf412b9f97084a6e131ed0f785afd..f7aa0b734dfebee4a3b139335b66ee3415f48713\n--- a/docs/agents/bootstrap-command.md\n+++ b/docs/agents/bootstrap-command.md\n@@ -189,7 +189,9 @@\n ```\n \n Use this only for tests, fixtures, or explicit local-only template inspection.\n-Use `--force` only when you intentionally want to overwrite existing target files.\n+Use `--force` only when you intentionally want to refresh the target template.\n+Legacy numeric Shiki decision cleanup is stamp-and-digest authorized before any\n+write; target ADR paths are not cleanup candidates.\n \n ## Slash Command\n \ndiff --git a/docs/agents/checklists.md b/docs/agents/checklists.md\nindex 4a7f03ade346f53a1c1d82981f3fd744f9ab4547..0c5ec6c53dc7197b7b7a7d18e667b325babd6c45\n--- a/docs/agents/checklists.md\n+++ b/docs/agents/checklists.md\n@@ -32,18 +32,18 @@\n | ID | Item | Blocking |\n | --- | --- | --- |\n | GD-01 | Plan was challenged against `CONTEXT.md`. | blocking for non-trivial Goals |\n-| GD-02 | Relevant ADRs were read or absence was recorded. | blocking for non-trivial Goals |\n+| GD-02 | Relevant SADRs and target ADRs were read or absence was recorded. | blocking for non-trivial Goals |\n | GD-03 | Ambiguous terms were resolved or converted to HITL blockers. | blocking |\n | GD-04 | Edge scenarios were considered. | advisory |\n | GD-05 | Code reality was checked when code could answer. | blocking when applicable |\n | GD-06 | New domain terms were added to `CONTEXT.md` when needed. | blocking when applicable |\n-| GD-07 | ADR was created or explicitly skipped using the ADR criteria. | blocking when applicable |\n+| GD-07 | A SADR or target ADR was created or explicitly skipped using the decision-record criteria. | blocking when applicable |\n \n ## CI \u2014 Context & Impact Checklist\n \n | ID | Item | Blocking |\n | --- | --- | --- |\n-| CI-01 | Relevant docs and ADRs identified. | blocking |\n+| CI-01 | Relevant docs, SADRs, and target ADRs identified. | blocking |\n | CI-02 | Relevant modules, interfaces, seams, callers, and tests identified. | blocking |\n | CI-03 | Dependencies and blocked-by relationships identified. | blocking |\n | CI-04 | Candidate locks identified. | blocking |\n@@ -62,7 +62,7 @@\n | PRD-04 | Implementation decisions are recorded without volatile code detail. | blocking |\n | PRD-05 | Testing decisions are recorded. | blocking |\n | PRD-06 | Out-of-scope items are explicit. | blocking |\n-| PRD-07 | Relevant glossary/ADR links are present when applicable. | blocking |\n+| PRD-07 | Relevant glossary/SADR/target-ADR links are present when applicable. | blocking |\n \n ## SF \u2014 Spec Freeze Checklist\n \n@@ -98,7 +98,7 @@\n | H-02 | Branch/worktree target is present. | blocking |\n | H-03 | Scope and non-goals are present. | blocking |\n | H-04 | Dependencies and locks are present. | blocking |\n-| H-05 | Relevant docs, ADRs, modules, and tests are referenced. | blocking |\n+| H-05 | Relevant docs, SADRs, target ADRs, modules, and tests are referenced. | blocking |\n | H-06 | Required skills are stated. | blocking |\n | H-07 | TDD expectations are stated. | blocking when behavior work |\n | H-08 | Acceptance criteria are included. | blocking |\ndiff --git a/docs/agents/codex-handoff.md b/docs/agents/codex-handoff.md\nindex ede468f47cb277f69a857e8c45df703e4ed0151d..6f97bdffb67e44d7edea2c71e01ea28933c4eb6c\n--- a/docs/agents/codex-handoff.md\n+++ b/docs/agents/codex-handoff.md\n@@ -46,7 +46,7 @@\n \n ## Context\n - Domain terms:\n-- Relevant ADRs:\n+- Relevant SADRs and target ADRs:\n - Relevant docs:\n - Relevant modules/interfaces/seams:\n - Similar tests/prior art:\ndiff --git a/docs/agents/completion-check-agent.md b/docs/agents/completion-check-agent.md\nindex 13e4c035db35025f4e3cdcc5adfb24314ac4d521..61bfa1218cf36ca605250c91919e620f33238aa0\n--- a/docs/agents/completion-check-agent.md\n+++ b/docs/agents/completion-check-agent.md\n@@ -32,7 +32,7 @@\n - `AGENTS.md`\n - `CLAUDE.md`\n - `CONTEXT.md`\n-- relevant ADRs\n+- relevant SADRs and target ADRs\n - parent Goal issue\n - PRD issue or document\n - task issue\ndiff --git a/docs/agents/decision-control.md b/docs/agents/decision-control.md\nindex 7d8ee5ab03a5ccbcbd04056ae61de03785a2a9c9..8c94ecda00fffaa0e92c325ab83aa9776bd1dac1\n--- a/docs/agents/decision-control.md\n+++ b/docs/agents/decision-control.md\n@@ -11,7 +11,7 @@\n | Decision | Owner | When | Required inputs | Output | Enforcement surface |\n | --- | --- | --- | --- | --- | --- |\n | Is the Goal clear enough to plan? | Planner + Guardian when needed | Goal intake | User request, existing docs, risk signals | Goal readiness verdict | Goal checklist, issue template, ledger |\n-| Must `grill-with-docs` run? | Planner / Skill Gate | Before PRD or issue split | Ambiguity, domain terms, ADR conflicts, unclear boundaries | Skill invocation or explicit skip rationale | Skill Gate checklist |\n+| Must `grill-with-docs` run? | Planner / Skill Gate | Before PRD or issue split | Ambiguity, domain terms, SADR/target-ADR conflicts, unclear boundaries | Skill invocation or explicit skip rationale | Skill Gate checklist |\n | Is a PRD required? | Planner | After Goal context stabilizes | Goal, `grill-with-docs` output, Context & Impact output | PRD required / not required | PRD checklist, CCA evidence check |\n | Can the spec be frozen? | Operator only | End of Requirements Definition, before issue split | PRD/requirements summary, scope inventory (SF-02), open-question list | `spec_freeze` block in the plan + ledger evidence | `require_grilled_plan` (plan ingest/run), SF checklist |\n | Can a frozen spec change? | Operator only (Spec Amendment) | When implementation contradicts the frozen spec | Contested decisions, scoped re-grill result | recorded amendment + re-stamped freeze | SF checklist, ledger evidence |\ndiff --git a/docs/agents/domain.md b/docs/agents/domain.md\nindex 258da8ea81e87ac0a6b0b5b5e5c2fde980eaabb7..d57b0cf17ff3e1fa9e6e5885372bf083d8295421\n--- a/docs/agents/domain.md\n+++ b/docs/agents/domain.md\n@@ -5,7 +5,9 @@\n ## Layout\n \n - `CONTEXT.md` defines Shiki language. Keep it implementation-free.\n-- `docs/adr/` records hard-to-reverse decisions.\n+- `docs/adr/SADR-NNNN-*.md` records Shiki platform decisions.\n+- `docs/adr/NNNN-*.md` records target product decisions without sharing the\n+  SADR namespace.\n - `AGENTS.md` is the runtime-neutral agent constitution.\n - `CLAUDE.md` is the Claude Code wrapper around `AGENTS.md`.\n - `.shiki/` is the Target Repository mirror for Goals, PRDs, plans, Task DAGs, contracts, locks, worktree records, ledger entries, CCA verdicts, repair packets, reports, and handoffs.\n@@ -15,6 +17,6 @@\n \n - Use Shiki terms exactly when they exist.\n - Add glossary terms to `CONTEXT.md` only when the term is part of the domain, not a generic programming concept.\n-- Create ADRs only for hard-to-reverse, surprising, tradeoff-based decisions.\n+- Create SADRs or target ADRs only for hard-to-reverse, surprising, tradeoff-based decisions.\n - Keep product-specific language in the Target Repository, not in Shiki Template docs.\n-- If an ADR conflicts with a proposed implementation, surface the conflict before editing.\n+- If a SADR or target ADR conflicts with a proposed implementation, surface the conflict before editing.\ndiff --git a/docs/agents/execution-control.md b/docs/agents/execution-control.md\nindex ad8ee3cdb36be56fedc1f46408ca92b387d74047..bc63c3a73661d12b567016ba299f1a6b66bf9088\n--- a/docs/agents/execution-control.md\n+++ b/docs/agents/execution-control.md\n@@ -63,7 +63,7 @@\n | Branch name | Orchestrator | Orchestrator | Before implementer dispatch | branch policy | branch ref in task/ledger |\n | Lock reservation | Context & Impact | MergeGate | Before dispatch and before merge | `.shiki/locks/`, labels, PR metadata | lock id, owner, scope |\n | CI profile | Planner | MergeGate | During issue triage, rechecked at PR | labels, `.shiki/policy.yaml`, workflow matrix | required check list |\n-| Required status checks | Repo maintainer / Guardian | GitHub branch protection / rulesets | Repository setup, updated via ADR | GitHub settings | branch/ruleset config, ADR |\n+| Required status checks | Repo maintainer / Guardian | GitHub branch protection / rulesets | Repository setup, updated via target ADR | GitHub settings | branch/ruleset config, target ADR |\n | CCA judgment | CCA | GitHub required check / MergeGate | Every non-draft PR update | GitHub Action check | structured CCA verdict |\n | Repair assignment | CCA | MergeGate | After failed CCA/CI/review | repair packet, issue/PR comment | repair id and attempt count |\n | Merge readiness | MergeGate | GitHub branch protection / Guardian if needed | After CCA complete | required checks, review rules, rulesets | mergegate check output |\ndiff --git a/docs/agents/guardian-policy.md b/docs/agents/guardian-policy.md\nindex 4147c7adf05a47adb4fd24ada1a1c2e1f2ba6d2d..a20d9cc6c00a569bb057b9ca8d6b51c7fc4a9847\n--- a/docs/agents/guardian-policy.md\n+++ b/docs/agents/guardian-policy.md\n@@ -98,8 +98,8 @@\n `origin/<default_branch>` and reuses each job's existing base fetch; a shallow\n (`--depth`) or second base fetch would make the proof unresolvable and silently\n disable the carry, so neither is permitted. CCA reads `guardian_comment_carried` as\n-a recorded authority in the deterministic signal and does not re-derive it. See ADR\n-0018 for the full decision, the rejected diff/patch-equality and path-disjointness\n+a recorded authority in the deterministic signal and does not re-derive it. See\n+SADR-0018 for the full decision, the rejected diff/patch-equality and path-disjointness\n alternatives, and the accepted residual.\n \n ## External AI Guardian Review (SADR-0010)\ndiff --git a/docs/agents/implementation-policy.md b/docs/agents/implementation-policy.md\nindex c754144a9fb5bf4a38aa82a60e1670c59e626b44..85c37281bcad196c6b7b02343faf00b03cd9c57a\n--- a/docs/agents/implementation-policy.md\n+++ b/docs/agents/implementation-policy.md\n@@ -31,10 +31,13 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n ## Non-Negotiables\n \n 1. **Planning precedes implementation.** Non-trivial Goals start with `grill-with-docs` and Context & Impact.\n-2. **Domain language is controlled.** Terms must be checked against `CONTEXT.md`; hard-to-reverse tradeoffs must be checked against ADRs.\n+2. **Domain language is controlled.** Terms must be checked against `CONTEXT.md`; hard-to-reverse tradeoffs must be checked against Shiki SADRs and target ADRs.\n 3. **PRD precedes issue decomposition.** `to-prd` is used once the context is settled enough to become durable intent.\n 4. **Issues are vertical slices.** `to-issues` must produce independently grabbable tracer-bullet tasks, not horizontal layer tickets.\n 5. **Triage controls dispatch.** Only `ready-for-agent` / AFK tasks may be assigned to Codex without human input.\n@@ -95,19 +98,19 @@\n Process:\n \n - Challenge terms against `CONTEXT.md`.\n-- Check ADRs before reopening decisions.\n+- Check SADRs and target ADRs before reopening decisions.\n - Inspect code when code can answer a question.\n - Ask one question at a time when the operator is available.\n - Provide a recommended answer with each question.\n - Record resolved terms in `CONTEXT.md`.\n-- Create ADRs only for hard-to-reverse, surprising, tradeoff-based decisions.\n+- Create SADRs or target ADRs only for hard-to-reverse, surprising, tradeoff-based decisions.\n - Convert unresolved load-bearing questions into HITL blockers.\n \n Outputs:\n \n - Resolved design-tree decisions.\n - Updated glossary terms when needed.\n-- ADRs when justified.\n+- SADRs or target ADRs when justified.\n - HITL blockers when decisions remain unresolved.\n \n ## Phase 3 \u2014 Context & Impact\n@@ -117,7 +120,7 @@\n \n Outputs must include:\n \n-- Relevant documents and ADRs.\n+- Relevant documents, SADRs, and target ADRs.\n - Relevant modules, interfaces, seams, callers, and tests.\n - Candidate locks.\n - Dependencies.\n@@ -140,7 +143,7 @@\n - Testing decisions.\n - Out of scope.\n - Further notes.\n-- Links to relevant Goal, domain terms, and ADRs.\n+- Links to relevant Goal, domain terms, SADRs, and target ADRs.\n \n Phase 4 ends with Spec Freeze: the operator explicitly approves the PRD, the\n plan records a `spec_freeze` block (status, approved_by, source), and the SF\n@@ -240,7 +243,7 @@\n - Dependencies and locks.\n - Risk and Guardian requirements.\n - Ledger completeness.\n-- Documentation and ADR updates.\n+- Documentation and SADR/target-ADR updates.\n - Scope drift.\n \n CCA emits one of:\n@@ -301,7 +304,7 @@\n | Evidence failure | Behavior might be done, but proof is missing. | Add checks, PR evidence, ledger entry, or artifacts. |\n | Scope drift | PR changes unrelated behavior. | Revert unrelated changes or split into new Goal. |\n | Dependency block | Upstream task or lock not complete. | Block until dependency resolves. |\n-| Architecture block | Decision needs ADR or deep-module discussion. | `grill-with-docs` or `improve-codebase-architecture`. |\n+| Architecture block | Decision needs a SADR, target ADR, or deep-module discussion. | `grill-with-docs` or `improve-codebase-architecture`. |\n | Risk block | Security/production/policy change needs human approval. | Guardian decision. |\n \n ## Phase 5.5 \u2014 Execution Decision Control\ndiff --git a/docs/agents/runtime-auth-model.md b/docs/agents/runtime-auth-model.md\nindex 6a2e1a47e81ea89b5c68a210ec07772a9374babc..3c54748d24233dbd0ba8473950282547ef4ce935\n--- a/docs/agents/runtime-auth-model.md\n+++ b/docs/agents/runtime-auth-model.md\n@@ -148,7 +148,7 @@\n \n API-key based runners may be added by a target repository as an explicit extension. They are not the default Shiki template path.\n \n-If a repo opts into API-key mode, record the exception in an ADR and update the repo's `AGENTS.md`, `.shiki/config.yaml`, and workflow permissions.\n+If a repo opts into API-key mode, record the target-specific exception in a target ADR and update the repo's `AGENTS.md`, `.shiki/config.yaml`, and workflow permissions.\n \n ## GitHub Actions Runtime Compatibility\n \ndiff --git a/docs/agents/skill-gate.md b/docs/agents/skill-gate.md\nindex c50398b0d7c60a8598f2a5c88a10015730dced13..3acffe6444617bb1d54b77d4b4708158ec735bc6\n--- a/docs/agents/skill-gate.md\n+++ b/docs/agents/skill-gate.md\n@@ -9,7 +9,7 @@\n | Skill | Trigger | Expected output |\n | --- | --- | --- |\n | `setup-matt-pocock-skills` | First configuring a repo for these skills, or when issue tracker, triage labels, or domain docs are missing. | `docs/agents/` configuration and an `Agent skills` block in `AGENTS.md` or `CLAUDE.md`. |\n-| `grill-with-docs` | Ambiguous plan, unclear terminology, boundary decision, tradeoff, or ADR-worthy design choice. | Clarified decision tree, recommended answers, updates to `CONTEXT.md` or ADRs when decisions crystallize. |\n+| `grill-with-docs` | Ambiguous plan, unclear terminology, boundary decision, tradeoff, or decision-record-worthy design choice. | Clarified decision tree, recommended answers, updates to `CONTEXT.md`, SADRs, or target ADRs when decisions crystallize. |\n | `zoom-out` | Agent lacks a higher-level architectural map. | Map of relevant modules, callers, seams, and domain concepts. |\n | `to-prd` | Settled context should become a PRD. | PRD published to the issue tracker with problem, solution, user stories, decisions, testing, and out-of-scope notes. |\n | `to-issues` | Goal, PRD, or plan must become implementation tickets. | Vertical-slice issues with acceptance criteria, dependencies, and AFK/HITL classification. |\n@@ -17,7 +17,7 @@\n | `tdd` | Feature work or bug fix has observable behavior that can be specified with tests. | Red-green-refactor loop through public interfaces, one vertical slice at a time. |\n | `code-review` | The TDD loop is green and the implementer is about to create or update a PR. Default for every implementation task. | Pre-PR self-review of the task diff with fixes applied on the task branch; findings and resolutions recorded as a ledger entry naming the skill and a `## Pre-PR code review` PR body section. |\n | `diagnose` | Hard bug, failing check, regression, flaky behavior, or performance problem. | Reproduction loop, ranked hypotheses, instrumentation, fix, regression test, post-mortem. |\n-| `improve-codebase-architecture` | Structural friction, testability, AI-navigability, deep-module opportunity, or refactor request. | Architecture candidates and deepening recommendations informed by `CONTEXT.md` and ADRs. |\n+| `improve-codebase-architecture` | Structural friction, testability, AI-navigability, deep-module opportunity, or refactor request. | Architecture candidates and deepening recommendations informed by `CONTEXT.md`, SADRs, and target ADRs. |\n | `prototype` | A throwaway logic or UI prototype is needed to answer a design question. | Clearly marked disposable prototype and captured decision. |\n \n ## MergeGate Interaction\ndiff --git a/docs/operations.md b/docs/operations.md\nindex cf1f452d4c589e38ccd58553bda0ad8684b51eb7..ace48a90ffbec9e5c0a7113bef16a4858bc44b3f\n--- a/docs/operations.md\n+++ b/docs/operations.md\n@@ -89,6 +89,16 @@\n      target history, brought forward by `shiki migrate`, not a template.\n    - **Everything else** (CLI scripts, workflows, schemas, docs) is overwritten.\n \n+   A forced upgrade also migrates the former numeric Shiki decision paths to\n+   `SADR-NNNN-*`. Before the first write, the installer inventories the exact\n+   legacy Shiki paths and authorizes each deletion only when the existing\n+   `.shiki/install-stamp.json` names that path and its SHA-256 digest still\n+   matches the file. An absent, unreadable, malformed, incomplete, or\n+   mismatching stamp reports every blocked path and stops with no filesystem\n+   change. Target-owned `NNNN-*` ADRs are not cleanup candidates. A non-force\n+   install deletes nothing and retains any still-matching legacy stamp entries\n+   so a later forced upgrade can prove ownership.\n+\n    If the target has pending migrations, `--force` refuses and writes nothing \u2014\n    apply migrations first (step 3), then re-run. The run ends with a single\n    summary of every `.new` written, so a half-upgrade is visible.\ndiff --git a/docs/releasing.md b/docs/releasing.md\nindex 10d73828e7dbd64e9d9f6b83bc84d010537ce62a..aafeee8c4d441e1e427efee2ce4f81a647f020e1\n--- a/docs/releasing.md\n+++ b/docs/releasing.md\n@@ -67,7 +67,7 @@\n CLI in `scripts/`. There are no third-party Python runtime dependencies. The CLI\n is intentionally **not** exposed as a `[project.scripts]` console entry point\n because `scripts/shiki.py` is a dependency-free shim and `scripts/` is not\n-packaged as an importable distribution (see the ADR for the rationale).\n+packaged as an importable distribution (see SADR-0007 for the rationale).\n \n Supported install paths:\n \n@@ -155,6 +155,15 @@\n    [docs/operations.md](operations.md#the-new-convention). If the target has\n    pending migrations, `--force` refuses and writes nothing; apply migrations\n    (step 2) first, then re-run.\n+\n+   The SADR namespace upgrade has an additional pre-write gate: each former\n+   numeric Shiki decision path is removed only when the existing install stamp\n+   contains that exact path and a digest matching its current bytes. The\n+   installer aggregates absent/unreadable/malformed-stamp, missing-digest, and\n+   digest-mismatch blockers before raising, so refusal leaves the target tree\n+   byte-identical. Numeric target ADRs are preserved. A successful forced\n+   upgrade installs and stamps `SADR-*` paths and drops the removed legacy paths\n+   from the refreshed stamp.\n 2. Apply repository-local `.shiki/` state migrations with the migrate command:\n \n    ```bash\ndiff --git a/docs/usage.md b/docs/usage.md\nindex b83407ee3970dd395dd893010bc5dbe8bad26051..a130ef6e40dfd8169a5bc701339351097df1a09f\n--- a/docs/usage.md\n+++ b/docs/usage.md\n@@ -67,8 +67,10 @@\n shiki install-target /path/to/target-repo --local-only\n ```\n \n-Use `--force` only when you intentionally want to overwrite existing target\n-files.\n+Use `--force` only when you intentionally want to refresh the target template.\n+For the SADR namespace migration, `--force` deletes only exact legacy Shiki\n+decision paths whose existing install-stamp digest still matches; any ownership\n+blocker stops before the first write. Numeric target ADRs are never candidates.\n \n ## `shiki bootstrap-platform` \u2014 publish the Shiki platform repo\n \ndiff --git a/scripts/guardian_approval_signal.py b/scripts/guardian_approval_signal.py\nindex d059f92b2c82aa2b5d6ca421365537a87886c1cb..16924892714f4f5b9303c0ef0e325a7e70075630\n--- a/scripts/guardian_approval_signal.py\n+++ b/scripts/guardian_approval_signal.py\n@@ -199,8 +199,8 @@\n ) -> Any:\n     \"\"\"Evaluate SADR-0015 Contract Approval for the CCA Guardian signal.\n \n-    The SAME OR that MergeGate applies must reach this deterministic signal: ADR\n-    0015 is explicit that without it, a contract-approved PR still returns\n+    The SAME OR that MergeGate applies must reach this deterministic signal:\n+    SADR-0015 is explicit that without it, a contract-approved PR still returns\n     ``needs_guardian``, the CCA job fails, and the MergeGate policy check never\n     runs \u2014 the gate this change moves would never be reached. This resolves the\n     task the SAME way MergeGate does (the first Shiki task id in the PR body),\ndiff --git a/scripts/shiki_contract_approval.py b/scripts/shiki_contract_approval.py\nindex 941bcb96fdd8b9a85bc0c4f0f04d5dad9d04839c..4a4b418f6527413161db82670bec857a043360e2\n--- a/scripts/shiki_contract_approval.py\n+++ b/scripts/shiki_contract_approval.py\n@@ -142,7 +142,7 @@\n     changed_files_status: Any,\n     registration: Any,\n ) -> ContractApprovalResult:\n-    \"\"\"Whether this PR carries an SADR-0015 Contract Approval.\n+    \"\"\"Whether this PR carries a SADR-0015 Contract Approval.\n \n     The four conditions are evaluated IN ORDER, each failure recorded as a\n     distinct reason string:\ndiff --git a/scripts/shiki_guardian.py b/scripts/shiki_guardian.py\nindex a4ecb79bbda55fd5c8b06adfae2eb99760a03c45..4dd99bbe714e26f421fa19abac7a34ae1b195ecc\n--- a/scripts/shiki_guardian.py\n+++ b/scripts/shiki_guardian.py\n@@ -12,7 +12,7 @@\n # The carry re-derives its proof from git in-process on every judging invocation;\n # it is never read from an artifact, so there is nothing to go stale or be forged.\n # shiki_sync_proof is the ONLY guardian-family module that runs git, and every one\n-# of its invocations is read-only and local (ADR: prove-from-git base sync).\n+# of its invocations is read-only and local (SADR: prove-from-git base sync).\n from shiki_sync_proof import verify_pure_base_sync\n \n GUARDIAN_POLICY_PATH = \".shiki/guardian-policy.json\"\ndiff --git a/scripts/shiki_guardian_review.py b/scripts/shiki_guardian_review.py\nindex dd0f104d457899e0ba5ff1e162011c1e90b6f92b..e10ef58c046324e7ea3a3392feb3048ca77414ce\n--- a/scripts/shiki_guardian_review.py\n+++ b/scripts/shiki_guardian_review.py\n@@ -159,7 +159,7 @@\n     docs_only = _is_docs_only(changed_files)\n     if docs_only and matched == 0:\n         focus.append(\n-            \"Docs/ADR-only change: confirm the change is genuinely documentation/decision-only, introduces no behavior or contract drift, and stays consistent with the constitution and existing ADRs.\"\n+            \"Docs/decision-record-only change: confirm the change is genuinely documentation/decision-only, introduces no behavior or contract drift, and stays consistent with the constitution and existing SADRs and target ADRs.\"\n         )\n \n     # Conservative augmentation: unknown (no category and not docs-only), mixed\ndiff --git a/scripts/shiki_installer.py b/scripts/shiki_installer.py\nindex be064ab7a29ff6836f7f7a1d70500a43e0d11d7b..9fd8eefd137ce7003dad5c0dcb2e48e2cd47ab32\n--- a/scripts/shiki_installer.py\n+++ b/scripts/shiki_installer.py\n@@ -118,6 +118,7 @@\n # what lets a target carry it without a governance-schema change.\n INSTALL_STAMP_PATH = \".shiki/install-stamp.json\"\n INSTALL_STAMP_VERSION = 1\n+LEGACY_SHIKI_SADR_MAX = 18\n \n # Under --force the shipped surface splits three ways, so nothing is lost and\n # nothing is silently stale.\n@@ -237,9 +238,98 @@\n         )\n \n \n+def legacy_shiki_adr_paths() -> tuple[str, ...]:\n+    \"\"\"Former numeric paths for Shiki SADR-0001 through SADR-0018.\n+\n+    The authoritative slugs come from the current SADR filenames. SADR-0019 is\n+    the namespace decision itself and never shipped under a numeric legacy path.\n+    \"\"\"\n+    paths: list[str] = []\n+    for sadr in sorted((ROOT / \"docs\" / \"adr\").glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\")):\n+        number = sadr.name.split(\"-\", 2)[1]\n+        if int(number) <= LEGACY_SHIKI_SADR_MAX:\n+            paths.append(f\"docs/adr/{sadr.name.removeprefix('SADR-')}\")\n+    return tuple(paths)\n+\n+\n+def preflight_legacy_sadr_cleanup(target: Path, *, force: bool) -> tuple[str, ...]:\n+    \"\"\"Authorize exact legacy Shiki ADR removals before the first write.\n+\n+    A non-force install never removes existing files. A forced install needs no\n+    stamp when no legacy Shiki path is present. When legacy paths do exist, each\n+    must be named in a readable install stamp and its current bytes must match\n+    the recorded digest.\n+    \"\"\"\n+    if not force:\n+        return ()\n+    present = tuple(\n+        relative\n+        for relative in legacy_shiki_adr_paths()\n+        if (target / relative).exists() or (target / relative).is_symlink()\n+    )\n+    if not present:\n+        return ()\n+\n+    stamp_path = target / INSTALL_STAMP_PATH\n+    try:\n+        raw = stamp_path.read_text(encoding=\"utf-8\")\n+        stamp = json.loads(raw)\n+    except FileNotFoundError:\n+        detail = \"install stamp is absent\"\n+        blockers = [f\"{relative}: {detail}\" for relative in present]\n+    except (OSError, UnicodeDecodeError) as error:\n+        detail = f\"install stamp is unreadable ({error})\"\n+        blockers = [f\"{relative}: {detail}\" for relative in present]\n+    except json.JSONDecodeError as error:\n+        detail = f\"install stamp is malformed ({error})\"\n+        blockers = [f\"{relative}: {detail}\" for relative in present]\n+    else:\n+        if not isinstance(stamp, dict):\n+            blockers = [\n+                f\"{relative}: install stamp is malformed (expected an object)\"\n+                for relative in present\n+            ]\n+        else:\n+            digests = stamp.get(\"digests\")\n+            blockers = []\n+            if not isinstance(digests, dict):\n+                blockers.extend(\n+                    f\"{relative}: install stamp has no digest for this path\"\n+                    for relative in present\n+                )\n+            else:\n+                for relative in present:\n+                    expected = digests.get(relative)\n+                    if not isinstance(expected, str) or not expected:\n+                        blockers.append(\n+                            f\"{relative}: install stamp has no digest for this path\"\n+                        )\n+                        continue\n+                    try:\n+                        actual = _sha256_file(target / relative)\n+                    except OSError as error:\n+                        blockers.append(\n+                            f\"{relative}: legacy path is unreadable ({error})\"\n+                        )\n+                        continue\n+                    if actual != expected:\n+                        blockers.append(\n+                            f\"{relative}: digest mismatch (stamp {expected}, current {actual})\"\n+                        )\n+\n+    if blockers:\n+        joined = \"\\n\".join(f\"- {blocker}\" for blocker in blockers)\n+        raise ShikiError(\n+            \"legacy Shiki ADR cleanup is not authorized:\\n\"\n+            f\"{joined}\\nNo files were written.\"\n+        )\n+    return present\n+\n+\n def install_template(target: Path, *, force: bool, validate: bool) -> list[NewFileNote]:\n     # Preconditions first: never rewrite the tree of a target that cannot be\n     # upgraded cleanly (see refuse_pending_migrations).\n+    legacy_cleanup = preflight_legacy_sadr_cleanup(target, force=force)\n     refuse_pending_migrations(target)\n \n     # Resolve the target's required CODEOWNERS owner once from its\n@@ -250,6 +340,10 @@\n     # verbatim. See ``codeowners_required_owner``.\n     codeowners_owner = codeowners_required_owner(target)\n \n+    for relative in legacy_cleanup:\n+        (target / relative).unlink()\n+        info(f\"removed stamped legacy Shiki ADR: {target / relative}\")\n+\n     notes: list[NewFileNote] = []\n     for relative in TEMPLATE_PATHS:\n         source = ROOT / relative\n@@ -647,6 +741,22 @@\n     digests = target_stamp_digests(target)\n     existing = read_install_stamp(target)\n     if existing is not None:\n+        # A non-force install deliberately leaves legacy Shiki ADRs in place.\n+        # Preserve only still-matching ownership proof so a later forced\n+        # upgrade can authorize cleanup; never carry a missing or stale digest.\n+        existing_digests = existing.get(\"digests\")\n+        if isinstance(existing_digests, dict):\n+            for relative in legacy_shiki_adr_paths():\n+                expected = existing_digests.get(relative)\n+                path = target / relative\n+                if not isinstance(expected, str) or not path.is_file():\n+                    continue\n+                try:\n+                    actual = _sha256_file(path)\n+                except OSError:\n+                    continue\n+                if actual == expected:\n+                    digests[relative] = actual\n         if _incoming_is_older(commit, existing.get(\"platform_commit\")):\n             warn(\n                 \"kept existing install stamp: incoming install \"\ndiff --git a/scripts/shiki_loop.py b/scripts/shiki_loop.py\nindex 3611641da8aeb5038b1da90e3114b09a473ec6d8..84caa491b9cad0edc4176f209c13e9221e975681\n--- a/scripts/shiki_loop.py\n+++ b/scripts/shiki_loop.py\n@@ -424,7 +424,7 @@\n         # unresolvable CCA verdict has already produced its terminal stop and is never\n         # laundered into a head-moving sync (SADR-0010). It also never fires while the\n         # policy (Guardian) gate is red \u2014 for ANY reason, not only when it is the sole\n-        # red check \u2014 so a head-bound Guardian approval, or an SADR-0015\n+        # red check \u2014 so a head-bound Guardian approval, or a SADR-0015\n         # contract-approval carry that base movement defeated (the policy gate compares\n         # base and head task governance directly), is never silently restored by\n         # adopting base wholesale. It is bounded by a durable per-task counter so the\ndiff --git a/scripts/shiki_tasks.py b/scripts/shiki_tasks.py\nindex aec3809f24b4a0c8c96fc50971cff9ae9ad30381..d531052e9d9717058d4631483cefab44c26a65c1\n--- a/scripts/shiki_tasks.py\n+++ b/scripts/shiki_tasks.py\n@@ -821,7 +821,7 @@\n         \"prompt\": prompt,\n         \"entry_skill\": \"grill-with-docs\",\n         \"required_next_steps\": [\n-            \"Run grill-with-docs until terminology, boundaries, risks, and ADR-worthy decisions are settled.\",\n+            \"Run grill-with-docs until terminology, boundaries, risks, and decision-record-worthy choices are settled.\",\n             \"Obtain the operator's explicit approval of the PRD/requirements (Spec Freeze).\",\n             \"Write a machine-readable plan JSON with grill_with_docs.status=complete and spec_freeze.status=frozen.\",\n             \"Run shiki plan ingest --plan-file PLAN.json.\",\ndiff --git a/scripts/test_shiki_goal_loop.sh b/scripts/test_shiki_goal_loop.sh\nindex 753c7b516e10f0be3bc92a4bd0eb1fa699776ae6..8f3820a0a079b3044424cfe1a6c6b7fe68896e67\n--- a/scripts/test_shiki_goal_loop.sh\n+++ b/scripts/test_shiki_goal_loop.sh\n@@ -231,8 +231,8 @@\n \n # High/critical risk merges autonomously when all required checks are green:\n # the \"MergeGate policy check\" required check IS the Guardian gate (it enforces\n-# guardian-policy.json \u2014 human approval OR external AI guardian review, ADR\n-# 0010), so green checks mean a recorded authority approved. The fake gh returns\n+# guardian-policy.json \u2014 human approval OR external AI guardian review,\n+# SADR-0010), so green checks mean a recorded authority approved. The fake gh returns\n # green checks, simulating that the Guardian gate passed.\n python3 \"$ROOT/scripts/shiki.py\" goal create --target \"$TARGET\" --title \"Guardian gate\" --outcome \"High risk merges via policy gate\" >/tmp/shiki-goal-loop-guardian-goal.json\n GGOAL=\"$(json_get /tmp/shiki-goal-loop-guardian-goal.json goal_id)\"\ndiff --git a/tests/test_adr_references.py b/tests/test_adr_references.py\nindex 51706329c797d5dd9b47e082a333fa83b261d482..d005da4f771f5e8085ec22d4b787980e68f5ae8c\n--- a/tests/test_adr_references.py\n+++ b/tests/test_adr_references.py\n@@ -1,8 +1,8 @@\n-\"\"\"Every ``ADR ####`` citation must resolve to exactly one decision record.\n+\"\"\"Every target ADR and Shiki SADR citation resolves in its own namespace.\n \n Background. ``scripts/mergegate_check.py`` justifies ``is_bookkeeping_closeout``\n-with \"ADR 0017\" nine times and ``scripts/guardian_approval_signal.py`` cites\n-0016/0017 seven times, yet for a stretch the numbering in ``docs/adr/`` stopped\n+with \"SADR-0017\" nine times and ``scripts/guardian_approval_signal.py`` cites\n+SADR-0016/SADR-0017 seven times, yet for a stretch the numbering in ``docs/adr/`` stopped\n at 0015: the authoring commits lived on a coordinator branch and no task ever\n declared ``docs/adr/...`` in its locks, so nothing carried the records to the\n default branch. The stated basis of an exemption that merged seven PRs with zero\n@@ -11,11 +11,12 @@\n and the directory drifted with no signal.\n \n This is the same unbound-pair shape behind other silent failures: a reference\n-and its target with nothing asserting they agree. This test binds them. It scans\n-every ``scripts/*.py`` (plus ``docs/**/*.md`` and the root ``*.md`` governance\n-files, which are cheap) for the ``ADR ####`` pattern and asserts each cited\n-number resolves to exactly one ``docs/adr/####-*.md``. The converse is\n-deliberately NOT required: an ADR may exist without being cited.\n+and its target with nothing asserting they agree. This test binds them. Target\n+``ADR NNNN`` citations resolve only to ``docs/adr/NNNN-*.md``; Shiki\n+``SADR-NNNN`` citations resolve only to ``docs/adr/SADR-NNNN-*.md``. The scan\n+covers current governance docs, scripts, workflows, prompts, packaging metadata,\n+and repo-local Codex guidance while deliberately excluding historical\n+``.shiki`` evidence. The converse is not required: a record may be uncited.\n \n The suite is dependency free: standard-library ``unittest`` only.\n \"\"\"\n@@ -31,17 +32,18 @@\n \n REPO_ROOT = Path(__file__).resolve().parents[1]\n \n-# \"ADR 0016\" -- a four-digit citation. The trailing \\b keeps a five-digit token\n-# from being read as a truncated four-digit number.\n-_CITATION = re.compile(r\"\\bADR (\\d{4})\\b\")\n-# \"0016-locks-bound-product-paths.md\" -- the leading four digits name the record.\n-_ADR_FILENAME = re.compile(r\"^(\\d{4})-.*\\.md$\")\n+# ``ADR 0016`` / ``SADR-0016`` -- a namespace plus four-digit citation. The\n+# trailing ``\\b`` keeps a five-digit token from being read as a truncated one.\n+_CITATION = re.compile(r\"\\b(S?ADR)[ -](\\d{4})\\b\")\n+# Target ADRs retain the numeric filename; Shiki platform decisions use SADR.\n+_DECISION_FILENAME = re.compile(r\"^(?:(SADR)-)?(\\d{4})-.*\\.md$\")\n \n \n class Violation(NamedTuple):\n     \"\"\"A citation that does not resolve to exactly one decision record.\"\"\"\n \n     citing_file: str  # repo-relative path of the file that cites the ADR\n+    namespace: str  # \"ADR\" for target decisions; \"SADR\" for Shiki decisions\n     number: str  # the cited four-digit number, e.g. \"0016\"\n     line: int  # 1-based line of the citation\n     reason: str  # \"missing\" (zero matches) or \"ambiguous\" (two or more)\n@@ -49,44 +51,59 @@\n \n     @property\n     def expected_path(self) -> str:\n-        return f\"docs/adr/{self.number}-*.md\"\n+        prefix = \"SADR-\" if self.namespace == \"SADR\" else \"\"\n+        return f\"docs/adr/{prefix}{self.number}-*.md\"\n \n     def describe(self) -> str:\n         \"\"\"Name the citing file, the cited number, and the missing path.\"\"\"\n         if self.reason == \"missing\":\n             return (\n-                f\"{self.citing_file}:{self.line} cites ADR {self.number} \"\n+                f\"{self.citing_file}:{self.line} cites {self.namespace} {self.number} \"\n                 f\"but no document matches {self.expected_path}\"\n             )\n         joined = \", \".join(self.matches)\n         return (\n-            f\"{self.citing_file}:{self.line} cites ADR {self.number} which \"\n+            f\"{self.citing_file}:{self.line} cites {self.namespace} {self.number} which \"\n             f\"resolves to {len(self.matches)} documents (expected exactly one \"\n             f\"at {self.expected_path}): {joined}\"\n         )\n \n \n-def _adr_index(root: Path) -> dict[str, list[Path]]:\n-    \"\"\"Map every four-digit ADR number to the record files that claim it.\"\"\"\n-    index: dict[str, list[Path]] = {}\n+def _adr_index(root: Path) -> dict[tuple[str, str], list[Path]]:\n+    \"\"\"Map each decision namespace and number to the files that claim it.\"\"\"\n+    index: dict[tuple[str, str], list[Path]] = {}\n     adr_dir = root / \"docs\" / \"adr\"\n     if adr_dir.is_dir():\n         for path in sorted(adr_dir.glob(\"*.md\")):\n-            match = _ADR_FILENAME.match(path.name)\n+            match = _DECISION_FILENAME.match(path.name)\n             if match:\n-                index.setdefault(match.group(1), []).append(path)\n+                namespace = \"SADR\" if match.group(1) else \"ADR\"\n+                index.setdefault((namespace, match.group(2)), []).append(path)\n     return index\n \n \n def _citing_files(root: Path, include_docs: bool = True) -> list[Path]:\n-    \"\"\"Files scanned for citations: scripts/*.py, and cheaply the markdown.\"\"\"\n+    \"\"\"Current, shipped text surfaces scanned for decision citations.\"\"\"\n     files = sorted((root / \"scripts\").glob(\"*.py\"))\n+    files += sorted((root / \"scripts\").glob(\"*.sh\"))\n     if include_docs:\n         docs = root / \"docs\"\n         if docs.is_dir():\n             files += sorted(docs.rglob(\"*.md\"))\n         files += sorted(root.glob(\"*.md\"))\n-    return files\n+        pyproject = root / \"pyproject.toml\"\n+        if pyproject.is_file():\n+            files.append(pyproject)\n+        for relative in (\".github\", \".codex\"):\n+            surface = root / relative\n+            if surface.is_dir():\n+                files += sorted(\n+                    path\n+                    for path in surface.rglob(\"*\")\n+                    if path.is_file()\n+                    and path.suffix in {\".md\", \".py\", \".sh\", \".toml\", \".yaml\", \".yml\"}\n+                )\n+    return sorted(set(files))\n \n \n def find_adr_reference_violations(\n@@ -94,10 +111,9 @@\n ) -> list[Violation]:\n     \"\"\"Return every ``ADR ####`` citation under ``root`` that fails to resolve.\n \n-    A citation resolves when exactly one ``docs/adr/####-*.md`` matches its\n-    number. Zero matches is a \"missing\" violation; two or more is \"ambiguous\".\n-    Records that are never cited produce nothing -- the converse is not\n-    required.\n+    A citation resolves when exactly one record in the same namespace matches\n+    its number. Zero matches is a \"missing\" violation; two or more is\n+    \"ambiguous\". Records that are never cited produce nothing.\n     \"\"\"\n     root = Path(root)\n     index = _adr_index(root)\n@@ -110,13 +126,15 @@\n         rel = path.relative_to(root).as_posix()\n         for lineno, line in enumerate(text.splitlines(), start=1):\n             for match in _CITATION.finditer(line):\n-                number = match.group(1)\n-                found = index.get(number, [])\n+                namespace, number = match.groups()\n+                found = index.get((namespace, number), [])\n                 if len(found) == 1:\n                     continue\n                 reason = \"missing\" if not found else \"ambiguous\"\n                 rels = [p.relative_to(root).as_posix() for p in found]\n-                violations.append(Violation(rel, number, lineno, reason, rels))\n+                violations.append(\n+                    Violation(rel, namespace, number, lineno, reason, rels)\n+                )\n     return violations\n \n \n@@ -129,8 +147,8 @@\n         self.assertEqual(\n             violations,\n             [],\n-            \"Every 'ADR ####' citation must resolve to exactly one \"\n-            f\"docs/adr/####-*.md:\\n{detail}\",\n+            \"Every ADR/SADR citation must resolve exactly once in its own \"\n+            f\"docs/adr namespace:\\n{detail}\",\n         )\n \n     def test_scan_actually_reads_scripts(self) -> None:\n@@ -142,12 +160,38 @@\n             \"the citation scan must cover scripts/*.py\",\n         )\n \n+    def test_platform_citations_do_not_use_unresolved_numeric_shorthand(self) -> None:\n+        shorthand = re.compile(r\"SADR-\\d{4}/\\d{4}\")\n+        offenders = []\n+        for path in _citing_files(REPO_ROOT):\n+            text = path.read_text(encoding=\"utf-8\")\n+            if shorthand.search(text):\n+                offenders.append(path.relative_to(REPO_ROOT).as_posix())\n+        self.assertEqual(\n+            offenders,\n+            [],\n+            \"repeat the SADR prefix so every cited number is independently resolvable\",\n+        )\n+\n     def test_carried_records_are_indexed(self) -> None:\n         # The two records this Goal carried onto the default branch, and which\n         # the scripts cite most, must be present.\n         index = _adr_index(REPO_ROOT)\n-        self.assertIn(\"0016\", index)\n-        self.assertIn(\"0017\", index)\n+        self.assertIn((\"SADR\", \"0016\"), index)\n+        self.assertIn((\"SADR\", \"0017\"), index)\n+\n+    def test_platform_tree_has_only_sadr_0001_through_0019(self) -> None:\n+        adr_dir = REPO_ROOT / \"docs\" / \"adr\"\n+        sadrs = sorted(adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\"))\n+        self.assertEqual(\n+            {path.name.split(\"-\", 2)[1] for path in sadrs},\n+            {f\"{number:04d}\" for number in range(1, 20)},\n+        )\n+        self.assertEqual(\n+            list(adr_dir.glob(\"[0-9][0-9][0-9][0-9]-*.md\")),\n+            [],\n+            \"the Shiki platform tree must not retain legacy numeric SADR paths\",\n+        )\n \n \n class FixtureAdrReferences(unittest.TestCase):\n@@ -180,6 +224,26 @@\n         self.assertIn(\"9991\", message)  # cited number\n         self.assertIn(\"docs/adr/9991-*.md\", message)  # missing path\n \n+    def test_target_adr_does_not_satisfy_missing_platform_sadr(self) -> None:\n+        root = self._make_repo(\n+            adr_names=[\"0042-target-decision.md\"],\n+            script_body='\"\"\"Target ADR 0042 exists, but platform SADR-0042 does not.\"\"\"\\n',\n+        )\n+        violations = find_adr_reference_violations(root, include_docs=False)\n+        self.assertEqual(len(violations), 1)\n+        self.assertEqual(violations[0].namespace, \"SADR\")\n+        self.assertEqual(violations[0].number, \"0042\")\n+        self.assertEqual(violations[0].expected_path, \"docs/adr/SADR-0042-*.md\")\n+\n+    def test_target_adr_and_platform_sadr_may_reuse_a_number(self) -> None:\n+        root = self._make_repo(\n+            adr_names=[\"0042-target-decision.md\", \"SADR-0042-platform-decision.md\"],\n+            script_body='\"\"\"Target ADR 0042 and platform SADR-0042 are distinct.\"\"\"\\n',\n+        )\n+        self.assertEqual(\n+            find_adr_reference_violations(root, include_docs=False), []\n+        )\n+\n     def test_uncited_adr_is_allowed(self) -> None:\n         # An ADR exists but nothing cites it -> not a violation. The converse\n         # (every ADR must be cited) is explicitly NOT required.\ndiff --git a/tests/test_guardian_review.py b/tests/test_guardian_review.py\nindex 88829e7810212635e2fc98aa5918d6f26294677d..00a85a237acd5bf2d6cb5b294128198c422402ca\n--- a/tests/test_guardian_review.py\n+++ b/tests/test_guardian_review.py\n@@ -162,7 +162,7 @@\n \n     def test_docs_only_is_light_and_not_conservative(self):\n         focus = gr.classify_review_focus([\"docs/adr/0099-x.md\", \"README.md\"], \"\", \"low\")\n-        self.assertTrue(any(\"Docs/ADR-only\" in f for f in focus))\n+        self.assertTrue(any(\"Docs/decision-record-only\" in f for f in focus))\n         self.assertFalse(any(\"conservative default\" in f.lower() for f in focus))\n \n     def test_unknown_category_is_conservative(self):\ndiff --git a/tests/test_policy_job_merged_prs.py b/tests/test_policy_job_merged_prs.py\nindex d45512e74681e5e649279236c7d4e79d5c3da471..b97989f8bd74d091de1a6b5275a20d8dc68c4512\n--- a/tests/test_policy_job_merged_prs.py\n+++ b/tests/test_policy_job_merged_prs.py\n@@ -3,8 +3,8 @@\n \n ``.github/workflows/shiki-cca-completion.yml`` has two consumers of the merged-PR\n proof: the CCA *signal* (``guardian_approval_signal.py`` in \"Prepare CCA context\")\n-and the authoritative gate (``mergegate_check.py`` in \"Enforce MergeGate\"). ADR\n-0017's bookkeeping-closeout exemption (condition 6) needs the merged implementation\n+and the authoritative gate (``mergegate_check.py`` in \"Enforce MergeGate\").\n+SADR-0017's bookkeeping-closeout exemption (condition 6) needs the merged implementation\n PR proven, and a closeout carries NO ``post_merge_reconcile`` marker. The signal\n already resolves ``MERGED_PRS`` unconditionally, but \"Enforce MergeGate\" used to\n resolve it only inside ``grep -q '<!-- shiki:post_merge_reconcile -->'``. So a\ndiff --git a/tests/test_sadr_install_migration.py b/tests/test_sadr_install_migration.py\nnew file mode 100644\nindex 0000000000000000000000000000000000000000..d51eac872b1c79ff4579c4c1401ca5d775a60d8d\n--- /dev/null\n+++ b/tests/test_sadr_install_migration.py\n@@ -0,0 +1,237 @@\n+\"\"\"SADR installation and collision-safe legacy upgrade behavior.\"\"\"\n+\n+from __future__ import annotations\n+\n+import contextlib\n+import hashlib\n+import io\n+import json\n+import shutil\n+import tempfile\n+import unittest\n+from pathlib import Path\n+\n+import shiki_test_support  # noqa: F401  (path bootstrap)\n+\n+import shiki_installer\n+from tests.test_adr_references import find_adr_reference_violations\n+\n+\n+PRODUCT_NUMBERS = tuple(range(13, 19))\n+\n+\n+def _quiet_install(target: Path, *, force: bool, validate: bool) -> None:\n+    with contextlib.redirect_stdout(io.StringIO()):\n+        shiki_installer.install_template(target, force=force, validate=validate)\n+\n+\n+def _sha256(path: Path) -> str:\n+    return hashlib.sha256(path.read_bytes()).hexdigest()\n+\n+\n+def _legacy_sadr_paths() -> tuple[str, ...]:\n+    return tuple(\n+        f\"docs/adr/{path.name.removeprefix('SADR-')}\"\n+        for path in sorted((shiki_installer.ROOT / \"docs\" / \"adr\").glob(\"SADR-*.md\"))\n+        if path.name != \"SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\"\n+    )\n+\n+\n+def _create_legacy_upgrade_fixture(\n+    target: Path,\n+) -> tuple[dict[str, bytes], tuple[str, ...]]:\n+    adr_dir = target / \"docs\" / \"adr\"\n+    adr_dir.mkdir(parents=True)\n+    product_bytes = {\n+        f\"{number:04d}-cubeshare-product-decision.md\": (\n+            f\"# CubeShare ADR {number:04d}\\n\\nDo not rewrite {number}.\\n\"\n+        ).encode()\n+        for number in PRODUCT_NUMBERS\n+    }\n+    for name, content in product_bytes.items():\n+        (adr_dir / name).write_bytes(content)\n+\n+    legacy_paths = _legacy_sadr_paths()\n+    for relative in legacy_paths:\n+        (target / relative).write_bytes(f\"legacy Shiki bytes for {relative}\\n\".encode())\n+\n+    stamp_path = target / shiki_installer.INSTALL_STAMP_PATH\n+    stamp_path.parent.mkdir(parents=True, exist_ok=True)\n+    stamp = {\n+        \"version\": shiki_installer.INSTALL_STAMP_VERSION,\n+        \"platform_commit\": None,\n+        \"installed_at\": \"2000-01-01T00:00:00+00:00\",\n+        \"digests\": {\n+            relative: _sha256(target / relative) for relative in legacy_paths\n+        },\n+    }\n+    stamp_path.write_text(\n+        json.dumps(stamp, indent=2, sort_keys=True) + \"\\n\", encoding=\"utf-8\"\n+    )\n+    return product_bytes, legacy_paths\n+\n+\n+def _tree_snapshot(root: Path) -> dict[str, tuple[str, bytes | str]]:\n+    \"\"\"Recursive byte/shape snapshot used to prove preflight is write-free.\"\"\"\n+    snapshot: dict[str, tuple[str, bytes | str]] = {}\n+    for path in sorted(root.rglob(\"*\")):\n+        relative = path.relative_to(root).as_posix()\n+        if path.is_symlink():\n+            snapshot[relative] = (\"symlink\", path.readlink().as_posix())\n+        elif path.is_dir():\n+            snapshot[relative] = (\"directory\", \"\")\n+        else:\n+            snapshot[relative] = (\"file\", path.read_bytes())\n+    return snapshot\n+\n+\n+class FreshSadrInstallTests(unittest.TestCase):\n+    def setUp(self) -> None:\n+        self.tmp = Path(tempfile.mkdtemp(prefix=\"shiki-sadr-fresh-\"))\n+        self.addCleanup(lambda: shutil.rmtree(self.tmp, ignore_errors=True))\n+        self.target = self.tmp / \"target\"\n+        self.adr_dir = self.target / \"docs\" / \"adr\"\n+        self.adr_dir.mkdir(parents=True)\n+        self.product_bytes = {\n+            f\"{number:04d}-product-decision.md\": (\n+                f\"# Product ADR {number:04d}\\n\\nTarget-owned bytes {number}.\\n\"\n+            ).encode()\n+            for number in PRODUCT_NUMBERS\n+        }\n+        for name, content in self.product_bytes.items():\n+            (self.adr_dir / name).write_bytes(content)\n+\n+    def test_fresh_install_preserves_product_adrs_and_installs_all_sadrs(self) -> None:\n+        _quiet_install(self.target, force=False, validate=True)\n+\n+        for name, expected in self.product_bytes.items():\n+            self.assertEqual((self.adr_dir / name).read_bytes(), expected)\n+\n+        sadr_numbers = {\n+            path.name.split(\"-\", 2)[1]\n+            for path in self.adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\")\n+        }\n+        self.assertEqual(sadr_numbers, {f\"{number:04d}\" for number in range(1, 20)})\n+\n+        numeric_records = {\n+            path.name\n+            for path in self.adr_dir.glob(\"[0-9][0-9][0-9][0-9]-*.md\")\n+        }\n+        self.assertEqual(numeric_records, set(self.product_bytes))\n+        stamp = json.loads(\n+            (self.target / shiki_installer.INSTALL_STAMP_PATH).read_text(\n+                encoding=\"utf-8\"\n+            )\n+        )\n+        for path in self.adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\"):\n+            relative = path.relative_to(self.target).as_posix()\n+            self.assertEqual(stamp[\"digests\"][relative], _sha256(path))\n+        self.assertEqual(find_adr_reference_violations(self.target), [])\n+\n+\n+class LegacySadrUpgradeTests(unittest.TestCase):\n+    def setUp(self) -> None:\n+        self.tmp = Path(tempfile.mkdtemp(prefix=\"shiki-sadr-upgrade-\"))\n+        self.addCleanup(lambda: shutil.rmtree(self.tmp, ignore_errors=True))\n+        self.target = self.tmp / \"target\"\n+        self.adr_dir = self.target / \"docs\" / \"adr\"\n+        self.product_bytes, self.legacy_paths = _create_legacy_upgrade_fixture(\n+            self.target\n+        )\n+\n+    def test_force_upgrade_deletes_only_stamped_legacy_sadrs(self) -> None:\n+        _quiet_install(self.target, force=True, validate=True)\n+\n+        for relative in self.legacy_paths:\n+            self.assertFalse((self.target / relative).exists(), relative)\n+        for name, expected in self.product_bytes.items():\n+            self.assertEqual((self.adr_dir / name).read_bytes(), expected)\n+\n+        sadrs = list(self.adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\"))\n+        self.assertEqual(len(sadrs), 19)\n+        refreshed = json.loads(\n+            (self.target / shiki_installer.INSTALL_STAMP_PATH).read_text(encoding=\"utf-8\")\n+        )\n+        for relative in self.legacy_paths:\n+            self.assertNotIn(relative, refreshed[\"digests\"])\n+        for path in sadrs:\n+            relative = path.relative_to(self.target).as_posix()\n+            self.assertEqual(refreshed[\"digests\"][relative], _sha256(path))\n+\n+    def test_non_force_install_never_deletes_legacy_files(self) -> None:\n+        before = {\n+            relative: (self.target / relative).read_bytes()\n+            for relative in self.legacy_paths\n+        }\n+        _quiet_install(self.target, force=False, validate=False)\n+        for relative, expected in before.items():\n+            self.assertEqual((self.target / relative).read_bytes(), expected)\n+        refreshed = json.loads(\n+            (self.target / shiki_installer.INSTALL_STAMP_PATH).read_text(encoding=\"utf-8\")\n+        )\n+        for relative in self.legacy_paths:\n+            self.assertEqual(refreshed[\"digests\"][relative], _sha256(self.target / relative))\n+\n+\n+class LegacyCleanupPreflightTests(unittest.TestCase):\n+    def setUp(self) -> None:\n+        self.tmp = Path(tempfile.mkdtemp(prefix=\"shiki-sadr-preflight-\"))\n+        self.addCleanup(lambda: shutil.rmtree(self.tmp, ignore_errors=True))\n+\n+    def test_all_ownership_blockers_abort_before_any_write(self) -> None:\n+        cases = (\n+            (\"absent\", \"install stamp is absent\"),\n+            (\"unreadable\", \"install stamp is unreadable\"),\n+            (\"malformed\", \"install stamp is malformed\"),\n+            (\"missing-digest\", \"install stamp has no digest\"),\n+            (\"digest-mismatch\", \"digest mismatch\"),\n+        )\n+        for name, expected_reason in cases:\n+            with self.subTest(name=name):\n+                target = self.tmp / name\n+                _, legacy_paths = _create_legacy_upgrade_fixture(target)\n+                stamp_path = target / shiki_installer.INSTALL_STAMP_PATH\n+\n+                if name == \"absent\":\n+                    stamp_path.unlink()\n+                elif name == \"unreadable\":\n+                    stamp_path.unlink()\n+                    stamp_path.mkdir()\n+                elif name == \"malformed\":\n+                    stamp_path.write_text(\"{not-json\\n\", encoding=\"utf-8\")\n+                else:\n+                    stamp = json.loads(stamp_path.read_text(encoding=\"utf-8\"))\n+                    if name == \"missing-digest\":\n+                        stamp[\"digests\"] = {}\n+                    else:\n+                        stamp[\"digests\"] = {\n+                            relative: \"deadbeef\" for relative in legacy_paths\n+                        }\n+                    stamp_path.write_text(\n+                        json.dumps(stamp, indent=2, sort_keys=True) + \"\\n\",\n+                        encoding=\"utf-8\",\n+                    )\n+\n+                before = _tree_snapshot(target)\n+                with self.assertRaises(shiki_installer.ShikiError) as raised:\n+                    _quiet_install(target, force=True, validate=False)\n+                after = _tree_snapshot(target)\n+\n+                self.assertEqual(before, after, f\"{name} preflight changed the tree\")\n+                message = str(raised.exception)\n+                self.assertIn(expected_reason, message)\n+                self.assertIn(\"No files were written\", message)\n+                for relative in legacy_paths:\n+                    self.assertIn(relative, message)\n+\n+    def test_absent_stamp_is_allowed_when_no_legacy_path_exists(self) -> None:\n+        target = self.tmp / \"no-legacy\"\n+        target.mkdir()\n+        _quiet_install(target, force=True, validate=False)\n+        self.assertTrue(\n+            (target / \"docs\" / \"adr\" / \"SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\").is_file()\n+        )\n+\n+\n+if __name__ == \"__main__\":\n+    unittest.main()\n\n2026-08-07T08:29:14.229525Z ERROR codex_models_manager::manager: failed to renew cache TTL: missing field `base_instructions` at line 94 column 5\napply patch\npatch: completed\n/private/tmp/shiki-adr-collision.pa0KwY/.worktrees/shiki-t-20260807t073307221254z-2d22d654-introduc/.shiki/ledger/L-20260807T082914000000Z-7dd00019.json\n/private/tmp/shiki-adr-collision.pa0KwY/.worktrees/shiki-t-20260807t073307221254z-2d22d654-introduc/.shiki/tasks/T-20260807T073307221254Z-2d22d654.json\ndiff --git a/.codex/skills/shiki/SKILL.md b/.codex/skills/shiki/SKILL.md\nindex c8e2e85d9d75d49cc4f2d6286ee22659c4e53e75..93cded623b2c15d9f65c76491a1c2d813ac9e6c0\n--- a/.codex/skills/shiki/SKILL.md\n+++ b/.codex/skills/shiki/SKILL.md\n@@ -18,6 +18,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n Codex CLI does not register this skill as a custom `/shiki` slash command. When\n the user types `/shiki` in Codex CLI and gets \"Unrecognized command\", explain\n that this is expected: invoke Shiki in Codex with natural language such as\n@@ -69,7 +72,7 @@\n \n ## Responsibilities\n \n-- Claude Code implements and repairs by default (`shiki runner claude`, ADR 0008); Codex implements and repairs tasks explicitly assigned to `codex`.\n+- Claude Code implements and repairs by default (`shiki runner claude`, SADR-0008); Codex implements and repairs tasks explicitly assigned to `codex`.\n - Claude Code Action can act as GitHub-side CCA or reviewer.\n - CCA judges completion.\n - MergeGate authorizes state transitions and merge readiness.\ndiff --git a/.github/prompts/cca-completion-check.md b/.github/prompts/cca-completion-check.md\nindex ed203ad5f302ecc81372d8ab27d2b1232ca97497..fd89950528333ef6242e6d5443904f5392ab7e56\n--- a/.github/prompts/cca-completion-check.md\n+++ b/.github/prompts/cca-completion-check.md\n@@ -19,6 +19,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n ## Required Reading\n \n Read only the files needed to judge the PR contract. Prefer this order:\ndiff --git a/.github/prompts/codex-task-handoff.md b/.github/prompts/codex-task-handoff.md\nindex 695e211d76dea1c60e8d5bb360e78487808286a4..59441d576b01a39569722f226c854e5a740e9eb5\n--- a/.github/prompts/codex-task-handoff.md\n+++ b/.github/prompts/codex-task-handoff.md\n@@ -2,7 +2,7 @@\n \n You are Codex Front acting as the Implementer for a Shiki task.\n \n-Follow `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant ADRs, and `docs/agents/codex-handoff.md`.\n+Follow `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant Shiki SADRs and target ADRs, and `docs/agents/codex-handoff.md`.\n \n Rules:\n \ndiff --git a/.shiki/ledger/L-20260807T082800000000Z-c0de0019.json b/.shiki/ledger/L-20260807T082800000000Z-c0de0019.json\nnew file mode 100644\nindex 0000000000000000000000000000000000000000..bac355ecea44037859b3715f58bc32f3353cfcfa\n--- /dev/null\n+++ b/.shiki/ledger/L-20260807T082800000000Z-c0de0019.json\n@@ -0,0 +1,34 @@\n+{\n+  \"actor\": \"codex\",\n+  \"evidence\": [\n+    \"Full task diff reviewed against scope, non-goals, locks, acceptance checks, failure atomicity, path ownership, and compatibility requirements\",\n+    \"Finding repaired: tests/test_sadr_install_migration.py now asserts every fresh-installed SADR path has the matching SHA-256 digest in .shiki/install-stamp.json\",\n+    \"Re-review found no remaining blocking issues; rejected findings: none\",\n+    \"python3 -m unittest discover -s tests: 1147 tests passed\",\n+    \"bash scripts/test_shiki_control_plane.sh: passed\",\n+    \"bash scripts/test_shiki_adversarial_state.sh: passed\",\n+    \"python3 scripts/validate_shiki.py --target .: passed\"\n+  ],\n+  \"goal_id\": \"G-20260807T073307219752Z-1ee1ed68\",\n+  \"id\": \"L-20260807T082800000000Z-c0de0019\",\n+  \"links\": [],\n+  \"review\": {\n+    \"applied_fixes\": [\n+      \"Added direct fresh-install stamp path/digest assertions and reran focused plus full verification\"\n+    ],\n+    \"findings\": [\n+      {\n+        \"detail\": \"The fresh-install test proved SADR files were installed but did not directly prove their paths and digests were recorded in the refreshed install stamp.\",\n+        \"resolution\": \"fixed_and_re_reviewed\",\n+        \"severity\": \"blocking\",\n+        \"title\": \"Fresh-install stamp evidence was indirect\"\n+      }\n+    ],\n+    \"rejected_findings\": [],\n+    \"verdict\": \"clean_after_repairs\"\n+  },\n+  \"summary\": \"code-review implementer verdict CLEAN after re-review: findings=1; applied_fixes=1 (fresh-install stamp-digest assertion); rejected_findings=0 (none)\",\n+  \"task_id\": \"T-20260807T073307221254Z-2d22d654\",\n+  \"timestamp\": \"2026-08-07T08:28:00+00:00\",\n+  \"type\": \"review\"\n+}\ndiff --git a/.shiki/ledger/L-20260807T082914000000Z-7dd00019.json b/.shiki/ledger/L-20260807T082914000000Z-7dd00019.json\nnew file mode 100644\nindex 0000000000000000000000000000000000000000..432bcd63849c04eaaeaca3993c03a07c1152bb1a\n--- /dev/null\n+++ b/.shiki/ledger/L-20260807T082914000000Z-7dd00019.json\n@@ -0,0 +1,19 @@\n+{\n+  \"actor\": \"codex\",\n+  \"evidence\": [\n+    \"RED resolver: the target-ADR-does-not-satisfy-SADR fixture expected one missing SADR violation but received zero before namespace-aware indexing\",\n+    \"RED fresh install: the product-ADR fixture installed no SADR records before the platform rename\",\n+    \"RED forced upgrade: matching stamped legacy Shiki ADR paths remained before cleanup was implemented\",\n+    \"RED non-force upgrade: refreshed stamp omitted preserved legacy ownership evidence before matching digest carry-forward was implemented\",\n+    \"GREEN focused: 11 ADR/SADR resolver tests and 5 fresh-install/upgrade/preflight tests passed\",\n+    \"REFACTOR: separated namespace indexing, exact legacy-path inventory, read-only cleanup authorization, and stamp refresh responsibilities\",\n+    \"GREEN regression: 1147 unit tests, control-plane shell suite, adversarial shell suite, and validate_shiki passed\"\n+  ],\n+  \"goal_id\": \"G-20260807T073307219752Z-1ee1ed68\",\n+  \"id\": \"L-20260807T082914000000Z-7dd00019\",\n+  \"links\": [],\n+  \"summary\": \"tdd red-green-refactor evidence for separate ADR/SADR resolution and collision-safe install migration\",\n+  \"task_id\": \"T-20260807T073307221254Z-2d22d654\",\n+  \"timestamp\": \"2026-08-07T08:29:14+00:00\",\n+  \"type\": \"check\"\n+}\ndiff --git a/.shiki/tasks/T-20260807T073307221254Z-2d22d654.json b/.shiki/tasks/T-20260807T073307221254Z-2d22d654.json\nindex e7e150cc1637f1cfac2199e04055e93b53630dcb..00241552cac63069660eb722bd076decf6a5031d\n--- a/.shiki/tasks/T-20260807T073307221254Z-2d22d654.json\n+++ b/.shiki/tasks/T-20260807T073307221254Z-2d22d654.json\n@@ -36,7 +36,9 @@\n     \"L-20260807T080107948254Z-61f24da0\",\n     \"L-20260807T080228312210Z-07ac626e\",\n     \"L-20260807T080228575038Z-d574f629\",\n-    \"L-20260807T080256330734Z-5bf40cb6\"\n+    \"L-20260807T080256330734Z-5bf40cb6\",\n+    \"L-20260807T082800000000Z-c0de0019\",\n+    \"L-20260807T082914000000Z-7dd00019\"\n   ],\n   \"locks\": [\n     \"path:.codex/**\",\ndiff --git a/AGENTS.md b/AGENTS.md\nindex 7f041100d49c2a7525f51cb0f5d696a4a6d1c78c..a06bfbabc6fa48a007389e25e12d7ed523936bed\n--- a/AGENTS.md\n+++ b/AGENTS.md\n@@ -22,7 +22,7 @@\n - **Codex Front** is an optional implementer through Codex App, Codex CLI, Codex IDE extension, or Codex Web signed in with ChatGPT OAuth/subscription auth, dispatched with `shiki runner codex` when a task is explicitly assigned to `codex`.\n - **GitHub CCA** is implemented by Claude Code Action by default, using `CLAUDE_CODE_OAUTH_TOKEN`.\n \n-Do not assume `openai/codex-action`, `OPENAI_API_KEY`, or API-key based Codex automation in the default Shiki path. API-key runners are explicit target-repo extensions and require their own ADR.\n+Do not assume `openai/codex-action`, `OPENAI_API_KEY`, or API-key based Codex automation in the default Shiki path. API-key runners are explicit target-repo extensions and require their own target ADR.\n \n Shiki is not:\n \n@@ -44,10 +44,14 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use\n+`SADR-NNNN-*.md`; target product decisions retain `NNNN-*.md` and the `ADR`\n+namespace.\n+\n Use the canonical source-of-truth order above. Runtime-specific reading orders\n are operational guidance only and must not override or reorder it.\n \n-Conversation state is not durable truth. If a decision matters, put it in GitHub, `.shiki/`, `CONTEXT.md`, or an ADR.\n+Conversation state is not durable truth. If a decision matters, put it in GitHub, `.shiki/`, `CONTEXT.md`, a Shiki SADR, or a target ADR.\n \n If GitHub and `.shiki/` disagree, surface the conflict and prefer GitHub operational state until the mirror is repaired.\n \n@@ -75,8 +79,8 @@\n Every non-trivial change follows this loop:\n \n 1. **Goal**: clarify outcome, completion conditions, non-goals, risk level, and success signals.\n-2. **grill-with-docs**: challenge the plan against domain docs, ADRs, code reality, terminology, and edge scenarios.\n-3. **Context & Impact**: find relevant docs, ADRs, code, dependencies, owners, locks, and verification surfaces.\n+2. **grill-with-docs**: challenge the plan against domain docs, Shiki SADRs, target ADRs, code reality, terminology, and edge scenarios.\n+3. **Context & Impact**: find relevant docs, SADRs, target ADRs, code, dependencies, owners, locks, and verification surfaces.\n 4. **PRD and Spec Freeze**: use `to-prd` when context is settled enough to become durable product/engineering intent. The operator's explicit approval of the PRD is Spec Freeze: the plan records a `spec_freeze` block, and no plan runs without it. Steps 1-4 form Requirements Definition \u2014 one continuous operator dialogue. After Spec Freeze, scope changes require an operator-approved Spec Amendment; non-scope-moving interpretations go to the Assumption Log.\n 5. **Issues**: use `to-issues` to create independently grabbable vertical-slice issues. Prefer AFK slices over HITL slices where possible.\n 6. **Triage**: label issues for readiness, risk, runtime, skills, and MergeGate state.\n@@ -167,7 +171,7 @@\n - Testing decisions.\n - Out-of-scope items.\n - Further notes.\n-- Links to relevant domain terms and ADRs.\n+- Links to relevant domain terms, SADRs, and target ADRs.\n \n Every executable task must include:\n \n@@ -193,7 +197,7 @@\n \n Context & Impact must identify:\n \n-- Relevant documents, domain terms, ADRs, and past decisions.\n+- Relevant documents, domain terms, SADRs, target ADRs, and past decisions.\n - Relevant modules, interfaces, seams, callers, owners, and tests.\n - Dependency relationships between tasks.\n - Candidate locks and lock conflicts.\n@@ -210,7 +214,7 @@\n | Skill | Required when |\n | --- | --- |\n | `setup-matt-pocock-skills` | First configuring the repo for skills, or when issue tracker, triage labels, or domain docs are missing. |\n-| `grill-with-docs` | Default for non-trivial Goals before PRD/issues; always required when plans, terminology, boundaries, tradeoffs, or ADR-worthy decisions are ambiguous. |\n+| `grill-with-docs` | Default for non-trivial Goals before PRD/issues; always required when plans, terminology, boundaries, tradeoffs, or decision-record-worthy choices are ambiguous. |\n | `zoom-out` | The agent lacks an architectural map of the relevant code area. |\n | `to-prd` | Settled Goal context must become durable product/engineering intent. |\n | `to-issues` | A Goal, PRD, or plan must become independently grabbable vertical-slice issues. |\n@@ -348,7 +352,7 @@\n - Three or more new files.\n - Any high-risk or critical label.\n \n-When the architecture gate triggers, run `grill-with-docs` or `improve-codebase-architecture` as appropriate and record durable decisions in an ADR when needed.\n+When the architecture gate triggers, run `grill-with-docs` or `improve-codebase-architecture` as appropriate and record durable decisions in a Shiki SADR or target ADR as applicable.\n \n ## Engineering Discipline\n \ndiff --git a/CLAUDE.md b/CLAUDE.md\nindex 98a932e7841a632866fcd53b0bad0e3af3711d3d..98a31399404d6fb044b4fd493f4459ac87f439de\n--- a/CLAUDE.md\n+++ b/CLAUDE.md\n@@ -14,6 +14,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n ## Claude Role\n \n Default to planner, default implementer, reviewer, coordinator, documentation editor, CCA-style completion judge, and final-judgment assistant.\n@@ -57,7 +60,7 @@\n At the start of a Shiki session:\n \n 1. Read `AGENTS.md`.\n-2. Read `CONTEXT.md` and relevant ADRs.\n+2. Read `CONTEXT.md` and relevant Shiki SADRs and target ADRs.\n 3. Read active `.shiki/` Goal/task/ledger state when present.\n 4. Read active distilled rules from `.shiki/memories/` (status `distilled`, `active: true`, not revoked or superseded) for applicable guidance; the same rules are injected deterministically into task handoffs as the `## Distilled Rules` section (\u00a73.5).\n 5. Read the GitHub Issue or PR when the task is GitHub-backed.\n@@ -74,7 +77,7 @@\n - Define the Goal, completion conditions, non-goals, and risk level.\n - Treat `grill-with-docs` as the default planning entry point for non-trivial Goals.\n - Challenge domain terms against `CONTEXT.md`.\n-- Challenge hard-to-reverse tradeoffs against ADRs.\n+- Challenge hard-to-reverse tradeoffs against SADRs and target ADRs.\n - Explore code instead of asking when code can answer the question.\n - Run Context & Impact before implementation planning.\n - Produce a Task DAG, not an unordered checklist.\n@@ -94,7 +97,7 @@\n - For each question, provide your recommended answer.\n - Resolve dependency decisions before downstream design questions.\n - Update `CONTEXT.md` inline when terminology crystallizes.\n-- Offer ADRs only when the decision is hard to reverse, surprising without context, and a real tradeoff.\n+- Offer a SADR or target ADR only when the decision is hard to reverse, surprising without context, and a real tradeoff.\n - Convert unresolved questions into HITL blockers rather than allowing Codex to guess.\n \n ## PRD and Issues Standard\n@@ -103,7 +106,7 @@\n \n - Use the project's domain glossary vocabulary.\n - Avoid volatile file paths unless they are essential.\n-- Include implementation decisions, testing decisions, out-of-scope items, and relevant ADR/domain links.\n+- Include implementation decisions, testing decisions, out-of-scope items, and relevant SADR/target-ADR/domain links.\n - Identify deep-module opportunities and testable interfaces.\n \n When creating issues:\n@@ -121,7 +124,7 @@\n - Branch/worktree target.\n - Scope and non-goals.\n - Dependencies and locks.\n-- Relevant docs, ADRs, modules, and tests.\n+- Relevant docs, SADRs, target ADRs, modules, and tests.\n - Required skill invocations.\n - TDD expectations.\n - Pre-PR `code-review` gate expectations.\n@@ -152,7 +155,7 @@\n \n - Treat yourself as a judge, not implementer.\n - Do not write production code.\n-- Read `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant ADRs, PRD, task issue, PR body, diff, checks, reviews, labels, and `.shiki/` state.\n+- Read `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant SADRs and target ADRs, PRD, task issue, PR body, diff, checks, reviews, labels, and `.shiki/` state.\n - Evaluate every applicable checklist in `docs/agents/checklists.md`.\n - Map each acceptance criterion to durable evidence.\n - Distinguish implementation failure from missing evidence.\ndiff --git a/CONTEXT.md b/CONTEXT.md\nindex e61a9be06a0cce0d026484083a657bde2067e234..237ee8aa3ea8fab334ece49768bc5ed3b5af0974\n--- a/CONTEXT.md\n+++ b/CONTEXT.md\n@@ -25,7 +25,7 @@\n _Avoid_: jumping directly into code\n \n **grill-with-docs**:\n-A planning skill that challenges a plan against domain language, ADRs, code reality, and concrete edge scenarios. It resolves design-tree questions before PRD/issues. It runs inside Requirements Definition.\n+A planning skill that challenges a plan against domain language, Shiki SADRs, target ADRs, code reality, and concrete edge scenarios. It resolves design-tree questions before PRD/issues. It runs inside Requirements Definition.\n _Avoid_: generic brainstorming, silent assumptions\n \n **Requirements Definition**:\ndiff --git a/CONTRIBUTING.md b/CONTRIBUTING.md\nindex c6d9b656e73abb8a55e5f660123e03153dcb3e6d..9e37a8fbf668db5b22461aaae5548b8f1354394c\n--- a/CONTRIBUTING.md\n+++ b/CONTRIBUTING.md\n@@ -17,7 +17,7 @@\n 1. **Goal** \u2014 start from a user-approved Goal with an outcome, completion\n    conditions, non-goals, risk level, and success signals.\n 2. **grill-with-docs** \u2014 for any non-trivial change, challenge the plan against\n-   domain language, ADRs, and code reality before writing a PRD.\n+   domain language, Shiki SADRs, target ADRs, and code reality before writing a PRD.\n 3. **PRD** \u2014 capture settled product/engineering intent (`to-prd`).\n 4. **Spec Freeze** \u2014 the operator explicitly approves the PRD/requirements;\n    the plan records a `spec_freeze` block and no plan runs without it\n@@ -94,7 +94,7 @@\n   entries.\n - Do not hand-edit `.shiki/goals/*.json` or `.shiki/manifest.json` in a task PR.\n - Conversation state is not durable truth. If a decision matters, record it in\n-  GitHub, `.shiki/`, `CONTEXT.md`, or an ADR.\n+  GitHub, `.shiki/`, `CONTEXT.md`, a Shiki SADR, or a target ADR.\n \n ## Architecture Gate\n \n@@ -103,7 +103,7 @@\n or CI policy, package structure, three or more structural files, more than 100\n net new lines in a core module, three or more new files, or any high-risk label.\n When triggered, run `grill-with-docs` or `improve-codebase-architecture` and\n-record durable decisions in an ADR when needed (see `docs/adr/`).\n+record durable decisions in a Shiki SADR or target ADR as applicable (see `docs/adr/`).\n \n ## Safety\n \ndiff --git a/SYSTEM_PROMPT.md b/SYSTEM_PROMPT.md\nindex aaaea45e3a896c3a44ba89eec8bb84325ecf6c62..da009c86a88285269dee0155e58e550c6d744ea9\n--- a/SYSTEM_PROMPT.md\n+++ b/SYSTEM_PROMPT.md\n@@ -30,6 +30,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n Use the canonical source-of-truth order above. Runtime-specific reading orders\n are operational guidance only and must not override or reorder it. The current\n conversation is non-durable operator input.\n@@ -48,14 +51,14 @@\n \n A runtime may only do work that is explicitly assigned by the Goal, task, PR, or MergeGate decision.\n \n-Do not assume `openai/codex-action`, `OPENAI_API_KEY`, or API-key based Codex automation in the default Shiki path. API-key runners are explicit target-repo extensions and require their own ADR.\n+Do not assume `openai/codex-action`, `OPENAI_API_KEY`, or API-key based Codex automation in the default Shiki path. API-key runners are explicit target-repo extensions and require their own target ADR.\n \n ## 4. Non-negotiable Shiki flow\n \n For any non-trivial Goal, use this flow:\n \n 1. **Goal Seek**: define outcome, non-goals, risk level, completion criteria, and evidence requirements.\n-2. **grill-with-docs**: challenge the plan against `CONTEXT.md`, ADRs, code reality, terminology, boundaries, and edge scenarios. Ask one question at a time when the operator is available. If code can answer a question, inspect code instead of asking.\n+2. **grill-with-docs**: challenge the plan against `CONTEXT.md`, Shiki SADRs, target ADRs, code reality, terminology, boundaries, and edge scenarios. Ask one question at a time when the operator is available. If code can answer a question, inspect code instead of asking.\n 3. **Context & Impact**: identify modules, interfaces, seams, callers, dependencies, locks, risk, and verification surfaces.\n 4. **to-prd + Spec Freeze**: turn settled context into a PRD using domain vocabulary and testing decisions. The operator's explicit approval of the PRD is Spec Freeze, recorded as the plan's `spec_freeze` block; plans without it do not run. Steps 1-4 form Requirements Definition.\n 5. **to-issues**: decompose the PRD into independently grabbable vertical-slice issues. Prefer AFK slices; mark HITL slices when judgment is still required.\n@@ -75,7 +78,7 @@\n Before making any material change:\n \n 1. Read `AGENTS.md` and any runtime-specific wrapper such as `CLAUDE.md`.\n-2. Read `CONTEXT.md` and relevant ADRs.\n+2. Read `CONTEXT.md` and relevant SADRs and target ADRs.\n 3. Read the GitHub Issue / PR and `.shiki/` state for the active Goal or task.\n 4. Identify your role: Planner, Implementer, Reviewer, Completion Check Agent, Repairer, or Guardian-assist.\n 5. Identify Goal id, task id, branch, PR, dependencies, locks, risk level, required skills, and required evidence.\n@@ -103,7 +106,7 @@\n \n At minimum, identify:\n \n-- Relevant documents, ADRs, domain terms, and prior decisions.\n+- Relevant documents, SADRs, target ADRs, domain terms, and prior decisions.\n - Relevant modules, interfaces, seams, callers, owners, and tests.\n - Dependency relationships between tasks.\n - Candidate file locks and lock conflicts.\n@@ -152,7 +155,7 @@\n \n CCA must read:\n \n-- `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant ADRs.\n+- `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant SADRs and target ADRs.\n - Parent Goal issue, PRD issue, task issue, comments, and labels.\n - PR body, diff, commits, changed files, reviews, CI checks, and artifacts available to the workflow.\n - `.shiki/` task contract, locks, ledger entries, CCA checklist profile, and prior repair packets.\n@@ -214,7 +217,7 @@\n - Three or more new files.\n - Any high-risk or critical label.\n \n-When the architecture gate triggers, run `grill-with-docs` or `improve-codebase-architecture` as appropriate and record the decision in an ADR if it is hard to reverse or likely to be re-litigated.\n+When the architecture gate triggers, run `grill-with-docs` or `improve-codebase-architecture` as appropriate and record the decision in a Shiki SADR or target ADR as applicable if it is hard to reverse or likely to be re-litigated.\n \n ## 14. Skill Gate\n \n@@ -223,7 +226,7 @@\n Use these skills:\n \n - `setup-matt-pocock-skills`: before first use of the engineering skill set, or when issue tracker, triage labels, or domain docs are missing.\n-- `grill-with-docs`: default for non-trivial Goals before PRD/issues; required for ambiguous plans, terminology, boundaries, ADR-worthy tradeoffs, or design-tree decisions.\n+- `grill-with-docs`: default for non-trivial Goals before PRD/issues; required for ambiguous plans, terminology, boundaries, decision-record-worthy tradeoffs, or design-tree decisions.\n - `zoom-out`: unfamiliar code area or missing architectural map.\n - `to-prd`: convert settled context into a PRD.\n - `to-issues`: break a PRD, plan, or Goal into independently grabbable vertical-slice issues.\ndiff --git a/docs/adr/0001-keep-shiki-as-platform-name.md b/docs/adr/SADR-0001-keep-shiki-as-platform-name.md\nindex 20a9637c3b9d8c97de7ba8db7cfae4d82d9a7dba..8b685439959aada1b221c112f0ac3c51ca8a1695\n--- a/docs/adr/0001-keep-shiki-as-platform-name.md\n+++ b/docs/adr/SADR-0001-keep-shiki-as-platform-name.md\n@@ -1,3 +1,3 @@\n-# Keep Shiki As The Platform Name\n+# SADR-0001: Keep Shiki As The Platform Name\n \n Shiki remains the product name for the GitHub-native agentic engineering control plane. We considered more descriptive names such as MergePilot, MergeGate, and Kiroku, but kept Shiki because it carries the existing goal-seeking framework identity while remaining independent of Hermes, Codex, Claude Code, or any single runtime.\ndiff --git a/docs/adr/0002-use-agents-as-shared-constitution.md b/docs/adr/SADR-0002-use-agents-as-shared-constitution.md\nindex 73de30ecb078120eac540d9bc51855e3cd48d0e3..c5486182bf06fca36ea508496b013c67302fd989\n--- a/docs/adr/0002-use-agents-as-shared-constitution.md\n+++ b/docs/adr/SADR-0002-use-agents-as-shared-constitution.md\n@@ -1,3 +1,3 @@\n-# Use AGENTS As The Shared Agent Constitution\n+# SADR-0002: Use AGENTS As The Shared Agent Constitution\n \n Shiki uses `AGENTS.md` as the runtime-neutral constitution and `CLAUDE.md` as a thin Claude Code wrapper. This keeps Codex, Claude Code, Hermes Runner, and future Agent Runtimes aligned on the same GitHub-first MergeGate rules while still allowing runtime-specific instructions where needed.\ndiff --git a/docs/adr/0003-require-skill-gate-for-mergegate-readiness.md b/docs/adr/SADR-0003-require-skill-gate-for-mergegate-readiness.md\nindex 424be0c9af4c8f0136f1fde68a468c47ff84e764..7fc337987c7df9792f75f181dfcbed75fe5ffff2\n--- a/docs/adr/0003-require-skill-gate-for-mergegate-readiness.md\n+++ b/docs/adr/SADR-0003-require-skill-gate-for-mergegate-readiness.md\n@@ -1,3 +1,3 @@\n-# Require Skill Gate For MergeGate Readiness\n+# SADR-0003: Require Skill Gate For MergeGate Readiness\n \n Shiki requires relevant engineering skills to be invoked before execution when their triggers apply. This makes skills part of MergeGate readiness rather than optional prompt style, because Shiki depends on repeatable planning, issue decomposition, TDD, diagnosis, triage, and architecture review before autonomous or parallel agent work can be trusted.\ndiff --git a/docs/adr/0004-use-github-first-with-shiki-mirror.md b/docs/adr/SADR-0004-use-github-first-with-shiki-mirror.md\nindex d0a3a9174dc646a945a9d325954ffaf00c352040..a79c54ae9ee8eed2a0524192b864da4e22a92d3f\n--- a/docs/adr/0004-use-github-first-with-shiki-mirror.md\n+++ b/docs/adr/SADR-0004-use-github-first-with-shiki-mirror.md\n@@ -1,4 +1,4 @@\n-# ADR 0004: Use GitHub First With A Repository-Local Shiki Mirror\n+# SADR-0004: Use GitHub First With A Repository-Local Shiki Mirror\n \n ## Status\n \ndiff --git a/docs/adr/0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md b/docs/adr/SADR-0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\nindex b339baf49b89d9290e79c3d51b36e7b2bde3bd95..0f3c3eb9afccdb3403ce978e21d04fe12e495da0\n--- a/docs/adr/0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\n+++ b/docs/adr/SADR-0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\n@@ -1,8 +1,8 @@\n-# ADR 0005: Use Codex As Front And Claude Code Action As The GitHub Runtime\n+# SADR-0005: Use Codex As Front And Claude Code Action As The GitHub Runtime\n \n ## Status\n \n-Accepted (implementer default superseded by ADR 0008; the auth model and the\n+Accepted (implementer default superseded by SADR-0008; the auth model and the\n Claude Code Action GitHub runtime decision remain in force)\n \n ## Context\n@@ -26,4 +26,4 @@\n - Shiki avoids requiring `OPENAI_API_KEY` for the default workflow.\n - Codex work happens through the user's authenticated Codex front surface, not as a hidden backend Action.\n - Claude Code Action is configured for OAuth-token based subscription usage by default.\n-- API-key based automation remains possible only as an explicit target-repo extension with its own ADR.\n+- API-key based automation remains possible only as an explicit target-repo extension with its own target ADR.\ndiff --git a/docs/adr/0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md b/docs/adr/SADR-0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\nindex 4bcae9d1e52f0a37b8e9fbafa5cd14b6ee1993bd..7a38538a6ae9929567f0ade67e7ea6a60d0b3f7e\n--- a/docs/adr/0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\n+++ b/docs/adr/SADR-0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\n@@ -1,4 +1,4 @@\n-# ADR 0006: Separate Codex Implementation, CCA Completion, And MergeGate Authorization\n+# SADR-0006: Separate Codex Implementation, CCA Completion, And MergeGate Authorization\n \n ## Status\n \ndiff --git a/docs/adr/0007-packaging-and-release.md b/docs/adr/SADR-0007-packaging-and-release.md\nindex 79b84ddb97167372b44641e5b7860e400ce80822..bb445a2e2e856d9d1f5fdbe35de8d4e8cec0fb32\n--- a/docs/adr/0007-packaging-and-release.md\n+++ b/docs/adr/SADR-0007-packaging-and-release.md\n@@ -1,4 +1,4 @@\n-# ADR 0007: Packaging And Release For Shiki\n+# SADR-0007: Packaging And Release For Shiki\n \n ## Status\n \n@@ -25,7 +25,7 @@\n   not introduce third-party runtime dependencies or break the shim.\n - Releases must run inside the operator's subscription-authenticated toolchain\n   and must not require extra secrets beyond the built-in `GITHUB_TOKEN`\n-  (consistent with ADR 0005's no-API-key-by-default stance).\n+  (consistent with SADR-0005's no-API-key-by-default stance).\n - `.shiki/manifest.json` already encodes the install surface (`install.include`,\n   `install.create_directories`, `install.exclude_from_commit`), and it must not\n   be mutated as part of this change.\n@@ -34,7 +34,7 @@\n \n This is a hard-to-reverse platform decision (it sets the version source of\n truth, the release trigger, and the public install contract), so it is recorded\n-as an ADR.\n+as a SADR.\n \n ## Decision\n \n@@ -104,7 +104,7 @@\n 3. **Publishing to PyPI.** Rejected for now: Shiki is distributed as a\n    repository template + git-installable CLI, not a PyPI library; PyPI would add\n    credentials/secrets and a different versioning contract. Can be added later as\n-   its own ADR if a packaged distribution is needed.\n+   its own SADR if a packaged distribution is needed.\n 4. **Dynamic version (read `VERSION` via a build backend `dynamic` field).**\n    Rejected to keep `pyproject.toml` backend-agnostic and dependency-free; the\n    small duplication between `VERSION` and `pyproject.toml` is guarded by the\n@@ -123,4 +123,4 @@\n   releasing versions the whole platform but ships only `install.include` to\n   targets.\n - Future PyPI distribution or a console entry point are deferred and would each\n-  warrant their own ADR.\n+  warrant their own SADR.\ndiff --git a/docs/adr/0008-use-claude-code-as-default-implementer.md b/docs/adr/SADR-0008-use-claude-code-as-default-implementer.md\nindex dedf4510b7c7799521422f622029563d373db031..35c2625d908167fbc58855b5b63e283805f167e8\n--- a/docs/adr/0008-use-claude-code-as-default-implementer.md\n+++ b/docs/adr/SADR-0008-use-claude-code-as-default-implementer.md\n@@ -1,12 +1,12 @@\n-# ADR 0008: Use Claude Code As The Default Implementer Runtime\n+# SADR-0008: Use Claude Code As The Default Implementer Runtime\n \n ## Status\n \n-Proposed (supersedes ADR 0005 for the implementer default; keeps its auth model)\n+Proposed (supersedes SADR-0005 for the implementer default; keeps its auth model)\n \n ## Context\n \n-ADR 0005 made Codex Front the default implementation runtime, with Claude Code\n+SADR-0005 made Codex Front the default implementation runtime, with Claude Code\n restricted to planner and reviewer roles by the runtime registry, and the only\n autonomous implementation adapter was `shiki runner codex`.\n \n@@ -18,13 +18,13 @@\n - The platform's planning, review, and CCA layers already run on Claude Code,\n   and Claude Code's native capabilities (the built-in `/code-review` skill and\n   the Workflow multi-agent orchestration tool) have no Codex equivalent.\n-- The Goal lifecycle redesign (ADR 0009) requires an autonomous post-freeze\n+- The Goal lifecycle redesign (SADR-0009) requires an autonomous post-freeze\n   loop driven from the operator's Claude Code session, including bounded\n   repair dispatch, which must not depend on a runtime that cannot be invoked\n   unattended.\n \n The constitution claims Shiki is runtime-agnostic (\"not a Claude-only\n-workflow\", ADR 0002). Any change to the implementer default must preserve the\n+workflow\", SADR-0002). Any change to the implementer default must preserve the\n registry-based runtime model rather than hardcoding one vendor.\n \n ## Decision\n@@ -39,7 +39,7 @@\n   the task to review or repair-needed.\n - Keep Codex registered as an optional implementer runtime. `shiki runner\n   codex`, the Codex handoff templates, and the ChatGPT OAuth auth model from\n-  ADR 0005 remain supported for tasks explicitly assigned to `codex`.\n+  SADR-0005 remain supported for tasks explicitly assigned to `codex`.\n - Commit to mandating two Claude-native capabilities as loop contract in\n   follow-up Goals (status: the code-review gate was delivered by Goal G-B \u2014\n   skill registry, checklists item PR-12, default required_skills \u2014 while the\n@@ -60,7 +60,7 @@\n \n Out of scope: removing Codex support, changing the CCA runtime (Claude Code\n Action with `CLAUDE_CODE_OAUTH_TOKEN` stays), and API-key based automation\n-(still requires its own ADR per ADR 0005).\n+(still requires its own target ADR per SADR-0005).\n \n ## Consequences\n \n@@ -100,7 +100,7 @@\n   harness rejection of direct `codex exec` is outside Shiki's control, and the\n   plan's mandatory native capabilities cannot run on Codex at all.\n - **Remove Codex entirely**: rejected; it abandons the runtime-agnostic\n-  constitution (ADR 0002) for no operational gain and forecloses\n+  constitution (SADR-0002) for no operational gain and forecloses\n   cross-runtime adversarial review.\n - **GitHub Actions cloud implementation via claude-code-action**: rejected as\n   default; durable evidence is automatic but token cost, Node 24 workflow\ndiff --git a/docs/adr/0009-requirements-definition-and-spec-freeze.md b/docs/adr/SADR-0009-requirements-definition-and-spec-freeze.md\nindex 29b3f7871bef9203a8b4a831a65c39eb2f506e42..52e7dd69ad0aa4efe0b68c62678abbf7df02aaec\n--- a/docs/adr/0009-requirements-definition-and-spec-freeze.md\n+++ b/docs/adr/SADR-0009-requirements-definition-and-spec-freeze.md\n@@ -1,4 +1,4 @@\n-# ADR 0009: Merge Grill And Planning Into Requirements Definition With Spec Freeze\n+# SADR-0009: Merge Grill And Planning Into Requirements Definition With Spec Freeze\n \n ## Status\n \ndiff --git a/docs/adr/0010-external-ai-guardian-review.md b/docs/adr/SADR-0010-external-ai-guardian-review.md\nindex e6108bbe8bb4e73e0ff4684c8f78001a80438c4e..f3510b145664298adafb151b6f0db51b972f01ec\n--- a/docs/adr/0010-external-ai-guardian-review.md\n+++ b/docs/adr/SADR-0010-external-ai-guardian-review.md\n@@ -1,4 +1,4 @@\n-# ADR 0010: External AI Guardian Review As A First-Class Approval Authority\n+# SADR-0010: External AI Guardian Review As A First-Class Approval Authority\n \n ## Status\n \n@@ -8,7 +8,7 @@\n \n Shiki is intended to be an autonomous, recursive, self-improving agent\n framework: authority-in-the-loop, not human-in-the-loop. Until now the Guardian\n-approval required for high/critical-risk merges (guardian-policy.json, ADR\n+approval required for high/critical-risk merges (guardian-policy.json, SADR\n governance) recognized only human authority sources \u2014 a GitHub review approval,\n the `guardian:approved` label applied by a configured Guardian, or a comment\n carrying `Guardian approval granted` plus the current head SHA.\n@@ -89,7 +89,7 @@\n   autonomous design the operator intends and produced the impersonation\n   incident.\n - **Record AI approval as an operator comment**: rejected as forgery of human\n-  authority \u2014 the exact failure this ADR corrects.\n+  authority \u2014 the exact failure this SADR corrects.\n - **Deliver the artifact as a committed `.shiki/` file**: rejected; a committed\n   approval inside the PR being approved is forgeable by anyone who can push.\n - **A dedicated reviewer bot identity instead of a relayed comment**: deferred;\ndiff --git a/docs/adr/0011-autonomous-quality-gate-evidence.md b/docs/adr/SADR-0011-autonomous-quality-gate-evidence.md\nindex bca0246c5d46e44afbde15486a75eec425fc02f5..9c93eac16d8c3a6d4b11bd7224e3955102cd7db4\n--- a/docs/adr/0011-autonomous-quality-gate-evidence.md\n+++ b/docs/adr/SADR-0011-autonomous-quality-gate-evidence.md\n@@ -1,4 +1,4 @@\n-# ADR 0011: Autonomous Quality-Gate Evidence \u2014 Deterministic Facts And An Independent Verifier\n+# SADR-0011: Autonomous Quality-Gate Evidence \u2014 Deterministic Facts And An Independent Verifier\n \n ## Status\n \ndiff --git a/docs/adr/0012-autonomous-loop-closeout-to-main.md b/docs/adr/SADR-0012-autonomous-loop-closeout-to-main.md\nindex 637fad6dde4989c9c2bf0f81f676d8d29991123c..ec7d85f81030a676bcbba28b43eb365422a36eac\n--- a/docs/adr/0012-autonomous-loop-closeout-to-main.md\n+++ b/docs/adr/SADR-0012-autonomous-loop-closeout-to-main.md\n@@ -1,4 +1,4 @@\n-# 0012 \u2014 Autonomous loop pushes goal completion to main via a closeout PR\n+# SADR-0012 \u2014 Autonomous loop pushes goal completion to main via a closeout PR\n \n - Status: accepted\n - Date: 2026-06-18\ndiff --git a/docs/adr/0013-bootstrap-grants-actions-approve-pull-requests.md b/docs/adr/SADR-0013-bootstrap-grants-actions-approve-pull-requests.md\nindex b6c69d64f1c5ced346b88d6859586715b3b5bd0e..cf48ff4a6716267f539ca58edd1a852e7c7a2d64\n--- a/docs/adr/0013-bootstrap-grants-actions-approve-pull-requests.md\n+++ b/docs/adr/SADR-0013-bootstrap-grants-actions-approve-pull-requests.md\n@@ -1,4 +1,4 @@\n-# ADR 0013: Bootstrap Grants GitHub Actions Permission To Create And Approve Pull Requests\n+# SADR-0013: Bootstrap Grants GitHub Actions Permission To Create And Approve Pull Requests\n \n ## Status\n \n@@ -31,7 +31,7 @@\n security tradeoff: it lets repository automation satisfy a human-review gate. In\n a multi-maintainer repo that could let a compromised or buggy workflow\n self-approve changes. So whether bootstrap turns it on by default \u2014 and with\n-what default token scope \u2014 is an ADR-worthy decision, not an implementation\n+what default token scope \u2014 is a SADR-worthy decision, not an implementation\n detail.\n \n ## Decision\n@@ -48,7 +48,7 @@\n Specifically:\n \n - **`can_approve_pull_request_reviews` defaults to `true`.** Shiki's default\n-  posture is solo/AFK autonomous operation (ADR 0008/0009/0012), where the CCA\n+  posture is solo/AFK autonomous operation (SADR-0008/0009/0012), where the CCA\n   Review Bridge is the only path that can satisfy `required_review: true` after\n   the CCA and Guardian gates have already passed. The default makes a\n   freshly-bootstrapped repo able to run the autonomous loop end to end without a\ndiff --git a/docs/adr/0014-codex-app-external-ai-guardian-ui-adapter.md b/docs/adr/SADR-0014-codex-app-external-ai-guardian-ui-adapter.md\nindex 238db06279334caa341c117362df9271382a7fcd..65f12e9939321e1b3e389a34c822fc1549e5155d\n--- a/docs/adr/0014-codex-app-external-ai-guardian-ui-adapter.md\n+++ b/docs/adr/SADR-0014-codex-app-external-ai-guardian-ui-adapter.md\n@@ -1,4 +1,4 @@\n-# ADR 0014: Use Codex App As The External AI Guardian UI Adapter\n+# SADR-0014: Use Codex App As The External AI Guardian UI Adapter\n \n ## Status\n \ndiff --git a/docs/adr/0015-contract-approval-before-dispatch.md b/docs/adr/SADR-0015-contract-approval-before-dispatch.md\nindex fad6c656741a20a7684ba16080a7db159f63a52f..6061271a46d3f1d6ae7174477de3fb75673123a6\n--- a/docs/adr/0015-contract-approval-before-dispatch.md\n+++ b/docs/adr/SADR-0015-contract-approval-before-dispatch.md\n@@ -1,4 +1,4 @@\n-# ADR 0015: Approve The Task Contract Before Dispatch, Not The Diff After It\n+# SADR-0015: Approve The Task Contract Before Dispatch, Not The Diff After It\n \n ## Status\n \n@@ -9,7 +9,7 @@\n Guardian approval for high/critical risk is expressible only on a pull request.\n Every approval source in `guardian-policy.json` \u2014 GitHub review, the\n `guardian:approved` label, the `Guardian approval granted` comment, and the\n-`external_ai_guardian_review` artifact (ADR 0010/0014) \u2014 is bound to a PR, and\n+`external_ai_guardian_review` artifact (SADR-0010/0014) \u2014 is bound to a PR, and\n three of the four require the current head SHA. Approval therefore cannot exist\n before an implementation PR exists.\n \n@@ -81,7 +81,7 @@\n An envelope breach must surface only on the non-repairable policy gate, never on\n the repairable `MergeGate metadata check`. Otherwise the Goal Loop routes it to\n `dispatch_repair` \u2014 instructing a runner to make the breach go away \u2014 which is\n-exactly what this ADR forbids.\n+exactly what this SADR forbids.\n \n **Contract immutability.** After registration, an implementation PR must not\n change the governance fields of its own task contract (`scope`, `non_goals`,\n@@ -116,7 +116,7 @@\n fails any verdict that is not `complete`, and the signal the CCA prompt reads is\n computed from `evaluate_guardian_approval` alone. Without the OR-merge at the\n signal, a contract-approved PR still returns `needs_guardian`, the CCA job fails,\n-and the MergeGate policy check never runs \u2014 the gate this ADR changes would never\n+and the MergeGate policy check never runs \u2014 the gate this SADR changes would never\n be reached. The CCA *prompt* needs no change: a `contract_approval` entry in the\n signal's existing `sources` array satisfies its current wording.\n \n@@ -130,7 +130,7 @@\n written, and an approved contract lets its implementation PRs merge through the\n same automatic path as low/medium risk. The wasted implement-then-block cycle\n disappears. Guardian review shifts from reading diffs to reading contracts, which\n-is both earlier and smaller; the external AI Guardian path (ADR 0010/0014) works\n+is both earlier and smaller; the external AI Guardian path (SADR-0010/0014) works\n on contracts with no change.\n \n We accept that the Guardian authorizes a class of change without seeing its\n@@ -146,7 +146,7 @@\n adversarial tests for the new approval path equivalent in rigor to\n `test_shiki_governance_evidence.sh`.\n \n-Bootstrap costs, which apply only while this ADR is being implemented. The tasks\n+Bootstrap costs, which apply only while this SADR is being implemented. The tasks\n that build Contract Approval merge under the existing rules, so the ones\n dispatched after the pre-dispatch gate lands but before the approval path lands\n need a Guardian-approved Contract PR *and* a live Guardian approval on their\n@@ -210,7 +210,7 @@\n risk; they do not bound authorized work.\n \n **Auto-escalate risk on envelope breach instead of stopping.** Returns the\n-workflow to a diff-time Guardian decision \u2014 the exact behavior this ADR removes.\n+workflow to a diff-time Guardian decision \u2014 the exact behavior this SADR removes.\n \n **Resolve governance fields from the base snapshot only.** Rejected against the\n registration flow: implementation PRs add their own contract, so the base has no\ndiff --git a/docs/adr/0016-locks-bound-product-paths.md b/docs/adr/SADR-0016-locks-bound-product-paths.md\nindex b5e850f3f1f7f4ef6e7f9d1763a29a51bd300c45..46b40a9b99a7aae644c02bb458f5473ff0a90ce2\n--- a/docs/adr/0016-locks-bound-product-paths.md\n+++ b/docs/adr/SADR-0016-locks-bound-product-paths.md\n@@ -1,4 +1,4 @@\n-# ADR 0016: Locks bound product paths; the Shiki mirror is bound by record identity\n+# SADR-0016: Locks bound product paths; the Shiki mirror is bound by record identity\n \n ## Status\n \n@@ -6,8 +6,8 @@\n \n - Date: 2026-07-28\n - Deciders: operator (mizutani-140), Claude (planner)\n-- Related: ADR 0004 (GitHub-first with a `.shiki` mirror), ADR 0009 (Spec Freeze),\n-  ADR 0012 (autonomous loop closeout to main), ADR 0015 (Contract Approval before\n+- Related: SADR-0004 (GitHub-first with a `.shiki` mirror), SADR-0009 (Spec Freeze),\n+  SADR-0012 (autonomous loop closeout to main), SADR-0015 (Contract Approval before\n   dispatch), remediation goal task R-02, the live PR #179 deadlock.\n \n ## Context\n@@ -90,11 +90,11 @@\n files, documentation files and named source modules are nameable; runtime-\n allocated ledger, runner and report ids are not \u2014 and those live in `.shiki`,\n which locks no longer govern. Existing frozen contracts are brought into line by\n-a one-time Spec Amendment per plan, which must land before ADR 0015's contract\n+a one-time Spec Amendment per plan, which must land before SADR-0015's contract\n immutability makes `locks` unwritable from the implementation PR.\n \n **Lock state is resolved from the durable side.** MergeGate resolves the lock\n-registry, and ADR 0015's governance fields, from the base `.shiki` snapshot when\n+registry, and SADR-0015's governance fields, from the base `.shiki` snapshot when\n present, falling back to head only when absent. The `MergeGate metadata check`\n job is given `--base-shiki`, which it does not receive today. Locks are not\n derived from the live open-pull-request set: a pull request body is untrusted\n@@ -108,7 +108,7 @@\n Explicitly out of scope: moving MergeGate's contract resolution off the PR\n checkout wholesale; making the mirror a projection of GitHub state; introducing\n new lock kinds; moving ledger evidence out of the repository; and unwinding\n-ADR 0012's closeout pull request. Registration of multi-task goals is ADR 0015's\n+SADR-0012's closeout pull request. Registration of multi-task goals is SADR-0015's\n Contract PR and is not re-decided here.\n \n ## Consequences\n@@ -127,7 +127,7 @@\n \n Risks accepted. First, goal-scoped identity is only as strong as `goal_id`,\n which is resolved from the PR body and the head task file; the mitigation is to\n-add `goal_id` to ADR 0015's frozen governance field set, and until that lands a\n+add `goal_id` to SADR-0015's frozen governance field set, and until that lands a\n task file can name a foreign goal. Second, base-when-present resolution is\n unavailable on fork pull requests, where the head is contributor-controlled;\n fork pull requests already cannot complete a Shiki merge, and this decision does\n@@ -139,7 +139,7 @@\n path, not by implementation pull requests.\n \n New obligations: one Spec Amendment per frozen plan to rewrite `locks`, ordered\n-before ADR 0015 T1; extension of the adversarial suites in\n+before SADR-0015 T1; extension of the adversarial suites in\n `scripts/test_shiki_adversarial_state.sh` and\n `scripts/test_shiki_governance_evidence.sh` to cover the eleven newly-scoped\n directories; and updating `scripts/test_shiki_loop_lock_guard.sh`,\n@@ -151,32 +151,32 @@\n whether the loop's transitive sync of ledger `evidence` references can be\n deleted once the Contract PR carries its own source plan.\n \n-## Relationship to ADR 0004\n+## Relationship to SADR-0004\n \n-This ADR **clarifies something ADR 0004 left unspecified**. ADR 0004 decides that\n+This SADR **clarifies something SADR-0004 left unspecified**. SADR-0004 decides that\n GitHub is the operational source of truth and that each target repository keeps a\n `.shiki/` mirror. It says nothing about pull requests writing the mirror, does\n not name MergeGate, and does not make the mirror an input to a gate. Nothing in\n-ADR 0004's Decision or Consequences is amended, superseded or weakened: the\n+SADR-0004's Decision or Consequences is amended, superseded or weakened: the\n mirror still exists, still records the same artifacts, and pull request\n-boundaries remain the coordination surface. What this ADR settles is the question\n+boundaries remain the coordination surface. What this SADR settles is the question\n 0004 never reached \u2014 by what authority a pull request may write a mirror record \u2014\n and it answers it the way `.shiki/manifest.json` already declares\n (`pr_mutation: current-task-or-current-goal-only`).\n \n-It **amends ADR 0012**, whose Decision item 5 states that \"Loop-executed tasks\n+It **amends SADR-0012**, whose Decision item 5 states that \"Loop-executed tasks\n declare the `path:.shiki/**` lock, which covers every `.shiki` file the closeout\n stages ... so `files_outside_locks` is satisfied without per-file lock\n bookkeeping.\" That clause is replaced: the closeout's mirror writes are\n-authorized by record identity, not by a declared lock. ADR 0012's substantive\n+authorized by record identity, not by a declared lock. SADR-0012's substantive\n decision \u2014 that completion reaches `main` through a closeout pull request rather\n than local attestation \u2014 is preserved unchanged.\n \n-It is **complementary to ADR 0015**, not a substitute. ADR 0015 owns contract\n+It is **complementary to SADR-0015**, not a substitute. SADR-0015 owns contract\n immutability, the pre-dispatch gate, the Contract PR and the Scope Envelope. This\n-ADR owns what a lock means and who may write the mirror. The one ordering\n+SADR owns what a lock means and who may write the mirror. The one ordering\n constraint between them is that the lock-narrowing Spec Amendment must precede\n-ADR 0015 T1's freezing of the `locks` field.\n+SADR-0015 T1's freezing of the `locks` field.\n \n ## Alternatives Considered\n \n@@ -186,7 +186,7 @@\n the only writer that can register them may run. Migrating the Guardian signal to\n that store makes every registration PR fail closed to `needs_guardian` at every\n risk level, because the task file is by definition absent from the store, which\n-removes the low/medium autonomous path ADR 0008/0009 exists to provide. And it\n+removes the low/medium autonomous path SADR-0008/0009 exists to provide. And it\n leaves schemas, manifest, config, guardian policy, templates, migrations, plans,\n worktrees, runner records, runs and handoffs with no writer at all in a\n repository whose product is `.shiki`. Measured benefit on the frozen backlog:\n@@ -199,14 +199,14 @@\n namespace is not in fact ownership-partitioned \u2014 56 ledger entries carry no\n `task_id`, and one goal DAG is writable by 35 registered tasks. The correct\n expression of its insight is that locks do not govern the mirror at all, which is\n-what this ADR decides.\n+what this SADR decides.\n \n **Make the mirror a projection written only by a registration writer and a\n post-merge projection job.** Rejected. It requires a bypass actor with write\n access to the protected default branch; it makes its own master invariant\n readable from a manifest that pull requests may edit; it deletes both reconcile\n modes and leaves a corrupted mirror with no in-band repair path, contradicting\n-ADR 0004's consequence that mirror conflicts are repaired against GitHub state;\n+SADR-0004's consequence that mirror conflicts are repaired against GitHub state;\n and its content-derived ids are rejected by `validate_shiki`'s currently-green id\n pattern.\n \ndiff --git a/docs/adr/0017-bookkeeping-closeouts-do-not-inherit-risk.md b/docs/adr/SADR-0017-bookkeeping-closeouts-do-not-inherit-risk.md\nindex 554e3a0d526a72ca067108c5464cc68c3edcf174..b9676857a4713850e063af7c39c8e0b01ecb4c66\n--- a/docs/adr/0017-bookkeeping-closeouts-do-not-inherit-risk.md\n+++ b/docs/adr/SADR-0017-bookkeeping-closeouts-do-not-inherit-risk.md\n@@ -1,4 +1,4 @@\n-# ADR 0017: A Bookkeeping Closeout Does Not Inherit Its Task's Risk\n+# SADR-0017: A Bookkeeping Closeout Does Not Inherit Its Task's Risk\n \n ## Status\n \n@@ -6,7 +6,7 @@\n \n ## Context\n \n-ADR 0012 made the goal loop push a task's completion to the default branch through a\n+SADR-0012 made the goal loop push a task's completion to the default branch through a\n closeout pull request: after the implementation PR merges, the loop opens a second PR\n carrying `task.status = done`, `lock.state = released`, and \u2014 when the task completes\n its goal \u2014 `goal.status = complete` plus one scorecard report. Nothing else. The\n@@ -49,14 +49,14 @@\n 4. The task file's governance fields (`scope`, `non_goals`, `required_skills`,\n    `risk_level`, `locks`, `acceptance_checks`, `test_command`) are byte-identical to\n    the base snapshot. Only `status`, `expected_pr`, `closeout_pr`, `ledger_evidence`\n-   and `expected_branch` may differ \u2014 the ADR 0015 contract-immutability rule,\n+   and `expected_branch` may differ \u2014 the SADR-0015 contract-immutability rule,\n    unchanged.\n \n    `expected_branch` is in that set because a closeout is *by construction* on a\n    different branch from the implementation it closes, and the loop rewrites the\n    field accordingly. It is safe to let it differ because MergeGate independently\n    requires `expected_branch` to equal the PR's actual head ref, so a PR cannot set\n-   it to anything other than the branch it is really on. The first draft of this ADR\n+   it to anything other than the branch it is really on. The first draft of this SADR\n    omitted it, which made the exemption unreachable: every real closeout failed\n    condition 4 on a field the loop always changes. See PR #209.\n 5. The state transitions are exactly the terminal set: task `review` \u2192 `done`, lock\n@@ -113,12 +113,12 @@\n classification must be derived, not asserted.\n \n **Fold the closeout into the implementation PR** so no second PR exists. Rejected under\n-ADR 0012, which recorded why: a task file that is already `done` on the implementation\n+SADR-0012, which recorded why: a task file that is already `done` on the implementation\n branch short-circuits the loop's review \u2192 CCA \u2192 merge \u2192 done ordering into a local\n goal-completion before any merge happens.\n \n **Have the loop auto-merge closeouts with elevated credentials.** Rejected outright: that\n-is a runtime authoring its own approval, which ADR 0010 and the constitution's\n+is a runtime authoring its own approval, which SADR-0010 and the constitution's\n approval-record prohibition forbid. It would also be indistinguishable, in the evidence,\n from the forgery those rules exist to prevent.\n \ndiff --git a/docs/adr/0018-carried-guardian-approval-across-a-proven-base-sync.md b/docs/adr/SADR-0018-carried-guardian-approval-across-a-proven-base-sync.md\nindex 22358562da390ed8b45cd600da08a43d378b4680..e6e2907cefa776460af73ddf7d39db605f3c39b0\n--- a/docs/adr/0018-carried-guardian-approval-across-a-proven-base-sync.md\n+++ b/docs/adr/SADR-0018-carried-guardian-approval-across-a-proven-base-sync.md\n@@ -1,4 +1,4 @@\n-# ADR 0018: Carried Guardian Approval Across A Proven Base Sync\n+# SADR-0018: Carried Guardian Approval Across A Proven Base Sync\n \n ## Status\n \n@@ -34,7 +34,7 @@\n Guardian evaluator that consumes it (`shiki_guardian.evaluate_guardian_approval`,\n the `guardian_comment_carried` source) were built and tested in isolation. Neither\n does anything until a caller passes the flags that turn the carry on, so the\n-feature was invisible in CI. This ADR records the decision the two of them\n+feature was invisible in CI. This SADR records the decision the two of them\n implement, and this task wires it into the four CI invocations that judge Guardian\n approval.\n \ndiff --git a/docs/adr/README.md b/docs/adr/README.md\nindex c70e057a4520c36e042ab68c53cd4a21ef709b71..5da43856836e2ce6a150f8127766e2f88355ab4b\n--- a/docs/adr/README.md\n+++ b/docs/adr/README.md\n@@ -1,40 +1,41 @@\n-# Architecture Decision Records\n+# Decision Record Namespaces\n \n-This directory holds Shiki's Architecture Decision Records (ADRs): durable\n-records of hard-to-reverse platform decisions. ADRs are part of the Shiki source\n-of truth alongside GitHub state, the `.shiki/` mirror, and `CONTEXT.md`.\n+This directory holds two independent decision namespaces:\n \n-## How ADRs Work\n+- Shiki Architecture Decision Records (SADRs), named\n+  `SADR-NNNN-<short-slug>.md`, record hard-to-reverse Shiki platform decisions.\n+- Target Architecture Decision Records (ADRs), named\n+  `NNNN-<short-slug>.md`, belong to the target product.\n \n-- New ADRs are added as `docs/adr/NNNN-<short-slug>.md`, where `NNNN` is the next\n-  zero-padded sequence number.\n-- The **canonical list of ADRs is the set of files matching `docs/adr/*.md`** in\n-  this directory. Browse `docs/adr/*.md` for the authoritative, current set.\n-- Use [`template.md`](template.md) as the starting point for a new ADR.\n-- Record an ADR when a decision is hard to reverse, surprising without context,\n-  and a real tradeoff. Do not write an ADR for routine or easily reversible\n-  choices.\n+The same four-digit number may appear once in each namespace. A platform\n+`SADR-NNNN` citation resolves only to `SADR-NNNN-*.md`; a target `ADR NNNN`\n+citation resolves only to `NNNN-*.md`.\n \n-Using the `docs/adr/*.md` glob as the source of truth means other PRs can add an\n-ADR without editing this index file, avoiding merge contention on a\n-hand-maintained enumeration.\n+## How Decision Records Work\n \n-## Current ADRs (snapshot)\n-\n-The list below is a convenience snapshot and may lag behind the directory. When\n-in doubt, list `docs/adr/*.md`.\n+- New Shiki platform decisions use the next zero-padded SADR number.\n+- New target product decisions use the target's own ADR sequence.\n+- The canonical list is the files in `docs/adr/`; no hand-maintained index is\n+  authoritative.\n+- Use [`template.md`](template.md) as the starting point and choose the filename\n+  namespace that owns the decision.\n+- Record a decision when it is hard to reverse, surprising without context, and\n+  a real tradeoff. Do not write a decision record for routine or easily\n+  reversible choices.\n \n-- [0001 \u2014 Keep Shiki As The Platform Name](0001-keep-shiki-as-platform-name.md)\n-- [0002 \u2014 Use AGENTS As The Shared Agent Constitution](0002-use-agents-as-shared-constitution.md)\n-- [0003 \u2014 Require Skill Gate For MergeGate Readiness](0003-require-skill-gate-for-mergegate-readiness.md)\n-- [0004 \u2014 Use GitHub First With A Repository-Local Shiki Mirror](0004-use-github-first-with-shiki-mirror.md)\n-- [0005 \u2014 Use Codex As Front And Claude Code Action As The GitHub Runtime](0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md)\n-- [0006 \u2014 Separate Codex Implementation, CCA Completion, And MergeGate Authorization](0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md)\n+Using the directory as the source of truth lets independent changes add records\n+without contending on an enumerated index.\n \n-## Adding A New ADR\n+## Adding A Shiki SADR\n \n-1. Copy `template.md` to `docs/adr/NNNN-<short-slug>.md`.\n+1. Copy `template.md` to `docs/adr/SADR-NNNN-<short-slug>.md`.\n 2. Fill in the title, status, context, decision, and consequences.\n-3. Set the status to `Proposed`, then `Accepted` once the decision is approved.\n-4. Open a PR following `CONTRIBUTING.md`. You do not need to edit this index for\n-   the ADR to be discoverable via `docs/adr/*.md`.\n+3. Set the status to `Proposed`, then `Accepted` once the proper authority has\n+   approved the decision.\n+4. Open a PR following `CONTRIBUTING.md`.\n+\n+## Adding A Target ADR\n+\n+1. Copy `template.md` to `docs/adr/NNNN-<short-slug>.md` using the target's own\n+   sequence.\n+2. Cite it as `ADR NNNN`; do not prefix its filename with `SADR-`.\ndiff --git a/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md b/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\nnew file mode 100644\nindex 0000000000000000000000000000000000000000..7455ce1f3bed02ddddb851ac21a1d1a648c9aeba\n--- /dev/null\n+++ b/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\n@@ -0,0 +1,55 @@\n+# SADR-0019: Separate Shiki Platform SADRs From Target ADRs\n+\n+## Status\n+\n+Accepted\n+\n+## Context\n+\n+Shiki installs its platform decision records into `docs/adr/` in every target\n+repository. Shiki and its targets previously used the same numeric\n+`NNNN-<slug>.md` namespace. A target that already owned ADRs 0013 through 0018\n+therefore acquired two records for each number during installation, making an\n+`ADR NNNN` citation ambiguous. Renumbering target decisions would rewrite target\n+history, while renumbering Shiki decisions per target would destroy their stable\n+platform identity.\n+\n+Upgrades also need to remove the former Shiki-owned numeric paths without ever\n+mistaking a target-owned ADR for platform content. The install stamp already\n+records exact shipped paths and content digests, so it can prove ownership only\n+when both still match.\n+\n+## Decision\n+\n+Shiki platform decisions use the `SADR` namespace:\n+\n+- records are named `docs/adr/SADR-NNNN-<slug>.md`;\n+- live platform citations use `SADR-NNNN`;\n+- target product decisions retain `docs/adr/NNNN-<slug>.md` and `ADR NNNN`;\n+- the reference validator resolves the two namespaces independently, so the same\n+  four-digit number may exist once in each namespace.\n+\n+Before a forced upgrade writes anything, the installer inventories the exact\n+former numeric Shiki ADR paths. It may delete a legacy path only when the\n+existing install stamp contains that exact path and its recorded SHA-256 digest\n+matches the file currently on disk. All ownership blockers are reported\n+together and abort the upgrade before its first write. A non-forced install\n+never performs this cleanup.\n+\n+Historical `.shiki/` evidence remains unchanged because it records the namespace\n+that existed when that evidence was produced.\n+\n+## Consequences\n+\n+- Targets may use any ADR number without colliding with Shiki's platform SADRs.\n+- Fresh installs preserve target ADR bytes and install a stable SADR set.\n+- Forced upgrades fail closed when legacy ownership cannot be proven, and a\n+  successful upgrade refreshes the stamp without removed legacy paths.\n+- Current governance, packaging, workflow, prompt, script, and skill references\n+  must move with the platform records.\n+\n+## Alternatives Considered\n+\n+- Collision-only refusal leaves affected targets permanently unupgradable.\n+- Renumbering target ADRs mutates target-owned history.\n+- Per-target Shiki renumbering makes platform citations non-portable.\ndiff --git a/docs/adr/template.md b/docs/adr/template.md\nindex a92a97daddd0b00ab9dd626cc8d86942da2eafed..17dd79175b5fee2105935557f691b40cf9492942\n--- a/docs/adr/template.md\n+++ b/docs/adr/template.md\n@@ -1,14 +1,14 @@\n-# NNNN: <Short Decision Title>\n+# <SADR-NNNN or NNNN>: <Short Decision Title>\n \n ## Status\n \n-Proposed | Accepted | Superseded by ADR-NNNN\n+Proposed | Accepted | Superseded by SADR-NNNN or target ADR NNNN\n \n ## Context\n \n What is the situation, constraint, or force that requires a decision? Reference\n-relevant domain terms (`CONTEXT.md`), prior ADRs, and code reality. State the\n-problem, not the solution.\n+relevant domain terms (`CONTEXT.md`), prior Shiki SADRs or target ADRs, and code\n+reality. State the problem, not the solution.\n \n ## Decision\n \ndiff --git a/docs/agents/autonomous-loop-e2e-acceptance.md b/docs/agents/autonomous-loop-e2e-acceptance.md\nindex bbf84c6081987f159ac6ad9ecf3c82b7aa72e7e8..5c13c1d02a05a90b0dc46cbef7c95a9846d0c237\n--- a/docs/agents/autonomous-loop-e2e-acceptance.md\n+++ b/docs/agents/autonomous-loop-e2e-acceptance.md\n@@ -40,8 +40,8 @@\n 3. `gh auth status` is logged in with merge permission on the repo.\n 4. Branch protection + required checks are configured exactly as the goal-loop\n    required-check set expects (see `docs/agents/branch-protection-smoke.md`).\n-5. The repo `.shiki/policy` permits autonomous low/medium-risk merges (ADR\n-   0008/0009); the MergeGate policy check is the recorded-authority gate.\n+5. The repo `.shiki/policy` permits autonomous low/medium-risk merges\n+   (SADR-0008/0009); the MergeGate policy check is the recorded-authority gate.\n \n The operator's only actions are: register the task and start the loop. **No\n manual pre-clearing** means after `shiki loop run` starts, the operator does not\ndiff --git a/docs/agents/bootstrap-command.md b/docs/agents/bootstrap-command.md\nindex 05f7f203a52cf412b9f97084a6e131ed0f785afd..f7aa0b734dfebee4a3b139335b66ee3415f48713\n--- a/docs/agents/bootstrap-command.md\n+++ b/docs/agents/bootstrap-command.md\n@@ -189,7 +189,9 @@\n ```\n \n Use this only for tests, fixtures, or explicit local-only template inspection.\n-Use `--force` only when you intentionally want to overwrite existing target files.\n+Use `--force` only when you intentionally want to refresh the target template.\n+Legacy numeric Shiki decision cleanup is stamp-and-digest authorized before any\n+write; target ADR paths are not cleanup candidates.\n \n ## Slash Command\n \ndiff --git a/docs/agents/checklists.md b/docs/agents/checklists.md\nindex 4a7f03ade346f53a1c1d82981f3fd744f9ab4547..0c5ec6c53dc7197b7b7a7d18e667b325babd6c45\n--- a/docs/agents/checklists.md\n+++ b/docs/agents/checklists.md\n@@ -32,18 +32,18 @@\n | ID | Item | Blocking |\n | --- | --- | --- |\n | GD-01 | Plan was challenged against `CONTEXT.md`. | blocking for non-trivial Goals |\n-| GD-02 | Relevant ADRs were read or absence was recorded. | blocking for non-trivial Goals |\n+| GD-02 | Relevant SADRs and target ADRs were read or absence was recorded. | blocking for non-trivial Goals |\n | GD-03 | Ambiguous terms were resolved or converted to HITL blockers. | blocking |\n | GD-04 | Edge scenarios were considered. | advisory |\n | GD-05 | Code reality was checked when code could answer. | blocking when applicable |\n | GD-06 | New domain terms were added to `CONTEXT.md` when needed. | blocking when applicable |\n-| GD-07 | ADR was created or explicitly skipped using the ADR criteria. | blocking when applicable |\n+| GD-07 | A SADR or target ADR was created or explicitly skipped using the decision-record criteria. | blocking when applicable |\n \n ## CI \u2014 Context & Impact Checklist\n \n | ID | Item | Blocking |\n | --- | --- | --- |\n-| CI-01 | Relevant docs and ADRs identified. | blocking |\n+| CI-01 | Relevant docs, SADRs, and target ADRs identified. | blocking |\n | CI-02 | Relevant modules, interfaces, seams, callers, and tests identified. | blocking |\n | CI-03 | Dependencies and blocked-by relationships identified. | blocking |\n | CI-04 | Candidate locks identified. | blocking |\n@@ -62,7 +62,7 @@\n | PRD-04 | Implementation decisions are recorded without volatile code detail. | blocking |\n | PRD-05 | Testing decisions are recorded. | blocking |\n | PRD-06 | Out-of-scope items are explicit. | blocking |\n-| PRD-07 | Relevant glossary/ADR links are present when applicable. | blocking |\n+| PRD-07 | Relevant glossary/SADR/target-ADR links are present when applicable. | blocking |\n \n ## SF \u2014 Spec Freeze Checklist\n \n@@ -98,7 +98,7 @@\n | H-02 | Branch/worktree target is present. | blocking |\n | H-03 | Scope and non-goals are present. | blocking |\n | H-04 | Dependencies and locks are present. | blocking |\n-| H-05 | Relevant docs, ADRs, modules, and tests are referenced. | blocking |\n+| H-05 | Relevant docs, SADRs, target ADRs, modules, and tests are referenced. | blocking |\n | H-06 | Required skills are stated. | blocking |\n | H-07 | TDD expectations are stated. | blocking when behavior work |\n | H-08 | Acceptance criteria are included. | blocking |\ndiff --git a/docs/agents/codex-handoff.md b/docs/agents/codex-handoff.md\nindex ede468f47cb277f69a857e8c45df703e4ed0151d..6f97bdffb67e44d7edea2c71e01ea28933c4eb6c\n--- a/docs/agents/codex-handoff.md\n+++ b/docs/agents/codex-handoff.md\n@@ -46,7 +46,7 @@\n \n ## Context\n - Domain terms:\n-- Relevant ADRs:\n+- Relevant SADRs and target ADRs:\n - Relevant docs:\n - Relevant modules/interfaces/seams:\n - Similar tests/prior art:\ndiff --git a/docs/agents/completion-check-agent.md b/docs/agents/completion-check-agent.md\nindex 13e4c035db35025f4e3cdcc5adfb24314ac4d521..61bfa1218cf36ca605250c91919e620f33238aa0\n--- a/docs/agents/completion-check-agent.md\n+++ b/docs/agents/completion-check-agent.md\n@@ -32,7 +32,7 @@\n - `AGENTS.md`\n - `CLAUDE.md`\n - `CONTEXT.md`\n-- relevant ADRs\n+- relevant SADRs and target ADRs\n - parent Goal issue\n - PRD issue or document\n - task issue\ndiff --git a/docs/agents/decision-control.md b/docs/agents/decision-control.md\nindex 7d8ee5ab03a5ccbcbd04056ae61de03785a2a9c9..8c94ecda00fffaa0e92c325ab83aa9776bd1dac1\n--- a/docs/agents/decision-control.md\n+++ b/docs/agents/decision-control.md\n@@ -11,7 +11,7 @@\n | Decision | Owner | When | Required inputs | Output | Enforcement surface |\n | --- | --- | --- | --- | --- | --- |\n | Is the Goal clear enough to plan? | Planner + Guardian when needed | Goal intake | User request, existing docs, risk signals | Goal readiness verdict | Goal checklist, issue template, ledger |\n-| Must `grill-with-docs` run? | Planner / Skill Gate | Before PRD or issue split | Ambiguity, domain terms, ADR conflicts, unclear boundaries | Skill invocation or explicit skip rationale | Skill Gate checklist |\n+| Must `grill-with-docs` run? | Planner / Skill Gate | Before PRD or issue split | Ambiguity, domain terms, SADR/target-ADR conflicts, unclear boundaries | Skill invocation or explicit skip rationale | Skill Gate checklist |\n | Is a PRD required? | Planner | After Goal context stabilizes | Goal, `grill-with-docs` output, Context & Impact output | PRD required / not required | PRD checklist, CCA evidence check |\n | Can the spec be frozen? | Operator only | End of Requirements Definition, before issue split | PRD/requirements summary, scope inventory (SF-02), open-question list | `spec_freeze` block in the plan + ledger evidence | `require_grilled_plan` (plan ingest/run), SF checklist |\n | Can a frozen spec change? | Operator only (Spec Amendment) | When implementation contradicts the frozen spec | Contested decisions, scoped re-grill result | recorded amendment + re-stamped freeze | SF checklist, ledger evidence |\ndiff --git a/docs/agents/domain.md b/docs/agents/domain.md\nindex 258da8ea81e87ac0a6b0b5b5e5c2fde980eaabb7..d57b0cf17ff3e1fa9e6e5885372bf083d8295421\n--- a/docs/agents/domain.md\n+++ b/docs/agents/domain.md\n@@ -5,7 +5,9 @@\n ## Layout\n \n - `CONTEXT.md` defines Shiki language. Keep it implementation-free.\n-- `docs/adr/` records hard-to-reverse decisions.\n+- `docs/adr/SADR-NNNN-*.md` records Shiki platform decisions.\n+- `docs/adr/NNNN-*.md` records target product decisions without sharing the\n+  SADR namespace.\n - `AGENTS.md` is the runtime-neutral agent constitution.\n - `CLAUDE.md` is the Claude Code wrapper around `AGENTS.md`.\n - `.shiki/` is the Target Repository mirror for Goals, PRDs, plans, Task DAGs, contracts, locks, worktree records, ledger entries, CCA verdicts, repair packets, reports, and handoffs.\n@@ -15,6 +17,6 @@\n \n - Use Shiki terms exactly when they exist.\n - Add glossary terms to `CONTEXT.md` only when the term is part of the domain, not a generic programming concept.\n-- Create ADRs only for hard-to-reverse, surprising, tradeoff-based decisions.\n+- Create SADRs or target ADRs only for hard-to-reverse, surprising, tradeoff-based decisions.\n - Keep product-specific language in the Target Repository, not in Shiki Template docs.\n-- If an ADR conflicts with a proposed implementation, surface the conflict before editing.\n+- If a SADR or target ADR conflicts with a proposed implementation, surface the conflict before editing.\ndiff --git a/docs/agents/execution-control.md b/docs/agents/execution-control.md\nindex ad8ee3cdb36be56fedc1f46408ca92b387d74047..bc63c3a73661d12b567016ba299f1a6b66bf9088\n--- a/docs/agents/execution-control.md\n+++ b/docs/agents/execution-control.md\n@@ -63,7 +63,7 @@\n | Branch name | Orchestrator | Orchestrator | Before implementer dispatch | branch policy | branch ref in task/ledger |\n | Lock reservation | Context & Impact | MergeGate | Before dispatch and before merge | `.shiki/locks/`, labels, PR metadata | lock id, owner, scope |\n | CI profile | Planner | MergeGate | During issue triage, rechecked at PR | labels, `.shiki/policy.yaml`, workflow matrix | required check list |\n-| Required status checks | Repo maintainer / Guardian | GitHub branch protection / rulesets | Repository setup, updated via ADR | GitHub settings | branch/ruleset config, ADR |\n+| Required status checks | Repo maintainer / Guardian | GitHub branch protection / rulesets | Repository setup, updated via target ADR | GitHub settings | branch/ruleset config, target ADR |\n | CCA judgment | CCA | GitHub required check / MergeGate | Every non-draft PR update | GitHub Action check | structured CCA verdict |\n | Repair assignment | CCA | MergeGate | After failed CCA/CI/review | repair packet, issue/PR comment | repair id and attempt count |\n | Merge readiness | MergeGate | GitHub branch protection / Guardian if needed | After CCA complete | required checks, review rules, rulesets | mergegate check output |\ndiff --git a/docs/agents/guardian-policy.md b/docs/agents/guardian-policy.md\nindex 4147c7adf05a47adb4fd24ada1a1c2e1f2ba6d2d..a20d9cc6c00a569bb057b9ca8d6b51c7fc4a9847\n--- a/docs/agents/guardian-policy.md\n+++ b/docs/agents/guardian-policy.md\n@@ -98,8 +98,8 @@\n `origin/<default_branch>` and reuses each job's existing base fetch; a shallow\n (`--depth`) or second base fetch would make the proof unresolvable and silently\n disable the carry, so neither is permitted. CCA reads `guardian_comment_carried` as\n-a recorded authority in the deterministic signal and does not re-derive it. See ADR\n-0018 for the full decision, the rejected diff/patch-equality and path-disjointness\n+a recorded authority in the deterministic signal and does not re-derive it. See\n+SADR-0018 for the full decision, the rejected diff/patch-equality and path-disjointness\n alternatives, and the accepted residual.\n \n ## External AI Guardian Review (SADR-0010)\ndiff --git a/docs/agents/implementation-policy.md b/docs/agents/implementation-policy.md\nindex c754144a9fb5bf4a38aa82a60e1670c59e626b44..85c37281bcad196c6b7b02343faf00b03cd9c57a\n--- a/docs/agents/implementation-policy.md\n+++ b/docs/agents/implementation-policy.md\n@@ -31,10 +31,13 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n ## Non-Negotiables\n \n 1. **Planning precedes implementation.** Non-trivial Goals start with `grill-with-docs` and Context & Impact.\n-2. **Domain language is controlled.** Terms must be checked against `CONTEXT.md`; hard-to-reverse tradeoffs must be checked against ADRs.\n+2. **Domain language is controlled.** Terms must be checked against `CONTEXT.md`; hard-to-reverse tradeoffs must be checked against Shiki SADRs and target ADRs.\n 3. **PRD precedes issue decomposition.** `to-prd` is used once the context is settled enough to become durable intent.\n 4. **Issues are vertical slices.** `to-issues` must produce independently grabbable tracer-bullet tasks, not horizontal layer tickets.\n 5. **Triage controls dispatch.** Only `ready-for-agent` / AFK tasks may be assigned to Codex without human input.\n@@ -95,19 +98,19 @@\n Process:\n \n - Challenge terms against `CONTEXT.md`.\n-- Check ADRs before reopening decisions.\n+- Check SADRs and target ADRs before reopening decisions.\n - Inspect code when code can answer a question.\n - Ask one question at a time when the operator is available.\n - Provide a recommended answer with each question.\n - Record resolved terms in `CONTEXT.md`.\n-- Create ADRs only for hard-to-reverse, surprising, tradeoff-based decisions.\n+- Create SADRs or target ADRs only for hard-to-reverse, surprising, tradeoff-based decisions.\n - Convert unresolved load-bearing questions into HITL blockers.\n \n Outputs:\n \n - Resolved design-tree decisions.\n - Updated glossary terms when needed.\n-- ADRs when justified.\n+- SADRs or target ADRs when justified.\n - HITL blockers when decisions remain unresolved.\n \n ## Phase 3 \u2014 Context & Impact\n@@ -117,7 +120,7 @@\n \n Outputs must include:\n \n-- Relevant documents and ADRs.\n+- Relevant documents, SADRs, and target ADRs.\n - Relevant modules, interfaces, seams, callers, and tests.\n - Candidate locks.\n - Dependencies.\n@@ -140,7 +143,7 @@\n - Testing decisions.\n - Out of scope.\n - Further notes.\n-- Links to relevant Goal, domain terms, and ADRs.\n+- Links to relevant Goal, domain terms, SADRs, and target ADRs.\n \n Phase 4 ends with Spec Freeze: the operator explicitly approves the PRD, the\n plan records a `spec_freeze` block (status, approved_by, source), and the SF\n@@ -240,7 +243,7 @@\n - Dependencies and locks.\n - Risk and Guardian requirements.\n - Ledger completeness.\n-- Documentation and ADR updates.\n+- Documentation and SADR/target-ADR updates.\n - Scope drift.\n \n CCA emits one of:\n@@ -301,7 +304,7 @@\n | Evidence failure | Behavior might be done, but proof is missing. | Add checks, PR evidence, ledger entry, or artifacts. |\n | Scope drift | PR changes unrelated behavior. | Revert unrelated changes or split into new Goal. |\n | Dependency block | Upstream task or lock not complete. | Block until dependency resolves. |\n-| Architecture block | Decision needs ADR or deep-module discussion. | `grill-with-docs` or `improve-codebase-architecture`. |\n+| Architecture block | Decision needs a SADR, target ADR, or deep-module discussion. | `grill-with-docs` or `improve-codebase-architecture`. |\n | Risk block | Security/production/policy change needs human approval. | Guardian decision. |\n \n ## Phase 5.5 \u2014 Execution Decision Control\ndiff --git a/docs/agents/runtime-auth-model.md b/docs/agents/runtime-auth-model.md\nindex 6a2e1a47e81ea89b5c68a210ec07772a9374babc..3c54748d24233dbd0ba8473950282547ef4ce935\n--- a/docs/agents/runtime-auth-model.md\n+++ b/docs/agents/runtime-auth-model.md\n@@ -148,7 +148,7 @@\n \n API-key based runners may be added by a target repository as an explicit extension. They are not the default Shiki template path.\n \n-If a repo opts into API-key mode, record the exception in an ADR and update the repo's `AGENTS.md`, `.shiki/config.yaml`, and workflow permissions.\n+If a repo opts into API-key mode, record the target-specific exception in a target ADR and update the repo's `AGENTS.md`, `.shiki/config.yaml`, and workflow permissions.\n \n ## GitHub Actions Runtime Compatibility\n \ndiff --git a/docs/agents/skill-gate.md b/docs/agents/skill-gate.md\nindex c50398b0d7c60a8598f2a5c88a10015730dced13..3acffe6444617bb1d54b77d4b4708158ec735bc6\n--- a/docs/agents/skill-gate.md\n+++ b/docs/agents/skill-gate.md\n@@ -9,7 +9,7 @@\n | Skill | Trigger | Expected output |\n | --- | --- | --- |\n | `setup-matt-pocock-skills` | First configuring a repo for these skills, or when issue tracker, triage labels, or domain docs are missing. | `docs/agents/` configuration and an `Agent skills` block in `AGENTS.md` or `CLAUDE.md`. |\n-| `grill-with-docs` | Ambiguous plan, unclear terminology, boundary decision, tradeoff, or ADR-worthy design choice. | Clarified decision tree, recommended answers, updates to `CONTEXT.md` or ADRs when decisions crystallize. |\n+| `grill-with-docs` | Ambiguous plan, unclear terminology, boundary decision, tradeoff, or decision-record-worthy design choice. | Clarified decision tree, recommended answers, updates to `CONTEXT.md`, SADRs, or target ADRs when decisions crystallize. |\n | `zoom-out` | Agent lacks a higher-level architectural map. | Map of relevant modules, callers, seams, and domain concepts. |\n | `to-prd` | Settled context should become a PRD. | PRD published to the issue tracker with problem, solution, user stories, decisions, testing, and out-of-scope notes. |\n | `to-issues` | Goal, PRD, or plan must become implementation tickets. | Vertical-slice issues with acceptance criteria, dependencies, and AFK/HITL classification. |\n@@ -17,7 +17,7 @@\n | `tdd` | Feature work or bug fix has observable behavior that can be specified with tests. | Red-green-refactor loop through public interfaces, one vertical slice at a time. |\n | `code-review` | The TDD loop is green and the implementer is about to create or update a PR. Default for every implementation task. | Pre-PR self-review of the task diff with fixes applied on the task branch; findings and resolutions recorded as a ledger entry naming the skill and a `## Pre-PR code review` PR body section. |\n | `diagnose` | Hard bug, failing check, regression, flaky behavior, or performance problem. | Reproduction loop, ranked hypotheses, instrumentation, fix, regression test, post-mortem. |\n-| `improve-codebase-architecture` | Structural friction, testability, AI-navigability, deep-module opportunity, or refactor request. | Architecture candidates and deepening recommendations informed by `CONTEXT.md` and ADRs. |\n+| `improve-codebase-architecture` | Structural friction, testability, AI-navigability, deep-module opportunity, or refactor request. | Architecture candidates and deepening recommendations informed by `CONTEXT.md`, SADRs, and target ADRs. |\n | `prototype` | A throwaway logic or UI prototype is needed to answer a design question. | Clearly marked disposable prototype and captured decision. |\n \n ## MergeGate Interaction\ndiff --git a/docs/operations.md b/docs/operations.md\nindex cf1f452d4c589e38ccd58553bda0ad8684b51eb7..ace48a90ffbec9e5c0a7113bef16a4858bc44b3f\n--- a/docs/operations.md\n+++ b/docs/operations.md\n@@ -89,6 +89,16 @@\n      target history, brought forward by `shiki migrate`, not a template.\n    - **Everything else** (CLI scripts, workflows, schemas, docs) is overwritten.\n \n+   A forced upgrade also migrates the former numeric Shiki decision paths to\n+   `SADR-NNNN-*`. Before the first write, the installer inventories the exact\n+   legacy Shiki paths and authorizes each deletion only when the existing\n+   `.shiki/install-stamp.json` names that path and its SHA-256 digest still\n+   matches the file. An absent, unreadable, malformed, incomplete, or\n+   mismatching stamp reports every blocked path and stops with no filesystem\n+   change. Target-owned `NNNN-*` ADRs are not cleanup candidates. A non-force\n+   install deletes nothing and retains any still-matching legacy stamp entries\n+   so a later forced upgrade can prove ownership.\n+\n    If the target has pending migrations, `--force` refuses and writes nothing \u2014\n    apply migrations first (step 3), then re-run. The run ends with a single\n    summary of every `.new` written, so a half-upgrade is visible.\ndiff --git a/docs/releasing.md b/docs/releasing.md\nindex 10d73828e7dbd64e9d9f6b83bc84d010537ce62a..aafeee8c4d441e1e427efee2ce4f81a647f020e1\n--- a/docs/releasing.md\n+++ b/docs/releasing.md\n@@ -67,7 +67,7 @@\n CLI in `scripts/`. There are no third-party Python runtime dependencies. The CLI\n is intentionally **not** exposed as a `[project.scripts]` console entry point\n because `scripts/shiki.py` is a dependency-free shim and `scripts/` is not\n-packaged as an importable distribution (see the ADR for the rationale).\n+packaged as an importable distribution (see SADR-0007 for the rationale).\n \n Supported install paths:\n \n@@ -155,6 +155,15 @@\n    [docs/operations.md](operations.md#the-new-convention). If the target has\n    pending migrations, `--force` refuses and writes nothing; apply migrations\n    (step 2) first, then re-run.\n+\n+   The SADR namespace upgrade has an additional pre-write gate: each former\n+   numeric Shiki decision path is removed only when the existing install stamp\n+   contains that exact path and a digest matching its current bytes. The\n+   installer aggregates absent/unreadable/malformed-stamp, missing-digest, and\n+   digest-mismatch blockers before raising, so refusal leaves the target tree\n+   byte-identical. Numeric target ADRs are preserved. A successful forced\n+   upgrade installs and stamps `SADR-*` paths and drops the removed legacy paths\n+   from the refreshed stamp.\n 2. Apply repository-local `.shiki/` state migrations with the migrate command:\n \n    ```bash\ndiff --git a/docs/usage.md b/docs/usage.md\nindex b83407ee3970dd395dd893010bc5dbe8bad26051..a130ef6e40dfd8169a5bc701339351097df1a09f\n--- a/docs/usage.md\n+++ b/docs/usage.md\n@@ -67,8 +67,10 @@\n shiki install-target /path/to/target-repo --local-only\n ```\n \n-Use `--force` only when you intentionally want to overwrite existing target\n-files.\n+Use `--force` only when you intentionally want to refresh the target template.\n+For the SADR namespace migration, `--force` deletes only exact legacy Shiki\n+decision paths whose existing install-stamp digest still matches; any ownership\n+blocker stops before the first write. Numeric target ADRs are never candidates.\n \n ## `shiki bootstrap-platform` \u2014 publish the Shiki platform repo\n \ndiff --git a/scripts/guardian_approval_signal.py b/scripts/guardian_approval_signal.py\nindex d059f92b2c82aa2b5d6ca421365537a87886c1cb..16924892714f4f5b9303c0ef0e325a7e70075630\n--- a/scripts/guardian_approval_signal.py\n+++ b/scripts/guardian_approval_signal.py\n@@ -199,8 +199,8 @@\n ) -> Any:\n     \"\"\"Evaluate SADR-0015 Contract Approval for the CCA Guardian signal.\n \n-    The SAME OR that MergeGate applies must reach this deterministic signal: ADR\n-    0015 is explicit that without it, a contract-approved PR still returns\n+    The SAME OR that MergeGate applies must reach this deterministic signal:\n+    SADR-0015 is explicit that without it, a contract-approved PR still returns\n     ``needs_guardian``, the CCA job fails, and the MergeGate policy check never\n     runs \u2014 the gate this change moves would never be reached. This resolves the\n     task the SAME way MergeGate does (the first Shiki task id in the PR body),\ndiff --git a/scripts/shiki_contract_approval.py b/scripts/shiki_contract_approval.py\nindex 941bcb96fdd8b9a85bc0c4f0f04d5dad9d04839c..4a4b418f6527413161db82670bec857a043360e2\n--- a/scripts/shiki_contract_approval.py\n+++ b/scripts/shiki_contract_approval.py\n@@ -142,7 +142,7 @@\n     changed_files_status: Any,\n     registration: Any,\n ) -> ContractApprovalResult:\n-    \"\"\"Whether this PR carries an SADR-0015 Contract Approval.\n+    \"\"\"Whether this PR carries a SADR-0015 Contract Approval.\n \n     The four conditions are evaluated IN ORDER, each failure recorded as a\n     distinct reason string:\ndiff --git a/scripts/shiki_guardian.py b/scripts/shiki_guardian.py\nindex a4ecb79bbda55fd5c8b06adfae2eb99760a03c45..4dd99bbe714e26f421fa19abac7a34ae1b195ecc\n--- a/scripts/shiki_guardian.py\n+++ b/scripts/shiki_guardian.py\n@@ -12,7 +12,7 @@\n # The carry re-derives its proof from git in-process on every judging invocation;\n # it is never read from an artifact, so there is nothing to go stale or be forged.\n # shiki_sync_proof is the ONLY guardian-family module that runs git, and every one\n-# of its invocations is read-only and local (ADR: prove-from-git base sync).\n+# of its invocations is read-only and local (SADR: prove-from-git base sync).\n from shiki_sync_proof import verify_pure_base_sync\n \n GUARDIAN_POLICY_PATH = \".shiki/guardian-policy.json\"\ndiff --git a/scripts/shiki_guardian_review.py b/scripts/shiki_guardian_review.py\nindex dd0f104d457899e0ba5ff1e162011c1e90b6f92b..e10ef58c046324e7ea3a3392feb3048ca77414ce\n--- a/scripts/shiki_guardian_review.py\n+++ b/scripts/shiki_guardian_review.py\n@@ -159,7 +159,7 @@\n     docs_only = _is_docs_only(changed_files)\n     if docs_only and matched == 0:\n         focus.append(\n-            \"Docs/ADR-only change: confirm the change is genuinely documentation/decision-only, introduces no behavior or contract drift, and stays consistent with the constitution and existing ADRs.\"\n+            \"Docs/decision-record-only change: confirm the change is genuinely documentation/decision-only, introduces no behavior or contract drift, and stays consistent with the constitution and existing SADRs and target ADRs.\"\n         )\n \n     # Conservative augmentation: unknown (no category and not docs-only), mixed\ndiff --git a/scripts/shiki_installer.py b/scripts/shiki_installer.py\nindex be064ab7a29ff6836f7f7a1d70500a43e0d11d7b..9fd8eefd137ce7003dad5c0dcb2e48e2cd47ab32\n--- a/scripts/shiki_installer.py\n+++ b/scripts/shiki_installer.py\n@@ -118,6 +118,7 @@\n # what lets a target carry it without a governance-schema change.\n INSTALL_STAMP_PATH = \".shiki/install-stamp.json\"\n INSTALL_STAMP_VERSION = 1\n+LEGACY_SHIKI_SADR_MAX = 18\n \n # Under --force the shipped surface splits three ways, so nothing is lost and\n # nothing is silently stale.\n@@ -237,9 +238,98 @@\n         )\n \n \n+def legacy_shiki_adr_paths() -> tuple[str, ...]:\n+    \"\"\"Former numeric paths for Shiki SADR-0001 through SADR-0018.\n+\n+    The authoritative slugs come from the current SADR filenames. SADR-0019 is\n+    the namespace decision itself and never shipped under a numeric legacy path.\n+    \"\"\"\n+    paths: list[str] = []\n+    for sadr in sorted((ROOT / \"docs\" / \"adr\").glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\")):\n+        number = sadr.name.split(\"-\", 2)[1]\n+        if int(number) <= LEGACY_SHIKI_SADR_MAX:\n+            paths.append(f\"docs/adr/{sadr.name.removeprefix('SADR-')}\")\n+    return tuple(paths)\n+\n+\n+def preflight_legacy_sadr_cleanup(target: Path, *, force: bool) -> tuple[str, ...]:\n+    \"\"\"Authorize exact legacy Shiki ADR removals before the first write.\n+\n+    A non-force install never removes existing files. A forced install needs no\n+    stamp when no legacy Shiki path is present. When legacy paths do exist, each\n+    must be named in a readable install stamp and its current bytes must match\n+    the recorded digest.\n+    \"\"\"\n+    if not force:\n+        return ()\n+    present = tuple(\n+        relative\n+        for relative in legacy_shiki_adr_paths()\n+        if (target / relative).exists() or (target / relative).is_symlink()\n+    )\n+    if not present:\n+        return ()\n+\n+    stamp_path = target / INSTALL_STAMP_PATH\n+    try:\n+        raw = stamp_path.read_text(encoding=\"utf-8\")\n+        stamp = json.loads(raw)\n+    except FileNotFoundError:\n+        detail = \"install stamp is absent\"\n+        blockers = [f\"{relative}: {detail}\" for relative in present]\n+    except (OSError, UnicodeDecodeError) as error:\n+        detail = f\"install stamp is unreadable ({error})\"\n+        blockers = [f\"{relative}: {detail}\" for relative in present]\n+    except json.JSONDecodeError as error:\n+        detail = f\"install stamp is malformed ({error})\"\n+        blockers = [f\"{relative}: {detail}\" for relative in present]\n+    else:\n+        if not isinstance(stamp, dict):\n+            blockers = [\n+                f\"{relative}: install stamp is malformed (expected an object)\"\n+                for relative in present\n+            ]\n+        else:\n+            digests = stamp.get(\"digests\")\n+            blockers = []\n+            if not isinstance(digests, dict):\n+                blockers.extend(\n+                    f\"{relative}: install stamp has no digest for this path\"\n+                    for relative in present\n+                )\n+            else:\n+                for relative in present:\n+                    expected = digests.get(relative)\n+                    if not isinstance(expected, str) or not expected:\n+                        blockers.append(\n+                            f\"{relative}: install stamp has no digest for this path\"\n+                        )\n+                        continue\n+                    try:\n+                        actual = _sha256_file(target / relative)\n+                    except OSError as error:\n+                        blockers.append(\n+                            f\"{relative}: legacy path is unreadable ({error})\"\n+                        )\n+                        continue\n+                    if actual != expected:\n+                        blockers.append(\n+                            f\"{relative}: digest mismatch (stamp {expected}, current {actual})\"\n+                        )\n+\n+    if blockers:\n+        joined = \"\\n\".join(f\"- {blocker}\" for blocker in blockers)\n+        raise ShikiError(\n+            \"legacy Shiki ADR cleanup is not authorized:\\n\"\n+            f\"{joined}\\nNo files were written.\"\n+        )\n+    return present\n+\n+\n def install_template(target: Path, *, force: bool, validate: bool) -> list[NewFileNote]:\n     # Preconditions first: never rewrite the tree of a target that cannot be\n     # upgraded cleanly (see refuse_pending_migrations).\n+    legacy_cleanup = preflight_legacy_sadr_cleanup(target, force=force)\n     refuse_pending_migrations(target)\n \n     # Resolve the target's required CODEOWNERS owner once from its\n@@ -250,6 +340,10 @@\n     # verbatim. See ``codeowners_required_owner``.\n     codeowners_owner = codeowners_required_owner(target)\n \n+    for relative in legacy_cleanup:\n+        (target / relative).unlink()\n+        info(f\"removed stamped legacy Shiki ADR: {target / relative}\")\n+\n     notes: list[NewFileNote] = []\n     for relative in TEMPLATE_PATHS:\n         source = ROOT / relative\n@@ -647,6 +741,22 @@\n     digests = target_stamp_digests(target)\n     existing = read_install_stamp(target)\n     if existing is not None:\n+        # A non-force install deliberately leaves legacy Shiki ADRs in place.\n+        # Preserve only still-matching ownership proof so a later forced\n+        # upgrade can authorize cleanup; never carry a missing or stale digest.\n+        existing_digests = existing.get(\"digests\")\n+        if isinstance(existing_digests, dict):\n+            for relative in legacy_shiki_adr_paths():\n+                expected = existing_digests.get(relative)\n+                path = target / relative\n+                if not isinstance(expected, str) or not path.is_file():\n+                    continue\n+                try:\n+                    actual = _sha256_file(path)\n+                except OSError:\n+                    continue\n+                if actual == expected:\n+                    digests[relative] = actual\n         if _incoming_is_older(commit, existing.get(\"platform_commit\")):\n             warn(\n                 \"kept existing install stamp: incoming install \"\ndiff --git a/scripts/shiki_loop.py b/scripts/shiki_loop.py\nindex 3611641da8aeb5038b1da90e3114b09a473ec6d8..84caa491b9cad0edc4176f209c13e9221e975681\n--- a/scripts/shiki_loop.py\n+++ b/scripts/shiki_loop.py\n@@ -424,7 +424,7 @@\n         # unresolvable CCA verdict has already produced its terminal stop and is never\n         # laundered into a head-moving sync (SADR-0010). It also never fires while the\n         # policy (Guardian) gate is red \u2014 for ANY reason, not only when it is the sole\n-        # red check \u2014 so a head-bound Guardian approval, or an SADR-0015\n+        # red check \u2014 so a head-bound Guardian approval, or a SADR-0015\n         # contract-approval carry that base movement defeated (the policy gate compares\n         # base and head task governance directly), is never silently restored by\n         # adopting base wholesale. It is bounded by a durable per-task counter so the\ndiff --git a/scripts/shiki_tasks.py b/scripts/shiki_tasks.py\nindex aec3809f24b4a0c8c96fc50971cff9ae9ad30381..d531052e9d9717058d4631483cefab44c26a65c1\n--- a/scripts/shiki_tasks.py\n+++ b/scripts/shiki_tasks.py\n@@ -821,7 +821,7 @@\n         \"prompt\": prompt,\n         \"entry_skill\": \"grill-with-docs\",\n         \"required_next_steps\": [\n-            \"Run grill-with-docs until terminology, boundaries, risks, and ADR-worthy decisions are settled.\",\n+            \"Run grill-with-docs until terminology, boundaries, risks, and decision-record-worthy choices are settled.\",\n             \"Obtain the operator's explicit approval of the PRD/requirements (Spec Freeze).\",\n             \"Write a machine-readable plan JSON with grill_with_docs.status=complete and spec_freeze.status=frozen.\",\n             \"Run shiki plan ingest --plan-file PLAN.json.\",\ndiff --git a/scripts/test_shiki_goal_loop.sh b/scripts/test_shiki_goal_loop.sh\nindex 753c7b516e10f0be3bc92a4bd0eb1fa699776ae6..8f3820a0a079b3044424cfe1a6c6b7fe68896e67\n--- a/scripts/test_shiki_goal_loop.sh\n+++ b/scripts/test_shiki_goal_loop.sh\n@@ -231,8 +231,8 @@\n \n # High/critical risk merges autonomously when all required checks are green:\n # the \"MergeGate policy check\" required check IS the Guardian gate (it enforces\n-# guardian-policy.json \u2014 human approval OR external AI guardian review, ADR\n-# 0010), so green checks mean a recorded authority approved. The fake gh returns\n+# guardian-policy.json \u2014 human approval OR external AI guardian review,\n+# SADR-0010), so green checks mean a recorded authority approved. The fake gh returns\n # green checks, simulating that the Guardian gate passed.\n python3 \"$ROOT/scripts/shiki.py\" goal create --target \"$TARGET\" --title \"Guardian gate\" --outcome \"High risk merges via policy gate\" >/tmp/shiki-goal-loop-guardian-goal.json\n GGOAL=\"$(json_get /tmp/shiki-goal-loop-guardian-goal.json goal_id)\"\ndiff --git a/tests/test_adr_references.py b/tests/test_adr_references.py\nindex 51706329c797d5dd9b47e082a333fa83b261d482..d005da4f771f5e8085ec22d4b787980e68f5ae8c\n--- a/tests/test_adr_references.py\n+++ b/tests/test_adr_references.py\n@@ -1,8 +1,8 @@\n-\"\"\"Every ``ADR ####`` citation must resolve to exactly one decision record.\n+\"\"\"Every target ADR and Shiki SADR citation resolves in its own namespace.\n \n Background. ``scripts/mergegate_check.py`` justifies ``is_bookkeeping_closeout``\n-with \"ADR 0017\" nine times and ``scripts/guardian_approval_signal.py`` cites\n-0016/0017 seven times, yet for a stretch the numbering in ``docs/adr/`` stopped\n+with \"SADR-0017\" nine times and ``scripts/guardian_approval_signal.py`` cites\n+SADR-0016/SADR-0017 seven times, yet for a stretch the numbering in ``docs/adr/`` stopped\n at 0015: the authoring commits lived on a coordinator branch and no task ever\n declared ``docs/adr/...`` in its locks, so nothing carried the records to the\n default branch. The stated basis of an exemption that merged seven PRs with zero\n@@ -11,11 +11,12 @@\n and the directory drifted with no signal.\n \n This is the same unbound-pair shape behind other silent failures: a reference\n-and its target with nothing asserting they agree. This test binds them. It scans\n-every ``scripts/*.py`` (plus ``docs/**/*.md`` and the root ``*.md`` governance\n-files, which are cheap) for the ``ADR ####`` pattern and asserts each cited\n-number resolves to exactly one ``docs/adr/####-*.md``. The converse is\n-deliberately NOT required: an ADR may exist without being cited.\n+and its target with nothing asserting they agree. This test binds them. Target\n+``ADR NNNN`` citations resolve only to ``docs/adr/NNNN-*.md``; Shiki\n+``SADR-NNNN`` citations resolve only to ``docs/adr/SADR-NNNN-*.md``. The scan\n+covers current governance docs, scripts, workflows, prompts, packaging metadata,\n+and repo-local Codex guidance while deliberately excluding historical\n+``.shiki`` evidence. The converse is not required: a record may be uncited.\n \n The suite is dependency free: standard-library ``unittest`` only.\n \"\"\"\n@@ -31,17 +32,18 @@\n \n REPO_ROOT = Path(__file__).resolve().parents[1]\n \n-# \"ADR 0016\" -- a four-digit citation. The trailing \\b keeps a five-digit token\n-# from being read as a truncated four-digit number.\n-_CITATION = re.compile(r\"\\bADR (\\d{4})\\b\")\n-# \"0016-locks-bound-product-paths.md\" -- the leading four digits name the record.\n-_ADR_FILENAME = re.compile(r\"^(\\d{4})-.*\\.md$\")\n+# ``ADR 0016`` / ``SADR-0016`` -- a namespace plus four-digit citation. The\n+# trailing ``\\b`` keeps a five-digit token from being read as a truncated one.\n+_CITATION = re.compile(r\"\\b(S?ADR)[ -](\\d{4})\\b\")\n+# Target ADRs retain the numeric filename; Shiki platform decisions use SADR.\n+_DECISION_FILENAME = re.compile(r\"^(?:(SADR)-)?(\\d{4})-.*\\.md$\")\n \n \n class Violation(NamedTuple):\n     \"\"\"A citation that does not resolve to exactly one decision record.\"\"\"\n \n     citing_file: str  # repo-relative path of the file that cites the ADR\n+    namespace: str  # \"ADR\" for target decisions; \"SADR\" for Shiki decisions\n     number: str  # the cited four-digit number, e.g. \"0016\"\n     line: int  # 1-based line of the citation\n     reason: str  # \"missing\" (zero matches) or \"ambiguous\" (two or more)\n@@ -49,44 +51,59 @@\n \n     @property\n     def expected_path(self) -> str:\n-        return f\"docs/adr/{self.number}-*.md\"\n+        prefix = \"SADR-\" if self.namespace == \"SADR\" else \"\"\n+        return f\"docs/adr/{prefix}{self.number}-*.md\"\n \n     def describe(self) -> str:\n         \"\"\"Name the citing file, the cited number, and the missing path.\"\"\"\n         if self.reason == \"missing\":\n             return (\n-                f\"{self.citing_file}:{self.line} cites ADR {self.number} \"\n+                f\"{self.citing_file}:{self.line} cites {self.namespace} {self.number} \"\n                 f\"but no document matches {self.expected_path}\"\n             )\n         joined = \", \".join(self.matches)\n         return (\n-            f\"{self.citing_file}:{self.line} cites ADR {self.number} which \"\n+            f\"{self.citing_file}:{self.line} cites {self.namespace} {self.number} which \"\n             f\"resolves to {len(self.matches)} documents (expected exactly one \"\n             f\"at {self.expected_path}): {joined}\"\n         )\n \n \n-def _adr_index(root: Path) -> dict[str, list[Path]]:\n-    \"\"\"Map every four-digit ADR number to the record files that claim it.\"\"\"\n-    index: dict[str, list[Path]] = {}\n+def _adr_index(root: Path) -> dict[tuple[str, str], list[Path]]:\n+    \"\"\"Map each decision namespace and number to the files that claim it.\"\"\"\n+    index: dict[tuple[str, str], list[Path]] = {}\n     adr_dir = root / \"docs\" / \"adr\"\n     if adr_dir.is_dir():\n         for path in sorted(adr_dir.glob(\"*.md\")):\n-            match = _ADR_FILENAME.match(path.name)\n+            match = _DECISION_FILENAME.match(path.name)\n             if match:\n-                index.setdefault(match.group(1), []).append(path)\n+                namespace = \"SADR\" if match.group(1) else \"ADR\"\n+                index.setdefault((namespace, match.group(2)), []).append(path)\n     return index\n \n \n def _citing_files(root: Path, include_docs: bool = True) -> list[Path]:\n-    \"\"\"Files scanned for citations: scripts/*.py, and cheaply the markdown.\"\"\"\n+    \"\"\"Current, shipped text surfaces scanned for decision citations.\"\"\"\n     files = sorted((root / \"scripts\").glob(\"*.py\"))\n+    files += sorted((root / \"scripts\").glob(\"*.sh\"))\n     if include_docs:\n         docs = root / \"docs\"\n         if docs.is_dir():\n             files += sorted(docs.rglob(\"*.md\"))\n         files += sorted(root.glob(\"*.md\"))\n-    return files\n+        pyproject = root / \"pyproject.toml\"\n+        if pyproject.is_file():\n+            files.append(pyproject)\n+        for relative in (\".github\", \".codex\"):\n+            surface = root / relative\n+            if surface.is_dir():\n+                files += sorted(\n+                    path\n+                    for path in surface.rglob(\"*\")\n+                    if path.is_file()\n+                    and path.suffix in {\".md\", \".py\", \".sh\", \".toml\", \".yaml\", \".yml\"}\n+                )\n+    return sorted(set(files))\n \n \n def find_adr_reference_violations(\n@@ -94,10 +111,9 @@\n ) -> list[Violation]:\n     \"\"\"Return every ``ADR ####`` citation under ``root`` that fails to resolve.\n \n-    A citation resolves when exactly one ``docs/adr/####-*.md`` matches its\n-    number. Zero matches is a \"missing\" violation; two or more is \"ambiguous\".\n-    Records that are never cited produce nothing -- the converse is not\n-    required.\n+    A citation resolves when exactly one record in the same namespace matches\n+    its number. Zero matches is a \"missing\" violation; two or more is\n+    \"ambiguous\". Records that are never cited produce nothing.\n     \"\"\"\n     root = Path(root)\n     index = _adr_index(root)\n@@ -110,13 +126,15 @@\n         rel = path.relative_to(root).as_posix()\n         for lineno, line in enumerate(text.splitlines(), start=1):\n             for match in _CITATION.finditer(line):\n-                number = match.group(1)\n-                found = index.get(number, [])\n+                namespace, number = match.groups()\n+                found = index.get((namespace, number), [])\n                 if len(found) == 1:\n                     continue\n                 reason = \"missing\" if not found else \"ambiguous\"\n                 rels = [p.relative_to(root).as_posix() for p in found]\n-                violations.append(Violation(rel, number, lineno, reason, rels))\n+                violations.append(\n+                    Violation(rel, namespace, number, lineno, reason, rels)\n+                )\n     return violations\n \n \n@@ -129,8 +147,8 @@\n         self.assertEqual(\n             violations,\n             [],\n-            \"Every 'ADR ####' citation must resolve to exactly one \"\n-            f\"docs/adr/####-*.md:\\n{detail}\",\n+            \"Every ADR/SADR citation must resolve exactly once in its own \"\n+            f\"docs/adr namespace:\\n{detail}\",\n         )\n \n     def test_scan_actually_reads_scripts(self) -> None:\n@@ -142,12 +160,38 @@\n             \"the citation scan must cover scripts/*.py\",\n         )\n \n+    def test_platform_citations_do_not_use_unresolved_numeric_shorthand(self) -> None:\n+        shorthand = re.compile(r\"SADR-\\d{4}/\\d{4}\")\n+        offenders = []\n+        for path in _citing_files(REPO_ROOT):\n+            text = path.read_text(encoding=\"utf-8\")\n+            if shorthand.search(text):\n+                offenders.append(path.relative_to(REPO_ROOT).as_posix())\n+        self.assertEqual(\n+            offenders,\n+            [],\n+            \"repeat the SADR prefix so every cited number is independently resolvable\",\n+        )\n+\n     def test_carried_records_are_indexed(self) -> None:\n         # The two records this Goal carried onto the default branch, and which\n         # the scripts cite most, must be present.\n         index = _adr_index(REPO_ROOT)\n-        self.assertIn(\"0016\", index)\n-        self.assertIn(\"0017\", index)\n+        self.assertIn((\"SADR\", \"0016\"), index)\n+        self.assertIn((\"SADR\", \"0017\"), index)\n+\n+    def test_platform_tree_has_only_sadr_0001_through_0019(self) -> None:\n+        adr_dir = REPO_ROOT / \"docs\" / \"adr\"\n+        sadrs = sorted(adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\"))\n+        self.assertEqual(\n+            {path.name.split(\"-\", 2)[1] for path in sadrs},\n+            {f\"{number:04d}\" for number in range(1, 20)},\n+        )\n+        self.assertEqual(\n+            list(adr_dir.glob(\"[0-9][0-9][0-9][0-9]-*.md\")),\n+            [],\n+            \"the Shiki platform tree must not retain legacy numeric SADR paths\",\n+        )\n \n \n class FixtureAdrReferences(unittest.TestCase):\n@@ -180,6 +224,26 @@\n         self.assertIn(\"9991\", message)  # cited number\n         self.assertIn(\"docs/adr/9991-*.md\", message)  # missing path\n \n+    def test_target_adr_does_not_satisfy_missing_platform_sadr(self) -> None:\n+        root = self._make_repo(\n+            adr_names=[\"0042-target-decision.md\"],\n+            script_body='\"\"\"Target ADR 0042 exists, but platform SADR-0042 does not.\"\"\"\\n',\n+        )\n+        violations = find_adr_reference_violations(root, include_docs=False)\n+        self.assertEqual(len(violations), 1)\n+        self.assertEqual(violations[0].namespace, \"SADR\")\n+        self.assertEqual(violations[0].number, \"0042\")\n+        self.assertEqual(violations[0].expected_path, \"docs/adr/SADR-0042-*.md\")\n+\n+    def test_target_adr_and_platform_sadr_may_reuse_a_number(self) -> None:\n+        root = self._make_repo(\n+            adr_names=[\"0042-target-decision.md\", \"SADR-0042-platform-decision.md\"],\n+            script_body='\"\"\"Target ADR 0042 and platform SADR-0042 are distinct.\"\"\"\\n',\n+        )\n+        self.assertEqual(\n+            find_adr_reference_violations(root, include_docs=False), []\n+        )\n+\n     def test_uncited_adr_is_allowed(self) -> None:\n         # An ADR exists but nothing cites it -> not a violation. The converse\n         # (every ADR must be cited) is explicitly NOT required.\ndiff --git a/tests/test_guardian_review.py b/tests/test_guardian_review.py\nindex 88829e7810212635e2fc98aa5918d6f26294677d..00a85a237acd5bf2d6cb5b294128198c422402ca\n--- a/tests/test_guardian_review.py\n+++ b/tests/test_guardian_review.py\n@@ -162,7 +162,7 @@\n \n     def test_docs_only_is_light_and_not_conservative(self):\n         focus = gr.classify_review_focus([\"docs/adr/0099-x.md\", \"README.md\"], \"\", \"low\")\n-        self.assertTrue(any(\"Docs/ADR-only\" in f for f in focus))\n+        self.assertTrue(any(\"Docs/decision-record-only\" in f for f in focus))\n         self.assertFalse(any(\"conservative default\" in f.lower() for f in focus))\n \n     def test_unknown_category_is_conservative(self):\ndiff --git a/tests/test_policy_job_merged_prs.py b/tests/test_policy_job_merged_prs.py\nindex d45512e74681e5e649279236c7d4e79d5c3da471..b97989f8bd74d091de1a6b5275a20d8dc68c4512\n--- a/tests/test_policy_job_merged_prs.py\n+++ b/tests/test_policy_job_merged_prs.py\n@@ -3,8 +3,8 @@\n \n ``.github/workflows/shiki-cca-completion.yml`` has two consumers of the merged-PR\n proof: the CCA *signal* (``guardian_approval_signal.py`` in \"Prepare CCA context\")\n-and the authoritative gate (``mergegate_check.py`` in \"Enforce MergeGate\"). ADR\n-0017's bookkeeping-closeout exemption (condition 6) needs the merged implementation\n+and the authoritative gate (``mergegate_check.py`` in \"Enforce MergeGate\").\n+SADR-0017's bookkeeping-closeout exemption (condition 6) needs the merged implementation\n PR proven, and a closeout carries NO ``post_merge_reconcile`` marker. The signal\n already resolves ``MERGED_PRS`` unconditionally, but \"Enforce MergeGate\" used to\n resolve it only inside ``grep -q '<!-- shiki:post_merge_reconcile -->'``. So a\ndiff --git a/tests/test_sadr_install_migration.py b/tests/test_sadr_install_migration.py\nnew file mode 100644\nindex 0000000000000000000000000000000000000000..d51eac872b1c79ff4579c4c1401ca5d775a60d8d\n--- /dev/null\n+++ b/tests/test_sadr_install_migration.py\n@@ -0,0 +1,237 @@\n+\"\"\"SADR installation and collision-safe legacy upgrade behavior.\"\"\"\n+\n+from __future__ import annotations\n+\n+import contextlib\n+import hashlib\n+import io\n+import json\n+import shutil\n+import tempfile\n+import unittest\n+from pathlib import Path\n+\n+import shiki_test_support  # noqa: F401  (path bootstrap)\n+\n+import shiki_installer\n+from tests.test_adr_references import find_adr_reference_violations\n+\n+\n+PRODUCT_NUMBERS = tuple(range(13, 19))\n+\n+\n+def _quiet_install(target: Path, *, force: bool, validate: bool) -> None:\n+    with contextlib.redirect_stdout(io.StringIO()):\n+        shiki_installer.install_template(target, force=force, validate=validate)\n+\n+\n+def _sha256(path: Path) -> str:\n+    return hashlib.sha256(path.read_bytes()).hexdigest()\n+\n+\n+def _legacy_sadr_paths() -> tuple[str, ...]:\n+    return tuple(\n+        f\"docs/adr/{path.name.removeprefix('SADR-')}\"\n+        for path in sorted((shiki_installer.ROOT / \"docs\" / \"adr\").glob(\"SADR-*.md\"))\n+        if path.name != \"SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\"\n+    )\n+\n+\n+def _create_legacy_upgrade_fixture(\n+    target: Path,\n+) -> tuple[dict[str, bytes], tuple[str, ...]]:\n+    adr_dir = target / \"docs\" / \"adr\"\n+    adr_dir.mkdir(parents=True)\n+    product_bytes = {\n+        f\"{number:04d}-cubeshare-product-decision.md\": (\n+            f\"# CubeShare ADR {number:04d}\\n\\nDo not rewrite {number}.\\n\"\n+        ).encode()\n+        for number in PRODUCT_NUMBERS\n+    }\n+    for name, content in product_bytes.items():\n+        (adr_dir / name).write_bytes(content)\n+\n+    legacy_paths = _legacy_sadr_paths()\n+    for relative in legacy_paths:\n+        (target / relative).write_bytes(f\"legacy Shiki bytes for {relative}\\n\".encode())\n+\n+    stamp_path = target / shiki_installer.INSTALL_STAMP_PATH\n+    stamp_path.parent.mkdir(parents=True, exist_ok=True)\n+    stamp = {\n+        \"version\": shiki_installer.INSTALL_STAMP_VERSION,\n+        \"platform_commit\": None,\n+        \"installed_at\": \"2000-01-01T00:00:00+00:00\",\n+        \"digests\": {\n+            relative: _sha256(target / relative) for relative in legacy_paths\n+        },\n+    }\n+    stamp_path.write_text(\n+        json.dumps(stamp, indent=2, sort_keys=True) + \"\\n\", encoding=\"utf-8\"\n+    )\n+    return product_bytes, legacy_paths\n+\n+\n+def _tree_snapshot(root: Path) -> dict[str, tuple[str, bytes | str]]:\n+    \"\"\"Recursive byte/shape snapshot used to prove preflight is write-free.\"\"\"\n+    snapshot: dict[str, tuple[str, bytes | str]] = {}\n+    for path in sorted(root.rglob(\"*\")):\n+        relative = path.relative_to(root).as_posix()\n+        if path.is_symlink():\n+            snapshot[relative] = (\"symlink\", path.readlink().as_posix())\n+        elif path.is_dir():\n+            snapshot[relative] = (\"directory\", \"\")\n+        else:\n+            snapshot[relative] = (\"file\", path.read_bytes())\n+    return snapshot\n+\n+\n+class FreshSadrInstallTests(unittest.TestCase):\n+    def setUp(self) -> None:\n+        self.tmp = Path(tempfile.mkdtemp(prefix=\"shiki-sadr-fresh-\"))\n+        self.addCleanup(lambda: shutil.rmtree(self.tmp, ignore_errors=True))\n+        self.target = self.tmp / \"target\"\n+        self.adr_dir = self.target / \"docs\" / \"adr\"\n+        self.adr_dir.mkdir(parents=True)\n+        self.product_bytes = {\n+            f\"{number:04d}-product-decision.md\": (\n+                f\"# Product ADR {number:04d}\\n\\nTarget-owned bytes {number}.\\n\"\n+            ).encode()\n+            for number in PRODUCT_NUMBERS\n+        }\n+        for name, content in self.product_bytes.items():\n+            (self.adr_dir / name).write_bytes(content)\n+\n+    def test_fresh_install_preserves_product_adrs_and_installs_all_sadrs(self) -> None:\n+        _quiet_install(self.target, force=False, validate=True)\n+\n+        for name, expected in self.product_bytes.items():\n+            self.assertEqual((self.adr_dir / name).read_bytes(), expected)\n+\n+        sadr_numbers = {\n+            path.name.split(\"-\", 2)[1]\n+            for path in self.adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\")\n+        }\n+        self.assertEqual(sadr_numbers, {f\"{number:04d}\" for number in range(1, 20)})\n+\n+        numeric_records = {\n+            path.name\n+            for path in self.adr_dir.glob(\"[0-9][0-9][0-9][0-9]-*.md\")\n+        }\n+        self.assertEqual(numeric_records, set(self.product_bytes))\n+        stamp = json.loads(\n+            (self.target / shiki_installer.INSTALL_STAMP_PATH).read_text(\n+                encoding=\"utf-8\"\n+            )\n+        )\n+        for path in self.adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\"):\n+            relative = path.relative_to(self.target).as_posix()\n+            self.assertEqual(stamp[\"digests\"][relative], _sha256(path))\n+        self.assertEqual(find_adr_reference_violations(self.target), [])\n+\n+\n+class LegacySadrUpgradeTests(unittest.TestCase):\n+    def setUp(self) -> None:\n+        self.tmp = Path(tempfile.mkdtemp(prefix=\"shiki-sadr-upgrade-\"))\n+        self.addCleanup(lambda: shutil.rmtree(self.tmp, ignore_errors=True))\n+        self.target = self.tmp / \"target\"\n+        self.adr_dir = self.target / \"docs\" / \"adr\"\n+        self.product_bytes, self.legacy_paths = _create_legacy_upgrade_fixture(\n+            self.target\n+        )\n+\n+    def test_force_upgrade_deletes_only_stamped_legacy_sadrs(self) -> None:\n+        _quiet_install(self.target, force=True, validate=True)\n+\n+        for relative in self.legacy_paths:\n+            self.assertFalse((self.target / relative).exists(), relative)\n+        for name, expected in self.product_bytes.items():\n+            self.assertEqual((self.adr_dir / name).read_bytes(), expected)\n+\n+        sadrs = list(self.adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\"))\n+        self.assertEqual(len(sadrs), 19)\n+        refreshed = json.loads(\n+            (self.target / shiki_installer.INSTALL_STAMP_PATH).read_text(encoding=\"utf-8\")\n+        )\n+        for relative in self.legacy_paths:\n+            self.assertNotIn(relative, refreshed[\"digests\"])\n+        for path in sadrs:\n+            relative = path.relative_to(self.target).as_posix()\n+            self.assertEqual(refreshed[\"digests\"][relative], _sha256(path))\n+\n+    def test_non_force_install_never_deletes_legacy_files(self) -> None:\n+        before = {\n+            relative: (self.target / relative).read_bytes()\n+            for relative in self.legacy_paths\n+        }\n+        _quiet_install(self.target, force=False, validate=False)\n+        for relative, expected in before.items():\n+            self.assertEqual((self.target / relative).read_bytes(), expected)\n+        refreshed = json.loads(\n+            (self.target / shiki_installer.INSTALL_STAMP_PATH).read_text(encoding=\"utf-8\")\n+        )\n+        for relative in self.legacy_paths:\n+            self.assertEqual(refreshed[\"digests\"][relative], _sha256(self.target / relative))\n+\n+\n+class LegacyCleanupPreflightTests(unittest.TestCase):\n+    def setUp(self) -> None:\n+        self.tmp = Path(tempfile.mkdtemp(prefix=\"shiki-sadr-preflight-\"))\n+        self.addCleanup(lambda: shutil.rmtree(self.tmp, ignore_errors=True))\n+\n+    def test_all_ownership_blockers_abort_before_any_write(self) -> None:\n+        cases = (\n+            (\"absent\", \"install stamp is absent\"),\n+            (\"unreadable\", \"install stamp is unreadable\"),\n+            (\"malformed\", \"install stamp is malformed\"),\n+            (\"missing-digest\", \"install stamp has no digest\"),\n+            (\"digest-mismatch\", \"digest mismatch\"),\n+        )\n+        for name, expected_reason in cases:\n+            with self.subTest(name=name):\n+                target = self.tmp / name\n+                _, legacy_paths = _create_legacy_upgrade_fixture(target)\n+                stamp_path = target / shiki_installer.INSTALL_STAMP_PATH\n+\n+                if name == \"absent\":\n+                    stamp_path.unlink()\n+                elif name == \"unreadable\":\n+                    stamp_path.unlink()\n+                    stamp_path.mkdir()\n+                elif name == \"malformed\":\n+                    stamp_path.write_text(\"{not-json\\n\", encoding=\"utf-8\")\n+                else:\n+                    stamp = json.loads(stamp_path.read_text(encoding=\"utf-8\"))\n+                    if name == \"missing-digest\":\n+                        stamp[\"digests\"] = {}\n+                    else:\n+                        stamp[\"digests\"] = {\n+                            relative: \"deadbeef\" for relative in legacy_paths\n+                        }\n+                    stamp_path.write_text(\n+                        json.dumps(stamp, indent=2, sort_keys=True) + \"\\n\",\n+                        encoding=\"utf-8\",\n+                    )\n+\n+                before = _tree_snapshot(target)\n+                with self.assertRaises(shiki_installer.ShikiError) as raised:\n+                    _quiet_install(target, force=True, validate=False)\n+                after = _tree_snapshot(target)\n+\n+                self.assertEqual(before, after, f\"{name} preflight changed the tree\")\n+                message = str(raised.exception)\n+                self.assertIn(expected_reason, message)\n+                self.assertIn(\"No files were written\", message)\n+                for relative in legacy_paths:\n+                    self.assertIn(relative, message)\n+\n+    def test_absent_stamp_is_allowed_when_no_legacy_path_exists(self) -> None:\n+        target = self.tmp / \"no-legacy\"\n+        target.mkdir()\n+        _quiet_install(target, force=True, validate=False)\n+        self.assertTrue(\n+            (target / \"docs\" / \"adr\" / \"SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\").is_file()\n+        )\n+\n+\n+if __name__ == \"__main__\":\n+    unittest.main()\n\ndiff --git a/.codex/skills/shiki/SKILL.md b/.codex/skills/shiki/SKILL.md\nindex c8e2e85d9d75d49cc4f2d6286ee22659c4e53e75..93cded623b2c15d9f65c76491a1c2d813ac9e6c0\n--- a/.codex/skills/shiki/SKILL.md\n+++ b/.codex/skills/shiki/SKILL.md\n@@ -18,6 +18,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n Codex CLI does not register this skill as a custom `/shiki` slash command. When\n the user types `/shiki` in Codex CLI and gets \"Unrecognized command\", explain\n that this is expected: invoke Shiki in Codex with natural language such as\n@@ -69,7 +72,7 @@\n \n ## Responsibilities\n \n-- Claude Code implements and repairs by default (`shiki runner claude`, ADR 0008); Codex implements and repairs tasks explicitly assigned to `codex`.\n+- Claude Code implements and repairs by default (`shiki runner claude`, SADR-0008); Codex implements and repairs tasks explicitly assigned to `codex`.\n - Claude Code Action can act as GitHub-side CCA or reviewer.\n - CCA judges completion.\n - MergeGate authorizes state transitions and merge readiness.\ndiff --git a/.github/prompts/cca-completion-check.md b/.github/prompts/cca-completion-check.md\nindex ed203ad5f302ecc81372d8ab27d2b1232ca97497..fd89950528333ef6242e6d5443904f5392ab7e56\n--- a/.github/prompts/cca-completion-check.md\n+++ b/.github/prompts/cca-completion-check.md\n@@ -19,6 +19,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n ## Required Reading\n \n Read only the files needed to judge the PR contract. Prefer this order:\ndiff --git a/.github/prompts/codex-task-handoff.md b/.github/prompts/codex-task-handoff.md\nindex 695e211d76dea1c60e8d5bb360e78487808286a4..59441d576b01a39569722f226c854e5a740e9eb5\n--- a/.github/prompts/codex-task-handoff.md\n+++ b/.github/prompts/codex-task-handoff.md\n@@ -2,7 +2,7 @@\n \n You are Codex Front acting as the Implementer for a Shiki task.\n \n-Follow `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant ADRs, and `docs/agents/codex-handoff.md`.\n+Follow `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant Shiki SADRs and target ADRs, and `docs/agents/codex-handoff.md`.\n \n Rules:\n \ndiff --git a/.shiki/ledger/L-20260807T082800000000Z-c0de0019.json b/.shiki/ledger/L-20260807T082800000000Z-c0de0019.json\nnew file mode 100644\nindex 0000000000000000000000000000000000000000..bac355ecea44037859b3715f58bc32f3353cfcfa\n--- /dev/null\n+++ b/.shiki/ledger/L-20260807T082800000000Z-c0de0019.json\n@@ -0,0 +1,34 @@\n+{\n+  \"actor\": \"codex\",\n+  \"evidence\": [\n+    \"Full task diff reviewed against scope, non-goals, locks, acceptance checks, failure atomicity, path ownership, and compatibility requirements\",\n+    \"Finding repaired: tests/test_sadr_install_migration.py now asserts every fresh-installed SADR path has the matching SHA-256 digest in .shiki/install-stamp.json\",\n+    \"Re-review found no remaining blocking issues; rejected findings: none\",\n+    \"python3 -m unittest discover -s tests: 1147 tests passed\",\n+    \"bash scripts/test_shiki_control_plane.sh: passed\",\n+    \"bash scripts/test_shiki_adversarial_state.sh: passed\",\n+    \"python3 scripts/validate_shiki.py --target .: passed\"\n+  ],\n+  \"goal_id\": \"G-20260807T073307219752Z-1ee1ed68\",\n+  \"id\": \"L-20260807T082800000000Z-c0de0019\",\n+  \"links\": [],\n+  \"review\": {\n+    \"applied_fixes\": [\n+      \"Added direct fresh-install stamp path/digest assertions and reran focused plus full verification\"\n+    ],\n+    \"findings\": [\n+      {\n+        \"detail\": \"The fresh-install test proved SADR files were installed but did not directly prove their paths and digests were recorded in the refreshed install stamp.\",\n+        \"resolution\": \"fixed_and_re_reviewed\",\n+        \"severity\": \"blocking\",\n+        \"title\": \"Fresh-install stamp evidence was indirect\"\n+      }\n+    ],\n+    \"rejected_findings\": [],\n+    \"verdict\": \"clean_after_repairs\"\n+  },\n+  \"summary\": \"code-review implementer verdict CLEAN after re-review: findings=1; applied_fixes=1 (fresh-install stamp-digest assertion); rejected_findings=0 (none)\",\n+  \"task_id\": \"T-20260807T073307221254Z-2d22d654\",\n+  \"timestamp\": \"2026-08-07T08:28:00+00:00\",\n+  \"type\": \"review\"\n+}\ndiff --git a/.shiki/ledger/L-20260807T082914000000Z-7dd00019.json b/.shiki/ledger/L-20260807T082914000000Z-7dd00019.json\nnew file mode 100644\nindex 0000000000000000000000000000000000000000..432bcd63849c04eaaeaca3993c03a07c1152bb1a\n--- /dev/null\n+++ b/.shiki/ledger/L-20260807T082914000000Z-7dd00019.json\n@@ -0,0 +1,19 @@\n+{\n+  \"actor\": \"codex\",\n+  \"evidence\": [\n+    \"RED resolver: the target-ADR-does-not-satisfy-SADR fixture expected one missing SADR violation but received zero before namespace-aware indexing\",\n+    \"RED fresh install: the product-ADR fixture installed no SADR records before the platform rename\",\n+    \"RED forced upgrade: matching stamped legacy Shiki ADR paths remained before cleanup was implemented\",\n+    \"RED non-force upgrade: refreshed stamp omitted preserved legacy ownership evidence before matching digest carry-forward was implemented\",\n+    \"GREEN focused: 11 ADR/SADR resolver tests and 5 fresh-install/upgrade/preflight tests passed\",\n+    \"REFACTOR: separated namespace indexing, exact legacy-path inventory, read-only cleanup authorization, and stamp refresh responsibilities\",\n+    \"GREEN regression: 1147 unit tests, control-plane shell suite, adversarial shell suite, and validate_shiki passed\"\n+  ],\n+  \"goal_id\": \"G-20260807T073307219752Z-1ee1ed68\",\n+  \"id\": \"L-20260807T082914000000Z-7dd00019\",\n+  \"links\": [],\n+  \"summary\": \"tdd red-green-refactor evidence for separate ADR/SADR resolution and collision-safe install migration\",\n+  \"task_id\": \"T-20260807T073307221254Z-2d22d654\",\n+  \"timestamp\": \"2026-08-07T08:29:14+00:00\",\n+  \"type\": \"check\"\n+}\ndiff --git a/.shiki/tasks/T-20260807T073307221254Z-2d22d654.json b/.shiki/tasks/T-20260807T073307221254Z-2d22d654.json\nindex e7e150cc1637f1cfac2199e04055e93b53630dcb..00241552cac63069660eb722bd076decf6a5031d\n--- a/.shiki/tasks/T-20260807T073307221254Z-2d22d654.json\n+++ b/.shiki/tasks/T-20260807T073307221254Z-2d22d654.json\n@@ -36,7 +36,9 @@\n     \"L-20260807T080107948254Z-61f24da0\",\n     \"L-20260807T080228312210Z-07ac626e\",\n     \"L-20260807T080228575038Z-d574f629\",\n-    \"L-20260807T080256330734Z-5bf40cb6\"\n+    \"L-20260807T080256330734Z-5bf40cb6\",\n+    \"L-20260807T082800000000Z-c0de0019\",\n+    \"L-20260807T082914000000Z-7dd00019\"\n   ],\n   \"locks\": [\n     \"path:.codex/**\",\ndiff --git a/AGENTS.md b/AGENTS.md\nindex 7f041100d49c2a7525f51cb0f5d696a4a6d1c78c..a06bfbabc6fa48a007389e25e12d7ed523936bed\n--- a/AGENTS.md\n+++ b/AGENTS.md\n@@ -22,7 +22,7 @@\n - **Codex Front** is an optional implementer through Codex App, Codex CLI, Codex IDE extension, or Codex Web signed in with ChatGPT OAuth/subscription auth, dispatched with `shiki runner codex` when a task is explicitly assigned to `codex`.\n - **GitHub CCA** is implemented by Claude Code Action by default, using `CLAUDE_CODE_OAUTH_TOKEN`.\n \n-Do not assume `openai/codex-action`, `OPENAI_API_KEY`, or API-key based Codex automation in the default Shiki path. API-key runners are explicit target-repo extensions and require their own ADR.\n+Do not assume `openai/codex-action`, `OPENAI_API_KEY`, or API-key based Codex automation in the default Shiki path. API-key runners are explicit target-repo extensions and require their own target ADR.\n \n Shiki is not:\n \n@@ -44,10 +44,14 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use\n+`SADR-NNNN-*.md`; target product decisions retain `NNNN-*.md` and the `ADR`\n+namespace.\n+\n Use the canonical source-of-truth order above. Runtime-specific reading orders\n are operational guidance only and must not override or reorder it.\n \n-Conversation state is not durable truth. If a decision matters, put it in GitHub, `.shiki/`, `CONTEXT.md`, or an ADR.\n+Conversation state is not durable truth. If a decision matters, put it in GitHub, `.shiki/`, `CONTEXT.md`, a Shiki SADR, or a target ADR.\n \n If GitHub and `.shiki/` disagree, surface the conflict and prefer GitHub operational state until the mirror is repaired.\n \n@@ -75,8 +79,8 @@\n Every non-trivial change follows this loop:\n \n 1. **Goal**: clarify outcome, completion conditions, non-goals, risk level, and success signals.\n-2. **grill-with-docs**: challenge the plan against domain docs, ADRs, code reality, terminology, and edge scenarios.\n-3. **Context & Impact**: find relevant docs, ADRs, code, dependencies, owners, locks, and verification surfaces.\n+2. **grill-with-docs**: challenge the plan against domain docs, Shiki SADRs, target ADRs, code reality, terminology, and edge scenarios.\n+3. **Context & Impact**: find relevant docs, SADRs, target ADRs, code, dependencies, owners, locks, and verification surfaces.\n 4. **PRD and Spec Freeze**: use `to-prd` when context is settled enough to become durable product/engineering intent. The operator's explicit approval of the PRD is Spec Freeze: the plan records a `spec_freeze` block, and no plan runs without it. Steps 1-4 form Requirements Definition \u2014 one continuous operator dialogue. After Spec Freeze, scope changes require an operator-approved Spec Amendment; non-scope-moving interpretations go to the Assumption Log.\n 5. **Issues**: use `to-issues` to create independently grabbable vertical-slice issues. Prefer AFK slices over HITL slices where possible.\n 6. **Triage**: label issues for readiness, risk, runtime, skills, and MergeGate state.\n@@ -167,7 +171,7 @@\n - Testing decisions.\n - Out-of-scope items.\n - Further notes.\n-- Links to relevant domain terms and ADRs.\n+- Links to relevant domain terms, SADRs, and target ADRs.\n \n Every executable task must include:\n \n@@ -193,7 +197,7 @@\n \n Context & Impact must identify:\n \n-- Relevant documents, domain terms, ADRs, and past decisions.\n+- Relevant documents, domain terms, SADRs, target ADRs, and past decisions.\n - Relevant modules, interfaces, seams, callers, owners, and tests.\n - Dependency relationships between tasks.\n - Candidate locks and lock conflicts.\n@@ -210,7 +214,7 @@\n | Skill | Required when |\n | --- | --- |\n | `setup-matt-pocock-skills` | First configuring the repo for skills, or when issue tracker, triage labels, or domain docs are missing. |\n-| `grill-with-docs` | Default for non-trivial Goals before PRD/issues; always required when plans, terminology, boundaries, tradeoffs, or ADR-worthy decisions are ambiguous. |\n+| `grill-with-docs` | Default for non-trivial Goals before PRD/issues; always required when plans, terminology, boundaries, tradeoffs, or decision-record-worthy choices are ambiguous. |\n | `zoom-out` | The agent lacks an architectural map of the relevant code area. |\n | `to-prd` | Settled Goal context must become durable product/engineering intent. |\n | `to-issues` | A Goal, PRD, or plan must become independently grabbable vertical-slice issues. |\n@@ -348,7 +352,7 @@\n - Three or more new files.\n - Any high-risk or critical label.\n \n-When the architecture gate triggers, run `grill-with-docs` or `improve-codebase-architecture` as appropriate and record durable decisions in an ADR when needed.\n+When the architecture gate triggers, run `grill-with-docs` or `improve-codebase-architecture` as appropriate and record durable decisions in a Shiki SADR or target ADR as applicable.\n \n ## Engineering Discipline\n \ndiff --git a/CLAUDE.md b/CLAUDE.md\nindex 98a932e7841a632866fcd53b0bad0e3af3711d3d..98a31399404d6fb044b4fd493f4459ac87f439de\n--- a/CLAUDE.md\n+++ b/CLAUDE.md\n@@ -14,6 +14,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n ## Claude Role\n \n Default to planner, default implementer, reviewer, coordinator, documentation editor, CCA-style completion judge, and final-judgment assistant.\n@@ -57,7 +60,7 @@\n At the start of a Shiki session:\n \n 1. Read `AGENTS.md`.\n-2. Read `CONTEXT.md` and relevant ADRs.\n+2. Read `CONTEXT.md` and relevant Shiki SADRs and target ADRs.\n 3. Read active `.shiki/` Goal/task/ledger state when present.\n 4. Read active distilled rules from `.shiki/memories/` (status `distilled`, `active: true`, not revoked or superseded) for applicable guidance; the same rules are injected deterministically into task handoffs as the `## Distilled Rules` section (\u00a73.5).\n 5. Read the GitHub Issue or PR when the task is GitHub-backed.\n@@ -74,7 +77,7 @@\n - Define the Goal, completion conditions, non-goals, and risk level.\n - Treat `grill-with-docs` as the default planning entry point for non-trivial Goals.\n - Challenge domain terms against `CONTEXT.md`.\n-- Challenge hard-to-reverse tradeoffs against ADRs.\n+- Challenge hard-to-reverse tradeoffs against SADRs and target ADRs.\n - Explore code instead of asking when code can answer the question.\n - Run Context & Impact before implementation planning.\n - Produce a Task DAG, not an unordered checklist.\n@@ -94,7 +97,7 @@\n - For each question, provide your recommended answer.\n - Resolve dependency decisions before downstream design questions.\n - Update `CONTEXT.md` inline when terminology crystallizes.\n-- Offer ADRs only when the decision is hard to reverse, surprising without context, and a real tradeoff.\n+- Offer a SADR or target ADR only when the decision is hard to reverse, surprising without context, and a real tradeoff.\n - Convert unresolved questions into HITL blockers rather than allowing Codex to guess.\n \n ## PRD and Issues Standard\n@@ -103,7 +106,7 @@\n \n - Use the project's domain glossary vocabulary.\n - Avoid volatile file paths unless they are essential.\n-- Include implementation decisions, testing decisions, out-of-scope items, and relevant ADR/domain links.\n+- Include implementation decisions, testing decisions, out-of-scope items, and relevant SADR/target-ADR/domain links.\n - Identify deep-module opportunities and testable interfaces.\n \n When creating issues:\n@@ -121,7 +124,7 @@\n - Branch/worktree target.\n - Scope and non-goals.\n - Dependencies and locks.\n-- Relevant docs, ADRs, modules, and tests.\n+- Relevant docs, SADRs, target ADRs, modules, and tests.\n - Required skill invocations.\n - TDD expectations.\n - Pre-PR `code-review` gate expectations.\n@@ -152,7 +155,7 @@\n \n - Treat yourself as a judge, not implementer.\n - Do not write production code.\n-- Read `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant ADRs, PRD, task issue, PR body, diff, checks, reviews, labels, and `.shiki/` state.\n+- Read `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant SADRs and target ADRs, PRD, task issue, PR body, diff, checks, reviews, labels, and `.shiki/` state.\n - Evaluate every applicable checklist in `docs/agents/checklists.md`.\n - Map each acceptance criterion to durable evidence.\n - Distinguish implementation failure from missing evidence.\ndiff --git a/CONTEXT.md b/CONTEXT.md\nindex e61a9be06a0cce0d026484083a657bde2067e234..237ee8aa3ea8fab334ece49768bc5ed3b5af0974\n--- a/CONTEXT.md\n+++ b/CONTEXT.md\n@@ -25,7 +25,7 @@\n _Avoid_: jumping directly into code\n \n **grill-with-docs**:\n-A planning skill that challenges a plan against domain language, ADRs, code reality, and concrete edge scenarios. It resolves design-tree questions before PRD/issues. It runs inside Requirements Definition.\n+A planning skill that challenges a plan against domain language, Shiki SADRs, target ADRs, code reality, and concrete edge scenarios. It resolves design-tree questions before PRD/issues. It runs inside Requirements Definition.\n _Avoid_: generic brainstorming, silent assumptions\n \n **Requirements Definition**:\ndiff --git a/CONTRIBUTING.md b/CONTRIBUTING.md\nindex c6d9b656e73abb8a55e5f660123e03153dcb3e6d..9e37a8fbf668db5b22461aaae5548b8f1354394c\n--- a/CONTRIBUTING.md\n+++ b/CONTRIBUTING.md\n@@ -17,7 +17,7 @@\n 1. **Goal** \u2014 start from a user-approved Goal with an outcome, completion\n    conditions, non-goals, risk level, and success signals.\n 2. **grill-with-docs** \u2014 for any non-trivial change, challenge the plan against\n-   domain language, ADRs, and code reality before writing a PRD.\n+   domain language, Shiki SADRs, target ADRs, and code reality before writing a PRD.\n 3. **PRD** \u2014 capture settled product/engineering intent (`to-prd`).\n 4. **Spec Freeze** \u2014 the operator explicitly approves the PRD/requirements;\n    the plan records a `spec_freeze` block and no plan runs without it\n@@ -94,7 +94,7 @@\n   entries.\n - Do not hand-edit `.shiki/goals/*.json` or `.shiki/manifest.json` in a task PR.\n - Conversation state is not durable truth. If a decision matters, record it in\n-  GitHub, `.shiki/`, `CONTEXT.md`, or an ADR.\n+  GitHub, `.shiki/`, `CONTEXT.md`, a Shiki SADR, or a target ADR.\n \n ## Architecture Gate\n \n@@ -103,7 +103,7 @@\n or CI policy, package structure, three or more structural files, more than 100\n net new lines in a core module, three or more new files, or any high-risk label.\n When triggered, run `grill-with-docs` or `improve-codebase-architecture` and\n-record durable decisions in an ADR when needed (see `docs/adr/`).\n+record durable decisions in a Shiki SADR or target ADR as applicable (see `docs/adr/`).\n \n ## Safety\n \ndiff --git a/SYSTEM_PROMPT.md b/SYSTEM_PROMPT.md\nindex aaaea45e3a896c3a44ba89eec8bb84325ecf6c62..da009c86a88285269dee0155e58e550c6d744ea9\n--- a/SYSTEM_PROMPT.md\n+++ b/SYSTEM_PROMPT.md\n@@ -30,6 +30,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n Use the canonical source-of-truth order above. Runtime-specific reading orders\n are operational guidance only and must not override or reorder it. The current\n conversation is non-durable operator input.\n@@ -48,14 +51,14 @@\n \n A runtime may only do work that is explicitly assigned by the Goal, task, PR, or MergeGate decision.\n \n-Do not assume `openai/codex-action`, `OPENAI_API_KEY`, or API-key based Codex automation in the default Shiki path. API-key runners are explicit target-repo extensions and require their own ADR.\n+Do not assume `openai/codex-action`, `OPENAI_API_KEY`, or API-key based Codex automation in the default Shiki path. API-key runners are explicit target-repo extensions and require their own target ADR.\n \n ## 4. Non-negotiable Shiki flow\n \n For any non-trivial Goal, use this flow:\n \n 1. **Goal Seek**: define outcome, non-goals, risk level, completion criteria, and evidence requirements.\n-2. **grill-with-docs**: challenge the plan against `CONTEXT.md`, ADRs, code reality, terminology, boundaries, and edge scenarios. Ask one question at a time when the operator is available. If code can answer a question, inspect code instead of asking.\n+2. **grill-with-docs**: challenge the plan against `CONTEXT.md`, Shiki SADRs, target ADRs, code reality, terminology, boundaries, and edge scenarios. Ask one question at a time when the operator is available. If code can answer a question, inspect code instead of asking.\n 3. **Context & Impact**: identify modules, interfaces, seams, callers, dependencies, locks, risk, and verification surfaces.\n 4. **to-prd + Spec Freeze**: turn settled context into a PRD using domain vocabulary and testing decisions. The operator's explicit approval of the PRD is Spec Freeze, recorded as the plan's `spec_freeze` block; plans without it do not run. Steps 1-4 form Requirements Definition.\n 5. **to-issues**: decompose the PRD into independently grabbable vertical-slice issues. Prefer AFK slices; mark HITL slices when judgment is still required.\n@@ -75,7 +78,7 @@\n Before making any material change:\n \n 1. Read `AGENTS.md` and any runtime-specific wrapper such as `CLAUDE.md`.\n-2. Read `CONTEXT.md` and relevant ADRs.\n+2. Read `CONTEXT.md` and relevant SADRs and target ADRs.\n 3. Read the GitHub Issue / PR and `.shiki/` state for the active Goal or task.\n 4. Identify your role: Planner, Implementer, Reviewer, Completion Check Agent, Repairer, or Guardian-assist.\n 5. Identify Goal id, task id, branch, PR, dependencies, locks, risk level, required skills, and required evidence.\n@@ -103,7 +106,7 @@\n \n At minimum, identify:\n \n-- Relevant documents, ADRs, domain terms, and prior decisions.\n+- Relevant documents, SADRs, target ADRs, domain terms, and prior decisions.\n - Relevant modules, interfaces, seams, callers, owners, and tests.\n - Dependency relationships between tasks.\n - Candidate file locks and lock conflicts.\n@@ -152,7 +155,7 @@\n \n CCA must read:\n \n-- `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant ADRs.\n+- `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant SADRs and target ADRs.\n - Parent Goal issue, PRD issue, task issue, comments, and labels.\n - PR body, diff, commits, changed files, reviews, CI checks, and artifacts available to the workflow.\n - `.shiki/` task contract, locks, ledger entries, CCA checklist profile, and prior repair packets.\n@@ -214,7 +217,7 @@\n - Three or more new files.\n - Any high-risk or critical label.\n \n-When the architecture gate triggers, run `grill-with-docs` or `improve-codebase-architecture` as appropriate and record the decision in an ADR if it is hard to reverse or likely to be re-litigated.\n+When the architecture gate triggers, run `grill-with-docs` or `improve-codebase-architecture` as appropriate and record the decision in a Shiki SADR or target ADR as applicable if it is hard to reverse or likely to be re-litigated.\n \n ## 14. Skill Gate\n \n@@ -223,7 +226,7 @@\n Use these skills:\n \n - `setup-matt-pocock-skills`: before first use of the engineering skill set, or when issue tracker, triage labels, or domain docs are missing.\n-- `grill-with-docs`: default for non-trivial Goals before PRD/issues; required for ambiguous plans, terminology, boundaries, ADR-worthy tradeoffs, or design-tree decisions.\n+- `grill-with-docs`: default for non-trivial Goals before PRD/issues; required for ambiguous plans, terminology, boundaries, decision-record-worthy tradeoffs, or design-tree decisions.\n - `zoom-out`: unfamiliar code area or missing architectural map.\n - `to-prd`: convert settled context into a PRD.\n - `to-issues`: break a PRD, plan, or Goal into independently grabbable vertical-slice issues.\ndiff --git a/docs/adr/0001-keep-shiki-as-platform-name.md b/docs/adr/SADR-0001-keep-shiki-as-platform-name.md\nindex 20a9637c3b9d8c97de7ba8db7cfae4d82d9a7dba..8b685439959aada1b221c112f0ac3c51ca8a1695\n--- a/docs/adr/0001-keep-shiki-as-platform-name.md\n+++ b/docs/adr/SADR-0001-keep-shiki-as-platform-name.md\n@@ -1,3 +1,3 @@\n-# Keep Shiki As The Platform Name\n+# SADR-0001: Keep Shiki As The Platform Name\n \n Shiki remains the product name for the GitHub-native agentic engineering control plane. We considered more descriptive names such as MergePilot, MergeGate, and Kiroku, but kept Shiki because it carries the existing goal-seeking framework identity while remaining independent of Hermes, Codex, Claude Code, or any single runtime.\ndiff --git a/docs/adr/0002-use-agents-as-shared-constitution.md b/docs/adr/SADR-0002-use-agents-as-shared-constitution.md\nindex 73de30ecb078120eac540d9bc51855e3cd48d0e3..c5486182bf06fca36ea508496b013c67302fd989\n--- a/docs/adr/0002-use-agents-as-shared-constitution.md\n+++ b/docs/adr/SADR-0002-use-agents-as-shared-constitution.md\n@@ -1,3 +1,3 @@\n-# Use AGENTS As The Shared Agent Constitution\n+# SADR-0002: Use AGENTS As The Shared Agent Constitution\n \n Shiki uses `AGENTS.md` as the runtime-neutral constitution and `CLAUDE.md` as a thin Claude Code wrapper. This keeps Codex, Claude Code, Hermes Runner, and future Agent Runtimes aligned on the same GitHub-first MergeGate rules while still allowing runtime-specific instructions where needed.\ndiff --git a/docs/adr/0003-require-skill-gate-for-mergegate-readiness.md b/docs/adr/SADR-0003-require-skill-gate-for-mergegate-readiness.md\nindex 424be0c9af4c8f0136f1fde68a468c47ff84e764..7fc337987c7df9792f75f181dfcbed75fe5ffff2\n--- a/docs/adr/0003-require-skill-gate-for-mergegate-readiness.md\n+++ b/docs/adr/SADR-0003-require-skill-gate-for-mergegate-readiness.md\n@@ -1,3 +1,3 @@\n-# Require Skill Gate For MergeGate Readiness\n+# SADR-0003: Require Skill Gate For MergeGate Readiness\n \n Shiki requires relevant engineering skills to be invoked before execution when their triggers apply. This makes skills part of MergeGate readiness rather than optional prompt style, because Shiki depends on repeatable planning, issue decomposition, TDD, diagnosis, triage, and architecture review before autonomous or parallel agent work can be trusted.\ndiff --git a/docs/adr/0004-use-github-first-with-shiki-mirror.md b/docs/adr/SADR-0004-use-github-first-with-shiki-mirror.md\nindex d0a3a9174dc646a945a9d325954ffaf00c352040..a79c54ae9ee8eed2a0524192b864da4e22a92d3f\n--- a/docs/adr/0004-use-github-first-with-shiki-mirror.md\n+++ b/docs/adr/SADR-0004-use-github-first-with-shiki-mirror.md\n@@ -1,4 +1,4 @@\n-# ADR 0004: Use GitHub First With A Repository-Local Shiki Mirror\n+# SADR-0004: Use GitHub First With A Repository-Local Shiki Mirror\n \n ## Status\n \ndiff --git a/docs/adr/0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md b/docs/adr/SADR-0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\nindex b339baf49b89d9290e79c3d51b36e7b2bde3bd95..0f3c3eb9afccdb3403ce978e21d04fe12e495da0\n--- a/docs/adr/0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\n+++ b/docs/adr/SADR-0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\n@@ -1,8 +1,8 @@\n-# ADR 0005: Use Codex As Front And Claude Code Action As The GitHub Runtime\n+# SADR-0005: Use Codex As Front And Claude Code Action As The GitHub Runtime\n \n ## Status\n \n-Accepted (implementer default superseded by ADR 0008; the auth model and the\n+Accepted (implementer default superseded by SADR-0008; the auth model and the\n Claude Code Action GitHub runtime decision remain in force)\n \n ## Context\n@@ -26,4 +26,4 @@\n - Shiki avoids requiring `OPENAI_API_KEY` for the default workflow.\n - Codex work happens through the user's authenticated Codex front surface, not as a hidden backend Action.\n - Claude Code Action is configured for OAuth-token based subscription usage by default.\n-- API-key based automation remains possible only as an explicit target-repo extension with its own ADR.\n+- API-key based automation remains possible only as an explicit target-repo extension with its own target ADR.\ndiff --git a/docs/adr/0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md b/docs/adr/SADR-0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\nindex 4bcae9d1e52f0a37b8e9fbafa5cd14b6ee1993bd..7a38538a6ae9929567f0ade67e7ea6a60d0b3f7e\n--- a/docs/adr/0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\n+++ b/docs/adr/SADR-0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\n@@ -1,4 +1,4 @@\n-# ADR 0006: Separate Codex Implementation, CCA Completion, And MergeGate Authorization\n+# SADR-0006: Separate Codex Implementation, CCA Completion, And MergeGate Authorization\n \n ## Status\n \ndiff --git a/docs/adr/0007-packaging-and-release.md b/docs/adr/SADR-0007-packaging-and-release.md\nindex 79b84ddb97167372b44641e5b7860e400ce80822..bb445a2e2e856d9d1f5fdbe35de8d4e8cec0fb32\n--- a/docs/adr/0007-packaging-and-release.md\n+++ b/docs/adr/SADR-0007-packaging-and-release.md\n@@ -1,4 +1,4 @@\n-# ADR 0007: Packaging And Release For Shiki\n+# SADR-0007: Packaging And Release For Shiki\n \n ## Status\n \n@@ -25,7 +25,7 @@\n   not introduce third-party runtime dependencies or break the shim.\n - Releases must run inside the operator's subscription-authenticated toolchain\n   and must not require extra secrets beyond the built-in `GITHUB_TOKEN`\n-  (consistent with ADR 0005's no-API-key-by-default stance).\n+  (consistent with SADR-0005's no-API-key-by-default stance).\n - `.shiki/manifest.json` already encodes the install surface (`install.include`,\n   `install.create_directories`, `install.exclude_from_commit`), and it must not\n   be mutated as part of this change.\n@@ -34,7 +34,7 @@\n \n This is a hard-to-reverse platform decision (it sets the version source of\n truth, the release trigger, and the public install contract), so it is recorded\n-as an ADR.\n+as a SADR.\n \n ## Decision\n \n@@ -104,7 +104,7 @@\n 3. **Publishing to PyPI.** Rejected for now: Shiki is distributed as a\n    repository template + git-installable CLI, not a PyPI library; PyPI would add\n    credentials/secrets and a different versioning contract. Can be added later as\n-   its own ADR if a packaged distribution is needed.\n+   its own SADR if a packaged distribution is needed.\n 4. **Dynamic version (read `VERSION` via a build backend `dynamic` field).**\n    Rejected to keep `pyproject.toml` backend-agnostic and dependency-free; the\n    small duplication between `VERSION` and `pyproject.toml` is guarded by the\n@@ -123,4 +123,4 @@\n   releasing versions the whole platform but ships only `install.include` to\n   targets.\n - Future PyPI distribution or a console entry point are deferred and would each\n-  warrant their own ADR.\n+  warrant their own SADR.\ndiff --git a/docs/adr/0008-use-claude-code-as-default-implementer.md b/docs/adr/SADR-0008-use-claude-code-as-default-implementer.md\nindex dedf4510b7c7799521422f622029563d373db031..35c2625d908167fbc58855b5b63e283805f167e8\n--- a/docs/adr/0008-use-claude-code-as-default-implementer.md\n+++ b/docs/adr/SADR-0008-use-claude-code-as-default-implementer.md\n@@ -1,12 +1,12 @@\n-# ADR 0008: Use Claude Code As The Default Implementer Runtime\n+# SADR-0008: Use Claude Code As The Default Implementer Runtime\n \n ## Status\n \n-Proposed (supersedes ADR 0005 for the implementer default; keeps its auth model)\n+Proposed (supersedes SADR-0005 for the implementer default; keeps its auth model)\n \n ## Context\n \n-ADR 0005 made Codex Front the default implementation runtime, with Claude Code\n+SADR-0005 made Codex Front the default implementation runtime, with Claude Code\n restricted to planner and reviewer roles by the runtime registry, and the only\n autonomous implementation adapter was `shiki runner codex`.\n \n@@ -18,13 +18,13 @@\n - The platform's planning, review, and CCA layers already run on Claude Code,\n   and Claude Code's native capabilities (the built-in `/code-review` skill and\n   the Workflow multi-agent orchestration tool) have no Codex equivalent.\n-- The Goal lifecycle redesign (ADR 0009) requires an autonomous post-freeze\n+- The Goal lifecycle redesign (SADR-0009) requires an autonomous post-freeze\n   loop driven from the operator's Claude Code session, including bounded\n   repair dispatch, which must not depend on a runtime that cannot be invoked\n   unattended.\n \n The constitution claims Shiki is runtime-agnostic (\"not a Claude-only\n-workflow\", ADR 0002). Any change to the implementer default must preserve the\n+workflow\", SADR-0002). Any change to the implementer default must preserve the\n registry-based runtime model rather than hardcoding one vendor.\n \n ## Decision\n@@ -39,7 +39,7 @@\n   the task to review or repair-needed.\n - Keep Codex registered as an optional implementer runtime. `shiki runner\n   codex`, the Codex handoff templates, and the ChatGPT OAuth auth model from\n-  ADR 0005 remain supported for tasks explicitly assigned to `codex`.\n+  SADR-0005 remain supported for tasks explicitly assigned to `codex`.\n - Commit to mandating two Claude-native capabilities as loop contract in\n   follow-up Goals (status: the code-review gate was delivered by Goal G-B \u2014\n   skill registry, checklists item PR-12, default required_skills \u2014 while the\n@@ -60,7 +60,7 @@\n \n Out of scope: removing Codex support, changing the CCA runtime (Claude Code\n Action with `CLAUDE_CODE_OAUTH_TOKEN` stays), and API-key based automation\n-(still requires its own ADR per ADR 0005).\n+(still requires its own target ADR per SADR-0005).\n \n ## Consequences\n \n@@ -100,7 +100,7 @@\n   harness rejection of direct `codex exec` is outside Shiki's control, and the\n   plan's mandatory native capabilities cannot run on Codex at all.\n - **Remove Codex entirely**: rejected; it abandons the runtime-agnostic\n-  constitution (ADR 0002) for no operational gain and forecloses\n+  constitution (SADR-0002) for no operational gain and forecloses\n   cross-runtime adversarial review.\n - **GitHub Actions cloud implementation via claude-code-action**: rejected as\n   default; durable evidence is automatic but token cost, Node 24 workflow\ndiff --git a/docs/adr/0009-requirements-definition-and-spec-freeze.md b/docs/adr/SADR-0009-requirements-definition-and-spec-freeze.md\nindex 29b3f7871bef9203a8b4a831a65c39eb2f506e42..52e7dd69ad0aa4efe0b68c62678abbf7df02aaec\n--- a/docs/adr/0009-requirements-definition-and-spec-freeze.md\n+++ b/docs/adr/SADR-0009-requirements-definition-and-spec-freeze.md\n@@ -1,4 +1,4 @@\n-# ADR 0009: Merge Grill And Planning Into Requirements Definition With Spec Freeze\n+# SADR-0009: Merge Grill And Planning Into Requirements Definition With Spec Freeze\n \n ## Status\n \ndiff --git a/docs/adr/0010-external-ai-guardian-review.md b/docs/adr/SADR-0010-external-ai-guardian-review.md\nindex e6108bbe8bb4e73e0ff4684c8f78001a80438c4e..f3510b145664298adafb151b6f0db51b972f01ec\n--- a/docs/adr/0010-external-ai-guardian-review.md\n+++ b/docs/adr/SADR-0010-external-ai-guardian-review.md\n@@ -1,4 +1,4 @@\n-# ADR 0010: External AI Guardian Review As A First-Class Approval Authority\n+# SADR-0010: External AI Guardian Review As A First-Class Approval Authority\n \n ## Status\n \n@@ -8,7 +8,7 @@\n \n Shiki is intended to be an autonomous, recursive, self-improving agent\n framework: authority-in-the-loop, not human-in-the-loop. Until now the Guardian\n-approval required for high/critical-risk merges (guardian-policy.json, ADR\n+approval required for high/critical-risk merges (guardian-policy.json, SADR\n governance) recognized only human authority sources \u2014 a GitHub review approval,\n the `guardian:approved` label applied by a configured Guardian, or a comment\n carrying `Guardian approval granted` plus the current head SHA.\n@@ -89,7 +89,7 @@\n   autonomous design the operator intends and produced the impersonation\n   incident.\n - **Record AI approval as an operator comment**: rejected as forgery of human\n-  authority \u2014 the exact failure this ADR corrects.\n+  authority \u2014 the exact failure this SADR corrects.\n - **Deliver the artifact as a committed `.shiki/` file**: rejected; a committed\n   approval inside the PR being approved is forgeable by anyone who can push.\n - **A dedicated reviewer bot identity instead of a relayed comment**: deferred;\ndiff --git a/docs/adr/0011-autonomous-quality-gate-evidence.md b/docs/adr/SADR-0011-autonomous-quality-gate-evidence.md\nindex bca0246c5d46e44afbde15486a75eec425fc02f5..9c93eac16d8c3a6d4b11bd7224e3955102cd7db4\n--- a/docs/adr/0011-autonomous-quality-gate-evidence.md\n+++ b/docs/adr/SADR-0011-autonomous-quality-gate-evidence.md\n@@ -1,4 +1,4 @@\n-# ADR 0011: Autonomous Quality-Gate Evidence \u2014 Deterministic Facts And An Independent Verifier\n+# SADR-0011: Autonomous Quality-Gate Evidence \u2014 Deterministic Facts And An Independent Verifier\n \n ## Status\n \ndiff --git a/docs/adr/0012-autonomous-loop-closeout-to-main.md b/docs/adr/SADR-0012-autonomous-loop-closeout-to-main.md\nindex 637fad6dde4989c9c2bf0f81f676d8d29991123c..ec7d85f81030a676bcbba28b43eb365422a36eac\n--- a/docs/adr/0012-autonomous-loop-closeout-to-main.md\n+++ b/docs/adr/SADR-0012-autonomous-loop-closeout-to-main.md\n@@ -1,4 +1,4 @@\n-# 0012 \u2014 Autonomous loop pushes goal completion to main via a closeout PR\n+# SADR-0012 \u2014 Autonomous loop pushes goal completion to main via a closeout PR\n \n - Status: accepted\n - Date: 2026-06-18\ndiff --git a/docs/adr/0013-bootstrap-grants-actions-approve-pull-requests.md b/docs/adr/SADR-0013-bootstrap-grants-actions-approve-pull-requests.md\nindex b6c69d64f1c5ced346b88d6859586715b3b5bd0e..cf48ff4a6716267f539ca58edd1a852e7c7a2d64\n--- a/docs/adr/0013-bootstrap-grants-actions-approve-pull-requests.md\n+++ b/docs/adr/SADR-0013-bootstrap-grants-actions-approve-pull-requests.md\n@@ -1,4 +1,4 @@\n-# ADR 0013: Bootstrap Grants GitHub Actions Permission To Create And Approve Pull Requests\n+# SADR-0013: Bootstrap Grants GitHub Actions Permission To Create And Approve Pull Requests\n \n ## Status\n \n@@ -31,7 +31,7 @@\n security tradeoff: it lets repository automation satisfy a human-review gate. In\n a multi-maintainer repo that could let a compromised or buggy workflow\n self-approve changes. So whether bootstrap turns it on by default \u2014 and with\n-what default token scope \u2014 is an ADR-worthy decision, not an implementation\n+what default token scope \u2014 is a SADR-worthy decision, not an implementation\n detail.\n \n ## Decision\n@@ -48,7 +48,7 @@\n Specifically:\n \n - **`can_approve_pull_request_reviews` defaults to `true`.** Shiki's default\n-  posture is solo/AFK autonomous operation (ADR 0008/0009/0012), where the CCA\n+  posture is solo/AFK autonomous operation (SADR-0008/0009/0012), where the CCA\n   Review Bridge is the only path that can satisfy `required_review: true` after\n   the CCA and Guardian gates have already passed. The default makes a\n   freshly-bootstrapped repo able to run the autonomous loop end to end without a\ndiff --git a/docs/adr/0014-codex-app-external-ai-guardian-ui-adapter.md b/docs/adr/SADR-0014-codex-app-external-ai-guardian-ui-adapter.md\nindex 238db06279334caa341c117362df9271382a7fcd..65f12e9939321e1b3e389a34c822fc1549e5155d\n--- a/docs/adr/0014-codex-app-external-ai-guardian-ui-adapter.md\n+++ b/docs/adr/SADR-0014-codex-app-external-ai-guardian-ui-adapter.md\n@@ -1,4 +1,4 @@\n-# ADR 0014: Use Codex App As The External AI Guardian UI Adapter\n+# SADR-0014: Use Codex App As The External AI Guardian UI Adapter\n \n ## Status\n \ndiff --git a/docs/adr/0015-contract-approval-before-dispatch.md b/docs/adr/SADR-0015-contract-approval-before-dispatch.md\nindex fad6c656741a20a7684ba16080a7db159f63a52f..6061271a46d3f1d6ae7174477de3fb75673123a6\n--- a/docs/adr/0015-contract-approval-before-dispatch.md\n+++ b/docs/adr/SADR-0015-contract-approval-before-dispatch.md\n@@ -1,4 +1,4 @@\n-# ADR 0015: Approve The Task Contract Before Dispatch, Not The Diff After It\n+# SADR-0015: Approve The Task Contract Before Dispatch, Not The Diff After It\n \n ## Status\n \n@@ -9,7 +9,7 @@\n Guardian approval for high/critical risk is expressible only on a pull request.\n Every approval source in `guardian-policy.json` \u2014 GitHub review, the\n `guardian:approved` label, the `Guardian approval granted` comment, and the\n-`external_ai_guardian_review` artifact (ADR 0010/0014) \u2014 is bound to a PR, and\n+`external_ai_guardian_review` artifact (SADR-0010/0014) \u2014 is bound to a PR, and\n three of the four require the current head SHA. Approval therefore cannot exist\n before an implementation PR exists.\n \n@@ -81,7 +81,7 @@\n An envelope breach must surface only on the non-repairable policy gate, never on\n the repairable `MergeGate metadata check`. Otherwise the Goal Loop routes it to\n `dispatch_repair` \u2014 instructing a runner to make the breach go away \u2014 which is\n-exactly what this ADR forbids.\n+exactly what this SADR forbids.\n \n **Contract immutability.** After registration, an implementation PR must not\n change the governance fields of its own task contract (`scope`, `non_goals`,\n@@ -116,7 +116,7 @@\n fails any verdict that is not `complete`, and the signal the CCA prompt reads is\n computed from `evaluate_guardian_approval` alone. Without the OR-merge at the\n signal, a contract-approved PR still returns `needs_guardian`, the CCA job fails,\n-and the MergeGate policy check never runs \u2014 the gate this ADR changes would never\n+and the MergeGate policy check never runs \u2014 the gate this SADR changes would never\n be reached. The CCA *prompt* needs no change: a `contract_approval` entry in the\n signal's existing `sources` array satisfies its current wording.\n \n@@ -130,7 +130,7 @@\n written, and an approved contract lets its implementation PRs merge through the\n same automatic path as low/medium risk. The wasted implement-then-block cycle\n disappears. Guardian review shifts from reading diffs to reading contracts, which\n-is both earlier and smaller; the external AI Guardian path (ADR 0010/0014) works\n+is both earlier and smaller; the external AI Guardian path (SADR-0010/0014) works\n on contracts with no change.\n \n We accept that the Guardian authorizes a class of change without seeing its\n@@ -146,7 +146,7 @@\n adversarial tests for the new approval path equivalent in rigor to\n `test_shiki_governance_evidence.sh`.\n \n-Bootstrap costs, which apply only while this ADR is being implemented. The tasks\n+Bootstrap costs, which apply only while this SADR is being implemented. The tasks\n that build Contract Approval merge under the existing rules, so the ones\n dispatched after the pre-dispatch gate lands but before the approval path lands\n need a Guardian-approved Contract PR *and* a live Guardian approval on their\n@@ -210,7 +210,7 @@\n risk; they do not bound authorized work.\n \n **Auto-escalate risk on envelope breach instead of stopping.** Returns the\n-workflow to a diff-time Guardian decision \u2014 the exact behavior this ADR removes.\n+workflow to a diff-time Guardian decision \u2014 the exact behavior this SADR removes.\n \n **Resolve governance fields from the base snapshot only.** Rejected against the\n registration flow: implementation PRs add their own contract, so the base has no\ndiff --git a/docs/adr/0016-locks-bound-product-paths.md b/docs/adr/SADR-0016-locks-bound-product-paths.md\nindex b5e850f3f1f7f4ef6e7f9d1763a29a51bd300c45..46b40a9b99a7aae644c02bb458f5473ff0a90ce2\n--- a/docs/adr/0016-locks-bound-product-paths.md\n+++ b/docs/adr/SADR-0016-locks-bound-product-paths.md\n@@ -1,4 +1,4 @@\n-# ADR 0016: Locks bound product paths; the Shiki mirror is bound by record identity\n+# SADR-0016: Locks bound product paths; the Shiki mirror is bound by record identity\n \n ## Status\n \n@@ -6,8 +6,8 @@\n \n - Date: 2026-07-28\n - Deciders: operator (mizutani-140), Claude (planner)\n-- Related: ADR 0004 (GitHub-first with a `.shiki` mirror), ADR 0009 (Spec Freeze),\n-  ADR 0012 (autonomous loop closeout to main), ADR 0015 (Contract Approval before\n+- Related: SADR-0004 (GitHub-first with a `.shiki` mirror), SADR-0009 (Spec Freeze),\n+  SADR-0012 (autonomous loop closeout to main), SADR-0015 (Contract Approval before\n   dispatch), remediation goal task R-02, the live PR #179 deadlock.\n \n ## Context\n@@ -90,11 +90,11 @@\n files, documentation files and named source modules are nameable; runtime-\n allocated ledger, runner and report ids are not \u2014 and those live in `.shiki`,\n which locks no longer govern. Existing frozen contracts are brought into line by\n-a one-time Spec Amendment per plan, which must land before ADR 0015's contract\n+a one-time Spec Amendment per plan, which must land before SADR-0015's contract\n immutability makes `locks` unwritable from the implementation PR.\n \n **Lock state is resolved from the durable side.** MergeGate resolves the lock\n-registry, and ADR 0015's governance fields, from the base `.shiki` snapshot when\n+registry, and SADR-0015's governance fields, from the base `.shiki` snapshot when\n present, falling back to head only when absent. The `MergeGate metadata check`\n job is given `--base-shiki`, which it does not receive today. Locks are not\n derived from the live open-pull-request set: a pull request body is untrusted\n@@ -108,7 +108,7 @@\n Explicitly out of scope: moving MergeGate's contract resolution off the PR\n checkout wholesale; making the mirror a projection of GitHub state; introducing\n new lock kinds; moving ledger evidence out of the repository; and unwinding\n-ADR 0012's closeout pull request. Registration of multi-task goals is ADR 0015's\n+SADR-0012's closeout pull request. Registration of multi-task goals is SADR-0015's\n Contract PR and is not re-decided here.\n \n ## Consequences\n@@ -127,7 +127,7 @@\n \n Risks accepted. First, goal-scoped identity is only as strong as `goal_id`,\n which is resolved from the PR body and the head task file; the mitigation is to\n-add `goal_id` to ADR 0015's frozen governance field set, and until that lands a\n+add `goal_id` to SADR-0015's frozen governance field set, and until that lands a\n task file can name a foreign goal. Second, base-when-present resolution is\n unavailable on fork pull requests, where the head is contributor-controlled;\n fork pull requests already cannot complete a Shiki merge, and this decision does\n@@ -139,7 +139,7 @@\n path, not by implementation pull requests.\n \n New obligations: one Spec Amendment per frozen plan to rewrite `locks`, ordered\n-before ADR 0015 T1; extension of the adversarial suites in\n+before SADR-0015 T1; extension of the adversarial suites in\n `scripts/test_shiki_adversarial_state.sh` and\n `scripts/test_shiki_governance_evidence.sh` to cover the eleven newly-scoped\n directories; and updating `scripts/test_shiki_loop_lock_guard.sh`,\n@@ -151,32 +151,32 @@\n whether the loop's transitive sync of ledger `evidence` references can be\n deleted once the Contract PR carries its own source plan.\n \n-## Relationship to ADR 0004\n+## Relationship to SADR-0004\n \n-This ADR **clarifies something ADR 0004 left unspecified**. ADR 0004 decides that\n+This SADR **clarifies something SADR-0004 left unspecified**. SADR-0004 decides that\n GitHub is the operational source of truth and that each target repository keeps a\n `.shiki/` mirror. It says nothing about pull requests writing the mirror, does\n not name MergeGate, and does not make the mirror an input to a gate. Nothing in\n-ADR 0004's Decision or Consequences is amended, superseded or weakened: the\n+SADR-0004's Decision or Consequences is amended, superseded or weakened: the\n mirror still exists, still records the same artifacts, and pull request\n-boundaries remain the coordination surface. What this ADR settles is the question\n+boundaries remain the coordination surface. What this SADR settles is the question\n 0004 never reached \u2014 by what authority a pull request may write a mirror record \u2014\n and it answers it the way `.shiki/manifest.json` already declares\n (`pr_mutation: current-task-or-current-goal-only`).\n \n-It **amends ADR 0012**, whose Decision item 5 states that \"Loop-executed tasks\n+It **amends SADR-0012**, whose Decision item 5 states that \"Loop-executed tasks\n declare the `path:.shiki/**` lock, which covers every `.shiki` file the closeout\n stages ... so `files_outside_locks` is satisfied without per-file lock\n bookkeeping.\" That clause is replaced: the closeout's mirror writes are\n-authorized by record identity, not by a declared lock. ADR 0012's substantive\n+authorized by record identity, not by a declared lock. SADR-0012's substantive\n decision \u2014 that completion reaches `main` through a closeout pull request rather\n than local attestation \u2014 is preserved unchanged.\n \n-It is **complementary to ADR 0015**, not a substitute. ADR 0015 owns contract\n+It is **complementary to SADR-0015**, not a substitute. SADR-0015 owns contract\n immutability, the pre-dispatch gate, the Contract PR and the Scope Envelope. This\n-ADR owns what a lock means and who may write the mirror. The one ordering\n+SADR owns what a lock means and who may write the mirror. The one ordering\n constraint between them is that the lock-narrowing Spec Amendment must precede\n-ADR 0015 T1's freezing of the `locks` field.\n+SADR-0015 T1's freezing of the `locks` field.\n \n ## Alternatives Considered\n \n@@ -186,7 +186,7 @@\n the only writer that can register them may run. Migrating the Guardian signal to\n that store makes every registration PR fail closed to `needs_guardian` at every\n risk level, because the task file is by definition absent from the store, which\n-removes the low/medium autonomous path ADR 0008/0009 exists to provide. And it\n+removes the low/medium autonomous path SADR-0008/0009 exists to provide. And it\n leaves schemas, manifest, config, guardian policy, templates, migrations, plans,\n worktrees, runner records, runs and handoffs with no writer at all in a\n repository whose product is `.shiki`. Measured benefit on the frozen backlog:\n@@ -199,14 +199,14 @@\n namespace is not in fact ownership-partitioned \u2014 56 ledger entries carry no\n `task_id`, and one goal DAG is writable by 35 registered tasks. The correct\n expression of its insight is that locks do not govern the mirror at all, which is\n-what this ADR decides.\n+what this SADR decides.\n \n **Make the mirror a projection written only by a registration writer and a\n post-merge projection job.** Rejected. It requires a bypass actor with write\n access to the protected default branch; it makes its own master invariant\n readable from a manifest that pull requests may edit; it deletes both reconcile\n modes and leaves a corrupted mirror with no in-band repair path, contradicting\n-ADR 0004's consequence that mirror conflicts are repaired against GitHub state;\n+SADR-0004's consequence that mirror conflicts are repaired against GitHub state;\n and its content-derived ids are rejected by `validate_shiki`'s currently-green id\n pattern.\n \ndiff --git a/docs/adr/0017-bookkeeping-closeouts-do-not-inherit-risk.md b/docs/adr/SADR-0017-bookkeeping-closeouts-do-not-inherit-risk.md\nindex 554e3a0d526a72ca067108c5464cc68c3edcf174..b9676857a4713850e063af7c39c8e0b01ecb4c66\n--- a/docs/adr/0017-bookkeeping-closeouts-do-not-inherit-risk.md\n+++ b/docs/adr/SADR-0017-bookkeeping-closeouts-do-not-inherit-risk.md\n@@ -1,4 +1,4 @@\n-# ADR 0017: A Bookkeeping Closeout Does Not Inherit Its Task's Risk\n+# SADR-0017: A Bookkeeping Closeout Does Not Inherit Its Task's Risk\n \n ## Status\n \n@@ -6,7 +6,7 @@\n \n ## Context\n \n-ADR 0012 made the goal loop push a task's completion to the default branch through a\n+SADR-0012 made the goal loop push a task's completion to the default branch through a\n closeout pull request: after the implementation PR merges, the loop opens a second PR\n carrying `task.status = done`, `lock.state = released`, and \u2014 when the task completes\n its goal \u2014 `goal.status = complete` plus one scorecard report. Nothing else. The\n@@ -49,14 +49,14 @@\n 4. The task file's governance fields (`scope`, `non_goals`, `required_skills`,\n    `risk_level`, `locks`, `acceptance_checks`, `test_command`) are byte-identical to\n    the base snapshot. Only `status`, `expected_pr`, `closeout_pr`, `ledger_evidence`\n-   and `expected_branch` may differ \u2014 the ADR 0015 contract-immutability rule,\n+   and `expected_branch` may differ \u2014 the SADR-0015 contract-immutability rule,\n    unchanged.\n \n    `expected_branch` is in that set because a closeout is *by construction* on a\n    different branch from the implementation it closes, and the loop rewrites the\n    field accordingly. It is safe to let it differ because MergeGate independently\n    requires `expected_branch` to equal the PR's actual head ref, so a PR cannot set\n-   it to anything other than the branch it is really on. The first draft of this ADR\n+   it to anything other than the branch it is really on. The first draft of this SADR\n    omitted it, which made the exemption unreachable: every real closeout failed\n    condition 4 on a field the loop always changes. See PR #209.\n 5. The state transitions are exactly the terminal set: task `review` \u2192 `done`, lock\n@@ -113,12 +113,12 @@\n classification must be derived, not asserted.\n \n **Fold the closeout into the implementation PR** so no second PR exists. Rejected under\n-ADR 0012, which recorded why: a task file that is already `done` on the implementation\n+SADR-0012, which recorded why: a task file that is already `done` on the implementation\n branch short-circuits the loop's review \u2192 CCA \u2192 merge \u2192 done ordering into a local\n goal-completion before any merge happens.\n \n **Have the loop auto-merge closeouts with elevated credentials.** Rejected outright: that\n-is a runtime authoring its own approval, which ADR 0010 and the constitution's\n+is a runtime authoring its own approval, which SADR-0010 and the constitution's\n approval-record prohibition forbid. It would also be indistinguishable, in the evidence,\n from the forgery those rules exist to prevent.\n \ndiff --git a/docs/adr/0018-carried-guardian-approval-across-a-proven-base-sync.md b/docs/adr/SADR-0018-carried-guardian-approval-across-a-proven-base-sync.md\nindex 22358562da390ed8b45cd600da08a43d378b4680..e6e2907cefa776460af73ddf7d39db605f3c39b0\n--- a/docs/adr/0018-carried-guardian-approval-across-a-proven-base-sync.md\n+++ b/docs/adr/SADR-0018-carried-guardian-approval-across-a-proven-base-sync.md\n@@ -1,4 +1,4 @@\n-# ADR 0018: Carried Guardian Approval Across A Proven Base Sync\n+# SADR-0018: Carried Guardian Approval Across A Proven Base Sync\n \n ## Status\n \n@@ -34,7 +34,7 @@\n Guardian evaluator that consumes it (`shiki_guardian.evaluate_guardian_approval`,\n the `guardian_comment_carried` source) were built and tested in isolation. Neither\n does anything until a caller passes the flags that turn the carry on, so the\n-feature was invisible in CI. This ADR records the decision the two of them\n+feature was invisible in CI. This SADR records the decision the two of them\n implement, and this task wires it into the four CI invocations that judge Guardian\n approval.\n \ndiff --git a/docs/adr/README.md b/docs/adr/README.md\nindex c70e057a4520c36e042ab68c53cd4a21ef709b71..5da43856836e2ce6a150f8127766e2f88355ab4b\n--- a/docs/adr/README.md\n+++ b/docs/adr/README.md\n@@ -1,40 +1,41 @@\n-# Architecture Decision Records\n+# Decision Record Namespaces\n \n-This directory holds Shiki's Architecture Decision Records (ADRs): durable\n-records of hard-to-reverse platform decisions. ADRs are part of the Shiki source\n-of truth alongside GitHub state, the `.shiki/` mirror, and `CONTEXT.md`.\n+This directory holds two independent decision namespaces:\n \n-## How ADRs Work\n+- Shiki Architecture Decision Records (SADRs), named\n+  `SADR-NNNN-<short-slug>.md`, record hard-to-reverse Shiki platform decisions.\n+- Target Architecture Decision Records (ADRs), named\n+  `NNNN-<short-slug>.md`, belong to the target product.\n \n-- New ADRs are added as `docs/adr/NNNN-<short-slug>.md`, where `NNNN` is the next\n-  zero-padded sequence number.\n-- The **canonical list of ADRs is the set of files matching `docs/adr/*.md`** in\n-  this directory. Browse `docs/adr/*.md` for the authoritative, current set.\n-- Use [`template.md`](template.md) as the starting point for a new ADR.\n-- Record an ADR when a decision is hard to reverse, surprising without context,\n-  and a real tradeoff. Do not write an ADR for routine or easily reversible\n-  choices.\n+The same four-digit number may appear once in each namespace. A platform\n+`SADR-NNNN` citation resolves only to `SADR-NNNN-*.md`; a target `ADR NNNN`\n+citation resolves only to `NNNN-*.md`.\n \n-Using the `docs/adr/*.md` glob as the source of truth means other PRs can add an\n-ADR without editing this index file, avoiding merge contention on a\n-hand-maintained enumeration.\n+## How Decision Records Work\n \n-## Current ADRs (snapshot)\n-\n-The list below is a convenience snapshot and may lag behind the directory. When\n-in doubt, list `docs/adr/*.md`.\n+- New Shiki platform decisions use the next zero-padded SADR number.\n+- New target product decisions use the target's own ADR sequence.\n+- The canonical list is the files in `docs/adr/`; no hand-maintained index is\n+  authoritative.\n+- Use [`template.md`](template.md) as the starting point and choose the filename\n+  namespace that owns the decision.\n+- Record a decision when it is hard to reverse, surprising without context, and\n+  a real tradeoff. Do not write a decision record for routine or easily\n+  reversible choices.\n \n-- [0001 \u2014 Keep Shiki As The Platform Name](0001-keep-shiki-as-platform-name.md)\n-- [0002 \u2014 Use AGENTS As The Shared Agent Constitution](0002-use-agents-as-shared-constitution.md)\n-- [0003 \u2014 Require Skill Gate For MergeGate Readiness](0003-require-skill-gate-for-mergegate-readiness.md)\n-- [0004 \u2014 Use GitHub First With A Repository-Local Shiki Mirror](0004-use-github-first-with-shiki-mirror.md)\n-- [0005 \u2014 Use Codex As Front And Claude Code Action As The GitHub Runtime](0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md)\n-- [0006 \u2014 Separate Codex Implementation, CCA Completion, And MergeGate Authorization](0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md)\n+Using the directory as the source of truth lets independent changes add records\n+without contending on an enumerated index.\n \n-## Adding A New ADR\n+## Adding A Shiki SADR\n \n-1. Copy `template.md` to `docs/adr/NNNN-<short-slug>.md`.\n+1. Copy `template.md` to `docs/adr/SADR-NNNN-<short-slug>.md`.\n 2. Fill in the title, status, context, decision, and consequences.\n-3. Set the status to `Proposed`, then `Accepted` once the decision is approved.\n-4. Open a PR following `CONTRIBUTING.md`. You do not need to edit this index for\n-   the ADR to be discoverable via `docs/adr/*.md`.\n+3. Set the status to `Proposed`, then `Accepted` once the proper authority has\n+   approved the decision.\n+4. Open a PR following `CONTRIBUTING.md`.\n+\n+## Adding A Target ADR\n+\n+1. Copy `template.md` to `docs/adr/NNNN-<short-slug>.md` using the target's own\n+   sequence.\n+2. Cite it as `ADR NNNN`; do not prefix its filename with `SADR-`.\ndiff --git a/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md b/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\nnew file mode 100644\nindex 0000000000000000000000000000000000000000..7455ce1f3bed02ddddb851ac21a1d1a648c9aeba\n--- /dev/null\n+++ b/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\n@@ -0,0 +1,55 @@\n+# SADR-0019: Separate Shiki Platform SADRs From Target ADRs\n+\n+## Status\n+\n+Accepted\n+\n+## Context\n+\n+Shiki installs its platform decision records into `docs/adr/` in every target\n+repository. Shiki and its targets previously used the same numeric\n+`NNNN-<slug>.md` namespace. A target that already owned ADRs 0013 through 0018\n+therefore acquired two records for each number during installation, making an\n+`ADR NNNN` citation ambiguous. Renumbering target decisions would rewrite target\n+history, while renumbering Shiki decisions per target would destroy their stable\n+platform identity.\n+\n+Upgrades also need to remove the former Shiki-owned numeric paths without ever\n+mistaking a target-owned ADR for platform content. The install stamp already\n+records exact shipped paths and content digests, so it can prove ownership only\n+when both still match.\n+\n+## Decision\n+\n+Shiki platform decisions use the `SADR` namespace:\n+\n+- records are named `docs/adr/SADR-NNNN-<slug>.md`;\n+- live platform citations use `SADR-NNNN`;\n+- target product decisions retain `docs/adr/NNNN-<slug>.md` and `ADR NNNN`;\n+- the reference validator resolves the two namespaces independently, so the same\n+  four-digit number may exist once in each namespace.\n+\n+Before a forced upgrade writes anything, the installer inventories the exact\n+former numeric Shiki ADR paths. It may delete a legacy path only when the\n+existing install stamp contains that exact path and its recorded SHA-256 digest\n+matches the file currently on disk. All ownership blockers are reported\n+together and abort the upgrade before its first write. A non-forced install\n+never performs this cleanup.\n+\n+Historical `.shiki/` evidence remains unchanged because it records the namespace\n+that existed when that evidence was produced.\n+\n+## Consequences\n+\n+- Targets may use any ADR number without colliding with Shiki's platform SADRs.\n+- Fresh installs preserve target ADR bytes and install a stable SADR set.\n+- Forced upgrades fail closed when legacy ownership cannot be proven, and a\n+  successful upgrade refreshes the stamp without removed legacy paths.\n+- Current governance, packaging, workflow, prompt, script, and skill references\n+  must move with the platform records.\n+\n+## Alternatives Considered\n+\n+- Collision-only refusal leaves affected targets permanently unupgradable.\n+- Renumbering target ADRs mutates target-owned history.\n+- Per-target Shiki renumbering makes platform citations non-portable.\ndiff --git a/docs/adr/template.md b/docs/adr/template.md\nindex a92a97daddd0b00ab9dd626cc8d86942da2eafed..17dd79175b5fee2105935557f691b40cf9492942\n--- a/docs/adr/template.md\n+++ b/docs/adr/template.md\n@@ -1,14 +1,14 @@\n-# NNNN: <Short Decision Title>\n+# <SADR-NNNN or NNNN>: <Short Decision Title>\n \n ## Status\n \n-Proposed | Accepted | Superseded by ADR-NNNN\n+Proposed | Accepted | Superseded by SADR-NNNN or target ADR NNNN\n \n ## Context\n \n What is the situation, constraint, or force that requires a decision? Reference\n-relevant domain terms (`CONTEXT.md`), prior ADRs, and code reality. State the\n-problem, not the solution.\n+relevant domain terms (`CONTEXT.md`), prior Shiki SADRs or target ADRs, and code\n+reality. State the problem, not the solution.\n \n ## Decision\n \ndiff --git a/docs/agents/autonomous-loop-e2e-acceptance.md b/docs/agents/autonomous-loop-e2e-acceptance.md\nindex bbf84c6081987f159ac6ad9ecf3c82b7aa72e7e8..5c13c1d02a05a90b0dc46cbef7c95a9846d0c237\n--- a/docs/agents/autonomous-loop-e2e-acceptance.md\n+++ b/docs/agents/autonomous-loop-e2e-acceptance.md\n@@ -40,8 +40,8 @@\n 3. `gh auth status` is logged in with merge permission on the repo.\n 4. Branch protection + required checks are configured exactly as the goal-loop\n    required-check set expects (see `docs/agents/branch-protection-smoke.md`).\n-5. The repo `.shiki/policy` permits autonomous low/medium-risk merges (ADR\n-   0008/0009); the MergeGate policy check is the recorded-authority gate.\n+5. The repo `.shiki/policy` permits autonomous low/medium-risk merges\n+   (SADR-0008/0009); the MergeGate policy check is the recorded-authority gate.\n \n The operator's only actions are: register the task and start the loop. **No\n manual pre-clearing** means after `shiki loop run` starts, the operator does not\ndiff --git a/docs/agents/bootstrap-command.md b/docs/agents/bootstrap-command.md\nindex 05f7f203a52cf412b9f97084a6e131ed0f785afd..f7aa0b734dfebee4a3b139335b66ee3415f48713\n--- a/docs/agents/bootstrap-command.md\n+++ b/docs/agents/bootstrap-command.md\n@@ -189,7 +189,9 @@\n ```\n \n Use this only for tests, fixtures, or explicit local-only template inspection.\n-Use `--force` only when you intentionally want to overwrite existing target files.\n+Use `--force` only when you intentionally want to refresh the target template.\n+Legacy numeric Shiki decision cleanup is stamp-and-digest authorized before any\n+write; target ADR paths are not cleanup candidates.\n \n ## Slash Command\n \ndiff --git a/docs/agents/checklists.md b/docs/agents/checklists.md\nindex 4a7f03ade346f53a1c1d82981f3fd744f9ab4547..0c5ec6c53dc7197b7b7a7d18e667b325babd6c45\n--- a/docs/agents/checklists.md\n+++ b/docs/agents/checklists.md\n@@ -32,18 +32,18 @@\n | ID | Item | Blocking |\n | --- | --- | --- |\n | GD-01 | Plan was challenged against `CONTEXT.md`. | blocking for non-trivial Goals |\n-| GD-02 | Relevant ADRs were read or absence was recorded. | blocking for non-trivial Goals |\n+| GD-02 | Relevant SADRs and target ADRs were read or absence was recorded. | blocking for non-trivial Goals |\n | GD-03 | Ambiguous terms were resolved or converted to HITL blockers. | blocking |\n | GD-04 | Edge scenarios were considered. | advisory |\n | GD-05 | Code reality was checked when code could answer. | blocking when applicable |\n | GD-06 | New domain terms were added to `CONTEXT.md` when needed. | blocking when applicable |\n-| GD-07 | ADR was created or explicitly skipped using the ADR criteria. | blocking when applicable |\n+| GD-07 | A SADR or target ADR was created or explicitly skipped using the decision-record criteria. | blocking when applicable |\n \n ## CI \u2014 Context & Impact Checklist\n \n | ID | Item | Blocking |\n | --- | --- | --- |\n-| CI-01 | Relevant docs and ADRs identified. | blocking |\n+| CI-01 | Relevant docs, SADRs, and target ADRs identified. | blocking |\n | CI-02 | Relevant modules, interfaces, seams, callers, and tests identified. | blocking |\n | CI-03 | Dependencies and blocked-by relationships identified. | blocking |\n | CI-04 | Candidate locks identified. | blocking |\n@@ -62,7 +62,7 @@\n | PRD-04 | Implementation decisions are recorded without volatile code detail. | blocking |\n | PRD-05 | Testing decisions are recorded. | blocking |\n | PRD-06 | Out-of-scope items are explicit. | blocking |\n-| PRD-07 | Relevant glossary/ADR links are present when applicable. | blocking |\n+| PRD-07 | Relevant glossary/SADR/target-ADR links are present when applicable. | blocking |\n \n ## SF \u2014 Spec Freeze Checklist\n \n@@ -98,7 +98,7 @@\n | H-02 | Branch/worktree target is present. | blocking |\n | H-03 | Scope and non-goals are present. | blocking |\n | H-04 | Dependencies and locks are present. | blocking |\n-| H-05 | Relevant docs, ADRs, modules, and tests are referenced. | blocking |\n+| H-05 | Relevant docs, SADRs, target ADRs, modules, and tests are referenced. | blocking |\n | H-06 | Required skills are stated. | blocking |\n | H-07 | TDD expectations are stated. | blocking when behavior work |\n | H-08 | Acceptance criteria are included. | blocking |\ndiff --git a/docs/agents/codex-handoff.md b/docs/agents/codex-handoff.md\nindex ede468f47cb277f69a857e8c45df703e4ed0151d..6f97bdffb67e44d7edea2c71e01ea28933c4eb6c\n--- a/docs/agents/codex-handoff.md\n+++ b/docs/agents/codex-handoff.md\n@@ -46,7 +46,7 @@\n \n ## Context\n - Domain terms:\n-- Relevant ADRs:\n+- Relevant SADRs and target ADRs:\n - Relevant docs:\n - Relevant modules/interfaces/seams:\n - Similar tests/prior art:\ndiff --git a/docs/agents/completion-check-agent.md b/docs/agents/completion-check-agent.md\nindex 13e4c035db35025f4e3cdcc5adfb24314ac4d521..61bfa1218cf36ca605250c91919e620f33238aa0\n--- a/docs/agents/completion-check-agent.md\n+++ b/docs/agents/completion-check-agent.md\n@@ -32,7 +32,7 @@\n - `AGENTS.md`\n - `CLAUDE.md`\n - `CONTEXT.md`\n-- relevant ADRs\n+- relevant SADRs and target ADRs\n - parent Goal issue\n - PRD issue or document\n - task issue\ndiff --git a/docs/agents/decision-control.md b/docs/agents/decision-control.md\nindex 7d8ee5ab03a5ccbcbd04056ae61de03785a2a9c9..8c94ecda00fffaa0e92c325ab83aa9776bd1dac1\n--- a/docs/agents/decision-control.md\n+++ b/docs/agents/decision-control.md\n@@ -11,7 +11,7 @@\n | Decision | Owner | When | Required inputs | Output | Enforcement surface |\n | --- | --- | --- | --- | --- | --- |\n | Is the Goal clear enough to plan? | Planner + Guardian when needed | Goal intake | User request, existing docs, risk signals | Goal readiness verdict | Goal checklist, issue template, ledger |\n-| Must `grill-with-docs` run? | Planner / Skill Gate | Before PRD or issue split | Ambiguity, domain terms, ADR conflicts, unclear boundaries | Skill invocation or explicit skip rationale | Skill Gate checklist |\n+| Must `grill-with-docs` run? | Planner / Skill Gate | Before PRD or issue split | Ambiguity, domain terms, SADR/target-ADR conflicts, unclear boundaries | Skill invocation or explicit skip rationale | Skill Gate checklist |\n | Is a PRD required? | Planner | After Goal context stabilizes | Goal, `grill-with-docs` output, Context & Impact output | PRD required / not required | PRD checklist, CCA evidence check |\n | Can the spec be frozen? | Operator only | End of Requirements Definition, before issue split | PRD/requirements summary, scope inventory (SF-02), open-question list | `spec_freeze` block in the plan + ledger evidence | `require_grilled_plan` (plan ingest/run), SF checklist |\n | Can a frozen spec change? | Operator only (Spec Amendment) | When implementation contradicts the frozen spec | Contested decisions, scoped re-grill result | recorded amendment + re-stamped freeze | SF checklist, ledger evidence |\ndiff --git a/docs/agents/domain.md b/docs/agents/domain.md\nindex 258da8ea81e87ac0a6b0b5b5e5c2fde980eaabb7..d57b0cf17ff3e1fa9e6e5885372bf083d8295421\n--- a/docs/agents/domain.md\n+++ b/docs/agents/domain.md\n@@ -5,7 +5,9 @@\n ## Layout\n \n - `CONTEXT.md` defines Shiki language. Keep it implementation-free.\n-- `docs/adr/` records hard-to-reverse decisions.\n+- `docs/adr/SADR-NNNN-*.md` records Shiki platform decisions.\n+- `docs/adr/NNNN-*.md` records target product decisions without sharing the\n+  SADR namespace.\n - `AGENTS.md` is the runtime-neutral agent constitution.\n - `CLAUDE.md` is the Claude Code wrapper around `AGENTS.md`.\n - `.shiki/` is the Target Repository mirror for Goals, PRDs, plans, Task DAGs, contracts, locks, worktree records, ledger entries, CCA verdicts, repair packets, reports, and handoffs.\n@@ -15,6 +17,6 @@\n \n - Use Shiki terms exactly when they exist.\n - Add glossary terms to `CONTEXT.md` only when the term is part of the domain, not a generic programming concept.\n-- Create ADRs only for hard-to-reverse, surprising, tradeoff-based decisions.\n+- Create SADRs or target ADRs only for hard-to-reverse, surprising, tradeoff-based decisions.\n - Keep product-specific language in the Target Repository, not in Shiki Template docs.\n-- If an ADR conflicts with a proposed implementation, surface the conflict before editing.\n+- If a SADR or target ADR conflicts with a proposed implementation, surface the conflict before editing.\ndiff --git a/docs/agents/execution-control.md b/docs/agents/execution-control.md\nindex ad8ee3cdb36be56fedc1f46408ca92b387d74047..bc63c3a73661d12b567016ba299f1a6b66bf9088\n--- a/docs/agents/execution-control.md\n+++ b/docs/agents/execution-control.md\n@@ -63,7 +63,7 @@\n | Branch name | Orchestrator | Orchestrator | Before implementer dispatch | branch policy | branch ref in task/ledger |\n | Lock reservation | Context & Impact | MergeGate | Before dispatch and before merge | `.shiki/locks/`, labels, PR metadata | lock id, owner, scope |\n | CI profile | Planner | MergeGate | During issue triage, rechecked at PR | labels, `.shiki/policy.yaml`, workflow matrix | required check list |\n-| Required status checks | Repo maintainer / Guardian | GitHub branch protection / rulesets | Repository setup, updated via ADR | GitHub settings | branch/ruleset config, ADR |\n+| Required status checks | Repo maintainer / Guardian | GitHub branch protection / rulesets | Repository setup, updated via target ADR | GitHub settings | branch/ruleset config, target ADR |\n | CCA judgment | CCA | GitHub required check / MergeGate | Every non-draft PR update | GitHub Action check | structured CCA verdict |\n | Repair assignment | CCA | MergeGate | After failed CCA/CI/review | repair packet, issue/PR comment | repair id and attempt count |\n | Merge readiness | MergeGate | GitHub branch protection / Guardian if needed | After CCA complete | required checks, review rules, rulesets | mergegate check output |\ndiff --git a/docs/agents/guardian-policy.md b/docs/agents/guardian-policy.md\nindex 4147c7adf05a47adb4fd24ada1a1c2e1f2ba6d2d..a20d9cc6c00a569bb057b9ca8d6b51c7fc4a9847\n--- a/docs/agents/guardian-policy.md\n+++ b/docs/agents/guardian-policy.md\n@@ -98,8 +98,8 @@\n `origin/<default_branch>` and reuses each job's existing base fetch; a shallow\n (`--depth`) or second base fetch would make the proof unresolvable and silently\n disable the carry, so neither is permitted. CCA reads `guardian_comment_carried` as\n-a recorded authority in the deterministic signal and does not re-derive it. See ADR\n-0018 for the full decision, the rejected diff/patch-equality and path-disjointness\n+a recorded authority in the deterministic signal and does not re-derive it. See\n+SADR-0018 for the full decision, the rejected diff/patch-equality and path-disjointness\n alternatives, and the accepted residual.\n \n ## External AI Guardian Review (SADR-0010)\ndiff --git a/docs/agents/implementation-policy.md b/docs/agents/implementation-policy.md\nindex c754144a9fb5bf4a38aa82a60e1670c59e626b44..85c37281bcad196c6b7b02343faf00b03cd9c57a\n--- a/docs/agents/implementation-policy.md\n+++ b/docs/agents/implementation-policy.md\n@@ -31,10 +31,13 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n ## Non-Negotiables\n \n 1. **Planning precedes implementation.** Non-trivial Goals start with `grill-with-docs` and Context & Impact.\n-2. **Domain language is controlled.** Terms must be checked against `CONTEXT.md`; hard-to-reverse tradeoffs must be checked against ADRs.\n+2. **Domain language is controlled.** Terms must be checked against `CONTEXT.md`; hard-to-reverse tradeoffs must be checked against Shiki SADRs and target ADRs.\n 3. **PRD precedes issue decomposition.** `to-prd` is used once the context is settled enough to become durable intent.\n 4. **Issues are vertical slices.** `to-issues` must produce independently grabbable tracer-bullet tasks, not horizontal layer tickets.\n 5. **Triage controls dispatch.** Only `ready-for-agent` / AFK tasks may be assigned to Codex without human input.\n@@ -95,19 +98,19 @@\n Process:\n \n - Challenge terms against `CONTEXT.md`.\n-- Check ADRs before reopening decisions.\n+- Check SADRs and target ADRs before reopening decisions.\n - Inspect code when code can answer a question.\n - Ask one question at a time when the operator is available.\n - Provide a recommended answer with each question.\n - Record resolved terms in `CONTEXT.md`.\n-- Create ADRs only for hard-to-reverse, surprising, tradeoff-based decisions.\n+- Create SADRs or target ADRs only for hard-to-reverse, surprising, tradeoff-based decisions.\n - Convert unresolved load-bearing questions into HITL blockers.\n \n Outputs:\n \n - Resolved design-tree decisions.\n - Updated glossary terms when needed.\n-- ADRs when justified.\n+- SADRs or target ADRs when justified.\n - HITL blockers when decisions remain unresolved.\n \n ## Phase 3 \u2014 Context & Impact\n@@ -117,7 +120,7 @@\n \n Outputs must include:\n \n-- Relevant documents and ADRs.\n+- Relevant documents, SADRs, and target ADRs.\n - Relevant modules, interfaces, seams, callers, and tests.\n - Candidate locks.\n - Dependencies.\n@@ -140,7 +143,7 @@\n - Testing decisions.\n - Out of scope.\n - Further notes.\n-- Links to relevant Goal, domain terms, and ADRs.\n+- Links to relevant Goal, domain terms, SADRs, and target ADRs.\n \n Phase 4 ends with Spec Freeze: the operator explicitly approves the PRD, the\n plan records a `spec_freeze` block (status, approved_by, source), and the SF\n@@ -240,7 +243,7 @@\n - Dependencies and locks.\n - Risk and Guardian requirements.\n - Ledger completeness.\n-- Documentation and ADR updates.\n+- Documentation and SADR/target-ADR updates.\n - Scope drift.\n \n CCA emits one of:\n@@ -301,7 +304,7 @@\n | Evidence failure | Behavior might be done, but proof is missing. | Add checks, PR evidence, ledger entry, or artifacts. |\n | Scope drift | PR changes unrelated behavior. | Revert unrelated changes or split into new Goal. |\n | Dependency block | Upstream task or lock not complete. | Block until dependency resolves. |\n-| Architecture block | Decision needs ADR or deep-module discussion. | `grill-with-docs` or `improve-codebase-architecture`. |\n+| Architecture block | Decision needs a SADR, target ADR, or deep-module discussion. | `grill-with-docs` or `improve-codebase-architecture`. |\n | Risk block | Security/production/policy change needs human approval. | Guardian decision. |\n \n ## Phase 5.5 \u2014 Execution Decision Control\ndiff --git a/docs/agents/runtime-auth-model.md b/docs/agents/runtime-auth-model.md\nindex 6a2e1a47e81ea89b5c68a210ec07772a9374babc..3c54748d24233dbd0ba8473950282547ef4ce935\n--- a/docs/agents/runtime-auth-model.md\n+++ b/docs/agents/runtime-auth-model.md\n@@ -148,7 +148,7 @@\n \n API-key based runners may be added by a target repository as an explicit extension. They are not the default Shiki template path.\n \n-If a repo opts into API-key mode, record the exception in an ADR and update the repo's `AGENTS.md`, `.shiki/config.yaml`, and workflow permissions.\n+If a repo opts into API-key mode, record the target-specific exception in a target ADR and update the repo's `AGENTS.md`, `.shiki/config.yaml`, and workflow permissions.\n \n ## GitHub Actions Runtime Compatibility\n \ndiff --git a/docs/agents/skill-gate.md b/docs/agents/skill-gate.md\nindex c50398b0d7c60a8598f2a5c88a10015730dced13..3acffe6444617bb1d54b77d4b4708158ec735bc6\n--- a/docs/agents/skill-gate.md\n+++ b/docs/agents/skill-gate.md\n@@ -9,7 +9,7 @@\n | Skill | Trigger | Expected output |\n | --- | --- | --- |\n | `setup-matt-pocock-skills` | First configuring a repo for these skills, or when issue tracker, triage labels, or domain docs are missing. | `docs/agents/` configuration and an `Agent skills` block in `AGENTS.md` or `CLAUDE.md`. |\n-| `grill-with-docs` | Ambiguous plan, unclear terminology, boundary decision, tradeoff, or ADR-worthy design choice. | Clarified decision tree, recommended answers, updates to `CONTEXT.md` or ADRs when decisions crystallize. |\n+| `grill-with-docs` | Ambiguous plan, unclear terminology, boundary decision, tradeoff, or decision-record-worthy design choice. | Clarified decision tree, recommended answers, updates to `CONTEXT.md`, SADRs, or target ADRs when decisions crystallize. |\n | `zoom-out` | Agent lacks a higher-level architectural map. | Map of relevant modules, callers, seams, and domain concepts. |\n | `to-prd` | Settled context should become a PRD. | PRD published to the issue tracker with problem, solution, user stories, decisions, testing, and out-of-scope notes. |\n | `to-issues` | Goal, PRD, or plan must become implementation tickets. | Vertical-slice issues with acceptance criteria, dependencies, and AFK/HITL classification. |\n@@ -17,7 +17,7 @@\n | `tdd` | Feature work or bug fix has observable behavior that can be specified with tests. | Red-green-refactor loop through public interfaces, one vertical slice at a time. |\n | `code-review` | The TDD loop is green and the implementer is about to create or update a PR. Default for every implementation task. | Pre-PR self-review of the task diff with fixes applied on the task branch; findings and resolutions recorded as a ledger entry naming the skill and a `## Pre-PR code review` PR body section. |\n | `diagnose` | Hard bug, failing check, regression, flaky behavior, or performance problem. | Reproduction loop, ranked hypotheses, instrumentation, fix, regression test, post-mortem. |\n-| `improve-codebase-architecture` | Structural friction, testability, AI-navigability, deep-module opportunity, or refactor request. | Architecture candidates and deepening recommendations informed by `CONTEXT.md` and ADRs. |\n+| `improve-codebase-architecture` | Structural friction, testability, AI-navigability, deep-module opportunity, or refactor request. | Architecture candidates and deepening recommendations informed by `CONTEXT.md`, SADRs, and target ADRs. |\n | `prototype` | A throwaway logic or UI prototype is needed to answer a design question. | Clearly marked disposable prototype and captured decision. |\n \n ## MergeGate Interaction\ndiff --git a/docs/operations.md b/docs/operations.md\nindex cf1f452d4c589e38ccd58553bda0ad8684b51eb7..ace48a90ffbec9e5c0a7113bef16a4858bc44b3f\n--- a/docs/operations.md\n+++ b/docs/operations.md\n@@ -89,6 +89,16 @@\n      target history, brought forward by `shiki migrate`, not a template.\n    - **Everything else** (CLI scripts, workflows, schemas, docs) is overwritten.\n \n+   A forced upgrade also migrates the former numeric Shiki decision paths to\n+   `SADR-NNNN-*`. Before the first write, the installer inventories the exact\n+   legacy Shiki paths and authorizes each deletion only when the existing\n+   `.shiki/install-stamp.json` names that path and its SHA-256 digest still\n+   matches the file. An absent, unreadable, malformed, incomplete, or\n+   mismatching stamp reports every blocked path and stops with no filesystem\n+   change. Target-owned `NNNN-*` ADRs are not cleanup candidates. A non-force\n+   install deletes nothing and retains any still-matching legacy stamp entries\n+   so a later forced upgrade can prove ownership.\n+\n    If the target has pending migrations, `--force` refuses and writes nothing \u2014\n    apply migrations first (step 3), then re-run. The run ends with a single\n    summary of every `.new` written, so a half-upgrade is visible.\ndiff --git a/docs/releasing.md b/docs/releasing.md\nindex 10d73828e7dbd64e9d9f6b83bc84d010537ce62a..aafeee8c4d441e1e427efee2ce4f81a647f020e1\n--- a/docs/releasing.md\n+++ b/docs/releasing.md\n@@ -67,7 +67,7 @@\n CLI in `scripts/`. There are no third-party Python runtime dependencies. The CLI\n is intentionally **not** exposed as a `[project.scripts]` console entry point\n because `scripts/shiki.py` is a dependency-free shim and `scripts/` is not\n-packaged as an importable distribution (see the ADR for the rationale).\n+packaged as an importable distribution (see SADR-0007 for the rationale).\n \n Supported install paths:\n \n@@ -155,6 +155,15 @@\n    [docs/operations.md](operations.md#the-new-convention). If the target has\n    pending migrations, `--force` refuses and writes nothing; apply migrations\n    (step 2) first, then re-run.\n+\n+   The SADR namespace upgrade has an additional pre-write gate: each former\n+   numeric Shiki decision path is removed only when the existing install stamp\n+   contains that exact path and a digest matching its current bytes. The\n+   installer aggregates absent/unreadable/malformed-stamp, missing-digest, and\n+   digest-mismatch blockers before raising, so refusal leaves the target tree\n+   byte-identical. Numeric target ADRs are preserved. A successful forced\n+   upgrade installs and stamps `SADR-*` paths and drops the removed legacy paths\n+   from the refreshed stamp.\n 2. Apply repository-local `.shiki/` state migrations with the migrate command:\n \n    ```bash\ndiff --git a/docs/usage.md b/docs/usage.md\nindex b83407ee3970dd395dd893010bc5dbe8bad26051..a130ef6e40dfd8169a5bc701339351097df1a09f\n--- a/docs/usage.md\n+++ b/docs/usage.md\n@@ -67,8 +67,10 @@\n shiki install-target /path/to/target-repo --local-only\n ```\n \n-Use `--force` only when you intentionally want to overwrite existing target\n-files.\n+Use `--force` only when you intentionally want to refresh the target template.\n+For the SADR namespace migration, `--force` deletes only exact legacy Shiki\n+decision paths whose existing install-stamp digest still matches; any ownership\n+blocker stops before the first write. Numeric target ADRs are never candidates.\n \n ## `shiki bootstrap-platform` \u2014 publish the Shiki platform repo\n \ndiff --git a/scripts/guardian_approval_signal.py b/scripts/guardian_approval_signal.py\nindex d059f92b2c82aa2b5d6ca421365537a87886c1cb..16924892714f4f5b9303c0ef0e325a7e70075630\n--- a/scripts/guardian_approval_signal.py\n+++ b/scripts/guardian_approval_signal.py\n@@ -199,8 +199,8 @@\n ) -> Any:\n     \"\"\"Evaluate SADR-0015 Contract Approval for the CCA Guardian signal.\n \n-    The SAME OR that MergeGate applies must reach this deterministic signal: ADR\n-    0015 is explicit that without it, a contract-approved PR still returns\n+    The SAME OR that MergeGate applies must reach this deterministic signal:\n+    SADR-0015 is explicit that without it, a contract-approved PR still returns\n     ``needs_guardian``, the CCA job fails, and the MergeGate policy check never\n     runs \u2014 the gate this change moves would never be reached. This resolves the\n     task the SAME way MergeGate does (the first Shiki task id in the PR body),\ndiff --git a/scripts/shiki_contract_approval.py b/scripts/shiki_contract_approval.py\nindex 941bcb96fdd8b9a85bc0c4f0f04d5dad9d04839c..4a4b418f6527413161db82670bec857a043360e2\n--- a/scripts/shiki_contract_approval.py\n+++ b/scripts/shiki_contract_approval.py\n@@ -142,7 +142,7 @@\n     changed_files_status: Any,\n     registration: Any,\n ) -> ContractApprovalResult:\n-    \"\"\"Whether this PR carries an SADR-0015 Contract Approval.\n+    \"\"\"Whether this PR carries a SADR-0015 Contract Approval.\n \n     The four conditions are evaluated IN ORDER, each failure recorded as a\n     distinct reason string:\ndiff --git a/scripts/shiki_guardian.py b/scripts/shiki_guardian.py\nindex a4ecb79bbda55fd5c8b06adfae2eb99760a03c45..4dd99bbe714e26f421fa19abac7a34ae1b195ecc\n--- a/scripts/shiki_guardian.py\n+++ b/scripts/shiki_guardian.py\n@@ -12,7 +12,7 @@\n # The carry re-derives its proof from git in-process on every judging invocation;\n # it is never read from an artifact, so there is nothing to go stale or be forged.\n # shiki_sync_proof is the ONLY guardian-family module that runs git, and every one\n-# of its invocations is read-only and local (ADR: prove-from-git base sync).\n+# of its invocations is read-only and local (SADR: prove-from-git base sync).\n from shiki_sync_proof import verify_pure_base_sync\n \n GUARDIAN_POLICY_PATH = \".shiki/guardian-policy.json\"\ndiff --git a/scripts/shiki_guardian_review.py b/scripts/shiki_guardian_review.py\nindex dd0f104d457899e0ba5ff1e162011c1e90b6f92b..e10ef58c046324e7ea3a3392feb3048ca77414ce\n--- a/scripts/shiki_guardian_review.py\n+++ b/scripts/shiki_guardian_review.py\n@@ -159,7 +159,7 @@\n     docs_only = _is_docs_only(changed_files)\n     if docs_only and matched == 0:\n         focus.append(\n-            \"Docs/ADR-only change: confirm the change is genuinely documentation/decision-only, introduces no behavior or contract drift, and stays consistent with the constitution and existing ADRs.\"\n+            \"Docs/decision-record-only change: confirm the change is genuinely documentation/decision-only, introduces no behavior or contract drift, and stays consistent with the constitution and existing SADRs and target ADRs.\"\n         )\n \n     # Conservative augmentation: unknown (no category and not docs-only), mixed\ndiff --git a/scripts/shiki_installer.py b/scripts/shiki_installer.py\nindex be064ab7a29ff6836f7f7a1d70500a43e0d11d7b..9fd8eefd137ce7003dad5c0dcb2e48e2cd47ab32\n--- a/scripts/shiki_installer.py\n+++ b/scripts/shiki_installer.py\n@@ -118,6 +118,7 @@\n # what lets a target carry it without a governance-schema change.\n INSTALL_STAMP_PATH = \".shiki/install-stamp.json\"\n INSTALL_STAMP_VERSION = 1\n+LEGACY_SHIKI_SADR_MAX = 18\n \n # Under --force the shipped surface splits three ways, so nothing is lost and\n # nothing is silently stale.\n@@ -237,9 +238,98 @@\n         )\n \n \n+def legacy_shiki_adr_paths() -> tuple[str, ...]:\n+    \"\"\"Former numeric paths for Shiki SADR-0001 through SADR-0018.\n+\n+    The authoritative slugs come from the current SADR filenames. SADR-0019 is\n+    the namespace decision itself and never shipped under a numeric legacy path.\n+    \"\"\"\n+    paths: list[str] = []\n+    for sadr in sorted((ROOT / \"docs\" / \"adr\").glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\")):\n+        number = sadr.name.split(\"-\", 2)[1]\n+        if int(number) <= LEGACY_SHIKI_SADR_MAX:\n+            paths.append(f\"docs/adr/{sadr.name.removeprefix('SADR-')}\")\n+    return tuple(paths)\n+\n+\n+def preflight_legacy_sadr_cleanup(target: Path, *, force: bool) -> tuple[str, ...]:\n+    \"\"\"Authorize exact legacy Shiki ADR removals before the first write.\n+\n+    A non-force install never removes existing files. A forced install needs no\n+    stamp when no legacy Shiki path is present. When legacy paths do exist, each\n+    must be named in a readable install stamp and its current bytes must match\n+    the recorded digest.\n+    \"\"\"\n+    if not force:\n+        return ()\n+    present = tuple(\n+        relative\n+        for relative in legacy_shiki_adr_paths()\n+        if (target / relative).exists() or (target / relative).is_symlink()\n+    )\n+    if not present:\n+        return ()\n+\n+    stamp_path = target / INSTALL_STAMP_PATH\n+    try:\n+        raw = stamp_path.read_text(encoding=\"utf-8\")\n+        stamp = json.loads(raw)\n+    except FileNotFoundError:\n+        detail = \"install stamp is absent\"\n+        blockers = [f\"{relative}: {detail}\" for relative in present]\n+    except (OSError, UnicodeDecodeError) as error:\n+        detail = f\"install stamp is unreadable ({error})\"\n+        blockers = [f\"{relative}: {detail}\" for relative in present]\n+    except json.JSONDecodeError as error:\n+        detail = f\"install stamp is malformed ({error})\"\n+        blockers = [f\"{relative}: {detail}\" for relative in present]\n+    else:\n+        if not isinstance(stamp, dict):\n+            blockers = [\n+                f\"{relative}: install stamp is malformed (expected an object)\"\n+                for relative in present\n+            ]\n+        else:\n+            digests = stamp.get(\"digests\")\n+            blockers = []\n+            if not isinstance(digests, dict):\n+                blockers.extend(\n+                    f\"{relative}: install stamp has no digest for this path\"\n+                    for relative in present\n+                )\n+            else:\n+                for relative in present:\n+                    expected = digests.get(relative)\n+                    if not isinstance(expected, str) or not expected:\n+                        blockers.append(\n+                            f\"{relative}: install stamp has no digest for this path\"\n+                        )\n+                        continue\n+                    try:\n+                        actual = _sha256_file(target / relative)\n+                    except OSError as error:\n+                        blockers.append(\n+                            f\"{relative}: legacy path is unreadable ({error})\"\n+                        )\n+                        continue\n+                    if actual != expected:\n+                        blockers.append(\n+                            f\"{relative}: digest mismatch (stamp {expected}, current {actual})\"\n+                        )\n+\n+    if blockers:\n+        joined = \"\\n\".join(f\"- {blocker}\" for blocker in blockers)\n+        raise ShikiError(\n+            \"legacy Shiki ADR cleanup is not authorized:\\n\"\n+            f\"{joined}\\nNo files were written.\"\n+        )\n+    return present\n+\n+\n def install_template(target: Path, *, force: bool, validate: bool) -> list[NewFileNote]:\n     # Preconditions first: never rewrite the tree of a target that cannot be\n     # upgraded cleanly (see refuse_pending_migrations).\n+    legacy_cleanup = preflight_legacy_sadr_cleanup(target, force=force)\n     refuse_pending_migrations(target)\n \n     # Resolve the target's required CODEOWNERS owner once from its\n@@ -250,6 +340,10 @@\n     # verbatim. See ``codeowners_required_owner``.\n     codeowners_owner = codeowners_required_owner(target)\n \n+    for relative in legacy_cleanup:\n+        (target / relative).unlink()\n+        info(f\"removed stamped legacy Shiki ADR: {target / relative}\")\n+\n     notes: list[NewFileNote] = []\n     for relative in TEMPLATE_PATHS:\n         source = ROOT / relative\n@@ -647,6 +741,22 @@\n     digests = target_stamp_digests(target)\n     existing = read_install_stamp(target)\n     if existing is not None:\n+        # A non-force install deliberately leaves legacy Shiki ADRs in place.\n+        # Preserve only still-matching ownership proof so a later forced\n+        # upgrade can authorize cleanup; never carry a missing or stale digest.\n+        existing_digests = existing.get(\"digests\")\n+        if isinstance(existing_digests, dict):\n+            for relative in legacy_shiki_adr_paths():\n+                expected = existing_digests.get(relative)\n+                path = target / relative\n+                if not isinstance(expected, str) or not path.is_file():\n+                    continue\n+                try:\n+                    actual = _sha256_file(path)\n+                except OSError:\n+                    continue\n+                if actual == expected:\n+                    digests[relative] = actual\n         if _incoming_is_older(commit, existing.get(\"platform_commit\")):\n             warn(\n                 \"kept existing install stamp: incoming install \"\ndiff --git a/scripts/shiki_loop.py b/scripts/shiki_loop.py\nindex 3611641da8aeb5038b1da90e3114b09a473ec6d8..84caa491b9cad0edc4176f209c13e9221e975681\n--- a/scripts/shiki_loop.py\n+++ b/scripts/shiki_loop.py\n@@ -424,7 +424,7 @@\n         # unresolvable CCA verdict has already produced its terminal stop and is never\n         # laundered into a head-moving sync (SADR-0010). It also never fires while the\n         # policy (Guardian) gate is red \u2014 for ANY reason, not only when it is the sole\n-        # red check \u2014 so a head-bound Guardian approval, or an SADR-0015\n+        # red check \u2014 so a head-bound Guardian approval, or a SADR-0015\n         # contract-approval carry that base movement defeated (the policy gate compares\n         # base and head task governance directly), is never silently restored by\n         # adopting base wholesale. It is bounded by a durable per-task counter so the\ndiff --git a/scripts/shiki_tasks.py b/scripts/shiki_tasks.py\nindex aec3809f24b4a0c8c96fc50971cff9ae9ad30381..d531052e9d9717058d4631483cefab44c26a65c1\n--- a/scripts/shiki_tasks.py\n+++ b/scripts/shiki_tasks.py\n@@ -821,7 +821,7 @@\n         \"prompt\": prompt,\n         \"entry_skill\": \"grill-with-docs\",\n         \"required_next_steps\": [\n-            \"Run grill-with-docs until terminology, boundaries, risks, and ADR-worthy decisions are settled.\",\n+            \"Run grill-with-docs until terminology, boundaries, risks, and decision-record-worthy choices are settled.\",\n             \"Obtain the operator's explicit approval of the PRD/requirements (Spec Freeze).\",\n             \"Write a machine-readable plan JSON with grill_with_docs.status=complete and spec_freeze.status=frozen.\",\n             \"Run shiki plan ingest --plan-file PLAN.json.\",\ndiff --git a/scripts/test_shiki_goal_loop.sh b/scripts/test_shiki_goal_loop.sh\nindex 753c7b516e10f0be3bc92a4bd0eb1fa699776ae6..8f3820a0a079b3044424cfe1a6c6b7fe68896e67\n--- a/scripts/test_shiki_goal_loop.sh\n+++ b/scripts/test_shiki_goal_loop.sh\n@@ -231,8 +231,8 @@\n \n # High/critical risk merges autonomously when all required checks are green:\n # the \"MergeGate policy check\" required check IS the Guardian gate (it enforces\n-# guardian-policy.json \u2014 human approval OR external AI guardian review, ADR\n-# 0010), so green checks mean a recorded authority approved. The fake gh returns\n+# guardian-policy.json \u2014 human approval OR external AI guardian review,\n+# SADR-0010), so green checks mean a recorded authority approved. The fake gh returns\n # green checks, simulating that the Guardian gate passed.\n python3 \"$ROOT/scripts/shiki.py\" goal create --target \"$TARGET\" --title \"Guardian gate\" --outcome \"High risk merges via policy gate\" >/tmp/shiki-goal-loop-guardian-goal.json\n GGOAL=\"$(json_get /tmp/shiki-goal-loop-guardian-goal.json goal_id)\"\ndiff --git a/tests/test_adr_references.py b/tests/test_adr_references.py\nindex 51706329c797d5dd9b47e082a333fa83b261d482..d005da4f771f5e8085ec22d4b787980e68f5ae8c\n--- a/tests/test_adr_references.py\n+++ b/tests/test_adr_references.py\n@@ -1,8 +1,8 @@\n-\"\"\"Every ``ADR ####`` citation must resolve to exactly one decision record.\n+\"\"\"Every target ADR and Shiki SADR citation resolves in its own namespace.\n \n Background. ``scripts/mergegate_check.py`` justifies ``is_bookkeeping_closeout``\n-with \"ADR 0017\" nine times and ``scripts/guardian_approval_signal.py`` cites\n-0016/0017 seven times, yet for a stretch the numbering in ``docs/adr/`` stopped\n+with \"SADR-0017\" nine times and ``scripts/guardian_approval_signal.py`` cites\n+SADR-0016/SADR-0017 seven times, yet for a stretch the numbering in ``docs/adr/`` stopped\n at 0015: the authoring commits lived on a coordinator branch and no task ever\n declared ``docs/adr/...`` in its locks, so nothing carried the records to the\n default branch. The stated basis of an exemption that merged seven PRs with zero\n@@ -11,11 +11,12 @@\n and the directory drifted with no signal.\n \n This is the same unbound-pair shape behind other silent failures: a reference\n-and its target with nothing asserting they agree. This test binds them. It scans\n-every ``scripts/*.py`` (plus ``docs/**/*.md`` and the root ``*.md`` governance\n-files, which are cheap) for the ``ADR ####`` pattern and asserts each cited\n-number resolves to exactly one ``docs/adr/####-*.md``. The converse is\n-deliberately NOT required: an ADR may exist without being cited.\n+and its target with nothing asserting they agree. This test binds them. Target\n+``ADR NNNN`` citations resolve only to ``docs/adr/NNNN-*.md``; Shiki\n+``SADR-NNNN`` citations resolve only to ``docs/adr/SADR-NNNN-*.md``. The scan\n+covers current governance docs, scripts, workflows, prompts, packaging metadata,\n+and repo-local Codex guidance while deliberately excluding historical\n+``.shiki`` evidence. The converse is not required: a record may be uncited.\n \n The suite is dependency free: standard-library ``unittest`` only.\n \"\"\"\n@@ -31,17 +32,18 @@\n \n REPO_ROOT = Path(__file__).resolve().parents[1]\n \n-# \"ADR 0016\" -- a four-digit citation. The trailing \\b keeps a five-digit token\n-# from being read as a truncated four-digit number.\n-_CITATION = re.compile(r\"\\bADR (\\d{4})\\b\")\n-# \"0016-locks-bound-product-paths.md\" -- the leading four digits name the record.\n-_ADR_FILENAME = re.compile(r\"^(\\d{4})-.*\\.md$\")\n+# ``ADR 0016`` / ``SADR-0016`` -- a namespace plus four-digit citation. The\n+# trailing ``\\b`` keeps a five-digit token from being read as a truncated one.\n+_CITATION = re.compile(r\"\\b(S?ADR)[ -](\\d{4})\\b\")\n+# Target ADRs retain the numeric filename; Shiki platform decisions use SADR.\n+_DECISION_FILENAME = re.compile(r\"^(?:(SADR)-)?(\\d{4})-.*\\.md$\")\n \n \n class Violation(NamedTuple):\n     \"\"\"A citation that does not resolve to exactly one decision record.\"\"\"\n \n     citing_file: str  # repo-relative path of the file that cites the ADR\n+    namespace: str  # \"ADR\" for target decisions; \"SADR\" for Shiki decisions\n     number: str  # the cited four-digit number, e.g. \"0016\"\n     line: int  # 1-based line of the citation\n     reason: str  # \"missing\" (zero matches) or \"ambiguous\" (two or more)\n@@ -49,44 +51,59 @@\n \n     @property\n     def expected_path(self) -> str:\n-        return f\"docs/adr/{self.number}-*.md\"\n+        prefix = \"SADR-\" if self.namespace == \"SADR\" else \"\"\n+        return f\"docs/adr/{prefix}{self.number}-*.md\"\n \n     def describe(self) -> str:\n         \"\"\"Name the citing file, the cited number, and the missing path.\"\"\"\n         if self.reason == \"missing\":\n             return (\n-                f\"{self.citing_file}:{self.line} cites ADR {self.number} \"\n+                f\"{self.citing_file}:{self.line} cites {self.namespace} {self.number} \"\n                 f\"but no document matches {self.expected_path}\"\n             )\n         joined = \", \".join(self.matches)\n         return (\n-            f\"{self.citing_file}:{self.line} cites ADR {self.number} which \"\n+            f\"{self.citing_file}:{self.line} cites {self.namespace} {self.number} which \"\n             f\"resolves to {len(self.matches)} documents (expected exactly one \"\n             f\"at {self.expected_path}): {joined}\"\n         )\n \n \n-def _adr_index(root: Path) -> dict[str, list[Path]]:\n-    \"\"\"Map every four-digit ADR number to the record files that claim it.\"\"\"\n-    index: dict[str, list[Path]] = {}\n+def _adr_index(root: Path) -> dict[tuple[str, str], list[Path]]:\n+    \"\"\"Map each decision namespace and number to the files that claim it.\"\"\"\n+    index: dict[tuple[str, str], list[Path]] = {}\n     adr_dir = root / \"docs\" / \"adr\"\n     if adr_dir.is_dir():\n         for path in sorted(adr_dir.glob(\"*.md\")):\n-            match = _ADR_FILENAME.match(path.name)\n+            match = _DECISION_FILENAME.match(path.name)\n             if match:\n-                index.setdefault(match.group(1), []).append(path)\n+                namespace = \"SADR\" if match.group(1) else \"ADR\"\n+                index.setdefault((namespace, match.group(2)), []).append(path)\n     return index\n \n \n def _citing_files(root: Path, include_docs: bool = True) -> list[Path]:\n-    \"\"\"Files scanned for citations: scripts/*.py, and cheaply the markdown.\"\"\"\n+    \"\"\"Current, shipped text surfaces scanned for decision citations.\"\"\"\n     files = sorted((root / \"scripts\").glob(\"*.py\"))\n+    files += sorted((root / \"scripts\").glob(\"*.sh\"))\n     if include_docs:\n         docs = root / \"docs\"\n         if docs.is_dir():\n             files += sorted(docs.rglob(\"*.md\"))\n         files += sorted(root.glob(\"*.md\"))\n-    return files\n+        pyproject = root / \"pyproject.toml\"\n+        if pyproject.is_file():\n+            files.append(pyproject)\n+        for relative in (\".github\", \".codex\"):\n+            surface = root / relative\n+            if surface.is_dir():\n+                files += sorted(\n+                    path\n+                    for path in surface.rglob(\"*\")\n+                    if path.is_file()\n+                    and path.suffix in {\".md\", \".py\", \".sh\", \".toml\", \".yaml\", \".yml\"}\n+                )\n+    return sorted(set(files))\n \n \n def find_adr_reference_violations(\n@@ -94,10 +111,9 @@\n ) -> list[Violation]:\n     \"\"\"Return every ``ADR ####`` citation under ``root`` that fails to resolve.\n \n-    A citation resolves when exactly one ``docs/adr/####-*.md`` matches its\n-    number. Zero matches is a \"missing\" violation; two or more is \"ambiguous\".\n-    Records that are never cited produce nothing -- the converse is not\n-    required.\n+    A citation resolves when exactly one record in the same namespace matches\n+    its number. Zero matches is a \"missing\" violation; two or more is\n+    \"ambiguous\". Records that are never cited produce nothing.\n     \"\"\"\n     root = Path(root)\n     index = _adr_index(root)\n@@ -110,13 +126,15 @@\n         rel = path.relative_to(root).as_posix()\n         for lineno, line in enumerate(text.splitlines(), start=1):\n             for match in _CITATION.finditer(line):\n-                number = match.group(1)\n-                found = index.get(number, [])\n+                namespace, number = match.groups()\n+                found = index.get((namespace, number), [])\n                 if len(found) == 1:\n                     continue\n                 reason = \"missing\" if not found else \"ambiguous\"\n                 rels = [p.relative_to(root).as_posix() for p in found]\n-                violations.append(Violation(rel, number, lineno, reason, rels))\n+                violations.append(\n+                    Violation(rel, namespace, number, lineno, reason, rels)\n+                )\n     return violations\n \n \n@@ -129,8 +147,8 @@\n         self.assertEqual(\n             violations,\n             [],\n-            \"Every 'ADR ####' citation must resolve to exactly one \"\n-            f\"docs/adr/####-*.md:\\n{detail}\",\n+            \"Every ADR/SADR citation must resolve exactly once in its own \"\n+            f\"docs/adr namespace:\\n{detail}\",\n         )\n \n     def test_scan_actually_reads_scripts(self) -> None:\n@@ -142,12 +160,38 @@\n             \"the citation scan must cover scripts/*.py\",\n         )\n \n+    def test_platform_citations_do_not_use_unresolved_numeric_shorthand(self) -> None:\n+        shorthand = re.compile(r\"SADR-\\d{4}/\\d{4}\")\n+        offenders = []\n+        for path in _citing_files(REPO_ROOT):\n+            text = path.read_text(encoding=\"utf-8\")\n+            if shorthand.search(text):\n+                offenders.append(path.relative_to(REPO_ROOT).as_posix())\n+        self.assertEqual(\n+            offenders,\n+            [],\n+            \"repeat the SADR prefix so every cited number is independently resolvable\",\n+        )\n+\n     def test_carried_records_are_indexed(self) -> None:\n         # The two records this Goal carried onto the default branch, and which\n         # the scripts cite most, must be present.\n         index = _adr_index(REPO_ROOT)\n-        self.assertIn(\"0016\", index)\n-        self.assertIn(\"0017\", index)\n+        self.assertIn((\"SADR\", \"0016\"), index)\n+        self.assertIn((\"SADR\", \"0017\"), index)\n+\n+    def test_platform_tree_has_only_sadr_0001_through_0019(self) -> None:\n+        adr_dir = REPO_ROOT / \"docs\" / \"adr\"\n+        sadrs = sorted(adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\"))\n+        self.assertEqual(\n+            {path.name.split(\"-\", 2)[1] for path in sadrs},\n+            {f\"{number:04d}\" for number in range(1, 20)},\n+        )\n+        self.assertEqual(\n+            list(adr_dir.glob(\"[0-9][0-9][0-9][0-9]-*.md\")),\n+            [],\n+            \"the Shiki platform tree must not retain legacy numeric SADR paths\",\n+        )\n \n \n class FixtureAdrReferences(unittest.TestCase):\n@@ -180,6 +224,26 @@\n         self.assertIn(\"9991\", message)  # cited number\n         self.assertIn(\"docs/adr/9991-*.md\", message)  # missing path\n \n+    def test_target_adr_does_not_satisfy_missing_platform_sadr(self) -> None:\n+        root = self._make_repo(\n+            adr_names=[\"0042-target-decision.md\"],\n+            script_body='\"\"\"Target ADR 0042 exists, but platform SADR-0042 does not.\"\"\"\\n',\n+        )\n+        violations = find_adr_reference_violations(root, include_docs=False)\n+        self.assertEqual(len(violations), 1)\n+        self.assertEqual(violations[0].namespace, \"SADR\")\n+        self.assertEqual(violations[0].number, \"0042\")\n+        self.assertEqual(violations[0].expected_path, \"docs/adr/SADR-0042-*.md\")\n+\n+    def test_target_adr_and_platform_sadr_may_reuse_a_number(self) -> None:\n+        root = self._make_repo(\n+            adr_names=[\"0042-target-decision.md\", \"SADR-0042-platform-decision.md\"],\n+            script_body='\"\"\"Target ADR 0042 and platform SADR-0042 are distinct.\"\"\"\\n',\n+        )\n+        self.assertEqual(\n+            find_adr_reference_violations(root, include_docs=False), []\n+        )\n+\n     def test_uncited_adr_is_allowed(self) -> None:\n         # An ADR exists but nothing cites it -> not a violation. The converse\n         # (every ADR must be cited) is explicitly NOT required.\ndiff --git a/tests/test_guardian_review.py b/tests/test_guardian_review.py\nindex 88829e7810212635e2fc98aa5918d6f26294677d..00a85a237acd5bf2d6cb5b294128198c422402ca\n--- a/tests/test_guardian_review.py\n+++ b/tests/test_guardian_review.py\n@@ -162,7 +162,7 @@\n \n     def test_docs_only_is_light_and_not_conservative(self):\n         focus = gr.classify_review_focus([\"docs/adr/0099-x.md\", \"README.md\"], \"\", \"low\")\n-        self.assertTrue(any(\"Docs/ADR-only\" in f for f in focus))\n+        self.assertTrue(any(\"Docs/decision-record-only\" in f for f in focus))\n         self.assertFalse(any(\"conservative default\" in f.lower() for f in focus))\n \n     def test_unknown_category_is_conservative(self):\ndiff --git a/tests/test_policy_job_merged_prs.py b/tests/test_policy_job_merged_prs.py\nindex d45512e74681e5e649279236c7d4e79d5c3da471..b97989f8bd74d091de1a6b5275a20d8dc68c4512\n--- a/tests/test_policy_job_merged_prs.py\n+++ b/tests/test_policy_job_merged_prs.py\n@@ -3,8 +3,8 @@\n \n ``.github/workflows/shiki-cca-completion.yml`` has two consumers of the merged-PR\n proof: the CCA *signal* (``guardian_approval_signal.py`` in \"Prepare CCA context\")\n-and the authoritative gate (``mergegate_check.py`` in \"Enforce MergeGate\"). ADR\n-0017's bookkeeping-closeout exemption (condition 6) needs the merged implementation\n+and the authoritative gate (``mergegate_check.py`` in \"Enforce MergeGate\").\n+SADR-0017's bookkeeping-closeout exemption (condition 6) needs the merged implementation\n PR proven, and a closeout carries NO ``post_merge_reconcile`` marker. The signal\n already resolves ``MERGED_PRS`` unconditionally, but \"Enforce MergeGate\" used to\n resolve it only inside ``grep -q '<!-- shiki:post_merge_reconcile -->'``. So a\ndiff --git a/tests/test_sadr_install_migration.py b/tests/test_sadr_install_migration.py\nnew file mode 100644\nindex 0000000000000000000000000000000000000000..d51eac872b1c79ff4579c4c1401ca5d775a60d8d\n--- /dev/null\n+++ b/tests/test_sadr_install_migration.py\n@@ -0,0 +1,237 @@\n+\"\"\"SADR installation and collision-safe legacy upgrade behavior.\"\"\"\n+\n+from __future__ import annotations\n+\n+import contextlib\n+import hashlib\n+import io\n+import json\n+import shutil\n+import tempfile\n+import unittest\n+from pathlib import Path\n+\n+import shiki_test_support  # noqa: F401  (path bootstrap)\n+\n+import shiki_installer\n+from tests.test_adr_references import find_adr_reference_violations\n+\n+\n+PRODUCT_NUMBERS = tuple(range(13, 19))\n+\n+\n+def _quiet_install(target: Path, *, force: bool, validate: bool) -> None:\n+    with contextlib.redirect_stdout(io.StringIO()):\n+        shiki_installer.install_template(target, force=force, validate=validate)\n+\n+\n+def _sha256(path: Path) -> str:\n+    return hashlib.sha256(path.read_bytes()).hexdigest()\n+\n+\n+def _legacy_sadr_paths() -> tuple[str, ...]:\n+    return tuple(\n+        f\"docs/adr/{path.name.removeprefix('SADR-')}\"\n+        for path in sorted((shiki_installer.ROOT / \"docs\" / \"adr\").glob(\"SADR-*.md\"))\n+        if path.name != \"SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\"\n+    )\n+\n+\n+def _create_legacy_upgrade_fixture(\n+    target: Path,\n+) -> tuple[dict[str, bytes], tuple[str, ...]]:\n+    adr_dir = target / \"docs\" / \"adr\"\n+    adr_dir.mkdir(parents=True)\n+    product_bytes = {\n+        f\"{number:04d}-cubeshare-product-decision.md\": (\n+            f\"# CubeShare ADR {number:04d}\\n\\nDo not rewrite {number}.\\n\"\n+        ).encode()\n+        for number in PRODUCT_NUMBERS\n+    }\n+    for name, content in product_bytes.items():\n+        (adr_dir / name).write_bytes(content)\n+\n+    legacy_paths = _legacy_sadr_paths()\n+    for relative in legacy_paths:\n+        (target / relative).write_bytes(f\"legacy Shiki bytes for {relative}\\n\".encode())\n+\n+    stamp_path = target / shiki_installer.INSTALL_STAMP_PATH\n+    stamp_path.parent.mkdir(parents=True, exist_ok=True)\n+    stamp = {\n+        \"version\": shiki_installer.INSTALL_STAMP_VERSION,\n+        \"platform_commit\": None,\n+        \"installed_at\": \"2000-01-01T00:00:00+00:00\",\n+        \"digests\": {\n+            relative: _sha256(target / relative) for relative in legacy_paths\n+        },\n+    }\n+    stamp_path.write_text(\n+        json.dumps(stamp, indent=2, sort_keys=True) + \"\\n\", encoding=\"utf-8\"\n+    )\n+    return product_bytes, legacy_paths\n+\n+\n+def _tree_snapshot(root: Path) -> dict[str, tuple[str, bytes | str]]:\n+    \"\"\"Recursive byte/shape snapshot used to prove preflight is write-free.\"\"\"\n+    snapshot: dict[str, tuple[str, bytes | str]] = {}\n+    for path in sorted(root.rglob(\"*\")):\n+        relative = path.relative_to(root).as_posix()\n+        if path.is_symlink():\n+            snapshot[relative] = (\"symlink\", path.readlink().as_posix())\n+        elif path.is_dir():\n+            snapshot[relative] = (\"directory\", \"\")\n+        else:\n+            snapshot[relative] = (\"file\", path.read_bytes())\n+    return snapshot\n+\n+\n+class FreshSadrInstallTests(unittest.TestCase):\n+    def setUp(self) -> None:\n+        self.tmp = Path(tempfile.mkdtemp(prefix=\"shiki-sadr-fresh-\"))\n+        self.addCleanup(lambda: shutil.rmtree(self.tmp, ignore_errors=True))\n+        self.target = self.tmp / \"target\"\n+        self.adr_dir = self.target / \"docs\" / \"adr\"\n+        self.adr_dir.mkdir(parents=True)\n+        self.product_bytes = {\n+            f\"{number:04d}-product-decision.md\": (\n+                f\"# Product ADR {number:04d}\\n\\nTarget-owned bytes {number}.\\n\"\n+            ).encode()\n+            for number in PRODUCT_NUMBERS\n+        }\n+        for name, content in self.product_bytes.items():\n+            (self.adr_dir / name).write_bytes(content)\n+\n+    def test_fresh_install_preserves_product_adrs_and_installs_all_sadrs(self) -> None:\n+        _quiet_install(self.target, force=False, validate=True)\n+\n+        for name, expected in self.product_bytes.items():\n+            self.assertEqual((self.adr_dir / name).read_bytes(), expected)\n+\n+        sadr_numbers = {\n+            path.name.split(\"-\", 2)[1]\n+            for path in self.adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\")\n+        }\n+        self.assertEqual(sadr_numbers, {f\"{number:04d}\" for number in range(1, 20)})\n+\n+        numeric_records = {\n+            path.name\n+            for path in self.adr_dir.glob(\"[0-9][0-9][0-9][0-9]-*.md\")\n+        }\n+        self.assertEqual(numeric_records, set(self.product_bytes))\n+        stamp = json.loads(\n+            (self.target / shiki_installer.INSTALL_STAMP_PATH).read_text(\n+                encoding=\"utf-8\"\n+            )\n+        )\n+        for path in self.adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\"):\n+            relative = path.relative_to(self.target).as_posix()\n+            self.assertEqual(stamp[\"digests\"][relative], _sha256(path))\n+        self.assertEqual(find_adr_reference_violations(self.target), [])\n+\n+\n+class LegacySadrUpgradeTests(unittest.TestCase):\n+    def setUp(self) -> None:\n+        self.tmp = Path(tempfile.mkdtemp(prefix=\"shiki-sadr-upgrade-\"))\n+        self.addCleanup(lambda: shutil.rmtree(self.tmp, ignore_errors=True))\n+        self.target = self.tmp / \"target\"\n+        self.adr_dir = self.target / \"docs\" / \"adr\"\n+        self.product_bytes, self.legacy_paths = _create_legacy_upgrade_fixture(\n+            self.target\n+        )\n+\n+    def test_force_upgrade_deletes_only_stamped_legacy_sadrs(self) -> None:\n+        _quiet_install(self.target, force=True, validate=True)\n+\n+        for relative in self.legacy_paths:\n+            self.assertFalse((self.target / relative).exists(), relative)\n+        for name, expected in self.product_bytes.items():\n+            self.assertEqual((self.adr_dir / name).read_bytes(), expected)\n+\n+        sadrs = list(self.adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\"))\n+        self.assertEqual(len(sadrs), 19)\n+        refreshed = json.loads(\n+            (self.target / shiki_installer.INSTALL_STAMP_PATH).read_text(encoding=\"utf-8\")\n+        )\n+        for relative in self.legacy_paths:\n+            self.assertNotIn(relative, refreshed[\"digests\"])\n+        for path in sadrs:\n+            relative = path.relative_to(self.target).as_posix()\n+            self.assertEqual(refreshed[\"digests\"][relative], _sha256(path))\n+\n+    def test_non_force_install_never_deletes_legacy_files(self) -> None:\n+        before = {\n+            relative: (self.target / relative).read_bytes()\n+            for relative in self.legacy_paths\n+        }\n+        _quiet_install(self.target, force=False, validate=False)\n+        for relative, expected in before.items():\n+            self.assertEqual((self.target / relative).read_bytes(), expected)\n+        refreshed = json.loads(\n+            (self.target / shiki_installer.INSTALL_STAMP_PATH).read_text(encoding=\"utf-8\")\n+        )\n+        for relative in self.legacy_paths:\n+            self.assertEqual(refreshed[\"digests\"][relative], _sha256(self.target / relative))\n+\n+\n+class LegacyCleanupPreflightTests(unittest.TestCase):\n+    def setUp(self) -> None:\n+        self.tmp = Path(tempfile.mkdtemp(prefix=\"shiki-sadr-preflight-\"))\n+        self.addCleanup(lambda: shutil.rmtree(self.tmp, ignore_errors=True))\n+\n+    def test_all_ownership_blockers_abort_before_any_write(self) -> None:\n+        cases = (\n+            (\"absent\", \"install stamp is absent\"),\n+            (\"unreadable\", \"install stamp is unreadable\"),\n+            (\"malformed\", \"install stamp is malformed\"),\n+            (\"missing-digest\", \"install stamp has no digest\"),\n+            (\"digest-mismatch\", \"digest mismatch\"),\n+        )\n+        for name, expected_reason in cases:\n+            with self.subTest(name=name):\n+                target = self.tmp / name\n+                _, legacy_paths = _create_legacy_upgrade_fixture(target)\n+                stamp_path = target / shiki_installer.INSTALL_STAMP_PATH\n+\n+                if name == \"absent\":\n+                    stamp_path.unlink()\n+                elif name == \"unreadable\":\n+                    stamp_path.unlink()\n+                    stamp_path.mkdir()\n+                elif name == \"malformed\":\n+                    stamp_path.write_text(\"{not-json\\n\", encoding=\"utf-8\")\n+                else:\n+                    stamp = json.loads(stamp_path.read_text(encoding=\"utf-8\"))\n+                    if name == \"missing-digest\":\n+                        stamp[\"digests\"] = {}\n+                    else:\n+                        stamp[\"digests\"] = {\n+                            relative: \"deadbeef\" for relative in legacy_paths\n+                        }\n+                    stamp_path.write_text(\n+                        json.dumps(stamp, indent=2, sort_keys=True) + \"\\n\",\n+                        encoding=\"utf-8\",\n+                    )\n+\n+                before = _tree_snapshot(target)\n+                with self.assertRaises(shiki_installer.ShikiError) as raised:\n+                    _quiet_install(target, force=True, validate=False)\n+                after = _tree_snapshot(target)\n+\n+                self.assertEqual(before, after, f\"{name} preflight changed the tree\")\n+                message = str(raised.exception)\n+                self.assertIn(expected_reason, message)\n+                self.assertIn(\"No files were written\", message)\n+                for relative in legacy_paths:\n+                    self.assertIn(relative, message)\n+\n+    def test_absent_stamp_is_allowed_when_no_legacy_path_exists(self) -> None:\n+        target = self.tmp / \"no-legacy\"\n+        target.mkdir()\n+        _quiet_install(target, force=True, validate=False)\n+        self.assertTrue(\n+            (target / \"docs\" / \"adr\" / \"SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\").is_file()\n+        )\n+\n+\n+if __name__ == \"__main__\":\n+    unittest.main()\n\n2026-08-07T08:29:32.533776Z ERROR codex_models_manager::manager: failed to renew cache TTL: missing field `base_instructions` at line 94 column 5\nexec\n/bin/zsh -lc 'git diff --check' in /private/tmp/shiki-adr-collision.pa0KwY/.worktrees/shiki-t-20260807t073307221254z-2d22d654-introduc\n succeeded in 0ms:\nexec\n/bin/zsh -lc 'python3 scripts/validate_shiki.py --target .' in /private/tmp/shiki-adr-collision.pa0KwY/.worktrees/shiki-t-20260807t073307221254z-2d22d654-introduc\ndiff --git a/.codex/skills/shiki/SKILL.md b/.codex/skills/shiki/SKILL.md\nindex c8e2e85d9d75d49cc4f2d6286ee22659c4e53e75..93cded623b2c15d9f65c76491a1c2d813ac9e6c0\n--- a/.codex/skills/shiki/SKILL.md\n+++ b/.codex/skills/shiki/SKILL.md\n@@ -18,6 +18,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n Codex CLI does not register this skill as a custom `/shiki` slash command. When\n the user types `/shiki` in Codex CLI and gets \"Unrecognized command\", explain\n that this is expected: invoke Shiki in Codex with natural language such as\n@@ -69,7 +72,7 @@\n \n ## Responsibilities\n \n-- Claude Code implements and repairs by default (`shiki runner claude`, ADR 0008); Codex implements and repairs tasks explicitly assigned to `codex`.\n+- Claude Code implements and repairs by default (`shiki runner claude`, SADR-0008); Codex implements and repairs tasks explicitly assigned to `codex`.\n - Claude Code Action can act as GitHub-side CCA or reviewer.\n - CCA judges completion.\n - MergeGate authorizes state transitions and merge readiness.\ndiff --git a/.github/prompts/cca-completion-check.md b/.github/prompts/cca-completion-check.md\nindex ed203ad5f302ecc81372d8ab27d2b1232ca97497..fd89950528333ef6242e6d5443904f5392ab7e56\n--- a/.github/prompts/cca-completion-check.md\n+++ b/.github/prompts/cca-completion-check.md\n@@ -19,6 +19,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n ## Required Reading\n \n Read only the files needed to judge the PR contract. Prefer this order:\ndiff --git a/.github/prompts/codex-task-handoff.md b/.github/prompts/codex-task-handoff.md\nindex 695e211d76dea1c60e8d5bb360e78487808286a4..59441d576b01a39569722f226c854e5a740e9eb5\n--- a/.github/prompts/codex-task-handoff.md\n+++ b/.github/prompts/codex-task-handoff.md\n@@ -2,7 +2,7 @@\n \n You are Codex Front acting as the Implementer for a Shiki task.\n \n-Follow `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant ADRs, and `docs/agents/codex-handoff.md`.\n+Follow `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant Shiki SADRs and target ADRs, and `docs/agents/codex-handoff.md`.\n \n Rules:\n \ndiff --git a/.shiki/ledger/L-20260807T082800000000Z-c0de0019.json b/.shiki/ledger/L-20260807T082800000000Z-c0de0019.json\nnew file mode 100644\nindex 0000000000000000000000000000000000000000..bac355ecea44037859b3715f58bc32f3353cfcfa\n--- /dev/null\n+++ b/.shiki/ledger/L-20260807T082800000000Z-c0de0019.json\n@@ -0,0 +1,34 @@\n+{\n+  \"actor\": \"codex\",\n+  \"evidence\": [\n+    \"Full task diff reviewed against scope, non-goals, locks, acceptance checks, failure atomicity, path ownership, and compatibility requirements\",\n+    \"Finding repaired: tests/test_sadr_install_migration.py now asserts every fresh-installed SADR path has the matching SHA-256 digest in .shiki/install-stamp.json\",\n+    \"Re-review found no remaining blocking issues; rejected findings: none\",\n+    \"python3 -m unittest discover -s tests: 1147 tests passed\",\n+    \"bash scripts/test_shiki_control_plane.sh: passed\",\n+    \"bash scripts/test_shiki_adversarial_state.sh: passed\",\n+    \"python3 scripts/validate_shiki.py --target .: passed\"\n+  ],\n+  \"goal_id\": \"G-20260807T073307219752Z-1ee1ed68\",\n+  \"id\": \"L-20260807T082800000000Z-c0de0019\",\n+  \"links\": [],\n+  \"review\": {\n+    \"applied_fixes\": [\n+      \"Added direct fresh-install stamp path/digest assertions and reran focused plus full verification\"\n+    ],\n+    \"findings\": [\n+      {\n+        \"detail\": \"The fresh-install test proved SADR files were installed but did not directly prove their paths and digests were recorded in the refreshed install stamp.\",\n+        \"resolution\": \"fixed_and_re_reviewed\",\n+        \"severity\": \"blocking\",\n+        \"title\": \"Fresh-install stamp evidence was indirect\"\n+      }\n+    ],\n+    \"rejected_findings\": [],\n+    \"verdict\": \"clean_after_repairs\"\n+  },\n+  \"summary\": \"code-review implementer verdict CLEAN after re-review: findings=1; applied_fixes=1 (fresh-install stamp-digest assertion); rejected_findings=0 (none)\",\n+  \"task_id\": \"T-20260807T073307221254Z-2d22d654\",\n+  \"timestamp\": \"2026-08-07T08:28:00+00:00\",\n+  \"type\": \"review\"\n+}\ndiff --git a/.shiki/ledger/L-20260807T082914000000Z-7dd00019.json b/.shiki/ledger/L-20260807T082914000000Z-7dd00019.json\nnew file mode 100644\nindex 0000000000000000000000000000000000000000..432bcd63849c04eaaeaca3993c03a07c1152bb1a\n--- /dev/null\n+++ b/.shiki/ledger/L-20260807T082914000000Z-7dd00019.json\n@@ -0,0 +1,19 @@\n+{\n+  \"actor\": \"codex\",\n+  \"evidence\": [\n+    \"RED resolver: the target-ADR-does-not-satisfy-SADR fixture expected one missing SADR violation but received zero before namespace-aware indexing\",\n+    \"RED fresh install: the product-ADR fixture installed no SADR records before the platform rename\",\n+    \"RED forced upgrade: matching stamped legacy Shiki ADR paths remained before cleanup was implemented\",\n+    \"RED non-force upgrade: refreshed stamp omitted preserved legacy ownership evidence before matching digest carry-forward was implemented\",\n+    \"GREEN focused: 11 ADR/SADR resolver tests and 5 fresh-install/upgrade/preflight tests passed\",\n+    \"REFACTOR: separated namespace indexing, exact legacy-path inventory, read-only cleanup authorization, and stamp refresh responsibilities\",\n+    \"GREEN regression: 1147 unit tests, control-plane shell suite, adversarial shell suite, and validate_shiki passed\"\n+  ],\n+  \"goal_id\": \"G-20260807T073307219752Z-1ee1ed68\",\n+  \"id\": \"L-20260807T082914000000Z-7dd00019\",\n+  \"links\": [],\n+  \"summary\": \"tdd red-green-refactor evidence for separate ADR/SADR resolution and collision-safe install migration\",\n+  \"task_id\": \"T-20260807T073307221254Z-2d22d654\",\n+  \"timestamp\": \"2026-08-07T08:29:14+00:00\",\n+  \"type\": \"check\"\n+}\ndiff --git a/.shiki/tasks/T-20260807T073307221254Z-2d22d654.json b/.shiki/tasks/T-20260807T073307221254Z-2d22d654.json\nindex e7e150cc1637f1cfac2199e04055e93b53630dcb..00241552cac63069660eb722bd076decf6a5031d\n--- a/.shiki/tasks/T-20260807T073307221254Z-2d22d654.json\n+++ b/.shiki/tasks/T-20260807T073307221254Z-2d22d654.json\n@@ -36,7 +36,9 @@\n     \"L-20260807T080107948254Z-61f24da0\",\n     \"L-20260807T080228312210Z-07ac626e\",\n     \"L-20260807T080228575038Z-d574f629\",\n-    \"L-20260807T080256330734Z-5bf40cb6\"\n+    \"L-20260807T080256330734Z-5bf40cb6\",\n+    \"L-20260807T082800000000Z-c0de0019\",\n+    \"L-20260807T082914000000Z-7dd00019\"\n   ],\n   \"locks\": [\n     \"path:.codex/**\",\ndiff --git a/AGENTS.md b/AGENTS.md\nindex 7f041100d49c2a7525f51cb0f5d696a4a6d1c78c..a06bfbabc6fa48a007389e25e12d7ed523936bed\n--- a/AGENTS.md\n+++ b/AGENTS.md\n@@ -22,7 +22,7 @@\n - **Codex Front** is an optional implementer through Codex App, Codex CLI, Codex IDE extension, or Codex Web signed in with ChatGPT OAuth/subscription auth, dispatched with `shiki runner codex` when a task is explicitly assigned to `codex`.\n - **GitHub CCA** is implemented by Claude Code Action by default, using `CLAUDE_CODE_OAUTH_TOKEN`.\n \n-Do not assume `openai/codex-action`, `OPENAI_API_KEY`, or API-key based Codex automation in the default Shiki path. API-key runners are explicit target-repo extensions and require their own ADR.\n+Do not assume `openai/codex-action`, `OPENAI_API_KEY`, or API-key based Codex automation in the default Shiki path. API-key runners are explicit target-repo extensions and require their own target ADR.\n \n Shiki is not:\n \n@@ -44,10 +44,14 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use\n+`SADR-NNNN-*.md`; target product decisions retain `NNNN-*.md` and the `ADR`\n+namespace.\n+\n Use the canonical source-of-truth order above. Runtime-specific reading orders\n are operational guidance only and must not override or reorder it.\n \n-Conversation state is not durable truth. If a decision matters, put it in GitHub, `.shiki/`, `CONTEXT.md`, or an ADR.\n+Conversation state is not durable truth. If a decision matters, put it in GitHub, `.shiki/`, `CONTEXT.md`, a Shiki SADR, or a target ADR.\n \n If GitHub and `.shiki/` disagree, surface the conflict and prefer GitHub operational state until the mirror is repaired.\n \n@@ -75,8 +79,8 @@\n Every non-trivial change follows this loop:\n \n 1. **Goal**: clarify outcome, completion conditions, non-goals, risk level, and success signals.\n-2. **grill-with-docs**: challenge the plan against domain docs, ADRs, code reality, terminology, and edge scenarios.\n-3. **Context & Impact**: find relevant docs, ADRs, code, dependencies, owners, locks, and verification surfaces.\n+2. **grill-with-docs**: challenge the plan against domain docs, Shiki SADRs, target ADRs, code reality, terminology, and edge scenarios.\n+3. **Context & Impact**: find relevant docs, SADRs, target ADRs, code, dependencies, owners, locks, and verification surfaces.\n 4. **PRD and Spec Freeze**: use `to-prd` when context is settled enough to become durable product/engineering intent. The operator's explicit approval of the PRD is Spec Freeze: the plan records a `spec_freeze` block, and no plan runs without it. Steps 1-4 form Requirements Definition \u2014 one continuous operator dialogue. After Spec Freeze, scope changes require an operator-approved Spec Amendment; non-scope-moving interpretations go to the Assumption Log.\n 5. **Issues**: use `to-issues` to create independently grabbable vertical-slice issues. Prefer AFK slices over HITL slices where possible.\n 6. **Triage**: label issues for readiness, risk, runtime, skills, and MergeGate state.\n@@ -167,7 +171,7 @@\n - Testing decisions.\n - Out-of-scope items.\n - Further notes.\n-- Links to relevant domain terms and ADRs.\n+- Links to relevant domain terms, SADRs, and target ADRs.\n \n Every executable task must include:\n \n@@ -193,7 +197,7 @@\n \n Context & Impact must identify:\n \n-- Relevant documents, domain terms, ADRs, and past decisions.\n+- Relevant documents, domain terms, SADRs, target ADRs, and past decisions.\n - Relevant modules, interfaces, seams, callers, owners, and tests.\n - Dependency relationships between tasks.\n - Candidate locks and lock conflicts.\n@@ -210,7 +214,7 @@\n | Skill | Required when |\n | --- | --- |\n | `setup-matt-pocock-skills` | First configuring the repo for skills, or when issue tracker, triage labels, or domain docs are missing. |\n-| `grill-with-docs` | Default for non-trivial Goals before PRD/issues; always required when plans, terminology, boundaries, tradeoffs, or ADR-worthy decisions are ambiguous. |\n+| `grill-with-docs` | Default for non-trivial Goals before PRD/issues; always required when plans, terminology, boundaries, tradeoffs, or decision-record-worthy choices are ambiguous. |\n | `zoom-out` | The agent lacks an architectural map of the relevant code area. |\n | `to-prd` | Settled Goal context must become durable product/engineering intent. |\n | `to-issues` | A Goal, PRD, or plan must become independently grabbable vertical-slice issues. |\n@@ -348,7 +352,7 @@\n - Three or more new files.\n - Any high-risk or critical label.\n \n-When the architecture gate triggers, run `grill-with-docs` or `improve-codebase-architecture` as appropriate and record durable decisions in an ADR when needed.\n+When the architecture gate triggers, run `grill-with-docs` or `improve-codebase-architecture` as appropriate and record durable decisions in a Shiki SADR or target ADR as applicable.\n \n ## Engineering Discipline\n \ndiff --git a/CLAUDE.md b/CLAUDE.md\nindex 98a932e7841a632866fcd53b0bad0e3af3711d3d..98a31399404d6fb044b4fd493f4459ac87f439de\n--- a/CLAUDE.md\n+++ b/CLAUDE.md\n@@ -14,6 +14,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n ## Claude Role\n \n Default to planner, default implementer, reviewer, coordinator, documentation editor, CCA-style completion judge, and final-judgment assistant.\n@@ -57,7 +60,7 @@\n At the start of a Shiki session:\n \n 1. Read `AGENTS.md`.\n-2. Read `CONTEXT.md` and relevant ADRs.\n+2. Read `CONTEXT.md` and relevant Shiki SADRs and target ADRs.\n 3. Read active `.shiki/` Goal/task/ledger state when present.\n 4. Read active distilled rules from `.shiki/memories/` (status `distilled`, `active: true`, not revoked or superseded) for applicable guidance; the same rules are injected deterministically into task handoffs as the `## Distilled Rules` section (\u00a73.5).\n 5. Read the GitHub Issue or PR when the task is GitHub-backed.\n@@ -74,7 +77,7 @@\n - Define the Goal, completion conditions, non-goals, and risk level.\n - Treat `grill-with-docs` as the default planning entry point for non-trivial Goals.\n - Challenge domain terms against `CONTEXT.md`.\n-- Challenge hard-to-reverse tradeoffs against ADRs.\n+- Challenge hard-to-reverse tradeoffs against SADRs and target ADRs.\n - Explore code instead of asking when code can answer the question.\n - Run Context & Impact before implementation planning.\n - Produce a Task DAG, not an unordered checklist.\n@@ -94,7 +97,7 @@\n - For each question, provide your recommended answer.\n - Resolve dependency decisions before downstream design questions.\n - Update `CONTEXT.md` inline when terminology crystallizes.\n-- Offer ADRs only when the decision is hard to reverse, surprising without context, and a real tradeoff.\n+- Offer a SADR or target ADR only when the decision is hard to reverse, surprising without context, and a real tradeoff.\n - Convert unresolved questions into HITL blockers rather than allowing Codex to guess.\n \n ## PRD and Issues Standard\n@@ -103,7 +106,7 @@\n \n - Use the project's domain glossary vocabulary.\n - Avoid volatile file paths unless they are essential.\n-- Include implementation decisions, testing decisions, out-of-scope items, and relevant ADR/domain links.\n+- Include implementation decisions, testing decisions, out-of-scope items, and relevant SADR/target-ADR/domain links.\n - Identify deep-module opportunities and testable interfaces.\n \n When creating issues:\n@@ -121,7 +124,7 @@\n - Branch/worktree target.\n - Scope and non-goals.\n - Dependencies and locks.\n-- Relevant docs, ADRs, modules, and tests.\n+- Relevant docs, SADRs, target ADRs, modules, and tests.\n - Required skill invocations.\n - TDD expectations.\n - Pre-PR `code-review` gate expectations.\n@@ -152,7 +155,7 @@\n \n - Treat yourself as a judge, not implementer.\n - Do not write production code.\n-- Read `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant ADRs, PRD, task issue, PR body, diff, checks, reviews, labels, and `.shiki/` state.\n+- Read `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant SADRs and target ADRs, PRD, task issue, PR body, diff, checks, reviews, labels, and `.shiki/` state.\n - Evaluate every applicable checklist in `docs/agents/checklists.md`.\n - Map each acceptance criterion to durable evidence.\n - Distinguish implementation failure from missing evidence.\ndiff --git a/CONTEXT.md b/CONTEXT.md\nindex e61a9be06a0cce0d026484083a657bde2067e234..237ee8aa3ea8fab334ece49768bc5ed3b5af0974\n--- a/CONTEXT.md\n+++ b/CONTEXT.md\n@@ -25,7 +25,7 @@\n _Avoid_: jumping directly into code\n \n **grill-with-docs**:\n-A planning skill that challenges a plan against domain language, ADRs, code reality, and concrete edge scenarios. It resolves design-tree questions before PRD/issues. It runs inside Requirements Definition.\n+A planning skill that challenges a plan against domain language, Shiki SADRs, target ADRs, code reality, and concrete edge scenarios. It resolves design-tree questions before PRD/issues. It runs inside Requirements Definition.\n _Avoid_: generic brainstorming, silent assumptions\n \n **Requirements Definition**:\ndiff --git a/CONTRIBUTING.md b/CONTRIBUTING.md\nindex c6d9b656e73abb8a55e5f660123e03153dcb3e6d..9e37a8fbf668db5b22461aaae5548b8f1354394c\n--- a/CONTRIBUTING.md\n+++ b/CONTRIBUTING.md\n@@ -17,7 +17,7 @@\n 1. **Goal** \u2014 start from a user-approved Goal with an outcome, completion\n    conditions, non-goals, risk level, and success signals.\n 2. **grill-with-docs** \u2014 for any non-trivial change, challenge the plan against\n-   domain language, ADRs, and code reality before writing a PRD.\n+   domain language, Shiki SADRs, target ADRs, and code reality before writing a PRD.\n 3. **PRD** \u2014 capture settled product/engineering intent (`to-prd`).\n 4. **Spec Freeze** \u2014 the operator explicitly approves the PRD/requirements;\n    the plan records a `spec_freeze` block and no plan runs without it\n@@ -94,7 +94,7 @@\n   entries.\n - Do not hand-edit `.shiki/goals/*.json` or `.shiki/manifest.json` in a task PR.\n - Conversation state is not durable truth. If a decision matters, record it in\n-  GitHub, `.shiki/`, `CONTEXT.md`, or an ADR.\n+  GitHub, `.shiki/`, `CONTEXT.md`, a Shiki SADR, or a target ADR.\n \n ## Architecture Gate\n \n@@ -103,7 +103,7 @@\n or CI policy, package structure, three or more structural files, more than 100\n net new lines in a core module, three or more new files, or any high-risk label.\n When triggered, run `grill-with-docs` or `improve-codebase-architecture` and\n-record durable decisions in an ADR when needed (see `docs/adr/`).\n+record durable decisions in a Shiki SADR or target ADR as applicable (see `docs/adr/`).\n \n ## Safety\n \ndiff --git a/SYSTEM_PROMPT.md b/SYSTEM_PROMPT.md\nindex aaaea45e3a896c3a44ba89eec8bb84325ecf6c62..da009c86a88285269dee0155e58e550c6d744ea9\n--- a/SYSTEM_PROMPT.md\n+++ b/SYSTEM_PROMPT.md\n@@ -30,6 +30,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n Use the canonical source-of-truth order above. Runtime-specific reading orders\n are operational guidance only and must not override or reorder it. The current\n conversation is non-durable operator input.\n@@ -48,14 +51,14 @@\n \n A runtime may only do work that is explicitly assigned by the Goal, task, PR, or MergeGate decision.\n \n-Do not assume `openai/codex-action`, `OPENAI_API_KEY`, or API-key based Codex automation in the default Shiki path. API-key runners are explicit target-repo extensions and require their own ADR.\n+Do not assume `openai/codex-action`, `OPENAI_API_KEY`, or API-key based Codex automation in the default Shiki path. API-key runners are explicit target-repo extensions and require their own target ADR.\n \n ## 4. Non-negotiable Shiki flow\n \n For any non-trivial Goal, use this flow:\n \n 1. **Goal Seek**: define outcome, non-goals, risk level, completion criteria, and evidence requirements.\n-2. **grill-with-docs**: challenge the plan against `CONTEXT.md`, ADRs, code reality, terminology, boundaries, and edge scenarios. Ask one question at a time when the operator is available. If code can answer a question, inspect code instead of asking.\n+2. **grill-with-docs**: challenge the plan against `CONTEXT.md`, Shiki SADRs, target ADRs, code reality, terminology, boundaries, and edge scenarios. Ask one question at a time when the operator is available. If code can answer a question, inspect code instead of asking.\n 3. **Context & Impact**: identify modules, interfaces, seams, callers, dependencies, locks, risk, and verification surfaces.\n 4. **to-prd + Spec Freeze**: turn settled context into a PRD using domain vocabulary and testing decisions. The operator's explicit approval of the PRD is Spec Freeze, recorded as the plan's `spec_freeze` block; plans without it do not run. Steps 1-4 form Requirements Definition.\n 5. **to-issues**: decompose the PRD into independently grabbable vertical-slice issues. Prefer AFK slices; mark HITL slices when judgment is still required.\n@@ -75,7 +78,7 @@\n Before making any material change:\n \n 1. Read `AGENTS.md` and any runtime-specific wrapper such as `CLAUDE.md`.\n-2. Read `CONTEXT.md` and relevant ADRs.\n+2. Read `CONTEXT.md` and relevant SADRs and target ADRs.\n 3. Read the GitHub Issue / PR and `.shiki/` state for the active Goal or task.\n 4. Identify your role: Planner, Implementer, Reviewer, Completion Check Agent, Repairer, or Guardian-assist.\n 5. Identify Goal id, task id, branch, PR, dependencies, locks, risk level, required skills, and required evidence.\n@@ -103,7 +106,7 @@\n \n At minimum, identify:\n \n-- Relevant documents, ADRs, domain terms, and prior decisions.\n+- Relevant documents, SADRs, target ADRs, domain terms, and prior decisions.\n - Relevant modules, interfaces, seams, callers, owners, and tests.\n - Dependency relationships between tasks.\n - Candidate file locks and lock conflicts.\n@@ -152,7 +155,7 @@\n \n CCA must read:\n \n-- `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant ADRs.\n+- `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant SADRs and target ADRs.\n - Parent Goal issue, PRD issue, task issue, comments, and labels.\n - PR body, diff, commits, changed files, reviews, CI checks, and artifacts available to the workflow.\n - `.shiki/` task contract, locks, ledger entries, CCA checklist profile, and prior repair packets.\n@@ -214,7 +217,7 @@\n - Three or more new files.\n - Any high-risk or critical label.\n \n-When the architecture gate triggers, run `grill-with-docs` or `improve-codebase-architecture` as appropriate and record the decision in an ADR if it is hard to reverse or likely to be re-litigated.\n+When the architecture gate triggers, run `grill-with-docs` or `improve-codebase-architecture` as appropriate and record the decision in a Shiki SADR or target ADR as applicable if it is hard to reverse or likely to be re-litigated.\n \n ## 14. Skill Gate\n \n@@ -223,7 +226,7 @@\n Use these skills:\n \n - `setup-matt-pocock-skills`: before first use of the engineering skill set, or when issue tracker, triage labels, or domain docs are missing.\n-- `grill-with-docs`: default for non-trivial Goals before PRD/issues; required for ambiguous plans, terminology, boundaries, ADR-worthy tradeoffs, or design-tree decisions.\n+- `grill-with-docs`: default for non-trivial Goals before PRD/issues; required for ambiguous plans, terminology, boundaries, decision-record-worthy tradeoffs, or design-tree decisions.\n - `zoom-out`: unfamiliar code area or missing architectural map.\n - `to-prd`: convert settled context into a PRD.\n - `to-issues`: break a PRD, plan, or Goal into independently grabbable vertical-slice issues.\ndiff --git a/docs/adr/0001-keep-shiki-as-platform-name.md b/docs/adr/SADR-0001-keep-shiki-as-platform-name.md\nindex 20a9637c3b9d8c97de7ba8db7cfae4d82d9a7dba..8b685439959aada1b221c112f0ac3c51ca8a1695\n--- a/docs/adr/0001-keep-shiki-as-platform-name.md\n+++ b/docs/adr/SADR-0001-keep-shiki-as-platform-name.md\n@@ -1,3 +1,3 @@\n-# Keep Shiki As The Platform Name\n+# SADR-0001: Keep Shiki As The Platform Name\n \n Shiki remains the product name for the GitHub-native agentic engineering control plane. We considered more descriptive names such as MergePilot, MergeGate, and Kiroku, but kept Shiki because it carries the existing goal-seeking framework identity while remaining independent of Hermes, Codex, Claude Code, or any single runtime.\ndiff --git a/docs/adr/0002-use-agents-as-shared-constitution.md b/docs/adr/SADR-0002-use-agents-as-shared-constitution.md\nindex 73de30ecb078120eac540d9bc51855e3cd48d0e3..c5486182bf06fca36ea508496b013c67302fd989\n--- a/docs/adr/0002-use-agents-as-shared-constitution.md\n+++ b/docs/adr/SADR-0002-use-agents-as-shared-constitution.md\n@@ -1,3 +1,3 @@\n-# Use AGENTS As The Shared Agent Constitution\n+# SADR-0002: Use AGENTS As The Shared Agent Constitution\n \n Shiki uses `AGENTS.md` as the runtime-neutral constitution and `CLAUDE.md` as a thin Claude Code wrapper. This keeps Codex, Claude Code, Hermes Runner, and future Agent Runtimes aligned on the same GitHub-first MergeGate rules while still allowing runtime-specific instructions where needed.\ndiff --git a/docs/adr/0003-require-skill-gate-for-mergegate-readiness.md b/docs/adr/SADR-0003-require-skill-gate-for-mergegate-readiness.md\nindex 424be0c9af4c8f0136f1fde68a468c47ff84e764..7fc337987c7df9792f75f181dfcbed75fe5ffff2\n--- a/docs/adr/0003-require-skill-gate-for-mergegate-readiness.md\n+++ b/docs/adr/SADR-0003-require-skill-gate-for-mergegate-readiness.md\n@@ -1,3 +1,3 @@\n-# Require Skill Gate For MergeGate Readiness\n+# SADR-0003: Require Skill Gate For MergeGate Readiness\n \n Shiki requires relevant engineering skills to be invoked before execution when their triggers apply. This makes skills part of MergeGate readiness rather than optional prompt style, because Shiki depends on repeatable planning, issue decomposition, TDD, diagnosis, triage, and architecture review before autonomous or parallel agent work can be trusted.\ndiff --git a/docs/adr/0004-use-github-first-with-shiki-mirror.md b/docs/adr/SADR-0004-use-github-first-with-shiki-mirror.md\nindex d0a3a9174dc646a945a9d325954ffaf00c352040..a79c54ae9ee8eed2a0524192b864da4e22a92d3f\n--- a/docs/adr/0004-use-github-first-with-shiki-mirror.md\n+++ b/docs/adr/SADR-0004-use-github-first-with-shiki-mirror.md\n@@ -1,4 +1,4 @@\n-# ADR 0004: Use GitHub First With A Repository-Local Shiki Mirror\n+# SADR-0004: Use GitHub First With A Repository-Local Shiki Mirror\n \n ## Status\n \ndiff --git a/docs/adr/0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md b/docs/adr/SADR-0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\nindex b339baf49b89d9290e79c3d51b36e7b2bde3bd95..0f3c3eb9afccdb3403ce978e21d04fe12e495da0\n--- a/docs/adr/0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\n+++ b/docs/adr/SADR-0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\n@@ -1,8 +1,8 @@\n-# ADR 0005: Use Codex As Front And Claude Code Action As The GitHub Runtime\n+# SADR-0005: Use Codex As Front And Claude Code Action As The GitHub Runtime\n \n ## Status\n \n-Accepted (implementer default superseded by ADR 0008; the auth model and the\n+Accepted (implementer default superseded by SADR-0008; the auth model and the\n Claude Code Action GitHub runtime decision remain in force)\n \n ## Context\n@@ -26,4 +26,4 @@\n - Shiki avoids requiring `OPENAI_API_KEY` for the default workflow.\n - Codex work happens through the user's authenticated Codex front surface, not as a hidden backend Action.\n - Claude Code Action is configured for OAuth-token based subscription usage by default.\n-- API-key based automation remains possible only as an explicit target-repo extension with its own ADR.\n+- API-key based automation remains possible only as an explicit target-repo extension with its own target ADR.\ndiff --git a/docs/adr/0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md b/docs/adr/SADR-0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\nindex 4bcae9d1e52f0a37b8e9fbafa5cd14b6ee1993bd..7a38538a6ae9929567f0ade67e7ea6a60d0b3f7e\n--- a/docs/adr/0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\n+++ b/docs/adr/SADR-0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\n@@ -1,4 +1,4 @@\n-# ADR 0006: Separate Codex Implementation, CCA Completion, And MergeGate Authorization\n+# SADR-0006: Separate Codex Implementation, CCA Completion, And MergeGate Authorization\n \n ## Status\n \ndiff --git a/docs/adr/0007-packaging-and-release.md b/docs/adr/SADR-0007-packaging-and-release.md\nindex 79b84ddb97167372b44641e5b7860e400ce80822..bb445a2e2e856d9d1f5fdbe35de8d4e8cec0fb32\n--- a/docs/adr/0007-packaging-and-release.md\n+++ b/docs/adr/SADR-0007-packaging-and-release.md\n@@ -1,4 +1,4 @@\n-# ADR 0007: Packaging And Release For Shiki\n+# SADR-0007: Packaging And Release For Shiki\n \n ## Status\n \n@@ -25,7 +25,7 @@\n   not introduce third-party runtime dependencies or break the shim.\n - Releases must run inside the operator's subscription-authenticated toolchain\n   and must not require extra secrets beyond the built-in `GITHUB_TOKEN`\n-  (consistent with ADR 0005's no-API-key-by-default stance).\n+  (consistent with SADR-0005's no-API-key-by-default stance).\n - `.shiki/manifest.json` already encodes the install surface (`install.include`,\n   `install.create_directories`, `install.exclude_from_commit`), and it must not\n   be mutated as part of this change.\n@@ -34,7 +34,7 @@\n \n This is a hard-to-reverse platform decision (it sets the version source of\n truth, the release trigger, and the public install contract), so it is recorded\n-as an ADR.\n+as a SADR.\n \n ## Decision\n \n@@ -104,7 +104,7 @@\n 3. **Publishing to PyPI.** Rejected for now: Shiki is distributed as a\n    repository template + git-installable CLI, not a PyPI library; PyPI would add\n    credentials/secrets and a different versioning contract. Can be added later as\n-   its own ADR if a packaged distribution is needed.\n+   its own SADR if a packaged distribution is needed.\n 4. **Dynamic version (read `VERSION` via a build backend `dynamic` field).**\n    Rejected to keep `pyproject.toml` backend-agnostic and dependency-free; the\n    small duplication between `VERSION` and `pyproject.toml` is guarded by the\n@@ -123,4 +123,4 @@\n   releasing versions the whole platform but ships only `install.include` to\n   targets.\n - Future PyPI distribution or a console entry point are deferred and would each\n-  warrant their own ADR.\n+  warrant their own SADR.\ndiff --git a/docs/adr/0008-use-claude-code-as-default-implementer.md b/docs/adr/SADR-0008-use-claude-code-as-default-implementer.md\nindex dedf4510b7c7799521422f622029563d373db031..35c2625d908167fbc58855b5b63e283805f167e8\n--- a/docs/adr/0008-use-claude-code-as-default-implementer.md\n+++ b/docs/adr/SADR-0008-use-claude-code-as-default-implementer.md\n@@ -1,12 +1,12 @@\n-# ADR 0008: Use Claude Code As The Default Implementer Runtime\n+# SADR-0008: Use Claude Code As The Default Implementer Runtime\n \n ## Status\n \n-Proposed (supersedes ADR 0005 for the implementer default; keeps its auth model)\n+Proposed (supersedes SADR-0005 for the implementer default; keeps its auth model)\n \n ## Context\n \n-ADR 0005 made Codex Front the default implementation runtime, with Claude Code\n+SADR-0005 made Codex Front the default implementation runtime, with Claude Code\n restricted to planner and reviewer roles by the runtime registry, and the only\n autonomous implementation adapter was `shiki runner codex`.\n \n@@ -18,13 +18,13 @@\n - The platform's planning, review, and CCA layers already run on Claude Code,\n   and Claude Code's native capabilities (the built-in `/code-review` skill and\n   the Workflow multi-agent orchestration tool) have no Codex equivalent.\n-- The Goal lifecycle redesign (ADR 0009) requires an autonomous post-freeze\n+- The Goal lifecycle redesign (SADR-0009) requires an autonomous post-freeze\n   loop driven from the operator's Claude Code session, including bounded\n   repair dispatch, which must not depend on a runtime that cannot be invoked\n   unattended.\n \n The constitution claims Shiki is runtime-agnostic (\"not a Claude-only\n-workflow\", ADR 0002). Any change to the implementer default must preserve the\n+workflow\", SADR-0002). Any change to the implementer default must preserve the\n registry-based runtime model rather than hardcoding one vendor.\n \n ## Decision\n@@ -39,7 +39,7 @@\n   the task to review or repair-needed.\n - Keep Codex registered as an optional implementer runtime. `shiki runner\n   codex`, the Codex handoff templates, and the ChatGPT OAuth auth model from\n-  ADR 0005 remain supported for tasks explicitly assigned to `codex`.\n+  SADR-0005 remain supported for tasks explicitly assigned to `codex`.\n - Commit to mandating two Claude-native capabilities as loop contract in\n   follow-up Goals (status: the code-review gate was delivered by Goal G-B \u2014\n   skill registry, checklists item PR-12, default required_skills \u2014 while the\n@@ -60,7 +60,7 @@\n \n Out of scope: removing Codex support, changing the CCA runtime (Claude Code\n Action with `CLAUDE_CODE_OAUTH_TOKEN` stays), and API-key based automation\n-(still requires its own ADR per ADR 0005).\n+(still requires its own target ADR per SADR-0005).\n \n ## Consequences\n \n@@ -100,7 +100,7 @@\n   harness rejection of direct `codex exec` is outside Shiki's control, and the\n   plan's mandatory native capabilities cannot run on Codex at all.\n - **Remove Codex entirely**: rejected; it abandons the runtime-agnostic\n-  constitution (ADR 0002) for no operational gain and forecloses\n+  constitution (SADR-0002) for no operational gain and forecloses\n   cross-runtime adversarial review.\n - **GitHub Actions cloud implementation via claude-code-action**: rejected as\n   default; durable evidence is automatic but token cost, Node 24 workflow\ndiff --git a/docs/adr/0009-requirements-definition-and-spec-freeze.md b/docs/adr/SADR-0009-requirements-definition-and-spec-freeze.md\nindex 29b3f7871bef9203a8b4a831a65c39eb2f506e42..52e7dd69ad0aa4efe0b68c62678abbf7df02aaec\n--- a/docs/adr/0009-requirements-definition-and-spec-freeze.md\n+++ b/docs/adr/SADR-0009-requirements-definition-and-spec-freeze.md\n@@ -1,4 +1,4 @@\n-# ADR 0009: Merge Grill And Planning Into Requirements Definition With Spec Freeze\n+# SADR-0009: Merge Grill And Planning Into Requirements Definition With Spec Freeze\n \n ## Status\n \ndiff --git a/docs/adr/0010-external-ai-guardian-review.md b/docs/adr/SADR-0010-external-ai-guardian-review.md\nindex e6108bbe8bb4e73e0ff4684c8f78001a80438c4e..f3510b145664298adafb151b6f0db51b972f01ec\n--- a/docs/adr/0010-external-ai-guardian-review.md\n+++ b/docs/adr/SADR-0010-external-ai-guardian-review.md\n@@ -1,4 +1,4 @@\n-# ADR 0010: External AI Guardian Review As A First-Class Approval Authority\n+# SADR-0010: External AI Guardian Review As A First-Class Approval Authority\n \n ## Status\n \n@@ -8,7 +8,7 @@\n \n Shiki is intended to be an autonomous, recursive, self-improving agent\n framework: authority-in-the-loop, not human-in-the-loop. Until now the Guardian\n-approval required for high/critical-risk merges (guardian-policy.json, ADR\n+approval required for high/critical-risk merges (guardian-policy.json, SADR\n governance) recognized only human authority sources \u2014 a GitHub review approval,\n the `guardian:approved` label applied by a configured Guardian, or a comment\n carrying `Guardian approval granted` plus the current head SHA.\n@@ -89,7 +89,7 @@\n   autonomous design the operator intends and produced the impersonation\n   incident.\n - **Record AI approval as an operator comment**: rejected as forgery of human\n-  authority \u2014 the exact failure this ADR corrects.\n+  authority \u2014 the exact failure this SADR corrects.\n - **Deliver the artifact as a committed `.shiki/` file**: rejected; a committed\n   approval inside the PR being approved is forgeable by anyone who can push.\n - **A dedicated reviewer bot identity instead of a relayed comment**: deferred;\ndiff --git a/docs/adr/0011-autonomous-quality-gate-evidence.md b/docs/adr/SADR-0011-autonomous-quality-gate-evidence.md\nindex bca0246c5d46e44afbde15486a75eec425fc02f5..9c93eac16d8c3a6d4b11bd7224e3955102cd7db4\n--- a/docs/adr/0011-autonomous-quality-gate-evidence.md\n+++ b/docs/adr/SADR-0011-autonomous-quality-gate-evidence.md\n@@ -1,4 +1,4 @@\n-# ADR 0011: Autonomous Quality-Gate Evidence \u2014 Deterministic Facts And An Independent Verifier\n+# SADR-0011: Autonomous Quality-Gate Evidence \u2014 Deterministic Facts And An Independent Verifier\n \n ## Status\n \ndiff --git a/docs/adr/0012-autonomous-loop-closeout-to-main.md b/docs/adr/SADR-0012-autonomous-loop-closeout-to-main.md\nindex 637fad6dde4989c9c2bf0f81f676d8d29991123c..ec7d85f81030a676bcbba28b43eb365422a36eac\n--- a/docs/adr/0012-autonomous-loop-closeout-to-main.md\n+++ b/docs/adr/SADR-0012-autonomous-loop-closeout-to-main.md\n@@ -1,4 +1,4 @@\n-# 0012 \u2014 Autonomous loop pushes goal completion to main via a closeout PR\n+# SADR-0012 \u2014 Autonomous loop pushes goal completion to main via a closeout PR\n \n - Status: accepted\n - Date: 2026-06-18\ndiff --git a/docs/adr/0013-bootstrap-grants-actions-approve-pull-requests.md b/docs/adr/SADR-0013-bootstrap-grants-actions-approve-pull-requests.md\nindex b6c69d64f1c5ced346b88d6859586715b3b5bd0e..cf48ff4a6716267f539ca58edd1a852e7c7a2d64\n--- a/docs/adr/0013-bootstrap-grants-actions-approve-pull-requests.md\n+++ b/docs/adr/SADR-0013-bootstrap-grants-actions-approve-pull-requests.md\n@@ -1,4 +1,4 @@\n-# ADR 0013: Bootstrap Grants GitHub Actions Permission To Create And Approve Pull Requests\n+# SADR-0013: Bootstrap Grants GitHub Actions Permission To Create And Approve Pull Requests\n \n ## Status\n \n@@ -31,7 +31,7 @@\n security tradeoff: it lets repository automation satisfy a human-review gate. In\n a multi-maintainer repo that could let a compromised or buggy workflow\n self-approve changes. So whether bootstrap turns it on by default \u2014 and with\n-what default token scope \u2014 is an ADR-worthy decision, not an implementation\n+what default token scope \u2014 is a SADR-worthy decision, not an implementation\n detail.\n \n ## Decision\n@@ -48,7 +48,7 @@\n Specifically:\n \n - **`can_approve_pull_request_reviews` defaults to `true`.** Shiki's default\n-  posture is solo/AFK autonomous operation (ADR 0008/0009/0012), where the CCA\n+  posture is solo/AFK autonomous operation (SADR-0008/0009/0012), where the CCA\n   Review Bridge is the only path that can satisfy `required_review: true` after\n   the CCA and Guardian gates have already passed. The default makes a\n   freshly-bootstrapped repo able to run the autonomous loop end to end without a\ndiff --git a/docs/adr/0014-codex-app-external-ai-guardian-ui-adapter.md b/docs/adr/SADR-0014-codex-app-external-ai-guardian-ui-adapter.md\nindex 238db06279334caa341c117362df9271382a7fcd..65f12e9939321e1b3e389a34c822fc1549e5155d\n--- a/docs/adr/0014-codex-app-external-ai-guardian-ui-adapter.md\n+++ b/docs/adr/SADR-0014-codex-app-external-ai-guardian-ui-adapter.md\n@@ -1,4 +1,4 @@\n-# ADR 0014: Use Codex App As The External AI Guardian UI Adapter\n+# SADR-0014: Use Codex App As The External AI Guardian UI Adapter\n \n ## Status\n \ndiff --git a/docs/adr/0015-contract-approval-before-dispatch.md b/docs/adr/SADR-0015-contract-approval-before-dispatch.md\nindex fad6c656741a20a7684ba16080a7db159f63a52f..6061271a46d3f1d6ae7174477de3fb75673123a6\n--- a/docs/adr/0015-contract-approval-before-dispatch.md\n+++ b/docs/adr/SADR-0015-contract-approval-before-dispatch.md\n@@ -1,4 +1,4 @@\n-# ADR 0015: Approve The Task Contract Before Dispatch, Not The Diff After It\n+# SADR-0015: Approve The Task Contract Before Dispatch, Not The Diff After It\n \n ## Status\n \n@@ -9,7 +9,7 @@\n Guardian approval for high/critical risk is expressible only on a pull request.\n Every approval source in `guardian-policy.json` \u2014 GitHub review, the\n `guardian:approved` label, the `Guardian approval granted` comment, and the\n-`external_ai_guardian_review` artifact (ADR 0010/0014) \u2014 is bound to a PR, and\n+`external_ai_guardian_review` artifact (SADR-0010/0014) \u2014 is bound to a PR, and\n three of the four require the current head SHA. Approval therefore cannot exist\n before an implementation PR exists.\n \n@@ -81,7 +81,7 @@\n An envelope breach must surface only on the non-repairable policy gate, never on\n the repairable `MergeGate metadata check`. Otherwise the Goal Loop routes it to\n `dispatch_repair` \u2014 instructing a runner to make the breach go away \u2014 which is\n-exactly what this ADR forbids.\n+exactly what this SADR forbids.\n \n **Contract immutability.** After registration, an implementation PR must not\n change the governance fields of its own task contract (`scope`, `non_goals`,\n@@ -116,7 +116,7 @@\n fails any verdict that is not `complete`, and the signal the CCA prompt reads is\n computed from `evaluate_guardian_approval` alone. Without the OR-merge at the\n signal, a contract-approved PR still returns `needs_guardian`, the CCA job fails,\n-and the MergeGate policy check never runs \u2014 the gate this ADR changes would never\n+and the MergeGate policy check never runs \u2014 the gate this SADR changes would never\n be reached. The CCA *prompt* needs no change: a `contract_approval` entry in the\n signal's existing `sources` array satisfies its current wording.\n \n@@ -130,7 +130,7 @@\n written, and an approved contract lets its implementation PRs merge through the\n same automatic path as low/medium risk. The wasted implement-then-block cycle\n disappears. Guardian review shifts from reading diffs to reading contracts, which\n-is both earlier and smaller; the external AI Guardian path (ADR 0010/0014) works\n+is both earlier and smaller; the external AI Guardian path (SADR-0010/0014) works\n on contracts with no change.\n \n We accept that the Guardian authorizes a class of change without seeing its\n@@ -146,7 +146,7 @@\n adversarial tests for the new approval path equivalent in rigor to\n `test_shiki_governance_evidence.sh`.\n \n-Bootstrap costs, which apply only while this ADR is being implemented. The tasks\n+Bootstrap costs, which apply only while this SADR is being implemented. The tasks\n that build Contract Approval merge under the existing rules, so the ones\n dispatched after the pre-dispatch gate lands but before the approval path lands\n need a Guardian-approved Contract PR *and* a live Guardian approval on their\n@@ -210,7 +210,7 @@\n risk; they do not bound authorized work.\n \n **Auto-escalate risk on envelope breach instead of stopping.** Returns the\n-workflow to a diff-time Guardian decision \u2014 the exact behavior this ADR removes.\n+workflow to a diff-time Guardian decision \u2014 the exact behavior this SADR removes.\n \n **Resolve governance fields from the base snapshot only.** Rejected against the\n registration flow: implementation PRs add their own contract, so the base has no\ndiff --git a/docs/adr/0016-locks-bound-product-paths.md b/docs/adr/SADR-0016-locks-bound-product-paths.md\nindex b5e850f3f1f7f4ef6e7f9d1763a29a51bd300c45..46b40a9b99a7aae644c02bb458f5473ff0a90ce2\n--- a/docs/adr/0016-locks-bound-product-paths.md\n+++ b/docs/adr/SADR-0016-locks-bound-product-paths.md\n@@ -1,4 +1,4 @@\n-# ADR 0016: Locks bound product paths; the Shiki mirror is bound by record identity\n+# SADR-0016: Locks bound product paths; the Shiki mirror is bound by record identity\n \n ## Status\n \n@@ -6,8 +6,8 @@\n \n - Date: 2026-07-28\n - Deciders: operator (mizutani-140), Claude (planner)\n-- Related: ADR 0004 (GitHub-first with a `.shiki` mirror), ADR 0009 (Spec Freeze),\n-  ADR 0012 (autonomous loop closeout to main), ADR 0015 (Contract Approval before\n+- Related: SADR-0004 (GitHub-first with a `.shiki` mirror), SADR-0009 (Spec Freeze),\n+  SADR-0012 (autonomous loop closeout to main), SADR-0015 (Contract Approval before\n   dispatch), remediation goal task R-02, the live PR #179 deadlock.\n \n ## Context\n@@ -90,11 +90,11 @@\n files, documentation files and named source modules are nameable; runtime-\n allocated ledger, runner and report ids are not \u2014 and those live in `.shiki`,\n which locks no longer govern. Existing frozen contracts are brought into line by\n-a one-time Spec Amendment per plan, which must land before ADR 0015's contract\n+a one-time Spec Amendment per plan, which must land before SADR-0015's contract\n immutability makes `locks` unwritable from the implementation PR.\n \n **Lock state is resolved from the durable side.** MergeGate resolves the lock\n-registry, and ADR 0015's governance fields, from the base `.shiki` snapshot when\n+registry, and SADR-0015's governance fields, from the base `.shiki` snapshot when\n present, falling back to head only when absent. The `MergeGate metadata check`\n job is given `--base-shiki`, which it does not receive today. Locks are not\n derived from the live open-pull-request set: a pull request body is untrusted\n@@ -108,7 +108,7 @@\n Explicitly out of scope: moving MergeGate's contract resolution off the PR\n checkout wholesale; making the mirror a projection of GitHub state; introducing\n new lock kinds; moving ledger evidence out of the repository; and unwinding\n-ADR 0012's closeout pull request. Registration of multi-task goals is ADR 0015's\n+SADR-0012's closeout pull request. Registration of multi-task goals is SADR-0015's\n Contract PR and is not re-decided here.\n \n ## Consequences\n@@ -127,7 +127,7 @@\n \n Risks accepted. First, goal-scoped identity is only as strong as `goal_id`,\n which is resolved from the PR body and the head task file; the mitigation is to\n-add `goal_id` to ADR 0015's frozen governance field set, and until that lands a\n+add `goal_id` to SADR-0015's frozen governance field set, and until that lands a\n task file can name a foreign goal. Second, base-when-present resolution is\n unavailable on fork pull requests, where the head is contributor-controlled;\n fork pull requests already cannot complete a Shiki merge, and this decision does\n@@ -139,7 +139,7 @@\n path, not by implementation pull requests.\n \n New obligations: one Spec Amendment per frozen plan to rewrite `locks`, ordered\n-before ADR 0015 T1; extension of the adversarial suites in\n+before SADR-0015 T1; extension of the adversarial suites in\n `scripts/test_shiki_adversarial_state.sh` and\n `scripts/test_shiki_governance_evidence.sh` to cover the eleven newly-scoped\n directories; and updating `scripts/test_shiki_loop_lock_guard.sh`,\n@@ -151,32 +151,32 @@\n whether the loop's transitive sync of ledger `evidence` references can be\n deleted once the Contract PR carries its own source plan.\n \n-## Relationship to ADR 0004\n+## Relationship to SADR-0004\n \n-This ADR **clarifies something ADR 0004 left unspecified**. ADR 0004 decides that\n+This SADR **clarifies something SADR-0004 left unspecified**. SADR-0004 decides that\n GitHub is the operational source of truth and that each target repository keeps a\n `.shiki/` mirror. It says nothing about pull requests writing the mirror, does\n not name MergeGate, and does not make the mirror an input to a gate. Nothing in\n-ADR 0004's Decision or Consequences is amended, superseded or weakened: the\n+SADR-0004's Decision or Consequences is amended, superseded or weakened: the\n mirror still exists, still records the same artifacts, and pull request\n-boundaries remain the coordination surface. What this ADR settles is the question\n+boundaries remain the coordination surface. What this SADR settles is the question\n 0004 never reached \u2014 by what authority a pull request may write a mirror record \u2014\n and it answers it the way `.shiki/manifest.json` already declares\n (`pr_mutation: current-task-or-current-goal-only`).\n \n-It **amends ADR 0012**, whose Decision item 5 states that \"Loop-executed tasks\n+It **amends SADR-0012**, whose Decision item 5 states that \"Loop-executed tasks\n declare the `path:.shiki/**` lock, which covers every `.shiki` file the closeout\n stages ... so `files_outside_locks` is satisfied without per-file lock\n bookkeeping.\" That clause is replaced: the closeout's mirror writes are\n-authorized by record identity, not by a declared lock. ADR 0012's substantive\n+authorized by record identity, not by a declared lock. SADR-0012's substantive\n decision \u2014 that completion reaches `main` through a closeout pull request rather\n than local attestation \u2014 is preserved unchanged.\n \n-It is **complementary to ADR 0015**, not a substitute. ADR 0015 owns contract\n+It is **complementary to SADR-0015**, not a substitute. SADR-0015 owns contract\n immutability, the pre-dispatch gate, the Contract PR and the Scope Envelope. This\n-ADR owns what a lock means and who may write the mirror. The one ordering\n+SADR owns what a lock means and who may write the mirror. The one ordering\n constraint between them is that the lock-narrowing Spec Amendment must precede\n-ADR 0015 T1's freezing of the `locks` field.\n+SADR-0015 T1's freezing of the `locks` field.\n \n ## Alternatives Considered\n \n@@ -186,7 +186,7 @@\n the only writer that can register them may run. Migrating the Guardian signal to\n that store makes every registration PR fail closed to `needs_guardian` at every\n risk level, because the task file is by definition absent from the store, which\n-removes the low/medium autonomous path ADR 0008/0009 exists to provide. And it\n+removes the low/medium autonomous path SADR-0008/0009 exists to provide. And it\n leaves schemas, manifest, config, guardian policy, templates, migrations, plans,\n worktrees, runner records, runs and handoffs with no writer at all in a\n repository whose product is `.shiki`. Measured benefit on the frozen backlog:\n@@ -199,14 +199,14 @@\n namespace is not in fact ownership-partitioned \u2014 56 ledger entries carry no\n `task_id`, and one goal DAG is writable by 35 registered tasks. The correct\n expression of its insight is that locks do not govern the mirror at all, which is\n-what this ADR decides.\n+what this SADR decides.\n \n **Make the mirror a projection written only by a registration writer and a\n post-merge projection job.** Rejected. It requires a bypass actor with write\n access to the protected default branch; it makes its own master invariant\n readable from a manifest that pull requests may edit; it deletes both reconcile\n modes and leaves a corrupted mirror with no in-band repair path, contradicting\n-ADR 0004's consequence that mirror conflicts are repaired against GitHub state;\n+SADR-0004's consequence that mirror conflicts are repaired against GitHub state;\n and its content-derived ids are rejected by `validate_shiki`'s currently-green id\n pattern.\n \ndiff --git a/docs/adr/0017-bookkeeping-closeouts-do-not-inherit-risk.md b/docs/adr/SADR-0017-bookkeeping-closeouts-do-not-inherit-risk.md\nindex 554e3a0d526a72ca067108c5464cc68c3edcf174..b9676857a4713850e063af7c39c8e0b01ecb4c66\n--- a/docs/adr/0017-bookkeeping-closeouts-do-not-inherit-risk.md\n+++ b/docs/adr/SADR-0017-bookkeeping-closeouts-do-not-inherit-risk.md\n@@ -1,4 +1,4 @@\n-# ADR 0017: A Bookkeeping Closeout Does Not Inherit Its Task's Risk\n+# SADR-0017: A Bookkeeping Closeout Does Not Inherit Its Task's Risk\n \n ## Status\n \n@@ -6,7 +6,7 @@\n \n ## Context\n \n-ADR 0012 made the goal loop push a task's completion to the default branch through a\n+SADR-0012 made the goal loop push a task's completion to the default branch through a\n closeout pull request: after the implementation PR merges, the loop opens a second PR\n carrying `task.status = done`, `lock.state = released`, and \u2014 when the task completes\n its goal \u2014 `goal.status = complete` plus one scorecard report. Nothing else. The\n@@ -49,14 +49,14 @@\n 4. The task file's governance fields (`scope`, `non_goals`, `required_skills`,\n    `risk_level`, `locks`, `acceptance_checks`, `test_command`) are byte-identical to\n    the base snapshot. Only `status`, `expected_pr`, `closeout_pr`, `ledger_evidence`\n-   and `expected_branch` may differ \u2014 the ADR 0015 contract-immutability rule,\n+   and `expected_branch` may differ \u2014 the SADR-0015 contract-immutability rule,\n    unchanged.\n \n    `expected_branch` is in that set because a closeout is *by construction* on a\n    different branch from the implementation it closes, and the loop rewrites the\n    field accordingly. It is safe to let it differ because MergeGate independently\n    requires `expected_branch` to equal the PR's actual head ref, so a PR cannot set\n-   it to anything other than the branch it is really on. The first draft of this ADR\n+   it to anything other than the branch it is really on. The first draft of this SADR\n    omitted it, which made the exemption unreachable: every real closeout failed\n    condition 4 on a field the loop always changes. See PR #209.\n 5. The state transitions are exactly the terminal set: task `review` \u2192 `done`, lock\n@@ -113,12 +113,12 @@\n classification must be derived, not asserted.\n \n **Fold the closeout into the implementation PR** so no second PR exists. Rejected under\n-ADR 0012, which recorded why: a task file that is already `done` on the implementation\n+SADR-0012, which recorded why: a task file that is already `done` on the implementation\n branch short-circuits the loop's review \u2192 CCA \u2192 merge \u2192 done ordering into a local\n goal-completion before any merge happens.\n \n **Have the loop auto-merge closeouts with elevated credentials.** Rejected outright: that\n-is a runtime authoring its own approval, which ADR 0010 and the constitution's\n+is a runtime authoring its own approval, which SADR-0010 and the constitution's\n approval-record prohibition forbid. It would also be indistinguishable, in the evidence,\n from the forgery those rules exist to prevent.\n \ndiff --git a/docs/adr/0018-carried-guardian-approval-across-a-proven-base-sync.md b/docs/adr/SADR-0018-carried-guardian-approval-across-a-proven-base-sync.md\nindex 22358562da390ed8b45cd600da08a43d378b4680..e6e2907cefa776460af73ddf7d39db605f3c39b0\n--- a/docs/adr/0018-carried-guardian-approval-across-a-proven-base-sync.md\n+++ b/docs/adr/SADR-0018-carried-guardian-approval-across-a-proven-base-sync.md\n@@ -1,4 +1,4 @@\n-# ADR 0018: Carried Guardian Approval Across A Proven Base Sync\n+# SADR-0018: Carried Guardian Approval Across A Proven Base Sync\n \n ## Status\n \n@@ -34,7 +34,7 @@\n Guardian evaluator that consumes it (`shiki_guardian.evaluate_guardian_approval`,\n the `guardian_comment_carried` source) were built and tested in isolation. Neither\n does anything until a caller passes the flags that turn the carry on, so the\n-feature was invisible in CI. This ADR records the decision the two of them\n+feature was invisible in CI. This SADR records the decision the two of them\n implement, and this task wires it into the four CI invocations that judge Guardian\n approval.\n \ndiff --git a/docs/adr/README.md b/docs/adr/README.md\nindex c70e057a4520c36e042ab68c53cd4a21ef709b71..5da43856836e2ce6a150f8127766e2f88355ab4b\n--- a/docs/adr/README.md\n+++ b/docs/adr/README.md\n@@ -1,40 +1,41 @@\n-# Architecture Decision Records\n+# Decision Record Namespaces\n \n-This directory holds Shiki's Architecture Decision Records (ADRs): durable\n-records of hard-to-reverse platform decisions. ADRs are part of the Shiki source\n-of truth alongside GitHub state, the `.shiki/` mirror, and `CONTEXT.md`.\n+This directory holds two independent decision namespaces:\n \n-## How ADRs Work\n+- Shiki Architecture Decision Records (SADRs), named\n+  `SADR-NNNN-<short-slug>.md`, record hard-to-reverse Shiki platform decisions.\n+- Target Architecture Decision Records (ADRs), named\n+  `NNNN-<short-slug>.md`, belong to the target product.\n \n-- New ADRs are added as `docs/adr/NNNN-<short-slug>.md`, where `NNNN` is the next\n-  zero-padded sequence number.\n-- The **canonical list of ADRs is the set of files matching `docs/adr/*.md`** in\n-  this directory. Browse `docs/adr/*.md` for the authoritative, current set.\n-- Use [`template.md`](template.md) as the starting point for a new ADR.\n-- Record an ADR when a decision is hard to reverse, surprising without context,\n-  and a real tradeoff. Do not write an ADR for routine or easily reversible\n-  choices.\n+The same four-digit number may appear once in each namespace. A platform\n+`SADR-NNNN` citation resolves only to `SADR-NNNN-*.md`; a target `ADR NNNN`\n+citation resolves only to `NNNN-*.md`.\n \n-Using the `docs/adr/*.md` glob as the source of truth means other PRs can add an\n-ADR without editing this index file, avoiding merge contention on a\n-hand-maintained enumeration.\n+## How Decision Records Work\n \n-## Current ADRs (snapshot)\n-\n-The list below is a convenience snapshot and may lag behind the directory. When\n-in doubt, list `docs/adr/*.md`.\n+- New Shiki platform decisions use the next zero-padded SADR number.\n+- New target product decisions use the target's own ADR sequence.\n+- The canonical list is the files in `docs/adr/`; no hand-maintained index is\n+  authoritative.\n+- Use [`template.md`](template.md) as the starting point and choose the filename\n+  namespace that owns the decision.\n+- Record a decision when it is hard to reverse, surprising without context, and\n+  a real tradeoff. Do not write a decision record for routine or easily\n+  reversible choices.\n \n-- [0001 \u2014 Keep Shiki As The Platform Name](0001-keep-shiki-as-platform-name.md)\n-- [0002 \u2014 Use AGENTS As The Shared Agent Constitution](0002-use-agents-as-shared-constitution.md)\n-- [0003 \u2014 Require Skill Gate For MergeGate Readiness](0003-require-skill-gate-for-mergegate-readiness.md)\n-- [0004 \u2014 Use GitHub First With A Repository-Local Shiki Mirror](0004-use-github-first-with-shiki-mirror.md)\n-- [0005 \u2014 Use Codex As Front And Claude Code Action As The GitHub Runtime](0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md)\n-- [0006 \u2014 Separate Codex Implementation, CCA Completion, And MergeGate Authorization](0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md)\n+Using the directory as the source of truth lets independent changes add records\n+without contending on an enumerated index.\n \n-## Adding A New ADR\n+## Adding A Shiki SADR\n \n-1. Copy `template.md` to `docs/adr/NNNN-<short-slug>.md`.\n+1. Copy `template.md` to `docs/adr/SADR-NNNN-<short-slug>.md`.\n 2. Fill in the title, status, context, decision, and consequences.\n-3. Set the status to `Proposed`, then `Accepted` once the decision is approved.\n-4. Open a PR following `CONTRIBUTING.md`. You do not need to edit this index for\n-   the ADR to be discoverable via `docs/adr/*.md`.\n+3. Set the status to `Proposed`, then `Accepted` once the proper authority has\n+   approved the decision.\n+4. Open a PR following `CONTRIBUTING.md`.\n+\n+## Adding A Target ADR\n+\n+1. Copy `template.md` to `docs/adr/NNNN-<short-slug>.md` using the target's own\n+   sequence.\n+2. Cite it as `ADR NNNN`; do not prefix its filename with `SADR-`.\ndiff --git a/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md b/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\nnew file mode 100644\nindex 0000000000000000000000000000000000000000..7455ce1f3bed02ddddb851ac21a1d1a648c9aeba\n--- /dev/null\n+++ b/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\n@@ -0,0 +1,55 @@\n+# SADR-0019: Separate Shiki Platform SADRs From Target ADRs\n+\n+## Status\n+\n+Accepted\n+\n+## Context\n+\n+Shiki installs its platform decision records into `docs/adr/` in every target\n+repository. Shiki and its targets previously used the same numeric\n+`NNNN-<slug>.md` namespace. A target that already owned ADRs 0013 through 0018\n+therefore acquired two records for each number during installation, making an\n+`ADR NNNN` citation ambiguous. Renumbering target decisions would rewrite target\n+history, while renumbering Shiki decisions per target would destroy their stable\n+platform identity.\n+\n+Upgrades also need to remove the former Shiki-owned numeric paths without ever\n+mistaking a target-owned ADR for platform content. The install stamp already\n+records exact shipped paths and content digests, so it can prove ownership only\n+when both still match.\n+\n+## Decision\n+\n+Shiki platform decisions use the `SADR` namespace:\n+\n+- records are named `docs/adr/SADR-NNNN-<slug>.md`;\n+- live platform citations use `SADR-NNNN`;\n+- target product decisions retain `docs/adr/NNNN-<slug>.md` and `ADR NNNN`;\n+- the reference validator resolves the two namespaces independently, so the same\n+  four-digit number may exist once in each namespace.\n+\n+Before a forced upgrade writes anything, the installer inventories the exact\n+former numeric Shiki ADR paths. It may delete a legacy path only when the\n+existing install stamp contains that exact path and its recorded SHA-256 digest\n+matches the file currently on disk. All ownership blockers are reported\n+together and abort the upgrade before its first write. A non-forced install\n+never performs this cleanup.\n+\n+Historical `.shiki/` evidence remains unchanged because it records the namespace\n+that existed when that evidence was produced.\n+\n+## Consequences\n+\n+- Targets may use any ADR number without colliding with Shiki's platform SADRs.\n+- Fresh installs preserve target ADR bytes and install a stable SADR set.\n+- Forced upgrades fail closed when legacy ownership cannot be proven, and a\n+  successful upgrade refreshes the stamp without removed legacy paths.\n+- Current governance, packaging, workflow, prompt, script, and skill references\n+  must move with the platform records.\n+\n+## Alternatives Considered\n+\n+- Collision-only refusal leaves affected targets permanently unupgradable.\n+- Renumbering target ADRs mutates target-owned history.\n+- Per-target Shiki renumbering makes platform citations non-portable.\ndiff --git a/docs/adr/template.md b/docs/adr/template.md\nindex a92a97daddd0b00ab9dd626cc8d86942da2eafed..17dd79175b5fee2105935557f691b40cf9492942\n--- a/docs/adr/template.md\n+++ b/docs/adr/template.md\n@@ -1,14 +1,14 @@\n-# NNNN: <Short Decision Title>\n+# <SADR-NNNN or NNNN>: <Short Decision Title>\n \n ## Status\n \n-Proposed | Accepted | Superseded by ADR-NNNN\n+Proposed | Accepted | Superseded by SADR-NNNN or target ADR NNNN\n \n ## Context\n \n What is the situation, constraint, or force that requires a decision? Reference\n-relevant domain terms (`CONTEXT.md`), prior ADRs, and code reality. State the\n-problem, not the solution.\n+relevant domain terms (`CONTEXT.md`), prior Shiki SADRs or target ADRs, and code\n+reality. State the problem, not the solution.\n \n ## Decision\n \ndiff --git a/docs/agents/autonomous-loop-e2e-acceptance.md b/docs/agents/autonomous-loop-e2e-acceptance.md\nindex bbf84c6081987f159ac6ad9ecf3c82b7aa72e7e8..5c13c1d02a05a90b0dc46cbef7c95a9846d0c237\n--- a/docs/agents/autonomous-loop-e2e-acceptance.md\n+++ b/docs/agents/autonomous-loop-e2e-acceptance.md\n@@ -40,8 +40,8 @@\n 3. `gh auth status` is logged in with merge permission on the repo.\n 4. Branch protection + required checks are configured exactly as the goal-loop\n    required-check set expects (see `docs/agents/branch-protection-smoke.md`).\n-5. The repo `.shiki/policy` permits autonomous low/medium-risk merges (ADR\n-   0008/0009); the MergeGate policy check is the recorded-authority gate.\n+5. The repo `.shiki/policy` permits autonomous low/medium-risk merges\n+   (SADR-0008/0009); the MergeGate policy check is the recorded-authority gate.\n \n The operator's only actions are: register the task and start the loop. **No\n manual pre-clearing** means after `shiki loop run` starts, the operator does not\ndiff --git a/docs/agents/bootstrap-command.md b/docs/agents/bootstrap-command.md\nindex 05f7f203a52cf412b9f97084a6e131ed0f785afd..f7aa0b734dfebee4a3b139335b66ee3415f48713\n--- a/docs/agents/bootstrap-command.md\n+++ b/docs/agents/bootstrap-command.md\n@@ -189,7 +189,9 @@\n ```\n \n Use this only for tests, fixtures, or explicit local-only template inspection.\n-Use `--force` only when you intentionally want to overwrite existing target files.\n+Use `--force` only when you intentionally want to refresh the target template.\n+Legacy numeric Shiki decision cleanup is stamp-and-digest authorized before any\n+write; target ADR paths are not cleanup candidates.\n \n ## Slash Command\n \ndiff --git a/docs/agents/checklists.md b/docs/agents/checklists.md\nindex 4a7f03ade346f53a1c1d82981f3fd744f9ab4547..0c5ec6c53dc7197b7b7a7d18e667b325babd6c45\n--- a/docs/agents/checklists.md\n+++ b/docs/agents/checklists.md\n@@ -32,18 +32,18 @@\n | ID | Item | Blocking |\n | --- | --- | --- |\n | GD-01 | Plan was challenged against `CONTEXT.md`. | blocking for non-trivial Goals |\n-| GD-02 | Relevant ADRs were read or absence was recorded. | blocking for non-trivial Goals |\n+| GD-02 | Relevant SADRs and target ADRs were read or absence was recorded. | blocking for non-trivial Goals |\n | GD-03 | Ambiguous terms were resolved or converted to HITL blockers. | blocking |\n | GD-04 | Edge scenarios were considered. | advisory |\n | GD-05 | Code reality was checked when code could answer. | blocking when applicable |\n | GD-06 | New domain terms were added to `CONTEXT.md` when needed. | blocking when applicable |\n-| GD-07 | ADR was created or explicitly skipped using the ADR criteria. | blocking when applicable |\n+| GD-07 | A SADR or target ADR was created or explicitly skipped using the decision-record criteria. | blocking when applicable |\n \n ## CI \u2014 Context & Impact Checklist\n \n | ID | Item | Blocking |\n | --- | --- | --- |\n-| CI-01 | Relevant docs and ADRs identified. | blocking |\n+| CI-01 | Relevant docs, SADRs, and target ADRs identified. | blocking |\n | CI-02 | Relevant modules, interfaces, seams, callers, and tests identified. | blocking |\n | CI-03 | Dependencies and blocked-by relationships identified. | blocking |\n | CI-04 | Candidate locks identified. | blocking |\n@@ -62,7 +62,7 @@\n | PRD-04 | Implementation decisions are recorded without volatile code detail. | blocking |\n | PRD-05 | Testing decisions are recorded. | blocking |\n | PRD-06 | Out-of-scope items are explicit. | blocking |\n-| PRD-07 | Relevant glossary/ADR links are present when applicable. | blocking |\n+| PRD-07 | Relevant glossary/SADR/target-ADR links are present when applicable. | blocking |\n \n ## SF \u2014 Spec Freeze Checklist\n \n@@ -98,7 +98,7 @@\n | H-02 | Branch/worktree target is present. | blocking |\n | H-03 | Scope and non-goals are present. | blocking |\n | H-04 | Dependencies and locks are present. | blocking |\n-| H-05 | Relevant docs, ADRs, modules, and tests are referenced. | blocking |\n+| H-05 | Relevant docs, SADRs, target ADRs, modules, and tests are referenced. | blocking |\n | H-06 | Required skills are stated. | blocking |\n | H-07 | TDD expectations are stated. | blocking when behavior work |\n | H-08 | Acceptance criteria are included. | blocking |\ndiff --git a/docs/agents/codex-handoff.md b/docs/agents/codex-handoff.md\nindex ede468f47cb277f69a857e8c45df703e4ed0151d..6f97bdffb67e44d7edea2c71e01ea28933c4eb6c\n--- a/docs/agents/codex-handoff.md\n+++ b/docs/agents/codex-handoff.md\n@@ -46,7 +46,7 @@\n \n ## Context\n - Domain terms:\n-- Relevant ADRs:\n+- Relevant SADRs and target ADRs:\n - Relevant docs:\n - Relevant modules/interfaces/seams:\n - Similar tests/prior art:\ndiff --git a/docs/agents/completion-check-agent.md b/docs/agents/completion-check-agent.md\nindex 13e4c035db35025f4e3cdcc5adfb24314ac4d521..61bfa1218cf36ca605250c91919e620f33238aa0\n--- a/docs/agents/completion-check-agent.md\n+++ b/docs/agents/completion-check-agent.md\n@@ -32,7 +32,7 @@\n - `AGENTS.md`\n - `CLAUDE.md`\n - `CONTEXT.md`\n-- relevant ADRs\n+- relevant SADRs and target ADRs\n - parent Goal issue\n - PRD issue or document\n - task issue\ndiff --git a/docs/agents/decision-control.md b/docs/agents/decision-control.md\nindex 7d8ee5ab03a5ccbcbd04056ae61de03785a2a9c9..8c94ecda00fffaa0e92c325ab83aa9776bd1dac1\n--- a/docs/agents/decision-control.md\n+++ b/docs/agents/decision-control.md\n@@ -11,7 +11,7 @@\n | Decision | Owner | When | Required inputs | Output | Enforcement surface |\n | --- | --- | --- | --- | --- | --- |\n | Is the Goal clear enough to plan? | Planner + Guardian when needed | Goal intake | User request, existing docs, risk signals | Goal readiness verdict | Goal checklist, issue template, ledger |\n-| Must `grill-with-docs` run? | Planner / Skill Gate | Before PRD or issue split | Ambiguity, domain terms, ADR conflicts, unclear boundaries | Skill invocation or explicit skip rationale | Skill Gate checklist |\n+| Must `grill-with-docs` run? | Planner / Skill Gate | Before PRD or issue split | Ambiguity, domain terms, SADR/target-ADR conflicts, unclear boundaries | Skill invocation or explicit skip rationale | Skill Gate checklist |\n | Is a PRD required? | Planner | After Goal context stabilizes | Goal, `grill-with-docs` output, Context & Impact output | PRD required / not required | PRD checklist, CCA evidence check |\n | Can the spec be frozen? | Operator only | End of Requirements Definition, before issue split | PRD/requirements summary, scope inventory (SF-02), open-question list | `spec_freeze` block in the plan + ledger evidence | `require_grilled_plan` (plan ingest/run), SF checklist |\n | Can a frozen spec change? | Operator only (Spec Amendment) | When implementation contradicts the frozen spec | Contested decisions, scoped re-grill result | recorded amendment + re-stamped freeze | SF checklist, ledger evidence |\ndiff --git a/docs/agents/domain.md b/docs/agents/domain.md\nindex 258da8ea81e87ac0a6b0b5b5e5c2fde980eaabb7..d57b0cf17ff3e1fa9e6e5885372bf083d8295421\n--- a/docs/agents/domain.md\n+++ b/docs/agents/domain.md\n@@ -5,7 +5,9 @@\n ## Layout\n \n - `CONTEXT.md` defines Shiki language. Keep it implementation-free.\n-- `docs/adr/` records hard-to-reverse decisions.\n+- `docs/adr/SADR-NNNN-*.md` records Shiki platform decisions.\n+- `docs/adr/NNNN-*.md` records target product decisions without sharing the\n+  SADR namespace.\n - `AGENTS.md` is the runtime-neutral agent constitution.\n - `CLAUDE.md` is the Claude Code wrapper around `AGENTS.md`.\n - `.shiki/` is the Target Repository mirror for Goals, PRDs, plans, Task DAGs, contracts, locks, worktree records, ledger entries, CCA verdicts, repair packets, reports, and handoffs.\n@@ -15,6 +17,6 @@\n \n - Use Shiki terms exactly when they exist.\n - Add glossary terms to `CONTEXT.md` only when the term is part of the domain, not a generic programming concept.\n-- Create ADRs only for hard-to-reverse, surprising, tradeoff-based decisions.\n+- Create SADRs or target ADRs only for hard-to-reverse, surprising, tradeoff-based decisions.\n - Keep product-specific language in the Target Repository, not in Shiki Template docs.\n-- If an ADR conflicts with a proposed implementation, surface the conflict before editing.\n+- If a SADR or target ADR conflicts with a proposed implementation, surface the conflict before editing.\ndiff --git a/docs/agents/execution-control.md b/docs/agents/execution-control.md\nindex ad8ee3cdb36be56fedc1f46408ca92b387d74047..bc63c3a73661d12b567016ba299f1a6b66bf9088\n--- a/docs/agents/execution-control.md\n+++ b/docs/agents/execution-control.md\n@@ -63,7 +63,7 @@\n | Branch name | Orchestrator | Orchestrator | Before implementer dispatch | branch policy | branch ref in task/ledger |\n | Lock reservation | Context & Impact | MergeGate | Before dispatch and before merge | `.shiki/locks/`, labels, PR metadata | lock id, owner, scope |\n | CI profile | Planner | MergeGate | During issue triage, rechecked at PR | labels, `.shiki/policy.yaml`, workflow matrix | required check list |\n-| Required status checks | Repo maintainer / Guardian | GitHub branch protection / rulesets | Repository setup, updated via ADR | GitHub settings | branch/ruleset config, ADR |\n+| Required status checks | Repo maintainer / Guardian | GitHub branch protection / rulesets | Repository setup, updated via target ADR | GitHub settings | branch/ruleset config, target ADR |\n | CCA judgment | CCA | GitHub required check / MergeGate | Every non-draft PR update | GitHub Action check | structured CCA verdict |\n | Repair assignment | CCA | MergeGate | After failed CCA/CI/review | repair packet, issue/PR comment | repair id and attempt count |\n | Merge readiness | MergeGate | GitHub branch protection / Guardian if needed | After CCA complete | required checks, review rules, rulesets | mergegate check output |\ndiff --git a/docs/agents/guardian-policy.md b/docs/agents/guardian-policy.md\nindex 4147c7adf05a47adb4fd24ada1a1c2e1f2ba6d2d..a20d9cc6c00a569bb057b9ca8d6b51c7fc4a9847\n--- a/docs/agents/guardian-policy.md\n+++ b/docs/agents/guardian-policy.md\n@@ -98,8 +98,8 @@\n `origin/<default_branch>` and reuses each job's existing base fetch; a shallow\n (`--depth`) or second base fetch would make the proof unresolvable and silently\n disable the carry, so neither is permitted. CCA reads `guardian_comment_carried` as\n-a recorded authority in the deterministic signal and does not re-derive it. See ADR\n-0018 for the full decision, the rejected diff/patch-equality and path-disjointness\n+a recorded authority in the deterministic signal and does not re-derive it. See\n+SADR-0018 for the full decision, the rejected diff/patch-equality and path-disjointness\n alternatives, and the accepted residual.\n \n ## External AI Guardian Review (SADR-0010)\ndiff --git a/docs/agents/implementation-policy.md b/docs/agents/implementation-policy.md\nindex c754144a9fb5bf4a38aa82a60e1670c59e626b44..85c37281bcad196c6b7b02343faf00b03cd9c57a\n--- a/docs/agents/implementation-policy.md\n+++ b/docs/agents/implementation-policy.md\n@@ -31,10 +31,13 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n ## Non-Negotiables\n \n 1. **Planning precedes implementation.** Non-trivial Goals start with `grill-with-docs` and Context & Impact.\n-2. **Domain language is controlled.** Terms must be checked against `CONTEXT.md`; hard-to-reverse tradeoffs must be checked against ADRs.\n+2. **Domain language is controlled.** Terms must be checked against `CONTEXT.md`; hard-to-reverse tradeoffs must be checked against Shiki SADRs and target ADRs.\n 3. **PRD precedes issue decomposition.** `to-prd` is used once the context is settled enough to become durable intent.\n 4. **Issues are vertical slices.** `to-issues` must produce independently grabbable tracer-bullet tasks, not horizontal layer tickets.\n 5. **Triage controls dispatch.** Only `ready-for-agent` / AFK tasks may be assigned to Codex without human input.\n@@ -95,19 +98,19 @@\n Process:\n \n - Challenge terms against `CONTEXT.md`.\n-- Check ADRs before reopening decisions.\n+- Check SADRs and target ADRs before reopening decisions.\n - Inspect code when code can answer a question.\n - Ask one question at a time when the operator is available.\n - Provide a recommended answer with each question.\n - Record resolved terms in `CONTEXT.md`.\n-- Create ADRs only for hard-to-reverse, surprising, tradeoff-based decisions.\n+- Create SADRs or target ADRs only for hard-to-reverse, surprising, tradeoff-based decisions.\n - Convert unresolved load-bearing questions into HITL blockers.\n \n Outputs:\n \n - Resolved design-tree decisions.\n - Updated glossary terms when needed.\n-- ADRs when justified.\n+- SADRs or target ADRs when justified.\n - HITL blockers when decisions remain unresolved.\n \n ## Phase 3 \u2014 Context & Impact\n@@ -117,7 +120,7 @@\n \n Outputs must include:\n \n-- Relevant documents and ADRs.\n+- Relevant documents, SADRs, and target ADRs.\n - Relevant modules, interfaces, seams, callers, and tests.\n - Candidate locks.\n - Dependencies.\n@@ -140,7 +143,7 @@\n - Testing decisions.\n - Out of scope.\n - Further notes.\n-- Links to relevant Goal, domain terms, and ADRs.\n+- Links to relevant Goal, domain terms, SADRs, and target ADRs.\n \n Phase 4 ends with Spec Freeze: the operator explicitly approves the PRD, the\n plan records a `spec_freeze` block (status, approved_by, source), and the SF\n@@ -240,7 +243,7 @@\n - Dependencies and locks.\n - Risk and Guardian requirements.\n - Ledger completeness.\n-- Documentation and ADR updates.\n+- Documentation and SADR/target-ADR updates.\n - Scope drift.\n \n CCA emits one of:\n@@ -301,7 +304,7 @@\n | Evidence failure | Behavior might be done, but proof is missing. | Add checks, PR evidence, ledger entry, or artifacts. |\n | Scope drift | PR changes unrelated behavior. | Revert unrelated changes or split into new Goal. |\n | Dependency block | Upstream task or lock not complete. | Block until dependency resolves. |\n-| Architecture block | Decision needs ADR or deep-module discussion. | `grill-with-docs` or `improve-codebase-architecture`. |\n+| Architecture block | Decision needs a SADR, target ADR, or deep-module discussion. | `grill-with-docs` or `improve-codebase-architecture`. |\n | Risk block | Security/production/policy change needs human approval. | Guardian decision. |\n \n ## Phase 5.5 \u2014 Execution Decision Control\ndiff --git a/docs/agents/runtime-auth-model.md b/docs/agents/runtime-auth-model.md\nindex 6a2e1a47e81ea89b5c68a210ec07772a9374babc..3c54748d24233dbd0ba8473950282547ef4ce935\n--- a/docs/agents/runtime-auth-model.md\n+++ b/docs/agents/runtime-auth-model.md\n@@ -148,7 +148,7 @@\n \n API-key based runners may be added by a target repository as an explicit extension. They are not the default Shiki template path.\n \n-If a repo opts into API-key mode, record the exception in an ADR and update the repo's `AGENTS.md`, `.shiki/config.yaml`, and workflow permissions.\n+If a repo opts into API-key mode, record the target-specific exception in a target ADR and update the repo's `AGENTS.md`, `.shiki/config.yaml`, and workflow permissions.\n \n ## GitHub Actions Runtime Compatibility\n \ndiff --git a/docs/agents/skill-gate.md b/docs/agents/skill-gate.md\nindex c50398b0d7c60a8598f2a5c88a10015730dced13..3acffe6444617bb1d54b77d4b4708158ec735bc6\n--- a/docs/agents/skill-gate.md\n+++ b/docs/agents/skill-gate.md\n@@ -9,7 +9,7 @@\n | Skill | Trigger | Expected output |\n | --- | --- | --- |\n | `setup-matt-pocock-skills` | First configuring a repo for these skills, or when issue tracker, triage labels, or domain docs are missing. | `docs/agents/` configuration and an `Agent skills` block in `AGENTS.md` or `CLAUDE.md`. |\n-| `grill-with-docs` | Ambiguous plan, unclear terminology, boundary decision, tradeoff, or ADR-worthy design choice. | Clarified decision tree, recommended answers, updates to `CONTEXT.md` or ADRs when decisions crystallize. |\n+| `grill-with-docs` | Ambiguous plan, unclear terminology, boundary decision, tradeoff, or decision-record-worthy design choice. | Clarified decision tree, recommended answers, updates to `CONTEXT.md`, SADRs, or target ADRs when decisions crystallize. |\n | `zoom-out` | Agent lacks a higher-level architectural map. | Map of relevant modules, callers, seams, and domain concepts. |\n | `to-prd` | Settled context should become a PRD. | PRD published to the issue tracker with problem, solution, user stories, decisions, testing, and out-of-scope notes. |\n | `to-issues` | Goal, PRD, or plan must become implementation tickets. | Vertical-slice issues with acceptance criteria, dependencies, and AFK/HITL classification. |\n@@ -17,7 +17,7 @@\n | `tdd` | Feature work or bug fix has observable behavior that can be specified with tests. | Red-green-refactor loop through public interfaces, one vertical slice at a time. |\n | `code-review` | The TDD loop is green and the implementer is about to create or update a PR. Default for every implementation task. | Pre-PR self-review of the task diff with fixes applied on the task branch; findings and resolutions recorded as a ledger entry naming the skill and a `## Pre-PR code review` PR body section. |\n | `diagnose` | Hard bug, failing check, regression, flaky behavior, or performance problem. | Reproduction loop, ranked hypotheses, instrumentation, fix, regression test, post-mortem. |\n-| `improve-codebase-architecture` | Structural friction, testability, AI-navigability, deep-module opportunity, or refactor request. | Architecture candidates and deepening recommendations informed by `CONTEXT.md` and ADRs. |\n+| `improve-codebase-architecture` | Structural friction, testability, AI-navigability, deep-module opportunity, or refactor request. | Architecture candidates and deepening recommendations informed by `CONTEXT.md`, SADRs, and target ADRs. |\n | `prototype` | A throwaway logic or UI prototype is needed to answer a design question. | Clearly marked disposable prototype and captured decision. |\n \n ## MergeGate Interaction\ndiff --git a/docs/operations.md b/docs/operations.md\nindex cf1f452d4c589e38ccd58553bda0ad8684b51eb7..ace48a90ffbec9e5c0a7113bef16a4858bc44b3f\n--- a/docs/operations.md\n+++ b/docs/operations.md\n@@ -89,6 +89,16 @@\n      target history, brought forward by `shiki migrate`, not a template.\n    - **Everything else** (CLI scripts, workflows, schemas, docs) is overwritten.\n \n+   A forced upgrade also migrates the former numeric Shiki decision paths to\n+   `SADR-NNNN-*`. Before the first write, the installer inventories the exact\n+   legacy Shiki paths and authorizes each deletion only when the existing\n+   `.shiki/install-stamp.json` names that path and its SHA-256 digest still\n+   matches the file. An absent, unreadable, malformed, incomplete, or\n+   mismatching stamp reports every blocked path and stops with no filesystem\n+   change. Target-owned `NNNN-*` ADRs are not cleanup candidates. A non-force\n+   install deletes nothing and retains any still-matching legacy stamp entries\n+   so a later forced upgrade can prove ownership.\n+\n    If the target has pending migrations, `--force` refuses and writes nothing \u2014\n    apply migrations first (step 3), then re-run. The run ends with a single\n    summary of every `.new` written, so a half-upgrade is visible.\ndiff --git a/docs/releasing.md b/docs/releasing.md\nindex 10d73828e7dbd64e9d9f6b83bc84d010537ce62a..aafeee8c4d441e1e427efee2ce4f81a647f020e1\n--- a/docs/releasing.md\n+++ b/docs/releasing.md\n@@ -67,7 +67,7 @@\n CLI in `scripts/`. There are no third-party Python runtime dependencies. The CLI\n is intentionally **not** exposed as a `[project.scripts]` console entry point\n because `scripts/shiki.py` is a dependency-free shim and `scripts/` is not\n-packaged as an importable distribution (see the ADR for the rationale).\n+packaged as an importable distribution (see SADR-0007 for the rationale).\n \n Supported install paths:\n \n@@ -155,6 +155,15 @@\n    [docs/operations.md](operations.md#the-new-convention). If the target has\n    pending migrations, `--force` refuses and writes nothing; apply migrations\n    (step 2) first, then re-run.\n+\n+   The SADR namespace upgrade has an additional pre-write gate: each former\n+   numeric Shiki decision path is removed only when the existing install stamp\n+   contains that exact path and a digest matching its current bytes. The\n+   installer aggregates absent/unreadable/malformed-stamp, missing-digest, and\n+   digest-mismatch blockers before raising, so refusal leaves the target tree\n+   byte-identical. Numeric target ADRs are preserved. A successful forced\n+   upgrade installs and stamps `SADR-*` paths and drops the removed legacy paths\n+   from the refreshed stamp.\n 2. Apply repository-local `.shiki/` state migrations with the migrate command:\n \n    ```bash\ndiff --git a/docs/usage.md b/docs/usage.md\nindex b83407ee3970dd395dd893010bc5dbe8bad26051..a130ef6e40dfd8169a5bc701339351097df1a09f\n--- a/docs/usage.md\n+++ b/docs/usage.md\n@@ -67,8 +67,10 @@\n shiki install-target /path/to/target-repo --local-only\n ```\n \n-Use `--force` only when you intentionally want to overwrite existing target\n-files.\n+Use `--force` only when you intentionally want to refresh the target template.\n+For the SADR namespace migration, `--force` deletes only exact legacy Shiki\n+decision paths whose existing install-stamp digest still matches; any ownership\n+blocker stops before the first write. Numeric target ADRs are never candidates.\n \n ## `shiki bootstrap-platform` \u2014 publish the Shiki platform repo\n \ndiff --git a/scripts/guardian_approval_signal.py b/scripts/guardian_approval_signal.py\nindex d059f92b2c82aa2b5d6ca421365537a87886c1cb..16924892714f4f5b9303c0ef0e325a7e70075630\n--- a/scripts/guardian_approval_signal.py\n+++ b/scripts/guardian_approval_signal.py\n@@ -199,8 +199,8 @@\n ) -> Any:\n     \"\"\"Evaluate SADR-0015 Contract Approval for the CCA Guardian signal.\n \n-    The SAME OR that MergeGate applies must reach this deterministic signal: ADR\n-    0015 is explicit that without it, a contract-approved PR still returns\n+    The SAME OR that MergeGate applies must reach this deterministic signal:\n+    SADR-0015 is explicit that without it, a contract-approved PR still returns\n     ``needs_guardian``, the CCA job fails, and the MergeGate policy check never\n     runs \u2014 the gate this change moves would never be reached. This resolves the\n     task the SAME way MergeGate does (the first Shiki task id in the PR body),\ndiff --git a/scripts/shiki_contract_approval.py b/scripts/shiki_contract_approval.py\nindex 941bcb96fdd8b9a85bc0c4f0f04d5dad9d04839c..4a4b418f6527413161db82670bec857a043360e2\n--- a/scripts/shiki_contract_approval.py\n+++ b/scripts/shiki_contract_approval.py\n@@ -142,7 +142,7 @@\n     changed_files_status: Any,\n     registration: Any,\n ) -> ContractApprovalResult:\n-    \"\"\"Whether this PR carries an SADR-0015 Contract Approval.\n+    \"\"\"Whether this PR carries a SADR-0015 Contract Approval.\n \n     The four conditions are evaluated IN ORDER, each failure recorded as a\n     distinct reason string:\ndiff --git a/scripts/shiki_guardian.py b/scripts/shiki_guardian.py\nindex a4ecb79bbda55fd5c8b06adfae2eb99760a03c45..4dd99bbe714e26f421fa19abac7a34ae1b195ecc\n--- a/scripts/shiki_guardian.py\n+++ b/scripts/shiki_guardian.py\n@@ -12,7 +12,7 @@\n # The carry re-derives its proof from git in-process on every judging invocation;\n # it is never read from an artifact, so there is nothing to go stale or be forged.\n # shiki_sync_proof is the ONLY guardian-family module that runs git, and every one\n-# of its invocations is read-only and local (ADR: prove-from-git base sync).\n+# of its invocations is read-only and local (SADR: prove-from-git base sync).\n from shiki_sync_proof import verify_pure_base_sync\n \n GUARDIAN_POLICY_PATH = \".shiki/guardian-policy.json\"\ndiff --git a/scripts/shiki_guardian_review.py b/scripts/shiki_guardian_review.py\nindex dd0f104d457899e0ba5ff1e162011c1e90b6f92b..e10ef58c046324e7ea3a3392feb3048ca77414ce\n--- a/scripts/shiki_guardian_review.py\n+++ b/scripts/shiki_guardian_review.py\n@@ -159,7 +159,7 @@\n     docs_only = _is_docs_only(changed_files)\n     if docs_only and matched == 0:\n         focus.append(\n-            \"Docs/ADR-only change: confirm the change is genuinely documentation/decision-only, introduces no behavior or contract drift, and stays consistent with the constitution and existing ADRs.\"\n+            \"Docs/decision-record-only change: confirm the change is genuinely documentation/decision-only, introduces no behavior or contract drift, and stays consistent with the constitution and existing SADRs and target ADRs.\"\n         )\n \n     # Conservative augmentation: unknown (no category and not docs-only), mixed\ndiff --git a/scripts/shiki_installer.py b/scripts/shiki_installer.py\nindex be064ab7a29ff6836f7f7a1d70500a43e0d11d7b..9fd8eefd137ce7003dad5c0dcb2e48e2cd47ab32\n--- a/scripts/shiki_installer.py\n+++ b/scripts/shiki_installer.py\n@@ -118,6 +118,7 @@\n # what lets a target carry it without a governance-schema change.\n INSTALL_STAMP_PATH = \".shiki/install-stamp.json\"\n INSTALL_STAMP_VERSION = 1\n+LEGACY_SHIKI_SADR_MAX = 18\n \n # Under --force the shipped surface splits three ways, so nothing is lost and\n # nothing is silently stale.\n@@ -237,9 +238,98 @@\n         )\n \n \n+def legacy_shiki_adr_paths() -> tuple[str, ...]:\n+    \"\"\"Former numeric paths for Shiki SADR-0001 through SADR-0018.\n+\n+    The authoritative slugs come from the current SADR filenames. SADR-0019 is\n+    the namespace decision itself and never shipped under a numeric legacy path.\n+    \"\"\"\n+    paths: list[str] = []\n+    for sadr in sorted((ROOT / \"docs\" / \"adr\").glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\")):\n+        number = sadr.name.split(\"-\", 2)[1]\n+        if int(number) <= LEGACY_SHIKI_SADR_MAX:\n+            paths.append(f\"docs/adr/{sadr.name.removeprefix('SADR-')}\")\n+    return tuple(paths)\n+\n+\n+def preflight_legacy_sadr_cleanup(target: Path, *, force: bool) -> tuple[str, ...]:\n+    \"\"\"Authorize exact legacy Shiki ADR removals before the first write.\n+\n+    A non-force install never removes existing files. A forced install needs no\n+    stamp when no legacy Shiki path is present. When legacy paths do exist, each\n+    must be named in a readable install stamp and its current bytes must match\n+    the recorded digest.\n+    \"\"\"\n+    if not force:\n+        return ()\n+    present = tuple(\n+        relative\n+        for relative in legacy_shiki_adr_paths()\n+        if (target / relative).exists() or (target / relative).is_symlink()\n+    )\n+    if not present:\n+        return ()\n+\n+    stamp_path = target / INSTALL_STAMP_PATH\n+    try:\n+        raw = stamp_path.read_text(encoding=\"utf-8\")\n+        stamp = json.loads(raw)\n+    except FileNotFoundError:\n+        detail = \"install stamp is absent\"\n+        blockers = [f\"{relative}: {detail}\" for relative in present]\n+    except (OSError, UnicodeDecodeError) as error:\n+        detail = f\"install stamp is unreadable ({error})\"\n+        blockers = [f\"{relative}: {detail}\" for relative in present]\n+    except json.JSONDecodeError as error:\n+        detail = f\"install stamp is malformed ({error})\"\n+        blockers = [f\"{relative}: {detail}\" for relative in present]\n+    else:\n+        if not isinstance(stamp, dict):\n+            blockers = [\n+                f\"{relative}: install stamp is malformed (expected an object)\"\n+                for relative in present\n+            ]\n+        else:\n+            digests = stamp.get(\"digests\")\n+            blockers = []\n+            if not isinstance(digests, dict):\n+                blockers.extend(\n+                    f\"{relative}: install stamp has no digest for this path\"\n+                    for relative in present\n+                )\n+            else:\n+                for relative in present:\n+                    expected = digests.get(relative)\n+                    if not isinstance(expected, str) or not expected:\n+                        blockers.append(\n+                            f\"{relative}: install stamp has no digest for this path\"\n+                        )\n+                        continue\n+                    try:\n+                        actual = _sha256_file(target / relative)\n+                    except OSError as error:\n+                        blockers.append(\n+                            f\"{relative}: legacy path is unreadable ({error})\"\n+                        )\n+                        continue\n+                    if actual != expected:\n+                        blockers.append(\n+                            f\"{relative}: digest mismatch (stamp {expected}, current {actual})\"\n+                        )\n+\n+    if blockers:\n+        joined = \"\\n\".join(f\"- {blocker}\" for blocker in blockers)\n+        raise ShikiError(\n+            \"legacy Shiki ADR cleanup is not authorized:\\n\"\n+            f\"{joined}\\nNo files were written.\"\n+        )\n+    return present\n+\n+\n def install_template(target: Path, *, force: bool, validate: bool) -> list[NewFileNote]:\n     # Preconditions first: never rewrite the tree of a target that cannot be\n     # upgraded cleanly (see refuse_pending_migrations).\n+    legacy_cleanup = preflight_legacy_sadr_cleanup(target, force=force)\n     refuse_pending_migrations(target)\n \n     # Resolve the target's required CODEOWNERS owner once from its\n@@ -250,6 +340,10 @@\n     # verbatim. See ``codeowners_required_owner``.\n     codeowners_owner = codeowners_required_owner(target)\n \n+    for relative in legacy_cleanup:\n+        (target / relative).unlink()\n+        info(f\"removed stamped legacy Shiki ADR: {target / relative}\")\n+\n     notes: list[NewFileNote] = []\n     for relative in TEMPLATE_PATHS:\n         source = ROOT / relative\n@@ -647,6 +741,22 @@\n     digests = target_stamp_digests(target)\n     existing = read_install_stamp(target)\n     if existing is not None:\n+        # A non-force install deliberately leaves legacy Shiki ADRs in place.\n+        # Preserve only still-matching ownership proof so a later forced\n+        # upgrade can authorize cleanup; never carry a missing or stale digest.\n+        existing_digests = existing.get(\"digests\")\n+        if isinstance(existing_digests, dict):\n+            for relative in legacy_shiki_adr_paths():\n+                expected = existing_digests.get(relative)\n+                path = target / relative\n+                if not isinstance(expected, str) or not path.is_file():\n+                    continue\n+                try:\n+                    actual = _sha256_file(path)\n+                except OSError:\n+                    continue\n+                if actual == expected:\n+                    digests[relative] = actual\n         if _incoming_is_older(commit, existing.get(\"platform_commit\")):\n             warn(\n                 \"kept existing install stamp: incoming install \"\ndiff --git a/scripts/shiki_loop.py b/scripts/shiki_loop.py\nindex 3611641da8aeb5038b1da90e3114b09a473ec6d8..84caa491b9cad0edc4176f209c13e9221e975681\n--- a/scripts/shiki_loop.py\n+++ b/scripts/shiki_loop.py\n@@ -424,7 +424,7 @@\n         # unresolvable CCA verdict has already produced its terminal stop and is never\n         # laundered into a head-moving sync (SADR-0010). It also never fires while the\n         # policy (Guardian) gate is red \u2014 for ANY reason, not only when it is the sole\n-        # red check \u2014 so a head-bound Guardian approval, or an SADR-0015\n+        # red check \u2014 so a head-bound Guardian approval, or a SADR-0015\n         # contract-approval carry that base movement defeated (the policy gate compares\n         # base and head task governance directly), is never silently restored by\n         # adopting base wholesale. It is bounded by a durable per-task counter so the\ndiff --git a/scripts/shiki_tasks.py b/scripts/shiki_tasks.py\nindex aec3809f24b4a0c8c96fc50971cff9ae9ad30381..d531052e9d9717058d4631483cefab44c26a65c1\n--- a/scripts/shiki_tasks.py\n+++ b/scripts/shiki_tasks.py\n@@ -821,7 +821,7 @@\n         \"prompt\": prompt,\n         \"entry_skill\": \"grill-with-docs\",\n         \"required_next_steps\": [\n-            \"Run grill-with-docs until terminology, boundaries, risks, and ADR-worthy decisions are settled.\",\n+            \"Run grill-with-docs until terminology, boundaries, risks, and decision-record-worthy choices are settled.\",\n             \"Obtain the operator's explicit approval of the PRD/requirements (Spec Freeze).\",\n             \"Write a machine-readable plan JSON with grill_with_docs.status=complete and spec_freeze.status=frozen.\",\n             \"Run shiki plan ingest --plan-file PLAN.json.\",\ndiff --git a/scripts/test_shiki_goal_loop.sh b/scripts/test_shiki_goal_loop.sh\nindex 753c7b516e10f0be3bc92a4bd0eb1fa699776ae6..8f3820a0a079b3044424cfe1a6c6b7fe68896e67\n--- a/scripts/test_shiki_goal_loop.sh\n+++ b/scripts/test_shiki_goal_loop.sh\n@@ -231,8 +231,8 @@\n \n # High/critical risk merges autonomously when all required checks are green:\n # the \"MergeGate policy check\" required check IS the Guardian gate (it enforces\n-# guardian-policy.json \u2014 human approval OR external AI guardian review, ADR\n-# 0010), so green checks mean a recorded authority approved. The fake gh returns\n+# guardian-policy.json \u2014 human approval OR external AI guardian review,\n+# SADR-0010), so green checks mean a recorded authority approved. The fake gh returns\n # green checks, simulating that the Guardian gate passed.\n python3 \"$ROOT/scripts/shiki.py\" goal create --target \"$TARGET\" --title \"Guardian gate\" --outcome \"High risk merges via policy gate\" >/tmp/shiki-goal-loop-guardian-goal.json\n GGOAL=\"$(json_get /tmp/shiki-goal-loop-guardian-goal.json goal_id)\"\ndiff --git a/tests/test_adr_references.py b/tests/test_adr_references.py\nindex 51706329c797d5dd9b47e082a333fa83b261d482..d005da4f771f5e8085ec22d4b787980e68f5ae8c\n--- a/tests/test_adr_references.py\n+++ b/tests/test_adr_references.py\n@@ -1,8 +1,8 @@\n-\"\"\"Every ``ADR ####`` citation must resolve to exactly one decision record.\n+\"\"\"Every target ADR and Shiki SADR citation resolves in its own namespace.\n \n Background. ``scripts/mergegate_check.py`` justifies ``is_bookkeeping_closeout``\n-with \"ADR 0017\" nine times and ``scripts/guardian_approval_signal.py`` cites\n-0016/0017 seven times, yet for a stretch the numbering in ``docs/adr/`` stopped\n+with \"SADR-0017\" nine times and ``scripts/guardian_approval_signal.py`` cites\n+SADR-0016/SADR-0017 seven times, yet for a stretch the numbering in ``docs/adr/`` stopped\n at 0015: the authoring commits lived on a coordinator branch and no task ever\n declared ``docs/adr/...`` in its locks, so nothing carried the records to the\n default branch. The stated basis of an exemption that merged seven PRs with zero\n@@ -11,11 +11,12 @@\n and the directory drifted with no signal.\n \n This is the same unbound-pair shape behind other silent failures: a reference\n-and its target with nothing asserting they agree. This test binds them. It scans\n-every ``scripts/*.py`` (plus ``docs/**/*.md`` and the root ``*.md`` governance\n-files, which are cheap) for the ``ADR ####`` pattern and asserts each cited\n-number resolves to exactly one ``docs/adr/####-*.md``. The converse is\n-deliberately NOT required: an ADR may exist without being cited.\n+and its target with nothing asserting they agree. This test binds them. Target\n+``ADR NNNN`` citations resolve only to ``docs/adr/NNNN-*.md``; Shiki\n+``SADR-NNNN`` citations resolve only to ``docs/adr/SADR-NNNN-*.md``. The scan\n+covers current governance docs, scripts, workflows, prompts, packaging metadata,\n+and repo-local Codex guidance while deliberately excluding historical\n+``.shiki`` evidence. The converse is not required: a record may be uncited.\n \n The suite is dependency free: standard-library ``unittest`` only.\n \"\"\"\n@@ -31,17 +32,18 @@\n \n REPO_ROOT = Path(__file__).resolve().parents[1]\n \n-# \"ADR 0016\" -- a four-digit citation. The trailing \\b keeps a five-digit token\n-# from being read as a truncated four-digit number.\n-_CITATION = re.compile(r\"\\bADR (\\d{4})\\b\")\n-# \"0016-locks-bound-product-paths.md\" -- the leading four digits name the record.\n-_ADR_FILENAME = re.compile(r\"^(\\d{4})-.*\\.md$\")\n+# ``ADR 0016`` / ``SADR-0016`` -- a namespace plus four-digit citation. The\n+# trailing ``\\b`` keeps a five-digit token from being read as a truncated one.\n+_CITATION = re.compile(r\"\\b(S?ADR)[ -](\\d{4})\\b\")\n+# Target ADRs retain the numeric filename; Shiki platform decisions use SADR.\n+_DECISION_FILENAME = re.compile(r\"^(?:(SADR)-)?(\\d{4})-.*\\.md$\")\n \n \n class Violation(NamedTuple):\n     \"\"\"A citation that does not resolve to exactly one decision record.\"\"\"\n \n     citing_file: str  # repo-relative path of the file that cites the ADR\n+    namespace: str  # \"ADR\" for target decisions; \"SADR\" for Shiki decisions\n     number: str  # the cited four-digit number, e.g. \"0016\"\n     line: int  # 1-based line of the citation\n     reason: str  # \"missing\" (zero matches) or \"ambiguous\" (two or more)\n@@ -49,44 +51,59 @@\n \n     @property\n     def expected_path(self) -> str:\n-        return f\"docs/adr/{self.number}-*.md\"\n+        prefix = \"SADR-\" if self.namespace == \"SADR\" else \"\"\n+        return f\"docs/adr/{prefix}{self.number}-*.md\"\n \n     def describe(self) -> str:\n         \"\"\"Name the citing file, the cited number, and the missing path.\"\"\"\n         if self.reason == \"missing\":\n             return (\n-                f\"{self.citing_file}:{self.line} cites ADR {self.number} \"\n+                f\"{self.citing_file}:{self.line} cites {self.namespace} {self.number} \"\n                 f\"but no document matches {self.expected_path}\"\n             )\n         joined = \", \".join(self.matches)\n         return (\n-            f\"{self.citing_file}:{self.line} cites ADR {self.number} which \"\n+            f\"{self.citing_file}:{self.line} cites {self.namespace} {self.number} which \"\n             f\"resolves to {len(self.matches)} documents (expected exactly one \"\n             f\"at {self.expected_path}): {joined}\"\n         )\n \n \n-def _adr_index(root: Path) -> dict[str, list[Path]]:\n-    \"\"\"Map every four-digit ADR number to the record files that claim it.\"\"\"\n-    index: dict[str, list[Path]] = {}\n+def _adr_index(root: Path) -> dict[tuple[str, str], list[Path]]:\n+    \"\"\"Map each decision namespace and number to the files that claim it.\"\"\"\n+    index: dict[tuple[str, str], list[Path]] = {}\n     adr_dir = root / \"docs\" / \"adr\"\n     if adr_dir.is_dir():\n         for path in sorted(adr_dir.glob(\"*.md\")):\n-            match = _ADR_FILENAME.match(path.name)\n+            match = _DECISION_FILENAME.match(path.name)\n             if match:\n-                index.setdefault(match.group(1), []).append(path)\n+                namespace = \"SADR\" if match.group(1) else \"ADR\"\n+                index.setdefault((namespace, match.group(2)), []).append(path)\n     return index\n \n \n def _citing_files(root: Path, include_docs: bool = True) -> list[Path]:\n-    \"\"\"Files scanned for citations: scripts/*.py, and cheaply the markdown.\"\"\"\n+    \"\"\"Current, shipped text surfaces scanned for decision citations.\"\"\"\n     files = sorted((root / \"scripts\").glob(\"*.py\"))\n+    files += sorted((root / \"scripts\").glob(\"*.sh\"))\n     if include_docs:\n         docs = root / \"docs\"\n         if docs.is_dir():\n             files += sorted(docs.rglob(\"*.md\"))\n         files += sorted(root.glob(\"*.md\"))\n-    return files\n+        pyproject = root / \"pyproject.toml\"\n+        if pyproject.is_file():\n+            files.append(pyproject)\n+        for relative in (\".github\", \".codex\"):\n+            surface = root / relative\n+            if surface.is_dir():\n+                files += sorted(\n+                    path\n+                    for path in surface.rglob(\"*\")\n+                    if path.is_file()\n+                    and path.suffix in {\".md\", \".py\", \".sh\", \".toml\", \".yaml\", \".yml\"}\n+                )\n+    return sorted(set(files))\n \n \n def find_adr_reference_violations(\n@@ -94,10 +111,9 @@\n ) -> list[Violation]:\n     \"\"\"Return every ``ADR ####`` citation under ``root`` that fails to resolve.\n \n-    A citation resolves when exactly one ``docs/adr/####-*.md`` matches its\n-    number. Zero matches is a \"missing\" violation; two or more is \"ambiguous\".\n-    Records that are never cited produce nothing -- the converse is not\n-    required.\n+    A citation resolves when exactly one record in the same namespace matches\n+    its number. Zero matches is a \"missing\" violation; two or more is\n+    \"ambiguous\". Records that are never cited produce nothing.\n     \"\"\"\n     root = Path(root)\n     index = _adr_index(root)\n@@ -110,13 +126,15 @@\n         rel = path.relative_to(root).as_posix()\n         for lineno, line in enumerate(text.splitlines(), start=1):\n             for match in _CITATION.finditer(line):\n-                number = match.group(1)\n-                found = index.get(number, [])\n+                namespace, number = match.groups()\n+                found = index.get((namespace, number), [])\n                 if len(found) == 1:\n                     continue\n                 reason = \"missing\" if not found else \"ambiguous\"\n                 rels = [p.relative_to(root).as_posix() for p in found]\n-                violations.append(Violation(rel, number, lineno, reason, rels))\n+                violations.append(\n+                    Violation(rel, namespace, number, lineno, reason, rels)\n+                )\n     return violations\n \n \n@@ -129,8 +147,8 @@\n         self.assertEqual(\n             violations,\n             [],\n-            \"Every 'ADR ####' citation must resolve to exactly one \"\n-            f\"docs/adr/####-*.md:\\n{detail}\",\n+            \"Every ADR/SADR citation must resolve exactly once in its own \"\n+            f\"docs/adr namespace:\\n{detail}\",\n         )\n \n     def test_scan_actually_reads_scripts(self) -> None:\n@@ -142,12 +160,38 @@\n             \"the citation scan must cover scripts/*.py\",\n         )\n \n+    def test_platform_citations_do_not_use_unresolved_numeric_shorthand(self) -> None:\n+        shorthand = re.compile(r\"SADR-\\d{4}/\\d{4}\")\n+        offenders = []\n+        for path in _citing_files(REPO_ROOT):\n+            text = path.read_text(encoding=\"utf-8\")\n+            if shorthand.search(text):\n+                offenders.append(path.relative_to(REPO_ROOT).as_posix())\n+        self.assertEqual(\n+            offenders,\n+            [],\n+            \"repeat the SADR prefix so every cited number is independently resolvable\",\n+        )\n+\n     def test_carried_records_are_indexed(self) -> None:\n         # The two records this Goal carried onto the default branch, and which\n         # the scripts cite most, must be present.\n         index = _adr_index(REPO_ROOT)\n-        self.assertIn(\"0016\", index)\n-        self.assertIn(\"0017\", index)\n+        self.assertIn((\"SADR\", \"0016\"), index)\n+        self.assertIn((\"SADR\", \"0017\"), index)\n+\n+    def test_platform_tree_has_only_sadr_0001_through_0019(self) -> None:\n+        adr_dir = REPO_ROOT / \"docs\" / \"adr\"\n+        sadrs = sorted(adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\"))\n+        self.assertEqual(\n+            {path.name.split(\"-\", 2)[1] for path in sadrs},\n+            {f\"{number:04d}\" for number in range(1, 20)},\n+        )\n+        self.assertEqual(\n+            list(adr_dir.glob(\"[0-9][0-9][0-9][0-9]-*.md\")),\n+            [],\n+            \"the Shiki platform tree must not retain legacy numeric SADR paths\",\n+        )\n \n \n class FixtureAdrReferences(unittest.TestCase):\n@@ -180,6 +224,26 @@\n         self.assertIn(\"9991\", message)  # cited number\n         self.assertIn(\"docs/adr/9991-*.md\", message)  # missing path\n \n+    def test_target_adr_does_not_satisfy_missing_platform_sadr(self) -> None:\n+        root = self._make_repo(\n+            adr_names=[\"0042-target-decision.md\"],\n+            script_body='\"\"\"Target ADR 0042 exists, but platform SADR-0042 does not.\"\"\"\\n',\n+        )\n+        violations = find_adr_reference_violations(root, include_docs=False)\n+        self.assertEqual(len(violations), 1)\n+        self.assertEqual(violations[0].namespace, \"SADR\")\n+        self.assertEqual(violations[0].number, \"0042\")\n+        self.assertEqual(violations[0].expected_path, \"docs/adr/SADR-0042-*.md\")\n+\n+    def test_target_adr_and_platform_sadr_may_reuse_a_number(self) -> None:\n+        root = self._make_repo(\n+            adr_names=[\"0042-target-decision.md\", \"SADR-0042-platform-decision.md\"],\n+            script_body='\"\"\"Target ADR 0042 and platform SADR-0042 are distinct.\"\"\"\\n',\n+        )\n+        self.assertEqual(\n+            find_adr_reference_violations(root, include_docs=False), []\n+        )\n+\n     def test_uncited_adr_is_allowed(self) -> None:\n         # An ADR exists but nothing cites it -> not a violation. The converse\n         # (every ADR must be cited) is explicitly NOT required.\ndiff --git a/tests/test_guardian_review.py b/tests/test_guardian_review.py\nindex 88829e7810212635e2fc98aa5918d6f26294677d..00a85a237acd5bf2d6cb5b294128198c422402ca\n--- a/tests/test_guardian_review.py\n+++ b/tests/test_guardian_review.py\n@@ -162,7 +162,7 @@\n \n     def test_docs_only_is_light_and_not_conservative(self):\n         focus = gr.classify_review_focus([\"docs/adr/0099-x.md\", \"README.md\"], \"\", \"low\")\n-        self.assertTrue(any(\"Docs/ADR-only\" in f for f in focus))\n+        self.assertTrue(any(\"Docs/decision-record-only\" in f for f in focus))\n         self.assertFalse(any(\"conservative default\" in f.lower() for f in focus))\n \n     def test_unknown_category_is_conservative(self):\ndiff --git a/tests/test_policy_job_merged_prs.py b/tests/test_policy_job_merged_prs.py\nindex d45512e74681e5e649279236c7d4e79d5c3da471..b97989f8bd74d091de1a6b5275a20d8dc68c4512\n--- a/tests/test_policy_job_merged_prs.py\n+++ b/tests/test_policy_job_merged_prs.py\n@@ -3,8 +3,8 @@\n \n ``.github/workflows/shiki-cca-completion.yml`` has two consumers of the merged-PR\n proof: the CCA *signal* (``guardian_approval_signal.py`` in \"Prepare CCA context\")\n-and the authoritative gate (``mergegate_check.py`` in \"Enforce MergeGate\"). ADR\n-0017's bookkeeping-closeout exemption (condition 6) needs the merged implementation\n+and the authoritative gate (``mergegate_check.py`` in \"Enforce MergeGate\").\n+SADR-0017's bookkeeping-closeout exemption (condition 6) needs the merged implementation\n PR proven, and a closeout carries NO ``post_merge_reconcile`` marker. The signal\n already resolves ``MERGED_PRS`` unconditionally, but \"Enforce MergeGate\" used to\n resolve it only inside ``grep -q '<!-- shiki:post_merge_reconcile -->'``. So a\ndiff --git a/tests/test_sadr_install_migration.py b/tests/test_sadr_install_migration.py\nnew file mode 100644\nindex 0000000000000000000000000000000000000000..d51eac872b1c79ff4579c4c1401ca5d775a60d8d\n--- /dev/null\n+++ b/tests/test_sadr_install_migration.py\n@@ -0,0 +1,237 @@\n+\"\"\"SADR installation and collision-safe legacy upgrade behavior.\"\"\"\n+\n+from __future__ import annotations\n+\n+import contextlib\n+import hashlib\n+import io\n+import json\n+import shutil\n+import tempfile\n+import unittest\n+from pathlib import Path\n+\n+import shiki_test_support  # noqa: F401  (path bootstrap)\n+\n+import shiki_installer\n+from tests.test_adr_references import find_adr_reference_violations\n+\n+\n+PRODUCT_NUMBERS = tuple(range(13, 19))\n+\n+\n+def _quiet_install(target: Path, *, force: bool, validate: bool) -> None:\n+    with contextlib.redirect_stdout(io.StringIO()):\n+        shiki_installer.install_template(target, force=force, validate=validate)\n+\n+\n+def _sha256(path: Path) -> str:\n+    return hashlib.sha256(path.read_bytes()).hexdigest()\n+\n+\n+def _legacy_sadr_paths() -> tuple[str, ...]:\n+    return tuple(\n+        f\"docs/adr/{path.name.removeprefix('SADR-')}\"\n+        for path in sorted((shiki_installer.ROOT / \"docs\" / \"adr\").glob(\"SADR-*.md\"))\n+        if path.name != \"SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\"\n+    )\n+\n+\n+def _create_legacy_upgrade_fixture(\n+    target: Path,\n+) -> tuple[dict[str, bytes], tuple[str, ...]]:\n+    adr_dir = target / \"docs\" / \"adr\"\n+    adr_dir.mkdir(parents=True)\n+    product_bytes = {\n+        f\"{number:04d}-cubeshare-product-decision.md\": (\n+            f\"# CubeShare ADR {number:04d}\\n\\nDo not rewrite {number}.\\n\"\n+        ).encode()\n+        for number in PRODUCT_NUMBERS\n+    }\n+    for name, content in product_bytes.items():\n+        (adr_dir / name).write_bytes(content)\n+\n+    legacy_paths = _legacy_sadr_paths()\n+    for relative in legacy_paths:\n+        (target / relative).write_bytes(f\"legacy Shiki bytes for {relative}\\n\".encode())\n+\n+    stamp_path = target / shiki_installer.INSTALL_STAMP_PATH\n+    stamp_path.parent.mkdir(parents=True, exist_ok=True)\n+    stamp = {\n+        \"version\": shiki_installer.INSTALL_STAMP_VERSION,\n+        \"platform_commit\": None,\n+        \"installed_at\": \"2000-01-01T00:00:00+00:00\",\n+        \"digests\": {\n+            relative: _sha256(target / relative) for relative in legacy_paths\n+        },\n+    }\n+    stamp_path.write_text(\n+        json.dumps(stamp, indent=2, sort_keys=True) + \"\\n\", encoding=\"utf-8\"\n+    )\n+    return product_bytes, legacy_paths\n+\n+\n+def _tree_snapshot(root: Path) -> dict[str, tuple[str, bytes | str]]:\n+    \"\"\"Recursive byte/shape snapshot used to prove preflight is write-free.\"\"\"\n+    snapshot: dict[str, tuple[str, bytes | str]] = {}\n+    for path in sorted(root.rglob(\"*\")):\n+        relative = path.relative_to(root).as_posix()\n+        if path.is_symlink():\n+            snapshot[relative] = (\"symlink\", path.readlink().as_posix())\n+        elif path.is_dir():\n+            snapshot[relative] = (\"directory\", \"\")\n+        else:\n+            snapshot[relative] = (\"file\", path.read_bytes())\n+    return snapshot\n+\n+\n+class FreshSadrInstallTests(unittest.TestCase):\n+    def setUp(self) -> None:\n+        self.tmp = Path(tempfile.mkdtemp(prefix=\"shiki-sadr-fresh-\"))\n+        self.addCleanup(lambda: shutil.rmtree(self.tmp, ignore_errors=True))\n+        self.target = self.tmp / \"target\"\n+        self.adr_dir = self.target / \"docs\" / \"adr\"\n+        self.adr_dir.mkdir(parents=True)\n+        self.product_bytes = {\n+            f\"{number:04d}-product-decision.md\": (\n+                f\"# Product ADR {number:04d}\\n\\nTarget-owned bytes {number}.\\n\"\n+            ).encode()\n+            for number in PRODUCT_NUMBERS\n+        }\n+        for name, content in self.product_bytes.items():\n+            (self.adr_dir / name).write_bytes(content)\n+\n+    def test_fresh_install_preserves_product_adrs_and_installs_all_sadrs(self) -> None:\n+        _quiet_install(self.target, force=False, validate=True)\n+\n+        for name, expected in self.product_bytes.items():\n+            self.assertEqual((self.adr_dir / name).read_bytes(), expected)\n+\n+        sadr_numbers = {\n+            path.name.split(\"-\", 2)[1]\n+            for path in self.adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\")\n+        }\n+        self.assertEqual(sadr_numbers, {f\"{number:04d}\" for number in range(1, 20)})\n+\n+        numeric_records = {\n+            path.name\n+            for path in self.adr_dir.glob(\"[0-9][0-9][0-9][0-9]-*.md\")\n+        }\n+        self.assertEqual(numeric_records, set(self.product_bytes))\n+        stamp = json.loads(\n+            (self.target / shiki_installer.INSTALL_STAMP_PATH).read_text(\n+                encoding=\"utf-8\"\n+            )\n+        )\n+        for path in self.adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\"):\n+            relative = path.relative_to(self.target).as_posix()\n+            self.assertEqual(stamp[\"digests\"][relative], _sha256(path))\n+        self.assertEqual(find_adr_reference_violations(self.target), [])\n+\n+\n+class LegacySadrUpgradeTests(unittest.TestCase):\n+    def setUp(self) -> None:\n+        self.tmp = Path(tempfile.mkdtemp(prefix=\"shiki-sadr-upgrade-\"))\n+        self.addCleanup(lambda: shutil.rmtree(self.tmp, ignore_errors=True))\n+        self.target = self.tmp / \"target\"\n+        self.adr_dir = self.target / \"docs\" / \"adr\"\n+        self.product_bytes, self.legacy_paths = _create_legacy_upgrade_fixture(\n+            self.target\n+        )\n+\n+    def test_force_upgrade_deletes_only_stamped_legacy_sadrs(self) -> None:\n+        _quiet_install(self.target, force=True, validate=True)\n+\n+        for relative in self.legacy_paths:\n+            self.assertFalse((self.target / relative).exists(), relative)\n+        for name, expected in self.product_bytes.items():\n+            self.assertEqual((self.adr_dir / name).read_bytes(), expected)\n+\n+        sadrs = list(self.adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\"))\n+        self.assertEqual(len(sadrs), 19)\n+        refreshed = json.loads(\n+            (self.target / shiki_installer.INSTALL_STAMP_PATH).read_text(encoding=\"utf-8\")\n+        )\n+        for relative in self.legacy_paths:\n+            self.assertNotIn(relative, refreshed[\"digests\"])\n+        for path in sadrs:\n+            relative = path.relative_to(self.target).as_posix()\n+            self.assertEqual(refreshed[\"digests\"][relative], _sha256(path))\n+\n+    def test_non_force_install_never_deletes_legacy_files(self) -> None:\n+        before = {\n+            relative: (self.target / relative).read_bytes()\n+            for relative in self.legacy_paths\n+        }\n+        _quiet_install(self.target, force=False, validate=False)\n+        for relative, expected in before.items():\n+            self.assertEqual((self.target / relative).read_bytes(), expected)\n+        refreshed = json.loads(\n+            (self.target / shiki_installer.INSTALL_STAMP_PATH).read_text(encoding=\"utf-8\")\n+        )\n+        for relative in self.legacy_paths:\n+            self.assertEqual(refreshed[\"digests\"][relative], _sha256(self.target / relative))\n+\n+\n+class LegacyCleanupPreflightTests(unittest.TestCase):\n+    def setUp(self) -> None:\n+        self.tmp = Path(tempfile.mkdtemp(prefix=\"shiki-sadr-preflight-\"))\n+        self.addCleanup(lambda: shutil.rmtree(self.tmp, ignore_errors=True))\n+\n+    def test_all_ownership_blockers_abort_before_any_write(self) -> None:\n+        cases = (\n+            (\"absent\", \"install stamp is absent\"),\n+            (\"unreadable\", \"install stamp is unreadable\"),\n+            (\"malformed\", \"install stamp is malformed\"),\n+            (\"missing-digest\", \"install stamp has no digest\"),\n+            (\"digest-mismatch\", \"digest mismatch\"),\n+        )\n+        for name, expected_reason in cases:\n+            with self.subTest(name=name):\n+                target = self.tmp / name\n+                _, legacy_paths = _create_legacy_upgrade_fixture(target)\n+                stamp_path = target / shiki_installer.INSTALL_STAMP_PATH\n+\n+                if name == \"absent\":\n+                    stamp_path.unlink()\n+                elif name == \"unreadable\":\n+                    stamp_path.unlink()\n+                    stamp_path.mkdir()\n+                elif name == \"malformed\":\n+                    stamp_path.write_text(\"{not-json\\n\", encoding=\"utf-8\")\n+                else:\n+                    stamp = json.loads(stamp_path.read_text(encoding=\"utf-8\"))\n+                    if name == \"missing-digest\":\n+                        stamp[\"digests\"] = {}\n+                    else:\n+                        stamp[\"digests\"] = {\n+                            relative: \"deadbeef\" for relative in legacy_paths\n+                        }\n+                    stamp_path.write_text(\n+                        json.dumps(stamp, indent=2, sort_keys=True) + \"\\n\",\n+                        encoding=\"utf-8\",\n+                    )\n+\n+                before = _tree_snapshot(target)\n+                with self.assertRaises(shiki_installer.ShikiError) as raised:\n+                    _quiet_install(target, force=True, validate=False)\n+                after = _tree_snapshot(target)\n+\n+                self.assertEqual(before, after, f\"{name} preflight changed the tree\")\n+                message = str(raised.exception)\n+                self.assertIn(expected_reason, message)\n+                self.assertIn(\"No files were written\", message)\n+                for relative in legacy_paths:\n+                    self.assertIn(relative, message)\n+\n+    def test_absent_stamp_is_allowed_when_no_legacy_path_exists(self) -> None:\n+        target = self.tmp / \"no-legacy\"\n+        target.mkdir()\n+        _quiet_install(target, force=True, validate=False)\n+        self.assertTrue(\n+            (target / \"docs\" / \"adr\" / \"SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\").is_file()\n+        )\n+\n+\n+if __name__ == \"__main__\":\n+    unittest.main()\n\n2026-08-07T08:29:39.546315Z ERROR codex_models_manager::manager: failed to renew cache TTL: missing field `base_instructions` at line 94 column 5\n succeeded in 223ms:\nShiki validation passed\n\ncodex\nImplemented locally in the assigned worktree.\n\n- Renamed platform records to SADR-0001\u20130018 and added [SADR-0019](/private/tmp/shiki-adr-collision.pa0KwY/.worktrees/shiki-t-20260807t073307221254z-2d22d654-introduc/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md:1).\n- Added independent ADR/SADR resolution and collision fixtures in [test_adr_references.py](/private/tmp/shiki-adr-collision.pa0KwY/.worktrees/shiki-t-20260807t073307221254z-2d22d654-introduc/tests/test_adr_references.py:35).\n- Added digest-authorized, pre-write legacy cleanup in [shiki_installer.py](/private/tmp/shiki-adr-collision.pa0KwY/.worktrees/shiki-t-20260807t073307221254z-2d22d654-introduc/scripts/shiki_installer.py:241).\n- Added fresh-install, collision-upgrade, failure-atomicity, and non-force coverage in [test_sadr_install_migration.py](/private/tmp/shiki-adr-collision.pa0KwY/.worktrees/shiki-t-20260807t073307221254z-2d22d654-introduc/tests/test_sadr_install_migration.py:104).\n- Recorded TDD and structured implementer code-review evidence. One test-evidence finding was fixed and re-reviewed clean.\n\nLocal verification:\n\n- 1,147 unit tests passed.\n- Control-plane suite passed.\n- Adversarial suite passed.\n- `validate_shiki` and `git diff --check` passed.\n\nNo commit, push, `gh` command, PR, or approval record was created. Exact-head CI, independent loop review, CCA, Guardian approval, and MergeGate remain owned by the Shiki goal loop.\ndiff --git a/.codex/skills/shiki/SKILL.md b/.codex/skills/shiki/SKILL.md\nindex c8e2e85d9d75d49cc4f2d6286ee22659c4e53e75..93cded623b2c15d9f65c76491a1c2d813ac9e6c0\n--- a/.codex/skills/shiki/SKILL.md\n+++ b/.codex/skills/shiki/SKILL.md\n@@ -18,6 +18,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n Codex CLI does not register this skill as a custom `/shiki` slash command. When\n the user types `/shiki` in Codex CLI and gets \"Unrecognized command\", explain\n that this is expected: invoke Shiki in Codex with natural language such as\n@@ -69,7 +72,7 @@\n \n ## Responsibilities\n \n-- Claude Code implements and repairs by default (`shiki runner claude`, ADR 0008); Codex implements and repairs tasks explicitly assigned to `codex`.\n+- Claude Code implements and repairs by default (`shiki runner claude`, SADR-0008); Codex implements and repairs tasks explicitly assigned to `codex`.\n - Claude Code Action can act as GitHub-side CCA or reviewer.\n - CCA judges completion.\n - MergeGate authorizes state transitions and merge readiness.\ndiff --git a/.github/prompts/cca-completion-check.md b/.github/prompts/cca-completion-check.md\nindex ed203ad5f302ecc81372d8ab27d2b1232ca97497..fd89950528333ef6242e6d5443904f5392ab7e56\n--- a/.github/prompts/cca-completion-check.md\n+++ b/.github/prompts/cca-completion-check.md\n@@ -19,6 +19,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n ## Required Reading\n \n Read only the files needed to judge the PR contract. Prefer this order:\ndiff --git a/.github/prompts/codex-task-handoff.md b/.github/prompts/codex-task-handoff.md\nindex 695e211d76dea1c60e8d5bb360e78487808286a4..59441d576b01a39569722f226c854e5a740e9eb5\n--- a/.github/prompts/codex-task-handoff.md\n+++ b/.github/prompts/codex-task-handoff.md\n@@ -2,7 +2,7 @@\n \n You are Codex Front acting as the Implementer for a Shiki task.\n \n-Follow `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant ADRs, and `docs/agents/codex-handoff.md`.\n+Follow `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant Shiki SADRs and target ADRs, and `docs/agents/codex-handoff.md`.\n \n Rules:\n \ndiff --git a/.shiki/ledger/L-20260807T082800000000Z-c0de0019.json b/.shiki/ledger/L-20260807T082800000000Z-c0de0019.json\nnew file mode 100644\nindex 0000000000000000000000000000000000000000..bac355ecea44037859b3715f58bc32f3353cfcfa\n--- /dev/null\n+++ b/.shiki/ledger/L-20260807T082800000000Z-c0de0019.json\n@@ -0,0 +1,34 @@\n+{\n+  \"actor\": \"codex\",\n+  \"evidence\": [\n+    \"Full task diff reviewed against scope, non-goals, locks, acceptance checks, failure atomicity, path ownership, and compatibility requirements\",\n+    \"Finding repaired: tests/test_sadr_install_migration.py now asserts every fresh-installed SADR path has the matching SHA-256 digest in .shiki/install-stamp.json\",\n+    \"Re-review found no remaining blocking issues; rejected findings: none\",\n+    \"python3 -m unittest discover -s tests: 1147 tests passed\",\n+    \"bash scripts/test_shiki_control_plane.sh: passed\",\n+    \"bash scripts/test_shiki_adversarial_state.sh: passed\",\n+    \"python3 scripts/validate_shiki.py --target .: passed\"\n+  ],\n+  \"goal_id\": \"G-20260807T073307219752Z-1ee1ed68\",\n+  \"id\": \"L-20260807T082800000000Z-c0de0019\",\n+  \"links\": [],\n+  \"review\": {\n+    \"applied_fixes\": [\n+      \"Added direct fresh-install stamp path/digest assertions and reran focused plus full verification\"\n+    ],\n+    \"findings\": [\n+      {\n+        \"detail\": \"The fresh-install test proved SADR files were installed but did not directly prove their paths and digests were recorded in the refreshed install stamp.\",\n+        \"resolution\": \"fixed_and_re_reviewed\",\n+        \"severity\": \"blocking\",\n+        \"title\": \"Fresh-install stamp evidence was indirect\"\n+      }\n+    ],\n+    \"rejected_findings\": [],\n+    \"verdict\": \"clean_after_repairs\"\n+  },\n+  \"summary\": \"code-review implementer verdict CLEAN after re-review: findings=1; applied_fixes=1 (fresh-install stamp-digest assertion); rejected_findings=0 (none)\",\n+  \"task_id\": \"T-20260807T073307221254Z-2d22d654\",\n+  \"timestamp\": \"2026-08-07T08:28:00+00:00\",\n+  \"type\": \"review\"\n+}\ndiff --git a/.shiki/ledger/L-20260807T082914000000Z-7dd00019.json b/.shiki/ledger/L-20260807T082914000000Z-7dd00019.json\nnew file mode 100644\nindex 0000000000000000000000000000000000000000..432bcd63849c04eaaeaca3993c03a07c1152bb1a\n--- /dev/null\n+++ b/.shiki/ledger/L-20260807T082914000000Z-7dd00019.json\n@@ -0,0 +1,19 @@\n+{\n+  \"actor\": \"codex\",\n+  \"evidence\": [\n+    \"RED resolver: the target-ADR-does-not-satisfy-SADR fixture expected one missing SADR violation but received zero before namespace-aware indexing\",\n+    \"RED fresh install: the product-ADR fixture installed no SADR records before the platform rename\",\n+    \"RED forced upgrade: matching stamped legacy Shiki ADR paths remained before cleanup was implemented\",\n+    \"RED non-force upgrade: refreshed stamp omitted preserved legacy ownership evidence before matching digest carry-forward was implemented\",\n+    \"GREEN focused: 11 ADR/SADR resolver tests and 5 fresh-install/upgrade/preflight tests passed\",\n+    \"REFACTOR: separated namespace indexing, exact legacy-path inventory, read-only cleanup authorization, and stamp refresh responsibilities\",\n+    \"GREEN regression: 1147 unit tests, control-plane shell suite, adversarial shell suite, and validate_shiki passed\"\n+  ],\n+  \"goal_id\": \"G-20260807T073307219752Z-1ee1ed68\",\n+  \"id\": \"L-20260807T082914000000Z-7dd00019\",\n+  \"links\": [],\n+  \"summary\": \"tdd red-green-refactor evidence for separate ADR/SADR resolution and collision-safe install migration\",\n+  \"task_id\": \"T-20260807T073307221254Z-2d22d654\",\n+  \"timestamp\": \"2026-08-07T08:29:14+00:00\",\n+  \"type\": \"check\"\n+}\ndiff --git a/.shiki/tasks/T-20260807T073307221254Z-2d22d654.json b/.shiki/tasks/T-20260807T073307221254Z-2d22d654.json\nindex e7e150cc1637f1cfac2199e04055e93b53630dcb..00241552cac63069660eb722bd076decf6a5031d\n--- a/.shiki/tasks/T-20260807T073307221254Z-2d22d654.json\n+++ b/.shiki/tasks/T-20260807T073307221254Z-2d22d654.json\n@@ -36,7 +36,9 @@\n     \"L-20260807T080107948254Z-61f24da0\",\n     \"L-20260807T080228312210Z-07ac626e\",\n     \"L-20260807T080228575038Z-d574f629\",\n-    \"L-20260807T080256330734Z-5bf40cb6\"\n+    \"L-20260807T080256330734Z-5bf40cb6\",\n+    \"L-20260807T082800000000Z-c0de0019\",\n+    \"L-20260807T082914000000Z-7dd00019\"\n   ],\n   \"locks\": [\n     \"path:.codex/**\",\ndiff --git a/AGENTS.md b/AGENTS.md\nindex 7f041100d49c2a7525f51cb0f5d696a4a6d1c78c..a06bfbabc6fa48a007389e25e12d7ed523936bed\n--- a/AGENTS.md\n+++ b/AGENTS.md\n@@ -22,7 +22,7 @@\n - **Codex Front** is an optional implementer through Codex App, Codex CLI, Codex IDE extension, or Codex Web signed in with ChatGPT OAuth/subscription auth, dispatched with `shiki runner codex` when a task is explicitly assigned to `codex`.\n - **GitHub CCA** is implemented by Claude Code Action by default, using `CLAUDE_CODE_OAUTH_TOKEN`.\n \n-Do not assume `openai/codex-action`, `OPENAI_API_KEY`, or API-key based Codex automation in the default Shiki path. API-key runners are explicit target-repo extensions and require their own ADR.\n+Do not assume `openai/codex-action`, `OPENAI_API_KEY`, or API-key based Codex automation in the default Shiki path. API-key runners are explicit target-repo extensions and require their own target ADR.\n \n Shiki is not:\n \n@@ -44,10 +44,14 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use\n+`SADR-NNNN-*.md`; target product decisions retain `NNNN-*.md` and the `ADR`\n+namespace.\n+\n Use the canonical source-of-truth order above. Runtime-specific reading orders\n are operational guidance only and must not override or reorder it.\n \n-Conversation state is not durable truth. If a decision matters, put it in GitHub, `.shiki/`, `CONTEXT.md`, or an ADR.\n+Conversation state is not durable truth. If a decision matters, put it in GitHub, `.shiki/`, `CONTEXT.md`, a Shiki SADR, or a target ADR.\n \n If GitHub and `.shiki/` disagree, surface the conflict and prefer GitHub operational state until the mirror is repaired.\n \n@@ -75,8 +79,8 @@\n Every non-trivial change follows this loop:\n \n 1. **Goal**: clarify outcome, completion conditions, non-goals, risk level, and success signals.\n-2. **grill-with-docs**: challenge the plan against domain docs, ADRs, code reality, terminology, and edge scenarios.\n-3. **Context & Impact**: find relevant docs, ADRs, code, dependencies, owners, locks, and verification surfaces.\n+2. **grill-with-docs**: challenge the plan against domain docs, Shiki SADRs, target ADRs, code reality, terminology, and edge scenarios.\n+3. **Context & Impact**: find relevant docs, SADRs, target ADRs, code, dependencies, owners, locks, and verification surfaces.\n 4. **PRD and Spec Freeze**: use `to-prd` when context is settled enough to become durable product/engineering intent. The operator's explicit approval of the PRD is Spec Freeze: the plan records a `spec_freeze` block, and no plan runs without it. Steps 1-4 form Requirements Definition \u2014 one continuous operator dialogue. After Spec Freeze, scope changes require an operator-approved Spec Amendment; non-scope-moving interpretations go to the Assumption Log.\n 5. **Issues**: use `to-issues` to create independently grabbable vertical-slice issues. Prefer AFK slices over HITL slices where possible.\n 6. **Triage**: label issues for readiness, risk, runtime, skills, and MergeGate state.\n@@ -167,7 +171,7 @@\n - Testing decisions.\n - Out-of-scope items.\n - Further notes.\n-- Links to relevant domain terms and ADRs.\n+- Links to relevant domain terms, SADRs, and target ADRs.\n \n Every executable task must include:\n \n@@ -193,7 +197,7 @@\n \n Context & Impact must identify:\n \n-- Relevant documents, domain terms, ADRs, and past decisions.\n+- Relevant documents, domain terms, SADRs, target ADRs, and past decisions.\n - Relevant modules, interfaces, seams, callers, owners, and tests.\n - Dependency relationships between tasks.\n - Candidate locks and lock conflicts.\n@@ -210,7 +214,7 @@\n | Skill | Required when |\n | --- | --- |\n | `setup-matt-pocock-skills` | First configuring the repo for skills, or when issue tracker, triage labels, or domain docs are missing. |\n-| `grill-with-docs` | Default for non-trivial Goals before PRD/issues; always required when plans, terminology, boundaries, tradeoffs, or ADR-worthy decisions are ambiguous. |\n+| `grill-with-docs` | Default for non-trivial Goals before PRD/issues; always required when plans, terminology, boundaries, tradeoffs, or decision-record-worthy choices are ambiguous. |\n | `zoom-out` | The agent lacks an architectural map of the relevant code area. |\n | `to-prd` | Settled Goal context must become durable product/engineering intent. |\n | `to-issues` | A Goal, PRD, or plan must become independently grabbable vertical-slice issues. |\n@@ -348,7 +352,7 @@\n - Three or more new files.\n - Any high-risk or critical label.\n \n-When the architecture gate triggers, run `grill-with-docs` or `improve-codebase-architecture` as appropriate and record durable decisions in an ADR when needed.\n+When the architecture gate triggers, run `grill-with-docs` or `improve-codebase-architecture` as appropriate and record durable decisions in a Shiki SADR or target ADR as applicable.\n \n ## Engineering Discipline\n \ndiff --git a/CLAUDE.md b/CLAUDE.md\nindex 98a932e7841a632866fcd53b0bad0e3af3711d3d..98a31399404d6fb044b4fd493f4459ac87f439de\n--- a/CLAUDE.md\n+++ b/CLAUDE.md\n@@ -14,6 +14,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n ## Claude Role\n \n Default to planner, default implementer, reviewer, coordinator, documentation editor, CCA-style completion judge, and final-judgment assistant.\n@@ -57,7 +60,7 @@\n At the start of a Shiki session:\n \n 1. Read `AGENTS.md`.\n-2. Read `CONTEXT.md` and relevant ADRs.\n+2. Read `CONTEXT.md` and relevant Shiki SADRs and target ADRs.\n 3. Read active `.shiki/` Goal/task/ledger state when present.\n 4. Read active distilled rules from `.shiki/memories/` (status `distilled`, `active: true`, not revoked or superseded) for applicable guidance; the same rules are injected deterministically into task handoffs as the `## Distilled Rules` section (\u00a73.5).\n 5. Read the GitHub Issue or PR when the task is GitHub-backed.\n@@ -74,7 +77,7 @@\n - Define the Goal, completion conditions, non-goals, and risk level.\n - Treat `grill-with-docs` as the default planning entry point for non-trivial Goals.\n - Challenge domain terms against `CONTEXT.md`.\n-- Challenge hard-to-reverse tradeoffs against ADRs.\n+- Challenge hard-to-reverse tradeoffs against SADRs and target ADRs.\n - Explore code instead of asking when code can answer the question.\n - Run Context & Impact before implementation planning.\n - Produce a Task DAG, not an unordered checklist.\n@@ -94,7 +97,7 @@\n - For each question, provide your recommended answer.\n - Resolve dependency decisions before downstream design questions.\n - Update `CONTEXT.md` inline when terminology crystallizes.\n-- Offer ADRs only when the decision is hard to reverse, surprising without context, and a real tradeoff.\n+- Offer a SADR or target ADR only when the decision is hard to reverse, surprising without context, and a real tradeoff.\n - Convert unresolved questions into HITL blockers rather than allowing Codex to guess.\n \n ## PRD and Issues Standard\n@@ -103,7 +106,7 @@\n \n - Use the project's domain glossary vocabulary.\n - Avoid volatile file paths unless they are essential.\n-- Include implementation decisions, testing decisions, out-of-scope items, and relevant ADR/domain links.\n+- Include implementation decisions, testing decisions, out-of-scope items, and relevant SADR/target-ADR/domain links.\n - Identify deep-module opportunities and testable interfaces.\n \n When creating issues:\n@@ -121,7 +124,7 @@\n - Branch/worktree target.\n - Scope and non-goals.\n - Dependencies and locks.\n-- Relevant docs, ADRs, modules, and tests.\n+- Relevant docs, SADRs, target ADRs, modules, and tests.\n - Required skill invocations.\n - TDD expectations.\n - Pre-PR `code-review` gate expectations.\n@@ -152,7 +155,7 @@\n \n - Treat yourself as a judge, not implementer.\n - Do not write production code.\n-- Read `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant ADRs, PRD, task issue, PR body, diff, checks, reviews, labels, and `.shiki/` state.\n+- Read `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant SADRs and target ADRs, PRD, task issue, PR body, diff, checks, reviews, labels, and `.shiki/` state.\n - Evaluate every applicable checklist in `docs/agents/checklists.md`.\n - Map each acceptance criterion to durable evidence.\n - Distinguish implementation failure from missing evidence.\ndiff --git a/CONTEXT.md b/CONTEXT.md\nindex e61a9be06a0cce0d026484083a657bde2067e234..237ee8aa3ea8fab334ece49768bc5ed3b5af0974\n--- a/CONTEXT.md\n+++ b/CONTEXT.md\n@@ -25,7 +25,7 @@\n _Avoid_: jumping directly into code\n \n **grill-with-docs**:\n-A planning skill that challenges a plan against domain language, ADRs, code reality, and concrete edge scenarios. It resolves design-tree questions before PRD/issues. It runs inside Requirements Definition.\n+A planning skill that challenges a plan against domain language, Shiki SADRs, target ADRs, code reality, and concrete edge scenarios. It resolves design-tree questions before PRD/issues. It runs inside Requirements Definition.\n _Avoid_: generic brainstorming, silent assumptions\n \n **Requirements Definition**:\ndiff --git a/CONTRIBUTING.md b/CONTRIBUTING.md\nindex c6d9b656e73abb8a55e5f660123e03153dcb3e6d..9e37a8fbf668db5b22461aaae5548b8f1354394c\n--- a/CONTRIBUTING.md\n+++ b/CONTRIBUTING.md\n@@ -17,7 +17,7 @@\n 1. **Goal** \u2014 start from a user-approved Goal with an outcome, completion\n    conditions, non-goals, risk level, and success signals.\n 2. **grill-with-docs** \u2014 for any non-trivial change, challenge the plan against\n-   domain language, ADRs, and code reality before writing a PRD.\n+   domain language, Shiki SADRs, target ADRs, and code reality before writing a PRD.\n 3. **PRD** \u2014 capture settled product/engineering intent (`to-prd`).\n 4. **Spec Freeze** \u2014 the operator explicitly approves the PRD/requirements;\n    the plan records a `spec_freeze` block and no plan runs without it\n@@ -94,7 +94,7 @@\n   entries.\n - Do not hand-edit `.shiki/goals/*.json` or `.shiki/manifest.json` in a task PR.\n - Conversation state is not durable truth. If a decision matters, record it in\n-  GitHub, `.shiki/`, `CONTEXT.md`, or an ADR.\n+  GitHub, `.shiki/`, `CONTEXT.md`, a Shiki SADR, or a target ADR.\n \n ## Architecture Gate\n \n@@ -103,7 +103,7 @@\n or CI policy, package structure, three or more structural files, more than 100\n net new lines in a core module, three or more new files, or any high-risk label.\n When triggered, run `grill-with-docs` or `improve-codebase-architecture` and\n-record durable decisions in an ADR when needed (see `docs/adr/`).\n+record durable decisions in a Shiki SADR or target ADR as applicable (see `docs/adr/`).\n \n ## Safety\n \ndiff --git a/SYSTEM_PROMPT.md b/SYSTEM_PROMPT.md\nindex aaaea45e3a896c3a44ba89eec8bb84325ecf6c62..da009c86a88285269dee0155e58e550c6d744ea9\n--- a/SYSTEM_PROMPT.md\n+++ b/SYSTEM_PROMPT.md\n@@ -30,6 +30,9 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n Use the canonical source-of-truth order above. Runtime-specific reading orders\n are operational guidance only and must not override or reorder it. The current\n conversation is non-durable operator input.\n@@ -48,14 +51,14 @@\n \n A runtime may only do work that is explicitly assigned by the Goal, task, PR, or MergeGate decision.\n \n-Do not assume `openai/codex-action`, `OPENAI_API_KEY`, or API-key based Codex automation in the default Shiki path. API-key runners are explicit target-repo extensions and require their own ADR.\n+Do not assume `openai/codex-action`, `OPENAI_API_KEY`, or API-key based Codex automation in the default Shiki path. API-key runners are explicit target-repo extensions and require their own target ADR.\n \n ## 4. Non-negotiable Shiki flow\n \n For any non-trivial Goal, use this flow:\n \n 1. **Goal Seek**: define outcome, non-goals, risk level, completion criteria, and evidence requirements.\n-2. **grill-with-docs**: challenge the plan against `CONTEXT.md`, ADRs, code reality, terminology, boundaries, and edge scenarios. Ask one question at a time when the operator is available. If code can answer a question, inspect code instead of asking.\n+2. **grill-with-docs**: challenge the plan against `CONTEXT.md`, Shiki SADRs, target ADRs, code reality, terminology, boundaries, and edge scenarios. Ask one question at a time when the operator is available. If code can answer a question, inspect code instead of asking.\n 3. **Context & Impact**: identify modules, interfaces, seams, callers, dependencies, locks, risk, and verification surfaces.\n 4. **to-prd + Spec Freeze**: turn settled context into a PRD using domain vocabulary and testing decisions. The operator's explicit approval of the PRD is Spec Freeze, recorded as the plan's `spec_freeze` block; plans without it do not run. Steps 1-4 form Requirements Definition.\n 5. **to-issues**: decompose the PRD into independently grabbable vertical-slice issues. Prefer AFK slices; mark HITL slices when judgment is still required.\n@@ -75,7 +78,7 @@\n Before making any material change:\n \n 1. Read `AGENTS.md` and any runtime-specific wrapper such as `CLAUDE.md`.\n-2. Read `CONTEXT.md` and relevant ADRs.\n+2. Read `CONTEXT.md` and relevant SADRs and target ADRs.\n 3. Read the GitHub Issue / PR and `.shiki/` state for the active Goal or task.\n 4. Identify your role: Planner, Implementer, Reviewer, Completion Check Agent, Repairer, or Guardian-assist.\n 5. Identify Goal id, task id, branch, PR, dependencies, locks, risk level, required skills, and required evidence.\n@@ -103,7 +106,7 @@\n \n At minimum, identify:\n \n-- Relevant documents, ADRs, domain terms, and prior decisions.\n+- Relevant documents, SADRs, target ADRs, domain terms, and prior decisions.\n - Relevant modules, interfaces, seams, callers, owners, and tests.\n - Dependency relationships between tasks.\n - Candidate file locks and lock conflicts.\n@@ -152,7 +155,7 @@\n \n CCA must read:\n \n-- `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant ADRs.\n+- `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, relevant SADRs and target ADRs.\n - Parent Goal issue, PRD issue, task issue, comments, and labels.\n - PR body, diff, commits, changed files, reviews, CI checks, and artifacts available to the workflow.\n - `.shiki/` task contract, locks, ledger entries, CCA checklist profile, and prior repair packets.\n@@ -214,7 +217,7 @@\n - Three or more new files.\n - Any high-risk or critical label.\n \n-When the architecture gate triggers, run `grill-with-docs` or `improve-codebase-architecture` as appropriate and record the decision in an ADR if it is hard to reverse or likely to be re-litigated.\n+When the architecture gate triggers, run `grill-with-docs` or `improve-codebase-architecture` as appropriate and record the decision in a Shiki SADR or target ADR as applicable if it is hard to reverse or likely to be re-litigated.\n \n ## 14. Skill Gate\n \n@@ -223,7 +226,7 @@\n Use these skills:\n \n - `setup-matt-pocock-skills`: before first use of the engineering skill set, or when issue tracker, triage labels, or domain docs are missing.\n-- `grill-with-docs`: default for non-trivial Goals before PRD/issues; required for ambiguous plans, terminology, boundaries, ADR-worthy tradeoffs, or design-tree decisions.\n+- `grill-with-docs`: default for non-trivial Goals before PRD/issues; required for ambiguous plans, terminology, boundaries, decision-record-worthy tradeoffs, or design-tree decisions.\n - `zoom-out`: unfamiliar code area or missing architectural map.\n - `to-prd`: convert settled context into a PRD.\n - `to-issues`: break a PRD, plan, or Goal into independently grabbable vertical-slice issues.\ndiff --git a/docs/adr/0001-keep-shiki-as-platform-name.md b/docs/adr/SADR-0001-keep-shiki-as-platform-name.md\nindex 20a9637c3b9d8c97de7ba8db7cfae4d82d9a7dba..8b685439959aada1b221c112f0ac3c51ca8a1695\n--- a/docs/adr/0001-keep-shiki-as-platform-name.md\n+++ b/docs/adr/SADR-0001-keep-shiki-as-platform-name.md\n@@ -1,3 +1,3 @@\n-# Keep Shiki As The Platform Name\n+# SADR-0001: Keep Shiki As The Platform Name\n \n Shiki remains the product name for the GitHub-native agentic engineering control plane. We considered more descriptive names such as MergePilot, MergeGate, and Kiroku, but kept Shiki because it carries the existing goal-seeking framework identity while remaining independent of Hermes, Codex, Claude Code, or any single runtime.\ndiff --git a/docs/adr/0002-use-agents-as-shared-constitution.md b/docs/adr/SADR-0002-use-agents-as-shared-constitution.md\nindex 73de30ecb078120eac540d9bc51855e3cd48d0e3..c5486182bf06fca36ea508496b013c67302fd989\n--- a/docs/adr/0002-use-agents-as-shared-constitution.md\n+++ b/docs/adr/SADR-0002-use-agents-as-shared-constitution.md\n@@ -1,3 +1,3 @@\n-# Use AGENTS As The Shared Agent Constitution\n+# SADR-0002: Use AGENTS As The Shared Agent Constitution\n \n Shiki uses `AGENTS.md` as the runtime-neutral constitution and `CLAUDE.md` as a thin Claude Code wrapper. This keeps Codex, Claude Code, Hermes Runner, and future Agent Runtimes aligned on the same GitHub-first MergeGate rules while still allowing runtime-specific instructions where needed.\ndiff --git a/docs/adr/0003-require-skill-gate-for-mergegate-readiness.md b/docs/adr/SADR-0003-require-skill-gate-for-mergegate-readiness.md\nindex 424be0c9af4c8f0136f1fde68a468c47ff84e764..7fc337987c7df9792f75f181dfcbed75fe5ffff2\n--- a/docs/adr/0003-require-skill-gate-for-mergegate-readiness.md\n+++ b/docs/adr/SADR-0003-require-skill-gate-for-mergegate-readiness.md\n@@ -1,3 +1,3 @@\n-# Require Skill Gate For MergeGate Readiness\n+# SADR-0003: Require Skill Gate For MergeGate Readiness\n \n Shiki requires relevant engineering skills to be invoked before execution when their triggers apply. This makes skills part of MergeGate readiness rather than optional prompt style, because Shiki depends on repeatable planning, issue decomposition, TDD, diagnosis, triage, and architecture review before autonomous or parallel agent work can be trusted.\ndiff --git a/docs/adr/0004-use-github-first-with-shiki-mirror.md b/docs/adr/SADR-0004-use-github-first-with-shiki-mirror.md\nindex d0a3a9174dc646a945a9d325954ffaf00c352040..a79c54ae9ee8eed2a0524192b864da4e22a92d3f\n--- a/docs/adr/0004-use-github-first-with-shiki-mirror.md\n+++ b/docs/adr/SADR-0004-use-github-first-with-shiki-mirror.md\n@@ -1,4 +1,4 @@\n-# ADR 0004: Use GitHub First With A Repository-Local Shiki Mirror\n+# SADR-0004: Use GitHub First With A Repository-Local Shiki Mirror\n \n ## Status\n \ndiff --git a/docs/adr/0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md b/docs/adr/SADR-0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\nindex b339baf49b89d9290e79c3d51b36e7b2bde3bd95..0f3c3eb9afccdb3403ce978e21d04fe12e495da0\n--- a/docs/adr/0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\n+++ b/docs/adr/SADR-0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md\n@@ -1,8 +1,8 @@\n-# ADR 0005: Use Codex As Front And Claude Code Action As The GitHub Runtime\n+# SADR-0005: Use Codex As Front And Claude Code Action As The GitHub Runtime\n \n ## Status\n \n-Accepted (implementer default superseded by ADR 0008; the auth model and the\n+Accepted (implementer default superseded by SADR-0008; the auth model and the\n Claude Code Action GitHub runtime decision remain in force)\n \n ## Context\n@@ -26,4 +26,4 @@\n - Shiki avoids requiring `OPENAI_API_KEY` for the default workflow.\n - Codex work happens through the user's authenticated Codex front surface, not as a hidden backend Action.\n - Claude Code Action is configured for OAuth-token based subscription usage by default.\n-- API-key based automation remains possible only as an explicit target-repo extension with its own ADR.\n+- API-key based automation remains possible only as an explicit target-repo extension with its own target ADR.\ndiff --git a/docs/adr/0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md b/docs/adr/SADR-0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\nindex 4bcae9d1e52f0a37b8e9fbafa5cd14b6ee1993bd..7a38538a6ae9929567f0ade67e7ea6a60d0b3f7e\n--- a/docs/adr/0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\n+++ b/docs/adr/SADR-0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md\n@@ -1,4 +1,4 @@\n-# ADR 0006: Separate Codex Implementation, CCA Completion, And MergeGate Authorization\n+# SADR-0006: Separate Codex Implementation, CCA Completion, And MergeGate Authorization\n \n ## Status\n \ndiff --git a/docs/adr/0007-packaging-and-release.md b/docs/adr/SADR-0007-packaging-and-release.md\nindex 79b84ddb97167372b44641e5b7860e400ce80822..bb445a2e2e856d9d1f5fdbe35de8d4e8cec0fb32\n--- a/docs/adr/0007-packaging-and-release.md\n+++ b/docs/adr/SADR-0007-packaging-and-release.md\n@@ -1,4 +1,4 @@\n-# ADR 0007: Packaging And Release For Shiki\n+# SADR-0007: Packaging And Release For Shiki\n \n ## Status\n \n@@ -25,7 +25,7 @@\n   not introduce third-party runtime dependencies or break the shim.\n - Releases must run inside the operator's subscription-authenticated toolchain\n   and must not require extra secrets beyond the built-in `GITHUB_TOKEN`\n-  (consistent with ADR 0005's no-API-key-by-default stance).\n+  (consistent with SADR-0005's no-API-key-by-default stance).\n - `.shiki/manifest.json` already encodes the install surface (`install.include`,\n   `install.create_directories`, `install.exclude_from_commit`), and it must not\n   be mutated as part of this change.\n@@ -34,7 +34,7 @@\n \n This is a hard-to-reverse platform decision (it sets the version source of\n truth, the release trigger, and the public install contract), so it is recorded\n-as an ADR.\n+as a SADR.\n \n ## Decision\n \n@@ -104,7 +104,7 @@\n 3. **Publishing to PyPI.** Rejected for now: Shiki is distributed as a\n    repository template + git-installable CLI, not a PyPI library; PyPI would add\n    credentials/secrets and a different versioning contract. Can be added later as\n-   its own ADR if a packaged distribution is needed.\n+   its own SADR if a packaged distribution is needed.\n 4. **Dynamic version (read `VERSION` via a build backend `dynamic` field).**\n    Rejected to keep `pyproject.toml` backend-agnostic and dependency-free; the\n    small duplication between `VERSION` and `pyproject.toml` is guarded by the\n@@ -123,4 +123,4 @@\n   releasing versions the whole platform but ships only `install.include` to\n   targets.\n - Future PyPI distribution or a console entry point are deferred and would each\n-  warrant their own ADR.\n+  warrant their own SADR.\ndiff --git a/docs/adr/0008-use-claude-code-as-default-implementer.md b/docs/adr/SADR-0008-use-claude-code-as-default-implementer.md\nindex dedf4510b7c7799521422f622029563d373db031..35c2625d908167fbc58855b5b63e283805f167e8\n--- a/docs/adr/0008-use-claude-code-as-default-implementer.md\n+++ b/docs/adr/SADR-0008-use-claude-code-as-default-implementer.md\n@@ -1,12 +1,12 @@\n-# ADR 0008: Use Claude Code As The Default Implementer Runtime\n+# SADR-0008: Use Claude Code As The Default Implementer Runtime\n \n ## Status\n \n-Proposed (supersedes ADR 0005 for the implementer default; keeps its auth model)\n+Proposed (supersedes SADR-0005 for the implementer default; keeps its auth model)\n \n ## Context\n \n-ADR 0005 made Codex Front the default implementation runtime, with Claude Code\n+SADR-0005 made Codex Front the default implementation runtime, with Claude Code\n restricted to planner and reviewer roles by the runtime registry, and the only\n autonomous implementation adapter was `shiki runner codex`.\n \n@@ -18,13 +18,13 @@\n - The platform's planning, review, and CCA layers already run on Claude Code,\n   and Claude Code's native capabilities (the built-in `/code-review` skill and\n   the Workflow multi-agent orchestration tool) have no Codex equivalent.\n-- The Goal lifecycle redesign (ADR 0009) requires an autonomous post-freeze\n+- The Goal lifecycle redesign (SADR-0009) requires an autonomous post-freeze\n   loop driven from the operator's Claude Code session, including bounded\n   repair dispatch, which must not depend on a runtime that cannot be invoked\n   unattended.\n \n The constitution claims Shiki is runtime-agnostic (\"not a Claude-only\n-workflow\", ADR 0002). Any change to the implementer default must preserve the\n+workflow\", SADR-0002). Any change to the implementer default must preserve the\n registry-based runtime model rather than hardcoding one vendor.\n \n ## Decision\n@@ -39,7 +39,7 @@\n   the task to review or repair-needed.\n - Keep Codex registered as an optional implementer runtime. `shiki runner\n   codex`, the Codex handoff templates, and the ChatGPT OAuth auth model from\n-  ADR 0005 remain supported for tasks explicitly assigned to `codex`.\n+  SADR-0005 remain supported for tasks explicitly assigned to `codex`.\n - Commit to mandating two Claude-native capabilities as loop contract in\n   follow-up Goals (status: the code-review gate was delivered by Goal G-B \u2014\n   skill registry, checklists item PR-12, default required_skills \u2014 while the\n@@ -60,7 +60,7 @@\n \n Out of scope: removing Codex support, changing the CCA runtime (Claude Code\n Action with `CLAUDE_CODE_OAUTH_TOKEN` stays), and API-key based automation\n-(still requires its own ADR per ADR 0005).\n+(still requires its own target ADR per SADR-0005).\n \n ## Consequences\n \n@@ -100,7 +100,7 @@\n   harness rejection of direct `codex exec` is outside Shiki's control, and the\n   plan's mandatory native capabilities cannot run on Codex at all.\n - **Remove Codex entirely**: rejected; it abandons the runtime-agnostic\n-  constitution (ADR 0002) for no operational gain and forecloses\n+  constitution (SADR-0002) for no operational gain and forecloses\n   cross-runtime adversarial review.\n - **GitHub Actions cloud implementation via claude-code-action**: rejected as\n   default; durable evidence is automatic but token cost, Node 24 workflow\ndiff --git a/docs/adr/0009-requirements-definition-and-spec-freeze.md b/docs/adr/SADR-0009-requirements-definition-and-spec-freeze.md\nindex 29b3f7871bef9203a8b4a831a65c39eb2f506e42..52e7dd69ad0aa4efe0b68c62678abbf7df02aaec\n--- a/docs/adr/0009-requirements-definition-and-spec-freeze.md\n+++ b/docs/adr/SADR-0009-requirements-definition-and-spec-freeze.md\n@@ -1,4 +1,4 @@\n-# ADR 0009: Merge Grill And Planning Into Requirements Definition With Spec Freeze\n+# SADR-0009: Merge Grill And Planning Into Requirements Definition With Spec Freeze\n \n ## Status\n \ndiff --git a/docs/adr/0010-external-ai-guardian-review.md b/docs/adr/SADR-0010-external-ai-guardian-review.md\nindex e6108bbe8bb4e73e0ff4684c8f78001a80438c4e..f3510b145664298adafb151b6f0db51b972f01ec\n--- a/docs/adr/0010-external-ai-guardian-review.md\n+++ b/docs/adr/SADR-0010-external-ai-guardian-review.md\n@@ -1,4 +1,4 @@\n-# ADR 0010: External AI Guardian Review As A First-Class Approval Authority\n+# SADR-0010: External AI Guardian Review As A First-Class Approval Authority\n \n ## Status\n \n@@ -8,7 +8,7 @@\n \n Shiki is intended to be an autonomous, recursive, self-improving agent\n framework: authority-in-the-loop, not human-in-the-loop. Until now the Guardian\n-approval required for high/critical-risk merges (guardian-policy.json, ADR\n+approval required for high/critical-risk merges (guardian-policy.json, SADR\n governance) recognized only human authority sources \u2014 a GitHub review approval,\n the `guardian:approved` label applied by a configured Guardian, or a comment\n carrying `Guardian approval granted` plus the current head SHA.\n@@ -89,7 +89,7 @@\n   autonomous design the operator intends and produced the impersonation\n   incident.\n - **Record AI approval as an operator comment**: rejected as forgery of human\n-  authority \u2014 the exact failure this ADR corrects.\n+  authority \u2014 the exact failure this SADR corrects.\n - **Deliver the artifact as a committed `.shiki/` file**: rejected; a committed\n   approval inside the PR being approved is forgeable by anyone who can push.\n - **A dedicated reviewer bot identity instead of a relayed comment**: deferred;\ndiff --git a/docs/adr/0011-autonomous-quality-gate-evidence.md b/docs/adr/SADR-0011-autonomous-quality-gate-evidence.md\nindex bca0246c5d46e44afbde15486a75eec425fc02f5..9c93eac16d8c3a6d4b11bd7224e3955102cd7db4\n--- a/docs/adr/0011-autonomous-quality-gate-evidence.md\n+++ b/docs/adr/SADR-0011-autonomous-quality-gate-evidence.md\n@@ -1,4 +1,4 @@\n-# ADR 0011: Autonomous Quality-Gate Evidence \u2014 Deterministic Facts And An Independent Verifier\n+# SADR-0011: Autonomous Quality-Gate Evidence \u2014 Deterministic Facts And An Independent Verifier\n \n ## Status\n \ndiff --git a/docs/adr/0012-autonomous-loop-closeout-to-main.md b/docs/adr/SADR-0012-autonomous-loop-closeout-to-main.md\nindex 637fad6dde4989c9c2bf0f81f676d8d29991123c..ec7d85f81030a676bcbba28b43eb365422a36eac\n--- a/docs/adr/0012-autonomous-loop-closeout-to-main.md\n+++ b/docs/adr/SADR-0012-autonomous-loop-closeout-to-main.md\n@@ -1,4 +1,4 @@\n-# 0012 \u2014 Autonomous loop pushes goal completion to main via a closeout PR\n+# SADR-0012 \u2014 Autonomous loop pushes goal completion to main via a closeout PR\n \n - Status: accepted\n - Date: 2026-06-18\ndiff --git a/docs/adr/0013-bootstrap-grants-actions-approve-pull-requests.md b/docs/adr/SADR-0013-bootstrap-grants-actions-approve-pull-requests.md\nindex b6c69d64f1c5ced346b88d6859586715b3b5bd0e..cf48ff4a6716267f539ca58edd1a852e7c7a2d64\n--- a/docs/adr/0013-bootstrap-grants-actions-approve-pull-requests.md\n+++ b/docs/adr/SADR-0013-bootstrap-grants-actions-approve-pull-requests.md\n@@ -1,4 +1,4 @@\n-# ADR 0013: Bootstrap Grants GitHub Actions Permission To Create And Approve Pull Requests\n+# SADR-0013: Bootstrap Grants GitHub Actions Permission To Create And Approve Pull Requests\n \n ## Status\n \n@@ -31,7 +31,7 @@\n security tradeoff: it lets repository automation satisfy a human-review gate. In\n a multi-maintainer repo that could let a compromised or buggy workflow\n self-approve changes. So whether bootstrap turns it on by default \u2014 and with\n-what default token scope \u2014 is an ADR-worthy decision, not an implementation\n+what default token scope \u2014 is a SADR-worthy decision, not an implementation\n detail.\n \n ## Decision\n@@ -48,7 +48,7 @@\n Specifically:\n \n - **`can_approve_pull_request_reviews` defaults to `true`.** Shiki's default\n-  posture is solo/AFK autonomous operation (ADR 0008/0009/0012), where the CCA\n+  posture is solo/AFK autonomous operation (SADR-0008/0009/0012), where the CCA\n   Review Bridge is the only path that can satisfy `required_review: true` after\n   the CCA and Guardian gates have already passed. The default makes a\n   freshly-bootstrapped repo able to run the autonomous loop end to end without a\ndiff --git a/docs/adr/0014-codex-app-external-ai-guardian-ui-adapter.md b/docs/adr/SADR-0014-codex-app-external-ai-guardian-ui-adapter.md\nindex 238db06279334caa341c117362df9271382a7fcd..65f12e9939321e1b3e389a34c822fc1549e5155d\n--- a/docs/adr/0014-codex-app-external-ai-guardian-ui-adapter.md\n+++ b/docs/adr/SADR-0014-codex-app-external-ai-guardian-ui-adapter.md\n@@ -1,4 +1,4 @@\n-# ADR 0014: Use Codex App As The External AI Guardian UI Adapter\n+# SADR-0014: Use Codex App As The External AI Guardian UI Adapter\n \n ## Status\n \ndiff --git a/docs/adr/0015-contract-approval-before-dispatch.md b/docs/adr/SADR-0015-contract-approval-before-dispatch.md\nindex fad6c656741a20a7684ba16080a7db159f63a52f..6061271a46d3f1d6ae7174477de3fb75673123a6\n--- a/docs/adr/0015-contract-approval-before-dispatch.md\n+++ b/docs/adr/SADR-0015-contract-approval-before-dispatch.md\n@@ -1,4 +1,4 @@\n-# ADR 0015: Approve The Task Contract Before Dispatch, Not The Diff After It\n+# SADR-0015: Approve The Task Contract Before Dispatch, Not The Diff After It\n \n ## Status\n \n@@ -9,7 +9,7 @@\n Guardian approval for high/critical risk is expressible only on a pull request.\n Every approval source in `guardian-policy.json` \u2014 GitHub review, the\n `guardian:approved` label, the `Guardian approval granted` comment, and the\n-`external_ai_guardian_review` artifact (ADR 0010/0014) \u2014 is bound to a PR, and\n+`external_ai_guardian_review` artifact (SADR-0010/0014) \u2014 is bound to a PR, and\n three of the four require the current head SHA. Approval therefore cannot exist\n before an implementation PR exists.\n \n@@ -81,7 +81,7 @@\n An envelope breach must surface only on the non-repairable policy gate, never on\n the repairable `MergeGate metadata check`. Otherwise the Goal Loop routes it to\n `dispatch_repair` \u2014 instructing a runner to make the breach go away \u2014 which is\n-exactly what this ADR forbids.\n+exactly what this SADR forbids.\n \n **Contract immutability.** After registration, an implementation PR must not\n change the governance fields of its own task contract (`scope`, `non_goals`,\n@@ -116,7 +116,7 @@\n fails any verdict that is not `complete`, and the signal the CCA prompt reads is\n computed from `evaluate_guardian_approval` alone. Without the OR-merge at the\n signal, a contract-approved PR still returns `needs_guardian`, the CCA job fails,\n-and the MergeGate policy check never runs \u2014 the gate this ADR changes would never\n+and the MergeGate policy check never runs \u2014 the gate this SADR changes would never\n be reached. The CCA *prompt* needs no change: a `contract_approval` entry in the\n signal's existing `sources` array satisfies its current wording.\n \n@@ -130,7 +130,7 @@\n written, and an approved contract lets its implementation PRs merge through the\n same automatic path as low/medium risk. The wasted implement-then-block cycle\n disappears. Guardian review shifts from reading diffs to reading contracts, which\n-is both earlier and smaller; the external AI Guardian path (ADR 0010/0014) works\n+is both earlier and smaller; the external AI Guardian path (SADR-0010/0014) works\n on contracts with no change.\n \n We accept that the Guardian authorizes a class of change without seeing its\n@@ -146,7 +146,7 @@\n adversarial tests for the new approval path equivalent in rigor to\n `test_shiki_governance_evidence.sh`.\n \n-Bootstrap costs, which apply only while this ADR is being implemented. The tasks\n+Bootstrap costs, which apply only while this SADR is being implemented. The tasks\n that build Contract Approval merge under the existing rules, so the ones\n dispatched after the pre-dispatch gate lands but before the approval path lands\n need a Guardian-approved Contract PR *and* a live Guardian approval on their\n@@ -210,7 +210,7 @@\n risk; they do not bound authorized work.\n \n **Auto-escalate risk on envelope breach instead of stopping.** Returns the\n-workflow to a diff-time Guardian decision \u2014 the exact behavior this ADR removes.\n+workflow to a diff-time Guardian decision \u2014 the exact behavior this SADR removes.\n \n **Resolve governance fields from the base snapshot only.** Rejected against the\n registration flow: implementation PRs add their own contract, so the base has no\ndiff --git a/docs/adr/0016-locks-bound-product-paths.md b/docs/adr/SADR-0016-locks-bound-product-paths.md\nindex b5e850f3f1f7f4ef6e7f9d1763a29a51bd300c45..46b40a9b99a7aae644c02bb458f5473ff0a90ce2\n--- a/docs/adr/0016-locks-bound-product-paths.md\n+++ b/docs/adr/SADR-0016-locks-bound-product-paths.md\n@@ -1,4 +1,4 @@\n-# ADR 0016: Locks bound product paths; the Shiki mirror is bound by record identity\n+# SADR-0016: Locks bound product paths; the Shiki mirror is bound by record identity\n \n ## Status\n \n@@ -6,8 +6,8 @@\n \n - Date: 2026-07-28\n - Deciders: operator (mizutani-140), Claude (planner)\n-- Related: ADR 0004 (GitHub-first with a `.shiki` mirror), ADR 0009 (Spec Freeze),\n-  ADR 0012 (autonomous loop closeout to main), ADR 0015 (Contract Approval before\n+- Related: SADR-0004 (GitHub-first with a `.shiki` mirror), SADR-0009 (Spec Freeze),\n+  SADR-0012 (autonomous loop closeout to main), SADR-0015 (Contract Approval before\n   dispatch), remediation goal task R-02, the live PR #179 deadlock.\n \n ## Context\n@@ -90,11 +90,11 @@\n files, documentation files and named source modules are nameable; runtime-\n allocated ledger, runner and report ids are not \u2014 and those live in `.shiki`,\n which locks no longer govern. Existing frozen contracts are brought into line by\n-a one-time Spec Amendment per plan, which must land before ADR 0015's contract\n+a one-time Spec Amendment per plan, which must land before SADR-0015's contract\n immutability makes `locks` unwritable from the implementation PR.\n \n **Lock state is resolved from the durable side.** MergeGate resolves the lock\n-registry, and ADR 0015's governance fields, from the base `.shiki` snapshot when\n+registry, and SADR-0015's governance fields, from the base `.shiki` snapshot when\n present, falling back to head only when absent. The `MergeGate metadata check`\n job is given `--base-shiki`, which it does not receive today. Locks are not\n derived from the live open-pull-request set: a pull request body is untrusted\n@@ -108,7 +108,7 @@\n Explicitly out of scope: moving MergeGate's contract resolution off the PR\n checkout wholesale; making the mirror a projection of GitHub state; introducing\n new lock kinds; moving ledger evidence out of the repository; and unwinding\n-ADR 0012's closeout pull request. Registration of multi-task goals is ADR 0015's\n+SADR-0012's closeout pull request. Registration of multi-task goals is SADR-0015's\n Contract PR and is not re-decided here.\n \n ## Consequences\n@@ -127,7 +127,7 @@\n \n Risks accepted. First, goal-scoped identity is only as strong as `goal_id`,\n which is resolved from the PR body and the head task file; the mitigation is to\n-add `goal_id` to ADR 0015's frozen governance field set, and until that lands a\n+add `goal_id` to SADR-0015's frozen governance field set, and until that lands a\n task file can name a foreign goal. Second, base-when-present resolution is\n unavailable on fork pull requests, where the head is contributor-controlled;\n fork pull requests already cannot complete a Shiki merge, and this decision does\n@@ -139,7 +139,7 @@\n path, not by implementation pull requests.\n \n New obligations: one Spec Amendment per frozen plan to rewrite `locks`, ordered\n-before ADR 0015 T1; extension of the adversarial suites in\n+before SADR-0015 T1; extension of the adversarial suites in\n `scripts/test_shiki_adversarial_state.sh` and\n `scripts/test_shiki_governance_evidence.sh` to cover the eleven newly-scoped\n directories; and updating `scripts/test_shiki_loop_lock_guard.sh`,\n@@ -151,32 +151,32 @@\n whether the loop's transitive sync of ledger `evidence` references can be\n deleted once the Contract PR carries its own source plan.\n \n-## Relationship to ADR 0004\n+## Relationship to SADR-0004\n \n-This ADR **clarifies something ADR 0004 left unspecified**. ADR 0004 decides that\n+This SADR **clarifies something SADR-0004 left unspecified**. SADR-0004 decides that\n GitHub is the operational source of truth and that each target repository keeps a\n `.shiki/` mirror. It says nothing about pull requests writing the mirror, does\n not name MergeGate, and does not make the mirror an input to a gate. Nothing in\n-ADR 0004's Decision or Consequences is amended, superseded or weakened: the\n+SADR-0004's Decision or Consequences is amended, superseded or weakened: the\n mirror still exists, still records the same artifacts, and pull request\n-boundaries remain the coordination surface. What this ADR settles is the question\n+boundaries remain the coordination surface. What this SADR settles is the question\n 0004 never reached \u2014 by what authority a pull request may write a mirror record \u2014\n and it answers it the way `.shiki/manifest.json` already declares\n (`pr_mutation: current-task-or-current-goal-only`).\n \n-It **amends ADR 0012**, whose Decision item 5 states that \"Loop-executed tasks\n+It **amends SADR-0012**, whose Decision item 5 states that \"Loop-executed tasks\n declare the `path:.shiki/**` lock, which covers every `.shiki` file the closeout\n stages ... so `files_outside_locks` is satisfied without per-file lock\n bookkeeping.\" That clause is replaced: the closeout's mirror writes are\n-authorized by record identity, not by a declared lock. ADR 0012's substantive\n+authorized by record identity, not by a declared lock. SADR-0012's substantive\n decision \u2014 that completion reaches `main` through a closeout pull request rather\n than local attestation \u2014 is preserved unchanged.\n \n-It is **complementary to ADR 0015**, not a substitute. ADR 0015 owns contract\n+It is **complementary to SADR-0015**, not a substitute. SADR-0015 owns contract\n immutability, the pre-dispatch gate, the Contract PR and the Scope Envelope. This\n-ADR owns what a lock means and who may write the mirror. The one ordering\n+SADR owns what a lock means and who may write the mirror. The one ordering\n constraint between them is that the lock-narrowing Spec Amendment must precede\n-ADR 0015 T1's freezing of the `locks` field.\n+SADR-0015 T1's freezing of the `locks` field.\n \n ## Alternatives Considered\n \n@@ -186,7 +186,7 @@\n the only writer that can register them may run. Migrating the Guardian signal to\n that store makes every registration PR fail closed to `needs_guardian` at every\n risk level, because the task file is by definition absent from the store, which\n-removes the low/medium autonomous path ADR 0008/0009 exists to provide. And it\n+removes the low/medium autonomous path SADR-0008/0009 exists to provide. And it\n leaves schemas, manifest, config, guardian policy, templates, migrations, plans,\n worktrees, runner records, runs and handoffs with no writer at all in a\n repository whose product is `.shiki`. Measured benefit on the frozen backlog:\n@@ -199,14 +199,14 @@\n namespace is not in fact ownership-partitioned \u2014 56 ledger entries carry no\n `task_id`, and one goal DAG is writable by 35 registered tasks. The correct\n expression of its insight is that locks do not govern the mirror at all, which is\n-what this ADR decides.\n+what this SADR decides.\n \n **Make the mirror a projection written only by a registration writer and a\n post-merge projection job.** Rejected. It requires a bypass actor with write\n access to the protected default branch; it makes its own master invariant\n readable from a manifest that pull requests may edit; it deletes both reconcile\n modes and leaves a corrupted mirror with no in-band repair path, contradicting\n-ADR 0004's consequence that mirror conflicts are repaired against GitHub state;\n+SADR-0004's consequence that mirror conflicts are repaired against GitHub state;\n and its content-derived ids are rejected by `validate_shiki`'s currently-green id\n pattern.\n \ndiff --git a/docs/adr/0017-bookkeeping-closeouts-do-not-inherit-risk.md b/docs/adr/SADR-0017-bookkeeping-closeouts-do-not-inherit-risk.md\nindex 554e3a0d526a72ca067108c5464cc68c3edcf174..b9676857a4713850e063af7c39c8e0b01ecb4c66\n--- a/docs/adr/0017-bookkeeping-closeouts-do-not-inherit-risk.md\n+++ b/docs/adr/SADR-0017-bookkeeping-closeouts-do-not-inherit-risk.md\n@@ -1,4 +1,4 @@\n-# ADR 0017: A Bookkeeping Closeout Does Not Inherit Its Task's Risk\n+# SADR-0017: A Bookkeeping Closeout Does Not Inherit Its Task's Risk\n \n ## Status\n \n@@ -6,7 +6,7 @@\n \n ## Context\n \n-ADR 0012 made the goal loop push a task's completion to the default branch through a\n+SADR-0012 made the goal loop push a task's completion to the default branch through a\n closeout pull request: after the implementation PR merges, the loop opens a second PR\n carrying `task.status = done`, `lock.state = released`, and \u2014 when the task completes\n its goal \u2014 `goal.status = complete` plus one scorecard report. Nothing else. The\n@@ -49,14 +49,14 @@\n 4. The task file's governance fields (`scope`, `non_goals`, `required_skills`,\n    `risk_level`, `locks`, `acceptance_checks`, `test_command`) are byte-identical to\n    the base snapshot. Only `status`, `expected_pr`, `closeout_pr`, `ledger_evidence`\n-   and `expected_branch` may differ \u2014 the ADR 0015 contract-immutability rule,\n+   and `expected_branch` may differ \u2014 the SADR-0015 contract-immutability rule,\n    unchanged.\n \n    `expected_branch` is in that set because a closeout is *by construction* on a\n    different branch from the implementation it closes, and the loop rewrites the\n    field accordingly. It is safe to let it differ because MergeGate independently\n    requires `expected_branch` to equal the PR's actual head ref, so a PR cannot set\n-   it to anything other than the branch it is really on. The first draft of this ADR\n+   it to anything other than the branch it is really on. The first draft of this SADR\n    omitted it, which made the exemption unreachable: every real closeout failed\n    condition 4 on a field the loop always changes. See PR #209.\n 5. The state transitions are exactly the terminal set: task `review` \u2192 `done`, lock\n@@ -113,12 +113,12 @@\n classification must be derived, not asserted.\n \n **Fold the closeout into the implementation PR** so no second PR exists. Rejected under\n-ADR 0012, which recorded why: a task file that is already `done` on the implementation\n+SADR-0012, which recorded why: a task file that is already `done` on the implementation\n branch short-circuits the loop's review \u2192 CCA \u2192 merge \u2192 done ordering into a local\n goal-completion before any merge happens.\n \n **Have the loop auto-merge closeouts with elevated credentials.** Rejected outright: that\n-is a runtime authoring its own approval, which ADR 0010 and the constitution's\n+is a runtime authoring its own approval, which SADR-0010 and the constitution's\n approval-record prohibition forbid. It would also be indistinguishable, in the evidence,\n from the forgery those rules exist to prevent.\n \ndiff --git a/docs/adr/0018-carried-guardian-approval-across-a-proven-base-sync.md b/docs/adr/SADR-0018-carried-guardian-approval-across-a-proven-base-sync.md\nindex 22358562da390ed8b45cd600da08a43d378b4680..e6e2907cefa776460af73ddf7d39db605f3c39b0\n--- a/docs/adr/0018-carried-guardian-approval-across-a-proven-base-sync.md\n+++ b/docs/adr/SADR-0018-carried-guardian-approval-across-a-proven-base-sync.md\n@@ -1,4 +1,4 @@\n-# ADR 0018: Carried Guardian Approval Across A Proven Base Sync\n+# SADR-0018: Carried Guardian Approval Across A Proven Base Sync\n \n ## Status\n \n@@ -34,7 +34,7 @@\n Guardian evaluator that consumes it (`shiki_guardian.evaluate_guardian_approval`,\n the `guardian_comment_carried` source) were built and tested in isolation. Neither\n does anything until a caller passes the flags that turn the carry on, so the\n-feature was invisible in CI. This ADR records the decision the two of them\n+feature was invisible in CI. This SADR records the decision the two of them\n implement, and this task wires it into the four CI invocations that judge Guardian\n approval.\n \ndiff --git a/docs/adr/README.md b/docs/adr/README.md\nindex c70e057a4520c36e042ab68c53cd4a21ef709b71..5da43856836e2ce6a150f8127766e2f88355ab4b\n--- a/docs/adr/README.md\n+++ b/docs/adr/README.md\n@@ -1,40 +1,41 @@\n-# Architecture Decision Records\n+# Decision Record Namespaces\n \n-This directory holds Shiki's Architecture Decision Records (ADRs): durable\n-records of hard-to-reverse platform decisions. ADRs are part of the Shiki source\n-of truth alongside GitHub state, the `.shiki/` mirror, and `CONTEXT.md`.\n+This directory holds two independent decision namespaces:\n \n-## How ADRs Work\n+- Shiki Architecture Decision Records (SADRs), named\n+  `SADR-NNNN-<short-slug>.md`, record hard-to-reverse Shiki platform decisions.\n+- Target Architecture Decision Records (ADRs), named\n+  `NNNN-<short-slug>.md`, belong to the target product.\n \n-- New ADRs are added as `docs/adr/NNNN-<short-slug>.md`, where `NNNN` is the next\n-  zero-padded sequence number.\n-- The **canonical list of ADRs is the set of files matching `docs/adr/*.md`** in\n-  this directory. Browse `docs/adr/*.md` for the authoritative, current set.\n-- Use [`template.md`](template.md) as the starting point for a new ADR.\n-- Record an ADR when a decision is hard to reverse, surprising without context,\n-  and a real tradeoff. Do not write an ADR for routine or easily reversible\n-  choices.\n+The same four-digit number may appear once in each namespace. A platform\n+`SADR-NNNN` citation resolves only to `SADR-NNNN-*.md`; a target `ADR NNNN`\n+citation resolves only to `NNNN-*.md`.\n \n-Using the `docs/adr/*.md` glob as the source of truth means other PRs can add an\n-ADR without editing this index file, avoiding merge contention on a\n-hand-maintained enumeration.\n+## How Decision Records Work\n \n-## Current ADRs (snapshot)\n-\n-The list below is a convenience snapshot and may lag behind the directory. When\n-in doubt, list `docs/adr/*.md`.\n+- New Shiki platform decisions use the next zero-padded SADR number.\n+- New target product decisions use the target's own ADR sequence.\n+- The canonical list is the files in `docs/adr/`; no hand-maintained index is\n+  authoritative.\n+- Use [`template.md`](template.md) as the starting point and choose the filename\n+  namespace that owns the decision.\n+- Record a decision when it is hard to reverse, surprising without context, and\n+  a real tradeoff. Do not write a decision record for routine or easily\n+  reversible choices.\n \n-- [0001 \u2014 Keep Shiki As The Platform Name](0001-keep-shiki-as-platform-name.md)\n-- [0002 \u2014 Use AGENTS As The Shared Agent Constitution](0002-use-agents-as-shared-constitution.md)\n-- [0003 \u2014 Require Skill Gate For MergeGate Readiness](0003-require-skill-gate-for-mergegate-readiness.md)\n-- [0004 \u2014 Use GitHub First With A Repository-Local Shiki Mirror](0004-use-github-first-with-shiki-mirror.md)\n-- [0005 \u2014 Use Codex As Front And Claude Code Action As The GitHub Runtime](0005-use-codex-as-front-and-claude-code-action-as-github-runtime.md)\n-- [0006 \u2014 Separate Codex Implementation, CCA Completion, And MergeGate Authorization](0006-separate-codex-implementation-cca-completion-and-mergegate-authorization.md)\n+Using the directory as the source of truth lets independent changes add records\n+without contending on an enumerated index.\n \n-## Adding A New ADR\n+## Adding A Shiki SADR\n \n-1. Copy `template.md` to `docs/adr/NNNN-<short-slug>.md`.\n+1. Copy `template.md` to `docs/adr/SADR-NNNN-<short-slug>.md`.\n 2. Fill in the title, status, context, decision, and consequences.\n-3. Set the status to `Proposed`, then `Accepted` once the decision is approved.\n-4. Open a PR following `CONTRIBUTING.md`. You do not need to edit this index for\n-   the ADR to be discoverable via `docs/adr/*.md`.\n+3. Set the status to `Proposed`, then `Accepted` once the proper authority has\n+   approved the decision.\n+4. Open a PR following `CONTRIBUTING.md`.\n+\n+## Adding A Target ADR\n+\n+1. Copy `template.md` to `docs/adr/NNNN-<short-slug>.md` using the target's own\n+   sequence.\n+2. Cite it as `ADR NNNN`; do not prefix its filename with `SADR-`.\ndiff --git a/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md b/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\nnew file mode 100644\nindex 0000000000000000000000000000000000000000..7455ce1f3bed02ddddb851ac21a1d1a648c9aeba\n--- /dev/null\n+++ b/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\n@@ -0,0 +1,55 @@\n+# SADR-0019: Separate Shiki Platform SADRs From Target ADRs\n+\n+## Status\n+\n+Accepted\n+\n+## Context\n+\n+Shiki installs its platform decision records into `docs/adr/` in every target\n+repository. Shiki and its targets previously used the same numeric\n+`NNNN-<slug>.md` namespace. A target that already owned ADRs 0013 through 0018\n+therefore acquired two records for each number during installation, making an\n+`ADR NNNN` citation ambiguous. Renumbering target decisions would rewrite target\n+history, while renumbering Shiki decisions per target would destroy their stable\n+platform identity.\n+\n+Upgrades also need to remove the former Shiki-owned numeric paths without ever\n+mistaking a target-owned ADR for platform content. The install stamp already\n+records exact shipped paths and content digests, so it can prove ownership only\n+when both still match.\n+\n+## Decision\n+\n+Shiki platform decisions use the `SADR` namespace:\n+\n+- records are named `docs/adr/SADR-NNNN-<slug>.md`;\n+- live platform citations use `SADR-NNNN`;\n+- target product decisions retain `docs/adr/NNNN-<slug>.md` and `ADR NNNN`;\n+- the reference validator resolves the two namespaces independently, so the same\n+  four-digit number may exist once in each namespace.\n+\n+Before a forced upgrade writes anything, the installer inventories the exact\n+former numeric Shiki ADR paths. It may delete a legacy path only when the\n+existing install stamp contains that exact path and its recorded SHA-256 digest\n+matches the file currently on disk. All ownership blockers are reported\n+together and abort the upgrade before its first write. A non-forced install\n+never performs this cleanup.\n+\n+Historical `.shiki/` evidence remains unchanged because it records the namespace\n+that existed when that evidence was produced.\n+\n+## Consequences\n+\n+- Targets may use any ADR number without colliding with Shiki's platform SADRs.\n+- Fresh installs preserve target ADR bytes and install a stable SADR set.\n+- Forced upgrades fail closed when legacy ownership cannot be proven, and a\n+  successful upgrade refreshes the stamp without removed legacy paths.\n+- Current governance, packaging, workflow, prompt, script, and skill references\n+  must move with the platform records.\n+\n+## Alternatives Considered\n+\n+- Collision-only refusal leaves affected targets permanently unupgradable.\n+- Renumbering target ADRs mutates target-owned history.\n+- Per-target Shiki renumbering makes platform citations non-portable.\ndiff --git a/docs/adr/template.md b/docs/adr/template.md\nindex a92a97daddd0b00ab9dd626cc8d86942da2eafed..17dd79175b5fee2105935557f691b40cf9492942\n--- a/docs/adr/template.md\n+++ b/docs/adr/template.md\n@@ -1,14 +1,14 @@\n-# NNNN: <Short Decision Title>\n+# <SADR-NNNN or NNNN>: <Short Decision Title>\n \n ## Status\n \n-Proposed | Accepted | Superseded by ADR-NNNN\n+Proposed | Accepted | Superseded by SADR-NNNN or target ADR NNNN\n \n ## Context\n \n What is the situation, constraint, or force that requires a decision? Reference\n-relevant domain terms (`CONTEXT.md`), prior ADRs, and code reality. State the\n-problem, not the solution.\n+relevant domain terms (`CONTEXT.md`), prior Shiki SADRs or target ADRs, and code\n+reality. State the problem, not the solution.\n \n ## Decision\n \ndiff --git a/docs/agents/autonomous-loop-e2e-acceptance.md b/docs/agents/autonomous-loop-e2e-acceptance.md\nindex bbf84c6081987f159ac6ad9ecf3c82b7aa72e7e8..5c13c1d02a05a90b0dc46cbef7c95a9846d0c237\n--- a/docs/agents/autonomous-loop-e2e-acceptance.md\n+++ b/docs/agents/autonomous-loop-e2e-acceptance.md\n@@ -40,8 +40,8 @@\n 3. `gh auth status` is logged in with merge permission on the repo.\n 4. Branch protection + required checks are configured exactly as the goal-loop\n    required-check set expects (see `docs/agents/branch-protection-smoke.md`).\n-5. The repo `.shiki/policy` permits autonomous low/medium-risk merges (ADR\n-   0008/0009); the MergeGate policy check is the recorded-authority gate.\n+5. The repo `.shiki/policy` permits autonomous low/medium-risk merges\n+   (SADR-0008/0009); the MergeGate policy check is the recorded-authority gate.\n \n The operator's only actions are: register the task and start the loop. **No\n manual pre-clearing** means after `shiki loop run` starts, the operator does not\ndiff --git a/docs/agents/bootstrap-command.md b/docs/agents/bootstrap-command.md\nindex 05f7f203a52cf412b9f97084a6e131ed0f785afd..f7aa0b734dfebee4a3b139335b66ee3415f48713\n--- a/docs/agents/bootstrap-command.md\n+++ b/docs/agents/bootstrap-command.md\n@@ -189,7 +189,9 @@\n ```\n \n Use this only for tests, fixtures, or explicit local-only template inspection.\n-Use `--force` only when you intentionally want to overwrite existing target files.\n+Use `--force` only when you intentionally want to refresh the target template.\n+Legacy numeric Shiki decision cleanup is stamp-and-digest authorized before any\n+write; target ADR paths are not cleanup candidates.\n \n ## Slash Command\n \ndiff --git a/docs/agents/checklists.md b/docs/agents/checklists.md\nindex 4a7f03ade346f53a1c1d82981f3fd744f9ab4547..0c5ec6c53dc7197b7b7a7d18e667b325babd6c45\n--- a/docs/agents/checklists.md\n+++ b/docs/agents/checklists.md\n@@ -32,18 +32,18 @@\n | ID | Item | Blocking |\n | --- | --- | --- |\n | GD-01 | Plan was challenged against `CONTEXT.md`. | blocking for non-trivial Goals |\n-| GD-02 | Relevant ADRs were read or absence was recorded. | blocking for non-trivial Goals |\n+| GD-02 | Relevant SADRs and target ADRs were read or absence was recorded. | blocking for non-trivial Goals |\n | GD-03 | Ambiguous terms were resolved or converted to HITL blockers. | blocking |\n | GD-04 | Edge scenarios were considered. | advisory |\n | GD-05 | Code reality was checked when code could answer. | blocking when applicable |\n | GD-06 | New domain terms were added to `CONTEXT.md` when needed. | blocking when applicable |\n-| GD-07 | ADR was created or explicitly skipped using the ADR criteria. | blocking when applicable |\n+| GD-07 | A SADR or target ADR was created or explicitly skipped using the decision-record criteria. | blocking when applicable |\n \n ## CI \u2014 Context & Impact Checklist\n \n | ID | Item | Blocking |\n | --- | --- | --- |\n-| CI-01 | Relevant docs and ADRs identified. | blocking |\n+| CI-01 | Relevant docs, SADRs, and target ADRs identified. | blocking |\n | CI-02 | Relevant modules, interfaces, seams, callers, and tests identified. | blocking |\n | CI-03 | Dependencies and blocked-by relationships identified. | blocking |\n | CI-04 | Candidate locks identified. | blocking |\n@@ -62,7 +62,7 @@\n | PRD-04 | Implementation decisions are recorded without volatile code detail. | blocking |\n | PRD-05 | Testing decisions are recorded. | blocking |\n | PRD-06 | Out-of-scope items are explicit. | blocking |\n-| PRD-07 | Relevant glossary/ADR links are present when applicable. | blocking |\n+| PRD-07 | Relevant glossary/SADR/target-ADR links are present when applicable. | blocking |\n \n ## SF \u2014 Spec Freeze Checklist\n \n@@ -98,7 +98,7 @@\n | H-02 | Branch/worktree target is present. | blocking |\n | H-03 | Scope and non-goals are present. | blocking |\n | H-04 | Dependencies and locks are present. | blocking |\n-| H-05 | Relevant docs, ADRs, modules, and tests are referenced. | blocking |\n+| H-05 | Relevant docs, SADRs, target ADRs, modules, and tests are referenced. | blocking |\n | H-06 | Required skills are stated. | blocking |\n | H-07 | TDD expectations are stated. | blocking when behavior work |\n | H-08 | Acceptance criteria are included. | blocking |\ndiff --git a/docs/agents/codex-handoff.md b/docs/agents/codex-handoff.md\nindex ede468f47cb277f69a857e8c45df703e4ed0151d..6f97bdffb67e44d7edea2c71e01ea28933c4eb6c\n--- a/docs/agents/codex-handoff.md\n+++ b/docs/agents/codex-handoff.md\n@@ -46,7 +46,7 @@\n \n ## Context\n - Domain terms:\n-- Relevant ADRs:\n+- Relevant SADRs and target ADRs:\n - Relevant docs:\n - Relevant modules/interfaces/seams:\n - Similar tests/prior art:\ndiff --git a/docs/agents/completion-check-agent.md b/docs/agents/completion-check-agent.md\nindex 13e4c035db35025f4e3cdcc5adfb24314ac4d521..61bfa1218cf36ca605250c91919e620f33238aa0\n--- a/docs/agents/completion-check-agent.md\n+++ b/docs/agents/completion-check-agent.md\n@@ -32,7 +32,7 @@\n - `AGENTS.md`\n - `CLAUDE.md`\n - `CONTEXT.md`\n-- relevant ADRs\n+- relevant SADRs and target ADRs\n - parent Goal issue\n - PRD issue or document\n - task issue\ndiff --git a/docs/agents/decision-control.md b/docs/agents/decision-control.md\nindex 7d8ee5ab03a5ccbcbd04056ae61de03785a2a9c9..8c94ecda00fffaa0e92c325ab83aa9776bd1dac1\n--- a/docs/agents/decision-control.md\n+++ b/docs/agents/decision-control.md\n@@ -11,7 +11,7 @@\n | Decision | Owner | When | Required inputs | Output | Enforcement surface |\n | --- | --- | --- | --- | --- | --- |\n | Is the Goal clear enough to plan? | Planner + Guardian when needed | Goal intake | User request, existing docs, risk signals | Goal readiness verdict | Goal checklist, issue template, ledger |\n-| Must `grill-with-docs` run? | Planner / Skill Gate | Before PRD or issue split | Ambiguity, domain terms, ADR conflicts, unclear boundaries | Skill invocation or explicit skip rationale | Skill Gate checklist |\n+| Must `grill-with-docs` run? | Planner / Skill Gate | Before PRD or issue split | Ambiguity, domain terms, SADR/target-ADR conflicts, unclear boundaries | Skill invocation or explicit skip rationale | Skill Gate checklist |\n | Is a PRD required? | Planner | After Goal context stabilizes | Goal, `grill-with-docs` output, Context & Impact output | PRD required / not required | PRD checklist, CCA evidence check |\n | Can the spec be frozen? | Operator only | End of Requirements Definition, before issue split | PRD/requirements summary, scope inventory (SF-02), open-question list | `spec_freeze` block in the plan + ledger evidence | `require_grilled_plan` (plan ingest/run), SF checklist |\n | Can a frozen spec change? | Operator only (Spec Amendment) | When implementation contradicts the frozen spec | Contested decisions, scoped re-grill result | recorded amendment + re-stamped freeze | SF checklist, ledger evidence |\ndiff --git a/docs/agents/domain.md b/docs/agents/domain.md\nindex 258da8ea81e87ac0a6b0b5b5e5c2fde980eaabb7..d57b0cf17ff3e1fa9e6e5885372bf083d8295421\n--- a/docs/agents/domain.md\n+++ b/docs/agents/domain.md\n@@ -5,7 +5,9 @@\n ## Layout\n \n - `CONTEXT.md` defines Shiki language. Keep it implementation-free.\n-- `docs/adr/` records hard-to-reverse decisions.\n+- `docs/adr/SADR-NNNN-*.md` records Shiki platform decisions.\n+- `docs/adr/NNNN-*.md` records target product decisions without sharing the\n+  SADR namespace.\n - `AGENTS.md` is the runtime-neutral agent constitution.\n - `CLAUDE.md` is the Claude Code wrapper around `AGENTS.md`.\n - `.shiki/` is the Target Repository mirror for Goals, PRDs, plans, Task DAGs, contracts, locks, worktree records, ledger entries, CCA verdicts, repair packets, reports, and handoffs.\n@@ -15,6 +17,6 @@\n \n - Use Shiki terms exactly when they exist.\n - Add glossary terms to `CONTEXT.md` only when the term is part of the domain, not a generic programming concept.\n-- Create ADRs only for hard-to-reverse, surprising, tradeoff-based decisions.\n+- Create SADRs or target ADRs only for hard-to-reverse, surprising, tradeoff-based decisions.\n - Keep product-specific language in the Target Repository, not in Shiki Template docs.\n-- If an ADR conflicts with a proposed implementation, surface the conflict before editing.\n+- If a SADR or target ADR conflicts with a proposed implementation, surface the conflict before editing.\ndiff --git a/docs/agents/execution-control.md b/docs/agents/execution-control.md\nindex ad8ee3cdb36be56fedc1f46408ca92b387d74047..bc63c3a73661d12b567016ba299f1a6b66bf9088\n--- a/docs/agents/execution-control.md\n+++ b/docs/agents/execution-control.md\n@@ -63,7 +63,7 @@\n | Branch name | Orchestrator | Orchestrator | Before implementer dispatch | branch policy | branch ref in task/ledger |\n | Lock reservation | Context & Impact | MergeGate | Before dispatch and before merge | `.shiki/locks/`, labels, PR metadata | lock id, owner, scope |\n | CI profile | Planner | MergeGate | During issue triage, rechecked at PR | labels, `.shiki/policy.yaml`, workflow matrix | required check list |\n-| Required status checks | Repo maintainer / Guardian | GitHub branch protection / rulesets | Repository setup, updated via ADR | GitHub settings | branch/ruleset config, ADR |\n+| Required status checks | Repo maintainer / Guardian | GitHub branch protection / rulesets | Repository setup, updated via target ADR | GitHub settings | branch/ruleset config, target ADR |\n | CCA judgment | CCA | GitHub required check / MergeGate | Every non-draft PR update | GitHub Action check | structured CCA verdict |\n | Repair assignment | CCA | MergeGate | After failed CCA/CI/review | repair packet, issue/PR comment | repair id and attempt count |\n | Merge readiness | MergeGate | GitHub branch protection / Guardian if needed | After CCA complete | required checks, review rules, rulesets | mergegate check output |\ndiff --git a/docs/agents/guardian-policy.md b/docs/agents/guardian-policy.md\nindex 4147c7adf05a47adb4fd24ada1a1c2e1f2ba6d2d..a20d9cc6c00a569bb057b9ca8d6b51c7fc4a9847\n--- a/docs/agents/guardian-policy.md\n+++ b/docs/agents/guardian-policy.md\n@@ -98,8 +98,8 @@\n `origin/<default_branch>` and reuses each job's existing base fetch; a shallow\n (`--depth`) or second base fetch would make the proof unresolvable and silently\n disable the carry, so neither is permitted. CCA reads `guardian_comment_carried` as\n-a recorded authority in the deterministic signal and does not re-derive it. See ADR\n-0018 for the full decision, the rejected diff/patch-equality and path-disjointness\n+a recorded authority in the deterministic signal and does not re-derive it. See\n+SADR-0018 for the full decision, the rejected diff/patch-equality and path-disjointness\n alternatives, and the accepted residual.\n \n ## External AI Guardian Review (SADR-0010)\ndiff --git a/docs/agents/implementation-policy.md b/docs/agents/implementation-policy.md\nindex c754144a9fb5bf4a38aa82a60e1670c59e626b44..85c37281bcad196c6b7b02343faf00b03cd9c57a\n--- a/docs/agents/implementation-policy.md\n+++ b/docs/agents/implementation-policy.md\n@@ -31,10 +31,13 @@\n 5. Runtime-specific wrappers such as `CLAUDE.md`, `.codex/`, `.claude/`, `.github/prompts/`, and hooks may add stricter instructions but must not weaken the shared constitution.\n <!-- shiki-source-of-truth:end -->\n \n+Within `docs/adr/`, Shiki platform decisions use `SADR-NNNN-*.md`; target\n+product decisions retain `NNNN-*.md` and the `ADR` namespace.\n+\n ## Non-Negotiables\n \n 1. **Planning precedes implementation.** Non-trivial Goals start with `grill-with-docs` and Context & Impact.\n-2. **Domain language is controlled.** Terms must be checked against `CONTEXT.md`; hard-to-reverse tradeoffs must be checked against ADRs.\n+2. **Domain language is controlled.** Terms must be checked against `CONTEXT.md`; hard-to-reverse tradeoffs must be checked against Shiki SADRs and target ADRs.\n 3. **PRD precedes issue decomposition.** `to-prd` is used once the context is settled enough to become durable intent.\n 4. **Issues are vertical slices.** `to-issues` must produce independently grabbable tracer-bullet tasks, not horizontal layer tickets.\n 5. **Triage controls dispatch.** Only `ready-for-agent` / AFK tasks may be assigned to Codex without human input.\n@@ -95,19 +98,19 @@\n Process:\n \n - Challenge terms against `CONTEXT.md`.\n-- Check ADRs before reopening decisions.\n+- Check SADRs and target ADRs before reopening decisions.\n - Inspect code when code can answer a question.\n - Ask one question at a time when the operator is available.\n - Provide a recommended answer with each question.\n - Record resolved terms in `CONTEXT.md`.\n-- Create ADRs only for hard-to-reverse, surprising, tradeoff-based decisions.\n+- Create SADRs or target ADRs only for hard-to-reverse, surprising, tradeoff-based decisions.\n - Convert unresolved load-bearing questions into HITL blockers.\n \n Outputs:\n \n - Resolved design-tree decisions.\n - Updated glossary terms when needed.\n-- ADRs when justified.\n+- SADRs or target ADRs when justified.\n - HITL blockers when decisions remain unresolved.\n \n ## Phase 3 \u2014 Context & Impact\n@@ -117,7 +120,7 @@\n \n Outputs must include:\n \n-- Relevant documents and ADRs.\n+- Relevant documents, SADRs, and target ADRs.\n - Relevant modules, interfaces, seams, callers, and tests.\n - Candidate locks.\n - Dependencies.\n@@ -140,7 +143,7 @@\n - Testing decisions.\n - Out of scope.\n - Further notes.\n-- Links to relevant Goal, domain terms, and ADRs.\n+- Links to relevant Goal, domain terms, SADRs, and target ADRs.\n \n Phase 4 ends with Spec Freeze: the operator explicitly approves the PRD, the\n plan records a `spec_freeze` block (status, approved_by, source), and the SF\n@@ -240,7 +243,7 @@\n - Dependencies and locks.\n - Risk and Guardian requirements.\n - Ledger completeness.\n-- Documentation and ADR updates.\n+- Documentation and SADR/target-ADR updates.\n - Scope drift.\n \n CCA emits one of:\n@@ -301,7 +304,7 @@\n | Evidence failure | Behavior might be done, but proof is missing. | Add checks, PR evidence, ledger entry, or artifacts. |\n | Scope drift | PR changes unrelated behavior. | Revert unrelated changes or split into new Goal. |\n | Dependency block | Upstream task or lock not complete. | Block until dependency resolves. |\n-| Architecture block | Decision needs ADR or deep-module discussion. | `grill-with-docs` or `improve-codebase-architecture`. |\n+| Architecture block | Decision needs a SADR, target ADR, or deep-module discussion. | `grill-with-docs` or `improve-codebase-architecture`. |\n | Risk block | Security/production/policy change needs human approval. | Guardian decision. |\n \n ## Phase 5.5 \u2014 Execution Decision Control\ndiff --git a/docs/agents/runtime-auth-model.md b/docs/agents/runtime-auth-model.md\nindex 6a2e1a47e81ea89b5c68a210ec07772a9374babc..3c54748d24233dbd0ba8473950282547ef4ce935\n--- a/docs/agents/runtime-auth-model.md\n+++ b/docs/agents/runtime-auth-model.md\n@@ -148,7 +148,7 @@\n \n API-key based runners may be added by a target repository as an explicit extension. They are not the default Shiki template path.\n \n-If a repo opts into API-key mode, record the exception in an ADR and update the repo's `AGENTS.md`, `.shiki/config.yaml`, and workflow permissions.\n+If a repo opts into API-key mode, record the target-specific exception in a target ADR and update the repo's `AGENTS.md`, `.shiki/config.yaml`, and workflow permissions.\n \n ## GitHub Actions Runtime Compatibility\n \ndiff --git a/docs/agents/skill-gate.md b/docs/agents/skill-gate.md\nindex c50398b0d7c60a8598f2a5c88a10015730dced13..3acffe6444617bb1d54b77d4b4708158ec735bc6\n--- a/docs/agents/skill-gate.md\n+++ b/docs/agents/skill-gate.md\n@@ -9,7 +9,7 @@\n | Skill | Trigger | Expected output |\n | --- | --- | --- |\n | `setup-matt-pocock-skills` | First configuring a repo for these skills, or when issue tracker, triage labels, or domain docs are missing. | `docs/agents/` configuration and an `Agent skills` block in `AGENTS.md` or `CLAUDE.md`. |\n-| `grill-with-docs` | Ambiguous plan, unclear terminology, boundary decision, tradeoff, or ADR-worthy design choice. | Clarified decision tree, recommended answers, updates to `CONTEXT.md` or ADRs when decisions crystallize. |\n+| `grill-with-docs` | Ambiguous plan, unclear terminology, boundary decision, tradeoff, or decision-record-worthy design choice. | Clarified decision tree, recommended answers, updates to `CONTEXT.md`, SADRs, or target ADRs when decisions crystallize. |\n | `zoom-out` | Agent lacks a higher-level architectural map. | Map of relevant modules, callers, seams, and domain concepts. |\n | `to-prd` | Settled context should become a PRD. | PRD published to the issue tracker with problem, solution, user stories, decisions, testing, and out-of-scope notes. |\n | `to-issues` | Goal, PRD, or plan must become implementation tickets. | Vertical-slice issues with acceptance criteria, dependencies, and AFK/HITL classification. |\n@@ -17,7 +17,7 @@\n | `tdd` | Feature work or bug fix has observable behavior that can be specified with tests. | Red-green-refactor loop through public interfaces, one vertical slice at a time. |\n | `code-review` | The TDD loop is green and the implementer is about to create or update a PR. Default for every implementation task. | Pre-PR self-review of the task diff with fixes applied on the task branch; findings and resolutions recorded as a ledger entry naming the skill and a `## Pre-PR code review` PR body section. |\n | `diagnose` | Hard bug, failing check, regression, flaky behavior, or performance problem. | Reproduction loop, ranked hypotheses, instrumentation, fix, regression test, post-mortem. |\n-| `improve-codebase-architecture` | Structural friction, testability, AI-navigability, deep-module opportunity, or refactor request. | Architecture candidates and deepening recommendations informed by `CONTEXT.md` and ADRs. |\n+| `improve-codebase-architecture` | Structural friction, testability, AI-navigability, deep-module opportunity, or refactor request. | Architecture candidates and deepening recommendations informed by `CONTEXT.md`, SADRs, and target ADRs. |\n | `prototype` | A throwaway logic or UI prototype is needed to answer a design question. | Clearly marked disposable prototype and captured decision. |\n \n ## MergeGate Interaction\ndiff --git a/docs/operations.md b/docs/operations.md\nindex cf1f452d4c589e38ccd58553bda0ad8684b51eb7..ace48a90ffbec9e5c0a7113bef16a4858bc44b3f\n--- a/docs/operations.md\n+++ b/docs/operations.md\n@@ -89,6 +89,16 @@\n      target history, brought forward by `shiki migrate`, not a template.\n    - **Everything else** (CLI scripts, workflows, schemas, docs) is overwritten.\n \n+   A forced upgrade also migrates the former numeric Shiki decision paths to\n+   `SADR-NNNN-*`. Before the first write, the installer inventories the exact\n+   legacy Shiki paths and authorizes each deletion only when the existing\n+   `.shiki/install-stamp.json` names that path and its SHA-256 digest still\n+   matches the file. An absent, unreadable, malformed, incomplete, or\n+   mismatching stamp reports every blocked path and stops with no filesystem\n+   change. Target-owned `NNNN-*` ADRs are not cleanup candidates. A non-force\n+   install deletes nothing and retains any still-matching legacy stamp entries\n+   so a later forced upgrade can prove ownership.\n+\n    If the target has pending migrations, `--force` refuses and writes nothing \u2014\n    apply migrations first (step 3), then re-run. The run ends with a single\n    summary of every `.new` written, so a half-upgrade is visible.\ndiff --git a/docs/releasing.md b/docs/releasing.md\nindex 10d73828e7dbd64e9d9f6b83bc84d010537ce62a..aafeee8c4d441e1e427efee2ce4f81a647f020e1\n--- a/docs/releasing.md\n+++ b/docs/releasing.md\n@@ -67,7 +67,7 @@\n CLI in `scripts/`. There are no third-party Python runtime dependencies. The CLI\n is intentionally **not** exposed as a `[project.scripts]` console entry point\n because `scripts/shiki.py` is a dependency-free shim and `scripts/` is not\n-packaged as an importable distribution (see the ADR for the rationale).\n+packaged as an importable distribution (see SADR-0007 for the rationale).\n \n Supported install paths:\n \n@@ -155,6 +155,15 @@\n    [docs/operations.md](operations.md#the-new-convention). If the target has\n    pending migrations, `--force` refuses and writes nothing; apply migrations\n    (step 2) first, then re-run.\n+\n+   The SADR namespace upgrade has an additional pre-write gate: each former\n+   numeric Shiki decision path is removed only when the existing install stamp\n+   contains that exact path and a digest matching its current bytes. The\n+   installer aggregates absent/unreadable/malformed-stamp, missing-digest, and\n+   digest-mismatch blockers before raising, so refusal leaves the target tree\n+   byte-identical. Numeric target ADRs are preserved. A successful forced\n+   upgrade installs and stamps `SADR-*` paths and drops the removed legacy paths\n+   from the refreshed stamp.\n 2. Apply repository-local `.shiki/` state migrations with the migrate command:\n \n    ```bash\ndiff --git a/docs/usage.md b/docs/usage.md\nindex b83407ee3970dd395dd893010bc5dbe8bad26051..a130ef6e40dfd8169a5bc701339351097df1a09f\n--- a/docs/usage.md\n+++ b/docs/usage.md\n@@ -67,8 +67,10 @@\n shiki install-target /path/to/target-repo --local-only\n ```\n \n-Use `--force` only when you intentionally want to overwrite existing target\n-files.\n+Use `--force` only when you intentionally want to refresh the target template.\n+For the SADR namespace migration, `--force` deletes only exact legacy Shiki\n+decision paths whose existing install-stamp digest still matches; any ownership\n+blocker stops before the first write. Numeric target ADRs are never candidates.\n \n ## `shiki bootstrap-platform` \u2014 publish the Shiki platform repo\n \ndiff --git a/scripts/guardian_approval_signal.py b/scripts/guardian_approval_signal.py\nindex d059f92b2c82aa2b5d6ca421365537a87886c1cb..16924892714f4f5b9303c0ef0e325a7e70075630\n--- a/scripts/guardian_approval_signal.py\n+++ b/scripts/guardian_approval_signal.py\n@@ -199,8 +199,8 @@\n ) -> Any:\n     \"\"\"Evaluate SADR-0015 Contract Approval for the CCA Guardian signal.\n \n-    The SAME OR that MergeGate applies must reach this deterministic signal: ADR\n-    0015 is explicit that without it, a contract-approved PR still returns\n+    The SAME OR that MergeGate applies must reach this deterministic signal:\n+    SADR-0015 is explicit that without it, a contract-approved PR still returns\n     ``needs_guardian``, the CCA job fails, and the MergeGate policy check never\n     runs \u2014 the gate this change moves would never be reached. This resolves the\n     task the SAME way MergeGate does (the first Shiki task id in the PR body),\ndiff --git a/scripts/shiki_contract_approval.py b/scripts/shiki_contract_approval.py\nindex 941bcb96fdd8b9a85bc0c4f0f04d5dad9d04839c..4a4b418f6527413161db82670bec857a043360e2\n--- a/scripts/shiki_contract_approval.py\n+++ b/scripts/shiki_contract_approval.py\n@@ -142,7 +142,7 @@\n     changed_files_status: Any,\n     registration: Any,\n ) -> ContractApprovalResult:\n-    \"\"\"Whether this PR carries an SADR-0015 Contract Approval.\n+    \"\"\"Whether this PR carries a SADR-0015 Contract Approval.\n \n     The four conditions are evaluated IN ORDER, each failure recorded as a\n     distinct reason string:\ndiff --git a/scripts/shiki_guardian.py b/scripts/shiki_guardian.py\nindex a4ecb79bbda55fd5c8b06adfae2eb99760a03c45..4dd99bbe714e26f421fa19abac7a34ae1b195ecc\n--- a/scripts/shiki_guardian.py\n+++ b/scripts/shiki_guardian.py\n@@ -12,7 +12,7 @@\n # The carry re-derives its proof from git in-process on every judging invocation;\n # it is never read from an artifact, so there is nothing to go stale or be forged.\n # shiki_sync_proof is the ONLY guardian-family module that runs git, and every one\n-# of its invocations is read-only and local (ADR: prove-from-git base sync).\n+# of its invocations is read-only and local (SADR: prove-from-git base sync).\n from shiki_sync_proof import verify_pure_base_sync\n \n GUARDIAN_POLICY_PATH = \".shiki/guardian-policy.json\"\ndiff --git a/scripts/shiki_guardian_review.py b/scripts/shiki_guardian_review.py\nindex dd0f104d457899e0ba5ff1e162011c1e90b6f92b..e10ef58c046324e7ea3a3392feb3048ca77414ce\n--- a/scripts/shiki_guardian_review.py\n+++ b/scripts/shiki_guardian_review.py\n@@ -159,7 +159,7 @@\n     docs_only = _is_docs_only(changed_files)\n     if docs_only and matched == 0:\n         focus.append(\n-            \"Docs/ADR-only change: confirm the change is genuinely documentation/decision-only, introduces no behavior or contract drift, and stays consistent with the constitution and existing ADRs.\"\n+            \"Docs/decision-record-only change: confirm the change is genuinely documentation/decision-only, introduces no behavior or contract drift, and stays consistent with the constitution and existing SADRs and target ADRs.\"\n         )\n \n     # Conservative augmentation: unknown (no category and not docs-only), mixed\ndiff --git a/scripts/shiki_installer.py b/scripts/shiki_installer.py\nindex be064ab7a29ff6836f7f7a1d70500a43e0d11d7b..9fd8eefd137ce7003dad5c0dcb2e48e2cd47ab32\n--- a/scripts/shiki_installer.py\n+++ b/scripts/shiki_installer.py\n@@ -118,6 +118,7 @@\n # what lets a target carry it without a governance-schema change.\n INSTALL_STAMP_PATH = \".shiki/install-stamp.json\"\n INSTALL_STAMP_VERSION = 1\n+LEGACY_SHIKI_SADR_MAX = 18\n \n # Under --force the shipped surface splits three ways, so nothing is lost and\n # nothing is silently stale.\n@@ -237,9 +238,98 @@\n         )\n \n \n+def legacy_shiki_adr_paths() -> tuple[str, ...]:\n+    \"\"\"Former numeric paths for Shiki SADR-0001 through SADR-0018.\n+\n+    The authoritative slugs come from the current SADR filenames. SADR-0019 is\n+    the namespace decision itself and never shipped under a numeric legacy path.\n+    \"\"\"\n+    paths: list[str] = []\n+    for sadr in sorted((ROOT / \"docs\" / \"adr\").glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\")):\n+        number = sadr.name.split(\"-\", 2)[1]\n+        if int(number) <= LEGACY_SHIKI_SADR_MAX:\n+            paths.append(f\"docs/adr/{sadr.name.removeprefix('SADR-')}\")\n+    return tuple(paths)\n+\n+\n+def preflight_legacy_sadr_cleanup(target: Path, *, force: bool) -> tuple[str, ...]:\n+    \"\"\"Authorize exact legacy Shiki ADR removals before the first write.\n+\n+    A non-force install never removes existing files. A forced install needs no\n+    stamp when no legacy Shiki path is present. When legacy paths do exist, each\n+    must be named in a readable install stamp and its current bytes must match\n+    the recorded digest.\n+    \"\"\"\n+    if not force:\n+        return ()\n+    present = tuple(\n+        relative\n+        for relative in legacy_shiki_adr_paths()\n+        if (target / relative).exists() or (target / relative).is_symlink()\n+    )\n+    if not present:\n+        return ()\n+\n+    stamp_path = target / INSTALL_STAMP_PATH\n+    try:\n+        raw = stamp_path.read_text(encoding=\"utf-8\")\n+        stamp = json.loads(raw)\n+    except FileNotFoundError:\n+        detail = \"install stamp is absent\"\n+        blockers = [f\"{relative}: {detail}\" for relative in present]\n+    except (OSError, UnicodeDecodeError) as error:\n+        detail = f\"install stamp is unreadable ({error})\"\n+        blockers = [f\"{relative}: {detail}\" for relative in present]\n+    except json.JSONDecodeError as error:\n+        detail = f\"install stamp is malformed ({error})\"\n+        blockers = [f\"{relative}: {detail}\" for relative in present]\n+    else:\n+        if not isinstance(stamp, dict):\n+            blockers = [\n+                f\"{relative}: install stamp is malformed (expected an object)\"\n+                for relative in present\n+            ]\n+        else:\n+            digests = stamp.get(\"digests\")\n+            blockers = []\n+            if not isinstance(digests, dict):\n+                blockers.extend(\n+                    f\"{relative}: install stamp has no digest for this path\"\n+                    for relative in present\n+                )\n+            else:\n+                for relative in present:\n+                    expected = digests.get(relative)\n+                    if not isinstance(expected, str) or not expected:\n+                        blockers.append(\n+                            f\"{relative}: install stamp has no digest for this path\"\n+                        )\n+                        continue\n+                    try:\n+                        actual = _sha256_file(target / relative)\n+                    except OSError as error:\n+                        blockers.append(\n+                            f\"{relative}: legacy path is unreadable ({error})\"\n+                        )\n+                        continue\n+                    if actual != expected:\n+                        blockers.append(\n+                            f\"{relative}: digest mismatch (stamp {expected}, current {actual})\"\n+                        )\n+\n+    if blockers:\n+        joined = \"\\n\".join(f\"- {blocker}\" for blocker in blockers)\n+        raise ShikiError(\n+            \"legacy Shiki ADR cleanup is not authorized:\\n\"\n+            f\"{joined}\\nNo files were written.\"\n+        )\n+    return present\n+\n+\n def install_template(target: Path, *, force: bool, validate: bool) -> list[NewFileNote]:\n     # Preconditions first: never rewrite the tree of a target that cannot be\n     # upgraded cleanly (see refuse_pending_migrations).\n+    legacy_cleanup = preflight_legacy_sadr_cleanup(target, force=force)\n     refuse_pending_migrations(target)\n \n     # Resolve the target's required CODEOWNERS owner once from its\n@@ -250,6 +340,10 @@\n     # verbatim. See ``codeowners_required_owner``.\n     codeowners_owner = codeowners_required_owner(target)\n \n+    for relative in legacy_cleanup:\n+        (target / relative).unlink()\n+        info(f\"removed stamped legacy Shiki ADR: {target / relative}\")\n+\n     notes: list[NewFileNote] = []\n     for relative in TEMPLATE_PATHS:\n         source = ROOT / relative\n@@ -647,6 +741,22 @@\n     digests = target_stamp_digests(target)\n     existing = read_install_stamp(target)\n     if existing is not None:\n+        # A non-force install deliberately leaves legacy Shiki ADRs in place.\n+        # Preserve only still-matching ownership proof so a later forced\n+        # upgrade can authorize cleanup; never carry a missing or stale digest.\n+        existing_digests = existing.get(\"digests\")\n+        if isinstance(existing_digests, dict):\n+            for relative in legacy_shiki_adr_paths():\n+                expected = existing_digests.get(relative)\n+                path = target / relative\n+                if not isinstance(expected, str) or not path.is_file():\n+                    continue\n+                try:\n+                    actual = _sha256_file(path)\n+                except OSError:\n+                    continue\n+                if actual == expected:\n+                    digests[relative] = actual\n         if _incoming_is_older(commit, existing.get(\"platform_commit\")):\n             warn(\n                 \"kept existing install stamp: incoming install \"\ndiff --git a/scripts/shiki_loop.py b/scripts/shiki_loop.py\nindex 3611641da8aeb5038b1da90e3114b09a473ec6d8..84caa491b9cad0edc4176f209c13e9221e975681\n--- a/scripts/shiki_loop.py\n+++ b/scripts/shiki_loop.py\n@@ -424,7 +424,7 @@\n         # unresolvable CCA verdict has already produced its terminal stop and is never\n         # laundered into a head-moving sync (SADR-0010). It also never fires while the\n         # policy (Guardian) gate is red \u2014 for ANY reason, not only when it is the sole\n-        # red check \u2014 so a head-bound Guardian approval, or an SADR-0015\n+        # red check \u2014 so a head-bound Guardian approval, or a SADR-0015\n         # contract-approval carry that base movement defeated (the policy gate compares\n         # base and head task governance directly), is never silently restored by\n         # adopting base wholesale. It is bounded by a durable per-task counter so the\ndiff --git a/scripts/shiki_tasks.py b/scripts/shiki_tasks.py\nindex aec3809f24b4a0c8c96fc50971cff9ae9ad30381..d531052e9d9717058d4631483cefab44c26a65c1\n--- a/scripts/shiki_tasks.py\n+++ b/scripts/shiki_tasks.py\n@@ -821,7 +821,7 @@\n         \"prompt\": prompt,\n         \"entry_skill\": \"grill-with-docs\",\n         \"required_next_steps\": [\n-            \"Run grill-with-docs until terminology, boundaries, risks, and ADR-worthy decisions are settled.\",\n+            \"Run grill-with-docs until terminology, boundaries, risks, and decision-record-worthy choices are settled.\",\n             \"Obtain the operator's explicit approval of the PRD/requirements (Spec Freeze).\",\n             \"Write a machine-readable plan JSON with grill_with_docs.status=complete and spec_freeze.status=frozen.\",\n             \"Run shiki plan ingest --plan-file PLAN.json.\",\ndiff --git a/scripts/test_shiki_goal_loop.sh b/scripts/test_shiki_goal_loop.sh\nindex 753c7b516e10f0be3bc92a4bd0eb1fa699776ae6..8f3820a0a079b3044424cfe1a6c6b7fe68896e67\n--- a/scripts/test_shiki_goal_loop.sh\n+++ b/scripts/test_shiki_goal_loop.sh\n@@ -231,8 +231,8 @@\n \n # High/critical risk merges autonomously when all required checks are green:\n # the \"MergeGate policy check\" required check IS the Guardian gate (it enforces\n-# guardian-policy.json \u2014 human approval OR external AI guardian review, ADR\n-# 0010), so green checks mean a recorded authority approved. The fake gh returns\n+# guardian-policy.json \u2014 human approval OR external AI guardian review,\n+# SADR-0010), so green checks mean a recorded authority approved. The fake gh returns\n # green checks, simulating that the Guardian gate passed.\n python3 \"$ROOT/scripts/shiki.py\" goal create --target \"$TARGET\" --title \"Guardian gate\" --outcome \"High risk merges via policy gate\" >/tmp/shiki-goal-loop-guardian-goal.json\n GGOAL=\"$(json_get /tmp/shiki-goal-loop-guardian-goal.json goal_id)\"\ndiff --git a/tests/test_adr_references.py b/tests/test_adr_references.py\nindex 51706329c797d5dd9b47e082a333fa83b261d482..d005da4f771f5e8085ec22d4b787980e68f5ae8c\n--- a/tests/test_adr_references.py\n+++ b/tests/test_adr_references.py\n@@ -1,8 +1,8 @@\n-\"\"\"Every ``ADR ####`` citation must resolve to exactly one decision record.\n+\"\"\"Every target ADR and Shiki SADR citation resolves in its own namespace.\n \n Background. ``scripts/mergegate_check.py`` justifies ``is_bookkeeping_closeout``\n-with \"ADR 0017\" nine times and ``scripts/guardian_approval_signal.py`` cites\n-0016/0017 seven times, yet for a stretch the numbering in ``docs/adr/`` stopped\n+with \"SADR-0017\" nine times and ``scripts/guardian_approval_signal.py`` cites\n+SADR-0016/SADR-0017 seven times, yet for a stretch the numbering in ``docs/adr/`` stopped\n at 0015: the authoring commits lived on a coordinator branch and no task ever\n declared ``docs/adr/...`` in its locks, so nothing carried the records to the\n default branch. The stated basis of an exemption that merged seven PRs with zero\n@@ -11,11 +11,12 @@\n and the directory drifted with no signal.\n \n This is the same unbound-pair shape behind other silent failures: a reference\n-and its target with nothing asserting they agree. This test binds them. It scans\n-every ``scripts/*.py`` (plus ``docs/**/*.md`` and the root ``*.md`` governance\n-files, which are cheap) for the ``ADR ####`` pattern and asserts each cited\n-number resolves to exactly one ``docs/adr/####-*.md``. The converse is\n-deliberately NOT required: an ADR may exist without being cited.\n+and its target with nothing asserting they agree. This test binds them. Target\n+``ADR NNNN`` citations resolve only to ``docs/adr/NNNN-*.md``; Shiki\n+``SADR-NNNN`` citations resolve only to ``docs/adr/SADR-NNNN-*.md``. The scan\n+covers current governance docs, scripts, workflows, prompts, packaging metadata,\n+and repo-local Codex guidance while deliberately excluding historical\n+``.shiki`` evidence. The converse is not required: a record may be uncited.\n \n The suite is dependency free: standard-library ``unittest`` only.\n \"\"\"\n@@ -31,17 +32,18 @@\n \n REPO_ROOT = Path(__file__).resolve().parents[1]\n \n-# \"ADR 0016\" -- a four-digit citation. The trailing \\b keeps a five-digit token\n-# from being read as a truncated four-digit number.\n-_CITATION = re.compile(r\"\\bADR (\\d{4})\\b\")\n-# \"0016-locks-bound-product-paths.md\" -- the leading four digits name the record.\n-_ADR_FILENAME = re.compile(r\"^(\\d{4})-.*\\.md$\")\n+# ``ADR 0016`` / ``SADR-0016`` -- a namespace plus four-digit citation. The\n+# trailing ``\\b`` keeps a five-digit token from being read as a truncated one.\n+_CITATION = re.compile(r\"\\b(S?ADR)[ -](\\d{4})\\b\")\n+# Target ADRs retain the numeric filename; Shiki platform decisions use SADR.\n+_DECISION_FILENAME = re.compile(r\"^(?:(SADR)-)?(\\d{4})-.*\\.md$\")\n \n \n class Violation(NamedTuple):\n     \"\"\"A citation that does not resolve to exactly one decision record.\"\"\"\n \n     citing_file: str  # repo-relative path of the file that cites the ADR\n+    namespace: str  # \"ADR\" for target decisions; \"SADR\" for Shiki decisions\n     number: str  # the cited four-digit number, e.g. \"0016\"\n     line: int  # 1-based line of the citation\n     reason: str  # \"missing\" (zero matches) or \"ambiguous\" (two or more)\n@@ -49,44 +51,59 @@\n \n     @property\n     def expected_path(self) -> str:\n-        return f\"docs/adr/{self.number}-*.md\"\n+        prefix = \"SADR-\" if self.namespace == \"SADR\" else \"\"\n+        return f\"docs/adr/{prefix}{self.number}-*.md\"\n \n     def describe(self) -> str:\n         \"\"\"Name the citing file, the cited number, and the missing path.\"\"\"\n         if self.reason == \"missing\":\n             return (\n-                f\"{self.citing_file}:{self.line} cites ADR {self.number} \"\n+                f\"{self.citing_file}:{self.line} cites {self.namespace} {self.number} \"\n                 f\"but no document matches {self.expected_path}\"\n             )\n         joined = \", \".join(self.matches)\n         return (\n-            f\"{self.citing_file}:{self.line} cites ADR {self.number} which \"\n+            f\"{self.citing_file}:{self.line} cites {self.namespace} {self.number} which \"\n             f\"resolves to {len(self.matches)} documents (expected exactly one \"\n             f\"at {self.expected_path}): {joined}\"\n         )\n \n \n-def _adr_index(root: Path) -> dict[str, list[Path]]:\n-    \"\"\"Map every four-digit ADR number to the record files that claim it.\"\"\"\n-    index: dict[str, list[Path]] = {}\n+def _adr_index(root: Path) -> dict[tuple[str, str], list[Path]]:\n+    \"\"\"Map each decision namespace and number to the files that claim it.\"\"\"\n+    index: dict[tuple[str, str], list[Path]] = {}\n     adr_dir = root / \"docs\" / \"adr\"\n     if adr_dir.is_dir():\n         for path in sorted(adr_dir.glob(\"*.md\")):\n-            match = _ADR_FILENAME.match(path.name)\n+            match = _DECISION_FILENAME.match(path.name)\n             if match:\n-                index.setdefault(match.group(1), []).append(path)\n+                namespace = \"SADR\" if match.group(1) else \"ADR\"\n+                index.setdefault((namespace, match.group(2)), []).append(path)\n     return index\n \n \n def _citing_files(root: Path, include_docs: bool = True) -> list[Path]:\n-    \"\"\"Files scanned for citations: scripts/*.py, and cheaply the markdown.\"\"\"\n+    \"\"\"Current, shipped text surfaces scanned for decision citations.\"\"\"\n     files = sorted((root / \"scripts\").glob(\"*.py\"))\n+    files += sorted((root / \"scripts\").glob(\"*.sh\"))\n     if include_docs:\n         docs = root / \"docs\"\n         if docs.is_dir():\n             files += sorted(docs.rglob(\"*.md\"))\n         files += sorted(root.glob(\"*.md\"))\n-    return files\n+        pyproject = root / \"pyproject.toml\"\n+        if pyproject.is_file():\n+            files.append(pyproject)\n+        for relative in (\".github\", \".codex\"):\n+            surface = root / relative\n+            if surface.is_dir():\n+                files += sorted(\n+                    path\n+                    for path in surface.rglob(\"*\")\n+                    if path.is_file()\n+                    and path.suffix in {\".md\", \".py\", \".sh\", \".toml\", \".yaml\", \".yml\"}\n+                )\n+    return sorted(set(files))\n \n \n def find_adr_reference_violations(\n@@ -94,10 +111,9 @@\n ) -> list[Violation]:\n     \"\"\"Return every ``ADR ####`` citation under ``root`` that fails to resolve.\n \n-    A citation resolves when exactly one ``docs/adr/####-*.md`` matches its\n-    number. Zero matches is a \"missing\" violation; two or more is \"ambiguous\".\n-    Records that are never cited produce nothing -- the converse is not\n-    required.\n+    A citation resolves when exactly one record in the same namespace matches\n+    its number. Zero matches is a \"missing\" violation; two or more is\n+    \"ambiguous\". Records that are never cited produce nothing.\n     \"\"\"\n     root = Path(root)\n     index = _adr_index(root)\n@@ -110,13 +126,15 @@\n         rel = path.relative_to(root).as_posix()\n         for lineno, line in enumerate(text.splitlines(), start=1):\n             for match in _CITATION.finditer(line):\n-                number = match.group(1)\n-                found = index.get(number, [])\n+                namespace, number = match.groups()\n+                found = index.get((namespace, number), [])\n                 if len(found) == 1:\n                     continue\n                 reason = \"missing\" if not found else \"ambiguous\"\n                 rels = [p.relative_to(root).as_posix() for p in found]\n-                violations.append(Violation(rel, number, lineno, reason, rels))\n+                violations.append(\n+                    Violation(rel, namespace, number, lineno, reason, rels)\n+                )\n     return violations\n \n \n@@ -129,8 +147,8 @@\n         self.assertEqual(\n             violations,\n             [],\n-            \"Every 'ADR ####' citation must resolve to exactly one \"\n-            f\"docs/adr/####-*.md:\\n{detail}\",\n+            \"Every ADR/SADR citation must resolve exactly once in its own \"\n+            f\"docs/adr namespace:\\n{detail}\",\n         )\n \n     def test_scan_actually_reads_scripts(self) -> None:\n@@ -142,12 +160,38 @@\n             \"the citation scan must cover scripts/*.py\",\n         )\n \n+    def test_platform_citations_do_not_use_unresolved_numeric_shorthand(self) -> None:\n+        shorthand = re.compile(r\"SADR-\\d{4}/\\d{4}\")\n+        offenders = []\n+        for path in _citing_files(REPO_ROOT):\n+            text = path.read_text(encoding=\"utf-8\")\n+            if shorthand.search(text):\n+                offenders.append(path.relative_to(REPO_ROOT).as_posix())\n+        self.assertEqual(\n+            offenders,\n+            [],\n+            \"repeat the SADR prefix so every cited number is independently resolvable\",\n+        )\n+\n     def test_carried_records_are_indexed(self) -> None:\n         # The two records this Goal carried onto the default branch, and which\n         # the scripts cite most, must be present.\n         index = _adr_index(REPO_ROOT)\n-        self.assertIn(\"0016\", index)\n-        self.assertIn(\"0017\", index)\n+        self.assertIn((\"SADR\", \"0016\"), index)\n+        self.assertIn((\"SADR\", \"0017\"), index)\n+\n+    def test_platform_tree_has_only_sadr_0001_through_0019(self) -> None:\n+        adr_dir = REPO_ROOT / \"docs\" / \"adr\"\n+        sadrs = sorted(adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\"))\n+        self.assertEqual(\n+            {path.name.split(\"-\", 2)[1] for path in sadrs},\n+            {f\"{number:04d}\" for number in range(1, 20)},\n+        )\n+        self.assertEqual(\n+            list(adr_dir.glob(\"[0-9][0-9][0-9][0-9]-*.md\")),\n+            [],\n+            \"the Shiki platform tree must not retain legacy numeric SADR paths\",\n+        )\n \n \n class FixtureAdrReferences(unittest.TestCase):\n@@ -180,6 +224,26 @@\n         self.assertIn(\"9991\", message)  # cited number\n         self.assertIn(\"docs/adr/9991-*.md\", message)  # missing path\n \n+    def test_target_adr_does_not_satisfy_missing_platform_sadr(self) -> None:\n+        root = self._make_repo(\n+            adr_names=[\"0042-target-decision.md\"],\n+            script_body='\"\"\"Target ADR 0042 exists, but platform SADR-0042 does not.\"\"\"\\n',\n+        )\n+        violations = find_adr_reference_violations(root, include_docs=False)\n+        self.assertEqual(len(violations), 1)\n+        self.assertEqual(violations[0].namespace, \"SADR\")\n+        self.assertEqual(violations[0].number, \"0042\")\n+        self.assertEqual(violations[0].expected_path, \"docs/adr/SADR-0042-*.md\")\n+\n+    def test_target_adr_and_platform_sadr_may_reuse_a_number(self) -> None:\n+        root = self._make_repo(\n+            adr_names=[\"0042-target-decision.md\", \"SADR-0042-platform-decision.md\"],\n+            script_body='\"\"\"Target ADR 0042 and platform SADR-0042 are distinct.\"\"\"\\n',\n+        )\n+        self.assertEqual(\n+            find_adr_reference_violations(root, include_docs=False), []\n+        )\n+\n     def test_uncited_adr_is_allowed(self) -> None:\n         # An ADR exists but nothing cites it -> not a violation. The converse\n         # (every ADR must be cited) is explicitly NOT required.\ndiff --git a/tests/test_guardian_review.py b/tests/test_guardian_review.py\nindex 88829e7810212635e2fc98aa5918d6f26294677d..00a85a237acd5bf2d6cb5b294128198c422402ca\n--- a/tests/test_guardian_review.py\n+++ b/tests/test_guardian_review.py\n@@ -162,7 +162,7 @@\n \n     def test_docs_only_is_light_and_not_conservative(self):\n         focus = gr.classify_review_focus([\"docs/adr/0099-x.md\", \"README.md\"], \"\", \"low\")\n-        self.assertTrue(any(\"Docs/ADR-only\" in f for f in focus))\n+        self.assertTrue(any(\"Docs/decision-record-only\" in f for f in focus))\n         self.assertFalse(any(\"conservative default\" in f.lower() for f in focus))\n \n     def test_unknown_category_is_conservative(self):\ndiff --git a/tests/test_policy_job_merged_prs.py b/tests/test_policy_job_merged_prs.py\nindex d45512e74681e5e649279236c7d4e79d5c3da471..b97989f8bd74d091de1a6b5275a20d8dc68c4512\n--- a/tests/test_policy_job_merged_prs.py\n+++ b/tests/test_policy_job_merged_prs.py\n@@ -3,8 +3,8 @@\n \n ``.github/workflows/shiki-cca-completion.yml`` has two consumers of the merged-PR\n proof: the CCA *signal* (``guardian_approval_signal.py`` in \"Prepare CCA context\")\n-and the authoritative gate (``mergegate_check.py`` in \"Enforce MergeGate\"). ADR\n-0017's bookkeeping-closeout exemption (condition 6) needs the merged implementation\n+and the authoritative gate (``mergegate_check.py`` in \"Enforce MergeGate\").\n+SADR-0017's bookkeeping-closeout exemption (condition 6) needs the merged implementation\n PR proven, and a closeout carries NO ``post_merge_reconcile`` marker. The signal\n already resolves ``MERGED_PRS`` unconditionally, but \"Enforce MergeGate\" used to\n resolve it only inside ``grep -q '<!-- shiki:post_merge_reconcile -->'``. So a\ndiff --git a/tests/test_sadr_install_migration.py b/tests/test_sadr_install_migration.py\nnew file mode 100644\nindex 0000000000000000000000000000000000000000..d51eac872b1c79ff4579c4c1401ca5d775a60d8d\n--- /dev/null\n+++ b/tests/test_sadr_install_migration.py\n@@ -0,0 +1,237 @@\n+\"\"\"SADR installation and collision-safe legacy upgrade behavior.\"\"\"\n+\n+from __future__ import annotations\n+\n+import contextlib\n+import hashlib\n+import io\n+import json\n+import shutil\n+import tempfile\n+import unittest\n+from pathlib import Path\n+\n+import shiki_test_support  # noqa: F401  (path bootstrap)\n+\n+import shiki_installer\n+from tests.test_adr_references import find_adr_reference_violations\n+\n+\n+PRODUCT_NUMBERS = tuple(range(13, 19))\n+\n+\n+def _quiet_install(target: Path, *, force: bool, validate: bool) -> None:\n+    with contextlib.redirect_stdout(io.StringIO()):\n+        shiki_installer.install_template(target, force=force, validate=validate)\n+\n+\n+def _sha256(path: Path) -> str:\n+    return hashlib.sha256(path.read_bytes()).hexdigest()\n+\n+\n+def _legacy_sadr_paths() -> tuple[str, ...]:\n+    return tuple(\n+        f\"docs/adr/{path.name.removeprefix('SADR-')}\"\n+        for path in sorted((shiki_installer.ROOT / \"docs\" / \"adr\").glob(\"SADR-*.md\"))\n+        if path.name != \"SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\"\n+    )\n+\n+\n+def _create_legacy_upgrade_fixture(\n+    target: Path,\n+) -> tuple[dict[str, bytes], tuple[str, ...]]:\n+    adr_dir = target / \"docs\" / \"adr\"\n+    adr_dir.mkdir(parents=True)\n+    product_bytes = {\n+        f\"{number:04d}-cubeshare-product-decision.md\": (\n+            f\"# CubeShare ADR {number:04d}\\n\\nDo not rewrite {number}.\\n\"\n+        ).encode()\n+        for number in PRODUCT_NUMBERS\n+    }\n+    for name, content in product_bytes.items():\n+        (adr_dir / name).write_bytes(content)\n+\n+    legacy_paths = _legacy_sadr_paths()\n+    for relative in legacy_paths:\n+        (target / relative).write_bytes(f\"legacy Shiki bytes for {relative}\\n\".encode())\n+\n+    stamp_path = target / shiki_installer.INSTALL_STAMP_PATH\n+    stamp_path.parent.mkdir(parents=True, exist_ok=True)\n+    stamp = {\n+        \"version\": shiki_installer.INSTALL_STAMP_VERSION,\n+        \"platform_commit\": None,\n+        \"installed_at\": \"2000-01-01T00:00:00+00:00\",\n+        \"digests\": {\n+            relative: _sha256(target / relative) for relative in legacy_paths\n+        },\n+    }\n+    stamp_path.write_text(\n+        json.dumps(stamp, indent=2, sort_keys=True) + \"\\n\", encoding=\"utf-8\"\n+    )\n+    return product_bytes, legacy_paths\n+\n+\n+def _tree_snapshot(root: Path) -> dict[str, tuple[str, bytes | str]]:\n+    \"\"\"Recursive byte/shape snapshot used to prove preflight is write-free.\"\"\"\n+    snapshot: dict[str, tuple[str, bytes | str]] = {}\n+    for path in sorted(root.rglob(\"*\")):\n+        relative = path.relative_to(root).as_posix()\n+        if path.is_symlink():\n+            snapshot[relative] = (\"symlink\", path.readlink().as_posix())\n+        elif path.is_dir():\n+            snapshot[relative] = (\"directory\", \"\")\n+        else:\n+            snapshot[relative] = (\"file\", path.read_bytes())\n+    return snapshot\n+\n+\n+class FreshSadrInstallTests(unittest.TestCase):\n+    def setUp(self) -> None:\n+        self.tmp = Path(tempfile.mkdtemp(prefix=\"shiki-sadr-fresh-\"))\n+        self.addCleanup(lambda: shutil.rmtree(self.tmp, ignore_errors=True))\n+        self.target = self.tmp / \"target\"\n+        self.adr_dir = self.target / \"docs\" / \"adr\"\n+        self.adr_dir.mkdir(parents=True)\n+        self.product_bytes = {\n+            f\"{number:04d}-product-decision.md\": (\n+                f\"# Product ADR {number:04d}\\n\\nTarget-owned bytes {number}.\\n\"\n+            ).encode()\n+            for number in PRODUCT_NUMBERS\n+        }\n+        for name, content in self.product_bytes.items():\n+            (self.adr_dir / name).write_bytes(content)\n+\n+    def test_fresh_install_preserves_product_adrs_and_installs_all_sadrs(self) -> None:\n+        _quiet_install(self.target, force=False, validate=True)\n+\n+        for name, expected in self.product_bytes.items():\n+            self.assertEqual((self.adr_dir / name).read_bytes(), expected)\n+\n+        sadr_numbers = {\n+            path.name.split(\"-\", 2)[1]\n+            for path in self.adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\")\n+        }\n+        self.assertEqual(sadr_numbers, {f\"{number:04d}\" for number in range(1, 20)})\n+\n+        numeric_records = {\n+            path.name\n+            for path in self.adr_dir.glob(\"[0-9][0-9][0-9][0-9]-*.md\")\n+        }\n+        self.assertEqual(numeric_records, set(self.product_bytes))\n+        stamp = json.loads(\n+            (self.target / shiki_installer.INSTALL_STAMP_PATH).read_text(\n+                encoding=\"utf-8\"\n+            )\n+        )\n+        for path in self.adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\"):\n+            relative = path.relative_to(self.target).as_posix()\n+            self.assertEqual(stamp[\"digests\"][relative], _sha256(path))\n+        self.assertEqual(find_adr_reference_violations(self.target), [])\n+\n+\n+class LegacySadrUpgradeTests(unittest.TestCase):\n+    def setUp(self) -> None:\n+        self.tmp = Path(tempfile.mkdtemp(prefix=\"shiki-sadr-upgrade-\"))\n+        self.addCleanup(lambda: shutil.rmtree(self.tmp, ignore_errors=True))\n+        self.target = self.tmp / \"target\"\n+        self.adr_dir = self.target / \"docs\" / \"adr\"\n+        self.product_bytes, self.legacy_paths = _create_legacy_upgrade_fixture(\n+            self.target\n+        )\n+\n+    def test_force_upgrade_deletes_only_stamped_legacy_sadrs(self) -> None:\n+        _quiet_install(self.target, force=True, validate=True)\n+\n+        for relative in self.legacy_paths:\n+            self.assertFalse((self.target / relative).exists(), relative)\n+        for name, expected in self.product_bytes.items():\n+            self.assertEqual((self.adr_dir / name).read_bytes(), expected)\n+\n+        sadrs = list(self.adr_dir.glob(\"SADR-[0-9][0-9][0-9][0-9]-*.md\"))\n+        self.assertEqual(len(sadrs), 19)\n+        refreshed = json.loads(\n+            (self.target / shiki_installer.INSTALL_STAMP_PATH).read_text(encoding=\"utf-8\")\n+        )\n+        for relative in self.legacy_paths:\n+            self.assertNotIn(relative, refreshed[\"digests\"])\n+        for path in sadrs:\n+            relative = path.relative_to(self.target).as_posix()\n+            self.assertEqual(refreshed[\"digests\"][relative], _sha256(path))\n+\n+    def test_non_force_install_never_deletes_legacy_files(self) -> None:\n+        before = {\n+            relative: (self.target / relative).read_bytes()\n+            for relative in self.legacy_paths\n+        }\n+        _quiet_install(self.target, force=False, validate=False)\n+        for relative, expected in before.items():\n+            self.assertEqual((self.target / relative).read_bytes(), expected)\n+        refreshed = json.loads(\n+            (self.target / shiki_installer.INSTALL_STAMP_PATH).read_text(encoding=\"utf-8\")\n+        )\n+        for relative in self.legacy_paths:\n+            self.assertEqual(refreshed[\"digests\"][relative], _sha256(self.target / relative))\n+\n+\n+class LegacyCleanupPreflightTests(unittest.TestCase):\n+    def setUp(self) -> None:\n+        self.tmp = Path(tempfile.mkdtemp(prefix=\"shiki-sadr-preflight-\"))\n+        self.addCleanup(lambda: shutil.rmtree(self.tmp, ignore_errors=True))\n+\n+    def test_all_ownership_blockers_abort_before_any_write(self) -> None:\n+        cases = (\n+            (\"absent\", \"install stamp is absent\"),\n+            (\"unreadable\", \"install stamp is unreadable\"),\n+            (\"malformed\", \"install stamp is malformed\"),\n+            (\"missing-digest\", \"install stamp has no digest\"),\n+            (\"digest-mismatch\", \"digest mismatch\"),\n+        )\n+        for name, expected_reason in cases:\n+            with self.subTest(name=name):\n+                target = self.tmp / name\n+                _, legacy_paths = _create_legacy_upgrade_fixture(target)\n+                stamp_path = target / shiki_installer.INSTALL_STAMP_PATH\n+\n+                if name == \"absent\":\n+                    stamp_path.unlink()\n+                elif name == \"unreadable\":\n+                    stamp_path.unlink()\n+                    stamp_path.mkdir()\n+                elif name == \"malformed\":\n+                    stamp_path.write_text(\"{not-json\\n\", encoding=\"utf-8\")\n+                else:\n+                    stamp = json.loads(stamp_path.read_text(encoding=\"utf-8\"))\n+                    if name == \"missing-digest\":\n+                        stamp[\"digests\"] = {}\n+                    else:\n+                        stamp[\"digests\"] = {\n+                            relative: \"deadbeef\" for relative in legacy_paths\n+                        }\n+                    stamp_path.write_text(\n+                        json.dumps(stamp, indent=2, sort_keys=True) + \"\\n\",\n+                        encoding=\"utf-8\",\n+                    )\n+\n+                before = _tree_snapshot(target)\n+                with self.assertRaises(shiki_installer.ShikiError) as raised:\n+                    _quiet_install(target, force=True, validate=False)\n+                after = _tree_snapshot(target)\n+\n+                self.assertEqual(before, after, f\"{name} preflight changed the tree\")\n+                message = str(raised.exception)\n+                self.assertIn(expected_reason, message)\n+                self.assertIn(\"No files were written\", message)\n+                for relative in legacy_paths:\n+                    self.assertIn(relative, message)\n+\n+    def test_absent_stamp_is_allowed_when_no_legacy_path_exists(self) -> None:\n+        target = self.tmp / \"no-legacy\"\n+        target.mkdir()\n+        _quiet_install(target, force=True, validate=False)\n+        self.assertTrue(\n+            (target / \"docs\" / \"adr\" / \"SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md\").is_file()\n+        )\n+\n+\n+if __name__ == \"__main__\":\n+    unittest.main()\n\n2026-08-07T08:29:58.509149Z  WARN codex_mcp::rmcp_client: failed to initialize MCP client during shutdown: MCP startup failed: handshaking with MCP server failed: connection closed: initialize response\n2026-08-07T08:29:58.509312Z  WARN codex_mcp::rmcp_client: failed to initialize MCP client during shutdown: MCP startup failed: handshaking with MCP server failed: Send message error Transport [rmcp::transport::worker::WorkerTransport<rmcp::transport::streamable_http_client::StreamableHttpClientWorker<rmcp::transport::auth::AuthClient<codex_rmcp_client::http_client_adapter::StreamableHttpClientAdapter>>>] error: Auth error: OAuth authorization required, when send initialize request\ntokens used\n621,419\n",
  "stdout": "Implemented locally in the assigned worktree.\n\n- Renamed platform records to SADR-0001\u20130018 and added [SADR-0019](/private/tmp/shiki-adr-collision.pa0KwY/.worktrees/shiki-t-20260807t073307221254z-2d22d654-introduc/docs/adr/SADR-0019-separate-platform-sadr-and-target-adr-namespaces.md:1).\n- Added independent ADR/SADR resolution and collision fixtures in [test_adr_references.py](/private/tmp/shiki-adr-collision.pa0KwY/.worktrees/shiki-t-20260807t073307221254z-2d22d654-introduc/tests/test_adr_references.py:35).\n- Added digest-authorized, pre-write legacy cleanup in [shiki_installer.py](/private/tmp/shiki-adr-collision.pa0KwY/.worktrees/shiki-t-20260807t073307221254z-2d22d654-introduc/scripts/shiki_installer.py:241).\n- Added fresh-install, collision-upgrade, failure-atomicity, and non-force coverage in [test_sadr_install_migration.py](/private/tmp/shiki-adr-collision.pa0KwY/.worktrees/shiki-t-20260807t073307221254z-2d22d654-introduc/tests/test_sadr_install_migration.py:104).\n- Recorded TDD and structured implementer code-review evidence. One test-evidence finding was fixed and re-reviewed clean.\n\nLocal verification:\n\n- 1,147 unit tests passed.\n- Control-plane suite passed.\n- Adversarial suite passed.\n- `validate_shiki` and `git diff --check` passed.\n\nNo commit, push, `gh` command, PR, or approval record was created. Exact-head CI, independent loop review, CCA, Guardian approval, and MergeGate remain owned by the Shiki goal loop.\n",
  "task_id": "T-20260807T073307221254Z-2d22d654"
}
